# R — FINAL: the consummation tranche closes

**Tranche**: R (functional truth · the editorial instrument · the 2.0.0 cut)
**Branch**: `tranche-q` (main tree, single writer) · **Close-synthesis authored**: 2026-07-04 at `825bd58`
**Charter**: `R.md` · **Board**: `PROGRESS.md` · **Ratified spec**: `audit/pass2/SYNTHESIS-v2.md` (amended at pass 3; convergence 87.8/84 → 93.2/88 → **100/100**, `audit/pass2/PASS3-VERDICT.md`)
**Ratification**: 2026-07-03 owner pass — 6 rows ratified as speced (Q4/Q7/Q8/Q10/Q11/Q12), **2 FLIPPED** (Q1 hero-lab KILLED entirely; Q2 X2 NCSU-alias retirement in-wave, in the W7 gate). R.W5 KILLED — the wave-letter gap is kept deliberately as the record of the kill.

> **Amendment discipline**: the X1/X2/X3 wire rows below are recorded at their
> honest close-synthesis state (§1 W7 row + §7). The orchestrator fires the
> master merge → the Pages wire run → the prod deploy → the alias retirement,
> and **amends those rows in place**; nothing else in this document is
> provisional.

---

## §1 — Per-wave gate table (evidence-backed; misses carry named successors)

| Wave | Status | Gate evidence (close entry + artefacts) | Goal-criterion reconciliation | Misses → named successor |
|---|---|---|---|---|
| **R.W0** substrate | **complete** (2026-07-03) | 14/14 rows executed serially; per-row hash table in `PROGRESS.md §Verification artefacts (R.W0)` (`aace524..e68b720` + tag/merge/worktree git-ops); lint 0; vitest 1934/51 files at the merged tree; `git status` clean; master fast-forwarded `15b0382..e68b720`; 10 retro-tags == registry ≥ v0.6.0 (0 missing); 5 stale worktrees + 5 lane branches removed | Goal met: the substrate is honest — spec-of-record committed (`color-picker.md` + P1–P10 merged `8f1d8e7`), tag/merge debt healed, precepts submodule resolved by REVERT (superseded draft; no out-of-band lineage publish), doc-truth + §3.4 pin policy recorded | Two recorded live-tree drifts (merge depth 3→8→14; worktrees 5-not-3) — recorded per lesson 3, **not** misses; none open |
| **R.W1** gamut + perceptual → **2.0.0** | **complete** (2026-07-03) | Independent verifier **12/12 PASS**; **2.0.0 PUBLISHED** (`dist-tags.latest=2.0.0`, version commit `96f124d`, annotated `v2.0.0` pushed, merge `20bbc8d`); vitest 1996/1996 (56 files) · lint 0 · `tsc -p tsconfig.lib.json` 0; bench 0.13–0.18 ms mean (ceiling 0.5); GAMUT_ALPHA=1.0 with pink oracle + tiered guard (ΔL 0.0834 < 0.09); KF-1 5-file grammar fix; `extractFunctions` fresh-build `.d.ts` lock; boundary API + goldens @1e-3; OKHSL/OKHSV; ΔE-2000 (14 Sharma vectors) + ΔE-ITP; K-DISP 760→522 LoC barrels byte-identical; `/easing` 5-export guard; both letters dispatched inside the cut (kf `9a0f6cb`, fourier `cd26c65`) | Goal met: one honest 2.0.0 bundling every output-changing row (the keyframes-2.2.0 semver lesson applied) | `srgbToLinear` decode-threshold defect (discovered at R.W1.6, out of remit) → **BOOK** (next output-changing minor/major — tranche S receives it); `Color.try()` → **BOOK** (demand-triggered) |
| **R.W2** functional truth | **complete_with_misses** (2026-07-03) | Composite gate green: boot-smoke cold PASS · gh-pages built · hero-lab grep-zero · zero ungated rAF · zero phantom · lint 0 · typecheck 0 · vitest 1998/1998 (58 files); boot cure `0123648` (dist byte-identical); hero-lab deletion `9ed9175` (Q1 FLIP); Tabs migration + named-export tripwire `6ed3677` (born-RED on the 4 dead bindings, GREEN post); P0 rows `648843b..46ad669`; K-PALID landed `871c69f`; e2e 38/38 then 37/38 with the deviation's root eliminated `7c6abdd`; D8-1 recurrence escalated (glass-ui `1599c230`) and CURED same-day by BG (`4b637036`), probe **CURE_OBSERVED** — book RETIRED | **Gates pass, goal partially unmet — recorded honestly.** Two N.W10 rows' literal fixes were **REFUTED by triumvirate experiment**, not landed: K-INV5 (deletion-probe: the row's literal fix was wrong; the functional kernel landed via save-P0) and K-W3DIFF (REFUTED-AS-CONTRIVANCE: no diff-consumer surface exists; stored `atomDiff` is write-only). The adjudicated books ARE the honest record — `waves/R.W2.md §BOOKS` | K-INV5 residual → **R.W3, DISCHARGED** (`c4eb9d2` typed `ApiUnavailable` latch + "offline — saved locally" affordance; behavior verified in the W3 π close, browse section). K-W3DIFF → **trigger-bound BOOK** (first demo version-compare/remix-review surface; alt-exit = stop persisting `atomDiff`, api-side) |
| **R.W3** the instrument | **complete** (2026-07-04) | Verifier: all clauses PASS, **taste bar MET**; gate (a)–(f) + overlay riders GREEN (`audit/R.W3-visual-runtime/close/DELTA.md` gate ledger); e2e **38/38 first-run, all 5 projects**; lint 0 · typecheck 0 · vitest 1998/1998; both probes GREEN (keystone 3/3, instrument 6/6); perf 0.75 ms mean (<2 ms budget); close-blocker root-cured `fceed47` (PaneSlot `mode="out-in"` dev-only remount defect); gate-(f) provenance **repaired** `ad1ab93` — I1–I4 genuinely falsified (retroactive at `b2544c3`), I5/I2r honestly reclassified as standing guards, never falsified oracles | Goal met: the picker plate reads as the treatment's editorial instrument (verifier's words); K-INV5 residual discharged in-wave | GooBlob lg corner-BREAK reads intermittently (canvas anchor wander) — **taste residual, producer-side anchor bias** → carried on the relay-letter ledger (§8), not regressed |
| **R.W4** suffusion | **complete** (2026-07-04) | Verifier: gate (a)–(d) + easing riders + E-lane clauses ALL PASS, **taste MET** (screenshot-corroborated); `audit/R.W4-visual-runtime/close/DELTA.md`: 17→3 transition families (inventory at `transition-inventory.md`), both forks deleted grep-zero, demo/ ≤400 LoC over-cap 0 (`9675ef3`), a11y parity additive-only; `/easing` consume 24/24 names + Q12 steps banded round-trip (`probe-w4.mjs` 13/13); E4 raw-OKLab verdict bug caught in-lane and cured `337f254` (Δ was inflated ~500×); suites: vitest 1998/1998 · playwright 38/38 · lint 0 · vue-tsc 0 · boot cold PASS | Goal met: the keystone's grammar suffused to every surface; both remaining demo forks dead by consume, not workaround | EasingPicker preset SelectTrigger accessible-name defect (producer-owned; combobox announces only "linear") → **relay letter item** (§8; fires at the glass-ui fix) |
| ~~R.W5~~ | **KILLED** | Q1 FLIP at ratification (owner order) — `waves/R.W5.md` + `docs/frontend-design/hero-lab.md` deleted; app artifacts deleted at R.W2 `9ed9175` | Not a miss: killed, never slipped; the gap documents the kill | — |
| **R.W6** twin-tie | **complete** (2026-07-03) | 5 wire-envelope shape rows inline (`7351297`, merge `8a2a617`), transcribed against `J-diff-shape.md §3/§4`, shape-not-bytes; **isolated-clone proof**: no sibling checkout, diff rows 14/14 + full api suite 37 files / 224 green; `cd api && tsc --noEmit` 0; contract-currency invariant recorded (`waves/R.W6.md §2` + the W6 close entry); FN charter reconciled + FN-5-before-M.W10 rider delivered (fourier `cd26c65`) | Goal met: the twin-tie is durable inside value.js's own tree, zero cross-repo reads; `canonical_digest` SKIPPED **sanctioned** (adoption = stored-hash migration, not parity); the three named twin divergences left un-unified by design | FN-7 + FN-6 → **BOOKS** (fourier-owned, by design — not misses) |
| **R.W7** wire + close | **complete_with_misses** (2026-07-04; X3 GREEN + first-ever CI green; X1/X2 = maintainer-on-host residuals, §7) | X5+X4 landed `825bd58` (`docs/dev-deploy-standard.md §6/§7`); relay letter finalized + cascade-vjs response authored (this wave's letters commit); FINAL.md = this document; full verify set at HEAD: lint 0 · typecheck 0 (lib+demo, api) · vitest **1998/1998** (58 files) · playwright — §6; ι sweep clean (§3); π reconciliation clean (§4) | Goal reconciliation at the merge ceremony: **X1 prod deploy, X2 alias retirement, X3 Pages wire run, master merge + tag are orchestrator/maintainer acts that fire AT/AFTER the merge** — recorded PENDING below (§7) with live-probe evidence; the close-synthesis half is complete | X1 → PENDING-ON-MERGE · X2 → PENDING (maintainer-on-host) · X3 → PENDING-ON-MERGE — all three inside this wave's own ceremony, amended here when fired; no cross-tranche deferral |

---

## §2 — The §10 zero-drop fold ledger, reconciled row by row

Every `R.md §10` row → its landing or its book. Nothing silently dropped.

| §10 row | Verdict | Evidence |
|---|---|---|
| W0-1..W0-14 → R.W0 | **LANDED** | Per-row hash table, `PROGRESS.md §Verification artefacts (R.W0)`: `aace524` `8f1d8e7` `6ce7236` `de6428d` `cb986e6` `e68b720` + tags ×10 + ff-merge + worktree cleanup; W0-7 REVERT recorded with rationale; keyframes devDep KEEP recorded |
| R.W1 slate (U10/Q7 · KF-1 5-file · extractFunctions `.d.ts` guard · bezierPresets rows · boundary API+Into+goldens · OKHSL/OKHSV · ΔE · K-DISP · `/easing` guard · R8-24 closed-verified · 2.0.0 + letters) → R.W1 | **LANDED** | W1 close entry (verifier 12/12); publish `96f124d`/`v2.0.0`/merge `20bbc8d`; letters kf `9a0f6cb` + fourier `cd26c65`; R8-24 verified shipped in 1.2.0 — closed |
| R.W2 slate (boot fix · Tabs migration · named-export tripwire · dual-pane internal confirm · U9 · U33 · mix-RAF PRM · watercolor consume-or-delete · X6/X8/X9 · kill-list · K-W3DIFF/K-PALID/K-INV5 · save-P0/kC · hero-lab artifact deletion) → R.W2 | **LANDED / ADJUDICATED** | `0123648` · `6ed3677` · `9ed9175` · `648843b..46ad669` · K-PALID `871c69f`; K-INV5 → discharged at R.W3 `c4eb9d2`; K-W3DIFF → trigger-bound BOOK (§5); dual-pane external no-shim was the D8-1 book — RETIRED (CURE_OBSERVED) |
| R.W3 slate (picker-bearing U-rows · U8 · ComponentSliders lift · O.W7 gamut-truth half · overlay signature · Q11 lens) → R.W3 | **LANDED** | `0cbef49..c4eb9d2`; overlay riders GREEN (`lane-b/EVIDENCE.md`); ComponentSliders 419→367 (probe I4: 368 incl. close state); Q11 lens live with keyed override + caption |
| R.W4 slate (card/shell/pane U-rows · U27-consume via `/easing` · T19/T20/T21 · O.W7 Parse-Lab half/Q10 · glass-ui §5 self-owed retirements) → R.W4 | **LANDED** | `4d8ad79..9675ef3`; 24/24 names + §1.4 substitution record cited; T19/T20/T21 at `65ba2c6`; forks dead grep-zero (`54f1353`, D4) |
| hero-lab treatment → KILLED | **KILLED, verified** | Q1 FLIP; deletion `9ed9175`; hero-lab grep-zero in the R.W2 composite gate; absorbed by NO other wave |
| R.W6 slate (5 shape rows · contract-currency invariant · contract-of-record note · canonical_digest option · FN-1..7) → R.W6 | **LANDED** | `7351297`/merge `8a2a617`; invariant + note recorded (§8 below carries the note verbatim); canonical_digest SKIPPED sanctioned; FN charter fourier `cd26c65` |
| R.W7 slate (X1/X3 + X2 in-wave + X5 fold + X4 record · relay letter · FINAL.md · merge + tag) → R.W7 | **LANDED / PENDING-ON-MERGE** | X5+X4 `825bd58`; relay letter finalized + cascade-vjs response (letters commit); FINAL.md = this document; X1/X2/X3 + merge/tag = the orchestrator ceremony (§7) |
| BOOKS row (D8-1 verify · U6 dock-fission · U3-uSatColor · goo-blob→blob · GAP-3 table · aurora-metal · parse-that re-pin · K-W5RT · S.H3 · CH-10/13 · R8-23 · FN-7 · R-4/5/6/7/8/10 defers) → §3.3 + §4 | **ON THE BOOKS** | §5 table below; D8-1 RETIRED (the one book that closed in-tranche); `/easing` GAP-3 17th-specifier watch ADDED at R.W4 D7 |
| R8-18 → fourier-owned carry | **ROUTED** | Named in the FN charter (fourier `cd26c65`) |
| R8-19 → kf-owned carry | **ROUTED** | Named in the kf letter (kf `9a0f6cb`, `docs/tranches/S/KF-VALUEJS-2.0.0.md`) |
| CLOSED-by-O/P/Q row | **NOT RE-FOLDED** | Verified no wave re-folded them (setSubProperty · color2Into · mixColorsInto · parseCSSSubValue · contrast-color() · VJ-Q5..Q9) |
| OBSOLETE row | **PRUNED** | No wave consumed any pruned item; the "13-of-24 easing drop" claim specifically disproven by the 24/24 D5 probe |

---

## §3 — ι integrity sweep (close-blocking; run 2026-07-04 at `825bd58`)

- **`git reflog --since='2026-07-03'`** — every entry is an attributable wave
  commit, sanctioned merge (`r-w1`/`r-w6` at `20bbc8d`/`8a2a617`), or benign
  `reset: moving to HEAD`. **One incident, recorded and fully reverted**: the
  mid-bisect sequence at 2026-07-03 16:31 (detached checkouts
  `tranche-q → a0c3891 → e80b359 → tranche-q` + two `reset: moving to HEAD`
  ops at `6ed3677`) — the D8-1 regression bisect's index-staging excursion
  reported by the R.W3 regression-fix lane; the tree returned to `tranche-q`
  at `6ed3677` with zero lineage mutation. **No other agent-attributed
  mutating op; zero unauthorized mutations.**
- **`docs/precepts` submodule** — gitlink `63240e6` (matches HEAD's recorded
  pin); `git -C docs/precepts status` clean; log tip `63240e6` as expected.
  The R.W0 W0-7 REVERT decision stands (superseded-draft rationale in
  `PROGRESS.md`).
- **`git stash list`** — **EMPTY** ✓ (close-blocking condition met).
- **`git worktree list`** — main tree only (`/Users/mkbabb/Programming/value.js
  825bd58 [tranche-q]`); zero lane worktrees.
- **`git status --porcelain`** at sweep: **EMPTY** (clean tree). A stale
  session-start snapshot listed 3 modified demo files
  (`Markdown.vue` / `useMarkdownHighlighting.ts` / `AboutPane.vue`); the live
  tree shows them committed at the R.W4 Lane-C landing (`17383a6`) — recorded
  per lesson 3 (drift on the record, live tree wins).

**Verdict: ι CLEAN.** Zero unauthorized mutations; the one recorded incident
was reverted in full and is on the record above.

---

## §4 — π compare-at-close reconciliation (the tranche-level paired-π close)

Both design waves carried paired `{baseline, close}` visual-runtime archives
with per-page DELTA verdicts; this wave ships deploy + docs (no fresh capture
owed) — its π obligation is this reconciliation.

- **R.W3** (`audit/R.W3-visual-runtime/`, 18+18 shots, `close/DELTA.md`):
  picker = "the treatment's instrument, landed"; gradient = "inherited
  keystone deltas only, no regression"; browse = "no regression + the K-INV5
  degraded state". **Every unintended delta resolved, not noted** — the one
  in-close blocker (PaneSlot dev-remount, e2e 9-12/38) was **root-cured**
  (`fceed47`) to 38/38, and the gate-(f) provenance defect was **repaired on
  the record** (`ad1ab93`), not papered. The GooBlob corner-wander line is an
  explicitly-recorded taste residual (producer-side), not an unresolved delta.
- **R.W4** (`audit/R.W4-visual-runtime/`, 36+36 shots + a11y prewave/close
  snapshots, `close/DELTA.md`): all six pages verdict-clean; the one defect a
  close probe surfaced (E4 gamut verdict on display-denormalized OKLab,
  Δ inflated ~500×) was **cured in-lane** (`337f254`) before close — resolved,
  not noted. A11y deltas verified additive-only; the single regression-class
  finding (EasingPicker trigger name) is producer-owned and rides the relay
  letter.

**Verdict: π CLEAN.** No unintended delta survived either close.

---

## §5 — Open BOOKS at close (books, never gates; the successor receives this table)

| Book | Trigger | Status at R close |
|---|---|---|
| `/easing` in the GAP-3 subpath watch — the R.W4 consume made `/easing` the **17th** distinct glass-ui specifier (D7 record, `waves/R.W4.md §BOOKS`) | the glass-ui 5.0.0 cut's by-name MIGRATION table | **OPEN** (relay letter GAP-3 carries it) |
| EasingPicker preset SelectTrigger accessible-name defect (producer-owned; combobox announces only "linear"; the old fork's combobox was named) | the glass-ui fix; carried by the relay letter | **OPEN** (relay item 8) |
| ~~D8-1 no-shim verify~~ | ~~glass-ui rebuilds dist with `layer(components)`~~ | **RETIRED 2026-07-03** — escalation `1599c230` → BG cure `4b637036` (emission site `vite.utility-emit.ts` + self-heal); probe **CURE_OBSERVED** (visibleCount 2, no `!important`). Close-day re-check (2026-07-04): `dist/styles/index.css:266` carries `layer(components)` ✓; **watch line**: `dist/styles/deferred.css:34` still bare — flagged in the relay confirmation (value.js does not consume the deferred subset; informational, not a reopen) |
| ~~K-INV5 typed degraded-backend + "offline — saved locally"~~ | ~~R.W3 dispatch~~ | **DISCHARGED** — verified: landed at R.W3 `c4eb9d2` (typed `ApiUnavailable` latch; ONE doomed attempt per cold session; save/publish affordances) with behavior confirmed in the R.W3 π close (browse verdict) |
| K-W3DIFF PaletteDiff render (REFUTED-AS-CONTRIVANCE; `/diff` recomputes, stored `atomDiff` write-only) | first demo version-compare / remix-review surface; alt-exit = stop persisting `atomDiff` (api-side) | **OPEN**, trigger-bound |
| `srgbToLinear` decode-threshold defect (`conversions/transfer.ts` + gamut.ts inline copy compare ENCODED input vs `SRGB_LINEAR_TRANSITION` 0.003131 instead of `SRGB_TRANSITION` 0.04045 — non-invertible dark-sRGB roundtrip, ~8e-3 max) | next library minor/major (output-changing; snapshot-rippling) | **OPEN** → tranche S |
| `Color.try()` | demand for a non-throwing parse that neither drags parse-that into the parse-that-ZERO `/color` subpath nor duplicates the memoized custom-name path | **OPEN** |
| glass-ui 5.0.0 adopt event (goo-blob→blob re-point · GAP-3 subpath table walk incl. `/easing`+`/tabs` · uSatColor GAP-1 consume · U6 dock-fission verify + U16 sizing half · GAP-4 blob perf · aurora-metal re-verify) | the BG/BH joint cut | **WAITING** (peer floor verified still `^1.0.0` on 2026-07-04 — the letter's item 6 ask stands) |
| parse-that `^1.0.0` re-pin | kf **S.H4** publishes the 1.0.0 cut (verify = span-absence + the 4 `.chain()` sites + chainError-retired + falsy-seed-fix) | **WAITING** (do not pre-pin) |
| vue-router 4→5 (K-W5RT) | stable vue-router 5 | **WAITING** |
| S.H3 Pratt consume-edge | parse-that presents the sketch | **WAITING** |
| CH-10 / CH-13 / R8-23 spec-gated grammar longhands | as previously recorded | **WAITING** |
| fourier FN-7 doc-relocation co-decision + CONSTELLATION.md pointer | fourier-N execution | **WAITING** (de-urgented by the in-tree contract-of-record note, §8) |
| color2Into currency (the O-era egress discipline stays current as new spaces land — OKHSL/OKHSV joined at 2.0.0) | any new color-space addition | standing note (closed-by-O/P/Q row; carried as currency, not re-folded) |
| **X3 first CF-Pages wire run** | the R.W7 master merge (the orchestrator fires it) | **PENDING-ON-MERGE** — the orchestrator amends this row + §7 with the run id/URL + the served R-era demo observation |

---

## §6 — Authoritative counts (run at close-synthesis, 2026-07-04, HEAD `825bd58`)

| Gate | Number | Note |
|---|---|---|
| vitest | **1998 passed / 1998** (58 files) | fresh run at close |
| playwright | **38 passed / 38** (5 projects: smoke / smoke-admin / smoke-mobile / smoke-reactivity / smoke-safari) | fresh run at close |
| eslint (`--max-warnings=0`) | **exit 0** | |
| `npm run typecheck` (vue-tsc lib + demo) | **0 errors** | |
| `cd api && npx tsc --noEmit` | **0 errors** | |
| `bench/gamut-boundary.mjs` | headline (Into 96/jnd/display-p3) **mean 0.3289 ms · p95 0.6391 ms** — UNDER the 0.5 ms contract ceiling | close re-run under concurrent suite load; the R.W1 quiet-machine record was 0.13–0.18 ms mean — both on the record per lesson 3 (no standing threshold script by design) |
| registry | `npm view @mkbabb/value.js version` → **2.0.0** | `dist-tags.latest=2.0.0`; tag `v2.0.0` pushed at R.W1 |

---

## §7 — The wire record (X1 · X2 · X3 · X5 · X4)

- **X5 — rollback runbook: LANDED** (`825bd58`, `docs/dev-deploy-standard.md §6`).
  Both surfaces documented stranger-followable: api forward-fix via master
  revert + break-glass on-host pin (with the reconcile caveat), CF Pages
  dashboard promote / `workflow_dispatch` re-ship / local wrangler. Discharges
  the N.W8′-folded BOOK. The runbook's webhook probe is on the record: an
  unsigned poke returns `Hook not found.` — a manual POST cannot fire a deploy.
- **X4 — openapi table-vs-source decision: RECORDED** (`825bd58`,
  `docs/dev-deploy-standard.md §7`): `api/src/routes/**` is authoritative; the
  `ROUTES` table + `/openapi.json` are a faithful-by-review projection, not
  the contract.
- **X1 — prod deploy: FIRED-AND-BLOCKED-ON-HOST (maintainer-on-host residual,
  amended 2026-07-04).** The master pushes fired (`bdba8fc`..`f9b1810`), but the
  babb.dev deploy webhook is **dead on the host** — the runbook's probe stands
  (`POST deploy.babb.dev/hooks/value-js` → `Hook not found.`, 404) and the
  post-push probe confirms prod still serves I-era lineage (`/health` 404).
  **Maintainer instructions**: on the babb.dev spine host, either re-register
  the `value-js` hook in the webhook daemon config, or run
  `scripts/deploy-hook.sh` directly at `/srv/constellation/palette-api`
  (resets to `origin/master`, builds, brings up the rs0 compose). The
  post-deploy spot probe = `/health` lineage stamp 200 + the `/diff` envelope.
  Named successor: the maintainer at the host; verified by S.W0's api rider.
- **X2 — NCSU-alias retirement (Q2 FLIP, in the W7 gate): PENDING,
  maintainer-on-host (unreachable off-campus — amended 2026-07-04).**
  `mbabb.fi.ncsu.edu/colors/` still answers (HTTP 200, same palette-api body);
  SSH to the host **times out off-campus** (port 22, NCSU firewall).
  **Maintainer instructions** (per `api/apache-vhost.conf:19-27`): on NCSU VPN
  or on-campus, SSH to `mbabb.fi.ncsu.edu` with sudo, remove the `/colors/`
  reverse-proxy `<Location>` block from the Apache vhost, `apachectl
  configtest`, `systemctl reload apache2`; let the DNS/cert lapse. Sequenced
  after X1 confirms `color.babb.dev`+`api.color.babb.dev` serve HEAD lineage.
  Named successor: the maintainer on the NCSU network; booked in S §7.3.
- **X3 — first CF-Pages wire run: GREEN (amended 2026-07-04).** Run
  **28723903374** (`workflow_dispatch` @ master `f9b1810`) → **success, 2m26s**;
  `https://color.babb.dev/` serves the fresh R-era build (hashed bundle
  `assets/index-DuifQ5gI.js` — the first deployable demo since the Tabs drift).
  The road there was the first-wire chronicle CH-14a predicted, **six rungs,
  each root-cured**: (1) `actions/checkout` refuses `../`-sibling paths on
  current runners → the nested-workspace layout (`c5aa091`); (2) glass-ui
  master's lockfile out-of-sync → same-day producer cure (our escalation →
  their `99009e2a`, + `tranche/BG` pushed to origin); (3) the tarball budget
  honestly re-derived for the 2.0.0 surface (320→440 KB, zero vendor chunks
  verified, `a5dbf1e`); (4) CI's glass-ui@master tree lacked the D8-1 cure →
  checkouts pinned to `tranche/BG` + sibling builds under Node 24 (`102b37b`;
  un-pin booked at the 5.0.0 master landing); (5) a LIVE producer abrogation
  caught by the fresh build — `onTransitionEnd` deleted from
  `useLayerTransition` at BG tip, masked locally by a stale dist — consume
  migrated (`b5f94bc`); (6) the CF secrets never mirrored to GitHub (the
  never-fired wire hid it) — set from the repo's own `.env`, and the
  wrangler-action's non-interactive npx refusal replaced with an explicit
  `npx --yes wrangler@4` step (`f9b1810`). **CI itself went green for the
  first time in the unified workflow's history** (run 28722062107, 15m36s,
  full ladder, both node legs).

---

## §8 — The R.W6 contract-of-record note (carried forward verbatim)

> **Contract of record**: `fourier-analysis/docs/tranches/J/design/J-diff-shape.md` **§3/§4** is the bilateral wire-envelope contract for the CRUD twins. value.js binds to it **by transcription** (zero cross-repo reads — dispatch-homes A.0, verified): `api/test/conformance/diff.test.ts:5-6` cites it as the source of rules; the probe's assertions re-encode §3/§4 locally.

The contract-currency invariant (the value.js half; fourier FN-5 mirrors):
**any change to `api/src/lib/crud/atomdiff.ts`, `api/src/hash.ts`, the
`PaletteVersion` shape (`api/src/models.ts:153`), or the URN catalog
re-verifies the fourier twin and updates CONFORMANCE-MATRIX.**

---

## §9 — Cross-repo dispatch record at close

| Event | Status |
|---|---|
| D8-1 `layer(components)` ask → glass-ui BG (pass-2 early dispatch) | **SENT → ESCALATED** (`1599c230`, mid-R.W2 recurrence) **→ CURED** (BG `4b637036`) **→ CURE_OBSERVED** — the letter's item 7 is now a confirmation |
| kf KF-1 re-pin letter (inside the 2.0.0 cut) | **SENT 2026-07-03** — kf `9a0f6cb` (`docs/tranches/S/KF-VALUEJS-2.0.0.md`) |
| fourier peer-floor note (`^0.13.0`→`^2.0.0`) | **SENT 2026-07-03** — fourier `cd26c65` (`docs/tranches/N/VALUEJS-2.0.0-NOTE.md`) |
| fourier FN-1..FN-7 charter (paired-authored) | **DELIVERED** (dev-time `83d4c9f`; reconciled + FN-5-before-M.W10 rider, fourier `cd26c65`) |
| glass-ui relay letter (`letters/GLASSUI-RELAY.md`, finalized this wave: D8-1 → CURED-AND-VERIFIED confirmation · `/easing` GAP-3 watch · EasingPicker a11y defect · U6 per-view-accent taste-note · peer-floor `^1.0.0` live-verified) | **READY FOR DISPATCH** (the orchestrator delivers — no cross-repo write from this lane) |
| cascade-vjs response (`letters/CASCADE-VJS-RESPONSE.md`: ACCEPT unplugin-vue-markdown — verified already landed at `^32.0.0` == registry latest, lockfile coherent; DECLINE `file:`→published per the Q4 ratified §3.4 pin policy) | **READY FOR DISPATCH** |

---

## §10 — Process lessons

### The six PASS3-VERDICT §4 lessons (folded verbatim)

1. **Cut lane worktrees from the tranche head, and re-verify the base before trusting any tree-fact** (PASS2-VERDICT §5; SYNTHESIS-v2 §13). The pass-2 stale worktrees (`15b0382`, 3 commits behind) produced the pass's only two integrity defects — kf1's false "`extractFunctions` absent from source" finding and the SPEC's five-section amplification of it — and the contradiction was visible *inside the pass's own inputs* (boot-blast §1.2 had it right). When parallel lanes disagree on a tree fact, the synthesizer resolves it against the **live tree** before elevating either claim: cross-lane reconciliation is a synthesis obligation, not an optimization.
2. **Byte-identity of touched files does NOT imply suite-cleanliness at head** (pass-3 addition, from L2 §2/§6). The 4 KF-1 files were provably byte-identical across `15b0382..e80b359`, yet the head re-run failed 1 test — a *consumer of the changed surface* (`test/parsing-extract-functions.test.ts:36`) added at head-side `23d1a91` that the stale base could not contain. The carry argument covers the change; only a head re-run covers the change's *blast radius*. PASS2-VERDICT §5.1's stricter re-run ruling is vindicated: the "minutes of work" bought a real 5th-file finding that would otherwise have surfaced mid-R.W1.
3. **Record count drift; never silently reconcile it** (pass-3 addition, from L3 §3). The keyframes bare-specifier recount (12 per installed copy vs the critic's audit-time 48) was flagged as environment drift with both figures on the record — the qualitative fact the fix turns on was independently reproduced under either count. An unexplained delta silently "fixed" is a future integrity defect.
4. **Certify in the amended text, never the discharge lane's word.** All nine pass-3 certifications traced each mustFix to the amended text itself and re-verified the discharge's *new* citations against the live trees — which is exactly how lesson-2's residual was caught and canonized instead of laundered.
5. **Hoist worktree-authored reports before cleanup, with provenance lines** (W0-14, executed at pass 3): worktrees are ephemeral; evidence is not. Every amended document carries a datestamped `AMENDED AT PASS 3` provenance line so the corpus stays self-honest about what changed when.
6. **Unbounded safety claims never auto-ratify** (carried from pass 2, worth the FINAL.md ink): the gamut bound was measured *because bounded-beats-sampled*, and the measurement moved the disposition from auto-ratify to an armed two-option owner call. The model case.

### The R-execution lessons (the board's additions)

7. **The stale-worktree lesson is already canonized** as lesson 1 — R's
   execution re-confirmed it from the consuming side: every wave that trusted
   the live tree over an inherited inventory number (W0's 3→8→14 merge depth,
   5-not-3 worktrees) recorded the drift instead of reconciling it.
8. **Live-producer mid-round drift**: a `file:`-dep producer can rebuild its
   dist MID-ROUND and move the ground under a landed cure — glass-ui's 15:10
   rebuild re-emitted the unlayered `components.css` import at a NEW site
   (`dist/styles/index.css:266`) after R.W2's first half had verified the old
   one gone. The cure was **same-day escalation to the producer's inbox with
   the binding no-consumer-workaround prohibition held** (`1599c230` → BG
   `4b637036` → probe CURE_OBSERVED, book retired same day). The pin policy
   (§3.4) survives this lesson *because* the escalation path worked: `file:`
   currency + a named-defect escalation channel beats a stale registry pin
   that would have hidden the regression until the next version event.
9. **Dev-vs-build silent divergence**: R.W3's PaneSlot defect class — Vue 3.5
   `<Transition mode="out-in">` whose `afterLeave→update` remount never fires
   under vite dev while the production build behaves — means a green build
   plus a green e2e suite can still hide a dev-only dead surface (or vice
   versa). The R.W3 cure was root-removal (drop `out-in`, default simultaneous
   cross-fade) rather than a dev-only patch; the class is recorded in
   `demo/DESIGN.md §Motion`. Probes that gate a design wave must run against
   BOTH the dev server and the built artifact when a Transition/remount
   surface is in play.
10. **Session-limit lane-death + journal-resume**: R.W2's Lane B died on a
    server rate limit with most row work uncommitted in-tree. The recovery
    pattern that worked — recover the work-order from the lane journal,
    re-dispatch a completion lane against the in-tree residue, row-scope the
    commits (`648843b..46ad669`) so each row lands attributable — is the
    standing pattern (first proven at tranche L) for any lane that can die
    mid-flight: journal first, commit row-scoped, never batch a wave's work
    into one perishable uncommitted pile.

---

## §11 — Close ceremony record (amended 2026-07-04)

- Master merge: **FIRED** — `bdba8fc` (`merge(R · close)`, `--no-ff`, 200 files
  +10,203/−4,222), then the X3 wire-fix fast-forwards through `f9b1810`;
  `tranche-q` kept ff-synced throughout.
- Annotated tag at the close commit: **`tranche-r-close`** at `bdba8fc`, pushed.
- X3 wire run: **28723903374 → SUCCESS (2m26s)**; `color.babb.dev` serves the
  R-era bundle (`assets/index-DuifQ5gI.js`). CI first-green: run 28722062107.
  The six-rung chronicle is §7 X3.
- X1 post-deploy spot-probe: **NOT FIRED** — the host webhook is dead
  (`Hook not found.`); maintainer-on-host instructions in §7 X1.
- X2 alias-retirement observation: **NOT FIRED** — host unreachable off-campus;
  maintainer instructions in §7 X2. Both X1/X2 are this repo's own
  maintainership (never external waits); the wave closes
  **complete_with_misses** with these two as the named residuals.
- ι addendum (post-`825bd58` window): the live **keyframes drive executed the
  booked parse-that `^1.0.0` re-pin as value.js 2.0.1** (`a7eabcc`; hand-back
  letter `letters/KF-EXECUTED-THE-REPIN-2.0.1.md`) — the §5 books
  "parse-that re-pin @ kf S.H4" and "color2Into currency" are **DISCHARGED
  cross-instance** by the letter+book mechanism, widened verify green
  (1998/58 + tsc + build + css-parity + color2Into). **Scoping blemish
  recorded**: that commit swept the whole dirty tree (the in-flight S audit
  lanes + five root-level scratch JPEGs) instead of scoping its paths; the
  scratch was relocated at `4963f33`; no content harm. Cross-instance commits
  into this repo must path-scope — carried as an S process lesson.
