# DEEP-FINDINGS — W8 deep-dive net-new ledger

**2026-06-03.** Synthesis of the W8 deep-dive (12 agents over the live local deployments :9001/:5260/:5210/:9143/:5240/:5220/:5176/:5230/:5250 + apis). This is the granular second pass: each agent re-ran *one* repo (or a cross-cutting thread) against `MASTER-FINDINGS.md`, opening the secondary surfaces the first pass never examined (panes, admin, dashboard, survey, equation/morph/gallery, 404/offline) and grounding every row at `file:line`.

**Scope of this doc:** ONLY W8 NET-NEW. Rows already in `MASTER-FINDINGS.md` (re-derived/re-confirmed by W8) are not re-listed. Per-repo net-new totals: value.js 12 · fourier 13 · glass-ui 11 · speedtest 8 · keyframes.js 9 · slides 9 · muster 14 · words 13 · sudoku 7 · bbnf-buddy 8 · dep-drift 12 · completeness-critic 13. Severity matches MASTER (P0/P1/P2). "Folds into" cites the wave to update — a few need NEW lanes (flagged in §D).

---

## §A — Per-repo net-new findings (W8)

### value.js (front K / api L) — 12 net-new

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| goo-blob (PRM) | PRM single-frame renders the BARE body — satellites seeded at opacity:0 never paint; on color-change the one-frame render advances satellites by full wall-clock elapsed → arbitrary mid-orbit snap. The "single-frame path" the C3 row names must be a defined RESTING POSE. | P1 | useMetaballRenderer.ts:278-282,330-334; useBlobSatellites.ts:137,149-278 | `satellites.poseStatic()` → static orbit layout @ ~0.75 opacity, mood frozen at target (not idle-mid-tween). | K.W3 (Metaballs primitive PRM contract) |
| watercolor-dot | SVG-watercolor RAF (border-radius morph) runs with ZERO PRM gate; CSS PRM guard only touches anim/transition-*duration*, never this JS ref-mutation. SECOND uncovered perpetual blob (C3 covered only the WebGL one). | P1 | useWatercolorBlob.ts:128,108,87-109; animations.css:32-41 | Gate on matchMedia PRM → deterministic resting shape, never start rAF. | K.W3 (BlobDot SVG tier) |
| mix pane | mix-animation canvas RAF (blur+fill full pane/frame) has NO PRM gate — third uncovered JS loop. Sweep is decoration; result is the payload. | P1 | useMixingAnimation.ts:209-214,206,91-207 | Under PRM skip to `done` (or single static merged frame); bind 1800/600/600/200ms to `--duration-*`. | K (demo-local JS-RAF PRM gate) |
| loading (26 sites) | `animation-iteration-count:1 !important` (the PRM guard) freezes every Tailwind `animate-spin` after ONE rotation → reads as a hung UI during loads. | P2 | animations.css:38; 26 `animate-spin` sites incl. BrowsePane:28, MixPane:100, AdminUsersPanel:24/35/41/93, ColorInput:67, PaletteSlugBar:26 | Under PRM swap to glass-ui Pulse/Skeleton opacity-breath busy-variant. | K (adopt glass-ui Spinner/Pulse breath) |
| gradient pane | Gradient stops are focusable `<button>`s with ZERO keyboard ops — no @keydown for move/add/remove; add=dblclick, remove=contextmenu are mouse-only. Whole edit model is pointer-only. | P1 | GradientStopEditor.vue:118-142,115,141 | @keydown Arrow ±1%/Shift±10% move, Backspace/Delete remove, Enter/Space add. | K (gradient-pane a11y) |
| gradient pane | Sole discoverability hint at `text-muted-foreground/40` (40% of an already-muted token) + mouse-verb-only phrasing — severe contrast fail on the only instructional text. | P1 | GradientStopEditor.vue:146 | Drop the `/40`; phrase device-neutrally. | K (gradient-pane a11y) |
| color picker | `role=textbox` contenteditable parse-fail has NO `aria-invalid`, badge not linked via `aria-describedby` (badge is pointer-events:none) — SR users get zero programmatic invalid signal. AND `.error-badge` (right:0.5rem) renders directly ON TOP of `.send-btn` (right:0.25rem), occluding submit. | P1 | ColorInput.vue:13,80-82,306-309,331-335 | `:aria-invalid` + badge id/aria-describedby; reposition badge off the send button. | K (form-a11y row — contenteditable + occlusion specifics) |
| pane router | Pane enter (0.45s `--spring-snappy` overshoot) vs leave (0.3s `--ease-out` no spring) use different clock AND curve family — the swap accelerates out flat but springs in. | P2 | App.vue:244-248,250-253 | Unify leave onto the spring family (shorter `--duration-normal --spring-gentle`). | K motion |
| color picker | Spectrum dot applies `filter:url(#watercolor-filter)` to a left/top-% moving element with a box-shadow → per-frame SVG filter-region raster on the hottest drag path; plus a dead `.spectrum-dot:hover{transform:none}`. | P2 | SpectrumCanvas.vue:274,273,238-239,275-277 | Drop the SVG filter on the moving dot; position via `translate()` on a will-change layer; delete dead rule. | K motion (extends the spectrum-dot-jumps row) |
| admin api | `/admin/tags` returns a BARE array while `/admin/queue`,`/flagged`,`/audit` return the `{data,total,limit,offset}` envelope — consumer must special-case tags (systemic to the tag surface). | P2 | api/src/routes/admin/tags.ts:15-17 (live: tags=`[]`, others=envelope) | Wrap tags in the same envelope; update `useAdminTags` to read `.data`. | L (api admin contract) |
| goo-blob shader | Hue perturbation `hsv.x += …` has no `fract()` before `hsv2rgb` → discontinuous hue pop at the 0/1 seam; brightness shifted twice (uBrightnessShift + edge-glow re-lift) → edge clipping. | P2 | metaball.frag.glsl:149,151,154-155 | `hsv.x = fract(hsv.x + …)`; apply edge-glow as a `max()` ceiling, clamp once. | K.W3 (Metaballs shader) |
| goo-blob | `mergeRate` is named as a rate but behaves as an INVERSE stagger multiplier (excited=0.3 merges fastest, sleepy=2.0 slowest); reads backwards to a maintainer, no comment. | P2 | useBlobSatellites.ts:169,154; useBlobMood.ts:16/29/55/68 | Rename `mergeStaggerScale` (or invert+divide) during the useSpring mood rewrite. | K.W3 (mood-on-useSpring FSM row) |

> Minor P2 cluster (one demo type-hygiene/correctness sweep, fold into K): `: any` at ColorSpaceSelector.vue:7,66 / useSliderGradients.ts:50,69 / ColorInput.vue:220; missing `:key` at ColorSpaceSelector.vue:23-28; dead `inject` at GeneratePane.vue:11.

### fourier-analysis (J) — 13 net-new

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| scripts/dev.sh | **Self-shuts the local stack** — `kill -0 "${PIDS[-1]}"` uses a NEGATIVE array subscript; macOS bash 3.2.57 errors `bad array subscript` under `set -euo pipefail` → trap cleanup kills the just-started API. Reproduced live. | **P0** | dev.sh:80,3,70,77 | `API_PID=$!; PIDS+=("$API_PID"); kill -0 "$API_PID"`; drop the dead `.dev.pids` rm. | deploy W6 (the concrete repro for the booked dev.sh row) |
| /morph | The `/morph` keyframes.js 3-phase tween ignores PRM (grep PRM = 0). DISTINCT surface from the epicycle clock — separate route + engine. | P1 | useFourierMorph.ts:145-213; FourierMorphDemo.vue:127-135 | Under PRM jump to end state, phase→idle; compose off the shared RAF primitive. | J motion (epicycle-PRM row → multi-surface) |
| /equation | Convergence-curve transition is a 500ms `easeInOutSine` RAF with ZERO PRM gate. Third ungated RAF surface. | P1 | useCurveTransition.ts:26,36-54,8 | `progress=1 + onFrame() once` under PRM. | J motion |
| /visualize | Two more ungated epicycle-canvas RAFs: hover-scale damping loop + continuous golden-shimmer tick. | P2 | useCanvasHover.ts:53-62,72-74,96-98,121-123 | Snap scale instantly + freeze shimmer under PRM. | J motion |
| /visualize | Canvas empty-state (dot-grid + dropzone + prompt) drawn in HARDCODED `rgba(150,150,150,α)` — color-scheme-blind; 0.6-grey prompt on dark canvas fails AA; hardcodes `'Fira Code'` not `--font-mono`. Distinct from the layout-void row. | P1 | placeholder.ts:8,33,55,56,59-60 | Read resolved CSS tokens at draw time (the VIZ_COLORS pattern). | J empty-state (adds dark-mode contrast to the void row) |
| /gallery | A fetch FAILURE is indistinguishable from an empty DB — catch fires a toast then `entries=[]`, no error ref, falls through to the cold-empty CTA. Live: `/api/visualizations`→500 shows "go create one". | P1 | stores/gallery.ts:95-98; GalleryView.vue:266 | Persistent `error` ref + distinct `role=alert` + Retry before the empty CTA. | J gallery/empty-state |
| /visualize | "Computing" rainbow-bar is `rainbow-slide 1.4s linear infinite` with NO PRM guard — even though GalleryMarquee (structurally identical) WAS carved. | P2 | ImageUpload.vue:159,162-165 | Add the same `@media PRM` carve; hoist a shared `.loading-rainbow`. | J motion |
| /equation | Coefficient-family hover machine is MOUSE-ONLY (onMouseMove geometry+class scan) — keyboard/SR users can never trigger the family highlight or popover. CSS-Highlight fix alone doesn't restore keyboard. Popover hand-positioned via getBoundingClientRect where Anchor Positioning is the idiom. | P2 | useCoeffHover.ts:28-49,36-41; EquationView.vue:420-423,262,100 | useTextHighlight + focusable spans (`:focus-within`) + `anchor()` popover; drop `trust:true`. | J useTextHighlight (adds keyboard-a11y + anchor dims) |
| /paper | SECOND in-repo `<mark>`-injection consumer — `highlightFuzzy` string-builder via `v-html` at PaperSearchDropdown + PaperSearchModal. fourier ALONE now has 2 useTextHighlight sites. | P2 | searchHelpers.ts:34-72; PaperSearchDropdown.vue:58; PaperSearchModal.vue:6 | Migrate both + equation hover onto useTextHighlight. | J useTextHighlight (hardens the ≥2-consumer gate from inside one repo) |
| /visualize | Floating play/pause button hardcodes `color:#fff` + white border + white `:focus-visible` outline assuming a dark backdrop — but the canvas is LIGHT by default → invisible focus ring + poor contrast. `transition:box-shadow` over a `backdrop-filter`; no `@supports` fallback. | P2 | AnimationControls.vue:153,149,183,155,151-152 | Drive ink/border/ring from tokens resolving against the live surface (couples to `--dock-fg-on-aurora`); pseudo-element shadow; `@supports` fallback. | J motion/a11y + glass-ui `--dock-fg-on-aurora` |
| /visualize | Trace tip-dot glow `0.2 + 0.1*sin(performance.now()/300)` is WALL-CLOCK keyed — pulses even when PAUSED (hover/resize/scrub redraws advance it); no PRM gate. Distinct from the flat-alpha-trail row. | P2 | epicycles.ts:280,281-284 | Drive glow phase from the animation clock; static under PRM. | J motion |
| /visualize | Scoped Vue `<Transition>` enters (panel-swap/slide/fade/pop) fire regardless of PRM, unlike the carved global tab-slide; /paper callout hover transitions `box-shadow` (paint) ungated. | P2 | VisualizationView.vue:430-440; EquationView.vue:444-452; style.css:92; PaperArticleWindow.vue:199,203-205 | One shared `@media PRM` zeroing enter transforms. | J motion (PRM-consistency tail) |
| /visualize | Timeline caret-value readout appears ONLY on `:hover`/`[data-held]` — keyboard arrow-key seeks get the ARIA value but no visible caret label. | P2 | GlassTimeline.vue:102-103,90-105,62-64 | Reveal on `:focus-within` too. | J a11y tail (J.W6) |

### glass-ui (AS — PRIMITIVE SOURCE) — 11 net-new

> Theme: glass-ui's OWN hand-rolled (non-reka) interactive components (DataTable, custom Tabs, SortableList) carry the keyboard+ARIA gaps the primitive-level first pass missed. **Spec clarification for the glass-ui-first mandate: every hand-rolled `role=` or `@click`-interactive custom component must match the reka-ui keyboard+focus contract.**

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| data/DataTable | Sortable headers mouse-only: bare `<th @click>`, no role/tabindex/keydown; `aria-sort` at ZERO sites repo-wide; sort state is a Unicode arrow string (SR reads a stray glyph). | P1 | DataTable.vue:318-331,195-199; TableHead.vue | `aria-sort` on `<th>` + focusable control + keydown; visually-hidden status text. | AS — Data-category a11y wave |
| data/DataTable | Clickable rows mouse-only: `<tr @click>`/card-div carry cursor-pointer + click but no role/tabindex/keydown — keyboard can never select a row. | P1 | DataTable.vue:360-363,390-393,237-240 | When a `select` listener is bound, give the row tabindex+role+keydown. | AS — Data-category a11y wave |
| nav/BouncyTabs | `.bouncy-btn` has NO `:focus-visible` ring (invisible keyboard focus); as a "Tabs" pattern uses `aria-pressed` not role=tablist/tab/aria-selected; no roving tabindex/arrow keys. | P1 | BouncyToggle.vue:411-449,293-329 | Add focus-ring; single-select variant → role=tablist + roving + Arrow keys. | AS — Tabs-family a11y wave |
| nav/UnderlineTabs + CarouselDots | Hand-rolled `role=tablist` tabs lack roving tabindex + Arrow/Home/End; UnderlineTabs has no `:focus-visible`; CarouselDots announces as tab but only `@click`. | P2 | UnderlineTabs.vue:34-45,92-117; CarouselDots.vue:43,57,75 | Roving tabindex + Arrow/Home/End on both; focus-visible; or compose reka's roving primitive. | AS — Tabs-family a11y wave |
| data/SortableList | Pointer-only: ZERO keyboard reorder, no aria-grabbed/dropeffect/roving; drop-zone `shimmer …infinite` + drag-ghost `rotate(1.5deg)` have NO PRM gate. | P1 | useSortable.ts:117,150,353-361,461; SortableList.vue:189,124; SortableItem.vue (0 PRM) | Keyboard reorder (Space grab, Arrow move, Esc cancel) + aria-live; PRM-gate the shimmer/ghost (mirror Skeleton). | AS — SortableList a11y + PRM |
| feedback | Two bare Tailwind `animate-spin` spinners (no motion-safe) keep rotating under PRM while sibling StatusDot uses `motion-safe:animate-ping`; InfiniteScroll loading/end regions have no aria-live/role=status. | P2 | InfiniteScroll.vue:50,46-58; ConfirmDialog.vue:40; StatusDot uses motion-safe | One spinner recipe: `motion-safe:animate-spin` + role=status + name; aria-live region. | AS — spinner/feedback motion-safe + live-region |
| nav/vertical-rail | `.glass-dock.vertical` sets `width/height:auto` AND lists a `transition: height/width` over them — dead per its own comment; the rail snaps. New locus of the interpolate-size chronic. | P2 | dock.css:206-207,213-220,182-184,367 | Drop the dead entries OR `interpolate-size:allow-keywords`+`calc-size(auto)` under `@supports`. | AS — interpolate-size/calc-size migration (add the rail) |
| nav/BouncyTabs | Press WAAPI hardcodes `duration:200` (not a token) unlike every sibling; `readToken` runs `getComputedStyle` 3×/click — forced sync style read per press. | P2 | BouncyToggle.vue:175,163-165,15-21,368-424 | Read `--duration-fast` once; cache resolved tokens (re-resolve on dark-mode flip). | AS — Tabs-family wave (token-bind + reflow) |
| feedback/Pulse | The `ring` variant under PRM gets `animation:none;opacity:0.6` but keeps `border-top-color:transparent` → a frozen partial arc that reads as a render glitch. | P2 | Pulse.vue:127-135,178-180 | Render a complete ring (or static dot) under PRM. | AS — feedback PRM resting-pose |
| data/ScrollingText | Marquee pauses on `:hover` only; host not focusable — keyboard user has no way to pause to read overflow text in the animated path. | P2 | ScrollingText.vue:125-127,71-76,129-139 | Add `:focus-within` pause + tabindex on the content. | AS — ScrollingText keyboard polish |
| custom/StatusDot | Conveys status by COLOR ALONE when no label/aria-label (role only set when aria-label present) — active vs error indistinguishable + unannounced (WCAG 1.4.1). | P2 | StatusDot.vue:3,43-69 | Derive a default accessible name from variant; make name non-optional when variant≠custom. | AS — StatusDot default-name |

### speedtest (AT — friday.institute) — 8 net-new

> **MASTER corrections at fold time:** (1) the "VT ready/finished leak P1" row mis-cites speedtest — speedtest uses keyframes.js ElementMorph (hardened `.then().catch()`), ZERO useViewTransition/`.ready` usage; re-label AT-R2 as a CLS-free re-architecture **enhancement**, not a leak fix, and drop the "requires glass-ui `.ready` swallow" blocker. (2) The "Gauge sized by JS ratio" row is largely already satisfied — the dial IS aspect-ratio/container sized; `DIAL_DIAMETER_RATIO` only sizes the under-meter bar width (intentional canvas/DOM parity) → **demote/close** that row.

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| completion-whirl | The completion "farewell whirl" (2500ms 4π canvas spin) + 550ms settle + 600ms abort are NumericAnimation calls that DON'T pass `respectReducedMotion:true` (engine defaults false) → full whirl plays under PRM. The loudest motion on the completion edge. Morph IS gated; the longer whirl is NOT. | P1 | meterCompletion.ts:103-110,126-129,227-230; numeric.ts:46,103,275; SpeedtestResults.vue:1050-1056 | Pass `respectReducedMotion:true` to the 3 ctors (flip the existing opt-in). | AT (broaden needle-spring row to the whirl; promote to P1) |
| stats-cards | StatsCards mount with no loading/empty guard → `cards`=[] when stats null collapses the grid to height 0, then pops to 4 cards on fetch (CLS, different route than dial). Sibling charts DO get a Skeleton breath. | P1 | StatsCards.vue:35-44,2; AdminOverviewView.vue:34 vs 67-76 | 4 placeholder cards / Skeleton breath grid reserving footprint from first paint. | AT (CLS/intrinsic-size reserve, extended to the dashboard) |
| charts | `useChartData` swallows EVERY fetch error with bare `catch{}`→null; no error state — a 404/flaky API reads as "0 Mbps average". The MAP path has a proper `dataState` union; charts don't. | P1 | useChartData.ts:24,33,44; composable.ts:64,98-101 | Add `error` ref / reuse the map's `dataState`; distinguish abort vs failure. | AT (dashboard lane) |
| admin | Result/session DELETE fires directly from the row dropdown/context-menu with NO confirm — immediate + irreversible (accidental keyboard activation deletes a record). Subnet dialogs prove the pattern exists. | P2 | AdminDataView.vue:69,82,120,126; ResultsTable.vue:156-161 | Gate behind glass-ui AlertDialog (destructive confirm). | AT (admin polish) |
| charts | Per-tab chart loading `<div aria-label>` has no role=status/aria-live; no aria-busy; loading + resolution unannounced; visible "Loading chart…" duplicates aria-label. | P2 | ChartsView.vue:63-75,104-116; AdminOverviewView.vue:72-82 | role=status aria-live=polite; drop the redundant aria-label. | AT (dashboard a11y) |
| stats-cards | `transition-shadow` declared with NO `:hover` rule changing box-shadow anywhere — dead transition / missing affordance on interactive-looking cards. | P2 | StatsCards.vue:8,47-54 | Add a glass-ui Card hover variant or remove the vestigial class. | AT (micro-interaction polish) |
| admin-login | Single-field login hero never focuses the token Input on mount — keyboard/SR users land on body and must tab in. | P2 | AdminLoginView.vue:134-144,80+ | `useTemplateRef` + `.focus()` in onMounted (when not already authed). | AT (admin a11y) |
| meter-prewarm | Running pre-data Skeleton is `aria-hidden=true` with no companion live region — SR users get NO "measuring…" cue during the phase-start→first-data gap. | P2 | MeterColumn.vue:41-46 | Keep Skeleton decorative; add a visually-hidden aria-live="Measuring download…". | AT (running a11y; complements mobile-blank-card row) |

### keyframes.js (A — animation ENGINE) — 9 net-new

> **MASTER correction (must apply):** the keyframes-A "reduced-motion" row + §B "Animation engine" owner row assert "glass-ui /motion wraps these heavy engines → constellation-wide hole" — **FALSE.** glass-ui /motion wraps only the LIGHT engines (SmoothProgress/SpringProgress/NumericAnimation + ElementMorph/Timeline); NO sibling imports the heavy Animation/CSSKeyframesAnimation/AnimationGroup. Re-scope the PRM lever to (1) light-engine default-OFF in RAFPlayback and (2) heavy-engine gate for the demo + external sudoku-class consumers.

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| 2.2.0 release | The split that broke sudoku is a **SEMVER VIOLATION** — CHANGELOG documents it under "Breaking changes" yet it shipped as MINOR (2.1.1→2.2.0), so every `^2.x` consumer auto-resolved to 2.2.0 and broke. The root cause is the version number. | **P0** | CHANGELOG.md "Breaking changes"; package.json `2.2.0` | Re-release as 3.0.0 (major); gate releases on a changeset "major" when a runtime named export leaves the barrel. | A.W1-W4 (first item; the CI-break row) |
| dts contract | The dts LIES about runtime: it emits the moved heavy classes as concrete VALUE decls (`export declare class AnimationGroup`, `export { Animation_2 as Animation }`) so `import { Animation }` compiles then crashes. Types don't catch it. | **P0** | dist/keyframes.d.ts:134,183,336 vs dist/keyframes.js export list | Emit them as `export type {…}` (the API-Extractor config collapses index.ts's type-only intent back to a class decl). | A.W1-W4 (pairs with 3.0.0) |
| string-easing | The split's goal (light engines never pull value.js) is DEFEATED for the common case: a STRING easing name to a light engine triggers `await import("./engine")` dragging the 24KB heavy chunk + value.js. String names are the ergonomic default; speedtest + glass-ui hit this. | P1 | numeric.ts:109-113,177; timeline.ts:50,78; speedtest meterCompletion.ts; glass-ui useNumericTransition.ts:26 | Hoist the named-easing registry into the LIGHT side so common names resolve synchronously. | A.W4 (sharpens string-easing footgun) |
| string-easing | Stateless `.at(progress)` returns SILENT LINEAR for a string easing before `ready()` resolves — `.at()` never auto-triggers ready(); wrong curve, no warning. | P1 | numeric.ts:112,156,170-188,197,270 | `.at()` resolves via the light registry, or throws "await .ready()". | A.W4 (the `.at()` variant) |
| /amiga demo | FOUR infinite CSSKeyframesAnimation children in an AnimationGroup with ZERO PRM — the engine's OWN shop window, worse than fourier's epicycle. The heavy driver has no PRM path at all. | P1 | useAmigaAnimations.ts:28-96; group.ts:351; engine.ts:678-701 | The engine-base PRM gate (A.W4) snapping infinite loops to a resting frame. | A.W4 (live grounding for the heavy-engine row) |
| demo scene | Demo has ZERO PRM anywhere incl. the scene cross-fade (`Transition name=scene` scale-pop); `:css="ready"` gates on scene-router readiness, NOT PRM. | P1 | App.vue:113,169,406-423 | `@media PRM` carve until the scene-swap moves to startViewTransition (which inherits glass-ui's carve). | A (the scene-swap row — adds PRM FAIL) |
| dogfood gap | The demo NEVER exercises its own dynamic-import contract — every composable deep-imports `@src/animation/engine`, bypassing the barrel + `loadAnimationEngine()`, so the shop-window cannot reproduce the sudoku-class break pre-publish. | P1 | useCubeAnimations.ts:4-5; useAmigaAnimations.ts:4-5; App.vue:152; grep loadAnimationEngine demo/→0 | One demo path (or published-package e2e smoke) constructing via `await loadAnimationEngine()` against the BUILT dist barrel. | A.W4 close (the structural guard) |
| tick latency | `draw()` is async and AWAITS `tick()` BEFORE rescheduling rAF — the next-frame schedule is gated behind the tick microtask chain + `await sleep(delay)`; AnimationGroup awaits `Promise.all` over all children first → frame-late drift, distinct from long-task INP. | P2 | engine.ts:703-726,644-648; group.ts:323-344,351-352,370 | Schedule the next rAF synchronously at the TOP of draw, cancel on done. | A.W4 (sharpens the tick-yield row) |
| PRM default | The light-engine PRM gate is opt-IN with `respectReducedMotion:false` default in 3 dup copies, NONE in the shared RAFPlayback loop — so glass-ui useSpring/useAnimatedNumber/useNumericTransition animate under PRM unless every call site opts in (they don't). | P1 | smooth.ts:42; spring.ts:63; numeric.ts:103; playback.ts (no PRM); glass-ui useSpring.ts:15 etc. | Move ONE PRM gate into RAFPlayback + default to TRUE; glass-ui inherits PRM-correct motion for free. | A.W4 (the TRUE constellation-wide PRM lever) |

> P2 also: WAAPI shadow `tickLoop` fires-and-forgets the async `tick()` (race on pause/iteration reads) — waapi.ts:200-211, folds into the WAAPI-loses-spring row.

### slides (B — bespoke, no glass-ui) — 9 net-new (3 rows)

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| dock 390px + inert dark | Always-expanded dock has overflow:visible + NO max-width so controls+10 dots overflow; body `overflow-x:hidden` clips → DarkToggle+chevron UNREACHABLE at 390px. AND `.dark` class bound on dock/edges/progress with ZERO consuming rule + no `color-scheme:dark` → light chrome over force-dark slide (WCAG 1.4.11, dead markup). | P1 | dock.css:479-481; deck.css:113; DeckView.vue:57,64,75,101 | Adaptive pager + clamp always-expanded to `--dock-wrap-max-width` + `color-scheme:dark` on dark chrome. | B.W2 + glass-ui R66/R72/R187 |
| pager dots a11y | 10 jump buttons have ZERO focus-visible (only the edge button); `aria-current=true` vs `step`. WCAG 2.4.7. | P1 | deck.css:431-443,476; DeckPager.vue:11-20,18 | focus-visible outline + `aria-current=step`. | B.W2 + glass-ui R72 |
| JS lifecycle cluster | count-up recursive rAF never cancels (leaks/stacks); constellation frame re-schedules unconditionally even when 8/10 idle (no suspend); PRM read once with no change-listener; page-turn split clock 0.5s/0.6s + inactive slides have no content-visibility (391 `#slide-NN` painted off-stage); `load()` no try/catch → stuck "Loading"; `offsetWidth` unscaled 1280 → 2560px oversampled. | P2 | useDeckNav.ts:81-100,18; constellation.ts:347-357,306,82-89; deck.css:145; DeckPage.vue:21 | cancel rAF; suspend no-canvas; mql listener; unify page-turn + content-visibility; try/catch; rect width; useRAFLoop+PRM composable. | B.W0/B.W1/B.W3-W4 |

### muster (K — exemplar) — 14 net-new

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| AuroraHost / useAuroraEffects | The ENTIRE reactive Aurora-effects layer is uninvoked DEAD CODE — bigger than MASTER row 114. `useAuroraEffects()` (opacity ceiling, per-phase tint, hero drop, winner-hue drift, PRM) is NEVER called; AuroraHost passes a STATIC config. Cascade steps 1+2 are computed-then-discarded; the unit spec asserts the refs → false green. | P1 | AuroraHost.vue:32; useAuroraConfig.ts:159; useMusterApp.ts:452-455; useVerdictMoment.ts:11-16,302; useVerdictMoment.spec.ts:279-285 | Invoke `useAuroraEffects({phase,revealStaggerActive,winnerCategoryHue})`; thread outputs into the config; pass `categoryHueFor` so winnerCategoryHue is non-null. | K.W4 (extends row 114 to Aurora steps 1+2) |
| RankedVerdict + EliminatedFold | Refines row 114: the consumers have NO PROP to accept reveal flags — structurally impossible without a component-API change. Rows have no opacity:0/translateY start; EliminatedFold hardcodes `opacity:0.6` statically. Not pure data-flow wiring. | P1 | RankedVerdict.vue:204-209; EliminatedFold.vue:27-31,144 | Add `isRevealed?:boolean[]` + `.is-revealed` rows (opacity:0→1) using the dead `--reveal-stagger-step` token; `revealed?:boolean` on EliminatedFold. | K.W4 (names the missing props + dead token) |
| FirstRunWalk | Docstring promises "Escape closes cleanly" but there is NO keydown/Escape listener — keyboard users can't dismiss the walk. | P1 | FirstRunWalk.vue:194-196 | `useEventListener(document,'keydown', Escape→skip())` guarded to open. | K.W4 onboarding a11y |
| FirstRunWalk | Docstring claims it spotlights the corresponding aside layer via `data-walk-spotlight` — emitted NOWHERE, consumed NOWHERE. The "guided tour" is a disconnected centered card. | P2 | FirstRunWalk.vue:64-67 | Emit `data-walk-spotlight=<step.id>` + dim non-spotlit layers, OR delete the dead claim. | K.W4 onboarding |
| FirstRunWalk | The `first-run-walk-in` keyframe fires ONLY on mount; advancing steps swaps title/body IN PLACE with zero transition — the "temporal cascade" is an instant text snap. | P2 | FirstRunWalk.vue:287,114-121 | Key the card on `step.id` / `<Transition mode=out-in>` reusing the 320ms token. | K.W4 onboarding motion |
| useReRank | Verdict reorder commits ONLY on a global `document 'pointerup'` — keyboard weight-tuning (arrow keys) fires no pointerup, so rank order + the MANDATORY focus-to-winner NEVER settle for keyboard users (functional a11y break). | P1 | useReRank.ts:265-270,243-248 | Also commit on slider value-settle / keyup for keyboard, gated by the same `changed` check. | K.W4 re-rank a11y |
| useReRank | `commitReorder.finished.then()` force-moves focus to `.winner-hero__name` (global querySelector) on EVERY pointer re-rank — yanks focus from the slider a user is tuning; fragile global query no-ops in empty state. | P2 | useReRank.ts:243-248; WinnerHero.vue:204 | Only move focus when activeElement isn't the slider; use a forwarded template ref. | K.W4 re-rank a11y |
| WinnerHero | Score badge shows the spring value which overshoots >1.0 (~1.07) for a score "out of 1.0"; the bar model-value hits ~107; aria-label uses the TRUE score → AT/visual diverge. | P2 | WinnerHero.vue:99-105,225-228,231-235; useVerdictMoment.ts:18 | Clamp the DISPLAYED value to [0,1]; overshoot the motion, not the number. | K.W4 verdict polish |
| micro-tweens | Every micro-tween writes `var(--duration-fast,120ms)` but the token is 200ms → the 120ms fallback is DEAD; EliminatedFold uniquely uses 150ms — a third inconsistent fallback. Author's 120ms mental model ≠ 200ms runtime. | P2 | EliminatedFold.vue:127; SlugIdentityBadge:65, OriginsLayer:168, ConstraintsLayer:311, VoterRow:164, RecentRunsMenu:205; glass-ui tokens.css:72 | Accept 200ms + delete the misleading fallbacks, OR define `--duration-fast:120ms` locally. | K.W4 motion-token unification |
| ScoreFairnessField etc. | `transition: r 160ms` (SVG radius) + EliminatedFold chevron + PresetRail + RankedVerdict have ZERO PRM guards while FirstRunWalk/AuroraHost DO guard — decorative motion under reduce. | P2 | ScoreFairnessField.vue:281; EliminatedFold.vue:127; PresetRail.vue:77-78 | Wrap decorative transitions in `@media PRM{transition:none}`. | K.W4 PRM sweep |
| ScoreFairnessField | Axis labels `font-size:3px` in SVG user units — sub-pixel on narrow viewports + ignores browser text-zoom. | P2 | ScoreFairnessField.vue:270,253-256 | Render labels as rem-based HTML overlay, or a viewBox-relative size with a floor. | K.W4 verdict a11y |
| TravelMatrix | Strips MetricCell's glass-wash via a SCOPED host class (`background:transparent` etc.) reaching into a vendored component — the per-instance override anti-pattern. | P2 | TravelMatrix.vue:88-94,142-149; metric-cell.js | glass-ui MetricCell `appearance="bare"`/`surface=false` variant. | AS (glass-ui MetricCell bare variant — net-new prop) |
| AuroraHost | `.aurora-host` uses static `100vh/100vw` while the shell uses `dvh` → mobile chrome overshoot + scrollbar overflow. | P2 | styles.css:158-159; App.vue:457; VerdictStage.vue:116 | `100dvh / 100%`. | K.W4 viewport-unit unification |
| score gauges | Concrete instance of the timid-achromatic chronic on the PRIMARY gauge: runner-up bars + headline score bar bind `--progress-fill:var(--primary)` (S=10% near-grey) while the contribution sub-bars beside them are vivid → the most important gauge is the dullest. | P2 | RankedVerdict.vue:236-238; WinnerHero.vue:231-235; tokens.css:369 | Commit one saturated jewel `--accent` and bind `--progress-fill` to it. | K.W4 (folds into row 232/284 chronic) |

> P2 robustness: MobileInstrumentSheet `#mobile-sheet-body` teleport target lives in a DrawerContent without `force-mount` (vaul Presence unmounts on close) → a programmatic mobile close removes the single-mount instrument tree's home. Fix: `force-mount` on DrawerPortal. (MobileInstrumentSheet.vue:64-79; InstrumentAside.vue:50; useMusterApp.ts:553-557) → K.W4 + AS vaul-to-native Drawer.

### words (A — warm-paper dictionary) — 13 net-new

> **MASTER reframings:** (1) row 144 under-describes — there are TWO virtualization engines (hand-rolled `composables/virtual/*` AND `@tanstack/vue-virtual`); the fix is converge on tanstack + DELETE the 785-LoC fork, not merely add content-visibility. (2) the A.W2 "PWA (Workbox)" framing is wrong — the existing hand-rolled SW actively FAILS to install in prod (P0); it's fixing a broken-in-prod SW, not a greenfield enhancement.

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| service-worker | **PWA install FAILS in prod** — `STATIC_ASSETS` caches `/assets/index.css` (Vite emits a hashed name) + `/assets/themed-cards.css` (never built); `cache.addAll()` is ATOMIC → one 404 rejects install → the entire offline/cache layer is dead. | **P0** | public/service-worker.js:7-13,19,16-24; dist has index-NonSZd9v.css, no themed-cards.css | Generate the precache manifest at build (Workbox injectManifest) prefixed with `BASE_URL`. | A.W2 (PWA/SW hardening) + W6 deploy build-standard |
| service-worker | API caching matches `/api/` but prod paths are `/words/api/…` → staleWhileRevalidate NEVER engages; if it DID it would intercept the SSE lookup stream (`/stream`, text/event-stream) and replay a truncated synthesis stream (only `/generate-name` is excluded). | P1 | service-worker.js:78-85,80,202-214; core.ts:6; lookup.ts; SSEClient.ts:152 | Derive the API prefix from `BASE_URL`; explicit bypass for `text/event-stream`/`/stream`. | A.W2 (PWA/SW hardening) |
| index.html | Every static `<link>` is `/`-rooted (manifest, 5+ favicons, apple splash) → 404 under the `/words/` base → installable-PWA criteria fail, favicons/splash missing. Vite doesn't rewrite hand-authored absolute hrefs. | P1 | index.html:5-29; manifest.json:5-6; vite.config.ts:16 | `%BASE_URL%`-prefixed/relative hrefs. | A.W2 + W6 deploy build-standard |
| service-worker | `cacheFirst/networkFirst` fall back to `cache.match('/offline.html')` but no offline.html exists → silent degrade to bare `Response('Offline',503)`; the promised offline UX never renders. | P2 | service-worker.js:182-184,196-198 | Author a warm-paper offline.html + add to the precache manifest. | A.W2 (PWA/SW hardening) |
| AnimatedText | Beyond the typewriter (row 141): AnimatedText runs `liftDown …infinite` on EVERY char span (per-char delay, ~4s) — perpetual ambient bounce on primary text with NO PRM guard; hardcoded grey 8-layer text-shadow ignores warm paper. | P1 | AnimatedText.vue:88-92,22-25,124-154,156-159; LoadingModal.vue:4,45 | One-shot `liftDown` + `@media PRM{animation:none}`; token-driven shadow. | A (headword reveal — sibling of row 141) |
| SearchResultItem + WordListRow | Systemic keyboard/focus gap: SearchResultItem `<button>` has NO focus-visible ring (Tab focus ≠ roving selection); WordListRow is a plain `<div @click>` with no role/tabindex/keydown → the whole wordlist (hundreds of rows) is mouse-only. Only 7 of ~250 files use focus-visible. | P1 | SearchResultItem.vue:2-13; WordListRow.vue:2-12; grep focus-visible→7 | WordListRow→`<button>`/role + keydown; focus-visible ring synced with roving; adopt glass-ui focus-ring token. | A.W2 a11y + glass-ui focus-ring adoption |
| SearchResultItem + WordListRow | Pills hardcode COLD Tailwind defaults (emerald-600/cyan-600/orange-600 on /10, mastery-silver hsl on /15) → sub-4.5:1 at 2xs sizes on warm paper; `bg-orange-400` hot dot has no aria. | P1 | SearchResultItem.vue:154-172; WordListRow.vue:82-108,41 | Warm `--section-color-*`/jewel tokens + stronger text token for tiny pills; sr-only label on the dot. | A.W1 (extends row 138 to consumer hardcodes) |
| virtual/* | TWO parallel virtual-scroll stacks: the 785-LoC hand-rolled `composables/virtual/*` AND a direct `@tanstack/vue-virtual` in WordList — two windowing engines, two height models. | P2 | useVirtualSectionWindow.ts:71; useVirtualGrid.ts:1; WordList.vue:78,157 | Converge on tanstack (already a dep, proven via useVirtualGrid); retire the 785-LoC fork. | A.W2 (extends row 144) |
| useVirtualSectionWindow | `SESSION_HEIGHT_CACHE` module Map keyed by id is NEVER evicted (no cap, no clear on nav/unmount) → unbounded memory on the hottest surface; recompute runs in a bare rAF (no yield); consumer forces `as unknown as`. | P2 | useVirtualSectionWindow.ts:54,178,243-249; DefinitionContentView.vue:230-241 | LRU cap / per-instance + clear on unmount; route through useYieldToMain (or delete via tanstack converge). | A.W2 (useYieldToMain + cache bound) |
| transitions.css | PRM blocks are enumerated by class-name so several animations ESCAPE: `result-list-enter/leave/move`, `.animate-cluster-in`, YoshiAvatar golden-shimmer, ShimmerText sweep all lack PRM. | P2 | transitions.css:4-21,80,113,174; DefinitionContentView.vue:278-280; YoshiAvatar.vue:99; ShimmerText.vue:49 | Replace enumerate-by-name with one global `@media PRM{*{…}}` base (adopt glass-ui reset). | A (global PRM base) |
| theme.css | Paper-texture token DRIFT: `--paper-clean-texture` redeclared ONLY under `.dark` (60×60) while light inherits glass-ui's 200×200; consumers further disagree (`.word-card` hardcodes 60px) → grain scale visibly changes between modes/cards. | P2 | theme.css:168-171; glass-ui tokens.css:853,998; index.css:184; WordList.vue:218 | Consume glass-ui's light-dark()-native texture for both modes; replace the 60px hardcode. | A.W1 (extends row 139 to textures) |
| index.css | `.dialog/popover/card-surface` stack feTurbulence + `background-blend-mode:multiply` + backdrop-blur; the code COMMENT admits this is GPU-expensive in Safari yet only `.word-card` gets the cheap path — general cards keep the triple-stack (iOS Safari paint root). | P2 | index.css:157-185; theme.css:168 | Drop blend-mode on blurred surfaces; texture via a single non-blurred `::before`; reserve blur for the fill. | A.W2 (atmosphere/perf) |
| index.html | The Fraunces CDN loads BOTH italic + roman across the FULL opsz+wght ranges (huge woff2) while the app renders only roman wght 900; `display=swap` causes a FOUT then typewriter → double reflow on the LCP element. | P2 | index.html:62; AnimatedText.vue:158 | Self-host the full-axes Fraunces from glass-ui; interim drop the italic axis + `font-display:optional`. | A.W1 (folds into row 140 + glass-ui self-hosted Fraunces) |

### sudoku (live :5250 — BLANK) — 7 net-new

> **MASTER correction:** the M1 "keyframes spec-stale" P1 note must ELEVATE P1→P0 and expand from 2 cited sites to **4 runtime-breaking VALUE imports** (the 5th, `HandwrittenGlyph.vue:7`, is `import type` → harmless). The idiomatic fix is `await loadAnimationEngine()`, NOT a NumericAnimation rewrite (different ctor).

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| keyframes barrel | **BLANK SCREEN root** — 4 value imports resolve `Animation`/`CSSKeyframesAnimation` against the symlinked 2.2.0 barrel that no longer exports them → SyntaxError at module-eval → `mount()` never runs. | **P0** | usePathAnimation.ts:2; glyphAnimations.ts:7; VineBorder.vue:3; DoodleAccents.vue:4; dist/keyframes.js:510 | `const { Animation } = await loadAnimationEngine()` (cache the promise) at the 4 sites. | M1 (elevate P0; 4 sites + async-boundary refactor) |
| type/value asymmetry | `typecheck` reports GREEN over the dead runtime — the dts re-exports `Animation` as a TYPE while the .js barrel omits it as a VALUE; no build-time guard catches a value-vs-type divergence in a symlinked dep. | P1 | keyframes.d.ts:134 vs dist/keyframes.js:510; HandwrittenGlyph.vue:7 (type-only, safe) | Add a smoke gate that evaluates the module graph (playwright `import()` assertion). | M1 (acceptance gate) |
| useLineBoil ×3 | Grid boil + control-divider boil + DarkModeToggle sun/star boils are rAF/timer JS with NO PRM gate; the global CSS PRM `@media` only touches CSS animation props. `useReducedMotion.ts` exists but is never consulted here. | P1 | HandDrawnGrid.vue:33-39; ControlPanel.vue:45-49; DarkModeToggle.vue:113-117; index.css:211-216 | Gate every useLineBoil on the reactive `useReducedMotion()`; add a reactive `paused`/`enabled` getter to pencil-boil so fourier/bbnf inherit it. | WAVE C motion + shared pencil-boil PRM ask |
| SvgFilters | Reads PRM ONCE as a plain const (no `change` listener) for the feTurbulence boil → enabling PRM after load keeps it running; diverges from the reactive composable (STALE guard, not absent). | P2 | SvgFilters.vue:16-18,24 | Replace with reactive `useReducedMotion()` + watch. | WAVE C motion (unify on the reactive composable) |
| SudokuCell | The interactive cell is a transparent `<input opacity-0>` with NO aria-label/value/position; wrapper has no role=gridcell/grid; `isActive=isHovered||isFocused` merges focus+hover so keyboard focus is indistinguishable; only a 1px @30% focus ring. | P1 | SudokuCell.vue:98-109,29,125-128; index.css:190-194 | aria-label (row/col/value) + role=grid/row/gridcell; distinct `:focus-visible` ring (≥2px, full --color-ring). | WAVE C a11y (board ARIA + focus-visible) |
| OptionSelector | Size/difficulty controls are plain `<button>`s with NO role=radiogroup/radio/aria-checked (selected = scribble underline only, invisible to AT), no arrow-key roving, no `:focus-visible`; hover applies `filter:url(#wobble-*)` (JS turbulence) with no PRM fallback; tooltips show on `:hover` only. | P1 | OptionSelector.vue:29-45,67-70,54-56; ControlPanel.vue (.icon-btn hover) | radiogroup + roving + aria-checked; focus-visible ring; gate hover filters on PRM; tooltip on `:focus-visible`. | WAVE C a11y + motion |
| HandwrittenGlyph | Every SOLVED glyph starts an iterationCount:Infinity wiggle that `setAttribute('d')` per frame + carries `filter=url(#grain-static)` → on a 16×16 board up to 256 perpetual rAF loops each re-tessellating + re-running an feTurbulence filter (INP/raster root). | P2 | glyphAnimations.ts:64,66-73,82-83; HandwrittenGlyph.vue:57-79,205; pencilConfig.ts:179 | Cap concurrent wiggles (subset / IntersectionObserver), single shared clock, drop grain filter on active glyphs. | WAVE C perf (solved-board animation budget) |

### bbnf-buddy (no tranche dir; glass-ui bump = cross-cutting) — 8 net-new

> **MASTER corrections:** (1) keyframes-break for bbnf is NEGATIVE today (it pins a real 2.1.1 copy, NOT the symlinked 2.2.0) — but the 3.1.1-bump row must gain a keyframes-barrel pre-flight gate since the bump crosses the 2.2.0 Animation-move. (2) The P0 PRM row UNDER-SCOPED the motion surface — it listed 4 RAF loops but missed the CSS-transition layer, the pencil-boil (5th loop), the never-pausing runtime loop, and the WCAG-2.2.2 favicon. The loop count is **15, not 4** (per dive 11).

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| keyframes dep | bbnf does NOT break (real published 2.1.1 copy where Animation is still barrel-exported; vue-tsc exit 0, app boots). LATENT: the 3.1.1-bump wave crosses 2.2.0+ → must audit/migrate every keyframes import in the same commit or inherit sudoku's blank screen. | P2 | node_modules/@mkbabb/keyframes.js v2.1.1; runtime.ts:40-43 | Add a keyframes-barrel pre-flight gate to the bump wave. | MASTER bbnf bump row (line 156) |
| runtime.ts | Main mascot loop NEVER stops — when paused it still `render(frozenTime)` + reschedules rAF forever; ZERO visibilitychange/IntersectionObserver gate (only useFavicon pauses on hidden). Perf/battery root the PRM-only row missed. | P2 | runtime.ts:710-716; grep visibilitychange→NONE; useFavicon.ts:99-102 | When paused: render once + cancelAnimationFrame; add visibility gate (glass-ui useRAFLoop after 3.1.1). | MASTER bbnf P0 row (widen to {PRM,paused,hidden}) |
| animations.css + vendored glass-ui.css | A SEPARATE PRM hole the P0 row missed: animations.css declares 5 named Vue transitions with ZERO `@media PRM`, and vendored glass-ui 2.0.0 dist/glass-ui.css ships ZERO PRM queries → even glass-ui's own CSS transitions run under reduce. | P1 | animations.css:1-83; grep PRM src/→0; glass-ui 2.0.0 css→0 | Global `@media PRM` carve in animations.css; 3.1.1 bump brings glass-ui's PRM-disciplined CSS. | MASTER bbnf P0 row (broaden to CSS-transition layer) |
| preset.css | Hand-rolled light-mode `--muted-foreground hsl(24 5% 46%)` is 4.41:1 on bg / 4.49:1 on card — BELOW AA 4.5:1 — and paints the smallest text. STATIC token miss (MASTER captured value.js's DYNAMIC hole, not this). | P1 | preset.css:28,25,29; PreviewGrid.vue:51; App.vue:441 | Delete the hand-rolled palette in the 3.1.1 bump; interim drop lightness to ~42%. | MASTER bbnf bump row (motivates deleting the palette) |
| KeyframeTimeline | Timeline dots are bare `<div @pointerdown>` — no tabindex/role/keydown/aria-label → keyframes entirely keyboard-INACCESSIBLE (can't select/retime/delete). WCAG 2.1.1. | P1 | KeyframeTimeline.vue:166-176,186-196,280-300 | Each dot a real `<button aria-label aria-current>` + keydown retime/select/delete + focus-visible. | NET-NEW — bbnf editor-a11y pass (after bump) |
| ToolsLayer + MagnetToolButton | Tool selection state is VISUAL ONLY (`is-active`) with NO aria-pressed/aria-current on toggle + mutually-exclusive tool buttons; grep aria-pressed/role=radiogroup/toolbar = 0. | P2 | ToolsLayer.vue:116-120,159-165; MagnetToolButton.vue:42-45 | aria-pressed on toggles + role=radiogroup/aria-checked on tool group; glass-ui DockIconButton gains a selected/pressed prop. | MASTER DockIconButton row (line 65) — add selected/aria-pressed seam |
| useFavicon | The animated favicon re-serializes the mascot SVG at ~6.7fps with ZERO PRM gate — recognized WCAG 2.2.2 (Pause/Stop/Hide) concern; pauses only on hidden, never on PRM. | P2 | useFavicon.ts:29,75-82,108,99-102 | Under PRM paint once (resting frame), don't start the loop. | MASTER bbnf P0 row (favicon needs the static-frame treatment, WCAG-2.2.2) |
| PreviewGrid + pencil-boil | `transition-all` on a button whose child SVG `:d` path is rewritten EVERY ~6.7fps preview tick → transitions a constantly-morphing path. Pencil-boil is a perpetual 8fps (375ms 3-frame) stop-motion with no PRM gate (5th continuous-motion surface). | P2 | PreviewGrid.vue:34; useEmotionPreviews.ts:250; runtime.ts:754-758; pencil-boil.ts:48,80 | `transition: background-color,box-shadow,outline-color` (never the morphing SVG); freeze boilFrame at 0 under PRM; tokenize the 125ms cadence. | MASTER bbnf transition:all row (157) + P0 PRM row (boil = 5th surface) |

### cross-cutting dep-drift + dev-health — 12 net-new

> All NET-NEW vs MASTER (which touches these only at standard-definition altitude with no file:line). See §C for the runtime/orchestration breaks; the dev.sh family + version-pin reconciliation are summarized in §B (epidemics 4 + 6) and §D (NEW K.W6 dep-cohort wave).

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| sudoku | The blank-render root, bounded: 4 value-import sites + the surviving-export list (Animation/CSSKeyframesAnimation/AnimationGroup/getTimingFunction/… all moved to the lazy engine chunk). | **P0** | usePathAnimation.ts:2 etc.; engine-DuAFoqZF.js:699 | Migrate exactly as fourier did (`loadAnimationEngine`). | K.W6 dep-cohort + sudoku migration |
| sudoku pkg | Compound silent drift: `@mkbabb/value.js:^0.5.0` resolves 0.10.0 (5 minor bumps ahead); `keyframes:^2.0.0` resolves 2.2.0 — the symlink bypasses semver; a clean install would break differently. | P1 | package.json; node_modules symlinks | Bump specs to `^2.2.0`/`^0.10.0` to match symlink targets. | K.W6 dep-cohort (version-pin reconciliation) |
| fourier dev.sh | Self-shutdown via negative array subscript (see §C / fourier §A row). | **P0** | dev.sh:80 (bash 3.2.57) | Scalar PID capture / port-probe idiom. | W6 deploy dev.sh standard |
| sudoku dev.sh | cleanup crash: unguarded `for p in "${PIDS[@]}"` under `set -u` → `unbound variable` if the backend fails before any PID is appended → teardown orphans the frontend. | P1 | dev.sh:40,43,3 | `"${PIDS[@]:-}"` + `[[ -n "$p" ]]` guard (also latent in speedtest:137). | W6 deploy dev.sh standard (bash-3.2 sub-spec) |
| fourier/sudoku/words/muster | Ctrl+C hang: all idle on a bare `wait` instead of a sleep-loop; on bash 3.2 the trap is DEFERRED while blocked in `wait`. value.js/speedtest already solved it. | P2 | fourier:87; sudoku:71; words:115; muster:6 | `while true; do sleep 1; done`. | W6 deploy dev.sh standard |
| muster dev.sh | Blunt teardown: 6-line script with `trap 'kill 0' EXIT` (blasts the whole pgid), no kill_tree, no port probe, no Docker awareness — least-robust in the constellation. | P2 | dev.sh (full file) | Adopt the value.js standard template. | W6 deploy dev.sh standard (primary adopter) |
| value.js/deploy | SIGPIPE foot-gun: under `pipefail`, `cmd | grep -q PAT || die` — `grep -q` closes the pipe → producer gets SIGPIPE (141) → a SUCCESSFUL match can fire `|| die`. | P2 | deploy.sh:78-80; dev.sh:130; deploy-hook.sh:210 | Capture-then-match: `out=$(cmd); grep -qE … <<<"$out"`. | W6 deploy script-hardening standard |
| deploy | Provenance gap: value.js dev.sh declares "Adopted from deploy/templates/dev.sh" but **deploy/templates/dev.sh DOES NOT EXIST** — the constellation standard the mature scripts cite has no canonical source, so each app's dev.sh independently drifted. | P2 | value.js dev.sh:2-3; `find deploy -name dev.sh`→empty | Promote value.js's dev.sh into `deploy/templates/dev.sh` as the canonical template (SHAPE variants); re-derive the rest. | W6 deploy dev.sh standard (the missing artifact) |
| keyframes.js | Lib dist ships `dist/_redirects` (a Cloudflare SPA fallback) inside `files:["dist"]` → lands in every consumer's node_modules — a demo gh-pages artifact bleeding into the library build (demo + library vite builds share an output dir). | P2 | keyframes.js/dist/_redirects; package.json files | Separate demo gh-pages output from library dist, or exclude `_redirects`. | keyframes A build-hygiene |
| keyframes.js | Latent local-dev break: the moved engine is a CONTENT-HASHED dynamic-import chunk (`engine-DuAFoqZF.js`); the hash changes per rebuild → a consumer's Vite optimizeDeps pre-bundled against the OLD hash 504s after a sibling `build:watch`. The symlink-dev topology is maximally exposed. | P2 | dist/keyframes.js (`import("./engine-…")`×3); speedtest dev.sh:200 | Pin the dynamic-import chunk to a STABLE name (`engine.js`); or `optimizeDeps.exclude` keyframes. | keyframes A build-hygiene + K.W6 dep-cohort |
| fourier | Stale glass-ui COPY at 3.1.0 (spec `^3.1.0`) while source is 3.1.1 — fourier won't see 3.1.1-only changes (dts-cohort, asideSide prereq groundwork) until reinstalled. Invisible because both satisfy `^3.1.0`. | P2 | fourier web/node_modules glass-ui v3.1.0; spec `^3.1.0` | `npm install` (or symlink) to pull 3.1.1; the asideSide prereq needs 3.1.1+. | J config wave prereq + K.W6 dep-cohort |
| bbnf-buddy | Frozen-by-pin time-bomb: the ONLY app with a keyframes 2.1.1 COPY (not symlinked 2.2.0); 2.1.1's barrel STILL exports `getTimingFunction` so runtime.ts/easing.ts VALUE imports are safe today PURELY by the stale copy. `^2.1.1` permits 2.2.0 → the moment it reinstalls/symlinks it inherits sudoku's exact break. Latent P0. | P1 | package.json `^2.1.1`; runtime.ts:40-43; easing.ts:20-25 | In the bump, migrate `getTimingFunction` to `loadAnimationEngine()` (NumericAnimation stays top-level) BEFORE bumping keyframes. | bbnf bump wave (keyframes-2.2 value-import migration) |

### completeness-critic (secondary surfaces) — 13 net-new

> Examines the surfaces the first pass never opened (panes, admin, dashboard, survey, equation/morph/gallery, 404/offline). Several SHARPEN existing rows; the meta-finding (instrumented-run gap) is the most consequential.

| Surface | Net-new finding | Sev | Evidence (file:line) | Idiomatic fix | Folds into |
|---|---|---|---|---|---|
| value.js — 9 dock panes | The ENTIRE secondary surface renders on the vendored shadcn-vue tree (60 `@components/ui/*` imports vs 36 glass-ui), NOT glass-ui — the first pass audited only the picker pane (which IS glass-ui). For a glass-ui-first consummation this is the single largest adoption gap and was entirely unexamined. | P1 | AdminPane:65/BrowsePane:113/ExtractPane:76/GeneratePane:3/MixPane:3/GradientPane:3/PalettesPane:90/ConfigSliderPane:17/AboutPane:42 | Migrate pane-content primitives onto glass-ui at the root, staged per pane. | K (glass-ui-first) — WIDEN K.W3/K.W4 beyond the picker to the pane-content layer |
| value.js — dynamic bg | The app writes `--background` to the live color but NEVER re-derives `--ring`/`--foreground`/`--muted-foreground` against the tint — a focus ring inheriting static `--ring` can fall below 3:1 (WCAG 2.4.7 + 1.4.11), distinct from the text-contrast row. | P1 | useAppColorModel.ts:74-79; grep `--ring` dynamic→0 | Add `--ring`/focus-indicator to the booked `contrastForegroundFor(bg)` utility's emitted channels. | K.W4 (add --ring channel to contrastForegroundFor) |
| value.js — ExtractPane/MixPane | Two `animate-spin` spinners during quantize/mix with no PRM (demo PRM exists only in blob files). | P2 | ExtractPane.vue:33; MixPane.vue:100 | Global PRM carve in animations.css; adopt glass-ui feedback/skeleton+progress. | K motion (extend PRM beyond the blob) |
| value.js — AdminAuthGate | Admin token form clears on a rejected token with NO aria-invalid/error/`:user-invalid` — wrong token silently clears. Rides shadcn Input/Button. | P2 | AdminAuthGate.vue:5-23,32-58 | `:user-invalid` + aria-invalid/describedby on the glass-ui Input variant; migrate the form. | K (form-a11y row → admin surface) |
| fourier — spinners (10) | 10 `animate-spin` loaders ungated under PRM (the only carve is scoped to `[role=tabpanel]`); useFourierMorph also ungated. | P2 | EquationView:223,240; VisualizationView:157; EquationPanel:108; ContourSettings:314; gallery panels; style.css:91 | Global `@media PRM{.animate-spin{animation:none}}`. | J motion (widen epicycle-PRM to a global spin/loop carve, 10 sites + morph) |
| fourier — /equation states | Full 4-state machine never analyzed; error banners use red-on-red literals (not tokens) + no role=alert/aria-live; empty state is timid italic. | P2 | EquationView.vue:220-247,243-245,229-233,320-324 | `--destructive` tokens; role=alert + aria-live; glass-ui empty-states composition. | J (equation surface — net-new row) |
| speedtest — StatsCards | Dashboard metric values snap (static Math.round/.toLocaleString) while the SAME repo uses useAnimatedNumber for the meter — the count-up vocabulary is absent on the dashboard. | P2 | StatsCards.vue:39,42-49; MeterColumn.vue:201 | Wrap the 3 values in useAnimatedNumber (PRM-bracketed). | AT (extend dashboard-reveal row to count-up) |
| speedtest — dashboard | An entire dashboard surface (StatsCards/ResultsTable/DashboardMap/survey) was never captured; ResultsTable sortable columns carry no aria-sort; the per-row stagger is "proposal #19" authored-as-comment, not shipped. | P2 | ResultsTable.vue:90-93,143; DashboardMap.vue:100-107 | aria-sort on DataTable headers; ship proposal-#19 under PRM; capture these surfaces next round. | AT (dashboard a11y + capture-gap flag) |
| cross-repo | speedtest's survey ALREADY implements the `:required`+`:user-invalid` form-a11y pattern value.js books as net-new — value.js should ADOPT the idiom, not invent it. | P2 | SurveyField.vue:27-37; SurveyStep.vue:7-8 vs useAppColorModel.ts:61 | Model the glass-ui Input invalid variant on speedtest's survey idiom; one pattern across value.js/words/survey. | AS (Input invalid variant — note the existing proof-of-pattern) |
| words — offline/error | `usePWA.isOffline` is plumbed but NEVER consumed by any .vue → offline is silent; NotFound heading off the φ-ladder + cold ink; ErrorBoundary has no role=alert/aria-live. | P2 | usePWA.ts:15,287; grep isOffline .vue→0; NotFound.vue:3; ErrorBoundary.vue:3-34 | Surface isOffline as a glass-ui Notification banner; re-rung the 404 heading + warm ink; role=alert on ErrorBoundary. | words A (net-new offline/error rows; corrects SW-rot framing) |
| keyframes demo | The demo SCENE pages (/easing /spring /amiga /square) have ZERO PRM in source (all PRM strings are in dist/ only) — the shop-window the user sees animates fully under reduce, no page-level fallback. | P1 | grep PRM demo/**→empty; EasingScene.vue:25,92; SpringScene.vue:25,104 | Each scene page requests a static resting pose once the engine-base gate lands. | A.W4 (widen heavy-engine-PRM to the demo scene pages) |
| bbnf — editor | The grammar editor (EditorPanel/JsonPanel) surfaces parse errors with ZERO role=alert/aria-live/aria-invalid; 15 RAF/animate-spin sites confirm loops beyond the 4 mascot ones. | P2 | EditorPanel.vue+JsonPanel.vue; grep role=alert→0; grep RAF→15 | role=alert + aria-live on the parse-error region; aria-invalid on the editor; PRM gate must cover all 15 loops. | bbnf bump wave (editor a11y; correct loop count to 15) |
| constellation | **ZERO instrumented runs** — every verdict is eyeball+grep+code-read; the cited CLS (speedtest 0.3228, muster 0.0729)/INP/contrast magnitudes were NOT freshly measured; no axe-core/Lighthouse/LoAF against any of the 12 live local ports (all confirmed reachable). | P1 | MASTER §A evidence is file:line/grep; W7 verified cites not metrics; no Lighthouse/axe report in grand-audit/ | Add a W-tier instrumented pass (axe-core via @axe-core/playwright + Lighthouse CI + LoAF) against each live port. | W6/deploy + each repo's W6 e2e/axe — promote the fourier-only J.W6 axe item to a CONSTELLATION-WIDE gate |

---

## §B — Cross-cutting EPIDEMICS

The high-leverage meta-findings — patterns recurring across repos that resolve to ONE root fix each.

### 1. PRM-on-JS/canvas-RAF-loops (the biggest theme)

The CSS `@media (prefers-reduced-motion)` guard everywhere covers ONLY CSS animation/transition durations — it does NOT reach JS/canvas/rAF loops, which is where the heaviest, most vestibular-hostile motion lives. W8 enumerated the ungated JS/canvas loops across the constellation:

| Repo | Ungated JS/canvas RAF loops (W8 net-new) |
|---|---|
| value.js | goo-blob PRM resting-pose gap (renders bare body); watercolor-dot border-radius morph; mix-animation canvas; ExtractPane/MixPane spinners |
| fourier | /morph engine; /equation curve-transition (500ms); epicycle hover-scale; epicycle golden-shimmer; tip-glow wall-clock pulse; rainbow loading-bar; 10 `animate-spin` spinners; scoped Vue `<Transition>` enters — **a 5+-surface pattern, MASTER named only the epicycle clock** |
| keyframes.js | /amiga (4 infinite heavy children); /cube; /square; /easing; /spring; demo scene cross-fade; light-engine default-OFF gate (`respectReducedMotion:false`) |
| muster | ScoreFairnessField SVG `r` transition; EliminatedFold chevron; PresetRail; RankedVerdict |
| words | AnimatedText liftDown (per-char infinite); result-list transitions; .animate-cluster-in; YoshiAvatar golden-shimmer; ShimmerText sweep (enumerate-by-name PRM lists let scoped anims escape) |
| sudoku | 3× useLineBoil (grid/divider/toggle); SvgFilters feTurbulence (STALE snapshot); per-cell infinite glyph wiggle; hover wobble filters |
| bbnf-buddy | mascot runtime loop (never stops on pause/hidden); holo-rotation; pencil-boil 8fps; animated favicon (WCAG 2.2.2); emotion previews; CSS-transition layer + vendored glass-ui.css (0 PRM) — **count is 15, not 4** |
| glass-ui | SortableList shimmer/ghost; InfiniteScroll + ConfirmDialog bare `animate-spin`; Pulse ring frozen-arc |

**ONE root fix · owner keyframes.js (engine) + glass-ui (composable) + per-app carve.** Move the PRM gate into keyframes.js's shared `RAFPlayback` loop and DEFAULT `respectReducedMotion:true` (opt-out, a11y-safe) — glass-ui useSpring/useAnimatedNumber/useNumericTransition then inherit PRM-correct motion for free; add the heavy-engine base gate (Animation/AnimationGroup) for the demo + external consumers; expose a reduced-motion-aware RAF primitive (glass-ui `useRAFLoop` + `useIntersectionPause`) that pauses off-screen/tab-hidden; each app adopts a global `@media PRM{*{…}}` base + the composable instead of enumerate-by-name lists.

### 2. Frozen-spinner

The PRM guard sets `animation-iteration-count:1 !important`, which freezes Tailwind `animate-spin` (`spin … infinite`) after exactly ONE 0.01ms rotation → a static non-rotating icon during long loads that reads as a HUNG UI. **Sites:** value.js 26 (`animate-spin` across browse/extract/mix/admin/color-name/slug) + fourier 10 (equation/viz/gallery-admin) + value.js ExtractPane/MixPane (2) + glass-ui InfiniteScroll + ConfirmDialog (bare, run under PRM). **ONE root fix · owner glass-ui:** ship one motion-safe Spinner recipe (`motion-safe:animate-spin` + `role=status` + accessible name) backed by a Pulse/Skeleton opacity-breath busy-variant (opacity is vestibular-safe); consumers swap raw `animate-spin` for it.

### 3. P9 utility-distribution gap

glass-ui-component-used utilities (the rounded-panel family + others) silently no-op in consumers because the consumer's stylesheet doesn't carry the glass-ui utility layer / token. W8 found additional silently-unstyled surfaces beyond the MASTER's named one:

- **muster TravelMatrix** strips MetricCell's `glass-wash` via a scoped host class (per-instance override) — the wash utility doesn't reach it cleanly → needs a MetricCell `appearance="bare"` variant.
- **muster** `var(--duration-fast, 120ms)`/`150ms` fallbacks are DEAD (token resolves 200ms) across 6 components — the author's value silently no-ops; `--reveal-stagger-step` token declared with ZERO consumers.
- **fourier** stale glass-ui 3.1.0 copy → 3.1.1-only utilities/tokens silently absent.
- **bbnf-buddy** vendored glass-ui 2.0.0 ships ZERO PRM utilities → the carve never applies.
- **value.js** `--popover`/`--active-tab-color` mode-forked literals + `--ring` never re-derived against the dynamic bg.

**ONE root fix · owner glass-ui + dep-cohort:** every consumer on the 3.1.1 cohort (so the utility/token layer is present), plus glass-ui ships the missing component variants (MetricCell bare, Input invalid) so consumers stop reaching into vendored roots; net-new tokens (`--dock-fg-on-aurora`, `--configurator-aside-min`) so consumers stop hand-setting magic numbers.

### 4. Dep-drift runtime breaks

The keyframes.js 2.2.0 barrel split (Animation/CSSKeyframesAnimation/AnimationGroup/getTimingFunction moved to a lazy engine chunk) + a SEMVER VIOLATION (shipped MINOR for a documented breaking change) means `^2.x` consumers auto-resolved to the broken barrel. **Affected:** sudoku BLANK (4 value-import sites, the live break); bbnf-buddy LATENT P0 (safe ONLY by a stale 2.1.1 copy; `^2.1.1` permits 2.2.0 → inherits the break on reinstall via `getTimingFunction`); fourier IMMUNE (already migrated to `loadAnimationEngine` — the reference pattern). Compound silent drift: sudoku `value.js:^0.5.0` resolves 0.10.0; fourier glass-ui copy stuck 3.1.0. The symlink-dev topology + content-hashed engine chunk add an optimizeDeps-504 vector. **ONE root fix · owner keyframes.js + a NEW K.W6 dep-cohort wave:** re-release the boundary as **3.0.0** + make the dts type-only (so a value import fails at COMPILE time); migrate every consumer of a moved export to `loadAnimationEngine()` (sudoku P0, bbnf P1-latent); reconcile every package.json spec to its symlink target; pin the engine chunk to a stable name. The dts type-only fix is the structural enforcement (types catch it, not a grep).

### 5. Form / interactive-primitive a11y (keyboard-operability + aria-invalid)

Hand-rolled `role=`/`@click`-interactive controls across the constellation lack the keyboard + ARIA contract that reka-ui-backed primitives get for free. **Sites:** value.js (contenteditable no aria-invalid; gradient stops keyboard-inoperable; AdminAuthGate no invalid feedback); fourier (coeff-hover mouse-only; timeline caret hover-only; equation error no role=alert); glass-ui's OWN components (DataTable headers/rows mouse-only + no aria-sort; BouncyTabs/UnderlineTabs/CarouselDots no roving/focus-visible; SortableList no keyboard reorder; StatusDot color-only); speedtest (admin-login no autofocus; charts no aria-live; ResultsTable no aria-sort; delete no confirm); muster (FirstRunWalk no Escape; useReRank keyboard never settles); words (WordListRow plain div; SearchResultItem no focus-visible; ErrorBoundary no role=alert); sudoku (SudokuCell no aria/grid; OptionSelector no radiogroup; no focus-visible anywhere); bbnf (KeyframeTimeline keyboard-inaccessible; tools no aria-pressed; editor parse-fail no role=alert). **ONE root fix · owner glass-ui:** ship the Input `invalid` variant (`:user-invalid`+aria-invalid, modeled on speedtest's already-working survey idiom), harden every hand-rolled custom component to the reka-ui keyboard+focus contract, and make the glass-ui-first mandate explicitly REQUIRE that contract; apps adopt native `:user-invalid`/aria + role=alert (no JS toasts).

### 6. dev.sh portability + the missing canonical template

The `deploy/templates/dev.sh` the mature scripts cite as their source DOES NOT EXIST → each app's dev.sh independently drifted, and three (fourier/sudoku/muster) carry bash-3.2 breaks the two mature ones (value.js/speedtest) already fixed. **Bugs:** fourier negative array subscript (self-shut, P0); sudoku unguarded `${PIDS[@]}` under `set -u` (cleanup crash); 4 scripts hang on bare `wait` (deferred trap); muster `kill 0` blunt teardown; SIGPIPE `grep -q || die` foot-gun. **ONE root fix · owner deploy (W6):** promote value.js's dev.sh into `deploy/templates/dev.sh` as the canonical template (SHAPE variants) + a standing bash-3.2-portability sub-spec (no negative subscripts; guard `${arr[@]}` under `set -u`; sleep-loop not bare wait; capture-then-match not pipe|grep-q under pipefail); re-derive fourier/sudoku/muster/words.

---

## §C — Runtime / build BREAKS (P0s)

Every blank-screen / console-error / build-fail / failed-fetch grounded in a live port or a build artifact.

| # | Repo / surface | Break | Sev | Evidence | Status |
|---|---|---|---|---|---|
| C1 | **sudoku :5250** | BLANK SCREEN — 4 value imports of `Animation`/`CSSKeyframesAnimation` against the symlinked keyframes 2.2.0 barrel (which moved them to the lazy engine chunk) → SyntaxError at module-eval → `createApp().mount()` never runs. HTTP serves 200; the JS graph is dead. | **P0** | usePathAnimation.ts:2, glyphAnimations.ts:7, VineBorder.vue:3, DoodleAccents.vue:4; live `/@fs/.../keyframes.js/dist/keyframes.js` lacks Animation; capture sudoku-5250.png blank | The KNOWN one, now root-caused + bounded. Fix: `loadAnimationEngine()` + bump specs. Prod sudoku.babb.dev is 200 (built against a real 2.0.0). |
| C2 | **fourier scripts/dev.sh** | Local dev stack SELF-SHUTS — `kill -0 "${PIDS[-1]}"` negative subscript errors `bad array subscript` under bash 3.2.57 + `set -euo pipefail` → trap cleanup kills the just-started API. Orchestration-time, not serve-time. | **P0** | dev.sh:80; reproduced in `/bin/bash -c 'a=(1 2 3); echo ${a[-1]}'` | NEW (MASTER booked dev.sh at standard altitude with no repro). fourier currently runs on :5260 via a path that avoided this. |
| C3 | **words service-worker.js** | PWA install FAILS in prod — `cache.addAll()` is atomic; `STATIC_ASSETS` lists `/assets/index.css` (hashed name) + `/assets/themed-cards.css` (never built) → one 404 rejects install → the entire offline/cache layer is dead. INFERRED from code (words is VPN-gated, HTTP 000). | **P0** | service-worker.js:7-13,19; dist has index-NonSZd9v.css, no themed-cards.css | NEW. App still renders (network-first); offline/precache is silently dead in prod. Companion: all `/`-rooted favicon/manifest/splash links 404 under `/words/`. |

**Non-breaks confirmed (prevent re-raising):** keyframes.js 2.2.0 dts emits the moved heavy classes as VALUE decls so a value import passes typecheck then crashes — a CI BLIND SPOT (not a current break but the mechanism behind C1). glass-ui dist was momentarily JS-only (the speedtest dev.sh ran `vite build` without `emit-types`) leaving the declared `types` exports dangling for consumers — transient orchestration, fixed by running the full build. value.js :9001/api :3001, fourier :5260 (api down → graceful error states), speedtest :9143, glass-ui :5210, keyframes :5240, slides :5220, muster :5176, bbnf :5230 all serve 200 with no module-graph break. fourier is IMMUNE to C1 (already on `loadAnimationEngine`); bbnf is safe TODAY only via a stale 2.1.1 copy (latent — see §B.4).

---

## §D — Spec fold map

Per authored tranche/wave, the W8 net-new items folding in. **NEW lanes flagged.**

### value.js — K (front) / L (api)
- **K.W3 (Metaballs/BlobDot primitive):** goo-blob PRM resting-pose (not bare body); watercolor-dot BlobDot SVG tier PRM; frag-shader hue-wrap + double-brightness; `mergeRate`→`mergeStaggerScale` during the useSpring mood rewrite.
- **K.W4 (aurora/contrast):** add `--ring`/focus-indicator channel to the booked `contrastForegroundFor(bg)` utility.
- **K motion:** pane-slide enter/leave curve unification; spectrum-dot SVG-filter perf root + dead rule; demo-local JS-RAF PRM gate (mix-animation); extend PRM beyond the blob to all demo loops.
- **K (a11y / form):** gradient-stop keyboard ops + readable hint; ColorInput contenteditable aria-invalid + badge/send occlusion; AdminAuthGate invalid feedback.
- **K (glass-ui-first) — WIDEN scope:** the 9 dock panes render on shadcn-vue, not glass-ui (60 vs 36 imports) — the single largest adoption gap; widen K.W3/K.W4 beyond the picker to the pane-content layer + adopt glass-ui feedback/skeleton+progress for the frozen-spinner fix.
- **K (cleanup):** demo type-hygiene (`: any`, `:key`, dead inject).
- **L (api):** `/admin/tags` bare-array → envelope.

### fourier — J
- **J motion:** /morph PRM; /equation curve-transition PRM; epicycle hover-scale + shimmer PRM; tip-glow wall-clock + PRM; rainbow loading-bar PRM; scoped `<Transition>` PRM tail; the 10 `animate-spin` spinners (global spin carve). **The single "epicycle RAF ignores PRM" row becomes a 5+-surface pattern → fix = the shared RAF primitive, not inline guards.**
- **J useTextHighlight:** /equation coeff-hover keyboard + Anchor Positioning; /paper `highlightFuzzy` 2nd consumer (hardens the ≥2-consumer gate from inside fourier).
- **J empty-state/gallery:** placeholder dark-mode contrast + token-blind ink; gallery error-masquerading-as-empty (role=alert + Retry); equation 4-state machine (destructive tokens + role=alert + empty-states composition).
- **J motion/a11y + glass-ui token:** floating play button white-on-light contrast + invisible focus ring (consumes `--dock-fg-on-aurora`).
- **J a11y tail (J.W6):** timeline caret on `:focus-within`.
- **deploy W6:** dev.sh negative-subscript (the concrete repro for the booked row).
- **J config wave PREREQ:** refresh fourier's stale glass-ui 3.1.0→3.1.1 before adopting asideSide.

### glass-ui — AS
- **AS — Data-category a11y wave (NEW lane):** DataTable sortable-header + clickable-row keyboard + aria-sort.
- **AS — Tabs-family a11y wave (NEW lane):** BouncyTabs/UnderlineTabs/CarouselDots roving + focus-visible + correct role; BouncyToggle token-bind + reflow.
- **AS — SortableList a11y + PRM (NEW lane):** keyboard reorder + aria-live; shimmer/ghost PRM.
- **AS — spinner/feedback consistency:** motion-safe Spinner recipe + InfiniteScroll aria-live; Pulse ring PRM resting pose; ScrollingText `:focus-within`; StatusDot default name.
- **AS — interpolate-size migration:** add the vertical-rail dead size-transition to scope.
- **AS — MetricCell bare variant (net-new prop):** for muster's TravelMatrix surface-strip.
- **AS — Input invalid variant:** model on speedtest's already-working survey `:user-invalid` idiom (resolves the net-new-vs-adoption question toward adoption).
- **Spec clarification (NEW standing rule):** the glass-ui-first mandate must require every hand-rolled `role=`/`@click`-interactive custom component to match the reka-ui keyboard+focus contract.

### speedtest — AT
- **AT (canvas motion lane):** completion-whirl `respectReducedMotion:true` (promote to P1).
- **AT (CLS/reserve):** StatsCards empty-collapse → Skeleton breath reserve.
- **AT (dashboard):** charts swallowed-error → error state; charts aria-live; StatsCards count-up via useAnimatedNumber; ResultsTable aria-sort + proposal-#19 stagger.
- **AT (admin):** delete-confirm AlertDialog; admin-login autofocus.
- **AT (running a11y):** meter-prewarm aria-live.
- **MASTER corrections:** re-label AT-R2 (VT leak) as an enhancement not a leak fix; demote/close the gauge-JS-ratio row.

### keyframes.js — A
- **A.W1-W4 (FIRST item):** re-release 3.0.0 (semver) + make the dts type-only.
- **A.W4:** string-easing light-side registry (+ the `.at()` silent-linear variant); heavy-engine base PRM gate (grounded by /amiga + the demo scene pages); move the PRM gate into RAFPlayback + default TRUE (the true constellation-wide lever); tick reschedule-latency; WAAPI shadow-tick race.
- **A.W4 close:** dist-barrel consumer smoke (the dogfood guard); demo scene-swap PRM carve.
- **A build-hygiene:** `dist/_redirects` leak; stable engine-chunk name (optimizeDeps).
- **MASTER correction:** drop the "glass-ui /motion wraps the heavy engines → constellation-wide hole" rationale (FALSE — it wraps only light engines).

### slides — B
- **B.W2:** dock 390px clamp + adaptive pager + `color-scheme:dark` on dark chrome; pager-dot focus-visible + `aria-current=step`.
- **B.W0/W1/W3-W4:** JS lifecycle cluster (cancel rAF, suspend idle canvas, mql listener, page-turn+content-visibility, try/catch, rect width, useRAFLoop+PRM composable).

### muster — K.W4 (+ AS)
- **K.W4 (verdict cascade — extends row 114):** invoke the dead useAuroraEffects (Aurora steps 1+2 also discarded); add reveal PROPS to RankedVerdict/EliminatedFold + the dead `--reveal-stagger-step` token.
- **K.W4 (onboarding):** FirstRunWalk Escape handler; data-walk-spotlight contract; per-step transition.
- **K.W4 (re-rank a11y):** keyboard commit path; focus-theft guard.
- **K.W4 (polish):** WinnerHero score-clamp [0,1]; micro-tween fallback unification; PRM sweep; ScoreFairnessField axis-label sizing; aurora-host dvh; score-gauge jewel accent (row 232/284 instance); MobileInstrumentSheet force-mount.
- **AS:** MetricCell bare variant.

### words — A
- **A.W2 (PWA/SW hardening — REFRAME as fixing-broken-prod):** precache-manifest at build (the P0 install fail); API subpath + SSE bypass; absolute-link subpath fix; offline.html. + W6 deploy build-standard for the manifest generation.
- **A.W1 (color/atmosphere):** consumer cold-pill contrast; paper-texture token drift; Fraunces CDN payload (self-host).
- **A.W2 (perf):** dual-virtualization converge on tanstack (delete the 785-LoC fork); SESSION_HEIGHT_CACHE bound + yield; paper+blur paint cost.
- **A (motion):** AnimatedText liftDown one-shot + PRM; global PRM base (adopt glass-ui reset).
- **A.W2 (a11y) + glass-ui focus-ring:** WordListRow keyboard; SearchResultItem focus-visible.
- **words A (net-new):** surface isOffline; 404/ErrorBoundary a11y.

### sudoku — WAVE C (frontend-design lenses) + M1
- **M1 (ELEVATE P1→P0):** keyframes 2.2 migration to `loadAnimationEngine()` at 4 value-import sites + a runtime-import smoke gate as the acceptance criterion.
- **WAVE C motion lens:** the global PRM `@media` covers only CSS — gate all useLineBoil/SvgFilters/glyph-wiggle/hover-filter JS loops (best fixed once via a reactive `paused` getter on pencil-boil so fourier/bbnf inherit it).
- **WAVE C a11y lens (NEW spec):** SudokuCell board ARIA + focus-visible; OptionSelector radiogroup semantics.
- **WAVE C perf lens (NEW spec):** solved-board animation budget (cap concurrent infinite wiggles).

### bbnf-buddy — the glass-ui 3.1.1 bump wave (+ a NEW editor-a11y pass)
- **Bump wave (extend):** keyframes-barrel pre-flight gate + migrate `getTimingFunction` to `loadAnimationEngine()` BEFORE bumping keyframes (the latent-P0 time-bomb); delete the hand-rolled palette (the AA contrast fail).
- **P0 PRM row (BROADEN):** runtime loop never stops (pause/hidden); CSS-transition layer + vendored glass-ui.css; pencil-boil 5th surface; animated favicon (WCAG 2.2.2); loop count is 15 not 4.
- **DockIconButton row (line 65):** selected/aria-pressed seam for the tool buttons.
- **NEW — bbnf editor-a11y pass:** KeyframeTimeline keyboard access; tool aria-pressed; editor parse-fail role=alert/aria-invalid. + PreviewGrid transition:all fix.

### deploy — W6 (+ a NEW K.W6 dep-cohort wave)
- **W6 deploy dev.sh standard (the missing artifact):** create `deploy/templates/dev.sh` as the canonical template; bash-3.2-portability sub-spec; re-derive fourier/sudoku/muster/words. + SIGPIPE `grep -q || die` hardening across deploy.sh/deploy-hook.sh.
- **NEW — K.W6 dep-cohort / version-reconciliation wave:** every package.json spec must match its symlink/copy target; every consumer of a moved keyframes export migrates to `loadAnimationEngine()` (sudoku P0, bbnf P1-latent); keyframes re-release 3.0.0 + type-only dts + stable engine-chunk name; `dist/_redirects` leak.

### Constellation — W6 / instrumented gate
- **NEW (promote fourier-only J.W6 → constellation-wide):** an instrumented W-tier pass (axe-core via @axe-core/playwright + Lighthouse CI for CLS/INP/LCP + dev-only LoAF) against each of the 12 live local ports — the cited CLS/INP/contrast magnitudes were NEVER measured this pass (eyeball+grep only).
