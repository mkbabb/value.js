# B.W4 close audit — doc-drift inventory

**Lane**: B.W4 Lane 3 (doc-drift), read-only close audit for tranche B.
**Method**: A's `audit/W7-doc-drift.md` 14-item inventory re-checked against the current tree (`docs/tranches/A/**`, `CLAUDE.md`, `git log`, the registered `docs/precepts` SHA, `docs/tranches/A/FINAL.md` — written by B.W0 after W7 was inventoried). NEW B-introduced drift checked across root `CLAUDE.md`, `demo/CLAUDE.md`, `docs/tranches/B/**`. Tree state: `git ls-files -s docs/precepts` → `3c32fae`; `ls test/*.test.ts` → 26; `ls e2e/smoke/*.spec.ts` → 3; B.W0–B.W3 closed and committed; B.W4 not yet run.
**Verdict**: The W7 inventory is **still accurate** — all 14 items remain valid and unfixed, because B.W4 (the wave that fixes them) has not run. One item's evidence base shifted: A's `FINAL.md` now exists and partially pre-acknowledges several W7 items. NEW B-introduced drift: **9 items** across `CLAUDE.md`, `demo/CLAUDE.md`, `docs/tranches/B/PROGRESS.md`, `B.W2.md`, `B.W3.md`, `findings.md`, `Bz-legacy-cruft.md`, `coordination/Q.md`.

This lane inventories drift. It does not fix it; the B.W4 close ceremony owns the corrections.

---

## §1 — W7 inventory re-check (14 items)

A's `audit/W7-doc-drift.md` records 14 drift items across 6 files. Each is re-checked below against the current tree. **All 14 remain valid and unfixed** — expected, since B.W4 (the fixing wave) is `planned`, not run. Status column: STILL-VALID = drift present, unfixed; SUPERSEDED = a later doc changed the evidence base.

| # | W7 item | Re-check status |
|---|---|---|
| 1 | `CLAUDE.md` line 16 — `npm test` "1372 tests, 24 files" | **STILL-VALID.** `CLAUDE.md:16` still reads `1372 tests, 24 files`. `ls test/*.test.ts` → 26. B.W0–W3 all gate on `vitest` 1409 (`PROGRESS.md` lines 120,143,159,172). |
| 2 | `CLAUDE.md` line 61 — `e2e/` "playwright E2E tests (14 specs)" | **STILL-VALID — and now more wrong.** `CLAUDE.md:61` still reads `14 specs`. Reality after B.W3 Lane D (`afe102a`): all 16 root specs deleted, `e2e/smoke/` has exactly 3 (`page-load`, `color-space-switching`, `view-switch`). The line must become `e2e/smoke/ — playwright smoke suite (3 specs)`. The W7 "14 vs 16" reconciliation is moot — the answer is now 3. |
| 3 | `CLAUDE.md` line 60 — `test/` "vitest unit tests (24 files)" | **STILL-VALID.** `CLAUDE.md:60` still reads `24 files`; 26 on disk. |
| 4 | `A.md` — `research/Aa..Ae` shorthand omits `Ag` (6 sites) | **STILL-VALID.** `A.md` lines 12, 34, 55, 104 still write `research/Aa..Ae`; `research/` holds `Aa, Ab, Ad, Ae, Ag`. Unfixed — B.W4 step 7 owns the `Ag→Ac` rename + cite update. |
| 5 | `A.md §8` — missing retire-rationale for `Ad-20` and `Ae-12` | **STILL-VALID.** `grep "Ad-20\|Ae-12" A.md` → 0 matches. `A.md` line 104 routes `Ag-6` and the `Ab` block but is silent on `Ad-20`/`Ae-12`. **SUPERSEDED-in-part**: A's `FINAL.md:49` now explicitly says "`Ad-20`/`Ae-12` retire-rationale to be recorded in `A.md §8` by B.W4" — the gap is acknowledged but not closed. B.W4 step 6 owns it. |
| 6 | `A.md §10` — defends non-contiguous filenames B.W4 will rename | **STILL-VALID.** `A.md §10` legend unchanged; the `Ag→Ac` rename has not run (`research/Ag-design-tokens-hierarchy.md` still on disk). |
| 7 | `A.md §8` — `Ag` finding-count 13-vs-14 disagreement with HARDEN-6 | **STILL-VALID.** Unchanged; B.W4 must settle against the actual `research/Ag` file. |
| 8 | `findings.md` header line 6 — glass-ui HEAD `d244dd5` matches no other doc | **STILL-VALID.** `A/findings.md:6` still reads `glass-ui HEAD d244dd5`. A's `FINAL.md` and `coordination/Q.md` settle on Q-close `4b16de7`; the `d244dd5` outlier is unfixed. |
| 9 | `findings.md §intro` — stale "planning-only" Mode line | **STILL-VALID.** `A/findings.md:7` still reads "Mode: tranche development only. No source edits this session per user directive." A executed all 8 waves (W0–W7 commits + close); the framing is stale. |
| 10 | `coordination/Q.md` (A) — precepts baseline `3310a8c` stale | **STILL-VALID.** `A/coordination/Q.md:104` still reads "glass-ui pins `docs/precepts` at `3310a8c`" and "value.js currently pins no precepts SHA". Index now pins `3c32fae`; value.js is registered. Unfixed. |
| 11 | `coordination/Q.md` (A) §intro — phantom `SPEC §"Document Set"` citation | **STILL-VALID.** `A/coordination/Q.md:10` still cites "precept `SPEC §"Document Set"`" for the coordination-artefact rule HARDEN-6 §1 proved non-existent. Unfixed. |
| 12 | `coordination/Q.md` (A) — contested-boundary disposition left dangling | **STILL-VALID** as an A-doc fix. **SUPERSEDED-in-substance**: B's own `coordination/Q.md §4` now records the boundary MOOT (Q closed, never wrote value.js); A's `FINAL.md` references it. A's own `coordination/Q.md` text is not updated, but the disposition is no longer dangling tranche-wide. |
| 13 | `dispatch/AGENT.md` (A) — `Aa..Ae` omits `Ag` + phantom `STYLE.md` citation | **STILL-VALID.** `A/dispatch/AGENT.md:24` still reads `research/Aa..Ae`; line 54 still cites "the precept `STYLE.md`" HARDEN-6 §9 proved absent from `docs/precepts/instructions/`. Both unfixed. |
| 14 | `waves/W7.md` (A) — phantom "dual ceiling" + stale `3310a8c` header; `PROGRESS.md` `2e...` placeholder + unacknowledged `3c32fae` advance | **STILL-VALID for `W7.md`** (line 4 still reads "dual ceiling" + `3310a8c`; line 5 still `Aa..Ae`). **PARTIALLY-FIXED for `PROGRESS.md`**: the `2e...` placeholder is gone — `A/PROGRESS.md:221,255` now carry the real W6 hash `065c6fe`; and line 263 now records "B.W0 advanced the shared pin to `3c32fae`" — the SHA acknowledgment W7 flagged as missing is present. The `W7.md` header phantom-ceiling and stale-SHA text remain unfixed. |

**§1 summary**: 14/14 W7 items remain valid. None has been fixed in source (B.W4 has not run). Items 5, 12, and 14 are partially superseded — A's `FINAL.md` (written by B.W0 after W7 was inventoried) pre-acknowledges them, and `A/PROGRESS.md` already carries the real `065c6fe` hash and the `3c32fae` acknowledgment. The W7 inventory is a sound work-list for the B.W4 close ceremony; items 2 and 5 need re-wording (item 2's count is now 3, not the contested 14/16; item 5's gap is now FINAL.md-acknowledged).

---

## §2 — NEW B-introduced drift

Drift introduced or left by tranche B's own execution. The B.W4 close ceremony fixes these.

| # | Doc | Stale claim | Reality | Fix |
|---|---|---|---|---|
| N1 | `CLAUDE.md` line 16, 60 | "vitest (jsdom) — 1372 tests, 24 files" / "vitest unit tests (24 files)" | Every B wave gates on `vitest` 1409 (`B/PROGRESS.md:120,143,159,172`); 26 `.test.ts` files on disk. (Same as W7 items 1+3 — re-listed because `B.W4.md:29` explicitly assigns the fix to B.W4 and `Bz-legacy-cruft.md:76` re-files it.) | Update to `1409 tests, 26 files` in both places. |
| N2 | `CLAUDE.md` line 17, 61 | line 17 "playwright — desktop (Chromium) + mobile (Pixel 7)"; line 61 "`e2e/` — playwright E2E tests (14 specs)" | B.W3 Lane D (`afe102a`) deleted all 16 root specs, created `e2e/smoke/` with 3 role-based specs, **removed the `mobile` (Pixel 7) project** from `playwright.config.ts`, added a `smoke` project. The structure block names `e2e/` not `e2e/smoke/`. | line 17 → `playwright — smoke suite (desktop Chromium)`, drop "mobile (Pixel 7)"; line 61 → `e2e/smoke/ — playwright smoke suite (3 specs)`. The `test:e2e` command note (`CLAUDE.md:9` area) should reflect `--project=smoke`. |
| N3 | `CLAUDE.md` lines 35-58 (`src/` structure block) | `src/parsing/` block lists `index.ts, units.ts, color.ts, math.ts, utils.ts, grammars/`; `src/units/` block lists `index.ts, constants.ts, utils.ts, normalize.ts, color/` | B.W3 Lane B (`8d6dfac`) committed 5 new library files now on disk and re-exported from `src/index.ts`: `src/parsing/{animation-shorthand,extract,serialize,stylesheet}.ts` and `src/units/interpolate.ts`. None appear in `CLAUDE.md`'s structure tree. `src/parsing/` also gained a `CLAUDE.md`. | Add the 5 files to the structure block. `B.W4.md:29` says "file structure if changed by B.W2 consolidation" — it underscopes: the change is B.W3's, not B.W2's. |
| N4 | `demo/CLAUDE.md` lines 16, 67-93 | "components/ui/ — ~180 shadcn-vue"; the composables table lists 23 root-level composables including `useSatelliteBlobs`, `useCardMenu`, `useHeightTransition`, `useHoverPopover`, etc.; "composables/ — 23 composable/utility files" | B.W2 consolidation deleted composables (`usePaneRouter.ts` is the new source-of-truth; per the brief B.W2 deleted 7 composables/components). `demo/@/composables/` root now holds only `prng.ts, useFilteredList.ts, usePaneRouter.ts, useSafeStorage.ts, useViewManager.ts` + 3 subdirs (`auth/`, `color/`, `palette/`) — 20 `.ts` files total, not 23. `ui/alert/` is now barrel-only (`index.ts`; the 3 SFCs `Alert.vue`/`AlertTitle.vue`/`AlertDescription.vue` deleted per B.W2.md:41); `ui/table/` directory is **gone**. The "~180" count and the flat 23-composable table are stale. | Re-derive the composable list and count from the current tree; note `usePaneRouter.ts`; mark `ui/alert` as a glass-ui re-export barrel; correct the `ui/` count. The whole `demo/CLAUDE.md` structure section predates the Mar-2026 restructure (it still shows a flat `composables/` with no `auth/color/palette/` subdirs) — B.W4 should reconcile it wholesale. |
| N5 | `B/PROGRESS.md` "Wave log" table | The table has **7 rows** — B.W0–B.W3 each appear **twice**: once `closed` with commit hashes, once `planned` with `— — —`. B.W4 appears once as `planned`. | The duplicate `planned` rows for B.W2 and B.W3 are stale leftovers from the pre-execution template; the real closed rows above them supersede them. | Delete the 3 stale `planned` rows for B.W2 and B.W3 (they shadow the `closed` rows); keep one `planned` row for B.W4. B.W4 step 2 ("every B wave-log row shows `closed`") cannot be satisfied while the duplicate `planned` rows exist. |
| N6 | `B.W3.md:82` and `findings.md:26` | `B.W3.md:82` commit line: `fix(types/w3): close custom-component typecheck cluster (-155 errors)`; `findings.md:26` / `B.W3.md:40`: "the ~155-error custom typecheck cluster" | Actual commit `92ee51f` is titled `(-86 errors)`; `B/PROGRESS.md:170` records "vue-tsc 212 → 126 … The 86 custom-bucket errors fully cleared". The planned ~155 was an estimate; the landed figure is 86. The vue-tsc total moved ~290(planned)→212(B.W2 actual)→126(B.W3 actual). | Reconcile `B.W3.md` and `findings.md` to the landed 86 / 212→126 figures, or annotate the ~155 as the pre-execution estimate. Plan-vs-actual drift. |
| N7 | `Bz-legacy-cruft.md:76` | "`CLAUDE.md:16,60` says '1372 tests, 24 files.' … **B.W7 fix.**" | Tranche B has **5 waves** (B.W0–B.W4); there is no B.W7. The hardening round collapsed six waves to five (`findings.md:30`, item L). The CLAUDE.md test-count fix is owned by **B.W4** (`B.W4.md:17,29`, `findings.md:28` item J). | Change "B.W7 fix" → "B.W4 fix" in `Bz-legacy-cruft.md:76`. Stale wave number from the pre-hardening 6+ wave plan. |
| N8 | `B.W2.md:1,57-65,87` (Lane C) | Title "+ UnderlineTabs migration"; §"Lane C — UnderlineTabs structural migration"; step 1 "import `<UnderlineTabs>` … replace `<Tabs class="underline-tabs">`"; sub-gate C "`grep underline-tabs demo/` returns nothing (deletion proof)"; commit-plan line "migrate PaletteDialog tabs to `<UnderlineTabs>`, retire `.underline-tabs` CSS" | B.W2 Lane C **re-scoped** the migration. Commit `c2efa83` is titled "UnderlineTabs — glass-ui shipped the wrong shape; keep reka-ui Tabs". `coordination/Q.md §3` (B) and `findings.md:24` (item F) record the orchestrator decision: glass-ui's `<UnderlineTabs>` is header-only, cannot host `PaletteDialog`'s 5 content panels; the demo **keeps** reka-ui `<Tabs>` + the `.underline-tabs` override; the gap re-files to glass-ui. The migration described in `B.W2.md` Lane C did NOT happen — `.underline-tabs` was NOT retired, no `<UnderlineTabs>` import landed. | `B.W2.md` Lane C, sub-gate C, and the commit-plan line describe a migration the wave rejected. `B.W4.md:7` and `B.W2.md:7` carry "Hardening note" blocks for renumbering — Lane C needs an analogous re-scope note (or a rewrite) so the wave spec matches what landed. `findings.md` item F is already correct; only the `B.W2.md` spec drifts. The close should reconcile it. |
| N9 | `B/coordination/Q.md:7,116` | line 7: "**keyframes.js**: HEAD `19d1a1b`"; §9 line 143: "keyframes.js's pin … `458c2d1`"; §7 line 116: "B reads glass-ui at HEAD `888d227`" | Internal SHA consistency check: the doc pins glass-ui at `4b16de7` (Q close, §2a) but §7's conflict-resolution protocol still says "B reads glass-ui at HEAD `888d227`" — the B-open snapshot SHA. After B.W0–W3 ran (all consuming the §2a Q-close re-verification at `4b16de7`), §7's `888d227` baseline is stale relative to the rest of the doc. Minor — §7 is a protocol clause, not a ship-state claim — but it contradicts §2/§2a. | B.W4 step 4 ("Update `coordination/Q.md §3`") should also reconcile §7's read-SHA to `4b16de7` or note `888d227` as the historical B-open baseline, consistent with how §2 is labelled. |

**§2 summary**: 9 new B-introduced drift items. N1–N4 are stale counts/structure in `CLAUDE.md` + `demo/CLAUDE.md` that B's execution made wrong (and B.W4 step 5 already half-anticipates N1–N3). N5 is a malformed `B/PROGRESS.md` wave-log that blocks B.W4 close-step 2. N6–N8 are plan-vs-actual drift inside B's own wave specs (typecheck count estimate, a stale wave number, and the un-reconciled Lane C re-scope). N9 is a minor internal-SHA inconsistency in B's `coordination/Q.md`. N5 and N8 are the load-bearing ones — N5 because the close-honesty check ("zero `planned`") fails against it, N8 because the wave spec asserts a migration that the tranche explicitly rejected.

---

## Disposition

- **§1**: all 14 W7 items are a valid B.W4 work-list. Re-word item 2 (count is 3, not 14/16) and item 5 (FINAL.md now acknowledges it). Items 5/12/14 are partly pre-acknowledged in `A/FINAL.md` and `A/PROGRESS.md`; the source fixes still owe.
- **§2**: 9 new items. The B.W4 close ceremony fixes N1–N4 under its doc-drift + CLAUDE.md steps (5, 17, 29), N5 under its `PROGRESS.md` reconcile step (2), and should add N6–N9 to its scope — they are B-internal spec drift that step 17's "any inaccurate statements found in the doc-drift audit" covers.

This audit inventories; it does not fix. The B.W4 close ceremony owns every correction above.
