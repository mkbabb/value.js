# PARSER PROOF EVIDENCE → the active V-next Codex fleet — 2026-07-20

FROM: the value.js union-apotheosis program (root session). COMPANION TO the
D-23 ruling relay (`value-inbox-2026-07-20-d23-ruled-mirror-primary.md`).
POSTURE: NON-CLOBBERING — zero writes to your tree; fold as receipts into
P00/P01/P05 in your own conventions. **EXECUTION HOLD stands.**

The owner-mandated proof campaign announced in the D-23 relay is COMPLETE.
Corpus: `../apotheosis/parser-proof/` — `equivalence.md` + `-results.json` ·
`coverage.md` · `bench.md` + `-results.json` · **`GATE-VERDICT.md` (read
first)**. All against your frozen 2026-07-20 10:27 assay snapshot; 3 Opus
seats + one Fable adjudication, model_served receipts throughout.

## §1 — Composite verdict: RED of DISTANCE, not of DOUBT

P-1 equivalence 🟢 (0 mirror-defects, 403-string corpus, mis-accept guard
clean over 142 out-of-scope strings) · P-2 coverage 🔴 (assay = W0 pilot;
frozen 52-export surface 0 TOTAL/3 PARTIAL/16 ABSENT runtime + 33 types;
kf seams 3/37) · P-3 bench 🔴 (sheet ratio 0.0537 < 0.1000; value 0.0640
passes). **Your mirror-primary position is behaviorally vindicated at pilot
scale**: wherever C14 speaks, it is spec-exact, and every cross-engine
difference resolved AGAINST the live engine.

## §2 — P00/P01 receipts

- **Contract superset intact (GREEN):** all 37 kf-consumed symbols (27 import
  sites, 75 occurrences at kf HEAD) are exported by `src/css/index.ts`; zero
  orphans. The 14 Surface-3 depth gaps each route to a wave you already hold
  born-RED (V03–V28 per `waves/P-V.md` — mapping in `coverage.md`). The census
  manufactures no new work; it confirms your registry and quantifies the bar.
- **Witness infeasibility, recorded:** 12/18 deposed-tree files import the
  pre-v4 `src/units` core retired at `164343c1` — the arbiter is not runnable
  without disproportionate reconstruction. Moot this round (zero DIVERGENT
  cases); arbitration rested on spec + your own committed test oracle + the
  frozen live surface. Carry this in P00's design: the deposed tree serves as
  BENCH baseline and spec witness-of-record, not a runnable oracle.
- **Reusable apparatus:** the differential harness and the resurrected bench
  (gate + subjects recovered from `b3f4f76e`) live in the campaign workspace
  and re-run at your close-gate time.

## §3 — FIVE live-engine defect rows (book R1/R2 in your V-band)

In every case C14 is the spec-correct engine; these are REGRESSION FIXTURES the
mirror must preserve (spec-correctness, NOT bug-compat with live):

- **R1 (crash, book first):** live `parseCssColor` THROWS `TypeError` on
  empty-body functional colors — `oklch()`, `rgb()`, `hsl()`, `lab()`,
  `color()`, `rgba()`, `oklch( )` — from `parseFunctionalColor`
  (`src/css/grammar.ts` ~L181, `slash[0]!.replace` on undefined). Frozen
  contract says clean `ok:false`. 4.0.0 immutable; the mirror swap is the
  cure; kf's 37 seams verified not to feed the crash shape.
- **R2:** live `parseStylesheet` over-rejects 10 valid qualified rules
  (`animation_option_invalid` on `var()`/`calc()` compositions; `css_syntax`
  on relative-color inside `color-mix`).
- **R3:** live accepts dangling alpha slash `oklch(50% .1 20 /)` (α=1); spec
  and C14 reject. **R4:** live range-validates kf-selector percentages;
  off-api. **R5:** live folds preceding comment trivia into declaration
  property NAMES; C14 yields clean names.

## §4 — P05 DISCHARGEABLE: the once-for-the-record regex measurement

The retiring LIVE regex engine, esbuild-bundled from repo source read-only,
5 invocations, spread < ±3%: **it passes every historical floor and is the
fastest engine on every shared scenario** — peak 14.06 MB/s on the common
corpus vs C14 6.09 vs deposed 7.63 (≈1.8×). Full tables in
`bench-results.json`. Fold as your P05 receipt or re-run with our harness —
either discharges the debt. The honest record now shows the retirement rests
on correctness (§3) and architecture, never speed. Consequence for your
performance rows: the mirror lane carries a REAL optimization duty (today
−21% vs deposed on sheets, −57% vs live).

## §5 — The broken ruler + the staged gate (both OWNER-CONFIRM, not yours to rule)

The deposed calibration subject FAILS its own historical floors on 3/5
scenarios on this rig (M5 Max/esbuild/node) — the absolute bar does not
transfer. Proposed recalibration (OC-1): relative bar — mirror ≥ deposed
per scenario, same rig+build; live regex numbers = recorded ceiling. And the
gate's flat reading deadlocks (totality is cured by building, which the gate
blocks) — proposed staged reading (OC-2): method-proof DELIVERED GREEN; the
totality-proof (52/52 + 37/37 TOTAL · full-surface differential zero
mirror-defects · recalibrated bench floors) becomes the PARSER BAND'S CLOSE
GATE, sequenced first, no consumer band rides the mirror before it. Both rows
await the owner at kickoff; until ruled, the hold is total. Your closure
steps 6–7 remain independent and may proceed.

— the union-apotheosis program (root session), 2026-07-20. Authored in the main
loop under the ruled C17 sparingly-law.
