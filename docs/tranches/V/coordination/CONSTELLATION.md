# Tranche V — Constellation Contract

## 1. Completed BI Baseline and Immediate Post-BI Producer Cut

**HISTORICAL (V-A136):** Formation inspected active `tranche/BI` after P000/P001. The final published BI baseline is immutable `v6.0.0`/master/origin/npm 6.0.0 at peeled commit `e5b3a2095b6c3e330b5d82ca3330f1eac4e3c895` (annotated tag object `b262e2d30976d0330014f359803e0c2c65b9b691`). The exact V component deltas below are not present in that artifact, while the primary then contained active post-BI producer work. W3's immediate producer-cut language, `G_NEXT` comparison, and ordered Value → Keyframes → Glass release procedure are historical; Value4 and Keyframes6 are now immutable. The live Glass remainder is governed by the current-stage override below.

**V-A168 current-stage override:** the relay rows below that mention bootstrap replacement, Keyframes rebuild/refreeze, Value/K publication, U-F77/B1/B14 producer ordering, residual Consumer-Cut/W2 producer procedure, or unannotated Value3 demo canon are historical/retired transport procedure. The live handoff is singular: Glass consumes immutable registry Value4 + Keyframes6, closes strict declarations/HeaderRibbon, removes/annotates stale demo names, and returns product-green Q003 evidence from the honest 5/0 baseline before W17a emits fresh Glass7 bytes. W33a publishes those bytes, then W17b refreshes downstream demo/dev locks and the routed value.js mount. A falsified causal hypothesis may be declined; a slab, occlusion corruption or lost target may not. V-A120's composed Blob visibility and Dock-affordance rows remain open. The latest Glass source/lock is LANDED/IMPLEMENTED but dirty; it is not a packed or immutable Glass7 candidate.

**V-A136 current-stage correction:** the 3-active/0-nested inert Configurator backing-plane experiment failed native capture and is fully reverted. Honest 5/0 is the current Q003 baseline; Chromium-only attribution is withdrawn. The current Glass source observation is 176 files/1,132 tests with typecheck green, while fresh build/pack/verifier/native evidence and any Glass7 coordinate remain absent. No workaround or historical count below supplies release credit.

**V-A168 formation correction:** the current register carries the API/UI families plus the acyclic W17a→W33a→W17b→W18–W32→W33b release rail. W18 follows W6, W29's before packet is immutable/reconstructible, and Q003 must be product-green before fresh Glass bytes. None reopens Value4/Keyframes6 or creates a new wave/gate.

### Preserve under retained P-coordinate ownership

| Mechanism | BI owner |
|---|---|
| palette-honest Aurora reveal door, atom controls, post-arm replay facade | P046 |
| Blob settled seam, BLOB_HERO, lightness floor, satellites/containment and WebGPU/WebGL2-equivalent scene facade | P047 |
| shared procedural lifecycle and readiness transitions | P043 |
| renderer capability, failure and engine identity | P045 |
| pause/resume, offscreen resource and scheduling behavior | P054 |
| PRM dock terminal geometry | P031/P041 |
| dock crossfade identity/noninteractive face | P036 |
| honest mask/safe inset/reserved band | P038/P039 |
| WatercolorDot face register | P051 |
| WatercolorDot noninteractive face-only boundary (T-28 outline and button/tag host abrogated in V) | P051 |
| SegmentedTabs active-indicator geometry/fill | P092 |
| Slider spectrum tokens and UA-outline suppression | P093 |
| workbench housing/regions/phases/reserved geometry/title interval/optional boundaries | P122 |
| Plus Jakarta/Fira faces plus family-neutral proportional type-size/line-height pair | P019 |
| retained Plus Jakarta/Fira source faces and proportional surface | P019 |
| semantic font entries plus packed asset emission/URL/dependency truth | P010/P127; P128's generator is retired |
| native font-load/request/stability evidence | P019 product review plus P127 packed-consumer smoke; P061's runner is retired |
| semantic font entry/export/declaration projection only | P010 |

### Immediate post-BI producer contract amendments

1. **P109:** whole-header contraction means padding, band footprint, and title token step—not compositor-only title scale.
2. **P017 + P063:** expose named veil per-mode alpha and clarity-window levers; no consumer recipe fork.
3. **P017:** carry the T-45 ladder edge-sampling/host-radius clip cure and bright-ground read.
4. **P038:** export `--dock-pill-h`; **P041:** mount arrive-expanded at the final pill pose or expose one settled seam.
5. **P047 terminal engine and fixed-footprint ruling:** its current WebGPU-preferred/WebGL2-equivalent contract stands. Retire the superseded single-WebGL2/drain-`metaball.wgsl` arm. W17 establishes and validates the producer measurement seam/protocol and freezes the rendered `b₀` baseline in `docs/tranches/V/audit/pi/w17/p047-producer-baseline/manifest.json`. Its four rows freeze seed bytes, canonical CSS color, complete effective-config digest and P047 `settled` frame; sample the transparent renderer buffer at DPR 1 and 2 in both WebGPU and WebGL2; threshold premultiplied alpha at `≥0.5`; select the connected component containing the explicitly exposed simulation-origin coordinate; and divide `2√(A/π)` by the smaller outer-footprint dimension. W17 changes no Picker paint parameter and carries no final target. W29 then preserves W20's locked Picker outer footprint/lifecycle geometry, changes only `BlobConfig.geometry.bodyRadius:0.26→0.325`, and records the final WebGPU/WebGL2 × DPR1/2 rows in `docs/tranches/V/audit/pi/w29/picker-painted-component/manifest.json`: rendered diameter ratio `0.66019…0.66042`, centroid error ≤`1.342%`, hull inset exactly `12.0%`, antialias edge ≤`1.885 CSS px` at DPR1 and ≤`0.948 CSS px` at DPR2, identical alpha-area counts across engines, and 0px chassis/footprint/seat movement. No `heroScale`, second radius, size alias or public introspection survives. The dedicated Blob workbench is independently container-scaled; no universal 100–105px or second size primitive survives.
6. **Per-site semantic component manifest:** the two ConfirmDialog confirmations → P106 Dialog; `VersionHistoryDrawer.vue` Sheet → P107 fixed-right Drawer; `ActionButton.vue` and `PaletteSlugBar.vue` HoverCards → P104 Tooltip, with keyboard-reachable/accessibly described triggers; `ColorInput.vue` HoverCard → P103 Popover/disclosure preserving textbox focus and dismissal; ToggleChip → P091 Chip selection mode; DockIconButton/DockSelectTrigger/useLayerTransition sites → P034/P036/P040/P041. “None external,” Sheet→Dialog and universal HoverCard→Popover are all false.
7. **P122:** retain direct landmark-neutral housing. Expose `--instrument-title-gap` with producer default `calc(var(--instrument-dial-padding-inline, 1.5rem) / 2.618)`; the settled title-line-box end→headline-line-box start equals it. Value defines that gap as `G` and proves rendered `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px` at P019 floor/fluid/ceiling and 1440/390. Replace the 1.2fr:1fr grid with typed `proportion="golden"|"preview-dominant"`: default golden is exact `61.8033989/38.1966011`, preview is `66.6666667/33.3333333`; absent inspector makes stage 100%, narrow order is stage→inspector→action. Boundaries are a unique set from `{stage-inspector,inspector-action}`; reserve is `none|stage|inspector|both`, default none. Value's binding eight-workbench inventory deliberately consumes boundaries `[]` and reserve `none`; a rare Picker wrap is content flow, not a persistent chassis reserve. The producer remains the sole authority for the six computed region intervals: `--instrument-dial-padding-inline|block` are the outer logical insets of the combined stage+inspector composition; `--instrument-dial-gap` is the stage↔inspector separation on the active layout axis; `--instrument-control-padding-inline|block` are the action-region boundary→action-content insets; and `--instrument-control-gap` is the separation between adjacent action children. At 1440/390/320/400% zoom, each mapped measurement equals its computed producer value within `±0.5px`; value defines, overrides, copies or substitutes none of them. No consumer grid, universal min-block, nested main or compatibility path.
8. **P019/P010/P127 font split:** P019 owns the retained Plus Jakarta Sans/Fira Code sheets/ranges/metrics/fallbacks and a family-neutral paired kicker/headline size+line-height contract whose size and line-height ratios remain exactly `1/√φ` at floor, throughout the fluid arm and at ceiling; the fixture exposes all three arms so value can apply Fraunces/Fira and prove the P122/ink relation above. P010 owns semantic entry/export/declaration projection. P127 owns hashed emission, packed-consumer URL resolution, dependency truth and the package boundary; P019's native product review plus P127's packed-consumer smoke prove requests/stability. Fraunces assets remain value-owned. Explicitly migrate speedtest's `?url` case. No base64 corpus, deferral plugin, revived P128 build-project generator or revived P061 scenario runner survives.
9. **P043/P045/P054 shared renderer state:** P043 owns readiness/lifecycle transitions, P045 capability/failure/engine identity, and P054 pause/resume/resource scheduling. P046/P047 expose only scene-facade projections so value.js can render truthful `fetching|active|parked|unsupported|failed` state and a persistent still/pause control without inspecting internals.
10. **Direct handoff + P127 handshake (current registry boundary):** record this live consumer relay as the direct immutable handoff that superseded P004's registry, and amend P127 from its stale `^3.1.0`/`^5.2.0` pair to Value `^4.0.0` plus Keyframes `^6.0.0` for Glass `7.0.0`. Value `4.0.0` is now registry-published at `44ddaff7a22283a4f7a42608893eeae7bc234424`, with tarball `https://registry.npmjs.org/@mkbabb/value.js/-/value.js-4.0.0.tgz`, integrity `sha512-Z8ywb4htSxJlRFvoU1DNtvzr9Bsuaw9ahT/hvNlKbnRj6fTnLuXjn0itKq1Q5s6rwg24ct0zcLZ04BuR3/SzGw==`, and provenance run `29497728532`; Keyframes 6.0.0 is now the immutable registry boundary and Glass follows only that pair. Its lock was resolved from the Value record—never from a bootstrap/rehearsal tarball, forced lock, downgrade or guessed API. **V-A136 current state:** the registry-only pair install is proven; Glass has 176 files/1,132 tests and typecheck green, but build/pack/verifier/native Q003 evidence and a fresh Glass7 artifact remain open. The `BOOTSTRAP ONLY` manifest remains historical evidence; W33 alone creates and publishes the fresh Glass candidate and merges final downstream locks. P004 remains retired; no registry is revived to carry the message.
11. **P010 library externalization:** after W17 migrates Glass against packed Value 4 declarations, replace the bare `@mkbabb/value.js` entry in `vite.library.ts` with exactly `@mkbabb/value.js/color`, `@mkbabb/value.js/css` and `@mkbabb/value.js/easing`. `/css` owns `parseCssColor(source:string): ParseResult<CssColor>` and `serializeCssColor(color:CssColor): Result<string,ColorIssue>` over the closed thirteen-space CSS-native union; all seventeen spaces retain factory/conversion/object-oracle coverage, while the four library-only spaces explicitly convert to `CssColor` and no general library-string serializer exists. Failure-explicit final color operations remain on parser-free `/color`; named easing functions plus the exact `CubicBezier`/`steppedEase`/`easing(name)` Result constructors remain on `/easing`, and the `timingFunctions` aggregate is absent. Rollup exact-match externalization does not make the removed root cover capability subpaths; leaving it would bundle the peer. No prefix umbrella, local parser or compatibility external survives.
12. **P092 SegmentedTabs register:** the one fill matches the active inner button within 0.5px across orientation/viewport/direction, never pill/halo/double-fill, with separate focus. V retires global PaneSegmentedControl. Owner-state, Admin Names and Mix Colors/Palettes source tabs are the three earned consumers; W17 deletes corrections and W23/W24/W26/W30 prove them.
13. **P051 WatercolorDot boundary:** remove the public interactive `tag`/button-host branch in Glass `G_NEXT`; preserve only the seeded noninteractive face/edge without active outline. W17 recounts live HEAD from the formation baseline of 25 explicit calls, then W19–W22/W25–W27 migrate Dock seal/edit, ColorSpaceSelector, eyedropper, Spectrum, channel, palette, Generate, Mix and Gradient. Operable faces use named geometric seats; ornamental faces are aria-hidden; data-bearing static faces remain present as noninteractive named list/text content with zero activation/focus/drag semantics. Acceptance is zero face activation/focus/drag host—not zero internal hover paint—and no ring/shadow/pseudo-element crosses the face; this contract does not imply HTML `[inert]`.
14. **P046/BI.W-FIELD-CORE Aurora closure:** T-32 sets terminal `MAX_NUCLEI = 8` on the one typed `AuroraZones` substrate and proves the same eight configured centers/bounded region effects in WebGPU and WebGL2. T-38 consumes `../glass-ui/docs/tranches/BI/waves/BI.W-FIELD-CORE.md` exactly: cursor-local luminance lean, velocity/domain-warp burst, input-medium gating and named amplitude. No value shader fork, second zone substrate or pixel-inferred pointer state survives.
15. **P059/P060 product language:** rendered headings, blurbs, labels, captions, empty/error copy name public product concepts and observable behavior, never tranche/wave/gate/proof IDs, internal codenames or planning vocabulary. Real public imports remain the example source; native review stays with the owning product slice at its release Browser boundary, independent of whether the evidence-derived semver is minor or major. P061's declined scenario runner remains absent.
16. **P127/profile three-edge correction:** reopen the profiler's former two-edge disposition. Its exact accepted map and ordinary bite test recognize `/color`, `/css` and `/easing`; `/css` leaves the rejected set, while the root, old/private entries and prefix lookalikes remain rejected. Glass Migration/dependency canon, source imports, `vite.library.ts`, profiler and packed graph must tell that same three-edge truth before the row can ship.
17. **Explicit color failure and actual-band contrast:** `cssToOklch` consumes `/css` `ParseResult<CssColor>` and preserves or maps failure to an explicit Glass color-domain error. Success carries exactly readonly empty diagnostics; failure carries a readonly nonempty diagnostic tuple. Invalid tone, invalid surface, unresolved cascade token and lazy-pending solve are distinct; none becomes empty string, `var(--primary)`, warm-card default, inherited foreground or cached identity success. The accent path constructs the actual typed band color and uses Value `/color` Color-against-Color WCAG ratio; delete the background-lightness proxy, `minContrast * 0.1`, catch-to-empty and CSS masking fallback.
18. **Aurora degree-domain hue:** Value 4 `/color` `interpolateHue` accepts and returns physical degrees; normalized-turn helpers remain GPU-local and do not define this public contract. Glass retains Aurora-domain warm/cool pole and bounded-throw composition, consumes the degree operation, and records named warm wrap `350→12`, cool wrap `10→348`, near-pole no-overshoot `60→70`/`260→250`, midpoint/zero identity and `22°` maximum-throw vectors.
19. **Fresh package bytes:** A85 is manifested; V-A93 holds Keyframes's 184,710-byte archive and authorizes the 184,254-byte strict replacement. V-A88/V-A90–V-A92 hold Glass's 966,350-byte archive for declaration and inferred HeaderRibbon behavior; V-A94/V-A95 additionally hold native interaction credit for cue/aperture and post-drag layer defects. **V-A130 current state (V-A124/V-A125 Q003 history):** Value4 and Keyframes6 are immutable and the pair install is proven; Glass owns stale demo-canon cleanup plus the fresh declaration/default-persistent/explicit-collapsible/Q003 live-backdrop-adjacency correction from the restored honest 5/0 baseline, native close and publication. No Keyframes rebuild/refreeze/re-close, Value rehearsal substitution, alias, internal declaration leak, DOM/VNode oracle, offset/cover/reset, consumer sizing/overflow, shim, file link, hoisting, `skipLibCheck`, timing cloak, fallback or dual path.
20. **V-A97 release-cycle break (historical; current stage V-A130):** Keyframes 6's packed runtime remains Value-only; its Glass 7 edge is demo/dev input, not a publication prerequisite. Value4 and Keyframes6 are now published. W33 publishes Glass 7 from exact registry Value4 + Keyframes6, then refreshes Keyframes's demo/dev Glass edge and mandatory lock as an ordinary downstream consumer change. No `file:` lock, force, fabricated/stale lock acceptance, hidden peer, CI bypass or circular publish.

### Mirrored immediate post-BI Glass executor addendum

The active immediate post-BI Glass executor—historically labeled the “Glass 7” executor before the semver audit—has mirrored these exact amendments into `../glass-ui/docs/tranches/BI/coordination/valuejs-inbox-2026-07-15-v-formation.md` active-disposition rows 112–114; they create no new tranche or meta gate. Those rows are the Glass execution brief, not a published coordinate; the live producer/adoption contract is the fresh Glass7 strict/native/release close from the immutable registry pair:

- Preserve the six-value P122 tuple as one semantic producer authority. Dial padding is the combined stage+inspector composition's outer logical inset; dial gap is the stage↔inspector separation on the active layout axis; control padding is the action-region boundary→action-content inset; control gap is the separation between adjacent action children. Responsive resolution stays behind those properties. value.js defines, overrides, copies or substitutes none; each mapped rect measurement must equal the computed producer value within `±0.5 CSS px` at 1440, 390, 320 and 400% zoom.
- `scripts/profile-bundle.mjs` is live through `package.json`'s `profile:bundle` command and the README workflow. Its former two-edge disposition is reopened: update it unconditionally to recognize, report and bite-detect exactly `@mkbabb/value.js/color`, `@mkbabb/value.js/css` and `@mkbabb/value.js/easing`, removing `/css` from the rejected set while still rejecting the removed root, old/private entries and prefix lookalikes. Do not retire it, use a package-prefix matcher or retain a vacuous root-only branch.
- Execute producer visual work Browser-first: open, inspect and capture the live routed story/state in the in-app Browser, make one bounded change, and reinspect it there. Computer Use or headed hardware is limited to Browser-inaccessible native AT/OS/device/Safari/GPU surfaces. Standalone/headless Playwright is permitted only for named cross-engine binary automation, exhaustive repetition or synthetic timing the Browser cannot express and may never author or attest visual gestalt; literal filenames remain coordinates.

**HISTORICAL (V-A123):** The baseline inbox and its V-A93 transport replacement remain chronology. Value4 and Keyframes6 are now immutable; the registry-only pair is proven. The held Glass archive remains non-release evidence, while fresh Glass bytes, Q003 native truth and the routed value witness remain RED. Bootstrap readiness is not release readiness.

**CURRENT V-A168 Q003 instruction:** Do not add a second sampler experiment, cover/reset, route/engine mask, compositor watchdog or degraded material path. Preserve the already-sound opaque-live/transparent-capture split, `min-h-0` aperture, nested-cell reduction and Configurator sibling sampling. The +70ms-intact/+900ms-corrupt one-sampler capture and the later approximately 95→5 nested-filter reduction both falsify those mechanisms as sufficient causes. The inert Configurator backing-plane experiment that reached 3 active/0 nested also failed and was fully reverted; the honest baseline is 5 active/0 nested, with Chromium-only attribution withdrawn. Glass W17a/W33a must now return repeated forward/reverse native captures with zero slab, occlusion corruption and lost target. A causal hypothesis may be declined; the product defect may not. This is not a browser-upgrade-only wait, and no current Glass archive or interaction row gains credit from the upstream commit alone.

### Exact active-session relay — 2026-07-15

V sent the complete direct handoff above to active Glass task `019f6610-6022-7df1-95b3-472b17a64656`. After the executor reported P127's registry `E404` and the Value-parser contradiction, V sent exact amendments to that same task. **HISTORICAL (V-A123):** the bootstrap/rehearsal transport and producer migration steps are complete and superseded by immutable Value4/Keyframes6. The live Glass handoff remains exact `/color`+`/css`+`/easing`, strict declarations, native Q003, fresh Glass7 publication and downstream routed-mount proof; no file links, forced locks, root/prefix externals, guessed names, local/general parser or serializer, fallback, alias or dual path. W3 records each ask as LANDED, RED or DECLINED; W33 records the sole Glass candidate and registry equality. Re-deadlining closes none of the adoption/release obligations.

**HISTORICAL (V-A123):** At the Glass safe boundary, that executor ACKed exact `/color` + `/css` + `/easing` and the thirteen-space `CssColor` boundary. The 184,710-byte and 184,254-byte Keyframes archives and the 966,350-byte Glass archive retain transport/runtime evidence only. **CURRENT:** immutable Value4 + Keyframes6 are the sole producer inputs; Glass remains RED on declaration ownership, explicit HeaderRibbon behavior, Q003 live-backdrop adjacency and fresh publication. No release version/tag/registry/lock coordinate is guessed.

| Exact relay family | Binding content and current truth |
|---|---|
| Optical Bench constitution | One spectral/procedural protagonist and noninteractive WatercolorDot counter-signature; quiet neutral glass; Fraunces identity, Fira numeric data, Plus Jakarta controls/body; structural dividers only for real grouping; earned decoration/motion/affordance; public product language. |
| P019 + P122 + supplied Lab crop | Exact paired `1/√φ` size/line-height through all three clamp arms; exact `G` and ink inequality; landmark-neutral golden/preview chassis; exact six region-spacing properties plus separate title gap; opt-in boundaries/reserve; 0px outer growth. P019 and the P122 source/tests are credited. After the relay, Glass deleted the universal chassis minimum without alias and added an ordinary absence contract at `src/components/instrument-chassis/styles.css:17–20` and `tests/components/instrument-chassis.contract.test.ts:123–134`; V-A5 is source/test **LANDED**. **Historical held rehearsal only:** the old Glass `G_NEXT=7.0.0` coordinates are not a current artifact; the native P019/P122 Browser witness and a fresh packed Glass7 remain pending, so the combined producer row is RED rather than final SHIPPED. |
| P047 + P043/P045/P054 | WebGPU-preferred/WebGL2-equivalent Blob; sole `bodyRadius`; named settled frame and `{x,y}` origin. W17 owns the exact seed/color/config/frame, DPR 1/2, dual-engine alpha-component protocol and frozen `b₀` producer baseline; it has no final target. W29 alone changes `bodyRadius:0.26→0.325` and owns the final ratio `0.66019…0.66042`, centroid ≤`1.342%`, exact `12.0%` hull inset, AA edge ≤`1.885 CSS px` at DPR1/≤`0.948 CSS px` at DPR2, identical cross-engine alpha areas and 0px seat/chassis/footprint movement. The mass/origin seams are credited; the W17 baseline and W29 final artifacts retain their separate terminal dispositions. Lifecycle, capability/failure/identity and resource scheduling remain separately owned; no radius alias or public introspection is added. |
| P046 field core | One typed `AuroraZones`, terminal `MAX_NUCLEI = 8`, the same eight bounded centers/effects in both engines, and the exact luminance/velocity/domain-warp/input-gate/amplitude pointer law; no consumer shader or pixel-inferred state. Existing source is credited; exact dual-engine/native evidence remains owed. |
| P051/P092/P093 | WatercolorDot remains a noninteractive face-only surface with consumer-owned semantic seats; SegmentedTabs remains one rect-matched fill with separate focus and exactly three earned value consumers; Slider retains spectrum and UA-outline ownership. Existing P051/P092 source/tests are credited; native review remains owed. |
| Semantic component cut | The exact P106 Dialog, P107 Drawer, P104 Tooltip, P103 Popover, P091 Chip and P034/P036/P040/P041 Dock per-site manifest remains binding; no blanket rename, forwarding wrapper or alias. |
| Header, veil, Dock and state | P109 full-header contraction; P017/P063 veil/clarity/clip/bright-ground; P031/P036/P038/P039/P040/P041 terminal Dock geometry/identity/inset/overflow/focus; truthful renderer `fetching|active|parked|unsupported|failed` and persistent still/pause control. |
| Fonts, examples and copy | P019 owns the product font surface, P010 semantic projection and P127 packed emission/URL/dependency/release truth. P059/P060 own useful real-import specimens and end-user copy. Declined P128/P061 and superseded P004 remain retired. Glass `demo/stories/data/search.vue:17,37–68,404–409,551` visibly emits planning statuses `wired|proof|consumer|triage`; the owning product slice must replace that vocabulary and inspect it natively. |
| Package and color cut — historical transport/archive truth | Direct handoff plus P127 records immutable Value `4.0.0`, Keyframes `6.0.0` and evidence-selected Glass `7.0.0`; the prepublication Value-3 statement is historical. W7's exact-seven bootstrap and Keyframes rehearsal lineage are durably recorded but never substitute for registry bytes. V-A88/V-A90–V-A92/V-A94/V-A96 and V-A124 keep Glass RED until declaration ownership, explicit persistent/collapsible HeaderRibbon behavior, actual-aperture cue placement and live-backdrop adjacency integrity are exact; W17 owns that correction, native close, remaining old-pin removal, routed mount and public-surface audit; W33 owns the fresh Glass candidate and publication. P010/library externalization recognizes exactly `/color`, `/css`, `/easing`; parser/easing/color rulings remain unchanged. No stale pack, undeclared declaration reach, DOM/VNode oracle, root/prefix matcher, local parser, silent fallback, offset/cover/reset, cycle, hidden pin, alias, shim, file-link or dual path. |
| Current registry/order truth | Value `4.0.0` is immutable at gitHead `44ddaff7a22283a4f7a42608893eeae7bc234424` with its V-A109 tarball/integrity/provenance. V-A115's fresh no-Glass registry-only Keyframes lock and green `npm ci` culminated in immutable Keyframes `6.0.0`: gitHead `5a9183a7afe24702081a7b87c8adc7286ddce9a0`, tag object `26190755ce1e57c54cb14ef0a454ae02ed2b3da0`, tarball `https://registry.npmjs.org/@mkbabb/keyframes.js/-/keyframes.js-6.0.0.tgz`, integrity `sha512-mpb3gSxU8UgO4HBBG2he6CFNCq7tW+k9id82DgAjeeDdeAmtEzmZ2/kuK3j5AbUZRULcN1QNkNJychNk49bT4Q==`, shasum `5cc5fdecb886f4df33371d3358239594a8965b6b`, provenance run `29499708034`. Glass now follows that exact immutable pair. **Current Glass evidence (V-A136):** 176 files/1,132 tests and typecheck are green; the earlier 1,127/769 counts, 3,572-module demo build and strict package closure are historical reports. Build, pack, verifier, native Q003 packet and Glass7 coordinate remain uncredited. The pair graph is already installed/proven; Glass must close Q003 natively and publish fresh Glass7. The historical V-A110 DNS failure never grounds a synthetic lock or artifact substitution. |
| Evidence | Native in-app Browser first: open/inspect/capture, one bounded change, reinspect. Unavailable access remains explicitly pending and is never Playwright-substituted. Ordinary tests/manifests/builds remain ordinary; no runner, receipt schema or gate farm is introduced. |

**V-A122 Glass-plan correction:** Q023's “drawer + sheet” wording is not a license to animate Drawer blur radius. The Drawer owner law is fixed-radius blur plus scalar dim; the graded edge belongs to a distinct sheet/overlay surface, and the engage register is minted only if a genuine second consumer exists. Otherwise Glass explicitly declines that register. This correction is sent to the active Glass task and creates no Value wave or process gate.

**V-A168 current-stage correction:** Glass has already proven the registry-only Value4/Keyframes6 install (one physical deduped chain, no legacy/file/link/git edges). U-F77/B1/B14 and residual Consumer-Cut/W2 producer-order language is historical/retired. The active Glass remainder is strict declarations/HeaderRibbon, stale demo-canon cleanup, native Q003 product-green evidence from honest 5/0, and a fresh Glass7 artifact; any older “regenerate the pair,” Keyframes re-close, Value rehearsal, or product-level decline instruction is historical. Value's own consumer refresh still waits for immutable Glass7.

The relay and audits surface V-A5 through V-A117. V-A117 records immutable Keyframes6; V-A88/V-A90–V-A92/V-A94–V-A96 hold Glass's first rehearsal, declaration and interaction credit. V-A97 keeps the immutable publication rail Value4→Keyframes6→Glass7 and breaks the demo-only cycle only after publication. W17 remains RED for fresh Glass declaration/native bytes, Value consumer refresh, consumers and routed mount; W33 remains sole release owner. No new wave/gate/compatibility path.

**HISTORICAL (V-A123):** V-A84–V-A87 settle earlier lineage; V-A93 holds 184,710 bytes and authorizes 184,254 bytes. V-A88/V-A90–V-A92/V-A94–V-A96 hold first Glass bytes/declaration/native interaction credit. The old W17 instruction to rebuild Keyframes or recreate a Value rehearsal is retired; the live W17 remainder is Glass strict-declaration/Q003-native validation, fresh Glass7 bytes and the downstream routed consumer. No classifier/parser/fallback/alias, nightly/ambient/typecheck mask, offset/cover/reset or consumer sizing shim.

## 2. Value-owned adoption

- Dock seal/edit, Picker selector/Spectrum/channel, eyedropper, palette, Generate, Mix and Gradient actions use WatercolorDot only as a noninteractive face inside a named geometric seat; ornamental and data-bearing static faces follow the hidden-versus-named-list distinction above.
- Owner-state, Admin Names and Mix source-mode tabs consume P092's one rect-matched fill without local background/pill/descendant geometry. The global pane selector/split state are deleted.
- Generate chooses existing Dock controls for the action cluster; no new producer primitive is justified.
- value.js executes the exact per-site Dialog/Drawer/Tooltip/Popover/Chip/Dock manifest above and deletes its forwarding wrappers; component names are never migrated by spelling alone.
- `/blob-config` supplies runtime-free configuration; `/blob` remains behind the lazy renderer boundary.
- value.js consumes the Aurora material/reveal seam without adding a local counter-filter or scheduler.
- Every real workbench maps to `InstrumentChassis` regions/phases or records why it is a semantic article/specimen Card; each divider is explicitly earned, and value.js creates no housing/boundary recipe.

## 3. Three-package clean cut

| Order | Package | Version | Required change |
|---:|---|---|---|
| 1 | `@mkbabb/value.js` | 4.0.0 | sole capability entries, final objects, parse result |
| 2 | `@mkbabb/keyframes.js` | 6.0.0 | selected packed dependency is exact `@mkbabb/value.js: 4.0.0`; the 5.3.5 packed diff proves the major; consume the decided Value capabilities, retire the named seams and keep Glass absent from runtime/optional dependencies |
| 3 | `@mkbabb/glass-ui` | 7.0.0 | Value `^4.0.0`, Keyframes `^6.0.0`, Pencil `^0.9.2`; first 966,350-byte rehearsal held by V-A88/V-A90–V-A92/V-A94/V-A95; fresh declaration/HeaderRibbon/Q003-native rehearsal pending; preserve immutable v6 |

**HISTORICAL (V-A123):** Each publish stage had its own fresh external install/build/type smoke: Value 4 alone, then Keyframes 6 without a Glass runtime or publication cycle, then Glass 7. Value4 and Keyframes6 have completed that order. Registry truth is now immutable Value 4.0.0 and Keyframes 6.0.0; Glass follows only that pair. W7's exact-seven bootstrap and held rehearsal lineage remain historical; V-A88/V-A90–V-A92/V-A94/V-A95 reject the first Glass archive until declaration ownership, explicit persistent/collapsible behavior and Q003 native integrity are exact. The live W17 work is fresh Glass bytes/native Browser, remaining old-pin removal, the nonempty routed mount and downstream consumer refresh; it does not recreate Value rehearsal bytes or replace producer bootstrap installs. W33 packs and publishes the remaining fresh Glass candidate and records registry equality, integrity, `gitHead`, export and declaration hashes; Keyframes's demo/dev Glass edge and mandatory lock refresh only after Glass publication. No file link, forced/fabricated/stale lock, downgrade, alias, shim, hoisting reliance, predicted artifact or dual graph bridges the stages. Existing Keyframes 6.0.0 and Glass `v6.0.0` coordinates remain immutable.

V-A115 supersedes the preceding V-A110 lock sentence: the exact registry-only Keyframes lock and fresh `npm ci` are observed green, and V-A117 records its immutable pack/provenance publication. Historical DNS failure remains rationale for never fabricating a lock.

## 4. External triggers

- NCSU-origin retirement is banked until VPN access; it is not a V source wave.
- W33 owns the canonical same-origin `https://color.babb.dev/api/**` edge: non-proxying DNS → dual-stack L4 TCP load balancer with PROXY v2 → redundant private NGINX+njs/QuickJS TLS edges. The one catch-all raw-buffer dispatcher serves immutable SPA assets, validates original API path bytes before routing, strips one literal `/api`, preserves the raw query, signs the private Hono origin and fully settles cookie-changing responses. W10/W16 own application/dev semantics. Cloudflare HTTP proxying, Worker/WAF/Pages/VPC ingress, public/direct Hono, credentialed CORS and an unsigned test bypass are retired rather than held as fallbacks.
- No V wave waits for an unrelated future glass-ui tranche; only the named Glass W17/W33 execution brief is an execution dependency. “glass-7 epoch” and evidence-derived `G_NEXT` are historical target labels until a fresh packed/provenance Glass7 coordinate exists.
