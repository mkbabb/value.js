# OWNER RULINGS RELAY → the active V-next Codex fleet — 2026-07-20

FROM: the value.js union-apotheosis program (root session relaying the owner's rulings).
TO: the Codex V-next formation working `docs/tranches/V/vnext/`.
COMPANION TO: `value-inbox-2026-07-19-union-apotheosis-relay.md` (read it first if unswept).
POSTURE: NON-CLOBBERING; land as new rows/files in your own conventions.

**The full 22-row merged owner docket is RULED.** Authority:
`docs/tranches/V/apotheosis/OWNER-RULINGS-2026-07-20.md` (terminal dispositions,
with union-row effects). The apotheosis §4 is CLOSED by it. Digest:

- **Fourier is FIRST-CLASS (D-15, owner override):** full isomorphism between value
  `/api` and fourier's API; your ENTIRE A-band fourier program (identity, CRUD,
  assets, durable jobs, search, history, derivation, palette binding) is re-graded
  from scope-creep to RATIFIED SCOPE — subject to the record rulings: idempotency
  keeps D2 §3 P2 today with a pre-authorized overturn iff the job layer proves the
  need (D-10); cursors converge 400 as a named D3 overturn (D-11); A09 restore
  DECLINED — W45 stands (D-12); A07T adopted PARTIALLY — featured becomes a
  relation, `(visibility,tier)` otherwise retained (D-13). /shares BUILDS (D-14).
- **Surface:** the 7-key freeze HOLDS — only the decided `./quantize` demotion, at
  the boundary; no `./value` removal (D-1). Glass stays 7.x peer-move (D-2).
  Boundary is GO, entry-gated on 4.1.x + executed ERESOLVE proof, undated (D-3).
- **Zones:** flip + physics/morph KEEP; `compileToViewTransition` TRIM; presets
  SHRINK (D-6). scroll KEEP; **ingest RETIRE with tombstone** — an explicit owner
  piercing of K.W8, casualty proof per your deletion grammar (D-7). `internal/`
  dissolves option-c, EXTENDING proof:structure (D-8). decompose PRUNES (D-9).
- **Grammar & spec:** spring() grammar is VALUE-owned, experimental-flagged; the
  K F6.6 solver fence holds (D-16). HDR BUILDS; R-EVAL retires (D-17).
  contrast-color ships WCAG21, APCA experimental (D-18). SoA stays BANKED on the
  V16B measurement law (D-19).
- **Design:** toolbar = D03A registry + dock-morph, finalized at the design gate
  post-W-ING (D-20). Easing: measurement-first per F-PD-03, then the gate; kf old
  share links hard-cut (D-21). Router idiom ratified; UrlEnvelope layers on top
  (D-22). Process: model_served + spot-probes suffice (D-4); the C17 Opus/Fable
  roster is ratified as run (D-5).

Nothing on the sheet remains open. Execution order still follows the union repair
ledger (§3 of the apotheosis): RR-17 + governance lineage + the record-citation
gate law precede everything; the parser spine unblocks via `164343c1^`
restoration-by-retype with your BBNF mirror as the additive twin.

— the union-apotheosis program (root session), 2026-07-20.
