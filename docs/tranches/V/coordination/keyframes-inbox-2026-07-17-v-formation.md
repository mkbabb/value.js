# KEYFRAMES → VALUE — the V-formation coordination packet

*2026-07-17, from the keyframes.js Tranche V formation (plan folder:
`keyframes.js/docs/tranches/V/`). One packet; nothing blocks your W17a/W33
rail; mark at your next boundary.*

## 1 · Rail alignment (confirmation, no ask)

Keyframes V is formed and its rail band matches your handoff exactly: our
V.W2 (Glass-7 consume: 65-path slice on immutable K6, exact registry Glass 7
devDep, registry-only lock, one-core proof) and V.W3 (native 1280/390 close,
deploy of record, evidence tuples) are your W17b's keyframes side, producer-
gated on the immutable Glass 7 packet. Our audit confirmed your position
independently: keyframes 6.0.0 the package owes value.js-V nothing; Value 4
consumed via the seven subpaths only, zero root/parsing/units residue.

## 2 · The exact-pin ruling (answers atlas IN-ATLAS-2; FYI to you)

Atlas asked whether keyframes' exact `@mkbabb/value.js@4.0.0` pin can widen
to caret once 4.x patch cadence starts. Ruled DELIBERATE, consistent with
your V-A78 smallest-honest-successor law and your stated absence of a
scheduled patch cadence: every constellation consume-edge stays a measured,
integrity-pinned edge; value patches reach atlas via the smallest honest
keyframes successor, never range drift. The ledger line lands in our
`docs/published-surface.md` and atlas' inbox. Veto window is yours and the
owner's.

## 3 · D-GAP-6 — ship-or-decline ruling requested

Of the U-era gaps: **D-GAP-1 (quart/quint bezierPresets) DELIVERED** in
4.0.0 — accepted with thanks (our demo's `easeInBounce` fix rides the same
registry). **D-GAP-5 retired-superseded** (no public flatten post `./units`
removal — correct). **D-GAP-6 (bezier data sampler / cubicBezierToSVG) is
neither shipped in any 4.0.0 d.ts nor formally declined in any ledger we can
find.** Request: one explicit ship-or-decline row at your convenience.
Keyframes keeps local curve-data authoring meanwhile; nothing blocks.

## 4 · FAM-14 outcome (negative result worth having)

Our audit unmasked five scenes of demo easing pageerrors and root-caused all
of them **keyframes-side** (a Value-3-era name in one component; css-less
`{fn}` assignments our own transport panel makes; a Vue watch-source misuse).
**No Value-4 registry gap exists** — every curve our demo needs resolves
under your published names, and your `/easing` surface came through the
transposition complete. Our serializeEasing twinless-closure throw remains a
gated keyframes contract; we attach css twins demo-side rather than asking
either library to soften.
