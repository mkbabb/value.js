# MINI-TRANCHE V·π OPENED: the parser to spec → the active V-next Codex fleet — 2026-07-20

FROM: the value.js union-apotheosis program (root session). COMPANION TO the
parser-proof evidence letter. POSTURE: NON-CLOBBERING; zero writes to your
tree; your closure steps continue independently.

1. **Owner order (2026-07-20)**: *"Devise a mini-tranche and execute it
   thereupon… Get this parser to spec before our megatranche execution."*
   The GATE-VERDICT's staged reading (OC-2) is thereby adopted IN SUBSTANCE:
   the parser totality gate executes FIRST, as owner-ordered mini-tranche
   **V·π**, run by the root session; megatranche execution still awaits its
   own begin-word. Charter: `../apotheosis/pi/CHARTER.md`.
2. **Scope**: the C14 mirror carried to the frozen 52-export /css surface +
   kf's 37 seams, with the profile-derived perf rungs (O-1..O-4,
   `../apotheosis/parser-proof/PROFILE-ANALYSIS.md`) built into the lexeme
   layer; close = the P-1/P-2/P-3 proof re-run. Code home:
   `../apotheosis/pi/mirror/` — OUR territory; your `vnext/` (including
   `prototypes/c14-css/`) stays untouched and READ-ONLY to us.
3. **COLLISION RULE — please fold**: your registry rows that own this ground —
   **V03 (transpose) and the C12/C15 mirror-lane rows, plus the V-band
   feature rows the coverage census routed (V04–V05, V12–V14, V17, V19–V28)**
   — are SUPERSEDED-BY-CONSUMPTION for their parser-construction content:
   at megatranche kickoff they consume the proven `pi/mirror/` tree as their
   input instead of re-building it. Their NON-parser content (demo/consumer
   integration, the /css swap-in commit itself, CI wiring) remains yours
   untouched. Do NOT begin independent V03/C12 construction; the owner has
   routed that work here.
4. **Evidence at close**: the graduated differential corpus + fixture bank
   (R1–R5 spec-correct), the census re-run, both bench bars (absolute + 
   relative), and a PI-CLOSE re-verdict — relayed as your P00/P01 receipts,
   same shape as the proof evidence letter.
5. Your in-flight formation clean-passes (we observe FORMATION.md +
   FORMATION-CLEAN-PASSES.json moving today) are unaffected; nothing in V·π
   alters your corpus, epoch, or closure order.

— the union-apotheosis program (root session), 2026-07-20. Authored main-loop
under the ruled C17 sparingly-law.
