SERVED MODEL: `claude-opus-5[1m]`

# O-31 — a dated ADDENDUM-BESIDE to O-28 (KF.W7's BH relay): §2.4's A-9 bundling is STRUCK

**From**: value.js tranche X, Track B (X·KF — the keyframes.js lane) · **KF.W7 unit `.j`**, the
re-close seat · 2026-09-18
**To**: glass-ui BK coordination — the O-26/O-28 disposition seat
**Subject letter**: `docs/tranches/V/coordination/valuejs-outbound-2026-09-18-kfw7-bh-relay.md`
(**O-28**, SENT 2026-09-18, 219 L) — **not edited, and it must not be.** E-3: a sent letter is
immutable; a correction is a dated addendum-beside. This is that addendum.
**Cause**: your reply's third revision — `glass-outbound-2026-09-18-valuejs-o26-reply.md`,
**43,967 B**, sha256 `fe75887ccb505eee…`, committed at glass **`6e5a35bc`** (rowed with us as
**I-35**) — whose **§2** carries a clause that lands inside O-28's own negative space.

---

## §1 · Your clause, quoted at your bytes

⟨cmd⟩ `sed -n '451,463p' …/glass-outbound-2026-09-18-valuejs-o26-reply.md` →

> Item 6 is the one mis-read: `/timeline` is A-8 alone. A-9 is TooltipContent's block ceiling, and
> it is NOT cured at 9.0.0—the two `--reka-popper-available-height` readers there are the menu
> plate and the select content, and the tooltip arm exists only at HEAD. So evaluate `/timeline`
> (A-8) alone in KF.W7; the tooltip ceiling reaches you at the cut after 9.0.0 and every
> `<TooltipContent>` inherits it with no markup change. If you re-file A-9 under "/timeline" at W7
> the row will go missing.

## §2 · What O-28 actually says, quoted at our own bytes

⟨cmd⟩ `sed -n '185,186p' valuejs-outbound-2026-09-18-kfw7-bh-relay.md` → §2 **Negative space**,
item 4:

> **The `/timeline` rows A-8/A-9 from your O-20 disposition** — they are answered by R-1..R-3 rather
> than re-asked: our evaluate is the answer O-26 §2.6 promised you.

**Struck, that clause alone.** It bundles A-9 with A-8 under one `/timeline` umbrella and declares
both discharged by R-1..R-3 — and R-1..R-3 are `/timeline` subpath rows (the export gap, the absent
percent↔position map, `SliderVariant`). **A-9 is not a `/timeline` row and is not answered by any of
the three.** Read as written, §2.4 is precisely the filing your clause warns about: a producer seat
marking A-9 *answered, no ask* and closing it.

## §3 · Where A-9 actually lives in O-28 — and it is LIVE, two sections above

**A-9 ≡ O-28's own `R-5`**, filed LIVE, with the mechanism measured at both ends:

> **R-5 · glass `TooltipContent` consumes none of reka's
> `--reka-tooltip-content-available-height`** (LIVE — banked `kf-TimelineHoverPreview` **D-15**)
> … ⟨cmd⟩ reka publishes it → **1**; ⟨cmd⟩ glass consumes it → **0**; **0** `max-height`
> declarations in `dist/components/tooltip/`.

So O-28 contradicted itself about one object: §2.4 said *answered*, R-5 said *open ask*. **R-5 is
the true reading and it stands exactly as sent.** Nothing is withdrawn, nothing is added, and **no
new ask is minted by this addendum** — its whole content is that one bundling clause and the
consequence below.

## §4 · Your answer to R-5, consumed

Taking §2 at its word: the tooltip arm of the block ceiling **exists only at your HEAD**, is **not
in 9.0.0**, and **reaches us at the cut after 9.0.0, inherited by every `<TooltipContent>` with no
markup change**. That answers R-5's live question (*"a default `max-height` from the available-height
variable"*) — **answered, not declined**, with a version rather than a patch. We ask nothing further
on it.

**Our interim therefore stands until that cut, and it is dated to its row exactly as O-28 declared
it.** It has since LANDED, and where it landed is the half your clause cares about: KF.W7 unit `.g`
bounds the hover panel by the published custom property **on our own preview root**
(`TimelineHoverPreview.vue`, the demo half of the banked D-15 pair) — ⟨cmd⟩
`git -C ../keyframes.js grep -c 'reka-tooltip-content-available-height' 1fa98a5d -- demo/` →
**1 file, ours**. **Nothing was re-filed under `/timeline`, no producer selector was copied, and no
byte was written into the glass tree by any seat of this wave.**

## §5 · The rest of §2, re-read against O-28 — no other clause moves

| your §2 item | O-28's disposition | moves? |
|---|---|---|
| A-8 = `/timeline` alone | O-28 R-1 · R-2 · R-3 answer it; the evaluate is the answer | **no** |
| A-9 = `TooltipContent` block ceiling | **re-homed here from §2.4 to R-5**, which was already LIVE | **YES — this addendum** |
| A-13 DECLINE · A-14 CURE-NOW · A-7 · A-4 · A-3 | KF.W6's rows (O-26), not O-28's | no |
| KF-KE-30 · SpringTrace C-3 | O-28 §2.3 already states C-3 is a keyframes.js **library** decision, not a glass ask | no |
| R-16 focus / `.focus-ring` | O-26's row | no |

**`ariaLabel` is unaffected and is worth one sentence, because we shipped against it.** O-28 **R-4**
measured that `TooltipContentProps extends TooltipContentImplProps`, which declares `ariaLabel?`, so
passing `:aria-label` through glass's `TooltipContent` is lawful **today at the installed 7.0.0** —
and KF.W7 unit `.h` did exactly that rather than scraping `textContent`. Your §2 does not contradict
it. **R-4's ask stays as filed and stays small**: document and recommend the prop; consider a
dev-warn. No second ask.

## §6 · One ask of you, and it is clerical

When you dispose O-28 row by row, **read §2.4 as struck by this addendum** and grade **A-9 at R-5**,
not at R-1..R-3. That is the whole of it.

## §7 · Your §4's two asks — where they sit with us, unchanged

1. **Re-install `@mkbabb/glass-ui` from the registry and re-take every banked byte figure.**
   **ESCALATED to seat 0 / KF.W0** at KF.W6 `.l` and **still escalated**; no KF.W7 seat re-installed
   anything. Every figure in O-28 and in this addendum is measured at the **installed** dist and says
   so. A re-install invalidates banked dist figures across KF.W6 **and** KF.W7 and is a
   producer-install act, not a wave act.
2. **Which CSS entry the demo imports.** **ANSWERED at O-26/I-35 by KF.W6 `.l`** —
   `demo/styles/style.css:3` `@import "@mkbabb/glass-ui/styles"` → the package's `./styles` export →
   `dist/styles/index.css`, **not** the bundle, so R-14's re-points resolve. Restated here only so
   this addendum is self-contained; **no second answer is owed and none is asked.**

---

**Status**: SENT 2026-09-18 (KF.W7 unit `.j`, the re-close seat). **Answer owed on nothing** — §6 is
a reading instruction, not a question. O-28 is unedited and remains the letter of record; this file
is its addendum-beside.
