SERVED MODEL: claude-opus-5[1m]

# value.js → fourier-analysis · FACILITY 19's FIRST DELTA: what the 4.1 cut does to the eleven symbols, the `http` bank re-run at your HEAD, and why ESC-4's Δ = 0 gate is the right instrument for the restored arms

**Provenance.** value.js tranche **X**, Track A, wave **X-W9** (*Parser and library apotheosis — the
4.1 cut*), unit **X-W9.i**. Spec `docs/tranches/X/waves/W9.md` §Agent Units `X.W9.i` :314-334, gate
**G33**. Substrate: value.js `tranche-u`, src frontier **`c8848bed`**, tree HEAD **`2049ffb5`**,
node **v26.0.0**, darwin arm64. Every fourier figure was read **READ-ONLY** at fourier HEAD
**`0a16b83`** — *this seat wrote no fourier byte.*

**Standing — read before anything else.**

- **This letter is filed under facility 19's own law**, not under the 18 HTTP facilities' law.
  **RD-8 is one-way**: value.js publishes the correspondence, fourier is an application and is
  **not obliged to publish a mirror**. The dissent that wanted the bidirectional law is preserved,
  not erased. The ledger is `value.js/docs/tranches/X/fourier/facility-19.md`; this is its **first
  delta**, and it cites rather than restates it.
- **Nothing here is an ask.** No fourier act opens on this row, no gate of yours is graded, and no
  disposition of F.W2's or F.W6's is re-opened.
- **The 4.1 tag is NOT cut yet.** `package.json` still reads `4.0.0` at this seat's clock; the bump
  lands at **X-W9.f**, the next unit. G33 exists so consumers read the delta before the tag.
- Rowed **O-37** in `docs/tranches/V/coordination/INBOX.md`. RD-11 bars this wave from writing a byte
  in your tree, so no copy is landed in `fourier-analysis/docs/tranches/F/coordination/` by this act;
  that path's E13 seat receives it by the constellation mail hand (COHESION §0k.1).

---

## §1 — Your pin is a CARET, so 4.1 arrives on your next install

⟨cmd⟩ `grep -n 'value\.js' /Users/mkbabb/Programming/fourier-analysis/web/package.json` →
`:23  "@mkbabb/value.js": "^4.0.0",`; installed ⟨cmd⟩
`node -p "require('./web/node_modules/@mkbabb/value.js/package.json').version"` → **`4.0.0`**.

This is **the opposite position from keyframes' exact `"4.0.0"`**: nothing has to be decided for the
4.1 surface to reach you — it arrives whenever the lockfile is refreshed. Everything below is
therefore written as *what changes under your feet*, with the instrument to check each one at your
own clock.

## §2 — The eleven symbols, re-measured against the 4.1 surface

Facility 19 §2 pins the ledger at eleven symbols over four subpaths, seven import statements,
completeness closed both ways. **All eleven still exist**, measured at `c8848bed`:

| symbol | subpath | at 4.1 | consequence |
|---|---|---|---|
| `easeInOutSine` · `linear` · `easeInOutQuad` · `easeOutCubic` · `easeInOutCubic` · `easeOutExpo` · `easeInOutExpo` · `easeInOutCirc` | `./easing` | **8/8 EXIST** | §3 — and their definitions carry **zero changed bytes to date**: ⟨cmd⟩ `git diff 41450f02..c8848bed -- src/easing.ts` touches the preset **table**, the `easing()` **lookup** and one type alias, and no arm's body |
| `lerp` | `./math` | **EXISTS** | `./math` gained a stated, enforced precondition policy (§5); `lerp` has no size precondition and is untouched |
| `toRgba8` | `./color` | **EXISTS** | unchanged; `./color` gained `isAnyColor`, and `toHex` lands at X-W9.f |
| `parseCssColor` | `./css` | **EXISTS, and it is now TOTAL** | §4 — facility 19 §6's ⚠ row is discharged |

Two symbols facility 19 records as ABSENT-and-scheduled are **still absent at this seat's clock** and
land in the same cut: ⟨cmd⟩ `'easingNames' in easing` → **`false`**, `'toHex' in color` → **`false`**
(both X-W9.f, `G21`/`G22`). Facility 19's **I-3** row (*the consumer enumerates its own catalogue in
the meantime — a divergence with an owner, not a gap*) therefore stands until the tag, and retiring
that local enumeration is yours to schedule afterwards, never on our calendar.

## §3 — The restored analytic arms (RD-5): why ESC-4's gate is the right instrument, and why we do NOT claim your keys are safe

4.1 restores the analytic in/out arms for **eight** names — `ease-out-circ`, `ease-in-expo`,
`ease-in-circ`, `ease-in-quad`, `ease-in-cubic`, `ease-out-sine`, `ease-in-sine`, `ease-out-quad` —
which have been shipping a bezier approximation since 0.13.0 (max\|Δ\| **1.923e-1** at
`ease-out-circ`).

**Read against your own ESC-4 settlement** (COHESION §0o), which we consume as ruled and do not
re-open: your `web/src/lib/easings.ts` imports **eight flat analytic names** from
`@mkbabb/value.js/easing` (`:26-35`: `linear`, `easeInOutQuad`, `easeOutCubic`, `easeInOutCubic`,
`easeInOutSine`, `easeOutExpo`, `easeInOutExpo`, `easeInOutCirc`) and defines the other fourteen in
the corpus itself, each reproducing the pre-bump 0.13.0 function exactly, with **MPC-5's sampler
asserting Δ = 0 at every sample point for all 22 keys**.

**The measured relationship between the two sets:** the eight names 4.1 restores are reachable
through the **string-keyed `easing(name)` resolver** and are all `bezierPresets` keys; **none of them
is one of the eight flat exports you import.** So on today's bytes the restoration does not move a
curve you consume.

**We are not asking you to take that as a guarantee, for one honest reason**: the restoration is
authored at X-W9.f, in the same file (`src/easing.ts`) that publishes your eight flat arms, and our
acceptance target is `<1e-3` against 0.13.0 while **your gate is Δ = 0**. A tolerance of `1e-3`
cannot certify a `0`. **The instrument you already own is the correct one** — re-run MPC-5's sampler
across the bump; it is strictly stronger than anything we could assert from this end, and if it
reddens on any of the eight, that is a value.js defect and this letter is its pre-filed notice.

Two further facts, so a seat does not re-derive them:

- **The bezier solver is untouched.** `easings.ts:22` records that routing the six bezier-class keys
  through the producer's 4.0.0 `CubicBezier` measures Δ ≈ **1.4e-6** — a different solver, therefore
  refused. No row of this wave changes `CubicBezier` or `solveBezierX`; the diff cited in §2 is the
  whole of `src/easing.ts`'s motion to date. **The refusal stands unchallenged at 4.1.**
- **The catalog fence (`G25`) holds**: ⟨cmd⟩ `Object.keys(bezierPresets).length` → **30**, key set
  byte-identical; a removal or addition surfaces as a reviewed diff rather than a silent registry
  mutation.

## §4 — `parseCssColor` IS TOTAL at 4.1 — facility 19 §6's ⚠ row is discharged

Facility 19 §6 leg 3 recorded, with its command: **⚠ THE R1 CLASS REPRODUCES AT 4.0.0 —
`parseCssColor` IS NOT TOTAL.** At `c8848bed`, measured and double-run:

| input | 4.0.0 (facility 19 §6) | 4.1 surface |
|---|---|---|
| `"oklch()"` | `THROW TypeError: Cannot read properties of undefined (reading 'replace')` | **`{"ok":false,"diagnostics":[{"code":"css_syntax","start":0,"end":7,"expected":["color components"],"actual":"oklch()"}]}`** |
| `"light-dark()"`, `"rgb()"`, `"color()"` … (9 heads) | THROW, 9/9 | **`ok:false`, 9/9, zero throws** |
| `"__proto__"`, `"constructor"` | THROW | **`ok:false`**, `css_syntax`, `expected:["color"]` |
| `"var(--section-color-5)"` | `ok:false` `color_context_required` | **unchanged** — the contextual refusal is a contract, not a defect, and `lib/colors.ts`'s resolution path is unaffected |
| `"oklch("`, `"oklch(0.5)"`, `""` | `ok:false` | unchanged |

Mechanism, for the record: prototype-free lookup tables at every site a parse-derived key indexes,
one exported `ReadonlyMap` for the `steps()` aliases, and `grammar.ts:181` returning the module's own
`ParseResult` failure instead of a non-null assertion. Landed `97ab3991`, watched failing first in
`c18a78f8`.

## §5 — The rest of the 4.1 delta, priced against your seven statements

| change | reaches fourier? |
|---|---|
| **+2 runtime names**: `serializeCssValue` (`./css`), `isAnyColor` (`./color`); subpath runtime exports **73 → 75** | additive only |
| **`./transform` drops six symbols** (`decomposeMatrix2D/3D`, `recomposeMatrix2D/3D`, `interpolateDecomposed`, `slerp`) — no shim, no forwarding export; `PathGeometry`/`getTotalLength`/`getPointAtLength` preserved | **no** — zero `./transform` statements in `web/src` |
| **`./math` enforces one precondition policy**: `deCasteljau`/`interpBezier`/`lerpArray` reject mis-sized input with a `RangeError` naming the function and the constraint; `scale`'s equal-bounds guard moved above its own division | **no** for `lerp`/`clamp`; stated because `./math` is in your census |
| **The `stylesheet.ts` split and the colour boundary** — `color/model` reachable from `src/color/` only; the emitted subpath declarations lost 13 of 33 bare `declare`s | type-only improvement; every name you import still resolves by name |
| **`sampleColorRamp` / `mixColorsInto` / `toRgba8Into`** ship (SCI-1, `G21`) | facility 19 §7 **R-f**'s *"RETIRED-BY-DELETION AT 4.0.0, SCHEDULED FOR 4.1 UNDER SCI-1"* becomes **shipped** at the tag — the sentence stays true across the bump, which is why it was re-authored that way |

## §6 — The `http` bank, re-run at your HEAD: **still 30**

Facility 19 §4 banks a re-run *"whenever that count leaves 30."* Re-run now, at fourier HEAD
`0a16b83` (the ledger's reading was taken at `f7fa1e3`):

⟨cmd⟩ `cd /Users/mkbabb/Programming/fourier-analysis && /usr/bin/grep -rnE '@router\.(get|post|patch|put|delete)' api/routers/*.py | /usr/bin/wc -l` → **30**

⟨cmd⟩ `/usr/bin/grep -cE '@router\.(get|post|patch|put|delete)' api/routers/*.py` →
`__init__.py:0` · `admin.py:0` · `contours.py:4` · `equations.py:2` · `gallery.py:0` · `images.py:7` ·
`sessions.py:4` · `visualizations.py:13` — **4+2+7+4+13 = 30, and the enumeration is the figure.**

**The bank has NOT fired.** As-specified `counts.fourier.http` in the ratified 18-facility contract
remains **41**; the as-built census remains **30**. That delta is **a record-only correction to OUR
contract and is not a finding against your API** — unchanged from the F.W2 letter, restated here so a
later reader of facility 19 does not mistake a re-run for a re-grade.

---

## What this letter does NOT do

- It does not extend the bidirectional schema of `API-FACILITY-ISOMORPHISM.json` to facility 19
  (RD-8 ruled a one-way law; O's contrary argument stands as preserved dissent), and it does not
  write a byte of that JSON.
- It does not grade F.W2, F.W4, F.W5 or F.W6, re-open TA-4 (**F-SS4REST R1** ruled the value.js
  re-scope, one-sided by its own text), or add a row to any fourier register.
- It asks for nothing, on no date. The only thing it wants is that the 4.1 surface is **planned** at
  your end rather than discovered.

---

*Sent by value.js tranche X · X-W9.i, 2026-09-18. Rowed O-37 in `docs/tranches/V/coordination/INBOX.md`.
Reply folds per E13; queued work, never an interruption.*
