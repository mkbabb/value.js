# Tranche V — First-Party Consumer Cut

The 4.0 removal of `@mkbabb/value.js` root is not complete when only keyframes/glass compile. This manifest covers tracked HEAD source, demos, tests, benches, scripts, build configuration and current public documentation in first-party package repositories under `/Users/mkbabb/Programming`; hidden worktrees, generated output, clearly labeled non-executable historical archaeology and detached duplicates are excluded. Dirty/staged primary-tree changes are user work, not authority.

Historical packed-diff labels `K_NEXT=6.0.0` and `G_NEXT=7.0.0` are now fixed to immutable Keyframes6 and the evidence-selected Glass7 target. Value4 and Keyframes6 are immutable registry producer coordinates. V-A93's 184,710-byte and 184,254-byte Keyframes archives are historical transport evidence only. V-A88/V-A90–V-A92/V-A96 close Glass declarations and explicit persistent/collapsible HeaderRibbon behavior; V-A94/V-A95 record honest Aurora cue/aperture and post-drag evidence, while V-A124/V-A125 require live-backdrop adjacency isolation after the failed ~95→5 and reverted 3→0 filter experiments, with the honest 5/0 baseline restored, before a fresh Glass archive. V-A126 retires the producer-order tail: no Value/Keyframes rebuild or rehearsal remains live. V-A97 binds immutable publication to Value→Keyframes→Glass and keeps Keyframes's demo-only Glass edge outside the library-release prerequisite. Rehearsals are not registry coordinates; Glass consumes only the published pair.

**V-A168 current-stage override:** Historical producer/rehearsal/order procedures do not reopen Value4 or Keyframes6. The live rail is W17a product-green strict/native Glass rehearsal → W33a nonterminal publication → W17b registry consumer refresh/routed mount → W18–W32 → W33b final close. The API/UI rows V-A137–V-A168 remain RED under their named waves. A declined Q003 hypothesis cannot authorize bytes while corruption remains; no workaround or historical pack is current.

**V-A168 direct Glass brief, superseding V-A130's decline shorthand:** W17a's active Glass task consumes only registry Value4 + Keyframes6, closes strict declaration reach and persistent/explicit-collapsible HeaderRibbon behavior, removes/annotates stale Value3 demo canon, and repeats native Browser forward/reverse Q003 captures from honest 5/0 until the product shows zero slab, occlusion corruption and lost target. A falsified causal hypothesis may be declined; visible corruption may not. W17a emits one fresh rehearsal, W33a alone publishes it, and W17b refreshes the value consumer lock and routed mount. No held archive, browser-only wait, backing plane, filter target, sampler, mask/cover, Keyframes/Drawer change, alias, fallback or stale lock earns credit.

## 1. Repository disposition

| Repository | Authoritative package truth | Root consumers | Disposition / owner |
|---|---|---:|---|
| `atlas` (stale-archive) | tracked HEAD `1cbfcf3`; 1.1.3; peer `^3.1.0`, dev/resolved 3.1.0 | 16 | **STALE-ARCHIVE (two-atlas correction, `ATLAS-INBOUND-2026-07-17`, folded W40-S5):** this row describes the standalone `/Users/mkbabb/Programming/atlas` checkout — **stale master, 1.x-era**. The ACTIVE atlas of record is the sci-report `atlas/` subtree lineage (branch `p/totality`, commits `b066b2b..092df4f`), **ALREADY fully migrated against immutable value 4.0.0 registry bytes** (zero bare-root/`/parsing`/`/units` imports; `srgbToOKLab`/`oklabToRgb255` gone from clone-overlay) — its BUILD costs zero migration and is discharged at the active lineage. Any residual W17/W33 row is scoped explicitly to this stale-archive checkout, NOT "atlas"; the primary's unrelated dirty `src/filter/ui/SourceDataBrowser.vue` is preserved without overwrite. **sci-report (the app) deliberately holds value `3.1.0` + keyframes `5.3.5` + glass `6.0.0` and crosses to value 4 ATOMICALLY at its glass-7 consume (#227) — no early bump.** |
| `bbnf-buddy` | private 0.1; `^0.10.0`, resolved 0.10; active local/private, no origin | 7, including Vite config | **BUILD**; no origin blocks local compile, not migration |
| `bbnf-lang/playground` | private; `^0.10.0`, resolved 0.10; active 2026-07-15 | 3 | **BUILD** from tracked HEAD; protect dirty `playground/vite.config.ts` clean-break alias removal and untracked `playground/test-results/`, then three-way integrate without overwrite |
| `fourier-analysis/web` | private 0.1; tracked HEAD `^0.10.0`, resolved 0.10 | 6, including Vite config | **BUILD** from HEAD; preserve dirty `^0.13.0` package/lock and reconcile explicitly at W33 |
| `glass-ui` | immutable v6 baseline; first local 7.0.0 rehearsal HELD under V-A88/V-A90–V-A92/V-A94–V-A96; current ordinary batch LANDED/pre-release against immutable Value4 + Keyframes6 | undeclared/broken upstream declaration reaches, vacuous/inferred HeaderRibbon behavior, clipped Aurora cue and post-drag compositor corruption | **BUILD W17 / SHIP W33**; the immutable pair graph is already proven; Glass closes declaration plus Q003 native rows in one fresh rehearsal, then publishes; W33 alone records release coordinates |
| `keyframes.js` | immutable published 6.0.0 against exact Value4; held rehearsal candidates remain historical | exact Value4; root+`./engine`; no runtime/peer/optional Glass edge; false public `implements` absent; strict consumer green | **SHIPPED**; gitHead `5a9183a7…`, provenance `29499708034`; post-Glass demo/deploy edge remains downstream |
| `muster/frontend` | private 0.1; `^0.10.0`, resolved 0.10; quiet-active | 1 dynamic | **BUILD**; preserve lazy loading and all 89 current primary evidence/doc changes; no present source/manifest overlap |
| `sci-report` | private 1.0; tracked `dashboards/package.json` pins/resolves value 3.1.0; relocation landed at `4b45a3b6` | 6 | **BUILD** from HEAD; preserve four current untracked doc/research trees and update the landed dashboards manifest/lock at W33 |
| `speedtest` | 1.0; `^0.13.0`, resolved 0.13 | 8 source/probe + 2 tests | **BUILD** |
| `slides` | active clean primary at `b538506`; glass `3.13.0`, keyframes `^3.0.0`, lock transitively value 0.10/0.11 | 16 glass source/config files; one dynamic keyframes root plus Vite root classifier | **BUILD** Glass7/immutable Keyframes6 consumer cut; primary owns this remote |
| `words/frontend` | active 2026-07-15; glass `^3.0.0`, keyframes `^2.2.0`, root lock transitively value 0.10 | 88 glass source/config files; keyframes root in `FancyF` and Vite prebundle | **BUILD** from tracked HEAD; protect extensive dirty primary and modified root lock, then reconcile at W33 |
| `bbnf-lang-json-threaded-fork` | detached local-origin clone; `^0.5.1` | 3 duplicates | **RETIRE** as independent consumer: primary `bbnf-lang` owns the same easing mechanism; archive remains untouched |
| `slides-K` | clean duplicate clone at `5b546be`; same `git@github.com:mkbabb/slides.git` origin | duplicates the slides dependency graph | **RETIRE** as independent consumer; the primary `slides` BUILD row owns the remote and this clone remains untouched |
| `hassio-config-design-lab` | non-git prototype archive with seven vendored import maps; no package/lock | — | **RETIRE** from the package cut; revival is a new product ask, not a hidden V consumer |

No consumer census row is unowned. Glass producer rows remain explicitly RED until their owning work lands; immutable Keyframes6 is the producer artifact, while prospective source files in protected worktrees are historical context only. W17 proves all BUILD rows against the registry pair on dedicated clean worktrees; W33 updates tracked manifests/locks only after the Glass7 registry cut and will not overwrite dirty primary trees.

## 2. Exact old symbol → new contract

| Old root symbol/family | Sole 4.0 target | Semantic cut |
|---|---|---|
| `RGBColor` | `/color` `rgb(): Result<Color<"rgb">,ColorIssue>` | immutable typed Color; no nominal construction or raw byte helper |
| `OKLCHColor` | `/color` `oklch(): Result<Color<"oklch">,ColorIssue>` | immutable typed Color; no class construction |
| `color2` | `/color` `convertColor()` | total static conversion; no dynamic registry |
| `srgbToOKLab`, `oklabToRgb255`, raw OKLab/OKLCH helpers and direct pairwise primitives | `/color` `rgb`/`oklch` + `convertColor` + `toRgba8` | final-object construction/conversion and sole explicit byte projection; exact vectors retained, all raw/direct names deleted without aliases |
| old `mixColors`, lightness-proxy `safeAccentColor`, normalized-turn `interpolateHue` | `/color` exact `AnyColor`/`Result` operations | explicit mix space; actual typed surface plus requested WCAG ratio; public hue consumes/returns degrees; no silent identity/fallback |
| `FunctionValue` | `/value` `CssCall` | discriminate `kind === "call"`; no nominal `instanceof` |
| `parseCSSValueUnit` | `/css` `parseCssScalar()` | full-consumption `ParseResult<CssScalar>`; invalid edit remains a visible draft diagnostic |
| `parseCSSValue` | `/css` `parseCssValue()` | `ParseResult<CssScalar | CssCall | CssList>`; caller explicitly propagates/rejects |
| `parseCSSValues`, older `parseCSSSubValue` | `/css` `parseCssValues()` | one full-consumption `ParseResult<CssList>`; neither old name/options path survives |
| `parseCSSColor` | `/css` `parseCssColor()` | `ParseResult<CssColor>` over the closed thirteen-space CSS-native union; no transparent/substitute fallback and no library-only spelling |
| `parseCSSStylesheet`, `extractKeyframes`, `extractProperties` | `/css` `parseStylesheet`, `collectKeyframes`, `collectPropertyDescriptors` | recursive collectors return document-ordered `CollectedRule` rows with AST index paths; they neither concatenate duplicate keyframes nor erase conditional/layer ancestry; invalid pose import is a named diagnostic boundary |
| declaration-list flattening, `extractStyleRules` | `/css` `collectDeclarations`, `collectStyleRules` | one-list cascade keyed by CSS property: important beats non-important regardless of order; equal importance uses later source order; it never masquerades as `@property` extraction |
| `extractFunctions`, `extractAnimationOptions` | `/css` `collectCustomFunctions`, `collectAnimationOptions` | typed custom-function grammar remains producer-owned; `parseStylesheet` diagnoses every shorthand/list arm while source spans exist, then `collectAnimationOptions(readonly Declaration[])` structurally expands shorthand and returns frozen option rows for one deliberately selected rule without merging selectors; Keyframes deletes its local classifier and explicitly selects `.at(0)` because its engine consumes one animation |
| `extractTimelineOptions`, direct timeline/range parsers, `serializeTimelineOptions` | `/css` `collectTimelineOptions`, `parseAnimationTimeline`, `parseAnimationRange`, `serializeTimelineOptions` | `collectTimelineOptions(readonly Declaration[])` projects one selected rule; one typed VALUE grammar/inverse remains while Keyframes owns live TIME/DOM resolution, not a duplicate parser |
| `coerceToSyntax`, nullable syntax validation | `/css` `coerceToSyntax(): ParseResult<CssValue>` | mismatch/invalid descriptor is explicit; no `null`, permissive unknown component or second consumer validator |
| `parseCSSTime` | `/css` `parseCssScalar()` plus Keyframes-local explicit `s`/`ms` conversion | sole external site is `src/animation/engine/options.ts`; no second CSS token parser |
| Keyframes range-selector token parser/regex | `/css` `parseKeyframeSelector()` | `from` maps to the frozen typed percent selector at `0` and `to` to the one at `1`; canonical serialization emits `0%`/`100%` and reparsing preserves the typed values; named `entry|exit|cover|contain` carries an optional normalized `[0,1]` offset whose absence stays bare and differs from explicit zero; Keyframes keeps phase resolution but deletes duplicate token parsing |
| `formatCSS`, `serializeStylesheetItem`, `reverseAnimationShorthand`, `reverseCSSTime` | Keyframes-local `src/animation/compile/emit/css-text.ts` | one move-only text owner; value implementations/exports/tests die and no forwarding path remains |
| Keyframes `getTimingFunction` plus regex/throw guards | `/css` `parseTimingFunction()` + `/easing` numeric catalog | full-consumption `ParseResult<CssTimingFunction>`; invalid explicit/per-keyframe CSS is diagnosed, never inherited/defaulted; the kf omnibus API dies |
| `PropertyDescriptor` | `/css` `CSSPropertyDescriptor` | live typed `@property` metadata survives under its collision-free semantic name; no alias or `_2` rollup leak |
| `COMPUTED_UNITS` plus kf `WAAPI_INELIGIBLE_UNITS` | `/value` `isLayoutTrackingUnit()` | one producer-owned semantic classification; keyframes retains only its property-specific path-relative `%` exemption |
| `ValueUnit`/`ValueArray` mutation and kf `plain-vars.ts` | immutable `/value` final values → kf-owned `InterpSlot` | compile slots/sinks once; custom transform and group paths emit authored numbers/CSS directly, with no boxed-leaf adapter |
| `CSSCubicBezier`, throwing/partial bezier constructors | `/easing` `CubicBezier(...): Result<EasingFunction,EasingIssue>` | exact clean-major name and final numeric closure; no nominal class or compatibility export |
| `parseSteps`, `getTimingFunction`, identity-on-unknown lookup | `/css` `parseTimingFunction()` + `/easing` `steppedEase`/`easing` Results | CSS parsing stays in `/css`; numeric construction is total/failure-explicit and never masks invalid input as linear |
| named easing functions, `TimingFunction`, `timingFunctions`, preset tables | `/easing` `EasingFunction`, the exact nine named exports, `linear`, `CubicBezier`, `steppedEase`, `linearEasing`, `easing`, `jumpTerms`, `bezierPresets` | `easeOutExpo`, `smoothStep3` and `easeInBounce` remain canonical direct implementations alongside the six already retained named functions; dynamic lookup handles the `easing(name)` Result; the `timingFunctions` aggregate/fallback dies; no root import or parser dependency |
| `timingFunctionDescriptions` | bbnf-buddy `src/animation/easingGroups.ts` and Keyframes `demo/utils/reference-data/animationDescriptions.ts` | transpose only the exact display copy each UI renders; Value's data/export and every forwarding table die, while each consumer owns its own product prose |
| six kf quart/quint preset rows | `/easing` canonical `bezierPresets`/catalog | exact names/control points ship in value 4; kf's whole parallel bezier table dies |
| `flattenObject` leaf-predicate request (D-GAP-5) | **DECLINED—superseded** | `/units` and the generic flatten API die; two timeline callers consume compiler/typed-declaration output instead |
| `cubicBezierToSVG` and D-GAP-6 upstream sampler ask | **DECLINED upstream** | unused whole-element helper dies; one kf-local presentation sampler requires explicit `flipY` |
| `clamp`, `lerp` | `/math` same names | exact capability entry |

No audited non-core consumer needs `/transform` or `/quantize`. No audited consumer earns a value `/browser` entry: W17 moves the sole live computed-resolution/epoch/cache mechanism into Keyframes rather than publishing a consumer-less substrate.

The bounded current-source/active-consumer census closes the four formerly prose-only entries and the formerly partial `/css` row:

| Entry | Exact runtime exports | Exact type exports / disposition |
|---|---|---|
| `/value` | `isLayoutTrackingUnit` | `CssScalar`, `CssCall`, `CssList`, `CssValue`; immutable structural values only, with no class, constructor registry, DOM/cache owner or extra constant |
| `/css` | `parseCssScalar`, `parseCssValue`, `parseCssValues`, `parseKeyframeSelector`, `parseCssColor`, `serializeCssColor`, `parseStylesheet`, `collectKeyframes`, `collectDeclarations`, `collectPropertyDescriptors`, `collectCustomFunctions`, `collectStyleRules`, `collectAnimationOptions`, `coerceToSyntax`, `parseAnimationTimeline`, `parseAnimationRange`, `collectTimelineOptions`, `serializeTimelineOptions`, `parseTimingFunction` | `CssColorSpace`, `CssColor`, `ParseIssue`, `ParseResult`, `CssLinearStop`, `CssTimingFunction`, `Declaration`, `KeyframeSelector`, `KeyframeRule`, `CSSPropertyDescriptor`, `CustomFunctionParameter`, `CustomFunctionDescriptor`, `CSSAnimationOptions`, `ScrollerKeyword`, `TimelineAxis`, `ViewInset`, `AnimationTimelineValue`, `RangePhase`, `RangeBoundary`, `AnimationRangeValue`, `TimelineScopeValue`, `TriggerType`, `AnimationTriggerValue`, `CSSTimelineOptions`, `ScrollTimelineDescriptor`, `ViewTimelineDescriptor`, `StyleRule`, `KeyframesBlock`, `PropertyRule`, `CustomFunctionRule`, `StylesheetItem`, `Stylesheet`, `CollectedRule`; exactly 19 runtime/33 type names and ten source-consuming `ParseResult` operations; the four text helpers move once to Keyframes and every unlisted old parser/facade symbol is private or retired, never an extra export |
| `/math` | `clamp`, `scale`, `lerp`, `lerpArray`, `logerp`, `deCasteljau`, `cubicBezier`, `interpBezier`, `cubicBezierToString` | no public types; `cubicBezierToSVG` is deleted, while the actively imported numeric/string leaves remain |
| `/transform` | `decomposeMatrix2D`, `decomposeMatrix3D`, `recomposeMatrix2D`, `recomposeMatrix3D`, `slerp`, `interpolateDecomposed`, `PathGeometry`, `getTotalLength`, `getPointAtLength` | `DecomposedMatrix2D`, `DecomposedMatrix3D`, `Vec4`, `Mat4`, `Point`, `PathSample`; Keyframes' two morph modules consume `PathGeometry`, and retained value matrix/path tests own the rest |
| `/quantize` | `quantizePixels`, `dominantColor` | `QuantizeOptions`, `QuantizedColor`, `QuantizeIssue`; both operations return `Result`, and `QuantizedColor` contains final `Color<"oklch">` plus population—not CSS text or parallel raw tuples |

The fixed `/css` counts remain exactly 19 runtime and 33 type exports while one canonical W9 grammar accepts and canonical-roundtrips the declared arithmetic already consumed by immutable Keyframes 5.3.5: numeric `calc(1px + 2px)`, conditional `if(supports(...))`, `sibling-index()`, and arithmetic inside `@function`. Focused vectors prove immutable AST success and canonical parse→serialize→parse behavior for all four families, plus exact readonly-nonempty diagnostics for malformed operator, function and `supports` arms. No second stylesheet normalizer, Keyframes-local parser, fallback, alias or dual grammar survives. Counts change only if a separately enumerated declaration defect proves that necessary.

The value demo's active quantize consumers are `demo/@/components/custom/image-palette-extractor/quantize-worker.ts`, `composables/useImageQuantize.ts` and `composables/useExtractSession.ts`. W2/W8 migrate them to handle the `Result` and consume `QuantizedColor.color`; any CSS presentation explicitly calls `/css` after the final-object result. Keyframes' live `/math` imports consume `clamp|scale|lerp|lerpArray|cubicBezierToString`, and its live `/transform` imports consume `PathGeometry`; the retained source/test leaves above remain one exact public set rather than a wildcard.

**HISTORICAL (V-A126):** V-A73 makes value's original internal consumer cut explicit. W7 emitted one exact-seven bootstrap and the historical W17 transport was consumed into the immutable Keyframes6 library. Glass now consumes only immutable registry Value4 + Keyframes6; the live W17 remainder is Glass strict/native/release closure, followed by the stale value.js consumer refresh and routed Browser proof. No old export, alias, shim, parser, file link, hoisting, offset/cover/reset, consumer sizing/overflow, fallback or dual path may mask the graph.

`/easing` is exactly 16 runtime and five type names: `linear`; the nine direct numeric functions `easeOutCubic`, `easeInOutSine`, `easeInOutCubic`, `easeInOutQuad`, `easeInOutExpo`, `easeInOutCirc`, `easeOutExpo`, `smoothStep3`, `easeInBounce`; and `CubicBezier`, `steppedEase`, `linearEasing`, `easing`, `jumpTerms`, `bezierPresets`. Ordinary direct-call samples pin the three newly recaptured functions, and name-resolution samples pin their camel-case and kebab-case spellings without a `timingFunctions` or description registry.

`ARCHITECTURE.md §3` is the sole mechanical package contract: the seven explicit `types`/`import` export-map peers, absent package root and seven complete exact declaration blocks. Glass consumes only the `/color` + `/css` + `/easing` subset it needs—`Result`, structural `Color`/`AnyColor`, all 17 mapped factories and object/conversion oracles, `RGBA8`/`toRgba8`, `ParseResult`, CSS-native color/timing and named easing exports—but that consumer slice is not the whole `/css` declaration. Only thirteen CSS-native spaces cross `/css`; library-only colors explicitly convert. Consumer code may not infer names from 3.x source or prose. **HISTORICAL (V-A126):** the bootstrap and held intermediate were transport evidence only; immutable Value4 and Keyframes6 now supply the producer surface. W7's exact-seven checks remain historical evidence, and the live Glass cut must reject missing, extra, default, wildcard and value/type drift across all seven entries.

**HISTORICAL (V-A126):** The held Glass rehearsal consumed historical Keyframes bytes and selected 7.0.0; earlier archives are transport evidence only. The immutable Keyframes6 artifact removes the false public `implements` claim and passes strict consumption. Glass source closes inference with omitted/`mode="persistent"` behavior and explicit `mode="collapsible"` ownership of a focusable disclosure button. V-A94/V-A95 hold native interaction/rehearsal credit for the clipped Aurora cue and transient post-drag compositor corruption; V-A96 holds strict declaration credit until accidental stable Reka/VueUse type chains are absent. The ordinary Glass batch is LANDED/pre-release; fresh Glass7 bytes, native/product rows and the routed constellation remain RED. No exposed internal members, nightly/ambient injection, hoisting, file-link, DOM/VNode inference, sizing, overflow, z-index, offset/cover/reset, timing, alias or fallback workaround.

## 3. Tracked callsite manifest

### atlas — BUILD (stale-archive checkout)

**CORRECTION (two-atlas disambiguation, `ATLAS-INBOUND-2026-07-17-two-atlas-disambiguation.md`, folded W40-S5):** the callsites below describe the STALE-ARCHIVE standalone `/Users/mkbabb/Programming/atlas` checkout (1.x-era master). The ACTIVE atlas of record — the sci-report `atlas/` subtree lineage, branch `p/totality`, commits `b066b2b..092df4f` — is **ALREADY fully migrated against immutable value 4.0.0** (zero bare-root/`/parsing`/`/units`; `srgbToOKLab`/`oklabToRgb255` replaced by structural `rgb`/`mixColors`/`toRgba8` with a cached-endpoint hot path; `CSSCubicBezier` → fallible `CubicBezier`; easing/math leaves on their capability subpaths). This §3 atlas manifest is therefore **discharged at the active lineage**; the rows below are retained only as the stale-archive callsite record.

- `/color`: `src/story/clone-overlay.ts` (`srgbToOKLab`, `oklabToRgb255`).
- `/easing`: `src/motion/buildMarkAnimation.ts`, `motion-director.ts`, `reveal-register.ts`, `useScrollLettering.ts`, `useScrollProgress.ts`, `useScrollTimeline.ts`, `useSectionReveal.ts`, `variant-registers.ts`, `src/platform/composables/useCountUp.ts`, `src/story/corridor.ts`; the exact migration retains canonical `easeOutExpo` and `smoothStep3` direct calls rather than reimplementing them locally.
- `/math`: `src/motion/buildMarkAnimation.ts`, `motion-director.ts`, `resolveVariant.ts`, `reveal-register.ts`, `seededVariety.ts`, `useCoverProgress.ts`, `useScrollLettering.ts`, `useScrollProgress.ts`, `useScrollTimeline.ts`, `useSectionReveal.ts`, `src/platform/chrome/background/composables/aurora-nuclei.ts`, `src/platform/composables/useCountUp.ts`, `src/story/StoryCorridor.vue`, `clone-overlay.ts`, `corridor.ts`.

Conversion vectors and every motion call build unchanged; this is direct capability migration, not an excuse to retain the root. At the formation re-census, `src/motion/useScrollTimeline.ts` is clean and unrelated `src/filter/ui/SourceDataBrowser.vue` is dirty. W17 starts from tracked HEAD `1cbfcf3` in a separate worktree and does not edit that primary. W33 re-censuses immediately before integration and applies only the import/manifest/lock delta; any conflict that cannot preserve then-current user work blocks the cut rather than overwriting or normalizing it.

### bbnf-buddy — BUILD

- `/value`: `src/editor/components/animation/AnimationWorkspace.vue` (`FunctionValue` → `CssCall.kind`).
- `/css`: `src/components/EditableNumber.vue`, `AnimationWorkspace.vue`, `src/poses/css.ts`.
- `/easing`: `src/animation/easing.ts`, `easingGroups.ts`, `runtime.ts`; `easingGroups.ts` replaces its `timingFunctionDescriptions` import with the local display strings this UI actually renders and continues to consume Value's numeric/preset authority.
- Build configuration: `vite.config.ts:42` removes the bare-root `optimizeDeps.include` and lists only `/value`, `/css` and `/easing`, the capability entries this application actually imports. A package root kept solely for prebundling is still a forbidden consumer.

`EditableNumber` keeps invalid text as a draft plus diagnostic; `AnimationWorkspace` handles `ParseResult` before the final-value discriminant; `cssToPose` returns a named invalid-pose result. None may catch and return a silent null/substitute.

The primary bbnf-lang tree currently carries a user delta in `playground/vite.config.ts` that already removes a keyframes root/dist alias plus untracked `playground/test-results/`. W17 rehearses tracked HEAD in isolation. W33 three-way integrates the package/config cut onto that clean-break delta; it neither restores the alias nor overwrites the results.

### bbnf-lang/playground — BUILD

`playground/src/components/landing/useHeroSequence.ts`, `playground/src/composables/useScrollMorph.ts`, and `useScrollTimeline.ts` move `easeOutCubic` to `/easing` and retain their motion results.

### fourier-analysis/web — BUILD

`src/components/equation/ConvergencePlot.vue`, `composables/useCurveTransition.ts`, `lib/harmonics.ts`, and `src/lib/easings.ts` import `easeInOutSine`, `easeInOutCubic`, `easeInOutQuad`, `easeInOutExpo`, and `easeInOutCirc` as named `/easing` exports. Any dynamic name lookup calls failure-explicit `easing(name)` and handles its `Result`; no `timingFunctions` aggregate, property fallback, identity substitution, or local mirror survives. The migration worktree starts from tracked `^0.10.0`; dirty `^0.13.0` files are never normalized away.

`vite.config.ts:57` replaces the bare-root `vendor-math` manual chunk with a semantically named `vendor-easing` chunk whose sole input is `/easing`, the only value.js capability used by this application. Configuration-only resolution and chunk vocabulary are part of the clean break; neither may preserve an otherwise dead root.

### muster/frontend — BUILD

`src/composables/useSlugIdentity.ts` changes its dynamic import to `import("@mkbabb/value.js/color")`; `OKLCHColor`/`color2` become `oklch()`/`convertColor()`. The OKLCH→HSL range assumption gets a semantic vector test, and the module remains absent from the initial chunk.

All 89 current primary-tree evidence/doc changes—87 tracked items including `docs/tranches/K/PROGRESS.md` and two untracked audit documents—are user work. They have no present source/manifest overlap; W33 nevertheless stages only the exact frontend manifest/lock cut and re-censuses before integration.

### sci-report — BUILD

- `/easing`: `dashboards/demand/features/trajectory/DemandRidge.vue`, `dashboards/sci/features/rainbow/RainbowStack.vue`, `dashboards/usf/features/flow/composables/useArrivalSequence.ts`, `dashboards/usf/features/normalization/NormalizationFlip.vue`, `dashboards/usf-integrity/features/scatter/composables/useScatterOption.ts`.
- `/math`: `DemandRidge.vue`, `useScatterOption.ts`, `dashboards/vft-germination/features/germination/composables/useGerminationCurves.ts`.

The `app/package.json → dashboards/package.json` relocation already landed in `4b45a3b6`; W17 starts from tracked HEAD with the dashboard manifest as the sole authority. Four current untracked doc/research trees remain user-owned. W33 updates the dashboard manifest/lock only after re-censusing the exact primary state; inability to preserve adjacent work blocks the merge rather than reverting or overwriting it.

### speedtest — BUILD

- `/color`: `scripts/probes/wc-spectrum-evidence.mjs`, `src/features/speedtest/ui/meter/canvas/meter/rings-color.ts`, `tests/features/speedtest/ui/meter/canvas/meter/rings-color.test.ts`, `tests/phase-color-parity.test.ts`.
- `/css`: `src/components/dashboard/charts/chartMetrics.ts`, `src/components/dashboard/composables/useEChartsTheme.ts`, `tests/phase-color-parity.test.ts`.
- `/easing`: `src/design/motion.ts`.
- `/math`: `src/features/speedtest/composables/meter/meterCompletion.ts`, `paintMeter.ts`, `src/features/speedtest/ui/MeterColumn.vue`.

`RGBColor` becomes `rgb()` while preserving the explicit byte-space mixture. Canonical chart colors that fail parsing raise a named invariant error. ECharts theme failure retains the last valid theme and exposes configuration error; it may not return transparent as a masking fallback.

### slides — BUILD

- Preserve every existing semantic glass subpath across the 16 tracked source/config consumers, including Dock/Button/Dialog/forms/controls, Constellation, FourierField, StatusDot and the shared styles entry; where Glass7 retires an entry, the exact producer manifest names the one semantic replacement rather than routing through a root barrel.
- `src/deck/deckSpring.ts` replaces its dynamic keyframes root with the exact immutable `@mkbabb/keyframes.js@6.0.0/engine` capability entry and preserves lazy loading plus its spring-result vectors. `vite.config.ts` classifies the exact Glass7/Keyframes6 entries rather than treating package roots as immortal chunk vocabulary.
- `package.json`/lock move together to Glass7 and immutable Keyframes6 plus the one Value4 transitive runtime. The clean primary at `b538506` owns `git@github.com:mkbabb/slides.git`; the older `slides-K` clone is not a second execution target.

### words/frontend — BUILD

- All 88 tracked glass source/config consumers move to surviving Glass7 semantic entries. In particular `WordListView.vue` and `WordlistDashboard.vue` retire `@mkbabb/glass-ui/confirm-dialog` for P106 `Dialog` confirmation semantics; root imports such as `cn`, `Skeleton` and `Tabs` move to their exact entries; HoverCard remains only where its preview semantics survive, while Tooltip/Popover/Dialog sites retain their distinct interaction contracts.
- `src/components/custom/icons/FancyF.vue` moves `loadAnimationEngine` and `CSSKeyframesAnimation` off the keyframes root to the exact immutable Keyframes6 engine/type entries without making the animation eager. `vite.config.ts` deletes root/retired prebundle strings and lists only actually imported live entries.
- W17 starts from tracked HEAD in an isolated worktree. The extensively dirty primary and its already-modified root `package-lock.json` remain user-owned. W33 three-way applies the exact frontend manifest/import/lock delta onto that state; an unreconcilable overlap blocks rather than replacing the lock or adjacent work.

### Core siblings — BUILD

**HISTORICAL / IMPLEMENTED (V-A126):** W2 captured the AST census from tracked Keyframes HEAD/tag/npm 5.3.5 at `a59d3a22da080a8ed224e8d675112bb3bb0135b0`, separately recorded the protected prospective-6 files, and expanded every resulting keyframes module declaration and current Glass declaration census by exact callsite. The 169-keyframes/14-Glass baseline remains evidence to compare, not a current execution target. The manifest includes every named non-declaration consumer below; regex line counts are not allowed to masquerade as this manifest. This procedure is closed and cannot reopen Value4/Keyframes6 or mint a `K_NEXT` rehearsal:

- `glass-ui/vite.library.ts:33`: replace the bare-root external with exactly `@mkbabb/value.js/color`, `@mkbabb/value.js/css`, and `@mkbabb/value.js/easing`, the only capability entries Glass imports after migration. `/css` owns `parseCssColor`; `/color` owns the final-object operations; `/easing` owns numeric curves. The root is removed; otherwise Rollup would bundle the new subpaths.
- `keyframes.js/vite.config.ts:180-194`: replace `/^@mkbabb\/value\.js(\/|$)/` with the closed literal external set `@mkbabb/value.js/color`, `@mkbabb/value.js/value`, `@mkbabb/value.js/css`, `@mkbabb/value.js/easing`, `@mkbabb/value.js/math`, and `@mkbabb/value.js/transform`. A clean packed fixture resolves runtime and types for all six through value 4's `exports` map before building Keyframes; the emitted graph may name only members of this set. The removed root, prefix matching, private dist paths, `/units`, `/parsing`, `/quantize`, and `/browser` are absent.
- `value.js/src/units/{layout-cache,dom-metrics}.ts` plus the DOM-relative arms of `convertToPixels` are move-only inputs, not a seventh package entry. W8 deletes their exports, resize listener and value-side public tests after W2 freezes the exact behavior. W17 installs the sole mechanism at `keyframes.js/src/animation/resolve/browser.ts`; `demo/components/playback/AnimationVisualizer.vue` and `test/demo/instrument/resize-tracks.test.ts` import its local `bumpLayoutEpoch/getLayoutEpoch`, while `test/engine/computed-resolution.test.ts` becomes the local pixel-resolution test. The resolver consumes `/value` `isLayoutTrackingUnit`; no source, declaration, manifest or emitted chunk names `@mkbabb/value.js/browser`.
- `keyframes.js/bench/computed-real-dom.bench.ts:68` and `bench/playwright.bench.ts:112`: retire bare-root package resolution. Derive the packed bench import map only from the exact live capability exports it loads; no root export or private dist-layout fallback survives.
- `keyframes.js/bench/computed-scene.html:52-54` and `bench/loaf-scene.html:49-51`: replace both tracked bare-root/prefix import maps with explicit keys for only the capability subpaths each page imports. Their TS launchers (`computed-real-dom.bench.ts:96,104,183`; `playwright.bench.ts:149,185,272`) must exercise those exact maps from the packed artifact; neither `@mkbabb/value.js` nor a catch-all `@mkbabb/value.js/` key survives.
- `keyframes.js/scripts/observe/demo/subject-animates.mjs:78,112`: replace the root dist tree/bare import-map key with the exact packed capability entries actually imported. `scripts/gates/surface/deps-current.mjs:47` advances the value floor to 4.0; `scripts/gates/surface/published-surface.mjs:619` requires the exact capability-edge set and explicitly rejects a root edge. The valid `/math` literals in `boundary.mjs:161` and `consume-bundle.mjs:56` remain exact capability consumers and receive ordinary behavior validation rather than deletion.
- Keyframes commit `70b32501` already deleted `proof-css-parity.mjs`, `proof-scroll-roundtrip.mjs`, `proof-workaround-deletion.mjs` and `proof-diagnostics-channel.mjs`; V credits that landed subtraction and does not re-book it. W17 migrates and revalidates the surviving ordinary behavior at `test/compile/default-easing-css-twin.test.ts`, `test/compile/diagnostics-channel.test.ts` and `test/scroll/scroll-scene.test.ts` against the 4.0 entries.
- Keyframes' selector migration imports `/css` `parseKeyframeSelector` and `KeyframeSelector`; it deletes its duplicate selector regex/token production but keeps timeline phase resolution. Ordinary vectors cover `from`→typed percent `0`, `to`→typed percent `1`, authored `0%`/`100%`, canonical `0%`/`100%` serialization and typed reparse, bare `entry`, explicit `entry 0%`, `entry 50%`, `exit 100%`, the existing mixed-case policy, malformed, negative and above-100 behavior, with absent named offset observably distinct from zero.
- Keyframes' stylesheet/custom-function migration consumed the same `/css` grammar for numeric `calc(1px + 2px)`, conditional `if(supports(...))`, `sibling-index()`, and arithmetic inside `@function`. The focused immutable-AST/canonical-round-trip and malformed vectors were green before the historical W7 bootstrap; W17 deleted every Keyframes-local grammar/normalizer without fallback, alias or dual path; `/css` remains exactly 19 runtime/33 type exports.
- Keyframes' `demo/scenes/cube/matrix-editor/useTransformState.ts` retains the canonical `/easing` `easeInBounce` direct call. `demo/utils/reference-data/animationDescriptions.ts` receives only its rendered description copy and stops importing `timingFunctionDescriptions`; no numeric easing or shared prose registry is duplicated.
- `keyframes.js/docs/tranches/I/audit/investigate/probes/rc-parse-crash-seam.mjs` is the one remaining historical executable value-root import. **RETIRE W17**: delete the stale reproducer after the ordinary parser-result tests cover the live boundary; historical prose may remain as archaeology, but an executable dead-root probe may not survive the cut.
- `glass-ui/scripts/profile-bundle.mjs:330-340,353-358` is a live ordinary bundle profiler, invoked by `package.json`'s `profile:bundle` script and documented in `README.md`; **BUILD W17 is unconditional**. Wherever root-reach analysis classifies value.js as a heavy semantic leaf, recognize the closed set `@mkbabb/value.js/color`, `@mkbabb/value.js/css`, and `@mkbabb/value.js/easing`, report the exact reached edge, and reject the removed root and old/private entries. Its ordinary bite fixture imports each exact edge into the root-reachable graph and makes the profiler report all three. No retirement branch or vacuous root-only matcher remains.

### HISTORICAL / IMPLEMENTED (V-A126) — Keyframes U covenant on the immutable Value4→Keyframes6 rail

These rows are discharged dispositions, not live work, gates or another meta-tranche. “SHIPPED / IMPLEMENTED” records the immutable Value4 producer plus the Keyframes6 consumer deletion; no rehearsal or semver selection remains open.

| Covenant | Terminal disposition | Exact W17 consumer motion |
|---|---|---|
| `getTimingFunction` | **SHIPPED / IMPLEMENTED** `/css` `parseTimingFunction(): ParseResult<CssTimingFunction>`; `/easing` remains numeric-only | `compile/easing/easing-registry.ts`, public/engine/load-engine mirrors and docs are deleted; callables/catalog names resolve explicitly, CSS text parses once, and malformed stylesheet timing reports the issue instead of inheriting/defaulting |
| `plain-vars.ts` | **SHIPPED / IMPLEMENTED** immutable final value → keyframes-owned `InterpSlot`/sink | `plain-vars.ts`, `PlainProjection`, `_plainProj` and both hot-path adapters are deleted; custom and grouped transforms compare exact authored output |
| layout-tracking units | **SHIPPED / IMPLEMENTED** one `/value` `isLayoutTrackingUnit` classifier | `WAAPI_INELIGIBLE_UNITS` is deleted; ordinary WAAPI eligibility cases cover every old literal, negative controls, resize behavior and the existing path-relative `%` exception |
| KF-7 descriptor collision | **SHIPPED / IMPLEMENTED** `CSSPropertyDescriptor`, no alias | the four live kf modules are re-pointed; packed declarations contain neither bare `PropertyDescriptor` nor `PropertyDescriptor_2` |
| D-GAP-1 quart/quint | **SHIPPED / IMPLEMENTED** the exact six rows below | kf's parallel `NAMED_EASING_BEZIER` is deleted; picker/catalog read Value authority and preserve the six names/control points |
| D-GAP-5 shallow flatten | **DECLINE—SUPERSEDED W17**; no `/value`/`/css` leaf predicate | `useTimelineBuild.ts` reads the kf compiler's authored declaration snapshot; `timelineEngine.ts` reads `/css` collected declarations already keyed by CSS property; delete `demo/.../flattenVars.ts` and both imports |
| D-GAP-6 path data | **DECLINE upstream W7/W8/W17** as a one-demo presentation convention | delete value's unused `cubicBezierToSVG` implementation/export/tests; retain exactly one kf-local `sampleTimingPath(fn,{samples,flipY})`, pass `flipY:true` explicitly at its presentation sites, and cover point order in an ordinary test |
| diagnostics | **SHIPPED / IMPLEMENTED** | useful diagnostics-channel assertions remain in ordinary tests and the redundant proof command is deleted; no new framework |

The D-GAP-1 rows are exact:

| Name | Cubic control points |
|---|---|
| `ease-in-quart` | `[0.895, 0.03, 0.685, 0.22]` |
| `ease-out-quart` | `[0.165, 0.84, 0.44, 1]` |
| `ease-in-out-quart` | `[0.77, 0, 0.175, 1]` |
| `ease-in-quint` | `[0.755, 0.05, 0.855, 0.06]` |
| `ease-out-quint` | `[0.23, 1, 0.32, 1]` |
| `ease-in-out-quint` | `[0.86, 0, 0.07, 1]` |

The immutable producer coordinates are Value `4.0.0` and Keyframes `6.0.0`; the held W7/W17 tarballs remain transport evidence and are not current release inputs. The local scoped-package filename and registry tarball basename are deliberately distinct. W33 creates one designated fresh clean Glass tarball; that exact file is the sole release candidate and exact `npm publish <tgz>` input, and registry bytes plus decoded `dist.integrity` SHA-512 must equal it. W33 may create exactly one isolated noncandidate verification pack from the same clean tree solely to compare tar-member names and content hashes; it is deleted unpublished, never installed as the candidate and supplies no artifact coordinate or hash. W33 records only the Glass candidate coordinate/tag, observed `gitHead`, filename/byte length/SHA-512, registry URL/`dist.integrity`, `exportsSha256` as the lowercase SHA-256 of the UTF-8 RFC 8785 canonical bytes of the in-tarball `package.json.exports` value, and separate exact-byte `declarationSha256` values keyed by every literal `types` target. Glass `7.0.0` remains selected by its packed public-surface comparison; existing Glass `v6.0.0` remains immutably attached to peeled commit `e5b3a2095b6c3e330b5d82ca3330f1eac4e3c895` and is never moved or reused.

**V-A168 current stage:** the preceding procedure preserves the ordered-release law, but Value4 and Keyframes6 have already completed it and Glass has already proven its registry-only pair install. The only live candidate is a fresh evidence-selected Glass7 strict/native pack from that graph; Q003 must be product-green with zero slab, occlusion corruption and lost target after the ~95→5 and reverted 3→0 filter falsifications, alongside strict declaration/HeaderRibbon closure. A declined causal hypothesis is not release evidence. After W33a publication, W17b refreshes the value consumer graph; downstream canon/deploy locks close in W33b. No producer rebuild, Keyframes re-close, `K_NEXT` rehearsal or held rehearsal is current work.

V-A84–V-A87 settle earlier Value/Keyframes lineage; the held Keyframes archives are historical transport evidence and immutable 6.0.0 is now the only producer input. V-A88/V-A90–V-A92/V-A94/V-A95 hold Glass's 966,350-byte archive and native interaction credit behind that input; V-A124/V-A125/V-A126 supersede the wait-only interpretation with active live-backdrop-adjacency isolation from the honest 5/0 baseline. No Glass rehearsal is a release coordinate.

W17 requires zero root/`/units`/`/parsing` imports, bare `PropertyDescriptor`, `getTimingFunction`, `plain-vars`, duplicate layout-unit/preset tables or generic flatten helper in source, demo, tests, benches, scripts, build configuration or current public documentation; test-only, configuration-only or documentation-only survival is not permitted.

V-A84 historically held the 36,378-byte pack for comment/timeline failures; those repairs flowed into later bytes without a Keyframes parser, stripper, special route or fallback. V-A85's 38,126-byte manifest entry is now the sole eligible transport.

**HISTORICAL (V-A126):** V-A85–V-A87 stand as historical implementation/semver evidence; V-A93 holds the 184,710-byte archive and supplied authoritative 184,254-byte Keyframes bytes to the Glass declaration/HeaderRibbon/Q003 native rehearsal. Value rehearsal recreation is not current; the routed constellation remains pending after fresh Glass7.

## 4. Ordered cut and proof

1. W2 captured `git ls-files` plus tracked package/lock/config/script facts for every row and rejected hidden worktrees/generated output; each callsite received a target entry and named boundary outcome.
2. W7 replaced the existence-conditioned prepare branch with unconditional removal of prior output plus the authoritative seven-entry Value4 build. Its focused vectors and ordinary aggregate were green, and the exact-seven transport manifest records historical `BOOTSTRAP ONLY` evidence; no replacement bootstrap is current.
3. Keyframes semver selection and publication are complete at immutable 6.0.0. Glass has proven its registry-only Value4 + Keyframes6 install; its current direct source observation is 176 files/1,132 tests with typecheck green, while build/pack/verifier/native Q003 evidence remains uncredited. It must close declaration/HeaderRibbon defects plus Q003 live-backdrop adjacency in fresh Glass7 bytes, then publish. The stale value.js graph, routed app, product/native rows and BUILD consumers remain RED; no alias, exposed internal members, shim, file link, hoisting, `skipLibCheck`, DOM/VNode inference, offset/cover/reset, consumer sizing/timing fallback or rehearsal hash becomes release truth.
4. **Current Glass-only cut (V-A128):** W33 packs and publishes only the fresh evidence-selected Glass7 candidate from the already immutable Value4→Keyframes6 registry graph, proves registry bytes and decoded integrity match, then updates downstream manifests/locks and reruns their smokes. The historical Value→Keyframes→Glass procedure above is not a request to rebuild the two completed producers. Keyframes's release-phase graph contains registry Value4 and no unpublished Glass prerequisite; after Glass ships, its demo/dev Glass edge and mandatory lock may refresh as ordinary downstream consumer work. W33 records **SHIPPED** only for Glass7. Private packages need not publish, but must compile from registry artifacts.
5. Every execution-entry primary is re-censused. Current Atlas `SourceDataBrowser.vue`, bbnf-lang Vite/test-results, Fourier version files, Muster evidence/docs, sci-report untracked research, Words primary/root-lock work and Keyframes' five modified prospective-6.0 files are preserved; no clean consumer, landed relocation or prospective metadata is falsely described as artifact truth. Any overlapping state that cannot be three-way reconciled blocks only that repository's cut. Slides remains the sole primary for its remote; `slides-K` is untouched.
6. Final census over tracked active repositories' source, demos, tests, benches, scripts, build configuration and current public documentation contains zero bare-root, `/units`, `/parsing`, removed class, old parser, general library-string serializer, raw/direct color primitive, `parseSteps`, `timingFunctions`, silent identity, old conversion name, file link, or hidden worktree resolution. README/API pages, current guides, rendered examples and executable/copyable snippets are in scope. Clearly labeled non-executable historical archaeology is excluded; an old import in a runnable probe, shipped example or current instruction is not archaeology merely because its containing path is old.

V-A84 closes the pack-order ambiguity in step 2: the W7 focused suite must include leading/inter-rule stylesheet comments and zero-argument `animation-timeline: view()` through declarations, each with direct success, canonical round-trip and named failure vectors, before any replacement pack is emitted.

**HISTORICAL (V-A126):** V-A85's semver diff remains complete; V-A86/V-A87 invalidate earlier candidates, and V-A93 invalidates the 184,710-byte archive while authorizing only the 184,254-byte replacement. V-A88/V-A90–V-A92 invalidate the first Glass rehearsal for declaration isolation and inferred HeaderRibbon behavior; V-A94/V-A95 add native Q003 product defects without adding a gate. The old W17 instruction to carry corrected Keyframes bytes or recreate a Value rehearsal is retired; current W17 carries only the Glass strict/native/release close and then the value consumer refresh.
