# THE EDICT MATRIX — V-next canon, extracted 2026-07-19 (armA)

CANON SOURCES (all read in full):
- **L1 (charter letter):** `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/keyframes-inbox-2026-07-18-vnext-ingestion-prompt.md`
- **P (context packets):** `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md`
- **C (conversation addenda docket):** `/Users/mkbabb/Programming/value.js/docs/tranches/V/vnext/OWNER-AMENDMENTS.md` — self-described as "owner statements made after the two immutable seed letters"; a later, narrower statement supersedes on the named axis. **Addenda WIN where they tighten the letters.**

PROVENANCE NOTES (mechanical, not editorial):
1. The task's canonical file paths arrived as the literal string "undefined" (template failure upstream). The three docs above were located by evidence: commit `c654824e` landed the two seed letters; OWNER-AMENDMENTS.md is the only docket recording post-seed owner statements and yields exactly 23 rows (OA-01..OA-17 + authority law + 4-question deletion judgment + blocking rule + 2 parse-that/BBNF-boundary laws). If a different frozen-snapshot copy was intended, its path never resolved; content was read from the live tree read-only.
2. No path containing `r1-opus-refuted` was read.
3. Where C-rows and L1 conflict (model naming Fable→Sol, api/ extraction, subpath-key freeze, parse-that 1.0.1 branch), the C-row governs per the docket's own authority law.

Row grammar: **ID | edict digest | COMPLIANCE TEST** (what a compliant formed tranche must observably contain).

---

## PART 1 — THE CHARTER LETTER (E-L1-S0 .. E-L1-S9)

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-L1-S0.1 | Formation-only: NO source edits land from this prompt. Deliverable = the next tranche fully formed: plan folder, wave specs with born-RED gates wherever the defect is live, π/DELTA obligations for every visual claim, terminal disposition for every chronic/deferred item and prompt-recap row. | The formation output is a plan folder; `git diff` over `src/`, `api/`, `demo/` etc. from the formation session is empty; every wave spec touching a live defect names a born-RED gate; every visual claim row carries a π/DELTA obligation; a disposition ledger covers every chronic, deferred, and prompt-recap row with a terminal state. |
| E-L1-S0.2 | Model routing (supersedes every earlier routing paragraph; owner ruling 2026-07-18): ALL agents are Fable, declared explicitly on every spawn; ZERO Opus. Opus only for later per-tranche owner-ratified mechanical sweeps. Verify the tier ACTUALLY SERVED, not the declaration. Batches of 5–6 concurrent; thrice panels exactly 3 seats. **[TIGHTENED by E-C1/OA-01: Fable→Sol, Opus→Luna.]** | Every spawn record declares the model; every panel return records served-model evidence (not just the declaration); no seat ran on the forbidden tier; panel compositions are exactly 3; batch sizes 5–6. Under the addendum: explicitly declared Sol for formation/design/critique/adjudication; Luna only when separately authorized. |
| E-L1-S0.3 | Tree pins (G0′ law): pin tree+branch+HEAD before any cross-repo claim. Pins: value.js `tranche-u@db77dbd8` (4.0.0) · keyframes-v-exec `master@0dac636b` (6.0.0 CANONICAL kf) · parse-that `ef10d5b` · bbnf-lang `b3cf48e3b` · atlas ACTIVE `/Users/mkbabb/Programming/.p-totality/atlas` `p/totality@fe9abcf` (7.0.0; standalone `atlas@master` checkout is a STALE trap; npm latest ALREADY 7.0.0) · glass-ui `master` 7.0.0 (re-pin at read). | Every cross-repo claim in the formation cites the exact pinned SHA; no claim cites the stale standalone atlas checkout; any glass-ui claim carries a fresh re-pin recorded at read time. |
| E-L1-S0.4 | The 32-agent budget: steerable, registry-driven, adversarial. No lens permanently staffed; withhold the favored success narrative from most auditors; dedupe finding-families by mechanism, redirect excess to underexplored lenses; decompose any "redo the tranche"-strength finding into wave-shaped rows; check every "done" claim against the named close-class lies (green-over-broken, vacuous-green gates, declared captures missing on disk, masked fallbacks, alias smuggling, re-booked chronics, per-mechanism green over gestalt broken); require concrete deliverables (file:line, failing probe, reproduction, named defect row); reject status reports/vague optimism/"routine" claims. Registry stable = two consecutive passes surfacing nothing new. | A formation registry exists; agent dispatch records show lens rotation and narrative withholding; findings are deduped by mechanism; no surviving finding is "redo the tranche"-shaped; every accepted "done" is backed by file:line/probe/repro evidence; registry closure is evidenced by two consecutive no-new-findings passes. |
| E-L1-S1.1 | Mission: develop the next tranche across value.js/keyframes.js/parse-that + consumers (glass-ui, atlas, demos); each library uplifted and analyzed at library level; frontend work focuses on value.js **[SUPERSEDED on the named axis by E-C2/OA-02: full-route programs on BOTH demos, mobile+desktop]**; library set and demo set are separate workflow series; prior tranches are the mined evidence set (P5). | The wave registry separates a library series from a demo series; library-level analysis waves exist per repo; under OA-02 the demo bands cover every route of both demos on mobile and desktop. |
| E-L1-S1.2 | This value.js-owned tranche DIRECTS all keyframes.js library items as specs + bounded dispatches into kf's coordination inbox; the next kf-owned tranche implements/adapts (P4.5 protocol). Major architecture changes allowed so long as core features are not lost — "not lost" is MACHINE-CHECKED (§7 law 1). | Every kf library item is a spec/dispatch addressed to kf's coordination inbox (not a direct edit); the capability-diff gate (E-L1-S7.2) is specified as the machine check for "not lost". |
| E-L1-S2.1 | kf's parser exit was RIGHT (4 stages 2024-07→2026-06; parser-less since 2026-04-17; no unjust kf drop). kf-side work = boundary CENSUS LOCK: zero grammar productions both sides, ONE live row `src/animation/easing.ts:30,:38-39` (timing-function name table) — adjudicate consume-vs-ratify EXPLICITLY (P1.4). | The formed tranche contains a kf boundary census-lock wave/row whose gate asserts zero grammar productions on both sides, plus an explicit consume-vs-ratify adjudication row for easing.ts:30/:38-39. |
| E-L1-S2.2 | value's extant parser is the condemned party: unmeasured regex/char-split rewrite born at the v4 cut, which deleted the parse-that combinator incumbent (v1.0.0→v3.1.0) + ALL 11 benches + the perf gate. "Proper parse-that adoption" = restoration of the deposed incumbent; the head-to-head has never been run. | The tranche frames parser work as RESTORATION (not net-new adoption or contest); no wave re-contests the seat. |
| E-L1-S2.3 | OWNER DECREE (2026-07-18, DECIDED, not bench-contested): parse-that READOPTED outright, as published — `@mkbabb/parse-that` from the registry at published latest 1.0.0. Regex rewrite retired UNCONDITIONALLY. All parse-that-INTERNAL evolution (tape, substrates, Pratt revival, combinator additions) OUT OF SCOPE — routes as PT-E-channel ask letters. S.H3 Pratt disposition stays RETIRED. **[TIGHTENED by E-C10/OA-10 and E-C17/OA-17: exact published 1.0.0 registry artifact, no mutation/candidate/republication.]** | A wave consumes `@mkbabb/parse-that@1.0.0` from the registry (typed no-link receipt per OA-17); the regex parser's retirement is a wave outcome with no conditional survival; zero waves perform parse-that-internal work; discovered needs appear only as PT-E ask letters; no Pratt row exists. |
| E-L1-S2.4 | Wave 1 — R-PARSER restoration: resurrect the deposed tree (recipe `git show 164343c1^:src/parsing/`) modernized to the v4 public `/css` surface (parseStylesheet/collect*/ParseIssue signatures PRESERVED — consumers never move); re-add parse-that per value's historical pin convention; co-land wave prices the dependency edge; `parseFunctionalColor` ladder, deepFreeze-per-parse, per-char `/\s/.test` costs die with the regex parser. | An R-PARSER wave spec exists citing the `164343c1^` resurrection recipe; its gate asserts the frozen `/css` signatures byte/type-compatible; the dependency-edge pricing is assigned to a named co-land wave; the three named cost mechanisms are listed as dying with the regex parser. |
| E-L1-S2.5 | Wave 2 — bench restore as REGRESSION WITNESS: resurrect the bench corpus (`git show 164343c1^:bench/css-parse-perf.mjs`) + the portable ratio gate (MB/s + ns/call + co-scaling ratio); baseline the restored parser (born-RED until it meets the gate); measure the retiring regex parser ONCE for the record. The bench tunes; it does not re-contest the seat. | A bench-restore wave exists citing the recipe; its gate is born-RED with the recorded ratio recipe; a one-time regex-parser measurement row exists; no wave uses bench results to re-open the readoption decision. |
| E-L1-S2.6 | Wave 3 — spec-completeness census: born-RED coverage census of grammar/color/timeline surfaces vs the July-2026 stabilized set; gaps become waves ON the restored parse-that grammar, including named restores: `color-mix()`, relative-color `from`, `contrast-color()`, the 11-day HDR parse drop (owner decides vs the CSS-native-only law), `spring()` easing grammar (OPEN ownership decision row: solver kf-owned per K F6.6; grammar value-vs-kf open, leaning value). | A born-RED census wave exists against the July-2026 set; each named restore appears as a wave or decision row; HDR carries an owner-decision row; spring() grammar carries an open ownership-decision row citing K F6.6. |
| E-L1-S3.1 | Color program: best SOTA with near-perfect zero-alloc facilities for all spaces — a RESTORE program, not net-new (P2). Loss is SINGLE-SIDED (value v4 cut); kf NEVER owned gamut code; kf-side = consume-only + deletion of scar tissue (hand-rolled oklab ΔE; stale `sampleColorRamp`/`deltaEOK` docstrings). | The color band is framed as restoration; no wave attributes gamut loss to kf; a kf consume-only spec exists including deletion of the two named scars. |
| E-L1-S3.2 | The extant `mapColorToGamut` (32-iteration chroma bisection, NO ΔE stop criterion) is not CSS Color 4 §13-conformant. Decided path in priority order: R-DELTAE (prereq) → R-GAMUT (analytical Ottosson cusp+Halley + ΔE-OK JND clip; raytrace as TEST-SIDE exact oracle; zero-alloc kernel) → R-INTO (extends SCI-1, RIDES the W56 4.1.x vehicle, never forks it) → R-RAMP (kf backward-emit re-adopts, deletes local duplication) → remaining P6–P14 rows. WPT/§13 conformance vectors are NEW gate infrastructure. SCI-1 is DECIDED SHIP-4.1.x (D54) — inherit, extend, never re-adjudicate. | Restore waves exist in the stated priority order with the stated dependencies; R-INTO is attached to W56 (no fork); WPT/§13 vector gate infrastructure is a named new gate; no wave re-opens SCI-1/D54. |
| E-L1-S4.1 | Library structure (both repos): isomorphism and directory perfection INCLUDING coherence/isomorphism BETWEEN value.js and keyframes.js in an abstract facility; same treatment for all backend files (api/ surface and any successor), abstracted for those languages. **[api/ handling superseded by E-C5/OA-05: internal transpose, no extraction.]** | Structure waves define a cross-repo abstract isomorphism target (not per-repo tidiness only); backend structure waves exist under the same discipline. |
| E-L1-S4.2 | kf `src/animation` flatten + the `internal/` question are coordinated config-and-graph moves, born-RED at every anchor: 13 config/gate anchors + ≈340 import lines, census pre-run (P4.3); anchors MUST be re-derived at execution (N-ADJ-3 — ratified line-cites already drifted). | The flatten wave's gate enumerates the 13 anchors born-RED; a mandatory anchor re-derivation step is in the wave spec (not the cached cites). |
| E-L1-S4.3 | Structure governance: kf structure waves AMEND the ratified LT blueprint + EXTEND proof:structure, naming superseded rulings (LT-10, LT-16) explicitly — never a parallel authority. `internal/` is OWNER-DECISION (restructure, not prune): the owner's dislike is the mandate; LT-10's 40+-importer census is the cost sheet. | Each kf structure wave names the LT ruling it supersedes; no wave creates a parallel structure authority; an `internal/` owner-decision row exists citing the LT-10 census as its cost sheet. |
| E-L1-S4.4 | subpaths/ dissolves ("code smell supreme, NO SHIMS") with precision: the 7 files are 163-line export-map homes, not runtime shims; dissolution keeps the 7 keys frozen, uses explicit `/index` specifiers (D50 api-extractor boundary), drops `./quantize`, creates a real `transform/index.ts`, re-verifies the packed surface. **[Key-freeze loosened by E-C14/OA-14: removal of unworthy keys allowed under the four-part deletion judgment.]** | A subpaths-dissolution wave exists with a packed-surface re-verification gate; `/index` specifiers per D50; `./quantize` dropped; `transform/index.ts` created; any key removal beyond that satisfies the OA-14 deletion-judgment test (E-C19..E-C20). |
| E-L1-S4.5 | value hygiene: `transform/decompose.ts` PRUNE (609 LOC, zero consumers); `quantize` DEMOTE to demo; api/ EXTRACT from repo **[REVERSED by E-C5/OA-05 — internal transpose instead]**; 39 root PNGs deleted; demo-component tests displaced out of library test/ root; working-tree `dependencies` block (glass+kf) STRIPPED/RELOCATED to devDependencies + a pre-publish manifest gate. | Waves exist for: decompose prune, quantize demote, root-PNG deletion, demo-test displacement, deps strip + a pre-publish manifest gate; the api/ wave is an internal transpose (A00–A20 per OA-05), NOT an extraction. |
| E-L1-S4.6 | Goldilocks granularity everywhere: no god-modules, no sand; grouped files strip the module name (`easing-option`→`option`); long dirs → encapsulated modules; glass-ui is the REFERENCE MODEL for flattening/component idioms. | Structure wave acceptance criteria encode the granularity rules incl. module-name stripping; glass-ui is cited as the reference model in the structure specs. |
| E-L1-S4.7 | Tests-isomorphism is born-RED on BOTH repos: value must BUILD its structure gate from scratch + re-mirror its test tree; kf needs a NEW isomorphism rule + support-dir allowlist. | Two born-RED isomorphism-gate waves exist (one per repo) with the stated scopes. |
| E-L1-S4.8 | Demo/frontend (value-focused; broadened by OA-02): Aristotelian proportionality audit (cards, components, affordances, hierarchy, margins, paddings, dividing lines, small UI) alongside grand glass-ui suffusion; superfluous/duplicative/distracting elements marked for removal, under-afforded marked converse; pre-glass-7 carried-forward UI corpus audited NOW; recursive colocation (sub-components, composables, skeletons, constants, styles colocated; only truly global composables in `composables/`). Root glass-ui defects batched to the working glass agent — released only once large swaths are isolated, fully precepts-compliant wave addenda written, exact defects targeted; never interrupt piecemeal. W53 perceived-space plate rebuild is the named connection for any gamut-viz restore. | Demo waves include the proportionality audit with removal/converse markings; a pre-glass-7 corpus audit wave exists; colocation rules are in the structure spec; glass-ui defect dispatch is a batched release with the three stated preconditions; any gamut-viz restore row cites W53. |
| E-L1-S5.1 | Zone adjudication: dispositions are INPUT, not open questions — adopt the r2 zone tables (P3); verdicts rest on the four-tree consumer census (kf demo, value demo, glass-ui, atlas) + internal wiring graph, at pinned HEADs. | The formed tranche imports the P3 zone verdicts verbatim as registry rows; no wave re-derives a zone verdict; any new prune/demote claim carries a four-tree census at pinned HEADs. |
| E-L1-S5.2 | KEEP-EARNED includes zones Opus-era analysis condemned: waapi (`useWAAPI: true` default), orchestration/timeline (3 runtime importers + atlas ManualTimeline), orchestration/drag (glass `Draggable` runtime), svg/morph-svg (atlas via `./engine`), physics/spring/css (`springTimingFunction` demo+atlas+glass), the LIGHT/HEAVY load-engine split. | None of these zones appears as a prune or owner-decision row; each is registered KEEP-EARNED with its consumer evidence. |
| E-L1-S5.3 | Unilateral PRUNE-CANDIDATES ≈0.8k LOC ONLY: split-text (486), motion-path (~180), oscillator (~150 — keep-record's two named consumers FALSE on disk; record-correction rider MANDATORY). | Exactly these three zones are prune-candidate rows; the oscillator row carries a record-correction rider; no additional unilateral prunes exist. |
| E-L1-S5.4 | OWNER-DECISION ≈2.4k LOC (flip, draw-svg, physics/morph, scroll, ingest), each with an intact deliberate-KEEP record. The record-state trichotomy governs: intact record → owner decides; refuted record → shield pierced + correction duty; fence-register ruling (K F6 annex: K-1 thrice-affirmed JS sampler, no-polyfill law, spring-solver LEAD) → cite-to-overturn with new Baseline evidence. | The five zones are owner-decision docket rows; each cites its record (EP-3 PATH-B / K.W8 / K.W9); any prune proposal in scroll/timeline/physics cites and overturns the named F6 ruling with new Baseline evidence. |
| E-L1-S5.5 | Shrink notes, NOT prunes: emit/backward keeps consumed round-trip core + CC-3 refusal, trims `compileToViewTransition` (demo=0; NO phantom rows — compileToString/formatKeyframes do not exist); play-lifecycle recombines; presets catalog breadth. | Shrink-note rows exist for the three items; no row targets the two phantom symbols; emit/backward keeps the CC-3 refusal. |
| E-L1-S5.6 | Zone-orphaned tests BIND to zone verdicts (scroll 800 / svg 807 / ingest 1027 / waapi 737 LOC); no orphaned green. External-consumer census is a STANDING discipline: no vacuity claim without all four trees + the wiring graph, at pinned HEADs. | Each zone-verdict row names its bound test block and its fate follows the verdict; every vacuity claim in the formation cites the four-tree census. |
| E-L1-S5.7 | Gates/tests: the "most tests are overfit nonsense" presumption is REFUTED on tree (~98% behavior-bearing; residue ~500 LOC). kf: LAND staged W9 quartet + prunes FIRST; wire the two enforcement-free structural guards (lint + proof:structure — two one-line CI steps); demote masked lighthouse; fold boundary-cohesion's clamp invariant into proof:structure before it dies; MR2/MR4 before restructure churn. value: library gates stay producer+api; e2e adjudicated per-oracle (~5.9k+~856 ABROGATE · ~1.7k FOLD · ~1.7k KEEP-EARNED incl. safari trio + driven a11y · ~1.2k journey rides ratified W55); build the isomorphism gate; pre-publish manifest gate; resurrect bench/ with R-PARSER. | The tranche contains no blanket test-abrogation wave; kf apparatus waves exist in the stated sequence (W9-land first; MR2/MR4 before churn); the two CI wires, lighthouse demotion, and boundary-cohesion fold are explicit rows; the value e2e adjudication numbers are carried as per-oracle registry rows; the journey subset is attached to W55. |
| E-L1-S6.1 | Breaking changes allowed and PRICED (P4.2): the wedge is HARD (present-incompatible optional peer HARD-FAILS npm, ERESOLVE, proven twice); kf pins value EXACT 4.0.0. ONE coordinated co-land boundary (value 5 / kf 7 / glass peer-bump / active-atlas ranges / NON-optional framework peers in the chase ledger); until it, restructures stay internal behind frozen surfaces. NO stale-atlas catch-up wave (npm latest already 7.0.0). The value deps block is STRIPPED, not lockstep-bumped. | Exactly one co-land boundary wave exists naming all five legs + chase-site ledgers in advance; every pre-boundary restructure is surface-frozen; no atlas catch-up wave exists; the deps remedy is strip/relocate (no lockstep bump anywhere). |
| E-L1-S6.2 | Cross-repo: value DIRECTS kf library items as specs into kf's inbox (`docs/tranches/V/coordination/`); the kf successor implements; kf's demo/UI corpus + FOLD-FORWARD rows stay kf-owned. Direct cross-repo edits require an explicit OWNER GRANT. Glass-ui defect batching per §4; parse-that's standing PT-E ask letter is in the ingestion set (P5). | No wave edits kf/glass/parse-that trees without a recorded owner grant; kf-owned corpora are absent from value's wave scopes; the PT-E letter appears in the ingestion/registry inventory. |
| E-L1-S7.1 | Standing owner edicts, in force verbatim: NO quick solutions/workarounds — idiomatic gestalt approaches; architectural transpositions for elegance/simplicity/performance desirable; NO legacy code, aliases, shims, dual paths, masking fallbacks; EXTREME parsimony + fastidious care — KISS-forward, fewer lines, judged against the greater picture; adhere to the wave spec EXACTLY; pithy/laconic/fastidious analysis; pruning granted IFF vacuity/superfluity PROVEN — consumer count NOT enough; chronics become DECIDED rows, re-booking FORBIDDEN; a chronic riding 2+ closes un-decided is a DISEASE ROW — deciding it is a wave of its own; recap ALL prompts — an unaddressed ask becomes a registry row WITH an owning wave; silent drops FORBIDDEN; partial progress tracked — every partial/banked/abandoned item gets a terminal disposition (folded into a named wave / banked with a named re-trigger / retired with rationale); counting a partial as done is the close-class lie, FORBIDDEN. | Wave specs contain no shim/alias/dual-path/fallback mechanisms; every prune row carries a proof beyond consumer count; every chronic is a DECIDED row (none re-booked); each 2+-close chronic has its own deciding wave; a prompt-recap exists where every ask row names an owning wave; every partial has one of the three terminal dispositions. |
| E-L1-S7.2 | Ratified law 1 — the silent-drop tombstone law: no capability leaves a published surface without a by-name RIGHTLY/UNJUSTLY/UNCLEAR tombstone in the cutting release's CHANGELOG, MACHINE-CHECKED by a capability-diff gate (export-census diff vs prior tag; RED on any undeclared removal). Kills the "no advocate ⇒ no tombstone" mechanism. Owning repos: BOTH — value.js FIRST. | A capability-diff-gate wave exists for value.js (and a kf spec dispatch); the gate's rule is export-census diff vs prior tag, red on undeclared removal; every planned removal row pre-names its tombstone class. |
| E-L1-S7.3 | Ratified law 2 — G0′ tree pinning on every cross-repo claim. | Same test as E-L1-S0.3, applied tranche-wide as standing law. |
| E-L1-S7.4 | Ratified law 3 — re-derive anchors at execution: ratified blueprints' line-cites drift within days (N-ADJ-3); anchor re-derivation is a mechanical gate step. | Every wave that consumes line-cites includes an anchor re-derivation gate step; no gate hard-codes a cached line number as its oracle. |
| E-L1-S7.5 | Ratified law 4 — phase labels: the vision edicts ("majority on direct code implementation… visual verification") bind the FORMED tranche's implementation; the formation block binds THIS run. LABEL EVERY EDICT. | Every edict row in the formation is labeled formation-phase vs implementation-phase. |
| E-L1-S8.1 | Method — the thrice loop, all-Fable **[Sol per E-C1]**: the DAG spans library + component structures AND the constellation (kf, parse-that, atlas, glass-ui — every node/edge/cycle; DEPCRUISE-DERIVED then adjudicated, never hand-drawn); dynamic cluster batches, each viewed thrice: 2 fresh skeptics assuming the structure is WRONG + 1 adjudicator who PROVES/disproves with its own evidence (never vote-counts); iterate to perfection/readability/DIRIGIBILITY; convergence = two consecutive clean passes; ≤3 iterations per cluster then OWNER ESCALATION; ALL design routes through Fable + DesignSync; maximal parallelization. **[TIGHTENED by E-C15/OA-15: per-implemented-wave hostile triads, content-addressed.]** | DAG artifacts are depcruise-derived (tool output on record); each cluster shows a 2-skeptic+1-adjudicator record with adjudicator-owned evidence; convergence/escalation bookkeeping shows the 2-pass / ≤3-iteration rule; design outputs show DesignSync routing. |
| E-L1-S9.1 | RETURN CONTRACT: return only when the next tranche is fully formed — plan folder; wave specs with acceptance gates born-RED wherever the defect is live; π+DELTA obligations for every visual claim; a disposition for every chronic/deferred/prompt-recap row; the OWNER-DECISION docket as a SINGLE decision sheet. EVERY packet row (P1–P5) lands as a wave, gate, registry row, or owner-decision row — ZERO silent drops, CHECKED against the packets' row inventory. An inventory of problems without the resolving tranche is an incomplete return. If a genuine blocker prevents full formation: return the strongest rigorously converged core + its exact remaining gap. | A single owner-decision sheet exists; a packet-row inventory crosswalk exists mapping every P1–P5 row to its landing (wave/gate/registry/decision row) with zero unmapped rows; every problem named in the return has a resolving wave or an explicit blocker statement with the exact gap. |

---

## PART 2 — THE CONTEXT PACKETS (E-P0 .. E-P6)

### P0 — Provenance

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P0.1 | The r1 corpus silently executed on Opus; owner ruled r1 presumptively incorrect; r2 = union-with-demarcation (fresh analysis BEFORE opening r1 counterparts; tags FABLE-NEW / UNION-CONFIRMED / OPUS-REFUTED / OPUS-UNVERIFIABLE). Standing law: a model declaration is not model execution — VERIFY the tier actually served. | Every agent/panel return in the formation records served-model evidence; no formation citation resolves to an r1/pre-r2 document. |
| E-P0.2 | The 18 OPUS-REFUTED rulings are DEAD (each named: optional-peer-warning, tape-inadmissible, lockstep-bump, atlas-catch-up, measured-byte-scanner framing; + the 13 panel-2 zone flips incl. the double-gamut-loss refutation, the ~500-LOC e2e survivor replacement, boundary-cohesion fold-then-die, phantom compileToString/formatKeyframes rows). Pre-r2 letters (any copy stamped PROVENANCE HOLD) are DEAD — cite only this packet set. Excluded OPUS-UNVERIFIABLE items (composite-SoA "OVERFIT", "47 recorded catches", stale-read attribution, CHALLENGE-2 letter existence) may not be cited as established. | No formation row rests on a refuted ruling (checkable per name); no citation to a PROVENANCE-HOLD-stamped letter; no OPUS-UNVERIFIABLE item asserted as fact. |
| E-P0.3 | The byte-canonical owner prompt + all FIVE addenda are mirrored at `keyframes-v-exec/.../vnext/OWNER-PROMPT-verbatim.md` + `OWNER-ADDENDUM-*`; nothing silently dropped. Addenda 1–4's challenges were EXECUTED (verdicts = P2/P3); addendum 5 (all-Fable, no Opus) is standing law. Where a presumption was tested and REFUTED (e2e totality, tests-overfit, double gamut loss), the refutation STANDS per the owner's own challenge instruction. | The formation's provenance chain reaches the verbatim mirrors; no wave re-litigates an executed-and-refuted presumption; the refutation-stands rule is honored in the e2e/tests/gamut bands. |

### P1 — Parser archaeology

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P1.1 | The two-repo answer: kf-side exit RIGHTLY complete (4 dated stages; parser-less since 2026-04-17; NO unjust kf drop); value-side is the condemned event — v4 cut `164343c1` (release `7334c793`, +4,117/−24,330) deleted the combinator parser + all 11 benches + the `proof-perf-target` CI gate and grew the successor UNMEASURED in the same commit. | The archaeology is carried as decided input; no wave re-investigates kf parser history; parser waves cite `164343c1` as the deletion event. |
| E-P1.2 | The extant parser per-file record (grammar.ts 483 / stylesheet.ts 899 / timeline.ts 124 / syntax.ts 101; `charCodeAt` across css/* = 0; no bench survives; baseline GREENFIELD — resurrect via `164343c1^`). The SpanParser V8 falsification (~10–14% slower) is REAL but parse-that-INTERNAL — never tested parse-that-vs-custom on the value grammar. | Parser-wave scope covers exactly the four parser files (not the non-parser css/ remainder or src-root files); bench baseline is treated as greenfield; no row cites the SpanParser falsification against readoption. |
| E-P1.3 | Tape is OUT OF SCOPE: parse-that ships zero tape; everything tape-related is parse-that/bbnf-lang tranche territory; occupies NO formation context. RETIRED S.H3 Pratt row stays retired. | Zero formation rows/waves mention tape work as in-scope; no Pratt revival row exists. |
| E-P1.4 | Boundary census: census-PASS everywhere except the ONE live easing.ts row (adjudicate consume-vs-ratify explicitly; plus a kf regex-census row for addendum 2). `scroll/grammar.ts` + `parseAnimationCSS.ts` verified thin pass-throughs. value /css ALREADY OWNS the keyframes-adjacent grammar (closed 8-code ParseIssue union; collectAnimationOptions stylesheet.ts:827; parseTimingFunction grammar.ts:436; kf seams compile/selector.ts:3, validate.ts:47). | The consume-vs-ratify adjudication row exists; a kf regex-census row exists (addendum-2 discharge); the split-parsing question is answered by the on-disk ownership record, not re-opened. |
| E-P1.5 | The ad-hoc-fix family (U-F29 `329932b8`, U-F30 `0c212e8d`, D3 passes `d82c63cd`/`184a9ec9`) was patched on the PRE-v4 tree and deleted with it; recurrence risk lives in the successor; the defect-family register must track FAMILIES, not instances. | The defect register contains a mixColors/parseCSSValue FAMILY row (not instance rows) bound to the restored parser's gates. |
| E-P1.6 | THE OWNER DECREE — parse-that readopted as published: npm latest 1.0.0 (S.H4 cut made FOR value.js; repo `master@ef10d5b`); zero-copy Span core; byte-scanner leaves live in main entry; SpanParser variant rightly excised at 1.0.0 (`043c4d1`); subpath surface `.`/`./core`/`./diagnostics`/`./packrat`/`./utils`; service record v1.0.0→v3.1.0 under O.W6 bench + `proof-perf-target`. Consequences: regex retired unconditionally; R-PARSER is a restoration, not a contest; internal work OUT OF SCOPE → PT-E letters. | Same tests as E-L1-S2.3/S2.4; additionally the wave records the published-package facts (version, pin, subpaths) as its consumption contract. |

### P2 — Drops archaeology + THE RESTORE LEDGER

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P2.1 | The loss is SINGLE-SIDED: value v4 one-commit extinction (boundary.ts 604L + gamut.ts 526L + raytrace.ts 137L + okhsl.ts 270L, ΔE family grep=0 at HEAD, Into family, colorFilter, boundary samplers, sampleColorRamp/mixColorsN, 3.1.0 HDR surface ictcp()/jzazbz(), spring()/parseSpring grammar, measured parser+benches+gate). v4.0.0 CHANGELOG names ZERO of these. Named mechanism: no advocate ⇒ no tombstone. kf scar tissue to delete on restore: `compile/emit/backward/color.ts:120-124` hand-rolled ΔE; `backward.ts:30-32` stale docstrings. Extant `mapColorToGamut` not §13-conformant; `safeAccentColor` compounds (~67 dynamic evaluate calls upper-bound). | Restore waves cite the single-commit extinction; the tombstone law wave names the v4 CHANGELOG silence as its motivating defect; a kf scar-deletion spec rides R-RAMP/R-DELTAE; gamut wave's gate includes §13 conformance. |
| E-P2.2-R-DELTAE | P1: ΔE family — deltaEOK (+JND const), deltaE2000, deltaEITP — restored AS-WAS, typing modernized; PREREQUISITE of P2. Lands src/color/. | A wave spec restores the three ΔE functions + JND constant into src/color/; R-GAMUT declares it a prerequisite edge. |
| E-P2.2-R-GAMUT | P2: analytical Ottosson engine (cusp+Halley) + ΔE-OK JND clip criterion (§13-conformant) + raytrace exact-boundary as TEST-SIDE oracle; zero-alloc kernel under the frozen v4 facade. Lands src/color/ + test/. | A gamut wave exists with the §13-conformance gate, raytrace confined to test/, a zero-alloc assertion, and the v4 facade signatures frozen. |
| E-P2.2-R-INTO | P3: Into family EXTENDED beyond the decided SCI-1 pair: convert/mix/gamut-map/ramp out-param paths incl. `mapColorToGamutInto`/`safeAccentColor`-class hot paths — RIDES the W56 4.1.x vehicle, NEVER forks it. Lands src/color/. | An Into wave is attached to W56; scope names the out-param path families; no separate 4.1.x fork vehicle exists. |
| E-P2.2-R-PARSER | P4: DECIDED (owner decree P1.6): parse-that readopted as published — resurrect `164343c1^:src/parsing/` modernized to the v4 `/css` surface (signatures frozen); bench/ corpus + ratio gate restored as REGRESSION WITNESS (regex parser measured once for the record). Lands src/css/ + bench/ + CI. | Same as E-L1-S2.4/S2.5: restoration wave + born-RED bench gate + one-time regex measurement + frozen signatures. |
| E-P2.2-R-RAMP | P5: sampleColorRamp/At/Into + mixColorsN restored; kf backward-emit re-adopts → deletes the P2.1 scars. Lands src/color/. | A ramp wave exists; a kf consume-spec dispatch exists whose completion deletes the two named kf scars. |
| E-P2.2-R-MIX-GRAMMAR | P6: `color-mix()` grammar + structural node (zero hits today). Lands src/css/grammar.ts. | A wave (on the restored parse-that grammar) adds color-mix() parsing with a gate exercising the structural node. |
| E-P2.2-R-RELATIVE | P7: relative color `from` syntax (hard-fails today). Lands src/css/grammar.ts. | A wave adds relative-color `from` support; its gate is born-RED against the current hard-fail. |
| E-P2.2-R-CONTRAST | P8: `contrast-color()` grammar + WCAG-metrics republish decision. Lands src/css/ + src/color/. | A wave adds contrast-color() grammar AND an owner/registry decision row exists for the WCAG-metrics republish. |
| E-P2.2-R-OKHSL | P9: OKHSL/OKHSV restored (reuse P2's cusp math). Lands src/color/. | An OKHSL/OKHSV wave exists depending on R-GAMUT's cusp math (no duplicate cusp implementation). |
| E-P2.2-R-HDR | P10: the 11-day HDR parse drop (ictcp()/jzazbz()) — OWNER DECIDES vs the CSS-native-only law. | An owner-decision docket row exists for HDR restore; no wave pre-empts the decision. |
| E-P2.2-R-SPRING-GRAMMAR | P11: CSS `spring()` grammar (existed pre-v4) — OPEN ownership decision: kf-owns-SOLVER settled (K F6.6); grammar value-vs-kf undecided, leaning value. | A decision row exists for spring() grammar ownership citing K F6.6; solver ownership is not re-opened. |
| E-P2.2-R-EVAL | P12: calc/math static evaluator — WEAK candidate; spec-coverage mandate vs DOM-resolution status quo. | A decision row exists (not a build wave) weighing the two stated positions. |
| E-P2.2-R-SOA | P13: channel-fold SoA as a NEW primitive under the zero-alloc mandate (the 3.0.0 tombstone stands as written). Lands src/color/. | Any SoA row is framed as a NEW primitive; the 3.0.0 tombstone is not re-litigated. |
| E-P2.2-R-BOUNDARY | P14: gamut boundary samplers — GATED on the W53 perceived-space-plate rebuild's ACTUAL needs; built iff pulled. | A conditional row exists gated on W53's needs; no unconditional boundary-sampler wave exists. |
| E-P2.3 | Tombstones (RIGHTLY — NEVER re-litigate): OO/ValueUnit flip · kf 5.0.0 aliases · animate.ts zombie · loadEngine/loadCompiler/loadIngest accessors · flattenObject family (D28/D54) · cubicBezierToSVG · `timingFunctions` aggregate registry · BBNF grammar files · color-soa-as-3.0.0-decided · kf parsimmon parser + kf units/color (TRANSFERRED) · the U apparatus collapse (owner-ordered) · keyframes-vue (owner-killed). | No wave, restore row, or decision row proposes reviving any tombstoned item. |

### P3 — Zone dispositions + gates/tests program

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P3.0 | Method (standing discipline): verdicts rest on the four-tree consumer census (kf demo, value demo, glass-ui, atlas — exhaustive for every prune/demote-class call) + the internal wiring graph, at pinned HEADs. The r1 omission of the external census alone flipped five verdicts; three more flipped on the unchecked RECORD axis. | Every prune/demote-class row in the formation carries four-tree census evidence AND a record-axis check. |
| E-P3.1 | The kf zone table (canonical `0dac636b`) is adopted verbatim: KEEP-EARNED (engine, compile w/ emit-shrink, resolve, group, waapi, physics spring/decay/smooth/numeric/playback, orchestration sequence/stagger/view-transition/timeline, drag, morph-svg, presets w/ SHRINK, constants/validate/public/load-engine) · PRUNE-CANDIDATE (oscillator record-refuted + rider; split-text w/ named split-a11y-oracle casualty; motion-path) · OWNER-DECISION (physics/morph, flip, draw-svg, scroll w/ K-1 fence, ingest, internal/ restructure). Aggregates: prune ≈0.8k, owner-decision ≈2.4k. Record-state trichotomy + K F6 FENCE ANNEX (K-1 JS-sampler KEEP, no-polyfill law, ARCH-kill wall, spring-solver LEAD, OD-3): any V-next prune in scroll/timeline/physics must cite and overturn the named ruling. | The registry reproduces every zone verdict unchanged; split-text's row names the split-a11y-oracle casualty; the F6 annex is carried as a fence register; prune totals do not exceed the adopted aggregates without new census+record evidence. |
| E-P3.2 | The value zone table (`db77dbd8`) is adopted: color/ KEEP+RESTORE; css/ KEEP-surface REWRITE-parser (`collectDeclarations` orphan noted); value.ts/foundation/easing/transform-path KEEP (easing D13 halved-curve rider = restore-or-tombstone decision); decompose.ts PRUNE (or decided kf-adoption wave); quantize DEMOTE; subpaths/ DISSOLVE-ALLOWED keys frozen [see OA-14]; api/ EXTRACT [REVERSED by OA-05]; deps STRIP/RELOCATE + manifest gate (self-dep prior art v1.1.0–v2.0.1); 39 PNGs DELETE / demo tests DISPLACE (R14′ gate); e2e per E-union; demo KEEP-as-product tranche-restructure. Narrative headline: core SHRANK 5,020→4,647 (−7.4%); "explosion" ≈93% APPARATUS; genuinely-better is MEASURED (gamut 37→9 allocs; 37× lookup; HSL→RGB 3.8–4.4×; ~5× ramp fold). | Every value zone verdict lands as its stated row type; the easing D13 rider appears as a restore-or-tombstone decision row; the collectDeclarations orphan is noted in the css/ wave; the measured-better numbers are carried as evidence, not re-derived. |
| E-P3.3-1 | kf program row 1: LAND W9 FIRST (staged-only on `v/w9-staging@b920b190`): MR1 pageerror-keying · MR2 wire the 5 skip-masked browser oracles · MR3 `require_demo_green` break-glass · MR4 `test:demo` in CI · the staged prune (~1,384L cruft + `proof:owner-golden`→`review:`). | A kf spec dispatch exists to land W9 from the named staging SHA, enumerated MR1–MR4 + prune, sequenced FIRST in the kf apparatus band. |
| E-P3.3-2 | kf row 2: WIRE the two enforcement-free structural guards — `lint` (depcruise) + `proof:structure` run in NO workflow, ever; two one-line CI steps, the cheapest real wire before a restructure tranche. | A spec row adds both CI steps; sequenced before restructure waves. |
| E-P3.3-3 | kf row 3: DEMOTE `audit:lighthouse` (masked `\|\| echo`, cannot fail). | A spec row demotes/unmasks the lighthouse step. |
| E-P3.3-4 | kf row 4: weekly-cadence relabel is PRE-BOOKED at DISPOSITIONS CH-05 — fold, don't double-book. | The relabel is folded into the CH-05 row; no duplicate wave exists. |
| E-P3.3-5 | kf row 5: boundary-cohesion — FOLD its clamp-single-site invariant into proof:structure, THEN retire the file; FLAG the 3 partial readFileSync arms. | A fold-then-retire row exists with the readFileSync flag; no naked prune of boundary-cohesion. |
| E-P3.3-6 | kf row 6: BIND zone tests to zone verdicts; no orphaned green. | Each kf zone row names its test block; test fate follows verdict. |
| E-P3.3-7 | kf row 7: SEQUENCING LAW — MR2/MR4 land BEFORE the restructure's ~340-line import churn. | The dependency graph orders MR2/MR4 before any flatten/restructure wave. |
| E-P3.3-8 | kf row 8: excluded phantoms MUST NOT enter the tranche — the GS-04 known-violations doc-fix row (no baseline exists/wired) and the demo-Draggable "consumed" cell (filename-grep noise). | Neither phantom appears anywhere in the formed tranche. |
| E-P3.4-1 | value program row 1: library gate surface stays PRODUCER+API (the e2e fleet proves nothing about the packed library — 0/191 blocks import it; already CI-unwired at the v4 cut). | No formation row wires e2e into the library gate; library gates remain producer+api. |
| E-P3.4-2 | value row 2: the e2e adjudicated set — ABROGATE oracles/ design-census (~5.9k, 0 catches) + headless perf (~856) · FOLD ~1.7k (o1/o2→boot, browse→walk, mobile→viewport param, dual-pane→build-check, webgl→CH-7 annex + 1 context-loss probe, o16→glass producer surface) · KEEP-EARNED ~1.7k on VERIFIED catches (page-load, walk/admin-walk, color-space-switching, DRIVEN a11y battery w/ 2 born-RED WCAG catches, the safari trio) · plan-KEEP ~1.2k journey subset at the ratified W55 re-gate (which OWNS the .github edit); safari + real-GPU annex runs pre-deploy/nightly. | Per-oracle disposition rows exist matching the four buckets and the named fold targets; the journey subset is bound to W55 (and only W55 touches .github); the safari/GPU annex is scheduled pre-deploy/nightly. |
| E-P3.4-3 | value row 3: BUILD the tests-isomorphism/structure gate from scratch (born-RED; none exists) + displace the demo-component tests. | A born-RED isomorphism-gate wave + a displacement row exist. |
| E-P3.4-4 | value row 4: pre-publish manifest gate (deps strip; self-dep prior art). | A manifest-gate wave exists whose gate rejects the deps block and self-dependency class. |
| E-P3.4-5 | value row 5: resurrect bench/ + the ratio perf gate WITH R-PARSER. | The bench wave is coupled to R-PARSER (same band/dependency). |
| E-P3.4-6 | value row 6: the capability-diff gate (P0/§7 law) — export-census diff vs prior tag; RED on any removal lacking a by-name tombstone. | Same as E-L1-S7.2: the gate wave exists with that exact rule. |

### P4 — Fences, wedge, flatten, governance

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P4.1-1 | kf fence: `TimingFunction = (t: number) => number` FROZEN at `src/animation/constants/types.ts:45`. Atlas chase census (ACTIVE tree, 9 imports/8 files): THREE TimingFunction sites (useCountUp.ts:47, useScrollTimeline.ts:44, useScrollLettering.ts:57; IN-ATLAS-5 is the correction event) + `MorphSVG` via `@mkbabb/keyframes.js/engine` (buildMarkAnimation.ts:7) — the `./engine` subpath HAS a live external consumer. | No kf-touching wave changes the TimingFunction type; the chase ledger pre-names the three atlas sites; no wave removes/renames `./engine` without the co-land boundary. |
| E-P4.1-2 | kf fence: exports exactly `.` + `./engine`; the 44-key engine runtime mirror RE-VERIFIED BY EXECUTION after any structural move. | Every kf structural-move spec includes a post-move 44-key mirror execution check. |
| E-P4.1-3 | kf fence: kf 6.0.0 / value 4.0.0 are IMMUTABLE; breaking work goes behind the P4.2 co-land boundary. | No wave republishes/mutates the released 6.0.0/4.0.0 artifacts; breaking rows are sequenced behind the boundary wave. |
| E-P4.1-4 | kf fence: depcruise value.js-free-leaf law keyed `^src/animation/internal/` (`.dependency-cruiser.cjs:171`) — the key MOVES WITH any rename. | Any internal/ restructure spec includes updating the depcruise key in the same wave (re-derived at execution per law 3). |
| E-P4.1-5 | kf fence: `scenes/` exemplar fence; `docs/precepts/` read-only. | No wave writes to kf `docs/precepts/`; scenes/ changes respect the exemplar fence. |
| E-P4.2-1 | Wedge pricing (ERESOLVE-proven twice): a present-incompatible `peerOptional` HARD-FAILS npm install. glass 7 peers kf `^6` + value `^4` (optional) AND vue ^3.5 / tailwindcss ^4 / reka-ui ^2 / @lucide/vue ^1.16 (NON-optional — chase ledger). kf pins value EXACT `4.0.0`. ACTIVE atlas consumes the exact tuple. | The co-land boundary wave's pricing sheet lists every named peer edge incl. the non-optional framework peers; no wave ships a present-incompatible peer before the boundary. |
| E-P4.2-2 | The value deps bomb: working-tree `dependencies: {glass ^7, kf ^6}` (born `f2c8f565`; published 4.0.0 deps-free). If shipped: registry kf↔value cycle + dual-core forcing. Remedy: STRIP/RELOCATE + pre-publish manifest gate — NEVER lockstep-bump. | The deps wave strips/relocates (not bumps); the manifest gate blocks the class; no lockstep-bump row exists anywhere. |
| E-P4.2-3 | NO stale-atlas catch-up wave (npm latest already 7.0.0); sci-report crosses atomically at its own glass-7 consume (value W56 row). ONE co-land boundary for value 5 / kf 7 / glass peer-bump / atlas ranges; chase-site ledgers NAMED IN ADVANCE; internal-only restructures until then. | No atlas catch-up wave exists; exactly one boundary wave; chase ledgers enumerated in the boundary spec; all pre-boundary restructures declare frozen surfaces. |
| E-P4.3 | The flatten census is born-RED at every anchor — 13 config/gate anchors (tsconfig self-alias :30 + `@src/*` · vite :41/:156/:172/:225/:229 · vitest :18 · depcruise :84/:93/:171 · structure-gate resolver index.mjs:91 · engine-dts-rollup :34/:64 · surface gates 15 grep sites) + ≈340 import lines (test 277 + bench 37 + scripts 18 + demo ~9). Post-move: 44-key mirror re-execution + `proof:structure --selftest` + full depcruise run. Anchors RE-DERIVED at execution (N-ADJ-3: LT-10's cite already drifted :168→:171). | The flatten wave enumerates all 13 anchors with born-RED checks, carries the import-line census, mandates the three post-move verifications, and contains an anchor re-derivation step. |
| E-P4.4 | Structure governance (G5′): kf structure was JUST settled (proof:structure R1–R6, 755L, live). Structure waves AMEND the ratified LT blueprint + EXTEND the gate — never a parallel authority — naming superseded rulings (LT-10: kept internal/ against a 40+-importer edit, owner's new edict supersedes the taste ruling but the cost census STANDS; LT-16). Refutation amends the charter; silence re-litigates it. | Each structure wave names its LT amendment target; proof:structure is extended, not replaced; the LT-10 cost census is cited, not re-derived. |
| E-P4.5 | Ownership protocol: this tranche DIRECTS kf library items as SPECS + bounded dispatches into `keyframes.js/docs/tranches/V/coordination/` (grammar: `<SENDER>-INBOUND-*`); the kf successor implements; kf demo/UI corpus + FOLD-FORWARD rows stay kf-owned; direct cross-repo edits require an EXPLICIT OWNER GRANT. | Every kf deliverable is an INBOUND-grammar dispatch file; no value wave scopes kf demo/UI or FOLD-FORWARD items; any cross-repo edit row cites its owner grant. |

### P5 — The prior-record index (inherit as DECIDED; never re-derive)

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P5.1 | SCI-1 = DECIDED SHIP-4.1.x (value DECISIONS.md:82 D54; vehicle W56 — mixColorsInto/toRgba8Into). R-INTO extends it; NOTHING re-opens it. | W56 exists as the 4.1.x vehicle; R-INTO attaches to it; no row re-adjudicates SCI-1. |
| E-P5.2 | D-GAP-6 sampleBezier: DECLINED, accepted by kf (adopts only if a future 4.1 ships it). §D name-a-code re-open path STANDS (a named ParseIssue code re-opens the confirm-shipped verdict). RF-18 census-split ENACTED. | No wave ships sampleBezier absent a 4.1 decision; the §D re-open trigger is carried as a registry row; RF-18 is treated as enacted (not re-proposed). |
| E-P5.3 | PT-E (`parse-that/docs/tranches/A/VALUEJS-PT-E-2026-07-05.md`): the standing parse-that→value ask letter (diagnostics HIGH · inference MED · Pratt-dormant) — in the ingestion set. | The PT-E letter is rowed in the formation inventory with a disposition per ask (diagnostics/inference/Pratt). |
| E-P5.4 | kf R2-07 + W9: the gate/test prune blueprint is ADJUDICATED; execution STAGED-ONLY on `v/w9-staging@b920b190` — landing it is P3.3 row 1. CH-05 pre-books the weekly-relabel fold. | Same as E-P3.3-1/-4: land-W9 dispatch exists; CH-05 fold honored. |
| E-P5.5 | EP-3 PATH-B decided-terminal rows: flip/flipShared, drag/Draggable, DrawSVG/fromDrawSVG (drag's vacuity premise now DEFEATED by glass consumption; the disclosure row stays booked). K.W8/K.W9 book rows: ingest + scroll feature theses. The K F6 register is the standing fence annex. | The owner-decision rows for flip/draw-svg cite EP-3 PATH-B; drag is KEEP-EARNED with the disclosure row still booked; ingest/scroll decisions cite their book rows; the F6 register is carried whole. |
| E-P5.6 | kf FOLD-FORWARD (`docs/tranches/V/FOLD-FORWARD.md`): W7/W8 fold whole, W9 staged, W10 pre-rail subset landed, W11 folds, W12 substantially discharged, W13 close-ceremony; the 15-row marks register (glass §7 defects with live kf exposure); owner-checkout reconciliation safe+pending. | The formation treats these fold states as decided input; the 15-row marks register is referenced where glass §7 defects appear; nothing re-derives the fold states. |
| E-P5.7 | value vehicles: W53 (perceived-space plate rebuild — gates R-BOUNDARY), W55 (journey-subset re-gate — OWNS the .github edit), W56 (SCI-1 4.1.x cut). V-PRIME B3/B4 as recorded. | W53/W55/W56 exist as vehicles with their stated ownerships; R-BOUNDARY gates on W53; only W55 touches .github; V-PRIME B3/B4 carried as recorded. |
| E-P5.8 | Oscillator doc-truth defect row: published-surface.md:44 names two consumers FALSE on disk; `proof:publish` checks coverage-file existence, not consumer claims — a NEW gate rule candidate. | A defect row exists for the false consumer claims + a gate-rule-candidate row for consumer-claim verification in proof:publish. |

### P6 — Ingestion set + first actions

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P6.1 | Read before the first spawn (pin every tree per G0′): (1) the refined kickoff prompt + the packet set; (2) the r2 panel corpus (adjudication-panel1/2-r2, skeptic-{A,B,H1,H2,D,E,K,F}-r2, mirrored at keyframes-v-exec …/vnext/); (3) kf: FOLD-FORWARD, PROGRESS, OWNER-DECISIONS, INBOUND-LEDGER, R2-05 LT rows, published-surface.md, EP-3, F6 rulings; (4) value: DECISIONS.md (D50/D54), CARRY-LEDGER.md, V-PRIME.md, WL letters, DISPOSITION-LEDGER.md; (5) PT-E, the Q060 glass packet, glass §7 defect rows (kf marks register); (6) archaeology grants read-only: the four git histories; `164343c1^` for every resurrection recipe. | The formation's provenance/ingestion record lists all six ingestion classes as read, with G0′ pins recorded before the first spawn. |
| E-P6.2 | First actions, in order: row the two letters in the E13 inbox ledger → verify the r2 mirror trees → open the formation registry from P1–P5's row inventory (EVERY row lands as a wave, gate, registry row, or owner-decision row — ZERO silent drops) → dispatch the first thrice batch. | INBOX.md rows the two letters; an r2-mirror verification record exists; the registry's seed inventory is the P1–P5 row set with a complete landing crosswalk; the first dispatch is a thrice batch. |

---

## PART 3 — THE CONVERSATION ADDENDA (E-C1 .. E-C23) — from OWNER-AMENDMENTS.md; these WIN on their named axis

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-C1 | OA-01: seed model names Fable/Opus are REPLACED by Sol/Luna. Formation, design, critique, adjudication use explicitly declared Sol; later implementation may use Luna only when separately authorized. Served-model evidence REQUIRED. | Every spawn declares Sol explicitly; every return records served-model evidence; no Luna use without a separate authorization record. |
| E-C2 | OA-02: frontend scope broadened from "value-focused" — audit and refine EVERY page of BOTH value and keyframes demos on mobile AND desktop; preserve value's already-good identity. D00–D25 and M00–M11 are coequal full-route programs; refinement does not authorize generic visual replacement. | The registry contains both full-route bands (D and M) covering every route × {mobile, desktop}; no wave replaces value's identity wholesale. |
| E-C3 | OA-03: keyframes mobile is materially broken; Cube and every applicable scene need custom quaternion/multitouch interaction + Breath of Life. K12 and M03–M11 own one/two-pointer, cancellation, capture, keyboard, and reduced-motion truth. | K12 + M03–M11 exist with gates covering one-pointer, two-pointer, cancellation, capture, keyboard, and reduced-motion behavior per applicable scene. |
| E-C4 | OA-04: the value mobile tools bar is explicitly LOST (not merely an audit lead). D00A/D03A restore mount-independent capability descriptors and reachable mobile tools. | D00A/D03A exist; their gates assert mobile-tools reachability and mount-independent capability descriptors. |
| E-C5 | OA-05 (supersedes seed "api/ EXTRACT from repo"): the Palette API REMAINS within `value.js/api`. A00–A20 transpose the backend INTERNALLY; NO standalone extraction wave and NO compatibility proxy. | No extraction wave exists; A00–A20 are internal-transpose waves; no proxy/compat layer appears in any spec. |
| E-C6 | OA-06: specify Palette history, variants, ALL CRUD, mixing, sharing, and protocol isomorphism with Fourier; perfect BOTH API module layouts; runtime code remains language-local. A00–A26 + the authoritative API contract cover both services. | A00–A26 exist; an authoritative API contract artifact covers both services; no cross-language runtime sharing appears. |
| E-C7 | OA-07: BOTH demos require robust canonical URL/share state; shadcn is TOTALLY ABROGATED in favor of Glass + Tailwind v4, with missing Glass primitives designed at their owner (glass-ui). D01/D02, M01/M02, G00–G09 own the clean break; no copied vendor or dual state path survives. | URL/share-state waves exist for both demos; a shadcn-abrogation wave leaves zero shadcn/vendor-copied components; missing primitives are G-band (glass-owned) waves; no dual state path in any spec. |
| E-C8 | OA-08: the mega-tranche must contain AT LEAST 50 waves spanning value, keyframes, parse-that, Fourier, Glass, and consumers. (Realized: canonical registry = 190 waves across eight bands, six-plus repositories.) | The canonical wave registry counts ≥50 waves and spans the six named surfaces (observed target: 190 across bands P/V/K/A/G/D/M/C). |
| E-C9 | OA-09: Codex configuration must guarantee the requested tier — Sol ultra mode: `gpt-5.6-sol`, `model_reasoning_effort="ultra"`, `approval_policy="never"`, `sandbox_mode="danger-full-access"`; EVERY panel records the served tier. | The formation config records the four settings; every panel return includes the served-tier record. |
| E-C10 | OA-10 (chronology: later tightened by OA-17): a parse-that major up to 1.1 was briefly allowed, then BOUNDED — use parse-that for the full CSS prototype; leave novelty to the separate BBNF major session. The interim compatible-1.0.1 branch is SUPERSEDED by OA-17. | No wave targets parse-that >1.0.0; novelty items route to the BBNF session's handoff; the 1.0.1 branch appears nowhere as live authority. |
| E-C11 | OA-11: preserve ALL Sol parse-that research/proposals UNCOMMITTED in parse-that at `/Users/mkbabb/Programming/parse-that/docs/tranches/B/` — evidence/proposal material, NOT executable V-next authority; not committed by this tranche. | The bank directory holds the proposals; no V-next wave cites them as authority; no commit of that material is made by this tranche. |
| E-C12 | OA-12: the mobile easing selector is far too rounded/large/low-resolution — dramatic redesign. G09/M08 specify the Chronograph Contact Sheet: dense ruled vectors, ≤4px painted radius, independent ≥44px semantic seats, exact/adaptive high-resolution paths, a precise selected editor. | G09 and M08 exist with the five named spec parameters as acceptance criteria. |
| E-C13 | OA-13: coordinate EVERY parse-that action with the already-active BBNF-lang uplift. The frozen V1 stays historical; the append-only V2 stream pins SK-V25; P02 emits intent, P03 requires a same-pin clearance; V-next NEVER imports active V3/Tape/SoA/Pratt/topology novelty. | P02 has an intent-emission deliverable; P03's gate requires same-pin clearance; no wave imports the named novelty classes; V2 stream is append-only at SK-V25. |
| E-C14 | OA-14 (supersedes the seed's 7-key freeze / zone-only deletion language): deletions are WELCOME when they distill, clarify, perfect; affected consumers may be informed and migrated. Historical export count is NOT a preservation target. V00C/V29T may remove `./value`, `./quantize`, forwarding sectors, and other unworthy surfaces ONLY AFTER intrinsic-job adjudication, exact casualty proof, a by-name tombstone, and owned consumer migrations. NO alias or shim. | Every export-key removal row shows the four preconditions (intrinsic-job adjudication, casualty proof, tombstone, owned migrations); no alias/shim appears; V00C/V29T are the owning waves. |
| E-C15 | OA-15: EVERY implemented wave must be challenged by ≥2 fresh hostile gestalt passes then ratified — each pass judges total-tranche fit + original optimality, wave-contract adherence + friction, and feature behavior. One final-state pair of hostile Sol critics + a third Sol adjudicator is CONTENT-ADDRESSED in every terminal implementation return. A surviving finding changes the state digest, reopens its exact owner, forces a fresh triad. C08/C09 are additional whole-corpus passes and CANNOT substitute. | Every terminal implementation return embeds a content-addressed 2-critic+1-adjudicator triad record; surviving findings demonstrably reopen owners with fresh triads; C08/C09 exist separately. |
| E-C16 | OA-16: every runtime CSS `.bbnf` module extant at P01's coordinated execution pin is mirrored EXACTLY by one `.ts` file implemented as parse-that combinators; every non-runtime CSS grammar gets an explicit typed disposition. A generated parser may replace later but is NOT the present architecture. `CSS-MODULE-ISOMORPHISM.json` records the 15-runtime+1-excluded partition mapping stems one-for-one into `src/css/grammar/l4` — not an execution ceiling. After the BBNF session freezes the larger union, P00 takes a new pinned epoch and emits `test/proof/p00/css-module-isomorphism.execution.json` equal to the upstream freeze byte-for-byte; P01 consumes it. Today's `stylesheet→keyframes` correction is a formation-defect record, NOT permission for a local edge: execution requires the upstream canonical DAG and ZERO corrections. `pretty.bbnf` test-oracle-only holds only in the current receipt. Authorship independent at combinator/CST-fixture level, not module topology. Generated and combinator runtimes NEVER coexist. | CSS-MODULE-ISOMORPHISM.json exists with the 15+1 partition; P00's execution manifest is specified as byte-equal to the upstream freeze; P01 consumes it; the stylesheet→keyframes edge is recorded as a defect; no wave permits coexisting generated+combinator runtimes. |
| E-C17 | OA-17 (supersedes the OA-10 conditional 1.0.1 branch): value consumes the EXACT published `@mkbabb/parse-that@1.0.0` registry artifact. NO parse-that source mutation, candidate package, or republication in this tranche; blockers are banked and routed, holding adoption pending a new authorized epoch. P00 emits one typed registry/no-link receipt; P01–P07 bind and crater those exact bytes; P02/P03 may minimize and bank proposals only; P04 is always `no-rc`; C10 leaves parse-that untouched. | P00's deliverable includes the typed registry/no-link receipt; P01–P07 declare the exact-bytes binding; P04 is specified `no-rc`; C10's scope excludes parse-that changes; every blocker row is banked+routed, none mutates parse-that. |
| E-C18 | Authority law of the docket: it is the SINGLE chronological docket of post-seed owner statements. A later, narrower statement supersedes an earlier one ONLY on the named axis; every other seed law remains in force. An amendment changes formation authority, never retroactively edits the seed or the frozen parse-that V1 packet. | The formation cites the docket as the sole amendment authority; superseded seed rows are marked on the named axis only; the seed letters and V1 packet are byte-unmodified. |
| E-C19 | Deletion judgment (protocol): every deletion decision answers four falsifiable questions — (1) what intrinsic job, independent of consumer count, does the surface perform? (2) which retained primitive or application-owned composition performs every worthy job with less conceptual and runtime cost? (3) which exact imports/runtime behaviors/data formats/documentation claims are casualties, and which NAMED WAVE migrates each before terminal deletion? (4) does the packed capability diff contain a by-name tombstone and ZERO alias/shim/fallback/source-link/dual path? | Every deletion row in the tranche records answers to all four questions, with a named migrating wave per casualty. |
| E-C20 | Deletion judgment (decision rule): failure of questions 2–4 BLOCKS deletion. Failure to prove a unique intrinsic job FAVORS deletion. Age, LOC, and raw consumer count do NOT decide either direction. | No deletion proceeds with an unanswered Q2–Q4; no keep/delete rationale rests on age, LOC, or consumer count alone. |
| E-C21 | Parse-that/BBNF boundary: the active SK-V25 campaign is an execution PREDECESSOR for the terminal CSS module/edge freeze, but NEVER a Value runtime dependency. | No Value wave declares a runtime dependency on SK-V25 outputs; the freeze-consumption path goes through the P00 epoch mechanism only. |
| E-C22 | The ONLY admissible parse-that adoption branch is `unchanged-1.0.0-no-republish`. Every other proposal remains BANKED for the active campaign. | The adoption decision row carries exactly this branch string; all alternative proposals are in the bank with no live wave. |
| E-C23 | Coordination drift causes a NEW append-only snapshot; it NEVER causes an in-place edit of historical receipts or a Value-owned mutation intent. | Snapshot history is append-only (no rewritten historical receipt); no Value-side row expresses a parse-that mutation intent on drift. |

---

END OF MATRIX. 38 charter rows (E-L1-S0.1..S9.1) · 40 packet rows (E-P0.1..E-P6.2, incl. all 14 P2.2 restore-ledger rows, all 8 P3.3 + 6 P3.4 program rows, all P4 fences, all 8 P5 inherited-DECIDED rows) · 23 addenda rows (E-C1..E-C23).
