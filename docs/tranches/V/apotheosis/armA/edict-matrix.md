# THE EDICT MATRIX — V-next canon, rebuilt 2026-07-19 (armA, addenda-corrected)

CANON SOURCES (the binding triad — read in full):
- **L1 (charter letter):** `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/keyframes-inbox-2026-07-18-vnext-ingestion-prompt.md`
- **P (context packets):** `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md`
- **C (conversation addenda):** `/Users/mkbabb/.claude/jobs/9e7dadd0/tmp/apotheosis/CONVERSATION-ADDENDA.md` — owner edicts from the live 2026-07-19 session; **addenda WIN where they tighten the letters** (per the file's own header).

CROSS-CHECK SOURCE (NOT canon; used only for the FINDINGS section):
- `/Users/mkbabb/.claude/jobs/9e7dadd0/tmp/apotheosis/snapshot-vnext/OWNER-AMENDMENTS.md` — the GPT-5.6 **Sol Codex fleet's** own docket of post-seed owner statements (OA-01..OA-17 + deletion judgment + parse-that/BBNF boundary). It is a SECOND, independent capture of the owner's post-letter voice, addressed to a different fleet. Divergences from CONVERSATION-ADDENDA are recorded as FINDINGS (§ Findings) — they are not folded into the matrix rows.

PROVENANCE / CORRECTION NOTE:
1. The prior draft (`edict-matrix-draft.md`) lacked the CONVERSATION-ADDENDA file and substituted `OWNER-AMENDMENTS.md` as its addenda source. **The two use overlapping C/OA numbering for DIFFERENT content** (e.g. CONVERSATION `C2` = the mobile easing selector; OWNER `OA-02` = frontend-totality. CONVERSATION `C14` = the dual BBNF/parse-that parser; OWNER `OA-14` = the deletion-judgment/key-removal protocol. CONVERSATION `C15` = the parse-that semver bound; OWNER `OA-15` = the per-wave hostile triad). Every `E-C*` row below is therefore rebuilt from CONVERSATION-ADDENDA, and every `[TIGHTENED/SUPERSEDED by E-C…]` annotation on the letter rows is re-pointed to the correct CONVERSATION C-number with its correct content. The Codex-docket-only material (Sol/Luna renaming, the four-question deletion judgment, the `unchanged-1.0.0-no-republish` boundary, the CSS-MODULE-ISOMORPHISM machinery, the 190-wave/8-band shape) is NOT binding for this program and lives only in FINDINGS.
2. No path containing `r1-opus-refuted` was read.
3. Binding-model note (E-C17): for THIS program, agents are **Fable** (design/brainstorm/adjudication/novelty/frontend) and **Opus** (mechanical sweeps/inventory/extraction) — NOT the Codex docket's Sol/Luna.

Row grammar: **ID | edict digest (load-bearing specifics preserved) | COMPLIANCE TEST** (what a compliant formed tranche must observably contain).

---

## PART 1 — THE CHARTER LETTER (E-L1-S0.1 .. E-L1-S9.1)

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-L1-S0.1 | Formation-only: NO source edits land from this prompt. Deliverable = the next tranche fully formed: plan folder, wave specs with born-RED gates wherever the defect is live, π/DELTA obligations for every visual claim, terminal disposition for every chronic/deferred item and prompt-recap row. | Formation output is a plan folder; `git diff` over `src/`/`api/`/`demo/` from the formation session is empty; every live-defect wave names a born-RED gate; every visual-claim row carries a π/DELTA obligation; a disposition ledger covers every chronic/deferred/prompt-recap row with a terminal state. |
| E-L1-S0.2 | Model routing (supersedes every earlier routing paragraph; owner 2026-07-18): ALL agents Fable, declared on every spawn; ZERO Opus except later per-tranche owner-ratified mechanical sweeps. Verify the tier ACTUALLY SERVED, not the declaration. Batches 5–6 concurrent; thrice panels exactly 3 seats. **[GOVERNED for THIS run by E-C17: Fable = design/brainstorm/adjudication/novelty/frontend; Opus = mechanical sweeps/inventory/extraction; every seat reports `model_served`. NOT Sol/Luna — see Finding F1.]** | Every spawn declares its model; every return records served-model evidence (not just the declaration); no seat ran the forbidden tier; panels are exactly 3; batches 5–6; Opus appears only on mechanical/inventory/extraction seats. |
| E-L1-S0.3 | Tree pins (G0′ law): pin tree+branch+HEAD before any cross-repo claim. Pins: value.js `tranche-u@db77dbd8` (4.0.0) · keyframes-v-exec `master@0dac636b` (6.0.0 CANONICAL kf) · parse-that `ef10d5b` · bbnf-lang `b3cf48e3b` · atlas ACTIVE `/Users/mkbabb/Programming/.p-totality/atlas` `p/totality@fe9abcf` (7.0.0; standalone `atlas@master` is a STALE trap; npm latest ALREADY 7.0.0) · glass-ui `master` 7.0.0 (re-pin at read). | Every cross-repo claim cites the exact pinned SHA; no claim cites the stale standalone atlas checkout; every glass-ui claim carries a fresh re-pin at read time. |
| E-L1-S0.4 | The 32-agent budget: steerable, registry-driven, adversarial. No lens permanently staffed; withhold the favored success narrative from most auditors; dedupe finding-families by mechanism; decompose any "redo the tranche"-strength finding into wave-shaped rows; check every "done" against the close-class lies (green-over-broken, vacuous-green gates, declared captures missing on disk, masked fallbacks, alias smuggling, re-booked chronics, per-mechanism green over gestalt broken); require concrete deliverables (file:line, failing probe, reproduction, named defect row); reject status reports / vague optimism / "routine" claims. Registry stable = two consecutive passes surfacing nothing new. | A formation registry exists; dispatch records show lens rotation + narrative withholding; findings deduped by mechanism; no surviving "redo the tranche"-shaped finding; every accepted "done" backed by file:line/probe/repro; closure evidenced by two consecutive no-new-findings passes. |
| E-L1-S1.1 | Mission: develop the next tranche across value.js/keyframes.js/parse-that + consumers (glass-ui, atlas, demos); each library uplifted and analyzed at library level; frontend work focuses on value.js **[SUPERSEDED on the frontend-scope axis by E-C22: full-route audit of BOTH demos, mobile+desktop — plus the named defects E-C2/E-C3/E-C4]**; library set and demo set are separate workflow series; prior tranches are the mined evidence set (P5). | The registry separates a library series from a demo series; per-repo library-analysis waves exist; under E-C22 the demo bands cover every route of both demos on mobile AND desktop. |
| E-L1-S1.2 | This value.js-owned tranche DIRECTS all keyframes.js library items as specs + bounded dispatches into kf's coordination inbox; the next kf-owned tranche implements/adapts (P4.5). Major architecture changes allowed so long as core features are not lost — "not lost" is MACHINE-CHECKED (§7 law 1). | Every kf library item is a spec/dispatch to kf's coordination inbox (not a direct edit); the capability-diff gate (E-L1-S7.2) is named as the machine check for "not lost". |
| E-L1-S2.1 | kf's parser exit was RIGHT (4 stages 2024-07→2026-06; parser-less since 2026-04-17; no unjust kf drop). kf-side work = boundary CENSUS LOCK: zero grammar productions both sides, ONE live row `src/animation/easing.ts:30,:38-39` (timing-function name table) — adjudicate consume-vs-ratify EXPLICITLY (P1.4). | A kf boundary census-lock wave/row exists asserting zero grammar productions both sides + an explicit consume-vs-ratify adjudication row for easing.ts:30/:38-39. |
| E-L1-S2.2 | value's extant parser is the condemned party: unmeasured regex/char-split rewrite born at the v4 cut, which deleted the parse-that combinator incumbent (v1.0.0→v3.1.0) + ALL 11 benches + the perf gate. "Proper parse-that adoption" = RESTORATION of the deposed incumbent; the head-to-head has never been run. | Parser work is framed as restoration (not net-new adoption or contest); no wave re-contests the seat. |
| E-L1-S2.3 | OWNER DECREE (2026-07-18, DECIDED, not bench-contested): parse-that READOPTED outright, as published — `@mkbabb/parse-that` from the registry at published latest 1.0.0. Regex rewrite retired UNCONDITIONALLY. All parse-that-INTERNAL evolution (tape, substrates, Pratt revival, combinator additions) OUT OF SCOPE — routes as PT-E ask letters. S.H3 Pratt stays RETIRED. **[BOUNDED by E-C1: leveraged for the full CSS prototype as published, NOT uplifted with novelty this tranche, minor fixes only; a separate BBNF major session owns all parse-that/bbnf-lang evolution; E-C19 gives the up-to-spec adequacy check (minor-fix candidates only).]** | A wave consumes `@mkbabb/parse-that@1.0.0` from the registry; the regex parser's retirement is unconditional; zero waves do parse-that-internal work; discovered needs are PT-E ask letters only; no Pratt row exists; the up-to-spec adequacy assessment (E-C19) yields only minor-fix candidates. |
| E-L1-S2.4 | Wave 1 — R-PARSER restoration: resurrect the deposed tree (recipe `git show 164343c1^:src/parsing/`) modernized to the v4 public `/css` surface (parseStylesheet/collect*/ParseIssue signatures PRESERVED — consumers never move); re-add parse-that per value's historical pin convention; co-land wave prices the dependency edge; `parseFunctionalColor` ladder, deepFreeze-per-parse, per-char `/\s/.test` costs die with the regex parser. | An R-PARSER wave cites the `164343c1^` recipe; its gate asserts the frozen `/css` signatures byte/type-compatible; the dependency edge is priced by a named co-land wave; the three named costs are listed as dying with the regex parser. |
| E-L1-S2.5 | Wave 2 — bench restore as REGRESSION WITNESS: resurrect the bench corpus (`git show 164343c1^:bench/css-parse-perf.mjs`) + the portable ratio gate (MB/s + ns/call + co-scaling ratio); baseline the restored parser (born-RED until it meets the gate); measure the retiring regex parser ONCE for the record. The bench tunes; it does not re-contest the seat. | A bench-restore wave cites the recipe; its gate is born-RED with the recorded ratio recipe; a one-time regex-parser measurement row exists; no wave uses bench results to re-open the readoption. |
| E-L1-S2.6 | Wave 3 — spec-completeness census: born-RED coverage census of grammar/color/timeline surfaces vs the July-2026 stabilized set; gaps become waves ON the restored parse-that grammar, incl. named restores: `color-mix()`, relative-color `from`, `contrast-color()`, the 11-day HDR parse drop (owner decides vs the CSS-native-only law), `spring()` easing grammar (OPEN ownership row: solver kf-owned per K F6.6; grammar value-vs-kf open, leaning value). **[BROADENED by E-C12: the census targets the ENTIRE July-2026 CSS spec incl. experimental + CSSOM + WAAPI + keyframes/animation/timeline/stylesheet, fully typed congruent to DOM.]** | A born-RED census wave exists against the July-2026 set (breadth per E-C12); each named restore is a wave or decision row; HDR carries an owner-decision row; spring() grammar carries an open ownership row citing K F6.6. |
| E-L1-S3.1 | Color program: best SOTA with near-perfect zero-alloc facilities for all spaces — a RESTORE program, not net-new (P2). Loss is SINGLE-SIDED (value v4 cut); kf NEVER owned gamut code; kf-side = consume-only + deletion of scar tissue (hand-rolled oklab ΔE; stale `sampleColorRamp`/`deltaEOK` docstrings). **[REINFORCED by E-C13: full color-space support with robust zero-alloc functions for EVERY critical operation across demo + consumers.]** | The color band is framed as restoration; no wave attributes gamut loss to kf; a kf consume-only spec exists including deletion of the two named scars; zero-alloc coverage spans every critical operation. |
| E-L1-S3.2 | The extant `mapColorToGamut` (32-iteration chroma bisection, NO ΔE stop criterion) is not CSS Color 4 §13-conformant. Decided path in priority order: R-DELTAE (prereq) → R-GAMUT (analytical Ottosson cusp+Halley + ΔE-OK JND clip; raytrace as TEST-SIDE exact oracle; zero-alloc kernel) → R-INTO (extends SCI-1, RIDES the W56 4.1.x vehicle, never forks it) → R-RAMP (kf backward-emit re-adopts, deletes local duplication) → remaining P6–P14 rows. WPT/§13 conformance vectors are NEW gate infrastructure. SCI-1 = DECIDED SHIP-4.1.x (D54) — inherit, extend, never re-adjudicate. | Restore waves exist in the stated order with the stated deps; R-INTO attaches to W56 (no fork); WPT/§13 vector gate infra is a named new gate; no wave re-opens SCI-1/D54. |
| E-L1-S4.1 | Library structure (both repos): isomorphism + directory perfection INCLUDING coherence/isomorphism BETWEEN value.js and keyframes.js in an abstract facility; same treatment for all backend files (the api/ surface + any successor), abstracted for those languages. **[api/ disposition GOVERNED by E-C7: the palette API STAYS at `value.js/api` (not extracted) with full-facility spec + isomorphism to fourier-analysis' CRUD-viz API; the tension with P3.2's "EXTRACT" must be reconciled explicitly — see E-L1-S4.5 and Finding F9.]** | Structure waves define a cross-repo abstract isomorphism target (not per-repo tidiness only); backend structure waves exist under the same discipline; the api/ stay-vs-extract reconciliation is explicit (E-C7). |
| E-L1-S4.2 | kf `src/animation` flatten + the `internal/` question are coordinated config-and-graph moves, born-RED at every anchor: 13 config/gate anchors + ≈340 import lines, census pre-run (P4.3); anchors MUST be re-derived at execution (N-ADJ-3 — ratified line-cites already drifted). | The flatten wave's gate enumerates the 13 anchors born-RED; a mandatory anchor re-derivation step is in the spec (not the cached cites). |
| E-L1-S4.3 | Structure governance: kf structure waves AMEND the ratified LT blueprint + EXTEND proof:structure, naming superseded rulings (LT-10, LT-16) explicitly — never a parallel authority. `internal/` is OWNER-DECISION (restructure, not prune): owner dislike is the mandate; LT-10's 40+-importer census is the cost sheet. | Each kf structure wave names the LT ruling it supersedes; no parallel structure authority; an `internal/` owner-decision row cites the LT-10 census as its cost sheet. |
| E-L1-S4.4 | subpaths/ dissolves ("code smell supreme, NO SHIMS") with precision: the 7 files are 163-line export-map homes, not runtime shims; dissolution keeps the 7 keys frozen, uses explicit `/index` specifiers (D50 api-extractor boundary), drops `./quantize`, creates a real `transform/index.ts`, re-verifies the packed surface. **[NOTE: the binding CONVERSATION addenda contain NO deletion-judgment / key-removal loosening — the seed's 7-key-freeze language STANDS. The Codex docket's OA-14 loosening is Finding F3, NOT binding here.]** | A subpaths-dissolution wave exists with a packed-surface re-verification gate; `/index` specifiers per D50; `./quantize` dropped; `transform/index.ts` created; the 7 keys stay frozen (no export-key removal is authorized by our canon). |
| E-L1-S4.5 | value hygiene: `transform/decompose.ts` PRUNE (609 LOC, zero consumers); `quantize` DEMOTE to demo; api/ EXTRACT from repo **[REVERSED by E-C7: the palette API STAYS at `/api` — internal treatment, no standalone extraction; reconcile the P3.2 EXTRACT row explicitly]**; 39 root PNGs deleted; demo-component tests displaced out of library test/ root; working-tree `dependencies` block (glass+kf) STRIPPED/RELOCATED to devDependencies + a pre-publish manifest gate. | Waves exist for: decompose prune, quantize demote, root-PNG deletion, demo-test displacement, deps strip + pre-publish manifest gate; the api/ row is a STAY/internal-treatment reconciliation (E-C7), NOT an extraction. |
| E-L1-S4.6 | Goldilocks granularity everywhere: no god-modules, no sand; grouped files strip the module name (`easing-option`→`option`); long dirs → encapsulated modules; glass-ui is the REFERENCE MODEL for flattening/component idioms. **[Verbatim owner restatement in E-C11.]** | Structure acceptance criteria encode the granularity rules incl. module-name stripping; glass-ui is cited as the reference model. |
| E-L1-S4.7 | Tests-isomorphism is born-RED on BOTH repos: value must BUILD its structure gate from scratch + re-mirror its test tree; kf needs a NEW isomorphism rule + support-dir allowlist. **[E-C11: tests NEVER co-located — displaced into a tree isomorphic to source.]** | Two born-RED isomorphism-gate waves exist (one per repo) with the stated scopes; no co-located tests survive. |
| E-L1-S4.8 | Demo/frontend: Aristotelian proportionality audit (cards, components, affordances, hierarchy, margins, paddings, dividing lines, small UI) alongside grand glass-ui suffusion; superfluous/duplicative/distracting elements marked for removal, under-afforded marked converse; pre-glass-7 carried-forward UI corpus audited NOW; recursive colocation. Root glass-ui defects batched to the working glass agent — released only once large swaths are isolated, precepts-compliant addenda written, exact defects targeted; never interrupt piecemeal. W53 perceived-space plate rebuild = named connection for any gamut-viz restore. **[BROADENED to BOTH demos + mobile by E-C22; enriched by E-C10 (BREATH OF LIFE corpus — glass-ui BJ + IOS27-MICRO — suffused into value-demo animation/detail: watercolor-dot hover/beget, slider-area value styling, per-screen curve/padding refinement), E-C5 (shadcn TOTALLY abrogated → glass-ui + Tailwind v4), E-C6 (URL/share state), E-C2/E-C3/E-C4 named defects.]** | Demo waves include the proportionality audit with removal/converse markings; a pre-glass-7 corpus audit wave exists; colocation in the structure spec; glass-ui defect dispatch is a batched release with the three preconditions; any gamut-viz restore cites W53; BREATH OF LIFE suffusion, shadcn abrogation, and URL/share-state waves exist per the E-C rows. |
| E-L1-S5.1 | Zone adjudication: dispositions are INPUT, not open questions — adopt the r2 zone tables (P3); verdicts rest on the four-tree consumer census (kf demo, value demo, glass-ui, atlas) + internal wiring graph, at pinned HEADs. | The tranche imports the P3 verdicts verbatim as registry rows; no wave re-derives a verdict; any new prune/demote claim carries a four-tree census at pinned HEADs. |
| E-L1-S5.2 | KEEP-EARNED includes zones the Opus-era analysis condemned: waapi (`useWAAPI: true` default), orchestration/timeline (3 runtime importers + atlas ManualTimeline), orchestration/drag (glass `Draggable` runtime), svg/morph-svg (atlas via `./engine`), physics/spring/css (`springTimingFunction` demo+atlas+glass), the LIGHT/HEAVY load-engine split. | None of these appears as a prune or owner-decision row; each is KEEP-EARNED with its consumer evidence. |
| E-L1-S5.3 | Unilateral PRUNE-CANDIDATES ≈0.8k LOC ONLY: split-text (486), motion-path (~180), oscillator (~150 — keep-record's two named consumers FALSE on disk; record-correction rider MANDATORY). | Exactly these three are prune-candidate rows; oscillator carries a record-correction rider; no additional unilateral prunes. |
| E-L1-S5.4 | OWNER-DECISION ≈2.4k LOC (flip, draw-svg, physics/morph, scroll, ingest), each with an intact deliberate-KEEP record. Record-state trichotomy governs: intact → owner decides; refuted → shield pierced + correction duty; fence-register ruling (K F6 annex: K-1 thrice-affirmed JS sampler, no-polyfill law, spring-solver LEAD) → cite-to-overturn with new Baseline evidence. | The five zones are owner-decision docket rows citing their records; any prune in scroll/timeline/physics cites and overturns the named F6 ruling with new Baseline evidence. |
| E-L1-S5.5 | Shrink notes, NOT prunes: emit/backward keeps consumed round-trip core + CC-3 refusal, trims `compileToViewTransition` (demo=0; NO phantom rows — compileToString/formatKeyframes do not exist); play-lifecycle recombines; presets catalog breadth. | Shrink-note rows for the three items; no row targets the two phantom symbols; emit/backward keeps the CC-3 refusal. |
| E-L1-S5.6 | Zone-orphaned tests BIND to zone verdicts (scroll 800 / svg 807 / ingest 1027 / waapi 737 LOC); no orphaned green. External-consumer census is a STANDING discipline: no vacuity claim without all four trees + the wiring graph, at pinned HEADs. | Each zone-verdict row names its bound test block and its fate follows the verdict; every vacuity claim cites the four-tree census. |
| E-L1-S5.7 | Gates/tests: the "most tests are overfit nonsense" presumption is REFUTED on tree (~98% behavior-bearing; residue ~500 LOC). kf: LAND staged W9 quartet + prunes FIRST; wire the two enforcement-free structural guards (lint + proof:structure — two one-line CI steps); demote masked lighthouse; fold boundary-cohesion's clamp invariant into proof:structure before it dies; MR2/MR4 before restructure churn. value: library gates stay producer+api; e2e adjudicated per-oracle (~5.9k+~856 ABROGATE · ~1.7k FOLD · ~1.7k KEEP-EARNED incl. safari trio + driven a11y · ~1.2k journey rides ratified W55); build the isomorphism gate; pre-publish manifest gate; resurrect bench/ with R-PARSER. | No blanket test-abrogation wave; kf apparatus waves in the stated sequence (W9-land first; MR2/MR4 before churn); the two CI wires, lighthouse demotion, boundary-cohesion fold are explicit rows; value e2e adjudication numbers carried as per-oracle rows; journey subset attached to W55. |
| E-L1-S6.1 | Breaking changes allowed and PRICED (P4.2): the wedge is HARD (present-incompatible optional peer HARD-FAILS npm, ERESOLVE, proven twice); kf pins value EXACT 4.0.0. ONE coordinated co-land boundary (value 5 / kf 7 / glass peer-bump / active-atlas ranges / NON-optional framework peers in the chase ledger); until it, restructures stay internal behind frozen surfaces. NO stale-atlas catch-up wave (npm latest already 7.0.0). Deps block STRIPPED, not lockstep-bumped. | Exactly one co-land boundary wave names all five legs + chase-site ledgers in advance; every pre-boundary restructure is surface-frozen; no atlas catch-up wave; deps remedy is strip/relocate (no lockstep bump anywhere). |
| E-L1-S6.2 | Cross-repo: value DIRECTS kf library items as specs into kf's inbox (`docs/tranches/V/coordination/`); the kf successor implements; kf's demo/UI corpus + FOLD-FORWARD rows stay kf-owned. Direct cross-repo edits require an explicit OWNER GRANT. Glass-ui defect batching per §4; parse-that's standing PT-E ask letter is in the ingestion set (P5). | No wave edits kf/glass/parse-that trees without a recorded owner grant; kf-owned corpora absent from value's wave scopes; the PT-E letter is in the ingestion/registry inventory. |
| E-L1-S7.1 | Standing owner edicts, verbatim: NO quick solutions/workarounds — idiomatic gestalt; architectural transpositions for elegance/simplicity/performance desirable; NO legacy code/aliases/shims/dual paths/masking fallbacks; EXTREME parsimony + fastidious care — KISS-forward, fewer lines, judged against the greater picture; adhere to the wave spec EXACTLY; pithy/laconic/fastidious analysis; pruning granted IFF vacuity/superfluity PROVEN — consumer count NOT enough; chronics become DECIDED rows, re-booking FORBIDDEN; a chronic riding 2+ closes un-decided is a DISEASE ROW (deciding it is a wave of its own); recap ALL prompts — an unaddressed ask becomes a registry row WITH an owning wave; silent drops FORBIDDEN; every partial/banked/abandoned item gets a terminal disposition (folded into a named wave / banked with a named re-trigger / retired with rationale); counting a partial as done is the close-class lie, FORBIDDEN. **[Restated in E-C21.]** | Wave specs contain no shim/alias/dual-path/fallback; every prune carries proof beyond consumer count; every chronic is DECIDED (none re-booked); each 2+-close chronic has its own deciding wave; a prompt-recap exists where every ask row names an owning wave; every partial has one of the three terminal dispositions. |
| E-L1-S7.2 | Ratified law 1 — silent-drop tombstone law: no capability leaves a published surface without a by-name RIGHTLY/UNJUSTLY/UNCLEAR tombstone in the cutting release's CHANGELOG, MACHINE-CHECKED by a capability-diff gate (export-census diff vs prior tag; RED on any undeclared removal). Kills "no advocate ⇒ no tombstone". Owning repos: BOTH — value.js FIRST. | A capability-diff-gate wave exists for value.js (+ a kf spec dispatch); rule = export-census diff vs prior tag, red on undeclared removal; every planned removal pre-names its tombstone class. |
| E-L1-S7.3 | Ratified law 2 — G0′ tree pinning on every cross-repo claim. | Same test as E-L1-S0.3, applied tranche-wide as standing law. |
| E-L1-S7.4 | Ratified law 3 — re-derive anchors at execution: ratified blueprints' line-cites drift within days (N-ADJ-3); anchor re-derivation is a mechanical gate step. | Every wave consuming line-cites includes an anchor re-derivation gate step; no gate hard-codes a cached line number as its oracle. |
| E-L1-S7.5 | Ratified law 4 — phase labels: vision edicts ("majority on direct code implementation… visual verification") bind the FORMED tranche's implementation; the formation block binds THIS run. LABEL EVERY EDICT. | Every edict row is labeled formation-phase vs implementation-phase. |
| E-L1-S8.1 | Method — the thrice loop, all-Fable: the DAG spans library + component structures AND the constellation (kf, parse-that, atlas, glass-ui — every node/edge/cycle; DEPCRUISE-DERIVED then adjudicated, never hand-drawn); dynamic cluster batches, each viewed thrice: 2 fresh skeptics assuming the structure is WRONG + 1 adjudicator who PROVES/disproves with its own evidence (never vote-counts); iterate to perfection/readability/DIRIGIBILITY; convergence = two consecutive clean passes; ≤3 iterations per cluster then OWNER ESCALATION; ALL design routes through Fable + DesignSync; maximal parallelization. **[Restated + made per-wave/cluster/prototype by E-C20; full library+component DAGs mandated by E-C11.]** | DAG artifacts are depcruise-derived (tool output on record); each cluster shows a 2-skeptic+1-adjudicator record with adjudicator-owned evidence; convergence/escalation bookkeeping shows the 2-pass / ≤3-iteration rule; design outputs show DesignSync routing. |
| E-L1-S9.1 | RETURN CONTRACT: return only when the next tranche is fully formed — plan folder; wave specs with born-RED acceptance gates wherever the defect is live; π+DELTA obligations for every visual claim; a disposition for every chronic/deferred/prompt-recap row; the OWNER-DECISION docket as a SINGLE decision sheet. EVERY packet row (P1–P5) lands as a wave, gate, registry row, or owner-decision row — ZERO silent drops, CHECKED against the packets' row inventory. An inventory of problems without the resolving tranche is incomplete. Genuine blocker → return the strongest converged core + its exact gap. | A single owner-decision sheet exists; a packet-row inventory crosswalk maps every P1–P5 row to its landing with zero unmapped rows; every named problem has a resolving wave or an explicit blocker + exact gap. |

---

## PART 2 — THE CONTEXT PACKETS (E-P0 .. E-P6)

### P0 — Provenance

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P0.1 | The r1 corpus silently executed on Opus; owner ruled r1 presumptively incorrect; r2 = union-with-demarcation (fresh analysis BEFORE opening r1 counterparts; tags FABLE-NEW / UNION-CONFIRMED / OPUS-REFUTED / OPUS-UNVERIFIABLE). Standing law: a model declaration is not model execution — VERIFY the tier actually served. | Every agent/panel return records served-model evidence; no formation citation resolves to an r1/pre-r2 document. |
| E-P0.2 | The 18 OPUS-REFUTED rulings are DEAD (each named: optional-peer-warning, tape-inadmissible, lockstep-bump, atlas-catch-up, measured-byte-scanner framing; + 13 panel-2 zone flips incl. the double-gamut-loss refutation, the ~500-LOC e2e survivor replacement, boundary-cohesion fold-then-die, phantom compileToString/formatKeyframes rows). Pre-r2 letters (any PROVENANCE-HOLD copy) are DEAD — cite only this packet set. Excluded OPUS-UNVERIFIABLE (composite-SoA "OVERFIT", "47 recorded catches", stale-read attribution, CHALLENGE-2 existence) may not be cited as established. | No formation row rests on a refuted ruling (checkable per name); no citation to a PROVENANCE-HOLD letter; no OPUS-UNVERIFIABLE item asserted as fact. |
| E-P0.3 | The byte-canonical owner prompt + all FIVE addenda are mirrored at `keyframes-v-exec/.../vnext/OWNER-PROMPT-verbatim.md` + `OWNER-ADDENDUM-*`; nothing silently dropped. Addenda 1–4's challenges were EXECUTED (verdicts = P2/P3); addendum 5 (all-Fable, no Opus) is standing law. Where a presumption was tested and REFUTED (e2e totality, tests-overfit, double gamut loss), the refutation STANDS per the owner's own challenge instruction. | The provenance chain reaches the verbatim mirrors; no wave re-litigates an executed-and-refuted presumption; the refutation-stands rule is honored in the e2e/tests/gamut bands. |

### P1 — Parser archaeology

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P1.1 | The two-repo answer: kf-side exit RIGHTLY complete (4 dated stages; parser-less since 2026-04-17; NO unjust kf drop); value-side is the condemned event — v4 cut `164343c1` (release `7334c793`, +4,117/−24,330) deleted the combinator parser + all 11 benches + the `proof-perf-target` CI gate and grew the successor UNMEASURED in the same commit. | Archaeology carried as decided input; no wave re-investigates kf parser history; parser waves cite `164343c1` as the deletion event. |
| E-P1.2 | The extant parser per-file record (grammar.ts 483 / stylesheet.ts 899 / timeline.ts 124 / syntax.ts 101; `charCodeAt` across css/* = 0; no bench survives; baseline GREENFIELD — resurrect via `164343c1^`). The SpanParser V8 falsification (~10–14% slower) is REAL but parse-that-INTERNAL — never tested parse-that-vs-custom on the value grammar. | Parser-wave scope covers exactly the four parser files (not the non-parser css/ remainder or src-root files); bench baseline treated as greenfield; no row cites the SpanParser falsification against readoption. |
| E-P1.3 | Tape is OUT OF SCOPE: parse-that ships zero tape; everything tape-related is parse-that/bbnf-lang tranche territory; occupies NO formation context. RETIRED S.H3 Pratt row stays retired. | Zero formation rows mention tape work as in-scope; no Pratt revival row exists. |
| E-P1.4 | Boundary census: census-PASS everywhere except the ONE live easing.ts row (adjudicate consume-vs-ratify explicitly; plus a kf regex-census row for addendum 2). `scroll/grammar.ts` + `parseAnimationCSS.ts` verified thin pass-throughs. value /css ALREADY OWNS the keyframes-adjacent grammar (closed 8-code ParseIssue union; collectAnimationOptions stylesheet.ts:827; parseTimingFunction grammar.ts:436; kf seams compile/selector.ts:3, validate.ts:47). | The consume-vs-ratify adjudication row exists; a kf regex-census row exists (addendum-2 discharge); the split-parsing question is answered by the on-disk ownership record, not re-opened. |
| E-P1.5 | The ad-hoc-fix family (U-F29 `329932b8`, U-F30 `0c212e8d`, D3 passes `d82c63cd`/`184a9ec9`) was patched on the PRE-v4 tree and deleted with it; recurrence risk lives in the successor; the defect-family register must track FAMILIES, not instances. | The defect register contains a mixColors/parseCSSValue FAMILY row (not instance rows) bound to the restored parser's gates. |
| E-P1.6 | THE OWNER DECREE — parse-that readopted as published: npm latest 1.0.0 (S.H4 cut made FOR value.js; repo `master@ef10d5b`); zero-copy Span core; byte-scanner leaves live in main entry; SpanParser variant rightly excised at 1.0.0 (`043c4d1`); subpath surface `.`/`./core`/`./diagnostics`/`./packrat`/`./utils`; service record v1.0.0→v3.1.0 under O.W6 bench + `proof-perf-target`. Consequences: regex retired unconditionally; R-PARSER is a restoration, not a contest; internal work OUT OF SCOPE → PT-E letters. **[BOUNDED by E-C1/E-C19 (published-only, minor fixes only); paired with the E-C14 dual-definition prototype mandate — see E-P2.2-R-PARSER and E-C14.]** | Same tests as E-L1-S2.3/S2.4; the wave records the published-package facts (version, pin, subpaths) as its consumption contract. |

### P2 — Drops archaeology + THE RESTORE LEDGER

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P2.1 | The loss is SINGLE-SIDED: value v4 one-commit extinction (boundary.ts 604L + gamut.ts 526L + raytrace.ts 137L + okhsl.ts 270L, ΔE family grep=0 at HEAD, Into family, colorFilter, boundary samplers, sampleColorRamp/mixColorsN, 3.1.0 HDR surface ictcp()/jzazbz(), spring()/parseSpring grammar, measured parser+benches+gate). v4.0.0 CHANGELOG names ZERO of these. Named mechanism: no advocate ⇒ no tombstone. kf scar tissue to delete on restore: `compile/emit/backward/color.ts:120-124` hand-rolled ΔE; `backward.ts:30-32` stale docstrings. Extant `mapColorToGamut` not §13-conformant; `safeAccentColor` compounds (~67 dynamic evaluate calls upper-bound). | Restore waves cite the single-commit extinction; the tombstone-law wave names the v4 CHANGELOG silence as its motivating defect; a kf scar-deletion spec rides R-RAMP/R-DELTAE; the gamut wave's gate includes §13 conformance. |
| E-P2.2-R-DELTAE | P1: ΔE family — deltaEOK (+JND const), deltaE2000, deltaEITP — restored AS-WAS, typing modernized; PREREQUISITE of P2. Lands src/color/. | A wave restores the three ΔE functions + JND constant into src/color/; R-GAMUT declares it a prerequisite edge. |
| E-P2.2-R-GAMUT | P2: analytical Ottosson engine (cusp+Halley) + ΔE-OK JND clip criterion (§13-conformant) + raytrace exact-boundary as TEST-SIDE oracle; zero-alloc kernel under the frozen v4 facade. Lands src/color/ + test/. | A gamut wave with the §13-conformance gate, raytrace confined to test/, a zero-alloc assertion, and v4 facade signatures frozen. |
| E-P2.2-R-INTO | P3: Into family EXTENDED beyond the decided SCI-1 pair: convert/mix/gamut-map/ramp out-param paths incl. `mapColorToGamutInto`/`safeAccentColor`-class hot paths — RIDES the W56 4.1.x vehicle, NEVER forks it. Lands src/color/. | An Into wave attached to W56; scope names the out-param path families; no separate 4.1.x fork vehicle. |
| E-P2.2-R-PARSER | P4: DECIDED (owner decree P1.6): parse-that readopted as published — resurrect `164343c1^:src/parsing/` modernized to the v4 `/css` surface (signatures frozen); bench/ corpus + ratio gate restored as REGRESSION WITNESS (regex parser measured once for the record). Lands src/css/ + bench/ + CI. **[E-C14: the CSS parser is defined BOTH in idiomatic BBNF (bbnf module facilities, split sub-grammars) AND as the L4/Value-API parse-that impl in src/css/ — prototyped + benchmarked NOW (prototypes authorized; tranche EXECUTION not).]** | Restoration wave + born-RED bench gate + one-time regex measurement + frozen signatures; a dual BBNF-and-parse-that definition is prototyped and benchmarked (E-C14). |
| E-P2.2-R-RAMP | P5: sampleColorRamp/At/Into + mixColorsN restored; kf backward-emit re-adopts → deletes the P2.1 scars. Lands src/color/. | A ramp wave exists; a kf consume-spec dispatch whose completion deletes the two named kf scars. |
| E-P2.2-R-MIX-GRAMMAR | P6: `color-mix()` grammar + structural node (zero hits today). Lands src/css/grammar.ts. | A wave (on the restored parse-that grammar) adds color-mix() parsing with a gate exercising the structural node. |
| E-P2.2-R-RELATIVE | P7: relative color `from` syntax (hard-fails today). Lands src/css/grammar.ts. | A wave adds relative-color `from` support; its gate is born-RED against the current hard-fail. |
| E-P2.2-R-CONTRAST | P8: `contrast-color()` grammar + WCAG-metrics republish decision. Lands src/css/ + src/color/. | A wave adds contrast-color() grammar AND an owner/registry decision row for the WCAG-metrics republish. |
| E-P2.2-R-OKHSL | P9: OKHSL/OKHSV restored (reuse P2's cusp math). Lands src/color/. | An OKHSL/OKHSV wave depending on R-GAMUT's cusp math (no duplicate cusp implementation). |
| E-P2.2-R-HDR | P10: the 11-day HDR parse drop (ictcp()/jzazbz()) — OWNER DECIDES vs the CSS-native-only law. | An owner-decision docket row for HDR restore; no wave pre-empts the decision. |
| E-P2.2-R-SPRING-GRAMMAR | P11: CSS `spring()` grammar (existed pre-v4) — OPEN ownership decision: kf-owns-SOLVER settled (K F6.6); grammar value-vs-kf undecided, leaning value. | A decision row for spring() grammar ownership citing K F6.6; solver ownership not re-opened. |
| E-P2.2-R-EVAL | P12: calc/math static evaluator — WEAK candidate; spec-coverage mandate vs DOM-resolution status quo. | A decision row (not a build wave) weighing the two positions. |
| E-P2.2-R-SOA | P13: channel-fold SoA as a NEW primitive under the zero-alloc mandate (the 3.0.0 tombstone stands as written). Lands src/color/. | Any SoA row framed as a NEW primitive; the 3.0.0 tombstone not re-litigated. |
| E-P2.2-R-BOUNDARY | P14: gamut boundary samplers — GATED on the W53 perceived-space-plate rebuild's ACTUAL needs; built iff pulled. | A conditional row gated on W53's needs; no unconditional boundary-sampler wave. |
| E-P2.3 | Tombstones (RIGHTLY — NEVER re-litigate): OO/ValueUnit flip · kf 5.0.0 aliases · animate.ts zombie · loadEngine/loadCompiler/loadIngest accessors · flattenObject family (D28/D54) · cubicBezierToSVG · `timingFunctions` aggregate registry · BBNF grammar files · color-soa-as-3.0.0-decided · kf parsimmon parser + kf units/color (TRANSFERRED) · the U apparatus collapse (owner-ordered) · keyframes-vue (owner-killed). | No wave, restore row, or decision row proposes reviving any tombstoned item. |

### P3 — Zone dispositions + gates/tests program

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P3.0 | Method (standing discipline): verdicts rest on the four-tree consumer census (kf demo, value demo, glass-ui, atlas — exhaustive for every prune/demote-class call) + the internal wiring graph, at pinned HEADs. The r1 omission of the external census alone flipped five verdicts; three more flipped on the unchecked RECORD axis. | Every prune/demote-class row carries four-tree census evidence AND a record-axis check. |
| E-P3.1 | The kf zone table (canonical `0dac636b`) is adopted verbatim: KEEP-EARNED (engine, compile w/ emit-shrink, resolve, group, waapi, physics spring/decay/smooth/numeric/playback, orchestration sequence/stagger/view-transition/timeline, drag, morph-svg, presets w/ SHRINK, constants/validate/public/load-engine) · PRUNE-CANDIDATE (oscillator record-refuted + rider; split-text w/ named split-a11y-oracle casualty; motion-path) · OWNER-DECISION (physics/morph, flip, draw-svg, scroll w/ K-1 fence, ingest, internal/ restructure). Aggregates: prune ≈0.8k, owner-decision ≈2.4k. Record-state trichotomy + K F6 FENCE ANNEX (K-1 JS-sampler KEEP, no-polyfill law, ARCH-kill wall, spring-solver LEAD, OD-3): any V-next prune in scroll/timeline/physics must cite and overturn the named ruling. | The registry reproduces every zone verdict unchanged; split-text's row names the split-a11y-oracle casualty; the F6 annex is carried as a fence register; prune totals do not exceed the adopted aggregates without new census+record evidence. |
| E-P3.2 | The value zone table (`db77dbd8`) is adopted: color/ KEEP+RESTORE; css/ KEEP-surface REWRITE-parser (`collectDeclarations` orphan noted); value.ts/foundation/easing/transform-path KEEP (easing D13 halved-curve rider = restore-or-tombstone decision); decompose.ts PRUNE (or decided kf-adoption wave); quantize DEMOTE; subpaths/ DISSOLVE-ALLOWED keys frozen; api/ EXTRACT **[REVERSED by E-C7 → STAYS at /api]**; deps STRIP/RELOCATE + manifest gate (self-dep prior art v1.1.0–v2.0.1); 39 PNGs DELETE / demo tests DISPLACE (R14′ gate); e2e per E-union; demo KEEP-as-product tranche-restructure. Narrative headline: core SHRANK 5,020→4,647 (−7.4%); "explosion" ≈93% APPARATUS; genuinely-better is MEASURED (gamut 37→9 allocs; 37× lookup; HSL→RGB 3.8–4.4×; ~5× ramp fold). | Every value zone verdict lands as its stated row type (api/ as a STAY reconciliation per E-C7); the easing D13 rider is a restore-or-tombstone decision row; the collectDeclarations orphan is noted in the css/ wave; the measured-better numbers are carried as evidence, not re-derived. |
| E-P3.3-1 | kf program row 1: LAND W9 FIRST (staged-only on `v/w9-staging@b920b190`): MR1 pageerror-keying · MR2 wire the 5 skip-masked browser oracles · MR3 `require_demo_green` break-glass · MR4 `test:demo` in CI · the staged prune (~1,384L cruft + `proof:owner-golden`→`review:`). | A kf spec dispatch lands W9 from the named staging SHA, enumerated MR1–MR4 + prune, sequenced FIRST in the kf apparatus band. |
| E-P3.3-2 | kf row 2: WIRE the two enforcement-free structural guards — `lint` (depcruise) + `proof:structure` (run in NO workflow ever); two one-line CI steps, the cheapest real wire before a restructure tranche. | A spec row adds both CI steps; sequenced before restructure waves. |
| E-P3.3-3 | kf row 3: DEMOTE `audit:lighthouse` (masked `\|\| echo`, cannot fail). | A spec row demotes/unmasks the lighthouse step. |
| E-P3.3-4 | kf row 4: weekly-cadence relabel is PRE-BOOKED at DISPOSITIONS CH-05 — fold, don't double-book. | The relabel is folded into CH-05; no duplicate wave. |
| E-P3.3-5 | kf row 5: boundary-cohesion — FOLD its clamp-single-site invariant into proof:structure, THEN retire the file; FLAG the 3 partial readFileSync arms. | A fold-then-retire row exists with the readFileSync flag; no naked prune of boundary-cohesion. |
| E-P3.3-6 | kf row 6: BIND zone tests to zone verdicts; no orphaned green. | Each kf zone row names its test block; test fate follows verdict. |
| E-P3.3-7 | kf row 7: SEQUENCING LAW — MR2/MR4 land BEFORE the restructure's ~340-line import churn. | The dependency graph orders MR2/MR4 before any flatten/restructure wave. |
| E-P3.3-8 | kf row 8: excluded phantoms MUST NOT enter the tranche — the GS-04 known-violations doc-fix row (no baseline exists/wired) and the demo-Draggable "consumed" cell (filename-grep noise). | Neither phantom appears anywhere in the formed tranche. |
| E-P3.4-1 | value program row 1: library gate surface stays PRODUCER+API (the e2e fleet proves nothing about the packed library — 0/191 blocks import it; already CI-unwired at the v4 cut). | No formation row wires e2e into the library gate; library gates remain producer+api. |
| E-P3.4-2 | value row 2: the e2e adjudicated set — ABROGATE oracles/ design-census (~5.9k, 0 catches) + headless perf (~856) · FOLD ~1.7k (o1/o2→boot, browse→walk, mobile→viewport param, dual-pane→build-check, webgl→CH-7 annex + 1 context-loss probe, o16→glass producer surface) · KEEP-EARNED ~1.7k on VERIFIED catches (page-load, walk/admin-walk, color-space-switching, DRIVEN a11y battery w/ 2 born-RED WCAG catches, the safari trio) · plan-KEEP ~1.2k journey subset at the ratified W55 re-gate (which OWNS the .github edit); safari + real-GPU annex runs pre-deploy/nightly. | Per-oracle disposition rows match the four buckets + the named fold targets; the journey subset is bound to W55 (and only W55 touches .github); the safari/GPU annex is scheduled pre-deploy/nightly. |
| E-P3.4-3 | value row 3: BUILD the tests-isomorphism/structure gate from scratch (born-RED; none exists) + displace the demo-component tests. | A born-RED isomorphism-gate wave + a displacement row exist. |
| E-P3.4-4 | value row 4: pre-publish manifest gate (deps strip; self-dep prior art). | A manifest-gate wave whose gate rejects the deps block and self-dependency class. |
| E-P3.4-5 | value row 5: resurrect bench/ + the ratio perf gate WITH R-PARSER. | The bench wave is coupled to R-PARSER (same band/dependency). |
| E-P3.4-6 | value row 6: the capability-diff gate (P0/§7 law) — export-census diff vs prior tag; RED on any removal lacking a by-name tombstone. | Same as E-L1-S7.2: the gate wave exists with that exact rule. |

### P4 — Fences, wedge, flatten, governance

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P4.1-1 | kf fence: `TimingFunction = (t: number) => number` FROZEN at `src/animation/constants/types.ts:45`. Atlas chase census (ACTIVE tree, 9 imports/8 files): THREE TimingFunction sites (useCountUp.ts:47, useScrollTimeline.ts:44, useScrollLettering.ts:57; IN-ATLAS-5 is the correction event) + `MorphSVG` via `@mkbabb/keyframes.js/engine` (buildMarkAnimation.ts:7) — the `./engine` subpath HAS a live external consumer. | No kf-touching wave changes the TimingFunction type; the chase ledger pre-names the three atlas sites; no wave removes/renames `./engine` without the co-land boundary. |
| E-P4.1-2 | kf fence: exports exactly `.` + `./engine`; the 44-key engine runtime mirror RE-VERIFIED BY EXECUTION after any structural move. | Every kf structural-move spec includes a post-move 44-key mirror execution check. |
| E-P4.1-3 | kf fence: kf 6.0.0 / value 4.0.0 are IMMUTABLE; breaking work goes behind the P4.2 co-land boundary. | No wave republishes/mutates the released 6.0.0/4.0.0 artifacts; breaking rows sequenced behind the boundary wave. |
| E-P4.1-4 | kf fence: depcruise value.js-free-leaf law keyed `^src/animation/internal/` (`.dependency-cruiser.cjs:171`) — the key MOVES WITH any rename. | Any internal/ restructure spec updates the depcruise key in the same wave (re-derived at execution per law 3). |
| E-P4.1-5 | kf fence: `scenes/` exemplar fence; `docs/precepts/` read-only. | No wave writes to kf `docs/precepts/`; scenes/ changes respect the exemplar fence. |
| E-P4.2-1 | Wedge pricing (ERESOLVE-proven twice): a present-incompatible `peerOptional` HARD-FAILS npm install. glass 7 peers kf `^6` + value `^4` (optional) AND vue ^3.5 / tailwindcss ^4 / reka-ui ^2 / @lucide/vue ^1.16 (NON-optional — chase ledger). kf pins value EXACT `4.0.0`. ACTIVE atlas consumes the exact tuple. | The co-land boundary wave's pricing sheet lists every named peer edge incl. the non-optional framework peers; no wave ships a present-incompatible peer before the boundary. |
| E-P4.2-2 | The value deps bomb: working-tree `dependencies: {glass ^7, kf ^6}` (born `f2c8f565`; published 4.0.0 deps-free). If shipped: registry kf↔value cycle + dual-core forcing. Remedy: STRIP/RELOCATE + pre-publish manifest gate — NEVER lockstep-bump. | The deps wave strips/relocates (not bumps); the manifest gate blocks the class; no lockstep-bump row exists anywhere. |
| E-P4.2-3 | NO stale-atlas catch-up wave (npm latest already 7.0.0); sci-report crosses atomically at its own glass-7 consume (value W56 row). ONE co-land boundary for value 5 / kf 7 / glass peer-bump / atlas ranges; chase-site ledgers NAMED IN ADVANCE; internal-only restructures until then. | No atlas catch-up wave; exactly one boundary wave; chase ledgers enumerated in the boundary spec; all pre-boundary restructures declare frozen surfaces. |
| E-P4.3 | The flatten census is born-RED at every anchor — 13 config/gate anchors (tsconfig self-alias :30 + `@src/*` · vite :41/:156/:172/:225/:229 · vitest :18 · depcruise :84/:93/:171 · structure-gate resolver index.mjs:91 · engine-dts-rollup :34/:64 · surface gates 15 grep sites) + ≈340 import lines (test 277 + bench 37 + scripts 18 + demo ~9). Post-move: 44-key mirror re-execution + `proof:structure --selftest` + full depcruise run. Anchors RE-DERIVED at execution (N-ADJ-3: LT-10's cite already drifted :168→:171). | The flatten wave enumerates all 13 anchors born-RED, carries the import-line census, mandates the three post-move verifications, and contains an anchor re-derivation step. |
| E-P4.4 | Structure governance (G5′): kf structure was JUST settled (proof:structure R1–R6, 755L, live). Structure waves AMEND the ratified LT blueprint + EXTEND the gate — never a parallel authority — naming superseded rulings (LT-10: kept internal/ against a 40+-importer edit, the owner's new edict supersedes the taste ruling but the cost census STANDS; LT-16). Refutation amends the charter; silence re-litigates it. | Each structure wave names its LT amendment target; proof:structure is extended, not replaced; the LT-10 cost census is cited, not re-derived. |
| E-P4.5 | Ownership protocol: this tranche DIRECTS kf library items as SPECS + bounded dispatches into `keyframes.js/docs/tranches/V/coordination/` (grammar: `<SENDER>-INBOUND-*`); the kf successor implements; kf demo/UI corpus + FOLD-FORWARD rows stay kf-owned; direct cross-repo edits require an EXPLICIT OWNER GRANT. | Every kf deliverable is an INBOUND-grammar dispatch file; no value wave scopes kf demo/UI or FOLD-FORWARD items; any cross-repo edit row cites its owner grant. |

### P5 — The prior-record index (inherit as DECIDED; never re-derive)

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P5.1 | SCI-1 = DECIDED SHIP-4.1.x (value DECISIONS.md:82 D54; vehicle W56 — mixColorsInto/toRgba8Into). R-INTO extends it; NOTHING re-opens it. | W56 exists as the 4.1.x vehicle; R-INTO attaches to it; no row re-adjudicates SCI-1. |
| E-P5.2 | D-GAP-6 sampleBezier: DECLINED, accepted by kf (adopts only if a future 4.1 ships it). §D name-a-code re-open path STANDS (a named ParseIssue code re-opens the confirm-shipped verdict). RF-18 census-split ENACTED. | No wave ships sampleBezier absent a 4.1 decision; the §D re-open trigger is a registry row; RF-18 treated as enacted (not re-proposed). |
| E-P5.3 | PT-E (`parse-that/docs/tranches/A/VALUEJS-PT-E-2026-07-05.md`): the standing parse-that→value ask letter (diagnostics HIGH · inference MED · Pratt-dormant) — in the ingestion set. | The PT-E letter is rowed with a disposition per ask (diagnostics/inference/Pratt). |
| E-P5.4 | kf R2-07 + W9: the gate/test prune blueprint is ADJUDICATED; execution STAGED-ONLY on `v/w9-staging@b920b190` — landing it is P3.3 row 1. CH-05 pre-books the weekly-relabel fold. | Same as E-P3.3-1/-4: land-W9 dispatch exists; CH-05 fold honored. |
| E-P5.5 | EP-3 PATH-B decided-terminal rows: flip/flipShared, drag/Draggable, DrawSVG/fromDrawSVG (drag's vacuity premise now DEFEATED by glass consumption; the disclosure row stays booked). K.W8/K.W9 book rows: ingest + scroll feature theses. The K F6 register is the standing fence annex. | The owner-decision rows for flip/draw-svg cite EP-3 PATH-B; drag is KEEP-EARNED with the disclosure row still booked; ingest/scroll decisions cite their book rows; the F6 register is carried whole. |
| E-P5.6 | kf FOLD-FORWARD (`docs/tranches/V/FOLD-FORWARD.md`): W7/W8 fold whole, W9 staged, W10 pre-rail subset landed, W11 folds, W12 substantially discharged, W13 close-ceremony; the 15-row marks register (glass §7 defects with live kf exposure); owner-checkout reconciliation safe+pending. | The formation treats these fold states as decided input; the 15-row marks register is referenced where glass §7 defects appear; nothing re-derives the fold states. |
| E-P5.7 | value vehicles: W53 (perceived-space plate rebuild — gates R-BOUNDARY), W55 (journey-subset re-gate — OWNS the .github edit), W56 (SCI-1 4.1.x cut). V-PRIME B3/B4 as recorded. | W53/W55/W56 exist as vehicles with their stated ownerships; R-BOUNDARY gates on W53; only W55 touches .github; V-PRIME B3/B4 carried as recorded. |
| E-P5.8 | Oscillator doc-truth defect row: published-surface.md:44 names two consumers FALSE on disk; `proof:publish` checks coverage-file existence, not consumer claims — a NEW gate rule candidate. | A defect row for the false consumer claims + a gate-rule-candidate row for consumer-claim verification in proof:publish. |

### P6 — Ingestion set + first actions

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-P6.1 | Read before the first spawn (pin every tree per G0′): (1) the refined kickoff prompt + the packet set; (2) the r2 panel corpus (adjudication-panel1/2-r2, skeptic-{A,B,H1,H2,D,E,K,F}-r2, mirrored at keyframes-v-exec …/vnext/); (3) kf: FOLD-FORWARD, PROGRESS, OWNER-DECISIONS, INBOUND-LEDGER, R2-05 LT rows, published-surface.md, EP-3, F6 rulings; (4) value: DECISIONS.md (D50/D54), CARRY-LEDGER.md, V-PRIME.md, WL letters, DISPOSITION-LEDGER.md; (5) PT-E, the Q060 glass packet, glass §7 defect rows (kf marks register); (6) archaeology grants read-only: the four git histories; `164343c1^` for every resurrection recipe. | The provenance/ingestion record lists all six ingestion classes as read, with G0′ pins recorded before the first spawn. |
| E-P6.2 | First actions, in order: row the two letters in the E13 inbox ledger → verify the r2 mirror trees → open the formation registry from P1–P5's row inventory (EVERY row lands as a wave, gate, registry row, or owner-decision row — ZERO silent drops) → dispatch the first thrice batch. | INBOX.md rows the two letters; an r2-mirror verification record exists; the registry's seed inventory is the P1–P5 row set with a complete landing crosswalk; the first dispatch is a thrice batch. |

---

## PART 3 — THE CONVERSATION ADDENDA (E-C1 .. E-C23) — from `CONVERSATION-ADDENDA.md`; these WIN on their named axis

> IDs are keyed to the addenda's own `C#` labels. `E-C9` and `E-C15` are **referenced within the addenda but never defined as standalone rows** — they are cross-references (C9 = the thrice method; C15 = the parse-that semver bound), reproduced here for completeness. This section fully REPLACES the draft's PART 3, which was mis-sourced from `OWNER-AMENDMENTS.md` (see Findings).

| ID | Edict digest | COMPLIANCE TEST |
|---|---|---|
| E-C1 | Parse-that scope BOUND (owner, final message): parse-that is *leveraged* for the full CSS prototype AS PUBLISHED; it is **NOT uplifted with features of novelty** in this tranche — minor fixes only if needed. A separate major BBNF session owns ALL parse-that/bbnf-lang evolution. Confirms/tightens P1.6/P1.3. Semver: parse-that may never publish >1.1 regardless of change level (via E-C15). | No wave adds novelty/features to parse-that; parse-that usage is the published artifact + at most minor fixes; all evolution routes to the separate BBNF session; no parse-that publish targets >1.1. |
| E-C2 | Easing curve selector on mobile (value demo): dramatic REFINEMENT and RE-DESIGN — currently far too rounded, too large, low-res. | A named wave redesigns the mobile easing curve selector with acceptance criteria replacing the too-rounded/too-large/low-res state (dense/high-res, appropriately sized). |
| E-C3 | value.js mobile toolbar: the tools bar is LOST on mobile — restore/redesign. | A named wave restores a reachable mobile tools bar in the value demo; its gate asserts mobile tools reachability. |
| E-C4 | keyframes.js demo mobile: mostly broken on mobile; deep refinement. The Cube must fully support multi-touch with the custom quaternion orbital-drag facilities — as should every other animation. The kf demo becomes a proper single-screen application (like value.js), optimized for desktop affordances too, taking deep inspiration from value.js + glass-ui. | kf-demo waves cover mobile repair; the Cube (and every applicable animation) supports multi-touch quaternion orbital-drag with gated pointer behavior; the kf demo is specified as a single-screen app with desktop affordances, referencing value.js + glass-ui. |
| E-C5 | shadcn abrogation, TOTAL, both apps: idiomatic glass-ui + Tailwind v4 usage only, with proper colocation. Where a shadcn component has no true glass-ui equivalent: mark it, prototype/design one using glass-ui idioms, and spec it as a wave (IN glass-ui). | A shadcn-abrogation wave leaves zero shadcn components in both apps; missing primitives are marked + specced as glass-ui waves (glass-owned); colocation is applied. |
| E-C6 | Routing + state: value.js routing properly defined alongside state management; SAME for keyframes.js. Both demos get robust URL state management + share-URL facilities. | Both demos have routing + state-management waves and robust canonical URL + share-URL facilities specified. |
| E-C7 | Palette API STAYS at its current `/api` location. Full specification of EVERY facility — history, variants, all CRUD, mixing — proven and completely addressed. Deep scrupulous inspection of the /api module structure in BOTH repos, plus proper API design + isomorphism to fourier-analysis' CRUD-visualization API. **Explicit tension with P3.2's "api/ EXTRACT from repo"** — the later ruling is "stay within current /api"; any extract/stay decision must reconcile these explicitly. | An API band specs history/variants/all-CRUD/mixing; /api module structure inspected in both repos; a fourier-analysis CRUD-viz isomorphism target is named; the api/ stay-vs-extract tension is reconciled explicitly (no standalone extraction). |
| E-C8 | Scale: this is a MEGA-tranche — at least 50+ waves spanning, at the least, value.js, keyframes.js, parse-that, fourier-analysis, glass-ui, and likely sci-report/atlas. | The wave registry counts ≥50 waves and spans the named surfaces (value, kf, parse-that, fourier-analysis, glass-ui, + likely sci-report/atlas). |
| E-C9 | *(Referenced, not separately defined — cross-ref in E-C11.)* The thrice method = the E-C20 loop (2 Fable skeptics + 1 Fable adjudicator). | Wherever "C9/the thrice method" is invoked, the E-C20 loop structure is what is applied. |
| E-C10 | THE BREATH OF LIFE: read glass-ui's BREATH OF LIFE corpus (tranches **BJ + IOS27-MICRO**) and suffuse it into the value demo's animations/details — how watercolor dots animate/change on hover and procedurally beget; slider-area value styling with little watercolor-dot backgrounds; padding, margins, animation curves scrupulously refined on EVERY screen (both demos). The extant value UI is quite good — this REFINES details, errors, curves; the little things. | A BREATH-OF-LIFE ingestion+suffusion wave cites the BJ + IOS27-MICRO corpus; watercolor-dot hover/beget + slider-area styling + per-screen curve/padding refinement are specified on both demos; value's identity is refined, not replaced. |
| E-C11 | Structure: tests NEVER co-located — displaced into a tree isomorphic to source; grouped files strip the module name (`easing/easing-option.ts` → `easing/option.ts`); goldilocks granularity (no god-modules, no sand); long-running dirs pruned or agglomerated into logical sub-modules; glass-ui is the reference model. Full DAGs of library AND component structures, ruthlessly analyzed — every node/edge/cycle — in dynamic bespoke cluster batches via the thrice method (E-C9/E-C20). | Structure waves encode: isomorphic displaced tests, module-name stripping, goldilocks granularity, dir pruning/agglomeration, glass-ui reference model, and full depcruise-derived DAGs (library + component) analyzed via the thrice loop. |
| E-C12 | Full spec coverage: the ENTIRE CSS spec incl. experimental as of July 2026, CSSOM, WAAPI, keyframes, animation timeline, animation, stylesheet parsers — FULLY typed values congruent/aligned exactly to their DOM equivalents. kf gains full WAAPI support; value gains full keyframes/animation parsing that kf leverages. | The spec-coverage census (E-L1-S2.6) targets the full July-2026 CSS spec + CSSOM + WAAPI + keyframes/animation/timeline/stylesheet with DOM-congruent typed values; a kf-WAAPI spec dispatch exists; value's keyframes/animation parsing is specced with kf as the leveraging consumer. |
| E-C13 | Color: full color-space support with robust zero-alloc functions for EVERY critical operation (demo, consumers, etc.). | The color band's zero-alloc coverage spans every critical operation across demo + consumers (not only the R-INTO hot paths). |
| E-C14 | Dual parser definition: the CSS parser is defined BOTH in idiomatic BBNF (using the bbnf repo's module facilities to split sub-grammars) AND as the fully-defined L4 / Value-API parse-that implementation in `src/css/` — idiomatic, functional, TS-idiomatic, leveraging the full parse-that facility suite. Prototyped and benchmarked NOW (prototypes authorized; tranche EXECUTION is not). | R-PARSER's spec carries BOTH a BBNF definition (bbnf module facilities, split sub-grammars) AND a parse-that L4 impl in src/css/; a prototype + benchmark is produced in THIS formation (execution deferred to the tranche). |
| E-C15 | *(Referenced, not separately defined — cross-ref in E-C1.)* Semver bound: parse-that may never publish >1.1 regardless of change level. | No parse-that release plan exceeds 1.1; the E-C1 bound cites this ceiling. |
| E-C16 | Isolation: a GPT-5.6 **Sol Codex fleet** is ACTIVELY forming the tranche in `value.js/docs/tranches/V/vnext/` — that tree is READ-ONLY for this program; no clobbering; all our work is isolated (scratchpad + a separate landing dir). `scripts/dev/dev.sh` is an unowned dirty row — NEVER touch. `r1-opus-refuted/` is never loaded. | No write lands in `docs/tranches/V/vnext/` or `scripts/dev/dev.sh`; all program output is in the isolated scratchpad/landing dir; no `r1-opus-refuted` path is read. |
| E-C17 | Model routing (THIS run): **Fable** for design, brainstorming, adjudication, novelty, and all deep frontend design; **Opus** for mechanical sweeps/inventory/extraction. Fable cost is outrageous — every Fable seat is file-scoped and tight. Every seat reports the model id actually serving it (`model_served`) — a declaration is not execution (P0.1 law). | Every seat declares its role-appropriate model (Fable for design/critique/adjudication/novelty/frontend; Opus for mechanical/inventory/extraction) and reports `model_served`; Fable seats are file-scoped/tight; no seat's served tier contradicts its role. |
| E-C18 | kf session backtrace: audit the most recent claude-code keyframes.js session (its CHAT history/transcripts at `~/.claude/projects/-Users-mkbabb-Programming-keyframes-js/`) for the owner's exhortations + findings — verify the two seed letters faithfully carry them; any ask that never made it into the letters/packets is a SILENT DROP finding. | A backtrace-audit deliverable exists reconciling the kf-session owner exhortations against the two letters; any un-carried ask is recorded as a named silent-drop finding. |
| E-C19 | parse-that up-to-spec assessment: what of its most recent tranches + library development? Is the published 1.0.0 + repo HEAD adequate for the CSS parser program? Minor-fix candidates only. | An adequacy assessment of parse-that 1.0.0 + HEAD for the CSS parser program exists; its output is a minor-fix candidate list only (no novelty/uplift proposals as live waves). |
| E-C20 | The thrice method (EVERY wave/cluster/prototype): two Fable skeptics who assume the artifact is WRONG outright and propose alternatives; one Fable adjudicator who agglomerates + proves/disproves with its OWN evidence. Iterate until perfection, readability, DIRIGIBILITY; convergence = two consecutive clean passes. | Every wave/cluster/prototype record shows a 2-Fable-skeptic (assume-wrong + alternatives) + 1-Fable-adjudicator (own-evidence) triad; convergence bookkeeping shows two consecutive clean passes. |
| E-C21 | KISS/parsimony: more code and complexity is likely NOT better — pithy, laconic, fastidious. Little time on contrived gates or process; the MAJORITY on direct code implementation (via agent orchestration) + VISUAL verification. Probe parsimony: parsimonious playwright/DevTools; fastidious design+code analysis. | Wave specs favor fewer lines/lower complexity; process/gate overhead is minimized; the plan allocates the majority of effort to direct implementation + visual verification; probe usage is parsimonious with fastidious analysis. |
| E-C22 | Frontend audit totality: the frontend audit goes against EVERY page of EVERY app of value.js AND keyframes.js — mobile AND desktop; fully brainstormed affordances to add, refine, or remove. | The demo bands enumerate every route × {mobile, desktop} of BOTH apps, each with brainstormed add/refine/remove affordance markings. |
| E-C23 | The union mandate: audit the Codex-begotten waves against the original edicts; perform an INDEPENDENT Fable-side mega-tranche formation; UNION the two — an apotheosis for each: what was done correctly, wrongly, partially. | Three deliverables exist: (a) a Codex-waves-vs-edicts audit; (b) an independent Fable-side formation; (c) a union/apotheosis marking each item correct/wrong/partial. |

---

## FINDINGS — divergences between the two post-letter owner captures

Two independent captures of the owner's post-seed voice exist: **CONVERSATION-ADDENDA.md** (the binding canon for THIS program) and the Codex fleet's **OWNER-AMENDMENTS.md** (snapshot-vnext). They were addressed to different fleets and diverge materially. The binding capture governs; the Codex-only material is recorded here and is NOT folded into the matrix rows above.

- **F1 — Model naming (hard divergence).** CONVERSATION `C17` = **Fable** (design/brainstorm/adjudication/novelty/frontend) + **Opus** (mechanical sweeps/inventory/extraction) — the Claude tiers for our program. OWNER `OA-01` = **replace Fable→Sol, Opus→Luna** (GPT-5.6 tiers for the Codex fleet). The prior draft applied Sol/Luna to the letter rows; that is the Codex fleet's routing, not ours. Binding: Fable/Opus. (Sub-point: `C17` also *permits* Opus for mechanical work in THIS run, where L1 §0 had confined Opus to a later ratified phase — `C17` is the governing clarification on that axis.)

- **F2 — C-number semantics collide (the root cause of the draft's error).** The two docs reuse overlapping index numbers for DIFFERENT content: CONVERSATION `C2` = mobile easing selector vs OWNER `OA-02` = frontend totality; CONVERSATION `C14` = dual BBNF/parse-that parser vs OWNER `OA-14` = deletion-judgment/key-removal; CONVERSATION `C15` = parse-that semver bound vs OWNER `OA-15` = per-wave hostile triad. The draft's `E-C1..E-C23` were built off the OA rows and are therefore mis-attributed. PART 3 above is rebuilt from CONVERSATION-ADDENDA.

- **F3 — Deletion-judgment / export-key removal is Codex-only.** OWNER `OA-14` + the "Deletion judgment" section (four falsifiable questions; explicit permission to remove `./value`/`./quantize` keys) has **no counterpart** in CONVERSATION-ADDENDA. Our binding canon does NOT loosen the seed's 7-key freeze. The draft imported this loosening into E-L1-S4.4 — corrected to hold the freeze. (The four-question deletion protocol is a sound discipline but is not owner-mandated for our program.)

- **F4 — Parse-that strictness differs.** CONVERSATION `C1`/`C19` permit "minor fixes only if needed" and `C15` bounds publishing at ≤1.1 (implying some change is publishable). OWNER `OA-17` + the boundary section mandate `unchanged-1.0.0-no-republish` — ZERO source mutation, no republish. The Codex capture is strictly tighter than our canon on the fix-vs-no-fix axis. Binding: published-only + minor fixes if needed.

- **F5 — The BBNF isomorphism machinery is Codex-only.** OWNER `OA-13`/`OA-16` specify heavy apparatus (SK-V25 pin, `CSS-MODULE-ISOMORPHISM.json`, a 15-runtime+1-excluded partition, a P00 byte-equal freeze epoch). CONVERSATION `C14` only asks for a dual definition (BBNF via bbnf module facilities + parse-that L4 in src/css/) prototyped+benchmarked NOW. The elaborate topology/freeze machinery has no basis in our binding canon.

- **F6 — Our program's meta-asks are absent from the Codex docket.** CONVERSATION `C16` (isolation from the Codex fleet), `C17` (Fable/Opus routing), `C18` (kf-session backtrace / silent-drop audit), `C19` (parse-that up-to-spec assessment), and `C23` (the union/apotheosis mandate — audit the Codex waves, run an independent Fable formation, union them) do not appear in OWNER-AMENDMENTS. Expected: a fleet's own docket cannot self-mandate its adversarial audit. These define OUR arms and must be carried whether or not the Codex tree records them.

- **F7 — Wave-count shape.** CONVERSATION `C8` = "at least 50+ waves" across value/kf/parse-that/fourier/glass/(likely sci-report,atlas). OWNER `OA-08` = the realized "190 waves across eight bands." Consistent as a floor (190 ≥ 50), but the 190/eight-band shape is a Codex realization, NOT an owner-mandated structure — do not treat it as a target for the independent Fable formation.

- **F8 — Codex self-config is inapplicable.** OWNER `OA-09` (`gpt-5.6-sol`, `reasoning_effort=ultra`, `approval_policy=never`, `sandbox=danger-full-access`) is the Codex fleet's own tier config; it has no bearing on our Fable/Opus program and must not be adopted.

- **F9 — api/ disposition: convergent conclusion, divergent framing.** Both captures land the palette API STAYING (OWNER `OA-05` internal transpose with named A00–A20 waves; CONVERSATION `C7` "stays at /api"). But `C7` explicitly flags the UNRESOLVED tension with P3.2's "EXTRACT" and adds the fourier-analysis CRUD-viz **isomorphism** mandate, whereas `OA-05` states the reversal as settled and omits the isomorphism ask. Binding: stay + reconcile the P3.2 tension explicitly + fourier isomorphism.

- **F10 — Named enrichments present only in CONVERSATION.** `C10` (the glass-ui **BREATH OF LIFE** corpus — BJ + IOS27-MICRO — watercolor-dot suffusion) and `C12` (full July-2026 CSS spec incl. experimental + CSSOM + WAAPI, DOM-congruent typing; kf-gains-full-WAAPI) are specific, load-bearing owner asks with no explicit counterpart in OWNER-AMENDMENTS. They must be carried as waves even though the Codex docket omits them.

---

END OF MATRIX. **38 charter rows** (E-L1-S0.1..S9.1) · **64 packet rows** (E-P0.1..E-P6.2, incl. all 14 P2.2 restore-ledger rows R-DELTAE..R-BOUNDARY, all 8 P3.3 + 6 P3.4 program rows, all 12 P4 fences/wedge/governance rows, all 8 P5 inherited-DECIDED rows) · **23 addenda rows** (E-C1..E-C23, keyed to CONVERSATION-ADDENDA's own C# labels; E-C9/E-C15 are documented cross-references) · **10 findings** (F1..F10, OWNER-AMENDMENTS vs CONVERSATION-ADDENDA divergences). Total 125 edict rows + 10 findings.
