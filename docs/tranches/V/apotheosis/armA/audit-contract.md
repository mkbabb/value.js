# Arm A — Audit Contract: RETURN-CONTRACT + PROCESS COMPLIANCE

**Lens:** return-contract soundness + process compliance — charter §§0/8/9 (E-L1-S0.*, S8.1, S9.1) and addenda E-C8 (scale), E-C9/E-C20 (thrice method).
**Subject:** frozen snapshot `…/apotheosis/snapshot-vnext` (frozen 2026-07-19 21:29), read against the binding triad (L1 charter / P packets / CONVERSATION-ADDENDA) and the edict matrix.
**Grading legend:** verdict (CORRECT / PARTIAL / WRONG / MISSING) · evidence (file + anchor/quote) · severity (P0 charter-breach / P1 material / P2 minor) · edict-matrix row tested.
**Model serving this seat:** `claude-opus-4-8[1m]` (Opus 4.8, 1M context) — reported per E-C17 `model_served` law (Opus-class; this is a mechanical inventory/extraction seat, correctly routed).

**Fairness posture:** this arm grades what the Codex formation did CORRECTLY with the same rigor as the wrongs. The dominant finding is that the corpus is a genuinely rigorous, near-complete formation whose defects are almost all **honestly self-reported**, not concealed. The one structural gap (convergence) is the corpus's own headline, not a lie.

---

## PART I — WHAT THE FORMATION DID CORRECTLY

### A1 — Wave-spec 7-cell completeness (born-RED where defect live)
**Verdict: CORRECT. Severity: n/a (compliant). Edict: E-L1-S9.1, FORMATION-completion cond 1.**
Sampled **>12 waves end-to-end across every band** in `waves/*.md`: P00, P01, V00A, V03, V15P, V24, V24P, K00, K12, K21, A05, A22S, G05, G07, D03A, D25, M08, M10T, C00U, C05, C08, C10. **Every one carries all seven cells** (ID · Mission · Dependencies · Born-RED witness · Deliverables · Falsifiable gates · Exclusions/terminal branch) with a **concrete, live** born-RED witness — not a stub:
- `waves/P-V.md` V24P: "`M0 0 L10` leaked NaN; garbage and invalid arc flags accepted; recursive subdivision permitted explosive work."
- `waves/G-D.md` D03A: "Generate/Gradient/Mix commands optional-chain refs populated only by desktop mounts; mobile Tools can be visible yet empty/no-op" (directly discharges E-C3 lost-toolbar).
- `waves/M-C.md` M08: "At 390×844 the family rail reaches about 593 px and clips after Expo; filters disappear; copy is about 16 px" (discharges E-C2 mobile easing).
Dependencies are **literal comma-separated wave IDs** everywhere (M-C.md header: "A range, prose condition or implicit owner is invalid"). Band-owner is the durable ID prefix (FORMATION.md §Ownership). `tools/validate-formation.mjs` is claimed to enforce cell count + band counts {P8,V44,K27,A36,G10,D33,M13,C19}. Rows-per-file verified 52+63+43+32 = **190**. FORMATION.md claim that "all 190 waves have mission/owner/deps/born-RED/deliverables/gates/exclusions" is **substantiated by sampling**.

### A2 — Wave count vs the ≥50 mega-tranche mandate
**Verdict: CORRECT. Severity: n/a. Edict: E-C8.**
**190 spec'd wave rows** (counted, not merely tabled) spanning value.js, keyframes.js, parse-that (P band, consume-only), fourier-analysis (A/C bands), glass-ui (G band), atlas + sci-report + 6 further consumers (C band). Far exceeds the ≥50 floor and covers every named surface. *Caveat (F7):* the 190/8-band shape is a **Codex realization**, not an owner-mandated structure — sound as a floor, not adoptable as a target for the independent Fable formation.

### A3 — The thrice method ACTUALLY RAN (formation grain) — real skeptic content, not theater
**Verdict: CORRECT. Severity: n/a. Edict: E-C20 / E-L1-S8.1 / E-C9.**
`reviews/V-ADJUDICATION.md` read in full: **two independent hostile Sol skeptics both REFUTE** (Gödel/`v_skeptic_a`, Volta/`v_skeptic_b`) with **15 finding families (V-F01..V-F15), each carrying a runnable falsifier + spec citation** (e.g. V-F05 cites CSS Color 4 §14.2 permitting Local-MINDE/EdgeSeeker/Ray Trace; V-F12 the `M0 0 L10` NaN leak; V-F13 a `RangeError` on deep nesting). The adjudicator (Goodall) produced a **material 33→44-row amendment** AND caught a real SCC (`P07 → V03 → … → V29 → P07`) with an explicit acyclic correction — evidence of independent adjudicator reasoning, not vote-counting. A **rejected-proposals table with proofs** ("Keep decomposition because 609 lines exist → Rejected") demonstrates genuine adversarial pressure. The same 2-skeptic+1-adjudicator pattern is ledgered across P/K/A/GD/MC/X bands (AUDIT-REGISTRY seats 12–32, all "REFUTE"/"NOT CLEAN"). **This is substantive; it is not theater.** The per-wave EXECUTION-grain triad (OA-15) is contractualized in RETURN-CONTRACT + FORMATION and correctly **not yet run** (production held 0/190).

### A4 — RETURN-CONTRACT rigor
**Verdict: CORRECT (exemplary). Severity: n/a. Edict: E-L1-S9.1.**
`RETURN-CONTRACT.md` + `return.schema.json`: canonical `vnext-wave-return/2`, **RFC-8785/JCS self-hash** (`return_hash` omitted from preimage), six typed statuses (COMPLETE|KEEP|PRUNE|REFUSED|BLOCKED|NOT_CLEAN), **typed-absence law** ("`null`, missing required members, prose sentinels and implicit N/A are forbidden"), an **8-cell standards operation vector** (parse/type/evaluate/resolve/adapt/serialize.{exact,edit,canonical}) each an enum of implemented/delegated/preserved_syntax_only/refused/not_applicable, **gate-receipt v3 sealing the implementation-challenge hash**, required `terminal_disposition` + `routed_remainder`. Bands add only under `annexes.<ns>` and "cannot redefine status or a universal field." V-F15 shows the two prior competing return schemas were unified onto this one. Directly and fully satisfies the return-contract edict.

### A5 — π/DELTA obligation on every visual claim
**Verdict: CORRECT. Severity: n/a. Edict: E-L1-S9.1, FORMATION standing law.**
FORMATION.md standing law: "**Every visual claim carries π/DELTA evidence.**" `waves/G-D.md` §"Shared π contract" enumerates the exact matrix (desktop 1440×900 / tablet 768×1024 / mobile 390×844 / compact 320×568 · light+dark · 200%+400% zoom · mouse/touch/keyboard/AT · default+PRM · Chromium/Firefox/Safari); each feature wave "captures its own born-RED visual and interaction evidence" and **"cannot defer its first responsive/a11y/animation-energy/route-state proof to D25."** D25/M11/C07 audit-only. Load-bearing and concrete, not decorative.

### A6 — Terminal disposition for every chronic/deferred/prompt-recap row; no re-booking; no partial-counted-done
**Verdict: CORRECT. Severity: n/a. Edict: E-L1-S7.1, E-P6.2.**
`PROMPT-RECAP.md` = **83-row request→owning-waves crosswalk**; `SEED-ROW-INVENTORY.json` = **149-row bijection** (folded 140 / retired 6 / banked 3); `DISPOSITIONS.md` = **96 binding keep/prune/replace/refuse rows** with owner + consequence. **Production is 0/190 → nothing is "counted done"**, so the "partial counted as done" close-lie is structurally impossible at formation stage. Every conditional facility **ends in terminal KEEP or complete PRUNE** (verified in rows V15P/V16B/V18H/V18V/V24, K14–K22), never report-only (`DISPOSITIONS.md`: "Report-only facility disposition → **Reject**"; "Closure wave repairing defects → **Reject**"). The named owner defects E-C2/C3/C4/C5/C6/C7/C10 all appear as PROMPT-RECAP rows with owning waves (mobile easing→M08/M11/C07; lost toolbar→D00A/D03A; Cube quaternion→K12/M05; BREATH→G05/D03–D24/D25/M03–M10; shadcn→G00–G09/D00/M00/C00; URL state→D01/D02, M01/M02; palette API stays→A00–A20). **No re-booked chronic observed.**

### A7 — Owner-decision docket exists as a single sheet
**Verdict: CORRECT (with a P2 caveat). Severity: P2. Edict: E-L1-S9.1.**
`OWNER-AMENDMENTS.md` self-declares "**This is the single chronological docket required by the seed**" — OA-01..OA-17 + a 4-question "Deletion judgment" + the parse-that/BBNF boundary. Every row uses the required grammar (ID · chronology · earlier position · later ruling · binding consequence). **P2 caveat:** owner decisions are split across two surfaces — `OWNER-AMENDMENTS.md` (chronological rulings) and `DISPOSITIONS.md` (binding per-surface dispositions) — rather than one merged decision sheet; each has a distinct role, so the single-docket edict is met but not maximally consolidated.

### A8 — G0′ tree pins on cross-repo claims
**Verdict: CORRECT. Severity: P2 (pin-currency note). Edict: E-L1-S0.3 / S7.3.**
`PROVENANCE.md` §"Repository register" = **18-repository pin table**: branch + exact HEAD SHA + NUL-porcelain `git status --porcelain=v1 -z` dirty digest at `2026-07-18T16:50:04-0400`; stale standalone atlas explicitly flagged EXCLUDED; each consumer wave "refreshes the exact NUL-delimited status." **P2 pin-currency divergence:** PROVENANCE pins value.js `tranche-u@c654824e` and keyframes-v-exec `master@81a56990`, whereas the edict matrix E-L1-S0.3 cites `db77dbd8` / `0dac636b`. PROVENANCE annotates both as "earlier source-evidence baseline remains `db77dbd8`/`0dac636b`" — i.e. it pinned the **current** HEAD (arguably more correct) and disclosed the baseline. Not a fabrication; a currency delta to reconcile in the union.

---

## PART II — GAPS, PARTIALS, AND DIVERGENCES

### B1 — Convergence NOT achieved: 0/2 clean passes; newest artifact reopens RR-17
**Verdict: MISSING (deliverable gap) — but HONESTLY self-reported. Severity: P1 material. Edict: E-L1-S0.4, E-L1-S9.1, FORMATION-completion cond.**
FORMATION.md completion law requires "**two consecutive fresh formation-wide hostile triads … clean after the 32-seat audit.**" The **newest snapshot artifact**, `reviews/ROOT-REPAIR-R3-ADJUDICATION.md` (19:39), returns **"NOT CLEAN. RR-17/C00U is reopened"** with three required correction slices and: "The whole-formation clean-pass manifest is absent. The clean-pass count therefore remains **zero** … Production remains **0/190**." `AUDIT-REGISTRY.md` §Stability confirms clean-pass count 0 and lists RR-14..RR-18 as focused-repair state. **By its own law the formation is INCOMPLETE.** This is the single most load-bearing fact for the apotheosis: a rich, rigorous corpus that has **not passed its own gate** and **cannot freeze a new corpus epoch** until Slice-1/2/3 (C00U source projection · disposition-aware bounds/resolver/receipt · C00U BLOCKED-return separation) land and a fresh RR-14..18 triad is clean. **Mitigation (grade the honesty CORRECT):** the corpus does not claim completion — this is its headline, self-tracked to file:line.

### B2 — RR-17 defect is real and precisely diagnosed (evidence the audit apparatus works)
**Verdict: CORRECT (as adversarial diagnosis) / the underlying tool defect is WRONG-in-code. Severity: P1. Edict: E-L1-S0.4 (registry stability), E-P6.2 (C00U discovery).**
`ROOT-REPAIR-R3-ADJUDICATION.md` UPHELDs a genuine false-green in `tools/resolve-consumer-universe.mjs:447-480`: the six-regex source projector "has no template-literal arm" for dynamic import and "a declaration whose named specifier is `type` enters the runtime arm" — proven by an in-memory application to three lines of TS yielding `{"dynamic_template_detected":false,"inline_type_classified_as_type":false}`, and traced through `scanEdges`/`observedEdgeMap`/lines 825-831 to a false `resolvable:true`. It further shows the unavailable-required-root branch is **not end-to-end inhabitable** across bounds-authority/resolver/receipt/annex-schema/live-replay (three-layer proof). This is **model process compliance working as designed** (audit waves surface defects; defects reopen owners; clean counter resets) — but it also means C00U (the consumer-universe discovery gate the whole close depends on) has a **live correctness defect** in its resolver code. Both truths hold.

### B3 — All-Sol routing is CONTENT-ATTESTED, not cryptographically VERIFIED-as-served
**Verdict: PARTIAL. Severity: P1 material. Edict: E-L1-S0.2, E-P0.1 ("a model declaration is not model execution — VERIFY the tier actually served").**
`AUDIT-REGISTRY.md` §Control law: "Every seat requested `gpt-5.6-sol` at `ultra`, and its persisted session content attests that route … **Provider-authenticated session existence, freshness, independence, model, and effort are an external trust boundary and are not claimed here.**" `PROVENANCE.md`: "Model declaration alone is insufficient; the served model must be verified **where the execution surface exposes it**" — and the surface exposes only JSONL `model`/`effort` fields, **not** cryptographic provenance. `P01-INDEPENDENT-AUTHORSHIP.json` `access_observability_limit`: "Codex JSONL exposes recorded tool calls … not operating-system reads … independence is enforced by fork_turns:none." **Served-tier is attested (JSONL content), not proven.** The verify-the-served-tier edict is met to attestation grade only. HONESTLY declared as a trust boundary → PARTIAL, not WRONG. (For our program the whole formation ran Sol/Luna, which under E-C17/F1 is the wrong fleet — a union framing, not a Codex defect.)

### B4 — The full-CSS parser prototype is a born-RED contract with 0 authors, not a produced+benchmarked artifact
**Verdict: PARTIAL. Severity: P1. Edict: E-C14 (dual definition "prototyped and benchmarked NOW"), E-P2.2-R-PARSER.**
`P01-INDEPENDENT-AUTHORSHIP.json`: `"state":"born_red"`, `"author_receipts":[]` (empty), `adjudication.receipt.state:"pending"`, `execution_input_epoch.state:"pending"`. Honest (no green-over-nothing), but E-C14 asks for a dual BBNF-and-parse-that prototype **produced and benchmarked in THIS formation**; the Codex corpus instead **specifies** it as P01 (a born-RED wave whose inputs P00/V01/V02 "do not exist until their owning work returns"). Under our canon this is a deferral of a formation-phase deliverable to execution phase — the prototype/benchmark artifact E-C14 requires is **not on disk**.

### B5 — Owner-decision docket encodes rulings DIVERGENT from our binding canon
**Verdict: WRONG-for-our-program (faithful-to-Codex-owner). Severity: P1 material. Edict: E-C17/F1, E-L1-S4.4/F3, E-C1/F4, F8.**
`OWNER-AMENDMENTS.md` records owner rulings that **conflict with CONVERSATION-ADDENDA** on three axes: **OA-01** Fable→Sol/Opus→Luna (our E-C17 = Fable/Opus); **OA-09** `gpt-5.6-sol`/ultra/danger-full-access config (inapplicable to us, F8); **OA-14** "deletions are welcome … historical export count is not a preservation target" → authorizes removing the `./value` and `./quantize` export keys (`DISPOSITIONS.md`: "`./value` public key → **Tombstone**"; V00C freezes only **six** keys), whereas our canon E-L1-S4.4/F3 **holds the 7-key freeze**; **OA-17** `unchanged-1.0.0-no-republish` ZERO mutation (our E-C1/F4 = "minor fixes only if needed"). These are not Codex-execution defects — the fleet faithfully carried a **different owner capture** — but **the OA docket cannot be adopted wholesale into the union**; each divergent axis must be re-decided against CONVERSATION-ADDENDA.

### B6 — Our program's meta-audit arms have no landing in the Codex crosswalk (expected)
**Verdict: MISSING by scope-boundary (not a Codex defect). Severity: P2. Edict: E-C16/C17/C18/C19/C23, F6.**
`PROMPT-RECAP.md` maps the **two seed letters**, not CONVERSATION-ADDENDA. The union/isolation/backtrace/adequacy asks — E-C16 (isolation), E-C17 (Fable/Opus), E-C18 (kf-session backtrace vs letters = silent-drop audit), E-C19 (parse-that up-to-spec adequacy), E-C23 (union/apotheosis) — are **absent** (a fleet cannot self-mandate its own adversarial audit). Expected per F6; flagged so the union arm carries them independently. The closest Codex row, "Audit Claude JSONL rather than an imagined workspace → Provenance packet; K00, M00 consume only adjudicated findings," addresses **findings** but not the **exhortation-vs-letter reconciliation** E-C18 demands.

---

## PART III — SUMMARY LEDGER (my lens)

| # | Finding | Verdict | Sev | Edict row |
|---|---|---|---|---|
| A1 | 190 waves carry full 7 cells + concrete born-RED (12+ sampled end-to-end) | CORRECT | — | E-L1-S9.1 |
| A2 | 190 spec'd waves ≫ 50-floor, spans all named surfaces | CORRECT | — | E-C8 |
| A3 | Thrice method genuinely ran (formation grain) — 15 falsifiable V-findings, SCC caught, real REFUTEs | CORRECT | — | E-C20/S8.1 |
| A4 | RETURN-CONTRACT: JCS self-hash, typed absence, 8-cell op vector, gate-receipt v3 | CORRECT | — | E-L1-S9.1 |
| A5 | π/DELTA matrix mandatory per visual claim; no defer-to-D25 | CORRECT | — | E-L1-S9.1 |
| A6 | Terminal disposition for all recap/chronic rows; 0/190 ⇒ no partial-done; conditionals end KEEP/PRUNE | CORRECT | — | E-L1-S7.1 |
| A7 | Single owner-decision docket exists (OWNER-AMENDMENTS) + dispositions sheet | CORRECT | P2 | E-L1-S9.1 |
| A8 | G0′ 18-repo pin table w/ HEAD+dirty digest; stale-atlas excluded | CORRECT | P2 | E-L1-S0.3 |
| B1 | Convergence NOT achieved: 0/2 clean passes, RR-17 reopened (self-reported) | MISSING | P1 | E-L1-S0.4/S9.1 |
| B2 | RR-17 = real resolver false-green, precisely diagnosed to file:line | CORRECT(diagnosis)/WRONG(code) | P1 | E-L1-S0.4 |
| B3 | Sol served-tier content-attested, NOT cryptographically verified | PARTIAL | P1 | E-L1-S0.2/E-P0.1 |
| B4 | CSS prototype born-RED, 0 authors — spec not produced/benchmarked | PARTIAL | P1 | E-C14 |
| B5 | OA docket diverges from our canon (model/deletion-keys/parse-that) | WRONG-for-us | P1 | E-C17/S4.4/E-C1 |
| B6 | Meta-audit arms (C18/C19/C23) absent from Codex crosswalk (expected) | MISSING(scope) | P2 | E-C16-23/F6 |

**Net:** On my lens the Codex formation is **process-compliant and return-contract-rigorous to a high standard** (A1–A8 all CORRECT). Its shortfalls are (i) **honest non-convergence** — 0/2 clean passes, RR-17 open, a new epoch cannot freeze until three correction slices land (B1/B2); (ii) **attestation-grade, not proof-grade, model provenance** (B3); (iii) **the flagship CSS prototype specified but not produced** (B4); and (iv) **an owner docket built on a different owner capture** that the union must not adopt wholesale (B5). None of A1–A8 or B1–B4 involves concealment: the corpus's self-reporting is candid to file:line, which is itself strong evidence of process integrity.
