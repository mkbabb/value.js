# ARM-A VERDICT — Adjudication of the Codex V-next Formation Audit

Adjudicator: Fable seat (`claude-fable-5`), 2026-07-19. Subject: the 8 lens audits + inventory + edict matrix under `armA/`, each P0/P1 finding independently re-verified against the frozen snapshot `…/apotheosis/snapshot-vnext` (2026-07-19 21:29). I re-ran every load-bearing grep myself (case-insensitive, whole corpus), re-read the cited wave rows, dispositions, OA docket, P01 JSON, and the R3 adjudication. Verdicts below are mine, not vote counts. No repo file touched; no `r1-opus-refuted` path read.

Verification headline: **the audit corpus is accurate to an unusual degree — of ~40 P0/P1 claims, all but one survived adjudication intact; 3 findings are refuted or materially downgraded (§5), 1 flagged-unverified row is resolved.** All zero-hit grep claims re-confirmed: `164343c1`, `src/parsing`, `css-parse-perf`, `LT-10`, `LT-16`, `N-ADJ-3`, `proof:structure`, `depcruise`, `dependency-cruiser`, `madge`, `SCI-1`, `mixColorsInto`, `toRgba8Into`, `W53/W55/W56`, `EP-3`, `K.W8`, `raytrace`, `sampleColorRamp`, `deltaEOK`, `api-extractor`, `self-dep`, `pre-publish` = **0 real hits each** (F6.6/D50 hits are SHA substrings only).

---

## 1. GRADED VERDICT SHEET (per program domain)

### PARSE — **PARTIAL** (readoption CORRECT · restoration WRONG)
| Evidence | Verified |
|---|---|
| parse-that readopted as published 1.0.0, immutable, `unchanged-1.0.0-no-republish` receipt (P04), all internal novelty routed external (DISPOSITIONS rows 9–16; BBNF-PARSE-THAT-MAJOR-HANDOFF) | ✔ tighter-than-canon, safe |
| `164343c1` / `src/parsing` / `css-parse-perf` = 0 corpus hits — the charter's named restoration recipe + bench-witness recipe silently dropped | ✔ grep re-run |
| P01 "cannot execute from the 15-file formation snapshot… the union is not yet frozen" (PARSER-CSS-COLOR.md:375–385); P00 gates "after—not before—the active CSS union closes" — the whole V03→V29T spine hard-blocked on the external SK-V25 GPT campaign | ✔ read verbatim |
| `P01-INDEPENDENT-AUTHORSHIP.json`: `state:"born_red"`, `author_receipts:[]` — the C14 "prototyped and benchmarked NOW" mandate unmet; 0 bytes of prototype exist | ✔ parsed JSON |
| Spec breadth (V01–V29: CSSWG lock, CSSOM V10I/S/D, Typed OM V11, color-mix V17, relative `from` V14, contrast-color V20) exceeds E-C12 | ✔ wave rows read |

### COLOR — **PARTIAL**
| Evidence | Verified |
|---|---|
| §14.2 correction is real and better than the seed: Local-MINDE control + Ray Trace challenger + EdgeSeeker refused (PARSER-CSS-COLOR.md:91–94, DISPOSITIONS.md:29); cusp/Halley barred from `css-*` names is spec-correct | ✔ |
| V15P converts the DECIDED cusp/Halley restore into terminal KEEP-or-PRUNE with owner-precut deletion annex (waves/P-V.md:97) — a decided P2 row made prune-eligible | ✔ |
| Raytrace role inverted: production challenger only; no test-side exact-boundary oracle; no raytrace.ts/boundary.ts anywhere (`raytrace` grep = 0 outside V15 prose) | ✔ |
| V18H/V18V deps = "V12, V16A" — no V15P edge; a V15P PRUNE strands OKHSL/OKHSV or forces duplicate cusp math | ✔ dep cells read |
| `SCI-1`/`D54`/`W56`/`mixColorsInto`/`toRgba8Into` = 0 hits — the inherited-DECIDED Into vehicle re-derived, not inherited; V16A excludes mix/ramp, punted to prune-eligible V16B | ✔ |
| kf scar deletion (backward/color.ts:120-124, backward.ts:30-32) absent from K band + coordination | ✔ grep |

### STRUCTURE — **PARTIAL** (engineering CORRECT · governance WRONG — carries the sole P0)
| Evidence | Verified |
|---|---|
| Machine-derived current DAGs (module-graph.mjs, hashes+commands); tests-isomorphism exact both repos, born-RED, machine-checked; one validator over both manifests = real abstract value↔kf(+fourier) isomorphism | ✔ (S-05 join re-verified by the auditor; artifacts read) |
| **P0**: `LT-10`/`LT-16`/`N-ADJ-3`/`proof:structure`/`depcruise` = 0 hits — the P4.3/P4.4 governance lineage wholly replaced by a parallel authority; "silence re-litigates it" realized at corpus scale | ✔ grep re-run |
| `.dependency-cruiser.cjs:171` leaf-law key never rowed; target forbids both `src/animation` and `src/internal` → the live gate goes green-by-vacancy post-K22T | ✔ (key absent from corpus) |
| Export surface = SIX keys (V00C "exact six-key freeze"), `./value` tombstoned — rests solely on Codex-only OA-14 ("Historical export count is not a preservation target"); binding canon holds the 7-key freeze (F3) | ✔ V00C + OA-14 read |
| api-stay reconciliation exemplary: OA-05 names both poles; library `src/api` facade prune explicitly distinguished from the retained server `/api` (DISPOSITIONS.md:37/93) | ✔ |

### FRONTEND — **CORRECT** (strongest domain; one P1 provenance debt)
| Evidence | Verified |
|---|---|
| All three owner marks carried with architecture, not cosmetics: D03A mount-independent capability registry (born-RED "mobile Tools visible yet empty/no-op" — row read); K12 single quaternion kernel + M05–M10; M08 Chronograph Contact Sheet with anti-token-tune exclusion | ✔ rows read |
| Born-RED premises verified on real trees by the auditor (OrbitalDrag.vue:279 triple-lane binding; 19 shadcn barrels/4 reka files/2 vendor deps) — provenance spot-checks held | accepted (auditor ran them read-only) |
| Live audit covers 14+7 routes × 390×844 + 1440×900 with measured geometry; E-C22 totality met | ✔ |
| P1 debt: BJ/IOS27 appear only as preserve/re-pin notes (PROVENANCE.md:54; HANDOFFS.md:260); the C10 corpus (IOS27-CODEX laws, SUFFUSION-MATRIX) never a bound design input; G04/G05 motion grammar independently invented | ✔ (see §5 — "nowhere" claims corrected, substance stands) |
| DesignSync honestly banked with G00/D00A/M00 retriggers, confirmed by X-ADJ amendment 14 | ✔ |

### API/ROUTING — **CORRECT**
| Evidence | Verified |
|---|---|
| Palette API complete+: history/variants/all-CRUD/mixing all enumerated (`value.mix.validate/preview/create/…` at API-OPERATIONS.md:143-154), 147-op closed registry signed `A-ADJ-SOL3-ROUTES-2026-07-18`, 130 HTTP = 89+41, 17 headless | ✔ counts + signature read |
| Fourier repo demonstrably read (889/652-line router cites; pinned HEAD `cd26c6533` + 27-entry dirty digest) | ✔ PROVENANCE row read |
| STATE-ROUTING: JCS envelope, byte-identical re-encode, 11 typed errors, history transaction law, credentials-never-in-URL, anti-vacuous-green decode clause — both apps | ✔ |
| Gaps are P2: no palette↔fourier facility-correspondence ledger (protocol isomorphism only); A21A/A21J job/asset infra depth wants owner ratification vs C7-vs-C8 licensing | ✔ |

### CONTRACT/PROCESS — **PARTIAL** (rigor CORRECT · convergence MISSING)
| Evidence | Verified |
|---|---|
| 190 waves × 7 cells with concrete born-RED (12+ sampled); 149-row seed bijection machine-checked; return-contract v2 (JCS self-hash, typed absence, gate-receipt v3) exemplary | ✔ |
| Thrice loop genuinely ran: V-ADJ 15 falsifiable families, SCC caught (P07→V03→…→P07), 33→44 amendment — adjudicators reasoned, not vote-counted | ✔ |
| **Convergence absent**: R3 ADJ (newest artifact) = "NOT CLEAN. RR-17/C00U is reopened… Production remains 0/190"; clean passes 0/2; pre-excluded pass files absent | ✔ read verbatim |
| Model provenance attestation-grade only: "external trust boundary and are not claimed here" (AUDIT-REGISTRY.md:10) vs the P0.1 verify-the-served-tier law | ✔ |
| OA docket = a different owner capture (OA-01 Sol/Luna, OA-14 key-removal, OA-17 zero-mutation) — faithful to Codex, non-adoptable wholesale for the union | ✔ rows read |

### APPARATUS — **WRONG on the C21 axis** (honesty credited throughout)
| Evidence | Verified |
|---|---|
| Byte inversion: tools/ 2,260 KB vs waves/ 160 KB (du re-run) — ~75% apparatus, ~4% deliverable, against "little time on contrived gates… majority on direct implementation" | ✔ |
| 316 KB clean-pass forensics while 0 passes exist and the core question (served-tier) is conceded unverifiable | ✔ |
| 211 KB P01 authorship police for a 0-byte prototype (born_red, 0 authors) | ✔ |
| The irony conviction: the anti-regex formation's consumer-universe gate is six regexes; R3 proves template dynamic-imports undetected + inline `type` misclassified → false `resolvable:true`; the 70 KB selftest never probed either grammar | ✔ R3 findings 1–2 UPHELD read |
| Credits are real: validate-formation + seed-bijection probed non-vacuous (mutations go RED); 32/32 JSONLs, 74/74 tools on disk; zero close-class lies in self-state | ✔ probe transcripts + spot checks |

### PACKET-CENSUS — **PARTIAL**
| Evidence | Verified |
|---|---|
| Zero-drop instrument sound over the two letters: 149-row bijection SHA-pinned, 14/15 spot rows faithful; structurally blind to CONVERSATION-ADDENDA (amendment_authority = OWNER-AMENDMENTS.md) | ✔ |
| Zone flips confirmed on disk: K15 view-transition Prune, K22 presets Prune-from-library, K14 flip Prune, K21 ElementMorph Prune-with-casualties (DISPOSITIONS.md:47,60,61,67-68) — vs seed KEEP-EARNED/OWNER-DECISION; `EP-3`/`K F6` citations = 0 | ✔ |
| spring() → **Reject** (DISPOSITIONS.md:28) and HDR → built (V19): designated owner-decision rows foreclosed by fiat (both factually defensible, procedurally pre-empted) | ✔ |
| Oscillator rider claim REFUTED — see §5 | ✔ |
| Pins current-not-seed (`c654824e`/`81a56990` vs `db77dbd8`/`0dac636b`) with baseline disclosed — currency delta, not fabrication | ✔ |

---

## 2. THE RIGHTS — what the Codex formation genuinely nailed

1. **Honesty discipline without peer.** 0/190 and 0/2 stated in every authority doc; failed passes booked as evidence never credit; the newest artifact is a self-issued NOT CLEAN against its own machinery. Zero close-class lies found by two independent lenses probing for exactly that.
2. **The seed-bijection machine check** — 149 rows, SHA-pinned letters, mutation-RED verified by execution. The strongest zero-silent-drop implementation this constellation has produced.
3. **The thrice loop turned on itself and it worked**: R3 caught a real false-green in the formation's own consumer-universe resolver, diagnosed it to file:line with an in-memory reproduction, prescribed 3 bounded slices, and reset nothing to green.
4. **The frontend band**: all three owner marks (C2/C3/C4) landed as architecture with on-disk-verified born-RED premises; the shadcn abrogation census is byte-exact (19 barrels/4 reka files/2 vendor deps); the identity work (Optical Instrumentarium / Chronographic Stage) is grounded in real tokens/fonts with an anti-template test.
5. **The palette/Fourier API registry**: 147 closed, signed, tuple-hashed operations; history/variants/CRUD/mixing complete-plus; single-tenant + security discipline; the HTTP/headless split correctly refuses maintenance routes.
6. **STATE-ROUTING.md**: the URL/share-state law is rigorous both apps, with the anti-vacuous-green decode clause the charter demands.
7. **Tests-isomorphism + target manifests**: exact, external, machine-checked on BOTH repos, born-RED, with executable path authorities and typed conditional edges — beyond what the seed specified.
8. **Spec-grade corrections of the seed itself**: the CSS Color 4 §14.2 three-algorithm reality (vs the seed's "cusp+Halley = §13-conformant" imprecision) and the EdgeSeeker refused-at-evaluate honesty; the K20 oscillator prune carrying the false-consumer record correction verbatim.
9. **api-stay reconciliation** (OA-05): both poles named, library-facade vs server surface distinguished — the model of how a seed reversal should be papered.
10. **Live visual audit**: 21 routes × 2 viewports with measured geometry and per-finding owning waves.

## 3. THE WRONGS — confirmed breaches, each with the exact repair

| # | Breach (sev) | Repair |
|---|---|---|
| W1 | **Governance re-founding** (P0): LT-10/LT-16/N-ADJ-3/proof:structure/depcruise = 0 hits; restructure runs on a parallel authority; live kf gates un-rowed | Add a lineage annex naming every superseded ruling (LT-10, LT-16); EXTEND proof:structure rather than replace; row `.dependency-cruiser.cjs` + the 755-line gate with explicit retire/extend dispositions |
| W2 | **Parser spine externally gated** (P1): P01/V03→V29T blocked on the unfrozen SK-V25 union; the self-contained `164343c1^` restoration silently dropped | Restore `164343c1^:src/parsing/` as the un-blocked baseline spine (BBNF mirror stays as the additive C14 definition), or present the external gating as a named owner-decision row |
| W3 | **Bench witness dropped** (P1): no `css-parse-perf.mjs` resurrection, no one-time regex baseline | Resurrect `164343c1^:bench/css-parse-perf.mjs` + the MB/s+ns/call+co-scaling ratio gate; measure the retiring regex parser once for the record |
| W4 | **Zone-verdict overturns without record citations** (P1): K15/K22/K14/K21 invert KEEP-EARNED/OWNER-DECISION rows; EP-3/K-F6 cited nowhere | Convert each flip to an owner-ratification row citing the r2 census + EP-3 PATH-B/K F6 fence; or supply a fresh four-tree census + record-axis evidence per flip |
| W5 | **Export-key freeze broken on non-binding authority** (P1): six keys, `./value` out, on OA-14 only | Put the six-key surface to the owner as a named ratification row against the binding 7-key freeze |
| W6 | **RR-17 false-green resolver** (P1): six-regex source projector; template dynamic-imports omitted; inline `type` misclassified; selftest blind | Land R3's three slices; replace the regex projector with es-module-lexer/TS compiler API; add the two grammars to the selftest battery |
| W7 | **SCI-1/D54/W56 DECIDED row not inherited** (P1): Into/SoA re-derived fresh; the 4.1.x vehicle vanished without reconciliation | Inherit D54 or record the single-5.0.0 supersession as an explicit owner row; carry `mapColorToGamutInto`/`safeAccentColor`-class hot paths by name |
| W8 | **Raytrace oracle inversion + cusp DAG gap** (P1): no test-side exact-boundary oracle; V18H/V18V lack the V15P edge | Add the raytrace exact oracle to V15's test side; wire V15P→V18H/V18V (or hoist cusp math to a shared module both depend on) |
| W9 | **R-RAMP kf-scar deletion absent** (P1) | Add the kf coordination dispatch: backward-emit re-adopts value ramps AND deletes backward/color.ts:120-124 + backward.ts:30-32 |
| W10 | **Apparatus inversion vs C21** (P1): ~75% apparatus/4% deliverable; 316 KB forensics conceding its own question; 211 KB police for 0-byte P01 | Execute the apparatus collapse list (forensics→core ~25 KB; return suite deferred; P01 police shelved; selftests folded via `--selftest`); spend the recovered effort writing the C14 prototype |

## 4. THE PARTIALS — started, incomplete, and what completion requires

1. **C14 prototype+benchmark NOW** — topology complete (15 BBNF ↔ 15 TS peers, CSS-MODULE-ISOMORPHISM.json); zero authors, zero benchmarks. Completion: write and bench the prototype in-formation.
2. **BREATH OF LIFE (C10)** — substance real (G05 budgets, D05 watercolor backplates, D15/D24 beget); ingestion not done. Completion: read IOS27-CODEX + SUFFUSION-MATRIX + BJ BAND-FEEDBACK-MOTION; reconcile G04/G05/M08 against the numbered laws before any motion wave.
3. **DesignSync** — banked with named G00/D00A/M00 retriggers; completion = execute the retrigger before implementation.
4. **kf fence pack (E-P4.1-1)** — TimingFunction contract retained (HANDOFFS.md:374; C02B), `./engine` preserved (K23); the three named atlas chase sites (useCountUp:47/useScrollTimeline:44/useScrollLettering:57) = 0 hits. Completion: pre-name them in the boundary chase ledger.
5. **13-anchor flatten census + ≈340 import lines** — replaced by byte-snapshot replay (arguably stronger as drift defense); the anchor enumeration and post-move config re-pointing owner are absent. Completion: enumerate the 13 anchors born-RED in K22T; own config re-pointing in a wave cell.
6. **Standing pre-publish manifest gate** — deps strip correct (V00B); only the boundary event covered; no recurring gate, no self-dep rule. Completion: add the standing gate naming the v1.1.0–v2.0.1 self-dep prior art.
7. **spring()/HDR/eval owner-decisions** — resolved by fiat (Reject/build/build); each factually defensible. Completion: docket rows for owner sign-off (spring citing K F6.6; V19 noted "R-HDR resolved by C12").
8. **PT-E letter** — routed wholesale external; completion: per-ask dispositions (diagnostics HIGH / inference MED / Pratt-dormant).
9. **Model provenance** — attestation-grade JSONL only. Completion: accept the trust boundary explicitly as an owner row, or add a provider-side check; stop pricing 316 KB against an admittedly unanswerable question.
10. **C2 mark homing** — M08 substance excellent; recap row homes it kf-only. Completion: dual-home M08 + D16/G09.
11. **Convergence** — the return is a strongest-core-with-named-gap; completion = W6 slices → fresh RR triad → the two whole-formation clean passes.
12. **P2 hygiene**: 39-PNG row visible only as hashed fold (surface in D00 text); D50 `/index` boundary superseded by silence (name the supersession); README tool index 11 gaps; kf manifest lacks conditional-path modeling; `c654824e`/`81a56990` pin-currency reconciliation.

## 5. REFUTED / CORRECTED AUDIT FINDINGS

1. **audit-packets B-21 (oscillator rider missing) — REFUTED.** K20's row carries it verbatim: mission "PRUNE Oscillator **and correct false consumer records**"; born-RED "Claimed Atlas runtime consumption is false"; deliverables "record correction… the false Atlas record" (waves/K-A.md:39). The mandatory rider is present; only P5.8's proof:publish gate-rule candidate remains open.
2. **audit-frontend FD-10 + audit-color R-BOUNDARY "MISSING/silent drop" — REFUTED as drops.** SEED-ROW-INVENTORY.json:1529 banks H-T-L270 with the named retrigger "D17 proves the perceived-space plate requires a reusable gamut-boundary sampler" — a terminal disposition (banked + re-trigger) exactly as the edict's iff-pulled gating requires. Only the W53 vehicle NAME is absent — downgrade to a P2 naming note (the packets auditor had this right).
3. **audit-packets B-16 ("IOS27-MICRO appears NOWHERE") — literal claim REFUTED.** IOS27 is named twice (PROVENANCE.md:54 "Active BJ and IOS27 tranche… preserve all 26 entries"; HANDOFFS.md:260 "26-entry BJ/IOS27 fingerprint and re-pin before G00"). The substantive finding stands in FD-05's correct form: the corpus is a preservation fingerprint, never a bound design input.
4. **audit-packets B-23 (TimingFunction fence UNVERIFIED) — RESOLVED: PARTIAL, not dropped.** Contract retained at HANDOFFS.md:374 + C02B; `./engine` externally honored (K23); the 3-site atlas chase ledger is genuinely absent (grep = 0).

Everything else survived: every zero-hit claim re-ran clean; V15P/V16A/V16B/V18H/V18V/V19/V25/V00C rows read as cited; P01 born_red/0-authors, the SK-V25 block prose, OA-01/05/14/17, DISPOSITIONS 28–68, the R3 verdict, and the byte inversion all confirmed on disk.

## 6. PRIORITY REPAIR LEDGER (for the Codex fleet — ≤15 rows)

| # | Repair | Sev |
|---|---|---|
| 1 | Land R3's 3 correction slices (C00U projection · disposition-aware bounds/resolver/receipt · BLOCKED-return separation); replace the six-regex projector with a real lexer; fresh RR-14..18 triad | P0-path |
| 2 | Restore governance lineage: LT-10/LT-16 supersession annex; extend (not replace) proof:structure; row `.dependency-cruiser.cjs:171` + all live kf gates with dispositions | P0 |
| 3 | Un-block the parser spine: `164343c1^:src/parsing/` as the self-contained restoration baseline; SK-V25 mirror demoted to additive definition or owner-decision row | P1 |
| 4 | Write + benchmark the C14 dual-definition prototype NOW (authors > 0 before any further P01 apparatus) | P1 |
| 5 | Resurrect `164343c1^:bench/css-parse-perf.mjs` + ratio gate; one-time regex-parser baseline row | P1 |
| 6 | Owner-ratification row: six-key export surface vs the binding 7-key freeze (OA-14 collision) | P1 |
| 7 | Owner-ratification rows for the four zone flips (view-transition K15, presets K22, flip K14, physics/morph K21) citing EP-3/K F6/r2 census | P1 |
| 8 | Reconcile SCI-1/D54/W56: inherit or record supersession; name the Into hot paths; de-prune-couple mix/ramp Into from V16B or gate the coupling | P1 |
| 9 | V15: add the raytrace test-side exact-boundary oracle; wire the V15P→V18H/V18V cusp dependency | P1 |
| 10 | kf dispatch: ramp re-adoption + deletion of the two named backward-emit scars | P1 |
| 11 | Apparatus diet: forensics→core; return suite deferred to first execution wave; P01 police shelved; selftests folded; strip SK-V25 state from P00/P01/C00U rows | P1 |
| 12 | Ingest BJ + IOS27-MICRO corpus as a bound design input; reconcile G04/G05/M08 with IOS27-CODEX's numbered laws | P1 |
| 13 | Restore the foreclosed owner-decision rows: spring() grammar (cite K F6.6), HDR-via-C12 sign-off, R-EVAL decision-not-build | P2 |
| 14 | Fence/gate completions: atlas 3-site chase ledger; standing pre-publish manifest gate (self-dep class); dual-home the C2 mark (M08 + D16/G09); README tool index + byte-ledger gaps | P2 |
| 15 | Extend the zero-drop instrument beyond the two letters: crosswalk the CONVERSATION-ADDENDA universe (C1–C23) or record it as an external-arm boundary owned by the union program | P2 |

— Arm-A adjudicator, `claude-fable-5`, 2026-07-19.
