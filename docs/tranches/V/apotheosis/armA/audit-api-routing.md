# Arm A — API + Routing/State Compliance Audit (lens E-C6 / E-C7)

Grader lens: the palette/Fourier API specification and the routing/URL-state law for both demos, graded against the binding triad (L1 charter · P packets · C conversation addenda) and the armA edict matrix. Snapshot root: `/Users/mkbabb/.claude/jobs/9e7dadd0/tmp/apotheosis/snapshot-vnext` (frozen 2026-07-19 21:29). Every finding: VERDICT + evidence (file:anchor) + severity + edict-row.

Files read fully for this lens: `API-OPERATIONS.md`, `STATE-ROUTING.md`, `FORMATION.md`, `KEYFRAMES-API.md`, `waves/K-A.md` (K + A bands), `PROVENANCE.md`, `API-TARGET-PATHS.json` (head), plus targeted greps of `DISPOSITIONS.md`, `OWNER-AMENDMENTS.md`, `CURRENT-DAGS.md`, `PROMPT-RECAP.md`. `api-contract.source.json` (253 KB) not read line-by-line; its 130-HTTP/17-headless claim is cross-checked against `API-OPERATIONS.md`'s enumerated rows (which I counted).

---

## HEADLINE VERDICT

The API + routing surface is the **strongest band of the Codex formation**. The palette API is specified completely and then some (history/variants/all-CRUD/mixing all present, with revisions, forks, provenance, shares, exports, moderation, catalog, curation, audit); the URL-state/share law (`STATE-ROUTING.md`) is rigorous, sound, and covers both apps with per-route schemas; the Fourier repo was demonstrably read; and the api/-stays-at-/api decision is explicitly reconciled. The material weaknesses are three: (1) the C7 "isomorphism to fourier-analysis' CRUD-viz API" is delivered as *protocol* isomorphism only, with no explicit facility-correspondence ledger; (2) the Fourier A-band's DEPTH (leased durable-job workers A21J, asset storage/quarantine A21A) exceeds a "CRUD-visualization" mandate and reads as C8-licensed backend-rebuild scope rather than isomorphism scope; (3) the extract/stay reconciliation lives in the Codex-only OA-05 docket, and the fourier-isomorphism half of C7 is never fused with it into a single reconciliation the binding canon (C7) demands. None is a charter breach; the band would survive to execution. All of this is moot in the immediate term — production is 0/190 and no formation clean pass exists — so "proven/complete" means *spec-complete*, not *executed*.

---

## FINDINGS

### 1. Palette API facility completeness — VERDICT: CORRECT — P0-scope satisfied — E-C7 / E-L1-S9.1
Owner ask (E-C7): "every facility — history, variants, all CRUD, mixing — proven and completely addressed." `API-OPERATIONS.md` delivers all four and exceeds them:
- **History**: `value.revision.list/detail/diff/revert` (:122-125), plus independent variant revisions (:134-137) and mix revisions (:150-153). Wave A10.
- **Variants**: `value.variant.create/list/detail/patch/delete` + `revision-list/detail/diff/revert` (:129-137). Wave A11.
- **All CRUD**: palette create/detail/list/mine/patch/trash/restore/purge/visibility (:111-121); forks (:126-128); tags (:180-186); color-names/proposals (:187-193); flags (:175-179); featured (:194-196). Waves A07/A08/A09/A16/A17.
- **Mixing**: `value.mix.validate/preview/create/list/detail/patch/delete/revert/materialize` (:143-154). Wave A12 — stateless preview + persisted recipe CRUD + materialize-as-palette.

This is a genuinely complete, well-factored resource surface (closed manifest signed `A-ADJ-SOL3-ROUTES-2026-07-18`, tuple SHA-256 pinned, `validate-corpus.mjs` rejects duplicate tuples). "Proven" is deferred to execution (correct under formation-only law E-L1-S0.1); the born-RED gates + A26 147-operation audit bijection are the proof apparatus. This is the single most-complete thing the formation did.

### 2. Fourier repo was actually read — VERDICT: CORRECT — P1 — E-C7 / E-P0.3(G0′)
The owner's implicit test ("did anyone read the fourier repo — evidence?") passes. `PROVENANCE.md:55` pins `fourier-analysis` at `/Users/mkbabb/Programming/fourier-analysis` branch `m/w1-bump-migration` HEAD `cd26c6533`, NUL-porcelain digest `a471c6fc…`, 27 dirty entries named ("run-board, lock/manifest and Vue component work"). `CURRENT-DAGS.md:173` cites "an approximately 889-line visualization router"; `waves/K-A.md` A21 born-RED cites "889-line visualization and 652-line admin routers plus layer-oriented services." Those are source-specific facts obtainable only by reading the tree. A21R born-RED enumerates the live "gallery, session, admin, image, contour, equation and compute surfaces." Verdict: the repo was inspected, not assumed.

### 3. C7 "isomorphism to fourier CRUD-viz API" delivered as PROTOCOL isomorphism only — VERDICT: PARTIAL — P2 — E-C7 / F9
`KEYFRAMES-API.md:15`: "Protocol isomorphism does not mean shared runtime code or identical domain resources." A01 (`waves/K-A.md:51`) defines "language-neutral protocol… both languages pass identical wire vectors" (RFC 9457, envelopes, ETags, 428/412, cursors, idempotency, cache/Vary/CORS, lifecycle). The structural CRUD shape is mirrored (palette↔visualization; revision↔revision; fork↔derivation; trash/restore/purge on both; binding A25). This is a *defensible and largely correct* reading — you cannot make palette resources domain-identical to visualization resources, and `KEYFRAMES-API.md:456` correctly refuses to "copy palette mixing, taxonomy or account resources merely for symmetry" (the right anti-over-isomorphism call, consistent with E-L1-S7.1 no-contrivance). **The gap**: C7 asks for "isomorphism to fourier-analysis' CRUD-visualization API" as a design target, but there is **no explicit facility-correspondence ledger** (a table asserting palette.fork ≅ fourier.derivation, palette.revision ≅ fourier.revision, etc.). The isomorphism is asserted structurally and machine-gated only at the wire/protocol level (A26 "cross-language protocol… parity"). A reader cannot verify the CRUD-viz isomorphism claim against a checked correspondence artifact — it is implicit in two parallel prose tables (`KEYFRAMES-API.md:425-458`). Correct direction, incomplete proof surface.

### 4. api/ stay-vs-extract tension — reconciled, but only in the Codex-only docket — VERDICT: PARTIAL — P2 — E-C7 / E-L1-S4.5 / E-P3.2 / F9
The decision is correct and consistently enforced: `FORMATION.md:117` standing law "The value palette API remains under the existing `/api`"; `KEYFRAMES-API.md:15,157`; A03 exclusion (`waves/K-A.md:53`) "`/api` remains in value.js; no keyframes server." The explicit reconciliation of the seed's opposing row exists — **`OWNER-AMENDMENTS.md:17` OA-05**: "Seed says `api/ EXTRACT from repo` → Palette API must remain within its current `value.js/api`; A00–A20 transpose the backend internally; no standalone extraction wave or compatibility proxy exists." That is a proper cite-the-tension-and-overturn. **The defect**: OA-05 is the GPT-5.6 *Codex docket*, not the binding CONVERSATION-ADDENDA. Under our canon (edict-matrix F9), C7 requires the extract/stay reconciliation **and** the fourier-isomorphism mandate to be resolved together explicitly. The formation reconciles the extract axis cleanly but never fuses it with the isomorphism ask; and the reconciliation is not surfaced in the formation's own binding-canon-facing documents (FORMATION/DISPOSITIONS assert "stays" as settled law without narrating the P3.2 EXTRACT overturn). Substantively correct outcome; the explicit dual reconciliation the addenda demand is only half-present.

### 5. "/api structure inspected in BOTH repos; kf has no server API" — VERDICT: CORRECT — P1 — E-C7 / FORMATION standing law
C7 asks for "deep scrupulous inspection of the /api module structure in BOTH repos." The value api structure is deeply specified: full target TS tree `api/src/{contract,platform/{http,db,security,audit},modules/{identity,palette,history,variant,mix,share,community,catalog,curation,lifecycle,audit,meta}}` (`KEYFRAMES-API.md:213-308`), with A03 transpose citing the current defect ("28 source-colocated tests," "layered/concern routers, backend gods"). For keyframes, `FORMATION.md:116` asserts "Keyframes.js has no grammar productions and **no server API**" — verified against the letters (P/packets describe kf's `ProgramSpec`/`CompileResult` library surface, never a server). The correct disposition for a repo with no server API is to confirm absence, which K00 does (pins "packed export/API-by-symbol manifests" = library package API, not HTTP). The owner's "both repos" phrasing is resolved correctly: value gets a full backend spec; kf's absence is asserted, not fabricated.

### 6. Fourier A-band DEPTH exceeds "CRUD-visualization API" — VERDICT: PARTIAL (scope tension) — P2 — E-C7 / E-C8 / E-L1-S7.1
36 A waves = 23 value (A00–A20) + 13 Fourier (A21R,A21,A22,A22S,A21A,A21J,A23,A23S,A23H,A24,A25,A23C,A26). The value 23 are squarely licensed by C7. Of the Fourier 13: A22/A23/A23S/A23H/A24/A25/A23C (identity, CRUD, search, history, derivation, binding, client-gen) are the literal fourier CRUD-viz surface and are justified; A22S (security overhaul — delete slug-login/session/owner/draft façade, resource-edit credentials) is a defensible prerequisite to *safe* CRUD and disciplined (owner-precut annex, casualty proof — matches E-L1-S7.1 no-shim). **The tension**: A21A (asset image/contour identity, quarantine, decode limits, dedup/reference lifecycle, blob storage) and A21J (durable leased job workers, 202/Location, heartbeat/reclaim, kill-and-join cancellation, scoped compute cache) are heavy compute-pipeline **infrastructure**. A fourier visualization IS image→contour→epicycle-jobs, so these are within the *resource domain* — but the DEPTH (worker leases, heartbeats, saturation handling) is a from-scratch backend rebuild that exceeds any "isomorphism" reading and is only loosely licensed by C8's "mega-tranche… spanning fourier-analysis." Not a breach; but if the owner intended C7 as "mirror the palette API's shape onto fourier's existing CRUD," the job/asset infra rebuild is beyond that. Flag for owner ratification, not rejection.

### 7. URL-state + share-URL law for BOTH apps — VERDICT: CORRECT (high quality) — P1 — E-C6 / FORMATION UrlEnvelope law
`STATE-ROUTING.md` is rigorous and directly satisfies C6 ("robust URL state management + share-URL facilities" for both demos):
- One discriminated state model / reducer / codec / router per demo; components dispatch actions, never touch URL/storage/history/API directly (:6-9). Route state is a projection, not a second store — sound architecture.
- Canonical form `https://host/#/route?v=1&s=base64url(UTF-8(JCS(envelope)))` (:10-22); deviations-from-defaults only; JCS/RFC-8785 ordering; **byte-identical re-encode** invariant (:34); 11 typed `UrlStateError` codes (:47-60); structural limits (depth 12, 512 nodes, 6144 UTF-16 share bound) (:38-42); ordered decode pipeline (:43-45).
- History transaction law (BEGIN/preview / COMMIT-push-once / popstate-replace-no-echo) is exactly specified (:67-97); credentials/sessions/pointer/frame-time never enter URLs (:80-82).
- Value: 14 routes with per-route defaults + shareable-deviation schemas (:104-114), incl. the D17A atmosphere 18-atom join and D18A blob 47-leaf join pinned to producer defaults (:121-184); oversized state → explicit immutable `/api` share resource (:116-119); admin routes filter-only, sharing disabled (:187-201).
- Keyframes: 7 routes, transient playback/pointer excluded, quaternion URL tuples finite/normalized/hemisphere-canonicalized (:211-229); no share server → oversized authoring refuses or exports local JSON (:224).
- Born-RED proof mandates noncanonical/duplicate-key/wrong-route/oversized/popstate-storm/interrupted-gesture payloads at every π viewport (:241-245); "a codec is not green merely because its own encoder can decode its own output" (:245) — exactly the vacuous-green defense the charter demands (E-L1-S0.4).

This is the second-strongest artifact after the palette API. Ownership/gates table (:233-239) binds codec law to D02/M01–M02 with C07–C09 closure.

### 8. URL codec apparatus vs KISS — VERDICT: minor tension — P2 — E-C21
The URL codec is elaborate (JCS over URL fragments, byte-identical re-encode gate, 11 error codes, structural node/depth limits, 6144/6000/4500 triple bound). E-C21 warns "little time on contrived gates… more code and complexity is likely NOT better." Counterweight: C6 explicitly asks for **robust** URL state, and share-URL correctness genuinely needs canonicalization (otherwise share links aren't reproducible). The apparatus is proportionate to a shareable-state requirement; I grade it a minor tension, not a fault — the design is sound and the robustness was requested. Note only.

### 9. Single-tenant + security discipline — VERDICT: CORRECT — P1 — E-L1-S7.1
Both services explicitly single-tenant (`KEYFRAMES-API.md:15`): no header/query/path/body field selects a tenant. Value: client-generated 32-byte recovery secret, salted digest only, HttpOnly/Secure/SameSite=Strict session cookie, CSRF on every mutation, secrets never in URL/body/log/audit/idempotency (:312). Fourier A22S: removes insecure slug-login/session/owner/draft façade, resource-edit credential via `Authorization: Resource`, constant-time compare, one deployment operator ≠ value administrator (`waves/K-A.md:76`; `API-OPERATIONS.md:22-26`). A13/A22S refuse response-body tokens, migration shims, credential escrow — clean adherence to the no-alias/no-shim edict. Idempotency-Key/If-Match required-preconditions are declared per-operation.

### 10. Idempotency/CAS/precondition + headless separation — VERDICT: CORRECT — P1 — E-C7
The manifest correctly separates 130 HTTP from 17 deliberately-headless operations (`API-OPERATIONS.md:262-289`: session/idempotency expiry, migrations, projection rebuild, first-admin bootstrap, fourier job claim/heartbeat/complete/reclaim, asset sweep). "No internal worker callback, cron, migration, projection rebuild or bootstrap HTTP route is admitted" (:288) — this is the right call (no maintenance surface leaking as routes) and matches the no-god-router discipline. First-administrator bootstrap is headless, "There is no `/api/admin/*` router" (:105) — correctly avoids the admin-god-module anti-pattern (E-L1-S4.5 / DISPOSITIONS).

### 11. Value API removes deployed cruft with tombstones — VERDICT: CORRECT — P1 — E-L1-S7.2
`API-OPERATIONS.md:202` "`/api/docs`, publish/unpublish commands, vote toggles, impersonation and every `/api/admin/*` path are absent"; A14 replaces `POST /vote` toggle with idempotent PUT/DELETE (`waves/K-A.md:66`); A18 PRUNEs impersonation/admin-god/shared-token/recovery-key exposure with an owner-precut annex. These are surface *removals* handled with the annex/casualty discipline the tombstone law (E-L1-S7.2) requires — no silent drops. (Note: the current deployed value.js api per project memory shipped publish/unpublish + vote toggle at tranche J; retiring them here is a real behavior change, correctly annexed.)

---

## WHAT IS MISSING / UNPROVEN (fair-record)

- **No facility-isomorphism ledger** (finding 3): the C7 isomorphism claim has no checked palette↔fourier correspondence artifact; only wire-vector parity is machine-gated.
- **Proof is aspirational** (E-L1-S0.1 context): API-RETURN-COVERAGE bijection, A26 147-op audit, and every born-RED gate are *specified* but 0/190 executed; `README`/`AUDIT-REGISTRY` record production 0/190 and zero clean formation passes. "Proven and completely addressed" (C7) is met at spec grain, not execution grain — which is correct for a formation but must not be read as delivered.
- **The extract/isomorphism dual reconciliation** (finding 4) is split across OA-05 (extract) and KEYFRAMES-API prose (isomorphism); the binding canon wants one fused C7 resolution.
- **A21A/A21J scope** (finding 6) wants explicit owner ratification against C7-vs-C8 licensing.

## WHAT IS CORRECT (fair-record, load-bearing)

- Palette API facility set is complete and well-factored (history/variants/all-CRUD/mixing + forks/shares/exports/moderation/catalog/curation/audit) — the band's crown.
- Fourier repo demonstrably read (889/652-line router cites; pinned HEAD + dirty digest).
- api/-stays-at-/api is correct and consistently enforced as standing law + A03 exclusion.
- Both demos' URL-state/share law is rigorous, architecturally sound, born-RED against the right adversarial payloads, and includes the anti-vacuous-green clause.
- Security (single-tenant, hashed credentials, CSRF, no-secrets-in-URL) and the HTTP/headless split are disciplined and no-shim compliant.
- Fourier's insecure surfaces are PRUNED with owner-precut annexes, and cross-domain over-isomorphism is correctly refused.
