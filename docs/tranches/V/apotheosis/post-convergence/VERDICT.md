# POST-CONVERGENCE AUDIT — VERDICT (2026-07-20)

Root-session synthesis over the three seat reports in this directory
(`delta-census.md` · `ruling-fidelity.md` · `convergence-adjudication.md`),
audited against frozen `snapshot-vnext-2` (2026-07-20 10:27). Roster per the
C17 sparingly-law: two Opus mechanical seats + one Fable adjudicator.

## The verdict

**The convergence is REAL at the layer it claims — a seal, not a search — and the
fleet's ingestion of the union is the most faithful letter-consumption event in
this constellation's record.**

1. **22/22 owner rulings LANDED** — zero contradicted, zero mutated, zero missing.
   D-15 (fourier first-class) landed to full depth: an 18-facility bijection over
   all 146 operations with `fork≅derivation`, `revision≅revision`, named refusals,
   the complete program un-banked — and the contract's declared source hash
   mechanically matches the registry bytes. D-1's six-key proposal is terminally
   rejected *by name* in the wave's born-state. Three residuals are cosmetic
   (prose-encoded `model_served`; the OA-01 Sol/Luna substitution reading; the
   F-GOV-05 label carried as mechanism, not literal).
2. **The clean passes are genuine.** The Fable adjudicator independently
   re-implemented their epoch hasher and reproduced the certified corpus epoch
   `61d4954f…` exactly; the epoch hash-binds our four authority documents, the
   addenda, CARRY-LEDGER, V-PRIME, both relays, AND the BJ/IOS27 corpus — AM-30's
   union-corpus convergence law holds in the strongest sense. The validators can
   fail (31-mutation vector hand-verified; the byte-ceiling actually fired and its
   rejection artifact persists; five prior failed epochs on disk). Production
   remains **0/193 by their own in-epoch text** — honesty preserved.
3. **The C14 prototype is REAL-AND-BENCHED** at the W0-pilot scale it claims:
   genuine parse-that combinator code on the exact published 1.0.0 (registry
   receipt verified), 17/17 tests, and the bench REPRODUCED in the frozen copy
   (median 12.4ms vs 16.6ms recorded, identical subject/corpus/runner hashes).
   The regex comparator is honestly absent — P05 debt, held born-RED.
4. **The apparatus diet executed**: net −1.6 MB (1.8 MB removed — the whole
   reviews/ dir, 49 tools incl. the P01 police organ, 13 schemas — vs 214 KB
   added), while the RR-17 six-regex resolver was replaced with a pinned
   TypeScript-AST projector. KL-5/KL-6/repair-14 discharged.
5. **Repair ledger: 12 LANDED · 5 PARTIAL · 0 MISSING.** The former sole P0 is
   cured: wave K00 now carries LT-10/LT-16 supersessions, proof:structure-EXTEND,
   and the depcruise anchors.

## The residual punch list (the next session's spine; consolidated)

1. **Unbound close-laws**: CH-4 p75-LCP ≤2.5s and W54 D-1 aurora-derive route
   through the inventory but bind NO gate text yet.
2. **The P4.5 dispatch cargo is unwritten**: the kf scar-cure pack
   (backward/color.ts:120-124, backward.ts:30-32), the easing consume-vs-ratify
   brief, the eight-zone regex census — inventoried, not authored.
3. **The record-citation gate is practiced, not installed** — no mechanical grep
   gate exists yet; compliance is currently behavioral.
4. **Parser-baseline divergence, openly adjudicated**: the fleet holds
   BBNF-mirror-primary; our union holds `164343c1^` restoration-primary. Their
   position is argued in-corpus, not smuggled — but it remains a divergence the
   owner may wish to re-ratify at execution kickoff.
5. **Epoch fragility (JOINT fix)**: their certified epoch pins files in OUR
   `apotheosis/` dir — this root session's own RUN-STATE.md edits already drifted
   one pinned input. Rule for both sides: epoch-pinned authorities are immutable;
   status lives elsewhere; they re-pin at the next triad.
6. **Critic diversity**: all six pass seats were gpt-5.6-sol — freshness is
   prompt-level, not model-level. Acceptable under D-4/D-5 as ruled; noted.
7. P05: measure the retiring regex parser once, for the record.
8. Full residual detail: `convergence-adjudication.md` §4 (12 rows).

## State at the execution hold

Owner order 2026-07-20: **no tranche execution from this session — compact, then
begin.** Fable sparingly at execution (only the most complex/design-forward
passes). The fleet's own closure order stands at steps 6–7 (fresh focused triad →
two consecutive whole-formation clean triads). Everything either side needs is
on disk: the union corpus, the rulings, the 114/114 crosswalk, the 193-wave
registry, this audit, and the two frozen snapshots.
