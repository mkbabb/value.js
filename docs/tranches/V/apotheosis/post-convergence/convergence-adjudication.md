# CONVERGENCE ADJUDICATION — the Codex fleet's post-union claim, judged

Adjudication seat (`claude-fable-5`, skeptic-adjudicator), 2026-07-20. Sole write: this file.
Inputs: S1 = `snapshot-vnext/` (pre-relay, 2026-07-19 21:29) · S2 = `snapshot-vnext-2/` (post-convergence, 2026-07-20 10:27) · `armC/UNION-APOTHEOSIS.md` (§2 delta, §3 repair ledger) · `OWNER-RULINGS-2026-07-20.md` · the live evidence trees `docs/tranches/V/evidence/{vnext-clean-passes,vnext-union,vnext-history}/`. Method: every grade below traces to a grep/read/hash re-run by this seat on the frozen snapshots and the evidence tree, plus one permitted bench re-run inside `S2/prototypes/c14-css/` (node_modules installed via the lockfile, removed after). `r1-opus-refuted` never opened. Lens throughout: the close-class lies — vacuous green, declared-but-missing, per-mechanism green over gestalt broken, re-derivation posing as inheritance.

---

## §1 — THE CONVERGENCE CLAIM: real, at the layer it claims

**Claim under test:** `S2/FORMATION-CLEAN-PASSES.json` — `"schema":"vnext-formation-clean-passes/4"`, `"status":"clean"`, two passes over `corpus_epoch_sha256 61d4954f…`, six seats (critic_a/critic_b/adjudicator × 2), all `gpt-5.6-sol`/`ultra`, both pass verdicts CLEAN, `finding_families:[]`.

### 1.1 The strongest verification this seat performed: the epoch reproduces

I re-implemented `tools/corpus-epoch.mjs` independently (Python, from the read source: walk S2 excluding `{.cache,.npm,.vite,coverage,dist,node_modules}`, self-exclude only `FORMATION-CLEAN-PASSES.json`, append the 9 fixed external canon files plus the 8 `DESIGN-INPUT-EPOCH.json` inputs, sort by code-unit, JCS-hash). Result: **exactly `61d4954fd8a50dd1c79c146c80b93156c893b562ce928e2ce9869fb794d9345c`** — bit-for-bit the certified epoch — once `RUN-STATE.md` is taken at its pinned pre-drift bytes (see §4 row 4; the drift is OUR side's edit, not theirs). Also verified: `diff -rq S2 ↔ live vnext/` is EMPTY — the snapshot is the live corpus.

Two consequences, both load-bearing:

1. **S2 is byte-for-byte the corpus the passes certified.** Every §3 grade below is a grade of the certified bytes, not of a moving target.
2. **The epoch hash-binds the UNION canon.** The external file list inside `corpus-epoch.mjs` is: both kf-inbox seeds, `armC/UNION-APOTHEOSIS.md`, `OWNER-RULINGS-2026-07-20.md`, `CONVERSATION-ADDENDA.md`, `CARRY-LEDGER.md`, `V-PRIME.md`, both relay letters, plus the 8 design inputs (incl. IOS27-CODEX / SUFFUSION-MATRIX / BAND-FEEDBACK-MOTION — all three re-hashed by this seat against the live glass-ui tree: byte-true). **AM-30's demand that a clean pass be "a property of the UNION corpus, never of either parent" is satisfied in the strongest available sense: our authorities are inside the hash.**

### 1.2 Fresh, or self-graded?

Fresh in structure, self-family in route:

- Critics carry `input_report_sha256: []` (never saw a sibling); prompts open `ASSUME-FORMATION-WRONG … Work independently; never read a sibling report`; the pass-2 adjudicator binds `predecessor_adjudication_sha256 aa333d59…`, which **matches the actual on-disk hash of the pass-1 ADJ report** (shasum verified). All six report files exist at the manifest paths with matching sha256 and byte counts. Pass-1-A vs pass-2-A coverage commands: 0/12 textually identical (differently-phrased fresh seats), pass-2 A vs B: 7/12 — plausible independent convergence on the obvious validators.
- All six seats are `gpt-5.6-sol` — the authors' own route. Prompt-level freshness, not model diversity. The owner accepted this posture (D-4 per-seat `model_served` + spot probes; the Fable/Opus→Sol/Luna substitution is recorded inside `DESIGN-INPUT-EPOCH.json` itself). Recorded, not waived: §4 row 8.

### 1.3 Can the validators actually fail? Yes — enumerated, and they drew blood

- `tools/selftest-clean-passes.mjs`: I counted the mutation vector by hand: **exactly 31** (line 9: 6 · line 10: 2 · line 11: 4 · line 12: 1 · line 13: 18) — matching the critics' "31 clean-pass mutations were rejected" claim. The mutations are precisely our close-class lies: prompt text forged from "Audit formation" to "Claim formation"; a live finding present under a CLEAN verdict; the epoch command smuggled in as a domain's "focused" command; duplicate commands across domains; `served_model` flipped to `gpt-5.6-terra`; `served_effort` degraded; byte-count tamper; ordering attacks; schema downgrade; trailing-byte injection. Each must be caught or the selftest throws `mutation N survived`.
- **The machinery fired in production:** `FORMATION-CLEAN-PASS-1-ADJ.oversize-rejection.json` — the pass-1 adjudicator's first report was **REJECTED at 12,946 bytes against the 12,000 fail-closed ceiling** and re-done (final: 11,148). A sibling epoch (`290f4df1…`) holds a `FORMATION-CLEAN-PASS-1-ADJ.invalid-receipt.json`. **Six** epoch directories exist under `evidence/vnext-clean-passes/` — five prior attempts that did not survive. This is not a rubber stamp; it is a gate with a body count.
- Structural anti-self-certification: the corpus **inside** the epoch still reads `README.md:39` "Formation-wide clean credit is **0/2** and production execution is **0/193**" — only the self-excluded manifest may say otherwise. The corpus cannot certify itself; a finding forces a new epoch.

### 1.4 Three pass-certified claims, sample-verified against S2

1. **"146 operations"** (`API-OPERATIONS.md:66` locator) — `api-contract.source.json` `closedCounts`: http 129 (value 88 + fourier 41) + headless 17 = **146** ✓; the prose at API-OPERATIONS.md ~line 66 says "every one of the 146 HTTP/headless operations exactly once" ✓. (Note: the registry was regenerated from the pre-union 147 — a D-15/AD-2 `REWRITE_THEN_ADOPT`, recorded in `UNION-ROW-INVENTORY.json`.)
2. **"193 waves"** — wave-table rows: `waves/G-D.md` 44 + `K-A.md` 63 + `P-V.md` 53 + `M-C.md` 33 = **193** ✓, matching UNION-INGESTION's adjudicated topology (190 + G00I + C00P + V16R).
3. **`STATE-ROUTING.md:257`** — "Born-RED proof must include noncanonical payloads, duplicate keys, wrong-route envelopes…" present at exactly that line ✓. (Bonus: `DESIGN-PROGRAM.md` viewport rows "Compact phone 320 × 568" / "Desktop 1440 × 900" ✓ at the cited section.)

### 1.5 The honest bound on what 2/2 means

The passes certify **formation** health: 12 domains × 1 focused command per critic, mostly the formation's own (mutation-tested) validators plus spot text probes. The deep row-level hostility happened one layer down, in the **union triad** (`evidence/vnext-union/`, 2026-07-20 01:33): two skeptics with genuinely divergent bodies (A 225 / B 548 lines past a shared 245-line input-echo preamble) and a third adjudicator ruling **"DESIGN-CLEAN; CORPUS-NOT-CLEAN"** with an 8-step prescription. Skeptic B found **eleven exact-locator contradictions** between the then-live corpus and the owner rulings (six-key V00C vs D-1; K09 ingest vs D-7; K14/K21 prunes vs KEEP; durable idempotency vs D-10; A09's self-contradictory restore; `value.palette.restore` + 16 `required-durable` ops still in the contract source; A07T vs D-13; V05 vs D-17; V16B vs D-19). I verified the cures landed in S2: V00C now "the ruled seven-key surface… only `./quantize` demoted"; K09 "Retire Keyframes text ingestion… D-7 orders retirement"; A09 "W45 already retired restore"; V05 "retire the static evaluator… D-17"; A02 "explicitly preserving D2 §3 P2". The sequence — hostile triad finds → repairs land → new epoch frozen → two clean passes seal — is the honest shape. **Their own texts bound the claim correctly: the passes "do not execute or satisfy C08/C09"; production remains 0/193.**

**§1 verdict: the convergence claim is REAL at the formation layer — two genuinely fresh consecutive passes over a frozen corpus that hash-binds the union canon, sealed by validators proven able to fail and observed failing. It is a seal, not a search: read it as "the union-repaired formation is internally consistent and machine-checked," never as row-level truth of 193 waves.**

---

## §2 — THE C14 PROTOTYPE: REAL-AND-BENCHED (at W0-pilot scale, honestly labeled)

`S2/prototypes/c14-css/` — 975 lines of TS across 15 `src/css/grammar/l4/` modules + api/cst/lower/result/serialize/value + 15 external test peers + bench + proof + 3 tools; 224 KB.

- **Genuinely parse-that-idiomatic:** `combinators.ts` imports `Parser, regex, string, Span` from `@mkbabb/parse-that` and builds `token/pattern/capture/spanned/whole` via `.map/.mapState/.skip/.next/.opt/.eof`; `color.ts` composes `oklch()` from `all/any` with typed `OklchCst` lowering; `easing.ts` the same for `cubic-bezier()`. This is combinator authorship, not a regex engine wearing a costume.
- **Exact published pin:** `package.json` dependencies = `"@mkbabb/parse-that": "1.0.0"` (exact, no range — C15/OA-17 compliant). `npm view @mkbabb/parse-that version` → **1.0.0**; the local `parse-that/typescript/package.json` is 1.0.0; `proof/package-receipt.json` binds the registry tarball (integrity + sha256 + 65 files/401,130 bytes).
- **Honest scale:** README states plainly "Only W0 is a product-shaped vertical: `oklch()`, `cubic-bezier()`, and one qualified stylesheet rule. The remaining modules are thin executable edge and responsibility witnesses, not CSS coverage claims" — and `proof/status.json` books full-CSS coverage, P00/P01 equivalence and both comparators as **born_red**, with "prohibited_credit" naming production support. Repair row 5 asked for exactly "the W0 pilot shape" — this is it, with authors > 0.
- **Benchmarks were actually RUN — and they reproduce.** `proof/benchmark.json`: 11 samples × 1000 iterations over the 3-case W0 corpus, median 16.60 ms, realistic sample variance, full runtime identity (node v26.0.0 / V8 14.6.202.33 / darwin 25.4.0 arm64 — this machine). I verified `runner_sha256` = my own shasum of `bench/compare.ts`, recomputed `subject_sha256` from the tree (match), then **re-ran the bench in the frozen copy** (`npm ci` off the lockfile, `npm test` → **17/17 pass** matching `proof/last-run.json`, `npm run bench`): MEASURED, median **12.37 ms**, checksum 11000, identical subject/corpus hashes. Same order, same tree, same runtime. The semantic guard is real: measurement goes INVALID unless all three parse results hash to pinned `expected_result_sha256` values. The comparators (`current`/`historical` adapters) were ABSENT and the harness says so — "absent comparators never become fabricated performance credit."

**§2 verdict: REAL-AND-BENCHED.** The one open debt is not the prototype's: the retiring regex parser is still unmeasured (banked to P05, whose wave text resurrects "the method of `164343c1^:bench/css-parse-perf.mjs`" with the ≤1.10 scaling-exponent gate and "one retiring-regex baseline only"). See §4 row 5.

---

## §3 — THE REPAIR LEDGER: all 20 rows, graded in S2

Crosswalk of record: `UNION-ROW-INVENTORY.json` (`state:"TERMINAL"`, 114/114, authority hashes for the union doc + rulings both re-verified by this seat) rows R-01…R-20.

| # | Repair | Grade | Evidence (verified in S2) |
|---|---|---|---|
| 1 | RR-17 / six-regex resolver | **LANDED** | `tools/resolve-consumer-universe.mjs` rebuilt on the TypeScript compiler API — pinned `typescript 6.0.3` (throws on drift, lines 35-37), full AST projection of import/export/dynamic/require + bounded CSS/Sass/host-file grammars, every ambiguity a fail-closed `syntaxFailure`; S1's resolver imported no `typescript` at all. KL-7's four-root narrowing REJECTED with stated reason (`UNION-INGESTION.md`: "improving the source projector cannot erase known or discoverable consumers") — a lawful, more-conservative decline. Caveat: graded on code-read; this seat could not execute the tool (write-fence). |
| 2 | Governance lineage (LT-10/LT-16, proof:structure, depcruise) | **LANDED** (formation layer) | K00: "LT-10/LT-16, D50, EP-3/K-F6 and the live `proof:structure`/dependency-cruiser rules are absent from the formation, so moves could green by vacancy" — with W-GOV annex, exact `.dependency-cruiser.cjs` rule dispositions, thirteen-anchor re-derivation, "proof:structure EXTEND contract", and a gate that "fails on an unrowed live gate or vacuous rule". K22T re-keys, never replaces; D-8 executes option-c "EXTENDING proof:structure — never a parallel authority". The P0's cure is now the FIRST K-band wave. |
| 3 | Record-citation grep-gate law | **PARTIAL** | The records are cited in the owning rows (A02 "explicitly preserving D2 §3 P2" + retrigger "naming D2 §3 P2"; K14 EP-3 PATH-B/D-6; A09 W45; V16A/V16R SCI-1/D54/W56; K09 D-7) — practice is real. But R-03 claims "structured record_refs join; grep only secondary" while `record_refs` appears once (inventory encoding note) and **zero** times in any validator; `N-ADJ-3` = 0 hits corpus-wide; TA-4 unnamed in A12/A13 text. The law is practiced, not installed. |
| 4 | Parser spine un-blocked; 164343c1^ baseline; BBNF demoted | **PARTIAL — unblocked ✓, baseline DIVERGED (reasoned, recorded)** | The blocker is dead: OA-17 "Value consumes the exact published `@mkbabb/parse-that@1.0.0`… blockers are banked and routed"; UNION-INGESTION "CLOSED by KL-1/P00–P07: no unfrozen SK-V25 artifact enters Value"; INBOX I-15/I-16 fold the upstream campaign to a boundary with elapsed no-write windows. But the baseline inversion was not adopted: their adjudicated law keeps the BBNF-module→TS-combinator mirror as P01's spine and demotes `164343c1^` to "isolated behavior witness, never runtime" (P01 row) — the inverse of our row's organization. Substance overlap is high (P01 is handwritten parse-that combinators — the C12 engine we demanded), and the divergence was openly adjudicated by the union triad, not smuggled. §4 row 6. |
| 5 | C14 prototype NOW | **LANDED** | §2: REAL-AND-BENCHED, 17/17, reproduced by this seat. |
| 6 | Bench resurrection + regex measured once | **PARTIAL** | P05 resurrects the `164343c1^:bench/css-parse-perf.mjs` method, "one retiring-regex baseline only", 1/4/16/64/256 KiB sheets, scaling-exponent 95%-UB ≤ 1.10, isolated processes, "cache-hit result mislabeled throughput" forbidden. The C14 bench ran; the regex parser itself is still unmeasured (comparators honestly ABSENT). Spec landed; the measurement is P05 execution debt. |
| 7 | Six-key ratification | **LANDED-BY-RULING** | D-1 held the 7-key freeze; V00C rewritten: "RULING — D-1 terminally rejects the former six-key proposal: the root and seven keys survive, with only `./quantize` demoted." Skeptic B had caught the live six-key text pre-repair — the cure is witnessed, not asserted. |
| 8 | Zone-flip ratification rows + regenerated KEYFRAMES-TARGET-PATHS | **LANDED-BY-RULING** | K14 "RULING — D-6 holds FLIP; the prior prune was unauthorized"; K21 EP-3 PATH-B KEEP; K15 trim / K22 shrink per D-6; K09 retire per D-7 (the owner's own piercing, tombstoned). KEYFRAMES-TARGET-PATHS.json regenerated (8,045→6,577 bytes); `conditional_paths` absent — **moot by terminality**: with D-6…D-9 ruled, no conditional arms remain to type. |
| 9 | A02/A08/A09/A07T as named-overturn rows | **LANDED** | R-09 `CLOSED_BY_RULING` (D-10…D-13). A02 preserves D2 §3 P2 with the named A21J retrigger; A08 authenticated bound cursors (D-11); A09 "W45 already retired restore" (D-12); A07T→D12F featured-as-relation with `(visibility,tier)` retained (D-13). |
| 10 | SCI-1/D54/W56 inheritance; Into de-coupled | **LANDED** | V16A "inheriting SCI-1/D54", names `mapColorToGamutInto`/`safeAccentColor`; **V16R added to the DAG** as "the one W56 vehicle" — `mixColorsInto`/`toRgba8Into`/`convertColorInto`/`mapColorToGamutInto`/`sampleColorRampInto`, additive over immutable 4.0.0, tri-tag tombstones, "no `sampleBezier` revival"; V16B (SoA) separated and BANKED per D-19. Re-derivation replaced by named inheritance — the exact cure. |
| 11 | Raytrace to test side; cusp kernel wired | **LANDED** | V15: "Ray Trace test-only exact-boundary oracle… proves Ray is reachable only from external tests… No production challenger"; Local-MINDE named as the sole production policy; V18H/V18V each "an explicitly non-CSS client of the single cusp kernel"; "never-`css-*`" naming law kept ("no cusp/Halley CSS name"). |
| 12 | kf dispatch pack on P4.5 | **PARTIAL** | Owners exist (R-12 FOLD → K00/K04I/K09/V18R/P4.5; K00 carries a "P4.5 cargo manifest for parse/zone scars"; K09 rides the P4.5 boundary). But the cargo is unwritten: `backward/color.ts:120-124`, `backward.ts:30-32`, the easing consume-vs-ratify brief and the eight-zone regex census appear **nowhere** in S2 (grep: 0). Routed, not loaded. |
| 13 | CARRY-LEDGER §B–§F fold | **PARTIAL** | R-13/ADD-1 terminally map the fold into 29 named owners (G00I, D03…D25, V16R — "exact CARRY mapping; W50 restore superseded" per D-12), and the D-band demonstrably covers the surfaces (D06 picker, D08/D13/A13 export+shares, D19–D24 admin, D17/D18 plates, D25 route-audit with perf in scope). But the W46–W56 identifiers, **the CH-4 p75-LCP ≤ 2.5 s close-law and W54 D-1 aurora-derive appear in zero wave/gate texts** — both ratified close-laws live only as inventory routing, not as binding gate language in any owning wave. The fold is routed, not yet expressed. §4 row 1. |
| 14 | Apparatus diet | **LANDED** | tools/ 85→43 files, 2,260→780 KB; FORMATION-CLEAN-PASS-PROTOCOL.md 28,037→2,479 bytes; RETURN-CONTRACT.md 42,669→7,333; return.schema.json 59,990→5,169 (+ `RETURN-VALIDATOR-DEFERRED.json` — AM-26's deferral honored); P01 police reduced to "compact W0 independent-authorship pilot only"; selftests folded behind `--selftest`; reviews/ moved out of the corpus to `evidence/vnext-history/` (S2 `reviews/` empty — diet, not amnesia; the protocol's ≤25,000-byte core holds at ~25.1 KB). No regrowth observed. |
| 15 | BJ/IOS27 as BOUND input | **LANDED** | `DESIGN-INPUT-EPOCH.json`: `"state":"BOUND_FORMATION_INPUT"`, owner **G00I** (a real wave: "Bind the cross-app Breath of Life design epoch and reconcile it by numbered law before motion design"); IOS27-CODEX/SUFFUSION-MATRIX/BAND-FEEDBACK-MOTION sha256-pinned — all three byte-verified by this seat against the live glass-ui tree; a substantive reconciliation section (shared laws, per-app signatures, the idle ruling, measurement-first via C00P); G04/G05 gate on G00I; and the file is **inside the corpus epoch**, so motion waves cannot green without it. AM-23 satisfied structurally. |
| 16 | Foreclosed owner rows restored | **LANDED-BY-RULING** | V19 "RULING — the 11-day HDR drop is unjust" (D-17 BUILD); V05 "retire the static evaluator" (D-17 RETIRE); V25 Value-owned experimental `spring()` behind the C12 flag with the K10 solver seam (D-16); SoA banked to V16B's workload law (D-19). Fiat replaced by rulings, exactly as demanded. |
| 17 | Boundary re-shape | **LANDED** | D-3 GO entry-gated undated; V16R lands **before** the boundary (G07 and C-band depend on it); G07: Glass 7.x minor only, "executed peer matrix proves both pre-boundary refusal and boundary success" (the two-leg ERESOLVE law); no Glass major from a value tranche (D-2); R-17 `CLOSED_BY_RULING → V16R,G07,C00,C05,C10`. |
| 18 | Deps strip as standing gate | **LANDED** | V00B is "the first mutation": runtime-dependency allowlist "`⊆ {@mkbabb/parse-that}` with exact `1.0.0`", the v1.1.0–v2.0.1 self-dependency prior-art refusal, retro-tombstone backfill, "exactly one permitted published parse-that dependency when present." |
| 19 | Seed bijection extended + recipient-existence rule | **LANDED** (inventory layer) | `CANON-SUPPLEMENT-INVENTORY.json` binds the five omitted inputs and "all 35 existing identities (C1–C23, Carry §B–§F, V′ §0–§4 and both receipts)" without disturbing the 41+108 seed law; `validate-union-inventory.mjs` resolves 114 owners against the 193-wave books and "rejects missing supplement identities/recipients"; pass-certified "149 seeds, 39 boundaries and 114 amendments mapped to owners." Not executed by this seat (write-fence); presence + pass command + the union adjudicator's step-1 demand ("make recipient existence fail closed") triangulate. |
| 20 | Completions | **PARTIAL** | `WAVE-ROUTING-PI.md` carries the Routing/π cell (model_served, E13 mail law, phase labels) and every wave row carries an `R0/R3 · π` cell; D50 named in K00; R-20 folds the rest to PROVENANCE/G00I/D25/M11/C01/C02/C07. Unverified by this seat: the atlas 3-site chase ledger, pin-currency note, PNG-row surfacing; `N-ADJ-3` absent corpus-wide. |

**Score: 12 LANDED (of which 4 landed-by-ruling) · 5 PARTIAL · 0 MISSING · 1 of the PARTIALs (row 4) is a recorded reasoned divergence rather than a gap.** Nothing was silently dropped: every row has a terminal crosswalk entry with named owners, and the three inventions the crosswalk added (G00I, C00P, V16R) are exactly the three the hostile triad ruled irreducible.

---

## §4 — THE RESIDUAL LEDGER (the next letter's spine; priority-ordered)

| # | Residual | Pri |
|---|---|---|
| 1 | **The two ratified close-laws lack binding gate text.** CH-4's p75-LCP ≤ 2.5 s and W54 D-1 aurora-derive exist only as inventory routing (R-13); no wave gate states the number. Demand the law verbatim in the owning gate (D25 or D03/D05) — a ratified close-law that no gate can fail on is a vacuous-green seed. | P1 |
| 2 | **The P4.5 dispatch cargo is unwritten.** Scar coordinates (`backward/color.ts:120-124`, `backward.ts:30-32`), the easing consume-vs-ratify brief, the eight-zone regex census: 0 corpus hits. Owners exist (K00/K04I/K09/V18R); the packet does not. First K-band execution must not open without it. | P1 |
| 3 | **Record-citation enforcement is practice, not machine.** R-03 claims a "structured record_refs join" that no validator implements; N-ADJ-3 uncited anywhere; TA-4 unnamed in A12/A13. Either install the gate (it is a grep; it is cheap — our own words) or re-grade R-03 from REWRITE to practice. | P1 |
| 4 | **The certified epoch is already unreproducible from live bytes — by OUR hand.** The epoch hashes live mutable externals; `RUN-STATE.md` has drifted from its pinned `bed84b9d…` (current `56216a8a…`), so `corpus-epoch.mjs` today yields `c5767352…`, not `61d4954f…`. Their 2/2 is true of a moment and cannot be re-proven without a new epoch. Joint fix: next epoch pins content-addressed COPIES of externals; our root session must treat RUN-STATE edits as epoch-invalidating events. | P1 |
| 5 | **The retiring regex parser is still unmeasured.** Row 6's "measure ONCE for the record" survives as P05 execution debt; the C14 comparators were honestly ABSENT. The record must exist before V03 retires the engine. | P1 |
| 6 | **The parser-baseline divergence is adjudicated but not owner-ratified.** BBNF-mirror-primary (theirs) vs restoration-primary (AM-1, ours) was never a docket row; both retire the regex engine into parse-that 1.0.0 combinators. If the owner cares about the organizing principle, it needs one D-row; otherwise record acceptance and close. | P2 |
| 7 | **Read the 2/2 at its true depth.** 12 domains × 1 focused command per critic certifies mechanism health + spot facts; the row-level hostility lives in the union triad and band adjudications. The next formation must not cite "2/2" as per-wave verification. | P2 |
| 8 | **Same-route seal.** All six clean-pass seats are gpt-5.6-sol (authors' route). Owner-accepted (D-4/D-5 substitution) — but the standing record should say the seal has prompt-freshness, not model-diversity; our Fable-side spot-probes (this document is one) are the compensating control. | P2 |
| 9 | **Evidence hygiene in the union triad.** All three `vnext-union/` files open with an identical 245-line input-echo (the "Converged tranche: W" read-only-mount dump) before the independent bodies begin at line 246. It superficially reads as three seats claiming a *different* convergence. Label input-echo blocks as such. | P3 |
| 10 | **Completions residue.** Atlas 3-site chase ledger, pin-currency reconciliation note, PNG-row surfacing: unverified; N-ADJ-3 absent. Small, but §3 row 20 stays PARTIAL until named. | P3 |
| 11 | **Empty `reviews/` needs a signpost.** S2's `reviews/` is an empty directory; R1–R4 and the triads live in `evidence/vnext-history/` + `evidence/vnext-union/`, evidence-only. One README line prevents a future seat from reading the emptiness as loss. | P3 |
| 12 | **This seat's execution gap.** Rows 1 and 19 were graded on code-read + pass-certification, not by running the projector/inventory validators (write-fence + node restriction). A root-session `--selftest` run of `resolve-consumer-universe.mjs` and `validate-union-inventory.mjs` would close the last verification hole cheaply. | P3 |

---

## VERDICT

**The convergence is REAL — at the formation layer, which is the only layer it claims.** The two consecutive clean passes are genuinely fresh in structure (six seats, empty critic inputs, hash-chained adjudications), ran over a frozen epoch that this seat independently recomputed to the exact certified digest — an epoch that hash-binds our entire union canon, the owner rulings, and the BJ/IOS27 design corpus — and were sealed by validators whose 31-mutation selftest I enumerated by hand and whose fail-closed ceiling demonstrably rejected their own adjudicator's first report; the C14 prototype is real parse-that-1.0.0 combinator code whose 17 tests pass and whose benchmark this seat re-ran to matching numbers on the identical tree; and the 20-row repair ledger lands 12, partials 5, and misses none, with the sole P0 (the kf parallel-authority) cured as the first K-band wave and every previously-silent record overturn now a named ruling. What convergence does NOT mean: production is 0/193 by their own text inside the epoch; two ratified close-laws still bind nothing; the P4.5 cargo is unwritten; the record-citation gate is practiced but not installed; the seal is same-route and, thanks to our own RUN-STATE.md edit, already unreproducible from live bytes. The fleet that a week ago re-founded governance on a parallel authority and shipped a 0-byte prototype has consumed a hostile 114-row crosswalk, repaired eleven exact-locator contradictions, and earned its 2/2 the way we demanded it be earned — the residual ledger above is real work, but it is a punch list, not an indictment.

— Adjudication seat, `claude-fable-5`, 2026-07-20. Sole write: this file.
