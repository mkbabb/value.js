# UNION ADJUDICATION — THE PARSE PROGRAM + THE COLOR PROGRAM (armC)

**Union adjudicator:** Fable seat (`claude-fable-5`), 2026-07-19.
**Subject:** the Codex V-next formation (frozen snapshot `…/apotheosis/snapshot-vnext`, 2026-07-19 21:29 — P band 8 waves + V band 44 waves, `PARSER-CSS-COLOR.md`, `DISPOSITIONS.md` parser/CSS-color tables) **versus** the Fable-side finals (`armB/final-PA-parse.md` — 7 waves + W0 pilot + OD-PA-1..8; `armB/final-PB-color.md` — 8 waves + docket), both graded against the binding canon (L1 charter §2/§3, L2 packets P1/P2, CONVERSATION-ADDENDA C1/C12/C13/C14/C15/C21).
**Inputs consumed:** Arm-A `VERDICT.md` + `audit-parse.md` + `audit-color.md` + `audit-packets.md` + `inventory.md`; the two armB finals; the Codex primary artifacts read directly this seat (`waves/P-V.md` whole, `PARSER-CSS-COLOR.md` whole, `DISPOSITIONS.md` whole).
**Method:** Arm-A's load-bearing zero-hit claims were spot-re-run this seat (`CSS_NATIVE_KEYWORD`/`consume-vs-ratify` = 0 · `sampleColorRamp|deltaEOK|backward/color` in waves/coordination = 0 · `mapColorToGamutInto|safeAccentColor` = 0 · `U-F29|U-F30` = 0 · `164343c1` = 0 — all confirmed on the frozen snapshot). Grading is symmetric: **the Fable side gets no home-team bias**, and §4 names every axis on which it loses.

**Headline:** the two formations fail in **mirror image**. The Codex built the deepest spec-coverage and machine-verification apparatus this constellation has seen, and broke the charter's *governance* spine (restoration recipe dropped, spine externally gated, decided rows re-opened, owner rows foreclosed). The Fable side built the charter-exact restoration/vehicle/governance spine at C21 scale, and is *thinner* exactly where the Codex is deep (standards machinery, operation-vector honesty, current-ED spec correction). The union is therefore not a coin flip per element — it is **Fable's spine wearing Codex's instruments**.

---

## §1 — PER-ELEMENT VERDICT TABLE

Severity of "why" is compressed; every claim traces to Arm-A rows (verified) or this seat's direct reads/greps. **UNION ruling** column is binding for the union wave-delta (§2).

### Parse program (L1 §2 · P1 · C1/C12/C14/C15)

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| P-1 | parse-that readopted as published; no novelty; ≤1.1 ceiling (P1.6, C1, C15) | **CORRECT (exceeds)** — immutable 1.0.0, P04 signed `unchanged-1.0.0-no-republish` receipt, all novelty routed external (DISPOSITIONS rows 1–10); stricter than C1's "minor fixes if needed" but safe | **CORRECT** — zero-motion default + fix-on-trip contingency rider on W3 (typing-only 1.0.x, C15 ceiling), truer to C1's letter | **MERGE**: Fable's rider law (C1 permits minor fixes; a hard zero-mutation vow over-freezes) + Codex's machine receipt (the no-link tarball/no-republish check is a cheap, real gate — adopt as a registry-row check on W3) |
| P-2 | R-PARSER = restoration of the deposed incumbent (`164343c1^:src/parsing/`), /css signatures frozen (L1 §2 w1, P2.2 r4) | **MISSING/WRONG** — `164343c1` = 0 hits corpus-wide; R-PARSER realized as a greenfield handwritten BBNF-mirror (P01→V03); the signature/six-key freeze sub-clause IS honored; the named recipe silently dropped, no owner row | **CORRECT** — W3 restoration-by-retype, honestly named (10/18 deposed files import the dead OO model; skeleton reused, semantic layer transposed), recipes verified on tree (`164343c1^` = `a992b8e6`, 19-file tree), consumer gate in-swap | **ADOPT-FABLE** W3 as the parser spine. The Codex BBNF-mirror is DEMOTED to the additive C14 twin (see P-7), never the sole source. Codex's V03 serializer/recovery/span contracts survive as C12-lane targets (see P-6) |
| P-3 | Self-contained spine (the charter recipe EXISTS NOW; E-C16 isolation) | **WRONG** — the whole V03→V29T spine hard-blocked on the unfrozen external SK-V25 GPT campaign ("P01 cannot execute from the 15-file formation snapshot"; P00 gates "after—not before—the active CSS union closes"); P01 `born_red`, 0 authors | **CORRECT** — zero external dependency; the BBNF exemplar is read-only reference; upstream needs route as PT-E ask letters | **ADOPT-FABLE**; **KILL the SK-V25 gate** (Codex-sourced kill, Arm-A repair #3): the upstream campaign becomes a reconciliation ask-letter on the PT-E channel, never a dependency of value's parser |
| P-4 | C14 "prototyped and benchmarked NOW" | **MISSING** — `P01-INDEPENDENT-AUTHORSHIP.json` deliberately born_red, **0 author receipts, 0 prototype bytes**; all authorship deferred behind the external freeze | **PARTIAL** — W0 is phase-labeled formation work targeting the real risk (the ValueUnit→v4 retype + memo-defeat + the one regex record), but at adjudication time it too is **a spec, not bytes** | **BOTH-SHORT — EXECUTE W0 as the union's first act.** Adopt Fable's W0 shape; adopt a sane slice of Codex's authorship rigor (isolated author, frozen read-set, differential fixtures) at pilot scale — not the 211 KB police apparatus |
| P-5 | Bench restore as regression witness + measure the retiring regex parser ONCE (L1 §2 w2, P3.4-5) | **PARTIAL** — forward-only: V03 co-scaling budget + P05 statistical latency/throughput vs the 1.0.0 control; no `bench/` resurrection, no `css-parse-perf.mjs`, no regex baseline (grep 0) | **CORRECT** — W0 record measurement (memo-defeat rule named: 7 memoized entry sites, LRU-4096) + W2 ratio gate (MB/s+ns/call+co-scaling) with honest RED (ratio-unmet-on-real-measurement) + 3-iteration deadlock→owner escalation | **ADOPT-FABLE** W0/W2; **graft Codex's harness discipline** (statistical methodology, allocation/retained-heap/bundle evidence, no-conformance-claims-from-microbenchmarks) into the W2 harness spec |
| P-6 | C12 spec breadth: July-2026 universe, CSSOM, WAAPI, DOM-congruent typed values | **CORRECT (exceeds — the Codex's strongest parse asset)** — V01 `standards-lock.json` (CSSWG/Snapshot closure, Webref, WPT projections) + V02 generated-facts/reviewed-support/feature-matrix total join + the **operation-vector algebra** (parse/type/evaluate/resolve/adapt/serialize×3; `preserved_syntax_only` can never green semantics; typed refusal codes; capability delegation) + the V04–V29 domain decomposition (definition matcher, numeric eval, substitution, selectors, queries, rules, CSSOM V10I/V10S/V10D, Typed OM V11, easing/animations/timelines/triggers V25–V28) | **PARTIAL** — W1 census + W5 execution lane are real but far thinner machinery; OD-PA-1 re-asks a scope question C12's "ENTIRE CSS spec" already leans decided — surfacing the pricing is right, risking a narrowing is not | **ADOPT-CODEX machinery onto Fable's frame**: V01/V02 + the operation-vector algebra become W1's machine form; the V04–V29 domain decomposition becomes the W5 execution program's wave stubs (C8's 50+-wave scale authorizes it). OD-PA-1 survives **re-scoped to sequencing/pricing only** — default-IN, never scope-narrowing |
| P-7 | C14 dual definition: idiomatic BBNF modules + parse-that TS in `src/css/`, congruence enforced | **CORRECT-partial** — the 15↔15 module-isomorphism topology (`CSS-MODULE-ISOMORPHISM.json` + AST-grade validator, zero-correction edges, no-second-scanner law) is faithful in SHAPE; but sole-source, externally gated, 0 bytes | **CORRECT-partial** — W6 behavioral-equivalence gate (shared conformance corpus, outputs diffed both directions), name-map refused per the owner's retired grep-ledger genre; BBNF home = in-value (OD-PA-5, C1-compliant); tombstone named-and-overturned per P4.4 | **MERGE — run BOTH gates**: Codex's machine module-join as the *topology* witness (it is AST-executable, not the condemned grep genre) + Fable's shared-corpus output-diff as the *semantic* witness. BBNF twin authored in-value (OD-PA-5); upstream reconciliation by ask letter |
| P-8 | Owner-decision rows: spring() grammar (P2.2 r11, K F6.6), R-HDR (r10), R-EVAL (r12) | **WRONG procedurally** — all three foreclosed by fiat: spring **Reject** (DISPOSITIONS:28), HDR **built** (V19), eval **built** (V05); each factually defensible (spring is genuinely not standard CSS; HDR arguably subsumed by C12), none presented to the owner; K F6.6 uncited | **CORRECT** — OD-PA-2/3/4 pre-swap docket rows with leans (HDR keep-lean, spring value-lean, eval retire-lean) and excise-at-cut mechanics + §7-1 tombstone duty on every NO | **ADOPT-FABLE docket; attach Codex's findings as evidence annexes** (spring-not-in-CSS strengthens a Reject recommendation but the owner signs; if HDR rules KEEP, adopt Codex V19's capability-delegation/no-hidden-clamp spec as the implementation content; if eval rules KEEP, V05's capability taxonomy is the shape) |
| P-9 | kf boundary (P1.4): census-lock, easing.ts:30/:38-39 consume-vs-ratify, WAAPI leverage route | **PARTIAL** — K02 "reject duplicate CSS ownership" + K09 ingest-on-value-spine carry the boundary LAW; the ONE named live row (easing.ts classifier) is unadjudicated (grep 0); kf gains a full generated-WAAPI universe (K04I/K04/K06) — beyond the seed | **CORRECT** — W7 single INBOUND letter: OD-PA-8 RATIFY-lean brief (both dispositions priced), the **eight-zone** kf regex census (addendum-2 made real, n=8 not n=1), the WAAPI-ENGINE route carrier, census-lock labeled standing-regression-lock (not fake born-RED) | **MERGE**: Fable's W7 letter is the carrier; **Codex's K04I generated-WAAPI-universe becomes the route's named destination spec** (the kf successor implements it) — the two halves are complementary, not rivals |
| P-10 | Ad-hoc-fix family U-F29/U-F30 (P1.5 — track families, not instances) | **MISSING** — grep 0; no defect-family register | **CORRECT** — W3 family regression tests + register row; W4 extends U-F30 (color-mix serialization) | **ADOPT-FABLE** |
| P-11 | Addendum-2 regex abrogation, BOTH repos | **PARTIAL** — value side strong (V03 forbids regex rescan/second scanner; the greppable no-regex law); kf-wide census absent | **CORRECT** — charter reconciliation ruling (parse-that's own `regex()` leaf is lawful engine machinery) + W7's 8-zone kf census with per-site parser-vs-classifier adjudication | **ADOPT-FABLE ruling + census; KEEP Codex's V03 no-regex-rescan gate text** verbatim in W3's predicate (it is the sharpest formulation either side wrote) |

### Color program (L1 §3 · P2 · C13)

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| C-1 | R-DELTAE (ΔE-OK/2000/ITP, prerequisite) | **CORRECT** — three independent numeric contracts (V15 + color contract law), JND evidence | **CORRECT** — F-01 as program root per the ledger's own priority; as-was recipes verified (`164343c1^:src/units/color/difference.ts` + `gamut.ts:65ff`); no vacuous heap gate on scalar-tuple signatures | **ADOPT-FABLE wave** (root placement, recipes, honest gate); the two sides agree on substance — convergent independent derivation |
| C-2 | Gamut conformance LAW (the seed said "§13, cusp+Halley conformant") | **CORRECT (exceeds — the Codex's strongest color asset)** — pinned the 2026-07-15 ED and found the seed imprecise: §14.2 permits **Local-MINDE / EdgeSeeker / Ray Trace**, requires ONE consistent selection; EdgeSeeker `refused-at-evaluate` (pseudocode incomplete); cusp/Halley can never carry a `css-*` name; browser strings are not the used-value oracle | **PARTIAL** — F-03's cure engineering is right (JND stop + local clip on the extant bisection = Local-MINDE in substance, corroborated by the pre-v4 tree's own §13.2 comment), but it never pins the current ED, never names the three-algorithm law, never states the selection explicitly — the seed's imprecision passed through unexamined | **MERGE**: F-03 AMENDED with the Codex law — exact ED pin, the named selection (**Local-MINDE, the both-sides control**), the consistency requirement, EdgeSeeker's honest refusal, the never-`css-*` naming law, browser-not-oracle. The union's spec-reference generator implements the SELECTED policy by name |
| C-3 | Production policy count + raytrace's role (E-P2.2-R-GAMUT: raytrace = TEST-side exact oracle) | **PARTIAL/WRONG** — raytrace INVERTED to a production challenger candidate; no test-side oracle anywhere; no raytrace.ts/boundary.ts in the target tree; a dual-candidate production posture | **CORRECT** — raytrace resurrected TEST-side as-was (`test/color/oracle/raytrace.ts`); the analytical engine must match the §13 reference within JND AND agree with the oracle at the boundary | **ADOPT-FABLE**; **KILL the Ray-Trace-as-production-challenger arm** (one selected policy, per Codex's own consistency law — the union turns the Codex's law against its own dual posture). Raytrace lives in test/ |
| C-4 | Cusp/Halley engine (R-GAMUT = DECIDED restore, P2 ledger) | **WRONG** — V15P converts the decided restore into terminal KEEP-or-PRUNE with an owner-precut deletion annex (re-opens a decided row; the r2 ledger nowhere authorizes pruning it); the naming law itself is correct | **CORRECT** — F-04 part-restore (sRGB Ottosson as-was, 9 Halley hits verified) / part-NEW (multi-gamut coefficient generalization priced as novel numerics, per the pre-v4 tree's own "sRGB-fit" comment); facade frozen | **ADOPT-FABLE F-04 + Codex's naming law** (explicitly non-CSS policy name). **KILL V15P's prune arm**; its worth-documentation demand survives as a docket annex if the owner wants the record |
| C-5 | R-OKHSL/OKHSV (reuse P2's cusp math; no duplicate solver) | **PARTIAL** — V18H/V18V made independent terminal KEEP/PRUNE (a milder decided-row re-open) with a real DAG gap: deps = "V12, V16A" only, no V15P edge → a V15P PRUNE strands them or forces duplicate cusp math (the exact thing the edict forbids) | **CORRECT** — F-08 reuses the F-04 cusp kernel (one kernel, two clients); corrected recipe (`…/gamut/okhsl.ts`); restore per the ledger | **ADOPT-FABLE** F-08 (the DAG gap dissolves with V15P's death); **fold Codex's worth-evidence demands** (independent reference vectors, perceptual/job analysis, bundle cost) into F-08's gate CONTENT — evidence discipline without prune-eligibility |
| C-6 | R-INTO / SCI-1 / D54 / W56 (inherited-DECIDED; hot paths by name) | **WRONG/MISSING** — `SCI-1`/`D54`/`W56`/`mixColorsInto`/`toRgba8Into`/`mapColorToGamutInto`/`safeAccentColor` ALL grep 0; the Into family re-derived fresh (V16A); the 4.1.x vehicle vanished into a single 5.0.0 with no reconciliation; mix/ramp Into explicitly excluded from V16A and punted to prune-eligible V16B | **CORRECT** — F-05 inherits D54 (idiom = the blessed out-buffer shape, twice-precedented), rides the W56 4.1.x vehicle, names the hot paths, and correctly places the alloc-collapse (facade allocs die HERE, not in the conformance wave) | **ADOPT-FABLE F-05**; **graft Codex V16A's measurement law** — "no observed V8 heap allocation at named warmed boundary", DCE/deopt/escape-analysis-aware, exact Node/V8/CPU/OS protocol — as the bench-row proof standard (the sharpest zero-alloc gate text either side wrote) |
| C-7 | R-SOA (3.0.0 tombstone stands; new-primitive only) | **PARTIAL** — V16B's terminal-evidence law is GOOD (end-to-end benefit on pinned weighted real workloads, identical outputs, no synthetic-only adoption) but it holds mix/ramp Into hostage (coupling defect) and re-derives what the seed marked decided | **CORRECT** — banked-with-trigger docket row; threshold pre-ratified before any exhibit runs (no post-hoc goalposts); null hypothesis = Into loops suffice (C21) | **MERGE**: Fable's banked row + trigger discipline; **Codex's end-to-end workload law becomes the trigger's evidentiary standard** if it fires. **DE-COUPLE**: mix/ramp Into land unconditionally with F-05 (union kill of the V16A-exclusion/V16B-hostage structure) |
| C-8 | R-RAMP + the kf scar cure (P2.2 r5, P2.1) | **PARTIAL** — V18R restores N-stop ramps well (independent scalar oracle, endpoint/continuity invariants, zero/one/limit/`+1`); the paired kf backward-emit re-adoption + scar deletion is MISSING (grep 0 in K band + coordination) | **CORRECT** — F-06 with the dispatch-time grep census (the frozen 2-site list was already stale: 5 sites live at kf HEAD) + the kf pin-advance rider (kf pins value EXACT 4.0.0 — manifest-falsifiable) + de-vacuized letter gate | **ADOPT-FABLE F-06**; **fold Codex V18R's oracle/invariant gate content** into F-06's vector family (independent scalar reference, endpoint drift refusal) |
| C-9 | R-CONTRAST (grammar + WCAG-republish decision) | **CORRECT on substance** — V20 is the best contrast spec either side wrote: CSS syntax/outcome model separate from named WCAG/APCA policies, browser disagreement recorded not flattened, compositing/candidate capabilities, typed refusals; the republish DECISION foreclosed (built, not docketed) | **CORRECT on governance** — F-07 evaluation half + OD-C1 republish docket (both branches costed; executable under either ruling); grammar half ceded to PA-parse by named seam | **MERGE**: Fable's ownership split (grammar→parse, evaluation→color) + OD-C1 docket; **Codex V20 adopted as the implementation spec** under either OD-C1 ruling |
| C-10 | Drop census / retro-tombstones (§7 law 1; the capability-diff gate is blind to pre-4.0.0 drops) | **PARTIAL** — V12's gate ("tagged/current/packed semantic losses have tombstones or restored vectors") + V30's `api-capability-diff.json`+`tombstones.json` are real anti-drop machinery, but no tri-tag (v3.1.0↔4.0.0↔HEAD) census exists and the named unrowed families (colorFilter, GAMUT_SECTOR_COEFFICIENTS, HDR raws, per-pair fast paths, sampleGamutBoundary/Into) appear nowhere | **CORRECT** — F-02 is the organ: tri-tag export census, every prior symbol → terminal row, families named at birth, retro-tombstones staged for the first 4.1.x CHANGELOG; cures the gate's structural blindness | **ADOPT-FABLE F-02; wire its outputs into Codex's V30 artifact shapes** (`tombstones.json` / capability-diff format) so one machine format serves both the retro and forward halves |
| C-11 | C13 zero-alloc law honesty | **CORRECT** — "zero-allocation claims apply only to named, warmed numeric kernels with caller-owned output; parser/diagnostics/DOM/UI carry no blanket claim" | **CORRECT** — structural signature enforcement + ONE bench spine, budgets ratified in-wave vs the measured 9-alloc datum (never pre-committed); 17 spaces (not 16) corrected | **MERGE both texts** — the two sides independently derived the same honest law; the union charter carries Codex's scoping sentence + Fable's structural-enforcement + ratified-datum discipline |
| C-12 | R-BOUNDARY (gated on the W53 plate rebuild) | **CORRECT with a naming gap** — banked in SEED-ROW-INVENTORY (H-T-L270) with the substantively-right retrigger ("D17 proves the plate requires a reusable sampler"); only the W53 vehicle NAME is absent (Arm-A refuted the "silent drop" reading) | **CORRECT** — banked on the named W53 pull; the full π/DELTA obligation vs the tag's reference captures travels with the trigger | **ADOPT-FABLE row wording** (names W53 + the π/DELTA travel clause); credit the Codex disposition as independently correct |

---

## §2 — THE UNION WAVE-DELTA (parse + color domain)

The unioned mega-tranche's base spine for these two domains = **Fable PA W0–W7 + PB F-01..F-08** (self-contained, decided-row-faithful, C21-sized), amended as follows. Every amendment names its source side.

### ADOPT (waves taken whole)

1. **PA W0** (Fable) — the C14-NOW pilot: retype pattern + memo-defeating harness + THE one regex record measurement. **Executes first**; the union has zero prototype bytes from either side until it runs.
2. **PA W2, W3, W4, W7** (Fable) — bench witness · restoration-by-retype swap (consumer gate in-swap; manifest WHITELIST for the parse-that runtime dep) · typed exposure/serialization/vector-flip · the kf INBOUND letter.
3. **PB F-01, F-02, F-05, F-06, F-08** (Fable) — ΔE root · drop-census organ · Into-on-D54/W56 · ramp+scar dispatch · OKHSL/OKHSV on the shared cusp kernel.
4. **Codex P04's no-republish receipt** (Codex) — as a machine registry-row check attached to PA W3 (parse-that `^1.0.0` re-added, registry-exact, no-link install proof).

### AMEND (merged waves, both sources)

5. **PA W1 ← Codex V01/V02**: the census's machine form becomes `standards-lock.json` + generated-facts/reviewed-support/feature-matrix total join + the **operation-vector algebra** (Codex text adopted near-verbatim: `preserved_syntax_only` greens only parse/exact/edit; refusal is typed; maturity/interop are independent axes). Fable's docket-before-cut sequencing law unchanged.
6. **PA W5 ← Codex V04–V09, V10I/V10S/V10D, V11, V22–V28**: the C12 execution lane inherits the Codex domain decomposition as its wave stubs (definition matcher, numeric eval, substitution, selectors, queries, rules, CSSOM facts/snapshots/adapters, Typed OM, image union, easing/animations/timelines/triggers) — scheduled across the mega-tranche under C8's 50+-wave scale. Codex's three-serializer law (exact/edit/canonical) + `SourceText` boundaryMap + `resource_limit`-at-`+1` bounds enter here as target capabilities, **not** as blockers on the W3 swap. OD-PA-1 re-scoped: sequencing/pricing only, default-IN.
7. **PA W6 ← Codex CSS-MODULE-ISOMORPHISM**: dual congruence gate — Codex's AST-grade module-join (topology witness) + Fable's shared-corpus behavioral diff (semantic witness). BBNF twin in-value per OD-PA-5.
8. **PB F-03 ← Codex V15 law**: exact 2026-07-15 ED pin; the three-algorithm §14.2 record; the explicit production selection (**Local-MINDE** — both sides' control); EdgeSeeker `refused-at-evaluate`; the never-`css-*` naming law; browser-strings-not-oracle. Fable's small-diff cure + spec-reference vector generator unchanged.
9. **PB F-04 ← Codex naming law**; **PB F-04/F-05 bench gates ← Codex V16A measurement protocol** ("no observed V8 heap allocation at named warmed boundary", DCE/deopt/escape-analysis-aware, pinned Node/V8/CPU/OS).
10. **PB F-06 ← Codex V18R gate content** (independent scalar oracle; endpoint/continuity invariants; zero/one/limit/`+1`).
11. **PB F-07 ← Codex V20** as the implementation spec under either OD-C1 ruling (named WCAG/APCA policies; browser disagreement recorded; typed refusals).
12. **SoA docket row ← Codex V16B evidence law**: the pre-ratified trigger fires into an end-to-end pinned-real-workload proof, never synthetic-only.
13. **Owner docket OD-PA-2/3/4 ← Codex evidence annexes**: spring-not-standard-CSS (strengthens Reject recommendation, K F6.6 cited); HDR-under-C12 (V19's capability-delegation spec = the KEEP-branch content); eval (V05's capability taxonomy = the KEEP-branch shape). **The rows go to the owner** — nothing is foreclosed.

### ADD (new rows the union creates)

14. **Zero-drop crosswalk row** (union-sourced, from Arm-A repair #15): the Codex seed-bijection instrument (149 rows, SHA-pinned, mutation-RED-verified) is adopted as the union's zero-drop format AND extended over the CONVERSATION-ADDENDA universe (C1–C23) it was structurally blind to.
15. **Provenance row**: the union's parse/color waves re-pin trees at execution (G0′; Codex's pins are current-HEAD, Fable's match; both disclose — carry the reconciliation note vs the seed pins `db77dbd8`/`0dac636b`).

### KILL (with source of the kill)

16. **Codex P00/P01's SK-V25 external gate** (killed on Fable + Arm-A evidence): the value parser spine never waits on an unfrozen external campaign. Upstream reconciliation = PT-E ask letter.
17. **Codex V15P's prune arm** (killed on the seed's decided-restore authority): cusp/Halley is restored (F-04), named non-CSS (Codex law kept), never prune-eligible in this tranche.
18. **Codex's Ray-Trace production-challenger arm** (killed by Codex's OWN consistency law): one selected production policy; raytrace confined to test/.
19. **The V16A-exclusion/V16B-hostage coupling** (killed on Arm-A + Fable evidence): mix/ramp Into are unconditional F-05 cargo.
20. **The P01 dual-author police apparatus at formation scale** (killed on C21): authorship rigor survives at W0-pilot scale; the 211 KB/0-byte inversion does not.
21. **Fable's OD-PA-1 as a scope question** (killed on C12's own text — a self-kill against the home team): "ENTIRE CSS spec" is decided; only sequencing/pricing goes to the owner.

---

## §3 — APOTHEOSIS NOTES

### What the Codex teaches the Fable side

1. **Re-derive the spec at read, even against the seed.** The §14.2 three-algorithm discovery corrected the CHARTER's own imprecision ("cusp+Halley = §13-conformant" was never true). The Fable color final — thrice-adjudicated, archaeology-verified — still inherited the seed's framing unexamined. Standing law for the union: every conformance claim pins the current ED/draft by digest, not the seed's paraphrase.
2. **Operation-vector honesty beats scalar status.** "Supports X" is a close-class lie generator; the Codex's parse/type/evaluate/resolve/adapt/serialize×3 vector with typed refusal makes partial support un-lieable. The Fable census rows were scalar by comparison.
3. **Machine-checked ledgers.** The 149-row SHA-pinned seed bijection with mutation-RED verification is the strongest zero-drop instrument the constellation has produced — the Fable side's row-landing tables are prose by comparison. Adopt the instrument, extend its universe.
4. **Measurement-law sharpness.** V16A's allocation-gate formulation (DCE/deopt/escape-analysis-aware, "no observed V8 heap allocation at named warmed boundary") and V18R's independent-scalar-oracle law are better gate TEXTS than the Fable equivalents; the union lifts them verbatim.
5. **State-reporting honesty as culture.** 0/190 production, 0/2 clean passes, a self-issued NOT-CLEAN as the newest artifact — no vacuous green anywhere. The Fable side has nothing to be ashamed of here, but the Codex made honesty *load-bearing infrastructure*.

### What the Fable side teaches the Codex

1. **Archaeological continuity is cheaper than greenfield.** The deposed tree already contains color-mix/relative-from/contrast/HDR/spring productions, the Ottosson engine, the raytrace oracle, and 11 benches; `git show 164343c1^` is a one-command recipe. The Codex re-authored (or planned to re-author) what exists — and got 0 bytes because its greenfield source was externally gated.
2. **Never gate your spine on someone else's unfrozen campaign.** Self-containedness is a structural property of a good plan; the Codex converted the tranche's central vertical into a hard downstream of an uncontrolled fleet — the exact fragility class the isolation law names.
3. **Decided rows are INPUT.** SCI-1/D54/W56, the P2 restore ledger, the r2 zone verdicts — inheritance is not optional context, it is the contract. Re-derivation without a supersession row is a silent re-open even when the fresh derivation is competent. The deletion-judgment protocol is sound engineering that was **aimed at the wrong targets** (decided restores, KEEP-EARNED zones).
4. **Owner rows are the owner's.** Three designated decisions (spring/HDR/eval) were foreclosed by fiat — each factually defensible, all procedurally wrong. The Fable docket (recommend + price both branches + excise-at-cut mechanics) shows how to be opinionated without usurping.
5. **Right-size the apparatus (C21).** ~75% apparatus / ~4% deliverable, 316 KB of forensics for an admittedly unanswerable question, 211 KB of police for a 0-byte prototype — versus 15 waves + 2 dockets covering the same two domains. Honest naming belongs here too: the Fable side's "restoration-by-retype" and "authored-RED vs born-RED" distinctions kept its own gates from inflating.
6. **Boundary work needs a carrier.** kf ramp-scar deletion, the easing.ts consume-vs-ratify row, the atlas chase sites — cross-repo obligations die without a named dispatch letter; the Codex's coordination machinery (excellent for BBNF) never carried the kf color/parse rows.

### The mirror-image lesson (both sides, one sentence each)

- The Codex built a formation that **verifies everything and inherits nothing**.
- The Fable side built a formation that **inherits everything and instruments less** — the union exists because each side's defect is the other's strength.

— union adjudicator, `claude-fable-5`, 2026-07-19. Written under the isolation law to `armC/union-parse-color.md`; no repo file touched.
