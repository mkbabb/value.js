# ARM-C UNION — API/ROUTING · GOVERNANCE/CO-LAND · PROCESS/SCALE/CONTRACT

Union adjudicator seat (`claude-fable-5`), 2026-07-19. Inputs read whole: Arm-A
`VERDICT.md` + `audit-api-routing.md` + `audit-contract.md` + `audit-apparatus.md` +
`inventory.md`; Arm-B `final-PE-api.md` + `final-PF-governance.md`; the binding canon
(L1 charter, CONVERSATION-ADDENDA; packets via the edict citations); the FROZEN Codex
snapshot's `OWNER-AMENDMENTS.md`, `waves/K-A.md` A-band (A00–A26, all 36 rows),
`waves/M-C.md` C-band (C00U–C10, all 19 rows), `STATE-ROUTING.md` head, plus my own
re-run greps over the whole snapshot corpus. Symmetry posture: the Fable side gets no
home-team credit — both formations' r1 drafts were materially wrong and both survived
only through their own adversarial loops; that is graded below, both directions.

**Own verification performed this seat (not inherited):** zero-hit greps re-run over the
frozen snapshot — `LT-10`, `LT-16`, `N-ADJ-3`, `proof:structure`, `depcruise`,
`164343c1`, `css-parse-perf`, `SCI-1`, `W56` = 0 each (Arm-A confirmed); **new zero-hit
family found by this seat**: `TA-4`, `J.W1c`, `D.W2`, `Lane D`, `a8ff7792`, `D-HARDEN`,
`W45`, `atomdiff` = **0 hits each** — the Codex corpus never cites a single one of
value.js's decided API records by name. Also verified: `ERESOLVE` real hits = 0 (the 4
grep hits are `validateResolvedReceipt` substrings), `peerOptional` = 0, `prerelease`
= 0, `P4.5` = 0, `INBOUND-LEDGER` = 0; every `INBOX` hit is the seed letters' filenames,
not the E13 mail law.

---

## 1. PER-ELEMENT VERDICT TABLE

Legend: verdicts CORRECT / PARTIAL / WRONG / MISSING with the why; UNION ruling names
the sourcing side. "Codex" = the frozen `vnext/` formation; "Fable" = armB finals.

### 1A — API / ROUTING (E-C6 / E-C7)

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| A1 | **Palette API facility completeness** (history/variants/all-CRUD/mixing) | **CORRECT** — 147-op closed signed registry (`A-ADJ-SOL3-ROUTES-2026-07-18`), 130 HTTP + 17 headless; revisions/forks/variants/mix/shares/exports/moderation/catalog/curation/audit all enumerated; the band's crown (Arm-A finding 1) | **PARTIAL** — PE-api never enumerates the surface; its census (F2-api-01) is *generated at execution* from live registries — correct method, but no on-disk facility spec exists today | **MERGE**: adopt Codex's `API-OPERATIONS.md` + `api-contract.source.json` as the facility spec of record NOW; Fable's F2-api-01 machine census runs at execution as the closure check (generated-vs-registry diff replaces trust in the signed table). One normative home rule (Fable A6/W2) governs: the source JSON is authoritative, the .md is a projection and says so. |
| A2 | **api/ stay-vs-extract (C7 vs P3.2)** | **PARTIAL** — outcome correct and consistently enforced (FORMATION standing law; A03 exclusion) but the reconciliation lives only in OA-05, a Codex-only owner capture; the P3.2 overturn never narrated in the binding-facing docs | **CORRECT** — supersession-tombstone form (PE W7e): C7 decided it, addenda win; P3.2's EXTRACT row RETIRED-superseded by name, off the docket, never re-litigated | **ADOPT-FABLE form, ADOPT-CODEX content**: W7e's named supersession tombstone is the union record; Codex's A03 internal transpose (domain DAG, external isomorphic tests, owner-precut annex) is the implementation spec it licenses. |
| A3 | **Fourier isomorphism (C7)** | **PARTIAL** — protocol isomorphism only (A01 wire vectors: RFC 9457, ETags, 428/412, cursors, idempotency); correct anti-over-isomorphism refusal, but **no facility-correspondence ledger** (Arm-A finding 3) | **PARTIAL→CORRECT-form** — contract-level single-home (CRUD-CONTRACT v3) + generated divergence census + every same-facility divergence CONVERGE/RATIFIED **with both repos' records cited by name**; but again no correspondence table shipped | **BOTH-INCOMPLETE-DO-X**: adopt Codex A01 as the neutral protocol layer + Fable's v3 as the adjudication layer; ADD the small facility-correspondence ledger (palette.fork ≅ fourier.derivation, palette.revision ≅ fourier.revision, mix ≁ anything — refused) as a v3 section. Neither side may claim C7 isomorphism without it. |
| A4 | **Fourier depth: A21A assets / A21J durable jobs** | **PARTIAL** — within the resource domain but a from-scratch backend rebuild exceeding any "isomorphism" reading; licensed only loosely by C8 (Arm-A finding 6) | **CORRECT-form** — PE keeps fourier at docket altitude (OD-api-J execution-mode row; session-hash wave; like/flag restore-vs-tombstone) and asks the ownership question Codex never asked | **DOCKET**: fuse into one merged-docket row (extends OD-api-J): (a) execution mode (direct-edit grant vs dispatch), (b) A21A/A21J depth ratification against C7-vs-C8, (c) like/flag restore (pre-B.W3 routes at `52bdcf5^` are Fable-side evidence Codex lacks). Codex's A21A/A21J rows are the implementation spec IF ratified. |
| A5 | **Mixing / remix rail** | **PARTIAL** — A12 (deterministic typed preview, persisted CRUD/history, materialization) is a complete fresh design with D-band consumers wired via A13's gate; but the TA-4 chronic (write-only rail, excised `a8ff7792`) and value's own J.W2 empty-diff precedent are cited nowhere (grep = 0) — the third-ride risk is invisible to it | **CORRECT** — OD-api-A real trichotomy with both repos' records verbatim; resurrection recipe pinned `a8ff7792^`; **consumer-inside-the-acceptance-gate law** (third ride of TA-4 forbidden); api semver + split-deploy window priced | **ADOPT-FABLE adjudication, ADOPT-CODEX content**: OD-api-A rules first; on RESTORE, Codex A12's resource contract (recipe tree, stateless preview, materialize) is the richer implementation spec for arms (ii)/(iii); Fable's consumer-in-gate + pricing rows are mandatory riders regardless of arm. |
| A6 | **URL-state + share law (C6)** | **CORRECT (high quality)** — STATE-ROUTING.md: one reducer/codec/router per demo, JCS envelope, byte-identical re-encode, 11 typed errors, history transaction law, credentials-never-in-URL, anti-vacuous-green decode clause, per-route schemas both apps (verified head myself) | **PARTIAL** — PE owns only the backend share slice (v3 share section; `/shares` BANKED at OD-api-H); client slice routed to PD-frontend F-10/F-15 — correct routing, but no artifact of STATE-ROUTING's grade exists Fable-side | **ADOPT-CODEX wholesale**: STATE-ROUTING.md becomes the union URL/state law for both apps; PD-frontend's C6 waves cite it instead of re-deriving. KISS tension (Arm-A finding 8) noted and dismissed — robustness was requested. |
| A7 | **/shares build-vs-bank** | Codex A13 **BUILDS** (stable revision links, bounded exports, revocable capability shares; oversized URL state → immutable `/api` share resource per STATE-ROUTING:116-119) | Fable **BANKS** (OD-api-H, re-trigger: encoded state > ~1.5 KB or a cross-device ask) | **DOCKET row, lean-BUILD**: the two conflict head-on. Evidence for build: STATE-ROUTING's own 6144-char bound plus the D18A 47-leaf blob join makes overflow a *specified* state, i.e. Fable's re-trigger condition is arguably already met on paper. Owner rules; if BANK wins, A13 shrinks to the palette-revision public links only. |
| A8 | **Idempotency backing** | **WRONG-form** — A02 replaces the in-process LRU ("process-local/racy/restart-losing") without citing the sanctioned D2 §3 P2 single-replica relaxation (grep D2/idempotency records = 0) — a silent overturn of a decided row | **CORRECT** — OD-api-E: KEEP the relaxation unless the owner overturns D2 §3 P2 *by name*; the record quoted verbatim from `idempotency.ts:34-36` | **ADOPT-FABLE form**: OD-api-E is the union row; Codex A02's durable reservation store is the overturn arm's implementation spec — and the union notes honestly that if A21J-class multi-process ambitions are ratified, the overturn becomes well-motivated. Never smuggled. |
| A9 | **Cursor semantics** | **WRONG-form** — A08's "cursor tamper/size/query mismatch refuses" overturns the D3/D.W2 Lane D documented graceful-restart semantic; `D.W2`/`Lane D` = 0 hits | **CORRECT** — OD-api-D: converge on 400 as a **named overturn** of D3, demo migration priced (client silently restarts today) | **ADOPT-FABLE form, ADOPT-CODEX content** (authenticated query/sort-bound cursors, snapshot-consistent sorts are good engineering); the overturn is named on the docket, demo migration is in the wave. |
| A10 | **Restore verb / lifecycle** | **CONFLICTED** — A09 builds a full `live→trashed→restored-or-purge-pending` state machine while its own born-RED cell concedes "restore is explicitly retired" — the V·W45 retirement record acknowledged as a fact, never as an *authority* (`W45` = 0 hits) | **CORRECT-form** — OD-api-F: RATIFY divergence (value keeps retirement, fourier keeps restore); record-state trichotomy applied (intact deliberate record → owner decides) | **DOCKET row, both arms on the sheet**: Codex's lifecycle redesign is a genuinely new architecture (trash/grace/purge + participant contract A09C) that may supersede the *context* of the old retirement (versions/revert as the honest surface); Fable's ratify arm preserves it. Owner decides; neither auto-lands. |
| A11 | **Tier / curation** — *union-seat-added finding* | **WRONG-form** — A07T deletes `tier` and migrates curation to featured-membership; born-RED "nine orthogonal tier/visibility combinations conceal archived privacy" — this overturns tranche L.W3's decided `(visibility, tier)` two-axis design and J.W1c §5.1's documented all-9-legal law **without citing either** (`J.W1c` = 0 hits). Neither Arm-A nor Fable-PE caught this (PE couldn't — firewall) | **MISSING** — PE-api has no tier row at all | **DOCKET row (NEW)**: A07T's featured-membership relation is a defensible, possibly superior design (it has an owner-precut annex and a real migration), but it is a named-record overturn and must be docketed as such, citing L.W3 + J.W1c §5.1. This is the fourth instance of the record-blindness disease (§3). |
| A12 | **Security / single-tenant / headless split** | **CORRECT** — single-tenant law, salted digests, HttpOnly/CSRF, A22S façade deletion, 17 deliberately-headless ops, no `/api/admin/*` router, deployed-cruft removal with owner-precut annexes (Arm-A findings 9–11) | **PARTIAL** — narrower (fourier session-hash wave W6b; no value-side security band) | **ADOPT-CODEX** (A05/A06/A18/A19/A22S and the HTTP/headless split), with Fable's W6b folded into A22S's scope. |
| A13 | **Anon-create + born-visibility** | **UNGRADED-by-Codex** — A05/A07 rework identity and publication CAS but no row addresses the anon-owned born-public unmanageable-row hole | **CORRECT** — OD-api-B: the composite defect proven on tree (null owner + born `visibility:"public"` + owner-gated publish ⇒ permanently unmanageable public rows); converge non-null owner recommended | **ADOPT-FABLE**: OD-api-B rides the merged docket; Codex A05/A07 are the implementation vehicle for whichever arm wins. |

### 1B — GOVERNANCE / CO-LAND (L1 §6, P4.x, E-C15/C16)

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| G1 | **Governance lineage** (LT-10/LT-16/N-ADJ-3/proof:structure/depcruise) | **WRONG — the sole P0** (Arm-A W1, re-verified 0 hits this seat): the whole restructure program runs on a parallel authority; live kf gates (`.dependency-cruiser.cjs:171`) never rowed; "silence re-litigates it" at corpus scale | **CORRECT** — G5′ amend-never-parallel rider; CAR-h gates pack EXTENDS proof:structure; anchors demoted to a mechanical gate step citing N-ADJ-3 | **ADOPT-FABLE**: every Codex K-band structure wave re-issues as an AMENDMENT to the ratified LT blueprint naming LT-10/LT-16 supersessions; the lineage annex (Arm-A repair 2) is a union blocking precondition for adopting any Codex K/V structure row. |
| G2 | **Cross-repo channel** | **WRONG-form** — HANDOFFS.md packet law is disciplined but is its own invention: `P4.5` = 0, `INBOUND-LEDGER` = 0; the K band is directly owned/implemented by this program, where L1 §6 says value DIRECTS kf items as specs into kf's inbox and the kf successor implements; no explicit direct-edit grant is on the binding record | **CORRECT** — F-GOV-02 dispatch pack (cars a–i) on the P4.5 channel with acknowledgment discipline + INBOUND-LEDGER rows; execution-mode question docketed (OD-14) rather than assumed | **ADOPT-FABLE channel, ADOPT-CODEX cargo**: the P4.5 dispatch protocol is the sole channel; Codex's 27 K rows + HANDOFFS packets become the cars' cargo (they are the best-specified kf material either side produced). Direct-edit grants ride merged-docket rows OD-14 (kf) / OD-api-J (fourier). |
| G3 | **Co-land boundary shape** | **PARTIAL** — C10 sole-cut single-window with ordered runbook (parse-that untouched → value 5 → kf 7 → Glass 8 → API → consumers → smoke → latest), temporary non-`latest` tag, C05 full rehearsal, 3-phase deletion annex: operationally the strongest cut design on either side. BUT: wedge never ERESOLVE-priced (`ERESOLVE`/`peerOptional`/`prerelease` = 0 real hits, verified this seat); **Glass 8.0.0 major assumed unilaterally**; no 4.1.x/W56/SCI-1 pre-boundary vehicle (0 hits — the inherited-DECIDED D54 row vanished, Arm-A W7) | **CORRECT-form** — F-GOV-05: go/no-go at ENTRY, two-leg ERESOLVE proof matrix with prerelease semantics PROVEN BY EXECUTION (`semver.satisfies('5.0.0-rc.1','^4||^5')===false`), 4.1.x before boundary, OD-12 glass peer ruling ratified once, chase ledgers, kf 44-key engine mirror re-verified by execution. Lighter than Codex on install-crater and rehearsal depth | **MERGE**: F-GOV-05 skeleton (entry gate, two-leg matrix, 4.1.x-first, OD-12) + Codex C00 network-disabled install craters + C05 rehearsal wave + C10 ordered runbook and deletion-annex chain folded inside as the operational core. **Glass 8.0.0-vs-7.x-peer-move is a NEW docket row**: a value-owned tranche cannot unilaterally schedule a glass major; the standing BH-relay law (owner edict 2026-07-12) routes glass-level change through the active glass BH inbox — Codex's G-band assumption is re-based on that channel. |
| G4 | **Export-key freeze / deletion authority** | **WRONG-for-the-union** — V00C six-key freeze + `./value` tombstone rest solely on OA-14, a Codex-owner capture the binding canon does not carry (Arm-A W5); ~135 KB of deletion machinery enforces it | **CORRECT** — 7-key freeze held; F-GOV-01c two-grain, tag-shape-agnostic capability census (v3.1.0's 8-key topology proven by execution) + the v4.0.0 CHANGELOG tombstone BACKFILL (the fired instance cured) | **ADOPT-FABLE machinery + DOCKET the surface**: the two-grain census and backfill land unconditionally (they are the tombstone law's machine); the six-vs-seven-key question goes to the owner as one ratification row (Arm-A repair 6). Codex's 4-question deletion-judgment grammar is ADOPTED as the union's deletion adjudication form — re-based on binding authority, and it may not fire on a frozen key until the row is ruled. |
| G5 | **Deps strip / manifest gate** | **PARTIAL** — V00B strips correctly but only covers the boundary event; no standing gate, no self-dep rule (Arm-A partial 6); queued behind V00A | **CORRECT** — F-GOV-01a/b: strip is the tranche's FIRST commit, dependency-free; allowlist gate (`dependencies ⊆ {@mkbabb/parse-that}`) that cannot rot; selftest-by-mutation | **ADOPT-FABLE**, day-one; V00B folds in. |
| G6 | **Owner-decision docket** | **PARTIAL** — OA-01..17 is a real chronological docket with the required grammar, but it is a *different owner capture* (Sol/Luna, OA-09, OA-14, OA-17) non-adoptable wholesale (Arm-A B5); spring()→Reject, HDR→built, R-EVAL→build were **foreclosed by fiat** — designated owner rows resolved without the owner | **CORRECT** — F-GOV-03 merged SINGLE sheet (26 rows + supersessions), five sibling sheets deduped, ratified at the FRONT before dependent work | **ADOPT-FABLE vehicle**: F-GOV-03's sheet absorbs the Codex-divergence rows as NEW rows — six-key surface (G4), zone flips K14/K15/K21/K22 (with EP-3/K-F6 citations restored), spring/HDR/R-EVAL re-opened as decisions (Codex's resolutions become recommendations with their evidence), restore verb (A10), tier (A11), idempotency (A8), cursors (A9), /shares (A7), A21A/J depth + fourier mode (A4), Glass 8.0.0 (G3), Opus-mechanical roster (OD-15). Codex's OA grammar (chronology, supersedes-on-named-axis-only) is adopted as the sheet's authority law. |
| G7 | **Zone-flip discipline** | **WRONG-form** — K15/K22/K14/K21 invert KEEP-EARNED/OWNER-DECISION rows; `EP-3`/`K F6` = 0 hits; two flips factually defensible but all procedurally pre-empted (Arm-A W4) | **CORRECT** — record-state trichotomy governs OD-1..5; oscillator record-refutation independently re-verified (VIS-1); fence checks run | **ADOPT-FABLE form**: each Codex flip becomes an evidence-bearing recommendation on the corresponding merged-docket row, citing the r2 census AND the EP-3/K-F6 record state. No flip lands unratified. |
| G8 | **Pin discipline / currency** | **CORRECT (P2 note)** — 16–18-repo register, HEAD + NUL-porcelain digests, stale-atlas excluded, baseline disclosed; pins current-not-seed (`c654824e`/`81a56990`) — a currency delta, not fabrication | **CORRECT** — same current pins in §1; G-0 re-pin-at-wave-open makes the *stamp*, not the file, the control | **ADOPT-BOTH**: Codex's register format + Fable's G-0 re-pin law. The `db77dbd8`-vs-`c654824e` delta is recorded once as reconciled (docs-only commits between). |
| G9 | **E13 mail/INBOX law** | **MISSING (out of scope, not a defect)** — every INBOX hit is a seed-letter filename; the 2026-07-17 owner edict never reached the Codex seed | **CORRECT** — G-0 template line + F-GOV-02 protocol page | **ADOPT-FABLE**: standing law, stamped into every union wave spec. |

### 1C — PROCESS / SCALE / CONTRACT (E-C8 / E-C20 / E-C21 / L1 §0·§8·§9)

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| P1 | **Scale (C8 ≥50 waves, six-plus repos)** | **CORRECT** — 190 waves × 7 cells, 8 bands, all named repos + 8 extra consumers; every cell sampled real (Arm-A A1/A2) | **CORRECT-at-different-grain** — six programs, ~50–60 coarser waves, code-first | **MERGE**: the Codex band registry is the union's *enumeration backbone* (nothing it names may be silently dropped); the Fable program partition is the union's *execution grain*. Where a Codex wave is process-about-process it dies per the kill list; where a Fable wave is coarser than the defect structure, the Codex rows re-grain it. |
| P2 | **Wave grammar + born-RED** | **CORRECT** — the 7-cell row with concrete live witnesses is the best wave grammar either side produced; `validate-formation.mjs` proven non-vacuous by mutation (Arm-A probes) | **PARTIAL** — Fable waves carry intent/gates/deps/π/routing but no uniform machine-validated grammar | **ADOPT-CODEX grammar, extend to 8 cells**: ID · Mission · Deps · Born-RED · Deliverables · Falsifiable gates · Exclusions · **Routing/π** (Fable's model-routing + π/DELTA cell). `validate-formation.mjs` adopted as-is. |
| P3 | **Return contract** | **CORRECT-but-premature** — v2/JCS/typed-absence/gate-receipt-v3 exemplary; 266 KB of execution-phase machinery at 0/190 executed (Arm-A A4 + apparatus A-7) | **MISSING** — no comparable artifact; relies on tranche norms | **ADOPT-CODEX SHRUNK** (apparatus diet #2): schema + ~15 KB validator, live+offline modes only, deferred to the first execution wave; the four-mode/deletion-annex mass returns only when a return exists to validate. |
| P4 | **Zero-drop instrument** | **CORRECT-but-scoped** — the 149-row seed bijection, SHA-pinned, mutation-RED proven: the strongest zero-silent-drop machine in the constellation (Arm-A A-2); structurally blind to CONVERSATION-ADDENDA | **PARTIAL** — landing maps cover C1–C23 1:1 (receipt-table form) but are prose tables, not machine-checked | **MERGE**: extend the bijection to a THIRD pinned source (CONVERSATION-ADDENDA) and regenerate the Fable receipt table from it — one machine check over letters + addenda + program files. This is Arm-A repair 15 made concrete. |
| P5 | **Thrice method** | **CORRECT** — genuinely ran at formation grain (15 falsifiable V-families, SCC caught, R3 turned on itself and found a real false-green) | **CORRECT** — ran per program; PE/PF adjudicators probed with their own evidence (PE ADJ-1 found what both skeptics missed) | **ADOPT-BOTH + Codex's registry discipline**: finding-family registry with clean-counter reset; Fable's C20-deviation annotation (mechanical waves ride gate-checks; C17 cost bound stated) adopted so the loop's cost stays honest. |
| P6 | **Convergence** | **MISSING (honestly)** — 0/2 clean passes; newest artifact self-issues NOT CLEAN; RR-17/C00U open with 3 prescribed slices (Arm-A B1/B2) | **NOT-CLAIMED** — finals are amended-thrice products; PF's anti-phantom gate is "RED today by design"; no whole-formation pass either | **BOTH-SHORT-DO-X**: the union program IS the convergence vehicle. Closing law: (1) RR-17's three slices land (or the regex resolver is replaced with es-module-lexer/TS API and the mandatory universe narrows to the canon four trees + lockfile evidence — Arm-A repair 1/3) before ANY Codex tool is trusted in a union gate; (2) two consecutive clean registry passes run over the UNIONED inventory with the extended bijection (P4). |
| P7 | **Model provenance** | **PARTIAL** — content-attested only; the trust boundary honestly conceded; 316 KB of forensics against a conceded-unanswerable question (Arm-A B3 + apparatus A-5) | **CORRECT-grade-for-cost** — model_served self-report per seat, zero apparatus | **ADOPT-FABLE + one owner row**: model_served on every seat; the provider trust boundary accepted explicitly as an owner row (Arm-A partial 9); the forensics suite cut to its ~25 KB core. |
| P8 | **Apparatus proportion (C21)** | **WRONG** — ~75% apparatus / 4% deliverable; 316 KB clean-pass forensics with 0 passes; 211 KB P01 police for a 0-byte prototype; the six-regex irony (apparatus A-4..A-8) | **CORRECT-direction** — code-first day-one (F-GOV-01, PE W3); no harness; gates that can fail | **ADOPT-FABLE sequencing + the Arm-A collapse list wholesale** (forensics→core, return suite deferred, P01 police shelved, selftests folded via `--selftest`, schemas merged, upstream campaign state stripped from wave rows). KEEP the load-bearing Codex gates: validate-formation, seed bijection, target-path/transpose, public-surface tarball validators — Arm-A proved these non-vacuous by mutation. |
| P9 | **Isolation (C16) + honesty (close-class lies)** | **CORRECT both** — zero close-class lies found by two lenses hunting exactly that; 0/190 stated everywhere; failed passes booked as evidence | **CORRECT both** — sole writes are own files; firewall honored | **CARRY-BOTH** as the union's baseline conduct standard; the Codex honesty discipline is named in §3 as the thing the union must not lose while dieting the apparatus. |

---

## 2. UNION WAVE-DELTA (this domain's concrete amendments to the mega-tranche)

**ADOPT-CODEX (source: snapshot `vnext/`), with named conditions:**
1. **A-band backbone A00–A20 + A21R–A26** as the API implementation inventory — CONDITIONAL on the record-overturn repair: A02 (idempotency), A08 (cursors), A09 (restore), A07T (tier), A12-adjacent fork semantics each re-formed as named overturns riding merged-docket rows (§1A A8–A11) before execution. No A-wave that touches a decided value record executes unratified.
2. **STATE-ROUTING.md wholesale** as the union URL/state/share law, both apps (§1A A6).
3. **API-OPERATIONS + api-contract.source.json** as the facility spec of record, verified at execution by the Fable generated census (§1A A1); add the facility-correspondence ledger (§1A A3).
4. **C00 install craters, C05 rehearsal, C10 ordered-cut runbook + deletion-annex chain** folded into the boundary wave (§1B G3).
5. **The 7-cell wave grammar + `validate-formation.mjs` + seed bijection**, bijection extended to the addenda (§1C P2/P4).
6. **The 4-question deletion-judgment grammar** re-based on binding authority (§1B G4).
7. **Return-contract v2 + gate-receipt v3**, shrunk and deferred per the diet (§1C P3).
8. **Finding-family registry + clean-counter reset** (§1C P5).

**ADOPT-FABLE (source: armB finals):**
9. **F-GOV-01 whole, day-one**: deps strip first commit; allowlist manifest gate; two-grain tag-shape-agnostic capability census; v4.0.0 CHANGELOG tombstone backfill.
10. **F-GOV-02 dispatch pack (cars a–i) on the P4.5 channel** as the sole cross-repo mechanism; Codex K rows + HANDOFFS packets loaded as car cargo; census-LOCK gate (car e) and kf-WAAPI uplift (car f) carried.
11. **F-GOV-03 merged single docket, ratified at the FRONT**, absorbing ~12 new Codex-divergence rows (§1B G6 list).
12. **F-GOV-04** (value e2e fleet adjudication) — the Codex corpus has D25/M11/C07 audit waves but no e2e-fleet per-oracle disposition; the orphan stays Fable-homed.
13. **F-GOV-05 skeleton** (entry go/no-go; two-leg ERESOLVE matrix, prerelease-priced; 4.1.x/W56/SCI-1 vehicle restored BEFORE the boundary; OD-12 ratified once) with Codex's operational core inside (§1B G3).
14. **PE-api F2-api-01/02** (machine census + CRUD-CONTRACT v3 single home) as the adjudication layer over the Codex registry; **consumer-in-gate law** (TA-4 third ride forbidden) as a standing rider on every restored rail.
15. **G-0 charter matter** stamped into every union wave: G0′ re-pin at open, anchor re-derivation gate step, model_served, E13 mail law, C15 parse-that ≤1.1, phase labels.

**ADD (neither side had it):**
16. The **palette↔fourier facility-correspondence ledger** (v3 section, small).
17. The **tier/A07T docket row** citing L.W3 + J.W1c §5.1 (this seat's find, §1A A11).
18. The **Glass-8.0.0-vs-peer-move docket row** + BH-inbox relay of every Codex G-band assumption (§1B G3).
19. The **provider-trust-boundary owner row** (accept attestation-grade provenance explicitly; §1C P7).

**KILL (with the sourcing verdict):**
20. Codex clean-pass forensics beyond the ~25 KB core; the P01 authorship police until an author exists; the four-mode return machinery until a return exists (Arm-A apparatus collapse list, adopted whole).
21. The six-regex consumer-universe resolver AS a trusted gate — replaced per RR-17's slices or es-module-lexer; the 15-root mandatory universe narrows to the canon four trees + lockfile evidence, other roots best-effort (the C02x adoption waves survive as cheap execution waves; their fail-closed machinery does not).
22. Codex's HANDOFFS as a *channel* (survives as cargo format only); OA-14's deletion loosening as *authority* (survives only if the owner re-rules it).
23. Fable r1 phantoms/circularities — already killed by its own adjudication; nothing further.

---

## 3. APOTHEOSIS NOTES — what each approach teaches the other

**Codex → Fable:**
- **Machine-checked zero-drop**: SHA-pinned source letters + a bijection that goes RED on one dropped row, proven by mutation. The Fable landing maps are prose by comparison. The union's zero-drop claim should never again rest on a hand-audited table.
- **Closed, signed operation registries**: 147 tuple-hashed ops with duplicate rejection. Enumerate-then-sign beats generate-later as a *formation* artifact; the generated census is the right *verifier*, not the right spec.
- **Rehearse the cut**: C05's publish-nothing full rehearsal (re-census, frozen manifests, install matrix, runbook) is the missing organ in Fable's boundary wave.
- **Honesty as architecture**: 0/190 and 0/2 in every authority doc; failed passes filed as evidence; the newest artifact a self-issued NOT CLEAN. The Fable finals are honest too, but Codex made honesty *machine-legible*.
- **Born-RED cells with measured geometry** ("Search is about 21 px") and the deletion-judgment 4-question grammar — both sharper instruments than anything Fable-side.

**Fable → Codex:**
- **Records are load-bearing.** The single deepest Codex disease in this domain is record-blindness: zero citations of TA-4, J.W1c, D.W2/Lane D, D2 §3 P2, V·W45, EP-3, K F6, LT-10, D54/SCI-1 (all grep-proven). Five API rows and four zone flips silently overturn decided rows. The Fable side caught the *same disease in itself* (the fourier-is-right bias, three instances) only because its skeptics were pointed at the records. Union law: **no wave touches a surface with a decided record unless the row cites it — and the gate is a grep, which is cheap.**
- **Owner capture is not fungible**: a docket built on a substituted owner (Sol/Luna, OA-14, foreclosed spring/HDR/eval) cannot be adopted wholesale; decisions foreclosed by fiat must reopen.
- **Code-first sequencing**: the committed deps bomb was a one-commit cure; it queued behind census ceremony on both sides' r1 drafts, and only the Fable final moved it to day one.
- **Ratify at the front**: rulings before dependent work, go/no-go at boundary ENTRY — Codex's C08/C09/C10 put every human decision terminal, where "no" is impossible.
- **Price the wedge with executions, not prose**: `semver.satisfies` run, ERESOLVE proven twice, prerelease ranges shown NOT to admit rc's. Codex's cut order is careful but its compatibility physics are asserted.
- **Proportion (C21)**: 4.9 KB `validate-formation.mjs` governs 190 waves and 759 edges; the 67 KB regex resolver produced the corpus's only open defect. The small, mutation-proven gate is the pattern; the cathedral is the anti-pattern.

**The shared lesson (both sides, symmetrically):** both r1 drafts were materially wrong in overlapping ways — record-blind convergence, apparatus over deliverable, decisions smuggled past the owner — and both were saved by their own adversarial loops (Codex's R3 caught its false-green resolver; Fable's skeptics caught the phantom programs, the docket circularity, the false born-RED premises; its adjudicators added findings both skeptics missed). Neither formation earned first-draft authority; the union institutionalizes that: **every load-bearing artifact ships with its refutation record attached, and a clean pass is only ever the property of the unioned corpus, not of either parent.**

— Arm-C union adjudicator, `claude-fable-5`, 2026-07-19. Sole write: this file.
