# UNION ADJUDICATION — STRUCTURE PROGRAM × FRONTEND/DESIGN PROGRAM
### Arm C · union adjudicator (`claude-fable-5`) · 2026-07-19

**Subjects.** Codex side: the frozen snapshot (`…/apotheosis/snapshot-vnext`, 2026-07-19 21:29) — waves/P-V.md (V00A–V00C, V24, V29T), waves/K-A.md (K00–K24), waves/G-D.md (G/D bands), waves/M-C.md (M band), TARGET-DAGS.md, DEMO-TARGET-DAGS.md, VALUE/KEYFRAMES-TARGET-PATHS.json, DISPOSITIONS.md, DESIGN-PROGRAM/PROVENANCE.md, LIVE-VISUAL-AUDIT.md — as graded by armA (audit-structure.md S-01..S-26, audit-frontend.md FD-01..FD-10, VERDICT.md incl. its §5 corrections). Fable side: armB/final-PC-structure.md (8 waves + OD-S1..S8) and armB/final-PD-frontend.md (24 waves + OD-1..9 + PL-1..11).

**Independent verification performed by this adjudicator (read-only, frozen snapshot):** re-ran the load-bearing greps — `LT-10|LT-16|N-ADJ-3|proof:structure|depcruise|dependency-cruiser` = **0 hits corpus-wide**; `EP-3|K.W8|K.W9` = **0**; `164343c1|css-parse-perf|SCI-1|D54` = **0**; `W53|W55|W56|perceived-space` = **SEED-ROW-INVENTORY.json only** (hashed bank, no wave text); `IOS27` = **PROVENANCE.md + HANDOFFS.md only** (pin/pull notes, never a bound design input). Re-read V00C (six-key freeze verbatim), K12/K14/K15/K22 rows, M08, D03A, and DISPOSITIONS rows 28 (spring Reject), 36–37 (quantize/`src/api`), 93 (api Keep). Every armA claim I tested held; armA VERDICT §5's corrections (oscillator rider PRESENT in K20; W53 BANKED-with-retrigger not silently dropped; IOS27 named-twice) are adopted into the grades below.

**Symmetry note (no home-team bias).** The Fable finals are ONE completed thrice iteration each — they have not run whole-formation clean passes at all, where Codex ran passes and honestly failed them (0/2, NOT CLEAN self-issued). Fable delivered zero executable tooling where Codex delivered 85 tools; Fable's frontend program shot zero new frames where Codex ran a 21-route measured live audit. Fable-side defects found by this adjudication and graded below: DesignSync is absent from the PD final entirely (not even banked — Codex handled it better); no product-identity device exists on the Fable side; the C10 corpus ingestion is NOT demonstrably done on the Fable side either (the "13 principles" trace to the C18 session backtrace, not to a read of IOS27-CODEX/SUFFUSION-MATRIX); and PC-structure's "glass-equivalents EMPTY-SET" claim collides with Codex's G-band gap census and must be re-censused.

---

## 1. PER-ELEMENT VERDICT TABLE

Verdicts: CORRECT / PARTIAL / WRONG / MISSING. Union rulings: **adopt-Codex** / **adopt-Fable** / **merge** (how stated) / **both-wrong-do-X**.

### 1A. Structure program

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| U-S1 | DAG derivation | **PARTIAL** — genuinely machine-derived (module-graph.mjs, hashes, Tarjan SCCs, reproducible commands; S-01) but the mandated depcruise/madge substituted with a bespoke six-regex projector its own R3 convicted (template dynamic-imports missed, inline `type` misclassified → false `resolvable:true`; RR-17 reopened) | **PARTIAL** — canon-compliant tooling (adopts pre-existing madge-src.json 26/0-cycles + depcruise-animation.json), but the value-demo component graph is a declared gap (F-01) not yet derived; nothing built | **Merge:** depcruise/es-module-lexer-class extraction (canon; lands R3's three slices or replaces the regex projector outright — armA repair 1) INSIDE Codex's artifact discipline (SHA-pinned graphs, printed commands, SCC adjudication, typed 759-edge WAVE-EDGE-POLICY). F-01 captures the demo gap graph with that tooling. |
| U-S2 | Target trees / goldilocks | **PARTIAL→CORRECT** — kf 153→66 files/12 domains is genuine goldilocks (S-07); manifests are executable authorities (S-24); dirigible architectures (S-25); BUT value css/ 26→105 (~70 under css/) is a 4× expansion adjudicated only by prose, sand risk acknowledged-ungated; kf manifest hard-codes K14–K22 prune outcomes with no conditional_paths (S-09) | **PARTIAL** — directory-grain "opening positions" only, no per-file manifests; correctly defers color/ (no pre-churn of PB's restore target) and cedes css/ to the parser program; less delivered by design | **Merge:** Codex's manifest+validator substrate is the union's executable path authority; the Fable "no pre-churn" law governs sequencing (color/ joint rider on the restore landing; css/ tree re-adjudicated by the union thrice batch against the 4× sand risk with a cohesion measure, not prose). Regenerate KEYFRAMES-TARGET-PATHS with typed conditional_paths for every K14–K22 outcome pending ratification (cures S-09). |
| U-S3 | Tests-isomorphism | **CORRECT** — exact, external, machine-checked BOTH repos, born-RED where live, support-dir allowlists; verified by armA's own join (S-05). One P2 sand: `.test.ts` per CSS token file (S-06) | **CORRECT (spec-only)** — F-02 one-motion gate+cure, kf rule honestly labeled born-GREEN-on-tree + selftest-fallible; 11 displaced tests + ghost test/parsing/ named; nothing built | **Merge:** adopt Codex manifests + validate-target-paths.mjs as the implementation of Fable F-02's rule-spec ownership model (spec homed in value.js, checksum-pinned copy in the kf packet); adopt Fable's honest-label law (born-RED only where the defect is live; kf side born-GREEN + planted-RED selftest); DELETE the CSS-token `.test.ts` mirrors (stylesheets exit the mirror law). |
| U-S4 | Module-name-strip rule | **CORRECT** — standing law + real enforcement code (validate-target-paths.mjs:270-274, case/Unicode-folded; S-04) | **CORRECT** — census-first (0 hits on all three unguarded trees, re-verified) + the `.vue` SFC exemption grounded in glass-ui's own `button/Button.vue` idiom (E1) | **Merge:** Codex's enforcement code + Fable's explicit `.vue`-SFC exemption written into the validator (glass-ui is the reference model; the dir-prefixed SFC is the idiom, not a violation). |
| U-S5 | Export surface (subpaths dissolution) | Dissolution **CORRECT** (shim-free, forbidden_roots, packed re-verify V29T; S-10); the SIX-key freeze + `./value` removal **WRONG vs canon** — rests solely on Codex-captured OA-14; binding canon holds the 7-key freeze (S-11, armA W5) | **CORRECT** — 7 keys frozen pre-boundary; the D5-corrected retarget (explicit per-key module specifiers, `/index` only for real dirs — no fabricated one-file dirs); OD-S7 opens the cross-repo export-grammar decision | **Merge:** dissolution mechanics + tombstone/casualty machinery from Codex; key COUNT goes to the owner as ONE ratification row (six-key + `./value`-into-`./css` + root-export vs the 7-key freeze — fold OD-S7 into the same row; the co-land boundary is the only cheap harmonization moment). Fable's explicit-specifier spec applies under either outcome. |
| U-S6 | D50 explicit-`/index` boundary | **MISSING** — superseded by silence (0 hits; S-12); the replacement design is coherent but unnamed | **CORRECT** — D50-explicitness reinterpreted correctly (explicitness is the requirement, not literal `/index`) | **Adopt-Fable** spec; write the named supersession row Codex owed (zero-silent-drop hygiene). |
| U-S7 | api/ EXTRACT vs C7 STAY | **CORRECT** — OA-05 names both poles verbatim, distinguishes the pruned library `src/api` facade from the retained server (DISPOSITIONS 37/93 re-read this session); the model of a papered seed reversal (S-13) | **CORRECT** — slice ceded whole to PE-api, OD-S3 merged into OD-api-1, explicit kf-N/A row | **Merge:** adopt Codex's OA-05 paper as the reconciliation text of the single union owner row; Fable's one-owner cession governs (no duplicate authorship); keep Codex's fourier protocol-isomorphism altitude ("share protocol vectors, not runtime modules"). |
| U-S8 | kf flatten + governance lineage | **WRONG (the sole P0)** — LT-10/LT-16/N-ADJ-3/proof:structure/depcruise = 0 hits (re-verified by me); wholesale re-founding on a parallel authority; `.dependency-cruiser.cjs:171` leaf law goes green-by-vacancy post-K22T; the 755-line live gate un-rowed (S-14/S-15); mechanism-credit: byte-snapshot replay is arguably a STRONGER drift defense than line-cites (S-16) | **CORRECT** — amends the LT blueprint naming LT-10/LT-16 superseded; 13 anchors re-derived at execution (3 vite drifts already caught — N-ADJ-3 vindicated); enforcement-BEFORE-churn phase order; leaf-law key re-keyed with the rename; clamp invariant folded before its file moves | **Adopt-Fable governance spine + adopt-Codex replay mechanism:** F-07's packet (phase 0 preconditions → enforcement → flatten → restructure) is the delivery vehicle; the 13-anchor born-RED census stands (canon-verbatim); Codex's immutable byte-snapshot + replay gates (K00→K22T, `execution_resnapshot_required`) ride WITH it as the second drift defense. Row `.dependency-cruiser.cjs` + proof:structure R1–R6 with explicit extend/retire dispositions (armA repair 2). |
| U-S9 | Zone verdicts in the target structure | **WRONG procedurally / defensible substantively** — K15 view-transition, K22 presets, K14 flip, K21 morph invert adopted KEEP-EARNED/OWNER-DECISION rows with EP-3/K-F6 cited nowhere (0 hits, re-verified); mitigation is real: four-question deletion judgment, owner-precut annexes, K14 requires G07-green-first, K20 carries the oscillator record-correction rider verbatim (armA §5.1) | **CORRECT** — defers zone membership to the PF-04 docket ("never flatten code that is about to die"); the record-state trichotomy honored | **Merge:** every Codex flip converts to an owner-ratification row citing the r2 census + EP-3 PATH-B / K F6 fence (armA repair 7); Codex's deletion-judgment apparatus (4 questions, precut annex, casualty proof, migration-before-deletion sequencing) is ADOPTED as the execution machinery once ratified. Fable's never-flatten-dying-code sequencing law binds K22T. |
| U-S10 | deps strip + pre-publish manifest gate | **PARTIAL** — V00B strip exact (tarball graph, bundle crater, born-RED); standing recurring gate + self-dep rule unnamed (0 grep hits; S-20) | **CORRECT** — single gate text co-owned with the CI seat, self-dep clause (v1.1.0–v2.0.1 prior art), and the decree-critical **parse-that whitelist** (`runtime deps ⊆ {@mkbabb/parse-that}` — a bare "no runtime deps" gate breaks the P1.6 readoption decree; K2) | **Adopt-Fable gate text** (whitelist included) implemented with **Codex's verification mechanics** (packed tarball dependency graph, fresh no-link install craters, value-public-surface validation). |
| U-S11 | Hygiene (decompose · quantize · PNGs · demo tests) | decompose **CORRECT** (V24 decided-terminal with a falsifiable KEEP bar — consumer-count-is-not-enough honored verbatim; S-18); quantize **CORRECT** (major tombstone, extract-worker home; S-19); demo/api test displacement **CORRECT** (S-22); 39 PNGs **PARTIAL** (hashed-fold-only, invisible in any wave text; S-21) | **CORRECT** — F-04 executes PNGs/.DS_Store/components.json now; decompose + quantize ride the boundary ledger (the 10 `./transform` symbols enumerated — the original omission reproduced the no-advocate⇒no-tombstone mechanism, caught by its own skeptics) | **Merge:** Codex's V24 KEEP-bar formulation replaces the seed's flat PRUNE (it is the stricter, falsifiable form); Fable's ledger scheduling governs WHEN (surface cuts execute only at the co-land crossing); surface the PNG row in visible wave text (armA P2 note). |
| U-S12 | Surface-freeze / co-land scheduling | **PARTIAL** — C10 sole-release honors the one-boundary law; but key removals and prunes are pre-decided into the sole path authority before ratification | **CORRECT** — the boundary-delta LEDGER (every surface delta = a row with a §7.1 tombstone draft + four-tree re-grep at execution HEAD; executes only at the crossing) cures the F-04⊥F-05 class by construction | **Adopt-Fable ledger; adopt-Codex crossing vehicle:** the ledger's rows execute inside the C10-style single coordinated release (value 5 / kf 7 / glass 8 / consumer majors), with Codex's capability-diff-vs-prior-tag machinery as the ledger-completeness witness. |
| U-S13 | Demo structure (value + kf) | **CORRECT-substance, rebuild-scale** — feature-first slices with a closed edge law (platform imports nothing above; no sibling feature edges; no view touches fetch/History/URL/clipboard; one block-scroll owner per route) + kf registry-of-pure-definitions/one-reducer/sole-math-owner (S-25) | **PARTIAL** — value demo "largely compliant post-W43", narrow deltas (barrels, cn(), palettes/ 84-file adjudication restored); risks under-serving C11's full-DAG mandate; kf demo deliberately narrow (PD owns the overhaul) | **Merge, split by app:** VALUE demo — Codex's closed edge laws become GATES; moves are defect-driven (a file moves when it violates a law), the Fable churn-aversion posture under PL-7/C21; the palettes/ long-dir adjudication stands. KF demo — the Codex-scale overhaul WINS (C4 explicitly authorizes restructure; Fable's own F-15..19 concur); M03 stage-first shell + the manifest laws are the target. |
| U-S14 | Abstract value↔kf isomorphism facility | **CORRECT** — one shared manifest grammar, ONE validator over both repos, same grammar declared for value, kf, AND fourier; extends to both backends (S-08) | **CORRECT** — F-02's portable rule spec (R1–R6 + R-ISO) homed in value.js, checksum-pinned into the kf packet; per-repo implementations (KISS, no new package) | **Merge:** Codex's single-validator machinery IS the implementation of Fable's ownership model — spec text homed in value.js, the validator validates both manifests, the kf packet pins the copy. Fourier's declaration carries. |
| U-S15 | Cross-repo delivery (P4.5) | **PARTIAL** — HANDOFFS.md packet law exists and is packetized; but K/M bands are authored as directly-executable waves of this formation with the kf-successor's implementing role unpapered | **CORRECT** — ONE internally-sequenced kf packet with phase-0 preconditions embedded (no phantom dependencies), DONE = rowed-in-inbox never "landed", OD-S4 grant row | **Adopt-Fable protocol; Codex rows as payload:** the union's kf library+demo wave rows (Codex K/M specs as amended above) travel INSIDE the single F-07-style dispatch packet; direct-edit mode only under the explicit OD-S4/OD-1 owner grant. |

### 1B. Frontend/design program

| # | Element | Codex verdict | Fable verdict | UNION ruling |
|---|---|---|---|---|
| U-F1 | C3 — value mobile toolbar | **CORRECT** — live empty-toggle witness (measured), root cause diagnosed (desktop-mount optional-chain refs), architectural remedy: D03A mount-independent capability registry with the zero-capability-route law (FD-01; row re-read this session) | **CORRECT** — F-06 decides the toolbar JOINTLY with the ratified W47 shell gates it collides with (PaneSegmentedControl 1→0, verified alive + mounted at Dock.vue:198); OD-5 delivers both candidate forms to the owner | **Merge:** D03A's registry architecture (Codex) + the W47 joint ruling and fold ledger (Fable — Codex cannot see W47; its corpus lacks the CARRY-LEDGER namespace entirely) + OD-5's owner design gate. Dock-to-card growth motion vocabulary rides U-F5's ingestion gate. |
| U-F2 | C4 — kf mobile overhaul + multi-touch + single-screen | **CORRECT (strongest band)** — K12 one-Pointer/one-Float64-quaternion kernel with falsifiable traces (born-RED premise triple-lane binding VERIFIED on disk at OrbitalDrag.vue:279); M03 single-screen shell (Monaco hidden-editor witness owned); M05–M10 per-scene multi-touch incl. M06 amiga (OrbitControls deleted); honest Home refusal row (FD-02) | **CORRECT** — F-15..19 with the amiga drop caught independently (S1-A5), the honest kf-desktop capture gap named, viewport-fit/safe-area cure (10 consumer sites, 0 `viewport-fit`), and the risk-postured cube ruling: cube = reference, NOT refactor target; unification BANKED with a named re-trigger | **Merge (the one real design conflict, resolved by sequencing):** adopt Codex's K12 kernel SPEC as the shared multi-pointer facility Fable's F-17 builds; it proves itself on the five non-cube scenes FIRST (square, sequence ×2, spring, easing, amiga); the CUBE migrates onto the kernel SECOND, gated on trace parity vs the extant triple-lane machine (cancel/lost-capture/1→2→1/antipodal/Safari-gesture behaviors) + physical-device proof (M05/C07) — Codex's end-state, Fable's never-lose-the-sole-complete-implementation risk posture. Per-scene gesture-semantics table (Fable) with "no meaningful gesture" as an owner-visible legal verdict. F-16's viewport-fit/safe-area wave adopts F-01's honest inset oracle. |
| U-F3 | C2 — easing selector redesign | **PARTIAL (attribution) / CORRECT (substance)** — M08 Chronograph Contact Sheet with density minima, radius ≤4px, ≤0.5px glyph deviation, and the anti-token-tune exclusion; but the owner-mark recap row homes C2 at kf only while the binding capture says "(value demo)"; the value side (D16+G09) exists uncredited (FD-03) | **CORRECT** — measurement-first re-diagnosis (the 3-decimal stepping ≈0.30px = imperceptible; OD-4 killed as misdiagnosis; the percept lives in tiles/strokes); demo-owned cures decoupled from the glass round-trip (19rem clamp, specimen strip); F-04 carries only the truly glass-owned arms | **Merge, dual-homed:** Fable F-03's measurement record runs FIRST and feeds the glass specs; Codex M08 is the kf-side redesign; Codex G09 (glass EasingPicker/CurveGlyph) + Fable F-04 (compact/density variant, screen-space hairline strokes) merge into ONE glass gap-wave; the recap row credits BOTH apps (armA repair 14's dual-homing). The M08 anti-gaming exclusion column is adopted as house style for every redesign gate. |
| U-F4 | C5 — shadcn abrogation | **CORRECT** — byte-exact premise census (19 barrels / 4 reka files / 2 vendor deps — armA verified on disk); anti-forwarding law; gap primitives specced as GLASS-owned waves (G03 toolbar, G06 ColorSwatch, G08 seat substrate, G09 EasingPicker) exactly as C5 directs (FD-04) | **CORRECT, wider census / one collision** — the IDIOM census (canonical `cn()` retired onto glass-ui's export, clsx+tailwind-merge devDeps dropped, stale components.json deleted, `demo/shared/` adjudicated not grep-collided) exceeds the import-tag census; BUT its "zero missing glass-ui component equivalents — prototype branch EMPTY-SET" collides with Codex's G-band gap findings | **Merge:** Fable's idiom census (cn()/devDeps/components.json/shared-adjudication) + Codex's gap-primitive register (G03/G06/G08/G09) + Fable's own PD gap rows (F-04 variant, F-05 living-dot API, AcceptableValue type export, layer-transition composable) unify into ONE glass-gap register riding the two-batch relay (U-F8). The EMPTY-SET claim is RE-CENSUSED against the merged register (it was scoped to whole-component equivalents; Codex found real ones). |
| U-F5 | C10 — Breath of Life | **PARTIAL (P1)** — substance real and non-checkbox (quantified motion tiers, G05 executable manifest, D05 slider watercolor backplates verbatim, D15/D24 restrained beget, D19–D24 machine ledger); but the owner-named BJ + IOS27-MICRO corpus was never a bound design input (my grep: IOS27 only in PROVENANCE/HANDOFFS pin rows) — the motion grammar was independently invented and diverges from IOS27-CODEX's numbered laws (FD-05) | **PARTIAL (the symmetric debt)** — F-05 living-dot API (engage envelope 40–60ms/t90≤150ms, seeded beget, idle-breath zero-rAF) + F-10's corrected 23-site/11-file per-family wiring map are strong; but the "13 principles" trace to the C18 session backtrace, NOT to a demonstrated read of IOS27-CODEX/SUFFUSION-MATRIX/BAND-FEEDBACK-MOTION — the ingestion mandate is not discharged on this side either | **Both-wrong-do-X:** mint **W-ING (NEW)** — a bound-input ingestion wave that reads glass-ui BJ (PLAN, BAND-FEEDBACK-MOTION, ASK-REDUCTION) + IOS27-MICRO (CHARTER, IOS27-CODEX numbered laws, SUFFUSION-MATRIX) and reconciles BOTH corpora's motion grammars against it BY LAW NUMBER (Codex G04/G05/M08; Fable F-05/F-10/F-11's envelope numbers) BEFORE any motion wave executes. Substance from both sides is then merged: Codex G05 budgets + D05 backplates + Fable F-05 API + F-10 family map, all law-reconciled. |
| U-F6 | C22 — frontend audit totality | **CORRECT** — the audit was RUN: 14+7 routes × 390×844 + 1440×900, measured geometry, per-route witnesses, add/refine/remove registers, honest formation-evidence labeling (FD-06) | **CORRECT-as-spec, unexecuted** — F-02/F-15 widen the matrix decisively: non-route surfaces (the AboutPane corpus mounts on NO route), named interaction states (every owner photo is one), authed admin band, {390, 768, 1024, 1440} with the boundary band (dock flips at `lg`), 14-not-15 routes; but zero frames were actually shot (profile lock) | **Merge:** Codex's live capture set = the union's BEFORE-evidence base (no re-shooting what exists); Fable's widened matrix = the COMPLETING audit spec (interaction states, non-route surfaces, authed admin via F-01's recipe, boundary-band spot set). The Fable F-01 probe-rig wave is the precondition for every new capture. |
| U-F7 | Design routing / DesignSync | **PARTIAL, well-handled** — DesignSync unused but honestly BANKED with named G00/D00A/M00 retriggers, sealed by X-ADJ amendment 14; substitute provenance spot-checked TRUE on disk (FD-07) | **MISSING** — the PD final never names DesignSync at all: not used, not banked, no retrigger. The charter's "ALL design routes through Fable + DesignSync" is silently unmet on the Fable side — Codex handled this obligation BETTER | **Adopt-Codex pattern:** the banked-method-gap + named-retrigger device carries into the union; the retriggers re-home to the union's design-gate waves (the F-02/F-04/F-05-class design artifacts and Codex's G00/D00A/M00 equivalents). DesignSync executes at the retrigger points before implementation; the Fable side's silent drop is recorded as a finding. |
| U-F8 | Glass relay + batching | **CORRECT** — one batched producer pack (G-band) with per-defect wave addenda + the HANDOFFS GL packet; no piecemeal interruption (FD-09) | **CORRECT** — TWO priced batches (batch #1 = the four centerpiece/unblocker rows EARLY — the target program's held-batch default provably starved three waves; batch #2 post-audits) + F-21 as the single reconvergence home with the honest BANKED posture for external latency | **Merge:** Codex's G-band wave specs are the PAYLOAD; Fable's two-batch relay + OD-2 + F-21 reconvergence is the DELIVERY architecture. An externally-gated adoption wave that cannot close closes BANKED with the glass landing named — never partial-as-done. |
| U-F9 | Product identity | **CORRECT** — Optical Instrumentarium / Chronographic Stage grounded in verified source tokens + real font stacks, composition grammars, signature gestures, and the anti-template test ("warm cream + serif + glass alone = generic premium template" — the strongest identity-preservation device in either corpus) (FD-08) | **MISSING-as-artifact** — the refinement law (PL-7) implicitly preserves identity but no identity register, no anti-template device, no named material/type registers exist | **Adopt-Codex wholesale:** both identity registers + the anti-template test become binding design inputs of every union D/M/G-class wave; Fable's PL-7 refinement law is the enforcement posture around them. |
| U-F10 | C6 — routing + URL state | **PARTIAL** — STATE-ROUTING.md's codec law is rigorous (JCS envelope, byte-identical re-encode, 11 typed errors, history transaction law, credentials-never-in-URL, anti-vacuous-green decode — both apps); but the extant stub-router architecture (render-null routes, no `<router-view>`, pane-machine truth) was never explicitly ruled keep-or-reshape — codec built atop an unadjudicated architecture | **CORRECT** — OD-S8 supplies exactly the missing ruling (ratify-lean: the idiom is deliberate, in-file-documented, isomorphic across the pair); F-14 is two-stage (architecture adjudicated FIRST, codec second); F-18 merges kf titles + legible `?state=` codec into one share-legibility story; OD-3 prices the old-link hard-cut | **Merge:** Fable's stage-1 architecture ruling (OD-S8) runs first; **Codex's STATE-ROUTING law is adopted as the union codec spec** (it is the more fully specified of the two); F-18's kf story + OD-3 carry; the round-trip probes (gradient-stops-lost; kf share-restore) are the born-REDs. |
| U-F11 | CARRY-LEDGER §B–§F fold (W46–W56, CH-4, W54, W53) | **MISSING** — the W46–W56 vehicle namespace is absent from the corpus (my grep: SEED-ROW-INVENTORY only); the two ratified close-laws (CH-4 p75-LCP≤2.5s "or V′ does not close"; W54 D-1 aurora-derive RUNS) are unowned; the P6 ingestion set NAMED CARRY-LEDGER.md and it was not folded — the zero-drop instrument was scoped to the two letters only. armA correction honored: W53's boundary-sampler bank IS terminal (banked with the D17 retrigger) — the vehicle NAME is what is absent | **CORRECT (the Fable side's decisive contribution)** — PL-9 makes the fold source LAW; W46→F-02 (rubric, amend-never-parallel), W47→F-06, W48→F-07, W49/W50→F-08 (D57 export seat; D55.iii routed by name), W52→F-09, W53→F-22 (rebuild ON v4, memo-first, R-BOUNDARY arrow corrected), W54+CH-4→F-12 (one mechanism family: the eager blob IS the LCP blocker), W55→F-23 + routed B3 tail; every §C/§D/§F row dispositioned by name | **Adopt-Fable wholesale:** PL-9 + waves F-06/07/08/09/12/22/23 + the F-24 disposition table enter the union as-is. This is the largest single Codex gap in these two domains and the Fable formation's largest single contribution. The union zero-drop instrument extends to L1+L2+ADDENDA+CARRY-LEDGER §B–§F+V-PRIME brackets (also discharging armA repair 15's addenda-crosswalk gap). |
| U-F12 | Motion oracles / π-DELTA / gate honesty | **CORRECT-leaning** — "a screenshot is evidence, not an oracle"; anti-gaming exclusions written into waves (M08/D25/G09); C07 frame/thermal/input-latency budgets; honest born-RED phone leg; K12 automated traces | **CORRECT** — PL-10 as universal LAW (any envelope/register number carries a trace, video, or timeline — motion-register tables are GATES with per-row trace ids); PL-11 (BORN-ABSENT labels for net-new capability; facts recorded, never dressed as gates — keeps the real REDs binding); PL-3 stills pairing | **Merge:** PL-10 + PL-11 become union-wide law; Codex's per-wave numeric budgets and anti-gaming exclusion columns are retained under them. Probe accounting = one line in the DELTA ledger (C21; no accounting theater). |
| U-F13 | Probe rig / capture honesty | **CORRECT** — the live audit ran against production; C07's phone leg truthfully born-RED rather than simulated green | **CORRECT** — F-01 mints the rig as a precondition wave from its own failure (profile lock, 0 frames): isolated profile, honest safe-area oracle (CDP inset override or real device — emulation resolves `env()` to 0), multi-touch synthesis harness, authed-admin recipe, each with a PROOF capture | **Adopt-Fable F-01** as the union's capture precondition; Codex's physical-device annex (M05/C07) rides it. Every downstream capture gate anchors to a named oracle class. |

### 1C. Process (cross-cutting, graded because both domains inherit it)

| # | Element | Codex | Fable | UNION |
|---|---|---|---|---|
| U-P1 | Convergence | **PARTIAL** — the thrice loop genuinely ran and convicted its own machinery (R3/RR-17 NOT CLEAN, production 0/190 stated everywhere; zero close-class lies found by two hostile lenses); but clean passes = 0/2 — the return is a strongest-core-with-named-gap | **PARTIAL** — each final is ONE completed thrice iteration (2 skeptics + adjudicator, evidence-re-derived); no whole-formation clean pass has been ATTEMPTED; less battle-tested than Codex's failed-honestly passes | **Both-wrong-do-X:** the UNION program itself gets a fresh thrice pass over the merged wave set (per C20/C23), then the two-consecutive-clean-pass bar applies to the union corpus — with the Codex clean-pass protocol's frozen-epoch discipline (trimmed per the armA apparatus diet) as the mechanism and the Fable seat-budget ledger (stated ceilings) as the cost law. |
| U-P2 | Zero-drop instrumentation | **CORRECT (instrument) / PARTIAL (scope)** — the 149-row SHA-pinned seed bijection with mutation-RED verification is the strongest zero-silent-drop implementation in the constellation; structurally blind to the CONVERSATION-ADDENDA and the CARRY-LEDGER | **CORRECT (scope) / prose (instrument)** — landing maps enumerate every canon row incl. addenda + fold source + recipient-existence checks; no machine check exists | **Merge:** Codex's bijection machinery re-pointed at the UNION canon universe (L1 + L2 + C1–C23 + CARRY-LEDGER §B–§F + V-PRIME brackets); Fable's recipient-existence law added as a validator rule (a handoff to an unresolvable name IS a silent drop, renamed). |

---

## 2. THE UNION WAVE-DELTA (structure + frontend domains)

Amendments the unioned mega-tranche makes. Source tag on every row: [C] = Codex-sourced, [F] = Fable-sourced, [C+F] = merged, [NEW] = both-wrong-do-X.

### 2.1 ADOPT

1. **[C] The manifest/validator substrate**: VALUE-TARGET-PATHS.json + KEYFRAMES-TARGET-PATHS.json + validate-target-paths.mjs + WAVE-EDGE-POLICY typed edges + module-graph hash discipline — the union's executable path authorities, regenerated under the amendments below.
2. **[C] The deletion-judgment apparatus** (4-question judgment, owner-precut annex, casualty proof, migration-before-deletion) as the union's prune execution machinery — gated behind ratification rows (2.2.3).
3. **[C] V24's decided-terminal decompose bar** (falsifiable KEEP conditions) superseding the seed's flat PRUNE; executed as a boundary-ledger row.
4. **[C] D03A** (mount-independent route capability registry) — the C3 architecture.
5. **[C] K12 kernel spec + M03 single-screen shell + M05–M10 scene waves + M08 contact sheet** — the C4/C2-kf substance, under the U-F2 sequencing amendment.
6. **[C] The G-band glass gap waves (G03/G06/G08/G09)** + the product-identity registers + the anti-template test + STATE-ROUTING.md as the codec law + LIVE-VISUAL-AUDIT as the before-evidence base + OA-05's api-stay reconciliation paper.
7. **[F] The boundary-delta ledger + surface-freeze law** (F-03/§1.4): every surface cut is a tombstoned, four-tree-re-grepped ledger row executing only at the co-land crossing.
8. **[F] The kf single-packet dispatch protocol** (F-07): phase-0 embedded preconditions (W9 quartet, MR2/MR4, CI-wire lint+proof:structure), enforcement-before-churn, 13-anchor born-RED census, DONE=rowed-never-landed; Codex K/M rows travel as the payload.
9. **[F] The CARRY-LEDGER fold whole** (PL-9; F-06/07/08/09/12/22/23 + F-24's disposition table) — incl. the two close-laws CH-4 and W54 D-1 with the OD-7 ownership row.
10. **[F] F-01 (probe rig) · PL-10 (motion oracles) · PL-11 (gate-label honesty) · the two-batch glass relay + F-21 reconvergence + BANKED posture · the recipient-existence law · the budgeted thrice seat ledger · the owner-decision dockets** (OD-S1 internal/ option-c, OD-S4..S8; OD-1..OD-9).
11. **[F] The manifest-gate single text with the parse-that whitelist** (`runtime deps ⊆ {@mkbabb/parse-that}`) + self-dep clause, implemented with [C] tarball/crater mechanics.

### 2.2 AMEND (merge-how)

1. **kf flatten [C+F]**: Fable governance spine (LT-10/LT-16 supersession annex, proof:structure EXTEND-never-replace, leaf-law re-key, anchor census) + Codex byte-snapshot replay gates as the second drift defense; `.dependency-cruiser.cjs:171` + the 755-line gate rowed with explicit dispositions.
2. **Export surface [C+F]**: dissolution mechanics + tombstones from Codex; ONE owner ratification row (six-key/`./value`-removal/root-export vs the 7-key freeze, OD-S7 folded in); Fable's explicit per-key specifiers under either outcome.
3. **Zone flips [C+F]**: K14/K15/K21/K22 (+ scroll/ingest decided-KEEPs) convert to owner-ratification rows citing EP-3 PATH-B / K F6 / the r2 four-tree census; Codex's apparatus executes the ratified outcomes; KEYFRAMES-TARGET-PATHS regenerated with typed conditional_paths so the path authority cannot pre-decide them.
4. **Tests-isomorphism [C+F]**: Codex manifests+validator implementing Fable's spec-ownership model + honest-label law; CSS-token `.test.ts` mirrors deleted.
5. **C4 cube [C+F]**: kernel proves on the five non-cube scenes first; cube migrates second under trace-parity + physical-device gates (U-F2).
6. **C2 [C+F]**: dual-homed; Fable measurement record first → merged G09/F-04 glass wave + Codex M08; recap row credits both apps.
7. **Value demo structure [C+F]**: Codex closed edge laws as gates; defect-driven moves (Fable posture); kf demo takes the Codex-scale overhaul (C4 authorizes).
8. **C22 audit [C+F]**: Codex captures = before base; Fable widened matrix (interaction states, non-route surfaces, authed admin, boundary band, 14 routes) = the completing pass on the F-01 rig.
9. **C10 [C+F]**: Codex G05 budgets + D05 backplates + Fable F-05 living-dot API + F-10's 23-site family map — ALL gated on W-ING (2.3.1).
10. **C6 [F→C]**: OD-S8 architecture ruling first; Codex STATE-ROUTING codec second; F-18 + OD-3 for kf.
11. **Zero-drop [C+F]**: Codex bijection machinery re-scoped to the union canon (addenda + CARRY-LEDGER included); recipient-existence as a validator rule.

### 2.3 ADD (both-wrong-do-X)

1. **W-ING [NEW]** — the BJ + IOS27-MICRO ingestion wave: read IOS27-CODEX (numbered laws), SUFFUSION-MATRIX, BAND-FEEDBACK-MOTION as BOUND design inputs; reconcile every motion grammar on both sides by law number; blocks every motion-executing wave. (Cures the symmetric C10 provenance debt.)
2. **W-GOV [NEW]** — the governance-lineage annex wave: names every superseded ruling (LT-10, LT-16), rows every live kf gate config with extend/retire dispositions, writes the D50-supersession row, and installs the "silence re-litigates" check into the union close.
3. **DesignSync retrigger execution row [NEW]** — the banked method gap (Codex's pattern) discharged at the union's design gates before implementation; recorded as unmet-on-both-sides at formation.
4. **Union convergence run [NEW]** — fresh thrice over the merged program; two consecutive clean passes on the union corpus (Codex protocol, apparatus-dieted; Fable seat budget).

### 2.4 KILL

1. **[C] The unilateral six-key freeze** as a settled fact (demoted to the ratification row) — canon-breach on OA-14-only authority.
2. **[C] The zone-flip fiat** (pre-decided prunes in the sole path authority) — demoted to ratification rows; the apparatus survives.
3. **[C] The CSS-token `.test.ts` mirror sand** (E-C21).
4. **[C] The bespoke six-regex source projector** (replaced per R3's own prescription with a real lexer) — the anti-regex formation's regexes die.
5. **[F] PC-structure's prose-only target trees** as authorities (superseded by the manifest substrate; they remain as opening positions).
6. **[F] The "glass-equivalents EMPTY-SET" claim** as stated (re-censused against the merged gap register).
7. **[F] The PD final's silent DesignSync omission** (replaced by 2.3.3).

---

## 3. APOTHEOSIS NOTES — what each approach teaches the other

**Codex → Fable:**
1. **Executable authority beats prose.** SHA-pinned manifests, one validator over both repos, typed conditional edges, mutation-tested gates — the Fable finals specify gates in prose that Codex ships as running, selftest-probed code. The union's Fable-side habit to acquire: never leave a law without its validator.
2. **Verify the premise on disk before cutting the wave.** Codex's born-RED witnesses were spot-checked TRUE (OrbitalDrag triple-lane, the 19-barrel census, the empty-toolbar measurement). The Fable PD program needed its skeptics to catch stale counts (15-vs-14 routes, ~20-vs-23 dot sites) that Codex-grade premise verification would have pre-empted.
3. **Run the audit before forming the audit wave.** The 21-route measured live audit made every Codex frontend wave evidence-grounded; the Fable audit is still a plan with zero frames.
4. **Honesty as architecture.** 0/190 and NOT CLEAN in every authority doc; the newest artifact convicts its own machinery; failed passes booked as evidence. The Fable side has honest LAWS (PL-11) but has not yet subjected itself to a whole-formation hostile pass.
5. **Identity as a falsifiable device.** The anti-template test is transferable craft: name what a generic outcome would look like, and gate against it.
6. **Deletion as a judged chain**, not a grep: four questions, precut annex, casualty proof, migration-before-deletion.

**Fable → Codex:**
1. **Governance continuity: amend, never re-found.** The letters' own sentence — "refutation amends the charter; silence re-litigates it" — names the Codex P0 exactly. Superseded rulings are named (LT-10, LT-16); live gates are rowed; parallel authorities are forbidden no matter how good the replacement machinery is.
2. **Inherited-DECIDED rows are inputs, not options.** The CARRY-LEDGER close-laws (CH-4, W54 D-1), EP-3/K F6 fences, D54/W53-56 vehicles: a formation that cannot see the standing record re-derives worse and forecloses owner decisions by fiat — even when factually defensible, that is procedural theft.
3. **Scheduling is a correctness property.** The boundary-delta ledger, enforcement-before-churn, never-flatten-dying-code, memo-first dependency arrows — the Fable formations repeatedly caught inversion classes (F-04⊥F-05, W53's arrow, the starved glass round-trip) that pure target-state thinking misses.
4. **Gate-label honesty (PL-11).** Distinguishing born-RED (live defect) from BORN-ABSENT (net-new capability) from recorded facts keeps the real REDs binding — the Codex corpus's uniform born-RED cell dilutes this.
5. **Parsimony as a budget, not a vibe.** Stated seat ledgers, ≤N-run probe budgets, one-line accounting, apparatus proportionality (the C21 conviction: 75% apparatus / 4% deliverable). Also: externally-gated waves close BANKED with a named re-trigger — never partial-as-done, never silently open.
6. **The recipient-existence law.** Every handoff cites a file/wave id that exists in the corpus — the check that catches phantom programs (armB's own "apparatus" sibling) and Codex's blind addenda universe alike.

**Shared lesson (both failed it):** an owner-named corpus (BJ/IOS27) is a BOUND INPUT, not a preservation fingerprint — read it before designing in its domain. And a formation is not converged until a hostile pass it did not author says so twice.

— Arm C union adjudicator, `claude-fable-5`, 2026-07-19.
