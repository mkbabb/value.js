# OWNER RULINGS — the merged docket, RULED 2026-07-20

The owner ruled the full 22-row merged docket (`armC/UNION-APOTHEOSIS.md` §4) in
conversation, 2026-07-20: **row 15 OVERRIDDEN upward** (fourier first-class); all
other rows ratified per the union recommendations. This sheet is the terminal
disposition record; §4 of the apotheosis is CLOSED by it. Union-row effects cite
the apotheosis §2 delta ids.

## Surface & release

| # | Ruling | Effect |
|---|---|---|
| D-1 | **7-key freeze HELD.** Only the already-decided `./quantize` demotion lands, at the co-land boundary. No `./value` removal; no root-export change without its own tombstone. | AM-12 resolved (freeze); KL-8 confirmed |
| D-2 | **Glass 7.x peer-move.** No glass major scheduled from a value tranche; any 8.0.0 is glass's call via the BH-inbox relay. | AM-19 sub-row resolved |
| D-3 | **Boundary GO — entry-gated, undated.** Enter only once 4.1.x (the SCI-1 vehicle) has shipped and the two-leg ERESOLVE matrix is proven by execution. | F-GOV-05 activated |
| D-4 | **Provider trust: per-seat `model_served` + spot-probes accepted.** No attestation spend. | Standing law |
| D-5 | **C17 roster ratified as run**: sweeps/censuses/inventories/mechanical lenses on Opus; all design, skepticism, adjudication, union on Fable. | Standing law |

## Zones & records

| # | Ruling | Effect |
|---|---|---|
| D-6 | **flip KEEP · physics/morph KEEP** (EP-3 PATH-B records hold) · **`compileToViewTransition` TRIM** (the seed's one real emit-shrink, demo=0) · **presets SHRINK to the consumed core.** | AM-11 rows resolved; K15 trim, K22 shrink authorized |
| D-7 | **scroll KEEP** (compile-wired, native fast lane, K-1 fence). **ingest RETIRE with tombstone** — an explicit owner piercing of the K.W8 book row: with value owning full stylesheet/keyframes parsing (C12) and kf leveraging it, kf-side text ingestion is structurally redundant. Fidelity tests fold into the value-side parse program; casualty proof per the deletion-judgment grammar (AD-12). | The one zone kill; travels on the P4.5 dispatch |
| D-8 | **`internal/` DISSOLVES into named modules (option-c)**, EXTENDING proof:structure — never a parallel authority. The 40+-importer cost is paid inside the flatten wave where anchors are re-derived. | AM-10 executes with this shape |
| D-9 | **transform/decompose.ts PRUNE** at the boundary, with tombstone. Four-tree census = proven vacuity. | AD-13's KEEP-bar moot; boundary-ledger row |
| D-10 | **Idempotency: D2 §3 P2 KEPT today** (no durable store). The overturn path is PRE-AUTHORIZED iff the now-first-class fourier job layer (D-15) lands and demonstrates the need — and must cite D2 §3 P2 by name at that moment. | AM-14 resolved with a named re-trigger |
| D-11 | **Cursor errors CONVERGE to 400** — a named D3/D.W2 overturn; demo migration priced in-wave. | AM-15 resolved |
| D-12 | **Restore verb: divergence RATIFIED.** W45's retirement stands; A09's lifecycle redesign DECLINED; revision-revert covers the need. | AM-16 resolved |
| D-13 | **Tier: A07T PARTIALLY ADOPTED as a named overturn** — featured leaves the tier enum and becomes a relation (curation, not classification); `(visibility,tier)` (L.W3 + J.W1c) retained for everything else. | AM-17 resolved |
| D-14 | **/shares BUILD** (value) — the C6 share-URL backend; kf gets no share server (refuse or export locally, per the Codex law). | AM-18's docket sub-row resolved |
| D-15 | **OWNER OVERRIDE — FOURIER IS FIRST-CLASS.** Full isomorphism between value `/api` and fourier's API; the COMPLETE fourier program is ratified — identity (A21A/J), CRUD, assets, durable jobs, search, history, derivation, palette binding (the A21–A26 depth). Nothing banked. ADD-9's facility-correspondence ledger upgrades from a small annex to a first-class bidirectional isomorphism CONTRACT (fork≅derivation, revision≅revision, refusals named). Direct-edit grant for fourier: GRANTED. kf stays dispatch-only (P4.5). The Codex A-band's fourier scope is re-graded from scope-creep to RATIFIED SCOPE — still subject to the record rulings D-10..D-13. | Overrides the union lean; ADD-11 + F2-api waves upgrade to first-class; AD-2 registry extends across both APIs |

## Grammar & spec

| # | Ruling | Effect |
|---|---|---|
| D-16 | **spring() grammar: VALUE owns it**, parsed behind the C12 experimental flag; the K F6.6 solver fence keeps the solver in kf. | R-11 closed |
| D-17 | **HDR BUILD** (C12 decides; the 11-day drop was unjust; V19 = the spec). **R-EVAL RETIRE** — DOM-resolution status quo; no static calc evaluator. | R-10 closed BUILD; R-12 closed RETIRE |
| D-18 | **contrast-color(): WCAG21 metrics REPUBLISHED with it; APCA behind the experimental flag** until the spec settles. | OD-C1 closed; V20 executes |
| D-19 | **SoA BANKED** with the pre-ratified threshold — fires only on a real end-to-end workload measured to V16B's law. The 3.0.0 tombstone stands. | R-13 closed BANKED |

## Design & demo

| # | Ruling | Effect |
|---|---|---|
| D-20 | **Mobile toolbar: D03A capability registry + the dock-morph candidate** (toolbar re-emerges as a collapsed dock state — single-screen identity, glass dock idiom). Final form at the design gate, with DesignSync frames, after W-ING binds the BREATH OF LIFE corpus. | OD-5 resolved; C3 wave shaped |
| D-21 | **Easing precision: MEASURE FIRST** (F-PD-03; the percept lives in tile size/stroke rendering), then set the gate from the record. **kf old share links HARD-CUT** — no redirect shims. | D-24 + OD-3 resolved |
| D-22 | **Router idiom RATIFIED** (`usePaneRouter` stays the source-of-truth); the UrlEnvelope codec layers on top; no router reshape mid-tranche. | OD-S8 resolved; STATE-ROUTING executes |

**Through-line as ruled:** decided records hold except the two explicit piercings
(D-7 ingest, D-13 featured-relation), both tombstoned by name; every silent Codex
overturn resolved here rather than by fiat; nothing dated, only gated; fourier
elevated to a first-class isomorphic peer of value's own API surface.
