# Union Ingestion

## State

**ROW INVENTORY TERMINAL (114/114); CORPUS OPEN — zero convergence credit.**
The July 19 union relay and July 20 owner rulings were not present in the
V-next corpus that received the R4 review. They invalidate every attempted
post-R4 freeze until the complete union projection is folded into the live
corpus, challenged and adjudicated. Production execution remains **0/193** and
whole-formation clean credit remains **0/2**.

The mail discipline is repaired at `../coordination/INBOX.md` rows I-13 and
I-14. The separate `UNION-ROW-INVENTORY.json` now gives every union row an
exact owner, terminal disposition, DAG effect and gate consequence, and
`tools/validate-union-inventory.mjs` resolves those owners against the 193-wave
books. `CANON-SUPPLEMENT-INVENTORY.json` separately binds the five omitted
inputs and projects all 35 existing identities (C1–C23, Carry §B–§F, V′ §0–§4
and both receipts) without changing the 41+108 seed law. These inventory steps
are terminal; `READ` is not corpus `FOLDED`.

## Immutable authority

| Input | SHA-256 | Role |
|---|---|---|
| `../coordination/value-inbox-2026-07-19-union-apotheosis-relay.md` | `24388663eabb4214765e5dc5ea24f22a1523af157ff52343751d6ea37dbad48f` | delivery receipt |
| `../coordination/value-inbox-2026-07-20-owner-rulings-relay.md` | `99571d62c8d42c44bea46b2b946f1a64bd0df0e8ac926f30f61fd361c5af03ee` | delivery receipt |
| `../apotheosis/armC/UNION-APOTHEOSIS.md` | `80bc507010995934aa09faef53c45889b06c18ba108540130efa5815ff1e4e57` | 72-row union and 20-row repair ledger |
| `../apotheosis/OWNER-RULINGS-2026-07-20.md` | `2ee23d3098f9f96e4df792095504cabf2e96db185d1cc4a0d7b717b4bb0ec5bb` | terminal 22-row owner docket |
| `../apotheosis/CONVERSATION-ADDENDA.md` | `dc88daeba1f884d8e667237a8d735b4b886340936331e1f3e34407896d7b0e26` | C1–C23 canon to enter the seed bijection |
| `../reformation/CARRY-LEDGER.md` | `725490e9b7afa02eb93476ec77557a34d4eba202e2a36265e8a97a432c67f9a9` | §B–§F fold-forward authority |
| `../reformation/V-PRIME.md` | `b749053ed16d3ec6092b323ad77775b6b099d3d7482e6eb370f1e28f9f20f59f` | bracket and supersession authority |

The rest of `../apotheosis/` is evidence, not automatic formation authority,
and remains pull-on-demand. The quarantined `r1-opus-refuted/` corpus remains
forbidden.

## Zero-drop account

The ingestion surface contains exactly:

- 72 union delta rows: AD-1–AD-17, AM-1–AM-30, ADD-1–ADD-13 and KL-1–KL-12;
- 20 priority repair rows;
- 22 terminal owner rulings D-1–D-22.

The adjudicated crosswalk is **114/114 terminal at the inventory layer**: 72
union rows, 20 repair rows and 22 owner rulings. The validator also pins the
original 149-row seed inventory separately, rejects missing supplement
identities/recipients and rehashes all five supplement inputs.
This does not claim that the corresponding wave bytes or gates have landed.
The crosswalk distinguishes already-satisfied, amend, add, retire and
supersede; it does not manufacture 114 waves when an existing owner can carry
the exact job, nor fold an irreducible execution unit into a god-wave merely
to preserve the old count.

## Adjudicated topology

The hostile pair and third adjudicator agree that the 190-wave registry is an
enumeration backbone, not the final graph. The minimal ownership-correct graph
has **193 waves**:

| Band | Count | Union change |
|---|---:|---|
| P | 8 | parser spine rewritten; no new wave |
| V | 45 | add `V16R`, the inherited SCI-1/D54/W56 4.1.x vehicle |
| K | 27 | terminal zone rulings rewrite existing owners |
| A | 36 | API rulings and first-class Fourier isomorphism rewrite existing owners |
| G | 11 | add `G00I`, the bound Breath/design-input epoch |
| D | 33 | defect-driven Value refinement; no wholesale-transpose addition |
| M | 13 | Keyframes overhaul and sequencing remain in existing owners |
| C | 20 | add `C00P`, the shared honest visual/input probe rig |
| **Total** | **193** | three irreducible additions |

`G00I` and `C00P` are separate because authority selection/reconciliation and
measurement infrastructure can fail independently and have different owners.
Neither belongs in one app's D/M band. `V16R` is a real minor release before the
later breaking boundary, so C10 is the sole **breaking** cut rather than the
sole release of any kind.

The adjudicated parser law is exact published `@mkbabb/parse-that@1.0.0`, a
fresh upstream BBNF module-topology receipt at P00/P01, and one BBNF-module →
TypeScript-combinator mirror. The historical Value parser supplies behavior and
benchmark witnesses only. No private parse-that source, novelty, candidate or
`^1.0.0` range enters Value.

The total consumer universe remains mandatory. The union's suggested four-root
narrowing is rejected: improving the source projector cannot erase known or
discoverable consumers. The wave grammar gains the compact Routing/π cell
defined in `WAVE-ROUTING-PI.md`; it does not duplicate full process prose in
every row.

## Repaired contradictions

The union found the following false pre-ingestion claims. They are now closed
only by the named owner ruling and its owning wave; any recurrence reopens the
formation and receives no execution or clean-pass credit:

- **CLOSED by D-1/V00C/V29T:** the former six-key/`./value` tombstone proposal;
  seven keys survive and only `./quantize` is demoted.
- **CLOSED by D-6/D-7/D-9:** the former blanket K14/K15/K21/K22 prunes, K09
  KEEP posture and conditional V24 arm.
- **CLOSED by D-10/D-12/D-13:** Value durable idempotency, restore and total
  tier-deletion proposals.
- **CLOSED by D-16/D-17:** spring rejection and R-EVAL implementation; HDR is
  mandatory.
- **CLOSED by D-2/D-3/G07/C10:** the former Glass 8 cut; the vehicle remains an
  exact Glass 7.x minor.
- **CLOSED by KL-1/P00–P07:** no unfrozen SK-V25 artifact enters Value; the
  published 1.0.0 assay remains self-contained and upstream work is PT-E only.
- **CLOSED by the apparatus diet and deferred return schema:** real path, DAG,
  seed, API and deletion gates remain; unused execution-return machinery does
  not govern formation.

## Closure order

1. finish the already-open RR-17 correction slice without expanding it;
2. obtain two independent hostile 114-row crosswalks and a third adjudication;
3. land the adjudicated row-to-wave/DAG/disposition projection;
4. extend the canon bijection to C1–C23, CARRY-LEDGER §B–§F and V-PRIME;
5. perform the apparatus diet and write the C14 published-1.0.0 prototype;
6. run a fresh focused union/RR-17 triad;
7. freeze the resulting union corpus and earn two consecutive whole-formation
   clean triads.

No earlier review, green selftest or old corpus hash can skip a step.
