# OWNER RULING D-23 — parser-baseline primacy — RULED 2026-07-20

**RULED: Position B — MIRROR-PRIMARY.** The C14 lane — BBNF modules mirrored 1:1
into parse-that TypeScript combinators (the `vnext/prototypes/c14-css` assay,
graded REAL-AND-BENCHED by the post-convergence audit) — is the baseline engine
for the /css swap. The deposed `164343c1^:src/parsing/` tree is demoted to
**behavior and benchmark witness only**. This supersedes the union's AM-1
restoration-primary arm; everything else in AM-1 stands (parse-that consumed as
exact published 1.0.0; the /css public surface frozen — `parseStylesheet` /
`collect*` / the 8-code `ParseIssue` union / kf's 29 consume sites never move;
`bench/` resurrected; the retiring regex parser measured once for the record;
parse-that-internal work stays PT-E; C15 semver ≤1.1).

**OWNER CONDITION — THE PROOF GATE PRECEDES EXECUTION.** The parser must be
PROVEN before tranche production execution begins:

1. **Semantic equivalence** — differential run of the mirror engine vs the LIVE
   v4 /css parser over the full corpus (value's test corpus + demo CSS + the
   assay corpus), with the deposed tree as arbitration witness on divergences:
   AST/value congruence, ParseIssue code fidelity, `collect*` semantics, and
   kf's 29-site consume seams all covered. Divergences classified
   mirror-defect / regex-defect (witness arbitrates) / spec-question.
2. **Coverage census** — the mirror's grammar coverage vs the frozen surface and
   the July-2026 spec set; every gap named and waved (gaps do not block the gate
   if the frozen-surface subset is total; the C12 lane owns the growth).
3. **Comparative performance** — the resurrected bench: mirror vs the regex
   parser (its once-for-the-record measurement) vs the deposed-tree baseline;
   the historical ratio gate is the bar.

Execution begins only when the gate is GREEN (or the owner explicitly
overrides). The fleet's P00/P01 born-REDs are exactly this gate — shared, not
duplicated.

**Begun:** the root session opened the proof campaign 2026-07-20 in isolation
(working copy of the frozen assay; zero writes to `vnext/` or any repo source).
Evidence lands in `post-convergence/../parser-proof/`; results relay to the
fleet as evidence letters.
