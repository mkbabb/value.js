# P-Band Thrice Record

## Cell

| Role | Agent | Model | Premise |
|---|---|---|---|
| Hostile A | Huygens `/root/p_skeptic_a` | `gpt-5.6-sol`, `ultra` | P00–P07 architecture, API, combinators, DAG, and tests are wrong |
| Hostile B | Mendel `/root/p_skeptic_b` | `gpt-5.6-sol`, `ultra` | P00–P07 corpus, fidelity, BBNF independence, craters, and release logic are wrong |
| Adjudicator | Bernoulli `/root/p_adjudicator` | `gpt-5.6-sol`, `ultra` | independently prove/disprove mechanisms and apply the latest owner ruling |

No agent edited source or read `vnext/r1-opus-refuted/`.

## Independently reproduced 1.0 defects

Against the built/published TypeScript parse-that surface, the cell reproduced:

| ID | Reproduction result | V-next disposition |
|---|---|---|
| PT-GLOBAL-DIAGNOSTICS | sequential recovered diagnostic counts accumulate `1 → 2` | P02 admits a narrow fix only if P01 full-CSS recovery reproduces it |
| PT-RAW-CROSS-SOURCE-MEMO | raw memoized execution returns `"hello"` for later source `"world"` | external unless P01 necessarily enters that raw path and a bounded guard suffices |
| PT-NESTED-RECOVERY-ROLLBACK | outer rollback pops the inner diagnostic and retains the outer item | P02 admits a narrow checkpoint fix only from a P01 RED |
| PT-STATE-RESULT-DIVERGENCE | failed returned state, `Parser.state`, and declared `parse(): T` disagree | external by default; declaration-only blocker corrections may be admitted |
| PT-COMBINATOR-TYPE-DIVERGENCE | `all`, nondiscarding wrappers, and assertions disagree between declarations and runtime values | external unless an exercised full-CSS signature is a blocker and a narrow declaration correction is truthful |
| PT-NONASCII-DISPATCH | dispatching `é` fails | P02 admits bounded existing-dispatch repair only if P01 exercises it |
| PT-EOF-DIAGNOSTIC | regex failure at EOF loses expected/furthest evidence | P02 admits a narrow merge repair only from a P01 RED |
| PT-CORE-SCC | apparent core reaches diagnostic/leaf/packrat sectors and a heavy shared chunk | external package-topology session |
| PT-LOADER-IDENTITY | ESM and CJS constructors differ and cross-format `instanceof` fails | external module-format/identity session |
| PT-DIST-SKIP | source tests can pass while artifact coverage skips when dist is absent | P00/P04–P06 require built tarball craters; larger package-test redesign is external |

These probes establish defects, not automatic authorization to redesign the
library. P01 is the necessity oracle for this tranche.

## Skeptic mechanisms and third-Sol result

| Mechanism | Third-Sol disposition | Proof consequence |
|---|---|---|
| The prior P01 preselected `RunContext`, entrypoints, and optional Tape/SoA while claiming to prototype them. | **Proved.** | Representation commitments were removed; P01 now prototypes full CSS on unchanged 1.0.0. |
| “Real CSS corpus” was undefined. | **Proved.** | P00/P01 now require digests, weights, semantic projections, valid/malformed/recovery/Unicode/pathological partitions, environments, warm/cold protocol, and route tracing. |
| Bytes and JavaScript UTF-16 code units were conflated. | **Proved.** | Branded original/processed code-unit and byte domains plus half-open boundary maps are binding; exact means original-string equality. |
| BBNF was falsely described as a missing independent oracle and coupled to npm parse-that. | **Proved.** | BBNF/Rust and npm artifacts are distinct; all host/emitter/grammar/oracle uplift routes to the separate major session. |
| Casualty census, migration, suitability, and uplift were one green crater. | **Proved.** | P04–P07 separate the immutable no-RC receipt, published-control crater, packed consumers, final V/K suitability, and C10 no-republish boundary. |
| A clean-breaking opaque/ESM-only parse-that reset should land immediately. | **Rejected under newest owner ruling.** | It remains a possible external-session alternative, not a V-next implementation. |
| Strictly additive compatibility must be preserved regardless of defects. | **Rejected.** | Defects are minimized and externally routed; no local correction, alias, shim or dual path may land. |
| Tape/SoA/Pratt or new `cut`/`attempt`/`label` families should be reopened here. | **Rejected.** | They are explicitly external-routed and cannot become a prototype prerequisite. |
| P07 should publish parse-that. | **Rejected.** | Parse-that remains the exact published 1.0.0 receipt and neither P07 nor C10 republishes it. |

## Binding owner-scope reconciliation

The earlier owner statement permitted a large beta change within the 1.1 line.
The later, narrower ruling supersedes that authorization for V-next:

> Published parse-that powers the full-CSS prototype. Only minor fixes proven
> necessary by that prototype may land. Novel features and the major
> parse-that/BBNF session occur separately.

Accordingly:

- a broad beta reset and every public breaking change remain unauthorized here
  and route to the active BBNF/parse-that major session;
- P00–P07 use one 1.0.0 registry/no-link control, full-CSS necessity proof,
  banked external blocker packets, a signed no-RC receipt, identical packed
  consumer craters, and the C10 no-republish handoff;
- every optional or architectural item is recorded in
  `coordination/BBNF-PARSE-THAT-MAJOR-HANDOFF.md` rather than silently dropped;
- no external session can mutate value/keyframes or feed a speculative surface
  back without a new owner return.

## Result

The original P band was **not clean** and its clean-pass count reset. The
replacement rows in `waves/P-V.md` are the binding P formation. They remain
subject to the later whole-corpus C08/C09 clean passes.
