# Root repair round 2 — skeptic B

Independent verdict at the reviewed snapshot: **NOT CLEAN**. No files were
edited by the reviewer.

Open at that snapshot:

1. **RR-17 ignored lock/transitive edges.** The resolver scanned only
   `package.json` dependency sections and selected source extensions. A
   two-root lockfile-only dependency fixture exited zero with a resolvable
   zero-edge receipt. Live Words lock data corroborated transitive parse-that,
   Value and BBNF edges absent from its root manifest.
2. **Slides-K was named but not mandatory.** The C02D wave requires an explicit
   row for the separate `/Users/mkbabb/Programming/slides-K` Git worktree, but
   the canonical bounds and bounds validator did not name it. Its disposition
   therefore depended on incidental discovery rather than bounds authority.
3. **Repair-ledger identities and counts were stale.** Six proof-tool hashes
   differed from current bytes, and clean-exec had moved from 224 to 225
   adversarial rejections.
4. **Bootstrap protocol prose described the superseded final-manifest matrix.**
   The current schema/tooling had moved to a pre-campaign authority file, but
   the reviewed prose had not yet followed it.

Closed by concurrent repair and rechecked during the review:

- same-size report mutation race;
- physically impossible async wall time;
- validation-time child-session/report drift;
- post-hoc bootstrap self-certification;
- clean-command allowlist parity;
- duplicate critic tags;
- RR-16 gate/triad chronology;
- executable D19–D25 Breath disposition proof, with 14 same-layer forgery
  rejections.

The consumer selftest remained green at two real-Git positives and 26 resolver
rejections, but lacked the lockfile-only crater and thus did not falsify the
first finding.
