# Routed Handoffs

## Packet law

Each sibling receives only:

1. authority and exact pins;
2. assigned wave IDs;
3. relevant packet sections;
4. named evidence pulls;
5. explicit exclusions;
6. required return contract.

The root does not reproduce or preload the deep corpus. No packet may read `vnext/r1-opus-refuted/`.

## Formation pins and consumer-universe law

Repository HEADs and exact NUL-delimited porcelain digests are canonical in
[`../PROVENANCE.md`](../PROVENANCE.md). Every receiving wave rechecks them
before mutation; drift is an overlap event, never permission to reset or clean.

C00U's included universe is exactly: parse-that/typescript; value library,
demo and API; canonical keyframes-v-exec library and demo; glass-ui; active
`.p-totality/atlas`; sci-report including its in-repository Atlas; bbnf-buddy;
Slides and its distinct mandatory Slides-K worktree; bbnf-lang/playground;
Muster/frontend at its canonical lowercase realpath; Speedtest;
Words/frontend; Fourier API and web; and latex-paper as the P06 direct
parse-that consumer.

The standalone `/Programming/atlas` is stale and excluded. Derived
`.p-totality/sci*`, keyframes owner/worktrees and BBNF forks/scratches; Claude
worktrees; csc411 and DNS roots with no current edge; and vendored Hassio
receive separate excluded rows. A newly unavailable root receives a named
retrigger. No root or edge is silently called inactive, tested or
authoritative. C00U inventories recursive package manifests, npm package-lock
v2/v3 direct/transitive edges, runtime/type/dynamic/CSS imports,
origins/worktrees and realpaths. Workspace child manifests are traversed
recursively. C00/C01–C04 own packed export/alias resolution, while A26/C05/C10
own deploy-root and cut evidence; those are not fabricated as dependency-edge
kinds. C05 reruns and freezes both the signed universe and those routed proofs.

`CONSUMER-UNIVERSE-BOUNDS.json` makes that prose executable. Its semantic
bounds require fifteen real-Git identities: `atlas-active`, `bbnf-buddy`,
`bbnf-lang`, `fourier-analysis`, `glass-ui`, `keyframes`, `latex-paper`,
`muster`, `parse-that`, `sci-report`, `slides`, `slides-k`, `speedtest`,
`value`, and `words`. They also require six typed `repository-subdirectory` identities:
`bbnf-lang/playground`, `fourier-analysis/api`, `fourier-analysis/web`,
`muster/frontend`, `sci-report/atlas`, and `words/frontend`. The Muster law
binds the lowercase canonical realpath; a case-variant spelling cannot create
a second root. Omitting or excluding a required Git root, or losing, escaping,
or realpath-drifting a required subdirectory, fails before a green receipt.

The machine handoff is a strict `consumer-universe.schema.json` record. C00U
runs `tools/resolve-consumer-universe.mjs --input <universe> --output <receipt>`
and returns the exact JCS `vnext-consumer-universe-receipt/2`. The receipt binds
the input bytes and semantic hash, schema, resolver and canonical 190-wave
registry. Exit 2 with `resolvable:false` is an auditable unavailable result,
not a dependency green; C05 must rerun the resolver and may proceed only on
exit zero with no blockers. Duplicate realpaths, unknown roots/owners and
prose-only exclusions are invalid rather than schedulable remainder.

The exact C ledger is:

```text
C00U C00 C01 C02 C02B C02D C02L C02M C02S C02W C03P C03 C04
C05 C06 C07 C08 C09 C10
```

Ranges are descriptive only; packets and scheduler dependencies use these
literal IDs.

## PT-E — parse-that and bbnf-lang

### Authority

- parse-that: `/Users/mkbabb/Programming/parse-that`,
  `master@ef10d5b78236c4a30a7bb22a6113b60bdc4bdf42`; preserve the exact
  eight-entry dirty fingerprint in provenance, including uncommitted banked Sol
  research.
- bbnf-lang: `/Users/mkbabb/Programming/bbnf-lang`,
  `master@af15f63e0d2d3d719938c13b906a50acbb92ea3b`; preserve the exact
  73-entry dirty fingerprint; only C02L may mutate the playground, and no V-next
  wave may mutate grammar/runtime/Tape/SoA/restart-major work.
- Waves: `P00 P01 P02 P03 P04 P05 P06 P07`; related consumer owners are
  `C00U C00 C02B C02L C05 C08 C09 C10`.
- Target: consume published `@mkbabb/parse-that@1.0.0` for the full-CSS
  prototype. Return `no-rc` and do not republish. Every substrate blocker is
  banked and routed to the active BBNF/parse-that campaign and prevents V-next
  adoption pending a separately authorized epoch.
- The 2026-07-18 upstream portfolio now contains four full orthogonal
  verticals—staged/compact tape, independently verified/schema-row,
  persistent incremental/rope, and GLL-SPPF/eager arena—under unchanged locks.
  This is coordination state, not Value design authority or product credit.
  `PT-0.W0` remains deliberately RED and does not freeze or recapture Value
  while V-next formation moves. Graph P3.1 resource twins are measured green,
  but semantic movement remains denied; CSS/JSON remain isolated assays with
  zero candidate or product credit. P00/P01 wait for the upstream task to
  reconcile the then-current published parse-that tip and artifact digest,
  then refresh V2 from that public truth. After the boundary stabilizes, only
  exact HEAD/tree/receipt identity returns upstream as consumer evidence, not
  authority.
- Novel execution models, new combinator families, Tape/SoA, and major
  parse-that/BBNF topology work are excluded and routed to the separate major
  BBNF session.
- P01 does not author a second `.bbnf` grammar. It consumes the terminal
  upstream modular CSS BBNF bytes as its frozen grammar/oracle authority and
  authors only their exact handwritten TypeScript combinator mirror inside the
  isolated V-next prototype harness. Historical host behavior is controlled by
  [`../BBNF-HOST-CONTROL.json`](../BBNF-HOST-CONTROL.json), whose validator
  materializes content-addressed archives rather than the active dirty checkout.
  The capsule is an execution control, not a package recommendation. P01 never
  writes `bbnf-lang` and routes any host/module insufficiency upstream.
- P01 authorship is governed by
  [`../P01-INDEPENDENT-AUTHORSHIP.json`](../P01-INDEPENDENT-AUTHORSHIP.json).
  It stays born RED until P00/V01/V02 freeze one execution epoch and two
  distinct Sol-ultra sibling sessions, each spawned with `fork_turns:none` and
  zero inherited dialogue, return canonical JSONL-backed receipts. The only
  common ancestry allowed is the epoch-bound coordinator; author-to-author
  ancestry, sibling transcript/artifact visibility, session reuse, or a third
  non-independent adjudicator fails closed. The same epoch binds the latest
  terminal-P00 append-only coordination tip, exact parse-that/BBNF pins and a
  persisted plus live `validate-pt-coordination --require-current` result; its
  stream hash appears in both isolated author directives.
  Access evidence is a canonical projection derived from each Codex JSONL,
  including tool calls and outputs, exec, file/MCP-read, follow-up and message records. Because the
  platform cannot prove uninstrumented operating-system non-access, the hard
  boundary is concurrent author sessions and both disjoint proposal roots
  frozen before any recorded peer-root disclosure; no stronger claim is made.
- Publication owner: C10 only.

### Packet

- Formation handoff P1 parser archaeology and owner decree.
- P-band wave specifications.
- V01/V02 registry contracts.
- V03 source-fidelity requirements.
- K09 ingest consumer requirements.

### Pull only when needed

- parse-that TypeScript `src/parse/{state,parser,leaf,utils,packrat}.ts`;
- parse-that package exports and packed bundle graph;
- the byte-canonical external handoff and, for P01 only after current-clearance,
  the exact public BBNF module syntax/examples already captured by the clean
  host capsule required to author the isolated CSS grammar; current uplift
  implementation remains reserved for SK-V25;
- historical value parser/benchmark files named by the handoff.

### Exclusions

- no Pratt, Tape, SoA, new combinator, run-model, package-topology, or BBNF
  novelty in this tranche;
- no breaking public change or removal in V-next; every such candidate is an
  active-major-session handoff row, never a local version branch;
- no value-specific primitive in parse-that;
- no publish before C10;
- never load the quarantined r1 directory.

### Return

P07 returns the exact accepted P04 unchanged-1.0.0 receipt/digest after
P01/P05/P06 and final V29/K09 suitability, plus the external-route ledger and
unconditional C10 no-republish instruction. Any surviving blocker returns an
explicit external owner and blocks this tranche.

## KF — keyframes.js library and demo

### Authority

- canonical checkout: `/Users/mkbabb/Programming/keyframes-v-exec`;
- current observed clean HEAD
  `master@81a56990736ced5b5edde0b84c527680ac7689b1`;
- source evidence baseline `master@0dac636b`;
- exact library waves: `K00 K01 K02 K03 K04I K04 K05 K06 K07 K08 K09 K10
  K11 K12 K13 K14 K15 K16 K17 K18 K19 K20 K21 K22 K22T K23 K24`;
- exact demo waves: `M00 M01 M02 M03 M04 M05 M06 M07 M08 M09 M10 M10T
  M11`.

### Packet

- Formation handoff P3 zone dispositions and P4 ownership/co-land laws.
- Target keyframes source/test tree.
- Ready/Refused mixed-track contract.
- Seven-route demo state and input matrix.
- Value V26–V29 grammar/property contracts.
- Glass G02–G06 contracts.

### Pull only when needed

- `adjudication-panel2-r2.md` for zone/test adjudication;
- `skeptic-K-r2.md` for pre-4.0 and facility archaeology;
- `skeptic-F-r2.md` for gates/tests;
- the exact owner prompt/addendum relevant to a disputed terminal facility;
- no unrelated skeptic corpus.

### Exclusions

- no grammar production or local property registry;
- no CSS `spring()`;
- no silent native/rAF fallback;
- no keyframes server API;
- no separate authoring route;
- no owner-checkout reset;
- no report-only facility disposition.

### Return

K24 and M11 are audit-only and return terminal zone, packed-surface, every-scene and every-viewport evidence.

## FA — Fourier API and frontend

### Authority

- `/Users/mkbabb/Programming/fourier-analysis`
- `m/w1-bump-migration@cd26c6533adc32dfe1453d74117d3cb73b89ea16`;
- preserve the exact 27-entry dirty fingerprint covering frontend/run-board,
  manifest and lock work;
- exact API/library waves in this checkout: `A21R A21 A22 A22S A21A A21J
  A23 A23S A23H A24 A25 A23C A26`;
- exact consumer/demo waves: `C03P C03 C04`.

### Packet

- Neutral protocol vectors from A01.
- Value API identity/history model for conceptual comparison only.
- Fourier target Python/test tree.
- Immutable palette-revision binding contract.
- Frontend route/state/visual audit matrix.

### Pull only when needed

- current `api/routers/visualizations.py`;
- current session/auth dependencies and models for A22S;
- CRUD library tests for cursor, ETag, idempotency and soft delete;
- visualization migration scripts;
- current frontend API client and every real visualization consumer.

### Exclusions

- no TypeScript server-runtime import;
- no copied palette mixing/taxonomy/account API;
- no content-hash revision identity;
- no GET side-effect view count;
- no insecure slug-session or owner façade;
- no handwritten client beside A23C generation.

### Return

A26 returns cross-language conformance evidence. C03P returns the independent
packed package/runtime migration without API mutation; C03 returns complete
generated-client consumption; C04 returns every-route visual evidence.

## GL — glass-ui

### Authority

- `/Users/mkbabb/Programming/glass-ui`
- observed dirty at
  `master@662948389ffd0b0e7d6d78a53388bf89301dca50`; preserve the exact
  26-entry BJ/IOS27 fingerprint and re-pin before G00;
- exact Glass waves: `G00 G01 G02 G03 G04 G05 G06 G07 G08 G09`;
- related consumer/closure waves: `C00 C01 C02 C02B C02D C02L C02M C02S C02W
  C03P C04 C05 C07 C08 C09 C10`.

### Packet

- Direct shadcn-abrogation inventory.
- Narrow selection, bounded drawer, toolbar and directional-transition requirements.
- Breath motion/material laws.
- Semantic ColorSwatch contract around paint-only WatercolorDot.

### Pull only when needed

- `docs/tranches/BJ/PLAN.md`;
- `docs/tranches/BJ/waves/BAND-FEEDBACK-MOTION.md`;
- `docs/tranches/BJ/waves/ASK-REDUCTION.md`;
- current WatercolorDot implementation and public declarations;
- exact value/keyframes call sites named by G00.

### Exclusions

- WatercolorDot remains decorative and paint-only;
- no upstream generic primitive without a real cross-product job;
- no direct shadcn façade;
- no perpetual rAF Breath;
- no app redesign from glass.

### Return

C00 returns packed consumer craters for value and keyframes, not workspace-link evidence.

## AT — active Atlas

### Authority

- active checkout: `/Users/mkbabb/Programming/.p-totality/atlas`
- clean
  `p/totality@fe9abcfcdd037a8707ae8a8da5c0183261f7ab41`;
- exact wave: `C01`; closure owners are `C00U C00 C05 C07 C08 C09 C10`.
- The standalone Atlas checkout is excluded as stale.

### Packet

- Value/keyframes breaking-surface manifests.
- Retained facilities: ManualTimeline, Draggable, MorphSVG, NumericAnimation and consumed color/easing/path operations.
- Packed candidate install instructions.
- Casualty ledger template.

### Pull only when needed

- exact source imports for value/keyframes/glass;
- package manifest/lockfile;
- representative runtime/demo/report entrypoints;
- only terminal facility records affecting a live Atlas import.

### Exclusions

- no stale-checkout catch-up;
- no compatibility aliases;
- no consumer-count-only KEEP;
- no workspace-link proof.

### Return

C01 returns packed builds/tests/runtime evidence and one terminal row for every casualty.

## SR — sci-report

### Authority

- `/Users/mkbabb/Programming/sci-report`,
  `feat/tranche-k-arc@da1e3763f53d56771221297a41df6b9f3049d4bc`;
- preserve the exact 378-entry dirty fingerprint across the embedded Atlas,
  dashboards, reports, tests, data, workflows and manifests; C02 begins with
  an exact overlap audit;
- exact wave: `C02`; closure owners are `C00U C00 C05 C07 C08 C09 C10`.

### Packet

- Value/keyframes/glass packed manifests.
- Breaking export/type/behavior ledger.
- Representative report/render acceptance set.
- Universal return contract.

### Pull only when needed

- package manifest and lockfile;
- exact imports of value/keyframes/glass;
- representative report generation and rendering paths;
- terminal facility record only when a live import is affected.

### Exclusions

- no inferred clean state or invented HEAD;
- no shared Atlas compatibility layer;
- no alias/shim migration;
- no broad unrelated report redesign.

### Return

C02 returns exact pin adoption, packed install, report/render evidence and terminal casualty routing.

## BB — bbnf-buddy

### Authority

- `/Users/mkbabb/Programming/bbnf-buddy`;
- clean `master@b218922042acdc8c94c35847bd134023c6085749`;
- exact wave: `C02B`; closure owners are `C00U C00 C05 C07 C08 C09 C10`.

### Packet

- Packed value 5, keyframes 7 and Glass 8 candidate manifests.
- Retained `NumericAnimation`/Program and `TimingFunction` contracts.
- Value parse-value and `FunctionValue` behavior vectors.
- One-clock and mascot-transition acceptance traces.
- Conditional parse-that receipt from P07 for observation only.

### Pull only when needed

- exact program/runtime, value and Glass imports;
- package manifest and lockfile;
- the mascot transition and every extra rAF owner;
- terminal K/V/G facility records only for live imports.

### Exclusions

- no parser or grammar novelty;
- no BBNF uplift;
- no private source path, compatibility alias or second clock;
- no parse-that version branch or consumer workaround.

### Return

C02B returns packed build and mascot-transition evidence, typed value/
`FunctionValue`/easing proof, one-clock ownership and a terminal casualty row
for every old import.

## SL — Slides

### Authority

- `/Users/mkbabb/Programming/slides`;
- clean `main@b538506a5b5611399a968104d452dda1c6393374`;
- exact wave: `C02D`; closure owners are `C00U C00 C05 C07 C08 C09 C10`;
- Slides-K is a separate mandatory C00U root and receives its own C02D row.

### Packet

- Packed keyframes 7 and Glass 8 candidate manifests.
- Accepted keyframes spring/Program behavior and failure/refusal law.
- Deck render/curve and reduced-motion acceptance matrix.

### Pull only when needed

- package manifest/lockfile and Glass imports;
- the exact dynamic spring import and swallowed cubic fallback;
- representative decks that render JS and CSS curves;
- Slides-K origin/realpath for its mandatory included row, never implicit
  mutation.

### Exclusions

- no swallowed cubic fallback or compatibility lookup;
- no unrelated deck redesign;
- no automatic Slides-K mutation or authority inference;
- no source-link proof.

### Return

C02D returns packed deck build/render evidence, CSS/JS curve agreement,
visible dynamic-load refusal and one explicit included row for Slides-K; an
unavailable Slides-K root is a typed retriggering blocker, never an exclusion.

## BL — bbnf-lang playground

### Authority

- repository `/Users/mkbabb/Programming/bbnf-lang`, canonical playground
  `/Users/mkbabb/Programming/bbnf-lang/playground`;
- dirty `master@af15f63e0d2d3d719938c13b906a50acbb92ea3`; preserve the exact
  73-entry repository fingerprint;
- exact wave: `C02L`; closure owners are `C00U C00 C05 C07 C08 C09 C10`.

### Packet

- Packed value 5, keyframes 7 and Glass 8 candidate manifests.
- Two ScrollTimeline and three easing edge receipts.
- Playground SSR/browser and cleanup acceptance matrix.
- PT-E exclusions and the conditional P07 receipt.

### Pull only when needed

- `playground/package.json`, lock and Vite configuration;
- exact ScrollTimeline/easing/Glass import sites;
- interaction cleanup and packed SSR/browser entries;
- no generated/runtime/restart corpus outside overlap evidence.

### Exclusions

- playground migration only;
- no grammar, generated-runtime, CSS-L4 parser, Tape/SoA, combinator or major
  parse-that/BBNF change;
- no fallback, second clock, source link or old Glass substrate.

### Return

C02L returns packed SSR/browser builds and exact semantic proof for both
timelines and all three easing jobs while preserving every unrelated dirty
path.

## MU — Muster frontend

### Authority

- canonical path `/Users/mkbabb/Programming/muster/frontend`;
- `/Users/mkbabb/Programming/Muster/frontend` resolves to the same lowercase
  realpath and must not become a duplicate root;
- dirty repository
  `master@87ee926d385824708265597d6018bb87cc69f2e9`; preserve the exact
  89-entry root fingerprint;
- exact wave: `C02M`; closure owners are `C00U C00 C05 C07 C08 C09 C10`.

### Packet

- Packed value 5 and Glass 8 candidate manifests.
- Broad Glass import census and the single lazy value import edge.
- SSR/client chunk, route-slug and visual behavior matrix.
- Explicit current keyframes non-edge verdict.

### Pull only when needed

- frontend manifest/lock and build configuration;
- all Glass imports and the lazy value import;
- SSR/client chunk graph and slug-producing routes;
- repository status only for overlap preservation.

### Exclusions

- frontend realpath only;
- no invented keyframes dependency;
- no duplicate-case package root, source link or unrelated tranche-image
  mutation;
- no eager loading of the value chunk.

### Return

C02M returns the canonical-realpath receipt, packed SSR/client builds, lazy
chunk and slug proof, Glass behavior evidence and an explicit keyframes
non-edge result.

## ST — Speedtest

### Authority

- `/Users/mkbabb/Programming/speedtest`;
- clean `master@7212e7332b5dcbcf5d608622d6418da2db845ea8`;
- exact wave: `C02S`; K13 is the replacement owner and K21 is the terminal
  deletion owner; exact implementation dependencies are `G07 V29T K10 K13`;
  closure owners are `C00U C00 C05 C07 C08 C09 C10`.

### Packet

- Complete live `ElementMorph` import/construction/promise census.
- K13 retained morph facility plus ordinary numeric Program contract.
- Settle, interruption, exact inline-style restoration, meter and PRM traces.
- Hashed provisional no-link V29T/K10/K13/Glass candidate manifests for the
  pre-prune migration; C00/C05 own the separate final-package repetition.

### Pull only when needed

- every `ElementMorph` source and type edge;
- play-promise consumers and settle/style/meter choreography;
- package manifest/lock and representative runtime entrypoints;
- K13/K21 terminal records relevant to those edges.

### Exclusions

- no partial choreography replacement;
- no physics alias, masking fallback, source link or casualty-free assertion;
- K21 cannot delete the old facility before C02S turns every live edge green.

### Return

C02S returns provisional no-link value/keyframes/Glass installs and exact
behavioral parity for settle, interruption, style restoration, meters and
reduced motion before K21 runs, with zero `ElementMorph`, alias or source-link
residue. It cannot claim final-pack proof; C00/C05 repeat after K22T/K23.

## WO — Words frontend

### Authority

- frontend `/Users/mkbabb/Programming/words/frontend`, with Git root
  `/Users/mkbabb/Programming/words`;
- dirty `master@26b16ffd36c90ed23405479c9ac5527829dc7649`; preserve the exact
  23-entry root fingerprint, including unrelated backend/docs work;
- exact wave: `C02W`; closure owners are `C00U C00 C05 C07 C08 C09 C10`.

### Packet

- Packed keyframes 7 and Glass 8 candidate manifests.
- Explicit `./engine`/Program/native migration law.
- Loader/CSS animation edge census and lifecycle/PRM matrix.

### Pull only when needed

- frontend manifest/lock and build configuration;
- `loadAnimationEngine`, `CSSKeyframesAnimation`, breathing and Glass call
  sites;
- mount/unmount/route-exit cleanup paths;
- unrelated backend paths only for overlap preservation.

### Exclusions

- no compatibility loader or dual engine;
- no infinite work after any lifecycle exit;
- no unrelated content/backend redesign or source-link proof.

### Return

C02W returns packed visual/accessibility evidence, explicit engine/Program/
native ownership and cancellation proof with no loader, old class or dual
engine residue.

## LP — latex-paper direct parse-that consumer

### Authority

- `/Users/mkbabb/Programming/latex-paper`;
- dirty `master@c14047399813a0e72f033fe9a0a5f749aa266768`; preserve the exact
  18-entry VIRT-specification/traces fingerprint;
- direct-consumer proof owner: `P06`; universe/packed-closure owners are
  `C00U C00 C05 C08 C09 C10`.

### Packet

- Exact published-1.0.0 control and P04 no-RC digest.
- P00 direct-consumer edge and P06 invocation/casualty requirements.
- Published-1.0.0/no-republish law; every blocker routes externally.

### Pull only when needed

- package manifest/lock and exact parse-that import/invocation sites;
- the minimum representative behavior corpus proving real parser execution;
- no unrelated VIRT corpus beyond overlap evidence.

### Exclusions

- no consumer adapter, private import, version branch, alias, shim or parser
  workaround;
- no mutation of existing VIRT work;
- no forced parse-that publication when the accepted branch is unchanged
  `1.0.0`.

### Return

P06 returns a no-link packed invocation proof and casualty row. C05 retains
the root in the frozen universe. C10 does not republish or otherwise change
parse-that.

## CL — constellation closure

### Authority

- exact closure waves: `C00U C00 C05 C06 C07 C08 C09 C10`;
- `C09` has the sole static dependency `C08`; before scheduling,
  `tools/resolve-reopenings.mjs` verifies the sealed C08 attempt ledger, exact
  terminal current-owner returns and persisted corpus epoch, then emits the
  sorted self-hashed resolved dependency receipt without rewriting the wave
  DAG;
- C10 is the sole release and cutover authority.

### Packet

- Every repository packet above plus the signed included/excluded/unavailable
  universe.
- Exact candidate tarball digests, OpenAPI/client digests and deployment
  schemas.
- Two consecutive fresh hostile Sol triads and all dynamically reopened owners.
- Named physical-device, security, concurrency, restart, storage, job, load
  and visual evidence.

### Exclusions

- no prose dependency, waived reopening or hand-picked root list;
- no RC, second release, early `latest`, compatibility tag kept after cutover,
  alias, shim, dual API/schema path or hidden fallback;
- no parse-that novelty or unconditional version bump.

### Return

C10 executes one temporary non-`latest` maintenance-window cut in this order:
verify unchanged parse-that 1.0.0, value `5.0.0`, keyframes `7.0.0`, Glass `8.0.0`, API
schemas/deploy, every included consumer, smoke, `latest`/traffic promotion,
then temporary-tag removal. Any parse-that blocker prevents C10 and remains
with the active campaign.
