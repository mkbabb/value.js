# THE PARSER PROOF GATE — COMPOSITE VERDICT (2026-07-20)

Root-session Fable adjudication over the three proof seats (`equivalence.md` ·
`coverage.md` · `bench.md` + the two results JSONs), executed under the owner's
D-23 condition (`../OWNER-RULING-D23-2026-07-20.md`): *the parser must be PROVEN
before tranche production execution begins.* Seats: 3× Opus (mechanical,
model_served verified); this adjudication: Fable main loop (the one
design-forward pass, per the C17 sparingly-law).

## COMPOSITE: 🔴 RED — but a RED of DISTANCE, not of DOUBT

| arm | verdict | one line |
|---|:--:|---|
| P-1 semantic equivalence | 🟢 GREEN | 0 mirror-defects across the 403-string corpus; every cross-engine difference is declared narrowing or a **live-side** defect |
| P-2 coverage census | 🔴 RED | assay = W0 pilot: frozen 52-export surface 0 TOTAL / 3 PARTIAL / 16 ABSENT runtime + 33 types ABSENT; kf seams 3/37 |
| P-3 comparative bench | 🔴 RED | C14 sheet ratio 0.0537 < 0.1000 floor (value 0.0640 ≥ 0.0500 passes); deterministic across 5 invocations |

**What the RED means precisely:** the mirror is *sound where it speaks and
nowhere near total.* P-1 proves the METHOD — on every input inside its declared
W0 shape, C14 is exactly right, and where it differs from the live engine the
spec sides with C14 every single time. P-2 measures the DISTANCE — 49 of 52
frozen exports have no assay peer; the completion path is enumerated (below).
P-3 adds one genuine engineering obligation (sheet throughput) and one broken
ruler (below).

## The four load-bearing findings

**F-1 — The mirror method is PROVEN.** Zero DIVERGENT_VALUE, zero MIS_ACCEPT,
zero FALSE_REJECT_IN_SHAPE. The mis-accept guard held over 142 out-of-scope
strings. D-23's mirror-primary bet is *behaviorally vindicated at pilot scale*.

**F-2 — The LIVE v4 parser has 5 recorded defects; R1 is a shipping crash.**
`parseCssColor("oklch()")` (and `rgb()`, `hsl()`, `lab()`, `color()`, `rgba()`)
throws `TypeError` from `parseFunctionalColor` (`src/css/grammar.ts` ~L181) —
the frozen public contract says clean `ok:false`. R2: 10 valid qualified rules
over-rejected (`animation_option_invalid` on `var()`/`calc()` compositions,
relative-color inside `color-mix`). R3 dangling-alpha leniency, R4 selector
range check off-api, R5 comment-trivia folded into declaration names. **In all
five, C14 is the spec-correct engine.** These become regression fixtures the
mirror must PRESERVE (spec-correctness, NOT bug-compatibility with live) — and
R1/R2 are defect rows for the fleet's V-band. 4.0.0 is immutable; the mirror
swap is the cure; no known consumer feeds the crash shape (kf's 37 seams
verified — none constructs empty functional colors).

**F-3 — The frozen surface is a TOTAL SUPERSET of kf's needs.** All 37
kf-consumed symbols are exported by `src/css/index.ts`; zero orphan imports.
The *contract* is intact — only the *mirror* of it is young. The 14 named
Surface-3 gaps each route to an already-born-RED fleet wave (V03–V28 per
`waves/P-V.md`); the gate manufactures NO new work, it confirms the registry's.

**F-4 — The bench inverted the folklore, and the ruler is broken.** The
once-for-the-record measurement of the retiring LIVE regex engine (the fleet's
P05 debt — data now on disk in `bench-results.json`): it PASSES every historical
floor and is the FASTEST engine on every shared scenario — ≈1.8× both
combinator engines (peak 14.06 MB/s vs C14 6.09 vs deposed 7.63 on the common
corpus). "The regex parser was slow" is dead as a retirement justification; the
retirement now rests where it always should have — on correctness (F-2's crash
and over-rejection are *its* defects) and architecture. Meanwhile the DEPOSED
tree — the gate's own calibration subject — FAILS its own floors on 3 of 5
scenarios on this rig (M5 Max, esbuild-bundled, node): the absolute bar does
not transfer across machine/build. A bar the calibration subject cannot meet is
a broken ruler, not a standard.

## The deadlock, named — and the staged reading (RECOMMENDATION)

Read flatly, the gate deadlocks: the totality RED can only be cured by
*building the mirror out* (the V03 transpose + the V-band), but the gate
precedes "production execution across ALL bands." More proving cannot cure it;
only building can. The D-23 ruling itself contains the release: *"The fleet's
P00/P01 born-REDs are exactly this gate — shared, not duplicated."*

**Recommended reading (owner ratifies at kickoff): the gate bifurcates.**

- **Method-proof: DELIVERED GREEN.** Equivalence sound where mirrored (P-1);
  contract superset intact (P-2 Finding A); bench resurrected + regex measured
  once-for-record (P-3, P05 dischargeable). The baseline CHOICE is proven.
- **Totality-proof: becomes the parser band's CLOSE GATE.** Execution may begin
  (owner's word, post-compaction, first commit = AM-13 deps-strip), with the
  parser band ordered FIRST among source bands, and NO consumer band (kf seams,
  demo, /css swap-in) rides the mirror until: **(G-1)** coverage 52/52 exports +
  37/37 kf symbols TOTAL (V03 transpose + owning V-waves); **(G-2)** the P-1
  differential harness graduated to the full surface, zero mirror-defects,
  R1–R5 held as spec-correct regression fixtures; **(G-3)** bench floors met
  under a recalibrated bar.

## OWNER-CONFIRM (two items, rule at kickoff)

1. **OC-1 — bench-bar recalibration.** The historical absolute floors
   (VALUE ≥ 0.0500 / SHEET ≥ 0.1000 vs jsonParser peak) don't transfer to this
   rig — the calibration subject itself fails them. Proposal: re-anchor
   RELATIVE — *the mirror ≥ the deposed baseline per scenario on the same
   rig+build* (today that means C14 must close a −21% sheet deficit), with the
   LIVE regex numbers held as the recorded ceiling, not a floor. Alternative:
   owner re-ratifies absolute floors measured on a named reference rig.
2. **OC-2 — the staged gate reading above.** Flat reading = deadlock; staged
   reading = the totality gate becomes the parser band's close condition and
   sequencing law. Silence holds execution fully blocked.

## Standing consequence

The owner's execution condition STANDS: no production execution from this
verdict alone. Everything is now on disk for the begin-word: the three reports,
the two results JSONs, the recovered harnesses (differential + bench, both
reusable at G-1/G-2/G-3 time), this verdict, and the evidence letter to the
fleet (`../../coordination/value-inbox-2026-07-20-parser-proof-evidence.md`).

— the union-apotheosis program (root session), 2026-07-20.
