# P-5 — UPSTREAM parse-that/bbnf UPLIFT SURVEY (read-only, 2026-07-20)

One Opus survey seat, read-only over `/Users/mkbabb/Programming/parse-that` +
`/Users/mkbabb/Programming/bbnf-lang` (+ 12 sibling worktrees) + the value-side
coordination ledger; zero writes anywhere. Companion to `PROFILE-ANALYSIS.md`
(its O-6 rung consumes §4 here). Owner question: *"What of our parse-that
uplifts, those documents, locks, and proto-tranche plants?"*

## 1. The committed truth — published 1.0.0 IS the tree

npm `@mkbabb/parse-that@1.0.0` = git tag `v1.0.0` (`7eab78c`). Committed delta
`v1.0.0..HEAD` = exactly ONE commit (`ef10d5b`), a 48-line docs-only
coordination letter. **Zero code change between what value consumes and
committed upstream HEAD.** The ENTIRE uplift — Tranche T/U trees, kernel
prototypes v1–v4, byte-class tests, Rust substrate edits — is uncommitted
working-tree/untracked preseal research.

## 2. The locks (I-15/I-16's substance)

- **Live coordination stream** `parse-that/docs/tranches/B/pt-e-bbnf-live-coordination-v2.json`
  (stream `value-vnext-2026-07-18`): every event asserts coordination-only, no
  novelty import, no production authority; active locks PT-C/E/J/K/R/V; pins
  all three repos' HEAD+dirty digests; publication branches limited to
  `unchanged-1.0.0-no-republish` | `compatible-1.0.1`.
- **Binding ruling** `parse-that/docs/tranches/T/VALUE-VNEXT-COORDINATION.md`:
  observation grants no authority; two deliberately separate tracks (value
  V-next on published 1.0.0 · upstream major research); "V4" demoted to
  measured control evidence only.
- **The no-write windows** (02:49:22–03:34:22 EDT + →05:26:09 EDT): elapsed
  CLEAN at their declared endpoints; zero parse-that production writes — the
  ledger/porcelain hashes in I-15/I-16 were message content, their durable
  counterparts are the artifacts above.
- **Proof handoff** `parse-that/docs/tranches/U/CLAUDE-PROOF-HANDOFF.md`
  (2026-07-20T15:46Z): parse-that proof ownership handed to Claude Code; Codex
  frozen from parse-that source until handback.
- **SK-V25 no-contact boundary** (`bbnf-lang/…/sk-v25/CAMPAIGN-STATE.md`,
  ratified 2026-07-20): upstream sends value NO further messages, freezes,
  snapshots, or authority requests. The channel is formally closed from their
  side; the reserved future conduit (`docs/tranches/T/coordination/VALUE-MAJOR-*.json`)
  does not exist yet.

## 3. Proto-tranche plants — Tranche U, born-RED, preseal

- **U** ("checked kernel, canonical regex, real consumers"): 10 waves, FORMED,
  born-RED; cannot seal before upstream T.W0–W7 + a sealed GESTALT-T. Wave
  spine: PT-K Rust memory/span/scanner safety (W0–W2, Apple/aarch64-only law),
  PT-R regex HIR/VM/bootstrap (W3–W5), PT-C Rust/TS/WASM isomorphism (W6),
  **PT-V packed value.js adoption + ask-letter closure (W7)**, GESTALT (W8–W9).
- **U.W0** = "make memory and regex lies executable": 55-test witness bank
  (33 satisfied controls + 22 intended-RED) converting adjudicated PT-K/PT-R
  defects into deterministic failing witnesses BEFORE any production edit.
  Hostile ruling: preserved as `U.W0-PRESEAL-RESEARCH`; acceptance grants
  witness authority only; PT-K/PT-R stay RED; **no value/production movement
  follows from it.**
- Evidence spine `docs/tranches/U/PT-K-MEASURED-MAP.md` @ `ef10d5b`: 136
  `unsafe` across 20 files; 24 x86/SSE/AVX matches RED vs the Apple-only law;
  `Span::new` 199 debug-only-checked sites; BumpSlab/PaddedView/lazy hazards.

## 4. Uplift documents + perf relevance to the MIRROR lane

All non-1.0.0 artifacts are **private/non-consumable** (`0.0.0-private`,
absent from npm). Enumeration:

| artifact | status | mirror-lane relevance |
|---|---|---|
| Five engine assays P/A/G/I/S (persistent/staged/GLL/incremental/streaming) — `T/MAJOR-PORTFOLIO.md` | 5 specified, **0/5 implemented** | none yet |
| **Byte-class/bitmap StructuralIndex** — `bbnf-lang/crates/simd-scan/` (alphabet/neon/wasm/scalar/compaction) | **IMPLEMENTED** (Apple-NEON + wasm32 only; `compile_error!` elsewhere) | **the ONE transferable idea** — 64-lane stripe → u64 membership bitmap, StructuralIndex once/parse, O(1) dispatch jumps; `scalar.rs` is the parity reference a TS mirror lexer would emulate. REIMPLEMENT, never import. |
| Threaded/fork JSON engine — worktree `bbnf-lang-json-threaded-fork` | **REJECTED dead end** (`b102aa92e` records the rejection) | do not pursue |
| Apple-ARM/browser-Wasm cut (U.W2/W6) | design only | none (Rust/Wasm world) |
| CSS-L4/JSON verticals (SK-V25, 4 candidates) | 0/4 complete | none yet |
| Hostile padding/number witnesses | preseal RED | none |
| PT-E ask letter `docs/tranches/A/VALUEJS-PT-E-2026-07-05.md` | historical, open | PT-E1 scoped diagnostics HIGH · PT-E2 inference MED · PT-E3 Pratt-dormant — the standing 1.1.x ask lane |

Upstream product perf bars (≥5× Lightning CSS, ≥1.1× sonic-rs,
bootstrap-95%-LB) are Rust-substrate bars — they do NOT transfer to the TS
mirror lane and must never be cited as mirror gates.

## 5. Consequences for value.js (synthesis)

1. **parse-that-as-published is the whole universe** for the mirror lane —
   confirmed from the upstream side with locks. Grammar-side optimization
   (PROFILE-ANALYSIS O-1..O-4) is the only lane that needs no one's
   permission.
2. **Engine-side asks (O-6) have a narrow legal shape**: TS-package changes on
   published 1.0.x→1.1 (mapState scalar variant, benign-failure inlining) via
   a PT-E letter — but the upstream is under a self-imposed no-contact
   boundary and mid-major-campaign; expect silence until their U.W7 (PT-V,
   "packed value.js adoption + ask-letter closure") turns to face us. Do not
   block any mirror gate on upstream response.
3. **The byte-class/bitmap idea is free to reimplement** in the mirror's
   lexeme layer (it is an idea + a scalar parity reference, not a consumable
   artifact); it is the post-floor rung, not the gate rung.
4. The no-contact boundary + proof handoff mean: **value initiates nothing
   upstream right now**; the reserved VALUE-MAJOR-* conduit signals when the
   upstream is ready. I-15/I-16 need no reply.

— the union-apotheosis program (root session), 2026-07-20. Survey: one Opus
seat, read-only; synthesis: Fable main loop.
