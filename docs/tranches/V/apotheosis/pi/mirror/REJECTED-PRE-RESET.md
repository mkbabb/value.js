# Pre-reset mirror disposition

The TypeScript runtime outside `mirror/apotheosis/` is retained solely as
rejected prototype evidence. Its exact nineteen-file grammar ledger is
`cells/syntax-number-start/g2/integration-base.ledger` at SHA-256
`115b93f3b44fd0df7fc941e499e9392405a39267b87c2867617521f93e273b4c`.

That tree contains the owner-rejected lexical/atom/component-value/CST route,
manual source-walking utilities, and unaudited direct sketches. Green historical
tests show only what those bytes once did; they convey no architecture,
conformance, integration, or performance credit. They are not part of the
active source census, cannot satisfy a feature dependency, and cannot be
imported by candidates.

The clean active root is `mirror/apotheosis/`. Preserving the old bytes in
place avoids a destructive move and keeps prior manifests reproducible while
operationally quarantining them from every new promotion and check.

The package defaults now check/test only the clean root and governed feature
cells. Historical commands are explicitly suffixed `:rejected-g0`; their
success remains evidence-only.
