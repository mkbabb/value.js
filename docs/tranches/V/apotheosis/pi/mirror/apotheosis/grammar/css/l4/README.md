# CSS grammar root

This active CSS grammar base follows the acknowledged BBNF production-family
DAG. Runtime files appear only through accepted feature promotion. The first
owner is `value-unit/numeric.ts`, implementing CSS Syntax consume-a-number as a
direct parse-that leaf. This Markdown marker is not a runtime module.
