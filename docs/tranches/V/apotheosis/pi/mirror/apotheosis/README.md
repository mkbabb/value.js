# Clean V·π direct-combinator prototype root

**Status: active greenfield prototype; first direct parser feature accepted.**

This directory is the integration root for the re-grounded prototype. Runtime
TypeScript beneath `grammar/` must be ordinary idiomatic
`@mkbabb/parse-that/core` production modules following the acknowledged
`MODULE-DAG.md` ownership direction. There is no lexer runtime, token-object
layer, atom algebra, generic component-value/CST substrate, imperative balanced
scanner, or broad remainder parser here.

The pre-reset prototype remains elsewhere under `mirror/` only as rejected,
content-addressed evidence. It is excluded from this root's source census and
may never be imported by a candidate or integrated production. A feature enters
`grammar/` only after its own sealed boundary, independent candidate authors,
quintet, triumvirate, exact promotion identity, and aggregate replay.

The first accepted source is the exact `SYNTAX-CONSUME-NUMBER` winner at
`grammar/css/l4/value-unit/numeric.ts`. It is one direct parse-that terminal
plus one semantic projection, with no scanner/token/CST substrate. Later
features import accepted owners; speculative shared helpers remain forbidden.
