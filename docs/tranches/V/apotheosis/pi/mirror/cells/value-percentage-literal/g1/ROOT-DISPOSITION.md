# VALUE-PERCENTAGE-LITERAL — G0 adjudication and G1 correction

**Disposition:** G0 is REJECTED for selection; its candidate and timing bytes
remain evidence. G1 H3 is the sole corrected subject for fresh 5×3 review. No
feature or production credit exists before that review closes.

## G0 findings adopted

- The semantic boundary is correct: an unbounded, prefix-consuming literal
  percentage belongs to `value-unit`, immediately above the numeric owner.
- `skip` is the best ordinary composition; `all` adds a tuple and the guarded
  construction parses `%` twice. H/S also used source-only `.ts` specifiers.
- The original benchmark was invalid for qualification: broad scalar doors
  were compared to an internal production, executable closure was post-bound,
  and the first/second harness lineage was false.
- Corrected v2 retained the raw regex-kernel diagnostic and honestly failed.
  Emit-shaped B was slower than H, the exact live regex kernel, and not a
  strict winner over the prior direct parser.
- Emit-correct H2 then won the comparable public percentage subset but failed
  the retained prior internal parser. That v3 result is terminal and retained.

## Gestalt ruling

The G0 gestalt's emit/tuple findings are accepted. Its raw-spelling blocker is
rejected by the higher semantic authority. CSS exponent percentages are valid;
the old `mirror/timeline.ts` exponent rejection is the already-recorded R14
defect, not a compatibility invariant. `consumeNumber` deliberately returns
only `{sign,type,value}`; spelling/extent remains a same-input parser-boundary
observation and may be captured by an owning parent without creating a token
object or bloating every numeric semantic leaf.

## G1 correction

H3 follows CSS Syntax's terminal identity directly. One `CSS_NUMBER_SOURCE`
and one `projectNumber` own numeric recognition and projection. `consumeNumber`
uses that owner as a number terminal; `percentageLiteral` uses the same owner
as a percentage terminal. There is no duplicated numeric regex, scanner,
cursor, token object, CST, raw remainder, or per-call parser construction.

This necessarily revises the exact numeric module bytes, so G1 is a combined
numeric-owner/percentage candidate. It must preserve the accepted numeric
semantics and performance while earning percentage credit. The frozen H3
candidate is SHA-256
`1d1c37d20a9ef97f13466e96fbd65175d7c387aebd877ef400c81951fda23dc3`.

H3 passes 2,936 direct differential numeric transactions against the accepted
numeric parser; the inherited G14 rail (77 successes, 336 failure runs, parent
and hostile cases); and all public plus sealed percentage observations. Its
first and only presealed v4 benchmark is PASS: public ratio 0.339 versus LIVE
(upper 0.341), 0.0682 versus the rejected public parser (upper 0.0686), and
internal ratio 0.931 versus the retained prior parser (upper 0.939).

Five fresh skeptics must now challenge the exact G1 bytes. If all blocking
defects close, three fresh adjudicators independently nominate an exact hash.
