# VALUE-PERCENTAGE-LITERAL G2 correction

G1 H3 is rejected. Its fused sibling regex bypassed the accepted
`consumeNumber` parser and lost the numeric child’s public `furthest` progress.

G2 returns to the already-authored H topology, corrects only its emitted-ESM
specifier, and binds an exact promotable integration tree: `percentage.ts` is
a sibling of the byte-identical accepted `numeric.ts` in `value-unit`, importing
the sole numeric parser owner. The
production is the direct grammar `consumeNumber.skip(string("%"))` followed by
the required semantic projection. It introduces no numeric regex, scanner,
cursor, token object, CST, tuple, or second `%` parse.

The earlier H/B/S candidates remain the three independent constructions for
this feature. This integration tree is the narrow H correction explicitly
requested by the G0 gestalt and G1 architecture rejection; it is not a fourth
independent design.
Acceptance remains withheld pending fresh five-skeptic and three-adjudicator
review of these exact correction bytes and their correctness/performance
closure.
