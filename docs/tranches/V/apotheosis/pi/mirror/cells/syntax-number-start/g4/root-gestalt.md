# SYNTAX-NUMBER-START generation 4 — root gestalt

**Verdict: REJECT before candidate code.**

Subject manifest SHA-256:
`8d5a75cf11d63fc250e9968641aae377ac8a97ed4a160470468d5c508064a342`.

The two independent challenges both reproduced the exact seal and rejected it
on complementary, load-bearing grounds. Challenge A is
`37ac9c3150622c2c3f9ea758af904ab657f0a503ea717487dcb5a610b3b5bbf5`;
challenge B is
`ee73c763133904585b4c1ac20844e090d43815fde75ff3c0d3350f2f8e9a1ba1`.
No candidate exists and the hidden corpus was never revealed.

## Adjudication

The semantic operation is now correctly isolated: CSS Syntax §4.3.10, direct
non-consuming parse-that composition, no lexer/token/atom/CST substrate. The
public truth matrix, explicit EOF endpoints, raw returned-state identity,
`ParserState.src`, diagnostic references/values, two-stage holdout seal,
ordered pairwise statistics, and BBNF ownership boundary are substantive
repairs. They carry forward only as rejected-generation evidence.

Generation 5 must union five corrections:

1. add parse-that's ordinary default `undefined` predecessor in raw,
   composition, holdout, and benchmark lanes; “any predecessor” may not omit
   the package default;
2. reject obvious implicit state coercion (`TemplateExpression`, binary string
   coercion, inherited string helpers) mechanically and narrow the AST claim
   truthfully: the quintet remains the binding control for all indirect source
   derivation that static syntax cannot prove absent;
3. spell the timed-mode sequence exactly as disable → restore/time disabled →
   enable → restore/time enabled → disable, with any exception invalidating the
   complete run and all toggles/restoration outside timing;
4. remove caller-substitutable live roots by deriving canonical paths from the
   verifier location, rejecting symlink/realpath drift, and recording resolved
   roots plus reproduced aggregate identities;
5. do not project candidates onto the rejected nineteen-file atom/CST grammar
   base. `ADDENDA-07 §8` orders quarantine before the pilot. Preserve that tree
   as rejected evidence outside the active grammar, establish the minimal
   direct-combinator base, and bind generation 5 promotion to that clean base.

The fifth item is the gestalt correction: even a perfectly verified overlay is
mis-sequenced if its projected aggregate still canonizes the architecture this
reset exists to remove. Isolated candidate ideas may be reusable, but no
source seat opens until the active base and its promotion identity are clean.

## Three altitudes

- **Total tranche:** the direct-combinator/BBNF direction remains optimal; the
  existing active base does not. Quarantine precedes candidate authoring.
- **Wave:** G4 closes most of G3's docket but not default-state, source-policy,
  benchmark-mode, or actual-live-root evidence. Its manifest is rejected.
- **Feature:** CSS truth and ownership are sound; the executable observation
  and promotion boundary is not yet sufficient to admit candidates.

No voting or paperwork can override these counterexamples. Start a new exact
generation after the clean-base move and rerun two independent challenges plus
gestalt.

MODEL: `gpt-5.6-sol`  
REASONING: `ultra`  
WORKFLOW: root adjudication, v2
