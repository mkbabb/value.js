# SYNTAX-NUMBER-START G6 — root gestalt

**Verdict: REJECT before candidate code.** The sealed subject was
`manifest.json` SHA-256
`fc3c059accc160841c391bb20bee0364e2defb3b69a8661ae00cfc9fa0e1f064`
(7,663 bytes). No H/B/S/D/R candidate exists; the unrevealed G6 holdout must be
destroyed and never reused.

## Independent challenges

- Specification challenge A REJECTED at
  `89b112ab2621b4a21c281af706c826ff218875833c61430b432fa4312dbcb29c`
  (12,889 bytes).
- Architecture challenge B REJECTED at
  `0809fb83866d14fddf22eb795a1ec136d11485d42e8a3f48116292428eb5ff2c`
  (8,468 bytes).

Both verified all 41 manifest-bound inputs. Both also accepted the exact CSS
Syntax semantics, state/diagnostic harness, 224-case holdout receipt, clean
base, G5 destruction chain, five lineage design, benchmark schedule, complete
130-file TypeScript package ledger, and child/parent configuration closure.

## Confirmed defects

1. The emitted `tsc -p tsconfig.apotheosis.json` is not a candidate-specific
   type gate. Its program resolved 277 inputs, only 81 of which were
   authenticated by G6/tool ledgers. The remaining live cell sources,
   third-party declarations, root-level resolution, and future glob matches can
   change type meaning while every G6 identity remains green.
2. More directly, the verifier accepts any canonical overlay but does not put
   that overlay into the emitted TypeScript project or mechanically require the
   exported `numberStart` to have type `Parser<boolean>`. A disposable external
   assay containing `export const numberStart = 1` passed the verifier and the
   emitted typecheck.
3. The verifier checks its canonical location but neither verifies nor emits
   its exact manifest-bound bytes. The protocol's drift law therefore lacks an
   executable link to the sealed manifest trust anchor.

## Required G7 disposition

- Build an isolated compiler program rooted only at the exact overlay source.
  Resolve imports and TypeScript libraries, then reject unless every resolved
  source is the candidate itself or lies in the already byte-bound parse-that
  or TypeScript package trees. No workspace glob, ambient root package, or
  unbound declaration may enter.
- Require zero syntactic/semantic diagnostics and inspect the exact exported
  symbol as `Parser<boolean>` (reject `any`, `unknown`, `Parser<any>`, and
  non-parser values). Emit the complete resolved-input ledger/identities.
- Make the sealed manifest SHA-256 the external trust anchor: the promotion
  invocation supplies the expected digest; the verifier hashes the manifest,
  obtains its own expected row from that exact manifest, hashes its running
  file, and emits both identities. A caller-chosen different digest is
  nonconformant and must not receive a gate receipt.
- Preserve G6 semantic/harness/benchmark inputs unchanged; form a fresh
  precursor, wholly fresh holdout, two challenges, and gestalt.

This is an exact promotion/type-proof failure, not a reason to add a lexical
runtime or scanner. The active direct-combinator base remains empty and GREEN.
G6 earns no implementation, integration, conformance, performance, or
production credit.
