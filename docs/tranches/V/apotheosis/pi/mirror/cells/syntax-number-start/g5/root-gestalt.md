# SYNTAX-NUMBER-START G5 — root gestalt

**Verdict: REJECT before candidate code.** The exact sealed subject was
`manifest.json` SHA-256
`41c482b059ddab64c51226a05022016c17adb55cb73292d665d29298aa1a0109`
(5,839 bytes). No H/B/S/D/R candidate source exists, and the hidden G5 corpus
must never be revealed or reused.

## Independent challenges

- Specification challenge A ACCEPTED the semantic, composition, holdout, and
  benchmark boundary at
  `4f9df0cd2a0c012a2b167cb4ec64972ba29ad490f296f383dac8610d95835307`
  (10,099 bytes).
- Architecture challenge B REJECTED at
  `a8c1669c95f37ec4a838b352cbaefbe11d4715d58193d61caa6078f30b67542f`
  (11,325 bytes).

One independent REJECT is sufficient to withhold authoring. The root confirms
both B findings rather than averaging the verdicts.

## Confirmed defects

1. The promotion closure hashes `tsconfig.apotheosis.json`, but that file
   inherits `tsconfig.json`, whose exact bytes and resolved effect are absent
   from the G5 closure and verifier. The verifier also executes the installed
   TypeScript AST implementation without binding that implementation's bytes.
   A compiler/configuration drift can therefore change the type and AST gates
   while every emitted G5 identity remains green.
2. `promotion-protocol.json` says its mechanical checks reject token/CST names
   and broad remainder regex patterns. `promotion-verifier.ts` does not inspect
   those names or regex-literal bodies. The later human quintet is binding, but
   it cannot retroactively make a false mechanical claim true.

These are exact-proof defects, not semantic failures in CSS Syntax §4.3.10.
The 406 G3 cases, 23 G4 endpoints, G5 `undefined` predecessor repair, full
state/diagnostic identity contract, clean zero-feature base, five intended
lineages, and paired diagnostics-mode benchmark design remain useful inputs to
a fresh formation generation.

## Required G6 disposition

- Bind the complete TypeScript configuration closure, including the inherited
  config, and bind the exact installed TypeScript implementation used by both
  AST inspection and the declared typecheck.
- Keep the mechanical verifier deliberately narrow and make every claim match
  an implemented check. Leave indirect source derivation, architectural names,
  regex breadth, scanner behavior, and idiomaticity as explicit all-candidate
  quintet obligations unless a sound narrow mechanical check is added.
- Form a new immutable precursor, obtain a wholly fresh hidden corpus, and run
  two new independent challenges plus a new gestalt. The G5 hidden corpus is
  discarded unrevealed and is ineligible for any future generation.

The total tranche remains on the correct reset architecture: no lexer, atom,
token-object, component-value, CST, or imperative scanner substrate; direct
parse-that combinator productions on the acknowledged module DAG; exact
denominator closure still RED. G5 receives no implementation, integration,
conformance, performance, or production credit.
