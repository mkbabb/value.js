# SYNTAX-NUMBER-START G6 boundary challenge A — specification/composition

**Verdict: REJECT before candidate code.** The semantic, occurrence,
composition, fresh-holdout, and benchmark boundaries are fit, and G6 repairs
G5's two literal findings as far as the compiler executable/configuration and
truthful AST claims go. The promoted typecheck is nevertheless not a closed
computation: its exact sealed config glob-loads 196 TypeScript program inputs
whose paths and bytes are outside the G6 public closure and the two tool
ledgers. This is a load-bearing continuation of the transitive-closure defect,
so H/B/S/D/R authoring cannot begin.

This receipt is bound to `g6/manifest.json` SHA-256
`fc3c059accc160841c391bb20bee0364e2defb3b69a8661ae00cfc9fa0e1f064`
(7,663 bytes).

## Seat receipt

- model served: `gpt-5.6-sol`
- reasoning effort: `ultra`
- workflow: independent exact-byte adversarial challenge, v2
- posture: assume the boundary is semantically wrong, compositionally leaky,
  scanner-prone, promotion-inexact, and benchmark-biased until each claim is
  reproduced
- hidden material: no hidden path or hidden corpus byte was sought, located,
  reconstructed, or inspected
- sibling independence: no sibling challenge, output, agent, or verdict was
  queried, messaged, listed, or read
- mutation: this receipt only; no manifest-bound input, corpus, candidate,
  grammar, verifier, dependency, configuration, or clean-base artifact changed

## Exact-byte and normative reproduction

The expected manifest hash and byte count reproduce. All 41 bound rows reproduce
their exact canonical path, byte count, and SHA-256: the 19,707-byte precursor
at `8a5559937ce73bd0db576fb36a35427a182c0cf682cae42c50781552e6a8c667`,
the 8,789-byte G6 holdout receipt at
`3331940795e87ab747910ae92467c570aeee0c8a1cbedeaa688123a15aef038c`,
the auxiliary G5 rejection/destruction receipt, and all 38 precursor closure
entries. No candidate directory exists, the active grammar has zero TypeScript
source and no symbolic link, and the old runtime remains outside the active
projection.

The pinned official CSS Syntax source at revision
`c7573530343759ace8e46438a1fa2c44515b5554` reproduces over HTTPS as 144,427
bytes at `3f129d17407f9bd027291bcfb8015beb86fc229bf5efe505b49ec2dc30be4390`.
Section 4.3.10 is sealed exactly: digit; sign then digit; dot then digit; or
sign, dot, digit returns true, every other triple returns false, and the
operation consumes no code point. Section 3.3 supplies filtered input; mapping
the source-native UTF-16 position is correctly an upstream obligation. Because
all success characters are unchanged ASCII and any filtered/replaced character
breaks rather than creates one of these bounded prefixes, the raw/mapped
observation contract does not invent an additional semantic arm.

The occurrence ledger is also correct. Plus, minus, and dot dispatch in
consume-a-token call this predicate with the current/reconsumed point; the
leading-digit arm enters numeric consumption directly. Consume-number has the
starts-with-number precondition. Owning the predicate once below `value-unit`
and importing it into those later consumers is compatible with the acknowledged
module DAG and creates no token object, CST, lexical runtime, or scanner.

## Fixtures, composition, and holdout

The public fixture algebra reproduces 406 G3 materialized cases (306 true, 100
false), 23 G4 endpoint cases (12 true, 11 false), and the three declared
million-code-unit hostiles. Together they cover all six positive spellings,
ASCII digit domains, EOF-short negative prefixes, nonzero UTF-16 offsets,
astral and unpaired-surrogate prefixes, non-ASCII numeral near misses, and
mapped raw/preprocessed positions.

The complete G6 harness is the exact G5 harness re-export. G4 exercises the six
inherited predecessors in raw-child and ordinary-parent composition; G5 adds
package-default `undefined` in both lanes. Each case therefore receives
`7 predecessors × 2 lanes × 2 diagnostics modes × 7 profiles = 196`
observations. The raw and parent calls demand boolean truth, identical returned
state, zero child consumption, correct following-parser offset, source
own-property/value preservation, exact diagnostic object/array/entry identity
and field preservation, and unchanged global diagnostics mode. The public
wording correctly treats seven values as representatives and makes
value-dependent branching/coercion a binding five-skeptic rejection duty; it
does not claim to enumerate JavaScript values.

The fresh receipt seals 224 hidden cases, balanced 112/112, at 94,812 bytes and
`04905e995203db11d833c3e5398f756b097609a1c5b4ea2683e81beb3258fbc6`.
Its arithmetic reproduces: `224 × 196 = 43,904` complete G6 observations. The
receipt reports independent spec-oracle agreement, all-nonzero offsets, number
spellings ending at EOF, actual predicate calls at EOF, astral/unpaired-prefix
stress, mapped preprocessing pairs, and true/false near misses. It records zero
internal or G3/G4 overlap by ID, source, source+offset, and semantic tuple. The
G5 corpus was never revealed and is separately bound as irreversibly destroyed,
with zero accessible or same-byte copies; the G6 custodian records no prior
holdout access or reused material. Candidate execution/reveal remains forbidden
until all five source/overlay/projected identities close. I relied only on this
public receipt metadata.

## G5 repair audit

G5 B2 is repaired. `promotion-protocol.json` now limits mechanical claims to
the exact AST checks actually implemented and explicitly disclaims token/CST
naming, regex breadth, aliases, indirection, recursion, source derivation,
scanner behavior, O(1), KISS, and idiomaticity. Every such axis is assigned to
each exact-source quintet reviewer. The verifier's AST visitor and the protocol
now agree; no later human gate is being used to retroactively make a false
mechanical assertion true.

The literal G5 B1 omissions are also repaired. The child config and its sole
parent reproduce at their sealed hashes, the parent has no further `extends`,
and the complete installed TypeScript 5.8.3 package reproduces as 130 regular,
non-symlink files against the byte-sorted ledger
`3a08d88df997493df6c0dc751d81e52377f155defada21c1a0a7d4d0df048f47`.
The verifier validates that tree before importing its canonical
`lib/typescript.js`; it emits the direct, ledger-verified `bin/tsc` path. The
64-row parse-that dist ledger, package 1.0.0, mirror package/lock, clean base,
and both configs all reproduce. The direct config typecheck is currently GREEN.

That repair stops one layer too early.

## Blocking finding A1 — the promoted TypeScript program is not sealed

`tsconfig.apotheosis.json` contains:

```json
{
  "extends": "./tsconfig.json",
  "include": ["apotheosis/**/*.ts", "cells/**/*.ts"]
}
```

The declared candidate-specific command is the exact compiler plus
`--project tsconfig.apotheosis.json --noEmit`. Reproducing the compiler's file
set for that config yields 277 TypeScript program inputs. Only 81 of those
inputs are authenticated by the G6 closure, the 130-file TypeScript package
ledger, or the 64-row parse-that dist ledger. The other 196 inputs are live and
unbound:

- seven existing `cells/syntax-number-start/**/*.ts` sources matched by the
  glob but absent from the 38-row precursor closure: the root contract/harness,
  G2 contract/harness, and G3/G4/G5 promotion verifiers;
- 186 declaration inputs under other installed packages, including
  `@types/node`, `undici-types`, Vitest/Vite, Rollup, PostCSS, and their
  transitive type dependencies; and
- three `lightningcss` declarations resolved outside the mirror altogether,
  from `/Users/mkbabb/Programming/value.js/node_modules/lightningcss/node/`.

The sealed `package-lock.json` describes intended package content; it does not
revalidate any of those live installed bytes. The whole-TypeScript ledger
authenticates the compiler implementation and its standard libraries, not the
program sources and third-party declaration environment that the compiler
loads. `skipLibCheck` does not close this gap: declarations still participate
in module/global type resolution, and the seven unbound ordinary sources are
checked normally.

Consequently an unbound cell edit/addition can make the promised typecheck RED,
or an unbound ambient/module declaration can change what the candidate means or
what types it sees, while the manifest, verifier, package/lock, compiler/config
hashes, source hash, overlay hash, and projected grammar hash remain unchanged.
The promotion receipt does not enumerate or aggregate the resolved compiler
input set. This violates the exact-identity purpose of the candidate gate and
the drift rule. It is not merely unrelated workspace noise: `cells/**/*.ts`
and `types: ["node", "vitest/globals"]` deliberately put those bytes into the
candidate's compilation.

Close the actual compiler program. Prefer a candidate-specific sealed config
whose `files`/`include` and `types` admit only the overlay projection and its
necessary bound declarations, then ledger and revalidate every remaining
resolved input. Alternatively, bind and verify the exact complete resolved
file set. Merely adding the seven present cell files is insufficient because
the glob admits future files and the third-party declaration graph remains
live. Since configs, verifier/protocol claims, and holdout-bound public inputs
must change, this requires a fresh generation and fresh holdout.

## Architecture and benchmark cross-check

Apart from A1, promotion is appropriately conservative. The verifier derives
the mirror/live roots from its canonical location; only the overlay is supplied.
It rejects symlinks, a non-single-file overlay, wrong target, non-UTF-8 source,
non-core imports, wrong export shape, and its enumerated syntax/source-access
floor. It truthfully leaves indirect or architectural evasion to all five
skeptics. The projected active grammar ledger is exactly the one overlay row
over an empty TypeScript base. H/B/S/D/R are isolated and constrained such that
five sources must yield at least three material lineages or return to formation.

The benchmark protocol has no independent blocker. Its explicit sequence is
disable/restore/time, enable/restore/time, then disable; any exception discards
the entire run. Candidate-local restoration stays timed and harness restoration
stays outside. The ten-row schedule gives every candidate exactly two visits to
each position and every ordered pair five precedences per cycle; six measured
cycles yield 60 paired rounds. All 20 ordered ratios reset the specified PRNG,
draw 10,000 sixty-round log-ratio resamples, and use zero-based endpoint 9,499.
A strict win requires the selected candidate's four upper endpoints below zero.
External full-token parsers are correctly denied credit against this internal
predicate.

## Three-altitude adjudication

### Total tranche

The feature is the correct dependency-first pilot for the reset: a tiny
normative CSS Syntax operation, direct parse-that candidates, no lexical
substrate, clean integration base, five all-axis skeptics, three synthesis
adjudicators, and explicit full-denominator RED status. The process is heavy
for a three-position predicate, but it exercises the owner-mandated 3×5×3 law
on reusable state, diagnostics, promotion, and benchmark machinery. Allowing
an unsealed TypeScript program here would propagate a false exactness premise
to every subsequent grammar family.

### Formation generation

G6 preserves the sound G5 semantic boundary, obtains a fresh and well-formed
holdout, closes the compiler executable and two-file config identities, and
makes the AST floor honest. It does not close the inputs selected by that
configuration. Because the candidate-specific type gate can change under 196
unbound program files, the exact G6 manifest cannot admit authors.

### Feature

CSS §4.3.10 truth, occurrence ownership, zero consumption, state/source/
diagnostics preservation, representative predecessor evidence, endpoint and
hostile coverage, holdout freshness, lineage constraints, anti-scanner law,
and benchmark arithmetic are all acceptable. No semantic repair is requested.
Admission fails only at exact promotion/typecheck closure.

## Required disposition

Reject G6 before candidate code. Preserve its semantic contract, fixtures,
harness, fresh-corpus design, truthful AST nonclaims, candidate topology, and
benchmark protocol through a new immutable precursor. Replace the globbed live
typecheck with a sealed candidate-specific compiler program and authenticate
every resolved declaration/source input. Then form a wholly fresh holdout and
rerun two independent exact-byte challenges plus root gestalt. Do not reveal or
reuse the G6 holdout, and grant no implementation, integration, conformance,
performance, apotheosis, or production credit.
