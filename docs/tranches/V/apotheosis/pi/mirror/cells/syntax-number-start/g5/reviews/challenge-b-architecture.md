# SYNTAX-NUMBER-START G5 boundary challenge B — architecture/performance

**Verdict: REJECT before candidate code.** Two load-bearing promotion-boundary
defects remain. This receipt is bound to `g5/manifest.json` SHA-256
`41c482b059ddab64c51226a05022016c17adb55cb73292d665d29298aa1a0109`
(5,839 bytes).

## Seat receipt

- model served: `gpt-5.6-sol`
- reasoning effort: `ultra`
- workflow: independent exact-byte adversarial challenge, v2
- posture: assume the formation is unidiomatic, scanner-prone, incorrectly
  promoted, and benchmark-biased until its executable boundaries prove
  otherwise
- hidden material: no hidden path or bytes were requested, discovered, or
  inspected
- sibling independence: no sibling agent was queried or messaged and no
  sibling receipt content or verdict was read. After this review's findings
  and verdict were already fixed, a directory-existence check disclosed only
  that a sibling-named file existed and its byte count; no byte of it was read.
- mutation: this receipt only; no manifest-bound input, candidate, grammar,
  clean base, package, verifier, corpus, or holdout artifact was changed

## Reproduction docket

The expected manifest hash and byte count reproduced. Its immutable precursor
is 16,915 bytes at
`87e3f7bda096701bbe7faad422db37351e67f1b71c1957bdb9e980f91bc3d7ed`;
the separate holdout receipt is 5,712 bytes at
`5edada80093d8d2f40293faaf613e366d25b85a75c5a85c8ec2c31828e0c8e15`.
All 30 manifest-bound public rows (precursor, receipt, and 28-entry public
closure) reproduced their exact path, byte count, and SHA-256.

The pinned official CSS Syntax source reproduced over HTTPS as 144,427 bytes
at `3f129d17407f9bd027291bcfb8015beb86fc229bf5efe505b49ec2dc30be4390`.
Section 4.3.10 has exactly the sealed alternatives: digit; sign followed by
digit; dot followed by digit; or sign, dot, digit. It consumes nothing.
Section 3.3 establishes the filtered-code-point precondition. The plus,
minus, and dot tokenizer dispatches are callers; the leading-digit arm enters
numeric consumption directly. No semantic mismatch was found in the feature
boundary.

The public fixture algebra reproduces 406 generation-3 cases (306 true, 100
false), 23 generation-4 endpoint cases (12 true, 11 false), and the three
declared million-code-unit hostiles. The G5 harness really adds package-default
`undefined` to both raw-child and ordinary-parent lanes while retaining the
six G4 predecessor representatives. Consequently each case receives 196
observations: seven predecessors by two lanes by two diagnostics modes by
seven diagnostic profiles. The raw and parent paths check same-state return,
boolean result, zero child consumption, following-parser composition,
diagnostic reference/value preservation, and global-mode preservation. Raw
coverage across every predecessor also checks `src` own-property/value
identity. This discharges the G4 default-predecessor defect without pretending
seven values enumerate JavaScript.

The withheld receipt binds 192 fresh cases (96/96), the exact precursor, all
28 public inputs, the clean base, and the 64-row parse-that dist ledger. It
reports 37,632 complete-G5 oracle observations and four-axis disjointness
against G3/G4. I did not inspect or try to reconstruct the hidden corpus. The
receipt/reveal order is structurally sound: all H/B/S/D/R source, overlay, and
projected identities close before reveal.

The active `mirror/apotheosis/grammar` contains no TypeScript source and no
symbolic link. Its empty grammar ledger and clean-base manifest reproduce. The
rejected nineteen-file atom/CST tree is outside the active projection. No G5
candidate or integrated `number-start.ts` exists. `npm run check` is GREEN;
the default test route truthfully exits green with zero tests and zero feature
credit.

The exact H and B provenance bytes reproduce: historical
`f5723e72:src/parsing/utils.ts` hashes to
`bcaa6cb2206a1d57776a71c260b6ed30e8c29ad48ed4f1069def2896a650133c`,
and BBNF
`af15f63e:grammar/css/l4/value-unit.bbnf` hashes to
`cb57f79050a6304d79e3a7d8c7b690c902f243880aab4c896a63899290dd727b`.
The target `grammar/css/l4/value-unit/number-start.ts` is a permissible
ledger-derived submodule beneath the acknowledged `value-unit` family. It
does not create a lexical runtime or token/CST layer. H/B/S may converge on a
bounded branch lineage; D is constrained to engine-native dispatch/chain and
R to a bounded zero-width terminal. Five isolated identities plus the
return-to-formation rule can yield the required three material lineages.

The benchmark repairs are mechanically coherent. The exact sequence is now
disable, restore/time disabled, enable, restore/time enabled, then disable;
exceptions invalidate the whole run. The ten-row schedule places every one of
H/B/S/D/R exactly twice in each position, orders every unordered pair in each
direction five times, and repeats six complete cycles over 60 measured
rounds. All 20 ordered ratios reset the specified PRNG independently, pair the
same round's disabled-plus-enabled totals, draw 10,000 by 60 resamples, and
use endpoint 9,499. Candidate-local diagnostic restoration remains inside
timing while harness restoration/toggles remain outside. No ratio,
denominator, or mode-sequence blocker survives.

## Blocking finding B1 — the promoted TypeScript configuration is not closed

The promotion protocol says the active TypeScript configuration is revalidated
and that drift of the configuration starts a new generation. The verifier
hashes only `mirror/tsconfig.apotheosis.json` at lines 298–300. Those exact
bytes are not a self-contained configuration:

```json
{
  "extends": "./tsconfig.json",
  "include": ["apotheosis/**/*.ts", "cells/**/*.ts"],
  "exclude": [".dts", "bench", "deps", "grammar", "harness",
              "stylesheet", "support", "test", "tools", "*.ts"]
}
```

The inherited `mirror/tsconfig.json` is absent from the 28-row public closure,
absent from the derived-root list, absent from the verifier, and absent from
every recorded promotion identity. Its current bytes are 586 bytes at
`a0d0395313c3eb6521ba9b8550345ba21e1dba49f8f68d7c56dfa330e736a1cc`.
Changing its `strict`, module-resolution, library, or related inherited
compiler options leaves every sealed G5 identity unchanged and the verifier
GREEN while changing the meaning of the binding `npm run check` and the
candidate export/type gate.

This is the same class of supplied-versus-live transitive closure defect G5
purports to remove, now one level below the child config. Bind the complete
configuration closure (at minimum the exact parent file) or replace the child
with a self-contained config and validate its resolved compiler options. The
exact file must join the public precursor, verifier roots, drift rule, and
promotion receipt.

The AST verifier also imports the live `typescript` package, but verifies no
installed TypeScript package or byte identity. The sealed package-lock records
an intended dependency; it does not prove the code executing
`ts.createSourceFile` is those installed bytes. The repaired promotion runner
must either bind that tooling identity or execute a reproducible locked
installation and record it. Otherwise the supposedly exact source-surface
gate still has an unrecorded transitive implementation.

## Blocking finding B2 — the mechanical anti-scanner claim is false

`promotion-protocol.json` line 36 places this assertion inside
`mechanical_checks`:

> token/CST names, broad remainder patterns ... rejected

`assertCandidateSurface()` does neither. Its forbidden-identifier set at
verifier lines 99–121 contains no token, atom, component-value, or CST names.
Its traversal never inspects a `RegularExpressionLiteral`'s pattern text, so
it cannot detect `[^)]*`, `[^;{}]*`, whole-remainder lookaheads, or any other
broad regex literal. The only regex-related identifier it rejects is the
global spelling `RegExp`; ordinary parse-that `regex(/.../)` is intentionally
allowed.

For example, this declaration is mechanically admissible under the exact AST
visitor and can be inserted into an otherwise conforming candidate without
changing its observable result:

```ts
const componentValueCST = regex(/(?=[^;{}]*$)/).opt();
```

It contains both forbidden architectural naming and an unbounded
whole-remainder assertion. A candidate can sequence it before a conforming
predicate and restore the resulting diagnostics in the permitted sandbox,
thereby passing the semantic/state harness while violating the O(1),
three-position, no-CST, no-broad-remainder contract. The eventual human
quintet should reject it, but a later human gate cannot make the sealed claim
that the *mechanical* verifier rejects it true.

G4 correctly treated an explicitly necessary-not-sufficient AST floor as
acceptable. G5 regresses that truthfulness by listing checks the floor does not
perform. Either implement narrow AST checks for the promised names/patterns,
or remove them from `mechanical_checks` and state unambiguously that they are
human-quintet obligations. Do not retain the false middle state. Because the
promotion protocol and verifier are holdout-bound public inputs, either repair
requires a fresh generation and seal.

## Three-altitude adjudication

### Total tranche

The feature choice and sequencing fit the reset: a minimal non-consuming CSS
Syntax operation, direct parse-that candidates, the acknowledged BBNF
`value-unit` owner, clean integration base, five all-axis skeptics, three
synthesis adjudicators, and explicit full-denominator RED status. This is
heavy for a three-position predicate, but the owner-mandated 3×5×3 law and the
reuse of shared harnesses make it substantive rather than ceremonial. The two
promotion defects would nevertheless let every later numeric foundation cite
an incomplete configuration/tool closure and a mechanical anti-scanner proof
that was never executed.

### Formation generation

G5 discharges G4's default-`undefined`, implicit-coercion wording,
diagnostics-mode schedule, clean-base, canonical-root derivation, and fresh
holdout dockets. The manifest/receipt/corpus identities are intact. It does
not close the transitive TypeScript configuration/toolchain or its own stated
AST rejection set. Therefore the exact manifest cannot admit H/B/S/D/R
source.

### Feature

The spec truth, zero-consumption result, state/diagnostics contract,
predecessor evidence, holdout shape, module owner/path, candidate topology,
and benchmark arithmetic are acceptable. No lexical layer, token object,
atom, CST, scanner, or broad remainder is authorized by the feature contract.
Admission fails solely because the promotion machinery cannot yet prove the
exact configuration and mechanical anti-scanner claims on which candidate
identity close relies.

## Required disposition

Start a new formation generation without candidate code. Preserve the sound
semantic fixtures, G5 harness repair, benchmark protocol, clean base, and
fresh-corpus design only through a new immutable precursor. Close the full
TypeScript configuration/tooling identity and make the AST floor's mechanical
claims exact—implemented or truthfully narrowed—then obtain a fresh holdout,
two fresh independent challenges, and root gestalt. Do not reveal the G5
holdout or reuse it after changing its bound public inputs.
