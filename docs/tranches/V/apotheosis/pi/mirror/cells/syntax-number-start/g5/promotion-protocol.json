{
  "protocol_id": "SYNTAX-NUMBER-START-G5-PROMOTION",
  "status": "PUBLIC_BOUNDARY_PRECURSOR",
  "active_root": "mirror/apotheosis",
  "target_path": "grammar/css/l4/value-unit/number-start.ts",
  "candidate_overlay_root": "cells/syntax-number-start/g5/candidates/<seat>/overlay",
  "allowed_overlay_paths": ["grammar/css/l4/value-unit/number-start.ts"],
  "allowed_runtime_imports": ["@mkbabb/parse-that/core"],
  "clean_base": {
    "manifest": "../../../apotheosis/clean-base.json",
    "manifest_sha256": "dcece5fb104fbcbf1f87ec5d548307f38a0a18bf98bfdb9a771aef0bd7da37d2",
    "typescript_source_count": 0,
    "empty_grammar_ledger_sha256": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
    "rejected_pre_reset_tree": "NOT_IN_ACTIVE_SOURCE_CENSUS_OR_PROJECTION"
  },
  "mechanical_verifier": "promotion-verifier.ts",
  "live_root_binding": {
    "verifier_location": "Derive mirror root from realpath(import.meta.url) at the exact sealed cells/syntax-number-start/g5/promotion-verifier.ts location.",
    "derived_roots": [
      "mirror/apotheosis",
      "mirror/apotheosis/grammar",
      "mirror/node_modules/@mkbabb/parse-that",
      "mirror/package.json",
      "mirror/package-lock.json",
      "mirror/tsconfig.apotheosis.json",
      "mirror/cells/syntax-number-start/g3/parse-that-dist.ledger"
    ],
    "canonicality": "Every directory must equal its canonical realpath. Symbolic links below active, dependency, or overlay roots are rejected.",
    "caller_arguments": "Only the candidate overlay root is caller-supplied; live mirror, clean base, package, lock, configuration, and dependency roots cannot be substituted."
  },
  "mechanical_checks": [
    "clean-base manifest matches and active grammar contains zero TypeScript sources before overlay",
    "overlay is one canonical regular UTF-8 file at the exact target path",
    "ordinary imports are restricted to @mkbabb/parse-that/core",
    "exactly one direct exported variable named numberStart; export declarations/assignments rejected",
    "computed property access, object destructuring/spread, imperative loops, direct source/string helpers, common reflection/serialization/coercion routes, token/CST names, broad remainder patterns, and dynamic code/imports rejected",
    "all 64 installed parse-that dist paths/bytes, installed package, active mirror package/lock, and active TypeScript config revalidated at derived canonical roots",
    "source, overlay-ledger, projected one-file grammar-ledger, clean-base, dependency aggregate, and resolved-root identities emitted"
  ],
  "human_review_boundary": "The AST policy is a necessary rejection floor, not proof of absence. All five skeptics must inspect exact candidate bytes for aliases, indirection, prototype/inherited coercion, recursive or computed source derivation, scanners, contrivance, and non-idiomatic grammar. Any one unresolved source-access path rejects that candidate.",
  "typecheck": "npm run check compiles the active root and every governed candidate cell; candidate-specific harness imports must also type the named numberStart export as Parser<boolean> before close.",
  "projection": "The active base ledger is empty. The overlay ledger and projected active grammar ledger are the same single canonical row `<source-sha256><two spaces>grammar/css/l4/value-unit/number-start.ts<LF>`.",
  "integration": "After quintet and triumvirate, copy only exact selected bytes into mirror/apotheosis/target_path, require the emitted projected hash, then rerun type, semantic, holdout, hostile, and benchmark evidence. This remains prototype integration, never production execution.",
  "drift_rule": "Any public input, verifier location/bytes, resolved live root, clean-base file/census, package/lock/config, dependency byte, source, overlay, or projected identity drift starts a new promotion generation."
}
