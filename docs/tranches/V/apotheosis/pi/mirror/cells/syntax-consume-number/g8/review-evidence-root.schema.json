{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:value-js:syntax-consume-number:g8:review-evidence-root",
  "type": "object",
  "additionalProperties": false,
  "required": ["feature_id", "generation", "status", "rows", "closure_sha256"],
  "properties": {
    "feature_id": { "const": "SYNTAX-CONSUME-NUMBER" }, "generation": { "const": 8 }, "status": { "const": "EXACT_REVIEW_EVIDENCE_ROOT" },
    "rows": { "type": "array", "minItems": 34, "maxItems": 34, "items": { "type": "object", "additionalProperties": false, "required": ["id", "kind", "path", "sha256", "bytes"], "properties": { "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9._-]*$" }, "kind": { "enum": ["pre-author", "candidate-set", "seat-delivery", "payload-manifest", "candidate-source", "author-receipt", "public-correctness", "holdout-correctness", "benchmark-receipt", "benchmark-evidence-root", "benchmark-attempt-ledger", "benchmark-raw"] }, "path": { "type": "string", "minLength": 1 }, "sha256": { "$ref": "#/$defs/sha256" }, "bytes": { "type": "integer", "minimum": 1 } } } },
    "closure_sha256": { "$ref": "#/$defs/sha256" }
  },
  "$defs": { "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" } }
}
