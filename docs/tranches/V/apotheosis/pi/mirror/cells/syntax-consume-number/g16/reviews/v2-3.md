# G16 corrected-subject skeptic 3 — benchmark reconstruction

- subject: `skeptic-subject-v2.json`
- subject SHA-256: `9747e051bc0f34fbe354dac311bba2e9322f4a2ca9f4d670a1548b49f0bcc749`
- axis: benchmark erratum, inference, lifecycle, work equivalence, and claim scope
- constraint: no timing was run; this review used only exact subject-bound bytes
- verdict: **ACCEPT**

## Total-tranche analysis

The evidence proves one deliberately narrow fact: on the frozen 64-row common
domain, exact Apple M5 Max / Node 26 / parse-that environment, and declared
operation-equivalent wrappers, H2's consume-number leaf is faster than the
deposed consume-number leaf under the predeclared process-aggregate estimator.
The subject expressly withholds full-parser throughput, production-input,
per-case, LIVE scalar, C14, feature, integration, and production claims. That
boundary is necessary and correctly drawn. This benchmark can support selection
of the numeric leaf; it cannot discharge the tranche's later integrated and
peer-wide performance rails.

No benchmark defect requires a candidate, corpus, estimator, or semantic
change. The v2 packet is appropriately an evidence correction rather than a
new implementation or timing generation.

## Wave / corrected-subject analysis

I independently reconstructed the frozen raw trace rather than trusting the
controller's summaries or the superseded result descriptions.

- All controlling hashes in the v2 subject match their bytes. The raw file is
  250,611 bytes, mode `0444`, 545 complete NDJSON records, and hashes to
  `372fef4094e34b1b78d9ab8e48fcf36bcda7557f2e8ba54cac637c222de11e54`.
- The trace contains exactly one attempt ID and the exact lifecycle cardinality:
  one start, binding record, controller environment, inference, and terminal;
  30 each of spawn/environment/preflight/warmup/summary/exit; and 360 sample
  blocks. Its terminal is `COMPLETE_FIRST_ATTEMPT`.
- The 30 manifest seeds occur once in declared order in 30 distinct child PIDs.
  Each child reports the bound Node/V8/Apple M5 Max environment, the expected
  loader arguments plus the sole `--disable-warning=DEP0205` append, byte-equal
  semantic preflight, zero-byte stderr, exit code zero, and no signal.
- Every four-block warmup schedule exactly reproduces the frozen seeded shuffle
  with two blocks in each lane order. Every twelve-block retained schedule
  reproduces its seed with six blocks in each order. Across the trace there are
  180 H2-first and 180 deposed-first blocks; none is removed or substituted.
- The harness opens the sole path with `wx`, accounts for short writes, fsyncs
  every complete record, and independently attempts final fsync and close. The
  raw event order demonstrates that every child completed before the next was
  spawned and that inference and the successful terminal followed all 30
  exits. I found no second attempt or alternate raw evidence path.

The correction boundary is truthful: it changes four descriptive fields only,
retains the original raw bytes and predeclared inference, and does not claim a
rerun, estimator change, or feature credit.

## Feature / benchmark analysis

Every one of the 360 paired records has positive safe-integer durations,
320,000 operations per lane, exact logged `ln(H2/deposed)` recomputation, and
the same cross-lane work checksum. The total timed work is therefore
230,400,000 lane operations. Independently reconstructing the workload from all
64 corpus rows and 5,000 repetitions yields the recorded checksum
`9d4c0a76638e3252770fc05fd56abd4dd1d10c72686dc79557a1d42f16f440b6`;
the lane equality is not merely a copied controller assertion.

Each of the 30 process aggregates exactly equals the arithmetic mean of its 12
retained paired log ratios, and the inference record contains those 30 values
in process order. Recomputing from them gives:

- mean log ratio: `-0.07336197220979548`
- sample standard deviation: `0.027244341177386378`
- one-sided 95% upper log bound (df 29, t = 1.6991270265):
  `-0.06491032168356216`
- geometric-mean H2/deposed ratio: `0.9292644012517273`
- one-sided upper ratio: `0.9371515017780626`
- point-estimate relative time reduction: `0.07073559874827273`

The upper bound is strictly below zero, so the frozen qualification is PASS.
The estimator uses process aggregates—not 360 blocks as pseudoreplicates—and
matches the manifest exactly.

Finally, sorting every retained lane duration and independently recomputing the
two middle observations and absolute deviations gives precisely the erratum:

- H2 median / MAD: `21,818,104 / 576,396 ns`
- deposed median / MAD: `23,620,145.5 / 749,750 ns`

The process-aggregate median/MAD remain
`-0.08039757244129006 / 0.018675995216382787`; they were not among the four
false fields and reconstruct unchanged. Thus the erratum repairs the actual
defect without altering the qualifying statistic.

## Verdict

**ACCEPT.** I reproduced no blocker in the exact corrected subject. This is
benchmark-evidence acceptance only; the five-skeptic unanimity, three synthesis
adjudications, exact-byte promotion, integrated parser benchmarks, and all
tranche-level performance claims remain outstanding.
