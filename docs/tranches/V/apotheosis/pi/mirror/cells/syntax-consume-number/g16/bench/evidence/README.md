# Evidence reservation

`correctness.json` is created during the root freeze from the deterministic
`--validate` output. `raw-first-attempt.ndjson` must remain absent until the one
authorized confirmatory timing attempt. A partial or failed raw file is final
and must never be removed, replaced, or retried.
