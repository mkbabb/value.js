# First-attempt evidence directory

`raw-first-attempt.ndjson` is intentionally absent before timing. The harness
alone creates it with `wx`. Never precreate, delete, truncate, replace, or retry
that path.
