# V·π ADDENDA-05 — root E-3 gestalt adjudication

2026-07-21 · target: `ADDENDA-05.md` SHA-256
`1bf4170857c1def8a2992b1275d6fe43d1a4d6b91e9f509f9046915e400ba525`
· status: **GESTALT GREEN / OWNER RATIFICATION REQUIRED**

This adjudication is prototype governance only. It changes no code, does not
accept W3, does not authorize PB0/PB1/PB3/PB6, and is orthogonal to every
other Phase-A correction decision and both Phase-B gates.

## 1. Challenge disposition

Both independent assume-faulty challenges ACCEPT the identical packet bytes.
They independently reproduced the five immutable CSSWG sources, canonical
76-root manifest digest, eight ordered banks, exact B31 code points, and the
81-source aggregate digest
`f288380235f1a9f5a925b4bb786e1f05ca499c0b2b1da4f65a4cefd9e626effa`.
Neither challenge found a semantic, diagnostic, ownership, KISS, replay, or
authority defect.

The challenges also independently confirmed the central three-witness ruling:
the pinned Easing-2 grammar and one-point evaluation make `linear(0)` and
`linear(0% 0)` valid even though LIVE and Chromium 148 reject them. Audit B's
additional Chrome 150 witness still rejects the one-point form while agreeing
on percentage-first multirow `linear()`. Under E-4 the immutable specification
remains the arbiter; browser lag does not narrow the prototype grammar.

## 2. Root adjudication of the correction boundary

| issue | root disposition |
|---|---|
| Empty easing list members were silently deleted | **CLOSED by design.** R8-W3 extends the existing R8 rule at the three recognized easing bodies and reuses the existing top-level emptiness helper. It creates neither a duplicate global row nor a new scanner. |
| `steps()` used numeric integrality and ignored excess arguments | **CLOSED by R27.** Exact one-or-two arity, literal signed ASCII integer spelling, and existing semantic count bounds are the minimal Phase-A repair. |
| `linear()` implemented a stale LIVE subset | **CLOSED by R28.** The existing `CssLinearStop` can represent the pinned unordered number/percentage-group grammar without used-value interpolation or surface growth. |
| A property coordinating list was collapsed into one range AST; `normal 0` lost its end boundary | **CLOSED by R29's ownership split.** W3 parses one scalar arm, W5 splits declaration lists, W4 may project only singular values and must not collapse plural input, and PB6 later owns the lossless list overlay. |
| View inset and animation-range shared a false `auto`/dimension domain | **CLOSED for the literal Phase-A slice by R30.** One private length-unit registry is shared with W4 syntax without changing `coerceToSyntax`; view and range consumers retain distinct rules. |
| CSS token boundaries, typed math, and current plural/range carriers cannot fit the immediate slice | **CORRECTLY DEFERRED.** R31 belongs to PB1, R32 to PB3 then PB6, and R33 to PB0 then PB6. These remain executable RED evidence and bar a tranche-final W3 verdict. |

## 3. Total-tranche optimality

The correction is the smallest safe Phase-A boundary. Pulling R31 into W3
would create the forbidden second tokenizer; pulling R32 into W3 would create
a local unit/math engine; and forcing R33 into the frozen range types would
erase list cardinality or mutate the 33-type contract. Deferring R27–R30 would
instead leave maintained tests affirmatively blessing common-mode spec defects
and would keep W4's serializer/collector claims non-falsifiable. The proposed
split avoids both errors.

The easing/timeline bundle remains an appropriate wave. The defects concern a
shared literal-envelope and serializer boundary, not a module so large that a
new feature wave would improve ownership. Its immediate repair stays bounded:
small raw classifiers, standalone-`normal` disambiguation, and one shared
private length registry over the existing parse-that leaves. The addendum's
ban on another tokenizer, scanner, unit registry, generic easing hierarchy,
timeline god module, public token kind, local math evaluator, or speculative
plural field is therefore both necessary and sufficient.

Downstream sequencing is exact: W4 remains close-held on accepted W2 and W3;
W5 remains join-held and owns declaration-list diagnostics; W7 receives the
corrected corpus; PB1/PB3/PB6 close the deferred rows. W0 reopens only if the
implementation violates the planned private boundary and moves shared
lexeme/scanner/type ownership.

## 4. Evidence and residual RED

The current prototype remains mechanically healthy at the shared level—11
test files, 59 passing tests, four explicitly owner-gated W1 TODOs, strict
TypeScript and 33-type + 19-runtime declaration parity GREEN, exact
`@mkbabb/parse-that@1.0.0`, and zero dependency vulnerabilities. Those results
do not discharge the W3 semantic rows.

W3 remains **AUTHOR-COMPLETE, E-1 REJECTED, NOT ACCEPTED**. After owner
ratification, R8-W3/R27–R30 and D-W3-1–D-W3-3 must land, all mechanical/spec/
browser/replay gates must pass, and two fresh independent E-1 implementation
audits must ACCEPT. Even then W3 can earn at most
`PROVISIONAL_PHASE_A_ACCEPTED` until R31–R33 are GREEN in their separately
gated Phase-B owners.

## 5. Model-routing receipt variance

The formation and both challenges truthfully record inherited Codex GPT-5
family routes; exact backend revisions and Opus/Fable labels were not exposed.
No seat is relabelled as Opus or Fable. The semantic packet is GREEN, but the
standing E-5 variance remains visible: the owner may accept the truthful Codex
substitution or require the challenges to be repeated on the originally named
routes before ratification.

## 6. Root verdict and owner decision

**GESTALT ACCEPT.** The correction packet is spec-complete for its declared
pre-PB1/pre-PB3/pre-PB6 boundary, optimally modular, parsimonious, executable,
and downstream-safe. No further formation repair is known.

The owner must explicitly ratify or reject all ten decisions in
`ADDENDA-05 §8` as one authorization, including the R8-W3/R27–R33 namespace,
immediate R27–R30 semantics, pinned-spec-over-browser `linear()` ruling,
deferred owners, scalar/list split, evidence/replay duties, conditional W0
reopening, frozen public surface, provisional status, and truthful model-route
variance. Silence, the expanded tranche goal, another addendum's approval, or
continued prototype work does not ratify this sheet.
