# V·π ADDENDA-04 — root E-3 gestalt adjudication

2026-07-21 · target: `ADDENDA-04.md` SHA-256
`d17f0301d4ec19dfd6a1561cca8ba7266cf29dfe7d396d3e21d65d6aa1b13a67`
· governing harden SHA-256
`1cd096e054f8219bdb2ca83f1b76343cbe37608f37e975cf2803abd2582028b4`
· status: **GESTALT GREEN / OWNER RATIFICATION REQUIRED**

This adjudication is prototype governance only. It changes no code, does not
accept W2, does not authorize PB0/PB1/PB4/PB5, and is orthogonal to W1/W3 and
both Phase-B gates.

## 1. Challenge history and current disposition

The correction packet improved through substantive rejection rather than
accruing stale GREEN credit:

1. the first sheet had a false diagnostic digest, compared allocated `Result`
   objects instead of values, and under-specified R23/R24 compatibility REDs;
2. its repair still made a false universal one-cycle precision claim, exposed
   by a valid percentage-backed HSL counterexample;
3. the bounded-two-cycle repair was semantically correct, but the governing
   harden artifact still carried the superseded R25 digest and therefore made
   the authority chain contradictory; and
4. the harden erratum now publishes the exact twelve-source R25 payload and
   `f1dac2a8…` digest, retracts `3397ba83…`, and freezes the supporting stress
   generator mapping without displacing the normative 61-source R26 bank.

Both fresh independent challenges ACCEPT the current sheet against the
corrected governing harden bytes. They independently reproduced the official
Color 4/5 objects, canonical 76-root manifest, every R19–R26 bank, 148-entry
named-color lock, exact R23/R24 compatibility projections, R25 precedence,
and R26's current and simulated post-R19–R22 results. All earlier verdicts are
historical evidence only.

## 2. Root adjudication of the semantic boundary

| issue | root disposition |
|---|---|
| Legacy RGB/HSL syntax accepted mixed channel kinds, `none`, and numeric legacy HSL S/L | **CLOSED by R19.** Private delimiter-sensitive legacy leaves over the existing `numUnit` parser are sufficient; modern syntax remains distinct. |
| Parser-produced concrete values missed Color-4 clamps and normalization | **CLOSED by R20–R22.** Scale first, then apply only the pinned direct-RGB/alpha, hue/HSL-saturation, perceptual-lightness, and chroma transforms; `color()` coordinates remain unbounded. |
| Frozen Phase-A types cannot preserve D50 missingness or `display-p3-linear` | **CORRECTLY DEFERRED by R23/R24.** W2 retains exact named compatibility failures; PB0 freezes lossless carriers and PB5 implements them after its owner gates. No zero-fill, false cast, or mislabeled coordinate is permitted. |
| Raw `from`/nested-substitution checks lose token position and context | **CORRECTLY DEFERRED by R25.** PB1 supplies decoded token identity/spans, PB4 owns substitution/provider behavior, and W2 replays. A W2 regex or second balanced scanner is forbidden. |
| Twelve-decimal serialization was described as lossless or universally one-cycle | **CLOSED by R26's true bound.** The exact `C0→s0→C1→s1→C2→s2→C3` chain requires all intermediates, `s2 === s1`, and `C3` deep-equal to `C2`, with no epsilon. The 24 direct-space sub-bank remains stronger one-cycle evidence; the 37-source supplement covers every percentage/alpha/extreme branch; the combined 61-source bank is the universal gate. No runtime formatter retry is added. |
| Serializer and diagnostic hostile inputs could throw or drift | **CLOSED in design by D-W2-1–D-W2-4.** One head-envelope check, one outer serializer exception boundary, persistent digested H-8 evidence, and a complete W1 color replay are the bounded direct repairs. |

## 3. Total-tranche optimality

The four-way split is optimal for the frozen compatibility ring. R19–R22 and
R26 are representable with the existing Phase-A value carriers; R23/R24 need
new lossless color-expression types; R25 needs the one shared PB1 token layer
and PB4 substitution owner; and the direct repairs need no new semantic
discretion. Pulling deferred rows into W2 would either destroy information or
create the second scanner/color hierarchy forbidden by the L4 architecture.
Deferring all corrections would leave the maintained bank affirmatively
blessing common-mode spec defects.

The precision adjudication also preserves KISS. A serializer-side retry loop
would add branch/iteration cost and alter first-pass LIVE-compatible spelling
to satisfy a property-test concern. The bounded evidence contract instead
states the true formatter/parser composition, retains the twelve-decimal
policy, and keeps runtime code simple. The exact 61-source branch decomposition
plus the fully specified supporting scalar stress receipt make that boundary
falsifiable without treating random sampling as the normative proof.

Downstream routing is complete: W1 replays every corrected color through all
three value doors; W4 consumes accepted/canonical values and the R26 chain but
does not normalize colors again; PB1/PB4/PB5 close the deferred lexical,
substitution, and representation rows. W0 reopens only if implementation moves
shared lexeme/result/type/factory ownership despite the bounded plan.

## 4. Evidence and residual RED

Both challenges reproduced the shared mechanical baseline: 11 test files, 59
passing tests and four owner-gated W1 TODOs; strict TypeScript and 33-type +
19-runtime declaration parity GREEN; exact `@mkbabb/parse-that@1.0.0`; zero
dependency vulnerabilities. Those observations do not discharge W2's born-RED
semantic rows.

W2 remains **AUTHOR-COMPLETE, E-1 REJECTED, NOT ACCEPTED**. After explicit
owner ratification, R19–R22/R26 and D-W2-1–D-W2-4 must land, the persistent
corpora and full W1/W4 replays must pass, and two fresh independent E-1
implementation audits must ACCEPT. Even then W2 can earn at most
`PROVISIONAL_PHASE_A_ACCEPTED` while R23–R25 and the PB1 lexical replay remain
RED.

## 5. Model-routing receipt variance

The formation and current challenges truthfully record inherited Codex routes;
the exact service backend and Opus/Fable labels were not exposed. No seat is
relabelled. The packet is semantically GREEN, but the E-5 variance remains an
explicit owner choice: accept the truthful Codex substitution or require the
challenges to be repeated on the originally named routes.

## 6. Root verdict and owner decision

**GESTALT ACCEPT.** The sheet is spec-correct for its declared boundary,
content-addressed, executable, parsimonious, authority-safe, and optimally
routed into the Phase-B lattice. No further known formation repair remains.

The owner must explicitly ratify or reject all eight decisions in
`ADDENDA-04 §7` as one authorization: the R19–R26 namespace; immediate
R19–R22 semantics; exact bounded-two-cycle R26 chain and 24/37/61 evidence;
lossless R23/R24 deferrals; token/substitution R25 deferral; D-W2 direct
repairs; persistent replays; frozen public surface and provisional status.
The owner must also rule on the E-5 route variance. Silence, the expanded
tranche goal, another addendum's approval, or continued prototype work does
not ratify this sheet.
