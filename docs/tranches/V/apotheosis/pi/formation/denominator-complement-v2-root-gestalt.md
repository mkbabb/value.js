# V·π denominator complement occurrence v2 — root gestalt

**Verdict: REJECT v2 before occurrence-owner formation.** Its pinned census,
replay, literal line evidence, and zero-terminal-exclusion invariant remain
useful; its claimed occurrence inventory and semantic routing do not close.

Exact subject:

- tool `bb174b1c921d9ccd685a3322d2f343f06f9572327426a8d353d16503d925af97`
  (24,654 bytes);
- artifact `3b7e1e1ee40c22594049635703b3443db213f3ed8d8291e18714dda88dc2fcc9`
  (3,224,120 bytes), payload
  `6c3e1861a5584304a6c7835096722afcdae424013652cb6c15ef380f79dbe8fb`;
- receipt `ecbc09be5cf01eff998041cc294fb4b56d7cad549a0f99a9828e245615b39a77`
  (6,899 bytes).

## Independent challenges

- Challenge A REJECTED at
  `015816d0bfe1c28fb76f06bc8fb7d5c34a8eb05f997a4dae950d334566d2c45d`
  (10,939 bytes): quoted-only grammar-carrier patterns miss 53 unquoted
  `<pre class=prod|railroad>` blocks in 20 sources; 179 of 260 affected
  nonblank lines appear in no category.
- Challenge B REJECTED at
  `82af2d5f33f077410beb0dda0b349e5100907a3744657af3234ea22f8b4b0542`
  (12,283 bytes): routing still principally consumes the rejected v1 semantic
  classification rather than the new inventory; the receipt's “28 dependency
  edges” is false; and the seven heuristic categories omit load-bearing parser
  algorithms.

## Root adjudication

The root confirms all findings. Computing an inventory before calling
`routeFor(v1Entry)` is ordering in code/JSON, not occurrence-derived routing.
Because every row remains retained, this defect has not yet deleted language,
but it cannot authorize owner priority or fixed-point extraction. Likewise,
the 3.2 MB line assay is substantive reproducible evidence, not an operation
denominator; it would become process theater if its noisy lines were counted as
semantic operations or copied forward wholesale.

## Minimal v3 repair

1. Accept quoted and unquoted Bikeshed grammar-class attributes and add pinned
   regression counts for the 53 previously missed blocks / 20 sources.
2. Give all 92 complement sources one neutral
   `OCCURRENCE_OWNER_REVIEW_RED` route. Preserve v1 status, dependency,
   historical, migration, and sibling information only as non-credit flags;
   differentiated routing requires exact occurrence-to-owner joins later.
3. Correct the dependency evidence accounting with an explicit denominator:
   26 source rows, 26 flags, and 27 evidence records in v2—not 28.
4. Retain zero terminal exclusions and all conflict/successor/history signals.
5. Make full pinned-source parser algorithms, recovery, and serialization
   mandatory next-stage inputs; the heuristic arrays never prove absence or
   operation completeness.
6. Regenerate reproducibly, then obtain two fresh independent challenges and
   root gestalt before occurrence-owner formation.

This rejection grants no operation denominator, parser, conformance,
movement, or production credit.
