# V·π denominator complement occurrence v3 — challenge A

**VERDICT: ACCEPT, strictly as a neutral occurrence-discovery/routing repair.**
The exact subject is mechanically reproducible, non-lossy with respect to the
rejected v2 line evidence, and explicit that it is neither a denominator nor an
absence/normativity/ownership proof. This verdict grants no CSS-source
inclusion, operation, parser, conformance, movement, package, release, or
production credit.

`MODEL: gpt-5.6-sol` · `REASONING: ultra` · `WORKFLOW: v2`

## Exact challenged subject

| artifact | independently observed SHA-256 | bytes |
|---|---|---:|
| `denominator/tools/inventory-complement-v3.mjs` | `507c271018ce902d6ce4be824e46a0c2a37d7b714ae583eb4f2b11f6b683f8aa` | 18,638 |
| `denominator/complement-occurrence-routing-v3.json` | `e84fd664badd815754337bff733dc796caa073314d40481c21442f9f22598bd2` | 170,987 |
| compact JSON payload | `fd831705a7d29d7fca52df2338185a5b448fb2477ad07280dc5d3e5bc5e8758b` | n/a |
| `formation/denominator-complement-occurrence-v3-2026-07-22.md` | `88c343dd58b87312df454b7da037bfbf8c6ac339dbe894e403975968713aa2de` | 6,102 |

The payload hash was recomputed over compact UTF-8 `JSON.stringify(payload)`
after removing the two appended digest-description fields. A fresh invocation
of the pinned tool against `/private/tmp/value-pi-csswg-complement.TpPSHC` and
the exact v2 inputs emitted the same 170,987 bytes and artifact hash.

## Altitude 1 — total-tranche / gestalt challenge

The repair has the right boundary for this point in the tranche. It removes
the rejected semantic pre-routing rather than trying to rehabilitate it: all
92 complement sources enter one RED owner-review queue, every terminal
exclusion remains false, and old classifications survive only as named
non-credit evidence pointers. That is the parsimonious prerequisite to the
real occurrence-to-owner/fixed-point work.

Most importantly, the subject does not elevate its heuristic arrays into the
CSS denominator. `method.inventory_limit` denies absence, normativity,
ownership, and operation completeness, while `method.mandatory_next_pass`
requires the next pass to read the full pinned bytes, expressly including
parser algorithms, token/component-value behavior, recovery, and
serialization. It also forbids deriving completeness or absence from this
assay. That obligation is load-bearing and is present in the content-addressed
payload, not merely in the prose receipt.

The optimal downstream consequence is therefore narrow: after a second
independent ACCEPT and root gestalt, this artifact may seed full-source owner
discovery. It must never seed feature candidates, operation totals, exclusions,
or conformance claims directly.

## Altitude 2 — assay as a unit

### Identity, replay, and non-loss

Independent checks found:

- the pinned source-universe raw identity is
  `c1b823396dcc38607d23243f578e58ef08de79136edd52abd1bbdf6a64f606b7`;
- all 168 addressed files reproduce their recorded SHA-256 and byte counts;
- the partition is exactly 76 seed plus 92 complement rows;
- complement directory identities are duplicate-free and byte-sorted;
- v2 and v3 each contain exactly 92 unique complement rows;
- every v3 row exactly reproduces its universe path, Git blob identity,
  SHA-256, and byte count;
- every v3 row points to the correct exact v2 entry index;
- all 92 routes are exactly `OCCURRENCE_OWNER_REVIEW_RED` and all 92 exclusion
  booleans are false; and
- fresh generation is byte-identical to the challenged artifact.

The tool also verifies all retained v2 line-addressed facts against the current
pinned bytes before emitting its compact pointers. I replayed every new v3
block fact and recovered opening fact against those bytes: zero source-identity,
line-fact, duplicate-block, or opening-to-block mismatches resulted.

### Grammar-carrier mechanics

I independently enumerated class-token carriers instead of accepting the
artifact's fixed expected counts. Across the exact 92 complement sources there
are 125 bounded `pre|table|div|dl` carrier openings: 72 quoted and 53 unquoted
(52 `prod`, one `railroad`). The v3 predicate retained every quoted block and
recovered every unquoted block. The independent delta is:

- 20 affected sources;
- 53 recovered blocks and 53 recovered definition-carrier openings;
- 260 affected nonblank lines;
- 179 affected lines absent from every v2 inventory category; and
- 72 retained quoted blocks.

The line-oriented block implementation is deliberately not a general markup
parser. Its generic risks are a multiline opening tag, a second bounded opening
on the same line, carrier tags outside its four-tag family, or nesting/irregular
closing forms. I searched the full exact complement bytes for those failure
shapes: there are zero multiline carrier openings, zero carrier openings
outside the bounded family, zero nested carrier starts, zero same-tag nested
openings, zero unclosed carriers, and the robust tag scan finds the same 125
openings as the tool. Thus no pinned evidence is lost here. These limitations
would be blockers to an absence or complete-denominator claim; they are not
blockers to this explicitly inclusive, non-complete discovery delta.

### Dependency-accounting denominators

Recomputation from the exact v2 bytes yields three different historical,
non-credit quantities, kept distinct in v3:

| quantity | independently reproduced count |
|---|---:|
| rows formerly routed `DEPENDENCY_OCCURRENCE_REVIEW` | 26 |
| `DEPENDENCY_EDGE_NORMATIVITY_AND_CONSUMPTION_UNPROVEN` flags | 26 |
| dependency/status evidence records on those rows | 27 |

There is no residual false 28-edge claim and none of these counts influences a
v3 route.

## Altitude 3 — per-source adversarial replay

The following values were independently derived from the pinned sources and
the exact rejected-v2 inventory union. They match every v3 regression row.

| source | blocks | nonblank | absent from v2 | starts |
|---|---:|---:|---:|---|
| `css-anchor-position-1` | 7 | 53 | 44 | 210, 729, 861, 1419, 1606, 1695, 1858 |
| `css-anchor-position-2` | 1 | 28 | 27 | 242 |
| `css-backgrounds-4` | 7 | 20 | 12 | 275, 474, 610, 943, 1042, 1459, 1777 |
| `css-borders-4` | 4 | 9 | 3 | 272, 382, 1267, 2991 |
| `css-content-3` | 2 | 4 | 3 | 190, 329 |
| `css-forms-1` | 2 | 8 | 4 | 153, 631 |
| `css-grid-3` | 1 | 3 | 2 | 451 |
| `css-inline-3` | 2 | 9 | 9 | 660, 1063 |
| `css-link-params-1` | 2 | 4 | 2 | 158, 172 |
| `css-page-3` | 1 | 23 | 3 | 648 |
| `css-pseudo-4` | 1 | 20 | 20 | 477 |
| `css-shadow-1` | 3 | 5 | 2 | 462, 534, 1374 |
| `css-shapes-2` | 1 | 3 | 2 | 131 |
| `css-template-1` | 2 | 9 | 5 | 1097, 1178 |
| `css-text-4` | 2 | 8 | 6 | 10514, 10680 |
| `css-transitions-2` | 1 | 1 | 0 | 143 |
| `css-ui-4` | 4 | 17 | 13 | 1028, 1165, 3038, 3062 |
| `css-view-transitions-2` | 6 | 24 | 14 | 1265, 1272, 1876, 2012, 2141, 2151 |
| `fill-stroke-3` | 2 | 6 | 4 | 69, 90 |
| `selectors-5` | 2 | 6 | 4 | 335, 482 |
| **total** | **53** | **260** | **179** | |

The other 72 complement sources have zero unquoted-carrier delta. Their exact
source identities, one neutral route apiece, false exclusion bit, and exact v2
evidence pointer were checked individually; there were zero row mismatches.

## Skeptical disposition

No confirmed blocker remains within the subject's stated neutral-discovery
scope. The regex assay remains inadequate for the next stage by itself, but the
subject says so exactly and binds the required full-source algorithm pass. My
ACCEPT is therefore intentionally non-transitive: it accepts this repair and
only this repair, not the CSS denominator it is meant to help discover.
