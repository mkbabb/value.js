# V·π occurrence → operation/owner formation v8 — root gestalt

Date: 2026-07-22

Disposition: **REJECT**

V8 is a deterministic RED research bundle, not an accepted occurrence,
operation, owner, compatibility, feature, or CSS denominator. Both independent
challenges rejected the exact frozen subject. Root confirms that their findings
attack distinct load-bearing claims: the source classifier does not close the
candidate universe, semantic relations are not independently proved, and the
replay launcher/resource receipts are not a sufficient trust boundary.

## Exact subject and independent inputs

| object | bytes | SHA-256 |
|---|---:|---|
| V8 manifest | 24,485 | `1491335ca478a942efdbd7000f62a4e4aa57ce32b7db93af18ddeb346f5767af` |
| V8 formation receipt | 9,772 | `d2cc0ff450c01898715501345f22669fac55f4b774aa1428f9e8cdb8fa86e768` |
| challenge A | 13,805 | `6d0c14190c5632994e4c2e9cbfdd5b16867d8a8689e7f7191f61dfbfc8cdd56d` |
| challenge B | 17,864 | `2c10bab979b0e2ef3c13d344e0ea2667b2950fba0fa41a0cde174024e67b018f` |

The reviewers were independent and did not read one another. Challenge A
attacked the exact corpus, source-order state, ownership, and occurrence
cardinality. Challenge B independently attacked no-throw behavior, structural
and operation completeness, relation-oracle independence, launcher trust, and
resource methodology.

## Three-altitude adjudication

### Total tranche

**REJECT.** V8 improved the source census and repaired the named V7 witnesses,
but it still starts from heuristic candidates and then proves internal
reproducibility of the resulting tables. That is not an independently closed
normative denominator. The next formation must have one byte-positioned,
source-ordered state machine whose states bound delimiter formation, markup
structure, and carrier extraction; it must also separate generator code from
the semantic oracle that challenges it.

No feature boundary, parser candidate, owner lattice, or cost decision may be
derived from V8.

### Denominator wave

**REJECT with narrow evidence retained.** Root replayed the frozen formation
before adjudication: exact generation, exact check, arbitrary-output equality,
29 fixtures, and 33/33 bundled mutations were GREEN under the required
`node --max-old-space-size=650` runtime, with the recorded runs beneath the
900 MiB declared ceiling. The exact source closure remains useful: 168 pinned
files and 14,609,103 source bytes. V8 also preserves the V7 quoted-tag,
comment, escaped-shorthand, raw-example, processed-code, and active-href
regressions.

Those are regression inputs only. The replay regenerates the same false
inclusions, omissions, conflicting dispositions, and fabricated joins. Its
mutation path mostly compares the generator with its own expected bytes; its
green result therefore cannot settle the challenged semantics.

### Feature-level corpus and evidence findings

Root confirms the following union of blockers:

1. **The state space is not closed.** Active references and operations are
   emitted from fenced code; boolean/alternate highlighted `pre` forms and
   processed-code headings can likewise create false syntax or structure.
2. **Delimiter formation crosses opaque states.** A shorthand opener inside a
   raw script or an escaped outer opener can consume a later active closer and
   suppress a genuine candidate. State classification occurs too late to bound
   pairing.
3. **One occurrence can receive contradictory dispositions.** There are 3,427
   href intervals classified both as excluded tag text and included parsed
   hrefs. An active relative CSSWG href is separately false-excluded because
   it is never resolved against its source URL.
4. **The advertised parsed-token model has bypasses.** Carriers are inherited
   from the terminally rejected V1 closed set rather than re-extracted. Raw
   attribute regexes can read `dfn-for`-like prose from another quoted
   attribute and fabricate a target join.
5. **Line ordinals are insufficient ownership coordinates.** Multiple
   headings and algorithms on one line collapse, deduplicate, and acquire the
   wrong heading owner despite their distinct byte order.
6. **The reference path is not no-throw.** A bare CSSWG draft root URL, including
   the active `href` form, reaches an undefined pathname segment and throws.
7. **Operation discovery is not a normative bijection.** Keyword headings miss
   ordinary procedures such as `Procedure` and `Convert a color`, while every
   definition term can become an operation candidate without operational
   semantics.
8. **Semantic verification is not independent.** Generation, semantic
   regeneration, and most mutation expectations share the same discovery and
   assembly implementation; any thrown exception or expected-byte mismatch is
   enough to pass a named mutation without proving its stated invariant.
9. **The executable trust/resource boundary is incomplete.** The launcher
   authenticates itself only after it is already executing and is excluded
   from the declared imported-module digest. Fixture-only returns before the
   post-import boundary; raw peak-memory, work-limit, dependency, and
   publication crash-point evidence is not frozen. A malformed quoted-tag
   family exhibits near-quadratic rescanning.

These findings invalidate the 64,530-reference, 160,605-lexical-row, and
5,006-operation-candidate tables as a fixed-point input. All owner-scope and
compatibility rows also remain explicitly unjoined to reviewed carriers and
the acknowledged module DAG.

## V9 repair docket

- Replace the parallel candidate passes with one byte-positioned,
  source-ordered Bikeshed/HTML state machine covering escapes, comments,
  fenced blocks, datablocks/raw elements, processed code, tags, attributes,
  and visible text.
- Form and close delimiters only within eligible states; malformed input must
  recover monotonically with an explicit work bound and never throw.
- Resolve relative links against each pinned source URL, and give every exact
  candidate interval one and only one included-or-reviewed-excluded
  disposition.
- Re-extract carriers, headings, containers, definitions, operations, and
  references from the shared token/state stream; use parsed attributes only
  and byte offsets for ancestry and ownership.
- Bind the operation queue to a separately reviewed normative
  production/algorithm ledger instead of heading words and tag names.
- Implement an independent relation oracle and invariant-specific mutations;
  generator byte equality remains tamper evidence, not semantic proof.
- Authenticate a frozen launcher before execution, seal imported dependency
  identities on every path, and content-address raw peak/work, malformed-scale,
  and publication fault-injection evidence.
- Retain all V6–V8 positive witnesses and add every exact counterexample from
  both V8 challenges. Regenerate all shards and identities, then obtain two
  fresh independent challenges and a new root gestalt.

## Terminal credit

V8 receives **zero** denominator, semantic, owner, operation, compatibility,
conformance, parser, feature, cost-lattice, movement, integration,
megatranche, or production credit. No active grammar TypeScript or production
source was created or changed by this denominator formation.
