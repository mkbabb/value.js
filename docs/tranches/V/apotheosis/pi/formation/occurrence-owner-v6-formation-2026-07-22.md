# V·π occurrence → operation/owner formation v6 — provisional freeze

Date: 2026-07-22

Status: **PROVISIONAL / RED-ONLY / AWAITING TWO FRESH INDEPENDENT CHALLENGES
AND ROOT GESTALT.** This receipt freezes a repaired candidate occurrence and
provenance bundle for hostile review. It does not accept the denominator,
assign a feature owner, promote an operation, grant compatibility or
conformance credit, authorize parser candidates, or authorize production
execution.

## Exact frozen subject

| object | bytes | SHA-256 |
|---|---:|---|
| `denominator/occurrence-owner-formation-v6.json` | 20,149 | `13c212e58c45f685e2a7cb5a1bd6a63c1f964a318a2701e2912edfdeb414564d` |
| manifest schema | 11,617 | `b3a7131d643b469bdce4b1538ae1946d5cec19b970de71a2026078c9262f53ba` |
| shard schema | 13,811 | `23eaa9f7f7895c4c7c47005e56f06300cf4097a4b3d8ccbbd61c332e15c6dc48` |

The nine frozen shards total 14,789,544 bytes:

| shard | rows | bytes | SHA-256 |
|---|---:|---:|---|
| `codes.json` | 21 | 888 | `99f23fafebe2fc4026720e922e1e4d2f3181ce9e45f96b7c08403136e6fb9bd4` |
| `strings.json` | 33,246 | 867,900 | `de9b940fb414a33837dc92452698f3f6920e93a8d1dfe99665ba778142b8e433` |
| `sources.json` | 168 | 24,279 | `eda304fa739968187289d91dec8cc76ab7b1bd731cdb3a283116f949c3d80fcc` |
| `contexts.json` | 7,283 | 1,081,451 | `ed15854e9a80c7ae57be0d2615049b67f1e04d4e3e001f768e6aeee395dcf7c2` |
| `carriers.json` | 17,079 | 2,994,741 | `0cbe88703bdccdca1a8954752835394bf06a3bc21215f2bd11c9193aaf054aa3` |
| `operations.json` | 5,153 candidates | 1,040,333 | `dfeb2db36ddf3f8fab9bc7dda282e69d74768c04b3efb2ce841173943751e364` |
| `references.json` | 63,201 | 8,774,923 | `f62bd9045056c9fd0b7daacc1830238606fae4ebff16c02d51856ab5460659e5` |
| `compatibility.json` | 52 | 3,102 | `a0491ca3c1fdd3981747d07a3c3b65a38850e44cf542b93aeafd5d1c65339bfb` |
| `owner-scope.json` | 14 | 1,927 | `e92605106189dc445c39799357f8983d22aad724c463e5debc2b5dcd61958868` |

Manifest content digest:
`af26c705c0d35ef6cd8eff35bd475e70e70e7df58a0d0f0dc886898c28a92235`.
The nine-file V6 tool ledger digest, with each line formed as
`<sha256><two spaces><bytes><two spaces><repo-relative-path>`, byte-sorted, is
`3b7cfa42a8333aa907221a26433da68c753ed59310545d1da9d1b72cd0289f19`.

## V5 terminal rejection bound into V6

V6 authenticates the exact rejected V5 manifest, schemas, provisional receipt,
both independent `REJECT` challenges, root gestalt, and terminal rejection
object. The V5 root gestalt is
`3264d24172298667848105b7984ec247016f6d49500558065dc13eaffcd10ae0`
(5,402 bytes); the terminal rejection object is
`1c8a269b959b466ec4b6f7f473e34ad1a8c31026a29627ea9b00311c144062ba`
(3,578 bytes). V6 inherits no acceptance or credit from V5.

## Repaired semantic boundary

### Contexts, carriers, stable IDs, and foreign keys

The evidence-backed verifier now independently rediscovers and reassembles
`contexts`, `carriers`, `operations`, `references`, and `owner_scope`, then
canonical-compares those five shards one at a time. It recomputes every
`ctxv6-*`, `occ-*`, `opv6i-*`, `opv6c-*`, and `refv6-*` identifier from its
defining fields. Exact carrier→context, operation→context,
required-operation→carrier, and reference→carrier relations are thereby part
of the regenerated byte comparison rather than shape-only checks.

The targeted mutation suite rejects 29/29 cases. New cases include context ID
and section-title corruption; carrier ID, semantic-name, and context-index
corruption; stale operation-context and reference-target foreign keys; dated
TR name corruption; and URL-boundary corruption. Mutations work on the
smallest affected table closure and restore from canonical baseline bytes;
they do not clone the full 14.8 MB bundle for every case.

### Quote/context-aware Bikeshed references

Reference rows now preserve link type (`!!type`), bibliography modifier,
processing context, and target form in distinct schema cells. The scanner
recognizes all 343 authenticated CSS shorthand `!!type` occurrences (341
single-quote wrappers plus two double-single-quote wrappers), with no folding
of the modifier into the semantic name. Bibliography `inline`, `current`, and
`snapshot` modifiers are separated from spec name, fragment, and display text;
the pinned closure contains six modifier rows.

Definition target construction applies effective inherited `dfn-for`,
`data-dfn-for`, and `for`, effective definition type, and alternate link text.
The authenticated `''display/grid-lanes''` occurrence now joins the one local
`grid-lanes` carrier. Slash-bearing CSS maybe bodies such as division and
custom-property fragments remain explicit `INLINE_MAYBE` values rather than
false scope/name pairs.

HTML comments and opening/closing tags remain excluded. Content inside
`script`, `style`, `xmp`, `code`, `samp`, and code-language `pre` elements is
also excluded, while Bikeshed text and grammar-bearing non-code blocks remain
eligible. Full-corpus assertions prove that the four challenged raw/code/style
literal examples do not leak as reference rows.

### URLs and algorithm markers

Dated W3C TR URLs use the dated publication identifier (for example
`WD-css-images-4-20230217`), never the year. The authenticated closure contains
297 dated-TR rows. Terminal prose/data punctuation is removed from occurrence
intervals; the frozen table contains zero punctuation-terminated spec URLs.
Exact fixtures cover dated and undated TR URLs, CSSWG drafts, anchors, and
punctuation.

Boolean and valued `data-algorithm`, boolean and valued `algorithm`, and exact
`algorithm` class tokens are recognized. The authenticated snapshot census
remains exactly 311 algorithm-bearing openings, with a distinct 293
literal-`div` subset.

## Integrity, publication, and memory boundaries

The launcher authenticates the module/schema/runtime/Ajv identities before
import and rechecks disk identities after import. This is truthfully scoped as
`PRE_AND_POST_IMPORT_DISK_IDENTITY_WITH_RESIDUAL_PATH_IMPORT_TOCTOU`; it is not
claimed as absolute capture of the bytes consumed by the module loader.

Publication is truthfully described as `DURABLE_FSYNCED_TWO_RENAME`, with
`bundle_atomicity: false`. Files and staging directories are fsynced, but the
manifest and shard directory are two sibling renames rather than one atomic
bundle rename.

Verification has a hard RSS check of 943,718,400 bytes. An independent macOS
`/usr/bin/time -l` full generation measured 820,035,584 bytes maximum RSS and
786,097,800 bytes peak footprint, versus V5's independently measured
1,201,160,192-byte maximum RSS. The reduction comes from shardwise semantic
comparison, baseline-byte reuse after the independent replay, and targeted
mutation restoration.

## Mechanical verification and RED boundary

Repository `--check`, fresh arbitrary-output generation, and byte-for-byte
comparison of the manifest and all nine shards are GREEN. Both schemas compile
and validate. The 18 exact counterfixtures have digest
`37813e84133fe1458e3671028d9c245a590ca3630e0a0bf03bc900a1d92aea36`;
the 29-case mutation suite has digest
`afe267a585635d7b9823c372bbf5c6d6076da4bf5e9036850a4997b59bbb1265`.

Counts remain 168 sources = 76 seed + 92 complement, 14,609,103 source
bytes, 5,153 operation candidates over 5,151 unique intervals, two alias
groups, ten required RED joins, 63,201 reference occurrences, 14 owner-source
rows, 52 compatibility rows, and 37 consumer symbols. Reviewed operation and
owner counts, owner edges, and every semantic/owner/operation/compatibility
credit remain zero. Every primary symbol remains
`RED_PRIMARY_SYMBOL_UNJOINED_TO_DAG`.

Replay command:

```sh
PINNED_CSSWG_ROOT=/private/tmp/value-pi-csswg-complement.TpPSHC \
KEYFRAMES_PRIMARY_ROOT=/Users/mkbabb/Programming/keyframes-v-exec \
OCCURRENCE_OWNER_V6_OUTPUT=docs/tranches/V/apotheosis/pi/denominator/occurrence-owner-formation-v6.json \
node docs/tranches/V/apotheosis/pi/denominator/tools/occurrence-owner-v6/launch.mjs \
  --source-root "$PINNED_CSSWG_ROOT" \
  --keyframes-root "$KEYFRAMES_PRIMARY_ROOT" \
  --output "$OCCURRENCE_OWNER_V6_OUTPUT" \
  --check
```

## Requested challenge

Two fresh independent reviewers should assume the exact V6 subject remains
wrong. They should challenge the five regenerated semantic shards, every
derived ID and foreign key, Bikeshed wrapper/modifier/scope/context fidelity,
the 343/297/6 censuses, inherited definition targets, raw-context exclusions,
URL boundaries, algorithm-marker closure, publication and loader claims,
memory constraint, arbitrary-path replay, schemas, mutations, and zero-credit
contract. Only two `ACCEPT` challenges plus a separate root gestalt can
promote these exact bytes. This receipt does not accept them.
