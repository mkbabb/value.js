# V·π occurrence → operation/owner V9 — RED pre-author blueprint formation

Date: 2026-07-22

Disposition: **INCOMPLETE RED BLUEPRINT; ZERO CREDIT; NOT A V9 DENOMINATOR
SUBJECT**

V8 cannot honestly be advanced to a full V9 occurrence denominator by
patching its parallel candidate scans. Its two independent challenges and
root adjudication require a new source-order analyzer, a separately authored
semantic oracle, and a reviewed normative-operation ledger. Those three
artifacts do not yet exist. This formation therefore freezes the smaller
artifact explicitly permitted by the V8 rejection docket: the exact V9
architecture contract and hostile fixture closure that must precede their
authorship.

No denominator shard, parser candidate, grammar module, owner assignment,
operation acceptance, compatibility join, cost lattice, production source, or
movement authority was created.

## Exact frozen subject

| object | bytes | SHA-256 |
|---|---:|---|
| V9 RED blueprint | 11,406 | `fbaaadb4e3de6fc5fcef756e6bd4a1824514faac5360afa2d4c8450caf170bb3` |
| V9 hostile fixture closure | 12,452 | `a6f61c71e47c9ed323fba148b3765ba8dd6e1cb11829884e57e90824d316f44a` |
| blueprint schema | 7,029 | `7eeb8244a5efb423e20dd3241884365ec60f1edd9b5786a4da421639f6ea579c` |
| fixture schema | 4,160 | `da938323a26b645753a6b33653ab58a2523acd70b4607391a971f53a3cdb825c` |
| internal verifier | 13,064 | `ad5033e87d62377069ca973e0b51147ab505531039359c1264501d3cd15e6c0a` |

Paths:

- `docs/tranches/V/apotheosis/pi/denominator/occurrence-owner-formation-v9-blueprint.json`
- `docs/tranches/V/apotheosis/pi/denominator/occurrence-owner-formation-v9-fixtures.json`
- `docs/tranches/V/apotheosis/pi/denominator/occurrence-owner-formation-v9-blueprint.schema.json`
- `docs/tranches/V/apotheosis/pi/denominator/occurrence-owner-formation-v9-fixtures.schema.json`
- `docs/tranches/V/apotheosis/pi/denominator/tools/occurrence-owner-v9-blueprint/verify.mjs`

The blueprint content-addresses all other formation assets. Its verifier in
turn authenticates the exact five terminal V8 inputs, the 168-source universe,
the V8 source and compatibility shards, and the inherited 29-case fixture
suite before checking the V9 closure.

## Frozen V8 authority

| input | bytes | SHA-256 |
|---|---:|---|
| V8 manifest | 24,485 | `1491335ca478a942efdbd7000f62a4e4aa57ce32b7db93af18ddeb346f5767af` |
| V8 challenge A | 13,805 | `6d0c14190c5632994e4c2e9cbfdd5b16867d8a8689e7f7191f61dfbfc8cdd56d` |
| V8 challenge B | 17,864 | `2c10bab979b0e2ef3c13d344e0ea2667b2950fba0fa41a0cde174024e67b018f` |
| V8 root gestalt | 7,305 | `d27225314682318d98c5bfde5abcfd4831949fa8d2d4ee9ac777fd8ff7e94f11` |
| V8 rejection | 3,272 | `1b34b1e557d8131a0d63647da2bb7418ea1347e9d9fd9e6d8c27b7eb09c634f5` |

## Retained inputs, all zero credit

The exact source universe remains 168 files and 14,609,103 source bytes with
path digest
`5282efdfe9db215382a223e8393944227be8b5940bb3047129125daa61e19a15`.
The source-universe manifest identity is
`c1b823396dcc38607d23243f578e58ef08de79136edd52abd1bbdf6a64f606b7`
(75,132 bytes), and the V8 source shard identity is
`ff44e41c3767f3941f9dc5fe01089c8cbcdac1c50cbd0ce0751190b65d3c6373`
(24,279 bytes).

The frozen compatibility input is the V8 shard at
`882687365568d26319288652c5911e7845d651efc4db0722f131805e02445aa4`
(3,102 bytes): 52 exports, 37 consumer symbols, and **zero** DAG-family joins.
It is retained as an exact assay input, not promoted.

The complete inherited V8 fixture module remains exact at
`3c5926b4911b0826fe86d4252aa93110083c22744bd2ffb4de5b3dd45d3ebc0a`
(22,402 bytes), carrying 29 V6–V8 cases with recorded digest
`89bef854a071f9566924a9dc62da511d78c12de1e5589bf7adc3b448c668d870`.
The V9 closure adds rather than replaces it.

## What the V9 boundary now requires

The analyzer must be one UTF-8-byte-positioned, source-ordered stream. Its
declared states cover visible text, escapes, comments, fenced blocks,
datablocks, raw script/style/xmp/pre, processed code, tags, parsed attributes,
and malformed-tag recovery. Carriers, headings, containers, definitions,
operations, and references must all consume this shared stream.

Delimiter formation and closing is permitted only within one compatible
eligible state. Every exact candidate interval must receive exactly one
included or reviewed-excluded disposition. Relative hrefs resolve against the
pinned source URL. Structural and semantic ownership stays byte-positioned;
line numbers are display metadata only. Malformed input is no-throw and must
advance within the explicit `8*N+64` transition bound.

Operation admission is no longer permitted from heading keywords or generic
`dt`/tag shapes. A separately authored, independently reviewed normative
operation ledger must exist before analyzer output. The blueprint fixes its
required row fields and four fixture-only identities but truthfully records
that the complete 168-source ledger is absent.

The semantic oracle may import none of the analyzer, discovery, generator, or
table-assembly code. It must independently check state partition, disposition
cardinality, byte replay, URL resolution, parsed-attribute provenance,
ancestry, normative-operation bijection, ID collisions, and publication
closure. Fifteen named invariant-specific mutations are fixed in the subject;
whole-output byte mismatch is not an adequate semantic mutation result.

## Fixture closure

The new closure has 18 unique cases and 1,121 UTF-8 input bytes. It freezes
the exact V8 counterexamples and necessary successor boundaries:

- fenced references, fenced headings, and fenced algorithms;
- Bikeshed datablocks;
- raw-element delimiter openers that must not pair across states;
- escaped outer delimiters containing live inner syntax;
- boolean-highlight and `language-*` raw `pre` forms;
- processed-code links without processed-code Markdown structure;
- relative href resolution and the no-throw CSSWG root URL;
- parsed definition attributes without quoted-prose fabrication;
- same-line byte-position heading/algorithm ownership;
- unterminated quoted-tag recovery and a geometric malformed family;
- exactly one disposition for an active CSSWG href;
- ledgered `Procedure` and `Convert a color` operations; and
- rejection of a descriptive `dt` as an operation.

Four fixture operation identities are present solely to close the fixture
relations. They carry no normative or denominator credit.

## Replay evidence

Commands run from `/Users/mkbabb/Programming/value.js`:

```sh
node docs/tranches/V/apotheosis/pi/denominator/tools/occurrence-owner-v9-blueprint/verify.mjs --negative-controls
```

Result: `GREEN_BLUEPRINT_INTERNAL_ONLY_ZERO_DENOMINATOR_CREDIT`.

- nine frozen precursor/input identities matched;
- 18/18 fixture rows passed internal closure checks;
- 1,121 UTF-8 fixture input bytes were accounted;
- all required states and all four fixture operation-ledger identities joined;
- all zero-credit and no-generation laws held; and
- five of five boundary negative controls rejected:
  `NC-CREDIT`, `NC-GENERATE`, `NC-LEDGER`, `NC-DUPLICATE-FIXTURE`, and
  `NC-UNDECLARED-STATE`.

Schemas were independently compiled in strict JSON Schema 2020-12 mode with
Ajv 8.20.0 and both exact JSON subjects passed:

```sh
node --input-type=module -e '<strict Ajv 2020 validation of both exact schema/subject pairs>'
```

This formation did **not** run or claim exact full-source regeneration,
arbitrary-output equality, analyzer mutation results, malformed scaling, RSS,
publication crash injection, or parser benches. There is no executable V9
analyzer against which those results could honestly be measured.

## Required continuation

1. Implement the one-stream analyzer and the independently coded oracle as
   disjoint import closures.
2. Materialize and independently review the full normative-operation ledger
   for the 168 pinned sources.
3. Replay all inherited V6–V8 cases plus this exact V9 closure.
4. Generate every shard twice into arbitrary roots and prove exact equality.
5. Run every invariant-specific mutation and name the precise rejecting
   relation.
6. Freeze pre-execution launcher/dependency identities, raw RSS and work
   evidence, arbitrary malformed no-throw evidence, and every publication
   fault point.
7. Only then freeze a full V9 manifest, obtain two fresh independent
   challenges, and perform root gestalt adjudication.

## Credits

All denominator, semantic, owner, operation, compatibility, conformance,
parser, feature, cost-lattice, movement, integration, megatranche, and
production credits remain **zero**.
