# V·π denominator complement classification — root gestalt

**Verdict: REJECT the v1 semantic routing; retain its mechanical census.** The
exact 168-source universe at
`c1b823396dcc38607d23243f578e58ef08de79136edd52abd1bbdf6a64f606b7`
and 92-row classification at
`1ec512250bafc09f510194b48e3fb87f4427b83c84a6a543b58e81040ae18af2`
replay byte-identically against CSSWG commit
`c7573530343759ace8e46438a1fa2c44515b5554` / tree
`75bf19c016ed98126381508073de6893c9f756f5`. The bijection, counts, ordering,
source hashes/bytes, and payload digest are GREEN. They establish enumeration,
not a safe semantic route or operation denominator.

## Independent challenges

- Challenge A REJECTED at
  `8687bc986b96b8f9208fef12d24d96573146766f82b3e5adb0e2fe7aa64926a7`
  (17,874 bytes): snapshot/meta non-uniqueness was unproved, dependency labels
  overstated evidence, exclusion evidence was too thin, and the three known
  conflicts were not shown exhaustive.
- Challenge B REJECTED at
  `a89eaa7dc2e6db5d49c7944553de22b158130870577f28d904188a124df83dc3`
  (16,429 bytes): status/exclusion precedence ran before parser-surface and
  successor-occurrence proof.

The root confirms the shared defect. Source maturity, obsolescence, migration
intent, or a higher-level sibling is not proof that a pinned source owns no
unique parser-facing occurrence.

## Confirmed counterexamples

- `css-page-template-1` contains `@template` / `@slot` grammar despite its
  non-CSS disclaimer.
- `css-preslev-1` contains `presentation-level` grammar despite terminal work
  status.
- `css-shaders-1` contains `custom()` and `filter-margin-*`; its warning does
  not prove exact successor coverage.
- `css-template-1` contains `@region` and substantive template/grid grammar;
  “may migrate” is not a completed occurrence mapping.
- Historical/meta sources, including `css-2010`, can contain many definition
  markers; a title-only snapshot classification is not per-row non-uniqueness
  proof.

## Required v2 ordering

1. Preserve the exact 168/92 mechanical census.
2. Inventory parser-facing occurrences in every complement source before any
   terminal exclusion: grammar/definition carriers, properties/descriptors,
   at-rules, functions, selectors, recovery/diagnostic algorithms, and
   serialization.
3. Rename citation/import evidence conservatively as dependency-occurrence
   review unless an exact consuming normative occurrence is proven.
4. For superseded, redirected, obsolete, or migrating sources, bind each
   occurrence to an exact successor occurrence or retain it as experimental /
   unresolved input. Document status alone never deletes language.
5. Run a systematic conflict scan instead of treating three hand-known RED
   rows as exhaustive.
6. Twice-challenge and gestalt the repaired exact artifact before normative
   fixed-point extraction or denominator credit.

This rejection grants no parser feature, denominator, conformance, movement,
or production credit. It protects the owner goal of complete July-2026 CSS plus
experimental facilities from silent loss.
