# V·π occurrence → operation/owner formation v4 — root gestalt

**Verdict: `REJECTED`.** V4 materially improves reproducibility, sharding,
schema closure, exact byte replay, primary 52/37 preservation, and the narrow
literal-`div` algorithm census. It nevertheless fails as an occurrence
denominator. Challenge A proved authenticated omissions and false positives in
the discovery language and demonstrated three semantic/provenance corruptions
that the verifier accepts after ordinary shard rebinding. Challenge B accepted
the frozen subject only as a reproducible RED-only formation bundle and found
no software-integrity defect in that narrower claim. A correct rejection is
therefore not a vote: the exact counterexamples and verifier escapes are
decisive.

## Exact adjudicated set

| object | SHA-256 | bytes | disposition |
|---|---|---:|---|
| manifest | `75d251f549a958d811cf6b445ddd364b99d6c98b188bf958d01d0a2a0b37acef` | 16,484 | rejected subject |
| manifest schema | `a91edf2b2deff5412cfd2937b55118bb6685207c27a0ba4838423982ce6727d6` | 10,509 | rejected contract |
| shard schema | `386213aaee2086a74246a4307b65f7898c0ee77e080f5d78ecff8a293f4a9998` | 13,589 | rejected contract |
| formation receipt | `0b0bb1a13124f72cf81a88426c4d6c7c2b7c723a4a9bea6725c0f37da1fe1223` | 5,926 | provisional receipt |
| challenge A | `b23b7301c391ee7f521d8a1ebe66b88daf3989d0585f3c10925efbf7bdd750d8` | 16,664 | `REJECT` |
| challenge B | `bfb71c8496a025c67880a6c9521d41127facf7a1b622297587e500861299640a` | 9,961 | `ACCEPT` of RED-only reproducibility |

## Root reproduction and union docket

Root independently replayed the exact bundle and independently reproduced all
five challenge-A discovery failures: an algorithm-bearing non-`div` element is
absent from the opening census; `[^...^]` markup shorthand is omitted; a
compound double-single-quoted CSS shorthand is omitted; display text remains
embedded in definition/IDL target names; and a single-quoted HTML attribute
value is falsely classified as a CSS reference.

Root also independently rebound canonical shards and the manifest digest and
confirmed that `verifyBundle` accepts all three challenged corruptions:

1. an algorithm-opening offset changed to zero;
2. an owner-scope line changed to `999999` with a forged zero hash;
3. parsed reference semantic fields and target association changed while the
   raw source interval remains unchanged.

The resulting blockers are exact:

- algorithm discovery is tag-name dependent rather than an inventory of every
  Bikeshed element carrying an algorithm marker;
- reference discovery uses global regular expressions rather than lexical
  Bikeshed context, omits authenticated shorthand families, misconstrues
  display text, and scans HTML attribute values;
- owner-scope provenance is copied from rejected V1 associations instead of
  being re-extracted from the authenticated owner source;
- verification replays bytes but does not reparse the algorithm opening tuple,
  reference semantics/targets, or owner-scope line/hash tuples.

The exact source closure (168 = 76 + 92), source-byte identities, compact
sharding, arbitrary-output replay, 52/37 RED inputs, ten RED required joins,
and zero-credit contract survive as useful machinery. They grant no
denominator closure or owner/operation credit.

## Required successor

V5 must preserve the surviving machinery while independently inventorying all
algorithm-bearing Bikeshed element forms and retaining a distinct literal-`div`
census; recognizing references in Bikeshed text context with exact target,
scope, anchor, and display-text semantics; excluding HTML attribute values;
re-extracting owner-scope inputs from the current authenticated owner source;
and mechanically reparsing all three semantic/provenance relations during
verification. Each repaired relation requires an exact counterfixture and a
mutation that fails after canonical rebinding. V5 remains RED and zero-credit
until two fresh challenges and a new root gestalt accept its exact frozen
bytes.

This verdict authorizes no parser candidate, feature boundary, owner edge,
cost lattice, production mutation, or megatranche execution.
