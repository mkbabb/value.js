# V·π occurrence → operation/owner formation v7 — root gestalt

Date: 2026-07-22

Disposition: **REJECT**

V7 is a reproducible RED research artifact, not an accepted occurrence,
operation, owner, compatibility, feature, or CSS denominator. Both independent
challenges rejected the exact frozen subject on confirmed corpus defects, and
the defects are complementary rather than duplicative.

## Exact subject and independent inputs

| object | bytes | SHA-256 |
|---|---:|---|
| V7 manifest | 21,945 | `259d9fce299411222a126245ce22c627b260f4260d7009ecaf94ab9fbb7c0968` |
| V7 formation receipt | 6,864 | `5c53eb81a34c1f8d95a2aff5134403217dbf5e53bee41b7dd3b19af1556e6861` |
| challenge A | 14,554 | `d63c351119de1732194f9134752adf0d08e85eddbb74f20fd7fa2ec0e78ec528` |
| challenge B | 7,641 | `9308962748155124807f4ea0603d6b89dbc47673f5abd4afb070fbaeb70b185d` |

The reviewers were independent and did not read each other. Challenge A
attacked normative/source coverage; Challenge B attacked the same frozen bytes
through fresh benign parser samples and heading-context fidelity.

## Three-altitude adjudication

### Total tranche

**REJECT.** A denominator must make active normative references discoverable,
inactive text distinguishable, and source ancestry faithful before owner or
feature rows can be derived. V7 fails all three. Its honest zero-credit status
limits the damage but does not permit promotion.

The next formation must stop repairing each newly found delimiter with another
local expression. One shared, source-ordered Bikeshed lexical-state model must
classify comments, escapes, raw/example elements, processed code, tag
attributes, quoted attribute values, and visible text. Headings, containers,
carriers, operations, and references must consume that same classification.

### Denominator wave

**REJECT with narrow evidence retained.** Root independently replayed the exact
bundle: manifest and all nine shards reproduced byte-for-byte; 21 fixtures and
30/30 mutation controls passed; the run took 7.3 seconds and 848,527,360 bytes
maximum RSS, below the declared 900 MiB cap. V7 also retains the V6 repairs to
LF/CRLF multiline shorthand, the two CSS Borders code-link witnesses, mixed
heading ancestry, owner-line text/hash fidelity, semantic dictionaries, and
the 168/52/37 identity joins.

Those are useful regression inputs only. Exact regeneration faithfully
regenerates the defects below.

### Feature-level corpus findings

1. **Missing active links.** Challenge A found 3,482 absolute CSSWG/W3C TR URLs
   inside active `a[href]` openings across 155 sources and zero exact reference
   rows. The CSS Color 5 `combine-integers` dependency is a normative witness.
2. **False escaped/raw references.** V7 emits 96 escaped shorthand occurrences
   and 51 strings in class-highlighted raw example blocks as live references.
3. **Comments become normative structure.** Ninety-nine carriers and fourteen
   operation intervals lie wholly inside HTML comments; commented headings
   become live ancestry.
4. **Quoted tags corrupt visible headings.** Challenge B found 32 authenticated
   carrier contexts whose headings are corrupted by quote-blind tag removal.
   The exact `compositing-1/2` witness freezes `normal blend mode` as
   `">normal blend mode`.

These errors invalidate the 63,373-reference and 5,151-operation-candidate
censuses as a fixed-point input. No owner or feature derivation may consume
them.

## V8 repair docket

- Parse active HTML/Bikeshed openings with quote-aware attribute boundaries;
  discover exact `href` reference occurrences and explicit exclusions.
- Classify escaped shorthand, comments, script/style/xmp, highlighted
  `pre` variants, and processed `code`/`samp` in one lexical pass.
- Derive visible heading text without allowing `>` inside quoted attributes to
  terminate a tag.
- Drive heading ancestry, containers, carriers, algorithms, definition-list
  operations, and references from that single classified source stream.
- Require a complete included-or-reviewed-excluded disposition for every
  candidate reference/structure interval; four selected leak counters are not
  a completeness proof.
- Retain and re-run every V6/V7 positive witness plus the new CSS Color 5,
  escaped internal-slot, raw JavaScript, CSS Display comment, and compositing
  quoted-heading negatives.
- Regenerate every affected shard, count, stable ID, schema identity, manifest
  digest, and resource receipt; then obtain two fresh independent challenges
  and a new root gestalt.

## Terminal credit

V7 receives **zero** semantic, owner, operation, compatibility, conformance,
parser, feature, cost-lattice, movement, integration, megatranche, or
production credit. No active grammar TypeScript or production source was
created or changed by this denominator formation.
