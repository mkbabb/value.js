# V·π occurrence/owner formation v3 — root gestalt

**Verdict: `REJECTED`.** V3 repairs the narrow output-path replay defect in
v2, but both fresh challenges independently found that its exact occurrence
substrate is incomplete and that its bound schema/receipt cannot carry the
claims made for them. It remains evidence about failed discovery machinery,
not the CSS denominator or a work queue for semantic ownership.

## Exact adjudicated set

| object | SHA-256 | bytes | disposition |
|---|---|---:|---|
| `denominator/occurrence-owner-formation-v3.json` | `3eaa1f5604a2e67087cfa1ddb8fedc571cd99f820ab716d6c7ec7ed1e86c5ef9` | 69,590,643 | rejected subject |
| `denominator/occurrence-owner-formation-v3.schema.json` | `80f081145d3ac554f79fd68becd468748e567391ebe08ae2e4229893d5cbd72d` | 6,775 | rejected contract |
| `formation/occurrence-owner-v3-formation-2026-07-22.md` | `7b6ae52d8abba34df2ea741d8f586323a22c9e7af4b09d6c5e01e2e39aaf793f` | 5,810 | false 81,647-byte aggregate claim |
| challenge A | `2b6ce7ff3f6f0c90af68d50ecdc01937e73cc52736410ba90d93de9cc9d8072c` | 14,309 | `REJECT` |
| challenge B | `f204b533e4147347005ea2f6373ffda182bd47ae24151dff052bcd4a76dae1cc` | 18,006 | `REJECT` |

The root independently reproduced the exact v3 bytes at an arbitrary output
path, recomputed payload SHA-256
`a630fff1643f5731af4a48a56ee9c8e8b9a4acea19ffc09881f761f8029f8ca2`,
and confirmed zero formal denominator, parser, owner, cost, compatibility,
conformance, audit, or production credit. That narrow replay result is the
only accepted v3 improvement.

## Confirmed union docket

1. The discovery grammar omits common authenticated Bikeshed syntax:
   single-quoted CSS shorthands, scoped double-single-quoted CSS shorthands,
   `<{element}>`, and scope extraction for slash-bearing IDL/property
   references.
2. The operation extractor misses the prevalent `<div algorithm>` and
   `<div algorithm="...">` forms. Challenge A measured 218 such blocks outside
   every emitted candidate and provided exact CSS Color counterexamples.
3. None of the ten required slices has an exact operation-candidate join;
   three have no enclosing operation candidate at all.
4. Two exact operation intervals receive duplicate IDs under different kinds.
   Counts therefore conflate candidate rows with unique operation boundaries.
5. The fixture suite omits the production syntaxes that reveal these defects.
6. The JSON Schema admits fabricated rows, contradictory counts, false
   digests, GREEN/owner-edge mutations, and nonzero audit/compatibility credit.
   The generator does not execute the schema.
7. Generator-file identity is not executed-byte identity and the runtime
   envelope is unpinned. A load/hash race can bind bytes other than those Node
   executed.
8. The receipt's generator total is false: the ten bound files total 81,708
   bytes, not 81,647.
9. The 52/37 routing is a RED prose transcription rather than an authenticated
   primary-source-to-DAG join.
10. The 69 MB monolith repeats RED strings, contexts, and nested v1 rows;
    91.5% is carrier/reference material. Its shape is hostile to independent
    review and contrary to the tranche's parsimony law.

Uniform RED labels do not cure missing occurrences: absent rows have no review
route. Exact hashes do not cure a schema that permits contradictions. A stable
69 MB replay is not a complete denominator.

## Required successor shape

V4 must be a new subject, never a patch to v3. It must add the missing syntax
families and operation forms, join every required slice, distinguish row IDs
from interval aliases and reviewed operations, and add exact counterfixtures.
It must use a compact content-addressed manifest plus independently reviewable
shards/tables rather than another repeated monolith. Closed schemas must reject
contradictory credits/counts/rows and be executed during generation; relations
outside JSON Schema must be recomputed by the verifier. The launcher must bind
the bytes actually executed and record the runtime envelope. Compatibility
routing remains RED until joined to primary 52/37 sources and the acknowledged
DAG.

After a fresh freeze, two wholly new hostile challenges and another root
gestalt are mandatory. Until then, denominator closure, feature-ledger closure,
owner edges, costs, and broad fan-out remain RED. This verdict authorizes no
parser, grammar, production, or megatranche mutation.
