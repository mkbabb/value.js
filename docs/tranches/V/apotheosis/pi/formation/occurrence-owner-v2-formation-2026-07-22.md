# V·π governed occurrence → operation/owner formation v2

**Status: `PROVISIONAL_AWAITING_TWO_CHALLENGES_AND_GESTALT`. Zero denominator,
parser, owner-graph, cost, compatibility, conformance, wave, or production
credit.** This v2 is a fail-closed challenge subject. It authenticates and
retains discovery evidence, but it does not promote any mechanical carrier or
reference into a reviewed operation or owner edge.

## Exact subjects

| subject | SHA-256 | bytes | disposition |
|---|---:|---:|---|
| `denominator/occurrence-owner-formation-v2.json` | `005a79b0e6d407e50baa15c3b6081499326f237705b4f4c711bcf0dfc10e12d5` | 69,589,449 | provisional v2 challenge subject |
| `denominator/occurrence-owner-formation-v2.schema.json` | `7a38ba2bf92192fa6f8d0bd8ddbfbeae3b493db068f688f57fb7fbdd2786647a` | 6,072 | bound JSON Schema 2020-12 contract |
| compact payload before digest/replay fields | `5e00b2c182db7e695392e8ed185e1453ec7de6ccca88d17f3298fc6dda7e2720` | n/a | reproduced by generator |
| modular generator identity | `a218c0c46b386b1ba13d000cc3fa73110796779bca8b7ba1dd7fa8c2640a8add` | 78,937 across 9 files | path/hash/byte manifest bound in artifact |

The generator identity is SHA-256 over byte-sorted UTF-8 records of the form
`path NUL sha256 NUL decimal-bytes LF`. Every component path, raw hash, and byte
size is serialized in the artifact. The exact replay command is also serialized
there; repository-root execution is required.

## Rejected v1 inputs authenticated before use

| exact input | SHA-256 | bytes |
|---|---:|---:|
| `denominator/occurrence-owner-formation-v1.json` | `86d021681ed0e46c067b7a103c35f34ec0793d8eca90059e0d50c6a554f46407` | 12,766,323 |
| `denominator/tools/occurrence-owner-formation-v1.mjs` | `74105301b0dcf66f1c4b014c18e52479e2799edcb6bfd626fd5c4ea68c3b4863` | 51,103 |
| `formation/occurrence-owner-v1-challenge-spec.md` | `7171bfac873e7d224f7fe82487ea00bd29c166c27dbbe79cbb53a1b29a2a68e0` | 19,828 |
| `formation/occurrence-owner-v1-challenge-architecture.md` | `6f127a66edca2a96123e2e3a08772c016c0c6578b9d87dcc51ecef3e1bdfad35` | 21,630 |
| `formation/occurrence-owner-v1-root-gestalt.md` | `0712c58b6cab1b7eef3dd67006f3b35b15c222fb54eeb7a0edf7cb6d9929021b` | 6,665 |
| `denominator/occurrence-owner-formation-v1-rejection.json` | `69b5f6696def012d9055e3011a5a880135499f4adfdbe244c5b4785b082c5dd8` | 3,115 |

The v1 artifact is consumed only as rejected authenticated discovery input.
All 17,079 v1 carrier objects are preserved verbatim under
`v1_discovery_row`, and all 10 required slices replay. No v1 semantic status,
owner edge, compatibility owner, or lattice row is accepted or carried
forward.

## Modular formation boundary

The nine files under `denominator/tools/occurrence-owner-v2/` separate:

1. exact input/source/set/DAG/census authentication;
2. Bikeshed ATX, setext, HTML heading, section, and container context;
3. dt+dd boundary formation plus Bikeshed, Markdown, and legacy HTML
   algorithm/recovery/serialization candidate discovery;
4. full-byte typed/scoped reference discovery with raw spelling, explicit
   normativity marker, anchor, and exact target carrier IDs;
5. fail-closed carrier diagnostics for duplication, multi-name, multi-domain,
   slug collision, same-symbol/multi-owner, context, and operation completeness;
6. explicit content-addressed compatibility candidates and exact owner-input
   lines; and
7. fixture execution, assembly, deterministic serialization, and replay.

Every mechanical operation candidate is
`RED_UNREVIEWED_OPERATION_BOUNDARY`. The reviewed-boundary set is empty.
Every discovery carrier has a RED semantic and owner disposition. Every
reference remains RED even when it has one candidate target. Multiple exact
target carrier IDs are ambiguous regardless of any coincident rejected v1
owner string.

Compatibility ownership uses one explicit table, not symbol-name regular
expressions. `parseTimingFunction` and `CssTimingFunction` route to the
`easing` review candidate. `ParseIssue` and `ParseResult` have no owner
candidate and remain `RED_PENDING_NON_TOKEN_RESULT_DIAGNOSTICS_BOUNDARY`.

## Validated identities and census

| measure | validated result |
|---|---:|
| authenticated source closure | 168 files / 14,609,103 bytes |
| independently derived membership | 168 = 76 seed + 92 exact universe-minus-seed complement |
| exact rejected discovery carriers retained and replayed | 17,079 |
| required source slices retained and replayed | 10 |
| exact parsed MODULE-DAG subject | 16 nodes / 46 edges / acyclic / all root-reachable |
| compatibility exports extracted from pinned `coverage.md` | 19 runtime + 33 type = 52 |
| keyframes-consumer symbols extracted from pinned `coverage.md` | 37 |
| v2 operation-boundary candidates | 4,836 |
| full-source typed/scoped reference occurrences | 46,460 |
| reviewed operation boundaries / reviewed owner dispositions | 0 / 0 |
| semantic owner edges / cost lattice | 0 / not emitted |

The 4,836 operation candidates include 129 algorithm sections, 64
serialization sections, 9 recovery sections, 86 Markdown list regions, 106
legacy HTML ordered-list regions, 7 explicitly marked algorithm blocks, 4,225
dt+dd term/body regions, and 210 term boundaries that conservatively remain
incomplete RED. HTML-implied `dt`/`dd` end tags are represented explicitly in
the boundary records rather than misread as EOF-spanning blocks.

## RED mass

| population | RED rows |
|---|---:|
| authenticated source semantic dispositions | 168 |
| rejected discovery carriers with v2 fail-closed dispositions | 17,079 |
| operation-boundary candidates | 4,836 |
| typed/scoped reference occurrences | 46,460 |
| owner-scope inputs | 14 |
| compatibility obligations | 52 |

Carrier diagnostics include 744 duplicated-interval groups / 1,514 involved
carriers, 1,058 multi-name carriers, 311 function carriers, 2,586
multi-domain-candidate carriers, 3,134 legacy-slug-collision carriers, 4,665
same-symbol/multi-owner carriers, 596 context-incomplete carriers, and 3,240
algorithm/recovery/serialization-completeness-unreviewed carriers. These
categories overlap; they are diagnostic populations, not a denominator or
cost sum.

Reference results remain 26,175 external/unresolved RED, 12,521 ambiguous
target-occurrence RED, and 7,764 single-target-but-unreviewed RED. The three
categories total all 46,460 occurrences. No reference owner edge is emitted.

## Adversarial fixture receipt

All 9/9 fixtures pass:

- scoped `[=set/Remove=]` does not join an unscoped `remove` definition;
- a gradient/color function remains multi-domain RED;
- `&lt;image-src>` and `<<image-src>>` retain one typed grammar identity;
- `[[FOO]]` and `[[!FOO#bar|Bar]]` remain distinct occurrences with marker and anchor;
- ATX serialization and its Markdown list are discovered;
- `<dt>` binds the adjacent `<dd>` while remaining unreviewed RED;
- change/history context propagates;
- multi-name definitions remain RED; and
- an unclosed algorithm block remains structural RED rather than consuming EOF as valid.

## Remaining blockers

1. Two fresh independent hostile challenges and a new root gestalt must accept
   this exact artifact hash.
2. Mechanical candidates still need content-addressed review for exact
   semantic and operation boundaries, including the large incomplete dt
   population.
3. Typed/scoped references need reviewed target and owner adjudication;
   external targets are neither fetched nor silently aliased.
4. Compatibility candidates need owner review, and diagnostics/results need a
   ratified non-token boundary.
5. Until reviewed unique operations exist, owner edges and any owner/cost
   lattice remain forbidden.

No parser, production source, active grammar, feature ledger, handoff, module
DAG, or inbox was changed by this formation.
