# Preflight prompt census — not an audit verdict

**Status:** mechanical/root preflight only; the receiving Opus audit must
independently reproduce or reject every statement below.  
**Route used:** `gpt-5.6-sol`, ultra reasoning. This is not represented as the
owner-requested Opus audit.

## Subject

The preflight inspected the three canonical rollout files named
in `raw-prompts/INDEX.json`, the generated prompt archives, the prior and
current Value handoffs, the steering/addenda chain, and the cross-task
delegations. It made no repository mutation.

## Reproduced prompt census

At the reviewed extraction boundary:

| task | canonical prompt events | unique bodies | classification |
|---|---:|---:|---|
| original Value tranche-V formation | 83 | 83 | 19 direct owner prompts, 64 delegated receipts/prompts |
| current V·π refinement | 28 | 28 | 13 direct owner prompts, 15 delegated receipts/prompts |
| BBNF coordination | 70 | 69 | separate cross-repository task archive |
| **total** | **181** | **180** | canonical `event_msg/user_message` events |

The earlier count of 304 was not a canonical prompt count. It included
synthetic/duplicated `response_item/message/user` envelopes and is rejected.
Canonical prompt preservation must use `event_msg/user_message`; the rollout
hash preserves the complete transport record, including excluded automatic
goal/plugin/environment envelopes.

## Direct V·π steering chronology

The first twelve direct owner prompts in the current task establish, in order:

1. deep prototype work now, outside production, with active BBNF coordination;
2. full CSS plus experiments, idiomatic parse-that, modular BBNF alignment,
   and continuous peer-beating benches;
3. an accounting of prototype and megatranche state;
4. rejection of contrived scanner-centered work;
5. rejection of the atom/CST/lexical layer;
6. a total first-principles reset plus at least three candidates, five
   skeptics, and three adjudicators;
7. status, convergence, and BBNF-blocker scrutiny;
8. the explicit ruling that Claude does not own BBNF and must not block it;
9. an audit of the original goal, inventory, addenda, and prototypes;
10. a percentage accounting of the audit;
11. an explanation for two days of work without an idiomatic CSS parser; and
12. the present forensic, raw-prompt-preserving handoff request.

The thirteenth clarifies that this session must develop the handoff and audit
programme, not execute the fresh audit itself.

The prior Value task additionally binds direct implementation over ceremony,
KISS/fewer LOC, material-defect addenda, no quick fixes or shims, wide
parallelism, visual verification, and the preference for the internal Browser
over standalone Playwright where it can supply the exact witness.

## Required corrections

1. `HANDOFF.md` is stale and unsafe as the continuation entry point. It omits
   later foundation G4, percentage G0–G2, dimension G0, the raw-prompt archive,
   and current model truth. Its file map also identifies an obsolete numeric
   generation as active.
2. The whole `docs/tranches/V/apotheosis/pi/` tree is untracked at the current
   Value HEAD. The new handoff must say so explicitly; file hashes are
   evidence identities, not committed-history identities.
3. The interrupted preflight route was Sol ultra, not Opus, and earns no audit
   credit. Historical Fable/Opus labels are requested routing, not
   served-model receipts.
4. `MODULE-DAG.md` had two nonterminal review identities before its terminal
   ACK. Only SHA-256
   `291e51451d84f2fd41adb8b9a31b3c33cf1503b6f47019a06e16702c98daca8f`
   carries the terminal structural acknowledgement.
5. The owner’s `3×5×3` requirement remains binding at the coherent-feature
   boundary defined by `ADDENDA-07`. Re-grouping, exemption, or weakening is a
   new owner/addendum decision; a handoff may not silently reinterpret it.
6. The direct-work edict and `3×5×3` create real tension. The correct current
   disposition is to keep the evidence machinery compact and reusable while
   retaining the owner-required seats—not to discard either law.
7. Historical duration percentages and convergence estimates are judgments
   over an incomplete denominator. They must not be promoted into measured
   completion facts.
8. The internal Browser preference supersedes unconditional standalone
   Playwright wording.
9. Glass C8 was later made historical/rejected through C9/C10. C11 was a
   non-authoritative candidate pending a later adjudication. None is current
   Value parser authority, and none authorizes a Value mutation.

## Handoff consequence

The raw prompt approach is fit for the requested lossless handoff after the
canonical-event correction. The generated archive is a discovery source, not
an authority source: every assistant finding still requires a separate
disposition from current bytes, executable replay, specification/browser
evidence, or an exact acknowledged cross-task receipt. No fresh-audit credit
follows from this preflight.
