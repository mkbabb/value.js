# V·π denominator complement occurrence v3 — root gestalt

**Verdict: ACCEPT the exact neutral discovery/routing stage.** This authorizes
only the next full-source occurrence-owner/fixed-point formation. It does not
accept a CSS operation denominator, any parser feature, or production work.

Exact subject:

- tool `507c271018ce902d6ce4be824e46a0c2a37d7b714ae583eb4f2b11f6b683f8aa`
  (18,638 bytes);
- artifact `e84fd664badd815754337bff733dc796caa073314d40481c21442f9f22598bd2`
  (170,987 bytes), payload
  `fd831705a7d29d7fca52df2338185a5b448fb2477ad07280dc5d3e5bc5e8758b`;
- formation receipt
  `88c343dd58b87312df454b7da037bfbf8c6ac339dbe894e403975968713aa2de`
  (6,102 bytes).

## Independent challenges

- Challenge A ACCEPTED at
  `bc1f62007df6f6457e58fa7314d2699ed41f479f536792ea377bb0564e3504c4`
  (7,666 bytes).
- Challenge B ACCEPTED at
  `e1fbfd9f4a0650f085a82c7446ca7b971afe6e4b18196d7f1e1bf5cbd90e51f7`
  (11,146 bytes).

Both independently reproduced the byte-identical pinned replay, exact payload,
168-source identity, 76+92 partition, 92-entry bijection, one neutral
`OCCURRENCE_OWNER_REVIEW_RED` route per complement source, and zero terminal
exclusions. Both also reproduced the quoted/unquoted grammar-carrier repair:
53 recovered blocks across 20 sources, 260 affected nonblank lines, 179 lines
formerly absent from every v2 category, 72 quoted blocks retained, and 53
unquoted opening lines added. The dependency assay denominators are explicitly
26 source rows, 26 flags, and 27 evidence records.

## Gestalt ruling

The v3 design is optimal for this stage because it stops pretending that
status, citation, history, or migration evidence establishes semantic routing.
All 92 sources remain RED under one neutral owner-review route. The 171 KB
delta preserves only the repaired evidence and exact pointers to v2 instead of
copying the rejected 3.2 MB assay forward. This is substantive and KISS enough
for a reproducible discovery rail.

Its limitations are binding, not footnotes. The grammar-carrier regex covers
the actual pinned opening-tag forms found by the independent scan, but the line
assay proves no absence, normativity, owner, fixed point, or operation
completeness. The next formation must read every full pinned source byte and
extract/join grammar productions, parser/token/component-value algorithms,
properties/descriptors, at-rules, functions, selectors, recovery, diagnostics,
and serialization. It may cite v3 for discovery and non-loss but may not count
heuristic lines as semantic operations.

The next formation must union these 92 complement sources with the 76 seed
sources, owner experiments, 52-export compatibility surface, 37 consumers, and
the terminally acknowledged module DAG; derive exact occurrence-to-owner rows;
then run two fresh challenges and gestalt before the denominator is accepted.

No lexer, token-object, atom, CST, scanner runtime, parser implementation,
conformance, benchmark, movement, or production credit follows from this
gestalt.
