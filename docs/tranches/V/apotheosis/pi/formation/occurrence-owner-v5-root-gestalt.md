# V·π occurrence → operation/owner formation v5 — root gestalt

Date: 2026-07-22

**Verdict: `REJECTED`.** Both independent challenges reject the exact V5
subject. Their blockers are complementary and promotion-bearing: challenge A
proves that the Bikeshed reference recognizer omits authenticated forms,
misclassifies source text, loses link semantics and inherited definition
scope, and that the verifier accepts forged context/carrier provenance;
challenge B independently proves the same incomplete context/carrier
authentication boundary, a 297-row dated-TR naming defect, terminal-URL
punctuation errors, and several overbroad integrity claims. Reproducibility and
an honest RED posture cannot promote a reproducible semantic error.

## Exact adjudicated set

| object | SHA-256 | bytes | disposition |
|---|---|---:|---|
| manifest | `9f64d59ac8effffdfe476805b5d2086bba8281f55609e3f575b714f7fdf51b10` | 18,048 | rejected subject |
| manifest schema | `e406bf06bcb06535a1e015b209d1c9c85c14485c3dd7f88be5cbec936c1a6a8e` | 10,510 | rejected contract |
| shard schema | `9e688ecd62e939679898ffb2201c4fb64443d74cb7e5f212670e705c7da0a5b2` | 13,590 | rejected contract |
| formation receipt | `a0000722fd3b774329c13fe6fddea3268e608770360e3ba74868bbcd5371b143` | 7,540 | provisional receipt |
| challenge A | `72941acb997243e7ee3a206d4f1478b8949fbe0d6a17647af71b8eb00a09e167` | 14,581 | `REJECT` |
| challenge B | `3fb419a4f0dfd41348eab52958894dc9bce71a506420470c71e13dd108fc0904` | 13,488 | `REJECT` |

The frozen nine-shard bundle totals 13,821,858 bytes and has manifest content
digest
`f5aa53b02d09b117f07116a3176a6d3a62312b7598a5b64ea943e39ef502b800`.

## Root union docket

The following failures are confirmed and must all be repaired in one fresh
subject before another challenge:

1. Context rows and carrier rows are not independently regenerated and
   canonical-compared by `verifyBundle()`. Stable context/carrier IDs,
   carrier names, and carrier→context joins can be forged after canonical
   rebinding. Stale operation context and reference target foreign keys can
   consequently survive.
2. Single-quoted Bikeshed CSS shorthands with `!!type` are omitted; the pinned
   source closure contains 343 such occurrences. Wrapper/link type, modifier,
   effective scope, display, fragment, and processing context do not have a
   sufficient schema representation.
3. Definition target construction ignores inherited `dfn-for`,
   `data-dfn-for`, and `for`; the authenticated `display/grid-lanes` target
   therefore fails to join.
4. Bibliography modifiers such as `inline`, `current`, and `snapshot` are
   folded into the specification name instead of represented separately.
5. Program and style literals in raw/code/style contexts are falsely emitted
   as CSS shorthands, while slash-bearing CSS maybe syntax is incorrectly
   forced into a scope/name pair.
6. Specification URL occurrence intervals consume terminal punctuation.
   Dated W3C TR URLs use the year as the spec name in 297 rows rather than the
   dated publication identifier.
7. Boolean `data-algorithm` is outside the advertised recognizer even though
   the receipt claims all boolean or valued forms.
8. `atomic_bundle_write: true` overstates a durable, fsynced two-rename
   publication. The loader ledger is a pre/post disk identity check with a
   residual path-import TOCTOU window, not absolute executed-byte capture.
9. Verification peaks near 1.2 GB for a roughly 13.8 MB bundle because it
   materializes duplicate whole table sets and whole-bundle mutation clones.

These are not feature-owner decisions. They invalidate the occurrence and
provenance foundation from which the owner lattice would be derived.

## Narrow facts retained without promotion

- The authenticated source closure remains exactly 168 sources: 76 seed plus
  92 complement, with 14,609,103 source bytes.
- The current source snapshot contains exactly 311 authenticated
  algorithm-bearing openings, including the distinct 293 literal-`div`
  subset. That snapshot census is retained; advertised boolean
  `data-algorithm` support is not.
- The 5,153 operation candidates over 5,151 intervals, two explicit alias
  groups, ten required RED joins, exact ADDENDA-01 owner-line provenance, and
  52-export/37-consumer inputs remain useful RED evidence.
- Every reviewed-operation, owner, owner-edge, compatibility, conformance,
  parser, and production credit remains zero.

## Required successor

V6 must regenerate and canonical-compare each semantic shard dependency,
recompute every derived ID and context edge, and add targeted mutations for
the accepted corruptions and stale downstream foreign keys. Its reference
model must become quote/context-aware and retain Bikeshed wrapper, modifier,
scope, fragment, display, link type, and inline/maybe context explicitly. It
must add authenticated fixtures and full-corpus census assertions for each
confirmed omission or corruption, correct URL targets and boundaries, make
publication and loader claims precise, and perform shardwise verification and
targeted mutation work within an explicit memory bound.

V6 remains RED-only. It must freeze new exact bytes and receive two fresh
independent challenges plus a new root gestalt. Nothing in V5 authorizes a
feature row, candidate, owner edge, cost lattice, parser implementation,
production mutation, or megatranche execution.
