# V·π denominator complement occurrence v3 — independent challenge B

**Verdict: ACCEPT the exact v3 subject for its one stated movement only: root
gestalt may adjudicate whether to authorize the full-source occurrence-owner /
fixed-point formation.** The artifact proves no CSS owner, inclusion,
normativity, operation denominator, parser behavior, conformance, or terminal
exclusion. I found no blocker within that deliberately narrow boundary.

MODEL: `gpt-5.6-sol`; REASONING: `ultra`; WORKFLOW: `v2`, fresh independent
adversarial pass. I did not inspect, query, message, or consume the sibling v3
challenge or its output.

## Exact subject and replay

| subject | expected SHA-256 | independently observed | bytes |
|---|---|---|---:|
| `denominator/tools/inventory-complement-v3.mjs` | `507c271018ce902d6ce4be824e46a0c2a37d7b714ae583eb4f2b11f6b683f8aa` | same | 18,638 |
| `denominator/complement-occurrence-routing-v3.json` | `e84fd664badd815754337bff733dc796caa073314d40481c21442f9f22598bd2` | same | 170,987 |
| formation receipt | `88c343dd58b87312df454b7da037bfbf8c6ac339dbe894e403975968713aa2de` | same | 6,102 |

The compact payload digest independently recomputes to
`fd831705a7d29d7fca52df2338185a5b448fb2477ad07280dc5d3e5bc5e8758b`.
A fresh invocation against only the pinned local source tree, the exact
source-universe subject, and the exact rejected-v2 tool/artifact/rejection/
challenge/gestalt subjects regenerated the 170,987-byte artifact byte for
byte. The invoked tool reverified every one of the 168 source SHA-256 and byte
identities at CSSWG commit/tree
`c7573530343759ace8e46438a1fa2c44515b5554` /
`75bf19c016ed98126381508073de6893c9f756f5`.

I separately checked the 92 entries against the complement rows and their v2
entry indices. Every source identity, v2 artifact pointer, rejected-v1 label,
rejected-v2 route, and ordered secondary-flag-code list agrees exactly. The
92 directories are unique and byte-sorted; every row has the sole primary
route `OCCURRENCE_OWNER_REVIEW_RED`; every terminal-exclusion value is false.
The route is a literal constant in the generator. Neither a v1 classification
nor a v2 route participates in its selection.

## Altitude 1 — total-tranche / gestalt

This is the right minimal repair after v2. It removes the rejected semantic
taxonomy from authority rather than renaming it again, while retaining its
useful warning evidence behind an exact, explicitly non-credit pointer. The
result cannot prioritize or omit a source: all 92 go to the same owner-review
queue, and all 92 remain non-excludable.

The 170,987-byte delta is useful evidence, not process theater. It is about
5.3% of the rejected 3,224,120-byte v2 artifact because it carries only the
92 neutral rows and the newly recovered, line-addressed grammar blocks. The
content-addressed v2 entry pointer preserves the old literal/status/conflict
evidence without copying 3.2 MB forward. A smaller status-only receipt would
lose the reproducible carrier repair; copying the old assay would add bulk
without new truth.

The mandatory next-stage boundary is load-bearing and is stated correctly in
both artifact and receipt: the owner/fixed-point formation must read the full
pinned bytes, including parser algorithms, CSS token/component-value behavior,
recovery, and serialization. It may cite this assay but may not infer absence,
normativity, ownership, fixed-point closure, or operation completeness from
the heuristic arrays or their counts. Therefore this ACCEPT can safely
authorize only root gestalt and, if root accepts, that full-source formation.
It cannot authorize candidate parser work or denominator credit.

The special loss counterexamples remain visible but non-credit. Exact v2
pointers preserve page-template and presentation-level status conflicts;
Color 3, shaders, template layout, scoping, and shadow-parts successor signals;
the historical snapshots; and the cascade/text/web-animation identity and
migration conflicts. Because route selection ignores all of those labels,
none can silently become inclusion, exclusion, priority, or ownership.

## Altitude 2 — artifact / wave analysis

### Carrier repair

An independent replay of the line-addressed delta confirmed:

- 53 newly recovered unquoted `prod|grammar|railroad` carrier blocks in
  exactly 20 complement sources;
- 260 distinct affected nonblank lines, each present in the corresponding
  repaired grammar-block inventory;
- 179 of those lines absent from every v2 inventory category;
- 53 recovered definition-carrier opening lines; and
- all 72 quoted blocks retained.

Every recovered opening is an unquoted class carrier on the bounded
`pre|table|div|dl` tag family, every recorded start/end lies in its exact
source, and every trimmed evidence fact matches its pinned line. The shared
carrier predicate drives both the block inventory and the opening-line delta.
The method still calls this heuristic discovery evidence and makes no absence
claim, so remaining multiline or differently marked grammar prose belongs to
the mandatory full-source pass rather than being falsely declared absent.

### Dependency, status, conflict, and recovery evidence

The three dependency denominators are truthful and now named distinctly:

| historical v2 denominator, non-credit | observed |
|---|---:|
| rows whose v2 route was `DEPENDENCY_OCCURRENCE_REVIEW` | 26 |
| `DEPENDENCY_EDGE_NORMATIVITY_AND_CONSUMPTION_UNPROVEN` flags | 26 |
| dependency/status evidence records attached to those 26 rows | 27 |

I also independently replayed all 93 v2 dependency/status records and all 119
ordinary line-addressed secondary-flag facts against the pinned source bytes;
no path, source hash, line, or fact mismatch appeared. The 49 synthetic level
or sibling facts remain recognizable as source-universe/metadata facts rather
than source-line claims. V3 does not call any of these 26/26/27 quantities
dependency edges or normativity evidence.

The recovery/serialization category remains available through each exact v2
entry pointer, but v3 does not mistake that narrow heuristic category for the
behavioral denominator. Its full-source next-pass law explicitly requires
parser algorithms, recovery, and serialization beyond the arrays. This closes
the v2 regression without manufacturing operation credit.

### Mechanical gate disposition

- exact 76 + 92 partition and 92-row complement bijection: **GREEN**;
- exact tool/artifact/receipt identities and payload digest: **GREEN**;
- byte-identical replay against the pinned local source tree: **GREEN**;
- one neutral route for all 92 and zero terminal exclusions: **GREEN**;
- rejected v1/v2 semantics absent from route selection: **GREEN**;
- old warning evidence retained only by exact non-credit pointer: **GREEN**;
- carrier regression counts and line facts: **GREEN**;
- dependency denominators 26/26/27: **GREEN**;
- full-source algorithm/recovery/serialization obligation: **GREEN**;
- claimed CSS owner, operation denominator, or conformance credit: **none**.

## Altitude 3 — per-entry findings

Every row below has finding **N**: the sole neutral route, exact source/v2
pointer, `PARSER_FACING_OCCURRENCES_DISCOVERED_NON_CREDIT`, and false terminal
exclusion all match. Additional codes show the old warning signals preserved
through that pointer; they never drive the v3 route.

- **DEP** dependency consumption/normativity unproved
- **H** historical/meta non-uniqueness unproved
- **LVL** directory/metadata level conflict
- **MIG** migration/successor occurrence diff required
- **PR** formerly primary RED
- **SIB** multi-level sibling ownership review
- **STAT** maturity/scope status coexists with parser-facing surface

| source | retained warning codes beyond N/P | source | retained warning codes beyond N/P |
|---|---|---|---|
| `compositing-2` | SIB | `css-2007` | H, STAT, SIB |
| `css-2010` | H, STAT, SIB | `css-2015` | H, STAT, MIG, SIB |
| `css-2017` | H, STAT, MIG, SIB | `css-2018` | H, STAT, MIG, SIB |
| `css-2020` | H, STAT, MIG, SIB | `css-2021` | H, STAT, MIG, SIB |
| `css-2022` | H, STAT, MIG, SIB | `css-2023` | H, STAT, MIG, SIB |
| `css-2024` | H, STAT, MIG, SIB | `css-2025` | H, STAT, MIG, SIB |
| `css-anchor-position-1` | DEP, MIG, SIB | `css-anchor-position-2` | SIB |
| `css-backgrounds-4` | SIB | `css-borders-4` | DEP, MIG |
| `css-box-4` | DEP, SIB | `css-break-4` | DEP, SIB |
| `css-cascade-3` | MIG, SIB, PR | `css-cascade-6` | MIG, SIB |
| `css-color-3` | SIB, PR | `css-color-6` | SIB |
| `css-color-hdr-1` | MIG | `css-conditional-values-1` | — |
| `css-contain-3` | MIG, SIB | `css-content-3` | — |
| `css-display-4` | DEP, SIB | `css-egg-1` | MIG |
| `css-exclusions-1` | — | `css-extensions-1` | — |
| `css-flexbox-2` | MIG, SIB | `css-fonts-5` | DEP, SIB |
| `css-forms-1` | STAT | `css-gaps-1` | DEP |
| `css-gcpm-3` | MIG, SIB | `css-gcpm-4` | STAT, MIG, SIB |
| `css-grid-3` | SIB | `css-highlight-api-1` | — |
| `css-image-animation-1` | — | `css-images-5` | DEP, SIB |
| `css-inline-3` | DEP, MIG | `css-line-grid-1` | — |
| `css-link-params-1` | — | `css-mixins-1` | DEP |
| `css-mobile` | STAT | `css-module` | — |
| `css-multicol-2` | MIG, SIB | `css-navigation-1` | — |
| `css-overflow-3` | DEP, STAT, SIB | `css-overflow-4` | DEP, MIG, SIB |
| `css-overflow-5` | SIB | `css-overscroll-1` | — |
| `css-page-3` | DEP, MIG, SIB | `css-page-4` | SIB |
| `css-page-floats-3` | MIG | `css-page-template-1` | STAT |
| `css-position-4` | SIB | `css-preslev-1` | STAT |
| `css-print` | STAT | `css-pseudo-4` | DEP |
| `css-regions-1` | DEP, MIG | `css-rhythm-1` | — |
| `css-round-display-1` | — | `css-ruby-1` | DEP |
| `css-scoping-1` | MIG, PR | `css-scroll-snap-2` | DEP, MIG, SIB |
| `css-shaders-1` | STAT, MIG, PR | `css-shadow-1` | DEP, MIG |
| `css-shadow-parts-1` | MIG, PR | `css-shapes-2` | SIB |
| `css-size-adjust-1` | — | `css-sizing-4` | DEP, MIG, SIB |
| `css-spatial-nav-1` | MIG | `css-tables-3` | MIG |
| `css-template-1` | STAT, MIG, PR | `css-text-4` | DEP, STAT, SIB |
| `css-text-5` | LVL, SIB, PR | `css-text-decor-4` | DEP, SIB |
| `css-transitions-2` | DEP, SIB | `css-tv` | — |
| `css-ui-4` | DEP, STAT, MIG, SIB | `css-view-transitions-2` | STAT, MIG, SIB |
| `css-viewport-1` | — | `fill-stroke-3` | DEP |
| `filter-effects-2` | SIB | `indexes` | H |
| `matrix` | STAT | `pointer-animations-1` | — |
| `selectors-5` | DEP, SIB | `selectors-nonelement-1` | STAT |
| `web-animations-2` | DEP, MIG, SIB | `web-animations-css-integration` | MIG, PR |

No per-entry exception, lost signal, extra route, terminal exclusion, or source
identity mismatch was found.

## Disposition

**ACCEPT** this exact v3 subject for root gestalt. If and only if root gestalt
also accepts, it may authorize the next full-source occurrence-owner /
fixed-point formation. That formation must resolve all 92 rows from the pinned
source bytes and may not promote the v1/v2 labels, v3 heuristic arrays, or
their counts into semantic truth. No parser prototype, owner decision,
operation denominator, conformance, movement, package, release, or production
credit follows from this challenge.
