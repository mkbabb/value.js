# V·π denominator complement occurrence v2 — independent challenge B

**Verdict: REJECT the exact v2 semantic-routing/receipt subject; ACCEPT only
its mechanical census, literal occurrence evidence, replay, and zero-terminal-
exclusion invariant.** The exact artifact is useful evidence, not process
theater, but it does not yet earn the receipt's claim that conservative routing
is GREEN and therefore does not authorize the occurrence-owner/fixed-point
formation. Two small, load-bearing repairs are required: remove the rejected
v1 semantic classification from route authority, and correct the receipt's
false dependency-edge count. The next formation must also extract algorithmic
parser behavior from the pinned sources rather than treating this heuristic
line inventory as the owner denominator.

MODEL: `gpt-5.6-sol`; REASONING: `ultra`; WORKFLOW: `v2`, fresh independent
adversarial pass. I did not inspect, query, message, or consume the sibling v2
challenge or its output.

## Exact subject and independent replay

| subject | expected SHA-256 | independently observed | bytes |
|---|---|---|---:|
| `denominator/tools/inventory-complement-v2.mjs` | `bb174b1c921d9ccd685a3322d2f343f06f9572327426a8d353d16503d925af97` | same | 24,654 |
| `denominator/complement-occurrence-routing-v2.json` | `3b7e1e1ee40c22594049635703b3443db213f3ed8d8291e18714dda88dc2fcc9` | same | 3,224,120 |
| formation receipt | `ecbc09be5cf01eff998041cc294fb4b56d7cad549a0f99a9828e245615b39a77` | same | 6,899 |

The payload digest recomputed independently as
`6c3e1861a5584304a6c7835096722afcdae424013652cb6c15ef380f79dbe8fb`.
A fresh invocation against only the pinned local tree
`/private/tmp/value-pi-csswg-complement.TpPSHC`, the exact universe, seed,
rejected v1, and v1-rejection subjects regenerated the artifact byte for byte.
That replay reverified all 168 source hashes and byte counts. The 76 + 92
partition, sorted duplicate-free 92-row bijection, source identities, 14,141
per-source distinct discovery-line sum, 16,022 serialized line facts, route
counts 26/46/12/3/5, and all 92 `terminal_exclusion_authorized: false` values
are GREEN.

The pinned source tree was used only at the already recorded commit/tree
identity `c7573530343759ace8e46438a1fa2c44515b5554` /
`75bf19c016ed98126381508073de6893c9f756f5`. No network or moving draft was
substituted.

## Altitude 1 — total-tranche / gestalt

The occurrence-first reset is directionally correct. It fixes the dangerous v1
failure: none of `css-page-template-1`, `css-preslev-1`, Color 3, shaders,
template layout, redirects, snapshots, or identity-conflict rows can disappear
merely because a title, status, or successor sentence says they are old,
experimental, moved, or non-CSS. Zero terminal exclusion is an effective
loss-prevention invariant because every complement source remains in the next
owner queue.

The 3.2 MB artifact is justified only as a generated, content-addressed assay.
It gives reviewers exact source lines and makes regeneration falsifiable. It
would become process theater if expanded into a parallel hand-maintained CSS
spec database, or if its 16,022 noisy heuristic facts were counted as 16,022
semantic operations. The KISS next step is a compact owner ledger that cites
the pinned sources and this assay; do not copy all line facts forward.

The exact v2 nevertheless overclaims at the semantic boundary. Its public
method says inventory precedes routing, but `routeFor(v1Entry)` at tool lines
199–218 routes from the **semantically rejected v1 classification**. At lines
401–421 the new inventory is computed before the route only temporally and in
serialized key order; the route function never consumes that inventory. Apart
from eight experimental and five successor overrides, the 92 labels are still
the old status/citation partition with safer names. This does not itself lose
language because all rows are retained, but it cannot earn a GREEN semantic
route or set review priority under an occurrence-first claim.

The parsimonious repair is not another taxonomy. Either:

1. give all 92 rows one neutral `OCCURRENCE_OWNER_REVIEW_RED` route and retain
   the present status/history/dependency/sibling facts only as non-credit flags;
   or
2. derive each differentiated route from an exact occurrence-to-owner or
   occurrence-to-successor join produced by the next formation.

The first option is preferable now. It separates the sound discovery assay
from the later semantic decision and avoids laundering rejected v1 judgments
through renamed buckets.

## Altitude 2 — artifact / wave analysis

### Confirmed strengths

- Every complement row is present exactly once and is source-addressed.
- Every inventory fact replays to the pinned line; no line/source mismatch was
  found.
- Every route is explicitly non-credit, and all terminal exclusions are false.
- Historical snapshots and `indexes` remain occurrence-diff RED.
- `css-color-3`, `css-scoping-1`, `css-shaders-1`,
  `css-shadow-parts-1`, and `css-template-1` remain successor-diff RED.
- `css-cascade-3`, `css-text-5`, and
  `web-animations-css-integration` remain identity/migration RED.
- Page-template and presentation-level syntax are retained despite their
  status prose.
- Category totals and the payload digest are mechanically reproducible.

### Blocking defects

**B-1 — routing still depends on rejected v1 semantics.** The exact tool accepts
`v1Entry.classification` as the principal branch input. Calling the result
"occurrence-first conservative routing GREEN" is therefore not proved. The
inventory is evidence beside the route, not authority for it. Repair with the
single neutral route or exact owner joins described above.

**B-2 — the exact receipt's dependency-edge count is false.** Receipt line 110
claims 28 dependency edges. The artifact contains 26 dependency-route rows,
26 `DEPENDENCY_EDGE_NORMATIVITY_AND_CONSUMPTION_UNPROVEN` flags, and 27 attached
dependency evidence records (25 seed edges plus two redirect-chain records;
`css-shadow-1` owns the second record). None of these reproducible quantities
is 28. Correct the receipt and state the chosen denominator explicitly.

### Required non-blocking guard for the next formation

The inventory categories are not an operation inventory. An independent broad
probe found 1,628 of 1,855 pinned lines containing parse/token/grammar/syntax/
invalid/consume-family words outside the inventory union. That probe is itself
heuristic, but concrete omissions are load-bearing: `css-shadow-1` lines
1475–1531 contain the part-mapping parsing algorithms; `css-mixins-1` has
parse-time and token-production requirements such as lines 192, 231, 238,
426, 970, and 979–990; historical snapshots carry forward-compatible parsing
requirements, for example `css-2007` lines 220–229 and `css-2015` lines
395–414. The artifact candidly disclaims operation completeness, so this does
not invalidate its literal evidence. It does mean the next owner formation
must read the full pinned source and add an explicit parser-algorithm/recovery/
serialization operation pass; it may not compute the fixed point from these
seven arrays alone.

## Altitude 3 — per-entry findings

Codes below are findings, not acceptance credit:

- **D** — retained dependency-review input; cited/imported evidence proves no
  normative consumer. Exact owner/consumer join is still required.
- **X** — retained experimental/later/non-language review input; the label has
  no exclusion or normativity force. Full syntax and algorithm ownership is
  still required.
- **H** — historical/meta occurrence-diff correctly remains RED.
- **S** — successor/redirect occurrence-diff correctly remains RED.
- **I** — identity/migration conflict correctly remains RED.

Every one of the 92 rows was checked for exact source identity, route, inventory
presence, secondary flags, and false terminal exclusion:

| source | finding | source | finding |
|---|---|---|---|
| `compositing-2` | X | `css-2007` | H |
| `css-2010` | H | `css-2015` | H |
| `css-2017` | H | `css-2018` | H |
| `css-2020` | H | `css-2021` | H |
| `css-2022` | H | `css-2023` | H |
| `css-2024` | H | `css-2025` | H |
| `css-anchor-position-1` | D | `css-anchor-position-2` | X |
| `css-backgrounds-4` | X | `css-borders-4` | D |
| `css-box-4` | D | `css-break-4` | D |
| `css-cascade-3` | I | `css-cascade-6` | X |
| `css-color-3` | S | `css-color-6` | X |
| `css-color-hdr-1` | X | `css-conditional-values-1` | X |
| `css-contain-3` | X | `css-content-3` | X |
| `css-display-4` | D | `css-egg-1` | X |
| `css-exclusions-1` | X | `css-extensions-1` | X |
| `css-flexbox-2` | X | `css-fonts-5` | D |
| `css-forms-1` | X | `css-gaps-1` | D |
| `css-gcpm-3` | X | `css-gcpm-4` | X |
| `css-grid-3` | X | `css-highlight-api-1` | X |
| `css-image-animation-1` | X | `css-images-5` | D |
| `css-inline-3` | D | `css-line-grid-1` | X |
| `css-link-params-1` | X | `css-mixins-1` | D |
| `css-mobile` | X | `css-module` | X |
| `css-multicol-2` | X | `css-navigation-1` | X |
| `css-overflow-3` | D | `css-overflow-4` | D |
| `css-overflow-5` | X | `css-overscroll-1` | X |
| `css-page-3` | D | `css-page-4` | X |
| `css-page-floats-3` | X | `css-page-template-1` | X |
| `css-position-4` | X | `css-preslev-1` | X |
| `css-print` | X | `css-pseudo-4` | D |
| `css-regions-1` | D | `css-rhythm-1` | X |
| `css-round-display-1` | X | `css-ruby-1` | D |
| `css-scoping-1` | S | `css-scroll-snap-2` | D |
| `css-shaders-1` | S | `css-shadow-1` | D |
| `css-shadow-parts-1` | S | `css-shapes-2` | X |
| `css-size-adjust-1` | X | `css-sizing-4` | D |
| `css-spatial-nav-1` | X | `css-tables-3` | X |
| `css-template-1` | S | `css-text-4` | D |
| `css-text-5` | I | `css-text-decor-4` | D |
| `css-transitions-2` | D | `css-tv` | X |
| `css-ui-4` | D | `css-view-transitions-2` | X |
| `css-viewport-1` | X | `fill-stroke-3` | D |
| `filter-effects-2` | X | `indexes` | H |
| `matrix` | X | `pointer-animations-1` | X |
| `selectors-5` | D | `selectors-nonelement-1` | X |
| `web-animations-2` | D | `web-animations-css-integration` | I |

The special counterexample rows are correctly retained but remain unresolved:

- `css-page-template-1`: 52 discovery lines include `@template`/`@slot`
  material; the non-CSS disclaimer cannot exclude them.
- `css-preslev-1`: 30 property/descriptor block-lines preserve
  `presentation-level` material; status cannot exclude it.
- `css-color-3`: 384 discovery lines, including 74 property-block lines and
  110 function lines, require exact Color 4/5/6 occurrence joins or explicit
  retention.
- `css-shaders-1`: 116 discovery lines preserve `custom()` and
  `filter-margin-*`; the Filter Effects sentence is not a completed mapping.
- `css-template-1`: 478 discovery lines preserve `@region` and substantive
  template/grid material; "may migrate" proves no owner.
- `css-scoping-1` and `css-shadow-parts-1`: their one-line heuristic hits are
  mostly redirect markup, but both remain correctly RED until joined to exact
  `css-shadow-1` owners.
- the twelve H rows prove no non-uniqueness; snapshots also carry parser-policy
  prose missed by the current category arrays.
- `css-cascade-3`, `css-text-5`, and
  `web-animations-css-integration` are correctly primary RED. In particular,
  the Level 5 directory / Level 4 metadata mismatch for `css-text-5` is real.

## Disposition and minimal repair

The exact trio is **REJECTED for semantic-routing acceptance**. Preserve the
artifact as content-addressed discovery evidence; do not regenerate its 3.2 MB
payload merely to hide the history. Produce a small v2.1 route/receipt repair
that:

1. uses one neutral owner-review RED route for all 92 rows unless an exact
   occurrence-derived owner join exists;
2. corrects 28 to the explicitly defined 26 rows or 27 evidence records;
3. makes full-source parser algorithms, recovery, and serialization mandatory
   inputs to the next owner/fixed-point stage; and
4. retains zero terminal exclusions and every H/S/I conflict until exact joins
   discharge them.

Then run two fresh independent challenges and root gestalt. Until then this
challenge authorizes no occurrence-owner formation, denominator, parser,
conformance, movement, package, release, or production work.
