# V9 occurrence-owner corpus tooling — repair generation 2 implementation receipt

Date: 2026-07-22  
Scope: denominator research tooling only  
Status: **GREEN replay / RED authority / zero denominator and parser credit**

## Exact subject

| artifact | bytes | SHA-256 |
|---|---:|---|
| `denominator/tools/occurrence-owner-v9/analyzer.mjs` | 46,651 | `919af7dbd7a094e13c2d72d8e82684b5f9d9201f88aaa462bccfeb1fdeb107cd` |
| `denominator/tools/occurrence-owner-v9/oracle.mjs` | 44,369 | `899cf3d2c336c4ac84b855da902cb00e6269336080b19ae051e1ec0fc66a881e` |
| `denominator/tools/occurrence-owner-v9/test.mjs` | 31,914 | `6c4da44635d2a1e31f68966360a5fdaa5483240ff1780a60561c069e33e75ebb` |
| `denominator/occurrence-owner-formation-v9-repair2-evidence.json` | 7,894 | `c82f75e684f1990d1e458a124f7c8ca592377a0bf01f0bf1b473108d17963cb4` |
| deterministic stdout | 14,468 | `ec77c14f630df19f54befa2117f385dbf25312d448748efd12c929f6bac87861` |

The machine-readable evidence does not self-reference; this receipt binds its
post-write identity explicitly.

## Rejected input union

This generation repairs the exact union of two independent REJECT reviews:

- challenge A: 25,666 bytes,
  `b79556fe5aab2f7a6c7424267ad994e8577fbcaf265a5cf9a412d2c706f52a21`;
- challenge B: 17,956 bytes,
  `475cb782b16b725b515b605381e0862f072cf2e1351ff85190378a9f31c08a71`.

The repairs are executable rather than status-only: source-coordinate-safe
ASCII raw closes; correct closing-ATX and ATX/Setext exclusivity; explicit
reference-shape and specification-URL admission; exact non-spec href
dispositions; normalized multi-scope definition owners without generic
`label[for]` fabrication; complete ledger-field, owner, and duplicate-interval
enforcement; and indexed stack/range/heading/ledger/carrier relations.

The oracle does not import analyzer helpers. It uses a generated ASCII-only
raw-tag regex, declarative ledger validators, independent reference-shape
predicates, and separately derived owner relations. The test rail freezes all
source counterexamples that defeated repair generation 1 and adds mutations of
the repaired evidence seams.

## Replay

Syntax checks were GREEN. Three workspace executions (including the timed run)
and one execution from `/tmp` produced byte-identical stdout. The rail covers:

- 18 frozen V9 fixtures and 29 inherited V8 evidence cases;
- 26 exact semantic counterexamples;
- 16 independently rejected result mutations;
- 512 arbitrary decoded byte sequences (130,605 input bytes; 237,348 decoded
  UTF-8 bytes), with no throw;
- all 13 declared state labels;
- 35 geometric observations over seven families, with zero benchmark or
  formal-bound credit.

One local timed suite observation was 0.39 seconds, 259,850,240 bytes maximum
resident set size, and 226,619,800 bytes peak footprint. These figures are not
a denominator-scale measurement.

The old deterministically quadratic `stack.some` path was also replayed outside
the frozen stdout at 500, 1,000, 2,000, 4,000, and 8,000 nested non-processed
elements. The largest 88,000-byte/16,000-tag input recorded 120,001 instrumented
units and 8.876291 ms in this one local observation. That is evidence that the
identified scan is gone, not a formal complexity or performance proof.

## Honest resource boundary

The analyzer no longer performs a whole-source Unicode fold, depth scan per
boundary, linear matching-close search, full-ledger search per operation,
full-carrier search per reference, full-range search per heading, full-heading
search per algorithm, or per-scalar `Buffer.byteLength` slice.

Some costs are necessarily output-sensitive: a many-to-many definition join
must emit every resolved carrier ID; projected text can revisit overlapping
malformed/nested semantic containers; and raw candidate/preimage materialization
can exceed source size. The 512-size carrier family deliberately demonstrates
the many-to-many output cost and accounts for the suite's elevated local RSS.
No global formal bound is claimed.

## Authority boundary

No 168-source run occurred. No operation ledger, denominator shard, publisher,
launcher, parser, grammar, production file, or compatibility surface was
created or changed. Every broad credit remains zero. This exact subject still
requires two fresh independent exact-byte hostile challenges and root gestalt
before it may seed the next denominator formation.
