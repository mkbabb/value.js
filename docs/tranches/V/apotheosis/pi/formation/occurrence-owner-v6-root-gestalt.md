# V·π occurrence → operation/owner formation v6 — root gestalt

Date: 2026-07-22

**Verdict: `REJECTED`.** Both independent challenges reject the exact V6
subject. Challenge A proves genuine Bikeshed links are erased inside `<code>`,
valid multiline references are erased, mixed Bikeshed/HTML headings create
false ancestry, and owner/code dictionary semantics can be corrupted after
canonical rebinding. Challenge B independently proves a complete authenticated
census of six multiline bibliography and 22 multiline definition omissions.
The failures are promotion-bearing even though replay, stable row IDs, the
declared memory ceiling, and the RED-only authority boundary are mechanically
sound.

## Exact adjudicated set

| object | SHA-256 | bytes | disposition |
|---|---|---:|---|
| manifest | `13c212e58c45f685e2a7cb5a1bd6a63c1f964a318a2701e2912edfdeb414564d` | 20,149 | rejected subject |
| manifest schema | `b3a7131d643b469bdce4b1538ae1946d5cec19b970de71a2026078c9262f53ba` | 11,617 | rejected contract |
| shard schema | `23eaa9f7f7895c4c7c47005e56f06300cf4097a4b3d8ccbbd61c332e15c6dc48` | 13,811 | rejected contract |
| formation receipt | `019d1e1c4f988ec5046700ad44cf82fd6e90df1ffc19984181fed21ddec8097f` | 8,717 | provisional receipt |
| challenge A | `54b847e2054302f6940eb609807c75e73145cbf82027fedd0c16e5d1dfca6008` | 12,626 | `REJECT` |
| challenge B | `bf9aa9249e42df1a27656b470c0d4f767c0573bfd6f30ac0f482dbfb1948ec5f` | 10,279 | `REJECT` |

The frozen content digest is
`af26c705c0d35ef6cd8eff35bd475e70e70e7df58a0d0f0dc886898c28a92235`.

## Root reproduction

Root independently reproduced the three direct discovery failures against the
exact V6 code:

```text
Use <code>{{DOMPointReadOnly/x}}</code>.                         => []
See [[css2/about#property-defs|CSS property\ndefinition conventions]]. => []
Use [=input progress\nvalue=].                                  => []
```

Root also decoded carrier row 2,177 in authenticated
`css-contain-3/Overview.bs`, lines 57–61. It receives the false section stack
`Security Considerations` level 1, lines 37–129, then `Appendix A. Changes`
level 2, lines 42–129. The actual multiline-opening August 2022 `h3` is absent.
Inspection independently confirms that the regeneration set contains only
contexts, carriers, operations, references, and owner scope; `codes` and
`strings` are not regenerated. Owner verification dereferences the input-line
string but neither compares it to the authenticated source line nor hashes it
against the stored SHA-256. Code verification requires only sorted unique
`RED_`-prefixed values, so a different RED-prefixed semantic code can survive.

These reproduce the architecture of challenge A's rebound mutations and the
common-mode discovery omissions. Challenge B's exact source census fixes the
minimum known multiline denominator at six bibliography plus 22 definition
occurrences; none exists in V6.

## Union docket

1. Delimited shorthands are rejected whenever their exact slice contains LF
   or CR. Multiline raw intervals, display text, semantic whitespace, scope,
   target, modifier, and processing context are therefore absent.
2. Blanket opacity for all `<code>` and `<samp>` content contradicts Bikeshed
   shorthand processing. Authenticated `{{DOMPointReadOnly/x}}` and related
   links in CSS Borders 4 are erased even though the official rendered draft
   links them. True raw script/style examples still require exclusion.
3. The heading model combines independently parsed Bikeshed and HTML levels,
   misses multiline HTML opening tags, and consequently creates false
   Security/Changes ancestry and carrier→context edges.
4. Owner-source tuples do not prove that their interned line text equals the
   authenticated source line or that the UTF-8 text hashes to their stored
   SHA-256.
5. The semantic codes dictionary is neither independently rederived nor bound
   to the exact closed RED vocabulary, so status meaning can change while
   indexes and `RED_` prefixes remain valid.

## Narrow V6 facts retained without promotion

- Exact repository and arbitrary-path replay, canonical shards, the truthful
  staged two-rename/TOCTOU disclosures, and independent sub-900 MiB checks
  pass.
- The 168 = 76 + 92 source closure, 14,609,103 source bytes, 311 current
  algorithm-bearing openings including 293 literal `div` openings, 52 exports,
  37 consumer symbols, and ten required RED operation joins remain inputs.
- Existing typed-reference, bibliography-modifier, URL, inherited-scope,
  stable-ID, foreign-key, and zero-credit checks remain useful local evidence.
- Reviewed operations, reviewed owners, owner edges, denominator, parser,
  compatibility, conformance, audit, movement, and production credit are all
  zero.

## Required successor

V7 must parse valid multiline Bikeshed delimiters across LF and CRLF while
preserving exact byte intervals and normalizing only semantic cells. It must
distinguish Bikeshed-processed links inside code spans from true raw code/style
literals, use a single source-ordered heading stack that understands multiline
HTML openings, rederive/canonical-compare source-backed owner text/hash tuples,
and bind the codes dictionary to the exact semantic vocabulary. Exact fixtures,
authenticated censuses, and mutations must cover every confirmed failure.

V7 remains RED-only and must receive two fresh independent challenges plus a
new root gestalt. V6 authorizes no feature row, parser candidate, owner edge,
cost lattice, compatibility or conformance credit, production mutation, or
megatranche execution.
