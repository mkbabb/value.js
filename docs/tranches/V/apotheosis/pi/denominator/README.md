# V·π CSS denominator materialization

**Status: BORN-RED formation tooling; no denominator or parser credit.** This
directory contains offline corpus-indexing tools and generated evidence for the
full July-2026 CSS/owner-experiment feature ledger. Nothing here is imported by
the parse-that runtime.

The current 76-root packet is an authenticated proposed seed, not a normative
operation denominator. The materialization sequence is:

1. enumerate the complete pinned CSSWG Overview-source universe and disposition
   the complement;
2. derive and review normative-reference fixed-point closure;
3. seal owner experiments as a separate exact manifest;
4. extract operation-atomic grammar/prose/algorithm carriers;
5. join the 52-export/37-consumer compatibility obligations and acknowledged
   BBNF ownership DAG;
6. derive feature rows and validate the occurrence↔feature bijection;
7. twice-challenge and gestalt-adjudicate the exact denominator hashes.

`tools/source-universe.mjs` implements only step 1's mechanical enumeration. It
does not classify complement objects and does not infer normative operations.
Its JSON output is formation evidence, never a lexer, token stream, atom layer,
component-value tree, CST, or runtime scanner.

The first generated artifact is `source-universe.json`, raw SHA-256
`c1b823396dcc38607d23243f578e58ef08de79136edd52abd1bbdf6a64f606b7`
and canonical payload digest
`8cabb03dd4d88f4e90d67d5ecce4de1f1a181cc747b6073ab8c7a067fad6ce5f`.
It reproduces 168 authoritative top-level Overview sources at the pinned tree:
76 seed members and 92 explicitly `UNCLASSIFIED_RED` complement objects. This
closes enumeration only; it deliberately does not close any of their semantic
dispositions.
