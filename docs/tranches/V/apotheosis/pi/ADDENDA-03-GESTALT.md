# V·π ADDENDA-03 — root E-3 gestalt adjudication

2026-07-21 · target: `ADDENDA-03.md` SHA-256
`9d8006bb2e6f9d900d00e503d6e8b8fe2e84a730892d4ee558e72eab4ef57f02`
· status: **GESTALT GREEN / OWNER RATIFICATION REQUIRED**

This adjudication is design/prototype governance only. It changes no code,
does not accept W1, does not ratify either `ADDENDA-02` gate, and authorizes no
production execution.

## 1. Challenge disposition

Both fresh independent challenges ACCEPT the exact amended digest. The earlier
REJECT sections remain valid history for the superseded revision and supply no
GREEN credit.

| challenged defect | root disposition |
|---|---|
| R13 left `a/**/b` lowering discretionary | **CLOSED.** The frozen legacy adapter must lower it to a two-keyword space list after PB1; comma/slash adjacency, trailing-empty slash, quoted-comment data, and unterminated-comment results are terminal and executable. |
| Current unterminated-comment evidence was false | **CLOSED.** The sheet now records the fabricated `foo/*` slash lists and the LIVE-only `fn(a/*)` success exactly; PB1 legacy adapters must reject, while L4 recovery remains PB0-owned. |
| R16 omitted `1e%` and a derived boundary bank | **CLOSED.** Both auditors independently reproduced all 21 `B16` sources across scalar/value/values. Classification remains a narrow complete-input legacy ruling, never a whole-sheet rejection rule. |
| R18 lacked exact sources and ASTs | **CLOSED.** `Q(n)` and `L(n,E)` are code-point-defined; Q(1–4), all 16 newline rows, the Q(2)/Q(4) call ASTs, and values wrappers reproduced independently. |
| Formation/W0 receipts were incomplete | **CLOSED.** The served Codex routes are recorded truthfully and the one post-W1 W0 structural replay is already ACCEPTED. |

## 2. Total-tranche optimality

The amended split is the smallest safe boundary:

1. retain D-W1-1–D-W1-8 as repairs under existing Phase-A authority;
2. after explicit owner ratification, allow only R14, R15, narrow R16
   classification/fixtures, and the immediate R18 divergence fixtures in W1;
3. keep R13, R17, and complete R18 preprocessing/token/string semantics RED
   and solely owned by the future shared PB1 scanner;
4. keep W2 responsible only for the color overlay, W5 held on rejected W1,
   PB6 responsible for the complete named-range inventory, and PB12 responsible
   for later typed/rule integration.

Pulling comment, Unicode, escape, or token ownership into W1 would create the
second scanner forbidden by the Phase-B architecture. Deferring all six rows
would leave known W1 semantics vague and make later audits non-falsifiable.
The proposed split avoids both errors and preserves the frozen 52-export,
eight-code, 37-symbol, fail-fast stylesheet, and raw custom-property seams.

## 3. Evidence and residual RED

Fresh challenge evidence records:

- 8 test files; 30 pass; 4 intentional pre-ratification TODOs;
- `npm run check` GREEN;
- declaration/source parity GREEN at 33 types + 19 runtimes;
- dependency audit: zero vulnerabilities;
- full R13 current-oracle probes, 21 × 3 `B16` door probes, Q(1–4), and
  4 backslash counts × 4 newline forms × 3 doors reproduced.

The four TODOs are not feature credit. W1 remains **REJECTED** until owner
ratification, the authorized immediate slice lands, mechanical gates rerun,
and two fresh independent E-1 implementation audits ACCEPT it. R13/R17/full-
R18 remain RED until separately owner-gated PB1 exists. W2's color rows remain
RED until W2.

## 4. Model-routing receipt variance

The addendum writer and Audit A record `gpt-5.6-sol`; Audit B truthfully records
an inherited Codex audit seat whose backend alias was not exposed. No
Opus/Fable-labelled route was available in this Codex runtime. The semantic
challenges are GREEN, but this visible E-5 routing variance is not relabelled or
concealed; the owner may accept the truthful Codex substitution or require the
two challenges to be repeated on the originally named routes before
ratification.

## 5. Root verdict and owner decision

**GESTALT ACCEPT.** The wave is optimal as a provisional pre-PB1 W1 boundary,
the amended correction ledger is complete enough to implement and audit, and
no downstream re-scope beyond the written holds is required.

The owner must now explicitly ratify or reject the five-part authorization in
`ADDENDA-03 §4`, and separately rule on the E-5 route variance above. Silence,
continued Phase-A work, or approval of `ADDENDA-02` does not ratify this sheet.

