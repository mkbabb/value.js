# V·π ADDENDA-06 FOURTEENTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A15

status: `REJECT`

date: 2026-07-21

challenged artifact: current fourteenth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`e57052ddaa53a8bcb1f4849cb850eab61ce9e9a94baa5b680a52e1a9f78751d7`

governing harden raw-byte SHA-256:
`de49b6d0112afec63c53a5a285cd4b9ffaca07f736ebe08ad06212c4f775cfb8`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact fourteenth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B15 seat. I read the current sheet, governing harden chain,
benchmark research, Charter, and Phase-A PI and re-challenged §12H's locator
replacement, report-only neutral execution, AUTHOR_WAVE/final field cuts,
first-anchor proposal/commit/established lifecycle, context-core/scope/chain/
authority provenance, direct/no-plan results, staged matrices, source union,
WAL/statistics ancestry, strict owner gates, and BR-10. No harness, smoke,
pilot, profile, benchmark, measurement, optimization, parser/dependency code,
PB0/PB13P, BBNF coordination, production, inbox, script, or other file changed.
This audit ledger is my only write.

## Current A15 verdict

**REJECT.** Section 12H substantively repairs the A14 surface: it defines one
full group-address binding, gives report-only environments a tagged available/
unavailable execution path, separates author-wave from final-only
compatibility and OC-1 evidence, serializes all eligible first-anchor proposals
before an atomic post-ACCEPT commit, and gives evidence-only recovery an
acyclic context-core → transition-chain → qualification-authority order.
Those are the right refinements.

The exact current bytes nevertheless leave five construction blockers. The
required-result envelope retains the old bare `groupLocatorOrNull` despite the
new binding-only rule. A report-only valid run produces a neutral batch
statistics artifact that the still-exact T9 decision/publication/terminal
schemas do not admit. The new `FIRST_ANCHOR_PROPOSAL` direct source has no exact
versioned source receipt. The final established/measured anchor-mode arrays are
not fields of any versioned final completion or final-environment verdict, so
post-close establishment is not content-addressed into final acceptance.
Finally, the blanket chain-scoped-preflight rule is impossible for synthetic
anchor obligations, especially their environment-unavailable no-plan branch,
because those obligations intentionally have no physical census row or base
peer preflight receipt.

These are receipt-algebra and reachability defects, not requests to weaken the
strict peer objective. Any repair changes normative bytes and requires two new
independent zero-credit challenges. Nothing here authorizes a harness, pilot,
measurement, parser optimization, dependency or pin change, PB0/PB13P, BBNF
exchange, coordination, inbox, script, or production action.

## 1. Total-tranche (gestalt) analysis

The tranche shape is sound. Finite registered peers, independently complete
pairwise workloads, forced class-local peer groups, complete Williams cycles,
one-shot pilot/T9 execution, authority-first deterministic recovery, neutral
report-only evidence, exact stage-due publication, and post-accepted anchor
authority all serve the owner's actual objective: a readable and correct
parser that wins honestly rather than a benchmark-shaped parser.

The remaining faults are at the joins between those layers. A content-
addressed rail cannot rely on prose to decide whether a direct group field was
replaced, which batch-artifact tag a T9 valid terminal may publish, what bytes
constitute a first-anchor proposal source, which final receipt proves the
established-anchor mode, or what peer receipt a synthetic obligation should
name. Those choices affect digests and acceptance. Leaving them to the future
harness would reintroduce exactly the implementation discretion this formation
is meant to remove.

The narrow refinement is exact schema completion. No denominator, corpus,
schedule, threshold, bootstrap population, required-environment conjunction,
owner decision, parser module, or BBNF boundary needs redesign.

## 2. Wave/addendum analysis

The fourteenth amendment correctly discharges the challenged A14 intent:

- §12H.1 forms a unique binding over kind, class, class-local group,
  kind-wide group, partition, and located group digest.
- §12H.2 gives required and report-only scope rows disjoint tagged
  dispositions, preserves one-shot execution, and keeps report-only evidence
  outside every required close predicate.
- §12H.3 gives AUTHOR_WAVE exact `NOT_DUE` cuts for compatibility and OC-1
  adjudication and makes both final-only fields non-empty and conjunctive at
  graduation/perfection.
- §12H.4 makes eligibility and serial registry state total across all
  first-anchor operations and admits no partial post-close commit set.
- §12H.5 removes the context/transition cycle and makes direct/no-plan
  evidence bind the same transition authority as planned execution.

The addendum is still not implementable from its exact schemas because those
new concepts are not carried through every old exact preimage they replace.
The failures below are born-RED schema defects. They are not optional report
polish and cannot be repaired by an implementation convention.

## 3. Feature analysis A15-01 — required result v4 retains the forbidden bare group locator

Section 12H.1 says `groupAddressBindingDigest` is the sole downstream group
address and that a current schema carrying a direct group field replaces the
entire old tuple by that binding. Section 12G's exact required result envelope
version 3 contains this direct field:

```text
requiredEnvironmentIndex,qualificationKind,groupLocatorOrNull,
batchPlanDigestOrEmptyString,...
```

Section 12H.5 then defines required result version 4 by retaining the exact
version-3 fields in order and inserting only
`qualificationAuthorityDigest,terminalEvidenceSupersessionTransitionChainDigest`
after the context. That construction necessarily retains
`groupLocatorOrNull`; it neither replaces it with
`groupAddressBindingDigestOrNull` nor directly binds the new address. Thus the
same section both forbids and republishes a bare locator.

Plan ancestry does not resolve an exact field-array contradiction. Direct
no-plan envelopes require literal null, while execution envelopes need the
one located binding; both cuts can be represented exactly. Advance the current
required result formula so its direct address field is precisely
`groupAddressBindingDigestOrNull`, require non-null exactly for an execution
group and null for direct/no-plan sources, and mechanically prove equality to
the source plan and mapping. Then audit every other current direct address
field against the same replacement rule. Until that field array is exact,
two implementations can emit different valid-looking result bytes.

## 4. Feature analysis A15-02 — a valid report-only T9 terminal cannot publish its neutral artifact

Section 12H.2 correctly defines neutral report-only peer and batch statistics
artifacts and requires a valid report-only result to carry the latter. But the
non-conflicting exact T9 closure law remains narrower:

- §12C.2's closure event is
  `STATISTICS_COMPLETE [batchStatisticsArtifactDigest,prefix]`; and
- §12C.3 states that the `statisticsArtifactDigest` in a VALID T9 decision,
  terminal, execution, history, wave/final, and close artifact is exactly the
  ordinary `batchStatisticsArtifactDigest`, with no substitute.

The new `reportOnlyBatchStatisticsArtifactDigest` has a different tag and a
different terminal state (`REPORT_ONLY_OBSERVATION`, `NO_VERDICT_EFFECT`).
Section 12H versions neither the T9 valid-decision source union nor
`STATISTICS_COMPLETE`, terminal, WAL/execution binding, or disposition
function to admit that artifact under a literal REPORT_ONLY scope. Therefore
the promised report-only run can reach complete raw samples but cannot close
`T9_VALID` without violating the exact valid-artifact law; conversely, using
the ordinary batch artifact would emit the forbidden wave/final state.

Define one exact scope-tagged valid-statistics union and version every affected
decision/publication/terminal/execution preimage: REQUIRED must publish the
ordinary batch artifact and REPORT_ONLY must publish the neutral report-only
batch artifact. The scope disposition and wrapper must select the branch before
T8, and the report-only branch must remain structurally excluded from required
result matrices and verdict predicates. Merely naming the neutral artifact in
the later report-only result does not make the earlier T9 terminal lawful.

## 5. Feature analysis A15-03 — `FIRST_ANCHOR_PROPOSAL` is not an exact source receipt

The older closed source union has an exact
`FIRST_ANCHOR_ESTABLISHMENT source` array. Section 12G changes proposal and
environment-establishment identities. Section 12H then changes the source tag
again to literal `FIRST_ANCHOR_PROPOSAL` and says it binds the chain-scoped
preflight receipt, qualification authority, transition chain, and prospective
proposal receipt.

No exact current source array, version tag, field order, or digest formula is
published for that new tag. It is also unclear which superseded establishment/
environment fields remain after the rename. This violates the otherwise closed
source algebra: `obligationResultSourceDigest` cannot be reproduced from the
sheet, and the required result matrix cannot distinguish two different field
orders that satisfy the prose.

Publish the complete current `FIRST_ANCHOR_PROPOSAL` source array and its
versioned tag. It must bind the canonical synthetic obligation row and mapping,
required-environment receipt, first-anchor absence and semantic establishment,
prospective proposal and environment-proposal receipt, context, qualification
authority, transition chain, and exact no-timing/no-speed state, with no
superseded registry-advance or accepted-establishment descendant.

## 6. Feature analysis A15-04 — established versus measured final anchor mode is not in a final receipt preimage

Section 12H.4 defines useful closed `ESTABLISHED_PAIR` and
`MEASURED_WAVE_GREEN_PAIR` arrays. The established branch must bind the
post-close atomic set, commit receipt, accepted history event, durable
successor prefix, and accepted establishment receipt; the measured branch must
bind the original two WAVE_GREEN rows. Mixing is forbidden.

But no versioned final schema contains either array or a digest of it. The
still-current `benchmarkFinalCompletionMatrixDigest` and
`finalEnvironmentVerdictRow` carry accepted-wave result rows and ancestor
close/history digests only. In the proposal branch those rows still say
`FIRST_ANCHOR_PROPOSAL`; they do not content-address the later
`acceptedFirstAnchorEstablishmentReceiptDigest`, established registry rows,
or the chosen mode. Section 12H.3 versions stage close and perfection close,
but their transitive final verdict/aggregate ancestry cannot bind evidence
that no final completion or final-environment row contains.

Consequently a final serializer cannot prove the established branch, and a
checker cannot tell whether a proposal close acquired the exact post-close
authority before being treated as a discharged anchor. Version the final
completion and final-environment verdict schemas with one exact per-environment
anchor-mode row/digest. Require the established or measured array, its branch-
specific empties, and the applicable post-close ancestry. Carry the resulting
mode vector through final verdict, aggregate, stage close, history, and
perfection. Do not turn `WAVE_ANCHOR_ESTABLISHED` into a speed win.

## 7. Feature analysis A15-05 — blanket chain-scoped preflight provenance is impossible for synthetic no-plan rows

Section 12H.5 says every post-authority direct/no-plan wrapper, source, and
result uses `chainScopedPeerCellPreflightDispositionReceiptDigest`. That
receipt requires a non-null `physicalCensusIndex`, a
`canonicalPeerCensusRow`, and a base peer-cell preflight receipt.

Synthetic fixed/immediate first-anchor obligations deliberately have
`physicalCensusIndexOrNull = null`, map through
`ANCHOR_ESTABLISHMENT_PENDING`, and possess no physical peer row or base
preflight receipt. The strict result branch order also selects
`ENVIRONMENT_UNAVAILABLE` before the proposal branch. Thus a synthetic anchor
in an unavailable required environment must emit a direct no-plan unavailable
source, but the blanket §12H.5 requirement asks it to name a chain-scoped peer
receipt that cannot exist. Inventing a physical row would violate the
first-anchor no-self-comparison law.

Narrow the provenance union exactly. Physical static/no-plan sources must bind
their chain-scoped peer receipt. Synthetic proposal or unavailable sources
must instead bind qualification authority and the transition chain together
with their canonical synthetic obligation row, resolution mapping row,
first-anchor absence receipt, and applicable environment/proposal evidence.
Version the affected source receipts and state the direct-envelope cut. The
common authority and chain remain mandatory; the impossible physical receipt
does not.

## 8. Prior staged-matrix, WAL, statistics, and strict-close recheck

Apart from A15-01 through A15-05, the retained law remains coherent:

- stage-due censuses separate author-wave obligations from final predecessors,
  and accepted-wave plus current-final key sets still form the complete
  obligation×required-environment product without re-emitting wave rows;
- the physical census, first-match terminal partition, globally indexed
  execution cells, multi-class projection, forced 15-peer groups, and exact
  four-string peer rows preserve complete independently owned pairwise
  denominators;
- one create-before-observation pilot and one atomically launched T9 execution
  retain finite phases, sealed action/closure WALs, raw-store corruption
  binding, authority-first deterministic finalization, one global live-detected
  replacement, and no crash/resume or result-conditioned redraw;
- valid required peer statistics remain median matched-block log ratios with
  complete Williams cycles, deterministic paired bootstrap, exact f64/u64
  encodings, `ceil(0.90*finalBlockCount)`, finite rejection exhaustion, and no
  partial batch artifact;
- wave GREEN/AMBER/RED/BLOCKED remains distinct from final FASTER/SLOWER/
  INCONCLUSIVE, and unresolved AMBER debt cannot close perfection; and
- required environments remain conjunctive; semantic, absolute, work/limit,
  resource, allocation/retention/scaling, artifact-retention, compatibility,
  and OC-1 predicates remain controlling. Report-only evidence cannot rescue
  any of them.

No peer may be omitted, no class favorably regrouped, no required environment
pooled, no old loss redrawn, and no benchmark-only parser path introduced.
**GREEN apart from A15-01 through A15-05.**

## 9. Owner and BBNF/uplift boundary

All six §13 owner decisions remain exact and unratified: continuous-rail
authority, strict-close meaning, statistics/lifecycle law, exactly one OC-1A/B
choice, freeze/PB0/retention/artifact-store duty, and uplift authority. Even a
repaired and twice-accepted sheet would authorize only harness construction;
§11 still requires a separately reviewed GREEN freeze packet before a pilot.
PB0 and PB13P retain Gate 1/Gate 2 boundaries.

BR-10 remains conservative and correct. No mutually acknowledged,
content-addressed, reproducible bbnf-lang/parse-that artifact has been admitted
as implemented and hardened, so no uplift may enter, no performance claim may
be imported, and `@mkbabb/parse-that@1.0.0` cannot move. A future uplift still
requires local semantic/no-throw/diagnostic/limit replay, only genuinely
crossable grammar×engine attribution, local evidence, and a separate E-3 pin
decision. Communication remains owner-mediated, acknowledgement-gated,
identity/evidence-only, and non-cross-writing. **GREEN.**

## 10. Exact repair and rechallenge condition

Before another challenge:

1. replace the required result envelope's bare group locator by the exact
   group-address binding and audit every current direct group field against the
   same rule;
2. version a scope-tagged T9 valid-statistics decision/publication/terminal
   union so report-only runs can publish only the neutral batch artifact;
3. publish the exact current `FIRST_ANCHOR_PROPOSAL` source receipt and every
   branch-specific field cut;
4. bind one exact established-or-measured anchor-mode vector into final
   completion, final environment verdicts, and every downstream final/close
   receipt;
5. split direct/no-plan provenance so physical sources use chain-scoped peer
   receipts while synthetic anchor sources use exact obligation/absence/
   authority/chain provenance without inventing a physical peer;
6. preserve the repaired report-only non-rescue law, author/final evidence
   cuts, total atomic commit population, evidence-only byte-identical-scope
   transition chain, and all prior semantic, denominator, Williams, WAL,
   bootstrap, strict-environment, owner, retention, PB0/PB13P, idiomatic-
   grammar/no-fast-path, and BR-10 safeguards; and
7. record the next exact sheet and governing harden digests and obtain two
   fresh independent zero-credit challenges. This rejection grants no
   acceptance credit to later bytes.

Until then, `ADDENDA-06` remains **PROPOSED, NOT RATIFIED**. No benchmark
harness, smoke, pilot, qualification, measurement, optimization, grammar,
dependency, parser pin, PB0/PB13P, BBNF, coordination, inbox, script, or
production action is authorized.

---

## HISTORICAL BOUNDARY — A14 AND ALL EARLIER A REPORTS BELOW ARE ZERO-CREDIT HISTORY

# V·π ADDENDA-06 THIRTEENTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A14

status: `REJECT`

date: 2026-07-21

challenged artifact: current thirteenth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`3ef873828d6a619a872117163a4de821ac3e21fc325c5f41934d8512bfca578f`

governing harden raw-byte SHA-256:
`97315f992a34d288191dd4a240c9e28853b4dbc01baed0423bd84f323d456ff2`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact thirteenth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B14 seat. I read the current sheet, governing harden chain,
benchmark research, Charter, and Phase-A PI and re-challenged multi-class
partitioning and group locators, required/report-only environment scope,
stage-due wave/final matrices and accepted-wave ancestry, the exact version-2
outer/history/close/perfection topology, the post-close first-anchor commit,
all prior source/result/WAL/statistics/strict-owner law, and BR-10. No harness,
smoke, pilot, profile, benchmark, measurement, optimization, parser/dependency
code, PB0/PB13P, BBNF coordination, production, inbox, script, or other file
changed. This audit ledger is my only write.

## Current A14 verdict

**REJECT.** Section 12G materially and correctly repairs A13: it represents
several workload classes per kind, introduces an unambiguous
`[qualificationKind,classIndex,groupIndex]` locator, publishes stage-due
matrices plus a forward-only accepted-wave/final completion receipt, supplies
exact wave/final/aggregate/stage-close/history/perfection schemas, freezes an
exact combined required/report-only scope for no-observation proofs, and moves
first-anchor mutation after an accepted close. The current bytes nevertheless
leave three construction-level choices unresolved.

First, §12G declares the full locator the sole group address but does not
replace or version the exact older downstream preimages that still contain a
bare `groupIndex`. Second, it registers report-only environments and retains
the earlier one-run-per-report-only-environment law, but the only environment-
qualified plan wrapper and result/source algebra require a required-
environment availability receipt; a report-only T7/T8/T9 chain cannot be
formed. Third, the post-close commit is acyclic but optional: the history event
allows zero or more commits and no equality proves that every eligible first-
anchor operation appears exactly once. An accepted first-anchor close can
therefore advance history while permanently omitting, duplicating, or only
partially publishing the registry commits needed by future candidates.

These are identity, environment-topology, and append-only completeness defects.
They do not justify a narrower peer denominator, another draw, a weaker strict
win, or parser work. Any repair changes normative bytes and requires two new
independent zero-credit challenges. Nothing in this report authorizes a
harness, pilot, measurement, optimization, grammar/dependency/pin change,
PB0/PB13P, BBNF exchange, coordination, inbox, script, or production action.

## 1. Total-tranche (gestalt) analysis

The performance rail is now close to the right tranche shape. Its finite
LIVE/deposed/C14 registry, independently complete pairwise semantics, exact
four-string cells, class-local forced groups, balanced matched blocks,
one-shot pilot and qualification WALs, authority-first crash recovery,
valid-only statistics, conjunctive required environments, and semantics-first
strict close are appropriately hard to game. Stage-local publication also
fits the actual prototype sequence: authors can close wave evidence now while
graduation later adds predecessor results without re-emitting or rerunning the
sealed wave anchors. The exact forward history order removes the prior digest
cycle, and prospective first-anchor rows no longer mutate accepted registries
before performance acceptance.

The remaining defects sit at three joins that an implementation must traverse:
the new operation-wide class/group address into old exact run/statistics
schemas; the combined environment scope into an actual report-only run; and
an accepted multi-operation stage close into the complete future anchor
registry. Leaving any of these to harness interpretation would defeat the
sheet's content-addressed, no-redraw purpose. The narrow repair is schema
completion. No workload, Williams, bootstrap, threshold, owner choice, grammar
module, or BBNF boundary needs redesign.

## 2. Wave/addendum analysis

The thirteenth amendment successfully discharges the challenged A13 surface:

- §12G.1 mechanically partitions every executable cell into exactly one
  kind/class/group/peer coordinate and prevents unlike workload classes from
  merging.
- §12G.3 makes author-wave and final obligations due at the correct stage and
  proves their accepted-wave/current-final union is the full obligation by
  required-environment Cartesian product.
- §12G.4 supplies acyclic exact preimages for stage verdicts, operation
  aggregates, stage closes, history events/prefixes, and perfection close.
- §12G.5 keeps first-anchor registry rows prospective until after an accepted
  author-wave close, so a rejected candidate cannot become an accepted anchor.
- The prior exact source union, evidence-blocking precedence, segmented
  one-shot WALs, finite bootstrap exhaustion, required-environment
  conjunction, OC-1A/B, durable retention, and BR-10 uplift quarantine remain
  intact.

The wave still cannot reach implementation because §12G's new locator and
environment objects stop short of all downstream exact preimages, and its
history event does not prove the complete commit population. Those are born-
RED schema defects, not discretionary documentation polish.

## 3. Feature analysis A14-01 — the sole group locator does not reach exact downstream identities

Section 12G.1 says the literal array
`[qualificationKind,classIndex,groupIndex]` is the **sole** group address and
that version-1/bare-group-index addresses are historical only. It advances the
plan to version 5, the group execution digest to version 3, the schedule to
version 3, and activation to version 4. It then says wrappers,
`qualificationId`, run IDs, attempts, WALs, authority, statistics, and terminal
receipts inherit the locator through ancestry.

That transitive sentence does not resolve the exact controlling preimages that
also embed stale direct address fields. Among the still-normative formulas:

- `v-pi-qualification-id/2` directly hashes
  `canonicalGroupPartitionDigest,groupIndex,
  canonicalGroupDigestForGroupIndex` after the plan;
- `v-pi-peer-statistics-artifact/1` and
  `v-pi-batch-statistics-artifact/1` directly hash the same bare group index;
- `v-pi-bootstrap-partial-trace/2` directly hashes that old address; and
- the earlier schedule/statistics/partial-trace families contain corresponding
  direct fields even when plan or activation ancestry is also present.

Section 12G replaces only fields literally named `groupIndexOrNull` in result
envelopes. It neither publishes replacement field arrays/version tags for the
direct `groupIndex` formulas nor says whether the redundant old fields are
removed, replaced by `groupLocator`, or retained with a mechanical equality to
the plan's locator. These choices produce different bytes under purportedly
exact schemas. A class-local index is not an operation-wide address by itself,
and leaving it in a current preimage contradicts the explicit sole-address
rule even when neighboring class/partition bytes could disambiguate it.

Advance every exact downstream family containing a direct group address and
publish its exact field replacement. The simplest consistent rule is to bind
`groupLocator` plus the version-2 kind-class vector (and the located class
partition/group digest where needed), mechanically prove equality to plan v5,
and version every changed preimage. It is not sufficient to say the locator is
available transitively while an exact hash still directly serializes a
different address vocabulary.

## 4. Feature analysis A14-02 — report-only environments have no lawful execution path

The portable-cohort law defines one `qualificationRunId` for **each required
or report-only environment** and says each different environment class may
execute the portable qualification once. Section 12G.2 now correctly gives
report-only classes a registration receipt, a canonical vector, and a unique
combined-scope ordinal; it freezes that scope before T7 and includes every
report-only group/sentinel/namespace key in the no-observation proof.

But the only current environment-qualified wrapper remains the required-
environment schema from §12E.4. It requires:

`requiredEnvironmentAvailabilityVectorDigest`, a required-environment index
`e`, and `requiredEnvironmentAvailabilityReceiptDigest[e]`.

A report-only registration is deliberately absent from that vector and cannot
lawfully supply those fields. Consequently it cannot form the wrapper, current
schedule/verification, T7 admission, activation, T8 attempt, T9 execution,
statistics artifact, or retained report. The result algebra is correctly
limited to required environments for strict close, but no separate report-
only evidence envelope exists either. Plan v5 binding the *combined vector*
does not manufacture an environment-specific run wrapper.

Choose one exact policy before construction. If report-only measurements are
part of the rail, define a disjoint report-only environment wrapper using the
registration receipt, carry it through run/schedule/T7/activation/T8/T9 and a
durable report-only artifact, and prove that artifact never enters a required
matrix, peer-win claim, or verdict recovery. If report-only classes are only
registrations with no observations, retract the earlier per-report-only run
law and state that no T7/T8/T9 object may exist for them. The current sheet
simultaneously promises the run and makes it unrepresentable.

## 5. Feature analysis A14-03 — the post-close anchor commit population is not exact

The new causal direction is correct: proposal → accepted author-wave close →
commit → history event → successor prefix, with future contexts alone
permitted to consume the commit. The exact commit preimage binds the operation,
candidate, accepted close row, proposal, and both proposed registries, so a
rejected close cannot mutate anchor authority.

Completeness is still optional. The history event allows "zero or more"
`orderedFirstAnchorRegistryCommitReceiptDigests`, and §12G.5 says each
first-anchor operation **may** form a commit. There is no frozen eligible-
operation array and no projection equality between that array and the event's
commit array. Thus an accepted stage containing several first-anchor
operations can publish no commits, a strict subset, a duplicate, or a commit
not corresponding one-to-one with its first-anchor close rows while still
matching the prose "operation order." Once its successor prefix is durable,
append-only history supplies no later place to repair the missing atomic
registry advance without inventing another authorization path.

For every accepted `AUTHOR_WAVE` close, derive the exact ordered operation IDs
whose close rows use the two synthetic `ANCHOR_ESTABLISHED` obligations. The
stage event's commit array must equal one valid commit for every and only those
IDs, exactly once and in frozen operation order; it must be literal empty when
the eligible set is empty. Reject duplicates, omissions, foreign rows, mixed
branches, and commits from rejected/non-wave closes. This makes the post-close
advance atomic across the accepted stage and leaves future context authority
total rather than optional.

## 6. Prior source/result/WAL/statistics and strict-close recheck

The prior hardening remains sound and is not reopened by these findings:

- canonical control integers, `u64be` raw counters, `f64be` measured/derived
  scalars, RNG v3, bootstrap seed v5, bounded canonical-decimal rejection, and
  deterministic exhaustion are exact and non-optional;
- one create-before-observation pilot WAL and one atomically launched T9 WAL
  retain closed failure phases, total longest-prefix decisions, one
  live-detected whole-block replacement, authority-before-derived evidence,
  and no action-bearing resume or result-conditioned redraw;
- independently complete pairwise denominators and exact four-string physical
  cells remain visible through terminal and executable partitions;
- valid per-peer statistics precede the batch artifact, decision,
  publication, terminal, execution, result, and close; partial bootstrap
  exhaustion cannot become a valid batch;
- wave GREEN/AMBER/RED/BLOCKED remains separate from final
  FASTER/SLOWER/INCONCLUSIVE; the sole cross-block point estimate remains the
  median paired-log aggregate and required final winning blocks remain
  `ceil(0.90 * finalBlockCount)`; and
- semantic, absolute, deterministic-work/limit, allocation/retention/scaling,
  required-environment, artifact-retention, compatibility, and OC-1 predicates
  remain conjunctive. Report-only observations, once representable, still
  cannot rescue required evidence.

No retry, peer omission, favorable regrouping, environment pooling, fabricated
baseline, or benchmark-only fast path is admitted. **GREEN apart from
A14-01–A14-03.**

## 7. Owner and BBNF/uplift boundary

All six §13 owner decisions remain explicit and unratified: continuous-rail
authority, strict-close meaning, statistics and lifecycle law, exactly one
OC-1 choice, freeze/PB0/retention/artifact-store duty, and uplift authority.
Even a repaired and twice-accepted sheet would authorize only harness
construction; §11 still requires a separately GREEN freeze packet before a
pilot. PB0 and PB13P retain their independent Gate 1/Gate 2 boundaries.

BR-10 remains exact and conservative. No current bbnf-lang receipt identifies
a mutually acknowledged, reproducible artifact whose status is implemented
and hardened, so no uplift is admissible and the `@mkbabb/parse-that@1.0.0`
pin cannot move. A future uplift still needs local semantic/no-throw/diagnostic/
limit replay, crossable grammar×engine attribution, local profiles and
performance evidence, and a separate E-3 pin decision. Communication remains
owner-mediated, acknowledgment-gated, identity/evidence-only, and non-cross-
writing. **GREEN.**

## 8. Exact repair and rechallenge condition

Before another challenge:

1. propagate the full group locator into every exact downstream identity,
   statistics, partial-trace, and terminal family that still directly hashes
   a bare group index; publish/version the exact replacement preimages;
2. define a complete report-only execution-and-artifact path or explicitly
   prohibit/retract report-only execution, while preserving absolute
   non-rescue of required close;
3. make the post-accepted-close commit array the exact duplicate-free
   projection of every and only eligible first-anchor operation in the stage;
4. retain all repaired §12G stage matrices, accepted-wave ancestry,
   forward-only close/history/perfection schemas, combined no-observation
   scope, and first-anchor proposal-before-close direction unchanged;
5. preserve every prior semantic, peer, workload, Williams, WAL, authority,
   statistics, no-retry, environment, artifact-retention, OC-1, PB0/PB13P,
   idiomatic-grammar/no-fast-path, and BR-10 safeguard; and
6. record the new sheet and governing harden digests and obtain two fresh
   independent zero-credit challenges. This rejection gives later bytes no
   acceptance credit.

Until then, `ADDENDA-06` remains **PROPOSED, NOT RATIFIED**. No benchmark
harness, smoke, pilot, qualification, measurement, optimization, grammar,
dependency, parser-pin, PB0/PB13P, BBNF, coordination, inbox, script, or
production action is authorized.

---

## HISTORICAL BOUNDARY — A13 AND ALL EARLIER A REPORTS BELOW ARE ZERO-CREDIT HISTORY

# V·π ADDENDA-06 TWELFTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A13

status: `REJECT`

date: 2026-07-21

challenged artifact: current twelfth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`e2fdc3768716d45e4851c173314307e82b5cd24f6b960ec1b13326db0d0a17fe`

governing harden raw-byte SHA-256:
`bbb3b7138ffd4ececc6fc7e06a943069a9103a6350131ee2f19ff4eae9a07c79`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact twelfth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B13 seat. I read the current sheet, governing harden amendment and
retained prefix/statistics/role/environment/version law, benchmark research,
Charter, and Phase-A PI; challenged §12F's obligation and execution ordinals,
kind/group/peer mappings, ten-tag source union, obligation×environment matrix,
first-anchor establishment, external-peer precedence, operation-wide ten-
namespace no-observation recovery, strict owner decisions, and BR-10. No
harness, smoke, pilot, profile, benchmark, measurement, optimization,
parser/dependency code, PB0/PB13P, BBNF coordination, production, inbox,
script, or other file changed. This audit ledger is my only write.

## Current A13 verdict

**REJECT.** Section 12F materially repairs the A12 defects: obligations and
execution cells now have global ordinals, executable work maps exactly into
same-kind groups, the ten source tags distinguish every terminal path, result
rows cover the obligation×required-environment product, evidence blocking
precedes measured non-wins, and evidence-only repair is operation-wide and
pre-observation. The repaired bytes still do not form an executable continuous
rail. First, §12E explicitly retracts every version-1 history/wave/final/
aggregate/perfection schema, while §12F publishes only the version-2 result-
envelope formula and merely says the remaining envelopes "advance" without
giving their exact hash preimages. Second, one purported complete result matrix
contains both author-wave anchor obligations and graduation-only predecessor
obligations, but the ten-source union has no lawful representation for final
obligations that are intentionally not yet run at author-wave close. The wave
receipt therefore cannot be formed without either structural RED or executing
the final tournament early. Third, first-anchor establishment advances the
accepted-anchor registries after semantic GREEN but before a successful
performance close, so a later required-environment/resource/absolute failure
can leave an unaccepted candidate in append-only anchor history.

These are content-addressing and stage-order blockers, not invitations to relax
strict wins, rerun a failed draw, or change parser code. This verdict grants no
harness, pilot, benchmark, measurement, optimization, grammar, dependency,
pin, PB0/PB13P, BBNF, coordination, inbox, or production authority. Any
normative repair invalidates this report and requires two fresh independent
zero-credit challenges.

## 1. Total-tranche (gestalt) analysis

The twelfth amendment is directionally optimal. The kind-free physical census
preserves every predecessor and anchor role, same-kind byte-identical work is
deduplicated without shrinking its logical obligations, and the exact
execution→group→peer mapping prevents favorable regrouping. The closed
source union correctly distinguishes static terminal evidence, unavailable
environments, T7 admission failure, one-shot pilot invalidity, ordinary T9
invalidity, bounded-rejection exhaustion, current wave/final statistics,
retained wave evidence, and non-comparative first-anchor establishment. The
operation-wide recovery cut is substantially stronger than the rejected
affected-cell proof: its common snapshot, ten authoritative namespaces, local
cross-product rows, global prefix proofs, and one append-only transition chain
close the redraw loophole.

The remaining defects are at the seam between those correct local objects and
the continuous tranche timeline. A proof rail needs an exact accepted wave
receipt now and an exact graduation receipt later. The current sheet instead
requires one already-complete cross-stage matrix while withholding the final
work until W7, then removes the exact outer-envelope schemas that could have
defined staged publication. It also mutates accepted-anchor registries before
the same proof says the candidate is accepted. Those gaps can make a lawful
wave uncloseable or make a failed candidate authoritative downstream.

The narrow remedy is a forward-only two-stage result topology plus an exact
post-close anchor commit. No corpus, peer set, grouping, Williams schedule,
bootstrap method, threshold, no-retry law, or grammar boundary needs to change.

## 2. Wave/addendum analysis

The current sheet discharges the twelfth-repair themes in substance:

- §12F.1 assigns every logical obligation and executable cell one stable
  operation-wide ordinal and proves exhaustive obligation→resolution and
  execution→group/peer mappings.
- §12F.2's ten tags and first-applicable branch order cover all static,
  unavailable, pre-T7, pilot, T9, statistics, retained, and first-anchor
  sources without inventing valid statistics or a retry.
- The complete-matrix row count and key projection prevent dropping an
  obligation or required environment once the complete matrix exists.
- §12F.3 gives evidence blocking precedence over SLOWER/INCONCLUSIVE and
  leaves N/A/incomparable predecessors visible without manufacturing a win.
- §12F.4 proves the complete old operation context unobserved across all
  kinds, groups, required/report-only environments, ten local namespaces, and
  ten global prefixes before an evidence-only terminal repair.
- Immutable P-3 quarantine, UTF-8 qualification, independently complete
  pairwise denominators, exact four-string cells, forced 15-peer groups,
  complete Williams cycles, median paired-log statistics, one-shot T8/T9,
  bounded RNG exhaustion, required-environment conjunction, OC-1A/B, durable
  retention, strict semantic-first close, and BR-10 remain intact.

The addendum nevertheless fails its close criterion because it has neither an
exact current outer receipt topology nor a lawful stage-local result
population. The first-anchor registry mutation compounds that stage defect.

## 3. Feature analysis A13-01 — the current history and close envelopes have no exact schemas

Section 12E.4 now says that §12F retracts **all** old result, history, wave,
final, aggregate, and perfection schemas and that the version-1 bytes cannot be
serialized or used as an alternate. Section 12F supplies an exact
`benchmarkQualificationResultDigest` version-2 array. It does not supply an
exact array or digest formula for any of these current objects:

- `benchmarkHistoryDigest` version 2;
- the current benchmark wave receipt;
- the current benchmark final receipt;
- the current benchmark aggregate verdict; or
- the current benchmark perfection close.

Instead it says history "advances to version 2," says wave/final/close data are
mechanical filters with expected key/cardinality arrays, and says aggregate and
perfection bind history transitively. The owner-decision text then names
"version-2 forward-only history/wave/final/aggregate/perfection envelopes" as
if exact schemas existed. No version tag, field order, preimage, digest
formula, or exact expected-key/cardinality-array placement exists for those
five objects.

Consequently two implementations can hash different history/close objects
while each claims the same complete matrix; a root cannot content-address the
accepted filter proof; and the transition chain, external-peer claim vector,
anchor rows, semantic/absolute/resource receipts, OC-1 choice, and operation/
tranche verdict have no single current outer envelope. This directly violates
the sheet's own canonical-byte and retention law.

Publish the exact version-2 hash preimage for every retracted outer object,
including the complete matrix, ordered result-envelope digests, transition
chain, exact filter-key/cardinality proofs, environment-close rows, anchor
fields, external-peer state, non-peer safeguards, semantic/absolute/work/
allocation receipts, OC-1 receipt, artifact store, and final verdict in one
acyclic order. Version-1 bytes must remain historical only.

## 4. Feature analysis A13-02 — the complete matrix is impossible at author-wave close

The obligation projection intentionally contains both:

- fixed/immediate anchor obligations with `qualificationKind="wave"`, due at
  author-wave acceptance and later retained; and
- registered-predecessor obligations with `qualificationKind="final"`, due
  only at W7/PB13P/perfection.

Section 12F.2 nevertheless requires one result for **every obligation in every
required environment** and calls any missing evidence structural RED. At an
ordinary author-wave close, the final predecessor groups have intentionally
not reached T7, T8, or T9: BR-7 expressly reserves LIVE/deposed/C14 tournaments
for W7. None of the ten source tags represents a lawful not-yet-due final
obligation. `T7_RESOURCE_BLOCKED` cannot be used: its exact source requires a
real resource-admission failure receipt, and lack of a not-yet-scheduled T7
attempt is not such a failure. A static terminal applies only to the frozen
preflight terminal mapping; environment unavailability requires its own
preflight result; and valid/invalid final statistics require the forbidden
early execution.

Therefore the author-wave matrix must either omit final rows (violating the
Cartesian cardinality), fabricate T7 failure evidence, or run the final
tournament before W7. Deferring the matrix until W7 also fails: the exact wave
receipt needed to accept and retain the candidate at author close would not
exist, defeating the continuous rail and immediate-anchor history.

Define a staged, forward-only topology. One safe shape is an exact wave matrix
over every and only due wave obligations, followed at graduation by an exact
completion matrix that binds the immutable accepted wave receipt/rows and adds
every final obligation. Both need exact due-key/cardinality projections; final
close must prove the completed union equals the full obligation×environment
product. A pending/not-due token must not count as evidence, discharge an
obligation, survive final completion, or hide a peer.

## 5. Feature analysis A13-03 — first-anchor registry advancement precedes acceptance

In first-anchor mode, §12F.2.1 forms establishment after semantic GREEN and
then forms `firstAnchorRegistryAdvanceReceiptDigest` whose next fixed and
immediate registries are the candidate's unique append-only advances. The
environment establishment result consumes that advance receipt, and the same-
candidate close consumes the resulting `ANCHOR_ESTABLISHED` rows. No current
schema distinguishes a prospective registry digest from a committed accepted-
anchor registry, and no post-close commit receipt binds a successful wave or
operation verdict before the next registries become authoritative.

This ordering is unsafe. A candidate can pass semantics, create the append-
only advance, and then fail a required environment, T7 resource condition,
absolute-performance clause, deterministic-work/limit gate, allocation/
retention/scaling gate, or other mandatory close predicate. It is not an
accepted bundle, yet later candidates can consume its advanced registries as
genuine prior accepted anchors. Because the advance is append-only, the sheet
provides no lawful rollback.

Keep the establishment and next-registry rows prospective while evaluating the
same-candidate non-comparative close. After every required wave-acceptance
predicate is GREEN, append one exact acceptance/registry-commit receipt that
binds the successful wave/operation receipt and atomically advances both
registries. Only that committed digest may seed a later candidate. This keeps
the chain acyclic: the close binds the proposal, and the post-close commit
binds the close; the close must not depend on its own committed registry.

## 6. Prefix, statistics, role, environment, and recovery recheck

Apart from the cross-stage blockers above, the retained mechanical law is
coherent:

- numeric control values remain safe JSON integers; observed counters and
  derived scalars remain exact `u64be`/`f64be` strings;
- complete Williams cycles, common pilot `N`, paired log orientation, the
  sole median-log aggregate, exact final winning-block count, finite bounded-
  rejection RNG, and valid-only peer/batch statistics remain controlling;
- pilot and T9 are one-shot, authority-first, segmented-WAL executions with
  deterministic closure and no action-bearing crash resume or result-
  conditioned redraw;
- a multi-role physical baseline is deduplicated only within one byte-identical
  same-kind execution cell; predecessor-final and anchor-wave obligations stay
  logically distinct;
- required environments remain independently reported and conjunctive;
  report-only vectors cannot rescue RED or BLOCKED evidence; and
- §12F.4's ten local namespaces plus ten global prefix proofs, one immutable
  store cut, complete kind×group/sentinel×environment coverage, changed-only
  repair rows, and one identically bound append-only transition chain close
  the prior affected-cell redraw gap.

No defect found here justifies a retry, alternate seed, smaller denominator,
peer omission, pooled environment, valid statistics on invalid evidence, or a
benchmark-only fast path.

## 7. Owner, BBNF, and authority boundaries

All six owner decisions remain necessary, including exact continuous-rail and
strict-close law, the statistics/one-shot topology, one explicit OC-1A/OC-1B
choice, the T0–T9 freeze and owner-named durable artifact store, and separate
uplift authority. The owner text must be updated to name the repaired staged
matrix and exact current outer-envelope versions; it cannot ratify schemas that
do not exist.

BR-10 remains correct and blocked. No parse-that uplift is admissible until
both tasks acknowledge the same content-addressed package/commit/API/batch as
implemented and hardened, every claimed grammar×engine cell genuinely crosses
without a semantic shim, local semantic/performance rails pass, and a separate
E-3 pin decision lands. The active BBNF task has acknowledged no such artifact.
Nothing in this rejection opens coordination, authorizes source inference,
accepts remote measurements as local proof, or changes the 1.0.0 pin.

## 8. Required repair before a fresh challenge

1. Publish exact version-2 history, wave, final, aggregate, and perfection
   schemas in one acyclic chain; bind the complete matrix, ordered result
   envelopes, exact filter key/cardinality proofs, transition chain, environment
   close/peer state, anchors, semantic/absolute/work/allocation gates, OC-1,
   retention store, and operation/tranche verdicts.
2. Split result publication by due stage, or provide an equally exact staged
   topology: author-wave closure covers every wave obligation now; graduation
   binds those immutable rows and completes every final obligation; the final
   union must equal the full obligation×required-environment product exactly.
3. Make first-anchor registry rows prospective until a post-close atomic
   commit binds a successful acceptance receipt; only the committed dual-
   registry advance may become a genuine later anchor.
4. Update §13 owner choices and §14 sequence to the exact repaired versions
   and stage order. Do not permit a pending row to discharge evidence, execute
   final peers early, or allow an unaccepted candidate into anchor history.
5. Preserve without weakening every accepted semantic, denominator, grouping,
   Williams, statistics, numeric, authority, segmented-WAL, one-shot,
   environment, no-redraw, OC-1, modularity, anti-fast-path, retention, PB0/
   PB13P, BBNF, pin, and production boundary.
6. Restart both independent zero-credit challenges on the new exact addendum
   and harden digests. Root gestalt and all six owner decisions remain
   downstream; no harness, pilot, or measurement is permitted.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 ELEVENTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A12

status: `REJECT`

date: 2026-07-21

challenged artifact: current eleventh-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`d282f72206facfe6905ea93141191e7c350f88210324e5d78fd84445fbac48e0`

governing harden raw-byte SHA-256:
`4e6651f4d37e3b49549fe6195c8d544a2fe1f8fb09db9f13c2559973e23ca2ff`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact eleventh-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B12 seat. I read the current sheet, complete governing harden chain,
benchmark research, Charter, and Phase-A PI; challenged the exact §12E
transposition, kind-free physical census, role obligations and result
deduplication, comparable-only/no-qualified-peer close, required-environment
union/vector/wrapper, plan-v4/schedule-v2/verification-v2/activation-v3 chain,
fresh downstream version-1 schemas, evidence-only terminal supersession, and
all retained semantic/statistical/one-shot/owner/BBNF law. No harness, smoke,
pilot, profile, benchmark, measurement, optimization, parser/dependency code,
PB0/PB13P, BBNF coordination, production, inbox, script, or other file changed.
This audit ledger is my only write.

## Current A12 verdict

**REJECT.** Section 12E materially repairs A11: it now creates one physical
census, projects predecessor-final and anchor-wave obligations, defines exact
required-environment receipts, and publishes concrete plan/schedule/activation
and close envelopes. But its new result algebra is not total over outcomes the
same governing sheet requires. A group that cannot satisfy T7 resource
admission, a terminal `PILOT_INVALID`, or a terminal `T9_INVALID` has no allowed
`canonicalObligationResultRow` source/state pair, even though earlier
controlling law maps those cases to `WAVE_BLOCKED` or final evidence-BLOCKED.
The external-peer aggregate then recognizes only static artifact/ABI/
denominator blocking and can label an unavailable or invalid executable peer
`COMPARABLE_PEER_NOT_BEATEN` instead of evidence-BLOCKED. The repaired topology
also leaves `obligationIndex`, first-anchor establishment, and the exact
cross-product/application proof for evidence-only supersession undefined.
These are normative closure gaps: a conforming run can reach a lawful state
that the content-addressed matrix cannot encode.

This verdict applies only to the challenged bytes. It grants no harness,
pilot, benchmark, measurement, optimization, grammar, dependency, pin,
PB0/PB13P, BBNF, coordination, inbox, or production authority. Any normative
repair invalidates this report and requires two fresh independent zero-credit
challenges.

## 1. Total-tranche (gestalt) analysis

The eleventh amendment is directionally optimal. One kind-free physical row
for a baseline with several roles avoids duplicate registry claims, while the
role projection correctly keeps historical predecessors out of author-wave
tournaments and retains anchor debt at graduation. The AVAILABLE/UNAVAILABLE
tagged union is timing-independent, the environment wrapper is forward-only,
and plan v4, schedule v2, verification v2, and activation v3 repair the stale
version topology. The no-qualified-peer operation path also correctly refuses
to manufacture a faster-than-LIVE claim.

The remaining defects are at proof-totality altitude. The tranche promises a
complete matrix that distinguishes performance loss from missing or invalid
evidence. Its exact version-1 downstream schemas omit the very invalid/
pre-activation branches created by the one-shot pilot, T9, and resource laws.
That can turn a runner failure into a performance loss, or prevent history and
close from being formed at all. Likewise, an initial door cannot become an
established anchored door unless an exact establishment result exists, and an
evidence-only repair is not mechanically pre-observation if the absence proof
does not bind the complete affected kind×environment set.

The narrow repair is to complete the downstream state algebra and indexing,
not to weaken strict wins, rerun failures, add peers, or simplify away the
one-shot rail. No parser, grammar, workload, threshold, Williams schedule, or
bootstrap method needs to change.

## 2. Wave/addendum analysis

The current sheet discharges A11's named repair themes in substance:

- §12E.1 gives every physical role exactly one wave/final obligation and
  deduplicates only byte-identical same-kind work.
- §12E.3 defines a retained AVAILABLE/UNAVAILABLE evidence union and vector,
  separates class availability from per-group T7 resource admission, and
  quarantines report-only environments.
- §12E.4 publishes exact plan v4, environment wrapper v1, schedule v2,
  verification v2, activation v3, result/history/wave/final/aggregate/close
  envelopes, and the intended forward-only chain.
- §12E.2 retains literal terminal non-wins, blocks required artifact/ABI/
  denominator failures, makes anchor N/A/incomparability RED, and allows a
  no-qualified-peer operation close only through the full semantic, anchor,
  absolute, resource, scale, and environment conjunction.
- Complete pairwise denominators, exact peer rows, forced 15-baseline groups,
  complete Williams cycles, median paired-log statistics, one-shot T8/T9,
  no result-conditioned redraw, OC-1A/B, and acknowledgement-gated BR-10 remain
  intact.

The addendum nevertheless fails its own exact-close criterion because its
new canonical result rows cover only valid statistics, retained valid wave
statistics, static preflight terminals, and pre-T7 environment unavailability.
They do not cover later evidence blocking or first-anchor establishment. The
close algebra and supersession proof consequently remain implementation-
selected on reachable states.

## 3. Feature analysis A12-01 — lawful blocked executions have no canonical result row

The controlling law deliberately creates these non-statistical outcomes:

1. §12B.3 says failure to produce
   `statisticsResourceAdmissionReceiptDigest` leaves the group **T7 BLOCKED**
   and creates no T8 attempt.
2. BR-6/§§12A–12C say `PILOT_INVALID` maps every affected cell to `INVALID`,
   `WAVE_BLOCKED`, or final evidence-BLOCKED and creates no T9 execution.
3. A terminal `T9_INVALID`, including deterministic bootstrap rejection
   exhaustion or crash/incomplete, likewise maps every affected cell to
   `INVALID`, `WAVE_BLOCKED`, or final evidence-BLOCKED and publishes no valid
   peer/batch statistics artifact.

Section 12E.4 then closes `canonicalObligationResultRow` to exactly five
source/state families: `WAVE_STATISTICS` with GREEN/AMBER/RED;
`FINAL_STATISTICS` with FASTER/SLOWER/INCONCLUSIVE;
`RETAINED_WAVE_STATISTICS`; `STATIC_TERMINAL`; or
`REQUIRED_ENVIRONMENT_UNAVAILABLE`. It explicitly points statistics sources to
§12C.3's **valid-only** artifacts. There is no source for T7 admission
failure, pilot invalidity, T9 invalidity, `INVALID`, `WAVE_BLOCKED`, or final
evidence-BLOCKED.

This is not a cosmetic missing label:

- T7 BLOCKED has no environment-qualified activation or T9 receipt and is not
  a static terminal or unavailable-environment row, so it can enter neither
  `benchmarkQualificationResultDigest` nor the narrowly named no-launch
  terminal/unavailable history array.
- `PILOT_INVALID` has activation/pilot evidence but no T9 execution receipt,
  while `benchmarkQualificationResultDigest` requires a non-empty
  `t9ExecutionReceiptDigest`.
- `T9_INVALID` has an execution receipt but no valid §12C.3 statistics source,
  so none of its obligation rows can use an allowed pair.

Thus a conforming one-shot failure necessarily yields either a missing result
row (which §12E calls structural RED), an invented source tag, or laundering
invalid evidence into a valid statistics state. Define exact canonical source/
state rows and no-launch/invalid result envelopes for T7 BLOCKED,
`PILOT_INVALID`, and `T9_INVALID`, binding their actual admission, panel,
terminal, WAL, and execution receipts as applicable. They must map to
`WAVE_BLOCKED` or final evidence-BLOCKED without opening another attempt or
creating a valid statistics artifact.

## 4. Feature analysis A12-02 — peer-claim precedence misclassifies environment and execution blocking

Section 12E.2 derives `EXTERNAL_PEER_EVIDENCE_BLOCKED` only when a registered
predecessor's **static physical disposition** is
`UNAVAILABLE_ARTIFACT`, `UNSUPPORTED_ABI`, or
`NO_COMMON_DENOMINATOR`. Its next row says any executable predecessor not
final `FASTER` is `COMPARABLE_PEER_NOT_BEATEN`.

That ordering has no branch for an executable predecessor whose required
environment is `UNAVAILABLE_ENVIRONMENT`, whose group is T7 BLOCKED, or whose
pilot/T9 terminal is `INVALID`. Each is executable in the physical partition
but cannot have final `FASTER`. Literal evaluation therefore selects
`COMPARABLE_PEER_NOT_BEATEN`, a performance non-win, even though BR-6,
§12B.3, §12E.3, and the final-state table require evidence-BLOCKED. The
required-environment peer-claim aggregate propagates the same wrong category,
so the exact close can report performance RED where the evidence verdict must
be BLOCKED.

Extend the strict precedence to consume the canonical obligation result for
required-environment unavailability, T7 blocking, and pilot/T9 invalidity
before testing `FASTER`. `COMPARABLE_PEER_NOT_BEATEN` must apply only to an
eligible **valid final-statistics** row whose state is SLOWER or INCONCLUSIVE,
not to absence or invalidity of evidence. Report-only classes remain excluded.

## 5. Feature analysis A12-03 — role-result indexing and first-anchor establishment are undefined

Section 12E.1's exact obligation row has no `obligationIndex`; it is the
five-field array `[physicalIndex,role,kind,stage,stateFamily]`, sorted by
physical index and role order. Section 12E.4 nevertheless adds an
`obligationIndex` field to every canonical result row and sorts by it without
defining whether it is a global projection index, a per-physical-role ordinal,
or another value. A fixed+immediate multi-role physical row therefore has two
distinct logical result obligations but no canonical numeric key for them.
Two implementations can hash different valid-looking result arrays while both
claiming the same role projection.

The state family also omits the established-new-door case. BR-6 expressly
says a new door **establishes its anchor and makes no predecessor claim**. Such
a door has no prior fixed/immediate comparison and therefore no §12C.3 wave
statistics state. Yet §12E.2 and the close row require both fixed and immediate
anchor digests/states, and the allowed result pairs contain no
`ANCHOR_ESTABLISHED` source/state or canonical establishment receipt. Treating
the candidate as its own baseline would be a contrived comparison and would
contradict the no-predecessor-claim rule.

Define `obligationIndex` mechanically in the role-projection artifact (or
remove it and use the already unique `[physicalIndex,role]` key), bind the
exact obligation row in each result, and add one non-comparative first-anchor
establishment receipt/state with exact later fixed/immediate registry effects.
It must not emit GREEN/FASTER, clear unrelated debt, or avoid the two-anchor
law once a genuine prior accepted baseline exists.

## 6. Feature analysis A12-04 — evidence-only supersession lacks a total absence/application proof

Section 12E.5 correctly confines terminal reconsideration to exact artifact
recovery or correction of retained pre-observation ABI evidence and uses the
acyclic order new context → transition → plan. But the exact authorization
contains only an opaque `noAffectedObservationProofDigest`. No schema binds:

- every affected logical obligation kind on the physical row;
- every required and report-only environment under the old context;
- the old pilot-attempt and T9-execution keyspaces/action-store roots whose
  nonexistence is being proved; or
- the frozen snapshot/cut at which absence was checked.

The plan-v4 field `orderedAppliedTerminalEvidenceSupersessionTransitionDigests`
also has no exact membership, sort, deduplication, or relationship to
`benchmarkHistoryDigest`'s differently named
`orderedTerminalEvidenceSupersessionTransitionDigests`. Consequently an
implementation can omit the transition from one affected group, apply it to
only one kind/environment, reorder it, or claim absence over a partial store
without violating any stated byte formula.

Define a content-addressed no-observation census over the complete affected
`kind×environment` obligation set and the exact durable attempt/execution store
snapshot; require one proof row per member. Define the canonical applied-
transition set, order, deduplication, group coverage, and equality/projection
law into history. Preserve the existing prohibition after any affected T8
attempt or timing observation and retain every old context, receipt, and
history byte.

## 7. Owner, BBNF, and authority boundaries

All six owner decisions remain necessary, including the mutually exclusive
OC-1A/OC-1B choice, exact artifact-store selection, strict peer/non-peer close,
and the corrected plan-v4/schedule-v2/verification-v2/activation-v3 topology.
The repair must keep semantics subordinate to G-2/PB13G and keep PB0/Gate 2
separate. Owner ratification may authorize prototype harness construction only;
the later T0/T1 GREEN freeze still precedes every pilot or measurement.

BR-10 remains correct and blocked: no parse-that uplift is admissible until
both tasks acknowledge the same content-addressed package/commit/API/batch as
implemented and hardened, every claimed local grammar×engine cell is genuinely
crossable without a semantic shim, local semantic/performance rails pass, and
a separate E-3 pin decision lands. The active BBNF task has acknowledged no
such artifact. Nothing in this rejection opens coordination, authorizes source
inference, accepts remote measurements as local proof, or changes the 1.0.0
pin.

## 8. Required repair and disposition

1. Add exact result-source/state families and envelopes for T7 admission
   BLOCKED, `PILOT_INVALID`, and `T9_INVALID`; bind their real evidence and map
   them only to `WAVE_BLOCKED` or final evidence-BLOCKED without retry.
2. Make external-peer precedence consume all evidence-blocked obligation
   states before valid final performance states; environment unavailability or
   invalid execution must never become `COMPARABLE_PEER_NOT_BEATEN`.
3. Define the role obligation index/key exactly and add a canonical
   non-comparative first-anchor establishment result consistent with BR-6.
4. Make terminal-evidence supersession prove absence over the complete affected
   kind×environment/store snapshot and define exact transition-set ordering,
   coverage, and history projection.
5. Preserve the kind-free census, static first-match partition, exact
   environment union/vector/wrapper, plan-v4/schedule-v2/verification-v2/
   activation-v3 chain, valid-only statistics, pairwise denominator ownership,
   one-shot/no-redraw law, strict peer/no-peer close, owner choices, and every
   BBNF/production boundary.
6. Restart both independent zero-credit challenges on the new exact addendum
   and harden digests. Root gestalt and all six owner decisions remain
   downstream; no harness, pilot, or measurement is permitted.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 TENTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A11

status: `REJECT`

date: 2026-07-21

challenged artifact: current tenth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`e0ae5c58f539f94df8d278aec9f3893431aa100af570a8b49fabf5da80396a9a`

governing harden raw-byte SHA-256:
`a749362f4f5d7439850fc4ab92eb72c39e73d3fa96c781ee0e312e198bc8d6ee`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact tenth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B11 seat. I read the current sheet, complete governing harden chain,
benchmark research, Charter, and Phase-A PI; challenged the exact §12D
transposition, outcome-free census, first-match terminal/executable partition,
empty-executable-set law, qualification-kind and role routing, required-
environment preflight, executable-only grouping, downstream binding, strict
terminal mapping, §12C valid-statistics compatibility, prior receipt/state/
statistics law, owner decisions, and BBNF uplift boundary. No harness, smoke,
pilot, profile, benchmark, measurement, optimization, parser/dependency code,
PB0/PB13P, BBNF coordination, production, inbox, script, or other file changed.
This audit ledger is my only write.

## Current A11 verdict

**REJECT.** Section 12D now partitions every census row exactly once into a
static terminal disposition or an executable row, and it correctly excludes
terminal rows from §12C's valid-only statistics. But the repaired law still
lacks a total executable proof topology in three places. First, it merges
predecessor and anchor roles and then places every executable census row into
every required `qualificationKind`, while the governing schedule requires
historical predecessor tournaments only at W7/final and requires anchor debt
to retain the wave disposition. The sheet defines no outcome-free role×kind
activation projection, so literal execution either runs the wrong tournament
and assigns the wrong state family or silently filters rows contrary to the
complete-projection law. Second, the required-environment `AVAILABLE` /
`UNAVAILABLE_ENVIRONMENT` rows are called receipts but have no canonical
envelope, digest formula, evidence union, or vector binding. Third, the
history/wave/final/aggregate/close append rule names no exact prior envelopes
or canonical matrix row, and the owner-decision text still asks for an
activation receipt version 2 after §12D advances the controlling version-2
activation to version 3. These gaps leave grouping, environment conjunction,
and strict close implementation-selected rather than mechanically reproducible.

This verdict applies only to the challenged bytes. It grants no harness,
pilot, benchmark, measurement, optimization, grammar, dependency, pin,
PB0/PB13P, BBNF, coordination, inbox, or production authority. Any normative
repair invalidates this report and requires two new independent zero-credit
challenges.

## 1. Total-tranche (gestalt) analysis

The tenth amendment is a material improvement. The complete census is
outcome-free; terminal evidence cannot disappear merely because a subject
cannot run; the six-way first-match partition is static; executable peers alone
enter equal-work groups; `q=0` creates no fictitious Williams run; and the
terminal table refuses to convert absence, incomparability, or an empty
denominator into a peer win. That closes A10's central defect without weakening
semantic-first eligibility or §12C's valid-only statistics.

The remaining faults sit at tranche altitude because the rail's purpose is a
proportionate continuous proof followed by a complete, reproducible close.
Running LIVE/deposed/C14 tournaments at every feature wave contradicts BR-7's
cost boundary and E-2; applying wave states to predecessor roles or final states
to anchor-debt roles contradicts BR-6/BR-9. Allowing an implementation to omit
those rows instead would violate the new no-drop partition. Likewise, a prose
environment status and an unspecified close envelope cannot prove the
Charter's pinned-environment, complete-matrix claim. These are proof-law
defects, not invitations to weaken strict peer beating or add process ceremony.

The optimal repair is narrow: retain the complete census and static
dispositions, add exact pre-observation role/kind and environment projections,
and publish one exact close-matrix row/envelope that consumes them. No parser,
grammar, benchmark harness, or performance threshold needs to change.

## 2. Wave/addendum analysis

The current sheet discharges the named A10 repair themes in substance:

- §12D.1 retains every registered predecessor and required anchor exactly once,
  merges duplicate roles deterministically, and keeps terminal rows free of an
  invented denominator.
- §12D.2's first-match order is exhaustive and timing-independent. The fixed-
  field preflight receipt binds the census row, disposition, evidence, optional
  executable row, close-environment vector, and static marker.
- §12D.3 routes only `EXECUTABLE` rows to the existing workload tuple,
  canonical four-string row order, forced consecutive 15-baseline groups, and
  §12C.3/§12C.4 statistics. Its empty projection correctly yields no plan,
  pilot, qualification, or false GREEN.
- §12D.4 gives every terminal token an exact RED/BLOCKED/non-win meaning and
  keeps environment availability orthogonal to semantic/artifact preflight.
- Complete pairwise ownership, complete Williams cycles, median paired-log
  statistics, one-shot T8/T9, no result-conditioned rerun, two anchors, OC-1,
  artifact retention, PB0/PB13G gates, and acknowledgement-gated BR-10 remain
  intact.

However, the addendum does not yet meet its own born-RED standard of an exact,
content-addressed, mechanically reconstructible final matrix. The missing
role/kind projection, environment receipt schema, and close-envelope schema are
normative omissions. The activation-version mismatch additionally makes the
owner's requested statistics decision disagree with the artifact topology the
same section asks the owner to ratify.

## 3. Feature analysis A11-01 — merged roles have no qualification-kind routing law

Section 12D.1 constructs a complete census **for each** candidate,
`qualificationKind`, and public operation from the registered predecessor set
plus fixed/immediate anchors. It forbids removal of a registered predecessor
or required anchor and merges all roles held by one baseline into
`orderedRequiredPeerRoles`. Section 12D.3 then says every executable row is in
one forced group and is timed exactly once per required qualification kind and
environment.

The controlling laws assign different work and meanings to those roles:

1. BR-7 says LIVE/deposed/C14 tournaments occur at W7, **not every wave**;
   feature-wave qualification compares the fixed and immediately previous
   accepted anchors.
2. Section 12C.3 permits only `WAVE_GREEN|WAVE_AMBER|WAVE_RED` in a wave peer
   artifact and only `FASTER|SLOWER|INCONCLUSIVE` in a final artifact.
3. BR-6/BR-9 say predecessor cells require final `FASTER`, while fixed and
   immediate anchors retain their wave disposition and AMBER-debt meaning at
   perfection.

No frozen mapping says that `REGISTERED_PREDECESSOR` activates the final kind,
that anchor roles activate the wave kind, or how one merged multi-role row
binds both obligations without duplicating or dropping its cell. Literal
§12D therefore admits only two bad implementations: place all executable
registry and anchor rows into both kinds, contradicting BR-7 and producing
state-family rows with no governing role meaning; or filter by role without a
content-addressed authority, contradicting `orderedExecutablePeerCellsDigest`,
the no-removal rule, and the no-favorable-regrouping claim. A peer's expected
speed need not drive the filter for this still to be undefined author choice.

The repair should freeze an outcome-free role×qualification-kind obligation
projection before grouping. It must keep the full census/terminal coverage,
define exactly which role obligations consume a wave result and which consume
a final result, define the multi-role case, and bind the projection into class,
plan, schedule, activation, result, history, and close. It may not inspect
timing or allow a terminal row to move into execution.

## 4. Feature analysis A11-02 — required-environment preflight is not a receipt schema

Section 12D.4 requires exactly one `AVAILABLE` or
`UNAVAILABLE_ENVIRONMENT` receipt per frozen required environment in vector
order, but it defines neither receipt as a canonical tagged array and defines
no digest over the ordered vector. `UNAVAILABLE_ENVIRONMENT` is described only
as binding the close vector, environment class, partition binding, "its
admission-evidence digest," and `NO_T8_OR_T9`; `AVAILABLE` has no stated fields
or evidence predicate at all. The admission-evidence bytes, tag, ordering,
empty/nonempty law, and relation to the per-batch
`statisticsResourceAdmissionReceiptDigest` are undefined.

This prevents the promised mechanical checks. Two implementations can issue
different bytes for the same environment status; a class can be labelled
available without a specified proof; the close cannot content-address the
ordered environment census; and the packet cannot distinguish global class
availability from a particular batch's failure to reach T7 resource
admission. Binding `closeEnvironmentVectorDigest` in each static peer receipt
does not prove that every vector index has one status/result, and
`qualificationRunId` on successful executable runs does not supply the missing
receipt for an unavailable class.

Define an exact required-environment census row and first-match status/evidence
union, its ordered vector digest, and the relation between `AVAILABLE`, each
group's T7 admission, `UNAVAILABLE_ENVIRONMENT`, and an empty executable set.
Bind that vector to every relevant plan/result and to the exact close matrix.
Report-only classes must remain disjoint and unable to rescue the required
vector.

## 5. Feature analysis A11-03 — downstream closure is not canonically instantiated

Section 12D.3 says schedule, mechanical schedule-verification, and activation
advance from their immediately prior controlling versions to their next
integer versions while retaining fields and appending the partition binding
and group preflight receipts. The prior exact tags are schedule v1,
verification v1, and activation v2, so the controlling successors are v2,
v2, and **v3**. Yet required owner decision 3 still names a "version-2
activation receipt." An owner cannot ratify both statements as one exact
statistics topology.

More seriously, §12D.4 says every history, supersession, wave, final,
aggregate-verdict, and perfection-close envelope advances from an immediately
prior controlling version and appends the partition/terminal/applicable-result
fields. The sheet defines no exact prior tagged-array formulas for those
envelopes. Nor does it define the canonical close row that associates census
index, merged role, qualification kind, required environment index, static
preflight receipt, executable group/statistics result or terminal receipt, and
the resulting role-specific RED/BLOCKED/FASTER/debt disposition.
"Applicable executable preflight/statistics digest or terminal receipt" is
therefore ambiguous in cardinality and order, especially for a multi-role peer
and multiple required environments. A prose reconstruction check cannot hash
or reproduce bytes that the normative schema never defines.

The repair must publish exact next-version tags/arrays (or one replacement
close-matrix family), exact empty-array rules, census/role/kind/environment
indices, and a deterministic total projection from every preflight cell to one
terminal or executable result per required obligation. Then update the owner
decision to the actual activation version. Existing §12C valid-only peer/batch
statistics remain correct inputs and must not be widened with terminal states.

## 6. Owner, BBNF, and authority boundaries

All six owner decisions remain necessary. The repair must preserve the
mutually exclusive OC-1A/OC-1B choice, strict non-win semantics, artifact-store
selection, PB0/Gate-2 separation, and the rule that ratification permits only
prototype harness construction before a later GREEN freeze; it does not permit
a pilot or measurement.

BR-10 is still correct: no parse-that uplift is admissible until both tasks
acknowledge the same content-addressed package/commit/API/batch as implemented
and hardened, the local 2×2 grammar×engine cells are semantically crossable,
and a separate E-3 pin decision lands. The active BBNF task has acknowledged no
such artifact. Nothing in this rejection opens coordination, authorizes source
inference, accepts remote measurements as local proof, or changes the 1.0.0
pin.

## 7. Required repair and disposition

1. Add a frozen, outcome-free role×qualification-kind obligation projection
   that preserves the complete census while making BR-7's wave/final schedule
   and BR-6's role-specific state semantics total, including multi-role rows.
2. Define exact canonical required-environment availability/evidence receipts
   and one ordered vector digest; distinguish class unavailability from per-
   batch T7 admission and bind both to executable results and close.
3. Define exact downstream schedule/verification/activation successor tags and
   one canonical history/wave/final/aggregate/close matrix family with total
   census×role×kind×environment coverage, exact ordering, and exact empty-set
   encodings. Correct the stale activation-v2 owner-decision text.
4. Preserve §12C valid-only statistics, static terminal no-launch/non-win
   semantics, executable-only no-regrouping, `q=0`, pairwise denominators,
   one-shot/no-redraw law, environment conjunction, and every owner/BBNF/
   production boundary.
5. Restart both independent zero-credit challenges on the new exact addendum
   and harden digests. Root gestalt and all six owner decisions remain
   downstream; no harness, pilot, or measurement is permitted.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 NINTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A10

status: `REJECT`

date: 2026-07-21

challenged artifact: current ninth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`a8865dcffd4c9166002014d4335d0d14bcb30babc679251c13a2abc7fdaf4811`

governing harden raw-byte SHA-256:
`d8f9292bb1b5aadcff08f2badc8d93f3cfdafcd779f0143a584bf71920d7ef64`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact ninth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B10 seat. I read the current sheet, complete governing harden chain,
benchmark research, Charter, and Phase-A PI; challenged the exact §12C
transposition, durable-invalid-first T8 selector, segmented action/closure
prefix algebra and every event cut, valid-only peer/batch statistics envelopes,
bounded-rejection partial-prefix law, independently complete pairwise
denominators, canonical group census, environment conjunction, Williams
cycles, numeric/statistical law, owner decisions, and BBNF uplift boundary.
No harness, smoke, pilot, profile, benchmark, measurement, optimization,
parser/dependency code, PB0/PB13P, BBNF coordination, production, inbox,
script, or other file changed. This audit ledger is my only write.

## Current A10 verdict

**REJECT.** Section 12C makes a valid batch statistics artifact require one
`VALID` per-peer artifact for every member of `orderedPeerCellsInGroup`, while
BR-3 requires every peer/anchor cell to enter exactly one canonical group.
The packet never separates executable timing cells from terminal semantic,
artifact, or registry dispositions. Consequently an exact registered cell
whose mandatory pairwise stratum is empty, whose operation is inapplicable or
incomparable, or whose artifact/ABI is unavailable has no canonical evidence
path: it cannot produce the required valid statistics artifact, cannot be
dropped from the frozen group, and cannot be run merely to relabel its
specific state as batch-wide `INVALID`. The exhaustive state table and the
valid-only batch topology therefore do not compose into a total close law.

This verdict applies only to the challenged bytes. It grants no harness,
pilot, benchmark, measurement, optimization, grammar, dependency, pin,
PB0/PB13P, BBNF, coordination, inbox, or production authority. Any repair
invalidates this report and requires two new independent zero-credit
challenges.

## 1. Total-tranche (gestalt) analysis

The ninth amendment repairs the prior selector blocker: durable invalid pilot
terminals now win before literal `OBSERVATION_OK`, `DOUBLE`, and
`SUBJECT_SELECTED` success/incomplete cuts. Its segmented action/closure proof
also keeps authority, derived statistics, decision, publication, terminal,
and final evidence in one forward direction. Those are material improvements
to the one-shot/no-redraw guarantee.

The remaining defect is nevertheless tranche-level. Strict peer beating is a
conjunction over a finite registry, and the Charter explicitly requires a
missing or incomparable baseline to be reported rather than imputed. A proof
topology that has exact receipt shapes only for executable valid timing peers
cannot account for the rest of that finite registry without either silently
dropping cells or converting a semantic/artifact disposition into a runner
failure. Either outcome weakens the claimed complete denominator and makes
the close matrix implementation-selected rather than mechanically total.

The intended repair belongs in proof orchestration, not grammar code. It must
preserve correctness-first semantics, readable modular parse-that modules,
complete pairwise ownership, and the refusal to invent a peer win.

## 2. Wave/addendum analysis

The ninth amendment discharges its named A9/B9 repair themes in substance:

- §12C.1 is a strict first-match T8 selector. Durable invalid origins are
  chosen by frozen code precedence and action-event index before the literal
  success rows, and state-illegal records enter the corrupt suffix.
- §12C.2's sealed action segment, closure prefix, and combined pre-append
  prefix are acyclic. Closure event `i` binds prefix `i`, not itself or a
  descendant; authority is event zero; the final prefix is computed only
  after terminal publication.
- §12C.3 gives each executable peer an exact block projection, version-5
  seed, accepted-draw population, median-log aggregate, interval, winning
  count, and kind-specific state, then publishes only the complete batch
  digest.
- §12C.4 correctly makes rejection exhaustion a prefix of completed prior
  peer artifacts plus current-peer draws/medians, never a current-peer or
  batch artifact, interval, redraw, or new execution.
- Required-environment conjunction, complete Williams cycles, dynamic final
  win count, two-anchor debt, OC-1A/B, PB0/PB13G authority, durable retention,
  and acknowledgement-gated BR-10 admission remain intact.

But the addendum's born-RED standard is a complete final matrix, not merely a
canonical success artifact. The new valid-only envelopes expose that the
pre-existing group census has no disjoint disposition lane. The wave cannot
be accepted while its finite peer registry lacks a total pre-run routing law.

## 3. Feature analysis A10-01 — valid-only statistics and the complete peer census conflict

BR-3 first constructs an independent candidate×baseline denominator for every
registered predecessor and anchor, retaining empty mandatory strata so that
they yield `NO_COMMON_DENOMINATOR`. It then states that **every peer/anchor
cell** belongs to exactly one workload equivalence class, that `q>=1`, and
that the forced groups concatenate the complete ordered cell list exactly
once. The batch plan puts every grouped baseline into
`orderedSubjectArtifacts`; plans, runs, schedules, pilots, T9 executions, and
close rows bind that fixed partition.

Section 12C.3 independently requires:

1. one `peerStatisticsArtifactDigest[q]` for every
   `orderedPeerCellsInGroup[q]`;
2. every such artifact to end in literal `"VALID"`;
3. its state to be only `WAVE_GREEN|WAVE_AMBER|WAVE_RED` or
   `FASTER|SLOWER|INCONCLUSIVE`; and
4. the batch artifact to contain exactly as many valid peer-artifact digests
   as the group contains peer rows.

It expressly forbids `WAVE_BLOCKED`, registry/evidence states, and `INVALID`
inside a valid peer artifact. That is correct locally, but the sheet supplies
no complementary canonical route for a non-executable cell.

For a concrete frozen case, take a registered predecessor whose exact bundle
cannot be durably recovered. BR-2 and BR-6 require
`UNAVAILABLE_ARTIFACT`, a cell-specific evidence-BLOCKED state. Yet BR-3 still
places that peer cell in a group and its bundle in the scheduled subject set.
The subject cannot execute T8/T9, so it cannot emit the required valid peer
artifact. Treating the launch failure as `PILOT_INVALID`/`T9_INVALID` maps
**every** cell in the grouped batch to `INVALID`, erasing the required
`UNAVAILABLE_ARTIFACT` meaning and blocking otherwise executable peers.
Silently omitting the peer changes `q`, `m`, the forced partition, Williams
schedule, plan, qualification, and run identities and violates the complete-
census rule.

The same hole exists before timing for mandatory empty strata
(`NO_COMMON_DENOMINATOR`), absent operations (`NOT_APPLICABLE`), and semantic
or cost-boundary distortion (`INCOMPARABLE`); `UNSUPPORTED_ABI` is the same
artifact-class counterexample. A missing required environment likewise needs
its exact vector-level `UNAVAILABLE_ENVIRONMENT` close row without pretending
a report-only or different environment executed it.

This is not a demand to put blocked states into a `VALID` statistics artifact.
It is the absence of a prior, content-addressed partition and complementary
disposition-receipt family that proves every frozen registered cell is routed
exactly once either to an executable canonical group or to its exact terminal
state. Without that partition, §12C.3's exact-length batch law is not total.

## 4. Feature analysis A10-02 — selector, segmented prefixes, valid statistics, and exhaustion otherwise hold

I found no remaining overlap in §12C.1. An action prefix containing an
`OBSERVATION_INVALID` is selected at durable-invalid step 3 before the
`OBSERVATION_OK` row can be considered. A justified `PILOT_N_EXHAUSTED`
round and its invalid subject completion preserve the earlier origin; an
unjustified invalid completion is state-illegal and enters the corrupt suffix.
The complete derivation intent is reachable only after every ordered selected
subject and remains one pure VALID/derivation-invalid plan.

The segmented prefix algebra has no confirmed digest cycle. Prefix zero has
the sealed action segment and an empty closure array; authority event zero
binds prefix zero; each later event binds only its own pre-append prefix; valid
statistics bind prefix one and authority; the decision binds the batch; the
publication binds its later prefix; terminal, final WAL, and execution remain
descendants. Invalid synthesis uses the decision-pre-append and event-specific
prefix cuts, while a committed invalid detection remains the fixed block
anchor.

For executable peers, the per-block receipt projection, ratio direction,
median paired-log aggregate, complete-Williams block count, bootstrap seed,
accepted-draw count/order, type-1 bounds, dynamic final winning count, and
wave/final state split are exact. On exhaustion, the current peer contributes
only its canonical accepted-draw and completed-median prefixes; prior peers
contribute only their complete valid per-peer artifacts; no batch artifact or
partial interval exists. I found no route from exhaustion, nonfinite
derivation, corruption, or host resource behavior to another seed, draw,
pilot, T9 execution, or environment.

## 5. Required repair and disposition

1. Before canonical equal-work grouping, define a content-addressed exhaustive
   partition of every registered peer/anchor cell into exactly one of:
   executable qualification eligibility or one exact terminal semantic/
   registry/artifact disposition. Bind both sides and proof of complete,
   duplicate-free registry coverage in T0/T1 review and close history.
2. Form `orderedAllPeerCells`, groups, `orderedSubjectArtifacts`, `m`, Williams
   plans, and §12C valid-only statistics only from the executable partition.
   A cell must never move between partitions after observations; a repair to
   its artifact/ABI/semantic status follows the existing reviewed E-3/causal
   supersession law and preserves the prior disposition.
3. Define exact canonical terminal-disposition receipts for
   `INCOMPARABLE`, `NO_COMMON_DENOMINATOR`, `NOT_APPLICABLE`,
   `UNAVAILABLE_ARTIFACT`, and `UNSUPPORTED_ABI`; bind the frozen required-
   environment vector and exact `UNAVAILABLE_ENVIRONMENT` row at close. These
   receipts are not peer wins and cannot rescue RED/BLOCKED evidence.
4. State how an executable group with zero remaining cells is omitted without
   inventing `q>=1`, and prove every executable peer is timed exactly once
   while every terminal cell is reported exactly once. Preserve §12C's
   valid-only peer/batch artifacts rather than laundering blocked states into
   them.
5. Preserve every other ninth-amendment safeguard and the proposed/not-
   ratified boundary. Then restart both independent challenges on the new
   exact addendum and governing-harden digests. Root gestalt and all six owner
   decisions remain downstream; no harness, pilot, or measurement is
   permitted.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 EIGHTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A9

status: `REJECT`

date: 2026-07-21

challenged artifact: current eighth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`5541413532fccaaf0e71d52f26da784e9a53d6dc282c588c34eedc0d9173a21f`

governing harden raw-byte SHA-256:
`89ebd79e137dce68fdc036c61d2be54b0407862d44eeca342e92b70da31e8e28`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact eighth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B9 seat. I read the current sheet, Charter, PI, benchmark research,
and governing harden chain; challenged the exact §12B transposition, the total
T8 selector, raw action-store framing and corrupt-suffix binding, segmented
closure WAL, plan/authority/decision receipt versions, post-authority re-entry,
pre-T7 statistics-resource admission, bounded rejection and exhaustion
receipts, pairwise/group/environment ownership, Williams/statistical law,
wave/final states, owner boundary, and BBNF uplift admission. No harness,
smoke, pilot, profile, benchmark, measurement, optimization, parser/dependency
code, PB0/PB13P, BBNF coordination, production, inbox, script, or other file
changed. This audit ledger is my only write.

## Current A9 verdict

**REJECT.** Section 12B.2 asserts a total, mutually exclusive T8
prefix-to-plan selector, but at least one ordinary terminal-observation row and
the durable-invalid-terminal row overlap on the same hash-correct WAL prefix.
Those rows can select different failure-code consequences. The ambiguity is
present in governing harden §13.2 as well as the addendum, so the challenged
bytes cannot yet be the deterministic one-plan authority claimed by the
packet.

This verdict applies only to the challenged bytes. It grants no harness,
pilot, benchmark, measurement, optimization, grammar, dependency, pin,
PB0/PB13P, BBNF, coordination, inbox, or production authority. Any repair
invalidates this report and requires two new independent zero-credit
challenges.

## 1. Total-tranche (gestalt) analysis

The intended rail remains a strong fit for the expanded Charter. It keeps
semantic correctness prior to timing, preserves the immutable P-3 continuity
lane, makes peer beating a finite falsifiable matrix, retains independently
complete candidate×baseline workloads, forbids result-conditioned redraws,
and confines proof-runner complexity outside the readable modular grammar.
The eighth amendment also correctly keeps PB0/PB13G, owner, artifact-retention,
and acknowledgement-gated parse-that uplift boundaries separate.

The selector defect is nevertheless tranche-level, not editorial. The rail's
anti-p-hacking guarantee depends on one raw prefix selecting exactly one plan,
failure classification, and terminal suffix before authority. Two admissible
rows for the same prefix undermine the very closure topology that makes an
unchanged losing or invalid attempt one-shot. Ratifying around that ambiguity
would move a design choice into harness implementation and make independent
reconstruction non-canonical.

## 2. Wave/addendum analysis

The eighth amendment discharges the B8 repair themes in substance:

- action-store framing, corrupt raw-record retention, raw-store-view identity,
  and separate closure-WAL genesis remove the corrupt-tail/authority alias;
- plan v2 and authority/receipt v3 carry the valid prefix, corrupt suffix, raw
  view, and deterministic closure function without a reverse digest edge;
- the pre-T7 statistics-resource plan and admission receipt prevent host
  resource behavior from becoming a post-result verdict or retry reason;
- accepted-draw, rejected-chain, partial-trace, and first-exhausted `[r,d]`
  receipts make bounded rejection finite and reproducible without another
  seed, draw, execution, or environment; and
- complete pairwise denominators, the exact four-string peer row, forced
  maximal grouping, required-environment conjunction, Williams cycles, the
  sole median-log aggregate, distinct wave/final states, strict RED/BLOCKED
  non-win law, OC-1A/B, and BR-10 remain intact.

But the section's own born-RED claim is exact totality, and that claim is not
met. A deterministic closure function cannot delegate overlapping-row
resolution to an unwritten implementation convention.

## 3. Feature analysis A9-01 — T8 selector predicates are not mutually exclusive

The closed pilot WAL schema defines two terminal observation events:
`OBSERVATION_OK` and `OBSERVATION_INVALID`. It further states that terminal
invalidity leaves a shorter ordered observation prefix. Section 12B.2 then
contains both of these rows:

1. a generic “terminal observation `o<lastO` ... or terminal `lastO` before
   `ROUND_COMPLETE`” row, which selects `PILOT_BETWEEN_OBSERVATIONS`; and
2. “a durable invalid observation, round, or subject terminal” row, which
   selects the phase/locus of the invalid origin and retains its earlier
   precedence code.

After a valid `OBSERVATION_INVALID(s,k,o)` append and before ancestor closure,
the longest hash-correct prefix satisfies both descriptions. The first row's
bare `INVALID` consequence falls through to ordinary incomplete-prefix
classification, while the second expressly preserves the durable invalidity
code. The selector supplies neither an `OBSERVATION_OK` qualifier on the first
row nor an intra-table priority rule. Its three-step outer order does not help:
there is no authority and no corrupt suffix, so step 3 still has two matching
hash-correct rows.

The same issue reaches `ROUND_COMPLETE`: the unqualified “final
`ROUND_COMPLETE`” row can describe a `PILOT_INVALID` round terminal that is
also named by the durable-invalid-terminal row. Calling all rows mutually
exclusive does not make these event-status sets disjoint.

Because `finalizationPlanDigest` binds the selected phase, locus, plan class,
and disposition function before authority, the ambiguity can produce different
plan bytes from the same frozen raw-store view. That is a blocker for
authority v3, byte-identical post-authority re-entry, and proof-independent
reconstruction.

## 4. Feature analysis A9-02 — raw-store, closure, resource, and RNG repairs otherwise hold

The new action/closure segmentation is acyclic: the raw-store view seals the
canonical valid prefix plus retained corrupt suffix before closure genesis;
plan v2 precedes authority; decision/publication/terminal events descend only
in the closure WAL; and the final WAL receipts bind both ordered segments.
Corrupt action bytes cannot advance observations or replacement state, and a
nonempty suffix forces schema-mismatch INVALID rather than a derivation path.

Statistics-resource admission is correctly pre-observation and
environment-bound. Its safe-integer maxima are mechanically bounded by the
frozen peer group and complete Williams count; failure to reserve and verify
the selected store blocks T7 and opens no pilot. Post-authority computation is
pure and recoverable rather than a host-resource-selected redraw.

The bounded RNG remains unbiased on every accepted draw and total over the
finite candidate range. The canonical `(peerCellIndex,r,d,j)` traversal,
accepted-draw receipts, rejection chain, exact 65,536-record exhaustion trace,
partial accepted prefix, and authority-bound exhaustion receipt support one
deterministic `BOOTSTRAP_RNG_REJECTION_EXHAUSTED` decision with no partial
interval or retry. I found no dropped pairwise, environment, schedule,
statistics, wave/final, owner, or BBNF boundary in these repaired portions.

## 5. Required repair and disposition

1. Amend governing harden §13.2 and ADDENDA-06 §12B.2 so every success-path
   cut names its exact event/status predicate: for example,
   `OBSERVATION_OK`, `ROUND_COMPLETE ... DOUBLE`, and
   `ROUND_COMPLETE ... SUBJECT_SELECTED`; name invalid observation/round/
   subject terminals in disjoint rows with their exact precedence code.
2. Either make all row predicates provably disjoint or publish one explicit,
   total most-specific-row priority that yields the same plan bytes for every
   hash-correct prefix. Do not rely on the prose word “terminal” to imply OK.
3. Recheck the hash-correct-but-state-illegal explanatory row against the
   outer corrupt-suffix override so it is an unambiguous classification, not a
   second selectable branch.
4. Preserve every other eighth-amendment safeguard and the proposed/not-
   ratified boundary. Then restart both independent challenges on the new exact
   addendum and governing-harden digests. Root gestalt and all six owner
   decisions remain downstream; no harness, pilot, or measurement is permitted.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 SEVENTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A8

status: `ACCEPT`

date: 2026-07-21

challenged artifact: current seventh-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`a4fe985e73e520fb1db717b4408567ec4d124a8e3b4caf51f65e4dfa5f259a14`

governing harden raw-byte SHA-256:
`04a1725b11cb1ef630031458ed1261ecb0de9a47080b376d7165357ba33dd82b`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5-family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact seventh-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or contact the
concurrent B8 seat. I read the current sheet, Charter, PI, benchmark research,
and the complete governing harden chain; challenged the exact §12A
transposition, authority tagged union and plan graph, every post-authority
closure, valid-block normalization, pilot/statistics publication order,
failure phases and loci, finite RNG-v3/bootstrap-v5 failure, one-shot and
replacement law, pairwise/group/environment ownership, statistics and
wave/final gates, owner boundary, and BBNF uplift admission. Read-only hashing,
text/state-graph checks, binary64 constant checks, and Williams-count checks
ran. No harness, smoke, pilot, profile, benchmark, measurement, optimization,
parser/dependency code, PB0/PB13P, BBNF coordination, production, inbox, or
other file changed. This audit ledger is my only write.

## Current A8 verdict

**ACCEPT.** The exact seventh amendment transposes governing harden §12
without dropping a controlling repair and closes every A7 blocker. I found no
confirmed defect that would permit two valid evidence identities from the same
frozen bytes, an action-bearing retry after finalization, a result-conditioned
redraw, an ineligible peer win, cross-environment pooling, or premature
implementation authority.

This ACCEPT applies only to the challenged design bytes. It does not ratify the
sheet and grants no harness, pilot, benchmark, measurement, optimization,
grammar, dependency, pin, PB0/PB13P, BBNF, coordination, inbox, or production
authority. A normative byte change invalidates this challenge. Root gestalt,
the independent B8 result, and every explicit owner decision in §13 remain
mandatory.

## 1. Total-tranche (gestalt) analysis

The performance rail now fits the expanded Charter optimally. It keeps the
historical P-3 lane immutable, including the explicitly named UTF-16-code-unit
defect, while the qualification lane measures exact UTF-8 ingress and admits
timing only after semantic/result equivalence. Strict close is a finite,
falsifiable conjunction over LIVE, deposed, C14, anchors, and frozen required
environment classes; it neither invents an L4 peer nor hides a lost stratum in
an aggregate.

The design also protects the grammar goal rather than optimizing around it.
Complete independent pairwise workloads, operation-owned adapters, fixed
owner modules, profiles after unprofiled timing, and the ban on benchmark-only
fast paths preserve readable modular parse-that code. PB0 owns Phase-B
occurrence/resource closure without retroactively owning Phase A. G-2/PB13G
remain prior to every speed claim.

The protocol is intentionally exacting because one unchanged loss may not buy
another draw. The complexity is confined to the proof runner and durable
receipts, not imposed on grammar modules. The pre-measurement freeze packet is
the correct point to materialize and mechanically verify the resolved
versioned schemas; it is shared and append-only rather than repeated ceremony
per door.

## 2. Wave/addendum analysis

The seventh-amendment born-RED ledger is fully discharged:

- The authority proof is a closed three-row tagged union over all five optional
  proof fields. Recovery cannot carry stale owner bytes, and no optional proof
  can vary an otherwise identical authority receipt.
- `finalizationPlanDigest` is derived solely from the pre-authority prefix and
  commits the domain, attempt, phase/locus, any one permitted pre-decision
  block completion, and the fixed disposition function before authority.
- Raw observations precede authority. After authority, only deterministic
  derivation and idempotent publication remain; re-entry follows the existing
  plan/decision and cannot reacquire authority or resume work.
- Pilot panel and T9 statistics are computed after authority, bind that
  authority, are named by the finalization decision, and are published only
  after the decision. The controlling replacement expressly supersedes stale
  statistics-before-authority and immediate-publication ordering, so the
  graph has no reverse digest edge.
- The eleven pilot and twenty-one T9 phase tokens are closed ASCII
  vocabularies. The T9 table covers launch, primary/replacement block work,
  samples, subjects, detection, completion, replacement cuts, post-block,
  authority, statistics, decision, terminal, and corrupt suffixes.
- RNG schema v3 is finite: each draw inspects exactly `j=0..65535`, accepts the
  first unbiased rejection-sampling word, or terminates the one-shot execution
  with one of two exact BLOCKED invalidities. Bootstrap seed v5 binds the
  execution, launch, pilot terminal, schemas, plan, pairwise denominator, and
  subject artifacts without sample-dependent reseeding.

The repaired bytes retain every non-conflicting BR-1–BR-10 safeguard. The main
body's historical version text is unambiguous because §12A names its narrow
supersession scope and exact controlling versions; it provides history, not a
second executable branch.

## 3. Feature analysis A8-01 — authority and deterministic closure are canonical

The authority union now exhaustively fixes the empty/nonempty disposition of
owner token, live-owner proof, durable-termination receipt, proof-restart
receipt, and no-live-owner evidence for every allowed tag pair. Every other
combination is structural RED. The required atomic comparison observes the
same launch-bound bytes and pre-event WAL prefix but is not an unconstrained
sixth field.

The version-2 authority evidence and domain receipts bind the precomputed plan.
That plan excludes actor identity, recovery proof, panel/statistics result,
timestamp, and implementation choice. Its only nonempty pre-decision closure
descriptor is the already fixed invalid `BLOCK_COMPLETE` case. Thus neither a
recovery actor nor a live owner can select a different closure after gaining
authority.

The post-authority crash cases are total. Before decision, any re-entering
actor recomputes the same pure derivation and compare-and-appends the same
decision bytes. After decision, it appends only the next missing deterministic
suffix. This holds for both success and failure and prevents the stuck-prefix,
second-authority, and continuation-proof variants rejected by A7.

## 4. Feature analysis A8-02 — T8/T9 phases, loci, and block publication are total

The pilot table plus its exact token list keeps a completed subordinate index
only through the specified between-item cut, clears subject identity between
subjects, and synthesizes only unmatched intents. An authority event does not
create a new origin phase. Valid and invalid decisions carry exactly one
status-compatible artifact/failure union.

For T9, the most-specific-row rule resolves the replacement-specific rows
within the general between-valid-block class. A fully evidenced open valid
block is action-free normalized exactly once before authority; concurrent
normalizers can publish only the identical completion. Every other open block
closes invalid only after the authority-bound INVALID decision, except the one
committed-invalid-detection completion whose bytes were fixed before authority.

`BLOCK_COMPLETE` remains the sole receipt publisher. An invalid receipt uses
the committed detection-event digest, so insertion of authority before a
missing completion cannot change its identity. Replacement intent consumes
the one execution-global budget; a crash never refunds it, and no recovery row
starts or resumes replacement work. Complete raw evidence may be finalized to
VALID without replay; incomplete action evidence deterministically ends
`CONTROLLER_CRASH_OR_INCOMPLETE`/INVALID.

## 5. Feature analysis A8-03 — finite RNG and statistical law are exact

Read-only checks reconfirmed the published binary64 constants for 25, 50, 200,
1000, 0.90, 0.95, and 1.00. Control integers, exact observed `u64be:` counters,
and derived `f64be:` scalars remain disjoint. Non-finite raw measurement and
post-block derivation have closed phase-specific invalidities.

For every `m=2..16`, the written cycle law yields a complete Williams multiple:
wave counts are at least five, final counts at least ten, and
`ceil(0.90*finalBlockCount)` is never weaker than 9-of-10. `n` is therefore a
frozen positive block count well within the raw-u32 resample/draw ranges.

The only normative point estimate remains
`exp(median(pairedLogSpeedRatio))`, with speed oriented
baseline-duration/candidate-duration. The 10,000 paired resamples and type-1
positions 250/9750 are unchanged. Finite bounded rejection either produces a
first accepted unbiased index or publishes no draw and terminalizes
`BOOTSTRAP_RNG_REJECTION_EXHAUSTED`/
`BOOTSTRAP_RNG_RESOURCE_EXHAUSTED`; neither path permits another seed, block,
pilot, environment, or execution.

Wave and final semantics remain distinct: anchor cells use only
`WAVE_GREEN/AMBER/RED/BLOCKED`; final predecessor cells use only
`FASTER/SLOWER/INCONCLUSIVE` plus the exhaustive evidence dispositions.
Only final `FASTER` passes, and unresolved AMBER debt survives to perfection.

## 6. Feature analysis A8-04 — denominator, grouping, and environment laws do not game work

Every candidate×baseline denominator is independently complete over all
selected-frozen rows and retains empty mandatory strata. The exact canonical
peer row is four strings in fixed order; its JSON bytes are also its sort key.
Duplicate rows and subject IDs are RED. Equal-work classes partition only into
consecutive maximal fifteen-baseline groups, with the candidate inserted once,
so `2≤m≤16`; grouping cannot shrink or replace a cell's pairwise denominator.

Qualification kind, plan, workload class, full partition, group index/digest,
candidate causal receipt, artifacts, environment vector, and invariant schemas
all bind the portable identity. Schedule and activation are derived only after
review and bind the environment run. Required environments close
conjunctively; their samples are never pooled, and report-only vectors cannot
rescue RED or BLOCKED evidence.

The causal-source transition remains content-addressed and reviewed. Neutral,
unreachable, administrative, or bundle-only bytes open no attempt. Earlier
valid verdicts remain history, so no nominal digest, environment switch, or
favorable corpus append grants optional stopping.

## 7. Feature analysis A8-05 — owner and BBNF boundaries are intact

The sheet is still prominently `PROPOSED · NOT RATIFIED · NO HARNESS OR
MEASUREMENT AUTHORIZED`. Section 13 requires six explicit decisions, including
one and only one OC-1A/OC-1B floor policy and a durable artifact home.
Ratification would authorize construction only; a separately reviewed GREEN
freeze is still required before any pilot, and Gate 1/Gate 2 continue to bound
PB0/PB13P.

BR-10 admits no current uplift. It requires a mutually acknowledged,
content-addressed artifact explicitly implemented and hardened; complete local
semantic/no-throw/limit replay; crossable unchanged-grammar cells; and a
separate E-3 pin decision. The active-task status cannot substitute for those
receipts, no BBNF result becomes local proof, and 1.0.0 remains the control.

## 8. Required disposition

1. Preserve this A8 ACCEPT as credit only for exact challenged SHA-256
   `a4fe985e73e520fb1db717b4408567ec4d124a8e3b4caf51f65e4dfa5f259a14`.
2. Await the independent B8 challenge. If its exact-byte verdict is also
   ACCEPT, root may perform the E-3 gestalt adjudication; otherwise repair and
   restart both challenges at zero credit.
3. Even after two ACCEPTs and a GREEN gestalt, obtain all six explicit §13
   owner decisions and the recorded model-route variance before constructing
   the prototype harness. Obtain the later freeze-packet GREEN before any
   pilot or measurement.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 SIXTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A7

status: `REJECT`

date: 2026-07-21

challenged artifact: current sixth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`ee9fa993effee67a07f8d72799cc86d5e73328bc797a4e170f6e32551343b6c6`

governing harden raw-byte SHA-256:
`792124ab8caabdba248ec59732b028ddf187fefc9402be4d1ff7c953718dd976`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh, zero-credit, assume-faulty challenge of the exact sixth-
amended bytes. Every older report below the historical-boundary marker is
invalidated history and grants no acceptance credit. I did not read or contact
the concurrent B7 seat. I read the current sheet, research, complete governing
harden chain, Charter, and PI; attacked the finalization-authority proof graph,
every T8/T9 crash cut and receipt, canonical peer rows, sole block publication,
replacement transitions, numeric bytes, RNG-v2/bootstrap-v4, pairwise/group
ownership, environment isolation, complete Williams cycles, statistics,
wave/final law, owner authority, and uplift boundary. Only read-only hashing
and specification/state-graph checks ran. No harness, smoke, pilot, profile,
benchmark, or measurement ran. This audit ledger is my only write.

## Current A7 verdict

**REJECT.** The sixth amendment closes the three A6 blockers and materially
improves the one-shot proof protocol, but it still does not define one canonical,
acyclic, total execution for every admitted prefix. Four defects block
ratification:

1. the recovery authority rows leave `ownerTokenDigestOrEmptyString` and
   `liveOwnerProofDigestOrEmptyString` unconstrained, so the same recovery proof
   admits arbitrarily many authority receipt identities;
2. the valid T9 path both requires statistics to bind authority and publishes
   `STATISTICS_COMPLETE` before acquiring that authority, contradicting the
   earlier authority-before-statistics rule and making the required receipt
   impossible in the written order;
3. T9 still has no exhaustive phase/locus/synthesis table for ordinary cuts
   inside a primary block, and neither pilot nor T9 closes the
   `pilotFailurePhase`/`t9FailurePhase` token vocabulary; and
4. RNG-v2's unbounded rejection loop has no total terminal law for the valid
   deterministic case in which SHA-256 never yields a word below `limit`.

The first two defects break content-addressed finalization authority itself;
the third allows distinct terminal receipts from identical WAL bytes; the
fourth leaves the mandatory 10,000-resample statistic partial rather than
total. This rejection authorizes no repair, harness, pilot, measurement,
optimization, grammar change, PB0/PB13P work, dependency or pin change, BBNF
coordination, or production execution. Any normative repair invalidates A7 and
the concurrent B7 report regardless of its verdict and requires two new fresh
zero-credit challenges.

## 1. Total-tranche (gestalt) analysis

The rail remains optimal in its large architecture: immutable P-3 quarantine,
semantic eligibility before timing, complete independently owned pairwise
denominators, one reviewed causal attempt, required-environment conjunction,
complete Williams cycles, matched-log statistics, and strict non-win RED or
BLOCKED are the right refinement engine for Charter G-2 through G-5. It also
correctly refuses to weaken the readable modular grammar or admit an
unacknowledged parse-that uplift to obtain a number.

The present defects are nevertheless gestalt-level because they occur in the
shared T8/T9 authority and statistics substrate. Every door, peer, environment,
wave, and final cell would inherit the same non-canonical evidence graph. A
repair belongs in the benchmark E-3 formation packet before any harness bytes;
it is not a door-local implementation detail and cannot be deferred to W7 or
PB13P.

## 2. Wave analysis

The sixth-amendment born-RED intent is substantially discharged:

- A6's uncovered pilot prefixes now have explicit post-launch,
  between-observation, between-round, between-subject, post-last-subject,
  derivation, and post-panel rows.
- The canonical peer cell is exactly one four-string array; row bytes are also
  sort bytes; group slices are consecutive and byte-preserving.
- `BLOCK_COMPLETE` is the sole block-receipt publisher. Invalid receipts bind a
  committed detection-event anchor, so a recovery authority append cannot
  change the block receipt.
- The replacement order explicitly closes the primary, consumes the budget at
  intent, opens the replacement block, and covers replacement-intent-only,
  open-replacement, invalid replacement, and later-invalid-primary cuts.
- Raw `u32be(r,d)`, arbitrary-precision canonical-ASCII `j`, RNG schema v2,
  and bootstrap seed v4 remove A6's four-byte overflow defect.

The wave still fails completeness because the authority and T9 terminal laws
below admit no unique mechanical implementation. As written, a structural
checker cannot prove a single valid receipt for all reachable prefixes, so the
wave cannot receive E-3 ACCEPT.

## 3. Feature analysis A7-01 — recovery authority rows are not canonical

The §6 authority table is presented as the exact closed union, but only the
`LIVE_OWNER` row fully constrains its optional fields. For
`RECOVERY/DURABLE_TERMINATION`, the required-empty column names proof-restart
and no-live-owner evidence, but does **not** require
`ownerTokenDigestOrEmptyString` or `liveOwnerProofDigestOrEmptyString` to be
empty. For `RECOVERY/PROOF_RESTART_NO_LIVE_OWNER`, it requires only durable-
termination evidence empty and again leaves both owner/live-proof fields free.

Those two fields enter `finalizationAuthorityEvidenceDigest`. Consequently the
same launch, WAL prefix, durable termination (or proof restart/no-live-owner),
and atomic comparison can be encoded with empty strings or any pair of unrelated
64-hex values, producing distinct evidence, authority, WAL-event, failure,
terminal, and execution digests. The prose that the required evidence consists
of the named recovery proofs does not close fields that the exact table omits
from its required-empty column.

This is not cosmetic redundancy. Atomic compare-and-append uniqueness prevents
two events in one WAL, but it cannot tell a verifier which of several candidate
receipt bytes is canonical before the append. The repair must give every row an
exact nonempty/empty rule for **all five** optional proof fields. In particular,
the recovery rows need an explicit disposition for owner-token and live-owner-
proof fields rather than allowing free bytes.

## 4. Feature analysis A7-02 — valid T9 statistics and authority form an
incompatible order

The common authority law requires `FINALIZATION_AUTHORITY` before any valid
statistics finalization. BR-6 also requires every bootstrap/statistics artifact
to bind `t9FinalizationAuthorityReceiptDigest`. Those two requirements are
acyclic when authority is acquired after all controlling raw block bytes but
before the bootstrap/statistics artifact is formed and
`STATISTICS_COMPLETE` is appended.

The exclusive-valid-T9 paragraph instead requires every block **and one
`STATISTICS_COMPLETE` receipt**, then says the live executor acquires
`LIVE_OWNER` authority before `T9_TERMINAL`. That written trace is:

```text
... complete blocks
-> STATISTICS_COMPLETE(statisticsArtifactDigest)
-> FINALIZATION_AUTHORITY
-> T9_TERMINAL
```

It cannot satisfy the artifact-binding law because
`statisticsArtifactDigest` must already contain the authority receipt that does
not yet exist. It also directly contradicts the earlier mandatory trace
`FINALIZATION_AUTHORITY -> valid ... statistics finalization`. Omitting
authority from the statistics artifact violates BR-6; moving the event before
statistics violates the exclusive-valid paragraph as currently written. A
structural verifier therefore has no valid live T9-success trace.

The recovery paragraph exposes the intended acyclic topology: after complete
durable sample/postcheck bytes, acquire authority, synthesize missing derived
block/statistics receipts, then terminalize. The live-owner law must state the
same order explicitly, with raw controlling evidence before authority and
authority-bound statistics after it. The later generic summary cannot cure the
opposite event order in the normative valid-path paragraph.

## 5. Feature analysis A7-03 — T9 crash phases and failure tokens are not
exhaustive bytes

The pilot now has a useful exhaustive phase table, but T9 has only a replacement
cut table plus four generic locus categories. Reachable primary-block prefixes
remain unenumerated, including:

- after primary `BLOCK_INTENT` before `SUBJECT_INTENT`;
- after `SUBJECT_INTENT` before `SUBJECT_PRECHECK_COMPLETE`;
- after precheck before the first `SAMPLE_INTENT`;
- after a terminal sample before the next sample intent;
- after the final sample before `SUBJECT_COMPLETE`;
- after `SUBJECT_COMPLETE` before the next subject intent; and
- after the final subject completion before valid `BLOCK_COMPLETE`.

The text says recovery may synthesize unmatched sample/subject/block intents
child-to-parent, but it does not say whether a just-completed subordinate index
remains the locus through each between-items cut, as the pilot table does. Thus
the same prefix can plausibly yield `[b,a,p,j]`, `[b,a,p,null]`, or
`[b,a,null,null]`, changing `T9_FAILURE_DECISION` and every descendant receipt.
The replacement table does not resolve ordinary primary traversal cuts.

Separately, `pilotFailurePhase` and `t9FailurePhase` are hashed payload fields,
but neither has a closed enum nor an exact mapping from each table/cut to a
canonical string. Descriptive prose in a Markdown table is not declared to be
the literal JSON token. Two otherwise conforming implementations can emit
`"after_subject_complete"`, `"BETWEEN_SUBJECTS"`, or the prose label and obtain
different valid-looking receipts.

Repair requires a closed phase enum and one total longest-prefix table for T9
at the same granularity as T8, including primary and replacement traversal,
post-block/statistics, detection-without-completion, decision/synthesis
re-entry, exact locus retention, exact synthesized events, and phase-token
mapping. Existing authority and replacement rules should remain unchanged.

## 6. Feature analysis A7-04 — RNG-v2 removes overflow but remains only
almost-surely terminating

The repair correctly defines `u32be(r)` and `u32be(d)` as four raw big-endian
octets and makes `j` an unbounded canonical ASCII decimal final preimage field.
This removes the A6 overflow and delimiter defects. It does not, however, make
the deterministic algorithm total for every admitted frozen identity.

For any non-power-of-two `n`, `limit < 2^64`; a candidate word may be rejected.
The sheet says to increment `j` until acceptance but supplies neither a proof
that the fixed SHA-256 sequence for every valid seed/r/d eventually enters
`[0,limit)`, nor a terminal state if it does not. SHA-256 is a deterministic
finite-output function; random-oracle intuition gives probability-one
termination, not a proof that every content-addressed seed terminates. An
unbounded mathematical index prevents wrap but permits an infinite valid trace
with no selected block and therefore no 10,000-resample artifact or terminal
failure receipt. Real resource exhaustion while representing/hash-processing an
ever-growing decimal also has no frozen invalidity mapping.

The close claim is proof-total, not probabilistic liveness. Repair must either
define a finite pre-observation rejection bound plus one exact terminal
statistics invalidity (without retry seed or redraw), or use a demonstrably
total deterministic mapping whose bias/disposition is explicitly governed.
The current no-retry, sample-independent seed, raw-byte encoding, and unbiased
selection requirements must survive.

## 7. Independent checks that passed and must survive repair

### Identity, pairwise ownership, and grouping

- All five challenged identities above recomputed exactly. P-3 remains
  `08b927753ee717c67a3031861a88e35e629ecb585d5f84f7e1ba9e4378070be2`;
  P-4 remains
  `437a7f5f305b286e19dd1da79d3a245ff9e39acd62c91cc4eee165468ee757d0`.
- Causal source bytes, null/GENESIS transitions, reviewed reachability,
  environment-class separation, and one causal attempt per run remain exact.
- Every denominator is independently candidate×baseline complete. Empty
  mandatory strata remain visible; complete eligibility receipts and
  subject-specific peer manifests prevent a favorable multiway intersection.
- `canonicalPeerCellRow` is exactly four required strings; JSON row bytes are
  the unsigned sort bytes. Duplicate rows/IDs are RED. The q-cell census,
  consecutive maximal 15-baseline chunks, class/partition/group digests, and
  candidate-once subject artifact list are exhaustive and deterministic.

### Plan, schedule, pilot, and block receipts

- `batchPlanDigest` version 2 is pre-review and contains no ID-derived order.
  The T0→T9 no-backedge exclusions permit every required forward edge and
  prevent schedule, pilot, or result bytes from reseeding an earlier stage.
- Every pilot start, adaptive observation, selected common `N`, failure,
  panel, terminal, and qualification-execution receipt now has a forward-only
  WAL position. The expanded T8 table resolves A6's post-launch and between-
  item locus holes, subject identity is present iff `s` is non-null, and
  re-entry cannot reacquire authority.
- Valid block receipt publication is acyclic: a valid receipt binds the prefix
  before its sole `BLOCK_COMPLETE`; an invalid receipt binds the committed
  detection event, so a recovery authority event inserted before completion
  cannot alter it. The exact primary/replacement sequence and global budget
  close A6's competing traces.

### Numeric, Williams, statistics, and verdict law

- The published critical f64 encodings remain correct:
  `25=4039000000000000`, `50=4049000000000000`,
  `200=4069000000000000`, `1000=408f400000000000`,
  `0.90=3feccccccccccccd`, `0.95=3fee666666666666`, and
  `1.00=3ff0000000000000`. JSON control integers, observed `u64be:` counters,
  derived `f64be:` scalars, exact arithmetic, signed zero, and non-finite
  mappings contain no stale decimal-statistic path.
- For all `m=2..16`, the Williams generator and complete-cycle formulas retain
  balanced cycles and the exact wave/final counts. The final consistency gate
  remains `ceil(0.90*finalBlockCount)` and never weakens 9-of-10.
- Speed orientation is solely baseline/candidate. The only point statistic is
  `exp(median(pairedLogSpeedRatio))`; the 10,000-resample type-1 interval,
  separate wave GREEN/AMBER/RED/BLOCKED law, final
  FASTER/SLOWER/INCONCLUSIVE law, fixed/immediate anchors, required environment
  conjunction, and strict RED/BLOCKED non-win rule remain coherent.
- RNG-v2's raw `u32be`, canonical ASCII `j`, no-delimiter final field,
  bootstrap-v4 seed inputs, and no retry seed are byte-unambiguous apart from
  the totality defect in A7-04.

### Scope and authority

- The sheet remains proposed, authorizes no harness or measurement, preserves
  G-2/PB13G precedence, and carries PB0/Gate-1/Gate-2 boundaries correctly.
- BR-10 still admits only a mutually acknowledged, implemented-and-hardened,
  reproducible uplift. The current BBNF boundary is correctly BLOCKED rather
  than waived, and no pin change is implied.

## 8. Required disposition

Return the packet to harden. The minimal repair should:

1. close every optional authority-proof field for all three authority rows;
2. put live T9 authority after complete raw block evidence but before the
   authority-bound bootstrap/statistics artifact and `STATISTICS_COMPLETE`;
3. publish exact pilot/T9 phase enums and an exhaustive T9 cut/locus/synthesis
   table; and
4. make RNG-v2 termination total without a retry seed or result-conditioned
   redraw.

Any normative-byte change invalidates A7 and B7. Only two new independent
ACCEPT challenges on one exact repaired artifact, followed by root gestalt and
explicit owner ratification, can admit benchmark-harness construction. It
still cannot admit a pilot before the separate T0/T1 freeze packet is GREEN.

---

## Historical reports below — invalidated; zero acceptance credit

# V·π ADDENDA-06 FIFTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A6

status: `REJECT`

date: 2026-07-21

challenged artifact: current fifth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`5d0aa7f8b69824b569960c8366b931d238f324e23083fa05527fd5ed7d2e1cc2`

governing harden raw-byte SHA-256:
`61830d31dad321137356517aabaa4155fbe1a16cfa6b32fbbe2dcc4fc17cf11f`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh zero-credit, assume-faulty challenge of the exact fifth-
amended bytes. Every report below the historical-boundary marker is invalidated
history and grants no acceptance credit. I did not read or communicate with
the concurrent B6 seat. I read the current sheet, research, governing harden,
Charter, and PI; attacked the canonical numeric bytes, pilot and T9 state
machines, receipt graph, pairwise/group partition, batch-plan-v2 topology,
environment isolation, complete Williams cycles, bootstrap-v3, statistics,
wave/final law, owner authority, and uplift boundary. I ran only read-only
hashing and exact arithmetic/state-law checks. No harness, smoke, pilot,
profile, benchmark, or performance measurement ran. This audit ledger is my
only write.

## Current A6 verdict

**REJECT.** The fifth amendment repairs A5's index, precedence, and
round/subject-synthesis ambiguity, and its numeric/grouping/T9 identities are
substantially stronger. It still does not define a total one-shot execution.
There are three concrete blockers:

1. a durable pilot can crash after `PILOT_LAUNCH` but before the first
   `SUBJECT_INTENT`, or between a completed subject and the next subject
   intent, yet the exact failure-locus law assigns no legal `s,k,o` tuple to
   either prefix;
2. live T9 replacement simultaneously requires completion before the next
   intent and directs `BLOCK_REPLACEMENT_INTENT` immediately after
   `BLOCK_INVALID_DETECTED`, without saying whether/where the explicit failed-
   primary `BLOCK_COMPLETE` and replacement `BLOCK_INTENT` occur; and
3. bootstrap-v3 serializes the unbounded rejection counter as `u32be(j)` but
   defines neither `u32be` canonically nor the required disposition at
   `j=2^32`, so the supposedly exact RNG is not total for every digest seed.

The first two defects admit no canonical recovery trace for ordinary durable
prefixes and directly affect permanent INVALID/BLOCKED evidence. The third
leaves a normative statistics preimage undefined. They are formation defects
present in governing harden §10.2/§10.4 and the retained bootstrap law, not
mere writer omissions.

This rejection authorizes no repair, harness construction, smoke, pilot,
measurement, optimization, grammar change, PB0/PB13P work, parse-that pin
change, BBNF coordination, dependency change, or production execution. A
normative repair must proceed through harden, invalidate this A6 report and
the independent B6 report regardless of its verdict, and receive two new
zero-credit challenges.

## 1. Blocking defect A6-5A-1 — the pilot crash function has uncovered prefixes

The sheet calls recovery a total function, but its own locus restrictions
exclude reachable states in the closed WAL.

Consider the exact valid durable prefix containing only event zero,
`PILOT_LAUNCH`, followed by a controller crash and the required proof of no
live owner. No subject, round, or observation intent exists. The precedence
correctly selects `CRASH_OR_INCOMPLETE`, but none of the written locus cases
can encode it:

- `s,k,o` requires an observation intent;
- `s,k,null` requires a committed subject/round locus;
- `s,null,null` is allowed **only** for a crash between a durable
  `SUBJECT_INTENT` and `ROUND_INTENT`, neither of which exists; and
- `null,null,null` is assigned to common-panel derivation failure after all
  subjects select, which is not this state.

The same hole recurs after one selected `SUBJECT_COMPLETE` and before the next
`SUBJECT_INTENT`: no intent is unmatched, not all subjects have selected, and
there is again no legal tuple. Consequently step 2 cannot commit the canonical
`FAILURE_DECISION`; steps 5–6 correctly synthesize nothing, but the failure
receipt's subject/locus fields have no governed values. Choosing all-null,
choosing the next expected `s`, or refusing terminalization each violates a
different sentence. The prefix is reachable without WAL corruption or an
impossible transition, so `HARNESS_SCHEMA_DIGEST_MISMATCH` is not a lawful
escape.

Repair needs an exhaustive phase/locus table that explicitly covers at least
post-launch/pre-first-subject, between subjects, between rounds, between
observations, post-last-subject/pre-panel-derivation, and derivation. It must
state the exact subject ID/empty-string and null tuple for each, while retaining
the no-work-resumption rule.

There is an additional proof-binding omission at the same boundary. Recovery
is authorized only by a durable controller/process termination receipt or a
proof-program restart with no live owner, but neither fact has a closed WAL
event nor a digest field in `pilotFailureReceiptDigest`, `pilotPanelDigest`, or
`pilotTerminalReceiptDigest`. Thus the permanent terminal bytes cannot prove
that recovery authority existed rather than being manufactured while an owner
was live. The corresponding T9 terminal likewise lacks a bound recovery-
authority receipt. A repair should bind the exact authority evidence in the
committed failure decision/terminal chain without introducing a backward
digest edge.

Because pilot failure maps every cell in the activated batch/environment to
`INVALID` and permanently consumes T8, this is a blocking identity and safety
defect.

## 2. Blocking defect A6-5A-2 — live T9 replacement has two incompatible traces

The closed T9 schema has distinct events for `BLOCK_INVALID_DETECTED`,
`BLOCK_REPLACEMENT_INTENT`, `BLOCK_COMPLETE`, and `BLOCK_INTENT`. General law
says every completion/receipt is durable before the next intent. Replacement
law then says that after a complete failed-block receipt is published in
`BLOCK_INVALID_DETECTED`, append `BLOCK_REPLACEMENT_INTENT` and execute the
same block with `a=1`. It does not say whether the primary's explicit
`BLOCK_COMPLETE[b,0,...,"INVALID"]` occurs before replacement intent, nor
whether replacement intent substitutes for or must precede a separate
`BLOCK_INTENT[b,1,...]`.

For one first eligible live invalidity, the text therefore fails to select
between at least these distinct hash chains:

```text
BLOCK_INVALID_DETECTED(b,0,...)
BLOCK_COMPLETE(b,0,...,INVALID)
BLOCK_REPLACEMENT_INTENT(b,1,...)
BLOCK_INTENT(b,1,...)
...
```

and the literal replacement instruction:

```text
BLOCK_INVALID_DETECTED(b,0,...)
BLOCK_REPLACEMENT_INTENT(b,1,...)
[BLOCK_INTENT(b,1,...)?]
...
```

The latter leaves the primary `BLOCK_INTENT` open under the crash law; the
former inserts an event the replacement sequence never mandates. Omitting the
replacement `BLOCK_INTENT` also makes the `a=1` subject/sample actions lack the
block intent that recovery uses to derive an open partial block; including it
without an exact order leaves another digest choice.

The ambiguity changes every later WAL prefix, replacement receipt, statistics
artifact, terminal receipt, and execution receipt for the same samples. It
also makes a crash immediately after replacement intent non-canonical: the
recovery law cannot tell whether it must close the primary, synthesize an
empty replacement block, or record no open block. Repair needs one closed
event-transition table for primary-valid, primary-invalid/replaced,
replacement-valid, replacement-invalid, second-invalid-primary, and crashes
at every boundary. It must say exactly which event publishes each block
receipt and require one controlling `BLOCK_COMPLETE` per attempt before the
next intent.

## 3. Blocking defect A6-5A-3 — bootstrap-v3's rejection stream is not total

The bootstrap law hashes `u32be(r) || u32be(d) || u32be(j)`, but the fifth
numeric schema defines canonical JSON control numbers, `u64be:` strings for
observed counters, and `f64be:` strings for scalars; it never defines the raw
four-byte `u32be` helper. Endianness is suggested by the name, but width,
unsigned range, overflow behavior, and accepted input type are not normative.

More importantly, rejection sampling increments `j` with no cap or terminal
disposition. For supported non-power-of-two block counts, rejection is
possible. If a deterministic seed produces rejected words for
`j=0..2^32-1`, the next required preimage contains `u32be(2^32)`, which has no
four-byte unsigned encoding. Cryptographic unlikelihood is not a proof-law
case and cannot make an exact content-addressed algorithm partial.

Repair can define raw `u32be` exactly and either use an unbounded canonical
counter encoding or freeze a maximum plus a specific terminal statistics
invalidity before overflow. It must keep the seed independent of observed
samples and must not permit a retry seed.

## 4. Independent checks that passed and must survive repair

### Canonical bytes and immutable identities

- The challenged addendum, governing harden, research, Charter, and PI hashes
  above recomputed exactly. The durable P-3 aggregate remains
  `08b927753ee717c67a3031861a88e35e629ecb585d5f84f7e1ba9e4378070be2`;
  P-4 remains
  `437a7f5f305b286e19dd1da79d3a245ff9e39acd62c91cc4eee165468ee757d0`.
- Big-endian IEEE-754 recomputation confirms every published constant:
  `25=4039000000000000`, `50=4049000000000000`,
  `200=4069000000000000`, `1000=408f400000000000`,
  `0.90=3feccccccccccccd`, `0.95=3fee666666666666`, and
  `1.00=3ff0000000000000`. Signed zero is distinct as
  `8000000000000000`.
- The exact published numeric-schema array hashes to
  `46c5aa929714a678f5e7680ccf964d66f2d53a0f551def3e35942a356188135f`.
  Safe schema/control integers, exact `u64be:` observed counters, `f64be:`
  scalars, non-finite invalidity mapping, and current version-2/version-3 tags
  are internally coherent apart from A6-5A-3. No stale batch-plan/1,
  qualification-id/1, qualification-execution/1, pilot-terminal-v1, or
  bootstrap-v2 tag remains normative in the current sheet.

### Pairwise, grouping, plans, and forward graph

- Every candidate×baseline denominator is independently complete, includes
  empty mandatory strata, binds exact eligibility receipts, and cannot become
  a favorable multiway intersection.
- The workload-equivalence tuple is semantics/cost/input complete. Total peer
  ordering rejects duplicate IDs/order bytes. For every `q≥1`, forced slices
  `[15g,min(15(g+1),q))` are duplicate-free, exhaustive, consecutive, and
  maximal; candidate insertion yields exactly `2≤m≤16`. Class, full partition,
  group index/digest, and per-cell identities remain bound through plan,
  qualification, run, schedule, activation, evidence, history, and close.
- `batchPlanDigest` version 2 contains no ID-derived order. The successful-path
  graph is acyclic and forward-only:
  T0 plan/review → T2 causal receipt → T3 portable qualification → T4
  environment run → T5 order → T6 schedule receipt → T7 activation →
  T8 pilot terminal → T9 execution. Schedule and activation bind derived
  order without feeding T0/plan/runner bytes. Pilot and T9 terminal/final-WAL
  formulas have no digest cycle.
- Causal source transitions, GENESIS/ABSENT sentinels, raw hypothesis bytes,
  reviewed bundle supersession, qualification-kind separation, frozen required
  environment conjunction, and no-pooling/report-only law remain correct.

### Williams, statistics, and verdict law

The written base-row/rotation/reversal generator was recomputed for every
supported `m`. Every even cycle contains each directed first-order pair once;
every odd doubled/reversed cycle contains each directed pair twice. Exact
counts are:

| `m` | cycle | wave blocks | final blocks | final wins required |
|---:|---:|---:|---:|---:|
| 2 | 2 | 6 | 10 | 9 |
| 3 | 6 | 6 | 12 | 11 |
| 4 | 4 | 8 | 12 | 11 |
| 5 | 10 | 10 | 10 | 9 |
| 6 | 6 | 6 | 12 | 11 |
| 7 | 14 | 14 | 14 | 13 |
| 8 | 8 | 8 | 16 | 15 |
| 9 | 18 | 18 | 18 | 17 |
| 10 | 10 | 10 | 10 | 9 |
| 11 | 22 | 22 | 22 | 20 |
| 12 | 12 | 12 | 12 | 11 |
| 13 | 26 | 26 | 26 | 24 |
| 14 | 14 | 14 | 14 | 13 |
| 15 | 30 | 30 | 30 | 27 |
| 16 | 16 | 16 | 16 | 15 |

All cycles are complete. Speed orientation is solely baseline median divided
by candidate median; the sole aggregate is
`exp(median(pairedLogSpeedRatio))`; the 10,000-resample type-1 interval uses
positions 250/9,750; and wave anchor states remain distinct from final
peer-cell states. `ceil(0.90*finalBlockCount)` yields the table above. Semantic
eligibility precedes timing, and no RED/BLOCKED/non-win is converted to GREEN.

### Authority and uplift

The proposal remains non-executable and correctly requires two fresh ACCEPTs,
root gestalt, every owner decision, a separate GREEN freeze packet, Gate 1 for
PB0, Gate 2 for PB1+/PB13P, and a separate parse-that pin decision. The BBNF
boundary remains acknowledgement-gated and BLOCKED; no uplift is imputed from
task status or remote measurements. OC-1A/B remains a closed owner choice.

## 5. Three-altitude adjudication

### Total tranche / gestalt

The semantics-first, complete-pairwise, finite-peer continuous rail is the
right fit for a full readable CSS prototype, and the fifth amendment's durable
WAL direction is preferable to optional reruns. The tranche would be less
credible, not more, if a rare crash or replacement boundary could choose its
own receipt history. A small closed transition table plus canonical recovery-
authority binding is the KISS repair; it does not justify weakening one-shot
execution, complete CSS scope, or peer-beating close.

### Packet / wave

The addendum faithfully transposes governing harden §10, including the three
residual defects. It is mechanically GREEN on immutable identities, source
bytes, numeric constants, complete pairwise ownership, maximal 15-peer
partition, environment/run separation, forward schedule topology, digest
acyclicity, complete Williams cycles, statistics, wave/final states, owner
gates, and uplift boundary. It is RED on total pilot recovery, exact live T9
replacement serialization, and total bootstrap-v3 encoding. Because these
bytes control permanent attempt consumption and verdict evidence, the packet
cannot receive E-3 acceptance.

### Feature / rule

- **BR-1–BR-4:** immutable lanes, sealed builds, causal identity, pairwise
  eligibility, grouping, and corpus anti-gaming law are GREEN.
- **BR-5:** create-before-observation, one-shot IDs, numeric observations,
  precedence, deterministic open-round/subject synthesis, and terminal digest
  acyclicity are GREEN. Post-launch/between-subject pilot crash loci and live
  T9 replacement event order are RED; recovery-authority evidence is unbound.
- **BR-6:** median-log aggregate, complete schedules, orientation, interval,
  winning-block threshold, and distinct wave/final laws are GREEN. The
  bootstrap-v3 seed is pre-observation and correctly bound, but its `u32be(j)`
  stream is partial and therefore RED.
- **BR-7–BR-9:** proportionate schedule, profile/resource separation, strict
  predecessor/anchor closure, environment conjunction, and OC-1A/B are GREEN.
- **BR-10:** acknowledgement and independent local proof remain mandatory;
  no uplift is presently admissible, which is GREEN policy and RED external
  coordination status.

---

## Invalidated challenge history — zero acceptance credit

All content below this marker predates challenged SHA-256
`5d0aa7f8b69824b569960c8366b931d238f324e23083fa05527fd5ed7d2e1cc2`.
It is retained only as an audit trail and grants no current acceptance credit.

# V·π ADDENDA-06 FOURTH-AMENDMENT FRESH ADVERSARIAL CHALLENGE A5

status: `REJECT`

date: 2026-07-21

challenged artifact: current fourth-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`67eb829e69a6f9c8613924a6d62ff37d2136c249c428aa2c81d1b6bfb2a126ba`

governing harden raw-byte SHA-256:
`c8c47202482c8d790e340b879b7bd2efa0e30625c801e381f969e34f27e115f5`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh zero-credit, assume-faulty challenge of the exact fourth-
amended bytes. Every report below is invalidated history and grants no
acceptance credit. I did not read or communicate with the concurrent B5 seat.
I read the research, governing harden chain, Charter, and PI law; traced the
T0→T9 graph and every forward exclusion; challenged the create-before-
observation pilot and canonical receipt family; recomputed the complete
Williams schedules for every supported `m=2..16`; and rechecked causal,
pairwise, grouping, environment, statistic, wave/final, owner, uplift, and
authority law. No harness, smoke, pilot, profile, benchmark, or performance
measurement ran. This audit ledger is my only write.

## Current A5 verdict

**REJECT.** The fourth amendment fixes the former T2→T3 prohibition and
repeatable-pilot defect, but the terminal pilot failure identity is still not
canonical. One durable observation prefix can validly encode more than one
`pilotFailureReceiptDigest`: the three numeric indices have no declared base
or derivation; concurrent invalidities have no priority rule; and the phrases
"where unavailable" / "when finalizable" do not mechanically decide whether
the failing round and subject digests must be present. Because the failure
receipt feeds the terminal panel, this ambiguity changes `pilotPanelDigest`,
the permanent `PILOT_INVALID` history, and every bound BLOCKED cell without any
change to observations. That is a structural identity defect, not editorial
debt.

This rejection authorizes no repair, harness construction, smoke, pilot,
measurement, optimization, grammar change, PB0/PB13P work, parse-that pin
change, BBNF coordination, dependency change, or production execution. A
normative repair must proceed through harden, invalidate this A5 report and
the independent B5 report regardless of its verdict, and receive two new
zero-credit challenges.

## 1. Blocking defect A6-4A-1 — terminal pilot failure bytes are not unique

The packet rightly makes pilot launch create-before-observation and terminal,
but its failure schema does not define a total function from one durable event
prefix to one canonical receipt.

### 1.1 Index bases and derivations are missing

`pilotFailureReceiptDigest` includes
`failingSubjectOrderIndexOrNull`, `failingRoundIndexOrNull`, and
`failingObservationIndexOrNull`. The prose introduces a subject index `s` and
adaptive exponent `k=0`, but never states that the encoded subject index is
zero-based, that `roundIndex = k`, or whether the observation index is zero-
or one-based. Thus the same failure on the first observation of the first
subject at `pilotN=1` admits, among others, `[0,0,0]` and `[1,1,1]`; both are
safe non-negative integers, and neither violates the written envelope. The
digest differs.

The repair needs exact index law: zero-based indices are the natural choice;
`subjectOrderIndex` must equal the position in `pilotSubjectOrder`,
`roundIndex` must equal `k` and satisfy `pilotN=2^roundIndex`, and
`failingObservationIndex` must equal the zero-based position in the terminal
round's ordered observation prefix. It must also prescribe JSON `null` for
non-observation failures such as exhaustion after seven OK observations and
derivation failure after all subjects select.

### 1.2 Simultaneous invalidities have no deterministic precedence

The enum says an observation has exactly one code, but no priority maps an
event satisfying more than one predicate. A signaled process can also lack a
finite duration and leave an incomplete observation; an environment-class
change can coincide with a frozen-threshold breach; a harness mismatch can
coincide with process failure. Choosing `PROCESS_SIGNAL`,
`TIMER_OR_DURATION_NONFINITE`, `FROZEN_THRESHOLD_BREACH`, or
`CRASH_OR_INCOMPLETE` changes both the observation union and failure digest.
Enum listing order is not declared to be precedence.

The repair needs an exhaustive detection/precedence table, including the
supervisor/controller-crash path, so every observable combination emits one
and only one `pilotInvalidityCode`. It must preserve the dedicated
`PILOT_N_EXHAUSTED` and `PILOT_DERIVATION_INVALID` mappings and must not turn a
process signal/nonzero exit into generic incomplete merely because the panel
was not committed.

### 1.3 Crash-prefix inclusion remains discretionary

For a crash before a committed round receipt, the sheet says subject/digest
fields and the failing-round array are empty "where unavailable," while the
failing subject receipt is included "when finalizable." It never gives a
mechanical availability/finalizability predicate. An executor that reconstructs
a terminal round from the durably appended observation prefix and an executor
that leaves `terminalPilotRoundDigestOrEmptyString` empty can both claim
compliance. The same discretion affects
`failingSubjectReceiptDigestOrEmptyString` and the failing-subject fields.

The repair must define the append-log identity and a deterministic recovery
algorithm: exactly which durable prefix fields are mandatory, when a terminal
round/subject receipt is synthesized, and the sole cases that use empty string
or JSON `null`. Raw controller metadata beside a receipt cannot cure a hash
that omits mechanically available normative observations.

Because `pilotPanelDigest` binds the ambiguous failure receipt and the panel
then binds `qualificationExecutionId` eligibility, bootstrap/history/close
receipts, and the permanent INVALID/BLOCKED mapping, A6-4A-1 blocks acceptance.

## 2. Fourth-amendment checks that passed

The following challenged laws are coherent and require preservation through
repair:

- The exact stage chain is acyclic:
  `T0→T1→T2→T3→T4→T5→T6→T7→T8→T9`. Each no-backedge
  exclusion applies only to later values and permits the required immediately
  forward dependency. T0 contains plans, not derived IDs/order; T5 derives the
  environment-bound order; T6 seals it; T7 activates; only T8-valid opens T9.
- `pilotAttemptId` binds one exact run and activation. Atomic durable create
  precedes clock read, warmup, semantic observation, and subject launch.
  `LAUNCHED`, `PILOT_VALID`, and `PILOT_INVALID` are terminal against restart;
  neither a valid nor invalid pilot may repilot or choose another `N`.
- Subject order is the first T6 Williams row; subjects are sequential fresh
  processes; adaptive rounds are the predetermined `2^0..2^30` sequence with
  seven observations and no round/process restart. The common-`N` and ordered
  flag rules retain the frozen nearest-power/tie-smaller, short-window, and
  too-slow behavior.
- A valid panel uniquely creates `qualificationExecutionId`; T9 uses exactly
  its common `N`. Bootstrap seed v2 binds execution, panel, plan, exact
  pairwise denominator, and both candidate/baseline bundles before the
  unchanged unbiased SHA-256 counter stream.
- `PILOT_INVALID` starts no T9 work and maps to existing `INVALID`,
  `WAVE_BLOCKED`, and final evidence-BLOCKED. The one whole-block invalidity
  replacement remains limited to T9 after a valid pilot; it cannot replace a
  pilot panel.
- Causal source/genesis/hypothesis bytes, complete independent pairwise
  denominators, subject-specific peer cells, byte-identical grouping,
  qualification-kind separation, environment conjunction, one valid T9
  batch, and history preservation remain intact.

## 3. Williams, statistics, and verdict recomputation

The written generator produces a complete balanced cycle for every supported
subject count. The exact counts remain:

| `m` | cycle | wave blocks | final blocks | final wins required |
|---:|---:|---:|---:|---:|
| 2 | 2 | 6 | 10 | 9 |
| 3 | 6 | 6 | 12 | 11 |
| 4 | 4 | 8 | 12 | 11 |
| 5 | 10 | 10 | 10 | 9 |
| 6 | 6 | 6 | 12 | 11 |
| 7 | 14 | 14 | 14 | 13 |
| 8 | 8 | 8 | 16 | 15 |
| 9 | 18 | 18 | 18 | 17 |
| 10 | 10 | 10 | 10 | 9 |
| 11 | 22 | 22 | 22 | 20 |
| 12 | 12 | 12 | 12 | 11 |
| 13 | 26 | 26 | 26 | 24 |
| 14 | 14 | 14 | 14 | 13 |
| 15 | 30 | 30 | 30 | 27 |
| 16 | 16 | 16 | 16 | 15 |

Every cycle is untruncated. The sole speed orientation remains
`baselineMedianDuration / candidateMedianDuration`; the sole point aggregate
is `exp(median(pairedLogSpeedRatio))`; the bootstrap uses exactly `n` complete
blocks and type-1 positions 250/9,750. Wave anchor states and final peer states
remain disjoint and exhaustive. OC-1A/B remains a closed owner choice, and no
registered-peer non-win becomes GREEN.

## 4. Three-altitude adjudication

### Total tranche / gestalt

The semantics-first, finite-peer, pairwise rail still fits the owner goal and
must remain. The new one-shot pilot is the right anti-optional-stopping design,
but a content-addressed proof system cannot leave terminal failure bytes to
implementation convention. Repairing the receipt mapping is narrower and
more optimal than weakening terminality or adding a replacement attempt.

### Packet / wave

The packet faithfully transposes harden §9, including its residual ambiguity;
therefore the confirmed defect is in the controlling formation law as well as
the addendum. The packet is mechanically GREEN on the stage topology,
one-launch rule, complete schedules, common `N`, bootstrap-v2, and terminal
BLOCKED mapping, but it fails the canonical-identity and exhaustive-failure
ledger. It cannot receive E-3 acceptance until harden defines that law and the
writer transposes it.

### Feature / rule

- **BR-1–BR-4:** immutable lanes, sealed registry/causal identity, pairwise
  semantic denominators, anti-gaming corpus freeze, Phase-A/PB0 ownership, and
  environment isolation are GREEN.
- **BR-5:** durable launch and one-shot execution are GREEN; failure receipt
  indices, precedence, and crash-prefix inclusion are RED.
- **BR-6:** common `N`, execution binding, bootstrap-v2, aggregate, complete
  cycles, distinct wave/final states, and invalid-panel BLOCKED mapping are
  GREEN only after BR-5's receipt bytes become unique.
- **BR-7–BR-9:** proportionate schedule, resource/profile separation, strict
  finite predecessor/anchor closure, environment conjunction, and OC-1A/B are
  GREEN.
- **BR-10:** no uplift is presently admissible; acknowledgement, independent
  local proof, crossable cells, and separate E-3 pin authority remain correct.

## 5. Required disposition

1. Append a narrow controlling harden amendment that makes every pilot failure
   event/prefix map to one canonical index/code/digest tuple.
2. Repair only the corresponding normative pilot receipt and recovery text in
   `ADDENDA-06.md`; retain all fourth-amendment protections.
3. Treat both A5 and B5 as zero-credit after any changed normative byte and
   obtain two fresh independent challenges of the new artifact.
4. Only after two ACCEPTs may root run gestalt and surface §13's six owner
   decisions. No owner decision or old audit cures this structural defect.

---

The reports below are preserved historical challenges against superseded
bytes. They grant no acceptance credit to the current or any later artifact.

# V·π ADDENDA-06 THIRD-AMENDMENT FRESH ADVERSARIAL CHALLENGE A4

status: `ACCEPT`

date: 2026-07-21

challenged artifact: current third-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`878b841782265ecc7c0dc4009d5f01789a0cc264d20757b17a547f1baf7abbe4`

governing harden raw-byte SHA-256:
`04a2bbb11269d242a173c5f2f8d08ab555fd655fcc80466655486c5cdfb639fa`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

charter raw-byte SHA-256:
`6a80a698fc6501b9f52904e3fddc4540775509f16b802bd900a7527600c9c817`

Phase-A sheet raw-byte SHA-256:
`fa83ca19532ba0fce9ff5e7ea3a75b0b1b82f1354b6c752bcb153ced2f25404d`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh zero-credit, assume-faulty challenge of the exact third-
amended bytes. Every earlier A report below is invalidated history and conferred
no acceptance credit. I did not read or communicate with the concurrent other
fresh-challenge seat. I read the complete research and governing harden chain
plus the Charter and PI law; mechanically traced the canonical identities and
T0→T7 graph;
expanded the Williams schedules for every supported subject count; and
recomputed the durable local evidence identities. No harness, smoke, pilot,
profile, benchmark, or performance measurement ran. This audit ledger is my
only write.

## Current A4 verdict

**ACCEPT.** The third amendment fully discharges the documented causal-byte,
pairwise-denominator, qualification/batch-plan, and schedule-topology defects.
The current sheet has one canonical source-delta encoding, owns every
candidate×baseline denominator independently (including excluded rows and
empty mandatory strata), permits physical grouping only for byte-identical
work, separates wave and final plans/runs, keeps the runner schema invariant,
and derives the environment-bound schedule only after the reviewed causal
receipt and portable/run identities exist. I found no stale conflicting prose
that reintroduces a multiway denominator, an ID cycle, an unreviewed retry, a
partial Williams cycle, an alternate aggregate, or a wave/final state leak.

This is an E-3 design acceptance only. `ADDENDA-06` remains **PROPOSED · NOT
RATIFIED** and authorizes no benchmark construction, smoke, pilot,
qualification, measurement, optimization, grammar change, PB0/PB13P work,
parse-that pin change, BBNF coordination, dependency change, or production
execution. A second fresh independent ACCEPT, root gestalt, and every explicit
owner decision in §13 remain prerequisites; even owner ratification opens only
harness construction, while §11's reviewed T0→T7 freeze must become GREEN
before any smoke or pilot.

## 1. Canonical identities and causal anti-gaming

The causal identity is now uniquely encodable:

- `changedPaths` uses the exact three-member JSON row with JSON `null` as the
  sole absence sentinel; literal `"GENESIS"` is the sole typed non-digest
  exception for both prior identities.
- Paths are non-empty workspace-relative POSIX strings, forbid every named
  ambiguous segment/byte form, sort strictly by raw UTF-8 bytes, and occur
  exactly once. Addition, deletion, and unequal modification are the only
  valid transitions.
- The hypothesis digest covers the exact stored strict-UTF-8 bytes, including
  any BOM or newline bytes, without normalization.
- Initial and later candidate transitions have disjoint, closed type laws.
  Bundle-only, unreachable, administrative, formatting-only, and reviewed
  performance-neutral deltas open no receipt or new draw.
- `candidateCausalReceiptDigest` binds the delta and the mechanical, two-
  challenge, and root-disposition digests. Every initial or superseding
  candidate goes through the same pre-pilot review class, and old valid
  verdicts remain in history.

The portable `qualificationId` version 2 binds the reviewed causal receipt,
frozen environment vector, exact pre-reviewed batch plan and peer-cell map,
subject/build/runtime/invariant-runner/statistics identities, and harness
bundle. `qualificationRunId` then binds exactly one environment class. The
one-valid-run law is scoped to that run ID; required environments close
conjunctively, while samples never pool or compete across classes.

## 2. Complete independent pairwise denominators

The current BR-3/BR-9 composition is mechanically complete:

1. Every predecessor, fixed anchor, and immediate anchor receives a separate
   candidate×baseline eligibility enumeration over every relevant selected-
   bank row.
2. Each included and excluded row has a content-addressed eligibility receipt
   binding both subjects' semantic/result and cost-boundary evidence. Omission
   is structural RED.
3. Mandatory strata remain present even with an empty row list, making
   `NO_COMMON_DENOMINATOR` deterministic instead of allowing the stratum to
   disappear.
4. `pairwiseDenominatorDigest` binds the exact common cost boundary, ordered
   strata, traversal rows/inputs, projection, and joins;
   `peerCellManifestDigest` additionally binds the subject-specific complete
   eligibility receipt.
5. Baselines share a Williams batch only after byte identity of the denominator
   and every named workload/cost/projection/join property is proved. Grouping
   cannot shrink another cell's pairwise work, and each cell retains its own
   peer identity in raw, statistical, history, and close artifacts.

This closes the former multiway-intersection route. `INCOMPARABLE`,
`NOT_APPLICABLE`, and an empty mandatory denominator retain only their terminal
meanings and cannot be converted into peer wins.

## 3. Acyclic plan, schedule, and activation proof

The exact dependency graph now evaluates without a fixed point:

```text
T0 frozen causal/build/schema/artifact/pairwise/batch-plan/environment bytes
  → T1 mechanical + 2× semantic challenge + root disposition
  → T2 candidateCausalReceiptDigest
  → T3 qualificationId v2
  → T4 qualificationRunId
  → T5 ID-derived relabelling and complete Williams sequence
  → T6 batchScheduleDigest + mechanical verification
  → T7 batchActivationReceiptDigest
  → smoke/pilot authority
```

No T2–T7 value may feed the reviewed inputs, batch plan, invariant
`runnerSchemaDigest`, challenge/root digests, causal receipt, or portable ID.
The pre-GREEN packet contains the subject set, pairwise manifests, `m`, cycle
and block counts, qualification kind, and invariant order algorithm, but no
ID-derived order or schedule/activation receipt. The post-GREEN verifier alone
derives and seals the complete order; callers cannot choose it. Wave and final
are distinct `qualificationKind` values with distinct batch plans, block counts,
portable/run IDs, schedules, and one-valid-run scopes.

## 4. Schedule, statistic, and state recomputation

I expanded the written Williams generator for every `m=2..16`. Every row is a
permutation. Even cycles place each subject once in each position and each
directed first-order carryover once; odd doubled/reversed cycles do both twice.
The exact complete-cycle results are:

| `m` | cycle | wave blocks | final blocks | final wins required |
|---:|---:|---:|---:|---:|
| 2 | 2 | 6 | 10 | 9 |
| 3 | 6 | 6 | 12 | 11 |
| 4 | 4 | 8 | 12 | 11 |
| 5 | 10 | 10 | 10 | 9 |
| 6 | 6 | 6 | 12 | 11 |
| 7 | 14 | 14 | 14 | 13 |
| 8 | 8 | 8 | 16 | 15 |
| 9 | 18 | 18 | 18 | 17 |
| 10 | 10 | 10 | 10 | 9 |
| 11 | 22 | 22 | 22 | 20 |
| 12 | 12 | 12 | 12 | 11 |
| 13 | 26 | 26 | 26 | 24 |
| 14 | 14 | 14 | 14 | 13 |
| 15 | 30 | 30 | 30 | 27 |
| 16 | 16 | 16 | 16 | 15 |

The sole direction is
`baselineMedianDuration / candidateMedianDuration`; the sole cross-block
point is `exp(median(pairedLogSpeedRatio))`. The deterministic 10,000-resample
SHA-256 counter stream, unbiased rejection reduction, exact `n` from the full
cycle, type-1 positions 250/9,750, and `ceil(0.90*n)` final consistency law
remain coherent. Wave anchor cells use only the exhaustive
`WAVE_GREEN/AMBER/RED/BLOCKED` law; final peer cells independently use
`FASTER/SLOWER/INCONCLUSIVE` plus the closed evidence/registry states. Neither
law can relax the other.

## 5. Recomputed historical evidence and narrow claims

Read-only local recomputation reproduced:

| artifact | SHA-256 / value |
|---|---|
| P-3 aggregate | `08b927753ee717c67a3031861a88e35e629ecb585d5f84f7e1ba9e4378070be2` |
| P-4 diagnostic report | `437a7f5f305b286e19dd1da79d3a245ff9e39acd62c91cc4eee165468ee757d0` |
| recoverable LIVE bundle | `c73c8ff2ca2da3ed9bcd7b2016ef00aba0416a79d1787d88943fc3ab0bdccad0` |
| recoverable deposed bundle | `6c62368f6b4e6c1c28e6c0542bc86004c243fba6bb6d70ac0bba8435a81618eb` |
| recoverable C14 bundle | `2b57626fff0975276f7d46d84ffc01835bc83bfe4f28ce6b5855622f6a22f0d6` |
| `demo/styles/animations.css` | `a64eb64fc847b1f1feffe49fd8b211f2aa335f43ceeac8bb626095e89968444b`; 12,821 UTF-8 bytes; 12,748 UTF-16 units |

All five written T0 corpus identities also match the sealed P-3 aggregate.
These facts prove only the current sheet's narrow historical claims. They do
not make the temporary rig durable and are not a V·π benchmark.

## 6. Three-altitude adjudication

### Total tranche / gestalt

The two-lane, semantics-first, finite-peer rail remains optimal for the owner
goal: it can falsify rather than promise peer beating, attributes cost to the
module-owned operation, and refuses to trade complete CSS scope, correctness,
idiomatic parse-that, or readable grammar for a number. Phase-A T1 owns its
resource closure; PB0/Gate 2 separately owns Phase-B T2/T4. The repaired
identity graph and pairwise law make that design implementable without hidden
human choices or comparator dilution.

### Packet / wave

The current sheet faithfully transposes BR-1 through BR-10 and controlling
harden §6–§8. It remains proportionate: one shared pre-measurement freeze,
append-only reviewed amendments, non-gating author smoke, accepted-boundary
wave qualification, and final predecessor tournaments. Exact P-3 remains
quarantined; P-4 is only a historical diagnostic report. Every owner choice is
explicit, especially OC-1A/B and durable artifact retention.

### Feature / rule

- **BR-1/BR-2:** immutable lane separation, exact registry/environment/causal
  identities, and one-valid-environment-run law are GREEN.
- **BR-3/BR-4:** complete independent pairwise semantics, acyclic T1
  selection, anti-gaming, Phase-A resource ownership, T3 freeze, and PB0
  Phase-B ownership are GREEN.
- **BR-5/BR-6:** pilot, common `N`, observer boundary, invalid whole-block
  replacement, complete schedules, median-log aggregate, bootstrap, and
  exhaustive wave/final laws are GREEN.
- **BR-7/BR-8:** proportionate replay, profile separation, deterministic work,
  limits, scale, and honest `REPORT_ONLY` memory are GREEN.
- **BR-9:** finite LIVE/deposed/C14 peers, exact per-cell denominators,
  two-anchor debt, required-environment conjunction, strict RED/BLOCKED non-win
  law, and closed OC-1A/B are GREEN.
- **BR-10:** no uplift is admissible today. Future attribution is local to an
  unchanged crossable grammar row, requires mutual content-addressed
  acknowledgement and independent local replay, preserves the 1.0.0 vector,
  and still needs separate E-3 pin authority.

## 7. Required disposition

1. Preserve these exact challenged bytes and this A4 acceptance.
2. Obtain one independent fresh acceptance against the same raw SHA-256.
3. Root performs the E-3 gestalt adjudication.
4. Surface all six §13 owner decisions; silence authorizes none.
5. Even after ratification, construct only the prototype harness and freeze
   packet. No smoke or pilot starts until the packet receives its 1+2 reviews,
   root GREEN, and T2→T7 identities/activation.

---

The reports below are preserved historical challenges against superseded
bytes. They grant no acceptance credit to the current or any later artifact.

# V·π ADDENDA-06 SECOND-AMENDMENT FRESH ADVERSARIAL CHALLENGE A

status: `REJECT`

date: 2026-07-21

challenged artifact: current second-amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`8fc37af444cc10f75b4b2c58dbfd6f8adf700e458325bdf048d3a62c2a6846be`

governing harden raw-byte SHA-256:
`457ff5cf0117d9072336cd8f329a13f9083de15acb167c11be44b3a2605cf827`

research raw-byte SHA-256:
`1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a new independent assume-faulty challenge of the second-amended bytes.
The earlier A reports below are invalidated history and conferred no acceptance
credit. I did not read or communicate with the other current challenge seat. I
performed read-only digest, identity-graph, schedule, and statistical-law
checks; no harness, pilot, profile, benchmark, or performance measurement ran.
This audit ledger is my only write.

## Current verdict

**REJECT.** The second amendment correctly transposes the causal-source,
environment-cohort, complete-cycle, median-log aggregate, and separate
wave/final predicates in isolation. However, the composed pre-measurement
identity graph is circular and therefore cannot produce the exact reviewed
freeze, causal receipt, qualification/run identity, and order receipt that the
sheet makes prerequisites to a pilot. The causal-delta envelope also leaves
added/deleted-path encoding and path uniqueness open, so it is not yet the
claimed canonical source-delta identity.

These are design defects, not implementation requests. `ADDENDA-06` remains
PROPOSED and NOT RATIFIED and authorizes no benchmark construction, pilot,
measurement, optimization, grammar change, PB0/PB13P work, parse-that uplift,
coordination, dependency change, or production execution.

## 1. Blocking defect A6-2A-1 — reviewed receipt/order identity is circular

The following normative dependency cycle is present in the current sheet:

```text
freeze-packet challenge/root digests
  -> candidateCausalReceiptDigest                 (§3 and §11)
  -> qualificationId                             (§3)
  -> qualificationRunId                          (§3)
  -> orderSeed -> complete order sequence         (§6)
  -> freeze packet / runnerSchemaDigest           (§6 and §11)
  -> freeze-packet challenge/root digests
```

The exact conflicting requirements are:

1. §3 defines `candidateCausalReceiptDigest` from the mechanical-check,
   challenge-A, challenge-B, and root-disposition digests. Those digests exist
   only after the §11 packet has been frozen and reviewed.
2. `qualificationId` includes that causal receipt and `runnerSchemaDigest`;
   `qualificationRunId` includes `qualificationId`.
3. §6 seeds the subject relabelling and complete Williams order from
   `qualificationRunId`.
4. §6 then requires the complete order sequence to seal into
   `runnerSchemaDigest` **and** the pre-timing batch receipt, while §11 item 6
   requires the scheduled order sequence inside the packet reviewed before the
   causal receipt exists.

No ordinary SHA-256 construction can satisfy that graph by direct evaluation.
It is not merely a path-order or implementation-choice issue: both the causal
receipt and runner-schema digest are inputs to values that the pre-causal
review is required to contain. The closing sentence in §11 that receipt/ID
formation is a post-GREEN mechanical act does not cure the cycle because the
order sequence is both derived from that post-GREEN ID and required in the
pre-GREEN reviewed bytes.

The repair needs one acyclic ownership rule. For example: make
`runnerSchemaDigest` identify only the invariant order algorithm; freeze and
review the sorted scheduled subject artifacts, `m`, cycle length/count, and
environment cohort but not the derived order; after GREEN, form the causal
receipt and IDs, mechanically derive the order, and bind it to a separate
post-review `batchScheduleDigest`/run receipt. Any equivalent construction must
show a topological evaluation order and must not put an ID-derived sequence
back into an input digest of that ID.

This defect blocks BR-2, BR-5, §11, the no-retry identity, and every later
qualification claim. It is sufficient by itself for REJECT.

## 2. Defect A6-2A-2 — the causal path-delta envelope is not fully canonical

The causal envelope defines each changed-path row as:

```text
[repoRelativePosixPath, oldSourceSha256OrAbsent, newSourceSha256OrAbsent]
```

but never defines the JSON value for `Absent`. The global canonical rules
require digest strings to be lowercase 64-hex, yet an added or deleted file
necessarily has one non-digest side. `null`, `"ABSENT"`, an empty string, and
field omission produce different bytes and are all compatible with the prose.
The sheet also sorts rows by path but does not require path uniqueness, so one
manifest may repeat a path with contradictory transitions and still have a
well-formed digest.

Define one exact sentinel and require exactly one row per normalized path,
with valid transitions (at least one side a digest, and old/new not equal).
Also bind `causalHypothesisDigest` explicitly to the raw UTF-8 hypothesis bytes
rather than leaving its derivation implicit. This closes both canonical
reproduction and the mechanical anti-gaming check.

## 3. Second-amendment transposition checks that passed

I found no defect in these repaired predicates considered independently:

- `qualificationId` includes the causal receipt and close-environment vector;
  environment-specific order/bootstrap use `qualificationRunId`; samples are
  not pooled and every required environment is conjunctive.
- `speedRatio = baselineMedianDuration / candidateMedianDuration` is used with
  the correct faster-than-one orientation. The sole point statistic is the
  exponentiated median paired log ratio.
- The deterministic 10,000-resample SHA-256 counter stream, unbiased modulo
  reduction, log-space median, and type-1 positions 250/9,750 remain exact.
- Wave cells use only `WAVE_GREEN`, `WAVE_AMBER`, `WAVE_RED`, or
  `WAVE_BLOCKED`; final peer cells independently use `FASTER`, `SLOWER`, or
  `INCONCLUSIVE` plus the closed evidence/registry states.
- A source delta must be causally reviewed; bundle-only, administrative,
  unreachable, formatting-only, or performance-neutral changes do not open a
  draw. Old verdicts remain history.
- The modular proof layout follows grammar ownership and thin joins; it forbids
  an omnibus adapter, duplicated grammar/scanners, semantic normalization, and
  benchmark-only fast paths.
- BR-10 admits no uplift today and retains parse-that 1.0.0 unless an
  acknowledged implemented-and-hardened artifact passes local semantics,
  attribution, E-3, and owner authority.

## 4. Exhaustive Williams/count recomputation

I expanded the stated base row, modular translations, and odd-`m` reversals for
every supported `m=2..16`. Every row is a permutation. Even cycles give every
subject every position once and every directed carryover pair once; odd cycles
give each twice. The second-amendment count formulas reproduce:

| `m` | cycle | wave blocks | final blocks | final wins required |
|---:|---:|---:|---:|---:|
| 2 | 2 | 6 | 10 | 9 |
| 3 | 6 | 6 | 12 | 11 |
| 4 | 4 | 8 | 12 | 11 |
| 5 | 10 | 10 | 10 | 9 |
| 6 | 6 | 6 | 12 | 11 |
| 7 | 14 | 14 | 14 | 13 |
| 8 | 8 | 8 | 16 | 15 |
| 9 | 18 | 18 | 18 | 17 |
| 10 | 10 | 10 | 10 | 9 |
| 11 | 22 | 22 | 22 | 20 |
| 12 | 12 | 12 | 12 | 11 |
| 13 | 26 | 26 | 26 | 24 |
| 14 | 14 | 14 | 14 | 13 |
| 15 | 30 | 30 | 30 | 27 |
| 16 | 16 | 16 | 16 | 15 |

Thus the complete-cycle arithmetic itself is GREEN; the blocker is the
cycle's impossible placement in the receipt/digest dependency graph.

## 5. Reproduced local identities and evidence altitude

Read-only SHA-256 recomputation reproduced:

| artifact | SHA-256 |
|---|---|
| P-3 aggregate | `08b927753ee717c67a3031861a88e35e629ecb585d5f84f7e1ba9e4378070be2` |
| P-4 report | `437a7f5f305b286e19dd1da79d3a245ff9e39acd62c91cc4eee165468ee757d0` |
| recoverable LIVE bundle | `c73c8ff2ca2da3ed9bcd7b2016ef00aba0416a79d1787d88943fc3ab0bdccad0` |
| recoverable deposed bundle | `6c62368f6b4e6c1c28e6c0542bc86004c243fba6bb6d70ac0bba8435a81618eb` |
| recoverable C14 bundle | `2b57626fff0975276f7d46d84ffc01835bc83bfe4f28ce6b5855622f6a22f0d6` |

The durable aggregate also retains the five stated T0 corpus digests and the
12,748 UTF-16-unit versus 12,821 UTF-8-byte `sheet-demo` distinction. These
checks support the sheet's deliberately narrow historical claims only. They
do not make the temporary rig reproducible and do not constitute a V·π
measurement.

## 6. Three-altitude adjudicative summary

### Total tranche

The two-lane, semantics-first, finite-peer, module-owned rail is optimal in
shape and remains subordinate to G-2/PB13G. The circular identity graph would,
however, stop the entire tranche at the freeze boundary or force a harness to
invent an undocumented fixed-point/ordering exception. Repairing the graph now
is materially cheaper and safer than discovering it after harness authorship.

### Packet/wave

The second amendment discharges all four previously reported B blockers in its
local formulas, but it does not form an executable BR-2/BR-5/§11 whole. A new
normative repair changes the challenged bytes and requires two entirely new
independent challenges before root gestalt.

### Feature/rule

BR-1, BR-3, BR-4, BR-7, BR-8, BR-9, BR-10, OC-1A/B, the historical identities,
semantic projection law, common-denominator law, environment conjunction,
complete-cycle arithmetic, median-log aggregate, and separate wave/final
predicates survived challenge. BR-2/BR-5/§11 fail on the receipt/order cycle;
the causal-source sub-envelope additionally fails canonical added/deleted-path
encoding.

## 7. Required disposition

1. Break the causal-receipt/qualification/run/order/runner-schema/freeze-packet
   cycle with an explicit acyclic identity graph.
2. Define the absence sentinel, changed-path uniqueness/transition law, and
   exact hypothesis digest.
3. Recompute the sheet SHA and preserve this REJECT as invalidated history.
4. Restart two independent challenges on the repaired bytes; only two fresh
   ACCEPT verdicts permit root gestalt.

---

The reports below are preserved historical challenges against superseded
bytes. They grant no acceptance credit to the current or any later artifact.

# V·π ADDENDA-06 FRESH ADVERSARIAL CHALLENGE A — repaired continuous benchmark rail

status: `ACCEPT`

date: 2026-07-21

challenged artifact: current amended `ADDENDA-06.md`

challenged raw-byte SHA-256:
`58580b1bad90b336cd6d58767da6f1a45df917e02f5d2685239e1d69d57e145f`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This is a fresh independent assume-faulty challenge of the repaired sheet. I
gave no acceptance credit to the earlier challenge, did not read or contact
challenge B, and recomputed the current artifact and recoverable evidence from
the filesystem. I ran only digest, corpus-accounting, and schedule-algebra
checks—no benchmark, pilot, profile, or performance measurement. I did not edit
the challenged addendum, harness, grammar, production, Phase-B, parse-that,
BBNF, coordination, inbox, or other artifact. This report is my only write.

## Fresh verdict

**ACCEPT.** The repaired bytes fully discharge the four blockers from the
first A challenge and the complete controlling repair set. The duration ratio
now has one correct orientation; the bootstrap, pilot, Williams schedule,
median, quantile, non-finite, and invalidation laws are executable; T1
selection is canonical and acyclic; qualification identity cannot be changed
nominally to retry a valid loss; all cell outcomes are closed; exact historical
artifacts and ABI failure have blocking dispositions; P-4 is described only at
its supportable altitude; OC-1 is a closed owner choice; uplift attribution is
local to crossable grammar rows; and the pre-pilot freeze has its own required
reviews.

This acceptance is an **E-3 design verdict only**. `ADDENDA-06` remains
PROPOSED and NOT RATIFIED. It authorizes no harness, pilot, measurement,
optimization, grammar change, PB0/PB13P work, parse-that pin change, or
production execution. A second fresh independent ACCEPT, root gestalt, and
explicit owner decisions in §13 are still required. Owner ratification would
authorize harness construction only; §11 must then become GREEN before even a
pilot.

## 1. Fresh identity and historical-evidence recomputation

Current raw bytes independently reproduce these identities:

| artifact | independently recomputed SHA-256 |
|---|---|
| amended `ADDENDA-06.md` | `58580b1bad90b336cd6d58767da6f1a45df917e02f5d2685239e1d69d57e145f` |
| `formation/bench-rail-harden.md` | `7854d945fd9fb4ee4ff3fb540866cec85f9b23be8f21d1beb27e9f39f29ddb79` |
| `formation/bench-rail-research.md` | `1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9` |
| durable P-3 aggregate | `08b927753ee717c67a3031861a88e35e629ecb585d5f84f7e1ba9e4378070be2` |
| durable P-4 report | `437a7f5f305b286e19dd1da79d3a245ff9e39acd62c91cc4eee165468ee757d0` |

The amendment history correctly preserves the initial sheet's recorded
identity
`3df662d2fff1fabb1fd6bf2a5f2f81532e8568a30bc5e0614aa679d00664fd0b`
and both first REJECT challenges as non-credit-bearing history. Those old bytes
are not the current challenged artifact; the former A report remains preserved
below this fresh report.

The three recoverable temporary bundles independently reproduce the finite
initial peer registry:

| subject | independently recomputed SHA-256 |
|---|---|
| LIVE regex | `c73c8ff2ca2da3ed9bcd7b2016ef00aba0416a79d1787d88943fc3ab0bdccad0` |
| deposed pre-v4 parse-that | `6c62368f6b4e6c1c28e6c0542bc86004c243fba6bb6d70ac0bba8435a81618eb` |
| C14 parse-that 1.0.0 | `2b57626fff0975276f7d46d84ffc01835bc83bfe4f28ce6b5855622f6a22f0d6` |

The exact T0 inputs also reproduce the five cited corpus identities:

| scenario | independently recomputed SHA-256 |
|---|---|
| `value-historical` | `27d54ed8ea6b809a4d1e60063274b0aa00ba2fbd172dc8f712d27e3102bcdf09` |
| `sheet-historical` | `3c85ecaf55dd5e25e8658b95553d890bac57d452f61f926bbb5c3343f90bce55` |
| `sheet-demo` | `a64eb64fc847b1f1feffe49fd8b211f2aa335f43ceeac8bb626095e89968444b` |
| `value-common` | `b6a3c77f3986efd0d882c10b16e8707a5ff31e606a34c79d238e9180655906e5` |
| `sheet-common` | `ab43f1dcc6bcfba1d9558e50947587f1a4deb5def4885272a9ab41db07889b4f` |

The raw demo file remains 12,821 UTF-8 bytes but decodes to 12,748 JavaScript
UTF-16 code units. BR-1 retains that historical `.length` behavior only under
`legacy_code_units_per_second`; qualification counts raw ingress UTF-8 bytes.
The P-3 aggregate also confirms Node 26.0.0/V8 14.6, Darwin 25.4.0, Apple M5
Max/18 cores, five invocations, fifteen samples, `N=1000`, and 50 warmups.
These recomputations make §0–§2 **GREEN without promoting the old temporary rig
to a reproducible baseline**.

## 2. Correct speed direction and exhaustive outcome law

BR-6 now defines, exactly once:

```text
speedRatio = baselineMedianDuration / candidateMedianDuration
pairedLogSpeedRatio = log(speedRatio)
```

A faster candidate therefore produces `speedRatio > 1`. The interval,
9-of-10 final condition, `<0.95` regression threshold, and both anchor clauses
all bind to that orientation. There is no surviving inverted normative formula
in the repaired addendum; throughput ratios are corroborating only.

The nine states are exhaustive for a qualification cell: `FASTER`, `SLOWER`,
`INCONCLUSIVE`, `INCOMPARABLE`, `NO_COMMON_DENOMINATOR`, `NOT_APPLICABLE`,
`UNAVAILABLE_ARTIFACT`, `UNSUPPORTED_ABI`, and `INVALID`. Only `FASTER` passes
a mandatory eligible peer cell. Performance RED is kept distinct from evidence
BLOCKED, while terminal registry dispositions cannot be presented as wins.
Pilot and schema failures occur before a valid qualification cell exists and
cannot be smuggled into `INCONCLUSIVE` or retried as a valid loss.

## 3. Canonical qualification identity and no nominal retry

The qualification envelope is a compact ordered JSON array that binds the
public operation, candidate bundle, UTF-8-sorted subject/artifact registry
projection, subject/build/runtime/runner/statistics/scenario/harness digests,
all scheduled peers, and both scheduled anchors. Digest strings, JSON numbers,
raw bytes, and hexadecimal encodings have closed canonical forms. Timestamps
remain receipts rather than attempt identity, while the actual environment is
bound to the frozen runtime schema.

Administrative metadata, paths/layout, result logging, comments with unchanged
bundle bytes, retry seeds, requested order, and unrelated fixture appends cannot
create another attempt. Supersession is limited to changed candidate bytes, a
versioned verdict-affecting schema change with a pre-run causal hypothesis and
§11 review, or an independently frozen E-3 semantic/corpus denominator change.
Every valid earlier verdict and both identities remain in the close history.
This closes optional stopping and nominal-digest retry without preventing a
real code repair or governed semantic evolution.

## 4. T1 acyclic selection, owner lattice, and resource closure

The repaired T1 law is mechanically complete:

- five closed maturity values, three result classes, four operation classes,
  three global UTF-8 size bands, and mandatory/secondary status;
- a compact ordered row envelope binding operation, exactly one non-registry
  grammar owner, accepted wave, fixture source, production, semantics, affected
  thin joins, and resource disposition;
- collision RED, exact deduplication, a stratum-owned complete-bank digest,
  and `selectionKey = SHA256(literal || bankDigest || rowDigest)`;
- unsigned key/row/canonical-byte tie order, all rows at `≤1024`, exactly 1,024
  above the ceiling, and a selected-bank digest retaining selection order; and
- scenario-manifest construction only after selection, so no digest seeds
  itself.

The complete bank stays coverage evidence; the selected frozen bank is the
peer denominator under the ceiling. Additions, removals, and reclassification
require E-3 and preserve superseded identities. Ordinary semantic rows are
mandatory; only root can freeze a secondary reason. The sheet therefore gives
neither feature nor harness authors a favorable-subset lever.

The pre-pilot Phase-A lattice owns T1 rows and cells, grammar-owner modules,
thin joins, semantic projections, selected and complete banks, and one
operation-specific resource record. Deterministic work, limits/`limit+1`, and
complexity are mandatory at perfection; profile, scale, and retained-memory
dispositions must be frozen. `REPORT_ONLY` cannot masquerade as memory GREEN.
PB0 owns only the occurrence-derived Phase-B T2/T4 duties after Gate 1 and does
not retroactively own Phase A. This is a genuine module-aligned lattice, not an
omnibus adapter or a cosmetic BBNF directory.

T3's whole registry, mandatory strata, provenance, declared goals, and oracle
classification likewise freeze before any pilot. T4 preserves deterministic
semantic scale/hostile/limit work and refuses random-gibberish or replication
headlines. The T0–T4 anti-gaming and operation/size/maturity ownership laws are
**GREEN**.

## 5. Exact pilot, order, bootstrap, median, and numeric failure law

The pilot is now reproducible: each subject begins at `pilotN=1`, obtains
exactly seven observations after semantic check and frozen warmup, doubles
until the finite positive median reaches 25 ms, and fails at `pilotN > 2^30`
or any non-finite/non-positive duration. Pilot observations never enter the
qualification sample. `dMax`, `x=200/dMax`, nearest power-of-two `N` on the
closed `[2^0,2^30]` set, smaller-power tie rule, and shared-`N` flags fix the
previous “toward 200 ms” discretion. Too-slow and short-window subjects remain
visible; the runner may not split the corpus or choose a more flattering `N`.

The median is exact for odd and even counts. Durations, derived ratios, host
`log`, and host `exp` must be finite, and durations must be positive; violation
invalidates the whole matched block. Node/V8 is pinned by the runtime schema.

I independently expanded the stated Williams construction for every supported
subject count `m=2..16`. Every row was a permutation; even `m` produced `m`
rows with every directed carryover pair exactly once, and odd `m` produced the
forward-plus-reverse `2m` cycle with every directed pair exactly twice. The
qualification identity seeds only a relabelling; it does not change that
balance. Each block executes every subject once before advancing.

The paired bootstrap closes every formerly delegated choice: ordered JSON seed,
fixed SHA-256 counter framing, big-endian `u32` counters, first-eight-byte
unsigned word, rejection sampling at `floor(2^64/n)*n`, no library RNG, exactly
10,000 resamples, exact even/odd median, type-1 one-based positions 250 and
9,750, log-space selection, then exponentiation. Five-block wave and ten-block
final cohorts are explicit, with 9-of-10 applying only to final qualification.
There is no harness-author choice capable of moving a boundary verdict.

## 6. Observer boundary, invalidation, artifacts, ABI, and P-4

Each subject/scenario/block runs in a fresh process, with one opaque
module-scoped sink inside the clock and semantic/result/diagnostic projections
immediately outside it. The runner retains raw samples, order, pilot, common
`N`, warmup, sink, exits, and environment receipts. It may invalidate only for
the closed process, schema/digest, semantic-pre/post, numeric/timer,
environment-class, or pre-frozen observable-threshold reasons. No individual
subject or inspected outlier is deleted. One whole-block retry is allowed only
for invalidity; the second makes the cell `INVALID`. A valid loss never retries
under the same identity.

Exact observed bundles must be copied and verified or reconstructed byte for
byte before the first pilot. A differently digested rebuild is an appended
peer, not a replacement. Missing bytes yield `UNAVAILABLE_ARTIFACT`; existing
bytes that cannot execute on the frozen ABI yield `UNSUPPORTED_ABI`; both block
strict close. This makes the finite predecessor objective falsifiable without
silently dropping history.

P-4 is now accurately limited to a durable historical diagnostic report backed
by currently recoverable temporary inputs. It does not claim durable driver,
analyzer, commands, manifest, or six raw profiles, and its broad timing/sample
summary is not generalized per file. Old self-time and estimated gains may
motivate new content-addressed profiles but cannot become close gates. The
P-3/P-4 artifact and ABI treatment is **GREEN**.

## 7. OC-1, two anchors, strict close, and cost/KISS

OC-1 is closed and mutually exclusive. OC-1A keeps strict peer GREEN and both
unchanged P-3 floors conjunctive; OC-1B keeps the same compatibility run and
reports both floors as non-gating while the strict peer matrix controls. No
silence, recalibration, omitted compatibility lane, or UTF-16-to-byte relabel
selects either choice.

Both fixed first-accepted and immediate accepted anchors rerun freshly in the
candidate's matched blocks on the identical selected-frozen manifest. AMBER is
explicit debt and cannot renew a 5% budget; perfection requires it closed. The
strict LIVE/deposed/C14 common-semantic matrix passes only when every mandatory
eligible cell is `FASTER`; missing, incomparable, invalid, or losing evidence
cannot be averaged away. New L4 doors make no invented LIVE comparison.

The rail is necessarily substantial because the owner requested a falsifiable
complete-CSS, all-predecessor speed claim, but its schedule is proportionate:
non-gating author smoke, one post-semantic wave qualification, transitive
replays only for shared-core changes, and historical tournaments at
graduation. SHA-256 supplies selection, order, and bootstrap determinism without
another statistics dependency. Profiles and memory runs are outside timing.
The proof tree separates generated manifests/raw data from authored LOC, keeps
one adapter per public operation, and forbids duplicated grammar, hidden
evaluators, semantic-normalizing shims, or corpus fast paths. The authority,
KISS, modularity, and cost boundaries are **GREEN**.

## 8. Parse-that uplift and communication boundary

BR-10 admits only an artifact whose package/commit/API/batch digest both sides
acknowledge as implemented and hardened, which is independently reproducible
and passes all local semantic, diagnostic, no-throw, limit, result, profile,
and performance rails. The current BBNF task has acknowledged no such artifact,
so uplift admission and communication remain **BLOCKED, NOT WAIVED**.

Engine attribution is now explicitly local to each grammar row where both
engines compile the identical grammar unchanged and reproduce the same
operation, cost boundary, and projection. A non-crossable current grammar
supports neither a current-row engine effect nor a full interaction. A
crossable last-accepted grammar may report only its own engine effect; it may
not donate that effect to the uplift-only current grammar, which stays a named
combined cohort. Adoption still needs a separate E-3 pin decision and retains
the 1.0.0 control. Owner-mediated communication is limited to acknowledged
identities and local classified evidence, with no source inference,
cross-write, or remote-number substitution. This repair is **GREEN**.

## 9. Pre-pilot freeze and remaining authority

The finite packet before any pilot contains runner/statistics schemas, complete
predecessor/artifact/ABI dispositions, the complete Phase-A T1 owner/resource
lattice and banks, any measured-cohort T3 registry, every qualification/build/
runtime/harness identity and invalidation threshold, and reconstruction/digest
receipts. It receives one structural check, two independent semantic/
anti-gaming challenges, and root disposition; a normative byte change resets
both. Only GREEN opens smoke, pilot, or qualification. Append-only E-3
amendments avoid repeating ceremony per door.

The repaired §13 exposes all remaining owner choices: continuous rail, strict
close, exact statistics and identity/no-retry law, exactly one OC-1 choice,
freeze/PB0/retention, and uplift authority. Gate 1 still controls PB0; Gate 2
still controls PB1+/PB13P; PB13G remains correctness authority; and any engine
pin has its own E-3. The authority chain is closed and contains no implicit
ratification.

**Fresh challenge A: ACCEPT.** These exact bytes may proceed to the other fresh
independent challenge and, only if it also ACCEPTs unchanged bytes, root E-3
gestalt and explicit owner decision. No implementation or measurement follows
from this report.

---

## Preserved first challenge history — superseded bytes, no acceptance credit

The report below is the original A challenge of raw-byte SHA-256
`3df662d2fff1fabb1fd6bf2a5f2f81532e8568a30bc5e0614aa679d00664fd0b`.
Its `REJECT` verdict remains historical evidence of the repairs required; it
does not describe or grant credit to the amended bytes accepted above.

# V·π ADDENDA-06 ADVERSARIAL CHALLENGE A — continuous benchmark rail

status: `REJECT`

date: 2026-07-21

challenged artifact: `ADDENDA-06.md`

challenged raw-byte SHA-256:
`3df662d2fff1fabb1fd6bf2a5f2f81532e8568a30bc5e0614aa679d00664fd0b`

model_served: inherited Codex GPT-5 family route; the service-side backend
revision and an Opus/Fable label were not exposed to this seat, so this receipt
claims neither.

This was an independent assume-faulty challenge. I read the controlling
`formation/bench-rail-harden.md` and underlying historical evidence, but did
not read or contact the other addendum auditor. I did not run a performance
measurement, edit the challenged sheet, or change any implementation,
production, Phase-B, parse-that, BBNF, coordination, or inbox artifact. This
report is my only write.

## Verdict

**REJECT.** The sheet authenticates the old P-3 evidence, quarantines its
UTF-16-unit defect, defines a strong common-semantic peer rule, preserves
correctness and modularity over speed, assigns PB0/PB13P correctly, and grants
no premature code authority. It nevertheless cannot be ratified as an exact
measurement law for four independently blocking reasons:

1. BR-6 defines the paired observation as
   `candidateMedian / baselineMedian` over timed durations, but awards
   `FASTER` when the ratio is greater than one. That declares a slower
   candidate faster and reverses the regression gates.
2. The claimed "frozen paired-bootstrap method" does not pin an RNG, seed
   framing, unbiased index derivation, even-sample median, or percentile
   convention. Those choices are postponed to the future harness, leaving
   material verdict authority with its implementer after ratification.
3. T1 says to select the first 1,024 rows by ascending SHA-256 "using" a seed,
   but defines no seeded hash or tie/encoding rule. The seed consequently has
   no executable role, and two conforming authors can freeze different
   denominators.
4. BR-10 first permits an engine-only effect at the last accepted grammar when
   that grammar crosses both engines, then says any non-crossable cell forbids
   an engine-gain attribution. The scope of that prohibition is ambiguous and
   can either erase a valid paired effect or falsely extend it to an
   un-crossable grammar.

These are gate defects, not requests to weaken the strict peer objective. The
ratio direction defect is already present in the controlling harden report, so
both that normative input and this sheet require correction. Any normative
repair changes the challenged artifact and requires two fresh independent
challenges before root gestalt and owner ratification.

## 1. Artifact and historical identity recomputation

The challenged sheet and its two formation inputs independently hash as
follows:

| artifact | independently recomputed SHA-256 |
|---|---|
| `ADDENDA-06.md` | `3df662d2fff1fabb1fd6bf2a5f2f81532e8568a30bc5e0614aa679d00664fd0b` |
| `formation/bench-rail-harden.md` | `4afeff15cfda94e478caef4f3eba7175a3beaefad2b7f9613ccb2d13d9bafa51` |
| `formation/bench-rail-research.md` | `1062bff0f193eddeb2fe5810cf2ebfd27f2ed0a4cd4cf2df43f63902370f58c9` |

The durable aggregate raw bytes reproduce its cited identity exactly:

| evidence | independently recomputed SHA-256 |
|---|---|
| `../parser-proof/bench-results.json` | `08b927753ee717c67a3031861a88e35e629ecb585d5f84f7e1ba9e4378070be2` |

The surviving temporary subject bundles also reproduce every cited observed
bundle identity:

| subject | independently recomputed SHA-256 |
|---|---|
| LIVE regex | `c73c8ff2ca2da3ed9bcd7b2016ef00aba0416a79d1787d88943fc3ab0bdccad0` |
| deposed pre-v4 parse-that | `6c62368f6b4e6c1c28e6c0542bc86004c243fba6bb6d70ac0bba8435a81618eb` |
| C14 parse-that 1.0.0 | `2b57626fff0975276f7d46d84ffc01835bc83bfe4f28ce6b5855622f6a22f0d6` |

Those hashes authenticate observed bundles, not reproducible reconstruction
closures. BR-2 correctly refuses to promote them until source, adapter, lock,
build, bundle, and environment identities become durable.

## 2. T0 corpus and UTF-16/UTF-8 defect reproduction

I rebuilt the four in-source corpora independently from the recovered P-3
runner. Array-corpus hashes use the exact UTF-8 bytes of
`JSON.stringify(array)`; the sheet corpora use the exact source string bytes.
The legacy size column is the sum of JavaScript string `.length` values, as in
the recovered runner.

| T0 scenario | hash envelope | legacy code units | independently recomputed SHA-256 |
|---|---|---:|---|
| `value-historical` | compact JSON array | 185 | `27d54ed8ea6b809a4d1e60063274b0aa00ba2fbd172dc8f712d27e3102bcdf09` |
| `sheet-historical` | raw string | 315 | `3c85ecaf55dd5e25e8658b95553d890bac57d452f61f926bbb5c3343f90bce55` |
| `sheet-demo` | raw file text | 12,748 | `a64eb64fc847b1f1feffe49fd8b211f2aa335f43ceeac8bb626095e89968444b` |
| `value-common` | compact JSON array | 59 | `b6a3c77f3986efd0d882c10b16e8707a5ff31e606a34c79d238e9180655906e5` |
| `sheet-common` | compact JSON array | 201 | `ab43f1dcc6bcfba1d9558e50947587f1a4deb5def4885272a9ab41db07889b4f` |

For `demo/styles/animations.css`, decoding the exact raw file as UTF-8 yields
12,748 JavaScript UTF-16 code units while the raw file is 12,821 UTF-8 bytes.
The difference is exactly reproduced, and the old aggregate indeed labels the
12,748 `.length` result as bytes. BR-1 correctly preserves that defective
calculation only in the named P-3 compatibility lane, renames its reported unit
`legacy_code_units_per_second`, and requires raw ingress UTF-8 bytes for new
qualification claims. This quarantine is **GREEN**.

The remaining recovered mechanics also match the sheet: fixed subject order,
`N=1000`, 50 warmups before each sample, fifteen samples, five invocations,
normalizer after the subject scenarios, and peak-based legacy ratios. Keeping
those historical oddities out of qualification is correct.

## 3. Blocking BR-6 direction defect

BR-5 times elapsed duration. BR-6 then defines each subject's block statistic
as the median of those timed samples and writes:

```text
log(candidateMedian / baselineMedian)
```

Its `FASTER` rule requires the resulting ratio and lower confidence bound to
be greater than `1.00`, with at least nine of ten block ratios greater than
`1.00`. This is mathematically inverted for durations. For a baseline median
of 100 ns and candidate median of 200 ns, the written ratio is 2.0 and the
candidate can satisfy all three speed predicates while taking twice as long.
Likewise a candidate slowing from 100 ns to 110 ns produces 1.10, so the
written `<0.95` regression rule does not see the slowdown.

The surrounding law proves the intended orientation: larger is meant to mean
faster, `<0.95` is meant to mean a regression, and BR-9 requires `FASTER`.
Therefore the parsimonious correction is not to invert every threshold. It is
to define a named speed ratio consistently as:

```text
speedRatio = baselineMedianDuration / candidateMedianDuration
pairedLogSpeedRatio = log(speedRatio)
```

Every reported "ratio" in BR-6 must be renamed or explicitly bound to that
definition. The `>1.00`, lower-bound `>1.00`, 9-of-10, `<0.95`, upper-bound
`<1.00`, fixed-anchor, and immediate-anchor clauses may then retain their
current orientation. The controlling harden BR-6 lines carrying the inverted
formula must be repaired in the same E-3 amendment; correcting only the writer
sheet would leave it in conflict with its stated controlling source.

## 4. Blocking bootstrap under-specification

The matched-block design itself is sound: one block contains every eligible
subject, order rotates by a manifest-seeded balanced schedule, medians are
formed within blocks, comparisons pair only within blocks, pilots are
excluded, and invalidation replays the complete block. Ten final blocks,
fifteen samples per subject, a paired interval, and the independent 9-of-10
condition are materially stronger than the old five unpaired invocations.

The sheet nevertheless asks the owner to ratify a **frozen** bootstrap while
stating that the future harness will pin the RNG. It also leaves these
verdict-bearing details undefined:

- how the five variable-width seed fields are framed before SHA-256;
- how the 256-bit digest initializes the RNG;
- the RNG algorithm and version;
- how random words map without modulo bias to one of ten paired blocks;
- whether the median of an even sample is the mean of its two center values or
  one center order statistic; and
- which exact percentile/quantile convention selects the two 95% bounds from
  10,000 bootstrap medians.

Those are not harmless implementation details: different legitimate choices
can move a boundary cell between `FASTER` and `INCONCLUSIVE`. They are hidden
measurement authority and contradict owner decision 3.

An exact repair must freeze the complete transform in the sheet before either
audit reruns. One sufficient, dependency-free definition is:

1. encode the seed tuple as UTF-8 compact JSON of the ordered array
   `[schemaVersion, scenarioManifestDigest, candidateBundleDigest,
   baselineBundleDigest, "bootstrap-v1"]`, then SHA-256 those bytes;
2. use SHA-256 counter mode as the normative random stream, with each draw
   hashing the seed digest plus fixed-width unsigned big-endian resample, draw,
   and rejection counters;
3. map a 64-bit word to `[0,n)` by rejection sampling, not `% n` with bias;
4. for each of exactly 10,000 resamples, draw `n=10` paired block log-speed
   ratios with replacement and define the even median as the arithmetic mean
   of sorted positions 5 and 6 (one-based);
5. sort the 10,000 bootstrap medians and define the type-1 percentile
   `Q(p)=x[ceil(10000p)]` in one-based notation, hence bounds `x[250]` and
   `x[9750]`; and
6. exponentiate only after selecting both log-space bounds.

Another fully specified deterministic method is acceptable, but "the future
harness must pin" is not. The seed must also include any verdict-relevant
qualification schema/build/runtime configuration identity or bind those
identities transitively through the scenario manifest; the sheet must say
which.

The pilot rule needs the same mechanical precision. "Toward 200 ms" and
"where feasible" do not determine one power-of-two `N` when one common `N`
cannot place every subject in the 50–1,000 ms window. Freeze a deterministic
selection and failure/reporting rule before timing; the runner may not choose
the most flattering feasible interpretation after pilot observation.

## 5. T0–T4 anti-gaming and no-retry challenge

### T0

T0 is exact, immutable, historically scoped, and cannot be misrepresented as
full-CSS evidence. **GREEN.**

### T1

The production/maturity/result/size stratification, separation of accept,
reject, recovery, and serialization work, pre-timing freeze, retained full-bank
digest, 1,024-row ceiling, and prohibition on post-timing sampling are sound.
The selection sentence is not executable, however: sorting by ordinary row
SHA-256 does not use the claimed scenario-manifest seed.

The repair must define one exact candidate byte envelope and one exact seeded
ordering key, for example:

```text
rowDigest = SHA256(canonicalRowBytes)
selectionKey = SHA256(
  UTF8(JSON.stringify([scenarioManifestDigest, rowDigest, "t1-select-v1"]))
)
```

Sort by unsigned `selectionKey` bytes, break the impossible-but-defined tie by
unsigned `rowDigest` bytes and then canonical row bytes, and take exactly the
first 1,024. The scenario-manifest digest must be computed from a pre-selection
manifest that does not recursively contain the selected rows or its own final
digest. Alternatively remove the seed claim and sort solely by a fully defined
row digest. The present hybrid cannot stand.

### T2

T2 correctly remains RED until PB0 binds exact reviewed occurrences to public
operations, expected semantics, owners, and dispositions. Generic unknown
acceptance earns no typed-L4 credit. **GREEN as a gated contract.**

### T3

Path/commit/raw/decoded hashes, provenance, goal, oracle result, exclusions,
per-file reporting, and UTF-8 weighting close the main real-source gaming
routes. The amended sheet should say explicitly that the entire T3 registry
and every mandatory stratum freeze before inspecting candidate timing, just as
T1 does; owner approval alone is not a timing-independent selection algorithm.
This is a required clarity repair, not authority to add a corpus now.

### T4

Named deterministic transforms, semantic validity, geometric sizes, limits and
`limit+1`, hostile operators, multiple denominators, and PB0-owned work/memory
budgets are correctly separated from headline throughput. Random gibberish and
replicated trivial tokens cannot manufacture a win. **GREEN as a gated
contract.**

### No retry

The complete-block invalidation and prohibition on retrying a valid batch are
correct in principle. The escape "code, configuration, or corpus change with a
new digest" is too broad unless the identity and supersession rules are made
exact. A harmless flag/configuration change could otherwise buy a new draw,
and a post-RED corpus change could erase the losing denominator.

Define the qualification identity as a canonical tuple including at least the
candidate bundle, subject registry, build schema/flags, runtime flags,
scenario/corpus manifest, harness, and statistics-schema digests. A new
candidate-code bundle may open a new attempt. A build/runtime/statistics or
corpus change may open a differently named experiment only after its already
required pre-timing amendment/receipt; it must retain the prior valid verdict
and cannot supersede a frozen gate silently. An unchanged qualification tuple
never reruns merely because it lost.

## 6. Semantic peer, strict-close, and two-anchor challenge

BR-3 is one of the sheet's strongest sections. It requires the same public
operation and cost boundary, spec-correct result class, a lossless common
semantic projection without a hidden parser/evaluator, symmetric inclusion of
result construction/serialization, and eligibility freeze before timing.
Every eligible row in every mandatory stratum forms the denominator; rejects,
recoveries, parse, and serialization are different cells. Deep-freeze cost is
not subtracted from LIVE. This is **GREEN**.

BR-9 correctly seals the finite evidenced predecessor set as LIVE, deposed,
and C14 rather than promising comparisons with unidentified history. It
requires `FASTER` on every comparable mandatory cell, reports absent or
different operations rather than fabricating L4 baselines, and makes a
zero-row denominator non-passing. Correctness, CSS completeness, readability,
and modularity remain non-negotiable even if the strict speed claim stays RED.
This strict-peer meaning is **GREEN once BR-6 is repaired**.

The fixed-first-accepted plus immediately-previous accepted anchors close the
cumulative 4.9% decay loophole. To remove one remaining implementation
ambiguity, the amended wording should state that both preserved anchor bundles
are freshly executed on the candidate's identical frozen scenario manifest;
an old measurement from a superseded corpus is not silently compared with a
new one. AMBER debt reappears at graduation and cannot be renewed. The
two-anchor policy is otherwise **GREEN**.

The schedule is proportionate: smoke is non-gating, one qualification occurs
at semantic acceptance, dependency changes trigger only affected replays, and
the full predecessor tournament waits for W7. T2/T4 scale and memory gates
remain PB0-priced instead of being invented now. The strict final matrix may
be expensive, but its cost follows the owner's complete-CSS and peer-beating
objective rather than measurement theater.

## 7. PB0, PB13P, modular structure, and authority

Ownership is correctly bounded:

- Phase-A T1 and W7 can exist independently of PB0.
- PB0 alone freezes occurrence-derived T2/T4 dispositions, deterministic work,
  limits, complexity, memory budgets/report-only reasons, joins, profiles, and
  the benchmark fields that enter the derived Gate-2 packet.
- No PB0 benchmark work executes before Phase-B Gate 1.
- PB13P remains provisional and cannot execute before the separate PB0-derived
  Gate 2 or waive PB13G.
- Memory stays `REPORT_ONLY` until PB0 ratifies an operation-specific budget.

The durable `mirror/bench/{manifest,corpus,adapters,runners,reports}` structure
matches grammar ownership without creating a grammar mirror inside the
harness. Adapter restrictions forbid fixture fast paths, evaluator logic,
hidden catches, duplicated grammar, and semantic normalization. Raw evidence,
reconstruction recipes, manifests, and profiles become content-addressed and
path-independent. Authored harness LOC remains separate from generated data.
This structure is readable, modular, non-contrived, and **GREEN**.

The top banner, §0, §12, and §13 withhold harness code, measurements,
optimization, grammar changes, PB0, PB13P, pin changes, production execution,
and performance verdicts until the full E-3 sequence and exact owner choices.
Ratification would authorize only prototype benchmark machinery/evidence, with
the separate Phase-B and engine-pin gates intact. There is no hidden code
authority. There **is** hidden measurement discretion in BR-5/BR-6/T1 as
described above; that is why this challenge rejects.

## 8. Uplift crossable-cell and communication challenge

BR-10 correctly admits no current uplift: the active BBNF task has not
acknowledged a mutually content-addressed package/commit/API/batch whose status
is explicitly implemented and hardened. Proposals, private observations,
branches, status claims, and remote numbers cannot change the 1.0.0 pin. Any
future artifact must reproduce local semantic, diagnostic, no-throw, limit,
result, profile, and performance evidence, then receive a separate E-3 pin
decision. Communication remains owner-mediated and contains identities and
classified evidence, not private source or cross-writes. **GREEN.**

The factorial rule is also right in substance: a full grammar×engine effect
and interaction require all four semantically identical cells. The final
sentence is overbroad relative to the preceding one. Repair it to state:

- an engine effect may be attributed only within a grammar row for which the
  old-engine and uplift-engine cells both compile unchanged, expose the same
  operation/cost boundary, and pass the same projection;
- if the current grammar cannot cross to 1.0.0, no engine effect at the current
  grammar and no full factorial interaction may be claimed;
- a crossable last-accepted grammar row may still report its own paired
  old-versus-uplift engine effect, but that effect cannot be imputed to the
  uplift-only current grammar; and
- the uplift-API current grammar remains a separately named combined cohort.

That preserves the harden intent and makes "a non-crossable cell forbids an
engine-gain attribution" local to the missing comparison rather than globally
self-contradictory.

## 9. OC-1 and exact repair set

OC-1 is correctly left to one explicit owner choice: either VALUE `0.0500` and
SHEET `0.1000` remain an additional compatibility close gate, or they become a
separately reported legacy calibration after the strict peer gate. The sheet
does not silently recalibrate, delete, or promote those peak/normalizer floors
to modern UTF-8 qualification evidence. **GREEN.**

Before another challenge, the formation must make this exact repair set:

1. Correct the paired duration orientation in controlling harden BR-6 and
   addendum BR-6 to `baselineDuration / candidateDuration`; bind every ratio,
   interval, 9-of-10, regression, and two-anchor clause to that definition.
2. Freeze the entire bootstrap algorithm and pilot-`N` rule in the normative
   sheet, including canonical seed framing, deterministic random stream,
   unbiased resampling, median, quantile, and failure conventions. Do not defer
   any verdict-affecting choice to the harness author.
3. Define one exact T1 candidate serialization and seeded ordering algorithm,
   or remove the unused seed claim; also state the pre-selection manifest
   digest construction so it is non-recursive.
4. Bind no-retry to a canonical qualification identity; require pre-timing
   authority for config/corpus/statistics changes and retain every prior valid
   verdict rather than silently superseding a loss.
5. State that T3 freezes before candidate timing and that both fixed/immediate
   anchors are rerun on the identical current frozen manifest.
6. Clarify BR-10's crossable-row rule exactly as in §8, preserving the valid
   old-grammar paired effect while forbidding attribution at a non-crossable
   grammar or of a nonexistent full interaction.
7. Preserve without weakening: the exact T0 identities; UTF-16 compatibility
   quarantine; complete common-semantic denominator; all mandatory strata;
   9-of-10 and no-retry strictness; both anchors and AMBER-debt closure; PB0/
   PB13P gates; finite predecessor registry; correctness-first law; modular
   proof tree; no benchmark-only fast path; OC-1 owner choice; and separate
   E-3 parse-that pin authority.
8. Record the amended sheet digest and obtain two fresh independent ACCEPT
   challenges. This rejection provides no acceptance credit for later bytes.

Until those repairs survive the full E-3 route, `ADDENDA-06` remains
**PROPOSED, NOT RATIFIED**, no benchmark harness or V·π qualification run is
authorized, and the only valid performance claims remain the historical P-3/
P-4 observations.
