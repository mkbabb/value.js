# V·π ADDENDA-03 — independent E-3 adversarial challenge B

2026-07-21 · exact target SHA-256
`c83270db3d84dc34e92d2e815fa2756470c6fe33e128f638bc82c10494882c76`
· posture: assume faulty, prove otherwise · code edits: none

Independence: this pass did not read, contact, or consume the other challenge.
It read both sealed W1 audits, the focused repair receipt, the W1 research and
harden formation, the proposed addendum, the ratified W1 authority, the
proposed Phase-B PB1 boundary, the current prototype, LIVE, and the pinned
primary source objects.

Model receipt: inherited Codex audit seat; the exact backend alias was not
exposed to this seat.

## Verdict

**REJECT.** The scope, ownership, PB1 sequencing, R14–R18 rulings, surface
locks, and replay plan are otherwise sound, but the R13 ledger contains a
confirmed false current-result row. The natural trailing unterminated-comment
witnesses `foo/*` and `foo /*` are accepted by both LIVE and the mirror value
door as slash lists, not rejected as the table says. `fn(a/*)` adds a second
placement-dependent split: LIVE accepts a slash-list argument while the mirror
rejects. The addendum cannot be ratified with a current LIVE/mirror column that
erases the exact hostile behavior R13/PB1 must repair.

This verdict challenges the proposal only. It does not authorize a repair,
semantic code, W1, PB1, or any deferred GREEN credit. Repairing the R13 ledger
is semantic evidence work and restarts both addendum challenges.

## 1. Total-tranche / gestalt analysis

### Scope and optimality

The addendum is the smallest safe response to the two W1 audits:

- D-W1-1–D-W1-8 repair observable Phase-A contract and proof defects without
  widening any export, AST, diagnostic union, or result shape.
- R14, R15, and R16 are genuinely W1-sized. They reuse the shared numeric leaf,
  narrow R4 to the grammar arm that actually carries `[0,100]`, and classify a
  complete-input compatibility boundary without pretending to tokenize CSS.
- R13 and R17 cannot be repaired safely in W1. Comments, preprocessing,
  Unicode scalar handling, escapes, decoded values, and processed↔original
  offsets share one source-state problem. Keeping them behind PB1 avoids the
  exact second-scanner failure the audits exposed.
- R18 is correctly split. Odd/even delimiter parity and raw Phase-A string
  acceptance are immediate scanner invariants; preprocessing, escape decoding,
  bad-string recovery, normalized token values, and spans remain PB1 work.

No smaller local patch would close the comment/Unicode classes truthfully, and
moving exponent selectors or the four already-representable named offsets into
PB6 would leave Phase-A's public selector door knowingly spec-wrong.

### PB1 sequencing

The sequence is compatible with proposed `ADDENDA-02` and does not bypass its
two owner gates:

1. This addendum can ratify only the immediate W1 correction and deferred seam
   classifications.
2. PB1 remains unauthorized until `ADDENDA-02` Gate 1, PB0's structural check
   and two semantic audits, and explicit owner Gate 2.
3. Before PB1, W1 can become only a provisional non-color Phase-A artifact;
   W2 must still activate the color seam.
4. At PB1, W0 receives its structural replay and W1/W2/W5 are explicitly in
   the exposed replay path. A provisional W1 is therefore replay-exposed, not
   silently treated as an untouched in-progress wave. Its shared-scanner
   re-anchor and two fresh E-1 passes remain mandatory.
5. Tranche perfection stays RED while R13, R17, or full R18 is deferred.

That ordering is important: PB1 does not become a prerequisite for making the
small selector corrections, but no pre-PB1 audit can mislabel the provisional
artifact as the final shared L4 substrate.

### Downstream fit

- **W2:** the addendum preserves the typed color seam and requires W2 to replay
  the repaired/ratified W1 bank when color activates. It assigns no global
  number, comment, Unicode, or escape grammar to color.
- **W5:** W5 may consume repaired nested diagnostics and retain R5/R12, but it
  cannot implement R13 locally or interpret R16 as whole-sheet invalidity.
- **PB6:** full timeline-range name inventory and maturity remain there; R15
  changes only the four names already present in the frozen Phase-A union.
- **PB12a/PB12b:** recovered rule integration and property-goal meaning remain
  separate. In particular, PB12b—not W1 or W5—decides whether R16's actual
  component-token sequence matches a typed goal.

No downstream re-scope beyond the addendum's stated replay obligations is
needed.

## 2. Whole-addendum analysis

### Authority and ratification boundary

The headline, §0, §4, and §7 all independently state that the sheet is
proposed, not ratified, and grants no semantic implementation authority. The
owner request is bounded to one decision over the hardened six-row ledger and
the provisional sequence. It expressly excludes PB0/PB1+, Phase-B feature
code, the production tree, surface widening, named-range inventory growth,
stylesheet recovery, and the production swap.

The reset rule is sound: a semantic edit after either challenge invalidates
both challenge verdicts. The post-ratification W1 artifact is separately
sealed and then receives two new E-1 passes; an addendum challenge is never
substituted for a feature audit.

### Gate and replay soundness

The proposal carries every hardened prerequisite:

- one W0 structural replay after shared `lexeme.ts` changes;
- declarations/parity, lexeme, scanner, freeze, and trailing-input rails;
- at least 50 asserted accepts and 20 asserted rejects for each of the four W1
  doors, with branch diversity and deterministic hostile/no-throw inputs;
- exact success values or first diagnostic `code` + `expected[]`, never
  acceptance-only comparison;
- executable deferred rows rather than TODO/skip credit;
- both selector kinds, exponent selectors, negative/>100 named offsets, and
  the twelve-decimal serializer policy;
- no local comment/Unicode tokenizer or second scanner;
- the W2 color seam visibly RED;
- two fresh W1 E-1 audits after every immediate row is sealed.

The current four TODOs are explicitly called incomplete because the true
ledger has six classes. This is a truthful pre-ratification state, not proof of
close. The addendum correctly forbids starting the W1 re-audits on that state.

One row prevents the gate from being sound as written: R13's final witness
claims current rejection for an unterminated `/*` around a valid value. The
current result is placement- and door-dependent, and the false summary omits
the slash-list acceptance path most likely to regress if the future PB1
compatibility adapter is tested only against rejection examples.

### Counts and cost

The proposal adds no public door or feature wave. Its one S-grade focused
repair slice, one S-grade immediate post-ratification correction slice, one W0
structural replay, two addendum challenges, and two later W1 audits match the
Phase-A scale. PB1 implementation and replay cost remain in the separately
gated Phase-B budget; they are not hidden in the S-grade estimate.

### Pre-ratification mutation check

No post-proposal semantic edit was observed. `ADDENDA-03.md` was sealed at
16:11:14 local time; the relevant W0/W1 source and maintained test files were
last modified between 15:55:15 and 15:58:02. The only later files under the
mirror were declaration outputs freshly emitted by the mechanical parity run.

The current runtime does already contain audit-discovered behavior from the
pre-addendum W1 artifact:

- R14's finite exponent selectors already parse through `numUnit`;
- R16's three malformed complete inputs already reject;
- the W0 odd/even parity invariant and D-W1-2 line continuation repair already
  exist;
- R15 is not implemented, and R13/R17/full-R18 remain incomplete.

The proposal handles those facts correctly: existing behavior receives no
expected-divergence credit before ratification, its new rows remain TODO/RED,
and R15 has not been smuggled into the parser. Thus no semantic row was newly
landed by or after this addendum. Ratification must still precede converting
the pre-existing R14/R16/R18 behavior into a GREEN classified gate.

## 3. Existing-repair feature analysis

### D-W1-1 — `--` identifier start

**Sound.** The shared leaf accepts exact `--`, longer hyphen runs, digits after
the initial `--`, and case-preserved custom identifiers. Independent probes
showed `---` and `--5` agree with LIVE as keyword values. The row does not claim
escaped-identifier or preprocessing completeness; R17/PB1 retains that work.

### D-W1-2 — line continuation

**Sound with the R18 boundary intact.** One active U+005C followed by LF,
CRLF, CR, or FF agrees with LIVE and the pinned string-token algorithm. Raw or
even-run newlines reject in the mirror and disagree with LIVE's permissive raw
regex; that disagreement is not credited by D-W1-2 and belongs to R18.

### D-W1-3 — nested failure carrier

**Sound.** Independent probes reproduced exact first diagnostic preservation:
`outer(fn())` returns `function argument`; `outer(sibling-index(1))` returns
`zero-argument function`; list and deeper-call arms preserve the same result.
The addendum correctly leaves `start/end/actual` recorded-not-gating.

### D-W1-4 — non-finite numeric failure

**Sound.** `1e309` and its nested/list forms reject with `css_syntax` and
`expected:[]` in mirror and LIVE. The row neither rejects finite exponents nor
turns a non-finite number into a value.

### D-W1-5 / D-W1-6 — census and hostile banks

**Sound as a close requirement.** Current tests assert the Boolean result for
each row instead of merely counting the array, and `1e`/`--` are positive
boundaries. The maintained banks are branch-diverse and the public doors reject
non-string runtime values without throwing. The addendum correctly requires
R13/R17/full-R18 to become executable deferred ledgers before re-audit; the
current TODOs cannot discharge that gate.

### D-W1-7 — differential door state

**Sound.** The harness now compares projected structural successes and exact
first `code`/`expected[]` failures with `isDeepStrictEqual`. Color remains an
explicit W2 RED seam. The addendum further requires every disagreement to map
to an already-ratified row before a door can claim GREEN.

### D-W1-8 — selector serializer

**Sound.** The policy is canonical and precision-limiting, not arbitrary-float
identity. Existing fixtures cover both selector kinds and idempotence; the
post-R15 gate expressly adds exponent, negative, and >100 named offsets.
NaN/infinity hand-built internal objects do not become a public no-throw
contract.

## 4. Correction-row feature analysis

### R13 — comments preserve token boundaries

**Required semantics and ownership are sound; current-result ledger is
blocking-wrong.** The pinned Syntax-3 algorithm consumes comments before token
recognition and returns no token. Therefore `a/**/b` remains two significant
ident tokens rather than `ab`, while leading/trailing comments around one atom
do not change that atom. The legacy space-list adapter is conditional on
preserving its semantic contract; exact trivia and source intervals remain in
the L4 CST. The required post-PB1 legacy failure for a parse-error-producing
unterminated comment and the PB0-frozen L4 recovery split are correct.

Current probes show the broader RED seam:

| witness / door | current mirror | current LIVE |
|---|---|---|
| `foo/*` / scalar | reject `scalar` | reject `scalar` |
| `foo/*` / value | **accept slash list `[foo, *]`** | **accept same** |
| `foo /*` / value | **accept slash list `[foo, *]`** | **accept same** |
| `/*foo` / value | reject `scalar` | reject `scalar` |
| `fn(a/*)` / value | reject `scalar` | **accept call with slash-list argument** |
| `fn(/*a)` / value | reject `scalar` | reject `scalar` |

Thus the R13 table's final current-result cell, simply `reject`, is false for
the obvious trailing forms. The repaired addendum must enumerate at least
leading, trailing, spaced-trailing, and nested-call unterminated comments and
state the per-door current results above. The required PB1 result remains
legacy `css_syntax` rejection for all parse-error forms; the L4 result remains
owned by its future PB0 recovery row. No local comment-aware splitter was
added.

### R14 — finite exponent percentages

**Spec-faithful.** The pinned number algorithm consumes an exponent only when
`e/E`, optional sign, and a following digit are present. The proposed results
are numerically correct:

- `1e2%` / `1e+2%` → percent ratio `1`;
- `1e-2%` → percent ratio `0.0001`;
- `entry 1e-2%` → named offset `0.0001`;
- finite standalone values outside 0..100 → R4 range diagnostic;
- incomplete or non-finite spellings → generic selector diagnostic.

Generic scalar/value exponents remain LIVE agreement rows, so the correction
is not over-broadened into a numeric wave.

### R15 — standalone-only R4

**Spec-faithful.** The verified Scroll Animations source spells the grammar as
`from | to | <percentage [0,100]> | <timeline-range-name> <percentage>`.
Only the standalone arm is bounded. Accepting any finite percentage for the
four frozen names without clamping is therefore correct, and the current
named-range `0%..100%` diagnostic must disappear when this row lands.

The row does not claim the four-name union is the current full inventory;
PB6 retains that denominator and maturity decision.

### R16 — narrow numeric-token boundary

**Spec-faithful and properly narrow.** Syntax-3 produces one percentage or
dimension token and leaves following code points for subsequent tokens;
`1%%`, `1%px`, and `1a%b` cannot truthfully become one `{value, unit}` scalar.
The three complete-input legacy doors may reject with `css_syntax` /
`expected:["scalar"]`, but the addendum expressly does not infer that an
enclosing declaration or stylesheet is invalid. R12 and PB12b remain intact.

### R17 — identifier preprocessing/Unicode/escapes

**Correctly retracted from W1.** Current `-\u00e9`/`--\u00e9` acceptance and bare
`\u00e9` rejection demonstrate the partial ASCII-dispatch seam. Treating that as
conformance would be false. PB1 is the right owner for preprocessing,
surrogate/NUL handling, escapes, decoded names, function heads, and span maps;
PB7 only consumes the resulting tokens.

### R18 — odd/even active-backslash parity

**Spec-faithful and correctly split.** Sequential string-token consumption
makes BS(1)/BS(3) odd-active and BS(2)/BS(4) even-active. Independent probes
showed the mirror accepts one- and three-backslash line continuation, rejects
two and four, keeps odd-run quotes open, and closes at even-run quotes. LIVE's
raw regex accepts the bad even-run/raw-newline cases, so they require the
ratified divergence fixtures named here. Full escape decoding and recovery
remain PB1-only.

The future W0 replay must exercise the 1–4 quote/newline cross-product and all
LF/CRLF/CR/FF endings; the addendum already requires that parity coverage and
does not permit a feature-local replacement scanner.

## 5. Mechanical and oracle evidence

Executed from `pi/mirror/`:

- `npm test -- --reporter=verbose` → **8 files passed; 30 tests passed; 4
  explicit E-3 TODO/RED rows**.
- `npm run check` → GREEN.
- `npm run dts-parity` → GREEN, **33 type + 19 runtime exports**.
- `npm audit --omit=dev` → **0 vulnerabilities**.
- Source/test scan → no `.skip`/`.only`; the only deferred syntax is the four
  named TODOs, which this proposal itself declares incomplete.
- Current source scan → no R15 named-offset implementation and no R13/R17
  local tokenizer; source/test mtimes predate the proposal seal.

Pinned-primary-source verification:

- `css-syntax-3/Overview.bs` at CSSWG commit
  `c7573530343759ace8e46438a1fa2c44515b5554` re-hashed to SHA-256
  `3f129d17407f9bd027291bcfb8015beb86fc229bf5efe505b49ec2dc30be4390`.
- `scroll-animations-1/Overview.bs` at the same commit re-hashed to SHA-256
  `c730c2d6cdae2aeb9d5616b844ce378613a24b4af7e1eeca640adbbaedd683e1`.
- The inspected carriers confirm comment consumption, numeric-token selection,
  exponent syntax, string-token newline behavior, ident-start rules, and the
  bounded-standalone/unbounded-named keyframe-selector grammar.

Independent executable probes covered D-W1 nested diagnostics and non-finite
numbers; leading/trailing/nested/adjacent R13 comment placements, including
unterminated forms; R14 complete/incomplete/non-finite
exponents; R15 negative/>100 named offsets; R16 inputs in the three W1 doors;
R17 non-ASCII start forms; and R18 quote/newline BS(1)–BS(4) parity. Results
matched the proposal's required classifications except for the false R13
current-result cell described above.

## 6. Exact findings and close conditions

### Blocking findings

1. **B-1 — R13 understates current unterminated-comment behavior.**
   `ADDENDA-03 §3 R13` records the current LIVE/mirror result for an
   unterminated `/*` around a valid value as `reject`. Both parsers instead
   accept trailing `foo/*` and `foo /*` in `parseCssValue` as a slash list; in
   a call, LIVE accepts `fn(a/*)` while the mirror rejects. This is not a span
   detail: it changes `ok` and the AST and is the precise raw-slash collision
   R13 exists to eliminate. Repair the table and executable fixture ledger to
   enumerate leading/trailing/nested placements and each affected public W1
   door. Preserve the already-correct required result: legacy adapters reject
   unterminated-comment parse errors after PB1, while the L4 door follows the
   PB0-frozen recovery row. Because this is a semantic evidence correction,
   both addendum challenges must restart on the new digest.

### Confirmed non-blocking truths that must ride to ratification/implementation

1. The current four TODOs do not represent the full six-row ledger. R17 and
   R18 must become explicit executable rows before W1 re-audit.
2. R14/R16/immediate-R18 behavior exists in the pre-addendum artifact but is
   unratified and earns no GREEN credit. The post-ratification pass must seal
   its classification and fixtures; R15 must then make the named-offset code
   change.
3. The future W0 replay must include raw/BS(1)–BS(4) quote and newline cases
   across LF, CRLF, CR, and FF, not only the currently maintained subset.
4. A provisional W1 is replay-exposed at PB1 and receives the explicit W1
   shared-scanner replay/audits. It cannot be omitted from replay cost merely
   because its pre-PB1 status was labelled provisional.
5. This REJECT applies only to addendum digest
   `c83270db3d84dc34e92d2e815fa2756470c6fe33e128f638bc82c10494882c76`.
   Any semantic repair restarts both challenges.

**Final: REJECT.**

---

## Fresh re-challenge B — amended ADDENDA-03 (2026-07-21)

Exact amended target SHA-256:
`9d8006bb2e6f9d900d00e503d6e8b8fe2e84a730892d4ee558e72eab4ef57f02`.

Posture: fresh assume-faulty pass. This pass did not read, contact, or consume
the other challenge. It re-read the amended proposal, the governing W1
research/harden chain, the accepted W0 replay receipt, the current prototype,
LIVE, and the two pinned primary source objects. It appends evidence only; it
does not alter or erase the historical rejection against the prior digest.

Model receipt: inherited Codex audit seat; the exact backend alias was not
exposed to this seat.

### Re-challenge verdict

**ACCEPT.** The amendment discharges prior B-1 and the other named textual
repairs without opening a new semantic, authority, surface, sequencing, or
KISS defect. The R13 current-result table now matches LIVE and mirror exactly;
its terminal post-PB1 legacy lowerings are unambiguous. R16's generated bank is
complete as specified and mechanically true across all three legacy doors.
R18's code-point-defined Q/L families and AST projections are exact. The W0
shared-leaf replay is legitimately ACCEPTED and remains valid because no
shared source changed during the amendment.

This is an addendum challenge only. It is not owner ratification, W1
acceptance, PB0/PB1 authorization, semantic implementation permission, or
production execution.

### 1. R13 terminal lowerings and prior B-1

The amended row now separates three questions that the prior digest conflated:

1. current placement-dependent LIVE/mirror behavior;
2. the terminal fail-fast Phase-A lowering after PB1; and
3. the separately PB0-owned recovered L4 result.

The terminal adjacency table is internally complete for the correction it
claims:

- leading/trailing comments around one atom lower to `K("foo")`, with
  `parseCssValues` applying only its ordinary singleton space-list wrapper;
- comments at either function-argument edge lower to one argument `K("a")`;
- `a/**/b` preserves two significant ident tokens and uses the frozen AST's
  sole component-sequence representation, `List("space",[K("a"),K("b")])`;
- comments adjacent to a real comma or slash do not create a third token or an
  empty arm, so the lowerings are the exact two-item comma/slash lists;
- a real trailing slash remains a genuine R8 empty arm and rejects;
- quoted comment spelling remains raw keyword data in all three doors.

The `separator:"space"` choice is now terminal legacy compatibility authority,
not a discretionary PB1 decision. It does not claim exact serialization:
PB1/L4 still owns the source/trivia intervals, and the frozen Phase-A AST has
no adjacency-only separator kind. This is the smallest non-god lowering that
preserves the two semantic component values without concatenating them.

Fresh executable probes reproduced every amended current unterminated-comment
projection:

| source / door | LIVE | mirror |
|---|---|---|
| `foo/*`, `foo /*` / scalar | reject `scalar` | same |
| `foo/*`, `foo /*` / value | slash list `[K("foo"),K("*")]` | same |
| `foo/*`, `foo /*` / values | same slash list | same |
| `/*foo` / all three doors | reject `scalar` | same |
| `fn(a/*)` / value | call with one slash-list argument | reject `scalar` |
| `fn(a/*)` / values | singleton-space wrapper around that LIVE call | reject `scalar` |
| `fn(/*a)` / value and values | reject `scalar` | same |

This exactly repairs historical B-1. The required PB1 legacy result is also
correct: all of those unterminated-comment parse-error forms reject
`css_syntax` / `expected:["scalar"]`. The L4 door is not prejudged; it remains
bound to the future PB0 recovery/diagnostic row. No R13 implementation or local
comment scanner exists in the current W1 tree, and the row remains executable
`DEFERRED_PB1`, not GREEN credit.

### 2. R16 exact `1e%` and generated B16 bank

The amended bank is a deterministic set of **21 unique sources**:

```text
{ "1e%" } ∪
{ N + T | N ∈ {"1",".5","-1","1e2","1e-2"}
        | T ∈ {"%%","%px","px%","a%b"} }
```

An assertion-driven probe executed all 21 sources through
`parseCssScalar`, `parseCssValue`, and `parseCssValues`: **63 LIVE calls and 63
mirror calls**. Every LIVE scalar/value result was exactly one fabricated
numeric scalar with `value:Number(N)` and `unit:T`; every LIVE values result was
its singleton space-list wrapper. `1e%` independently produced value `1`, unit
`e%`. Every mirror call rejected `css_syntax` /
`expected:["scalar"]` exactly as the proposal records.

The bank is broad enough to prove the minimized boundary without claiming an
infinite divergence whitelist: plain, fractional, signed, complete-exponent,
and negative-exponent numeric prefixes cross four multiple-token tails. The
rule remains narrow to complete-input legacy adapters. PB1 preserves the true
tokens, PB3 owns numeric semantics, PB12b owns typed-goal validity, W5 cannot
promote it to whole-sheet rejection, and R12 remains intact.

### 3. R18 Q/L code-point sources and exact ASTs

The source families are now host-language-independent. Fresh probes built every
source with `String.fromCodePoint(0x5c)` and counted the contiguous U+005C run
immediately before the tested quote/newline; every count equalled `n`.

For `Q(1..4)`:

- LIVE value and values rejected all four with `css_syntax` /
  `expected:["scalar"]`;
- mirror rejected odd `Q(1)` and `Q(3)`;
- mirror `Q(2)` and `Q(4)` produced exactly `C(2)`/`C(4)`, with args
  `[K(RawQ(n)),K("b")]` and the exact raw quote/backslash spelling;
- mirror values wrapped each even call in exactly one singleton space list;
- scalar rejected all four in LIVE and mirror because Q is a call source.

For `L(1..4,E)`, across **LF, CRLF, CR, and FF** and all three doors:

- LIVE accepted all 16 source spellings as raw keyword scalars (values used
  the ordinary singleton wrapper);
- mirror accepted odd `n=1,3` with byte-for-byte/raw-code-point spelling;
- mirror rejected even `n=2,4` with exact `css_syntax` /
  `expected:["scalar"]`.

That is the amended table's full **4 × 4 × 3** newline-door matrix, not a
hand-escaped visual approximation. Sequential CSS string-token consumption
supports the odd/even result. The exact pinned Syntax-3 raw source re-hashed to
`3f129d17407f9bd027291bcfb8015beb86fc229bf5efe505b49ec2dc30be4390`.
Full escape decoding, bad-string recovery, normalized values, preprocessing,
and spans remain PB1-only.

### 4. Accepted W0 replay and current mechanics

The post-W1 W0 receipt is evidence-backed and scoped correctly as one scaffold
replay. Re-running the current tree produced:

- `npm test -- --reporter=verbose`: **8 files passed, 30 tests passed, 4
  deliberate E-3 TODO/RED rows**;
- `npm run check`: GREEN;
- `npm run dts-parity`: GREEN, **33 types + 19 runtimes**;
- `npm audit --omit=dev`: **0 vulnerabilities**.

An independent generated utility probe repeated the receipt's 1–4 matrix:

```text
quoted close             [false, true, false, true]
splitTopLevel arm count  [1, 2, 1, 2]
splitValueTokens count   [1, 3, 1, 3]
topLevelColon            [-1, 5, -1, 7]
emptyTopLevelItem        [none, 6, none, 8]
balancedUntil            odd reject / even exact capture
```

The replay receipt also retains the one-active-U+005C LF/CRLF/CR/FF,
double-hyphen ident, single-exec/nonzero-offset/span, dependency, freeze,
trailing-input, and exact-barrel evidence. The amended Q/L tables add future
post-ratification W1 semantic fixtures; they do not mutate the already-replayed
shared leaf. Relevant source/test files still predate the amended proposal, so
there is no basis to reopen W0 until a later repair changes that foundation.

### 5. Authority, surface, KISS, sequencing, and holds

All whole-packet boundaries remain sound:

- status is still `PROPOSED / NOT RATIFIED / NO NEW SEMANTIC CODE AUTHORIZED`;
- both fresh addendum challenges plus root gestalt and explicit owner
  ratification precede R14–R16/R18 classification or code credit;
- the surface remains exactly 19 runtime + 33 type exports, the legacy result
  and eight-code diagnostic union remain unchanged, and the W2 color seam is
  RED;
- no Phase-B gate is bypassed: PB1 still requires ADDENDA-02 Gate 1, accepted
  PB0 reviews, and owner Gate 2;
- the only future scanner is PB1; current W1/util files contain no R13 comment
  patch, Unicode fallback tokenizer, pre-strip pass, or second token cursor;
- after ratification, immediate rows seal, mechanical gates run, and two fresh
  W1 E-1 audits adjudicate only a provisional non-color artifact;
- W2 alone activates color and must replay the complete W1 bank; W5 remains
  held on rejected W1 and may not absorb R13 or broaden R16;
- PB1 later replays W0/W1/W2/W5 under the separately ratified quiescence plan;
  tranche perfection remains RED while R13, R17, or full R18 is deferred;
- the cost sheet still prices only the bounded Phase-A correction/review work
  and leaves PB1 implementation/replay in the Phase-B budget.

The amendment itself changed only authority/evidence text. The mirror source
and maintained W1 test mtimes predate the amended addendum; only declaration
outputs were refreshed by the parity command. Pre-existing R14/R16/R18 behavior
remains uncredited until ratification, R15 remains unimplemented, and no
semantic row was newly landed during formation.

### 6. Fresh findings

Blocking findings: **none**.

Non-blocking condition carried forward: the current four TODOs are not the
six-row executable correction ledger. Before the future W1 audits, R13,
R17/full-R18, R14, R15, and the full B16/R18 fixture sets must be sealed and
reported exactly as the amended gate requires. That future work is not a
defect in this proposed authority and earns no present GREEN feature credit.

This ACCEPT applies only to amended digest
`9d8006bb2e6f9d900d00e503d6e8b8fe2e84a730892d4ee558e72eab4ef57f02`.
Any semantic change restarts both challenges.

**Fresh final: ACCEPT.**
