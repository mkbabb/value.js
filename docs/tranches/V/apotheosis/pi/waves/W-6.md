# π.W6 — GRADUATION (opus·medium)

Sheet `../PI.md`; charter `../CHARTER.md` (G-1 coverage, G-2 equivalence).
Architecture `../formation/research-architecture.md` §F (W6); overrides
`../formation/harden.md` §3B (corpus), §4 (harness graduation), Q10 (kf seam
probe). Runs ‖ W7 after {W1..W5} all closed. Engine `@mkbabb/parse-that@1.0.0` AS
PUBLISHED.

## Mission

Graduate the differential to the FULL mirror barrel and re-run the totality
census: prove G-1 (52/52 + 37/37 TOTAL) and G-2 (0 MIRROR_DEFECT; R1–R12 all
asserted EXPECTED_DIVERGENCE). No new grammar — this wave measures and re-verdicts.

## Inputs (exact paths)

- Full mirror barrel `pi/mirror/index.ts` (W1–W5 closed).
- Corpus + machine forms:
  `/Users/mkbabb/.claude/jobs/9e7dadd0/tmp/parser-proof/equivalence/` (harvest.mjs,
  harness.ts) + `../parser-proof/equivalence-results.json` (403 items) +
  `../parser-proof/equivalence.md` §2 provenance.
- Census oracle: `../parser-proof/coverage.md` (P-2 method).
- kf consume seams: `keyframes-v-exec/src` (grep `@mkbabb/value.js/css` — 27 sites,
  37 symbols) — READ-ONLY reference for the compile-time probe.
- Live surface for the differential's LIVE side:
  `/Users/mkbabb/Programming/value.js/dist/subpaths/css.js` (built), or `src/css`.

## Deliverables (under `pi/mirror/`)

1. **The graduated differential run** — mirror vs LIVE export-by-export over the
   composed corpus (§4 gating: `result.ok` EXACT; `ok`⇒value tree structurally
   value-equal, **freeze-excluded** §2; `!ok`⇒`diagnostics[0].code` EXACT +
   `expected[]` EXACT; `start/end/actual` recorded-not-gating, escalate only on a
   surfaced non-trivial-span callsite). Every item lands in exactly one of AGREE
   (GREEN) / EXPECTED_DIVERGENCE (R1–R12, asserted GREEN) / MIRROR_DEFECT (RED,
   halts).
2. **The kf types-only seam probe** — a `tsc`-only module importing all 37
   kf-consumed symbols from the mirror d.ts; must compile (Q10 — compile-time
   TOTALity witness for Surface 2).
3. **P-2 census re-run** — `coverage.md` method against `pi/mirror/index.ts`.

## Ledger rows owned (PI §2)

- G-1: **all 52** (19 runtime + 33 type) re-census to TOTAL (was 0/3/49);
  **all 37** kf-consumed symbols to TOTAL (was 0/3/34) via the compile probe.
- G-2: **all R1–R12** asserted EXPECTED_DIVERGENCE (the closed spec-correction
  whitelist); the property lane (round-trip `parse∘serialize==parse` for color,
  timeline options, value) enters as a GREEN rail (bounded seed, NOT fuzz gating).
- §2h: **K-5** (dts-parity re-run) · **K-6** (per-door minimums + H-8 matrix as
  explicit checklist).

## Born-RED gate (H §5)

Full-surface differential (§3B corpus: 403 + 142 formerly-OUT_OF_SCOPE + R1–R12 +
hostile set; **≥50 accept / ≥20 reject per door**; color exhaustive over named +
the H-8 space matrix) with **0 MIRROR_DEFECT**; R1–R12 all asserted
EXPECTED_DIVERGENCE; kf types-only seam probe **compiles**; P-2 census reads
**52/52 + 37/37 TOTAL**. Gate = G-1 + G-2 GREEN.

## DO-NOTs

- Do NOT add new grammar or "fix" a MIRROR_DEFECT by widening the whitelist — a
  disagreement not in R1–R12 is a defect by construction; route it back to its
  owning wave.
- Do NOT gate on `start/end/actual` spans unless the run surfaces a live callsite
  passing non-trivial spans on an in-scope door (then that door's Tier-2 must be
  call-site-exact) — H §4.
- Do NOT run a random fuzzer in the gate — the property lane is a bounded
  deterministic seed list.
- Do NOT run the LIVE side against a stale `dist` — rebuild `dist/subpaths/css.js`
  if the repo moved (read-only consume; no repo edits).
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.
