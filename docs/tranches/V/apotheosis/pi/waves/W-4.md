# π.W4 — ANALYSIS TRANSPOSE (opus·medium)

Sheet `../PI.md`; charter `../CHARTER.md`. Architecture
`../formation/research-architecture.md` §B (collect\*/coerce/serializeCssValue),
§C; overrides `../formation/harden.md` — **H-2 (freeze strip), H-3 (close-gated
on W2+W3), H-5 (the R2 predicate — H GOVERNS R), H-6/K-9 (round-trip stability)**.
Authors ‖ W1/W2/W3 after W0; **CLOSES only after W2 and W3 merge** (their runtime
exports are called here — not seam stubs). Engine `@mkbabb/parse-that@1.0.0` AS
PUBLISHED.

## Mission

Transpose live's CssValue-TREE analysis near-verbatim: the `analyze.ts` helpers,
the `collect*` walkers, `coerceToSyntax`, and `serializeCssValue`. **This is not
parsing code** (H-6 verified: no recognition regex) — it is ordered decisions +
serialization over already-parsed value trees. Transpose is "verbatim **MINUS
freeze**" (H-2 — a named, enumerated divergence).

## Inputs (exact paths)

- `pi/mirror/{lexeme,util,result,types}.ts`, `deps/*` (W0); **W2**
  `serialize.ts::serializeCssColor`; **W3** `timeline.ts` runtime
  (`parseAnimationTimeline`/`parseAnimationRange`, `parseTimingFunction`). The
  private `parseTimelineScope`/`parseAnimationTrigger` helpers stay with W4 and
  transpose from LIVE `stylesheet.ts:44-80`; they consume W3's two public
  timeline parsers and are not new barrel exports.
- Live anchors: `/Users/mkbabb/Programming/value.js/src/css/stylesheet.ts` —
  analyze block `:99-363` (timingFunctionValue/timelineValue/animationArm/
  expandAnimationShorthand/optionDeclarationValid/animationCascade/components/
  repeated), collectors `:747-897` (`collect` generic `:747-761`,
  `collectDeclarations` `:772`, `collectAnimationOptions` `:827`,
  `collectTimelineOptions` `:879` re-parse-via-serialize round-trip `:874-897`),
  `serializeCssValue` `:81` (one cosmetic regex `:92`, NOT recognition).
  `…/src/css/syntax.ts` — `coerceToSyntax` `:91`, whole module (101 L), one
  keyword-guard regex `:64` (NOT a parser).

## Deliverables (under `pi/mirror/`)

1. **`stylesheet/analyze.ts`** — the `:99-363,781-825` analyze helpers,
   near-verbatim, **freeze calls stripped** (the ~19 `Object.freeze`/`deepFreeze`
   sites H-2 enumerated: `timingFunctionValue:141/144`, cubic:157, steps:172,
   linear:183/185/188, `timelineValue:201/202/216/231/237`, `keywordValue:241/243`,
   `numberValue:245/247`, `listValue:251`, arm:313).
2. **`stylesheet/collect.ts`** — `collect` + the 6 typed collectors +
   `collectDeclarations` + `collectAnimationOptions` + `collectTimelineOptions`
   (verbatim minus freeze at :754/755/760/805/829/860/871).
3. **`syntax.ts`** — `coerceToSyntax` + `isSupportedSyntaxDescriptor` verbatim;
   ONLY the `parseCssValue` import rewired (to `grammar/value.ts`).
4. **`serialize.ts::serializeCssValue`** — verbatim transpose (the `:92` cosmetic
   `\s+([:;])` replace kept as-is; output-cosmetic, not parsing).

## Ledger rows owned (PI §2)

- §2a: `coerceToSyntax`, `collectAnimationOptions`, `collectCustomFunctions`,
  `collectDeclarations`, `collectKeyframes`, `collectPropertyDescriptors`,
  `collectStyleRules`, `collectTimelineOptions`.
- §2b: `syntax_descriptor_invalid` + `syntax_mismatch` (coerceToSyntax);
  `animation_option_invalid` (collect side); `CSSAnimationOptions`/`Declaration`
  exercised.
- §2c seam-probe symbols: `coerceToSyntax`, `collectAnimationOptions`,
  `collectCustomFunctions`, `collectPropertyDescriptors`, `collectKeyframes`,
  `collectStyleRules`, `collectTimelineOptions`, `CSSAnimationOptions`,
  `Declaration`.
- §2d **R2 — H-5 predicate GOVERNS (H overrides R's under-specified fix)**, two
  rules + consequence, ALL asserted:
  1. **Acceptance:** an `animation*` declaration value whose tree contains any
     `var(`/`env(` call anywhere → `optionDeclarationValid` returns `true`
     (defer). Live's non-substitution checks (negative duration, bad direction/
     fill keywords, `emptyComma`) STAY.
  2. **Slotting:** `animationArm` treats a math-head call
     (`calc|min|max|clamp|round|mod|rem|sin|cos|tan|asin|acos|atan|atan2|exp|log|
     pow|sqrt|hypot|sign|abs`) as satisfying the FIRST unfilled numeric slot in
     live's order (duration→delay→iteration), NEVER reordering. `attr()` is OUT.
  3. **Collection consequence:** post-parse `components()` reading a deferred
     `var()` gets `undefined` → that row OMITS the field; the mirror ACCEPTS the
     declaration and yields a partial `CSSAnimationOptions`. Assert accept-bit AND
     omitted-field (K-4).
- §2e SUBSUMED: gap 14 slice (var/env defer). gap 4 (typed calc/math) DEFERRED —
  math-heads are opaque here.
- §2g: **H-2** (strip ~19 analyze/collect freeze sites) · **H-5** (R2 predicate).
- §2h: **K-2** (grep mirror for freeze = 0 at close) · **K-4** (slotting fixture)
  · **K-9** (`serializeCssValue↔parseAnimationRange` round-trip: land
  `serializeCssValue` BEFORE `collectTimelineOptions`; assert
  `parse(serialize(v))≡canonical`).

## Born-RED gate (H §5)

collect\* + `coerceToSyntax` + `serializeCssValue` doors differential **GREEN**
over hand-built `Stylesheet` fixtures (path model, important-cascade); **R2
accept+omit** (H-5) asserted arm-by-arm on `animation: n calc(2s) 1 …`;
`collectTimelineOptions` round-trip-stable (K-9); `coerceToSyntax` both `syntax_*`
codes; `serializeCssValue` `:`/`;` spacing. **CLOSE-GATED on W3** (timingFunction/
parseAnimationRange are W3 runtime) **and W2** (serializer arm) — the differential
uses the LIVE W2/W3 exports, NOT seam stubs (H-3/K-3). Grep the mirror for
`Object.freeze`/`deepFreeze` = **0** (K-2).

## DO-NOTs

- Do NOT close on W2/W3 seam stubs — wait for their merge (H-3; simultaneous-close
  ceiling is 3).
- Do NOT carry any `Object.freeze`/`deepFreeze` from the transposed source (H-2;
  enumerate + strip; the grep-0 is a gate).
- Do NOT adopt R's original R2 fix — H-5's two-rule predicate + collection
  consequence governs.
- Do NOT treat `attr()` as an R2 numeric substitution (OUT).
- Do NOT re-parse timeline options by any path other than the preserved
  serialize→re-parse round-trip (`:874-897`).
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.

Ownership clarification (2026-07-21): the earlier input line incorrectly called
the private scope/trigger helpers W3 runtime. LIVE owns them in `stylesheet.ts`,
and W3's ratified self-contained brief owns only the public timeline/range
parsers. Keeping the helpers private in W4 changes no feature, surface, type, or
dependency; it is the smallest provenance-preserving correction and must be
challenged in W4's ordinary E-1 passes.
