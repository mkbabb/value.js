# π.W1 — VALUES (opus·medium)

Sheet `../PI.md`; charter `../CHARTER.md`. Architecture
`../formation/research-architecture.md` §B (value/scalar/kf-selector rows), §D-1
(value-atom dispatch), §D-4 (kf-selector dispatch); overrides
`../formation/harden.md` §5 (born-RED W1), H-3 (color rows gate on W2). Runs ‖
W2/W3/W4 after W0. Engine `@mkbabb/parse-that@1.0.0` AS PUBLISHED.

## Mission

Author the value/scalar tier as parse-that combinators over the W0 lexeme layer:
`parseCssScalar`, `parseCssValue`, `parseCssValues`, `parseKeyframeSelector`.
The color door is imported through a **typed seam stub** (real impl lands W2) —
so W1's color-bearing scalar fixtures stay RED behind that seam until W2 merges
(H-3 close-order `W2 ⟹ W1(color rows)`).

## Inputs (exact paths)

- `pi/mirror/lexeme.ts`, `util.ts`, `result.ts`, `types.ts`, `deps/*` (W0).
- Live anchors: `/Users/mkbabb/Programming/value.js/src/css/grammar.ts` —
  `parseCssScalar` `:334` (trim→color door→number+unit→quoted→operator keywords
  `:327`→ident), `parseCssValue` `:393` (comma>slash>space precedence `:341-369`;
  top-level `:`/`;` standalone keyword tokens via splitValueTokens `:117`; call
  grammar with color-name exclusion `:371`, zero-arg rules `:374-382`, comma-args
  flattening `:385-387`), `parseCssValues` `:397` (non-list→singleton space list),
  `parseKeyframeSelector` `:405` (from/to→percent 0/1; percent range `:418-425`;
  named+offset).

## Deliverables (under `pi/mirror/`)

1. **`grammar/value.ts`** — `parseCssScalar` + `parseCssValue` + `parseCssValues`.
   D-1 value-atom head is a `dispatch()` table (digit/`+`/`.`→numeric · `#`→hex
   seam · `"`/`'`→quoted · `-`→ordered any(number, ident/custom) · `a-z_`→
   ident-or-fnHead ladder). Nesting via `Parser.lazy`. Color door via a typed
   seam import (stub until W2).
2. **`grammar/keyframe-selector.ts`** — `parseKeyframeSelector` +
   `serializeKeyframeSelector` (internal). D-4 dispatch head from day one.

## Ledger rows owned (PI §2)

- §2a: `parseCssScalar`, `parseCssValue`, `parseCssValues`, `parseKeyframeSelector`.
- §2b: `keyframe_selector_invalid` emission; `KeyframeSelector`/`KeyframeRule`
  exercised.
- §2c seam-probe symbols produced here: `parseCssScalar`, `parseCssValues`,
  `parseKeyframeSelector`, `KeyframeSelector`, `KeyframeRule`.
- §2d: **R8** (reject empty/trailing list items everywhere — Syntax 3; the
  animation-* `animation_option_invalid` coded path is W5's, not here) · **R4**
  (kf-selector percent range 0..100 is live+spec CORRECT — KEEP, do not "fix").
- §2d KEEPS: `from`/`to`→percent 0/1 · operator/`:`/`;` keyword scalars ·
  zero-arg call rules.
- §2e SUBSUMED slice: gap 13 unit algebra (numUnit generic) · gap 14 var/env as
  generic call.

## Born-RED gate (H §5)

value + kf-selector doors differential **GREEN on non-color inputs**;
color-scalar fixtures remain RED behind the W2 seam (H-3). W1 bank (minus color
rows) discharged: scalar {number+unit, quoted, operator/`:`/`;` keyword, ident};
value {comma>slash>space precedence, nesting via `lazy`, **R8 empty-item
reject**}; kf-selector {from/to→0/1, **percent range R4**, named+offset}. Per-door
minimum ≥50 accept / ≥20 reject (H §3B).

## DO-NOTs

- Do NOT implement color here — consume the typed color seam; leave color rows RED.
- Do NOT relax the list grammar to accept `a,,b`/`a,` (R8 — reject; H-9 confirms
  0 real-corpus regressions; the seed cases become expected-divergence fixtures).
- Do NOT lowercase `--*` idents (case preserved by the lexeme layer; R7 is W5's
  concern but the ident leaf must already preserve case).
- Do NOT use bare `regex()`/`map`/`mapState`; tokens come ONLY from `lexeme.ts`.
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.
