# π.W7 — BENCH (opus·medium)

Sheet `../PI.md`; charter `../CHARTER.md` (G-3 bench). Architecture
`../formation/research-architecture.md` §F (W7), §D (O-4 law); overrides
`../formation/harden.md` §6 (bench protocol frozen), Q9. Runs ‖ W6 after {W1..W5}
closed. Engine `@mkbabb/parse-that@1.0.0` AS PUBLISHED.

## Mission

Resurrect the P-3 comparative bench on the FULL mirror grammar, V8-profile it,
apply O-4 dispatch tables ONLY where the profile shows a real ladder, and emit
the G-3 verdict reporting BOTH bars per scenario. Perf is measurement here, not
rebuild — O-1/O-2/O-3 are already in `lexeme.ts` from W0.

## Inputs (exact paths)

- Full mirror grammar (W1–W5 closed) + `pi/mirror/bench` harness.
- Frozen rig + baselines:
  `/Users/mkbabb/Programming/value.js/docs/tranches/V/apotheosis/parser-proof/bench-results.json`
  (schema `p3-comparative-bench-aggregate/1`; Apple M5 Max; node v26; 5
  invocations × 15 samples × N=1000; warmup 50/sample; ratio = engine peak MB/s ÷
  jsonParser peak, interleaved per sample) + `../parser-proof/bench.md` +
  `../parser-proof/PROFILE-ANALYSIS.md` (P-4 profiles; profile driver at
  `…/jobs/9e7dadd0/tmp/parser-proof/profile/`).
- Gate bar source: `scripts/gates/proof-perf-target.mjs @ b3f4f76e` (VALUE floor
  0.05 / SHEET floor 0.10) — READ-ONLY reference for the recorded bar.

## Deliverables

1. **The P-3 run on the mirror** — the 5 FROZEN scenarios, byte-identical
   sha256-pinned inputs (`value-historical`, `sheet-historical`, `sheet-demo`,
   `value-common`, `sheet-common`) — do NOT add or alter scenarios; only the
   engine under test changes.
2. **V8 profile** of the mirror on the sheet-common scenario; identify any real
   ladder; apply **O-4 `dispatch()`** tables to D-1/D-2 IFF the profile shows the
   megamorphism (D-3/D-4/D-5 already tabled from their waves). **O-5 `memoize`
   only if backtracking is measured** (grammar is near-LL(1) — expected unused).
3. **The G-3 verdict** — both bars, per scenario, over the 5-invocation vector.

## Ledger rows owned (PI §2)

- §2f: **O-4** applied (profile-gated for D-1/D-2) · **O-5** (only-if-measured).
  (O-1/O-2/O-3 already discharged in W0 — this wave confirms them under profile,
  does not rebuild. O-6 is OUT — upstream no-contact.)
- §2h: **K-7** (sheet bar unreachable → OC-1 owner row; do NOT relax G-2 to chase
  perf).

## Born-RED gate (H §5/§6)

Report BOTH bars, per scenario, over 5 invocations:
1. **Absolute:** `VALUE_RATIO_FLOOR 0.05`, `SHEET_RATIO_FLOOR 0.10` vs jsonParser
   peak.
2. **Relative:** mirror peak ≥ deposed peak, per scenario.
3. **Spread bound:** a bar is "met" only if met in the **median** and not RED in a
   majority of the 5 invocations (the rig reports a verdict vector; C14 was
   `[RED×5]`).

Baselines to beat: C14 value **0.0640 (PASS ≥0.05)** / sheet **0.0537 (FAIL
≥0.10)** → the mirror must ≈**2×** sheet throughput to clear absolute (PROFILE §3:
O-1+O-2+O-3 target the ~55–60% pure lexeme/trivia/bookkeeping overhead ⇒ 1.8–2.2×
⇒ the floor is plausibly meetable grammar-side alone). **Absolute-met on both =
unconditional GREEN. Relative-only-met (≥ deposed but < absolute sheet) = the OC-1
owner-decision row** carried to `PI-CLOSE.md` — the bench-bar recalibration is
owner's; H/W do NOT pre-empt it.

## DO-NOTs

- Do NOT add, remove, or re-input any bench scenario (Q9 — comparability to the
  C14/deposed baselines requires byte-identical sha256-pinned inputs).
- Do NOT relax G-2 (spec-correctness) to chase the sheet bar (K-7) — if
  relative-only, file the OC-1 row and stop.
- Do NOT file the O-6 engine ask (PT-E letter) — upstream is under a no-contact
  boundary; O-6b byte-class reimplementation is out of π scope (only after the
  floor is met grammar-side, and it is a later lane).
- Do NOT apply O-4/O-5 speculatively — profile-evidence-gated only.
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.
