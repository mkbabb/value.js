# π.W2 — COLOR (opus·high · L door — budget alone, K-8)

Sheet `../PI.md`; charter `../CHARTER.md`. Architecture
`../formation/research-architecture.md` §B (parseCssColor/serializeCssColor rows),
§D-6 (color-function name-keyed Record), §E (R1/R3/R6/R9); overrides
`../formation/harden.md` §5 (born-RED W2), **H-8 (the color-door matrix +
named-color exhaustiveness governs the corpus)**. Runs ‖ W1/W3/W4 after W0; its
close FLIPS W1's color rows GREEN (H-3 `W2 ⟹ W1`).

## Mission

Author the full color door — all 13 spaces + hex + named + `color()` — as
parse-that combinators, plus the verbatim `serializeCssColor` transpose. This is
one of the two L doors; take it as a whole seat (K-8).

## Inputs (exact paths)

- `pi/mirror/{lexeme,util,result,types,named-colors}.ts`, `deps/color-model.ts` (W0).
- Live anchors: `/Users/mkbabb/Programming/value.js/src/css/grammar.ts` —
  `parseCssColor` `:257` (Tier-2 skeleton: trim → var/env+context guard `:260` →
  hsv/kelvin/ictcp/jzazbz reject `:263` → transparent `:264` → named `:265` → hex
  `:267` → fn dispatch); channel/percent scaling `:183-252`; `color()` srgb→rgb +
  xyz-d50 D65 adapt `:239-243`; `serializeCssColor` `:289` (format/angle/
  alphaSuffix `:283-287` + the 10-arm switch). Colors built via the transposed
  `deps/color-model` factories.

## Deliverables (under `pi/mirror/`)

1. **`grammar/color.ts`** — `parseCssColor`. D-6 dispatch = lex `fnHead` ONCE then
   a name-keyed `Record<string,Parser>` (rgb/rgba/hsl/hsla/hwb/lab/lch/oklab/
   oklch/color), each row carrying its per-space channel grammar + percent-scale
   triple + hue flag (`:183-224`). All Tier-2 guards transposed to their live
   decision points, producing `color_context_required` where live does.
2. **`serialize.ts::serializeCssColor`** — verbatim transpose (10-arm switch;
   `isAnyColor` + CSS_COLOR_SPACES guards intact). No freeze (§2).

## Ledger rows owned (PI §2)

- §2a: `parseCssColor`, `serializeCssColor`.
- §2b: `color_context_required` emission; `CssColorSpace`/`CssColor` exercised.
- §2c seam-probe symbols: `parseCssColor`, `serializeCssColor`, `CssColor`,
  `CssColorSpace`.
- §2d corrections (EXPECTED_DIVERGENCE fixtures): **R1** (empty functional-color
  → clean `ok:false`; the no-throw guard from W0 makes this free — assert it) ·
  **R3** (trailing `/` no-alpha → reject; grammar requires alpha after `/`) ·
  **R6** (hsl/hwb `<number>` sat/light/white/black ≡ percentage → `/100`; fixture
  `hsl(120 50 50)`→s=0.5) · **R9** (legacy=all-comma / modern=none-comma, two
  arms; mixed `rgb(255, 0 0)` REJECTED).
- §2d KEEP: `serializeCssColor` canonical forms incl. `color(xyz …)`.
- §2e SUBSUMED: gap 1 slice — 13 spaces + hex + named + `color()`.
  (relative-color / color-mix / light-dark / contrast-color are DEFERRED — NOT
  frozen /css exports; out of π scope.)
- §2h: **K-6** (corpus blind spot — the H-8 matrix is an explicit checklist, not
  a count) · K-8 (L door budgeted alone).

## Born-RED gate (H §5, H-8)

Color door differential **GREEN** on:
- the **H-8 matrix**: 13 spaces × {legacy comma, modern space, none-channel} ·
  hex {3,4,6,8} · `color()` {srgb→rgb, srgb-linear, display-p3, a98-rgb,
  prophoto-rgb, rec2020, xyz, xyz-d50 D65-adapt};
- **exhaustive** `NAMED_COLORS` round-trip — assert `Object.keys(NAMED_COLORS).
  length` matches source (do NOT hard-code the count);
- guards {var/env→`color_context_required`, hsv/kelvin/ictcp/jzazbz→`css_syntax`,
  transparent};
- **R1/R3/R6/R9** expected-divergences asserted;
- `serializeCssColor` canonical round-trip incl. `color(xyz …)`;
- ≥50 accept / ≥20 reject (H §3B).
On close, **W1's color-scalar rows flip GREEN** (H-3 close-order).

## DO-NOTs

- Do NOT parse relative-color / color-mix / light-dark / contrast-color as typed
  color here — they are not frozen /css exports (DEFERRED megatranche).
- Do NOT accept mixed legacy/modern commas (R9) or dangling `/` (R3).
- Do NOT take hsl/hwb raw numbers as-is (R6 — scale by /100).
- Do NOT hard-code the named-color count; read it from source (H-8).
- Do NOT freeze any built color (§2; K-2).
- Do NOT use bare `regex()`/`map`/`mapState`; tokens ONLY from `lexeme.ts`.
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.
