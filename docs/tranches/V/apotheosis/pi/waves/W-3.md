# π.W3 — EASING + TIMELINE (opus·medium)

Sheet `../PI.md`; charter `../CHARTER.md`. Architecture
`../formation/research-architecture.md` §B (parseTimingFunction, the three
timeline exports), §D-3 (easing dispatch), §D-5 (timeline dispatch), §E
(R10/R11); overrides `../formation/harden.md` §5 (born-RED W3). Runs ‖ W1/W2/W4
after W0; **independent of W1/W2** in both authoring AND close. Its exports are a
CLOSE-prerequisite for W4 (H-3 `{W2,W3} ⟹ W4`). Engine `@mkbabb/parse-that@1.0.0`
AS PUBLISHED.

## Mission

Author the easing + timeline tier: `parseTimingFunction` (4
`CssTimingFunction` kinds), `parseAnimationTimeline`, `parseAnimationRange`, and
the verbatim `serializeTimelineOptions` transpose.

## Inputs (exact paths)

- `pi/mirror/{lexeme,util,result,types}.ts`, `deps/value-types.ts` (JumpPosition) (W0).
- Live anchors: `/Users/mkbabb/Programming/value.js/src/css/grammar.ts` —
  `parseTimingFunction` `:436` (5 keywords; step-start/end; cubic-bezier x∈[0,1]
  `:449`; steps aliases + jump-none≥2 `:457-462`; linear ≥2 stops ≤2 positions
  `:466-478`). `…/src/css/timeline.ts` — `parseAnimationTimeline` `:17`
  (scroll()/view() arg grammars), `parseAnimationRange` `:66` (2/1-split
  disambiguation `:78-82`), `serializeTimelineOptions` `:108` (+ serializeTimeline/
  Range/Scope/Trigger `:86-107`), comma-leniency `:23,38`, unitless offset `:16`.

## Deliverables (under `pi/mirror/`)

1. **`grammar/easing.ts`** — `parseTimingFunction`; D-3 dispatch table from day one
   (`l` linear-kw/`linear(` · `e` ease* · `s` steps · `c` `cubic-bezier(`).
2. **`timeline.ts`** — `parseAnimationTimeline` (D-5 dispatch: `a`uto/`n`one/
   `s`croll/`v`iew/`-`-name) + `parseAnimationRange` (Tier-1 boundary grammar +
   transposed 2/1-split) + `serializeTimelineOptions` (verbatim). Tier-2 emits
   `timeline_option_invalid` at live's decision points.

## Ledger rows owned (PI §2)

- §2a: `parseTimingFunction`, `parseAnimationTimeline`, `parseAnimationRange`,
  `serializeTimelineOptions`.
- §2b: `timeline_option_invalid` emission; **`CssTimingFunction` 4 kinds**;
  `CssLinearStop`; the timeline/range/trigger/scope type cluster exercised.
- §2c seam-probe symbols: `parseTimingFunction`, `parseAnimationRange`,
  `parseAnimationTimeline`, `serializeTimelineOptions`, `CSSTimelineOptions`,
  `AnimationRangeValue`, `AnimationTimelineValue`, `AnimationTriggerValue`,
  `RangeBoundary`, `RangePhase`, `TriggerType`, `CssTimingFunction`, `CssLinearStop`.
- §2d corrections: **R10** (scroll()/view() commas REJECTED — space-separated
  only) · **R11** (timeline offset unit required except `0` — reject `entry 5`,
  accept `entry 0`).
- §2e SUBSUMED: gap 2 slice (keyword/steps/linear easing; `spring()` DEFERRED
  V25) · gap 6 slice (frozen timeline/range; trigger DEPTH DEFERRED V28).

## Born-RED gate (H §5)

easing + timeline + range doors differential **GREEN** incl. **R10/R11**; all 4
timing kinds (keyword ×5, step-start/end, cubic-bezier x∈[0,1], steps aliases +
jump-none≥2, linear ≥2 stops ≤2 positions); timeline {auto/none/scroll()/view()/
--name}; range {1/2-split disambiguation}. Independent of W1/W2. Per-door ≥50
accept / ≥20 reject (H §3B). **W3 exports are what W4 closes against — not seam
stubs** (H-3/K-3).

## DO-NOTs

- Do NOT accept commas in scroll()/view() (R10) or bare unitless non-zero timeline
  offsets (R11).
- Do NOT parse `spring()` (DEFERRED V25) or deep trigger phases (DEFERRED V28).
- Do NOT freeze any result (§2; K-2).
- Do NOT use bare `regex()`/`map`/`mapState`; tokens ONLY from `lexeme.ts`.
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.
