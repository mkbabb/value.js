# π.W0 — SCAFFOLD (opus·high)

Prereq gate: **must close before ANY fan-out.** Sheet `../PI.md`; charter
`../CHARTER.md`. Architecture `../formation/research-architecture.md` §A/§B/§C;
overrides `../formation/harden.md` §1/§2. Engine `@mkbabb/parse-that@1.0.0` AS
PUBLISHED — its ONLY consumable surface is the d.ts set below; no engine edits.

## Mission

Bootstrap `pi/mirror/` as a self-contained package and land the whole
non-grammar substrate: deps, types (byte-parity + parity gate), the fused lexeme
layer (O-1/O-2/O-3 by construction), the transposed scanners/result helpers, the
52-export barrel over stubs, and the born-RED test bank enumerating every export.
At W0 close every DOOR is provably RED; every INFRA test is GREEN.

## Inputs (exact paths)

- Seed source (structure precedent, re-founded per below):
  `/Users/mkbabb/.claude/jobs/9e7dadd0/tmp/parser-proof/c14-css/src` — KEEP its
  `package.json`/`tsconfig.json` discipline, `runComplete` outer-guard idea, the
  l4 topology as naming precedent. **RE-FOUND** `combinators.ts` and every l4
  module (they are replaced by the lexeme layer — the O-1/O-2 mandate).
- Engine surface (the ONLY allowed parse-that API):
  `…/c14-css/node_modules/@mkbabb/parse-that/dist/{index,leaf,parser,state,utils,split}.d.ts`.
- Type contract: `/Users/mkbabb/Programming/value.js/src/css/types.ts` (131 L).
- Barrel to mirror: `/Users/mkbabb/Programming/value.js/src/css/index.ts` (60 L).
- Result/scanner anchors: `…/src/css/grammar.ts:33-61` (success/failure/
  EMPTY_DIAGNOSTICS), `:63-126` (splitTopLevel/splitValueTokens/topLevelColon/
  emptyComma); `…/src/css/stylesheet.ts:365-384,580-593` (further splitters).
- deps anchors: `…/src/foundation/result.ts` (Result/ok/err, ~6 L); `…/src/color/
  model.ts:4-134` (SpaceId…ColorFactory + SPACE_SCHEMA + factories + isAnyColor);
  `…/src/color/anchors.ts:70-80` (adaptXyzD50ToD65, D50_TO_D65) + matrix multiply;
  `…/src/value.ts:3-23` (CssValue/CssScalar/CssCall/CssList); `…/src/easing.ts:13`
  (JumpPosition); `…/src/css/named-colors.ts` (150 L, 149 entries).

## Deliverables (all under `pi/mirror/`)

1. **package.json** — `"private": true`, `"type": "module"`; deps:
   `@mkbabb/parse-that": "1.0.0"` EXACT; devDeps: `vitest`, `tsx`, `typescript`,
   `@types/node`; scripts: `test` (vitest run), `check` (tsc --noEmit),
   `dts-parity` (emit d.ts + diff vs live), `bench` (stub). **Zero other deps.**
2. **tsconfig.json** — seed from c14: `target ES2022`, `module/moduleResolution
   NodeNext`, `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`,
   `verbatimModuleSyntax`, `skipLibCheck`. Add a declaration-emit variant (or
   tsc-API call) so `dts-parity` can materialize `index.d.ts`.
3. **`deps/foundation.ts`** — Result/ok/err verbatim (foundation/result.ts).
4. **`deps/color-model.ts`** — SpaceId…ColorFactory + SPACE_SCHEMA + factories +
   `isAnyColor` (model.ts:4-134 verbatim subset) + `adaptXyzD50ToD65` + `D50_TO_D65`
   + `multiply` (anchors.ts:70-80 + matrix helper).
5. **`deps/value-types.ts`** — CssValue/CssScalar/CssCall/CssList (value.ts:3-23)
   + JumpPosition (easing.ts:13).
6. **`named-colors.ts`** — verbatim transpose **MINUS the `Object.freeze`
   wrapper** (H-2/§2 freeze-strip; a plain object literal — freeze is invisible).
7. **`types.ts`** — `src/css/types.ts:1-131` **byte-for-byte in the type bodies**;
   ONLY the four import lines rewire (`../color/model`→`deps/color-model`,
   `../foundation/result`→`deps/foundation`, `../easing`→`deps/value-types`
   (JumpPosition), `../value`→`deps/value-types`). Keep the `:131` convenience
   re-exports on the module but do NOT surface them in `index.ts`.
8. **`result.ts`** — success/failure/EMPTY_DIAGNOSTICS transposed from
   `grammar.ts:33-61` so diagnostics are contract-shaped by construction. The
   `FREEZE` constant is **removed** and success/failure **never freeze** (§2
   ruling — mirror does not freeze anywhere).
9. **`lexeme.ts`** — the fused leaf layer per research §A:
   `lexeme(re,build)` (sticky-`y` asserted at load; ONE exec at `state.offset`;
   span end sealed BEFORE `skipBlockComments`; ONE object built; zero staging) ·
   `lit` · `num`/`numUnit` (O-3 one-pass) · `ident` (case preserved, incl.
   `--custom`) · `fnHead` (lowercased) · `quoted` · `balancedUntil(stop)` ·
   `runComplete(grammar,input)` (parseState + `offset===length`; exception→null).
   Custom leaves via the public `Parser` ctor + `createParserContext`; trivia via
   `skipBlockComments`. **No bare `regex()`, no `.skip(trivia)`, no `map`/
   `mapState` staging anywhere in the mirror** (O-1/O-2 law).
10. **`util.ts`** — splitTopLevel/splitValueTokens/topLevelColon/emptyComma
    transposed VERBATIM (their exact trim/drop/quote/depth semantics ARE contract
    behavior at raw-string seams). Engine `splitBalanced` stays UNUSED (K-1/Q11:
    default the transposed util; swap only on a proven behavioral-identity test —
    unproven, so do not swap).
11. **`index.ts`** — the 52-export barrel, line-mirroring `src/css/index.ts`
    exactly (33 types + 19 runtime, nothing more), over **stub** grammar modules
    that throw/`ok:false` so every door is born-RED.
12. **`test/` infra + door stubs** — the born-RED bank enumerating every export;
    the differential-harness skeleton wired to the barrel; the bench harness stub.

## Ledger rows owned (PI §2)

- §2a runtime: none produced yet — the 52-barrel stub-homes all 19 (born-RED).
- §2b: **all 33 type bodies** (byte-parity) + `ParseIssue`/`ParseResult` shape;
  `css_syntax` (no-throw guard) + `trailing_input` (reserved-unused guard).
- §2f: **O-1, O-2, O-3** built into `lexeme.ts` from day one (perf is not
  retrofitted — W7 measures, does not rebuild).
- §2g: **H-2** (strip result + named-colors freeze) · **H-7** (three-layer
  recognition — scanners get own tests) · **H-9** (trailing_input 0-sites guard;
  freeze-independence probe) · H-4 note (parity gate is name+type scoped, NOT
  `from`-scoped).
- §2h: **K-1** (sanction `balancedUntil`+transposed splitters as the imperative
  recognition layer) · **K-5** (`dts-parity` born-GREEN, re-run every wave).

## Born-RED gate (H §5 — close condition)

- `tsc --strict` clean.
- `dts-parity.test.ts` **GREEN** — the mirror's emitted `index.d.ts` export-set +
  resolved type text == live's `src/css` d.ts, symbol-for-symbol and
  member-for-member (name+type scoped, not `from`; H-4). This is the K-5 gate.
- `lexeme.test.ts` GREEN — num/numUnit/ident/fnHead/quoted/lit +
  `skipBlockComments` span-seal-before-trivia.
- `scanners.test.ts` GREEN — splitTopLevel/splitValueTokens/balancedUntil
  trim/drop/quote/depth (H-7 — these are contract behavior; R3/R5/R8 live here).
- `freeze-independence.test.ts` GREEN — asserts the two consumer greps
  (`Object.isFrozen` / parse-result mutation in `keyframes-v-exec/src` + `demo`)
  return empty (§2 standing guard).
- `no-trailing-input.test.ts` GREEN — 0 `trailing_input` construction sites in
  the mirror (H-9/Q6).
- `barrel.test.ts` — all 52 names importable + type-level probe.
- **Every door's differential is RED** (stubs) — provably born-RED, all doors.

## DO-NOTs

- Do NOT edit repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, any INBOX.
- Do NOT add any repo/package dep beyond parse-that + vitest/tsx/typescript/@types.
- Do NOT use bare `regex()`, `.skip(trivia)`, `map`/`mapState` staging, or `.opt()`
  trivia in any mirror module (kills O-1/O-2).
- Do NOT reintroduce `FREEZE`/`Object.freeze`/`deepFreeze` anywhere (§2; K-2).
- Do NOT surface `types.ts:131` convenience re-exports in `index.ts` (barrel is
  exactly 52).
- Do NOT swap in engine `splitBalanced` (Q11 — unproven; keep the transposed util).
- Do NOT drop the assay `serialize.ts` SourceDocument machinery back in (no live
  peer; out of the 52-surface — research §C).
