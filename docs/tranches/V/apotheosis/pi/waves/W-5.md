# π.W5 — STYLESHEET (opus·high · L door — split W5a/W5b pre-authorized, K-8)

Sheet `../PI.md`; charter `../CHARTER.md`. Architecture
`../formation/research-architecture.md` §B (parseStylesheet row), §D-2 (at-rule
dispatch), §E (R2/R5/R7/R12); overrides `../formation/harden.md` §5 (born-RED W5),
H-7 (balanced scanners), Q8 (fail-fast CONFIRMED contract). The JOIN wave — runs
after {W1,W2,W3,W4}. Engine `@mkbabb/parse-that@1.0.0` AS PUBLISHED.

## Mission

Author `parseStylesheet` — the block-structure grammar + at-rule dispatch +
declaration parsing + all descriptor validation — wiring W1 values, W3 timeline
validators, and W4 analyzers. Live `stylesheet.ts` is 899 L; **if seat-sized, split
W5a (blocks + declarations) / W5b (at-rules + index)** (K-8).

## Inputs (exact paths)

- `pi/mirror/lexeme.ts`, `util.ts`, `result.ts`, `types.ts` (W0); **W1**
  `grammar/value.ts`; **W3** `timeline.ts` validators; **W4** `stylesheet/
  {analyze,collect}.ts`, `syntax.ts`.
- Live anchor: `/Users/mkbabb/Programming/value.js/src/css/stylesheet.ts` —
  `blocks()` `:431-482` (comment skip, `;`-preludes, balanced braces/quotes),
  `parseStyleBody` declarations-else-nested fallback `:559-578`, at-rule dispatch,
  descriptor validation (`@property` `:638-674`, `@function` prelude `:595-622`,
  scope prelude `:524-557`, scroll/view-timeline `:703-721`, unknown passthrough
  `:723-733`), `parseItems`/`parseStylesheet` `:743`, top-level selector comma
  split `:738`, declaration-name handling `:391`.

## Deliverables (under `pi/mirror/`)

1. **`stylesheet/blocks.ts`** — block-structure grammar (prelude/body, quotes,
   comments) transposing `blocks()` semantics. Uses W0 `balancedUntil` +
   transposed `splitTopLevel` for the depth/quote-aware seams (H-7 — the
   imperative recognition layer; NOT combinators).
2. **`stylesheet/declarations.ts`** — declaration grammar + Tier-2 validation
   wiring (R7 `--*` case-preserve; R12 `--*` value fallback; R2 via W4).
3. **`stylesheet/at-rules.ts`** — D-2 dispatch after `@`: `k`eyframes · `p`roperty
   · `f`unction · `v`iew-timeline · `s`-bucket (scope/starting-style/scroll-
   timeline, 3-way lit ladder) · default→unknown passthrough. All 9
   `StylesheetItem` kinds + descriptor validators.
4. **`stylesheet/index.ts`** — `parseItems` / `parseStylesheet`.

## Ledger rows owned (PI §2)

- §2a: `parseStylesheet`.
- §2b: `animation_option_invalid` (declaration side); the StylesheetItem/StyleRule/
  KeyframesBlock/PropertyRule/CustomFunctionRule/descriptor type cluster exercised.
- §2c seam-probe symbols: `parseStylesheet`, `Stylesheet`, `StylesheetItem`,
  `StyleRule`, `CSSPropertyDescriptor`, `CustomFunctionDescriptor`,
  `CustomFunctionParameter`.
- §2d corrections: **R5** (preceding comment is trivia → clean decl name; the
  lexeme layer gives this free — assert it) · **R7** (preserve `--*` case,
  lowercase others; css-vars 1) · **R12** (`--*` value: attempt `parseCssValue`,
  on failure fall back to raw keyword scalar `{type:"keyword",value:<raw>}`) ·
  **R2** declaration side (H-5 acceptance rule; the coded `animation_option_invalid`
  path stays — R8 KEEP).
- §2d KEEPS: whole-sheet **fail-fast** (Q8 CONFIRMED contract — kf
  `validate.ts:182` depends on whole-sheet `ok:false`); unterminated comment fails
  the sheet.
- §2e SUBSUMED: gap 10 slice (frozen 9-kind at-rule model); gap 6 slice
  (@keyframes/kf rules). Recovery / typed selectors / media queries DEFERRED
  (V09/V07/V08 — unknown-at-rule passthrough is the frozen behavior).
- §2g: **H-7** (balanced scanners carry own tests — already in W0 `scanners.test`,
  re-exercised at every raw-string seam here).
- §2h: **K-8** (L door; W5a/W5b split pre-authorized — journal a work-order,
  StructuredOutput crash on over-scope is the L-tranche lesson).

## Born-RED gate (H §5)

`parseStylesheet` differential **GREEN** on: all 9 `StylesheetItem` kinds;
nesting; every descriptor validator (`@property`/`@function`/scope/scroll-timeline/
view-timeline); unknown-at-rule passthrough; **R2/R5/R7/R12**; the **hostile set**
— empty bodies `{}`, dangling slash, unterminated comment, `!important` trailing,
comment-before-name (R5), uppercase `--*` (R7), nested recovery-not-attempted
(**fail-fast** affirmed). Per-door ≥50 accept / ≥20 reject (H §3B).

## DO-NOTs

- Do NOT implement CSS-Syntax-3 error recovery / drop-invalid-and-continue —
  fail-fast is CONTRACT (Q8; `recover()` exists in the engine but is UNUSED).
- Do NOT lowercase `--*` declaration names (R7); DO lowercase all others (`:391`).
- Do NOT hard-parse `--*` values into rejection — R12 fallback to raw keyword.
- Do NOT typed-parse selectors / media queries / attempt recovery (DEFERRED
  megatranche); unknown at-rules pass through verbatim.
- Do NOT freeze any result (§2; K-2).
- Do NOT touch repo `src/`, `scripts/dev/dev.sh`, `vnext/`, keyframes, INBOX.
