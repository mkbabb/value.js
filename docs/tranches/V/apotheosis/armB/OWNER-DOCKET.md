# OWNER-DOCKET — the single merged decision sheet (arm B, all six programs) — 2026-07-19

**47 rows** merged from the six FINALs' dockets (PA OD-PA-1..8 · PB OD-C1 + banked rows ·
PC OD-S1..S8 · PD OD-1..9 · PE OD-api-A..J · PF §3 incl. VIS rows), deduped with sources
cited. This sheet is F-GOV-03's ratification input; it supersedes PF §3 wherever a FINAL
amended an r1 row (conflicts resolved final-over-r1; a direct-edit grant can never arise
from silence). Record-state trichotomy governs: intact record → owner decides · refuted
record → shield pierced + correction duty · fence ruling → cite-to-overturn.

**Timing bands:** [PRE-SWAP] gates PA-W3's cut · [FRONT] ratified at F-GOV-03 before
dependent work · [DESIGN-GATE] ruled at the owning wave's design gate · [BOUNDARY] feeds
F-GOV-05.

| ID | Question | Evidence | Recommendation | Default-if-silent |
|---|---|---|---|---|
| D-01 [FRONT] | kf `orchestration/flip` (176 LOC): keep vs prune | EP-3 PATH-B decided-terminal row, machine-checked; auto-flip trigger unfired (PF OD-1, P3.1) | owner decides — record intact | KEEP (no motion without ruling) |
| D-02 [FRONT] | kf `svg/draw-svg` (~194): keep vs prune | EP-3 PATH-B; atlas hits prose-only but the record holds a real historical live-proof (PF OD-2) | owner decides | KEEP |
| D-03 [FRONT] | kf `physics/morph`: keep vs prune | rides flip's PATH-B; glass `MorphRect` TYPE-only edge (PF OD-3) | couple to D-01 | KEEP (follows D-01) |
| D-04 [FRONT] | kf `scroll/` (1,233): keep vs overturn-and-prune | K.W9 book + compile-wired + native fast lane; K-1 thrice-affirmed fence — cite-to-overturn with new Baseline evidence (PF OD-4) | KEEP (default) | KEEP |
| D-05 [FRONT] | kf `ingest/` (835): the round-trip thesis | K.W8 book intact; zero consumption disclosed (PF OD-5) | owner rules the thesis | KEEP (record intact) |
| D-06 [FRONT] | kf `internal/` target shape (restructure is MANDATED — the owner's dislike) | 535 LOC / 9 leaves / fan-in 11 (measured); LT-10's 40+-importer cost census stands; G5′ names the superseded ruling; depcruise leaf key `:171` moves with any rename | **(c) split by affinity** — scheduler/raf → engine-adjacent, errors/reduced-motion → constants-adjacent (PC-final OD-S1, the only option that IS a restructure); (b) first-class-primitives rename priced as the cheap fallback (PF OD-6's stale r1 rec) | (c) |
| D-07 [PRE-SWAP] | R-HDR `ictcp()`/`jzazbz()` parse: KEEP vs EXCISE at the cut | the productions are IN the deposed tree (post-swap they exist unless excised); the 11-day unpapered 3.1.0 drop; asymmetry exhibit: the factories are LIVE at `color/index.ts` while css parse is dead (PA OD-PA-2 + PB exhibit + PF OD-7) | KEEP-lean (three sheets concur); excision carries §7-law-1 tombstone duty | KEEP at cut |
| D-08 [PRE-SWAP] | `spring()` grammar ownership (solver stays kf, K F6.6) | deposed production `timeline/easing.ts:143-153` rides the retype; timing-function grammar precedent P1.4 (PA OD-PA-3 + PF OD-8) | **VALUE** — CSS-spec territory (all three sheets concur); if kf-owns: excise-at-cut + spec routes via PA-W7 | VALUE |
| D-09 [PRE-SWAP] | R-EVAL: the deposed 536-LOC calc/math evaluator is WIRED into the root `ValuesValue` production; v4 css has zero calc | `index.ts:7` imports `createMathFunctionParsers`; extant css calc grep = 0 (PA OD-PA-4 + PF OD-9) | **RETIRE** — excise the arm at cut with by-name tombstone; DOM-resolution status quo; kf `resolve/` owns calc; restore = a capability ADD needing its own vectors | RETIRE + tombstone |
| D-10 [PRE-SWAP] | Spec-scope bound: does "ENTIRE CSS spec" (C12) include selectors/media/gradients/filters/transforms? | all five exist as modules in bbnf's `grammar/css/l4/` 15-module exemplar (PA OD-PA-1) | bound to the /css surface's domains for THIS program; extensions ruled-in land at PA-W5 or spawn registry follow-ons — the narrowing is RULED, never silent | bound-to-/css, ruled |
| D-11 [pre-W6] | BBNF home: in-value vs extend bbnf's l4 suite | C1 assigns bbnf-lang evolution to the BBNF session — landing our suite there crosses the ownership boundary; duplication vs the existing 15-module corpus PRICED; "BBNF grammar files" tombstone named-and-overturned by C14 (PA OD-PA-5 + PF OD-23) | **IN-VALUE**; reconciliation routed by ask letter | IN-VALUE |
| D-12 [PRE-SWAP frame] | Surface thaw: signature-freeze vs C12's exact-DOM-congruent types | the letter's actual law is "signatures preserved", not a byte-freeze of the 33 types; 19 value + 33 type exports machine-censused (PA OD-PA-6) | signature-freeze STANDS; ADDITIVE type motion lawful within it; any RESHAPING of the 33 rides the co-land boundary | as recommended |
| D-13 [PRE-PUBLISH] | Parser-swap release vehicle: 4.1.x minor vs the value-5 boundary | glass peers value `^4.0.0` — a 4.1.x reaches every ^4 consumer immediately; precondition: the W0/W3 divergence ledger EMPTY or spec-corrections-only (PA OD-PA-7 + PF OD-16) | 4.1.x-eligible ONLY with the precondition; default = FOLLOW W56's SCI-1 cut, never ride it without that program's explicit pricing | FOLLOW W56 / hold to boundary |
| D-14 [with PA-W7] | kf `easing.ts:30/:38-39`: consume-vs-ratify the two-regex timing-name classifier | zero productions; documented "value.js-free light engine" law + F.W7 round-trip rationale ON DISK; sweep verdict "legitimate CSS-keyword classifiers" (PA OD-PA-8 + PF CAR-d) | **RATIFY as fenced default**; consume variant priced in the brief with the overturn burden explicit | RATIFY |
| D-15 [FRONT] | `contrast-color()` WCAG metrics: republish vs evaluation-internal | frozen-facade parsimony; both arms costed; wave executable under either ruling (PB OD-C1 + PF OD-13) | internal-lean | internal |
| D-16 [FRONT] | R-SOA: banked row + the bench threshold | 3.0.0 tombstone stands; null hypothesis = Into loops meet budgets (C21); threshold must be ratified BEFORE any exhibit runs — no post-hoc goalposts (PB docket + PF OD-17) | ratify the measured batch-win threshold NOW; prototype spend only on trigger; terminal either way | stays BANKED; null hypothesis wins |
| D-17 [FRONT] | R-BOUNDARY gamut samplers: banked on the W53 pull | CARRY-LEDGER W53/B1; the F-PD-22 needs-memo (emitted at F-PD-02) is the unblocking artifact; full π/DELTA travels with the trigger (PB docket) | iff pulled: sampler API from the F-PB-04 kernel + raytrace agreement; else stays banked (re-trigger: any gamut-viz consumer, four-tree census at pull) | BANKED |
| D-18 [FRONT] | Census owner-tier rows from F-PB-02 (colorFilter restore-vs-tombstone chief among them; GAMUT_SECTOR_COEFFICIENTS, HDR raws, per-pair fast paths, sampleGamutBoundary/Into) | every symbol verified published at `164343c1^:src/subpaths/color.ts`; the no-advocate⇒no-tombstone mechanism's unpapered families (PB F-02) | per-row ruling at F-GOV-03 with four-tree consumer census at pull | DECLINE-tombstone (paper, not code) |
| D-19 [VIS] | `transform/decompose.ts` (609) PRUNE vs kf-adoption | 0 consumers on all four trees, re-verified; its 10 `./transform` symbols are a SURFACE cut → F-PC-03 ledger row executing at F-GOV-05 (PC OD-S2 + PF VIS-2) | PRUNE, tombstone RIGHTLY — visibility row; proceeds unless the owner objects | PRUNE at boundary |
| D-20 [VIS] | kf `physics/oscillator` (~150) prune | record REFUTED: `published-surface.md:44`'s BOTH named consumers re-verified FALSE on disk (0 hits glass-ui/src + kf/demo); shield PIERCED (PF VIS-1) | prune + record-correction rider; proceeds unless the owner objects | PRUNE + correction |
| D-21 [FRONT, pre-gate] | kf-demo execution mode for F-PD-15..19 (+ kf delivery generally) | C4 pulls the kf demo in-scope and WINS on scope (addenda-WIN law); P4.5 requires an explicit grant for cross-repo edits (PD OD-1 + PC OD-S4 + PF OD-14 — the one open conflict: PD recommends (a), PF recommended (b)) | **(a) direct edits with owner countersign at ratification** for the kf DEMO series (PD's adjudicated finding); library items stay dispatch regardless | (b) DISPATCH — silence cannot mint a grant; under (b) the kf waves' close gates become dispatch+verification-window, stated honestly |
| D-22 [FRONT] | Glass relay batching: two batches (#1 early) vs single held batch | centerpiece starvation PROVEN under the held default (three waves could not execute inside the program); one letter = the priced interruption (PD OD-2; PF OD-22's hold-rec superseded by PD's thrice adjudication) | **two batches, #1 EARLY** (EasingPicker variant, WatercolorDot API, AcceptableValue type, layer-transition composable) | two batches, #1 early |
| D-23 [DESIGN-GATE] | kf old share-link posture when `?state=` goes readable | no-dual-paths edict vs link continuity; real shared links die (PD OD-3 + PF OD-18) | hard cut (edict-conformant) | hard cut |
| D-24 [conditional] | EasingPicker precision law — ONLY IF F-PD-03's A/B shows a perceptible stepping percept | the 3-decimal claim ≈0.30px/step at the 304px seat adjudicated IMPERCEPTIBLE; the low-res percept lives in the specimen tiles; the prior OD-4 KILLED as resting on a misdiagnosis (PD OD-4′; PF OD-19 superseded) | no docket unless the measurement mints one | row dissolves (no action) |
| D-25 [DESIGN-GATE] | C3 mobile-toolbar form: persistent second band vs one-gesture dock surface | owner-marked centerpiece; decided JOINTLY with the W47 shell gates (PaneSegmentedControl 1→0 collision) (PD OD-5 + PF OD-20) | thrice delivers BOTH candidates at the F-PD-06 design gate; owner picks | wave BANKS at the design gate (ship neither without the pick) |
| D-26 [DESIGN-GATE] | WatercolorDot dose-column class: substrate vs hover whisper | glass perf/dose law vs the breath-of-life ask (PD OD-6 + PF OD-21) | substrate-by-law; whisper only if the glass agent ratifies the exception | substrate |
| D-27 [FRONT] | CH-4/W54 boot-kill ownership confirmation | a ratified close-law must have exactly ONE owner; no sibling program claims it (PD OD-7) | F-PD-12 owns; re-homing is an explicit act | PD owns |
| D-28 [FRONT] | D53.vi app-root rename (`demo/color-picker/` → `app/`) | CARRY-LEDGER §C: "next formation decides"; tree churn vs naming truth (PD OD-8) | route to PC's tree-move machinery if approved; else retire with rationale | RETIRE with rationale |
| D-29 [at F-PD-24] | §C/§F untracked residue: D49 `research/` trio + `audit/rehearsal/` (carries W44's HeaderRibbon manifest edit ON DISK) — track vs archive | history truth vs tree hygiene (PD OD-9) | TRACK (they carry decision provenance) | TRACK |
| D-30 [FRONT] | `stylesheet.ts` R4 tracked-RED carry (899 > 500) | allowlisting is the masked-fallback lie; the file dies with R-PARSER (PC OD-S5 + PF OD-24) | tracked-RED, NAMED transfer to PA-W3 | tracked-RED |
| D-31 [FRONT] | `transform/path.ts` (564 > 500): split vs reasoned allowlist | kf-consumed spine (PathGeometry ×2); natural seam = geometry vs parsing/serialization (PC OD-S6 + PF OD-25) | split at the natural seam iff thrice finds one; else ONE reasoned allowlist row | thrice-conditional as stated |
| D-32 [BOUNDARY] | Export-surface grammar at the co-land boundary: kf `.`+`./engine` vs value 7-subpaths-no-root | the largest cross-repo structural divergence; live pain documented (the demo hand-copied `debounce` over a ~36KiB chunk drag) (PC OD-S7) | decision row ONLY (no wave — KISS); options: (i) RATIFIED-DIVERGENCE / (ii) value adds a root key / (iii) root+domain doors both sides; feeds the F-PC-03 ledger; resolves the debounce row either way | (i) status quo RATIFIED-DIVERGENCE |
| D-33 [FRONT] | Router idiom, both demos: ratify the render-null Stub navigation-model vs condemn | BOTH routers verified (`Stub = { render: () => null }`, no `<router-view>`, in-file documented, isomorphic across the pair) (PC OD-S8) | RATIFY as the pair's DEFINED shared idiom; F-PD-14 builds C6 on it; condemnation = churn without a defect | RATIFY |
| D-34 [FRONT] | Mixing verb design — the REAL trichotomy: (i) client-only ratify+tombstone / (ii) RESTORE with fourier's no-op-422 (fork DIES, priced) / (iii) RESTORE with value's own J.W2 semantics (empty-diff remix; fork folds in) | "fork = remix-with-zero-overrides under the 422 guard" is internally contradictory and OFF the sheet; `a8ff7792^` forks.ts: "fork IS remix-with-empty-diff: ONE code path"; fork UI live (PE OD-api-A) | **RESTORE via (iii)** — value's own precedent, no capability loss; consumer-in-gate MANDATORY either way; NO server color-math endpoint (sub-ruling stands) | (iii) |
| D-35 [FRONT] | Anon-create + born-visibility policy | value allows `userSlug: null` AND every palette born `visibility:"public"` → anon rows born public yet permanently unmanageable; NO shipped client exercises anon create; fourier born-draft + required owner (PE OD-api-B) | **CONVERGE: require non-null owner** (the sweep's own recommendation, restored); fallback if the owner keeps anon: anon rows born NON-public | non-null owner |
| D-36 [FRONT] | Visibility semantics: enum naming; unpublish target; transition policy | value HAS a live guard (`assertVisibilityTransition`, `visibility.ts:51-88`); all-9-tuples-legal is DOCUMENTED design (J.W1c §5.1); fourier forbids pairs (PE OD-api-C, premise corrected) | CONVERGE SEMANTICS, RATIFY NAMES: per-repo legal-transition table at the existing `from` composition point IFF v3 defines forbidden pairs; unpublish target stays each repo's non-public default | as recommended |
| D-37 [FRONT] | Stale/malformed cursor: fourier 400 `urn:contract:cursor-invalid` vs value's DOCUMENTED graceful restart | value's restart is a decided record (D3/D.W2 Lane D, `crud-list.ts:53-56` verbatim), not a masked fallback (PE OD-api-D) | CONVERGE on 400 — but as a NAMED overturn of D3; demo migration priced (silent restart today) | RATIFY-DIVERGENCE (an overturn never defaults) |
| D-38 [FRONT] | Idempotency backing: in-process LRU vs Mongo 24h TTL | the LRU is the SANCTIONED single-replica KISS relaxation (D2 §3 P2, in-file verbatim; body-hash conflict already contract-faithful) (PE OD-api-E) | **KEEP the relaxation** unless the owner overturns D2 §3 P2 by name; recorded in v3 either way | KEEP |
| D-39 [FRONT] | Restore verb: fourier live `POST /{slug}/restore` vs value's deliberate retirement | value V·W45 item 1 record intact + deliberate (typed 404; versions/revert is the honest surface) (PE OD-api-F) | **RATIFY DIVERGENCE** — shield unpierced; fourier keeps restore | RATIFY |
| D-40 [FRONT] | History verbs: value-only revert + versions/:hash — port to fourier vs ratify | no fourier product ask on record (PE OD-api-G) | **RATIFY DIVERGENCE**; a port row BANKS with re-trigger (a fourier revert ask) | RATIFY |
| D-41 [FRONT] | `/shares` snapshot endpoint: build vs bank | spec ships in v3 so activation is a switch (PE OD-api-H) | **BANK** with named re-trigger (encoded state >~1.5KB, or a cross-device share ask) | BANK |
| D-42 [FRONT] | ETag derivation basis: at-rest `currentHash\|updatedAt` vs canonical-digest | identical 428/412/`*` semantics; byte-compat never exercised (PE OD-api-I) | **RATIFY DIVERGENCE** | RATIFY |
| D-43 [FRONT] | Fourier execution mode + the like/flag pull-in | C8 puts fourier in-scope but no direct-edit grant exists; pre-B.W3 fourier SHIPPED like+flag routes (`52bdcf5^`), carved out at B.W3, scar tissue survives — a record-bearing drop (PE OD-api-J) | (a) direct-edit grant REQUESTED (C8 scale favors it); (b) RESTORE both patterned on value's votes/flags twins — only by owner pull-in | (a) dispatch mode; (b) stays docketed (no auto-implementation) |
| D-44 [BOUNDARY] | Glass peer strategy at the crossing | wedge ERESOLVE-hard either way; prerelease semantics priced (`^4 || ^5` does NOT admit rc) (PF OD-12) | **RANGE-WIDEN `^6\|\|^7` + `^4\|\|^5` as a glass MINOR pre-boundary** (prerelease-admitting variant iff a staging window is used); hard bump = the alternative; ratified ONCE — the train preamble cites the ruling | range-widen |
| D-45 [FRONT] | easing D13 halved-curve rider: restore vs tombstone | from D13 (PF OD-11; P3.2 easing row) | restore-or-tombstone — terminal either way, no silent carry | TOMBSTONE with rationale (restorable later by a new wave) |
| D-46 [FRONT] | THE OPUS-MECHANICAL ROSTER (L1 §0 rider: "per-tranche owner-ratified") | named seat classes: census scripts, vector transcription, mechanical resurrection sweeps-under-pattern, import rewrites, fold/delete sweeps, chase edits, ledger checkers (PF OD-15 + C17) | ratify the named list for THIS tranche; Fable holds all design/adjudication | NO Opus seats (all-Fable) — ratification cannot default |
| D-47 [with F2-api-03] | oklab boundary acceptance-domain matrix + at-rest backfill | `currentColor`? `var()`? `none`? — owner-visible, not an in-wave pocket; `oklabColors` is denormalized + radius-searched (PE W3 deliverables) | owner rules the matrix per v3; explicit backfill/no-backfill row | reject CSS-wide keywords (fail-explicit, the D-HARDEN posture) + NO backfill |

## Supersession records (decided, papered — never re-asked)

- **api/ STAYS at `/api`** — C7 is the owner's LATER ruling (addenda WIN); P3.2's EXTRACT row
  RETIRED-superseded with a named tombstone (executes as F2-api-07e's registry row; PC OD-S3
  merged away; PF r1's "or re-rule" removed as re-litigation).
- **parse-that leveraged-as-published, semver ≤1.1 forever** (C1/C15) — charter matter;
  preventive seat = the F-GOV-06 formation-grep + the cap in every PT-E letter.
- **SCI-1 = DECIDED SHIP-4.1.x (D54; vehicle W56)** — inherited DECIDED; R-INTO extends,
  never re-opens; D-GAP-6 `sampleBezier` rides the same vehicle.

## Unilateral riders (evidence-complete, fence-clean — no docket row; owner VISIBILITY only)

- kf `orchestration/split-text` (486 LOC; 0 consumers on all four trees; casualty = the
  split-a11y oracle, named) · kf `svg/motion-path` (~180; 0 consumers; no EP-3 row) ·
  value `quantize` DEMOTE-to-demo (+ `./quantize` key drop via the F-PC-03 ledger).
