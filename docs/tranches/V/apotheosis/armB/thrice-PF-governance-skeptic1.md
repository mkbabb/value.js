# THRICE — PF-GOVERNANCE — SKEPTIC 1 (assume the program is WRONG)

**Target:** `armB/program-PF-governance.md` · **Date:** 2026-07-19 · **Stance:** adversarial by charter (C20) — every claim below carries evidence gathered this session (sweeps re-read; repos, git tags, and all five sibling program files probed). Verdict up front: **the program's central boast — "zero silent drops … No row of the canon relevant to governance lands nowhere" (§5) — is FALSE at formation time**, its flagship "born-RED" gate is vacuously green at birth, its retro birth-certificate is unrunnable as specified, and its own DAG violates its own ratification law. The 8-wave shape is 6 parts process to 2 parts enforceable reality, against C21's explicit instruction. Counter-program: 5 waves, at the end.

---

## A — COMPLETENESS ATTACK: the zero-drop claim fails against the formation's own roster

### A1. THE PHANTOM SIBLING (the killshot). PF routes ~20+ canon rows to a program that does not exist.

PF §0 names its siblings "(parser, color, structure, frontend, **apparatus**)" and its §5 landing map routes to `[SIBLING:apparatus]` at least: P3.3 rows 1–8 (the ENTIRE kf gates program — W9 land, lint/proof:structure CI wiring, lighthouse demote, weekly relabel, boundary-cohesion fold, zone-test binding, sequencing), P3.4 rows 1/2/3/5 (incl. **the e2e adjudicated set**), P0.2 rows 6–14/16–18 partials, kf R2-07/W9 staging, CH-05 (program lines 352, 364, 366, 369, 379).

The armB formation on disk is: `program-PA-parse.md`, `program-PB-color.md`, `program-PC-structure.md`, `program-PD-frontend.md`, `program-PE-api.md`, `program-PF-governance.md`. **There is no apparatus program.** Grep "apparatus" across the five siblings: PA=0, PC=0, PD=0, PB=1 (incidental), PE=2 (incidental).

Worse, the handoff is MUTUAL-to-phantom: PC-structure line 429 routes "P3.4 rows 1,2,5,6 (producer+api scope, e2e set, bench, capability-diff) | **HANDOFF → gates/parser programs**", and PC's closing roster (lines 450–457) expects a "**Zones program**" and a "**Gates program**" — neither exists either. Net effect: **the e2e per-oracle adjudication (~13.4k LOC; the owner's addendum-3 EXPLICIT challenge, P3.4 row 2's ~5.9k ABROGATE / ~1.7k FOLD / ~1.7k KEEP disposition) has NO owning program anywhere in armB.** Same for most of P3.3. PF is the self-declared routing spine; its G-08 "audits sibling receipt before close" — receipt by a program that does not exist cannot be audited. This is the exact silent-drop laundering mechanism the program claims to exist to kill.

### A2. Docket double-booking — two sheets, one phantom owner.

PF §3 claims THE single owner-decision docket ("single sheet", L1 §9). PC-structure line 454 assigns "P3.1 verdicts + **owner docket** (F-08 hard dependency)" to the phantom Zones program, and PC §5 (line 391) carries its OWN "OWNER-DECISION DOCKET (rows this program surfaces)". Two docket sheets plus a phantom third owner is not "presented as a single decision sheet"; PF, the coordination program, never reconciled them.

### A3. Roster inconsistency inside PF itself.

PE-api EXISTS (`program-PE-api.md`) but is absent from PF §0's sibling list; PF §4/§5 then route rows to an "API program" it never chartered ("C6 … C14 → [SIBLING:parser/color/API]"). The program that owns the constellation's coordination cannot state its own formation's roster.

### A4. C11 is missing from the landing map.

PF §5's addenda block enumerates C1–C8, C10, C12–C23 — **C11 (the structure edict: displaced tests, module-name strip, goldilocks granularity, glass-ui reference model, full thrice-analyzed DAGs) appears nowhere in the map.** It happens to land in PC (2 cites), but the zero-drop audit instrument itself has a hole: a receipt audit keyed on PF's map would never check C11.

### A5. C6 is mis-routed.

"C6 (routing/state) → [SIBLING:parser/color/API]" (line 392–394). C6 is demo URL-state + share-URL facilities — frontend work, actually owned by PD-frontend F-10 (`program-PD-frontend.md:198` "C6 — URL STATE + SHARE, WHOLE-APP"). The router filed the row under the wrong sibling on a word-match ("routing"); a G-08 receipt check against PF's map would interrogate PE/PA and find nothing, and PASS or FAIL for the wrong reason either way.

### A6. The kf-WAAPI engine row: dropped by the very channel that claims monopoly over it.

PF's charter: "All sibling programs' kf items ride THIS channel" (G-03). PA-parse F-04 does exactly that: "kf's full-WAAPI ENGINE support (the absent members measured in sweep-kf-lib §5 — persist(), pseudoElement, iterationComposite, getAnimations(), CSS.registerProperty, updatePlaybackRate, per-keyframe composite, eligibility liberalization) … **routes by name to the kf program's WAAPI wave via the kf inbox**" (`program-PA-parse.md:49`, reiterated :134/:147). PF's G-03 initial dispatch pack (cars a–e) contains **no WAAPI car**, and PF's §5 routes C12 wholesale to "[SIBLING:parser/color/API]" with no kf-engine half. The program claims its truth base "both consumed whole"; sweep-kf-lib §5 measured this exact absent-member list and PF landed none of it. A sibling routed a row INTO PF's channel and the channel has no slot for it — silent drop, first order.

### A7. The standing mail/INBOX law is absent from the coordination-spine program.

Owner edict 2026-07-17 (E13, standing law): every value.js session/wave opens with the 4-path coordination-mail sweep; `docs/tranches/V/coordination/INBOX.md` is the durable ledger; no wave closes with unread mail. PF cites the glass-relay fond (G-05) but never carries the mail law — the one standing owner edict squarely IN its domain. A governance program that codifies channel grammar while omitting the owner's standing inbox discipline has the wrong completeness bar.

---

## B — GATE ATTACK: vacuous, mis-labeled, or unrunnable as specified

### B1. G-01's "born-RED" gate is vacuously GREEN at birth.

Claimed: "born-RED — the defect is live today: RED = a wave spec citing any of the three drifted vite anchors at their P4.3 line numbers (:41/:156/:229)." Checked: **no armB wave spec cites the stale numbers** — PC-structure, the only anchor-citing sibling, already cites the RE-DERIVED values (":41→:37, :156→:153, :229→:227 — the mandate is vindicated", `program-PC-structure.md:264`). The drift lives in the frozen LETTERS (canon, not a "ratified wave spec"), so the RED condition cannot fire against anything the formation produced. A gate green at birth that only fires on hypothetical future sloppiness is a lint rule, not a born-RED gate; labeling it born-RED is exactly the close-class inflation the canon names. Additionally, the GREEN criterion ("a re-derivation stamp (HEAD + date) newer than the pinned tree's HEAD") is incoherent — HEAD advances continuously; any ratification-time stamp is stale the moment glass or kf lands a commit. The real control is execution-time re-derivation, which is a STEP in each executing wave's procedure — a template line, not a wave-owning gate.

### B2. G-05's gate cannot fail — self-referential by construction.

"RED = any glass-touching change in ANY sibling program shipping outside a batch (**checked against the ledger — the ledger check is the gate**)." A change shipped outside the batch by definition never enters the ledger; the check can only see what self-reports. No detection mechanism is named (a real one exists and is cheap: close-time join of every sibling program's deliverable paths grep'd for glass-ui file paths against ledger rows). The program diagnoses this exact disease at G-03a ("a gate that cannot fail is the lighthouse-mask disease") and then ships it at G-05.

### B3. G-06's REDs are review prose, not gates.

"RED = … any parse-that version >1.1 **proposed anywhere in the formation**" — no checker exists or is specified for "proposed anywhere." The scripted `npm view` assertions run post-cut, when tags are immutable — detection strictly after irreversibility. The one enforceable seat (a pre-publish version-cap check inside parse-that) is outside this tranche's write scope per C1 (a separate BBNF session owns parse-that). As written, the wave's acceptance gates cannot mechanically fail before the harm.

### B4. G-02's RED-1 "birth certificate" is unrunnable as specified.

The gate is spec'd as "export-census script diffing the built public surface (**the 7 frozen subpath keys**) against the prior published tag," with RED-1 = the retro-run v3.1.0→v4.0.0. Verified this session: `git show v3.1.0:package.json` → exports = **8 different keys**: `.`, `./color`, `./parsing`, `./math`, `./easing`, `./transform`, `./units`, `./quantize` (plus runtime dep `@mkbabb/parse-that ^1.0.0`). The v3.1.0→v4.0.0 delta therefore includes **three whole-key extinctions (`.` root, `./parsing`, `./units`)** that a census framed on "the 7 frozen subpath keys" structurally cannot represent — while the famous symbol-grain extinctions (deltaE/raytrace/okhsl/Into) died UNDER keys that survived (`./color`). The spec conflates key-grain and symbol-grain; the actual instrument must be a two-sided (subpath, symbol) walker over BOTH published tarballs. As written, the "MUST go red" birth certificate either cannot run or reds on the wrong axis.

### B5. The G-07/G-08 ordering bug: ratification after the cargo ships; go/no-go after the crossing.

G-04's own gate: "Prune riders EXECUTE only post-owner-ratification." §3's header: the docket "ratifies via **-08**." G-08 is terminal ("Dependencies: all F-PF-governance waves"), i.e., AFTER G-07 — yet G-07 declares "prunes are boundary cargo" and depends only on "-04 (docket ratified…)", contradicting §3's ratification locus. And G-08's "boundary go/no-go memo" adjudicates a crossing that, per the DAG, has already happened. Either the docket ratifies at a mid-program owner gate before G-07 (correct), or the program's own law is violated by its own dependency graph. As written it is both ambiguous and self-contradictory.

### B6. The manifest gate is the weak (denylist) form.

"fails on runtime `dependencies` ∩ {self, glass-ui, keyframes.js}" — correct to permit parse-that (v3.1.0 shipped exactly `@mkbabb/parse-that ^1.0.0`, verified; R-PARSER re-adds it), but a 3-name denylist passes ANY other accidental runtime dep. The historical invariant is stronger and simpler: **runtime deps ⊆ {@mkbabb/parse-that}** (allowlist). KISS-stronger, same LOC.

---

## C — RE-LITIGATION, PRIORITY, AND ALTITUDE ATTACKS

### C1'. OD-10 re-opens a DECIDED row.

C7 is the owner's LATER ruling ("Palette API should exist within its current /api"; addenda WIN where they tighten). The canon's own law: inherit as DECIDED; never re-derive; re-booking forbidden. The reconciliation duty C7 names is a NOTE retiring P3.2's EXTRACT row with rationale — not an owner-decision row inviting the owner to re-rule their own ruling ("or re-rule"). Docket bloat is not neutrality; it is re-litigation with extra steps.

### C2'. OD-12 double-state.

G-06(d) codifies RANGE-WIDENING as a ratified train LAW ("landing BEFORE the boundary"), while OD-12 keeps widen-vs-bump open for owner ratification at G-08. The train can pass its own GREEN with an unresolved docket row contradicting clause (d). Pick one: a recommendation-with-default inside the docket row, ratified once.

### C3'. The easing brief is falsely symmetric.

PF adopts the record-state trichotomy (fence/documented law → cite-to-overturn), yet G-03(d) dispatches consume-vs-ratify as a symmetric coin-flip. CONSUME requires overturning kf's DOCUMENTED value.js-free light-engine law plus the depcruise leaf rule (`.dependency-cruiser.cjs:171`); PF's own sweep already concluded "a two-regex string classifier, zero productions … legitimate CSS-keyword classifiers, the strongest consume-vs-ratify candidate" for RATIFY (sweep-kf-lib §4). The brief should carry RATIFY as default with the overturn burden explicit. Manufacturing an open adjudication out of a fenced default is contrived process (C21).

### C4'. Wave-shape / altitude: 6 of 8 waves are documents.

G-01 (register file), G-03 (protocol doc + dispatches), G-04 (docket sheet), G-05 (ledger + checklist), G-06 (RELEASE-TRAIN.md), G-08 (audit memo) produce paper; only G-02 and G-07 touch enforceable reality. C21: "Little time on contrived gates or process." PIN-REGISTER.md is ceremony that contradicts its own law — any written pin is stale on write (the program itself concedes "glass advances quickly — re-derive at dispatch"); G0′ re-pinning is a template line stamped into every wave spec, not the program's first wave. Meanwhile **the single cheapest, highest-risk defusal in the whole evidence base — the committed deps bomb (a publish from `tranche-u@c654824e` ships the registry kf↔value cycle TODAY, sweep-value-lib §4) — is a 2-line package.json move queued BEHIND wave 1's register ceremony.** Wrong priority by the program's own risk table.

### C5'. Model routing omits the required rider.

L1 §0: Opus "may only ever return for pure mechanical implementation sweeps in a LATER phase, **per-tranche owner-ratified**." PF pre-assigns "Opus mechanical" across G-01/02/04/06/07 with no ratification rider. Small, but the program is the one policing declaration≠execution (P0.1) for everyone else.

---

## D — WHAT SURVIVED THE ATTACK (skeptic's honesty ledger)

- **VIS-1/oscillator: CONFIRMED on disk this session.** `published-surface.md:44` is EXACT at kf `81a56990` (no drift), and both named consumers are FALSE: grep for `Oscillator|waveformValue|KF-OSCILLATOR` across glass-ui `src/` and kf `demo/` = **0 hits**. The pierced-shield rider is sound.
- The deps bomb (G-02 RED-2) is genuinely born-RED: committed at HEAD, verified.
- G-07's ERESOLVE proof matrix is a real, falsifiable instrument; the price-sheet numbers (47 kf files, /css 29, glass 5 imports, 3 atlas TimingFunction sites) match the sweeps/letters.
- G-03's ledger-completeness check (glob dispatch files vs INBOUND-LEDGER rows) is mechanically checkable — a real gate.
- The §1 pin table itself is accurate (value `c654824e`, kf `81a56990`, sweep-verified), and the 11-importer internal/ correction is right.

The disease is not the program's facts — it is the ROUTING (phantoms, misfiles, drops), the GATE SEMANTICS (three of eight waves carry gates that cannot fail or cannot run), and the SHAPE (paper-heavy, code-last, ratification after the ship sails).

---

## E — THE COUNTER-PROGRAM (5 waves, title + intent grain)

**W-G1 — DEFUSE + PUBLISH-INTEGRITY (code first, day one).**
Strip the committed deps block to devDependencies immediately (the 2-line defusal does not wait on a register). Land the pre-publish manifest gate in ALLOWLIST form (runtime deps ⊆ {@mkbabb/parse-that}; packed keys ≡ the 7). Land the capability-diff gate as a two-sided (subpath, symbol) census over PUBLISHED TARBALLS of both tags — the v3.1.0→v4.0.0 birth certificate must flag the `.`/`./parsing`/`./units` KEY extinctions AND the deltaE/raytrace/okhsl/Into SYMBOL extinctions, with the RIGHTLY/UNJUSTLY taxonomy attached; `--selftest` mutation-proof on the value gate and in the kf gate spec. (Absorbs G-02 + G-03a.)

**W-G2 — ROSTER + CHANNEL (kill the phantoms).**
Deliverable 1: the reconciled FORMATION ROSTER — the six real programs by filename, plus terminal homes for the two orphan workstreams: the e2e per-oracle adjudication and the kf gates/W9-land program get a NEW seventh program (P-APPARATUS) or are explicitly absorbed by name (e2e → a PD-frontend annex; kf gates → named G-03 dispatch cars) — no row may route to a program name that has no file. Deliverable 2: the dispatch protocol page + the FULL initial dispatch pack including the cars PF dropped: the kf-WAAPI engine uplift (PA F-04's ROUTE row, sweep-kf-lib §5 member list attached), the kf isomorphism rule + support-dir allowlist, W9-land + MR2/MR4 sequencing, scar deletions, and the easing brief with RATIFY as fenced default. Deliverable 3: the landing map REGENERATED from the roster as a 1:1 receipt table (canon row → program file → wave id), mechanically diffable — C11 and C6 filed correctly. (Absorbs G-01's registry + G-03; the pin/re-derivation law becomes a mandatory template block in every wave spec of every program, not a wave.)

**W-G3 — THE DOCKET (one sheet, mid-program owner gate).**
Merge PF §3 + PC §5 into the single sheet; retire OD-10 as DECIDED-STAY with the C7 reconciliation note; fold OD-12 into the train clause as recommendation-with-default. Prune riders (split-text, motion-path, oscillator+record-correction — all census-verified, VIS-1 confirmed on disk) ride here. **HARD GATE: owner ratification of docket + train order occurs HERE, before any boundary work — this is the go/no-go, moved to where "no" is still possible.** (Absorbs G-04 + the ratification halves of G-06/G-08.)

**W-G4 — TRAIN + BOUNDARY (one wave; the train is the crossing's plan section, not a doc wave).**
Sequencing folded into the crossing spec: 4.1.x (W56/SCI-1 vehicle, D-GAP-6 rider) → glass peer widen per ratified docket → freeze → value 5.0.0 → kf 7.0.0 → atlas ranges → post-cut assertions. Gates kept because they are real: the ERESOLVE proof matrix, capability-diff green-with-tombstones on both cuts, 44-key mirror re-verified by execution, npm view assertions. Glass batching lands here as the batch ledger + the three release criteria, with its enforcement mechanism NAMED: close-time join of all sibling deliverable paths (grep for glass-ui paths) against ledger rows — a check that can actually fail. (Absorbs G-05 + G-06 + G-07.)

**W-G5 — CLOSE AUDIT.**
Zero-drop re-walk against W-G2's receipt table — now auditable because every row names a real file and wave id; close-lie screen (vacuous-green, missing captures, masked fallbacks, re-booked chronics); disease-row screen; two consecutive clean passes; thrice per C20. (= G-08 with a substrate that can support it.)

**Demoted from waves to stamped template lines in every wave spec, formation-wide:** G0′ re-pin at open · anchor re-derivation at execution · `model_served` reporting (+ the L1 §0 owner-ratification rider on any Opus seat) · phase labels · parse-that ≤1.1 · the 2026-07-17 mail/INBOX sweep law.

Five waves, two of them code-bearing from day one, one owner gate placed where it can still refuse, zero phantom siblings, and every gate either machine-checkable or explicitly a review step — nothing labeled a gate that cannot fail.

*— skeptic 1, thrice loop, armB PF-governance. All probes this session: read-only; writes confined to this file.*
