# THRICE — PD-FRONTEND — SKEPTIC 1 (assumption: the program is WRONG) — 2026-07-19

Target: `armB/program-PD-frontend.md`. Canon read whole: L1 + L2 + CONVERSATION-ADDENDA.
Evidence: the four armB sweeps + fresh repo reads at the pinned trees (value
`tranche-u@db77dbd8`, kf `master@0dac636b`, glass-ui `master`) + the pre-2026-07-18 tranche
docs the packets' own ingestion set names (P6 row 4: CARRY-LEDGER.md, V-PRIME.md). Seat model:
`claude-fable-5` (verified from my own system prompt; a declaration is not execution).

VERDICT: **REJECT AS FORMED.** The program is a competent render of the four sweeps — and
that is the defect: it inherited the sweeps' blind spots and counts unverified, dropped the
value-side fold source almost whole, minted one owner-docket row on a misdiagnosis, and gated
a MOTION program entirely on still photographs. Its claim "18 waves, nothing load-bearing
dropped" (§1) is refuted below by name. What survives is real (F-08's grep gates, F-12's
core, F-15, OD-1/OD-3/OD-5, the relay discipline) — the counter-program keeps it.

---

## A — COMPLETENESS: the silent drops

### A1 · THE KILL: the CARRY-LEDGER §B tail (W46–W56) — the value frontend fold source — is dropped all but whole

The owner's program of record folds `docs/tranches/V/reformation/CARRY-LEDGER.md` **WHOLE**
into the next formation ("nothing lost: head, tail, or interval" — the file's own title
line). The V′ frontier stopped AT W46: the entire §B tail is the *unexecuted, ratified*
value frontend wave program — exactly the "pre-glass-7 carried-forward UI corpus" L1 §1/§4
orders audited NOW, and exactly the O13 fold-ALL-forward mandate the program's own backtrace
input calls "the letters' backbone." P6 row 4 puts CARRY-LEDGER.md in the mandatory ingestion
set. The program cites the file — once — for §F's gh-pages empty-mount row, plus name-checks
of W53/W55/W56. Everything else is ABSENT, and the §5 landing map has **no CARRY-LEDGER
section at all**. Dropped by name (CARRY-LEDGER.md:21-30):

- **W46 (F0 constitution & chrome law)** — seat-law/proportion-law/type-Card tuple, 18
  ratified composition pairs, the Fable+DesignSync design-system project opening at W46-open.
  This is the already-RATIFIED Aristotelian apparatus. F-01 re-invents that audit from
  scratch as a **parallel authority** — the exact sin P4.4 forbids on the kf side ("amend
  the ratified blueprint… never a parallel authority; silence re-litigates it").
- **W47 (F1 shell & scene)** — eleven-routes-direct, dock-expand scene rect 0px, the D53.iv
  coupling rows; §F routes the glass M1/M2 replies "fold at the receiving wave: M1→W47,
  M2→W47" — the program retires the ActionBarLayer shim (good) but the W47 fold targets are
  unrowed.
- **W48 (F2 picker instrument)** — A137/A138, pointer↔keyboard↔numeric↔AT parity, the Blob
  0px chassis with its named before-frame tag `v-blob-b0-26-ref-w40`.
- **W49/W50 (palette read/write)** — D57's Prepare→Download export seat + the retirement of
  legacy `demo/palettes/export.ts`/`usePaletteExport.ts`; D55.iii `__Host-value-session`.
- **W52 (F6 admin suite)** — five full-width routes, companion 50%→0, half-open audit windows.
- **W54 (F8 atmosphere & GPU)** — **"D-1 aurora-derive RUNS or V′ does not close"**, D-2 blob
  truthful lifecycle, arm-after-critical-paint 0px, WebGPU/WebGL2 parity.
- **W55's SUBSTANCE** — **CH-4: p75 LCP ≤2.5s — "the ~5s boot dies or V′ does not close"** +
  CH-7 the real-GPU oracle. The program's only W55 contact is a routing note about the
  .github edit. The ratified close-or-die perf gate vanishes (see A2).
- **§F residue beyond the empty-mount:** the `audit/rehearsal/` track-or-archive disposition;
  the §D expectation that the EasingSpecimenStrip Chip site SELF-HEALS at glass's BJ land
  ("verify visually") — F-03 redesigns that very strip without even noting the inbound fix.

Any one of these is a registry row; together they refute the program's zero-drop
certification. F-18's close gate ("the §5 landing map re-checked against L1/L2/addenda")
re-checks against the wrong list — the fold source is not on it, so the close would certify
the hole.

### A2 · Zero performance program inside an animation-suffusion program

The program ADDS motion everywhere — living dots at every consumer site, idle breath,
hover/beget, detail passes on every screen — and contains **no perf wave, no perf gate, no
budget row**. Against: CH-4 (ratified, close-or-die, p75 LCP ≤2.5s; the ~5s boot is the Q14
lineage — LCP 5141/TBT 5988, the eager-WebGL-blob boot blocker); the backtrace's own bonus
principle ("slow-load/stutter is a first-class perf lens," A17/F01/F46); principles 9/12
(zero idle rAF, Safari-honest) which the program cites as *vocabulary* but never as a
*check* — F-05's "idle-cost note (rAF dead at settle) in the wave record" is prose, not a
gate that can fail. A program that widens the animated surface while dropping the standing
LCP death-gate is green-over-broken by construction.

### A3 · C11/L1 §8's component-DAG mandate is fudged into an eyeball

C11: "Full DAGs of library AND component structures, ruthlessly analyzed — every node, edge,
cycle — in dynamic bespoke cluster batches via the thrice method." L1 §8: "depcruise-derived,
then adjudicated — never hand-drawn." The COMPONENT half of that mandate belongs to the
frontend program, and it appears nowhere: no graph wave, no depcruise/madge derivation of
either demo's component/composable graph. The landing map launders it as "colocation checks
inside F-01/F-11" — a checkbox, not a DAG adjudication. (The arm already has `madge-src.json`
tooling runs at the apotheosis root — the method is available and unused.)

### A4 · C6 is narrowed to its second clause

C6: "value.js **routing properly defined**, alongside state management; … robust URL state
management and share-URL facilities." F-10 does URL state + share only. The routing
architecture itself — hash mode, `Stub = { render: () => null }` on every route, **no
`<router-view>` anywhere**, panes driven by `useViewManager` out-of-band
(`router/index.ts:16-19` says so in its own comment) — is never adjudicated
keep-or-rework. "Routing properly defined" is precisely an invitation to rule on the
Stub/no-view model; the program treats it as settled by silence.

### A5 · F-13 drops the amiga scene by name

C4: multi-touch for the cube "**as should every other animation**." F-13 upgrades "square,
sequence (×2), spring, and easing." The amiga scene has a live single-pointer drag surface:
`demo/scenes/amiga/useSphereSpin.ts` — one `activePointer`, `setPointerCapture(e.pointerId)`
(:103-104), single-pointer guards (:116,:139), OrbitControls as the background gesture. It
is an "other animation" with an interaction surface and it is silently absent from the
upgrade list — no wave, no bank, no rationale.

### A6 · The audit matrix misses the surface class where ALL the owner's evidence lives

F-01's gate is "15/15 routes × 2 viewports… probe budget ≤2 runs (one sweep per viewport)."
Three defects, one for each word of the gate:
1. **There are 14 named routes, not 15** (`router/index.ts:21-38`: 9 public + 5 admin + a
   catch-all *redirect*). The "15/15" gate is unsatisfiable as written or quietly fudged;
   the Residuals' "fully enumerated" boast is off-by-one on its own foundation.
2. **Routes are not screens.** The AboutPane corpus (`scenes/about/` — `Markdown.vue` at 408
   LOC is one of the demo's largest SFCs, plus Katex, ColorNutritionLabel) mounts via
   `usePaneRouter`, on NO route; dialogs, dock layers, the kebab menu, the easing editor are
   all interaction states. Every owner-mark photograph in evidence set A is an INTERACTION
   state (kebab open, easing editor open) — a one-navigation-sweep-per-viewport budget
   structurally cannot photograph the defect class the audit exists to find. The gate can go
   GREEN while blind.
3. **The 5 admin routes require an authed admin session against the live api.** Nothing in
   the program provisions auth for capture; predictable outcome: silent scope-trim of a
   third of the matrix, gate still declared green.

Also: the two-point viewport matrix (390/1440) never exercises the `sm`/`lg` boundary band —
the program's own F-02 evidence says the dock crowds below `sm` and flips behavior at 1024px
(`Dock.vue:71`). Breakpoint-boundary defects are invisible at 390 and 1440 by construction.

---

## B — EVIDENCE-GROUNDING: what the truth does not support

### B1 · OD-4 + F-03's second RED assertion rest on a misdiagnosis

The program asserts "low-res" = the 3-decimal drag quantization, mints a RED assertion
("drag emits 3-decimal-stepped control points") and an OWNER DOCKET ROW (OD-4: "the
'low-res' mark condemns 3-decimal stepping"). Verified on disk: `useEasingPicker.ts`
`setHandle` does `+cx.toFixed(3)` — **0.001 steps in unit space ≈ 0.3px at the 304px seat.
No human perceives 0.3px stepping.** The probe evidence itself locates the low-res percept
elsewhere: "large, heavily-rounded pill chips… each holding a **low-resolution curve
thumbnail**" (sweep-probe, set A annotation) — the SPECIMEN TILES (22px glyphs), not the
drag. The RED probe would pass RED (the rounding is real code) and its cure would change
nothing the owner can see — a textbook vacuous-RED: true-on-disk, causally irrelevant. The
owner is being asked to rule (OD-4) on a phantom mechanism. Required: a measurement/A-B
re-diagnosis BEFORE any precision-law docket.

### B2 · Counts repeated from the sweeps without verification, in a canon whose law 3 is "re-derive anchors"

- "~20 consumer sites across **7 files**" (F-06) — truth at HEAD: **23 `<WatercolorDot`
  template sites across 11 .vue files** (a 12th file matches on a comment only). The sweep's
  own §5 list already named 11 files while saying "7"; the program copied the fudge.
- "all **15** routes" (F-01) — 14 (A6.1).
Both are the program's foundational enumerations; both are wrong; neither was re-derived.

### B3 · F-17 contradicts the ratified W53 recipe and inverts the canon dependency

CARRY-LEDGER W53 rider (B1 arm-A, :27): "**rebuild the perceived-space plate ON v4** from tag
`v-perceived-space-plate-ref-w40` (…the old code imports dead v4-cut APIs — rebuild, not
restore)." The program instead declares F-17 "BLOCKED on the sibling color program landing
R-DELTAE + R-GAMUT." No canon source states that dependency; P2.2 row 14 runs the arrow the
OTHER way (R-BOUNDARY is gated on W53's actual needs). Consequence of the invented block:
the needs-memo — the one artifact the color program waits on to decide R-BOUNDARY — is
deferred behind two library waves it was supposed to precede. The memo is an audit-time
deliverable (F-01 can emit it); the plate rebuild rides v4 per the ratified recipe.

### B4 · Probe oracles that cannot honestly run — from the program that cites principle 12

- **F-12 GREEN** requires "insets live on an iOS-profile emulation capture." Ordinary
  playwright/DevTools emulation resolves `env(safe-area-inset-*)` to **0** regardless of
  `viewport-fit` — the honest oracles are a real device or CDP
  `Emulation.setSafeAreaInsetsOverride`, neither named. As written the gate either cannot go
  green or goes green via a masked no-op capture.
- **F-13's** born-RED ("two-finger gesture on the square scene is inert") and its GREEN
  capture series require multi-touch **synthesis**; no mechanism is named anywhere.
- The program builds ~10 capture-gated waves on top of a probe sweep that shot **zero
  frames** because the sole browser profile was locked by a concurrent arm — and mints no
  probe-infrastructure precondition, no isolated-profile requirement, no fallback law. The
  same lock will bite every wave.

### B5 · A motion program gated exclusively on stills

PL-3 defines every visual claim as a **(before, after)** still pair. The program's subject
matter is MOTION: engage envelopes with numeric laws (attack 40–60ms, t90 ≤150ms), two
registers with damping ratios, hard-cuts with 120–170ms pins, beget sequences, idle breath.
**Not one gate can measure any of those numbers.** The MOTION-REGISTER tables (F-07/F-16)
are deliverables, not checks; no trace, no video, no timeline capture exists anywhere in the
gate set. Every motion law in the program is decorative prose — the close-class lie's visual
twin: still-green over motion-unverified.

---

## C — GATES THAT CANNOT FAIL (or cannot discriminate)

- **F-11's C5-kf certification**: `grep reka-ui = 0` is already true (sweep-proven). Born-
  GREEN by definition; it cannot be RED, so it gates nothing.
- **F-09's "no commits touch glass-ui"**: a constraint restated as a gate; cannot fail.
- **F-02's GREEN**: "reachable in ≤1 **obvious** gesture with visible affordance" — "obvious"
  is judge-dependent prose; the gate has no falsifier. (Fix: tap-count + a named capture of
  the visible affordance + both OD-5 candidates delivered to the design gate.)
- **F-05's GREEN is unstated entirely** — the wave defines RED (grep absence of a feature =
  trivially red for any net-new work) and π/DELTA "after = interaction capture series," with
  no acceptance criterion connecting the captures to the envelope laws it cites.
- **F-06**: wires 23 sites, photographs 3. Twenty sites ship capture-unverified under a
  program whose charter is "a π/DELTA capture pair for every visual claim."
- **F-08's "one boot capture proving no visual regression"** after a 48-site import rewrite
  proves route `/` renders once. (Low real risk — the barrels are re-exports — and F-08's
  three grep gates are the best in the program; keep them, drop the pretense of the boot
  capture as a visual-regression proof.)

## D — THE SCHEDULING CONTRADICTION: the centerpiece path runs through an agent the program vows never to interrupt

Under the program's own defaults: OD-2 default = HOLD the F-09 relay until F-01+F-11
complete; F-04 deps on "F-03 **(adopted variant)**"; F-06 deps on F-05; F-07 is sequenced
after F-02/04/06. Glass adoption can only happen after the relay lands AND the working glass
agent (mid-BI/P, protected by PL-4/L1 §4) acts on it — an external actor with no SLA.
Therefore under the stated defaults **F-04, F-06, and F-07 cannot execute inside this
program at all**, and PL-8 (no shims, no dual paths) forbids the only interim path. The
program half-knows this (OD-2's early-release option) but leaves the broken sequencing as
the default on its two OWNER-MARKED centerpieces (C2, C10). Additionally F-04's
demo-achievable content — replacing the flat `min(100%, 19rem)` seat budget
(`EasingAuthoringStage.vue:104-107`), a pure demo edit — is needlessly imprisoned behind the
glass adoption it does not require.

## E — WHAT STANDS (skeptic's duty to say so)

F-08's grep-zero gates; F-12's viewport-fit finding and cure (evidence is genuinely strong);
F-15's readable-URL wave and OD-3's honest surfacing of link death; OD-1's correct refusal
to assume a cross-repo grant; OD-5's two-candidate delivery; PL-4's relay discipline; the
landing-map FORM (row-cited) — the check just ran against an incomplete canon list. The
backtrace consumption (D1–D6, F1–F4) is faithful.

---

## THE COUNTER-PROGRAM (title + intent grain)

Law deltas first: add **PL-9 (fold source)** — the zero-drop check runs against
L1+L2+ADDENDA **+ CARRY-LEDGER §B–§F**; add **PL-10 (motion oracle)** — any claim citing an
envelope/register/hard-cut number carries a trace or video capture, not a still; amend PL-2
— viewport matrix is {390, 768, 1024, 1440} with interaction states enumerated per surface;
flip OD-2's default to EARLY-RELEASE the two owner-marked centerpiece rows; kill OD-4 until
K-04's measurement re-diagnosis; add docket rows: admin-band capture provisioning; amiga
gesture semantics; CH-4/W54 ownership (this program vs a sibling — decided, not silent).

- **K-00 · PROBE RIG & ORACLE HONESTY** — isolated browser profile (the lock that zeroed the
  probe sweep); trace/video motion oracle; CDP safe-area override; multi-touch synthesis
  harness; authed admin session recipe. Precondition of every capture gate.
- **K-01 · VALUE FULL-SURFACE AUDIT, W46-AMENDED** — adopt/amend the ratified W46 F0
  constitution (seat/proportion/type-Card law, the 18 pairs) as the rubric — never a
  parallel authority; matrix = 14 routes + non-route surfaces (AboutPane corpus, dialogs,
  dock layers, kebab, easing editor) × 4 widths × named interaction states; admin band
  provisioned or owner-dispositioned; mints born-RED rows; emits the W53 needs-memo AND the
  shareable-state census.
- **K-02 · KF FULL-SURFACE AUDIT** — F-11 + interaction states + boundary widths; first
  desktop captures; 15-row marks fold; the reka-grep row recorded as a FACT, not a gate.
- **K-03 · COMPONENT DAG, BOTH DEMOS** — depcruise/madge-derived component+composable
  graphs, thrice-adjudicated in cluster batches (C11/L1 §8 discharge).
- **K-04 · C2 RE-DIAGNOSIS + DEMO SEAT FIX** — measure the percept (stroke px, tile glyph
  px, quantization A/B) BEFORE any docket; land the demo-owned seat size budget now (no
  glass dependency).
- **K-05 · EASINGPICKER GLASS VARIANT — SPEC+PROTOTYPE, EARLY RELAY** — carries the §D note
  that the Chip §7 fix may self-heal the strip (verify before specing around it).
- **K-06 · WATERCOLORDOT LIVING API — SPEC+PROTOTYPE, EARLY RELAY** — with a machine
  idle-cost gate (rAF-dead-at-settle asserted from trace) and a stated GREEN.
- **K-07 · C3 MOBILE TOOLBAR** — F-02 with a falsifiable GREEN (tap count + affordance
  capture + both OD-5 candidates).
- **K-08 · VALUE SUFFUSION** — the verified 23-site/11-file ledger as the wiring census;
  per-family design (not indiscriminate hover-beget on wax seals and empty states); capture
  per FAMILY plus full-site checklist; rides the K-17 perf budget.
- **K-09 · VALUE DETAIL PASS** — F-07 + trace-verified envelopes on centerpieces; the
  motion-register table as a GATE (each row: register + trace id).
- **K-10 · SHADCN TERMINAL (VALUE)** — F-08 kept whole.
- **K-11 · C6 WHOLE: ROUTING ADJUDICATION + URL STATE/SHARE** — rule the Stub/no-router-view
  model keep-or-rework first, then the per-pane codec program (F-10's content).
- **K-12 · KF STAGE CURE** — F-12 with K-00's honest safe-area oracle.
- **K-13 · KF MULTI-TOUCH, EVERY ANIMATION** — F-13 + amiga dispositioned by name
  (sphere-spin two-finger semantics or an owner row); synthesis-harness gates.
- **K-14 · KF SINGLE-SCREEN MATURATION** — F-14 unchanged in substance.
- **K-15 · KF READABLE URL STATE** — F-15 + OD-3 unchanged.
- **K-16 · KF DETAIL PASS** — F-16 + the trace oracle.
- **K-17 · VALUE PERF: CH-4 + W54 FOLD** — the p75 LCP ≤2.5s gate (Q14 lineage) owned or
  explicitly re-homed by owner decision; W54's aurora-derive D-1 / blob lifecycle /
  arm-after-critical-paint folded or terminally dispositioned; post-suffusion idle-cost
  census.
- **K-18 · CARRY-LEDGER RECONCILIATION** — W47/W48/W49/W50/W52 ratified rows folded into
  K-01's ledger or terminally dispositioned BY NAME (banked with re-trigger / retired with
  rationale); §F residue rowed (audit/rehearsal, M1/M2→W47 folds, Chip self-heal verify).
- **K-19 · W53 PLATE REBUILD** — per the ratified recipe (ON v4, from tag
  `v-perceived-space-plate-ref-w40`); needs-memo already emitted at K-01; R-BOUNDARY pull
  decided by the memo, never the reverse.
- **K-20 · GLASS GAP REGISTER + BATCH RELAY** — F-09 with the two early releases DECIDED
  (flipped default), remainder batched after K-01+K-02.
- **K-21 · CLOSE** — DELTA ledger; zero-drop check against the PL-9 canon list (fold source
  included); probe-budget + oracle-honesty accounting.

— Skeptic 1, thrice loop, PD-frontend. Model served: claude-fable-5.
