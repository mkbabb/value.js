# PD-FRONTEND — AMENDED FINAL (thrice-adjudicated) — armB independent Fable formation — 2026-07-19

Tree pins (G0′): value.js `tranche-u@db77dbd8` (4.0.0) · keyframes-v-exec `master@0dac636b`
(6.0.0, kf pins value EXACT 4.0.0) · glass-ui `master` 7.0.0 (advances fast — RE-PIN AT READ).
Canon: L1 (ingestion prompt) + L2 (packets P0–P6) + CONVERSATION-ADDENDA (C1–C23; addenda WIN
where they tighten) + **CARRY-LEDGER.md §B–§F + V-PRIME brackets (the fold source — PL-9)**.
Evidence: the four armB sweeps + probe evidence set A + the adjudicator's own repo reads at the
pins (every load-bearing count re-derived; see ADJUDICATION LOG §7).
Formation phase only — NO source edits land from this document (L1 §0; phase labels per §7 law 4).
Independence firewall honored: zero Codex-corpus reads.

Thrice provenance: program `program-PD-frontend.md` (18 waves) → skeptic1 (REJECT AS FORMED) +
skeptic2 (inventory/dependency rejection) → THIS adjudication. Verdict on the target program:
its evidence anchoring was largely TRUE (C2/C3/C10 diagnoses, F-08 facts, F-12's viewport-fit
find all verified on disk) but its INVENTORY was cut against the addenda alone — the ratified,
unexecuted CARRY-LEDGER §B tail was consumed at one row of ten, including two "or V′ does not
close" laws — and its dependency shape inverted its own critical path through glass. Both
skeptics' core attacks are PROVEN on my own evidence; the counts they corrected are corrected
below; the two places a skeptic overreached are DISPROVEN by name (log §7). 24 waves.

---

## 1. CHARTER

This program perfects the two demos as PRODUCTS: the value demo by refinement (the owner: "the
extant UI is quite good" — details, errors, curves, the little things), the kf demo by overhaul
(C4: a proper single-screen application a la value, multi-touch everywhere, mobile-first with
desktop affordances). It terminates shadcn residue in both apps (C5), adjudicates and then
instruments routing + URL state/share in both (C6, FULL width — architecture first, codec
second), suffuses the glass-ui BREATH OF LIFE canon (C10) into every screen (C22), **and folds
the ratified, unexecuted V′ frontend tail (CARRY-LEDGER §B W46–W55 + §C/§D/§F riders) whole —
fold, bank, or retire BY NAME, never by silence** — including the two ratified close-laws:
CH-4 (p75 LCP ≤2.5s; the ~5s boot dies) and W54 D-1 (aurora-derive RUNS). Glass-ui-level needs
are never edited directly: they become named gap-prototype waves in glass idiom, relayed in TWO
priced batches. Motion claims carry motion oracles, not stills. Design quality over checkbox
coverage; probe parsimony with honest budgets; KISS in the wave cut — 24 waves, and the
zero-drop check runs against the FULL canon list including the fold source.

## 2. PROGRAM LAW (binds every wave)

- **PL-1 · Thrice (C20/L1 §8):** every wave's design artifact rides 2 Fable skeptics + 1 Fable
  adjudicator; convergence = two consecutive clean passes; ≤3 iterations then owner escalation.
- **PL-2 · Probe parsimony, honest budgets (C21 + standing edict), AMENDED:** audit-wave
  viewport matrix = {390×844, 768, 1024, 1440×900} — 390/1440 full sweeps, 768/1024 as a
  boundary-band spot set (the dock flips behavior at `lg` and crowds below `sm`; two-point
  matrices are blind to the band by construction). Interaction states are ENUMERATED per
  surface (the owner's own evidence frames are interaction states: kebab open, easing editor
  open). Evidence set A frames are the "before" images wherever they exist. Budgets are stated
  per wave and honest (value audit ≤4 runs: 2 viewports × {public, admin-authed} + one
  boundary-band spot run); batched runs; no dev-server marathons; fastidious static analysis
  first.
- **PL-3 · π/DELTA (stills):** every STATIC visual claim = (before capture id, after capture
  id, one-line verdict); unpaired claims block wave close; the DELTA ledger (F-24) is the
  terminal registry.
- **PL-4 · Glass relay (L1 §4 + the BH-relay fond):** zero direct glass-ui edits from this
  program; every glass-level need lands as a precept-compliant wave addendum in glass idiom on
  the F-20 gap register, released in TWO batches (batch #1 = the owner-marked centerpiece rows,
  early; batch #2 = audit-sourced rows, after both audits). The batching law forbids piecemeal
  interruption; it does not forbid two well-formed batches.
- **PL-5 · Terminal dispositions (L1 §7):** every ledger row ends folded / banked (named
  re-trigger) / retired (rationale); a mark that rode ≥2 closes un-decided is DISEASE and
  deciding it is a wave-level act. Counting a partial as done is the close-class lie. An
  externally-gated wave that cannot close closes BANKED with its re-trigger named — never
  silently open, never partial-as-done.
- **PL-6 · Model honesty (P0.1/C17):** every seat reports the model actually serving it; a
  declaration is not execution. Formed-tranche execution is all-Fable; Opus only for flagged
  mechanical sweeps under per-tranche owner ratification.
- **PL-7 · Refinement law (C10):** value demo work is refinement, never rebuild; kf demo work
  may restructure but core features are never lost (machine-checked where a surface exists).
- **PL-8 · No shims, no dual paths (L1 §7):** stopgaps (the ActionBarLayer local
  `useLayerTransition`) retire on adoption; migrations move the consumer, not the compat layer.
- **PL-9 · Fold source (NEW):** the zero-drop certification runs against L1 + L2 + ADDENDA
  **+ CARRY-LEDGER §B–§F + V-PRIME §3 brackets (B1–B5)**. Every §B wave row, §C tail, §D
  expectation, and §F carry appears in the F-24 disposition table BY NAME as folded / banked /
  retired / ROUTED (with the receiving program named). The two close-laws (CH-4, W54 D-1)
  cannot be dissolved by audit or routed into silence.
- **PL-10 · Motion oracle (NEW):** any claim citing an envelope/register/hard-cut/damping
  NUMBER (attack 40–60ms, t90 ≤150ms, 120–170ms pins, rAF-dead-at-settle) carries a trace,
  video, or timeline capture — not a still. The motion-register tables are GATES (each row:
  surface → register → trace id), not prose deliverables.
- **PL-11 · Gate labels (NEW):** born-RED is minted ONLY where a defect is live on disk today.
  Net-new capability gates are labeled **BORN-ABSENT** (feature gates with a stated GREEN).
  Facts that are already true (kf reka-grep = 0) are RECORDED as facts, never dressed as gates.
  This keeps the real born-REDs (toolbar burial, stage collision, boot LCP) binding.

---

## 3. THE WAVE SET

### F-PD-frontend-01 | program infra | PROBE RIG & ORACLE HONESTY (precondition of every capture gate)
- **Intent:** the program's own probe sweep shot ZERO new frames because the sole browser
  profile was locked by a concurrent arm (sweep-probe §status, 300s wait timed out). Every
  capture gate downstream inherits that failure mode unless the rig is a wave. Stand up: an
  isolated browser profile per program; the motion oracle recipe (trace/video/timeline capture
  per PL-10); an HONEST safe-area oracle (ordinary emulation resolves `env(safe-area-inset-*)`
  to 0 regardless of `viewport-fit` — use CDP `Emulation.setSafeAreaInsetsOverride` or a real
  device, named per capture); a multi-touch synthesis harness (CDP touch dispatch /
  playwright touchscreen) for F-17's gates; the authed-admin capture recipe (seeded session
  against the live api) for the admin band.
- **Deliverables:** the rig recipe doc + one proof capture per oracle class (profile isolation,
  trace, safe-area override, two-finger synthesis, authed admin route).
- **Gates:** all five proof captures exist and are honest (the safe-area proof shows a nonzero
  inset; the two-finger proof shows two active pointers). BORN-ABSENT (infrastructure).
- **Deps:** none (opens the program with F-02). **Routing:** Fable-directed; the harness
  mechanics are Opus-ELIGIBLE mechanical work under ratification.

### F-PD-frontend-02 | value.js | THE VALUE EVERY-SURFACE AUDIT ON THE W46 CONSTITUTION (C22 + W46 fold)
- **Intent:** the Aristotelian proportionality + affordance audit — **on the ratified W46
  rubric, never a parallel authority** (CARRY-LEDGER §B W46: seat-law/proportion-law/type-Card
  tuple, 18 ratified composition pairs; refutation amends, silence re-litigates — P4.4). The
  matrix: **14 routes** (9 public + 5 admin; the catch-all is a redirect, not a screen —
  `router/index.ts:21-38`) **+ the non-route surfaces** (the AboutPane corpus at
  `demo/scenes/about/`, dialogs, dock layers, the kebab menu, the easing editor — every
  owner-mark photograph in set A is an interaction state) × the PL-2 viewport set with named
  interaction states per surface. Admin band captured authed via F-01. Consumes the pre-glass-7
  carried-forward UI corpus NOW (L1 §1/§4): CARRY-LEDGER §F incl. the gh-pages prod-preview
  empty-mount as the FIRST probe, the `audit/rehearsal/` + D49 `research/` track-or-archive
  inputs, and the §D Chip/EasingSpecimenStrip self-heal expectation (verify visually). Emits
  EARLY: the W53 needs-memo (F-22) and the shareable-state census (F-14).
- **Deliverables:** SCREEN-LEDGER (surface × viewport × state, capture ids, file:line anchors
  per mark); remove/afford/refine marks; DISEASE tags per PL-5; glass-gap rows → F-20; the W53
  needs-memo; the shareable-state census; a written disposition for every §B/§F row the audit
  touches; the refinement backlog feeding F-03/06/10/11.
- **Gates:** 14/14 routes + the enumerated non-route surface list covered at 390+1440 with the
  boundary-band spot set; admin band captured authed (or an owner disposition recorded — never
  silent scope-trim); every mark anchored + captured; probe budget ≤4 runs. Audit wave — it
  MINTS the program's born-RED rows.
- **π/DELTA:** the matrix is the π base; set A frames 01–06 + `value-mobile-390-*` reused as
  before-frames.
- **Deps:** F-01. **Routing:** Fable (design judgment throughout).

### F-PD-frontend-03 | value.js | C2 — RE-DIAGNOSIS + THE DEMO-OWNED EASING SEAT CURE (glass-independent)
- **Intent:** the owner's "far too rounded, too large, low-res" is cured in TWO repos; this
  wave lands everything DEMO-OWNED now, decoupled from the glass round-trip. First the
  measurement re-diagnosis: stroke px at the seat (unit-space `stroke-width="0.035"` ≈ 10.6px
  at 304px — real), tile glyph px, and the quantization A/B — **the 3-decimal drag stepping is
  0.001 unit ≈ 0.30px at the 304px seat: imperceptible; the low-res percept lives in the
  specimen tiles/thumbnails** (adjudicated; the old OD-4 rested on this misdiagnosis and is
  killed as framed). Then the demo cures: a real mobile size budget replacing the flat
  `min(100%, 19rem)` clamp (`EasingAuthoringStage.vue:105` — demo-owned, `!important`s the
  seat's own comment retires at the P7 adopt); the specimen-strip redesign
  (`EasingSpecimenStrip.vue` is DEMO code composing glass `Chip shape="cell"` — demo altitude
  unless a glass primitive is PROVEN necessary), executed AFTER verifying whether the inbound
  glass §7 Chip fix self-heals the strip (CARRY-LEDGER §F).
- **Deliverables:** the measurement record (stroke px / glyph px / A/B verdict — the input to
  any precision docket, per OD-4′); the mobile size budget; the strip redesign or a
  self-healed verdict; glass-variant needs distilled into the F-04 spec.
- **Born-RED (live today):** `value-mobile-390-easing-editor.png` is the defect photograph;
  no mobile-specific seat budget exists on disk. GREEN = the seat fits the 390 viewport
  without the oversized/blobby percept, measured + photographed.
- **π/DELTA:** before = `value-mobile-390-easing-editor/strip`; after = same-viewport pairs;
  1440 non-regression vs `03-value-desktop-gradient-1440`.
- **Deps:** F-02 (marks), F-01. **Routing:** Fable (owner-marked centerpiece).

### F-PD-frontend-04 | glass-ui (spec+prototype) | C2 — EASINGPICKER COMPACT/HIGH-RES VARIANT (glass-gap prototype; relay batch #1)
- **Intent:** the truly glass-owned arms of C2: screen-space hairline strokes
  (non-scaling-stroke or px-normalized — `EasingPicker.vue:367` `stroke-width="0.035"` is
  unit-relative today), a compact/density-aware variant for narrow seats, crisp specimen glyph
  support, magnetic overpull (principle 5) for handle drag past bounds. Precision law rides
  OD-4′ (measurement-gated — no phantom docket). Carries the §D note: the inbound Chip §7 fix
  may self-heal the strip visuals — the spec must not design around a defect already owed a
  cure.
- **Deliverables:** precept-compliant glass wave addendum (density prop, stroke law, specimen
  tile support) + an isolated prototype (NOT landed in glass — relayed via F-20 batch #1).
- **Gate:** BORN-ABSENT (no density variant exists); prototype captures at 390 + 1440 with
  measured stroke px recorded; spec passes the thrice loop.
- **Deps:** F-03's measurement record. **Routing:** Fable (deep design).

### F-PD-frontend-05 | glass-ui (spec+prototype) | C10 — WATERCOLORDOT LIVING API (glass-gap prototype; relay batch #1)
- **Intent:** hover animate/change + procedural beget are NET-NEW — no hover/beget API exists
  in glass `watercolor-dot` (grep-verified: WatercolorDot.vue/index.ts/prng.ts/
  useWatercolorBlob.ts, zero hover/beget/spawn). Design the living dot: engage-envelope hover
  (attack 40–60ms, t90 ≤150ms — principle 8), seeded procedural beget (offspring from the
  parent's PRNG line), lull-filament idle (never zero; CSS-only compositor breath; ZERO idle
  rAF — principle 9), Safari-honest (no per-frame SVG turbulence — principle 12), blur law
  (fixed radii, layer-opacity animation — principle 11).
- **Deliverables:** glass wave addendum + isolated prototype: hover/beget API (props/events),
  idle breath, dose-column ruling per OD-6, perf-law compliance notes.
- **Gate:** **BORN-ABSENT with a STATED GREEN (PL-10/PL-11):** engage envelope measured from
  trace within the stated law (attack 40–60ms, t90 ≤150ms); rAF-dead-at-settle asserted from
  trace (a machine check, not prose); beget sequence captured as a motion series; both themes.
- **π/DELTA:** interaction capture series (hover enter/exit, beget) + the idle trace.
- **Deps:** F-01 (motion oracle). **Routing:** Fable (breath-of-life centerpiece).

### F-PD-frontend-06 | value.js | C3 × W47 — THE MOBILE TOOLBAR, RECONCILED WITH THE SHELL FOLD
- **Intent:** the 5-action toolbar (reset/copy/random/palettes/extract) is buried below `lg`
  behind the icon-only layer-swap trigger — restore it as a first-class mobile affordance,
  **decided JOINTLY with the ratified W47 shell gates it collides with**: `PaneSegmentedControl`
  1→0 (it lives ALIVE at `demo/shell/PaneSegmentedControl.vue` and is mounted in the mobile
  dock at `Dock.vue:198` — a toolbar redesign formed in ignorance of that gate could
  re-entrench a condemned component), dock-expand scene rect 0px, eleven-routes-direct,
  main/H1/active-subtree = 1, the D53.iv couplings, and the §F fold targets (glass M1
  DarkModeToggle passive variant + M2 layer-transition successor → W47; the ActionBarLayer
  shim retirement is specced here, EXECUTED at F-21 on glass adoption). Dock-to-card growth +
  edge squish-stretch (principle 6) is the named motion vocabulary. Anchors re-derived at
  execution (law 3).
- **Deliverables:** thrice-converged design — BOTH OD-5 candidate forms delivered to the owner
  design gate; the W47 fold ledger (each W47 gate: folded here / dispositioned); the toolbar
  implementation post-ruling.
- **Born-RED (live today):** at 390×844 no persistent tool affordance exists (set A
  `04-value-mobile-home-390` toolbar absent; `05-…-kebab-menu` the surrogate). **GREEN
  (falsifiable):** each of the 5 actions reachable in ≤2 taps from the home state, with a
  visible (captured) affordance at rest; PaneSegmentedControl count = the ruled value;
  desktop non-regression.
- **π/DELTA:** before = set A 04/05; after = same-viewport; desktop vs
  `02-value-desktop-toolsbar-open-1440`.
- **Deps:** F-02; F-20 iff a GlassDock-level need surfaces. **Routing:** Fable.

### F-PD-frontend-07 | value.js | W48 FOLD — THE PICKER INSTRUMENT PARITY CLOSE
- **Intent:** the ratified W48 wave, folded whole: one atomic close; A137/A138 die;
  PR-01..03; pointer↔keyboard↔numeric↔AT parity on the picker instrument; the Blob 0px chassis
  vs the `.26` before-frame reconstructed from tag `v-blob-b0-26-ref-w40` (HeroBlob.vue:206 +
  seat.css:10 at the tag); protected slices resume-never-regress.
- **Deliverables:** the parity matrix (input modality × operation → capture/trace id); the
  chassis gate record.
- **Gates:** as ratified in the W48 spec file (`reformation/waves/`), anchors re-derived;
  born-RED wherever the W48 spec's gates are red on disk today (re-verified at open, not
  presumed).
- **π/DELTA:** parity interactions are motion/AT claims — PL-10 traces + AT transcript;
  chassis = still pair vs the tag frame.
- **Deps:** F-02 (audit marks on the picker fold in). **Routing:** Fable.

### F-PD-frontend-08 | value.js | W49/W50 FOLD — PALETTE READ/WRITE + THE D57 EXPORT SEAT
- **Intent:** the ratified palette waves folded whole: card count = entity count, quiet-opaque
  tuple, native `aria-pressed` seat, storage-recovery against W45's live api (W49);
  register→recover→rotate→lost-response-logout, draft→publish→unpublish→trash→restore, CH-8 UI
  half closes (W50); **D57**: the Prepare→Download seat (IndexedDB `ExportOperation`,
  FORMAT→FILENAME swap, fresh-activation download, 60s-`pagehide` lease) + RETIRE legacy
  `demo/palettes/export.ts`/`usePaletteExport.ts`, wiring panes to `export/serializers`.
  **D55.iii** (the `__Host-value-session` cookie transition + idempotency-key retirement)
  spans api+client: ROUTED BY NAME to the sibling API program, with the client half's
  adoption row banked here (re-trigger: the api program's cookie cut). **V-PRIME B4 rider:**
  the orphaned-probe deletions are superseded by W50 journeys — the deletion is legal only
  once the journeys stand (sequenced inside this wave's close).
- **Deliverables:** the read/write journey records; the export seat; the legacy retirement;
  the D55.iii routing letter + banked client row.
- **Gates:** as ratified in the W49/W50 spec files, anchors re-derived, born-state re-verified
  at open; legacy export files deleted (grep 0).
- **π/DELTA:** journey flows are motion claims (PL-10); seat states are still pairs.
- **Deps:** F-02; the api dependency is the LIVE W45 api (already landed, §A). **Routing:**
  Fable.

### F-PD-frontend-09 | value.js | W52 FOLD — THE ADMIN SUITE
- **Intent:** the ratified W52 wave folded whole: five full-width admin routes; companion
  50%→0; half-open audit windows; exact event-row cardinality — against W45 typed Problems.
  Captures ride F-01's authed-admin recipe.
- **Deliverables/Gates:** as ratified in the W52 spec file, anchors re-derived, born-state
  re-verified; the F-02 admin-band marks fold in.
- **π/DELTA:** per-route pairs at both form factors, authed.
- **Deps:** F-01 (auth recipe), F-02. **Routing:** Fable.

### F-PD-frontend-10 | value.js | C10 — THE VALUE SUFFUSION (dots wired + slider-value watercolor)
- **Intent:** wire the living-dot behaviors at the RE-DERIVED census — **23 `<WatercolorDot`
  template sites across 11 .vue files** (Dock, ConsoleRail, SpectrumCanvas, MixResultDisplay,
  MixSourceSelector, GenerateControls, ImageEyedropper, EmptyState, ColorSpaceSelector,
  CurrentPaletteEditor, SwatchHoverMenu) — with PER-FAMILY design, not indiscriminate
  hover-beget on wax seals and empty states; give the channel sliders' value readouts the
  watercolor-dot background treatment (today plain `<span class="channel-meter fira-code">`,
  `ComponentSliders.vue`).
- **Deliverables:** the per-family behavior map (which families hover, which beget, which only
  breathe — a thrice design artifact); wired sites; the slider-value treatment; stopgap
  removals the new API subsumes.
- **Born-RED (live today):** no dot backgrounds exist in the slider area; before-frame =
  `value-mobile-390-sliders-closeup` (plain readouts 92.0/88.8/20.0/82.7%).
- **π/DELTA:** capture per FAMILY (motion series per PL-10 where the family animates) **plus
  the full 23-site wiring checklist** (each site: wired/excluded-by-design, one line) — no
  site ships unaccounted; idle-cost trace post-wiring feeds F-12's census.
- **Deps:** F-05 via F-21 (glass adoption) — the wave DESIGN work (family map, slider
  treatment) starts at F-02's close; the wiring executes at F-21. **Routing:** Fable.

### F-PD-frontend-11 | value.js | THE VALUE DETAIL PASS — curves, margins, paddings, EVERY screen
- **Intent:** discharge the F-02 refine backlog with the breath-of-life vocabulary:
  choreographed-never-batched (principle 3), two-registers-two-intents (principle 7),
  momentum-marked curves (principle 4), the commit hard-cut (principle 10), the dose column
  (principle 13). W46-law consumption (override 0). Refinement, never rebuild (PL-7). Consumes
  the PC-structure demo-DAG cluster verdicts where they mark demo components for
  recombination (routing per §5 — no duplicate DAG derivation here).
- **Deliverables:** per-screen refinement commits mapped 1:1 to ledger rows; the value
  MOTION-REGISTER table **as a gate** (each row: surface → register → trace id, PL-10).
- **Gates:** every F-02 refine row terminal per PL-5; the F-02-minted born-RED rows close
  GREEN with capture pairs; envelope/register numbers trace-verified on the centerpiece
  surfaces; zero silent partials.
- **π/DELTA:** one pair per ledger row batched into ≤3 probe runs; traces per motion row.
- **Deps:** F-02; sequenced after F-03/06 land and F-21 for glass-gated rows. **Routing:** Fable.

### F-PD-frontend-12 | value.js | THE BOOT KILL — CH-4 + W54 FOLD (ratified close-laws; cannot be dropped)
- **Intent:** the single worst hole in the target program, cured: **CH-4 — p75 LCP ≤2.5s on
  the named matrix; "the ~5s boot dies or V′ does not close"** (CARRY-LEDGER §B W55; Q14
  lineage: LCP 5141/TBT 5988, the eager-WebGL-blob boot blocker, producer-gated → U.W-PERF)
  **+ W54 folded whole**: aurora-derive **D-1 RUNS or V′ does not close**; D-2 blob truthful
  lifecycle; arm-after-critical-paint 0px; boot SeedToken negative set; B0→app-ready atoms
  byte-identical; `script-src 'self'`; WebGPU/WebGL2 parity. The two rows are one mechanism
  family (the eager blob IS the LCP blocker). Includes the post-suffusion idle-cost census
  (F-10's added animation surface must not regress the budget — the suffusion rides THIS
  wave's budget). A frontend program that widens the animated surface carries the perf gate;
  ownership is confirmed at OD-7 (owner may re-home it EXPLICITLY — never silently).
- **Deliverables:** the de-eagered boot; aurora-derive running; the lifecycle + parity
  records; the idle-cost census.
- **Born-RED (live today):** p75 LCP > 2.5s on the named matrix (Q14 record; re-measured at
  wave open via F-01's trace oracle); aurora-derive D-1 unmet.
- **π/DELTA:** LCP/TBT trace pairs (PL-10 — this is a trace-native wave); boot capture pair.
- **Deps:** F-01 (trace oracle); coordinates with F-10 (idle census). **Routing:** Fable.

### F-PD-frontend-13 | value.js | C5 — SHADCN TERMINAL ABROGATION (value; glass-gated arms excised to F-21)
- **Intent:** finish the already-component-abrogated state terminally: dissolve `demo/ui/`
  (19 one-line re-export barrels; **48 importing files / 90 import lines re-derived at HEAD**
  — re-derive again at execution, law 3) → direct `@mkbabb/glass-ui` imports, unifying the
  barrel/direct two-track; delete the stale root `components.json` (dead `demo/@` path + Sass
  ref — a live shadcn scaffold config on a shadcn-free repo). The 4 reka-ui `AcceptableValue`
  type leaks (GradientVisualizer:28, MixConfigBar:15, GenerateControls:33, AuroraPane:25) and
  the ActionBarLayer shim retirement are glass-gated: SPECCED at F-20 (rows a/b), EXECUTED at
  F-21 — this wave closes clean without them.
- **Deliverables:** barrel dissolution + import unification; deletions.
- **Born-RED (live today):** `demo/ui/` exists (19 files); `components.json` exists at root.
  GREEN = both greps zero + typecheck/build green + one boot SMOKE capture (a smoke check,
  not a visual-regression proof — the regression claim is not made).
- **π/DELTA:** non-visual — build gates + the smoke capture.
- **Deps:** none (barrel work is independent). **Routing:** the import rewrite is mechanical —
  Opus-ELIGIBLE under per-tranche owner ratification; Fable-directed.

### F-PD-frontend-14 | value.js | C6 FULL WIDTH — ROUTING ADJUDICATION, THEN URL STATE + SHARE
- **Intent:** C6 is "routing **properly defined**, alongside state management" — the
  architecture is adjudicated FIRST, the codec built second. Stage 1: rule the extant model
  keep-or-reshape with rationale — hash mode, `Stub = { render: () => null }` on every route,
  NO `<router-view>` anywhere, pane truth in `useViewManager`/`usePaneRouter`
  (router/index.ts:16-19 declares the model in its own comment). Stage 2: the general
  per-pane URL-state facility on the adjudicated architecture: palette slug, gradient
  stops+easing, mix inputs, generate config — human-readable params, schema-driven codec
  colocated per pane owner; hash mode kept if ruled (GH Pages); generation-guard/debounce
  extended; back/forward semantics preserved; share flow emitting full state
  (today: `useColorUrl` round-trips ONLY `{space, color}`; share = `copyLink(
  window.location.href)`, App.vue:366); boot-seed precedence (URL > localStorage > default)
  extended coherently.
- **Deliverables:** the routing adjudication record (thrice-converged); URL-state schema +
  codecs; the share flow.
- **Born-RED (live today):** reproduction — author gradient stops, copy the share link, open
  fresh: stops LOST. That script is the RED probe. The routing adjudication itself is a
  DECISION deliverable, not a RED gate (PL-11).
- **π/DELTA:** the round-trip probe RED→GREEN; one capture pair for the share affordance.
- **Deps:** F-02 (shareable-state census). **Routing:** Fable.

### F-PD-frontend-15 | keyframes.js (mode per OD-1, decided AT RATIFICATION) | C22 — THE KF EVERY-SCREEN AUDIT + MARKS FOLD
- **Intent:** the kf mirror of F-02: 7 scenes (home/cube/amiga/square/easing/spring/sequence)
  × the PL-2 viewport set + named interaction states + the chrome (docks, drawer, share,
  shortcuts modal); consumes the 15-row glass-§7 marks register (kf FOLD-FORWARD) and the kf
  sweep; **records** kf's C5 state (`grep 'from "reka-ui"'` = 0 — already true; recorded as a
  FACT per PL-11, not dressed as a gate); INCLUDES the first kf desktop captures (the probe
  sweep's named un-shot gap — desktop claims are code-grounded prediction until then).
- **Deliverables:** kf SCREEN-LEDGER with capture ids incl. the desktop base set;
  remove/afford/refine marks with anchors; DISEASE tags; the C5-kf fact row; every
  marks-register row dispositioned into F-16..19 or banked.
- **Gates:** 7 scenes × 2 primary viewports + boundary spot set covered; kf desktop captured
  (closes the named gap); every marks row dispositioned.
- **π/DELTA:** before frames = `kf-mobile-390-landing`/`kf-mobile-390-animation-list` + the
  new desktop base.
- **Deps:** F-01; OD-1 rules the execution mode for the whole kf series **as a ratification
  pre-gate** (the addenda-conformant default is (a) direct-edit with owner countersign — C4
  WINS on scope; under (b) the kf waves' close gates become dispatch-plus-verification-window
  and say so honestly). **Routing:** Fable.

### F-PD-frontend-16 | keyframes.js | C4a — THE MOBILE STAGE CURE
- **Intent:** cure the live render defect: at 390×844 the cube overflows the viewport
  bottom-right and the hero copy overlaps the cube body (set A, both frames); add
  `viewport-fit=cover` — missing repo-wide (grep = 0) while TEN `env(safe-area-inset-*)`
  consumer sites exist (layout.css ×5 + stage split; count re-derived), so every inset
  silently resolves to 0 on iOS; stage/hero reconciliation (the layout band system vs the
  cube's 3D bounding box at narrow widths); safe-area re-verification **via F-01's honest
  oracle** (CDP inset override or real device — named in the capture record).
- **Deliverables:** viewport meta fix; stage/hero reconciliation; safe-area verification.
- **Born-RED (live today):** `kf-mobile-390-landing.png` = the collision photograph;
  `grep -r viewport-fit` = 0. GREEN = no subject/copy intersection at 390×844; a nonzero-inset
  capture via the honest oracle. Registry note: the `@supports not (height:100dvh)` 100vh
  fallbacks are RETIRED-OK (correctly gated; rationale recorded, no wave).
- **π/DELTA:** before = the two set A kf frames; after = same viewport + the inset capture.
- **Deps:** F-15. **Routing:** Fable.

### F-PD-frontend-17 | keyframes.js | C4b — MULTI-TOUCH EVERY ANIMATION (cube untouched; amiga named)
- **Intent:** the cube's quaternion orbital facility is COMPLETE (pinch scale+pan+Z-twist,
  Safari gesture events, de-jumped pinch exit, inertia, axis-lock) and is the REFERENCE, not
  the refactor target: **the cube is not extracted or reshaped this tranche** (non-regression
  by isolation; C4 asks the OTHERS reach cube quality; a unification row is BANKED with
  re-trigger "the new composable proves iso-capable"). Build a NEW small shared multi-pointer
  scrub composable (colocation-correct home) and give **square, sequence (×2), spring, easing,
  AND amiga** genuine multi-touch — amiga has a live single-pointer drag surface
  (`useSphereSpin.ts`: one `activePointer`, `setPointerCapture`, pointerId guards,
  OrbitControls background gesture) and was silently absent from the target program's list.
  Per-scene gesture SEMANTICS designed (what pinch/twist MEANS per scene); "no meaningful
  gesture" is a LEGAL terminal verdict per scene, owner-visible in the semantics table.
- **Deliverables:** the shared composable; the per-scene semantics table (thrice-converged;
  amiga row present); upgraded scenes; inertia where meaningful; cube non-regression capture.
- **Born-RED (live today):** `useDragScrub.ts` is single-pointer (`setPointerCapture` +
  window `pointermove`); a two-finger gesture on the square scene is inert — asserted via
  F-01's synthesis harness (the RED probe is runnable, not rhetorical).
- **π/DELTA:** one synthesized-gesture motion series per upgraded scene (PL-10); cube
  non-regression series.
- **Deps:** F-15, F-16, F-01 (harness). **Routing:** Fable (gesture semantics are design).

### F-PD-frontend-18 | keyframes.js | C4c + C6 — APP MATURATION + HUMAN-READABLE URL STATE (merged: one share-legibility story)
- **Intent:** mature kf a la value and glass: per-scene `document.title` (today static
  "keyframes.js", index.html:14 — grep-proven absent), scene-navigation affordances, desktop
  dock/anchor polish (the `@supports (anchor-name)` tether system as the reference), chrome
  hierarchy with deep value/glass inspiration; AND replace the opaque `?state=` blob
  (`btoa(encodeURIComponent(JSON.stringify(…)))`, hashSharing.ts:6-9) with human-readable
  per-facet params (scene in path; controls/options legible), preserving the existing
  robustness (try/catch decode, `isValidState` guard, initial-nav-only restore, router-guard
  idiom); SharePopover updated; title-on-restore lands with the codec (the two halves are one
  story — the F-14/F-15 split in the target program was a touchpoint, not a wave boundary).
- **Deliverables:** per-scene titles incl. share-restore; navigation/affordance refinements
  from the F-15 ledger; desktop polish; readable codec; SharePopover; old-link posture per
  OD-3.
- **Born-RED (live today):** title static; share URL one opaque blob. GREEN = title tracks
  scene + restored state; round-trip probe (share → fresh open → restored) green; URL
  legibility sample recorded.
- **π/DELTA:** desktop before/after vs the F-15 desktop base; round-trip probe RED→GREEN.
- **Deps:** F-15, F-16; OD-3. **Routing:** Fable.

### F-PD-frontend-19 | keyframes.js | THE KF DETAIL PASS — curves, margins, paddings, EVERY screen
- **Intent:** C10's "both demos" clause: breath-of-life refinement of kf's screens —
  curves/paddings/margins, proportionality, motion-register discipline; the fixed-px caps
  pass (`--header-items-max-w:500px`, `--dock-panel-width:17rem`). Consumes PC-structure's kf
  demo-DAG cluster verdicts (routing per §5).
- **Deliverables:** ledger-mapped refinements; the kf MOTION-REGISTER table **as a gate**
  (PL-10: each row surface → register → trace id).
- **Gates:** every F-15 refine row terminal per PL-5; capture pairs per row; motion rows
  trace-verified.
- **π/DELTA:** per-row pairs batched into ≤3 probe runs; traces per motion row.
- **Deps:** F-15..18 (rides last in the kf series). **Routing:** Fable.

### F-PD-frontend-20 | glass-ui (spec/relay only) | THE GLASS GAP REGISTER + TWO-BATCH RELAY
- **Intent:** the standing vehicle for every glass-level need, per the batching law and the
  BH-relay fond — **in two priced batches** (PL-4): **batch #1 (EARLY, at F-04+F-05
  convergence):** (c) EasingPicker compact variant, (d) WatercolorDot living API, (a)
  `AcceptableValue`-equivalent exported type, (b) public layer-transition composable — the
  four rows that gate five downstream waves; one letter, interruption cost priced and
  accepted at OD-2 (decided at ratification, default = early). **Batch #2 (after F-02+F-15):**
  audit-sourced rows (e: any GlassDock aperture need from F-06; f: F-02/F-15 rows). Every row
  a named prototype or spec in glass idiom.
- **Deliverables:** the gap register; the two batch letters to the glass BH inbox.
- **Gates:** zero direct glass-ui edits from this program (a standing CONSTRAINT, recorded —
  not dressed as a gate per PL-11); every gap row has spec-or-prototype; both batch letters
  sent at their trigger points.
- **π/DELTA:** prototype captures travel with their source waves (F-04/F-05).
- **Deps:** fed by 02/03/04/05/06/13/15. **Routing:** Fable (spec authorship).

### F-PD-frontend-21 | value.js | GLASS ADOPTION RECONVERGENCE (the single home for external latency)
- **Intent:** the one wave that absorbs the glass round-trip — everything glass-gated
  converges here so every other wave closes clean (PL-5): glass devDep bump (re-pin at read);
  EasingPicker variant adopted (the seat `!important`s retired per the seat's own P7-adopt
  note); WatercolorDot living API wired per F-10's family map at the 11 files; ActionBarLayer
  shim retired (PL-8); the 4 `AcceptableValue` leaks closed via the exported type; the §D
  EasingSpecimenStrip Chip self-heal + V-A95 verified visually at the same bump. Re-runs per
  glass landing if the batch ships in parts.
- **Deliverables:** the adoption commits; the shim/leak retirements (grep `from "reka-ui"` in
  demo = 0; shim gone); the self-heal verification record.
- **Gates:** adoption greps zero; F-10 wiring checklist complete; π/DELTA pairs for every
  adopted visual. **If glass has not shipped by program close: this wave closes BANKED with
  the named re-trigger (the glass BJ/BI landing) — honest, never partial-as-done.**
- **Deps:** F-20 batch #1 + the external glass landing; F-10 (family map). **Routing:** Fable.

### F-PD-frontend-22 | value.js | W53 — THE PERCEIVED-SPACE PLATE: NEEDS-MEMO FIRST, REBUILD ON v4
- **Intent:** per the RATIFIED rider (CARRY-LEDGER §B W53, B1 arm-A): **rebuild the plate ON
  v4** from tag `v-perceived-space-plate-ref-w40` (the tag holds PerceivedSpacePlate.vue +
  envelopePlatePaint.ts + usePerceivedRamp.ts; the old code imports dead v4-cut APIs —
  rebuild, not restore). The dependency arrow runs THE PACKET'S way: the **needs-memo is
  emitted EARLY (at F-02)** and is the UNBLOCKING artifact for the sibling color program's
  R-BOUNDARY decision (P2.2 row 14) — the plate is NOT blocked on R-DELTAE/R-GAMUT; restored
  color kernels are adopted OPPORTUNISTICALLY when the color program lands (a banked adoption
  row, re-trigger named). W53's wider workbench law (five features, disjoint slices, W46-law
  consumption, one action grammar each) folds into F-02's marks + F-11's discharge.
- **Deliverables:** the needs-memo (already emitted at F-02; referenced); the rebuilt plate on
  v4; the banked kernel-adoption row.
- **Gates:** plate visual gates minted born-RED at F-02 from the plate marks; rebuild lands
  against the extant v4 surface.
- **π/DELTA:** plate before/after at both viewports.
- **Deps:** F-02 (memo + marks). EXTERNAL: none blocking (the inversion is corrected).
  **Routing:** Fable.

### F-PD-frontend-23 | value.js | W55 SUBSTANCE — THE ADVERSARIAL TRUTH PASS
- **Intent:** the ratified W55 row's frontend substance, folded: a11y/mobile/zoom/RTL/AT
  truth; reopen-not-accept posture; **CH-7: the real-GPU oracle RUNS** (dispositioned: the
  safari + real-GPU annex runs pre-deploy/nightly per P3.4 — the RUN evidence is owed here,
  the CI wiring is the gates program's). CH-4 is verified CLOSED by F-12's record. The B3
  tail (journey-subset re-gate + the 11.7k-line e2e corpus pruning + the .github edit) is
  ROUTED BY NAME to the sibling gates program (P3.4), with the two intersections honored: the
  DRIVEN a11y battery is KEEP-EARNED (this wave consumes, never orphans it) and W55 owns the
  .github edit (nothing in this program touches CI).
- **Deliverables:** the truth-pass record (a11y/zoom/RTL/AT findings dispositioned); the
  CH-7 run evidence; the B3 routing letter.
- **Gates:** born-RED rows minted from findings; zero findings accepted-without-reopen.
- **Deps:** F-11, F-19 (runs against the refined product). **Routing:** Fable.

### F-PD-frontend-24 | program (docs) | PROGRAM CLOSE — THE DELTA LEDGER + ZERO-DROP vs THE FULL CANON
- **Intent:** the close: every visual claim in F-01..23 paired (PL-3 stills; PL-10 traces);
  every ledger row terminal (PL-5); **the zero-drop check runs against the PL-9 canon list —
  L1/L2/addenda + CARRY-LEDGER §B–§F + V-PRIME brackets — with the §B/§C/§D/§F disposition
  table: every row BY NAME folded/banked/retired/ROUTED (receiving program named)**; the F-20
  batch #2 released; the owner-decision docket with recorded outcomes. Probe-run counts are
  ONE LINE in the DELTA ledger (the dedicated accounting-theater gate is dropped — C21).
- **Deliverables:** the DELTA ledger (claim → capture/trace pair → verdict); the CARRY-LEDGER
  disposition table; the residual register; the docket outcomes.
- **Gates:** zero unpaired visual claims; zero silent partials; zero un-dispositioned rows;
  the disposition table covers every §B wave row (W46–W56), §C tail, §D expectation, §F carry.
- **Deps:** all. **Routing:** Fable.

---

## 4. OWNER-DECISION DOCKET

| ID | Decision | Tension | Default |
|---|---|---|---|
| OD-1 | **kf demo execution mode** for F-15..19 — RATIFICATION PRE-GATE, not a docket residual: (a) direct edits from this value-owned program under explicit owner grant, or (b) specs + bounded dispatches into kf's inbox | C4 (addendum) pulls the kf demo in-scope and **WINS on scope** (addenda-WIN law); P4.5 requires an explicit grant for cross-repo edits | **(a) with owner countersign at ratification** (addenda-conformant); under (b) every kf wave's close gate is re-stated as dispatch+verification-window, honestly |
| OD-2 | **Glass relay batching**: two-batch shape (batch #1 = the four centerpiece/unblocker rows, EARLY; batch #2 = audit-sourced) vs single held batch | L1 §4 batching law vs the critical path of F-04/05→F-21 (external latency dominant) | **two batches, #1 early** — decided at ratification; the interruption cost is one letter, priced and accepted |
| OD-3 | **kf old share-link posture** when `?state=` goes readable: hard cut (old links → home) vs a single-release decode-only reader | no-dual-paths edict vs link continuity | hard cut (edict-conformant); surfaced because real shared links die |
| OD-4′ | **EasingPicker precision law** — ONLY IF F-03's A/B measurement shows a human-perceptible stepping percept (the 3-decimal claim ≈0.30px/step is adjudicated imperceptible; the prior OD-4 is KILLED as resting on a misdiagnosis) | measurement vs taste | no docket unless the measurement mints one |
| OD-5 | **C3 form**: persistent second-band mobile toolbar vs a redesigned one-gesture dock surface | screen economy vs first-class affordance — owner-marked | thrice loop delivers BOTH candidates at the F-06 design gate, jointly with the W47 shell ruling |
| OD-6 | **WatercolorDot dose-column class** (principle 13): substrate (no medium recruitment) vs whisper halo on hover | perf/dose law vs the breath-of-life ask | substrate-by-law; whisper only if the glass agent ratifies the exception |
| OD-7 | **CH-4/W54 ownership confirmation**: this program owns the boot-kill (F-12) unless the owner re-homes it explicitly | a ratified close-law must have exactly one owner; no armB sibling program claims it | this program owns; re-homing is an explicit act, never silence |
| OD-8 | **D53.vi app-root rename** (`demo/color-picker/` → `app/`) — CARRY-LEDGER §C: "next formation decides" | tree churn vs naming truth | route to the structure program's tree-move machinery if approved; else retire with rationale |
| OD-9 | **§C/§F untracked residue**: D49 `research/` trio + `audit/rehearsal/` (carrying W44's HeaderRibbon manifest edit ON DISK) — track-or-archive | history truth vs tree hygiene | track (they carry decision provenance); ruled at F-24's disposition table |

---

## 5. THE LANDING MAP (zero silent drops — every relevant row, cited)

**CARRY-LEDGER (the fold source — the section the target program lacked):**
§B W46 → F-02 (rubric adopted, amend-never-parallel). §B W47 → F-06 (whole, incl.
PaneSegmentedControl 1→0, D53.iv, §F M1/M2 fold targets). §B W48 → F-07. §B W49/W50 → F-08
(D57 inside; D55.iii ROUTED to the API program by name; V-PRIME B4 sequenced inside). §B W52 →
F-09. §B W53 → F-22 (ratified rider honored: rebuild ON v4; memo-first; R-BOUNDARY arrow per
P2.2 row 14) + workbench law into F-02/F-11. §B W54 → F-12 (whole; D-1/D-2 close-laws). §B W55
→ CH-4 → F-12; substance → F-23; B3 tail + .github → ROUTED to the gates program BY NAME
(intersections honored: DRIVEN a11y battery never orphaned; no CI touch here). §B W56 → ROUTED
BY NAME to the governance program (release/co-land boundary; its D54/SCI-1 rider already rides
the color program's W56 vehicle) — recorded in F-24's table. §C D49 residue + §F
audit/rehearsal → OD-9 at F-24. §C D53.vi → OD-8. §C dev.sh → owner row, NEVER touched
(recorded). §C B5 → owner-held, never proxied (recorded). §C E13 → standing law, honored at
every wave open. §D glass §7 fixes (Chip self-heal, V-A95) → F-03 (verify-before-design) +
F-21 (verify-at-adoption). §D kf/atlas rows → recorded facts (already discharged/external).
§F gh-pages prod-preview empty-mount → F-02's FIRST probe. §F ActionBarLayer shim → F-06 spec
+ F-21 execution.

**Addenda:** C2 → F-03 (demo cure + re-diagnosis) + F-04 (glass variant) + OD-4′. C3 → F-06
(+OD-5, jointly with W47). C4 → F-15..19 (+OD-1 pre-gate; sweep tempers "mostly broken" to
named defects — collision, insets, single-pointer scrub — all waved; amiga included by name).
C5 → F-13 (value) + F-20/F-21 (glass-gated arms) + F-15 fact row (kf already zero). C6 →
F-14 (value, FULL width: architecture adjudicated then codec) + F-18 (kf, +OD-3). C7 → ROUTED
to the sibling API program (STAY-at-/api per the addendum's reconciliation note; the demo-side
client surface audited in F-02 and handed over). C8 → this program contributes 24 of the ≥50
mega-tranche waves. C10 → F-05/10/11/19 + the 13 principles as wave vocabulary AND PL-10
gates. C11 → demo-facing: F-13 (barrels) + colocation checks in F-02/F-15; **the demo
component-DAG derivation + thrice is OWNED BY the sibling structure program
(PC-structure F-01 derives "value demo component graph; kf src + kf demo via depcruise/madge")
— this program CONSUMES its cluster verdicts at F-11/F-19; routing explicit, not silent**.
C12/C13/C14 → ROUTED to the sibling parser/color programs (C13's demo-facing zero-alloc
consumption exercised by F-22's plate). C16 → honored (isolation; this file is the only
write; zero Codex reads). C17 → PL-6 + per-wave routing notes; this seat reports its served
model. C18 → consumed (backtrace sweep input; D-rows land below). C19 → ROUTED (parser
program). C20 → PL-1. C21 → PL-2 honest budgets + the KISS cut + F-24's one-line accounting.
C22 → F-02/F-15/F-24 (with routes-are-not-screens honored: non-route surfaces + interaction
states in both matrices). C23 → this document IS the amended independent Fable frontend
formation; the union is the parent's act.

**L1:** §4 demo paragraph — Aristotelian audit → F-02/F-15 (on the W46 constitution);
suffusion → F-05/10/11/19; pre-glass-7 corpus NOW → F-02 inputs (§F rows); recursive
colocation → F-02/F-13/F-15 checks + PC-structure verdicts; glass batching → F-20/PL-4/OD-2;
W53 → F-22. §7 edicts → PL-3/5/8/9/10/11 + born-RED discipline + phase labels (this doc =
formation). §8 thrice → PL-1. §9 return contract → F-24.

**L2:** P3.2 `demo/` row → F-02/11/13/22. P3.4 → ROUTED to the gates program with the two
named intersections (DRIVEN a11y battery KEEP-EARNED, never orphaned — F-23/F-24 check; W55
owns the .github edit — no CI touch here). P4.5 → OD-1 pre-gate. P5 vehicles: W53 → F-22;
W55 → F-23 + routing; W56/SCI-1 → ROUTED (color/governance); kf FOLD-FORWARD 15-row marks →
F-15. P0.1 → PL-6.

**Backtrace (C18):** D1 → F-05/10/11/19. D2 → F-14. D3 → F-13/20/21. D4 → F-15..19 + OD-1.
D5 → ROUTED (parser program). D6 → F-03/04/06. F1–F3 → ROUTED (library programs). F4 (api/
EXTRACT vs C7 STAY) → ROUTED to the API program with the reconciliation duty named. The 13
principles → 1 (PL-7), 2 (F-20 spec language), 3/4/10 (F-11/F-19 + PL-10), 5 (F-04), 6
(F-06), 7 (motion-register GATES, F-11/F-19), 8 (F-05 stated GREEN), 9 (F-05/10 + F-12 idle
census), 11/12 (F-05 + F-01 oracle honesty), 13 (OD-6).

**Value sweep:** conclusions 1–2 → F-13; 3 → F-06; 4 → F-03/04; 5 → F-05/10 (census corrected
23 sites/11 files); 6 → F-14; stale `components.json` + ActionBarLayer shim → F-13/F-21;
MEMORY.md `demo/@` drift → stale-record note (downstream plans cite the live flat tree).

**kf sweep:** viewport-fit + safe-area → F-16 (count re-derived: 10 sites); 100vh gated
fallbacks → RETIRED-OK note (F-16); multi-touch cube-only → F-17 (+amiga); per-scene title →
F-18; opaque `?state=` → F-18; fixed-px caps → F-19; kf shadcn zero → F-15 fact row.

**Probe sweep:** set A reuse → PL-2; the kf-desktop un-shot gap → F-15 first-capture
obligation; the profile-lock failure → F-01 (the rig is now a wave); the seat's Opus-served
disclosure → PL-6 exemplar (code-grounded claims stand on file:line; desktop predictions
demoted to capture-required, discharged at F-15).

## 6. RESIDUALS

- The value demo is ONE app: **14 routes** + non-route surfaces; kf is 7 scenes + chrome —
  both matrices finite, enumerated in F-02/F-15 with interaction states.
- kf desktop remains photographically unverified until F-15; no wave asserts a desktop visual
  fact before that capture lands.
- F-21 may close BANKED (named re-trigger: the glass landing) — the honest terminal state for
  externally-gated adoption; nothing else in the program waits on glass.
- This program touches no CI, no .github, no glass-ui source, no `scripts/dev/dev.sh`, and
  lands no source edits in the formation phase.
- Anchor line-cites herein were verified at the pins but MUST be re-derived at execution
  (law 3; e.g. the program's EasingPicker.vue:396 stroke anchor is :367 at my read).

---

## 7. ADJUDICATION LOG (every skeptic claim → PROVEN/DISPROVEN, on my own evidence)

Adjudicator's method: I read the fold source and the repos myself at the pins — CARRY-LEDGER.md
whole, V-PRIME.md §3, value `demo/color-picker/router/index.ts`, `demo/ui/`, `components.json`,
`useColorUrl.ts`, App.vue share, `EasingAuthoringStage.vue`, `EasingSpecimenStrip.vue`,
Dock.vue, `scenes/about/`; glass-ui `useEasingPicker.ts`/`EasingPicker.vue`/`watercolor-dot/`;
kf `useSphereSpin.ts`, `useDragScrub.ts`, `hashSharing.ts`, layout.css, index.html; the five
sibling armB programs (ownership checks); sweep-probe.md. Never vote-counted.

### Skeptic 1

- **S1-A1 (CARRY-LEDGER §B tail dropped all but whole) → PROVEN.** My read of
  CARRY-LEDGER.md:21-30 confirms W46 (constitution, 18 pairs), W47 (PaneSegmentedControl 1→0
  "lives ALIVE at demo/shell/ for exactly this gate" — confirmed alive on disk and mounted at
  Dock.vue:198), W48, W49/W50 (D57/D55.iii), W52, W54 ("D-1 aurora-derive RUNS or V′ does not
  close"), W55 ("CH-4 … the ~5s boot dies or V′ does not close"); §F carries the Chip
  self-heal + M1/M2→W47 + audit/rehearsal rows. Grep of the target program: no W46/47/48/49/
  50/52/54, no CH-4, no LCP; the landing map has no CARRY-LEDGER section; F-18 checked
  against L1/L2/addenda only. I additionally verified NO sibling armB program owns any of it
  (grep across PA/PB/PC/PE/PF: only W53/W55/W56 name-checks). **Adopted:** PL-9; waves
  F-06/07/08/09/12/22/23; the §5 CARRY-LEDGER section; F-24's disposition table.
- **S1-A2 (zero perf program inside an animation-suffusion program) → PROVEN.** CH-4 is a
  ratified close-law (CARRY-LEDGER.md:29); the target program had zero perf waves/gates while
  adding animated surface, and its P3.4 routing note would let the ~856-LOC headless perf
  oracles die with nothing curing the boot. **Adopted:** F-12 (CH-4+W54, one mechanism
  family) + OD-7 + the post-suffusion idle census.
- **S1-A3 (component-DAG mandate fudged into an eyeball) → PARTIALLY DISPROVEN.** My own
  check the skeptic did not run: **PC-structure F-01 explicitly derives "value src refresh +
  value demo component graph; kf src + kf demo via depcruise/madge" and maps C11 whole to its
  program** — the DAG derivation + thrice adjudication is OWNED by the sibling; a new DAG
  wave here (S1's K-03) would double-book it. PROVEN residue: the target's landing map never
  NAMED that routing ("colocation checks" reads as silent narrowing). **Adopted:** explicit
  routing in §5 + consumption hooks at F-11/F-19; no duplicate wave.
- **S1-A4 (C6 narrowed to its second clause) → PROVEN.** router/index.ts:16-19 declares the
  model in its own comment ("we don't use `<router-view>` for rendering"); every route is
  `Stub = { render: () => null }`; the target program never adjudicated it. **Adopted:**
  F-14 stage 1 (architecture ruling) before stage 2 (codec).
- **S1-A5 (amiga dropped by name) → PROVEN.** kf `demo/scenes/amiga/useSphereSpin.ts` read:
  one `activePointer`, `canvasEl.setPointerCapture(e.pointerId)`, `e.pointerId !==
  activePointer` guards — a live single-pointer interaction surface absent from F-13's list.
  **Adopted:** F-17 names amiga; "no meaningful gesture" legal only as an owner-visible
  table verdict.
- **S1-A6.1 (14 routes, not 15) → PROVEN.** router/index.ts:21-38: 9 public + 5 admin named
  routes + a catch-all REDIRECT. The target's own F-01 parenthetical enumerates 14 while
  saying 15. **Adopted:** 14 everywhere; the redirect is not a screen.
- **S1-A6.2 (routes are not screens; interaction states) → PROVEN.** `demo/scenes/about/`
  (AboutPane, ColorNutritionLabel, katex/, markdown/) mounts on no route; every owner-mark
  frame in set A is an interaction state (kebab open, easing editor open). A one-sweep-per-
  viewport budget cannot photograph that defect class. **Adopted:** matrices = routes +
  non-route surfaces + named interaction states (F-02/F-15); PL-2 amended.
- **S1-A6.3 (admin band unprovisioned) → PROVEN.** All 5 admin routes carry
  `meta: { admin: true }`; nothing in the target provisions authed capture. **Adopted:**
  F-01 authed-admin recipe; F-02 gate forbids silent scope-trim.
- **S1-A6.4 (breakpoint band invisible at 390/1440) → PROVEN** on the program's own F-02
  evidence (dock behavior flips at `lg`, crowds below `sm`). **Adopted:** PL-2 matrix
  {390, 768, 1024, 1440} with the band as a spot set.
- **S1-B1 (OD-4 + the second RED assertion rest on a misdiagnosis) → PROVEN.** Verified:
  glass `useEasingPicker.ts:162-163,191` does `+cx.toFixed(3)` — 0.001 unit steps ≈ 0.30px at
  the 304px seat, below perceptual threshold; the probe sweep's own annotation locates the
  "low-res" percept in the specimen tiles/thumbnails. A RED that passes on disk but cannot
  photograph the owner's percept is a vacuous-RED. **Adopted:** OD-4 killed as framed →
  OD-4′ (measurement-gated); F-03 runs the re-diagnosis (stroke px / glyph px / A/B) before
  any precision docket; the tile/stroke arms of C2 stand (they were correctly diagnosed).
- **S1-B2 (counts unverified) → PROVEN.** My greps: 23 `<WatercolorDot` template sites / 11
  .vue files (target said ~20/7); 14 routes (said 15). Additionally re-derived: `demo/ui`
  imports = 48 FILES / 90 import lines (the target's "48 import sites" was a defensible file
  count — recorded, not condemned). **Adopted:** corrected censuses in F-10/F-02/F-13; law-3
  re-derivation noted in Residuals.
- **S1-B3 (F-17/W53 dependency inverted) → PROVEN.** CARRY-LEDGER.md:27 rider verbatim:
  "rebuild the perceived-space plate ON v4 … rebuild, not restore"; P2.2 row 14 gates
  R-BOUNDARY on W53's needs — the arrow runs memo→color-program, not color-program→plate.
  **Adopted:** F-22 memo-first (emitted at F-02), rebuild on v4, kernels adopted
  opportunistically (banked row).
- **S1-B4 (dishonest probe oracles; no rig) → PROVEN in substance.** sweep-probe.md confirms
  0 new frames (profile locked; 300s timeout); no safe-area/multi-touch mechanism was named
  anywhere in the target. Safe-area emulation resolving `env()` to 0 without an explicit
  inset override is the known emulation behavior — the cure (name the honest oracle: CDP
  `Emulation.setSafeAreaInsetsOverride` or real device) is correct under either exact CDP
  surface, and F-01's gate requires a PROOF capture showing a nonzero inset, which makes the
  oracle self-verifying. **Adopted:** F-01 as precondition wave; F-16/F-17 gates re-anchored
  to it.
- **S1-B5 (motion program gated on stills) → PROVEN.** PL-3 as written pairs stills only;
  the envelope/register/hard-cut numbers (40–60ms, t90 ≤150ms, 120–170ms) appear in no gate
  that can measure them. **Adopted:** PL-10; motion-register tables become gates
  (F-11/F-19); F-05's GREEN stated from trace.
- **S1-C (gates that cannot fail) → PROVEN, each.** kf reka-grep already 0 (my check
  concurs) → recorded as FACT (PL-11, F-15); F-09's no-glass-edits → constraint, not gate
  (F-20); F-02's "obvious gesture" → falsifiable GREEN (≤2 taps + rest-state affordance
  capture, F-06); F-05 GREEN → stated (trace-asserted envelope + rAF-dead-at-settle); F-06's
  3-of-23 → capture-per-family + full 23-site checklist (F-10); F-08 boot capture → demoted
  to smoke, regression claim withdrawn (F-13).
- **S1-D (the scheduling contradiction: centerpieces starved through the held relay) →
  PROVEN.** Under the target's own defaults (OD-2 HOLD + F-04 dep "adopted variant" + F-06
  dep F-05 + F-07 after them), three waves cannot execute inside the program; F-04's
  demo-curable half (the 19rem clamp — verified demo-owned at EasingAuthoringStage.vue:105)
  was imprisoned needlessly. **Adopted:** two-batch relay (OD-2 decided, batch #1 early);
  F-03 lands the demo-owned cures now; F-21 is the single reconvergence home; PL-5 BANKED
  posture makes the external gate honest.

### Skeptic 2

- **S2-A (the grand silent drop, A1–A6) → PROVEN.** Same evidence as S1-A1/A2 plus: A4's
  collision verified (Dock.vue:198 mounts PaneSegmentedControl; CARRY-LEDGER W47 holds the
  1→0 gate) — F-02/W47 must be decided jointly (**adopted:** F-06 merges them); A6's V-PRIME
  B4 verified verbatim at V-PRIME.md:111 ("the W44 routed-mount witness and W50 journeys
  supersede them") — **adopted:** B4 sequenced inside F-08. The defense S2 pre-empted ("P5
  scoped the tail out") is indeed defeated: P6 row 4 + L1 §9 + PL-5 all bind.
- **S2-B1 (OD-2 default starves the centerpieces) → PROVEN** (= S1-D). **Adopted:** as above.
- **S2-B2 (the demo-curable arms of C2 are coupled to glass; wrong altitude) → PROVEN.**
  Verified: the 19rem clamp is demo-owned with the seat's own "retired at the P7 adopt"
  comment; `EasingSpecimenStrip.vue` is demo code composing glass `Chip shape="cell"` (:98-
  :117). **Adopted:** F-03 (demo cure now, incl. strip redesign at demo altitude unless a
  glass primitive is PROVEN necessary); F-04 keeps only the truly glass-owned arms.
- **S2-B3 (no adoption/reconvergence wave) → PROVEN.** Three glass-gated arms had no home
  that could close under PL-5. **Adopted:** F-21, with the BANKED posture.
- **S2-B4 (F-17 contradicts the rider on disk) → PROVEN** (= S1-B3). **Adopted:** F-22.
- **S2-C (OD-1 default re-litigates the addendum; six waves' gates fiction under (b)) →
  PROVEN.** The target's own tension cell concedes "C4 … WINS on scope" then defaults (b).
  **Adopted:** OD-1 elevated to a ratification PRE-GATE with default (a)+countersign; under
  (b) the kf gates are re-stated honestly as dispatch+verification-window.
- **S2-D1 (C6 narrowed to codec altitude) → PROVEN** (= S1-A4). **Adopted:** F-14 two-stage.
- **S2-D2 (C11 reduced to colocation checks) → PARTIALLY DISPROVEN** (= S1-A3): the demo
  component-DAG is owned by PC-structure F-01 (my sibling-program check); the true defect was
  the unnamed routing. **Adopted:** explicit §5 routing + F-11/F-19 consumption.
- **S2-E1 (routes off-by-one) → PROVEN** (= S1-A6.1).
- **S2-E2 (WatercolorDot census 11 files) → PROVEN** — my grep: 11 import files, 23 template
  sites. **Adopted:** F-10 census.
- **S2-E3 (≤2-run budget infeasible/vacuous) → PROVEN** (admin auth + interaction states).
  **Adopted:** F-02 honest ≤4-run budget; PL-2.
- **S2-E4 (born-RED dilution) → PROVEN.** F-05/F-10-class absence gates are a different
  animal from live-defect REDs. **Adopted:** PL-11 (BORN-ABSENT labels; facts recorded, not
  gated); the real REDs (F-06 burial, F-16 collision, F-12 LCP) keep their teeth.
- **S2-E5 (probe-budget accounting theater) → PROVEN in part.** C21 condemns
  process-about-process; but PL-2 still needs evidence. **Adopted:** one-line run counts in
  the DELTA ledger; the dedicated accounting gate dropped.

### Cross-skeptic conflicts, adjudicated

- **Gesture-core extraction (target: extract the cube's core; S2: do not touch the cube):**
  ruled WITH S2. The cube facility is the sole complete implementation and the owner's
  reference; C4 asks the OTHER animations reach it, not that it be reshaped; KISS/C21 and
  PL-7's never-lose-features both point to isolation. A NEW small shared composable serves
  the five other scenes; unification is BANKED with a named re-trigger. (F-17.)
- **F-14+F-15 merge (S2) vs keep split (S1 implicitly):** ruled WITH S2 — per-scene titles
  and share legibility are one share-restore story; the split was a touchpoint, not a
  boundary. (F-18.)
- **New DAG wave (S1 K-03) vs close-attached record (S2 G-20):** BOTH overruled by the
  sibling-ownership finding — PC-structure owns the derivation; this program consumes.
- **Wave grain:** S1's counter-program (21) and S2's (~20) both recovered the same inventory;
  the final lands at **24** — the delta over the target's 18 is recovered RATIFIED inventory
  (W47/48/49-50/52/54-CH-4/W55-substance + the rig + reconvergence), net of two merges
  (F-14+F-15→F-18; F-03's demo half→F-03) — not inflation. Every added wave is a fold, not
  an invention.

### Convergence statement

The amended program survives both skeptics' strongest surviving points by construction: the
fold source is law (PL-9) with every §B/§C/§D/§F row landed by name (§5, F-24); the two
close-laws have owning waves (F-12) and an ownership docket row (OD-7); the glass round-trip
is re-architected (early batch #1, demo-owned cures decoupled, one reconvergence home with an
honest BANKED posture); motion claims carry motion oracles (PL-10); the capture rig is a
precondition wave (F-01); every count the skeptics corrected is re-derived here and every gate
they condemned as unfailable is re-stated falsifiable or demoted to a recorded fact (PL-11).

— Adjudicator, thrice loop, PD-frontend. Model served: claude-fable-5.
