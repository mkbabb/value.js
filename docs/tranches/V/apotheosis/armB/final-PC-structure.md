# FINAL PROGRAM PC-STRUCTURE — the structure tranche program (both libraries + both demo component trees)
### armB independent Fable formation · THRICE-ADJUDICATED FINAL · 2026-07-19

**Pins (G0′):** value.js `tranche-u@c654824e` (src-identical to canon `db77dbd8`; `git diff db77dbd8..HEAD -- src package.json` = empty) · kf `keyframes-v-exec master@81a56990` (letters' `0dac636b` +2 docs-only commits) · glass-ui `cdc322b9` · atlas ACTIVE `.p-totality/atlas@fe9abcf` · parse-that `ef10d5b`. Re-pin at every wave execution (§7.2 law).
**Provenance:** L1 = `keyframes-inbox-2026-07-18-vnext-ingestion-prompt.md`; L2 = the P0–P6 packets; C# = CONVERSATION-ADDENDA (addenda WIN); sweeps = `armB/sweep-{value-lib,kf-lib,value-demo,kf-demo}.md`. This FINAL supersedes `program-PC-structure.md` after the thrice loop: skeptic briefs `thrice-PC-structure-skeptic{1,2}.md`, adjudicated in §8 with independent evidence (every verdict re-derived on tree this session — never vote-counted). Independence firewall honored: zero Codex-corpus reads.
**Phase label (§7.4):** FORMATION output; the waves bind the implementation phase.

---

## §1 — PROGRAM CHARTER (amended)

1. This program owns the STRUCTURE slice of the mega-tranche (C8): target module/directory
   structures for the value.js library, the keyframes.js library, and both demo component
   trees — derived from machine DAGs, never hand-drawn (L1 §8). **The `/api` backend slice
   is CEDED whole to program PE-api** (charter + F-PE-api-02/-08 already own the STAY
   ratification and the 125-file structure execution; §8 K3.1) — this program contributes
   only the abstract structure-grammar facility (§4 F-02) as PE's rule input.
2. Method is DAG-thrice (C20): dynamic bespoke cluster batches, AGGLOMERATED to five
   (L1 §8 "agglomerate clusters as fits"), each viewed by 2 Fable skeptics + 1 Fable
   adjudicator; convergence = two consecutive clean passes; ≤3 iterations per batch before
   owner escalation. **Seat ledger stated in §3** (C17: Fable cost is outrageous).
3. Every gate is born-RED wherever the defect is live today; this doc says what RED looks
   like, with file:line evidence — and every gate is **failable AND passable by
   construction** (the skeptics' gate-inventory audit is adopted whole, §8).
4. **The surface-freeze law, made schedulable:** surfaces stay FROZEN until the ONE co-land
   boundary (P4.2, executed by F-PF-governance-07). Every surface-affecting cut — the ten
   `./transform` symbols dying with decompose, the quantize demote + `./quantize` key drop,
   the `collectDeclarations` trim — is a **boundary-delta LEDGER row** (authored F-03,
   tombstoned §7.1, four-tree re-grepped at execution HEAD) and EXECUTES only at the
   crossing. Pre-boundary waves perform internal-only moves, witnessed by pack-parity.
   (§8 K1/S2-1 — the original F-04⊥F-05 contradiction is cured by construction.)
5. kf library waves are SPECS + bounded dispatches into kf's coordination inbox (P4.5);
   **one packet, not four letters** (§8 S2-10); direct cross-repo edits only under explicit
   owner grant (OD-S4).
6. Governance: kf structure waves AMEND the ratified LT blueprint + EXTEND proof:structure,
   naming LT-10/LT-16 superseded (P4.4 G5′) — never a parallel authority.
7. KISS/parsimony (C21) throughout: fewer, well-cut waves; goldilocks granularity; glass-ui
   is the reference model for flattening/component idioms — **including its deliberate
   dir-prefixed `.vue` idiom (`button/Button.vue`, `watercolor-dot/WatercolorDot.vue`,
   ls-verified): the C11 name-strip rule binds TS modules; `.vue` SFCs are exempt** (§8 E1).
8. Model routing (C17): Fable for every design cut, thrice seat, and adjudication; Opus for
   mechanical sweeps. Every seat reports `model_served` (P0.1).
9. **ONE-OWNER-PER-ARTIFACT law (new):** no gate, census, decision row, or module cut is
   authored in two programs. Reconciled ownerships: manifest gate + deps strip + capability-
   diff = **F-PF-governance-02** (CI seat) with **F-PA-parse-03's whitelist rule folded into
   the single gate text** (`runtime deps ⊆ {@mkbabb/parse-that}` — the parse-that decree,
   L1 §2/P1.6, is a REAL runtime dep; a bare "no runtime deps" gate is decree-breaking, §8
   K2); css/ internal cut = **F-PA-parse-03** (its deliverable text already claims it); api
   = **PE-api** (OD-S3 merges into PE's OD-api-1); kf per-scene title = **F-PD-frontend-14**;
   zone verdicts docket = **F-PF-governance-04**; the co-land crossing = **F-PF-governance-07**.
10. **Recipient-existence law (new):** every handoff and dependency names a file/wave id
    that exists in the arm corpus, checked at close (§4 F-08). The corpus has NO apparatus/
    gates program (ls-verified: six programs PA–PF; PF itself routes P3.3 rows to a
    nonexistent "[SIBLING:apparatus]", program-PF-governance.md:11,366) — this program
    carries the **arm-level escalation row**: the apparatus program is FORMED or PF adopts
    its rows; meanwhile the kf packet (F-07) embeds the P3.3 preconditions as in-letter
    phase-0 gates so no wave of this program depends on a phantom (§8 K3.4).

---

## §2 — THE TARGET STRUCTURES (opening positions, thrice-adjudicated per batch)

### 2.1 value.js library (26 TS files, 0 cycles, 4,647 core LOC — sweep-value-lib §1–2)

```
src/
  foundation/      result + math (+index)          — result.ts 6-LOC sand folds
  color/           model · anchors · operations (+index)
                   — R1–R6 ENFORCED now; the anchors.ts tables-vs-23-fns split is a
                     DEFERRED JOINT RIDER cut once against the POST-RESTORE mass
                     (PB-color lands boundary/gamut/raytrace/okhsl/ΔE/Into/ramp here,
                     ~tripling the dir — never goldilocks-cut a third of the final mass;
                     the program's own "no pre-churn" law, applied uniformly; §8 E2/S2-6)
  css/             PA-OWNED internal cut (F-PA-parse-03: restored combinator core,
                   "internally re-cut to goldilocks granularity", css/index.ts byte-stable).
                   This program's binding interface is a GATE, not a co-signed doc nobody
                   booked (§8 K3.2): the ported R1–R6 + a css→color module-door rule bind
                   whatever PA lands. stylesheet.ts's R4 RED = named transfer to PA (OD-S5).
  transform/       path.ts (+ real index.ts)        — decompose.ts dies via the F-03 LEDGER
                   at the boundary (its 6 runtime + 4 type symbols ride `./transform` today:
                   src/subpaths/transform.ts:7-24, tree-verified — NOT internal)
  easing.ts value.ts                                — kf-consumed spines stay (P3.2)
  (quantize.ts → demo, AT the boundary via ledger)  (subpaths/ → dissolved, keys frozen)
```
- Exports: the 7 keys FROZEN pre-boundary. Retarget spec CORRECTED (§8 D5): each key gets
  an **explicit per-key module specifier** — `/index` only where a real dir exists
  (`./color ./css ./transform`); root-module keys (`./value ./easing ./math ./quantize`)
  point at explicit module files (`dist/value.js`, `dist/easing.js`, `dist/foundation/math.js`,
  `dist/quantize.js`) — no fabricated one-file dirs (that is the sand §1.7 bans). D50
  explicitness is the requirement, not literal `/index`.
- test/ re-mirrors src/; the 11 demo-referencing root tests (count re-verified this session)
  displace to `demo/test/` (existing home, 3 files verified); ghost `test/parsing/` dies
  (dir verified extant, mirrors deleted src); one library import idiom + the 7-file
  packed-surface suite kept as a named deliberate probe.

### 2.2 keyframes.js library (single `src/animation/**` tree — sweep-kf-lib §1)

```
src/               ← FLATTENED from src/animation (13 anchors re-derived; ~340 external +
  engine/ compile/ resolve/ group/ waapi/ physics/ orchestration/ svg/ scroll/
  ingest/ presets/ constants/ <internal-successor>/     648 intra-src import lines)
  easing.ts index.ts load-engine.ts public.ts validate.ts
```
- Exports exactly `.` + `./engine` frozen (package.json exports re-verified this session);
  **`./engine`'s source is `public.ts`** (vite.config.ts:172 re-verified) with a LIVE
  external consumer (atlas `MorphSVG`, buildMarkAnimation.ts:7) — P4.1.
- `internal/` (535 LOC / 9 leaves / 11 importers) restructures per OD-S1 (amended, §5);
  the depcruise leaf-law key `.dependency-cruiser.cjs:171` moves with any rename.
- Zone membership defers to the **PF-04 zone docket** (the real owner of P3.1's verdicts —
  "zones program" was an unresolvable name; §8 ADJ-1): never flatten code that is about
  to die.
- Delivery: ONE internally-sequenced packet, enforcement BEFORE churn (§8 S2-7/S2-10).

### 2.3 value.js demo (259 files, single app — sweep-value-demo §1–3)

Largely compliant post-W43. Deltas: `demo/ui/` (19 one-line barrel dirs, ls-verified)
DISSOLVES onto direct `@mkbabb/glass-ui` imports; stale root `components.json` DELETED;
the 4 reka-ui `AcceptableValue` type leaks (exactly the 4 files, grep re-verified) closed
via a glass-ui-exported type (BH relay if absent). **Widened per §8 S2-3/S2-2/D1:** the C5
close is an IDIOM census, not just an import-tag census — `demo/shared/utils.ts` carries
the canonical shadcn `cn()` (clsx+twMerge; sole clsx/tailwind-merge importer in the demo,
grep-verified) while glass-ui already exports `cn` (glass-ui/src/index.ts:281, verified) —
retire + drop both devDeps; `demo/shared/` itself is adjudicated (16 files reference
`shared/ui/{EmptyState,PaneHeader}.vue` — a legitimate dir the old gate grep collided with);
`palettes/` (84 files re-counted this session / ~9k LOC) gets the C11 long-dir goldilocks
adjudication the original dropped. C5 headline stands: zero missing glass-ui component
equivalents — the "prototype missing components" branch is EMPTY-SET on evidence.

### 2.4 keyframes.js demo (58 vue / 127 ts — sweep-kf-demo)

Mature glass-ui-native single-screen SPA; ZERO shadcn/reka imports (C5 satisfied).
Narrow work: `transport/` (~30 files) goldilocks re-cut, scene-registry colocation audit —
**gated on kf `test:demo` (MR4), the right oracle class for playback/RAF logic, not a
screenshot** (§8 G6). The per-scene `document.title` hook is **CEDED to F-PD-frontend-14
by name** (it owns that deliverable verbatim, program-PD-frontend.md:262ff; §8 K3.5).
Mobile defects (viewport-fit, non-cube multi-touch) are PD-frontend rows, handed off by name.

### 2.5 api/ — CEDED

**PE-api owns the whole slice**: STAY-at-`/api` ratification (F-PE-api-02), the C7 facility
census (F-PE-api-01), the 125-file module-structure execution + gate set (F-PE-api-08), the
fourier isomorphism (F-PE-api-05/07), and the single owner-decision row **OD-api-1**
(EXTRACT-vs-STAY — this program's OD-S3 is MERGED into it; recommendation unchanged: STAY,
P3.2's row superseded with a named tombstone). This program's contribution: the shared
structure-grammar spec (F-02) as PE's rule input, and the explicit landing-table N/A row —
**kf has NO api/ dir** (PE census-proven; the C7 "BOTH repos" row closes N/A on the kf side,
§8 D4).

---

## §3 — THE DAG-THRICE BATCHING PLAN (C20, executable, budgeted)

Machine-derived DAGs are ADOPTED where they already exist on disk — `armB/madge-src.json`
(26 files / 0 cycles, re-verified this session) and `armB/depcruise-animation.json` (476KB)
predate this program; F-01 does NOT re-run them (§8 S2-9/P1). Gap graphs (value demo
component graph; api graph → handed to PE) are captured once in F-01; every wave re-derives
its own anchors at execution per §7.3 regardless.

**Five agglomerated batches** (was 13 clusters — thrice on the contested cuts, not on
mechanical mirroring; canon-compliant per L1 §8 "agglomerate clusters as fits"):

| Batch | Contents | Waves |
|---|---|---|
| B1 | value gate grammar + test re-mirror + import idioms | 02 |
| B2 | value boundary ledger + subpaths dissolution + library re-cut | 03, 04, 05 |
| B3 | value demo: barrel dissolution + shared/ adjudication + palettes long-dir | 06 |
| B4 | the kf structure packet (enforcement → flatten → restructure) | 07 |
| B5 | reconciliation + close (opener rows re-checked, zero-drop, recipient existence) | 01, 08 |

Per batch: 2 fresh Fable skeptics assume the structure is WRONG; 1 Fable adjudicator proves/
disproves with its own evidence. Convergence = two consecutive clean passes; ≤3 iterations →
owner escalation. **Seat ledger: 5 × 3 × 2 = 30 seats baseline; 45 absolute ceiling at full
escalation** — stated, bounded, batched 5–6 concurrent (was an unstated ≥78-seat floor, §8
P2/S2-8). §2's structures are OPENING POSITIONS the batches adjudicate — acceptance gates
bind only after the owning batch converges. Consumer-edge truth is always the four-tree
census at pinned HEADs (P3 method note).

---

## §4 — THE WAVE SET (8 waves)

### F-PC-structure-01 | program-wide | THE THIN OPENER: cluster cut + reconciliation rows (non-serializing)
- **Intent:** adopt the existing DAG artifacts, capture only the gap graphs (value demo
  component graph; api graph → delivered to PE-api as input), cut the §3 batches, open the
  thrice registry with the seat ledger — and execute the cross-program reconciliation this
  adjudication mandates. **No downstream wave serializes on this one** except for its own
  batch brief; every wave re-derives at execution anyway (§7.3).
- **Deliverables:** batch map + briefs; thrice registry + seat ledger; the reconciliation
  rows, each a written notice in the arm corpus: (a) css/ internal cut ceded to F-PA-parse-03;
  (b) api slice ceded to PE-api, OD-S3→OD-api-1 merge; (c) manifest-gate SINGLE TEXT
  co-authored into F-PF-governance-02 with PA's whitelist rule; (d) kf title hook ceded to
  F-PD-frontend-14; (e) zone-verdict dependency re-pointed at F-PF-governance-04; (f) the
  co-land execution re-pointed at F-PF-governance-07; (g) the **ARM ESCALATION row**: no
  apparatus program exists — form it or PF adopts P3.3 rows 1/2/7 + the e2e adjudication.
- **Acceptance gates:** every edge in the adopted+gap DAGs machine-derived (tool output on
  disk; zero hand-drawn edges); value src DAG = 26 files / 0 cycles reproduced from the
  archived artifact (a delta is a RED requiring an enumerated per-file explanation tied to
  landed commits — no open-ended escape hatch, §8 G1); all seven reconciliation rows exist
  as corpus artifacts; every later wave cites its batch brief.
- **Dependencies:** none. **π/DELTA:** none. **Model routing:** Opus extraction; Fable
  batch cut + reconciliation prose.

### F-PC-structure-02 | value.js | THE STRUCTURE GATE + TEST RE-MIRROR, ONE MOTION (born-RED → GREEN in-wave)
- **Intent:** build the value↔kf structural-isomorphism ABSTRACT FACILITY and discharge it
  against value's live defects in the same wave (a gate and its cure are one motion;
  born-RED is a state within the wave, not a wave boundary — §8 S2-15). kf's live
  proof:structure R1–R6 ruleset (755L, selftest-proven) abstracts into a portable rule SPEC
  + selftest vectors; **the spec + vector file are HOMED in value.js** (this repo owns the
  text; the F-07 packet embeds a checksum-pinned copy — named home, no copy-paste drift,
  §8 S2-15); implementations stay per-repo (KISS — no new package).
- **Deliverables:** the shared rule spec (R1 stutter · R2 fragment/hollow-shim · R3
  impure-barrel · R4 500-line ceiling · R5 kind-dir ban · R6 unused-exports + NEW R-ISO
  test-mirror rule) with the glass-ui reference prose **including the `.vue` exemption**
  (`button/Button.vue` is the idiom, not a violation — ls-verified; §8 E1); the value gate
  + `--selftest`; the CURE: `test/{foundation,color,css,transform}/` mirrors; the 11
  demo-referencing tests (count re-verified) moved to `demo/test/`; ghost `test/parsing/`
  deleted; one library idiom (`../src/...`) + the 7-file packed-surface suite kept as the
  named surface probe; the css→color module-door rule authored (binds PA's restored tree).
- **Acceptance gates (born-RED, defects live today — with HONEST ownership labels at
  birth, §8 E3):** R-ISO RED on ghost `test/parsing/`, the 11 displaced tests, missing
  mirrors, 3 coexisting idioms — **cured to GREEN in-wave (this program's own RED set)**;
  ported R4 RED on `stylesheet.ts` 899 (label at birth: NAMED TRANSFER → PA, OD-S5),
  `decompose.ts` 609 (label: dies via the F-03 ledger row at the boundary), `path.ts` 564
  (label: OD-S6 disposition in F-05); selftest proves every rule passes AND fails;
  **test-count parity SUMMED across the library + demo vitest projects, with a per-file
  move ledger** (a naive single-project "diff = 0" is self-contradictory when 11 files
  move, §8 G7); packed-surface suite green against a fresh pack.
- **Dependencies:** F-01 batch brief (B1). **π/DELTA:** none.
- **Model routing:** Fable facility design + rule cut; Opus grep census + mechanical moves.

### F-PC-structure-03 | value.js | MANIFEST TRUTH + THE BOUNDARY-DELTA LEDGER (the surface law, mechanized)
- **Intent:** defuse the committed registry bomb NOW (internal to the manifest, not the
  export surface) and open the ONE ledger every surface-affecting cut rides to the
  F-PF-governance-07 crossing — killing the original F-04⊥F-05 contradiction by
  construction (§8 K1/S2-1).
- **Deliverables:** deps STRIP/RELOCATE → devDependencies (the block is COMMITTED at HEAD:
  `dependencies: {"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}` —
  re-verified `git show HEAD:package.json` this session); the pre-publish manifest gate as
  ONE TEXT co-owned with F-PF-governance-02 (which owns the CI seat) **with the PA
  whitelist: `runtime dependencies ⊆ {@mkbabb/parse-that}`** — never "no runtime deps"
  (the decree-breaking spec, §8 K2); self-dep clause (prior art: value shipped a
  SELF-dependency v1.1.0–v2.0.1); **the COMPLETE boundary-delta ledger**, one row per
  surface delta, each with a §7.1 tombstone draft + a four-tree re-grep obligation at
  execution HEAD: (1) the TEN `./transform` symbols dying with decompose
  (`decomposeMatrix2D/3D, recomposeMatrix2D/3D, slerp, interpolateDecomposed` +
  `DecomposedMatrix2D/3D, Vec4, Mat4` — subpaths/transform.ts:7-24, tree-verified; the
  original manifest omitted ALL of them, reproducing the exact no-advocate⇒no-tombstone
  mechanism §7.1 exists to kill, §8 K1); (2) quantize DEMOTE to the demo extract workbench
  + `./quantize` key drop (consumers verified demo-only); (3) `collectDeclarations` trim
  (0 external consumers). Ledger rows EXECUTE at F-PF-governance-07, not before.
- **Acceptance gates (BORN-RED, genuine):** the manifest gate is RED at HEAD on the
  committed deps block — its first green IS the strip (the one born-RED both skeptics
  certify as real); ledger completeness gate: an export-census diff of the planned
  boundary state vs today enumerates EXACTLY the ledger rows (any unlisted delta = RED —
  the capability-diff gate, owned by F-PF-governance-02, is the witness; owner NAMED, no
  phantom, §8 G8).
- **Dependencies:** F-01 (reconciliation row c); OD-S2 RULED (prune vs kf-adoption — priced
  in §5). **π/DELTA:** none.
- **Model routing:** Fable ledger + tombstone prose; Opus census harness.

### F-PC-structure-04 | value.js | SUBPATHS DISSOLUTION + HYGIENE (internal-only, pack-parity-witnessed)
- **Intent:** dissolve `subpaths/` with the 7 keys frozen — a genuinely INTERNAL move
  (keys and symbol sets unchanged; only dist paths behind the keys move) — and execute the
  zero-dependency hygiene immediately (decoupled from the contentious prunes it was chained
  to, §8 K1-fix/S1-PC′-04).
- **Deliverables:** `subpaths/*.ts` (163 LOC, pure re-export homes, zero top-level runtime
  logic) deleted in favor of real per-dir `index.ts` incl. a NEW `transform/index.ts`;
  exports map retargeted with **explicit per-key specifiers — `/index` only for real dirs
  (`./color ./css ./transform`), module files for the root-module keys (`./value` →
  `dist/value.js`, `./easing` → `dist/easing.js`, `./math` → `dist/foundation/math.js`,
  `./quantize` → `dist/quantize.js`)** — no fabricated one-file dirs (§8 D5); hygiene: 39
  root PNGs deleted, `src/.DS_Store` + `test/.DS_Store` purged, stale root `components.json`
  deleted (dead `demo/@/components` path, Sass cite).
- **Acceptance gates:** packed-surface re-verify: `npm pack` + api-extractor diff = **7 keys,
  same symbol set** — now satisfiable by construction because every surface delta is a
  ledgered F-03 row executing later (§8 K1); library build + full suite green; the F-02
  packed-surface probe suite green against the dissolved layout.
- **Dependencies:** F-02 (probe suite exists), F-03 (ledger open — so nothing rides this
  wave that belongs on it). **π/DELTA:** none.
- **Model routing:** Opus mechanical; Fable export-map review.

### F-PC-structure-05 | value.js | LIBRARY RE-CUT — UNCOUPLED ZONES ONLY (+ the name-strip rider)
- **Intent:** cut what no other program is about to rewrite: foundation sand, the one
  abstraction leak, the path.ts seam. **No color/ split now** — the anchors.ts
  tables-vs-fns cut is a DEFERRED JOINT RIDER, triggered by PB-color's restore landings
  and thrice'd against the post-restore mass (the "no pre-churn" law applied uniformly,
  §8 E2/S2-6). **No css/ cut ever** — PA owns it (§8 K3.2); the interface is F-02's gate +
  door rule.
- **Deliverables:** `foundation/result.ts` (6 LOC) folded; the `css/grammar.ts →
  color/anchors` direct edge re-pointed through `color/index` (a one-line reroute — cheap
  even in a dying file) + the door RULE live in the gate (binds PA's restored tree so the
  leak cannot rebirth); `transform/path.ts` (564) split-or-allowlist per OD-S6; the C11
  **name-strip rider, census-first**: the census is ALREADY run for the dir-prefix pattern —
  value src = 0 hits (full file list verified), value demo = 0, kf demo = 0 — so the rider
  executes only if the wave-time re-census differs; `.vue` exempt by rule (§8 E1 — a whole
  wave for a plausibly-empty rename map was ceremony); the color/ joint-rider row entered
  in the registry with its named trigger (PB R-DELTAE/R-GAMUT/R-OKHSL landed).
- **Acceptance gates:** ported R1–R6 GREEN on all touched files; madge re-run: 0 cycles,
  leak edge gone; R4 tracked-RED remains ONLY on `stylesheet.ts` (named PA transfer,
  OD-S5 — never silently allowlisted) and `path.ts` carries its OD-S6 disposition;
  name-strip: zero stutter-named TS files across the three unguarded trees post-wave.
- **Dependencies:** F-02 (gate live); batch B2 converged. (The old "decompose/quantize
  already out" dependency is DELETED — those are boundary rows now, and this wave never
  needed them, §8 K1.) **π/DELTA:** none.
- **Model routing:** Fable (the design heart of the value-lib slice); Opus mechanics.

### F-PC-structure-06 | value.js demo | SINGLE-TRACK GLASS-UI + C5 IDIOM CLOSE + THE LONG-DIR ADJUDICATION
- **Intent:** finish the structural half of C5 as an IDIOM census, not an import-tag grep
  (§8 S2-3): single-track glass-ui, shadcn idiom residue retired, the dropped C11 long-dir
  row restored (§8 D1).
- **Deliverables:** `demo/ui/` (19 barrel dirs) deleted; 48 sites rewritten to direct
  glass-ui imports (correct subpaths); the 4 reka-ui `AcceptableValue` type leaks
  (MixConfigBar.vue, GenerateControls.vue, AuroraPane.vue, GradientVisualizer.vue — the
  exact four re-verified by grep) rerouted through a glass-ui-exported type, with a BH-inbox
  relay letter if the export is missing (standing relay edict); **`cn()` retired onto
  glass-ui's `cn` (src/index.ts:281, export verified) + `clsx`/`tailwind-merge` devDeps
  dropped** (sole demo importer = shared/utils.ts, grep-verified; `reka-ui` devDep STAYS —
  it is a NON-optional glass-ui peer, P4.2); **`demo/shared/` adjudicated** (§8 S2-2): the
  16 `shared/ui` import sites are legitimate (EmptyState/PaneHeader); the `debounce` copy
  carries its DOCUMENTED deliberate rationale (the file header records the T.W6.5 root-
  barrel-shed decision + the 36KiB chunk cost, read this session) — it is NOT silently
  retired; it routes as evidence to OD-S7 and stays-or-dies with that ruling;
  **`palettes/` (84 files / ~9k LOC) goldilocks adjudication** + a demo long-dir census
  (C11's "long-running directories" row, restored); `ActionBarLayer.vue` local
  `useLayerTransition` shim → retire-on-adopt registry row; colocation residue audit
  (confirm, don't churn).
- **Acceptance gates (corrected, §8 G4/S2-2):** grep scoped to the DISSOLVED SPECIFIER SET
  (the 19 `demo/ui/*` barrel families) = 0 — `shared/ui` is named-exempt (the old
  `from "../ui/|/ui/` pattern collided with a legitimate dir and could never pass); direct
  `from "reka-ui"` = 0; `clsx|tailwind-merge` import grep under demo = 0; demo typecheck +
  build green; palettes/ adjudication delivered as a decision record (agglomerate/keep,
  with rationale), not silent.
- **π/DELTA (§8 G5/S2-15):** **NO screenshot DELTA — no visual claim is made.** The barrel
  dissolution rewrites import specifiers onto the identical glass-ui modules the barrels
  already re-export; render identity is by-construction, and buying screenshot pairs for a
  by-construction claim violates probe parsimony (C21). Fallback named per skeptic 1: if
  any thrice seat contests the by-construction argument, the freeze protocol
  (prefers-reduced-motion + seeded PRNG) is the stated capture discipline — never
  "non-noise" by vibes.
- **Dependencies:** F-01 (B3 brief); glass-ui relay answered for the type export (or a
  typed local alias documented as the tracked interim — NO silent shim).
- **Model routing:** Opus import rewrites; Fable type-leak closure + shared//palettes
  adjudications.

### F-PC-structure-07 | keyframes.js | THE KF STRUCTURE PACKET — ONE SPEC-DISPATCH (P4.5), internally sequenced
- **Intent:** everything kf-structural in ONE letter (was four dispatches with 3× repeated
  fence blocks, §8 S2-10/P4): enforcement BEFORE churn, then the flatten, then the
  restructure — self-contained preconditions so no phase depends on a phantom program
  (§8 K3.4). AMENDS the ratified LT blueprint; names **LT-10** (kept `internal/` against a
  40+-importer census — the owner's edict supersedes the taste ruling; the cost census
  stands) and **LT-16** superseded (P4.4 G5′).
- **Phase 0 — in-letter preconditions (the P3.3 sequencing law, embedded as gates for the
  kf implementer, NOT external dependencies):** land the W9 quartet (staged at
  `v/w9-staging@b920b190`, confirmed extant) · wire MR2/MR4 · CI-wire `lint` +
  `proof:structure` (confirmed in NO workflow today). The arm escalation row (§1.10)
  stands in parallel — if an apparatus program forms, it adopts these rows and the packet
  cites it; either way the packet is executable.
- **Phase 1 — enforcement first (§8 S2-7):** R-ISO(kf) test-mirror rule + support-dir
  allowlist `{_root, characterization, demo, fixtures, support}` + **boundary-cohesion's
  clamp-single-site invariant folded into proof:structure BEFORE that file's anchors move**
  (P3.3 row 5) — all path-parametric (single path constant, exactly the existing depcruise
  `internal/` key idiom) so they are authored ONCE and survive the flatten. Honest framing
  kept: R-ISO(kf) is born-GREEN-on-tree (12 test dirs already mirror), proven fallible by
  selftest + a planted wrong-zone file going RED — not contrived-RED.
- **Phase 2 — the flatten:** `src/animation/** → src/**` with all 13 config/gate anchors
  RE-DERIVED at execution HEAD (§7.3; the sweep's re-derivation at `81a56990` already found
  3 vite drifts :41→:37, :156→:153, :229→:227 — N-ADJ-3 vindicated); the ~340-external +
  648-intra-line churn ledger; frozen-surface fences stated ONCE (exports `.` + `./engine`;
  `./engine` sources `public.ts` — vite.config.ts:172 re-verified; atlas `MorphSVG` live);
  depcruise key-move list (:84, :93, :171). **The anchor-census gate stands, born-RED
  (§8 S2-4 DISPROVEN):** L1 §4 mandates the flatten "born-RED at every anchor" VERBATIM,
  and the gate's catch class is real — a PARTIAL move (12/13 anchors retargeted) stays RED,
  and post-flatten it is a standing guard against any path regressing to `src/animation/*`;
  its GREEN additionally requires the triple verify (44-key engine mirror re-executed +
  `proof:structure --selftest` + full depcruise `lint`) + zero packed-surface diff.
- **Phase 3 — restructure + riders:** `internal/` per the OD-S1 ruling (amended
  recommendation, §5) with the leaf-law key re-keyed (`.dependency-cruiser.cjs:171` moves
  with the rename, P4.1); `engine/play-lifecycle` 552L/5-file recombine (P3.1); kf-side
  name-strip census (src is already R1-guarded; demo census = 0 hits, verified).
- **Demo rider:** `transport/` (~30 files) goldilocks re-cut spec + scene-registry
  colocation audit, **gated on kf `test:demo` (MR4) — the right oracle for playback/RAF
  logic; no screenshot** (§8 G6); the vacuous "proof:structure untouched-green" gate is
  DELETED (its default scope is the library corpus and cannot see demo —
  scripts/gates/structure/index.mjs:37-43,69-70 verified; extending `--scope=demo` is noted
  as a kf-owned option, §8 G3); per-scene `document.title` CEDED to F-PD-frontend-14;
  viewport-fit + non-cube multi-touch → PD handoff rows by name.
- **Acceptance (program-side):** DONE = the packet rowed in kf's inbox ledger (auditable
  dispatch), NEVER "landed" — landing is the kf successor's; OD-S4 governs any direct-edit
  grant. **π/DELTA:** none program-side.
- **Dependencies:** F-02 (the spec+vectors it embeds, checksum-pinned); PF-04 zone-docket
  verdicts RULED (never flatten dying code); OD-S1 ruled.
- **Model routing:** Fable packet authorship + thrice; Opus churn (kf-side, per OD-S4 mode).

### F-PC-structure-08 | program-wide | STRUCTURE CLOSE: CONVERGENCE + LEDGER HANDOVER + ZERO-DROP WITH EXISTENCE CHECK
- **Intent:** close the way the amended charter demands: batch convergence, the ledger
  handed to the real crossing, and a zero-drop audit that can actually fail.
- **Deliverables:** per-batch convergence records (two consecutive clean passes, 5 batches);
  the boundary-delta ledger FINAL → **F-PF-governance-07** (rows: the 10 `./transform`
  symbols · quantize demote + `./quantize` drop · `collectDeclarations` trim · any renamed
  public symbol — each tombstoned §7.1); glass-ui relay letters logged; the zero-drop audit
  vs §6 **with the recipient-existence check: every handoff row must cite a file/wave id
  present in the arm corpus** (the check that catches the phantom apparatus program — a
  handoff to an unresolvable name IS the silent drop, renamed; §8 K3.4); registry rows for
  every partial (terminal dispositions only); the color/ joint-rider row confirmed live
  with its PB trigger.
- **Acceptance gates:** no batch below 2-clean-passes; the only remaining REDs are the
  NAMED transfers (stylesheet R4 → PA; ledger rows → PF-07 pending the crossing);
  zero unlanded §6 rows; recipient-existence check = 0 unresolvable names; F-06/F-07
  declared artifacts (adjudication records, dispatch row, move ledgers) exist on disk
  (declared-captures-missing is a close-class lie).
- **Dependencies:** all prior waves. **π/DELTA:** aggregation only.
- **Model routing:** Fable close adjudication.

---

## §5 — OWNER-DECISION DOCKET (amended)

| ID | Decision | Options + pricing | Recommendation |
|---|---|---|---|
| OD-S1 | kf `internal/` target shape (owner mandate: RESTRUCTURE, not prune — L1 §4) | (a) distribute the 9 leaves to nearest-consumer zones + tiny shared residue; (b) rename to a first-class primitives home (leaf law re-keyed); (c) split by affinity: scheduler/raf leaves → engine-adjacent, errors/reduced-motion → constants-adjacent. Pricing = LT-10's 40+-importer census (stands) + measured fan-in 11 | **(c) — the only option that IS a restructure**; a bare rename re-labels the disliked bucket (the no-alias edict's spirit, §8 S2-12); (b) priced as the cheap fallback if the owner wants only the name gone. **Isomorphism rider:** value's `foundation/` (2 files, 126 LOC) is the same abstract facility — the F-02 spec's naming prose covers both so the twins never diverge (pairing noted, not a forced co-decision — 126 LOC does not buy a second docket row) |
| OD-S2 | `transform/decompose.ts` PRUNE vs decided kf-adoption wave | prune = −609 LOC, 0 consumers on all four trees (re-verified); adopt = new kf surface with zero demonstrated demand | PRUNE, tombstone RIGHTLY — **executed as an F-03 ledger row at the PF-07 crossing** (the 10 `./transform` symbols are a surface cut, never pre-boundary; §8 K1) |
| OD-S3 | ~~api EXTRACT vs STAY~~ | **MERGED into PE-api's OD-api-1** (same decision, one docket row, PE owns; §8 K3.1) | position of record unchanged: STAY + harden; P3.2 row superseded with a named tombstone |
| OD-S4 | kf wave delivery mode | P4.5 default = spec-dispatch; direct cross-repo edits need explicit grant | dispatch by default (now ONE packet); owner grants direct mode if the kf successor is this same formation |
| OD-S5 | stylesheet.ts R4 tracked-RED carry | live RED (honest signal) until R-PARSER lands vs temporary allowlist (masks) | tracked-RED, named transfer to PA — allowlisting is the masked-fallback lie |
| OD-S6 | `transform/path.ts` (564 > 500) | split (geometry vs parsing/serialization) vs single REASONED allowlist row | split at the natural seam if thrice finds one; else a reasoned allowlist row (keep value's allowlist near-empty) |
| OD-S7 **(new)** | export-surface grammar at the co-land boundary | kf publishes `.` + `./engine`; value publishes 7 subpaths and NO root key (exports map verified) — the largest cross-repo structural divergence, and the boundary is the only cheap harmonization moment (§8 S2-13). Evidence of live pain: the demo hand-copied `debounce` because the bare specifier dragged a ~36KiB chunk (shared/utils.ts header, read this session). Options: (i) status quo ratified as RATIFIED-DIVERGENCE; (ii) value adds a root key alongside subpaths; (iii) kf-style root+domain doors both sides | decision row ONLY (no wave — KISS); feeds the F-03 ledger; resolves the debounce-copy row (F-06) either way |
| OD-S8 **(new)** | router architecture, both demos | BOTH route through render-null `Stub` components with no `<router-view>` (value color-picker/router/index.ts:19; kf app/scene/router.ts:15 — both verified this session): routes as a navigation model driving a pane/scene machine. Ratify as the pair's DEFINED shared idiom vs condemn and regularize (§8 S2-14/D2) | ratify the stub-navigation-model as the shared pattern (it is deliberate, documented in-file, and isomorphic across the pair) — PD-frontend builds C6 URL-state/share on the ratified idiom; condemnation would be churn without a defect |

---

## §6 — THE LANDING TABLE (every letter/packet/addenda row → where it lands; amended rows marked ►)

| Row | Lands |
|---|---|
| L1 §4 kf flatten + internal/, born-RED anchors, re-derive (N-ADJ-3) | F-07 (phases 2–3) |
| L1 §4 structure governance: AMEND LT, name LT-10/LT-16; internal/ OWNER-DECISION | F-07 (amendment), OD-S1 |
| L1 §4 subpaths dissolves — 7 keys frozen, explicit specifiers (D50), drops `./quantize`, real transform/index.ts, packed re-verify | ► F-04 (dissolution, corrected per-key spec); `./quantize` drop = F-03 ledger row @ PF-07 |
| L1 §4 value hygiene: decompose PRUNE · quantize DEMOTE · api EXTRACT · 39 PNGs · demo-test displacement · deps STRIP + manifest gate | ► F-03 ledger rows @ PF-07 (decompose/quantize) · PE-api + OD-api-1 (api) · F-04 (PNGs) · F-02 (displacement) · F-03 + PF-02 single text (deps + gate, PA whitelist folded) |
| L1 §4 goldilocks; strip module names; glass-ui reference | ► F-05 (+ census-first strip rider, `.vue` exempt), F-02 spec prose |
| L1 §4 tests-isomorphism born-RED BOTH repos | F-02 (value, born-RED + cured in-wave); F-07 phase 1 (kf; honest born-GREEN + selftest-fallible) |
| L1 §4 demo colocation | F-06 (audit-confirm); F-07 demo rider (kf) |
| L1 §4 Aristotelian audit · suffusion · glass batching · W53 plate | HANDOFF → program-PD-frontend.md (exists) |
| L1 §6 / P4.2 one co-land boundary; internal-until; STRIP-not-bump | ► Charter §1.4 (ledger law); F-03; executed by F-PF-governance-07 |
| L1 §7.1 tombstone law + capability-diff gate | ► F-03 tombstones; gate owner = F-PF-governance-02 (named; no phantom) |
| L1 §7.2 pins · §7.3 re-derive · §7.4 labels | Header + every wave; F-07 phase 2 re-derivation |
| L1 §8 / C20 DAG-thrice, dynamic clusters | ► §3 (5 agglomerated batches, seat ledger); F-01/F-08 |
| P3.1 internal/ row + play-lifecycle shrink | F-07 phase 3 + OD-S1 |
| P3.1 zone verdicts, presets breadth, emit-shrink, zone-test binding, fence annex | ► HANDOFF → F-PF-governance-04 (the REAL zone-docket owner; "zones program" was unresolvable, §8 ADJ-1); F-07 depends on its rulings |
| P3.2 decompose · quantize · subpaths · api · deps · PNGs/demo-tests · demo restructure | ► F-03/F-03/F-04/PE-api/F-03/F-04+F-02/F-06 |
| P3.2 color RESTORE / css parser REWRITE | ► HANDOFF → program-PB-color.md / program-PA-parse.md; F-02's gate + door rule is the interface (no co-signed doc; PA-03 owns the css cut); color/ final cut = F-05's deferred joint rider |
| P3.3 rows 1/2/7 (W9 · CI-wire lint+proof:structure · MR2/MR4-before-churn) | ► F-07 phase-0 in-letter gates + the §1.10 arm escalation row (apparatus program is a phantom in this corpus) |
| P3.3 row 5 clamp fold-then-die | F-07 phase 1 (BEFORE the churn, §8 S2-7) |
| P3.4 row 3 isomorphism gate + displacement | F-02 |
| P3.4 row 4 manifest gate | ► F-03 + F-PF-governance-02 single text |
| P3.4 rows 1,2,5 (producer+api scope, e2e set, bench) | ► HANDOFF → PF/PA where real (bench → PA-02); e2e adjudication → the §1.10 escalation row (PF routes it to the phantom apparatus — flagged, not silently re-dropped) |
| P3.4 row 6 capability-diff | F-PF-governance-02 (F-03's ledger witness) |
| P4.1 fences | F-07 fence block (stated once) |
| P4.3 flatten census | F-07 phase 2 (re-derived; 3 vite drifts caught) |
| P4.4 G5′ | F-07 governance clause |
| P4.5 ownership protocol | Charter §1.5; OD-S4; F-07 = the ONE packet |
| P5 LT rows · D50 · oscillator doc-truth · W53/W55/W56 · D54/SCI-1 | F-07 amendment targets · F-04 · HANDOFF → PF-04 rider / PD / PB |
| C5 shadcn abrogation TOTAL | ► F-06 (WIDENED to idiom census: barrels + config + type-leaks + `cn()` retire + devDep drops; prototype branch EMPTY-SET on evidence); F-07 confirms kf demo clean |
| C6 routing + URL state | ► OD-S8 (the structural ruling THIS program owes) + HANDOFF → PD (URL-state/share on the ratified idiom) |
| C7 palette API | ► PE-api whole (spec, STAY, structure, fourier isomorphism) + OD-api-1; **kf side = explicit N/A row (kf has NO api/ dir — PE census-proven)** |
| C8 mega-tranche scale | Charter §1.1 |
| C11 structure edict | ► the program entire; palettes/ long-dir adjudication RESTORED (F-06); name-strip as census-first rider (F-05) |
| C16 isolation · C17 routing | Header; §1.8; per-wave notes; every seat reports model_served |
| C20 thrice | §3 (budgeted); F-01/F-08 |
| C21 KISS/probe parsimony | Charter §1.7; F-06's no-visual-claim ruling; the thin opener |
| C2/C3/C4/C10/C22 | HANDOFF → program-PD-frontend.md (anchors already mined in the sweeps) |
| C1/C12/C13/C14/C15/C19 | HANDOFF → PA/PB |
| C18/C23 meta-arms | Out of program (apotheosis arms) |

## §7 — NAMED OUT-OF-PROGRAM HANDOFFS (zero silent drops; every recipient existence-checked)

1. **program-PD-frontend.md:** C2, C3, C4, C10, C22, Aristotelian audit, glass batching,
   W53 plate, C6 URL-state (on OD-S8's ratified idiom), kf viewport-fit + non-cube
   multi-touch, per-scene title (F-PD-frontend-14 owns it).
2. **program-PA-parse.md:** css/ internals whole (F-PA-parse-03's own deliverable text);
   stylesheet.ts R4 tracked-RED (OD-S5); bench resurrection (PA-02); the manifest-gate
   whitelist rule folded into the single PF-02 text.
3. **program-PB-color.md:** the restore ladder into `src/color/` as-is; the anchors.ts
   split rides F-05's deferred joint rider at PB's landing trigger.
4. **program-PE-api.md:** the api slice whole + OD-api-1; receives F-01's api gap-graph +
   F-02's structure-grammar spec as inputs.
5. **program-PF-governance.md:** F-PF-governance-02 (capability-diff + manifest gate + deps
   strip CI seat) · F-PF-governance-04 (zone docket — F-07's verdict dependency) ·
   F-PF-governance-07 (the co-land crossing — executes the F-03 ledger).
6. **ARM ESCALATION (owner):** the apparatus program (P3.3 rows 1/2/7 beyond F-07's
   in-letter gates; the e2e per-oracle adjudication; lighthouse demote) exists in NO armB
   file — form it or PF adopts the rows. Until ruled, F-07 is self-contained by
   construction and nothing in this program depends on the phantom.

---

## §8 — ADJUDICATION LOG (every skeptic claim → PROVEN(adopted)/DISPROVEN(evidence); all evidence re-derived this session)

**Skeptic 1 (S1):**
- **K1 — F-04⊥F-05 unschedulable under the frozen-surface law; manifest omits the decompose deltas → PROVEN.** My evidence: `src/subpaths/transform.ts:7-24` re-exports 6 runtime + 4 type symbols from `../transform/decompose` through the frozen `./transform` key (read whole); the exports map carries exactly the 7 keys (`git show HEAD:package.json`); original charter §1.4 + F-04's "same symbol set" gate + F-05's pre-boundary prune/demote cannot coexist; F-04/F-14's manifest lists named only `./quantize` + `collectDeclarations` — the 10 dying symbols appeared in NO row, reproducing the no-advocate⇒no-tombstone mechanism verbatim. **Adopted:** charter §1.4 rewritten as the ledger law; F-03 authors the COMPLETE ledger; execution at F-PF-governance-07; F-04/F-05 keep only internal moves, pack-parity-witnessed.
- **K2 — the "no runtime deps" manifest gate blocks the parse-that decree → PROVEN.** L1 §2/P1.6: parse-that READOPTED as a registry runtime dep ("re-add `@mkbabb/parse-that` per value's historical pin convention"); program-PA-parse.md F-03 deliverables carry "the manifest-gate WHITELIST rule … must whitelist it, never strip it" (read directly). **Adopted:** allowlist spec `runtime deps ⊆ {@mkbabb/parse-that}`, one text, PF-02 CI seat.
- **K3.1 — api double-owned with PE → PROVEN.** PE-api read: 11 waves; charter claims both backends' structure treatment; F-PE-api-02 executes STAY + hardening; F-PE-api-08 executes the 125-file re-cut; OD-api-1 duplicates OD-S3. **Adopted:** slice ceded whole; OD-S3 merged; old F-07 deleted.
- **K3.2 — css "contract co-signed by the parser program" has no counterparty → PROVEN.** grep of PA for co-sign/contract/skeleton = 0 hits; PA-03 claims the internal re-cut itself ("the 899-LOC stylesheet god-module does not survive as one file"). **Adopted:** css cut ceded to PA; the binding interface = F-02's ported gate + door rule (a gate, not a doc awaiting an unbooked signature).
- **K3.3 — deps strip + manifest gate double-owned with PF-02 → PROVEN.** program-PF-governance.md:70-80 read: PF-02 = "capability-diff + manifest gate + deps strip". **Adopted:** single gate text, PF-02 owns the CI seat, F-03 executes the strip in coordination.
- **K3.4 — the kf gates dependency terminates in a phantom → PROVEN.** armB ls: exactly six programs (PA,PB,PC,PD,PE,PF); PF names "apparatus" as a sibling (:11) and routes P3.3/e2e rows to "[SIBLING:apparatus]" (:364-369) — no such file exists. **Adopted:** F-07 phase-0 in-letter gates (self-contained) + the §1.10 arm escalation row + F-08's recipient-existence check.
- **K3.5 — title hook double-booked with PD → PROVEN.** F-PD-frontend-14 read: per-scene `document.title` is its born-RED deliverable. **Adopted:** ceded by name.
- **E1 — F-11 contradicted by its own reference model; near-empty census → PROVEN.** glass-ui `button/Button.vue`, `watercolor-dot/WatercolorDot.vue` ls-verified; dir-prefix stutter census run this session: value src = 0 (full file list), value demo = 0, kf demo = 0. **Adopted:** F-11 dissolved into F-05's census-first rider (+ kf side inside F-07); `.vue` exempt by rule, idiom cited in the F-02 spec.
- **E2 — color/ pre-churn violates the program's own "structure-follows-surviving-content" law → PROVEN.** P2.2 lands boundary(604)+gamut(526)+raytrace(137)+okhsl(270)+ΔE+Into+ramp into src/color/ (~tripling 891 LOC); cutting anchors.ts first re-litigates the cut. **Adopted:** deferred joint rider on PB's landing trigger; R1–R6 enforcement meanwhile.
- **E3 — the R4 born-RED trio is discharge theater → PROVEN in substance.** stylesheet→PA transfer, decompose→deletion (now ledger row), path→possible allowlist: none cured by this program's structural work; the honest program-owned RED set is the R-ISO family. **Adopted:** F-02 labels ownership at birth; R-ISO is the in-wave RED→GREEN; the R4 rule still ports whole (the rule is right; the theater was in claiming its discharges).
- **Gate inventory G1–G8 → PROVEN (7 of 8; one moot).** G1 F-01 escape hatch (tightened: enumerated per-file delta tied to landed commits); G2 F-07 escape hatch (moot — wave ceded); G3 F-13 "proof:structure untouched-green" vacuous (VERIFIED: scripts/gates/structure/index.mjs default scope = library corpus, demo scope authored-but-unselected `--scope=demo`, :37-43,:69-70,:95 — gate deleted, `test:demo` is the witness); G4 F-12 grep cannot pass (VERIFIED: `demo/shared/ui/{EmptyState,PaneHeader}.vue` exists, 16 files reference `shared/ui` — gate re-scoped to the dissolved specifier set, shared/ui named-exempt); G5 "non-noise" DELTA unoperationalized (adopted via the S2-15 resolution: no visual claim → no captures; S1's freeze protocol named as the fallback); G6 F-13 screenshot = wrong oracle class for transport/RAF (adopted: MR4 `test:demo` gates the rider); G7 F-03 count-diff self-contradictory (adopted: summed cross-project parity + per-file move ledger); G8 capability-diff owner unnamed (adopted: F-PF-governance-02, named).
- **D1 — C11 palettes/ long-dir row dropped → PROVEN** (84 files re-counted). Adopted: F-06 adjudication row.
- **D2 — C6 routing fudged → PROVEN** (Stub idiom verified: value `color-picker/router/index.ts:19` `const Stub = { render: () => null }`, no `<router-view>`; kf `demo/app/scene/router.ts:15` identical). Adopted: OD-S8, this program owes the ruling.
- **D3 — F-07 plans what it doesn't own → PROVEN, moot by cession** (PE-08 is the executor).
- **D4 — C7 "BOTH repos" needs the kf N/A row → PROVEN** (PE pin line: kf has NO api, census-proven). Adopted: explicit N/A row in §6.
- **D5 — the `dist/<dir>/index` retarget is unexecutable for 4 of 7 keys → PROVEN** (value.ts/easing.ts/quantize.ts are src-root files; foundation/ holds math+result — a per-36-LOC-file dir is the banned sand). Adopted: per-key explicit module-file specifiers; `/index` only for real dirs.
- **P1 — F-01 re-derives what exists → PROVEN** (`armB/madge-src.json` 26 files/0 cycles + `depcruise-animation.json` 476KB, both timestamped before the program). Adopted: artifacts adopted; opener thinned + non-serializing.
- **P2 — no thrice budget arithmetic (≥78-seat floor) → PROVEN.** Adopted: 5 batches, 30 baseline / 45 ceiling, stated.
- **P3 — wave-shape census (process majority) → PROVEN in substance.** Adopted: 14→8 waves; merges (02+03), cessions (07), dissolutions (11), one kf packet.
- **P4 — four dispatch letters where one suffices → PROVEN.** Adopted: F-07 single packet, fences stated once.

**Skeptic 2 (S2):**
- **S2-1 ≡ K1 → PROVEN** (same tree evidence; the "same class, two treatments" observation adopted — ALL surface cuts now ride one ledger).
- **S2-2 — F-12 gate unsatisfiable + demo/shared/ never adjudicated → PROVEN** (collision class verified: shared/ui exists, 16 referencing files; the exact 107-line count not re-derived — not load-bearing). Adopted: scoped gate + the shared/ adjudication row in F-06.
- **S2-3 — C5 narrowed to component imports; `cn()` residue → PROVEN** (`demo/shared/utils.ts` = clsx+twMerge `cn()`, sole clsx/tailwind-merge importer, grep-verified; glass-ui exports `cn` at src/index.ts:281, verified). Adopted: idiom census framing; cn retire + devDep drops. **Caveats added by this adjudication:** `reka-ui` devDep STAYS (non-optional glass-ui peer, P4.2); the `debounce` copy is a DOCUMENTED deliberate decision (T.W6.5 header, read) — routed to OD-S7, never silently retired.
- **S2-4 — F-08's anchor-census gate is contrived RED → DISPROVEN.** Evidence: L1 §4 mandates the flatten as "coordinated config-and-graph moves, **born-RED at every anchor**" — verbatim canon, not the program's invention; and the gate's catch class is real: a PARTIAL move (12/13 anchors) stays RED — exactly the N-ADJ-3 drift-class failure the letters memorialize — and post-flatten it stands as a permanent guard against `src/animation/*` path regressions. The C21 "contrived gates" charge fails against a canon-mandated, regression-catching gate. F-07 keeps it, with the catch class stated so it never reads as a to-do list.
- **S2-5 — priority inversion (decree programs wait on wave 6) → PROVEN in effect, cured structurally.** Rather than authoring contracts day one (S2's CS-01 — which would recreate K3.2's unbooked-counterparty defect), the cure is CESSION: PA owns css/ (unblocked at day zero), PB lands into color/ as-is (unblocked; it needs only F-02's test mirrors, which land in the first value wave — PB:390 read: "lands compliant from birth" under the mirrors). Nothing in this program gates R-PARSER or R-DELTAE.
- **S2-6 ≡ E2 → PROVEN.** Adopted (deferred joint rider).
- **S2-7 — enforcement-after-churn inverts the program's own sequencing law → PROVEN.** The flatten churns 265+ test import lines with no mirror lock and the clamp invariant trapped in a moving file; rules are path-parametric (the depcruise `internal/` key idiom). Adopted: F-07 phase order enforcement → flatten → restructure.
- **S2-8 — settled-§2 vs 13 adjudicating clusters; ~117-seat worst case → PROVEN in substance.** Adopted: §2 relabeled OPENING POSITIONS; 5 agglomerated batches (canon-compliant: L1 §8 "agglomerate clusters as fits"; C20 still thrice's every batch); ledger stated.
- **S2-9 — F-01 double work vs §7.3 → PROVEN in part.** Existing artifacts adopted, opener thinned and non-serializing; a thin opener SURVIVES (batch cut + registry + the seven reconciliation rows are real, one-time work) — full deletion of the opener would orphan the reconciliation duty.
- **S2-10 — four kf envelopes → PROVEN.** Adopted: one packet.
- **S2-11 — F-11 phantom half; F-07 plan-only → PROVEN** (census-verified emptiness; PE executor). Adopted: rider + cession.
- **S2-12 — OD-S1's (b) is a rename, not a restructure; decision is paired → PROVEN in main, PARTIALLY adopted.** Recommendation flipped to (c) (the only true restructure; (b) priced as fallback). The "co-decide value's foundation/ as one row" half is adopted as an isomorphism RIDER, not a second docket row: foundation/ is 2 files / 126 LOC — a forced pairing row is ceremony (C21); the F-02 spec's shared naming prose is the anti-divergence mechanism.
- **S2-13 — the export-grammar divergence has no row → PROVEN** (kf `.`+`./engine` verified from package.json; value 7-subpaths-no-root verified; the debounce hand-copy pain documented in shared/utils.ts's own header, read this session). Adopted: OD-S7, decision-row-only, feeds the F-03 ledger.
- **S2-14 ≡ D2 → PROVEN** (both routers verified). Adopted: OD-S8 with a ratify-lean recommendation (the idiom is deliberate, in-file-documented, and already isomorphic across the pair — condemnation would be churn without a named defect).
- **S2-15 minors → PROVEN, all adopted:** vectors homed in value.js + checksum-pinned in the F-07 packet; F-12's DELTA dropped (no visual claim; freeze protocol as the named fallback — reconciling S1-G5 and S2); F-02/F-03 merged into one motion; the F-04→05→06 serialization collapsed by the ledger design.

**Adjudicator-original (ADJ):**
- **ADJ-1 — "zones program" and "release program"/"gates program" were unresolvable handoff names beyond K3.4's finding:** the real owners exist and are now named — zone docket = F-PF-governance-04, co-land crossing = F-PF-governance-07, publish-integrity gates = F-PF-governance-02 (all read this session). Every §6/§7 row now cites an existing file/wave id; F-08 checks it mechanically.
- **ADJ-2 — the grammar→anchors leak heal was itself pre-churn of the dying parser** (grammar.ts dies with R-PARSER): resolved as the one-line reroute (harmless) PLUS the door RULE in the gate, which is what actually binds PA's restored tree.
- **Convergence note:** the two skeptics' counter-programs (PC′ 8 waves / CS 9 waves) agree on every structural cure adopted here — one ledger, one kf packet, cession over co-signature, census-first stripping, scoped demo gates, budgeted thrice. Their one direct conflict (S1: gate-is-the-contract vs S2: contracts-first day one) is resolved by cession, which satisfies S1's counterparty objection AND S2's unblocking motive more strongly than either counter-proposal. The one skeptic claim rejected outright (S2-4) is rejected on canon text + catch-class analysis, with the clarification folded in so the gate can never be read as theater.

— end of FINAL program PC-structure (armB, independent Fable formation; 8 waves; thrice-adjudicated).
