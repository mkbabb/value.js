# THRICE — PF-GOVERNANCE — SKEPTIC 2 (assume WRONG; attack architecture + parsimony)

**Seat:** armB thrice loop, skeptic 2 · 2026-07-19 · model verified from own system prompt:
`claude-fable-5`. **Target:** `armB/program-PF-governance.md`. **Evidence base:** the two armB
sweeps (consumed whole), L1/L2/addenda, plus two fresh probes run this seat (`git show
v3.1.0:package.json`; the v4.0.0 CHANGELOG section) — cited inline. Firewall honored (no
vnext / snapshot-vnext / armA).

**Verdict up front:** the program's FACTS are sound (its sweep-anchored claims re-verify), but
its ARCHITECTURE is wrong in three load-bearing ways: (1) a terminal-ratification inversion
that makes its own boundary wave circular and structurally forbids the ~2.4k OWNER-DECISION
LOC from ever shipping inside the one-boundary law; (2) a C21 violation at program scale —
6 of 8 waves are paper, born-RED doctrine is misapplied to prose, and standing law is
re-encoded as deliverables (the parallel-authority disease it forbids kf-side); (3) concrete
gate-design defects and at least three mis-homed/undispatched canon rows. The counter-shape
is 3 waves + charter matter.

---

## §1 — FATAL: the terminal-ratification inversion (three expressions of one defect)

**1.1 The docket circularity is on the program's own face.** §3 header: the docket "surfaces
via F-PF-governance-04, ratifies via **-08**." But wave 07's dependency line reads "-04
(**docket ratified** — prunes are boundary cargo)" — and 08 is terminal, depending on ALL
waves *including 07*. So 07 requires a ratification that, per the program's own §3, only
happens after 07. Circular. Either the dependency note on 07 is a lie or the §3 ratification
placement is; both cannot stand.

**1.2 The OWNER-DECISION LOC can never ship.** Wave 04: prune riders "EXECUTE only
post-owner-ratification." Ratification = 08 (post-07). The boundary (07) is decreed "LAST
among breaking changes" and P4.2 permits exactly ONE crossing. Therefore flip, draw-svg,
physics/morph, scroll, ingest (~2.4k LOC, docket OD-1..5) — if the owner rules PRUNE — have
no legal vehicle: they missed the only boundary and the next major is forbidden. The program
converts owner decisions into dead letters by construction. (The unilateral riders escape only
because the program quietly lets the structure program execute them pre-ratification —
"structure program executes, governance carries the tombstone" — which contradicts wave 04's
own execute-only-post-ratification clause. A second internal contradiction.)

**1.3 Go/no-go after the go.** 08's deliverables include "the boundary go/no-go memo"; 08
depends on 07. A go/no-go ruled after the crossing it governs is ceremony, not a gate. The
root cause of all three: stuffing every adjudication into a terminal close wave. **The cure
is structural, not editorial: the docket ratifies BEFORE any dependent work (wave-0 exit
gate), and go/no-go is 07's ENTRY gate.** No amount of wording inside the 8-wave shape fixes
this; the shape is wrong.

---

## §2 — PARSIMONY: the program is paper wearing a wave costume (C21, addenda-WIN)

C21: "Little time on contrived gates or process; the majority on direct code implementation…
and VISUAL verification." Score the 8 waves: only **02** (gates + strip) and **07** (the
crossing) execute anything. 01, 03, 04, 05, 06, 08 produce markdown: PIN-REGISTER.md, a
protocol doc, a docket sheet, a batch ledger, RELEASE-TRAIN.md, an audit memo. 6/8 paper,
and under C8's 50+-wave floor this program spends ~16% of the mega-tranche's wave budget on
coordination prose. Specific counts:

**2.1 Wave 01 is sand.** A pin table + a re-derivation template is the first hour of
formation bookkeeping, not a wave. Worse, its "born-RED" gate — "RED = a wave spec citing
any of the three drifted vite anchors" — is a **documentation-freshness condition**, not a
defect gate: it goes red whenever any pinned tree advances (glass "advances quickly" per the
program's own §1), i.e., it is permanently re-reddening by external drift and can be turned
green by editing prose. That is the misuse of born-RED doctrine (born-RED = live defect in
the artifact, cured by the wave's work). And the re-derivation law is ALREADY standing law
(L1 §7.3, N-ADJ-3) — codifying standing law as a wave deliverable is double-booking canon.

**2.2 Wave 06 creates a parallel authority.** RELEASE-TRAIN.md's contents are, item by item,
already canon: C15 (parse-that ≤1.1), P4.1.3 (immutable tags), D54/W56 (4.1.x before the
boundary, never forked), P3.3 row 7 (MR2/MR4 first), P0.2 row 4 (no atlas catch-up), P4.2
(internal-until-boundary). Re-encoding decided canon into a new normative doc is precisely
the G5′ disease the program forbids kf-side ("AMEND the ratified blueprint… never a parallel
authority") — now with two places for the same law to drift apart. The ONE novel item in 06
is the glass RANGE-WIDENING recommendation — that is a docket row (OD-12) plus a glass
dispatch, ~10 lines, not a wave.

**2.3 Wave 05 books a standing invariant as a wave and legislates over siblings.** Glass
batching is a standing owner edict (2026-07-12, restated L1 §4) — an invariant to cite, not
a wave to execute. The wave's remaining deliverable is "the dispatch format" — process sand.
Its π/DELTA clause then imposes capture obligations on the *frontend program's* prototypes:
dual bookkeeping for one capture set, which is itself a close-lie vector (two ledgers, one
truth). The frontend program owns its π; governance owns only the channel row.

**2.4 Wave 08 is the tranche's close, not a program's.** The zero-drop row-inventory audit
against P0–P6 + C1–C23 is L1 §9's FORMATION/close obligation for the whole tranche. A
governance-program-local close wave that "audits sibling receipt" across all programs is the
tranche close ceremony wearing a program badge — and it drags the docket and go/no-go to
terminal position, causing §1. Delete the wave; keep the audit where it belongs.

---

## §3 — RE-LITIGATION of a decided row

**OD-10 re-asks what the addenda already ruled.** C7 (later, addenda-WIN): the palette API
"stays at its current `/api` location." The addenda note requires the extract/stay tension be
*reconciled explicitly* — i.e., paper the supersession of P3.2's EXTRACT row with rationale —
not re-presented to the owner as an open ask ("or re-rule"). Putting a decided row back on
the docket violates the program's own INPUT-not-open-questions charter line and L1 §7's
re-booking prohibition. Correct form: a supersession record in the registry (P3.2 EXTRACT →
RETIRED, superseded by C7), zero owner cost.

---

## §4 — GATE-DESIGN DEFECTS (fresh probe evidence)

**4.1 The birth certificate cannot run as spec'd.** Wave 02a defines the census as "the
built public surface (**the 7 frozen subpath keys**) against the prior published tag," and
RED-1 demands the retro-run v3.1.0→v4.0.0. Probed this seat: `git show v3.1.0:package.json`
— v3.1.0's export map is a **different topology**: a root `.` key + `./parsing` + `./units`
(+ color/math/easing/transform…, `main`/`types` present). A census script hard-shaped to the
7 v4 keys cannot express the very extinction it must certify (the root barrel, /parsing,
/units are where the deposed capabilities lived). The gate must be **tag-shape-agnostic**:
derive each side's key set from that tag's own export map (cleanest: census the published
npm tarballs, `npm pack @mkbabb/value.js@3.1.0` vs `@4.0.0` — no need to build a dead tree).
As written, RED-1 is unimplementable.

**4.2 The fired instance stays unpapered forever — the law's own scene of the crime.** 02's
GREEN is "strip landed + both gates CI-wired + capability-diff green-with-tombstones **on the
next cut**." So the v3.1.0→v4.0.0 RED is never cured: the program mechanizes the tombstone
law forward and leaves the one place it FIRED (L1 §7.1: "value.js first — it is where the
mechanism fired") without paper. Probed this seat: the v4.0.0 CHANGELOG's Breaking section
names ZERO D-ledger families by name — the whole extinction hides under "raw color-conversion
exports, and other root-barrel internals," plus the indicted continuity line ("`/color` owns
… gamut mapping"). And the repo has **prior art for exactly the missing deliverable**: the
3.1.0 entry (directly below) retroactively corrected the dishonest 3.0.0 record in-place —
"the pushed `v3.0.0` tag is immutable and left untouched — the CHANGELOG correction is the
honest record." The program omits the v4.0.0 CHANGELOG tombstone BACKFILL (14-row D ledger,
by-name RIGHTLY/UNJUSTLY/UNCLEAR). That backfill is the birth certificate's cure and the
law's actual discharge; without it the gate is a monument, not a mechanism.

**4.3 The manifest gate's blocklist rots.** 02b: fail on `dependencies ∩ {self, glass-ui,
keyframes.js}`. But R-PARSER legitimately RE-ADDS `@mkbabb/parse-that` as a runtime dep
(L1 §2: "re-add @mkbabb/parse-that per value's historical pin convention") — so `dependencies
== ∅` is not the invariant, and a 3-name blocklist silently passes any OTHER accidental
runtime dep (vue, atlas, a tailwind plugin…). The KISS invariant is an **allowlist**:
`dependencies ⊆ {@mkbabb/parse-that}` — anything else red. One line, cannot rot.

**4.4 Prerelease semantics break the crossing-window story.** 06d claims range-widening
"lets both sides of the boundary install during the crossing window," and 07's RED matrix
installs "a value@5 **prerelease**." npm range semantics: `^4 || ^5` does NOT admit
`5.0.0-rc.x` (prereleases satisfy a range only when the range names a prerelease of the same
tuple). So the widened peer cures nothing for the prerelease-based matrix — RED stays red
until the FINAL 5.0.0 exists, and the matrix as designed cannot distinguish "wedge priced"
from "prerelease semantics." Either the matrix crosses on finals only, or the widen must
include a prerelease-admitting range for the window. The program prices the wedge and then
mis-measures it.

**4.5 The deps bomb waits on a pin register.** The committed `dependencies:{glass ^7, kf ^6}`
block (sweep §4: in-repo at HEAD, sharper than the letters) is a live publish bomb; the strip
is a one-line edit. The program parks it behind wave 01 (a paper wave) via 02's dependency
edge. When the defect is a live bomb and the cure is one line, KISS ordering is: defuse
first, paper later. Mis-priority on the program's own evidence.

---

## §5 — SILENT DROPS AND MIS-HOMINGS (checked against the program's own §5 landing map)

**5.1 The boundary CENSUS LOCK gate is never dispatched.** L1 §2: kf-side work "is the
boundary CENSUS LOCK: **zero grammar productions on both sides**, with ONE live row." The
program dispatches the one row's adjudication (03d) but no durable LOCK — no kf-side gate
(grep/depcruise rule) preventing future grammar productions from re-growing. The lock is the
governance-shaped half of §2's kf mandate; the landing map has no row for it. Silent drop.

**5.2 C12's kf-side WAAPI uplift must ride the channel governance owns — and doesn't.**
"kf gains full WAAPI support" is a MAJOR kf library item; per P4.5 (which wave 03 enshrines
as the SOLE channel) it lands as a value→kf dispatch. The initial dispatch pack (03a–e) and
the §4 cross-repo map omit it entirely; §5 waves it at "[SIBLING:parser/color/API]" — none
of which can touch kf. The sweep even priced the gap (sweep-kf-lib §5: persist(),
pseudoElement, iterationComposite, getAnimations, registerProperty, updatePlaybackRate,
per-keyframe composite, gate liberalization — all measured ABSENT). A program whose charter
is "every sibling's kf items ride THIS channel" forgot the largest kf item in the addenda.

**5.3 C4 is mis-homed in a way that contradicts wave 03.** The landing map routes C4 (kf
demo mobile rebuild) to [SIBLING:frontend]. But kf's demo/UI corpus is **kf-owned** (P4.5,
restated verbatim in wave 03's intent); a value-side frontend program cannot execute it
without an owner grant. C4's correct landing is a dispatched spec into kf's inbox (or an
explicit owner-grant row). As mapped, the program violates the ownership protocol it just
formalized.

**5.4 (minor) 05 is absent from 07's dependencies** though the glass peer move (06/07 cargo)
must clear 05's batch-release criteria; and the addendum-2 kf regex CENSUS (P1.4's "also a
kf regex-census row") has no dispatch of its own — it free-rides inside 03d's brief without a
row, which is how rows get lost.

---

## §6 — THE COUNTER-PROGRAM (3 waves + charter matter; every §1–§5 defect dissolved)

**G-0 — CHARTER MATTER, not waves** (lives in the tranche plan folder; zero acceptance-gate
theater): the G0′ pin register + re-pin stamp in the wave-spec TEMPLATE; the anchor
re-derivation procedure (already law — cite, don't re-legislate); the G5′
amend-never-parallel rider; phase labels; the standing glass-batching invariant + channel
constants; the registry with the defect-FAMILY axis (P1.5). The train laws (C15 cap,
immutable tags, W56-before-boundary, MR2/MR4-first, no-atlas-catch-up, W55 owns .github)
are CITED as canon in the specs they bind — no RELEASE-TRAIN.md.

**W-GOV-1 — Publish integrity, defused NOW** (first executable wave of the tranche; no
dependencies). Intent: strip the committed deps block immediately (one commit); the manifest
gate as dependency **allowlist** (`⊆ {@mkbabb/parse-that}`) + packed-surface check; the
capability-diff gate built **tag-shape-agnostic** (census from published tarballs); run the
v3.1.0→v4.0.0 birth certificate AND cure it in the same wave — the v4.0.0 CHANGELOG
tombstone backfill (14-row D ledger, by-name RIGHTLY/UNJUSTLY/UNCLEAR; Fable adjudicates the
taxonomy), on the repo's own 3.1.0 correction precedent. Born-RED honestly: both defects
(bomb + unpapered extinction) are live today, and both GREEN inside the wave.

**W-GOV-2 — The coordination pack, ratified at the FRONT** (the docket + dispatch wave;
depends on nothing but formation output). Intent: assemble the owner-decision docket (§3
rows OD-1..9, OD-11..13, VIS-1 — OD-10 becomes a supersession RECORD per C7, off the docket)
and obtain **ratification as this wave's exit gate** — before any dependent sibling work;
author the full kf dispatch pack in one sitting: capability-diff spec (with mutation
selftest), kf-7 co-land spec, scar-deletion trigger, easing consume-vs-ratify brief, **the
boundary census-LOCK gate spec**, **the C12 WAAPI-uplift spec**, **the C4 kf-demo-rebuild
spec** (or its owner-grant row), the proof:publish consumer-claims rule, the oscillator
record correction; the glass batch ledger (marks register folded, three release criteria)
and the atlas chase ledger. Every dispatch rowed; unilateral prune riders (split-text,
motion-path, oscillator, decompose, quantize-demote) attached with census cites and
zone-test bindings. Owner-decision prunes become determinate HERE — in time to be boundary
cargo.

**W-GOV-3 — The co-land boundary** (terminal among breaking changes; depends on W-GOV-1/2 +
sibling terminal waves). Intent: the single crossing, with the go/no-go as its **ENTRY
gate** (owner rules on the ratified docket + sibling state BEFORE the freeze); the wedge
price sheet and chase ledgers re-derived at execution; the ERESOLVE matrix run on **final
versions** (or prerelease-admitting ranges for the window — the semantics priced, §4.4);
capability-diff green-with-tombstones on BOTH cuts; 44-key mirror by execution; T-census
freeze check; npm view assertions. The train ORDER (widen-first → value 5 → kf 7 → atlas
ranges → assertions) lives in this wave's spec preamble — the one place it binds.

The tranche-level close ceremony (zero-drop row inventory, disease-row screen, two clean
registry passes, thrice-run) is the TRANCHE's close wave and audits this program along with
every other — not a governance-owned duplicate.

**Net:** 8 waves → 3 + charter matter; every decision ratified before the work it gates; the
inversion of §1 dissolved; ~5 governance wave-slots returned to C21's "direct code
implementation and visual verification."

---

## Evidence appendix (this seat's probes)

1. `git show v3.1.0:package.json` — root `.` export + `./parsing` + `./units` + `main`/
   `types` present: the pre-v4 topology the 02a census cannot express (§4.1).
2. `CHANGELOG.md` [4.0.0] section — no by-name D-ledger tombstones ("raw color-conversion
   exports, and other root-barrel internals"; "`/color` owns … gamut mapping" continuity
   line); [3.1.0] entry = in-repo precedent for retroactive correction of an immutable tag's
   record (§4.2).
3. Sweep cross-checks used as-is: deps block committed at HEAD (sweep-value §4); decompose
   zero-consumer four-tree grep (sweep-value §7); internal/ fan-in 11 (sweep-kf §2); anchors
   9/13 exact (sweep-kf §3); WAAPI absence list (sweep-kf §5).

*— skeptic 2, armB thrice loop. This file is the seat's sole write.*
