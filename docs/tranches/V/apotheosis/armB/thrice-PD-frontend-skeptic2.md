# THRICE — PD-FRONTEND — SKEPTIC 2 (bias: architecture + parsimony) — 2026-07-19

Target: `armB/program-PD-frontend.md` (18 waves). Posture per charter: ASSUME WRONG in every
way; attack with evidence; propose the alternative. Evidence base: the program's own four
sweeps, the live repos at the G0′ pins (value `tranche-u@db77dbd8`, kf `master@0dac636b`),
CARRY-LEDGER.md / V-PRIME.md (pre-07-18 canon, read directly), L1/L2/addenda. Zero Codex-corpus
reads (independence firewall honored). Model serving this seat: `claude-fable-5` (verified from
my own system prompt, P0.1 law).

VERDICT: the program is a well-evidenced AUDIT of the owner's addenda marks wearing the costume
of a tranche program. Its wave set is cut from C2–C6/C10/C22 alone and never reconciles with the
ratified, UNEXECUTED V′ frontend tail it was ordered to consume — so it silently drops ten
ratified waves including two "or V′ does not close" laws; its dependency graph inverts its own
critical path (the glass round-trip); its single inherited vehicle (F-17/W53) contradicts the
ratified rider on disk; and a third of its waves hang on a docket row (OD-1) whose default
re-litigates an addendum. The 18-wave grain is roughly right; the CONTENT of the set is cut
against the wrong inventory.

---

## A. THE GRAND SILENT DROP — the CARRY-LEDGER §B tail (W46–W56) is consumed at ONE row of ten

The fold source law (owner program of record, standing memory; L2 P6 row 4 orders
`CARRY-LEDGER.md` into the ingestion set; L1 §4: "the pre-glass-7 carried-forward UI corpus
audited NOW"): the next formation consumes CARRY-LEDGER **WHOLE — §B tail W46–W56 + riders**.
The V′ frontier stopped at W46; every §B row below is a ratified, born-gated, UNEXECUTED
frontend wave. The program cites CARRY-LEDGER **§F only** (the W44 glass-adoption carries, F-01
inputs) and inherits exactly ONE §B row (W53 → F-17). Grep the program: no "§B", no W46, W47,
W48, W49, W50, W52, W54; no CH-4; no LCP; no aurora. PL-5 — the program's OWN law — demands
fold/bank/retire per carried row; silence is none of the three. The dropped rows, each a
concrete hole (CARRY-LEDGER.md:21–29):

- **A1 · W55/CH-4 — THE BOOT-PERF KILL IS MISSING ENTIRELY.** "CH-4: p75 LCP ≤2.5s on the named
  matrix — **the ~5s boot dies or V′ does not close**" (CARRY-LEDGER.md:29). Lineage: Q14 RULED
  ESCALATION (LCP 5141 / TBT 5988, eager-WebGL-blob boot blocker → U.W-PERF). The program has
  ZERO perf waves. Worse: its landing map routes P3.4 to the sibling gates program, and P3.4
  ABROGATES the ~856-LOC headless perf oracles — so under this program the perf oracles die AND
  nothing anywhere cures the boot. The frontend program polishes watercolor dots and margins on
  an app that takes ~5s to paint, against a ratified close-law. This is the single worst hole in
  the program.
- **A2 · W54 — aurora-derive D-1 ("RUNS or V′ does not close"), blob truthful lifecycle,
  arm-after-critical-paint 0px, WebGPU/WebGL2 parity** (CARRY-LEDGER.md:28). A second ratified
  close-law, absent. It is also the mechanism-level CURE for A1's named blocker (the eager
  WebGL blob) — the two rows are one program.
- **A3 · W46 — the constitution & chrome law** ("seat-law/proportion-law/type-Card tuple; 18
  composition pairs ratified once", CARRY-LEDGER.md:21). This IS the Aristotelian
  proportionality apparatus, already wave-shaped. F-01/F-07 re-derive proportionality from
  scratch as if no constitution wave existed — a parallel authority, the exact sin P4.4 names
  kf-side ("refutation amends the charter; silence re-litigates it").
- **A4 · W47 — shell & scene** ("dock-expand scene rect 0px; **PaneSegmentedControl 1→0 — it
  lives ALIVE at demo/shell/ for exactly this gate**", CARRY-LEDGER.md:22). Direct collision
  with F-02: the mobile dock surfaces PaneSegmentedControl (Dock.vue:197-204, the program's own
  anchor set), and W47 holds a ratified gate to REMOVE it. A toolbar redesign formed in
  ignorance of the 1→0 gate can re-entrench a component a ratified wave already condemned.
  F-02 and the W47 shell decisions must be decided together or in order — today they are not
  even mutually aware.
- **A5 · W48/W49/W50/W52 — picker parity (pointer↔keyboard↔numeric↔AT), palette read/write UI,
  the D57 Prepare→Download export seat (incl. RETIRING legacy `demo/palettes/export.ts` /
  `usePaletteExport.ts`), the admin suite** (CARRY-LEDGER.md:23-26). All demo-product waves,
  squarely this program's jurisdiction, all absent. Under C22 ("EVERY page of EVERY app") the
  program audits these screens in F-01 — and then has no wave that discharges their already-
  ratified gates, so F-01 will re-mint rows W48–W52 already own. Re-derivation without
  disposition is double-booking, the re-booked-chronic close-class lie in wave form.
- **A6 · V-PRIME B4** (V-PRIME.md:111): the orphaned-probe deletions are superseded by "the W44
  routed-mount witness and **W50 journeys**" — a §B-tail dependency the program never maps
  (its landing map covers B3 only, via the W55 note).

Anticipated defense: "L2 P5 names only W53/W55/W56 as value vehicles — the letter scoped the
tail out." Defeated three ways: (1) P6 row 4 orders CARRY-LEDGER read whole and L1 §9 demands
zero silent drops **checked against the row inventory**; (2) the addenda post-date and WIDEN the
frontend scope (C22 totality) — you cannot take C22's every-screen mandate and refuse the
ratified gates on those same screens; (3) PL-5 is the program's own law — even if the intent is
"the tail dissolves into my fresh audit," that is a FOLD and must be declared row-by-row. The
close-laws (CH-4, D-1) cannot be dissolved by audit at all.

## B. DEPENDENCY ARCHITECTURE — the glass round-trip inverts the program's own critical path

- **B1 · OD-2's default starves the owner's two centerpiece marks.** The chain is: F-03/F-05
  (specs+prototypes) → F-09 relay → the glass agent implements → glass publishes → F-04/F-06
  adopt → F-07 (sequenced "after F-02/04/06 land") → F-18. The relay is the program's ONLY
  egress to glass, and OD-2's stated default HOLDS it until "F-01+F-11 complete". So the two
  owner-marked centerpieces (C2 easing, C10 dots) carry the longest latency in the entire
  program — audit-complete + relay + external glass tranche + adopt — by the program's own
  choice of default. With external latency the dominant unknown, the only sane shape is the
  inverse: cut the two centerpiece specs FIRST and relay them as batch #1 immediately; the
  audit-sourced rows go as batch #2. The batching law forbids piecemeal interruption; it does
  not forbid two well-formed batches.
- **B2 · F-04's block on F-03 is partially FALSE — the "too large" arm of C2 is demo-curable
  today.** The 19rem clamp is a demo-owned `:deep()` override
  (`EasingAuthoringStage.vue:104-107`, `inline-size: min(100%, 19rem)` with `!important`s the
  seat's own comment says are "retired at the P7 adopt"). A mobile size budget needs zero glass
  changes. Likewise the "too rounded" pill chips: `EasingSpecimenStrip.vue` is DEMO code
  (`workbenches/gradient/GradientVisualizer/easing/`) composing glass `Chip shape="cell"` — the
  tile redesign is demo-owned unless a new glass primitive is proven necessary. The program
  routes "specimen tile spec" into the GLASS wave F-03 (wrong repo altitude) and blocks the
  whole seat re-fit on the glass variant. Only stroke-law + drag-precision are truly
  glass-owned. The correct cut: demo-side cure NOW (size budget + strip redesign), glass-side
  variant adoption LATER — the program couples the curable to the blocked.
- **B3 · No adoption/reconvergence wave exists.** Three waves (F-04, F-06, F-08's leak-closure
  + shim-retirement arms) cannot close until glass ships work this program is forbidden to do
  — yet no wave models the adoption event (glass devDep bump, variant adopt, dot-API wiring,
  `ActionBarLayer` shim retirement, `AcceptableValue` closure). Under PL-5 ("counting a partial
  as done is the lie") these waves are structurally condemned to sit open or close partial.
  One explicit GLASS ADOPTION RECONVERGENCE wave is the honest home for every glass-gated item;
  everything else closes clean without it.
- **B4 · F-17 contradicts the ratified rider ON DISK and inverts the packet dependency.** The
  program declares F-17 "BLOCKED on the sibling color program landing R-DELTAE + R-GAMUT."
  The ratified W53 rider says the opposite of a block: "**B1 arm-A: rebuild the
  perceived-space plate ON v4** from tag `v-perceived-space-plate-ref-w40` … the old code
  imports dead v4-cut APIs — **rebuild, not restore**" (CARRY-LEDGER.md:27) — i.e. buildable
  against the extant v4 surface now. And P2.2 row 14 runs the dependency the other way:
  R-BOUNDARY is gated on W53's actual needs — so the needs-memo is the UNBLOCKING artifact for
  the color program and must be emitted EARLY. The program makes F-17 dead-last-blocked,
  which (a) re-litigates a ratified recipe and (b) starves the sibling program's row-14
  decision until program end. Invert it: memo first, rebuild on v4, adopt restored kernels
  opportunistically.

## C. OD-1 — a third of the program hangs on a docket row whose default re-litigates an addendum

F-11..16 (6 of 18 waves) are indeterminate pending OD-1, and the stated default is (b):
specs-and-dispatches. Two defects: (1) under (b), a third of the program is letter-writing —
its born-RED/π/DELTA close gates (after-captures of kf fixes) are undischargeable by this
program, so six waves' gates are fiction under their own default; (2) the default takes the
LETTER's side (P4.5) against the ADDENDUM (C4 pulls the kf demo into this program's scope —
the program's own OD-1 tension cell says "C4 … WINS on scope"), violating the addenda-WIN law
it opens with. Either shape is formable; presenting ONE wave set that pretends to be both is
not. OD-1 is a ratification PRE-GATE, not a docket residual — and the addenda-conformant
default is (a) with owner countersign.

## D. NARROWED ADDENDA — two asks quietly shrunk to their easiest fragment

- **D1 · C6 is "routing properly defined, alongside state management" — the program built only
  the URL-codec fragment.** F-10/F-15 are share-codec waves. Nowhere is the routing/state
  ARCHITECTURE adjudicated: value's routes are `Stub { render: () => null }` with NO
  `<router-view>` (router/index.ts:19 — navigation-model-only routing), pane truth lives in
  `useViewManager`/`usePaneRouter`; kf runs a bespoke scene machine. Is Stub-routes-sans-view
  the "properly defined" routing the owner asked for? Maybe — but that is an adjudication the
  program never stages. C6 carried at codec altitude only = silent narrowing.
- **D2 · C11's component-structure DAG is reduced to "colocation checks."** C11: "Full DAGs of
  library AND component structures, ruthlessly analyzed — every node, edge, cycle — in dynamic
  bespoke cluster batches via the thrice method." The landing map hands library structure to
  the sibling program and keeps for the demos only "colocation checks inside F-01/F-11." No
  wave runs the demo component DAG (depcruise/madge over `demo/` trees), no cluster batches,
  no thrice over the component graph. The 31k-LOC value demo + 197-file kf demo get no
  structural analysis at all.

## E. FACTUAL ROT + GATE DEFECTS (the truth-sweep inputs were trusted, not verified)

- **E1 · "15/15 routes" is off-by-one and the gate can only close vacuously.** The router has
  **14 real routes + a catch-all redirect** (`router/index.ts:21-38`; the program itself names
  only 14). "15/15 covered" is satisfiable only by counting a redirect as a screen.
- **E2 · F-06's census is wrong: "~20 sites across 7 files."** Live grep: `WatercolorDot` is
  imported in **11** demo files (Dock, ConsoleRail, SpectrumCanvas, MixResultDisplay,
  MixSourceSelector, GenerateControls, ImageEyedropper, EmptyState, ColorSpaceSelector,
  CurrentPaletteEditor, SwatchHoverMenu), with 6 more files referencing the token family. The
  sweep's own prose enumerates 11 files then says "7"; the program copied the number without
  checking. A wiring wave scoped to 7 files under-scopes by a third.
- **E3 · F-01's "probe budget ≤2 runs (one sweep per viewport)" is performative parsimony.**
  The matrix includes 5 ADMIN routes (auth/session required — `meta: { admin: true }`),
  plus interaction states (kebab open, easing editor open, dialogs, dock layers) that a single
  linear sweep cannot capture. The gate will be either violated or satisfied shallowly —
  a vacuous-green gate of exactly the class L1 §0 warns about. Budget honestly (e.g. ≤4 runs:
  2 viewports × {public, admin-authed}) or drop the number.
- **E4 · Born-RED dilution.** L1 mints born-RED "wherever the defect is live." F-05 (a NET-NEW
  API the program itself labels net-new) and F-10 (a feature that never existed) mint born-RED
  rows for ABSENCE. Absence-of-a-feature RED is a different animal from defect RED — blending
  them cheapens the discipline that makes the real ones (F-02, F-12) binding. Label them
  BORN-ABSENT/feature gates.
- **E5 · F-18 meta-process.** "Probe-budget accounting proving PL-2 was honored" is
  process-about-process (C21: little time on contrived gates or process). Keep the DELTA
  ledger; drop the accounting theater.

## F. WHAT SURVIVES MY ATTACK (for the adjudicator's union)

The evidence anchoring is genuinely strong: C2/C3/C10 diagnoses (19rem seat, unit-relative
strokes, 3-decimal quantization, kebab burial, static dots) are correct and file:line-true —
I verified the load-bearing ones. F-12's viewport-fit find is real and load-bearing. The
landing map's addenda coverage (C1–C23) is honest work. PL-4/PL-6 discipline is right. The
program's failure is not evidence — it is INVENTORY (cut only from the addenda, blind to the
ratified V′ tail) and DEPENDENCY SHAPE (glass round-trip, OD-1/OD-2 defaults, F-17 inversion).

---

## G. THE COUNTER-PROGRAM (title + intent grain; ~20 waves)

Openers (parallel):
- **G-01 · VALUE SCREEN AUDIT ON THE W46 CONSTITUTION** — F-01's matrix (14 routes × 2
  viewports + named interaction states, honest ≤4-run budget, admin authed) with W46's
  seat-law/proportion-law/type-Card tuple FOLDED IN as the rubric (no parallel authority);
  every CARRY-LEDGER §B/§F row it touches gets a written disposition.
- **G-02 · KF SCREEN AUDIT + FIRST DESKTOP CAPTURES** — as F-11; OD-1 decided AT RATIFICATION
  (addenda-conformant default: direct-edit with owner countersign), not carried as docket fog.
- **G-03 · GLASS GAP PACK, EARLY RELAY** — EasingPicker density/stroke/precision variant +
  WatercolorDot living API + AcceptableValue export + layer-transition composable, specced in
  glass idiom and relayed IMMEDIATELY as batch #1 (OD-2 inverted; interruption cost priced);
  audit-sourced rows follow as batch #2.

Value cures (glass-independent, post-audit, parallel):
- **G-04 · EASING SEAT DEMO CURE NOW** — mobile size budget (retire the flat 19rem clamp) +
  demo-owned specimen-strip redesign; glass-variant adoption deferred to G-18.
- **G-05 · MOBILE TOOLBAR × W47 SHELL RECONCILIATION** — F-02's dual candidates decided
  JOINTLY with W47's ratified shell gates (PaneSegmentedControl 1→0, dock-expand rect 0px).
- **G-06 · THE CH-4 BOOT KILL** — the ~5s LCP boot blocker (Q14/U.W-PERF lineage): eager
  WebGL-blob de-eagering, p75 LCP ≤2.5s born-RED on the named matrix. Non-negotiable close law.
- **G-07 · ATMOSPHERE/GPU TRUTH (W54 fold)** — aurora-derive D-1 RUNS; blob truthful
  lifecycle; arm-after-critical-paint 0px. Paired with G-06 (same mechanism family).
- **G-08 · PICKER PARITY (W48 fold)** — pointer↔keyboard↔numeric↔AT parity; A137/A138.
- **G-09 · PALETTE UI + D57 EXPORT SEAT (W49/W50 fold)** — read/write journeys; the
  Prepare→Download seat; retire legacy export.ts/usePaletteExport.ts; api-spanning slices
  (D55.iii cookie) routed to the API program by name.
- **G-10 · ADMIN SUITE (W52 fold)** — five full-width routes, companion 50%→0.
- **G-11 · VALUE DETAIL PASS** — the G-01 refine backlog, glass-independent rows only;
  W46-law consumption (override 0); the MOTION-REGISTER table.

kf series (mode fixed by G-02's ratified OD-1):
- **G-12 · KF STAGE CURE** — viewport-fit=cover + cube/hero collision + safe-area re-verify
  (F-12 as-is; it was well-cut).
- **G-13 · KF MULTI-TOUCH, BOUNDED** — multi-touch for the non-cube scenes via a NEW small
  shared scrub composable; the cube's complete reference implementation is NOT extracted or
  refactored (non-regression by isolation, not by test); per-scene semantics table where
  "no meaningful gesture" is a legal terminal verdict, owner-visible.
- **G-14 · KF APP MATURATION + READABLE URL STATE** — F-14+F-15 merged (per-scene titles and
  legible share state are one share-legibility story; the F-15→F-14 dependency was a
  touchpoint, not a wave boundary); OD-3 decided inside.
- **G-15 · KF DETAIL PASS** — as F-16.

Cross-cutting:
- **G-16 · ROUTING+STATE ARCHITECTURE ADJUDICATION (C6 full width)** — value Stub-routes/no-
  router-view + pane-truth topology and kf's scene machine adjudicated (keep/reshape, with
  rationale); THEN the value URL-state/share codec (F-10 content) lands on the adjudicated
  architecture.
- **G-17 · SHADCN TERMINAL SWEEP** — barrel dissolution (48 sites), components.json deletion,
  import unification; the glass-gated leak/shim items EXCLUDED (they live in G-18).
- **G-18 · GLASS ADOPTION RECONVERGENCE** — the single wave where external latency is
  absorbed: glass bump; EasingPicker variant adopted (seat `!important`s retired per the
  seat's own P7-adopt note); WatercolorDot living API wired at the 11 import files + the
  slider-value watercolor treatment; ActionBarLayer shim retired; AcceptableValue leaks
  closed. Re-runs per glass landing if the batch ships in parts.
- **G-19 · W53 PLATE — NEEDS-MEMO FIRST, REBUILD ON v4** — per the B1 arm-A rider: memo
  emitted EARLY (it unblocks the color program's R-BOUNDARY row), plate rebuilt against the
  extant v4 surface from the reference tag, restored kernels adopted opportunistically.
- **G-20 · CLOSE** — DELTA ledger; the §B-tail disposition table (every W46–W56 row + rider
  folded/banked/retired BY NAME); the demo component-DAG thrice record (C11) attached; no
  probe-budget theater.

Grain notes: 20 waves vs 18 — the delta is not inflation; it is the recovered inventory
(G-06/07/08/09/10 are ratified waves the target dropped) net of two merges (F-14+F-15;
F-03's demo-owned half into G-04) and one dissolution (F-09-as-wave becomes the G-03 pack +
a standing register; a registry row is not a wave).

## H. EVIDENCE APPENDIX

| # | Claim | Evidence |
|---|---|---|
| A1 | CH-4 close-law dropped | CARRY-LEDGER.md:29 ("~5s boot dies or V′ does not close"); program: zero perf waves; landing map routes P3.4 away while P3.4 abrogates headless perf oracles |
| A2 | W54/D-1 dropped | CARRY-LEDGER.md:28 ("aurora-derive RUNS or V′ does not close") |
| A3 | W46 constitution re-derived in parallel | CARRY-LEDGER.md:21 vs F-01/F-07 (no W46 mention anywhere in the program) |
| A4 | F-02 vs W47 PaneSegmentedControl 1→0 collision | CARRY-LEDGER.md:22; Dock.vue:197-204 (program's own anchor) |
| A5 | W48/W49/W50/W52 dropped | CARRY-LEDGER.md:23-26 |
| A6 | V-PRIME B4 unmapped | V-PRIME.md:111 (supersession depends on W50 journeys) |
| B2 | 19rem clamp is demo-owned, curable now | EasingAuthoringStage.vue:103-107 (`min(100%, 19rem)`, "retired at the P7 adopt"); strip = demo file `easing/EasingSpecimenStrip.vue` |
| B4 | F-17 block contradicts rider | CARRY-LEDGER.md:27 ("rebuild ON v4 … rebuild, not restore"); L2 P2.2 row 14 (R-BOUNDARY gated on W53's needs, not vice versa) |
| E1 | 14 real routes, not 15 | router/index.ts:21-38 (14 named + catch-all redirect) |
| E2 | 11 WatercolorDot import files, not 7 | `grep -rl 'from "@mkbabb/glass-ui/watercolor-dot"' demo` → 11 files |
| E3 | Admin routes need auth; ≤2-run budget infeasible | router/index.ts:30-34 (`meta: { admin: true }`) |
| C | OD-1 default vs addenda-WIN | program §4 OD-1 tension cell ("C4 … WINS on scope") vs its own default (b) |

— Skeptic 2, thrice loop, PD-frontend. Served by `claude-fable-5`.
