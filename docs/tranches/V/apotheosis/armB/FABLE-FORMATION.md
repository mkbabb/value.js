# FABLE-FORMATION — the independent Fable mega-tranche formation (arm B) — 2026-07-19

**Assembled from the six thrice-adjudicated FINALs** (`final-PA-parse.md`, `final-PB-color.md`,
`final-PC-structure.md`, `final-PD-frontend.md`, `final-PE-api.md`, `final-PF-governance.md` —
all six present; none missing). Binding canon: L1 (the refined kickoff prompt) + L2 (packets
P0–P6) + CONVERSATION-ADDENDA C1–C23 (addenda WIN where they tighten). Independence firewall
honored: zero reads of `vnext/`, `snapshot-vnext`, `armA`, or any `r1-opus-refuted` path.
Companion files: `WAVE-REGISTRY.md` (the complete dependency-ordered wave table, 61 rows) and
`OWNER-DOCKET.md` (the single merged decision sheet, 47 rows + 3 supersession records).

**Tree pins (G0′, as re-verified by the finals; re-pin at every wave open):**
value.js `tranche-u@c654824e` (src-identical to canon `db77dbd8`; 4.0.0 published immutable,
HEAD carries the committed deps bomb) · keyframes-v-exec `master@81a56990` (canon `0dac636b`
+2 docs-only; 6.0.0) · parse-that `master@ef10d5b` = npm 1.0.0 · glass-ui `master@cdc322b9`
(7.0.0; advances fast — re-pin at read) · atlas ACTIVE `.p-totality/atlas p/totality@fe9abcf`
(7.0.0; standalone checkout is a STALE trap) · bbnf-lang `master@af15f63e` · fourier-analysis
`m/w1-bump-migration@cd26c653` · sci-report `feat/tranche-k-arc@da1e3763`.

---

## §1 — MISSION

Form and execute the next mega-tranche across the tightly-coupled trio (value.js,
keyframes.js, parse-that) plus consumers (glass-ui, atlas, fourier-analysis, the demos),
per L1 §1 and C8. Six programs, one formation:

- **PA-parse** — parse-that READOPTED as published (owner decree P1.6): restoration-by-retype
  of the deposed combinator tree behind the frozen `/css` surface; bench witness; July-2026
  spec census; C12 DOM-congruence execution; the C14 dual BBNF definition; the kf boundary
  letter.
- **PB-color** — the single-sided v4 color extinction RESTORED: ΔE family → §13 conformance
  cure → analytical gamut engine (part-restore/part-new, honestly priced) → Into extension on
  the W56 4.1.x vehicle → ramps + kf scar-cure dispatch → contrast eval → OKHSL; plus the
  drop-census/retro-tombstone organ.
- **PC-structure** — machine-DAG structure perfection both libraries + both demo trees:
  the value↔kf isomorphism gate, the boundary-delta ledger, subpaths dissolution, the ONE kf
  structure packet, the C5 idiom close (merged with PD's abrogation wave).
- **PD-frontend** — the two demos perfected as products: value by refinement, kf by overhaul
  (C2/C3/C4/C10/C22); the ratified V′ tail (CARRY-LEDGER §B W46–W56 + §C/§D/§F) folded WHOLE;
  the CH-4 boot-kill and W54 close-laws owned; glass gap prototypes relayed in two priced
  batches; C6 routing adjudicated then instrumented.
- **PE-api** — the palette API fully specified and machine-witnessed, STAYING at `/api` (C7
  decided); CRUD-CONTRACT v3 as the single normative home; the TA-4 remix rail restored
  consumer-in-gate; fourier isomorphism at contract level; both backends structure-treated.
- **PF-governance** — the coordination spine: day-one deps-bomb defusal + publish-integrity
  gates + the v4 tombstone backfill; the roster/channel/dispatch pack (phantoms killed); the
  merged docket RATIFIED AT THE FRONT; the orphaned e2e fleet adjudication homed; the ONE
  co-land boundary with entry-gated go/no-go; the tranche close.

Core features are never lost — machine-checked (capability-diff gates both repos, two-grain,
tag-shape-agnostic; the fired v4 instance cured by CHANGELOG backfill). Every chronic,
deferred item, and prompt-recap row carries a terminal disposition; partials fold, bank
(named re-trigger), or retire — never count as done.

## §2 — MODEL ROUTING (C17 + P0.1 + L1 §0)

- **Fable** for all design, brainstorming, adjudication, novelty, deep frontend design,
  thrice seats, cross-repo letters, and every contested cut. **Opus** only for mechanical
  sweeps/inventory/extraction/census scripts/chase edits — and only under the per-tranche
  owner ratification at docket row **D-46 (OD-15)**; default-if-silent is NO Opus seats.
- Every seat reports `model_served` from its own system prompt — **a declaration is not
  execution** (the P0.1 config-error lesson, standing law). Fable cost is outrageous: every
  Fable seat is file-scoped and tight; batches of 5–6 concurrent; thrice panels exactly 3.
- Phase labels on every edict (L1 §7 law 4): this document is FORMATION output; the waves
  bind the implementation phase. PA-W0 is the one phase-labeled formation pilot (C14's
  "prototyped and benchmarked NOW").

## §3 — THE CO-LAND BOUNDARY (P4.2, executed by F-GOV-05)

**ONE coordinated crossing**: value 5.0.0 / kf 7.0.0 / glass peer move per ratified D-44
(range-widen `^6||^7` + `^4||^5` as a glass MINOR pre-boundary is the default) / atlas
ranges. The wedge is HARD (present-incompatible `peerOptional` ERESOLVE-fails, proven by
execution twice; prerelease semantics priced — `^4 || ^5` does NOT admit `5.0.0-rc.x`).
Until the crossing: **surfaces frozen, restructures internal-only, pack-parity-witnessed**.
Every surface-affecting cut (the ten `./transform` symbols dying with decompose, quantize
demote + `./quantize` key drop, `collectDeclarations` trim, abrogated e2e, any renamed public
symbol) is a **boundary-delta LEDGER row** (authored F-PC-03, tombstoned per §7 law 1,
four-tree re-grepped at execution HEAD) and EXECUTES only at the crossing. Go/no-go is
F-GOV-05's ENTRY gate; ratification (F-GOV-03) precedes it, so ruled prunes have a lawful
vehicle. Order: value 4.1.x (W56/SCI-1, D-GAP-6 rider) → glass peer move → FREEZE →
value 5.0.0 → kf 7.0.0 → atlas ranges → post-cut assertions. No stale-atlas catch-up
(npm is already 7.0.0). The value deps block is STRIPPED day-one (F-GOV-01), never
lockstep-bumped. kf pins value EXACT — the PB scar-cure needs only a kf-owned minor
pin-advance to ≥4.1.x (a dispatch rider, NOT the boundary). W55 owns the `.github` edit;
sci-report crosses atomically at its own glass-7 consume (registry row, no wave).

## §4 — METHOD (C20 / L1 §8)

- **The thrice loop on every wave/cluster/prototype**: two fresh Fable skeptics assuming the
  artifact is WRONG outright + one Fable adjudicator who proves/disproves with its OWN
  evidence — never vote-counts. Convergence = two consecutive clean passes; ≤3 iterations
  per cluster → owner escalation. All six programs herein are themselves thrice products
  (every skeptic claim PROVEN/DISPROVEN on tree in their §-logs).
- **DAG batching**: machine-derived DAGs only (madge/depcruise artifacts adopted where they
  exist — `armB/madge-src.json` 26 files/0 cycles, `armB/depcruise-animation.json`; gap
  graphs captured once at F-PC-01), analyzed in dynamic agglomerated cluster batches
  (PC runs five; seat ledger 30 baseline / 45 ceiling). Anchors are RE-DERIVED at execution
  (N-ADJ-3 — the sweeps already caught 3 vite drifts); consumer-edge truth is always the
  four-tree census at pinned HEADs.
- **Gate honesty**: born-RED only where the defect is LIVE on disk today; authored-RED for
  ordinary TDD absence; BORN-ABSENT for net-new capability with a stated GREEN; facts
  recorded as facts. Every gate failable AND passable by construction (selftest-by-mutation
  where applicable). Motion claims carry motion oracles (traces/video), not stills; π/DELTA
  only where a visual claim exists (probe parsimony, C21).
- **Coordination law**: every session/wave opens with the E13 4-path mail sweep; kf items are
  SPECS + bounded dispatches into kf's inbox (P4.5) rowed in INBOUND-LEDGER; glass needs ride
  the two-batch relay (D-22); no wave closes with unread mail.

## §5 — SCOPE BOUNDS

- **C1** — parse-that is *leveraged as published* (npm 1.0.0) for the full CSS prototype; it
  is NOT uplifted with novel features in this tranche. Minor fixes only, fix-on-trip rider on
  PA-W3; all tape/substrate/Pratt/diagnostics evolution belongs to the separate BBNF session;
  needs route as PT-E ask letters.
- **C15** — parse-that may never publish >1.1 regardless of change level. Preventive seat:
  the F-GOV-06 formation-grep (no >1.1 proposal in any program/spec/dispatch file) + the cap
  carried in every PT-E letter; `npm view` post-cut is confirmation only.
- **C16** — isolation: the Codex fleet's `value.js/docs/tranches/V/vnext/` is READ-ONLY (and
  unread by this arm — firewall); all writes land in the apotheosis working dir / a separate
  landing dir; `scripts/dev/dev.sh` is an unowned dirty row, NEVER touched; `r1-opus-refuted/`
  is never loaded. The formed tranche's plan folder lands in the repo only on owner order,
  after the union pass.

## §6 — PROGRAM INDEX

| Program | File | Waves | One line |
|---|---|---|---|
| PA-parse | `armB/final-PA-parse.md` | 7 + 1 formation pilot | parse-that readoption by retype; bench witness; spec census; C12 execution; BBNF dual definition; kf boundary letter |
| PB-color | `armB/final-PB-color.md` | 8 | R-DELTAE → §13 cure → gamut engine → Into → ramp/dispatch → contrast → OKHSL; drop-census organ |
| PC-structure | `armB/final-PC-structure.md` | 8 (one merged with PD) | isomorphism gate; boundary ledger; subpaths dissolution; library re-cut; C5 idiom close; the kf packet |
| PD-frontend | `armB/final-PD-frontend.md` | 24 (one merged into PC) | both demos perfected; V′ tail folded whole; boot-kill; C6; glass relay + reconvergence |
| PE-api | `armB/final-PE-api.md` | 8 | machine census; CRUD-CONTRACT v3; oklab migration; remix rail; fourier landings; witness close |
| PF-governance | `armB/final-PF-governance.md` | 6 | defuse + publish integrity; roster/dispatch pack; front-ratified docket; e2e adjudication; co-land; close |

**Totals: 61 registry rows** (62 raw − 1 duplicate merged) across 7 repos (value.js,
keyframes.js, glass-ui, fourier-analysis, atlas, parse-that [bounded rider/channel only],
bbnf-lang [read-only exemplar]). Cross-program merges and stale-id resolutions are logged in
`WAVE-REGISTRY.md` §M/§R.

---

## §7 — GAPS (L1 §9 fallback clause: what a FULL formation still owes that this core does not carry)

This is the strongest rigorously converged core; its exact remaining gap:

1. **The plan folder is UNLANDED.** C16 isolation forbids repo writes; the formed tranche
   exists as six FINALs + this assembly in the apotheosis dir. A full formation mints
   per-wave spec FILES in `docs/tranches/<next>/waves/` with their gates verbatim — owed
   after the owner's union ruling.
2. **The docket is UNRATIFIED.** OWNER-DOCKET.md (47 rows) is F-GOV-03's input, not its
   output; dependent waves (pre-swap grammar cuts D-07/08/09/10, kf-demo mode D-21, Opus
   roster D-46, glass peer strategy D-44, boundary go/no-go) are gated until the owner rules.
3. **The full 1:1 receipt table is specified, not generated.** Each program carries its own
   landing map; F-GOV-02.6 specifies the machine-diffable canon-row → program → wave-id
   receipt across ALL of L1/L2/addenda/P1–P5 — the assembled union receipt (and its zero-drop
   check against the packets' row inventory) is owed at F-GOV-02/F-GOV-06.
4. **Registry stability pass two.** L1 §0: stable = two consecutive passes surfacing nothing
   new. This assembly is arm-level pass one; a second independent pass is owed.
5. **The assembly itself has not ridden a thrice loop.** Every program is thrice-adjudicated;
   this charter + registry + docket assembly is single-seat work (adjudication logs consumed,
   conflicts resolved on final-over-r1 and grant-cannot-default principles — see
   WAVE-REGISTRY §M-6) and would take a skeptic pair in a full formation.
6. **PA-W0's outputs do not exist yet.** C14 authorizes the prototype/bench NOW; the
   transposition pilot, the memo-defeating bench harness, and the ONE regex-parser record
   measurement are specced but unrun. A full formation carries them as evidence.
7. **Stale-id residue inside the finals.** PB cites PA's r1 wave/OD ids; PC cites r1-PF wave
   ids; PF's merged sheet cites r1 OD numbering. The resolution map is WAVE-REGISTRY §R; the
   underlying files are not re-issued (an errata sheet or re-issue is owed at landing).
8. **One routed row lacks a receiving wave**: PD F-08 routes the D55.iii api half
   (`__Host-value-session` cookie transition + idempotency-key retirement) to the API program
   by name; PE's final carries no row for it. Provisionally homed at F2-api-02 (spec) +
   F2-api-05 (landing); must be confirmed into PE's wave text at landing.
9. **The consolidated π/DELTA ledger** (PD F-24) and the per-program DELTA registries exist
   as gate specs only; captures accrue in execution.
10. **C23's union is not this arm's act.** The apotheosis (Codex-corpus audit × this
    formation → union) is the parent's; this file is the independent Fable side only.

— arm-B assembler, 2026-07-19. Sole writes: this file, WAVE-REGISTRY.md, OWNER-DOCKET.md.
Model serving this seat, from its own system prompt: `claude-fable-5`.
