# F-PE-api — THE API PROGRAM — AMENDED FINAL (thrice-adjudicated)

Adjudicated 2026-07-19 under the binding canon (L1 charter + L2 packets P0–P6 +
CONVERSATION-ADDENDA C-rows; addenda win). Inputs: `armB/program-PE-api.md` (11 waves, 8 OD
rows) + `armB/thrice-PE-api-skeptic1.md` + `armB/thrice-PE-api-skeptic2.md`. Every skeptic
claim re-probed by the adjudicator's OWN evidence this session (file:line below; never
vote-counted). Tree pins: value.js `tranche-u@db77dbd8` (api `2.0.0`, api/package.json:3),
fourier-analysis live tree, keyframes-v-exec `master@0dac636b` (NO api — census NULL).
Independence firewall honored: no `vnext/`, no snapshot, no armA. Full log in §6.

**Adjudication headline**: the program's truth-sweep and skeleton survive (TA-4 restoration,
oklab-on-the-library, the fourier gap evidence, test displacement, STAY-at-/api). Its
adjudication layer does not: BOTH skeptics independently converged on (i) an incomplete
divergence universe closed by assertion, (ii) a fourier-is-right bias that silently overturns
value's NAMED decided records (D2 §3 P2, D3/D.W2 Lane D, V·W45 item 1, D-HARDEN-3 §3 W4 —
all verified verbatim on tree), and (iii) process mass against C21. The adjudicator's own
probes added a fourth: the program's F-11b premise "value has no transition guard" is FALSE
on tree (`service/visibility.ts:51-88` — a live guard, all-pairs-legal BY DOCUMENTED DESIGN,
J.W1c §5.1). The amended program: **8 waves**, machine-derived census, one normative home,
every record-overturn named on the docket, code-first sequencing.

---

## 1 — PROGRAM CHARTER (amended)

The palette API becomes a FULLY SPECIFIED, machine-witnessed product surface **staying at
`/api`** — STAY is DECIDED by addendum C7 (addenda WIN; later + specific); it is executed as
a named SUPERSESSION TOMBSTONE of P3.2's EXTRACT row (registry row in W7), not re-litigated
as a fresh owner decision (§7 no-re-litigation; skeptic2 A7 adopted). The mixing facility
(the TA-4-excised J.W2 atom-diff rail, excision pinned `a8ff7792`) goes to the owner as a
REAL verb-design trichotomy and, on RESTORE, returns **with its consumer inside the
acceptance gate** — never write-only again (its third ride is forbidden, §7 disease-row law).
Isomorphism with fourier-analysis is **contract-level, single-home** (CRUD-CONTRACT v3), its
divergence universe **generated from both live route registries** — never hand-enumerated —
and every same-facility divergence lands as CONVERGE or RATIFIED-DIVERGENCE **with both
repos' on-tree records cited by name**. Both backends receive the §4/C11 structure treatment.
Witnesses land as ordinary tests in each repo's EXISTING conformance battery (value
`api/test/conformance/`, fourier `api/tests/conformance/`) — no vector corpus, no mirrored
fixtures, no cross-repo harness (C21; skeptic1 C4 + skeptic2 A5, convergent, adopted). Zero
silent drops, checked against the GENERATED census, not a sweep's row list.

---

## 2 — THE WAVE SET (8 waves)

### F2-api-01 | both | THE MACHINE ROUTE/FACILITY CENSUS
- **Intent**: Replace the hand-enumerated sweep-§3.4 universe (proven incomplete: restore,
  revert, versions/:hash, gallery alias, creation-default visibility all missing) with a
  GENERATED census: value's route table from the live-registry `openapi.json` generator
  (`modules/meta/openapi.ts`) + fourier's from FastAPI introspection, machine-diffed into THE
  divergence/gap table. Every row pre-classified EXISTS / GAP / DIVERGENT / RECORDED-DECISION
  with the on-tree record QUOTED: D2 §3 P2 (`platform/http/idempotency.ts:34-36`), D3/D.W2
  Lane D (`service/crud-list.ts:53-56`), V·W45 item 1 (`routes/crud.ts:160-165`), D-HARDEN-3
  §3 W4 (`service/oklab.ts:10-21`), J.W1c §5.1 (`service/visibility.ts:41-59`), TA-4
  (`a8ff7792`), fourier B.W3 like/flag carve-out (`routers/gallery.py:3-9` + pre-B.W3
  `52bdcf5^:api/routers/gallery.py:218,:295`).
- **Deliverables**: the generated, record-annotated census table (formation registry
  artifact — bookkeeping, NOT a permanent doc-vs-doc CI gate; the retired proof-idiom law
  is respected); the OD-docket evidence bundle.
- **Acceptance gate (mechanical)**: RED = any mounted route in either registry absent from
  the table (closure by enumeration, the RG-2 lesson — never by assertion).
- **Dependencies**: none (opens the program).
- **π/DELTA**: none.
- **Model routing**: Opus mechanical generation/diff; one Fable seat annotates records.

### F2-api-02 | both | CRUD-CONTRACT v3 — THE SINGLE NORMATIVE HOME
- **Intent**: ONE contract document family (absorbs the program's 01+05+10-spec+09c): the C7
  full-facility spec of record + every census divergence adjudicated CONVERGE-to-X /
  RATIFIED-DIVERGENCE **with both records cited by name** + the share-URL backend section
  (unlisted-as-primitive ratified; the no-token-in-URL invariant as a SERVER-SIDE route/schema
  conformance assertion — no client source grep, per the retired proof-idiom law; `/shares`
  spec'd + BANKED via OD-api-H) + the cross-repo layer VOCABULARY (fourier's
  horizontal-vs-value's-vertical layout adjudicated here — isomorphism of facility, not
  directory shape; no separate thrice loop for a foregone conclusion). Value-side "SPEC" =
  the generated `openapi.json` + a thin facility index pointing at v3 — no second normative
  home (P4.4 parallel-authority principle; skeptic2 A6 adopted).
- **Deliverables**: v3 amendment (home per precedent: fourier docs, value-side mirror row);
  the amended OD docket (§3); per-converged-row born-RED test specs handed to W5/W6.
- **Acceptance gates**: mechanical — RED = any GENERATED census row without a verdict
  (checkable against the W1 table; closable by this wave's own work — the
  implementations-terminal check moves to W8). Thrice: ONE loop over the whole contract
  (2 Fable skeptics assuming it is wrong + 1 Fable adjudicator); convergence = two
  consecutive clean passes. **C20-deviation annotation (owner-visible)**: thrice runs on the
  contract (the genuinely contested artifact) and on this program itself; mechanical waves
  ride gate-checks instead — the C17 Fable-cost bound is the stated rationale.
- **Dependencies**: F2-api-01.
- **π/DELTA**: none.
- **Model routing**: Fable (3 seats exactly, file-scoped).

### F2-api-03 | value | OKLAB MIGRATION — THE PRE-AUTHORIZED BOUNDARY CURE (ships first)
- **Intent**: Execute the IN-FILE pre-authorized migration (`oklab.ts:16-21` orders its own
  wholesale replacement) onto published `@mkbabb/value.js` `parseCSSColor → convert(oklab)`.
  Honest framing (skeptic1 A4 + skeptic2 A9 adopted): the narrow hex/rgb domain is the
  DELIBERATE D-HARDEN-3 §3 W4 fail-explicit cure, not itself the defect — the defect-family
  member (P1.5 register: families, not instances) is the INLINE DUPLICATE pipeline; the
  boundary rejection of `hsl()`/`oklch()`/named colors is the user-facing consequence this
  wave cures. Zero dependencies — it does NOT queue behind any spec wave.
- **Deliverables**: `oklab.ts` on the library surface; acceptance-domain matrix
  (`currentColor`? `var()`? `none`?) on the OD sheet (owner-visible, not an in-wave pocket);
  explicit backfill/no-backfill decision row for at-rest `oklabColors` (denormalized +
  radius-searched); defect-family register row citing P1.5; C13 rider re-check after 4.1.x.
- **Acceptance gates (born-RED)**: `POST /palettes` with `oklch(0.7 0.1 200)` + a named
  color → 201 + correct `oklabColors` — **RED today (rejected, `oklab.ts:66-80`)**;
  equivalence on the legacy hex/rgb domain with a **STATED tolerance: ≤1e-6 per OKLab
  component** — derived from the consumer: the color-distance search radius defaults to
  0.15 (`crud-list.ts:167` `colorRadius ?? 0.15`), five orders of magnitude of headroom;
  exact-equality and unstated-tolerance gates are both forbidden (vacuous-gate class).
- **Dependencies**: none.
- **π/DELTA**: none.
- **Model routing**: Opus mechanical swap + tests; Fable adjudicates only the
  acceptance-domain matrix (which rides the OD sheet).

### F2-api-04 | value | THE MIXING VERB DOCKET + R-REMIX RAIL (OD-fused, consumer-mandatory)
- **Intent**: Resolve OD-api-A (the REAL trichotomy, §3) and on RESTORE land the rail whole.
  The internal contradiction the program shipped is cured: fourier's remix 422s any no-op
  (`visualizations.py:536-542`; atoms exclude title/tags) so "fork = remix-with-zero-
  overrides" ALWAYS 422s under the program's own gate — mutually exclusive as written
  (skeptic2 A1 PROVEN). The adjudicator's stronger find: **value's own J.W2 remix allowed the
  empty diff** — `a8ff7792^:api/src/services/palette/forks.ts`: "fork IS
  remix-with-empty-diff: ONE code path" — so a drop-the-no-op-guard arm is value's OWN
  on-tree precedent, a RATIFIED-DIVERGENCE candidate from fourier's later tightening, not a
  novelty.
- **Resurrection recipe (pinned, §7 law 2/3)**: `git show a8ff7792^:api/src/lib/crud/atomdiff.ts`
  (+ routes/service/model from the same tree), modernized to the current modules/ layout.
  **Isomorphism is CONTRACT-level ONLY**: fourier's write sequence is deliberately
  NON-transactional ("standalone-topology-honest", `visualizations.py:493-496`); value's
  fork/remix is a `withTransaction` H1 site (`service/forks.ts:32-40,:94`) — the fourier code
  path is never ported (skeptic2 A8b adopted).
- **Deliverables**: routes + service + `atomdiff.ts` + `PaletteVersion.atomDiff`
  (nullable-backfill note); the demo CONSUMER in-scope — version-history diff rendering + the
  remix flow; `useVersionHistory.ts` fork call site (`:102`) + `api/versions.ts` client
  (`:43-50`) migrated per the ratified verb design; fork tombstone iff retired; **api semver
  pricing row: route removal on api `2.0.0` = major → `3.0.0`, plus the split-deploy order
  window (api at mbabb.fi.ncsu.edu/colors/, demo on gh-pages — repo-atomic ≠ deploy-atomic;
  the live demo's fork affordance must never 404 mid-deploy)** (skeptic2 A8a adopted).
- **Acceptance gates (born-RED)**: diff-envelope conformance on `GET /:slug/diff`
  (`{fromHash,toHash,ops[],identical}`, immutable Cache-Control, If-None-Match/304) — **RED
  today (404)**; version-carries-atomDiff — **RED today (`model.ts:84-98`)**; the no-op
  semantics test per the RATIFIED verb design (422 arm OR empty-diff-allowed arm — whichever
  the owner picks); **a live demo consumer probe — the rail may not close green write-only
  (kills the third ride of the TA-4 chronic; skeptic2 A2 adopted)**.
- **π/DELTA**: MANDATORY pair (fork is surfaced UI today — `useVersionHistory.ts:102`;
  skeptic1 D5 adopted): before = fork affordance, after = the ratified flow. Probe-parsimonious.
- **Dependencies**: F2-api-02 (verb design ratified); F2-api-07 (value test tree).
- **Model routing**: Fable designs the verb/envelope + the demo flow; Opus mechanical
  resurrection + test port.

### F2-api-05 | value | VALUE-SIDE CONVERGENCE LANDINGS (OD-gated; every overturn named)
- **Intent**: Land ONLY what the ratified docket converges, each landing citing the record it
  supersedes: (a) cursor stale/malformed → 400 `urn:contract:cursor-invalid` IFF OD-api-D so
  rules — explicitly OVERTURNING the D3/D.W2 Lane D documented semantic by name
  (`crud-list.ts:53-56`), demo client migrated to handle 400 (it silently restarts today);
  (b) visibility transition policy IFF OD-api-C converges — **corrected premise: value HAS a
  live guard** (`assertVisibilityTransition`, `visibility.ts:51`, called at `:88`);
  all-pairs-legal is DOCUMENTED design (J.W1c §5.1: "all 9 (visibility, tier) tuples are
  legal resting states"; the `from` parameter is the pre-built composition point for a
  forbidden-pair rule) — the program's "value has none" born-RED gate is deleted as
  factually false; (c) idempotency backing → Mongo TTL IFF the owner OVERTURNS D2 §3 P2
  (OD-api-E) — never smuggled as implementation; (d) restore-verb outcome per OD-api-F;
  (e) any further v3 value-side rows. Session token-at-rest hashing re-witnessed (already
  tested — expected-green witness, honestly labeled).
- **Acceptance gates**: born-RED per CONVERGED row only, from v3's test specs (e.g. stale-
  cursor 400 — RED today, silent restart; restart-replay — RED today IFF OD-api-E overturns).
  No gate exists for rows the docket RATIFIES.
- **Dependencies**: F2-api-02 (verdicts), F2-api-07 (test tree), OD ratifications.
- **π/DELTA**: none.
- **Model routing**: Opus mechanical from v3 specs; Fable spot-adjudication on the demo
  stale-cursor failure-mode UX.

### F2-api-06 | fourier | FOURIER-SIDE LANDINGS + THE GAP-TRIO AT THE RIGHT ALTITUDE
- **Intent**: (a) fourier's v3 convergence half. (b) **Session-token hashing at rest** as a
  wave (security hardening; plaintext `uuid4` `_id` confirmed `routers/sessions.py:22-33`;
  value's hashed-at-rest standard is the pattern; Fable adjudicates live-session
  invalidation). (c) **like/flag write routes go to the DOCKET, not auto-implementation**
  (skeptic2 A10 adopted on the adjudicator's stronger evidence): pre-B.W3 fourier SHIPPED
  `POST /gallery/{hash}/like` + `POST /gallery/{hash}/flag` (`52bdcf5^:api/routers/
  gallery.py:218,:295`); the B.W3 convergence carved them out and re-landed publish/CRUD but
  never like/flag (`routers/gallery.py:3-9`); the scar tissue survives (likes/views fields
  `models/visualization.py:150-151`; flag URNs `lib/crud/errors.py:71-72`, unmounted) — a
  DROPPED capability pair with a record trail → OD-api-J restore-vs-tombstone, implemented
  here only on owner pull-in. (d) Mechanical pair, day-one: dissolve the `api/slugs.py` thin
  wrapper (documented H-W3-8 compat surface, 2 call sites — reroute both; no-shims edict) +
  unify the two test homes (`api/tests/` + `api/services/__tests__/` both live, probed) into
  one source-isomorphic tree. (e) The committed route-census baseline + CI diff lands
  fourier-side here (the W7 gate's twin).
- **Acceptance gates (born-RED)**: at-rest inspection test (register → read sessions → token
  not plaintext) — **RED today**; shim-absence — **RED today**; single-test-home — **RED
  today**; per-converged v3 row born-RED; like/flag conformance born-RED IFF OD-api-J pulls
  them in.
- **Dependencies**: F2-api-02 for the convergence half; (b)/(d)/(e) day-one; OD-api-J for (c).
  Fourier-side test ordering is governed HERE, not by value's W7 (skeptic2 A7 wrong-repo fix).
- **π/DELTA**: none (fourier frontend wiring is fourier territory; seam noted).
- **Model routing**: Opus mechanical; Fable on the session-invalidation policy.
- **Execution-mode caveat**: all fourier edits contingent on OD-api-J's mode verdict
  (in-tranche direct vs relay/dispatch — skeptic1 D6 adopted).

### F2-api-07 | value | /api STRUCTURE PASS (day-one mechanical; absorbs the program's 02+08)
- **Intent**: (a) displace the 28 colocated `src/modules/*/__tests__/` files (probed: 28)
  into the EXISTING `api/test/` home, tree-isomorphic to `src/` (C11/§4); (b) bidirectional
  zero-import boundary rule (library `src/` ↮ `api/src`; api consumes `@mkbabb/value.js`
  published only) — honest label: **expected born-GREEN witness** (the boundary already
  holds); (c) `_parity.ts` (exists at `api/_parity.ts`, unwired — no package script) → one-
  line CI step; (d) **the COMMITTED route-census baseline file + CI diff — RED on any route
  removal lacking a by-name tombstone**. This REPLACES the program's unimplementable
  "openapi diff vs prior tag": probed — the repo's 31 tags are all library tags
  (v0.6.0…v4.0.0), the api has no tags and sits at `2.0.0`, and `openapi.json` is runtime-
  generated (only `modules/meta/openapi.ts` is committed) — no baseline exists to diff
  (skeptic1 C1 adopted; same §7-law-1 analog, implementable, both repos); (e) the P3.2
  EXTRACT→STAY supersession tombstone as a REGISTRY row (C7 decided it; no OD slot);
  (f) layer-law note + goldilocks verdict note (one-line-cheap or dies, C21).
- **Acceptance gates (born-RED)**: test-isomorphism check — **RED today (28 colocated)**;
  `_parity` wired + green; baseline-diff gate wired (its first RED will be W4's fork
  retirement — caught WITH its tombstone, the law working as designed).
- **Dependencies**: NONE — day-one, before any wave adds value-side tests (skeptic1 D3
  adopted: mechanical hygiene never queues behind owner ratification). Ordering law binds
  value-side waves only (W4/W5).
- **π/DELTA**: none.
- **Model routing**: Opus mechanical; one Fable seat on the goldilocks verdicts.

### F2-api-08 | both | WITNESS CLOSE (the zero-drop terminal)
- **Intent**: v3's converged rows land as ORDINARY tests in each repo's OWN existing
  conformance battery (value `api/test/conformance/` — 8 suites live; fourier
  `api/tests/conformance/` — live); the contract text carries its canonical examples inline.
  NO vector corpus, NO mirrored fixture files, NO cross-repo hash bookkeeping (the mirror-
  sync invariant was unenforceable without the harness the wave forswore — skeptic1 C4 +
  skeptic2 A5, convergent, adopted). Program close runs the zero-drop check against the
  GENERATED W1 census.
- **Acceptance gate (mechanical)**: RED = any W1 census row without a terminal disposition —
  implemented / ratified / owner-decided / tombstoned. Partials disposed per §7 (folded /
  banked-with-re-trigger / retired-with-rationale).
- **Dependencies**: all prior.
- **π/DELTA**: none.
- **Model routing**: Opus census check; Fable signs the close.

---

## 3 — OWNER-DECISION DOCKET (amended: 10 rows; every side's record on the sheet)

| Row | Question | Evidence on the sheet | Recommendation |
|---|---|---|---|
| **OD-api-A** (was 2+3) | **Mixing verb design — the real trichotomy**: (i) client-only ratify + tombstone the API expectation; (ii) RESTORE with fourier's no-op-422 tightening — verbatim fork DIES as a PRICED product regression (UI must collect ≥1 changed color; fork tombstone); (iii) RESTORE with value's OWN J.W2 semantics (empty-diff remix allowed; fork folds in as zero-delta; RATIFIED divergence from fourier at the no-op row). "Fork = remix-with-zero-overrides under the 422 guard" is internally contradictory and OFF the sheet. | fourier `visualizations.py:536-542`; `a8ff7792^` forks.ts ("fork IS remix-with-empty-diff"); `a8ff7792` ("write-only… demo consumes neither"); fork UI live `useVersionHistory.ts:102` | **RESTORE via (iii)** — value's own precedent, no capability loss, no UI redesign forced; consumer-in-gate mandatory either way. Sub-ruling stands: NO server color-math endpoint (C13 — the API records deltas). |
| **OD-api-B** (was 4, re-cut) | **Anon-create + born-visibility policy** — the REAL composite: value allows `userSlug: null` (`routes/crud.ts:85`; session token still required `:74-75`) AND every palette is born `visibility:"public"` (`service/crud.ts` + `forks.ts`); anon-owned rows are born public yet permanently unmanageable (publish/patch/delete owner-gated → null never matches). The program's "anon cannot publish" invariant is vacuous-by-birth. | Demo census: NO shipped path exercises anon create (`usePaletteActions.ts:40-45` ensureUser+ensureSession, early-return; `useSlugMigration.ts:34`; `useUserAuth.ts:99-106` auto-register); sweep synthesis 4 recommends non-null; fourier born-`draft` + required owner (`models/visualization.py:120,:183`) | **CONVERGE: require non-null owner** (the sweep's own recommendation, silently reversed by the program — restored); fallback arm if the owner keeps anon: anon-created rows born NON-public. |
| **OD-api-C** (was 5, corrected) | **Visibility semantics**: enum naming (`private` vs `draft`); unpublish target; transition policy — fourier forbids pairs (`is_legal_visibility_transition`, model:36-49); value legalizes all 9 tuples BY DOCUMENTED DESIGN with a LIVE malformed-target guard (`visibility.ts:51-88`, J.W1c §5.1). The program's "value has none" is false on tree. | Both records quoted | **CONVERGE SEMANTICS, RATIFY NAMES** (unchanged verdict, honest premises): adopt a per-repo legal-transition table at the existing `from` composition point IFF v3 defines forbidden pairs; unpublish target stays each repo's non-public default. |
| **OD-api-D** (was 6, honest) | **Stale/malformed cursor**: fourier 400 `urn:contract:cursor-invalid` (`cursors.py:65`) vs value's DOCUMENTED graceful restart (D3/D.W2 Lane D, `crud-list.ts:53-56`) — a decided record, not a masked fallback. | Both records quoted | **CONVERGE on 400** — but as a NAMED overturn of D3 (contract-visible failure wins cross-repo), demo migration priced in W5a. |
| **OD-api-E** (NEW) | **Idempotency backing**: value's in-process LRU is the SANCTIONED single-replica KISS relaxation (D2 §3 P2, `idempotency.ts:34-36` verbatim, body-hash conflict already contract-faithful) vs the contract-reference Mongo 24h TTL (`lib/crud/idempotency.py:1-42`). | Both records quoted | **KEEP the relaxation** (the single-replica deployment premise is on record) unless the owner overturns D2 §3 P2 by name; recorded in v3 either way. The program's silent convergence is dead. |
| **OD-api-F** (NEW) | **Restore verb**: fourier live `POST /{slug}/restore` (`visualizations.py:430-457`) vs value's DELIBERATE retirement (V·W45 item 1, `crud.ts:160-165`: the versions/revert rail is the honest surface; typed 404). | Both records quoted | **RATIFY DIVERGENCE** — value's record is intact + deliberate (record-state trichotomy: owner decides, shield unpierced); fourier keeps restore. |
| **OD-api-G** (NEW) | **History verbs**: value-only `POST /:slug/revert` + `GET /:slug/versions/:hash` (`routes/versions.ts:43,:49`); fourier has list-versions only (`visualizations.py:860`). Port or ratify. | Route census | **RATIFY DIVERGENCE** — no fourier product ask on record; a port row banks with a named re-trigger (a fourier revert ask). |
| **OD-api-H** (was 7) | **`/shares` snapshot endpoint**: build vs bank. | unchanged | **BANK with named re-trigger** (encoded workbench state > ~1.5KB, or a cross-device share ask); spec ships in v3 (W2) so activation is a switch. |
| **OD-api-I** (was 8) | **ETag derivation basis**: value at-rest `currentHash\|updatedAt` vs fourier canonical-digest. | unchanged (survived both skeptics) | **RATIFY DIVERGENCE** — identical 428/412/`*` semantics; byte-compat never exercised. |
| **OD-api-J** (NEW) | **Fourier execution mode + gap-trio pull-in**: (a) does this value-owned program edit the fourier tree directly, or dispatch specs (P4.5-adapted)? C8 puts fourier in-scope; no explicit direct-edit grant exists. (b) like/flag write routes: RESTORE vs TOMBSTONE — dropped-at-B.W3 capabilities with the pre-B.W3 routes on record. | `52bdcf5^` routes; `gallery.py:3-9`; C8; P4.5 | **(a) direct-edit grant requested** (C8 scale favors it); **(b) RESTORE both** patterned on value's votes/flags twins — but only by owner pull-in, never auto-committed. |

Removed from the docket: OD-api-1 (STAY — decided by addendum C7; executes as the W7
supersession tombstone). The program's 8 rows → 10 that are REAL owner calls with both sides'
records attached.

---

## 4 — SEQUENCING SKETCH

Day-one, in parallel: **W7** (value structure, no deps) · **W3** (oklab, no deps) ·
**W6(b)(d)(e)** (fourier mechanical, mode-gated) · **W1** (census). Then **W1 → W2** (contract
+ docket) → owner ratification → **W4** (remix rail) + **W5** (value convergence) +
**W6(a)(c)** (fourier convergence + pulled-in trio) → **W8** (witness close). Critical path:
W1 → W2 → OD → W4/W5/W6 → W8. The first product-code lands day-one (W3), not behind three
process waves.

---

## 5 — LANDING MAP (every canon row → landing; zero silent drops against the GENERATED census)

| Canon row | Landing |
|---|---|
| **C7** (stays at /api; full spec; module structure BOTH repos; fourier isomorphism) | W2 (spec/contract) · W7e (STAY tombstone — decided, not re-litigated) · W7/W6d (structure both backends; kf NO-api NULL re-recorded) · W4/W2/W6/W8 (isomorphism) |
| **C7 ↔ P3.2 EXTRACT tension** | DECIDED: addenda win; W7e supersession tombstone names P3.2's row (§7 law 1 discipline) |
| **C6** backend slice | W2 share section (unlisted primitive; no-token-in-URL as SERVER-side conformance; /shares + OD-api-H); client slice seamed to the frontend program (sweep-value-demo §4) — not owned, not dropped |
| **C8** (mega-tranche; fourier in scope) | 8 waves, 2 fourier-side; OD-api-J governs execution mode |
| **C11/§4** (tests displaced; backend treatment befitting the language) | W7a (value, born-RED, 28 files) · W6d (fourier, born-RED, two homes + shim) |
| **C13** | W3 rider post-4.1.x; OD-api-A sub-ruling (no server color-math) |
| **C16/C17/C20/C21** | isolation honored (this file is the only write) · per-wave routing + model_served law · thrice on contract + program with the deviation annotated (W2) · 8 waves, no harness, no doc-vs-doc CI gates, day-one code |
| **L1 §4 backend law** | W7 + W6d + v3 layer vocabulary (W2) |
| **L1 §7 law 1** (tombstone + capability-diff) | committed route-census baseline + CI diff BOTH repos (W7d/W6e — implementable, replaces diff-vs-prior-tag) · fork tombstone iff retired (W4) · P3.2 tombstone (W7e) · like/flag restore-vs-tombstone (OD-api-J) |
| **L1 §7** (no masked fallbacks / chronics decided / partials disposed / records cited) | OD-api-D names the D3 overturn · TA-4 decided via OD-api-A with consumer-in-gate (third ride forbidden) · OD-api-E carries D2 §3 P2 · W8 terminal dispositions |
| **P1.5 defect-family register** | W3 (api-side member of the duplicate-color-parsing family) |
| **P3.2 deps-block row** | structure program's property; adjacent non-drop pointer (unchanged) |
| **P3.4 row 1** (library gates producer+api) | W7 preserves the api CI lane |
| **P4.5** | OD-api-J (adapted for fourier; kf N/A — no api) |
| **Sweep TA-4 / §1.3 / §3.4A** | W4 (rail, born-RED ×3 + consumer gate) · OD-api-A · W3 |
| **Sweep §3.4B** (votes/flags/oklab-search/tier/hashed-tokens) | W6b (session hash, wave) · OD-api-J (like/flag, docket) · oklab-search + tier = value-unique KEEP rows in v3 |
| **Sweep §3.4C** (all divergences) | W1 census (SUPERSET — adds restore/revert/versions:hash/gallery-alias/born-visibility) → W2 verdicts → OD-api-B/C/D/E/F/G/I → W5/W6 landings |
| **Sweep §3.5** (no cross-repo harness) | W8 (existing batteries; vector corpus killed) |
| **Sweep PART 2** (kf NO api) | NULL record, W1 census + this map |
| **Sweep synthesis 1–4** | OD-api-A · W4 · W6b/OD-api-J · W2+W5 — synthesis 4's non-null-owner recommendation RESTORED (OD-api-B) after the program silently reversed it |
| **Sweep-value-demo §6/§4** | W4 demo rider (client migration, DELTA mandatory) · W5a (stale-cursor 400 UX) · W2 share seam |
| **P0.1** (declared ≠ served) | every seat reports model_served |

Rows NOT landed here (C2/C3/C4/C5/C10/C12/C14/C15/C18/C19/C22; P1–P2 parser/color; P3.1/P3.3
kf; P4.1–P4.4) remain OTHER arm-B programs' property — named for the union pass.

---

## 6 — ADJUDICATION LOG (every skeptic claim → verdict, on the adjudicator's own evidence)

### Skeptic 1

| # | Claim | Verdict | Evidence + disposition |
|---|---|---|---|
| A1 | OD-api-4's "anon = live product affordance" is uncited/false; program silently reverses sweep synthesis 4 | **PROVEN** (adopted: OD-api-B re-cut, recommendation CONVERGE, both readings + census on sheet) | My probes: `usePaletteActions.ts:40-45` (ensureUser+ensureSession, early return), `useSlugMigration.ts:34`, `useUserAuth.ts:99-106` (auto-register), `routes/crud.ts:74-75` (session required) + `:85` (`userSlug ?? null`); sweep line 234: "align anon-ownership policy (recommend fourier's non-null owner)". No shipped client exercises anon create. |
| A2 | F-11c overturns the sanctioned D2 §3 P2 record uncited | **PROVEN** (adopted: OD-api-E; convergence now owner-gated) | `platform/http/idempotency.ts:34-36` verbatim: "the sanctioned single-replica KISS relaxation — D2 §3 P2… contract-faithful regardless of backing". |
| A3 | OD-api-6 mislabels a documented semantic as "masked fallback" | **PROVEN** (adopted: OD-api-D carries BOTH records; converge kept but as a named D3 overturn) | `crud-list.ts:53-56` verbatim: "Befitting graceful per D3… the documented pagination semantic, not a silent data-loss. (D.W2 Lane D rationale.)"; fourier `cursors.py:61-66` stale-sort 400 confirmed. |
| A4 | F-04 framing wrong (D-HARDEN-3 deliberate cure) + equivalence gate vacuous without tolerance | **PROVEN** (adopted: W3 reframed as the pre-authorized migration; stated tolerance ≤1e-6 vs the 0.15-radius consumer) | `oklab.ts:10-21` header confirms D-HARDEN-3 §3 W4 + the in-file replacement order; `crud-list.ts:167` `colorRadius ?? 0.15`. |
| B1 | Restore-verb divergence dropped entirely | **PROVEN** (adopted: W1 census row + OD-api-F, recommendation RATIFY on value's intact record) | fourier `visualizations.py:430-457` live; value `crud.ts:160-165` V·W45 retirement note verbatim; absent from sweep §3.4A/B/C and all 8 program OD rows. |
| B2 | revert + versions/:hash value-has/fourier-lacks, unrowed | **PROVEN** (adopted: W1 row + OD-api-G) | value `routes/versions.ts:43` (get-by-hash) + `:49` (revert); fourier route census: `@router.get("/{slug}/versions")` at `:860` is the only versions route. |
| B3 | F-05's gate closes green over an incomplete universe (+ gallery alias unrowed) | **PROVEN** (adopted: W1 machine census from both live registries; W2/W8 gates run against the GENERATED table) | §3.4 read whole this session: no restore/revert/versions:hash/born-visibility rows; `GET /api/gallery/cursor` live (`routers/gallery.py:37`). |
| C1 | F-08e "diff vs prior tag" unimplementable | **PROVEN** (adopted: committed route-census baseline + CI diff, both repos — W7d/W6e) | `git tag` = 31 library/tranche tags (v0.6.0…v4.0.0), zero api tags; api at `2.0.0` (api/package.json:3); `git ls-files | grep openapi` → only `modules/meta/openapi.ts` (generator; nothing committed to diff). |
| C2 | F-01's gate hostage to another wave | **PROVEN** (adopted: W2's verdict gate closable by its own work; census-terminal gate moved to W8) | Program text: "cannot close green until OD-api-2 lands a verdict and its wave discharges" — structurally un-closable, the partial-counted-as-done risk. |
| C3 | F-05's gates cannot fail | **PROVEN IN PART** (adopted: mechanical RED = generated-row-without-verdict; thrice convergence retained — it is canon process (C20), not the defect; the defect was the absent mechanical check over an unaudited list) | Combined with B3's evidence. |
| C4 | F-07's mirror-sync invariant unenforceable; existing batteries suffice | **PROVEN** (adopted: F-07 killed; W8 lands rows in each repo's own battery) | value `api/test/conformance/` (8 suites, listed) + fourier `api/tests/conformance/` live; any real hash-equality check IS the forsworn cross-repo step — convergent with skeptic2 A5. |
| C5 | F-02/F-10 too thin to be waves | **PROVEN** (adopted: 02 folded into W7; 10's spec folded into W2 + OD-api-H) | F-02 = 1 lint rule + tombstone (own gate labeled born-GREEN); F-10 = spec + banked facility. |
| D1 | Six of eleven waves are documents; spec family split across 3 waves/2 homes for no reason | **PROVEN IN PART** (adopted: one contract home (W2); code lands in 5 of 8 waves, W3 day-one. C7 does mandate the spec — consolidation, not deletion) | Program structure read whole. |
| D2 | F-09c thrice over a foregone conclusion | **PROVEN** (adopted: layout adjudication folded into W2's single contract thrice) | Program text carries its own going-in "recommendation: KEEP" — a thrice loop to ratify it fails C17 cost law. |
| D3 | Contrived dependency chain delays free wins | **PROVEN** (adopted: W7 day-one, zero deps; ordering law value-side only) | Program: 08 ← 02 ← {OD-api-1, 01}; displacement is mechanical, the "shared check home" is file-layout convenience. |
| D4 | F-03's recipe unpinned; the commit is a8ff7792 | **PROVEN** (adopted: pinned `a8ff7792^` recipe in W4) | `git log`: `a8ff7792` "refactor(T.W1 · api · TA-4): excise the write-only atom-diff apparatus", 2026-07-10; tree contains `api/src/lib/crud/atomdiff.ts` at `a8ff7792^`. |
| D5 | DELTA mis-scoped as conditional; fork IS surfaced today | **PROVEN** (adopted: DELTA mandatory; UI migration in-wave, W4) | `useVersionHistory.ts:102` `forkPalette` call; client `api/versions.ts:43-50`. |
| D6 | Fourier execution ownership asserted, not granted | **PROVEN IN PART** (adopted: OD-api-J mode row. C8 genuinely puts fourier in-scope, so in-tranche is a plausible reading — but no direct-edit grant is articulated anywhere; the question is real and cheap to ask) | C8 text; P4.5 names kf only. |

### Skeptic 2

| # | Claim | Verdict | Evidence + disposition |
|---|---|---|---|
| A1 | OD-api-3 refuted by F-03's own gate: fork=remix-with-zero-overrides ALWAYS 422s under the fourier no-op guard | **PROVEN — and strengthened** (adopted: OD-api-A trichotomy; the adjudicator ADDED value's own J.W2 empty-diff precedent as arm (iii)'s on-tree basis) | fourier `visualizations.py:536-542` (empty `atom_ops` → 422; atoms exclude title/tags); `a8ff7792^:api/src/services/palette/forks.ts`: "fork IS remix-with-empty-diff: ONE code path" — value's own history contradicts the 422 gate the program paired with fork-retirement; sweep §3.4A even names the 422 "the fourier tightening over value.js's verbatim-copy fork". |
| A2 | F-03 re-books the TA-4 chronic (write-only, third ride) via the conditional consumer | **PROVEN** (adopted: live demo consumer INSIDE W4's acceptance gate; write-only close forbidden) | `a8ff7792` message: "write-only… the demo consumes neither /remix nor /diff… atomDiff column has NO reader". |
| A3 | Creation-default visibility divergence missed; OD-api-4's invariant vacuous-by-birth | **PROVEN** (adopted: born-visibility row in W1/W2; OD-api-B re-cut around anon-create + born-visibility) | value born `visibility:"public"` at create (`service/crud.ts`, probed) and fork (`service/forks.ts`, probed); fourier default `draft` (`models/visualization.py:120,:183`); publish/unpublish owner-gated (`routes/publish.ts:54-64`) → null owner never passes; anon rows are born public and never touch the publish verb. |
| A4 | Process mass violates C21; two gates resurrect the retired proof idiom; F-04 needs no spec dependency | **PROVEN IN PART** (adopted: W3 ships day-one with zero deps; one normative home; the no-token-in-URL check becomes a SERVER-side route/schema conformance assertion. PARTIAL on the row-inventory: a formation registry is bookkeeping, not the retired repo-level grep-proof idiom — kept as bookkeeping, never a permanent CI gate; the permanent gate is the committed route baseline, which is structural) | Memory law (feedback-proof-idiom-retired) confirmed; program dependency graph read whole. |
| A5 | F-07 is redundant apparatus (mini-harness) | **PROVEN** (adopted: killed; W8 uses existing batteries; contract text carries canonical examples) | Convergent with S1-C4; both repos' conformance suites verified live. |
| A6 | Dual normative authority (SPEC.md vs v3) | **PROVEN** (adopted: v3 sole normative home; value SPEC = generated openapi + thin index) | P4.4's parallel-authority principle; no drift gate existed between the two homes. |
| A7 | F-02 not a wave; F-08 ordering wrong-repo for F-06; OD-api-1 re-litigates a decided addendum row | **PROVEN** (adopted: 02 folded into W7; fourier test ordering governed in W6; OD-api-1 off the docket → W7e supersession tombstone) | C7 addendum text IS the owner ruling + "addenda WIN"; the addendum's "reconcile explicitly" is satisfied by the named tombstone, not a fresh STAY-vs-EXTRACT question. |
| A8 | (a) fork retirement unpriced (api semver + split-deploy window); (b) fourier's write sequence topology-unsafe as recipe | **PROVEN** (adopted: W4 pricing row — api 2.0.0→3.0.0 major + deploy-order window; isomorphism declared CONTRACT-level; code recipe = `a8ff7792^` under withTransaction) | fourier remix docstring: "NO Mongo transaction — standalone-topology-honest" (`visualizations.py:493-496`); value fork/remix `withTransaction` H1 site (`service/forks.ts:32-40,:94`); api version 2.0.0 confirmed. |
| A9 | F-04 gate underspecified for denormalized at-rest data; framing = pre-authorized migration | **PROVEN** (adopted in W3: stated tolerance, backfill/no-backfill decision row, acceptance-domain matrix on the OD sheet) | `oklab.ts:10-21`; radius-search consumer `crud-list.ts:159-181` (`?? 0.15`). |
| A10 | Fourier gap trio at wrong altitude; likes may re-litigate a decided prune | **PROVEN — on corrected evidence** (adopted: like/flag → OD-api-J restore-vs-tombstone; session-hash stays a wave). The skeptic's own citation ("liked_ips stripped viz.py:80") is only a response-projection strip — WEAK; but my git probe found the stronger fact: pre-B.W3 fourier SHIPPED `POST /gallery/{hash}/like` AND `POST /{hash}/flag` (`52bdcf5^:api/routers/gallery.py:218,:295`), carved out at B.W3 and never re-landed (`gallery.py:3-9`) — a record-bearing drop, hence a docket row, not auto-implementation | As cited. |
| A11 | Thrice under-application unexplained | **PROVEN IN PART** (adopted: explicit C20-deviation annotation in W2, owner-visible; thrice concentrated on the contract + this program-level loop itself — the C17 cost bound is the stated rationale, no longer silent) | C20 text vs program routing notes. |

### Adjudicator-added finding (neither skeptic caught it)

| # | Finding | Disposition |
|---|---|---|
| ADJ-1 | The program's F-11b premise and born-RED gate — "visibility transition guard… value has none; RED today (no guard)" — is **FALSE ON TREE**: `assertVisibilityTransition` is live at `service/visibility.ts:51-59`, called at `:88`; a malformed target already throws 422. All 9 from→to resting tuples are legal BY DOCUMENTED DESIGN (J.W1c §5.1), with the `from` param recorded as the composition point for any future forbidden-pair rule. A third instance of the fourier-is-right-over-value's-decided-records disease both skeptics diagnosed. | OD-api-C carries both records; W5b's gate re-specified (lands only IFF v3 defines forbidden pairs; the "no guard" born-RED claim deleted). |

**Convergence duty check**: skeptic1's strongest surviving points (census incompleteness →
machine census W1; record-blind convergence → the 10-row docket with records; unimplementable
gates → committed baselines) and skeptic2's strongest (the fork/remix contradiction →
OD-api-A trichotomy; write-only third ride → consumer-in-gate; process mass/single home →
8 waves, v3 sole authority) are all load-bearing in the amended program. Where the two
skeptics converged independently (kill F-07; fold F-02; day-one mechanical hygiene;
fourier-altitude caution), the final adopts the convergent fix outright.

— Arm B, F-PE-api AMENDED FINAL. 8 waves · 10 OD rows · every born-RED gate mechanical and
live-defect-anchored · every overturned record named on the sheet. Adjudicator: thrice-PE-api,
2026-07-19.
