# FINAL PROGRAM PA-PARSE — parse-that readoption + spec-completeness (arm B, thrice-adjudicated)

**Adjudicated:** 2026-07-19 · thrice loop over `armB/program-PA-parse.md` vs skeptic1 + skeptic2 · every claim re-proven on tree, never vote-counted. Firewall honored (no vnext / snapshot-vnext / armA / r1-opus-refuted).
**Pins (G0′, re-verified this seat):** value.js `tranche-u@c654824e` (published 4.0.0 immutable) · parse-that `master@ef10d5b` == npm `1.0.0` · keyframes-v-exec `master@81a56990` · bbnf-lang `master@af15f63e0` · resurrection base `164343c1^` = `a992b8e6` (19-file `src/parsing/` incl. CLAUDE.md = 18 TS files; 11-file `bench/`; `"@mkbabb/parse-that": "^1.0.0"` pin — all re-verified).
**Shape:** 7 tranche waves + 1 formation-phase pilot (W0, phase-labeled per §7-law-4). The original 9-wave shape is superseded: F-05 was double-booked against the deposed tree's own contents, F-09 was sand, F-01 was formation work mis-booked; C12 gains the execution lane the original lacked.

---

## 1 — PROGRAM CHARTER (amended)

1. **The decree is input, not a question (P1.6):** `@mkbabb/parse-that` is READOPTED outright from the registry (`^1.0.0`); the v4 regex/char-split parser is retired unconditionally. The bench is a regression WITNESS — it tunes, never re-contests.
2. **The honest name is RESTORATION-BY-RETYPE.** The deposed tree's grammar SKELETON (productions, sequencing, combinator leaves) is reused; its entire semantic layer is rewritten: 10 of 18 deposed TS files import the deleted `src/units` OO model (`ValueUnit`/`ValueArray`/`FunctionValue` — tombstoned RIGHTLY, never re-litigated), the error layer is throw-based `CSSParseError extends Error` (deposed `index.ts:518`) vs v4's `ParseResult`/`ParseIssue` Result discipline, and export-name overlap with the frozen surface is ZERO (`parseCssColor` grep over the deposed tree = 0). The retype is Fable-grade design; per-production sweeps under Fable-authored transposition patterns are Opus work (C17 honored, inversion corrected).
3. **The `/css` public surface is SIGNATURE-frozen, machine-censused**: the live `src/css/index.ts` exports **19 values** (7 grammar + 1 syntax + 3 timeline + 8 stylesheet) **+ 33 types** (ParseIssue and ParseResult are INSIDE the 33 — never double-counted). The freeze is the letter's actual law — `parseStylesheet`/`collect*`/`ParseIssue` **signatures preserved, consumers never move** — not a byte-freeze of the 33 types; DOM-congruence type motion (C12) is additive and rides the OD-PA-6 surface-thaw ruling. The freeze baseline is a machine-derived export census vs the 4.0.0 tag, generated in-wave — never a hand count.
4. **parse-that itself: ZERO motion by default (C1/C19)**; bounded fix-on-trip contingency RIDER on W3 (not a wave); semver ceiling **1.1 absolute** (C15). All tape/substrate/Pratt/diagnostics-redesign work is the BBNF session's; needs route as PT-E-channel letters.
5. **Spec target: July 2026 (C12)** — with the "ENTIRE CSS spec" bound surfaced as an OWNER-DECISION row (OD-PA-1), never silently narrowed: selectors, media queries, gradients, filters, transforms all exist as modules in bbnf's `grammar/css/l4/` 15-module exemplar and are in-or-out by ruling, not by omission.
6. **The parser is DUALLY defined (C14)**: idiomatic BBNF sub-grammar modules + the parse-that TS implementation in `src/css/`; congruence enforced by **behavioral equivalence over a shared conformance corpus** (output-diffed), never a production-name map (the owner retired the grep-ledger gate genre as "overfit junk"). The dual definition names and overturns the P2.2 "BBNF grammar files" RIGHTLY tombstone explicitly (P4.4: silence re-litigates; C14 is the addendum that wins).
7. **Addendum-2 reconciliation row (recorded, not smuggled):** "all regex-based parsing abrogated" binds the AD-HOC regex/char-split parse paths on the value side; parse-that's own `regex()` leaf (`typescript/src/parse/leaf.ts:317`, used by the deposed grammar at 10+ sites) is engine-internal machinery and survives by this ruling. The kf half of addendum 2 gets a REAL census (W7): regex-method call sites exist in EIGHT kf zones, not one.
8. **Standing laws bind every wave**: born-RED only where genuinely RED on a real measurement (no red-by-absence, no dead-green); silent-drop tombstone law (§7-1); anchors re-derived at execution (§7-3); KISS/parsimony (C21); thrice loop (C20); probe parsimony — π/DELTA only where a visual claim exists.

---

## 2 — THE WAVE SET

### W0 · FORMATION-PHASE PILOT — the transposition pilot + the record measurement (C14-NOW; phase-labeled, not a tranche wave)
- **Phase label (§7-law-4):** this is formation work — C14's "prototyped and benchmarked NOW; tranche EXECUTION is not" — its outputs enter the tranche as evidence.
- **Intent:** Pilot the REAL risk by name: the ValueUnit→v4-typed-node RETYPE of 2–3 deposed productions (one `.map()`-heavy color path, `parseTimingFunction`, one stylesheet rule), throw→Result error-layer transposition included; BBNF twins drafted FROM bbnf's `grammar/css/l4/` split; the parse-that mechanics themselves need no proof (26 months in service, v1.0.0→v3.1.0).
- **Deliverables:** the transposition pattern note (which half is reused, which rewritten — per-layer); bench harness draft from `164343c1^:bench/css-parse-perf.mjs` **with the memo-defeat rule named** (the deposed entry parsers are memoized at 7 sites — `index.ts:542/:618/:627`, `color/color.ts:721`, `stylesheet/stylesheet.ts:637`, `animation-shorthand.ts:164`, `units.ts:115`; LRU-capped 4096 per `utils.ts:15` — a non-defeating harness measures cache-hit fiction); **the ONE regex-parser record measurement** (numbers + corpus hash — taken once, ever; W2 inherits it); a BBNF-home cost note for OD-PA-5.
- **Gates:** prototype output equivalence on the shared corpus with a BOUNDED divergence rule — mismatches admissible only where the regex incumbent is spec-WRONG per cited spec text (later formalized by W1 vectors); thrice convergence on the transposition pattern. The gate can fail: unbounded divergence or an unworkable pattern escalates to the owner before W3 is specced.
- **Routing:** Fable designs the transposition + BBNF twins; Opus runs diffing and bench executions.

### W1 · value.js · SPEC CENSUS + THE PRE-SWAP DECISION DOCKET (C12 census; regex-tree-FIRST)
- **Intent:** The July-2026 census on TODAY'S tree — no parser dependency (the original's F-03 dependency was false by its own parenthesis); born-RED vectors authored here are genuinely RED on the regex parser. PLUS the single owner sheet of every decision the grammar cut needs BEFORE it is specced.
- **Deliverables:** the census ledger (values/color/easing/animations/scroll-timeline/stylesheet/CSSOM typed-value congruence/WAAPI keyframe formats), one row per production/surface: IN-DEPOSED-TREE / GAP / DECISION, DOM-equivalent type named per row (the deposed-tree column exists because color-mix, contrast-color, relative-from, HDR, spring are ALREADY in the resurrection base — the census must distinguish restore-rides-in from genuinely-absent); the kf-leverage map (input to W7); the ROUTE row for kf's full-WAAPI ENGINE support (carrier = W7's letter, by name); **the decision docket as ONE owner sheet**: OD-PA-1 (spec-scope bound), OD-PA-2 (R-HDR keep/excise-at-cut), OD-PA-3 (spring grammar ownership), OD-PA-4 (R-EVAL calc arm — the deposed grammar WIRES the 536-LOC math evaluator into its root `ValuesValue` production while v4 css has zero calc hits; the cut cannot be specced without this verdict), OD-PA-5 (BBNF home), OD-PA-6 (surface thaw), OD-PA-7 (release vehicle).
- **Gates:** born-RED conformance vectors per GAP row, all RED on the extant tree by construction and by RUN; ledger closes on registry stability (second independent pass surfaces zero new rows); the docket sheet delivered before W3's spec is authored — a hard sequencing gate.
- **Dependencies:** none (program opener; W0 evidence folds in).
- **π/DELTA:** none. **Routing:** Opus enumeration sweep; Fable adjudicates DOM-congruence typing design and authors the docket briefs.

### W2 · value.js · BENCH WITNESS — bench/ + the portable ratio gate
- **Intent:** Resurrect the parser benches (`css-parse-perf.mjs`, `parser-namelookup.mjs`) + the portable ratio gate (MB/s + ns/call + co-scaling ratio) on the producer gate surface (P3.4-1). The regex record is INHERITED from W0 — measured once, ever.
- **Deliverables:** `bench/` restored (parser benches; the 9 color benches are the color program's — seam cited); the ratio gate wired as a `proof-perf-target`-class producer CI step; memo-defeat rule enforced in harness; **the deadlock disposition**: if the rebuild cannot meet the recipe ratio after **3 tuning iterations → owner escalation** (L1 §8) — the seat is never re-contested; the TARGET is re-derived under owner eyes.
- **Gates:** **born-RED honestly** — RED = ratio UNMET on a real measurement of the WIP rebuild (W3 in flight), never red-by-absence; goes green only when W3 meets the recipe.
- **Dependencies:** W0 (harness + record), W3 opened (a real measurable subject exists).
- **π/DELTA:** none. **Routing:** Opus runs; one Fable seat derives the ratio target.

### W3 · value.js · THE SEAT REBUILD — restoration-by-retype, /css signatures frozen, consumer gate inside
- **Intent:** The swap, honestly named and singly cut: parse-that `^1.0.0` re-added per the historical pin convention; the deposed grammar skeleton resurrected **with its module cut PRESERVED** (the deposed tree is already goldilocks: 18 TS files, max 754L; `stylesheet/` is 5 files, max 643L — the "899-LOC god-module" indictment names the dying v4 regex file, not the replacement; any further re-cut is the structure program's gated wave); the semantic layer transposed to the v4 typed model per W0's pattern; the calc/HDR/spring arms cut per W1's verdicts — decided-YES rides in, decided-NO is excised WITH by-name tombstones (§7-1); the regex parser retired; `deepFreeze`-per-parse and the per-char `/\s/.test` loops die with it.
- **Deliverables:** the restored combinator core in `src/css/` behind the frozen surface; the memo-policy row adjudicated explicitly (the deposed LRU-4096 cache is a design decision, not a smuggled default); **manifest-gate WHITELIST rule** (parse-that is a REAL runtime dep; the deps-STRIP gate must never strip it); U-F29/U-F30 defect-FAMILY regression tests + family register row; CHANGELOG engine-swap entry; **the export-census diff vs the 4.0.0 tag run as an in-wave script** (the standing capability-diff GATE build belongs to the gates program — this wave cannot claim an unbuilt gate green; it runs the check itself and archives the output); packed-surface re-verification (the D50-class check for any behind-surface motion); restored-parser tests land in the isomorphic tree (`test/css/`), the ghost `test/parsing/` dir stays dead.
- **Gates:** entire existing css corpus green UNCHANGED; producer+api green; W2 ratio RED→GREEN (headline discharge); **kf suite green against a packed value build carrying the rebuild — the dominant consumer (29 sites/39 symbols) gates INSIDE the swap wave, link-level, pre-publish**; packed-surface re-verify green; the corrected predicate: **zero ad-hoc regex/char-split parse paths in `src/css` outside parse-that combinator API calls** (defined, greppable, failable — the deposed tree's `regex()` leaves are lawful per charter §7); anchors re-derived at execution.
- **Dependencies:** W0 (pattern), W1 (docket + vectors), W2 (witness live).
- **π/DELTA:** none — the swap makes no visual claim; the pixel-stable-swatch capture is CUT (weak-equivalence theater; probe parsimony).
- **Routing:** Fable authors per-domain transposition specs + adjudicates; Opus sweeps per-production under them.

### W4 · value.js · GRAMMAR COMPLETION — decided-YES typed exposure + serialization + vector flip
- **Intent:** Complete what the swap carried in skeleton: expose color-mix / relative-from / contrast-color (+ decided-YES HDR/spring) through the /css typed surface — the deposed productions emit ValueUnit graphs; surfacing them through `parseCssColor`'s `ParseResult<CssColor>` requires additive typed nodes under the OD-PA-6 ruling. NOT a phantom restore wave: the productions exist; the typed exposure, serialization round-trips, and conformance discharge are the real work.
- **Deliverables:** additive typed nodes for the new forms; `serializeCssColor` round-trips (U-F30 family extension); W1 vector flip with **an honest re-baseline step**: re-verify which W1 vectors already went green at W3 (anchor re-derivation discipline — no stale RED claims survive to the wave spec); tombstone rows confirmed for every decided-NO; the ParseIssue-union policy row (stays CLOSED unless a new grammar genuinely needs a named code — then the P5 §D name-a-code path FIRES and the change is adjudicated, never smuggled); WCAG-republish half of contrast-color seam-cited to the color program.
- **Gates:** born-RED = the surviving W1 vectors for the exposed forms, RED at wave open by RUN (not by citation), GREEN at close; legacy corpus unchanged-green preserved separately from new-capability RED→GREEN; kf/glass/demo consumer suites unchanged-green; W2 ratio stays green.
- **Dependencies:** W3, W1. **π/DELTA:** ONE DELTA capture — the demo color input accepting a `color-mix()`/relative-`from` string (first user-visible new capability); single capture.
- **Routing:** Fable designs node shapes (DOM-congruent per C12); Opus implements; thrice per production cluster.

### W5 · value.js · C12 EXECUTION — DOM-congruence typing + WAAPI keyframe/animation surfaces (the lane the original lacked)
- **Intent:** Implement the census GAPs genuinely absent from the deposed tree: WAAPI keyframe-format typing (per-keyframe `composite`, `computedOffset`, `pseudoElement`, `KeyframeEffectOptions` equivalents), animation/timeline gaps, CSSOM typed-value congruence (CSSStyleValue/CSSNumericValue/CSSUnitValue/CSSKeywordValue/CSSTransformValue equivalents) — every typed value congruent to its DOM equivalent, under the OD-PA-6 surface ruling; any OD-PA-1-ruled scope extensions (selectors/media/gradients/filters/transforms) land here or spawn owner-sized follow-ons via the registry, never silently.
- **Deliverables:** the typed surfaces + parsing per ruled scope; type-surface motion defaults to the co-land boundary vehicle (P4.2) unless OD-PA-6 rules additive-minor; the kf-leverage handoff rows updated for W7.
- **Gates:** born-RED = W1's non-color GAP vectors, RED by RUN at open; census ledger rows flip to SUPPORTED with the DOM-equivalent congruence verdict recorded; no unowned GAP remains (every row SUPPORTED / waved / owner-docketed).
- **Dependencies:** W1, W3 (substrate), OD-PA-1/6. **π/DELTA:** none. **Routing:** Fable typing design; Opus implementation sweeps.

### W6 · value.js (bbnf-lang read-only exemplar) · THE DUAL DEFINITION (C14) — BBNF suite + behavioral-equivalence gate
- **Intent:** The full BBNF sub-grammar module suite (bbnf `@import` facilities; reconciled against the `grammar/css/l4/` 15-module exemplar) covering the W3+W4 (+W5 grammar-relevant) scope, at the OD-PA-5-ruled home.
- **Deliverables:** the module suite; **the congruence gate as BEHAVIORAL EQUIVALENCE** — both definitions run over the shared conformance corpus, outputs diffed; RED while any corpus family diverges (no production-name map: "production" has no stable machine identity across heterogeneous representations, and the name-ledger genre is owner-condemned); the tombstone act: the P2.2 "BBNF grammar files" RIGHTLY row is NAMED and overturned by C14's authority (P4.4 discipline — the excised prior art is cited as history, never as supporting precedent: it was already gone at `164343c1^`); the upstream-reconciliation ask letter to the BBNF session (grammar FILES authored here; bbnf-lang runtime/repo evolution is theirs per C1).
- **Gates:** born-RED — the equivalence corpus runs at wave open with the BBNF twin absent/partial and FAILS on real divergence counts (not on missing files: the runner exists from W0's pilot); green = zero diverging families both directions; thrice loop on the module cut.
- **Dependencies:** W0 (method piloted), W3, W4. **π/DELTA:** none. **Routing:** Fable decomposition; Opus scaffolding.

### W7 · keyframes.js (spec-dispatch per P4.5) · THE KF BOUNDARY LETTER + THE REAL ADDENDUM-2 CENSUS + ROUTE CARRIER
- **Intent:** ONE `<SENDER>-INBOUND-*` letter into `keyframes.js/docs/tranches/V/coordination/` — value DIRECTS, the kf successor implements; never a direct cross-repo edit.
- **Deliverables:** (a) the **easing.ts consume-vs-ratify brief** (re-verified at kf `81a56990`: `CSS_NATIVE_KEYWORD` regex + `CSS_FUNCTION_EASING` regex, the two classifier rows) — RATIFY-lean, both dispositions priced → OD-PA-8; (b) **the kf-wide addendum-2 regex census** — the enumerated reality, not n=1: regex-method call sites (`.test/.exec/.match`) across **eight zones** at `81a56990`: compile 4 · easing.ts 2 · engine 1 · ingest 1 · orchestration 2 · presets 1 · resolve 1 · svg 1, with `resolve/browser.ts:43` (`/^(sv|lv|dv|v)?(w|h|i|b|min|max)$/.exec(unit)` — viewport-unit classification) as row 1; each site adjudicated parser-vs-classifier IN the letter; (c) the **kf-leverage spec** from W1/W5 (the typed keyframes/animation/timeline surfaces for `compile/selector.ts` / `validate.ts` seams); (d) **the WAAPI-ENGINE route letter** — the carrier for W1's ROUTE row (`persist()`, `pseudoElement` targeting, `iterationComposite`, `getAnimations()`, `CSS.registerProperty`, `updatePlaybackRate`, per-keyframe composite, eligibility liberalization → the kf program's WAAPI wave); (e) the boundary census-lock statement — zero grammar productions both sides — **labeled a STANDING REGRESSION LOCK, explicitly not born-RED** (it is green today per P1.4; it locks, it does not discharge); (f) the seam re-verification protocol note (the packed-build gate itself lives in W3 and is already discharged there).
- **Gates:** the letter landed + rowed in kf's ledger; census-lock grep row green on both trees; every route row carries a named owning destination.
- **Dependencies:** W1, W3, W5. **π/DELTA:** none. **Routing:** one Fable seat (coordination prose is design); Opus census mechanics.

### Registry rows (not waves — F-09 dissolved)
- **PT-E1** (scoped diagnostics, HIGH) → ROUTED to the BBNF session; value's restoration noted as the live consumer (1.1-class work, forbidden here).
- **PT-E2** (`Parser<any>` inference leak, MED) → **fix-on-trip contingency RIDER on W3**: iff the rebuild trips it, a typing-only 1.0.x patch; fallback = consumer-side annotations; Fable adjudicates fix-vs-evolution (C1 boundary); any publish shows version ≤1.1.x (C15) with diff limited to the fix class.
- **PT-E3** (Pratt) → DORMANT/RETIRED stands (S.H3).
- Module-global diagnostics-buffer collision (parse-that `utils.ts`) → guard-class fix iff tripped; scoped redesign never smuggled.
- Untracked Sol B/T/U banks → provenance-only, routed to the BBNF session (C16).
- `collectDeclarations` export-orphan → FROZEN by surface law; prune is the structure program's dissolution wave.
- Addendum-2 reconciliation ruling (charter §7) → recorded registry row.

---

## 3 — OWNER-DECISION ROWS (docket delivered as ONE sheet at W1; pre-swap where they gate the cut)

| Row | Question | Recommendation | Gates wave | Timing |
|---|---|---|---|---|
| OD-PA-1 | **Spec-scope bound**: does "ENTIRE CSS spec" include selectors/media/gradients/filters/transforms (all present in bbnf's l4 exemplar)? | Bound to the /css surface's domains for THIS program; extensions ruled in land at W5 or spawn registry follow-ons — but the narrowing is RULED, never silent | W1/W5 | PRE-SWAP |
| OD-PA-2 | **R-HDR**: `ictcp()`/`jzazbz()` — KEEP-vs-EXCISE **at the cut** (the productions are in the deposed tree; post-swap they exist unless excised) | Keep-lean (the unpapered 11-day 3.1.0 drop; DOM/CSSOM congruence value); excision carries §7-1 tombstone duty | W3 | PRE-SWAP |
| OD-PA-3 | **spring() grammar ownership** (solver stays kf, K F6.6) | VALUE — CSS-spec territory; the deposed production (`timeline/easing.ts:143-153`) rides the retype; if kf-owns, excise-at-cut + spec routes via W7 | W3 | PRE-SWAP |
| OD-PA-4 | **R-EVAL**: the deposed 536-LOC calc/math evaluator is WIRED into the root `ValuesValue` production; v4 css has zero calc | RETIRE-lean (excise the arm at cut; DOM resolution status quo; kf `resolve/` owns calc) — with by-name tombstone; restore = capability ADD needing its own vectors | W3 | PRE-SWAP |
| OD-PA-5 | **BBNF home**: in-value vs extend bbnf's l4 suite | IN-VALUE — C1 assigns bbnf-lang evolution to the BBNF session, so landing our suite there crosses the ownership boundary; the duplication cost vs bbnf's existing 15-module corpus is PRICED in the brief, the "BBNF grammar files" tombstone is named-and-overturned, reconciliation routed by ask letter | W6 | pre-W6 |
| OD-PA-6 | **Surface thaw**: signature-freeze vs C12's exact-DOM-congruent types (additive node types for W4; congruence motion for W5) | Signature-freeze stands (the letter's law); ADDITIVE type motion lawful within it; any RESHAPING of the 33 rides the co-land boundary | W4/W5 | PRE-SWAP (frame), per-wave (application) |
| OD-PA-7 | **Release vehicle**: 4.1.x minor vs the value-5 co-land boundary | 4.1.x-eligible ONLY with precondition: the W0/W3 divergence ledger EMPTY or spec-corrections-only — glass peers value `^4.0.0` (verified), so a 4.1.x reaches every ^4 consumer outside co-land discipline; default = FOLLOW W56's SCI-1 cut, never ride it without that program's explicit pricing | W3 | PRE-PUBLISH |
| OD-PA-8 | **kf easing.ts:30/:38-39**: consume-vs-ratify the two-regex timing-name classifier | RATIFY — zero productions, documented light-engine law, F.W7 round-trip rationale; consume-variant priced in the brief | W7 | with W7 |

---

## 4 — ROW-LANDING MAP (zero silent drops; every original F-row re-lands)

| Source row | Lands as |
|---|---|
| L1 §2 wave (1) R-PARSER restoration | W3 (+ W0 pilot; vehicle → OD-PA-7) |
| L1 §2 wave (2) bench restore as regression witness | W2 (record measurement W0 — once, ever) |
| L1 §2 wave (3) spec census + named restores + spring row | W1 (census + docket), W4 (exposure), W5 (execution); OD-PA-1/2/3 |
| L1 §2 / P1.4 kf boundary lock + easing consume-vs-ratify | W7; OD-PA-8 |
| P1.2 extant-parser indictment | RED definitions in W1 vectors + W3 retirement |
| P1.3 / P1.6 tape + parse-that-internal OUT OF SCOPE | Charter §1.4; zero tape rows exist |
| P1.5 ad-hoc-fix FAMILY (U-F29/U-F30) | W3 family tests + register; W4 extends U-F30 |
| P2.2 row 4 R-PARSER | W3 |
| P2.2 rows 6/7/8 (color-mix, relative, contrast grammar half) | W3 skeleton-rides-in + W4 typed exposure (WCAG half seam-cited to color program) |
| P2.2 rows 10/11/12 (HDR, spring, eval) | OD-PA-2/3/4 pre-swap + W3 cut + W4 (YES) / tombstones (NO) |
| P3.4 rows 1/5 (producer+api; bench with R-PARSER) | W2 placement honors both |
| P3.4 row 4 / P4.2 (manifest gate; deps STRIP) | W3 WHITELIST rule |
| P3.4 row 6 / §7 law 1 (capability-diff + tombstones) | W3 in-wave census script vs 4.0.0 tag (standing gate build = gates program's, dependency honesty); W3/W4 tombstones |
| §7 law 2 (G0′) / law 3 (anchors) / law 4 (phase labels) | Header pins · every wave's gate step · W0's phase label |
| P5 PT-E E1/E2/E3 + §D name-a-code | Registry rows + W3 rider · W4 ParseIssue policy row |
| C1/C15/C19 | Charter §1.4; registry rows; C19 answered (adequate as-is) |
| C12 | W1 (census) + **W5 (execution — the amended lane)** + W7 (kf leverage); WAAPI-ENGINE half → W7 route letter (named carrier) |
| C14 | W0 (NOW pilot, phase-labeled) + W6 (suite + behavioral gate) |
| C17 | corrected routing: retype = Fable design + Opus sweeps-under-pattern |
| C20/C21 | thrice on W0/W1/W4/W6; 7 waves + pilot; one DELTA capture total; π cut where no visual claim |
| Addendum 2 (regex abrogation, both repos) | value half: W3 predicate + charter §7 reconciliation row; kf half: W7 eight-zone census |
| sweep-value-lib §5/§3 (test tree; collectDeclarations) | W3 isomorphic test landing; registry freeze row |
| sweep-parse-that §5/§6 | W3 recipe + WHITELIST; W0/W6 substrate |
| C16 (isolation; Sol banks) | honored; registry provenance route |
| Original F-01 | → W0 (phase-labeled) |
| Original F-02 | → W2 (RED redefined; escalation added) |
| Original F-03 | → W3 (honest name; consumer gate inside; re-cut removed; π cut) |
| Original F-04 | → W1 (dependency inverted to none; docket added) |
| Original F-05 | → dissolved into W3 (skeleton) + W4 (exposure/serialization/vectors) — the double-booking cured |
| Original F-06 | → OD-PA-2/3/4 pre-swap + W3 cut arms + W4 tombstones |
| Original F-07 | → W7 (letter + census + route carrier) + W3 (seam gate moved in) |
| Original F-08 | → W6 (behavioral gate; tombstone named) + W0 (pilot) |
| Original F-09 | → dissolved: registry rows + W3 contingency rider |

**Explicit non-ownership (routes, not drops):** kf full-WAAPI engine → kf program via W7 letter (named carrier). Color benches + WCAG republish + R-DELTAE/R-GAMUT/R-INTO/R-RAMP → color program. Isomorphism/structure/capability-diff standing-gate BUILDS → structure/gates program. The co-land boundary itself → program-level co-land wave.

**Wave count: 7 tranche waves (W1–W7) + 1 formation-phase pilot (W0).**

---

## 5 — ADJUDICATION LOG (every claim → PROVEN/DISPROVEN, my own evidence)

**Method:** every load-bearing claim re-derived on tree this seat: `git show/grep` over `164343c1^` (= `a992b8e6`), live `src/css/index.ts` at `c654824e`, kf `81a56990` zone census, bbnf-lang `grammar/css/l4/` listing, glass-ui `package.json`, parse-that `typescript/src/parse/leaf.ts`. No vote-counting; where both skeptics converged I still re-measured.

### Skeptic 1

- **A1 (double-booking: F-05/F-06 rows already in deposed tree) — PROVEN.** My greps: `color-mix()` parser at `color/color.ts:275/:312`; `contrast-color()` at `:398-:407`; `ictcpParser`/`jzazbzParser` at `:265/:270`; `spring()` at `timeline/easing.ts:143-153`; `relative-color.ts` whole file. The original program even cites `relative-color.ts` in F-03's intent while booking R-RELATIVE as an F-05 restore — internal contradiction confirmed. **Adopted:** F-05 dissolved; decisions moved pre-swap (OD-PA-2/3/4); W4 re-scoped to typed exposure + serialization + vector flip. **One correction to the skeptic:** post-swap the forms are in the SKELETON but not automatically exposed through `ParseResult<CssColor>` — typed-node exposure is real work, so W4 is a genuine wave, not a rider; its RED vectors are re-baselined at open by RUN.
- **A2 (retype not resurrection) — PROVEN.** 10 of 18 deposed TS files import `../units`/`../../units` (my line census, 13 import lines); `src/units` absent at HEAD (top-level = color/css/easing/foundation/quantize/subpaths/transform/value); OO model tombstoned RIGHTLY (P2.2). **Adopted:** honest name; C17 routing corrected; W0 pilots the retype by name.
- **A3 (broken "zero regex" predicate) — PROVEN.** Deposed `index.ts:1` imports `regex` from `@mkbabb/parse-that`; regex leaves at `color.ts:103/:129/:134/:188/:493`, `index.ts:30/:386/:495`, `math.ts:207`, `stylesheet.ts:63`; parse-that exports `regex` at `typescript/src/parse/leaf.ts:317`. **Adopted:** predicate rewritten ("zero ad-hoc regex/char-split parse paths outside parse-that combinator API calls") + the addendum-2 reconciliation recorded as a charter ruling (§7), not smuggled.
- **A4 (C12 has no execution wave) — PROVEN** on program text: F-04's GAPs feed only fixed-scope F-05/F-06. **Adopted:** W5.
- **A5 (frozen census miscounts; ParseIssue/ParseResult double-counted) — PROVEN.** Live `src/css/index.ts`: 19 value exports (7+1+3+8), 33 type exports WITH ParseIssue/ParseResult inside. Program said "15" then listed 19, and appended the two types separately. **Adopted:** machine-derived census, charter §1.3.
- **A6 (spec narrowing unsurfaced) — PROVEN.** bbnf `grammar/css/l4/` = 15 modules including `selectors/media/gradients/filters/transforms.bbnf` (listed this seat); F-04 enumerates none. **Adopted:** OD-PA-1.
- **A7 (gate-quality sweep) — PROVEN, all five limbs.** F-07 census-lock green-today (P1.4 census-PASS) → relabeled standing regression lock; F-02 red-by-absence → RED redefined as ratio-unmet-on-real-measurement; F-01 divergence escape hatch → bounded (incumbent-spec-wrong only); capability-diff "green by construction" while the gate build is routed away → replaced by an in-wave census script vs the 4.0.0 tag; the π swatch capture → CUT (no visual claim; probe parsimony).
- **A8 (addendum-2 kf census is n=1) — PROVEN.** My census at `81a56990` (`.test/.exec/.match` over src, tests excluded): compile 4 · easing.ts 2 · engine 1 · ingest 1 · orchestration 2 · presets 1 · resolve 1 · svg 1 — eight zones; `resolve/browser.ts:43` verified verbatim. **Adopted:** W7 deliverable (b), per-site parser-vs-classifier adjudication in the letter.
- **A9 (census-after-swap inversion) — PROVEN** (F-04 declares "Dependencies: F-PA-parse-03" while defining RED on today's tree). **Adopted:** W1 has no parser dependency; the swap is accepted against W1 vectors.
- **A10 (structure mixed into swap; no packed re-verify) — PROVEN.** Deposed tree already cut: max file 754L (`color/color.ts`); `stylesheet/` = 5 files, max 643L; the "899-LOC god-module" is the v4 file being deleted. F-03 lists no packed-surface re-verify. **Adopted:** module cut preserved; re-cut expelled to the structure program; packed-surface re-verify gate added.
- **A11 (F-09 sand) — PROVEN.** Zero-motion expected outcome with a gate satisfied by the wave's own document. **Adopted:** dissolved to registry rows + W3 rider.
- **A12 (WAAPI route has no carrier) — PROVEN** on program text (F-07 deliverables a–d lack the letter). **Adopted:** W7 deliverable (d), named carrier.
- **A13 (OD-PA-5 wedge under-priced) — PROVEN.** glass-ui `package.json` peers `"@mkbabb/value.js": "^4.0.0"` (read this seat) — a 4.1.x reaches every ^4 consumer immediately. **Adopted:** OD-PA-7 precondition (divergence ledger empty/spec-corrections-only; default FOLLOW W56, riding requires cross-program pricing).

### Skeptic 2

- **I-1 (category error: tombstoned semantic layer; zero name overlap; memoized entries) — PROVEN with one correction.** 10/18 units-importing files (verified); `parseCssColor` in deposed tree = 0 (verified); `CSSParseError extends Error` at `index.ts:518`, throws at `:547` (verified); memoized entry parsers = **7** sites, not 5 (`index.ts:542/:618/:627`, `color.ts:721`, `stylesheet.ts:637`, `animation-shorthand.ts:164`, `units.ts:115`). **Correction:** the cache is NOT "unbounded" — `utils.ts` caps it via LRU `PARSE_MEMO_MAX_ENTRIES = 4096`; the memo-defeat bench rider survives (cache-hit fiction is real regardless of bound), the unbounded characterization does not. **Adopted:** honest naming, routing fix, W0 retype pilot, memo-policy row in W3.
- **I-2 (R-EVAL is a prerequisite of the cut) — PROVEN.** `index.ts:7` imports `createMathFunctionParsers`; `MathFunction/CalcFunction` built (~:53); `ValuesValue` wires `MathFunction` (~:503); extant v4 `src/css/{grammar,stylesheet}.ts` calc grep = 0 — all verified. **Adopted:** OD-PA-4 pre-swap; same for HDR/spring (OD-PA-2/3).
- **I-3 (false F-04 dependency) — PROVEN** (self-contradicting parenthesis in the wave spec). **Adopted:** W1 opener, no parser dependency.
- **I-4 (F-01 is formation work; phase-label violation) — PROVEN.** C14: "Prototyped and benchmarked NOW … tranche EXECUTION is not." **Adopted:** W0 phase-labeled formation-phase pilot, outputs enter as evidence; also adopted the sharper point that the pilot must target the RETYPE risk, not "restoration mechanics" (26 months of service record need no proof).
- **I-5 (ONCE-law broken; memo fiction) — PROVEN.** F-01 measures prototype-vs-regex, F-02 promises "measured ONCE" — duplicate. **Adopted:** the single record measurement lives in W0 with the memo-defeating harness; W2 inherits. (Memo bound corrected per I-1.)
- **I-6 (BBNF tombstone re-litigated by silence; grep-genre gate; home lean) — PROVEN on limbs 1–2, DISPROVEN on limb 3's recommendation.** (1) P2.2 tombstones list "BBNF grammar files" under RIGHTLY; zero `.bbnf` files at `164343c1^` (verified — so the program's "pre-v4 in-repo grammars" prior-art cite is doubly wrong: the corpus was excised BEFORE the v4 base). Adopted: W6 names-and-overturns via C14/P4.4. (2) The name-map gate is the owner-retired grep-ledger genre; behavioral equivalence is the enforceable form the excised prior art actually used. Adopted: W6 gate = shared-corpus output diff. (3) **DISPROVEN as recommended:** "extend-in-bbnf as default-lean" collides with C1 — bbnf-lang evolution belongs to the separate BBNF session, and landing value's suite in that repo crosses the ownership boundary this program cannot cross. IN-VALUE stands as the lean, WITH skeptic 2's real demands absorbed: the duplication vs bbnf's existing l4 corpus is priced in the OD-PA-5 brief and reconciliation routes by ask letter.
- **I-7 (F-09 non-wave) — PROVEN** (= A11). Adopted identically.
- **I-8 (consumer gate exported out of the swap) — PROVEN** (F-03's gate list lacks the kf packed-build suite; F-07 holds it two waves later). **Adopted:** kf-suite-vs-packed-build is a W3 acceptance gate; W7 keeps only the protocol note.
- **I-9 (god-wave; re-cut aims at a corpse) — PROVEN** (file-size census above; = A10). Adopted.
- **I-10 (charter census error; byte-freeze vs C12 collision) — PROVEN.** 19 measured; L1 §2's actual law is "signatures preserved," not a byte-freeze of 33 types. **Adopted:** charter §1.3 signature-freeze + machine census; OD-PA-6 surface-thaw row; the DOM-congruence typing design surfaced as decision-then-wave (W5), no longer hidden inside a census wave.
- **I-11 (F-02 vacuous-red; no escalation path) — PROVEN.** **Adopted:** RED = real unmet-ratio measurement; deadlock disposition = 3 tuning iterations → owner escalation (L1 §8), target re-derived, seat never re-contested.

### Convergence check (the final vs both skeptics' strongest surviving points)

Retype-honesty + routing (S1-A2/S2-I-1): charter §1.2, W0, W3. Census-and-decisions-before-the-cut (S1-A1/A9, S2-I-2/I-3): W1 docket, hard sequencing gate. C12 execution lane (S1-A4): W5. Honest gates — no dead-green, no red-by-absence, bounded escape hatches, escalation paths (S1-A7, S2-I-11): W1/W2/W3/W4/W6 gate text. Phantom/sand waves dissolved (S1-A1/A11, S2-I-7): F-05/F-09 gone. Consumer gate in-swap (S2-I-8): W3. Behavioral-equivalence congruence + tombstone named (S2-I-6): W6. Real addendum-2 census + WAAPI carrier (S1-A8/A12): W7. Scope bound + surface thaw + wedge pricing as owner rows (S1-A6/A13, S2-I-10): OD-PA-1/6/7. The program's conceded-correct spine survives intact: the decree, the pins, the WHITELIST caveat, the ParseIssue-closed policy, the RATIFY lean, and the row-landing completeness both skeptics conceded.
