# THRICE — PROGRAM PA-PARSE — SKEPTIC 1 (assume WRONG outright) — 2026-07-19

**Seat:** skeptic 1 of the thrice loop over `armB/program-PA-parse.md`. Posture: the program is
wrong in decomposition, gates, priorities, and completeness until evidence says otherwise.
**Evidence run this seat (all fresh, read-only):** value.js `tranche-u@c654824e` — `src/css/index.ts`
export census; `git show/grep` over the resurrection base `164343c1^` (`src/parsing/`, `src/units/`);
kf `keyframes-v-exec@81a56990` per-zone regex-method census. Sweeps re-read whole. Firewall honored
(no vnext / snapshot-vnext / armA / r1-opus-refuted).

**Verdict up front: NOT RATIFIABLE as written.** The program's spine (restore → then "add" grammar
restores) contradicts what is physically in the tree it restores; its flagship born-RED gates are
dead-on-arrival, unmeetable, or self-described vacuous; its largest owner ask (C12) has census
machinery and zero implementation waves; and its "mechanical resurrection" pricing is falsified by
the deposed tree's own substrate. Nine attacks are load-bearing; a 7-wave counter-shape follows.

---

## A — THE ATTACKS (evidence-first)

### A1 · THE DOUBLE-BOOKING COLLAPSE — F-05 is a phantom wave; F-06's decision framing is backwards

The deposed tree at `164343c1^:src/parsing/` **already contains every production F-05 and F-06 book
as later restores**:

- `color-mix()` parser — `color/color.ts:68` (space map), `:275` (`--- color-mix() parser ---`, `colorMixSpace:277`, `colorMixHueMethod:295`);
- `contrast-color()` parser — `color/color.ts:398-407` ("CSS Color 5 `contrast-color()` (L7, Baseline April 2026) — VJ-Q1", `.istring("contrast-color")`);
- relative-color — the whole `color/relative-color.ts` file (the program itself cites it in F-03's intent);
- HDR — `ictcpParser`/`jzazbzParser` at `color/color.ts:265,:270`;
- `spring()` grammar — `timeline/easing.ts:143-151` ("PARSES + ROUND-TRIPS the author-side `spring(...)`").

Consequences the program never faces:
1. **F-05's born-RED gate is dead on arrival.** Its gate requires the three conformance tests RED
   "on the pre-wave tree" — but F-05 depends on F-03, and the post-F-03 tree parses all three forms
   natively. The gate is GREEN before the wave opens, i.e. exactly the vacuous-green close-class lie
   the charter names. F-05's own evidence line ("color-mix — zero hits today") is measured on the
   regex tree that F-03 has already deleted by then.
2. **F-06/OD-PA-1 is framed backwards.** After F-03, `ictcp()`/`jzazbz()` are LIVE; the owner
   decision is KEEP-vs-EXCISE a restored capability (with §7-1 tombstone duty on excision), not
   "restore vs uphold." Same for spring: the grammar rides in with F-03 whether or not OD-PA-2 is
   decided — the program's sequencing (decide at F-06, after the swap) guarantees either an
   undecided capability ships in the restored tree or an unspecced post-hoc excision. The decisions
   must land BEFORE the swap.
3. **The decomposition is wrong at the joint**: F-05 is not an implementation wave — it is a
   verification/serialization rider on F-03 (census vectors + `serializeCssColor` round-trips for
   forms the parser already accepts). Booking it as a grammar-restore wave inflates the map and
   fakes three born-RED discharges.

### A2 · THE "RESURRECTION" IS A RETYPE — the program's pricing and routing are falsified by the substrate

The deposed parsing tree imports the OLD OO model: **23 import lines** into `../units` / `../utils`
(`git grep -c` over `164343c1^:src/parsing`), types `ValueUnit`/`ValueArray`/`FunctionValue` and the
Color classes (`src/units/color/**` — 12+ files, incl. `conversions/ictcp.ts`). At HEAD, `src/units`
**does not exist** (src top-level = color/css/easing/foundation/quantize/subpaths/transform/value),
and the OO/ValueUnit model is **tombstoned RIGHTLY in P2.2 ("never re-litigate")**. So:

- Every parser `.map()` in the 19-file tree targets a dead, tombstone-protected class. The
  restoration is a **full retype of the grammar onto the v4 node model** using the deposed tree as
  reference — a design-heavy rewrite, not a resurrection.
- F-03 routes "the mechanical resurrection/modernization sweep" to **Opus** — C17 routes design to
  Fable. The program's biggest design surface is mispriced as mechanical.
- F-01 claims to "prove the restoration mechanics" — but if the prototype does not pilot the
  ValueUnit→v4-typed-node RETYPE specifically (it says "modernize to the v4 types" in passing while
  framing the risk as grammar architecture), it proves the wrong risk. The retype IS the risk.

### A3 · THE FLAGSHIP GATE PREDICATE IS BROKEN — "zero regex-method parse paths" is unmeetable or weaseled

F-03's acceptance gate: "zero regex-method parse paths remain on the value side." The deposed tree
**imports `regex` from `@mkbabb/parse-that`** (`164343c1^:src/parsing/index.ts:1`) and uses regex
leaves (e.g. `regex(/[^()]+/)` in the `var()` parser) — parse-that's own leaf set includes
`regex()` (sweep-parse-that §3, leaf.ts:317). The restored tree will therefore contain regex parse
paths **by construction**. As written the gate either (a) can never go green, deadlocking the wave's
headline discharge, or (b) gets silently reinterpreted at execution ("we meant ad-hoc regex, not
combinator leaves") — alias smuggling in gate form. The program also never reconciles addendum-2's
absolutism ("ALL regex-based parsing … entirely abrogated") with the fact that its decreed
destination engine ships a regex leaf. That reconciliation is an adjudication row and it is absent.

### A4 · C12 HAS NO EXECUTION WAVE — the program's largest owner ask lands as paperwork

C12: "value gains **full keyframes/animation parsing** that kf leverages … typed values congruent
exactly to their DOM equivalents." The program's row map lands C12 as "F-04 (census + congruence) +
F-07 (kf-leverage spec)" — a **ledger and a letter**. F-05 is fixed-scope (three color productions,
already moot per A1); F-06 is fixed-scope (HDR/spring/calc). **No wave implements** the census's
non-color discoveries: WAAPI keyframe-format typing (per-keyframe `composite`, `computedOffset`,
`pseudoElement`, `KeyframeEffectOptions`), animation/timeline gaps, CSSOM typed-value congruence.
F-04's gate says "every GAP lands as a wave row (feeding 05/06)" — 05/06 cannot receive them. This
is precisely §9's "inventory of problems without the tranche that resolves them" and §7's
unowned-ask violation, applied to the program's central mandate.

### A5 · THE FROZEN SURFACE CONTRADICTS C12 — and the frozen census miscounts itself

Charter §1.2 freezes "the 15 value exports … + all 33 type exports … signatures **byte-stable**."
C12 demands typed values re-aligned **exactly** to DOM equivalents — which is type-surface motion.
Both cannot hold; the program carries no surface-thaw decision row (only the ParseIssue-code policy)
and no vehicle for the congruence work (OD-PA-5 even contemplates shipping 4.1.x under the freeze).
And the freeze census is itself wrong: the charter says "the **15** value exports" then lists **19**
names; measured `src/css/index.ts` = **19 value exports** (7 grammar + 1 syntax + 3 timeline + 8
stylesheet) + 33 type exports, with `ParseIssue`/`ParseResult` double-counted (inside the 33 AND
appended separately). A frozen-surface law whose own census is off by 4 cannot anchor a
capability-diff claim of "green by construction."

### A6 · "ENTIRE CSS SPEC" IS SILENTLY NARROWED — no bounding decision row exists

The owner's C12 words: "ENTIRE CSS spec incl. experimental as of July 2026." F-04 enumerates
values/color/easing/animations/scroll/stylesheet/CSSOM/WAAPI — and omits **selectors, media
queries, gradients (Images L4), filters, transforms**, all five of which exist as modules in the
very bbnf `grammar/css/l4/` 15-module exemplar the program adopts as its C14 substrate
(sweep-parse-that §6: color, easing, **filters**, gradients, keywords, **media**, **selectors**,
**transforms**, …). Either the owner's "entire" is being silently reinterpreted as "the /css
surface's current domains" — a scope fudge that must be an OWNER-DECISION row — or the census will
explode mid-wave. F-08's "covering the FULL restored grammar" quietly redefines "full" the same
way. Nowhere is the narrowing surfaced for ruling.

### A7 · GATE-QUALITY SWEEP — vacuous, degenerate, and theatrical gates throughout

- **F-07** admits it: "RED today is vacuous — the build doesn't exist until 03." And its
  census-lock grep row is **green today** (P1.4: census-PASS everywhere but easing.ts) and is
  touched by no wave in this program — a gate that cannot fail gates nothing.
- **F-02**'s born-RED is degenerate: "RED = the gate script exits nonzero because no restored
  parser exists yet." Any script referencing a missing module is red; that is not the live defect
  (the UNMEASURED parser) under test. Worse, no deadlock disposition exists if the restored parser
  cannot meet the historical ratio recipe (a recipe calibrated on the pre-v4 world): F-03's
  headline RED→GREEN discharge is then unreachable and the program is silent on what happens.
- **F-01**'s gate carries its own escape hatch: "deep-equal … (divergences enumerated +
  adjudicated)" — mismatch is re-classed as paperwork; the gate cannot fail. It needs a bound
  (divergences admissible only where the regex parser is spec-WRONG per census vectors).
- **F-03** cites "capability-diff gate green by construction" — but the map's non-ownership note
  routes the capability-diff gate BUILD to the structure/gates program, with no dependency edge.
  A gate that may not exist at wave close cannot be claimed green.
- **F-03's π row** (pixel-stable swatch pre/post swap) is weak-equivalence theater: a swatch
  renders identically across a broad class of wrong parses, and the wave makes no visual claim —
  the charter requires π/DELTA for visual claims, not as garnish. Probe parsimony cuts it.

### A8 · ADDENDUM-2's KF HALF IS FUDGED — an n=1 "census"

The map claims F-07 "doubles as the kf regex-census row for addendum-2," with easing.ts:30/:38-39
as "the adjudicated residue." Measured at kf `81a56990`, regex-method call sites (`.test/.match/
.exec`) exist across **seven zones**: compile 4 · engine 1 · ingest 1 · orchestration 2 · presets 1
· resolve 1 · svg 1 — e.g. `resolve/browser.ts:43` `/^(sv|lv|dv|v)?(w|h|i|b|min|max)$/.exec(unit)`,
a live parse-class site (viewport-unit classification) in the zone that owns calc capability. Most
may adjudicate as classifiers, not parsers — **but that adjudication is what a census IS**, and
none was run; the program pre-declares the answer from one known row. Compounding it: F-07's
deliverable list does not actually contain the census — the map asserts a deliverable the wave
never specs.

### A9 · SEQUENCING INVERSION — census after the swap, with RED defined on a tree the swap deletes

F-04 depends on F-03, yet its born-RED examples are defined "on today's tree"
(`color-mix → ok:false`, relative-from hard-fails, ictcp dropped). Post-F-03, all of those are
GREEN (A1) — the RED definitions are stale by construction, and the census that should have told
the swap what surface to target runs after the seat is already swapped (rework risk on the frozen
surface). The honest order: census on the regex tree FIRST (true today-RED vectors, C12 target is
engine-independent), owner rows decided, then the swap accepted AGAINST the census vectors.

### A10 · STRUCTURE MIXED INTO THE SWAP — an unreviewable diff with a missing gate

F-03 bundles the engine swap WITH an internal goldilocks re-cut ("the 899-LOC stylesheet
god-module does not survive as one file"). A seat swap + module re-cut in one diff means no green
or red is attributable to either; the canon's own sequencing wisdom (restructure churn only behind
settled gates; internal-only restructures until the boundary) says split them. And having chosen to
restructure behind the frozen surface, F-03 lists **no packed-surface re-verification gate** (the
D50-class check P3.2 demands for exactly this move class) — gates are tests-green + producer+api +
ratio + a grep. The structure program owns structure; F-03 should restore into a stable shape and
leave the re-cut to the owning program's gated wave.

### A11 · F-09 IS SAND — a wave that passes by being written

F-09's expected outcome is ZERO motion and its gate in that case is "the recorded disposition set"
— i.e., the wave's deliverable is the document that the wave is, and it cannot fail. PT-E1/E2/E3
dispositions are **registry rows** plus a contingency clause attached to F-03 (fire-on-trip). As a
wave it exists to make "nine waves, no sand" a rounder sentence; it is the sand. (C21: little time
on contrived process.)

### A12 · THE WAAPI ROUTE HAS NO CARRIER — a route asserted, not mechanized

F-04 "routes by name to the kf program's WAAPI wave **via the kf inbox**" — but the only wave that
writes kf's inbox is F-07, and F-07's deliverables (easing brief, kf-leverage spec, seam protocol,
census-lock) do not include the WAAPI route letter. A route with no named carrier is a silent drop
wearing a route's clothes; per §7 an ask needs an **owning wave**, not a cell in another wave's
deliverable prose.

### A13 · (minor) OD-PA-5 under-prices the wedge

"4.1.x-eligible on the frozen surface": glass peers value `^4` — a 4.1.x parser swap reaches every
^4-range consumer (glass, demos) OUTSIDE the co-land discipline, carrying whatever behavioral
divergences F-01's ledger enumerated. The recommendation needs a precondition (divergence ledger
EMPTY, or divergences all spec-corrections) — and riding W56's SCI-1 vehicle couples engine-swap
risk onto the color program's cut, which that program did not price.

---

## B — THE COUNTER-PROGRAM (title + intent grain)

Seven waves; the phantom waves dissolve, C12 gains an execution lane, owner rows move ahead of the
swap, every gate predicate is defined and failable.

- **X1 · SCOPE RULING + SPEC CENSUS, REGEX-TREE-FIRST (C12).** Enumerate the July-2026 target on
  the extant tree BEFORE any swap: one row per production — IN-DEPOSED-TREE / GAP / DECISION — with
  the DOM-equivalent type named per row; born-RED vectors authored here are genuinely RED. Presents
  the "ENTIRE CSS spec" bounding question (selectors/media/gradients/filters/transforms — the bbnf
  l4 modules) as an OWNER-DECISION row, plus the HDR keep/excise and spring-ownership rows,
  **decided pre-swap** so the restoration lands a decided surface.
- **X2 · THE RETYPE PROTOTYPE (C14-now).** Bounded prototype whose named risk is the
  ValueUnit→v4-node RETYPE (2–3 productions incl. one `.map()`-heavy color path); BBNF congruence
  piloted as **shared-vector execution equivalence** (the pre-v4 equivalence-test prior art), never
  a name map; bench harness draft + measured numbers vs the regex incumbent. Divergence gate
  bounded: mismatches admissible only where the incumbent is spec-wrong per X1 vectors.
- **X3 · BENCH WITNESS.** bench/ + the portable ratio gate restored; the regex parser measured
  once for the record; born-RED defined as ratio-unmet (not module-missing) with an explicit
  deadlock disposition: N tuning passes → owner escalation (decree unaffected — the seat is not
  re-contested, the TARGET is re-derived).
- **X4 · R-PARSER AS RESTORATION-BY-RETYPE (the seat swap, whole).** The full deposed grammar —
  color-mix, contrast-color, relative-from, timeline, and the X1-decided HDR/spring rows riding in
  or excised WITH tombstones — retyped onto v4 nodes behind the frozen 19+33 `/css` surface
  (census corrected); `@mkbabb/parse-that ^1.0.0` re-added + manifest-gate WHITELIST; U-F29/U-F30
  family tests; file shape preserved (NO god-module re-cut here — that is the structure program's
  gated wave); packed-surface re-verify gate; acceptance = the X1 vector corpus RED→GREEN + full
  existing css suite green + X3 ratio green. Gate predicate replaces A3's broken one: "zero ad-hoc
  regex parse paths outside parse-that combinators" — defined, greppable, failable.
- **X5 · C12 EXECUTION (the wave the program lacks).** The census GAPs genuinely absent from the
  deposed tree: WAAPI keyframe-format typing, animation/timeline gaps, CSSOM typed-value
  congruence — with the surface-thaw OWNER-DECISION row (frozen 33 types vs exact DOM alignment)
  and the co-land boundary as default vehicle for any type-surface motion.
- **X6 · DUAL BBNF SUITE + EXECUTION-EQUIVALENCE GATE (C14).** Full suite reconciled against
  bbnf's l4 15-module split at the X1-ruled scope; congruence gate = corpus execution equivalence,
  RED on behavioral drift, not merely unmapped names.
- **X7 · KF BOUNDARY + THE REAL ADDENDUM-2 CENSUS.** ONE INBOUND letter carrying: the easing
  consume-vs-ratify brief; the kf-leverage spec; the seam re-verification protocol; **the kf-wide
  regex-parse census** (the 7-zone/~11-site enumeration, parser-vs-classifier adjudicated,
  `resolve/browser.ts:43` row 1); and the WAAPI-engine route — the carrier A12 found missing.
- **Registry rows, not waves:** PT-E1/E2/E3 terminal dispositions + fix-on-trip contingency clause
  attached to X4 (F-09 dissolved); ParseIssue-code policy; `collectDeclarations` freeze note;
  OD release-vehicle row with the A13 precondition.

**Docket (all pre-swap where they gate the swap):** OD-1 spec-scope bound · OD-2 HDR keep/excise ·
OD-3 spring ownership · OD-4 surface thaw vehicle · OD-5 release vehicle (wedge-priced) · OD-6
easing ratify/consume.

---

## C — BOTTOM LINE

The program is right about the decree, the pins, the whitelist caveat, and the zero-motion
parse-that posture — and wrong about nearly everything it built on top: it restores a tree it has
not read closely enough (five "later" capabilities are already inside it; its substrate is deleted
and tombstoned), defines its headline gates so they are dead-green, degenerate-red, or unmeetable,
files the owner's central C12 ask as paperwork, and pads the count with a wave that cannot fail.
The counter-shape does the same mission in 7 waves with every born-RED honest, every decision ahead
of the code it gates, and an implementation lane for the one mandate the program forgot to build.
