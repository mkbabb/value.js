# ARM B — FINAL PROGRAM PF-GOVERNANCE (thrice-adjudicated) — the cross-repo governance tranche program

**Formation seat:** armB (independent Fable formation; Codex corpus unread — firewall honored).
**Date:** 2026-07-19. **Status:** AMENDED FINAL — the r1 program survived on its FACTS and died
on its SHAPE; this final adopts every proven skeptic point (see §6 ADJUDICATION LOG).
**Canon:** L1 (`keyframes-inbox-2026-07-18-vnext-ingestion-prompt.md`) + L2 packets P0–P6 +
CONVERSATION-ADDENDA C1–C23 (addenda WIN where they tighten). **Truth base:** `armB/sweep-kf-lib.md`
+ `armB/sweep-value-lib.md` + this adjudication's own probes (git tags, semver execution,
five-program roster greps — cited in §6). **Adjudicator model (own system prompt):** `claude-fable-5`.

---

## §0 — PROGRAM CHARTER

PF-GOVERNANCE owns the constellation's coordination spine so every sibling program can cut
deep without breaking a consumer silently. **The formation roster is the six programs ON DISK
and nothing else: PA-parse, PB-color, PC-structure, PD-frontend, PE-api, PF-governance.**
The r1 program's phantom siblings ("apparatus", "zones program", "gates program") are
RETIRED as routing targets; their inventories are homed by name in this final (§2/§5) —
kf-side gates/tests work was ALWAYS dispatch cargo under P4.5 (the kf successor implements),
and the orphaned value-side e2e adjudication is homed as a named wave here (F-GOV-04).

Spine duties: ONE co-land boundary (value 5 / kf 7 / glass peers / atlas ranges), priced by
the ERESOLVE-hard wedge with prerelease semantics priced (semver-proven, §2 F-GOV-05); the
P4.5 dispatch protocol as the sole cross-repo channel; the capability-diff gate on BOTH repos
built tag-shape-agnostic so the v4 failure mechanism (*no advocate ⇒ no tombstone*) can never
fire again — AND its one fired instance CURED by the CHANGELOG tombstone backfill; **the
single merged owner-decision docket** (the formation currently carries FIVE sheets — PF §3,
PC OD-S1..6, PD OD-1..6, PB OD-C1..5, PA OD-PA-1..6, with R-HDR/spring-grammar/R-EVAL each
triple-booked — merged and deduped here, ratified at the FRONT, before any dependent work);
glass defect batching with a gate that can actually fail; semver discipline cited to canon
(no parallel-authority doc). Six waves, code-first, ratification before cargo, the boundary's
go/no-go as its ENTRY gate, zero silent drops audited against a 1:1 receipt table.

**G-0 — CHARTER MATTER (standing template lines, NOT waves; stamped into every wave spec of
every program):** G0′ re-pin at wave open (the §1 register is the seed; any written pin is
stale on write — the stamp, not the file, is the control) · anchor re-derivation as a
mechanical gate STEP (cite → re-grep → diff → proceed/halt; already law, L1 §7.3/N-ADJ-3 —
cited, not re-legislated) · `model_served` reporting on every seat + **the L1 §0 rider: any
Opus-mechanical assignment in tranche execution is valid only under the per-tranche owner
ratification obtained at docket row OD-15** · phase labels · parse-that ≤1.1 forever
(C15) · **the 2026-07-17 mail/INBOX law (E13): every session/wave opens with the 4-path
coordination-mail sweep; `docs/tranches/V/coordination/INBOX.md` is the durable ledger; no
wave closes with unread mail** · the G5′ amend-never-parallel rider (kf structure waves AMEND
the ratified LT blueprint + EXTEND proof:structure, naming LT-10/LT-16) · the formation
registry carries the defect-FAMILY axis (P1.5: families, not instances; the
mixColors/parseCSSValue ad-hoc-fix family is row 1, recurrence risk in the parser successor).

---

## §1 — G0′ PIN REGISTER (measured 2026-07-19; re-pin at every wave open per G-0)

| Repo | Tree | Branch @ HEAD | Version | Note |
|---|---|---|---|---|
| value.js | `/Users/mkbabb/Programming/value.js` | `tranche-u@c654824e` | 4.0.0 (published deps-free; **HEAD package.json carries the committed deps bomb** — verified this adjudication) | canon `db77dbd8` +docs only |
| keyframes.js | `/Users/mkbabb/Programming/keyframes-v-exec` | `master@81a56990` | 6.0.0 | +2 docs-only commits vs canon; zone LOC EXACT |
| parse-that | `/Users/mkbabb/Programming/parse-that` (pkg `typescript/`) | `master@ef10d5b7` | 1.0.0 (npm latest) | semver cap ≤1.1 (C15) |
| glass-ui | `/Users/mkbabb/Programming/glass-ui` | `master@cdc322b9` | 7.0.0 | advances quickly — re-pin at dispatch |
| atlas (ACTIVE) | `/Users/mkbabb/Programming/.p-totality/atlas` | `p/totality@fe9abcfc` | 7.0.0 (npm latest ALREADY 7.0.0) | standalone `atlas@master` = STALE trap |
| bbnf-lang | `/Users/mkbabb/Programming/bbnf-lang` | `master@af15f63e` | — | C14 substrate (parser program) |
| fourier-analysis | `/Users/mkbabb/Programming/fourier-analysis` | `m/w1-bump-migration@cd26c653` | — | C7 isomorphism reference |
| sci-report | `/Users/mkbabb/Programming/sci-report` | `feat/tranche-k-arc@da1e3763` | — | crosses at its own glass-7 consume (registry row) |

**Anchor-drift standing proof (sweep-kf-lib §3):** at kf HEAD, 3 of 13 P4.3 flatten anchors
had drifted (vite `:41→:37`, `:156→:153`, `:229→:227`); internal/ fan-in is **11** importers,
not "10-zone". Re-derivation is a template gate step in every executing wave — NOT a wave,
and NOT a born-RED gate (no formation artifact cites the stale numbers; PC-structure:264
already cites the re-derived values — adjudicated §6, S1-B1).

**Channel constants (verified on disk):** kf inbox = `keyframes-v-exec/docs/tranches/V/coordination/`,
grammar `VALUEJS-INBOUND-<date>-<topic>.md`, rowed in `INBOUND-LEDGER.md`. glass-ui inbox =
the ACTIVE glass tranche's `coordination/` (re-derive at dispatch). parse-that channel = PT-E
ask letters.

---

## §2 — THE WAVE SET (6 waves)

### F-GOV-01 — DEFUSE + PUBLISH INTEGRITY (code first, day one; NO dependencies)
- **Repo:** value.js.
- **Intent:** The single cheapest, highest-risk defusal goes FIRST — no paper wave ahead of
  it. Then both publish-path gates, built to run, and the fired tombstone instance CURED.
- **Deliverables:** (a) **the deps strip, immediately** — the COMMITTED
  `dependencies: {"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}` (in-repo at
  `c654824e`, verified) moves to devDependencies in the wave's first commit; never
  lockstep-bump (P0.2 row 3). (b) **pre-publish manifest gate, ALLOWLIST form**: RED unless
  runtime `dependencies ⊆ {@mkbabb/parse-that}` (the historical invariant — v3.1.0 shipped
  exactly `@mkbabb/parse-that ^1.0.0`, verified; R-PARSER lawfully re-adds it; any OTHER
  runtime dep is red, so the check cannot rot) + packed-surface check vs the current export
  map. (c) **capability-diff gate, TAG-SHAPE-AGNOSTIC and TWO-GRAIN**: the census derives
  each side's key set from that tag's OWN export map (census the published npm tarballs —
  `npm pack @mkbabb/value.js@<tag>` both sides; never assume "the 7 frozen keys": v3.1.0's
  map is a different 8-key topology — `.`, `./color`, `./parsing`, `./math`, `./easing`,
  `./transform`, `./units`, `./quantize`), and diffs at BOTH grains: subpath keys AND
  exported symbols per key. RED on any removal at either grain lacking a by-name
  RIGHTLY/UNJUSTLY/UNCLEAR tombstone in the cutting release's CHANGELOG. `--selftest` proves
  RED capability by mutation (remove one export, assert red) — a gate that cannot fail is
  the lighthouse-mask disease. (d) **the birth certificate AND its cure in the same wave**:
  run the retro-census v3.1.0→v4.0.0 — it must red on the three whole-key extinctions
  (`.` root, `./parsing`, `./units` — which the 4.0.0 CHANGELOG names at KEY grain) and on
  the SYMBOL-grain extinction the CHANGELOG never names (deltaE*/raytrace/okhsl/okhsv/Into
  family/sampleColorRamp/mixColorsN/`spring()` grammar/ictcp()/jzazbz() — zero by-name rows;
  the indicted continuity line "`/color` owns … gamut mapping" verified on disk); then
  **backfill the v4.0.0 CHANGELOG tombstones** (the 14-row D ledger, by-name
  RIGHTLY/UNJUSTLY/UNCLEAR; Fable adjudicates the taxonomy — P2.2's tombstone list is
  RIGHTLY; the gamut/ΔE/Into/ramp families are UNJUSTLY-restored-by-PB-color; HDR rides
  docket row OD-7) on the repo's own precedent (the [3.1.0] entry retroactively corrected
  the 3.0.0 record in-place; the tag stays immutable, the CHANGELOG is the honest record).
- **Acceptance gates (born-RED, honestly — both defects live today):** RED-1 = the committed
  deps block at HEAD (a publish from this branch ships the kf↔value registry cycle + dual-core
  forcing via kf's exact pin). RED-2 = the retro-census reds on the unpapered extinction at
  symbol grain. GREEN = strip landed + both gates CI-wired + selftest-mutation red proven +
  the backfill merged (the fired instance papered) + capability-diff green-with-tombstones
  thereafter.
- **Dependencies:** none. **π/DELTA:** none.
- **Model routing:** Opus mechanical (tarball census scripting, CI wiring) under the OD-15
  ratification; Fable adjudicates the tombstone taxonomy.

### F-GOV-02 — ROSTER + CHANNEL + THE FULL DISPATCH PACK (kill the phantoms)
- **Repo:** value.js (plan folder) → kf inbox, glass inbox, atlas ledger.
- **Intent:** One reconciled roster; one channel, with every kf item the siblings routed into
  it actually given a car; the landing map regenerated as a mechanically diffable receipt
  table; the glass batch ledger with an enforcement mechanism that can fail.
- **Deliverables:** (1) **THE FORMATION ROSTER** — the six on-disk programs by filename;
  phantom names (apparatus/zones/gates) retired with each orphan row's terminal home named:
  kf-side gates/tests inventory → the dispatch cars below; value-side e2e adjudication →
  F-GOV-04; capability-diff builds → F-GOV-01 (value) + car (a) (kf); value isomorphism gate →
  PC-structure F-02/F-03 (verified extant; PC F-10 dispatches the kf rule). (2) **the
  dispatch protocol page** (P4.5 grammar; acknowledgment discipline; bounded scope; every
  dispatch rowed in INBOUND-LEDGER). (3) **THE INITIAL DISPATCH PACK** — cars:
  - **(a) kf capability-diff gate spec** — census vs the 6.0.0 tag, tag-shape-agnostic,
    two-grain, `--selftest` mutation-proof (kf's 6.0.0 record models compliance → born-GREEN
    there, so the selftest is the anti-vacuity clause);
  - **(b) kf-7 co-land spec** (boundary member: version 7.0.0; value pin EXACT `4.0.0`→`5.0.0`;
    the 44-key `./engine` mirror re-verified by EXECUTION post-cut);
  - **(c) scar-tissue deletion spec**, triggered on PB-color's R-DELTAE/R-RAMP landing
    (`compile/emit/backward/color.ts:120-124` hand-rolled oklab ΔE; `backward.ts:30-32`
    stale docstrings);
  - **(d) easing.ts consume-vs-ratify brief — RATIFY AS FENCED DEFAULT** (adjudicated §6
    S1-C3′: the sweep's own verdict is "legitimate CSS-keyword classifiers, the strongest
    consume-vs-ratify candidate"; the `:31-37` faithful-CSS-twin doc law + F.W7 round-trip
    rationale are ON DISK at kf HEAD, verified; PA OD-PA-6 independently recommends RATIFY);
    the CONSUME variant is priced in the brief with the overturn burden explicit (kf's
    documented value.js-free light-engine law + depcruise leaf rule `:171` must be
    cite-overturned);
  - **(e) THE BOUNDARY CENSUS-LOCK GATE spec** (L1 §2's kf mandate, previously undispatched):
    a durable kf-side rule (grep/depcruise) asserting zero grammar productions kf-side —
    locks the boundary against re-growth, not just the one-time census;
  - **(f) THE kf FULL-WAAPI ENGINE UPLIFT spec** (C12's largest kf item, previously dropped;
    PA F-04's ROUTE row lands HERE): the sweep-kf-lib §5 absent-member list attached —
    `persist()`, `pseudoElement` targeting, `iterationComposite`, `getAnimations()`,
    `CSS.registerProperty`, `updatePlaybackRate`, per-keyframe `composite`, eligibility-gate
    liberalization;
  - **(g) the kf addendum-2 REGEX CENSUS** as its own rowed dispatch (no longer free-riding
    in the easing brief);
  - **(h) THE kf GATES PACK** (the ex-"gates program", correctly shaped as dispatch cargo):
    W9-land first (staged at `v/w9-staging@b920b190`) · MR2/MR4 before any restructure churn
    (P3.3 row 7, the sequencing law restated in the car) · wire `lint` + `proof:structure`
    as two one-line CI steps · demote the masked `audit:lighthouse` · CH-05 weekly-relabel
    fold · boundary-cohesion clamp-invariant fold-then-retire · zone-test binding rule ·
    the proof:publish consumer-CLAIMS rule (from the oscillator finding) · the oscillator
    record correction (`published-surface.md` Oscillator row: both named consumers — glass-ui
    BB `W-EASING-PRIMITIVE`, demo KF-OSCILLATOR scene — re-verified FALSE on disk this
    adjudication: 0 hits across glass-ui/src + kf/demo);
  - **(i) RESERVED: the PD kf-demo cars** (C4a/b/c, C6-kf, C22-kf, detail pass) — authored
    by PD-frontend as design briefs, DISPATCHED here iff docket row OD-14 rules mode (b);
    released as direct-edit grants iff (a).
  (4) **the glass batch ledger** (folds the kf 15-row marks register) + the three release
  criteria (isolation evidence, targeted defect, precepts-compliant addendum) + **the named
  enforcement mechanism: a close-time join — every sibling program's deliverable paths
  grep'd for glass-ui file paths against ledger rows; any glass-touching change outside a
  batch is RED** (the r1 "the ledger check is the gate" self-report loop is retired). π/DELTA
  for shadcn-gap prototypes stays OWNED BY THE ORIGINATING PROGRAM; the ledger carries
  capture POINTERS (paths, verified on disk at batch release) — one bookkeeping, not two.
  (5) **the atlas chase ledger** (3 TimingFunction sites: useCountUp.ts:47,
  useScrollTimeline.ts:44, useScrollLettering.ts:57 — IN-ATLAS-5; `MorphSVG` via `./engine`
  buildMarkAnimation.ts:7; re-derive at execution). (6) **THE RECEIPT TABLE** — the §5
  landing map regenerated as canon-row → program-file → wave-id, 1:1, mechanically diffable;
  **C11 → PC-structure** and **C6 → PD-frontend F-10/F-15** filed correctly.
- **Acceptance gates:** machine-checkable completeness — every dispatch has an
  INBOUND-LEDGER row (glob dispatch files vs ledger rows — the r1 program's one real
  paper-gate, kept); zero rows in the receipt table naming a program with no file (a grep
  over the table vs `ls program-*.md` — this is the anti-phantom gate, and it is RED at
  formation state today: "apparatus" rows exist); every sibling kf item cites a car id or an
  owner grant.
- **Dependencies:** F-GOV-01 (none blocking, but the strip's commit precedes any dispatch
  referencing the manifest gate). **π/DELTA:** none (prototype π lives with originators).
- **Model routing:** Fable (spec authorship, briefs); Opus (ledger checker, receipt-table
  diff) under OD-15.

### F-GOV-03 — THE MERGED DOCKET, RATIFIED AT THE FRONT
- **Repo:** value.js (single sheet). **Exit gate = OWNER RATIFICATION** — before any
  dependent sibling work, in time for ruled prunes to be boundary cargo (cures the
  terminal-ratification inversion, §6 S2-1.1/1.2/1.3).
- **Intent:** ONE sheet (L1 §9), merging and DEDUPING the formation's five sheets. Decided
  rows are recorded as supersessions, never re-asked.
- **Deliverables:** the §3 sheet; the supersession records (api/ STAY per C7 — off the
  docket, matching PC OD-S3's own form; the P3.2 EXTRACT row RETIRED-superseded with
  rationale); the unilateral prune riders attached (split-text 486 w/ named split-a11y-oracle
  casualty · motion-path ~180 · oscillator ~150 + record-correction · value decompose 609 ·
  quantize DEMOTE — each with four-tree census cites at pinned HEADs, zone-orphaned-test
  bindings (scroll 800 / svg 807 / ingest 1027 / waapi 737 bind to zone verdicts; no orphaned
  green), and the K F6 fence check (none of the three kf prunes touches a fenced zone)).
- **Acceptance gates:** every P3.1/P3.2 row disposed 1:1 (a row with no disposition is a
  silent drop); every sibling docket row absorbed or explicitly superseded (checkable: grep
  `OD-` across the five program files vs the merged sheet); zero rows re-asking a decided
  matter; RATIFIED = owner rulings recorded per row. Prune riders execute only
  post-ratification and behind frozen surfaces until the boundary — and ratification now
  PRECEDES the boundary, so ruled prunes have a legal vehicle.
- **Dependencies:** F-GOV-02 (the sheets to merge; the cars the rulings trigger).
- **π/DELTA:** none. **Model routing:** Fable (each row argued both ways ≤10 lines); Opus
  census re-greps under OD-15.

### F-GOV-04 — THE VALUE E2E FLEET ADJUDICATION (the orphan, homed)
- **Repo:** value.js (e2e/ ~13.4k LOC). Previously routed to the phantom "apparatus" program
  by r1-PF and owned by NO file in the formation (PD-frontend: zero e2e/oracle content —
  verified). The owner's addendum-3 explicit challenge cannot be ownerless. Homed here as
  governance's apparatus annex (skeptic 1's own sanctioned cure).
- **Intent:** Execute the adjudicated E-union disposition (P3.4 row 2): ABROGATE the
  oracles/design-census mass (~5.9k, 0 catches) + headless perf (~856) · FOLD ~1.7k
  (o1/o2→boot, browse→walk, mobile→viewport param, dual-pane→build-check, webgl→CH-7 annex +
  1 context-loss probe, o16→glass producer surface) · KEEP-EARNED ~1.7k on verified catches
  (page-load, walk/admin-walk, color-space-switching, the DRIVEN a11y battery, the safari
  trio) · the ~1.2k journey subset rides the ratified W55 vehicle — **W55 owns the `.github`
  edit; this wave does not touch it**. Library gate surface stays producer+api (P3.4 row 1).
- **Acceptance gates:** the disposition ledger 1:1 against the e2e tree (every spec file
  named ABROGATE/FOLD/KEEP/W55 — an unlisted file is a silent drop); folds land with their
  target suites green; no KEEP row without its verified-catch cite; the abrogated mass
  deleted in one reviewed commit (tombstoned in the 5.0.0 CHANGELOG via F-GOV-01's gate).
- **Dependencies:** F-GOV-03 (ratified docket confirms the disposition); W55 vehicle extant.
- **π/DELTA:** none (test adjudication; no visual claim). **Model routing:** Fable
  adjudicates per-oracle keep/fold verdicts; Opus executes the mechanical fold/delete sweeps
  under OD-15.

### F-GOV-05 — THE CO-LAND BOUNDARY + THE TRAIN (go/no-go at ENTRY)
- **Repo:** value.js + keyframes.js + glass-ui + atlas (ACTIVE), coordinated.
- **Intent:** The single coordinated crossing. **The go/no-go is this wave's ENTRY gate** —
  the owner rules on the ratified docket + sibling terminal state BEFORE the freeze (moved
  from terminal position, where "no" was impossible). The train ORDER lives in this wave's
  spec preamble, CITING canon per law rather than re-encoding it (no RELEASE-TRAIN.md — the
  parallel-authority disease): C15 parse-that ≤1.1 · P4.1.3 immutable tags · D54/W56 4.1.x
  before the boundary, never forked (D-GAP-6 rider fires at 4.1; PA OD-PA-5's parser-swap
  vehicle question rides the merged docket) · P3.3-row-7 MR2/MR4-first (restated in car h) ·
  P0.2-row-4 no-atlas-catch-up · P4.2 internal-until-boundary · W55 owns `.github`.
- **Order:** value 4.1.x (W56/SCI-1) → glass peer move per ratified OD-12 → FREEZE →
  value 5.0.0 → kf 7.0.0 → atlas ranges → post-cut assertions.
- **Deliverables:** the wedge price sheet (chase ledgers re-derived at execution): glass 7
  peers kf `^6` + value `^4` (peerOptional HARD-FAILS npm when present-incompatible —
  ERESOLVE, proven by execution twice; `optional` exempts ABSENT peers only) + the
  NON-optional framework peers (vue ^3.5 / tailwindcss ^4 / reka-ui ^2 / @lucide/vue ^1.16 —
  chase-ledger residents, untouched); kf pins value EXACT; atlas exact tuple + named chase
  sites (§2 F-GOV-02.5); kf→value 47 importing files, `/css` 29 sites/39 symbols (consumer
  signatures frozen by PA-parse); glass→value 5 imports.
- **Acceptance gates:** (1) **THE ERESOLVE PROOF MATRIX, PREREL-SEMANTICS-PRICED** (adjudicated
  §6 S2-4.4, PROVEN BY EXECUTION: `semver.satisfies('5.0.0-rc.1','^4 || ^5') === false`;
  `'5.0.0'` → true): TWO legs — the RED leg runs pre-crossing against the PUBLISHED state
  (installing the crossing tuple beside glass 7's live `^4` peer ERESOLVE-fails: the wedge
  demonstrated, not assumed); the GREEN leg runs on FINAL versions only, immediately
  post-cut inside the freeze window. If a prerelease staging window is wanted, the widened
  ranges must be explicitly prerelease-admitting (e.g. `^4 || >=5.0.0-rc.0 <6`) — `^4 || ^5`
  does NOT admit `5.0.0-rc.x` and a prerelease-based matrix would mis-measure the wedge.
  (2) capability-diff green-with-tombstones on BOTH cuts (every sibling-program removal —
  decompose, quantize demote, abrogated e2e, subpath dissolution — by-name in the 5.0.0
  CHANGELOG). (3) the kf 44-key engine mirror re-verified by EXECUTION. (4) `npm view`
  version assertions (post-cut confirmation — honestly labeled detection-after; the
  PREVENTIVE cap check is the formation-grep in F-GOV-06 + the PT-E letter carrying C15).
  (5) the T-census freeze check (`TimingFunction` at kf `constants/types.ts:45` unchanged,
  or IN-ATLAS-5 executed in-window). (6) glass cargo cleared the F-GOV-02.4 batch criteria
  (the missing dependency edge, added).
- **Dependencies:** F-GOV-01, -02, -03 (ratified — prunes are lawful cargo), -04; EXTERNAL:
  terminal waves of every surface-mutating sibling (PA R-PARSER, PB R-DELTAE..R-RAMP, PC
  dissolution/prunes). LAST among breaking changes.
- **π/DELTA:** none directly (demo-visible changes ride their owning programs).
- **Model routing:** Fable orchestrates + adjudicates surprises; Opus executes chase edits
  from the pre-written ledgers under OD-15.

### F-GOV-06 — THE TRANCHE CLOSE (hosted by governance; not a duplicate)
- **Repo:** value.js. Re-chartered per §6 S2-2.4: this is the TRANCHE's close ceremony
  (L1 §9's obligation), hosted by the governance program because the tranche must close
  somewhere — it audits this program along with every other. It contains NO ratification
  (moved to F-GOV-03) and NO go/no-go (moved to F-GOV-05 entry).
- **Deliverables:** the zero-drop re-walk against the F-GOV-02 receipt table (now auditable:
  every row names a real file + wave id) at close-time state; the close-lie screen
  (vacuous-green gates — every gate's selftest re-run; declared captures verified ON DISK at
  their ledger pointers; masked fallbacks; re-booked chronics); the disease-row screen (any
  chronic riding 2+ closes un-decided gets its own deciding wave); **the formation-grep
  checks**: no parse-that version >1.1 proposed in any program/spec/dispatch file (the C15
  preventive seat — mechanically runnable, unlike r1's "proposed anywhere" prose); no
  receipt-table row naming a fileless program; terminal dispositions for every
  partial/banked/abandoned item (folded-named / banked-with-re-trigger / retired-with-
  rationale — counting a partial as done is the forbidden close-lie).
- **Acceptance gates:** two consecutive clean registry passes (L1 §0 stability); RED on any
  inventory row without a named landing, any close-lie pattern, any unreceipted sibling
  route. Thrice-run per C20 (two skeptics assuming the close is a lie, one adjudicator).
- **Dependencies:** all waves (terminal). **π/DELTA:** none. **Model routing:** Fable.

---

## §3 — THE MERGED OWNER-DECISION DOCKET (ONE sheet; assembled + RATIFIED at F-GOV-03)

Merges PF-r1 §3 + PC OD-S1..6 + PD OD-1..6 + PB OD-C1..5 + PA OD-PA-1..6; duplicates deduped
(source rows cited); decided matters recorded as supersessions BELOW the sheet, not rows.
Trichotomy governs: intact record → owner decides; refuted record → shield pierced; fence
ruling → cite-to-overturn.

| # | Row | Sources | Record state | The decision asked (with recommendation) |
|---|---|---|---|---|
| OD-1 | flip | PF | EP-3 PATH-B intact | keep vs prune |
| OD-2 | draw-svg | PF | EP-3 PATH-B intact; atlas hits prose-only, record holds a real historical live-proof | keep vs prune |
| OD-3 | physics/morph | PF | rides flip's PATH-B; glass `MorphRect` TYPE-only | couple to OD-1 |
| OD-4 | scroll/ | PF | K.W9 book + K-1 fence (cite-to-overturn w/ new Baseline evidence) | keep (default) vs overturn-and-prune |
| OD-5 | ingest/ | PF | K.W8 book intact; zero consumption disclosed | the round-trip thesis |
| OD-6 | kf internal/ target shape | PF + PC OD-S1 | owner's dislike = mandate; fan-in **11** measured; LT-10 cost census stands; G5′ names the superseded ruling | shape (a) distribute / (b) first-class primitives home, leaf law re-keyed / (c) split — **recommend (b)** (PC's pricing adopted) |
| OD-7 | R-HDR ictcp()/jzazbz() parse restore | PF + PB OD-C2 + PA OD-PA-1 | 11-day unpapered drop | restore vs CSS-native-only law — restore-lean (both siblings concur); tombstone by name if declined |
| OD-8 | spring() grammar ownership | PF + PB OD-C3 + PA OD-PA-2 | solver settled kf (K F6.6) | value-vs-kf — **recommend value** (CSS-spec territory; all three sheets concur) |
| OD-9 | R-EVAL static calc evaluator | PF + PB OD-C4 + PA OD-PA-3 | WEAK candidate | **recommend RETIRE with rationale** (PA/PB concur; kf resolve/ owns calc); terminal disposition either way |
| OD-11 | easing D13 halved-curve rider | PF | from D13 | restore-or-tombstone |
| OD-12 | glass peer strategy | PF (r1 double-state cured) | wedge ERESOLVE-hard either way | **recommendation-with-default: RANGE-WIDEN `^6\|\|^7` + `^4\|\|^5` as a glass MINOR pre-boundary** (prerelease-admitting variant iff a staging window is used, §2 F-GOV-05); hard bump = the alternative. Ratified ONCE, here — the train preamble then cites the ruling |
| OD-13 | contrast-color() WCAG republish | PF + PB OD-C1 | P2.2 row 8 | republish vs evaluation-internal (PB leans internal) |
| OD-14 | kf-demo execution mode | PD OD-1 + PC OD-S4 | C4 pulls kf demo in-scope; P4.5 keeps it kf-owned | (a) explicit owner grant for direct edits vs **(b) dispatch cars (recommend, per protocol)** — rules F-GOV-02 car (i) |
| OD-15 | THE OPUS-MECHANICAL ROSTER | new (L1 §0 rider) | "per-tranche owner-ratified" is the law's own text | ratify the named Opus-mechanical seat list (census scripts, fold sweeps, chase edits, ledger checkers) for THIS tranche; Fable holds all design/adjudication (C17) |
| OD-16 | parser-swap release vehicle | PA OD-PA-5 | frozen surface makes a 4.1.x minor lawful | 4.1.x (ride/follow W56, never fork) vs hold to the 5.0.0 boundary |
| OD-17 | R-SOA bench threshold | PB OD-C5 | anti-goalpost clause | ratify the measured batch-win threshold BEFORE the exhibit runs |
| OD-18 | kf old share-link posture | PD OD-3 | no-dual-paths edict vs link continuity | hard cut (recommend) vs one-release decode-only reader |
| OD-19 | EasingPicker precision law | PD OD-4 | the "low-res" mark | full-float + display rounding (recommend) vs author-grid snap |
| OD-20 | C3 mobile-toolbar form | PD OD-5 | owner's personal mark | second-band vs one-gesture dock — thrice delivers both; owner picks at design gate |
| OD-21 | WatercolorDot dose-class | PD OD-6 | glass dose law vs breath-of-life | substrate-by-law (recommend) vs hover whisper iff glass agent ratifies |
| OD-22 | glass batch release timing | PD OD-2 | batching law vs critical path | hold for full batch (recommend) vs early-release the two centerpiece rows |
| OD-23 | BBNF canonical home | PA OD-PA-4 | C1/C14 | in-value now (recommend); upstream reconciliation → BBNF session |
| OD-24 | stylesheet.ts R4 tracked-RED carry | PC OD-S5 | allowlisting = masked-fallback lie | tracked-RED named transfer to PA (recommend) vs allowlist |
| OD-25 | transform/path.ts 564>500 | PC OD-S6 | R4 allowlist exists for this | split at natural seam iff thrice finds one; else reasoned allowlist row |
| VIS-1 | oscillator prune (visibility) | PF | record REFUTED — `published-surface.md` Oscillator row's BOTH named consumers (glass-ui BB `W-EASING-PRIMITIVE`, demo KF-OSCILLATOR scene) re-verified FALSE on disk (0 hits, this adjudication); shield PIERCED | proceeds + record-correction rider unless the owner objects |
| VIS-2 | transform/decompose.ts prune | PC OD-S2 (demoted from ask to visibility) | 0 consumers on all four trees, re-verified; P3.2 verdict PRUNE | proceeds, tombstone RIGHTLY, unless the owner objects |

**Supersession records (off the docket — decided, papered, never re-asked):**
- **api/ STAYS at `/api`** — C7 is the owner's LATER ruling (addenda WIN); P3.2's EXTRACT row
  RETIRED-superseded with rationale (r1's OD-10 "or re-rule" was re-litigation — removed;
  PC OD-S3's form adopted). PE-api hardens in place; fourier isomorphism per §4.
- **parse-that ≤1.1 / leveraged-as-published** (C1/C15) — charter matter, not a row.
- **SCI-1 SHIP-4.1.x (D54)** — inherited DECIDED; R-INTO extends, never re-opens.

Unilateral riders (evidence-complete, fence-clean, no docket row): kf split-text (486;
casualty split-a11y oracle, named) · kf motion-path (~180) · value quantize DEMOTE-to-demo.

---

## §4 — THE CROSS-REPO WAVE MAP

| Repo | What crosses | Vehicle | Channel |
|---|---|---|---|
| keyframes.js | capability-diff spec · kf-7 co-land · scar deletions · easing brief (RATIFY-default) · **census-LOCK gate** · **WAAPI engine uplift** · **regex census** · **the gates pack (W9-land, MR2/MR4, CI wiring, lighthouse, CH-05, clamp fold, zone-binding, consumer-claims rule, oscillator correction)** · zone-docket outcomes · (reserved) PD kf-demo cars | F-GOV-02 cars a–i, -03, -05 | `VALUEJS-INBOUND-*` + INBOUND-LEDGER (P4.5) |
| glass-ui | peer move per ratified OD-12 · batched root defects (kf 15-row marks register folded) · shadcn-gap prototype specs (C5; π owned by originators, pointers in the ledger) | F-GOV-02.4, -05 | active glass tranche `coordination/` (re-derive at dispatch) |
| atlas (ACTIVE) | ranges/devDeps at boundary · 3 TimingFunction chases (IN-ATLAS-5) · MorphSVG `./engine` continuity | F-GOV-05 | atlas chase ledger |
| sci-report | crosses atomically at its own glass-7 consume | registry row (no wave) | — |
| fourier-analysis | C7 API-isomorphism reference for PE-api; dispatch on demand | PE-api; channel via F-GOV-02 grammar | fourier inbox |
| parse-that | consumed as published 1.0.0; needs → PT-E letters carrying the C15 cap | charter matter + F-GOV-06 grep | PT-E |
| bbnf-lang | C14 dual-definition = PA-parse's; pin only | — | — |

---

## §5 — THE LANDING MAP (regenerated; 1:1 receipt form lands as F-GOV-02.6; corrections from adjudication marked ★)

Legend: [G-nn] = F-GOV-nn · [PA/PB/PC/PD/PE] = the on-disk sibling program (receipt audited
at G-06) · [DOCKET] = §3 · [CAR-x] = F-GOV-02 dispatch car. **No phantom targets.**

**L1:** §0 routing/verify-tier → G-0 template + OD-15 ★(rider added) · §0 pins → §1/G-0 ·
§2 parser waves → [PA]; consumer-signature freeze priced [G-05]; ★census-LOCK → [CAR-e] ·
§3 color ladder → [PB]; scars [CAR-c]; R-INTO-rides-W56 → G-05 preamble · §4 structure →
[PC]; api tension → supersession record ★(off docket); boundary tombstones [G-05/G-01] ·
§4 demo/glass batching → [G-02.4] (enforcement join ★named); audit [PD] · §5 zones →
[G-03/DOCKET]; test-binding → rider clauses · §6 co-land → [G-01/G-05] · §7 edicts:
tombstone law → [G-01] ★(backfill = the fired instance's cure); anchors → G-0 step
★(demoted from wave/born-RED); chronics/partials → [G-06]; prompt-recap → this map ·
§8 thrice → [G-03 briefs, G-06 close] · §9 single-sheet docket → §3 ★(five sheets merged);
zero drops → [G-02.6 receipt + G-06].

**L2:** P0.1 → G-0 · P0.2: r1 (wedge) → [G-05]; r3 (strip) → [G-01a]; r4 → G-05 preamble;
r2/r5 → [PA]; r6–14/16–18 → [G-03] inputs + ★[CAR-h] (ex-apparatus, kf-side) + [G-04]
(ex-apparatus, value e2e); r15 → [PB] premise · P1.4 → [CAR-d] + ★[CAR-g] own row · P1.5 →
G-0 registry axis · P1.6 → G-0 cap + PT-E · P2.1 → [CAR-c]; mechanism → [G-01 birth
certificate + backfill] · P2.2 rows 1–6,9,13,14 → [PB]; 7 → [PA]; 10/11/12 → [DOCKET
OD-7/8/9] ★(deduped ×3); 8 → [DOCKET OD-13] · P3.1 → [G-03 + DOCKET + riders]; shrink notes
→ [PC via CAR channel] · P3.2 → strip [G-01]; decompose [VIS-2 ★]; quantize rider; subpaths/
isomorphism [PC]; api supersession ★; e2e → ★[G-04] (orphan homed); PNGs/demo-tests [PC] ·
P3.3 rows 1–8 → ★[CAR-h] (phantom retired; P4.5-correct as dispatches); row 7 also G-05
preamble · P3.4: r1 → [G-04 scope note]; r2 → ★[G-04]; r3 → [PC F-02/03 ★verified extant];
r4 → [G-01b ★allowlist]; r5 → [PA bench]; r6 → [G-01c ★two-grain] · P4.1 → [G-05 price
sheet] · P4.2 → [G-01/G-05 ★prerelease-priced] · P4.3 → §1 note ★(step, not wave) · P4.4 →
G-0 rider · P4.5 → [G-02] · P5: SCI-1/W56 → G-05 preamble; D-GAP-6 → 4.1 rider; §D/RF-18 →
registry rows; PT-E → §4; W9 `b920b190` → [CAR-h]; CH-05 → [CAR-h]; EP-3/K.W8/W9/F6 →
[DOCKET record states]; marks register → [G-02.4]; W53 → [PB gating note]; W55 → [G-04
★.github untouched]; W56 → G-05; oscillator → [CAR-h + VIS-1] · P6 → G-0/G-02/G-06.

**Addenda:** C1/C15 → G-0 + supersession · C2/C3 → [PD] (OD-19/20 docket rows merged ★) ·
C4 → [PD briefs] + ★[CAR-i reserved] + [DOCKET OD-14] (ownership priced, not assumed) ·
C5 → [G-02.4 channel] + [PD census] · **C6 → [PD F-10/F-15] ★(mis-route corrected)** ·
C7 → supersession + [PE] + §4 fourier row · C8 → 6 governance waves of the 50+ · C10/C22 →
[PD] · **C11 → [PC] ★(previously unmapped)** · C12 → [PA census] + ★[CAR-f kf-WAAPI half] ·
C13 → [PB] · C14 → [PA] · C16 → honored (this file = the seat's sole write) · C17 → G-0 +
OD-15 · C18/C19 → probe arms (outside program) · C20 → [G-03/G-06] · C21 → the 6-wave
code-first cut ★ · C23 → this arm's charter. **★Mail/INBOX law (E13, 2026-07-17) → G-0
template line + G-02 protocol page (previously absent).**

---

## §6 — ADJUDICATION LOG (every skeptic claim → verdict, evidence, disposition)

All probes run by this adjudicator this session (read-only; the sole write is this file).
Key executions: `ls armB/` (roster) · greps over all six program files · `git show
v3.1.0:package.json` + `git show HEAD:package.json` (value.js) · CHANGELOG [4.0.0]/[3.1.0]
sections · sweep-kf-lib §2/§3/§4/§5 + sweep-value-lib §4 · kf `published-surface.md`
Oscillator row + consumer greps (glass-ui/src, kf/demo) · kf `src/animation/easing.ts:28-40`
· `semver.satisfies` execution.

### Skeptic 1

- **S1-A1 (phantom apparatus sibling) — PROVEN.** `ls armB/` shows exactly six program
  files; grep "apparatus" hits PF lines 11/352/364/366/369/379 as a routing TARGET and no
  sibling as an owner; PD-frontend greps e2e|oracle = 0 — the e2e adjudication was ownerless.
  ADOPTED: roster reconciliation (F-GOV-02.1); kf-side inventory → CAR-h (P4.5-correct:
  kf-repo work was always dispatch cargo); value e2e → F-GOV-04 (skeptic's own sanctioned
  absorb-by-name cure); anti-phantom gate in F-GOV-02 + F-GOV-06 grep.
- **S1-A2 (docket double-booking) — PROVEN AND UNDERSTATED.** Not two sheets but FIVE
  (PF §3, PC OD-S1..6, PD OD-1..6, PB OD-C1..5, PA OD-PA-1..6), with R-HDR, spring-grammar,
  and R-EVAL each booked THREE times. ADOPTED: §3 merged single sheet, sources cited,
  duplicates deduped, absorb-or-supersede gate at F-GOV-03.
- **S1-A3 (roster inconsistency; PE unchartered in PF §0) — PROVEN.** `program-PE-api.md`
  exists; PF §0 named "apparatus" instead. ADOPTED: §0 roster names the six files.
- **S1-A4 (C11 unmapped) — PROVEN.** grep C11 in r1-PF = 0 hits. ADOPTED: §5 C11 → PC ★.
- **S1-A5 (C6 mis-routed) — PROVEN.** r1-PF:392 routed C6 to parser/color/API;
  PD F-10 (:198) + F-15 (:273) own it. ADOPTED: §5 correction ★.
- **S1-A6 (kf-WAAPI car missing) — PROVEN.** PA F-04 (:49/:134/:147) routes it by name "to
  the kf program's WAAPI wave via the kf inbox"; r1-PF's pack had no car and no kf program
  exists. ADOPTED: CAR-f with the sweep-kf-lib §5 absent-member list.
- **S1-A7 (mail/INBOX law absent) — PROVEN.** grep inbox|mail in r1-PF = 0; the 2026-07-17
  owner edict is standing law. ADOPTED: G-0 template line + protocol page.
- **S1-B1 (G-01 born-RED vacuous) — PROVEN.** PC:262-264 cites the re-derived anchors
  (:37/:153/:227); no formation artifact cites the stale numbers, so the RED could never
  fire; the GREEN stamp criterion is incoherent against continuously advancing HEADs.
  ADOPTED: wave demoted to G-0 charter matter; re-derivation = template gate STEP.
- **S1-B2 (G-05 gate self-referential) — PROVEN** on the r1 text ("the ledger check is the
  gate"). ADOPTED: the close-time join (sibling deliverable paths grep'd for glass-ui paths
  vs ledger rows) — a check that can fail (F-GOV-02.4, F-GOV-06).
- **S1-B3 (G-06 REDs are prose) — PROVEN in substance.** "Proposed anywhere" had no checker;
  npm view fires post-irreversibility. ADOPTED: the C15 preventive seat = formation-grep over
  program/spec/dispatch files (mechanically runnable, F-GOV-06) + the cap carried in every
  PT-E letter; npm view kept, honestly labeled post-cut confirmation.
- **S1-B4 (birth certificate unrunnable on "the 7 frozen keys") — PROVEN BY EXECUTION.**
  `git show v3.1.0:package.json` → 8 keys: `.`, `./color`, `./parsing`, `./math`,
  `./easing`, `./transform`, `./units`, `./quantize`. ADOPTED: tag-shape-agnostic two-grain
  tarball census (F-GOV-01c).
- **S1-B5 (ratification-after-cargo ordering bug) — PROVEN** (textual: §3 "ratifies via
  -08" vs wave-07 "docket ratified" dependency vs terminal -08). ADOPTED: ratification =
  F-GOV-03 exit gate; go/no-go = F-GOV-05 ENTRY gate.
- **S1-B6 (manifest gate should be allowlist) — PROVEN.** v3.1.0 shipped runtime
  `@mkbabb/parse-that ^1.0.0` (verified); R-PARSER re-adds it; a 3-name denylist passes any
  other accidental dep. ADOPTED: `dependencies ⊆ {@mkbabb/parse-that}` (F-GOV-01b).
- **S1-C1′ (OD-10 re-litigation) — PROVEN.** C7 is the later owner ruling; "or re-rule"
  re-asked it. ADOPTED: supersession record, off the docket (PC OD-S3's form).
- **S1-C2′ (OD-12 double-state) — PROVEN** (train law vs open docket row). ADOPTED:
  recommendation-with-default in OD-12, ratified once; train preamble cites the ruling.
- **S1-C3′ (easing brief falsely symmetric) — PROVEN.** sweep-kf-lib §4: "legitimate
  CSS-keyword classifiers, the strongest consume-vs-ratify candidate"; the faithful-CSS-twin
  doc law + F.W7 rationale verified on disk at easing.ts:29-39; PA OD-PA-6 independently
  recommends RATIFY. ADOPTED: CAR-d carries RATIFY as fenced default, overturn burden
  explicit.
- **S1-C4′ (6/8 paper waves; deps bomb queued behind ceremony) — PROVEN IN PART.** The
  priority inversion is real (the bomb is committed at HEAD — verified — and its cure is
  one commit); the pin-register wave was sand. But W2/W3/W6-class paper is CANON-REQUIRED
  (P4.5 dispatches, L1 §9's single sheet + zero-drop audit) — the cure is consolidation and
  code-first ordering, not zero paper. ADOPTED: F-GOV-01 first and dependency-free; 8→6
  waves; register/laws demoted to G-0.
- **S1-C5′ (Opus rider missing) — PROVEN.** L1 §0's own text: "per-tranche owner-ratified."
  ADOPTED: OD-15 makes the ratification concrete; G-0 rider on every Opus assignment.
- **S1-D (survivors) — CONFIRMED by independent probes:** oscillator record-refutation
  re-verified (0 consumer hits); deps bomb committed at HEAD; price-sheet numbers match the
  sweeps; the INBOUND-LEDGER completeness check kept as a real gate.
- **S1-E (5-wave counter-program) — ADOPTED IN SUBSTANCE** (code-first W1, roster+channel
  W2, front-loaded docket W3, boundary W5, close W6), with two deviations: the train stays
  a PREAMBLE of the boundary wave (per S2-2.2, no standalone train wave OR doc), and the
  orphaned e2e adjudication gets its own wave (F-GOV-04) rather than an annex-hope — a row
  this size (~13.4k LOC, an owner addendum) needs an owning wave, not a routing suggestion.

### Skeptic 2

- **S2-1.1 (docket circularity) — PROVEN** (same textual basis as S1-B5). ADOPTED as above.
- **S2-1.2 (OWNER-DECISION LOC dead-lettered) — PROVEN.** Sound logic: terminal ratification
  + one-boundary law + boundary-last = ruled prunes have no lawful vehicle; and the r1
  decompose side-note ("structure program executes, governance carries the tombstone")
  contradicted its own execute-only-post-ratification clause. ADOPTED: F-GOV-03 ratifies
  before F-GOV-05; VIS-2 regularizes decompose (visibility, riders lawful as boundary cargo).
- **S2-1.3 (go/no-go after the go) — PROVEN.** ADOPTED: entry gate of F-GOV-05.
- **S2-2.1 (wave-01 sand; born-RED misapplied to doc-freshness) — PROVEN in the main**
  (S1-B1 evidence; the "permanently re-reddening by external drift" analysis is correct).
  ADOPTED: G-0 demotion; born-RED label reserved for live artifact defects (F-GOV-01's two).
- **S2-2.2 (RELEASE-TRAIN.md = parallel authority) — PROVEN IN PART.** The drift-apart risk
  is real and is the G5′ disease; ADOPTED: no RELEASE-TRAIN.md — the order lives in
  F-GOV-05's preamble, each law CITED to its canon home. DISPROVEN IN PART: the ordering
  itself is not wholly canon (widen-first, 4.1.x sequencing, OD-16's vehicle question needed
  a binding home) — a home was required; the wave-spec preamble is that home, a new
  normative doc is not.
- **S2-2.3 (wave-05 legislates π over siblings) — PROVEN IN PART.** ADOPTED: π owned by
  originating programs; the ledger holds capture POINTERS verified on disk at release — one
  bookkeeping. The batch ledger + enforcement join remain governance deliverables (real
  work, fallible gate) — folded into F-GOV-02, not a standalone wave.
- **S2-2.4 (wave-08 is the tranche's close) — PARTIAL.** PROVEN that terminal stuffing of
  docket+go/no-go caused §1 — both relocated. DISPROVEN that the close wave should vanish:
  L1 §9's zero-drop audit needs an owning wave (skeptic 1's counter-program keeps one too);
  ADOPTED middle: F-GOV-06 re-chartered explicitly as the TRANCHE close hosted by
  governance — no duplicate, no ratification, no go/no-go.
- **S2-§3 (OD-10) — PROVEN** (= S1-C1′). ADOPTED: supersession record.
- **S2-4.1 (census topology) — PROVEN BY EXECUTION** (= S1-B4). ADOPTED.
- **S2-4.2 (backfill omitted; fired instance unpapered) — PROVEN, with one precision:** the
  4.0.0 Breaking section DOES name the KEY-grain removals (root/`/parsing`/`/units` — text
  verified), so the unpapered extinction is specifically SYMBOL-grain (zero by-name
  capability tombstones; the "raw color-conversion exports, and other root-barrel internals"
  umbrella + the "`/color` owns … gamut mapping" continuity line verified). The [3.1.0]
  retro-correction precedent verified in its own heading ("the 3.0.0 record corrected").
  ADOPTED: F-GOV-01d — the backfill is the birth certificate's cure, in-wave; the two-grain
  census (4.1's cure) is what makes the symbol-grain hole visible at all.
- **S2-4.3 (allowlist) — PROVEN** (= S1-B6). ADOPTED.
- **S2-4.4 (prerelease semantics break the window story) — PROVEN BY EXECUTION.**
  `semver.satisfies('5.0.0-rc.1','^4 || ^5') === false`; `('5.0.0', …) === true`
  (includePrerelease would admit it, but npm peer resolution does not run that flag).
  ADOPTED: the two-leg matrix (RED pre-crossing on published state; GREEN on finals in the
  freeze window); any staging window requires explicitly prerelease-admitting ranges
  (F-GOV-05 gate 1; OD-12 notes the variant).
- **S2-4.5 (defuse-first) — PROVEN.** ADOPTED: F-GOV-01a is the tranche's first commit,
  dependency-free.
- **S2-5.1 (census-LOCK never dispatched) — PROVEN.** L1 §2's "boundary CENSUS LOCK: zero
  grammar productions on both sides" appears in no r1-PF deliverable; only the one row's
  adjudication was dispatched. ADOPTED: CAR-e, a durable kf-side gate.
- **S2-5.2 (C12 kf-WAAPI dropped) — PROVEN** (= S1-A6). ADOPTED: CAR-f.
- **S2-5.3 (C4 mis-homed, protocol violated) — PARTIAL.** PROVEN against r1-PF's landing map
  (C4 → [SIBLING:frontend] with no ownership note, no reserved car). DISPROVEN as a
  formation-wide violation: PD OD-1 (:324) and PC OD-S4 each already surface the
  execution-mode decision with dispatch as the recommendation — the formation did not
  assume the edit right; PF failed to carry the question. ADOPTED: merged docket row OD-14
  (once), CAR-i reserved on its ruling.
- **S2-5.4 (missing 05→07 dep edge; regex census free-riding) — PROVEN.** ADOPTED: F-GOV-05
  gate 6 (batch criteria as boundary dependency); CAR-g as an own-rowed dispatch.
- **S2 counter-shape (3 waves + charter matter) — ADOPTED IN SUBSTANCE** (G-0 charter
  matter; code-first; front ratification; boundary entry-gate), with the S1-convergent
  deviations: the close wave survives (re-chartered), and the orphan e2e work makes a sixth
  wave — a formation with a ~13.4k-LOC ownerless owner-challenge cannot claim zero drops at
  any wave count.

### Convergence statement

Both skeptics' strongest surviving points are structural and now load-bearing in this final:
(1) no route without a real file (receipt table + anti-phantom grep, RED today by design
until the r1 rows are re-homed — the one honest born-RED among the paper gates); (2)
ratify-then-ship (F-GOV-03 → F-GOV-05 entry); (3) code-before-paper (F-GOV-01 first,
dependency-free); (4) no gate that cannot fail (selftests, the close-time join, two-grain
tarball census, the formation-grep, two-leg matrix). Where the skeptics diverged (5 waves vs
3; close wave vs tranche-close-elsewhere; train home), the final takes the intersection that
satisfies both stated rationales and records the residual as design choice, not evidence
dispute.

---

*— thrice adjudicator, armB PF-governance, 2026-07-19. Sole write: this file. Model verified
from own system prompt: `claude-fable-5`.*
