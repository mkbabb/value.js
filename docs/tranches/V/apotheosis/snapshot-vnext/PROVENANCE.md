# Provenance

## Context law

The root session is seeded only with:

1. `docs/tranches/V/coordination/keyframes-inbox-2026-07-18-vnext-ingestion-prompt.md`
2. `docs/tranches/V/coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md`

Those two letters contain the load-bearing facts at citation grain.

Deep evidence is pull-on-demand only from:

`/Users/mkbabb/Programming/keyframes-v-exec/docs/tranches/V/coordination/vnext/`

The quarantined directory:

`/Users/mkbabb/Programming/keyframes-v-exec/docs/tranches/V/coordination/vnext/r1-opus-refuted/`

is never loaded. It remains evidence-trail quarantine only.

The audited Claude evidence source is chat history, not a substitute workspace. The exact keyframes session histories are:

- `/Users/mkbabb/.claude/projects/-Users-mkbabb-Programming-keyframes-js/10dfa2b9-2e44-4e6b-a6c5-b028a506ba71.jsonl`
- `/Users/mkbabb/.claude/projects/-Users-mkbabb-Programming-keyframes-js/58f34108-b347-4938-adcd-9e676fc3e1fa.jsonl`

These histories are evidence inputs only. They do not authorize treating a Claude session as a writable workspace.

## Model provenance

The original prompts used Fable and Opus terminology. Owner substitution for this tranche is:

- Fable → Sol
- Opus → Luna

Formation uses two fresh hostile Sol critics and one fresh Sol adjudicator. Luna is reserved for future mechanical implementation fanout after formation. Model declaration alone is insufficient; the served model must be verified where the execution surface exposes it.

## Repository register

The register below is a read-only snapshot taken at
`2026-07-18T16:50:04-0400`. `Status` is the entry count and SHA-256 of the
exact NUL-delimited output of `git status --porcelain=v1 -z`; the empty digest
is `e3b0c442…`. This gives a reproducible dirty-state fingerprint without
copying hundreds of unrelated user paths into every packet. A nonzero count is
dirty even when only representative path families are named.

| Repository | Canonical path | Branch | Exact audited HEAD | Status: count / SHA-256 | Representative dirty families and law |
|---|---|---|---|---|---|
| value.js | `/Users/mkbabb/Programming/value.js` | `tranche-u` | `c654824e0b252cda7f8490b67f182a48c48cc0ed` | `9 / 67abe419bb83f4de65ae898d27d6333244f09f4911367b7efb671780992f852b` | Modified `scripts/dev/dev.sh`; untracked user work under `docs/tranches/T` and `docs/tranches/V`, including this formation. Preserve every existing artifact. Earlier source-evidence baseline remains `db77dbd8`. |
| keyframes canonical | `/Users/mkbabb/Programming/keyframes-v-exec` | `master` | `81a56990736ced5b5edde0b84c527680ac7689b1` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean at this audit. This alone is the canonical keyframes execution checkout; earlier source-evidence baseline remains `0dac636b`. |
| keyframes owner checkout | `/Users/mkbabb/Programming/keyframes.js` | `master` | `a59d3a22da080a8ed224e8d675112bb3bb0135b0` | `252 / 2f3c71152e446fd54bfb81b5998131bf1554916d59f5e3ef0318ec1b2a0be77f` | Broad source, test, benchmark, workflow, documentation and build work. Excluded owner/worktree evidence root: never reset, overwrite or treat it as canonical. |
| parse-that | `/Users/mkbabb/Programming/parse-that` | `master` | `ef10d5b78236c4a30a7bb22a6113b60bdc4bdf42` | `8 / 7c36d761c35f3f3dbb702ef8405707e5c93458c33204b98b7255ff706ae6953d` | Untracked `.gitmodules`, instruction/precept/tranche material, the banked V-next Sol research under `docs/tranches/B/`, and Rust artifacts. Preserve all; no novel uplift in V-next. |
| bbnf-lang | `/Users/mkbabb/Programming/bbnf-lang` | `master` | `af15f63e0d2d3d719938c13b906a50acbb92ea3b` | `73 / a65e92377e178d1d511cf6c66727b0494982208e680a3aebbe0448413c93a0fa` | Active generated/runtime, CSS-L4, Tape, test, playground, `restart/skinny`, research and `xtask` work. Preserve all 73 entries; V-next may migrate the playground in C02L but may not perform grammar, Tape/SoA or major parser uplift. |
| glass-ui | `/Users/mkbabb/Programming/glass-ui` | `master` | `662948389ffd0b0e7d6d78a53388bf89301dca50` | `26 / 9d350abe038015e9c3338ec0e93903a1ccd3e288cb89bf7ed246487ac1503572` | Active BJ and IOS27 tranche/research/prototype work. Re-pin and overlap-audit before G00; preserve all 26 entries. |
| fourier-analysis | `/Users/mkbabb/Programming/fourier-analysis` | `m/w1-bump-migration` | `cd26c6533adc32dfe1453d74117d3cb73b89ea16` | `27 / a471c6fc1151d374ed31aa9e7d3da38c0bd65d98b98a1a177d684817e2f62631` | Existing run-board, lock/manifest and Vue component work. Preserve all 27 entries and isolate `A21R A21 A22 A22S A21A A21J A23 A23S A23H A24 A25 A23C A26 C03P C03 C04`. |
| active Atlas | `/Users/mkbabb/Programming/.p-totality/atlas` | `p/totality` | `fe9abcfcdd037a8707ae8a8da5c0183261f7ab41` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean at this audit. This is the sole canonical active Atlas consumer checkout for C01. |
| standalone Atlas | `/Users/mkbabb/Programming/atlas` | `master` | `1e2b911bef561102fa499f8c0f7c1256daf40b5d` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean but stale and excluded. Never substitute it for the active `.p-totality/atlas` root. |
| sci-report | `/Users/mkbabb/Programming/sci-report` | `feat/tranche-k-arc` | `da1e3763f53d56771221297a41df6b9f3049d4bc` | `378 / 87d395f406991ec3541738764241eed811f947704546b33160116655c9638438` | Extensive user work across embedded Atlas, dashboards, reports, tests, data, workflows and manifests. C02 begins with an exact overlap audit and preserves all 378 entries. |
| bbnf-buddy | `/Users/mkbabb/Programming/bbnf-buddy` | `master` | `b218922042acdc8c94c35847bd134023c6085749` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean at this audit; C02B owns the packed value/keyframes/Glass migration without parser novelty. |
| Slides | `/Users/mkbabb/Programming/slides` | `main` | `b538506a5b5611399a968104d452dda1c6393374` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean at this audit; C02D owns the live root. The separate Slides-K root receives its own derived-root disposition. |
| Slides-K | `/Users/mkbabb/Programming/slides-K` | `tranche/til-briefing-K` | `5b546be482b99dd02c8c56971ddf154795d69be4` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean at this audit; this distinct Git worktree is a mandatory C00U root. C02D returns its included row or a typed unavailable blocker and never infers mutation authority. |
| Muster frontend | `/Users/mkbabb/Programming/muster/frontend` | `master` | `87ee926d385824708265597d6018bb87cc69f2e9` | `89 / df1c7d57db8ff2503f45a167d990203761958289c9b25660dc45522a27206be8` | `realpath` resolves `/Users/mkbabb/Programming/Muster/frontend` to this same lowercase path and repository. Active tranche audit images/JSON and documentation are dirty; C02M preserves all 89 root entries and does not invent a keyframes edge. |
| Speedtest | `/Users/mkbabb/Programming/speedtest` | `master` | `7212e7332b5dcbcf5d608622d6418da2db845ea8` | `0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | Clean at this audit; C02S owns every live `ElementMorph` casualty. |
| Words frontend | `/Users/mkbabb/Programming/words/frontend` | `master` | `26b16ffd36c90ed23405479c9ac5527829dc7649` | `23 / 730a35342071fbaa83d1cbb34c4e027a39f3f3e5ae3e9dafff76be9486568526` | Git root is `/Users/mkbabb/Programming/words`; backend, docs, root lockfile and new research/test artifacts are dirty. C02W preserves all 23 root entries and scopes mutation to the frontend migration. |
| latex-paper | `/Users/mkbabb/Programming/latex-paper` | `master` | `c14047399813a0e72f033fe9a0a5f749aa266768` | `18 / 2b2479668e92f74f6f11edd11dfc9704f34f5da5dd3eb69e8e3a8f7faaef97e9` | Untracked VIRT specifications, traces and waves. Preserve all 18 entries; P06 owns its direct parse-that consumer proof and C00U/C05 retain it in the universe. |

No missing or abbreviated dirty-state description may be interpreted as
clean. Every implementation wave refreshes the exact NUL-delimited status,
records staged/unstaged/untracked overlap and preserves all user-owned work.

## C00U root law

The signed included universe is exact at formation time:

```text
parse-that/typescript
value library + demo + API
keyframes-v-exec library + demo
glass-ui
.p-totality/atlas
sci-report including its in-repository Atlas
bbnf-buddy
slides + the distinct mandatory Slides-K worktree
bbnf-lang/playground
muster/frontend at its canonical lowercase realpath
speedtest
words/frontend
fourier-analysis API + web
latex-paper as the P06 direct parse-that consumer
```

The stale `/Users/mkbabb/Programming/atlas` root is excluded. Derived
`.p-totality/sci*`, keyframes owner/worktrees and BBNF forks/scratches; Claude
worktrees; csc411 and DNS roots with no current edge; and vendored Hassio are
separately dispositioned rather than silently counted as tested. C00U scans
recursive package manifests, npm package-lock v2/v3 direct/transitive edges,
runtime/type/dynamic/CSS imports, origins/worktrees and realpaths. Workspace
child manifests are recursively traversed; C05 owns workspace install/link
topology. C00/C01–C04 own exact packed export/alias resolution. A26/C05/C10
own deploy-root provenance and consumer-cut evidence. Those routed truths are
not fabricated as C00U dependency-edge kinds. Every discovered root and
supported edge receives exactly one `included`, `excluded`, or `unavailable`
row. Required roots, including Slides-K, may be included or unavailable but
never excluded. `Unavailable` always names a retrigger; it never means
inactive. Consumer count is evidence of blast radius only, never a KEEP
verdict.

The executable boundary is [`consumer-universe.schema.json`](consumer-universe.schema.json)
plus [`tools/resolve-consumer-universe.mjs`](tools/resolve-consumer-universe.mjs).
Every root binds repository identity, scanned path, canonical realpath, branch,
HEAD, NUL-porcelain dirty digest, and separately evidenced origin, worktree and
deploy-root observations. Every edge binds source, internal root or explicit
`external:<package>` target, package, literal specifier, edge kind, evidence and
an existing canonical wave owner. Duplicate IDs/realpaths, unknown roots or
owners, incomplete discovery methods, absent evidence and mismatched evidence
hashes are invalid input. A valid unavailable disposition names an
existing-wave retrigger and produces an auditable receipt with
`resolvable:false` and exit status 2; it therefore cannot satisfy C05.

The resolver's `vnext-consumer-universe-receipt/2` is exact JCS plus a newline.
Its self-hash omits only `receipt_hash` and binds the semantic input hash,
physical input bytes, schema, resolver and full 190-wave registry. C05 reruns
the resolver and accepts only exit zero, `resolvable:true`, and an empty blocker
vector; a copied or hand-edited receipt is not closure evidence.

## Published parse-that control law

V-next consumes only the exact published `@mkbabb/parse-that@1.0.0` registry
receipt. P04/P07 return a signed `no-rc` receipt and C10 does **not** republish
parse-that. Every blocker, public change, feature novelty, combinator family,
Tape/SoA/Pratt change, package-topology change or BBNF uplift routes to the
active campaign and holds Value adoption pending a separately authorized
future epoch. Prior Sol research remains banked and uncommitted there.
The current `PT-0.W0` checkpoint is a fail-closed pin/content harness, RED
until a quiescent two-census capture. Earlier P/A/G/I/S parse families are
engine assays without product credit. Neither class is a Value input: P00/P01
may reopen only from a new terminal full-union epoch, and any later engine organ
must be independently reimplemented as part of a complete VT vertical rather
than imported from a parse-only artifact.
The frozen CSS-L4 W0 input epoch was subsequently independently adjudicated RED
at clean count zero: every critic finding held in substance. It therefore
supplies no CSS-support, schema/type, coverage, or candidate credit. The
replacement source-aware epoch is being built in an isolated BBNF evidence
root and remains outside Value until the same terminal resnapshot gate closes.
The next coordination checkpoint reports four full orthogonal verticals—
staged/compact tape, independently verified/schema-row, persistent
incremental/rope, and GLL-SPPF/eager arena—without relaxing any upstream lock.
That portfolio is not Value authority or product credit: `PT-0.W0` remains
deliberately RED and does not freeze or recapture Value while V-next formation
moves. Graph P3.1 resource twins are measured green, but semantic movement
remains denied; CSS/JSON remain isolated assays with zero candidate or product
credit. P00/P01 therefore still wait for a reconciled then-current published
tip plus artifact digest and refresh V2 from public truth. Once that boundary
is stable, Value returns only exact HEAD/tree/receipt identity upstream as
consumer evidence, never authority; T/U, the uncommitted bank, and private
novelty remain inadmissible.

## Package evidence

Observed current package relationships include:

- value.js working tree lists glass-ui and keyframes.js as production dependencies;
- keyframes.js 6 depends on value.js 4;
- glass-ui peers against value.js/keyframes.js;
- the current value dependency block therefore creates an invalid conceptual package cycle and must be relocated to demo/development ownership;
- Atlas currently consumes older value/keyframes/glass generations;
- sci-report also requires an explicit major adoption;
- Fourier’s frontend pins substantially older generations and requires a separate migration.

Exact package versions and lockfile state are re-read in the owning consumer wave.

## Standards evidence

The formation used July 2026 editor-draft and Webref inventory evidence.
Execution freezes `standards-lock.json`: exact CSSWG/Snapshot commits and
recursive normative closure, at-risk allowlist, curated Webref commit and
package integrity, `web-specs` version, WPT paths/META projection, browser
builds/OS/flags, Node/V8/CPU/OS and canonical manifest hash. “Current” and a
network-head read are never acceptable pins.

Claims of full CSS coverage refer to a total operation-vector join over the
complete facts inventory. Parse, type, evaluate, resolve, adapt and the three
serialization modes are independent cells; delegation and refusal are
operation outcomes, not stages. Total coverage never means universal local
evaluation of layout, cascade, resources, device profiles or live browser
objects.

## Dirty-state preservation

Every wave records:

- initial `git status --short`;
- pre-existing staged work;
- pre-existing unstaged work;
- untracked files;
- files the wave intends to touch;
- overlap resolution.

A wave may not clean, reset, stash, stage or commit unrelated user work.
