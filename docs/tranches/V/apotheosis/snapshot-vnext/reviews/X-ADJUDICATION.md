# Cross-Band Adjudication

Signature: `X-ADJ-SOL3-2026-07-18`

Verdict at signature: **NOT CLEAN**. Two independent Sol critics assumed the
assembled formation was wrong. A third Sol adjudicator tested both reports
against the canonical files and accepted the following binding amendments.
This is a historical formation verdict: each accepted row must be repaired and
then challenged by two entirely fresh whole-corpus passes before formation can
close.

## Binding amendment ledger

| # | Finding and adjudicated disposition | Owners / proof |
|---:|---|---|
| 1 | The ElementMorph graph was deletion-before-migration. G07 and C02S use hashed provisional no-link K10/K13 packs; K14/K20/K21 wait for their relevant migrations; K21 alone deletes; C00/C05 repeat final packs. | G07, C02S, K14, K20, K21, C00, C05; canonical wave graph |
| 2 | The former MC demo tree duplicated codecs and easing math. Delete that projection; `DEMO-TARGET-DAGS.md` is sole expanded demo authority. | M10T; `KEYFRAMES-TARGET-PATHS.json` + validator |
| 3 | The former `TARGET-DAGS.md` library tree omitted index/proof/bench. `KEYFRAMES-API.md` is sole expanded library authority. | K22T; same machine path manifest |
| 4 | The 77-row seed ledger overlapped broad and child ranges. Replace it with an exact, disjoint Markdown-block bijection and exact excerpt hashes. | formation root; `validate-seed-inventory.mjs` |
| 5 | Chronological narrow-axis owner amendments are sound. OA-14 allows six value paths only if `./value` has no independent intrinsic job after CSS ownership moves; consumer casualties remain mandatory evidence. | OWNER-AMENDMENTS, V29T, C00/C05 |
| 6 | C00U promised dynamic ownership without a resolver. Add a closed root/edge universe schema; unknown included owners and unavailable roots block C05; emit a hashed resolved-owner receipt. | C00U, C05 |
| 7 | C09 named dynamic reopenings without a resolver. Validate the latest clean C08 return, every owner-return hash/status and corpus epoch, then emit C09's exact dependency receipt. | C08, C09 |
| 8 | Consumer roots are now present, but Speedtest must own V/K/G and Words exactly K/G unless evidence disproves an edge. | C02S, C02W |
| 9 | `adapt` is an operation axis, not an outcome. Use implemented/delegated/refused/not_applicable for WAAPI; preservation is inapplicable there. | K04I and universal return contract |
| 10 | Five-column API tuples cannot generate OpenAPI or clients. Add one strict machine registry with owners, complete HTTP semantics and complete headless state/retry semantics. | A00–A26; API source/schema/validator |
| 11 | Returns could invent gate IDs and API ownership. Bind every return to the exact hashed wave-row contract and canonical gate set; bind API IDs to the strict manifest and owning wave; type the complete C10 release annex. | universal return schema/validator, C10 |
| 12 | C07 physical proof lacked named devices and budgets. Freeze hardware/OS/browser/viewport/DPR/refresh/power/thermal evidence and frame/input/memory/thermal thresholds; absence of iOS/Android hardware remains RED. | C07 |
| 13 | Easing shared-domain prose lacked an algorithm. Use certified `[0,1]` extrema, include 0/1, five-percent padding with 0.05 minimum, outward 0.05 rounding, no filter rescale, and typed refusal beyond finite/certified `[-8,8]`. | G09, M08 |
| 14 | DesignSync was unavailable and no substitute is equivalent. Bank the method gap at formation root; retrigger G00/D00A/M00 before implementation if it becomes callable. | seed ORCH-07, DESIGN-PROVENANCE |
| 15 | Runtime capacity is root plus three children, so requested 5–6 concurrency is capability-limited. Add a strict seat/round ledger with requested/served Sol evidence, family iteration 1–3 and owner escalation after three. | formation root, AUDIT-REGISTRY |
| 16 | The proposed module-graph ref defect was disproved: the tool already resolves an exact commit and refuses non-HEAD refs. | no amendment |
| 17 | PLAN understated Glass release semantics. C10 publishes exact Glass 8 with value 5/keyframes 7 and exact framework peer ranges. | PLAN, C10 typed annex |
| 18 | V1 parse coordination is immutable history; current clearance was stale as SK-V25 advanced. Preserve V1, append a fresh V2 snapshot, require exact same-pin/intent/ack/lock/candidate hashes, and consume no V3/Tape/SoA/Pratt/topology novelty. | P00, P02, coordination V2 validator |

## Accepted evidence state

- `node tools/validate-formation.mjs` proves 190 nodes and the current exact
  edge set; it is the topology authority, not historical dependency tables.
- `OWNER-AMENDMENTS.md` is the chronological prompt authority.
- The active BBNF/parse-that campaign is an execution predecessor and upstream
  coordination peer, never a runtime dependency. V-next appends coordination
  records and consumes only published 1.0.0; it owns no substrate change.
- DesignSync was not callable. Frontend-design and browser evidence informed
  formation but cannot be represented as DesignSync consultation.

The amendment ledger resets the formation clean-pass counter to zero.
