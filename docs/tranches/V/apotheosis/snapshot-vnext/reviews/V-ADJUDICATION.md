# V-Band Thrice Adjudication

## Cell

| Role | Sol | Premise | Result |
|---|---|---|---|
| Hostile A | Gödel `/root/v_skeptic_a` | V00–V31 and V24P are wrong; prove every restoration, edge and gate. | **REFUTE**; fifteen formation defects with runnable falsifiers. |
| Hostile B | Volta `/root/v_skeptic_b` | Independently reject standards, substrate, topology, oracle and performance assumptions. | **REFUTE**; live parser/path/timeline/complexity defects and a replacement DAG. |
| Adjudicator | Goodall `/root/v_adjudicator` | Reproduce both reports against the newest bounded parse-that ruling and current standards. | **NOT CLEAN**; accepted a 44-row V band and reset formation. |

No member edited source or documentation. Root applied only the proved
amendments below. The forbidden `r1-opus-refuted` corpus was not loaded.

## Proved finding families

| ID | Mechanism | Concrete evidence | Binding disposition |
|---|---|---|---|
| V-F01 | A scalar terminal stage cannot represent mixed operation truth. | A feature can parse/type/exact-serialize yet refuse resolve or delegate adapt. Existing prose also disagreed on whether serialize or refuse was a stage. | **Replace** with a total parse/type/evaluate/resolve/adapt/exact/edit/canonical vector; maturity and interop are separate axes. |
| V-F02 | Published parse-that adoption and novel substrate work were conflated. | Earlier P rows put BBNF/new execution ideas on the value path. | **Reject novelty.** Amended P01 is the sole full-CSS prototype on untouched 1.0.0; P02/P03 admit only necessity-proven minimal blockers; all novelty stays in the external bank. |
| V-F03 | Generated facts and adjudicated support were one impossible registry. | V02 prohibited semantics in generated data while V29 required support states in it; keys omitted extensions, aliases, types and relations. | **Split** immutable generated facts from reviewed support; accept only their total composite-key join. |
| V-F04 | Standards selections were not a reproducible closed universe. | URLs/dates omitted recursive normative closure, exact Webref/web-specs/WPT projections, browser builds and V8 environment. | **Add** `standards-lock.json` and `oracle-manifest.json` with exact artifacts, canonicalization and hashes. Unsupported is evidence, never skip-green. |
| V-F05 | The gamut-mapping law was stale. | [CSS Color 4 §14.2](https://drafts.csswg.org/css-color-4/#css-gamut-mapping) permits Local-MINDE, EdgeSeeker or Ray Trace and requires a selected algorithm consistently; the EdgeSeeker section still contains incomplete algorithm text. | **Amend V15.** Verify Local-MINDE control and Ray challenger; refuse EdgeSeeker evaluation while incomplete; select one production CSS policy. Cusp/Halley moves to V15P as non-CSS KEEP/PRUNE. |
| V-F06 | Hand-authored target edges reversed ownership or invented coupling. | Substitution→selector and image→transform had no consuming contract; prose simultaneously drew domain→OM edges and denied them. | **Replace** with producer→consumer constraints; delay physical transpose to V29T. |
| V-F07 | V00 moved topology before feature worth was decided. | It combined baseline, dependency deletion, export policy, cluster moves and test transpose; decomposition was pre-kept despite a seed PRUNE condition. | **Split** V00A/B/C and V29T. V24 is a terminal conditional re-adoption, not automatic retention. |
| V-F08 | Source fidelity could pass vacuously. | `serializeExact` could return a retained root string; preprocessing lacked CRLF/surrogate boundary bias, partition and edit-conflict laws. | **Strengthen V03** with `SourceText`, total boundary maps, interval partition, stable node edits, restored v4 capability diff and pathological limits. |
| V-F09 | CSSOM/Typed OM prose was not member-complete and could fake native identity. | No exact interface/member/exposure inventory; snapshots could imply liveness; Typed OM does not standardize the proposed path/motion native surface. | **Split** V10I/S/D; restrict V11 to inventoried members, opaque image snapshots and explicit DOMMatrix/SVG adapters. |
| V-F10 | Standards-required compatibility syntax conflicted with the global no-alias edict. | [CSS Images 4 Appendix A](https://drafts.csswg.org/css-images-4/#deprecated) requires `-webkit-image-set()` as a parse-time alias; [Selectors 4 Appendix B](https://drafts.csswg.org/selectors/#compat) requires web-compat parsing quirks. | **Narrow the edict** to project migration aliases. Add provenance-bearing `standards_compat_alias` rows with exact authored spelling and canonical lowering/serialization. |
| V-F11 | Domain inventories and dependencies were incomplete. | Missing `image(<color>)`, image-1D/stripes/light-dark routing, `display-p3-linear`, Color 5/6 maturity splits, explicit HDR/profile capability, and current trigger/timeline rows. | **Amend V12–V28** with exact routed owners and operation-vector evidence. |
| V-F12 | Path/motion parser defects and hidden context were live. | `M0 0 L10` leaked NaN; garbage and invalid arc flags were accepted; recursive subdivision permitted explosive work; motion could invent geometry. | **Split** bounded V24P path core from capability-gated V24M CSS motion; poison ambient DOM/layout/resource globals. |
| V-F13 | Complexity and allocation limits arrived after implementation. | Deep nested CSS produced `RangeError`; V16 preceded completed mix/ramp pipelines; allocation claims lacked DCE/deopt/GC/alias controls. | **Precommit limits in owners**, split V16A/B and V18R/H/V, and make V31 repeat rather than invent bounds. |
| V-F14 | Closure gates could pass by samples, skips or symbol names. | Same export names can conceal weaker semantics; all-unsupported browser rows could skip; workspace imports hide packed failures. | **Require** total semantic matrix, capability diff/tombstones, packed browser bundle and direct/peer/transitive consumer craters. |
| V-F15 | Two return schemas disagreed. | P–V YAML and the universal Markdown schema used incompatible statuses and evidence requirements. | **Unify** on `RETURN-CONTRACT.md`; band annexes may only add fields. |

## Rejected proposals

| Proposal | Decision | Proof |
|---|---|---|
| Add V02P as a second full-CSS prototype. | **Rejected.** | Amended P01 already owns the complete unchanged-1.0 prototype; duplication would create competing evidence and scope. |
| Split V11 and V23 into more waves. | **Rejected.** | Exact ordered subdeliverables and receipts fit one cohesive adapter/effect ownership boundary; more rows add sand without cutting a cycle. |
| Treat Local-MINDE as universally mandated or Ray as necessarily test-only. | **Rejected.** | The current ED permits either; conformance is consistency of the selected algorithm, not universal selection. |
| Implement EdgeSeeker from an editorial placeholder. | **Rejected now.** | Syntax/inventory remains covered; evaluate is explicitly refused until a complete pinned algorithm exists. |
| Keep decomposition because 609 lines/tests/consumers exist. | **Rejected.** | KEEP requires a unique CSS Transforms interpolation job and reference correctness; otherwise the seed PRUNE branch wins. |
| Browser majority as standards oracle. | **Rejected.** | Conforming implementations can differ, experimental rows may have zero support, and gamut used values are not exposed as a comparable string oracle. |
| Sample-only V30 or late-bound V31 thresholds. | **Rejected.** | Total claims require total joins; mutable late limits make every gate vacuous. |

## Accepted 44-row map

The prior 33 rows become 44 through these exact changes:

- V00 → V00A baseline, V00B dependency cut, V00C topology constraints;
- V10 → V10I WebIDL facts, V10S immutable snapshots, V10D live DOM adapter;
- V15 plus V15P non-CSS policy decision;
- V16 → V16A foundational kernels and V16B completed-pipeline bulk/SoA;
- V18 → V18R ramps, V18H OKHSL, V18V OKHSV;
- V24 remains the conditional inward transform decision and gains V24C CSS adapter;
- V24P becomes the bounded path core and gains V24M CSS motion;
- V29T performs the final accepted transpose.

All other IDs remain. Exact missions, dependencies, proofs and exclusions live
in [`../waves/P-V.md`](../waves/P-V.md).

## Acyclic dependency correction

The adjudicator's interim shorthand said V03 should consume P07, while P07
depends on V29. That would form `P07 → V03 → … → V29 → P07`. Root rejected the
literal edge as a graph-theoretic contradiction without changing the intended
evidence boundary:

```text
P01 prototype → P02/P03 optional minimal fix → P04/P05/P06 cratered receipt
P06 receipt → V03 production integration → … → V29
P04/P05/P06 + V29 + K09 → P07 final accept/reject → C10
```

P06 therefore supplies the exact substrate digest to V03; P07 remains the
post-semantics acceptance and C10 handoff. This is the only root correction to
the third-Sol dependency map and removes a provable SCC rather than selecting a
new mechanism.

## Required artifacts

- V00A: tagged/working/packed/API/consumer graphs with commands, versions and SHA-256;
- V01: `standards-lock.json`, `oracle-manifest.json`;
- V02/V29: generated facts, reviewed support, total feature matrix;
- V03: source contract, restored v4 corpus/capability diff and parser limits;
- every owner: immutable proof manifest, exact RED projection, fixed bounds and receipt hash;
- V29T: final source/import/export/test topology;
- V30: semantic capability diff, tombstones and packed direct/peer/transitive craters;
- V31: repeated precommitted limits and pinned V8/bundle/package evidence.

## Verdict

The previous V band is **not clean**. The 44-row replacement is the only
accepted formation branch. Its material amendment resets both whole-corpus
clean-pass counters; no V implementation may begin until the remaining scoped
cells and two later fresh corpus passes converge.
