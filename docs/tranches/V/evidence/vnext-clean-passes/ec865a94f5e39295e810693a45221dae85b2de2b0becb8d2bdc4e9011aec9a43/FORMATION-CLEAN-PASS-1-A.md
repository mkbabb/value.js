# Formation Clean Pass 1 — Critic A

- Epoch: `ec865a94f5e39295e810693a45221dae85b2de2b0becb8d2bdc4e9011aec9a43`
- Pass / role: `1 / critic_a`
- Task: `vnext_clean_p1_critic_a_d39079eca2ca836f24dad207e2626bc905759fe1ee43000e5b0b5cd518ca72f1`
- Prompt SHA-256: `d39079eca2ca836f24dad207e2626bc905759fe1ee43000e5b0b5cd518ca72f1`
- Requested/served route: `gpt-5.6-sol`, `ultra`.
- Independence: no sibling work used; prohibited quarantine never traversed; no mutation.
- Epoch before/after: exact frozen epoch, 163 files.

## Dispositions

1. `api-closure` — no defect. API and mutation controls joined 146 operations, 129 HTTP, 17 headless, and 378 paths.
2. `architecture-dags` — no defect. Five current graphs, 193 waves, 797 acyclic edges, sole root P00, full reachability.
3. `consumer-universe` — no defect. Bounds authenticate 15 roots, six typed subpaths and five package scopes.
4. `deletion-truth` — no formation-contract defect; schemas bind Git trees, effects, casualties, replacements, tombstones and residue classes.
5. `design-mobile-desktop` — no defect. Three-input epoch authenticated; 14 Value and seven Keyframes routes cover phone/desktop without claiming execution evidence.
6. `gate-soundness` — **F-01 survives**: the future return gate lacks normative information to authenticate state and gate receipts.
7. `parser-boundary` — no defect. Stale coordination is disclosed and owned by P00; C14 earns no P01/product credit.
8. `prompt-seed-bijection` — no defect. 149 seed rows and 114 union rows validate.
9. `quarantine-safety` — no defect. Basename and symlink refusal precede traversal.
10. `return-dependency-closure` — **F-01 survives**; graph closure cannot repair opaque top-level state/gate binding.
11. `state-routing` — no defect. Fourteen Value and seven Keyframes route records have bounded codec/transaction/recovery law.
12. `wave-formation` — no further defect. All 193 rows, gates, edge policy and proof layer validate.

## F-01 — return state and gate receipts have no reconstructible canonical binding

- Family: `gate-soundness / return-dependency-closure`.
- Mechanism: `RETURN-CONTRACT.md`, `return.schema.json`, and P00's deferred validator.
- Owner: `formation-root` defines representation; P00 implements it.

Evidence:

- `RETURN-CONTRACT.md:18-26` says `state_digest` covers repository identities and all acceptance artifacts, but gives no canonical preimage, ordering, inclusion rule, path resolution, byte projection or recomputation algorithm.
- `return.schema.json` constrains `state_digest` only as 64 lowercase hex; actor records repeat the opaque field.
- `RETURN-CONTRACT.md:40-45` requires each gate receipt hash, but gate records have no receipt path, byte length, schema identity or embedded bytes for a validator to locate and rehash.
- The shared wave law names a proof manifest and expected receipt hash, but defines no universal manifest schema or receipt coordinate/output projection.
- `RETURN-VALIDATOR-DEFERRED.json` and `RETURN-CONTRACT.md:122-130` require P00 to implement fail-closed validation against this schema, forcing P00 to invent normative semantics during execution.
- A malformed v3 object with unknown wave, empty dependencies, RED gate under COMPLETE, duplicated critics, missing reports, surviving finding, R5 with empty evidence, and zero state/self hashes produced `schema_errors: []`. Cross-field validation is deferred, but no validator can recompute bytes the record does not locate.

Consequence: a return can self-hash assertions while its state and gate receipt
remain detached from bytes. Dependency traversal proves self-consistent claims,
not identical reviewed state, executed gate and acceptance artifacts.

Required repair: define the canonical state-digest preimage; require each
artifact and gate receipt through a canonical path or embedded-byte coordinate
with schema, byte length and hash; define the proof-manifest/output join; add
P00 mutation canaries for substitution, omission, reorder and equal opaque
digests over distinct states.

## Commands, orphans and unsupported claims

The critic ran corpus epoch before/after, aggregate and focused API/DAG/target/
consumer/deletion/seed/union/clean/PT checks, direct route/reachability/budget
and malformed-return probes, and scoped Git status. No orphan beyond F-01.
Unsupported: “one immutable final state” and “P00 implements against this
schema” until the representation is reconstructible.

Verdict: NOT_CLEAN
