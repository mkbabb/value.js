# Tranche V — Formation Proportion Register

**State:** DECIDED at formation · **Execution truth:** W0 revalidates current coordinates; W18–W29 ship the dispositions; W32 closes the rendered record

This is the exhaustive mechanism-grouped register for route regions, Cards, headers, titles, readouts, intervals, padding, dividers, icons, buttons, ornaments, status, drag and focus species. A terminal verb is a product decision, not a claim that the implementation has landed. Stable semantic sites are used because W5 deliberately moves physical paths; W0 attaches current execution-base coordinates and tracked baseline frames, and W5's move manifest carries those same semantic IDs to their destination paths without changing a decision silently.

## 1. Stable surface inventory

The eleven first-class member routes are a closed inventory. `/about` and `/easing` are independent destinations, not a resurrected Picker companion or a nested Gradient miniature.

| Route | Member | Navigation position and product job | Feature owner |
|---|---|---|---|
| `/` | Picker | primary color instrument | W20/W21; shell W19 |
| `/palettes` | Library / My Palettes | owned palette inventory | W22/W23; shell W19 |
| `/browse` | Browse | public palette discovery | W22; shell W19 |
| `/extract` | Extract | image-to-palette workbench | W26; shell W19 |
| `/mix` | Mix | ordered color/palette convergence | W26; shell W19 |
| `/generate` | Generate | generative palette workbench | W25; shell W19 |
| `/gradient` | Gradient | gradient authoring instrument | W27; shell W19 |
| `/easing` | Easing | first-class neutral time/curve instrument adjacent to Gradient in navigation | W27; shell W19 |
| `/atmosphere` | Atmosphere | Aurora material preview/configuration | W28; shell W19 |
| `/blob` | Blob | Blob material preview/configuration | W29; shell W19 |
| `/about` | About | quiet trailing information destination, never the Picker's equal companion | W18; shell W19 |

The five operational routes are `/admin/users`, `/admin/names`, `/admin/audit`, `/admin/flagged`, and `/admin/tags`. Account is one Dock-opened Dialog over the current route. Unsupported/corrupt storage recovery is one inline `/palettes` state. Thus `R11` means the eleven rows above, `ADMIN5` means the five Admin routes, and `ALL18` means `R11 + ADMIN5 + Account + storage recovery`. `P122-8` means Picker, Generate, Extract, Mix, Gradient, Easing, Atmosphere, and Blob; `WORKSPACE-2` means Browse and Library.

## 2. Composition, route, and area families

| ID | Exhaustive stable sites and current witness | Decided relation and terminal result | Verb | Accountable primary; supporting owners / producer | Minimum π / DELTA |
|---|---|---|---|---|---|
| PR-04 | Current paired-view matrix: Picker↔About, Palettes↔Picker, Browse/Extract/Generate/Gradient/Admin5↔Palettes, Mix/Blob↔Picker; nested stage Cards across `P122-8` | Each of `ALL18` uses its binding composition; absent support is zero or a ≤15% invitation tray; page columns, empty halves and nested stage housing disappear | **REMOVE** | W18; W19/W22/W24–W29, BI P122 | Eighteen named frames; companion/nested-unearned Card area to 0, Card/main counts and collapse order |
| PR-09 | Gradient preview and embedded Easing selector are presently subordinate/tiny | `/gradient` and `/easing` are distinct P122 golden instruments; Easing curve is centered and container-clamped to 19–22rem | **ENLARGE** | W27; W19/W30, BI P122 | Direct frames for both routes at 1440/390; protagonist share and curve rect |
| PR-10 | Atmosphere and Blob currently give dense, equally weighted forms more acreage than preview | Both use exact preview-dominant `66.6666667% / 33.3333333%`; essentials precede scroll-confined advanced disclosure | **TIGHTEN** | W28; W29/W30, BI P122/P046/P047 | Both route frames; preview/inspector rects, disclosed height and scroll owner |
| PR-15 | Current route schema has no first-class About or Easing destination; removing the old companions would orphan them | Ship the closed eleven-route inventory in §1. About is a quiet trailing route; Easing is adjacent to Gradient. Every route has a Dock affordance, direct URL, title/H1 and focus/scroll settlement | **ADD-AFFORDANCE** | W19; W6/W18/W27/W30 | Direct-load and Dock journeys for all eleven; route/Dock count 9→11, `/about` and `/easing` reachability 0→1, main/H1 counts remain 1 |
| PR-16 | Browse/Library currently inherit an equal two-pane matrix; the earlier over-bound draft exceeded the exact preview upper bound | With a selected object, `WORKSPACE-2` uses protagonist `64%…66.6666667%` and complementary inspector `33.3333333%…36%`; without selection, protagonist is 100%; a secondary empty invitation is ≤15% | **ENLARGE / TIGHTEN** | W22; W23/W30 | Selected/unselected frames; complementary rect sums, protagonist bounds, empty share |
| PR-17 | Route swaps can expose a transient blank, duplicate scene landmark/H1, or abandoned focus target | One stable shell `<main>` and H1 own all sixteen routes; Account remains an overlay and recovery an inline state; one atomic active-subtree handoff | **TIGHTEN** | W19; W23/W30/W32 | Direct/Dock/Back/reversal filmstrip plus AT snapshots; main/H1/active-subtree counts always 1 |
| PR-18 | Global `PaneSegmentedControl`, left/right state and simultaneous mobile miniatures survive from the old matrix | Every `ALL18` mobile composition is one document-scrolling semantic sequence; global pane control/state/companion label disappear | **REMOVE** | W19; W22–W30, BI P092 | 390/320/actual-400%-zoom in-app Browser frames; global control/state count to 0, ordered rect/focus transcript |

## 3. Type, interval, material, Card, and boundary families

| ID | Exhaustive stable sites and current witness | Decided relation and terminal result | Verb | Accountable primary; supporting owners / producer | Minimum π / DELTA |
|---|---|---|---|---|---|
| PR-01 | Picker selected-space label→numeric headline; current Lab crop plus `.title-row` minimum and unconditional two-line/bottom-aligned reservation | P122 title-line-box→headline-line-box gap `G` only; `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px` at P019 floor/fluid/ceiling and complete-route 1440/390/320/actual-400%-zoom in-app Browser arms; ordinary tuple is one line and a true wrap grows below it; no Blob/Card/blank-line or persistent chassis reserve | **TIGHTEN** | W20; W0/W18/W21/W29, BI P122 | Tracked Lab before/after at every arm/frame; exact `G`, `I_before`/`I_after`, blank line 1→0, P122 reserve `none`, fixed chassis; actual zoom is not viewport emulation |
| PR-02 | Picker selected-space identity and numeric tuple currently compete at display scale | Fraunces identity is exactly one adjacent golden rung below the Fira Code headline using one P019 pair: `label/headline = 1/√φ` through floor/fluid/ceiling | **TIGHTEN** | W20; W17/W18/W31, BI P019 | P019 fixture plus Lab frames; resolved size/line-height ratio at all three clamp arms |
| PR-03 | Picker HeroBlob is corner-bound, timid in paint and over-authoritative in reservation | W20 first moves the locked footprint exactly one title-gap chassis-ward, preserves outer/chassis/seat geometry and readout clearance, and proves pre-paint centroid ≤5%/hull ≥12% on complete-route 1440/390/320/actual-400%-zoom Browser arms. W29 then alone grows the origin alpha component from measured `b₀` to `0.66 ±0.015` and closes the final buffer/native relation: centroid error ≤1.342% per axis, hull inset exactly 12.0%, stated DPR edge limits and equal WebGPU/WebGL2 area. | **ENLARGE / TIGHTEN** | W29; W0/W18/W20, BI P047 | Fixed-seed dual-engine/DPR sequence plus four routed layout arms; W20 displacement/collision/0px pre-paint placement, then W29 ratio/final centroid/hull/DPR/engine equality. |
| PR-05 | About's seven rules, automatic chassis dividers, caster shadows, corner marks and repeated row lines restate existing material/spacing boundaries | The binding `OPTICAL-BENCH-COMPOSITIONS.md §5` inventory applies: all eight P122 workbenches use boundaries `[]` and reserve `none`; only the five Admin fields retain one low-emphasis separator between adjacent rows and none after the final row; every other divider/caster/corner rule is removed | **REMOVE / KEEP** | W18; W19–W29, BI P122 | `ALL18` exact inventory; P122 boundary/reserve equality, five Admin adjacent-row jobs, all other divider/shadow/corner counts 0 |
| PR-12 | Dock/toolbar glyphs, swatch actions, tab faces, drag handles and compact row actions conflate visible size with target padding | Keep accessible seat/hit floors while stepping visible glyph and optical inset to the adjacent glass-ui rung; reservation exists only on collision axis | **TIGHTEN** | W18; W19–W27/W30 | Coarse-pointer/zoom hit probes; target polygon, glyph rect and optical-center delta |
| PR-14 | `/about` static definition Alert, default Card, seven dividers, empty Tooltip/cursor conversion paths and unbounded prose | One bounded structural article; lead paragraph, headings and interval express hierarchy; prose is `≤66ch`, with only the named `Conversion paths` ordered graph and fenced code specimens allowed to escape; ordered conversion data is readable without hover | **REMOVE / TIGHTEN** | W18; W19/W30 | Direct `/about` pair at all four arms; Card/Alert 1→0, divider 7→0, empty Tooltip/false trigger 1→0, prose measure and exception count |
| PR-19 | Global Dock/header at rest/stuck; current title scales inside an unchanged band and can clip edge/focus detail | One P109 signal contracts title by a semantic type step together with padding and band while the reserved scene origin remains fixed | **TIGHTEN** | W19; W17/W31, BI P109/P038/P041 | Rest→stuck filmstrip; title/padding/band rects, scene shift 0px, focus inset |
| PR-20 | `ALL18` headings, identity, labels, controls, values, code/provenance and dropdown options presently mix family/weight duties | Closed matrix: route H1/major argument=`text-display`+Fraunces; instrument identity=`--type-title`+Fraunces; palette identity=`--type-subheading`+Fraunces; section heading=`text-heading`+Plus Jakarta Sans; prose/help=`text-prose`+Plus Jakarta Sans; control/label=`text-small`+non-bold Plus Jakarta Sans; value/code/provenance=`text-mono-small` or existing `mono-caption`+Fira Code. P019's paired Picker scale is the sole exception | **TIGHTEN** | W18; W20–W29/W31, Glass P019/P010/P127 | Type board and `ALL18` text roles at 1440/390/320/actual-400%-zoom; computed role/family/weight/size/line-height, About `66ch` measure/exceptions and packed font-source evidence |
| PR-21 | `ALL18` structural plates are variably cream/brown, muddy dark, nested glass or seed-tinted chrome | One earned material tier per surface: ambient, structural glass, veil, specimen well or watercolor/data; seed tint remains outside structural chrome | **TIGHTEN** | W18; W19–W29/W32, BI P017/P063/P122 | Light/dark warm/cool/achromatic frames; tier, computed alpha/filter and rendered contrast |
| PR-22 | `ALL18` page columns, padding groups, inner stages, empty shelves and bounded entities all currently use Card-like housing | Browse/Library retain exactly one noninteractive Card container per rendered palette entity slip (`Card count = rendered entity count`), each with exact tuple `{size:"sm", material:"content", tier:"quiet", surface:"opaque", shadow:false, grain:false, specular:"off"}`. Its root owns no activation, focus or selection state and has palette-Card `[inert]` count `0`, keeping its live child reachable. Its sole specimen/identity selection seat is a native named `<button type="button" aria-pressed>`; palette `aria-selected`/option/listbox fiction is absent, and an optional reorder handle is a separate named control. Fields/lanes/empties/inspectors have zero Cards; each other composition has Card count `0`; grid/cartoon/Card-selected variants and nesting disappear | **REMOVE / KEEP** | W18; W22–W29 | Per-composition exact Card equation/data tuple; non-Browse/Library Card count 0, unearned/nested/variant count 0; root activation/focus/selection-state and palette-Card `[inert]` counts 0; native button/type/name, palette `aria-selected` count 0, pressed count `0|1`, pressed-ID↔selected-inspector-ID equality, no-selection inspector absence, Card/face selected delta 0 and seat selection/focus delta |
| PR-33 | `P122-8` stage+inspector composition and action-region intervals can drift through value-local padding, margins or responsive copies | Consume one producer tuple directly from computed style: `S122 = (--instrument-dial-padding-inline, --instrument-dial-padding-block, --instrument-control-padding-inline, --instrument-control-padding-block, --instrument-dial-gap, --instrument-control-gap)`. Dial padding is the combined stage+inspector composition's outer logical inset; dial gap is the stage↔inspector separation on the active layout axis; control padding is the action-region boundary→action-content inset; control gap is the separation between adjacent action children. The producer may resolve responsive values behind those same semantic properties; value defines, overrides, copies or substitutes none of them. Narrow topology changes order, not interval ownership. At 1440, 390, 320 and actual 400% in-app Browser zoom each mapped measurement equals its computed producer value within `±0.5 CSS px` | **TIGHTEN / KEEP** | W18; W20–W29/W32, BI P122 | Ordinary rect/computed-style strip for all eight workbenches at all four arms; six mapped equalities, consumer definition/override/copy count `0` |
| PR-34 | `WORKSPACE-2` retained palette-entity Cards and the surrounding fields/lanes/inspectors currently share or restate undifferentiated padding | Each retained Card uses exact tuple `{size:"sm", material:"content", tier:"quiet", surface:"opaque", shadow:false, grain:false, specular:"off"}` and invariant `C = --card-pad-inline = --spacing(4)` at every viewport; `block = 1.272C`, `section = block`, `footer = block/1.618`, `title = C/2.618`. Only the field/lane/inspector structural gutter responds: `--spacing(6)` at 1440, `--spacing(4)` at 390/320/actual-400%-zoom. Card/article root and face do not encode selection. One native named pressed-button seat spans specimen/identity and owns visible selection/focus; `aria-pressed` is true iff its palette ID equals selected inspector identity, at most one is true, and all are false when the inspector is absent. An optional reorder handle is separate. Outside the Card no `--card-pad-*` read/write/equation copy survives | **TIGHTEN / KEEP** | W22; W18/W23/W30/W32, glass-ui Card | Browse/Library data-tuple/rect/computed-style strip and pointer/Enter/Space/AT probe at all four arms; tuple and five Card rung equalities, structural-gutter equality, native button/name/type, palette `aria-selected`/option/listbox count 0, pressed count `0|1`, pressed-ID↔inspector-ID equality, no-selection inspector absence, Card/face selected delta 0, seat selection/focus delta and non-Card `--card-pad-*` use count `0` |
| PR-35 | About, `ADMIN5`, Account and storage recovery lack one decided outer/section/action rhythm; dense rows and quiet articles can therefore inherit accidental Card or route padding | The non-P122 matrix is exact: outer inline gutter is `--spacing(6)` at 1440 and `--spacing(4)` at 390/320/actual-400%-zoom; major section interval is `--space-phi-5` for About/recovery and `--spacing(6)` for `ADMIN5`/Account; Admin row block inset is `--spacing(4)` at 1440 and `--spacing(3)` at the three narrow/zoom arms; action gap is `--spacing(2)` for `ADMIN5`, `--spacing(4)` for Account/recovery and absent for About. About prose is `≤66ch` with only its two named wider example species. No collapsed margin, Card-padding variable or copied Card formula supplies these intervals | **TIGHTEN / KEEP** | W18; W22–W24/W30/W32 | Ordinary rect/computed-style strip for all eight non-P122 surfaces at all four arms; exact outer/section/row/action values, About measure/exceptions, collapsed-margin count `0`, Card-padding use count `0` |

## 4. Action, state, small-control, drag, and focus families

| ID | Exhaustive stable sites and current witness | Decided relation and terminal result | Verb | Accountable primary; supporting owners / producer | Minimum π / DELTA |
|---|---|---|---|---|---|
| PR-06 | Generate Regenerate/Save/Copy species; owner-state, Admin Names and Mix tabs with duplicated fills; adjacent palette action registers | One action/selection owner per semantic job: one Dock action family and one producer-owned selected fill; local pills/backgrounds disappear | **REMOVE** | W23; W17/W24–W26, BI P092 | Generate and three tab fixtures; action species 3→1, selected fills 2→1, focus remains separate |
| PR-07 | Dock seal/edit, selector, eyedropper, Spectrum, channels, palette, Generate, Mix and Gradient hover-only/unlabeled actions and drag state | Remove unnecessary hover paths; every surviving operation or grab seat has a persistent name, state and pointer/keyboard parity. ColorSpaceSelector specifically deletes `hide-indicator`, uses the sole producer `SelectItem` indicator/gutter and adds no local pill/halo/pseudo marker | **ADD-AFFORDANCE / REMOVE** | W23; W19–W22/W25–W27/W30, BI P051; W20 owns the Picker selector site | Exhaustive site manifest and representative journeys; unlabeled/hover-only paths 0; selector visible marker count 1; name/state/drag transcript |
| PR-08 | Palette command pending/failure/export, moderation result and storage-recovery truth can exist only in toast/console/transient copy | Durable entity/workspace/dialog status remains until acknowledged or terminally superseded; recovery names preserve/reset consequences | **ADD-AFFORDANCE** | W23; W15/W22/W24 | Reload/lost-response/error journeys; durable status identity, persistence and focus/announcement owner |
| PR-11 | `ADMIN5` rows obscure actor, authority scope, target evidence and irreversible effect | One compact review anatomy retains identity/scope/provenance/state and contextual confirmation; no decorative dashboard metrics | **ADD-AFFORDANCE** | W24; W10/W16/W30 | All five full-width routes; row rect/density, actor/scope/effect visibility and confirmation focus |
| PR-13 | Picker Blob specimen and action toolbar both expose Copy | Blob is inert presentation; one named action-region Copy remains | **REMOVE** | W20; W29/W30 | Picker pointer/keyboard/AT pair; specimen listener/Tooltip/label 1→0, total Copy 2→1 |
| PR-23 | Picker display headline, `contenteditable` and numeric controls currently overlap read/edit/announcement jobs | One labeled non-live readout; W21 semantic numeric fields are the sole editors with draft/commit/cancel/error; route H1 remains the only heading owner | **REMOVE / ADD-AFFORDANCE** | W20; W21/W30 | Readout→editor journey; CardTitle/H3/contenteditable/live role to 0, numeric editors present and equivalent |
| PR-24 | Exact live P051 census: Dock seal/edit, ColorSpaceSelector, eyedropper, Spectrum, channel, palette, Generate, Mix and Gradient faces | WatercolorDot is a noninteractive face only. Operable faces sit inside named geometric seats; ornamental faces are hidden; static data faces sit inside named list/text. Selector selection changes its seat and sole producer marker, never the face | **REMOVE / ADD-AFFORDANCE** | W18; W17/W19–W22/W25–W27/W30, BI P051 | Live recount 25→0 retired `tag`; face host/listener/register 0; seat/list semantics; selector seat/marker selected delta nonzero and face-pixel delta 0 |
| PR-25 | Owner-state, Admin Names and Mix Colors/Palettes selectors; old global pane selector | P092 survives only at the three earned sites; one fill matches active inner rect within 0.5px in every orientation/viewport/direction, focus separate | **REMOVE / TIGHTEN** | W23; W17/W19/W24/W26/W30, BI P092 | Three fixtures plus global absence; four-edge error, fill/focus count and override count 0 |
| PR-26 | Browse/Library PaletteCard root currently combines click, drag, expansion, rename, menu, transient result and hover swatch actions | Card/article root becomes a noninteractive container: it owns no activation, focus or selection state, and palette-Card `[inert]` count is `0` so its live child remains reachable. One native named `<button type="button" aria-pressed>` selection seat spans specimen/identity; no palette `aria-selected`, option/listbox or composite-selection semantics survive. Its pressed palette ID equals selected inspector identity, at most one is true, and clearing selection makes every seat false and removes the inspector. An optional sibling grab handle remains a separate named control; detail/verbs/durable status move to selected inspector; compact swatches remain data | **REMOVE / ADD-AFFORDANCE** | W22; W23/W30 | Selected/unselected Card and inspector under pointer, Enter, Space and AT; root activation/focus/selection-state and palette-Card `[inert]` counts 0, native button/name/type, pressed count `0|1`, pressed-ID↔inspector-ID equality, no-selection inspector absence, palette `aria-selected`/composite count 0, five body verb paths to 0 and separate handle coordinates |
| PR-27 | Browse/Library/Extract/Mix/Generate and `ADMIN5` empty/loading/error states; repeated large watercolor trio and shadow content | Primary empty invitation content-hugs; secondary empty is ≤15% or absent; one static three-face mark is aria-hidden; skeleton exists only during a real request | **TIGHTEN / REMOVE** | W22; W23–W26/W30 | Empty/loading/error/populated frames; area share, request timestamps, mark count and false skeleton/shadow count 0 |
| PR-28 | Account and corrupt-storage flows lack proportionate persistent recovery/action placement | Account is one side Dialog with identity→proof/secret→durable result→actions; storage recovery is inline diagnosis→preserve/export→separately confirmed reset | **ADD-AFFORDANCE / REMOVE** | W23; W15/W22/W30 | Account and recovery desktop/mobile journeys; overlay/main/Card counts, region order, focus return and preserved-data status |
| PR-29 | Picker channels/Spectrum plus Generate count, Extract sampler, Mix quantities, Gradient stops, Easing points, Atmosphere and Blob controls use divergent tracks, units and target rhythm | One W21 axis composition owns label/unit/reserved value/numeric entry/focus/target; spectral color appears only where the axis encodes color | **TIGHTEN / ADD-AFFORDANCE** | W21; W25–W30, BI P017/P063/P093 | One fixture per axis kind; track/data relation, target/label/value rects and pointer↔keyboard↔numeric equality |
| PR-30 | `ALL18` selected/focused/failed/pending/disabled states can be color-only, clipped or conflated | Focus is distinct from selection in both schemes, forced colors and reduced transparency; state/name/error/status are explicit and targets remain reachable. ColorSpaceSelector option and `aria-selected` agree; its seat/marker selected delta and independent focus delta are each nonzero in monochrome/forced colors while face delta is 0 | **ADD-AFFORDANCE / TIGHTEN** | W18; W19–W30 | Keyboard/AT/forced-color sample per shared mechanism; contrast, focus/selection pixel separation, selector option/ARIA/marker equality and state transcript |
| PR-31 | `ALL18` corner glyphs, decorative controls, repeated dashes/dots, caster details and unexplained micro-marks | A small mark survives only as data, status, named action, drag seat, focus/selection register or the single declared empty ornament; every other mark is removed | **REMOVE / KEEP** | W18; W19–W29 | Element-economy census; each retained mark maps to one job and all unexplained marks count 0 |
| PR-32 | Route slab swap, Dock morph, header contraction, Watercolor hover, Gradient/Easing and Aurora/Blob ambient motion compete or run without state truth | One owner per motion; reduced motion resolves terminal geometry; continuous ambient work stops within five seconds or exposes remembered still/pause; no gray/remount hole | **TIGHTEN / ADD-AFFORDANCE** | W19; W20–W29/W31/W32, BI P031/P036/P043/P045/P054 | First/middle/terminal/reversal sequences; owner, timing, chroma, pause/offscreen work and scene-shift DELTAs |

## 5. Coverage and execution rule

The register is exhaustive by semantic species: route regions/housing are PR-04/09/10/15–18/22/28; headers/titles/readouts/intervals are PR-01/02/03/19/20/23/33–35; spacing/padding/dividers are PR-01/05/12/16/19/22/33–35; icons/buttons/ornaments are PR-06/07/13/24–27/31; status/drag/focus are PR-07/08/11/23–30/32. An instance discovered at W0 joins its existing family and site list. A genuinely new mechanism receives a new terminal row with one W18–W29 primary before W18 starts; it is never filed as `CONSIDER`, `POLISH`, `LATER`, or generic `AUDIT`.

W0 replaces each current-witness description with tracked baseline and execution-base source coordinates, confirms the closed route inventory, and records any appended instance or family explicitly. W5's physical-move manifest preserves the stable site IDs through relocation. Feature waves turn the decided verbs into shipped coordinates. W32 samples every family; a product fix reopens its accountable primary and restarts the affected visual pass.
