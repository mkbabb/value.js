# V Re-formation Campaign — 2026-07-16 (owner-ordered)

**Charge (owner, verbatim core):** "V's been hardened and developed — but to what end. Ruthlessly
critique it." 32-agent audit of the plan, the waves, and everything landed ad hoc; fold the new
grand edicts; deliver the next tranche fully formed. Planning only — NO source edits land from
this campaign.

**New grand edicts to fold (from the owner's 2026-07-16 prompt):**

- E1 — Colocation/modularization finally settled, library AND demo: components colocated with
  sub-components/composables/skeletons/constants, recursively; only true module/global-level
  composables live in a `composables/` dir; same for styles. Backend gets the abstracted
  equivalent. Long-running dirs broken into common encapsulated modules.
- E2 — Structure idiom per keyframes.js critique + glass-ui flattening: no redundant file
  prefixes (`easing-option` → `option`), no orphan single-type files beside god modules, no god
  modules at all; ruthless per-file inspection for cohesion.
- E3 — KISS/parsimony as product law: fewer lines, fewer concepts; majority of effort on direct
  implementation and visual verification, not gates/process.
- E4 — Ruthless critique of all gates/proof/meta scripts and most tests; prune the superfluity.
- E5 — ALL design through Fable + the frontend design plugin (DesignSync). Aristotelian
  proportionality audit: cards, components, hierarchy, margins, paddings, dividers, small UI
  elements; mark superfluous/duplicative UI for removal AND under-afforded spots for addition.
- E6 — Glass-ui root-level defects: note, batch, relay to the active glass-ui agent (mid BI/P/Q)
  without interrupting; precepts-compliant wave addenda, exact defects, mindful of their context.
- E7 — Bi-directional communication with glass-ui, sci-report, atlas (all in ACTIVE execution).
- E8 — keyframes.js runs a parallel tranche development/audit process; coordinate precisely.
- E9 — Chronic/deferred discipline: every chronically deferred item becomes a DECIDED row
  (build/fold/retire with rationale); re-booking forbidden; a 2+-close-riding chronic is a
  disease row whose decision is a wave of its own.
- E10 — Prompt-recap totality: every ask hitherto addressed or carrying an explicit ledger row
  with an owner. Silent drops forbidden.
- E11 — Live-Glass audit linkage (owner, 2026-07-16, mid-campaign): pull the most recent
  glass-ui via a TEMPORARY file linkage and audit the app's current configuration against the
  actively changing library. Mechanism: zero-footprint `git archive` of glass HEAD into the
  session scratchpad → sandbox build/pack → static consumption-surface diff, then a reversible
  boot probe. EPHEMERAL: never a committed lock, never W17b/W33 release evidence, glass primary
  tree untouched.

**Standing constraints honored:** dirty worktrees preserved (no reset/mass-stage/cleanup);
Value4/Keyframes6 immutable; Glass W17a owner not collided with; V-A row law (one mechanism-level
addendum per material defect, folded into existing families).

**Agent budget:** 32, dispatched in batches of ≤3 concurrent. Fable owns orchestration,
synthesis, adjudication, all design. Opus/Sonnet take fanout; every spawn declares its model.

## Round log

| Round | Agents | Lenses | State |
|---|---|---|---|
| 1 | 9 | dirty-tree provenance · formation critique · gates/tests superfluity · src structure · demo colocation · keyframes census · prompt-recap gaps · chronic disease · cross-repo comms | COMPLETE — RF-1..RF-22, RF-25/26 |
| 2 | 3+ | api/ backend (charge 1+2) · fresh no-narrative repo adversary · frontend wave-spec soundness W19–W29 | api COMPLETE (RF-23/24); 2 in flight |

## Finding-family registry (this campaign)

Families extend, never duplicate, the mechanism families in `AGENT-REGISTRY.md`. Rows land here
as rounds return; each family gets a terminal owner in the re-formed tranche.

| # | Family | Evidence | Terminal owner |
|---|---|---|---|
| RF-1 | Unowned feature deletions in the dirty tree — SpectrumCanvas gamut-truth overlay (8 rows, −983), gradient perceived-space plate (4 rows, −1034, **conflicts W27 which plans to BUILD on it**), ink-walk/normalizedColorNames (−176). All forced by the v4 API cut (deleted `/color` internals), none decided by a current wave. | dirty-tree census families M/N/O; `W27.md:7,11`; `W21.md:7–25` | re-formed W21/W27 amendments + a DECISIONS row: rebuild-on-v4 vs terminally retire, per feature |
| RF-2 | DAG-order inversion in the dirty tree — demo consumer migration (~49 rows) landed ahead of its W17b gate; 4 demo-consumer tests staged into the producer commit V-A107 forbids (`ink`, `preview-chips`, `value-domain-clamp`, `view-accents`) | census families L + B-partial; `CONSUMER-CUT.md:5`; V-A107 | commit-sequencing law (L5, as amended round-3): the 4 tests unstage from the producer slice and land in W40-S4; the consumer bulk commits as W40-S4 per V-A73 (C1 completion, landing late) — the earlier hold-for-W17b reading created the W43 restructure collision (round-3 D3) and is superseded |
| RF-3 | CI coverage cliff — staged lean ci.yml (720→48) silently dropped demo typecheck, ALL e2e (83 files/11,708 L), api tests, boot-smoke from any gating. Pruning theater ≠ dropping product coverage. | gates census tables A–C; staged `.github/` diff | owner decision row + re-formed W16/W30/W33 must name what re-gates where |
| RF-4 | Proof-theater relocated into the unit suite — 8 meta tests (~842 L: G-CANON/G-SEC/G-DEMO/G-ORACLE canon-sync, gitignore, shot-policy, apparatus-grep) + fully dead `oracle-slate-teeth.mjs` (187 L) + CI-orphaned `boot-smoke`/`css-emission-probe` | gates census table D | W4 amendment: extend subtraction to the relocated theater; explicitly decide the two orphaned probes |
| RF-5 | W4 execution divergence — superseded design docs modified-not-deleted (3 rows); ALL 11 benches deleted where SUBTRACTION.md said retain-the-proven-useful-set; `.release-working/` clone + operational residue uncommittable | census families C/R/T | W4 execution note in re-formed spec |
| RF-6 | CLAUDE.md provenance CLOSED — root + src/parsing deletions are sanctioned user acts per SUBTRACTION.md "Preserve"; keep deleted, redistribute-half owed. **Owner-ruled 2026-07-17 (post-pass-6): TOTALITY — `src/subpaths/CLAUDE.md` (staged M) and `demo/CLAUDE.md` (unstaged M) delete too, modifications discarded. Pass-7 census correction: the tracked set is EIGHT, not six — `src/transform/CLAUDE.md` + `api/CLAUDE.md` (both clean, missed by census family J) die at W40-S1 under the same ruling; zero CLAUDE.md in-repo (`git ls-files '*CLAUDE.md'` → empty)** | census family J | W40-S1/S4 (deletions) + W41 (fold) |
| RF-7 | Load-bearing gate identified — `verify-packed-surface.mjs` (+ `fixtures/public-types/value-v4.ts`, V-A106) is the ONE gate with real teeth; keep against all pruning | gates census | protected row in re-formed W4/W33 |
| RF-8 | Canon disproportion — 1.46MB living canon, only ~30–35% product-proportionate; 8 status/ledger docs ~73% process; audit/AGENT-REGISTRY.md (56KB) is the largest PURE-PROCESS file (PALETTE-DOMAIN 167KB/ADDENDA 141KB are larger overall); ~306 supersession tokens; V-A152 missing from the addendum sequence | formation critique §1/§5 | CANON COMPRESSION wave in the re-formed tranche: small living core, sediment archived |
| RF-9 | Palette stack over-armored — ~70% of ~350KB has no consumer at this product's scale (AES-GCM replay ring, AEAD cursors, mTLS/PROXY-v2 edge, 20 admin routes, AuthorityGuard, RankEpoch, encrypted legacy cut, hand-rolled PNG DEFLATE); honest core ≈ a dozen routes + cookie login + OKLCH validation + JSON/CSS export | formation critique §4, file:line cites therein | PALETTE RIGHT-SIZING decision: honest core becomes W10–W16 replacement scope; armor RETIRED-AS-SPECULATIVE or BANKED outside V (owner-ratify bracket) |
| RF-10 | Wave transitive load — 9/10 sampled waves REQUIRES-CANON (only W5 standalone); W17's ~2KB live task buried in ~30KB historical prose; W33 same shape. Gates themselves are mostly falsifiable — the defect is load + historical pollution, not gate-farming | formation critique §2 | wave-spec rewrite law: every re-formed wave executable from its file + ≤2 named refs |
| RF-11 | Live canon contradictions — D27 still on the retired K_NEXT rail; D2 future-tense publication vs its own immutability header; test-count drift 1,127→1,131→1,132; circulating byte counts from TWO artifact families untagged (Keyframes archive lineage 184,710/708/684/254 + SHIPPED registry 184,430 V-A117; value bootstrap 36,447/38,126 V-A85/A81 — clean-pass-1 D1 caught the single-family conflation, and D2/D41's "184,254 sole input" is itself the drift: the registry authority is 184,430); producer hashes duplicated in 6 docs; V.md self-dated 07-15 while embedding 07-16 overrides | formation critique §3; clean-pass-1 D1 | single-source-of-truth law: ONE current-authority figure per artifact family (W40-S5); lineage lives in archived ADDENDA only |
| RF-12 | The convergence engine never closes — 2/2 reached twice, self-invalidated both times, ≥4 clock resets; formation-as-product | formation critique §5; AGENT-REGISTRY:39 | KILL the clock as a closing condition in the re-formed tranche; replace with the owner's own §5 evidence law (build + rendered verification) |
| RF-13 | Live-Glass (E11) consumer-surface drift — demo imports 3 specifiers dead in packed Glass 7 (`/controls`×2, `/confirm-dialog`×2, `/toggle-chip`×1; the latter two died at glass **5.0.0** per the install-truth packet `b43b9f91` — the earlier "removed AFTER the 11-subpath diff" reading was relay drift, corrected 2026-07-17; W44 carries the corrected provenance); successors exist (`dialog`, `chip`, `toggle-group`); 5 sites: EasingSpecimenStrip, PalettesPane, AdminUsersPanel, MobileMenuDropdown, ProfileSection. Plus 40 root-entry imports still to verify at symbol level. Artifact: glass7 HEAD `e7da7b5c`, tgz 954,335 B, sha256 `99d0383f…`, 74/68 surface, peers = immutable pair | inline E11 diff; scratchpad glass7-audit/ | W17b consumer-refresh spec gains the exact 5-site migration table; symbol-level diff owed before W17b executes |
| RF-15 | Demo colocation map — shared buckets fully dissolve: 38 single-consumer files colocate into features, 23 true-shared survive (color-session ~9, palettes ~5, platform ~5, shell ~3, shared ~1), 15/16 panes → features/scenes, `panes/` + `@/lib/palette/api` barrels die, `gamut-ink.ts` DEAD (0 importers), `style.css` 1043L splits to owners + foundation.css. Component tree already recursively colocated (picker is the exemplar). ADJUDICATED (Fable): `usePaletteManager` DISSOLVES into narrow ports (no-god-modules + ARCHITECTURE §1 beat bare colocation; rides RF-9 right-sizing); `ConfigSliderPane` PROMOTES to scenes-shared. Cross-feature violation to fix: ConsoleRail → palette api barrel | demo colocation audit; ARCHITECTURE §1 L39/58/60 | re-formed W5/W6 spec carries the full move table |
| RF-16 | Cross-repo comms map — exact outbound landing paths: glass `…/BI/coordination/valuejs-inbox-YYYY-MM-DD-<topic>.md` (INBOUND-MARKS auto-obligation); keyframes `…/tranches/V/VALUEJS-<TOPIC>-YYYY-MM-DD.md` (flat letter; V is their live forming tranche — the original U reading was superseded by RF-19's discovery, ratified clean-pass-4); atlas `sci-report/atlas/docs/tranches/P/coordination/YYYY-MM-DD-<topic>.md` (monorepo, NOT standalone repo); sci-report routes only via atlas. 9 open obligations tabled. Discrepancy: standalone atlas repo pins value ^3.1.0 vs monorepo P-tranche claims value-4 crossing landed | comms-map audit; CONSTELLATION.md §1–4 | coordination waves in re-formed tranche; outbound packets authored at formation close |
| RF-17 | Next-4.x library scope inputs from siblings — KF covenant (§B parseTimingFunction on /easing · §C unflattenObject decision · §D diagnostics-bearing parse + layout-tracking taxonomy · §E CSSPropertyDescriptor rename KF-7 · §I D-GAP-1/5/6) + atlas advisory (zero-alloc mixColorsInto/toRgba8-into, scrub-clamp law, validated-literal CubicBezier). All target the NEXT value cut, not immutable 4.0.0 | KF-TO-VALUEJS-U.md §B–§I; ATLAS-INBOUND-2026-07-16 §1 | a named library-evolution wave in the re-formed tranche decides ship/decline per covenant row |
| RF-18 | keyframes.js dirty tree DECODED — local master one commit behind origin; ~185 dirty files are byte-identical to immutable 6.0.0 (`5a9183a7`), i.e. the un-fast-forwarded release itself; resolution = ff-only advance, NOT a cleanup; V-A62's "unbuilt 6.0 label" RESOLVED. Genuine new work: 65-file Glass-7 demo-consumer slice + forming V docs | keyframes census Part A; kf R1-03 reconciliation | coordination letter row: recommend ff-only + census split to the kf V process |
| RF-19 | keyframes.js V-formation is LIVE — their docs/tranches/V/ holds the owner's verbatim prompt + audit lanes R1-01..05 (same 32-agent charge, same structure examples). Their structure map is drafted (11 prefix-disease instances, compile/frame consolidation, easing scattered across 4 dirs, zero god modules, clean barrels). Do NOT duplicate: exchange formation artifacts; reciprocal letters `VALUEJS-*` ↔ `KF-TO-VALUEJS-V.md` in their V dir | keyframes census Parts B/C | coordination wave: bilateral formation-exchange letter |
| RF-20 | Prompt-recap gaps — PROMPT-RECAP's method boundary (≤07-14) excludes the 07-16 REFORMATION prompt entirely; 3 asks UNOWNED (backend colocation, library file-idiom at keyframes-critique grain, dirty-tree commit sequencing) + DesignSync tooling unowned; over-claims RC-1→W10 (backend colocation phantom) and K-22→W16; 7 vague rows | prompt-recap audit Parts 1–2 | re-formed PROMPT-RECAP §7 (E1–E11 rows with owners); over-claims corrected |
| RF-21 | **THE DISEASE ROW** — colocation/modularization chronic rode A→D→K→N→T→U→V; re-issued verbatim 3× (T MANDATE E-1, DESIGN-CAMPAIGN 07-13, REFORMATION 07-16); marked LANDED at T.W1/`616e84f` yet resurfaced; V splits it dir-grain (W5/W6) vs component-grain (claimed done-by-observation). Sibling chronic: no-god-modules/KISS never executed with a down-metric (pass-4 measured LoC +158). Per E9: its decision IS a wave of its own | prompt-recap audit Part 3 (full trail with cites) | THE STRUCTURE-SETTLEMENT WAVE: one dedicated terminal wave uniting recursive colocation (RF-15) + library idiom (RF-14) + backend equivalent + long-dir dissolution, with a measured down-metric and terminal evidence — the chronic dies here |
| RF-22 | Design-canon adjudication (Fable, direct read) — PROPORTION-AUDIT/VISUAL-CONSTITUTION/OPTICAL-BENCH survive as the design authority: §1 IS the Aristotelian ruling; terminal-verb register (REMOVE/ADD-AFFORDANCE, no CONSIDER/LATER) already marks the superfluity (PR-13/14/16) and under-affordance (PR-07/08/11/15/16); §5 micro-UI laws are KISS-law-grade. E5 residue: (i) DesignSync pathway needs a DECISIONS row; (ii) live-register revalidation honestly blocked until the W17b routed mount; (iii) no NEW glass root-defect batch manufactured — existing producer asks already relayed (comms map obligation #1) | my PROPORTION-AUDIT.md read; recap audit #5 | DECISIONS row for DesignSync; register stays live through W18/W32 |
| RF-23 | api/ structure verdict — near-exemplar (0 route→repo edges, 0 god modules, pure barrels, healthy DI/errors/auth/validation); erosion: admin's 13 cross-capsule imports incl. 2 runtime leaks (`paginationQuery`→hoist to platform/http, session-token primitives→via session service); prefix renames = the 3 verified basename-repeats-parent hits (route-table EXEMPT — W45 deletes it; entity-echo repos stay). **CORRECTED BY ENUMERATION (W43 drafting):** `lru.ts` has 3 legal downward consumers across two capsules (resolve-session:25, idempotency:50, rate-limit:18) — it is a genuine platform/cache primitive and STAYS; the single-consumer colocation claim is retired | api audit CHARGE 1; W43.md §(c)3 census | backend rows inside THE STRUCTURE-SETTLEMENT WAVE (RF-21) |
| RF-24 | Palette right-sizing evidence COMPLETE — 7/7 RED families verified file:line (restore mount, versions/hash/revert, regex search fallback, caller-controlled contributor, unfiltered provenance, Mongo-only health, hand-kept route table with PROVEN drift: `/restore` on wire, absent from table); honest fix ≈ 9–11 files / 7–10 of 53 routes (~15–20%) vs plan's 100% teardown (30 RETIRE/16 REPLACE/6 BUILD); plan re-derives existing conformant substrate. Immutable-release CONCEPT survives right-sized (replaces legacy versions/hash); Workspace/Policy/Principal-bitmask/CAS/RankEpoch ontology dies | api audit CHARGE 2 | replaces W10–W16 with ONE surgical palette-hardening wave + a DECISIONS bracket (owner ratifies the armor retirement) |
| RF-14 | src/ structure verdict — live tree already clean of prefix disease with pure barrels; defects: `v4/` version-prefix dir (violates decided ARCHITECTURE §2 bare naming), redundant subpaths↔v4 double-barrel, 2 CSS god modules (stylesheet.ts 940 → +timeline.ts; grammar.ts 658 → +syntax.ts, both ARCHITECTURE-mandated seams), foundation/ unmaterialized, 8 empty git-rm'd dirs; DIVERGE: decline premature value.ts/model splits (anti-fragmentation) | src audit; ARCHITECTURE.md §2 L64–81 | library-structure rows inside THE STRUCTURE-SETTLEMENT WAVE (post-C1-commit, behind the immutable 7-entry surface) |
| RF-25 | Ledger integrity POSITIVE — 151/151 rows decision-terminal; 17/17 spot-checked folds real; zero re-deadlines inside V; zero partial-as-done lies (V-A74/A86/A93 caught green-over-broken; V-A132 keeps missing artifacts born-RED; relay-sent-as-done forbidden 3×); BANK clean (7 rows, named triggers); T-era watches correctly KILLED not re-booked. V's anti-partial machinery survives into the re-formation intact | disposition audit §§1,3,4,5 | carried forward as the re-formed tranche's evidence law |
| RF-26 | **THE DISEASE REGISTER + EXECUTION-GATE LAW** — 8 riders decided-but-never-executed across 2+ closes: D-1 aurora-derive (Tranche D→K→N→T→U→V), D-2 blob-extirpation (D→…→V), CH-3 colocation (=RF-21), CH-4 Q14 ~5s LCP, CH-5 glass adoption, CH-6 scene transitions ("still awful after prior close"), CH-7 real-GPU oracle never run, CH-8 palette CRUD truth. Each already HAS its dedicated wave — the re-formation adds NO new DECIDE rows; it adds the EXECUTION GATE: riders may not be re-chartered/re-amended; their next legitimate state is a RUNNING wave with product-green Browser evidence; any further "BUILD Wxx" is the forbidden re-booking. ~22/151 rows execution-terminal; 3/34 waves shipped | disposition audit §2 + bottom line | standing law in the re-formed V.md; per-rider gate rows in owning waves |
| RF-28 | Frontend wave-lens verdict — W29's completion gate UNSATISFIABLE (phantom `pi/w17/p047-producer-baseline/manifest.json` never captured; `.325` landed first; heal = captured `.26` before-frame reconstruction); shared chrome law (P051 ×14, P122 ×15) duplicated across 6 waves owned by none → F0 sole owner + override-count-0 gates; Picker = 3 files for 1 atomic close (W20+W21+W29-paint share one manifest) → F2 merge; W23 = 3 products stapled → F5 export split (pure, pre-mount-buildable); pre-mount-buildable set (serializers, wire clients, SeedToken parser, fixtures) paper-blocked by V-A166 over-reach → L8 unblock; born-REDs verified on disk (A137 `role="img"`, A138 no tabpanel, groundRecordInject alive, `.325` at HeroBlob.vue:162); W27 stranded clause confirmed; no cycles in W19–W29. ADOPTED: F0–F8 replaces W18–W29 (12 files → 9 single-closeable units) | frontend wave-lens audit (per-wave table + consolidation) | V-PRIME §2 frontend arc = F0–F8 as W46–W54 |
| RF-29 | Fresh-adversary findings — **B1 BLOCKER:** `demo/@/lib/picker-color.ts` UNTRACKED yet imported by 20+ tracked files (tree not git-reconstructible); B4: 4 untracked tests counted in the green 345; B7: relocated aurora tests outside vitest include (2 tests silently dropped); B2: glass-ui/keyframes undeclared in package.json, resolution via 4.8GB gitignored symlink island; B3: zero demo CI (why B1/B2 never tripped); B5/B6: 11GB stale worktrees (16 wf_*) + un-ignored `.release-working/` in lint scope; B9: coverage 1607→345 needs owner sign-off; B10: 17 `as unknown as` in v4 color math. CLEANS: no secrets; dist self-contained (0 bare imports); e2e NOT source-broken by the v4 cut; all glass5 specifiers resolve; no dangling v4 imports; lib+demo typecheck and 345 tests GREEN as-sits | fresh adversary (commands + file:line in report) | B1/B4/B7 → W40 sequencing slices; B5/B6 → W42; B2 → W44; B3/B9 → bracket B3 (implementing waves W42/W45/W55 + drop sign-off); B10 → DISPOSED: immutable 4.0.0, won't-fix recorded; hygiene note carried to WL (no public-surface change) |
| RF-27 | sci-report relay (owner, 2026-07-16) sharpens the atlas/SCI spec inputs — crossing was clean (one round, four commits, zero shims, CHANGELOG sufficed); three ranked confirmed findings: (1) `mixColorsInto` did not survive to 4.0.0 — zero-alloc mix path gone while SCI's densest canvas tier mixes ~3,243 marks/frame → restore an into-variant OR bless a hot-path idiom; (2) `mixColors` errors on t∉[0,1] → deliberately rule clamp-option vs consumer-clamp-law (they clamped consumer-side); (3) fallible `CubicBezier` makes compile-time-constant curves pay Result ceremony at module init → validated-literal path OR bless unwrap-at-init. ESM-only/no-require + /easing-vs-/math placement = census only, no asks | owner relay in-session; ATLAS-INBOUND-2026-07-16 §1 | three DECIDE rows in the library-evolution wave (with RF-17's KF covenant rows) |
