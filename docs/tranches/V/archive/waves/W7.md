> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W7 — Sole Package Surfaces

**State:** VALUE4 PRODUCER SHIPPED · HISTORICAL / NO LIVE WAVE DUTY · downstream adoption remains W17/W33-owned · **Meta-tranche:** V.H Hardening · **Depends on:** W2, W4, W8, W9

**V-A130 current-stage override:** W7's exact-seven transport and producer cut are historical evidence superseded by immutable Value4. W7 has no live Value publication, bootstrap or U-F77/Consumer-Cut producer-order duty; downstream adoption, Glass7 release and routed mount remain with W17/W33.

## Historical goal (implemented)

Make every public capability have one authored entry, close value.js's direct demo imports/typecheck, and emit the sole exact-seven Value 4 bootstrap transport needed for the constellation cut.

## Scope and file bounds

`package.json`, Vite library entries, `src/index.ts`, `src/subpaths/**`, package/self-resolution tests, current docs, every active `demo/**` value.js consumer and `tsconfig.demo.json`. Sibling-repository source migration—not value's own demo—is W17.

## Triumvirate and worktree

- **Specification:** `ARCHITECTURE.md §3–4`.
- **Plan:** author `/color`, `/value`, `/css`, `/easing`, `/math`, `/transform`, `/quantize`; execute W2's exact old-symbol→new-symbol and seven-entry export-name/kind manifests; export W8's `Result`/`AnyColor`, all seventeen final factories/operations and sole `RGBA8`/`toRgba8` from `/color`; export W9's complete 19-runtime/33-type `/css` grammar block—including the sole `parseKeyframeSelector`, standard `from`/`to` typed-percent semantics, value/color/stylesheet/custom-function/animation/timeline parsing, canonical `calc`/conditional/sibling-index/`@function` arithmetic, the keyed declaration reducer, context-preserving located rule collectors, `CSSPropertyDescriptor`, syntax coercion and typed timeline inverse—and export the exact `/value`, `/math`, `/transform`, `/quantize` plus 16-runtime/five-type easing blocks from `ARCHITECTURE.md`, with no unlisted runtime/type name. The easing block retains canonical `easeOutExpo`, `smoothStep3` and `easeInBounce`, while `timingFunctionDescriptions` moves out to its two UI consumers and is deleted from Value. Glass consumes only `/css`'s color/timing slice; that slice does not truncate the package declaration. No general library-string serializer or `timingFunctions` aggregate exists. Retire the consumer-less `/browser` proposal and hand its one live resolution/cache mechanism to W17's Keyframes owner; remove the unused whole-element `cubicBezierToSVG` rather than replacing it with an upstream presentation API; localize generic helpers; delete facade mirrors and parity machinery. Replace `test -f dist/value.js || npm run build` with unconditional output removal plus the authoritative production build for the sole W7 bootstrap pack, and create that pack only after W9's focused grammar suite and the full ordinary aggregate are green.
- **Implementation owner:** public-surface lead in the coordinated value-side C1 worktree; executable order is W8→W9→W7. Its boundary includes every active value-internal demo import/name/kind migration and preserves the separately landed W20/W29 files. Once the exact package, direct demo imports and demo typecheck, clean-consumer checks and one `BOOTSTRAP ONLY` pack are green together, C1 merges to the V execution branch so W11–W15 can consume it. The full demo build and nonempty routed Browser mount against migrated sibling bytes belong to W17; registry publication remains W33.

## Born RED / current remainder

The born RED was root/subpath duplication, BC-preserved root output, sibling old imports and existence-conditioned packing. V-A109 observes the completed atomic producer release at `4.0.0`; the A85 exact-seven transport archives remain historical held evidence. W7's only remaining relevance is downstream adoption evidence owned by W17/W33, not an unmerged Value integration or a second Value publication.

## Completion evidence

V-A73's direct-consumer arm is ordinary product evidence, not a separate gate: zero root/`/parsing`/`/units` reach remains in active `demo/**` source or demo TypeScript paths; every migrated old object/parser call compiles against the exact declaration name/kind; and the ordinary demo TypeScript check passes. The immutable pinned Glass/Keyframes graph may still prevent the full Vite build before W17, so no routed mount, zero-overlay or nonempty-`#app` claim is required or credited here.

Exactly one clean value.js tarball is emitted here as the exact-seven Value 4 bootstrap transport. It exposes only the seven decided entries, resolves types/runtime from a clean consumer, and has no root/old forwarding file. `docs/tranches/V/audit/bootstrap/w7/value4-exact-seven/manifest.json` records the source tree, observed tarball path, byte length, SHA-512, seven-entry export/declaration facts and literal `BOOTSTRAP ONLY`; the manifest remains, but the tarball is unclaimed, unpublished, neither a W17 rehearsal nor a W33 candidate, and never a compatibility layer. `/color` exposes only the final-object `Result`/`AnyColor` contract and all seventeen mapped factories; `/css` equals its complete exact block—19 runtime and 33 type names, including ten source-consuming `ParseResult` functions, `parseKeyframeSelector`, `collectDeclarations` as the only keyed reducer plus `CollectedRule` and the located rule types—while Glass imports only its `parseCssColor`/`serializeCssColor`/`parseTimingFunction` slice; `/value` owns only `isLayoutTrackingUnit` plus its four immutable types; `/math`, `/transform` and `/quantize` equal their exact `ARCHITECTURE.md` blocks; and `/easing` equals 16 runtime/five type names, with nine direct named numeric functions, total numeric signatures, failure-explicit `easing(name)` and the six quart/quint rows. A fixture enumerates each built ESM runtime name and each `.d.ts` value/type declaration name, compares both sets and their kinds to the seven canonical blocks, and fails on a missing/extra/default/wildcard/namespace export or value-versus-type drift. Selector tests distinguish bare `entry` from explicit `entry 0%`, accept normalized `entry 50%` and `exit 100%`, prove `from` is the same frozen typed percent selector `{kind:"percent",value:0}` as authored `0%` and `to` is the same `{kind:"percent",value:1}` as `100%`, emit canonical `0%`/`100%` through stylesheet serialization, reparse to typed equality, apply the grammar's existing ASCII-case policy, reject malformed/negative/above-100 input with exact spans, and round-trip without a second parser. Focused `/css` vectors also produce immutable ASTs and canonical serialize→reparse equality for numeric `calc(1px + 2px)`, conditional `if(supports(...))`, `sibling-index()` and arithmetic inside `@function`; malformed operator, function and `supports` arms return exact nonempty diagnostics. Runtime/type counts remain 19/33. Direct numeric samples bind `easeOutExpo`, `smoothStep3` and `easeInBounce`; name samples bind camel/kebab lookup, while `timingFunctionDescriptions` is absent from runtime and declarations. The four library-only color spaces retain final-object/conversion/oracle coverage but must explicitly convert to a CSS-native `CssColor`; quantize returns final `Color<"oklch">` Results; no parallel CSS/raw tuple authority, general library-string serializer, `cubicBezierToSVG`, `timingFunctions` aggregate or `/browser` entry exists. Removed raw/nominal/direct primitives, old parser names, bare `PropertyDescriptor`, `parseSteps`, silent identity, helpers and aliases are absent and value's own demo/tests use exact entries. The full ordinary aggregate passes before packing. The sole pack path plants a stale sentinel and old root only in ignored `dist`, performs unconditional remove→production build→pack from the clean tracked tree, fresh-installs that tarball and proves stale members absent, a current source-visible fixture present and all seven runtime/type entries resolvable; its `exportsSha256` covers RFC 8785 canonical bytes of the in-tarball `package.json.exports` value and its per-`types`-target `declarationSha256` map covers exact declaration bytes. W8/W9/W7 merge together only after this package, direct demo typecheck and bootstrap record are green; no compatibility placeholder exists. Old pinned sibling heads and the full routed build/mount remain a known external RED owned by W17, and no package is published or sibling manifest merged here.

The preceding bootstrap-only no-publication language is historical transport disposition. V-A109 supersedes it only for the observed Value `4.0.0` producer tag/tarball/integrity/provenance; it does not promote any held bootstrap to a release artifact or close W17/W33.

V-A84/V-A85 amend this completion row: the 36,378-byte and 36,447-byte packs remain HELD/DEFECTIVE. The required shorthand/list/cascade suite is now implemented and W7 emitted the fresh unclaimed/unpublished 38,126-byte replacement at the exact path and hashes in the bootstrap manifest, with focused 58/58, aggregate 2,332/2,332 and types/lint/build/pack/runtime green; `/css` remains 19/33 and `/easing` 16/5. This is IMPLEMENTED/HANDED transport evidence, not the observed Value release coordinate.

## Disjointness, artifacts, commit

W7 owns entries, the direct value-internal demo consumer/typecheck cut, the one bootstrap tarball and its persistent manifest; W8/W9 own internals. Prepare a breaking C1 commit set and merge the three value-side waves atomically after package, direct-demo, full ordinary aggregate, clean-consumer and bootstrap checks. W11–W15 branch from that merged state. W17 alone consumes the bootstrap, migrates the pinned sibling graph and proves the full build/nonempty route before deleting the transient tarball bytes; W33 alone publishes.

## Archaeology

Supersedes U-F22/U-F63/U-F64's barrel/gate solution and fulfills U-F77 at the stronger no-dual-surface level.
