> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W24 — Admin Review Suite

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W10, W16, W18, W19, W22, W23

## Goal

Realize all retained administrative facilities as one scoped, attributable five-route review suite rather than a generic dashboard tail.

## Scope and file bounds

Admin Users, Names, Audit, Flagged and Tags routes; deletion of the current Palettes companion/right label/mobile pane toggle from all five compositions; approved/search/proposal moderation, feature/withdraw, suspend/reactivate/close and grant revocation, tag changes, query/pagination, review rows, dangerous confirmations, actor/scope/provenance and responsive disclosure. `PALETTE-FACILITIES.md` is final: generic batch/import/prune, impersonation and destructive review deletion do not enter.

## Triumvirate and worktree

- **Specification:** `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`, W1 wire reproduction, `PALETTE-DOMAIN.md`, `VISUAL-CONSTITUTION.md §7 Admin`, `OPTICAL-BENCH-COMPOSITIONS.md §4`.
- **Plan:** give each Admin route full main width and remove the Palettes companion; one review-row/query grammar; quiet system attribution; direct scoped commands; mechanism-specific flows; no shared token/impersonation. Admin Names' pending/approved state is an earned P092 SegmentedTabs consumer with producer fill/focus geometry and no local pill/descendant correction.
- **Implementation owner:** admin-product lead with identity, moderation and responsive-table collaborators.

## Current RED

Five live admin routes use divergent table/filter/action patterns and each is forced beside an unrelated Palettes companion that consumes half the desktop and creates a false mobile pane choice. Some retained server facilities have no client consumer; actor/scope is obscured; destructive proposal/report/tag/account operations erase or partially mutate evidence; dangerous actions and mobile disclosure are inconsistent.

## Completion evidence

Every retained admin target has a reachable exact-wire consumer. All five routes use the full shell main: companion/right-label/config/global-pane selector are absent. Names' pending/approved P092 indicator matches the active inner rect within 0.5px at all orientation/viewport/direction specimens, focus is separate and no local fill/descendant rule survives. System attribution invents no identity. Scope union controls actions without probing. Users completes lifecycle/grants and authority races: r42/r43/r44 render the immutable creation reason plus exactly one Principal or bootstrap-workload attribution arm, preserve it through revocation, and never invent the initial Principal as self-grantor. Its close confirmation names immediate public/auth suppression, the exact 30-day irreversible aftermath, the explicit active→trashed Handle transition and subsequent ordinary sealed purge—never an instant cascade or vague “delete everything”; retained cancelled trash/restore episodes are history, not a blocker to the close-created schedulable episode, even when distinct owner/admin receipt namespaces own equal raw command UUIDs. Flagged policy detail renders runtime initial `"lineage created"`/creator and `"release published"`/publisher, while only cut-created clear policies omit both. Names preserves proposal/slot history; Audit queries committed-effect provenance through the generated discriminated subject control: no subject filter, type-wide filter, or the selected type's correctly typed ID. It never enables or sends a bare ID, coerces v5/v7, accepts another platform literal, or flattens results into an untyped string pair; platform, cut-v5 and live-v7 rows render their exact type/identity. An outcome filter/column and a failed-attempt row are absent. Its time control emits either no bounds or one complete `[from,to)` interval, explains that the upper bound is excluded, and never sends a one-sided, equal, reversed or longer-than-366-day pair. Changing either bound restarts from page one rather than replaying a cursor bound to different query bytes. Flagged renders the pinned `ReportTargetSnapshot` and remains reviewable after active-release change/purge; Tags never reuses a key. Each runs against W16 with stable cursor, typed Problems, attribution and focus return; legacy batch/import/impersonation paths are absent.

Audit consumes the generated closed contract without a local compatibility enum: filters and rows contain no retired `session-revoke` action or vote-edge subject, and declarative votes render under their palette subject. Each row renders the persisted action/subject, optional actor/command, required correlation, authority witness and omitted-or-present before/after digests without claiming access to the unhashed projection. Bootstrap therefore shows its one correlation with absent actor/command; delayed purge rows show the inherited cause correlation; and a reviewed Report shows exactly one `report-review` row even when the applied effect changed palette or release policy. No UI-only outcome, worker, duplicate policy row or inferred subject exists.

## π / DELTA

- π: complete state/full-width set for Users, Names, Audit, Flagged and Tags at 1440/390; Names includes horizontal/vertical, LTR/RTL, selected/focused P092 states; Audit includes bootstrap, ordinary mutation, r49-with-policy and inherited-cause purge rows.
- DELTA: companion/right-label/mobile-toggle area `50%→0`; Names indicator four-edge error ≤0.5px, one fill/focus and override count 0; row/query geometry, identity/provenance, Q-AUDIT emitted time-key count `0|2`, half-open boundary results, exact event-row cardinality/digest presence, pagination, confirmation focus and durable result.

## Disjointness, artifacts, commit

W24 owns admin composition only and consumes W10 policy, W16 transport, W22 entity species and W23 action grammar. Commit shared review anatomy, then mechanism slices; do not fold five routes into one unreviewable commit.

## Archaeology

Prevents “admin” from being a noun-only tail and terminally disposes previously unconsumed color-name, tag, flag, user, batch and provenance facilities.
