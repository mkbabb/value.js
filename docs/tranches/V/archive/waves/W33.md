> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W33 — Canon, Packed Constellation, and Ordered Release

**State:** PRE-RELEASE RED / UNCOMPLETED · **Meta-tranche:** V.A Adversarial Analysis · **Staged dependencies:** W33a depends on W17a; W33b depends on W17b and W30–W32

W33 cannot produce release credit until W17 supplies a fresh strict/native Glass7 candidate; the held archive and dirty source tree are not candidates.

**V-A168 current-stage override:** Value4 and Keyframes6 are immutable and their coordinates are observed above. U-F77/B1/B14, residual Consumer-Cut/W2 producer-order language and stale Value3 demo-canon prose below are historical/retired. W33's remaining package candidate is Glass7 only. Q003 remains Glass-owned: after the measured ~95→5 nested-filter reduction and reverted 3/0 backing-plane experiment both failed, the honest 5/0 baseline is restored, Chromium-only attribution is withdrawn, and W33a accepts only native cycles with zero slab, occlusion corruption and lost target. A failed causal hypothesis may be declined; the visible product defect may not. W33a records the fresh Glass tarball/provenance/registry equality; W17b refreshes the value consumer; W33b later closes downstream canon/deploy locks. The generic Value→Keyframes→Glass order below is historical release procedure, not a request to rebuild or republish the two completed producers.

**V-A168 direct Glass brief:** W33a may publish only one fresh Glass7 tarball produced after W17a's strict declaration/HeaderRibbon closure, stale-canon cleanup and repeated native forward/reverse Q003 packet from honest 5/0 with zero slab, occlusion corruption and lost target. Candidate bytes must be registry-equal and independently installed before the Glass package row is SHIPPED; W17b's routed mount and W33b's downstream close follow publication. No held archive, causal-hypothesis decline, browser-only wait or product workaround substitutes for a green product packet.

**V-A168 current correction:** the inert Configurator backing-plane experiment was fully reverted after it failed at 3 active/0 nested filters. W33 must use the restored honest 5/0 baseline; Chromium-only attribution is withdrawn, and no product workaround or product-level decline may stand in for native product-green evidence. W33 retains only Glass7 pack/provenance and final downstream canon/deploy closure; W17b owns the routed value mount, while API/data duties named in older completion prose belong to their owning waves.

**V-A165/V-A168 staged release correction:** **W33a** is the sole early Glass7 publication slice. It begins only after W17a returns fresh strict/native bytes whose Q003 cycles have zero slab, zero lost target and zero occlusion corruption; a declined causal hypothesis is insufficient. W33a proves candidate↔registry equality and immutable coordinates, then stops without claiming W33 complete. W17b refreshes value against that registry artifact and proves the routed mount. After W18–W32, **W33b** performs final canon/deploy/downstream-lock verification and alone closes W33/V. This is an internal execution order, not a new wave or gate.

## Goal

Make the Glass7 package, native Q003 evidence, registry coordinate and downstream consumer graph tell one truth; publish exactly one fresh Glass7 artifact and only then refresh the routed value.js consumer.

## Scope and file bounds

**Current Glass-only bounds:** one clean Glass7 candidate from registry Value4 + Keyframes6, strict reachable declarations and export-map closure, native product-green Q003 packet, exact tarball/registry equality and provenance coordinates, followed by W17b's registry lock refresh/routed Browser proof and W33b's final downstream close. Value/Keyframes release work, held archives, file-sibling locks and generic ordered-producer procedures are historical and excluded from W33 implementation. Broader palette deployment/cut material remains planned under its owning W0–W16/W30–W32 rows and cannot be counted green by a Glass pack.

## Triumvirate and worktree

- **Specification:** `V.md §4`, `coordination/CONSTELLATION.md`, `CONSUMER-CUT.md`, `PALETTE-DOMAIN.md §2/§7`, `PALETTE-WIRE-CONTRACT.md §6`, `PALETTE-CURSOR-CONTRACT.md`, `PALETTE-OPERATIONS-REPLAY-CONTRACT.md`, `PALETTE-LOCAL-RAIL.md`, `LEGACY-PALETTE-SOURCE-CONTRACT.md`, `PALETTE-EXPORT-CONTRACT.md`, `research/auth-cookie-order.md`.
- **Plan (current Glass-only bounds):** W33 creates and publishes one fresh Glass7 candidate from immutable Value4 + Keyframes6 after W17's strict/native/Q003 closure, then refreshes downstream registry locks and routed mount. The broad palette/data-cut/edge/deploy duties in the historical plan below remain owned by W1/W10–W16/W22–W24/W30–W32 and cannot be closed by a Glass pack.
- **Implementation owner:** release lead with one package owner and one clean-checkout verifier; primary release branches only after all tags are authorized.
- **Execution:** open and inspect the deployed product Browser-first. Cross-engine binary/package automation is the narrow Browser-inexpressible exception; it cannot author or attest visual gestalt.

## Current release disposition

**V-A150 scope correction:** the broad data-cut, bootstrap, edge and 50-route paragraphs below are final W33b verification/deploy obligations only. Their implementation owners are W1/W10–W16/W22–W24/W30–W32, and none is a prerequisite for W33a's Glass7 package publication. W33 does not reimplement them.

V-A128 supersedes any future-facing producer wording in the historical release procedure below: Value4 and Keyframes6 already satisfy their immutable release evidence. W33's live candidate is Glass7 only; the post-Glass demo/deploy lock refresh follows its publication.

Value `4.0.0` is immutable and SHIPPED: tag/gitHead `44ddaff7a22283a4f7a42608893eeae7bc234424`, tarball `https://registry.npmjs.org/@mkbabb/value.js/-/value.js-4.0.0.tgz`, integrity `sha512-Z8ywb4htSxJlRFvoU1DNtvzr9Bsuaw9ahT/hvNlKbnRj6fTnLuXjn0itKq1Q5s6rwg24ct0zcLZ04BuR3/SzGw==`, shasum `ccb962e592fb42e6602fc2bb6afbfb1763788b9d`, provenance run `29497728532`. Keyframes `6.0.0` is also immutable and SHIPPED: gitHead `5a9183a7afe24702081a7b87c8adc7286ddce9a0`, tag object `26190755ce1e57c54cb14ef0a454ae02ed2b3da0`, tarball `https://registry.npmjs.org/@mkbabb/keyframes.js/-/keyframes.js-6.0.0.tgz`, integrity `sha512-mpb3gSxU8UgO4HBBG2he6CFNCq7tW+k9id82DgAjeeDdeAmtEzmZ2/kuK3j5AbUZRULcN1QNkNJychNk49bT4Q==`, shasum `5cc5fdecb886f4df33371d3358239594a8965b6b`, provenance run `29499708034`. V-A97 is discharged for both producer boundaries. Glass 7 stays downstream of this immutable pair, with its held rehearsal/declaration/HeaderRibbon/Q003 rows still red. W33 and Tranche V are not closed.

## Historical RED / producer lineage (superseded for Value and Keyframes by immutable releases)

**HISTORICAL (V-A108):** Keyframes registry truth is now 6.0.0, not 5.3.5; the 184,710-byte and 184,254-byte archives remain historical transport evidence only. Glass's diff selects 7.0.0, while V-A88/V-A90–V-A92/V-A96 hold its 966,350-byte rehearsal for declaration and inferred HeaderRibbon behavior; V-A94 holds cue/aperture credit. **CURRENT V-A168:** nested-filter reduction from approximately 95 to 5 and a reverted 3/0 backing-plane experiment did not remove Q003 compositor corruption; the honest 5/0 baseline is restored and Chromium-only attribution is withdrawn. W33a requires native product-green evidence with zero slab, occlusion corruption and lost target; a declined causal hypothesis does not satisfy it, and no browser-only product deferral is allowed. V-A97's producer order is discharged through immutable Value4→Keyframes6. Glass must build and pack one fresh strict/native Glass7 candidate from the already-proven registry graph, close its remaining rows and publish it; no file link, force, fabricated/stale lock, hidden peer or CI bypass may bridge the graph. V-A107 physically partitions the five additional hidden demo consumers rather than introducing a test exclusion. V-A99–V-A106 require meaningful seven-entry behavior, exact export names/targets, strict type and runtime declaration consumption, and a single recorded tarball. Ignored `dist` and the undeployed L4/NGINX edge remain separate W33 RED; browser-global cookie ordering is not claimed.

V-A84 extends candidate validation: the producer replacement must include leading/inter-rule stylesheet-comment and zero-argument `animation-timeline: view()` success, canonical round-trip and named failure vectors before any Keyframes/Glass rehearsal or release candidate is accepted. The held 36,378-byte bootstrap is not eligible input.

V-A85 further rejects the 36,447-byte comments/timeline pack: `collectAnimationOptions` must structurally own shorthand expansion and shorthand/longhand cascade, return all effective animation rows, and share shorthand resets with timeline collection. Candidate vectors cover the reported shorthand, both declaration orders, both importance directions, duplicate shorthand, comma/list alignment, unchanged longhand-only behavior and exact failures. Keyframes' `firstAnimation`, `collectShorthandOptions`, `collectStyleAnimationOptions` and every alternate classifier/parser are absent before a candidate is accepted.

V-A86/V-A87 reject three Keyframes archives and V-A93 rejects the 184,710-byte archive; only the 184,254-byte strict-consumer-green lineage may descend. V-A88/V-A90–V-A92/V-A94–V-A96 forbid descent from held Glass bytes until declaration closure without broken upstream type chains, explicit persistent/collapsible left/right behavior, actual-aperture cue placement and during/after-drag layer integrity pass natively. W33 walks packed declarations, derives residual bare roots and rejects nightlies, ambient injection, hoisting/`skipLibCheck` masks; no exposed internal members, DOM/VNode oracle, externalization, hidden peer, consumer sizing/overflow/timing, offset/cover/reset, snapshot or formatter workaround.

**HISTORICAL (V-A126):** The 38,126-byte A85 bootstrap and Keyframes 49/49 focused consume prove the implementation direction but are not W33 candidate evidence. W33 now requires fresh Glass candidate bytes plus the remaining native/declaration close and full absence records; it never republishes or promotes a Value/Keyframes rehearsal.

W17 selected Keyframes 6.0.0 with exact Value `4.0.0`, and that producer is now immutable. The 231,628-byte, 184,655-byte, 184,684-byte, 184,710-byte and 184,254-byte archives are held historical transport evidence and never promoted or republished. W33's next candidate is Glass only, built from the immutable registry pair after V-A88/V-A90–V-A92/V-A94–V-A96 and V-A124/V-A125 close.

**V-A168 release obligation:** the fresh Glass candidate must carry W17a's repeated forward/reverse native evidence on the restored honest 5/0 baseline, showing zero slab, occlusion corruption and lost target. A candidate without that product-green packet cannot be published or marked SHIPPED; an explicit decline can retire only a tested causal hypothesis, and no browser-only wait or product workaround substitutes for the required result.

## Completion evidence

- Canon describes only the landed physical tree, seven entries, palette ontology, Optical Bench, CSS-first boot and ordinary evidence path. It distinguishes internal mounted `/health`/`/openapi.json` from public same-origin `/api/health`/`/api/openapi.json`; OpenAPI `servers`, client examples and deploy probes advertise `https://color.babb.dev/api` only, never the private/direct API origin. No custom `/docs` product route or hand-kept route table survives. Every example is a deliberately authored typed snippet checked against public `/color`, never generated from private source. W33 verifies the W10/W16 meta cut; it is not its production deletion owner.
- Before any release or deploy claim, the served entry document and every transitive SPA asset must reproduce W32's frozen `{urlPath,byteLength,sha256}` manifest and its canonical manifest digest exactly; an added, absent, renamed or byte-different script, stylesheet, font, image or renderer asset reopens W31/W32 rather than acquiring a new golden in W33. From the final public authority, the in-app Browser opens a cold boot and each of the eighteen canonical composition coordinates using the exact W32 route/fixture digest/seed/scheme/viewport/DPR/reduced-motion/font/renderer/settlement tuple, checks that all assets resolve to the frozen bytes, and re-captures representative Picker, Browse, Account and Admin states only after their named semantic terminals. Static structure/state counts must match exactly. Dynamic material and renderer frames must satisfy the originating W32 numeric relations and tolerances—including P019 `1/√φ`, line-box and inset `±0.5 CSS px`, Blob `0.66 ±0.015`, centroid ≤5%, hull inset ≥12%, Card/boundary/reserve counts and W31 timing bounds—but are not required to reproduce screenshot bytes or zero per-pixel drift. An out-of-tolerance or state/topology/interaction change reopens its owning wave and W32; W33 may not mint a replacement visual golden.
- A fresh authorized checkout deletes all prior output, executes the unconditional production build, and packs one designated Glass release-candidate tarball; that exact file is the sole release candidate and exact `npm publish <tgz>` input. W33 never reuses W17 rehearsal hashes, W7 bootstrap bytes, ignored `dist`, or an existence-conditioned `prepare`. The planted-stale-member/source-visible-fixture test passes against the exact release tree. W33 may make exactly one isolated noncandidate verification pack from the same clean tree solely to compare member names/content hashes; it is deleted unpublished and supplies no coordinate. Registry Value4 and immutable Keyframes6 fixtures prove the exact `/css` 19/33 and `/easing` 16/5 surfaces plus selector/grammar/easing vectors. Before Glass7 publication, W33 walks every packed `.d.ts` reachable from all `exports`/`typesVersions`, resolves relative edges, derives residual bare roots and requires direct dependency/peer/optional ownership; hoisted resolution is insufficient. Glass source, `vite.library.ts`, profiler accepted map and ordinary bite test agree on exact `/color`, `/css`, `/easing`; every edge resolves runtime/types and forbidden roots, old entries, local parser, general serializer, `parseSteps`, timing registries, silent identity and formatter residue are absent. W33 records the Glass coordinate/tag, observed `gitHead`, candidate bytes/SHA-512, registry URL/`dist.integrity`, canonical `exportsSha256` and exact declaration hashes; fetched registry bytes equal the candidate. Existing Glass `v6.0.0` remains immutable. After Glass7, all external consumers install registry artifacts and rerun their W17 smoke. W33 alone records **SHIPPED**.
- **HISTORICAL (V-A123):** The former registry order was Value `4.0.0`, Keyframes `K_NEXT`, Glass `G_NEXT`; Value4 and Keyframes6 have completed that order. The live W33 candidate is Glass7 only; its downstream demo/dev lock refresh follows Glass publication. Public-compatible diffs keep packages on their current majors, and only the enumerated incompatible breaks selected `6.0.0`/`7.0.0`.
- From the final immutable SPA asset manifest—not W23's earlier worktree—W33 loads the deployed export module in current stable Chromium/V8, Firefox/SpiderMonkey and Safari/WebKit/JavaScriptCore, recording exact browser/engine binaries. It verifies the packed value artifact that owns `toRgba8` and W23's canonical 1-color/50-color/escaping/alpha/out-of-gamut source snapshots. For each JSON/CSS/Tailwind/SVG/PNG vector, all three engines must match W23's exact filename, MIME, serialized SHA-256 and full bytes; PNG must also match W8's raw RGBA tuples, chunks, Adler-32 and CRC. One real fresh Prepare→reload→Download activation per engine proves the persisted prepared bytes, no reserialization, exact anchor properties, 60-second/`pagehide` revocation and only `handoff-initiated`. Any color-tuple divergence reopens W8; any framing/artifact divergence reopens W23 and blocks release. Disabling PNG, restoring a palette-local converter or accepting a new golden is not a disposition.
- A source write freeze yields exactly the nine named collections and one immutable restricted pre-run `cut-input.json`. The **only** tracked cut coordinate is `docs/releases/4.0.0/data-cut/<cutId>/cut-public-manifest.json`, written once after two transforms and encrypted-object verification. Its exact redacted body is the Domain §7 public-manifest DTO: cut ID/instant, transformer tree, collection/disposition/reject aggregate counts, logical domain digests, encrypted object coordinate/version/ciphertext digest/byte length and completion time—no row, row ordinal, slug, identity, source key/source-key digest, target key or per-row provenance. The restricted versioned object contains the logical input/source/target/disposition/provenance/reject manifests and rows under `LEGACY-PALETTE-SOURCE-CONTRACT.md`; plaintext is byte/digest verified, encrypted together, uploaded/version-verified and destroyed from every deployment disk. Two isolated transformations are byte-identical and each candidate's canonical export equals the restricted `cut-target.ndjson` before bootstrap. Any reject, count/reference/digest mismatch, write failure or bootstrap failure destroys that entire candidate and leaves the source deployment/credentials untouched; it is not re-deadlined as a partial seed.
- A fresh production schema begins not-ready: `/health` is 503 and registration/product traffic is `503 platform_uninitialized` with zero writes. The DB-direct command then records exactly one PlatformState/bootstrap tombstone, permanent PublicHandle claim, initial Principal/AuthFence, HMAC verifier, frozen eleven-scope Grant and AuthorityGuard at epoch 1; readiness flips atomically. Recovery plaintext is absent from logs/config/process state afterward while the operator retains the sole offline copy, a second invocation fails, and no bootstrap HTTP route exists. Before release, the guard's stored eligible-holder digest equals a transactionally recomputed set; concurrent authorized self-removal is exactly one success/one `409 last_root_authority`, and the distinct cross-target removal race is one success/one ordinary actor-fence 401/403 with zero second effect. If already bootstrapped, W33 verifies rather than recreates.
- The verified cut exposes exactly `|P+|` system-attributed public legacy Handles and no live private/unlisted/archived legacy row, Workspace or legacy identity/session/social edge. Every current version and active hash, Kahn release number, canonical content/tag/name row, claim/tombstone traversal, public-handle reservation, target count equation and logical digest matches §7/source contract; source counters and cross-lineage slug provenance do not leak into rank/fork truth. Only after this and bootstrap verification does the release deploy receive one target credential. The old application is stopped, every old runtime database credential is revoked, the new application has no source/archive network route, and on-wire probes prove no old collection, compatibility endpoint or fallback reader. After artifact verification, W33 deletes `api/tools/data-cut-v4/**` and its one-shot tests from the final tree; the tracked redacted public manifest plus transformer git-tree coordinate and restricted encrypted object version are sufficient provenance without publishing private row material. The encrypted offline archive is evidence, not a live quarantine.
- The final cut verifier recomputes each ContentObject's exact Workspace/Release/Report counter cardinalities from live target rows, requires object existence iff their sum is positive, requires cut-specific Workspace/Report zeros and `Σ Release refs=|V+|`, and proves every tombstone digest is non-owning. A mismatch, direct repair or collector fallback destroys the candidate before credential switch.
- The final authenticated API fixture gives owner O's r15 trash and distinct admin A's r41 close the same raw UUIDv7 under their different `principal:<ownerId>`/`principal:<adminId>` ReceiptNamespaces. After restore retains O's cancelled palette episode, A's close/aftermath creates and completes its distinct episode; the literal namespace persists on the Handle, both PurgeJobs and each canonical result record, and a command-only key, namespace hash, inferred actor alias or second identity fails release.
- Edge provenance records non-proxying DNS answers; one dual-stack internet-facing Network Load Balancer, TCP:443 listener and at least two availability zones; target/security-group IDs; `proxy_protocol_v2.enabled=true`; private target addresses; health contract; edge image and NGINX/njs artifact digests; explicit `js_engine qjs`; public edge certificate identity; immutable SPA asset manifest; current/verify-only MAC key IDs; private CA/version, edge client-certificate identity and Hono server-certificate identity for `api-origin.color.babb.dev`; and the private TLS Hono listener/security group. Release IAM may update only those named resources and read the named secrets. Edge targets have no public address and accept TLS/health only from the load-balancer security group; Hono accepts only the edge security group **and** a current private-CA edge client certificate with exact SNI/Host. Direct target, plaintext origin, wrong/no client certificate, public API DNS/listener, alternate object-site origin, Cloudflare HTTP proxy/Worker/WAF/Pages/VPC path and unsigned local/test bypass probes all fail. The transport choice follows AWS's [Layer-4 NLB contract](https://docs.aws.amazon.com/elasticloadbalancing/latest/network/introduction.html) and [PROXY-v2/client-address rules](https://docs.aws.amazon.com/elasticloadbalancing/latest/network/edit-target-group-attributes.html); observed IDs and digests, not predicted coordinates, enter the release record.
- `https://color.babb.dev` is the one browser authority. The same NGINX edges serve the immutable SPA and one external catch-all `location /` with `js_content edge.dispatch`; every API/static destination is named or `internal`, and the SPA contains only relative `/api` URLs. `merge_slashes off` is present but non-authoritative. The dispatcher's first operation reads `r.rawVariables.request_uri` as a Buffer, never `r.uri`, `$uri`, `$args` or `r.args`; this is required because NGINX documents `$request_uri` as the [full original URI](https://nginx.org/en/docs/http/ngx_http_core_module.html) while location matching uses a normalized URI. It splits at the first ASCII `?`, preserves query bytes, mints the exact UUIDv7 Problem correlation ID with WebCrypto, and classifies literal `/api` before any internal redirect.
- The byte-level path table covers canonical routes plus uppercase namespace, `%61`, `%2F`, `%5C`, `%2e`, `%25`, double encoding, adjacent slashes, dot segments, raw non-ASCII, malformed UTF-8 and an encoded structural `/api` prefix. It executes the Domain's exact one-decode/backslash/collapse/dot **projection only**, rejects a projected API target lacking the literal prefix, discards the projection, then validates literal API bytes. For post-vhost pre-content 400/414, the internal handler reuses the available raw target and emits the API Problem only for literal/projected API input; non-API input remains the fixed static 400. Parser failure before a request exists is observed as connection termination, not falsely claimed as JSON. Only after validation does the dispatcher strip one byte sequence `/api`, map an empty remainder to `/`, append the untouched query and set the internal proxy target. URI-form `proxy_pass`, `rewrite` prefix removal and a second decode are absent; the variable target preserves the validated request form under NGINX's [proxy URI rules](https://nginx.org/en/docs/http/ngx_http_proxy_module.html). `GET /api/palettes?q=sea%20foam` proves valid encoded query preservation separately from forbidden path encoding. HTTP/1.1 and HTTP/2 return the same closed status/media/body/correlation result.
- The NLB's PROXY-v2 address—not `preserve_client_ip`, `Forwarded`, `X-Forwarded-*`, `X-Real-IP` or another client header—is canonicalized and signed; native IPv4 and native IPv6 probes preserve their independent subjects, while a spoofed forwarding or second PROXY frame cannot replace the first transport identity. The dispatcher reads the exact body once, enforces the 1 KiB/64 KiB route bound, reconstructs only `Content-Type,Cookie,Origin,Sec-Fetch-Site,Sec-Fetch-Mode,Sec-Fetch-Dest,Idempotency-Key,If-Match`, rejects duplicate/ambiguous fields, strips non-session cookies and signs the Domain's ordered presence/value header block plus method/target/client-IP/correlation/body. The origin accepts only private-CA mTLS, exact SNI/Host, ±15-second time, canonical UUIDv7, current/30-second verify-only key, identical header-block reconstruction and constant-time MAC before route/body work. Private probes alter certificate/CA, Host, SNI, key, time, correlation, MAC, method, target, query, every signed header, body and client IP; each fails with zero domain writes, while the valid private request succeeds. Public `POST`/`PUT`/`PATCH`/`DELETE` preserve and require exactly `Origin: https://color.babb.dev`, `Sec-Fetch-Site: same-origin`, `Sec-Fetch-Mode: cors`, `Sec-Fetch-Dest: empty`; a browser GET carries that exact fetch tuple with absent-or-exact Origin, while a CLI/operator GET carries none. Missing unsafe, partial or variant tuples return `403 origin_forbidden`; no browser bearer/header/storage or credentialed CORS path exists. `PALETTE-LOCAL-RAIL.md` owns the separately proven local authority and real-PKI profile; no local origin is accepted by this production artifact.
- Every `/api/**` method/body/status/Problem and applicable reconstructed Cookie, signed origin/fetch fields, Idempotency-Key, single strong If-Match/ETag and exact content type survives the edge. Response headers are closed to the Domain allowlist. An on-wire generated assertion covers all 50 success rows and every reachable Problem status: exact JSON-versus-Problem media type, universal `Cache-Control: no-store`, edge-derived `Date`/`Content-Length` and the Wire's exact HSTS/nosniff/CORP/referrer/CSP fields; ETag, Set-Cookie and Retry-After occur only in their closed matrices, while `Vary`, `Location`, CORS, duplicate or unlisted fields never occur. For the three cookie-capable routes, the QuickJS handler begins one 10,000 ms timer before a private internal subrequest, awaits complete status/headers/body with a 65,537-byte sentinel, and copies only one exact `__Host-value-session=<st1>; Path=/; Secure; HttpOnly; SameSite=Strict` header for a fully settled origin response of at most 65,536 bytes that wins the timer. Duplicate/unknown cookie, attribute/order deviation, duplicate singleton response field, delayed header/body in both orders, abort, mTLS/private-network failure, 65,537 bytes and late completion are cookie-free contract/timeout Problems; 65,536 bytes passes. Every non-cookie route intercepts unexpected `Set-Cookie` before public headers, emits the cookie-free `502 edge_cookie_contract_violation` Problem and discards the origin body. `proxy_buffering on` without this settled handler is not evidence. Browser response-order probes are deliberately separate: an injected post-emission stale same-name cookie may win, but its token authorizes no new effect, `/sessions/me` is 401, and Account exposes signed-out/recovery rather than false continuity. W33 claims origin settlement and fail-closed recovery, never browser-global delivery order.
- For r3–r5 success, “applicable” means required: the origin and public edge each expose exactly one valid session cookie. Missing cookie is deliberately injected and must become cookie-free `502 edge_cookie_contract_violation`; it can never pass as a cookie-less 2xx.
- The same settled handler withholds every noncookie API response under the ten-second deadline and a 4,194,305-byte sentinel: exactly 4,194,304 bytes reaches the wire, while the next byte, abort, private failure or late completion is discarded and replaced by the buffered cookie-free `503 dependency_unavailable`. On-wire abuse probes verify `Retry-After=max(1,ceil((latestExhaustedWindowEndMs-mongoNowMs)/1000))`, including fractional upward rounding and no old-window 429 at the exact boundary.
- Product logs are generated from the Domain allowlist only: timestamp, deploy version, edge instance ID, method, mounted route template/`unmatched`, status, elapsed time, public correlation ID and outcome enum. Default request/access logging is disabled; bounded edge errors, Hono logs and a seeded secret/cookie/IP/query/path/header/MAC canary prove absence of forbidden material. The edge has no session-route rate limiter: native IPv4/IPv6 and two controlled egress fixtures prove the signed source reaches the one Mongo policy. Registration new effects obey source 5/10m with completed proof replay bypass; recovery failed proofs obey source 10/15m and post-verification handle 5/15m while correct proof/replay remains usable after hostile handle exhaustion. No registration-handle budget exists; W16 remains the exact time/key/dummy-proof proof.
- Live deploy confirms security headers, `st1_` cookie issue/replay/expiry and the same-origin auth behavior, and a deliberately triggered deployment event produces the expected webhook delivery/receipt with event identity, timestamp and terminal status. A genuine external deployment or webhook blocker leaves W33 **BLOCKED** with its exact evidence; recording it is not completion and no source-only result is called published.
- Every K/N/T/U/recent prompt, chronic, partial and banked route has terminal disposition; two final audits are clean.

## π / DELTA

- π: W32's exact immutable SPA asset-manifest digest and eighteen deterministic composition coordinates; one Browser-opened cold deployed boot plus Browser route/state reachability for all eighteen, with deployed Picker, Browse, Account and Admin states recaptured at their named settlement tuples.
- DELTA: manifest and served asset-byte drift exactly zero; missing/extra request zero; route/state/font/renderer identity, topology, Card/boundary/reserve and interaction counts exact; every dynamic proportion/material/motion measure remains inside its named W32 tolerance. Screenshot-byte or zero-per-pixel equality is explicitly not claimed. Any structural mismatch or out-of-tolerance measure is owned by W31/W32 or the originating feature wave and blocks W33 rather than being normalized here.

## Disjointness, artifacts, commit

Canon follows product truth. Package commits/tags remain per repository. Broad release commits/tags carry why, what, evidence, staged cut order and routed external remainder. User-owned CLAUDE deletions remain untouched absent attribution.

## Archaeology

Folds the live revalidation/release remainders of U-F21/F28/F39/F61/F63/F66/F68/F69/F71/F77, the 3.2/2.0 fiction and V campaign supersession. U-F41 remains BANKED until NCSU VPN access; U-F75 is already retired. Formation `FINAL.md` is not overwritten with execution claims until W33 lands.
