> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W17 — Post-BI Glass Adoption and Constellation Cut

**State:** VALUE4 + KEYFRAMES6 IMMUTABLE · GLASS IMPLEMENTATION/TYPECHECK LANDED · V-A168 Q003 ACTIVE RED / V-A114 ROUTED RED · FRESH GLASS7/PUBLISH OPEN · UNCOMPLETED · **Meta-tranche:** V.H Hardening · **Depends on:** W3, W7–W9

**V-A168 current-stage override:** the bootstrap, Keyframes-rehearsal, U-F77/B1/B14 and residual Consumer-Cut/W2 producer-order language below is retained as historical lineage. Value4 and Keyframes6 are already immutable, and Glass's registry-only pair install is already proven. Current W17a work is Glass-only strict declaration/HeaderRibbon close, stale Value3 demo-canon cleanup and product-green Q003 evidence from the honest 5/0 baseline before a fresh rehearsal; W33a publishes it; W17b then owns the stale value consumer graph and routed mount. A causal hypothesis may be declined, but remaining slab, occlusion corruption or lost target may not. No Keyframes refreeze, Drawer change, producer republish, Browser-only wait, backing plane, mask/cover, sampler workaround or filter-count target is implied.

**V-A131 source-state addition:** the active Glass7 source/lock is LANDED/IMPLEMENTED but dirty (HEAD `c181f0a78`, lock SHA-256 `e6216e2188ea7d08fff572745be168dbf368df3d30baa10c2caa837bc817581f`). V-A134's direct evidence is 176 files/1,132 tests and typecheck green; build/pack/verifier are not creditable in the current sandbox. These facts do not promote the held 966,350-byte archive. W17 still requires a fresh strict/native packed Glass7 artifact and Q003 disposition before W33 or downstream mount work.

## Goal

Close the post-immutable Glass consumer boundary from registry Value4 + Keyframes6: finish strict declaration/HeaderRibbon closure, remove or annotate stale Value3 canon, obtain native Q003 product-green evidence from the honest 5/0 baseline, and hand one fresh Glass7 rehearsal to W33a. After publication, refresh the value.js consumer graph and prove the routed nonempty mount through W17b. A falsified hypothesis may be declined, but the product must show zero slab, occlusion corruption and lost target. No Value/Keyframes rebuild, bootstrap promotion or compatibility path belongs to this wave.

## Scope and file bounds

**Current Glass-only bounds:** strict packed declaration reach, HeaderRibbon persistent/explicit-collapsible behavior, stale Value3 canon annotation/removal, native Q003 forward/reverse evidence from the honest 5/0 baseline, one fresh Glass7 rehearsal, and the downstream value.js registry refresh/routed-mount proof. Value4/Keyframes6 source, bootstrap bytes, producer locks and release paths are historical evidence and are not W17 implementation bounds. Clearly labeled archaeology remains excluded; Glass owns all producer-side implementation.

## Triumvirate and worktree

- **Specification:** `coordination/CONSTELLATION.md`, post-BI glass inbox, `ARCHITECTURE.md §4`, `CONSUMER-CUT.md`.
- **V-A168 direct Glass brief:** The active task `019f6610-6022-7df1-95b3-472b17a64656` consumes only registry Value4 + Keyframes6; closes strict declaration reach and persistent/explicit-collapsible HeaderRibbon behavior; removes/annotates stale Value3 demo canon; and repeats native in-app Browser forward/reverse Q003 captures from the honest 5-active/0-nested baseline until zero slab, occlusion corruption and lost target are observed. It then emits one fresh Glass7 rehearsal. W17a records that rehearsal/native handoff; W33a alone publishes. No held archive, product-level decline, browser-only wait, backing plane, filter target, sampler, mask/cover, Keyframes/Drawer change, alias, fallback, file link or stale lock earns credit.
- **Historical plan note (V-A128):** The long successor/bootstrap procedure below preserves the executed migration evidence only. It is not a current instruction to rebuild Value/Keyframes or select a successor; the direct Glass brief above and the current RED section govern.
- **Plan:** consume only the named post-BI V-CUT rows and exact active-session relay: P019's family-neutral paired type tokens; P122 landmark-neutral/title-gap/opt-in boundary+reserve with no universal minimum; shared renderer lifecycle; P047's producer measurement seam/protocol and four-case rendered baseline; P092 for earned owner/Admin/Mix tabs; P051 removal of interactive tag/host; P059/P060 public copy; and the P019/P010/P127 font/package split. Preserve immutable Glass v6, re-census/protect the active producer tree and do not choose its successor major from the tranche label. Credit landed producer source/tests but never infer native gestalt or complete measurement from them. Execute every W2 decision. Before any sibling migration, verify and consume the sole W7 exact-seven Value 4 tarball against `docs/tranches/V/audit/bootstrap/w7/value4-exact-seven/manifest.json`, including its recorded tree, path, bytes and SHA-512. Delete descendant tab corrections; reject P122 main/automatic divider/reserve/minimum. Recount live HEAD, then exhaustively migrate the formation census of 25 explicit value `WatercolorDot tag="div|button"` calls across Dock seal/edit, ColorSpaceSelector, eyedropper, Spectrum, channel, palette, Generate, Mix and Gradient. Ornamental faces simply drop the prop and become aria-hidden; data-bearing static faces remain present as noninteractive named list/text content with zero activation/focus/drag semantics; every operable face gets an enclosing semantic seat with slot/label semantics on that seat. In the Keyframes successor worktree migrate every old parsing import to the exact 19-runtime/33-type `/css` declaration, including `parseKeyframeSelector`, the sole keyed `collectDeclarations` reducer and context-preserving `CollectedRule` collectors; prove canonical `from`/`to` typed-percent handling plus numeric `calc`, conditional `if(supports(...))`, `sibling-index()` and `@function` arithmetic against the bootstrap; delete Keyframes' selector regex/token parser and local CSS grammar without a fallback, alias or dual path while retaining timeline phase resolution; move exactly `formatCSS`, `serializeStylesheetItem`, `reverseAnimationShorthand` and `reverseCSSTime` once into `src/animation/compile/emit/css-text.ts`; replace the sole `parseCSSTime` call in `src/animation/engine/options.ts` with `parseCssScalar` plus explicit `s`/`ms`; and keep only one local named-timeline projection in `src/animation/scroll/grammar.ts`. Retain canonical `/easing` direct calls to `easeOutExpo`, `smoothStep3` and `easeInBounce`, move only rendered description copy into bbnf-buddy `src/animation/easingGroups.ts` and Keyframes `demo/utils/reference-data/animationDescriptions.ts`, and delete Value's `timingFunctionDescriptions`. Then delete timing registry/plain-vars/WAAPI literal/duplicate descriptor+catalog+flatten seams, replace `vite.config.ts`'s package-prefix externalization with the exact six-entry `/color|/value|/css|/easing|/math|/transform` set, replace both TS resolver roots and both tracked HTML umbrella maps with exact packed capability keys, and retain explicit flipY plus ordinary diagnostics. Credit `70b32501` for the four already-absent proof scripts, migrate/revalidate the surviving ordinary tests, and delete the stale `docs/tranches/I/audit/investigate/probes/rc-parse-crash-seam.mjs` root-import reproducer. Diff the resulting Keyframes runtime exports, declarations, package entries and documented public behavior against published 5.3.5; select the exact `K_NEXT` and peer range before rehearsal, using the next 5.x minor when public-compatible and `6.0.0` only when the diff enumerates an incompatible public removal, signature change or behavior change, then pack the clean `K_NEXT` rehearsal against the exact bootstrap bytes. Migrate Glass against that exact Value-bootstrap/Keyframes-rehearsal pair to the exact three-entry `/color|/css|/easing` contract: CSS text parses only through `/css`, final-object color work only through `/color`, numeric curves only through `/easing`; exact-externalize all three and update the live package/README-wired `profile-bundle.mjs` matcher/bite test accordingly. Remove old raw/nominal/direct color imports, `parseSteps`, silent empty/default/identity fallbacks and the Keyframes cycle. Diff the resulting Glass runtime exports, declarations, component prop/event-slot entries and documented behavior against immutable 6.0.0; select the exact `G_NEXT` and peer range before rehearsal, using the next 6.x minor when public-compatible and `7.0.0` only when the diff enumerates an incompatible public removal, signature change or behavior change. Remove the hidden pin, switch to the migrated producer, pack the clean Glass `G_NEXT` rehearsal, freshly recreate the Value 4 rehearsal from the same frozen content, and run every isolated consumer without touching protected primaries. Finally, build the full value.js app against those exact rehearsal dependencies and prove the nonempty routed mount; once the W17 Value rehearsal has replaced the transport, delete the bootstrap tarball bytes but retain its `BOOTSTRAP ONLY` manifest record.
- **Implementation owner:** constellation lead with one owner per repository; coordinated three-repo worktrees, no independent merge.
- **V-A85 consume ruling:** accept only a fresh Value bootstrap whose producer structurally expands animation shorthand/lists and cascades shorthand with longhands. Delete Keyframes `firstAnimation`, `collectShorthandOptions` and `collectStyleAnimationOptions` before rehearsal; where Keyframes deliberately supports one animation it calls `collectAnimationOptions(declarations).at(0)` explicitly. No consumer classifier, token regex, parser, fallback or dual semantic owner remains.
- **Face semantics clarification:** where the migration prose says a data-bearing WatercolorDot face is “inert,” it means present noninteractive named list/text content with zero activation, focus or drag semantics; it does not mean the HTML `[inert]` attribute. HTML `[inert]` remains reserved for the separately measured modal/route cases, and the enclosing semantic seat remains reachable.

**V-A134 correction to the current RED narrative:** any older 1,127/769 or 1,131/771 Glass counts in the historical paragraphs below are superseded. The current direct run is 176 files/1,132 tests with typecheck green; build, pack and verifier remain uncredited in this sandbox.

**V-A168 Q003 correction:** the inert Configurator backing-plane trial reached 3 active/0 nested filters but still failed native capture and is fully reverted. W17's live baseline is the honest 5/0 topology; Chromium-only attribution is withdrawn. Only a repeated native forward/reverse packet with zero slab, occlusion corruption and lost target can resolve this product row. A falsified causal hypothesis may be declined, but visible corruption cannot. No workaround or new gate is introduced.

**V-A165/V-A168 execution correction:** W17 is staged without creating another wave. **W17a** closes strict declarations/HeaderRibbon, reconstructs the immutable pre-`.325` P047 baseline, and returns a fresh Glass rehearsal only after native forward/reverse Q003 cycles show zero slab, zero lost target and zero occlusion corruption at honest 5/0. Disproving an adjacency hypothesis may decline that hypothesis only; it cannot authorize bytes while the product remains corrupt. **W33a** then publishes those verified Glass bytes. **W17b** consumes registry Glass7 in value.js, removes the pinned mixed legacy graph and proves the canonical routed mount. W17 remains UNCOMPLETED until W17b; W33a does not close W33.

## Current RED

**HISTORICAL / V-A154 correction:** Value `4.0.0` and Keyframes `6.0.0` are now immutable; their bootstrap and rehearsal paragraphs below are historical transport context, not current publication work. The earlier Glass 176/1,131/771/3,572/strict-closure report is superseded: current direct evidence is 176 files/1,132 tests with typecheck green, while fresh build/pack/verifier/native evidence is uncredited. No fresh packed/provenance Glass7 artifact exists. V-A114 remains the concrete current mount failure: the stale value.js consumer still resolves Glass5/Keyframes5 packed root/old-parser imports before `createApp().mount()`. Glass's immutable-pair install is already proven; it must close declaration/HeaderRibbon/Q003-native rows and publish fresh Glass7, after which value refreshes the consumer graph and proves the canonical routed mount. The separate lazy About/doc `@src/units` closure also blocks the full demo build. No root restoration, alias, nested mask, file/tarball lock, static ground or second mount is a valid repair.

The born RED was a pinned Value3/Keyframes5 graph. V-A93 holds the 184,710-byte Keyframes archive for strict TS2420 failure; its 184,254-byte replacement is historical transport evidence and removes the false public `implements` claim without exposing internals. Glass consumed that archive during rehearsal; current Glass consumes immutable registry Value4 + Keyframes6 only. HeaderRibbon closes inference through one discriminated contract: omitted/`mode="persistent"` is expanded from first paint; `mode="collapsible"` requires a non-empty label and gives Glass ownership of the real focusable disclosure button. Source/type/build/focused checks and native 1280/390 persistent plus left/RTL-right collapse/pin/Escape are green; native OS reduced-motion remains pending without policy circumvention. V-A94 is native-implemented with stage/aperture `340.296875px`, cue bottom 12px inside and one canvas. **V-A168 current Q003 disposition:** native forward/reverse captures still show compositor plates corrupt beside WebGPU after nested filtered regions fell from approximately 95 to 5; a reverted inert backing-plane experiment reaching 3/0 also failed. The honest 5/0 baseline is restored, Chromium-only attribution is withdrawn, and Glass must return repeated native cycles with zero slab, occlusion corruption and lost target. Declining one causal hypothesis cannot close this row. V-A96 requires public declarations to stop leaking broken stable Reka/VueUse implementation-type chains. Fresh Glass declaration/pack, Value consumer refresh, and routed Browser remain RED.

## Completion evidence

- Value uses InstrumentChassis and Slider directly. InstrumentChassis stage/dial is landmark-neutral, so the shell remains the sole `<main>` owner without a consumer role patch. ConfirmDialog sites use P106 Dialog; VersionHistoryDrawer uses P107 Drawer; ActionButton/PaletteSlugBar use P104 Tooltip; ColorInput uses P103 Popover; ToggleChip uses P091 Chip; old Dock sites use P034/P036/P040/P041. Local forwarding wrappers, retired names and housing recipes are zero.
- P019's paired sizes/line-heights remain exactly `1/√φ` at floor/fluid/ceiling; value applies Fraunces/Fira and exposes the three-arm fixture consumed by W20. P122 exposes tight title gap, exact golden/preview proportion enum, producer collapse, no nested main, typed boundaries/reserves and no automatic minimum/1.2fr grid; Glass contains neither the universal `--instrument-min-block-size` consumer hook nor an equivalent alias. Every value P122 consumer passes the binding boundaries `[]`/reserve `none`, and Generate's narrow DOM order is specimen→model-inspector→commit-action. `--instrument-dial-padding-inline|block` are the outer logical insets of the combined stage+inspector composition; `--instrument-dial-gap` is the stage↔inspector separation on the active layout axis; `--instrument-control-padding-inline|block` are the action-region boundary→action-content insets; and `--instrument-control-gap` is the separation between adjacent action children. P092 matches owner/Admin/Mix fixtures; no global correction. P051 has no public tag/button host or activation/focus/drag listener: the execution-time recount and migration manifest contain every live call, the formation baseline is 25→0, operable sites use named seats, ornamental faces are hidden and data-bearing static faces have named list/text semantics. Independent producer hover paint may remain only if non-semantic and motion-policy compliant.
- `/blob-config` is runtime-free; `/blob` and `/aurora` renderer code sit behind causal imports. P043/P045/P054 own lifecycle/capability/failure/pause/resource behavior while P046/P047 expose scene facades; named reveal/settled/veil/header/dock seams resolve. P047's producer coordinates are exact: source `../glass-ui/src/components/blob/Blob.vue` plus `../glass-ui/src/components/blob/composables/useMetaballRenderer.ts`; ordinary tests `../glass-ui/tests/components/custom/blob/blob-surface.test.ts` plus `../glass-ui/tests/components/custom/blob/Blob.interaction.test.ts`; and tracked baseline record `docs/tranches/V/audit/pi/w17/p047-producer-baseline/manifest.json` with Browser-opened captures `webgpu-dpr1.png`, `webgpu-dpr2.png`, `webgl2-dpr1.png` and `webgl2-dpr2.png`. Each of its four rows records producer commit and source/test hashes, immutable seed bytes, canonical CSS color, complete effective-config digest, P047 settled-frame identity, engine, DPR, backing size, explicitly exposed `{x,y}` simulation origin, sampled premultiplied-alpha buffer hash, threshold `≥0.5`, origin-component area, baseline ratio `b₀ = 2√(A/π)` over the shorter footprint dimension, and the observed baseline centroid and hull coordinates. The routed producer specimen is opened, inspected and captured first in the in-app Browser; only a named engine/DPR surface inaccessible there may use headed real-GPU hardware, and no Playwright capture or source-only claim substitutes. W17 closes only the producer seam/protocol and freezes `b₀`; it neither changes `bodyRadius` nor requires W29's final mass, centroid, hull or 0px-geometry verdict.
- **V-A167:** because `.325` already exists in the live dirty tree, the `.26` `b₀` rows above must come from an identified immutable pre-change tree/blob, not a current-tree toggle or recreated config. The manifest records that exact tree and every source/test/config hash; inability to recover it leaves W17/W29 RED.
- **HISTORICAL:** Glass at `G_NEXT` supplies a small ordinary sheet for Plus Jakarta Sans and Fira Code only, with P019 metrics/ranges and product surface, P010 semantic projection, and P127 hashed assets/URL/dependency truth agreeing in a packed value consumer, including speedtest's former `?url` case. P019's native review and P127's packed smoke prove request/stability without reviving P061's runner or P128's generator. Fraunces remains value-owned and is handled by W4/W20/W31. No base64 corpus or deferral plugin survives.
- For W17 completion, Glass's routed Search specimen and every adopted value surface must render public product concepts and observable behavior; `wired|proof|consumer|triage`, tranche/wave/gate/proof vocabulary and internal codenames must be absent from visible copy. P059/P060 must keep real public imports and useful behavior; no copy framework or scenario runner may appear. Those native/copy rows remain RED notwithstanding the green Glass package rehearsal.
- Keyframes builds on Value 4 entries/floor, owns its `InterpSlot`, handles every parser result at its named boundary, and has no runtime/optional Glass dependency. `vite.config.ts` externalizes exactly the six packed Value 4 entries `/color`, `/value`, `/css`, `/easing`, `/math`, and `/transform`; each resolves runtime and types through the tarball export map, and no root, prefix predicate, old entry, private dist path, `/quantize`, or `/browser` survives. The value-side DOM-relative `convertToPixels` arms, computed endpoint resolver, layout epoch/cache and resize invalidation move once into `src/animation/resolve/browser.ts`; Value's copies/exports/listener/tests are deleted. `AnimationVisualizer.vue`, `resize-tracks.test.ts` and `computed-resolution.test.ts` consume that local owner, whose unit classification comes only from `/value` `isLayoutTrackingUnit`. Its public/internal `getTimingFunction` registry/mirrors are absent; invalid explicit/stylesheet timing is diagnosed rather than inherited/defaulted. `plain-vars.ts`, `PlainProjection`, `_plainProj` and both hot-path adapter branches are absent while custom/group output matches; `WAAPI_INELIGIBLE_UNITS` is absent while the complete old table and negative controls retain behavior; all four live descriptor sites use `CSSPropertyDescriptor`, packed declarations contain no bare/`_2` leak; the whole duplicate bezier table is absent and the six quart/quint picker rows match Value; `flattenVars.ts` and its two demo imports are absent after preset and CSS-import round trips; the sole local timing-path sampler receives explicit `flipY`. Both TS bench resolvers and both tracked HTML import maps resolve only the exact packed capability entries actually loaded; the launcher paths execute each map and no root or prefix umbrella survives. The useful CSS-parity, scroll-roundtrip and diagnostics-channel assertions live in ordinary tests; commit `70b32501` remains credited for the four absent proof files/command identities, the stale Tranche-I executable root-import probe is absent, and no proof successor appears. Glass at `G_NEXT` builds on Value `4.0.0` and Keyframes at `K_NEXT` floors against the exact `ARCHITECTURE.md §3` packed declarations: `/css` calls `parseCssColor(source:string): ParseResult<CssColor>` and `serializeCssColor(color:CssColor): Result<string,ColorIssue>` over the closed thirteen-space CSS-native union; all seventeen spaces retain factory/conversion/object-oracle coverage, and library-only colors explicitly convert before serialization; `/color` supplies only failure-explicit final-object `convertColor`/`mixColors`/`mapColorToGamut`/actual-surface `safeAccentColor`/degree-domain `interpolateHue`/`toRgba8`; and `/easing` uses the named easing exports plus exact `CubicBezier(x1,y1,x2,y2): Result<EasingFunction,EasingIssue>`, `steppedEase(count,position?): Result<EasingFunction,EasingIssue>` and failure-explicit `easing(name)` names, with no `timingFunctions` aggregate or `timingFunctionDescriptions`. `vite.library.ts` externalizes exactly `@mkbabb/value.js/color`, `@mkbabb/value.js/css`, and `@mkbabb/value.js/easing`; the live ordinary bundle profiler recognizes, reports, and bite-detects all three root-reachable edges and rejects the root, prefix umbrella, old/private entries, local parser and raw/nominal/direct primitives. Ordinary consumer tests prove parser success has exactly readonly empty diagnostics, failure has a readonly nonempty tuple and is not empty/default success, the requested contrast ratio is measured against the actual painted band, hue wrap stays in degrees, invalid easing never becomes identity, and dynamic Fourier lookup handles the `easing(name)` Result without a property or aggregate fallback. For W17 completion, every required Glass producer row consumed here must be implementation- and rehearsal-complete with exact source/test/Browser coordinates, or DECLINED with an implemented exact replacement; the still-open native/copy/P047-baseline rows remain RED and RE-DEADLINED cannot satisfy adoption. W17 records REHEARSED, never SHIPPED; W33 alone may attach the observed `G_NEXT` coordinate and mark SHIPPED.
- Keyframes' migrated CSS edge resolves all 19 runtime and 33 type names from the packed `/css` declaration and imports only the names it actually consumes. `parseKeyframeSelector` and `KeyframeSelector` are the sole token/AST authority: bare `entry` has absent offset, `entry 0%` has zero, `entry 50%` has `0.5`, `exit 100%` has `1`, `from` is the same frozen typed percent selector `{kind:"percent",value:0}` as authored `0%`, `to` is the same `{kind:"percent",value:1}` as `100%`, canonical stylesheet serialization emits `0%`/`100%`, reparsing preserves typed equality, mixed case follows the existing ASCII policy, malformed/negative/above-100 input fails with exact spans, and named-selector round trips preserve absent-versus-zero. The same packed grammar produces immutable ASTs and canonical serialize→reparse equality for numeric `calc(1px + 2px)`, conditional `if(supports(...))`, `sibling-index()` and arithmetic inside `@function`; malformed operator, function and `supports` arms yield exact readonly nonempty diagnostics. Keyframes keeps only timeline phase resolution; its selector regex/token parser, local CSS parser and duplicate stylesheet normalization are absent with no fallback, alias or dual grammar. The four text helpers have one implementation at `src/animation/compile/emit/css-text.ts`; their Value owners/exports/tests and all forwarding imports are absent. `src/animation/engine/options.ts` handles the `parseCssScalar` Result and converts only explicit `s`/`ms`; `src/animation/scroll/grammar.ts` owns the sole derived named-timeline projection without copying token grammar. `parseStylesheet` reports invalid animation/timeline grammar with original UTF-16 spans before total projection; option/timeline collectors receive one selected declaration list and never merge unrelated selectors. Keyframes follows each `CollectedRule.path`, evaluates supported ancestors or emits a named unsupported-context diagnostic, and selects only the last applicable same-name keyframes block; duplicate blocks never concatenate and inactive contexts never flatten. Ordinary declaration-cascade cases prove normal→normal later wins, important→normal important wins, normal→important important wins and important→important later wins. `/easing` resolves all 16 runtime/five type names; exact direct samples preserve `easeOutExpo`, `smoothStep3` and `easeInBounce`, while the Keyframes and bbnf-buddy UI files own their rendered descriptions with no Value description table. The `/css` runtime/type counts remain 19/33. No old `/parsing` symbol, duplicate CSS grammar, fabricated diagnostic span or text-helper dual path survives.
- W3 supplies only decided asks and present LANDED/RED coordinates; it supplies no package or compile claim. W17 selected `K_NEXT=6.0.0`; V-A86/V-A87 reject three Keyframes attempts and V-A93 rejects the 184,710-byte archive while authorizing the strict-consumer-green 184,254-byte replacement. Glass's packed diff selects `G_NEXT=7.0.0`, but V-A88/V-A90–V-A92 reject its first rehearsal and inferred-anchor corrections; V-A94/V-A95 subsequently reject a visual-interaction green over clipped cue geometry and compositor corruption. **V-A124/V-A125 keep Q003 active RED:** the ~95→5 reduction and reverted 3→0 backing-plane experiment both failed, restoring the honest 5/0 baseline and withdrawing Chromium-only attribution. **Historical V-A119 count, superseded by V-A134:** the ordinary Glass batch was previously reported as 1,127 tests/769 modules; the current direct source run is 176 files/1,132 tests with typecheck green, while fresh build/pack verification is presently unverified in this sandbox. Glass still must close declaration ownership plus Q003 native adjacency against the immutable pair, publish fresh Glass7 and let the full value.js build prove the nonempty routed product. No held pack, alias, shim, fallback, dependency-hoisting reliance, cycle or rehearsal hash supplies release truth; W33 alone owns fresh candidates, publication and SHIPPED coordinates.
- Producer visual/focus samples carry the small π/DELTA required by their live product owners; value does not substitute local CSS. Browser-unavailable rows remain explicitly pending and block the matching adoption/release claim rather than receiving Playwright or source-only credit.

## π / DELTA

- π: direct three-arm P019 pair and P122 tight gap/golden+preview proportions/absent-inspector/stage→inspector→action/landmark/boundary/reserve fixtures, including value's `[]`/`none` and Generate narrow-order consumes; the exact P047 source/tests and `p047-producer-baseline/manifest.json` with four internal-Browser-first dual-engine/DPR captures; component migrations; P092 owner/Admin/Mix; exhaustive P051 callsites; Dock/Slider/veil; `G_NEXT` fonts; every consumer tarball smoke.
- DELTA: type ratio/line-height/gap; main, boundary, reserve and automatic-divider counts; P047 seed/color/config/settled-frame identity, engine/DPR/origin/buffer hashes, frozen baseline `b₀` and observed baseline centroid/hull coordinates—with no W29 final-target claim; Generate region order; component identity/focus; tab edges/fill/overrides; all face/seat pixels plus interactive-host count 0; terminal Dock/veil/font agreement; and per-consumer entry/parser/slot/unit/descriptor/catalog/path/lazy/dependency change.
- **V-A168 Q003 π/DELTA:** from the restored honest 5-active/0-nested baseline, capture repeated native in-app Browser forward and reverse interactions beside WebGPU after settlement, recording live-backdrop adjacency, compositor-plate integrity, opaque-live/transparent-capture state, nested-cell topology, and the correction that yields zero slab, occlusion corruption and lost target. A declined experiment remains research evidence only. The reverted 3/0 backing-plane experiment, filter-count reduction and Chromium prior art carry no credit; no Browser-only wait, cover, mask, sampler or Keyframes/Drawer workaround may satisfy this row.

- The Keyframes manifest preserves all held rehearsal candidates, including the 184,710-byte TS2420 archive and the 184,254-byte strict replacement. Those archives are historical transport evidence only. The immutable producer is now registry `@mkbabb/keyframes.js@6.0.0`, gitHead `5a9183a7afe24702081a7b87c8adc7286ddce9a0`, provenance run `29499708034`, tarball `https://registry.npmjs.org/@mkbabb/keyframes.js/-/keyframes.js-6.0.0.tgz`, integrity `sha512-mpb3gSxU8UgO4HBBG2he6CFNCq7tW+k9id82DgAjeeDdeAmtEzmZ2/kuK3j5AbUZRULcN1QNkNJychNk49bT4Q==`, shasum `5cc5fdecb886f4df33371d3358239594a8965b6b`. Glass consumes only this immutable registry pair; no held archive supplies current release credit.
- The 966,350-byte Glass archive at `/private/tmp/glass-v7-rehearsal-20260716-kf-indent-final/mkbabb-glass-ui-7.0.0.tgz` retains exact runtime/build/profile/enumerated-native and semver evidence, but V-A88/V-A90–V-A92 mark it HELD/DEFECTIVE. Its declarations reach undeclared packages and its action-only right HeaderRibbon is unreachable. Current Glass disk implements the direct bare-root walk, removes `skipLibCheck`, deletes DOM/VNode inference and exposes the discriminated persistent/collapsible mode with a Glass-owned focusable disclosure button. The nonvacuous declaration probe walks every packed `.d.ts` reachable from `exports`/`typesVersions`, resolves relative edges and requires every residual bare package root to be directly listed in dependencies, peers or optionals; a hoisted no-lock install is supplementary only. V-A94/V-A95 and V-A124/V-A125 additionally require cue/aperture geometry and live-backdrop-adjacency integrity before repack; the reverted backing-plane experiment and Chromium-only attribution carry no credit. The ordinary major batch is LANDED/pre-release, not SHIPPED; no fresh artifact or registry coordinate exists. No shim, fallback, `skipLibCheck`, DOM/VNode oracle, first-frame timing cloak, offset/overflow cover, engine mask, canvas reset or transitive-hoisting reliance is allowed.

## Disjointness, artifacts, commit

One commit series per repo is tied by a cut manifest; unrelated work is not staged. Value4 and Keyframes6 are immutable producer releases. Glass 7.0.0 remains held until one fresh registry-only rehearsal closes V-A88/V-A90–V-A92/V-A94–V-A96 plus V-A168's product-green native Q003 result on the honest 5/0 baseline. W33a—not W17—publishes that fresh candidate without closing W33; W17a records the rehearsal/native handoff only. After Glass7, Keyframes may restore its demo/dev Glass edge and deploy workflow against registry consumers. W17b then refreshes value's consumer graph and proves the app. No sibling merge occurs before the set is green; no file link, forced/fabricated lock, stale-lock acceptance or CI bypass breaks the order.

V-A73/V-A75 make “locally green C1” include value's complete active `demo/**` import/name/kind migration, ordinary demo typecheck and the one `BOOTSTRAP ONLY` exact-seven pack—not the full Vite build or routed mount. W17 owns the sibling migrations, value dependency/pin removal, full build and nonempty in-app-Browser reload; it never repairs the graph through a retired export, alias or fallback.

## Archaeology

Terminally builds U-F2/F4/F13/F28/F68/F77 and the long-running glass-ui adoption disease; a relay alone no longer counts.
