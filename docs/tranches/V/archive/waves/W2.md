> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W2 — Topology and Final-Object Prototype

**State:** PLANNED · **Meta-tranche:** V.R Research · **Depends on:** —

**V-A130 current-stage override:** The Value3/SCC/root/`ValueUnit` statements below are historical RED evidence. Value4 and Keyframes6 are immutable; W2 has no live producer, publication or `K_NEXT` work. Its surviving duty is the recorded topology/callsite manifest and palette/API census. Current downstream UI, native, and Glass7 release work remains with W17/W33.

## Historical goal (implemented)

Validate the selected physical demo tree and disciplined-final-object API against real imports, typing, cycles, and the complete keyframes mutable interpolation path before the large clean break.

## Scope and file bounds

- Read/prototype: `demo/**`, `src/**`, build/export configuration, and every tracked active first-party consumer in `CONSUMER-CUT.md`.
- Write during execution: `docs/tranches/V/research/target-graph.md`, a disposable prototype outside the landing tree, and the ratified move/consumer manifest.
- No production source lands in W2.

## Triumvirate and worktree

- **Specification:** `ARCHITECTURE.md`, `CONSUMER-CUT.md`.
- **Plan:** prove the exact 17-row `ARCHITECTURE.md §2` `SpaceId→ChannelsBySpace` representation, `"none"`/range/unit/object-oracle laws and total 17×17 conversion table; prove canonical CSS spelling laws for exactly the thirteen CSS-native rows and rejection/no-round-trip laws for the four library-display-only rows; freeze the exact seven-entry runtime/type export-name-and-kind manifest, including `/css` 19/33 with its one selector parser, `/easing` 16/5 with nine named numeric functions, and the closed `/value`, `/math`, `/transform` and final-object `/quantize` blocks; then prove grammar composition, DOM separation, package self-reference without aliases, immutable CSS value→keyframes `InterpSlot`→authored custom/group/DOM emission paths, and the complete source/demo/test/bench/HTML/script/build-configuration/current-public-documentation consumer projection including every Keyframes U covenant row in `CONSUMER-CUT.md`.
- **Implementation owner:** library architect plus demo-topology and consumer collaborators; disposable worktree deleted after results are captured.

## Current RED

**HISTORICAL RED:** The demo was physically alias-shaped; runtime SCCs spanned 14 color and four parsing/color modules; `Color` had a dynamic index; late registration masked order; `ValueUnit` owned DOM state; root/subpaths duplicated 1,008 authored lines. Nine active package repositories resolved the root across old majors, nominal classes and silent parse fallbacks; the two-package sibling census missed them. The historical Keyframes 5.3.5 baseline at `a59d3a22da080a8ed224e8d675112bb3bb0135b0` with Value `^3.1.0` is superseded for the active rail by immutable Keyframes 6.0.0 against Value 4.0.0; its held rehearsal lineage remains evidence only.

## Completion evidence

- The prototype implements all 17 named factories and the exact schema table with `satisfies Record<SpaceId,SpaceSchema>`; every tuple is compile-time typed without `any` or per-space subclasses. One ordinary isolated `ANCHOR_ORACLE` row per space carries literal source→XYZ-D65 and XYZ-D65→source expectations plus the exact `bijective | hue-degenerate | lossy-kelvin` class; it imports no production constants or generated inverse. Off-axis vectors prove RGB `/255`/`*255`, hue `/360`/canonical `*360`, and OKLCH's raw-0.5 versus CSS-percent-0.4 split. ProPhoto vectors cover positive and negative below/equal/above both transfer thresholds, its frozen forward/inverse matrices and two adaptation matrices. Rec.2020 proves the frozen matrix plus positive/zero/negative sign-extended BT.1886 gamma-2.4 vectors and rejects PQ and the piecewise BT.2020 OETF. HSL/HSV/HWB/LCH/OKLCH prove below/equal/above the exact powerless predicates and accept/emit hue `"none"` only on the powerless arm. Kelvin independently copies the exact piecewise coefficients and proves 1000/1900/just-above-1900/6500/6600/just-above-6600/40000 forward XYZ vectors before exercising the exhaustive integer XYZ-distance argmin, lower tie, alpha carriage and non-finite rejection; it is never called bijective. The generated 17×17 matrix then proves every ordered conversion through the sole anchor graph against those independent row oracles and exact classes; there is no production direct path to preserve or compare. Deleting or substituting any row fails typechecking or a named literal vector; agreement among production paths cannot pass as independence.
- Its dependency graph is acyclic and grammar recursion is composed explicitly.
- Two packed keyframes workloads prove unit compatibility, identity padding, clone behavior, browser provenance and color carriage through keyframes-owned mutable `InterpSlot`s. **Numeric-slot:** 32 nested unitless numeric leaves, custom-transform sink plus group-compositor sink, 1,000 warm-up + 10,000 measured frames; compilation and one-time authored-shape sink construction are outside the sample. It permits no transient JS allocation in interpolation/refresh, no measured-sample GC, and compile allocation no more than baseline plus one slot and one stable sink writer per property. **Serialized-output:** 12 changing leaves (four numeric, four length/angle, four color) emitted to four DOM targets for 20 × 1,000-frame cycles. Changing CSS strings are expected output, not claimed allocation-free. Record allocated bytes/frame, minor/major GC count and pause, median/p95 frame cost, and forced-GC retained heap after warm-up and each cycle. It passes only when bytes/frame and GC pause do not exceed the identical baseline, no major GC or cycle-over-cycle retained-heap climb appears, final retained heap is within 5% of the first post-warm-up snapshot, median is ≤1.05× baseline, and p95 is ≤1.10×. Both workloads compare exact emitted values, authored custom/group outputs and target styles without `plain-vars` or boxed-leaf projection.
- Native package self-reference resolves exact entries with project aliases removed.
- The final move/consumer manifest has one destination and one owner per value file, a `color-session` domain, and the complete `CONSUMER-CUT.md` source/demo/test/bench/HTML/script/configuration/current-public-documentation set. Every active repository has an exact old-symbol→capability-entry map plus named nominal/`instanceof`/clone/parser outcome. The completed census recorded tracked Keyframes 5.3.5, protected the five prospective-6 metadata files, and enumerated every resulting AST module declaration plus the named migration seams. The manifest freezes Keyframes 5.3.5 and immutable Glass 6.0.0 as comparison baselines; W2 did not select `K_NEXT` or `G_NEXT`. Glass enumerates `vite.library.ts` and the live package/README-wired `scripts/profile-bundle.mjs` as semantic consumers. The Keyframes manifest fixes its final consumed capability set at exactly `/color`, `/value`, `/css`, `/easing`, `/math`, and `/transform`, including packed runtime/type resolution for each; it contains no root or prefix-externalization escape. The four proof-script deletions are credited to `70b32501`, not re-booked. Archives and execution-entry protected primary baselines retain their decided disposition. No generic “external consumers” owner remains.
- The bounded entry repair census matches `CONSUMER-CUT.md`: `/value` is one runtime predicate plus four immutable types; `/css` is the exact 19-runtime/33-type cohesive grammar block, with ten source-consuming `ParseResult` operations, `parseKeyframeSelector` as the sole selector-token parser, `collectDeclarations` as the only keyed reducer and every recursive collector returning context-preserving `CollectedRule` rows; `/easing` is exactly 16 runtime/five type symbols with nine direct named numeric functions; `/math` is the nine numeric/string leaves with no type export or `cubicBezierToSVG`; `/transform` is exactly nine runtime and six type symbols; `/quantize` is two Result-returning operations plus three types and no CSS/raw tuple authority. Selector vectors bind bare `entry`, explicit `entry 0%`, `entry 50%`, `exit 100%`, `from` as the frozen typed percent-zero selector, `to` as the frozen typed percent-one selector, canonical `0%`/`100%` serialization and typed reparsing, malformed, negative, above-100 and round-trip behavior; absent named offset differs from zero and all present offsets are normalized `[0,1]`. The same canonical grammar census covers immutable AST success and canonical round trips for numeric `calc`, conditional `if(supports(...))`, `sibling-index()` and arithmetic inside `@function`, plus malformed operator/function/`supports` arms with exact readonly nonempty diagnostics; it retains no duplicate stylesheet normalizer and does not change the 19/33 counts. The CSS census maps every live Keyframes grammar import, moves exactly four text helpers once to `src/animation/compile/emit/css-text.ts`, replaces the sole `parseCSSTime` consumer with `parseCssScalar` plus explicit `s`/`ms` conversion, and marks every other old facade symbol private, localized or retired. The easing census retains canonical `easeOutExpo`, `smoothStep3` and `easeInBounce` implementations, moves only consumer-rendered description copy into bbnf-buddy and Keyframes UI files, and deletes Value's `timingFunctionDescriptions`. Value's three image-quantize demo consumers handle the Result and final `Color<"oklch">`. An unlisted helper, default/wildcard/namespace export or value/type-kind mismatch is a named RED fixture rather than an implementation choice.

## Disjointness, artifacts, commit

W2 owns research artifacts only. W5–W9 execute the result. Commit as `docs(v-w2): ratify physical topology and final-object graph`; discard the prototype worktree.

## Archaeology

Supersedes V's min-manufactured-edge objective, 500-line gate, absent codemods, and unresolved OF-3/OF-4/NG-6 choices.
