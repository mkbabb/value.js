> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W9 — One Parse Result and Grammar Composition

**State:** VALUE4 PRODUCER SHIPPED · HISTORICAL / NO LIVE WAVE DUTY · downstream adoption remains W17/W33-owned · **Meta-tranche:** V.H Hardening · **Depends on:** W2, W8

**V-A130 current-stage override:** W9's parser/easing producer duties are complete in immutable Value4. The U-F77 and residual Consumer-Cut producer-order tails are retired. The only live downstream cut is a fresh Glass7 artifact after native Q003 live-backdrop adjacency closure and stale-canon cleanup; W9 does not reopen the producer or add a parallel grammar.

## Historical goal (implemented)

Give every CSS/value/stylesheet/timeline parse one explicit success/failure contract and remove fallback reparsing, stringization, and parser/color cycles.

## Scope and file bounds

`src/parsing/**`, `src/easing.ts`, CSS serialization, value.js demo parser consumers and tests. Keyframes parse consumers are read-only manifest/fixture inputs here; W17 alone writes them. Public entry naming is W7/W17.

## Triumvirate and worktree

- **Specification:** `ARCHITECTURE.md §2–3`.
- **Plan:** build `ParseResult<T>`/`ParseIssue` with readonly empty diagnostics on success and a readonly nonempty diagnostic tuple on failure; compose recursive grammar once; implement the complete exact `/css` block in `ARCHITECTURE.md`: full-consumption scalar/value/list/keyframe-selector/color/stylesheet/timing/timeline/range parsers, syntax coercion, typed timeline serialization, the closed stylesheet/custom-function/animation/timeline AST and seven distinct collectors, including separate `collectDeclarations` and `collectPropertyDescriptors`. The sole selector production maps standard `from` and `to` tokens directly to frozen typed percent values `0` and `1`; canonical stylesheet serialization emits `0%` and `100%` and reparsing preserves the typed values. The same value production accepts numeric `calc` arithmetic, conditional `if(supports(...))`, `sibling-index()` and arithmetic inside `@function`, produces immutable ASTs, and removes duplicate stylesheet normalization rather than retaining a second grammar. Keep the closed thirteen-space `CssColorSpace`/`CssColor`, `parseCssColor(): ParseResult<CssColor>` and `serializeCssColor(CssColor)` boundary; keep all seventeen factories/conversions/object oracles in `/color` while requiring explicit library-only→`CssColor` conversion and providing no general library-string serializer. Compile timing AST through `/easing`'s exact total `linear`, nine named easing exports—including retained `easeOutExpo`, `smoothStep3`, `easeInBounce`—`CubicBezier`, `steppedEase`, parser-independent `linearEasing` and failure-explicit `easing(name)` signatures; retain live `@property` metadata only as `CSSPropertyDescriptor`; add the exact six quart/quint rows to canonical `bezierPresets`; migrate callers by semantic handling; transpose only rendered description copy to the bbnf-buddy and Keyframes UI owners and delete Value's `timingFunctionDescriptions`; keep `/easing` parser-free; delete exception/null/status+value, `CSSCubicBezier`, `parseSteps`, `timingFunctions`, identity-on-unknown and bare-descriptor paths. W17 performs the one move-only four-helper text transposition and the `parseCSSTime` consumer adapter fixed by the same contract.
- **Implementation owner:** grammar lead with stylesheet/timeline and consumer migration units; one worktree due recursive type/API cut.
- **V-A85 producer ruling:** make the private structural animation-shorthand arm shared by source-span validation, `collectAnimationOptions` and `collectTimelineOptions`. It expands the already-parsed AST without text, regex or serialization/reparse, applies importance then source-order cascade per represented subproperty, lets shorthand reset all represented fields and lets a later longhand override only its field, and aligns comma/list components by the CSS repeatable-list law. `collectAnimationOptions` returns a frozen row per effective animation name, or one default row when any animation declaration exists; a singular consumer must select `.at(0)` explicitly.

## Born RED / current remainder

The born RED mixed throwing/null/status protocols, reparsing and silent substitutions. The A85 grammar/collector pack remains held transport evidence only; V-A109 records the completed W8→W9→W7 Value producer release at immutable `4.0.0`. W9's remaining work is only downstream consumer/rehearsal evidence, not an unmerged Value cut or a second publication.

## Completion evidence

- Every public parser returns `{readonly ok:true;readonly value:T;readonly diagnostics:readonly []} | {readonly ok:false;readonly diagnostics:readonly [ParseIssue,...ParseIssue[]]}` and declares full-consumption behavior. No alternate optional/possibly-empty diagnostic shape exists.
- Every diagnostic carries stable code plus UTF-16 span; no failed branch exposes `value:T`, every success diagnostic tuple is exactly empty and every failure tuple is readonly and nonempty.
- `parseTimingFunction` exercises every closed `CssTimingFunction` arm from `ARCHITECTURE.md`: CSS keywords; bounded-x/unbounded-finite-y `cubic-bezier()`; `steps()` defaults, aliases, positive-count and `jump-none≥2`; one/two/omitted-input `linear()` stops and used-value canonicalization; and whitespace. Malformed/non-finite numbers, arity/ranges, unknown text and trailing input return stable issues with exact UTF-16 spans. Non-CSS library catalog names resolve explicitly through `/easing`, outside this grammar. There is no throwing parser arm or invalid→undefined/default conversion.
- `parseKeyframeSelector` is the sole selector-token production used by standalone calls and `parseStylesheet`. A named selector's present `offset` is a finite normalized fraction in `[0,1]`; absence preserves bare spelling and is not coerced to zero. Ordinary cases cover bare `entry`, `entry 0%`, `entry 50%`, `exit 100%`, malformed tokens, negative and above-100 percentages, plus parse→private-serializer→parse equality. `parseKeyframeSelector("from")` returns the same frozen typed percent selector `{kind:"percent",value:0}` as authored `0%`, and `to` returns the same `{kind:"percent",value:1}` as `100%`; canonical stylesheet serialization emits `0%`/`100%`, reparsing preserves typed equality, and mixed case follows the grammar's existing ASCII policy. W17 deletes Keyframes' duplicate selector regex/parser while retaining phase resolution.
- Focused ordinary vectors parse numeric `calc(1px + 2px)`, conditional `if(supports(...))`, `sibling-index()` and arithmetic inside `@function` into immutable `/css` ASTs and prove canonical serialize→reparse equality. They also cover leading and inter-rule CSS block comments plus zero-argument `animation-timeline: view()` through declarations. Malformed operator, function, `supports`, comment and timeline arms return stable exact UTF-16 spans in readonly nonempty diagnostics. Standalone value parsing, custom-function parsing and `parseStylesheet` use the same production and normalization; there is no local/duplicate grammar or second stylesheet normalizer. These vectors and the full ordinary aggregate must be green before W7 emits its sole exact-seven bootstrap, and the 19-runtime/33-type export counts remain unchanged.
- The typed stylesheet AST, collectors and W2 declaration fixture expose only `CSSPropertyDescriptor`; source and value-side declarations contain zero bare `PropertyDescriptor`, compatibility alias or `_2` collision name. W7 proves the packed value declarations and W17 proves the coordinated packed Keyframes declarations.
- `/css`'s built runtime/type name-and-kind sets equal the 19/33 declaration block exactly. `collectDeclarations(readonly Declaration[])` is the only keyed reducer and returns one winning declaration per property: important beats non-important regardless of order and equal importance uses later source order; ordinary normal→normal, important→normal, normal→important and important→important vectors prove later/important/important/later winners. The four recursive stylesheet collectors return document-ordered `CollectedRule` arrays with immutable AST index paths; property/function/style rows remain distinct, duplicate keyframes are never concatenated, and context is never flattened. `CustomFunctionDescriptor`, animation options and scroll/timeline/range/trigger types remain producer-owned. Standalone helper parsers not exported by the block are private implementation arms or explicitly retired/localized, never undeclared public residue.
- `animation: 2s ease-in-out infinite alternate` projects structurally into one typed row. Ordinary vectors also cover shorthand before and after an overlapping longhand, important shorthand against normal longhand and the converse, duplicate shorthand declarations, comma-separated animations, list-valued longhands, unchanged longhand-only input, shorthand timeline reset, malformed/ambiguous slots, negative duration, malformed easing and empty list arms with original UTF-16 spans. The collector returns one frozen row per effective name and applies repeat/truncate list alignment; Keyframes' deliberate singular use is `.at(0)`. No public `parseAnimationShorthand`, consumer classifier, string parser, regex or serialize/reparse path exists, and `/css` remains 19 runtime/33 type exports.
- `bezierPresets`, descriptions and numeric catalog include exactly `ease-in-quart [0.895,0.03,0.685,0.22]`, `ease-out-quart [0.165,0.84,0.44,1]`, `ease-in-out-quart [0.77,0,0.175,1]`, `ease-in-quint [0.755,0.05,0.855,0.06]`, `ease-out-quint [0.23,1,0.32,1]`, and `ease-in-out-quint [0.86,0,0.07,1]`; table-driven samples and serialization names agree.
- Every demo/keyframes parser callsite is in W2's manifest and explicitly propagates diagnostics, converts to a named typed domain error, or deliberately rejects at a named boundary; none substitutes a color or silently creates a string value.
- Parser/color SCCs and global grammar registration are zero.
- `/easing` packed graph contains no parser dependency and equals 16 runtime/five type names; `/css` owns CSS value/color/stylesheet/custom-function/animation/timeline/timing syntax, and each of its ten source-consuming operations uses the one `ParseResult`. `parseStylesheet` validates option/timeline grammar while original UTF-16 spans exist; its collectors are total and cannot synthesize zero/placeholder spans. Ordinary tests prove `parseCssColor` returns `CssColor` only for the thirteen CSS-native spaces, rejects the four library-only spellings, preserves readonly-empty success diagnostics and readonly-nonempty failures, and requires explicit conversion of a library-only final object before `serializeCssColor`. The nine named easing exports are sole implementations; direct numeric vectors cover `easeOutExpo`, `smoothStep3` and `easeInBounce`, dynamic camel/kebab lookup handles the failure-explicit `easing(name)` Result, and no `timingFunctions` aggregate, `timingFunctionDescriptions` export or identity/property fallback survives. The bbnf-buddy and Keyframes UI owners hold only their rendered description copy. Unknown catalog names and invalid numeric constructors return issues rather than throwing, returning undefined or silently selecting identity/linear easing. W7's clean pack resolves both runtime and declarations from the current build, not pre-existing `dist`.
- Existing U-F29–F33 regression cases remain green without the deleted proof script.
- The four formatting/text implementations move once to `../keyframes.js/src/animation/compile/emit/css-text.ts` in W17, and their value implementations/exports/tests disappear in that same cut. Keyframes' sole `parseCSSTime` site instead handles `parseCssScalar` and explicit `s`/`ms` conversion. No second CSS grammar, forwarding export or text-helper dual path survives.

## Disjointness, artifacts, commit

W9 owns parse/serialize, value caller migration and sibling decision manifest; W8 final objects are input. Its commits remain held only until W7 closes the locally green value-side C1; W8/W9/W7 then merge atomically without publication. **HISTORICAL PLAN:** W17 executed sibling migration before W33 registry release. The current downstream sequence is Glass7 native closure, then registry/demo refresh; the immutable Value4 and Keyframes6 producer surfaces remain closed. No compatibility overload.

## Archaeology

Does not re-book the exact U-F29/F30 bugs; it removes the broader three-protocol architecture exposed after those fixes.
