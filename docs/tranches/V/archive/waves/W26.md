> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W26 — Extract and Mix Workbenches

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W18–W21, W23

## Goal

Give image extraction and N-ary color/palette mixing distinct proportionate stages while making both produce honest palette/color objects.

## Scope and file bounds

`workbenches/extract/**`: image drop/eyedropper/worker/loupe/result. `workbenches/mix/**`: ordered 2–12 color rack, two-or-more unequal palette sources, add/remove/reorder, method/leftover strategy, animation/result. Shared axis and palette promotion are consumed from W21/W23.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §5.2 and §7 Extract/Mix`.
- **Plan:** image-first develop sequence; keyboard/numeric sampler; ordered N-operand convergence trough; operand-derived tracks; no filler; explicit provenance/save; migrate both Mix add-slot species to named seats around P051's face; expose every Mix result color as an ordered named list/text item beside an inert face; label the result action by object truth (`Copy color` or `Copy palette`) with a durable announced result; retain Colors/Palettes as the third earned P092 source-mode selector with no local fill/geometry correction.
- **Implementation owner:** independent Extract and Mix feature leads; disjoint worktrees may run in parallel, integration only at shared axes/palette types.

## Current RED

Extract uses an underdeveloped flat control plate and pointer-first eyedropper. Mix's live 2–12 color and plural-palette capabilities are hidden behind superfluous shadow palettes/unequal species, and animation can obscure object relationships.

## Completion evidence

Extract empty→processing→result is honest, image dominates, axes adopt W21, sampler x/y has full parity and Save preserves provenance. Mix preserves Colors/Palettes modes through P092; its indicator matches within 0.5px across orientation/viewport/direction, focus is separate and no local fill/descendant rule remains. Mix proves 2/3/12 colors and unequal palettes, add/remove/reorder/leftover/provenance/no-filler/reduced-motion. Both add slots become named seats; WatercolorDot has no activation/focus/drag/tag state. Mix result faces are inert but every ordered color is exposed through named list/text semantics rather than `title`; its sole copy action says `Copy color` for scalar output and `Copy palette` for palette output, and announces the matching durable result. Both use one action grammar.

## π / DELTA

- π: Extract states/eyedropper; Mix 2/3/12 colors, unequal palettes, missing/result/reduced motion plus Colors/Palettes P092 selected/focused across orientation/direction at 1440/390.
- DELTA: stage shares, request/sample/order/provenance/strategy, Mix P092 four-edge error/fill/focus/override count, named-seat targets and WatercolorDot activation/focus/drag-host count 0; result list item/text count equals output color count; copy accessible-name/result identity follows scalar versus palette.

## Disjointness, artifacts, commit

Extract and Mix commit independently. Neither edits shared palette, axis or shell code; shared failures reopen W21/W23 rather than creating local variants.

## Archaeology

Builds T-13/T-19/T-41/T-44 and restores Extract's coherent treatment without restoring legacy code or deleting live N-ary Mix capability.
