> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W0 — Prompt and Product-State Atlas

**State:** PLANNED · **Meta-tranche:** V.R Research · **Depends on:** —

## Goal

Revalidate the formation-time semantic request and proportion registers against the execution base, attach a real UI/state atlas and exact current coordinates, and expand only when the live product exposes a genuinely new instance or mechanism. This is the scope boundary for all later waves, not a status ceremony.

**V-A160 prompt-census amendment:** include the substantive Glass session `e79fce3f-d24e-4654-8b27-d029653fedbe` (`2026-07-16T04:33:39.961Z…2026-07-16T16:08:26.385Z`) in the normalized, mechanism-deduplicated source set. Retain its proportional-card/spacing, gate-deletion, semver/publication and re-audit clauses; exclude workflow wrappers, status/capacity messages and task metadata. Born RED until the recap and request registry carry those clauses to W17/W33 and the named UI/API waves.

## Scope and file bounds

- Read: tranche A–U prompt/coverage/final documents, current V campaign, recent Claude Code user messages, live demo routes.
- Write during execution: `docs/tranches/V/research/state-atlas.md`; revalidated source/frame coordinates and any explicit appended instance/family in the existing `docs/tranches/V/research/proportion-register.md`; `docs/tranches/V/audit/request-registry.md`; minimal baseline frames under `docs/tranches/V/audit/pi/baseline/`.
- No product source, package, test, or sibling edits.

## Triumvirate and worktree

- **Specification:** `V.md`, `PROMPT-RECAP.md`, `VISUAL-CONSTITUTION.md`, `PROPORTION-AUDIT.md`, `OPTICAL-BENCH-COMPOSITIONS.md`.
- **Plan:** inspect the closed eleven-member inventory, `ADMIN5`, Account and storage recovery at 1440 and 390, light/dark where material differs; revalidate every stable site in the formation proportion register, attach current execution-base source coordinates/frames, append newly found instances to their mechanism family, and create a new row only for a genuinely new mechanism with one terminal verb and owner before W18. W5's later move manifest carries the stable semantic IDs to destination paths. Normalize prompts by mechanism and exclude meta commands. Recapture the supplied Lab/Blob crop at the live Picker route and the P019 floor/fluid/ceiling fixture; measure label/readout/Blob/card rects, `I_before` at every arm/frame, and Blob footprint center in old-Card coordinates so PR-01…03 enter execution as tracked current RED rather than an attachment.
- **Implementation owner:** one research lead plus independent prompt and visual readers; primary worktree, read-only Browser-first inspection.
- **Execution:** open, inspect and capture every baseline visual π in the live routed in-app Browser before recording coordinates. Computer Use/headed hardware is limited to a named Browser-inaccessible native/OS/device/Safari/GPU surface. Standalone/headless Playwright is allowed only for named cross-engine binary automation, exhaustive repetition or synthetic timing that the in-app Browser cannot express, and it cannot establish the baseline gestalt.

## Current RED

Prompt truth is split across K/N/T/U ledgers and recent sessions; U labels live UI rows relay/attest/complete-with-misses; there is no compact tracked current-state atlas spanning empty, loading, error, populated, auth, admin, mobile, and direct-route states. Formation has decided the exhaustive semantic families in `research/proportion-register.md`, but their exact current source coordinates and tracked execution-base frames remain RED. The 2026-07-15 Lab crop already proves an oversized label→headline void, a competing label rung and a timid painted Blob inside an over-reserved title row, but that evidence is not yet recaptured in the tracked route baseline.

## Completion evidence

- Every K/N/T/recent substantive row maps to a terminal status and W0–W33 owner.
- Every view/state—including hash/query/debug reachability such as `debug=1`—has a current frame or an explicit “not reachable” defect row.
- Every formation register site is observed on the execution base and carries a tracked witness plus current source coordinate; every newly found instance is folded into its existing family, while a genuinely new mechanism receives a terminal verb, one accountable W18–W29 primary, exhaustive sites and π/DELTA before W18. W5's move manifest preserves those semantic site IDs; no formation row is silently deleted or weakened. PR-01…03 include the crop, old rect, type/gap, rendered `I_before` at P019 floor/fluid/ceiling and 1440/390, chassis-local Blob footprint-center coordinate and paint protocol. W0 freezes exact seed bytes, canonical CSS color, complete effective HeroBlob config values plus canonical digest per reference viewport, and producer `settled` frame coordinate; it records rendered `b₀` in both engines/DPRs. Nominal 0.52 remains source provenance. W18 matches the old outer rect and moves the footprint exactly one title-gap chassis-ward; W20 locks it; W29 names exactly one intentional paint-config delta from the frozen manifest.
- Duplicate wording shares one mechanism family; workflow starts, watchdogs, compactions, and status prompts are absent.
- A fresh reader can select any later wave and find its current RED witness without reading an older tranche narrative.

## π / DELTA

- π: the minimum tracked execution-base atlas at 1440/390 for every one of the eighteen decided compositions, sharing a frame only when the same rendered state genuinely witnesses more than one register family; the supplied Lab relation is recaptured at the live Picker and at P019 floor/fluid/ceiling with its seed/config/settled coordinates rather than copied as an unattached image.
- DELTA: baseline-only measurements—route/state reachability, protagonist/support/Card/main counts, label/headline line gaps and `I_before` at every required arm/frame, computed type rungs, old outer/chassis/seat/Blob rects, frozen config digest and measured `b₀`. W0 claims no improvement; these coordinates are the falsifiable RED side consumed by W18–W32.

## Disjointness, artifacts, commit

W0 owns only research docs/baseline frames. It does not edit the visual constitution or product. Commit as `docs(v-w0): revalidate prompt, proportion, and product-state atlas` with a body naming source corpus, register changes and excluded meta commands.

## Archaeology

Consumes K `request-coverage.md`, N `prompt-coverage.md`, T-1…T-61, U-F1…F77, V handoff, and the two recent Claude project logs. Supersedes none of their historical text; supersedes their current status.
