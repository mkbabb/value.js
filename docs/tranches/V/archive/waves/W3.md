> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W3 — Post-BI Glass Reconciliation and Ask Decision

**State:** PLANNED · **Meta-tranche:** V.R Research · **Depends on:** —

**V-A130 current-stage override:** W3 is a historical decision census, not a live producer/rehearsal step. Value4 and Keyframes6 are immutable, and U-F77/B1/B14 plus residual Consumer-Cut/W2 producer-order language is retired. W3 creates no bootstrap, `K_NEXT` rehearsal or `G_NEXT` candidate; W17/W33 own only the remaining Glass strict/native/release cut, stale-canon cleanup and downstream refresh.

## Goal

Bind every Glass producer need to the immediate post-BI owner that must land it, decide its exact preserve/add/retire disposition, and correct the downstream consumer census without claiming artifacts that do not exist. The former Value4 bootstrap→Keyframes `K_NEXT`→Glass `G_NEXT` rehearsal chain is historical transport context.

## Scope and file bounds

- Read: completed `../glass-ui/docs/tranches/BI/**`, immutable v6 source/exports, the live post-BI producer head, value.js imports/package install, and keyframes floors.
- Write: `coordination/CONSTELLATION.md`, the additive BI inbox note, and W3 reconciliation report.
- No producer or value.js product source.

## Triumvirate and worktree

- **Specification:** `coordination/CONSTELLATION.md`, `CONSUMER-CUT.md`.
- **Plan:** verify every ask against its decided preserve/add/retire/value-owned disposition; freeze immutable v6, re-read the live post-BI producer head, enumerate removed component consumers and the required producer cut bundle, and verify the complete active/archive/dirty consumer manifest rather than inferring demand from the three-package core. Reconcile the exact direct handoff sent to active task `019f6610-6022-7df1-95b3-472b17a64656`: credit disk-present source/tests as **LANDED**, name every remaining obligation **RED**, and keep P004/P061/P128's retired machinery retired. W3 creates no candidate, rehearsal pack, compile inventory or final coordinate; the former bootstrap/Keyframes rehearsal wording is historical, while W17/W33 own the current Glass-only cut.
- **Implementation owner:** constellation lead; read-only sibling inspection only.

## Current RED

The installed glass package points to `.claude/worktrees/glass-ui-pinned`; immutable v6 lacks the V-required P019/P122/P047/P051 breaks and has names the older pin does not expose; earlier “none external” claims miss real value consumers. The live Glass tree now substantively carries P019/P051/P092/P122, P122's universal-minimum deletion and the P047 mass/origin seams, but P047 lacks the complete painted-component protocol, the Search story renders planning vocabulary, and native P019/P051/P092/P122 plus P046/P047 evidence remains open. These are present LANDED/RED facts, not a `G_NEXT` artifact claim. Historical Blob canon still carries the retired single-WebGL2 arm beside P047's dual-engine contract.

## Completion evidence

- The inbox note names P109, P017/P063, P038/P041, the P043/P045/P054 lifecycle split, P046/P047 scene facades, the P019/P010/P127 font/package split, and the exact per-site P091/P103/P104/P106/P107/P034/P036/P040/P041 consumer migrations. Its post-BI V-CUT core is executable rather than referential: P019 has the three-arm `1/√φ` pair, P122 has the exact title gap/proportions/narrow order/boundary+reserve absence contract, P047 has the dual-engine/DPR alpha-component protocol and origin seam, P051 removes the interactive host, and P092 supplies one rect-matched fill for exactly three earned value consumers.
- The exact active-session relay in `coordination/CONSTELLATION.md` is present in the Glass task/inbox flow without a new tranche or gate. W3's table distinguishes disk-present **LANDED** source/tests from still-**RED** implementation or evidence and records an exact decision/owner for each ask. **RE-DEADLINED**, pending, relay-only and status-only rows remain RED. Their redness blocks the matching W17 adoption and W33 release claim, not completion of W3's decision/status census; W33 alone may mark **SHIPPED** from the observed `G_NEXT` coordinate.
- P019 owns the product font surface, P010 semantic projection and P127 packed asset/URL/dependency truth. Direct handoff replaces P004. Native review belongs to each product owner. P004's registry, P061's runner and P128's generator remain absent rather than being renamed.
- The report records the universal P122 minimum as LANDED only when absent on disk, and records P047's full seed/color/config/frame, DPR 1/2, dual-engine origin-component measurement plus Search's removal of `wired|proof|consumer|triage` as LANDED or RED from present evidence. A required Browser-unavailable visual row remains RED; it is neither re-deadlined nor credited with Playwright evidence.
- **HISTORICAL (V-A123):** The former future Glass compile/adoption inventory named W17 consuming the sole W7 bootstrap and producing `K_NEXT`; those producer steps are now discharged by immutable Value4/Keyframes6. The live inventory is Glass strict/native validation and fresh Glass7 publication from the already-proven registry graph, followed by downstream consumer refresh. The pin cannot mask that current cut.
- The required post-BI V-CUT producer bundle and value-owned W17 adoption list are exact.
- **HISTORICAL (V-A123):** The direct immutable handoff and P127 receive the dependency amendment; the former release order Value `4.0.0` → Keyframes `K_NEXT` → Glass `G_NEXT` selected evidence-derived successors. Value4 and Keyframes6 have now shipped; only the fresh Glass7 candidate remains. Existing Glass `v6.0.0` remains immutable at peeled `e5b3a2095b6c3e330b5d82ca3330f1eac4e3c895`; the active dirty producer tree is re-censused and preserved, and the V producer bundle is an immediate post-BI cut, not a retag.
- `CONSUMER-CUT.md` names every tracked active first-party repository plus source/test/script/configuration consumer, including Slides and Words; records current Atlas `SourceDataBrowser.vue`, bbnf-lang Vite/test-results, Fourier version files, Muster evidence/docs, sci-report untracked research and Words primary/root-lock work; credits the landed sci-report relocation and clean Atlas motion file; retires the duplicate bbnf fork, `slides-K` clone and prototype archive; and assigns the post-Glass registry integration to W33. No generic “external consumers” remainder survives.

## π / DELTA

- π: record the exact future witness obligations—P019's floor/fluid/ceiling paired-type fixture; P122 at 1440, 390, 320 and 400% zoom; P047's named seed/color/config/settled-frame at DPR 1/2 in WebGPU and WebGL2; and the routed Search specimen—and attach only captures that actually exist. All visual inspection is native Browser-first, with no Playwright substitution for gestalt.
- DELTA: record exact present source/test/current-public-doc coordinates as LANDED and name missing packed declaration/import/external/profile changes, native captures and the eventual Glass7 release coordinates as RED. W17 closes the Glass strict/native handoff; W33 alone records the SHIPPED Glass artifact coordinates. W3 never predicts them.

## Disjointness, artifacts, commit

W3 owns coordination/decision docs only. The post-BI Glass executor edits producer specs/source; W17 owns the Glass strict/native handoff and the downstream value.js consumer refresh/routed mount after Glass7. Commit Value docs separately from the additive glass-ui inbox file; do not stage producer work.

## Archaeology

Replaces U's generic relay-close claim with immutable-v6 truth, retained P-coordinate producer ownership and an explicit post-BI Glass adoption.
