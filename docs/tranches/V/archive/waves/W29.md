> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W29 — Blob Material Studio and Configuration

**State:** LANDED/IMPLEMENTED SLICES · UNCOMPLETED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W17–W21, W28

The sole `.33→.325` calibration slice is implemented, but the wave remains RED for W17's producer baseline, routed 0px geometry witness, π/DELTA manifest and final Browser gestalt evidence. No terminal or SHIPPED credit is implied.

## Goal

Make Blob a crisp, center-ward material instrument whose morphology and fission can be understood and tuned without a 31-slider settings dashboard.

## Scope and file bounds

Blob renderer/lifecycle adapter, HeroBlob seat, material/morphology/fission model, persistent preview, essential controls, advanced grouped disclosure, reset/compare, scroll-confined inspector, `/blob-config` adoption and shared axes.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §3.2/§7`, `PROPORTION-AUDIT.md` PR-03/PR-10, `OPTICAL-BENCH-COMPOSITIONS.md §3 Blob`, B-05/B-09, BI P043/P045/P047/P054/P122.
- **Plan:** consume W17's immutable `docs/tranches/V/audit/pi/w17/p047-producer-baseline/manifest.json` and P047's dual-engine/fixed-footprint seam; project shared lifecycle; preserve one color atom; compose a separately scaled workbench; change only `BlobConfig.geometry.bodyRadius`—or P047's one explicitly named clean-major successor—from the frozen `0.26` baseline to exactly `0.325` in W0's effective-config manifest and grow Picker's measured `b₀` to `0.66 ±0.015` inside W20's footprint (`2r=0.52` is baseline provenance and `2r=0.65` is implementation description, never rendered proof); keep all other seed/color/config/frame bytes identical; record the final four-case verdict at `docs/tranches/V/audit/pi/w29/picker-painted-component/manifest.json`; permit no `heroScale`/second radius/size alias; map retained parameters to effects and retire disproportionate knobs.
- **Implementation owner:** Blob material lead with morphology, configuration and real-GPU collaborators.

## Current RED

The bounded Picker calibration slice is implemented: HeroBlob's sole effective `geometry.bodyRadius` is `0.325`, its explanatory nominal geometry is `0.65`, the exact-config test is updated, focused Vitest is 3/3 and targeted ESLint is green. P047's raw buffers measure area-equivalent diameter `0.660192…0.660415`, centroid error `≤1.342%`, hull inset exactly `12.0%`, antialiased edge `≤1.885 CSS px` at DPR1 and `≤0.948 CSS px` at DPR2, with equal WebGPU/WebGL2 alpha area. The wave remains RED until W17 supplies the immutable before coordinate and the routed Browser proves 0px chassis/seat/footprint change; the dedicated workbench, lifecycle/configuration reduction and final product evidence are also unbuilt.

**V-A120 visibility correction:** the raw-buffer ratio is not a composed-route witness. W29's routed evidence must show the connected Blob component visibly painted in screen space, with bounds, compositing/occlusion state and its relation to the headline→rail interval; if no visible component occupies that interval, the wave remains RED even when alpha-buffer numbers pass. Preserve one `.325` producer radius, fixed chassis/seat/footprint and noninteractive semantics; no second radius, reserve, cover, z-index or canvas fallback.

## Completion evidence

P047's WebGPU-preferred/WebGL2-equivalent contract is the sole engine contract; the historical single-WebGL2/WGSL-drain arm is absent from current code and canon. Value consumes the facade without a renderer fallback. The W17 producer-baseline manifest is the immutable before coordinate: its four WebGPU/WebGL2 × DPR1/2 rows share the exact seed/color/config/settled-frame protocol and freeze `b₀`. W29 alone changes the named `bodyRadius` field from `0.26` to `0.325` and writes `docs/tranches/V/audit/pi/w29/picker-painted-component/manifest.json`, whose corresponding four rows prove every other frozen byte unchanged. In Picker, W20's already-settled inline footprint/chassis/readout/rail rects and one-title-gap chassis-local placement grow/move by 0px while the connected central `alpha≥0.5` component reaches area-equivalent diameter `0.660192…0.660415` of the smaller footprint dimension—inside the `0.66 ±0.015` target—at the fixed settled reference in both engines and DPR 1/2. After that final paint change, alpha-centroid error is `≤1.342%` on each axis, hull inset is exactly `12.0%` of the shorter footprint dimension, antialiased edge width is `≤1.885 CSS px` at DPR1 and `≤0.948 CSS px` at DPR2, WebGPU/WebGL2 alpha areas are equal, and the readout stays collision-free. The routed Browser must reproduce those buffer-derived relations and 0px geometry; source radius and a nominal token do not close them. Blob remains a noninteractive specimen there. In the dedicated Blob workbench, the canvas instead fills the exact `preview-dominant` 66.6666667% stage across its viewport/container matrix; no Picker diameter is reused. DPR edges remain crisp. Settled/fission seams are observable and satellites detach as calm visible meatballs. Persistent preview dominates; essentials stay compact; advanced groups disclose into a scroll-confined inspector with reset/compare. Every retained axis adopts W21 and visibly affects a named morphology variable; actions consume W17/W18 vocabulary. Retired controls/code are absent. `/blob-config` is runtime-free. P043/P045/P054 distinguish fetching/active/parked/unsupported/failed with retry; permanent CSS remains complete without implying renderer activity. Ambient motion stops within five seconds or exposes the persistent announced still/pause contract.

**V-A167 before-coordinate correction:** `.325` landed before the baseline manifest. W29 may pair its after rows only with W17's immutable, hash-identified pre-change `.26` tree/config packet. It must not toggle the current dirty tree, synthesize a before frame or relabel the current state as `b₀`.

V-A120 is part of the final painted-component proof: the connected Blob must be visibly composited in routed screen space, with bounds and occlusion relation to the headline→rail interval recorded; if the interval is intentionally collapsed to the shared rhythm, that collapse is named and measured. Buffer-only geometry never closes W29.

## π / DELTA

- π: `audit/pi/w29/picker-painted-component/manifest.json` is the sole shared routed Blob packet: its four-row after record is paired by coordinates to W17's immutable producer baseline; W20 writes the topology/interval/occlusion rows and W29 writes the renderer/material rows. It also carries the Picker Lab crop before/after and separately named container-scaled Blob-workbench emerge/settled/fission, essentials, each advanced group and reset/compare at 1440/390, light/dark, both real engines/DPRs and reduced motion.
- DELTA: sole effective `bodyRadius 0.26→0.325`; Picker central-alpha-component `b₀→0.660192…0.660415` under W0's frozen seed/color/frame; W20 chassis/footprint/seat growth or displacement 0px; final centroid error `≤1.342%`, hull inset `12.0%`, antialiased edge `≤1.885 CSS px` DPR1/`≤0.948 CSS px` DPR2, WebGPU/WebGL2 alpha-area delta 0, specimen activation/focus listener count 0 and collision 0; workbench preview resize; fission 0.16–0.32, satellites, control→morphology and config/render graph.

- **V-A120 DELTA:** the sole manifest receives W20's screen-space bounds, compositing/occlusion and headline→rail occupation or named-rhythm collapse plus W29's raw-buffer, centroid, hull and 0px geometry fields; no second screenshot or packet is authoritative.

## Disjointness, artifacts, commit

W29 owns Blob only. W28 owns shared atoms/CSS ground; W31 owns final scheduling. Commit renderer seam, material/morphology and configuration independently; no second atom or axis system.

## Archaeology

Builds T-30/T-49/T-60 and U-F5 while preventing the dense settings form from passing on preview area alone. Aurora-zone T-32 is owned by W28/P046.
