> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W14 — Catalog, Search, and Pagination

**State:** PLANNED · **Meta-tranche:** V.H Hardening · **Depends on:** W11, W13

## Goal

Make public discovery and owner inventory complete, private-safe, and continuable under real filters/sorts.

**V-A161 current RED:** color-name search must be one bounded normalized byte-prefix authority; the current ranked-plus-regex substring fallback is not admissible. Prove limits/cursor ordering through the W14 contract and route the same semantics to W16/W22.

## Scope and file bounds

Every `PALETTE-WIRE-CONTRACT.md §3` list repository/service/route, including grant history; approved ColorNameSlot/active TagDefinition indexes, normalized color/search indexes, rank snapshots, the exact `PALETTE-CURSOR-CONTRACT.md` AES-GCM codec, owner/admin inventories, query tests and explain plans; the first complete W11 transformer run into an unreachable fresh candidate and its canonical count/digest/reference verification.

## Triumvirate and worktree

- **Specification:** `PALETTE-DOMAIN.md §2–3/§7`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md §§1–4`, `PALETTE-CURSOR-CONTRACT.md`, `LEGACY-PALETTE-SOURCE-CONTRACT.md`.
- **Plan:** implement the exact Q-* schemas/item fields and no other query grammar; immutable retained rank rows per 24-hour epoch under the Domain's token/score/color-distance law; the one leased CatalogBuildState coalescing monotonic dirty revisions into an atomically published complete epoch within 60 seconds; key-versioned encrypted cursor binds route/defaulted query/subject/last-scanned tuple and epoch; 500-row scan continuation; exact 422 tamper/mismatch and 410 genuine expiry; access/active-release recheck against current handle/policy and every provenance hop; exact current source-release fork list/count from indexed edges; total disjoint owner inventory and bounded grant/admin views; run §7's closed nine-collection transformer twice against one frozen restricted input, then build catalog/rank only after the canonical target/count/reference equations pass.
- **Implementation owner:** query/index lead plus API format unit; seeded realistic dataset.

## Current RED

Filtered scans can advance beyond 500 misses then return no cursor with `hasMore=true`; the client stops. `/mine` hides trash. Popular ranking and mutable slugs do not provide a stable continuation identity.

## Completion evidence

- `Q-OWNER-RELEASES` is the sixteenth closed page grammar. Its cursor binds r50, owner `principalId`, `{paletteId}`, and descending `{releaseNo,releaseId}`; crossing r18/r50 or owners is 422, while unpublish/withdraw/trash cannot eject, repeat or skip unchanged owner rows.

- 500 nonmatches followed by a match returns either the match or a valid continuation; `hasMore` never lacks a cursor.
- Cursor query-identity bytes/digest, route-instance `target`, route-specific last-scanned `after` union, `issuedAt`/`expiresAt`, deterministic-CBOR encoding, AES-256-GCM envelope/key version/nonce/AAD, private Principal and rank epoch match `PALETTE-CURSOR-CONTRACT.md` byte-for-byte; the fixed per-Q goldens run in two processes. Count/score coordinates encode only as smallest deterministic-CBOR unsigned integers in `0..9_007_199_254_740_991`: the maximum round-trips exactly and the next integer is 422. Same Q-ID with different palette/release/principal target, tamper/cross-route/cross-subject/cross-sort/changed-default, and an expired noncatalog cursor are 422 without plaintext coordinates. An absent/expired catalog rank epoch is `410 rank_epoch_expired` with explicit restart. Key retirement waits through the last issuance/deactivation bound plus 24 hours and 15 seconds rather than pretending clients report retained references.
- Q-AUDIT's query identity has only absent-time and paired `{from,to}` arms. Its goldens prove `[from,to)` at both boundaries, accept exactly 366 days, reject one-sided/equal/reversed/longer/null/duplicate bounds before repository work, and make a cursor invalid when either canonical boundary changes; no independent `from?`/`to?` representation survives.
- Noncatalog pages are explicitly live keyset views: a row whose eligibility or sort coordinate changes between requests may move, enter or leave; a row unchanged on both cannot be skipped or repeated, and consumers de-duplicate by stable item ID. Mutation probes assert only that achievable guarantee, while retained catalog epochs remain immutable.
- Q-CATALOG applies the exact 1–8 Unicode token AND/final-prefix law, checked `4·rankVoteCount+7·rankForkCount` popular score, current-public fork snapshot and minimum-to-any-color fixed-point ΔE—not a centroid. Every count and score is a `JsonUInt`; an input aggregation or weighted result above `Number.MAX_SAFE_INTEGER` is `platform_consistency_fault` and cannot publish an epoch or cursor. `PaletteCard.rank` exposes epoch/time values; `palettePolicyRevision/catalogTier` come from the same current policy recheck and a tier filter uses that value. A tier change before rebuild cannot leak an old tier or change historical rank order. Detail counts expose an exact current read timestamp.
- A burst of catalog-affecting mutations increments the one dirty revision clock but creates no event rows and coalesces into the builder's next majority snapshot. The builder writes a fresh UUID candidate unreachable from first pages/cursors, verifies the complete key/count/rank digest, and one lease-epoch CAS publishes its pointer plus acknowledges exactly the captured revision. Crash before/after that CAS leaves respectively the old/new epoch authoritative; a mutation after the snapshot remains dirty and is included by the immediate successor. The 30-second per-build bound yields ≤60 seconds for every committed mutation without promising one epoch per mutation. Expiry is the row's exact `rankedAt+24h`, enforced by application time with TTL only reclaiming rows; clean state refreshes before the final hour. Old epochs remain immutable, and active-release mismatch skips rather than hydrates stale content.
- Catalog lag never leaks private/trashed/withdrawn rows: each candidate rechecks current access, and filtering still advances from the last scanned row. Non-owner catalog contains only visible releases; owner inventory is the total disjoint unpublished/published/trash classifier with visibility/moderation substate. `create→publish→unpublish→trash→restore` never loses or duplicates an item; device drafts remain client-local.
- Repeated Q-CATALOG `tagId` values are an AND over the active Release's frozen membership and every named TagDefinition's current active state. Public Q-NAMES-SEARCH and admin Q-PROPOSALS use the exact normalized UTF-8 byte-prefix interval `[p,prefixSuccessor(p))`; token/fuzzy alternates are absent. Vectors distinguish one-vs-all tag membership, retired definitions, prefix boundary behavior and the no-upper-bound successor edge.
- Source-release public fork list/count applies the destination's current public predicate independently, emits neither actor nor private target identity, and advances from the last scanned edge. Public provenance separately rechecks each ancestor; hidden/purged hops emit only opaque ordinal-unavailable steps. A private fork is absent, appears after Publish, and disappears after unpublish/trash/withdrawal while internal provenance remains.
- Color-name search and active tag facets use indexed normalized data with recorded `explain()`/latency at realistic cardinality; rejected/withdrawn names and retired tags cannot enter new search/filter results while historical Release labels remain readable.
- Public color-name results are driven only by currently approved occupied slots. Rejected/withdrawn attempts remain reviewable history but are absent from public search; a newly approved later attempt for the same normalized slot replaces no historical row and appears exactly once.
- Every Q-* query rejects unknown/duplicate scalars, enforces exact bounds/defaults/sorts/items and returns the invariant Page envelope; Q-GRANTS pages complete immutable revoked history. Explain/latency evidence covers realistic cardinality.
- The completed W11–W14 transformer runs twice into separately named, unreachable fresh candidates under `LEGACY-PALETTE-SOURCE-CONTRACT.md`. The restricted source/dispositions/provenance/rejects/target use frozen ordering/domain digests; the two logical outputs are byte-identical, each candidate re-export equals the restricted `cut-target.ndjson`, every §7 equation/reference traversal passes, and each closed reject family destroys the candidate. Row-level artifacts remain only in the encrypted versioned cut bundle; tracked evidence contains redacted aggregate counts/digests and no source-key digest, slug or target key.
- Seed truth contains no live private/unlisted/archived legacy Handle and no legacy Principal/session/vote/report/fork edge. Public catalog rows equal `|P+|`, recomputed vote/fork contribution is zero, approved legacy names and valid TagDefinitions appear exactly once, every retired claim resolves its same-type tombstone, and every claimable legacy public handle remains permanently unavailable. No old collection is dropped and no runtime dual read is mounted in this wave.

## Disjointness, artifacts, commit

W14 owns read models/indexes/cursors; W13 owns writes; W15 consumes formats. Commit index/data migration with explain evidence, then query/cursor behavior.

## Archaeology

Retires the pagination bug, consumerless `/mine`/restore split, and unlisted discovery ambiguity as one catalog mechanism.
