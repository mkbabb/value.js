> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W18 — Visual Constitution: Material, Type, and Proportion

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W0, W6, W17b

**V-A139 current RED:** `ColorNutritionLabel.vue` still has an empty Tooltip/false hover trigger, a static Alert and repeated dividers. W18 must delete those concrete paths and leave readable semantic conversion content before claiming its zero-Alert/zero-divider/zero-tooltip contract.

**V-A166 dependency correction:** W18 starts only after W6's physical feature topology and W17b's registry Glass7 routed mount. It never styles the transient `demo/@` tree or begins against the pinned Glass5 graph.

## Goal

Ratify and install the Optical Bench constitution across all eleven members and the seven operational compositions before individual workbenches refine themselves.

## Scope and file bounds

Foundation/type/material/proportion styles; the eleven member-specific compositions (Picker, About, Browse, Library/My Palettes, Generate, Extract, Mix, Gradient, Easing, Atmosphere, Blob); Users, Names, Audit, Flagged, Tags, Account and storage-recovery compositions; `InstrumentChassis` region mapping; closed role→type matrix and About measure; retained palette-Card tuple; Watercolor/pastel identity; empty grammar; dark scheme; and the rule that every corner/boundary element has a named semantic home or is removed. Feature-specific content remains W19–W29. No local generic chassis/Card housing recipe is created.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md`, `OPTICAL-BENCH-COMPOSITIONS.md`, `PROPORTION-AUDIT.md`.
- **Plan:** prototype low-fi/real-rendered pairs for all eighteen compositions. Map workbenches to P122's landmark-neutral regions/phases or a justified non-chassis structure and implement—not re-decide—the binding §5 inventory: every P122 workbench uses boundaries `[]`/reserve `none`; only the five Admin lists retain one adjacent-row separator and no terminal rule. Implement the binding PR-33–35 spacing matrix: P122 consumes the six normatively mapped measurements—outer stage+inspector inline/block inset, stage↔inspector gap, action-content inline/block inset and adjacent-action-child gap—without value-local overrides; every Browse/Library entity Card uses exact `{size:"sm", material:"content", tier:"quiet", surface:"opaque", shadow:false, grain:false, specular:"off"}` with invariant `C=--spacing(4)`, while only workspace structure owns a responsive gutter; About/Admin5/Account/recovery use the exact non-P122 outer/section/row/action rungs. Ratify W0; implement the closed type matrix: route H1/major argument `text-display`+Fraunces, instrument identity `--type-title`+Fraunces, palette identity `--type-subheading`+Fraunces, section heading `text-heading`+Plus Jakarta Sans, prose `text-prose`+Plus Jakarta Sans, controls/labels `text-small`+non-bold Plus Jakarta Sans, values/code/provenance `text-mono-small` or existing `mono-caption`+Fira Code; P019 is the sole paired-scale exception. Install one shared pastel-rainbow identity composition consumed only by the Dock's exact `Palettes` destination and the `Palettes` substring of Library's `My Palettes` heading; `My` and all other identity text remain neutral. Browse/Library retain exactly one noninteractive Card container per rendered palette entity and zero Cards for fields/lanes/empties/inspectors; no cartoon/grid/Card-selected variant survives. The Card/article root owns no activation, focus or selection state and has palette-Card `[inert]` count `0`, keeping its live child reachable. One native named `<button type="button" aria-pressed>` child spans specimen/identity and owns visible selection/focus; palette `aria-selected`/option/listbox fiction is absent, its pressed palette ID must equal selected inspector identity with at most one true, and the no-selection state has all false plus no inspector. Any reorder handle remains a separate named control. In About, cap prose at `66ch`, allowing only the named `Conversion paths` ordered graph and fenced code specimens to escape; remove the default Card, static-definition Alert, seven repeated dividers and empty Tooltip/hover trigger. For Picker, transpose the old Card to a chassis with exact 0px outer inline/block growth at each reference viewport, move the Blob footprint center from W0's old-Card coordinate toward chassis center by exactly one settled P122 title-gap (`±0.5px`) while retaining its header-band relation, then encode P019 `1/√φ`; W20/W29 own the later lock/paint.
- **Implementation owner:** frontend design lead plus type/material collaborators; frontend-design review is mandatory before integration.
- **Execution:** open, inspect and capture each affected live routed composition in the in-app Browser, make one bounded change, and reinspect it there before widening the slice. W19–W29 inherit this Browser-first loop. Computer Use/headed hardware is limited to named Browser-inaccessible native/OS/device/Safari/GPU surfaces. Standalone/headless Playwright is allowed only for named cross-engine binary automation, exhaustive repetition or synthetic timing that the in-app Browser cannot express, and it cannot author or attest visual gestalt.

## Current RED

Cards read flat cream/brown or uniformly muddy dark, vary their chrome and selection treatment, and share padding with responsive workspace structure; empty companion cards receive half the stage; every Admin route still receives an unrelated Palettes companion; nested glass/casters compete; display/body/mono jurisdictions and action/empty species vary by pane. Picker's Blob footprint forces a large title→readout void while its face remains visually small. About gives a static definition Alert weight, repeats seven dividers, leaves prose unbounded, and wraps conversion paths in cursor/Tooltip chrome whose Tooltip content is empty.

## Completion evidence

Every shared surface has one tier and one exact type jurisdiction; no seed-tinted chrome or unearned nested glass remains. The closed role matrix resolves with the specified family/rung in all four arms, P019 is its sole paired-scale exception, and About prose is `≤66ch` outside its two named wider example species. Contrast/state/focus are explicit. Eighteen pairs separately prove topology; no family substitute. The shared pastel identity has exactly two consumers: Dock `Palettes` and Library `My Palettes`'s `Palettes` substring. Both are legible and recognizably the same composition in light and dark; pastel/rainbow identity applications on every non-`Palettes` textual element equal zero. Active content dominates, empties collapse and Admin companion is 0. Browse/Library satisfy `Card count = rendered palette-entity count`; every entity reports the exact `sm/content/quiet/opaque/false/false/off` tuple, invariant `C=--spacing(4)` ladder, noninteractive Card-container/face selection delta `0`, palette-Card `[inert]` count `0`, and one native named pressed-button seat spanning specimen/identity, with zero field/lane/empty/inspector/nested/cartoon/grid/selected-variant Cards. Palette `aria-selected`, option/listbox/composite semantics and root activation/focus/selection state are absent; pointer, Enter, Space and AT produce the same pressed palette and inspector identity, at most one seat is true, and all seats are false when the inspector is absent. Any reorder handle remains a separate named control. Each other composition has Card count `0`. About is one semantic article with Card/static Alert/repeated divider/empty Tooltip/false-trigger counts all zero and its conversion order readable without hover. P122 owns landmark-neutral housing, exact golden/preview proportions, absent-inspector expansion and narrow order; the eight value workbenches pass exact boundaries `[]`/reserve `none`, the five Admin fields retain exactly their adjacent-row separators, and every other divider is absent. PR-33–35 close every repeated region/Card/non-P122 inset family at 1440/390/320/actual-400%-zoom: P122's outer stage+inspector inline/block inset, stage↔inspector gap, action-content inline/block inset and adjacent-action-child gap equal their six computed producer values, retained Cards preserve their exact tuple/five-rung ladder while only the workspace gutter responds, and the non-P122 surfaces match their exact outer/section/row/action matrix. Value adds no grid/min-block recipe, P122 interval override, Card-padding clone or collapsed-margin substitute. W18 does not claim final PR-01…03: it records baselines, proves Picker chassis growth `0px/0px`, establishes the exact one-title-gap chassis-ward footprint displacement, fixes ownership and hands locked topology to W20/W29. Every register row has terminal primary/sites; unexplained ornament is zero.

## π / DELTA

- π: eighteen named low-fi→real-render pairs plus the material/type board and full shell in light/dark at 1440/390; a family sheet labels and captures each distinct member independently. The board includes the two exact pastel identity coordinates together in each scheme, with surrounding neutral labels visible. The same ordinary record adds PR-33–35 data-tuple/rect/computed-style/type strips at 320 and actual 400% in-app Browser zoom; no new gate or capture system is created. Picker's pair includes the tracked Lab crop relation.
- DELTA: per composition, its binding protagonist/support relation, collapse/order, exact Card equation/tuple, §5 boundary/reserve/retained-line values, type-role map and landmark counts. Pastel identity consumers are exactly Dock `Palettes` + Library-heading `Palettes` = 2 and non-`Palettes` textual consumers = 0 in each scheme. The eight P122 workbenches use exact golden `61.8033989/38.1966011` or preview `66.6666667/33.3333333`, boundaries `[]` and reserve `none`; Browse/Library use selected protagonist `64…66.6666667`, complementary inspector `33.3333333…36` and one exact-tuple Card per rendered entity; About, Admin, Account and recovery prove their content-hug/full-width/overlay/inline laws rather than a false P122 share. At 1440/390/320/actual-400%-zoom PR-33 records the six normatively mapped P122 equalities within `±0.5 CSS px` and consumer overrides/copies `0`; PR-34 records the exact data tuple, invariant `C=--spacing(4)` five-rung Card ladder, responsive workspace gutter, Card/article-root activation `0`, native named button/type, palette `aria-selected`/option/listbox count `0`, pressed count `0|1`, pressed-ID↔selected-inspector-ID equality, no-selection inspector absence, Card/face selected delta `0`, seat selection/focus delta, pointer/Enter/Space/AT parity, separate optional handle and non-Card Card-padding use `0`; PR-35 records exact outer/section/Admin-row/action rungs with collapsed-margin and Card-padding use `0`. Foundation records chroma/contrast and the exact type matrix; About records `66ch`, its two wider example species, Card 1→0, static Alert 1→0, repeated dividers 7→0, empty Tooltip/false trigger/hover state 1→0 and semantic conversion order. The Admin lists record one adjacent-row separator and zero terminal rules; every other non-P122 divider is zero. Picker records transposition 0px/0px, footprint displacement one title-gap toward chassis center (`±0.5px`), baseline type/gap/`b₀` and owner handoff.

## Disjointness, artifacts, commit

W18 owns foundation and composition contracts only. Individual features consume but do not fork them. Commit route-composition ratification, material/type, then proportion/empty grammar with tracked π references in body.

## Archaeology

Decides U-F10/F12/F17, T-3/T-11/T-16/T-18/T-24/T-34/T-50/T-53/T-56 and the latest “suffusion plus proportion” edict.
