> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W11 — Palette Handles, Workspaces, Releases, Access, and Lifecycle

**State:** PLANNED · **Meta-tranche:** V.H Hardening · **Depends on:** W1, W7, W9, W10

## Goal

Land the Route-B palette ontology and make access/lifecycle policy a resource invariant.

## Scope and file bounds

Palette models/schemas/repositories/access policy and routes; NamespaceClaim/identity-tombstone/handle/workspace/release/content/policy/provenance collections; stable TagDefinition references; the exact nine-collection canonical source/disposition/provenance/reject/target transformer in `api/tools/data-cut-v4/**` with `api/test/data-cut-v4/**`; API tests. The application graph may not import the offline tool. History transaction details are W12; commands/social/name decisions are W13; full cut/reseed waits until W14, and W33 removes the one-shot tool after the verified cut.

## Triumvirate and worktree

- **Specification:** `PALETTE-DOMAIN.md`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`, `LEGACY-PALETTE-SOURCE-CONTRACT.md`.
- **Plan:** create fresh collections/IDs, including the one permanent palette-slug NamespaceClaim collection shared by create, rename, fork and cut; consume W10's single branded canonical-spelling/raw-path/claim port with no palette-local normalizer; implement `PALETTE-DOMAIN.md §Canonical palette content` and its sole per-digest ContentObject reference port, including r9 create and r11's exact Workspace→linked-Handle owner/lifecycle read, unconditional canonical byte comparison, A→B atomic counters and synchronous zero-delete before the actual `{id,paletteId,revision}` Workspace CAS; implement §7's fixed UUID/time/NDJSON/digest/reference-count laws byte-for-byte plus stable TagDefinitions and immutable release field authority; decode all nine legacy collections under the exact consumed/retired field and reject vocabulary; implement the Principal-aware access/lifecycle/moderation matrix; map only valid live public standard/featured legacy palettes to system-attributed read-only lineages, map every other palette to the restricted encrypted bundle plus identity tombstone/retired claim where claimable, reserve every claimable legacy public handle, and retire unlisted/archived/mutable-public/schema-v1/tag-cascade paths.
- **Implementation owner:** palette domain lead with schema, policy, and lifecycle units; one database-cut worktree.

## Current RED

Palette documents conflate private/public mutable state; visibility is not enforced by secondary reads; `unlisted` has no writer; delete is a timestamp update with non-idempotent side effects; schema accepts invalid color shapes. The current cut story neither names all nine collections nor prevents inaccessible private live rows, dangling retired claims, nondeterministic IDs/times/history ordering, lost legacy handle reservations or free-form tags with no target definition.

## Completion evidence

- The owner inventory and Handle resource expose one distinct r50 `Q-OWNER-RELEASES` link for active or trashed owner lineages; absent/system/foreign/purged identities conceal as 404. No public route gains owner semantics.

- Identical content can create two independent handles/workspaces/releases while sharing only an immutable content payload. Its exact Workspace/Release/Report counters share that digest document as the one write guard; every referent persists only through the reference port, and zero deletes synchronously with no queue, collector, scan or direct-writer alternate.
- Anonymous/non-owner detail, history, provenance, fork, vote and report obey the same `active ∧ public ∧ moderation-clear ∧ release-visible` predicate.
- Owner inventory can read active private and trashed resources without exposing them publicly.
- Owner trash records grace/purge clocks plus the literal owning ReceiptNamespace and creates one `(palette,handleId,causeReceiptNamespace,causeCommandId)` episode carrying the r15 receipt's values; restore returns active/private and cancels only that exact matching queued/leased episode while retaining it as terminal history. An active Handle therefore has zero schedulable jobs even when old cancelled rows remain, a later trash/Principal-close uses its own namespace/command cause, and a trashed Handle stores and matches exactly one current episode. Equal raw UUIDs under distinct owner/admin Principal namespaces remain distinct; no hash or command-only alias exists. Unpublish, admin withdrawal and owner trash are distinct. Every create/rename contends on one permanent `(palette-slug,normalizedSpelling)` claim; rename inserts the destination, retires the source and updates the Handle atomically. r9 and fork create their clear PalettePolicy with exact `"lineage created"` rationale and authenticated actor; cut-created clear policies alone omit both. Purge retires any still-active claim in the same transaction as Handle tombstone insertion/live removal and never deletes a claim. Historical identity is therefore never reused. Release tombstones retain private lineage coordinates but no content reference; Report target snapshots pin moderation evidence/ContentObjects. r9 acquires one Workspace reference, r11 A→B releases/acquires atomically, a same-digest edit is zero, and only an exact post-delta sum of zero deletes shared content.
- Workspace create, Handle rename, fork and canonical public path lookup pass the exact W10 namespace vector set byte-for-byte. Uppercase body input returns the lowercase canonical slug; Unicode/whitespace/reserved input is rejected; noncanonical or percent-encoded path forms never redirect or alias; a canonical retired spelling never resolves or reclaims.
- Independent vectors prove the frozen `/css`→fixed-point D65 OKLCH conversion, half-even quantization, alpha/missing/range policy, Unicode 15.1 name folding, ordered duplicates, tag sorting, exact RFC 8785 bytes/domain digest and byte-comparison collision failure. r11 explicitly covers same-digest/same-bytes with zero counter movement and forced same-digest/different-bytes with `content_digest_collision` plus zero Workspace/counter/AuditOutbox/AuditEvent write; no second collision-attempt logger is invoked. W11 makes no further canonicalization choice. TagDefinition IDs plus label-at-publish are immutable, and retired tags block new Publish without rewriting history.
- Every Publish mints its Release with immutable `publicHistory=true`; there is no request field or later toggle. While the Handle remains active/public and policy-clear, the first Release becomes historical after the second Publish and remains publicly listable/readable; owner inventory sees all releases regardless. Unpublish/trash/owner suspension hides the entire lineage, and an individual ReleasePolicy withdrawal hides only that release without mutating it.
- Workspace edits cannot leak into public fields before Publish; historical manifests never change; active/history/withdrawn access is demonstrated.
- Two isolated transformer runs over one fixed restricted source/manifest produce byte-identical restricted disposition/provenance/reject/target NDJSON, deterministic UUIDv5 identities/times and the same Kahn-ordered release numbers; changing a source byte changes the domain digest. The target canonical re-export equals the encrypted-bundle target bytes. Tracked evidence is only the redacted aggregate public manifest and contains no row, slug, source-key digest or target key.
- The exact nine-collection matrix is executable: valid public standard/featured palettes alone create live read-only Handles; private/unlisted/archived/deleted palettes create no live Handle/Workspace/Release/content/policy/catalog row; every claim resolves one live subject or matching tombstone; every claimable legacy public handle is retired against a principal tombstone; no legacy row creates a Principal, credential, session, grant, owner Workspace or invented recovery material.
- Public history requires an exact currentHash/version payload match, complete acyclic roots/depths, deterministic local parent order and one Release per source version. Current tags map through exactly one transformed TagDefinition; missing/invalid/duplicate-normalized tags and invalid/colliding approved color-name rows are deterministic whole-run rejects, not defaults. Legacy cross-lineage/social/counter semantics receive explicit archive/retire rows.
- The deterministic transformer **must be complete at wave close**; old collections are not dropped in this wave. Its restricted `cut-rejects.ndjson` is encrypted evidence only; no quarantine collection, source reader, alias or runtime dual path exists. Every field/reject/disposition is closed by `LEGACY-PALETTE-SOURCE-CONTRACT.md`; an observed unknown blocks rather than widening a permissive decoder.

## Disjointness, artifacts, commit

W11 owns core data shape/policy/lifecycle and the transformer. W12 owns clocks/history, W13 receipts/social/cascade, W14 catalog. Commit schema/transformer with manifest/digest/access evidence; the one clean cut is rehearsed after W14 and deployed only in W33.

## Archaeology

Supersedes the “tidy API exemplar” conclusion and the dormant schema-v1 migration park with one decided clean break.
