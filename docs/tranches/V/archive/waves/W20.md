> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W20 — Picker Hierarchy, Readout, and Selector

**State:** LANDED/IMPLEMENTED SLICES · UNCOMPLETED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W18, W19

The inert-Blob consumer slice is implemented, but the wave remains RED for the complete hierarchy/layout Browser evidence, π/DELTA manifest and routed gestalt close. No terminal or SHIPPED credit is implied.

## Goal

Make the selected color the unmistakable specimen and join identity, values, color-space choice, and rail in one proportionate vertical argument.

## Scope and file bounds

`demo/picker` shell/display/selector/header/readout/reservation/hero seat, channel-strip bottom annotation and its selected-color binding, corner element and production PointerDebugOverlay/injected logging branches; no slider/spectrum rendering internals (W21) and no renderer morphology (W29).

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §3.2/§7 Picker`, `PROPORTION-AUDIT.md` PR-01…03/PR-13, `OPTICAL-BENCH-COMPOSITIONS.md §3 Picker`, B-04/B-07/B-11/B-15/B-23/B-31.
- **Plan:** make the contiguous Fira Code values a read-only numeric headline and delete the `contenteditable` path; consume BI P019's family-neutral paired kicker/headline size+line-height tokens at exact `1/√φ`, applying value-owned Fraunces to the `--type-title` identity arm and BI Fira to the headline; consume P122 `--instrument-title-gap` as `G` and close `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px` at P019 floor/fluid/ceiling and the complete-route 1440/390/320/actual-400%-zoom in-app Browser arms; bind every bottom channel meter to the same selected-color state and formatted cell as the headline, retire static range copy and hover-only duplicate value, keep the persistent meter as ordinary `aria-live="off"` text, and put the same channel name/value/unit in the slider's `aria-valuetext`; delete Blob block reservation; lock W18's chassis and footprint center after its exact one-title-gap chassis-ward move and prove unchanged outer rect/readout clearance in those same four routed arms; make Blob inert and move Copy to the action region; preserve legal-width/alpha behavior. ColorSpaceSelector deletes `hide-indicator`, consumes the sole glass-ui `SelectItem` indicator and gutter, and adds no local pill, halo, pseudo-element or marker: exactly one visible marker agrees with the selected option and `aria-selected`; its named seat/marker has a nonzero monochrome/forced-colors selected delta, focus has an independent nonzero delta, and WatercolorDot face delta is `0`.
- **Implementation owner:** picker hierarchy lead with readout and selector units; feature-local worktree.

## Current RED

Only the inert-Blob/sole-ActionToolbar-Copy leaf is implemented; it does not close this wave. The Lab crop's void still has the two live causes: the legacy Picker `<Card>` housing plus Blob-derived `.title-row` height/reservation, and an unconditional two-line/bottom-aligned Lab readout whose unused first line becomes blank air above ordinary values. Label/headline compete and Blob is timid. W18 owns the one Card→InstrumentChassis transposition; W20 deletes the Blob-derived reservation and resolves the readout/type hierarchy without a local wrapper, Card shrink or persistent clearance reserve. Readout/rail, responsive rhythm through 320px and real 400% zoom, true pathological wrap, alpha, annotation, direct editing, duplicate selector-marker ownership and selected-option state remain unresolved.

**V-A120 composition correction:** the July 16 capture adds a distinct headline→instrument obligation. In the settled routed Picker, that interval must be occupied by the visibly composited noninteractive Blob/rail or collapse to the named shared section rhythm; a transparent-buffer measurement or an empty canvas rectangle is insufficient. Record screen-space bounds, paint/compositing and occlusion at every named viewport/clamp arm, while preserving W18's fixed chassis and W29's sole `.325` radius. Do not add a card-local reserve, opacity/z-index cover, second Blob or screenshot-only adjustment.

**V-A140 current RED:** `ColorSpaceSelector.vue:200–209` and `ColorComponentDisplay.vue:136–140` currently skip a type rung (`display2→display4`). W20 must consume one shared P019 adjacent paired clamp (`1/√φ`) for label/headline and may not mint a local rung.

## Completion evidence

Short/long/min/max/negative values never grow W18's chassis. Ordinary tuple is one line after P122 gap; only actual wrap adds a line below through feature-local content flow, while P122 reserve remains `none`. Alpha is conditional. At every P019 clamp arm and complete-route 1440px, 390px, 320px, and actual 400%-zoom in-app Browser arm, the exact line-box gap is `G` and rendered ink satisfies `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px`; the zoom witness uses actual browser zoom rather than viewport substitution. `CardTitle`/H3/contenteditable are replaced by one non-live labeled domain readout text; W21 fields announce/edit, so routine color motion does not chatter as heading/status. The named baseline state is selected `lab(92% 88.8 20 / 82.70%)`: its former bottom copy `(0% - 100%) | (-125 - 125) | (-125 - 125) | (0% - 100%)` becomes `92.0% | 88.8 | 20.0 | 82.70%`. Moving the `a` channel to `40.0` records selected-color `lab(92% 40 20 / 82.70%)` and meter transcript `92.0% | 88.8 | 20.0 | 82.70% → 92.0% | 40.0 | 20.0 | 82.70%`; the headline and `aria-valuetext="a axis 40.0"` agree in the same settled frame. Persistent meters have `aria-live="off"`, no channel meter/status role exists, and routine pointer motion produces no live-region event. Selector has one named persistent seat and exactly one producer `SelectItem` marker in its gutter; `hide-indicator` and local pill/halo/pseudo/marker paths are absent. Visible marker, selected option, and `aria-selected` agree; seat/marker selected delta is nonzero in monochrome and forced colors, focus delta is independently nonzero, and selected/unselected WatercolorDot face opacity/pixel delta is `0`. P019/P122 close type/gap. Picker Blob omits pressLabel/Tooltip/events; ActionToolbar Copy remains sole Copy. Its locked footprint center is exactly one settled title-gap (`±0.5px`) farther toward the chassis center than W0's old-Card coordinate while remaining in the header band; its pre-paint alpha centroid is within 5% of footprint center, hull inset is ≥12%, outer/chassis/seat rect movement is `0px`, and readout collision stays clear in all four routed arms. PointerDebugOverlay/debug/provider/log branches are absent. No duplicate editor/ornament survives.

P019/P122's settled producer contract is consumed and verified here; it is not a source/test or native close claim. W20 owns the composed headline→instrument interval topology and its screen-space paint/occlusion witness jointly with W29; W29 alone owns the renderer alpha/material metrics and sole-radius calibration. Neither wave may close on pre-paint placement or buffer-only geometry.

V-A120 is completion-bearing: the settled routed Picker's headline→next-instrument interval contains a visibly composited noninteractive Blob/rail, or intentionally collapses to the named shared rhythm; screen-space bounds, paint/compositing and occlusion are recorded at every viewport/clamp arm. An empty canvas rectangle or transparent-buffer alpha is insufficient.

## π / DELTA

- π: the shared `docs/tranches/V/audit/pi/w29/picker-painted-component/manifest.json` is the sole packet for the routed Blob witness. W20 contributes the before/after Lab crop at P019 floor/fluid/ceiling and on the complete route at 1440px, 390px, 320px, and actual 400% in-app Browser zoom; representative RGB/OKLCH values, alpha absent/present, selected/focused option in color/monochrome/forced-colors, and read-only headline→numeric editor→Copy journey. Its topology rows record the exact headline→rail interval bounds, occupation or named-rhythm collapse, paint/compositing/occlusion relation, and the baseline/static-copy→meter plus settled `a: 88.8→40.0` selected-color/meter/headline/`aria-valuetext` transcript; W29 appends renderer/material rows to the same coordinates.
- DELTA: exact type ratio and line-box `G`; measured `I_before`/`I_after` with `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px` on every clamp and routed arm; readout flow and P122 reserve `none`; CardTitle/H3/contenteditable/routine-live-region count →0 and one labeled non-live readout; static range captions 4→0, persistent named-color meters 0→4, meter `aria-live` exact `off`, and before→after content/slider `aria-valuetext` equality; selector `hide-indicator`/local marker/pill/halo/pseudo/unlabeled/hover-only paths 0, visible marker count 1, option/`aria-selected` equality, monochrome+forced-colors seat/marker selected delta >0, independent focus delta >0, and face-pixel delta 0; Blob chassis-local footprint displacement = one title-gap toward center (`±0.5px`), outer/chassis/seat movement 0px, local centroid error ≤5%, hull inset ≥12%, events 0 and readout collisions 0 across all four routed arms; Copy 2→1; debug branches 0.

- **V-A120 DELTA:** record W20's routed screen-space topology fields—Blob bounds, paint/compositing/occlusion relation, and headline→rail occupation or named-rhythm collapse—in the sole W29 `picker-painted-component` manifest alongside the existing 0px geometry, centroid and hull evidence; W29 owns the renderer/material fields, and W30 consumes that one packet.

## Disjointness, artifacts, commit

W20 owns hierarchy/display/selector and W21 owns the replacement numeric editors. They form one stacked atomic integration: W20's geometry slice opens first, W21 stacks its semantic fields/axes, and the read-only-headline deletion plus editors merge and close together. No branch or deploy exposes a read-only headline without its editors, or retains contenteditable beside them. W29 paints only after that joint close.

## Archaeology

Builds T-2/T-4/T-5/T-7/T-16/T-17/T-33/T-35/T-40/T-51/T-59 and U-F9/U-F16/U-F26 ordering consequences.
