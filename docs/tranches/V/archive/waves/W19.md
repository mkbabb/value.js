> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W19 — Shell, Dock, Header, Navigation, and Scene Continuity

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W17, W18

## Goal

Make navigation and scene change one stable glass-ui chassis: reserved dock, whole-header contraction, no route hole, no visual identity collapse.

## Scope and file bounds

`demo/app`, `demo/shell`, the closed eleven-member router (`/`, `/palettes`, `/browse`, `/extract`, `/mix`, `/generate`, `/gradient`, `/easing`, `/atmosphere`, `/blob`, `/about`), scene slot, Dock/action layers, deletion of the global narrow `PaneSegmentedControl` and legacy left/right view state, pane header/chassis, stable landmark/H1 and route-scene focus restoration, desktop/narrow/zoom/keyboard scroll ownership, shell styles and motion. Feature bodies are out of scope.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §3–5.1, §6–7`, `OPTICAL-BENCH-COMPOSITIONS.md §2/§5`; BI P031/P036/P038/P039/P041/P092/P109/P122.
- **Plan:** install the eleven exact route records and a first-class Dock affordance for each, placing Easing adjacent to Gradient and About in the quiet trailing information position; render the exact Dock `Palettes` label through W18's shared pastel-rainbow identity composition in both schemes while every other Dock label stays neutral; fixed reserved band; truthful mount terminal; one crossfade owner; like-to-like continuity; one stable shell H1/`<main>` with landmark-neutral route scenes; one atomic ready-incoming handoff that hides/inerts outgoing and activates incoming with no observable 0/2-subtree gap; binding focus/scroll/announcement matrix; delete the consumer-less global pane selector/split-view state while keeping Dock route choice; document scroll, logical direction and whole-header contraction.
- **Implementation owner:** shell/motion lead with Dock and header collaborators; one worktree because they share the scene box.

## Current RED

**V-A120 Dock affordance row:** the dim home mark, floating eye and unlabeled vertical ellipsis in the supplied Picker shell are not self-justifying glass ornament. W19 must make a site-level decision for each: retain one route-identifying control only (remove the redundant home mark when Picker text already routes); give `…` a persistent named menu purpose and explicit expanded state or remove it; give the eye a durable named on/off mode and state or remove it. Primary route labels remain perceptible in light and dark. Tooltips do not substitute for a visible purpose/state, and no duplicate action species survives.

Direct hash transitions can leave a one-icon visual dock; scene mount is rAF-delayed and slab-crossfaded; whole cards may shift; title scales inside an unshrunk header; edge rings clip. Legacy view schema still pairs unrelated left/right panes and drives a global mobile toggle despite V's single-sequence compositions. Current scenes/chassis can transiently expose competing mains/H1s/active accessibility subtrees.

## Completion evidence

Direct, Dock, Back/Forward, redirect, in-route update and overlay journeys follow §5.1; reversal never focuses an abandoned scene and history restores by stable identity. All eleven exact member paths resolve directly and through one Dock affordance, with `/about` and `/easing` independently titled/focused and no companion/query/redirect surrogate. The Dock `Palettes` label is one shared pastel-rainbow identity coordinate in light and dark; its computed composition matches W18, every other Dock label is neutral, and Dock-local pastel/rainbow identity count is exactly 1. The complete route Dock survives; dock expansion changes scene rect by 0px; header stuck state contracts title/padding/band; active/inactive faces have one owner; no blank/remount flash occurs; reduced motion reaches terminal geometry immediately. One stable shell main and H1 persist. Route scenes and every InstrumentChassis region are landmark-neutral. Once incoming is ready, one atomic commit changes H1/title, hides/inerts outgoing and activates incoming; no observer/microtask or first/middle/terminal/reversal snapshot sees zero or two active route subtrees. The global `PaneSegmentedControl`, its `:deep()` correction, companion labels and legacy split-view state are absent; earned tabs remain feature-owned P092 consumers. Narrow/zoom/software-keyboard modes use document scrolling with no nested focus trap or unreachable action. V-A120 is completion-bearing: exactly one route-identifying control remains; the ellipsis is absent or visibly named with explicit expanded state; the eye is absent or exposes a durable named on/off state; labels remain perceptible in both schemes; no tooltip substitutes for purpose/state.

## π / DELTA

- π: direct load and Dock arrival for all eleven member routes, explicitly including `/about` and `/easing`; Dock rest/active `Palettes` identity beside its neutral siblings in both schemes; A→B, rapid A→B→A reversal, dock expansion mid-transition, narrow single-sequence composition and scroll-header transition at 1440/390/reduced motion; first/middle/terminal frames plus active accessibility-tree snapshots.
- DELTA: route/Dock inventory 9→11 and `/about`/`/easing` reachability 0→1; Dock pastel identity `Palettes` count 1 and non-`Palettes` count 0 per scheme; scene `y/height` 0px; main/H1/active-route-subtree counts all 1 at every sampled frame; global pane control/view-state/descendant correction counts 1→0; header contraction; incoming first frame; interruption continuity; focus/scroll/title/status transcript; opacity/transform owner, ring inset, 60/120Hz pacing and reduced-motion terminal; V-A120 home/eye/ellipsis retain/remove outcomes, named menu/state attributes and zero duplicate action species.


## Disjointness, artifacts, commit

W19 owns the shell/scene box. W20/W22 may not edit it; W30 audits. Commit dock/header/scene slices only when all shared rect invariants hold.

## Archaeology

Builds T-1/T-14/T-31/T-36/T-37/T-42/T-48/T-52/T-57/T-58/T-61 and U-F4/F7/F9/F11/F13/F76.
