> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W30 — Keyboard, Mobile, Zoom, RTL, and Assistive Truth

**State:** PLANNED · **Meta-tranche:** V.A Adversarial Analysis · **Depends on:** W16, W19–W29

## Goal

Operate the complete product without a pointer and under the layout/modalities that prior viewport smoke did not exercise.

## Scope and file bounds

Cross-feature semantics/focus/headings/live regions/logical CSS/targets; 320px, 390px, actual 400% zoom, real mobile keyboard, RTL, forced colors, reduced motion/transparency; full palette role/failure journeys; tests and minimal fixes in owning features.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §5.1–5.2`, `EVIDENCE.md §5`, `research/STANDARDS.md`.
- **Plan:** journey matrix across every operation and shared mechanism, including direct and Dock access to the closed eleven-member inventory and explicit `/about`/`/easing` title/H1/focus/Back-Forward behavior; VoiceOver/Safari, NVDA/Chrome and touch-screen-reader transcripts; execute navigation/direction/axis/reorder rows plus hit probes; prove global pane state absent, P092 only at owner/Admin/Mix tabs, exhaustive P051 face/seat/list migration, read-only headline/one numeric editor/action-region Copy, and the closed type matrix/About `66ch` measure at 1440/390/320/actual-400%-zoom. Re-prove Browse/Library's exact Card tuple, invariant `C=--spacing(4)` ladder, responsive workspace gutter and one native named `<button type="button" aria-pressed>` specimen/identity seat per entity. Pointer, Enter, Space and AT must agree on its pressed palette and selected-inspector identity; at most one is pressed and all are false when the inspector is absent. The Card/article root is a noninteractive container with zero activation/focus/selection state and palette-Card `[inert]` count `0`, so its live child remains reachable; palette `aria-selected`/option/listbox fiction is absent, Card/face selected pixels stay unchanged, seat selection/focus stays visible, and any reorder handle is a separate named control. Separately, ColorSpaceSelector retains `role="option"`/`aria-selected`; prove `hide-indicator`/local marker paths absent, one producer `SelectItem` indicator/gutter, matching option/`aria-selected`, nonzero monochrome+forced-colors seat/marker selected delta, independent focus delta and face delta `0`. Keep one labeled non-live output with no CardTitle/H3/routine live announcement. Rerun W20's four-meter selected-color contract and exact Lab `G`/ink plus Blob footprint/collision relations on the complete route at all four arms after W21 integration, using the exact alpha-bearing baseline and `a:88.8→40.0` transcript, one shared formatter and non-live slider text; verify Mix's semantic result-color list and context-correct Copy label/result; fix at owner/BI primitive; no overlay or parallel path.
- **Implementation owner:** fresh accessibility/mobile lead with keyboard, RTL and real-device collaborators.
- **Execution:** begin every journey by opening, inspecting and capturing its live routed state in the in-app Browser, then reinspect after each owning-feature fix. Use Computer Use or headed devices only for native VoiceOver/NVDA, mobile keyboards, OS/browser chrome and other Browser-inaccessible surfaces. Standalone/headless Playwright is allowed only for named cross-engine binary automation, exhaustive repetition or synthetic timing that the in-app Browser cannot express, and it cannot replace the Browser-opened rendered or assistive witness.

## Current RED

Card disclosure/reorder, swatch hover actions, eyedropper, gradient stops, spectrum and the current contenteditable headline lack coherent keyboard semantics; route focus/heading order is weak; overflow/100dvh and physical directions fail zoom/keyboard/RTL; several targets are 20–32px; palette cross-role failures lack a final browser witness. V's target deletes the contenteditable/global-pane paths rather than testing them as permanent controls.

## Completion evidence

Keyboard-only completes all palette/workbench/overlay/recovery journeys against W16. All eleven member paths, including `/about` and `/easing`, are directly and navigationally reachable with distinct titles/H1s, stable Back/Forward identity and no companion/query alias. Navigation has one stable main/H1/active subtree at all frames. Every axis/reorder runs LTR/RTL. At 390/320/actual-400%-zoom/coarse, owner, Admin Names and Mix P092 tabs keep one fill within 0.5px, separate focus/whole labels and zero override; global pane state is absent. The 400% witness is the complete live route at actual in-app Browser zoom, not responsive emulation. All eighteen compositions resolve the closed type-role matrix in all four arms; About prose is `≤66ch` outside only its named Conversion-path graph and fenced-code examples. Browse/Library entity Cards report exact `sm/content/quiet/opaque/false/false/off`, invariant `C=--spacing(4)` ladder and responsive workspace gutter. Card/article roots are noninteractive containers with zero activation/focus/selection state and palette-Card `[inert]` count `0`; Card/face selected delta is `0`. Each entity has one native named button of exact `type="button"` spanning specimen/identity; it alone owns visible selection/focus, uses `aria-pressed` rather than `aria-selected`, and has no option/listbox/composite wrapper. Pointer, Enter, Space and AT yield the same pressed palette and selected-inspector identity; pressed count is `0|1`, and zero means the inspector is absent. Any reorder handle remains a separate named control. Every explicit WatercolorDot tag is gone: ornamental faces are aria-hidden, data-bearing static faces have enclosing named list/text semantics, and operable Dock/selector/eyedropper/Spectrum/channel/palette/Generate/Mix/Gradient seats own name/activation/selection/focus/drag. ColorSpaceSelector separately retains option semantics: it has no `hide-indicator` or local pill/halo/pseudo/marker and exactly one visible producer marker in the `SelectItem` gutter; it agrees with the `role="option"` item and `aria-selected`, its seat/marker selected delta is nonzero in monochrome and forced colors, its focus delta is independently nonzero, and selected/unselected face pixels are identical. One labeled non-live readout remains, CardTitle/H3/live-status roles are zero and routine color motion produces no live announcement. In selected `lab(92% 88.8 20 / 82.70%)`, exactly four persistent channel meters read `92.0% | 88.8 | 20.0 | 82.70%`; after the `a` editor moves to `40.0`, the same four read `92.0% | 40.0 | 20.0 | 82.70%`, the headline names the same selected color and the operable slider exposes exact `aria-valuetext="a axis 40.0"`. Each meter cell is the output of the same channel formatter as its headline cell, including the alpha percent, each persistent meter has exact `aria-live="off"`, no meter/status role exists and pointer, keyboard and AT journeys produce no routine live-region event. On complete-route 1440/390/320/actual-400%-zoom arms, Lab keeps exact `G` and the ink inequality, Blob retains the one-title-gap displacement with unchanged outer/chassis/seat rects, and readout collisions equal zero. Numeric fields own edit semantics and Copy appears once in actions. Mix exposes one ordered semantic item per result color and its Copy accessible name/result truthfully distinguishes color from palette. Assistive/forced-color transcripts, reflow, hit regions and durable renderer/API state pass.

## π / DELTA

- π: representative full journeys at 1440/390/320/actual-400%-zoom/RTL/forced colors/mobile keyboard plus palette failures; 400% is exercised on the complete live route through the in-app Browser. Include direct and Dock access for all eleven members with explicit `/about` and `/easing` route-transition AT snapshots; owner/Admin/Mix P092; exhaustive P051 manifest/representative seats; Browse/Library Card data-tuple/computed ladder plus pointer/Enter/Space/AT pressed-seat→selected-inspector and no-selection pairs; the `ALL18` type strip; ColorSpaceSelector option/marker/focus states; and readout→editor→Copy with the exact four-meter Lab baseline→`a:88.8→40.0` pointer/keyboard/AT transcript.
- DELTA: route/Dock inventory 11 and `/about`/`/easing` direct reachability 1 each; overflow 0; focus/return/AT transcript; main/H1/active-subtree counts 1; global pane/contenteditable counts 0; exact type-role equality and About measure/exceptions; retained Card tuple/ladder/gutter equality with Card/article-root activation/focus/state count 0, palette-Card `[inert]` count 0, Card/face selected delta 0, native named button/type one per entity, palette-seat `aria-selected`/option/listbox/composite count 0, pressed count `0|1`, pressed-ID↔selected-inspector-ID equality, no-selection inspector absence, seat selection/focus delta, pointer/Enter/Space/AT parity and separate optional reorder handle; Picker selector `hide-indicator`/local marker/pill/halo/pseudo counts 0, visible producer marker count 1, option/`aria-selected` equality, monochrome+forced-colors seat/marker selected delta >0, independent focus delta >0 and face-pixel delta 0; labeled non-live output 1 and CardTitle/H3 counts 0; selected-color meter count 4, exact baseline/changed strings, alpha formatter equality, meter `aria-live="off"` count 4, slider `aria-valuetext` equality and routine live-announcement count 0; Lab exact `G`/ink inequality and Blob displacement/unchanged rect/collision 0 at all four arms; P092 four-edge/fill/focus; all P051 face/seat/list semantics and activation/focus/drag-face count 0; Mix result semantic-item count equals color count and Copy label/result matches object kind; hit polygons; logical axes; pointer↔keyboard equality; durable state.

## Disjointness, artifacts, commit

W30 audits fresh, but fixes land in the owning feature file with one integration owner for shared semantics. Commit by mechanism, not viewport; a product fix restarts the relevant audit witness.

## Archaeology

Reopens U-F25–F27/F55–F58 only where complete operation remains red; print/PWA/i18n/long-session remain explicitly retired unless this wave exposes a product requirement.
