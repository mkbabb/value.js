> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W22 — Palette Library, Cards, Browse, Search, and States

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W15, W16, W18–W21

## Goal

Make palette discovery and selection read as a proportional specimen library with honest empty/loading/error/populated states.

**V-A141/V-A142/V-A158/V-A161/V-A163 current RED:** cards and swatches use one native seat/action path, empty grids hug content, color search uses the bounded prefix contract, and proposals expose no caller-controlled attribution. These rows remain Browser- and wire-evidence obligations.

## Scope and file bounds

`demo/palettes` browse/search/list/card/swatches/color-search/empty/loading/error state components and styles, including the retained Card tuple, native pressed-button selection seat, selected-inspector identity, and visible unsupported/corrupt-storage recovery composition that consumes W15 semantics. Mutations/history and Account are W23; admin is W24.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §7 Palette library/Account and storage recovery`, `OPTICAL-BENCH-COMPOSITIONS.md §4`, `PALETTE-DOMAIN.md §6`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`.
- **Plan:** make Browse and Library use a selected protagonist of `64%…66.6666667%` with the complementary inspector at `33.3333333%…36%`, expanding to 100% when no selection exists; render only the `Palettes` substring of the Library heading `My Palettes` through W18's shared pastel-rainbow identity composition in both schemes while `My` and all other Library/Browse text remain neutral; render every palette identity at `--type-subheading` + Fraunces. Make each palette a bounded entity article with exactly one Card shell using `{size:"sm", material:"content", tier:"quiet", surface:"opaque", shadow:false, grain:false, specular:"off"}` at every viewport. The Card/article root is a noninteractive container: it owns no activation, focus or selection state, and palette-Card `[inert]` count is `0` so its live child remains reachable. Its one selection control is a native named `<button type="button" aria-pressed="false|true">` spanning specimen/identity; it alone owns activation, visible selection and focus, with no palette `aria-selected`, `role="option"`, listbox or composite-selection fiction. `aria-pressed="true"` iff that button's palette ID equals the selected inspector identity, so no more than one rendered seat is true; clearing selection sets every seat false and removes the inspector. Pointer click, Enter and Space converge on the same selected palette/inspector identity, and the AT transcript announces the button's name and pressed state. An available reorder handle is a separate named sibling control. The Card's `C=--spacing(4)` five-rung ladder never responds; only the surrounding workspace gutter does. Thus `Card count = rendered palette-entity count`; result fields, lanes, empties, inspectors and Cards themselves contain zero additional Cards. Retire cartoon/grid/Card-level selection variants, root `role=article` click/press, inline expansion/rename/menu/transient feedback and hover-only card swatch actions; full detail, rename/lifecycle/export verbs and durable operation state live in W23's selected inspector. Keep the compact swatch strip inert data. Distinguish Device Draft/Workspace/Published/Trash; quiet system attribution; content-hug empties; the static three-face `EmptyPaletteMark`; one search/filter family; W21 hue/SV axes; and no filler. Under P051, `CurrentPaletteEditor` and any surviving selected-inspector swatch action use named enclosing buttons; ornamental dots are aria-hidden and the face owns no activation/focus/drag/tag state. Unsupported/corrupt storage replaces the Library body with diagnosis→preserve/export→confirmed reset, never an empty lie.
- **Implementation owner:** palette-library design lead with card and state/search units.

## Current RED

Empty My Palettes can consume half the scene; the same large watercolor trio repeats everywhere; shadow palettes impersonate content; retained Cards drift between size/material/chrome and encode selection themselves; card bodies combine click, drag, expansion, rename, menu, feedback and swatches; loading and empty are visually confounded. Corrupt/schema-v1 storage can silently reset and has no owned preservation/reset composition.

## Completion evidence

Device Drafts/unpublished Server Workspaces/Published/Trash/Browse lists show the right resource type, bounded ordered colors, content digest/provenance and active/retired tag projections through the exact wire page/detail items; the published bucket retains unpublic lineages with honest visibility/moderation, and no retired route/query field is called. The `My Palettes` heading has exactly one pastel-rainbow identity substring—`Palettes`—in each scheme, consumes W18's shared composition, leaves `My` neutral, and gives Library/Browse non-`Palettes` identity text a pastel/rainbow count of zero. Palette names resolve as `--type-subheading` + Fraunces. In selected Browse/Library states the protagonist stays within `64%…66.6666667%`, the complementary inspector stays within `33.3333333%…36%`, and an absent selection yields a 100% protagonist. Every state satisfies `Card count = rendered palette-entity count`; every entity Card reports exact `sm/content/quiet/opaque/false/false/off`, invariant `C=--spacing(4)` and its five computed rungs at 1440/390/320/actual-400%-zoom, while only the workspace gutter changes. Nested/field/lane/empty/inspector/cartoon/grid/Card-selected variants are zero. A system row renders one quiet “Legacy system” annotation and no fabricated identity/edit affordance. The noninteractive article/Card container owns zero activation, focus, accessible selection state and selected-state paint; palette-Card `[inert]` count is `0`, keeping its live child reachable. Its spanning selection seat is one native named button of exact `type="button"`; palette-seat `aria-selected`, option/listbox/composite roles and Card-root press handlers are zero. The button's `aria-pressed` is true iff its palette ID equals the selected inspector identity, pressed count is `0|1`, and pressed count `0` implies inspector absence. Its visible selection and focus deltas are nonzero while Card/face selected delta remains `0`. Pointer, Enter, Space and AT journeys produce and announce the same pressed palette/inspector identity. Its optional named grab handle alone owns reorder, and the selected inspector owns detail/rename/menu/actions/durable feedback. Expanded in-card detail and hover-only action panels are absent. Every surviving add/edit/copy swatch action has a named geometric seat; WatercolorDot has zero activation/focus/drag host while non-semantic hover paint may remain. Static ornamental marks are aria-hidden; static swatch data have enclosing text/list semantics. Primary empties content-hug; secondary empties collapse. Storage recovery stays in the sole main, preserves raw data before confirmed reset and has no schema-v1 compatibility read. Color search uses W21 axes. Loading is request-backed; cursor failures are typed; no filler appears.

The same storage-recovery composition is the outer state for an export seat when IndexedDB cannot create or update an ExportOperation. It displays the last durable operation if readable, offers storage retry/preservation, and never invents a persisted `storage_unavailable` failure row; W23 resumes only after storage becomes writable.

## π / DELTA

- π: empty/loading/error/populated, selected/unselected, and unsupported/corrupt-storage recovery before/after export/reset at 1440/390, both schemes; each scheme includes `My Palettes` beside representative neutral Library/Browse identities and its paired Dock witness from W19. Selected/unselected pairs include pointer click, Enter, Space and AT transcripts for the native pressed-button seat, its selected inspector and the separate optional reorder handle. Ordinary Card data-tuple/computed-style strips add 320px and actual 400% in-app Browser zoom without a new capture system.
- DELTA: Library pastel identity substring count 1 and non-`Palettes` textual count 0 per scheme, with shared-composition equality to W18/W19; palette identity role/family equality; selected protagonist `64%…66.6666667%`, complementary inspector `33.3333333%…36%`, unselected protagonist 100%, invitation/secondary area; `Card count = rendered palette-entity count`, exact tuple equality, invariant `C=--spacing(4)` five-rung ladder, responsive workspace gutter, and nested/field/lane/empty/inspector/cartoon/grid/Card-selected counts 0; Card/article-root activation/focus/accessibility-state count 0, Card/face selected delta 0, native named button/type count one per entity, palette-seat `aria-selected`/option/listbox/composite count 0, pressed count `0|1`, pressed-ID↔selected-inspector-ID equality, no-selection inspector absence, seat selection/focus delta and pointer/Enter/Space/AT parity; attribution/inferred identity/edit counts; recovery main/focus/status; article-root activation 1→0, one selection control, separate optional grab handle and card-body expand/rename/menu/transient-feedback/hover-action paths 5→0 with their exact selected-inspector coordinates; WatercolorDot activation/focus/drag host 0 and named surviving seat target/focus pixels; request timestamps, search cursor and specimen density.

## Disjointness, artifacts, commit

W22 owns read/select/state surfaces. W23 owns commands/history. Commit state grammar before card styling, then search/browse/card slices with real W16 data.

## Archaeology

Builds T-12/T-13/T-19/T-35/T-41/T-43/T-44/T-56 and the renewed palette visual audit.
