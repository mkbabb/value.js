> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W8 — Final Color/Value Objects and Browser Extraction

**State:** VALUE4 PRODUCER SHIPPED · HISTORICAL / NO LIVE WAVE DUTY · downstream adoption remains W17/W33-owned · **Meta-tranche:** V.H Hardening · **Depends on:** W2, W4

**V-A130 current-stage override:** The old color-SCC/classes/`ValueUnit` statements below are historical RED evidence. Those Value4 model and deletion duties are complete in the immutable producer; U-F77's and the residual Consumer-Cut producer-order tails are retired. W8 has no live implementation or publication duty; W17/W33 own the fresh Glass7 candidate, native Q003 closure, stale-canon cleanup and downstream refresh.

## Historical goal (implemented)

Replace the color subclass/registry cycle and DOM-tainted value core with one typed final-object model and a one-way dependency graph.

## Historical scope and file bounds

`src/units/**`, `src/units/color/**`, new internal foundation/color/value modules, current DOM resolution/cache modules as move/delete-only inputs, the obsolete `src/math.ts` full-element SVG helper as delete-only input, affected tests and value/keyframes consumer fixture. Parsing composition and easing catalog stay in W9.

## Historical triumvirate and worktree

- **Specification:** `ARCHITECTURE.md §2` and W2 typed prototype.
- **Plan:** implement the exact 17-row `ARCHITECTURE.md §2` `SpaceId→ChannelsBySpace` schema, `Result`/`AnyColor`, named result factories, `"none"` law, immutable final `Color<S>`, the one static 17×17 anchor dispatch and the closed thirteen-space `CssColorSpace`/`CssColor` boundary; retain factory/conversion/object-oracle coverage for all seventeen while requiring the four library-only spaces to convert explicitly before CSS serialization, with no general library-string serializer. Then implement the exact `convertColor`, `mixColors`, `mapColorToGamut`, actual-surface-and-ratio `safeAccentColor`, degree-domain `interpolateHue`, `toRgba8`, the four immutable `/value` types, its sole `isLayoutTrackingUnit` runtime export and `/quantize`'s two Result operations/three types over final `Color<"oklch">`. Freeze the current DOM-resolution/epoch/cache behavior for W17, then delete it from value rather than publishing `/browser`. Prove the final-value→kf-owned-slot projection; delete the unused `cubicBezierToSVG` implementation/tests rather than growing a presentation sampler; migrate one layer at a time and delete old classes/registries/raw helpers/direct conversion pairs.
- **Implementation owner:** color and value model units on one coordinated library worktree; integration owner controls shared types and the move-only Keyframes handoff.

## Current RED

**HISTORICAL RED:** A 14-module color SCC, late serializer/dispatch registration, 17 space classes, `[key:string]:any`, parent-barrel imports, `HTMLElement[]` and layout cache ownership inside `ValueUnit` remained in the pre-V4 tree.

## Completion evidence

- Runtime SCCs in the color/value layers are zero.
- One final immutable generic object represents exactly the 17 frozen space IDs with their exact tuple keys, physical coordinates, input/canonical units, required-channel factories, thirteen canonical CSS spellings, four rejected library-display-only forms, white-point/anchor pairs and cross-realm `"none"` sentinel. `SPACE_SCHEMA` and `CONVERSION_ANCHORS` both satisfy exhaustive `Record<SpaceId,…>` types; the four displays have no parser/serializer round trip, and the subclass/optional-channel/NaN-normalization paths are absent.
- Ordinary schema tests consume W2's isolated literal `ANCHOR_ORACLE`: every one of the 17 rows proves source→XYZ-D65 and XYZ-D65→source without importing production constants and carries its exact `bijective | hue-degenerate | lossy-kelvin` class. Named born-RED vectors prove RGB `/255`/`*255`, hue `/360`/canonical `*360`, OKLCH raw-0.5 versus CSS-percent-0.4, positive/negative below/equal/above ProPhoto transfer branches and frozen matrices/adaptation, Rec.2020 positive/zero/negative gamma-2.4 plus frozen matrices, all five powerless-hue thresholds with `"none"` only on the powerless arm, and Kelvin's independently literal 1000/1900/just-above-1900/6500/6600/just-above-6600/40000 forward XYZ values plus exhaustive integer argmin/lower tie/alpha/non-finite law. The 17×17 numeric matrix covers all 289 ordered conversions through the single anchor graph; deleting Jzazbz, ICtCp, Kelvin or any row, sharing an oracle constant with production, changing a Kelvin coefficient/branch, treating Rec.2020 as PQ/piecewise OETF or asserting a Kelvin bijection fails a named test rather than shrinking the target.
- Conversion and CSS-native serialization import downward and require no registration order. `rgb`/`oklch` and every other factory return typed Results; `convertColor` alone traverses the authoritative anchor graph, `mixColors` names its space, `mapColorToGamut` names sRGB/P3, `safeAccentColor` receives the actual painted `AnyColor` surface plus requested WCAG ratio, and `interpolateHue` consumes/returns degrees. Every one of the seventeen rows retains factory/object/bidirectional-oracle coverage. `/css` accepts and emits only `CssColor`; `hsv`, `kelvin`, `ictcp` and `jzazbz` must first convert explicitly to one of its thirteen spaces, and their library spellings are rejected. Missing channels never become zero/`NaN`; invalid operations never become identity/default output; no general library serializer or implicit output-space selection exists.
- Pure value/color entries import no DOM/layout/browser code; after W2 freezes the exact behavior and literal corpus, `convertToPixels`, `getComputedValue`, `getLayoutEpoch`, `bumpLayoutEpoch`, the resize listener and their DOM/cache modules/exports/tests are absent from the immutable Value4 result. **CURRENT:** W17 has installed and proved the sole immutable Keyframes6 destination; W33 is now downstream Glass7/native closure and registry refresh. W8 does not reopen either producer.
- `/color` is the only Color→byte authority: `toRgba8(color,{gamut:"clip"})` converts all 17 spaces through the anchor graph, clips only by explicit policy, applies encoded-sRGB transfer and ties-even `uint8` quantization with straight alpha. Independent boundary/tie/alpha/chromatic/achromatic/out-of-gamut vectors fail any alternate matrix, transfer, generic-round or palette-local path.
- `/value` exports exactly `isLayoutTrackingUnit` plus `CssScalar|CssCall|CssList|CssValue`; the objects are deeply immutable and no nominal constructor, registry, DOM/cache primitive or extra constant survives. `/quantize` exports exactly `quantizePixels|dominantColor` plus `QuantizeOptions|QuantizedColor|QuantizeIssue`; invalid dimensions/pixel length/options are failure Results, and each result color is final `Color<"oklch">` plus population. The image-extractor worker/session/composable handle the Result and explicitly serialize through `/css` only at presentation; CSS strings and raw RGB/OKLab/OKLCH tuple authorities are absent from quantize.
- `isLayoutTrackingUnit` returns true for the exact computed/percentage/viewport/container table in `ARCHITECTURE.md` and false for unitless/px/angle/time/font-relative controls; W2's Keyframes compile fixture can delete its WAAPI literal without losing its path-relative `%` exception. No public array or browser dependency accompanies the predicate.
- External vector/color tests and W2's in-repository Keyframes `InterpSlot` fixture remain correct. The numeric-slot workload remains zero-transient-allocation/no-GC after compile, while the unit/color/DOM workload stays within the explicit baseline allocation, GC-pause, retained-heap, median and p95 bounds; changing serialized CSS strings are never misreported as allocation-free. **CURRENT:** W7's first packed Value4 artifact and W17's packed Keyframes6 proof are immutable; remaining evidence is Glass-only.
- `cubicBezierToSVG` is absent from source, tests and packed declarations. D-GAP-6 is explicitly declined upstream; W17 owns the one presentation-local sampler.
- The subclass forest, old registry, raw OKLab/OKLCH helpers, public direct pairwise converters, background-lightness contrast proxy and nominal/identity fallbacks are deleted, not wrapped. W7's clean prepare/pack test proves none leaks through stale declarations or output.

## Disjointness, artifacts, commit

**HISTORICAL DISJOINTNESS:** W8 owned model/operations and deletion of value-side browser internals; W9 owned parsers; W7 closed exports; W17 owned the single Keyframes destination. **CURRENT:** those producer artifacts are immutable; only Glass7/native/Q003 and downstream registry/demo refresh remain. Do not ship a class or browser adapter.

## Archaeology

Credits U-F60/U-F72/U-F73 numeric soundness and U-F74 raw-gamut behavior; changes representation and dependency ownership, not the proven math.
