> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W5 — Physical Demo Topology

**State:** PLANNED · **Meta-tranche:** V.H Hardening · **Depends on:** W2, W4

## Goal

Make physical ownership—not aliases—the demo's architecture.

## Scope and file bounds

Move `demo/@/**` and `demo/color-picker/**` into the target `app/shell/color-session/picker/palettes/workbenches/scenes/platform/shared` tree; update `tsconfig.demo.json`, Vite resolution, tests, docs imports, and asset paths; delete project aliases and forwarding UI directories. Palette endpoint/DTO/cache code stays palette-owned; atmosphere renderer state stays atmosphere-owned behind one shell lifecycle adapter.

## Triumvirate and worktree

- **Specification:** `ARCHITECTURE.md §1` plus W2 manifest.
- **Plan:** atomic owner-by-owner moves with exact import rewrites; no alias window.
- **Implementation owner:** topology lead with disjoint feature move units; one coordinated worktree because path moves touch shared resolution.

## Current RED

`242/248` demo TS/Vue files live under `demo/@`; hundreds of project alias uses and duplicated TS/Vite maps remain; route leaves sit in a generic panes directory.

## Completion evidence

`demo/@` and every project alias/`@src` reference are absent; direct dev and production build load every route; demo typecheck resolves physical paths; no forwarding file preserves an old location. `shared` contains no color-session, palette transport, or atmosphere coordination dumping ground. Git history shows real moves where content is retained, not copy-plus-stale-file.

## Disjointness, artifacts, commit

W5 changes paths/imports only; W6 changes responsibilities/styles/content. Commit per large ownership slice if each slice builds, then a final alias deletion commit. Broad move commits require bodies with manifest and build evidence.

## Archaeology

Executes the repeated recursive-colocation ask and retires V OF-ε1/ε2 placement forks. U-F45/U-F47 are credited local fixes, not proof that the global topology is done.
