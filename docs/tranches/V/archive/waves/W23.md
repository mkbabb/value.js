> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W23 — Palette Lifecycle, Social Actions, History, and Inventory

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W15, W16, W18, W19, W22

## Goal

Expose the complete workspace/release lifecycle and social facilities through one truthful action grammar, without mixing owner state, immutable public history, or admin authority.

**V-A145/V-A146/V-A156/V-A162/V-A164 current RED:** session recovery, mandatory idempotency/header replay, active-public social access, strict Problem decoding and redacted fork/provenance semantics must be closed in the lifecycle client/server cut. No legacy route, caller attribution, raw lineage or malformed-error fallback survives.

## Scope and file bounds

Account modal composition with registration/recovery/credential-rotation/logout and public-handle UI; palette workspace editor and action instrument; rename/tag/reorder/save/publish/history/restore-release/fork/vote/report/trash/restore; JSON/CSS/Tailwind/SVG/PNG exports; owner inventory and dialogs/popovers. Public-handle regeneration is retired; storage recovery composition is W22 over W15 semantics; admin review is W24.

## Triumvirate and worktree

- **Specification:** `PALETTE-DOMAIN.md`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-EXPORT-CONTRACT.md`, `VISUAL-CONSTITUTION.md §5–5.2/§7`, `OPTICAL-BENCH-COMPOSITIONS.md §4 Account`.
- **Plan:** one modal side Dialog opened from the Dock for identity/session, signed-out registration/recovery, active credential rotation/logout and secret-retention handoff; it remains an overlay with no second main or nested Card/Dialog. Use one selected-object inspector/action set, persistent durable-outbox state, explicit Device Draft/Workspace/Release/Handle language, accessible reorder, direct BI Dialog/Drawer/Popover, plus P104 Tooltip for the slug explanation where this wave owns that site.
- **Implementation owner:** palette workflow lead with owner-history and social units; shared action state has one owner.
- **Execution:** inherit W18's live routed Browser-first inspect→change→reinspect loop for Account, history and Prepare→Download. Cross-engine binary equality may use standalone automation only where the in-app Browser cannot express it; that output never substitutes for Browser visual judgment.

## Current RED

CRUD/export feedback expires or reaches only console warnings, recovery/rotation has no complete product flow, history/revert can lie, reorder/expand is pointer-first, restore lacks inventory, vote/fork/report target identity is ambiguous, and dialogs use retired wrappers.

## Completion evidence

- History/restore journeys consume r50 for owner lineage history and r18 only for public browsing. Unpublish, moderation withdrawal and trash suppress public access without erasing the owner's immutable release list; restore selects from the r50 result under the existing workspace CAS law.

The Account control opens one modal side Dialog whose desktop/mobile order and focus behavior match `OPTICAL-BENCH-COMPOSITIONS.md`; it never becomes a route chassis, Drawer, nested Card/Dialog or second main. Signed-out identity presents registration/recovery, active identity presents credential rotation/logout, and a durable operation result stays in that surface. The recovery-secret handoff precedes network mutation and temporarily replaces, rather than stacks beside, the action region. Registration/rotation generates the canonical `rs1_` value, presents it in a selectable accessible field with copy and manual-retention paths, reports copy failure without claiming retention, and enables commit only after explicit “I retained this recovery secret” confirmation. Plaintext stays memory-only through terminal/replay settlement and is then cleared; reload requires re-entry, screenshot/log/status evidence redacts it, and rotation leaves the old secret valid until the confirmed replacement transaction commits.

Every account/owner/social BUILD or REPLACE target terminally named in `PALETTE-FACILITIES.md` has a real browser consumer and every RETIRE spelling is absent; W23 does not wait for W1 to choose product scope. Its owner-state SegmentedTabs fixture consumes P092's single filled indicator: at each settled desktop/mobile, horizontal/vertical and LTR/RTL state the indicator's four edges match the active tab inner rect within 0.5 CSS px, focus remains separate, and no local active background/pill survives.

Registration and credential rotation generate/show/confirm a new canonical secret under the handoff above; recovery only consumes the user's existing secret and never generates, shows or revokes one. Public-handle regeneration is absent. The UI creates one UUIDv7 before send, persists only the non-secret intent shell, labels secret re-entry after reload, and distinguishes same-command identity replay from `idempotency_key_reused`, `public_handle_claimed`, proposed-secret 422 and generic current/recovery 401 without implying a second account. Rotation replaces the initiating HttpOnly session, signs a sibling old-policy session out without exposing a token, and lets a command-linked successor settle a body-lost receipt. Logout shows one durable pending result, leaves the stale session cookie untouched until browser-session end or fixed server expiry, then uses fenced `/sessions/me` re-derivation to reach one signed-out state; it never reports failure merely because a success body was lost, clears/overrules a newer valid session after a reordered stale 401, signs out on occupied-key 409, or lets a tombstone begin another command.

Create/edit/reorder/tag/rename/save/publish/restore-release/fork/vote/report/trash/restore and color-name proposal drive W16 resources without intercepted routes. Premature public visibility is a named conflict; create→publish→unpublish→trash→restore remains in one total inventory class with honest substate. Fork asks for the exact new slug before commit, reports `palette_slug_claimed` without phantom success, and opens the returned revision-1 private Workspace; it exposes neither destination nor actor before Publish. Public provenance rechecks every ancestor and renders a hidden/purged hop only as the opaque unavailable ordinal. A name-slot conflict identifies occupancy without leaking evidence; report repetition exposes the stable case. Every Publish has immutable public history: first→second keeps both visible, lineage unpublish/trash/owner suspension hides all, and one release withdrawal hides only that release.

All five JSON/CSS/Tailwind/SVG/PNG exports consume the exact durable immutable `ExportSnapshot` union and digest specified in `PALETTE-FACILITIES.md §6`; a retry never follows a newer Device Draft or Workspace revision. DeviceDraft, saved Workspace and immutable Release each exercise the atomic source-arm law against W16's hashed fixtures. Dirty Workspace overlay offers only Save then capture or copy to a new DeviceDraft; retired/unresolved/duplicate selected tags fail rather than disappear, while Release frozen labels survive retirement. Every serializer matches `PALETTE-EXPORT-CONTRACT.md` byte-for-byte: positional identifiers eliminate name collisions, the one fixed-point OKLCH spelling is shared, Tailwind is non-executable canonical JSON, SVG is font/network/script-free and rejects `U+FFFE/U+FFFF` at the shared law, and PNG constructs typed OKLCH atoms, consumes exactly one packed W8 `toRgba8(...,{gamut:"clip"})` call per atom and owns only exact DEFLATE/chunk/profile framing—no table, copied coefficient, color matrix/transfer, canvas, platform converter or alternate helper.

The fixed-point export CSS spelling is a W23 product-byte serializer over `ExportSnapshot` atoms only. It does not parse arbitrary CSS, convert a `Color`, expose a general library serializer or duplicate W9's `/css` contract; its ordinary export vectors must prove that boundary.

Preparation uses W15's exact durable record. The existing action seat first shows **Prepare FORMAT**, persists snapshot identity, and swaps in place to **Download FILENAME** only after serialized bytes/SHA/MIME/filename commit and verify; it never stacks a second action row. Download is a fresh activation and reload consumes the prepared bytes without reserialization. Each format—not one representative—proves filename/MIME/full hash, exact parse/decode, `<>&"'`/comment/newline/Unicode/duplicate-name/slugless-draft vectors, `U+FFFE/U+FFFF` rejection, reload identity, interrupted/quota/contract/handoff failure, acknowledgement/discard and URL lease behavior. Chromium/V8, Firefox/SpiderMonkey and Safari/WebKit/JavaScriptCore record exact versions and identical snapshots, prepared bytes and full-file hashes. PNG additionally consumes W8's committed `RGBA8` tuples and proves the decoded full raster plus all eighteen stored-block headers/BFINAL, Adler-32 and CRC against the sole `demo/palettes/export/png.ts` framing source; no table or second source digest exists. Status survives reload until acknowledged; stale edits offer the three decided choices. UI distinguishes lineage vote from immutable-release fork/report and preserves immutable local history. Palette-color reorder uses §5.2's explicit grab/move/drop/cancel contract, announces the new ordinal, and reaches the same order with pointer and keyboard under LTR/RTL. When filtering hides colors, reordered visible IDs replace only their original visible slots; hidden IDs remain in their exact slots and never jump behind the filtered set.

If IndexedDB is unavailable before capture, the seat enters W22's outer storage-recovery state and creates no ExportOperation. Persisted retryable failure codes are exactly quota, serialization resource and handoff; `storage_unavailable` is absent. If persisting a later failure state itself fails, the last durable operation remains authoritative and the seat returns to the same outer recovery state rather than claiming a memory-only failure was stored.

## π / DELTA

- π: two orthogonal real-browser sequences: (a) Account closed/open at 1440/390 then register→recover→rotate→reload/re-enter→lost-response logout, checkpointing replacement cookie current + sibling 401 immediately after rotation, then zero valid initiating session + one signed-out UI after logout replay settles; (b) one palette through device draft→workspace→private save→publish→edit→second release→unpublish→trash→restore, plus conflict/fork/vote/report and every five-format Prepare→reload→Download success/failure/retry. Sequence (b) repeats the exact export vector in current Chromium, Firefox and Safari and covers owner-state tabs at 1440/390, horizontal/vertical, LTR/RTL and focused selection.
- DELTA: Account overlay/main count, region order, opener focus return and durable result; pre-send retention confirmation, copy-failure/manual path, secret non-persistence/re-entry/redaction, cookie/session identity without token exposure and command/status identity; handle/workspace/revision/release identity, public r18 versus owner r50 history, private-fork absence then public appearance, opaque hidden provenance, destination claim/copied-manifest provenance, atomic source-arm identity, export snapshot/prepared-byte SHA/state across reload for five formats, W8 RGBA tuples/full-file hashes across three engines, ordered swatch continuity, keyboard/pointer equality, and selected-tab four-edge rect error/fill count/focus separation.

## Disjointness, artifacts, commit

W23 consumes W15 semantics and W22 species; it may not invent transport state or admin composition. Commit owner workflow, social/history, export source/preparation/serializers, then inventory, each backed by real W16 API fixtures.

## Archaeology

Builds the palette CRUD tails ignored by earlier “complete” claims and absorbs U-F56/F58 authenticated/error-state residue for owner and public actions.
