> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W4 — Repository Subtraction

**State:** PLANNED · **Meta-tranche:** V.H Hardening · **Depends on:** W0–W3

## Goal

Remove the proof theater and campaign residue so subsequent product work has one ordinary developer path.

## Scope and file bounds

Repository-only campaign residue—including the superseded V campaign/handoff and entire `design/**` tree with its dangling portfolio, pass charters/status documents and executable instruments—obsolete command identities, stale benches, ignored evidence/litter, and safe-delete branches/worktrees. Product wrappers, bindings, barrels, plugins, parsers, assertions, and source modules remain with the wave that lands their replacement. The hidden glass-ui pin is inventoried but deliberately remains installed: post-BI next-Glass migration and unpinning are W17 work, with the version chosen only from V-A78's public-surface diff.

## Triumvirate and worktree

- **Specification:** `SUBTRACTION.md`.
- **Plan:** inventory every unique product assertion in `scripts/gates/**`/obsolete CI, route it to an ordinary unit, packed-consumer, or browser test in its product wave, then delete the custom command identity; prove safe residue/worktree deletion without touching dirty user state; classify and preserve the glass-ui pin for W17 rather than manufacturing a broken pre-migration interval.
- **Implementation owner:** repository maintainer; primary worktree for tracked deletion, explicit per-worktree proof for external cleanup.

## Current RED

Ten chained proof programs include ledger/token checks; the superseded V design passes retain nine executable census/oracle instruments and a second status process; three `test.fail()` rows ship; one package benchmark crashes; thousands of ignored/cited image files and 16 non-primary worktrees remain; both plugins encode alternate paths.

## Completion evidence

Every unique product assertion has a named ordinary-test owner before its custom wrapper is deleted. W0's prompt/decision/deferral map contains the unique surviving substance of the old campaign; `DESIGN-CAMPAIGN.md`, `HANDOFF-2026-07-13.md` and the entire `design/**` tree—including `design/portfolio.md` and all nine pass-4 executable instruments—are absent, while `research/CONVERGENCE.md` remains the concise route/disposition record. From a clean checkout, ordinary `build`, `typecheck`, unit, selected real e2e, and pack commands run without any deleted proof/CI helper against the dependency baseline that existed on entry to W4; this is expressly not a post-BI Glass adoption claim. `npm run bench`, if retained, invokes only the small live dist-backed set and exits honestly. A branch/worktree is removable only after merged-state, unique-commit, dirty-state and ownership evidence is recorded for that exact path. No cited artifact is accidentally deleted and no user-owned or ambiguously attributed dirty file/worktree—including the present T communiqué and `u-gestalt/` material—is touched. `.claude/worktrees/glass-ui-pinned` still resolves exactly as it did on entry and is handed to W17 with its classification evidence.

## Disjointness, artifacts, commit

W4 does not restructure or delete product modules, adopt the post-BI next Glass artifact, or remove the glass-ui pin. Source and dependency-path deletion lands only with W5–W17/W31/W33 replacements. Commit repository-only deletion as `chore(v-w4): remove proof theater and campaign residue` with why/what/evidence and preserved remainder in the body.

## Archaeology

Disposes U-F1/F14/F15/F42, U-F43's residual helper identity, U-F53's fresh classification, U-F55's CI half, U-F64's proxy and V OF-1/OF-6 tooling halves without recreating their machinery. U-F50–F52 remain credited closures; U-F54 belongs to W32.
