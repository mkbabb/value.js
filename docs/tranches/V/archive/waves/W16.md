> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W16 — Real-Stack Palette API and Transport

**State:** PLANNED · **Meta-tranche:** V.H Hardening · **Depends on:** W10–W15

## Goal

Demonstrate palette/auth/admin semantics through actual Hono/Mongo and the typed W15 transport before dependent UI journeys are composed.

**V-A161–V-A164 current RED:** the real-stack cut must close bounded color-prefix search, strict Problem/error projection, Principal-derived proposal attribution, active-public access and per-hop fork/provenance privacy. Raw legacy routes and regex/DTO fallbacks are retired, not wrapped.

## Scope and file bounds

API/test fixtures, authenticated replica-set dev topology, two API processes, typed W15 transport conformance, `api/src/modules/meta/**` plus `api/src/app.ts` final mounted-route/OpenAPI integration, complete data-cut rehearsal, mock classification and transaction/failure injection. Product-browser journeys belong to W22–W24/W30.

## Triumvirate and worktree

- **Specification:** `PALETTE-DOMAIN.md §8`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-CURSOR-CONTRACT.md`, `PALETTE-OPERATIONS-REPLAY-CONTRACT.md`, `PALETTE-LOCAL-RAIL.md`, `PALETTE-EXPORT-CONTRACT.md §1`, `LEGACY-PALETTE-SOURCE-CONTRACT.md`, `research/auth-cookie-order.md`, `EVIDENCE.md §5`.
- **Plan:** recreate the post-W14 candidate from the frozen nine-collection source plus `cut-input.json` and verify its canonical bytes before bootstrap; integrate the complete mounted typed route graph into W10's OpenAPI projector without a hand table; run two API instances for receipts; exercise the command/access/catalog matrix through HTTP and typed transport; classify mocks as unit tests; publish the fixture for W22–W24/W30 browser journeys.
- **Implementation owner:** real-stack lead with API and browser collaborators; isolated test database and production build.

## Current RED

Twelve of fourteen palette/admin flow specs mock the backend; wildcard fixtures accept malformed If-Match and return invented envelopes. Slug auth, private reads, retries, history collision, and lifecycle races therefore pass unseen.

## Completion evidence

- The mounted 50-route surface contains public r18 and owner-only r50 as disjoint history reads. Owner history covers private, unpublished, withdrawn and trashed live lineages; public history never changes meaning under authentication.

Every one of the 50 target routes in `PALETTE-FACILITIES.md §3` and no RETIRE route is mounted. In particular `/docs`, the old user-palette cascade, admin import and both generic batch paths return route absence, not authorization-hidden handlers; `meta/route-table.ts` is absent on disk. The mounted-router-derived OpenAPI includes the complete target without a parallel hand table and changes when a disposable typed route is added/removed in the fixture. All API/transport cases in `PALETTE-DOMAIN.md §8` run through Hono against authenticated Mongo without route interception: the exact shared ASCII canonical-spelling vectors across registration/bootstrap/create/rename/fork/raw lookup with zero second normalizer or alias; canonical UUIDv7 validation plus server-derived registration/recovery/principal receipt namespaces; same-command two-replica registration, same-key/different-handle precedence, wrong/revoked-secret generic failure, two-distinct-key claim conflict, recovery replay and non-persistence of secrets/bearers; registration/recovery/rotation and permanent PublicHandle claims, including registration-versus-close; Workspace creation and Handle rename against one permanent palette-slug namespace, including rename-away-versus-old-spelling claim and two-renames-to-one-destination races; exact fork `{slug}` creation, same-command cross-replica replay and two-distinct-commands-to-one-destination, proving one complete unpublished lineage/one named conflict and zero partial claim/Handle/Workspace/Policy/edge/Release rows; unique `fork_edges.targetPaletteId`, many-destinations/one-source success and second-origin rejection; Principal/grant aftermath; one-time concurrent bootstrap with the frozen eleven-scope grant and permanent re-bootstrap denial outside Hono; two concurrent authorized **self-removals** from a two-holder state, with the AuthorityGuard producing one success/one `409 last_root_authority`, `eligibleCount=1` and a matching reconciled digest; separate cross-target mutual removal where one effect succeeds and the now-ineligible actor's retry returns ordinary 401/403 with zero second effect; large-cardinality bounded PrincipalAuthority work; shared abuse windows; access; resource CAS; immutable histories/policies; color-name slot concurrency/release/resubmission with stored display spelling/claim generation and a successor-stable old ETag; permanent TagDefinition keys and actor+Release Report identity; cross-process permanent-effect receipts; social edges; purge/provenance; catalog expiry/continuation; and the exact all-or-nothing source-state cut matrix. Independent canonical-content vectors cover syntax-equivalent colors, alpha default, achromatic hue, half-even boundaries, Unicode 15.1 composition/whitespace/casefold plus exact `U+FFFE/U+FFFF` rejection, duplicate order, exact color-name/tag normalizers, tag sorting/uniqueness, missing/out-of-range rejection and forced digest collision. W15's sanitized outbox receives actual delayed/lost/failed responses in a transport harness and proves secret re-entry reuses the durable command shell. The ordinary dev command exposes API-unavailable as an error, never fake success. Browser UI claims are owned and completed by W22–W24/W30; mocks remain labeled interaction units and are never cited as conformance.

For r30, mounted OpenAPI and W15's generated client expose one subject-query union: absent, type-only, or correctly typed pair; `AuditEventItem` remains flattened but is a discriminated result union. They also expose one time union: absent or paired `{from,to}`, never two independent optionals. Hono/Mongo/typed-client vectors cover every subject arm plus no filter, type-wide filter, platform `primary-ready`, cut-v5 resource subjects and live-v7 subjects; time vectors include the `from` event, exclude the `to` event, admit exactly 366 days, and preserve both canonical strings in QueryIdentity/golden bytes. A bare `subjectId`, v5 for a live-only type, malformed/wrong-version IDs, any other platform value, an obsolete `outcome` key, one-sided/equal/reversed/longer-than-366-day time bounds, explicit null and duplicate scalar keys each return `422 query_invalid` before the audit repository runs. A forced rejected command and `content_digest_collision` produce their ordinary Problem/log result but zero AuditOutbox/AuditEvent rows, proving audit is committed-effect provenance with no attempt logger. Changing either valid time boundary makes an old cursor `422 cursor_invalid` through query-digest mismatch; no generic string, independent subject pair, outcome field or independent time optionals survive.

One ordinary table-driven audit fixture executes bootstrap; every newly completed mutation row r3/r4/r5/r6/r9/r11/r12/r13/r15/r22/r23/r24/r25/r28/r32/r34/r36/r40/r41/r43/r44/r46/r47/r49; r49 separately with `none`, palette-policy and release-policy effects; an already-satisfied vote; every principal-aftermath batch/phase transition; and every palette-purge phase. Each case asserts the Domain table's exact event count, action, subject, actor/command/correlation presence, authority arrays, closed before/after state object, independent RFC 8785 bytes and domain-separated digest. Bootstrap proves one persisted generated correlation and no extra DTO field; each delayed-job row proves the persisted cause correlation/actor/command/witness survive into child jobs and events with no worker substitution. Effect, outbox and delivered event agree byte-for-byte; replay, abort and rejection produce zero, and r49 never produces a second policy event.

The export-source fixture freezes the exact bytes of one saved `WorkspaceDetail`, one immutable `ReleaseDetail`, and the complete paged r29 active TagDefinition observation consumed by a DeviceDraft. It proves each DTO's IDs/revision/digest/ordered colors/tag projections are internally one server response, that a retired selected Workspace tag is returned as retired rather than dropped, and that Release frozen labels survive current definition retirement. W23 consumes these fixtures by their recorded RFC 8785 hashes; no mock or dirty browser overlay may masquerade as a W16 source record.

The data-cut fixture contains all nine source collections and every §7/source-contract partition: public standard/featured, private, unlisted, archived, deleted, claimable/reserved/noncanonical spelling, every optional/nullable field, each exact enum/type/bound reject, complete/invalid/cyclic version graphs, equal-timestamp siblings, external fork references, valid/missing/duplicate-normalized tags, approved/pending/rejected names, identity references with and without `users` rows, normalized-identity collisions, dangling votes/flags and sensitive sessions. Two isolated runs over one restricted `cut-input.json` have identical UUIDs, source-derived/frozen times, release order, counts and **logical plaintext** artifact bytes/digests. Random-nonce encryption is performed only after that equality check and is verified by decrypting the stored immutable object version back to those digests; ciphertext equality across runs is neither expected nor claimed. The successful target has `|P+|` live system Handles and zero private legacy Handles/Workspaces/legacy Principals/sessions/votes/forks/reports; its active Releases exactly match currentHash, every claim traverses to one live subject or tombstone, every claimable old public handle is unavailable, tags/names resolve once with `attribution=system` and no invented actor, and the canonical re-export equals the restricted target artifact. Fault injection proves every closed reject family, count equation, digest/UUID collision and dangling reference destroys the candidate and leaves source unchanged. Row-level source/target/disposition/provenance/reject artifacts are present only inside the restricted encrypted object; tracked capture proves only aggregate counts/domain digests/object version and contains no dictionaryable source key.

The cut additionally proves for every digest that Workspace and Report refs are zero, Release refs equal exact live target-Release multiplicity, their global sum is `|V+|`, and the ContentObject count is the number of distinct live digests. A zero-count object, tombstone-counted reference or post-transform counter repair destroys the candidate.

The trust/transport matrix additionally proves: empty PlatformState makes health not-ready and registration/product traffic `503`/zero-write, then DB-direct bootstrap atomically makes readiness true; canonical and noncanonical `rs1_`/`st1_` vectors, independent HMAC vectors, key-ID retirement, dummy verification, 1 KiB body ceiling, fixed 30-day session expiry/no sliding renewal and TTL purge; exact registration/recovery/rotation secret roles after receipt lookup, including malformed same-key replay and rotation's always-present two-field envelope; every strict query/item/mutation DTO, the exact cursor bytes/route-specific scan coordinates in `PALETTE-CURSOR-CONTRACT.md`, and the closed titled `application/problem+json`/field-issue/resource-reference family; and the real-HTTPS `local` profile from `PALETTE-LOCAL-RAIL.md`, using the production edge artifact, relative `/api`, local PKI, mTLS+MAC, the exact signed header block and the profile's singular authority. Both `production` and `local` fixtures exercise unsafe `POST`/`PUT`/`PATCH`/`DELETE` with their exact `Origin`/`same-origin`/`cors`/`empty` tuple plus safe GET all-present-or-all-absent cases, exact Set-Cookie grammar, method/body/status/Problem/Idempotency-Key/If-Match/ETag preservation, no redirect, and no bearer JSON/header/storage or CORS response. Credential rotation with two sessions proves the initiating browser receives/replays a replacement cookie while its sibling immediately gets 401. A generated row×status assertion covers the Wire §5.2 matrix rather than sampling it: every success has the exact JSON/no-store/site-security fields, every Problem the exact Problem/no-store/site-security fields, ETag appears only on the direct one-clock success rows with the named strong value, every composite Workspace/Handle/Release/session response forbids HTTP ETag and carries all named contributing body clocks, Set-Cookie only on successful r3–r5, Retry-After only on 429, and `Vary`, `Location`, CORS or any unlisted/duplicate origin field fails at the edge. Independent joined-authority mutations prove no body field can change behind an unchanged claimed whole-response validator. Missing unsafe—including r22 `PUT /palettes/:slug/votes`—partial, cross-profile, trailing-slash/default-port/case-altered origin and every changed fetch token return `403 origin_forbidden` before credential/domain work. Public reads separately prove absent/malformed/revoked/expired/stale cookies remain anonymous and only a current session personalizes them. An actor admitted before another transaction revokes its grant/suspends it loses the AuthFence retry and creates neither receipt nor effect. Cross-Principal policy commands acquire distinct fences in ascending Principal-ID order (one if identical), revalidate actor/target roles, then CAS AuthorityGuard, so concurrent actor or target policy changes cannot slip through. Self/cross-grant attempts cannot exceed the actor union; root-bearing grants require both root scopes; redundant and twelfth active grants fail exactly. Grant creation returns revision/ETag 0 and its canonical r43 reason plus Principal attribution survive r42/r43/r44 byte-for-value; the bootstrap Grant instead returns the sole workload arm and fixed rationale, never a null/scalar creator or false self-grantor. The sole revocation CAS returns revision/ETag 1 without rewriting those creation fields, and a second revoke is `409 state_conflict` rather than another clock. Same-command two-replica logout/rotation/self-lifecycle proves the intent loser restarts completed-receipt precedence after the winner invalidates auth rather than returning a false 401. Registration-replay/recovery/rotation issuance racing credential rotation or suspend/close proves both orders: issuance-first makes policy change conflict/retry and invalidate that issued epoch; policy-first makes issuance re-read/reverify the receipt-bound/current credential under AuthFence and return generic 401 with no live session or new receipt.

The signing-proxy matrix independently mutates private CA/client certificate, SNI, Host, timestamp, key ID, correlation ID, MAC, method, exact target/query, body, each present/absent/duplicate allowed request header and signed client IP; missing/stale/tampered inputs fail before route/body work, while current and 30-second verify-only edge keys pass only in their prescribed interval. The local QuickJS dispatcher accepts only its simulated PROXY-v2 transport identity, reconstructs the exact Domain allowlist, parses/rebuilds only the one session cookie, signs the ordered header block and discards spoofed `Forwarded`, `X-Forwarded-*`, `X-Real-IP`, `True-Client-IP`, `CF-Connecting-IP`, `X-Value-Client-IP` and `X-Value-Edge-*` values. Response probes cover every singleton/duplicate/unlisted header and the exact sole Set-Cookie grammar. Binary vectors cover IPv4 dotted decimal without leading zeros; IPv4-mapped collapse; RFC 5952 longest-leftmost zero compression for native IPv6; prohibition of mixed dotted native IPv6, zones, PROXY `LOCAL`/`UNSPEC` and non-TCP families; and byte round-trip rejection. The full canonical address changes the MAC, while two native IPv6 addresses in one /64 share the exact abuse subject and adjacent /64s do not. Raw request-target fixtures execute the projection-only classifier, literal API validation and NGINX pre-content error handler, reject encoded API prefixes/percent/backslash/non-ASCII/malformed/dot/repeated-slash paths before routing, and preserve a valid percent-encoded query byte for byte. A log-capture assertion permits only timestamp/version/edge-instance/method/route-template/status/duration/correlation/outcome and searches every record for forbidden path, query, IP, header, cookie, body, MAC, secret and receipt material.

Fake Mongo time and two API processes prove the three shared abuse classes exactly: registration new-effect source 5/10m, recovery failed-proof source 10/15m and recovery failed-proof handle 5/15m. There is no registration-handle row. Exact completed registration replay bypasses the new-effect budget; correct recovery proof/replay bypasses both failure budgets even after hostile exhaustion; failed real and dummy/absent proofs share timing/count/status and N+1 returns the same no-store `429 abuse_window_exceeded`/`Retry-After`. Malformed recovery uses dummy work and source-failure only. Key rotation cannot reset a live row; TTL lag cannot extend or shorten a window. There is no process-local or test-only counter and no attacker-spendable identity lockout.

Public fork discovery is tested as private absent → Publish visible → unpublish/trash/withdrawal absent, with no actor leak and last-scanned continuation while the durable origin edge remains. Public history is first Publish→second Publish with both Releases readable, then unpublish/trash/owner suspension hiding the lineage and one ReleasePolicy withdrawal hiding only its target. Principal lifecycle tests admit only active→suspended, suspended→active and active/suspended→closed, reject every transition/grant-create from closed, and prove close's O(1) semantic transaction emits one projection-invalidation marker and is terminal before any leased annotation/private purge. A fixture with many owned Handles proves immediate read-time suppression without a lifecycle scan; a lost close response before and after leased aftermath returns the same receipt result.

Catalog fixtures prove the closed token AND/final-prefix corpus, checked `4·votes+7·public-forks` score, most-forked and minimum-to-any-color ΔE ordering. A burst before one CatalogBuildState snapshot coalesces to one epoch with no event rows; a mutation just after the snapshot remains dirty and is covered by the immediate successor within the 60-second outer bound. The candidate UUID is unreachable until complete key/count/rank-digest verification, and injected crashes immediately before/after the one lease-epoch publish-plus-ack CAS leave respectively the old/new epoch authoritative with no false `builtRevision`. A failed/incomplete candidate expires through its fixed row TTL, a clean state refreshes before the published epoch's final hour, and TTL lag never extends application expiry. The old cursor remains byte-stable, inaccessible/current-release-mismatched rows skip, and `PaletteCard.rank` never claims current counts. Count/score fixtures emit JSON number `9_007_199_254_740_991` exactly where legal, reproduce its smallest deterministic-CBOR unsigned cursor coordinate, and make an edge aggregation or `4·rankVoteCount+7·rankForkCount` overflow fail `platform_consistency_fault` with no response/cursor/published epoch; `9_007_199_254_740_992` never crosses JSON. A policy-tier change made after rank capture but before page read proves `catalogTier` filtering and the returned `palettePolicyRevision`/tier use the same current projection: the old epoch order is retained, the now-ineligible card is skipped, and no rank snapshot is relabeled current. Public detail returns exact majority-read edge/access counts with `asOf`; a hidden fork changes that detail count without mutating an old rank epoch. Workspace list/detail fixtures carry `displayName`, immutable `createdAt`, revision-governed `updatedAt`, bounded ordered colors, digest, optional fork base, active/retired tag projections with TagDefinition clocks, Handle/PalettePolicy clocks and coupled `trashedAt/purgeAfter`. r9 and r24 then r33 prove exact `"lineage created"` plus authenticated creator; r12 then r35 prove exact `"release published"` plus publisher. Later policy commands replace reason/actor together, while cut-created initial policies alone omit both. Report creation stores `targetPaletteId` plus immutable target snapshot, duplicate report returns only the caller's `resourceRef`, and review persists the exact `{none|palette-policy|release-policy}` applied effect with immutable before/after policy revisions. Review remains possible after release/Handle purge. Fake time/lease contention runs the 30-day purge grace, 30-second lease/10-second renewal, 100-ID phase cursor and `seal→releases→edges→complete` order; restore-versus-seal is one CAS winner, active claims retire before live removal, Reports pin Release/ContentObject, and receipts/audit/claims/fork edges/tombstones survive while vote edges and truly unreferenced content disappear synchronously. Exact ContentObject vectors cover create; never-published A→B deleting A; same-digest/same-bytes patch with zero counter movement; forced same-digest/different-bytes r11 collision with `content_digest_collision` and zero Workspace/counter/AuditOutbox/AuditEvent write; shared A retained then the last Workspace A→B deleting it; fork; Publish; Restore; first/duplicate/replayed Report; and zero-Release seal. One digest shared across a 100/101 Release-batch boundary deletes once, while a Report-pinned Release remains live and its independent Release/Report counters remain exact. Every create/patch/fork/publish/restore/report acquire races the last release/zero-delete and permits only two terminal states: a live referent plus object with exact counters, or no referent and no object; aborted and driver-retried receipt transactions leave neither counter nor orphan. Underflow, overflow, direct writes and a collector/candidate/scan path fail closed.

Save shares the inherited actor AuthFence with Handle lifecycle; it gains no Handle-local lock. In both orders, r11 races r15 owner trash: Save-first commits its exact Workspace/reference delta, then trash retries and commits; trash-first makes Save restart after the fence conflict, observe the inactive Handle and create no Workspace/counter/receipt effect. A separate owner-close-first race makes Save restart through the shared actor/target fence, fail ordinary 401/403 and create zero effect. These races fail if either command omits the sole operations wrapper/fence.

Cookie settlement injects both loss boundaries. Logout/self-lifecycle lost before durable body observation retains the stale cookie and replays the exact receipt; `/sessions/me` returns 401 without cookie deletion before or after outbox commit. Rotation tests replacement headers lost (predecessor replay) and Set-Cookie applied/body lost (the `issuedByCommandId` successor retrieves only its creating receipt); unrelated successors/sessions remain 409/403 and see no result. For each of the three cookie-changing routes, origin headers-before-body, body-before-deadline, body-after-deadline, abort, private-network failure and 65,536/65,537-byte response fixtures prove that the QuickJS proxy returns `Set-Cookie` only after the complete bounded origin response settles before ten seconds; every other case is `504 edge_origin_timeout` without a cookie. A `Set-Cookie` injected on any other route is intercepted before public headers and becomes cookie-free `502 edge_cookie_contract_violation`. Separately, two-tab registration/recovery/rotation responses are delayed and reordered in both browser arrival orders across the 30-second lease, 10-second hard timeout and drain boundary. The ordinary order may retain the newer cookie; a post-emission stale same-name response is allowed to replace it. In that negative arm the stale token authorizes no new effect, `/sessions/me` is 401, and Account exposes the explicit signed-out/recovery state without claiming the newer cookie survived. The lease/drain may reduce overlap but is never cited as a delivery-order proof. Local dispatcher probes exercise the raw request-target buffer, signed correlation ID and simulated PROXY-v2 identity; W33 alone owns the native IPv4/IPv6, HTTP/1.1/2 and byte-on-wire deployment claim.

Lost-response current-session logout, credential rotation, self-suspend/self-close and self-grant-revocation run through both API processes. Only the receipt-bound initiating cookie/key/binding retrieves the completed non-secret result; the same session/key with another binding is `409 idempotency_key_reused`. Another **valid** same-Principal session authenticates/authorizes normally, receives non-result-bearing 409 (or 403 without route scope), cannot see the result, and remains signed in; a new key, naturally expired/revoked/different-revoker cookie or fresh command gets ordinary 401/403 and performs zero writes. The retained digest never appears in response/log and cannot reach `/sessions/me` after revocation/epoch invalidation. W15 derives auth from the settled cookie plus `/sessions/me`, never local bearer state.

Receipt fixtures reproduce every golden in `PALETTE-OPERATIONS-REPLAY-CONTRACT.md`: exact typed `CommandBinding`, RFC 8785 bytes and domain-separated digest; reference-free deterministic-CBOR status/header/body plaintext; length-delimited AAD; and four stored `{replayKeyId,replayNonce,replayCiphertext,replayTag}` fields. They reject text-vs-raw digest substitution and every one-bit route/namespace/command/binding/AAD/tag change; r32 also rejects a completed replay with only composite `claimedRevision` altered. Two concurrent rewrappers and interrupted 100-row pages preserve byte-identical plaintext under a new key. An old-key writer held across the majority-committed `ReplayKeyState` change either commits within the 30-second bound and is found after the drain or aborts; any retry re-reads and uses the new writer. Only the subsequent two epoch-stable zero-receipt scans separated by 30 seconds and independent majority count permit old-key removal. Purge fixtures prove that palette/workspace compaction never deletes a receipt or AuditEvent and that replay emits the original bounded stored body/headers without reading a tombstone or mutable resource.

Cursor fixtures additionally pair the same Q-ID with different route targets—palette for releases, source Release for forks/provenance, target Principal for handle/grant admin—and require 422. They exercise `issuedAt`/`expiresAt`, noncatalog +24-hour expiry as 422, absent/expired rank epoch as 410, and conservative cursor-key retirement after the last issuance/deactivation plus 24 hours and 15 seconds. Live noncatalog mutation probes permit movement only when eligibility/sort coordinates change; an unchanged row cannot skip/repeat and the transport de-duplicates stable IDs. Catalog epoch probes retain their stronger immutable-snapshot claim.

Query fixtures prove repeated catalog tags are conjunctive across frozen Release membership and current-active TagDefinitions, while public name search and proposal review use the exact normalized byte-prefix interval with no token/fuzzy alternate. Runtime lineage fixtures cover ordinary absent base, external fork base, edit preservation, same-lineage restore, root/local/external Publish ancestry arms, post-Publish Workspace base advance, nearest-first local parents and the single edge-matched external source; every illegal mixed/cyclic/cross-lineage shape fails closed. Constructor fixtures cover every Domain initial clock. AuthFence-only rotation changes `CurrentSession` but leaves `PrincipalDetail` bytes and Principal ETag unchanged.

The signing-proxy runs the production and local SNI/Host pairs in both valid directions and rejects both cross-profile combinations. Every API origin response is withheld until complete ten-second settlement: a general route accepts 4,194,304 bytes, rejects 4,194,305/abort/late/private failure as buffered `503 dependency_unavailable`, while r3–r5 retain their 65,536/65,537 cookie-free 504 boundary. Fractional abuse-window remainder rounds `Retry-After` upward with the Domain formula; the exact boundary starts a new window and cannot emit an old-window 429.

The row×status assertion additionally requires exactly one Set-Cookie on each successful r3–r5 response; a missing cookie and every extra/other-route/error cookie are explicit contract-failure fixtures, not optional presence.

Principal-close fake-time fixtures stop before and after the 30-day boundary. They prove immediate read-time suppression without Handle scans; the `principal-aftermath` worker's 100-ID pages and `handles→complete` cursor; active owned Handle generation/lifecycle/visibility/close-derived clock, the one cause-correlated batch audit outbox row and palette-job insertion in one transaction; and then the ordinary palette `seal→releases→edges→complete` CAS with synchronous reference release. A pre-existing trashed Handle proceeds only when exactly one schedulable episode matches its stored namespace/command/eligibility, without reclocking. The binding `trash(c1)→restore(cancel c1)→close(c2)→aftermath` vector retains the cancelled c1 row, treats it as terminal history, creates exactly one schedulable c2 episode and completes; its adversarial arm sets `c1===c2` as raw UUID bytes while owner O and closing admin A are distinct, proving literal `principal:<ownerId>`/`principal:<adminId>` cause namespaces prevent the retained-row collision. The Handle retains only its namespace/command/eligibility coordinates; the original close correlation/actor/authority witness survive in both jobs, every delivered batch event and result bytes. A queued/leased job injected for a still-active Handle remains RED/fail closed. An absent or terminal-only episode for a trashed Handle, a mismatched namespace/command/eligibility, or multiple schedulable jobs all return `platform_consistency_fault` and leave cursor/counts unchanged; no repair write exists.

Purge restart fixtures stop at every exclusive cursor: Handle UUID, `(releaseNo,releaseId)` and vote UUID. They expire/reacquire the lease after Workspace reference release and at each 100/101 Release boundary, proving committed counter/object state needs no captured digest or later content cursor. Equal release numbers/IDs, lost leases and phase-empty transitions prove no skip/repeat. The principal progress/result schema has `additionalProperties:false`, exactly five required keys `{scannedHandles,transitionedActiveHandles,alreadyTrashedHandles,verifiedPaletteJobs,createdPaletteJobs}`, and rejects every missing or sixth key; fixtures prove `scanned=transitioned+alreadyTrashed`, `created=transitioned` and `verified=alreadyTrashed`, with no repair counter. Accumulated scalar count objects and the domain-separated RFC 8785 result digest, including literal `causeReceiptNamespace` and `causeCorrelationId`, are recomputed independently for both job kinds, and any cursor-kind/count/subject-tombstone mismatch fails completion. An injected per-digest counter/object mismatch aborts its originating seal/release transaction before cursor or scalar-count advance. A separately quiesced real-stack fixture recomputes the complete Workspace/Release/Report cardinalities for every digest and matches every counter/object-existence equation; that bounded fixture evidence is never a production purge-completion scan, lock or repair writer.

Identity vectors keep command and resource grammar disjoint: Idempotency-Key and live-only Workspace/Principal/Grant/Report paths accept canonical UUIDv7 only; cut-capable Palette/Release/ColorNameProposal/TagDefinition paths, DTOs and tag/release filters accept canonical v5 or v7. Cut-created v5 Handles/Releases/Names/Tags are read and administered through the same mounted routes, while v5 on a live-only path, v1/v4/v6/v8, uppercase and bad variants are structural 400s. OpenAPI projects those exact unions rather than advertising every ID as v7 or weakening the command key.

Authority provenance fixtures construct one grant covering both required scopes, two grants each covering one, and several overlapping grants. The frozen-order/lexicographically-least selection yields one or two unique sorted `grantIds` exactly; receipt, outbox, delivered AuditEvent and `AuditEventItem` agree byte-for-value on committed-effect fields and none has an outcome field. r43's normalized reason/Principal attribution and bootstrap's fixed reason/workload attribution survive r42 paging and r44 revocation unchanged. Revoke-after-effect cannot rewrite that witness, bootstrap/product events expose empty arrays, and an aftermath audit inherits the cause command set rather than a worker grant.

## Disjointness, artifacts, commit

W16 owns real-stack fixtures and API/transport conformance; it consumes W15's sole `vite.config.ts` and ordinary local dev launcher/config owner rather than creating second dev wiring. It does not implement or require future UI. Commit domain families separately with replica/failure topology in bodies.

## Archaeology

Replaces T-9's narrowed messaging cure and U-F56's mocked/populated gestalt with end-to-end product truth.
