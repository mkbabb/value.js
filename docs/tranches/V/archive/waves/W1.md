> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W1 — Palette Adversarial Failure Lab

**State:** PLANNED · **Meta-tranche:** V.R Research · **Depends on:** —

## Goal

Turn every palette/auth/concurrency claim into a reproducible real-stack fact and freeze the Route-B command/state model before product code changes.

## Scope and file bounds

- Read/run: every route in `api/src/modules/{session,admin,palette,color,meta}/**`, `api/src/platform/**`, `api/test/**`, every palette/color/admin client consumer, and Mongo replica-set fixtures.
- Write during execution: `docs/tranches/V/research/palette-failure-lab.md` and disposable probe output; no production implementation.
- Probes run Hono with a replica set and two API processes where idempotency is tested.

## Triumvirate and worktree

- **Specification:** `PALETTE-DOMAIN.md`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-CURSOR-CONTRACT.md`, `PALETTE-OPERATIONS-REPLAY-CONTRACT.md`, `PALETTE-LOCAL-RAIL.md`, `LEGACY-PALETTE-SOURCE-CONTRACT.md`, `PALETTE-EXPORT-CONTRACT.md`, `research/STANDARDS.md`.
- **Plan:** reproduce identity takeover, private secondary reads, stale writes, global-hash collision, lifecycle drift, retry race, vote drift, pagination hole, owner-inventory gap, and persistent UI-state lies; verify the already-decided 52-route/five-export manifest and command table against mounted wire, client and UI truth.
- **Implementation owner:** API/domain researcher with a client-state collaborator; isolated disposable database, primary source tree.

## Current RED

Slug-only login succeeds; private detail/history/fork routes lack one policy; a stale ETag can overwrite metadata; identical content shares one version row; double delete can double-decrement; process-local retries execute twice; pagination may emit `hasMore` without a cursor.

## Completion evidence

Each named defect has a command/browser reproduction, observed response/database delta, and W10–W16/W22–W24 owner. W1 records a three-column census for all 52 currently mounted routes and five exports: observed current behavior, terminal BUILD/REPLACE/RETIRE disposition, and the exact closed target in `PALETTE-FACILITIES.md` plus `PALETTE-WIRE-CONTRACT.md`. Every BUILD/REPLACE target has exact auth, target ID, strict body/query/schema ID, expected clocks, transaction, permanent replay tuple, aftermath, audit, Problem result, client and UI owner. For r30, the lab records current handling of no subject filter, type-only, correctly typed pair, bare ID, mismatched type/version, `subjectType=platform&subjectId=primary-ready`, another platform spelling, explicit null and duplicate keys; the target OpenAPI/client schema must be the Wire's closed query union and flattened discriminated result rather than two optional strings. A retired route or query spelling that is still live remains an explicit RED witness owned by W10–W16/W23; only the target column must omit it. W1 records the live catalog `offset/userSlug/visibility`, unsealed cursor, permissive fields and unsafe export serializers as RED rather than carrying them forward. It includes the fork route's exact `{slug}` body and atomic NamespaceClaim/private Handle/copied Workspace/clear Policy/provenance result rather than leaving new-lineage identity to implementation. The lab runs the complete canonical-spelling vector table through registration, bootstrap, Workspace create, rename, fork and raw-path lookup and records every second normalizer as RED. The mounted-router census—not the circular hand-kept route table—must expose the current omitted restore route and every other divergence. No hard step is represented by “decide,” “make atomic,” or “add auth.”

The route census includes one compact target→primary-writer→consumed-contract→reachable-consumer map for the closed 50-target surface. W10–W14 are primary server writers by resource mechanism; W15 owns transport, W16 real-stack verification and W22–W24 visible consumers. Contributor ranges never stand in for a primary writer.

## Disjointness, artifacts, commit

W1 writes research only. W10–W16 own tests and fixes. Commit as `docs(v-w1): record palette adversarial failure lab` with the environment and replica topology in the body.

## Archaeology

Reopens palette “CRUD truth” claims from N/S/U only where the product reproduction is red. U-F29/F30 parsing symptoms remain credited and are not re-booked.
