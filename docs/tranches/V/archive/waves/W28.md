> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W28 — Chromatic Boot and Aurora

**State:** PLANNED · **Meta-tranche:** V.F Frontend Realization · **Depends on:** W17–W21

## Goal

Make frame zero palette-honest and Aurora a causal, proportionate enhancement with one observable atom model and no gray or geometric transition.

## Scope and file bounds

Shell boot and atmosphere lifecycle adapter; `index.html`, same-origin `public/prepaint-seed.js`, typed ready/invalid-default boot consumer and `vite.config.ts` `groundRecordInject` removal; permanent CSS chromatic ground; seven-atom color model; Aurora scene/preview/config; renderer arm/reveal/settled seams; shared axes. Blob is W29; final network/performance is W31.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md §6–7`, especially §6.2, B-03/B-06/B-08, BI P043/P045/P046/P054/P122.
- **Plan:** replace injected stop constants with the strict numeric `SeedToken` boot union; resolve/persistently override the OS scheme first, synchronously apply the exact root `.dark` class and `colorScheme`, then validate/format tokens in the parser-free prepaint script and select URL token→storage token→resolved-scheme default once. Mint share/storage tokens only through the final `/color` projection. Let the critical CSS graph own the sole initial seed→seven-atom projection on the ready arm; expose a separate terminal invalid-default boot error with no synthesized seed. Hydrate from the frozen boot/computed atoms and reject or diagnose any conflicting full color without correcting the ground. Project P043 lifecycle, P045 capability/failure identity and P054 scheduling/pause through P046's Aurora facade as `ground → eligible → queued-after-critical-paint → fetching → active | parked | unsupported | failed`; preview-first chassis with compact controls; each atom/control maps to a perceptible restrained effect. Consume the single P046 `AuroraZones` substrate at terminal `MAX_NUCLEI = 8` with an eight-zone value fixture and WebGPU/WebGL2 parity. Consume `BI.W-FIELD-CORE` T-38 exactly—cursor-local luminance lean, velocity/domain-warp burst, input-medium gating and named amplitude—with no consumer shader fork or pixel-inferred pointer state.
- **Implementation owner:** chromatic-boot/Aurora lead with atom, shell-adapter and real-GPU collaborators.

## Current RED

Pre-module paint uses injected constants and a stop cache that cannot see URL precedence, while hydration separately parses URL/storage and derives the model; boot therefore has two authorities and can correct chroma after frame zero. Aurora runtime is statically reachable; atmosphere configuration is a generic selector/slider form; pointer response and zones are weak; shell and atmosphere split renderer ownership.

## Completion evidence

Natural-load traces—valid cold URL token+color, valid returning storage, first-visit default, persisted-light under dark OS, persisted-dark under light OS, and the complete negative set—prove exact mode→document mutation→URL-token→storage-token→resolved-scheme-default ordering before paint. The negative set includes `var()`/`env()`, `currentColor`, system and relative colors, CSS-wide keywords, URLs/calcs, self-reference, malformed/out-of-range/noncanonical tokens, corrupt JSON/schema, color-only legacy links, valid-color/token mismatch, invalid mode, and invalid selected default. Every ready `value-ground/3` arm and hydrated model name the same scheme/token/source; root class plus `colorScheme`, selected CSS and computed atom sets are byte-identical from B0 to app-ready, never empty, with no chroma or layout correction. The invalid-default trace instead proves `state="invalid-default"`, a visible semantic configuration error, absent `--ground-seed`/atom initialization and no normal workbench or synthesized fallback; it is expressly outside ready-arm atom equality. Corrupt or mismatched full input remains untouched, yields its exact typed diagnostic, and cannot replace the frozen ground. The same-origin external classic script contains no arbitrary CSS parser or `CSS.supports` path and passes strict `script-src 'self'`; `groundRecordInject`, `__GROUND_*__`, stop cache and parallel initial TS derivation are absent. The ready semantic shell is usable on the permanent CSS substrate. Eligibility checks visibility, seat presence, renderer capability, data-saving and motion policy; qualified work queues after critical paint/idle without pretending a final-LCP event exists. Aurora enters without rect/chroma discontinuity. `unrequested/fetching/active/parked/unsupported/failed` is truthful in UI and accessibility state with retry when actionable. Failure/reduced motion retains complete CSS truth without claiming the renderer is active. Preview is protagonist; every select/axis visibly changes its named atom/effect. Ambient motion stops within five seconds or a persistent announced still/pause control uses the producer seam. Atmosphere owns renderer state and exposes shell one lifecycle adapter.

## π / DELTA

- π: valid cold URL, returning storage, default, both persisted-scheme/OS oppositions and every grouped negative SeedToken case at frame 0/app-ready; one separate invalid-default terminal-error pair; frame 150/750/1500ms; Aurora rest/pointer/every control group/failure at 1440/390, light/dark, real GPU and reduced motion; eight configured/effective zone centers and bounded region effects in both engines.
- DELTA: mode/root-class/`colorScheme`, token/source/diagnostic and canonical-CSS transcript; ready-arm B0→app-ready selected seed/computed atom equality and negative-case rejection/no-empty-atom result; invalid-default state/error visibility plus absent seed/atoms; CSP request, lifecycle/status transcript, critical-paint→queue→fetch time, OKLCH first/middle/final without gray, scene rect 0px shift, pause/offscreen work, per-control rendered effect, configured/effective zone count and pointer lean/burst/medium-gate/amplitude coordinates.

## Disjointness, artifacts, commit

W28 owns CSS ground, atom model, shell adapter and Aurora composition. W29 consumes atoms but owns Blob; W31 owns scheduling measurements. Commit ground/model, adapter, renderer and config as separate slices.

## Archaeology

Builds T-1/T-8/T-25/T-26/T-27/T-32/T-38/T-39/T-49/T-60 and folds U-F23 plus U-F3/F7/F11 without a consumer counter-filter.
