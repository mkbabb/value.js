> ARCHIVED 2026-07-17 — historical; superseded by the W40-series wave set + reformation/MAPPING.md (the single old→new junction). Read-only.

# V.W32 — Real-GPU Gestalt and Concise π/DELTA Record

**State:** PLANNED · **Meta-tranche:** V.A Adversarial Analysis · **Depends on:** W19–W31

## Goal

Challenge the whole product—not its mechanisms—and create the smallest clean-checkout visual record that can falsify V's material, proportion, motion and chromatic claims.

## Scope and file bounds

All routes/states, real-GPU Chrome/Safari, both schemes where material differs, complete-route 1440/390/320/actual-400%-zoom plus representative RTL/reduced modes; `docs/tranches/V/audit/pi/final/**` coverage map/manifest and only fixes routed to owners.

## Triumvirate and worktree

- **Specification:** `VISUAL-CONSTITUTION.md`, `OPTICAL-BENCH-COMPOSITIONS.md`, `PROPORTION-AUDIT.md`, `EVIDENCE.md`.
- **Plan:** fresh non-author review; compare warm/cool/achromatic seeds; attack all eighteen decided compositions, tiering, object truth, interruption, cross-route identity and feature exceptions; close the proportion register by mechanism; verify exact Card equations and each retained Card's `sm/content/quiet/opaque/false/false/off` data tuple, invariant `C=--spacing(4)` ladder and responsive workspace gutter. For Browse/Library, falsify the one-native-named-`<button type="button" aria-pressed>` specimen/identity seat: Card/article root is a noninteractive container with zero activation/focus/selection state and palette-Card `[inert]` count `0`, no palette `aria-selected`/option/listbox fiction, Card/face selected delta `0`, seat selection/focus visible, pointer/Enter/Space/AT pressed palette equal to selected-inspector identity, pressed count `0|1`, no-selection inspector absent and optional reorder handle separate. Verify the closed type-role matrix/About `66ch` measure, the binding boundary/reserve/retained-line inventory and PR-33–35's producer/Card/non-P122 inset matrices at 1440/390/320/actual-400%-zoom. Separately exercise ColorSpaceSelector's retained option/`aria-selected` semantics, sole `SelectItem` indicator/gutter and independent selection/focus/face deltas. Sample shared mechanisms orthogonally rather than Cartesian screenshot multiplication. Recreate the Lab/Blob crop at P019 floor/fluid/ceiling and on the complete route at 1440/390/320/actual-400%-zoom in the in-app Browser; try to falsify the exact type, line-box `G`, rendered-ink inequality, footprint, collision and painted-mass relations.
- **Implementation owner:** fresh gestalt lead plus independent visual/accessibility/performance readers; headed hardware only for GPU claims.
- **Execution:** open, inspect and capture every candidate visual π in the live routed in-app Browser before it enters the record. Use Computer Use or headed hardware only for named Browser-inaccessible native AT/OS/device/Safari/GPU surfaces. Standalone/headless Playwright is allowed only for named cross-engine binary automation, exhaustive repetition or synthetic timing that the in-app Browser cannot express, and its captures are inadmissible as visual-gestalt attestation.

## Current RED

U's real-GPU annex never ran and relied on owner attestation; cited frames are often ignored/untracked. Per-mechanism greens coexist with muddy material, action sprawl, proportional imbalance and janky swaps. A full Cartesian route×state×scheme×viewport record would be impossible and obscure defects.

**HISTORICAL (V-A108):** The one-sampler/no-nesting capture falsified the remaining product-side sampler hypotheses; the Chromium attribution is withdrawn by V-A125. **CURRENT V-A124/V-A125:** nested-filter reduction from approximately 95 to 5 and a reverted 3/0 backing-plane experiment likewise failed to remove the corruption; the honest 5/0 baseline is retained. W32 is not the owner of a browser-upgrade wait or product fix. After Glass W17/W33 correct or explicitly decline the live-backdrop adjacency, W32 independently audits the resulting native evidence and records π/DELTA. No Glass cover/reset, route/engine mask, second sampler, watchdog or material degradation is allowed.

## Completion evidence

 The tracked coverage map contains one canonical frame for each of the eighteen decided compositions; one witness per shared empty/loading/error/focus/motion mechanism; and only feature-specific exceptions. Scheme pairs exist only for material/color claims, viewport pairs only for topology changes, and real-GPU sequences only for glass/Aurora/Blob/motion. Every omitted combination points to its mechanism witness. Every proportion-register family is sampled and terminal. W32 reads W31's already-frozen `docs/tranches/V/audit/pi/w31/activation-record.json`, verifies its entry/transitive closure, byte lengths and hashes against the same production build, then freezes the `{urlPath,byteLength,sha256}` projection in URL-path ASCII order as the immutable SPA asset manifest plus the SHA-256 of its RFC 8785 bytes; a missing, extra or changed asset reopens W31 rather than being derived or repaired here. W33 may deploy only those exact bytes. Each canonical visual coordinate separately freezes `{route,fixtureId,fixtureDigest,seedToken,scheme,viewport,dpr,reducedMotion,fontReady,renderer,settlement}`. `settlement` names the semantic terminal used by that witness—app/route ready, transition terminal, `document.fonts.ready`, and, where applicable, producer Aurora/Blob `settled` or paused—plus a named phase offset only for a deliberate motion sample. No witness relies on wall-clock guessing, and screenshot bytes are not an equality contract for dynamic rendering. Browse/Library satisfy their exact one-Card-per-rendered-entity equation with no other Cards; each entity Card reports `sm/content/quiet/opaque/false/false/off`, invariant `C=--spacing(4)` five-rung ladder and Card/face selected delta `0`, while only the workspace gutter responds. Card/article root is a noninteractive container with zero activation/focus/selection state and palette-Card `[inert]` count `0`, keeping its live child reachable. Each entity has exactly one native named button of type `button` spanning specimen/identity; it uses `aria-pressed`, owns visible selection/focus, and no palette seat/root/field uses `aria-selected`, option/listbox or composite-selection semantics. Pointer, Enter, Space and AT yield the same pressed palette and selected-inspector identity; at most one seat is true, and zero pressed seats means no inspector. An optional reorder handle is a separate named control. Each other composition has Card count `0`; cartoon/grid/Card-selected variants are absent. All eighteen compositions satisfy the closed type-role matrix at all four arms, P019 is its sole paired-scale exception, and About prose is `≤66ch` outside its two named wider examples. All eight P122 workbenches have boundaries `[]`/reserve `none`; only the five Admin fields retain the named adjacent-row separator, with no terminal rule, and every other divider is absent. At 1440/390/320/actual-400%-zoom, PR-33's six normatively mapped measurements—outer stage+inspector inline/block inset, stage↔inspector gap, action-content inline/block inset and adjacent-action-child gap—equal computed producer values within `±0.5 CSS px` with no consumer override, PR-34's exact Card tuple/ladder, pressed-seat semantics and separate workspace gutter match with no non-Card Card-padding use, and PR-35's non-P122 outer/section/row/action matrix matches with no collapsed-margin or Card-padding substitute. The 400% witness is the complete live route at actual in-app Browser zoom, not responsive emulation. Picker proves the P019 paired `1/√φ`, exact line-box gap `G`, and `I_after ≤ min(φG, I_before − G)` within `±0.5 CSS px` at all clamp and four routed arms, plus footprint center displaced one title-gap toward the chassis center, zero readout collisions, sole effective `bodyRadius 0.26→0.325`, rendered central-alpha ratio `0.660192…0.660415`, centroid error `≤1.342%`, hull inset `12.0%`, AA edge `≤1.885 CSS px` DPR1/`≤0.948 CSS px` DPR2, equal WebGPU/WebGL2 alpha area and 0px W29 chassis/footprint/seat movement in every routed arm. ColorSpaceSelector separately retains `role="option"`/`aria-selected`; it has no `hide-indicator` or local pill/halo/pseudo/marker, exactly one producer marker agrees with the option and `aria-selected`, seat/marker selected delta and independent focus delta are nonzero in monochrome/forced colors, and face delta is `0`. The global pane selector, contenteditable headline, interactive WatercolorDot face, automatic duplicate dividers and specimen Copy listener are absent. After W31, lazy-route/dialog, all three font arrivals, renderer failure/status/pause and tuned boundaries rerun affected assistive/reflow journeys. Each claim has decisive π/DELTA; two fresh gestalt passes enumerate zero new family or open visual gap.

## π / DELTA

- π: W31's production-derived activation-record digest; the orthogonal clean-checkout manifest defined in `EVIDENCE.md`; eighteen canonical composition frames plus the minimum shared-mechanism and feature-exception witnesses, with real-GPU sequences only where the claim needs them; the Picker/Blob pair is explicit. Every frame carries its deterministic coordinate and semantic settlement tuple. Existing composition captures carry ordinary PR-33–35 data-tuple/rect/computed-style/type strips at 1440/390, with only the corresponding mechanism strips added at 320 and actual 400% in-app Browser zoom.
- DELTA: activation-record→SPA-manifest missing/extra/hash mismatch count `0`; material, proportion, exact Card/boundary/reserve/retained-line/type inventory, interrupted motion, color continuity, interaction and performance measurements at gestalt level, each tied to the exact π witness it explains. Across 1440/390/320/actual-400%-zoom, PR-33 records its six normatively mapped producer equalities and zero consumer copies/overrides; PR-34 records exact Card tuple, invariant `C=--spacing(4)` five-rung ladder, responsive structural gutter, Card/article-root activation/focus/state count `0`, palette-Card `[inert]` count `0`, one native named button/type per entity, palette-seat `aria-selected`/option/listbox/composite count `0`, pressed count `0|1`, pressed-ID↔selected-inspector-ID equality, no-selection inspector absence, pointer/Enter/Space/AT parity, separate optional reorder handle, Card/face selected delta `0`, seat selection/focus delta and zero non-Card Card-padding use; PR-35 records exact outer/section/Admin-row/action rungs, About `66ch`/named exceptions and zero collapsed-margin/Card-padding substitutes. The type board records exact role/family/rung equality. Picker records selector producer-marker/option/ARIA/focus/face deltas, type, line-box `G`, `I_before`/`I_after` inequality, chassis-relative footprint displacement, collision count `0`, final paint centroid/hull/ratio and unchanged outer rect at all four arms. The coordinate tuple and named numeric tolerances, not screenshot-byte identity, are the W33 comparison authority for dynamic frames.

## Disjointness, artifacts, commit

W32 authors evidence only after owner waves are green and consumes rather than authors W31's activation families. Any product or asset-graph fix reopens and commits in its owning wave, then restarts the two-pass clock. Commit final frames/manifest as `docs(v-w32): record real-gpu optical-bench gestalt`.

## Archaeology

Builds U-F54 and the owner-attested annex as actual evidence; retires T/U screenshot hoards and headless false reads.
