> ARCHIVED 2026-07-17 — historical; superseded by V-PRIME.md + the wave specs. Read-only.

# Tranche V — Material Defect Addenda

These are normative amendments surfaced by fresh non-author audits after the first complete formation candidate. They do not create a fifth meta-tranche, a new gate, or extra waves. Each addendum replaces the defective mechanism in its owning specification and wave; implementation follows the amended wave exactly.

The governing economy is strict: prefer one shared authority over a queue, scan, alias, adapter or second path; delete the displaced mechanism; assign implementation through the owning wave's agent orchestration; validate through ordinary code, consumer manifests, real-stack behavior and Browser-first visual inspection. A later defect joins this register only when it has concrete file/behavior evidence and materially changes architecture, product truth or release evidence.

**V-A168 current-stage rule:** Rows before V-A130 preserve defect chronology and terminal dispositions; V-A131–V-A168 record the current Glass source/pack boundary, missing π artifacts, legacy API canon, routed UI and wire/access defects, the acyclic W17/W33 publication rail, W18's topology dependency, W29's reconstructible-before requirement and Q003's product-green release floor. Value4 and Keyframes6 are immutable; U-F77/B1/B14, residual consumer-cut producer-order language, scattered Glass handoff prose, stale non-historical Value3 documentation, and superseded stage labels are historical/retired. The live boundary is the sliced Glass W17a→W33a→W17b rail plus the named frontend/API waves below; no producer re-booking, K_NEXT rehearsal, alias, fallback, new wave or new gate is live.

### V-A136 — Q003 backing-plane correction (2026-07-16)

The inert Configurator backing-plane experiment reached 3 active filters / 0 nested filters and still failed native capture. It is fully reverted; the honest 5 active / 0 nested baseline is restored and Chromium-only attribution is withdrawn. No sampler, mask, cover, degraded material, route/engine branch or Keyframes/Drawer workaround is admissible. W17/W33 must produce repeated forward/reverse Browser captures with zero slab, occlusion corruption and lost target. A failed adjacency hypothesis may be declined, but the corrupt product may not. **Disposition: FOLD into W17a/W33a, ACTIVE RED; no new meta gate.**

### V-A137–V-A168 — Current frontend, API and execution corrections (2026-07-16)

| ID | Concrete defect | Normative correction / disposition | Owner |
|---|---|---|---|
| V-A137 | `demo/@/components/custom/color-picker/controls/SpectrumCanvas/SpectrumCanvas.vue:3–20,132–200` is pointer-only `role="img"`; it has no focusable keyboard path or named numeric S/V axes, contradicting W21's pointer↔keyboard↔numeric equality. | Build W21's shared axis controls and named numeric S/V inputs; delete canvas-local pointer-only semantics. Born RED until Browser parity and π/DELTA are recorded. | W21 → W30/W32 |
| V-A138 | `demo/@/components/custom/color-picker/controls/ComponentSliders/ConsoleRail.vue:12–35` declares a tablist/tab set without `aria-controls`/tabpanel linkage; `ComponentSliders.vue:48–87` provides no stable association. | Build one semantic tab/tabpanel association with selected-row identity and keyboard/AT parity; no role fiction or unlabeled tab rail. Born RED. | W21 |
| V-A139 | `demo/@/components/custom/color-picker/display/ColorNutritionLabel.vue:92–141` wraps a non-focusable cursor `<div>` in TooltipTrigger with empty TooltipContent and hover-only handlers; the same file has a static Alert plus four/repeated section dividers (`:8,15,40,68,90,146`) despite W18's zero-Alert/zero-repeated-divider/zero-empty-tooltip contract. | Build the named conversion-path graph as ordinary readable content, delete the empty Tooltip/false trigger, Alert and redundant dividers, and retain only semantic section structure. Born RED. | W18 |
| V-A140 | `ColorSpaceSelector.vue:200–209` uses `--type-display-2` while `ColorComponentDisplay.vue:136–140` uses `--type-display-4`; the intervening scale is two rungs (`1/φ²`) rather than W20/P019's adjacent `1/√φ` paired label/headline relation. | Build the shared P019 paired clamp at one golden rung; preserve the numeric headline and do not mint a local type token. Born RED. | W20 with Glass P019 liaison |
| V-A141 | `PaletteCard.vue:5–27` makes the Card root a cursor/click/press handler and adds producer cartoon/cast behavior; it has no native pressed seat, so Enter/Space/AT cannot equal pointer selection. | Build W22's one native named `<button type="button" aria-pressed>` seat spanning specimen/identity; delete root activation/focus/selection and local cartoon/hover action paths. Born RED. | W22 → W23/W30 |
| V-A142 | `SwatchHoverMenu.vue:13–50` makes `WatercolorDot tag="button"` the primary swatch, hides hover actions with `aria-hidden`, and uses a separate touch Popover path. | Build one shared geometric seat and pointer/keyboard/AT action path; WatercolorDot is face-only and hover-only action panels are deleted. Born RED. | W22/W23 |
| V-A143 | Mounted `POST /palettes/:slug/restore` exists at `api/src/modules/palette/routes/crud.ts:161–171` but is absent from `meta/route-table.ts:22–83`, so `/docs` and `/openapi.json` are false. | Retire the old restore handler under the W11/W16/W23 palette operation cut; remove the mounted path and make generated documentation agree with the remaining target surface. No route-table patch or alias. | W11/W16/W23 |
| V-A144 | `palette/schema.ts:79–93` accepts retired `offset`, `visibility`, and `userSlug`; `crud-list.ts:9–15,204–218` is cursor-only and silently ignores them. `crud-list.ts:48–57` also turns malformed/stale cursors into a restart from page one. | Build one cursor-only decoder: reject retired/duplicate/query-mismatched or malformed cursors with explicit `422` errors; never restart or strip unknown query semantics. | W1/W14/W16/W22 |
| V-A145 | The server exposes `/sessions/me` (`api/src/modules/session/routes.ts:58–62`) but the demo deliberately deleted its client wrapper and barrel export (`demo/@/lib/palette/api/sessions.ts:8–28`, `api/index.ts:37`); `client.ts:119–125` clears the token on any 401 instead of re-deriving auth. | Build the stated cookie/session recovery contract with a real `/sessions/me` consumer; delete direct token erasure and bearer/local-storage transport. | W15/W23 |
| V-A146 | Idempotency is opt-in (`api/src/platform/http/idempotency.ts:87–99`), while most mutations omit the key: palettes update/publish/unpublish/delete/revert/fork, sessions, color propose and admin writes. Replay storage (`:59–66,121–125,141–146`) also drops original ETag, Set-Cookie and other response headers. | Build mandatory UUIDv7 idempotency for every mutation with durable replay of the exact status/body/header tuple; missing keys fail explicitly. No opt-in bypass or replay shim. | W10/W13/W15/W16/W23 |
| V-A147 | `versions.ts:45–49` resolves a version hash without checking its `:slug` lineage (`service/versions.ts:99–105`); revert similarly accepts an arbitrary hash (`:131–135`). | Retire the legacy versions/hash/revert mounted paths under the palette operation cut; do not patch around the cross-slug leak with another route or alias. | W10/W16/W23 |
| V-A148 | `demo/@/lib/palette/api/client.ts:101–105` always sends `Content-Type: application/json`, including bodyless GETs, violating the closed wire tuple. | Build the transport cut so GET/no-body requests omit both body and Content-Type; keep conditional headers only for actual bodies. | W15/W16 |
| V-A149 | W17's current-red paragraph (`W17.md:33,42,44,46,51`) still speaks as if 1,131/771 builds, `G_NEXT` fonts and producer bootstrap/rehearsal work are current; these contradict V-A136's Glass-only boundary. | Mark the old counts/plan/π labels historical and keep W17 live only for fresh Glass7 strict/native/Q003, stale canon and routed mount. | W17/W33 |
| V-A150 | W33 completion prose (`W33.md:24,52–70`) still assigns palette/data-cut/edge/deploy work that W33's current Glass-only bounds (`:17`) exclude. | Mark those duties verify-only historical; assign live API/data/assistive/export rows to W1/W10–W16/W22–W24/W30–W32. W33 owns only fresh Glass7 pack/provenance and downstream registry lock/mount. | W33 + named owning waves |
| V-A151 | W7 still contains imperative bootstrap-emission completion text (`W7.md:29,33`) after Value4 is immutable. | Mark the entire bootstrap paragraph historical transport; no second Value pack/rehearsal or bootstrap install is live. | W7/W17 |
| V-A153 | `readoutReservation.ts:38–39,192–207` forces Lab `readoutLineCount=2`; `ColorComponentDisplay.vue:148–166` enforces `min-height: lines*1.12em` and bottom alignment, creating the screenshot's blank first line above ordinary values. `ColorPicker.vue:94–98` can also leave the Blob absent while `seat.css:81–90` reserves its space. | Build actual wrapping below the headline and require a settled idle/B4 Browser witness: no blanket worst-line reserve and no unpainted Blob acreage. Preserve `.325` calibration; any footprint change must re-baseline W20/W29. | W17/W20/W29 |
| V-A154 | Live Zod bodies use permissive `z.object` (`api/src/modules/palette/schema.ts:41–73`, color `schema.ts:7–20`, admin `schema.ts:15–62`) that strips unknown fields; `palette/format.ts:19–48,68–90` emits legacy `userSlug`, visibility/tier, deletion/fork/hash/source fields and optional `voted` outside the closed DTO contract. | Build strict request/response decoders: unknown, duplicate, null and trailing fields fail `422`; format only the exact discriminated PaletteCard/ReleaseDetail/Workspace DTOs. Delete legacy formatter fields and permissive masking. | W10/W14/W16/W22–W24/W30 |
| V-A155 | `meta/routes.ts:34–60,129–184` hard-codes a Mongo-only health check and generic OpenAPI 200/401 projection; it omits platform readiness, exact Problem responses, request/query schemas and 403/404/409/412/422/428/503 contracts. | Build the mounted typed HealthReady/OpenAPI projection from the exact route/schema owners; delete the hand-kept `/docs`/OpenAPI false-green surface and root sentinel rather than adding another table. | W10/W33 |
| V-A156 | `palette/service/crud.ts:44–68` returns private/unlisted palettes by slug; votes/forks/flags similarly accept non-public/deleted records (`service/votes.ts:39–47`, `forks.ts:45–50,93–104`, `flags.ts:31–35`). Public list filters to `visibility:'public'`, so detail/social access disagrees. | Build one shared active-public Release/access predicate for detail and social operations; retire old paths where the target cut says so. No route-local mask. | W11/W13/W14/W16/W22 |
| V-A157 | `Dock.vue:15,192–204` still renders global `PaneSegmentedControl` and `useViewManager.ts:23,51–93` retains mobile pane override/split state, contradicting W19's single document-scrolling scene. | Delete the global selector and legacy split state; preserve only feature-owned tabs and document-scroll routes. | W19/W30 |
| V-A158 | `PaletteCardGrid.vue:3–5` hard-codes `min-h-[120px]` even for empty states, creating filler acreage where W22 requires content-hug primary invitation and collapsed secondary shelf. | Build semantic empty/populated state layout with no filler/ghost-card reserve; do not replace it with another arbitrary minimum. | W22 |
| V-A159 | `palettes.ts:42–55` sends `/palettes?` when options are empty, violating the wire rule that no-query routes have no trailing `?`. | Build canonical URL construction that emits `/palettes` for an empty query and rejects duplicate/retired query keys. | W15/W16 |
| V-A160 | `PROMPT-RECAP.md §1` names only sessions through 2026-07-14; substantive Glass session `e79fce3f-d24e-4654-8b27-d029653fedbe` on 2026-07-16 is absent, including proportional-card, gate-abrogation, versioning and re-audit asks. | Extend W0's normalized/deduplicated session census through that session, retain substantive asks, and exclude meta-command/task-wrapper noise. | W0/W17/W33 |
| V-A161 | `api/src/modules/color/service/queries.ts:87–130` combines ranked text search with a regex substring fallback over `name`/`css`; the target `/color-names/search` contract is bounded normalized byte-prefix search, so fuzzy/regex fallback can return unbounded or differently ordered results. | Build the one bounded normalized-prefix search and explicit limit/cursor semantics in W14/W16/W22; delete the regex alternate and legacy DTO path. No fallback search or dual result order. | W14/W16/W22 |
| V-A162 | `demo/@/lib/palette/api/api-problem.ts:35–45` manufactures `about:blank`/`statusText` when a response is not JSON or not a Problem DTO, masking malformed server errors; the hand-kept OpenAPI projection also omits the closed error set. | Build one strict Problem decoder that rejects malformed payloads explicitly and derive every declared error response from its route owner. Delete the fabricated fallback and do not translate malformed errors into success-like status text. | W10/W15/W16/W23 |
| V-A163 | `api/src/modules/color/schema.ts:7–20`, `routes.ts:70–78`, `service/proposals.ts:20–24,43–49` and `demo/@/lib/palette/api/colors.ts:31–39` accept caller-supplied `contributor` attribution. | Remove `contributor` from the public body/DTO and derive proposal attribution solely from the authenticated Principal/session; unknown keys fail explicitly. No caller-controlled provenance field or compatibility alias. | W13/W16/W22 |
| V-A164 | `api/src/modules/palette/service/forks.ts:146–160,171–196` returns raw fork documents and ancestor lineage, including owner/hash/deleted/private fields, without a per-hop public-release check or unavailable redaction. | Retire the raw fork/provenance routes under W12–W16/W23 and expose only minimal release-scoped `PublicForkSummary`/`ProvenanceStep`; private, trashed or purged hops become `{kind:'unavailable',ordinal}`. No route-local masking. | W12/W16/W23 |
| V-A165 | W17 owns the post-publication value lock refresh/routed mount, W33 alone owns Glass publication, W33 formally depends on completed W17/W30–W32, and W18 depends on W17. Taken literally, Glass7 cannot publish until after the product whose mount requires Glass7. | Keep 34 waves and split the existing owners into an acyclic rail: **W17a** fresh strict/native Glass rehearsal → **W33a** nonterminal Glass7 publish/registry-equality slice → **W17b** value registry refresh and routed-mount proof → W18–W32 → **W33b** final canon/deploy/release close. No slice may claim its parent wave complete early; W33a is the only early publication authority. | W17/W33, V DAG and handoff |
| V-A166 | W18 depends only on W0/W17 even though its implementation paths and ownership presume the physical `demo/app`, `demo/picker`, `demo/palettes` and feature topology created by W5/W6. Starting W18 first would style transient paths and collide with W6. | Add W6 as an explicit W18 dependency. W18 begins only after W0, W6 and W17b are complete; this changes no scope or wave count. | W18 and every downstream frontend wave |
| V-A167 | The `.325` W29 source change landed before W17 wrote the required `.26` producer-baseline manifest. Capturing current `.325` bytes as the before image, or toggling the live tree and calling that history, would fabricate the `b₀` coordinate. | Reconstruct W17's before packet only from an identified immutable pre-change source tree/blob plus the frozen seed/color/config/frame protocol; record that tree and every source/test/config hash. Pair W29's current after rows to it. If the exact before identity cannot be recovered, W17/W29 remain RED. | W17/W29/W32 |
| V-A168 | V's “correct or evidence-backed decline” shorthand is weaker than the active Glass Q003 owner contract: disproving one adjacency hypothesis may decline only that experiment, while any slab, occlusion or lost target remains a product defect and forbids an artifact. | A Glass7 rehearsal/publication requires repeated native forward/reverse cycles with zero black slab, zero lost target and zero occlusion corruption at the honest 5/0 baseline. An evidence-backed decline may retire only a causal hypothesis or proposed grid change; it cannot close Q003 while corruption remains. | Glass Q003 PLAN; W17a/W33a/W32 |

| ID | Material defect | Normative correction | Owners and decisive evidence |
|---|---|---|---|
| V-A119 | The Glass consumer had closed its ordinary major batch against a provisional/held graph, which could be mistaken for a Glass7 release even though the immutable Value4→Keyframes6 pair had only just become available. | Credit the Glass batch only as **LANDED / pre-release**: one Chip declaration repair, typecheck, 1,127 unit tests, 769-module library build, 3,572-module production demo build, and strict package closure are green against exact `/color` `/css` `/easing` ownership. **Superseded by V-A123 for stage wording:** the registry-only Value4/Keyframes6 install is now proven as one physical deduped chain. Glass still must close Q003 in the native Browser and cut/publish one fresh Glass7 artifact. The 966,350-byte rehearsal remains held/defective; no source, rehearsal, or registry coordinate earns release credit. Keyframes' post-Glass demo/deploy edge stays downstream until that immutable Glass7 exists. | Glass task `019f6610-6022-7df1-95b3-472b17a64656`; W17/W33; `CONSUMER-CUT.md`; no new wave or meta-gate. |
| V-A120 | The July 16 Picker capture exposed a second composition defect not covered by the existing title-gap or raw-buffer rows: the numeric headline falls into an enormous unowned vertical field before the channel annotation, and the calibrated Blob can be geometrically correct in a transparent buffer while absent or occluded in the composed route. The same capture shows a dim home mark, floating eye and unlabeled vertical ellipsis whose purpose/state is not named. | **W20/W29:** in the settled routed Picker at every named viewport/clamp arm, the headline→next-instrument interval is either occupied by a visibly composited noninteractive Blob/rail or collapses to the named shared rhythm; record screen-space bounds, paint/compositing and occlusion, not buffer geometry alone. Preserve the `.325` producer radius and 0px chassis/seat/footprint law; do not add card-local reserves, opacity/z-index covers, a second Blob or a screenshot-only pass. **W19/W23:** make one explicit site decision for each dock affordance: retain one route-identifying control only; remove the redundant home mark when Picker text already routes; give `…` a persistent named menu purpose plus expanded state or remove it; give the eye a durable on/off mode/state or remove it. Primary labels remain perceptible in both schemes; no tooltip proliferation substitutes for naming or state. | `PROPORTION-AUDIT.md`, W19/W20/W23/W29, `VISUAL-CONSTITUTION.md §7`; July 16 in-app Browser capture; active Glass task `019f6610-6022-7df1-95b3-472b17a64656`. |
| V-A121 | Current-stage prose still interleaves historical bootstrap/held-rehearsal instructions with the immutable producer boundary: PROMPT-RECAP rows 208, 217, 224, 230, 235–239; W17's long plan; W33's generic ordered publish paragraph; and the active-session relay can make Value4/Keyframes6 look unpublished or make a source-green Glass batch look release-complete. | Mark those passages **HISTORICAL** or rewrite them to the live stage: Value4 and Keyframes6 are immutable; Glass's ordinary batch is LANDED/pre-release; only fresh registry-only Glass7 bytes, strict/native Q003, routed mount and post-Glass consumer locks remain. W33 owns the fresh Glass7 SHIPPED coordinate; W17/addenda retain the already observed producer coordinates. No new wave, alias, lock, fallback or release claim is created by prose repair. | `PROMPT-RECAP.md`, W17/W33, `CONSUMER-CUT.md`, `coordination/CONSTELLATION.md`; V-A119 and current immutable handoffs. |
| V-A122 | The active Glass BI Q023 shorthand still describes an asymmetric backdrop-blur engage as “drawer + sheet,” but the canonical Drawer performance/material law forbids per-frame Drawer blur-radius ramps: the Drawer owner has already discharged R23 with fixed-radius blur plus alpha dim, and the ≥2-consumer floor for a new engage register is not met by the Drawer alone. | Amend Glass Q023 before implementation: keep any graded sheet-edge band as a separate sheet/overlay-owned surface, and either identify a real second consumer for an engage register or decline that mint. Drawer remains material-constant with fixed blur and a scalar-driven dim; no Drawer blur-radius animation, new parallel token, alias, or gate farm. This is a Glass-plan correction, not a new Value wave or a reason to delay the Glass7 artifact beyond its existing strict/native/release obligations. | Glass BI `addenda/PLAN.md:99–103`, `waves/BI.W-DRAWER-PERF.md:6,36–39,91–93`, `addenda/reports/media-analysis.md:119–123`; Glass task `019f6610-6022-7df1-95b3-472b17a64656`. |
| V-A123 | Current-stage release rows still mix a registry graph that is already proven/installed with future-tense regeneration, and V-A120's Blob/Dock findings exist only in Current RED prose. That permits a wave to satisfy its old completion text while leaving the measured visual defect untouched; several historical rows also still instruct Keyframes/Value rehearsal work after their immutable releases. | Rewrite current rows to say Glass already has the one-physical registry Value4/Keyframes6 graph and now needs only strict/native validation plus a fresh Glass7 pack. Mark post-Glass producer instructions historical. Promote V-A120 into W19/W20/W29 completion and DELTA: Dock controls require terminal retain/remove/state outcomes; Picker requires screen-space Blob bounds/paint/occlusion and headline→rail occupation or an intentional shared-rhythm collapse. No new gate, wave, producer rebuild or consumer workaround. | `PROGRESS.md:4,24,42,58–59,86`; `coordination/CONSTELLATION.md:84,91`; `ARCHITECTURE.md:833–834`; `CONSUMER-CUT.md:205,211–212,218`; W19/W20/W29 completion and DELTA sections. |
| V-A124 | Native Q003 still corrupts compositor plates adjacent to the WebGPU Aurora surface after nested filtered regions were reduced from approximately 95 to 5. Filter count/nesting reduction is therefore not a sufficient product cause; the live-backdrop adjacency relationship remains unisolated. | **Glass W17/W33 only:** isolate the actual live-backdrop adjacency trigger with native in-app Browser captures of forward/reverse interaction and settled compositor plates. Keep Drawer fixed-radius blur plus scalar dim, preserve the WebGPU/WebGL2 product surface, and do not add a canvas mask, route/engine mask, filter-count target, z-index/opacity cover, sampler workaround, Keyframes change or parallel material token. A fresh Glass7 candidate remains RED until the causal adjacency is corrected or explicitly declined with an evidence-backed Browser disposition; the old rehearsal gains no credit. | Active Glass task `019f6610-6022-7df1-95b3-472b17a64656`; Keyframes evidence-only relay; W17/W33 Q003 native rows; no new wave/meta-gate. |
| V-A125 | A single inert Configurator backing-plane experiment temporarily reduced the route to 3 active filters / 0 nested but still failed native V-A95. The experiment was fully reverted; the honest baseline is 5 active / 0 nested after retaining only the valid nested-cell simplification. The prior Chromium-only attribution is withdrawn, and V-A95 remains ACTIVE RED. | **Glass W17/W33 only:** retain the reverted honest 5/0 baseline and isolate the actual live-backdrop adjacency trigger with repeated native in-app Browser forward/reverse captures. Treat the Chromium fix as background prior art, not a product diagnosis or wait condition. No backing-plane, filter-count target, mask/cover, sampler, route/engine workaround, Keyframes/Drawer change, package/lock mutation or Glass7 release credit; fresh Glass7 remains RED until correction or an evidence-backed explicit decline. | Active Glass task `019f6610-6022-7df1-95b3-472b17a64656`; W17/W33 Q003 native rows; V-A124 predecessor; no new wave/meta-gate. |
| V-A126 | U-F77/B1/B14 close-book wording still left the already-discharged Value4→Keyframes6 producer order looking future-booked, inviting forbidden re-booking or a second K_NEXT rehearsal. | **HISTORICAL / RETIRED:** Value4 and Keyframes6 remain the immutable producer rows. W17/W33 alone own the fresh Glass7 candidate, Q003/native closure and downstream adoption; delete any implication that Value or Keyframes must rebuild, repack or republish for V. No new wave or meta-gate. | `DISPOSITION-LEDGER.md:95,145,158`; W17/W33; immutable registry coordinates above; prompt/registry/convergence records. |
| V-A127 | After V-A126, residual Consumer-Cut/W2 prose still presented `K_NEXT`, bootstrap emission and Keyframes consumer migrations as future execution, despite immutable Keyframes6. | **HISTORICAL / IMPLEMENTED:** convert the producer covenant and W2 census to completed evidence, pin sibling consumer targets to immutable Keyframes6, and reserve live BUILD/SHIP language for Glass7 only. This is the same producer-order defect family; no new wave, rehearsal, alias or fallback. | `CONSUMER-CUT.md:5,17–18,144–184,199,211–214`; `waves/W2.md:5,33`; W17/W33; immutable Keyframes6 coordinates. |
| V-A128 | Glass asks were correct but scattered across historical Q003, HeaderRibbon, declaration, and release paragraphs, allowing an executor to miss the single current boundary or mistake held bytes for a candidate. | **Glass execution brief:** the active task `019f6610-6022-7df1-95b3-472b17a64656` consumes only registry Value4 + Keyframes6; closes strict declaration reach and **persistent-only** HeaderRibbon behavior (the opt-in-collapsible arm CUT per the 2026-07-16 glass mark; V-A92 superseded-in-part); repeats native in-app Browser forward/reverse Q003 captures from honest 5 active/0 nested baseline; corrects live-backdrop adjacency or records an evidence-backed decline; then emits one fresh Glass7 rehearsal. W17 records the handoff; W33 alone publishes and records SHIPPED, followed by downstream locks and routed mount. No held archive, browser-only wait, filter target, backing plane, sampler, mask, Keyframes/Drawer change, alias, fallback, file link or stale lock earns credit. | Active Glass task above; W17/W33; `coordination/CONSTELLATION.md`; `CONSUMER-CUT.md`; no new wave/meta-gate. |
| V-A129 | The live first-party demo canon still advertises retired Value3 surfaces: `demo/CLAUDE.md:141–146` names the root, `/parsing`, and `/units` as published exports, and `demo/@/utils/utils.ts:10–19` describes a root-barrel import as an active library concern. This contradicts the exact-seven clean break and leaves a future consumer or auditor with a false import contract even though active TypeScript imports are subpath-correct. | **W4/W17:** remove or mark those passages historical in the owning consumer tree, rewrite the integration section against the exact seven Value4 entries and the actual `/color` + `/css` + `/easing` Glass slice, and rerun the tracked-doc/consumer census. Do not restore a root export, add an alias, or count comment-only cleanup as a runtime migration. Until that correction is landed, documentation/canon closure and the routed consumer remain RED. | `demo/CLAUDE.md:141–146`; `demo/@/utils/utils.ts:10–19`; W4 clean-break census; W17 downstream refresh; no new wave/meta-gate. |
| V-A130 | After V-A129, current-stage summaries still carried V-A123 labels and W8 presented its shipped producer specification as an unlabeled future `Goal`/`Plan`. That leaves the authority clock and a historical wave plan open to contradictory interpretation. | **W3/W4/W17 documentation coordination:** label W8's goal/plan historical and advance the current summaries to V-A129's Glass-only boundary, preserving older V-A123 text only as chronology. Re-run the 34-wave authority scan; do not create a new wave, gate, producer migration or release claim. | `waves/W8.md:7–18`; `DECISIONS.md:64–65`; `coordination/CONSTELLATION.md:43,52–53`; `research/CONVERGENCE.md:126`; `audit/AGENT-REGISTRY.md:216,223`; no new wave/meta-gate. |
| V-A131 | The active Glass source has advanced materially beyond the older V-A119 count, but the current source/lock is dirty and the BI coordination corpus still contains pre-publication Value3/Keyframes5/Glass5 language. Treating the dirty graph or the 966,350-byte rehearsal as Glass7 would be a release-evidence lie; treating the old BI prose as current would reopen an already immutable producer boundary. | **Glass W17/W33:** credit only the registry-only source/lock as LANDED/IMPLEMENTED; the earlier 176-file/1,131-test/771-module report is superseded by V-A134's direct 176-file/1,132-test source run and pack/build RED evidence. Annotate the concrete stale BI rows at `docs/tranches/BI/addenda/REGISTRY.md:45–49,136,194–198`, `docs/tranches/BI/addenda/DISPOSITIONS.md:201`, `docs/tranches/BI/coordination/asks-and-consumes.md:79,104–116`, `docs/tranches/BI/addenda/reports/gates-consumers.md:154–159,172–198`, `docs/tranches/BI/addenda/reports/r2-consumer-verify.md:57–84`, `docs/tranches/BI/addenda/reports/wave-matrix-p063-p133.md:119–120`, and `docs/tranches/BI/coordination/valuejs-inbox-2026-07-15-v-formation.md:113–116` historical/superseded by immutable Value4 + Keyframes6 and V-A130. Keep Q003 ACTIVE RED on the honest 5 active/0 nested baseline; require a fresh packed strict/native Glass7 artifact and provenance before downstream lock or routed-mount work. No held archive promotion, source-to-release inference, alias, fallback, workaround or package/lock mutation in V. | Active Glass task `019f6610-6022-7df1-95b3-472b17a64656`; held archive `/private/tmp/glass-v7-rehearsal-20260716-kf-indent-final/mkbabb-glass-ui-7.0.0.tgz`; V-A125/V-A134; no new wave/meta-gate. |
| V-A132 | The π/DELTA artifacts named by the specifications are absent on disk, so source slices, raw-buffer numbers and prose cannot be promoted to visual completion. Missing paths include `audit/pi/w17/p047-producer-baseline/manifest.json`, `audit/pi/w29/picker-painted-component/manifest.json`, `audit/pi/w31/activation-record.json`, `audit/pi/w31/interaction-manifest.json`, and the W32 final-coverage directory; W0/W1 also lack `research/state-atlas.md`, `audit/request-registry.md`, `research/palette-failure-lab.md`. `EVIDENCE.md §6` explicitly disqualifies an absent cited artifact. | Keep W0/W1/W17/W20/W29/W31/W32 **BORN RED / MISSING ARTIFACT** with their named owners. Do not infer π/DELTA, routed gestalt, producer baseline, 0px geometry, activation matrix or palette-failure closure from implementation/tests. The artifacts must be created by their owning waves and inspected in the in-app Browser where specified; no new gate or wave is created. | `EVIDENCE.md:42,45,54,57–58`; `waves/W0.md`, `W1.md`, `W17.md`, `W20.md`, `W29.md`, `W31.md`, `W32.md`; `docs/tranches/V/audit/pi/` absent on disk. |
| V-A133 | The live API still exposes a false-green “SHIPPED/single source of truth” hand-kept route table and legacy routes, while the tranche requires generated exact-target routing and deletion of `/docs`, `/openapi.json`, the root liveness sentinel and retired CRUD/auth surfaces. Concrete sources are `api/src/modules/meta/route-table.ts:1–8`, `api/src/modules/meta/routes.ts:2–16,125,191`, `api/src/app.ts:83–86`, `demo/@/lib/palette/api/client.ts:118–123,145`, and `demo/@/lib/palette/api/sessions.ts:12–24`; they still use bearer/session-token and legacy `/sessions`, `/sessions/login`, `/palettes/mine`, `/vote`, `/flag`, `/docs` and related paths. | W1/W10–W16/W22–W24/W30 must treat the current API/client graph as **RED**, enumerate each legacy endpoint migration or retirement against the exact 50-target surface, delete the hand-kept route table and custom docs/openapi/root sentinel, and replace token/bearer auth with the stated recovery/session contract. No route alias, redirect, hidden handler or comment-only “SHIPPED” label earns credit. | `PALETTE-FACILITIES.md §2–3`; `PALETTE-WIRE-CONTRACT.md`; `PALETTE-DOMAIN.md`; named source paths above; no new wave/meta-gate. |
| V-A134 | A fresh direct Glass audit supersedes the earlier ordinary-count claim: current dirty HEAD `c181f0a7` passes `npm test` at 176 files/1,132 tests and `npm run typecheck`, but Vite cannot write `node_modules/.vite-temp`, npm pack cannot use the root-owned cache, and `verify:package` therefore has no candidate to inspect. The only archive remains the stale 966,350-byte held rehearsal (old base/lock, old package surface, no current optional peer changes), while native Q003 cycles 2/3 still show black compositor slabs beside WebGPU on the honest 5/0 baseline. | Reclassify W17 as implementation/test/typecheck **LANDED but PRE-RELEASE RED** and W33 as downstream-blocked. Do not claim a current Glass build, pack, verifier, native close or Glass7 coordinate from the dirty source or held archive. The network-capable Glass owner must rerun clean build/pack/strict verifier and the required native forward/reverse packet; retain the honest 5/0 baseline and all no-workaround prohibitions. | Active Glass task `019f6610-6022-7df1-95b3-472b17a64656`; `/private/tmp/glass-aurora-v-a95-cycle-2-immediate.png`, `...cycle-3-immediate.png`; held archive `/private/tmp/glass-v7-rehearsal-20260716-kf-indent-final/mkbabb-glass-ui-7.0.0.tgz`; no source edits or release credit. |
| V-A135 | The live demo router still omits first-class `/about` and `/easing` routes despite the closed eleven-route inventory, and its current route components are null-render stubs. `demo/@/router/index.ts:22–35` therefore cannot prove route reachability or the proportional UI scope described by W19/W27. | W19/W27 keep direct `/about` and `/easing` route bodies and Dock reachability **RED** until implemented and Browser-verified; route names in contracts do not count as mounted product surfaces. No redirect, stub, alias or route-table assertion substitutes for rendered route evidence. | `OPTICAL-BENCH-COMPOSITIONS.md`; `PROPORTION-AUDIT.md PR-15`; `waves/W19.md`, `W27.md`; `demo/@/router/index.ts:22–35`; no new wave/meta-gate. |
| V-A1 | Glass P122 was labeled implemented while the live producer still exposed four old spacing variables and its contract test forbade `instrument-(dial|control)`. | The active post-BI Glass rail targeting evidence-derived `G_NEXT` restored exactly six semantic region-spacing properties and matching source/test assertions; independent title gap remains separate. Native 1440/390/320/400%-zoom evidence remains release-pending under Browser-first, never Playwright-substituted. | `coordination/CONSTELLATION.md`, D35, W3/W17/W18/W32; live Glass task `019f6610-6022-7df1-95b3-472b17a64656` and inbox active-disposition rows 112–114. |
| V-A2 | W33 could deploy a rebuilt SPA after W32 and still call visual identity verified; final deployed inspection had no π/DELTA. | W32 freezes the entry document and every transitive asset as one canonical byte manifest. W33 may serve only those exact bytes and Browser-opens all eighteen deterministic coordinates. Static structure/state counts match exactly; dynamic type, material, renderer, proportion and motion claims remain inside their originating W32 numeric tolerances. Screenshot-byte or zero-per-pixel equality is not claimed; an out-of-tolerance or semantic/interaction delta reopens its owner and W32. | `EVIDENCE.md`, W32/W33. |
| V-A3 | Palette purge remembered only Release-tombstone/final-Workspace digests, so an A→B edit could strand A; scan-and-delete could also commit write skew against a new reference. | `ContentObject` itself is the sole per-digest Workspace/Release/Report reference guard. Every runtime referent mutation updates exact counters on that document; zero deletes synchronously. The candidate queue, captured digest, delayed content scan and entire purge content phase are retired. | D36, `PALETTE-DOMAIN.md §Canonical palette content/§Trash, withdrawal, purge`, W11–W13/W16/W33. |
| V-A4 | The first reference-port draft could skip byte comparison on a same-digest collision, Save filtered nonexistent Workspace-owned authority, and purge completion demanded an unbounded global scan. | Payload validation is independent of counter cancellation; same-digest/different-bytes always fails. Save joins Workspace to its Handle, applies the sole port, then CASes real Workspace fields through the inherited actor fence in one receipt transaction. Production purge completion checks only phase/cursor, subject-tombstone and scalar result-count equations—never retroactive content deltas; complete-graph recomputation belongs to quiesced fixtures and the unreachable cut verifier. | `PALETTE-DOMAIN.md §Save workspace/§Trash, withdrawal, purge`, W11/W16/W33. |
| V-A5 | Glass P122's restored six-property contract still left `min-block-size: var(--instrument-min-block-size, auto)` in the chassis root, reintroducing the universal reserve the contract explicitly retired. | **SOURCE/TEST LANDED on the active post-BI Glass producer tree after the exact V relay.** The universal property and consumer escape hatch are deleted without alias; the ordinary contract asserts their absence while preserving only explicit typed stage/inspector reserves. Genuine height follows content and typed P122 regions; value passes boundaries `[]` and reserve `none`. The combined producer row remains RED until native 1440/390/320/400%-zoom and evidence-derived `G_NEXT` artifact coordinates exist. | Glass `src/components/instrument-chassis/styles.css:17–20`; `tests/components/instrument-chassis.contract.test.ts:123–134`; inbox row 112; `coordination/CONSTELLATION.md §Exact active-session relay`; direct task `019f6610-6022-7df1-95b3-472b17a64656`. |
| V-A6 | P047 exposes `bodyRadius`, a settled frame and named origin, but its exact dual-engine/DPR painted-component measurement protocol was not implemented while “source-complete” language could imply the rendered-mass claim was closed. | Credit the Glass producer seam and ordinary protocol only when its source/tests are observed. W17 validates that seam under the four WebGPU/WebGL2 × DPR 1/2 cases and freezes the unchanged pre-W29 baseline `b0`; it does **not** own the target ratio. W29 alone changes Picker `geometry.bodyRadius` and records the final `0.66 ±0.015`, centroid/hull and 0px-geometry result. A nominal-radius proxy cannot satisfy either manifest. | `coordination/CONSTELLATION.md items 5 and §Exact active-session relay`, D19, W3/W17/W20/W29/W32; V-A68. |
| V-A7 | V routed font assets/evidence and the live relay through P128/P061/P004 even though live Glass has declined P128's generated build-project graph, declined P061's scenario runner and superseded P004's constellation registry. The semantic obligations survived, but their labels revived retired machinery. | P019 owns the product type/font surface, P010 semantic projection, and P127 packed asset emission/URL/dependency/release truth; native request/stability review stays with those product owners at major Browser boundaries. The relay is a direct immutable handoff plus P127 amendment. P128, P061 and P004 remain retired and no replacement registry/runner/generator is created. | D20/D35, `coordination/CONSTELLATION.md`, W3/W17; live Glass `FORMATION/waves/BI.W-P004.md`, `BI.W-P061.md`, `BI.W-P128.md`. |
| V-A8 | Glass's rendered Search specimen exposes internal planning vocabulary—`wired`, `proof`, `consumer`, `triage`—as visible status badges, contradicting the public-copy covenant. | P059/P060 replace the four statuses with observable end-user product states in the existing specimen source and inspect the routed result in the native Browser. Keep real imports and the useful search behavior; add no copy framework, scenario runner or Glass branch for value. | `../glass-ui/demo/stories/data/search.vue:17,37–68,404–409,551`; `coordination/CONSTELLATION.md item 15`; W3/W17/W32. |
| V-A9 | Value's ignored `dist/value.js` predates current HEAD, while `package.json`'s `prepare` runs `test -f dist/value.js || npm run build`. A stale file therefore suppresses the producer build and can enter `npm pack`, yielding a green command over an old root/entry graph. | W7 deletes the existence-conditioned prepare path and makes the seven-entry manifest/build authoritative. W17 removes prior output, builds unconditionally and packs fresh-installable **rehearsal** bytes in clean authorized worktrees. W33 independently repeats the clean build and packs exactly one sole release candidate per package; only that W33 file enters the release record and `npm publish`. Every stage rejects a tar member older than its tree or absent from its manifest. No current `dist`, current HEAD, rehearsal hash, predicted integrity or source-only result can stand in for the final artifact. | `package.json:79`; `dist/value.js` timestamp `2026-07-13T17:53:05-04:00` versus current HEAD `86a9a178cd7a6f2b573216966964c33513494f05` at `2026-07-15T13:46:00-04:00`; W7/W17/W33; P127 inbound direct handoff. |
| V-A10 | V required the next Glass artifact to consume exactly Value `/color` and `/easing`, yet Glass's live DOM-free `cssToOklch(css:string)` accepts arbitrary concrete CSS color text while Value 4 deliberately moves CSS text parsing out of parser-free `/color` to `/css`. The two-entry contract is impossible without deleting a real consumer, guessing an API, adding a Glass-local/DOM parser, or retaining the root. | Glass's closed Value set is exactly `/color`, `/css`, `/easing`. `/css` supplies `parseCssColor(source:string): ParseResult<CssColor>` and `serializeCssColor(color:CssColor): Result<string,ColorIssue>` over the closed thirteen-space CSS-native union; Glass explicitly propagates or maps failure to its color-domain error, then performs only failure-explicit final-object color operations through `/color`; `/easing` supplies named functions plus the exact failure-explicit numeric constructors. `vite.library.ts` exact-externalizes all three. The live profiler's accepted map and ordinary bite test add exact `/css`, remove it from the rejected set and continue to reject the root, old entries, prefix lookalikes and private paths. The prior two-edge **SHIPPED** claim is reopened until that three-edge source/external/profile/test cut lands. No root/prefix external, local parser, fallback, alias or dual path survives. | Glass `src/composables/color/index.ts:17–29,110–123`; `tests/composables/color/use-accent-tone.test.ts:9–20`; `vite.library.ts:28–34`; `scripts/profile-bundle.mjs:339–357`; `tests/scripts/profile-bundle-value-js.test.ts:45–74`; Value `ARCHITECTURE.md §2–3`, `CONSUMER-CUT.md`; W9/W17/W33. |
| V-A11 | Glass's color/accent path catches a failed CSS parse and returns `""`, substitutes a warm-card surface when the actual surface is absent, treats `var()`/unset/concrete-pending/failed solves as the same empty identity, caches that identity, and certifies contrast through an OKLab-lightness proxy plus `minContrast * 0.1`. These paths silently collapse distinct domain states and can claim AA without testing the color actually painted by `color-mix`. | The evidence-derived `G_NEXT` cut consumes `/css` `ParseResult<CssColor>` without catch-to-empty or substitute-color success. Invalid tone, invalid surface, unresolved cascade token and pending lazy solve remain distinct explicit Result/domain-error states; absence never becomes `var(--primary)`, the warm-card constant, inherited foreground or an empty-string success. The contrast solver constructs the actual typed band color from the resolved surface, resolved tone and exact band strength, then uses Value `/color`'s Color-against-Color WCAG-ratio path to produce an ink that clears the requested ratio against that same band. The old `bandLightness`, `safeAccentColor(..., bgLightness, minContrast * 0.1)`, catch-to-`""`, identity cache and CSS masking fallback are deleted together; unresolved CSS tokens are resolved at their owning rendered boundary or returned unresolved, never certified by a guessed default. | Glass `src/composables/color/accent-tone-solve.ts:21–88`; `src/composables/color/useAccentTone.ts:13–24,45–60,79–112`; `tests/composables/color/use-accent-tone.test.ts:84–138`; Value `/color` actual-surface contrast operations and D6/W8/W9/W17. |
| V-A12 | Glass Aurora's `applyTemperature` passes degree hues (`h`, `70`, `250`) to Value 3's normalized-turns `interpolateHue`; with `t=1`, integer-degree inputs normalize to `0`, so the derived direction is toward zero rather than the named warm/cool pole. Source prose and broad gamut tests can remain green while the temperature model is wrong. | Value 4's public `/color` `interpolateHue` consumes and returns physical hue degrees under the final `Color` schema; normalized-turn helpers remain GPU-local rather than defining the public color contract. Glass retains Aurora-domain pole/throw composition but consumes that degree-domain operation and deletes any local wrap substitute. Ordinary named vectors prove warm wrap (`350°→70°` moves increasing and a full capped throw yields `12°`), cool wrap (`10°→250°` moves decreasing and yields `348°`), warm/cool near-pole no-overshoot (`60°→70°` ends `70°`; `260°→250°` ends `250°`), midpoint/zero identity, and the `22°` maximum throw. | Glass `src/components/aurora/composables/color.ts:304–310,364–410`; Value `src/units/color/mix.ts:60–98`; `ARCHITECTURE.md §2` physical-degree schema; W8/W17 and the active Glass Aurora owner. |
| V-A13 | W13 required a strong `ETag` replay byte for every palette/social success even though the wire permits validators on only a closed subset; a universal assertion would reject correct rows or make forbidden headers appear authoritative. | Receipt tuples carry `etag` exactly for mutation rows r25/r28/r32/r34/r36/r40/r41/r43/r44/r46/r47/r49 and omit it for every other mutation. The broader public read/write ETag matrix remains solely `PALETTE-WIRE-CONTRACT.md §5.2`; replay preserves the original row-specific presence and bytes rather than inventing a validator. | `PALETTE-OPERATIONS-REPLAY-CONTRACT.md §2`, `PALETTE-WIRE-CONTRACT.md §5.2`, W13/W16. |
| V-A14 | “Dynamic bottom text” could pass as static range furniture or a chattering live region unrelated to the selected color. | Picker's four persistent meters, numeric headline and slider `aria-valuetext` consume one formatted selected-color state. The baseline `lab(92% 88.8 20 / 82.70%)` and `a:88.8→40.0` before/after transcript are binding; meters are ordinary `aria-live="off"` text and the slider alone speaks its channel name/value/unit. | `VISUAL-CONSTITUTION.md §7`, `PROMPT-RECAP.md T-4`, W20, `EVIDENCE.md`. |
| V-A15 | The pastel-rainbow “Palettes” ask lacked a bounded site/scheme contract and could either vanish at one site or spread into distracting brand color everywhere. | Exactly two textual substrings share the identity composition: Dock `Palettes` and the `Palettes` substring of Library heading `My Palettes`, each in light and dark. `My` and every other navigation, route, Admin, Account, action and status label remain neutral; the non-`Palettes` textual identity count is zero. | `VISUAL-CONSTITUTION.md`, W18 shared composition, W19 Dock witness, W22 Library witness, `PROMPT-RECAP.md T-10/T-43/T-56`. |
| V-A16 | W31's route/device wording allowed twenty favorable loads to hide Account/history/Admin/KaTeX/Aurora/Blob cost, pool unlike hardware or omit a coalesced chunk family. | Freeze M00–M17 × H-D/H-M before measurement. Every cell has 20 cold plus 20 returning loads in the printed alternating scheme order; every transitive asset belongs to exactly one activation-vector family; results remain per coordinate/hardware/cache class, with no pooling, outlier-only reruns or post-trace route substitution. | W31 exact matrix; `EVIDENCE.md`. |
| V-A17 | A required Glass producer row could return `RE-DEADLINED` and still make W3's terminal table look closed, re-booking the same dependency. | Coordination may report `RE-DEADLINED`, but the row remains RED. W3/W17/V close it only **SHIPPED** with source/test/native-Browser/evidence-derived `G_NEXT` coordinates or **DECLINED** with architectural rationale and the exact implemented replacement plus coordinates. | D37, `coordination/CONSTELLATION.md`, W3/W17. |
| V-A18 | Removed Value/Keyframes APIs could survive in current README/API pages, guides, rendered examples or copyable snippets while a source/configuration-only census passed. | W2/W17/W33 census active source, demos, tests, benches, scripts, configuration and current public documentation. Only clearly labeled non-executable historical archaeology is excluded; runnable probes, shipped examples and current instructions remain in scope regardless of path age. | `CONSUMER-CUT.md §3–4`, W2/W17/W33. |
| V-A19 | A blanket W33 “zero rendered drift” mixed exact immutable asset identity with nondeterministic GPU/font pixels, making the claim either impossible or vacuous. | Served entry/transitive asset bytes and manifest digest have exact zero drift. Deterministic route/state/fixture/seed/scheme/viewport/DPR/reduced-motion/font/renderer/settlement tuples recheck exact static counts and the originating named numeric tolerances; screenshot bytes and per-pixel equality are explicitly outside the claim. | Amended V-A2, W32/W33, `EVIDENCE.md`. |
| V-A20 | W2's former “direct-path equivalence” would preserve the pairwise conversion paths V intends to delete and let one production route certify another. | The sole runtime anchor graph is checked against independent literal bidirectional row oracles and exact conversion classes. No production direct path is retained or compared; agreement among production paths is not independent evidence. | `ARCHITECTURE.md §2`, W2/W8. |
| V-A21 | `BANK` was used inside active BUILD rows and with subjective triggers, allowing protected dirty state or a favored alternative to reactivate silently during V. | BANK is terminally outside V, has no owning V wave, and its exact empirical trigger may seed only a future formulation. Dirty/staged state is **PROTECT while BUILD W17/W33**, not BANK. The transactional mutable aggregate and functional kernel are RETIRED; only the exact Living Field, Worker scene and NCSU triggers remain banked. | `DECISIONS.md §1`, `DISPOSITION-LEDGER.md`, `research/CONVERGENCE.md`. |
| V-A22 | The parse-result prose admitted a mutable or empty diagnostic array on failure while the declaration required a readonly nonempty tuple, allowing a prose-conforming failure with no diagnosis. | `ParseResult<T>` has one exact shape everywhere: success carries `readonly diagnostics: readonly []`; failure carries `readonly diagnostics: readonly [ParseIssue,...ParseIssue[]]`. Parser consumers and packed declarations prove both arms; no status-plus-value, thrown, null, or empty-failure path survives. | `ARCHITECTURE.md §3`, W7/W9/W17. |
| V-A23 | Canonical CSS round-trip language covered four library-only spaces that CSS cannot spell, while `serializeCssColor(AnyColor)` supplied neither an output-space choice nor a possible standards-conforming result. | `/css` owns only the thirteen-space `CssColor` union. `parseCssColor` returns `ParseResult<CssColor>` and `serializeCssColor` accepts `CssColor`; `hsv`, `kelvin`, `ictcp`, and `jzazbz` retain full `/color` object/factory/conversion/oracle coverage but must explicitly convert to a chosen CSS-native color. Their library spellings are rejected and no general library-string serializer is added. | `ARCHITECTURE.md §2–3`, D38, W7–W9/W17/W33. |
| V-A24 | Fourier's row simultaneously retired `timingFunctions` and migrated that aggregate unchanged, preserving the legacy property/undefined/identity lookup beneath a clean-break declaration. | Fourier imports exact named easing functions from `/easing`; any dynamic catalogue lookup calls failure-explicit `easing(name)` and handles its `Result`. The `timingFunctions` aggregate, local mirror, property fallback and identity substitution are absent from source, declarations and current public documentation. | `CONSUMER-CUT.md §2–4`, `ARCHITECTURE.md §3`, W2/W7/W9/W17/W33. |
| V-A25 | W17 and W33 both appeared to own a final tarball hash, and “exports hash” had no canonical input, so rehearsal bytes or any of several incompatible digests could be reported as the immutable artifact. | W17 packs rehearsal bytes only. W33 performs a fresh clean build, designates one sole candidate and publishes that exact file; at most one isolated noncandidate verifier may compare same-tree members/hashes and is then deleted. Fetched registry bytes and decoded `dist.integrity` SHA-512 equal the candidate, and only that file supplies W33 coordinates. `exportsSha256` is lowercase SHA-256 over the UTF-8 RFC 8785 canonical bytes of its in-tarball `package.json.exports` object; each literal `types` target separately receives an exact-byte `declarationSha256`. | `ARCHITECTURE.md §3–4`, `CONSUMER-CUT.md §4`, D41, W17/W33. |
| V-A26 | W31 required W32's final asset manifest before measuring while W32 depended on W31, creating an impossible performance-to-visual DAG back-edge. | W31 derives every activation family from its own production Vite manifest plus observed transitive request graph and writes the complete activation record. W32 later verifies and freezes the projection from the same bytes; it is never an input to W31. | W31/W32, `EVIDENCE.md §5`. |
| V-A27 | Per-cell p75 INP was required without an interaction for every M-row, allowing absent or opportunistically selected interactions on About, Account, history and Admin routes. | M00–M17 each freeze one exact non-destructive interaction before execution; every load records that target/type/start/duration and resulting state, and the seeded fixture is reconstructed between loads. Another interaction may not be substituted after observing a result. | W31 exact route/interaction table and π/DELTA; `EVIDENCE.md §5`. |
| V-A28 | The 1,440-load performance count claimed unallocated boot oppositions, malformed/contextual inputs, mismatch and invalid-default cases, so those correctness arms could be skipped without changing the printed matrix. | Performance remains exactly 1,440 M×H cold/returning loads. A separate frozen B00–B08 matrix enumerates 27 fixture arms and 54 H-D/H-M natural-load witnesses for valid, opposition, contextual, malformed/state, mismatch and invalid-default behavior; it reports no percentile and is not pooled into performance. | W28/W31, `EVIDENCE.md §5`. |
| V-A29 | W30 and the evidence matrix could close after downstream integration while omitting W20's selected-color meters, allowing stale values, a missing alpha meter, formatter drift or a chattering live region. | W30 replays the exact four persistent meters, numeric headline and slider formatter from the baseline `lab(92% 88.8 20 / 82.70%)` through `a:88.8→40.0`; meters remain `aria-live="off"`, and slider `aria-valuetext` alone carries the identical channel/value/unit. | W20/W30, `EVIDENCE.md §4`. |
| V-A30 | Decision B-14 retained an unbounded W18/W22 “Palettes” band and omitted W19, contradicting the exact two-site identity law. | The decision now binds exactly Dock `Palettes` and the `Palettes` substring in Library `My Palettes`, each in light and dark, owned by W18/W19/W22. Every other textual identity application remains neutral and its count is zero. | D B-14, `VISUAL-CONSTITUTION.md`, W18/W19/W22. |
| V-A31 | W17 claimed P047 adoption while its π/DELTA and evidence rows omitted the producer source/tests and painted-component protocol, so nominal radius or stale imagery could satisfy the sibling cut. | W17 names and validates the exact producer source/tests, seed/color/config/settled frame, premultiplied-alpha origin component and four internal-Browser-first WebGPU/WebGL2 × DPR1/2 baseline captures, then freezes `b0`. W29 repeats that protocol after its sole `geometry.bodyRadius` change and owns the final ratio/centroid/hull/0px result. W32 may credit the final claim only from W29's manifest; source presence or W17's baseline is not the product result. | W3/W17/W29/W32, `EVIDENCE.md §3`, V-A6/V-A68. |
| V-A32 | r43 required a grant reason on input, but the persisted Grant and r42 response dropped it, permanently erasing the authority decision's rationale. | Grant persists immutable `creationReason`; r42, r43 and r44 return that exact value unchanged. No later status or revocation event rewrites it. | `PALETTE-DOMAIN.md`, `PALETTE-WIRE-CONTRACT.md` r42–r44, `PALETTE-FACILITIES.md`, W10/W16/W24. |
| V-A33 | The one-time workload-authorized bootstrap grant required a Principal creator even though no Principal could yet authorize it, making the bootstrap attribution unrepresentable or inviting a fabricated self-grantor. | Grant has one required discriminated `GrantAttribution`: `principal` or `bootstrap-workload`. The workload arm references the immutable `BootstrapRecord`; no nullable creator, scalar alias, system flag, HTTP bootstrap path or fabricated Principal exists. | `PALETTE-DOMAIN.md §Authority`, `PALETTE-FACILITIES.md`, W10/W16/W33. |
| V-A34 | Workspace creation, fork and publish created clear PalettePolicy/ReleasePolicy rows without the rationale/actor pair that r33/r35 require for every non-cut policy. | Runtime constructors persist exact provenance: lineage creation and fork use `"lineage created"` plus creator; publish uses `"release published"` plus publisher, all at the transaction instant. Only clean-cut-created policy rows use the exact absent pair. | `PALETTE-DOMAIN.md`, `PALETTE-WIRE-CONTRACT.md` r33/r35, `PALETTE-FACILITIES.md` r9/r12/r24, W11/W12/W16/W24. |
| V-A35 | Restore retained a cancelled purge row for audit, but principal-close treated any pre-existing row as active and faulted, so `trash→restore→close` could never reach mandated aftermath. | Purge episodes have one state law: only `queued|leased` are schedulable; `complete|cancelled` are immutable history. Restore cancels c1, later close creates fresh c2, and aftermath examines only a current schedulable episode while retaining c1's audit record. | `PALETTE-DOMAIN.md §Trash/aftermath`, `PALETTE-FACILITIES.md`, W10/W11/W13/W16/W33. |
| V-A36 | The current prompt tail carried substantive convergence, addendum, Browser-only, parsimony/fanout and exact Glass-coordinate fences that were partly treated as operational chatter and therefore absent from the recap ledger. | `PROMPT-RECAP.md §6` now records each substantive fence with its terminal owner. Pure workflow starts, capacity resets, status requests and crash-recovery commands remain operational metadata, but their product-bearing clauses are retained; no meta command mints a gate or wave. | `PROMPT-RECAP.md §1/§6`, `PROGRESS.md`, `audit/AGENT-REGISTRY.md`, active goal and Glass task `019f6610-6022-7df1-95b3-472b17a64656`. |
| V-A37 | Purge episode uniqueness used the raw `causeCommandId` while command receipts are namespaced. Two Principals may legally reuse one UUID, so cancelled owner-trash history could collide with a later admin-close episode despite the terminal-history repair. | Every PurgeJob persists the same closed `causeReceiptNamespace` that owns its triggering receipt. Episode uniqueness is exactly `(kind,subjectId,causeReceiptNamespace,causeCommandId)`; Handle trash state stores the matching namespace, principal aftermath inherits the close namespace into every palette job, and immutable result provenance includes it. Equal UUIDs under distinct Principal namespaces are a required green vector. | `PALETTE-DOMAIN.md`, `PALETTE-FACILITIES.md`, W10/W11/W13/W16/W24/W33. |
| V-A38 | V-A9 still called both W17 and W33 outputs “the candidate” after V-A25 made W17 rehearsal-only, allowing the material-defect ledger itself to contradict the release authority. | V-A9 now distinguishes clean W17 rehearsal packs from W33's independently built sole release candidate and exact publish input. A full-corpus release-language sweep permits no W17 hash, tarball or coordinate to stand as the immutable producer artifact. | V-A9/V-A25, D41, `CONSUMER-CUT.md`, W17/W33. |
| V-A39 | The 17-row architecture table and D7 still promised “canonical round-trip syntax spellings” for `hsv`, `kelvin`, `ictcp`, and `jzazbz` after V-A23 rejected those library spellings from `/css`. | The table distinguishes thirteen canonical CSS spellings from four numeric library-display forms with **no CSS spelling**; D7 makes that split explicit. Only `CssColor` round-trips through `/css`; all seventeen retain final objects, factories, conversion anchors and independent numeric/oracle evidence. | `ARCHITECTURE.md §2`, D7, V-A23, W8/W9. |
| V-A40 | W31 named one interaction per route, but its prose still let desktop/mobile rows choose semantically similar targets, sequences or terminal outcomes independently; incomparable runs could enter one percentile cell while the matrix count stayed green. | Before any run, W31 freezes exactly 36 `M00–M17 × H-D/H-M` entries with one complete accessible target, exact start state, hardware-specific input sequence and exact terminal state. Every one of a cell's 40 loads reuses that entry byte-for-byte; changing any coordinate invalidates and reruns the whole 1,440-load matrix. | W31, `EVIDENCE.md §5`, `audit/pi/w31/interaction-manifest.json`. |
| V-A41 | Q-AUDIT modeled `subjectType` and `subjectId` as independent optionals even though platform, UUIDv7 and ResourceId subjects have different identity grammars, admitting bare IDs and mistyped pairs into supposedly deterministic audit pages. | `AuditSubject` is one closed discriminated union. Query subjects are exactly absent, type-only, or a correctly typed pair; bare ID, wrong grammar/version, wrong platform literal, null or duplicate scalar key is `422 query_invalid`. OpenAPI, client, persistence, events and fixtures use that same union. | `PALETTE-DOMAIN.md`, `PALETTE-WIRE-CONTRACT.md`, W1/W10/W13/W16/W24. |
| V-A42 | V-A25's “single pack” language contradicted W7/W33 reproducibility wording that required a second clean pack, leaving implementations to publish either invocation or report coordinates from a verifier. | W33 designates exactly one sole clean candidate and publishes that exact file. It may create at most one isolated noncandidate verifier from the same clean tree solely to compare member names and content hashes; the verifier is deleted unpublished, is never installed and supplies no coordinate, hash, integrity or `gitHead`. | V-A25, `ARCHITECTURE.md §3–4`, `CONSUMER-CUT.md §4`, W7/W33. |
| V-A43 | `/value`, `/math`, `/transform` and `/quantize` had export paths but no closed symbol/type declarations, so an artifact with missing or extra runtime names could satisfy the seven-entry manifest. | `ARCHITECTURE.md §3` fixes exact compilable declaration blocks and runtime/type name-and-kind sets for all four entries. W2/W7 compare every packed entry against the block and reject missing, extra, default, wildcard, namespace and value/type-kind drift. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md §2`, W2/W7/W8/W17/W33. |
| V-A44 | Three sibling references were not followable evidence: V's Glass inbox path omitted `../`, the FIELD-CORE coordinate named a nonexistent `FORMATION/waves` file, and P122/profile/Browser dispositions still cited inbox lines 85–87 after they moved to 112–114. | Canonical V references use the repository-relative Glass inbox, `../glass-ui/docs/tranches/BI/waves/BI.W-FIELD-CORE.md`, and the live disposition rows 112–114. A line coordinate is re-read before handoff; stale or nonexistent coordinates cannot attest a producer claim. | `V.md`, `coordination/CONSTELLATION.md`, V-A1/V-A5, W3/W17. |
| V-A45 | The purported exact seven-entry declarations made `/css` only a Glass color/timing slice while V promised value, stylesheet and timeline grammar and live Keyframes consumed property, custom-function, animation-option, timeline, syntax and serializer semantics. The omitted surface could only be guessed, duplicated or silently dropped during W17. | `/css` has one exact cohesive grammar declaration: typed CSS value/color/stylesheet/timing/animation/timeline AST, total collectors, timeline serialization and syntax coercion. After V-A63 its source-consuming `ParseResult` set is exactly ten, including `parseKeyframeSelector`. `collectPropertyDescriptors` is distinct from declaration collection. Exactly four text-only helpers move once to Keyframes and every other 3.x parser export is explicitly private, localized or retired; no alias or hidden export survives. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md §2–4`, W2/W7/W9/W17/W33. |
| V-A46 | The active Glass inbox called P122/Lab **SHIPPED** while its native 1440/390/320/400%-zoom witness remained pending, and described `RE-DEADLINED` as terminal, contradicting V's own producer-close law. | Source and ordinary tests may be recorded as LANDED/IMPLEMENTED. The evidence-derived `G_NEXT=7.0.0` rehearsal coordinates now exist, but the combined producer row remains RED until the native Browser witness and W33 release/tag/registry coordinates exist. `RE-DEADLINED` is an explicit nonterminal RED disposition. The correction was sent directly to active Glass task `019f6610-6022-7df1-95b3-472b17a64656`; delivery is not evidence. | Glass inbox active dispositions, D37, `coordination/CONSTELLATION.md`, W3/W17. |
| V-A47 | W31 froze internal fixture labels such as `v31-user` and `v31-report` as literal accessible row names, allowing assistive technology to announce tranche vocabulary while the interaction manifest passed. | Planning IDs remain private fixture metadata. The four Admin targets use exact product-real row and control names, and the stable locator still includes the named containing row; public rendered/accessible copy contains no tranche, wave, proof or gate vocabulary. | W31 exact interaction table, `EVIDENCE.md`, `coordination/CONSTELLATION.md` product-language law. |
| V-A48 | Q-AUDIT left `from` and `to` independently optional and never chose boundary inclusivity, so two conforming servers could page different events at the same timestamp. | The time filter is either wholly absent or contains both bounds. A present window is the half-open interval `[from,to)`, requires `from < to` and duration at most 366 days; one-sided, equal, reversed, null or duplicate bounds return `422 query_invalid`. Cursor/query identity and goldens use those exact bytes and membership. | `PALETTE-WIRE-CONTRACT.md`, W1/W10/W14/W16/W24. |
| V-A49 | The new declaration collector was described as unconditional last-write-wins even though declarations retain `important`, so a later ordinary declaration could erase an earlier `!important` value. | Within one declaration list, an important declaration defeats every non-important declaration regardless of order; among equal importance, later source order wins. The keyed collector and D-GAP-5's two Keyframes consumers share that one rule and ordinary vectors. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md` D-GAP-5, W9/W17. |
| V-A50 | `collectAnimationOptions` and `collectTimelineOptions` were declared as `ParseResult` producers over spanless typed AST nodes even though every `ParseIssue` promises an exact UTF-16 source span, forcing collectors to fabricate coordinates or violate the issue contract. | `parseStylesheet` validates option and timeline grammar while source offsets exist. Both collectors are total typed projections over a valid AST and cannot emit diagnostics; only the source-consuming grammar operations own `ParseResult`, exactly ten after V-A63 adds `parseKeyframeSelector`. | `ARCHITECTURE.md §2–3`, W7/W9/W17. |
| V-A51 | The first total option-collector repair still accepted an entire Stylesheet, allowing unrelated selectors and conditional containers to merge into one animation/timeline option object that CSS never defines. | `collectAnimationOptions` and `collectTimelineOptions` accept exactly one caller-selected `readonly Declaration[]`. They preserve that declaration block's precedence and never scan or merge another selector, frame or container. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md`, W9/W17. |
| V-A52 | The proposed keyframes collector preserved 3.x's same-name concatenation and erased conditional/layer ancestry; separate `@keyframes` blocks could therefore become one invented animation, and an inactive nested rule could override an active one. The same context loss threatened property/custom-function/style collectors. | Parser-owned recursive collectors return document-ordered `CollectedRule<R>` entries with immutable AST index paths for every keyframes/property/custom-function/style rule. They never map, concatenate or select across contexts. Keyframes evaluates the recorded ancestor contexts, rejects unsupported context explicitly, and chooses the last applicable same-name keyframes block; consumers do not re-author the traversal. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md §2–4`, W2/W7/W9/W17. |
| V-A53 | At the correction-time census, W17's commit paragraph said W7–W9 were “already” locally merged although the producer was still Value 3.1 with root, `/parsing`, `/units`, existence-conditioned stale-output prepare, no `/value` or `/css` declarations and no Value-4 tarball. The formation plan thereby misstated future work as landed and could invite Glass to migrate against nonexistent bytes. | Treat those repository facts as the historical census that exposed the defect, not a perpetually current inventory. `PROGRESS.md` alone records moving execution state. W17 cannot enter until W8→W9→W7 is implemented and atomically closed as one locally green C1 cut; it then creates the first clean rehearsal tarball. No active partial tree, source green, plan declaration or predicted coordinate substitutes for that rehearsal. | Correction-time `package.json:3,19–20,25–65,79`; then-absent `dist/subpaths/{value,css}.d.ts`; W7–W9/W17; `PROGRESS.md`; Glass P127 safe-boundary correction; V-A67. |
| V-A54 | Browse/Library fixed Card count and self-consistent padding equations but never chose the producer material/size tuple. Both a large elevated glass caster and a compact matte specimen slip could therefore pass PR-34. | Every palette-entity Card is exactly `size="sm"`, `material="content"`, effective `tier="quiet"`, `surface="opaque"`, `shadow=false`, `grain=false`, `specular="off"`. It has no cartoon/grid or Card-level selection variant; the named child seat alone owns selection. Its Card root `C=--spacing(4)` at every viewport, while only the surrounding workspace gutter changes responsively. | `VISUAL-CONSTITUTION.md`, `OPTICAL-BENCH-COMPOSITIONS.md` PR-34, W18/W22/W32, `EVIDENCE.md`; producer Card defaults and current palette-card local well. |
| V-A55 | Font family/weight observations did not fix role-to-rung hierarchy, and “bounded” About prose admitted both a readable column and a nearly full-width article. | One closed typography matrix binds route H1/major argument, instrument identity, retained palette identity, section heading, prose, control/label and value/code/provenance roles to their named Glass/value rungs. P019 is the sole paired kicker/headline exception. About prose is `max-inline-size:66ch`; only named specimen examples may escape. Browser evidence records computed role, family, rung, measure, wrap and overflow at 1440/390/320/400% zoom. | `VISUAL-CONSTITUTION.md`, `OPTICAL-BENCH-COMPOSITIONS.md`, `research/proportion-register.md`, W18/W20–W29/W32. |
| V-A56 | The Picker selector required invariant WatercolorDot face pixels but its owning waves did not propagate B-27's visible selected marker. Keeping `hide-indicator` plus AT-only `aria-selected` could pass with no sighted selection affordance. | Delete `hide-indicator` and consume the producer SelectItem indicator/gutter as the sole marker. The open list has exactly one marker; selected-seat/marker pixels change in monochrome and forced colors, focus has an independent nonzero delta, the WatercolorDot face delta is zero, and `role=option`/`aria-selected` agree. No local pill, halo or pseudo-element substitute exists. | D B-27, `PROPORTION-AUDIT.md` PR-07, W20/W30/W32, `EVIDENCE.md`; current `ColorSpaceSelector.vue`/Glass `SelectItem.vue`. |
| V-A57 | The exact Lab header-to-headline relation was measured at P019 clamp arms and 1440/390 only. A narrow wrapper margin or wrap rule could reopen the vast gap at 320px or actual 400% zoom while every named check stayed green. | The complete routed Picker repeats the same `G`, `I_before`, `I_after`, `I_after ≤ min(φG, I_before − G)` and collision/Blob-footprint measurements at 320px and actual 400% Browser zoom, in addition to the existing 1440/390 and clamp-arm evidence. No alternate mobile equation, screenshot proxy or Playwright gestalt authority is admitted. | `PROPORTION-AUDIT.md` PR-01, `VISUAL-CONSTITUTION.md`, W20/W30/W32, `EVIDENCE.md`. |
| V-A58 | Q-AUDIT exposed and filtered `outcome=success|failure`, but the transactional outbox omitted the field and an aborted digest-collision transaction could not persist its promised failure audit without a second writer. | Audit is strictly immutable committed-effect provenance. Remove `outcome` from outbox/event persistence, Q-AUDIT, DTO/OpenAPI/client/cursor identity and fixtures; remove the aborted collision-audit promise. Do not create an attempt logger, after-abort write or parallel audit path. Edge request logs retain their unrelated closed transport outcome enum. | `PALETTE-DOMAIN.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-FACILITIES.md`, W10/W14/W16/W24. |
| V-A59 | Principal aftermath said Handle transition and PurgeJob creation were atomic, yet silently recreated a missing job behind an already-trashed Handle and counted it as repaired. That branch masked corruption the invariant says is impossible. | Only `active→trashed` creates a purge episode. An absent, mismatched or multiple schedulable job behind a trashed Handle is `platform_consistency_fault`; the batch makes no subject, count or cursor advance. Delete the repair branch and `repairedPaletteJobs` from progress/result bytes and every fixture. | `PALETTE-DOMAIN.md §Trash, withdrawal, purge`, `PALETTE-FACILITIES.md`, W10/W11/W13/W16/W33. |
| V-A60 | Rank invalidation named projection events and a lease but no durable identity, acknowledgement or crash boundary; “one epoch per mutation” also turned a burst into unbounded full rebuilds. | One `CatalogBuildState` owns monotonic `dirtyRevision`, `builtRevision`, lease identity/epoch/deadline and heartbeat. Mutations atomically advance the dirty revision; one builder coalesces them, builds an unreachable complete epoch, and atomically publishes it with the covered revision. Crash-before-publish and crash-after-publish-before-observation are idempotent; retained epochs expire by the explicit 24-hour TTL. Every mutation is covered within 60 seconds, never promised its own epoch. | `PALETTE-DOMAIN.md §Catalog rank`, `PALETTE-CURSOR-CONTRACT.md`, W13/W14/W16/W33. |
| V-A61 | Social counts and popular-score cursor coordinates were unbounded CBOR integers even though their JSON clients are IEEE-754 numbers. Adjacent values at or above `2^53` could round to one coordinate and repeat or skip rows. | Every public count, rank count, distance and score integer is a JSON-safe unsigned integer `0…Number.MAX_SAFE_INTEGER`; `4·rankVoteCount+7·rankForkCount` must also fit. Aggregation overflow is `platform_consistency_fault` before epoch publication or response. Deterministic CBOR uses the smallest unsigned encoding of that same exact value, with maximum/overflow and adjacent-boundary vectors. | `PALETTE-DOMAIN.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-CURSOR-CONTRACT.md`, W13–W16. |
| V-A62 | V called Keyframes local/tag/npm “clean 5.3.5” while the tracked HEAD/tag/npm remain 5.3.5 but its working package manifest, lock and tranche docs are dirty with a prospective `6.0.0` label. The label could be laundered into implementation or artifact truth. | Record the split exactly: tracked/tag/npm 5.3.5 at `a59d3a22…`; five protected modified files including `package.json`/lock at prospective 6.0.0; dependency still Value `^3.1.0`; no Keyframes artifact for the prospective label. W2/W17 re-census and reconcile the protected delta without overwriting it. W17's packed public-surface diff decides `K_NEXT`; only W33's observed tag/tarball/integrity coordinates establish that coordinate. | D2, `CONSUMER-CUT.md`, W2/W17/W33, `PROGRESS.md`, `research/CONVERGENCE.md`; Keyframes disk status. |
| V-A63 | The exact `/css` `KeyframeSelector` could not represent the live Keyframes grammar `entry 50%`; dropping the offset changes timing, while retaining Keyframes' token regex would violate the sole-grammar cut. | The named selector carries optional authored `offset` normalized to `[0,1]`; absence preserves the bare spelling. `/css` adds `parseKeyframeSelector(source): ParseResult<KeyframeSelector>` and becomes exactly 19 runtime exports, 33 type exports and ten source-consuming `ParseResult` operations. Keyframes retains phase-to-timeline resolution but deletes its duplicate token parser/regex. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md`, W2/W7/W9/W17/W33; Keyframes `compile/selector.ts` and named-range tests. |
| V-A64 | The closed `/easing` surface omitted live BUILD consumers of `easeOutExpo`, `smoothStep3` and `easeInBounce`, while the plans promised their repositories would compile unchanged. | Retain those three as canonical sole implementations—not aliases—so `/easing` has exactly 16 runtime exports, five types and nine named numeric functions. `timingFunctionDescriptions` remains absent from Value and moves once as rendered UI copy to bbnf-buddy and Keyframes; no aggregate, property lookup, identity fallback or dual implementation survives. | `ARCHITECTURE.md §3`, `CONSUMER-CUT.md`, W2/W7/W9/W17/W33; Atlas, sci-report, Keyframes and bbnf-buddy live callsites. |
| V-A65 | W3 depended on no product wave yet required Glass-7 candidate/final artifact coordinates; W17 depended on W3, and W33 alone could create those coordinates. The resulting cycle made the coordination wave impossible to close. | W3 only decides each producer ask and records the present LANDED/RED/DECLINED coordinates; it requires no rehearsal, release candidate or final artifact. W17 creates the first clean `G_NEXT` rehearsal and compile-error inventory after Value 4.0.0 and `K_NEXT` rehearsal bytes exist. W33 alone marks built rows SHIPPED with the observed `G_NEXT` tag/tarball/integrity coordinates. | V-A17, D37, W3/W17/W33, `coordination/CONSTELLATION.md`. |
| V-A66 | V's authoritative wave table still titled W3 “Active BI reconciliation and cut rehearsal” after V-A65 removed every rehearsal obligation from W3. The stale title could restore the dependency cycle by implication. | W3 is titled **Post-BI Glass reconciliation and ask decision** everywhere authoritative. It records only present producer state and terminal ask decisions; W17 owns rehearsals and W33 alone owns final artifact coordinates. | `V.md` wave DAG, W3/W17/W33; V-A65. |
| V-A67 | The historical V-A53 disk census survived as a present-tense claim that Value “remains 3.1” while delegated C1 execution had already changed package/source paths. Both readings were false: the old facts were stale, while a dirty partial Value-4-shaped tree was still not a completed cut. | Historical censuses are explicitly dated/correction-time evidence. `PROGRESS.md` is the sole current execution ledger and may report **ACTIVE, ATOMIC / UNCOMPLETED** without inventorying volatile files. No partial source/package/test delta closes W7–W9, authorizes W17, or supplies a rehearsal, tag, publish or artifact coordinate. | `V.md`, W17, `PROGRESS.md`; active C1 owner boundary; V-A53. |
| V-A68 | W17 depended on W29 yet already required P047's final `0.66 ±0.015`, centroid/hull and 0px-geometry result, while W29 separately owned those same outcomes. The duplicated ownership let W17 demand a future product delta or let W29 pass on its own predecessor's result. | W17 validates the Glass producer measurement seam/protocol and freezes the pre-W29 baseline `b0` at `audit/pi/w17/p047-producer-baseline/manifest.json` for the four WebGPU/WebGL2 × DPR1/2 cases. W29 alone changes Picker `geometry.bodyRadius`, repeats the identical protocol and owns `audit/pi/w29/picker-painted-component/manifest.json` with final `0.66 ±0.015`, centroid/hull and 0px geometry. W17 contains no target-ratio acceptance claim; W32 consumes W29's final manifest. | W17/W29/W32, `EVIDENCE.md`, `coordination/CONSTELLATION.md`; bounded W29 executor reproduction. |
| V-A69 | The palette domain fixed exactly five principal-aftermath counters but later called the same tuple “six,” allowing a repair/status count to reappear while W16 still expected five. | Preserve the bounded `handles→complete` worker and its exact existing tuple, in canonical order: `{scannedHandles,transitionedActiveHandles,alreadyTrashedHandles,verifiedPaletteJobs,createdPaletteJobs}`. Enforce `scannedHandles=transitionedActiveHandles+alreadyTrashedHandles`, `createdPaletteJobs=transitionedActiveHandles`, and `verifiedPaletteJobs=alreadyTrashedHandles`. Schema, DTO, fixtures and completion equations reject every missing or additional counter; no sixth or repair count returns. | `PALETTE-DOMAIN.md`, W16; V-A59. |
| V-A70 | Browse/Library's child selection seat carried `aria-selected` without a listbox/grid/tab composite, so a generic palette Card could expose an orphaned state while the visual affordance appeared correct. | Each palette entity retains one native `<button type="button">` selection seat with `aria-pressed="true"` iff its `paletteId` equals the inspector's selected identity. At most one seat is pressed; no selection means all are false and the inspector is absent. Card/article roots are noninteractive containers and carry neither selection state nor composite role; they do **not** receive the HTML `inert` attribute because their live descendant seat must remain reachable. The reorder handle remains a separate control. ColorSpaceSelector keeps its proper `option`/`aria-selected` contract. Every older “inert Card/root” phrase is interpreted only as “root has zero activation/focus/selection behavior,” never as the DOM attribute. | `VISUAL-CONSTITUTION.md`, `OPTICAL-BENCH-COMPOSITIONS.md`, `PROPORTION-AUDIT.md`, W18/W22/W30/W32. |
| V-A71 | Committed-effect auditing named an event shell but did not close event cardinality, action/subject mapping, canonical projection bytes or correlation inheritance. Implementers could emit one versus two events for a compound effect, hash incompatible objects, omit delayed-job causality or invent ad hoc attempt fields. | One normative construction table maps every committed mutation/effect family to exact event count, `AuditAction`, `AuditSubject`, actor/command/correlation presence and exact before/after projection schema. Digests are domain-separated SHA-256 over RFC 8785 canonical projection bytes. Bootstrap mints and persists one canonical UUIDv7 correlation using existing event fields; delayed jobs persist and inherit its `causeCorrelationId`. Report creation and any policy effect have separately enumerated cardinalities. Table-driven vectors cover bootstrap, every mutation, report with/without policy effect, aftermath and purge. No outcome, attempt logger or extra parallel audit writer exists. | `PALETTE-DOMAIN.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-FACILITIES.md`, W10/W13/W14/W16/W24; V-A58. |
| V-A72 | W20 already required Picker's Blob to be a noninteractive specimen with Copy only in the ActionToolbar, but the live consumer still supplied `pressLabel`, Tooltip, Blob Copy and click/hover/pointer handlers. A visually larger Blob could therefore preserve duplicate activation, focus and announcement behavior beneath an apparently correct card. | The W20 consumer cut deletes the Blob Tooltip, press label, Blob Copy and every click/hover/pointer/focus activation listener without replacement. HeroBlob remains an inert rendered specimen; ActionToolbar owns the sole named Copy control. Ordinary focused tests and the routed Browser witness prove zero Blob activation/focus listeners, zero Blob Copy/Tooltip, exactly one ActionToolbar Copy, unchanged specimen rendering apart from W29's separately owned radius delta, and no new alias or wrapper path. Active source edits receive no completion credit until that evidence exists. | B-23, W20/W29/W30/W32; live `HeroBlob.vue`/`ColorPicker.vue` contradiction and bounded W20 consumer lane. |
| V-A73 | Delegated C1 changed `package.json` to the no-root seven-entry Value-4 map while its owner boundary excluded `demo/**`. The in-app Browser then failed first on `/math` during output rebuild and, after fresh output appeared, durably on removed `./parsing`; disk census found 26 `/parsing|/units` occurrences across sixteen active demo source files plus `tsconfig.demo.json`, alongside old symbol shapes under otherwise valid entries. W7 already requires value's own direct demo/internal consumers to use the exact new surface, so a package/src/test-only C1 can never be locally green. | W8→W9→W7 C1 includes every active value.js direct internal consumer in `demo/**`, demo TypeScript paths and its ordinary typecheck tests. Re-census the complete exact-name/kind import graph at execution time; migrate old color/value/parser objects and all direct `/parsing|/units|root` reaches to the seven canonical declarations, delete obsolete callsite adapters, and preserve the disjoint landed W20/W29 slices. The package export-map change, direct internal-consumer migration and V-A75's exact-seven bootstrap pack close atomically; W17 alone owns the old pinned Glass/Keyframes sibling graph and the full routed build/mount. No temporary old export, alias, local parser, fallback, dual path or source-only green is permitted. | W2/W7–W9/W17, `CONSUMER-CUT.md`; in-app Browser `http://127.0.0.1:9000/` and exact 2026-07-16 disk census. |
| V-A74 | After the remaining old direct source imports disappeared, the routed in-app Browser reported zero Vite overlays and zero warning/error logs while `#app` still contained no rendered descendants: the atmosphere ground painted, but the product was a blank page. “Zero overlay” alone was therefore a vacuous green over a failed mount. | The routed witness is the product, not the absence of one error surface. After V-A75 migrates the pinned sibling bytes in W17, the canonical Picker URL has a nonempty `#app` subtree, exactly one `<main>`, the selected color-space control and numeric headline, and exactly one named ActionToolbar Copy; only then may zero Vite overlay and zero uncaught runtime error contribute evidence. A blank mount, error-boundary substitute or static-ground-only frame remains RED and gives W17/W20 no credit. | W17/W20, `PROGRESS.md`; in-app Browser reproduction on 2026-07-16 after the V-A73 direct-import migration. |
| V-A75 | W7 required the routed demo to mount before W17 could enter, but exact-seven Value 4 necessarily breaks the demo's immutable Glass-5/Keyframes-5 pin: `index.html` imports `App` before `createApp().mount()`, Dock reaches pinned Glass, and pinned Keyframes' emitted `parse-flatten` asks a nested Value root for missing `parseCSSValues`. The broader installed Keyframes-5 graph also retains root/`/parsing`/`/units`, but the observed immediate abort is that bare-root missing export. `vue-tsc` was green while an isolated 4,677-module Vite build failed on the same binding. W7 waiting for that old sibling graph and W17 waiting for W7 formed a bootstrap cycle. | W7 closes its direct Value consumers and emits exactly one clean, unclaimed exact-seven **bootstrap transport** by unconditional remove→build→pack. `docs/tranches/V/audit/bootstrap/w7/value4-exact-seven/manifest.json` records tree, tarball path, bytes, SHA-512, seven-entry exports/declarations and `BOOTSTRAP ONLY`; the manifest remains, while its tarball is neither W17 rehearsal nor W33 candidate and is never published or installed as a compatibility layer. The 36,378-byte selector/emerging transport was handed off on the explicit Keyframes request, but V-A84 now marks it HELD/DEFECTIVE for rehearsal; W9 must emit a fresh replacement before W17 consumes anything. W17 then migrates and packs Keyframes `K_NEXT`, then Glass `G_NEXT`, deletes the pinned Glass/Keyframes graph, proves the nonempty routed Picker from those exact rehearsal dependencies, freshly recreates the Value rehearsal from the same frozen content, and deletes/replaces the bootstrap tarball. W17 no longer depends on W15; the palette-client rail may proceed independently, but any whole-app Browser claim consumes W17's migrated graph rather than the broken pin. No root restoration, alias, shim, file-link lock or dual major exists at any point. | W7/W15/W17/W33, `ARCHITECTURE.md §4`, `CONSUMER-CUT.md`, `coordination/CONSTELLATION.md`; native Browser plus isolated Vite-build reproduction and corrected producer handoff. |
| V-A76 | The exact `/css` grammar accepted percentages and named-range offsets but rejected standard keyframe selectors `from` and `to`. Keyframes' public `addFrame` accepts both, while the clean cut deletes its local selector parser, so the advertised sole grammar could not consume a live standard input. | `parseKeyframeSelector("from")` returns the same frozen typed percent selector as `0%`, and `to` the same as `100%`; canonical stylesheet serialization emits `0%`/`100%`, and reparsing preserves the typed value. Focused vectors cover both aliases, mixed-case rejection or normalization according to the grammar's existing ASCII policy, authored `0%`/`100%`, named offsets and malformed bounds. The runtime/type export counts do not change; Keyframes retains no local parser, alias or fallback. | W7/W9/W17, `ARCHITECTURE.md §3`, `CONSUMER-CUT.md`; active Keyframes-6 consumer probe. |
| V-A77 | V-A70 corrected palette Card roots to noninteractive containers with HTML `[inert]` count zero, but W32's evidence row still called the contract an “inert Card/article ARIA law.” That stale phrase could disable the native pressed-button descendant while every other selection assertion remained correct. | W32 evidence says **noninteractive Card/article container, HTML `[inert]` count `0`** and retains zero root activation/focus/selection state plus the one reachable native pressed-button seat. No normative palette Card/root/shell uses “inert” as a container adjective; legitimate Picker-Blob/modal/route/static-face uses remain distinct. | `EVIDENCE.md` W32, W18/W22/W30/W32; V-A70. |
| V-A78 | V predetermined Keyframes `6.0.0` and Glass `7.0.0` as tranche-epoch labels even though only Value's removed root/`/parsing`/`/units` plus Result break prove a major. Keyframes' public compatibility against published 5.3.5 and Glass's packed public delta against immutable 6.0.0 had not been adjudicated, so version prose could manufacture breaking-release coordinates without a public break. | Value remains fixed at 4.0.0. W17 compares Keyframes runtime exports, declarations, package entries and documented public behavior against 5.3.5, and Glass runtime/declaration/component prop-event-slot entries against immutable 6.0.0. A public-compatible Keyframes cut takes the next 5.x minor and a public-compatible Glass cut the next 6.x minor; only an enumerated incompatible public removal/signature/behavior change selects Keyframes 6.0.0 or Glass 7.0.0. The exact `K_NEXT`/`G_NEXT` versions, peer ranges, filenames and URLs are decided from that observed diff before rehearsal and recorded as coordinates only by W33. Artifact order remains Value 4 → Keyframes `K_NEXT` → Glass `G_NEXT`; no dirty package label, plan epoch or desired architecture proves a major. | D2/D37/D38, W2/W3/W17/W33, `ARCHITECTURE.md §4`, `CONSUMER-CUT.md`, `coordination/CONSTELLATION.md`; authoritative producer semver correction. |
| V-A79 | The bootstrap `/css` declarations advertised CSS value/custom-function/stylesheet AST capability, but its runtime rejected grammar already consumed by immutable Keyframes 5.3.5: `calc(1px + 2px)`, `if(supports(...))`, `sibling-index()` and arithmetic inside `@function`. Deleting Keyframes' local grammar against that surface would turn a public-compatible input into failure despite type and export parity. | W9 owns one canonical parser implementation for the declared grammar and removes any duplicate stylesheet normalization. Focused vectors cover successful immutable ASTs and canonical round trips for numeric `calc`, conditional `if(supports(...))`, `sibling-index()` and `@function` arithmetic, plus malformed operator/function/supports arms with exact nonempty diagnostics. The pre-bootstrap producer aggregate is recorded as `2,319/2,319`; the 36,378-byte replacement bootstrap recorded `2,320/2,320` for this selector/grammar set but is now superseded for rehearsal by V-A84's comment and zero-argument timeline defects. W7 rebuilds the exact-seven bootstrap only after the expanded suite and full ordinary aggregate are green; W17 then deletes Keyframes' local parser without a fallback, alias or dual grammar. Runtime/type export counts stay fixed unless a separately enumerated declaration defect proves otherwise. | W7/W9/W17, `ARCHITECTURE.md §3`, `CONSUMER-CUT.md`; active Keyframes-5.3.5 bootstrap consumer probe. |
| V-A80 | W29's nominal `bodyRadius=0.33` proxy did not satisfy its own raw-buffer product relation: measured diameter ratio was `0.6710…0.6712` and hull inset only `11.5…11.75%`, below the required 12%, even though centroid remained good. Source `2r=0.66` had again been mistaken for painted mass. | The sole effective Picker/HeroBlob paint delta is `geometry.bodyRadius:0.26→0.325`. Under the unchanged seed/color/config/frame and footprint, the measured WebGPU/WebGL2 × DPR1/2 result is diameter ratio `0.66019…0.66042`, centroid error ≤`1.342%`, hull inset exactly `12.0%`, AA edge ≤`1.885 CSS px` at DPR1 and ≤`0.948 CSS px` at DPR2, with identical alpha areas across engines. Chassis/seat/footprint movement remains 0px; no `heroScale`, second radius or public introspection appears. | B-05, W29/W32, `EVIDENCE.md`, `coordination/CONSTELLATION.md`; P047 raw-buffer lane. |
| V-A81 | The corrected bootstrap tarball was handed to Keyframes with exact bytes and hashes, but the durable manifest required by W7/D41 and referenced by every downstream consumer was absent on disk. A declared manifest coordinate without its file cannot establish exact-seven transport truth. | The smallest honest manifest now exists at `docs/tranches/V/audit/bootstrap/w7/value4-exact-seven/manifest.json`: it records the corrected tarball path, 36,378 bytes, SHA-256, raw SHA-512, npm integrity, source HEAD and pre-pack synthetic tree, `BOOTSTRAP ONLY`/unclaimed/unpublished disposition, exact seven no-root entries, `/css` 19/33 and `/easing` 16/5 counts, and focused 46/46 plus aggregate 2,320/2,320/types/lint/build evidence. It deliberately records no release coordinate, export-object hash or declaration-byte hashes that were not supplied. V-A84 now marks that exact tarball HELD/DEFECTIVE for rehearsal; W17 must consume a fresh replacement and create the rehearsal before any release credit. | W7/D41, `ARCHITECTURE.md §3–4`, `CONSUMER-CUT.md`, `coordination/CONSTELLATION.md`; pass-1 palette/API and UI/Glass disk checks. |
| V-A82 | “Inert” was still used as shorthand for data-bearing WatercolorDot faces in normative P051 prose after V-A77 reserved HTML `[inert]` for measured modal/route cases. That adjective could reintroduce an inaccessible DOM attribute while the intended rule was only noninteractive face semantics. | All affected wording now says **present noninteractive named list/text content with zero activation/focus/drag semantics**; the enclosing named seat remains reachable. HTML `[inert]` is explicitly not implied and remains a separate modal/route mechanism. This is a terminology/semantics correction only; no new face, wrapper, alias or accessibility path is introduced. | B-25, W17, `VISUAL-CONSTITUTION.md`, `coordination/CONSTELLATION.md`; pass-1 UI/Glass audit. |
| V-A83 | The cursor contract called catalog pagination an immutable snapshot, while the catalog domain and W14 wire contract recheck eligibility, access, current policy and current tags and may skip a row from an expired or changed rank epoch. The broad wording could freeze eligibility or contradict the live projection behavior. | Only catalog rank/order coordinates are immutable snapshot data. Eligibility, access, current-policy and current-tag projections are rechecked at read and may skip an epoch row; every other query remains a live keyset view. No stronger snapshot or eligibility-freeze claim is permitted, and unchanged eligible rows retain the no-repeat/no-skip continuation invariant. | `PALETTE-CURSOR-CONTRACT.md:85`, `PALETTE-DOMAIN.md:384`, `PALETTE-WIRE-CONTRACT.md`, W14; fresh palette/API audit. |
| V-A84 | The corrected 36,378-byte bootstrap still fails valid stylesheet input at the canonical producer boundary: a leading or inter-rule CSS block comment makes `parseStylesheet` reject the whole source, and declaration parsing routes valid zero-argument `animation-timeline: view()` through generic `parseCssValues`, which rejects it even though the dedicated timeline parser succeeds. Keyframes cannot strip comments or special-route/fallback this grammar without violating the sole-parser cut. | W9 adds producer-owned comment elision at stylesheet rule scanning (leading and inter-rule) and admits zero-argument timeline functions through the declaration grammar, with direct, canonical-roundtrip and named-failure vectors. Rebuild exactly one fresh unclaimed/unpublished replacement bootstrap only after focused/full/types/build/lint are green. Reclassify the 36,378-byte bootstrap as `HELD/DEFECTIVE` for rehearsal; retain it only as prior selector/emerging-CSS evidence. Keyframes adds no local normalizer, parser, route or fallback. | Keyframes compile-roundtrip failures, `/private/tmp/value-v4-bootstrap-20260716-selector-emerging/mkbabb-value.js-4.0.0.tgz`, W7/W9/W17; explicit producer defect handoff. |
| V-A85 | The 36,447-byte comments/timeline replacement makes V-A84's direct Keyframes fixture 21/21 green, but its public `collectAnimationOptions(declarations)` ignores the already-parsed structural `animation` shorthand and returns `{}` for `animation: 2s ease-in-out infinite alternate`. Keyframes temporarily classified that `CssValue`, creating a second semantic owner, while the singular type masked comma-separated animations. | W9 owns structural shorthand/list expansion and cascade. The 38,126-byte bootstrap is producer-green; Keyframes deleted the classifier trio. **Single-source value bootstrap-transport authority (L2, fixed W40-S5): 38,126 is the SOLE current transport figure (V-A85/V-A81 lineage); the 36,378 and 36,447-byte packs are superseded pre-replacement lineage. This is the value.js bootstrap family — distinct from the Keyframes archive family (184,xxx), neither restated as the other's figure.** V-A86/V-A87 hold earlier candidates; V-A93 later holds 184,710 bytes and authorizes the strict 184,254-byte replacement. Glass/native, Value rehearsal and routed Browser remain RED. | Bootstrap and rehearsal manifests, W7/W9/W17/W33; direct producer and consumer reports. |
| V-A86 | The 231,628-byte Keyframes archive passed its own source/type/aggregate/pack/offline-install checks, but Glass's real demo production build exposed a packed-runtime boundary defect: `dist/engine/index.js` dynamically imports undeclared bare `prettier`, a PostCSS chunk is bundled into the runtime, and public `printWidth` survives. The package-local suite therefore produced a green-over-broken rehearsal that Glass cannot consume. | Reclassify that exact archive as **HELD / DEFECTIVE**, retaining its hashes and packed semver diff only as historical evidence. Keyframes W17 owns one clean correction: remove every Prettier/PostCSS runtime reach and bundled chunk, delete public `printWidth` from runtime and declarations, preserve exact Value `4.0.0`, and emit one fresh unclaimed/unpublished archive only after source/types/full aggregate/build/pack, tar-member/dependency/export inspection, isolated exact-Value4 install and the real Glass demo production build are green. Glass adds no alias, externalization exception, hidden peer/optional dependency, fallback or masking path and packs nothing from the held bytes. | `audit/rehearsal/w17/keyframes-6.0.0/manifest.json`, W17/W33, active Glass P127 production-build consume; exact Keyframes owner handoff. |
| V-A87 | The first formatter-free Keyframes correction removed the forbidden package reaches but its owned deterministic indentation was semantically malformed: five full-aggregate failures replayed compiled CSS into empty frames and collapsed color densification. | The 184,684-byte archive remains **HELD / DEFECTIVE**. The 184,708-byte intermediate is full-aggregate green and Glass-validated; 184,710 bytes later superseded it for runtime/gh-pages evidence but V-A93 now holds that archive too. The authoritative lineage continues only through 184,254 bytes. | `audit/rehearsal/w17/keyframes-6.0.0/manifest.json`, W17. |
| V-A88 | The 966,350-byte Glass 7 rehearsal passes its runtime/build/profile and enumerated native checks, but its reachable public declarations directly import undeclared transitive packages: `embla-carousel` from `Carousel.vue.d.ts`/`useCarousel.d.ts` and `@vue/shared` from `toast/use-toast.d.ts`. The manifest's broad `onlyDeclaredPeers:true` therefore confused runtime JS cleanliness with strict declaration-consumer isolation; successful hoisted resolution through `embla-carousel-vue`/Vue is not a declared package contract. | Reclassify the exact Glass archive **HELD / DEFECTIVE FOR STRICT DECLARATION ISOLATION**. Current Glass disk adds direct optional peer ownership for `embla-carousel` and removes the accidental `@vue/shared` toast declaration reach; this is source truth only until repacked. The acceptance probe walks every `.d.ts` reachable from packed `exports`/`typesVersions`, resolves relatives, derives each bare package root and requires direct packed dependency/peer/optional ownership; the residual set is empty. A no-lock install is supplementary because hoisting can mask the defect. No shim, alias, fallback, `skipLibCheck` or transitive-hoisting reliance. Emit one fresh unclaimed/unpublished rehearsal with exact bytes/hashes/export/declaration/dependency evidence; W33 alone releases. | `audit/rehearsal/w17/glass-7.0.0/manifest.json`, W17/W33; independent packed declaration traversal, disk recheck and direct Glass handoff. |
| V-A89 | The first native Browser pass over Keyframes' final gh-pages build found a visible-but-inoperable DarkModeToggle: `EditorShell.vue` passed obsolete Glass prop `position="right"`, so Glass 7 ignored it, defaulted the HeaderRibbon left beneath the z-panel and the control's center hit target resolved to the overlying panel. Package/type/build green did not attest the composed interaction. | Keyframes owns the direct composition correction `position="right"` → public Glass 7 `placement="right"`. No z-index escalation, click forwarding, pointer override, event workaround, alias or fallback is permitted. `npm run check` and the 3,554-module gh-pages build are green; the new immutable native input is `/private/tmp/keyframes-gh-pages-w17-placement-final-20260716` (53 files, 6,761,455 aggregate bytes; manifest SHA-256 `d650a049e8eb1f9653759e4febe510a13556d0aaa484eb3d5527a7f4fa36d162`; `index.html` SHA-256 `1c360600ec2cfac3bac4a088019a26bf0dcfcaac44a1c9ba04a05427492f10fa`). The host in-app Browser re-runs the stopped DarkModeToggle path first and stops on GREEN or the next material defect. This demo-only correction does not mutate the 10-file 184,710-byte library rehearsal, but W17 native close remains RED until the re-close returns. | Keyframes W17 rehearsal manifest, W17; native in-app Browser defect and exact Keyframes owner handoff. |
| V-A90 | V-A89's correct `placement="right"` exposed a Glass HeaderRibbon state-model defect: at 1280px the band collapsed to 14px, actions to 0px, and the 32px DarkModeToggle lay at `x=1295…1327`. The pass-through `#anchor` rendered no interactive control, but visibility was still governed by `pinned || pointerWithin || focusWithin`; with no anchor hit target, the action-only ribbon could never enter a revealing state. | Glass distinguishes a genuinely rendered interactive anchor from an anchorless action-only ribbon. Disk now implements `!hasAnchorControl || state`, DOM-based interactive-anchor detection, non-interactive anchor guards and ordinary anchorless-left/right plus anchored focus/pointer/pin/Escape vectors. Fresh pack and native evidence remain required: anchorless left/right nonzero in-viewport clickable actions plus anchored left/right collapsed/expanded keyboard/hover/touch/toggle behavior. Keyframes adds no width/min-width, z-index, overflow, placement or event workaround. One fresh Glass rehearsal closes V-A88 and V-A90; Keyframes then consumes exact bytes, rebuilds/refreezes and re-closes. | W17, Glass HeaderRibbon component/native Browser lane, Keyframes producer liaison and ownership decline. |
| V-A91 | The first V-A88/V-A90 source correction was eventually correct but could still green over both original mechanisms. Glass's declaration verifier followed relative `.d.ts` edges yet ignored residual bare-package imports and its installed-consumer fixture set `skipLibCheck:true`; it therefore could not attest direct declaration ownership. HeaderRibbon initialized anchor truth from slot existence, so the exact supplied-but-empty Keyframes anchor first rendered collapsed/inert and became expanded only after mounted DOM inspection; the focused test waited through that frame, while interactive↔empty transitions were unchallenged. | Keep the same W17 owners and archive boundary. Current Glass disk now derives residual bare roots from every reachable declaration, requires direct dependency/peer/optional ownership and typechecks the packed consumer without `skipLibCheck`; HeaderRibbon uses synchronous valid-VNode classification, omits phantom anchor spacing, covers anchored↔action-only transitions and preserves focus/inert behavior. These are **LANDED/IMPLEMENTED SOURCE SLICES**, not rehearsal credit. Fresh pack plus in-app Browser must still prove first-frame anchorless left/right actionability and genuinely anchored left/right reveal/pin/Escape behavior against final Keyframes bytes. No timeout, opacity, width, z-index, overflow, consumer omission or event workaround may hide the stale frame. | `../glass-ui/scripts/verify-export-types.mjs`, `../glass-ui/src/components/header-ribbon/HeaderRibbon.vue`, focused contracts and native W17 lane; independent source audit, Keyframes ACK and direct Glass handoff. |
| V-A92 | V-A91's synchronous valid-VNode classifier fixes the empty-fragment first frame but still equates renderable content with an interactive anchor. Text, a noninteractive element, a disabled control or an opaque component VNode can set the ribbon collapsible while `getAnchorControl()` returns null, recreating V-A90's unreachable action track and impossible Escape focus return under a new predicate. A VNode-shape heuristic cannot prove the rendered accessibility contract. | **SUPERSEDED-IN-PART — persistent-only (2026-07-16 glass mark, `GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md`):** Glass 7 CUT the `mode="collapsible"` arm; HeaderRibbon flattens to persistent-only (props `{ placement?, ariaLabel?, class }` + `#items` slot), deleting the anchor button/slot, `anchorLabel`, the reveal/pin/Escape/focus-presence machinery, `HeaderRibbonMode`, `HeaderRibbonAnchorSlotProps` and `--header-ribbon-actions-width`. V-A92's CORE intent is fully preserved (VNode inference stays dead; keyframes' action-only composition stays persistent; Glass owns no inference — no mode remains to infer). Only the opt-in-collapsible arm of the prescription below is retired. — Original prescription: Replace anchor inference with one explicit discriminated public behavior contract in Glass 7: omitted/`mode="persistent"` is expanded from the first frame; `mode="collapsible"` requires a non-empty `anchorLabel`, makes Glass own the real focusable disclosure button, and owns collapse/reveal/pin/Escape. The consumer's `anchor` slot is decoration inside that button, never the interaction oracle. Glass's two collapsible stories opt in; Keyframes's action-only composition remains persistent without omitting its pass-through slot or guessing source state. Ordinary tests cover absent/empty/text decoration, left/right, first render, mode transitions, required label typing, SSR and focus/pointer/touch/Escape behavior; the in-app Browser proves both valid modes. Delete DOM/VNode inference rather than retaining it as a fallback. No observer, timeout, consumer slot omission, width, z-index, overflow or event patch. | HeaderRibbon public props/types/component/stories/contracts, W17 native lane, active Glass and Keyframes tasks; pre-pack consumer census. |
| V-A93 | Glass's now-nonvacuous `skipLibCheck:false` packed consumer rejects the exact 184,710-byte Keyframes declaration at `dist/keyframes.d.ts:3578`: emitted `SpringProgress implements SpringPlayback`, but declaration stripping omits `disposed`, `_onFrame` and `_emitManagedFrame`, yielding TS2420. Package-local types/build/aggregate and declaration-kernel scans therefore greened over a public declaration that no strict consumer can typecheck. | Reclassify the 184,710-byte archive **HELD / DEFECTIVE FOR STRICT DECLARATION CONSUMPTION** while retaining its runtime, aggregate, gh-pages, packed-diff and semver evidence as historical. The internal `SpringPlayback` structural seam remains internal; current Keyframes source removes the public `implements` claim instead of exposing internal loop members or weakening `stripInternal`. The existing Keyframes W17 owner runs focused/full/types/build/pack plus a strict external consumer and returns one newly named unclaimed archive with exact coordinates. Glass consumes only those fresh bytes; it adds no `skipLibCheck`, patch, declaration augmentation or older-archive fallback. Separate Glass dependency declarations remain independently owned. | Exact 184,710-byte tgz declaration SHA-256 `6efc83c7d6a7e1c8182cdbad2ef53deec0b89899170eb835698fb13735ef0281`, Keyframes `spring/progress.ts` and `spring/types.ts`, Glass strict consumer, W17/W33; direct Glass→Keyframes handoff. |
| V-A94 | The native Aurora Q003 pass truthfully gated its “move to shape the field” interaction cue, but the cue was physically outside the painted aperture: the stage retained a 480px minimum while the visible studio track clipped near 344px, so the bottom-anchored prompt was cropped. Correct state logic therefore greened over broken layout ownership. Root independently reproduced the mechanism at 1280×720: stage/canvas `480px`, `.configurator-stage` `340.296875px`, `overflow:hidden`. | **IMPLEMENTED SOURCE/NATIVE GEOMETRY, NOT REHEARSAL CREDIT.** Current `AuroraStage.vue` deletes `min-h-[30rem]` and yields through `h-full min-h-0`. Root's fresh in-app Browser measures stage and aperture both exactly `340.296875px`; the ready WebGPU cue paints inside the actual field. The existing Q003 owner retains an ordinary regression vector binding the deleted floor/yielding contract and completes remaining responsive/native postures before pack. Cue legibility remains part of the same visual judgment. No offset, overflow reveal, duplicate cue, minimum-height inflation or consumer override. | `../glass-ui/demo/stories/substrates/aurora/AuroraStage.vue`; focused Aurora affordance contract; W17/Q003 native Browser and direct handoff. |
| V-A95 | In the same native Q003 pass, WebGPU drag remained responsive but large black rectangular layers then replaced portions of the preset ribbon, control text and bottom navigation. Root independently reproduced black rectangular preset layers immediately after a native drag on WebGPU `apple · metal-3`; they cleared within 900ms with zero warning/error logs. After V-A94's exact stage/aperture correction, a forward drag remained intact but a reverse drag plus 70ms capture again produced broad black slabs across preset, studio and navigation while `stageH=apertureH=340.296875px`. A fresh computed-style census grounds the mechanism: the live route allocates 95 backdrop-filter elements, 92 nested beneath another filtered surface, including one filtered Configurator containing all 17 filtered preset tiles—even the offscreen scroll tiles—and 52 filtered slider ranges. The mechanism is therefore still live, independent of the old overflow, and concentrated in shared nested material ownership rather than a single bad rectangle. | The existing Glass renderer/material/layering owner corrects the shared compositor boundary so each visual surface/scroll region owns one authoritative backdrop sample while nested cels retain their fill, rim, cast and specular affordance without allocating a filtered backing per offscreen child. Prove controls, preset ribbon and bottom navigation remain visually and interactively intact through repeated forward **and reverse** drags across the relevant WebGPU/WebGL2, scheme and reduced-motion states. Transience or nondeterminism does not make the defect green. The result cannot be hidden with z-index covers, raster/static replacement, engine masking, input-triggered canvas reset, per-site layer hacks or an overlay fallback. Exact paths, tests and native observations are required before fresh Glass rehearsal bytes. | Active Glass Q003 native/material owner, W17 in-app Browser lane and direct Value→Glass handoff; root 1280×720 forward/reverse immediate/settled captures plus bounded live DOM/computed-style topology census. |
| V-A96 | V-A88's nonvacuous strict consumer exposed a second declaration-closure mechanism after Glass's own direct-package corrections: stable Reka declarations import `DayOfWeek`, which stable `@internationalized/date` does not export, and VueUse declarations require ambient Web Bluetooth globals that ordinary TS 6 consumption does not activate. These are upstream implementation-type chains escaping through Glass's public declarations, not permission to pin a nightly or weaken the consumer. | The existing Glass strict-declaration owner inventories every reachable packed `reka-ui`/`@vueuse/core` type reach by actual Glass public family, then retains only props/events/types Glass intentionally promises as the smallest Glass-owned semantic contracts. Consumerless forwarding wrappers/types retire instead of being copied; runtime peers remain honest internals. Packed declarations contain zero accidental Reka/VueUse bare-type reach, while any deliberately retained upstream public type has one coherent directly declared stable dependency/type-environment contract and explicit rationale. A before/after reach census plus a fresh no-lock `skipLibCheck:false` consumer proves closure. No kitchen-sink type copy, alias facade preserving the chain, nightly pin, `node_modules` patch, ambient global injection, `types` override, `skipLibCheck`, declaration shim or externalization mask. | Active Glass P127/W17 strict-declaration lane; strict TS 6 consumer and direct Glass task report. |
| V-A97 | The strict-final Keyframes 6 rehearsal is product-green, but its mandatory clean release cannot precede immutable Value 4, and its source graph now dev-depends on unpublished Glass 7 while the mandatory `npm ci` lock still names the old graph. The producer root is also non-hermetic: Value `package.json:86–87` and its lock contain reverse `file:../glass-ui` and `file:../keyframes.js` dev edges, while `test/aurora-motion.test.ts` and `test/aurora-bracket.test.ts` are Glass/demo integration fixtures inside the producer test set. Treating either cycle as a package failure would invite a file link, forced resolution, fabricated lock or CI bypass; treating the rehearsal as releasable would invert the authoritative producer order. | Preserve one asymmetric producer DAG. For the Value 4 tag, delete the reverse file dev edges and move the two Aurora integration fixtures to the later consumer/demo lane; regenerate a registry-only lock and run `npm ci`, library types/build and the full producer-only test set before publishing the governed Value candidate with provenance. Keyframes then refreshes its mandatory lock from that registry artifact and publishes from its Value-only packed runtime graph while Glass remains a demo-only rehearsal input, not a runtime or release prerequisite. Glass 7 publishes from exact registry Value 4 + Keyframes 6. Only afterward may Value/Keyframes demos restore **registry** consumer edges and refresh their ordinary locks. No file/tarball path in a committed lock, force flag, stale-lock acceptance, shim, hidden peer, circular publication or CI bypass. | `package.json:86–87`, `package-lock.json`, `test/aurora-motion.test.ts`, `test/aurora-bracket.test.ts`; D2/D38/D41, W17/W33, authoritative 184,254-byte Keyframes rehearsal manifest and direct Keyframes producer-DAG handoff. |
| V-A98 | Moving the two direct Glass fixtures alone would leave `npm test` non-hermetic: `test/preview-chips.test.ts` imported `auroraHarmonyStops` and `resolveCalibratedAtmosphere`, whose calibration module runtime-imports Glass Aurora. A direct-import census would green over that transitive consumer edge. The release workflow also repacked implicitly through bare `npm publish --provenance`, so it could not preserve or prove the exact designated candidate. | Keep exactly two moved files. Move `aurora-motion.test.ts` and `aurora-bracket.test.ts` under `demo/test/glass/`, fold the Aurora-only preview-harmony describe into the moved bracket fixture, and leave only Value-owned interpolation-ramp assertions in `test/preview-chips.test.ts`. The existing `vitest.config.ts` include defines the producer suite without an exclusion. The provenance job packs exactly once, records hashes/coordinates, uploads that file, publishes its exact pathname, and then checks registry integrity/shasum/gitHead. After Glass 7 publishes, one demo-consumer config/script runs the moved fixtures against registry dependencies. | Independent producer-test/release audit; `test/preview-chips.test.ts`, `demo/test/glass/aurora-bracket.test.ts`, `demo/test/glass/aurora-motion.test.ts`, `vitest.config.ts`, `.github/workflows/release.yml`; W33/V-A97. |

| V-A99 | A fresh isolated `npm ci` removed the contaminated working `node_modules` and exposed 34 failing suites: 63 source files unreachable from every Value 4 export still implemented the retired mutable Value 3 parser/unit/color graph and eight imported the removed `@mkbabb/parse-that`. The obsolete benchmark and CI rails invoked the same dead graph. The first clean-break deletion then contracted the aggregate to 315 without proving that every surviving public behavior had been transposed. | Delete the unreachable Value 3 implementation and mechanism-specific tests instead of restoring `parse-that` or filtering failures. Retain the exact 24-file TypeScript closure reached by the seven public entries. Before tag, map every runtime/type export to direct Value 4 behavior, port the still-valid lerpArray/2D-transform/slerp/interpolation/CSS/easing/color/quantize oracles, assert the exact layout-unit and quart/quint tables, and compile all 62 public types through an isolated packed `skipLibCheck:false` consumer. Legacy class/parser/unit behavior retires; Aurora/ColorPicker/docs fixtures move to the later consumer lane. | Fresh isolated install, `tsconfig.lib.json --listFilesOnly`, independent seven-entry coverage and legacy-salvage audits; `src/v4/**`, `test/v4-*.test.ts`, `fixtures/public-types/value-v4.ts`, `scripts/ci/verify-packed-surface.mjs`; W7/W17/W33. |
| V-A100 | Public CSS validity was permissive at two authority points: `coerceToSyntax` returned success for every syntactically bracketed but unknown component, while `@property` accepted non-custom names, missing required descriptors, invalid syntax, invalid/missing initial values and every non-`true` inherits token as `false`. Consumers could therefore animate a guaranteed-invalid registration behind a green AST. | One closed syntax classifier owns supported components and their structural matches; unknown components fail `syntax_descriptor_invalid`, known mismatches fail `syntax_mismatch`, and integer/number/length/percentage/angle/time/resolution/flex/custom-ident/transform distinctions are explicit. `@property` requires a dashed custom-property name, supported syntax, literal `true|false`, and a syntax-conforming initial value except for universal `*`. The stylesheet uses the same coercer; Keyframes adds no parser, validator or fallback. | `src/v4/css/grammar.ts`, `src/v4/css/stylesheet.ts`, `test/v4-css-public.test.ts`; independent direct probes; W9/W17. |
| V-A101 | Quantize deduplication discarded a duplicate centroid's population and excluded achromatic centroids from dedupe entirely because it required a numeric hue even though the distance uses only lightness/chroma. Population conservation and dominant ordering could be false while the shallow uniform smoke stayed green. | Deduplication compares its actual L/C metric regardless of powerless hue and immutably folds the duplicate population into the retained row. Direct Value 4 vectors cover typed failures, transparent success, multi-cluster ordering, near-centroid population merge, downsample accounting, deterministic chromaWeight divergence and dominant selection. No old quantizer API or random harness returns. | `src/v4/quantize.ts`, `test/v4-quantize.test.ts`; legacy-salvage audit and born-RED near-achromatic fixture; W8/W17. |
| V-A102 | `safeAccentColor` could certify a floating-point candidate that fell below the requested contrast floor after its public RGBA8 projection; conversely, certifying bytes alone selected colors below the unquantized CSS-paint ratio used by existing consumers. Either single oracle could green while another supported rendering path breached. | Candidate selection requires the minimum of independently computed float-sRGB and ties-to-even clipped RGBA8 WCAG ratios to clear the requested floor against the same supplied surface. The search still preserves hue and chooses the nearest passing lightness; no magic margin, guessed surface, proxy lightness or consumer re-guard is added. Direct 4.5:1 projected-byte and existing 3:1 float consumer matrices both remain green. | `src/v4/color/operations.ts`, `test/v4-color-behavior.test.ts`, `test/view-accents.test.ts`; focused born-RED 4.480134 witness; W8/W17. |
| V-A103 | The tag workflow redirected bare `npm pack --json` into a JSON file, but the unconditional `prepare` build writes Vite output to stdout; the record was therefore unparsable before publication. The packed tarball also lacked `gitHead` when published by pathname, while package metadata and release/subpath canon still advertised retired units, manual/file-sibling publication and a root barrel. | Build and test first, stamp the peeled tag commit into the CI-only package manifest, then run the sole candidate pack with `--ignore-scripts`; this packages the already-checked output once and yields clean JSON. Verify the exact tarball through the isolated runtime/type consumer, preserve it, publish its pathname with provenance, and compare registry version/integrity/shasum/gitHead. README, changelog, metadata, `docs/RELEASE.md` and subpath canon describe only the exact-seven hermetic workflow. | `.github/workflows/release.yml`, `.github/workflows/ci.yml`, `package.json`, `docs/RELEASE.md`, `src/subpaths/CLAUDE.md`; fresh pack reproduction; W33. |
| V-A104 | The pages workflow still listens for workflow name `CI` although the producer workflow is `ci`, and checks out/builds file-sibling Glass/Keyframes trees. `scripts/dev/dev.sh` and demo Vite prose likewise assume local siblings. Folding those assumptions into the producer tag would recreate the reverse cycle; ignoring them forever would strand deployment. | Keep pages/dev/demo configuration out of the Value producer release commit. After immutable Value 4, Keyframes 6 and Glass 7 exist in that order, one registry-backed consumer commit refreshes demo dependencies/lock, removes file-sibling build assumptions, changes the deploy trigger to `ci`, and validates the routed product in the in-app Browser. This is an explicit post-Glass consumer disposition, not a release blocker or a file-link exception. | `.github/workflows/deploy-pages.yml`, `scripts/dev/dev.sh`, `vite.config.ts`; V-A97 producer DAG; W17/W33 consumer aftermath. |
| V-A105 | The first isolated packed rehearsal falsely rejected every valid exact entry because its verifier used CommonJS `require.resolve` against an intentionally ESM import-only export map. A gate aimed at package truth cannot demand a second CommonJS condition that the public contract explicitly does not expose. | The verifier installs the exact tarball with a private temporary npm cache, compiles all 62 public types under strict `skipLibCheck:false`, then executes a real ESM consumer from inside that installation. It counts the seven runtime entries and dynamically imports the forbidden root, `/parsing` and `/units` to prove rejection. No `require` condition, alias or compatibility export is added. | `scripts/ci/verify-packed-surface.mjs`; V-A99/V-A103 packed consumer boundary; W7/W33. |
| V-A106 | The corrected ESM verifier still compared only export counts, treated any thrown evaluation error as proof that a forbidden path was absent, and compiled type-only imports without exercising the declarations of runtime values. Same-count renames, an extra subpath, or an exported-but-throwing legacy entry could therefore remain green. | Compare the installed `package.json#exports` to the exact seven keys and exact declaration/runtime targets; compare every runtime namespace to its exact sorted name list; compile all 62 public types plus bindings from all seven runtime namespaces; and accept only `ERR_PACKAGE_PATH_NOT_EXPORTED` for the forbidden root, `/parsing` and `/units`. | Independent release-surface audit; `fixtures/public-types/value-v4.ts`, `scripts/ci/verify-packed-surface.mjs`; W7/W33. |
| V-A107 | Selective staging revealed a hidden producer-test collision: five tests remain under `test/` but are demo consumers. Four have unstaged Value-4 demo migrations; the unchanged `gradient-parse.test.ts` silently restores a HEAD dependency closure that imports deleted `/parsing`, `/units` and retired color names. Staging the migrations would contaminate the producer commit, while excluding the files would create a contrived second corpus. | Remove the five demo-consumer tests from the producer index while preserving their working copies for the post-Glass consumer commit: `gradient-parse`, `ink`, `preview-chips`, `value-domain-clamp` and `view-accents`. Keep the two named Glass fixtures and new demo-only consumer tests on that same later physical lane. The exact exported producer index must pass its ordinary unfiltered `npm test`; no exclusion or compatibility path is introduced. | Independent selective-index audit; V-A97/V-A98 producer partition; W17/W33 consumer aftermath. |
| **HISTORICAL / SUPERSEDED BY V-A125 — V-A108** | Glass Q003 falsified its remaining product-side hypotheses for V-A95. Native Browser capture with exactly one backdrop sampler, no nested filter and no filtered canvas ancestor stayed intact at +70ms but produced broad black slabs over every filtered chrome region at +900ms while ordinary DOM/WebGPU state remained sound. Chromium main `632a1e7b1be280befa88e7aa3244de5b3fbf37d5` (crrev 1661567, bug 523643493) independently repairs an HTML-in-canvas/decomposited-backdrop path by adding a post-filter saveLayer and clearing outside filter bounds. | The former `PENDING-HEAL`/browser-upgrade disposition is historical, not a current wait. V-A125 withdraws Chromium-only attribution, records the reverted 3/0 experiment, restores the honest 5/0 baseline and requires native live-backdrop-adjacency correction or an evidence-backed decline. No route/engine mask, backing plane, filter target, sampler or material degradation is authorized. | Historical V-A108 evidence; current owner W17/W33 under V-A125. |

| V-A109 | Release run `29496836585` / job `87615659200` failed at **pack sole release candidate** before verification or publish: the JSON file was contaminated by unconditional `prepare`/Vite stdout (ANSI/build output), so the pack record could not be parsed; npm remained E404. The first correction `e2652f1c` (`--silent`) failed identically in run `29497496655`, proving presentation flags were not an authority boundary. | Producer-only release correction `44ddaff7a22283a4f7a42608893eeae7bc234424` (tree `919dc0b4f912ebf4d62644fbc81d0d747c567ff1`, parent `e2652f1c`) isolates an empty `$RUNNER_TEMP/value-pack` destination, redirects pack chatter to an ordinary log, requires exactly one discovered `.tgz`, reads and hashes that archive directly, validates embedded name/version/`gitHead`, and emits the absolute path through `GITHUB_OUTPUT`. No npm stream parsing, file edge, stale lock, force flag, shim, manual byte substitution or CI bypass remains. Re-tagging `v4.0.0` at that commit produced successful provenance run `29497728532` (#13; 1m33s; publish and registry-coordinate steps green). The immutable registry record is `@mkbabb/value.js@4.0.0`, `gitHead 44ddaff7a22283a4f7a42608893eeae7bc234424`, tarball `https://registry.npmjs.org/@mkbabb/value.js/-/value.js-4.0.0.tgz`, integrity `sha512-Z8ywb4htSxJlRFvoU1DNtvzr9Bsuaw9ahT/hvNlKbnRj6fTnLuXjn0itKq1Q5s6rwg24ct0zcLZ04BuR3/SzGw==`, shasum `ccb962e592fb42e6602fc2bb6afbfb1763788b9d`. npm page shows version `4.0.0` published and provenance linked to run `29497728532` / transparency log `2184431812`; local strict verifier reports runtime `23/1/19/16/9/9/2` and `strictTypes 62`. | `.release-working/.github/workflows/release.yml`, commits `e2652f1c`, `44ddaff7`; GitHub runs `29496836585`, `29497496655`, `29497728532`; npm immutable metadata; W33 producer release lane. |

| V-A110 | Value's immutable release unblocks, but does not itself close, the Keyframes producer boundary. The prepared isolated Keyframes clone still carries its old 5.3.5/Value-3/optional-Glass lock. The exact registry-only refresh command—`npm install --package-lock-only --ignore-scripts --cache /private/tmp/keyframes6-registry-cache --no-audit --no-fund`—failed before mutating that lock because this Value execution shell returned `ENOTFOUND registry.npmjs.org` for `@mkbabb/value.js`; its private cache contains only Value 1.2.0/3.1.0 records. | Keyframes owns one fresh lock generated by a resolver that can reach npm: root `6.0.0`, exact registry Value `4.0.0` URL/integrity, and zero Glass, Value-3, `file:`, `link:`, git or optional reverse edges. The failed shell is an explicit environmental block, never permission to assemble a lock from rehearsal tarballs, old cache metadata or hand-written package records. Once that lock is real, Keyframes runs ordinary `npm ci`, source/types/build/full/packed evidence and publishes its own immutable producer artifact; Glass remains downstream and receives only published registry Value 4 + Keyframes 6. | Direct Value→Keyframes registry handoff; `/private/tmp/keyframes6-producer-a59d3a22-20260716.7I8qfV/repo`; private-cache resolver log `2026-07-16T12_28_44_089Z-debug-0.log`; W17/W33 DAG. |

| V-A111 | Fresh in-app Browser inspection of the canonical Picker route `http://127.0.0.1:9000/#/?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` is a renewed blank-mount witness: after a 1.8s settle, `#app` has `children.length=0`, `<main>` count is `0`, body text is empty, `vite-error-overlay` count is `0`, and the captured surface is only chromatic ground. The route title resolves `Color Picker`; this is not routing absence, a visual pass, or proof that the old A74 bootstrap failure has recurred. | W17's Value/demo boot owner must locate and remove the current single boot mechanism from the live graph, preserving exact capability imports and forbidding a static ground, root restore, alias, fallback, second app mount or error mask. The repair earns no Picker/UI credit until the in-app Browser observes one nonempty app subtree, exactly one main, selected Lab space, the numeric headline, and ActionToolbar's sole Copy at the same canonical URL. | Fresh in-app Browser route inspection 2026-07-16; W17 routed-mount evidence and V-A74 history. |

| V-A112 | The actual Picker source still preserves the retired Card/reservation topology: `ColorPicker.vue` roots the instrument in `<Card>`, and `seat.css` gives `.title-row` Blob-derived `padding-right` and `min-height`. That contradicts W18's one clean transposition to `InstrumentChassis`, W20's title-gap-only hierarchy and PR-01's explicit `reserve=none`. The inert-Blob/sole-Copy and W29 radius leaves do not remove this housing or blank-air mechanism. | W18 owns the single Card→InstrumentChassis transposition; W20 owns removal of the Blob-derived title reservation and the one-line/readout type-gap cut. Do not shrink the Card, add a local chassis wrapper or preserve a clearance reserve. The direct Browser proof keeps the old outer rect at 0px change while the title→headline gap is `G`, ink meets the PR-01 inequality, the title row has no Blob-derived block reservation, and the footprint/readout clearance remains real. W29's final material criterion remains `≤1.342%` centroid per axis, exactly `12.0%` hull inset and its stated DPR/engine rows; W20's earlier layout check is only the looser pre-paint placement stage. | `demo/@/components/custom/color-picker/ColorPicker.vue:5–7`; `demo/@/components/custom/color-picker/seat.css:81–90`; W18/W20/W29; `research/proportion-register.md` PR-01/PR-03. |

| V-A113 | Palette CRUD has a complete 50-target surface, but several Facilities rows list contributor ranges rather than one primary implementation writer. Its export contract also calls the product CSS formatter “canonical CSS color spelling,” which can be misread as a second Value CSS API. | W1 writes one compact 50-row primary-writer/consumed-contract/consumer map: W10–W14 own server mechanisms, W15 transport, W16 real-stack verification and W22–W24 visible consumers; a range is never a primary writer. W23's fixed-point CSS serializer accepts only `ExportSnapshot` atoms and creates deterministic product bytes; it is neither a `Color` conversion nor a generic parse/serialize API and may not replace W9's exact `/css` boundary. No route, serializer, barrel, fallback or duplicate Color→CSS authority is added. | `PALETTE-FACILITIES.md` target table; W1/W9/W23; `PALETTE-EXPORT-CONTRACT.md` CSS serializer. |

| V-A114 | V-A111's blank mount has one concrete current mechanism. `index.html` statically evaluates the App/router graph before `createApp().mount()`. That graph loads Glass `5.0.0`/Keyframes `5.x` packed runtime, which still imports the removed Value root and `/parsing` APIs; Vite's existing log records `parseCSSValues` missing from the nested legacy Value root. The Glass symlink's nested Value `3.1.0` and Keyframes `5.2.0` make direct package imports falsely green, but create a dual-core graph rather than a valid Value-4 consumer. `vue-tsc` passes because it does not execute that packed runtime closure. A separate lazy About/doc closure still has stale `@src/units` imports and fails the full demo build; it is independent of the initial Picker boot abort. | W17 follows the ordered clean cut: Keyframes obtains its real registry-only Value-4 lock and publishes 6; Glass migrates/rebuilds against registry Value 4 + Keyframes 6 and publishes 7; value then refreshes its registry consumer graph and removes every stale root/`/parsing`/`/units` doc import. No root restoration, alias, nested dependency mask, file/tarball lock, source-resolution dodge, local parser, error boundary/static ground or second mount is permitted. The canonical Browser route must then prove the V-A111 nonempty subtree/main/headline/sole-Copy witness; full demo build proves the separate About closure. | `demo/color-picker/index.html:205–212`; `demo/color-picker/App.vue:190–192`; Glass 5 packed `dist/aurora.js:11`, `dist/dock.js:27`; Vite log `/private/tmp/vjs-dev-9000.log:134285+`, `:135657–135676`; `assets/docs/rgb.md:2`; W17/W33. |

| V-A115 | V-A110's environmental resolver block is discharged by independently byte-identical registry-only Keyframes locks. The authoritative producer-clone `package-lock.json` SHA-256 is `525ffcdf6d2729f22d8f9b09944d0d47d76335df8407f8336fef8ae6bde52356`: lockfile v3 root `@mkbabb/keyframes.js@6.0.0`, exact Value `4.0.0`, registry tarball `https://registry.npmjs.org/@mkbabb/value.js/-/value.js-4.0.0.tgz`, and Value's published integrity. Audit of 374 packages finds zero Glass, Value3/non-4 Value, parse-that, `file:`, `link:`, git, non-registry resolution or integrity-less resolved entry; fresh `npm ci` contains one physical nonsymlinked Value4 and zero Glass. | The lock itself is LANDED producer evidence, not a Keyframes release. Keyframes finishes its committed-tree pack, strict packed runtime/type verification, tag/provenance publish and observed registry coordinate before `6.0.0` is SHIPPED. Glass consumes no rehearsal substitution while that artifact is absent; after immutable Keyframes 6 it refreshes only from registry Value4+Keyframes6. V-A114 remains red until the downstream graph fixes the routed mount. | Authoritative Keyframes direct handoff; W17/W33 ordered cut. |

| V-A116 | Keyframes' producer-only source, lock, types/build/tests, dependency graph, release-surface audit and newly packed strict-consumer checks are green against registry Value4. The clean-break commit is observed as `5a9183a7` with tree prefix `da710760`; its prior 184,382-byte archive remains evidence only. GitHub/npm publication is not yet observed because the current shell cannot reach the network. | Keyframes alone owns the final networked push/tag/provenance publication and must return the full commit/tag/peeled commit/registry tarball/integrity/shasum plus packed declaration/runtime map. No Glass consumption, rehearsal promotion or W33 SHIPPED row occurs from the commit or local pack alone. V-A114's stale Glass5 boot graph remains separately red until Glass consumes immutable Keyframes6 and rebuilds. | Keyframes producer handoff; W17/W33 ordered cut. |
| V-A117 | The Keyframes producer boundary is now immutable: registry `@mkbabb/keyframes.js@6.0.0` exists with provenance, exact Value4 runtime dependency and no Glass/peer/optional edge. The earlier local-commit and npm-page-not-found states are historical transport evidence, not current release status. | Credit Keyframes6 as SHIPPED only at the observed registry coordinates below. **Single-source Keyframes byte authority (L2, fixed W40-S5): 184,430 is the SOLE current registry figure; the 184,710 / 184,708 / 184,684 archives (held-defective) and 184,254 (strict replacement, pre-publish) are superseded lineage — referenced in this ADDENDA only, never restated as current (D2/D41 now point here).** **Superseded by V-A123 for stage wording:** Glass already owns a proven registry-only graph with one physical nonsymlinked Value4/Keyframes6 chain; it now closes the remaining declaration/HeaderRibbon/Q003-native rows and publishes a fresh Glass7 artifact. The old 966,350-byte Glass rehearsal remains HELD/DEFECTIVE; no file/tarball lock, alias, nested legacy package or fallback is admissible. | Keyframes run `29499708034`; gitHead `5a9183a7afe24702081a7b87c8adc7286ddce9a0`; tree `da7107600d237a49f6daa2064366c2b8ffe91cc0`; tag object `26190755ce1e57c54cb14ef0a454ae02ed2b3da0`; tarball `https://registry.npmjs.org/@mkbabb/keyframes.js/-/keyframes.js-6.0.0.tgz`; 184,430 bytes; SHA-256 `77d8860054f00700de9bc9aeca62dfec91f1422cbf839df62f40135972df0eb8`; raw SHA-512 `9a96f7812c54f1480ee070411b685ee8214d0aaeed5be93d89df360e002379e0dd7809ad133999dbf92e2b78f901b5194542dc37540d90d272721364e3d6d3e1`; integrity `sha512-mpb3gSxU8UgO4HBBG2he6CFNCq7tW+k9id82DgAjeeDdeAmtEzmZ2/kuK3j5AbUZRULcN1QNkNJychNk49bT4Q==`; shasum `5cc5fdecb886f4df33371d3358239594a8965b6b`; W17/W33. |
| V-A118 | A fresh in-app Browser visit to the npm package page still renders `not found` for `@mkbabb/keyframes.js@6.0.0`, while the networked provenance handoff reports the immutable publish and supplies registry tarball/integrity coordinates. The browser UI and the release record therefore disagree at this observation boundary. | Do not reverse or duplicate the immutable release on the stale UI signal, and do not use the npm page as a release oracle. Glass's registry-only install, exact tarball hash/integrity and subsequent packed consumer evidence are the decisive checks; recheck the UI only as an observability follow-up. No fallback, republish, tag move or alternate package path is authorized. | In-app Browser 2026-07-16; Keyframes provenance run `29499708034`; Glass W17 consumer lane. |

No addendum above authorizes compatibility code, a masked fallback, another persistence writer, a visual golden refresh in W33, or process-heavy substitutes for direct implementation and inspection. V-A10–V-A128 amend the existing owners and W2/W3/W8/W9→W7-bootstrap→W17→W33 plus the active post-6 Glass rail; they add no meta-tranche, gate or wave.
