> ARCHIVED 2026-07-17 — historical; superseded by V-PRIME.md §1 L-evidence; the 2/2 convergence clock is NOT a closing condition in V′ (RF-12). Read-only.

# Tranche V — Design Convergence

**V-A168 formation clock:** `0/2` clean passes. Value4 and Keyframes6 are immutable; Glass7/Q003/native/UI/API/π rows remain open. The acyclic rail is W17a→W33a→W17b→W18–W32→W33b, and Q003 remains ACTIVE RED until product-green native cycles.

## Pass 0: orthogonal portfolio

Independent routes were kept separate long enough to expose real disagreement:

- visual: Optical Bench, Pigment Cabinet, Living Chromatic Field;
- palette: mutable aggregate, immutable releases/workspaces, event stream;
- library: functional data kernel, disciplined final objects, injected algebra;
- performance: CSS-first enhancement, eager GPU, worker-owned scene.

No early auditor received a favored close narrative.

## Pass 1: research and synthesis

| Family | Best route | Pass-1 convergence | Exact open work consumed by V.R |
|---|---|---:|---|
| Visual | Optical Bench | 82% | BI veil/header/action seams; proportion atlas; real-GPU Blob/field read |
| Palette | immutable releases/workspaces | 92% | catalog cost; two-device conflict prototype; explicit private-save policy |
| Library | disciplined final objects | 91% | generic channel prototype; hot-path comparison; packed sibling migration |
| Performance | CSS-first enhancement | 84% | Aurora material seam; rebuilt Blob/dialog delta; Safari zoom/keyboard reads |

Root adjudication resolved the product choices rather than leaving forks:

- private remote workspaces save explicitly;
- conflicts offer load remote / duplicate local / discard local, never merge;
- Optical Bench owns global IA while Pigment Cabinet contributes palette object/status semantics only;
- immutable releases plus mutable workspaces are selected; the transactional mutable aggregate is retired because it collapses those two kinds of truth, and a latency result may optimize but cannot reopen that ontology;
- CSS is the permanent canonical material and WebGL is additive motion, not a fallback path;
- final objects plus keyframes-owned `InterpSlot` are selected; the functional data kernel is retired because its parallel representation/mapping layer restores the concepts this cut removes.

## Pass 2: adversarial critique

Four independent non-author critics began at 74% visual, 58% palette, 68% architecture and 63% performance/accessibility convergence. Their counterexamples changed the tranche rather than being filed as implementation detail:

- Optical Bench gained six related composition families with eleven separately evidenced members, direct BI P122 `InstrumentChassis`, one semantic axis, N-ary Mix, distinct owner/admin and Aurora/Blob waves, interrupted-motion evidence and orthogonal rather than Cartesian π coverage.
- Palette gained an executable route/command/CAS table, two-clock Publish, immutable manifest/field authority, separate lineage/release policy clocks, permanent-effect receipts, secure legacy disposition, purge/tombstone rules, exact social targets, rank-epoch expiry, durable outbox, full facility BUILD/RETIRE census, shared abuse controls and one post-ontology data cut.
- Final objects gained the closed `SpaceId→ChannelsBySpace` representation, total static conversion dispatch, immutable CSS values versus keyframes-owned mutable `InterpSlot`, exhaustive semantic consumer migration and a dependency-cycle-free staged cut.
- CSS-first became a permanent canonical substrate with an explicit renderer lifecycle/status/pause contract; route/dialog/font ownership moved before measurement; AT pairings, shell scroll ownership, effective hit regions, lab sampling and bounded GPU lifecycle budgets became executable.

The agglomerator found and closed the remaining branches: registry installs belong to W33 while W17 uses local tarballs; account recovery and five exports have UI owners; immutable releases are never mutated by moderation; permanent receipt effects survive response expiry; Publish creates initial release policy; and pedagogical snippets are authored/typechecked, never generated from private source.

| Family | Post-repair convergence | Disposition |
|---|---:|---|
| Optical Bench | 98% | **ADVANCE** to fresh audits |
| immutable releases/workspaces | 98% | **ADVANCE** to fresh audits |
| final objects + `InterpSlot` | 98% | **ADVANCE** to fresh audits |
| CSS-first performance/accessibility | 99% | **ADVANCE** to fresh audits |

Overall pass-two convergence is **98%**. The remaining two points are not an unnamed product gap: they are the mandated independent clean topology/ledger/recap/BI/full-tranche audits.

## Pass 3: tranche audit

After W0–W33 specs, ledgers, and BI inbox are authored, fresh non-author roles audit:

- wave topology/file ownership;
- U-F1…F77 and chronic dispositions;
- K/N/T/recent prompt coverage;
- BI producer/consumer ownership;
- full plan coherence, then a second clean confirmation.

100% convergence is recorded only after two consecutive full-tranche non-author passes enumerate zero open gaps. Any substantive change resets the clean-pass clock.

The first full-tranche adversary repeatedly refused a clean verdict. Its first pass exposed an incomplete first-party consumer boundary, palette scope left to W1, a missing concrete prepaint primitive, absent navigation/RTL-axis law and an impossible blanket zero-allocation claim. Its second pass then found two configuration-only consumers, grouped rather than exact target routes, circular initial admin authority and an unspecified color-name uniqueness lifecycle. Its third found that a last-holder pre-check could not serialize two root holders removing one another through different documents. Those mechanisms are now folded into `CONSUMER-CUT.md`, the exact 50-route target surface, one-time bootstrap tombstone plus shared AuthorityGuard epoch, ColorNameSlot, the frame-zero latch, navigation/axis tables and split allocation workloads. Role 30's final recheck was CLEAN. Root then tightened permanent public/tag/report namespace cases before the counted passes.

Role 31's first fresh pass was also NOT CLEAN. It found a broken W4→W17 pin order, contextual colors admitted by `CSS.supports`, six evidence pairs laundering eleven distinct workbench compositions, separate live/tombstone namespace collections, four retired routes without physical/UI deletion owners, an unenumerated bootstrap grant, glass/keyframes configuration/script consumers, stale Atlas/sci-report/keyframes baselines, and `domains/**` versus `modules/**` API topology. The repaired tranche preserved the pin through the semantic producer cut, used strict numeric SeedTokens, ultimately required eighteen composition pairs after the later operational-surface repair, serialized public/slug spellings in one permanent NamespaceClaim collection, gave every retired route W10/W16/W24 owners, froze eleven bootstrap scopes, included scripts/configuration and retained `api/src/modules/**`. The final pre-freeze re-census supersedes those transient repository facts: keyframes is now 5.3.5 at `a59d3a22da080a8ed224e8d675112bb3bb0135b0`, and the current dirty-state matrix is recorded in `CONSUMER-CUT.md`. The consecutive-clean clock remains **0/2** until two fresh roles inspect the exact unchanged tree.

Role 31's recheck again refused clean: scheme resolution had not actually applied the `.dark`/`colorScheme` material owner before paint; invalid default simultaneously prohibited fallback and was required to yield nonempty atoms; and the fork route created a new lineage without freezing its slug/NamespaceClaim/Workspace/Policy transaction. Those were repaired as an exact scheme-first document mutation with opposite-OS traces, a disjoint terminal `GroundSeedBoot.invalid-default` arm, and an exact fork `{slug}` transaction creating one unpublished lineage or `409 palette_slug_claimed`. Role 31 then returned CLEAN on the unchanged candidate, briefly establishing pass 1/2.

Role 32 independently refused that candidate with five mechanisms: production `/docs`/OpenAPI and Principal/Namespace surfaces had no in-bounds implementation owner; permanent normalized namespace claims lacked one exact spelling/path law; anonymous registration receipts depended on a Principal that did not yet exist; and fork provenance did not name its uniqueness key. W10 now owns the exact production modules/meta deletion and W16 only conformance; one ASCII canonicalization contract/vectors govern every claim/lookup; receipts use server-derived `registration`, `recovery:<claimId>` and `principal:<principalId>` namespaces with exact retry/conflict precedence; and `fork_edges.targetPaletteId` is the permanent unique origin key. The separately queued Keyframes U covenant was then folded without a new process: timing/slot/layout/descriptor/quart-quint rows ship on W2→W7/W8/W9→W17→W33, diagnostics stays ordinary, D-GAP-5 is superseded and D-GAP-6 is declined upstream. These substantive changes reset the consecutive-clean clock to **0/2**. Roles 31 and 32 now recheck one frozen tree.

A later frontend formation challenge found that the workbench-only pair set still left seven operational compositions implicit, Admin retained an unrelated Palettes companion, the P122 dial could create a second main, P092's global mobile consumer still depended on local descendant CSS, and P051 omitted Gradient stops. `OPTICAL-BENCH-COMPOSITIONS.md` now decides eighteen desktop/mobile compositions; W18/EVIDENCE measure each separately; W22/W23/W24 own storage recovery, Account and full-width Admin; and W17/W19/W21/W25/W27/W30 plus the BI inbox close the P122/P092/P051 seams. The Lab crop also now has exact PR-01…03 type/gap/painted-mass relations. This was a substantive tree change, so it contributes no clean pass and the clock remains **0/2**.

The final palette/edge challenge then rejected grouped DTOs, replay-by-current-state, unbounded authority aftermath, an under-specified legacy seed and the normalized Cloudflare ingress. The repaired tree now has one closed wire contract; permanent replay bytes and receipt-aware secret roles; bounded eleven-bit authority with distinct self/cross-target races; O(1) close plus leased aftermath; five exact immutable export formats; per-hop provenance privacy; an exact deterministic nine-collection map-or-reject transformer with private row artifacts; and three proof-safe database abuse classes with no attacker-spendable identity lockout. Cloudflare's HTTP rail is terminally retired in favor of non-proxying DNS → dual-stack L4 TCP/PROXY-v2 → redundant private NGINX+njs/QuickJS raw-buffer edges → mTLS plus header-bound signed private Hono. The namespace and dynamic Problem laws remain intact. Because these are substantive repairs, the clean clock remains **0/2** until two fresh roles inspect the exact frozen result.

The freeze challenge further split three prose-shaped facilities into exact authorities: route-specific cursor query/scan/envelope bytes, command-binding/permanent-replay bytes with key rewrap, and a production-identical real-HTTPS local rail. It also rejected the claim that a tab lease plus origin deadline can control browser ordering after a same-name cookie response is emitted. That claim is terminally retired; W15/W16/W23/W33 retain exact origin settlement, stale-token rejection and visible Account recovery. These repairs are substantive, so the counted audit clock remains **0/2** until the frozen tree receives two new clean verdicts.

A final live-constellation check found Glass `v6.0.0`/master/origin/npm 6.0.0 peeled to `e5b3a2095b6c3e330b5d82ca3330f1eac4e3c895` (annotated tag object `b262e2d30976d0330014f359803e0c2c65b9b691`) while the exact P019/P122/P047/P051 V producer breaks remain absent from that published artifact and the primary carries active post-BI work. Keyframes tracked HEAD/tag/npm advanced to 5.3.5 at `a59d3a22da080a8ed224e8d675112bb3bb0135b0` and had deleted the four proof scripts at `70b32501`; its working tree later acquired five protected modifications, including package/lock at a prospective `6.0.0` while the Value dependency remains `^3.1.0`. V therefore preserves Glass v6, protects both moving producer trees, and keeps the terminal rail Value 4.0.0 → evidence-derived `K_NEXT` → evidence-derived `G_NEXT` without treating a dirty version label or tranche epoch as implementation, public-break or artifact evidence. These are substantive baseline repairs, so the clock remains **0/2**.

## Superseded exact-tree convergence record

The first four late material addenda repaired the stale Glass P122 disposition/source contradiction, W33 deployment drift after W32, the orphaning/write-skew content collector, and the collision/Save/purge-completion precision defects. `ADDENDA.md` records them; their normative repairs live in the original owners. The palette correction reduced machinery: one ContentObject digest guard with exact Workspace/Release/Report counters and synchronous zero-delete replaces the captured-digest queue/scan and removes the purge content phase. At that freeze, live Glass source/test/inbox agreed on six P122 spacing properties plus a separate title gap, with Browser-first native coordinates pending.

Historical candidate `8d6b83fceb2406573171f457f8caca26c78f8b92c14699c28ff9831d736e3d0d` was the sorted per-file SHA-256 stream rehash of the 65 canonical files under `docs/tranches/V`, excluding only historical `design/**`. No byte changed between its two counted passes:

| Counted pass | Independent charters | Verdict |
|---|---|---|
| 1 | UI/Glass/proportion; palette/domain/concurrency; architecture/consumer/DAG | CLEAN / CLEAN / CLEAN |
| 2 | release-through-visual cross-check; end-to-end palette/product cross-check; full-thesis prompt/disposition cross-check | CLEAN / CLEAN / CLEAN |

That historical tree earned **2/2**, but substantive V-A5–V-A8 invalidated the record. It grants no credit to the amended tree.

## Superseded V-A5–V-A8 exact-tree convergence

Candidate `ea700fd9f2682df6ef12ae04034c57523797e060485f5140a543e38ef5218247` is the sorted per-file SHA-256 stream rehash of the same 65 canonical files after V-A5–V-A8, the exact active-session relay, P122's terminal source/test correction and the live-owner repoint. No byte changed between its two counted passes:

| Counted pass | Independent charters | Verdict |
|---|---|---|
| 1 | UI/Glass/proportion; palette/domain/concurrency; architecture/consumer/DAG | CLEAN / CLEAN / CLEAN |
| 2 | release-through-visual; end-to-end palette/product; full-thesis prompt/disposition | CLEAN / CLEAN / CLEAN |

Two consecutive clean full-tree passes enumerated zero open formation gap for that historical tree. Substantive V-A9–V-A65 invalidate its credit.

## Current V-A9–V-A85 amended candidate

The amended tree retains the same 65-file canonical scope and 34-wave topology. V-A9–V-A53 record the artifact, consumer, UI, palette, declaration, evidence and disk-truth repairs described in `ADDENDA.md`. A fresh three-lens pass then inspected unchanged candidate `cb45b265a207fe30c7e4c7d5759e1f7c63709722be6165f0f103ebaa730b4131`; every auditor reproduced that hash before and after, and every lens returned NOT CLEAN. The visual lens produced V-A54–V-A57: undecided palette-Card material/density, observational type hierarchy/About measure, AT-only selected-state escape and missing 320/400%-zoom Lab proportionality. The palette lens produced V-A58–V-A61: an unpersistable failure outcome, a masked purge-job repair, crash/volume-undefined rank invalidation and numbers wider than JSON cursor identity. The architecture lens produced V-A62–V-A65: dirty prospective Keyframes 6 mislabeled as clean 5.3.5, an unrepresentable named-range selector, three omitted live easing consumers and W3's final-artifact dependency cycle. No Pass-1 verdict counts; the twelve normative corrections changed the tree and reset the candidate.

V-A54–V-A65 select one quiet palette Card tuple and closed typography/selector/Lab obligations; reduce audit and purge to committed fail-closed paths; give catalog rebuilding one coalescing leased revision authority and JSON-safe coordinates; distinguish Keyframes tracked/tag/npm 5.3.5 from its protected dirty prospective 6.0 files; complete `/css` at 19 runtime/33 types/ten source parsers and `/easing` at 16 runtime/five types/nine named functions; and keep W3 coordination-only, W17 rehearsal-only and W33 the sole final-coordinate owner. The superseding C1 surface and owner boundary were sent directly to active task `019f6610-6022-7df1-95b3-472b17a64656`. That task's delegated source work is external execution, not formation convergence or artifact evidence.

The first post-V-A65 unchanged-tree audit inspected candidate `7af99c5934a3e8397e52836508e304f76c1a6ca922d0271a3218e644b95a0dd5` through three independent cross-composed lenses. All three reproduced the same hash and all three returned NOT CLEAN. Release/visual found the stale W3 rehearsal title and the later active Glass executor independently reproduced W17's final-P047 ownership inversion. Architecture/release found a historical Value-3 disk census still phrased as volatile current truth after delegated C1 began changing the source tree. Palette/product found the five-versus-six aftermath contradiction, orphaned `aria-selected` on generic Card selection and an underdetermined AuditEvent construction protocol. V-A66–V-A71 repair those mechanisms without adding a wave or process. The same active executor then exposed the already-specified but still-live W20 interactive-Blob/duplicate-Copy contradiction; V-A72 records its exact deletion/evidence owner. None of these findings earns a clean-pass credit.

V-A66–V-A68 keep W3 decision-only, make `PROGRESS.md` the sole moving execution ledger and split P047 evidence without circularity: W17 freezes producer baseline `b0`, while W29 alone owns the post-radius painted-component target. V-A69–V-A71 make the aftermath tuple exactly five keys, give Browse/Library selection one native pressed-button law and define every committed AuditEvent projection/cardinality/correlation byte. V-A72 deletes the Picker Blob's Tooltip, press semantics, activation listeners and duplicate Copy, retaining ActionToolbar as the sole action. Active C1, W20 and W29 source deltas remain uncredited partial execution.

The Browser-first recheck then reproduced an additional implementation-boundary contradiction before the next freeze: the active package map had deleted `/parsing` and `/units`, but 26 old reaches remained across sixteen value-demo source files plus `tsconfig.demo.json`, so the routed app rendered only Vite's export error. V-A73 does not invent a compatibility phase; it restores W7's existing rule that value's internal consumers belong to the atomic C1 and that W17 starts with siblings. After source reaches fell to zero, the next Browser reload exposed the stronger V-A74 counterexample: zero overlay and zero logged warning/error could coexist with an empty mounted root and atmosphere-only frame. The exact chain is App→Dock→pinned Glass 5/Keyframes 5→nested Value 1.2's missing root export, aborting ESM before Vue mount. V-A74 binds the result to the actual nonempty routed Picker, sole main landmark and named core controls; V-A75 gives W7 one exact-seven bootstrap transport and W17 the ordered Keyframes→Glass migration, pin deletion, rehearsal recreation and routed witness. No compatibility entry or second graph is introduced.

The first consumers of the held bootstrap then falsified two advertised `/css` behaviors. V-A76 makes standard `from`/`to` typed 0%/100% selectors with canonical serialization/reparse; V-A79 closes the single nesting/quote-aware arithmetic and custom-function grammar for `calc`, `if(supports(...))`, sibling index/count, dashed calls and typed/defaulted `@function` forms. Both slices are locally green and add no public export, but the old bootstrap remains deliberately held until Keyframes asks for exactly one replacement. V-A77 removes the final ambiguous HTML-inert phrase. V-A78 separately makes downstream semver a packed public-surface verdict against Keyframes 5.3.5 and Glass 6.0.0; only Value 4.0.0 is predetermined, and Keyframes' current break list is provisional until the final runtime/declaration diff.

P047's raw-buffer lane also falsified the provisional W29 calibration without widening the product wave. V-A80 assigns the existing owner the sole `bodyRadius 0.33→0.325` correction. Focused tests/lint are green and fixed buffers measure ratio `0.660192…0.660415`, centroid error `≤1.342%`, hull inset `12.0%`, bounded DPR1/2 AA edges and equal WebGPU/WebGL2 alpha area. This is implemented source/numeric evidence, not a completed wave: W17's immutable before coordinate and the routed 0px chassis/seat/footprint witness remain RED.

The first fresh pass then found four final formation defects. V-A81 was a declared-artifact-on-disk failure: the corrected bootstrap tarball and hashes existed, but the required durable manifest did not. The smallest honest `audit/bootstrap/w7/value4-exact-seven/manifest.json` now records exactly the supplied transport facts, seven no-root entries and `BOOTSTRAP ONLY` without guessed release/declaration hashes. V-A82 found “inert” still used as shorthand for static WatercolorDot faces; normative prose now says present noninteractive named list/text content with zero activation/focus/drag semantics and explicitly excludes HTML `[inert]`. V-A83 found the catalog cursor's broad immutable-snapshot wording contradicted live eligibility/access/current-policy/current-tag rechecks; only rank/order coordinates are immutable, while unchanged eligible rows retain no-repeat/no-skip continuation. V-A84 then found the handed bootstrap rejects valid leading/inter-rule CSS comments and zero-argument `animation-timeline: view()` through the canonical declaration path; that pack is held/defective pending one producer grammar repair and fresh replacement. The aggregate-count discrepancy is explained as pre-bootstrap `2,319/2,319` versus corrected pack `2,320/2,320` after the complete selector/grammar vector set. These amendments reset the clock, after which two further unchanged-tree passes returned CLEAN across all three lenses.

Those passes are historical only. V-A85 subsequently found that `collectAnimationOptions` ignored structural animation shorthand and forced Keyframes to become a second semantic owner; it also exposed the singular collector's silent loss of comma-separated animations. The producer now owns structural shorthand/list expansion, per-subproperty importance/source-order/reset cascade and a frozen row-list result, while W17 deletes the temporary Keyframes classifier. Both 36,378-byte and 36,447-byte packs are held/defective. This substantive amendment resets the current clock to **0/2**; no completed Value-4 cut, clean rehearsal tarball, tag, integrity, `gitHead`, exports hash or registry publication is credited.

Execution implemented A85. V-A93 holds the 184,710-byte Keyframes archive for strict TS2420 and authorizes the independently rehashed 184,254-byte replacement. V-A88 rejects Glass's first archive; V-A89 corrected placement; V-A90 proved action-only collapse; V-A91 rejected eventual-only correction and vacuous verification; V-A92 rejects VNode presence as an interaction oracle. Glass now consumes only the replacement; source implements a discriminated persistent/collapsible mode with a Glass-owned disclosure button, while fresh pack/native evidence remains open. Native Q003 found the cue outside its actual aperture (V-A94) and capture-only black compositor slabs after WebGPU drag (V-A95); strict consumption exposed broken Reka/VueUse chains (V-A96). V-A97/V-A98 bind registry-only Value4→Keyframes6→Glass7 publication, remove hidden Glass test reach and preserve one provenance candidate. V-A99–V-A106 restore direct behavior and packed strict type consumption for every exact entry, repair CSS/quantize/contrast defects, and make pack JSON, gitHead, export-map/name checks and ESM consumption exact. V-A107 physically partitions hidden demo tests from the producer index without an exclusion. Its isolated exported candidate is green through fresh `npm ci`, prepare/build, lint, strict types, 21 files/250 tests and exact packed consumption; the real index and immutable coordinates remain open. V-A108 then marks V-A95 `PENDING-HEAL`: one-sampler/no-nesting native capture falsified the remaining product hypotheses and a matching Chromium main saveLayer/bounds fix requires recheck in a rolled Browser binary, without a Glass workaround. The same existing owners close these mechanisms without a new wave. Clock **0/2** after V-A108.

### Current V-A109–V-A168 release and consumer boundary

V-A109 records the observed immutable Value4 producer: `@mkbabb/value.js@4.0.0`, gitHead `44ddaff7a22283a4f7a42608893eeae7bc234424`, provenance run `29497728532`, and exact registry integrity/shasum. V-A110's local DNS failure is historical and is superseded by V-A115's independently byte-identical registry-only Keyframes lock (`525ffcdf…`), clean `npm ci`, and zero legacy/file/link/git edges. V-A116 records the clean Keyframes producer tree; V-A117 records immutable Keyframes6 at gitHead `5a9183a7afe24702081a7b87c8adc7286ddce9a0`, tree `da710760…`, 184,430-byte tarball, provenance run `29499708034`, exact Value4 dependency and zero formatter/runtime residue. V-A118 records the stale npm UI `not found` observation as an observability discrepancy only; it does not reverse provenance-backed registry truth.

The current Glass lane has consumed only registry Value4 + Keyframes6, with one physical deduped chain and no legacy/file/link/git edges. **V-A134 supersedes the earlier 1,131-test/771-module report:** the direct current source run is 176 files/1,132 tests with typecheck green, while build/pack/verifier evidence is unavailable in the current sandbox. V-A120 adds two live product rows: composed screen-space Blob visibility/occupation of the headline→rail interval, and explicit Dock home/eye/ellipsis affordance/state decisions. V-A122 adds a Glass-plan correction: Q023's “drawer + sheet” engage shorthand must not reintroduce Drawer blur-radius animation; the fixed-radius Drawer law and real second-consumer floor govern. V-A123 corrects the stage language and promotes V-A120 into owning completion/DELTA contracts. **V-A124 records that native Q003 compositor plates still corrupt beside WebGPU after nested filtered regions were reduced approximately 95→5; V-A125 records that the reverted 3/0 inert backing-plane experiment also failed, restoring the honest 5/0 baseline and withdrawing Chromium-only attribution. V-A126 closes the stale U-F77/B1/B14 producer-order wording, V-A127 closes the residual Consumer-Cut/W2 future producer wording, V-A128 consolidates the singular Glass execution brief, V-A129 records stale Value3 names in the tracked demo canon, V-A130 corrects current-stage labels plus W8 historical-plan marking, V-A131 records the dirty current Glass source/lock and stale BI coordination rows, V-A132 records missing π artifacts, V-A133 records the legacy API/client canon, V-A134 records the uncreditable current pack/build/native boundary, V-A135 records missing `/about` and `/easing` surfaces, and V-A161–V-A164 add bounded color search, strict Problem decoding, Principal-derived proposal attribution and raw lineage privacy. V-A165 removes the W17/W33 publication cycle through W17a→W33a→W17b, V-A166 puts W18 after W6, V-A167 requires an immutable pre-`.325` baseline, and V-A168 makes Q003 product-green—not merely hypothesis-declined—before fresh bytes.** Value4 and Keyframes6 are immutable. Glass alone must close strict packed declarations/HeaderRibbon, remove or annotate stale canon, return zero-slab/zero-occlusion/zero-lost-target native Q003 evidence from honest 5/0, produce fresh Glass7, and then refresh the routed consumer. Strict declarations, HeaderRibbon, stale canon, π artifacts, API migration, route surfaces, color search, error, provenance, Q003 native evidence, fresh Glass7 packing/publication and the routed value.js nonempty mount remain open. The held Glass rehearsal and all prior bootstrap/rehearsal bytes remain non-release evidence; V-A121–V-A168 stage overrides mark older producer-rebuild and product-level-decline prose historical. These are substantive current obligations, so the convergence clock remains **0/2**; no completion claim is admitted.

| Historical pre-A85 final pass | Lenses | Verdict |
|---|---|---|
| 1 | palette/API; UI/Glass; architecture/release | CLEAN / CLEAN / CLEAN |
| 2 | palette/API; UI/Glass; architecture/release | CLEAN / CLEAN / CLEAN |

### Frozen-tree identity

The current candidate hash is deliberately **not embedded in any canonical file**: embedding it would change the bytes it purports to identify. At audit freeze, root observes the hash out of band from the repository root and reports that observed value in the final handoff. The canonical scope is every `*.md` below `docs/tranches/V`, excluding `docs/tranches/V/design/**`. The exact command is:

```sh
find docs/tranches/V -type f -name '*.md' ! -path 'docs/tranches/V/design/*' -print0 \
  | LC_ALL=C sort -z \
  | xargs -0 shasum -a 256 \
  | shasum -a 256
```

Discovery and path transport are NUL-delimited; paths are ordered bytewise under `LC_ALL=C`. Each inner record is the `shasum` textual frame `lowercase-hex`, two U+0020 spaces, the repository-relative path, then LF. The outer SHA-256 consumes that exact ordered textual stream. The final command prints the observed outer digest followed by `  -`; only its first field is the candidate identity. Two counted passes must inspect that same out-of-band digest, and any intervening byte change resets the clock.

## Banked/retired route registry

`DECISIONS.md §1` is the terminal authority. A banked route has one named empirical re-trigger; it is not V work. Retired routes may not return under a new synonym.
