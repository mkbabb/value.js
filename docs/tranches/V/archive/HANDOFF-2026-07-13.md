> ARCHIVED 2026-07-17 — historical; superseded by V-PRIME.md + SUBTRACTION.md. Read-only.

# THE HANDOFF OF RECORD — 2026-07-13 (read this FIRST on resume)

> **HISTORICAL / SUPERSEDED BY V-A130:** this packet is preserved for audit lineage. Do not resume its paused U/V campaign instructions or owner-packet recipes; the live V boundary is the Glass-only W17/W33 close.

**One paragraph**: Tranche **U is TERMINAL** — all ten waves decided, `FINAL.md` walks 77/77
families zero-drop (`proof:close-ledger` GREEN, CI-wired), and the owner packet P1..P8 is
assembled, presented, un-ruled. The **V structure campaign is PAUSED by owner order** after four
convergent passes: the composed thesis stands at **78%** (never-inflate floor of two independent
audit lenses), all six charters ADVANCED, the two-consecutive-clean-passes clock at **0 of 2**,
and pass-5 charters (η/θ/ι) are authored but NOT launched. Crons killed; no live runs; `:9000`
left serving; the glass-ui `v5*` tag NEVER fired (probed every round). This file synthesizes
everything; every claim cites its artifact.

---

## §1 The U stream (terminal — awaiting only the owner)

**State**: branch `tranche-u`, ten waves closed (`EXECUTION-STATE.md §2/§4` — the wall-recovery
manifest; `PROGRESS.md` — the slate; `FINAL.md` — the ledger walk; `audit/w-close-close-artefacts.md`
— the five close gates + misses M1..M6 + the packet inventory).

**The owner packet (P1..P8, verdict fields EMPTY — never proxied)**:

| # | Decision | File |
|---|---|---|
| P1 | the tranche close of record | `docs/tranches/U/FINAL.md` |
| P2 | **version cut / npm publish** — MAJOR forced, 4.0.0 recommended, fix LANDED + certified | `audit/w-close/publish-presentation.md` + `audit/w-lib/publish-packet.md` |
| P3 | **the owner-attest annex** — 11 visual rows + HG6 stub (real-GPU law; 2 headless false-reds on record) | `audit/w-close/annex-packet.md` |
| P4 | B3 Q14-LCP Pole A/B · B7 CI-teeth posture · B14 publish | `audit/w-close/book-register.md` |
| P5 | relay-close verification (BI live inbox; 2 cut-gated obligations) | `audit/w-close/relay-close.md` |
| P6 | the U-F3 Q14 LCP escalate (LCP ~5141 CI ≈ 2×; residual = producer RP-2 331 KiB, adopt-gated) | `audit/w-perf/u-f3-escalate.md` |
| P7 | the T.W8 HG6 taste verdict (empty stub) | `docs/tranches/T/audit/w8-certification/VERDICT-2026-07-12.md` |
| P8 | the close roll-up | `audit/w-close-close-artefacts.md` |

**Standing triggers** (fire independently of the campaign):
- **`git -C ../glass-ui tag --list 'v5*'` non-empty** → the ADOPT EVENT executes per
  `audit/w-adopt/coland/coland-ordering.md` (7-pin retirement · lock refresh · substrate UNPIN
  [`audit/w-adopt/substrate-pin.md` — pinned @ `2e559f7a`, co-land preview `296b8b2` is EPHEMERAL
  sandbox state discarded at unpin] · the real sibling co-land per `publish-packet.md §3` **+ the
  5-rename undercount correction** in substrate-pin.md §co-land-preview · boot-smoke · the four
  armed G-ADOPT gates flip). The unpin is orchestrator-owned; the version cut stays OWNER-HELD.
- **The next CF-Pages deploy** → discharges M3 (live wire currently serves `nosniff` ONLY; the
  landed `_headers` CSP/HSTS/X-Frame-Options await it) + the U-F36/F38 auth live-wire confirms.
- **NCSU VPN access** → the U-F41/X2 on-wire re-verify (attested-not-verified until then).

## §2 The V campaign synthesis (4 passes → what is now KNOWN)

**The campaign documents**: `DESIGN-CAMPAIGN.md` (charter §0 owner verbatim + registry §3 + pass
log §4) · `design/portfolio.md` (round-zero survey + 6 families) · `design/pass-{1..4}/`
(specs, protos, critiques, audits, AGGLOMERATION.md per pass — each fold supersedes prior framing).

### §2.1 The converged architecture (the composed thesis @ 78%)

ONE shape thesis operated by ONE vehicle under ONE enforcement battery:

- **The WHAT-tree** (glass-ui's codified LAW ∘ the measured placement predicate): flat component
  peers + pure barrels + kind-named own-runtime siblings + the T4 earned-dir rule + colocate-iff-
  single-owner styles + the 500-raw-line ceiling. Placement is computed by the CC-5 consumer-census
  (ground-truth-validated harness, 3/3) under a STATED objective (min-manufactured-runtime-edges —
  a chosen rule, with literal-colocation the named alternative → fork OF-4).
- **The vehicle**: atomic per-surface `git mv` + one-pass import rewrite — **NO transitional alias
  window** (value.js's `@` is a physical dir; glass-ui's alias-then-drop mechanism is explicitly
  not replayed; decision record in pass-3 charter-α §3). Discipline per surface: the ratifiable
  destination MANIFEST (human ratifies BEFORE the codemod) → codemod → `audit SAFE:true` ∧ oracle
  `typecheck Δ0` ∧ build ∧ smoke.
- **The enforcement battery**: `test:dist` culled **10 → 7** (the ratified Q13 five + `proof:barrel-pure`
  [a DERIVED runtime-purity property, never a hardcoded set] + `proof:grammar-fuzz`); the META-gate
  dropped; plus the born-RED complexity gate (`complexity-scan.mjs --gate N`, ratchets down as
  carves land — CI membership is an owner call).
- **The D3 dimension**: typed diagnostics extending the LANDED U-F29 `.eof()`+`CSSParseError` idiom
  (credited at `329932b`); grammar-fail-closed everywhere except the **2** spec-forced drop sites
  (sweep-falsified "single site"); the `#11 PropertyDescriptor→CSSPropertyDescriptor` rename is a
  decided 2.0.0-major cost riding the adopt rail (KF-7, 4 kf sites).

### §2.2 Load-bearing findings by surface

**D1 demo/** — `@` abrogation blast = **377 sites / ~221 file moves** (re-derived at HEAD;
+5% drift per generation is real — re-derive at landing). The **feature-capsule thesis is
REFUTED by measurement**: 77% of shared-bucket files are true kernel; the demo-wide manifest
(`pass-4/charter-epsilon-manifest.md`) rules 64 shared files → **49 KERNEL / 12 APP / 3 FEATURE /
0 ORPHAN**. The hardest bucket (`composables/color/`, 18 files) is ratified **6K/12A/0F — proven
UNIQUE** as the zero-manufactured-edge assignment by counterfactual (ratified=0 vs 1 vs 7), scatter
RUN green end-to-end incl. 6-project smoke 182/1/2 (the 1 failure proven orthogonal: worktree-absent
glass-ui dist stylesheet). `panes/` carve RAN (4-chassis + 10 leaf + dead barrel deleted).
`style.css`: **79.5% legitimately global** (tokens/a11y/cross-family registers) — the carve is
delete-dead + colocate-single-owner, not a dissolution. NG-6: the kernel→app inversion is ONE
type-only edge (R1 hoist recommended / R2 ratify — owner row).

**D3 src/** — 14 god-modules >500 LoC; the metric that goes DOWN is **G500/OVER500/MAXLOC**
(falls for carves, flat for relocations — the honest discriminator; "total complexity" named as
the un-pursued alternative). **3 carves RUN green** (color.ts 754→443 · parsing/index.ts 644→400 ·
decompose.ts 603→406; suite 2326, zero export change); 10 SPEC'd (2 gated on the registry
primitive); style-names DATA-EXEMPT. The **parsing/color→units/color merge is a RELOCATION, not
reduction** (NG-4 ruled by measurement); it composes with the carves but **amplifies capsule→kernel
edges 5→10** — argues carve-first-keep-capsule-under-parsing OR merge+grammar-factory (feeds OF-4/
OF-5). The **order-independent registry** (lazy conversion memos) is the delivered cycle-severance
primitive: color-SCC carve 2 FAIL→0, barrel SCC 14→5. TRUE merge blast = 4 renames + 6 src +
**15 test files + the canon-sync gate** (pass-2's "small blast" was a full-suite-unrun undercount).

**D2 api/** — the exemplar: passes glass-ui's OWN backend gate GREEN (0 violations), 5 vertical
capsules, 0 route→repo edges. Nothing to restructure; only the vocabulary fork (OF-2).

**D4 hygiene** — ratified kill-list in `pass-4/charter-zeta.md`: 39 root PNGs · 14 stale
worktrees (4-clause law applied per row) · 11 benches classified · the scripts prune table ·
docs-linkgraph (0 stale after false-positive correction — see P4-G6 for the doc-vs-instrument
drift to fix) · gate cull 10→7 · **OF-6: BOTH plugins are LIVE-wired in vite.config.ts against
the owner's "delete entirely"** (deferGlassFonts' empirical delta is adopt-blocked — P4-G7).

**Cross-repo** — glass-ui's BH convention is **codified-authoritative but partially executed**
(their demo landed at 5.0.0; their src flatten rides their BI as 5.1.0): value.js executing the
standard on src-class surfaces LEADS the constellation — leverage and hazard both named. Their
`@glass` was an alias-then-drop; ours is a physical cut.

### §2.3 The instruments (durable, re-runnable from the repo root)

`design/pass-4/instruments/`: `complexity-scan.mjs` (G500/OVER500/MAXLOC + `--gate N` born-RED) ·
`domain-edges.mjs` (XDOM) · `placement-census.mjs` (demo-wide CC-5; carries the α-reproduction
harness, exit 1 on drift) · `alias-census.mjs` · `panes-carve.mjs` · `style-census.mjs` ·
plus pass-3's `census-postmove.mjs` (+ harness, `design/pass-3/charter-alpha-harness.md`).
**Both pass-4 auditors re-ran all nine and every number reproduced.** The known hole (pass-5 η):
the **--apply codemods are NOT committed** — the carve/scatter RUN results are attested, their
executors died with the worktrees.

### §2.4 What keeps 78 from 100 (the pass-5 charters, authored NOT launched)

`design/pass-4/AGGLOMERATION.md` (`48eb4ff`):
- **η — execution reproducibility**: commit the `--apply` codemod set (carve + scatter + merge);
  back-fill β's registry cure as a committed patch; re-run from committed form.
- **θ — scope honesty + fork closure**: correct the `@src DEAD` mislabel (**live repo-wide at 235
  sites** — 203 test/ + 32 assets/docs + a vite alias); shrink the fork pile (every closable fork
  closed by measurement; only genuine owner calls survive); fix the P4-G6 doc-vs-instrument drift.
- **ι — the clause-9 PERFORMANCE limb**: "elegance, simplicity, and performance above all" — no
  charter in four passes improves a perf number; chart it (candidates: the carve set's effect on
  the eager chunk graph; the barrel purity → tree-shake deltas; measure, don't assert).
- Then: BOTH audits clean + zero surviving gaps = clean pass 1 of 2; pass 6 = pure confirmation.

## §3 THE OWNER DECISION PILE (consolidated — nothing here is decided)

| # | Decision | Where |
|---|---|---|
| U-1..8 | the P1..P8 packet (§1: publish 4.0.0 · annex+HG6 · Q14 Pole A/B · CI posture …) | `w-close-close-artefacts.md §3` |
| OF-1 | `@`-ban idiom: eslint `no-restricted-imports` vs `proof:*` never-ESLint (both cannot stand) | pass-2 AGG §4 |
| OF-2 | api vocabulary: conform `api/model/lib` vs ratify `routes/service/repository` | pass-2 AGG §4 |
| OF-3 | diagnostics boundary (NARROW: one spec-forced embed-warn site vs full `{value,diagnostics}` major) | pass-2 AGG §4 |
| OF-4w | placement OBJECTIVE: min-manufactured-edges vs literal colocation (7 divergent rows) | pass-3/4 folds |
| OF-5 | the coupled owner event (U ratify + T HG6 + the landing order) | memory + §1 |
| OF-6 | plugins: delete-and-inline vs delete-accepting-regression vs keep (both LIVE-wired) | `charter-zeta.md` |
| OF-ε1 | `@assets`: keep-alias vs relative vs relocate | `charter-epsilon.md` |
| OF-ε2 | panes dir shape: min-move vs per-pane-dir | `charter-epsilon.md` |
| NG-6 | the one type-only kernel→app edge: R1 hoist (recommended) vs R2 ratify | `charter-epsilon.md` |
| δ-1..3 | the 500-LoC ceiling value · the data-table exemption · complexity-gate CI membership | `charter-delta.md` |

## §4 Resume recipes

- **V pass 5**: read `DESIGN-CAMPAIGN.md` + `design/pass-4/AGGLOMERATION.md`, launch η/θ/ι on the
  pass-4 workflow pattern (3 charter lanes worktree-isolated → 2 fresh auditors → fable fold;
  batches ≤3; the durable-instrument law binds; REAL `${}` interpolation in workflow scripts — the
  `$`-escape passes literal text and misroutes fan-outs).
- **U adopt event**: on the v5 tag — §1 standing trigger, `coland-ordering.md` governs.
- **Fresh-session recovery**: `EXECUTION-STATE.md §1` (death classes) + §3 (paths); the memory
  topic file's ▶▶▶▶▶ block mirrors this handoff.
- **Evidence residue**: 17 campaign worktrees under `.claude/worktrees/wf_*` hold uncommitted
  prototype state (instruments already committed; RUN results documented). Safe to prune at the
  landing wave per ζ's worktree table — never blind-committed, never load-bearing.
- **⚠ UNRESOLVED working-tree state (surfaced, not acted on)**: TWO unstaged tracked DELETIONS of
  unknown provenance sit in the main tree — `CLAUDE.md` and `src/parsing/CLAUDE.md` (present since
  at least mid-pass-2; no campaign lane owns them; possibly the owner's own act, consistent with
  the glass-ui "CLAUDE.md delete-after-redistribute" doctrine but NOT executed by this campaign).
  Deliberate → commit them explicitly; accidental → `git restore CLAUDE.md src/parsing/CLAUDE.md`.
  Note the canon-sync gate + the U-era doc surface reference both files — a committed delete needs
  the redistribute half first.

**Session**: `https://claude.ai/code/session_01XskVMTQAWVgvWQvhiYECgb` · orchestrating transcript
`daa7c418-d0bc-4d88-913c-27283e6345eb` (workflow journals under its `subagents/workflows/`).
