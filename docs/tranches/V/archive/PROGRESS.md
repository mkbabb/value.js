> ARCHIVED 2026-07-17 — historical; superseded by V-PRIME.md §2 (the re-formed wave set). Read-only.

# Tranche V — Progress

**Formation:** AMENDED CANDIDATE · `0/2` unchanged-tree clean passes after V-A168; formation remains open because Glass publication/native/UI/API/UI-canon obligations are not papered over.
**Current Glass execution override (V-A136):** the direct current observation is 176 files/1,132 tests with typecheck green; the older Glass counts embedded in the historical execution paragraph below are superseded. Build, pack, package-verifier and native Q003 evidence remain RED/uncredited, the 3/0 backing-plane experiment is reverted, honest 5/0 is current, and no Glass7 artifact exists.
**Execution:** Value `4.0.0` and Keyframes `6.0.0` are the immutable SHIPPED producer rows. Value tag/gitHead `44ddaff7a22283a4f7a42608893eeae7bc234424`, npm integrity `sha512-Z8ywb4htSxJlRFvoU1DNtvzr9Bsuaw9ahT/hvNlKbnRj6fTnLuXjn0itKq1Q5s6rwg24ct0zcLZ04BuR3/SzGw==`, shasum `ccb962e592fb42e6602fc2bb6afbfb1763788b9d`, provenance run `29497728532`. Keyframes gitHead `5a9183a7afe24702081a7b87c8adc7286ddce9a0`, tree `da7107600d237a49f6daa2064366c2b8ffe91cc0`, tag object `26190755ce1e57c54cb14ef0a454ae02ed2b3da0`, tarball bytes `184430`, SHA-256 `77d8860054f00700de9bc9aeca62dfec91f1422cbf839df62f40135972df0eb8`, npm integrity `sha512-mpb3gSxU8UgO4HBBG2he6CFNCq7tW+k9id82DgAjeeDdeAmtEzmZ2/kuK3j5AbUZRULcN1QNkNJychNk49bT4Q==`, shasum `5cc5fdecb886f4df33371d3358239594a8965b6b`, provenance run `29499708034`. V-A115's registry-only lock/npm-ci and V-A116/V-A117 producer evidence are green. Held bootstrap/rehearsal archives remain history, not release credit. The active Glass source/lock is LANDED/IMPLEMENTED but dirty at HEAD `c181f0a78` (lock SHA-256 `e6216e2188ea7d08fff572745be168dbf368df3d30baa10c2caa837bc817581f`), with 176 files, 1,131 tests, a 771-module library build, a 3,572-module production demo build and strict package closure reported green. This is not a packed or immutable Glass7 release: Q003 live-backdrop adjacency remains ACTIVE RED, and no fresh tarball, provenance, registry coordinate or native packet exists. The immutable pair graph is installed/proven. A fresh 2026-07-16 in-app Browser recheck of the canonical Lab URL still returns a complete document with `#app` present but zero children, zero `<main>`, empty body text and no Vite overlay; this re-confirms V-A114's stale Glass5/Keyframes5 pre-mount graph rather than creating a new defect family. The stale lazy About `@src/units` closure is separately red. The one-sampler capture is historical evidence under V-A124, while V-A125 keeps the V-A95 disposition **ACTIVE RED** on the honest 5/0 baseline; V-A124/V-A125 Q003, V-A112 Picker housing/reservation and all unexecuted frontend/palette waves remain RED. Value and Keyframes publication do not close W17, W33 or V.
**V-A134 correction to the execution paragraph above:** its embedded 1,127/769 and later 1,131/771 Glass figures are historical reports, not current evidence. The current direct observation is 176 files/1,132 tests with typecheck green; build, pack and package verification are uncredited in the current sandbox. The held 966,350-byte archive remains stale and no Glass7 coordinate exists.

**V-A168 Q003 correction:** the inert Configurator backing-plane trial reached 3 active/0 nested filters but still failed native capture and is fully reverted. The honest 5/0 baseline is current; Chromium-only attribution is withdrawn. Q003 remains ACTIVE RED until repeated forward/reverse Browser captures show zero slab, occlusion corruption and lost target. A falsified adjacency hypothesis may be declined; the visible product defect may not. No workaround receives credit.

**V-A137–V-A140 frontend correction:** fresh source audit found SpectrumCanvas pointer-only semantics, an unlinked ConsoleRail tablist, About's empty Tooltip/false trigger plus Alert/repeated dividers, and a two-rung display2→display4 relation where W20/P019 requires one adjacent `1/√φ` rung. These rows are now named W18/W20/W21 obligations and remain born RED pending ordinary Browser/π evidence.

**V-A141–V-A154 current audit additions:** PaletteCard/SwatchHoverMenu still use face-as-control and dual hover/touch paths; API route metadata, cursor/query decoding, session recovery, idempotency replay, GET header shape, cross-slug version lineage, strict request/response DTOs, W17/W33 historical scopes, W7 bootstrap prose, and the recent Glass prompt recap all have concrete RED rows in `ADDENDA.md` and `DISPOSITION-LEDGER.md`.

**V-A155–V-A164 current audit additions:** health/OpenAPI projection, public Release access predicates, global pane split state, empty-grid filler, trailing-query construction, strict Problem decoding, bounded color search, Principal-derived proposal attribution, raw fork/provenance privacy and the omitted 2026-07-16 Glass session are explicit RED rows with named owners.

**V-A165–V-A168 execution corrections:** eliminate the W17/W33 release cycle with W17a→W33a→W17b→W18–W32→W33b; make W18 depend on W6; require a hash-identified immutable pre-`.325` baseline; and keep Q003/product release RED whenever any slab, occlusion or lost target remains, even if one causal hypothesis is declined.
**Authority:** `V.md`

**V-A168 current-stage correction:** Glass's registry-only Value4/Keyframes6 graph is already installed and independently proven as one physical deduped chain with zero legacy/file/link/git edges. U-F77/B1/B14 and residual Consumer-Cut/W2 producer-order prose are historical/retired; Value4 and Keyframes6 require no rebuild or rehearsal. The live remainder is Glass-only strict declaration/HeaderRibbon closure, product-green native Q003 evidence from the honest 5/0 baseline, stale Value3 demo-canon cleanup, W17a's fresh rehearsal, W33a's publication, then W17b's value consumer refresh and routed Browser proof. A causal experiment may be declined, but any remaining slab, occlusion corruption or lost target forbids bytes. Keyframes and Drawer make no Q003 change.

**V-A168 direct Glass brief:** From the honest 5-active/0-nested baseline, Glass repeats native in-app Browser forward and reverse captures beside WebGPU after settlement, records live-backdrop adjacency and compositor integrity, removes stale Value3 names from the tracked demo canon, and continues until every settled cycle has zero slab, occlusion corruption and lost target. The same task closes strict declarations/HeaderRibbon and emits one fresh Glass7 rehearsal; W33a publishes only that fresh candidate. Declining a falsified mechanism does not close the product. No Browser-only wait, backing plane, filter-count target, sampler, mask/cover, Keyframes/Drawer change, alias, fallback or held-byte credit is admissible.

The older execution paragraph's one-sampler evidence wording is retained as chronology; the operative disposition is V-A168: the reverted 3/0 experiment earns no credit, the honest 5/0 baseline is current, and Q003 remains ACTIVE RED until the native product is visibly uncorrupted. Only a causal hypothesis, never the product defect, may be declined.

## Meta-tranche slate

| Meta-tranche | State | Waves |
|---|---|---|
| V.R Research | PLANNED | W0–W3 |
| V.H Hardening | ACTIVE (Glass strict/native/Q003/release only; Value4/Keyframes6 immutable) | W4–W17 |
| V.F Frontend Realization | ACTIVE (bounded W20/W29 partials only) | W18–W29 |
| V.A Adversarial Analysis | PLANNED | W30–W33 |

## Wave slate

Thirty-one wave files remain **uncompleted**: 27 are fully PLANNED, W17 is open, W20/W29 have LANDED/IMPLEMENTED slices, and W33 is PRE-RELEASE RED. W8→W9→W7 is SHIPPED as the immutable Value producer boundary; its historical bootstrap transport remains held. W17 still owns the constellation, routed mount and downstream adoption. Value4 and Keyframes6 are both immutable; Glass's dirty 7.0.0 source graph is LANDED/IMPLEMENTED for tests/typecheck only, but fresh Glass7 packing/publication, native Q003 closure and strict packed evidence remain before the routed consumer can refresh. Separate lanes implemented W29's calibration and W20's inert-Blob slice, but V-A112 proves the legacy housing/reservation/readout mechanism remains. W29 still needs W17's baseline and routed 0px geometry witness; W20 still needs the full hierarchy/layout Browser evidence.

| Waves | State | Entry condition |
|---|---|---|
| W0–W3 | PLANNED | V execution opens |
| W4–W6, W10–W16 | PLANNED | named research artifact and dependency waves complete |
| W17 | VALUE4 + KEYFRAMES6 IMMUTABLE · GLASS LANDED/PRE-RELEASE · NATIVE/PUBLISH OPEN | W17a returns product-green strict/native rehearsal; W33a publishes; W17b refreshes value and proves the nonempty routed mount |
| W8→W9→W7 | VALUE4 + KEYFRAMES6 PRODUCERS SHIPPED · GLASS DOWNSTREAM CUT UNCOMPLETED | exact registry pair supersedes all bootstrap transport; Glass7 and routed/native/UI obligations remain independent |
| W18–W19, W21–W28 except W29 | PLANNED | BI consumer cut and visual constitution dependencies complete |
| W20, W29 | LANDED/IMPLEMENTED SLICES · UNCOMPLETED WAVES | W20 inert-Blob and W29 radius-only source/tests are locally green; wave close still requires every dependency and the exact amended native/Browser evidence |
| W30–W32 | PLANNED | the product surfaces they analyze are settled |
| W33 | PRE-RELEASE RED / UNCOMPLETED | W33a publishes the fresh W17a candidate without closing W33; W33b closes after W17b/W30–W32 |

## Formation facts

- The required 32-role minimum expanded to 67 recorded independent roles across prompt archaeology, landed-vs-plan truth, visual/product design, palette semantics, topology, library design, performance, accessibility, constellation consumers, ledgers, fresh adversarial review and Browser-first execution-boundary challenge.
- Recent Claude Code sessions were normalized to substantive user text; workflow starts, watchdogs, compact commands, status prompts, and duplicate restatements were excluded.
- The current browser baseline was inspected at desktop and mobile across Home, Palettes, Browse, Extract, Generate, Gradient, Mix, Atmosphere, and Blob.
- A fresh pre-implementation RED confirmation used the in-app Browser at `http://127.0.0.1:9000/` at 1440 and 390 CSS px: the Lab label→numeric gap is vast and the About companion Card/repeated rules compete with the Picker; the Browser console reported zero runtime errors. This corroborates the existing W18/W20 proportion and subtraction rows only; it is not completion evidence and mints no new wave.
- Published glass-ui BI was reconciled at immutable `v6.0.0`/peeled `e5b3a2095b6c3e330b5d82ca3330f1eac4e3c895` (npm 6.0.0); the active primary now carries post-BI producer work. The packed public diff now proves a Glass 7.0.0 major through eleven removed subpaths and two additions; immutable v6 remains untouched.
- All 52 mounted palette API routes and five exports received formation-time product dispositions; W1 verifies rather than decides them.
- Those dispositions resolve to one exact 50-route HTTP target surface, plus a non-HTTP one-time admin bootstrap, serialized root-authority guard and permanent public/tag/report/name namespace rules.
- Palette hardening now has a closed nine-collection source contract, private-only row artifacts with one redacted tracked manifest, exact JSON/CSS/Tailwind/SVG/PNG byte contracts, current edge truth distinct from labeled RankEpoch catalog values, report-pinned purge evidence, proof-safe abuse windows and an mTLS+header-bound-MAC private origin.
- Cursor query identity/scan coordinates and mutation replay bytes/key rewrap are separate closed authorities; the ordinary local browser rail uses the production edge artifact with real HTTPS and local PKI. The former browser-global no-late-cookie claim is retired: origin settlement remains exact, while a stale post-emission winner must fail authorization and enter explicit Account recovery.
- The no-root 4.0 cut includes nine non-core active first-party repositories in addition to keyframes/glass, with source/configuration callsites, duplicate archives and dirty-state reconciliation explicit.
- **HISTORICAL (V-A123):** The queued Keyframes U covenant is terminal on that same cut: timing/slot/layout/descriptor/quart-quint producers ship in Value 4, the named Keyframes seams retire in `K_NEXT`, D-GAP-5 is superseded, D-GAP-6 is declined upstream, and the clean packed diff now proves `K_NEXT=6.0.0`, while W33 alone records observed coordinates.
- Formation itself edited no product source. Separate active Glass task `019f6610-6022-7df1-95b3-472b17a64656` restored P122's six-property source/test contract; V records that producer result and the still-pending native Browser evidence in `ADDENDA.md`, `coordination/CONSTELLATION.md` and the Glass inbox rather than claiming the edit as formation work.
- V-A9 records the historical P127 `E404`/stale-dist failure antecedent. V-A109 is its terminal Value-only correction: `@mkbabb/value.js@4.0.0` is published with observed tag/gitHead, tarball, integrity, shasum and provenance. Bootstrap/rehearsal bytes remain explicitly non-release, and Keyframes/Glass retain their own RED rows.
- V-A10 corrects the Glass subset to exactly `@mkbabb/value.js/color`, `/css`, and `/easing`; CSS color parsing and timing readback make the prior two-entry claim vacuous. W7/W9/W17/W33 own the cut and packed verification.
- V-A11 records the silent-empty/default/identity fallback seams and the empirical `minContrast * 0.1` lightness proxy as RED. A successful contrast claim requires the actual color result and measured ratio, while parser failures retain named consumer outcomes.
- V-A12 records Aurora's degrees-versus-normalized-turn `interpolateHue` mismatch and requires warm/cool direction, wrap, cap, no-overshoot and zero-strength vectors under W17/W28.
- V-A13–V-A16 close row-specific replay ETags, selected-color meter transcripts, exactly two pastel `Palettes` sites and the frozen M00–M17 × H-D/H-M performance matrix.
- V-A17–V-A21 keep re-deadlined rows RED, include current public docs in the clean-break census, separate exact asset bytes from dynamic numeric tolerances, delete direct-path equivalence and keep BANK terminally outside V.
- V-A22–V-A25 make parser diagnostics, CSS-native color serialization, Fourier easing lookup and release-candidate/hash provenance mechanically single-valued.
- V-A26–V-A31 remove the W31/W32 back-edge, name every measured interaction and boot arm, and propagate the selected-color, two-site identity and P047 rendered obligations through final evidence.
- V-A32–V-A35 retain grant rationale, represent bootstrap attribution without a fabricated Principal, persist runtime policy provenance and let terminal purge history coexist with a new schedulable close episode.
- V-A36 closes the current prompt tail: convergence, material addenda and maximal available fanout are explicit; pure crash/status/reset commands remain operational metadata rather than product scope.
- V-A37 namespaces purge causes by the same receipt identity as their commands; V-A38/V-A39 remove the last stale W17-candidate and four-library-space CSS-spelling claims from the normative corpus.
- V-A40/V-A47 make every W31 desktop/mobile interaction comparable while keeping private fixture IDs out of the accessibility tree; changing one coordinate resets the whole 1,440-load matrix.
- V-A41/V-A48 close Q-AUDIT subject identity and its paired half-open `[from,to)` window across wire, cursor, OpenAPI/client and ordinary goldens.
- V-A42 separates W33's sole publishable candidate from the one optional deleted verifier; V-A43/V-A45 and V-A49–V-A52 close all seven declaration surfaces, the cohesive CSS grammar, declaration precedence, span ownership, selected-list projection and context-preserving stylesheet traversal; V-A63 later completes that surface at 19 runtime/33 types.
- **HISTORICAL (V-A123):** V-A44/V-A46 repair stale Glass coordinates and keep source/test readiness distinct from final SHIPPED; P127's rehearsal coordinates now exist, while the combined P122/Lab row remains RED for its exact native witness and W33 release/tag/registry coordinates.
- **HISTORICAL (V-A123):** V-A53 records the correction-time disk truth: Value was still 3.1 with root/`/parsing`/`/units`, existence-conditioned prepare, no Value-4 declaration surface and no rehearsal tarball. The delegated C1 later closed that producer slice and Value4/Keyframes6 became immutable. The first Glass archive is held by V-A88/V-A90/V-A91; Glass declaration/anchor/first-frame correction and native witness, remaining consumers and routed product remain open. Value rehearsal recreation is retired from the live plan.
- V-A54–V-A57 select one compact quiet palette Card tuple, close the role/rung and About measure matrix, restore one visible producer selection marker, and extend the exact Lab/Blob relation through 320px and actual 400% zoom.
- V-A58–V-A61 retire unrepresentable failure audits and masked purge repair, give catalog rebuilding one coalescing crash-safe state, and bind public/cursor integers to the JSON-safe range.
- V-A62 records tracked/tag/npm Keyframes 5.3.5 separately from five protected dirty files carrying a prospective 6.0 label. V-A63/V-A64 make `/css` exactly 19 runtime/33 type exports with ten source parsers and `/easing` exactly 16 runtime/five type exports with nine named functions. V-A65 removes W3's rehearsal/final-coordinate dependency cycle.
- V-A66–V-A68 remove W3's stale rehearsal title, keep volatile active-source truth in this ledger, and split P047 correctly: W17 validates/freezes producer baseline `b0`; W29 alone owns the post-radius `0.66 ±0.015`, centroid/hull and 0px-geometry result.
- V-A69–V-A71 close the five-key aftermath tuple, native-button palette selection semantics and one exact committed-effect AuditEvent construction authority.
- V-A72 records the live W20 contradiction: Picker Blob still carried Tooltip/press/activation/duplicate-Copy behavior despite the inert-specimen contract. Its disjoint consumer lane removed those paths, made the root aria-hidden/pointer-inert, retained exactly the action-region Copy, and returned 3/3 focused tests plus targeted ESLint green. That slice is **LANDED/IMPLEMENTED**, not terminal SHIPPED; native routed evidence remains open.
- V-A73 records the direct-consumer failure after the seven-entry map landed. The correction-time census was 26 old `/parsing|/units` occurrences across sixteen demo source files plus `tsconfig.demo.json`; the current disk recheck now finds none in active demo source or its TS path mappings, and aggregate demo `vue-tsc` is zero-diagnostic. This is implemented direct-consumer state, not a completed wave or routed product.
- V-A74 records the next native Browser result: overlay and warn/error counts reached zero, but `#app` had no rendered descendants and only the atmosphere ground painted. V-A75 identifies the exact remaining graph: `index.html` imports App before mount; App→Dock reaches pinned Glass 5.0/Keyframes 5.2, whose emitted `parse-flatten` asks nested Value 1.2 root for missing `parseCSSValues`. The isolated 4,677-module Vite build fails on that binding while `vue-tsc` passes. The old `/private/tmp/value-v4-bootstrap-20260716/mkbabb-value.js-4.0.0.tgz` is 35,544 bytes with SHA-512 `e502fdb8fd4bdc4aa6979156a9c573f8393a89ed30d8acb35d28a39704680b5cfd9f317223f268a17ae18ee6f6c40c883db9fe34823ad0817271fb56ab39fe04`; it is **HELD / DEFECTIVE / BOOTSTRAP ONLY**, unclaimed and unpublished, and supplies no rehearsal or artifact credit.
- V-A76's same producer owner landed canonical `from`/`to` typed selectors and its focused/full/type/build evidence. The 36,378-byte pack is held; A84's comments/timeline correction is proven by the held 36,447-byte pack; A85's 38,126-byte replacement and focused Keyframes consume are now implemented.
- V-A77 removes W32's last ambiguous “inert Card/article” evidence phrase. V-A78 makes downstream semver evidence-derived: Value 4.0.0 is fixed; W17 audits Keyframes against 5.3.5 and Glass against immutable 6.0.0, choosing same-major minors when public-compatible and majors only for enumerated public breaks. The clean packed Keyframes comparison proves concrete public breaks in ValueUnit-bearing signatures/`InterpolatedVar`/`plainVars`, `getTimingFunction` and AST/descriptor/diagnostic shapes, so `K_NEXT=6.0.0` is selected; the Glass packed diff removes eleven public subpaths and selects `G_NEXT=7.0.0`. Artifact order remains Value→Keyframes→Glass.
- V-A79 records the historical emerging-CSS failure and landed single-grammar correction; A84's comments/timeline fixture is 21/21 green in the held 36,447-byte pack. The A85 shorthand-complete replacement and clean Keyframes 6.0.0 rehearsal are now complete; Keyframes retains no local grammar, fallback or alias.
- The exact V-A10–V-A12 and superseding V-A63/V-A64 producer rulings were sent directly to active Glass task `019f6610-6022-7df1-95b3-472b17a64656`. That task delegated the atomic C1 source/package/test implementation; formation avoids those paths and credits no partial source tree, rehearsal, tag or publication.
- V-A80 supersedes the provisional W29 `0.33` calibration. The existing owner alone changed `geometry.bodyRadius 0.33→0.325`, updated only the explanatory nominal geometry `0.66→0.65` and exact-config test, and returned focused Vitest 3/3 plus targeted ESLint green. P047 raw buffers measure ratio `0.660192…0.660415`, centroid error `≤1.342%`, hull inset `12.0%`, AA edge `≤1.885 CSS px` DPR1/`≤0.948 CSS px` DPR2 and equal WebGPU/WebGL2 alpha area. Together with V-A72's implemented W20 cleanup, these remain protected nonterminal slices: W17 baseline and the routed 0px chassis/seat/footprint witness remain pending, and no pack/commit/tag/publish is claimed.
- **HISTORICAL (V-A123):** V-A81 closes the disk-truth gap found by Pass 1: `docs/tranches/V/audit/bootstrap/w7/value4-exact-seven/manifest.json` records supplied facts for the held archive and candidate. The A85 transport and Keyframes rehearsal were superseded by immutable Value4/Keyframes6; no Value rehearsal recreation remains current. Glass native/declaration close and fresh Glass7 publication remain.
- V-A82 replaces the remaining WatercolorDot “inert face” shorthand with present noninteractive named list/text content and zero activation/focus/drag semantics. HTML `[inert]` remains reserved for its separately measured modal/route cases; this is a terminology correction, not a new path.
- V-A83 narrows catalog cursor immutability to rank/order coordinates; eligibility, access, current policy and current tags are rechecked at read and may skip an epoch row, while unchanged eligible rows retain the live no-repeat/no-skip invariant.
- **HISTORICAL (V-A123):** V-A97 preserved the immutable release direction: Value4 and Keyframes6 were published from producer-only registry graphs, and Glass7 follows from exact registry Value4 + Keyframes6. The remaining demo/dev Glass edges and mandatory locks refresh only after Glass7. No file/tarball lock, force, fabricated/stale lock, hidden peer or CI bypass is admissible.
- **HISTORICAL (V-A123):** V-A84/A85 settled the Value bootstrap; V-A86/V-A87 held three Keyframes candidates, and V-A93 held the 184,710-byte archive for strict declaration failure. The 184,254-byte replacement fed the Glass declaration/HeaderRibbon/Q003 rehearsal and was superseded by immutable Keyframes6. Value rehearsal is not current; Glass native/release and the routed product remain open.
- The historical 36,447-byte candidate made its comments/timeline fixture 21/21 green but remains held for the A85 shorthand defect; its unsupplied path/hash/tree remain deliberately null. It is not the current bootstrap or rehearsal artifact.
- A85's 38,126-byte bootstrap is producer/consumer green with no classifier. The 184,710-byte Keyframes archive retains historical evidence but is held by V-A93. The 184,254-byte replacement removes the false public `implements` claim, passes 35/35 focused and 1,197 aggregate tests plus strict no-lock consumption, and is the sole permitted input to V-A88/V-A90–V-A92/V-A94–V-A96's Glass rehearsal. No commit/tag/publish is credited.

- Keyframes independently rehashed and installed the 38,126-byte A85 bootstrap without lock mutation, proved exact seven/no-root plus `/css` 19 and `/easing` 16 runtime exports and frozen multi-row shorthand output, deleted `firstAnimation`, `collectShorthandOptions` and `collectStyleAnimationOptions`, and now uses `collectAnimationOptions(...).at(0) ?? {}` at the singular adapter/editor. Source/types and 49/49 focused adapter/ingest/editor tests are green; no local classifier/parser/fallback/alias remains. This is an IMPLEMENTED W17 consumer slice, not aggregate, pack, rehearsal, commit, tag, publish or SHIPPED evidence.

- The Keyframes W17 manifest preserves every held candidate, including the 184,710-byte TS2420 failure, and records the authoritative 184,254-byte replacement: SHA-256 `c8f0d1a4…`, exact Value 4.0.0, root plus `./engine`, zero runtime/peer/optional Glass edge, 10 members, root/engine declarations `c96f9040…`/`fc4f8889…`, 34/44 exports, 35 focused and 1,197 aggregate passes, strict `skipLibCheck:false` no-lock consumer green, one nonsymlinked Value4 core. No commit/tag/registry coordinate exists.

- The first Glass W17 rehearsal remains exact historical evidence at `docs/tranches/V/audit/rehearsal/w17/glass-7.0.0/manifest.json`: 966,350 bytes with corroborated hashes, 73 export keys, 69 module entries, 68 `typesVersions`, 501 reachable declarations, clean runtime peers, exact Value `/color`+`/css`+`/easing`, green type/test/build/profile/enumerated-native evidence and a packed diff selecting `G_NEXT=7.0.0`. V-A88/V-A90–V-A92 reclassify it HELD/DEFECTIVE for undeclared declaration reach and inferred HeaderRibbon behavior. Current source deletes the inference through the discriminated persistent/collapsible mode, but V-A94/V-A95 require honest Aurora cue/aperture and post-drag compositor integrity and V-A96 requires removal of accidental Reka/VueUse public type chains before one fresh rehearsal; no shim, nightly pin, ambient injection, hoisting, `skipLibCheck`, DOM/VNode oracle, offset/overflow, z-index, reset, timing or consumer mask is allowed.
- Current Glass execution has replaced its stale rehearsal graph with a registry-only Value4/Keyframes6 install: one physical deduped core, zero legacy/file/link/git edges, and clean warning hygiene after the FuzzySearch accessibility correction (55 affected tests, zero stderr). The latest dirty 7.0.0 source graph is LANDED/IMPLEMENTED at 176 files / 1,131 tests / 771-module library build / 3,572-module production demo build with strict package closure. Dock layering/first-paint, Drawer reversible focus, Progress/Slider demarcation, strict declarations and Q003 native lanes remain active; no fresh Glass7 pack, provenance, registry coordinate or routed-mount credit exists.
- **V-A134 direct Glass correction:** the current external source run is 176 files/1,132 tests with typecheck green; the preceding 1,131/771 build-count bullet is historical and not current pack evidence. Build, pack and verifier remain uncredited because the current sandbox cannot write Vite temp/cache paths. Q003 native cycles 2/3 remain RED with broad compositor slabs beside WebGPU on the honest 5/0 baseline.
- Fresh in-app Browser recheck of the canonical Picker URL remains honestly RED: after 1.8 seconds `#app` has zero children, `<main>` count is zero, body text is empty, no Vite overlay is present, and `readyState` is complete. This confirms the prior pre-mount legacy-graph diagnosis; it does not create a new failure family or authorize a static ground, alias, fallback or second mount.

## Execution update rule

An execution update records only: state transition, concrete product evidence, commit, and routed remainder. It does not reproduce the formation narrative or create a second ledger.

**V-A121–V-A168 current-stage rule:** older bullets above preserve chronology and historical transport failures. They do not reopen Value4/Keyframes6 publication, Keyframes native close, or Value rehearsal creation; only the current top status and V-A119–V-A168 rows govern the live Glass7/native/routed/API/UI stage. Q003 remains product RED until native cycles have zero slab/occlusion/lost-target corruption; missing π artifacts, legacy API canon, strict error/search/provenance behavior and missing route surfaces remain owned by their named rows.
