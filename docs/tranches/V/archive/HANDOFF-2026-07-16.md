> ARCHIVED 2026-07-17 — historical; superseded by V-PRIME.md. Read-only.

# THE HANDOFF OF RECORD — 2026-07-16

> **READ THIS FIRST ON RESUME.** This is the current cross-system execution handoff for Tranche V. It supersedes `HANDOFF-2026-07-13.md`, which is historical archaeology. This document summarizes the live boundary; it does not replace `V.md`, `ADDENDA.md`, `DISPOSITION-LEDGER.md`, or an owning wave specification.

## 0. One-paragraph state

Tranche V is fully specified but not converged or complete: 34 wave files exist, 31 remain uncompleted, and the unchanged-tree formation clock is `0/2` after V-A168. Value W8→W9→W7 is terminally shipped as immutable `@mkbabb/value.js@4.0.0`; the Keyframes portion of W17 is terminally shipped as immutable `@mkbabb/keyframes.js@6.0.0`. Glass7 is not shipped and no fresh rehearsal exists. The active release-critical rail is **W17a** product-green strict/native Glass rehearsal → **W33a** nonterminal Glass7 publication → **W17b** value registry refresh/routed mount → W18–W32 → **W33b** final close. Q003 must have zero slab, zero occlusion corruption and zero lost target on repeated native cycles; declining a causal hypothesis cannot authorize a corrupt artifact. W20's inert-Blob cleanup and W29's `.325` calibration are protected implemented slices, not completed waves. All other frontend, palette API, accessibility, performance and final-release work remains owned by its named wave. Do not reopen Value4/Keyframes6, add compatibility paths, promote held bytes, or mask the blank app.

## 1. Authority and read order

Read in this order before touching source:

1. this handoff;
2. `V.md` and `PROGRESS.md` for the current stage and 34-wave DAG;
3. `ADDENDA.md` and `DISPOSITION-LEDGER.md`, especially V-A119–V-A168;
4. the exact owning `waves/W<N>.md` before any implementation;
5. `ARCHITECTURE.md`, `CONSUMER-CUT.md`, and `coordination/CONSTELLATION.md` for package and cross-repository work;
6. `VISUAL-CONSTITUTION.md`, `PROPORTION-AUDIT.md`, and `OPTICAL-BENCH-COMPOSITIONS.md` for UI work;
7. `PALETTE-DOMAIN.md`, `PALETTE-FACILITIES.md`, `PALETTE-WIRE-CONTRACT.md`, `PALETTE-CURSOR-CONTRACT.md`, `PALETTE-OPERATIONS-REPLAY-CONTRACT.md`, and `LEGACY-PALETTE-SOURCE-CONTRACT.md` for palette work;
8. `EVIDENCE.md` for the small Browser-first π/DELTA obligation.

When chronology conflicts with a current-stage override, the newest V-A row and the owning wave win. Historical bootstrap, rehearsal, `K_NEXT`, Value/Keyframes publication, `G_NEXT` selection and broad W33 prose must not be executed again.

### What supersedes the 2026-07-13 resume recipe

Tranche U remains terminal historical input; V does not reopen its close or owner packet. The old V campaign's 78% thesis, paused pass-5 η/θ/ι recipe, workflow journals, prototype worktrees, complexity instruments and two-pass clock are not a workflow to resume. Their useful substance was adjudicated into this tranche: `DECISIONS.md` closes OF-1…OF-6, OF-ε1/ε2, NG-6 and the δ ceiling; W4–W6 own alias/plugin/gate deletion and physical feature topology; W7–W9 supplied immutable Value4; W10–W16 and W22–W24 own the palette/API cut; W17/W33 own the remaining package constellation; W18–W32 own the glass-ui-suffused frontend, accessibility, performance and gestalt. Do not launch “pass 5,” restore the old proof battery, or treat old campaign worktrees as implementation authority.

## 2. Non-negotiable execution law

- Prefer the smallest idiomatic architecture that deletes concepts, branches and duplicated ownership. KISS is a product constraint, not an excuse for a local patch.
- No legacy code: no aliases, shims, redirects, dual imports, hidden peers, fallback parsers, default-on-error substitutions, stale locks, file/tarball dependency locks, or masked error boundaries.
- Glass-ui is the component/material producer. Value consumes its final public primitives; value does not fork them locally.
- Proportion is systemic: type rungs, line boxes, insets, dividers, control seats, empty states and material density are judged as one composition, not as isolated CSS tweaks.
- Use the in-app Browser first for every visual or interactive claim. Standalone/headless Playwright is not a gestalt author and is allowed only for a named binary/repetition/timing case the in-app Browser cannot express.
- Spend effort on source, ordinary tests, real-stack behavior, packed consumers and rendered verification. Do not build a gate farm, status framework, screenshot census or proxy benchmark.
- Every newly observed material defect gets one mechanism-level addendum and an existing wave owner. Rewordings and instances of an existing mechanism are folded into that family, not minted as new work.
- Preserve the dirty worktrees. Never reset, mass-stage, overwrite or “clean up” unowned changes. Coordinate before touching any active Glass, demo consumer, package, lock or test path.

## 3. Immutable producer facts

### Value4 — SHIPPED

- package: `@mkbabb/value.js@4.0.0`
- gitHead/tag commit: `44ddaff7a22283a4f7a42608893eeae7bc234424`
- producer tree: `919dc0b4f912ebf4d62644fbc81d0d747c567ff1`
- tarball: `https://registry.npmjs.org/@mkbabb/value.js/-/value.js-4.0.0.tgz`
- integrity: `sha512-Z8ywb4htSxJlRFvoU1DNtvzr9Bsuaw9ahT/hvNlKbnRj6fTnLuXjn0itKq1Q5s6rwg24ct0zcLZ04BuR3/SzGw==`
- shasum: `ccb962e592fb42e6602fc2bb6afbfb1763788b9d`
- provenance run: `29497728532`
- exports: exactly `/color`, `/value`, `/css`, `/easing`, `/math`, `/transform`, `/quantize`; no root, `/parsing`, `/units`, `/browser`, alias or compatibility entry
- observed runtime export counts in that order: `23/1/19/16/9/9/2`; strict public type count `62`

The live value.js worktree is not the release commit. Do not infer registry truth from its dirty HEAD and do not republish or retag Value4.

### Keyframes6 — SHIPPED

- package: `@mkbabb/keyframes.js@6.0.0`
- gitHead/peeled commit: `5a9183a7afe24702081a7b87c8adc7286ddce9a0`
- tree: `da7107600d237a49f6daa2064366c2b8ffe91cc0`
- annotated tag object: `26190755ce1e57c54cb14ef0a454ae02ed2b3da0`
- tarball: `https://registry.npmjs.org/@mkbabb/keyframes.js/-/keyframes.js-6.0.0.tgz`
- tarball bytes: `184430`
- SHA-256: `77d8860054f00700de9bc9aeca62dfec91f1422cbf839df62f40135972df0eb8`
- integrity: `sha512-mpb3gSxU8UgO4HBBG2he6CFNCq7tW+k9id82DgAjeeDdeAmtEzmZ2/kuK3j5AbUZRULcN1QNkNJychNk49bT4Q==`
- shasum: `5cc5fdecb886f4df33371d3358239594a8965b6b`
- provenance run: `29499708034`
- exports: exactly root and `./engine`; runtime dependency exactly Value `4.0.0`; no Glass runtime, peer or optional edge

Its major is evidence-derived: the packed diff removes public ValueUnit/InterpolatedVar/plainVars, `getTimingFunction`, descriptor/AST/diagnostic and weighted/blend-mode contracts. Do not downgrade it, reopen the old local grammar, or make Keyframes compensate for Glass Q003.

## 4. Glass7 — the active external boundary

Glass7 has no consumable rehearsal or registry coordinate. The old 966,350-byte archive is held/defective and is never an input, candidate or release fact.

Last directly grounded source state:

- repository: `/Users/mkbabb/Programming/glass-ui`
- branch: `codex/bi-p-q-execution`
- HEAD: `c181f0a78dae598c9c28c7d5760a8756d7c00a28`
- package version: `7.0.0`
- registry-only lock SHA-256: `e6216e2188ea7d08fff572745be168dbf368df3d30baa10c2caa837bc817581f`
- lock resolves Keyframes `6.0.0` and Value `4.0.0` from npm, with one physical deduped Value core and no Value3/file/link/git edge in that producer graph
- current source evidence recorded by V-A134: 176 files/1,132 tests and typecheck green
- current build, demo build, pack, strict package verifier, native Q003 packet, tarball, provenance and registry coordinate: **uncredited / absent**

The current target major was selected by the packed public diff against immutable Glass6, not by tranche naming. It removes eleven public subpaths without aliases—`motion-curves`, `focus-scope`, `notification`, `color-swatch`, `controls`, `icon-chip`, `icon-tooltip`, `metric-badge`, `metric-cell`, `metric-stack`, `spa-view`—and adds `dark-mode-toggle` and `metric`.

### Exact W17 Glass work

1. Consume only registry Value4 and Keyframes6. Glass imports exactly Value `/color`, `/css`, and `/easing`; no root, prefix fallback, local parser or general serializer.
2. Close strict declaration ownership across every reachable packed `.d.ts`: relative edges resolve, every residual bare package root is directly declared, and an isolated consumer typechecks with `skipLibCheck:false`. Hoisting is not evidence.
3. Close HeaderRibbon's explicit contract: persistent/expanded is the default; collapsible behavior is opt-in and requires a real focusable anchor. First-frame empty/action-only left and right ribbons are actionable; anchored left/right reveal, pin, focus, inert and Escape behavior are proven without timing, width, overflow, z-index or consumer-slot workarounds.
4. Remove or explicitly mark historical the stale Value3/root/`/parsing`/`/units` producer canon in active Glass/value demo documentation.
5. Resolve Q003 to product-green from the honest baseline below; a falsified hypothesis is research output, not release evidence.
6. Build, test, typecheck, produce the real demo build, pack one fresh unclaimed rehearsal, run the strict package verifier and isolated registry-only consumer, then hand the exact bytes to W33. Do not tag or publish from W17.

### Q003 / V-A95 truth

- The honest current route baseline is 5 active backdrop filters and 0 nested filters.
- Reducing approximately 95 nested regions to 5 active/0 nested did not remove native corruption.
- A separate inert Configurator backing-plane experiment reached 3 active/0 nested and still failed. It was fully reverted.
- Chromium-only causation and a Browser-upgrade wait were withdrawn. Filter count alone is falsified as the cause.
- Native in-app Browser cycles still showed broad black compositor slabs beside live WebGPU while ordinary DOM/state remained intact.
- Required result: repeated forward and reverse native captures after settlement with zero black slab, zero lost target and zero occlusion corruption. An evidence-backed decline may retire an adjacency hypothesis or ineffective grid change only; it cannot close Q003 while corruption remains.

Forbidden Q003 responses include a backing plane, mask/cover, opacity or z-index hide, route/engine fork, sampler change, filter-count target, Glass degradation, Keyframes change, Drawer blur/radius change, timing dodge or screenshot-only pass.

### Glass handoff output

Return one fresh absolute tarball path and its byte count, SHA-256, raw SHA-512, npm integrity and shasum; exact source commit/tree or a declared dirty synthetic-tree identity; newly derived package/export/typesVersions/declaration map; dependency/peer/optional map including Embla ownership; strict declaration closure; build/test/demo-build/package-verifier results; registry-free isolated import result; and the native Q003/HeaderRibbon packet. Current source has 74 export keys and 68 `typesVersions` keys, so the held archive's 73/68 surface must not be copied. Do not use the word SHIPPED. W33a owns publication and registry equality.

## 5. Why the value.js app is blank

The canonical URL remains:

`http://127.0.0.1:9000/#/?space=lab&color=lab(92%25+88.8+20+/+82.7%25)`

Fresh in-app Browser observation on 2026-07-16:

```json
{"appChildren":0,"bodyText":0,"main":0,"overlay":0,"readyState":"complete"}
```

This is a pre-mount ESM failure, not a route, layout or overlay problem. The local dependency topology is an unmanaged mixed graph:

- `node_modules/@mkbabb/glass-ui` → `.claude/worktrees/glass-ui-pinned`
- that pinned package is Glass `5.0.0`; its packed Dock/Aurora/color/easing chunks directly import removed Value-root APIs
- the pinned island still contains nested Keyframes `5.2.0` and Value `3.1.0`, while root `node_modules/@mkbabb/keyframes.js` now points to the live Keyframes6 tree
- this mixed old/new topology evaluates before `createApp().mount()` and is not a valid registry consumer graph

The only valid repair order is:

1. close fresh product-green Glass7 W17a evidence;
2. publish Glass7 through W33a without claiming W33 complete;
3. replace the pinned Glass5 island with registry Glass7 + Keyframes6 + Value4, regenerate the consumer lock without file/tarball tricks, and remove stale demo canon;
4. run the full demo build, including the separately stale lazy About closure;
5. reload the exact URL in the in-app Browser and prove one nonempty `#app` subtree, exactly one `<main>`, selected Lab space, the numeric headline and ActionToolbar's sole Copy.

Do not restore Value root/`/parsing`/`/units`, add aliases, lazily hide the import, render a static ground, catch the error into an empty shell or mount a second app.

## 6. Protected implemented slices

### W20 Picker consumer cleanup

- HeroBlob Tooltip, press label, click/hover/pointer/focus listeners and duplicate Blob Copy are removed.
- The Blob root is aria-hidden and pointer-inert.
- ColorPicker retains exactly the action-region Copy.
- Focused ordinary tests were 3/3 and targeted lint green.

This is LANDED/IMPLEMENTED, not wave completion. W20 still owes complete routed hierarchy, spacing, selection, typography, responsive and Browser π/DELTA evidence.

### W29 Blob calibration

- the sole effective `geometry.bodyRadius` is `0.325`
- no `heroScale`, second radius, public introspection, seat/chassis/footprint change or fallback exists
- focused tests were 3/3 and targeted lint green
- raw-buffer evidence: painted ratio `0.660192…0.660415`, centroid error `≤1.342%`, hull inset `12.0%`, AA edge `≤1.885 CSS px` at DPR1 and `≤0.948 CSS px` at DPR2, equal WebGPU/WebGL2 alpha area

Do not change `.325` merely to make the Blob look larger. The remaining work is composed screen-space visibility/occlusion and the routed 0px chassis/seat/footprint witness. Any geometry change reopens the complete W17/W29 baseline and raw-buffer matrix.

## 7. Current material RED families

The full normative text is V-A119–V-A168 in `ADDENDA.md`. The receiving system should use these groups to choose work, not invent another audit taxonomy.

### UI and interaction

- W18: About has an empty Tooltip/false trigger, static Alert and repeated dividers; retain readable semantic structure and remove distraction.
- W19/W30: delete global `PaneSegmentedControl` and legacy mobile split state; keep one document-scrolling route scene and feature-owned tabs only.
- W20: the label/headline relation currently skips `display-3`; use one shared adjacent P019 `1/√φ` pair. The Lab two-line reservation creates a blank first line; actual wrap, not worst-case reserved acreage, owns height. A missing Blob cannot leave an empty reserved field.
- W21: SpectrumCanvas is pointer-only `role="img"`; add named numeric S/V axes and pointer↔keyboard↔numeric equality. ConsoleRail's tabs need real tab/tabpanel linkage.
- W22/W23: PaletteCard root-click/cartoon behavior and SwatchHoverMenu's face-as-button plus split hover/touch paths are invalid. One native named pressed seat/action path owns selection. Empty PaletteCardGrid content hugs rather than reserving 120px.
- W19/W27: `/about` and `/easing` are still absent as first-class rendered routes.
- W20/W29: the headline→instrument interval must be occupied by a visibly composited Blob/rail or collapse to the shared rhythm; transparent-buffer success alone is insufficient.

### Palette API and client

- W11/W16/W23: retire the mounted legacy `/palettes/:slug/restore`, versions/hash/revert and raw fork/provenance paths rather than fixing the hand-kept route table.
- W14/W16/W22: accept one cursor-only query grammar; malformed, stale, duplicate, mismatched or retired query keys fail explicitly. Never restart at page one. Empty queries emit `/palettes`, not `/palettes?`.
- W15/W23: restore a real `/sessions/me` consumer and cookie-derived auth recovery; no bearer/local-storage token or clear-on-any-401 shortcut.
- W10/W13/W15/W16/W23: every mutation requires UUIDv7 idempotency and replays the exact status/body/header tuple, including ETag/Set-Cookie where applicable.
- W10/W14/W16/W22–W24/W30: request and response decoders are closed and strict. Unknown/duplicate/null/trailing fields and malformed Problems fail explicitly; no Zod stripping or fabricated `about:blank`/statusText fallback.
- W11/W13/W14/W16/W22: public detail and social operations share one active-public Release predicate. Private, deleted, unlisted and moderated records do not leak or accept actions.
- W14/W16/W22: color-name search is bounded normalized byte-prefix search only; delete ranked/regex substring fallback.
- W13/W16/W22: proposal attribution comes from the authenticated Principal; delete caller-controlled `contributor`.
- W12/W16/W23: public fork/provenance output is minimal and release-scoped. Private/trashed/purged ancestors become `{kind:'unavailable',ordinal}`; raw documents and lineage fields never cross the wire.
- W10/W33: health/readiness and OpenAPI derive from the mounted typed routes and full Problem set. Delete the Mongo-only false-green health, root sentinel, custom `/docs` and hand-kept route table.

### Canon and evidence

- W0 must include substantive user messages from Glass session `e79fce3f-d24e-4654-8b27-d029653fedbe` (`2026-07-16T04:33:39.961Z…2026-07-16T16:08:26.385Z`) while excluding task wrappers, status/capacity messages and meta commands.
- W7's bootstrap instructions, old W17 count/rehearsal prose and broad W33 palette/data/deploy ownership are historical. W33's live package duty is Glass7 only.
- Missing evidence remains honestly RED; prose, source presence, raw numbers or old screenshots do not substitute for absent π artifacts.

## 8. Wave frontier and execution order

There are exactly 34 wave files.

| Set | Current state | Live meaning |
|---|---|---|
| W7, W8, W9 | SHIPPED / historical | immutable Value4 producer; no live wave duty |
| W17 | UNCOMPLETED | Keyframes6 immutable; Glass strict/native/release and routed value adoption remain |
| W20, W29 | LANDED/IMPLEMENTED slices; UNCOMPLETED | protect the slices; finish the full wave contracts |
| W33 | PRE-RELEASE RED | W33a publishes only after W17a; W33b final close waits on W17b/W30–W32 |
| other 27 waves | PLANNED | execute by the DAG and exact wave specs |

Do not treat numerical order as the scheduler. Use these disjoint lanes:

1. **Glass critical path now:** continue the already-active W17a Glass owner through strict declarations, HeaderRibbon, product-green Q003 and a fresh rehearsal; then execute W33a publication and W17b value refresh. This lane is cross-repository work and must not collide with another Glass owner.
2. **Independent research now:** W0, W1 and W2 can proceed in separate bounded worktrees. W3 is a current-ask/status reconciliation only; close it by recording existing dispositions and the singular Glass brief, not by waiting for another rehearsal or inventing producer work.
3. **Hardening after W0–W3:** W4 opens repository subtraction; W5→W6 own physical demo topology/feature ownership, while W10→W16 own the palette domain/API/client/real-stack rail. Run these as disjoint source lanes with one integration owner per overlapping surface.
4. **Frontend after W6/W17b:** W18 establishes the shared visual constitution only after the physical feature topology and registry Glass7 routed mount, then W19–W29 execute by their dependencies. W20/W29 resume from their protected slices.
5. **Adversarial close:** W30 accessibility/mobile, W31 performance/first paint, W32 real-GPU gestalt and concise π/DELTA, then W33b final canon/deploy/downstream-lock verification.

If Glass remains externally active, do not spin or duplicate it: work W0/W1 or another dependency-clean disjoint lane and consume only the owner's concrete handoff.

## 9. Required artifacts still absent

These named paths are absent and therefore RED:

- `docs/tranches/V/research/state-atlas.md`
- `docs/tranches/V/audit/request-registry.md`
- `docs/tranches/V/research/palette-failure-lab.md`
- `docs/tranches/V/research/target-graph.md`
- `docs/tranches/V/audit/pi/w17/p047-producer-baseline/manifest.json`
- `docs/tranches/V/audit/pi/w29/picker-painted-component/manifest.json`
- `docs/tranches/V/audit/pi/w31/activation-record.json`
- `docs/tranches/V/audit/pi/w31/interaction-manifest.json`
- `docs/tranches/V/audit/pi/final/`

Create them only in their owning waves from real evidence. Do not prefill them, cite a future path as proof, or grow another verifier around them.

## 10. Worktree and collision facts

At the handoff observation:

- value.js primary: branch `tranche-u`, HEAD `86a9a178cd7a6f2b573216966964c33513494f05`, package source version `4.0.0`, heavily dirty; this is not the immutable release commit
- Glass primary: branch `codex/bi-p-q-execution`, HEAD `c181f0a78dae598c9c28c7d5760a8756d7c00a28`, package source version `7.0.0`, heavily dirty
- Glass registry lock SHA-256 remains `e6216e2188ea7d08fff572745be168dbf368df3d30baa10c2caa837bc817581f`
- value's current root lock is producer-only and does not constitute the demo's final Glass7 consumer lock

Dirty-file counts are volatile diagnostics, not evidence. Re-census immediately before assignment. Preserve all existing staged, unstaged and untracked work; use dedicated wave worktrees where specified. Before editing an overlapping file, identify the existing owner and either wait for its bounded handoff or explicitly transfer ownership.

## 11. Return contract for the receiving system

For each bounded unit, return only:

- wave/addendum IDs and exact state transition;
- files changed and the deleted/replaced mechanism;
- ordinary type/build/test/real-stack evidence with exact counts;
- in-app Browser route, viewport/state, measured result and π/DELTA for visual work;
- commit/tree identity if committed, with unrelated dirty state preserved;
- artifact path/hashes/declaration/export/dependency map for package work;
- one named remainder or blocker with its owner.

Do not return vague optimism, a gate dashboard, source-only “done”, a declared capture missing on disk, a held artifact promoted to candidate, a partial counted as complete, or a re-deadlined chronic. A material defect becomes an addendum before proceeding; an existing mechanism is folded into its current family.

## 12. Immediate resume checklist

1. Re-read the current-stage overrides and the exact owning wave.
2. Re-census both dirty primary trees and all live agents before assigning files.
3. Continue the singular Glass W17a owner or, if it is active elsewhere, take W0/W1 work that cannot collide.
4. Do not touch Value4/Keyframes6 release history.
5. When fresh product-green Glass7 rehearsal facts arrive, independently verify them before W33a publication.
6. After immutable Glass7 exists, execute W17b: replace the pinned mixed consumer graph and rerun the canonical Browser witness before any Picker/UI claim.
7. Execute the remaining DAG. Do not stop at the W17 package cut: 27 planned waves plus open W20/W29/W33 remain.
8. Only after implementation and the named evidence settle should fresh non-author audits attempt the two unchanged-tree clean passes. Do not spend the tranche on that clock.

## 13. What completion actually means

V is complete only when the fresh Glass7 artifact is immutable and registry-equal; Q003 is product-green; the canonical app mounts through one Value4/Keyframes6/Glass7 graph; all 31 open waves satisfy their amended completion evidence; every current UI/API/accessibility/performance family is green or terminally declined where the product contract permits decline; every named π/DELTA artifact exists and is inspected; downstream locks/canon contain no legacy path or hidden fallback; and the final adversarial passes surface no unowned mechanism. Anything less is partial progress and must remain labeled as such.
