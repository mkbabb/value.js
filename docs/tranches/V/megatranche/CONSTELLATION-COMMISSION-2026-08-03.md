# CONSTELLATION COMMISSION — 2026-08-03 (owner directive, durable capture)

**Provenance.** Owner message 2026-08-03, arriving with the weekly-wall reset. This file is the
program spine for the commission; `SCOPE.md §M-20` is the mark. It binds until the owner amends it.
Session-durability law: this file carries the program cursor — update it at every phase boundary so
any wall-severed session resumes here without loss.

**Context at capture.** Challenge saturation COMPLETE: validator GREEN at **88/88 components ·
264/264 hash-banked axes** (`registry/COMPLETENESS-LEDGER.md`); harvest 572 results · 6,033
defects · 875 BLOCKER. The interim (07-29..08-02) sessions closed the omission catch-up, the
archaeology denominator (107 tranches), and authored the parser trio
(`PARSER-{RESUME-HANDOFF,IN-FLIGHT-AUDIT,WAVE-ADDENDUM}-2026-07-29.md`). Closed owner rulings live
in `IN-FLIGHT-AUDIT-2026-07-29.md §6` and MUST be applied, not re-queued.

---

## §1 The commission, distilled

1. **Total megatranche audit, ≥2 days**, both registers: **style** (frontend, demo — visual audits
   of every page/component/state, Safari mobile+desktop, chrome-devtools MCP for telemetry) and
   **library** (colocation, modularization, Goldilocks grain, DAG tri-fold). The owner's original
   refinement prompts are used EXACTLY (reproduced in §5). A sibling glass-ui audit runs
   concurrently on the producer side — coordinate by batched packet only.
2. **Per-repo managed sub-sessions** (§2): each establishes a goal, a long-running horizon
   criterion, and an acceptance admission; **full per-repo wave specs are the admission
   requirement** into the coherent megatranche. Waves group by repo but interconnect
   multidimensionally (parser lane crosses value/parse-that/keyframes; CRUD provenance crosses
   value/fourier; glass suffusion crosses all).
3. **Greenfield/prototype work NOW, not deferred** — prototyping informs the wave specs, not the
   reverse.
4. **Model law**: Fable orchestrates + adjudicates; Fable ∥ Opus 5 parallelized for work; Opus for
   banausic/mechanical sweeps (M-12 tri-fold). Sol/Luna language binds Codex-side seats only;
   receipts stay literal.

## §2 Per-repo sub-sessions

Each row: GOAL → HORIZON CRITERION (the long-running convergence test) → ACCEPTANCE ADMISSION
(what admits the repo's waves into the megatranche).

### value.js (this session, root)
- **Goal**: the audit corpus (264 axes, 6,033 defects) adjudicated into mechanism cuts; frontend
  perfected per the frontend order in `IN-FLIGHT-RESUME-HANDOFF-2026-07-29.md`; library Goldilocks
  settled (M-18); the new combinator parser consumed once its lane's gates pass.
- **Horizon**: two consecutive adjudication passes surface nothing new; every original-ID carries a
  terminal BUILD/FOLD/RETIRE/BLOCKED-ON row; validator + graph authority both GREEN.
- **Admission**: canonical per-wave files under `docs/precepts/instructions/tranche/WAVE_SPEC.md`
  shape, born-RED gates attached, formation blockers from
  `MECHANISM-CUT-FORMATION-MAP-2026-07-29.md` each resolved by name.

### parse-that (+ the parser-prototype lane; Codex session `019fae36-1241-7d33-9c14-58d86be7fae3` driven from here)
- **Ground truth at capture (from the 08-03 lane survey)**: live authority is
  `coordination/PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` (SHA `ced23440…`) — status
  PAUSED_RESEARCH / TERMINAL_SOURCE_RED / NO_ACTIVE_WRITER; the 07-29 parser trio is
  superseded (the P5 token plane is REJECTED). Every P1–P6 candidate family died
  (best conforming A3 = 0.92–1.04×, KILL/PRUNE; Sol seal: NO-EDGE on Node 26/V8 14.6).
  **The ≥10x floor is retired as active admission law** — arithmetically impossible against
  the complete-work denominator (fixed native floor 311,883 µs > the 10x budget 187,063 µs);
  the unratified replacement portfolio is strict-3x / strict-2x / measured-break-even. The
  Codex session parked itself cleanly (pause-handoff §9: trust disk, recompute hashes, never
  resume the thread). The ONE open novelty claim (OPEN_UNPROVEN): a finite Value-CSS
  semantic-and-recovery algebra compiling to BOTH source-direct JS AND zero-function-import
  **Wasm**, no CST, observationally equivalent over value/complement/provenance/diagnostics/
  rollback/recovery/round-trip — exactly the owner's "WASM union between BBNF and parse-that."
- **Goal**: drive the lane per the owner's 08-03 begin-order ("greenfield work NOW"): verify
  the pause state byte-for-byte (§9 checklist), then prototype the surviving hypothesis in a
  **FRESH, non-overlapping writer root** (fresh-root law; frozen/preserved roots untouched
  forever) — hybrid combinator architectures with the SIMD-scan/lex union INSIDE the
  combinator library (scanner-based replacement stays forbidden), allocation discipline at
  functor/string/number grain, modern V8/TS facilities, deep profiling; re-derive the
  performance law honestly from the replacement portfolio, attacking the fixed native floor
  itself (the only arithmetic route back toward 2x/3x/10x).
- **Horizon**: a candidate that survives its own falsifiers with equivalence GREEN and beats
  the ratified floor of the replacement portfolio; the CSS Syntax differential proof standing.
- **Admission**: the RE-AUTHORED canonical parser addendum + differential proof + bench
  receipts under the ratified law; routing law absolute: parser → Value PLAW-BIND →
  V·L1..V·L4 → X-W9 → X-W11 (packed Value release) → Fourier F.W0; keyframes consumes as a
  TYPED consumer after value.js; direct parse-that→Fourier credit forbidden.
  **Carve, dated 2026-09-17 — X-W0.i / CC-011 / C-13**: the minted form of this clause routed
  through a label that named a wave **defined nowhere** — zero hits in
  `registry/adjudicated/layout-gestalt.md`, against `STATE.md:97`'s *"four born-RED waves
  **V·L1..V·L4**"* — so the law is rewritten against the four defined waves plus one
  explicitly defined new cut: **X-W9** (`X/waves/W9.md`, parser and library apotheosis) →
  **X-W11** (`X/waves/W11.md`, release and verified close). No other byte of this dated record
  is amended. The retired label is deliberately not re-spelled here so this law-bearing site
  greps clean; it is quoted verbatim in the tombstone,
  `docs/tranches/X/W0/ROUTING-LAW-V-L5.md`.

### keyframes.js
- **Goal**: library + demo audited with the same burning lucidity (D/L/C per component, tri-fold
  jury); consumes the new value.js parser at its gate; full glass suffusion; colocation/
  modularization settled.
- **Horizon**: per-component saturation + adjudication convergence (two dry passes), demo visually
  audited Safari mobile+desktop.
- **Admission**: full keyframes wave specs with born-RED gates + the parser-consumption wave keyed
  to the parse-that lane's release condition.

### fourier-analysis
- **Goal**: full frontend audit on the LATEST glass-ui (temp-link the newest dist); the
  value.js↔fourier **CRUD API union** designed: palette CRUD + fourier-viz CRUD sharing a
  provenance model with **git-like chains** — base version + derived variants, diff support with
  trie-like compression, remixing, version history, user provenance. Visualization spec +
  containerization examined for fidelity, design language, correctness, and deep performance.
- **Horizon**: CRUD union prototype proves the provenance chain end-to-end (create → derive →
  diff → remix → history walk) against both palette and fourier-viz objects; frontend audit
  saturated.
- **Admission**: fourier wave specs + the shared-provenance API contract document co-signed by the
  value.js API row.

### glass-ui (producer — coordinate-only, unchanged)
- Their own audit runs concurrently. ALL communique batched at ROOT
  (`glass-ui/docs/tranches/BJ/coordination/`), never ad-hoc, never piecemeal; O-19 remains the
  index; new items land as dated addenda. Aggressive bespoke→glass replacement census in every
  consumer repo (partial loss accepted, choosily); generalizable gaps marked as fold-in asks.

## §3 Standing laws (owner-restated this commission)
- FULL glass usage = absolute shadcn abrogation, components AND style.
- BREATH OF LIFE · MOVEMENT OF MOMENTUM · GOLDEN GLASS in all repos.
- Goldilocks modules; module-name stripping; tests displaced to an isomorphic external tree.
- DAG of library + component structure, tri-fold adjudicated (two assume-WRONG readers + one
  adjudicator), iterated to dirigibility.
- NO quick solutions/workarounds; NO legacy/shims/dual paths/masking fallbacks; pruning granted
  IFF vacuity/superfluity (consumer count insufficient alone).
- Aristotelian proportionality of cards/affordances/margins/dividers; mark superfluity for
  removal AND under-affordance for addition.
- KISS-forward; minimal ceremony, maximal direct implementation via agent orchestration + visual
  verification.
- Re-booking forbidden; every deferred item terminal-dispositioned.

## §4 Program cursor (update at every boundary)
- [x] 2026-08-03: saturation confirmed 88/264 GREEN post-harvest; redundant omissions launch
      stopped before any write (wf_bb1c807c-f47, TaskStopped, dispositioned in the validator;
      journal harvested empty; hashes intact).
- [x] 2026-08-03: absorption fan-out returned (closed rulings/mechanism map · parser lane/Codex
      session · interim commits/constellation states/mail). Digests inform §2 rows above.
- [x] 2026-08-03: E13 reconciliation — I-21..I-24 rowed (the O-19 receipt six days late, breach
      recorded; five off-ledger 08-02 intakes pointered). **LIVE DEADLINE: glass D-2
      card-condense reversal window closes at TR#79's cut** — owner decision row below.
- [x] 2026-08-03: owner mid-turn edict sworn as **L-18 (quartet challenge) / L-19
      (anti-contrivance presumption) / L-20 (waves validate against precepts)**.
- [x] 2026-08-03: **M-21 marked** — the interim corpus is CODEX-AUTHORED; its claims are audit
      subjects, not standing authority; Claude fully drives auditing + tranche development.
      **Provenance fleet DEPLOYED**: run `wf_83265e1d-a99` (6 Opus sweep lanes: sessions ·
      axes-quality · parser-verify · red-matrices · supersession · tooling → adversarial verify
      → one fresh Fable disposition ledger at `audit/codex-provenance/CODEX-PROVENANCE-AUDIT-2026-08-03.md`).
      Its per-artifact ADOPT/VERIFY-THEN-ADOPT/SUPERSEDE/RETIRE rows gate which Codex packets
      feed wave authoring.
- [x] 2026-08-03: **FLEET RETURNED — 15/15 seats, 0 errors. Master ledger: 30 rows, ADOPT 3 ·
      VERIFY-THEN-ADOPT 11 · SUPERSEDE 5 · RETIRE 11, nothing undispositioned.** Verdict: honest
      at the byte layer, inflated at the claim layer. Load-bearing corrections: (a) **C-05 — the
      Sol/Luna model law was Codex self-authorized, never owner law; M-12 + L-18 stand** (SCOPE
      M-20 §8 corrected in place); (b) **264/264 splits: 218 CHALLENGED + 46 REPORT-AUTHORED**
      (+5 workflow EXISTS-DIRECT re-homed) — the unqualified figure may not gate anything (C-04);
      (c) the R4 orchestrator DIED BY QUOTA 08-03 08:41Z mid-dispatch — its three final typed-DAG
      NO-GO verdicts exist only in `~/.codex` rollouts and are the next mail sweep's first read;
      (d) seven of nine RED fractions RETIRED as manufactured cross-products (new standing rule:
      a denominator cross-producting the audit's own categories is L-19 contrivance unless it
      names a product consumer); (e) the parser M2 denominator 1,870,633 µs is UNPROVEN
      (reconstruction 1,636,680; "10x impossible" survives and STRENGTHENS; 3x/2x budgets
      UNCITABLE until restated); (f) two frozen checksum receipts broken on disk (07-31
      resurrection 5/6, eight-hour 20/24) — `.sha256` files are append-never-rewrite henceforth.
      **C-02 evidence commit landed: `338c513b` (797 files, the Codex chain as-is).**
      **NEXT = the §5.2 re-opens queue**: carry ledger (15 dropped cuts + `c4af0ef9` VERIFIED) ·
      completeness-toolchain re-labels · 3 axes re-run as workflow challenges + reduced-motion
      quarantine sweep · API 0/9 re-measure at HEAD · Glass-8 trigger re-derivation vs I-21
      (TIME-SENSITIVE, TR#79) · 7e28 worktree census · checksum re-issues · M2 restatement.
- [x] 2026-08-03: §6 closed rulings applied; **M-22 ratifications** under delegated judgment
      (letter X · D-2 consumer-side · parser fresh root `parse-that-css-totality-p2` · M2
      restatement vs 1,636,680 µs · checksum law F-7). **M-23** (choosy Fable) and **M-24**
      (Claude Code fully owns all tranche items; **Codex FIRED**, re-engagement owner-option
      only) marked in SCOPE. OC-2 remains the one undispositioned prior item.
- [x] 2026-08-03: mechanism-cut ledger TERMINAL (formation blocker 3) —
      `registry/CARRY-CUT-LEDGER.md`, 117 original-IDs, all BUILD/FOLD/RETIRE/BLOCKED-ON/
      VERIFIED, zero silent drops.
- [x] 2026-08-03: formation blocker 5 RESOLVED (M-22 ¶1: **tranche letter = X**, A..W occupied
      on disk). Twelve wave files authored at `docs/tranches/X/waves/W0..W11.md` (250 gates,
      228 born-RED).
- [x] 2026-08-03: **TRANCHE X SPECIFIED** (`14d83356`) — the L-20 conformance loop closed at SIX
      passes (`docs/tranches/X/CONFORMANCE-2026-08-03.md`: pass 6 fable-served, 12/12 CONFORMANT,
      0 defects; R-E 12/12 with descriptive invocation abolished; 117/117 ledger ids ∅ both
      directions; 0 cycles; 0 one-directional hazard edges). Rulings R-A/R-B/R-E minted and
      applied tranche-wide; pass-3 Opus-served receipt corrected in-file. Status fields stay
      `planned`; graph authority (X-W0) and Glass-8 trigger (X-W0.j → X.W4.g) now live INSIDE
      the tranche. **Execution awaits the owner's begin-word.**
- [ ] keyframes.js sub-session formation — **census fan-out RUNNING** (`wf_92c8fc9d-011`:
      3 Opus lanes → fresh-Fable agglomeration → `formation/keyframes/CENSUS-2026-08-03.md`);
      then D/L/C bands → wave specs (the admission requirement).
- [ ] fourier-analysis sub-session formation — **census fan-out RUNNING** (same run: 3 Opus
      lanes incl. the CRUD/provenance seam table → `formation/fourier/CENSUS-2026-08-03.md`);
      then the shared-provenance API contract + wave specs (the admission requirement).
- [ ] Adjudication of the 264-axis corpus in bounded Goldilocks batches until two dry passes
      (formation blocker 2: only 14 apotheoses exist; most axes challenged-but-unadjudicated).
- [ ] Parser lane: verify the pause state per §9 (SHA `ced23440…`, builder `0732ebc2…`, residue
      `de1d62ff…`, v12 target ABSENT), then open the FRESH writer root and begin the
      surviving-hypothesis prototype (owner begin-order 2026-08-03; Claude-owned per M-24).
- [ ] Glass 8 trigger receipt (formation blocker 6; installed remains 7.0.0; the census event
      is owned by X-W0.j, measured FAIL 0/4 at authoring).
- [ ] The design-and-iteration prompt: owner's message truncated before its text — standing design
      tri-fold law governs until the exact text arrives.

## §6 Owner decision rows (live)
1. **Tranche letter** — the only formation dependency that is yours alone (blocker 5).
2. **D-2 / TR#79 window** — glass DECLINED the card-condense producer register with a reversal
   window closing at TR#79's cut. MT-F043 (your OM-18 mark) either ships as our consumer-side
   spacing cure (glass's ruling stands) or we send the shaped producer ask BEFORE TR#79. Default
   if unruled: accept consumer-side ownership — it respects glass's standing law and keeps the
   cure at our root.
3. **Parser fresh writer root** — per your 08-03 delegation I will name a fresh, non-overlapping
   root (working name `parse-that-css-totality-p2`) and record it here at open; say the word if
   you want a different root or a different resume posture against the pause handoff's
   owner-ruling clause.

## §5 The owner's exact prompts (referenced by §1; use EXACTLY)
The two prompts reproduced in the owner's 2026-08-03 message — the **seed/audit prompt**
("There's an active Claude Code instance herein… archeological dig… GPT Sol xhigh… ECOUTE-MOI")
and the **tranche formulation prompt** ("# Tranche formulation: the post-tranche audit and
next-tranche development prompt…") — govern verbatim. They live in the session transcript of
record; the Sol/Luna routing inside them binds Codex-side seats (M-12 governs Claude-side seats).
The third ("design and iteration") prompt was truncated from the message and is AWAITED.
