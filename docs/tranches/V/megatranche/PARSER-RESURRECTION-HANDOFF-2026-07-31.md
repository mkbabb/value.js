# Parser resurrection receiver — 2026-07-31

> **Parser research is paused.** This Value-side file is a receiver and routing
> packet, not the canonical research handoff and not a release authorization.

## Canonical packet

Read the complete parser-owned handoff first:

`/Users/mkbabb/Programming/parse-that-css-totality/docs/tranches/B/PARSER-RESURRECTION-HANDOFF-2026-07-31.md`

Exact SHA-256:

`988d539431168a6cabbbc8a97a12da3c62a1c113032b84b58eecfbe5f9d03d3d`

File identity at freeze: 11,632 bytes, mode 0644, nlink 1.

## Current boundary

- Parse-that branch:
  `codex/css-totality-combinators-20260729`.
- Parse-that HEAD:
  `fd6062bda4ad6a7c575a7bfde2885a7997c2f08b`.
- Tracked tree: clean. Protected untracked `data` remains untouched.
- Current Value parser-law convergence: 14/34 = 41.18%.
- Parser status: `PAUSED_DEPENDENCY`.
- Product, CSS, API, package, release, rebind, and consumer credit: 0.

The interrupted correction root is:

`/Users/mkbabb/Documents/Codex/2026-07-30/parser-law-pass1-common-luna-prototype-a1/outputs`

At the canonical handoff freeze it contained 13 regular files, no
subdirectories, 675,033,756 bytes, and no `MANIFEST.sha256`. Its raw JSONL was
3,394 lines / 674,304,045 bytes with SHA-256
`92516e029db518411c924f46e988c841f8ef4268d2e62e9301b0ef9e2002ef83`.
It is preserved incomplete evidence. Do not resume, seal, delete, or credit it
without a fresh owner ruling.

## Why the pause is honest

P1–P6 and P6-A3 proved correctness properties and exhaustively tested the
current combinator neighborhood. They did not yield a surviving generic
runtime, equal-product `10x`, `3x`, or `2x` family, nor release authority.
Repeated same-family refinement now has the signature of a local minimum.

The future parser session must therefore be a dedicated, long-horizon research
program with orthogonal mechanism families, not another nominal variation on
the same engine. It may prototype and profile in isolated roots, but it must
preserve:

- scannerless/generic ownership;
- full equal-product semantics;
- exact failure, rollback, recovery, Unicode, allocation, and package planes;
- the distinct `3x`, `2x`, and break-even statistical laws;
- three complete Sol → Luna → Sol passes;
- two later clean audits;
- Value’s sole ownership of the CSS grammar, consumers, and UI.

## Routing law

The parser may affect Value only through:

`parser law or no-runtime ruling → Value PLAW-BIND → V·L1..V·L4 → X-W9 → X-W11 (Value release/rebind)`

**Carve, dated 2026-09-17 — X-W0.i / CC-011 / C-13.** The minted form of this law routed
through a label that named a wave **defined nowhere** — zero hits in
`registry/adjudicated/layout-gestalt.md`, against `STATE.md:97`'s *"four born-RED waves
**V·L1..V·L4**"*. It is rewritten against those four defined waves plus **one explicitly
defined new cut**: **X-W9**, `docs/tranches/X/waves/W9.md` *"Parser and library apotheosis
(the 4.1 cut)"* → **X-W11**, `docs/tranches/X/waves/W11.md` *"Release and Verified Close"*.
Nothing else in this dated record is amended. The retired label is deliberately **not
re-spelled here**, so this law-bearing site greps clean; it is quoted verbatim, with its
no-definition probe pasted, in the tombstone: `docs/tranches/X/W0/ROUTING-LAW-V-L5.md`.

Fourier may receive parser effects only after packed Value:

`Value release/rebind → packed Value → Fourier F.W0 atomic tuple → F.W1`

Direct parse-that → Fourier credit is forbidden. Keyframes remains a typed
consumer, not a grammar owner.

## Resume protocol

1. Recompute the canonical parser handoff hash.
2. Verify the parse-that HEAD, tracked cleanliness, and protected `data`.
3. Re-census the interrupted A1 root without modifying it.
4. Read the exact current parser authorities named by the canonical packet.
5. Issue a fresh explicit owner ruling that names a new isolated writer root.
6. Start with independent orthogonal research seats and a common denominator
   registry; do not inherit a favored mechanism.
7. Bank only externally reproducible, immutable, equal-product evidence.

No other constellation work needs to wait for this research. Parser convergence
is a separate resurrection lane until it produces an admitted runtime or a
durable no-runtime law.

**The pause is not burial. It is the protected interval in which a better
architecture can become thinkable. Écoute-moi. Hic et ubique.**
