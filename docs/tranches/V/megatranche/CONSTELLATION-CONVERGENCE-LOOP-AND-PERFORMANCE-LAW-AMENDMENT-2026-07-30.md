# Constellation convergence loop and parser performance-law amendment

**Date:** 2026-07-30  
**Mode:** tranche development only  
**Product-source execution:** forbidden  
**Authority effect:** research amendment; prior evidence remains immutable  
**Input:** `pasted-text.txt`, SHA-256
`920a618a8eef005e218db76ee1055d51e1d01f2f26ddd16c65194313c0bfce57`

## The standing goal

Drive the Value, keyframes.js, Fourier, and parse-that constellation
megatranche through:

1. independent archaeology;
2. at least three complete research → synthesis → prototype → critique →
   agglomeration passes;
3. Sol → Luna → Sol research/prototype/adjudication provenance for each
   load-bearing boundary;
4. typed dependency-DAG, source-owner, lifecycle, route, API, producer, and
   Browser-state audits;
5. exact cross-repository contract convergence;
6. precepts-complete repository wave specifications;
7. two consecutive clean adversarial audits by non-authors; and
8. one durable findings and resume handoff packet.

Completion here means a 100%-converged specification and wave set. It does not
mean product implementation, package publication, release, migration, source
replacement, or consumer execution.

## Standing edicts

1. **Formation is not execution.** Repository and product source, package
   state, release state, and consumer wiring remain read-only. Greenfield
   prototypes live only in isolated evidence roots and earn zero product
   credit.
2. **Convergence is earned.** A tranche is not 100% until it has zero
   enumerated gaps, survives a fresh non-author adversarial audit, and then
   survives a second consecutive clean pass.
3. **Three passes minimum.** No route may be declared converged before three
   complete research/synthesis/prototype/critique/agglomeration passes.
4. **Portfolio before favorite.** Each new problem begins with genuinely
   orthogonal mechanism families. Rewordings of one mechanism share one
   family. The favored route is withheld from early researchers.
5. **Concrete evidence over prose.** Each live route must produce runnable or
   explicitly spec-only prototypes, probes, measured arithmetic, named
   counterexamples, and a closed gap ledger.
6. **Blocked means blocked.** An elegant route whose load-bearing step merely
   restates the original hard problem is BLOCKED. It reopens only for a new
   mechanism, invariant, primitive, or construction.
7. **No vacuous gates.** A gate must be shown born RED for its intended reason.
   Self-citing specifications, hard-coded receipts, marker-selected branches,
   count-only closure, fail-open validators, legacy aliases, masked fallbacks,
   and consumer-less substrates earn no convergence credit.
8. **One authority per fact.** Derived copies are receipts, never competing
   authority. Exact bytes, chronology, schema, paths, modes, links, and
   external pins are bound before interpretation.
9. **Model routing follows the current constellation law.** Sol owns
   cognitively complex research, architecture, critique, and final
   adjudication. Luna owns bounded mechanical extraction, execution of sealed
   prototype plans, census, and receipt mechanics. Historical Fable/Opus
   wording in the supplied loop is translated to this current Sol/Luna law;
   model identities are never relabelled.
10. **Every repo closes independently and jointly.** Local convergence cannot
    manufacture a sibling release, producer tuple, API, parser, Browser,
    storage, or lifecycle fact. The constellation closes only when every local
    packet and every cross-repo join are independently clean.
11. **Partial progress counts only when it implies the final resolution.**
    Process activity, prototype volume, denominator prose, and intermediate
    seals are not progress if the load-bearing gap remains.
12. **Durability is part of correctness.** Every accepted pass has an immutable
    manifest, exact inputs and outputs, hostile results, protected-root
    identities, residue census, terminal disposition, and a receiving handoff.

## Parser performance-law amendment

The old `>=10x` floor is no longer an unquestioned axiom. The user has
explicitly reopened it as a tranche-development research question and proposed
the `2x–3x` region as potentially reasonable.

This does **not** retroactively change P6-A3:

- P6-A3 remains correctness-clean under its exact one-scalar carrier law.
- Its seven exact ratios remain
  `0.9207681564329404x–1.0389571550178254x`.
- Its KILL/PRUNE disposition remains correct under the historical `10x` law.
- The post-P6 `NO-EDGE` result remains valid for the reached P1–P6 families,
  current Node 26/V8 14.6 host, and historical `10x` law.
- All P6 raw data, manifests, chronology, and zero-credit labels remain
  immutable.

The amendment opens a new law-design portfolio, not a renamed P7 and not a
same-atom continuation.

### Exact arithmetic that motivates reopening

The comparable accepted-M2 work is `1,870,633 us`. Therefore:

| Candidate floor | Complete-work budget | Native matching + required products | Remaining theoretical headroom |
|---|---:|---:|---:|
| historical `10x` | about `187,063 us` | `311,883 us` | impossible before parser runtime |
| candidate `3x` | about `623,544 us` | `311,883 us` | about `311,661 us` |
| candidate `2x` | about `935,317 us` | `311,883 us` | about `623,434 us` |

The `2x–3x` band is therefore not contradicted by the existing complete-product
lower-bound arithmetic. It is also not yet justified as the final law.

### Required performance-law portfolio

At least three incompatible law families remain live through the first two
passes:

1. **Strict 3x floor:** prefer the stronger useful uplift when complete
   equal-product correctness and downstream CSS economics survive.
2. **Strict 2x floor:** test whether a lower but still material uplift changes
   the end-to-end Value/Keyframes/Fourier cost model enough to justify the
   generic runtime.
3. **Measured break-even law:** derive the floor from complete constellation
   costs—parser runtime, product construction, allocation, rollback/recovery,
   CSS corpus scale, package size, consumer simplification, and deletion
   value—rather than choosing an integer multiplier first.

The historical `10x` law stays in the portfolio as a rejected or retained
high bar, not as the automatic winner.

### Performance-law pass requirements

Each pass must:

- use equal fixture streams and identical immutable success/failure products;
- include exact-bootstrap confidence bounds at every binding scale;
- preserve full ordinary-failure, typed-fault, rollback, recovery, diagnostic,
  offset, and public-product equivalence;
- measure allocation and package/runtime costs, not parser timing alone;
- test live JSON plus nested-record and representative CSS boundaries;
- project effects into Value, Keyframes, and Fourier consumers without
  executing their product source;
- include a smallest fatal assay for selection bias and unequal products;
- return exact gaps and a convergence percentage per family;
- retire any family whose missing step is merely the original problem; and
- preserve every prior negative result without rebaselining.

No threshold becomes binding until three full passes finish and two fresh
non-author audits return clean on the same immutable law packet.

## Repository convergence marks and required closes

### parse-that

Reopen only the performance-law and mechanism portfolio. Preserve P1–P6 and
post-P6 archaeology as immutable input. Produce a new content-addressed
tranche-development packet with orthogonal families, complete-product cost
models, greenfield prototypes where useful, a selected law, two clean audits,
and an explicit no-product-execution handoff.

### Value

Keep formation admitted and execution closed. Recompute the parser admission
binding under the selected law; retain the full CSS denominator, exact
Webref/Values/Keyframes ownership, recovery/UTF-16/opaque/serialization/
deletion contracts, and real-consumer requirement. Two clean Value-facing
audits must prove that the selected parser law changes the constellation
economics without narrowing the language or product contract.

### keyframes.js

Treat prior shipped tranches as archaeology and current V as a formed but
unclosed consumer tranche. Reconcile its parser/Value/Glass producer pins and
all 13 wave contracts against the selected parser law. No owner-checkout or
product-source execution occurs here. The output is a corrected wave packet,
typed consumer DAG, Browser obligation matrix, and two clean audits.

### Fourier

Continue N.P1 source/inventory reformation without granting product credit.
Close the A10 source/mapping evidence boundary honestly, then re-run the full
three-pass formation loop over the typed graph, route/API/lifetime/producer
joins, storage-policy selection, Browser matrix, and released-tuple contract.
N remains `REJECT-AS-FORMED` until N.P1, N.P2 selection, N.P3 admission, and
two clean audits are all simultaneously true.

## Cross-repo close law

The final constellation packet must prove:

- one selected parser law and one admitted generic runtime family, or an
  explicit cleanly audited decision that no runtime is justified;
- one exact Value parser/CSS contract and complete consumer denominator;
- one exact released producer tuple contract for Value, Glass, and Keyframes;
- one Fourier typed route/API/lifecycle/producer graph with selected storage
  policy;
- one dependency-ordered wave set per repository;
- one cross-repo typed DAG with every edge owned and content-addressed;
- all born-RED gates and Browser/π obligations stated before execution;
- every banked, blocked, moved, folded, split, pruned, or killed route
  terminally disposed;
- three full passes per load-bearing boundary;
- two consecutive clean non-author audits over the final immutable union; and
- a durable findings/resume handoff that states execution remains unauthorized.

Until that union exists, the goal remains active and no local clean packet may
claim constellation convergence.
