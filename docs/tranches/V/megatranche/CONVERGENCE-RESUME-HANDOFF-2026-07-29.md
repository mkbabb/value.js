# Convergence resume handoff — 2026-07-29

## Resume declaration

This is an active Value tranche-development handoff. Resume from commit
`c4af0ef9` and the dirty documentation/audit tree. Preserve all unrelated
changes. The preceding Claude audit root is quota-halted; no worker shown in
its terminal records is alive.

V·C2–V·C6 below are the binding source strategy and acceptance basis. They are
not dispatchable wave files. Before a dependency opens, transcribe that cut to
the canonical named-wave form required by
`docs/precepts/instructions/tranche/WAVE_SPEC.md`, including State, Goal,
Scope, Triumvirate Dispatch, File Bounds, Disjointness, Worktree Plan, named
Agent Units with sub-gates, Hard Gate, lint cadence, verification artefacts,
commit plan, dependencies, and archaeology. This is a document-shape
requirement; it must not delay dependency-free source work.

The findings authority is
[`CONVERGENCE-REAUDIT-2026-07-29.md`](./CONVERGENCE-REAUDIT-2026-07-29.md).
The coverage ledgers are:

- [`excavation/CONSTELLATION-TRANCHE-COVERAGE-2026-07-29.md`](./excavation/CONSTELLATION-TRANCHE-COVERAGE-2026-07-29.md);
- [`excavation/SESSION-COVERAGE-2026-07-29.md`](./excavation/SESSION-COVERAGE-2026-07-29.md);
- [`registry/COMPLETENESS-LEDGER.md`](./registry/COMPLETENESS-LEDGER.md).

## Governing execution law

Source deletion, consolidation, and direct implementation take most of the
time. A gate may be added only when it replaces a failed claim mechanism and
has a source or browser falsifier. No proof farm, alias layer, forwarding
directory, compatibility wrapper, command bus, universal chassis, or
producer-CSS copy enters this tranche.

Frontend proposal passes use one Sol xhigh and one Luna xhigh instance, both
with the frontend-design guidance. A final Sol xhigh instance chooses the
smallest coherent union. Luna xhigh implements bounded mechanical slices; Sol
xhigh challenges and closes them. Every visual claim closes in the internal
browser.

Exact Luna xhigh is available through the Codex thread/CLI surface and is the
mechanical route. If that surface is unavailable, pause the mechanical seat.
Terra xhigh may substitute only under an explicit owner instruction and must
be labelled as Terra in every receipt; no fallback is called Luna.

## Concurrent formation lane

### Goal

Finish the exact missing evidence and historical dispositions without
repeating banked work or blocking independent source changes.

### Work

1. Assign the 10 capacity-blocked component rows and seven queued omission
   rows to available executors. Produce only the 49 missing canonical axes.
2. Adjudicate every already-complete component corpus concurrently.
3. Repair the workflow wrapper so a child error or short
   `componentsRun/componentsRequested` result makes the outer run partial or
   failed and populates `failures`.
4. Complete the 45 `PENDING-DEEP` Glass tranche rows, newest-to-oldest,
   recording only changed or surviving dispositions.
5. Fold session roots by user task. Stop rereading a root once its instruction,
   close claim, and terminal disposition have a durable receiver.

### Completion

Formation closes when the completeness validator reports 264/264 banked axes,
each current component has one adjudicated disposition, all 107 actual tranche
rows are `DEEP`, the two non-tranche artefacts remain explicitly dispositioned,
and every surviving carry has one source owner and one receiver. This
completion does not gate source cuts whose dependencies are already satisfied.

## V·C1 — production demo boot

**Status:** COMPLETE at `c4af0ef9`.

**Goal:** Make the production demo entry traversable by Vite so a successful
gh-pages build mounts the application.

**Bounds:** `demo/color-picker/index.html` and
`demo/color-picker/main.ts`. Package exports and library source are excluded.

**Completion:** `npm run gh-pages` transforms 2,831 modules and emits a 526 KB
application entry; `npm run typecheck` passes; built direct and
`#/gradient` routes mount at 1440×900 and 390×844 in the internal browser.

## V·C2 — Glass 8 adoption and native swatch controls

**Status:** BLOCKED-ON the published Glass 8 packed contract.

**Goal:** Consume the producer-owned indicator surface and restore ordinary
native control ownership at every interactive swatch site.

**Prerequisite:** Verify the published Glass version, export path, packed
artifact, props, slots, and deletion notice. Do not infer them from active
Glass source.

**Bounds:** the exact Watercolor import and interaction sites, including
`CurrentPaletteEditor.vue`, `SwatchHoverMenu.vue`,
`MixSourceSelector.vue`, and `GenerateControls.vue`; related type imports and
focused tests. No copied Glass CSS or interim wrapper.

**Implementation:** A native button owns click, focus, name, disabled state,
and keyboard behavior. A paint-only swatch or producer indicator is its
visual child. Decorative Watercolor sites retain decorative semantics.

**Completion:** the old producer import path reaches zero; all six interactive
sites retain accessible names, Tab, Enter, Space, click, and disabled behavior;
there are no nested interactive descendants; the packed consumer build,
typecheck, focused tests, and desktop/phone browser cells pass.

## V·C3 — canon, route scenes, and action ownership

**Status:** NOT-STARTED.

**Goal:** Replace the physical two-pane chassis and competing action contracts
with one semantic, route-owned scene per route.

**Prerequisites:** V·C2 and a fresh graph census for the files being removed.

**Bounds:** `demo/DESIGN.md`, `demo/color-picker/App.vue`,
`demo/shell/viewSchema.ts`, `demo/shell/usePaneRouter.ts`,
`demo/shell/PaneSlot.vue`, Dock action layers, `demo/color-session/keys.ts`,
route scene roots, `PaneHeader.vue`, and their direct tests/styles. API,
parser, and palette CRUD internals are excluded.

**Implementation:**

1. Re-author the compact canon: maker/audience/job, measured chroma,
   Golden Glass, Breath states, driver/observer momentum, typography roles,
   scene topology, and fourteen route theses.
2. Reduce the route registry to identity and scene metadata.
3. Mount exactly one active scene. Optional inspector/action regions render
   only when they have a job.
4. Introduce one typed, owner-tokened `SceneActionSet`; delete component refs,
   `any`, string dispatch, Picker action priority, and physical side fields.
5. Make Gradient the hierarchy pilot and Mix the command-ownership canary.
   Correct Gradient's clipped phone select labels and dense easing rail.
6. Reconcile the canon against the rendered result at close.

**Completion:** every route has exactly one H1 and one marked protagonist;
each registered action changes its named state at 1440×900 and 390×844; Mix
shows Mix commands; mobile Generate, Gradient, and Mix commands work; absent
inspectors consume no space; pane selectors, side fields, stale refs, and the
two old action contracts are absent; no horizontal overflow or copied
InstrumentChassis recipe exists. Preserve `100dvh` unless this matrix produces
a specific counterexample.

## V·C4 — palette specimen and domain separation

**Status:** NOT-STARTED.

**Goal:** Separate factual palette rendering from selection, route state, and
mutation so the reusable library surface stays quiet and consumer-shaped.

**Prerequisite:** V·C3 establishes the inspector destination.

**Bounds:** `PaletteCard.vue`, `PaletteCardMeta.vue`, palette display leaves,
selected-entity inspector, palette route ports, and focused tests. Server
schema changes are excluded unless a reproduced contract defect requires one.

**Implementation:** Create a props-only `PaletteSpecimen`; let a native parent
control own selection where needed; move rename, menus, feedback, versions,
tags, export, publication, and destructive actions into the selected-entity
inspector. Preserve honest leaves and delete `PaletteCard.vue` after all
consumers move. Remove the three-tag slice and render factual counts.

**Completion:** N=0,1,2,5,50,200,201 fixtures preserve true counts, tags, and
colors; specimens import no CRUD ports; nested controls reach zero; each
mutation has one owner and a visible result; public, authenticated, Admin,
offline, and error states stay distinct; the aggregate card is deleted.

## V·C5 — direct-import subtraction

**Status:** NOT-STARTED.

**Goal:** Complete shadcn abrogation by deleting forwarding and configuration
residue after semantic destinations are stable.

**Prerequisite:** V·C3 and V·C4.

**Bounds:** the 19 `demo/ui/*` barrels and their 90 imports,
`components.json`, stale ESLint paths, `demo/shared/utils.ts`, package
dependencies, and direct consumer imports. Producer source is excluded.

**Implementation:** Rewrite consumers to the real Glass or local semantic
owner, move or retain debounce under its honest owner, delete unused `cn`,
`clsx`, and `tailwind-merge`, and remove stale config and aliases. Retain
`tw-animate-css` while live classes consume it. Retain direct Reka types unless
a producer-owned type export makes them unnecessary.

**Completion:** the 19 barrels, stale config, dead aliases, `cn`, `clsx`, and
`tailwind-merge` are absent; no replacement forwarding directory exists; all
imports resolve from their real owner; build, typecheck, lint, tests, and
representative browser null-deltas pass.

## V·C6 — release verification

**Status:** NOT-STARTED.

**Goal:** Prove the completed product and packed surfaces without adding
unfinished design or compatibility work.

**Prerequisite:** V·C2 through V·C5.

**Bounds:** test fixes that expose regressions, release records, and generated
package output owned by the release. New product features are excluded.

**Completion:** direct and hash production boot pass; all fourteen routes pass
desktop, phone, and 720×450 cells; keyboard command journeys pass; owned
reduced-motion and forced-color cells pass; packed exports resolve from a clean
consumer; API-labelled states remain distinct; Safari evidence is recorded
separately from Playwright WebKit.

## Independent parser lane

The active parser work proceeds beside V·C2–V·C5:

- parse-that supplies the smallest reusable runtime;
- Value implements CSS grammar, public behavior, consumer migration, and UI;
- BBNF waits for its W3 freeze and consumes the shared runtime without
  creating another CSS parser.

Its source boundaries and release gate remain in the parser handoff. Frontend
formation must not duplicate or reorder them.

## Two-day convergence cadence

| interval | direct work | concurrent evidence |
|---|---|---|
| remainder of day 0 | preserve V·C1; receive and verify Glass 8 if published | fill only the 49 missing axes; adjudicate complete corpora |
| day 1, if Glass 8 is published and V·C2 closes | execute V·C2, then begin V·C3 with the compact canon and Gradient pilot | deep-disposition newest Glass and sci tranche rows; repair wrapper propagation |
| day 1, if Glass 8 remains unpublished | no frontend source bypass; continue parser and formation work | deep-disposition newest Glass and sci tranche rows; repair wrapper propagation |
| day 2 | close V·C3 through Mix command proof; execute V·C4 | continue the 45-row Glass archaeology and parser lane without duplicating ownership |
| day 2+ | execute V·C5 and V·C6 | finish historical rows, terminal carries, and constellation receipts |

If Glass 8 remains unpublished, V·C2 stays blocked and parser/formation work
continues. V·C3 does not copy the unpublished producer surface to bypass it.

## Constellation communication boundary

The current named-peer, head, shared-surface, and conflict protocol is
[`coordination/CONSTELLATION-BOUNDARY-2026-07-29.md`](./coordination/CONSTELLATION-BOUNDARY-2026-07-29.md).

- Glass: send nothing. Its prior batch is acknowledged, and this audit found no
  new producer-root defect.
- Active Value CSS/parser task: send this ownership boundary, the completed
  boot commit, and the instruction to keep parser work outside frontend scene
  ownership.
- sci/Atlas: report no conflict and the exact plan delta: five frontend cuts
  plus release, parser parallel, Q remains green and non-blocking.
- BBNF: send one receipt only if its active plan would create a CSS parser
  before the W3 freeze.

Messages leave only after this handoff and its links exist on disk.
