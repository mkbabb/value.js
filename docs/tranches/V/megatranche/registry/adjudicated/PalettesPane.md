# PalettesPane — adjudicated apotheosis (M-23/L-18 fresh adjudicator) · served model: `claude-fable-5`

**Seat**: fresh Fable adjudicator (M-23/L-18), the incredulity seat over two hostile re-readers — reader-D (design-and-usage) and reader-L (library-and-code) — each of which challenged the full pass-5 challenge corpus for `demo/palettes/PalettesPane.vue` and its cone.
**Date**: 2026-08-06 · **HEAD**: `3ea0144c` · branch `tranche-u`. The readers' line anchors were cut at an earlier HEAD (`c654824e`-era); every anchor in this record was **re-resolved by this seat at `3ea0144c`** — re-resolve again before curing (the D-19 law).
**Corpus provenance**: all three governing challenge files are **EXISTS-ORIGINAL** (HYDRATION-LEDGER.md:109-111, pass 5, hand-authored — not in the 46-file REPORT-AUTHORED batch); **no r2 file exists for any axis** (directory listing, this seat) — the unsuffixed pass-5 files govern. Motion: M-33 is a quarantined-**FALSE** row (no bottom fade) and is NOT carried; M-34 (**TRUE** — the reduce obligation on the nine `.pane-scroll-fade` panes) and M-16 (TRUE, PaletteCardMeta) are cited where they bind.
**Wave authority for routing**: tranche X ratified (`docs/tranches/X/waves/W0..W11`); CC-ids per `registry/CARRY-CUT-LEDGER.md` (CC-003 Glass≥8 trigger · CC-006→X-W7 palette domain/arbitrary-N/CRUD · CC-004→X-W5 typed action contract · CC-007→X-W8 graph subtraction · CC-008→X-W9 library · CC-009→X-W10 design/motion canon · CC-031→X-W1 CI cliff · CC-044 WatercolorDot-impostor cut). Dispositions here are **ADJUDICATED, not VERIFIED** — nothing in this file stamps a wave.
**Method**: every verdict-level disagreement between the two readers, every kill, and every missed-find promotion was re-derived against the tree by this seat's own instruments before adoption. The registry was then swept for identity — five of the readers' fifteen "missed" finds and eight of their confirmed rows were already banked by sibling adjudications (CurrentPaletteEditor.md, PaletteCard.md, PaletteCardGrid.md, EmptyState.md, ColorSpaceSelector.md); those rows are **IDENTITY-FOLDED, never re-booked** (anti-rename law).

**Arbiter's own instruments this session (all static + node against installed dist; zero browser tooling — probe parsimony; the dated challenge evidence is never rewritten)**: full read of PalettesPane.vue (212 L whole); CurrentPaletteEditor.vue:30-169 + :245-275; PaletteColorStrip.vue (whole); usePaletteStore.ts (whole); usePaletteActions.ts:60-135; usePalettePorts.ts:175-185; useFilteredList.ts (whole); export.ts (whole) + usePaletteExport.ts (whole); PaletteCard.vue:1-60 + :96-104 + :139-147 + :200-329; PaletteCardSwatches.vue:1-65; PaletteCardMenu.vue:220-228 + emit census (15 static literals + the :52 dynamic makePublic/makePrivate pair = 17; `copyAll` never); SwatchHoverMenu.vue (whole); PaletteCardGrid.vue (whole); EmptyState.vue:1-60; PaneHeader.vue:40-58; Dock.vue:105-118 + :72; usePaneRouter.ts:151-161; foundation.css:143/:328/:670-720/:825-845; utils.css:88-140/:186-200; palettes-ramp.ts:30-85; vite.config.ts:270-293; tsconfig.demo.json:30-50; eslint.config.js:183-188 + glob census + `ls demo/@` (absent); package.json deps/exports + `readlink node_modules/@mkbabb/value.js` (real dir, 4.0.0); e2e/smoke/flows/palette-save.spec.ts:25-40 + `ls .github/workflows` (3 files, zero playwright/e2e hits); installed glass-ui 7.0.0 bytes: watercolor-dot.js (`inheritAttrs:!1`, no `tag` prop, **zero renderSlot**, `aria-hidden":"true"`, `pointerEvents:"none"`), WatercolorDot.vue.d.ts (slots `{}`), button-Bu9F4uU6.js props (emphasis/tone/size/iconOnly/loading/type/disabled/class/asChild/as — **no `variant`**), card/styles.css (`@utility cartoon-surface { position:relative; border-width:2px; box-shadow:var(--shadow-cartoon-md) }`, **0 `:hover` in the file**), glass-atom.css (`.cartoon-cast` reads `--cartoon-press-t`), property-regs.css (`--cartoon-press-t` initial 0), scale-paper.css `--focus-ring-shadow` (30%/15% alpha), utilities/base.css `.focus-ring:focus-visible { outline:none; … }`, useLiquidPress dist (`pressVar ?? "--press-t"`, onKeydown/onKeyup gated Enter/Spacebar); @vueuse/integrations dist/useSortable.js (defaultOptions.onUpdate survives an onEnd-only options bag; `moveArrayElement` mutates a non-ref list in place, re-insert deferred to nextTick); repo greps: `--card-press-t` (3 hits, all PaletteCard.vue), `--cartoon-press-t` demo writers (0), `data-color-surface` in demo/palettes (0), `copyAll` (1 hit), `\.focus()` in demo/palettes (2 hits, neither restorative), `palettes/export/` consumers (1: demo/test/export/byte-exact.test.ts), `@add-color` (2: PalettesPane:47 on the editor, BrowsePane:108 on the card — never PalettesPane's card), src→glass-ui/keyframes imports (0).

## Scoreboard

**34 adjudicated rows CONFIRMED-family (6 BLOCKER · 15 MAJOR · 10 MINOR · 3 INFO), of which 13 are IDENTITY-FOLDS to rows already banked in this registry (never re-booked) and 13 originate in the re-readers' missed finds · 10 claims KILLED (8 reader kills ratified after re-derivation + 2 reader missed-finds killed by this seat) · 12 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

Blocker-weight headline: **the pane's primary gestures are dead or destructive, and the layer that would have noticed is ungated.** The only add-colour affordance and every swatch control are producer-dropped spans (PP-1); the desktop swatch-edit Save/Cancel emits die in a hand-written prop bag two hops up (PP-3); the first drag of a session persists a scrambled order and a filtered drag rewrites the invisible remainder (PP-4/PP-5); a two-byte-corrupt store whiteouts the app through an unvalidated `palettes` field (PP-6); and the one e2e spec covering the save gesture targets a role/name the rendered DOM cannot contain, in a CI that runs no e2e at all (PP-2).

## Ruled disagreements (this seat's actual work)

| dispute | reader-D | reader-L | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C:N-6 severity** (LAN save destroys colours) | CONFIRMED BLOCKER | KILLED as BLOCKER (dev-only trigger) | **L wins — RESCOPED to PP-7 (MAJOR sequencing + MINOR dev hardening)** | vite.config.ts:286 `host: true` sits inside the `:280-292` `else` branch whose comment reads "Dev mode: serve the demo app with HMR" — no production mode takes it; both shipped origins are HTTPS secure contexts. The surviving mechanism is real and mine to confirm: `saveCurrentPalette` emits `saved` then **unconditionally** `clearCurrent` (CurrentPaletteEditor.vue, my read of the :247-266 body), and Vue routes handler throws through `callWithErrorHandling`, so a throw inside the saved handler (e.g. `crypto.randomUUID` at usePaletteStore.ts:85 on an insecure origin) does not stop the wipe. Trigger dev-only; sequencing production-real. |
| **D:P5-4 sub-claim** (the 0.5 guard "unreachable, edict-2 masking fallback") | CONFIRMED "exact" | KILLED (mis-scoped domain) | **L wins — sub-claim KILLED (K-7)** | The 1–50 bound lives in PALETTE-CONTRACT §3 and export/types.ts:56 (`/** 1..50 atoms */` on `ExportSnapshot`, which nothing in the app constructs — L-2). The local path enforces nothing: types.ts `colors: PaletteColor[]` uncapped; `createPalette`/`updatePalette` uncapped (my whole-file read). `100/n < 0.5` is reachable at n > 200 through the ordinary add-swatch gesture. The honest residual — the contract bound is enforced at reload.ts:191 and nowhere on this path — folds into PP-14 (and is kin to CurrentPaletteEditor A-2(ii)). |
| **D:P5-9 provability** (one interaction state; hover choreography never fires) | CONFIRMED MAJOR at source | UNPROVEN-NEEDS-LIVE | **D wins — statically decided, CONFIRMED** | This is not a computed-style question: the whole atom is `@utility cartoon-surface { position:relative; border-width:2px; box-shadow:var(--shadow-cartoon-md) }` and the producer file contains **zero** `:hover` (my grep of installed dist). The PaletteCard.vue:7-18 comment describes choreography no loaded rule contains. Identity: ≡ **PaletteCard.md PC-4** (already banked with the same conclusion) — folded, not re-booked (PP-12). |
| **D:P5-8 provability** (forced-colors kills data, keeps decoration) | SOURCE-CONFIRMED MAJOR | UNPROVEN-NEEDS-LIVE | **D wins on the structural half — and the row IDENTITY-FOLDS to PC-7 (BLOCKER)** | The tier-1 roster (foundation.css:679-697) grants `forced-color-adjust:none` to 16 selectors; PaletteColorStrip segments (`div.shrink-0`, inline backgroundColor) match none, and `[data-color-surface]` has 0 uses under demo/palettes (my greps). The strip is `aria-hidden="true" role="presentation"` (:3-5) — no second channel. The print twin (foundation.css:829-841 reuses the roster) is the same omission. **PaletteCard.md PC-7 already books both limbs at BLOCKER** — reader-D's "0/0/0 print hits" was true of the pass-5 corpus and false of the registry. Folded (PP-11); the WHCM/print pixel table stays UA-cell residue (U-4). |
| **C/P-1 mechanism** ("read() casts instead of parsing") | (not filed) | KILLED the mechanism, kept the finding | **L's kill RATIFIED (K-6); finding survives re-mechanized (PP-6)** | usePaletteStore.ts:22-32 DOES `JSON.parse` in try/catch and DOES reject non-numeric `version`; it never validates `palettes`, so `{"version":1}` passes and savedPalettes' `.filter` on `undefined` throws inside a computed (:51-55, my read). The prescribed pass-4 cure ("parse instead of cast") would add a parse that already exists and ship the whiteout unchanged — the cure-adjudicated-separately law (ColorSpaceSelector addendum item 2) in the flesh. |
| **D:D-13** (search field "has no accessible name") | KILLED (placeholder IS the accname) | (not filed) | **D's kill RATIFIED (K-2)** | Reader-D's own telemetry records tab stop 13 as `INPUT "Search your palettes..."` — the HTML-AAM placeholder fallback. Residual is the narrower, true MINOR: placeholder-only labels vanish on input (WCAG 3.3.2/2.4.6 quality) — PP-28, one-attribute cure (`aria-label` forwards through SearchBar's attrs). |
| **D:P5-12** (fallback triple "not the colour that ships", ΔL 0.161) | KILLED as category error (INFO residual) | CONFIRMED MINOR (duplication only) | **Reconciled: drift limb KILLED (K-3), duplication CONFIRMED (PP-33, INFO, already carried D-7/L-7)** | The stamped tokens are a function of the live accent + per-site surface lightness (palettes-ramp.ts:104-119 doctrine, utils.css:190-197 states the fallback class verbatim) — no static literal can equal a dynamic token at every seed. And the call-site fallbacks are structurally required: PalettesPane.vue:172-174 aliases a DIFFERENT token (`--palettes-ramp-title-*`) into the recipe slots; a bare `var()` would be guaranteed-invalid pre-stamp. The byte-identical triple duplication (verified: utils.css defaults ≡ rampTitleVars fallbacks) is real and already carried (edict 5). |
| **reader-L missed #1** (unbound `@add-color` leaves a live dead "Add … to current palette" button on every saved card) | (not filed) | filed MAJOR | **KILLED AS FILED by this seat (K-9)** | PaletteCard.vue:142 passes `:is-local="palette.isLocal"`; the popover Add button is `v-if="!isLocal"` (PaletteCardSwatches.vue:42); this pane renders **only** local palettes (savedPalettes filters `p.isLocal`, usePaletteStore.ts:51-55; filteredSaved ⊆ savedPalettes). The button never renders on this pane — there is no live dead control. Residual MINOR: the `addColor` emit channel is dead wiring on this consumer (declared at PaletteCard.vue:208, forwarded at :321-324, bound by BrowsePane:108 and never by PalettesPane:82-97) — swept by PP-17's typed-contract cure. |
| **reader-D missed #4** (`hidden lg:flex` overlay strands mobile edit with no Save/Cancel) | filed MAJOR | (not filed) | **KILLED by this seat (K-10)** | The ≤lg complement exists and auto-activates: Dock.vue:72 `mobileEditActive = !isDesktop && !!editTarget`, :111 `activeLayer = "mobile-edit"` (my read) — the exact complement of the overlay's 1024px gate. Already sealed at **CurrentPaletteEditor.md K-10 + superlative S-9**; reader-D filed a claim the registry had already adjudicated in the opposite direction. |
| **D:P5-1** (the ramp BLOCKER: "rainbow unreachable; chroma thrown away") | KILLED (gamut-cusp math + owner ruling) | KILLED (truncated quotation) | **Both kills RATIFIED, convergent (K-1)** | My source read confirms both refutations' substrate: palettes-ramp.ts:49-58 records the 3-stop analogous fan as "the Q4-record relocation clause, ruled in at Q5 — STANDS UNTOUCHED" and brackets the register to the owner; the ±40° docblock's own continuation — `enough that the three letterform stops read chromatically distinct / ("rainbow"), narrow enough to stay one analogous family` — is precisely the sentence reader-D's quotation cut. Reader-D's independent gamut-ceiling solve (measured stops on the sRGB cusp to six decimals; an iso-chroma ≥180° cure forces C≈0.082, duller than the "defect") is adopted as the arithmetic limb (reader-derived, spot-consistent with the resolver's own :36-38 doc). Residual: a **relay observation to the owner** (80° is narrow for the word "rainbow") — not a defect row. |

Two further reader kills ratified without a table row: **L/P5-1-framing** (K-5 — the "quoted-spec violation" BLOCKER promotion of the export defect: the clause governs ExportSnapshot serializers; export/serializers.ts names `../export.ts` as "the pre-contract routed seat that W50 will replace"; a forward spec is not a prohibition the tree violates — the code defect itself is PP-8) and **D:P5-3's seed-tint limb** (K-4 — chroma seen through a 66.4%-alpha unblurred plate is the ambient field's, a permitted lane; a composite sample cannot distinguish tinted-surface from permitted-tint-behind-translucent-surface; the elevation sign-flip limb stays UNPROVEN, U-1).

## Adjudicated defect roster

Severity · claim · proof standard · terminal disposition. IDENTITY-FOLD = the row is already banked in this registry under the named id and is never re-booked (anti-rename). Line anchors are HEAD `3ea0144c`.

- **PP-1 · BLOCKER — the pane's only add-colour affordance and every swatch control are producer-dropped decorative spans; the Plus glyph is additionally slot-dropped.** (C:N-5 ≡ reader-L C/N-5, converged; + reader-L missed #2.) My producer bytes: no `tag` prop, `inheritAttrs:!1` (class/style only), **zero `renderSlot`**, root span `aria-hidden="true"` + `pointerEvents:"none"`, d.ts slots `{}`. Consumers verified: CurrentPaletteEditor.vue:95-105 (tag="button", :aria-label, @click, `<Plus>` child — all discarded), SwatchHoverMenu.vue:13-20 (touch PopoverTrigger as-child — no anchor) and :29-36 (hover click). Desktop survives only via wrapper-div pointerenter (:2-6) into an `aria-hidden` teleport panel (:37-51). **IDENTITY-FOLD → CurrentPaletteEditor.md A-1 + PaletteCard.md PC-3 → CC-044 (one atomic cut, BLOCKED-ON CC-003 Glass≥8) + X-W7.c/d.** The producer-API question rides the standing glass-ui BH relay (owner edict), already sent.
- **PP-2 · BLOCKER — the one automated test of the pane's core gesture is unsatisfiable by construction, and e2e is absent from CI.** (C/e2e.) palette-save.spec.ts:34-37 targets `getByRole("button", { name: /Add current color .* to palette/ })`; per PP-1 no element carries that role or name — statically RED, no run needed (probe parsimony). `.github/workflows/` = ci.yml/deploy-pages.yml/release.yml, zero playwright/e2e references (my grep). **IDENTITY-FOLD → CurrentPaletteEditor.md A-3 + CC-031 (DR-09) → X-W1.**
- **PP-3 · BLOCKER — `commitEdit`/`cancelEdit` never reach a handler: the router's hand-written prop bag uses literal hyphenated keys the emit resolver cannot match.** (C/C-3, sharpened.) usePaneRouter.ts:156-157 `"onCommit-edit"` / `"onCancel-edit"` in an object literal (no template, no compiler camelize); Vue's emit resolves `onCommitEdit` and falls back to hyphenated keys **only for `update:*` model listeners** — neither matches. The desktop edit overlay's Save/Cancel (CurrentPaletteEditor.vue:75-80 → PalettesPane.vue:51-52 → the void) is a dead end-to-end path; the working control case is App.vue's template `@commit-edit` on `<Dock>`. **BOOK → X-W5 (CC-004 typed action contract: the router's prop bags become typed, compiler-visible bindings).**
- **PP-4 · BLOCKER — the first drag-reorder of each page load scrambles and persists the library order.** (C/C-1.) Mechanism fully re-derived by this seat from installed dist: useSortable's `defaultOptions.onUpdate` survives an onEnd-only bag; `moveArrayElement` takes the **non-ref** list without copying and splices synchronously with the re-insert deferred to nextTick; PalettesPane.vue:183 passes `pm.filteredSaved.value` (with an empty query, useFilteredList returns `items.value` — the cached computed array, same identity in onEnd), so :190's `ids` is short one element and :191 removes the wrong id before :194 persists. **IDENTITY-FOLD → PaletteCardGrid.md PG-1 → X-W7.**
- **PP-5 · MAJOR — a drag under any non-empty query relocates every hidden palette en bloc behind the visible ones, and persists it.** (C/C-2 + L/L-17.) reorderPalettes' second loop (usePaletteStore.ts:163-165) appends every non-listed palette to the tail; :190 maps ids from the visible subset only. **IDENTITY-FOLD → PaletteCardGrid.md PG-2 → X-W7.**
- **PP-6 · BLOCKER — a corrupt-but-versioned store (`{"version":1}`) whiteouts the app.** (C/P-1 as re-mechanized, K-6.) The serializer validates `version` and never `palettes` (usePaletteStore.ts:22-32); `savedPalettes` then `.filter`s `undefined` inside a computed (:51-55) — a render-path throw. **BOOK → X-W7 (CC-006 CRUD ownership: the store validates its whole schema or returns the default).** Rider: the recovery paths return the **shared mutable** `defaultStore` — see PP-26.
- **PP-7 · MAJOR (rescoped from C:N-6 BLOCKER per K-8) — the save path sequences a destructive `clearCurrent` unconditionally after `saved` with no success signal.** CurrentPaletteEditor `saveCurrentPalette`/`confirmUpdatePalette` both emit `saved`/`updated` then `clearCurrent`; Vue's `callWithErrorHandling` means a handler throw never aborts the wipe. Production-reachable throw not demonstrated (reader-L's boundary analysis adopted: useStorage writes are watcher-deferred); the demonstrated trigger (`crypto.randomUUID` at usePaletteStore.ts:85/:147 + utils.ts:15 on `http://<lan-ip>:9000`) is **dev-only** (vite.config.ts:280-292). **BOOK → X-W7 (save becomes transactional: clear only on confirmed commit); MINOR rider: guard/polyfill randomUUID for the LAN dev origin the repo deliberately serves.** Kin: CurrentPaletteEditor.md A-2(iii)/D4-01 (the same `clearCurrent` firing after a silent no-op — one family, two arms, cross-cited not re-booked).
- **PP-8 · MAJOR — the shipping export path: two raw-interpolation injection vectors (name → SVG text content, `c.css` → fill attribute), a rejecting zero-colour case (width 0 → toBlob null), collapsing slugify (`""` → dotfiles + the colliding `--palette--0`), same-tick `revokeObjectURL` on a never-appended anchor, a switch with no default, and every failure `console.warn`-silent while publish gets `card.showFeedback` fourteen lines away — beside a certified 12-module `export/` tree whose only consumer is a test.** (C:N-7 + L/L-2 + L/P5-2/3; BLOCKER framing killed, K-5.) All limbs re-verified in export.ts / usePaletteExport.ts / PalettesPane.vue:96,199-211; `palettes/export/` consumers = exactly `demo/test/export/byte-exact.test.ts` (my grep); the three-vocabulary seam (menu verbs → PaletteCard translation table → `format: string`) confirmed. **BOOK → X-W7 (the W50-named contract seat replaces the legacy module; the ExportFormat union becomes the one vocabulary — jointly with PP-17's channel cure at X-W5).**
- **PP-9 · MAJOR — the saved grid is an ARIA `list` owning zero `listitem`s, malformed in both states.** (C/N-8.) role="list" at PaletteCardGrid.vue:3; children are role="article" cards or the v-if'd EmptyState. **IDENTITY-FOLD → PaletteCardGrid.md PG-3 + EmptyState.md ES-9 → X-W7.**
- **PP-10 · MAJOR · GLASS-OWNED + consumer residue — the house focus indicator is a 30%-alpha ring over `outline:none`, structurally unable to clear SC 1.4.11 on light plates; the demo compounds it with hand-rolled `ring-ring/40` at 7 sites in this cone.** (D:P5-7.) Producer bytes verified: `--focus-ring-shadow` 30%/15% alpha (scale-paper.css), `.focus-ring:focus-visible { outline:none; box-shadow:… }` (base.css). Reader-D's recomputed composite (1.89:1, correcting the challenger's 1.76) is adopted as the chromium-cell number — the plate sample underneath is live (U-3). The WHCM outline at foundation.css:703-719 is the authored fallback, not a cure for the normal register. **Producer ring opacity → the standing glass-ui BH relay; consumer `ring-ring/40` sites (CurrentPaletteEditor.vue:46/:49/:52, PaletteCardSwatches.vue) die into the house focus register on the X-W7.c/d successor surfaces.**
- **PP-11 · BLOCKER (by identity) — the colour strip is absent from the demo's own colour-surface roster: erased in WHCM and dropped in print while decoration keeps full chroma; `aria-hidden` leaves no second channel.** (D:P5-8 + reader-D missed #9.) Roster + non-membership + zero `[data-color-surface]` verified by this seat. **IDENTITY-FOLD → PaletteCard.md PC-7 (BLOCKER, RESCOPED per its R-2: demo-owned, one-attribute interim cure) → X-W7.c.**
- **PP-12 · MAJOR — the card has one interaction state; its press choreography writes `--card-press-t` which nothing reads while `.cartoon-cast` reads `--cartoon-press-t` which nothing writes; the cast child double-paints a static shadow; Enter/Space handlers sit on an unfocusable div.** (D:P5-9 + reader-D missed #1 + missed #8, one mechanism family.) All four legs re-verified in producer bytes + greps (see instruments); the :7-18 and :28-29 comments are false as written; useLiquidPress's key handlers require focus the `role="article"` root (no tabindex) can never take. **IDENTITY-FOLD → PaletteCard.md PC-4 → X-W7.c G11 (+ X-W10 rider via PC-1's booking for the motion-canon law).**
- **PP-13 · MAJOR — three constitutionally distinct tiers (filter control `.search-seated`, authoring plate `.dashed-well`, saved specimen `bg-well`) paint the identical `--well-bg` token: vocabulary collapse, not a functional break.** (D:P5-2, reader-D's BLOCKER→MAJOR demotion ratified; border census corrected 1.5/1.5/2px.) Source half verified (utils.css:96-107/:132-137, PaletteCard.vue:19, foundation.css:143→:328); composited pixel identity stays live residue (U-2). **BOOK → X-W10 (CC-009 design canon: the material ladder names its rungs); the `.search-seated` recipe-over-producer leg is kin to AdminPane.md D-14/L-13 and the ASK-D seated-field rung (BOOKED SWAP, dies at the producer adopt event).**
- **PP-14 · MAJOR — the specimen well is scale-blind and its documented legibility floor is self-cancelling.** (D:P5-4 core + reader-D missed #3, promoted with this seat's rescope; the 0.5-dead-guard sub-claim KILLED, K-7.) Verified: fixed `h-10 w-full` for 1..n colours (:6-11); in the weighted branch the floor-then-renormalise (:60-67) guarantees every floored segment renders **strictly below 8%** whenever the floor engages (flooredTotal > 1), fully self-cancelling in the equal-weight case (20 equal → exactly the unfloored 5%) and never delivering the documented guarantee in any case — the :38-43 contract and :47 docstring are false on the admitted domain; the ordinary branch's 0.5% floor is a second, 16×-apart floor that IS reachable (n > 200, uncapped local path). **BOOK → X-W7.c (CC-006 "arbitrary-N truth" is this row's name): the successor specimen states a true floor or none, and the 1–50 contract bound gets one enforcement home.**
- **PP-15 · MAJOR — the primary destructive path guarantees focus loss to `<body>` and the only live region is created in the same tick as its content.** (D:P5-6, with reader-D's correction adopted: the EmptyState `role="status"` mounts v-if'd WITH its text — likely silence, not the wrong sentence.) Verified: the delete-all opener is inside `v-if="savedPalettes.length > 0"` (:62) and its own success removes it; zero restorative `.focus()` in the pane (my grep); EmptyState mounts with content (PaletteCardGrid.vue:22-31). **BOOK → X-W7 (focus-return + pre-mounted live region on the CRUD surface).** Kin: CurrentPaletteEditor.md D4-01 (same failure class, cross-cited).
- **PP-16 · MAJOR — the dead `variant` prop family: the ruled "quiet ghost" demotion of delete-all never landed (PalettesPane.vue:64 ships a filled secondary capsule), the kebab trigger wears a capsule wash (PaletteCard.vue:98), and the duplicate-name Update/Cancel render byte-identical.** (C/pass-4:N-2 as corrected + reader-D missed #2.) Producer props verified (no `variant`; emphasis defaults "secondary"); the Update/Cancel class strings verified character-identical (:151-166); reader-D's false-positive correction ratified — CurrentPaletteEditor.vue:97 is a WatercolorDot whose `variant` IS a declared prop, struck from the blast list. **The twins are IDENTITY-FOLD → CurrentPaletteEditor.md ADJ-M3; this pane's own sites (:64, PaletteCard.vue:98) BOOK → X-W4 (CC-040s semantic-controls re-vocabulary; glass-independent).**
- **PP-17 · MAJOR — the card action channel is one untyped string across 18 dispatcher entries with a silent-drop default; `copyAll` is registered and emitted by nobody; the `export` emit and `format` parameter are `string` end-to-end.** (reader-L missed #3 + #5, promoted; L/P5-3's localized version subsumed.) Verified: `defineEmits<{ action: [action: string] }>` (PaletteCardMenu.vue:224-227); menu emits 17 literals (15 static + the :52 dynamic pair — my census, correcting reader-L's "eighteen distinct literals emitted"); dispatcher holds 18 including the orphaned `copyAll` (PaletteCard.vue:290-319, `if (!fn) return`). A typo in any literal compiles, lints and no-ops. The copyAll consequence is **IDENTITY-FOLD → PaletteCard.md PC-8** (the no-copy-path BLOCKER); the channel mechanism itself was un-booked — **BOOK → X-W5 (CC-004: one closed union, exhaustiveness-checked; fixes 18, not L's 5).**
- **PP-18 · MAJOR — the demo's structural lint globs point at a tree W43 deleted; `demo/palettes/**` has zero structural enforcement; `no-unused-vars` is off.** (L/P4-4 + L/P5-8.) Verified: eslint.config.js:233-238/:275-276 glob `demo/@/**` (`ls demo/@` → absent); no `files` glob names demo/palettes; :185-186 both unused-vars rules off. **IDENTITY-FOLD → ColorSpaceSelector.md L-5(a) (lint-glob liveness, repo-wide, born-RED `eslint --print-config` command banked there) → X-W8.**
- **PP-19 · MAJOR — the demo type-checks against the stale published 4.0.0 tarball while Vite executes the local build; tsconfig declares three phantom specifiers and omits two real ones.** (L/P4-1 + L/L-11 + L/L-21.) Verified: real directory at 4.0.0 (`readlink` empty); tsconfig.demo.json:43-49 declares the un-exported root + `/parsing` + `/units` and omits `/css` + `/value`; package.json#exports = the closed 7-key set. **IDENTITY-FOLD → ColorSpaceSelector.md L-14 (exports↔tsconfig parity, born-RED generator command banked there) → X-W8.**
- **PP-20 · MAJOR — the published package declares glass-ui + keyframes.js as runtime dependencies and imports neither; the repo's own lint bans the src→glass-ui edge it forces on every consumer.** (L/L-12.) Verified: deps exactly `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}`; zero src imports (my grep); eslint inv-K-1 ban present. **BOOK → X-W9 (CC-008: the honest dependency surface ships with the 4.1 apotheosis).** Identity guard: if a library-band row later claims this fact, it is ONE identity homed here.
- **PP-21 · MAJOR — the local pane's editor lives inside the remote-browse mega-feature and is published in its "stable public API" seam; its only consumer is this pane reaching through `./browser/card`.** (L/P5-4.) Verified: file at browser/card/, re-exported at browser/index.ts, sole consumer PalettesPane.vue:41/:137-140. **BOOK → X-W7 (CC-006 domain split: the editor homes with the local library surface).**
- **PP-22 · MINOR — real tabbable buttons render inside the `aria-hidden="true"` teleported hover panel (WCAG 4.1.2 aria-hidden-focus); `inert` is the stated intent, `aria-hidden` the shipped mechanism.** (reader-D missed #5, promoted.) Verified: SwatchHoverMenu.vue:37-51 + the #actions fills (PaletteCardSwatches.vue:41-62, CurrentPaletteEditor.vue:46-54). **FOLD → PaletteCard.md PC-2/PC-3's booking (the hover panel dies with the specimen split, X-W7.c/d) — recorded here so the interim state is named.**
- **PP-23 · MINOR here (MAJOR as banked) — header count and grid emptiness bind two different truths 15 lines apart; a zero-match query wears first-run copy beside a live delete-all.** (D:P5-13.) Verified at :20/:62 vs :77. **IDENTITY-FOLD → PaletteCardGrid.md PG-5 (MAJOR; BLOCKER as booked at ES-1) — the sibling severity governs.** Reader-D's accname correction ratified: the aria-hidden Badge + sr-only companion mean AT hears "My Palettes (12 saved)" — correct.
- **PP-24 · MINOR — component instances held in a deep `reactive()` Record, never pruned (the `el &&` guard swallows the unmount-null).** (C:N-9 + C/N-4.) Verified at :84/:177; owner edict 7 applies. **IDENTITY-FOLD → PaletteCardGrid.md PG-8 (+ ActionFeedback.md AF-5 kinship) → X-W7.**
- **PP-25 · MINOR — three dead vue imports surviving a hard `--max-warnings=0` gate whose unused-vars rules are off; both import idioms (19 one-line `demo/ui` shims AND direct `@mkbabb/glass-ui/*` subpaths) seven lines apart in one script block.** (D:D-17/L-8 + L/L-6/L-19.) Verified: `watch`/`onMounted`/`nextTick` each occur exactly once (:128). **Shim extirpation IDENTITY-FOLD → the W-HYGIENE demo/ui row (ColorSpaceSelector.md L-4 lineage) → X-W8; the dead imports die free in any wave touching the file.**
- **PP-26 · MINOR — `defaultStore` is a shared mutable module constant handed by reference to useStorage AND returned by both corrupt-read recovery paths: first-visit saves mutate the "empty default" in place, and recovery hands back the polluted object.** (reader-L missed #4, promoted; verified at usePaletteStore.ts:8/:19/:26/:30 + the unshift sites.) Two-line cure (fresh object per read; factory default). **BOOK → X-W7, riding PP-6's store-validation cure.**
- **PP-27 · MINOR — N cards yield N identical "Palette menu" tab stops (no palette-name disambiguator, :100 verified sole site) and the only reorder affordance is an unnamed, unfocusable 16×16 SVG below the 2.5.8 target floor — reorder is mouse-only.** (D:P5-11 + D:D-9; source halves verified, the 34-stop live ordering stays U-6.) **BOOK → X-W7.c/d (the successor card names its menu per-entity and ships a keyboard reorder path per WCAG 2.5.7).**
- **PP-28 · MINOR — the search field's only label is its placeholder (vanishes on input; 3.3.2/2.4.6 quality).** (D:D-13's honest residual after K-2.) **BOOK → X-W7 (one forwarded `aria-label` today; the named field rides the ASK-D seated rung at the producer event).**
- **PP-29 · MINOR — `role="article"` is not a landmark role; the :2-4 comment's justification for the entity element carrying no interactive semantics is a misconception, propagated verbatim into challenge-C's cure text.** (reader-D missed #7, promoted; ARIA 1.2 landmark set verified by enumeration.) **BOOK → X-W7.c (the successor's semantics are chosen on true premises); flagged as a corpus-hygiene note against challenge-C-implementation.md:315.**
- **PP-30 · MINOR — the card barrel's PI-6 tree-shake claim conflates binding reachability with module side-effect reachability; no `sideEffects` field exists; two never-rendered SFCs' scoped styles ride into this pane's dev graph.** (L/P5-5; structure verified, byte counts U-8.) **BOOK → X-W8 (CC-007 barrels; PG-10 kinship).**
- **PP-31 · MINOR — the decorative "· empty plate ·" eyebrow is announced inside the `role="status"` region (implicit aria-atomic), unlike the correctly-hidden dot trio beside it.** (reader-D missed #6.) Verified: EmptyState eyebrow `<p>` unhidden; trio `aria-hidden`. **IDENTITY-FOLD → EmptyState.md ES-11 → X-W7 (its cure covers the `empty-eyebrow` prop this pane supplies at :78).**
- **PP-32 · INFO — `.pane-scroll-fade` on this pane's root is defined in a descendant's unscoped `<style>` and fades nothing; the name is a misnomer surviving a retired mechanism (M-33 ruled the fade-exists claim FALSE — not carried).** (D:P5-10 + L/P5-6, reader-D's INFO demotion ratified: PaneHeader.vue:41-53 documents the class as the scroll-timeline HOST.) **Rider that outranks the row: M-34 (TRUE) — the scroll-driven title shrink runs byte-identically under `prefers-reduced-motion` on all nine host panes including this one; no pass-5 axis filed it here. → X-W10 (motion canon owns the guard).**
- **PP-33 · INFO — the ramp fallback triple is byte-duplicated between call site and root recipe.** (D:P5-12's surviving residual; already carried as D-7/L-7, edict 5 — recorded, not re-booked.)
- **PP-34 · INFO — `class="capitalize"` on the pre-capitalised title is inert and constrains future copy.** (reader-D missed #10; verified :15.) Dies free in any X-W7 pass over the file.

## Killed-claims register

- **K-1 · D:P5-1 (BLOCKER, the ramp fan)** — killed twice over, convergently: (i) reader-D's gamut solve — the two lower stops sit ON the sRGB cusp at the certified L to six decimals; the prescribed iso-chroma ≥180° cure computes to C≈0.082, a 57% cut; (ii) reader-L's provenance kill — the challenge truncated the docblock mid-sentence, cutting exactly the line that names "rainbow" and the deliberate reconciliation. This seat verified the ruled-form clause ("STANDS UNTOUCHED… the owner rules the register", palettes-ramp.ts:49-58) and the full sentence at :75-78. Residual: an owner-relay observation, not a row.
- **K-2 · D:D-13 (MAJOR, "no accessible name")** — killed by the axis's own telemetry (placeholder = computed accname per HTML-AAM). Residual PP-28 (MINOR).
- **K-3 · D:P5-12 (drift limb)** — category error: a dynamic per-seed token can never equal a static literal; the fallbacks are structurally required by the :172-174 alias indirection. Residual PP-33 (INFO, already carried).
- **K-4 · D:P5-3 (seed-tint limb)** — the measured chroma is the permitted ambient field seen through a 66.4%-alpha unblurred plate; the finding requires a distinction its instrument cannot make. Elevation limb → U-1.
- **K-5 · L/P5-1-framing (export BLOCKER promotion)** — the "violated clause" governs ExportSnapshot serializers (and sits at :172, not :174); the legacy seat is named pre-contract, W50-replaceable, by the contract tree itself. The code defect survives as PP-8 (MAJOR).
- **K-6 · C/P-1's mechanism ("casts instead of parsing")** — false against the tree: read() parses and validates `version`; the hole is the unvalidated `palettes`. The pass-4 cure would have shipped the whiteout unchanged. Finding survives re-mechanized as PP-6.
- **K-7 · D/P5-4's dead-guard sub-claim ("0.5 unreachable; edict-2 masking fallback")** — mis-scoped domain: the 1–50 bound is the export-snapshot contract nothing constructs; the local render path is uncapped (types + store, my reads), so the guard is reachable at n > 200. The two-floors observation survives inside PP-14.
- **K-8 · C:N-6's BLOCKER severity ("one save click on the LAN origin destroys the colours")** — the demonstrated trigger is the dev server (`host: true` inside the dev-only else branch, my read); shipped origins are HTTPS secure contexts. Survives rescoped as PP-7 (MAJOR sequencing + MINOR dev hardening).
- **K-9 · reader-L missed #1 (live dead "Add to current palette" button on every saved card) — NEW KILL, this seat** — `v-if="!isLocal"` + `:is-local="palette.isLocal"` + this pane renders only local palettes: the button never mounts here. Residual: dead `addColor` wiring + BrowsePane asymmetry, swept by PP-17.
- **K-10 · reader-D missed #4 (mobile edit stranded with no Save/Cancel) — NEW KILL, this seat** — Dock.vue:72/:111's `mobile-edit` layer is the exact ≤1024px complement, auto-activating off `editTarget`; already sealed at CurrentPaletteEditor.md K-10/S-9. A reader filing against the registry it did not read.

## Superlatives (provenance preserved; this seat's confirmations noted)

1. **The ramp resolver's feasibility-aware cusp walk** (reader-D, arithmetic its own): measured stops agree with the theoretical sRGB gamut ceiling to six decimals on two hues — the D axis measured precision and filed it as a defect. The audit's most instructive self-indictment. (This seat verified the resolver doctrine at source; the solve itself is reader-derived.)
2. **K-PALID id-honesty** (reader-L; store read confirmed by this seat): `Palette.id` is an honest optional paid for with a real type predicate (usePaletteStore.ts:51-55), and every downstream guard names both the invariant and the dishonest code it replaced (usePaletteActions.ts:88-97).
3. **The count Badge** (both readers + four prior passes; :16-25 confirmed): aria-hidden numeral + sr-only "( N saved)" companion, v-if duplicated on both so the sr-only text can never orphan.
4. **The delete-all dialog** (reader-D; :102-122 confirmed): title + count-exact pluralised description + storage + irreversibility, Cancel first, and the destructive arm speaks real Glass 7 (`emphasis="text"` / `tone="destructive"`) — the one control group in the file using the shipped vocabulary is the one nobody hand-tuned. (Its focus-return failure is PP-15; both things are true.)
5. **PaletteCardGrid's first-child comment** (both readers; confirmed): the multi-root/$el trap documented at the exact line where the invariant lives, consumer named. Simultaneously PG-6's evidence and good engineering.
6. **EmptyState's two species** (reader-D; :1-28 confirmed): role="alert" vs role="status", the error register dropping every decoration, rationale written down.
7. **export/svg.ts as the counter-example** (reader-L; header + xmlEscape routing confirmed): the certified implementation is correct in every particular the shipping one gets wrong; the defect is that PalettesPane mounts the other one (PP-8).
8. **usePaletteActions' save-P0 repair record** (reader-L; :65-77 confirmed): the fix carries the restored contract AND the inverted one it replaced.
9. **vite.config's generated self-alias set** (reader-L): structurally incapable of drifting from the exports map — precisely the class of defect PP-19 found everywhere the mechanism was NOT used.
10. **The clean hostile check recorded to prevent re-litigation** (reader-D): PaletteCardMenu's Delete `@click` fires from the keyboard via reka-ui MenuItem's SELECTION_KEYS `.click()` — cosmetic inconsistency, not a keyboard-dead control.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **U-1** · D:P5-3 elevation sign-flip (+0.0246 L light / −0.1764 L dark vs host) — color-mix chains against a live accent; not statically decidable.
- **U-2** · D:P5-2/P5-8's composited pixel identities (rgb(233,225,217)/rgb(66,55,47) across the three wells; the WHCM substitution table) — source mechanisms confirmed (PP-13/PP-11); pixels are UA/chromium-cell.
- **U-3** · D:P5-7's exact focus-ring composite (reader-D's corrected 1.89:1) — the token arithmetic is static; the plate underneath is sampled. The verdict (< 3.0) does not turn on it.
- **U-4** · PC-7's WHCM/print renders (nobody has ever printed this card) — inherited from PaletteCard.md U-3.
- **U-5** · D:P5-5's 458×56 metadata row vs 40px specimen (1.40:1) — structural halves source-true; rendered type metrics live.
- **U-6** · D:P5-11's 34-stop tab ordering (the 12 identical stops are source-certain, PP-27).
- **U-7** · D:P5-10's crop geometry (visiblePx 5/100; the 2px scrollbar is a harness artifact — no authored scrollbar sizing exists).
- **U-8** · L/P5-5's dev network log + L/P4-2's 262,662-byte co-location cost; **U-9** · L/P5-7's 698-byte prod app chunk (out of this component's axis per CARRY-LEDGER §F — the standing gh-pages prod-preview probe owns it).
- **U-10** · C:N-6's LAN repro end-state numbers (appHtmlLen collapse, panesOnScreen 0) — mechanism source-confirmed (PP-7), end-to-end sequence live, dev-only.
- **U-11** · D:P5-14 RTL strip mirroring vs the export's ordinal claim (rests on PALETTE-CONTRACT Appendix W51, unopened by either reader or this seat).
- **U-12** · whether a zero-match query's screen truly leaves delete-all as the ONLY control (SearchBar ships no clear affordance per reader-D's dist read; unmeasured) — rides PG-5's cure regardless.

## Closing verdict

**ADJUDICATED — GRAVE, WITH ITS SPINE ALREADY BANKED**: 34 confirmed-family rows (6 BLOCKER · 15 MAJOR · 10 MINOR · 3 INFO); the five heaviest defects are real, source-certain, and 13 of 34 rows identity-fold to sibling adjudications rather than re-book; 10 claims killed with written refutations (two of them this seat's own kills of reader missed-finds — both readers filed against a registry they did not sweep); 12 live-only residue rows hand to the visual audit; every surviving row carries exactly one terminal disposition into X-W1/W4/W5/W7/W8/W9/W10, CC-044 (BLOCKED-ON CC-003), or the standing glass-ui BH relay — nothing stamped VERIFIED, nothing lost.

— end of apotheosis. The pass-5 challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
