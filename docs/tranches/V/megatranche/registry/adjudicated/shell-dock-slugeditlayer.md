Served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited.

# shell-dock-slugeditlayer — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: fresh-Fable adjudicator (incredulity seat) · **Date**: 2026-08-08 · **HEAD**: `c654824e` · branch `tranche-u`.
**Corpus**: three challenge axes (`challenge-D-design.md`, `challenge-C-implementation.md`, `challenge-L-library.md`) against `demo/shell/dock/layers/SlugEditLayer.vue` (119 lines) and its port/auth/router chain. All three are r1 EXISTS-ORIGINAL (HYDRATION-LEDGER area-shell pass 1); **no `-r2.md` exists for this component** — the challenge dir holds exactly the three axes + `evidence-slug-layer-open-1024.png` (verified by this seat's `ls`), so the r1 files govern and no AXIS-PROMOTIONS row applies.
**Re-readers adjudicated**: RR-DU (design-and-usage) · RR-LC (library-and-code). Both were attacked before either was adopted: every verdict-level disagreement between them was re-derived against the tree with this seat's own instruments, and one of RR-DU's kills is itself killed below.

**This seat's own instruments (all static + node; zero browser probes — probe parsimony)**: full reads of `SlugEditLayer.vue` (whole), `useSlugMigration.ts` (whole), `usePalettePorts.ts` (whole), `useUserAuth.ts:40-134`, `useViewManager.ts:1-100`, `viewSchema.ts:1-60`, `router/index.ts` (whole), `Dock.vue:55-158`; **node run against installed vue-router 5.1.0** (memory-history router + this repo's route shape: `router.push({name:"saved"})` → `SYNC THROW: Error: No match for {"name":"saved",…}` — the A-1 decider); **independent recomputation over `docs/tranches/V/megatranche/audit/visual/REPORT.json`** (60 results / 398 total `smallTapTargets` rows / eight slug shapes at exactly 30 each = 240 — the C-5 decider); producer bytes read whole-rule: `dist/components/dock/styles/controls/{touch-floor,icon-button}.css`, `overflow.css` coarse block, `density.css` all size buckets, `adaptive-legibility.css` head, `layers.css` `.dock-layer` gap rule (the L-4/D-7 decider); `demo/styles/foundation.css:660-725` + its import graph (root sheet, unlayered forced-colors block — the D-10 decider); censuses re-run: shell→palettes edges (5), palettes→shell back-edge (1, type-only), `<input` sites (4), `SlugEditLayer|Switch to slug|…` across test/+e2e/ (0), `slugBarRef` (3 sites, one file, never assigned), `PaletteSlugBar` reaches (type-import + 2 barrels + 1 prose), `palettes/browser` barrel importers (0), eslint.config.js `project|projectService` (absent), package.json#exports (7 keys, no root) vs tsconfig.demo.json:42-49 (8 entries incl. `/parsing` + `/units` phantoms); provenance spot-checks: motion-quarantine.md slug rows (none; M-32 = actiontoolbar), `animations.css:184-193` PRM guard (verbatim), S-audit `glassui-consume-map.md:165/:208` (verbatim, names this file), A-audit `HARDEN-5-coverage-gaps.md:41-48` (verbatim, names `:21`).

## Scoreboard

**35 adjudicated rows: 5 BLOCKER · 13 MAJOR · 12 MINOR · 5 INFO.** 14 of them are net-new (missed by all three challenge axes, surfaced by the re-readers, premises re-verified here). **14 killed-register rows**, including one kill-of-a-kill (RR-DU's refutation of C-5's arithmetic is itself refuted by this seat's recomputation) and one re-reader assertion killed by this seat's own empirical run.

Blocker-weight headline: **A-1** — every submit path of the component's primary action routes `setActiveTab("saved")` through an unchecked `as ViewId` cast into `router.push({name:"saved"})`, a named route that does not exist; the push **throws synchronously** (this seat's own run against installed vue-router 5.1.0). Three challenge axes and one re-reader quoted the surrounding lines and missed it.

## Ruled disagreements (the incredulity seat's actual work)

| id | RR-DU | RR-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-5 arithmetic** | KILLED it: "458 rows / 61 captures / 52.4%" | CONFIRMED exact: 398 / 60 / 240 = 60.3% | **RR-LC wins — RR-DU's kill is KILLED** | My recomputation over REPORT.json: `results` length **60**, every one carrying a `smallTapTargets` array; total rows **398**; the eight slug shapes (`input 160×23`, `input 160×20`, Switch-to-slug/Generate-new-slug/Cancel at 22×22 and 23×23) at **exactly 30 each = 240 = 60.3%**. RR-DU's "self-contradiction" complaint also fails: each capture holds 4 slug rows (input + 3 buttons), so "four rows × 60 captures = 240" is coherent — desktop matrices carry the 22×22/160×23 shapes, mobile the 23×23/160×20. C-5's headline is restored as authored. |
| **L-4 coarse floor** | Killed the "no floor inside glass-dock / doubly exempt" sub-claim; floor rides the size token | Confirmed L-4 **including** the "doubly exempt from the dock's own coarse-pointer floor" phrasing | **RR-DU wins; RR-LC's confirmed-row phrasing corrected** | My bytes read, whole-rule: `overflow.css` `@media (pointer: coarse) .glass-dock[data-size] { --dock-control-floor: var(--dock-touch-target, 2.75rem); --size-icon-btn: var(--dock-touch-target, 2.75rem); … }`; `density.css` every bucket: `--dock-control-size: max(calc(… * var(--dock-scale)), var(--dock-control-floor, 0px))`; `icon-button.css` `.dock-icon-button { width/height: var(--dock-control-size, var(--size-icon-btn)) }`. A NON-compact control inside `.glass-dock` therefore **does** get ≥44px on coarse. `touch-floor.css`'s `:not(:where(.glass-dock *))` exempts the dock precisely because the dock has the floor by token; the sole opt-out is `.dock-icon-button--compact { width/height: var(--dock-compact-control-size, auto); min-width: var(--dock-compact-control-min-width, 0) }`, which overrides the token rule later in the same file. D-7's premise ("producer default would give ≥44px on coarse") is CORRECT. |
| **D-7 cure ("delete `compact` three times")** | Structurally sound | KILLED: aggravates D-4's measured overflow (295 vs 290 @390, vs 234 @320) on the very pointer class it targets | **Both partial — cure direction SUSTAINED, wording RESCOPED** | The floor is real (above), so shedding `compact` is the only consumer-side road to it. RR-LC's width arithmetic is also real: on coarse each control becomes ≥44px (+~66px across three), and D-4's aperture numbers — though UNPROVEN-NEEDS-LIVE — are directionally supported by the producer's own overflow machinery existing at all. Adjudicated cure: delete `compact` ×3 **paired with a row diet** (the 16px decorative `LogIn` glyph dies per A-4/D-11; `w-40` re-derived) and a live witness of the `[data-dock-overflow]` scroll-track degradation at 320/390 (residue row). A cure that re-treats the symptom by keeping `compact` and hand-setting `--dock-compact-control-size` to the floor is equivalent and acceptable; L-4's padding-hoist cure stays dead (K-3). |
| **L-2(b)/D-1.2 premise** ("the callee cannot reject / catches internally") | Carried it inside its C-2 row: the admin branch "cannot throw synchronously" | KILLED the premise; found A-1 underneath | **RR-LC wins — twice** | My own empirical run: `router.push({name:"saved"})` **throws synchronously** in installed vue-router 5.1.0. Chain re-read whole by this seat: `useSlugMigration.ts:55/:69/:76 setActiveTab("saved")` → `usePalettePorts.ts:88 (tab: string) => depsSwitchView(tab as ViewId)` (unchecked cast — why vue-tsc is green) → `useViewManager.ts:78 router.push({name: id, query})`; `ViewId` (viewSchema.ts:35-49) = 14 members, no "saved"; router/index.ts:21-37 = 14 named routes, no "saved"; the :37 catch-all is a PATH redirect that named pushes never match. The throw lands inside an `async` body → rejection; the layer's `catch` (:57) stays unreachable (C-2 stands) — but the corpus reported a dead catch over a *silent* callee, and the truth is a dead catch over a callee that **throws on every submit**. RR-DU's parenthetical is killed (K-14). |
| **D-5 mechanism** (inert-phase crossfade; desktop-never/mobile-always) | KILLED: C-7's own samples show `inertAncestor: null, faceActive: true, manualFocusWorks: true`; real mechanism = reka-ui dropdown focus-restore race; failure is a cold-open race, not deterministic | Concurs the two seats contradict; outcome UNPROVEN-NEEDS-LIVE | **RR-DU's kill SUSTAINED; outcome stays residue** | The structural mechanism is certain from source (SlugEditLayer.vue:16-23: one `defineModel` write + one unretried `nextTick` focus, entered from a reka-ui `DropdownMenuItem` via `ProfileSection.vue:80` → `Dock.vue:212/223` → `:61`). The inert-phase *cause* is refuted by the sibling seat's own data; the per-platform *outcome* cannot be settled statically and both sampling runs must be re-executed before either timeline is carried (residue). The close half — no focus restore, no clear — is statically certain and CONFIRMED (C-7/D-5 row). |
| **D-3 severity** | BLOCKER | MAJOR | **MAJOR** | Grading consistency: the program grades AA-failure geometry/indicator rows MAJOR (D-7/L-4 here; the SC 2.5.8 adjudication INFO). D-3 blocks discoverability, not the flow — the destructive/irreversible rows hold the BLOCKER register. The compounding with A-4 (placeholder ink ≡ value ink) is noted on both rows: one `Input`-primitive migration cures D-3, C-6, A-4 and L-5 together. |
| **L-1 severity** | MAJOR (demoted; double-counts L-2(d)) | MAJOR (demoted; twin reaches the bundle through nothing) | **MAJOR — unanimous demotion ratified** | Mount census re-run by this seat: `PaletteSlugBar` reaches = 1 type-only import (erased by `verbatimModuleSyntax`) + 2 barrel re-exports + 1 spec prose line; the `palettes/browser` barrel itself has **zero importers** (my grep, → A-9). Zero shipped behaviour = maintainability MAJOR; its one live consequence (the null `slugBarRef` sink) is already charged in D-1/C-3/L-2(d). |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `c654824e`; re-resolve before executing any cure. Net-new rows carry A-ids with re-reader provenance; original axis ids are preserved for life.

### BLOCKER

- **A-1 — BLOCKER (net-new; RR-LC).** Every submit path of the primary action throws on a phantom named route. `useSlugMigration.ts:55/:69/:76` `setActiveTab("saved")` → `usePalettePorts.ts:88` unchecked `tab as ViewId` → `useViewManager.ts:78` `router.push({name:"saved"})` → **synchronous throw** (this seat's own node run against installed vue-router 5.1.0; 14 named routes, no "saved"; the catch-all is a path redirect). Per branch: (a) **admin** (:52-57, no try/catch) — the throw lands AFTER `clearUserSlug()` + `adminLogin()`, surfacing as an unhandled promise rejection while SlugEditLayer.vue:55-56 still clear and close; (b) **direct slug** (:74-88) — a SUCCESSFUL `userLogin()` (slug + token already persisted) is converted by the :77 catch into `slugBarRef.value?.setError("Login failed")` at a permanently-null ref, and the view never switches; (c) **migrate** (:61-70) — swallowed at :112-114 as `console.warn`. The residue is legacy: `activeTab: Ref<string>` + the :24 else-fallback are the old pre-router tab model "saved" belonged to. Cure sketch: the tab-name legacy dies (`setActiveTab("palettes")` or delete the concept), and the `as ViewId` cast at :88 becomes a typed parameter so the hole is unspellable. **Disposition: NO-WAVE-OWNER — next formation boundary, first pick (live, reachable-on-every-submit).**
- **C-1 — BLOCKER.** The classifier's unknown bucket is the destructive branch: `SlugEditLayer.vue:46` `isAdmin = !looksLikeSlug(normalized)` routes anything that is not four lowercase hyphenated words into `clearUserSlug()` + `adminLogin(<garbage>)` (`useSlugMigration.ts:52-57` → `useUserAuth.ts:129-133` → `:44-48`: slug ref nulled, `palette-user-slug` removed, token cleared). The slug is the only account handle; a typo destroys it. Boundary case re-traced by this seat from the read bytes: a lone `"` passes the :41 guard (tests `raw`), normalizes to `""`, lands admin — session destroyed without elevation (`isAuthenticated = !!adminToken` stays false on `""` — RR-LC's precision adopted). The happy path is exact by construction (server mints `adj-verb-color-animal` from 512 all-lowercase words — RR-LC's verification, spot-trusted with attribution). **Disposition: NO-WAVE-OWNER** — the classifier must never default to the destructive/elevated branch (explicit mode or server-verified dispatch); pairs with A-7.
- **C-2 / D-8 / L-2(a) — BLOCKER.** Fire-and-forget: the `async` submit body (:39-66) contains zero `await`; :42 sets `slugSwitching`, :54 fires the port bare, :55-56 clear + close, :63-65 reset — one synchronous run, so Vue never paints the spinner (:97) or the disabled guard (:95), and the `catch` (:57) is unreachable by construction (async callee → rejection). The UI advances on invocation, not outcome. **Disposition: NO-WAVE-OWNER** — await the port, gate the close on the result; the decorative states become real or die.
- **D-1 / C-3 / L-2(c)(d) — BLOCKER.** The error surface is dead twice over: `slugError` is written six times (:18, :43, :49, :59-62) and rendered zero times (template :75-119, full read); the port's parallel sink `slugBarRef.value?.setError(...)` (`useSlugMigration.ts:84-87`) is created at :30, returned at :121, and **assigned nowhere** (my grep: 3 sites, one file). The synchronous, reachable worst case: :48-52 sets "Already signed in." and returns — pressing submit on your own slug does literally nothing. Canon: VISUAL-CONSTITUTION.md:83 (state + associated error explicit) — verified verbatim by both re-readers. **Disposition: the error surface's terminal home is the W23 Account Dialog (constitution-assigned, VISUAL-CONSTITUTION.md:222); interim NO-WAVE-OWNER — render the state that exists.**
- **D-2 — BLOCKER (desktop-only).** The dock's open-hold predicate is one disjunct short: `Dock.vue:86` `shouldKeepOpen = actionBarLayerActive || anyEditActive || isAnyOpen` omits `slugEditMode` (:59); `:132` `:collapse-delay="5000"` (`always-expanded` on mobile), so the 5-second idle collapse runs mid-typing; the :76 watch is a cleanup written instead of a hold, and reopening runs `:17` `slugInput.value = ""` — the typed slug is discarded. Cure verified against the producer by both re-readers (dock.js: the collapse timer is blocked exactly by the ref-counted keepOpen count that `Dock.vue:87` drives) and the predicate/watch lines re-read by this seat. One-word repair: add the disjunct. Wall-clock numbers stay residue. **Disposition: NO-WAVE-OWNER.**

### MAJOR

- **D-3 + C-6 — MAJOR (demoted from RR-DU's BLOCKER — ruled above).** The field is optically absent in every state including focus: `:85` `bg-transparent border-none outline-none` with no `<style>` block, no ring/shadow utility, and nothing anywhere that restores an indicator (re-readers' searches of foundation.css/focus-ring.css/glass-ui.css, ratified); and its only naming affordance, the 22-char placeholder, provably cannot fit the hard `w-40`=160px box at any value of the fluid mono token (0.6em advance × 22 ≥ 184px at the smallest measured arm — arithmetic certain without a browser; the in-repo capture shows the clip). WCAG 2.4.7 in the normal register; the WHCM register is the one place it IS distinct (see K-6). **Disposition: NO-WAVE-OWNER — the glass-ui `Input` migration (third booking of the same identity: S-audit glassui-consume-map:165/:208, verified verbatim by this seat) cures D-3, C-6, A-4, and L-5 in one move.**
- **D-6 — MAJOR.** One undifferentiated `type="text"` field carries two authorities and renders the elevated one unmasked in the persistent top band; no `autocomplete`/`spellcheck`/`inputmode`/`name`; the only admission of the token role is the clipped-off placeholder tail. Canon assigns secret retention a designed treatment (VISUAL-CONSTITUTION.md:222, verbatim-verified). **Disposition: W23 (constitution-assigned Account Dialog owner); else NO-WAVE-OWNER at boundary.**
- **D-7 / L-4 / C-5 — MAJOR.** All three controls pass `compact` (:92, :105, :113) — the sole opt-out from the producer's coarse floor (my bytes ruling above) — and the sole unremediated T-36 survivor of the four dock `compact` sites (ActionBarToggle carries the per-instance token rider). The component contributes **240 of the corpus's 398 sub-24px tap-target rows (60.3%) — recomputed by this seat**; the always-mounted layer (Dock.vue:148-150, no `v-if`) charges it on every route. Adjudicated cure per the D-7 ruling above (delete `compact` ×3 + row diet + live overflow witness). **Disposition: consumer half NO-WAVE-OWNER; producer half → glass-ui BH relay with the CORRECTED ask — non-zero `--dock-compact-control-size`/`--dock-compact-control-min-width` defaults; the touch-floor `:not`-clause drop is struck from the relay text (K-2).**
- **L-3 — MAJOR.** The shell reaches identity through a feature at five edges (my grep: DockViewSelect.vue:8, Dock.vue:18, SlugEditLayer.vue:5, MobileMenuDropdown.vue:13, ProfileSection.vue:14 — all `SESSION_PORT_KEY`) and `demo/palettes` points back at `demo/shell` once (usePalettePorts.ts:19, type-only, runtime-erased — L-3 stated this correctly). Nothing in `sessionPort` (:127-135) is palette-specific; `demo/platform/auth/` already owns the concept. **Disposition: NO-WAVE-OWNER — move the session port to a platform/shell-neutral home; the back edge dies with it.**
- **L-1 — MAJOR (demoted — ruled above).** `looksLikeSlug`/`normalizeTokenInput` character-identical in two SFCs; the 243-line twin (`PaletteSlugBar.vue`) is mounted by nothing (census re-run by this seat). The submit/error block is NOT byte-identical (K-9). **Disposition: NO-WAVE-OWNER — delete the twin + its barrel lines; the shared grammar gets one home; executes with A-9.**
- **L-5 — MAJOR.** A raw, unlabelled, unconfigured `<input>` — the fourth parallel field idiom (my census: 4 sites) and the only dock/identity field with no accessible name; booked twice before and never closed (S-audit rows verified verbatim by this seat). **Disposition: NO-WAVE-OWNER — same identity as the D-3 Input migration; the S-audit P1 row is the booking of record (anti-rename).**
- **C-14 — MAJOR.** Zero coverage of any kind (my census: 0 hits across test/ + e2e/); the one oracle sweeping this DOM (o15:141) asserts only `[title]` count and declares the layer out of scope (:147-150, read by this seat). C's named green-keeping mutation (`looksLikeSlug` → `return false` routes every correct login into account destruction with all gates green) is ratified as the sharpest statement of the gap — and A-1 is its empirical vindication: a defect on every submit path lived under a fully green wall. **Disposition: NO-WAVE-OWNER — born-RED tests for the classifier + the route dispatch land with the A-1/C-1 cures.**
- **C-7 (mechanism) + D-5 (close half) — MAJOR.** Open-focus is one unretried `nextTick` racing a reka-ui dropdown's asynchronous focus restore (mechanism adjudicated above; per-platform outcome = residue). Close restores nothing and clears nothing (:86, :115 — statically certain). Booked three tranches ago (A-audit HARDEN-5:41-48, verified verbatim by this seat: "No focus trap, no focus restore-on-close", naming `:21`). **Disposition: NO-WAVE-OWNER; the overlay focus contract arrives whole with the W23 Dialog if that is the terminal home.**
- **D-13 — MAJOR.** Constitutional orphan: canon assigns Account to a modal side Dialog owned by W23 with W15 supplying auth state (VISUAL-CONSTITUTION.md:222, verbatim-verified by both re-readers); this surface is a 46px inert-able strip of navigation chrome. The §7-Shell supporting citation is struck (K-10); the row stands without it. **Disposition: W23 (constitution-assigned); else NO-WAVE-OWNER at boundary.**
- **A-2 — MAJOR (net-new; RR-LC).** The component's second async control is bare and uncatchable: `:107` `@click="slugEditMode = false; pm.onRegenerateSlug()"` closes the layer first, then fires un-awaited into `useSlugMigration.ts:91-104`, which has no try/catch on either limb; the else limb `await deps.userRegenerate()` reaches `useUserAuth.ts:112-127`, which throws on transport failure and explicitly at :124. Result: unhandled rejection, no signal anywhere, layer already gone. Verified by this seat's full reads. **Disposition: NO-WAVE-OWNER — lands with C-2's await discipline.**
- **A-3 — MAJOR (net-new; RR-LC).** `regenerate()` clears before it has a replacement: `deleteSession()` → storage + token cleared (:119-120) → only then `createSession()` (:123); on failure the old session is revoked and the old slug/token gone from storage while the in-memory ref still renders it (:118 comment, deliberate) — one refresh loses the account. `register()`/`login()` persist atomically; this is the sole clear-before-replace path, and A-2 makes its failure invisible. Verified by this seat's read. **Disposition: NO-WAVE-OWNER — create-before-clear reorder in `platform/auth`.**
- **A-4 — MAJOR (net-new; RR-DU).** The placeholder's muting is nullified inside the dock: `:85` `placeholder:text-muted-foreground`, but the producer re-pins `--muted-foreground: var(--foreground)` inside `:where(.glass-dock)` (`adaptive-legibility.css`, read by this seat; the `contrast-color()` upgrade path pins it to the same full-contrast ink). Placeholder ink ≡ value ink: with no boundary (D-3) there is no cue of any kind separating an empty field from a populated one; the in-repo capture corroborates. A producer documentation gap, not a producer defect. **Disposition: NO-WAVE-OWNER — cured for free by the `Input` migration; a border alone would not cure it.**
- **A-5 — MAJOR (net-new; RR-DU).** A destructive, irreversible identity action ships as an icon-only 22px control 28px from Cancel with no confirmation, no pending state, no result, no undo: `:104-110` "Generate new slug" → when `savedPalettes.length === 0`, `useSlugMigration.ts:101-103` falls straight to `userRegenerate()` with no dialog (read by this seat) — the account handle is replaced on one mis-tap beside the dismiss control. C-1 charges the typo route into destruction; D and L charged the geometry but never asked what the button does. The menu copies of the same call at least carry text labels. **Disposition: NO-WAVE-OWNER — confirm affordance; journey belongs to W23 when formed.**

### MINOR

- **D-12 / L-8 / C-4 — MINOR.** `:57-62` re-authors the `catch (e:any)` + `"409"/"404"/"429"` substring ladder that `useSlugMigration.ts:78-83` documents as ruled dead (S.W2 W2-6) and cures with the typed `ApiProblem.status` one file away — legacy that survived its own migration. **Disposition: NO-WAVE-OWNER — dies with the C-2/D-1 rework; see A-13.**
- **C-8 — MINOR.** Escape dismisses only when the input holds focus; `.stop` (:86) starves any ancestor fallback; the layer has four focusables, two outside the `<form>` (multi-root template). **Disposition: NO-WAVE-OWNER.**
- **C-9 / C-10 / L-6 / C-11 / L-7 — MINOR.** `onCopySlug` misfiled into the edit layer, reached by a four-hop imperative round-trip (`ProfileSection.vue:77` → `Dock.vue:213/224` → `:62` → `:68-70`) into a component that is inert at the call moment, while both menus already inject the port themselves; the `Promise<CopyResult>` is discarded with `void` (the adjacent menu row renders a Copied! affordance — asymmetry inside one menu); `defineExpose` publishes `slugSwitching` (:72), which nothing reads and which C-2 proves can never be observably true. **Disposition: NO-WAVE-OWNER — the copy action moves to its callers; the expose dies.**
- **C-12 — MINOR.** A pasted admin token survives dismissal in the always-mounted DOM: `slugInput` is cleared only on open (:17) and successful submit (:55) — never on Escape (:86) or Cancel (:115). Given :29-37's explicit `ADMIN_TOKEN=` handling, the retained value can be a live credential. **Disposition: NO-WAVE-OWNER.**
- **D-11 + A-6 (ink half; net-new, RR-DU) — MINOR.** Register incoherence on both axes: the decorative `LogIn` (:80, 16px, no `aria-hidden`) outweighs the three operable 14px glyphs in size AND in ink — the operable glyphs paint at `--opacity-icon-muted` = 0.8 of foreground (`icon-button.css` color-mix, read by this seat) while the decoration paints full foreground (A-4's token chain). Cross-layer glyph ladder 14/16/20/24px; `aria-label="Cancel"` (:114) collides with `Dock.vue:144` "Cancel edit"; the dead twin's refinements (`aria-hidden` on glyphs, state-bearing submit label) were all dropped in transplant. **Disposition: NO-WAVE-OWNER.**
- **L-9 — MINOR — IDENTITY GUARD.** tsconfig.demo.json#paths vs package.json#exports: recomputed by this seat — exports = 7 keys no root; paths = 8 entries incl. phantom `/parsing` + `/units`; `/value` + `/css` exported but absent from paths. **Fact-identical to the ColorSpaceSelector adjudication's L-14** — ONE identity, already FOLDed to W-HYGIENE config parity with a born-RED generator command. **Disposition: W-HYGIENE (existing booking; anti-rename — never re-booked under this corpus's id).**
- **A-7 — MINOR (net-new; RR-LC).** The classifier also misroutes in the inverse direction: `isAdmin` derives from the lowercased form (:45-46) but a mixed-case token shaped like four hyphenated words (`Abc-Def-Ghi-Jkl`) classifies as a slug and posts the credential (lowercased, :54) to the user-login endpoint — an elevated secret leaked into the wrong request. Re-traced by this seat from the read bytes. **Disposition: NO-WAVE-OWNER — dies with C-1's classifier redesign.**
- **A-8 — MINOR (net-new; RR-LC).** No gate can EVER catch this file's floating-promise family: eslint.config.js declares no `project`/`projectService` (my grep — absent), so `no-floating-promises`/`no-misused-promises`/`await-thenable` are structurally UNAVAILABLE, not merely off. The single mechanism under C-2, A-2, and the fleet-wide idiom. **Disposition: NO-WAVE-OWNER — repo-wide lint-program decision, its own row at the boundary.**
- **A-9 — MINOR (net-new; RR-LC).** `demo/palettes/browser/index.ts` — a self-described "top-level public seam" with ZERO importers (my grep; all live reaches go to sub-barrels) — is half of what keeps the dead twin nominally reachable. L-1's cure is incomplete without deleting the unimported barrel itself. **Disposition: NO-WAVE-OWNER — executes with L-1.**
- **A-10 — MINOR (net-new; RR-DU).** The accessible names are inaccurate, not merely terse: "Switch to slug" (:94) names the one path the input frequently will not take (everything else escalates privilege, C-1); "Generate new slug" (:106) and the menu's "Regenerate slug" name the same destructive call two ways, neither disclosing destruction; the donor's state-bearing submit label was dropped. **Disposition: NO-WAVE-OWNER.**
- **A-11 — MINOR (net-new; RR-DU).** The mode has no name anywhere in the accessibility tree: the `<form>` carries no label/role, the host `DockLayer id="slug-edit"` passes none, and activating the layer swaps the entire nav content with nothing announced — even a won focus race delivers only a truncated placeholder. Distinct from C-7/D-5 (where focus lands). **Disposition: NO-WAVE-OWNER.**
- **A-12 — MINOR (net-new; RR-DU).** `:77` `gap-1.5` hard-codes the producer's desktop rhythm: `.dock-layer` gap = `var(--dock-layer-gap)` (verbatim in `layers.css`, my read) contracts under `--dock-scale` on coarse (~4.7px at 0.78) while the form's 6px stays fixed — the row's two halves desynchronize exactly where the dock is most constrained. The equality at desktop scale 1 is why D's proportion pass recorded "sound". **Disposition: NO-WAVE-OWNER — read the token.**

### INFO

- **L-4-adj (SC 2.5.8) — INFO, carried forward.** The three controls pass WCAG 2.2 SC 2.5.8 via the spacing exception (centre distances 47/75/28 ≥ 24 — distances residue, exception-logic sound); MT-F004's flat 24×24 premise is over-stated at this site; the defect underneath is design-system-level (D-7/L-4), not an AA failure. Carried so no later seat re-charges MT-F004 here. Both seats declined to inflate a handed premise — see superlatives.
- **C-13 — INFO.** `:6` imports `writeClipboard` from the root barrel while `:4` uses the narrow `/dock` path and `./dom` exists in the producer's exports. **Disposition: NO-WAVE-OWNER, fleet-level sweep.**
- **L-10 — INFO (harness defect, not component defect).** The visual corpus measured this component only inert-at-opacity-0 (the uniform 30-per-shape fingerprint — corroborated by my recomputation; capture.mjs filters on nonzero rect with no visibility/inert check per RR-LC's read). The only active-state frame anywhere is the C seat's own `evidence-slug-layer-open-1024.png`. **Disposition: NO-WAVE-OWNER — the harness gains a visibility check; the active state joins the capture matrix (residue).**
- **A-13 — INFO (net-new; RR-LC).** The very file whose comment rules `catch (e:any)` dead re-offends 29 lines below it: `useSlugMigration.ts:112-114` `catch (e: any) { console.warn("Migration action failed:", e?.message) }` — verified by this seat's read. **Disposition: NO-WAVE-OWNER — dies with D-12's cure.**
- **A-14 — INFO (net-new; RR-DU).** The field's sole copy spends 3 of ~16 affordable characters on an ellipsis that can never render, and "token" is undefined anywhere in the surface; the donor's `enter slug...` fits. **Disposition: NO-WAVE-OWNER — copy dies with the Input migration.**

**Motion/PRM (law compliance)**: `motion-quarantine.md` carries NO row for `shell-dock-slugeditlayer` (this seat's grep — no FALSE row exists to carry; the file's nearest dock rows are **M-32**, `shell-dock-actiontoolbar`). The one `animate-spin` (:97) is unreachable (C-2) and PRM-guarded app-wide (`demo/styles/animations.css:184-193`, verbatim-verified by this seat). The component's own PRM measurement stays residue.

## Killed-claims register

- **K-1 — L-4's "no floor inside glass-dock / doubly exempt" sub-claim: KILLED** (RR-DU's kill, ratified by this seat's whole-rule bytes read — ruled above). It inverted the producer's design: the floor rides the size token; `touch-floor.css`'s `:not(:where(.glass-dock *))` prevents a redundant `min-block-size` from fighting the token, it does not exempt the dock. RR-LC's confirmed-L-4 phrasing is corrected under this row.
- **K-2 — L-4's producer-cure second half ("drop the `:not(--compact)` clause from touch-floor.css"): KILLED** — structurally inert at this site: the same selector still carries `:not(:where(.glass-dock *))` and every control here is a `.glass-dock` descendant. The relay ask is corrected to the surviving half (non-zero compact token defaults) before it is sent.
- **K-3 — L-4's cure #2 (hoist `--dock-compact-control-padding: 0.5rem 0.75rem` onto `.glass-dock` for all four consumers): KILLED** (RR-LC, sustained by arithmetic) — the value is asymmetric, tuned at T-36 for a labelled pill; applied to icon-only 14px glyphs it yields a ~38×30 non-square box and widens an already-tight row. The four `compact` consumers do not share one padding requirement.
- **K-4 — D-7's cure as worded ("delete the word compact three times. The producer's default *is* the design."): KILLED-AS-WORDED, RESCOPED** (ruling above) — the direction survives (the floor is real), the standalone wording does not: on coarse it adds ~66px to a row whose aperture D-4 measures (unproven-live) at 295/290; the cure lands paired with the row diet + a live overflow-track witness.
- **K-5 — D-5's mechanism (inert-phase crossfade) and its desktop-always/mobile-never determinism: KILLED** (RR-DU's kill sustained) — refuted by the sibling seat's own samples (`inertAncestor: null`, `faceActive: true`, `manualFocusWorks: true`; 2nd-open successes) on the same tree. The finding survives as C-7's dropdown-restore race; a repair aimed at the crossfade's inert phase would fix nothing.
- **K-6 — D-10's focus half ("no focus ring in Windows High Contrast"): KILLED** (RR-DU's kill, ratified by this seat: foundation.css IS the root sheet — its own line 1 is `@import "tailwindcss"` — and the :679+ forced-colors block is unlayered, so its `:where(…input…):focus-visible { outline: 2px solid Highlight }` beats Tailwind's layered `outline-none` by cascade-layer order). The at-rest half stands inside D-3's row (no border box for the forced palette to paint); forced colors is the one register where this field is distinct.
- **K-7 — L-2(b)/D-1.2's premise ("the callee cannot reject" / "catches its own failures internally"): KILLED** (RR-LC's kill, upgraded by this seat's empirical run into A-1). The truth is strictly worse than the accusation: the callee throws on every call and two of three branches have no handling at all.
- **K-8 — L-1 at BLOCKER: KILLED** (both re-readers, ratified) — demoted MAJOR; the twin ships zero behaviour and its one live consequence is already charged in D-1/L-2(d).
- **K-9 — L-1's "BYTE-DUPLICATED" lattice line (third item): KILLED** (RR-LC) — the two grammar functions are character-identical; the submit/error block differs in copy and dispatch. L-1's own table was accurate; the lattice line overstated.
- **K-10 — D-13's §7-Shell supporting citation: KILLED** (RR-DU, sustained) — VISUAL-CONSTITUTION.md:178 governs route changes, not layer swaps; D's own negative result endorses the producer's inert treatment; a modal Dialog would suppress the dock's navigation identity at least as completely. D-13 stands on the :222 assignment alone.
- **K-11 — C-5's "this layer is the outlier" comparison: KILLED** (RR-LC) — ActionBarToggle is also `compact`, merely remediated per-instance; the true statement is "the sole UNREMEDIATED compact consumer" (L-4 had it right).
- **K-12 — the L axis's citation anchors into useSlugMigration.ts: KILLED** (RR-LC, ratified by this seat's whole read) — every anchor 2-16 lines off (`:53`→:51, `:78-81`→:84-87, `:73-77`→:78-82, "returned at 105"→:121). All underlying claims true; prefer C-3/D-1's anchors when discharging L-2.
- **K-13 — RR-DU's kill of C-5's arithmetic ("458 rows / 61 captures / 52.4%"): KILLED — the kill itself.** This seat's recomputation over REPORT.json at HEAD returns exactly C-5's figures: 60 results, 398 rows, eight slug shapes × 30 = 240 = 60.3%. C-5 is restored as authored, and RR-DU's "four rows × 60 captures is self-contradictory" complaint fails (4 slug rows per capture × 60 = 240 is coherent). A re-reader's kill carries no more inherited authority than the claim it kills.
- **K-14 — RR-DU's C-2 parenthetical ("the admin branch … still cannot throw synchronously"): KILLED** by this seat's vue-router run — `setActiveTab("saved")` throws synchronously inside the admin branch's initial synchronous execution (surfacing as rejection through the async boundary). Recorded so the C-2 row is never quoted forward with the false parenthetical attached.

## Superlatives (provenance labeled)

1. **The producer's coarse-pointer floor is excellent design-system engineering** — the floor lives INSIDE the size token (`overflow.css` → `--dock-control-floor` → `density.css` `max()` in every bucket), so `touch-floor.css` can exempt `.glass-dock` without losing it and paint box stays separable from hit box as `DockControl.vue.d.ts` documents. Both RR seats saw half; the whole is better than either described; this component's `compact` is the one opt-out. (RR-DU's derivation, ratified by this seat's bytes read.)
2. **`adaptive-legibility.css`** — re-pinning `--muted-foreground: var(--foreground)` and deriving `--dock-fg-on-aurora` from live backdrop luma (with a `contrast-color()` upgrade path) so nothing goes illegible over moving aurora. A real solution to a real problem; A-4 is the consumer-side cost of the pin being undocumented — a producer documentation gap, not defect. (RR-DU; head re-read by this seat.)
3. **`normalizeTokenInput` (:29-37)** — 9 pure lines that accept a line pasted straight from a .env or shell export, case-insensitive, whitespace-tolerant, matched-quote-stripping. The one place a human was imagined; its problem is entirely the classifier wrapped around it. (Both RR seats independently.)
4. **`looksLikeSlug` is provably exact against the server** — all 512 word literals in `slugWords.ts` match `^[a-z]+$`, so the regex accepts every slug the system can mint; the whole BLOCKER lives in what the unknown bucket does. A far sharper statement than "the validator is wrong". (RR-LC's verification.)
5. **The W6-8 register comment (:88-90) + its accidental machine enforcement** — a design ruling written at the site, obeyed (three aria-labels, zero `title`), and swept by o15:141 even though the spec declares the layer out of scope. The pattern the rest of the component needed. (Both RR seats; o15 lines re-read by this seat.)
6. **`Dock.vue:78-89` U-F48** — three imperative watchers collapsed to one declarative predicate with the equivalence argument written in-line. D-2 vindicates the design: a predicate one disjunct short is a one-word repair with an obvious diff. (RR-DU.)
7. **Two seats declining to inflate a handed premise** — L-4 falsifying MT-F004's flat 24×24 framing via SC 2.5.8's spacing exception, and D-7 reading the sibling seat and concurring in writing rather than re-charging the row. This corpus at its best. (Both re-readers; carried as the INFO row.)
8. **C-14's named green-keeping mutation** — an exact three-line diff that keeps every gate green while destroying accounts; A-1 proved the point empirically before any wave ran. (Both re-readers; census re-run by this seat.)
9. **Explicit negative-results sections in D and L** (ten + nine rows) that saved later seats real probes — several spot-confirmed rather than re-derived by both re-readers and by this seat. (Corpus-original.)
10. **RR-LC's A-1 find itself** — three axes and one re-reader quoted `setActiveTab("saved")`'s surrounding lines repeatedly (D-1's sink at :84-87 is eight lines below it) and only the second re-reader asked where "saved" goes. Empirically verified twice, independently (RR-LC's run, then this seat's). The find of the corpus.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **A-1's shipped-browser manifestation** — the sync throw is empirical (memory-history harness, twice, independently) and matcher resolution is history-agnostic, but the full live chain (submit → unhandled-rejection console error → per-branch UI symptom, incl. the success-converted-to-silence slug arm) should be witnessed once in the hash-history app before the cure wave stamps its BEFORE frame.
2. **D-4's 320px aperture hit-test** — `elementFromPoint` null at Cancel's centre; `scrollWidth 295 / clientWidth 234` (320) and `295 / 290` (390). Load-bearing against the adjudicated D-7 cure (K-4's pairing requirement); the producer's `[data-dock-overflow]` scroll-track degradation must be witnessed at both arms, before and after `compact` deletion.
3. **D-2's wall-clock reproduction** — t≈4000 held / t≈6000 collapsed, groupW 285→56; mechanism proven in source + producer bytes; only the timings are live.
4. **D-5/C-7's focus timelines** — the two corpus seats' samples contradict on per-platform outcome (cold-open race vs deterministic split); at least one sampling run must be re-executed before any timeline is carried as settled. The mechanism (dropdown restore race) and the close-half are already adjudicated on source.
5. **All measured geometry** — 22×22 / 23.3×23.3 control rects, the 47/75/28px centre distances underpinning the SC 2.5.8 INFO row, `inputRect [392,30,160,21.5]`, the 200%-zoom negative result. Token derivations proven; pixels not.
6. **D-3's pixel-identity frames** — `shots/` is not in the repo (only `evidence-slug-layer-open-1024.png` is); the structural cause is certain, the byte-identical screenshot pair is not reproducible from here.
7. **D-6's long-token overflow** — `scrollWidth 749 / clientWidth 160`, no ellipsis/fade (UA clip default supports it; numbers live-only).
8. **Placeholder advance-width per arm** — the two corpus seats disagree on the rendered prefix (`…or to` vs `…or t`/`…or tc`); the in-repo 1024 capture matches neither seat's quoted 1440 prefix. Truncation itself is certain (arithmetic + capture); the per-arm character counts are not.
9. **iOS hardware pair** — sub-16px focus auto-zoom (fluid token measured 14px at 390 by both seats) and the missing `autocapitalize` corrupting typed tokens. Attribute absences proven in source; device behaviour is not. NEEDS-HARDWARE.
10. **C-2's live submit trace** (`net: []`, dialog at t+50ms, MutationObserver spinner watch) — corroborative only; the synchronous-execution proof is complete from source.
11. **PRM live measurement** (`animation-duration: 1e-05s`) — the guard is proven in bytes (`animations.css:184-193`); the measurement is live-only. No motion-quarantine row constrains it (M-32 is a different component).
12. **L-10's harness repair witness** — one committed capture of the ACTIVE layer joins the visual matrix so the next corpus stops measuring an opacity-0 phantom.

## Closing verdict

**ADJUDICATED — RED.** 35 rows survive adjudication (5 BLOCKER · 13 MAJOR · 12 MINOR · 5 INFO), 14 of them net-new after the challenge round — including **A-1: every submit of the primary action throws on a phantom named route through an unchecked cast, empirically verified twice**. 14 killed-register rows, one of them a kill-of-a-kill restoring C-5's recomputed 240/398 = 60.3% corpus share. This 119-line always-mounted component is the single largest a11y-geometry contributor in the visual corpus and carries two independent account-destruction paths (C-1 typo-route, A-3/A-5 regenerate-route) with zero test coverage (C-14) and a dead error surface (D-1) — nothing here is wave-stamped: terminal homes are **W23** (constitution-assigned Account Dialog: D-6, D-13, and the error-surface/journey rows), **W-HYGIENE** (L-9, existing config-parity identity, anti-rename), the **glass-ui BH relay** (D-7/L-4 producer half, with K-2's corrected ask), and **NO-WAVE-OWNER at the next formation boundary** for all remaining rows, A-1 first among them.

— end of apotheosis. The three challenge files, the evidence capture, and both re-reader returns remain immutable beside this record per E-1/E-3; this file is new, no dated evidence was rewritten.
