Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SquareScene)

# kf-SquareScene — adjudicated apotheosis

**Date** 2026-08-24 · **Corpus** `audit/kf-components/SquareScene/challenge-{D-design,L-library,C-consumption}.md` (all three `claude-opus-5[1m]`) · **Re-readers adjudicated**: design-and-usage (reader A, Fable) + library-and-code (reader B, Fable), both hostile, returns consumed whole.

**Substrate ruling.** Local checkout `/Users/mkbabb/Programming/keyframes.js` = the SACRED stale commit `8281638c`, 41 behind `origin/master` `81a56990`, 252 dirty rows — and this seat's own diff (`git diff origin/master --name-only -- demo/scenes/square/ demo/styles/design-idioms.css demo/composables/useDragScrub.ts demo/composables/useDoubleTap.ts demo/state/scenePlaybackAdapters.ts demo/app/scene/sceneExposedApi.ts`) returns **empty**: the entire audited unit is byte-identical worktree↔frontier. The sole blast-adjacent drift is `package.json`/`package-lock.json` (−53 lines) — the SAME uncommitted deletion of `origin/master`'s exact `"@mkbabb/glass-ui": "7.0.0"` devDep already ruled at kf-CubeScene/KF.W0. All line anchors in this record bind to worktree ≡ `origin/master`; KF.W0's re-anchor law applies before any cure.

**Method.** Every verdict-level disagreement between the readers was re-derived by this seat against the tree with its own instruments, never vote-counted. Instruments this session (static + repo-local only; zero browser, zero product-source writes — probe parsimony): the full three-file challenge corpus + both reader returns read whole; **full reads** of SquareScene.vue (331), useSquareDemo.ts (404), useSquareTumble.ts (50), useSquareKeyboard.ts (103), SquareScene.css (159), SquareInstrument.vue (212), test/demo/scenes/square-scene.test.ts (98); **targeted seam reads**: useDragScrub.ts:95-149 (the `onPointerDown → onStart?.(e) → onScrub(project(e))` decider), useKeyframeOps.ts:50-79 (the transform-less recompile), compile-bridge.ts:85-104 (`adoptCompiled` transplants the compiler whole + `anim.unflatten = source.unflatten`), css-animation.ts `resolveTransform`, playback.ts:110-152 (`_run` — `const result = step(now)` with NO try/catch; a sync throw skips `reschedule`, `_cleanup` never runs, `running` stays true forever), progress.ts:373-391 (`reset` writes `targetValue`), scenePlaybackAdapters.ts:80-100 (the `suspend` else-arm + `resume`), useSweepScene.ts:75-122 (the `!playback.running` guard + the unconditional stub adapter), sceneExposedApi.ts:16-34, design-idioms.css:8-82; **probes**: compiled glass-ui Card `shadow: { type: Boolean, default: !0 }` vs Surface `default: !1` (dist/card-Da665R8v.js / dist/Surface-DOHf5u2R.js — the D-24/L-21 decider), installed `dist/styles/utilities/a11y-overrides.css` read whole — the `@media (forced-colors: active)` block naming `.focus-ring:focus-visible { outline: 2px solid Highlight }` (the D-2 decider) AND the unlayered `!important` PRM universal reset (the PRM-inert decider), `dist/styles/tokens/scale-paper.css` `--rainbow-{red,orange,yellow,green,blue,indigo,violet}` oklch declarations (the D-23 collision decider); **census greps**: `animationGroup.pause()` across demo/scenes = exactly 1 (SquareScene.vue:238), `isPlaying` across demo/app+demo/state = comments + adapter-internal only, `Vars<T = any>` at src/animation/constants/types.ts:39, tsconfig `include: ["src/","demo/"]`, the no-shadow-playback-authority law text at useAnimationGroupPlayback.ts:33-35 + the `stop()` exemption at useAnimationGroupActions.ts:57; **registry identity reads**: kf-RibbonBar RB-1, kf-CopyButton K7 + the D-19 reversal, kf-CSSCodeEditor KF-CE-13, kf-ControlsPaneWrapper RR-1, kf-CubeScene (L-2/C-5, the PRM-reset decider, the F-1 manifest trifecta), kf-AmigaScene D-2/MISSED-F, GradientStopEditor C11/G5, kf-AnimationVisualizer's grabOffset row. Where BOTH readers independently verified a cell and this seat's spot-checks found no crack (the D-11/D-26 contrast arithmetic, the C-5(a) overshoot ratio, the surface-triad reachability chain, the D-8 stage-cell geometry), the corroborated cell is adopted without a third derivation.

## Scoreboard

**56 rows ADJUDICATED-CONFIRMED (1 BLOCKER · 16 MAJOR · 31 MINOR · 8 INFO) · 6 killed-claims register entries · 11 net-new misses adopted (5 reader-A · 5 reader-B · 1 this seat's ARB-1; a 12th reader-B miss BOOKING-KILLED as pre-banked RB-1 and a 13th folded as a rider, not a row) · 10 UNPROVEN-NEEDS-LIVE residues for SS-13.**

Headline survivor: **L-2, the sole BLOCKER** — one Keyframes-tab edit (even a whitespace round-trip) silently and permanently destroys the scene's custom `transformFunc` + `unflatten`, the exact primitive the scene exists to dogfood, and the seam (`useKeyframeOps` + `adoptCompiled`) would eat ANY scene's custom renderer — square is merely its only current consumer. The component's real condition: the best-composed gesture/loop architecture in the demo (twelve standing superlatives) wearing a broken signature instrument (D-1), a colour channel that has never painted a pixel (C-1), a playback FSM whose third state is unreachable (L-8/C-2), and docblocks that are the least reliable artefact in the unit — the corpus proved five asserted invariants false and the misses added two more ("not a fixed timer" sitting 46 lines above a fixed timer; "the sanctioned `--rainbow-*` family" over five raw hexes belonging to no token).

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; routings below are agglomeration input (M-25) for the SS-1/SS-2 spec authoring, keyed to the census wave sketch KF.W0–KF.W10.

## Ruled disagreements (this seat's actual work)

| dispute | reader A (design-and-usage) | reader B (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-1 severity** (dead colour paint) | MAJOR (downgraded from the axis's BLOCKER) | BLOCKER held | **MAJOR — A wins** | House ladder: nothing crashes or becomes unusable (drag, tour, egg all fire; the `data-palette-sweep` bloom carries the egg's event); the registry's BLOCKER precedents are shipping crashes, unusable controls, false facts on a first-class route (ColorSpaceSelector L-1), or triple-mounted architecture (ACG D-1). kf-CubeScene demoted its own dead-feature+live-throw C-1 to MAJOR on the same ladder. The occlusion itself is airtight (my read of the complete `.demo-box` selector set — nothing resets `background-image`; both gradient stops opaque). B's BLOCKER preserved as dissent: the scene is the demo's value.js-colour showcase and paints none of it. |
| **D-1 severity** (tether `viewBox`) | MAJOR (downgraded) | BLOCKER held | **MAJOR — A wins; B's mechanism correction adopted** | The element is `aria-hidden` decoration; nothing is inoperable. B's correction stands over both A and the axis: the tether loses to the telemetry by **z-index** (`.square-tether` carries NO z — my read of :146-155 — vs the strip's `--z-content` at :179/:201), not by DOM-order-at-equal-rung; the axis's reading suggests a reorder cure that would not work. The `viewBox` absence, the [12,88] px confinement, and the two orphaned declarations (`stroke-width: 0.8` + `vector-effect: non-scaling-stroke`, no-ops at 1:1) all re-verified in my full read. The axis's own note survives whole: the *intended* 0..100 reading is also defective (non-square plates), so the cure is a px-space path, not a `viewBox` patch. |
| **L-1/C-3 severity** (the shadow pause) | MAJOR | BLOCKER | **MAJOR — A wins** | The desync costs one dead transport press and a one-interaction lie, recoverable on the second press (my read of the `suspend()` else-arm + `resume()`). What keeps it at the very top of MAJOR: the law violation is exact and unique repo-wide — my census, `animationGroup.pause()` = **one hit in all of demo/scenes**, this line — against a law stated verbatim with its own proof name (`proof:no-shadow-playback-authority`, my read of the law text and the `stop()` exemption). |
| **L-2 severity** | BLOCKER | BLOCKER | **BLOCKER SUSTAINED — this seat's own four-file re-walk** | useKeyframeOps.ts:66-70 recompiles `.fromKeyframes(keyframes)` with NO transform argument; css-animation.ts `resolveTransform` → `this.unflatten = transform != null` → false + `_defaultTransform`; compile-bridge.ts:95-99 transplants the compiler whole and copies `anim.unflatten = source.unflatten`. Reachable: the channel carries `animation: anim` so it earns the triad (controlSurfaces' animation-present rule, corroborated by both readers). Silent (drag keeps working — the spring loop calls the local closure directly), permanent for the mount, Play-only, on the component's raison d'être, through a first-class editor surface. The cure is seam-level, not square-level. |
| **D-2** (forced-colors focus loss) | KILLED as filed; residual FOLDS to banked RB-1, not re-filed | evidence cell killed, row re-graded CONFIRMED-in-direction MAJOR, root re-filed as net-new missed row 1 | **KILLED as filed; residual ≡ RB-1 FOLD; B's missed row 1 BOOKING-KILLED as pre-banked** | My own read of installed a11y-overrides.css: the `@media (forced-colors: active)` block names `.focus-ring:focus-visible` with `outline: 2px solid Highlight; outline-offset: 2px` — D-2's written falsifier ("a forced-colors block anywhere in the served cascade that restores an outline on .focus-ring:focus-visible") is satisfied verbatim, so the finding dies as written. The surviving outcome — no indicator, because `design-idioms.css:76-79`'s unlayered duplicate lands later at equal (0,2,0) — is banked **VERBATIM as kf-RibbonBar RB-1** ("not an omission, a removal"), with SquareScene.vue:46 already enumerated among the four adopters at kf-CopyButton K7. Identity guard: ONE identity, zero re-booking. B's built-bytes cascade pin (109930 / 412696 / 551851 in `index-CL_QYCiO.css`) is adopted as a corroborating annotation ON RB-1, and B's cure correction (delete the demo duplicate; never add a demo-side forced-colors block) matches RB-1's. A's note that this also corrects the contrast implicit in kf-CopyButton's D-19 reversal is REDUNDANT with RB-1 (banked later, same registry) — no registry repair needed. |
| **D-24/L-21 sub-claim** ("`:shadow=\"false\"` is redundant") | KILLED (wrong component's prop table) | silent | **KILL CONFIRMED by this seat's own probe** | Compiled Card props: `shadow: { type: Boolean, default: !0 }` (**true**, dist/card-Da665R8v.js; Card forwards `shadow: n.shadow && !n.cartoon`); Surface's own default is `!1` (**false**) — both axes read Surface's table and attributed it to Card. `:shadow="false"` is LOAD-BEARING: drop it and the plate gains a shadow the I5 stage-card register does not want. The rest of D-24 (the two-hop `material→tier` default map) survives at INFO; its "phantom dependency" framing dies with L-20/C-7. |
| **D-9 severity** (title/protagonist same type rung) | MAJOR held | silent (not in B's roster) | **MINOR-RESCOPED — this seat** | A within-scene composition contradiction with zero functional loss, zero false prose (the :28-32 comment states the *box's* intent, not the strip's), and a one-class cure. The corpus's MAJOR rung otherwise carries false contracts, broken behavior, or a11y failures; the same family (D-10, D-22) sits at MINOR two rows away. A's MAJOR preserved as dissent; the strip-internal ladder facts (4× step over a flat 4 px gutter) adopted unchanged. |
| **C-5(a) crossing count** | mechanism confirmed; count not re-simulated (PLAUSIBLE) | six crossings CONFIRMED by closed-form derivation | **SIX ADOPTED — B's derivation** | Overshoot ratio exp(−πζ/√(1−ζ²)) = 0.1068 at ζ = 0.58 → the peak ladder 398.4 / 355.9 / 360.44 / 359.95 / 360.005 / 359.9995 crosses 360° six times before the 1e-3 settle floor; A's independent peak figure (398.5°) agrees to a tenth. Count is settle-tolerance-dependent; each crossing snaps the wrapped `t` green↔violet in one frame. Flicker visibility → SS-13, **post-C-1-cure only**. |
| **D-11 falsifier direction** | corrected the axis: "no plate luminance in [0,1] restores AA — the derivative runs the other way" | left at the axis's framing | **A's correction ADOPTED** | The 0.8-composited ink tracks the plate: the ratio is maximised near the white plate at ≈3.47:1 and every darker plate drives it toward 1. The finding is stronger than filed; the light-arm failure is unconditional on plate tone. |
| **D-15 dead half** | discharged statically (four sibling roots defeat scope-id propagation) | same discharge + the runtime line numbers + the built asset's double scope-id | **CONFIRMED; UNPROVEN label removed** | Both discharges accepted; SquareInstrument's template has four sibling roots (my read, :10/:12/:21/:35), so the scene's `data-v-*` never reaches the `<svg>` and `SquareScene.css:137-140` matches nothing. One identity with the PRM-inert rider (the rule is doubly dead under the producer's `!important` reset). |

## Adjudicated defect roster

Identity-deduped: one row per defect, all axis ids attached. Anchors bind to worktree ≡ `origin/master`. Routing targets = the census wave sketch; **NO-WAVE-OWNER** rows are SS-1/SS-2 spec-authoring intake — they agglomerate into one *square scene-repair wave* the forming specs must create.

### BLOCKER

- **L-2 — CONFIRMED (BLOCKER, unanimous, re-derived).** A keyframes-panel edit destroys the scene's custom renderer: `useKeyframeOps.updateFromString` recompiles with no transform (`:66-70`), `resolveTransform(undefined)` sets `unflatten = false` + the default DOM renderer, `adoptCompiled` transplants the compiler whole and copies the flag (compile-bridge.ts:95-99). From the first round-trip the nested `transform.a.b.c.d` leaf (useSquareDemo.ts:108) is unreachable from the engine path — silently and Play-only, against the scene's own prose ("the panel triad edits a LIVE animation"). **Disposition: NO-WAVE-OWNER (the square scene-repair wave's editor-seam packet): `updateFromString` must re-supply the animation's own renderer, or `adoptCompiled` must preserve a receiver's non-default transform — an engine-seam decision (`usesDefaultRenderer` already exists as the reference test, css-animation.ts's own comment). What the default renderer paints for a nested leaf → SS-13.**

### MAJOR

- **C-1 — CONFIRMED (MAJOR; B's BLOCKER as dissent).** The colour half of the scene's value.js consumption is dead paint: `el.style.backgroundColor` (useSquareDemo.ts:138, the sole colour write) is occluded by `.demo-box`'s own opaque `background-image` (SquareScene.css:52-56; CSS Backgrounds L3 §3.10). Both writers land dead: the tumble sweep (`:218-221` → useSquareTumble.ts:27-40 — the entire reason `parseCssColor`/`mixColors`/`serializeCssColor` are in the graph) and all five tour stops (`:349-368`). My read of the complete `.demo-box` selector set confirms nothing resets `background-image`. **Disposition: NO-WAVE-OWNER (colour packet — one honest cure: sweep the gradient's own custom property, or reset `background-image` while an inline colour owns the paint). Pixel witness → SS-13.**
- **D-1 — CONFIRMED (MAJOR; z-mechanism corrected).** No `viewBox` on the tether SVG (SquareInstrument.vue:12-17): per SVG §7.7 `preserveAspectRatio` is inert without one, so 1 user unit = 1 CSS px from the SVG's top-left — the scene's signature instrument renders as an ≤88 px hairline in the plate corner, detached from a subject travelling ±110 px, buried under the telemetry by z-index. Orphan corroboration: `stroke-width: 0.8` + `vector-effect: non-scaling-stroke` are no-ops at 1:1. **Disposition: NO-WAVE-OWNER (instrument packet; the cure is a px-space path — the axis's own note that `viewBox="0 0 100 100"` still mis-maps on non-square plates is adopted, and MISS-2 below is the same disease in the field frame).**
- **L-1/C-3 — CONFIRMED (MAJOR).** `captureFrame` pauses the group directly (SquareScene.vue:237-241) behind the machine, violating the repo's own named law — my census: the ONLY `animationGroup.pause()` in any scene. The intended sync write is dead (L-8/C-2), so `machine.status` stays `playing`, the transport lies for one interaction, and the first press takes `suspend()`'s else-arm (a visual no-op); a second press recovers. **Disposition: NO-WAVE-OWNER (playback-authority packet: dispatch PAUSE through the machine on the takeover edge — one authority, as the law already states).**
- **L-8/C-2 — CONFIRMED (MAJOR).** `isPlaying` is never exposed (`defineExpose({ facility, superKey })` at :313-323 — the description sits as PROSE inside the object literal), absent from `SceneExposedApi` (my read, :16-34), and written exactly once, to `false`, on an already-false ref — so `watch(isPlaying, …)` never fires and `mode = "playback"` is unreachable: the documented three-state FSM is a two-state one. Both readers' sharpening adopted: the invoked "cube/amiga contract" exists in no scene — CubeScene exposes an equally-unread `isPlaying` (banked at kf-CubeScene L-2/C-5, which already orders the SquareScene prose correction), AmigaScene has none; square copied a dead pattern. **Disposition: NO-WAVE-OWNER (playback-authority packet, settled WITH the banked kf-CubeScene L-2/C-5 cure — ONE playing-state authority; `facility.isPlaying()` exists).**
- **C-4 — CONFIRMED (MAJOR).** The keyboard layer never pauses the group: `useSquareKeyboard` is wired `{springX, springY, reseat, onTarget}` only (my full read — no `animationGroup` reference), every branch reaches `startLoop()`, and the T.A13 two-writer guarantee is implemented in `captureFrame` (pointerdown) alone. Play + any arrow = two rAF writers on one `el.style.transform`, last-write-wins per frame; the `c` egg extends the contention ≥2.6 s. The tumble is only *accidentally* safe (double-tap rides pointerdown). **Disposition: NO-WAVE-OWNER (playback-authority packet: the pause belongs on the FSM edge, not on one input modality).**
- **L-4/C-12 — CONFIRMED (MAJOR).** The instrument freezes and lies during playback: `onTick` has exactly two call sites (inside `frame()` and once in `paintRest()` — my read), the engine tour never pumps it, and the instrument renders unconditionally — so through the scene's headline verb the badge reads "settled", the tether freezes, the numerals and both `aria-valuenow`/`aria-valuetext` hold stale drag values. **Disposition: NO-WAVE-OWNER (instrument packet: pump the derived reads from the paint authority, whichever it is — or gate the instrument on `mode`).**
- **L-5 — CONFIRMED (MAJOR).** The "jump-free takeover" is jump-free in translation only: `seatFromPose` reads `m41/m42` and then `springSpin.reset(0, 0)` (my read, :303-320) — the tour's rotation (0→360°) and nested scale (100↔108%) are discarded, and the next spring frame paints `rotate(0)`: up to a 90° un-rotation in one frame, against two "no frame jump" comments. **Disposition: NO-WAVE-OWNER (playback-authority packet; see ARB-1 for the reverse edge).**
- **L-3 — CONFIRMED (MAJOR).** The tumble accumulator desyncs monotonically: `useSquareTumble`'s module-private `target += 360` is never told about `springSpin.reset(0, 0)` (my read of both sites + `SpringProgress.reset` writing `targetValue`). Three gestures → a two-turn barrel roll; each takeover adds a turn. **Disposition: NO-WAVE-OWNER (colour/egg packet: derive the next target from `spin.target`, or reset the accumulator in `seatFromPose`).**
- **D-3/L-12/C-6 (+ the PRM-inert rider) — CONFIRMED (MAJOR).** PRM is asserted in prose and unimplemented in fact: all three springs decline `respectReducedMotion` (default false — both readers; four sibling opt-ins exist), 100% of the scene's motion is unguarded JS including a gesture-triggered 360° roll, and the CSS blocks the comments call the compliance are the layer with no motion. RIDER (folds by reference, not a row): the CSS blocks are themselves INERT — the producer's unlayered `*:not([data-allow-motion]) … !important` PRM reset (my read of installed a11y-overrides.css) defeats every local `transition: none`, the identical mechanism banked as the kf-CubeScene D-16 decider and kf-ControlsPaneWrapper RR-1 — so the scene has ZERO effective PRM enforcement in either layer, the strictly stronger reading. **Disposition: KF.W9 (PRM mechanism unification, intensity form per the AmigaScene precedent; square = 0 of 4 motions gated, the sharpest §6.5 correction yet).**
- **D-27/L-7/C-9 — CONFIRMED (MAJOR).** Five `throw` sites execute inside the rAF frame (`num()` ×2, `colorAt` ×3) with no engine guard, and a throw does not drop a frame — it BRICKS the loop for the mount's lifetime: my read of `RAFPlayback._run` confirms a sync throw skips `reschedule`, `_cleanup` never runs, `running` stays true, and `useSweepScene`'s `!playback.running` guard makes `startLoop` a permanent no-op — drag, keyboard, and tumble all die silently. Reachable via the same editor path L-2 opens (`calc()` is ordinary authored CSS; `parseCssScalar("calc(1px + 2px)")` → ERR → `num()` throws), and via any `--rainbow-*` re-author in a syntax value.js 4.0.0 rejects (the megatranche R1 identity — `parseCssColor("oklch()")` throws; noted as a fold to the V·π parser program, never re-booked here). **Disposition: NO-WAVE-OWNER (scene-repair: degrade-not-throw in the frame path) + a library rider: `RAFPlayback._run` should fail a frame loudly and recoverably, not wedge — engine-seam decision.**
- **D-4 — CONFIRMED (MAJOR; announce half UNPROVEN).** The two `role="slider"` children are deliberately non-focusable (SquareScene.css:12-15 says so), so their live values are never announced — the "COMPLETE WCAG 4.1.2 contract" comment covers value, not operability — and the visible readout is `aria-hidden`, so there is no alternative channel. Registry temper kept: kf-AmigaScene D-2 banks SquareScene as the house exemplar among the subject scenes; the defect is the missing operability half only. **Disposition: NO-WAVE-OWNER (a11y packet); which ATs announce → SS-13.**
- **D-7 — CONFIRMED (MAJOR).** The accessible name instructs a pointer-only gesture while every keyboard affordance (Arrow×4/Home/End/`c`, double-tap) is undeclared — no `aria-keyshortcuts`, no `aria-describedby` — and all three on-screen instruction surfaces are `aria-hidden`. **Disposition: NO-WAVE-OWNER (a11y packet, one motion with D-4).**
- **D-5 — CONFIRMED (MAJOR).** The keyboard egg's discoverability hint ("press C to trace the field") is gated behind a pointer drag: `hasDragged` has exactly one write, in `captureFrame` (pointerdown); the keyboard path leaves it false forever. Cross-cut with L-17: the same single write is both too coarse (a bare tap trips it) and unreachable by keyboard. **Disposition: NO-WAVE-OWNER (a11y packet: disclose on any modality's first settle).**
- **D-6/L-6 — CONFIRMED (MAJOR).** The `c` egg hijacks ⌘C/Ctrl+C — no modifier guard, `preventDefault()` unconditional (my read, :73-77); the arrow/Home branch swallows ⌘←/⌘↑ identically (:81-91). Bypasses the demo's shortcut registry, so it is invisible to the shortcuts modal. **Disposition: NO-WAVE-OWNER (input packet: bare-key guard + registry registration).**
- **D-8 — CONFIRMED (MAJOR).** A fixed 12 rem subject with ±110 px travel and 1.08–1.12 scale inside an `overflow: hidden` plate: tour corner 193.7 px and full-drag 217.5 px half-extents against a 187.5/195 px half-width at 375/390 px viewports (the mobile stage-cell is full-bleed with padding-block-only reserve — corroborated geometry). The subject and its separator ring amputate during the headline animation. **Disposition: NO-WAVE-OWNER (geometry: clamp `--size`/`TRAVEL` responsively); rendered clip → SS-13.**
- **MISS-1 (reader A, adopted; verified by this seat) — ADJUDICATED-CONFIRMED (MAJOR).** **The grab is a teleport**: `useDragScrub.onPointerDown` calls `onScrub(project(e))` unconditionally on pointerdown (my read, :124-125), and square's `project` maps the ABSOLUTE pointer position into target space off the rest-centre `homeX/homeY` (:248-249, :260-263) — no grab offset is ever captured. A press anywhere but the box's exact centre re-targets the springs to bring the box centre to the press point: up to 96 px for an edge press, 136 px diagonal for a corner press (from `--size: 12rem` + `TRAVEL = 110`), and `releasePolicy: "persist"` leaves a bare click's displacement permanently. The :218-219 "follows the pointer ~1:1" comment is true of the delta only for a dead-centre grab. Precedent: the identical class ruled MAJOR at GradientStopEditor C11/G5 ("a grab is not a teleport", cured by `grabDx`), and kf-AnimationVisualizer's banked row records that file capturing a grabOffset — the house knows the cure; the demo's largest drag target declines it. Registry swept: no banked kf-* row covers this seam. **Disposition: NO-WAVE-OWNER (input packet: capture the grab offset at pointerdown; the rail seam's press-to-position idiom is correct for rails, wrong for a subject). Live magnitude → SS-13.**

### MINOR

- **D-9 — CONFIRMED (MINOR-RESCOPED, ruled above).** Chrome title and protagonist at the identical `text-display` rung (25.9–41.9 px both), a ~4× internal step over a flat 4 px gutter. NO-WAVE-OWNER (instrument packet; one class swap).
- **D-10 — CONFIRMED (MINOR).** The legend renders UPPERCASE (`text-mono-caption` carries `text-transform: uppercase`) against the lowercase axes it labels — three spellings of two axis names in one column. NO-WAVE-OWNER.
- **D-11 — CONFIRMED (MINOR; falsifier corrected — the finding is stronger than filed).** `.square-legend-hint { opacity: 0.8 }` composites the light-arm hints to ≈3.47:1 against a 4.5 floor, and NO plate luminance restores AA (ruled above). NO-WAVE-OWNER (a11y packet: drop the opacity or re-token); measured composite → SS-13.
- **D-12 — CONFIRMED (MINOR).** "double-click to tumble" instructs a mouse-only action for a recognizer (`useDoubleTap`) whose docblock exists because dblclick was touch-unreachable. NO-WAVE-OWNER (copy swap).
- **D-13/C-13.2 — CONFIRMED (MINOR).** `palette-sweep-host` — one occurrence in the tree, the call site; defined nowhere. NO-WAVE-OWNER (hygiene).
- **D-14/L-9/C-13.3 — CONFIRMED (MINOR).** `data-square-mode` written on every FSM transition, read by nothing (exhaustive grep, both readers + spot-check), and by L-8 it can only ever hold two of its three states. NO-WAVE-OWNER (rides the FSM repair).
- **D-15 — CONFIRMED (MINOR; dead half discharged statically, ruled above).** The scene sheet's `.square-tether { transition: none }` duplicates the child's rule, violates the file's own stated boundary, compiles to a selector that can match nothing, and is doubly dead under the producer PRM reset. NO-WAVE-OWNER (delete).
- **D-16 — CONFIRMED (MINOR; magnitude UNPROVEN).** A 160 ms `box-shadow` transition low-passes the 60 Hz-driven `--spring-tilt` glow — a per-frame-driven visual wants no transition. NO-WAVE-OWNER; SS-13 note: re-derive under the producer PRM override (transition-duration forced to 0.1 s).
- **D-17 — CONFIRMED (MINOR).** `select-none` on the Card is a second select-suppression authority; the seam it routes through owns a gesture-scoped one and documents itself as the only authority. Permanently blocks selecting the telemetry/legend/"drag me". NO-WAVE-OWNER (delete the class).
- **D-18 — CONFIRMED (MINOR).** Off-scale spacing literals (`gap: 0.15rem 0.45rem`, `0.15rem`) against a 0.25 rem grid, in a demo with a documented geometry-token layer. NO-WAVE-OWNER (rides KF.W6's token discipline).
- **D-19 — CONFIRMED (MINOR).** `Home` and `End` both recenter (APG assigns End→max); step ladder fixed at 0.25 with no fine grain, PageUp/Down unbound — 9 reachable positions per axis against a continuous pointer path, under a self-invoked "slider posture parity". NO-WAVE-OWNER (a11y packet).
- **D-20 — CONFIRMED (MINOR).** Physical-property chrome pinning; nothing mirrors under RTL; corpus-wide (no `dir` handling anywhere), which is why MINOR. NO-WAVE-OWNER (house-wide rider).
- **D-21 — CONFIRMED (MINOR).** The opaque subject slides over the corner readouts at high deflection (equal z rungs, box later in DOM); `pointer-events: none` spares the gesture, not the legibility. NO-WAVE-OWNER (instrument packet).
- **D-22 — CONFIRMED (MINOR).** Three type registers in a four-line corner block; italic doing no semantic work on instructions. NO-WAVE-OWNER.
- **L-10 — CONFIRMED (MINOR).** `num()` is unit-BLIND, not "unit-honest": only `%` is special-cased; `5rem`→`translate(5px…)`, `90deg`→px by position (readers' probes against installed 4.0.0). The `pct ? 1 : 0` arm is dead (the sole percent call site guards `!= null` first — my read); B's third gap adopted: `num(null)` falls past the `undefined` check to the terminal throw. NO-WAVE-OWNER (scene-repair; correct the docblock in the same motion).
- **L-11/C-5 — CONFIRMED (MINOR, one landing-family row).** (b) The L.W11 S4 "provenance fix" broke the "seamless by construction" landing: `--rainbow-green` (hsl(130 70% 50%) ≈ rgb(38,217,68) — design-idioms.css:18, sole unconditional definition, my read) ≠ `--subject-teal` (#52e898); the pre-substitution literal WAS token-exact. (a) The underdamped settle tail crosses the palette wrap six times (ruled above), and the last painted frame before the clear sits at wrapped t≈0 = violet. Masked today by C-1 — file the pair together. NO-WAVE-OWNER (colour packet).
- **L-13/C-11 — CONFIRMED (MINOR).** `animationGroup.singleTarget = false` is a raw poke at library-derived state that both the constructor and `setTargets` recompute unconditionally; the scene survives only because it targets the CHILD — an invariant stated nowhere. NO-WAVE-OWNER + a library rider: the routing deserves a supported opt-out, not a public mutable field the library also owns. Grouped-path paint under a revert → SS-13.
- **L-14/C-13.1 + C-13.4 — CONFIRMED (MINOR; ≡ KF-APP-4 class, FOLD for the gate).** Dead `computed` import in the SFC — and no tool can catch it (no vue-tsc, no eslint, no `noUnusedLocals`; re-verified at the frontier by reader A). C-13.4's redundant `as CssColor` cast (useSquareTumble.ts:37) rides the same hygiene commit. **The gate ≡ KF-APP-4 → KF.W4; the two artefacts die in the scene-repair wave.**
- **L-15 — CONFIRMED (MINOR).** The readout sync is triplicated across two mirrors with two rounding rules (`toFixed` vs `Math.round`) that disagree at tie-adjacent doubles, so `aria-valuenow` and `aria-valuetext` can announce different numbers; the duplication is the load-bearing half. NO-WAVE-OWNER (one `syncReadouts()`).
- **L-16/C-10 — CONFIRMED (MINOR).** `useSweepScene` unconditionally mints a stub `ScenePlayback` (progress pinned to 0) that square discards — exactly the decoy shape the SceneFacility docblock declares deleted; unregistered, so no gate sees it. The seam's real value here (rAF ownership + visibility-pause) is correct. NO-WAVE-OWNER (make the adapter opt-in at the seam).
- **L-17 — CONFIRMED (MINOR).** The progressive hint discloses on a bare tap (pointerdown, no movement threshold) against the comment's "once the first drag settles"; the house pattern (`useDoubleTap`'s 12 px `moveTolerance`) is one file over and unused. NO-WAVE-OWNER (rides D-5's disclosure cure).
- **L-18 — CONFIRMED (MINOR).** `onBeforeUnmount` in `useSquareKeyboard` against the folder's `onScopeDispose` idiom — and the repo's own suite calls it bare inside `harness()` with no scope (my read of the test file: the keyboard cases are bare; only the `useSquareDemo` case wraps in `effectScope`). NO-WAVE-OWNER (one-line swap).
- **L-19 — CONFIRMED (MINOR).** The cure this scene exists for has no regression test: 98 lines cover seven keyboard branches + one smoke construct; `num()`, `transformFunc`, `seatFromPose`, `frame()`, and all of `useSquareTumble` (including the throw sites) are untested — the exact S.G2 `"42px"` regression would ship silently. NO-WAVE-OWNER (scene-repair acceptance tests).
- **C-8 — CONFIRMED (MINOR; survives the L-20/C-7 kill as an independent discipline claim).** Root-barrel `import { Card } from "@mkbabb/glass-ui"` where `./card` exists and the sibling reaches for its subpath. NO-WAVE-OWNER (rides any subpath sweep).
- **MISS-2 (reader A, adopted; verified) — ADJUDICATED-CONFIRMED (MINOR).** The instrument's field frame is percentage-drawn (`inset: 12.5%`, quarter ticks; `TETHER_REACH = 38` plainly designed to coincide) while the subject's envelope is a fixed ±110 px — so the drawn field marks the reachable space on NO real viewport (coincidence only at a ≈293 px plate), and `useSquareKeyboard`'s stated purpose for the `c` egg ("REVEALING the bounded coordinate space the instrument field draws") fails on every one. The other half of D-1's px-vs-% disease. NO-WAVE-OWNER (instrument packet, one motion with D-1); rendered mismatch → SS-13.
- **MISS-3 (both readers, adopted; verified verbatim) — ADJUDICATED-CONFIRMED (MINOR).** `useSquareKeyboard`'s docblock asserts the precise opposite of its own code: ":20-21 'The legs are paced by the spring's own settle, **not a fixed timer**'" sits 46 lines above `tourTimer = setTimeout(step, 520)` (:66), with the springs destructured into scope and never consulted for `.settled` — and the inline comment at :62-65 quietly admits it. Beyond prose: open-loop pacing cuts short any leg whose spring has not settled. The cleanest instance of the corpus's own §7 false-invariant thesis, absent from its list. NO-WAVE-OWNER (settle-paced legs or an honest docblock).
- **MISS-4 (reader B, adopted; verified) — ADJUDICATED-CONFIRMED (MINOR).** `colorAt` re-parses two STATIC colour strings per tumble frame: the stops are resolved once at mount, yet every call runs `asColor()` twice + mix + serialize (four Result allocations per frame, ~120 frames per tumble). Parse-once/interpolate-many is the library's own idiom; the showcase demonstrates the inverse — and the per-frame parse is what moves the C-9 throw from a survivable init site into the loop-bricking frame path. Cure is one hoist. NO-WAVE-OWNER (colour packet).
- **MISS-5 (reader B, adopted; hand-verified) — ADJUDICATED-CONFIRMED (MINOR).** The tour's five colour stops are raw hexes belonging to NO `--rainbow-*` token, under the :332-337 comment claiming "the sanctioned `--rainbow-*` family": `--rainbow-violet` = hsl(300 75% 60%) ≈ #E64DE6 ≠ #C462D8 (my own HSL→RGB derivation), and no family member resolves to #5AC8FA or #3DD0C4 at all — the exact drift hazard the file's :150-157 comment describes eliminating persists five stops later in the same module. Becomes the visible palette the moment C-1 is cured. NO-WAVE-OWNER (colour packet).
- **MISS-6 (reader B, adopted; verified) — ADJUDICATED-CONFIRMED (MINOR).** The scene declines the library's `Vars<V>`/`TransformFunction<V>` generic: `Vars<T = any>` (types.ts:39, my read) leaves every leaf `any`, so the one primitive under proof carries zero compile-time contract — in a file `tsc --noEmit` DOES check today (tsconfig includes `demo/`, my read). One `interface SquareVars extends Vars` puts the authored corpus and the renderer under a checked contract; the declined check is precisely the defect class the T.A13 runtime cure (`num()`) exists for. NO-WAVE-OWNER (scene-repair; a KF.W4 rider notes the free win).
- **MISS-7 (reader B, adopted; verified) — ADJUDICATED-CONFIRMED (MINOR).** `useSquareDemo` returns `startLoop` (:403) and no caller consumes it (the SFC destructures ten of eleven — my read of :132); dead composable surface one token from the C-10 decoy in the same statement. NO-WAVE-OWNER (hygiene).
- **ARB-1 — this seat's own net-new finding — ADJUDICATED (MINOR).** **The celebrated takeover is unidirectional: the drag→playback edge has no pose adoption.** `resume()` un-pauses the group at its paused clock (`scenePlaybackAdapters.ts:95-98`, my read), and a first Play starts the tour at the 0% home pose — in both cases the next engine frame paints the tour pose while the box sits wherever the persist-policy drag left it: a one-frame snap of up to ~2×TRAVEL (≈220 px) plus a rotation appearing from nowhere, the mirror of L-5's MAJOR. The scene's own prose asserts the contrary ("±90px sits INSIDE the ±110px envelope so drag and playback share ONE coordinate world (the pose-capture takeover is seamless)", useSquareDemo.ts:338-339; "the group stays paused (Play resumes the tour from here)", SquareScene.vue:276-277 — "from here" is false: it resumes from the paused tour clock, not from the dragged pose). Graded MINOR, not MAJOR: an explicit Play press is a mode-change command where a discontinuity is more conventional than under an implicit grab — but it is a discontinuity the scene's own thesis forbids, and no axis or reader examined the reverse edge. Cure options for the spec: seat the TOUR from the pose (the engine's adopt/temporal-takeover idea the scene name-drops at :301), or spring the box home before starting. NO-WAVE-OWNER (playback-authority packet, one motion with L-5); snap magnitude → SS-13.

### INFO

- **D-23 (+ the collision sharpening) — CONFIRMED.** `--size` is a live flat-namespace instance (`.demo-box` vs the global `.icon`); reader A's second, sharper instance verified by this seat: **`--rainbow-{red,orange,yellow,green,blue,violet}` are declared by BOTH installed glass-ui 7.0.0 (`dist/styles/tokens/scale-paper.css`, oklch) and the demo (`design-idioms.css`, hsl, unlayered-and-later)** — a live same-name `:root` collision held only by import order, feeding `useSquareTumble`'s `getComputedStyle` read straight into `parseCssColor`. Sharpens lane-frontend §6.3. **KF.W6.**
- **D-24 residue — CONFIRMED (INFO; sub-claim killed, phantom framing dead).** The plate's documented `tier="resting"` reaches the DOM only through the producer's two-hop `material→tier` default map; render correct today. NO-WAVE-OWNER (prose or explicit props).
- **D-25 — CONFIRMED.** The plate title hardcodes "Transform" beside `anim.name = "Transform"` — two sources, one identity; and it names a CSS property, not the subject. NO-WAVE-OWNER.
- **D-26 — CONFIRMED (INFO; A's arithmetic correction adopted: 4.62:1, ~3% less headroom than filed).** The only live numeral clears AA narrowly in the light arm; watch the margin. Composite → SS-13 with D-11.
- **L-22 — CONFIRMED.** (a) The spring loop clears the engine's fill-forwards background keyed on the EGG's spin state, not paint authority (:234-236 — any drag after a paused tour discards it; masked by C-1); (b) forward `const` references safe only while nothing invokes during setup; (c) 60 Hz instrument props honestly documented, self-terminating. NO-WAVE-OWNER (a rides the colour packet).
- **MISS-8 (reader A, adopted; verified at all five sites) — CONFIRMED (INFO).** "The red motion-authority" names a violet token five times in the files that draw it (`--color-progress` → `--accent-kf` = oklch violet in both arms; the demo's own style.css records the deliberate repoint away from red and reserves `--accent-red` as destructive-only per banked kf-MbabbMenu). Load-bearing prose for future hue choices. NO-WAVE-OWNER (prose correction).
- **MISS-9 (reader A, adopted; verified) — CONFIRMED (INFO).** `will-change: transform` unconditional on a subject idle by construction (SquareScene.css:65; the loop self-terminates and the file's own docblock says so) — the inverse of the banked cube contract (kf-CubeScene L-2/C-5's gated promotion, where the bug is the gate never opening). The file already has three attributes to key on. NO-WAVE-OWNER (rides the FSM repair).
- **MISS-10 (reader B, adopted; verified) — CONFIRMED (INFO).** `dispose()` abandons every piece of DOM state it wrote onto a borrowed element (`transform`, `--spring-tilt`, `--spring-squash`, `backgroundColor`, `data-palette-sweep`) against its own stated ownership posture; benign today, inherited by any KeepAlive/portal future. NO-WAVE-OWNER (hygiene).

## Killed-claims register (6)

1. **L-20/C-7 (+ D-24's phantom-dep premise; all three axes) — KILLED ON BASELINE, unanimous, corroborated by this seat's own diffs.** The glass-ui absence exists ONLY in an uncommitted worktree edit; `origin/master` declares `"@mkbabb/glass-ui": "7.0.0"` exact with full lock entries, matching the installed copy, and `npm ci` never resolves import specifiers anyway. **≡ census F-1/SCH-1**, ruled at kf-AmigaScene and re-ruled with the manifest trifecta at kf-CubeScene and kf-CopyButton — folded, never re-booked; residue → KF.W0 §B-12. Fifth occurrence of the corpus-wide lesson: *name the tree before you grade it.*
2. **D-2 as filed — KILLED (ruled above).** The installed producer ships the exact forced-colors restoration the finding declared absent, naming this component's class; the finding dies by its own falsifier. Residual outcome ≡ **RB-1 FOLD** (kf-RibbonBar); cure = delete `design-idioms.css:76-79`, never add a demo forced-colors block.
3. **Reader B's missed row 1 (the focus-ring duplicate as a net-new MAJOR) — BOOKING-KILLED as pre-banked.** Identity guard: the defect is RB-1 verbatim, four adopters already enumerated at kf-CopyButton K7. B's built-bytes cascade pin and cure correction are adopted as annotations ON the banked row; SquareScene is recorded as RB-1's sharpest witness (the only `.focus-ring` adopter that is a scene subject).
4. **D-24/L-21's ":shadow=\"false\" is redundant" — KILLED by this seat's own probe (ruled above).** Card's compiled default is `true`; both axes read Surface's prop table. The attribute is load-bearing.
5. **L-11's "never reaches green at t=1" leg — KILLED (both readers, adopted).** The mix converges on `hues[2]` continuously to within 0.02% — arithmetically true, perceptually vacuous, and it misdirects the cure toward a clamp. The real landing defects are the token drift (b) and the overshoot wrap (a), both confirmed.
6. **D-1's z-mechanism sentence ("same rung, telemetry later in DOM") — MECHANISM KILLED, outcome unchanged (reader B, verified).** The tether carries NO z-index; the strip wins by an explicit rung over `auto`. Matters because the filed reading implies a DOM-reorder cure that would not work.

**Preserved kill-records (not new kills):** axis C's four §5 self-kills (the Card-prose convention; the spring auto-resume that cannot fire — promoted to superlative; the idempotent double-dispose; the `anim.name` ordering) — all standing, and the parent did not re-spend the cycles; reader A's C-5(a) count-abstention, superseded by B's derivation.

## Identity-guard folds (never re-booked)

- **L-20/C-7 ≡ census F-1/SCH-1** (banked kf-AmigaScene, trifecta at kf-CubeScene/kf-CopyButton) → KF.W0.
- **D-2 residual ≡ RB-1** (banked kf-RibbonBar; adopter census at kf-CopyButton K7) — SquareScene recorded as the sharpest witness; forced-colors render for all four adopters rides that row's SS-13 line.
- **The PRM-inert rider ≡ RR-1** (banked kf-ControlsPaneWrapper) + the universal-reset decider banked in kf-CubeScene's D-16 ruling — square-local consequence recorded inside the D-3 row, no new row.
- **The demo-wide forced-colors categorical gap ≡ KF-CE-13** (banked kf-CSSCodeEditor) → KF.W9.
- **L-8/C-2 cross-refs kf-CubeScene L-2/C-5** (the `isPlaying` shadow family; that banked row already orders the SquareScene prose correction) — the square's dead FSM books here; the cure is ONE playing-state authority, settled together.
- **L-14's enabling condition ≡ KF-APP-4** (banked → KF.W4: vue-tsc + `noUnusedLocals`); this record adds the square's witnesses.
- **The R1 parser crash-class note inside D-27/L-7/C-9 ≡ the megatranche R1 identity** (V·π parser program) — annotated, never re-booked.
- **MISS-1's class precedent = GradientStopEditor C11/G5 + kf-AnimationVisualizer's grabOffset row** — different components, so the square row books NEW here; the precedent fixes the cure shape (`grabDx`), not the identity.

## Superlatives (provenance: corpus → readers → this seat)

1. **One loop, one paint authority — verified IN the library, not trusted from the comment** (C S-A / L S★-3, both readers re-derived; spot-checked). Four transform writers compose into one string through one self-terminating rAF; the spring auto-resume cannot fire because `.play()` is never called.
2. **The only end-to-end `unflatten` consumer in the demo, routed exactly right** (L S★-1 / B S-2, three-file chain verified) — genuine, non-decorative coverage of a feature nothing else exercises; that the pinning mechanism is wrong is L-13's separate charge.
3. **`parseCssScalar` at the two-writer boundary** (C S-C) — the demo's best use of value.js and the counter-example to the library's own internal regex re-parsing; worth citing upward when the parser wave lands. (Its unit-blindness is L-10; the shape is still right.)
4. **The LIGHT/HEAVY engine split held perfectly** (B S-1) — every value.js-bearing constructor behind the warmed accessor, zero deep imports, at the scene with the demo's hardest consumption profile.
5. **Three springs off one foreign clock, structurally safe** (B S-4, net-new by that reader) — closed-form analytic stepping makes the big-dt hazard absent by construction, and `onArm`'s clock rebase closes the phase gap.
6. **Typography is 100% published rungs with the scoped blocks deliberately emptied of type** (C S-B) — zero `font-*` declarations across both style surfaces; the one override lives at the root in a declared layer.
7. **The ink is derived from the fill, not picked beside it** (D L-1) — `color-mix(… 25%, black)` over the same token: ≈11:1, AAA in both themes, drift-proof by construction; the comment understates it.
8. **The badge AA-by-construction recipe survives independent computation** (D L-2) — 7.84:1 / 5.02:1 at the 10 px rung; a parameterised colour invariant that actually holds.
9. **The touch contract is right where it is easy to get wrong** (D L-4 / C S-D) — 192 px target, `touch-action` on the right element, capture-throw fallback, `pointercancel` sharing the end path, drag-disjoint double-tap by construction.
10. **`seatFromPose`'s matrix read is exact and its failure mode documented** (L S★-2) — translation leftmost, so `m41/m42` ARE `tx/ty`; the KEEP-annotated catch. Its incompleteness is L-5; its correctness stands.
11. **Teardown is belt-and-braces and provably idempotent, each owner disposing its own** (L S★-4) — including the tumble spring disposing itself.
12. **Producer superlative (reader A's, and the fact that killed a corpus BLOCKER): glass-ui 7.0.0 got forced-colors AND its cascade layering right** — base rule inside `@layer components`, the nine-selector restoration unlayered so it wins by construction; only the demo's redundant re-declaration defeats it (RB-1). Also standing: progressive disclosure correctly staged (D L-5 — the instinct survives the D-5/L-17 gate fixes).

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **C-1 pixel witness** — double-tap, sample the box centre with `data-palette-sweep` set; if the pixel tracks the swept hue the row dies (it will not — occlusion is spec+source-certain).
2. **C-5(a) rendered flicker** — post-C-1-cure ONLY; six wrap crossings derived, visibility unmeasured.
3. **L-2 downstream paint** — what the default renderer paints for a nested leaf after `adoptCompiled`; the transform loss is statically certain.
4. **L-5 + ARB-1 snap magnitudes** — the mid-tour grab un-rotation and the reverse-edge resume snap, at real takeover instants.
5. **MISS-1 teleport magnitude** — live `getBoundingClientRect` at a real plate; the 96/136 px figures are root-16 arithmetic.
6. **MISS-2 field/envelope mismatch** — measure the rendered `.stage-cell` at desktop and 390 px against the ±110 px envelope.
7. **D-8 amputation render** — 375/390 px viewports during the tour and a full-travel drag.
8. **D-11/D-26 true composited backdrop** — the glass plate at `data-tier="resting"`; direction already settled (no plate tone rescues D-11).
9. **D-4 which-ATs** — whether any real AT polls `aria-valuenow` on unfocused slider children of a focused group.
10. **D-16 perceptual lag** — re-derived under the producer PRM override (0.1 s, not 160 ms); plus **L-13's grouped-path paint** if `singleTarget` ever reverts.

## Wave routing summary (M-25 agglomeration input)

- **KF.W0** — the substrate/manifest fold (L-20/C-7 residue; the uncommitted devDep deletion; re-anchor law for every cite in this record).
- **KF.W4** — L-14/C-13 gate fold (≡ KF-APP-4: vue-tsc + `noUnusedLocals`; the square's dead import + phantom-expose prose are its witnesses) + MISS-6's free-win rider (`useSquareDemo.ts` is already tsc-checked; the `Vars` interface costs nothing).
- **KF.W6** — D-23's two token-collision instances (the `--rainbow-*` producer/demo `:root` collision is the sharpest §6.3 witness yet) · D-18 rides.
- **KF.W9** — D-3/L-12/C-6 + the PRM-inert rider (PRM mechanism unification, intensity form; square = 0 of 4 motions gated) · the forced-colors demo-wide gap ≡ KF-CE-13 · RB-1's four-adopter render witness · the SS-13 roster above.
- **NO-WAVE-OWNER (the square scene-repair wave the SS-1/SS-2 authoring must create)** — the **editor-seam packet** (L-2 BLOCKER: preserve-the-renderer at `useKeyframeOps`/`adoptCompiled`, + the `RAFPlayback` degrade-not-wedge rider from D-27/L-7/C-9); the **playback-authority packet** (L-1/C-3 · L-8/C-2 with the kf-CubeScene cross-ref · C-4 · L-3 · L-5 + ARB-1 · D-14/L-9 · MISS-9); the **colour packet** (C-1 · L-11/C-5 · MISS-4 · MISS-5 · L-22a); the **instrument packet** (D-1 + MISS-2 px-space geometry · L-4/C-12 · D-9 · D-21 · D-15); the **input/a11y packet** (MISS-1 grabDx · D-4 · D-7 · D-5 + L-17 · D-6/L-6 · D-19 · D-11 · D-12); **geometry** (D-8); and the **hygiene tail** (D-10 · D-13 · D-16 · D-17 · D-20 · D-22 · L-10 · L-13/C-11 · L-15 · L-16/C-10 · L-18 · L-19 · C-8 · MISS-3 · MISS-7 · MISS-8 · MISS-10 · D-24/D-25 · L-22b/c).

## Closing verdict

**ADJUDICATED.** 56 rows confirmed (1 BLOCKER · 16 MAJOR · 31 MINOR · 8 INFO), 6 claims killed, 11 net-new misses adopted (5 reader-A, 5 reader-B, 1 this seat's ARB-1; one reader miss booking-killed as pre-banked, one folded as a rider), 10 residues to SS-13. Two of the corpus's four BLOCKER-weight claims died or demoted on evidence (D-2 by its own falsifier at the installed producer; C-1/D-1/L-1 by the house severity ladder), and the one that survived — the editor round-trip that silently eats the scene's custom renderer — is a seam defect wearing a component's name. The component's real condition: the demo's best-composed gesture and loop architecture (twelve standing superlatives, one of them the producer's) whose every serious defect is a **declared invariant the tree does not honour** — the FSM's third state, the two-writer guarantee, the seamless takeover (in both directions), the seamless landing, the settle-paced tour, the sanctioned palette, the unit-honest normalizer, the red that is violet. The docblocks are the unit's least reliable artefact, and the repair wave should treat every comment-stated invariant as a test obligation (L-19), not documentation. Nothing here stamps a wave: X·KF specs are forming, and this record is their SquareScene intake.

— end of apotheosis. The challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/square/SquareScene.css` · `demo/scenes/square/SquareScene.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SquareScene.css=IDENTICAL · SquareScene.vue=+2/-2**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` appears in this record but NOT in the re-anchor sense** — it is a component-local id here, and the record-qualification rule (KF-W0 §Carry C-1.R row 6) keeps this sweep from booking a dead-token cluster as a re-anchor obligation. Eight records are in this class: kf-ChannelOptions · kf-CubeScene · kf-KeyframesStringControls · kf-OrbitalDrag · kf-SquareScene · kf-SquareInstrument · kf-StartingStyleTarget · kf-TimelineCaret.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

