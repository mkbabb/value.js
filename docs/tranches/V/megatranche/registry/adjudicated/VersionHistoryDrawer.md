# VersionHistoryDrawer — adjudicated apotheosis (M-23/L-18, fresh Fable seat) · served model: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (M-23/L-18) · **Date**: 2026-08-07 · **Re-verification HEAD**: `024b103e` (branch `tranche-u`; the corpus's cells were cut in the `c654824e` era — every line anchor this seat re-used resolved byte-identical, and the D-19 law still applies: re-resolve anchors before executing any cure).
**Corpus**: `audit/components/VersionHistoryDrawer/` — challenge-C-implementation.md (r2 governs), challenge-D-design.md, challenge-L-library.md (+addenda A-0..A-6), evidence-C/C2/D, `shots/`, and the standing receipt `repro-L1-first-open.mjs`.
**Readers adjudicated**: two hostile re-readers — reader-1 (design-and-usage) · reader-2 (library-and-code). Both were attacked before either was adopted: every verdict-level disagreement was re-derived against the tree with this seat's own instruments, three of the readers' own CONFIRMED evidence cells were killed (K-11..K-13), and two of their kills were re-proven rather than trusted.
**Method (probe parsimony)**: zero browser probes. Instruments this seat ran: the repro receipt re-executed (`node repro-L1-first-open.mjs` → `FIRST open → loadVersions calls = 0 … EXIT=1`); full source reads of `VersionHistoryDrawer.vue` (whole, 169 lines), `useVersionHistory.ts` (whole), `BrowsePane.vue:150-300`, `PaletteCardMenu.vue:68-105`, `dateFormat.ts` (whole), `demo/palettes/api/versions.ts` (whole), `transport/client.ts:88-127`, `useContrastSafeColor.ts:330-362`, `usePalettePorts.ts:20-31/:155-192/:244/:273`, `useDialogBrowseActions.ts:30-80`; api reads: `service/versions.ts` (whole), `repository/paletteVersion.ts` (whole), `routes/versions.ts` (whole), `service/crud.ts:95-224`, `routes/crud.ts:60-95`; installed glass-ui 7.0.0 dist: `dialog-TNRDkcE4.js` (`scroll: { type: Boolean, default: !1 }`, ungated `"data-scroll"` stamp, the `te` content-region wrapper, DialogHeader `text-center sm:text-left`, DialogTitle `text-subheading` default), `components/dialog/placement.css` whole (unlayered import verified against `styles/index.css`'s `@layer theme, base, components, utilities` + `layer(components)` on components.css), `components/button/styles.css` (`@layer components`, `min-block-size: var(--button-size)`, gap token, svg glyph rule, `[data-loading]`), `components/button/Button.vue.d.ts` (no `variant`), `button-Bu9F4uU6.js` (`emphasis: { default: "secondary" }`, `aria-busy` stamp), `styles/tokens/sizing.css` (`--control-h-sm` = 36px at scale 1), `styles/typography/semantic.css` (`text-caption { … font-style: italic; font-weight: 400 }`), `styles/typography/scale.css` (`--type-caption` clamp arithmetic reproducing D's 12.03/12.18/14.38 ladder exactly), `styles/components.css` (the single `hover:hover`-gated reveal rule), `styles/utilities/base.css` (`.interactive-item` recipe); repo censuses: `group-hover:opacity-100` = **4** sites, `variant="outline|ghost|destructive|secondary|link"` = **27 files / 65 lines** in demo SFCs vs **2** `emphasis=` call sites, `demo/ui` = 19 barrels / 0 non-index files, `demo/palettes/export.ts` + 12-file `export/` with **no index.ts**, `eslint.config.js:230-250` + `ls demo/@` (absent), `test/demo` = 1 file / `demo/test` = 3 files (none versions), `node -e` invalid-date → `"Invalid Date"` (no throw); authorities: VISUAL-CONSTITUTION §4/§4.1 (`:68-90`), `demo/DESIGN.md:94-102` (well law), `demo/color-picker/index.html:1-12` (RTL seam), `animations.css:184-193` (PRM guard 1); banked frames read by this seat: `shots/01-desktop-light-populated.png`, `shots/02-row-hover-revert.png` (the italic "Revert" and the filled — not outline — capsule are visible in the raster).
**Wave authority for dispositions**: `registry/CARRY-CUT-LEDGER.md` §2 + `docs/tranches/X/waves/W1/W3/W4/W6/W7/W8/W10.md`, read this seat. Routing facts that bind the roster: **X-W4 holds `VersionHistoryDrawer.vue`** (`W4.md:127`, the `h-7`-family size-axis extirpation + the two `a11y-control-targets` specs, `:236`); **X-W7.d moves "versions" into the selected-entity inspector** (`W7.md:222`) and holds `useVersionHistory.ts`, `BrowsePane.vue`, `PaletteCardMenu.vue` (`:117/:107/:100`); **X-W3** already carries the versions API triad born-RED (G-3 unauthenticated list · G-6 cross-object revert transplant **P0** · G-7 release-identity rewrite · G-9/G-11 write contract); **X-W8** owns barrels/config/shadcn residue; **X-W10** owns the design canon. The drawer as a chassis is therefore scheduled to be **recomposed** at X.W7.d — chassis-level design rows land at that recomposition under X-W10 law, with X-W4 holding the SFC in the interim; the two waves must coordinate the carve (named-gap flags below).
**Motion law (M-rows)**: `audit/codex-provenance/motion-quarantine.md` covers the 46 REPORT-AUTHORED files; **no M-xx row names this component** and no FALSE row is carried here. The corpus's two motion claims (`animate-spin` neutralised; `transition-opacity/colors` on producer tokens) ride Guard 1 (`demo/styles/animations.css:184-193`, import chain verified in the quarantine receipt §1) and are retained as checked-negatives, not defects.

**Status verbs**: every disposition below is **ADJUDICATED, not VERIFIED** — nothing in this file stamps a wave.

## Scoreboard

**42 rows CONFIRMED (2 BLOCKER · 22 MAJOR · 15 MINOR · 3 INFO) · 13 rows in the killed register (10 reader kills sustained — every load-bearing one re-derived, not trusted; 3 NEW kills by this seat against the readers' own CONFIRMED cells) · 13 missed-by-the-axes findings adjudicated (readers' union; all confirmed, 4 folded into sibling rows) · 9 UNPROVEN-NEEDS-LIVE rows → visual-audit inputs.**

Blocker-weight survivors: **VHD-1** — the component's first open in any session never fetches (re-proven by execution in this seat: `EXIT=1`), and **VHD-2** — the component's only action is invisible at rest, un-revealable on every coarse-pointer device, invisible to the keyboard, and still tab-reachable and tap-hittable while wired to an unconfirmed destructive write.

## Ruled disagreements (this seat's actual work)

| # | contest | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| R-1 | C-3/D-02 severity: reader-1 BLOCKER vs reader-2 MAJOR | **BLOCKER** | Capability-zero for an entire input class plus a live hazard: the compiled reveal is the file's ONLY `hover:hover` rule (my grep of served `styles/components.css` → 1 hit), `.opacity-0` is paint-only (hit-testing and tab order intact, no `pointer-events`/`tabindex`/`aria-hidden` at `:75-84`), and the control it hides fires an unconfirmed destructive POST (VHD-3). A component whose whole action surface does not exist on touch AND remains invisibly tappable is graded on the compound, per the AdminTagsPanel ATP-2 precedent. |
| R-2 | D-04: challenge-D + reader-1 BLOCKER; reader-2 omitted the row entirely | **CONFIRMED, DEMOTED to MAJOR; the "irreversible" cell KILLED (K-11)** | The claim's irreversibility limb is refutable from the same service source both readers cite: the overwritten current state is itself a version row (recorded at its own attributed create/patch — `service/crud.ts:204-216`, `service/versions.ts:32-75`), the drawer lists it, and revert requires ownership, so the actor can revert back. Recovery exists in-product; it is non-total only at VHD-34's unattributed edge, and D-05 hides the path. Unconfirmed destruction stands (X-W7's own law: "nothing destroys on a single unconfirmed click"); the grade follows the corrected facts. Reader-2's silent omission is recorded as a coverage gap, not a dissent. |
| R-3 | D-05 severity: challenge-D + reader-1 BLOCKER vs reader-2 MAJOR | **MAJOR** | The impossible-history render is real and confirmed by construction (only two `loadVersions` call sites, neither fires on revert; `BrowsePane.vue:277-284` moves only `currentHash`). But it is owner-gated (requires a successful revert), and it **self-heals on close/reopen** — the reopen path works precisely because `versionPalette` stays truthy and `open` transitions (the same mechanism that makes VHD-1 first-open-only). A recoverable rendered lie behind an owner action is MAJOR; the unrecoverable-without-ritual first-open blank keeps the BLOCKER seat. |
| R-4 | C-11/D-10 severity: reader-1 MAJOR vs reader-2 MINOR | **MAJOR** | The invented ordinal (`v{{ total - i }}`, `:37`) is the row's primary identity and it feeds a destructive action: the user picks *what to revert to* by that label, and the component's own confirmed defects (VHD-4 stale `total` after revert, VHD-12 count drift) guarantee it goes wrong in real flows, up to `v0`/`v-1`. Mislabeling the input to destruction is MAJOR. Reader-2's demotion preserved as dissent. |
| R-5 | Reader-1's CONFIRMED C-4 cell: "The transport offers no seam either — client.ts builds its RequestInit without a signal" | **CELL KILLED (K-12) — citation-inheritance event** | `transport/client.ts:94-99` `RequestOptions extends RequestInit` (which includes `signal`) and `:115-118` spreads `...init` into the fetch — the seam exists and is open today. Reader-2 killed this exact falsehood in challenge-C (its K "C-4 seam limb"); reader-1 repeated the challenger's cell inside a CONFIRMED verdict without re-deriving it — the ColorSpaceSelector DISSENT §1 event class, recorded here as evidence about the fleet. C-4's verdict survives (VHD-9); its cure relocates to `demo/palettes/api/versions.ts` + the composable (2-line signature change), not the transport. |
| R-6 | Reader-1's CONFIRMED D-16 cell lists "no `safeCss`" among the swatch indictments | **CELL KILLED (K-13)** | `useContrastSafeColor.ts:334-359`, read this seat: `safeCss` is a WCAG **ink** certifier against the host surface (`certifyAccentInk`, 4.5/3.0 floors) — applying it to a swatch FILL would shift the very color the swatch exists to display. The canonical swatch (`PaletteColorStrip.vue:18-22`) paints `backgroundColor` raw. Reader-2's kill sustained and now applied to reader-1's cell; D-16's fork/AT limbs survive (VHD-21). |
| R-7 | A-3's "wholly inert" vs the glob object's actual contents | **CONFIRMED with precision** | `eslint.config.js:232-239`, read this seat: the `files` array carries **live** `demo/color-picker/**` globs beside the 4 dead `demo/@/**` globs — the OBJECT is not inert; the RULE is: its only banned pattern (`@components/custom/palette-browser/**/*.vue`, `:247`) is a W43-deleted alias with zero live resolutions, so the boundary ban can never fire. Same precision the ColorSpaceSelector L-5 ruling recorded (F's correction). One identity with ActionFeedback L-2 / ColorSpaceSelector L-5(a) — never re-booked as a new defect. |
| R-8 | challenge-D's D-09 evidence cell says the invisible band is "`h-9` (36)" | **Mislabel corrected in place** | The class on the button is `h-7` (28px); the rendered 36px is `.button`'s `min-block-size: var(--control-h-sm)` = `max(2.25rem × var(--ui-scale), 0px)` flooring it (my dist reads). 36 + `mt-2` 8 = the 44.0px D measured. The finding (two rhythms, 34% void per row — visually confirmed in `shots/01`) survives intact; the cell's class attribution was wrong, same mechanism family as K-2. |
| R-9 | L-8 census: challenge-L printed 2 sites; reader-1 said "three siblings" + the drawer; reader-2 said four | **Four, exactly** | My grep this seat: `ImageDropZone.vue:58` · `MixSourceSelector.vue:153` · `AdminTagsPanel.vue:99` · `VersionHistoryDrawer.vue:79`. Three of four carry a keyboard reveal; the drawer is the sole omission. The readers agree in substance; challenge-L's printed evidence stays killed (K-6). |

## Adjudicated defect roster

Severity-ranked. Axis ids preserved for life; `VHD-n` is the registry handle. Dispositions name the owning X wave or `NO-WAVE-OWNER`; `GLASS` = producer-owned, rides the standing glass-ui BH relay (owner edict).

### BLOCKER

- **VHD-1 (C-1 / D-01 / L-1 / A-0) — the first open never fetches; the panel asserts "— 0 versions" over an empty body until closed and reopened.** RE-PROVEN BY EXECUTION this seat: `node …/repro-L1-first-open.mjs` → `after FIRST open → loadVersions calls = 0 (expected 1) / after SECOND open → 1 (expected 2) / EXIT=1`. Mechanism re-read whole: `VersionHistoryDrawer.vue:158-167` is `watch(() => open, …)` with no `immediate` and no init call; `BrowsePane.vue:272-275` writes `versionPalette` and `versionDrawerOpen` in ONE synchronous handler against the `v-if="versionPalette"` mount (`:157-165`), so the first render already has `open === true` — no transition to observe. `versionPalette` is never nulled (sole writes `:273`/`:283`), which is why only open #1 is broken: the failure a developer structurally cannot see. Three axes + both readers + this seat's run agree. **Disposition: BUILD → X.W7.d** (the version surface's state machine — `useVersionHistory.ts` and `BrowsePane.vue` are in W7's modify list; G13's mutation table carries the "version" row). **Named carve**: the SFC itself is X-W4 property (`W4.md:127`); whichever wave lands second inherits the seam — recorded so neither wave's gate goes green over the other's edit.
- **VHD-2 (C-3 / D-02 / L-8; D-09 folds in) — the sole action is `opacity-0` at rest, revealed only inside `@media (hover: hover)`, focus-invisible, and still tab-reachable and tap-hittable; its invisible 44px band also gives 23 of 24 rows a second rhythm.** Compiled-CSS proof re-read this seat: the served `styles/components.css` contains exactly one `hover:hover` occurrence — `@media (hover:hover){.group-hover\:opacity-100:is(:where(.group):hover *){opacity:1}}`; `.opacity-0` is unconditional; `:79` has no `focus-visible` arm, no coarse-pointer arm, no `tabindex=-1`/`aria-hidden`. The cured idiom ships at 3 of the 4 in-repo sites (R-9). D-09's band: 36px floored button + `mt-2` = 44.0px in-flow void on every non-current row (`:76` v-if makes the current row the exception) — visually confirmed in `shots/01`. Severity ruling R-1. **Disposition: BUILD → X.W7.d** (the reveal/confirm seat is re-authored at the inspector recomposition under G13/G14 law — rest-visible, named, confirmed; D-09 dies in the same edit). Interim floor: X-W4's file touch (its own scope: size axis + a11y-control-targets) may land the `focus-visible:opacity-100` + coarse-pointer arm ahead of the recomposition; the carve note of VHD-1 applies.

### MAJOR

- **VHD-3 (D-04, demoted per R-2) — revert fires on a single activation with zero confirmation.** `:75-84` — the Button's only handler is `@click="$emit('revert', version.hash)"`; `BrowsePane.vue:277-284` calls `pm.versions.revert(...)` immediately; server-side it is a transactional `$set` of a published palette (`service/versions.ts:146-176`). No AlertDialog, no undo surface. The irreversibility cell is killed (K-11) — recovery exists via the log itself — but X-W7's G14 law ("nothing destroys on a single unconfirmed click") binds. **Disposition: BUILD → X.W7.d/e** — extend the G14 destructive-seat class beyond the four Admin seats to the version-revert seat (`dismiss="deliberate"` + `tone="destructive"` composition); **named gap**: W7.e's file list does not yet include the versions surface — the recomposition (W7.d) must adopt the seat or this row degrades to NO-WAVE-OWNER at execution.
- **VHD-4 (D-05, demoted per R-3) — after a revert the list is never refetched; the panel renders a newer version above `(current)` with the revert event itself absent.** Confirmed by construction this seat: `loadVersions` has exactly two call sites (the non-firing watcher `:158-167`, `loadMore` `:153-155`); `onRevert` moves only the `:current-hash` prop; both old/new `versionPalette` truthy ⇒ no re-mount. **Disposition: BUILD → X.W7.d** (G13: the version mutation's one visible result IS the refreshed history; pairs with X-W3 G-11's `201`-with-appended-revision so the client has a truthful payload to render).
- **VHD-5 (C-6 / D-07-width / L-9) — `w-[380px]` and `sm:max-w-[420px]` are dead: glass-ui's dialog placement CSS ships unlayered and outranks every `@layer utilities` rule.** Verified in dist this seat: `styles/index.css` declares the layer order and imports `../components/dialog/placement.css` with NO `layer()` (contrast `./components.css` `layer(components)` on the same line); `placement.css` pins `width: 75%` + `max-width: 24rem` (= the measured 384/292.5px). The declared 380–420px sheet has never rendered. **Disposition: split** — the consumer's dead classes delete in X-W4's file touch; the producer facts (unlayered placement + the missing `size` axis, A-1's surviving half) are **GLASS** — one BH-relay letter carrying BOTH the layering question and the size-axis ask (L-9's four-dialect census rides along). K-2 binds the relay: the `h-7` limb is NOT part of this mechanism and must not ride that letter as if it were.
- **VHD-6 (C-2) — revert is offered to users who provably cannot use it, and the refusal is silent through two swallow layers.** Verified: `PaletteCardMenu.vue:94` gates Versions on `!isLocal && versionCount > 1` with no `isOwned` (its neighbours Rename `:74` / Edit Tags `:84` both check); the drawer receives no owner identity (`:118-123`); `routes/versions.ts:49-51` enforces `requireOwnership`; the 403 dies at `useVersionHistory.ts:88-95` (`console.warn` → `undefined`) then `BrowsePane.vue:280` (`if (!updated) return`). **Disposition: BUILD → X.W7** (the menu gate in W7's `PaletteCardMenu` carve + the 31-site `catch → console.warn` table, W7 scope 3; the feedback rail `showFeedback` already exists at `BrowsePane.vue:262-264`).
- **VHD-7 (C-5 / D-06 / L-12; VHD-34 feeds it) — loading, error, empty, and never-fetched all render one identical blank while the subtitle asserts "0 versions" as fact; no live region anywhere.** Verified: `useVersionHistory.ts:56-62` swallows transport failure to `undefined`; `:141` `if (!page) return` inside `try/finally` leaves the exact success-shape-for-empty; `:11` publishes the count before it is known; template has zero `aria-live`/`role="status|alert"`. This seat's extension: the watcher (`:162-163`) zeroes `versions`/`total` at every open, so the subtitle asserts "0 versions" during every load, not only on failure. The named cure is one import away and already in the host's imports: `EmptyState.vue` (species law S.W5-5, error≠empty, `role="alert"` + Retry). VHD-34 makes the blank four-way ambiguous — the strongest argument for discriminated states. **Disposition: BUILD → X.W7.d** (G13 visible-result + the EmptyState species law).
- **VHD-8 (C-7 / L-2 / D-17) — the paging state machine exists twice; the composable built to own it has 7 of 10 exported members with zero consumers; the shipped copy is the broken one.** Verified whole this seat: `useVersionHistory.ts:65-86` is line-for-line the drawer's `:137-155` (page size 20, replace/append split, try/finally); consumers = `fetchVersions`/`revert`/`fork` only; the escape hatch is documented at `:27` ("raw; drawers manage their own list") and the drawer's justifying comment `:130-131` is false (one v-if-gated instance, `BrowsePane.vue:157`). D-17's "11 members" stays killed (K-10: it is 10). **Disposition: BUILD → X.W7.d** (one owner: the composable's machine becomes the only machine; the drawer becomes props/render).
- **VHD-9 (C-4, cure relocated per R-5/K-12) — no request identity: no generation token, no slug check on the response, no abort; a late page writes `versions`/`total` for whatever palette it was issued for.** Verified: `:137-151` nothing between the await and the writes consults which request resolved; `paletteSlug` is a live prop the host reassigns (`:283`). The transport seam EXISTS (`client.ts:94-118` forwards `signal`); the gap is `demo/palettes/api/versions.ts:24-32` + `useVersionHistory.ts:51-63`, which accept and forward no init. **Disposition: BUILD → X.W7.d** (generation token or `AbortController` threaded through the feature api module; live interleaving witness = residue U-4).
- **VHD-11 (C-11 / D-10, severity per R-4) — version identity is invented from render position (`v{{ total - i }}`) while the record carries a real server ordinal the component never reads.** Verified: `:37`; `depth` computed and stored server-side (`service/versions.ts:46-58/:70`), mirrored in `demo/palettes/types.ts`; `total` is a separate `countByPaletteSlug` round-trip (`service/versions.ts:92-95`). D-10's Vue-warning limb stays killed (K-8). **Disposition: split** — the true ordinal becomes first-class at **X-W3** (G-7's release identity mints `revisionNo`); the client consumes it at **X.W7.d** (render the server ordinal, never arithmetic on `total`).
- **VHD-14 (C-9 / D-18) — the a11y bundle: no live region and no `aria-busy`; no list semantics; every action named the bare "Revert"; the load-more button unmounts under its own focus (`activeAfter: "BODY"` inside an open modal).** Verified against the template (`:17-19` bare spinner; `:22-27` sibling divs; `:83`; `:88-97`); the probe log corroborates. The negative-note kill (K-3) does not touch the verdict. **Disposition: split — BUILD → X-W4** (naming/list/`aria-busy`/`<time>` semantics: the file + the two a11y-control-targets specs are W4's, `W4.md:236`) **+ X.W7.d** (the focus-safe pagination and state machine).
- **VHD-17 (D-11b + reader-1 missed italic-controls + tabular-figures INFO fold) — the panel's entire type is set outside VISUAL-CONSTITUTION §4's closed matrix, and both interactive controls render in the italic caption face.** Verified this seat: §4 lists seven roles, `:78` closes the matrix; `text-micro` (`--type-micro: 0.6875rem`, registered but not a §4 role) carries ordinal/timestamp/identity/chip/provenance (`:36,40,46,60,69`); and the promoted missed finding — served `semantic.css` registers `text-caption` with `font-style: italic; font-weight: 400`, whose clamp arithmetic reproduces D's measured 12.03/12.18/14.38 ladder and 400 weight exactly, so the utility (not `.button`'s 500/`--control-text`) styles both controls (`:79`, `:92`): **italic Revert and italic Load-older, visible in `shots/02`/`07`** — the one "~" row in D's §6 table that was wrong. The ordinal also lacks tabular figures against §4's live-number clause (INFO fold). **Disposition: BUILD → X.W7.d recomposition under X-W10 law** (roles per the matrix: ordinal/timestamp/hash → `text-mono-small`, identity → `--type-subheading`, controls → `text-small` non-italic); X-W4's interim file touch must not re-pin `text-caption` on controls.
- **VHD-18 (D-14a / D-13.1) — below 40rem the header centres over a left-aligned body; in RTL the header stays physically left — inherited from the producer's `text-center sm:text-left` DialogHeader class.** Verified verbatim in `dialog-TNRDkcE4.js` this seat. **Disposition: GLASS** (DialogHeader logical-property + breakpoint posture — rides the same BH relay letter as VHD-5); the consumer's exposure dies at the W7.d recomposition.
- **VHD-19 (D-12 / D-13.5) — the side sheet never mirrors in RTL (physical `right: 0` placement arms) and the current marker is pinned `-left-px` with a physical `rounded-r` — in-scope via the repo's own documented `dir` seam.** Verified: `placement.css` has only physical arms (this seat's whole-file read); `:31`; `index.html:1-12` documents the U-F58 seam and BOOKS per-component conversions. **Disposition: split — GLASS** (placement.css logical arms, same relay letter) **+ X.W7.d/X-W4** (the consumer's `-left-px`/`rounded-r`/`mr-1` physical residue at whichever touch lands first).
- **VHD-20 (D-15) — the producer's `scroll` axis is never passed; the consumer hand-rolls the scroll frame across three elements.** Verified: no `:scroll` at `:3`; default `!1`; the placement-agnostic `[data-scroll]` CSS arm (dvh clamp + `overflow-y: auto` surviving the side-placement override) makes the cure one consumer prop — A-1's producer-repair framing stays killed (K-1). **Disposition: BUILD → X-W4** (one prop + delete the three hand-rolled classes at `:3/:4/:15`; also deletes VHD-37's `mt-4` in the same edit).
- **VHD-21 (D-16 / A-2 a11y limb; safeCss limb killed K-13) — the colour swatches are a hand-rolled second species: anonymous to AT, not `aria-hidden`, with a verbatim fork in `AdminFlaggedPanel.vue:51-58`, while the canonical strip carries the fix.** Verified: `:52-57` vs `PaletteColorStrip.vue:2-5` (`aria-hidden="true" role="presentation"`, the W5-a11y comment). **Disposition: BUILD → X.W7** (scope 4's props-only `PaletteSpecimen` is the species-consolidation site; both forks re-point or die).
- **VHD-23 (D-08) — under forced colors the colour payload dies to identical black circles and the current-row delta measures zero on every surface property; the literal `(current)` text is the sole survivor.** Mechanism source-confirmed this seat (inline `background-color` with no `forced-color-adjust: none` — the textbook sanctioned exception for a colour specimen; `ring-2` is a strippable box-shadow; `bg-primary` bar forced to the sheet colour). Chromium-cell numbers labeled so; WebKit/HCM arm = residue U-5. **Disposition: BUILD → X.W7.d under X-W10 law** (VISUAL-CONSTITUTION §4.1's forced-colors clause; `forced-color-adjust: none` on the specimen swatches, a non-color current marker).
- **VHD-24 (D-33 / D-34) — a 75%-wide sheet at 320px (240px) is neither modal nor full-screen, and the panel's type is viewport-fluid while its width is viewport-independent above 40rem.** Geometry verified from `placement.css`; the type ladder re-derived exactly from the clamp tokens (this seat's arithmetic). **Disposition: split — GLASS** (the placement geometry/`size` axis, same relay) **+ X.W7.d** (the recomposition decides the small-viewport posture deliberately — sheet vs full-screen vs inspector section).
- **VHD-25 (L-5 API arm) — `revertToVersion` looks the version up by global content hash and never asserts it belongs to the target palette.** Verified: `repository/paletteVersion.ts:13-15` (`findOne({ _id: hash })`, unscoped); `service/versions.ts:131-137/:162-175` copies `version.name/colors` onto the target with no `version.paletteSlug === slug` check; `_id` IS the cross-palette content hash. **Disposition: FOLD → X-W3 G-6 (P0, already born-RED with this exact evidence)** — one identity, no re-book; the demo-side trigger remains L's honest HYPOTHESIS.
- **VHD-26 (L-4) — `demo/ui/` is 19 pure-alias barrels with zero content; the drawer climbs three levels to reach two of them.** Census re-run this seat: 19 dirs, 0 non-index files; `:105-112`. **Disposition: FOLD → X-W8 scope 2** (the 19-barrel / 92-import extirpation; identity from the ColorPicker adjudication's L-6 — never re-booked).
- **VHD-27 (L-7) — `demo/palettes/export.ts` ships while the 12-file certified `export/` directory is what the byte-exactness gate proves.** Verified: both exist; no `export/index.ts`, so `"./export"` resolves to the file; sole `export/` consumer is the test. **Disposition: FOLD → X-W7 scope 2 (verbatim: delete `export.ts`, route `usePaletteExport` at the certified serializers; gates G4-G7).**
- **VHD-28 (A-3, precision per R-7) — the barrel-seam invariant both `index.ts` files cite is enforced by a ban that can never fire.** Verified this seat with the live-glob precision. **Disposition: FOLD → X-W8 scope 3** (delete the dead globs, author the real boundary ban). One identity with ColorSpaceSelector L-5(a)/ActionFeedback L-2.
- **VHD-29 (L-3, cost limb rescoped — both readers + this seat concur) — a leaf presentational drawer imports the composition root (`usePalettePorts.ts`, 275 lines, provider `:244`) to obtain one Symbol, and the 32-member `browsePort` contradicts the file's own FIVE-narrow-ports charter (`:20-31`).** The 36-module/4.5× payload framing is struck: `BrowsePane` statically imports both the drawer and the same module — marginal shipped cost is zero; the defect is dependency **direction**. **Disposition: BUILD → X.W7.d** (`usePalettePorts.ts` in W7's modify list; the drawer's data arrives as props at the recomposition — which also releases VHD-35's retention for free).
- **VHD-36 (missed by ALL THREE axes; both readers converged independently — promoted) — `variant="outline"` and `variant="ghost"` are not props of glass-ui's Button: both controls collapse to the default `emphasis="secondary"` capsule, the outline-vs-ghost intent does not exist, and the dead attribute falls through to the DOM.** Verified this seat: `Button.vue.d.ts:6-19` (no `variant`; `size` IS real), compiled defaults `emphasis: { default: "secondary" }`, the corpus's own probe-run DOM dump prints `<button variant="outline" …>`, and `shots/02`/`07` render identical filled pills. Class census this seat: **65 lines across 27 demo SFCs** spell shadcn-era `variant=` values against **2** `emphasis=` call sites repo-wide (reader-2's Button-proximal count: 51) — invisible to vue-tsc (fallthrough attrs) and to every visual gate (the fallback renders fine). **Disposition: the two component sites die at the X-W4/W7.d file touches; the repo-wide class → BUILD → X-W8** (this IS "shadcn abrogation": the census joins W8's C-16 residue census; the cure decides `emphasis`/`tone` per seat, coordinated with VHD-3's `tone="destructive"`). At cure time the census must separate components that legitimately publish `variant` (e.g. `EmptyState`) from the dead-on-Button sites.

### MINOR

- **VHD-10 (C-8) — offset paging over non-unique `createdAt` re-serves rows and hides others; the duplicate renders under two ordinals.** Verified: `:153-155` offset = rendered length; `repository/paletteVersion.ts:22-27` no `_id` tiebreak. **Disposition: split → X-W3** (G-7's `revisionNo` is the total-order key) **+ X.W7.d** (cursor/tiebreak-aware client paging).
- **VHD-12 (C-12 / L-6) — every revert `$inc`s `versionCount` while inserting nothing, so the card badge and drawer total diverge permanently.** Verified this seat: dedup early-return (`service/versions.ts:40-44`) vs unconditional `$inc` (`:172`); two renderers (`PaletteCardMenu.vue:101` vs `:11`). **Disposition: FOLD → X-W3** (G-7 release identity makes a revert an appended revision — the divergence cause dies; G-11's `201` carries it to the client).
- **VHD-13 (C-13) — rapid reopen double-fetches; `loading` is a shared boolean cleared by whichever request settles first.** Verified: no in-flight guard on the watcher path. **Disposition: BUILD → X.W7.d** (dies with VHD-9's request identity).
- **VHD-15 (C-10) — the gate is vacuous: no test could fail if the component were gutted, and the one e2e that opens the drawer stubs `{data: [], total: 0}` and asserts a typeface.** Census re-run this seat (1 + 3 test files, none versions; `o10d…:148-153/:324-338` re-read). **Disposition: BUILD → X.W7** (the mounted assertions + w7-oracles in W7's create list) **on X-W1's restored-CI harness** (CC-031's HARD e2e job is the precondition that makes any new spec load-bearing).
- **VHD-22 (D-19 + reader-1 missed `.interactive-item` fold) — the row hover replaces the opaque well with a 50%-alpha accent inside a glass host whose well-law precondition ("nothing live sits behind an in-plate fixture") is false — and the hover recipe is a hand-copy of glass-ui's `.interactive-item` minus its focus ring and press state, applied to a row that is not interactive at all.** Verified this seat: `DESIGN.md:98` names VersionHistoryDrawer rows in the WELL rung while rung 3 names drawers CHROME — the law's own table sanctions a deployment its rationale forbids; served `utilities/base.css` ships the full recipe (`hover` = the same accent-mix, `:focus-visible`, `:active` scale) and `:25` copies exactly the half that signals to pointers only, as a false affordance whose sole real function is revealing the button VHD-2 proves unrevealable on touch. **Disposition: split → X-W10** (canon: rule the well-inside-glass case explicitly) **+ X.W7.d** (the recomposition either makes rows real interactive items via the producer utility or strips the affordance).
- **VHD-30 (L-10 / A-6) — `tsconfig.demo.json` paths: 3/8 phantom, 2 real keys omitted, the "CLOSED 8-key set" comment false.** IDENTITY GUARD: fact-identical to ColorSpaceSelector L-14 (adjudicated, FOLDed to W-HYGIENE config parity with a born-RED generator command); `W8.md:112` explicitly declines it ("another owner"). **Disposition: FOLD → the standing W-HYGIENE identity; NO X-WAVE OWNER — carried to the formation boundary by name, never re-booked as new.** A-6(b)'s false root-barrel comment (`demo/shared/utils.ts:16`) rides the same row.
- **VHD-31 (L-11) — `useDialogBrowseActions.onRevert` + its `modalStack` dep are dead: an unreachable branch guarding a dep no caller supplies, citing a migration source that no longer exists.** Verified this seat (`:38`, `:76-77`; BrowsePane destructures neither). **Disposition: BUILD → X.W7.d** (deleted in the mutation-ownership consolidation; pure subtraction).
- **VHD-33 (reader-2 missed, promoted) — `GET /palettes/:slug/versions` is unauthenticated and visibility-blind, returning every superseded name/colour set by slug.** Verified: `routes/versions.ts:25-41` bare, `:49-51` gated sibling. Already owned: **FOLD → X-W3 G-3 (born-RED with this exact evidence)**; reader-2's calibration (consistent with the detail-route posture, but strictly more disclosure) adopted verbatim.
- **VHD-34 (reader-2 missed, promoted) — an unattributed session accumulates `versionCount` with zero version rows: counter bumped on `contentChanged` (`crud.ts:191-194`, create `:106`) while the row is written only under `contentChanged && userSlug` (`:204`, create `:118`) — a fourth, undiagnosable cause of VHD-7's blank.** Verified this seat, including the honest reachability caveat (`routes/crud.ts:74/:85`: session required, `userSlug ?? null` — the guard was read, not driven; residue U-9). **Disposition: BUILD → X-W3** (X.A2/A3's write contract must make counter and log move together or derive one from the other).
- **VHD-37 (reader-1 missed, promoted) — `mt-4` on the scroll region double-spaces the header seam to 32px: the producer's content region already supplies `gap: 1rem`.** Verified this seat: the `te` region wrapper is rendered for every placed dialog; the side-placement rule replaces display properties only — `gap` survives in flex. **Disposition: FOLD → VHD-20's X-W4 edit** (dies with the `:scroll` adoption).
- **VHD-38 (reader-1 missed, promoted) — the in-flight indicator renders at the head of the list, ~2000px from the Load-older button that triggered it, while the producer's `loading` prop (which stamps `data-loading` + `aria-busy` + `cursor: progress`) sits unused.** Verified this seat (template order `:17-22`; `button-Bu9F4uU6.js` + `styles.css` stamps). **Disposition: BUILD → X.W7.d** (G13 visible-result at the point of interaction; pairs with VHD-14's `aria-busy` arm).
- **VHD-39 (reader-1 missed, promoted) — three per-instance copies of the DialogTitle voice override (`font-display font-medium` at VersionHistoryDrawer `:9`, FlagReportDialog `:9`, MigratePalettesDialog `:7`), one re-declaring the producer's own default class.** Verified (DialogTitle default `text-subheading leading-none tracking-tight` in dist). Three sites, one recipe = a missing producer voice axis. **Disposition: GLASS** (the DialogTitle `voice` ask joins the VHD-5 relay letter); the three sites re-point when it ships; no local `:deep` expansion meanwhile.
- **VHD-40 (reader-1 missed, promoted) — `mr-1 h-3 w-3` on the Revert icon fights three producer tokens: the button's own gap, the RTL seam (physical margin), and the `--ui-glyph` size.** Verified this seat (`.button` gap + svg glyph rule in served styles.css). **Disposition: FOLD → X-W4's size-axis file touch** (delete all three utilities).
- **VHD-41 (reader-1 missed, promoted) — the palette name is clamped in the row (`:46` `truncate`, no `title`) but unclamped in the description (`:10-12`), so one long name wraps the subtitle and pushes the whole list down — worst at the 292.5/240px arms.** Static halves verified; D-13/D-14c carry the measured arms. **Disposition: BUILD → X.W7.d** (the recomposition's identity treatment; one clamp policy for one datum).
- **VHD-42 (D-20) — the row-name comment documents a condition the code does not implement: `<!-- Name (if different from current) -->` over an unconditional render.** Verified in source and in `shots/01` (header "Sunset Blaze — 45 versions", first row "Sunset Blaze" directly beneath). **Disposition: FOLD → X.W7.d** (either implement the condition or correct the comment — a one-line truth repair in the recomposition).

### INFO

- **VHD-16 (C-14 + reader-2's formatDate half) — both exports of `dateFormat.ts` carry an unreachable `catch` (ECMA-402: `toLocaleString`/`toLocaleDateString` return `"Invalid Date"`, never throw — re-run this seat: `node -e … → "Invalid Date"`), so the honest `return iso` fallback is dead and an invalid timestamp reaches the DOM verbatim; a fresh Intl formatter is built per row per render.** Three consumers; any cure touches both exports. **Disposition: NO-WAVE-OWNER** (`dateFormat.ts` appears in no X file list) — named candidate X-W7 (nearest owner, palette family); recorded for the formation boundary.
- **VHD-32 (A-5(d)) — the file is named `…Drawer` but renders a placed `Dialog`, while glass-ui ships a real `./drawer` family under that name.** Verified (`:2-3`; the producer exports map). **Disposition: FOLD → X.W7.d** (the recomposition names the surface truthfully — or adopts the actual Drawer primitive — as a side effect).
- **VHD-35 (reader-2 missed, promoted) — `versionPalette` is never reset to null, so after one Versions click the KeepAlive'd pane retains the drawer plus a full `Palette` and up to N loaded `PaletteVersion` records for the session (FlagReportDialog shares the shape).** Verified (writes only at `:273`/`:283`). Not a leak in the RAF/listener sense — the one lifecycle cost the file carries, released for free by VHD-29's props-only cure. **Disposition: FOLD → X.W7.d.**

## Killed-claims register

Sustained kills were **re-derived by this seat**, not inherited; K-11..K-13 are NEW, entered by this seat against the readers' own cells.

| id | killed claim | refutation |
|---|---|---|
| **K-1 (A-1)** | "`scroll` default `true`; for `placement="right"` the prop is a no-op; glass-ui must move `oe` out of the centre-only branch" (challenge-L:636-669) | Re-verified in dist this seat, both limbs false: `scroll: { type: Boolean, default: !1 }` (extracted verbatim); `"data-scroll": f.scroll ? "" : void 0` stamped with no placement gate; and `placement.css` carries the placement-agnostic `[data-scroll] > region { min-block-size: 0; max-block-size: calc(100dvh - …); overflow-y: auto }` whose scroll properties **survive** the side-placement override (which replaces only display/flex/block-size members). The cited d.ts line states no default. The producer needs no repair on this axis; the cure is `:scroll` on the consumer (VHD-20), exactly as D-15 framed it. A-1's `size`-axis half survives separately inside VHD-5's relay. |
| **K-2 (D-07 `h-7` limb + its generalized law)** | "`h-7` is inert dead CSS by the same unlayered-cascade mechanism; per-instance box-model overrides on glass side sheets silently do nothing" | Button CSS is LAYERED (`@layer components`, my read of served `styles.css`) — `.h-7`'s `height` WINS its cascade; the 36px is `.button`'s `min-block-size: var(--control-h-sm)` = `max(2.25rem×var(--ui-scale), 0px)` flooring it. Different property, no cascade contest, and the "single relay retires all three overrides" cure would leave `h-7` exactly as dead. Two of four overrides die by unlayering; `h-7`'s cure is X-W4's size axis (`size` prop, delete the utility — W4 scope 3 verbatim). |
| **K-3 (C-9 negative note)** | "The Revert button's 28px height (`h-7`) clears the 24px floor" (challenge-C:414-415) | Rendered height is 36px (K-2's floor; D measured `h: 36` twice). Conclusion survives a fortiori; the number — inside a section framed as checked-negative, the place a later seat trusts most — is false and stays killed. |
| **K-4 (D-11a citation)** | VISUAL-CONSTITUTION §4.1 "never colour-only" cited for typographic hierarchy | §4.1 (`:84`, read this seat) governs STATE encoding, not type roles. The observation survives correctly under D-11b's §4 closed matrix (VHD-17); the miscitation dies. |
| **K-5 (L-3 cost framing)** | "36 runtime modules to obtain one Symbol … 4.5× reduction" | Graph statement true; marginal cost zero — `BrowsePane.vue` statically imports the drawer AND constructs the ports, so every module is in the host's graph before the drawer resolves. Severity rests on dependency direction (VHD-29), not payload. Both readers concur; the addendum's own honesty standard (A-5(c)) applied to itself. |
| **K-6 (L-8 census)** | "The only other instance of this idiom … two hand-rolled copies, drifted" | Four sites, not two (my grep — R-9); the exact-literal search string manufactured the undercount. Strengthens VHD-2 (the drawer is 1-of-4 and the sole keyboard omission); the printed evidence stays killed, and "no shared home ⇒ drift" is weakened: three authors converged on the focus arm without one. |
| **K-7 (A-2 safeCss limb)** | "`safeCss` is the 'this string is paintable' boundary; the shared swatch should apply it" | `safeCss` is a WCAG **ink** certifier against the host surface (`useContrastSafeColor.ts:334-359`, read this seat); every live call site binds foreground ink. Applying it to a swatch fill would deliberately shift the specimen colour; the canonical `PaletteColorStrip` paints raw. The cure would break the component's purpose. |
| **K-8 (D-10 warning limb)** | "duplicate `:key` → Vue duplicate-key warning" | The drawer appends (`:145`), so patchKeyedChildren's common-prefix scan consumes the old list and the keyed-map branch that owns the warning never builds (reader-2's derivation against runtime-core, adopted; C-8's opposite parenthetical is the correct one). Worse, not better: the duplicate renders under two ordinals with no diagnostic. A mounted repro would settle it beyond derivation — noted, not owed. |
| **K-9 (C-4 seam limb)** | "no seam for an AbortController: client.ts builds its RequestInit without a signal" | `RequestOptions extends RequestInit` + wholesale `...init` spread (`client.ts:94-118`, read this seat). The seam is open today; the missing plumbing is the feature api module (VHD-9's relocated cure). |
| **K-10 (D-17 count)** | "`useVersionHistory.ts` exports 11 members" | Ten (`:116-127`, counted this seat). D's "seven unconsumed" lands right only by compensating error. A hard number in a defect register, wrong. |
| **K-11 (NEW — reader-1's D-04 cell)** | "Revert is destructive, **irreversible**" | The overwritten current state is itself a version row (recorded at its own attributed create/patch), listed by this same drawer and reachable by a second owner revert — recovery exists in-product (non-total only at VHD-34's unattributed edge; hidden, not removed, by VHD-4). The unconfirmed-destruction verdict survives DEMOTED (VHD-3); the irreversibility cell dies. |
| **K-12 (NEW — reader-1's C-4 transport cell)** | "The transport offers no seam either" — repeated inside a CONFIRMED verdict | The falsehood reader-2 killed in the challenger, reproduced verbatim by the confirming reader without re-derivation — a citation-inheritance event (ColorSpaceSelector DISSENT §1 class), recorded as evidence about the fleet. Verdict survives; cell dies. |
| **K-13 (NEW — reader-1's D-16 cell)** | "no `safeCss`" listed among the swatch indictments | Same event class as K-12 against K-7's facts: certifying a swatch fill is anti-cure. D-16's fork/AT limbs unharmed (VHD-21). |

## Superlatives — preserved with provenance (they gate what cures may NOT break)

1. **The `+N` overflow chip (`:51-64`) is genuinely right and is the codebase's cited good precedent** — cap 8, honest remainder, `v-if`-gated so `+0` never renders, correct at 1/8/14 colours; visually confirmed (`shots/01`, the `+6`); `SCALABILITY-AUDIT.md:138` names it "the good precedent". It is also the one thing the AdminFlaggedPanel fork lacks — the VHD-21 consolidation must carry it, not average it away.
2. **`try { … } finally { loading.value = false }` (`:139-150`)** — the permanently-spinning-UI failure mode is structurally excluded, by construction rather than by a catch that must remember.
3. **The `(current)` state is not colour-only (`:38`)** — the literal text label is precisely what survives forced colors (D's own probe shows ring and bar evaporating); it satisfies VISUAL-CONSTITUTION §4.1 by exactly the margin the law was written for. VHD-23's cure adds to this; it must not replace it.
4. **Vue-3.5 and module hygiene are a result, not an absence** — reactive props destructure used correctly by the watcher; explicit `open` prop + `update:open` emit deliberately sidesteps the `defineModel` stale-read hazard this repo's memory records; the single type-only import correctly marked under `verbatimModuleSyntax`; zero rAF/intervals/listeners/observers — the PRM-RAF epidemic does not reach this file, and nothing is owed to `onUnmounted` (checked specifically, not inferred from silence).
5. **The one animation is properly governed** — `animate-spin` neutralised app-wide by Guard 1 (`animations.css:184-193`, served-import chain verified in the quarantine receipt); transitions ride producer duration/easing tokens; no motion row of this corpus is quarantined (no M-xx applies).
6. **API transactional discipline around the defects is exemplary** — `createVersionRecord`/`insertIfAbsent` genuinely idempotent; every cross-collection write (create/patch/revert/fork) threads a real `session` with a written partial-failure rationale. The `$inc` inside it is VHD-12; the machinery around it is what X-W3 builds on, not against.
7. **The provenance comments name unit, decision, and producer fold** (`:5-8`) — verified true against the compiled producer, and the one thing the existing e2e actually proves (o10d's Fraunces ≤500 assertion).
8. **`demo/palettes/browser/index.ts` is a barrel that knows what it costs** — named re-exports with the tree-shaking rationale written down, and its one apparent violation justified in place rather than hidden.
9. **Corpus superlative — challenge-C's vacuous-gate proof** (`:444-449`): it names the exact surviving mutation and observes the shipped first-open bug already lives inside that mutation's blast radius. Both halves re-verified by this seat's censuses.
10. **Corpus superlative — the banked receipt `repro-L1-first-open.mjs`** runs from a clean checkout on real Vue + jsdom with no repo state; re-run unmodified by both readers AND this seat (`EXIT=1` three times across three independent harnesses). It is the standard the rest of the corpus should be held to, and the model for every born-RED gate this roster routes.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **U-1** — whether `ring-2`'s outset shadow and the `-left-px` marker are clipped 1-2px by the scroll container (`overflow-y: auto` computes `overflow-x` to auto): one computed-style + rect read on the current row vs its scroll parent settles it.
- **U-2** — D-03's WebKit-only focus alternation (BUTTON → BODY → BUTTON inside the open modal): self-labelled PLAUSIBLE, not source-derivable (reka-ui focus-scope under a zero-opacity first stop); needs a driven WebKit cell.
- **U-3** — the 200%/400% zoom arm: challenge-D emulated by halving the viewport, which VISUAL-CONSTITUTION §3.2 explicitly forbids; a real in-app browser-zoom arm is still owed for this component.
- **U-4** — VHD-9/VHD-13's exact live interleavings (cross-palette paint, rapid-reopen double fetch): mechanism CONFIRMED from source; the specific renderings were produced against jsdom mocks.
- **U-5** — VHD-23's forced-colors numbers are Chromium-cell only; the WebKit / Windows-HCM arm is unbanked.
- **U-6** — VHD-2's coarse-pointer consequence rests on `@media (hover: hover)` never matching on touch devices: the CSS gate is byte-confirmed, the inference standard, but Playwright's mobile arms emulate viewport, not the hover feature — one real device or a `hasTouch`+`hover:none` Chromium arm is owed.
- **U-7** — L-6's live-commons limb (two remixes sharing `currentHash`; zero commons palettes with `versionCount > 1`): dated network observation, unverifiable under this seat's no-network law; the mechanism is source-confirmed (VHD-12/VHD-25).
- **U-8** — every measured pixel/colour/computed-style in challenge-D §5-§9 is a chromium-1440 cell claim, labeled so; the source mechanism behind each ruled row was independently confirmed, the numbers were not re-measured here.
- **U-9** — VHD-34's reachability (a session that resolves no `userSlug`): the guard was read (`routes/crud.ts:74/:85`), never driven; one authenticated-but-unresolved request settles it.

## Closing verdict

**ADJUDICATED (not VERIFIED): 42 rows CONFIRMED — 2 BLOCKER (VHD-1 re-proven by execution in this seat, VHD-2 by served-bytes derivation) · 22 MAJOR · 15 MINOR · 3 INFO — with 13 rows in the killed register (10 sustained by re-derivation, 3 entered by this seat against the readers' own confirmed cells: the irreversibility, transport-seam, and safeCss cells die while their verdicts survive). Dispositions: X-W3 ×6 (the versions API triad — G-3/G-6/G-7 already born-RED with this corpus's evidence) · X-W4 ×5 (the SFC's control chassis, interim holds flagged) · X-W7 ×19 (the W7.d inspector recomposition is where this drawer's chassis is scheduled to be re-authored; 3 named carve/adoption gaps recorded so no gate goes green over another wave's edit) · X-W8 ×4 (barrels, inert lint ban, the promoted repo-wide dead-`variant` class) · X-W10 ×2 (canon: the well-inside-glass contradiction, the type-matrix law) · GLASS ×1 consolidated BH-relay letter (unlayered placement + size axis + DialogHeader/placement logical properties + DialogTitle voice — with K-1/K-2 pinned so the relay asks only for what is actually producer-owned) · NO-WAVE-OWNER ×2 (VHD-16 dateFormat, VHD-30 tsconfig-parity identity) carried by name to the formation boundary. 9 UNPROVEN-NEEDS-LIVE rows feed the visual audit. The component's virtues — the honest overflow chip, the structural spinner guarantee, the non-colour-only current state, the clean lifecycle — are recorded as gates on the cures, not concessions.**

— end of apotheosis. The challenger corpus, both reader returns, and the dated evidence trees remain immutable beside this file per E-1/E-3; this file is new and rewrites nothing.
