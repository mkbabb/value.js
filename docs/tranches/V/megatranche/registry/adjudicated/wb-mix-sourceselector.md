# wb-mix-sourceselector — adjudicated apotheosis (M-23/L-18 fresh Fable seat) · served model id: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) — the incredulity seat over two hostile re-readers (reader-DU: design-and-usage · reader-LC: library-and-code), themselves over the full challenge corpus.
**Date**: 2026-08-24 · **HEAD**: `053f7d59` (demo/ worktree CLEAN, verified this seat) · branch `tranche-u`.
**Corpus**: `audit/components/wb-mix-sourceselector/` — challenge-C-implementation (r1-prior, r2-prior, r3) · challenge-D-design (r2-prior governs, r3) · challenge-L-library (single pass) + evidence/ + frames/ + swatch-key-churn.repro.mjs. Subject: `demo/workbenches/mix/MixSourceSelector.vue` (283 lines) + its state machine `useMixingState.ts` and mix math `demo/palettes/mix.ts`.
**Arbiter's own instruments (all static; zero browser tooling — probe parsimony + task law)**: whole-file reads of MixSourceSelector.vue, useMixingState.ts (139 lines), demo/palettes/mix.ts (147 lines); MixPane.vue:1-30/:55-80/:105-115 (`inject(LIBRARY_PORT_KEY)!` at :16-17; Card `relative pane-scroll-fade overflow-y-auto`; `<Transition name="vj-morph" mode="out-in">` on the result plate); MixConfigBar.vue:80-90/:140-170 (STRATEGIES `["discard","repeat","distribute"]` at :83, rendered as a `<Select>`; the Mix `<Button :disabled="!canMix">` with no `title`/`aria-describedby`/text); demo/styles/utils.css:84-190 (`.dashed-well` carries NO `position`; `.swatch-row > .vj-enter-leave-active { position: absolute }` under the comment "the leaving dot is lifted out of flow so the row closes smoothly"; "never a scoped twin (the S.W7-7 lesson)" in the same file); demo/styles/focus-ring.css:5-14 (the U-F25 tombstone verbatim: "the `--ring`/`--color-ring` token it reached resolved EMPTY — so even an un-clobbered ring painted nothing"); demo/shared/ui/EmptyState.vue:45-47 (the seeded ghost trio, `seed="plate-a/b/c"`); demo/styles/animations.css vj-morph charter + `.vj-enter-move` (~:99); **the MSS-13 crash re-derived at bytes by this seat** (mix.ts:87 `repeat` → `colors[0 % 0]` = `colors[NaN]` → `undefined.css` TypeError; :90-99 `distribute` → `hi = Math.min(1, -1) = -1`, `t === 0` branch → `colors[0]` on `[]` → same throw; `resultLength === 1` → `fracPos = 0/0 = NaN` → `colors[NaN]` → same throw; `discard` alone escapes via :125 `resultLength === 0` return); **corpus verbatims re-read where I rule**: C.r2:88 (the four-site census), C.r2:450 ("module-level `Map`"), C.r1:483-485 (the five-call-site find r3 claims as NEW), C.r3:16/:368, D.r2:252-255 (the D-2 ring limb), D.r2:432-436 (D-9's `.disclosure-group-trigger` cell), L:830-845 (the 30.3×); **registry cross-reads**: wb-mix-animationcanvas.md:7/:48 (MX-3)/:59 (MX-41)/:75 (MX-42) incl. its X-wave-bounds census (X-W6.j files = MixPane+MixConfigBar+MixSourceSelector+MixAnimationCanvas+useMixingAnimation; X.W4.g trigger-gated, holds `:168`/`:215`; X-W1 = CC-031; X-W7 shares MixSourceSelector/MixConfigBar; W8.md:112 declines tsconfig parity; **no X wave holds useMixingState.ts, demo/palettes/mix.ts, or the two RED mix specs**); CurrentPaletteEditor.md:33 (A-1/CC-044) /:34 (A-2 arm ii — the zero-colour write path) /:40 (C4-1) /:95 (K-9) /:75 (ADJ-m3, U-3); wb-mix-configbar.md:66 (MC-20) /:81 (K-1); wb-mix-resultdisplay.md:49 (MR-4) /:104; PaletteCard.md:67 (PC-25) /:74 (PC-32) /:77 (PC-35); PaletteCardMeta.md:50 (PM-13); AdminNamesPanel.md:55 (D-12/C-8); GenerateControls.md:32 (R-D); wb-generate-pane.md:43 (GEN-2); PaletteCardSwatches.md:27/:72 (PS-29); **motion-quarantine.md** (audit/codex-provenance/) grepped: the sole Mix-adjacent row is a PaneHeader item at :162 — **no M-xx FALSE row names this component; nothing quarantined is carried**.
**Method**: every verdict-level disagreement between the two re-readers was re-derived against the tree by this seat; every fold target was opened and its line read; the one MAJOR-minting new find (MSS-13) was independently re-executed at bytes before adoption; two of the re-readers' own sentences were corrected (RULINGS R-5, R-6).

## Scoreboard

**33 adjudicated rows survive (1 BLOCKER · 14 MAJOR · 16 MINOR · 2 INFO) — of which 10 are IDENTITY-FOLDS to banked registry rows (confirmed at HEAD, never re-booked) · 13 killed claims · 7 re-reader missed-finds admitted (4 DU + 3 LC), one raised to MAJOR by this seat's own re-derivation.**

Blocker-weight sentence: **the colors arm — the pane's DEFAULT mode — is born dead (every add path and every chip affordance is a decorative producer span, MSS-1), the only gates that would have said so are unsatisfiable and unwired (MSS-2), and the palettes arm that DOES work will synchronously crash inside a click handler the day a zero-colour palette meets the `repeat` or `distribute` Select option (MSS-13) — an operand the registry has already proven the product can write (CurrentPaletteEditor A-2 arm ii). Meanwhile the state machine under all of it is the cleanest module in the mix tree.**

## Ruled disagreements (this seat's actual work)

| # | question | reader-DU | reader-LC | RULING | deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| **R-1** | D-24's booking (the RED/unsatisfiable e2e pair) | CONFIRM at MAJOR (regraded from the axis's BLOCKER) | KILL as a double-count of D-1's mechanism, already banked | **FOLD — LC wins the booking, DU wins the grade annotation** | The (b) limb IS D-1 restated at the query surface (no role=button with that name can exist when the producer renders `<span aria-hidden>`); the (a) limb is a Dock seat's defect the D axis itself declines to connect. The whole row is banked verbatim at **MX-3** (wb-mix-animationcanvas.md:48: spec repair → MX-CLUSTER NO-WAVE-OWNER; CI e2e gating → X-W1/CC-031). Booking a second severity here inflates the ledger; losing DU's regrade loses the truth that this is a process defect downstream of D-1, not a second product blocker. Both halves carried inside the fold (MSS-2). |
| **R-2** | the zero-colour operand's severity | (did not reach the crash; carried C-17 at MINOR) | missed-find: CRASH under `repeat`/`distribute`, MAJOR | **LC's find ADOPTED at MAJOR — re-executed at bytes by this seat** | mix.ts:87: `0 % 0` is `NaN`, `[][NaN]` is `undefined`, `.css` throws; :93 `Math.min(lo+1, len-1)` = `Math.min(1, -1)` = `-1` with `t === 0` → :97 `colors[lo]` on `[]` — same throw; at `resultLength === 1`, `0/0 = NaN` → same throw. Synchronous inside `startMix` (useMixingState.ts:92-96), i.e. inside a click handler; the strategy is an ordinary Select option (MixConfigBar.vue:83/:146). Reachability is BANKED, not hypothesized: CurrentPaletteEditor.md:34 A-2 arm (ii) writes `colors: []` through ordinary controls — which also kills L-10's "unreachable" hypothesis (K-13). C-17 + the crash = ONE composite MAJOR (MSS-13), not a MINOR plus a footnote. |
| **R-3** | L-6's severity (opposite inject contracts, parent `!` vs child `?.… ?? []`) | MINOR | MAJOR | **MINOR — DU wins** | The child is otherwise 100 % controlled (4 props in, 5 emits out — my read :13-31); the masking arm fires only on a mount outside MixPane's provider, and no such mount exists (MixPane.vue:16 already holds the port). The port-graph pollution premise is real but is L-16's row, and its byte figures are UNPROVEN (U-9). A coherence defect with no reachable divergence is MINOR; the ColorSpaceSelector L-5(b) precedent (a deliberate two-key criticality difference is not a defect) does not fully apply — here the SAME key gets both treatments four lines apart — so it stays a row, at MINOR (MSS-14). |
| **R-4** | L-20's severity (demo runtime deps in devDependencies, incl. vue) | (not filed) | MAJOR, with its own note "the stated consequence is structural, not live" | **MINOR-RESCOPED — LC's own severity note is adopted as the ruling** | No live break path exists: deploy-pages runs plain `npm ci` (dev included), `files` publishes only `dist`, and no `--omit=dev` demo path exists (LC's reads, consistent with the manifest). Manifest-truth debt that bites exactly when L-13's workspace-split cure lands → it rides that decision (MSS-29), joined by LC's dead-devDeps trio (missed-find 3). |
| **R-5** | D-2's ring limb vs reader-LC's superlative #6 | KILL (category error: the :134-145 law is scoped to WatercolorDots) | superlative #6's closing clause calls :256-261 "its own indictment: re-mints the same ring-2 idiom" | **DU's kill UPHELD; LC's clause CORRECTED** | I re-read :134-145: the law names "rings on **WatercolorDots**", grounds itself in the dot's unlayered material shadow and per-seed silhouette — neither ground reaches the native `<button rounded-card>` wrapper, whose Tailwind ring is a box-shadow that follows its own border-radius, with PaletteCard's `cartoon-surface` shadow one level DOWN (inside the ring, not over it). The real defects at :256-261 are MSS-5 (channel collision) and MSS-4 (the wrapper itself); a cure wave sent by D-2 would delete the wrong declaration. LC's superlative stands minus its indictment clause. |
| **R-6** | D-9's producer-floor attribution | CONFIRM with a cure-safety rider (the applied class is `.disclosure-trigger`, floor-less) | KILL the attribution limb (the 44 px floor lives on `.disclosure-group-trigger`, an opt-in the producer never puts on a Collapsible) | **HARMONIZED — one row, corrected attribution** | Both readers converge from opposite directions on the same producer bytes; D.r2:436 itself names `.disclosure-group-trigger` (my read). The defect (a ~29.6 px trigger, arithmetic re-derived by DU to 0.1 px of the axis's live numbers) stands at MINOR; the cure is an explicit consumer floor or a producer ask — NEVER "consume the unused recipe", which would find nothing to consume (MSS-33). |

## Adjudicated roster — every row one terminal disposition, original ids for life

Line anchors are HEAD `053f7d59`; re-resolve before any cure (D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

**BLOCKER**

- **MSS-1 (D-1/C-1/L-1/L-2) — CONFIRMED (BLOCKER) · IDENTITY-FOLD → CC-044 + X.W4.g.** Every interactive WatercolorDot in the file is inert; colors mode — the DEFAULT mode (useMixingState.ts:41) — cannot accept a colour. Producer at the installed pin (both readers, consistent with the banked node probes at MX-3/A-1): props exactly `{color, variant, animate, cycleDuration, range, seed}`, `inheritAttrs:!1`, class/style-only readback, zero renderSlot, root `<span aria-hidden style="pointer-events:none">`. Consumer seats re-read whole by this seat: :146-151 chips (`tag="div"`, `:title` dropped), :164-176 add slot (`tag="button"`, `aria-label`, `:disabled`, `@click`, `<Plus>` — all dropped), :211-221 dropdown swatches (`tag="button"`, `:title`, `:aria-label`, `@click` — all dropped). Exactly THREE WatercolorDot `tag=` sites (r2's four-site census killed, K-7). Causation already ruled consumer-debt, not a glass seam (GenerateControls.md R-D: `tag` was never in the 7.0.0 contract). Canon names the cut: VISUAL-CONSTITUTION P051. **Terminal: CC-044** ("six WatercolorDot impostors", one atomic cut, BLOCKED-ON CC-003's Glass ≥8 trigger; no pre-8 wrapper) **+ X.W4.g** (trigger-gated; holds this file's `:168`/`:215` by line). Producer questions ride the standing glass-ui BH relay.

**MAJOR**

- **MSS-2 (D-24/C-2/C-2′/L-3) — CONFIRMED-AS-FOLD (MAJOR process defect, per R-1) → MX-3.** Both e2e gates (mix.spec.ts:38-43, safari/mix-flow.spec.ts:29-35) role-query an accessible name that resolves to zero nodes by construction, and `.github/workflows/` runs zero playwright — the RED gate is not a gate. LC's new corroboration adopted: no `@vue/test-utils` import and no `mount(` anywhere in the repo (grep EMPTY) — no gate of any species could have seen MSS-1. **Terminal: MX-3's routing verbatim — spec repair MX-CLUSTER (NO-WAVE-OWNER) · CI gating X-W1 (CC-031).** Which failure fires first today is U-5.
- **MSS-3 (D-11/C-14/L-5/L-21) — CONFIRMED (MAJOR) → X-W7.** `${sc.css}::${i}` puts the index inside the identity (:79-98, closure state per K-9 — NOT module state, K-8): any non-tail removal re-mints every survivor's key, so TransitionGroup tears the rack down and `.vj-enter-move` (animations.css ~:99, a FLIP move class) is unreachable. Root cause at bytes: `SelectedColor { css, source }` (useMixingState.ts:25-28) has no identity. The verbatim twin lives at useSwatchActions.ts:41-59 (banked C-9-as-rescoped, CurrentPaletteEditor.md:45). **Terminal: X-W7 — the TWO-SITE identity cure, carrying MSS-16 as a rider (the `.swatch-row` positioning fix must land in the same wave or the cure unmasks a second defect in three components).**
- **MSS-4 (D-5/C-5/L-7) — CONFIRMED (MAJOR) → X-W7.** :246-268 wraps `<PaletteCard>` in a native `<button :aria-pressed>`: button-inside-button (the card's menu trigger at PaletteCard.vue:96-104 is an unconditional real `<button>`) + `role="article"` inside a button — against the child's OWN root contract (:2-4: "button semantics on the card are omitted because inner interactive controls must be reachable"), while the producer's designed `click` emit (:26, :200-218) ships unused. **CURE-SAFETY RIDER (from K-3): the isolation that keeps the menu click from toggling selection is the demo's own `@click.stop` wrapper at PaletteCard.vue:82, NOT reka-ui — a cure that moves the seat inside the card believing reka guarantees isolation re-opens the double-fire.** **Terminal: X-W7** (the palette-card selection register).
- **MSS-5 (D-6) — CONFIRMED (MAJOR) → X-W4 + X-W10.** Selection (:259 `ring-2 ring-primary …`, specificity 0,1,0) and focus (:257 `focus-visible:ring-ring/40`, 0,2,0) write the same `--tw-ring-*` channel in the same layer — a selected card under keyboard focus loses its selection colour. Sharper (DU, verified this seat): `--ring`/`--color-ring` are minted NOWHERE and the repo's own tombstone says so verbatim (focus-ring.css:9-12) — focus replaces the selection ring with, at best, nothing. The tombstone limb is banked (ADJ-m3, CurrentPaletteEditor.md:75, which cites this file's :153/:170/:257 by line); the collision limb is this file's own. VISUAL-CONSTITUTION:89 "focus stays separate". **Terminal: X-W4 (register) + X-W10 (token law), per the ADJ-m3 precedent.** Painted result = U-3.
- **MSS-6 (D-7/L-19) — CONFIRMED (MAJOR) → X-W6.j.** :195's three tw-animate utilities replace the producer's complete `.disclosure-content` recipe (48-stop `--spring-smooth` + PRM arm, shipped in glass-ui.css at the pin) from `@layer utilities`; `overflow-hidden` is a pure duplicate. **Cure soundness carried (DU's addition, the thing a wave needs): deleting all three leaves a COMPLETE producer animation — name, duration, timing, PRM arm — so the cure deletes no motion and edict 6 is not engaged.** Delivered numbers = U-4. **Terminal: X-W6.j** (file in-wave), producer questions on the BH relay with MSS-31.
- **MSS-7 (D-25/L-22/C-6) — CONFIRMED (MAJOR) → NO-WAVE-OWNER (MX-CLUSTER).** MAX_COLORS is enforced nowhere the producer doesn't swallow: the reducer appends unconditionally (useMixingState.ts:55-57), the only guard rides a dropped `:disabled` (:172, dead per MSS-1), the second add path (:211-221) consults no guard at all, and the count mark died at :117-118. Cap VALUE correct per VC:202 (2-12). Cure home is the reducer — **no X wave holds useMixingState.ts** (wb-mix-animationcanvas.md:7 census). **Terminal: NO-WAVE-OWNER (MX-CLUSTER), with an X-W6.j announcement rider for the view.**
- **MSS-8 (D-18 + C-10 + DU-missed-3) — CONFIRMED (MAJOR composite) → X-W6.j.** The operand floor is authored twice with two values (view `MIN_COLORS = 1` :37/:39 vs reducer `canMix >= 2` :50-53 vs canon's 2-12), making n=1 terminal in colors mode — and DU's missed find pairs it: **the only sentence anywhere stating the palettes-mode floor is the EmptyState hint (:243), gated `v-if="savedPalettes.length === 0"` — at length 1 the hint vanishes, one card selects and ring-lights, and the Mix verb sits dead with zero stated reason** (MixConfigBar `:disabled="!canMix"`, no title/describedby — my read). C-10's boot-state contradiction folds in (boots at `[]`, can never return to it). **Terminal: X-W6.j** (both files in-wave: the view floor dies, disabled-reasons get announced); reducer-owned bounds ride MSS-7. Cross-ref MC-20 (the verb's two divergent homes, banked).
- **MSS-9 (D-19) — CONFIRMED (MAJOR) → X-W7, sequenced after MSS-3.** The ordered N-operand rack has no reorder affordance against canon that specifies the full grammar twice (VC:202 "add/remove/reorder"; VC:131 the horizontal-reorder table row naming operands, with the Space-grab/arrow/announce protocol). Zero keydown/draggable in the file (both readers; consistent with my whole-file read). MSS-3's identity cure is the stated prerequisite — index-keys make reorder unanimatable and unannounceable.
- **MSS-10 (D-20/C-8 + D-23/C-9 + C-19) — CONFIRMED (MAJOR) · IDENTITY-FOLD → AdminNamesPanel D-12/C-8 → X-W4.** The mode strip passes none of the producer's three shipped affordances (`ariaLabel`, `semantics`, `SegmentedTabOption.controls` — d.ts at the pin) while owning a v-if/v-else panel (:114/:230). Riders folded in at INFO: the `Array.isArray` branch defends a signature that cannot occur (emit typed `(value: string)`; the file's own :48 comment concedes it) — a deliberate contrivance; and `isPaletteSelected` runs five times per palette per render (:250-:258, re-counted this seat) — true, but r1's find (K-9 provenance kill). **Terminal: X-W4** (banked identity).
- **MSS-11 (D-17) — CONFIRMED (MAJOR) → X-W6.j.** The disclosure is unbounded (no max-block-size/overflow-y/virtualisation; producer's `.disclosure-content` adds none) and evicts COLOR SPACE / HUE METHOD / Mix **below the fold** of the pane's own scroll container (MixPane Card `overflow-y-auto`, my read) — DU's narrowing carried: evicted, not unreachable. Real-size geometry = U-7.
- **MSS-12 (C-15 + LC's publish extension) — CONFIRMED (MAJOR) → NO-WAVE-OWNER (MX-CLUSTER).** A palette that leaves `savedPalettes` stays an invisible, un-deselectable operand `canMix` still counts: the only `removePalette` emitter renders from `savedPalettes` (:246-268), and no prune exists in either file (my reads). LC's extension adopted: **publishing** a selected palette produces the same orphan through the store's `isLocal` narrowing — a non-destructive action. Cure = a reducer watcher/prune; no X wave holds the reducer.
- **MSS-13 (LC-missed-1 + C-17; kills L-10) — ADJUDICATED-NEW (MAJOR composite, per R-2) → NO-WAVE-OWNER (MX-CLUSTER).** A zero-colour palette is a legal, selectable, ring-lit operand (no colours-length guard anywhere: :57-67, :246-268, `addPalette` slug-dedupe only) — and under `repeat` or `distribute` it **synchronously throws `TypeError: Cannot read properties of undefined (reading 'css')` inside the click handler** (mechanism re-executed at bytes by this seat, header block above; `discard` alone escapes via mix.ts:125). Operand reachability is banked (CurrentPaletteEditor A-2 arm ii). All three axes probed only the default `discard` arm. Cure: reject the operand in the reducer + give `getColorAtIndex` the honest `len === 0` precondition its sibling `mixColorSequence` (:46-55) already models. UNBANKED before this record (registry grep: the only `getColorAtIndex` hit is wb-mix-animationcanvas.md:7's read note).
- **MSS-15 (L-8) — CONFIRMED (MAJOR) · FOLD → banked test/→demo inversion species (NO-WAVE-OWNER).** `demo/palettes/mix.ts` is consumed only by the mix workbench + test/mix-v4.test.ts; 10 test files import from `../demo/`. Species banked at wb-mix-animationcanvas + wb-mix-resultdisplay; no X wave holds mix.ts.
- **MSS-28 (L-14) — CONFIRMED (MAJOR) · IDENTITY-FOLD → ColorSpaceSelector L-5(a) (NO-WAVE-OWNER, lint-glob-liveness).** The demo module-boundary lattice globs `demo/@/**` (does not exist) and `demo/color-picker/**` (2 of 88 .vue files); no import rule reaches this file. One identity, banked with a born-RED `eslint --print-config` command — never re-booked.

**MINOR**

- **MSS-14 (L-6) — CONFIRMED (MINOR, per R-3) → X-W6.j.** Parent asserts the port (`inject(LIBRARY_PORT_KEY)!`, MixPane:16), child masks its absence four lines of feature-tree away (:33-34 `?.… ?? []` → the empty-state lie). One prop ends it; the child is otherwise fully controlled.
- **MSS-16 (DU-missed-2) — ADMITTED (MINOR) → X-W7, riding MSS-3.** The shared `.swatch-row` exit recipe absolutely-positions leavers with **no positioned containing block** (parent rule declares only the two custom properties — my utils.css read; `.dashed-well` static, TransitionGroup div static, nearest positioned ancestor = the pane Card), so a leaving dot resolves to its static position at the flex container's main-start — it teleports to the row's leading edge before scaling out, contradicting the recipe's own comment ("the row closes smoothly"). Double-masked today by MSS-1 (no chips) and MSS-3 (all chips leave together); becomes the shipped exit in THREE components the moment MSS-3's cure lands. One-line fix (`position: relative` on `.swatch-row`) in the same wave. Rendered jump is spec-derived (CSS Flexbox §4.1); static facts byte-certain.
- **MSS-17 (DU-missed-1) — ADMITTED (MINOR) → X-W6.j.** The mode swap — the only thing the component's one working control does — has no transition: :114/:230 are bare fragment branches (no `<Transition>` in the file, confirmed my whole-file read) while the house charter names `vj-morph` for exactly this and the parent uses it thirty lines away (MixPane:111, my read). CURE NOTE carried: `<template v-if>` fragments cannot host `<Transition>` — each branch needs a single root, the same restructure MSS-21 performs. Registry-checked: no banked row; no motion-quarantine row (my grep).
- **MSS-18 (LC-missed-2) — ADMITTED (MINOR) → X-W7.** One specimen, three silhouettes: chips carry NO seed (:146-151, default `""`), the ghost `seed="mix-add-slot"` (:169), dropdown swatches `palette-${slug}-${ci}` (:219) — so the comment at :161-163 ("the seeded dashed silhouette the next selection will fill") is false by the producer's own published ghost contract (d.ts: ghost of `color + seed` traces the SAME outline the solid of that seed fills). LC's offline PRNG derivation reproduces the corpus's live capture digit-for-digit. Species = C4-1 (CurrentPaletteEditor.md:40), second file, second self-refuting comment. **Terminal: X-W7 (specimen identity).**
- **MSS-19 (C-4/D-15/D-26) — CONFIRMED (MINOR) · IDENTITY-FOLD → MX-41 (X-W6.j / X.W4.g).** The 16 px, nameless, hover-revealed remove control (:152-158) — every limb already banked at MX-41 including the touch limb and the keyboard nuance (the button's own `focus-visible:opacity-100` does reveal it). LC's narrowing carried: the missing `type` limb has no reachable consequence (no ancestor form) — hygiene, not behaviour.
- **MSS-20 (D-27) — CONFIRMED (MINOR, structural half) → X.W4.g.** At n=0 the well's only mark is a 2 px dashed hairline whose colour IS the user's live picker colour (`--watercolor-color` ← `cssColorOpaque`, :166) — the sole affordance's non-text contrast has no floor by construction. The 2.88:1 number = U-1.
- **MSS-21 (D-3, as narrowed by K-5) — CONFIRMED (MINOR) → X-W6.j + X.W4.g.** The colors-mode empty state was designed (the same seeded-ghost invitation register EmptyState itself uses — verified: EmptyState.vue:45-47) but carries no copy (no eyebrow/message/hint) and, per MSS-1, no working affordance. A caption-ladder cure, much cheaper than "design one".
- **MSS-22 (D-21) — CONFIRMED (MINOR) → X.W4.g.** Nine dead utilities on the add slot, not five: the producer's inline `pointerEvents:"none"` also kills `cursor-pointer`/`hover:scale-110`/`active:scale-95` (:170), plus :175's class set on a never-rendered `<Plus>`. Revive or die with the seat.
- **MSS-23 (L-11/C-16/D-28) — CONFIRMED (MINOR) → X.W4.g.** `.add-slot-ghost` (:277-281) is a byte-identical scoped twin of CurrentPaletteEditor.vue:286-290, both centring a child the producer cannot render — in a file whose siblings `.dashed-well`/`.swatch-row` model the correct single-shared-recipe discipline, with the law in utils.css ("never a scoped twin — the S.W7-7 lesson"). Rider: the redundant `TransitionGroup` import (:2; it is a compiler built-in — MixPane resolves `<Transition>` without importing it).
- **MSS-24 (C-13/D-29) — CONFIRMED (MINOR) → X-W6.j.** `SelectedColor.source` is written by both add paths and read only by a `:title` the producer discards (:150); `data-mix-colors` re-runs `JSON.stringify(...)` inline per palette per render (:253-255).
- **MSS-25 (C-11/L-10/D-13) — CONFIRMED (MINOR) · IDENTITY-FOLD → PC-25 (X-W7.c).** `:css-color="''"` (:266) defeats `?? EMPTY_PALETTE_SWATCH` — banked verbatim ("Mix's `''` defeats `??`"). C's R-6 negative carried with it: contract violation, not a live crash. Not re-booked.
- **MSS-26 (C-18/L-4/D-16 + L-16's surviving limb) — CONFIRMED (MINOR) → NO-WAVE-OWNER (PS-29 identity).** `demo/ui/collapsible` is a one-line, single-consumer alias shim, and the file's own import block is register-inconsistent (:4/:7 subpath vs :5 barrel). The bare-root register is the demo's deliberate seam (PS-29 ruling); the payload framing is dead (K-10); what survives is intra-file inconsistency.
- **MSS-27 (L-9/L-17) — CONFIRMED (MINOR) · IDENTITY-FOLD → ColorSpaceSelector L-14 / W-HYGIENE (NO-WAVE-OWNER).** tsconfig.demo.json's hand-typed paths diverge from exports in both directions, name a file that does not exist, and self-describe as "a CLOSED 8-key set" — W8.md:112 already declines it as another owner's row. One identity.
- **MSS-29 (L-20 as rescoped by R-4 + LC-missed-3) — ADJUDICATED (MINOR) → NO-WAVE-OWNER (W-HYGIENE manifest).** Eight demo runtime packages (incl. vue) sit in devDependencies while `dependencies` holds only the two library packages — structural, no live path today; PLUS the dead-devDeps trio LC found: `sortablejs` + `@types/sortablejs` (zero importers; a K.W3 hard-gate ghost, banked only in HARVEST) and `@vue/test-utils` — **the exact harness MSS-2's cheapest cure needs, already installed, already unused.** Rides the L-13 workspace-split decision (U-8).
- **MSS-32 (D-8) — CONFIRMED (MINOR) → X-W6.j.** Two label registers for the same job ten lines apart (:119 `text-small font-display …` vs :183 `.section-label`); corroborated by the sibling seat (wb-mix-resultdisplay.md:30 PIN 3 cites both anchors).
- **MSS-33 (D-9, harmonized by R-6) — CONFIRMED (MINOR) → X-W6.j + BH-relay rider.** The disclosure trigger is a ~29.6 px tap target (`py-1` + the caption line box; arithmetic re-derived to 0.1 px of the live numbers). Cure = explicit consumer floor or producer ask; the "consume the producer's unused 44 px floor" framing is dead (the floor lives on `.disclosure-group-trigger`, which a Collapsible never wears). Live geometry confirmation = U-6.

**INFO**

- **MSS-30 (L-13) — CONFIRMED-AS-FOLD (INFO) → App.md G1.** The gh-pages build emits no application JS — banked and arbiter-measured at App.md:19 (same content hash); filing it on a component axis is a scope error. The `sideEffects`-attribution question is U-8; the nearby App.md C-6 tension (a DIFFERENT claim about `./demo/**` side-effects) is flagged so no wave conflates them.
- **MSS-31 (DU-missed-4) — ADMITTED (INFO) → glass-ui BH-RELAY.** The producer's own `.disclosure-icon` rotation selector keys on `.disclosure-group-trigger` and can never match a Collapsible (trigger class is `disclosure-trigger` — dist read), so every Collapsible consumer hand-rolls the chevron off JS state (:191-193) instead of the `data-state` the producer already stamps. Primary cure is a producer selector-widening; rides the standing BH relay envelope beside MSS-6's motion question. Interim demo option: `group-data-[state=open]:rotate-180` reading DOM truth.

## Killed-claims register

- **K-1 — D-2's ring limb ("the cards re-mint exactly that") — KILLED (DU's kill, UPHELD; ruling R-5).** The :134-145 law is verbatim scoped to WatercolorDots and both its grounds (unlayered material shadow; per-seed silhouette) fail to reach the native rounded-card wrapper, whose ring rides its own silhouette exactly as the law demands. The seat's real MAJOR is MSS-5, which D-2 never names. Corpus anchor re-read this seat (D.r2:252-255). Reader-LC's superlative clause repeating the indictment is corrected, not carried.
- **K-2 — D-2's alpha limb (`opacity-75` as self-contradiction of :186-189) — KILLED (DU, UPHELD).** Wrong law: the :186-189 comment governs ink-on-a-muted-token; whole-surface state opacity is the milder species the fleet already distinguished (MX-42 ruling). The site's true identity is the banked post-hoc-alpha census **PC-35** (verified: PaletteCard.md:77 → X-W7.c under X-W10 canon), whose adopted discipline (PM-13, verified) declines the house idioms. DU's four-alpha census of this file (:175, :200, :257, :260) folds there; none is booked here.
- **K-3 — C R-1's mechanism ("reka-ui's DropdownMenuTrigger stops propagation") — KILLED as attributed (DU, UPHELD).** The stopper is the demo's own `@click.stop` wrapper (PaletteCard.vue:82). The observation and r2-correction stand; the misattribution is cure-unsafe and rides MSS-4 as a rider.
- **K-4 — D §6 gap 1 ("forced colors… unproven, not clean") — KILLED as framed (DU, UPHELD).** Source-decided both ways: foundation.css's tier-1 roster (`.swatch-row > *`, `.watercolor-swatch`, + the print twin) deliberately exempts every colour mark, and the U-F25 focus bind reaches both native buttons — PASS; while SELECTION's sole carrier is a box-shadow ring forced-colors suppresses — FAIL, `aria-pressed` the only survivor (mechanism banked, AdminUsersPanel/BlobPane). Nothing here needs a WebKit frame — and D §2.6's unlabeled WebKit forced-colors row is inadmissible regardless (BlobPane.md:25, CSP G4: VOID in either direction).
- **K-5 — D-3's framing ("the colors-mode empty state was never designed") — KILLED as framed; defect NARROWED into MSS-21.** The seeded-ghost register is designed, shared, and commented (EmptyState's own trio — verified this seat); absent are the copy and an operable seat.
- **K-6 — D-30's consequence limb ("every state row is probe-derived") — KILLED (DU, UPHELD).** The four safari matrices hold mix.png at rest (banked, wb-mix-animationcanvas.md:7), and the L axis itself leans on one as primary evidence. The premise (no mix.png in the six STATE matrices) survives inside the U-list.
- **K-7 — C-1(r2)'s four-call-site census — KILLED (LC, UPHELD; verified this seat).** C.r2:88 counts `:122` — that is TransitionGroup's own documented `tag` prop (my read :120-123), passed and consumed correctly. The real WatercolorDot `tag=` sites are exactly three. A cure wave taking r2's list would repair a phantom.
- **K-8 — C-14(r2)'s "module-level `Map`" — KILLED (LC, UPHELD; verified this seat).** C.r2:450 verbatim; `<script setup>` emits both as per-instance closure state. Identity precedent: K-9 (CurrentPaletteEditor.md:95). The churn finding is untouched (MSS-3). L:289's "component-local" was already correct.
- **K-9 — C-19's "NEW r3" provenance — KILLED (LC, UPHELD; verified this seat).** C.r1:483-485 states the same finding with the same five anchors and the same cure; C.r3:16/:368 sells it as unreached. The finding itself is true and folds into MSS-10; the provenance claim dies — r3's "four new findings" is three.
- **K-10 — L-16's 30.3× payload framing — KILLED (LC, UPHELD).** Marginal cost at this subtree is zero: the glass root barrel is already in this file's module closure through PaletteCard's bare-root `writeClipboard` import and the 18/19 barrel census (banked PS-29/K-10, verified PaletteCardSwatches.md:27/:72); L's own P3 partially retracts the figure. The single-consumer/inconsistency limb survives (MSS-26); the closure byte-figures stay U-9.
- **K-11 — D-9's producer-floor attribution — KILLED as attributed (both readers convergent; ruling R-6).** `.disclosure-group-trigger`'s 44 px floor is an opt-in the producer never applies to a Collapsible (`.disclosure-trigger` carries border-radius + touch-action only). The defect survives at MSS-33.
- **K-12 — D-24 as an independent second BLOCKER — KILLED as booked (LC's kill adopted; ruling R-1).** One mechanism restated at the query surface + a CI limb already banked (MX-3/CC-031). Survives as MSS-2's fold at MAJOR-process.
- **K-13 — L-10's reachability hypothesis ("no path persists a zero-color palette") — KILLED (LC, UPHELD; load-bearing).** The banked update-path write (CurrentPaletteEditor A-2 arm ii: latched banner + emptied buffer → `colors: []`) produces the operand; L searched only the create path. This kill is what converts MSS-13 from latent to reachable.

## Superlatives — provenance preserved

1. **useMixingState.ts's ONE-CLOCK docblock, honoured in full** (all three axes + both readers; re-derived this seat from the whole file): zero timers, synchronous `startMix`, the single phase-guarded forward edge driven by the canvas `@settled`, a real re-entry guard at :83 — third independent crowning (wb-mix-configbar.md:98: "the cleanest module in the mix tree, again"). Every defect in this corpus sits at the view boundary or the math leaf, never in the machine.
2. **Array replacement over mutation, and it is load-bearing** (LC; re-verified :56/:60/:67/:71): the only reason the consumer's `watch(() => selectedColors, …)` fires at all — a cross-file correctness dependency nobody wrote down that happens to hold, with C.r3's own fragility note (the pruner compares by reference) as its falsifiable rider.
3. **`mixColorSequence`'s precondition block** (LC; mix.ts:46-55 re-read this seat): four named, throwing preconditions — the one function in this cone that refuses to mask, which is precisely what makes `getColorAtIndex`'s missing `len === 0` precondition (MSS-13) conspicuous rather than excusable.
4. **The palettes-mode empty state and its 14-line comment** (:231-244; all three axes + both readers): records the owner overrule by name, refuses ghost-card filler, and makes a checkable claim about its own semantics ("this store is synchronous — no loading species exists") that checks out.
5. **K-PALID, kept** (LC): selection keys on `slug` with the reason written (:55-59, useMixingState:63-72), against the tempting local-only `id` — and the types confirm the premise.
6. **The :134-145 register law as retirement craft** (LC, minus the indictment clause per R-5): mechanism, probe date, and replacement law for a dead ring — how a repo should retire a defect.
7. **The colour marks are enrolled in the demo's colour-survival roster in BOTH arms** (DU — the provision all three forced-colors discussions missed): `forced-color-adjust: none` on `.swatch-row > *` and `.watercolor-swatch`, repeated under `@media print` — the design answer that decides K-4.
8. **The producer contract predicts the consumer's failure in full** (LC; banked at CurrentPaletteEditor superlative #1): every fact needed to derive MSS-1 AND MSS-18 is published in the d.ts the consumer imports.
9. **The disclosure's a11y wiring is producer-owned and entirely correct** (DU: minted id pair, `aria-controls`/`aria-labelledby`/`role="region"`) — the in-file contrast proving MSS-10 is consumer authoring, not a systemic producer gap.
10. **`.dashed-well` and `.swatch-row` as one-recipe discipline with a written census** (DU) — craft and its violation (`.add-slot-ghost`, MSS-23) a hundred lines apart, with the governing law already written between them.
11. **The corpus's own evidentiary discipline** (both readers): D §2.2 refusing to let a RED gate prove the defect it was written for; C §R-1/§R-2 falsifying its own prior round by measurement; L §P3 retracting its own headline figure and striking its own pass-1 negative; three voluntary weakest-evidence disclosures, one per axis.

## UNPROVEN-NEEDS-LIVE residue — the next visual audit's opening slate for this component

- **U-1** — D-27's 2.88:1 ghost-stroke/well ratio (mechanism byte-certain; the number needs a computed-style read).
- **U-2** — D-14's rendered RTL badge (`-top-1 -right-1` and `text-left` are physical; no rtl mix capture exists; blocked behind MSS-1 anyway).
- **U-3** — MSS-5's PAINTED result on a focused selected card (identity with the banked U-3 residue at CurrentPaletteEditor ADJ-m3: does Tailwind's `var(--tw-ring-color, currentcolor)` fallback paint anything over the tombstoned token, both schemes).
- **U-4** — MSS-6's delivered motion (0.2s/ease-out vs the 48-stop `--spring-smooth` triple; cascade half is source-certain).
- **U-5** — which spec failure fires first today (C-2′'s dock-stability arm vs the add-slot assertion — decides whether wiring CI before curing the dock substitutes one silence for another).
- **U-6** — MSS-33's live trigger geometry (source arithmetic reproduces both axis numbers to 0.1 px; a confirming probe closes it).
- **U-7** — MSS-11's eviction at real library size (source shows no bound; the fold line is viewport-dependent).
- **U-8** — L-13's attribution A/B (`sideEffects: false` vs the inline-HTML entry; the dist artifact is gone at this HEAD so BASELINE must be named; App.md C-6's nearby-but-different ruling must not be conflated) — this decides whether MSS-29's workspace split is necessary or merely sufficient.
- **U-9** — L-16's port-closure byte figures (42 modules / 168,138 B: premise source-certain via usePalettePorts' seventeen composable imports; figures unreproduced).
- **U-10** — the six STATE matrices carry no mix.png (premise of D-30, surviving K-6): reduced-motion / forced-colors / zoom-200 / rtl / keyboard-focus arms are unwitnessed at rest+state; any future forced-colors frame must be labeled per the two-channel law (BlobPane CSP G4) or it is VOID.
- **U-11** — D-22 ("three compositions in one file") and the D-axis proportion figures (well ink ~4.6 %, 23.8 % share): live geometry plus a design judgment — not carried as a defect row without an owner ruling.

## Laws honoured

1. **Sole write** = this file; product source read-only; zero browser tooling.
2. **Dated evidence never rewritten** — the corpus and both re-reader returns stand immutable beside this record; corrections here are addenda (E-3).
3. **Motion/PRM**: motion-quarantine.md grepped this seat — no FALSE row names this component; no quarantined claim is carried (MSS-17's registry-check included).
4. **Anti-rename identity guards**: ten rows fold to banked identities (CC-044, MX-3, MX-41, ADJ-m3-tombstone-limb, AdminNamesPanel D-12/C-8, C-9-as-rescoped, C4-1, PC-25, ColorSpaceSelector L-14 + L-5(a), App.md G1, PS-29) — one identity each, never re-booked under a second name.
5. **Dispositions are ADJUDICATED, not VERIFIED** — X-wave routing follows the CARRY-CUT-LEDGER vocabulary and the X wave-bounds census re-read at wb-mix-animationcanvas.md:7; rows whose cure home no X wave holds (useMixingState.ts, demo/palettes/mix.ts, the RED specs, W-HYGIENE config/manifest rows) are marked **NO-WAVE-OWNER** for the next formation boundary; producer questions ride the standing glass-ui BH relay per the owner edict.

## Closing verdict

**ADJUDICATED — GRAVE.** 33 rows survive two hostile re-readings and this seat's counter-attack (1 BLOCKER · 14 MAJOR · 16 MINOR · 2 INFO, ten of them identity-folds to banked rows) against 13 killed claims — among the kills, one phantom call site, one false scope, one false provenance, one dead payload figure, one mis-attributed producer floor, and one reachability hypothesis whose death is load-bearing: the pane's default arm is born dead against shipped Glass 7 (MSS-1, CC-044-blocked), the gate stack that should have said so is unsatisfiable and unwired (MSS-2), and the working palettes arm crashes synchronously in a click handler on an operand the product can already write, under two ordinary Select options no axis thought to try (MSS-13 — this corpus's arbitration headline, raised from two MINORs by one byte-level re-derivation). The state machine beneath it all, its precondition discipline, and the producer's self-documenting contract remain the best-evidenced craft in the cone. Every surviving row leaves with exactly one terminal disposition; the U-list is the visual audit's opening slate, headed by the selection ring's painted truth and the state-matrix gap no capture has ever covered.

— end of apotheosis. The challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
