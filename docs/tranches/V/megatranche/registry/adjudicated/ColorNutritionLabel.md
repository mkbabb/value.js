# ColorNutritionLabel — adjudicated apotheosis (M-23/L-18 fresh adjudicator) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Seat**: fresh-Fable adjudicator (M-23/L-18 hostile-reread arbitration) — the incredulity seat, not a transcriber.
**Date**: 2026-08-04 · corpus HEAD `c654824e` · source re-verified at HEAD `2e334231` (branch `tranche-u`; every cited line anchor in `demo/scenes/about/ColorNutritionLabel.vue` (242 lines), `demo/color-session/ink.ts` (174 lines) and the dependency chain re-read byte-identical at the newer HEAD — the component tree is unchanged between the two).
**Corpus**: `docs/tranches/V/megatranche/audit/components/ColorNutritionLabel/` — `challenge-C-implementation.md` (r2 governs; pass-1 beside it), `challenge-D-design.md` (r2 governs; pass-1 beside it), `challenge-L-library.md`.
**Re-readers adjudicated**: `l18-hostile-reread-ColorNutritionLabel-design-and-usage` (reader-D) · `l18-colornutritionlabel-library-and-code` (reader-L). Both were attacked before either was adopted: every reader-vs-reader contradiction was re-derived against the tree with this seat's own instruments, and two reader evidence cells were killed in the process (one a citation-inheritance event of exactly the ColorSpaceSelector DISSENT-1 class).

**Arbiter's own instruments this session (static + node against the shipped dist; ZERO browser probes — probe parsimony; product source READ-ONLY)**: full reads of ColorNutritionLabel.vue (whole 242), ink.ts (whole 174), ErrorBoundary.vue (whole), AboutPane.vue:15-98, useColorPipeline.ts:55-185, useColorParsing.ts:25-100, useColorUrl.ts:20-85, useContrastSafeColor.ts:40-50/:150-172/:240-275/:340-365, picker-color.ts:95-205, ComponentSliders.vue:60-135, ConsoleRail.vue:168-182, ColorPicker.vue:346-356, hydrate.ts:60-100, color-model.ts:25-80, colorSpaceInfo.ts (13-key census by grep + header + rgb/kelvin/hex entries + lab conversions), src/color/operations.ts:20-265, src/color/anchors.ts:160-180/:330-380, viewSchema.ts:35-49/:100-115, ARCHITECTURE.md:35-50, demo/ui/alert/index.ts (whole), glass-ui `dist/styles/typography/scale.css` + `semantic.css` + `fonts.css` (font-style census: 4 faces, all `normal`, zero Fraunces), demo/styles/foundation.css:205-225 + animations.css:180-215, demo/color-picker/index.html:40-82 (Fraunces face census: two italic faces at :65/:73), o18-contrast-census.spec.ts:697-743 + `TEXT_FLOOR = 4.5` (:73), o10d:35-45, motion-quarantine.md M-03/M-17/M-29/M-33 verbatim; repo censuses re-run myself: `ColorNutritionLabel` in test/+e2e = 1 comment hit; the L-5 predicate = 5 sites; `.notes` consumers = 0; `demo/ui` = 19 dirs; `<h1` in demo = 0; PaneHeader `<h3>` :21; assets/docs = 11 files. **Node instruments against `dist/subpaths/color.js`**: 0–255 grey sweep → lch `"none"` 256/256, oklch `"none"` 256/256, hsl failures exactly `[[255, color_non_finite]]`, hsv-powerless-none at **28 grey values** `[0,3,6,23,26,43,46,50,51,52,62,74,96,122,135,143,157,159,160,161,187,225,228,243,247,248,251,253]`; `black→hsv = ["none",0,0]`; `rgb(255,255,254)→hsl` s = 1.000000000000085; **onward conversion of a none-hue color SUCCEEDS** (`lch(53.585 0 none)→hsv = [340, 6.6e-16, 0.502]` — numericSource substitutes 0), which bounds the boot-crash class; WCAG recompute of D-11's pixels (plate rgb(240,190,202) Y 0.5962 / ink rgb(170,0,67) Y 0.0895 → **4.63:1 exact**; plate OKLab L **0.8491760**; neutral at that L Y 0.6123 → **4.75:1**) — the linear-L decomposition holds with my own numbers.
**Registry identity sweep (anti-rename)**: ColorSpaceSelector.md (CSS-L-1..L-15 + MT-CSS-1/2), AboutPane.md (AB-1/2/10/17/26/36, riders A-1/A-3, K-6, observed-anomaly 2), CARRY-CUT-LEDGER.md X-W0..X-W11 rows. Several of the re-readers' "missed by the whole corpus" claims are pre-banked registry identities — ruled below, not re-booked.

**Environment disclosure**: no browser was needed; every ruling in this record is decidable from source + dist + tracked evidence + the two readers' internally-consistent live cells (adopted as corroboration only, never load-bearing where a static derivation exists). Pixel claims remain labeled by cell.

## Scoreboard

**27 CONFIRMED roster rows (5 BLOCKER · 15 MAJOR · 7 MINOR/INFO, several merged bundles) · 17 KILLED-CLAIMS register rows (12 reader kills ratified + 5 arbiter kills/corrections OF the readers) · 20 reader misses adopted as true (4 returned to pre-banked registry identities under the anti-rename guard; 3 promoted to BLOCKER/MAJOR roster rows) · 9 UNPROVEN-NEEDS-LIVE residue rows.**

The corpus's headline BLOCKER (the catalog lie) survives and is already owned born-RED at **X-W6.f** (one identity with CSS-L-1/AB-1 — cross-booked, never re-booked). The gravest NEW results are two crash BLOCKERS **no axis filed**: an ordinary two-click interaction (any achromatic color → select LCh/OKLCh) kills the entire pane grid behind the ErrorBoundary, and a persisted/deep-linked near-black boots the app to a genuine white screen from App's own setup — both measured against the shipped dist by this seat, both instances of the codebase forgetting its own documented powerless-hue pattern.

## Ruled disagreements and arbiter kills (the seat's actual work)

| # | dispute | RULING | deciding evidence (arbiter's own) |
|---|---|---|---|
| R1 | **The certification target: 5.625 (challenger + reader-L's confirmed D-11 cell) vs 5.75 (reader-D's kill)** | **5.75 — reader-D wins; reader-L's cell is KILLED as a citation-inheritance event.** | ink.ts:15/:17/:137 read by this seat: `certify(accent, surfaceL, floor + CERTIFY_HEADROOM)` — **additive**, 4.5 + 1.25 = 5.75; the codebase names the figure in prose (useContrastSafeColor.ts:167-168 "4.30:1 measured vs the **5.75** walked target"). Reader-L wrote "= 5.625 (ink.ts:15,17,137)" — repeating the challenger's multiplication **while citing the exact lines that refute it**. Same class as ColorSpaceSelector DISSENT-1: a stale cell repeated without re-derivation is evidence of inheritance, not confirmation. The D-11 verdict class SURVIVES on both readers' other grounds — and is sharper at 5.75 (the true light-arm gap is ~19.5%, and the o18 gate asserts only `TEXT_FLOOR = 4.5` (:73, my read), so the guard is measured 1.25 below its own contract). |
| R2 | **L-12 ("`?? \"\"` provably dead — exactly two null arms") vs C-2 (measured 3.97:1 through that same line)** | **L-12 KILLED; C-2 CONFIRMED — both readers concur and the seat's source read settles it.** | ink.ts:163-173: the `for (endpoint of endpoints)` loop falls through to `return null` — a THIRD arm L-12 never enumerated. Reachable: operations.ts `clears` takes `Math.min(floatRatio, byteRatio)` (unclipped luminance AND 8-bit painted), so an out-of-sRGB fill fails both endpoints → `contrast_unreachable` → `result.ok` false → null → `?? ""` → Vue drops `color:` → the saturated fill paints under inherited plate ink. Arithmetic re-derived by both readers independently and consistent with this seat's reads (black min(3.07, 4.77), white min(6.85, 4.41), painted outcome 3.97:1). **Arbiter correction of reader-D's cell**: reader-D attributed the null to ":169's unmoved-test rejecting an ink that would have passed" — imprecise; for the C-2 fill `safeAccentColor` errors before the identity test ever runs. The correct demotion target is the FLOAT-ratio leg (certifying against an unpaintable unclipped luminance), reader-L's account. C-2 unchanged. |
| R3 | **L-2's attribution ("the whole-SPA blank belongs to this component's seat") vs reader-D's ErrorBoundary kill** | **Kill RATIFIED — and it splits the two new crash rows correctly.** | ErrorBoundary.vue:59-68 (`onErrorCaptured` → paint fallback → `return false`) wraps the pane grid at App.vue:50-140 (my read). A render throw in ColorNutritionLabel, ComponentSliders or ConsoleRail is CAUGHT — it cannot produce `bodyLen: 0`. But `useColorPipeline(model)` runs at App.vue:245 in App's OWN setup — `onErrorCaptured` sees only descendants, so a throw THERE white-screens for real. Consequence: reader-L's missed-BLOCKER-1 outcome limb ("blanks the whole SPA") is **RESCOPED by this seat** — the two-click crash paints the announced fallback over the ENTIRE pane grid, and Try-again re-throws (the model still holds the `"none"` hue) — a persistent dead product surface with a working dock, not a white screen; the boot crash (missed-BLOCKER-2) is the true white screen. A repair briefed to the wrong symptom would probe the wrong layer. |
| R4 | **C-5's cure 2 ("certify off `cssColorOpaqueFrame` … provided app-wide" — reader-L confirms, reader-D kills)** | **Cure KILLED — reader-D wins; the defect mechanism survives untouched.** | My App.vue reads: CSS_COLOR_KEY is provided as the UNCOALESCED `cssColorOpaque` (:271); `cssColorOpaqueFrame` is destructured from the pipeline (:251) and reachable only via COLOR_MODEL_KEY — the 38-key bag the ColorSpaceSelector adjudication's L-7 forbids new consumers of. Adopting the cure couples a presentational leaf to the pipeline (L-7's own argument), and useColorPipeline records the coalescing scope as deliberate. The cure-adjudicated-separately law (ColorSpaceSelector addendum 2) applies verbatim. |
| R5 | **D-17 (motion register, MINOR) — reader-D adjudicates residue, reader-L REFUTES as a defect** | **REFUTED as a defect — reader-L's adjudication ratified under the quarantine standard (fresh, no FALSE row carried: no M-xx row names this component).** | My reads: animations.css:184-193 (global `!important` reduce guard) + the glass a11y override both reach the file; the file's only motion is `transition-colors`, whose colour legs the producer preserves deliberately (M-17's measured precedent, verbatim class). Filing central governance as a per-file gap is the M-03/M-29 scope-shopping pattern. **Surviving residue (reader-D's, INFO)**: `TooltipTrigger as-child` stamps `data-state` on the decorative well div, so a non-overlay element claims animations.css:202-212's overlay-only 150ms opacity carve-out (my read of both blocks) — a mis-scoped match surface with a benign outcome; rides whichever wave touches the file. |
| R6 | **"Missed by the whole corpus" framings vs the registry** | **Four of the readers' misses are PRE-BANKED identities — adopted as true, KILLED as novel, cross-booked under original ids (anti-rename).** | The Detailed-Guide orphan incl. ictcp/jzazbz = ColorSpaceSelector L-1's missingDoc(7) + AboutPane AB-2 (→ X-W6.f, `doc: null` authored empty state); the AboutPane markdownModules second partial table = AB-2 verbatim; the pane-level RTL identity-line corruption = AB-36 (that seat read `frames/d9-rtl.png` itself; NO-WAVE-OWNER); the mount-violation root = AB-10's carry-note + rider A-1 (the first-class `/about` destination "appears in NO ledger row and no X wave by name"). Booking any of these as new rows would violate the identity guard. |
| R7 | **Reader-D's faux-italic provenance cell** | **Finding CONFIRMED, cell CORRECTED.** | Reader-D cited "`glass-ui/dist/styles/fonts.css` … `\"Fraunces\"` italic IS loaded". My census: fonts.css has 4 faces, ALL `font-style: normal`, and **zero Fraunces** — foundation.css:214 states "the glass-ui corpus carries no Fraunces". The Fraunces italic faces are SELF-HOSTED at demo/color-picker/index.html:65/:73. Jakarta ships no italic anywhere → the component's 10 `italic` sites are browser-synthesised faux oblique, exactly as filed — with the repair pointed at the right font pipeline. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Wave ids are the CARRY-CUT-LEDGER X set; NO-WAVE-OWNER rows ride rider A-1's list to the next formation boundary under their original ids.

### BLOCKER band

- **CNL-1 · C-1 / D-1 / L-1 — the catalog lie, SHARPENED · BLOCKER.** `:210-215` falls to `colorSpaceInfo.rgb` for every space the 13-key table lacks; the selector offers 18. My own censuses: 13 keys at the cited lines, `DISPLAY_COLOR_SPACE_NAMES` = 18, `resolveColorSpace` folds hex→rgb BEFORE the lookup (:211). **Reader-L's kill of D-1's base arithmetic is RATIFIED by my source read**: the rgb entry itself is FALSE — `name: "CIE RGB (Red, Green, Blue)", created: "1931", whitePoint: "Varies"` (colorSpaceInfo.ts:18-25, read verbatim) against a library whose `rgb` anchors through `encodedSrgbToXyz` (anchors.ts, my read) — i.e. encoded sRGB, fixed D65, 1996. The fallback propagates an already-false article into six slots; true count 11 of 18. The hybrid row stands (AboutPane D-9-limb ruling: ranges TRUE from `PICKER_CHANNELS`, identity facts lie). **DISPOSITION: X-W6.f (f1/f2/f3) — ONE identity with CSS-L-1/AB-1, cross-booked. Rider from this seat: f1's authoring obligation now includes CORRECTING the rgb row to the encoded-sRGB truth, not merely adding five rows — a total catalog seeded with a false base article passes f1's shape gate while still lying.**
- **CNL-2 · NEW (reader-L miss 1, RESCOPED by this seat) — two ordinary clicks kill the pane grid: achromatic color + LCh/OKLCh · BLOCKER.** Premise measured by this seat against dist: **256/256 greys** emit `"none"` hue in both lch and oklch (residual chroma ≈ 5.6e-16 under `isPowerless`'s `c <= 0.000004`; `finishChannels` writes the sentinel). Chain re-read end-to-end: ColorPicker.vue:348-354 watcher → `updateToColorSpace` (useColorPipeline.ts:170-173, CROSS-space so the same-space short-circuit does not apply) → `setCurrentColor` writes the none-hue color to the model (its own hue-refresh is guarded, the WRITE is not) → `clampPickerColor` preserves `"none"` (picker-color.ts:197) → ComponentSliders `sliderValue` → `normalizedChannel` → `channelNumber` throws `Missing lch.h` in RENDER (the alpha arm three lines up IS guarded — the author knew) — and `astEcho` (useColorParsing.ts:95-99) throws the same way in ConsoleRail, a second surface. **Outcome rescoped per R3**: the ErrorBoundary catches it and paints the fallback over the whole pane grid; Try-again re-throws; the surface is persistently dead until the color changes by another route. **DISPOSITION: X-W9 (the consumer-proven library seam: the `"none"` protocol must be unrepresentable-to-forget at the demo boundary — the guarded/unguarded sibling arms are the proof the convention cannot be held by discipline) + X-W1 born-RED e2e (two clicks, assert no `role="alert"` fallback). If X-W9's cut excludes demo seams, this row escalates to the boundary with rider A-1.**
- **CNL-3 · NEW (reader-L miss 2, SHARPENED by this seat) — boot-time white screen from App's own setup; self-perpetuating via the persisted seed · BLOCKER.** useColorPipeline.ts:75-76 `const initHsv = convertPickerColor(model.value.color, "hsv"); const stableHue = ref(channelNumber(initHsv, "h"))` — unguarded, in App.vue:245's setup, OUTSIDE the ErrorBoundary (R3). The identical conversion twelve lines down (:88-95) sits in try/catch with the powerless-hue comment. **My sweep sharpens the trigger set**: not "black" alone — **28 of 256 greys** round-trip to hsv-powerless (`["none", 0, v]`), so any of those greys as the persisted/deep-linked seed (hydrate.ts `modelFrom` admits them — black parses and converts fine) throws `Missing hsv.h` before mount. My onward-conversion probe bounds the class: a persisted none-hue lch/oklch string does NOT boot-crash (numericSource substitutes 0) — it re-enters CNL-2's render crash instead. **DISPOSITION: X-W9 (same seam, same cure family as CNL-2) + X-W1 born-RED boot test over the seed matrix (`?color=black` at minimum).**
- **CNL-4 · C-2 (+ L-11 as root) — the fail-open ink pair · BLOCKER.** Confirmed per R2 with the mechanism corrected. The F-3 split's architecture is right (see superlatives); its tail (`?? ""`, :208) converts the library's honest `contrast_unreachable` refusal into an unmarked lie, and its dependency is inverted: `contrastInkFor` (ink.ts:157-174) rebuilds a PREDICATE from a SOLVER because `src/subpaths/color.ts` exports no contrast primitive (reader-L's grep, consistent with my operations.ts read — `contrast` is module-private). **DISPOSITION: X-W9 — publish the contrast primitive (`contrastRatio` or equivalent), retiring the epsilon-identity idiom; the consumer's atomic fill+ink pair (one computed yielding both or neither) rides the About recomposition bundle → NO-WAVE-OWNER (AB-17 / rider A-1); the gate that let it ship is CNL-5's.**
- **CNL-5 · C-12 — the only executing coverage is vacuous · BLOCKER.** My greps: 1 hit in test/+e2e and it is a comment (o10d:41); the o18 rows (:697-743, read whole) boot ONE color, take `.first()`, assert ratio-only at `TEXT_FLOOR = 4.5` — blind to CNL-1 (never reads a rendered fact), CNL-4 (never varies the color), CNL-6's hover bleed (never counts styled nodes), and one notch below the module's own 5.75 target (R1). **DISPOSITION: X-W1 (CC-030/CC-031) — the born-RED oracle successors named in the rows above, plus a parameterised `?space=`/`?color=` sweep (the surviving kernel of the killed L-14).**

### MAJOR band

- **CNL-6 · D-3 / C-3 / L-8 — hover lights 6 of 10 nodes across all four rows · MAJOR.** Statically certain from my reads: one component-level `hoveredPath` ref (:232), predicate `hoveredPath.includes(space)` (:123) with no row identity; lab's 4 paths (colorSpaceInfo.ts:133-138, read) all begin "Lab" → 4 + 2 "XYZ" = 6 lit, 4 resting; xyz worse (8 paths). The row-index cure retires the `:111`/`:123` casts as a side effect. **DISPOSITION: NO-WAVE-OWNER — the conversion-graph well is inside the About recomposition bundle (AB-17 / rider A-1).**
- **CNL-7 · D-9 / C-4 / L-10 — a live Tooltip stack per row displaying zero characters · MAJOR.** `:97` TooltipProvider INSIDE the v-for; `:139-140` empty `TooltipContent class="contents w-64 p-2"` (self-cancelling: `contents` deletes the box `w-64 p-2` would size); `.notes` consumers = 0 (my grep) against 13 authored strings. **DISPOSITION: NO-WAVE-OWNER (AB-17 — PR-14's "empty Tooltip 1→0"); the `notes` DATA disposition (render or delete) is decided at X-W6.f's catalog cut, not drifted.**
- **CNL-8 · D-8 — advertised operability without any · MAJOR (characterisation corrected).** `:109-113`: `cursor-pointer` + hover classes + mouse-only handlers, zero `role`/`tabindex`/name in the whole template (my read); inert on touch. Both readers' kill of the "tabIndex: -1 explicitly removed" cell RATIFIED — no `tabindex` attribute exists; -1 is the DOM default for a non-focusable div. Mechanism is omission, not removal. **DISPOSITION: NO-WAVE-OWNER (AB-17 — PR-07 ADD-AFFORDANCE / REMOVE).**
- **CNL-9 · D-7 — five sectional dividers ordered to zero · MAJOR.** `:15/:40/:68/:90/:146` + AboutPane's two = PR-14's seven; AboutPane rider A-3 already fixed the census vocabulary (7 components + 4 markdown `<hr>`). **DISPOSITION: NO-WAVE-OWNER (AB-17/A-3).**
- **CNL-10 · D-4 — fixed heading rung under fluid data rungs; wrong token, wrong family, five times · MAJOR (cure limb killed).** My scale.css read: `--type-subheading: 1.272rem` FIXED vs body/small clamps → crossover at 2086px (my arithmetic matches both readers to the decimal); `:18/:43/:71/:93/:149` all `font-display text-subheading` against the closed role matrix (`text-heading`, Jakarta). Reader-D's kill of the token-swap cure RATIFIED by the same read: `--type-heading: 1.618rem` is ALSO fixed — no consumer cure puts the block on one curve; the scale itself mixes regimes. **DISPOSITION: X-W10.d (role-matrix conformance) + the standing glass-ui BH relay for the fixed/fluid scale seam (producer-owned; owner edict — every glass-level change relays).**
- **CNL-11 · D-10 / C-8 — the Components rows disintegrate under RTL · MAJOR.** Mechanism forced by source (`:59-63`, three unisolated inline runs, no `dir`/`<bdi>`/`unicode-bidi` anywhere in the file — my read) + the tracked rtl-desktop capture both readers and the AboutPane seat read. The pane-level limb (title/caption) is AB-36's identity. **DISPOSITION: NO-WAVE-OWNER — one bidi-isolation bundle with AB-36 at the boundary; the atomic LTR-isolated range formatter is the named cure.**
- **CNL-12 · D-12 / C-11 — five H2 under an H3, route has no H1 · MAJOR.** PaneHeader.vue:21 `<h3>` (my read); `<h1` in demo = 0 (my grep). **DISPOSITION: X-W5 (route identity/H1/title settlement — the AboutPane routing precedent).**
- **CNL-13 · L-3 / L-3.4 — five declarations of "the channels of a space", no owner; two contradict on one page · MAJOR.** SPACE_SCHEMA carries no ranges/units against ARCHITECTURE.md's claim; the parser holds the ranges as literals; `src/subpaths/color.ts` exports no table; PICKER_CHANNELS; 11 `### Attributes` docs — with rgb.md's "0 to 1" rendering one scroll under the label's "0 to 255". **DISPOSITION: X-W9 (the schema becomes the published authority — the ARCHITECTURE claim becomes true or the doc is corrected) + X-W6.f for the markdown content rows (catalog-owned).**
- **CNL-14 · L-5 — the display-scale predicate copy-pasted five times · MAJOR.** My grep: exactly 5 sites (two in useSliderGradients.ts); `currentColorRanges` already ambient via the pipeline. **DISPOSITION: X-W9 — the predicate is a schema fact (`ChannelMeta` should carry it); consumer dedup follows the schema, not the reverse.**
- **CNL-15 · L-4 — nineteen one-line forwarding directories, banned by name · MAJOR.** My census: 19 dirs; the component's 3 edges (`:173/:174-179/:181`). ARCHITECTURE.md:39 bans the layer verbatim (my read). NOT the shadcn charge — see killed K1. **DISPOSITION: X-W8 (barrels/graph subtraction); the component's own edges die whenever any wave touches the file; the repo-wide identity keeps its X-W8 home (scope law — the count lives repo-wide).**
- **CNL-16 · L-7 / C-9 / D-19 + C-5 — whole-model two-way binding never written; per-tick fan-out off the uncoalesced projection · MAJOR (cure 2 killed per R4).** `defineModel<ColorModel>` (:186) read at exactly two lines (:211/:219), both `selectedColorSpace`; `model.value = next` per tick (useColorPipeline.ts:70, my read); `formattedRange` fresh object per evaluation. The `defineProps<{space}>` cure is sound and kills both rows. **DISPOSITION: NO-WAVE-OWNER — the MT-CSS-2 identity is a PHANTOM OWNER (AB-26/rider A-1 precedent); preserved under original ids for the boundary.**
- **CNL-17 · D-11-as-rescoped + the linear-L referent (reader-D's miss, arithmetic re-run by this seat) · MAJOR.** What survives of D-11 after R1 and reader-D's mechanism kill (the live-probe resolver EXISTS — useContrastSafeColor.ts:46-48/:240-272, my read; PRODUCER_TINTS is the non-browser fallback only): the referent composes tint-over-ambient as a LINEAR OKLab-L mix (ink.ts:103/:109/:116, my read) and certifies against a CHROMA-ZERO neutral (`surfaceColor(surfaceL)`, :74) — the exact divergence class the same file already diagnosed and fixed for the veil ("4.30:1 measured vs the 5.75 walked target", sRGB alpha-composite cure, useContrastSafeColor.ts:155-169) without applying it to `resting`, the rung this component selects (:199). My decomposition from D-11's own pixels: 4.63:1 exact; neutral-at-plate-L 4.75:1 → chroma-blindness ≈ 0.12 of the shortfall, the linear-L overstatement the rest of the way to 5.75. The dark-mode chroma collapse (8.95×) mechanism is credible from `certify`'s gamut-mapped walk (reader-L's account); outputs need-live. **DISPOSITION: the ink-referent family has no X owner → NO-WAVE-OWNER (rider A-1 extension, named "the resting-rung composite referent"); the gate-floor mismatch (4.5 asserted vs 5.75 targeted) rides X-W1's oracle successor.**
- **CNL-18 · NEW (reader-L miss 3, re-measured by this seat) — white cannot convert to HSL · MAJOR.** My dist run: grey sweep fails at exactly v=255 with `color_non_finite`; neighbours fine; `rgb(255,255,254)→hsl` s = 1.000000000000085 (out of declared domain). Mechanism from my anchors.ts read: `encodedToHsl` divides `delta / (1 - |2L - 1|)` BEFORE the powerless law can see the channels, and convertColor's non-finite check precedes `finishChannels` — the guard exists but runs in the wrong order. Demo consequence (traced, not run): white + select HSL throws through `valueOrThrow` inside the ColorPicker watcher, desynchronising `selectedColorSpace` from `model.color.space`, then `Unknown rgb channel: h` in render. **DISPOSITION: X-W9 — a library correctness row (powerless test precedes the division), with the CNL-2 gate covering the demo symptom.**
- **CNL-19 · D-6 (surface limb) + producer re-attribution · MAJOR.** The Alert reads as a chromatic stain, not a well (opaque neutral `--well-bg` derived from untinted `--card` inside a translucent tinted surround — mechanism static; pixels corroborated by the tracked light capture both readers read); the well tier is reserved for specimens. The 14px sub-limb is RE-ATTRIBUTED per killed K1: glass-ui's own AlertDescription hardcodes `text-sm` against the root's fluid `--control-text` — a PRODUCER seam. Reader-D's lede finding folds here: the one sentence the plate exists to deliver is its smallest text and its relative size DEGRADES with viewport (fluid title vs fixed description — ratio 1.17 → 1.43, static arithmetic from the same tokens). **DISPOSITION: NO-WAVE-OWNER (AB-17 recomposition — PR-14 `Alert 1→0`) + glass-ui BH relay for the fluid/fixed AlertDescription seam (with the corrected attribution so the fix lands in the right repository).**
- **CNL-20 · the mount is the common root (reader-D's miss ≡ AB-10/rider A-1) · MAJOR.** ARCHITECTURE.md:41-45 (my read): `/about` first-class, "not Picker's permanent right-hand companion", "no … hidden view-schema alias". Shipped: no `about` ViewId; `VIEW_MAP.picker.right = "about"` (viewSchema.ts, my read). Explains D-5's pane cap, CNL-12's H3 container, D-20's overflow, and the killed L-14's evidence hole in one row. **DISPOSITION: X-W5 (route topology) — cross-booked with AB-10's carry-note; the first-class-`/about` gap itself is rider A-1's, already handed to the boundary.**

### MINOR band (merged)

- **CNL-21 · D-18 / C-6 / L-6 — `colorSpaceInfo.hex` is unreachable dead data; the card contradicts its own guide on Hex · MINOR.** `resolveColorSpace` can never return "hex" (my read); both consumers resolve first (`:211`; ConsoleRail:173-174 with its forced `as any`); the hex entry (:313-333, read) holds correct facts that can never render while AboutPane loads hex.md. **DISPOSITION: X-W6.f (dual keying: docs by `DisplayColorSpace`, math by `SpaceId`).**
- **CNL-22 · C-10 / L-13 — three type escapes rooted in the bare `as const` · MINOR.** `:111/:123/:160` confirmed in my read, with `:154`'s uncast twin as the tell; the `satisfies` cure retires all three. Adjacent: the file HEADER asserts the totality invariant the data violates ("Keys cover the `DisplayColorSpace` union" — 13 ≠ 18) and points at a module that no longer holds the type (reader-L miss 4, verified). **DISPOSITION: X-W6.f — the satisfies cure makes the comment true or the build red; the header is corrected in the same edit.**
- **CNL-23 · dead authored data + the second partial table · MINOR.** `name` has zero consumers (my greps: only CNL and ConsoleRail touch the table; neither reads `name`) and where legible contradicts `PICKER_SPACE_NAMES`; with `notes` = 26 authored fields nothing renders (reader-L miss 5). The AboutPane markdownModules partial (reader-L miss 6) = AB-2's identity. **DISPOSITION: X-W6.f — the catalog cut decides every authored field's fate explicitly; cross-booked to AB-2 for the guide half.**
- **CNL-24 · composition/markup rows · MINOR (bundle).** D-15 (no declared 66ch measure — my read: no max-inline-size anywhere); D-14 (zero `<dl>/<ul>/<table>`; `.join(", ")` prose-flattening); kelvin's 1-cell `grid-cols-3` (:46 unconditional, 1-entry channel list — my reads); the graph that is not a graph (root restated 4-8×, no branch structure — the same data shape that guarantees CNL-6); the mb-2/space-y-4 rhythm split (`:92` vs the other four — my read) with the parent on the φ ladder; `rounded-card` dead ink at `:8` losing the cascade to the producer's `rounded-lg` — beside AboutPane's own comment documenting exactly that hazard (:31-41, my read); dead root classes + the parent's `p-0 m-0` override; the "Beginning of time" joke row (colorSpaceInfo.ts:245, read) in a factual column. **DISPOSITION: recomposition rows → NO-WAVE-OWNER (AB-17); rhythm rows → X-W6.g; the joke → X-W6.f content pass.**
- **CNL-25 · a11y/typography residue · MINOR (bundle).** Six decorative 24×24 `ArrowRight` icons without `aria-hidden` (default-attrs verified by reader-D; the in-repo idiom is ErrorBoundary.vue:23/:31 — my read); the faux-italic device, 10 sites, three semantics (R7 — provenance corrected); the channel name at `text-body` — a third role-matrix escape and the loudest text in the component (`:52-58`, my read); the forced-colors roster omission — the hovered node paints live color as data (the file's own C3 comment) yet appears in neither the U-F57 tier-1 `forced-color-adjust` roster nor as `[data-color-surface]`; under K-6's law this is NOT scope-shopping — it is the declared color-data roster missing a declared color-data surface (outcome needs-live, roster omission static). **DISPOSITION: aria/bidi rows → the boundary a11y bundle (with AB-36); type-role rows → X-W10.d; the roster row → X-W10.d with K-6 cited.**
- **CNL-26 · C-14 — `space=` deep-link is decorative · MINOR (sharpened).** My useColorUrl read adds the sharper guard: `applyUrlToModel` REJECTS a space-only URL outright (`if (!space || !color) return false`, :27) and the back/forward watch fires on `route.query.color` only — so `?space=xyz` alone is a no-op twice over, then overwritten by the model→URL sync. Consistent with AboutPane observed-anomaly 2 (the d3-jzazbz deep-link that didn't take; CC-081's serialization limb rides X-W9). **DISPOSITION: X-W5 (route/deep-link identity), cross-ref CC-081.**
- **CNL-27 · C-13 residue · INFO.** The fromEntries/entries round-trip with a latent positional join (`:217-230`/`:48`/`:57` — all 17 PICKER_CHANNELS rows currently collision-free, reader-L's check), the dead `:123` length guard, the static array class binding (`:127`), the R5 tooltip data-state residue, and the shipped-and-consumed `data-o18` hooks (legitimately counted, not defects). **DISPOSITION: rides whichever wave touches the file (X-W6.f edits these exact lines); recorded so nothing re-derives them.**

## Killed-claims register

Reader kills RATIFIED by this seat (K1–K12), then arbiter kills OF the readers (K13–K17):

- **K1 · D-6's shadcn/edict-4 limb + the §5 boundary row** — demo/ui/alert/index.ts is an 11-line barrel whose 9-line header documents its own conversion to a glass-ui re-export (B.W2/A.W7-N1); read whole by this seat. The component consumes the design system. The forwarding-DIRECTORY charge survives separately as CNL-15 (lattice, not edict 4). The 14px limb survives RE-ATTRIBUTED to the producer (CNL-19).
- **K2 · L-12 ("provably dead fallback, exactly two arms")** — killed per R2: the third arm (ink.ts:173) is live and is C-2's measured BLOCKER. The corpus's sharpest reader-vs-reader contradiction, settled against L-12 by the function's own text.
- **K3 · L-14 + the L header's "sole route `/#/about`"** — no `/about` exists (viewSchema/router reads; AB-10 concurrence); the catch-all served L's probes `/#/`; the component's pixels ARE in the matrix (both desktop panes render on `/#/`). Surviving kernel folded into CNL-5: no parameterised space/color sweep.
- **K4 · D-8's "tabIndex: -1 — explicitly removed"** — DOM default misread; no attribute exists (my template read). Defect intact as CNL-8.
- **K5 · D-11's mechanism limb ("the guard models the tier as a static flat tint")** — the live probe resolver exists and serves in-browser (useContrastSafeColor.ts:46-48/:148-221/:240-272, my reads); PRODUCER_TINTS is the jsdom fallback. The true mechanism is CNL-17's linear-L composite + neutral surrogate.
- **K6 · D-11's "5.625:1" certification target** — ink.ts:137 is additive: **5.75** (R1). The gap is larger than filed; a repair calibrated to 5.625 chases a number that exists nowhere in source.
- **K7 · L-2's component attribution ("the blank belongs to this seat")** — the ErrorBoundary catches descendant render throws and `return false`s (my read); the whole-app blank can only originate in App's own setup — which is exactly where CNL-3 found it. The kill did not merely remove a claim; it navigated to the real defect.
- **K8 · D §7 negative proof #2 ("the F-3 chain HOLDS — a pass by construction")** — a one-color exoneration generalised to the domain; the same error C's pass-1 made and formally retracted (challenge-C-implementation.md:585-596). Killed by C-2's scan + this seat's R2 mechanism.
- **K9 · D-17 as a motion defect** — central governance (two `!important` guards, colour legs deliberate); M-03/M-17/M-29 standard applied fresh — no M-xx quarantine row names this component, so no FALSE row is carried; cited by id per the quarantine law.
- **K10 · D-1's "12 documented spaces / the one state that works"** — the rgb base article is itself false (CNL-1); 11 of 18, and the "working" state publishes a wrong dossier for the space most users boot into.
- **K11 · C-5's cure 2 (`cssColorOpaqueFrame` … "provided app-wide")** — killed per R4; provided key is the uncoalesced `cssColorOpaque` (App.vue:271, my read); the cure would inject the 38-key bag into a presentational leaf.
- **K12 · D-4's cure limb ("swap to `text-heading` … one curve … crossover structurally impossible")** — `--type-heading` is fixed rem too (my scale.css read); the swap fixes THIS crossover by magnitude and leaves the mixed-regime scale intact. Producer seam; rides the BH relay (CNL-10).
- **K13 (arbiter) · reader-L's D-11 confirmation cell "= 5.625 (ink.ts:15,17,137)"** — citation-inheritance: the challenger's arithmetic repeated against the very lines that refute it (R1). The verdict class survives on independent grounds; the cell does not. Recorded as evidence about the fleet, per the ColorSpaceSelector DISSENT-1 precedent.
- **K14 (arbiter) · reader-L's "blanks the whole SPA" outcome limb on the two-click crash** — RESCOPED per R3: caught fallback over the pane grid with a re-throwing recovery loop, not a white screen. Severity unchanged (BLOCKER); symptom corrected so the repair and its gate assert the right thing.
- **K15 (arbiter) · reader-D's Fraunces provenance cell** — glass-ui's fonts.css carries no Fraunces and no italic face of anything (my census); the italic faces are the demo's self-hosted set (index.html:65/:73). Finding survives; attribution corrected (R7).
- **K16 (arbiter) · reader-D's C-2 mechanism cell ("the unmoved-test rejects an ink that would have passed")** — the null exits via `contrast_unreachable` before the identity test runs (R2); the float-ratio leg is the demotion target. C-2 unchanged.
- **K17 (arbiter) · four "missed by the whole corpus / no axis reports it" framings** — pre-banked registry identities (R6): guide-orphan-incl-ictcp/jzazbz (CSS-L-1 + AB-2), markdownModules partiality (AB-2), pane-level RTL (AB-36), the mount root (AB-10/rider A-1). True facts, false novelty; cross-booked, never re-booked.

## Adopted misses (beyond the three promoted to the roster)

The rows already woven into the roster: CNL-2/CNL-3 (the two crash BLOCKERs, reader-L), CNL-18 (white→HSL, reader-L), CNL-17's linear-L referent (reader-D), CNL-19's lede inversion (reader-D), CNL-22's false header comment (reader-L), CNL-23's dead `name` field + second partial table (reader-L), CNL-24's kelvin arity / rhythm split / rounded-card / dead root classes / joke row / graph-shape (reader-D), CNL-25's icon aria / faux italic / text-body channel name / forced-colors roster (reader-D), CNL-20's mount root (reader-D, cross-booked). This seat's own additions, small but earned: the 28-grey boot-trigger set (CNL-3), the onward-conversion bound separating the two crash classes (CNL-2/3), the `if (!space || !color)` guard sharpening (CNL-26), and the R1/R2 mechanism corrections.

## Superlatives (provenance preserved)

1. **The F-3 fill/ink split comment (ColorNutritionLabel.vue:190-208)** — names the prior defect with its measurement ("1.57:1 at the owner's own color"), splits the roles with stated reasons, implements two independent guards. The architecture is right; only the tail (`?? ""`) and the referent are not. Repairs preserve the split.
2. **`useContrastSafeColor.ts` is an unusually honest instrument** — probes the painted recipe through a 1×1 canvas with dual-ground draws, caches with the cost written out, and documents its own earned catches ("4.30:1 measured vs the 5.75 walked target"). It is why K5 was decidable in one read — and why CNL-17 could name the one place its own lesson wasn't applied.
3. **`safeAccentColor`'s dual-ratio certification (`Math.min(floatRatio, byteRatio)`)** — it will never certify an ink the painted pixels would fail, and its `contrast_unreachable` refusal is CORRECT behaviour. The library is more honest than its consumer; `?? ""` is what turns a refusal into a lie. (Reader-L; ratified by my operations.ts read.)
4. **`picker-color.ts:70`'s `satisfies Record<SpaceId, …>`** — the cure for the corpus's strongest BLOCKER, already shipped, ten lines from the table that lacks it. The cleanest possible statement of CNL-1. (Both readers; my read concurs.)
5. **The powerless-hue pattern is understood where it is applied** — three guarded sites with the comment "Powerless colors have no numeric hue; retain the last deliberate hue" (useColorParsing.ts:39-46, useColorPipeline.ts:88-95, the sliders' alpha arm) and the deliberate `stableHue` re-injection. CNL-2/CNL-3 are the two sites the same author forgot their own pattern — which is the argument that the convention must move into the types (X-W9), not the discipline.
6. **`demo/ui/alert/index.ts:1-9`** — a barrel that documents its own retirement, killing a false edict-4 charge in one read (K1). Provenance comments earning their keep exactly where the ColorSpaceSelector record said they do.
7. **`ErrorBoundary.vue`** — role/aria-live/tabindex/focus/recovery, every clause named, `return false` with the reason stated. It settled L-2's attribution (K7), rescoped a BLOCKER's blast radius (K14), and localised the true white-screen (CNL-3) — one 87-line file doing adjudication-grade work.
8. **The C axis's pass-2 self-correction (challenge-C-implementation.md:585-596)** — reopens its own pass-1 negative proof, states it was wrong, and extracts the law: "a null-returning guard is reachable until a SCAN says otherwise." The single reversal that produced the corpus's sharpest finding, and the exact discipline L-12 (K2) and D §7 (K8) lacked.

## UNPROVEN-NEEDS-LIVE residue (visual-audit / live-probe inputs)

1. **CNL-2/CNL-3 live confirmations** — the two-click fallback capture (assert `role="alert"` fallback text `Missing lch.h`), the `?color=black` white-screen, and the persistence loop (does the write-through seed re-crash after reload — source-traced only). Highest-value probes in this record.
2. **C-2's null-region census** (53/11,128 at L 0.47-0.54, C ≥ 0.35) and the live inline-style with no `color:` — arithmetic proven; region shape not re-scanned.
3. **C-5's render census** (160 self / 950 descendant updates per drag) and the microbench — mechanism confirmed (CNL-16); counts live-only.
4. **D-11's rendered pixels** (4.63/4.57 light, 6.15 dark) — light-arm arithmetic reproduced exactly by this seat from the reported RGBs; the dark chroma-collapse outputs (0.188 → 0.021) need the running guard; safari-desktop-dark/picker.png is in-repo and could settle it visually.
5. **D-2's forced-colors deltas** — WHCM engine required; the roster-omission limb is static and CONFIRMED (CNL-25); the tracked forced-colors capture is WebKit emulation and cannot settle it.
6. **D-5/D-20 geometry** (512px cap, 1047.9px in a 772px client, scrollH 8023) — unconditional grids static; measurements live-only; partially corroborated by the tracked light capture.
7. **D-6/CNL-19 surface deltas** (1.20/1.27/1.75:1) and the AlertDescription computed 14px — cascade resolution not performed here.
8. **C-7's touch latch** (tap lights 6 nodes, no dismissal) — consistent with source (no reset watcher); emulated-touch not statically decidable.
9. **D-4's rendered families** (does `.font-display` paint Fraunces at these five sites) — token sizes proven; paint-time family is a cascade question. Ride the X-W1 matrix with the parameterised space/color sweep (CNL-5).

## Closing verdict

**SOURCE-RED AFFIRMED AND WIDENED — the corpus was right about the catalog and the ink, wrong about its own blast radii, and blind to its two worst crashes.** The catalog lie stands, sharpened (the fallback article is itself false) and already owned at **X-W6.f** under its standing identity; the fail-open ink pair stands with its mechanism corrected and its cure split across **X-W9** (the missing contrast primitive) and the ownerless recomposition; and two crash BLOCKERs no axis filed — the two-click powerless-hue kill and the 28-grey boot white-screen, both measured against the shipped dist by this seat — land at **X-W9 + X-W1** as the sharpest possible argument that the library's `"none"` protocol must become unrepresentable-to-forget at the demo seam. Twelve reader kills ratified, five reader cells killed or corrected by this seat (one citation-inheritance event recorded for the fleet), four "novel" misses returned to their pre-banked registry identities, and the recomposition/MT-CSS-2/ink-referent families join rider A-1's phantom-owner list for the boundary. Dispositions here are ADJUDICATED, not VERIFIED; nothing in this record stamps a wave. The original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
