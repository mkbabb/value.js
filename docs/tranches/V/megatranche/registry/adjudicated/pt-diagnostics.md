claude-fable-5

# pt-diagnostics — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: apotheosis, parse-that module `diagnostics` · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-05 · **parse-that HEAD**: `ef10d5b` (main checkout, branch `master`, dirty only in `rust/`/`.cargo/`/`README.md`; `parse-that-css-totality-p2` never created — `ls -d` → `No such file or directory`, re-verified this session) · **value.js HEAD**: `0be528dc`, branch `tranche-u`.
**Corpus**: `audit/parse-that-modules/diagnostics/challenge-L-library.md` (Opus 5 [1m]; 24 defects · 5 BLOCKER · 6 superlatives · 3 self-kills · 1 in-place correction · 1 withdrawn superlative) + `challenge-C-consumption.md` (Opus 5 [1m]; 19 defects · 6 BLOCKER · 7 superlatives · 2 survived-falsifier NOT-defects · 1 addendum-withdrawn claim) + **two** L-18 hostile re-reads (Fable: `L-18-hostile-reread-diagnostics-library` and `-consumption`) — this module, uniquely in the pt-* family, drew a reader pair, so this seat's work included ruling five reader-vs-reader conflicts no prior apotheosis had to face.
**Method**: both challenge files read whole by this seat; every load-bearing verdict, every kill, every reader-missed finding, and every point where the two readers disagree re-derived against the live tree and the shipped dist with this seat's own instruments before adoption. The readers were then challenged back (incredulity seat): **one reader kill REVERSED with a live wrong-answer witness (L·B-3 → row 7)**, one reader-confirmed limb struck, one prescribed cure overturned, two severities re-rated for family consistency, one reader caught silently dropping a filed BLOCKER. Dispositions are **ADJUDICATED, not VERIFIED** — no bench run, `enableDiagnostics()`/`memoize()`/`resetPackrat()` never called (`diagnosticsEnabled` and `PACKRAT_ARMED` false in every process this seat opened; the P3-P5 witnesses exercise only the unarmed default path), parse-that read-only throughout; my only write is this file.
**This seat's own instruments (static reads + one throwaway unarmed node process in the prototypes workspace against published `@mkbabb/parse-that@1.0.0`)**: utils.ts whole (186); state.ts :25-139; parser.ts :34-80/:645-687; diagnostics.ts whole (14); debug.ts :55-125/:168-245; core.ts :1-12; index.ts :3-14; package.json :5-33; dist byte-reads: chunk :1-20/:50-82 (gate-free `collectDiagnostic`, `:53` the `errorOffset` fallback, `:77-82` alias/rebind), 0 imports / 125 lines / 3,516 B, `packrat-entry-CS1td-8B.js:881-883` + PACKRAT_ARMED :678/:682/:714/:722, `core.js:1` → packrat chunk → `:1` six diagnostics symbols, `diagnostics.js`/`.cjs` twin bindings, `utils.js:1` `import { s, b }` from the diagnostics chunk, `diagnostics.d.ts` 2 lines no ParserState; censuses: `.furthest =` → utils:32/:132 + parser:65 only; `.expected =` → utils:33/:44/:133 + parser:62 only; `collectDiagnostic` callers → parser.ts:666 sole; `mergeErrorState(` → **22** sites, **0** in packrat.ts; tier symbols over `test/` + `scripts/` → **rc=1 zero hits** (enable/disable scaffolding only, debug.test.ts:10-11/:27/:31 + reentrancy.test.ts:10-11/:14-15); formatters over `dist/*.js dist/*.cjs` → zero; `many()` progress guard + `any()` first-success arms read in source; value.js side: `src/parsing` absent, deps = glass-ui+keyframes.js, lockfile `parse-that` 0, `src/css/types.ts:10-27` read (ParseIssue 8-code union; success arm `diagnostics: readonly []`), idiom/example.test.ts:249-269 + example.ts:242-246, README:188-240, W1.md:460-500, W2.md:235-296, W3.md unit/gate map, parser-band.md:108-120. **Live witnesses (nothing armed)**: **P1** flag-probe via published exports → `false`, journal 0; **P2** `"\nx".lastIndexOf("\n",-1)` → 0 and `getLineAndColumn(0)` on `"\nabc"` → `{line:2,column:-1}`; **P3** nested-recover pop → journal **1** where `parser.ts:673` promises 0, survivor `furthestOffset:1` = the record recover pushed and promised to remove; **P4** failed-sync recover: baseline `furthest` 2 → post-recover **0**; **P5** two unarmed recovered parses → journal **2**.

## Scoreboard

**35-row roster: 25 CONFIRMED (from the two challenges, identities merged, severities adjudicated) + 10 MISSED-ADOPTED (the readers', all re-verified by this seat, one narrowed) · 12 KILLS sustained + 1 reader-kill REVERSED · 13 superlatives banked · 6 BLOCKERS SURVIVE.**

The six blockers compose into one sentence: **every value export is a latch operation on process-global state (O-8 total, 6/6); the labels the band's #1 debt needs exist only jointly with an unconditional `console.error`; the buffer sold as opt-in fills on the unarmed default path, is vacuous from its own subpath, and its rollback pops the wrong record under the exact two-level recovery composition a CSS grammar is** — witnessed live by this seat (P3/P4/P5). Calibration: C·D-0 (row 1) keeps every consequence prospective — parse-that has zero shipped consumers at value.js HEAD, contradicting O-15's readoption line on the record. The C-challenge's **RETIRE-in-whole** verdict is ADJUDICATED AND HELD: the merge rule, the per-parse model, and the layering port as semantics; not one of the six functions ports as mechanism.

## Adjudicated defect roster

Anchors are parse-that HEAD `ef10d5b`; re-resolve before executing any cure (D-19 lesson). Routing law: **W1** = X·P harness/corpus/gates · **W2** = X·P dual-target algebra (O-8/EQ-1..6/R-LAWs are its construction laws) · **W3** = X·P totality-and-recovery — unit **.b** owns the recovery lowering, the labels gate (`test/css-recovery/labels.test.ts`) and the `--assert-no-console` walk, **.c** the boundary/latch cures · **W4** = X·P PLAW-BIND/release packaging · **X·P REFINEMENT FOLD** = the finding sharpens a spec before its wave opens · **NO-WAVE-OWNER** = real defect, no X·P owner — recorded binding on any future parse-that source wave so it inherits rather than rediscovers.

| # | id | sev | claim (adjudicated form) | proof (this seat's anchors) | disposition |
|---|---|---|---|---|---|
| 1 | C·D-0 | CALIBRATION (filed BLOCKER-process; held as the frame) | The brief's consumption premise is false at HEAD: `value.js/src/parsing/` does not exist (successor `src/css/`, 7 files) and `@mkbabb/parse-that` is not a value.js dependency — deps = glass-ui+keyframes.js, lockfile 0 hits, `node_modules/@mkbabb/` = 3 dirs. Sole consume edge = the private prototypes workspace pinning `1.0.0`; `/diagnostics` imported **once**, for one symbol (`parsethat-surface-gaps.mjs:13`). 0 of the frozen 52 `/css` exports touch this module. **Contradicts O-15's "value.js readopted parse-that as published" — readoption has not landed in the manifest at `tranche-u` HEAD.** | All re-verified this seat: deps dict, `ls src/parsing`, lockfile grep, css.ts count. Both readers confirmed; both noted their own briefs inherited the false premise — as does this seat's ("value.js src/parsing (the consumer edges)"), corrected here on the record. | Calibration row, no owner — every severity below reads "blocking at adoption," never "breaking today." |
| 2 | L·B-1 / C·B-4 / C·D-7 | **BLOCKER** | `collectedDiagnostics` (utils.ts:95) is an unbounded, flag-ungated, source-unattributed, never-scoped module global. `collectDiagnostic` (:102-128) alone among its siblings carries no `diagnosticsEnabled` guard (contrast :52/:58/:71); sole caller is public `recover` (parser.ts:666); no reset on any parse boundary (parser.ts:34-49 snapshots packrat globals, never this buffer); `Diagnostic` (:84-93, 8 fields) carries no src/parse handle while the only renderer takes `src` separately (debug.ts:200) — interleaved parses braid irrecoverably. | **P5 live**: two unarmed recovered parses → journal 2; dist chunk :50-70 gate-free byte-for-byte. C·B-4's 160.8 MiB / 800,004 figure NOT re-run (MEASURE-AT-OPEN row 1); the structure needs no measurement. K-6 shape: parse #100,001 distinguishable by resident memory, feature nominally off. | **W2** — O-8/R-LAW-3: the journal is a **value of the parse** (`D` in `(V,C,P,D)`), never a side channel; **W3.b** returns it with the result. In-library cure (gate + cap + parse-boundary scope + src handle) = NO-WAVE-OWNER, binding on any future parse-that wave. |
| 3 | L·B-2 | **BLOCKER** | `getCollectedDiagnostics()` is provably always `[]` for a `/diagnostics`-only consumer: the buffer's one writer-caller is `Parser.prototype.recover`, and `Parser` is not on the subpath; the manual escape (`collectDiagnostic`) needs a `ParserState` the subpath exports neither as value nor type (row 24). The barrel's headline feature is unreachable from the barrel. | Census this seat's own: `collectDiagnostic` = definition + parser.ts:4 import + **:666 sole call** + 2 re-exports. dist/diagnostics.js imports six aliases from a chunk with zero imports — no Parser in the graph. **Seat correction 2: reader 1 dropped this filed BLOCKER from its return entirely; reader 2 caught it.** | **W4** — subpath surface truth at the next cut (export a producer path or de-publish the reader); dissolves under RETIRE. |
| 4 | L·B-4 / C·B-2 | **BLOCKER** | Labels and an unconditional `console.error` are reachable **only together**: seeding gated at utils.ts:33/:38 on the module global; `parser.ts:67-69` fires on every failed parse once armed, `this.state.toString()` evaluated eagerly (full ANSI render built regardless of stderr); `enableDiagnostics()` arity-0, no scope, no logger — while `parser.ts:693` and `debug.ts:355` both take injectable loggers, proving the seam was understood and not applied. **Struck limb (seat, after reader 2's kill): "save/restore is impossible" — the flag IS purely observable through published exports (P1); the reachability defect (no `isDiagnosticsEnabled` on any entry) survives.** | Dist byte-exact: chunk :14/:18; packrat-entry :881-883. **P1 live**: fresh state + `mergeErrorState(s,"probe")` → `s.expected !== undefined` ⟺ armed; returned `false`, journal untouched. Identity: **O-15 PT-01 = pt-core row 9** — cross-booked, primary HERE (the gate lives in this module's mergeErrorState). | **W2** — R-LAW-3 (diagnostics are values, never effects; the console-patch probe made unpassable); **W3.b**'s `--assert-no-console` gate is the named discharge. |
| 5 | C·B-1 (+ C·D-16 folded) | **BLOCKER** | The subpath cannot supply an EQ-4-comparable diagnostic in the shipping default: `mergeErrorState` is the only published writer of `state.expected`, both writes flag-gated; no `.label`/`.expected` combinator exists; default off (utils.ts:6). The band's #1 binding debt (parser-band.md:116, verified verbatim) is un-dischargeable through this module as shipped — cand-F's own doc concedes it (`color.ts:592`). D-16 folded: the function fuses watermark plumbing (always-on, load-bearing) with label recording (armed-only) — the fusion is why `.label()` never existed. | DEBT-1 trace re-verified by hand against leaf.ts arms; W1.md:470-472 RED rows verbatim; EQ-4's ruling (W2.md:266) verbatim: armed-only labels read as diagnostics-ABSENT. | **W2** — the algebra's *labelled zero-width failure with named expectations* is a required capability family; debt 1 is discharged in the wave's own value-level `D` journal, **never** via `mergeErrorState` labels. **W3.b**'s labels gate asserts every corpus rejection's expected-set unarmed. |
| 6 | C·B-3 | **BLOCKER** | O-8 violation is total: **6 of 6** value exports read or write process-global mutable state (reads :33/:38/:139; writes :9/:13/:115; rebind :143). Not a module containing a latch — a module that is nothing but latch operations. No wrapper can launder any (O-8 forbids the operator, not the spelling). | Inventory re-verified line-by-line against the two globals (utils.ts:6, :95). | **W2** — O-8 is the anchor of the RETIRE-in-whole verdict; arming/diagnostics become parameters of a parse. The one narrowing: see killed-claims K11 (single-parse scoping IS expressible; reentrant-safe scoping is not — still fatal). |
| 7 | L·B-3 / C·D-18 (merged) | **BLOCKER** — seat ruling; reader 1's severity kill **REVERSED** | `recover`'s rollback is an unqualified LIFO `pop()` (utils.ts:147), not a mark/rewind: under the canonical two-level recovery composition (outer recover whose `sync` itself recovers — exactly a resilient CSS stylesheet's block/declaration shape, and the README's own rustc/GCC framing), a failed outer sync destroys the inner's legitimate record and retains the outer record it explicitly promised to remove — silently, length restored. C·D-18's variant (consumer `sync` calling the published `collectDiagnostic`) reaches the same corruption; **one row, the stronger reachability** (no consumer cooperation beyond composing the public combinator twice). | **P3 live, this seat's own** (neither reader ran it): `p.recover(q.recover(r,t).then(z), s)` on `"abc"` → journal **1** where parser.ts:673 promises 0; survivor `furthestOffset:1`/`found:"bc"` = **D_outer**, the record recover pushed; D_inner destroyed. Reader 1's reachability objection (today's consumers don't nest) is answered by row 1's calibration: severity measures the adoption path, and the adoption grammar is the nested shape. Reader 1's merge-the-rows point ADOPTED. | **W3.b** — the recovery lowering implements R-LAW-1's mark/rewind (record journal length before `sync`, truncate on failure), per W2.md:275-278. In-library cure NO-WAVE-OWNER. |
| 8 | L·M-5 | **MAJOR** | `collectDiagnostic` ends in `resetErrorState` (utils.ts:127 → :132 `furthest = -1`) on the **live** parse state; neither recover exit path restores it (parser.ts:668-681 touch isError/offset/value only); parser.ts:60 then renders the final error from exactly that field. One recovery anywhere degrades the enclosing parse's deepest-failure position. Honest bound (neither challenge states it): observable only when the enclosing parse ultimately fails with its deepest probe before the recovery point. | **P4 live**: baseline `furthest` 2 (no recover) → **0** post-failed-sync-recover, unarmed. Falsifier grep exact: `.furthest =` at utils:32/:132/parser:65 only. R-LAW-1 violated on the state. | **NO-WAVE-OWNER** — cure with row 9's snapshot; **REFINEMENT FOLD** rider: W3.b's rollback-exactness (EQ-5) restores the full diagnostic substate, informed by this witness. |
| 9 | R1-M1 (MISSED-ADOPTED) | **MAJOR** | The failed-sync path — whose whole purpose is to let the error propagate untouched — propagates a state whose `furthest`/`expected`/`suggestions`/`secondarySpans` were already destroyed: `popLastDiagnostic()` restores the buffer, **nothing restores the state**; the parser.ts:673 comment is true of the journal and false of the state. Consequence: arming diagnostics and using `recover()` are mutually destructive — the tier's two headline features cancel on the default path; the armed render prints a caret at the recovery checkpoint with an emptied expected-set. | P4 is this row's witness on the unarmed limb (furthest 2→0). Exhaustive greps confirm no restore path. Neither challenge doc records the state limb of the failed-sync path. | **NO-WAVE-OWNER** — the mark/rewind cure must snapshot `{furthest, expected, suggestions, secondarySpans}` around `sync`, not just journal length; W3.b's EQ-5 lowering inherits the requirement via row 8's fold. |
| 10 | L·M-4 / C·D-8 | **MAJOR** | `getCollectedDiagnostics()` returns the live binding (utils.ts:139); `clearCollectedDiagnostics()` **rebinds** rather than truncates (:143) — one signature, two aliasing semantics: a live self-updating view before clear, a permanently detached snapshot after; `readonly` compile-time-only, elements mutable (`Diagnostic`'s three arrays, :89-91), vs value.js's deep-Readonly `ParseIssue`. The discipline exists at the wrong boundary: :120-122 spreads defensively where :139 hands off by reference. | Source + dist :77-82 byte-exact. GROUND-B survives by luck of style (clear-then-fresh-call, example.test.ts:249/:261/:269). | **NO-WAVE-OWNER** — one-token cures (`[...]` at :139, `length = 0` at :143) shared with row 11; dissolves under RETIRE. |
| 11 | R1-M2 (MISSED-ADOPTED) | **MAJOR** | `clearCollectedDiagnostics()` — the call README:229 makes mandatory — is itself a `recover()` corruptor mid-parse: clear REBINDS (:143), `pop()` reads the **current** binding (:147), so an enclosing recover's push lands in array A, a mid-parse clear rebinds to B, and the failure-path pop removes an unrelated record from B (or `undefined`) while the target strands in A. User code genuinely runs mid-parse (`.map`/`.chain` callbacks; parser.ts:35-42 documents nested `.parse()` as supported). Extends row 7's corruption to a deliberately published export. | Mechanism certain from the rebind/pop pair; call-path reads verified. The library cannot state a correct usage for `clear` under its own supported nesting pattern. | **NO-WAVE-OWNER** — same one-token cure (`length = 0`); recorded so the future wave inherits the pair as one identity. |
| 12 | L·B-5 / C·D-10 | **MAJOR** (re-rated from both readers' BLOCKER — pt-core row 20 precedent, seat correction 8) | The entire collection tier is untested and no gate asserts any named `/diagnostics` export: `export {}` keeps `proof:subpath` GREEN (subpath-gate stats file existence :25-36, imports only `/core`+`/packrat` :39-43; dist-surface covers the root barrel only :14-15; manifest-gate is shape-only). The only diagnostics symbols any test names are the enable/disable scaffolding. Four live defects (rows 2/7/8/16) stand on this untested path, at 1.0.0. | Seat grep, widened past the L-doc's `test/`-only scope to all `scripts/proof-*.mjs`: **rc=1, zero output** across ten symbols including `.recover(`. | **W4** — RC-P gate surface: golden export list + a subpath-gate that imports and asserts `/diagnostics` and `/utils` (the L-doc's six-line cure). **W1 rider**: the lane's harness asserts the diagnostics posture directly (row 33). |
| 13 | L·M-6 | **MAJOR** | The barrel's own doc comment is false about the module it publishes: `diagnostics.ts:3-5` binds the buffer to the toggles ("the **opt-in** collected-diagnostics buffer and its enable/disable toggles") while :102-128 is gate-free — the toggles govern record *population*, never *collection*. Three of fourteen lines assert a gating relationship that does not exist. Attribution narrowing held (K6): README:191-193's "Zero overhead when off" scopes to the flag and README:221-226 documents the collection as unconditional — the doc-falsity rows are this comment and README:235-237 (row 17), not README:193. | Verbatim re-read both files this session. Default-posture records verified hollow-but-real (P5 rows carry offset/line/column/found + three empty arrays). | **W4** — doc truth at the next cut; any wave touching the file corrects the comment to shipped truth. |
| 14 | L·M-7 / C·D-9 | **MAJOR** | The surface is not closed under its own semantics: publishes the mutator (`collectDiagnostic`) without its inverse (`popLastDiagnostic`), the setter pair without the getter (`isDiagnosticsEnabled`), read types without producers (`addSuggestion`/`addSecondarySpan`/`reportUnclosedDelimiter` on no entry; `Suggestion["kind"]` a closed 2-member union), and the data without its renderer (row 17). For a wave whose reason to touch the module is emitting better rejections, the emit half is the missing half. | All five entry files read whole this seat; chunk aliases f/i/p/r verified; internal writers parser.ts:419/:469 as filed. | **W2** — the emission algebra is authored in the fresh root (capability families); **W4** rider: golden-surface closure check with row 12. |
| 15 | L·M-8 / C·D-17 (+ R2-M3 folded) | **MAJOR** | `core.ts:3-5`'s isolation guarantee is false at the shipped bytes — `/core` loads the entire diagnostics module in two hops (`core.js:1` → packrat chunk `:1` → six diagnostics symbols), unshakeable (parser.ts:4 hard-imports; 22 mergeErrorState sites). R2-M3's sharpening folded: the **packrat limb is equally false** (the chunk `/core` imports IS the packrat entry, latch included); only json/csv holds — the subpath split is cosmetic on two of three axes. Direction discipline held: the chunk itself imports nothing (S1), so L·S-1/C·S-6 stand while this row stands. | Both hops byte-read this seat; identity: **pt-core row 3 PRIMARY** ("false on two of three clauses", BLOCKER there — the 26-line barrel exists to make the claim; MAJOR here as the inbound-edge record). | **W4** cross-book — chunk topology + comment correction ride pt-core row 3's disposition; no re-book. |
| 16 | C·D-6 / L·m-6 | **MAJOR** (C's severity held over L's MINOR) | Dual-package hazard on a module whose state is 100% global: `./diagnostics` ships both conditions (package.json:18-22), ESM binds `diagnostics-DDazRHgl.js` while CJS binds `DpUY87_d.cjs` — two flags, two buffers; `enableDiagnostics()` arms half the fleet, `clear` drains half the leak. ESM-internal unity is real and separately banked (S7). | Twin bindings byte-read; C's measured one-process non-identity NOT re-run (MEASURE-AT-OPEN row 3). Identity: **pt-core row 5 PRIMARY** (the package-wide dual-format hazard, MAJOR, W4/RC-P); this module is where it bites hardest. | **W4** cross-book — single-format ship or cross-format identity/latch gate, pt-core row 5's unit; the 100%-global aggravation recorded here. |
| 17 | L·M-10 / C·B-5 | **MAJOR** (reader 2's BLOCKER re-rated; reader 1's phrasing kill K9 held) | `formatDiagnostic`/`formatAllDiagnostics` — the only interpreters of the published shape, and the ones README:229-232's headline example calls — ship as `.d.ts` declarations with no runtime in any bundle; README:235-237's "TypeScript … exposes `formatDiagnostic()`" is false. Narrowed per K9: the debug runtime largely DOES ship (statePrint/addCursor/formatExpected in the chunk — `toString()` needs them); exactly the two formatters were tree-shaken. Failure is loud (unresolvable import), hence MAJOR not BLOCKER. | Per-symbol grep over `dist/*.js dist/*.cjs` → zero, this seat; `dist/debug.d.ts:12/:17` declared inside `files:["./dist"]`. | **W4** — RC-P declaration-surface check; same dts-ghost family as pt-core rows 38/47 (SF-2's acceptance list) — add, don't re-book. |
| 18 | R2-M1 (MISSED-ADOPTED) | **MAJOR** | value.js's frozen `ParseResult<T>` forbids diagnostics on SUCCESS — the success arm is `{ok: true; value; diagnostics: readonly []}` (types.ts:25-27, an **empty tuple**) — so `recover()`'s entire product (partial success carrying N diagnostics, which GROUND-B asserts GREEN: `isError false` + `diagnostics.length 1` on `"(1px, ?, 3)"`) is **unrepresentable** in the `/css` contract. Both challenges analyze only the failure arm. At adoption the lowering must discard-on-success, flip ok:false on a success, or reopen the frozen contract — a decision that must be RULED before W3.b opens. | types.ts:25-27 + example.test.ts:251-264 verified verbatim this seat; neither challenge contains "readonly []"/"success arm" (reader's grep, spot-held). | **X·P REFINEMENT FOLD, binding W3.b** — recovered-success diagnostics ride the value-level `D` journal beside the result or the contract reopens; the choice is recorded in the wave spec, not improvised mid-wave. |
| 19 | R2-M2 (MISSED-ADOPTED) | **MAJOR** | **R-LAW-5 — the one recovery law that names `recover` — appears zero times in either challenge doc**, and live parse-that violates its declaration requirement: a recovered branch returns `state.ok(sentinel)` (parser.ts:681), `any()` returns on the first non-error arm (leaf.ts, both the unrolled and loop arms re-read this seat), so a recovering non-final alternative silently starves every later arm; nothing in README §Error Recovery declares it and zero tests pin it (`.recover(` uncovered, row 12). Lands on the band: both candidates are any/dispatch-shaped. | W2.md:287-290 verbatim; mechanism re-derived from the live `any()` arms + `ok()`. | **W3.b** — the lowering declares the alt/recover rule (forbid non-final-arm recovery or declare the alternative); **W1 rider** — the malformed corpus gains a non-final-arm recovery construction so the G-4 probe can bite. |
| 20 | L·M-3 / C·D-14 | **MAJOR at identity** (pt-core row 11 PRIMARY) | Four line/column implementations, **two** conventions (K7's narrowing held), and `getLineAndColumn` returns a wrong answer at offset 0 on a leading-newline source — `lastIndexOf("\n", -1)` clamps to 0, matches index 0 → `{line:2, column:-1}` where `{1,0}` is correct; the broken method is public on root+`/core` and sits on the armed console.error path (`debug.ts:59` uncompensated). The killed newline sub-clause (K2) replaced by the true shape: `getLineNumber` is 0-based EXCEPT at newline offsets, where the exclusive-slice/inclusive-fromIndex shifts cancel — a mixed-basis method. | **P2 live**: clamp → 0; `{line:2,column:-1}` reproduced on `"\nabc"`. Identity: pt-core row 11 already holds the trio + the collectDiagnostic fork at MAJOR/NO-WAVE-OWNER; this corpus adds the clamp wrong-answer witness and the C·S-5 promotion (S9). | **NO-WAVE-OWNER** cross-book — one convention, one implementation; the cure DIRECTION is ruled in K1: install the slice-first algorithm INTO state.ts, never the reverse. |
| 21 | L·M-9 | Identity **MAJOR** at pt-core row 14; diagnostics-side residue **MINOR** (reader 1's bound held) | `clone()` passes five constructor args, dropping expected/suggestions/secondarySpans while inheriting `furthest` — breaking state.ts:38-41's own invariant. Sole caller packrat recall's scratch (packrat.ts:351); snapshot/applyAnswer carry three fields; no mergeErrorState in packrat.ts (grep 0, this seat). Reader conflict ruled: the clone's invariant break is destroyed before it can be read (snapshot discards it), so the observable residue — the discarded deeper-failure watermark — is gated on memoize+left-recursion+eval-set: MINOR here; the design-model contradiction stays MAJOR at its pt-core home. | state.ts:101-109 re-read; packrat cites at reader-corrected lines (:351/:356/:252-260). | **NO-WAVE-OWNER** cross-book to pt-core row 14 (copy all 8 or rename), binding on any packrat-touching wave. |
| 22 | L·M-1 | **MINOR** (re-rated per reader 2's kill K4; **cure ruled to reader 1**) | Two dead-store array allocations per new-furthest event with diagnostics off (utils.ts:34-35 ungated beside the gated :33; only writers flag-guarded; arrays escape into the long-lived state, so escape analysis inapplicable). Magnitude unproven either way — see K3 (INFO-A's refutation is methodologically unsound) and MEASURE-AT-OPEN row 2. **Cure ruling (seat, resolving the reader conflict): the length-guard (`if (state.suggestions.length) …`), NOT the gated hoist — the hoist strands stale non-empty arrays across an arm→disarm transition mid-state-lifetime (disable is callable from `.map` callbacks), changing observable behavior; the length-guard is exact and allocation-free unarmed.** | Source + dist :15-16; 22-site census (the L-doc's 20 corrected). | **NO-WAVE-OWNER** + MEASURE-AT-OPEN row 2. |
| 23 | L·M-2 (surviving limb) | **MINOR** | The split-to-count antipattern (`.split("\n").length` materializing one string per prefix line) at utils.ts:109, duplicated at state.ts:122 and :131 — the fork propagates it rather than isolating it; the allocation-free `fromIndex` idiom sits three times adjacent. The equivalence claim and prescribed cure are DEAD (K1); the F2 SlicedString self-correction is banked (S13). | Token-identical column expressions verified; three sites confirmed. | **NO-WAVE-OWNER** — fix the count once in state.ts after row 20's algorithm installation. |
| 24 | L·M-11 | **MINOR** (both readers' kills K10 held) | `dist/diagnostics.d.ts` omits `ParserState`, so two of six functions take (one returns) a type the subpath won't let you name — an ergonomics wart, not a blocked path: TS infers it at every realistic call site, the strict/NodeNext probe compiles exit 0, and a one-line type import from `.`/`./core` erases at build. | d.ts re-read (2 lines); C·S-7/INFO-B probes held. | **W4** — golden declaration surface with row 12. |
| 25 | C·D-12 (+ R1-M5 rider, row 26) | **MINOR** | `Diagnostic` is structurally non-conformant with frozen `ParseIssue` on all five EQ-4 fields: no `code`, two positions with no invariant, no `end` (unrecoverable — `found` clamps at 20), armed-only `expected`, `found` where `actual: string\|null` is wanted. No total information-preserving map exists. | Both shapes re-read verbatim this seat. | **X·P REFINEMENT FOLD** — W3.b's codes/diagnostics unit maps `furthestOffset → start` and derives `end` from its own span discipline; nothing maps from `Diagnostic`. |
| 26 | R1-M5 (MISSED-ADOPTED) | **MINOR** | `Diagnostic.offset` is write-only: written once (utils.ts:116, the recover checkpoint), read by nothing in src/, any bundle, or any consumer (formatDiagnostic uses `furthestOffset` for both display and caret, debug.ts:202/:207; the one live consumer reads `.found`). A wave mapping `offset → start` would invent semantics, not port them. | debug.ts :200-229 re-read — no `d.offset` read; grep confirms. | Rider on row 25's fold: map `furthestOffset`; record `offset` as un-interpreted. |
| 27 | C·D-15 / L·m-1 / L·m-2 / L·m-7 | **MINOR** | `found` (utils.ts:113) is lossy four ways on one line: fixed UTF-16 cut (lone-surrogate hazard), single-character escape list (`\r`/`\t`/`\x1b` pass into ANSI context, debug.ts:224-226), a fresh `RegExp` per record, and no extent field anywhere — the shape's only byte-level record cannot carry COMP-1/R-LAW-2 (skipped bytes recorded only as a clamped escaped 20-char prefix; the complement cannot be reconstructed). | Line re-read source+dist. | **X·P REFINEMENT FOLD** — W3.b: recovery-skipped bytes enter `C` as `(offset,length,kind)` spans; `found` is not a carrier and nothing ports it. |
| 28 | C·D-13 | **MINOR** | `mergeErrorState` (and all six) published from two homes with no canonical one; `/diagnostics` has zero exclusive exports; the barrel has already been narrowed once (index.ts:10-12's excision note) so the forward hazard is real. Consumers split across homes (cand-F root; probe subpath; idiom suite root — the reader's third edge held, making `/diagnostics` a one-importer subpath). | index.ts:5 + diagnostics.ts:7 re-read. | **W4** — canonicality/deprecation note at the next cut; golden list binds both. |
| 29 | R1-M3 (MISSED-ADOPTED) | **MINOR** | `utils.ts` is mis-factored at the file level — flag tier (:5-18) + error/journal tier (:20-148) + byte-scanners (:150-186) in one file — so the chunk named "diagnostics" is really "utils.ts": `/utils` evaluates `diagnosticsEnabled` and `collectedDiagnostics` to obtain two allocation-free scanners (`dist/utils.js:1` imports `{s,b}` from the diagnostics chunk — witnessed), and `/diagnostics` ships ~25 scanner lines as dead weight. The file-level root cause of row 15: the tier boundary was drawn in the barrels (cosmetic), never in utils.ts (real). | dist/utils.js:1 byte-read this seat. | **W4** — split `utils.ts` (diagnostics impl / `scan.ts`) with row 15's topology work. |
| 30 | R1-M4 (MISSED-ADOPTED) | **MINOR** | `formatDiagnostic` renders ONE record with TWO conventions: header from the record's own correct `d.line`/`d.column` (:203, the slice-first fields), caret from `getLineAndColumn` via `new ParserState(src, undefined, d.furthestOffset, true)` + addCursor (:207-208 → debug.ts:59) — the clamp-broken path. Leading-newline + `furthestOffset 0` → header `1:0`, body highlights line 2 with the caret pad computed from column −1 (silently misplaced, no throw). The tier's only renderer disagrees with the tier's own shape. | debug.ts :200-229 + :55-94 re-read this seat; pad arithmetic verified (`" ".repeat(lnWidth+4-1)`). | **NO-WAVE-OWNER** — binding on any wave porting the renderer: one convention before port. |
| 31 | R1-M6 (MISSED-ADOPTED, **seat-narrowed**) | **MINOR** | The renderers are quadratic in source size: `formatAllDiagnostics` performs **≥N** full-source `split("\n")` for N diagnostics (one per record at debug.ts:58; **2N only when secondarySpans present** — the :100 split is gated at :216, correcting the reader's "at least 2N"), and `formatSecondarySpans` rescans lines from index 0 per span (:106-121). Scoped MINOR because the formatters ship no runtime (row 17) — binding on the porting wave, not on any consumer today. | Gate at :216 verified — the seat's precision correction against reader 1. | **NO-WAVE-OWNER** — a cached line index per src retires all three sites at port time. |
| 32 | R2-M4 (MISSED-ADOPTED) | **MINOR** | W1's G-5 quarantine can be asserted DIRECTLY from outside the package — the constructive half of K5: the P1 predicate (`fresh state + mergeErrorState(s, "probe"); s.expected !== undefined`) is a pure, side-effect-free flag reader through published exports, upgrading G-5 from a byte-empty-stderr proxy to a positive entry/exit assertion (the G-4 shape), closing the transitive-dependency-arms hole C·D-11 correctly named. | **P1 re-run by this seat**: `false`, journal 0. | **X·P REFINEMENT FOLD** — bank the predicate into W1's G-5 at open, asserted `=== false` at bench-cell entry AND exit. |
| 33 | L·i-1 / O-15 PT-03 | INFO | The packrat latch is a one-way latch, exact at the cited bytes: :678 init, :682/:714 reads, :722 the only write; `resetPackrat` never disarms; `packrat.ts:289` says so outright. Not charged here; recorded because W2.md:254 generalizes it into the rule `diagnosticsEnabled` also breaks — while S6 records the flag is NOT itself the latch class. | Grep re-run this seat, exactly four occurrences. Identity: pt-core row 10. | Cross-book — no re-book. |
| 34 | L·m-5 / F1 / C·INFO-B | INFO | `expected?: string[]` is a redundant tri-state but NOT a hidden-class hazard (ES2022 `useDefineForClassFields` installs the slot at construction — chunk field declaration verified by the readers, held) and the signature forces no consumer casts (tsc exit-0 probe). Two challenge self-kills, both correct — the corpus at its best. | Spot-held; both passes' falsifications concur. | Roster hygiene — the branches die with the algebra split (row 5's fold). |
| 35 | L·i-2 | INFO | `sideEffects: false` vs `_initWhitespace()` — not on this module's graph (the chunk is effect-free beyond two `let` initializers); honest for `/diagnostics`, charged to the core/packaging roster. | package.json:6 re-read. | Cross-book: pt-core row 23 owns it. |

## Seat rulings & corrections (the incredulity seat's own findings — against the READERS)

1. **L·B-3's kill REVERSED — the roster's one verdict flip, with a live witness (P3).** Reader 1 killed the BLOCKER to MAJOR on reachability ("a three-way conjunction"; `.recover(` nowhere nested in any tree). Reader 2 confirmed BLOCKER ("strictly stronger than C·D-18"). This seat built the composition neither ran: journal length **1** where the engine's own comment promises **0**, the survivor being the exact record `recover` pushed — and ruled BLOCKER, because row 1's calibration makes every severity an adoption-path severity, and the adoption grammar (resilient CSS: block-level recover containing declaration-level recover) IS the nested shape; the README's own compiler framing invites it. Reader 1's merge-into-one-row point is adopted; its reachability facts stand as the *today* bound.
2. **Reader 1 silently dropped L·B-2** — a filed BLOCKER absent from its return entirely, no kill, no mention. Reader 2 covered it; this seat re-verified the one-writer census. A hostile re-read that drops a filed BLOCKER without ruling on it is a roster-completeness failure of exactly the kind L-18 exists to catch; recorded as fleet evidence.
3. **Reader 1's confirmed L·B-4 carries a killed limb.** Its row text asserts "the barrel withholds the getter that would make save/restore possible" — the impossibility framing reader 2 killed and this seat's P1 re-confirmed dead (the flag is purely observable through two published symbols). The reachability defect survives; the word "possible" does not. Struck in row 4.
4. **The L·M-1 cure conflict is ruled to reader 1.** Reader 2's kill correctly re-rated the severity but endorsed the L-doc §8.4 cure ("hoist :34-35 inside the existing flag test") — the cure reader 1 proved non-behavior-preserving: across an arm→disarm transition mid-state-lifetime, the gated hoist strands stale non-empty `suggestions`/`secondarySpans` on the state (their only other clear is `resetErrorState`, which the propagating path may never reach), attributing an old watermark's extras to a new one. The length-guard is exact. Row 22 carries the ruling.
5. **Reader 1's R1-M6 narrowed**: "at least 2N full-source splits" overstates — the `formatSecondarySpans` split (debug.ts:100) is gated on spans non-empty (:216), so the default-posture bound is N, 2N with spans. The finding survives at the accurate constant (row 31).
6. **Reader 2's MISSED-3 is not novel at the roster level** — pt-core row 3 already records core.ts:3-5 "false on two of three clauses." Confirmed and folded into row 15 as the packrat-limb sharpening; identity guard, no re-book.
7. **Severity re-rates for pt-* family consistency** (both readers held the filed BLOCKERs): L·B-5/C·D-10 → MAJOR (pt-core row 20 = the same gate-absence class at MAJOR); L·M-10/C·B-5 → MAJOR (loud failure; dts-ghost family = pt-core rows 38/47); C·D-6 ruled MAJOR (C over L's MINOR), cross-booked to pt-core row 5. Recorded as re-rates, not kills.
8. **Seat self-kill (L-18 both ways).** This seat hypothesized a negative-index hazard in `collectDiagnostic` when `furthest === -1` (an EOF-silent leaf failing under recover would slice `src.slice(-1, 19)`). Dead at the bytes: utils.ts:104 guards with the `errorOffset` fallback (`state.furthest >= 0 ? state.furthest : errorOffset`) — dist chunk :53 concurs. The tree already holds the case; recorded because a killed hypothesis is evidence the same way a defect is.
9. **Both readers' remaining measurements spot-checked exact** — the 22-site mergeErrorState census, the zero-hit tier grep (this seat re-widened it), the four PACKRAT_ARMED lines, the chunk's 0 imports / 125 lines / 3,516 B, the twin CJS binding, the ParseIssue/ParseResult verbatims, the GROUND-B line cites, the W1/W2/parser-band verbatims. An unusually clean pair outside the rows above.

## Killed-claims register (12 sustained + 1 reversed)

Sustained, each decider re-verified or re-run by this seat where load-bearing: **K1** — L·M-2's equivalence claim and §8 cure 6 ("delete utils.ts:106-110 in favor of `state.getLineAndColumn`"): the two algorithms diverge at exactly `furthest === 0` over a leading newline (P2: clamp → 0, `{2,-1}` vs the inline's `{1,0}`); the cure would inject a negative column into every offset-0 Diagnostic — the correct cure is the inverse (both readers; the corpus's own C·S-5 was the falsifier). **K2** — L·M-3's newline-offset sub-clause is inverted: at a newline offset the two methods AGREE (exclusive slice cancels inclusive fromIndex — re-derived on the live bytes); everywhere else they differ by one. **K3** — C·INFO-A's REFUTED verdict: a `heapUsed` delta cannot see scavenged young-gen churn (it measures retention, not allocation rate) and the all-arms-at-offset-0 fixture fires the new-furthest event once per parse — the refutation is unproven, the structural claim stands, the magnitude is MEASURE-AT-OPEN row 2. **K4** — L·M-1's MAJOR: two young-gen arrays per new-furthest event bounded by distinct offsets is MINOR; cure corrected per Seat ruling 4. **K5** — C·D-11's "impossible"/L·B-4.1's "cannot": the flag is purely observable (P1); "must clobber" and the W1-G-5-unassertable corollary die; the reachability defect survives. **K6** — C·B-4's "which falsifies README:193": README's zero-overhead claim scopes to the flag and README:221-226 documents the collection as unconditional; the false-doc rows are diagnostics.ts:3-5 (row 13) and README:235-237 (row 17). **K7** — C·D-14's "three mutually inconsistent conventions": its own measurement prints 0/1/1 — two conventions plus one edge divergence. **K8** — L·M-9's MAJOR-here: the clone's invariant break is destroyed before observation; MAJOR lives at the pt-core row 14 identity, MINOR residue here (row 21). **K9** — L·M-10's "no debug runtime module is emitted at all": statePrint/addCursor/formatExpected ship in the chunk (`toString()` requires them); exactly two formatters are absent. **K10** — L·M-11's MAJOR: one erasing type-import away, tsc exit 0; MINOR. **K11** — C §5 WRAP limb (b) "scope the journal per parse — impossible": clear-then-parse-then-copy scopes a single non-reentrant parse with published exports (GROUND-B does it); the true, still-fatal claim is reentrant-safe scoping is impossible. **K12** — the provenance bundle, merged from both readers and verified at the corrected lines, none changing a verdict: W1's G-5 falsifier text at :496-498 not :487-489; R-LAW-1 at W2.md:275-278 not :281-283; the L-doc's mergeErrorState census 20 → **22** (drops leaf.ts:291/:303/:360, understating L·M-1); packrat cite drifts (:351/:356/:252-260/:196-202/:262-272); `parse(val)` at :77-79; parseState at :34-49/:51-75; README center-truncation at :205-208; test/ 18 entries not 17; `proof:all` chains ten not nine. **REVERSED** — reader 1's kill of L·B-3's BLOCKER: Seat ruling 1, row 7, witness P3.

## Superlatives (banked, L-18 both ways)

- **S1 — the true runtime leaf** (L·S-1/C·S-6, re-verified at the bytes): 0 imports, 125 lines, 3,516 B — held only by `import type`/`export type` discipline under verbatimModuleSyntax; one value import from state.js would drag debug+ansi into every consumer of the toggles. One character from collapse, and the tree holds it.
- **S2 — the per-parse migration is real and self-documenting** (L·S-2/C·S-4): the five per-state functions touch only the state passed in; the rationale block matches the bytes. It is also what made this seat's P1 probe side-effect-free — the discipline pays for its own verification.
- **S3 — mergeErrorState's common path is allocation-free**, dedupe in-place via `includes` on a tiny array — right hot-parser instinct, which is why L·M-1 reads as a two-line lapse.
- **S4 — the snapshot's three spreads are load-bearing** against resetErrorState's reassignment; dropping any one is a shared-mutation bug the tree avoided — and the exact care not applied at the :139 boundary (row 10's irony, held).
- **S5 — the byte-scanners are textbook** (charCodeAt literals, memchr-style indexOf, single writeback, break-not-scan on unterminated comments) — the strongest evidence the file's lapses are oversights, not the author's standard.
- **S6 — the flag is genuinely two-way** (C·S-1): this module is NOT the PT-03 latch class; its sin is globality, not one-wayness. The corpus contradiction (W1's G-5 falsifier *reason*) is raised correctly with the conclusion left standing.
- **S7 — one chunk identity across all ESM entries** (C·S-2/L·F-3): the five-entry build did not fracture the global — which the corpus's own DEBT-1 probe silently depends on (toggles from root, mergeErrorState from `/diagnostics`).
- **S8 — the shim is honest, minimal, wildcard-free** (C·S-3/C·S-7): explicit named list against index.ts:14's `export *`; the d.ts a faithful 2-line mirror.
- **S9 — C·S-5 PROMOTED**: `collectDiagnostic`'s inline slice-first line/column is correct exactly where the shared helper is broken — the single most load-bearing superlative in the corpus, because it is the falsifier that killed L·M-2's equivalence and cure (K1). A superlative doing adjudication work.
- **S10 — reader 2's LIVE-S-1, seat-verified**: `many()`'s progress guard (`if (state.offset === savedOffset) break;`, immediately after the success branch) already satisfies R-LAW-4's progress clause by construction — a hypothesis the reader formed and killed against itself; the silent-truncation residual folds to MEASURE-AT-OPEN row 4.
- **S11 — reader 2's LIVE-S-2, seat-re-run (P1)**: the flag the library refuses to export is nonetheless purely observable through two published symbols — the constructive kill that rescues W1's G-5 from resting on a stderr proxy (row 32).
- **S12 — value.js's frozen `/css` contract is the asset** (reader 2's LIVE-S-3, types verified): deep-Readonly, closed 8-code union, spans not points, non-empty failure tuple, empty-tuple success arm — it forbids PT-07, the hollow-diagnostic posture, and the two-position drift at the type level, before any code is written. RETIRE is right because the consumer already holds the better algebra.
- **S13 — the corpus's self-kill discipline**: F1 (no hidden-class transition), F2 (SlicedString retraction), F3 (ESM unity), INFO-B (tsc exit 0), the withdrawn recover commendation, the addendum's withdrawn cast-count — all re-verified or spot-held; only INFO-A's *method* fails (K3). Passes that kill their own strongest-sounding claims are worth more than passes that do not — and this seat added its own (Seat ruling 8).

## MEASURE-AT-OPEN residue (bench-dependent rows for the lane's W1 harness)

1. **C·B-4's 160.8 MiB / 800,004-object retention** — challenger-measured, N=1, one machine, `--expose-gc`; never re-run by either reader or this seat (no-bench law). Re-measure at W1 open before the figure is banked into any gate or cure justification; the structural mechanism (rows 2/13) is settled without it.
2. **L·M-1's magnitude** — INFO-A's refutation is method-unsound (K3); if any cure is ever justified on speed rather than hygiene, the instrument is an interleaved-cell throughput bench on a progressing grammar (the DEBT-2 reject leg, parser-band.md:117), not a heapUsed delta.
3. **C·D-6's measured ESM/CJS non-identity** — one disposable-process witness at W4 open **only if** the dual-format ship survives RC-P (shared with pt-core residue 6).
4. **R-LAW-5 starvation + zero-width-sync truncation probes** (rows 19, S10) — W1 corpus rows: a non-final-arm recovery construction, and a zero-width `sync` inside `many().recover()` (truncates silently at the first recovery while the diagnostic stays in the buffer — an R-LAW-4 under-count needing a grammar bug to reach).
5. **The G-5 amendment** (row 32) — bank the P1 predicate at bench-cell entry AND exit into W1's G-5 at open; until then G-5's stderr proxy stands as specified.

## Closing verdict

**Six blockers survive and the C-challenge's RETIRE-in-whole is ADJUDICATED AND HELD: a tier that is nothing but latch operations (6/6 global), whose labels arrive only with a stderr effect, whose buffer fills unarmed and is vacuous from its own subpath, and whose rollback corrupts the journal under the exact composition a resilient CSS grammar is — witnessed live by this seat, unarmed, at the published bytes.** The routing is clean: the algebra laws own the retirement (rows 2/4/5/6 → **W2**: O-8, R-LAW-3, EQ-4 — diagnostics become parameters and values of a parse); the recovery mechanics own the re-authoring (rows 7/18/19 + the folds of 25/27 → **W3.b**: mark/rewind per R-LAW-1, the recovered-success representation ruled before the wave opens, R-LAW-5 declared, skipped bytes into `C`); the packaging truths go to **W4** (rows 3/12/13/14/16/17/24/28/29, with four cross-books into pt-core rows 3/5/38-47 so no identity is booked twice); two spec sharpenings and the G-5 upgrade leave as the **REFINEMENT FOLD**; and eight rows have **no wave owner**, recorded sharpest-first (the state-destroying reset and the LIFO/rebind journal pair) so any future parse-that source wave inherits a complete debt roster. What ports is semantic and named: the merge rule, the per-parse threading, the leaf layering — S2/S3/S4's craft — into a home (`ParseIssue`/`ParseResult`, S12) that already forbids at the type level everything this module gets wrong at runtime. The fleet lesson this corpus adds to pt-core's: **a reader pair is not redundancy, it is a jury** — one reader dropped a filed BLOCKER the other caught, one endorsed a cure the other had proved regressive, and their one severity deadlock was broken not by preference but by a witness neither had run. The roster above is what survives two challenges, two hostile re-reads, and one adjudicator, all attacked.

— end of apotheosis. Both challenge records and both hostile re-reads remain immutable beside this file per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.

## RE-ADJUDICATION STAMP (E-3 addendum — appended, nothing above rewritten)

**Seat 2 receipt**: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, fresh session 2026-08-06. The apotheosis above was written 2026-08-05 but never delivered to the orchestrator; before completing delivery this seat re-ran the incredulity pass with its own instruments rather than trusting its predecessor. **Re-derived independently this session (all static; nothing armed, nothing executed, parse-that read-only at `ef10d5b`, STOP-check re-clean)**: both challenge files read whole (tallies, self-kills, the L-doc's in-place L-M2 correction and withdrawn superlative, the C-addendum's D-17/D-18 — all as characterized above); `utils.ts` whole + `parser.ts` parseState/recover + `state.ts` clone/three-line-col-methods + `diagnostics.ts` whole + `leaf.ts` `any()` arms; the K1/K2 arithmetic re-derived from the ECMA `lastIndexOf` position-clamp rule; the censuses re-measured exact (22 `mergeErrorState` sites / 0 in packrat.ts; `collectDiagnostic` sole caller `:666`; `.furthest =` three writers, no restore path; tier symbols **rc=1 zero hits** over `test/`+`scripts/`; `core.js:1` → packrat chunk `:1` → six diagnostics symbols; formatters absent from every shipped `.js`/`.cjs`; twin CJS chunk `DpUY87_d.cjs`; 0-import / 125-line chunk with gate-free `collectDiagnostic`; the `:104` `errorOffset` fallback — this seat independently formed the same negative-index hypothesis as Seat ruling 8 and killed it at the same line); row 18's decider (`diagnostics: readonly []` success arm) and the pt-core cross-book rows 3/5/9/11/14/20 re-read verbatim. **One header drift found and resolved**: value.js HEAD has moved `0be528dc` → `1caf0f11` (two unrelated doc-bank commits); the **row-1 calibration was re-verified TRUE at the new HEAD** — deps still `{glass-ui, keyframes.js}`, lockfile `parse-that` hits 0, `src/parsing` absent, zero `@mkbabb/parse-that` imports under `src/`/`demo/`/`test/`. **Witness posture**: P1/P2 are statically certain and were re-derived from source by this seat; P3/P4/P5's mechanisms are statically certain from the recover source read this session — the live numbers remain the first seat's and are attributed so. **No row, kill, superlative, severity, or disposition changed.** The roster stands as adjudicated: 35 rows · 6 blockers · 12 kills sustained + 1 reversed · 13 superlatives · RETIRE-in-whole HELD.
