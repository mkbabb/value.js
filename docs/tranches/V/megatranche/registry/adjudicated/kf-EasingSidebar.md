claude-fable-5

# kf-EasingSidebar — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/scenes/easing/EasingSidebar.vue` (234 L) — reader-1 ("design-and-usage": 30 confirmation cells · 7 kills · 8 misses · 10 superlatives) and reader-2 ("library-and-code": 25 confirmation cells · 4 kills · 6 misses · 7 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/EasingSidebar/challenge-{D-design,L-library,C-consumption}.md`, 387/273/325 L, all opus-5[1m]).
**Method (incredulity)**: every reader-vs-reader head-on contradiction (the smooth-step-3 polarity; the write-through cure; the L-i2 grade; the D-17 residue), every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption. **Zero browser tooling** (law); rulings are static reads + `node` executions against the **installed** `@mkbabb/value.js@4.0.0` and this seat's own pretty-print of the installed `@mkbabb/glass-ui@7.0.0` dist — product source read-only; the irreducibly visual residue rides to SS-13/KF.W9. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only + node-against-dist)**: EasingSidebar.vue whole (234 L); useEasingDemo.ts `:30-135` + `:175-215` + `:245-275` + `:300-320` + `:360-410`; TimingFunctionPanel.vue whole (166 L, worktree); EasingScene.vue `:1-60`; EasingTarget.vue `:20-40/:150-180/:200-220/:283-290`; ControlsPaneWrapper.vue `:100-150/:175-185` (the Drawer `v-if` / rail `v-else-if` two-branch over `createReusableTemplate`) + ControlsPaneWrapper.css media/rail cells; easingGroups.ts whole (**29 items / 10 families / 28 reachable after the Custom filter** — correcting reader-1's 33/32); AnimationVisualizer.vue `:1-20` (`aria-hidden="true"`; `Math.min(effectiveT/duration, 1)` — no easing); design-idioms.css `:47` (`--rail-width: clamp(25rem, 33svi, 32rem)`); font-roles.json `:50-85` (control-label→`.labeled-field-label`; `monoAllowedSelectors`; `_monoContract` clauses c/d); style.css demo-typography (zero `text-transform`; the dead `.labeled-field-label` override); glass-ui dist pretty-printed by this seat (1359 L): `setHandle` `:72-80` (**`r.value = "custom"` at `:79`**), `H` throw `:59-62`, `easingFn` `:83-88`, the six-field `value` computed `:108-115` (**no `preset` field**), `initialPreset ?? "ease-out-back"` `:65-66`, `q` fn-identity comparator `:345`, `We` write-through `:355-376`, the model watch `{deep, immediate}` `:377-380`, the SOLE prop watch (`mode`) `:332-337`, `preset` consumed once as `initialPreset` `:328`, keyboard Home/End/±0.01 `:444-466`, root `grid gap-4 lg:grid-cols-[1fr_18rem]` `:564`, canvas card `overflow-hidden` `:231`, `38cqi` `:599`, svg `aria-label: e.label` `:602` (the only binding), steps Slider `min:1 max:12` `:~982`, `.focus()` count = **1** (`:524`, the readout select-contents helper — correcting both readers' 0); labeled-field.js greps (`tooltip|labelClass` 0; `inheritAttrs` 0); glass-ui.css (`.labeled-field{gap:calc(var(--spacing)*2)}`; `.glass-label{--font-text/500/foreground}`); **`dist/styles/accessibility.css` line 1 imports `glass/a11y-fallback.css`, whose `forced-colors: active` block borders every glass tier incl. `.glass-card` with `1px solid CanvasText` — the D-17 decider**; `dist/styles/index.css` tail `@source "../*.js"`; node probes: `easing("smooth-step-3")` → **ok** (Hermite: reg(0.25)=0.156250=3t²−2t³ exactly) vs `CubicBezier(0.65,0,0.35,1)` → **max |Δ| = 0.111014 at t=0.35** — the polarity decider; `steppedEase(1,"jump-none")` → `step_count_invalid` (all three other 1-count terms ok; `(2,"jump-none")` ok); `cubicBezierToString(0.123456,…)` → 2 dp; **19 of 30** `bezierPresets` quads fail 2-dp round-trip at the 5e-4 tolerance (max Δ 0.005) — reader-2's figures reproduced; dead-export greps `currentEasingFn/svgPath/comparisonCurves/currentFamily` → **0 consumers each**; phantom-prop census: **4 files — EasingSidebar 1+1 · SpringPhysicsFacet 2+2 · ChannelOptions 5+5 · LayerConfigPanel 5+0 = `tooltip` ×13, `label-class` ×8** (KF-CO-2's banked 10+5 = the two ChannelOptions-corpus files exactly); `@container easing-editor` → 0; `container-name` sites → exactly 3; App.vue `:61-62` `#tabs-content` vs `:73` `#target` sibling slots.
**Substrate disclosure (inherited SCH-1, re-run by this seat)**: checkout = the sacred stale `8281638c`, worktree dirty (252 rows). `git diff origin/master` over this corpus's blast surface — `demo/scenes/easing/`, `demo/utils/reference-data/`, `demo/components/instrument/transport/controls-pane/`, `demo/styles/` — is **EMPTY**: every EasingSidebar row below is **frontier-durable by identity**. Exactly two cited files diverge: `TimingFunctionPanel.vue` (worktree lacks master's EE-02 css-twin — 9 lines at `:144`; sibling anchors here are worktree coordinates) and `package.json` (worktree reverts the frontier's `"@mkbabb/glass-ui": "7.0.0"` exact devDep + `proof:structure`) — so every glass-ui-7.0.0 cell was measured against the frontier's declared producer, and the F-1 phantom is worktree-only (census SCH-1, banked at kf-App). The frontier's `check` script is **still plain `tsc`** and `vue-tsc` is in neither coordinate's devDependencies nor `node_modules/.bin` — the KF-CB-8 extension is frontier-durable too.
**Law compliance**: product source read-only; no browser tooling; **sole write = this file.**

## Scoreboard

**34 adjudicated roster rows — 2 BLOCKER · 12 MAJOR · 13 MINOR · 7 INFO**, of which **8 are folds by reference to banked identities (KF-ET-6 · KF-CO-17 · KF-CO-2 · KF-ET-1 · KF-ET-20 · KF-ET-5 · census SCH-1/F-1 · KF-CB-8) — zero re-bookings**. **10 killed-claims register entries, all SUSTAINED** — chief among them the **smooth-step-3 polarity reversal** (the corpus's shared BLOCKER thesis is inverted: the caption is TRUE and the widget is the liar, decided by this seat's own 0.111 probe) and the **write-through cure-kill** (the corpus-unanimous "delete the remount machinery" repair cannot seat a preset display — `setHandle` stamps `"custom"`; the model carries no `preset`). **13 distinct missed rows ADMITTED** (8 from reader-1, 6 from reader-2, one identity shared). **7 adjudicator corrections against the readers**, including one reader-2 unproven-residue cell reversed at the bytes (D-17's fallback file exists, is imported, and covers `.glass-card`) and one reader-1 blanket confirmation split (the "26 % re-implements vendor machinery" quantification). **10 superlative cells sustained.**

Blocker-weight survivors: **KF-ES-1** (the standing seed-echo filter permanently swallows real edits in both arms) and **KF-ES-2** (step-start/step-end edits dropped on stage while corrupting the `steps` tile's next open) — both frontier-durable, both cure-routed to the SS-1/SS-2 spec, neither dischargeable by either sibling's existing predicate (both donors are defective — ruled below).

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **The smooth-step-3 polarity** (D-1/C-7/L-M3 core) | KILLED the corpus thesis: the two `smooth-step-3`s are DIFFERENT curves; the caption is TRUE; missed-BLOCKER filed (the picker seeds a materially wrong curve) | CONFIRMED the corpus framing MAJOR: "the panel prints a statement its own widget falsifies" | **Reader-1's KILL SUSTAINED; reader-2's framing REVERSED; the row survives with inverted polarity as a KF-ET-6 fold** | My probe against installed value.js 4.0.0: `easing("smooth-step-3")` → ok, and it IS the Hermite smoothstep (reg(0.25) = 0.156250 = 3t²−2t³ exactly); `bezierPresets["smooth-step-3"] = [0.65,0,0.35,1]` deviates from it by **max 0.111014 at t = 0.35** — 222× the file's own `quadEq` tolerance. So the caption ("engine-native — editing here authors a custom cubic-bezier") is literally true, `NAMED_EASING_BEZIER`'s omission is CORRECT, `isBezierEditable` false is CORRECT — and the defect is the `:112` `name in bezierPresets` seed gate exhibiting an 11 %-wrong curve as the selection. The `:86-89` comment stays false in both clauses (quart/quint in both maps; sines byte-identical; strict subset — convergent probes, mine included). **Cure-lock derived**: the corpus-unanimous "one catalogue kills L-M3" prescription is REFUTED — merging catalogues makes `isBezierEditable("smooth-step-3")` true and hands the user a silently-wrong editable curve; `seedFor` must gate on bezier-EXPRESSIBILITY (`NAMED_EASING_BEZIER`), treating smooth-step-3 like ease-in-bounce. This ANSWERS the owner-ruling flag KF-ET-6 banked ("repointing at bezierPresets flips smooth-step-3's class — a behaviour decision"): the measurement decides it — **never repoint**. |
| **The write-through cure** (D-3/C-3/L-M2's repair half) | CONFIRMED wholesale: "~26 % of the file re-implements vendor machinery"; premise + cure adopted together | KILLED the cure: `EasingPickerValue` has no `preset`; `setHandle` stamps `"custom"`; `preset/steps/term` initial-only | **Reader-2's KILL SUSTAINED at my bytes; reader-1's blanket confirmation SPLIT** | My pretty-print: the `value` computed (`:108-115`) carries exactly `mode/css/fn/points/steps/term` — no preset; `We` (`:355-376`) seats points via `N` = `setHandle`, which unconditionally executes `r.value = I` (`I = "custom"`, `:79`) — every model-driven re-seat leaves the Preset Select reading "custom"; `c.preset` is consumed once, as `initialPreset` (`:328`); the ONLY prop watch is `mode` (`:332-337`). So the comment's PREMISE ("modelValue is EMIT-ONLY") is false — `k(l, We, {deep, immediate})` (`:377-380`) is a full write-through — but its CONCLUSION ("a remount is the only blessed re-seat seam") is TRUE for the bezier-preset arm. The honest cure is a SPLIT: model write-through for the steps arm (`We` writes `steps`/`term` directly, `:368-373`), remount (or a producer `preset` seam — BH ask) for named-preset seating. ONE identity with **KF-CO-17** (the sibling seat's banked row) — this ruling SHARPENS it: the vendor limitation "that does not exist" partially does. |
| **L-i2 grade** (`steps(1, jump-none)` throws) | Kept the axis's INFO ("two clicks") while its own proof showed reachability | Raised to MAJOR: "ONE interaction away" from the `step-start` seed | **Reader-2's RAISE ADOPTED — MAJOR, GLASS-OWNED; reader-1 overruled against its own proof** | My probes: `steppedEase(1,"jump-none")` → `step_count_invalid`; all other 1-count terms ok. My source read: `seedFor("step-start")` seeds `{steps: 1, term: "jump-start"}` (EasingSidebar.vue:107-109), the vendor's term Select offers all four `jumpTerms` unvalidated against count, `H` (`:59-62`) throws inside the `easingFn` computed (`:83-88`), and `stepPathD` reads it into the rendered `<path d>` — the picker's RENDER throws before `onPickerChange` can run. One Select pick from a shipped tile's seed. No `onErrorCaptured` in the file (whole read). Label follows the cure's owner (ColorSpaceSelector D-10 precedent): the producer must validate term-vs-count (or degrade); consumer riders = the 1-step seeds and the absent boundary. |
| **D-17 residue** (forced-colors) | KILLED decisively: `accessibility.css` IMPORTS `glass/a11y-fallback.css`, which borders every tier | Left half-alive in unproven ("glass-ui ships one such block… addressing only selection/invalid") | **Reader-1's KILL SUSTAINED at my own bytes; reader-2's residue cell REVERSED** | My read: `dist/styles/accessibility.css` line 1 = `@import "./glass/a11y-fallback.css"`, and that file's `forced-colors: active` block zeroes the glass, gives `.glass-material/.glass-wash/.glass-quiet/…/.glass-card/.glass-drawer` a `1px solid CanvasText` border, opaques the floating tiers and `display:none`s the glass pseudo-layers. All three of D-16's stacked surfaces are covered. Reader-2 repeated the D axis's import-blind read — the same error class the kf-EasingTarget record logged as citation-inheritance. D-17 is dead; forced-colors leaves the D-16/SS-13 question. |
| **L-M4 grade + count** | MAJOR; "three representations of one curve" implicitly retained | MINOR; KILLED the third representation (the raw callable is dead code) | **Reader-2's DOWNGRADE + KILL SUSTAINED; the literal half folds to KF-ET-1** | `currentEasingFn` has 0 consumers repo-wide (my grep; Vue computeds are lazy — never evaluated). Two representations survive: the picker's 3-dp verified readout and the 2-dp `cubicBezierToString` path shared by the header literal and the CSS twin. The catalogue-wide loss (my probe: **19 of 30** quads fail round-trip, max Δ 0.005 = 10× `quadEq`) is **KF-ET-1's banked identity** (the literal fork, BLOCKER, cure-locked) — never re-booked; the sidebar-side residue (discarding the vendor's `reparseOk`-paired `css`/`fn`) books MINOR here and feeds the same spec. |
| **L-B1's terminal symptom** | Confirmed the blocker without the ball clause | KILLED "the ball on stage animates the other curve" | **KILL SUSTAINED — convergent with reader-1's own visualizer facts** | EasingTarget's tiles paint per-tile static portraits (`fnForCurve`, name-keyed; the `cubic-bezier` tile is filtered out with the Custom family — my reads); AnimationVisualizer is `aria-hidden` and applies NO easing (`Math.min(effectiveT/duration, 1)` — my read). No stage ball ever binds the authored curve. The BLOCKER stands on the header-literal + `previewAnim` + picker-display divergence. |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md` §(a): W0 Substrate Settle · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**; producer defects are **GLASS-OWNED**, riding the standing glass-ui BH relay (owner edict 2026-07-12). Routings are bookings, not acts. Line anchors are worktree-at-`8281638c` (= `origin/master` bytes for every EasingSidebar-chain file); re-resolve before any cure (D-19).

### BLOCKER

- **KF-ES-1 · L-B1 = C-2 — `isSeedEcho` is a STANDING by-value filter measured against the SEED, which is never consumed and never refreshed after a custom drag — a real edit returning to the seed value is permanently swallowed, in both arms.** Predicate `:144-157`; application `:185`; the seed refreshes only when `seedFor` returns non-null (`:159-166`) and `seedFor("cubic-bezier")` returns null (`:115-118`). Bezier arm: tile `ease` → drag (name flips to `cubic-bezier`, seed still `ease`) → pick `ease` in the picker's own Preset Select → the vendor emits (its `q` comparator requires `e.fn === t.fn`, `:345` of my print — function identity across a recompute, essentially always false; the `J` latch is cleared inside `We`) → `quadEq(bezierPresets.ease, v.points)` true → dropped; the picker says `ease`, the demo animates the dragged quad, terminally. Steps arm needs no name change at all: `steps` seeds `{4, "jump-end"}` → drag 4→7 (applied) → drag 7→4 (swallowed) — picker renders 4, demo animates 7. The file's own comment (`:140-143`) claims the opposite ("can never swallow a real edit"). Ball-clause killed (ruled above); blast = header literal + `previewAnim` + the picker's own display. **BLOCKER (unanimous).** → **NO-WAVE-OWNER** (SS-1/SS-2): the echo predicate must compare the LIVE demo state, not the seed — and NEITHER sibling is a clean donor (TimingFunctionPanel's primary arm is correct but its `!seed` arm is a hard-coded `"ease-out-back"` standing filter of the same class, `:121-127` worktree — kill #9). The shared-seat composable (KF-ES-12) is the cure vehicle.
- **KF-ES-2 · D-2 = C-1 = L-B2 — with `step-start`/`step-end` selected, every step-count and jump-term edit is written to `stepOptions` and read by NOTHING, because the promotion at `:192` is gated on a three-name `isSteps` while every consumer tests the single literal `"steps"` — and the dropped write corrupts what the `steps` tile opens with next time.** `:186-193` (write `:190`, dead promotion `:192`); `isSteps` three-name at useEasingDemo.ts:68-71; `currentEasingFn`/`cssValue`/`svgPath` all gate on `"steps"` (`:78-114`); `cssValue` returns the unchanged bare name so the `watch(cssValue)` re-seat never fires (`:308-315`, fail-soft catch and all); reachability via easingGroups Steps family; the vendor's steps slider renders in steps mode regardless of `playback` (min 1 / max 12, my print `:~982`). Second-order leak: `seedFor("steps")` reads the corrupted `stepOptions` (`:103-106`). Blast-radius correction sustained (kill #10): `currentEasingFn`/`svgPath` are dead exports — the live damage runs through `cssValue`→`previewAnim`, the header literal, and the picker's own contradictory readout (its copy affordance hands over `steps(6, jump-start)` while the app runs a 1-step curve). **BLOCKER (unanimous).** → **NO-WAVE-OWNER** (SS-1/SS-2): the spec must DECIDE the semantics (promote to `steps(n,term)` on edit, or disable the steps controls for the singular names) and route the write through a composable `setStepOptions` that owns the reconciliation (KF-ES-23's enabler note).

### MAJOR

- **KF-ES-3 · D-1 = C-7 = L-M3 (+ C-6's comment half) — the two-catalogue smooth-step-3 row, POLARITY REVERSED: the picker seeds and draws a curve 0.111 off the one selected and animated, under a caption that is telling the truth.** = **KF-ET-6 identity (banked MAJOR, KF.W4) — FOLD-EXTENDED, never re-booked.** This seat's additions: the 0.111014 measurement (ruled table); the third same-tick divergence (`selectEasing`'s else-arm resets `bezierControlPoints` to `[0,0,1,1]`, useEasingDemo.ts:255-257 — so picker, stored quad and stage disagree three ways); and the **cure-lock** that answers KF-ET-6's owner-ruling flag: gate `seedFor` on `NAMED_EASING_BEZIER`, never merge the catalogues. The `:86-89` comment stays false in both clauses and must be corrected by whichever wave touches the file. **MAJOR (banked grade; reader-1's BLOCKER preserved as dissent — scope is one tile of 28, but its picker-copy affordance exports the 11 %-wrong quad).** → **fold to KF-ET-6 → KF.W4**, cure-locked.
- **KF-ES-4 · D-3 = C-3 = L-M2 (premise) — the load-bearing "glass-ui 4.0.1 modelValue is EMIT-ONLY" rationale is stale against the installed-and-frontier-declared 7.0.0 — but the remount it justifies is NOT redundant.** Premise falsity confirmed at my bytes (`We` + the deep/immediate model watch); cure SPLIT-RULED (table): steps arm model-writable, bezier-preset arm remount-only (no `preset` field; `setHandle`→`"custom"`). = **KF-CO-17 identity (banked at kf-ChannelOptions for the sibling seat) — FOLD-EXTENDED with the cure-kill sharpening**; the byte-adjacent twin comment lives at TimingFunctionPanel.vue:25-31. **MAJOR.** → **fold to KF-CO-17**; this seat's instance rides the SS-1/SS-2 seat-composable spec (KF-ES-12) + **GLASS-OWNED rider** on the BH relay: a `preset` seam on the model (or `initialPoints`), so consumers can re-seat a named preset without remount — the vendor already ships the echo suppression both seats re-implement.
- **KF-ES-5 · C-4 = D-3 (remount half) — the picker tears ITSELF down mid-interaction: a preset pick in its own dropdown, or a deterministic keyboard trip (`Home`/`ArrowDown`×10 → handle 2 `End`/`ArrowUp` → `[0,0,1,1]` = `linear`), lands in `nameForQuad` → `selectEasing` → new `:key` → remount under the user's fingers; nothing restores focus.** Keyboard handler verified in my print (`:444-466`; steps 0.01/0.1, Home x=0, End x=1); `nameForQuad` hits for all 29 NAMED-covered presets; `.focus()` count = **1** and it is the readout select-contents helper (`:524`), not a mount restore — correcting both readers' 0. WCAG 2.4.3/3.2.2. **MAJOR.** → **NO-WAVE-OWNER** (the re-seat spec must preserve focus across whatever seam survives KF-ES-4's split); focus destination + AT teardown → SS-13.
- **KF-ES-6 · reader-2 miss M-1, ADMITTED and verified — the rebuild fallback silently seats the picker on `ease` while the demo holds an authored custom curve, then KF-ES-1 arms permanently on the `ease` quad.** `pickerSeed` init `:121-127` (`seedFor(...) ?? {preset: "ease"}`); the pane body is mounted from TWO branches over `createReusableTemplate` (ControlsPaneWrapper.vue — Drawer `v-if="isMobileLayout && showSheet"` / rail `v-else-if="!isMobileLayout"`, my read), so crossing the 1024 px boundary (or a mobile `showSheet` drop) rebuilds EasingSidebar while the demo context — owned by EasingScene in the SIBLING `#target` subtree — survives with `currentEasingName = "cubic-bezier"`. The rebuilt picker, its Preset Select and its copyable readout all say `ease`; the stage keeps the authored quad. The sibling handles exactly this case explicitly (TimingFunctionPanel.vue:118-127). **MAJOR.** → **NO-WAVE-OWNER** (the seed for `cubic-bezier` must carry the live quad — trivially expressible once the seat composable exists); one resize witness → SS-13.
- **KF-ES-7 · D-4 — the picker's root grid is VIEWPORT-gated (`lg:grid-cols-[1fr_18rem]`, my print `:564`; compiled via `@source "../*.js"`) inside a component that sizes its canvas in CONTAINER units — and the rail (400–512 px) exists only at viewports where `lg:` is active, so the two-column split is in force at every viewport where the panel renders.** Geometry convergent (fixed 288+16 px column vs a 344–456 px container); the branch is resolved STATICALLY by reader-1's observation, re-verified at my print: the canvas card is `overflow-hidden` (`:231`), zeroing the grid item's automatic minimum — the `1fr` track **CRUSHES** (≈40–152 px used width against a 200 px-floor canvas, letterboxed by `xMidYMid meet`), it does not overflow. **MAJOR, GLASS-OWNED** (the producer's `lg:` breakpoint contradicts its own `38cqi` container sizing — the cure is a container-query variant on the picker root, one class) → **BH relay letter**; used-width measurement at 1024/1600 px → SS-13. Consumer half is already correct (the `container-type` adoption is a sustained superlative).
- **KF-ES-8 · D-5 = C-10 — a full English sentence renders in `text-mono-caption` (Fira Code, 12 px, letter-spaced, `text-transform: uppercase` — my read of the vendor utility), uppercasing the two CSS identifiers it names, and `data-register="code"` launders it past the font census whose `_monoContract` clause (c) licenses only an identifier CHIP.** `:42-49`; zero `text-transform` anywhere in demo/styles (my grep); DESIGN.md "never a general UI voice". A user cannot copy what they read on a surface whose thesis is literal fidelity. **MAJOR.** → **NO-WAVE-OWNER** (SS-1/SS-2 copy + register: chip the name, body-voice the sentence — one motion with KF-ES-9/KF-ES-27).
- **KF-ES-9 · D-6 as-corrected — the catalogue-gap caption is a bare `v-if` `<p>` with no `role="status"`, no `aria-live`, no `aria-describedby` — the one message that says "your edit will not do what you think" is the unannounced one.** Comparison half corrected (kill #6): under `:playback="false"` the vendor's preview-status live region does not exist; only the copy-state region survives, conditionally. The blessed seam (`LabeledField` `description` → computed `aria-describedby`) is shipped and unused. **MAJOR.** → **NO-WAVE-OWNER** (route through the description seam or add the live region; same motion as KF-ES-8/KF-ES-27).
- **KF-ES-10 · D-7 = C-5 = L-M1 — `tooltip` and `label-class` are phantom props on `LabeledSlider`; both die as inert DOM attributes on `div.labeled-field`, and the unit `(ms)` exists nowhere else.** = **KF-CO-2 identity (the banked phantom-prop FAMILY) — FOLD-EXTENDED with this seat's corrected repo census: 4 files, `tooltip` ×13 / `label-class` ×8** (KF-CO-2's banked 10+5 = ChannelOptions + LayerConfigPanel exactly; EasingSidebar 1+1 and SpringPhysicsFacet 2+2 are the extension sites — reader-1's "4 files" right, C-5's "5" and L-M1's "3" both wrong). Honest mitigation sustained: `.glass-label` already renders the requested typography; the loss is the dead affordance and the unit. Enabling gate = KF-ES-34/KF-CB-8. **MAJOR (family).** → **fold to KF-CO-2** (the rename/rewire sweep: `tooltip`→`description`); this site's rider: `description="Sweep duration in milliseconds"` also discharges half of KF-ES-11.
- **KF-ES-11 · D-8 — the duration slider ships no visible value readout and no marks while the vendor control in the same card welds its live value into its label (`"Steps (n) — " + n`).** Vendor renders no text node in the slider (convergent); AT gets `aria-valuenow`, sighted users get nothing — the inversion is the point. Honest residue adopted: glass-ui exposes no live-value seam, so the cure is a demo-side readout or a producer ask. **MAJOR.** → **NO-WAVE-OWNER** + **GLASS-OWNED rider** (LabeledSlider value-readout seam) on the BH relay.
- **KF-ES-12 · L-M5 — two hand-maintained `EasingPicker` seats with no shared composable: same seven bindings, duplicated `quadEq` (divergently typed), divergent keys, divergent `isSeedEcho` semantics, byte-identical scoped CSS (including the same global `container-name`), duplicated stale rationale.** Both refinements adopted and verified: (a) "exactly one of them is right" is OVERSTATED — the panel's `!seed` arm is a hard-coded `"ease-out-back"` standing filter (kill #9); (b) the panel has its own remount defect this corpus's readers alone caught: its value-stable `pickerKey` is computed from the LIVE stored quad (`:92-101`), so the FIRST drag away from a seeded preset flips `seedPreset` → key → **remount mid-drag**, and the rebuilt picker displays the vendor default. Neither donor is clean; the composable must be specified, not lifted. **MAJOR.** → **NO-WAVE-OWNER** (SS-1/SS-2: `useEasingPickerSeat` — seed, key, live-state echo predicate, container CSS, one parameter for where truth lives), cross-referenced to KF-CO-17's OPTIONS-UNIT so the two specs converge on one artifact.
- **KF-ES-13 · L-i2 RAISED (ruled table) — `steps(1, jump-none)` is ONE Select pick away from the `step-start`/`step-end` seed and throws inside the vendor's RENDER, with no error boundary on this surface.** Chain at my own bytes + probes (ruled table). **MAJOR, GLASS-OWNED** (term Select unvalidated against count; `H` throws in a render-bound computed) → **BH relay letter**; consumer riders booked here: the `steps: 1` seeds are what make it one pick, and the file mounts no `onErrorCaptured`. Rendered outcome → SS-13.
- **KF-ES-14 · reader-1 miss M2, ADMITTED — the `:playback="false"` justification ("the gallery race IS the motion preview") is false in BOTH authoring modes at EVERY viewport, not merely under the mobile sheet: while the sidebar is doing its one job, no pixel anywhere moves along the authored curve.** Steps: the `steps` tile races a hard-coded `steppedEasing(4, "jump-end")` portrait ignoring `stepOptions` (EasingTarget.vue `fnForCurve` — my read; = the mechanism KF-ET-15 banked for the tile side). Bezier: the first drag flips to `cubic-bezier`, whose tile the Custom filter deletes (= KF-ET-5 Path B). The only other mover, AnimationVisualizer's ball, is linear AND `aria-hidden` (my read). D-10's mobile-sheet half survives with its premise WEAKENED (the Drawer is `mode="live-behind"`, `:show-overlay="false"`, resting at peek — my read of ControlsPaneWrapper); its desktop concession ("that reasoning holds") is struck. The engineering half (single-clock refusal) remains a sustained superlative — the DESIGN premise it is justified by does not hold. **MAJOR.** → **NO-WAVE-OWNER** (SS-1/SS-2 must design the authoring motion-preview story; restoring vendor playback is PRM-safe per the vendor's own `useReducedMotion` arm), cross-refs KF-ET-15/KF-ET-5.

### MINOR

- **KF-ES-15 · D-9 + D-11 + C-8 — the scoped `:deep()` block restates the vendor default in three pierced selectors and changes exactly one thing: it halves the design-system label→control gap (hard `0.25rem` vs the vendor's `calc(var(--spacing)*2)` = 0.5rem — my CSS read) and detaches it from the token, while the vendor ships the intended knob as the public `layout` prop.** Two rhythms for one relationship inside one card (the picker's own groups are `gap-2`). **MINOR (bundle; reader-1's MAJOR on the D-9 half preserved as dissent).** → **NO-WAVE-OWNER** (delete the block's field rules; keep the container rules).
- **KF-ES-16 · L-M4 as-rescoped — the vendor's `reparseOk`-verified `css` (and `fn`) are discarded at the seam and re-derived downstream through the lossier 2-dp path.** The literal-side blast = **KF-ET-1 identity (banked BLOCKER, cure-locked) — FOLD, never re-booked**; this seat's probe rides as the fold's rider (19 of 30 catalogue quads fail 2-dp round-trip at 5e-4, max Δ 0.005 — outside the file's own same-curve definition, so `nameForQuad` would refuse its own output). "Three representations" killed (ruled table). **MINOR here (the consumption residue; reader-1's MAJOR as dissent).** → **fold to KF-ET-1's spec** (consume `v.css`/`v.fn` where the demo needs a literal/callable).
- **KF-ES-17 · D-12 — `label="duration"` lowercase under three Title-case vendor labels in one card; no transform anywhere rescues it.** **MINOR.** → **NO-WAVE-OWNER** (one string).
- **KF-ES-18 · D-13 + reader-2 miss M-6 (ADMITTED) — `container-name: easing-editor` is inert (zero `@container easing-editor` rules — my grep) AND declared from two `<style scoped>` blocks into the flat global container namespace Vue does not scope** (exactly 3 `container-name` sites repo-wide, two of them this name — my grep). `container-type` alone carries the load. **MINOR.** → **NO-WAVE-OWNER** (delete the name in both files; same edit as KF-ES-12's shared CSS).
- **KF-ES-19 · D-14 + reader-1 miss M7 (ADMITTED) — caption prose: "engine-native" jargon, no terminal punctuation, a warning phrased as present-tense description, the THIRD naming of the curve on one screen; and the authorizing comment names an "elastic" family that does not exist (`:38-40` "(bounce/elastic families)"; easingGroups has no elastic and Bounce = exactly ONE item — my read), overstating the catalogue gap it explains: after KF-ES-3's cure-lock the caption fires for exactly two names (`ease-in-bounce`, `smooth-step-3`).** **MINOR.** → **NO-WAVE-OWNER** (one copy spec with KF-ES-8).
- **KF-ES-20 · D-15 — the `control-label` font-census role targets `.labeled-field-label`, a class the installed producer never emits (0 hits in glass dist — my grep; the real class is `.glass-label`), so the census role matches zero elements and the demo's `@layer demo-typography` override is dead.** Benign in pixels, vacuous as a gate; repo-wide, this component merely instantiates it. **MINOR.** → **KF.W5** (census-truth arm, with the KF-ET-27/S-10 design-idioms precedent).
- **KF-ES-21 · D-16 — three translucent surfaces stacked on one axis (`Card cartoon tier="quiet"` + the picker's two own `glass-card`s), and the demo's two hosts of one primitive disagree about wrapping (the sibling uses a bare div — my TFP read `:12`).** Forced-colors is NO LONGER part of this question (D-17 dead — every tier gets a CanvasText hairline for free). **MINOR.** → **KF.W6** (tier decision); legibility → SS-13.
- **KF-ES-23 · C-11 — ":168 the demo's ONE authoring seam" vs four write shapes, two of them raw cross-boundary ref writes (`:190` `stepOptions`, `:62` `duration`); the `:190` bypass is KF-ES-2's enabling condition and the `:62` bypass is KF-ES-25's.** **MINOR.** → **NO-WAVE-OWNER** (`setStepOptions`/`setDuration` methods on the composable own the reconciliation/rebase; rides the KF-ES-1/2 spec).
- **KF-ES-24 · L-m1 — `catalogueGap` is a hand-synced `ref` + sync function where one `computed` is the idiom, and its first clause is dead (implied by `!isBezierEditable`, whose first disjunct is `"cubic-bezier"`).** **MINOR.** → **NO-WAVE-OWNER** hygiene.
- **KF-ES-25 · L-m3 — the raw `duration` write bypasses any rebase seam: `phase = ((now − startTime) / (duration × 2)) % 1` with `startTime` rebased only in `onArm`/`reset` (my reads), so a mid-play drag 1500→3000 halves the phase instantly.** The sidebar is the sole mutator repo-wide. **MINOR**; magnitude → SS-13. → **NO-WAVE-OWNER** (rebase inside `setDuration`).
- **KF-ES-26 · L-m4 + reader-2 miss M-4 (ADMITTED) — the dead-export surface.** = **KF-ET-20 identity (banked: five zero-consumer exports) — FOLD-EXTENDED**: the ENTIRE `timingCurveUtils` + family-lookup import block of `useEasingDemo` is reachable only through the dead computeds, and the sidebar carries the ~20-member context into its prop contract while touching 8. My re-run: 0 consumers each for the four the corpus named. **MINOR.** → **fold to KF-ET-20** (consume-or-delete, decided with KF-ET-5's `currentFamily` double-status).
- **KF-ES-27 · reader-1 miss M5, ADMITTED — flat `gap-3` (`:15`) spaces the caption identically from the picker it annotates and the unrelated duration field below: no proximity binding for sighted users to mirror the programmatic binding AT also lacks (KF-ES-9).** The vendor's own two-level rhythm (gap-2 inside groups, gap-4 between) is the in-card counter-example. **MINOR.** → **NO-WAVE-OWNER** (the `description`-seam move cures KF-ES-9, this row, and KF-ES-29 in one edit).
- **KF-ES-28 · reader-1 miss M6, ADMITTED — `label="Easing curve editor"` is applied by the vendor exactly once, as the `aria-label` of the inner `<svg role="group">` (my print `:602`): the CANVAS claims the PANEL's name while the Card, caption and duration field belong to no named region and no heading — contrast the sibling's `<h3 class="text-title">`.** **MINOR.** → **NO-WAVE-OWNER** (panel landmark/heading; restore the vendor's narrower canvas default).

### INFO

- **KF-ES-22 · C-9 = L-m2 — the two cross-vendor jump-term casts are inert (identical four-member unions, one installed value.js instance, glass-ui's `JumpTerm` imported FROM value.js) and pre-silence the one drift signal the seam exists to catch — but nothing typechecks demo/ templates today anyway (KF-ES-34), so their teeth are contingent.** **INFO (reader-1's MINOR as dissent).** → NO-WAVE-OWNER hygiene, discharged with KF-ES-34.
- **KF-ES-29 · reader-1 miss M8, ADMITTED — the `v-if` caption is a layout-shifting insert directly above a drag target (one caption line + gap on every catalogue-boundary crossing).** **INFO.** → rides KF-ES-27's cure.
- **KF-ES-30 · L-i1 — `quadEq` referenced (`:156`) before its declaration (`:169-172`); no live TDZ (earliest call is the child's immediate emission, after parent setup); fragile to a future edit.** **INFO.** → NO-WAVE-OWNER.
- **KF-ES-31 · L-i3 + D-18 — `const demo = props.demo` snapshots the prop at setup (correct today: one context per scene mount, closure-carried); RTL is unprovisioned demo-wide, so no RTL defect is chargeable.** **INFO** (recorded, not charged).
- **KF-ES-32 · reader-2 miss M-5, ADMITTED — `isSeedEcho`'s steps-arm defaults (`seed.term ?? "jump-end"`, `seed.steps ?? 4`, `:148-151`) are DEAD: `seedFor` populates both on every steps branch and the only other seed source is bezier — false reassurance sitting inside the file's most defect-dense predicate.** **INFO.** → dies with the KF-ES-1 rewrite.
- **KF-ES-33 · C-12 = census F-1 — three glass-ui subpath imports against a dependency the worktree declares nowhere (deps = `{"@mkbabb/value.js": "4.0.0"}` — my read).** = **census SCH-1/F-1 identity (banked at kf-App ruling 1; KF-ET-3 precedent) — FOLD, never re-booked.** This seat's git reads DATE it: the frontier declares `@mkbabb/glass-ui: "7.0.0"` as an exact devDep (the worktree package.json is one of only two divergent files on this blast surface), so the phantom is worktree-only and every 7.0.0 cell above is evidence about the declared producer. **INFO-as-folded.** → **fold to SCH-1 → KF.W0**; rider: this component's 3 subpath edges.
- **KF-ES-34 · reader-1 miss M4 = reader-2 miss M-3 (ONE identity) — no SFC is typechecked anywhere: `check` is plain `tsc` (both git coordinates — my reads), `vue-tsc` is in neither devDependencies nor `node_modules/.bin`, CI's merge path runs `check:lib` only.** = **KF-CB-8 identity (banked: demo never typechecked on the merge path; the KF-ET-11 fold precedent) — FOLD-EXTENDED with the sharpening the readers add: the binary is ABSENT, not merely unwired, at the frontier too.** This is the mechanism behind KF-ES-10 (a template type error under vue-tsc) and the contingency in KF-ES-22. **INFO-as-folded (the identity's grade lives at KF-CB-8).** → **fold to KF-CB-8 → KF.W4** (gate topology).

*Also folded without a row*: reader-1 miss M3 ("authoring any custom curve leaves ZERO tiles pressed") = **KF-ET-5 Path B, banked verbatim at kf-EasingTarget** — one identity, sidebar-as-writer noted there already; zero re-booking.

## Killed-claims register — 10 entries, all SUSTAINED

1. **The corpus's smooth-step-3 thesis ("both halves of the caption are false" / "its exact cubic-bezier" / D-1's "the copy is falsified by the widget").** KILL SUSTAINED by this seat's own probe (max Δ 0.111014 at t=0.35; the registry curve is the exact Hermite smoothstep): the caption is TRUE, the widget lies, and the corpus-unanimous "one catalogue" cure would ship an 11 %-wrong editable curve. Reader-2's confirming frame reversed. Survivor = KF-ES-3 as polarity-corrected, folded to KF-ET-6, cure-locked.
2. **"`v-model` re-seating is available and would eliminate the remount entirely" (L-M2) / "~26 % of the file re-implements vendor machinery" (D-3) / "55 of 145 lines" (C-3).** KILL SUSTAINED at my bytes: the model has no `preset`, `setHandle` stamps `"custom"` (`:79`), `preset`/`steps`/`term` are initial-only, only `mode` is prop-watched. The premise-half of all three axes survives; the cure-half dies; the machinery is partially load-bearing. Reader-1's blanket confirmation split.
3. **D-17 — "nothing forces surfaces; the tier boundaries are carried by nothing under forced colors."** KILL SUSTAINED and re-derived: `accessibility.css` imports `glass/a11y-fallback.css`; every tier incl. `.glass-card` gets `1px solid CanvasText`; glass zeroed; pseudo-layers removed. Reader-2's unproven-residue cell repeating the import-blind read is REVERSED.
4. **C-6 bullet 1 — "the `:112` gate never excludes anything the demo can name."** KILL SUSTAINED: `ease-in-bounce` (the whole Bounce family) is reachable and absent from `bezierPresets` — the gate excludes one name correctly and wrongly admits exactly one other. Count corrected: **28** reachable tiles (29 items minus Custom), not reader-1's 32.
5. **L-m5 — "prop-vs-inject inconsistency" as a defect.** CONVERGENT KILL SUSTAINED: EasingSidebar mounts in App.vue's `#tabs-content` slot (`:61-62`), a SIBLING of the `#target` slot holding the provider (`:73`); `inject` would resolve `undefined` and crash on the non-null assertion. The prop is the only working channel (= SUP-1, sustained as a superlative).
6. **D-6's supporting comparison — "the vendor announces TWO strictly less consequential messages."** HALF-KILL SUSTAINED: the preview-status live region sits in the `e.playback` branch this surface removes; one conditional copy-state region survives. D-6's core (no live region, no describedby) unharmed.
7. **L-B1's terminal symptom — "the ball on stage animates the other curve."** KILL SUSTAINED (ruled table): no stage ball binds the selection; tiles are static name-keyed portraits; the visualizer is linear and `aria-hidden`.
8. **L-M4's "three inconsistent representations."** KILL SUSTAINED: the raw-precision callable lives only in `currentEasingFn`, which has zero consumers and never evaluates. Two representations; the precision defect survives inside KF-ET-1's banked identity.
9. **L-M5's "exactly one of them (the panel's) is right."** KILL SUSTAINED and EXTENDED at my TFP read: the panel's `!seed` arm is a hard-coded `"ease-out-back"` standing filter of KF-ES-1's class, AND its live-quad-derived `pickerKey` remounts the picker on the first drag away from any seeded preset. Neither predicate is a donor.
10. **D-2/C-1/L-B2's consumer bullets citing `currentEasingFn` and `svgPath` as damaged consumers.** CORRECTION SUSTAINED: both are dead exports (0 consumers, lazily never evaluated); the blocker's live blast is `cssValue`→`previewAnim` + the header literal + the picker's own readout. The blocker itself unharmed.

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-2's D-17 residue** — carried "glass-ui ships one such block… addressing only selection/invalid" into its unproven list after reader-1 had killed the claim; the fallback file exists, is imported, and covers every tier. The one reversed cell of this adjudication.
2. **Reader-2's smooth-step-3 frame** — confirmed the corpus's "widget falsifies the caption" without probing the registry curve; the polarity inverts under one node run. (The comment-falsity core it measured survives intact.)
3. **Reader-1's D-3 blanket confirmation** — adopted the "~26 % re-implements vendor machinery" quantification while the machinery's preset arm has no vendor replacement; split by the cure-kill it did not run.
4. **Reader-1's tile arithmetic** — "33 items / 32 reachable" is 29/28 (my easingGroups census); propagates into its C-6 kill and its missed#3 (both survive with corrected counts).
5. **Both readers' `.focus()` = 0** — the count is 1 (the readout select-contents helper, `:524`); substance (no mount restore) unchanged, but a zero that isn't a zero is exactly the cell class this program flags.
6. **Reader-1's L-i2 grade** — retained the axis's INFO against its own proof of one-pick reachability from the `step-start` seed; overruled to MAJOR/GLASS-OWNED (reader-2's raise adopted).
7. **The phantom-prop census** — C-5's "5 files", L-M1's "3 files", reader-1's "4 files": my count is 4 files / `tooltip` ×13 / `label-class` ×8, and KF-CO-2's banked "10+5 across two files" is exactly the ChannelOptions-corpus pair — the family extends, the identities reconcile, nobody re-books.

## Superlatives — sustained, with provenance (L-18 runs both ways)

1. **SUP-1 · the `demo` prop is the only channel that works** — proven statically by the C axis, sustained by both readers, re-verified at my App.vue read; it kills L-m5 and is the best single derivation in the corpus.
2. **The `container-type` adoption is load-bearing to the digit** — `38cqi` verbatim at my print `:599`; the comment names the exact mechanism; the one vendor claim in the file that survives 7.0.0 (and the correct half of the KF-ES-7 producer defect).
3. **The seed gate interrogates the vendor's own object** — `name in bezierPresets` reads the identical deduped module instance the picker imports; the vendor's silent `"ease-out-back"` fallback (`:65-66` of my print) is unreachable from this seat, while the sibling hard-codes that literal to compensate. (The irony stands: the TRANSCRIBED catalogue is the one that drifted — and the polarity ruling shows the transcription's OMISSION was the correct part.)
4. **`:playback="false"` is correct ENGINEERING** — a refusal of a second uncoordinated rAF clock against the scene's zero-rAF-at-rest guarantee; sustained as engineering while its design premise falls (KF-ES-14). The vendor's PRM apparatus makes restoration safe when the spec calls for it.
5. **`min="300"` is structurally load-bearing** — the sole guard between the raw `:62` write and `phase = x / (duration × 2)` division; `step="100"` gives 48 stops. Both verified at my reads.
6. **Value-based echo recognition is the right PRINCIPLE** — the vendor's `q` requires `e.fn === t.fn` (`:345`), so emissions are neither singular nor ordered and an order-based guard would be unsafe; the component reasoned correctly about undocumented behaviour. Only the comparand is wrong (KF-ES-1).
7. **The monotonic seed counter does real work** — returning to a previously-seeded name re-seats where the sibling's value-stable key cannot; and the counter design confines re-keys to selection changes, avoiding the sibling's mid-drag remount class entirely.
8. **`quadEq` typed to the exact 4-tuple** — arity at the compiler in a repo where (KF-ES-34) nothing else checks templates at all.
9. **The design law anticipated the vendor adoption** — font-roles.json `_monoContract` clause (d) pre-licenses the EasingPicker's own mono voice with provenance, and `monoAllowedSelectors` carries `[data-testid='easing-picker']`; the demo's violation (KF-ES-8) is of a contract that is itself precise and was authored in the right order (reader-1's own find, sustained at my read).
10. **Zero bespoke controls, zero custom properties, a 1,082-line shadow cluster deleted before the census asked** — the template is Card/CardContent/EasingPicker/one `<p>`/LabeledSlider; the file contributes nothing to the S-1..S-8 shadow tally and avoids the flat `--kf-*` hazard entirely; and the producer's `a11y-fallback.css` ladder (the file that kills D-17) means this posture inherits correct forced-colors behaviour for free — the standing argument for consuming the published primitive.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **KF-ES-7's used canvas width** at 1024 px (rail floor) and 1600 px (rail cap) — the CRUSH branch is resolved statically; only the measurement remains. One glance decides the producer letter's urgency.
2. **KF-ES-1's one-glance witness** — `steps` tile: drag count 4→7→4; the picker renders 4 risers while the demo animates 7.
3. **KF-ES-6's resize witness** — author a custom curve, cross 1024 px: the picker's Preset Select flips to `ease` while the stage header keeps the authored `cubic-bezier(…)`.
4. **KF-ES-5's focus destination** after the preset-pick/keyboard remount, and whether any AT announces the torn-out copy region.
5. **KF-ES-13's rendered outcome** — dev overlay, blank panel, or partial subtree when the render throws on `steps(1, jump-none)`.
6. **KF-ES-14/D-10's mobile half** — gallery occlusion at 390 px, peek vs expanded, under the live-behind no-overlay Drawer (the weakened premise noted in the roster).
7. **KF-ES-21's tier legibility** — the triple glass stack; forced-colors is OUT of this question (D-17 dead).
8. **KF-ES-8's contrast** — the uppercased 12 px `text-muted-foreground` caption over the `tier="quiet"` Card's composited plate (source-derived ratios would be manufactured — both readers correctly refused).
9. **KF-ES-25's magnitude** — the sweep-phase teleport on a mid-play duration drag.

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c`, whose EasingSidebar chain is byte-identical to `origin/master` — every row frontier-durable by identity; the only divergent cited files are the sibling panel (worktree lacks master's EE-02 twin) and package.json (the SCH-1 phantom, cured at the frontier). 34 roster rows — 2 BLOCKER · 12 MAJOR · 13 MINOR · 7 INFO, with 8 folds by reference (KF-ET-6, KF-CO-17, KF-CO-2, KF-ET-1, KF-ET-20, KF-ET-5, SCH-1/F-1, KF-CB-8) and zero re-bookings. 10 kills sustained — the decisive two ruled at this seat's own instruments: the smooth-step-3 POLARITY REVERSAL (the caption is true, the widget exhibits a curve 0.111 off the selection, and the corpus-unanimous merge-the-catalogues cure is refuted — KF-ET-6's owner question now has a measured answer) and the WRITE-THROUGH CURE-KILL (the model cannot seat a preset display — the remount machinery all three axes sentenced to deletion is partially load-bearing, and the honest repair is a split seam). The component is the best-postured consumer seat in the demo tree — zero bespoke controls, zero tokens, the right container, the right clock refusal, the right delivery channel — wearing three defective predicates: an echo filter that compares against the wrong value, a steps promotion gated on the predicate that makes it unreachable, and a seed gate keyed to the wrong catalogue's superset. Routings are M-25 agglomeration input: behavioral rows NO-WAVE-OWNER for SS-1/SS-2 (the seat composable + the literal/steps semantics are ONE spec with the banked KF-ET-1/KF-ET-6 cure-locks); producer rows GLASS-OWNED on the BH relay (the viewport-gated grid, the steps(1, jump-none) render throw, the preset seam, the slider readout seam); structural folds to KF.W0/W4/W5/W6; 9 visual rows to SS-13/KF.W9. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.
