Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequenceAxis; receipt read from this seat's own system prompt, not inherited).

# kf-SequenceAxis — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-12 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (2026-07-28), `origin/master` `81a56990` · **Registry tree** value.js `tranche-u` · **Corpus**: three challenge axes (`audit/kf-components/SequenceAxis/challenge-{D-design,L-library,C-consumption}.md` — D: 13 defects/4 superlatives · L: 12 defects/1 BLOCKER/5 superlatives · C: 7 defects/0 blockers/4 superlatives) re-read whole by this seat, plus two fresh hostile re-reader returns consumed whole: **reader-DU** (design-and-usage: 21 confirmed · 12 kills · 5 misses · 8 superlatives · 6 unproven) and **reader-LC** (library-and-code: 21 confirmed · 5 kills · 8 misses · 8 superlatives · 6 unproven).

**Sibling authority (identity-guard source).** `kf-SequencePlayhead.md` r3 (2026-08-11, this registry) is the banked scene-level home for the sequence cluster: D-1/L-1/C-1 (the 12px origin split + the ruling-1 cure `gap: 0.5rem var(--col-gap)`), N-1 (two-clock domain error), N-2 (stale `_duration`), N-4 (traveller ±12.8px), N-5 (`--stagger-max` dead write), N-16 (five labels over four rules), D-2/L-4/C-5 (the `top: calc(0.75rem + 1.25rem)` hardcode), D-10/L-2 (the four-side padding transcription), D-13/L-12 (`grid-column: unset`), D-16 (forced-colors), D-15 (RTL), K-6 (the ~2.26px diamond incursion). Its own identity guard names this adjudication and requires the folds. **Eleven of this corpus's rows fold there by reference; none is re-booked.**

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every load-bearing cell was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SequenceAxis.vue (49 L), useSequenceInstrument.ts (45 L), the three challenge files; SequenceTarget.css:30-120/:180-259; SequenceTarget.vue:1-30/:54-110/:140-160/:190-200; SequencePlayhead.vue:8-35/:50-70; design-idioms.css:170-215/:248-262; useSequenceDemo.ts:59-75/:99-131; git runs — `log --follow` on the subject (**two** commits: `4686aa46` 2026-06-17, `f3b5b7dc` 2026-06-24), per-file last-commit census over `demo/scenes/sequence/` (**two** files untouched since `f3b5b7dc`: SequenceAxis.vue AND useSequenceInstrument.ts), `git show 74ee9d27` (the splice check: `gap: 0.5rem 0` at added-line 69 inside `.seq-stage`; `column-gap: var(--col-gap)` at added-line 98 inside `.seq-row`); installed glass-ui 7.0.0 — `styles/theme/bridges.css` (`@theme inline`, `--color-muted-foreground: var(--muted-foreground)`), `styles/glass/ladder.css` (`:where(.glass-card, .glass-resting, .glass-quiet, .glass-wash) { --muted-foreground: var(--on-glass-muted) }`; `.glass-resting { border: 1px solid var(--glass-border-accent) }`; the `@container style(--glass-backdrop: light)` and `contrast-color()` arms), `tokens/on-glass-fg.css` + `tokens/color-radius.css` + `tokens/dark-arm.css` (token values verbatim), Surface material default `elevated` + `resolveSurfaceClass` → `glass-${tier}`, all four `forced-colors` files, `typography/utilities.css` (text-mono-caption verbatim; text-mono-small/micro present); a node WCAG run (my own arithmetic): on-glass-muted **6.292** light / **10.234** dark; neutral-5 5.209/7.698; border 1.938/2.823; greps — `--tick-p` (exactly 2), `--stagger-max` (exactly 1, the write), `--col-gap` (exactly 3), `forced-colors` demo (0), `max-width: *1023px` (16, classified 10 at-rules / 3 comments / 3 JS), scene `pointer-events` census, the scene custom-property census (**16** authored, 1 `--seq-*`), `1600` in scene (1), type-token overrides (0), test/scripts `SequenceAxis|seq-axis|quarters` (0); docs — sequence.md:116-120 + :210-214 (the namespace line lives at :118-119), DESIGN.md:80-92, layout.css:30-36 (`--graph-pitch`/`--graph-major` tokenized, unconsumed), font-roles.json `_monoContract` verbatim, S/audit/pass1/design/sequence.md:70-85 (the collision claim sits under "### Mobile (375px shot)"), DIGEST refinement (the unapplied container-query remedy), J.W7c-impl.md C-SEQ-2 charter row, `.stage-cell` fixed/inset-0 block.

## Scoreboard

**21 roster rows booked here — 1 MAJOR · 12 MINOR · 8 INFO — plus 11 folds-by-identity to the banked scene home (no row). Killed-claims register: 17 entries. 9 superlatives (one of the corpus's own killed at the bytes). 7 UNPROVEN residues → SS-13. Net-new from the 13 filed misses: 5 rows (SA-1..SA-5) + 2 limbs/riders; 5 misses booking-killed as pre-banked.**

**Headline.** Three results. (1) **The contrast superlative died at the token, and both a challenge axis and a re-reader inherited the same wrong chain**: D's S-D computed the tick labels from `--neutral-5`, and reader-DU "recomputed it independently and matched to two decimal places" — but the axis sits inside a default-material `<Card>` that renders `glass-resting`, the ladder rebinds `--muted-foreground` to `--on-glass-muted` inside it, and the `@theme inline` bridge makes `text-muted-foreground` late-binding, so the true figures are **6.29:1 light / 10.23:1 dark** (this seat's own arithmetic, agreeing with reader-LC). The conclusion (AA both themes) survives *strengthened*; the cell, the token, and the "+0.39 thin margin" headline are dead — and matching a wrong number to two decimal places is the citation-inheritance signature, one event per reader in this corpus (see kills 8 and 9). (2) **The registry's recurring lesson fired again**: reader-DU's headline miss — the ruler names 1600ms while the master clock runs 1940ms — is banked N-1 at kf-SequencePlayhead, one directory away, with N-2 beside it; five of the thirteen filed misses were pre-banked. The readers re-derive brilliantly and do not read the bank. (3) One genuinely new coupling was admitted where nobody had it: **the banked ruling-1 geometry cure moves the `0` tick's left edge onto the playhead diamond's centre at p=0** (this seat's derivation over the readers' figures), so the origin repair and the head/label collision must land as one commit — a rider now attached to the banked D-1 row.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census taxonomy (KF.W0/W6/W7/W9) with **NO-WAVE-OWNER** for the sequence scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **The label-contrast chain: reader-DU superlative ("D S-D verified, 4.89/7.30 from `--neutral-5`, matched to 2 d.p.") vs reader-LC kill ("operative token is `--on-glass-muted`; 6.29/10.24")** | **LC WINS — the superlative cell is KILLED; conclusion survives strengthened** | Every link re-verified at the installed 7.0.0: `@theme inline` suppresses `--color-*` freezing so `text-muted-foreground` → `color: var(--muted-foreground)` per element; `ladder.css` `:where(.glass-card, .glass-resting, …)` rebinds it to `--on-glass-muted` (hsl(30 26% 35%) light / hsl(34 16% 72%) dark); SequenceTarget.vue:8 `<Card :shadow="false">` passes no `material` → Surface default `elevated` → `resolveSurfaceClass` → `glass-resting`. My node run: **6.292 light / 10.234 dark** vs bare bg (LC's 6.29/10.24 ✓; DU's 4.89/5.21/7.30/7.70 reproduce only the wrong chain). `--border` is NOT ladder-overridden — D-4's hairline figures (1.938/2.823, my run) stand. Two residual movers stay live (§UNPROVEN): the `@container style(--glass-backdrop: light)` arm (zero demo writes of `--glass-backdrop` — my grep — so only a glass-floating/overlay ancestor could set it) and the `@supports (color: contrast-color(white))` arm (UA-dependent). |
| 2 | **D-3's S-corpus contradiction: DU confirms "375px clears, S's figure is FALSE by ~1.2px" (refined ~365px via the letter-spacing ink term) vs LC kills the refutation (unmodelled terms; S is shot-witnessed)** | **LC WINS — the refutation limb is NOT ESTABLISHED; D-3's substance stays MAJOR; crossover → SS-13** | The unmodelled term is real: `.glass-resting` carries `border: 1px solid var(--glass-border-accent)` (ladder.css, read) — 2px of width DU's chain never subtracts, collapsing the 375px margin to ~+0.8px before font-fallback variance; and the S claim sits under the section header "### Mobile (375px shot) — the weak axis" with a measured "~230px track" (S/design/sequence.md:79-83, read) — an observation register a sub-pixel arithmetic margin cannot overturn. What IS proven regardless (both readers + this seat): the `A < 6w` threshold, `white-space: nowrap`, zero mitigation anywhere (the sole @media sets height/margin only; git: both commits pre-date S), and the S-filed container-query remedy unapplied (DIGEST refinement verbatim). |
| 3 | **The geometry cure: both readers derive the subgrid-gutter third origin (~3.625rem), kill C-1's dichotomy and the naive one-line cures, and demand the three-rect probe "MUST run before any cure"** | **Kills ADMITTED as refinement; the imperative SUPERSEDED — the banked ruling-1 cure is branch-independent** | The dichotomy kill is sound (verified: `.seq-stage` gap column 0 at :44; `.seq-row` subgrid `column-gap: var(--col-gap)` at :73; two subgrid levels; under gutter-centring BOTH sub-units are ~6px wrong in opposite directions — an outcome C-1's falsifier table omits). But kf-SequencePlayhead ruling 1 already homes the repair at `SequenceTarget.css:44` (`gap: 0.5rem var(--col-gap)`), under which the parent and subgrid gutters equalize and **no redistribution exists to be ambiguous about** — the probe is a witness (SS-13 #1), not a gate. Both readers' parent-gutter cures (DU's kill-entry prescription; LC's missed-1) ≡ the banked repair — recorded as its 2nd and 3rd independent derivations. LC's variant (delete `--track-inset` + the row override) noted as an optional simplification behind the banked minimal form. |
| 4 | **D-2's grade: DU kills MAJOR (the two-horn double-booking with D-4) vs LC confirms MAJOR** | **MINOR — DU wins** | The construction is real (verified: `.stage-field-x` on `.seq-axis` itself, :6; bands at 0/25/50/75%; interior ticks centred on the same fractions, :30-31; no background/offset/shadow in the full ruleset :27-41) — but the same physical fact cannot be MAJOR twice on opposite horns: at 1.938:1 (my run) the hairline sits at D-4's quiet horn, which D itself concedes ("the faintest element on screen"). One tension, two MINOR rows (D-2, D-4), the horn decided by paint → SS-13 #3. |
| 5 | **Grade contests: L-2 MAJOR (DU) vs MINOR (LC) · L-4/D-6 MAJOR (LC) vs MINOR (DU + bank) · L-5/C-7 MAJOR (both) · D-5/L-6 INFO (DU) vs MINOR (LC)** | **MINOR · MINOR-as-banked · MINOR-as-rider · INFO** | L-2: one caller, one frozen legal 5-tuple, no live defect — contract laxity (LC's demotion adopted; the false-generality mechanism confirmed: `calc(100% / 4)` literal in a shared idiom with SpringTarget consumers ×2, my read). L-4/D-6 folds to the banked D-2/L-4/C-5 row, which carries MINOR — grade-stability (severity anti-rename). L-5/C-7's MAJOR weight belongs to banked F-1, not to the new content (the census-legend refinement) — rider at MINOR. D-5/L-6: every party disclaims visible ink; the 2.4px overhang dies in the 0.5rem row-gap; DU's INFO adopted, LC's MINOR preserved as dissent. |
| 6 | **The exclusivity enumeration: DU kills L-1's "only file not modified" (two files) vs LC's missed-5 cell re-asserting it from mtimes** | **DU WINS — verified by git; LC's cell killed** | My per-file census: SequenceAxis.vue AND useSequenceInstrument.ts both last touched `f3b5b7dc` (2026-06-24), both pre-dating 74ee9d27 (2026-07-04). Filesystem mtimes are not provenance. The drift thesis itself survives untouched — the newest of the axis's two commits is still ten days before the geometry tokens were born. |
| 7 | **Both readers' "sole forced-colors rule" cells (DU: accessibility.css; LC: glass/a11y-fallback.css)** | **BOTH CELLS KILLED — four files; D-11's verdict unchanged, one correction to DU** | My grep: `forced-colors` rules live in **four** dist files (components.css, accessibility.css, glass/a11y-fallback.css, utilities/a11y-overrides.css). A "sole" claim is a claim about a scope (the ColorSpaceSelector L-9.1 law). Correction: a11y-fallback DOES reach this scene's chrome — `.glass-resting` gets a CanvasText border — so DU's "nothing here matches" is false at the Card; the axis's own graphic layer (a background-image gradient, removed in forced colors) remains unprovisioned. INFO, fold ≡ banked D-16 → KF.W9. |
| 8 | **Booking posture of the 13 filed misses** | **5 BOOKING KILLS · 5 admitted rows · 2 limbs/riders · 1 fold-into-row** | Pre-banked (≡, no row, independent confirmations recorded): DU-m1 two-clock (+ its `reseatRow` corollary) ≡ **N-1/N-2**; LC-m1 `--track-inset`-is-the-fault ≡ the **D-1 ruling-1 repair**; LC-m2 `--stagger-max` ≡ **N-5**; LC-m3 four-side padding transcription ≡ **D-10/L-2**; LC-m8 `grid-column: unset` ≡ **D-13/L-12**; also both readers' C-6 confirmations ≡ **N-4**, and DU-m3's vertical incursion ≡ **K-6**/SS-13 #4 (its horizontal-overlap + cure-coupling content admitted as the D-1 rider). Admitted: SA-1..SA-5 below. DU-m5 folds into the D-10/L-11 row as its corrected scene census (kill 13). LC-m5's outward-reach limb folds into L-3 as limb (b). |
| 9 | **DU's kill of D-10's citation/evidence vs LC's confirmation cell repeating D-10's support** | **DU's kill VERIFIED and RE-FIRED against LC — the corpus's second inheritance-class event** | The `--seq-*` convention line is sequence.md:118-119 (:210-214 is "Recess the timeline" — my read); and `--ball-p` carries no `--seq-` prefix, so D-10's "the sibling honours it" refutes itself. LC's cell repeats both the wrong cite ("D-10 cites :212; the actual line is 118-119 — citation off, claim right") and then **re-asserts the refuted support verbatim** ("the sibling honours it (`@property --ball-p` …)"). The finding survives at scene level via the corrected census (1 of 16). |

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the sequence scene-repair packet). Anchors bind to worktree = `origin/master` current for this scene; re-resolve before any cure.

### MAJOR

- **D-3 (substance)** — **the `1200`/`1600` labels collide at narrow widths and the tree carries no mitigation of any kind.** Threshold `A < 6w` proven from `nowrap` (:33) + centred 0.75 tick (:31) + right-hugged terminal (:39-41); `w = 2.8em` from the caps-tracking rung (D-7's term feeds this); zero container query / min-width / label thinning / font step anywhere (sole @media :43-47 sets height+margin only); the S-filed remedy ("hide the 0.25/0.75 labels, keep 0/800/1600") unapplied — git shows both of the file's commits pre-date S. **The S-contradiction limb is killed (ruling 2); the exact crossover (DU ~369px → ~365px refined) is UNPROVEN → SS-13 #2.** Cure shape: the S container query, correctly thresholded on the CARD's width (the container idiom is already native to the scene — `.seq-track`/`.seq-playhead-track` both declare `container-type: inline-size`), landing with D-9. **NO-WAVE-OWNER (ruler-legibility item of the packet).**

### MINOR

- **D-2** *(grade ruled 4)* — the graduations are painted through the middle of the numerals that name them: `.stage-field-x` bands at 0/25/50/75% of the axis's own 1.1rem box; interior ticks centred on the identical fractions; no background/offset/halo in the tick's full ruleset. One tension with D-4 (the horn is a paint question). Cure shape (DU's, sound): move the rules to a bottom-anchored short tick band, which also delivers the two-pitch law. **NO-WAVE-OWNER (one item with D-4).**
- **D-4** — the graduations read ~1.94:1 light / ~2.82:1 dark bare (my arithmetic; washed-top slightly lower), and `.stage-field-x` ships ONE pitch where DESIGN.md:82-83 states the law of two ("fine and major pitches are mixed") — with the two-pitch vocabulary **already tokenized and unconsumed** (`--graph-pitch: 1rem` / `--graph-major: 5rem` / `--graph-major-opacity: 11%`, layout.css:32-35, my read; `graph-major` absent from the scene). No 1.4.11 claim (the numerals duplicate the rules — correctly declined by all). Note `.stage-field-x` is multi-consumer (SpringTarget :63/:141) — the cure edits a shared idiom. **NO-WAVE-OWNER (with D-2; cross-scene note).**
- **D-7** — a CAPS utility on pure numerals: `text-transform: uppercase` inert; 0.1em caps tracking widens every label ~10% (feeding D-3's threshold); the trailing letter-space biases `-50%` centring ~0.05em left (stacking with, not cancelling, the D-1 origin error); the quieter rungs `text-mono-small`/`text-mono-micro` sit in the same producer file (verified present). **THE TRAP, preserved from both readers**: sub-claim 4's "`tabular-nums` is redundant" must never be actioned — font-roles.json `_monoContract` clause (b) is the sole clause licensing this mono call-site (verbatim re-read), and deleting the token reds the census. **NO-WAVE-OWNER.**
- **D-8a / L-9 / C-5** — the ruler states no unit: bare integers where its authorizing charter (J.W7c C-SEQ-2, verbatim: "quarter labels = `q × STAGGER_MAX` ms") and its own :5 comment both say milliseconds; the nearest rendered "ms" is a different column. Couples with L-10: a `ms` legend added inside the strip trips the positional edge-hug. **NO-WAVE-OWNER (one commit with L-10).**
- **D-9** — `@media (max-width: 1023px)` at :43 is a raw viewport literal against DESIGN.md:90-91's law, and the wrong query family for D-3's width problem (the card's width, not the viewport's, is what compresses). Register corrected by this seat (kill 15): 16 sites = **10 live at-rules / 3 prose comments / 3 JS `useMediaQuery` strings** — three different cure families; a wave scoped to "16 sites" will find six that are not tokenisable CSS. T-lane F1 + T.F born-RED gate filed and unapplied (both readers concordant). **NO-WAVE-OWNER (breakpoint-token family; the T.F gate is the packet's input).**
- **L-2** *(grade ruled 5)* — `quarters` is false generality: the tick count is a prop while the rules are hardcoded `calc(100% / 4)` in a shared multi-consumer idiom; `[0, 0.2, 0.4, 0.6, 0.8, 1]` is type-valid and renders six labels over four unrelated rules with no error at any layer. LC's cure (b) — publish `--tick-count: {{ quarters.length }}`, gradient `calc(100% / var(--tick-count, 4))` — is structurally sound, keeps SpringTarget at 4 via the fallback, **and closes banked N-16's axis half in the same edit**; the dead `--stagger-max` write (banked N-5) is the vestigial hook for exactly this cure. **NO-WAVE-OWNER (with the N-16 fold).**
- **L-3 / C-3 / D-6b (+ LC-m5 as limb b)** — **the encapsulation seam is bidirectional and undeclared.** (a) Inward: the parent's scoped stylesheet animates `.seq-axis` — declared only at :21 — for the boot wipe AND its MANDATORY PRM degrade (SequenceTarget.css:221-223/:238-243), surviving solely on Vue's parent-scope-id-on-child-root behaviour; any re-root silently kills wipe and guard together (symmetric failure → MINOR, C's grade, over L's MAJOR). (b) Outward: the child's `grid-column: 2` (:22) is an assertion about the parent's track list authored where the parent cannot maintain it — and it is the declaration that actually drifted (git: the axis pre-dates the geometry tokens by ten days). Mechanism ≡ banked N-8's seam posture; the owning-side home (hoist the placement into SequenceTarget.css, which already styles this element) is the packet-spec input. LC-m5's "only file" support killed (ruling 6). **NO-WAVE-OWNER.**
- **L-8 / C-4** — `readonly number[]` + bare `number` are wider than the stylesheet's unstated invariants (sorted, endpoint-anchored, unique, in [0,1]; `staggerMax` collapse at small scales). No live defect (frozen `as const` 5-tuple, module-constant 1600). value.js `clamp` is already imported one file over (SequencePlayhead.vue:15, verified) — the cheapest guard. **NO-WAVE-OWNER.**
- **L-10** — `:first-child`/`:last-child` are positional against ALL children, not ticks; any added sibling (including D-8a's own cure) silently voids the edge-hug; `:nth-child(1 of .seq-axis-tick)` is the robust form. **NO-WAVE-OWNER (with D-8a).**
- **L-5 / C-7 (as rider; grade ruled 5)** — the census's `b` legend ("no glass-ui import") is import-scoped and jointly misleading for cascade-only consumers: this zero-import file's entire visual identity (utility, tokens, hairline ink) resolves through the undeclared glass-ui — verified: `text-mono-caption` is an `@utility` inside node_modules; `--border`/`--muted-foreground` vendor-only; zero demo definitions. **The F-1 substrate folds ≡ census F-1/SCH-1 (never re-booked); the NEW content — a second census column separating import-coupled from cascade-coupled before F-1's remediation scopes its regression surface — routes to KF.W0 §B-12 as a legend rider.**
- **SA-1** *(NET-NEW — DU-m2, verified)* — **the ruler carries no typographic subordination to the data it frames, and is in fact louder.** The tick's class triple is byte-identical to the row label's (`text-mono-caption text-muted-foreground tabular-nums` — Axis :10 vs Target :85, both read), and the rows' `@ms` sub-line is explicitly dampened (`.seq-row-at { font-weight: 400; opacity: 0.8 }`, css:100-103) while the ticks ride full weight/opacity — the instrument's scale sits ABOVE its data in the hierarchy. One-token cure: the `text-mono-small`/`text-mono-micro` rungs in the same producer file. All three axes examined the type in isolation; none compared it to the labels one column left. **NO-WAVE-OWNER (with D-7).**
- **SA-2** *(NET-NEW — LC-m4, verified)* — **`.seq-axis` is the scene's only decorative overlay without `pointer-events: none`.** Census (mine): `.seq-handle::after` (css:150), `.seq-playhead-track` (:30), `.seq-playhead::after` (:85), `.progress-rail` (:176), `.progress-ball` (:186) all declare it; the axis and its ticks declare neither it nor `user-select`, leaving five caret-selectable spans floating over a drag-first stage. Compounds banked N-12: a grab at the diamond (the inert grab cue) lands on the axis band and can start a text selection. **NO-WAVE-OWNER (with the N-12 interaction spec).**

### INFO

- **D-5 / L-6** *(grade ruled 5; LC's MINOR as dissent)* — fixed `line-height: 1.1rem` against fluid `--type-caption` (ratio 1.45→1.10 across the range); the ≤1023px block compresses the box to 0.95rem leaving the 17.6px line box (2.4px overhang, absorbed by the row-gap; digits ~1.2px below optical centre of the compressed strip — DU's derivation, the only real consequence). Three raw literals in a stylesheet defining zero tokens, against the scene's own φ-ladder precedent (css:95-103, read). Listed so SS-13 does not re-litigate. **NO-WAVE-OWNER (hygiene).**
- **D-10 / L-11 (+ DU-m5 as corrected census)** — `--tick-p` unprefixed, unregistered, dead `, 0` fallback (inline style always supplies; exactly one write + one read, my grep). Scene-level truth (this seat's census): the scene authors **sixteen** custom properties and exactly ONE (`--seq-glow`) carries the stated `--seq-*` prefix — pinning the convention on this file's 1 is arbitrary; the exposure is the scene's at 15/16. Zero collisions against the installed glass tree (readers' probes, concordant). D-10's citation/evidence killed (ruling 9). **NO-WAVE-OWNER (scene token-hygiene row).**
- **D-12** — `aria-hidden="true"` is defensible, recorded as a non-defect: no focusable descendants; the domain independently exposed by the five sliders' min/now/max triple (Target :99-104, read). **Relay to the forthcoming kf-SequenceTarget corpus** (out of this axis's scope, TRUE at the bytes): `text-mono-caption` on the row label uppercases the child span, so `@800ms` renders `@800MS` while the accessible name stays lowercase; and the sliders lack `aria-valuetext` (unit never announced — concordant with banked N-14's class). **KF.W9 datum + relay.**
- **D-13** — three tranche identifiers in a 49-line file's comments (:2, :2-3, :42) against the filed T.F19 provenance sweep (unapplied); zero user-facing copy to judge (the absence IS D-8a). **NO-WAVE-OWNER (prose sweep).**
- **L-12** — zero coverage: no test mounts this SFC (grep test/+scripts: 0); the sequence suite is composable-only; the visual gate is desktop-only. This file is the cheapest mount in the folder (zero imports/injections/lifecycle). The packet's born-RED assertions (banked N-10) gain the axis pair: three-rect equality AND terminal-label = `sequence.duration`. **NO-WAVE-OWNER (test obligation, with N-10).**
- **SA-3** *(NET-NEW — DU-m4, verified)* — the component's only motion violates the scene's own compositor doctrine: `seq-ruler-wipe` interpolates `clip-path` (+opacity) (css:227-230, read) in a file that states the translateX-only law three times (css:111-113, :189-192; Playhead :32-35). Compositor-clean equivalents exist (`scaleX` reveal / mask sweep). INFO, not perf: 420ms one-shot on a 17.6px strip. Filed because all three axes audited the wipe's PRM guard and none its implementation. **NO-WAVE-OWNER (with the packet's motion spec).**
- **SA-4** *(NET-NEW — LC-m6, verified)* — two access idioms for one kind of value: `ROW_COUNT` imported directly (Target :143) while `STAGGER_MAX` threads injection→prop to reach a template literal; the prop is one of the two lax surfaces L-8 flags. **This seat's qualification**: the prop shape is ALSO what keeps the leaf import-free and mountable in isolation (the S-A/render-once superlatives ride it) — the inconsistency is the finding; the direct-import cure is not obviously right. **NO-WAVE-OWNER (spec framing).**
- **SA-5** *(NET-NEW — LC-m7, limb-corrected: kill 14)* — `powerOn`'s 760ms `window.setTimeout` retains no handle, is never cleared, and has no `onScopeDispose` (useSequenceInstrument.ts, whole read): leaving the scene inside 760ms leaves a live timer writing an orphaned ref. The "keep-alive re-entry never replays" limb is KILLED — the doc comment declares "fired ONCE on first entry … guarded against re-fire" (documented intent, not drift). Cross-ref banked N-17 (the 760 vs 780ms truncation lives in the same six lines). **NO-WAVE-OWNER (hygiene, with N-17).**

### Folds-by-identity (≡ banked rows at kf-SequencePlayhead r3 — no row here, confirmations recorded)

- **D-1 / L-1 / C-1** (the 12px origin split) ≡ **banked D-1**, MAJOR, with the ruling-1 cure (`gap: 0.5rem var(--col-gap)`) — this corpus contributes: both readers' independent derivations of the parent-gutter repair (2nd/3rd), the C-1-dichotomy kill (a real refinement of the branch analysis), **and the NEW cure-coupling rider (from DU-m3, extended by this seat): after the ruling-1 cure, the axis's `0` tick left edge lands at 4rem = the diamond head's centre at p=0, widening the head/label overlap to ~4-5.7px — the origin repair and the collision cure are one commit. Witness → SS-13 #4.**
- **DU-m1 (+ corollary)** ≡ **N-1 / N-2** (two-clock domain; stale `_duration`) — booking-killed; DU's arithmetic (800 vs 970 at midtrack; 65% vs 53.6% gate arrival; the reseatRow 36% escalation) is concordant with the banked rows and recorded as another independent derivation of each.
- **D-8b / L-7 / C-2** (five labels over four rules) ≡ **N-16** — the axis-half of that row; L-2's `--tick-count` cure closes it.
- **C-6** (traveller ±12.8px off its gate; both readers confirmed) ≡ **N-4**; DU's kill of C-6's reconciliation claim admitted (the mappings differ in DOMAIN, not just origin — the N-1 mechanism speaking).
- **D-6 / L-4** (the `top: calc(0.75rem + 1.25rem)` literal + 0.3rem mobile drift + row-gap omission) ≡ **banked D-2/L-4/C-5**, MINOR stands (LC's MAJOR dies, ruling 5).
- **LC-m3** (all four padding sides transcribed) ≡ **banked D-10/L-2**. **LC-m2** (`--stagger-max` dead write) ≡ **N-5**. **LC-m8** (`grid-column: unset` dead) ≡ **banked D-13/L-12**. **LC-m1** (`--track-inset` presumes an undeclared gutter) ≡ the **banked D-1 repair's root-cause statement**.
- **D-11** (forced-colors) ≡ **banked D-16** → KF.W9, with ruling 7's four-file correction.
- **The F-1 limb** everywhere it appears ≡ **census F-1/SCH-1** → KF.W0 §B-12.

## Killed-claims register — 17 entries

1. **L-1 "BLOCKER"** — grade kill (both readers + the banked MAJOR concordant): aria-hidden, non-interactive, zero AT loss, 12px static on decorative chrome; L's own §0 concedes the unit's quality. MAJOR at the banked row.
2. **L-1's git transcript** (one commit shown as verbatim shell capture) — the real output is TWO commits (`f3b5b7dc`, `4686aa46`); my run. DU's kill verified.
3. **L-1's exclusivity claim** ("the only file not modified after the geometry rewrite") — TWO files last touched `f3b5b7dc` (SequenceAxis.vue, useSequenceInstrument.ts); my per-file census. **Re-fired against reader-LC's missed-5 cell**, which re-asserts it from filesystem mtimes.
4. **L-1/L-5's `git show 74ee9d27` block** — a `.seq-row` declaration (`column-gap: var(--col-gap)`, added-line 98) spliced into the `.seq-stage` hunk (which ends `gap: 0.5rem 0`, added-line 69); verified at the diff. Self-defeating as printed: were the spliced line real, it would kill L-1. Both readers concordant.
5. **The naive one-line cures (L-1's `margin-left`; D's `left: var(--track-inset)`) and C-1's "exactly one of the two is off" dichotomy** — killed by both readers via the subgrid-gutter centring derivation (a third origin at ~3.625rem, both sub-units wrong ~6px opposite ways); this seat re-verified the two-level subgrid structure. **The readers' "probe MUST run before any cure" imperative is itself killed** — the banked ruling-1 parent-gutter cure is sound under every branch (ruling 3).
6. **D-2 MAJOR** — the two-horn double-booking: D-2 grades the loud horn MAJOR while D-4 grades the same fact's quiet horn MINOR; at 1.94:1 the tree is on the quiet horn. MINOR (ruling 4). LC's MAJOR dies as a severity kill.
7. **D-3's refutation of the S corpus** ("375px clears; S's figure is FALSE") — NOT ESTABLISHED: the +1.2px margin is smaller than the unmodelled `.glass-resting` 2px border term (verified in ladder.css) and the font-fallback variance; S's claim sits under a "375px shot" section header with a measured track figure. DU's confirmation of the limb and its ~365px ink refinement demoted to UNPROVEN (SS-13 #2). D-3's substance unharmed.
8. **D's S-D superlative and reader-DU's verification of it** ("token-derived contrast … 4.89:1 light, thin +0.39 margin … recomputed independently, matched to two decimal places") — KILLED at the token (ruling 1): the operative foreground inside the default-material Card is `--on-glass-muted` via the ladder rebind; true figures 6.29/10.23 (this seat's arithmetic). **Citation-inheritance event #1**: DU re-ran the arithmetic and inherited the resolution chain — identical-to-wrong is not verification.
9. **D-10's convention citation + supporting evidence, and reader-LC's cell repeating it** — the `--seq-*` line is sequence.md:118-119 (not :212), and `--ball-p` is NOT `--seq-*`-prefixed: the cited "sibling honours it" example refutes the claim it supports. LC's confirmation cell corrects the cite and then repeats the refuted support verbatim — **citation-inheritance event #2**, one per reader in this corpus.
10. **C-6's reconciliation claim** ("Reconciling C-1 fixes 1↔2") — the axis labels ride ×1600 while the playhead rides progress over 1940 (banked N-1): align every origin and the ruler still mislabels the playhead by up to 340ms. DU's kill adopted; C-6's own rect-only probe cannot detect it (needs `demo.sequence.duration` beside the rects).
11. **C-1's §LABEL-subgrid cite** — `column-gap: 0.75rem` is design-idioms.css:259 (rule :255-261; comment opens :250); verified. Provenance correction only — the idiom-inversion reading (the pivot of the endorsed cure) is exactly right.
12. **Both readers' "sole forced-colors rule" cells** — four dist files carry the media query (my grep); DU's "nothing here matches" additionally false at the Card chrome (a11y-fallback borders the glass ladder). D-11's axis-level verdict unchanged (ruling 7).
13. **Reader-DU MISS-5's "fourteen custom properties … exactly ONE prefixed"** — the scene authors **sixteen**: DU's own enumeration omits `--stagger-max` (Target.vue:63 — the dead write the registry banked as N-5) and `--ball-glow` (Scrubber.vue:149). Ratio 1/16; the substance (arbitrary to pin `--tick-p`) strengthens; the cell is corrected.
14. **Reader-LC missed-7's "keep-alive re-entry never replays the wipe" as defect** — the composable's doc comment declares "fired ONCE on first entry … guarded against re-fire" (read whole): documented intent. The uncancelled-timer limb survives as SA-5.
15. **Reader-DU's D-9 register arithmetic** ("NINE live CSS … ten counting both"; "FOUR are prose" listing three) — my classification: **10 live at-rules / 3 prose comments / 3 JS strings = 16**. The three-cure-families point survives; the counts are corrected.
16. **BOOKING KILLS ×5 (+2 partials)** — DU-m1 ≡ N-1/N-2 · LC-m1 ≡ the D-1 repair · LC-m2 ≡ N-5 · LC-m3 ≡ D-10/L-2 · LC-m8 ≡ D-13/L-12; partials: DU-m3's vertical incursion ≡ K-6, both C-6 cells ≡ N-4. All filed as "missed/new" — true of the challenge axes, false of the registry (banked 2026-08-11, one directory away). The same lesson as the playhead r3's K-16: **the readers re-derive brilliantly and do not read the bank.** Each recorded as an independent confirmation.
17. **Severity kills (consolidated)** — L-2 MAJOR→MINOR · L-4/D-6 MAJOR→banked MINOR · L-5/C-7 MAJOR→MINOR-rider (the MAJOR weight is F-1's, already banked) · D-5/L-6 MINOR→INFO (LC's grade preserved as dissent). No new evidence accompanied any contested grade; grade-stability is anti-rename applied to severity.

## Superlatives (provenance preserved)

1. **The bespoke verdict is verified, twice independently** (D S-A; C §4.3; both readers re-confirmed): glass-ui 7.0.0 ships no graduated-scale primitive — `./axes` is a prop vocabulary, timeline geometry is segment math, ScrubberTimeline exposes `modelValue`/`label` only, ContinuousMarkers is interactive buttons. 49 lines / 2 props / zero imports is near the minimum honest ruler. Two axes checked the census claim instead of citing it.
2. **PRM honesty is complete and double-guarded** (D S-C; verified whole this session): the JS arm returns before `.is-powering-on` ever lands; the CSS arm covers PRM-enabled-after-mount; `both` fill means the killed animation leaves no residual (the `to` frame IS the settled frame). Now carrying SA-3's qualification: the guarded wipe is itself off the scene's compositor doctrine — guarded AND off-law.
3. **Derived, never transcribed** (CS-4; both readers): `Math.round(q * staggerMax)` threads the single `STAGGER_MAX` that also bounds `reseatRow` and the sliders' `aria-valuemax`; `grep 1600` → one hit tree-wide (my run). Correct in the value domain, wrong in the pixel domain — and (banked N-1's qualification) wrong in WHICH value domain to name; still the better failure than the reverse.
4. **The edge-hug pair is the correct minimal solution** — the corpus's only unanimous superlative (three axes + both readers + this seat): no single-expression substitute exists (`calc(var(--tick-p) * -100%)` gives −25% at q=0.25); the degenerate length-1 case is unreachable against the frozen tuple.
5. **A props-down leaf that renders exactly once inside a 60fps scene** (L S-A/S-B; LC's mechanism verified logically): the parent re-renders every frame, but both bindings are reference-stable module constants, so the props diff bails — one render for the life of the scene, zero teardown surface, and the only file in the folder that can say so. Exemplary inv-ζ reading: subscribing to `progress` to redraw static labels would be the misuse.
6. **`tabular-nums` is load-bearing legality, not decoration** (CS-3; both readers preserved it): `_monoContract` clause (b) is the sole license for this mono call-site, and D-7's own sub-claim recommends the exact deletion that would red the census — the corpus contains its trap and its disarm.
7. **`aria-hidden` is right on both counts at once** (D-12/CS-2/S-D, all independent): no focusable descendants (the violation not committed) AND the domain independently exposed (information not lost) — with the banked N-14 thinness qualification (no `aria-valuetext` anywhere in the scene).
8. **CORPUS superlative — C's §4 "Considered and rejected"** (both readers, concordant): five falsifiable non-findings published with their arithmetic, two of which D filed as defects — the published rejects are what let a three-way disagreement be adjudicated in one read. The practice this exercise exists to produce.
9. **CORPUS superlative — D-4's reproducible WCAG method** (LC's framing, adopted): I reproduce D's bare-background hairline pair to three significant figures from tokens alone (1.938/2.823). That the same method with the wrong foreground token produced S-D's dead cell is the strongest argument FOR the method — token-derived arithmetic is exactly what made the error findable, attributable, and correctable without a browser.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The three-rect probe** (`.seq-axis` / `.seq-track` / `.seq-playhead-track` lefts+widths, one mount) — settles the subgrid-gutter branch (4.25 / ~4.625 / 5rem) as a WITNESS; the ruling-1 cure is branch-independent (ruling 3). Decision table must include the both-wrong-by-6px outcome C-1 omitted. *(Carried ≡ banked SS-13 #1.)*
2. **D-3's collision at 360px and 375px** — paints at both widths, both themes, BEFORE any cure: decides the crossover (~352-369px band per font/border terms), reconciles the S shot, and closes kill 7's residue. The threshold and the zero-mitigation fact need no paint.
3. **The D-2/D-4 horn** — a colour-picker reading of the composited hairlines at the stage top decides whether the tree defaces its numerals or ships an ungraduated ruler. **Any label re-measure must use the corrected chain** (`--on-glass-muted` inside the Card; never `--neutral-5`), and should record whether the `--glass-backdrop` style-query or `contrast-color()` arms fire in this scene (zero demo writes of `--glass-backdrop` — my grep — so only a glass-floating/overlay ancestor could set it).
4. **The diamond-vs-terminal-label ink question** at p=0 and p=1, desktop + mobile — the box overlap is proven (~2.2px today; head 2.26px inside the ruler's box); the ink strike is not. **Re-shoot AFTER the ruling-1 cure: the `0` tick's left edge lands on the head's centre (this record's rider) — the before/after pair is the born-RED material for landing the geometry and collision cures together.**
5. **D-5/L-6's 2.4px overhang** — all parties disclaim visible ink; listed only so SS-13 does not re-litigate; the ~1.2px below-optical-centre offset is the one measurable residue.
6. **The wipe's main-thread cost** (SA-3) — a trace during boot would close the "trivially cheap" assumption; INFO either way.
7. **SA-2's selection witness** — a drag beginning on the axis band (e.g. at the inert diamond) at p=0: confirm caret selection engages over the ticks; pairs with banked SS-13 #9 (the N-12/N-13 compound).

## Identity guards (fold-by-reference — never re-booked)

- **D-1/L-1/C-1 ≡ banked D-1** at kf-SequencePlayhead (scene home; anti-rename) — this record contributes two cure derivations, the dichotomy kill, and the collision-coupling rider; the rider's witness is SS-13 #4.
- **DU-m1 ≡ N-1/N-2 · C-6 ≡ N-4 · LC-m2 ≡ N-5 · D-8b/L-7/C-2 ≡ N-16 · D-6/L-4 ≡ D-2/L-4/C-5 · LC-m3 ≡ D-10/L-2 · LC-m8 ≡ D-13/L-12 · D-11 ≡ D-16** — all at kf-SequencePlayhead r3; independent confirmations recorded there-wards, rows stay there.
- **The F-1 limb ≡ census F-1/SCH-1** → KF.W0 §B-12; this record adds the b-legend cascade-coupled column rider (L-5/C-7) and one more pattern-ledger datum: **two citation-inheritance events (kills 8, 9), one per reader — the fourth and fifth of their class in this scene's ledger.**
- **The `@800MS` render + missing `aria-valuetext` observations** relay to the forthcoming **kf-SequenceTarget** adjudication (out of axis scope, verified true).
- **L-12's test obligation ≡ banked N-10's list** — extended by the axis pair (three-rect equality; terminal label = `sequence.duration`).

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold (§B-12) + the b-legend cascade-coupled census column (L-5/C-7 rider). — **KF.W9**: the forced-colors fold (≡ D-16, four-file correction) · the D-12 datum + kf-SequenceTarget relay. — **KF.W6/W7**: no axis-owned rows (the material/export registers are untouched by this file).
- **NO-WAVE-OWNER — the sequence scene-repair packet (SS-1/SS-2 authoring must home it):** the **ruler-legibility trio D-3 · D-2 · D-4** (container-query label thinning; graduations off the label band onto a bottom tick set; the two-pitch law via the already-minted `--graph-*` tokens — one raster item, SpringTarget cross-scene note) · the **geometry commit rider** (the ruling-1 cure lands WITH the head/label collision cure — SS-13 #4's before/after is its gate) · the **seam row L-3/C-3/D-6b** (both directions; owning-side home per banked N-8) · **L-2 + the N-16 fold** (`--tick-count`, one edit closes both) · the **type rows D-7 + SA-1** (rung drop + subordination; the tabular-nums trap disarm stated in-spec) · **D-8a + L-10** (the `ms` legend and the selector hardening, one commit) · **D-9** (the breakpoint-token family, T.F's born-RED gate, corrected 10/3/3 register) · **SA-2** (pointer-events, with the N-12 interaction spec) · hygiene **D-5/L-6 · D-10/L-11 (scene token census 15/16) · D-13 · SA-3 · SA-4 · SA-5 (+N-17)** · the test obligation (L-12 ≡ N-10 extension).

## Verdict

**ADJUDICATED.** The axis corpus survives hostile re-reading the same way its sibling's did: every load-bearing geometric and token fact re-derived clean, while the grades, the cures, the "sole"s and the "only"s fell — a BLOCKER became the banked MAJOR, two MAJORs became MINORs, a superlative died at the token inside a default-material Card, and five "misses" were found already banked one directory away. This record books one MAJOR (the unmitigated narrow-width collision, its S-refutation limb killed), twelve MINORs, eight INFOs, admits five modest new rows (a ruler louder than its data, selectable text on a drag stage, an off-doctrine wipe, an idiom split, an uncancelled timer), and attaches one genuinely new rider to the banked geometry row: the cure that aligns the ruler parks its `0` under the playhead's diamond, so the repair and the collision fix are one commit or neither. The component remains what every reader found: a 49-line props-down leaf that renders once, hides itself from AT correctly, derives every number it shows — and sits 12px off the clock it names, in a frame whose real defects live one level up. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life; SA-1..SA-5 minted here for life.

— end of apotheosis. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3; the banked kf-SequencePlayhead r3 rows referenced here are untouched by this record.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/sequence/SequenceAxis.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SequenceAxis.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · `.d` measured, `.e` landed) — **D-11 (≡ banked D-16) · killed-claims #12 (ruling 7)**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here; **measured by seat `.d`**, which
was forbidden to land it. **Nothing above this line is rewritten** — no edit, no re-grade, no renumber, no
deletion, no new id (E-1 / E-3). Every bullet is a dated MEASUREMENT; none re-books a row or moves a severity.*

- **cell** — producer bytes read at glass **7.0.0**'s shipped dist — **read-only; zero glass-ui bytes written**
- **capture** — none — a file-level enumeration over the installed producer dist
- **stamp** — substrate keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD) · bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448` (built in the §0m.2 capture clone, **measured and double-run** at `.d` and **re-hashed identical at `.e`**)

- **Ruling 7's FOUR-FILE correction re-measured, and the figure is larger, not different in kind.** ⟨`find …/dist -name '*.css' | LC_ALL=C sort | xargs grep -l 'forced-colors'`⟩ → **TWELVE files**. **All four of the ruling's files are present and all four still resolve.**
- **The ruling's CONCLUSION is REINFORCED, not disturbed**: if no *"the sole forced-colors rule is X"* cell survived four files, none survives twelve. **The verdict is unchanged; only its denominator is.**
- Published as an **enumeration, not a numeral**, so the next census reproduces it rather than inheriting it. **No row is re-graded and no id is minted.**
