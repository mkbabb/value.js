# GallerySearchBar — adjudicated apotheosis (M-23/L-18, fresh-adjudicator seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt

**Seat**: fresh Fable adjudicator (the incredulity seat — both hostile re-readers were challenged back before either was adopted).
**Date**: 2026-08-25 · fourier HEAD `cd26c65` (`m/w1-bump-migration`) · producer pins: installed `@mkbabb/glass-ui` 4.0.0 (dist read at the bytes) + `git show v7.0.0` at `/Users/mkbabb/Programming/glass-ui` (the F.W1 baseline).
**Corpus**: challenge-D-design.md · challenge-L-library.md · challenge-C-consumption.md over `web/src/components/visualization/gallery/GallerySearchBar.vue` (223 lines), re-read by two hostile re-readers (design-and-usage = reader-A · library-and-code = reader-B), both returns adjudicated here.
**This seat's own instruments (all static + node + git-show; zero browser probes — probe parsimony; product source READ-ONLY)**: whole-file read of the subject (223 lines, every disputed anchor re-verified: :48-54, :55-63, :64, :65-74, :79-80, :86, :101, :119-124, :134-223); `GalleryView.vue` :43-58 / :90-120 / :205-245 (the `activeTab` ref at :43 = the view's OWN state — the L-5 reachability decider; the debounce watcher; the eight-line binding block; the `v-if="activeTab === 'gallery'"` at :229); `stores/gallery.ts` :30-42 / :60-100 (`resetAndFetch` sends `{limit: 20, sort, owner}` only — re-confirmed); `lib/api.ts` :49-64 / :102 / :161 / :397-411 (the shared `"listVisualizations"` abort key); `basis-display.ts` whole (7 lines); `e2e/gallery.spec.ts` :15-25 / :43-64 (the guard-wrapped sole assertion; `grep -rn glass-dock src/` → **0**); repo census `--ease-apple-spring` → 9 lines / 4 files + sole package hit README.md:211; aria-label counterexamples read (`ContourSettings.vue:211`, `SpeedSelect.vue:32`); `node` read of web/package.json (`lucide-vue-next` dep **undefined** / dev `^1.0.0`); **installed glass-ui 4.0.0 at the bytes**: `a11y-overrides.css:100-122` (the coarse `[data-size="icon"]` floor + the producer's own "only RAISES" prose — the K-1 decider), `glass/ladder.css:60-82` (`.glass-resting` three-leg box-shadow) + `:185-220` (overlay band `--muted-foreground: var(--foreground)` lift vs content-tier "NO unconditional --muted-foreground lift here" — the D-M5 decider), `tokens/scheme-motion.css:184` (`--spring-bouncy` full linear() — the IMPROVE-1 decider), Select chunk (trigger destructures `size`, reflects **no** `data-size` — the D-M1-leg decider), lucide `Icon.js:41` (the `aria-hidden` auto-spread); **vue runtime-dom at the bytes**: `:645-665` (`patchDOMProp` value branch — `oldValue = el.value` LIVE, guarded — the K-3 decider) + `:1570-1605` (`vModelText` composition guards); **producer at the tag (the K-12 decider)**: `git show v7.0.0:src/styles/glass/ladder.css` → `.glass-wash { … border: 1px solid var(--glass-border-accent); … }` + `git show v7.0.0:src/styles/glass/glass-capsule.css` (no border declaration — background/backdrop/box-shadow/radius only); **registry re-reads**: fr-GalleryFeaturedCarousel FR-GFC-1/-4, fr-GalleryInfiniteGrid C-4/D-13 fold row ("incl. its MISS-1 dead-controls arm … Do NOT re-book"), fr-BasisSelector B-3/M-10/i-3/m-3/:150, fr-BasisCanvas D-1/D-2/M-α6/:157, fr-GalleryCard :61 (K-6/K-10), fr-App R-1, fr-AdminUserList FR-AUL-15/-24/-40, fr-AdminAuditLog AA-2/AA-21, fr-EquationModeToggle :33-34, fr-FunctionInput :43, fr-GalleryCardModal GCM-21/GCM-35/K-1/K-7.

Dispositions are **ADJUDICATED, not VERIFIED** — routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave. Line anchors are HEAD `cd26c65`; re-resolve before executing any cure.

## Scoreboard

**34 adjudicated defect rows (22 booked at this site · 12 FOLDS BY REFERENCE) — 0 new BLOCKER at this site · 9 MAJOR booked · 13 kills banked (two of them this seat's own, one against each re-reader) · 10 missed finds adopted after re-proof · 11 superlatives · 9 UNPROVEN-NEEDS-LIVE.**

The component's heaviest facts are all **already banked**: the dead-controls trio (FR-GFC-1), the `#888888` resolver (fr-BasisCanvas D-1), the module-eval snapshot (D-2), the frozen-palette pill contrast (fr-BasisSelector B-3), the phantom easing token (M-α6), the v7 Button break (AA-2). What is genuinely new here is the **a11y cluster on the pill itself** (no focus indicator, five unnamed controls, a self-destructing clear button, a mislabeled disclosure) plus the **glass-ladder misuse pair** on the drawer — all F.W3/W4-shaped — and one unbanked store seam (the shared abort key).

## Ruled disagreements (this seat's actual work)

| row | reader-A (design-and-usage) | reader-B (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-B1 booking** | CONFIRMED-as-fact, **FOLD** to banked FR-GFC-1 at MAJOR ("DO NOT RE-BOOK") | CONFIRMED **BLOCKER**, "registry-swept: no adjudicated row books this" | **A wins — FOLD BY REFERENCE at the banked MAJOR (blocker-weight). B's re-book KILLED (K-13).** | My registry reads at the bytes: FR-GFC-1 contains the identical zero-query-builder census verbatim ("searchQuery/tierFilter/basisFilter have zero query-builder or `.filter()` readers repo-wide … resetAndFetch sends {limit: 20, sort, owner} only") with "two BLOCKER filings in dissent"; fr-GalleryInfiniteGrid C-4/D-13 already folds "FR-GFC-1 (incl. its MISS-1 dead-controls arm) … **Do NOT re-book**". B's sweep instrument — `grep listVisualizations` over `registry/adjudicated/` — was structurally too narrow: the banked row spells the sink by its store method, not the api function name. |
| **BREAK-2** (v7 border loss on `.basis-pill-btn`) | KILLED — `.glass-wash` supplies the border at v7.0.0 | CONFIRMED MAJOR — `.glass-capsule` declares no border, `border-style` stays `none` | **KILLED — A's kill SUSTAINED, B's confirm REFUTED at the tag (K-12).** | My own `git show v7.0.0:src/styles/glass/ladder.css`: `.glass-wash { … border: 1px solid var(--glass-border-accent); box-shadow: var(--glass-material-rim), var(--glass-shadow-wash); }`. The composed host string both readers quote is `"glass-wash glass-capsule"` — B (like the D axis it was checking) read `glass-capsule.css` whole and never opened the sibling class in the same string. `border-style` is `solid`; all three local `border-color` declarations paint. See DISSENT 1. |
| **L-4 severity** (IME guards) | MAJOR — "the `:value` patch lands on the next flush" | MINOR — write-back half killed (K-3) | **B wins — MINOR, emit-side only. A's MAJOR demoted.** | My read of runtime-dom.esm-bundler.js:654-660: `oldValue` is the **live** `el.value`, and the write is guarded `if (oldValue !== newValue \|\| !("_value" in el))` — the synchronous identity round-trip is a no-op, so composition is never interrupted in the element. Only the emit-side store pollution survives. |
| **L-5 severity** (debounce timer) | MAJOR — "a tab switch mid-debounce orphans the timer" | MINOR — reachability cell killed (K-9) | **B wins — MINOR.** | My read: `GalleryView.vue:43` `const activeTab = ref<…>("gallery")` is the view's own state; the `v-if` at :229 unmounts only the child bar. The watcher and timer live in a still-mounted view; only a route change orphans them, and even then `resetAndFetch()` runs against the app-scoped Pinia store — a wasted round trip, not a dead-scope deref. |
| **C-2 grade** (basis vocabulary mismatch) | INFO rider | MAJOR fold | **COMPOSED**: FOLD → fr-BasisSelector M-10 (banked MAJOR → F.W3/W4); **site-weight INFO while FR-GFC-1 stands** (the control reaches nothing); carried as a **WAVE-LOCK** on any F.W5-W8 wiring of `basisFilter` — wire it without the banked normaliser and the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`. | Both readers' facts identical and verified (basis-display keys 3 display names; `GalleryCard.vue:39` holds the `startsWith("fourier")` normaliser this file lacks); the disagreement was pure altitude. |
| **Clear-button focus destruction** (both readers' missed find) | MAJOR | MINOR | **A wins — MAJOR.** | FR-AUL-24 family altitude: banked MAJOR for "fires on 100% of confirmed destructive paths (WCAG 2.4.3)". Here it fires on 100% of keyboard activations of the input's only clear affordance — the focused element unmounts in the same flush (`v-if="searchQuery"` :55, zero refs/`.focus()` whole-file). Cheapness of the cure does not discount the harm. |
| **Disclosure cluster shape** | one MAJOR row | MAJOR (conflation) + MINOR (dismiss path) split | **One MAJOR row (FR-GSB-15) — one repair unit; B's split preserved as grain.** | The aria-pressed conflation, the missing `aria-expanded`/`aria-controls`/panel identity, and the missing Escape/outside-click are one authored surface cured in one edit; splitting invites a half-repair. |

## FOLDS BY REFERENCE — 12 identities, never re-booked (identity guard)

- **FR-GSB-1 = D-B1 / L-1 / C-1 — FOLD → banked FR-GFC-1 (MAJOR, blocker-weight → F.W4) + FR-GFC-4 (list contract → F.W5-W8).** Three of four controls (`searchQuery`, `tierFilter`, `basisFilter`) inert end-to-end; every dead interaction destroys accumulated pagination (`gallery.ts` clears `entries` synchronously before the await). **Third fold instance** (after fr-GalleryInfiniteGrid). This site is the *emitting component* instance; riders adopted onto the banked row: the in-repo counterexample `AdminUserList.vue:61` (`q: searchQuery.value || undefined` — the wiring idiom exists one directory away), and both re-readers' independent wire censuses (api.ts serialises four keys; `visualizations.py` declares `(request, limit, sort, cursor, owner)`). BLOCKER filings (all three axes + reader-B) preserved as dissent against the banked grade.
- **FR-GSB-2 = D-B2 / C-3 — FOLD → fr-BasisCanvas M-α6 (MINOR → F.W1).** `--ease-apple-spring` definition-absent at the pin (my census: 9 lines / 4 files, sole package hit = README prose at :211); the drawer's enter transition is invalid-at-computed-value-time; the leave (`--ease-standard`, real) animates. Severity inflation **killed again** (K-7; the banked K-6 precedent at fr-GalleryCard, which names `GallerySearchBar:188-189` inside its five-site census); the README relay cell is dead upstream (K-10). **NEW rider booked (verified by this seat): IMPROVE-1** — `--spring-bouncy` IS defined at the installed 4.0.0 (`scheme-motion.css:184`, full linear() stop-set) and the producer's excision note names it the successor register: a **two-token substitution at :188-189 restores the enter arm today**, without the tri-package transaction. Rides M-α6's F.W1 line; executable early.
- **FR-GSB-3 = L-3 / C-4b / D-M2b — FOLD → fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER → F.W1).** `cssVarToHex` four-arm parser + `#888888` fallthrough vs `oklch()`/`light-dark()` tokens. Do NOT re-book.
- **FR-GSB-4 = L-2 / C-4a / D-M2a — FOLD → fr-BasisCanvas D-2/BC-5/C-3 (MAJOR → F.W1), with the fr-App R-1 entry-route condition.** `basisDisplay` snapshots the reactive palette at module eval; the `computed` at :30-37 registers no dependency. The universal-masking framing is killed (K-4); every witness pins the entry route.
- **FR-GSB-5 = D-M3 — FOLD → fr-BasisSelector B-3 (BLOCKER → F.W3/W4) — second instance site.** The pressed pill label fails AA in all six theme×base cells at `.basis-pill-btn` (:218-222) exactly as at `.basis-toggle`; **both re-readers' independent instruments byte-match the banked six ratios** (light 4.24/4.04/4.48 · dark 3.50/3.70/3.33) — the identity is now triply reproduced. Composited-backdrop residue → SS-13.
- **FR-GSB-6 = C-2 ⊕ L-9 / C-14 — FOLD → fr-BasisSelector M-10 (MAJOR → F.W3/W4) + i-3 (→ F.W0).** The two-domain basis key space (display keys vs persisted `fourier-epicycles`/`fourier-series`), `basisFilter: string` wider than its closed domain, and the `Record<string,…>` erasure are all one banked repair unit (`BasisKey` + `normalizeBasisKey` + `satisfies`; `noUncheckedIndexedAccess`). Carries the **WAVE-LOCK** from ruling 5. The store repeats the widening at `gallery.ts:38`.
- **FR-GSB-7 = C-15 — FOLD → FR-GIG-15 (MINOR → F.W3/W4).** `scheduler.yield()` arithmetically inert at `limit: 20 < chunkSize: 24`; identical arithmetic and the identical lane-frontend.md contradiction already banked. The fresh-contradiction presentation was a re-book.
- **FR-GSB-8 = C-16 (coverage half) — FOLD → fr-GalleryCard D-7.** Client sort exposes 3 of the server's 5 `SORT_KEYS`.
- **FR-GSB-9 = D §6 BREAK-1 (fact) — FOLD → fr-AdminAuditLog AA-2 (ADJUDICATED BLOCKER, census-extension → F.W1).** v7.0.0 drops Button `variant` + `size="icon"` — true at the tag, banked, routed; the novelty claim is killed (K-5) and D's magnitude corrected: **124** `variant="` lines (not 101) / 38 `size="icon"` / 35 importing files (both re-readers' greps agree).
- **FR-GSB-10f = reader-B missed find — FOLD → the banked `GalleryTier` re-declaration census (fr-GalleryCard / fr-GalleryFeaturedCarousel; FR-AFP-59 class).** :17/:24 hold one of the two widened `"all" | …` inline variants against the `types.ts:96` export. All three axes flagged only `basisFilter` and called the tier union "exact" — exact and *duplicated* is the banked defect.
- **FR-GSB-11f = reader-B missed find — FOLD → fr-AdminAuditLog AA-21 (MAJOR → F.W4) + FR-AUL-40.** The input carries zero mobile hygiene attributes (`autocapitalize/autocorrect/spellcheck/inputmode/enterkeyhint/name/autocomplete`) on a lowercase machine-slug datum (`placeholder="Search by slug..."`, `fira-code`) — the tree's most acute instance of the banked class; `type="search"` alone UA-supplies the clear control this file hand-builds at :55-63 (composes with FR-GSB-18's cure).
- **FR-GSB-12f = reader-B missed find — FOLD → fr-GalleryCardModal GCM-35 (→ F.W3/W4) — second instance.** The ornamental basis glyph at :124 (ℱ / Tₙ / Pₙ) sits inside each pill's accessible name with no `aria-hidden` — hand-authored spans do not auto-hide the way the lucide neighbours do (Icon.js:41).

## Adjudicated defect roster — booked at this site

### MAJOR

- **FR-GSB-10 = D-B3 — MAJOR → F.W3/W4** (family precedent fr-FunctionInput D-1, banked MAJOR-demoted, identical class + identical cure surface; new site, not a re-book). The primary input suppresses its own focus indicator (`outline-none` at :52) and nothing compensates: the scoped block (:134-223, whole-read by this seat) has no `:focus-within`/`:focus-visible`; glass-ui's ring is class-scoped (`.focus-ring:focus-visible`, applied by Button's CVA and SelectTrigger — the two sibling Buttons DO ring, isolating the defect to the input); fourier's global rescue list is a closed four-class set (`style.css:136-143`) that excludes it. **Correction to the D axis carried**: the cure is not a hand-written `:focus-within` — the pin already exports the whole control (`@mkbabb/glass-ui/forms` → `Input`: tokened height/border/placeholder, focus-visible ring, forced-colors arm; `grep -rn 'glass-ui/forms' web/src` → 0). Caret-as-indicator conformance frame → SS-13.
- **FR-GSB-11 = D-B4 / C-9 ⊕ reader-A missed find M1 — MAJOR → F.W3/W4** (altitude per fr-AppHeader's identical-name-cluster ruling). **FIVE controls with no accessible name, not three**: the input (placeholder only, `type="text"`), the clear button (:55-63) and the filter toggle (:65-74) — icon-only, lucide auto-`aria-hidden` (Icon.js:41 spread, neither call site passes a slot or a11y prop), Button synthesises nothing — **plus both `<SelectTrigger>`s (:86, :101)**, whose accessible name falls to the selected *value* ("Newest, combobox, collapsed" — nothing says it sorts) since reka's trigger renders `role="combobox"` with no name and the chevron auto-hides. All three axes enumerated exactly three and stopped. Three in-repo counterexamples verified by this seat: `ContourSettings.vue:211` (`aria-label="Contour extraction strategy"`), `SpeedSelect.vue:32`, `AdminUserList.vue:252-255`. WCAG 4.1.2 + 3.3.2.
- **FR-GSB-12 = D-M4 — MAJOR → F.W3/W4** (rides FR-GSB-10's `Input` adoption — the tokened placeholder is the cure). The placeholder — the input's only naming affordance — renders at **2.02:1 light / 2.70:1 dark** (both re-readers' independent instruments byte-match; typed value passes at 16.31/14.98 — exactly the one string that had to survive was dimmed). Two authorities set it (:52 utility + :155-157 scoped rule; the unlayered scoped rule wins), so the utility is also dead code (rides FR-GSB-30).
- **FR-GSB-13 = D-M5 — MAJOR → F.W3/W4.** The drawer panel is by construction an overlay (`position: absolute; top: 100%; z-index: var(--z-bar)`, :165-173; the author's own comment at :77) wearing the content-tier `.glass-resting` (:80). The ladder partitions on exactly this criterion (my bytes-read): the overlay band gets the full AA darken **and** `--muted-foreground: var(--foreground)` (:192-204); the content tiers get the floor only, with the verbatim "NO unconditional --muted-foreground lift here" (:215-219) — and the idle pill labels (:211 `color: var(--muted-foreground)`) are precisely the muted register the ladder's own prose warns cannot clear 4.5:1 on a translucent plate. Cure = one class (`glass-floating`/`glass-overlay`), with the second-order benefit that the muted lift comes free.
- **FR-GSB-14 = D-M6 — MAJOR → F.W3/W4** (dies with FR-GSB-13's rung switch). The unlayered scoped `box-shadow` at :182 (raw `0 8px 24px`) replaces — never composes with — `.glass-resting`'s three-leg composite (`--glass-material-rim`, `--glass-under-shadow-default`, `--glass-shadow-resting`; my read ladder.css:67-76 inside `@layer components`), deleting the material rim; unlayered author CSS outranks any `@layer` regardless of specificity.
- **FR-GSB-15 = D-M7 / C-6 ⊕ L-7 / C-11 — MAJOR → F.W3/W4** (one repair unit; ruling 7). Disclosure semantics cluster: `aria-pressed` bound to `showFilters || hasActiveFilters` (:70) while `@click` toggles only `showFilters` (:71) — AT announces "pressed" for a region not in the tree whenever a filter is set with the drawer closed; the visual channel repeats the conflation (:69/:159-162). No `aria-expanded`, no `aria-controls`, no panel `id`/`role`, no Escape, no outside-click (whole-file: zero lifecycle hooks, zero listeners — script is `ref` + two `computed`); `@click.stop` at :71 guards a dismisser that was never registered (no ancestor binds `@click`); `@vueuse/core` (with `onClickOutside`) installed and unused. Family instances banked fleet-wide (fr-CoefficientsPanel FR-CP-39 · fr-GalleryDraftsSection M-2 · fr-CanvasControlsDock M-5 · fr-PaperSidebar L-5(a)); fourier's own 2026-06-16 D4-10 finding names this exact panel and is still live in the tree.
- **FR-GSB-16 = D-M1 (SelectTrigger leg ONLY; icon-Button legs killed, K-1) — MAJOR → F.W3/W4** (family: fr-ContourSettings M-4, fr-EquationModeToggle FR-EMT-4, fr-ConvergenceTimeline). The `h-8` literals on both SelectTriggers (:86, :101) pin 2rem on every pointer: the trigger destructures `size` and reflects **no `data-size`** (my chunk read), so the coarse `[data-size="icon"]` floor cannot reach it, and glass-ui's `cn` buckets `h-(--control-h-md)` with `h-8` last-write-wins. On coarse at fourier's 18px ≤768px root: **36px vs the 49.5px floor** (`max(2.5rem × 1.5, 2.75rem)`). Carries the four-heights proportion residue (44 / 36 / 60.75 stack in one drawer) → SS-13.
- **FR-GSB-17 = L-11 / C-7 — MAJOR → F.W9/W10.** The one e2e test named for this component is vacuous by construction (my read, gallery.spec.ts): every assertion the test owns sits inside `if (await filterToggle.isVisible()) {` (:57-63) — deleting the search bar turns the spec GREEN; the `.glass-dock` expand branch (:49-53) is unreachable (`grep -rn glass-dock src/` → **0**, my run) and the `.or()` disjunction at :19-22 hides the same drift. No test in the suite types into the input or clicks a pill and asserts a result set — which is exactly how FR-GSB-1's identity shipped.
- **FR-GSB-18 = both re-readers' missed find — MAJOR → F.W4** (ruling 6; family FR-AUL-24, banked MAJOR; fr-MobileFloatingToc M-10; new site citing the family). Activating the clear button destroys the element holding focus: `v-if="searchQuery"` (:55) goes false in the same flush as its own `@click` emit (round-tripped synchronously at GalleryView :233), the focused button unmounts, focus falls to `<body>`, and the next Tab restarts from the document top (WCAG 2.4.3). Zero template refs, zero `.focus()`, zero `nextTick` anywhere in the file (this seat's whole-read). Cure composes with FR-GSB-11f: `type="search"` UA-supplies a clear control that does not self-destruct; otherwise restore focus to the input after the emit.

### MINOR

- **FR-GSB-19 = L-4(a) — MINOR → F.W3/W4** (write-back half killed K-3 at the bytes; A's MAJOR demoted, ruling 3). The hand-rolled `:value` + `@input` pair omits Vue's composition guards (`vModelText` registers `compositionstart/end` and gates on `e.target.composing`; the plain-prop path does not), so partial IME buffers are emitted into the store per composition keystroke — and, today, each schedules a destructive debounced `resetAndFetch`. Element-side corruption does NOT occur (patchDOMProp identity guard). Dies with the `Input`/`v-model` adoption. Live symptom → SS-13 (store-pollution probe, not element corruption).
- **FR-GSB-20 = L-5 — MINOR → F.W4** (rider on the banked FR-AUL-15 / PSM-22 teardown unit; reachability corrected, K-9). `searchTimer` (GalleryView :94-98) is never cleared on scope dispose; a route change away from `/gallery` mid-debounce fires `resetAndFetch()` against the app-scoped store — a wasted round trip and an unobserved mutation, not a dead-scope deref. `useDebounceFn` installed and unused; the debounce is also decolocated from the input that produces the events.
- **FR-GSB-21 = D-M8 (fine-pointer arm only; coarse column killed K-2) — MINOR.** The authored `:size` props on the two in-Button icons (14 at :62, 15 at :73) never render: lucide sets width/height as presentation attributes (lose to author CSS) and emits no `size-` class, so Button's CVA `[&_svg:not([class*=size-])]:size-(--ui-glyph)` wins. `<Search>` at :47 is the only icon honouring its prop — a leading-glyph-vs-siblings mismatch that is unit-invariant.
- **FR-GSB-22 = D-M9 — MINOR → F.W3/W4** (merges into FR-GSB-18's cure). The clear button mounts/unmounts with no transition inside the flex row, jumping the input's content box ~32px on the first and last keystroke while the caret is live; the author demonstrably knows the technique (the drawer 20 lines below is wrapped in `<Transition>`).
- **FR-GSB-23 = L-8 / C-5 / D-i1 — MINOR → F.W3/W4** ("permanent" consequence killed K-8). `$event as any` ×2 (:84, :99) widens the ARGUMENT position, deleting the emit contract the component just declared — a one-character `SelectItem` typo type-checks; both `<SelectValue />`s are placeholder-less (reka defaults `placeholder` to `""`), so any unmatched value renders a permanently empty trigger. The narrowing cast (`$event as typeof props.tierFilter`) compiles today.
- **FR-GSB-24 = L-6 / C-8 / D-m4(a) — MINOR.** `:class="{ active: … }"` at :119 is a dead binding — no `.active` rule in the scoped block (whole-read), none reachable from glass-ui (all compounded under dock ancestors); the live hook is `[aria-pressed="true"]` at :218.
- **FR-GSB-25 = reader-A missed find M3 — MINOR.** `.filter-toggle.is-active` (:69, :159-162) is a redundant re-implementation of the ghost variant's shipped pressed register (`aria-pressed:bg-foreground/10 aria-pressed:text-foreground`, verbatim in the CVA) bound to the identical expression as `aria-pressed` — and the unlayered scoped rule **dims** the producer's plate from 10% to 8%. Deleting :69 + :159-162 is behaviour-preserving-plus. **Registry amendment note**: fr-EquationModeToggle FR-EMT-3 cites `GallerySearchBar:69-70` as a house-pattern conformance exemplar — it is half-conforming, carrying a parallel class channel.
- **FR-GSB-26 = C-17 / D-m7 — MINOR** (kills D's own §5 S-4 verification cell, K-11). The :205-208 comment's parity claim with BasisSelector's `.basis-toggle` recipe is false in four respects: conditional-vs-unconditional colour-var bind, neutral-vs-tinted non-pressed hover, missing `min-w-[5.5rem]`/`border-width: 2px`, and `--pill-color` vs `--pill-c`. A comment asserting parity that does not hold defeats the next reader's inspection.
- **FR-GSB-27 = reader-A missed find M4 — MINOR** (rides fr-BasisSelector m-3 — `.cm-serif` is inert, `--font-serif-math` defined nowhere — and the serif-fallback unproven). The ℱ glyph loses the six optical constants the sibling recipe exists to supply: :124 applies one flat `text-[1.1em]` span to all three icons where BasisSelector judges ℱ needs `2.2em` + margin/translate/min-width compensation — a ~2× under-size on the pill for the app's default basis, and the fourth falsehood in the :205-208 comment. The shipped `@utility fourier-f` is the correct vehicle.
- **FR-GSB-28 = reader-B missed find — MINOR → F.W5-W8.** `resetAndFetch()` and `fetchNextPage()` share one abort key: the literal `"listVisualizations"` (my read, api.ts:411; `abortable()` at :54-57 aborts the prior controller; :161 defaults every call's signal to it) — so a filter/search refetch cancels an in-flight infinite-scroll page and vice versa, and the reset's synchronous `entries = []` can bring the sentinel into view fast enough to abort its own reset. The **over-broad dual** of the banked FR-AUL-3 / R6-8 abort-key class (that direction is unbanked). Symptom → SS-13.
- **FR-GSB-29 = reader-B missed find — MINOR → F.W3/W4** (rider on the FR-GSB-6 type-canon unit). The `sort` union `"newest" | "views" | "likes"` is authored three times (:16, :23, gallery.ts:32) with no exported canon (`types.ts` exports `GalleryTier` but no sort type) and none of the three is the server's truth (`cursors.py SORT_KEYS` = 5). The one control that works is typed by triplication.
- **FR-GSB-30 — MINOR band → F.W3/W4** (D-m3/m4/m5/m6/m8 · L-10/L-12/L-13/L-14 · C-10/C-13 · D-i2, spot-verified): `max-width: 32rem` duplicated :148/:171 with no shared property (drawer-to-pill alignment maintained by hand); `justify-content: flex-start` at :140 dead code **on corrected grounds** (K-10: live when the root exceeds 32rem; dead because `normal` behaves as `flex-start`); dual placeholder authorities (:52 utility inert under :155-157); the hand-rolled divider at :64 computes to 1.22:1 / 1.26:1 — functionally invisible — while `./separator` ships at BOTH pins; no `role="search"` on :45, no `role="group"`/label on :80; `hasActiveFilters` folds `sort !== "newest"` into a predicate named *filters* and omits `searchQuery`; no clear-all affordance; the emitted query is never trimmed (compounds at FR-GSB-1's repair); `basisOptions` (:30-37) is an identity re-wrap of a shape the sibling iterates directly (the `computed` camouflage that made FR-GSB-4 hard to see); four hand-written prop/emit pairs where four `defineModel`s collapse 13 lines here + 8 at the call site; SelectTrigger's shipped `rounded-pill` forced to `rounded-lg` at :86/:101.
- **FR-GSB-31 = C-12 — MINOR → F.W0** (substrate: dependency classification). `lucide-vue-next` imported at runtime (:3) but declared under devDependencies (my node read: dep **undefined** / dev `^1.0.0`) — harmless under `private: true` + Vite bundling, fatal under any `npm ci --omit=dev` stage. The rename half folds to the banked 35-site `@lucide/vue` census row (fr-AdminAuditLog D-i4).

## Killed-claims register — 13 kills, with refutations

- **K-1 — D-M1's icon-Button legs (27px / 31.5px coarse claims + "44px floor defeated") — KILLED; re-verified by this seat at the installed bytes.** `a11y-overrides.css:106-122`: `@media (pointer: coarse) { [data-size="icon"], … { min-block-size: var(--touch-target, 2.75rem); min-inline-size: … } }` with the producer's own prose "Targets `Button size="icon"` (via the `data-size="icon"` attr Button.vue reflects) … only RAISES". Both call sites pass `size="icon"`; the floor keys on an ATTRIBUTE `cn`'s class merging cannot touch. Coarse result: 49.5px at the 18px root. Fourth banked instance of the citation-inheritance class (fr-AdminFlaggedPanel ruling 1 · fr-CoefficientsPanel R-1 · fr-GalleryFeaturedCarousel K1): the falsifier grepped fourier's tree only, structurally excluding the producer's answer. What survives of D-M1 is FR-GSB-16.
- **K-2 — D-M8's coarse column ("glyph fills the box edge-to-edge"; 16px-vs-24px table) — KILLED.** Dies with K-1 (a 1.5rem `--ui-glyph` glyph in a ≥2.75rem box); the 24px cell is also internally inconsistent with D-M1's own 18px root (1.5rem = 27px there). The fine-pointer arm survives as FR-GSB-21.
- **K-3 — L-4(b), the write-back half ("assigns el.value unconditionally … while the composition buffer is live") — KILLED; re-verified by this seat at the cited lines.** runtime-dom :654 `oldValue = … el.value` (LIVE DOM value) · :660 `if (oldValue !== newValue || !("_value" in el))` — the identity round-trip never writes; functionally the same early-return `vModelText` uses. The claim also self-contradicts ("whenever oldValue !== newValue" is not "unconditionally"). Demotes L-4 to FR-GSB-19 MINOR.
- **K-4 — L-2 / C-4's universal chromatic-masking account ("the pills show stale-but-chromatic hexes") — KILLED per the banked fr-App R-1 ruling** (re-read this session): on the DEFAULT `/paper`-first path the lazy gallery chunk evaluates after `resolveVizColors()` and snapshots the poisoned `#888888` — the frozen and live sides agree at grey; chromatic pills require a deep-link/saved-tab `/gallery` entry. D-M2 states it correctly; L and C inverted it, and C-4's Appendix-A ΔE table rests on the one-of-two entry paths premise. Both defective outcomes survive inside FR-GSB-3/4; every witness pins the entry route.
- **K-5 — D §6 BREAK-1's novelty claim ("an addition to the corpus … book a distinct break-surface row") — KILLED.** Registry-first violation: banked as fr-AdminAuditLog AA-2 (ADJUDICATED BLOCKER, census-extension → F.W1), listed at fr-AdminFlaggedPanel among identities that "fold by reference — never re-booked". Magnitude also corrected: 124 `variant="` lines, not 101. The tag facts themselves are true and fold (FR-GSB-9).
- **K-6 — D §6 BREAK-3 ("`rounded-full` vs `--radius-pill` — a silent visual diff nobody will attribute to the uplift") — KILLED.** `--radius-pill: 9999px` vs Tailwind v4's `calc(infinity * 1px)`: CSS Backgrounds §5.5 clamps both to the identical fully-rounded geometry on any real control height. Not new at 7.0.0 either — glass-ui's `cn` has a closed `rounded` enum that `rounded-pill` doesn't match, so both classes already co-ship at the installed pin with the same non-effect.
- **K-7 — D-B2's BLOCKER / C-3's MAJOR grading + C-3's live BH relay ("ship the token or strike README:211") — KILLED by standing adjudication.** fr-GalleryCard :61: MINOR at the banked severity, fold to fr-BasisCanvas M-α6; "BLOCKER killed (K-6); the README relay cell killed (K-10 — discharged upstream). Do NOT re-book" (README clean at v7.0.0/8.0.0-HEAD). **Third instance** of the calibration signal fr-GalleryCard :130 predicted ("two independent axes filed a banked MINOR as BLOCKER") — this corpus makes the axes' count three.
- **K-8 — C-5's consequence ("a typo pins `aria-pressed="true"` **forever**") — KILLED on the word that carries the severity.** `hasActiveFilters` is a pure computed over current props; selecting "All tiers" restores the predicate. Fully user-recoverable; the type-hole half survives as FR-GSB-23 at MINOR.
- **K-9 — L-5's reachability cell ("a tab switch mid-debounce orphans the timer") — KILLED; re-verified by this seat.** The timer and watcher live in GalleryView, whose `activeTab` is its own ref (:43); the `v-if` at :229 unmounts only the child bar. Only a route change orphans them — against a live app-scoped store. Demotes L-5 to FR-GSB-20 MINOR.
- **K-10 — D-m4(c)'s mechanism ("`justify-content: flex-start` is inert against a `width: 100%` child") — MECHANISM KILLED, conclusion survives on corrected grounds.** `.search-pill` is `width: 100%; max-width: 32rem` — free space exists whenever the root exceeds 32rem, so the declaration is live there; it is dead code because `normal` already behaves as `flex-start`. The corrected reason rides FR-GSB-30; the stated one must not enter a repair spec.
- **K-11 — D §5 S-4's verification cell ("the comment is accurate") — KILLED.** Contradicted by D's own D-m7 and by the four verified divergences (FR-GSB-26). The narrower half survives: the comment correctly names what the vendor owns vs what the local hook adds.
- **K-12 — D §6 BREAK-2 ("at 7.0.0 `.basis-pill-btn` loses its border entirely — `border-style` stays `none`") — KILLED BY THIS SEAT at the tag, against reader-B's MAJOR confirm.** `git show v7.0.0:src/styles/glass/ladder.css`: `.glass-wash { … border: 1px solid var(--glass-border-accent); box-shadow: var(--glass-material-rim), var(--glass-shadow-wash); }` — the sibling class in the very composed string both the D axis and reader-B quoted (`"glass-wash glass-capsule"`) supplies `border-width` and `border-style`; `.glass-capsule` (git-show, no border declaration) cannot reset what it never sets. All three local `border-color` declarations paint at v7. Reader-A's kill SUSTAINED. This also strengthens S-10: the component's v7 exposure is the banked token surface only.
- **K-13 — reader-B's D-B1 BLOCKER re-book ("registry-swept: no adjudicated fr-*.md row books this") — KILLED BY THIS SEAT.** Registry-first violation (the GCM K-7 precedent): FR-GFC-1 banks the identity verbatim (MAJOR, blocker-weight, the same zero-reader census) and fr-GalleryInfiniteGrid already folds its own instance with "Do NOT re-book". B's sweep instrument — one api-identifier grep — was structurally too narrow to find a row that spells the sink by its store method. The fact-quality of B's wire census is preserved as riders on FR-GSB-1.

## Superlatives — with provenance

- **S-1 — Zero teardown surface, verified by exhaustion (both re-readers; whole-file re-verified by this seat).** One piece of state (`showFilters`) across 223 lines; zero lifecycle hooks, watchers, timers, listeners, observers, rAF, template refs; imports pull only `ref`/`computed`, three icons, one data module, two glass-ui barrels. Unmount is a no-op by construction — in a file whose own parent leaks a `setTimeout` (FR-GSB-20). The contrast proves the discipline deliberate.
- **S-2 — A genuinely controlled leaf at goldilocks size (reader-B).** Purely presentational: no store/router/api import; all four values arrive as props and leave as emits, so the dual-source-of-truth class is structurally impossible — a CHOICE, not a house norm (BasisSelector.vue:37 does the opposite two directories away). It is why FR-GSB-1's remedy barely touches this file.
- **S-3 — Visual state DERIVED from accessibility state (both; endorsed with the registry's own citation).** :120 `:aria-pressed` drives both semantics and paint via `[aria-pressed="true"]` at :218 — one selector, two channels, undriftable; strictly better than its own model (BasisSelector gates the colour var on a second JS predicate). Half-bitten by FR-GSB-24/25 — strip the dead `.active` and the parallel `.is-active` and this is the repo's reference implementation.
- **S-4 — Named-property, asymmetric, compositor-only motion (both).** :186-203 transitions exactly `opacity` + `transform`, 0.3s rich enter / 0.2s plain leave travelling half the distance. The dead enter arm is a TOKEN failure (banked M-α6), not a design failure — and the repair is proven two tokens at the current pin (FR-GSB-2's IMPROVE-1).
- **S-5 — Flexbox overflow safety, all four members (both).** `flex-1 min-w-0` on the input + `shrink-0` on icon, divider, toggle; `min-w-0` is the non-obvious load-bearing half.
- **S-6 — PRM delegated, not re-forked (reader-B).** No local `prefers-reduced-motion` block and none needed: the producer's `!important` utilities-layer clamp strips `transform` and outranks the unlayered scoped rules (important declarations invert layer order). Three siblings re-fork local blocks; this file's restraint is the more correct posture, on the right side of a producer file the registry has adjudicated three times.
- **S-7 — Toggle-off sentinel discipline (both).** :122 clicking the active pill CLEARS; `''` is the single no-filter sentinel at all five sites incl. the store initialiser. No third state.
- **S-8 — Typographic voice matched to the datum (reader-B).** `fira-code` on a slug field — the shipped utility used precisely as intended inside a Computer-Modern-serif page.
- **S-9 — The `--pill-c` single-property projection over the vendor chassis — FOLD → fr-GalleryCardModal SUP-3 (recorded, not re-awarded).** One inline custom property per instance, three color-mix derivations; the vendor primitive is fed, never restyled — the reason this file's cascade was diagnosable at all.
- **S-10 — Lowest uplift blast radius in the folder (reader-B; STRENGTHENED by this seat's K-12).** At `git show v7.0.0`: all four Select call sites survive verbatim, `./button`/`./select`/`./separator` all present — and with BREAK-2 dead, the pill border survives too. The component's real v7 exposure is the already-banked token surface. A genuinely unusual position in this tree.
- **S-11 — DOM order equals visual order for the disclosure (reader-A, recorded by no axis).** `.filter-anchor` is the immediate next sibling of `.search-pill`, positioned out of flow rather than reordered — Tab from the toggle lands in the panel's first control with no machinery. Given the drawer has NO focus management (FR-GSB-15), this single structural choice is the only reason the panel is keyboard-reachable; a future refactor that portals the panel must not silently destroy it.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **One coarse-pointer frame settles the whole D-M1 residue** (768×1024, `pointer: coarse`): expect basis pills 60.75px, SelectTriggers pinned 36px (FR-GSB-16), icon buttons FLOORED at 49.5px (K-1). If the icon buttons measure 27/31.5px, K-1 is wrong and D-M1 restores whole.
2. Composited pressed-pill contrast under `.glass-resting`'s backdrop-filter over live gallery imagery (FR-GSB-5's six opaque-ground ratios cannot plausibly close a 1.0-1.2 gap, but measure it).
3. **Entry-route pin on every witness** (the fr-App R-1 condition): deep-link `/gallery` = authored chromatic hexes; default `/paper`-first = `#888888` all three. Both defective; which, per path, is the live question (K-4).
4. Drawer contribution to the `overflow-y-auto` scroller's scrollable overflow (GalleryView :220) — spec-predictable, browser-confirmable.
5. Select label truncation: "Most Viewed" in two `w-full h-8` triggers inside `flex gap-2` within a 32rem-capped panel (`[&>span]:line-clamp-1`).
6. IME live session for FR-GSB-19 — probe **store pollution from partial composition text**, not element corruption (K-3 rescoped the probe).
7. The abort-key cross-cancel symptom (FR-GSB-28): does the emptied grid fire the sentinel fast enough to abort its own reset, and does the skeleton clear before content arrives.
8. Whether the generic `serif` fallback covers U+2131/U+2099 at all and with what metrics (carried at fr-BasisSelector unproven #5) — decides whether FR-GSB-27 is a proportion defect or a tofu defect.
9. Whether the text caret alone satisfies any reviewer's reading of WCAG 2.4.7 for FR-GSB-10 — the suppression is statically closed; the conformance verdict wants one live frame.

## DISSENT — preserved, not ruled away

1. **An inheritance-shaped event on BREAK-2 (evidence about the fleet, recorded per the ColorSpaceSelector DISSENT-1 precedent).** Reader-B presented its BREAK-2 confirm as independently derived ("git-show at the named baseline — BASELINE LAW") and quoted its own read of `glass-capsule.css` — yet reproduced the D axis's exact partial read: neither opened `.glass-wash`, the sibling class in the same composed string both quoted. A git-show of the wrong file at the right tag is not verification. This seat's one-command read of `ladder.css` at the tag settled it (K-12).
2. **Sweep instruments must match the registry's spelling.** Reader-B's "registry-swept: no adjudicated row books this" (D-B1) was manufactured by grepping one api identifier over `registry/adjudicated/` — the banked row describes the sink by store method and prose. Sweeps must grep the mechanism's prose forms and the component/store names, never a single spelling (K-13). Reader-A's line-cited fold was the correct method.
3. **Severity dissents preserved**: all three axes + reader-B filed BLOCKER on the dead-controls identity — preserved as dissent against FR-GFC-1's banked MAJOR (blocker-weight), joining the two already recorded there. Reader-A's L-4/L-5 MAJORs were demoted on byte evidence (K-3/K-9); reader-A's clear-button-focus MAJOR was adopted over reader-B's MINOR on the FR-AUL-24 family altitude.
4. **The calibration signal compounds**: the phantom easing token has now been filed above its banked MINOR by three independent axes across two components (fr-GalleryCard :130's predicted pattern). Future challengers touching `--ease-apple-spring` should be handed the M-α6 row before they file.

---

**ADJUDICATED — a small, disciplined, genuinely well-shaped leaf whose four working superlatives sit on top of an advertising surface that is three-quarters false: the dead-controls trio, the resolver, the snapshot, the pill contrast, the easing corpse and the v7 Button break are ALL banked identities folded by reference (12 folds, zero re-books); what books NEW here is the site's own a11y cluster — no focus indicator, five unnamed controls, a self-destructing clear affordance, a mislabeled unmanaged disclosure — plus the glass-ladder misuse pair on the drawer and one unbanked abort-key seam; 9 MAJOR + 13 MINOR-band rows routed overwhelmingly to F.W3/W4, the test vacuity to F.W9/W10, the wire seams to F.W5-W8 (under the M-10 WAVE-LOCK: no basisFilter wiring without the normaliser), the dependency misclassification to F.W0, and the token folds to F.W1 — with the enter-arm repair proven a two-token substitution executable at the current pin; 13 kills banked, including one against each hostile re-reader (BREAK-2 dead at the tag's own bytes; the BLOCKER re-book dead at the registry's); preserve S-1/S-3/S-7/S-11 through every cure — nothing here stamps a wave.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this file per E-1/E-3.
