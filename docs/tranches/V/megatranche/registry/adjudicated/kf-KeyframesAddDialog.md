claude-fable-5

# kf-KeyframesAddDialog — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator (apotheosis seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Anchors**: value.js @ `c654824e` (branch `tranche-u`) · keyframes.js @ `8281638c` (branch `master`, working tree = the audited tree) · installed producer `node_modules/@mkbabb/glass-ui` 7.0.0 (dist is the contract of record); reka-ui 2.9.9; @vueuse/core 14.3.0; @mkbabb/value.js 4.0.0.
**Corpus**: three axis challenges (D design · L library · C consumption, all `claude-opus-5[1m]`, 2026-08-04/05) at `docs/tranches/V/megatranche/audit/kf-components/KeyframesAddDialog/`, re-read by two hostile re-readers (design-and-usage = **R-A**; library-and-code = **R-B**), both returns adjudicated here.
**Twin note**: this component is the 161-line **twin** of the already-banked `kf-CSSPasteDialog` (80-line parameterised shell, `demo/components/instrument/timeline/CSSPasteDialog.vue`). That record's R-7 is the twin-fold and its superlative #4 names *this* file's `DialogTitle` nesting as the negative comparator. A large fraction of this component's defects are therefore either the same 3-surface / 3-mount facility classes already booked there, or the fold itself — folded by reference below, never re-booked (identity guard).
**Method (L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (the BLOCKER↔BLOCKER-vs-demote question, the D-2 kill mechanism, the D-5 rung, the two axes' contradictory deploy-platform claim, the S-9 count, and R-A's vs R-B's disjoint MISSED sets) was re-derived against the tree by this seat with its own instruments. One reader factual contradiction (deploy platform) was settled by artifact read; one axis kill was re-verified against a possibly-stale artifact and upheld.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony; product source READ-ONLY)**: `KeyframesAddDialog.vue` read whole (162 lines — sink caller :103, `Ï`/`Tab` guards :112-120 with unconditional `preventDefault`, unconditional `animateProgressBar` :133-136, `useMagicKeys` watch :140-147, duplicate `watch`+`onMounted` :150-160, nested `DialogDescription`-in-`DialogTitle` :24-31, `hljs css … text-small` with no `font-mono` :37, `contenteditable` with no `spellcheck`/`autocorrect` :38, dead footer tokens `sticky bottom-0 class grid` :41 + `progress-bar w-full bottom mt-2` :50); `useHighlightCSS.ts:84-148` (the `el.innerHTML = s` sink :111, marker set-`""`/read/set-`"true"` :110/:117/:124 never cleared, bare `setCodeTheme()` :104, unconditional `themeStyle.remove()` :143); `KeyframeCard.vue:45-46` (the fully-instrumented `focus-ring hljs css … role=textbox aria-multiline aria-label` comparator, same `hljs`-no-`font-mono` shape); `CSSPasteDialog.vue:11-12,17,54,58` (siblings not nested; `font-mono` present; `defineModel`); `KeyframesEditor.vue:75-80,168-169,218-230,254-258` (`v-model:open/:text`, the **mirror-watch** `watch(addKeyframesString, v => kfControls.addKeyframes = v)`, the byte-twin `onKeyDown`/`animateProgressBar`); `useKeyframeOps.ts:25-40,146-153,155-191` (`withErrorToastAsync`, the hidden model-write in `format`, the close-on-success-only at :183). Producer/spec reads: **deploy** `.github/workflows/deploy-pages.yml:1` ("Cloudflare Pages deploy-of-record") + `scripts/pages-deploy.sh` (`wrangler pages deploy`); `find -name _headers` → none (only `public/_redirects`); `grep http-equiv demo/app/index.html` → only `X-UA-Compatible` (no CSP); glass-ui dist `@utility text-{small,subheading,heading}` in `styles/typography/semantic.css` (`text-small` **binds `font-family: var(--font-text)`** as its first declaration; `text-subheading` = `var(--type-subheading)`/600; `text-heading` = `var(--type-heading)`/`var(--type-weight-heading)`), `styles/typography/scale.css` (`--type-small: clamp(0.875rem,…,1.25rem)`, `--type-subheading: 1.272rem`, `--type-heading: 1.618rem`, `--type-weight-heading: 700`), `tailwindcss/preflight.css:109-125` (`code,kbd,samp,pre{font-family:…mono}` in `@layer base`), `components/button/styles.css` (`.button{gap: calc(0.375rem*var(--ui-scale))}` and `.button>svg:not([class*="size-"]){inline-size: var(--ui-glyph)}`, `--ui-glyph: calc(1rem*var(--ui-scale))`), `components/card/styles.css` (`.card-title{min-width:0; font-size:var(--type-heading); overflow-wrap:anywhere; …}`); `useMonacoCancellationGuard.ts:27-31` (the real, Monaco-only `unhandledrejection` listener); `grep useMagicKeys demo/` → 2 hits, both this file; `grep -c registerShortcut(` → **19**; `grep KeyframesAddDialog test/` → 0; `grep 'from "@components/instrument"' demo/ test/` → 0 (the async umbrella barrels are unimported); KF wave taxonomy from `formation/keyframes/CENSUS-2026-08-03.md:179-206`.

## Scoreboard

**1 BLOCKER (stays) · 15 MAJOR (12 own + 3 folded) · 20 MINOR (18 own + 2 folded) · 7 INFO/fold-by-reference · 12 KILLED · 7 superlatives · 10 UNPROVEN-NEEDS-LIVE residue rows for SS-13.**

Two structural facts organise everything:

1. **The corpus's one surviving BLOCKER is real and stays a BLOCKER** — unlike the twin `kf-CSSPasteDialog`, whose two axis BLOCKERs were both demoted because each rested on an unproven half. This one rests on **no** unproven half: the sink is verbatim, the `?state=`→sink chain is complete and code-cited at every hop, and the sole escape hatch (a CSP) is **verified closed** (Cloudflare Pages, no `_headers`, no meta-CSP). The only "gate" is one keystroke (Shift+Alt+F) — the exact affordance the component exists to invite.
2. **The hljs-on-contenteditable apparatus mostly does not work.** Two MISSED findings (both re-derived and upheld) establish that during ordinary authoring the surface (a) **never syntax-highlights** and (b) **renders in the proportional UI face, not mono** — both caused by `highlight()`'s `el.innerHTML` destroying the Vue-owned `<code>` node on the always-empty first open. This makes the "component stays; swap the well" verdict the twin got the more attractive here, because the highlighting it would sacrifice is already inert.

The component is the **redundant re-author of `CSSPasteDialog`** (D-11/C-6 = the twin fold, R-7). The cure spine: (1) the **one-line sink fix** (`el.textContent = s`, front-loaded — it blocks nothing and kills the BLOCKER *and* its data-corruption twin *and* re-decides three highlight rows); (2) the **twin fold** (KF.W7, constraints pinned by the banked R-7); (3) **registry adoption** for the reformat shortcut (KF.W6, dissolving the `Ï` cluster); (4) the **failure-posture / busy-state contract** (KF.W7, joined to KF.W2's registry).

## Ruled disagreements (the seat's actual work)

| id | R-A | R-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **The XSS rung** (L-1/C-1/D-27.1) | BLOCKER — "stands on all three axes" | BLOCKER — CSP leg CLOSED "GitHub Pages cannot serve custom headers" | **BLOCKER — stays; R-B's platform premise is FALSE, the conclusion holds** | This seat's read: `deploy-pages.yml:1` = **Cloudflare Pages** (not GitHub Pages), and CF Pages *can* serve headers via a `_headers` file — but `find -name _headers` returns nothing (only `public/_redirects`), and `demo/app/index.html` carries only `X-UA-Compatible`. So the CSP leg is closed by *absence of a `_headers` file*, not by platform incapacity. R-A named the platform correctly and checked for `_headers`; R-B reasoned from a wrong premise to the right answer. Recorded as a reader-fact-error that did not move the verdict. Unlike the twin's demoted pair, no half is UNPROVEN: the rung stays. |
| **D-2 rung** (Tab trap) | BLOCKER-arm KILLED (tabbable-order evidence wrong: close-X is LAST in DOM); forward-Tab / 2.1.1 arm survives MAJOR | BLOCKER KILLED on two errors (tab order + FocusScope `loop` rescue); forward-Tab survives MAJOR | **MAJOR-with-escalation — both readers converge; unlike the twin, neither misread the dist slot-order** | Both correctly read the shipped chunk's `default: [slot, showClose ? closeX : …]` — slot **first**, close **last** — so Shift+Tab from the `<pre>` (if tabbable) hits FocusScope's `first`-edge `loop` branch and escapes to the close X, then the button. The 2.1.2 no-trap arm dies; the forward-Tab / 2.1.1 arm (unconditional `preventDefault()` on `key==="Tab"`, no documented exit, no `<form>` so no Enter) survives. Residual (does `pre[contenteditable].tabIndex >= 0`?) is SS-13 probe #1; a witnessed no-exit-in-any-direction promotes back. (Contrast: the twin's R-B *misread* the same slot order — K-9 there; here R-B got it right.) |
| **D-5 rung** (description at `text-subheading`) | MAJOR | MAJOR | **MAJOR — upheld, with a noted tension** | `text-subheading` is a **heading-family utility** (`1.272rem`/600, one √φ step below the `text-heading` title's `1.618rem`/700). Both readers grade MAJOR; the `cn` last-wins merge drops glass's `text-sm` default outright (verified). Tension recorded honestly: the twin's analogous D-M4 (`text-body` on *its* description) was ruled **MINOR** because rank survived on color+position. This one is a full rung worse (a heading-family utility, not the fluid body ladder), which is why MAJOR holds — but color rank (`text-muted-foreground`) and position survive, so it is not a BLOCKER. |
| **Two MISSED MAJORs** (R-A: proportional face + highlighting inert) | RAISED (both MAJOR) | not raised | **ADOPTED at MAJOR — re-derived by this seat from the cascade** | (a) `@utility text-small { font-family: var(--font-text); … }` (verified, first declaration) emits into `@layer utilities`; preflight `pre{…mono}` is `@layer base` → text-small wins on the `<pre>`. Because `highlight()` destroys the `<code>` via `innerHTML`, the text lives directly in the `<pre>` → **proportional Plus Jakarta Sans**, not mono. This is *why* the twin's `font-mono` is redundant (its `<code>` survives — K-3 there) yet its **absence here is a defect**. (b) The always-empty first open writes `innerHTML = ""` + marker `"true"`; every later `highlightAll()` short-circuits at :117 → **syntax highlighting never runs during typing**. C-7 saw "never re-highlights" but graded it MINOR-as-perf; R-A sharpens to never-at-all. Both upheld MAJOR. |
| **S-9 count** (C-3 "19" vs D-23 "16") | 19 (C-3 right) | 19 | **19 — verified by this seat** (`grep -c registerShortcut(` → 19). D-23's 16 undercounts. |
| **L-5 rung** (root-barrel import) | (folded to F-1) | MAJOR → **collapses to MINOR-on-F-1** (glass `sideEffects:["*.css"]`, root barrel is the demo's dominant idiom, 31 sites) | **MINOR-on-F-1 — R-B wins; matches the twin's C-9 (root barrel = standing pattern)** | The chunk-weight arm was never built; the barrel is tree-shakeable by declaration; F-1 (the phantom-dependency) is the durable defect and folds to the census. |

**Cure-level ruling inside an agreed verdict**: R-B's MISSED "innerHTML **corrupts ordinary CSS**" (KAD-2) is the non-security face of the same sink and is adopted at MAJOR — it fires with no attacker (`content:"&amp;"`→`&`, `/* <b> */` loses `<b>`), round-trips into the submitted model via `onInput`→`props.text`→`onSubmit`, and shares the BLOCKER's exact one-line cure. Booked separately so the sink is fixed *even if the CSP had held*.

## Adjudicated defect roster

Dispositions are **ADJUDICATED**, not VERIFIED — X·KF specs are forming; these routings are agglomeration input (M-25). Wave targets from `CENSUS-2026-08-03.md:179-206` (KF.W2 Parse Façade / W5 D-L-C library audit / W6 Glass Suffusion / W7 Timeline Evaluate = the twin-fold home / W8 Structure-Colocation / W9 Safari Visual Audit). Line anchors are keyframes.js `8281638c` working tree; re-resolve before any cure (D-19).

### BLOCKER (1)

- **KAD-1 — BLOCKER · DOM-XSS `innerHTML` sink, reachable from a crafted `?state=` share URL** (L-1 / C-1 / D-27.1). Sink `useHighlightCSS.ts:111` (`el.innerHTML = s`; the sibling writer :123 escapes via `hljs.highlight().value`). Sole caller `KeyframesAddDialog.vue:103`; argument = `await props.format(props.text)` → `formatEditorCSS` = bare `prettier.format(css,{parser:"scss"})` (no escape). Provenance chain each hop code-cited: `router.ts:50 restoreStateFromParam` → `hashSharing.ts:29-44 isValidState` (two `typeof==="object"` guards, no leaf validation) → `controlOptionsStore.ts:59-64 Object.assign` (no whitelist) → `:19-24` (`keyframeControls.{dialogOpen,addKeyframes}` both in the shared bucket, so the payload opens its own dialog) → `useKeyframesState.ts:24 ref(kfControls.addKeyframes)` → `KeyframesEditor.vue:75-80`. **CSP leg CLOSED** (this seat: Cloudflare Pages deploy-of-record, no `_headers` file, no meta-CSP). Gate = one keystroke (Shift+Alt+F), which is the component's advertised affordance. **Cure**: `el.textContent = s` — one line in the composable, also semantically correct (`s` is plain text). Identity: distinct from every banked row — `useHighlightCSS` is not consumed by the twin; the sole `setHighlightingString` caller is this file. **→ KF.W5 (composable hardening — FRONT-LOAD: the fix blocks nothing and any wave sequencing must land it first)**; structural cure = the KF.W6 well-swap removes the whole `innerHTML` path.

### MAJOR — component's own (12)

- **KAD-2 — MAJOR · the same sink CORRUPTS ordinary non-malicious CSS** (R-B MISSED, adopted). `reformat()` assigns prettier's plain-text output to `innerHTML` (:103→:111), so the HTML parser entity-decodes every `&`-sequence and swallows `<name…>` as a tag; the corrupted DOM is then re-read by `highlight()` (`innerText`) and folded into `props.text` by `onInput` on the next keystroke → `onSubmit` emits it (:134). Fires with no attacker present. Shares KAD-1's `textContent` cure — booked separately so the sink is fixed independent of the security leg. **→ KF.W5**.
- **KAD-3 — MAJOR · syntax highlighting is INERT for the entire authoring session** (R-A MISSED, adopted; sharpens C-7). The always-empty first open (`controlOptionsStore.ts:42 addKeyframes:""`, cleared after every success `useKeyframeOps.ts:185`) makes `highlight()` write `innerHTML=""` and set marker `"true"` (:110/:124); every later `highlightAll()` short-circuits at :117. The only recolour path is `reformat()` (Shift+Alt+F, undiscoverable — see KAD-12). The component's core advertised affordance does not function during typing. **→ KF.W5** (the marker/idempotence redesign) / **KF.W6** (well-swap moots it).
- **KAD-4 — MAJOR · the code surface renders in the PROPORTIONAL UI face, not mono** (R-A MISSED, adopted; cascade re-derived). `text-small` binds `font-family: var(--font-text)` (`@layer utilities`, verified) and beats preflight `pre{…mono}` (`@layer base`); because `highlight()` destroys the `<code>` wrapper, the visible glyphs sit directly in the `<pre>` and inherit the proportional face — so a tab-indented, syntax-"highlighted" CSS editor renders in Plus Jakarta Sans. 2-of-3 with `KeyframeCard.vue:45` (same `hljs`-no-`font-mono` shape); the twin's `font-mono` (redundant there — K-3) is the fix that is *missing* here. **→ KF.W6**. Exact face = residue #4.
- **KAD-5 — MAJOR · floating promises; `reformat()` fails silently on the state the editor lives in** (L-2 / C-4; +R-B's third: bare `setCodeTheme()` per keystroke). `reformat()` (:143) and `animateProgressBar()` (:135) are bare `async` calls; prettier throws on malformed CSS, the rejection has nowhere to go, and every sibling op routes through `withErrorToastAsync` + Retry (`:97/:113/:159`). R-B's third floating promise: `ensureThemeStyle()` calls the async `setCodeTheme()` bare (`useHighlightCSS.ts:104`) on every keydown. Falsifier corrected (both axes): a global `unhandledrejection` listener **does** exist (`useMonacoCancellationGuard.ts:27`, verified) — but it only `preventDefault()`s Monaco `"Canceled"` and never toasts, so the finding survives. **→ KF.W5**.
- **KAD-6 — MAJOR · `DialogDescription` nested inside `DialogTitle` → `<h2><h3>…</h3><p>…</p></h2>`; `DialogHeader` unused** (L-3 / C-2 / D-4). Verified tags: reka `DialogTitle`→h2, `DialogDescription`→p; glass `CardTitle`→h3. Content-model violation + accessible-name pollution (reka wires `aria-labelledby`→title subtree, which now contains the description). `DialogHeader` is in the same barrel, used correctly at `KeyboardShortcutsModal.vue:4-8`; the twin `CSSPasteDialog.vue:11-12` keeps them siblings — the banked twin's **superlative #4** names *this* nesting as its negative. This component's own defect. **→ KF.W7** (the twin fold adopts CSSPasteDialog's sibling composition — R-7 constraint) / KF.W6 standalone.
- **KAD-7 — MAJOR (escalatable) · Tab trapped forward; primary action unreachable by the direction every user tries** (D-2, demoted from BLOCKER — ruled above). `key==="Tab"` `preventDefault()` unconditional (:117-120), no Esc-arm/F6, no `<form>` for Enter. Shift+Tab escapes via FocusScope `loop`; forward Tab does not. WCAG 2.1.1. Residual = SS-13 probe #1 (`pre[contenteditable].tabIndex`). **→ KF.W6** (well-swap restores native Tab; else a documented keyboard exit).
- **KAD-8 — MAJOR · typographic rung inversion — the description is typeset at a heading-family rung** (D-5, ruled above). `text-subheading` (1.272rem/600) on the supporting line vs `text-heading` (1.618rem/700) title. Color+position rank survive. **→ KF.W6**.
- **KAD-9 — MAJOR · the only in-dialog completion signal fires on failure, and there is no in-dialog error surface** (D-6; C-12(iii) sharpener). `onSubmit` (:133-136) emits + `animateProgressBar()` with no gate; the parent closes the dialog only on success (`useKeyframeOps.ts:183`), so on parse failure the sweep runs while nothing was added — and because success unmounts the bar mid-flight, the sweep is *only ever visible on failure*. No `aria-live`/`aria-invalid`/error node. **→ KF.W7** + cross-ref **KF.W2**'s failure-posture registry (as the twin's R-4 did).
- **KAD-10 — MAJOR · async submit, no busy state; re-entry re-appends stops** (D-7). glass `Button` ships `loading`/`aria-busy`/derived-`disabled` (verified d.ts + chunk), none bound (:42-46); `addKeyframesStringToAnimation` awaits `parseAnimationCSS` and loops `addFrame` (`useKeyframeOps.ts:172-177`) with no in-flight guard, so repeated clicks duplicate the stops. **→ KF.W7**.
- **KAD-11 — MAJOR · `prefers-reduced-motion` not honored on the 1000ms width sweep; the engine gate is opt-in** (D-8). `{ duration: 1000 }` only (:128); `respectReducedMotion` defaults `false` (`constants/defaults.ts`, types documented). Not delegable to CSS (WAAPI/rAF inline `width` write). Twin at `KeyframesEditor.vue:254-258` — the fix is a pair. **→ KF.W9** (PRM mechanism unification) + KF.W6.
- **KAD-12 — MAJOR · S-9 — the reformat hotkey hand-rolls a second global keyboard mechanism beside the house `/keyboard` registry** (C-3 / D-23b). Sole `useMagicKeys` site in the demo (verified: 2 hits, both this file); the registry has **19** `registerShortcut` calls (verified) surfaced by `KeyboardShortcutsModal`; this binding enters none of them (only record = the :138 comment), is layout-dependent (macOS `Ï`/US-literal disjunct), and leaks a window listener for the component's life. glass ships `allowInInput` + an `e.code` fallback (verified d.ts) — `registerShortcut("Shift+Alt+KeyF", reformat, {allowInInput:true,label,group})` is the exact cure and dissolves the `Ï` cluster (KAD-16 INFO). New S-row (S-9). **→ KF.W6** (registry adoption).
- **KAD-13 — MAJOR · the `format` prop's JSDoc declares a pure formatter; the wiring secretly writes the parent model, and `reformat()` never emits `update:text`** (L-4 / C-5; +R-B's triple-write MISSED). `:79-80` documents `(raw)=>Promise<string>` "returns the formatted result"; the impl also does `kfControls.addKeyframes = formatted; addKeyframesString.value = formatted` (`useKeyframeOps.ts:149-150`), and `reformat()` (:100-107) emits nothing. **Triple-write** (R-B): the parent already mirrors `addKeyframesString → kfControls.addKeyframes` (`KeyframesEditor.vue:168-169`, verified), so the `:149` write is dead — which makes the L-4/C-5 cure (`emit("update:text", formatted)` + a genuinely pure formatter) cheap. **→ KF.W7** (the fold's text-hoisting contract) / KF.W5.

### MAJOR — folded by reference (identity guard — never re-booked) (3)

- **KAD-F1 — no scroll owner / unbounded content** (D-3 / C-1-scroll / L-2-scroll) → **FOLD to `kf-CSSPasteDialog` R-2** (the 3-mount `DialogContent`-without-`scroll` class; L-2's enumeration counted all three). KeyframesAddDialog is co-site #2. This site's tell: the inert `sticky bottom-0` footer (KAD-16) is the pin-the-actions idiom the author intended, defeated because `scroll` was never turned on. MAJOR-escalatable there. **→ KF.W6**.
- **KAD-F2 — no accessible name / role / `aria-multiline` / focus indicator on the editing host** (D-1) → **FOLD to `kf-CSSPasteDialog` R-6 + R-25** (the 3-surface a11y-posture class: `KeyframeCard` = full instrumentation, this file + CSSPasteDialog = "none"). `:33-39` ships `outline-none` with nothing put back and none of role/aria; the in-folder kill-shot is `KeyframeCard.vue:45-49` (`focus-ring` + `role=textbox` + `aria-multiline` + `:aria-label`, verified verbatim). MAJOR. **→ KF.W6** (four attributes + one class, or the swap).
- **KAD-F3 — 161-line re-authoring of the 80-line `CSSPasteDialog`; hand-rolled `open` v-model** (D-11 / C-6 / L-4-ext) → **FOLD to `kf-CSSPasteDialog` R-7** (the twin fold itself — this is the KeyframesAddDialog-side view). The banked R-7 pinned the fold constraints: adopt CSSPasteDialog's sibling a11y composition (KAD-6) + this file's text-hoisting contract (KAD-13); the genuine deltas that justify *some* bespoke code are four (DialogTrigger, highlighting, Tab-insert, reformat) — none of which requires re-deciding the footer/type-rungs/heading-nesting. `defineModel` half has no defence (six-site house idiom, both siblings follow it). **→ KF.W7**.

### MINOR — component's own (18, clustered)

- **KAD-14 — highlight-machinery cluster** (L-6/C-8, C-7, L-14, L-15/C-9, + R-B dead-export). (a) reformat re-highlights only because the marker `""` is falsy (:110 vs :117) — normalising it to `"true"` silently turns reformat into "inject raw text, never colourise". (b) `highlightAll()` per keydown is a highlighting no-op after mount but rewrites the full github theme `textContent` every keystroke (`:104`, no equality check). (c) `onMounted` (:156-160) is a hand-rolled `{immediate:true}` on the watch above it, both passing an already-owned element. (d) `#highlightjs-theme` is an adopt-or-create singleton removed unconditionally on unmount with no refcount — **latent-only** (child/parent co-terminate; `useCodeHighlight` has exactly 2 consumers and `KeyframesEditor` exactly 1 mount, verified — R-B's kill of C-9's "two mount sites" holds). Fold identity → T `lane-18-brittle-selectors.md:37` #6. (e) `useHighlightCSS` exports `highlight`, used by no consumer (R-B). **→ KF.W5**.
- **KAD-15 — progress-bar cluster** (L-12/C-12, D-20, + R-A scaleX + R-A footer-placement, L-11). `animateProgressBar` and `onKeyDown` are byte-twins of `KeyframesEditor.vue:218-230,254-258` on two engine idioms (`loadAnimationEngine()` child vs warmed `kfEngine()` parent); the presentation half was already folded to `design-idioms.css:131-144`, the animator left behind. `.progress-bar` declares no width, so `w-full` paints it **complete at rest** (D-20). It animates `width` (layout) not `transform: scaleX()` (R-A MISSED — the compositor-safe form that also dissolves the D-26 RTL and D-20 rest-state rows). It is placed **inside** `DialogFooter`, which is *why* `grid` (KAD-16) overrides the footer's flex — hoisting the bar out lets the footer be bare (R-A MISSED, the structural root of D-12). `animateProgressBar` keeps no handle, guards before the `await` then re-reads the null-able ref after it (`[null]` reaches the engine intact) — latent (boot-warm) — L-11. **→ KF.W6/W7**; the animator fold and RTL referral ride design-idioms.css's owner.
- **KAD-16 — dead-token cluster** (D-12/D-18/D-14/D-19 + R-A `relative`). `:41` `class="sticky bottom-0 class grid"`: literal `class` (defined by nothing), `grid` defeats DialogFooter's flex (last-wins `cn` merge, verified), `sticky bottom-0` inert (no scrolling ancestor, `position:fixed`). `:50` bare `bottom` (no such utility/rule). `:37` `css` (defined by nothing; the language is passed explicitly at `useHighlightCSS.ts:122`) + `relative` (no abs-positioned descendant — inert, unlike KeyframeCard where it anchors the overlay — R-A MISSED). `:43` `type="submit"` with no `<form>` in the portalled tree (also removes the Enter workaround for KAD-7). **→ KF.W6/W7** (die in the fold/swap).
- **KAD-17 — caret/selection cluster** (L-7, C-13, L-8, L-9). `collapseToEnd()` (:106) runs synchronously before the microtask `innerHTML` write it targets — a no-op (L-7); it reaches global `window.getSelection()` — exactly what the util it imports (`contenteditable.ts:8-10 target.ownerDocument.defaultView`) exists to avoid (C-13). `insertTabAtCursor` fires no `input` event, so a Tab as the last edit is dropped from the model (self-heals next keystroke) — L-8. The tab is four U+00A0; the BLOCKER version (mangled parse) is dead (value.js tokenizer `/\s/` matches U+00A0 — verified, identity-fold to the twin's L-9/K-1); the indent-compounding survives MINOR (postcss excludes U+00A0). **→ KF.W5/W6**.
- **KAD-18 — MINOR · `bg-transparent` on the `<pre>` is dead** (D-13) — the runtime-injected unlayered hljs `.hljs{background:#0d1117}` wins, and its death is what keeps the contrast decidable (12.3:1 / 6.2:1 recomputed by both readers). Fold identity to the twin's D-13 (`bg-muted/50` = the honest version). **→ KF.W6**.
- **KAD-19 — MINOR · long lines escape the panel** (D-10) — the `<pre>` has no `overflow-x`; hljs's `pre code.hljs{overflow-x:auto}` never matches (`hljs` is on the `<pre>`, and the `<code>` is destroyed). Same overflow class as the twin's R-26. **→ KF.W6**.
- **KAD-20 — MINOR · no empty state** (D-21) — the always-blank state after success/first open renders a 25vh slab with no placeholder/prompt/example; no accessible name to fall back on (KAD-F2). **→ KF.W6/W7**.
- **KAD-21 — MINOR · prose: the description restates the title; capitalization disagrees with itself** (D-22) — "Add keyframes"/"Add keyframes to the animation"/"Add Keyframes" (:25/:29/:45); the one explanatory slot spends zero information at the moment the input contract (append-not-merge) is non-obvious. **→ KF.W7** (copy).
- **KAD-22 — MINOR · three focus idioms + one absence across the four affordances** (D-23a) — trigger keeps the UA outline by accident, `<pre>` suppresses with no substitute (KAD-F2), footer Button gets glass `focus-ring` free, toolbar sibling uses `focus-visible:ring-2 ring-accent`. `.focus-ring` is the demo's "SINGLE keyboard-focus affordance." **→ KF.W6**.
- **KAD-23 — MINOR · the one confirming action ships at default (secondary) emphasis** (R-A MISSED, adopted) — a one-action dialog never declares its primary; glass `Button` documents `emphasis: primary|secondary|quiet|text` with a distinct primary rung. `CSSPasteDialog.vue:21` shares the omission (2-site). **→ KF.W6**.
- **KAD-24 — MINOR · the demo's lazy-loading seam for this subtree is DEAD CODE** (R-B MISSED, adopted) — `keyframes/index.ts` and `instrument/index.ts` wrap this subtree in `defineAsyncComponent` "so the umbrella never eager-loads the Monaco/highlight.js chunk," but neither barrel is imported anywhere (verified: `grep 'from "@components/instrument"'` → 0; the umbrella's only inbound ref is its own re-export). The real mount bypasses both (`SpringPhysicsFacet.vue` direct import), so the component is statically reachable and two modules + their documented invariant are unreachable. Not this component's defect per se — its subtree's. **→ KF.W8** (structure/colocation).
- **KAD-25 — MINOR · rich contenteditable, no paste handling, no `plaintext-only`** (R-B MISSED, adopted) — a paste from any highlighted page inserts foreign `<span style>` markup; the marker-gated highlight (KAD-14) never re-normalises, so pasted colours sit over the theme for the dialog's life. `contenteditable="plaintext-only"` (or the swap) is the primitive for a code well. Relates to the twin's R-1 (contenteditable↔VDOM class). **→ KF.W6** (swap).

### MINOR — folded by reference (2)

- **KAD-F4 — no `spellcheck="false"` / `autocorrect` / `autocapitalize` / `dir` on a source-code region** (R-A MISSED) → **FOLD to `kf-CSSPasteDialog` R-20** (the 3-surface class: `grep spellcheck|autocorrect|autocapitalize demo/` → 0 repo-wide, verified; all three contenteditable surfaces share it). On iOS Safari the platform mutates the typed CSS (auto-capitalise after `;`/`{`, red-squiggle idents) and `onInput` emits the mutated `innerText` into the model. R-A grades MAJOR; the banked identity is MINOR — folded at MINOR for consistency, R-A's MAJOR preserved in DISSENT. **→ KF.W6** (attrs) / **KF.W9** (iOS witness).
- **KAD-F5 — `min-h-[25vh]`: `vh` where the repo owns `dvh`, arbitrary bracket where a token exists** (D-15) → **FOLD to `kf-CSSPasteDialog` R-10** (the T-tranche `min-h-[Nvh]` carry, `17-styles-idioms.md:345`); this site is 25vh, the twin 20vh. **→ KF.W6**.

### INFO / fold-by-reference (identity guard — never re-booked)

- **F-1 phantom dependency** (L-5 / C-11 / D fold-forward): census row RED, ruled "F-1 first." All six glass imports ride it; L-5's MAJOR arm collapses to MINOR-on-F-1 (ruled above). **FOLD by reference → census F-1** (KF.W0/W6 precondition). Not re-booked.
- **Root-barrel import** (C-9 / L-5-arm): the demo's standing pattern (glass `sideEffects:["*.css"]`, root barrel dominant). Non-defect — matches the twin's C-9.
- **Zero test coverage** (R-B MISSED): `grep KeyframesAddDialog test/` → 0 (verified) despite a 9-suite demo shelf. Fold identity → the twin's C-10. **→ KF.W8** (test-tree isomorphism); the two invariants a characterization test pins are the falsy-`""` marker (KAD-14) and the undocumented `format`-writes-model requirement (KAD-13).
- **`Ï` cluster** (L-10 / C-14 / D-24): `Ï` untypeable in a CSS editor (`content:"Ï"` legal), the literal triplicated with no rationale, the bare-`Ï` watch arm asserts no modifier. Dissolves entirely under KAD-12's `e.code` registry. **→ KF.W6**.
- **`R1` value.js throw class** (C's K-5): CONTAINED — this component's one value.js edge (submit → `resolveKeyframes`) is the diagnostics-returning path, not the R1 crash surface. Cleared — do not re-file.
- **Colocation** (L-17): `keyframes/components/` holds single-owner children; fold → V `R1-05-demo-colocation.md:118`. Module size Goldilocks-clean (161 lines). **→ KF.W8**.
- **RTL** (D-26): `.progress-bar` `linear-gradient(to right,…)` is the one physical dependency — out-of-file referral to design-idioms.css's owner; theoretical today (no `dir`/RTL policy). Dissolved by KAD-15's `scaleX` cure.

## Killed-claims register — 12 rows

- **K-1 · D-16 "no gap between button label and icon" (zero separation)** (R-A). KILLED — probed the wrong stylesheet. The Button base is `components/button/styles.css`, whose `.button{gap: calc(0.375rem*var(--ui-scale))}` = **6px** at scale 1 (verified by this seat), applied unconditionally. Survives as an INFO 2px inconsistency with the twin's explicit `gap-2` (8px).
- **K-2 · D-17(:45 half) / C-15 (submit FileIcon clause)** (R-A). KILLED for :45 — the submit `FileIcon` is a direct child of glass `Button` and is sized by `.button > svg:not([class*="size-"]){inline-size: var(--ui-glyph)}` = **16px** (`--ui-glyph: calc(1rem*var(--ui-scale))`, verified). Adding `icon-md` would be a no-op-to-regression. The :12 `FilePlus2` half (inside a hand-rolled `<button>`, not `.button`) survives inside KAD-F2/KAD-16.
- **K-3 · D-2's tabbable-order evidence + the BLOCKER rung** (both readers). KILLED — the shipped chunk renders the close X **after** the default slot (`default: [slot, showClose ? closeX : …]`, verified), so the `<pre>` is the FIRST tabbable, not a middle one, and FocusScope's `loop` rescues Shift+Tab. Forward-Tab / 2.1.1 arm survives (KAD-7). *This seat notes for the fleet*: the twin's R-B misread this exact slot order (K-9 there); here both readers got it right.
- **K-4 · C-2's CardTitle-secondary clause** (R-A). KILLED — `.card-title` carries the full ramp (`min-width:0; font-size:var(--type-heading); overflow-wrap:anywhere; …`, verified in `components/card/styles.css`), so the prescribed cure ("use the `text-heading` utility instead") would regress `min-width:0` + `overflow-wrap:anywhere`. Residual, sharpened: the author's `class="text-heading"` only changes the weight (700 vs semibold), so the CardTitle borrow into a dialog is near-redundant and **unexplained** — more arbitrary than C-2 argued, not less.
- **K-5 · D-25's "exactly one `forced-colors` block"** (R-A). KILLED as stated — four glass dist files carry `forced-colors`, and the `focus-ring` submit Button IS covered. The substantive observation (the `border-none` `<pre>` has no boundary once backgrounds are forced) survives as the D-25 residual (INFO), sharpened by the contrast.
- **K-6 · L-2 / C-4 falsifier ("no `unhandledrejection` listener anywhere")** (both readers). KILLED — `useMonacoCancellationGuard.ts:27` registers a real window-level `unhandledrejection` listener (verified). The FINDING survives (it only `preventDefault()`s Monaco `"Canceled"` — `:20-28` — and never toasts), but the published falsifier is false in both axes; recorded so an arbiter does not conclude the demo has no rejection surface.
- **K-7 · L-3's folded sub-note (the `dialog/styles.css` √φ-interval comment)** (R-B). KILLED on provenance — the installed package ships no `src/`, and `dist/components/dialog/` has no `dialog/styles.css`; the quoted comment is unreadable from this tree (source-over-dist, the error the corpus itself warns about). The rendered-DOM half of L-3 is unaffected (KAD-6).
- **K-8 · L-5's root-barrel MAJOR rung** (R-B). KILLED/collapsed to MINOR-on-F-1 — glass `sideEffects:["*.css"]` (barrel tree-shakeable) + the root barrel is the demo's dominant idiom (31 sites); grading a repo-wide house choice as a MAJOR against one 161-line component mislocates it.
- **K-9 · C-9's "two mount sites" second leg** (R-B). KILLED — there is exactly ONE mount site (`SpringPhysicsFacet`); the async umbrella barrels have zero importers (verified). L-15's latent-only framing is correct; the hazard stands as latent. Feeds the dead-lazy-seam row (KAD-24).
- **K-10 · D-14's "four global class tokens" (`scale-on-hover` as demo-local)** (R-B). KILLED for `scale-on-hover` — it is a glass-owned Tailwind `@utility` (`btn.css:1`, verified), consuming a design-system utility, not the flat-namespace hazard. The three-token version (`hljs`, `css`, `progress-bar`) survives inside KAD-16; the `css`-defined-by-nothing core is confirmed.
- **K-11 · L-9's BLOCKER (nbsp corrupts parse) + the corpus's own K-1..K-5** (both readers). Re-verified correct against the tree and recorded so nobody re-derives them: value.js `/\s/` matches U+00A0 (submit path safe); `@lucide/vue` is declared (`package.json:74`, not a phantom); `scale-on-hover` real; per-site `loadAnimationEngine()` is the documented default; the R1 throw class is not on this path.
- **K-12 · L-16 (the contenteditable/`v-model` caret race BLOCKER)** — self-killed by the parent's pre-boot (verified: `KeyframesEditor` boots the highlighter at its own mount). Recorded that the SAME node-clobber has a *second* consequence the pre-boot does NOT close — the marker short-circuit (KAD-3, highlighting inert) — so L-16's kill does not cover KAD-3.

## Superlatives — 7, with provenance

1. **`toastGuard.ts` / `isInsideToaster`** (D-S1/S-1/SUP-1, all three axes + both readers; reka dispatch re-walked). 17-line docblock naming the private `data-sonner-toaster` attribute, the pinned dep+version, why no public predicate exists, the single-file blast radius, and the exit condition. Single-sourced. This component consumes it correctly at :17-22 (reka dispatches `interactOutside` on the original click target, so `event.target` is the clicked node). Fold identity to the twin's superlative #3 — re-awarded for this component's consumption.
2. **`onSubmit` refuses to close its own dialog** (L-SUP-3/S-3/SUP-3). It emits + animates, nothing else; the close is owned exclusively by the parent's SUCCESS branch (`useKeyframeOps.ts:183`), so a failed parse leaves the dialog open, the draft intact, a Retry toast up. The single best design decision in the file — and the D axis, whose lens it most belongs to, missed it while grading the same seam's other half (KAD-9).
3. **`useCodeHighlight(() => [addKeyframesEl.value])` — the scoped-ownership contract** (S-3/SUP-... /S-C). The composable records the bug it cures in its own source (`useHighlightCSS.ts:68-76`: "never the whole document (D.W3.S1: the global `document.querySelectorAll("pre")` was the bug)"); the getter closes over one `useTemplateRef`, so the owned set empties itself on unmount. (Sharp irony: the *right* ownership discipline wraps a sink KAD-1 makes unsafe.)
4. **`contenteditable.ts` resolves the selection through `target.ownerDocument.defaultView.getSelection()`** (S-2/SUP-2/S-D), null-guarding both view and `rangeCount`, with the rationale in the docblock — the correct discipline for a portalled subtree. Honestly docked: the consumer reaches global `window.getSelection()` seven lines away (:106, KAD-17).
5. **The trigger's DOM shape is load-bearing for `useToolbarKeyboard`'s roving tabindex** (SUP-3/S-E). A real `<button aria-label="Add keyframes">` through `DialogTrigger as-child` is exactly what the container-based roving tabindex depends on (the composable's docblock names this component); the portal correctly keeps the submit and close buttons OUT of the toolbar cohort. Both halves right; C-10's proposed glass-`Button` swap preserves it.
6. **The heavy-engine edge honours the value.js firewall** (SUP-2/S-5). `loadAnimationEngine` from the LIGHT barrel, awaited at the point of need (`:71/:127`), the documented DEFAULT (`kf-engine.ts:12-13`); no static `@mkbabb/value.js/*`, no deep `@src/animation/engine/*`. The submit path (→ `resolveKeyframes`) is the diagnostics-returning, best-hardened value.js edge in the editor — the precise contrast that makes KAD-5 a defect (the reformat path in the same stack skipped the discipline).
7. **The corpus published its own failed attacks** (D §5 / L's L-9-FALSIFIED + L-16 dead BLOCKERs + K-1..K-5 / C's K-1..K-5), and both re-readers then killed more of each other and of themselves — R-A killing its own axis-mate's certainty language, R-B convicting D of source-drift while (here) correctly reading the dist slot order. The record's trustworthiness comes from this visible mortality.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **D-2/KAD-7 residual** — does `pre[contenteditable].tabIndex >= 0` in the target engines? Decides whether the primary action is reachable at all and whether KAD-7 promotes toward BLOCKER. Tab and Shift+Tab out of the editor; report where focus lands. Per-engine (chromium / webkit / safari-app are separate cells).
2. **KAD-1 blast radius** — a demo-context severity read: on the CF-Pages origin with no auth/cookies of value, what does the executed script reach? The mechanism is proven; the impact ceiling is a severity discussion, not a mechanism gap.
3. **KAD-F1 / KAD-19 overflow geometry** — the exact content height at which the footer leaves the viewport; the horizontal paint of a long single declaration over the scrim.
4. **KAD-3 / KAD-4 / D-13 — one computed-style read on the `<pre>` settles all three**: confirm the injected unlayered `.hljs` background wins AND `text-small`'s `font-family: var(--font-text)` resolves as the rendered face during authoring. Cascade is static; the resolved `getComputedStyle` is not.
5. **C-7 recalc magnitude** — the cost of rewriting the full github-dark payload into a head `<style>` per keystroke. Path static, cost live.
6. **C-10 visual equivalence** — `<Button emphasis="text" iconOnly>` vs the eight hand-rolled trigger utilities at the toolbar's `h-10`.
7. **KAD-9 / C-12(iii)** — whether any part of the 1000ms sweep is visible before reka's exit Presence unmounts the node on the happy path.
8. **L-7 caret landing** — where `collapseToEnd()` leaves the caret after the microtask deletes its anchor node. Ordering proven; landing engine-dependent.
9. **L-9 indent compounding** — the visible double-indent across repeated Shift+Alt+F. Tokenizer asymmetry static; the pixels not.
10. **KAD-F4 iOS behaviour** — the precise iOS Safari `autocapitalize`/`autocorrect` mutation inside a `contenteditable pre`. Zero-hit attribute census static; the mutation needs real iOS Safari (KF.W9's cell).

## DISSENT — preserved, not ruled away

1. **The XSS stays BLOCKER** (vs the twin's demoted pair). Recorded as this seat's deliberate distinction: the twin demoted because each BLOCKER rested on an author-marked-unproven half; here no half is unproven and the CSP hatch is verified closed. If SS-13 residue #2 finds the executing origin genuinely inert (no auth, no adjacent same-site value), a future seat may argue a context demotion — but the *sink* is a fix-before-adoption defect regardless, and KAD-2 (data corruption) forces the same one-line cure independent of any security framing.
2. **R-A's proportional-face + highlighting-inert MISSED at MAJOR** — adopted by this seat over the silence of L and C. They overlap C-7, which under-graded the same mechanism as MINOR-as-perf; the record credits C-7's partial and books the sharper case at MAJOR.
3. **R-A's MAJOR on the spellcheck class** — preserved against the folded MINOR (KAD-F4). The banked twin R-20 is MINOR; consistency argues MINOR; R-A's instinct that a source-code surface mutated by the platform on a first-class mobile target is more than cosmetic is recorded, and KF.W9's iOS witness may revive it.
4. **Reader factual mortality, recorded as fleet evidence** — R-B's "GitHub Pages" deploy premise (wrong; it is Cloudflare Pages) reaching the right no-CSP conclusion joins the twin's K-9 (R-B's dist-order misread) and the ColorSpaceSelector citation-inheritance event as evidence that "verified" cells require artifact excerpts, not assertions. Here the error was harmless because a second, independent leg (no `_headers` file) closed the hatch; spec authoring should still require the excerpt.

## Closing verdict

**ADJUDICATED — 1 BLOCKER (the `innerHTML` DOM-XSS sink; STAYS, unlike the twin's demoted pair — no unproven half, CSP verified closed) · 15 MAJOR (12 own incl. two upheld MISSED — highlighting-inert + proportional-face — and the innerHTML-corrupts-CSS twin of the sink; 3 folded to the banked twin's R-2/R-6+R-25/R-7) · 20 MINOR (18 own, clustered; 2 folded) · 7 INFO/fold-by-reference · 12 killed · 7 superlatives · 10 UNPROVEN-NEEDS-LIVE residue rows. The component is the redundant re-author of `CSSPasteDialog`; the cure spine is the one-line sink fix (`textContent`, FRONT-LOADED, KF.W5), the twin fold (KF.W7, constraints pinned by the banked R-7), registry adoption for the reformat shortcut (KF.W6, dissolving the `Ï` cluster), and the failure-posture + busy-state contract (KF.W7 joined to KF.W2's registry). F-1 / R1-containment / min-h-vh / the three 3-surface a11y-and-scroll classes fold by reference, never re-booked. NO-WAVE-OWNER rows for SS-1/SS-2 spec authoring: the whether-to-keep-hljs-at-all question (highlighting is inert during authoring, so a swap sacrifices nothing real) and the spellcheck severity re-grade.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **KeyframesAddDialog.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

