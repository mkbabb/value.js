claude-fable-5

# fr-CollapsibleSection — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/ui/CollapsibleSection.vue` (72 lines, read whole) + its four callsites, its ContourSettings fork twin, and the glass-ui 4.0.0→7.0.0 disclosure chain.
**Substrate**: fourier HEAD `cd26c65` / branch `m/w1-bump-migration` — re-pinned by this seat (`git log -1`); the subject file matches every anchor used below at the working tree. Installed glass-ui **4.0.0** / producer glass-ui **7.0.0** at `/Users/mkbabb/Programming/glass-ui` HEAD `7d9abe08` (both re-pinned); installed value.js 0.13.0; installed reka-ui 2.x dists read directly.
**Corpus**: 3 challenge files (D design 518 lines · L library 537 · C consumption 562) at `docs/tranches/V/megatranche/audit/fourier-components/CollapsibleSection/` — ~70 accusation rows, 4 pairwise-disjoint BLOCKER claims, 2 second-pass addenda.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read), returns via the orchestration channel; the challenge files sit immutable beside this record.
**Method (M-23/L-18)**: both readers were attacked before either was adopted. Every kill was re-derived (one reader kill was RESCOPED, none un-killed); every adopted missed find was re-proven with this seat's own instruments; all six verdict-level reader-vs-reader conflicts were ruled on fresh reads. **Two citation-inheritance events were caught inside reader-LC's confirmation cells** (the nonexistent `--spring-smooth-*` tokens; the "canonical section trigger" 2.75rem cell) — recorded in DISSENT as the fleet lesson. One identity-guard catch overruled BOTH readers (C-1 is already banked).
**Arbiter's own instruments this session (all static + node against source and installed dists; zero browser probes — probe parsimony; product source read-only)**: full read of CollapsibleSection.vue (72); glass-ui 7.0.0: `src/components/_shared/disclosure/disclosure.css` (WHOLE — `@keyframes disclosure-open`:1 / `disclosure-close`:13 / `@layer components`:25 / `.disclosure-trigger` = radius+touch-action+focus+disabled only / `.disclosure-group-trigger` = flex+2.75rem floor+1rem padding+hover `--control-surface-bg-hover` / `.disclosure-content` `animation-duration: var(--spring-present-duration)` + `--disclosure-content-size: var(--reka-collapsible-content-height)` + `animation-name: disclosure-open/close` / `.disclosure-content-body { padding: 0 0.25rem 1rem }` / **the producer's own PRM carve: `@media (prefers-reduced-motion: reduce) { .disclosure-content { animation-duration: 0.01ms } }`** / a forced-colors block), `grep -rn spring-smooth src/styles/` → **0 hits**, `scheme-spring.css:103` `--spring-present-settle: 0.2s` + `:109` `--spring-present-duration: calc(settle × tempo)`, `scheme-motion.css:264` `--motion-tempo: 1` (+ the `:248` consumer-knob prose), `grep collapsible src/styles/` → 0 + `grep "@keyframes collapsible" src/` → 0, `Collapsible.vue` (whole head — props `{open?, defaultOpen?, disabled?, class?}`, `inheritAttrs: false`, `forwardedAttrs` destructures OUT `as/asChild/as-child/unmountOnHide/unmount-on-hide`, single `<RekaCollapsibleRoot as="div">`), `CollapsibleContent.vue` (whole — `forceMount?` :12 → `:force-mount` :45, `role="region"` :47 + `:aria-labelledby="ids.trigger"` :48, `cn('disclosure-content', props.class)` :49), `CollapsibleTrigger.vue:30-41` (`:id`/`:aria-controls`, `cn('disclosure-trigger', …)` :38), `AccordionTrigger.vue:41` (`<RekaAccordionHeader as="h3" class="disclosure-header">`) + `:48` (`disclosure-trigger disclosure-group-trigger`), `AccordionContent.vue:50` (`disclosure-content-body`), package.json (7.0.0; peers value.js ^4.0.0 / keyframes ^6.0.0 / pencil-boil ^0.11.2 / @lucide/vue ^1.16.0); reka-ui installed dist: `Presence/usePresence.js` (WHOLE, 122 — the state machine :14-24, the `"none"` short-circuit :44, `isAnimating` :55 → `ANIMATION_OUT` :57, `handleAnimationEnd` :71-87 bound :95-96, node-null arm :98, state watcher :105-108, `isPresent` :113, `getAnimationName` :116-118), `shared/useForwardProps.js` (WHOLE, 37 — defaultProps admits only non-undefined `.default` :13-17, preservedProps = vnode.props :20-24, key-set union :25-31), `Collapsible/CollapsibleRoot.js` (`unmountOnHide: {type: Boolean, required: false, default: true}`), `Collapsible/CollapsibleContent.js` (`beforematch` :64, `hidden: !present ? (unmountOnHide ? "" : "until-found") : void 0` :82, `(unmountOnHide ? present : true) ? renderSlot : createCommentVNode` :90); installed glass-ui 4.0.0 dist: `CollapsibleContent-C_s6fG7r.js` head (`unmountOnHide: { type: Boolean }` — **no `default` field**; `useForwardPropsEmits`; single `createBlock`; trigger recipe verbatim `tap-squish focus-ring rounded-control transition-control disabled:pointer-events-none disabled:opacity-disabled`; `animate-collapsible` literal class strings present ×1), `styles/animations.css:18/:29` (`@keyframes collapsible-open/close`), `styles/index.css:195-222` (the FULL `@source "../*.js"` comment — reader-LC's quote is verbatim-accurate), `styles/utilities/btn.css:55` `@utility transition-control` + `:67` `@utility transition-collapse` + **the :60-65 comment: the collapse timing "rides the data-state `animate-accordion-*`/`animate-collapsible-*` keyframes"** (a producer-designed-for-emission cell neither reader held), `grep transition-collapse|transition-control dist/styles/components.css dist/glass-ui.css` → **0 and 0**, `utilities/base.css:174-178` (`.focus-ring:focus-visible` = outline:none + `box-shadow: var(--focus-ring-shadow)`), `tokens/scale-paper.css:66-68` (the two-leg color-mix ring shadow), `utilities/a11y-overrides.css:1-20` (PRM blanket rewrites `animation-duration/iteration-count` + `transition-duration/property` only), `typography/utilities.css:65-67` (`@utility cm-serif { font-family: var(--font-serif-math, serif) }`), `tokens/scheme-motion.css:67` `--duration-fast: 0.2s` + `:217` `--ease-out: var(--motion-ease-out)`, `dist/styles/components.css` `rounded-control` → **1 compiled rule**; **tw-animate-css installed dist read: `--animate-collapsible-down/up` `@theme inline` keys + `@keyframes collapsible-down/up` (reading `--reka-collapsible-content-height`) ARE in the imported artifact** — a second new cell on the hinge; fourier: `git diff web/package.json` (committed pins glass ^3.1.0 / keyframes ^2.2.0 / value.js ^0.10.0; the bump is uncommitted M.W1a work), installed glass 4.0.0 `peerDependencies["@mkbabb/value.js"] = "^0.10.0 || ^0.11.0"` + `peerDependenciesMeta` optional, **stale-dist re-run**: `web/dist/assets/*.css` all `Jun 12 18:13` vs installed glass-ui pkg `Jun 17 21:48`, `grep -c rounded-control web/dist/assets/*.css` → **0 across all 8**, `.gitignore:42 web/dist/`; census greps: `<CollapsibleSection` → **4 usages / 3 files** (ContourPreview:34, EqCoefficientsPanel:13, FunctionInput:94/:176), `#actions|v-slot:actions` → **0**, `matchMedia` → **3 sites** (router/index.ts:15-17 named helper — read; PaperView:176; SvgFilters:9), `aria-hidden="true"` → **24**, `role="heading"|aria-level` → **0**, overflow-literal census → 6 hits with **only App.vue:26 `<main …overflow-y-auto>` an ancestor** (CoefficientsSpectrum:78 a descendant), `motion-tempo` in web/src → **0**; EquationView.vue:210-216 (`<Transition name="slide-down">` + `v-if="components.length"`) + `:353-361` (`.eq-panel-left-wrap` **`overflow-y: clip`** :358, mobile flip :361) + `:373-375` (`.eq-panel-left` `@apply … overflow-y-auto` — scoped, never in a class attribute); VisualizationView.vue:252-262 (`panel-swap` out-in remount; ContourPreview inside `v-if="isEditing"`) + `:363-371` (`.viz-panel-left` — NO overflow declaration); ContourSettings.vue:253-268 (the fork rebuild) + `:348-376` (`.advanced-trigger:hover` :350-352; the B.W2.d comment :354-360 — DIFFERENT prose from A.W3.d, citing it as prior art; rules :361-375 identical to the subject's :54-71 modulo class name); CoefficientsSpectrum.vue:35 (`expanded = ref(false)`) / :37-39 (slice 12↔40) / :122-134 (Show-more button); callsite wrappers all `div.cartoon-card px-3 py-2` (my reads) with body gutters `pt-1`/`pt-1`/`p-2`/none; FunctionInput.vue:149-166 (Compute Button + the `flex flex-wrap gap-1.5` preset-pill row at x=0, `Tooltip v-for` at :157); style.css:1-3 (imports incl. `@import "@mkbabb/glass-ui/styles"`), :13-15 (`--font-sans:` Computer Modern), :17-22 (`font-serif` body), :136-143 (the `:focus-visible` enumeration — `.collapsible-trigger` absent); package.json scripts (no vitest, no lint) + devDeps (lucide-vue-next ^1.0.0, reka-ui, cva, clsx, tailwind-merge all dev); e2e = 8 specs; **registry identity sweep** over `adjudicated/*.md` for the peer-deadlock / lucide / `--font-serif-math` / `web/dist` — three folds found (fr-AnimationControls C-1; fr-AppHeader FR-AH-33 + GLASS-RELAY; fr-BasisSelector m-3/m-9).

## Scoreboard

**31 adjudicated register rows (1 BLOCKER · 7 MAJOR · 16 MINOR · 7 INFO) + 3 identities FOLDED BY REFERENCE (never re-booked) · 11 kills in the killed register · 11 missed finds banked (5 folded into composite rows) · 8 superlatives with provenance.**

Blocker-weight survivor: **B-1** — the unlayered scoped `animation` shorthand names keyframes glass-ui 7.0.0 deletes, and the consequence at the F.W1 target is not a lost animation but a **functional hang**: reka's `usePresence` waits forever in `unmountSuspended` for an `animationend` no dead keyframe name can fire, so all four sections (and the ContourSettings fork twin — five disclosure surfaces) **visually never close**. Re-derived by this seat from the reka state machine, not adopted.

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **D/M-6's fate** (the "250 ms is 100 ms short at 7.0.0; 350 ms clock" claim) | KILLED three ways | CONFIRMED the mechanism inside its L-7 row ("glass 7.0.0's disclosure.css:102-103 drives the animation from `--spring-smooth-duration`… I confirm its mechanism at the target pin") | **DU wins — KILLED (K-1). LC's confirmation cell is citation-inheritance and dies with it.** | My greps and reads: `spring-smooth` over glass 7.0.0 `src/styles/` → **0 hits**; disclosure.css's only clock is `animation-duration: var(--spring-present-duration)` = `calc(0.2s × --motion-tempo)` (scheme-spring.css:103/:109; tempo default 1 at scheme-motion.css:264) = **200 ms**, ≤ today's 0.2s — the 250 stays a 50 ms lead. LC hedged ("I did not independently re-derive D's exact 0.35s figure") yet asserted the token's existence at cited lines; the token does not exist anywhere in the producer tree. Residual: `--motion-tempo` is a documented consumer knob (fourier overrides 0) — an INFO rider on m-11, not an F.W1 gate. |
| R-2 | **B-1's consequence and grade** | Missed-find BLOCKER: the reka `unmountSuspended` HANG; "a hard gate, not a screenshot-diff row" | MAJOR: "a FUTURE regression contingent on an uplift that has not landed"; the hang unexamined | **DU wins on the consequence — the hang is real (this seat's own full usePresence read). Grade: BLOCKER, uplift-conditional; LC's conditionality is adopted as the qualifier, not the demotion.** | The trace, re-derived: `getComputedStyle(node).animationName` returns the **specified ident** for a dead keyframe name, so the `=== "none"` short-circuit (`:44`) is not taken; `prevAnimationNameRef` holds `"collapsible-open"` (state watcher :105-108), current is `"collapsible-close"` → `isAnimating` true (:55) → `ANIMATION_OUT` (:57) → `unmountSuspended`, whose ONLY exit is `ANIMATION_END` from real `animationend`/`animationcancel` events (:71-87, bound :95-96) or node-null (:98). No matching `@keyframes` ⇒ no animation ⇒ no event ⇒ `isPresent` (:113) true forever ⇒ `CollapsibleContent.js:82` emits `hidden: void 0` and `:90` keeps the slot rendered. Nothing else sets height 0 at 7.0.0 (disclosure.css is animation-driven only — my whole-file read). Five surfaces: 4 callsites + ContourSettings :364-369, byte-same hazard. |
| R-3 | **C-1's booking** (the value.js 0.13.0 / glass 4.0.0 peer deadlock) | CONFIRMED BLOCKER as filed | MAJOR with two attribution corrections (manifest-owned, not component-owned; working-tree-only) | **BOTH overruled on identity — FOLD BY REFERENCE to the banked fr-AnimationControls C-1 (this seat's identity-guard catch). LC's two corrections adopted as riders on that identity.** | My registry sweep: the peer-range violation is banked at fr-AnimationControls C-1 (census §1 pins the identity, G33 `npm ls` gate) and fr-AppHeader **already demoted its own reader's re-raise to this same fold on 2026-08-07**. My `git diff web/package.json`: committed pins are glass `^3.1.0` / keyframes `^2.2.0` / value.js `^0.10.0`; the deadlock manifest is the uncommitted M.W1a bump. One identity, one booking, forever. |
| R-4 | **B-2's grade** (four headings with zero heading semantics) | BLOCKER (WCAG 1.3.1 Level A, F2/H42, primary control surface) | MAJOR ("a Collapsible is not an Accordion; the disclosure itself is correctly exposed") | **MAJOR.** DU's dissent preserved. | Operability is intact (reka supplies `aria-expanded`/`aria-controls`/`type="button"` — verified in the installed trigger emission); the defect is information-structure, not lockout; and the shipped cure is a **design decision** (the Accordion path carries `as="h3"` — my AccordionTrigger.vue:41 read; see i-4), which the altitude precedent (fr-BasisSelector R-6) grades MAJOR, not BLOCKER. |
| R-5 | **The scope-id question** (does `data-v-16a925e2` reach the content element?) | Left UNPROVEN (#2 in its residue) | KILLED the SS-13 tag: statically decidable, resolves POSITIVE via the `setScopeId` recursion trace | **LC's resolution ADOPTED-AS-RESCOPED — the trace is the best static argument available and every hop it needs is single-root (this seat re-read all five); but its corroboration cell is STRUCK as circular** (the compiled selector `.collapsible-content[data-v-16a925e2]` only proves the SFC compiler emits the attribute selector — that is the reason the question matters, not evidence the attribute lands — and it comes from the same stale artifact both readers killed). A thin SS-13 residue is retained at trivial cost. | What NEITHER reader stated, and this seat adds: **B-1's hang is coupled to this question** — the post-uplift hang requires the scoped rule to MATCH, i.e. the scope id to land. If it lands (the adopted reading): hang at 7.0.0. If it does not: the scoped block is dead today too, today's animation rides the unproven substrate-utility emission (M-7), and at 7.0.0 the producer grammar simply works. **The F.W1 instruction is identical on both branches** — delete/replace the block and witness close-completion — so the routing is robust to the residue; the residue's cheapest witness is one frame of today's collapse animating (≙ scope-id proof). |
| R-6 | **D/m-8's producer provenance** (the 44 px floor) | Killed the provenance: the `min-block-size: 2.75rem` recipe is `.disclosure-group-trigger`, applied ONLY by AccordionTrigger — "the producer's answer for a titled section header is 44 px" does not stand | Repeated it: "glass-ui 7.0.0's canonical section trigger sets `min-block-size: 2.75rem` outright (disclosure.css:60 — verified)" | **DU wins.** The 32 px arithmetic survives (m-9); the provenance dies. | My disclosure.css read: the floor, the 1rem padding, and the hover all sit on `.disclosure-group-trigger`; the Collapsible path's `.disclosure-trigger` sets radius + touch-action + focus-visible + disabled ONLY. LC's "verified" verified the line's existence, not the class's applier — the corpus's misattribution, inherited. Second inheritance-shaped event in one return (DISSENT-1). |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · terminal disposition → route**. Full proof cells live in the challenge files and the reader returns, incorporated by reference; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **B-1 = D/B-1 ∘ DU-missed-1 (the hang) — BLOCKER (uplift-conditional; LC's MAJOR dissent preserved as the qualifier).** The scoped, **unlayered** `animation` shorthand (`CollapsibleSection.vue:60-65`) names `collapsible-open`/`collapsible-close`, which exist at the installed pin (glass 4.0.0 `dist/styles/animations.css:18/:29` — my grep) and are **deleted at 7.0.0** (`grep collapsible src/styles/` → 0; the grammar moves to `disclosure-open`/`disclosure-close` inside `@layer components`, shipped via `Collapsible.vue`'s style block). Because the scoped block is unlayered and a SHORTHAND, it beats the layered producer rules for `animation-name`, `animation-duration` AND `animation-timing-function` — the substrate motion never reaches the element while the block survives. The consequence at the F.W1 target is R-2's ruled hang: **the section visually never closes**, on all five disclosure surfaces (4 callsites + the ContourSettings twin, whose rules are character-identical — my diff-read). Cure clauses, all verified this session: (a) the fix is a **deletion**, not a rewrite — the producer's 7.0.0 grammar is static, `@layer`-shipped, and emission-independent; (b) the local PRM guard (`:66-71`, `animation: none`) deletes with it — **the 7.0.0 producer carries its own PRM carve** (`.disclosure-content { animation-duration: 0.01ms }`, my read; see S-4's coda); (c) the A8-17 `onanimationend` cure is sequencing-coupled, not free (K-2); (d) the R-5 coupling: the hang presumes the scope id lands — both branches route identically. **ADJUDICATED → F.W1** (hard gate: the block and its twin die in the uplift commit; witness = close-completes + close-animates on all five surfaces; execution of the broader component rework rides F.W3/W4).

### FOLDED BY REFERENCE (identity guard — banked rows, never re-booked)

- **F-1 = C-1** (`@mkbabb/value.js` 0.13.0 violates installed glass-ui 4.0.0's `peerOptional ^0.10.0 || ^0.11.0`) **= fr-AnimationControls C-1 (banked; G33 `npm ls` gate; → F.W1)** — one identity; fr-AppHeader already folded its re-raise to it. Riders this record contributes: (i) **working-tree provenance** (my `git diff`): the committed manifest pins `^3.1.0`/`^2.2.0`/`^0.10.0` — the ERESOLVE is the uncommitted M.W1a bump, so C-1's correction to lane-frontend §5/§9 carry #5 ("red today, not on uplift") is true of the working tree and only the working tree; (ii) glass 7.0.0's peer set re-verified by this seat (value.js ^4.0.0 / keyframes ^6.0.0 / pencil-boil ^0.11.2 / @lucide/vue ^1.16.0) — the atomic tri-package transaction stands confirmed. The component's stake is one import line (`:2`).
- **F-2 = C-5** (`lucide-vue-next` runtime-imported, declared only in devDependencies; ditto reka-ui/cva/clsx) **= fr-AppHeader FR-AH-33 (→ F.W0 manifest hygiene; rename → F.W1) + fr-BasisSelector m-9** — one manifest identity. Riders: the 35-site `@lucide/vue` rename count is a CURRENT debt (contra lane-frontend §1 row 13); C-5's narrowing of §9 carry #10 to `tailwind-merge` alone stands (my devDeps read); LC's sharpening adopted — glass 4.0.0's `peerDependenciesMeta` does NOT mark `@lucide/vue` optional, so it is a required peer.
- **F-3 = D/M-5** (`cm-serif` resolves to UA generic `serif`; `--font-serif-math` declared nowhere) **= fr-BasisSelector m-3 + fr-AppHeader's GLASS-RELAY row (the undeclared producer variable; 26 hits / 17 files tree-wide per that seat's census) — → F.W1 token parity + the standing glass BH relay.** Local rider booked here at MINOR: the four section TITLES are among the consumers, falling to generic serif inside an app whose `--font-sans` is remapped onto Computer Modern (style.css:13-15/:17-22, my read) — D's own MAJOR→MINOR regrade ratified. Rendered face is SS-13 residue #5.

### MAJOR

- **M-1 = D/B-2 = L-12 = C-11 — MAJOR (R-4).** Four presentationally-styled section headings carry zero heading semantics — no `<h*>`, no `role="heading"`, no `aria-level` (my repo grep → 0 on this path; callsites wrap in bare `div.cartoon-card`) — an SC 1.3.1 F2/H42 pattern on the app's primary control surface. 7.0.0 hands back the region binding (`role="region"` + `aria-labelledby` — my CollapsibleContent.vue:47-48 read) but not the heading, and that binding arrives coupled to m-5 (the landmark name). Reframed by i-4: the producer already ships a heading-emitting disclosure (Accordion, `as="h3"`). **→ F.W3/W4** (the Collapsible-vs-Accordion decision resolves M-1 + M-5 + m-9 in one stroke).
- **M-2 = L-3 = C/N-5 — MAJOR.** `unmountOnHide` is never forwarded (glass 4.0.0 declares it with **no `.default`** — my chunk read — so `useForwardProps` excludes it from the key set :13-31, and reka applies its own `default: true`, CollapsibleRoot.js), so collapsing a section **tears out the default slot** (`CollapsibleContent.js:90` → `createCommentVNode`): `CoefficientsSpectrum.expanded` (my reads :35/:37-39/:122-134) is destroyed, ContourPreview's live SVG is unmounted per toggle, and every enter transition replays on reopen. The ★★★ trace ratified end-to-end with this seat's own four reads. **Cure corrected per DU-missed-3, verified**: at 7.0.0 the forwarding fix is STRUCTURALLY IMPOSSIBLE — `Collapsible.vue` neither declares the prop nor lets it through (`forwardedAttrs` destructures out `unmountOnHide`/`unmount-on-hide`; `inheritAttrs: false` ×3) — the escape is **`forceMount` on CollapsibleContent** (`:12`/`:45`, my read; also the only lever at the 4.0.0 pin). C's `v-bind="$attrs"` proposal is dead at the target. **→ F.W3/W4** (forceMount passthrough or a state-preserving surface) with the F.W1 sequencing rider.
- **M-3 = D/M-2 = L-1 = C/N-1 — MAJOR.** `el.scrollIntoView({behavior:'smooth'})` (`:26`) is the component's largest motion and is ungated by `prefers-reduced-motion` in a file whose CSS half IS gated (`:66-71`); no CSS can reach an explicit `ScrollIntoViewOptions.behavior` — the glass blanket rewrites `animation-*`/`transition-*` only (my a11y-overrides read) — and the 3-key prop surface offers no opt-out. LC's demotion of the L axis's BLOCKER ratified (2.3.3 is AAA; the gate fires rarely per M-4). The cure is a two-line in-tree copy (i-5). **→ F.W3/W4** (one repair unit with M-4/m-15).
- **M-4 = D/M-3 = L-2 = C-4(b,c) — MAJOR.** The `closest('.overflow-y-auto, .overflow-auto')` probe (`:24`) matches class ATTRIBUTES; neither real clipping/scrolling ancestor carries the literal token (`.eq-panel-left` is `@apply`-inlined scoped CSS — my :373-375 read; `.viz-panel-left` declares no overflow — my :363-371 read), so the gate measures `App.vue:26 <main>` at every callsite (my census: sole literal ancestor). The `?? el.parentElement` arm is unreachable and would be constant-false (every parent is `div.cartoon-card px-3 py-2` — my callsite reads). Sharpened by i-6: the equation route's TRUE clipper (`.eq-panel-left-wrap`, `overflow-y: clip` :358) is invisible to the probe AND unscrollable by `scrollIntoView` — three different boxes for one affordance. **→ F.W3/W4** (the m-15 cure deletes the probe outright).
- **M-5 = D/M-4 — MAJOR.** No hover affordance on a fine pointer at EITHER pin: 4.0.0's trigger recipe is hover-free (verbatim from my chunk read); 7.0.0's `.disclosure-trigger` is hover-free — the hover lives on `.disclosure-group-trigger`, Accordion-only (my disclosure.css read; R-6). The app disagrees with itself one directory over (`.advanced-trigger:hover`, ContourSettings :350-352 — my read). **→ F.W3/W4** + **GLASS-RELAY** (producer ask per the standing BH law: is the bare Collapsible trigger MEANT to ship affordance-free, or is a hover rung missing from `.disclosure-trigger`? cite the Accordion asymmetry).
- **M-6 = D/M-7 = C-2 = L-6 — MAJOR.** No controlled `open`, no `update:open` re-emit, no `defineExpose`, and `defaultOpen` is a mount-time snapshot (`:14`; file read whole — no watch), so: the equation route's primary result lands in a CLOSED drawer the callsite cannot open (`EqCoefficientsPanel:13 :default-open="false"` behind `EquationView:212-214`'s `v-if` — mounts only after Compute, materialises collapsed, no badge), and remount boundaries (`panel-swap` out-in, my VV:252-262 read) discard user state — compounded by M-2: the Coefficients subtree is not merely closed, it is never built. glass emits `update:open`; the wrapper swallows it. **→ F.W3/W4** (v-model surface).
- **M-7 = missed-find, both readers convergent — MAJOR (evidence-integrity + wave-gate).** The corpus's decisive instrument — `web/dist/assets/*.css` — is a **stale artifact**: gitignored (`.gitignore:42`), built `Jun 12 18:13`, five days BEFORE the installed glass-ui (`Jun 17 21:48`), and provably divergent (`.rounded-control` is 1 compiled rule in the installed `components.css` yet 0/8 in the built chunks — my re-runs of both readers' probes). Everything the L addendum stood on it is unproven (K-4), and **the corpus's central disagreement — is the scoped block redundant duplication (D/M-1) or the element's only animation (L/A2 + C-3)? — is UNDECIDED in both directions.** This seat narrows the hinge with two new cells: (i) glass 4.0.0's own `btn.css:60-65` documents that collapse timing "rides the `animate-collapsible-*` keyframes" — the producer DESIGNED for consumer-side emission; (ii) the installed tw-animate-css artifact ships the `--animate-collapsible-down/up` theme keys AND `@keyframes collapsible-down/up` (my read) — so the only unproven link is Tailwind's candidate extraction over the `@source "../*.js"`-registered chunks. **→ F.W1 PRE-GATE**: one `npm run build` + grep, before anyone touches `:54-71`, books the L-14 relay, or trusts a dist-derived claim; `PaperSidebar.vue:258` is the canary (adopted). Note B-1 is NOT hostage to this hinge: at 7.0.0 the producer grammar is static and layer-shipped.

### MINOR

- **m-1 = L-4 = D/i-2 = C-8 — MINOR.** `rootEl.value?.$el ?? rootEl.value` (`:21`): the right arm hands a component proxy to three Element methods; `$el: any` launders it past vue-tsc. Unreachable at both pins (single-root verified: my 4.0.0 chunk head read + 7.0.0 `<RekaCollapsibleRoot as="div">` read); the in-tree conformant idiom is MobileFloatingToc:27-33 (adopted). LC's MINOR ruling of the MAJOR/INFO/MINOR spread ratified. → **F.W3/W4**.
- **m-2 = L-5 = D/m-5 = C-12 + C/N-2 — MINOR.** The 250 ms timer (`:20-28`) is never captured/cleared and never re-reads `open`: N toggles queue N callbacks; a close-within-window still measures a collapsing section (the root stays mounted, `:22` doesn't fire); the unmount arm is benign. One-line `onCleanup` cure — which the m-15 rewrite subsumes entirely. → **F.W3/W4**.
- **m-3 = D/m-1 = L-15 = C-10 — MINOR.** Three dead extension hooks: `.collapsible-section`, `.collapsible-trigger`, `group` — zero rules, zero variants anywhere (my greps; the chevron rotates off the direct `:37` binding; the global `:focus-visible` enumeration at style.css:136-143 pointedly excludes `.collapsible-trigger` — my read). → **F.W3/W4**.
- **m-4 = D/m-9 = L-10 = C-16 + D/i-3 ∘ DU-missed-5 (geometry, folded) — MINOR.** `<slot name="actions" />` (`:43`) has zero consumers (my grep → 0) — and as banked it is **geometrically unusable**: the header row declares no `gap` (`:35`), the trigger is `flex-1` (its `gap-2` is internal), and the slot is a bare sibling — the first action ever projected sits flush against a full-bleed click target (my template read; contrast the producer's own `justify-content: space-between` header recipe). The M-audit's HIGH ask **A-3-new** rests on the false premise that this slot is "the sole reason fourier keeps a parallel CollapsibleSection" (corpus row verified in-tree by D, adopted) — re-argue before booking upstream. → **F.W3/W4** (gap + shrink-0 land with the first consumer) + **NO-WAVE-OWNER** (the A-3-new re-argument, SS-3/SS-4 census input).
- **m-5 = D/m-10 ∘ DU-missed-4 (landmark, folded) — MINOR.** The decorative subtitle is folded into the trigger's ACCESSIBLE NAME (`:38-41`, em-dash compound). Forward consequence traced through the uplift (my 7.0.0 reads): `role="region"` + `aria-labelledby="ids.trigger"` makes each open section a **named landmark whose name is the compound deck** — three new landmarks on /equation with zero fourier code change. Fixing m-5 (`aria-describedby` for the deck — cheap once 7.0.0 plumbs ids) is a PREREQUISITE of M-1's region-binding win, not independent of it. → **F.W3/W4**.
- **m-6 = C/N-3 — MINOR.** The file's only comment (`:57-59`) directs readers to `@mkbabb/glass-ui/styles/animations.css` — a specifier the export map does not carry (adopted receipt, LC re-verified); acting on it throws `ERR_PACKAGE_PATH_NOT_EXPORTED`. The underlying mechanism is sound (style.css:3 → `./styles` → animations.css:18/:29 — my greps). Same false path at ContourSettings:357. The comment dies inside B-1's deletion. → **F.W1** (rides B-1's edit).
- **m-7 = L-9 = C/N-4 — MINOR.** The hand-rolled fork at ContourSettings:255-265 + :361-375 is UNAVOIDABLE given the wrapper's surface (3 scalars, hardcoded trigger content — the divider-flanked "Advanced" affordance is unbuildable through it); the forker knew (the B.W2.d comment cites A.W3.d as prior art — my read) and the rules are character-identical modulo class name. The fork inherits B-1's hazard verbatim and dies under the same F.W1 gate. Cure: `#title` slot + `variant`, not discipline. → **F.W3/W4** with the F.W1 gate rider.
- **m-8 = L-11 = C-14 — MINOR.** Zero automated coverage reaches this component: no unit harness in web/ at all (my scripts read), none of the 8 e2e specs asserts any of the four section titles; `@axe-core/playwright` IS wired but on viz routes, and axe has no rule for M-1's class anyway (adopted refinement). → **F.W3/W4** (a section-toggle e2e is the born-RED seed for B-1's witness too).
- **m-9 = D/m-8 (provenance corrected per R-6) — MINOR.** The trigger is ≈32 px (py-1.5 + text-sm line box) — clears 2.5.8 AA (24 px), fails 2.5.5 AAA (44 px); glass 4.0.0's coarse-pointer floor enumerates three classes, not this one (adopted cell, thrice-agreed). The "producer's own 44 px floor for this exact role" claim is DEAD (accordion-only — my read); the Accordion decision (i-4) would bring the floor honestly. → **F.W3/W4**.
- **m-10 = D/m-2 — MINOR.** The padding contract is split: component owns `pb-1` and no top padding; the four callsites re-invent the above-body gutter three different ways (`pt-1`/`pt-1`/`p-2`/none — my reads). → **F.W3/W4**.
- **m-11 = C-7 = D/m-4 = L-7 (D/M-6 leg KILLED, R-1) — MINOR.** Three hardcoded clocks (`250`/`0.2s`×2/`duration-200`) bypass the shipped `--duration-fast: 0.2s` (my token grep), and the JS timer is a hand-kept 50 ms lead with nothing linking them. What survives of the 7.0.0 leg after K-1: the post-uplift clock is 200 ms × `--motion-tempo` — the lead stays 50 ms at default tempo, and only the tempo KNOB (INFO rider) makes any constant fragile. The m-15 cure deletes the timer's reason for existing. → **F.W3/W4**.
- **m-12 = D/m-7 = C-6 — MINOR.** Sole bare-root `@mkbabb/glass-ui` import among the tree's three Collapsible consumers (the other two use `/collapsible`); non-break at 7.0.0 (root barrel re-export — adopted, DU-verified); convention + uplift blast radius only. → **F.W3/W4**.
- **m-13 = C-9 — MINOR.** The decorative `<ChevronRight>` (`:37`) carries no `aria-hidden="true"` against 24 in-tree exemplars (my census, exact); lucide supplies no default (adopted receipt). Inside the trigger's accname computation it adds noise to m-5's compound. → **F.W3/W4**.
- **m-14 = L-13 — MINOR.** This wrapper is a conditional GATE on the R5-7 derived-evidence surface with inverted polarity: source-derived loop evidence OVER-counts what a DOM census sees behind a closed section (my reads: the `Tooltip v-for` at FunctionInput:157 sits inside the Function section; EqCoefficientsPanel's `:default-open="false"` + M-2's teardown ⇒ CoefficientsSpectrum's tooltips absent from the DOM at boot). Carry: record enclosing disclosure state, or force-mount, before comparing derived to live. → **NO-WAVE-OWNER** (SS-3/SS-4 census methodology; F.W4 execution carry).
- **m-15 = LC-missed (re-proven) — MINOR.** `scrollIntoView({block:'end'})` on the section ROOT is unconditionally unsafe for tall sections: `:23` measures the whole root, `:26` aligns its BOTTOM edge, and the `:25` gate constrains position, never height — when `rect.height > scrollport.height` the just-clicked trigger scrolls off-screen (arithmetic, verified by construction; the 480 px-max `.eq-panel-left` column makes tall content realistic). The unbanked cure is one token: **`block: 'nearest'`** — no-ops when visible, scrolls the minimum, delegates ancestor resolution to the browser — deleting the probe (M-4), the predicate, both forced reflows, the dead arm (m-1's live half), and the timer's purpose (m-2/m-11) in one stroke. All three axes proposed heavier cures. Frequency SS-13. → **F.W3/W4** (the repair unit's shape, with M-3's PRM gate).
- **m-16 = LC-missed (re-proven) — MINOR.** `.collapsible-content { overflow: hidden }` (`:54-56`) is permanent, not animation-scoped, and the body wrapper (`:46`) has ZERO inline padding — while glass's focus indicator is a box-shadow ring (base.css:174-178, my read; two color-mix legs, scale-paper.css:66-68, my read) painted OUTSIDE the border box and clipped by ancestor overflow. The preset-pill row starts at x=0 of the content box and the Compute Button spans it (my FunctionInput:149-166 read); their rings clip left/right. The closest producer contrast: the Accordion body's `padding: 0 0.25rem 1rem` is exactly the room a ring needs (and D mis-read it as an indent — K-9). Paint extent SS-13. → **F.W3/W4**.

### INFO

- **i-1 = D/i-4 — INFO.** Callsite count drift in the corpus: A8-17 says 5, design-synth says 3; the tree has **4 usages / 3 files** (my grep, exact; ContourSettings:358 is a comment). → **NO-WAVE-OWNER** (census correction, SS-3/SS-4).
- **i-2 = D/m-11 — INFO.** F.W1 imposes a new body typographic register (`.disclosure-content` sets color/size/leading — my read); a visual-diff row, small but non-zero blast radius. → **F.W1** screenshot row; SS-13 input #6.
- **i-3 = C-17 — INFO.** Formatting diverges intra-file and no linter exists to catch it (my package.json read: no eslint/prettier in either block). → **F.W0** (substrate lint decision; do not fix by hand).
- **i-4 = DU-missed-6 (re-proven) — INFO.** glass-ui 7.0.0 already SHIPS the heading-emitting disclosure: `AccordionTrigger.vue:41` `<RekaAccordionHeader as="h3" …>` wrapping the trigger, with the header display recipe, the hover, and the 44 px floor (my reads). This converts three separate D findings (M-1 heading + M-5 hover + m-9 floor) into ONE decision — Collapsible-vs-Accordion for titled sections — the KISS reading. → **F.W3/W4** decision input.
- **i-5 = DU-missed-7 (re-proven) — INFO.** The repo already holds the JS reduced-motion idiom M-3 needs, three sites, one a named helper (`router/index.ts:15-17` — my read; PaperView:176; SvgFilters:9). The corpus's highest-severity cluster is a two-line in-tree copy; no design debate needed. → **F.W3/W4** (rides M-3).
- **i-6 = DU-missed-8 (re-proven) — INFO.** The never-named true clipper: `.eq-panel-left-wrap { overflow-y: clip }` (:358) — clipping-but-not-scrolling, invisible to the probe, unscrollable by scrollIntoView; flipped to `overflow: visible; flex: none` below 1024 px (:361). The concrete shape of the axes' parked SS-13 item. → **F.W3/W4** (rides M-4); SS-13 input #3.
- **i-7 = LC-missed (meta) — INFO.** The corpus never reconciled its three-way severity spread: four pairwise-disjoint BLOCKERs, the same facts filed at three severities (timer: MAJOR/MINOR/INFO; `$el`: MAJOR/INFO/MINOR; heading: BLOCKER/MINOR/MINOR), and tallies from 13 to 24 for 72 lines. **This record is the deduplicated cross-axis ledger F.W4 books from**; naive tally-summing triple-counts ~a dozen rows. → **NO-WAVE-OWNER** (fleet method, SS-3/SS-4).

## Killed-claims register

- **K-1 · D/M-6 ("the 250 ms timer is 100 ms SHORT at 7.0.0 — the animation becomes 350 ms via `--spring-smooth-settle`; re-grade LOW→MAJOR, F.W1 gate") — KILLED** (reader-DU; ratified + settled by this seat's own producer reads, R-1). The cited tokens do not exist (`grep spring-smooth` → 0 tree-wide); the actual 7.0.0 clock is `--spring-present-duration` = 0.2s × tempo = **200 ms** (my scheme-spring/scheme-motion reads); and the unlayered shorthand keeps ANY substrate clock off the element while it survives (the same cascade fact B-1 rests on). **Reader-LC's L-7 confirmation cell ("I confirm its mechanism at the target pin… `--spring-smooth-duration`") dies with it** — citation-inheritance, recorded in DISSENT-1. Residual: the tempo knob, INFO rider on m-11.
- **K-2 · D/M-6's routing clause ("the `onanimationend` cure has ZERO uplift coupling and can land today") — KILLED AS STATED, RESCOPED** (reader-DU's kill, sharpened by this seat). The cure IS landable today (the keyframes exist at 4.0.0 — my animations.css grep), but it is tightly coupled FORWARD: if the scoped block survives the uplift, no `animationend` ever fires and the scroll silently never runs — the same mechanism as B-1's hang. The cure may only land WITH B-1's deletion gate, never before it; and the m-15 cure makes it moot.
- **K-3 · D/X-1 (mechanism) + D/S-5 + L/S-3's find-in-page clause — KILLED** (both readers, unanimous; every link re-read by this seat: the absent `.default` in the 4.0.0 chunk, the useForwardProps key-set :13-31, reka's `default: true`, `hidden: ""` + `createCommentVNode` at CollapsibleContent.js:82/:90, the unreachable `beforematch` :64). X-1's mechanism is exactly inverted (the missing default is why the prop VANISHES, not why it is false); the closed subtree is REMOVED, not `until-found`-hidden; find-in-page cannot work. **X-1's conclusion (do not land the booked `:inert` cure) survives a fortiori** — removed-from-DOM is strictly stronger.
- **K-4 · The L second-pass addendum's evidence base (A1 census, A2's inversion of L-8, S-6, A4's re-tag) — KILLED** (both readers; my own re-run of the staleness probes: dist CSS `Jun 12 18:13` vs installed glass `Jun 17 21:48`; `rounded-control` 1 rule installed vs 0/8 built; `.gitignore:42`). The freshness test compared the artifact to the component source, never to the package it reasoned about. Survivors: L-15 (source-grep-based, m-3) and the QUESTION itself (M-7).
- **K-5 · L-14 as stated ("glass ships neither the rule nor a documented `@source` requirement") — KILLED** (reader-LC; ratified by my read of the FULL comment at dist/styles/index.css:203-222 — LC's quote is verbatim-accurate: "`../*.js` reaches the compiled render-functions PRECISELY in the SHIPPED context… Locked by proof:emission"). The producer ships exactly the documented requirement; C-3's "node_modules is never scanned" premise dies with it (an explicit `@source` is the opposite of auto-detection). Whether the mechanism FIRES in fourier's build remains M-7's gate — the glass BH relay is NOT owed unless the fresh build comes back empty.
- **K-6 · C-4(a) ("the element measured and the element scrolled are UNRELATED BY CONSTRUCTION") — KILLED** (both readers). `scrollIntoView` scrolls every scrollable ancestor; `<main>` — the measured box — is a member of the scrolled set. The proven defect is (b)/(c): the probe cannot see the real panel boxes and the fallback is dead (M-4). Filing (a) separately inflated the MAJOR and would send a fixer at the wrong line.
- **K-7 · C/N-5's recompute sub-claim ("previewPath/previewViewBox keep recomputing regardless") — KILLED** (reader-LC; Vue-semantics kill ratified — computeds are lazy; both deref sites live inside the torn-out slot, so they go dirty and stay dirty at zero cost). The teardown half survives in M-2.
- **K-8 · C-K-6's narrowing ("the finding narrows to exactly three collapse classes; the trigger's other four classes DO work") — KILLED** (reader-LC; ratified by my own grep: `transition-control` = `@utility`-only at btn.css:55, 0 hits in components.css AND glass-ui.css — dead by the identical mechanism as `transition-collapse`:67). Four tokens across two components; `focus-ring`/`tap-squish` survive via pre-shipped RULES (base.css), a different mechanism than C credited. The trigger loses its transition on the same terms the content loses its animation.
- **K-9 · D's accordion-only provenance band (m-3's "indent"; B-2's `.disclosure-header` shipped "precisely so a heading can host the trigger row" for THIS component; m-8's "canonical section trigger" 44 px) — KILLED** (reader-DU; ratified by my disclosure.css + AccordionTrigger:41/:48 + AccordionContent:50 reads — all three classes are Accordion-path-only; the Collapsible path receives `.disclosure`/`.disclosure-trigger`/`.disclosure-content` and nothing else; `padding: 0 0.25rem 1rem` is symmetric inline room, not an indent). The underlying observations survive on their own (m-9, M-1, m-16); the provenance dies; **reader-LC's m-8 cell repeated the dead provenance** (R-6, DISSENT-1).
- **K-10 · D/i-1's clause ("the IDENTICAL PARAGRAPH is copy-pasted at ContourSettings:353-360 — two comments, one shelf life") — KILLED** (reader-DU; ratified by my reads of both comments: A.W3.d prose vs B.W2.d prose citing A.W3.d as prior art — different words, different wave ids). What IS shared: the unreachable specifier (m-6) and the hazard class. L-9's narrower rules-identity claim is exact and CONFIRMED (m-7).
- **K-11 · The L axis's line-number provenance ("every line number above was read") — KILLED** (reader-LC; spot-ratified by this seat against the live file: scrollIntoView `:26` not :27, `open` ref `:14` not :15, `rootEl` `:15` not :16, `<Collapsible` `:34` not :33, actions slot `:43` not :44, PRM block `:66-71` not :65-70 — a systematic-but-inconsistent +1 drift in the script-block cites while other cites in the same document are exact). No verdict changes; every L coordinate must be re-resolved before driving an edit (the D-19 lesson).

## Superlatives — with provenance (L-18 runs both ways)

1. **★★★ L-3's forwarding trace** (challenge-L:101-121) — the best work in the corpus; survived this seat's hostile four-hop re-derivation byte-for-byte (absent `.default` → key-set exclusion → reka's own `default: true` → the comment-vnode branch), correctly identifying the NON-OBVIOUS causal inversion (the missing default is what makes the prop vanish), and in doing so refuting two D rows (X-1, S-5) and one clause of its own axis's superlative (L/S-3). Both readers crowned it; this seat's reads confirm every quoted body.
2. **★★★ D/S-1's hand-computed contrast pair** — reader-DU recomputed both arms from the raw token graph and matched the reported digits exactly (5.11:1 / 5.68:1); reader-LC honestly declined to re-derive and filed it unproven. Adopted on DU's ratification with LC's flag preserved; the glass-surface backdrop caveat rides SS-13.
3. **★★ D §4.4 + the Standing note** ("reason about consumer-visible CSS from `dist/styles/` only" — the two divergent style trees, each of whose `@source` comments is true of its own context) — ratified; the single most portable sentence in the corpus, with the recorded irony that the same axis-pair then decided the emission question on a stale build (M-7). The discipline was right; the artifact was not.
4. **★★ L/S-6's `animation: none` vs `animation-duration: 0` distinction** — mechanism verified by this seat at usePresence:44/:117: only the literal computed name `"none"` short-circuits to immediate UNMOUNT; the chosen form is margin, and it was chosen. **This seat's coda**: the 7.0.0 producer's own PRM carve is the OTHER form (`animation-duration: 0.01ms` — my disclosure.css read); when B-1 deletes the local block, the PRM path exits via a near-instant `animationend` instead of the short-circuit — correct, but the margin is spent; recorded so no future audit re-books the form-change as a regression.
5. **★ The `#actions` sibling placement** (`:43` — outside the `<button>`, all three axes + both readers) — the naive implementation nests interactive controls inside the disclosure button; this file got the rare thing right on a slot that has never had a consumer. Geometry caveat banked (m-4); the structure must survive any refactor.
6. **★ The A.W3.d chain is real end-to-end at the installed pin** (my greps: keyframes :18/:29; the single cascade entry style.css:3) — and this seat sharpens what the corpus muddled: the chain is STATIC (shipped keyframes via the cascade import), so the scoped block genuinely animates today regardless of the emission hinge, which touches only the substrate UTILITIES. Two axes independently falsified their own no-op hypotheses against it; the fork's B.W2.d comment is honest provenance of the same chain.
7. **★ C §7/§11.3's falsification receipts** — ten hypotheses raised and killed with pasted receipts (the type-less-button kill re-verified twice over; the lucide byte-identical chevron geometry de-risking the 35-site sweep). Preserved as the pattern that keeps later seats from re-running dead branches.
8. **★ The shared wrong guess, killed twice independently** — L/S-5 and C/K-2 both raised the `__VLS_WithSlots` collapse hypothesis and both killed it with live tsc probes carrying CONTROL assertions, then re-aimed at the real hole (`$el: any`, m-1). Recording a shared wrong guess and its shared kill is exactly what stops a third pass.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **THE EMISSION HINGE (M-7)** — does a CURRENT fourier build emit `.data-[state=open]:animate-collapsible-down` / `-up` / `transition-collapse` / `transition-control` via glass's `@source "../*.js"` over the chunk class strings + tw-animate-css's shipped keys? One `npm run build` + grep — NOT a browser question; this seat did not run it (a build writes product `dist/`, and this seat's only write is this record). Decides D/M-1 vs L/A2+C-3, the deletability of `:54-71` TODAY (not at 7.0.0 — B-1's deletion there is unconditional), L-14's relay, and whether PaperSidebar:258 animates at all.
2. **The scope-id DOM witness** (R-5 residue) — one attribute read on the live content element, or equivalently one frame of today's collapse animating. Cheap; couples to B-1's hang-reachability but not to its routing.
3. Whether M-4's mis-gating is user-visible below 1024 px, where the wrap flips to `overflow: visible` (i-6) and the measured box definitively diverges from the clipping box.
4. Whether `main.scrollHeight > main.clientHeight` ever holds on `/v/:slug` — if never, the whole watch body is INERT at the ContourPreview callsite (sharpens M-4 from mis-gated to dead; cure unchanged).
5. The rendered face for `cm-serif` (F-3's local rider) — which UA generic serif paints is platform-dependent.
6. i-2's F.W1 typographic delta — a screenshot-diff row, not statically decidable.
7. m-16's paint extent — the clipping mechanism is static; how much ring is lost per control is paint.
8. m-15's frequency — whether the Controls section exceeds the scrollport at real breakpoints (the unsafety is arithmetic; the frequency is not).

## DISSENT — preserved, not ruled away

1. **Two citation-inheritance events inside reader-LC's confirmation cells.** (i) The L-7 cell confirmed D/M-6's mechanism "at the target pin" citing `--spring-smooth-duration` at disclosure.css:102-103 — the token does not exist anywhere in glass 7.0.0 (my grep); the cell was inherited from the challenge, and LC's own hedge ("did not re-derive the 0.35s figure") marked the seam without stopping the assertion. (ii) The m-8 cell repeated "glass 7.0.0's canonical section trigger sets `min-block-size: 2.75rem` (disclosure.css:60 — verified)" — the line exists but belongs to the Accordion-only class; "verified" reached the line, not the applier. LC's cells BESIDE these were genuinely excellent original work (the working-tree manifest provenance, the `transition-control` extension of C-K-6, the lazy-computed kill, the line-drift audit, the `block:'end'`/focus-clipping missed finds). Fleet lesson, same as fr-BasisSelector DISSENT-1: **a confirmation cell needs the same receipt discipline as an accusation cell** — this is the exemplar's predicted stale-cell-repetition event, twice.
2. **Reader-DU's B-2 BLOCKER dissent preserved** (R-4): four of four headings on the primary control surface, Level A, with the failure compounding m-5's landmark naming at the uplift. If SS-13's live pass shows screen-reader navigation of /equation materially degraded, the severity re-opens.
3. **Reader-LC's B-1 conditionality dissent preserved** (R-2): "a future regression contingent on an uplift that has not landed" is a true description of today's tree; the BLOCKER grade is carried by the F.W1 program being the tranche's own committed next step and by the failure mode being functional and silent. If F.W1 is descoped, B-1 regrades to MAJOR-with-gate automatically.
4. **Both readers missed the banked identity on C-1** (R-3): DU confirmed it at BLOCKER against this component; LC corrected the attribution but still filed it as this corpus's row. The identity guard exists precisely because a true fact re-booked under a new name splits its repair; fr-AnimationControls C-1 keeps the booking, this record contributes the riders.
5. **This seat's adopted-not-rerun cells, enumerated**: the a11y-overrides `:115-121` coarse-floor enumeration (thrice-agreed, corpus + both readers); the lucide chevron path bytes; the glass export-map `./styles` roster (m-6); MobileFloatingToc's conformant idiom; glass 7.0.0 `src/index.ts:135` root-barrel re-export (m-12); D/S-1's arithmetic (DU-ratified); the 4.0.0 chunk's tail past its head (single-root confirmed at the head read). Every other load-bearing cell in this record was re-derived with this seat's own instruments listed above.

## Closing verdict

**ADJUDICATED-DEFECTIVE — a 72-line wrapper whose one BLOCKER is a time bomb armed by the very wave that will audit it: the unlayered scoped shadow-animation block silently kills the producer's 7.0.0 disclosure grammar and hangs reka's presence machine, so all five of the app's disclosure surfaces stop closing the day F.W1 lands unless the block (and its ContourSettings twin) dies in the same commit — a hard F.W1 gate with a five-surface close-completion witness. Beneath it: 7 MAJORs (the heading/landmark gap now reframed as a Collapsible-vs-Accordion decision the producer already ships; the unmountOnHide teardown with its cure corrected to forceMount — the prescribed forwarding fix is structurally impossible at 7.0.0; the ungated smooth scroll whose two-line cure sits in-tree; the class-attribute scroll probe measuring the wrong box at every callsite; the hoverless trigger at both pins → glass BH relay; the uncontrolled-open drawer swallowing the primary result; and the corpus's own stale-dist evidence base, which leaves the scoped block's TODAY-deletability undecided — one `npm run build` pre-gate at F.W1, narrowed this session by two new producer cells), 16 MINORs dominated by one insight (a single `block:'nearest'` token deletes the probe, the predicate, the timer, and the dead arm at once), 7 INFOs, 3 identities folded by reference (the peer deadlock → fr-AnimationControls C-1; the devDeps manifest → fr-AppHeader FR-AH-33; the `--font-serif-math` seam → fr-BasisSelector m-3), 11 kills — including the corpus's only cross-version timing claim, dead on nonexistent tokens, and one reader's confirmation of it caught as citation-inheritance — and 11 missed finds banked after re-proof. Routings are M-25 agglomeration input: F.W1 owns B-1's deletion gate + the emission pre-gate + the fold riders; F.W3/W4 owns the heading decision, the scroll rewrite, the state-preservation surface, and the fork retirement; F.W5–W8 gets nothing — the component is API-inert, and that negative is recorded; the census-methodology rows go to SS-3/SS-4 with NO-WAVE-OWNER. Nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
