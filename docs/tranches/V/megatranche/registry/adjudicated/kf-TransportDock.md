Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component TransportDock; receipt read from this seat's own system prompt, not inherited).

# kf-TransportDock — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale, 41 behind), `origin/master` `81a56990`, worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus**: two fresh hostile re-reader returns (design-and-usage · library-and-code) over `audit/kf-components/TransportDock/challenge-{D-design,L-library,C-consumption}.md` (313 + 218 + 346 L, all three re-read whole by this seat) — jointly: 25 confirmed clusters · 8 kills · 5 misses · 6 unproven · 7 superlatives.

**Substrate (re-run by this seat, decisive for two rulings).** `git status --short` marks `TransportDock.vue` ` M` against the stale local HEAD, but **`git diff origin/master --stat -- demo/components/instrument/transport/` touches only `TimingFunctionPanel.vue` + `useTimingFunctionEditor.ts`** — the audited SFC and all three colocated composables are byte-identical to the landed frontier. Manifests: worktree dirty with 0 glass-ui hits; **`origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDependency; origin lock ×3 glass-ui entries** — the phantom-dep posture is a local uncommitted edit, ≡ census SCH-1/F-1 as ruled at kf-App. Every line anchor below binds to worktree ≡ `origin/master`.

**Method (incredulity).** Every kill, every reversal, every new booking and every fold was re-derived at the bytes before adoption; the readers were attacked, not transcribed. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session: whole reads of `TransportDock.vue` (403 L), `usePlayActuation.ts` (88), `useMenubarMeasure.ts` (28), `useIconSpin.ts` (29), `useAnimationProgress.ts`, `useRafLoop.ts`, `useDemoTicker.ts`; `AnimationControlsGroup.vue` binding census (`:98/:100/:103/:105-108/:130/:176/:273/:325`); `useControlsKeyboardShortcuts.ts:45-58`; `kf-engine.ts:45-58` (the throw verbatim); `play-lifecycle.ts:474-484` (`reset()` = cancelWAAPI + fillBackwards + settle, **no `playback.stop()`**); `defaults.ts:87` (`respectReducedMotion: false`) + opt-in precedents (`useSceneSwap.ts:45`, `animationOptionsStore.ts:49`); lucide `createLucideIcon.mjs` (bare arrow) + `Icon.mjs` (functional `h("svg")`) + `defaultAttributes.mjs` (`width: 24`); `@vue/runtime-core` `:1761` (`shapeFlag & 4 ? instance : vnode.el`); installed glass-ui 7.0.0: `keyboard.js` whole dispatch (window bubble listeners, skip = INPUT|TEXTAREA|SELECT|contentEditable|.monaco-editor — BUTTON absent, **no `e.repeat` guard**, `space: [" "]`), `button-B7c944jy.js` (0 `emits`, 0 `inheritAttrs`, sole propagation stop = the disabled-guarded click; `"data-control-target": a.iconOnly ? "" : void 0`), `dock.js` `:270` (`collapseDelay: t = 3600`), `:299-305` (`D()`/expand → `"hover"` unless alwaysExpanded), `:329-337` (`N()` → `"pinned"`), `:516` (`?? 3600`; `dock-scroll-x` unconditional horizontal), `:520/:703/:722` (startCollapsed Boolean-cast dead-`?? !0`; `initialExpanded: !a.value`), `:650-656` (`data-dock-overflow` stamp), `components/button/styles.css` (`.button` min-block-size floor; `[data-icon-only]`; `[data-emphasis="quiet"]` transparent/no-glass), `styles/utilities/responsive.css` whole (the `[data-control-target]` coarse floor — the file's entire content), `styles/tokens/sizing.css` + `light-dark.css` coarse block (`--ui-scale: 1.5`, `--control-floor: 2.75rem`), dock `shell.css` (`white-space: nowrap; max-inline-size: var(--dock-max-inline-size)` = `min(80vw, 64rem)`) + `overflow.css` (the `[data-dock-overflow]` scroll+mask seam; the coarse `--dock-control-floor` block), `scale-paper.css` (the 7 `--rainbow-pastel-*` oklch definitions, single definition site), `utilities/btn.css` (`.rainbow-pastel` linear-gradient recipe), `_shared/feedback.d.ts` (STATUS_DOT_STATES vs PULSE_STATES) + the warning-mark rotated-square CSS, `useDockShellProps.d.ts` `:72-96` (the `"grow"` doc vs the BG.W-DOCK-CAP-SCROLL-FADE paragraph, read verbatim); reka `TooltipTrigger.js` (`focus`, not `focusin`) + `SelectRoot.js:19`/`PopperRoot.js:7` (`inheritAttrs: false`); **this seat's own node oracles**: an independent oklch→sRGB→WCAG derivation (own code) reproducing **all seven** pastel hex values and ratios to the digit (yellow `#e6dca8` 1.39 … indigo `#bbacd7` 2.10 ceiling), and a comment-count state machine → template 70 + script 73 = **143 exactly**; executed `npx prettier --check` (SFC warns, all three composables pass); greps: `--dock-margin` definitions across demo/src/producer = **0** (13 use-lines), `--rainbow-pastel-*`/`--rainbow-indigo` in demo = **0**, barrel consumers of `@components/instrument{,/transport}` = **0/0** (ACG `:130` eager-imports the SFC), `useIconSpin|useMenubarMeasure` in test/ = **0**, dock-styles svg-sizing rules for the trigger = **0**, `dist/gh-pages` = git-IGNORED (check-ignore). Registry instruments: **kf-AnimationControlsGroup.md** (M-7, D-11/C-8, M-11 — the decisive bank), kf-App.md (KF-APP-4 `:44`, KF-APP-6 `:51`, SCH-1 ruling), kf-ChromeDock.md (D-13/L·m-10 ruling, RR-1 MISSED #4, RR-2 MISSED #1, D-1→KF-APP-6 fold + `c7f7c96d` provenance, KF-APP-26 DockSelectTrigger rider, m-2/C-13 dockCardinality rider, D-18 citing this component as the house pattern), kf-EditorStartScreen.md (ND-1, the refiling-ordinal ledger), kf-AnimationVisualizer.md (KF-AV-1/KF-AV-25 useDemoTicker context), `formation/keyframes/CENSUS-2026-08-03.md` (the KF.W0..W10 taxonomy); sweeps: `pressedPointers|usePlayActuation|press-origin` in registry = **0**, `useDemoTicker` = 3 files (context rows, no superlative), no banked Space-double-fire or aria-describedby-duplication row.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave sketch KF.W0–W10.

## Scoreboard

**33 roster rows, ids TD-1..TD-33 minted for life — 0 fresh BLOCKER · 9 MAJOR · 11 MINOR · 10 INFO · 3 folds carried at banked identities** (one carrying blocker-weight at its bank: KF-APP-6). **Killed-claims register: 10 entries** — the readers' 8 kills all SUSTAINED on this seat's own instruments, **plus 2 booking kills of this seat's own against rows both readers confirmed fresh**. **7 superlatives** (with two honesty codas from the bank). **8 UNPROVEN residues** → KF.W9 SS-13, one of them this seat's own addition.

**Headline.** The readers' work is excellent and their kills all hold — but the corpus repeats the registry's one recurring failure in a new place: **the readers swept the registry for their MISSED findings and not for their CONFIRMED ones.** Their headline reversal (L-M-1, the per-frame `animationProgress` churn, complete with the correct `shouldUpdateComponent` prop-identity mechanism that defeats challenge C's false `withCtx` kill) is mechanically right and **already banked at kf-AnimationControlsGroup M-7 — at MINOR, naming both components and the closed-dropdown consumer verbatim**; likewise their L-m-3/C-15 (the dead `reset(true)` arm) is banked at the same file's D-11/C-8. Two booking kills, zero derivation errors. What survives as genuinely new is a four-piece yield the three original axes all missed: **TD-21** (the press-origin `Set` is shared between the two play mirrors, falsifying the composable's own docblock), **TD-22** (`expand()` silently DEMOTES a pinned transport to `"hover"` while the comment three lines up calls it a "re-pin" — a second consumer site of the banked ChromeDock mechanism), **TD-23** (all three working tooltips announce their trigger's name twice — description ≡ name), and **TD-32** (a FOURTH doc-drift cell in `useDockShellProps.d.ts`, the one that manufactured challenge D's false MAJOR). The component's deepest defect remains TD-1: its **only keyframes.js consumption is 100 % dead** — an `SVGSVGElement` tested against `instanceof HTMLElement`, twice.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **L-M-1 — readers confirm MAJOR + reverse challenge C's §5 kill #2; is it a fresh booking?** | **BOOKING KILL — FOLD ≡ kf-AnimationControlsGroup M-7 (MINOR at its bank); the kill-reversal itself is SUSTAINED** | The mechanism re-derived: `useAnimationProgress.ts` assigns a **fresh `Record`** to `animationProgress.value` per rAF tick under `{ guard: isPlaying }`; ACG `:103` binds it, so the parent's render effect reads the ref (setup-ref auto-unwrap) and re-renders per frame; the child's vnode carries the prop in dynamicProps, so `shouldUpdateComponent`'s identity compare is true per frame and TransportDock re-renders regardless of popover state. C's kill attacked `withCtx` dependency-tracking — a different mechanism that never reaches prop-identity diffing; **the kill is false and both its author and the readers are right about that**. But the row is PRE-BANKED: kf-AnimationControlsGroup.md:58 — "`useAnimationProgress` reassigns a fresh object per frame; both this component and the 403-line TransportDock re-render at 60fps while playing, to drive a dot that renders only inside an open dropdown" — **MINOR, NO-WAVE-OWNER (efficiency)**. Grade follows the bank (the readers' own severity split concedes cost UNPROVEN-NEEDS-LIVE); the readers' sharper child-boundary mechanism is adopted as a strengthening rider AT the bank. |
| 2 | **L-m-3/C-15 — the dead `reset(all)`/`expandTimeline` payloads as a fresh MINOR** | **PARTIAL BOOKING KILL — the reset arm FOLDS ≡ kf-AnimationControlsGroup D-11/C-8; the expandTimeline half survives as a component-local rider** | kf-AnimationControlsGroup.md:69 already banks: destructive `resetAllStores()`+reload wired to a branch nothing can take; TransportDock `:158` sole emitter passes `false`; the `:368` tombstone; "narrow the emit type" as the cure. Verified again this session. The bank does NOT name `expandTimeline` (emitted only at `:171` inside `v-if="storedControls.isTimelineExpanded"` `:168`, so only `false` is reachable) — that half rides TD-15 as the rider. |
| 3 | **C-B-1 (BLOCKER: all glass-ui imports unresolvable under `npm ci`)** | **KILL SUSTAINED ≡ census SCH-1/F-1 → KF.W0 §B-12 (the substrate law's next firing)** | My own probes: the audited SFC ≡ `origin/master` (empty diff over the file); `origin/master:package.json:77` glass-ui `7.0.0` **exact devDependency**, origin lock ×3, matching the installed producer. The BLOCKER was filed against an uncommitted local manifest edit at a baseline the challenge never named — the identical event kf-ChromeDock (L/B-1), kf-MatrixEditor (K17) and kf-SequencePlayhead (the dirty-manifest law) have each already ruled. Residue rides KF.W0 §B-12: the uncommitted deletions would revive F-1 if committed. |
| 4 | **C-2 — headline (no-op `:always-expanded="false"` + 19-line NAMED DECISION) vs its cure paragraph (`startCollapsed` "default true")** | **Headline CONFIRMED MAJOR (TD-5); cure paragraph KILLED; the mount-state consequence FOLDS ≡ kf-ChromeDock RR-1 MISSED #4** | My dock.js reads: `:695` `alwaysExpanded: { type: Boolean }` no default → absent = `false` → the binding changes nothing; `:703` `startCollapsed: { type: Boolean }` no default → Vue's Boolean cast yields `false`, so `:520`'s `?? !0` arm is **DEAD on the SFC path**; `:722` seeds `initialExpanded: !a.value` → a dock with no attribute — exactly TransportDock — **mounts EXPANDED ("hover")**. So the NAMED DECISION at `:24-42` is false twice over: the lever is inert AND the documented collapsed detent is not what ships. The two-docks-opposite-states row is already banked (kf-ChromeDock RR-1 MISSED #4, MINOR, batch with D-13); what books HERE is the component's own false prose + inert binding. |
| 5 | **D-M2 (MAJOR: long name grows the dock past the viewport)** | **KILL SUSTAINED — the producer caps and scrolls; INFO residue only (TD-29)** | My CSS reads: `shell.css` `.glass-dock { white-space: nowrap; max-inline-size: var(--dock-max-inline-size) }`, token `min(80vw, 64rem)`; `overflow.css` `.dock-scroll-x[data-dock-overflow]:not(.dock-overflow-wrap) .dock-layer--full { overflow-x: auto; … mask-image: … }`; dock.js `:516` emits `dock-scroll-x` unconditionally for a horizontal non-wrap dock and `:650-656` stamps `data-dock-overflow` from measured scrollWidth. The MAJOR was built on the `"grow"` doc-comment ("nothing clips or scrolls") without reading the same doc's own BG.W-DOCK-CAP-SCROLL-FADE paragraph fourteen lines later — the producer doc-drift cell booked as TD-32. Surviving residue: `.text-ellipsis` at `:115` with no overflow/width terms is a no-op class (= L-i-3). |
| 6 | **D-B3 (BLOCKER: the reset spin ignores prefers-reduced-motion)** | **SEVERITY KILL SUSTAINED — mechanism real, exposure zero; latent-on-cure rider bundled INTO TD-1** | Both facts re-verified (`defaults.ts:87` `respectReducedMotion: false`; `useIconSpin.ts:13-15` no opt-in; opt-in precedents live at `useSceneSwap.ts:45` + `animationOptionsStore.ts:49`) — and the animation is UNREACHABLE: TD-1 proves `resetIconSpin()` returns at `:23` on every invocation, forever. A BLOCKER filed on a code path the filer's own two axis-mates proved dead. Cure law: `respectReducedMotion: true` + `onScopeDispose(() => animation.stop())` land IN the TD-1 repair, never before it. |
| 7 | **D-B2 (contrast) — filed BLOCKER, readers adopt MAJOR + strike the "only signal" cell** | **CONFIRMED MAJOR (TD-8) — independently re-derived by this seat; cell strike sustained** | My own oklch→sRGB→WCAG code against the installed `scale-paper.css` bytes reproduced **every hex and every ratio to the digit**: ceiling `2.10:1` (indigo `#bbacd7`) against WCAG 1.4.11's 3:1, floor 1.39 (yellow); recipe verified (`btn.css` 7-stop `to right` gradient); theme-invariant (single definition site; demo defines zero `--rainbow-pastel-*`). The "glyph shape is the ONLY signal" cell dies on `:67`/`:203` — one ternary swaps `.rainbow-pastel`↔`.rainbow-vivid`, a second (colour-only) whole-control signal. The finding needs no such cell. |
| 8 | **The five reader-filed misses** | **TD-21/TD-22/TD-23 ADMITTED MINOR · TD-32 ADMITTED INFO→BH relay · TD-33 ADMITTED INFO (corpus-method)** — one precision rider added | All re-verified at the bytes: (21) `usePlayActuation` called ONCE (`:329-335`), one `Set` (`:45`) bound to both mirrors — enforcing "either control", not the docblock's own "SAME control" (`:22-24`); registry sweep 0. **This seat's rider: the cross-control pass is mouse-specific** — touch/pen get implicit pointer capture, so `pointerup` retargets to the captured element; the mouse path (no capture; the inert-swap mid-crossfade retargets pointerup) is the live one. (22) dock.js `D()` writes `"hover"` unconditionally under `!alwaysExpanded`; `N()` (collapsed-pill click) is what writes `"pinned"`; `actuatePlay` `:325` + the `:322-324` "re-pin" comment inverted — a second consumer site of the banked kf-ChromeDock RR-2 MISSED #1 mechanism, plus the comment-inversion rider that is new. (23) `:104/:147`, `:158/:165`, `:171/:175` — description ≡ accessible name verbatim on the three working tooltips; the play control alone differentiates (`:63` vs `:79`); no banked row. (32) `useDockShellProps.d.ts:72-73` "nothing clips or scrolls" vs `:87-94`'s own "a capped axis is INTRINSICALLY a scroll axis" — the FOURTH cell in the kf-ChromeDock D-13 drift ledger, and the cell that manufactured D-M2. (33) `dist/gh-pages` is git-IGNORED (my check-ignore) — D's cascade-order cells rest on an unreproducible local build. |
| 9 | **Challenge-L cell correction (this seat's own)** | **L `:35` "test/demo/instrument/ has transport-play-actuation.test.ts and nothing else" is FALSE as written** | The directory carries **9** test files (my `ls`). The substance survives in the readers' correctly-scoped form: zero tests touch `useIconSpin`/`useMenubarMeasure` (my grep = 0), and the untested unit is the broken one. Booked so the false census cell is never re-cited. |
| 10 | **D-B1 refiling ordinal** | **Harmonized: the FOURTH registry occurrence** | kf-EditorStartScreen's ND-1 counts per-corpus: kf-App banked → kf-ChromeDock's readers re-filed (2nd) → kf-EditorStartScreen's re-filed (3rd) → this corpus's D axis (4th). The readers' per-reader count ("the D axis is the fifth") is arithmetic on a different basis, not an error. |

**Adjudicator's corrections register (against this corpus's readers):** (1) L-M-1 booked fresh at MAJOR — ruling 1; (2) L-m-3/C-15 booked fresh — ruling 2; (3) TD-21's cross-control scenario scoped to the mouse path — ruling 8; (4) the challenge-L test-inventory cell — ruling 9; (5) the refiling ordinal — ruling 10. Everything else the readers filed survived this seat's re-derivation intact — including all eight of their kills.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0–W10** per the census wave sketch where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the transport repair packet); **glass-ui BH relay** per the standing owner edict. Anchors bind to worktree ≡ `origin/master` `81a56990`; re-anchor before any cure (D-19).

### MAJOR

| id | merged ids | claim (compressed; the challenge files carry the full cells) | terminal disposition |
|---|---|---|---|
| **TD-1** | L-B-1 / C-1 (+ D-B3 as latent rider) | **The component's only keyframes.js consumption is 100 % dead.** `ref="resetIconEl"` lands on lucide `RotateCcw` — a functional component (bare arrow, verified at the dist), so Vue's setRef (`runtime-core:1761`, `shapeFlag & 4` = 0) assigns `vnode.el` = the `<svg>`; both `resolveElement` branches test `instanceof HTMLElement` (`useIconSpin.ts:5,7`) — false for `SVGSVGElement` — so `resetIconSpin()` returns at `:23` on every invocation, from both entry points (`:158` click; the `R` shortcut via `defineExpose` `:373` → ACG `:325` → `useControlsKeyboardShortcuts.ts:52`). Filed BLOCKER by both axes; **MAJOR** — `emit('reset', false)` still fires; only the engine feedback is absent, which is exactly why nobody noticed. The untested composable is the broken one | **NO-WAVE-OWNER** (transport packet). Cure spec: widen to `Element` or ref a host element, **AND in the same commit** `respectReducedMotion: true` + `onScopeDispose(() => animation.stop())` (rulings 6; TD-4) + the `useIconSpin` test C-12 names |
| **TD-2** | L-B-2 | **Space on the focused expanded Play button actuates twice — net no-op; holding Space rapid-toggles at key-repeat rate.** `:72-73` carry no `.stop` (the collapsed mirror `:208-209` does); Space keydown arms locally then bubbles to glass-ui's **window** registry (`keyboard.js` — BUTTON not in the skip set, no `e.repeat` guard, `space: [" "]`, default event keydown) which fires `toggleAnimationGroup()` (the SAME function as `@toggle-play`, `useControlsKeyboardShortcuts.ts:50`) → toggle #1; local keyup actuates → toggle #2. Button declares no emits/inheritAttrs (0/0, my grep), so both handlers are native listeners on a real `<button>`. Enter is single-fire (no window Enter shortcut). The `:311-312` "can never drift" claim is falsified by its own bindings. Filed BLOCKER; **MAJOR** (Enter and click both work; the broken path is Space-while-focused) | **NO-WAVE-OWNER** — one-word cure (`.stop` on `:72-73`, matching the mirror) + a composition test (button mounted WITH the registered shortcut — the isolation suite structurally cannot see this). **Producer rider → glass-ui BH relay**: the registry dispatch has no `e.repeat` guard and does not skip BUTTON targets — the F2 class re-opens at the window for every consumer |
| **TD-4** | L-M-2 / C-17 | `useIconSpin` violates the tree's own engine idiom on three axes: eager `kfEngine()` at composable setup (`:12` — throws by contract, `kf-engine.ts:49-56`, a silent dependency on main.ts's global warm ordering), mount-time `fromString()` parse of a static string on every scene mount (`:13-20` — a full A13→A2→value.js `parseStylesheet` round trip for an animation TD-1 proves can never render; zero colour functions, so the R1 crash class is NOT reachable here), zero teardown (`reset()` never stops the RAFPlayback — `play-lifecycle.ts:476-482`, my read). The in-tree precedent is one directory away (`CubeTarget.vue:198/:204/:234-235`: lazy at gesture, stop-before-replay, `onScopeDispose`). **Latent today only because of TD-1** — curing TD-1 without teardown converts a dead feature into a live rAF-on-detached-node leak | **NO-WAVE-OWNER** — one commit with TD-1 (lazy `loadAnimationEngine()` at first spin, dispose on scope exit) |
| **TD-5** | C-2 (headline; ruling 4) | `:always-expanded="false"` is the framework default — a no-op binding — under which the 19-line NAMED DECISION (`:24-42`) documents an inert lever AND claims a collapsed resting detent the dock does not have: with no `start-collapsed` attribute the transport **mounts EXPANDED** (`initialExpanded: !a.value`, dock.js:722; effective startCollapsed = false per the Boolean cast). The prose's "the ONLY kf-side lever" is stale against installed 7.0.0 | **NO-WAVE-OWNER** — reconcile the block against the real levers (`start-collapsed`, `collapse-delay`); the boot-posture decision batches with kf-ChromeDock RR-1 MISSED #4 (banked); the false cure paragraph is killed (K3) |
| **TD-6** | C-3 / D-M3 | The animation-select's tooltip is keyboard-unreachable: `TooltipTrigger as-child` merges onto a non-focusable wrapper `<div>` (`:92`) and reka listens on non-bubbling `focus` (my `TooltipTrigger.js` read); keyboard focus lands on the SelectTrigger `<button>` inside; `aria-describedby`/`data-state` stamp the wrapper, not the combobox. The three sibling tooltips as-child onto real buttons and work. The wrapper is vestigial (`relative flex gap-1.5` around one rendered child) | **NO-WAVE-OWNER** — delete the wrapper div; `as-child` onto the trigger. One markup pass with TD-11/TD-14 |
| **TD-7** | C-4 / D-M1 | Both play controls opt out of glass-ui's size contract — no `icon-only`, hand-rolled `rounded-full p-0 w-10 h-10`/`w-8 h-8` — forfeiting `[data-control-target]`, the SOLE selector of the coarse tap floor (`responsive.css`, entire file; stamped only when `a.iconOnly`, my button-js read). Source-derived geometry: expanded 40×40 fine / 40×~60 coarse (the `.button` `min-block-size: var(--control-h-md)` floor with `--ui-scale: 1.5`, `--control-floor: 2.75rem` at coarse); collapsed mirror 32×40 / 32×~60 — **never a circle on any pointer**, and the inline axis sits under the 44px floor on both. The three sibling dock controls get the floor by construction (`--dock-control-floor` at coarse). Demo declares none of the five governing tokens (grep = 0) | **NO-WAVE-OWNER** — `icon-only` on both Buttons; delete the hand-roll. Rendered boxes → KF.W9 SS-13 #2 |
| **TD-8** | D-B2 (ruling 7) | White glyph over `.rainbow-pastel` — the IDLE/first-run Play state — never reaches WCAG 1.4.11's 3:1 anywhere on the ramp: **ceiling 2.10:1** (independently re-derived by this seat from the token bytes, all seven stops digit-exact), theme-invariant (one definition site; zero demo overrides); `text-white` (utilities) out-cascades quiet's `--muted-foreground` (@layer components). The vivid arm's glyph-span estimate (1.33–3.47:1) is source-derived, per-pixel unproven | **NO-WAVE-OWNER** (interim: a computed on-gradient ink) + **glass-ui BH relay** (the rainbow rung with compliant ink — the J-era ask at `J/glassui-AX-handoff.md:346-350`, still unlanded; one relay with TD-9/TD-10). Vivid per-pixel → SS-13 #3 |
| **TD-9** | D-M4 | `emphasis="quiet"` — the lowest rung — declared on the file's own stated "PRIMARY first-run gesture" (`:29-33`), then repainted by hand (`.rainbow-*` + `text-white`), opting the CTA out of glass material entirely (quiet gates off glass-wash/capsule/specular); the one dock row reads from three material vocabularies (quiet-repainted Button · `.dock-select-trigger` specular · DockControl glass-capsule) | **NO-WAVE-OWNER** consumer arm; the rainbow-rung producer cure rides the TD-8 BH relay |
| **TD-10** | D-M5 / C-10 | `StatusDot :state="isStarted ? 'warning' : 'unknown'"` renders a **hazard mark** (rotated amber square + bar, my CSS extraction) for "paused" — semantic-token misuse on a health vocabulary (`STATUS_DOT_STATES`, no idle rung); `label` omitted while the adjacent text names the animation, never the state, so paused-vs-idle is invisible to AT. The D-axis split is correct: the `unknown` half is a producer vocabulary gap, not a demo defect | **NO-WAVE-OWNER** (use `unknown` for both states + a `label` meanwhile) + **glass-ui BH relay** (a neutral/idle StatusDot rung — sibling of the TD-8 ask) |

### MINOR

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| **TD-11** | L-m-1 / C-9 / D-m6 | `<template v-for>` with no `:key` (`:121-141`) → UNKEYED_FRAGMENT (readers re-compiled with `@vue/compiler-dom`; flag 256) — reka SelectItems patched by index across a channel-set change; the per-row `--dot-p` inline style can paint one animation's progress onto another's row. The `<template>` wrapper has one child and buys nothing | **NO-WAVE-OWNER** — `v-for` + `:key="name"` on `SelectItem` directly |
| **TD-12** | L-m-2 / C-8 | `class="p-0 m-0 cursor-pointer"` on `<Select>` (`:94`) silently discarded — Select declares no class prop; reka `SelectRoot`/`PopperRoot` set `inheritAttrs: false` (my reads) and render fragments; no Vue warning fires | **NO-WAVE-OWNER** — delete |
| **TD-15** | L-m-3 / C-15 (ruling 2) | The `reset(all)` payload is dead (sole emit `false`, `:158`; parent's `clear()` arm unreachable) — **FOLD ≡ kf-AnimationControlsGroup D-11/C-8** (banked with the cure: delete the arm + narrow the emit). **Component-local rider (new): `expandTimeline` is likewise only ever `false`** (`:171` inside `v-if="storedControls.isTimelineExpanded"` `:168`) — the contract advertises a two-way switch the component cannot throw | **FOLD** + rider rides the banked cure |
| **TD-16** | L-m-4 / C-14 | Duplicate import statements (`@lucide/vue` `:222-227`+`:249`; `/dock` `:229-233`+`:251`) and the SFC is not prettier-clean (my run: SFC warns, composables pass) despite the shipped organize-imports plugin — the drift lane in which the C-1 site left review's sight | **NO-WAVE-OWNER** — mechanical |
| **TD-17** | L-m-6 | Both colocated composables reach into the caller's template-ref namespace by hard-coded string (`"menubarHostEl"`, `"resetIconEl"`) with generics the runtime cannot honour (`useTemplateRef<HTMLElement>` over an SVG — TD-1 is the standing proof this failure mode is live here); rename the attribute and the composable silently yields null forever | **NO-WAVE-OWNER** — pass the ref (or accept it as an argument) in the TD-1 re-authoring |
| **TD-18** | C-7 | The lazy `defineAsyncComponent` barrel (`transport/index.ts:11`) has **zero consumers** (my greps: 0 imports of either barrel) while the one real consumer eager-imports the SFC (ACG `:130`) — the lazy guarantee is not merely unused but contradicted; the module sits in both graphs | **NO-WAVE-OWNER** — delete the export or route the consumer through it; decide once |
| **TD-19** | C-12 (untested half) + L-i-4 | 2 of 3 colocated composables have zero tests (my grep) — and the untested one is the broken one (TD-1). The typecheck half (no vue-tsc anywhere; CI runs `check:lib` only) **FOLDS ≡ kf-App KF-APP-4 → KF.W4**. Honesty cell (L `:176`, sustained): vue-tsc would ALSO have believed the `useTemplateRef<HTMLElement>` assertion — the gate row is context, not cause, for TD-1 | untested half **NO-WAVE-OWNER** (tests land with the TD-1/TD-2 cures); typecheck **FOLD → KF.W4** |
| **TD-20** | D-m5 | The empty-state `<List>` glyph (`:112-114`) is the only icon in the file with no size class → lucide's 24px default (my `defaultAttributes` read) beside a 12px chevron (`--dock-trigger-icon-size, 0.75rem`) in one trigger; zero svg-sizing rules in the dock stylesheets (my grep) | **NO-WAVE-OWNER** — `icon-md` |
| **TD-21** | **NET-NEW** (readers' miss #1; ruling 8) | **The press-origin guard is shared between the two play mirrors**: one `usePlayActuation()` call (`:329-335`), one `Set<number>` (`:45`), bound to both controls — enforcing "down and up on EITHER play control", not the docblock's own "down+up on the SAME target" (`usePlayActuation.ts:22-24`); the SFC's `:311-312` "can never drift" claim compounds. The 11-case isolation suite drives one handler set and cannot express two controls. **This seat's rider: live on the mouse path specifically** (touch/pen implicit pointer capture retargets pointerup to the pressed element; mouse has no capture, and the mid-gesture crossfade/inert swap is exactly what retargets it). Registry sweep 0 — first booking | **NO-WAVE-OWNER** — per-control origin (a Set per binding, or keyed by control id) in the TD-2 commit; the composition test covers both |
| **TD-22** | **NET-NEW site** (readers' miss #2; mechanism ≡ **kf-ChromeDock RR-2 MISSED #1**, banked MAJOR) | `actuatePlay`'s `dockRef.value?.expand()` (`:325`) **DEMOTES a pinned transport to the transient `"hover"` state** — dock.js `D()` writes `"pinned"` only under `alwaysExpanded` (which `:43` disables), else `"hover"`; the pin is authored by the user's collapsed-pill click (`N()`). Pressing Play thus makes the dock LESS sticky, then the 3600ms idle timer collapses it — while the comment at `:322-324` calls the call a "re-pin", the inverse of the byte. New consumer site + new comment-inversion rider on the banked producer mechanism | **MINOR** — one cure spec with the ChromeDock row (producer: expand() honours pinned, or consumer: keepOpen()-equivalent); the comment corrects in the same commit |
| **TD-23** | **NET-NEW** (readers' miss #4; ruling 8) | All three working `<Tooltip>` sites give the trigger an `aria-describedby` whose content verbatim equals the trigger's `aria-label` (`:104/:147`, `:158/:165`, `:171/:175`) — AT announces the name twice, on the keyboard path (reka's focus handler is what opens them). The play control alone does it right (`:63` "Pause animation" vs `:79` "Pause"). The `:53-56` T.C3 invariant books only the visible double-tooltip kill, not this a11y-tree doubling. Registry sweep 0 — first booking | **NO-WAVE-OWNER** — differentiate the tooltip copy (the play control is the in-file pattern) or drop the redundant tooltips (TD-6's cure already deletes one) |

### INFO

| id | merged ids | claim | disposition |
|---|---|---|---|
| **TD-24** | L-m-5 / C-6 / D-m10 | The "monotonic by construction" peak is destroyed on every transport unmount (`useMenubarMeasure.ts:22-26` removes both props, `peak = 0`; the transport is `v-if`'d on channel cardinality, ACG `:98`) — the high-water mark is per-instance, the SFC comment (`:286-291`) claims page scope. **Severity per the readers' own correction (only D-m10 got it right): the consuming chain is itself IACVT today** — `--dock-band-reserve-stable`'s token-floor arm contains `var(--dock-margin)` (TD-13's bank), so the reset has NO rendered consequence and C-6's "falls back to the token floor" consequence is wrong in mechanism (the whole `max()` is guaranteed-invalid) | **INFO today / MINOR once KF.W6 cures the token** — sequenced strictly after the KF-APP-6 cure; the fix (module-scope peak, or don't clear PEAK_PROP) lands then |
| **TD-25** | C-13 | Prose names `DockSelectTrigger` (`:110`), a primitive 7.0.0 does not export — the **second site** of the identical rider banked at kf-ChromeDock (KF-APP-26 fold rider: "the :375 rationale names a retired primitive") | rides the banked rider; correct in any touching wave |
| **TD-26** | C-16 | `storedControls` is a shared ambient reactive store passed as a prop (ACG `:176`→`:100`); the emit round trip is ceremony — the parent mutates the same object in place (`:106-108`); readonly-prop warnings cannot fire on object mutation | **NO-WAVE-OWNER** — state the topology honestly (provide/inject, the sibling idiom) when the transport packet opens |
| **TD-27** | L-i-2 | `String(key)` (`:98`) launders a type-permitted nullish into `"null"`/`"undefined"` (reka `AcceptableValue` includes null; store field `string \| null`). Unreachable today | **NO-WAVE-OWNER** hygiene |
| **TD-28** | L-i-1 / D-m9 (count corrected) | **143** of the non-style lines are historical tranche narration (my own state-machine count: template 70 + script 73 — L's count exact, D's 87 false), two blocks orphaned: `:278-291` narrates a mechanism living wholly in `useMenubarMeasure.ts`; `:293-319` restates `usePlayActuation`'s docblock and makes the "can never drift" claim TD-2/TD-21 falsify twelve lines away. Not a defect alone; it is why the drift was invisible | **NO-WAVE-OWNER** — the largest single deletion available; reconcile narration to where code lives, in the packet |
| **TD-29** | L-i-3 (+ D-M2 residue, ruling 5) | `.text-ellipsis` on SelectValue (`:115`) is a no-op — no overflow, no nowrap on that node, no width constraint | **NO-WAVE-OWNER** — delete or complete the trio |
| **TD-30** | C-5 (demoted per the readers, sustained) | The crossfade-strand cure covers 1 of 4 transport actions (`reset`/`timeline`/`select` still actuate from `@click`/reka's click path in the same layer). The dilemma is fair but not statically decidable — whether the crossfade actually strands a synthesized click is unproven; the supporting "2000 ms" cell was false (compiled 3600, my dock.js:270 read; the d.ts figure is the banked ChromeDock D-13 drift cell) | **argumentative INFO** — resolves at SS-13 #1; if the strand reproduces, extend the cure; if not, `usePlayActuation`'s rationale narrows to the expand()-provoked case |
| **TD-31** | D-m1 / D-m7 / D-m8 / D-m11 / D-m12 | The design-minor cluster, all verified at my file read: inert `justify-items-center` in a flex host (`:6`); the collapsed pill has no empty-state identity while the expanded trigger does (`:184` vs `:112`); one datum at `font-bold` in the dropdown vs `font-semibold` in the pill (`:138`/`:184`); the two-deep `var()` fallback at `:9` masks the TD-13-bank failure (silent-degradation idiom); demo overrides six of the seven consumed vivid stops but not `--rainbow-indigo` (my grep: 0 in demo) — latent drift only | **NO-WAVE-OWNER** hygiene cluster |
| **TD-32** | **NET-NEW** (readers' miss #3) | The **FOURTH doc-drift cell** in `useDockShellProps.d.ts` (verified verbatim, `:72-73` vs `:87-94`): the `overflow: "grow"` doc says "nothing clips or scrolls" while the same doc-comment's own BG.W-DOCK-CAP-SCROLL-FADE paragraph and the compiled CSS make the capped inline axis intrinsically a scroll axis. This is the cell that manufactured D-M2's false MAJOR. Joins `:120` (delay 2000 vs 3600), `:124` (startCollapsed "default true"), `:158-160` (backgroundCanvas selector) in the ledger banked at kf-ChromeDock D-13 | **glass-ui BH relay** — append to the banked drift ledger (owner edict) |
| **TD-33** | **NET-NEW** (readers' miss #5) | Corpus-method: challenge D settled cascade-order/bundle cells against `dist/gh-pages/assets/index-CL_QYCiO.css` — an untracked (git-IGNORED, my check-ignore) local build from the dirty worktree, reproducible from no commit. Affects D-m3's `.size-6`/`.h-10` ordering cell and D-m9's harvested-class cell; the `--dock-margin` DEFS=0 cell is NOT affected (re-derived from source + producer by this seat and at three substrates by the bank). Under the standing STALE-DIST law those cells are worktree-scoped assertions | **INFO** — rides the D axis's citations into any repair wave; D-m3's ordering re-derives from an origin/master build before citing |

## Identity-guard folds (never re-booked)

- **TD-3 (L-M-1 / C-11) ≡ kf-AnimationControlsGroup M-7** (MINOR, NO-WAVE-OWNER efficiency) — ruling 1. The readers' `shouldUpdateComponent` prop-identity mechanism is the strengthening rider at the bank; challenge C's §5 kill #2 is dead (K2) but the row was never this component's to book.
- **TD-13 (D-B1) ≡ kf-App KF-APP-6** (BLOCKER at its bank; `TransportDock.vue:389/:395` already named there; `c7f7c96d` deletion provenance at kf-ChromeDock) → **KF.W6 phantom-token audit**; rendered consequence at 390×844 → **KF.W9 SS-13** (already routed at the bank). **The fourth registry occurrence of this bank being re-filed fresh** (ruling 10) — the identity-guard ledger grows by one.
- **C-B-1 ≡ census SCH-1/F-1 → KF.W0 §B-12** (killed as filed; the substrate law's next firing — ruling 3).
- **TD-15 (reset arm) ≡ kf-AnimationControlsGroup D-11/C-8** — ruling 2; the expandTimeline rider rides the banked cure.
- **TD-19 (typecheck half) ≡ kf-App KF-APP-4 → KF.W4**.
- **TD-22's producer mechanism ≡ kf-ChromeDock RR-2 MISSED #1** (adopted MAJOR there); this row books the new consumer site + the comment inversion only.
- **TD-25 ≡ the kf-ChromeDock KF-APP-26 DockSelectTrigger rider** (second site).
- **TD-5's mount-state consequence ≡ kf-ChromeDock RR-1 MISSED #4**; the `startCollapsed`/`collapseDelay` doc-figures ≡ the kf-ChromeDock D-13 drift ledger, which **TD-32 extends to four cells**.
- **The dockCardinality conflated-signature fact ≡ kf-ChromeDock m-2/C-13** (rides KF-APP-13) — carried in superlative 1's rider, not re-booked.
- **TD-21 / TD-23 / TD-32 / TD-33: first bookings** — each checked against the kf-* registry (`pressedPointers|usePlayActuation|press-origin` = 0; no aria-describedby-duplication row; the drift ledger holds three cells, not four; no corpus-method row for the D read set) and the census S-1..S-8/F-1 rows.

## Killed-claims register — 10 entries

| # | killed claim | adjudication |
|---|---|---|
| **K1** | C-B-1 (BLOCKER: glass-ui unresolvable under `npm ci`) — and L §7's "F-1 gates all of it" + D-B1's "token-level sibling of F-1" framings | **SUSTAINED (ruling 3)** — a BLOCKER against an uncommitted local manifest edit at an unnamed baseline; frontier declares 7.0.0 exact (my git reads). ≡ SCH-1/F-1 → KF.W0 §B-12. The recurring lesson, verbatim from three prior apotheoses: a dirty-worktree claim without a named comparison baseline is inadmissible |
| **K2** | Challenge C §5 kill #2 ("per-frame animationProgress re-renders the whole dock" — killed via withCtx dependency-tracking) | **THE KILL IS KILLED (ruling 1)** — wrong mechanism; prop-identity diffing at the child boundary re-renders TransportDock per frame regardless of popover state. C-11's bounded form covers only the allocation. But see K9: the surviving finding was never unbooked territory |
| **K3** | C-2's cure paragraph ("glass-ui already ships the detent: `startCollapsed`, default `true`") | **SUSTAINED (ruling 4)** — `{ type: Boolean }` + Vue's cast makes the `?? !0` arm dead on the SFC path; effective default `false`; the dock mounts EXPANDED. Third derivation of the kf-ChromeDock L/m-10 ruling |
| **K4** | D-M2 (MAJOR: "a long animation name widens the transport pill monotonically and overflows the viewport — no truncation, no wrap, no scroll") | **SUSTAINED (ruling 5)** — the shell is hard-capped at `min(80vw, 64rem)` and the capped axis scrolls (`data-dock-overflow` + `overflow-x: auto`, re-verified at the bytes). Built on TD-32's doc-drift cell. INFO residue = TD-29 |
| **K5** | D-B3 at BLOCKER ("the 360° twist ignores prefers-reduced-motion") | **SEVERITY SUSTAINED-AS-KILLED (ruling 6)** — mechanism real, path dead (TD-1): zero vestibular exposure at any motion preference today. The D axis filed a BLOCKER on a path its two axis-mates proved dead and its falsifier never checked reachability. Latent-on-cure, bundled into TD-1 |
| **K6** | C-5's "collapseDelay defaults to 2000 ms" cell + its MAJOR grade | **SUSTAINED (ruling and TD-30)** — compiled 3600 (`dock.js:270/:516`, my reads); the 2000 is the banked d.ts drift cell. The dilemma survives as argumentative INFO; the strand is UNPROVEN-NEEDS-LIVE |
| **K7** | D-m9's "87 of 403 lines are comment" | **SUSTAINED** — my own oracle: 70 + 73 = **143**; the L axis's count exact. D-m9's qualitative cells all verify (TD-28); only the arithmetic dies; its harvested-class cell rides TD-33 |
| **K8** | D-B2's "the glyph shape is the ONLY signal distinguishing running from stopped" cell | **SUSTAINED** — `:67`/`:203`: one ternary swaps pastel↔vivid, a second (colour-only — itself a 1.4.1 concern) whole-control signal. Cell struck; TD-8 stands on the 2.10:1 ceiling alone, which this seat reproduced independently |
| **K9** | **Both readers' L-M-1 as a fresh MAJOR booking** ("MAJOR (mechanism source-conclusive…)") | **BOOKING KILL — this seat's own (ruling 1)** ≡ kf-AnimationControlsGroup M-7, banked at MINOR with both components named. The readers ran registry-first sweeps for their misses and none for their confirms — the exact asymmetry the kf-MatrixEditor record convicted its own r2 seat of |
| **K10** | **Both readers' L-m-3/C-15 as a fresh MINOR** | **PARTIAL BOOKING KILL — this seat's own (ruling 2)** ≡ kf-AnimationControlsGroup D-11/C-8 (reset arm, cure included). Only the expandTimeline half was unbooked; it rides as TD-15's rider |

Also corrected, below row-weight: challenge-L's `:35` test-inventory cell (ruling 9 — 9 files in the directory, not 1; substance intact) and the readers' refiling ordinal (ruling 10).

## Superlatives — 7 (provenance: axes → readers → this seat; every one re-verified at the bytes)

1. **The channel-zone elision is a real subtraction, decided once** (all three axes independently; readers concordant; my read of `:88-149` + `surfaceTabs.ts`). The `v-if` wraps `<DockSeparator />` WITH the `<Select>`, so a one-or-zero-channel scene renders no node and no orphan separator — the dangling-divider failure is structurally impossible; cardinality flows from `dockCardinality`, the same pure function ChromeDock reads. **Banked rider carried (kf-ChromeDock m-2/C-13):** the shared module is real, the shared CALL is fictional — `{ tabs: [], channels: … }` here (`:357`) vs `{ tabs: …, channels: [] }` there; no invocation exercises the conflated signature. Rides KF-APP-13.
2. **The progress ring is a one-custom-property handoff and the promotion is real** (L S-2 / C S+2; my reads of `:361-364` + `design-idioms.css:146-147`). `dotStyle` allocates `{ "--dot-p": String(p) }` and nothing else; gradient/ring/glow live in the promoted stylesheet idiom. The correct JS/CSS division for a 60 Hz visual — and the honest boundary of TD-3's fold: the churn is the producer's, not this call site's.
3. **`useMenubarMeasure` is genuinely acyclic and it actually tears down** (L S-4; my whole read). Content-driven height, never a function of the reserve it feeds; `Math.ceil` rounds the right direction for a reserve; both properties removed on unmount — discipline global-var writers almost never show. The same teardown line is TD-24's per-instance-peak flaw: both readings are correct; keep the discipline, fix the scope.
4. **The collapsed play mirror carries a distinct accessible name on BOTH ternary arms** (C S+5 / D S-2; `:195-199` vs `:63`). Corroborated from the bank's other side: **kf-ChromeDock's adjudicated BLOCKER (D-18/C-1) cites this exact block as the house pattern its cure needs** ("a real focusable Button with a disambiguated aria-label in the same slot"). The rare case of a component being another component's prescribed medicine.
5. **`usePlayActuation` is the best-specified unit in the transport — with the two riders both readers owe it** (all three axes' superlative; 11 driven cases re-confirmed present). No DOM, no emit, native semantics reconstructed (press-origin, isPrimary + non-primary-mouse rejection, pointercancel, Enter/`e.repeat`, Space-armed-keyup). **Rider 1 (TD-2):** the contract is right and the composition is wrong — isolation cannot see a window listener on the same key. **Rider 2 (TD-21):** the press-origin Set is shared by both mirrors, weaker than its own docblock. Nobody may cite this superlative as evidence the play path is sound.
6. **`.menubar-safe-pb` is real defensive CSS with both arms load-bearing — and currently void** (C S+4; `:387-397`, my read). The inline `0px` env() fallback and the `@supports not` arm each cover a distinct browser class; the reasoning is correct. Honest coda: both arms reference `var(--dock-margin)`, so under the KF-APP-6 bank both compute to zero until KF.W6 cures the token.
7. **`useDemoTicker` is the correct shape for a demo-wide rAF driver — the machinery under the very finding all three axes reached for** (readers' addition, verified whole by this seat). ONE module-level `RAFPlayback` + subscriber Set; `reconcile` parks the driver when hidden or when no subscriber is enabled and restarts at most one loop; per-subscriber `onScopeDispose`. N transport controls, one visibility-gated loop — the hard part, done right. **Honesty codas from the bank:** kf-AnimationVisualizer KF-AV-25 notes the shared loop iterates subscribers with no isolation (one throwing tick kills every demo ticker), and KF-AV-1 proves the per-subscriber gate is no rescue for a stale consumer — the driver's quality does not transfer to its subscribers.

## UNPROVEN-NEEDS-LIVE — KF.W9 SS-13 visual-audit inputs

1. **TD-30 strand probe** — press-and-hold reset/timeline/select across an idle-collapse boundary (~3.6 s); does the trailing synthesized click strand? Decides C-5's dilemma.
2. **TD-7 rendered geometry** — `getBoundingClientRect()` on both play buttons, fine and coarse emulation; the 40×60 / 32×60 / 32×40 figures are token-derived, no box measured.
3. **TD-8 vivid arm** — per-pixel contrast under the 24px glyph over the 7-stop ramp while playing; the idle ceiling (2.10:1) is a token fact and needs no browser.
4. **TD-3 cost profile (at its bank)** — the per-frame re-render's magnitude vs GlassDock's own per-frame work; a live profile can demote, not kill.
5. **TD-13's rendered consequence (at its bank)** — the painted `bottom`/`padding-bottom` at 390×844; named here so it is not re-derived.
6. **TD-21 live twin** — mouse-down on the collapsed mirror, crossfade, mouse-up on the expanded Button: assert actuation fires (the born-RED gate's witness).
7. **TD-2 live twin** — focused expanded Play + one Space press: assert net state change is exactly one toggle after the cure (and two-then-none before it); hold Space: assert no rapid-toggle.
8. **NEW (this seat) — the AT-activation path of `usePlayActuation`**: the composable listens to NO `click`; if VoiceOver/NVDA synthesize only a `click` (rather than a full pointer sequence), the play control is inert to AT activation. D's S-1 falsifier named this and the readers dropped it; it is the one hole in superlative 5 no static read can close.

Rerouted, not SS-13: **TD-33/D-m3's cascade-ordering cell** → re-derive from an origin/master build (build-level, not visual).

## Routing summary (M-25 agglomeration input)

- **NO-WAVE-OWNER — the transport repair packet (SS-1/SS-2 intake)**: TD-1+TD-4 (one commit: element guard + lazy engine + teardown + PRM + the missing test) · TD-2+TD-21 (one commit: `.stop` + per-control origin + the composition test) · TD-5 (prose/boot-posture, batch with the banked ChromeDock row) · TD-6 · TD-7 · TD-8/TD-9/TD-10 consumer arms · TD-11 · TD-12 · TD-16 · TD-17 · TD-18 · TD-19 (tests half) · TD-20 · TD-22 (one spec with the ChromeDock mechanism) · TD-23 · TD-26 · TD-27 · TD-28 · TD-29 · TD-31.
- **KF.W0 §B-12**: K1's manifest schism (uncommitted deletions vs origin's 7.0.0 pins).
- **KF.W4**: TD-19's typecheck half (≡ KF-APP-4).
- **KF.W6**: TD-13 at its bank (phantom-token audit); TD-24 sequenced strictly after it.
- **KF.W9 SS-13**: the 8 probes above.
- **glass-ui BH relay (owner edict, one letter)**: TD-8/TD-9 (the rainbow rung with compliant ink — re-raising the unlanded J-era ask) · TD-10 (neutral/idle StatusDot rung) · TD-2's rider (keyboard registry: `e.repeat` guard + BUTTON-target policy) · TD-32 (the fourth `useDockShellProps.d.ts` drift cell, appended to the ChromeDock D-13 ledger).

## Closing verdict

**ADJUDICATED — the readers' eight kills all hold, their four substantive misses are all real, and their two loudest confirmations were never theirs to book.** The corpus's structural lesson is now a theorem of this registry: derivation is not where fresh seats fail — every mechanism both readers filed re-derived cleanly at the bytes, down to the seven pastel hex values this seat reproduced digit-for-digit with its own code — the failure is **an unread registry on the confirm side** (K9/K10: L-M-1 and the dead reset arm were banked at the parent component's own apotheosis) and **an unnamed baseline** (K1: a BLOCKER filed against an uncommitted lockfile edit, the substrate law's third-plus firing). What this component adds to the forming X·KF specs is a coherent repair packet with one spine: a transport whose only engine consumption is dead (TD-1), whose primary keyboard activation is a self-cancelling no-op (TD-2), whose press-origin guard is weaker than its own docblock (TD-21), whose Play press quietly demotes the user's pin (TD-22), and whose 143 lines of narration assert, at length, the exact invariants those four rows falsify — plus a four-item producer letter (rainbow rung, idle StatusDot, keyboard-registry repeat guard, the fourth doc-drift cell) for the glass-ui BH relay. Terminal shape: **0 fresh BLOCKER · 9 MAJOR · 11 MINOR · 10 INFO across 33 rows, TD ids for life; 3 folds carried at banked identities (KF-APP-6 blocker-weight at its bank); 10 killed claims; 7 superlatives; 8 probes to KF.W9 SS-13.** Nothing here stamps a wave; every disposition is M-25 agglomeration input awaiting the owner's begin-word.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.
