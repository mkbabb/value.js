Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component TransportDock, round 2; receipt read from this seat's own system prompt, not inherited).

# kf-TransportDock — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-25 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale, 41 behind), `origin/master` `81a56990`; **re-verified this session: `git diff origin/master` over the SFC + all three colocated composables = 0 lines, and over `demo/app/dock/ChromeDock.vue` = empty** (its ` M` flag is against the stale local HEAD) · **Registry tree** value.js `tranche-u` · **Corpus (r2)**: two fresh hostile re-reader returns over the full challenge corpus (`audit/kf-components/TransportDock/challenge-{D-design,L-library,C-consumption}.md`, 313+218+346 L, all three re-read whole by this seat) **run registry-aware against the banked r1** — reader-DU (design-and-usage: 22 confirm cells · 13 kills incl. 3 self-kills · 4 misses · 6 superlatives) and reader-LC (library-and-code: 11 confirm clusters · 10 kills incl. 4 self-kills · 2 misses · 6 superlatives).

**Supersession (recorded, nothing lost).** This record supersedes the 2026-08-24 r1 apotheosis at this same path, **banked whole at commit `1eb80754`** — the full r1 cells, rulings 1–10, and kill register K1–K10 live there and are carried, not restated. Roster identities **TD-1..TD-33 are preserved for life** (anti-rename); this record appends **TD-34..TD-41**. r1 is **corrected in exactly two places**: (a) r1's roster silently dropped two filed challenge rows — D-m2 and D-m4 carried no disposition (this seat re-checked the r1 row map: TD-31 disposes D-m1/m7/m8/m11/m12, TD-20 = D-m5, TD-11 ⊃ D-m6, TD-24 ⊃ D-m10, TD-28 ⊃ D-m9, TD-33 ⊃ D-m3's cell; **D-m2/D-m4 appear nowhere**) — cured as TD-34/TD-35; (b) r1's superlative 4 praised the collapsed mirror's split accessible name without confronting the inert-exclusivity fact r1's own method block had already established — cured by the TD-39 coda, the superlative retained. Everything else in r1 was re-verified where this corpus touches it and stands.

**Substrate shift (recorded).** Both readers measured a worktree whose `package.json` had **zero** glass-ui lines (the F-1 posture as an uncommitted local deletion). This seat's grep today: worktree `package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` — **the local deletion has been reverted since the readers ran**. K1 (r1) is unaffected — it was killed against `origin/master`, never against the worktree — but any future seat citing the readers' worktree-manifest cells must date them.

**Method (incredulity).** Every admitted miss, every rider, and every kill-of-a-kill below was re-derived at the bytes by this seat before adoption; the readers were attacked, not transcribed — one of reader-DU's proof cells is **reversed** (ruling 3) and one citation is corrected. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session: whole reads of `TransportDock.vue` (403 — host `:2-10` incl. `data-dock-tether="bottom"` + `fixed left-0 right-0 z-dock` with no pointer-events term; face orders `:57-80` vs `:183-213`; mirror bindings `:205-209` five `.stop`s, **no `@click`**; `actuatePlay` `:321-327`), `usePlayActuation.ts` (88 — `:45` one `Set`, `:47` `spaceArmed`, **`:61` delete-as-guard**, `:64-66` cancel, `:72`/`:77` preventDefault on both keyboard branches, no blur/disarm anywhere), `useControlsKeyboardShortcuts.ts` (73 — 18 registrations; **`:50` `registerShortcut("Space", … { preventDefault: true })`**; no Enter row; Escape `:51` no-preventDefault), installed glass-ui 7.0.0 `keyboard.js` **whole** (105 — `:76-79` window bubble keydown+keyup; `:48-52` skip = INPUT|TEXTAREA|SELECT|contentEditable|.monaco-editor, BUTTON absent; **`:70` `e.options.preventDefault && n.preventDefault(), e.handler(n)` — preventDefault BEFORE handler**; `:30` `space: [" "]`; `:69` default event keydown; no `e.repeat` guard), `dock.js` (`:270` `collapseDelay: t = 3600` + open-state `c() ? "pinned" : i ? "hover" : "collapsed"`; `:301-305` `D()` writes `"hover"` unconditionally under `!alwaysExpanded`; `:328-334` `N()` → `"pinned"`; **`:801-808` root listeners: onFocusin/onTouchstart bubble + onPointerdownCapture/onPointercancelCapture/onClickCapture** — capture phase; `:822-838` layer renders: `--full` inert `≠full && ≠keepalive`, `--summary` inert `≠summary` + **`onClick: J(k)` where `:718` destructures `onClickCollapsed: k`** — the summary layer's ONE bubble handler; `:536-549` press-keepalive machinery **scoped by `closest(".dock-layer--full")` `:540` — the summary mirror can never receive keepalive**; `:915-919` `w()` focus handoff + `:1113-1115` active-face `tabindex:-1`/inert stamps; `:1121-1135` DockControl `shape/type/as` defaults), `useLiquidPress-D0PFjKuk.js` (handler set incl. **`onBlur: y` and `onPointerleave: y`** release; keydown `!e.repeat && v()` — no preventDefault, no stop; **`setPointerCapture` = 0 across useLiquidPress + button + dock, my grep** — no explicit capture anywhere on the press path), dock styles (`layers.css` — **`.dock-layer { grid-area: 1/1 }`**, `.is-leaving { pointer-events: none }`; `crossfade.css` `.dock-face` grid + `.dock-face-content { gap: var(--dock-layer-gap, 0.375rem) }`; `density.css` md `--dock-layer-gap: calc(0.375rem * var(--dock-scale))`; `overflow.css` coarse block `--dock-scale = var(--ui-scale) × var(--dock-coarse-scale, 0.78)`), `ChromeDock.vue` `:213-218` (**`pointer-events-none` host wrapping `pointer-events-auto`** — and `:collapse-delay="2500" :start-collapsed="true"`, the real levers, in use one directory away) + keyed `v-for` `:key="scene.id"`/`:key="tab.value"`, `AnimationControlsGroup.vue` `:96-109`/`:318-330` (v-if gate, bindings, unconditional shortcut wiring), `AnimationControlsGroup.css` `:107`/`:170-180` (mobile stage `fixed; inset: 0` under z-dock; timeline cell fixed full-bleed), demo-style greps (`pointer-events` = 5 hits, **none reaching the transport host**; `--dropdown-min-width` DEFS=1 `layout.css:16`; `[data-dock-tether]` rules `:159-166` anchor-only; `icon-lg`/`icon-md` `design-idioms.css:108-119` incl. the `& svg` arm), producer d.ts (`hideIndicator?: boolean`, `for?: "select"|"dropdown"|"popover"` — declared, reader-LC's self-kill re-verified). Registry sweeps: `pointer-events-none` ∉ {kf-TransportDock, kf-ChromeDock} rosters; `spaceArmed` = **0** registry-wide; `grep 'Space' kf-AnimationControlsGroup.md` = **0** rows (M-1 there is ArrowLeft/Right/Home/End vs the tablist — the both-handlers-run class, a different defect); kf-ChromeDock D-7 is within-dropdown glyph order, not face order — **all six admitted misses are first bookings**.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave sketch KF.W0–W10.

## Scoreboard

**41 roster rows — TD-1..TD-33 carried at `1eb80754` + TD-34..TD-41 minted here for life: 0 fresh BLOCKER · 12 MAJOR (+3) · 14 MINOR (+3) · 12 INFO (+2) · 3 folds at banked identities** (KF-APP-6 blocker-weight at its bank). **Kill register grows K1–K10 → K1–K17**: the readers' six self-kills banked as negative results (two independently convergent), one reader proof-cell **reversed by this seat** (K17), zero reader kills overturned. **9 superlatives** (r1's 7, one now carrying a coda + a third rider, plus 2 new). **13 UNPROVEN probes** → KF.W9 SS-13.

**Headline.** Round 2 earns its seat: the r2 readers ran the registry sweep on the confirm side (the exact failure r1 convicted round 1 of — not one banked row was re-booked) and still pulled **six unbanked defects out of a 33-row component**, four of them in the two most-audited surfaces in the file. The transport's host is a **full-viewport-width pointer trap** whose guard the sibling dock declares eight lines into its own template (TD-36); the **primary CTA swaps ends of the pill between the two dock faces** (TD-37); the collapsed mirror's celebrated `.stop` set is **inverted against the dock's actual listener phases**, leaving an undeclared mouse-only pin (TD-38); and — the sharpest — glass-ui's own Space shortcut, registered with `preventDefault: true`, **cancels native Space activation on every focused button it does not skip**, so the transport's Reset and Collapse-timeline controls silently do nothing while playback toggles instead (TD-40). The play path's actuation composable now carries **five** riders: its keyboard arm has no disarm (TD-41), and this seat's own derivation shows its pointer arm **leaks a stale press-origin** whenever a release lands on neither mirror — which TD-37's end-swap makes the *default* in-place outcome. The 143 lines of in-file narration continue to assert, at length, invariants the bindings falsify.

## Rulings (this seat's actual work, r2)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **Reader-LC miss #1 — Space is INOPERABLE on the two `@click` transport controls and fires playback instead** | **ADMITTED MAJOR — TD-40** | Every leg re-derived: `useControlsKeyboardShortcuts.ts:50` registers Space with **`preventDefault: true`**, invoked unconditionally at ACG setup (`:322`); `keyboard.js:70` calls `preventDefault()` **before** the handler; `:48-52` never skips BUTTON; `:76` window bubble. The victims are real native buttons (`dock.js:1121-1135` `as/type: "button"`) whose only local keydown is useLiquidPress's press animation (no preventDefault, no stop — my read); reka `TooltipTrigger`'s listener set carries no keydown. Cancelling the Space keydown default suppresses the button's keyup activation → no synthesized click → `:158`/`:171` never run — **and `toggleAnimationGroup()` runs instead**. Enter is unregistered (my read of all 18 rows) → Enter works; Space misfires. Distinct from TD-2 (the play button has local handlers and double-fires; these have none and zero-fire while a different command executes) and from kf-ACG M-1 (arrow keys, tablist victim, both-handlers-run class). **Scope rider (this seat's, per the ColorSpaceSelector scope law): the mechanism is APP-WIDE** — every focusable button outside the skip set loses Space activation while ACG is mounted (the banked kf-CopyButton superlative S-2 praises exactly the native activation this suppresses) — booked here where proven, scope stated, generalization routed to the keyboard spec row beside kf-ACG M-1/M-4 + the standing BH-relay registry item. |
| 2 | **Reader-DU miss #1 — the transport host is a full-bleed pointer trap** | **ADMITTED MAJOR — TD-36** | `TransportDock.vue:2-10`: `fixed left-0 right-0 z-dock` + `data-dock-tether="bottom"`, ~90 px band by the file's own measurement (`:263-264`), **no pointer-events term** on the host, in the scoped block (`:387-397`, padding only), or in any demo stylesheet reaching it (my grep: 5 `pointer-events` hits, all unrelated; `[data-dock-tether]` rules `layout.css:159-166` are anchor-geometry only). The in-tree precedent is exact: **`ChromeDock.vue:213-218` — `fixed left-1/2 -translate-x-1/2 … pointer-events-none` wrapping `<div class="pointer-events-auto">`** — the *narrower* host guards, the full-bleed one does not. Live exposure real in shape: the mobile stage is `fixed; inset: 0` under z-dock (`AnimationControlsGroup.css:107`) and the band-reserve chain is IACVT under the banked KF-APP-6, so content is not held clear. Every pixel outside the centred pill occludes hit-testing beneath. Occluded extent → SS-13 #9. First booking (sweep re-run). |
| 3 | **Reader-DU miss #2 — the play CTA changes ends between the two faces; "the geometric precondition for TD-21"** | **ADMITTED MAJOR — TD-37; one proof cell REVERSED (K17) and replaced by a sharper rider at TD-21 (this seat's own)** | Orders verified at the template: expanded face opens with Play (`:57-77`, `:58` "rail-core: PLAY, FIRST"); collapsed face is name-then-Play (`:183-213`, `:182` "animation name first, play button on right"). Concentricity verified — **citation corrected: for GlassDock's summary/full pair the grid-stack is `layers.css` `.dock-layer { grid-area: 1/1 }`** (reader-DU cited `crossfade.css`'s `.dock-face`, the DockCrossfade family — same mechanism, wrong selector family). So every expand/idle-collapse translates the primary control across the pill. **The reader's cell (ii) — "the reversal is the geometric precondition for banked TD-21" — is INVERTED**: were the mirrors co-located, an in-place cross-mirror release would exercise the shared Set in the direction of user intent. What the reversal actually does is make the in-place release land on **neither** control — and then `usePlayActuation.ts:61`'s delete-as-guard **leaks the pressed id** (no handler runs, nothing deletes it; `pointercancel` does not fire on a mouse release-elsewhere; `setPointerCapture` = 0 across the producer press path, my grep; mouse `pointerId` is persistent) — so the **next** drag-release over either play control actuates with no press-origin on it: **F3, the defect the composable was written to kill, resurrected by a stale entry**. Booked as a rider AT TD-21 (same identity: the Set is the defect; this is its second live path), not a new row. Δx magnitude → SS-13 #10; the stale-Set live twin → SS-13 #13. |
| 4 | **Reader-DU miss #3 — the mirror's five `.stop`s are on the wrong events; the unstopped click is an undeclared pin** | **ADMITTED MINOR — TD-38, with this seat's sequencing derivation added** | Producer side verified: the dock root's pointer/click listeners are **capture-phase** (`dock.js:806-808`) — a target-side `.stop` in the bubble cannot reach them — and its bubble listeners are focusin/touchstart (`:801-805`), which pointer `.stop`s do not touch; `.dock-layer--summary`'s ONE bubble handler is `onClick → onClickCollapsed → N() → "pinned"` (`:832-838` + `:718` + `:328-334`). Consumer side: `:205-209` stops pointerdown/up/cancel + keydown/keyup, **no `@click`**. So three `.stop`s are provably inert, the two keyboard `.stop`s are load-bearing (they are the mirror's TD-2 immunity — verified against `keyboard.js:76`), and the synthesized click sails through to the pin. Modality asymmetry verified: `usePlayActuation.ts:72/:77` preventDefault on both keyboard branches suppresses the synthesized click → keyboard never pins; pointer does. **This seat's addition**: event order is pointerup → (actuate → `expand()` → `"hover"`, TD-22's demotion) → click → `N()` → `"pinned"` — so the mouse path, if the click lands pre-inert, ends **pinned**: the undeclared route *accidentally delivers the exact "re-pin" the TD-22-inverted comment promised*, mouse-only. Which branch ships (Vue's microtask inert-patch may beat the click task) → SS-13 #11. Falsifies `:311-312` a third time, as the reader states. |
| 5 | **Reader-LC miss #2 — `spaceArmed` has no cancellation path** | **ADMITTED MINOR — TD-41, extended by this seat into a two-arm cancellation law** | `:47` set at `:78`, cleared only inside the keyup handler (`:82-84`); the returned interface (`:31-37`) exposes no disarm; no blur/visibilitychange counterpart exists (whole read). Reachable by construction: the focused layer goes inert mid-press (dock.js layer stamps; 3600 ms idle timer), and the flag is one closure shared by both mirrors — the next Space keyup on either actuates with no matching keydown. Producer precedent verified on the same element: **useLiquidPress binds `onBlur: y` and `onPointerleave: y`** — glass-ui solved exactly this for its press physics on the very button these handlers ride. **This seat's extension**: the pointer arm has the *same* hole — `pointercancel` (`:64-66`) covers touch interruption, **not** mouse release-elsewhere (ruling 3's stale-Set path) — so the cure is ONE law: every arm of the actuation contract needs a cancellation path (blur disarms Space; per-control origin + release-elsewhere cleanup for the Set), landing in the TD-2/TD-21 commit. The 11-case isolation suite structurally cannot see either (never dispatches an orphan keyup). First booking (`spaceArmed` registry grep = 0). |
| 6 | **Reader-DU miss #4 — the corpus's best-loved superlative rests on a premise its own author killed** | **ADMITTED MINOR — TD-39, scoped to the NAME-SPLIT half only; superlative 4 gains a coda, not deletion** | Premise verified at the bytes (r1's own method block + re-read this session): exactly one dock layer is non-inert in steady state (`dock.js:822-838`), so the two play controls are never simultaneously in the a11y tree — the `:187-192` rationale ("two identically-named controls are indistinguishable") describes a state that cannot occur, and what ships is one logical command whose announced name mutates with chrome state, suffixed by an implementation detail ("collapsed dock") no user can act on. Both original axes held the fact and reasoned past it (D WITHDRAWN-3 → D-S-2; C S+5's falsifier → "but not wrong") — a corpus-internal contradiction the r2 reader is right to book. **Scoping (this seat's)**: the banked kf-ChromeDock D-18/C-1 cure prescribes this block as the house pattern — its load-bearing half (**a real focusable Button in the collapsed slot**) is untouched by this row; only the state-dependent *name* half must not propagate. The reader's honest falsifier is strengthened by my read: the press-keepalive can only ever target `.dock-layer--full` controls (`dock.js:540`), so the transient both-exposed window is press-scoped and full-side-only — no AT-navigation state ever sees two play controls. Cure: one stable name (drop the suffix), decided in the TD-5/TD-28 prose pass. |
| 7 | **Reader-DU's two UNDISPOSED challenge rows (D-m2 / D-m4)** | **r1 CORRECTED — ADMITTED INFO as TD-34 / TD-35** | Roster omission verified (supersession note above). D-m2 at the bytes: `design-idioms.css:114-119` `@utility icon-lg { @apply size-6; & svg { @apply size-6 } }` — the class ON the svg sets a 24 px border-box; `:76` `pl-0.5` (2 px) yields a 22×24 content box → `preserveAspectRatio` fits the glyph at −8.3 % with a net ~+1 px nudge; `:212` `icon-md pl-px` → 19×20, −5 %: same glyph, two mirrors, two optical scales, the nudge partly spent on shrinkage. D-m4: `:57` hand-rolls `gap-3` (fixed 12 px) where the producer's rhythm token governs every sibling gutter (`crossfade.css` `.dock-face-content { gap: var(--dock-layer-gap, 0.375rem) }`, `layers.css` same on `.dock-layer`; `density.css` md scales it by `--dock-scale`; coarse ≈ 7.02 px) — the transport's internal gutters alone do not scale with pointer type. Both INFO, riding the TD-31 hygiene cluster. |
| 8 | **The readers' kill work** | **ALL SUSTAINED; six self-kills BANKED as negative results (K11–K16)** | Both readers independently re-derived r1's K1–K8 with zero drift (reader-DU reproduced the 3600 ms compile, the 143-line count, the pastel ceiling; reader-LC re-compiled the UNKEYED_FRAGMENT flag and re-ran prettier) — recorded as corroboration at the bank, nothing re-opened. The self-kills were each re-verified by this seat before banking: the line-clamp structural-impossibility lead dies at the import map (DockTrigger `for="select"` renders **reka's** SelectTrigger, not glass select's — the `[&>span]:line-clamp-1` string never reaches this trigger; the inverse residue belongs to ChromeDock); `--dropdown-min-width` is owned (`layout.css:16`, DEFS=1 — both readers convergent, my grep third); the idle-collapse focus-destruction lead dies at `dock.js:915-919` (`w()` moves displaced focus to the incoming face after nextTick; `:1113-1115` makes it a legitimate target); `hideIndicator`/`for`/`shape` are declared props (d.ts + compiled defaults, my reads); the inline-arrow rerender lead dies at cacheHandlers; the progress-map key-space lead dies by shared derivation (→ superlative 8). |
| 9 | **Booking harmonizations** | **No re-books; riders adopted at the banks** | Reader-DU's D-B1-at-BLOCKER row stays what r1 made it: **TD-13 FOLD ≡ kf-App KF-APP-6**, blocker-weight at the bank (the reader's own fold note concurs; recorded so the *fifth* refiling ordinal is not compounded — the ND-1 ledger counts this corpus's D axis as the fourth). Reader-DU's TD-10 rider (`isStarted` is a GROUP-scalar painted into a PER-ROW indicator slot — every dropdown row renders the identical mark, zero per-row information, while `ChromeDock.vue:250/:266` uses the same slot row-variantly) — verified at both templates, **adopted at TD-10**. Reader-DU's TD-11 rider (ChromeDock writes `v-for` + `:key` directly on SelectItem in the structurally identical dropdown — a local slip against an in-tree pattern) — verified `:256-258`, **adopted at TD-11**. Reader-LC's re-cite of TD-21's docblock anchors adopted at the bank. Reader-LC's L-M-1 and C-B-1 handling was registry-correct this round (folded to kf-ACG M-7 / census SCH-1 without re-booking) — the K9/K10 lesson took. |

**Adjudicator's corrections register (against this corpus's readers):** (1) reader-DU miss #2's TD-21-precondition cell — reversed, K17/ruling 3; (2) reader-DU's crossfade-geometry citation — `layers.css`, not `crossfade.css`, for the summary/full pair (ruling 3); (3) reader-LC miss #1's scope — app-wide, stated per the scope law (ruling 1); (4) reader-DU's "eight utilities on the host line" — the host carries ten across the two array rows (`:5-8`, my count; nothing turns on it). Everything else in both returns survived this seat's re-derivation intact.

## Adjudicated defect roster

Routing law: **KF.W0–W10** per the census wave sketch; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the transport repair packet); **glass-ui BH relay** per the standing owner edict. Anchors bind to worktree ≡ `origin/master` `81a56990`; re-anchor before any cure (D-19).

### Carried rows — TD-1..TD-33 (full cells at `1eb80754`; one line each here so the registry file stays usable)

| id | sev | claim (compressed) | disposition |
|---|---|---|---|
| TD-1 | MAJOR | The only keyframes.js consumption is 100 % dead: functional-lucide ref = `SVGSVGElement` vs `instanceof HTMLElement` ×2 (`useIconSpin.ts:5,7`); both entry points dead | NO-WAVE-OWNER (packet; cure with TD-4 + PRM + test, one commit) |
| TD-2 | MAJOR | Space on the focused expanded Play actuates twice (window registry + local keyup); held Space rapid-toggles; no `.stop` at `:72-73` | NO-WAVE-OWNER + BH-relay rider (registry `e.repeat`/BUTTON policy) — **now one commit with TD-38/TD-40** |
| TD-4 | MAJOR | `useIconSpin`: eager `kfEngine()` at setup, mount-time parse, zero teardown — latent only via TD-1 | NO-WAVE-OWNER (in the TD-1 commit) |
| TD-5 | MAJOR | `:always-expanded="false"` is a no-op; the 19-line NAMED DECISION documents an inert lever and a collapsed detent that does not ship (mounts EXPANDED) | NO-WAVE-OWNER; boot posture batches with kf-ChromeDock RR-1 M#4 |
| TD-6 | MAJOR | Select tooltip keyboard-unreachable: `as-child` onto a non-focusable div; describedby on the wrapper | NO-WAVE-OWNER (markup pass with TD-11/TD-14) |
| TD-7 | MAJOR | Both play Buttons opt out of `icon-only`, forfeiting the sole coarse tap-floor selector; never a circle on any pointer | NO-WAVE-OWNER; boxes → SS-13 |
| TD-8 | MAJOR | White glyph over `.rainbow-pastel` ceiling 2.10:1 vs 3:1, theme-invariant | NO-WAVE-OWNER interim + BH relay (rainbow rung); vivid arm → SS-13 |
| TD-9 | MAJOR | `emphasis="quiet"` on the stated PRIMARY CTA, hand-repainted; three material vocabularies in one row | NO-WAVE-OWNER; producer arm rides the TD-8 relay |
| TD-10 | MAJOR | StatusDot `warning` = "paused" hazard-mark misuse, no `label` · **r2 rider: `isStarted` is a group-scalar in a per-row slot — every row identical, the selection slot vacated (ChromeDock uses it row-variantly)** | NO-WAVE-OWNER + BH relay (idle rung) |
| TD-11 | MINOR | `<template v-for>` no `:key` → UNKEYED_FRAGMENT; `--dot-p` can paint across rows · **r2 rider: ChromeDock keys the identical dropdown — a local slip, not house style** | NO-WAVE-OWNER |
| TD-12 | MINOR | `class` on `<Select>` silently discarded (`inheritAttrs:false` fragments) | NO-WAVE-OWNER — delete |
| TD-13 | FOLD | `--dock-margin` phantom (DEFS=0/USES=10; `:389/:395`) ≡ **kf-App KF-APP-6** (BLOCKER at its bank) → KF.W6; fourth refiling recorded, r2's D-B1 row is the fold's fifth citation, not a booking | FOLD |
| TD-3 | FOLD | per-frame `animationProgress` identity churn ≡ **kf-ACG M-7** (MINOR at its bank; the child-boundary mechanism is the strengthening rider there) | FOLD |
| TD-15 | MINOR | dead `reset(all)` arm ≡ kf-ACG D-11/C-8 (FOLD) + component-local `expandTimeline`-only-`false` rider | FOLD + rider |
| TD-16 | MINOR | duplicate imports ×2 specifiers; SFC not prettier-clean | NO-WAVE-OWNER |
| TD-17 | MINOR | composables bind template-refs by hard-coded string with unhonourable generics | NO-WAVE-OWNER (TD-1 re-authoring) |
| TD-18 | MINOR | lazy barrel has zero consumers; the one consumer eager-imports | NO-WAVE-OWNER |
| TD-19 | MINOR | 2 of 3 composables untested (the broken one included); typecheck half ≡ kf-App KF-APP-4 → KF.W4 | split |
| TD-20 | MINOR | empty-state `<List>` unsized: 24 px beside a 12 px chevron | NO-WAVE-OWNER |
| TD-21 | MINOR | press-origin `Set` shared by both mirrors ("either control", not "same target"); mouse-path live · **r2 rider (this seat): `:61` delete-as-guard leaks a stale id on release-over-neither-control (TD-37 makes that the in-place default; no capture, persistent mouse pointerId) → delayed F3 resurrection** | NO-WAVE-OWNER (TD-2 commit; cancellation law with TD-41) |
| TD-22 | MINOR | `expand()` DEMOTES a pinned dock to `"hover"` while the comment calls it "re-pin" (≡ kf-ChromeDock RR-2 M#1 mechanism; consumer site + comment inversion) · **r2 tie: TD-38's undeclared click-pin is the accidental delivery of the promised re-pin, mouse-only** | one cure spec with the ChromeDock row |
| TD-23 | MINOR | three tooltips announce name twice (description ≡ aria-label verbatim) | NO-WAVE-OWNER |
| TD-24 | INFO | menubar peak destroyed per unmount — moot today (consuming chain IACVT under TD-13) | INFO→MINOR after KF.W6 |
| TD-25 | INFO | prose names retired `DockSelectTrigger` (≡ ChromeDock rider, second site) | rides the bank |
| TD-26 | INFO | `storedControls` ambient store wearing a prop's clothes | NO-WAVE-OWNER |
| TD-27 | INFO | `String(key)` launders nullish | NO-WAVE-OWNER |
| TD-28 | INFO | 143 narration lines (count is L's, D's 87 killed); two orphaned blocks assert falsified invariants | NO-WAVE-OWNER — the largest deletion |
| TD-29 | INFO | `.text-ellipsis` no-op (D-M2's MAJOR killed at the producer cap/scroll) | NO-WAVE-OWNER |
| TD-30 | INFO | crossfade-strand cure covers 1 of 4 actions — argumentative, strand unproven | SS-13 #1 decides |
| TD-31 | INFO | design-minor cluster (inert `justify-items-center`; no collapsed empty-state; bold/semibold split; two-deep `var()` mask; 6-of-7 rainbow override) | NO-WAVE-OWNER — **TD-34/TD-35 join this cluster** |
| TD-32 | INFO | the FOURTH `useDockShellProps.d.ts` doc-drift cell (manufactured D-M2's false MAJOR) | BH relay (D-13 ledger) |
| TD-33 | INFO | corpus-method: D's cascade cells cite a git-ignored local `dist/gh-pages` build | re-derive from an origin build before citing |

### New rows — TD-34..TD-41 (minted this record)

| id | source | sev | claim (adjudicated) | terminal disposition |
|---|---|---|---|---|
| **TD-34** | D-m2 (r1-undisposed; ruling 7) | INFO | The optical nudges **shrink the glyph instead of translating it, by different amounts in the two mirrors**: `icon-lg` sets a 24 px border-box on the svg itself and `pl-0.5` eats 2 px of it (22×24 content, −8.3 %, net ~+1 px nudge); the mirror's `icon-md pl-px` yields 19×20 (−5 %). Same glyph, two optical scales, the intended nudge mostly spent on shrinkage | **NO-WAVE-OWNER** — translate (`translate-x`), don't pad; joins the TD-31 cluster |
| **TD-35** | D-m4 (r1-undisposed; ruling 7) | INFO | Hand-rolled `gap-3` (fixed 12 px) inside the dock instead of `--dock-layer-gap` — the one token every producer gutter rides (`0.375rem × --dock-scale`; ≈7 px at coarse) — so the transport's internal rhythm alone ignores `--dock-scale` while every sibling control grows 1.17× | **NO-WAVE-OWNER** — `gap: var(--dock-layer-gap, 0.375rem)`; TD-31 cluster |
| **TD-36** | DU miss #1 (ruling 2) | **MAJOR** | **The transport host is a full-viewport-width pointer trap**: `fixed left-0 right-0 z-dock`, ~90 px tall, no `pointer-events: none` anywhere on it and no re-enabling wrapper — every pixel of the band outside the centred pill swallows pointer events over the stage/timeline beneath (mobile stage is `fixed; inset: 0` under z-dock; the band-reserve that would hold content clear is IACVT under the TD-13/KF-APP-6 bank). The sibling dock declares the exact guard it omits (`ChromeDock.vue:213-218`: `pointer-events-none` host + `pointer-events-auto` child) — and the sibling is *narrower* | **NO-WAVE-OWNER** — adopt the ChromeDock pair (two classes); **sequenced note**: independent of, but masked-in-consequence by, the KF.W6 token cure; occluded extent → SS-13 #9 |
| **TD-37** | DU miss #2 (ruling 3) | **MAJOR** | **The primary Play CTA changes ends of the dock between the two faces**: collapsed = `[name][play]` (play trailing, `:182-213`); expanded = `[play] │ [select] │ [reset][chip]` (play leading, `:57-80`); the faces are concentric (`layers.css` `.dock-layer { grid-area: 1/1 }`, this seat's corrected anchor), so every hover-expand and every 3600 ms idle collapse translates the most-used control across the pill width — out from under the reaching pointer, defeating motor memory; the file's own "the detent never strands the play affordance" (`:30-33`) holds for presence and fails for position. **And the reversal converts TD-21's shared-Set pass into a stale-origin leak** (the in-place release lands on neither control — ruling 3's rider, banked at TD-21) | **NO-WAVE-OWNER** — one face order (an SS-2 design decision: play occupies the same end in both faces); Δx → SS-13 #10 |
| **TD-38** | DU miss #3 (ruling 4) | MINOR | **The collapsed mirror's five `.stop`s are inverted against the dock's listener phases**: the three pointer `.stop`s are provably inert (the dock's pointer/click listeners are capture-phase, `dock.js:806-808`), the two keyboard `.stop`s are the only load-bearing ones (TD-2 immunity), and the one bubble handler the dock actually registers on that layer — `onClick → N() → "pinned"` (`:832-838`) — is left unstopped: a mirror press **doubles as an undeclared pin on the pointer path only** (keyboard preventDefaults the click away, `usePlayActuation.ts:72/:77`). Sequencing (this seat): pointerup→`expand()`→`"hover"` then click→`"pinned"` — the undeclared route accidentally delivers the "re-pin" the TD-22-inverted comment promised, mouse-only. Third independent falsification of `:311-312` | **NO-WAVE-OWNER** — one propagation policy for the mirror (stop-or-don't, stated), in the TD-2/TD-40 commit; the inert-timing branch (does the click land at all?) → SS-13 #11 |
| **TD-39** | DU miss #4 (ruling 6) | MINOR | **The state-dependent accessible-name split rests on a false premise**: exactly one dock layer is ever in the a11y tree (`inert`, `dock.js:822-838`), so "two identically-named controls" (`:187-192`) is a state that cannot occur — what ships is one logical command whose announced name mutates with transient chrome state, suffixed by an implementation detail ("collapsed dock"). Both original axes established the premise and praised the conclusion anyway; the registry adopted it (r1 superlative 4), and **kf-ChromeDock's banked D-18/C-1 cure prescribes the block** — so the error is mid-propagation. Scoped: the focusable-Button half of that prescription is correct and untouched; only the name split must not travel | **NO-WAVE-OWNER** — one stable name across both faces (drop the suffix), in the TD-5/TD-28 prose pass; **rider on the kf-ChromeDock D-18 cure spec**: copy the Button, not the name split |
| **TD-40** | LC miss #1 (ruling 1) | **MAJOR** | **Space — the standard button activation key — is inoperable on Reset (`:158`) and Collapse-timeline (`:171`) and fires playback instead**: the global Space shortcut (`useControlsKeyboardShortcuts.ts:50`, `preventDefault: true`) is dispatched from a window bubble listener that never skips BUTTON targets and preventDefaults **before** invoking (`keyboard.js:48-52/:70`), cancelling the keydown default → the browser never synthesizes the activation click these two `@click`-only native buttons entirely depend on → the pressed control does nothing and `toggleAnimationGroup()` runs. Enter unaffected (unregistered). The component's own design *depends* on the same mechanism (`usePlayActuation` preventDefaults precisely to suppress the synthesized click, `:309-310`). **App-wide scope stated**: every non-skipped focusable button in the demo loses Space activation while ACG is mounted | **NO-WAVE-OWNER** — one keyboard-propagation commit with TD-2/TD-38 (demo side: scope the Space registration away from button targets, or stop keydown at the controls); **+ glass-ui BH relay rider** on the standing registry item: the dispatcher needs a BUTTON-target/`defaultPrevented` policy, now with the app-wide suppression evidence; live twin → SS-13 #12 |
| **TD-41** | LC miss #2 (ruling 5) | MINOR | **`usePlayActuation`'s keyboard arm has no cancellation path**: `spaceArmed` (`:47`) is cleared only inside the keyup handler; a focus change / inert swap mid-press (reachable: layer inert stamps + the 3600 ms timer) strands it `true`, and the flag is one closure shared by both mirrors — a later bare Space keyup on either actuates with no matching keydown (F3's keyboard twin). The producer solved this on the same element (`useLiquidPress` binds `onBlur`/`onPointerleave` release). **Extended (this seat): the pointer arm shares the hole** — `pointercancel` does not cover mouse release-elsewhere (the TD-21 stale-Set rider) — so the cure is one cancellation law across both arms | **NO-WAVE-OWNER** — blur/`pointerleave`-class disarm + per-control origin, in the TD-2/TD-21 commit; the isolation suite gains the orphan-keyup and stale-id cases; live twin → SS-13 #13 |

## Identity-guard folds (r2 delta — nothing re-booked)

- **All 33 r1 identities checked against both r2 returns: zero re-bookings attempted or admitted.** Reader-LC folded L-M-1 → kf-ACG M-7 and C-B-1 → census SCH-1 unprompted; reader-DU recorded D-B1 as the KF-APP-6 fold's citation, not a filing.
- **TD-37's stale-Set rider ≡ TD-21** (one identity: the shared Set; second live path).
- **TD-38's pin mechanism ≡ TD-22's banked producer row** (N()/D() asymmetry) — TD-38 books only the propagation-policy inversion and the modality asymmetry.
- **TD-39 ≡ r1 superlative 4's subject** — booked as its corrective coda + a rider on the kf-ChromeDock D-18 cure spec, not as a competing a11y row.
- **TD-40 is NOT TD-2** (zero-fire + wrong command vs double-fire) **and NOT kf-ACG M-1** (suppressed-native-activation class vs both-handlers-run class; different keys, different victims) — all three are inputs to ONE keyboard-propagation spec row.
- **TD-34/TD-35/TD-36/TD-37/TD-38/TD-39/TD-40/TD-41: first bookings** — registry sweeps in the method block (pointer-events-none, spaceArmed, Space rows, face-order rows) all zero at the relevant files.

## Killed-claims register — K1–K10 carried (at `1eb80754`, all standing) + K11–K17 (r2)

| # | killed claim | adjudication |
|---|---|---|
| **K11** | Reader-DU SELF-1: glass-ui's `[&>span]:line-clamp-1` makes `.text-ellipsis` structurally impossible (and ChromeDock's `line-clamp-none` is the neutralizer) | **SELF-KILL BANKED, re-verified** — `dock.js` binds `SelectTrigger` from **reka-ui**, not glass select's chunk; no line-clamp reaches this trigger; TD-29 stands unmodified as a plain no-op. Inverse residue (why ChromeDock carries the neutralizer at all) belongs to ChromeDock's file, noted there-not-here |
| **K12** | Both readers' hypothesis: `min-w-[var(--dropdown-min-width)]` is a second phantom token of the D-B1 class | **CONVERGENT SELF-KILL BANKED** — `layout.css:16` defines it (DEFS=1, "recurs ×3"), my grep third. Recorded because KF-APP-6 invites exactly this over-generalization: the phantom class is real and is NOT ambient in this file |
| **K13** | Reader-DU SELF-3: idle auto-collapse destroys keyboard focus (outgoing layer inert while holding activeElement) | **SELF-KILL BANKED, re-verified at `dock.js:915-919`** — `w()` moves contained focus to the incoming face after nextTick; `:1113-1115` stamps the active face `tabindex:-1`. Deliberately handled; no finding |
| **K14** | Reader-LC's candidate: `hide-indicator`/`for="select"`/`shape="icon"` are undeclared props leaking as invalid DOM attributes | **SELF-KILL BANKED, re-verified** — all three declared (`SelectItem.vue.d.ts` `hideIndicator?: boolean`; `DockTrigger.vue.d.ts` `for?: "select"\|"dropdown"\|"popover"`; `dock.js:1121` `shape: { default: "icon" }`) |
| **K15** | Reader-LC's candidate: inline arrow handlers at `:158`/`:171` churn identity per render, forcing 60 Hz prop patches on DockControl | **SELF-KILL BANKED** — `<script setup>` enables cacheHandlers; the compiler emits the handlers into `_cache`. Recorded so no wave "optimises" them on a false premise |
| **K16** | Reader-LC's candidate: the progress map's key space diverges from `animationNames` (a second TD-1-shaped silent dogfood death) | **SELF-KILL BANKED → SUPERLATIVE 8** — both sides derive from the same two sources under the same discriminant (`channels?.map(c => c.name) ?? Object.keys(…)` at ACG `:181-183` vs `useAnimationProgress`'s channel/group paths); the one predicate divergence is unreachable behind the `:98` mount gate. The single most likely second-death site, attacked and cleared |
| **K17** | Reader-DU miss #2, cell (ii): "the order reversal is the geometric precondition for the banked TD-21 press-origin defect" | **KILLED BY THIS SEAT (ruling 3)** — inverted: co-located mirrors would make the cross-mirror release match user intent; the reversal instead makes the in-place release land on **neither** control, which leaks a stale id through `:61`'s delete-as-guard (no capture, persistent mouse pointerId) — a *delayed* F3, strictly worse and differently shaped. TD-37 stands on its other three legs; the corrected consequence is banked as TD-21's r2 rider |

## Superlatives — 9 (r1's seven carried with amendments; two new)

1–3, 6–7 **carried whole from r1** (channel-zone elision + dockCardinality rider; the `--dot-p` one-property handoff; `useMenubarMeasure`'s acyclic teardown; `.menubar-safe-pb`'s two load-bearing-but-void arms; `useDemoTicker` with its KF-AV codas) — re-affirmed by both r2 readers, nothing moved.
4. **The collapsed mirror is a real focusable play control in the collapsed face — the half of the pattern kf-ChromeDock's D-18 cure rightly wants** *(r2 coda, per TD-39)*: the **name-split** half is built on a premise the corpus itself disproved (inert exclusivity) and must not travel with the prescription. One command, one stable name, one focusable seat.
5. **`usePlayActuation` is the best-specified unit in the transport — now carrying FIVE riders**: TD-2 (composition vs a window listener), TD-21 (shared Set + the r2 stale-id leak), TD-38 (its preventDefault purity is also what forks the modalities at the dock's click handler), TD-40 (the same suppression mechanism it uses deliberately is what kills its sibling controls accidentally), TD-41 (no cancellation path in either arm). The contract remains the right shape; every rider is composition, not derivation. **Nobody may cite this superlative as evidence the play path is sound.**
8. **NEW (reader-LC; K16): the second dogfood path is provably NOT dead** — the progress ring's key space holds by shared derivation, not luck; the strongest candidate for a second TD-1 was attacked at its sharpest point and survived. A negative result banked so no future seat re-derives it.
9. **NEW (reader-DU): the file subtracts, with provenance, five separate times** — the DM-1 band-aid EXCISED for a structural cure (`:293-302`), "Clear all & reload" MOVED OUT (`:368-372`, zero `Trash2` symbols remain), the single-animation static name span DELETED (`:82-87`), the `.progress-dot` recipe PROMOTED to the idiom layer (`:399-402`, verified present), every `title=` passthrough REMOVED (`:53-56`, zero remain). Verified at this seat's whole read. TD-28 is right that 143 narration lines is too many — this is the half of that prose that earns its keep.

## UNPROVEN-NEEDS-LIVE — KF.W9 SS-13 visual-audit inputs (13)

1–8 **carried from r1**: the TD-30 strand probe (~3.6 s boundary) · TD-7 rendered boxes (fine/coarse) · TD-8 vivid-arm per-pixel · TD-3 cost profile (at its bank) · TD-13 rendered consequence at 390×844 (at its bank) · TD-21 cross-mirror live twin · TD-2 Space single-toggle twin · the AT-activation path of `usePlayActuation` (no `click` listener — the one hole in superlative 5 no static read closes).
9. **TD-36 occluded extent** — `getBoundingClientRect()` on the `[data-dock-tether="bottom"]` host at 1440×900 and 390×844 + an `elementFromPoint()` sweep along the band's midline outside the pill; before and after the KF.W6 `--dock-margin` cure.
10. **TD-37 translation magnitude** — the play control's centre-to-centre Δx between the two faces (the reversal is source-conclusive; the distance is not).
11. **TD-38 end-state probe** — dock state after a mouse press on the collapsed mirror (`pinned` if the synthesized click beats the inert patch, `hover` if not) vs after an Enter press (predicted `hover`). Decides which branch of the inverted `.stop` set ships; either way the policy is wrong.
12. **TD-40 live twin** — focus Reset, press Space: predicted no reset + playback toggle; Enter resets. Repeat on the Collapse chip. Then the app-wide cell: Space on a focused CopyButton.
13. **TD-41 + TD-21-rider twins** — (a) hold Space on the expanded Play, force the inert swap (or Tab away), release, then press-release Space on the surviving mirror: assert exactly one actuation, none from the orphan keyup; (b) mouse-down on the collapsed mirror, release over the select region (no actuation expected), then later drag onto either play control from outside and release: **the static read predicts a spurious actuation** from the stale id — the born-RED witness for the cancellation law.

## Routing summary (M-25 agglomeration input, r2 delta)

- **NO-WAVE-OWNER — the transport repair packet** gains: **the keyboard-propagation commit** TD-2 + TD-38 + TD-40 (one propagation policy: `.stop` symmetry on both mirrors decided once, Space registration scoped away from button targets, composition tests that mount the buttons WITH the registry) · **the actuation-cancellation commit** TD-21(+r2 rider) + TD-41 (per-control origin, release-elsewhere cleanup, blur-class disarm, orphan-keyup + stale-id test cases) · **TD-36** (the ChromeDock pointer-events pair, two classes) · **TD-37** (one face order — SS-2 design decision) · **TD-39** (one stable name, with TD-5/TD-28's prose pass) · **TD-34/TD-35** (TD-31 cluster).
- **KF.W6**: unchanged (TD-13 at its bank; TD-24 sequenced after; TD-36's *consequence* interacts — the band-reserve cure does not remove the trap, only the content beneath it).
- **KF.W9 SS-13**: the 13 probes above.
- **glass-ui BH relay (one letter, extending r1's four items)**: the keyboard-registry item now carries TD-40's app-wide suppression evidence (BUTTON-target/`defaultPrevented` policy + `e.repeat` guard); rider on the kf-ChromeDock D-18 cure spec: adopt the focusable-Button half of the transport's collapsed slot, **not** the name split (TD-39).
- **Cross-registry riders placed**: TD-10 (vacated per-row slot), TD-11 (ChromeDock keyed precedent), TD-21 (stale-id leak), TD-22 (TD-38's accidental re-pin), kf-ChromeDock D-18 (TD-39).

## Closing verdict

**ADJUDICATED — r2 supersession: the second re-read round vindicates the registry discipline and still moves the frontier.** Both r2 readers ran registry-first on the confirm side (zero attempted re-books — the K9/K10 lesson held), independently re-derived every r1 kill without drift, killed six of their own leads in writing, and surfaced **six unbanked defects** in a component already carrying 33 adjudicated rows — two of them (TD-36, TD-40) in the class a repair wave would have shipped straight past: a full-bleed pointer trap whose guard the sibling dock demonstrates, and a global shortcut that silently disables Space on every button it doesn't skip, including the two transport controls beside the one it double-fires. This seat's own contribution beyond admission: one reader proof-cell reversed into a sharper defect (the end-swap does not precondition TD-21 — it converts it into a stale-origin leak, K17), the two-arm cancellation law unifying TD-41 with the pointer side, the sequencing derivation tying TD-38's undeclared pin to TD-22's inverted comment, the app-wide scoping of TD-40, and the correction of r1's only two bookkeeping omissions (TD-34/TD-35). Terminal shape: **41 rows (TD ids for life) — 0 fresh BLOCKER · 12 MAJOR · 14 MINOR · 12 INFO · 3 folds; K1–K17; 9 superlatives; 13 SS-13 probes.** The repair packet's spine now reads: a transport whose only engine consumption is dead, whose primary control double-fires on Space while its two neighbours zero-fire on the same key, whose host swallows a viewport-wide band of pointer events, whose CTA moves out from under the reaching pointer on every crossfade, and whose actuation contract — the best unit in the file — leaks press state in both modalities the moment composition exceeds its isolation suite. Nothing here stamps a wave; every disposition is M-25 agglomeration input awaiting the owner's begin-word.

— end of apotheosis r2. The three challenge files, both r2 reader returns, and the r1 record (commit `1eb80754`) remain immutable beside this file per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.
