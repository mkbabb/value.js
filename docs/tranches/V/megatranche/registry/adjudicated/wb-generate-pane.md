# wb-generate-pane — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-10 · **HEAD**: `954b255c` · branch `tranche-u` · **Corpus**: `demo/workbenches/generate/` (GeneratePane.vue **40** · GenerateControls.vue **311** · composables/useColorGeneration.ts **42** — re-measured by this seat; the r2 L-file's §Scope numbers are wrong, see K-15) plus the contract surfaces it rides (usePalettePorts/usePaletteActions/usePaletteStore · usePaneRouter.ts · App.vue PaneSlot wiring · viewSchema.ts · Dock.vue · PaneHeader.vue · color-session/generate-color.ts · o20/o18 oracles). Challenge axes on disk: challenge-C-implementation.md · challenge-D-design.md · challenge-L-library.md (r2, governing) + two `seat-1-9268f054` axis files whose unowned rows the readers adopted explicitly.
**Re-readers adjudicated**: TWO hostile returns (design-and-usage · library-and-code): return-1 = 29 confirmed · 9 killed · 9 missed; return-2 = 24 confirmed · 8 killed · 7 missed. Both attacked before adoption; every kill re-derived, every promoted miss re-walked in source, and every verdict-level reader-vs-reader disagreement (L-5, C-8, D-4's a11y limb, C-9, the o18-rail framing, D-6.4's mechanism, four severity conflicts) ruled with this seat's own instruments.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling, no dev server)**: whole-file reads of GeneratePane.vue, GenerateControls.vue, useColorGeneration.ts, generate-color.ts; `wc -l` on all three component files; installed glass-ui 7.0.0 probed by node/grep — **`dist/components/button/Button.vue.d.ts` prop union = `{emphasis, tone, size, iconOnly, loading, type, disabled, class} + PrimitiveProps` — NO `variant`**; compiled `button-Bu9F4uU6.js` `emphasis: { default: "secondary" }`, zero `variant` handling, `glass-wash glass-capsule` gated on the emphasis computed; **`dist/components/select/index.d.ts` exports `SelectProps`/`SelectEmits`; `Select.vue.d.ts` `"update:modelValue": [value: SelectionValue]`; root `index.d.ts:18` `export * from "./components/select"`; `SelectionValue = string | number`** (selection.d.ts:2); select chunk `TypeError("[glass-ui] Select received a non-scalar value.")` ×3 guards; `dist/watercolor-dot.js` — `inheritAttrs:!1` true, `renderSlot` count 0, no `tag`, root `aria-hidden:"true"`, consumer style spread FIRST then `pointerEvents:"none"` (component wins), **`seed: c = ""` destructured ONCE at setup while the silhouette watch recomputes from `(newColor + captured c)` and the turbulence seed is a live computed** — the seed-staleness miss verified at the bytes; `dist/slider.js` = **2-line barrel**, impl chunk `slider-*.js` = **3 `pointercancel` occurrences** (window-level register + teardown); `dist/dock.js` DockLayer `inert: !i.value || void 0` + `"aria-hidden": !i.value || void 0`; `dist/components/instrument-chassis/types.d.ts` = `proportion?: "golden"|"preview-dominant"` — no `variant` prop; glass-ui.css spectrum rules — thumb `width:calc(var(--slider-thumb-size,1rem)*.75)` = 12px, `background:var(--slider-thumb-bg,transparent)`, track `background:var(--slider-track-bg,var(--secondary))`, **range `background:0 0` producer-side**; demo/ui = 19 dirs, select barrel re-exports 8 component names and ZERO types; `grep InstrumentChassis demo/` = 0; App.vue:77/:83-92 (mobile PaneSlot, no `:on-mount`) vs :101-109 (desktop `:on-mount="onDesktopLeftMount"`), :317 `generatePaneRef = ref<any>(null)` sole writer :322-328; usePaneRouter.ts:73/:106-110/:191-199; usePalettePorts.ts:138-153 = **15 members counted**; usePaletteActions.ts:65-77 `onCurrentPaletteSaved(name, colors)` + `expandedId` feedback; usePaletteStore.ts:60-95 dedup on `(name.toLowerCase(), colorsMatch)` + `slug: createSlug(name)`; MixPane.vue:39-46 ("Mixed Color"/"Mixed Palette" — the family); PaletteColorStrip.vue:2-4 `aria-hidden role="presentation"`; ExtractControls.vue:13-35/:144-152 (plate-ink + `data-o18="extract-k-rail"` + non-bold label twin); PaneHeader.vue:54-57 + :185-205 (scroll-timeline trio); animations.css:184-193 PRM guard; src/css/grammar.ts:283-285 **`Number(value.toFixed(12))`**; foundation.css:678-696 tier-1 roster (rosters `.glass-slider[data-variant="spectrum"] .slider-range` — producer-transparent — and `.generate-swatch` + `.watercolor-swatch`; `.generate-swatch` again at :834 print); o20-generate-plate.spec.ts:36-41/:85-105; `.github/workflows/` grep playwright|e2e = **0**; o18 grep generate = **0**; eslint.config.js:71 `no-unused-vars: "off"`, `noUnusedLocals` in no tsconfig; canon verbatim re-reads — OPTICAL-BENCH-COMPOSITIONS.md:41 (Generate row: P122 golden, "Landmark-neutral chassis; no nested flat Card/local grid"), Card-inventory closing ("the other sixteen compositions have Card count 0"), Generate boundaries `[]`/reserve `none`, :108-area **P051** ("WatercolorDot owns only its noninteractive organic face/edge and removes its button/tag branch in the clean major … Operable sites use named geometric seats; ornamental faces are aria-hidden"); VISUAL-CONSTITUTION.md:89 (PaneSegmentedControl retired) + :104 (one slider composition; "feature waves own their domain arrangement, not new slider mechanics"); PROPORTION-AUDIT.md:72 (card law 7 — "Accessibility floors do not require bloated visible chrome") + :78 (card law 13 — mono roles carry no weight clause) + :15 (the sole non-bold instruction, sans-scoped); motion-quarantine.md:124-125 + :150-172 (**M-33 FALSE — not carried; M-34 TRUE — the one surviving reduced-motion obligation, MQ-1**, blast radius names Generate); **X wave bounds read at W0-W11**: X-W4 unit b holds GenerateControls.vue (LabeledField Select adoption — deletes the `section-label` siblings, names triggers through `labelledBy`), unit d holds the SceneActionSet (deletes `generatePaneRef` + the nine `?.` dispatches, models `unavailable`), unit g (trigger-gated, CLOSED unless X-W0.j census PASS) holds impostor seat `:203` + CC-105 (the four `--slider-track-bg` sites incl. GenerateControls.vue:305); X-W5 owns viewSchema.ts (deletes `left/right` + `PaneSegmentedControl.vue`, "the shell caps neither block axis") + gates A3/C2 (mobile Regenerate parity/liveness, RED of record); X-W7 owns usePalettePorts/usePaletteActions/usePaletteStore; X-W8 owns `demo/ui/**`; X-W10 owns PaneHeader.vue (":73 Move PaneHeader.vue's three scroll-timeline declarations inside `@media (prefers-reduced-motion: no-preference)`") + unit f owns foundation.css; X-W1 owns every CI byte; W6:17 — **CC-108/A-17: "the producer's InstrumentChassis is dead"**, and W6's routed-scene list (Gradient, space catalog, Easing, HeroBlob, Blob/Atmosphere, Mix) **omits Generate**; **no X wave's bounds hold `GeneratePane.vue`, `useColorGeneration.ts`, `generate-color.ts`, `o20-generate-plate.spec.ts`, or an o18 generate enrollment.**

## RULING PINS (this seat's own derivations)

1. **Return-2's biggest miss is real and fourth-verified: `variant` is a retired Button prop, and the plate's whole action register rides it.** GenerateControls.vue:158 `variant="primary-audacious"`, :167/:177 `variant="ghost"`; the installed d.ts has no `variant`, the compiled component defaults `emphasis: "secondary"` and handles no `variant` — so **all three plate buttons render `data-emphasis="secondary"`**: Regenerate never enters the "deliberate-primary register" its own comment (:153-155) claims, and the two "ghost" icon buttons render as filled secondary capsules. The corpus's own D-axis measurement (the "ghost" button's class list carrying `glass-wash glass-capsule`) proved it and no seat before return-2 read it. Blast radius re-counted: **51 `variant=` bindings near `<Button` across 22 demo files** — the same glass-7 adoption-residue class as the `tag=` sweep (ColorSpaceSelector L-8 precedent).
2. **L-5 dies, and dies harder than return-2's kill states.** The producer exports exactly the type L-5 says it withholds (`SelectEmits`/`SelectionValue`, component barrel + root `export *` — my reads). Return-2's causal re-homing (the demo/ui barrel drops type exports) is true but not even load-bearing: **GenerateControls.vue:14 already imports from `@mkbabb/glass-ui`** (`writeClipboard`), so `SelectionValue` was one token away on an import line that already exists. The reka-ui reach (:33) and the unchecked `as PresetName`/`as HarmonyName` casts (:75-81) are a consumer choice — MINOR, cured wherever X-W4.b touches these Selects.
3. **D-4's screen-reader limb dies; the naming split survives at MINOR.** glass-ui's DockLayer renders `inert`/`aria-hidden` on non-active layers (my dock.js read) and Dock.vue gates the action-bar layer on `actionBarLayerActive` — in the default state the dock's three verbs are not in the AT tree, so "hears 'Save palette' twice" is false except while the Tools layer is open. What survives: one verb shipping under two names ("Copy colors" dock / "Copy all colors" plate), three verbs with two owners, and an o20 assertion (:36-41) whose own comment ("EVERY Regenerate on the page lives inside the plate") is false of the DOM it passes over — `getByRole` skips the hidden layer, so the oracle measures visibility, not ownership.
4. **The BLOCKER's cure is not the bare wire either seat implies.** The emit is `save: [colors: string[], name: string]`; the port action is `onCurrentPaletteSaved(name: string, colors: PaletteColor[])` — **argument order AND element type both differ**, and the child already owns the right shape (`stripColors`, :55-57) which the emit flattens to `string[]` precisely so the parent can rebuild it (return-2's re-derivation miss — the same seam). The honest cure reshapes the emit to `[name, PaletteColor[]]` and lands on the port action, discharging C-1, C-6, and the re-derivation in one edit. **No X wave holds GeneratePane.vue** — the BLOCKER has no owner.
5. **The count slider's re-roll is real under five harmonies and false under the default.** My rng-consumption arithmetic on generate-color.ts: `golden` (:102-106) draws nothing per-hue — count changes append cleanly, and golden IS the default (useColorGeneration.ts:22). `analogous` recomputes `step` from count (every hue moves); `complementary`/`triadic`/`split-complementary` draw per-iteration jitter and `random` draws per-hue — stream shift, every swatch replaced. Return-1's headline ("the user can never add one colour") is corrected to "under five of the six harmonies"; the finding stands MAJOR — the count control's semantics change silently with a selection made in a different control, and the ramp painted into the track churns under the thumb while dragging.
6. **D-1's cure is dead twice, and its defect has no home.** The r1 spelling (`variant="golden"`) names a prop that never existed (`proportion` — my d.ts read; the addendum's self-correction adopted). Since the corpus was written, tranche X ruled the producer chassis **dead** (CC-108/A-17; W6:17 "the producer's InstrumentChassis is dead … a fact to preserve by subtraction") — so the canon's "InstrumentChassis at golden" composition can no longer be built as ruled, and W6's routed-scene recomposition list omits Generate. The confirmed substance (Card-as-page-column + local grid vs the ruled landmark-neutral golden split with Card count 0) survives as a composition divergence whose current ruling home does not exist: NO-WAVE-OWNER, flagged for the next formation's recomposition table.
7. **Motion carriage per the quarantine**: M-33 (the "bottom fade") is a FALSE row and is NOT carried. **M-34 is the quarantine's one surviving TRUE reduced-motion obligation (MQ-1)** and Generate is in its named blast radius: `pane-scroll-fade` on GeneratePane.vue:31 hosts the `--pane-scroll` timeline driving three scroll-driven animations that ignore both `animation-duration` guards (measured byte-identical under `reduce` by the quarantine's own probe). Return-1's promotion is admitted — and its sting: challenge-D files "`prefers-reduced-motion` is handled" as a proved negative for this pane, true of the dots' transition and false of the pane's dominant motion. **X-W10:73 names the exact cure** and owns the file.

## Scoreboard

**41 adjudicated defect rows: 2 BLOCKER · 9 MAJOR · 22 MINOR · 8 INFO.** All 53 reader-confirmed rows survive in some form (merges + 6 severity rulings below); **16 reader-found misses admitted** (9 + 7, every one re-walked in source, two corrected in scope); **15 killed claims** with written refutations. Headline: **the pane throws away the user's palette name on every save and has no owner for the fix; its per-swatch verb and its entire colour output are erased from both the runtime and the accessibility tree; its three action buttons all wear a retired prop and render at one emphasis; its dock verbs are silent no-ops on every phone; and the one oracle written for it compares across a serialization boundary that cannot equate — while the files that must change (GeneratePane.vue, useColorGeneration.ts, generate-color.ts, o20/o18) sit outside every tranche-X wave bound.**

## Ruled disagreements (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **L-5** | return-1: CONFIRMED MINOR (producer withholds the type); return-2: KILLED | **KILLED as filed** (PIN 2) | `SelectEmits`/`SelectionValue` exported at component barrel + root; the file already imports from the root at :14. Residue = the consumer cast, GEN-22. |
| **C-8 reachability** | return-1: unproven hypothesis; return-2: KILLED (producer guard) | **KILLED** (PIN details in K-11) | `TypeError("[glass-ui] Select received a non-scalar value.")` in the select chunk + `SelectItem` iterating `PRESET_NAMES` only. Residue folds into GEN-22. |
| **D-4/L-9 severity + a11y limb** | return-1: MAJOR incl. "SR hears twice"; return-2: a11y limb killed, MINOR | **return-2 wins — MINOR** (PIN 3) | DockLayer `inert`/`aria-hidden` gating read at the bytes; o20's visibility-not-ownership limb survives inside GEN-12. |
| **C-9** | return-1: CONFIRMED MINOR; return-2: "unrounded" KILLED, MAJOR inflated | **SPLIT** — head confirmed MINOR (GEN-39), the literal "unrounded" killed (K-13) | grammar.ts:283-285 `Number(value.toFixed(12))` — rounding exists; the ~57-char-per-colour payload stands. |
| **o18-rail framing** | return-1 missed-row: "skipped by two landed remediations" (MAJOR); return-2: rail half = census gap, not skipped remediation | **return-2's correction adopted; ink half stands as a true skip** | ExtractControls' `trackInk`+inset ring serves its pre-image state (`:min="1"` means generate always has a gradient); the E1-R1 ink skip is clean — four raw `text-muted-foreground` sites against two written in-repo measurements of that ink failing. GEN-7. |
| **D-6.4 mechanism** | return-1: mis-attribution kill + replacement roster finding; return-2: mis-attribution + "forced-colors doesn't strip background-image" | **both refutations recorded (K-4); return-1's replacement ADMITTED (GEN-33); rendering outcome R-6** | glass-ui.css: `--slider-track-bg` styles `.slider-track`; `.slider-range` is producer-painted `background:0 0` for every spectrum consumer — the rostered element never paints, decidable from bytes alone. |
| **count re-roll headline** | return-1: MAJOR, "can never add one colour" | **CONFIRMED MAJOR, head corrected** (PIN 5) | Golden — the default — appends cleanly; the defect is the silent cross-control semantic change under the other five. |
| **D-1 severity** | return-1: BLOCKER; return-2: MAJOR | **MAJOR** (PIN 6) | No dead verb, no data loss — a composition divergence; and the decided cure's producer is dead (CC-108), so BLOCKER-weight urgency attaches to nothing executable. |
| **D-5 / D-10 / L-3 / L-4 severity** | return-1 MAJOR (D-5, D-10) · return-2 MAJOR (L-3, L-4) | **all four MINOR here** | D-5: both palette renderings are already AT-hidden (GEN-2), so the duplication is purely visual. D-10: the name affordance defect is masked today by GEN-1 (the name never persists); escalation rider recorded — it becomes the naming seam the day GEN-1's cure lands. L-3: indirection cost, and return-2's own singleton evidence makes the cure trivial. L-4: 2 of 90+ edges live here; repo identity rides X-W8 (the scope law, ColorSpaceSelector addendum item 1). |

## Verdict register — 41 rows

Line anchors are HEAD `954b255c`; re-resolve before executing any cure (the D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: a wave id means that wave's bounds own the surface today; **X-W4.g rows are trigger-gated** (CLOSED unless the X-W0.j Glass-8 census PASSes — on FAIL they join the cluster); **NO-WAVE-OWNER** rows are booked for the next formation boundary. **GEN-CLUSTER** = GeneratePane.vue · useColorGeneration.ts · generate-color.ts · o20-generate-plate.spec.ts · the absent o18 generate enrollment — the NO-WAVE-OWNER booking this record establishes.

### BLOCKER

- **GEN-1 — D-2 ≡ C-1 ≡ L-1: the palette name the user types is discarded on every save; every generated palette persists as "Generated Palette".** Emit `save: [colors: string[], name: string]` (GenerateControls.vue:48-50, fired :103); handler bound at arity 1 (GeneratePane.vue:14) hardcoding `pm.createPalette("Generated Palette", …)` (:19); sink derives `slug` and the dedup key's name half from the constant (usePaletteStore.ts:66-88). TypeScript's fewer-arity assignability makes it gate-invisible; the source comment :44-47 books the wire that was never written. Family: MixPane.vue:41-45 hardcodes two more names. Cure ruled (PIN 4): reshape the emit to `[name, PaletteColor[]]` (reusing `stripColors`) onto `onCurrentPaletteSaved` — one edit discharging GEN-1 + GEN-6's save limb + the re-derivation seam; return-1's kill of L-1's composable-store cure stands (K-8). → **GEN-CLUSTER, NO-WAVE-OWNER** (GeneratePane.vue in no bounds; adjacent: X-W4.b holds GenerateControls, X-W7 the port).
- **GEN-2 — D-3 ≡ C-2 + C-3: the per-swatch copy verb does not exist at runtime and the pane's entire colour output is erased from the accessibility tree.** `tag="button"`, `:aria-label`, `@click` on WatercolorDot (:199-208) are all discarded: producer `inheritAttrs:!1`, hand-forwards only class/style, hardcoded `aria-hidden` span with `pointerEvents:"none"` component-side-winning, zero slots, no `tag` prop (this seat's node probe — the fifth independent verification in this registry). `copyColor` (:111-113) has no live caller. Second AT layer: PaletteColorStrip is `aria-hidden role="presentation"` — both renderings hidden; the labeled region (:129-132) contains no colour content for AT. P051 ratifies the producer (the cure is a named geometric seat, never a producer-forwarding ask — K-14). X-W4.g's mechanism is literally this cut (impostor seat `:203` → native `<button>` + paint-only leaf). → **X-W4.g (trigger-gated; on census FAIL → GEN-CLUSTER bank)**.

### MAJOR

- **GEN-3 — C-4 ≡ L-8: the dock's Regenerate / Save palette / Copy colors are silent no-ops on every mobile viewport.** Confirmed by construction (my App.vue read): `generatePaneRef`'s sole writer is `onDesktopLeftMount`, bound only on the desktop PaneSlot inside `v-else` of `v-if="!isDesktop"`; the mobile slot passes no `:on-mount`; the action bar keys on `view === "generate"` with no breakpoint arm; the double `?.` chains (usePaneRouter.ts:196-198) swallow every miss. → **X-W4.d** (deletes `generatePaneRef` + the nine dispatches; models `unavailable`); witnessed by **X-W5** gates A3 (dock-action parity) + C2 (`/#/generate` Regenerate at 390 → `specimenChanged: true`), both RED of record.
- **GEN-4 — D-1: the ruled composition was never built — a Card page-column + local grid where canon rules a landmark-neutral golden chassis with Card count 0.** GeneratePane.vue:31 + GenerateControls.vue:219 vs OPTICAL-BENCH-COMPOSITIONS.md Generate row/boundary table (my verbatim re-reads); `InstrumentChassis` demo consumers = 0. Severity + cure ruled at PIN 6: the chassis is dead producer-side (CC-108) and W6's recomposition omits Generate — the divergence stands with no executable ruling. D-19's sharper half (the pane expresses no state at all) folds here; D-5 (the palette rendered twice, the count twice) and D-13 ride as MINOR satellites. Geometry numbers R-1. → **NO-WAVE-OWNER** — flagged for the next formation's recomposition table.
- **GEN-5 — return-2's missed finding, ADMITTED and fourth-verified (PIN 1): all three plate buttons pass the retired `variant` prop and render at one secondary emphasis.** The primary-register comment :153-155 is false at runtime; the "ghost" icon pair renders as filled capsules; 51 sites / 22 files repo-wide incl. `variant="destructive"` on a destructive confirm (FlagReportDialog.vue) rendering neutral. The producer is correct and needs no relay (`axes.d.ts`: tone is "NEVER a `variant` member"). Local three sites ride any wave touching the file (X-W4.b holds it); → repo-wide sweep: **NO-WAVE-OWNER** (glass-7 residue class, the L-8/`tag=` sweep's sibling).
- **GEN-6 — D-11 ≡ C-5 ≡ C-6: the pane has no commit truth.** Both copy verbs discard glass-ui 7's `CopyResult` (:106-113 — the producer documents it as the honest primitive "for consumers that own their own feedback"; App.vue:362 shows the house pattern); the save bypasses `onCurrentPaletteSaved` — the port action whose `expandedId` write is the app's only save confirmation; zero `aria-live`/`status`/`alert` in the tree (grep re-run). Save limb discharges with GEN-1's cure; → copy feedback: **GEN-CLUSTER/NO-WAVE-OWNER** (GenerateControls seam, no wave's mechanism names it); port side: **X-W7**.
- **GEN-7 — D-9 (upgraded) + return-1's missed process shape (o18 half corrected): the seed line, count label and both icon glyphs wear raw `text-muted-foreground` — the ink this repo has twice measured failing and remediated everywhere around this pane.** :212/:288/:173/:183 vs ExtractControls' `.plate-ink` (E1-R1: "2.82–3.08:1") and PaneHeader's rung ("4.29:1") — the direction is structurally certain; the exact 3.19:1 is R-2. The process shape is admitted for the ink (a family cure applied to one of two consumers) and corrected for the rail (census gap, not skipped remediation — ruled disagreements). The killed D-9 premise (K-7) does not weaken the contrast row. → **NO-WAVE-OWNER** (no X wave names generate ink; the census-identity rider joins the o18 row in GEN-9).
- **GEN-8 — D-6 ≡ L-6a ≡ C-10 + L-6b: the count slider's thumb is a 12px transparent ring over arbitrary user colour — the component's one true tap-target failure — and the hand-rolled rail sits outside the contrast census its remediated twin is inside.** Producer geometry (my CSS read): thumb 12×24 at defaults, fill `transparent`, ring `var(--background)`; no fixed ring colour can carry 1.4.11 over user-generated gradient (mechanism certain; per-preset ratios R-3). Rail: GenerateControls.vue:292-307 vs ExtractControls.vue:18-35; o18 keys on extract only (grep = 0 for generate). VISUAL-CONSTITUTION:104 is the standing law ("adopt that one composition … not new slider mechanics"). The `--slider-track-bg: transparent` override is load-bearing, not decorative (return-2's missed INFO row, admitted inline here): without it the producer track paints `--secondary` OVER the hand-rolled ramp, so any rail cure that deletes the per-instance override must land the ramp through the producer's track input in the same move. → thumb geometry: **glass-ui BH relay** (producer-owned; the CC-046/A-16 SliderProps receipt rides X-W4.b); rail + CC-105: **X-W4.g (trigger-gated)**; o18 enrollment: **GEN-CLUSTER, NO-WAVE-OWNER**.
- **GEN-9 — C-7: the component's only oracle compares `data-stops` (raw `generatePalette` output, `oklch(65.524221608648% …)`-form) to `getComputedStyle().backgroundColor` by string equality — unfixable as written; its comment claims "byte-identical rgb() strings" the serializer has never produced; Playwright is absent from CI; unit coverage is zero.** Mechanism confirmed statically end-to-end (spec + PreviewStrip stamp + generatedCss chain + my serializer read); the live "deterministically failing" run is dated evidence (R-5). → CI: **X-W1** (CC-031); oracle repair + any unit coverage: **GEN-CLUSTER, NO-WAVE-OWNER**.
- **GEN-10 — return-1's missed finding, ADMITTED: the seed is a write-only provenance token.** Sole writers `Math.random` (useColorGeneration.ts:24/:31); no seed input, route/query param, or persistence anywhere; the `select-all` invites a copy nothing can consume; a reload destroys the palette the token names. All three axes cited the seed; none checked it reproduces anything — and D-9's killed premise (K-7) fell out of the same blindness. Cure direction (either a seed re-entry path or dropping the provenance costume) is a product decision. → **GEN-CLUSTER, NO-WAVE-OWNER**.
- **GEN-11 — return-1's missed finding, ADMITTED with head corrected (PIN 5): under five of the six harmonies the count slider re-rolls every swatch instead of extending, and the ramp in its own track churns under the thumb.** rng-consumption arithmetic re-derived by this seat; golden (the default) appends cleanly. → **GEN-CLUSTER, NO-WAVE-OWNER** (generate-color.ts + useColorGeneration.ts unowned).

### MINOR

- **GEN-12 — D-4 ≡ L-9 (post-kill): one verb, two names ("Copy colors" / "Copy all colors"); three verbs, two owners; the o20 containment oracle measures a11y visibility, not ownership** (its comment is false of the DOM it passes). AT duplication exists only while the Tools layer is open (PIN 3). → verb contract: **X-W4.d**; oracle: **GEN-CLUSTER, NO-WAVE-OWNER**.
- **GEN-13 — D-10 ≡ C-11 + return-1's missed prefill: the editable title has no rest affordance (hover-only, undiscoverable on touch), a hand-rolled focus ring, no maxlength/empty guard — and ships a pre-filled value where a placeholder belongs, so naming starts with a destructive select-all-delete on the pane's least discoverable control.** :52 + :144-149 (my read). Escalation rider: this row becomes the naming seam when GEN-1's cure lands. Measured rest-state numbers R-4. → **GEN-CLUSTER/NO-WAVE-OWNER** (adjacent X-W4.b).
- **GEN-14 — D-5: two facts rendered four times** (palette as strip + dots; count as Badge + label ~290px apart), both palette renderings AT-hidden — purely visual redundancy, subordinate to GEN-4's composition row. → rides GEN-4, **NO-WAVE-OWNER**.
- **GEN-15 — D-13: the plate expresses one boundary three ways (stroke + material + caster shadow) inside a Card that already drew one, on a route whose boundary set is `[]`.** :132 vs the canon boundary table (my read). → rides GEN-4.
- **GEN-16 — D-8 (head only): the two control labels wear the mono/value voice** (`.section-label` → `text-mono-caption`, producer utility) against card law 13's sans-owns-controls matrix. Systemic, not local; the bold tail is killed (K-6). X-W4.b deletes both labels here (LabeledField); the utility's voice is a design-system row → **X-W4.b** locally + **glass-ui BH relay** for the utility.
- **GEN-17 — D-12: two physical utilities break RTL** — `ml-auto` (:156), `text-right` (:288); logical forms exist. → rides any wave touching the file (X-W4.b); strictly **NO-WAVE-OWNER**.
- **GEN-18 — D-14 ≡ C-12 ≡ L-2: a non-null-asserted injection nothing reads** (GeneratePane.vue:10; grep = 1 = the declaration), twin at GradientPane.vue:8; invisible to gates (`noUnusedLocals` absent, `no-unused-vars` off — my greps). → **GEN-CLUSTER, NO-WAVE-OWNER**.
- **GEN-19 — D-15 ≡ C-13 ≡ L-7: masking `?.` on methods `defineExpose` makes unconditional, over an `any`-typed bridge in the pre-3.5 idiom** — the exact mask that hid GEN-3; return-2's census sharpening adopted (nine `any`s dock-button-to-seed). → **X-W4.d** (the SceneActionSet deletes the bridge).
- **GEN-20 — L-3: a 15-member port injected to call one function** (my member count), strengthened by return-2: the store is a module-level singleton, so the indirection buys nothing. Severity ruled MINOR (disagreements). → **X-W7**.
- **GEN-21 — L-4: `demo/ui/` is a 19-directory pure re-export alias and this file uses both names four lines apart** (:3-12 vs :14-15). Component fact MINOR here; repo identity (90+ edges) is the W-HYGIENE/X-W8 row per the scope law. → **X-W8**.
- **GEN-22 — L-5-residue ≡ C-8-residue: the reka-ui `AcceptableValue` reach + two unchecked casts into literal unions a render-critical computed dereferences** (:33, :75-81 → generate-color.ts:220). Producer guard forecloses the throw (K-11); type hygiene only. → **X-W4.b**.
- **GEN-23 — L-10: `tsconfig.demo.json#paths` drift** (3 dangling targets, 2 shipped keys missing, "CLOSED 8-key set" false by one — consistent with this seat's registry precedent). **IDENTITY GUARD (anti-rename): fact-identical to ColorSpaceSelector L-14 — ONE identity, already FOLDed to W-HYGIENE, never re-booked.** → **W-HYGIENE**.
- **GEN-24 — L-11: the demo's duplicate `debounce` justified by a root-barrel export that does not exist** (no `.` key, no `main`/`module`/`types`). → **W-HYGIENE**.
- **GEN-25 — L-13: the pane shell hand-restated across nine panes; Generate/Gradient near-identical twins down to the dead inject.** → shell recomposition: **X-W5** partially; the shared-shell extraction: **NO-WAVE-OWNER**.
- **GEN-26 — seat1-L-4b (unowned by r2, adopted): GenerateControls imports a sibling feature's card internals** (`PaletteColorStrip` from `palettes/browser/card`, :16) while its true shared sibling lives in color-session. → **X-W7** (palettes tree owner) else NO-WAVE-OWNER.
- **GEN-27 — seat1-L-2 (unowned by r2, adopted): glass-ui 7 ships the scroll-condensing CardHeader (`shrink` + `card-scroll.css`) and the demo re-implements it in PaneHeader** — the producer CSS is parsed and unused; PaneHeader's own comment books the knob. → **X-W10** (PaneHeader owner).
- **GEN-28 — return-2's missed finding, ADMITTED and byte-verified: WatercolorDot's silhouette seed is captured once at setup while its colour is watched** — an in-place patch (`:key="i"` + colour-derived `:seed`) renders f(newColor, staleSeed) silhouettes vs f(newColor, newSeed) wet edges; a fresh mount of the same pair differs from an in-place update, contradicting the d.ts's determinism claim. Cosmetic while GEN-2 keeps the dots inert; moot if X-W4.g deletes the dot. → **glass-ui BH relay** (per the standing relay edict); discharged by **X-W4.g** on census PASS.
- **GEN-29 — return-1's missed finding, ADMITTED (PIN 7): the pane's dominant motion is a scroll-driven scrub both PRM guards fire on and cannot stop** (M-34 TRUE / MQ-1; M-33 FALSE not carried; challenge-D's "PRM is handled" proved-negative is false for this motion). → **X-W10** (:73 names the exact cure).
- **GEN-30 — return-1's missed finding, ADMITTED: every visible label is disconnected from its control** — an orphan `<label>` with no `for`/control (:287-291), `section-label` spans unbound to triggers whose aria-labels differ from the printed text (:221/:223, :255/:257), a naked-number Badge (:150-152). → Selects: **X-W4.b** (LabeledField is literally this cure); slider label + Badge: the VISUAL-CONSTITUTION:104 composition row, **NO-WAVE-OWNER**.
- **GEN-31 — return-1's missed finding, ADMITTED: `grid-cols-2` has no narrow arm** in a component with exactly two responsive declarations (:219; :205); ~149px cells must seat "Split complementary" at 320-390. Truncation magnitude R-11. → **NO-WAVE-OWNER**.
- **GEN-32 — return-1's missed finding (via D-6.4's kill), ADMITTED: the forced-colors roster row for the spectrum slider protects an element the producer paints transparent** — `.slider-range` is `background:0 0` producer-side for every spectrum consumer, so foundation.css:693 preserves nothing, and the colour-bearing `.slider-track` is unrostered. Decidable from bytes; rendering outcome R-6. → **X-W10.f** (foundation.css owner), coordinated with GEN-8's rail cure.

### INFO

- **GEN-33 — D-19 (scope-corrected): the route's async chunk has no loading/error/timeout arm** — all ten registry entries identical; shell-owned, surfaces here; the pane-side state absence folds into GEN-4. → **X-W5/X-W3** shell territory.
- **GEN-34 — D-20: the pane's height is decided by its `palettes` companion, not its content** (`h-full` chain into the neighbour-sized grid row; dead-tail magnitudes R-4). X-W5's hard gate ("the shell caps neither block axis") names the class. → **X-W5**.
- **GEN-35 — D-21: the documented 390px graceful wrap is the only state the plate chrome renders on desktop** (structural row-minimum ≈437px vs ≈440px box; the comment :139-142 describes a layout the code does not produce). Rect table R-4. → **NO-WAVE-OWNER**.
- **GEN-36 — D-22: the route's schema row resurrects the retired mobile `PaneSegmentedControl`** (viewSchema.ts:152-159 `right: "palettes"`; Dock.vue renders it whenever `right !== null`; VISUAL-CONSTITUTION:89). → **X-W5** (:43 deletes the control and the axis — the exact cure).
- **GEN-37 — D-23: the route was never captured in six of the ten a11y/state matrices** — four safari shots only; forced-colors/keyboard/reduced-motion/rtl/zoom frames absent, so three D findings have no tracked frame. Feeds the visual-audit residue (R-1..R-6). → **GEN-CLUSTER, NO-WAVE-OWNER** (evidence row).
- **GEN-38 — L-12: a second physical `@mkbabb/value.js@4.0.0` under node_modules; vite.config's "a package does not install itself" premise false; nothing broken.** Alias-rebind rider stays a hypothesis (R-10). → **W-HYGIENE**.
- **GEN-39 — C-9 (post-kill): five colours ≈294 chars of 12-decimal oklch reach the clipboard and localStorage** — a library-wide serialization posture (src/css), not a component defect; a demo-side rounding would mint a second divergent precision policy (return-2's cure caution adopted). Also what makes GEN-9's oracle unfixable as written. → **NO-WAVE-OWNER** (serializer policy; V·π/X-W9 adjacency noted, not booked).
- **GEN-40 — D-18 + return-2's missed clampHue/domain rows, merged: dead declarations and false comments** — inert `relative`/`mx-auto` on the pane root (copied from a sibling, not even self-consistent across copies); `clampHueToRanges`' wrapping branch unreachable with a comment describing an ascending range (my preset-table read: no descending pair exists); `generatePalette` documents 1–50 vs the UI's 1–12 and is pure-or-impure on an optional seed. → **GEN-CLUSTER, NO-WAVE-OWNER**.
- **GEN-41 — return-1's missed finding, ADMITTED: the route description praises output instead of naming mechanism** ("pleasing … aesthetic") — the first line AT hears, since everything below it is hidden (GEN-2); three nameable mechanisms omitted. → **GEN-CLUSTER, NO-WAVE-OWNER**.

## Killed claims — 15, with refutations

- **K-1 — D-17 (the 44px floor).** No such floor in the cited canon: card law 7 (PROPORTION-AUDIT:72, re-read) separates glyph/target/reservation and says "Accessibility floors do not require bloated visible chrome"; PR-12 names no number. Applicable floor = WCAG 2.5.8 AA 24×24; nine of D-17's ten rows pass it; the one failure (the 12px thumb) is GEN-8's. The remedy would build exactly what law 7 forbids.
- **K-2 — D-3.3 / C-2's "operable ornament / lie in the UI" limb.** Producer bytes: no listener is ever applied (`inheritAttrs:!1`, attrs read = class/style only) and the consumer style spreads FIRST so `pointerEvents:"none"` always wins — `cursor:pointer` never paints (the used cursor resolves from the parent hit target), `active:scale-95` never fires. The user sees ordinary swatches, not clickable ones; the defect (GEN-2) is worse — there is no verb at all — but the "UI advertises one" charge is false.
- **K-3 — C-14 (no pointer-capture recovery in the producer).** The grep hit a two-line barrel. The impl chunk carries 3 `pointercancel` occurrences (my count): window-level `pointerup`/`pointercancel`/`blur` release, registered on pointerdown/touchstart, torn down on unmount. Residual iOS thumb-capture question stays R-12.
- **K-4 — D-6.4 (consumer blanked the rostered element / the ramp is stripped).** Two errors: `--slider-track-bg` styles `.slider-track`, while the rostered `.slider-range` is producer-painted `background:0 0` for every spectrum consumer (no consumer act involved); and forced-colors forces `background-color`, not `background-image` — the div paints a gradient image, so "stripped" is unproven (R-6). The true, sharper finding underneath is GEN-32.
- **K-5 — D-16's "exists for mouse only" limb.** Same mechanism as K-2: the press feedback exists for no input modality. The surviving halves (untokenized motion; the verb animating in the Dock seat and not the plate seat) ride GEN-12.
- **K-6 — D-8's bold tail.** Card law 13 assigns mono roles families/sizes, no weights (my :78 re-read); the sole non-bold clause (:15) is sans- and options-scoped. The honest citation is consistency: the remediated extract twin's identical label carries no `font-bold` (my :15 read of ExtractControls).
- **K-7 — D-9's premise ("the seed is the pane's only reproducibility affordance").** The seed reproduces nothing (GEN-10) — no reader, no input, no persistence; the premise inverted into a bigger finding. The contrast measurement itself survives (GEN-7).
- **K-8 — L-1's cure (move the store write into the renamed composable).** Contingent on a `demo/domain/palette/store.ts` that does not exist (my `ls demo/` — no `domain/`), and it would put a store write inside the one module whose purity is load-bearing (the seed-exact preview law and the corpus's own negative proofs rest on it). C-1's port-action cure wins, as amended by PIN 4.
- **K-9 — D-1's r1 cure spelling — twice dead.** No `variant` prop exists on the chassis (`proportion` — my d.ts read); and tranche X has since killed the producer chassis entirely (CC-108/A-17, W6:17). No downstream executor may work from §2's spelling, or from the chassis cure at all.
- **K-10 — L-5 as filed.** The producer exports `SelectProps`/`SelectEmits`/`SelectionValue` at component barrel + root (my reads); the prescribed BH relay is aimed at the wrong repository; and the file already imports from the producer root at :14, so the barrel's type-drop (real — my read of demo/ui/select) is not even a forcing cause. Residue = GEN-22.
- **K-11 — C-8's render-throw mechanism.** The producer refuses non-scalars at the seam (`TypeError` guard, my chunk read) and `SelectItem` iterates `PRESET_NAMES` only — the non-PresetName-string path has no writer. Residue folds into GEN-22 as type hygiene.
- **K-12 — D-4's "screen-reader hears it twice" limb.** DockLayer stamps `inert` + `aria-hidden` on non-active layers (my dock.js read) and the action-bar layer is gated by `actionBarLayerActive` — no duplicate accessible name in the default state; the census counted DOM attributes, not accessible names. Survivors ride GEN-12.
- **K-13 — C-9's "unrounded" literal.** `Number(value.toFixed(12)).toString()` at grammar.ts:283-285 (my read) — rounding exists; the payload-length head survives as GEN-39 at MINOR, not MAJOR.
- **K-14 — C-2's producer cure limb ("ask glass-ui for a polymorphic root").** P051 (re-read verbatim) rules the button/tag branch REMOVED in the clean major and operable sites onto named geometric seats — glass-ui 7 executed canon; X-W4.g's mechanism ratifies the local-seat cure and CC-044 forbids the forwarding ask. D-3's local cure is the only one carried.
- **K-15 — the r2 L-file's §Scope census.** Under an explicit "every line number re-verified" assurance: GenerateControls.vue = 311 (not 392), useColorGeneration.ts = 42 (not 48), GeneratePane.vue = 40 — my `wc -l`, and the tree unchanged since `f2c8f565`. The body citations do land; the scope header does not.

## Superlatives — carried with provenance, re-verified where load-bearing

1. **The seed-exact preview law is structurally true and must survive any rebuild.** `generatePalette` is pure and mulberry32-seeded; each option row previews by calling the same function with only the candidate varied (:94-100); the stamp rides one shared serializer. A row's strip and the palette selecting it yields are the same bytes by construction — verified independently by both readers and consistent with this seat's generate-color read. What is broken is the oracle checking the law (GEN-9), never the law.
2. **The value.js boundary is right by construction**: `@mkbabb/value.js/color` + `/css` — real exports-map keys an npm consumer could write verbatim; no deep path in the transitive graph. Both L seats reached it independently.
3. **`useColorGeneration.ts` (42 lines) is the correct shape**: four refs, one pure computed, one reseed write; no watcher/timer/rAF/listener — why the leak class misses this component entirely, and why K-8's cure is declined.
4. **The generated swatches are enrolled in both colour-preservation rosters including print** (foundation.css :689 forced-colors + :834 print, re-read) — and doubly covered via the producer's `.watercolor-swatch` roster row.
5. **`clampHueToRanges` is real colour work** — proportional mapping into multi-range spans, not endpoint snapping; C's 480-distinct-hue empirical check stands. (Its dead wrapping branch is GEN-40's, a comment defect, not a math one.)
6. **PreviewStrip's honest truncation** — seven segments + mask fade documented as "reads as 'continues'", with the rejected alternative written down.
7. **glass-ui's Select refuses rather than degrades** (`TypeError` at the seam over a published `SelectionValue` contract) — the correct design-system error posture, and the fact that killed C-8.
8. **The SelectItem `#description` slot is consumed correctly** (:243-248/:272-277) — in a file that simultaneously passes four retired props, the one place the current producer contract is read right.
9. **`usePaletteStore`'s lazy singleton** — one `useStorage` binding for the app's lifetime, defensive serializer, the cured regression named in the comment.
10. **The comments carry the ruling — and are load-bearing enough to be wrong**: :44-47 books the never-written name wire (GEN-1), :139-142 describes a wrap the code doesn't produce (GEN-35), :153-155 claims a register the retired prop never engages (GEN-5). Provenance discipline far above norm; three of its cells are now defects of record.
11. **The corpus proves its negatives** (no ungated rAF, no defineModel hazard, verbatimModuleSyntax clean, keyboard operability measured) — two of those negatives were load-bearing for kills in this record; one (the pane-level PRM negative) was itself false (GEN-29), which is the strongest argument for the practice, not against it.

## UNPROVEN-NEEDS-LIVE — the visual-audit inputs

- **R-1** — D-1 geometry (mainW/cardShare/two passes reporting the same quantity differently); the structural halves are confirmed statically.
- **R-2** — D-9's exact 3.19:1 on `bg-well` (runtime `color-mix` products); direction structurally certain from the repo's two prior rulings on the same ink.
- **R-3** — D-6's per-preset thumb ratios (pastel 1.19 worst); mechanism certain (transparent fill, background-coloured ring, arbitrary track).
- **R-4** — the D-7/D-10/D-20/D-21 rect tables: chromeRowH, inputScrollW, emptyTailPx 65/125.9, the wrap firing at 1440 (measured twice, mechanism visible in markup).
- **R-5** — C-7's live "deterministically failing ×2" run — dated; mechanism confirmed statically; re-run rides the oracle repair (GEN-9).
- **R-6** — forced-colors rendering of the hand-rolled gradient div and the roster correction's visible effect; no `forced-colors-desktop/generate.png` exists (GEN-37) to settle it.
- **R-7** — C-15's boundary sweep (count ≤ 0, seed aliasing) — measured, correctly self-labelled UI-unreachable; stays INFO.
- **R-8** — D-24's route drift off `/#/generate` — hypothesis, shell territory; carry the operational warning (assert the rendered `h3` = "Generate" before trusting a rect).
- **R-9** — GEN-31's truncation magnitude at 320–390 (the structural facts are static).
- **R-10** — L-12's rider that the Vite self-alias rebinds glass-ui's runtime value.js imports — needs a production build-graph probe.
- **R-11** — GEN-31 duplicate of R-9 consolidated: use R-9.
- **R-12** — whether reka-ui's thumb-level pointer-capture leak (the `useSliderTouchGates` hazard) is covered by the producer's window-level recovery — iOS Safari touch cell.

## Closing verdict

**CONFIRMED-WITH-RULINGS**: 41 adjudicated rows (2 BLOCKER · 9 MAJOR · 22 MINOR · 8 INFO), 15 kills, 16 reader misses admitted — and the component's defining fact is jurisdictional: **the BLOCKER's cure file and the whole GEN-CLUSTER (GeneratePane.vue · useColorGeneration.ts · generate-color.ts · the o20 oracle · the o18 enrollment) have NO owning wave in tranche X**, while every adjacent surface is owned (X-W4.b/.d/.g, X-W5, X-W7, X-W8, X-W10, X-W1). The next formation boundary must seat the cluster or the name-drop, the dead swatch verbs' bank-FAIL branch, the seed's false provenance, and the unfixable oracle all survive another tranche by default.

— end of apotheosis. The r2 challenge corpus, both seat-1 axis files, and both re-reader returns remain immutable beside this file per E-1/E-3.
