# Markdown — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-07-27 · **HEAD**: `c654824e` · branch `tranche-u` · **Corpus**: 11 accusations (L-1..L-11) against `demo/scenes/about/markdown/` (Markdown.vue, useMarkdownColors.ts, useMarkdownHighlighting.ts, index.ts), AboutPane.vue's seam, the `assets/docs/` corpus, and their supply chain — **plus one NEW row (MT-MD-12) raised by worker-O and confirmed by this seat**.
**Workers adjudicated**: worker-F (Fable 5, slug `Markdown`) · worker-O (Opus 5 [1m], slug `markdown-guide-adjudication`).
**Method (L-14)**: both workers were attacked before either was adopted. All eight material disagreements (L-1 priority, L-2 cure scope, L-4 ownership, L-5 count+scope, L-7 count, L-10 cure shape, L-11 sub-claims, MT-MD-12's existence) were re-derived against the tree with this seat's own instruments. Three suspiciously-agreed verdicts (L-3, L-8, L-9) were independently re-measured, not spot-trusted. One registry-level catch belongs to neither worker: four of the fold targets both workers named fresh (`MT-W-PATHS`, `MT-W-UISHIM`, `MT-W-SHELL-ADDR`, the lattice wave) **already exist as adjudicated identities** in this registry — they are cross-booked below under their standing names, never re-minted (anti-rename).
**Arbiter's own instruments this session (static + tsc/vue-tsc; zero browser probes — every verdict was decidable from source, dist, and compiler runs; probe parsimony honored)**: whole-file reads of Markdown.vue (409 lines), useMarkdownColors.ts (83), AboutPane.vue (100), markdown/index.ts (9), useMarkdownHighlighting.ts:60-85, useContrastSafeColor.ts:60-80/:295-305/:340-382, vite.config.ts:10-45/:68-78/:155-168, plugins/vite-source-export.ts:80-92, eslint.config.js rule-object census, tsconfig.demo.json:25-55, demo/shell/viewSchema.ts + useViewManager.ts anchors, Dock.vue:195-205, router/index.ts:22-37, demo/ui/alert/index.ts, installed glass-ui 7.0.0 (Skeleton.vue.d.ts prop set; `glass-ui.css` skeleton selector census). Fresh probes run by this seat: **(a)** a strictTemplates program (`extends` tsconfig.demo.json + `vueCompilerOptions.strictTemplates: true`) through vue-tsc — **271 errors: 263 TS2353 + 8 TS2322**, of which exactly 3 in `demo/scenes/about/markdown/` at Markdown.vue (4,19)/(6,23)/(7,23), each naming only `surface`; **(b)** a `--traceResolution` tsc probe — `@mkbabb/value.js/css` → `node_modules/@mkbabb/value.js/dist/subpaths/css.d.ts` **with Package ID `…css.d.ts@4.0.0`** while `/color` → local `dist/subpaths/color.d.ts`; **(c)** an exported-symbol extraction + set-diff of local vs installed css.d.ts — **52 = 52, both differences empty** (382 vs 350 lines is entirely private `_2` forks: 25 hits local, 0 installed); **(d)** count re-derivations: `demo/ui` = 19 dirs × 1 file; `../ui/` import sites = **90 statements / 48 files by BOTH workers' methods** (F's own single-line regex re-run by this seat yields 90, not F's 71); corpus census h2=53 h3=85, tables/fences/quotes/images 0×11, exactly **3 anchors** (lch.md, oklch.md, xyz.md); `?source` consumers 0, `@src` in assets/+demo/ 0, test/ `@src` importers = **4 files**; `bumpProbeEpochOnMount` call sites = ConsoleRail.vue:133, useViewAccents.ts:89, useContrastSafeColor.ts:300/:348 — **not useMarkdownColors, not HeroBlob.vue** (import :42, fold :99, no bump); `--phi-*` rungs at foundation.css:458+; `find demo -name style.css` → nothing; `wc -l` demo .vue ranking App.vue 417 / ColorPicker.vue 414 / **Markdown.vue 408 (third)**; global-CSS runtime imports = exactly 4 lines (App.vue:199-200 root + Markdown.vue:37-38); `/css` demo blast radius 10 sites / 9 files, `/value` 0; eslint `no-restricted-imports` objects at :204 (src, LIVE — verified on src/value.ts), :241 and :279 (the TWO demo objects — both dead on the subject: `--print-config` on useMarkdownColors.ts → `None`); `@components` referents = 2, both prose; `demo/@` → No such file or directory; `pretypecheck: npm run build` at package.json:64; DocItem = 1 hit (its own declaration); demo/color-session/index.ts absent.
**Environment disclosure (L-12)**: the dev server :9000 is UP and API-LESS; no verdict in this corpus needed data or a browser — the About corpus is static content and every ruling closed from source + compiler receipts. The ColorSpaceSelector seat's committed display-p3 probe (blank under "Detailed Guide") is cited as corroboration only, never load-bearing — the cast→undefined→v-if chain is proven in source. playwright-webkit and safari-app remain separate evidence cells (INBOX I-20); no pixel claim in this record exists yet — that absence IS finding L-11.

## Scoreboard

**7 CONFIRMED (L-3, L-4, L-6, L-7, L-8, L-9, MT-MD-12) · 5 RESCOPED (L-1, L-2, L-5, L-10, L-11) · 0 REFUTED whole · 0 UNVERIFIABLE-HERE · 1 GLASS-OWNED arm (inside L-8, relayed — cites INBOX I-20).**

No row died: every accusation's factual core survived both workers and this seat's attack. What died were headlines and cures: L-1's epistemic-contamination claim (refuted by this seat's own 52=52 symbol diff), F's demo-wide strictTemplates gate (un-landable at a measured 271), F's 71-site count (refuted by F's own regex), the "largest component" superlative (third, 408), the implied mobile unreachability (a live Dock toggle), and both workers' habit of minting fold-target waves the registry already owns.

The single most consequential worker delta: **worker-O found MT-MD-12 and worker-F missed it while reading the same file** — F even noted useSafeAccentFn's bump "side effect … not a hazard" without noticing its absence in the current code is a stated-contract violation.

## Ruled disagreements (the tri-fold's actual work)

| id | worker-F | worker-O | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-1 priority** | CONFIRMED BLOCKER; "land FIRST" upheld (G1→G3 ordering), "only defect" demoted as hyperbole | RESCOPED; mechanism confirmed, priority claim REFUTED (52=52 symbols, typecheck exits 0); earns priority on cost+sequencing only | **O wins** | This seat's own extraction: local and installed css.d.ts export **identical 52-symbol sets, empty diff both directions**; the 382/350 split is 25 private `_2` forks (0 installed), structurally assignable. Zero existing evidence is invalidated today — the row is prophylactic against V·π reshaping `src/css/`. Mechanism separately re-proven by this seat's traceResolution (`/css` → tarball `@4.0.0`, `/color` → local) and the paths/exports set-diff (3 phantom keys with MISSING targets, `/css` + `/value` absent, the "CLOSED 8-key set" comment false twice: 7 keys, no root). F's residual ordering point survives narrowly: any typecheck gate inherits resolution fidelity — truthful *today* only because the symbol sets happen to coincide. |
| **L-2 cure** | Ship `strictTemplates: true` in tsconfig.demo.json; gate = zero TS2353 demo-wide | Scoped strict program; the "one vue-tsc run" cure REFUTED at 271 errors | **O wins, decisively** | This seat ran the probe: **exactly 271 errors (263 TS2353 + 8 TS2322)** — O's number to the digit. Many are `aria-label`/`data-*` on glass-ui components: an attrs-fallthrough design question at the producer boundary, not a mechanical sweep. F's G3 ("assert exit 0 and zero TS2353" with strictTemplates SHIPPED) can never go green inside one wave — the arc-wearing-a-wave's-name shape. F's instrument evidently saw only what it grepped for (the component's 3) — the L-9 lesson in its purest form. Adopted: `tsconfig.demo.strict.json` scoped to the scene (G-MD-1), demo-wide widening BANKED with O's runnable re-trigger. |
| **L-4 ownership** | BUILD whole in-wave (plugin + @src alias + prose) | SPLIT: plugin+alias FOLD to a build-graph wave; only the prose builds here | **F wins, with O's corrections adopted** | No build-graph wave exists anywhere in this registry (grep across all adjudicated files: zero hits for the plugin) — O's fold names an owner that does not exist, which is the deferral shape wearing a fold's name. And ownership is genuinely this scene's: the plugin's sole claimed purpose (vite.config.ts:70-74's own comment) is `assets/docs/*.md` `?source` embeds — this wave's very corpus; consumers measured 0 by this seat; the cited golden `test/docs-source-snippets.test.ts` does not exist (cited at plugins/vite-source-export.ts:86-87). The About wave retiring its own dead supply chain is cohesion, not trespass. O's corrections stand: test/ `@src` importers = **4** (this seat re-measured: gradient-v4-consume, parsing-easing, path-geometry, decompose-targeted — vitest's alias stays), and the boundary hole is a build-path hole (vite performs no typechecking; the hard `npm run typecheck` gate WOULD catch a demo `@src` deep-import — F's own nuance, kept). Gate = deletion greps + `npm run gh-pages` product assertion. |
| **L-5 count + scope** | 71 sites; delete demo/ui + rewrite all sites inside this wave | 90 sites / 48 files; FOLD whole — partial cure manufactures a dual path | **O wins on both** | This seat ran **F's own regex**: 90, not 71 (and O's multiline matcher: 90/48 — three independent measurements now agree: this seat, worker-O, and the ColorSpaceSelector seat). Rewriting 88 foreign files inside the Markdown wave is an arc; curing only Markdown's 2 leaves 2-direct/88-barrel — strictly worse. FOLD to the **standing W-HYGIENE 90-site barrel extirpation** (ColorPicker adjudication L-6; cross-booked at CSS-adj L-4) — NOT a fresh `MT-W-UISHIM`. Markdown.vue:35-36 are 2 of the 90 and ride that wave. |
| **L-7 count** | 142 dead lines (149 − the 7-line adjacency rule kept whole) | 146 (149 − 3 surviving adjacency lines); challenger's `a:0` census refuted | **O wins** | Within the :149-155 adjacency rule only `> h2 + h3` is live (h2=53, h3=85, no other levels exist in the corpus); after excision 3 lines survive (selector + declaration + brace), not 7 — 146 of 331 (44%). Both workers independently caught the live `a` rule (3 anchors: lch/oklch/xyz.md:13 — re-verified by this seat's link census); the integer stays method-soft (comment-line accounting), the census gate is the durable measure. Row CONFIRMED regardless. |
| **L-10 cure** | Extract-derived union + route absence to the existing "Oh snap" Alert | `Partial<Record<DisplayColorSpace, DocModule>>` + delete the cast; Record<> cure refuted as non-completable (7 unwritten docs) | **MERGED — O's typing + F's explicit-absence, both landing inside V·MT-W-CSS1** | O's typing is simpler and drift-proof (no fifth copy of the member list; `Extract` silently drops a typo'd member — F's shape is weaker than F believed). F's unique verified insight stands: the "Oh snap" Alert (Markdown.vue:19-31) is authored but **unreachable** — AboutPane's `v-if` (:51) unmounts Markdown entirely when the module is undefined, so absence is silent today (corroborated by the CSS-seat's display-p3 probe). REGISTRY CATCH: **V·MT-W-CSS1 already owns this cure whole** — its adjudicated scope moves markdownModules into the total catalog (`doc: DocModule \| null`) and makes the :51 v-if "an explicit authored empty state." Neither worker saw the sibling booking. What builds HERE is the module seam only: DocItem (zero consumers) dies, `DocModule`'s `{ default: any }` gets typed. |
| **L-11 sub-claims** | CONFIRMED whole; register `/about` + `/about/:space` in-wave | RESCOPED: "largest component" false (third); mobile unreachability false (Dock toggle); routing = shell-topology decision, FOLD | **O wins; fold target corrected to the standing W18 identity** | This seat's `wc -l`: 417/414/**408 — third**. Dock.vue:195-205: `PaneSegmentedControl` bound to `viewManager.mobilePaneIndex` — a live user toggle; the harm is auditability, not availability. Router: 14 real paths + catch-all, every one `component: Stub`, panes from the schema — a new path IS a `ViewId`/VIEW_MAP topology decision, not a route line; F's Stage-5 underestimated it. REGISTRY CATCH: `/about` routing **already has an owner** — ARCHITECTURE §3.1 W18 ("About is a quiet trailing destination"), carried at layout-gestalt row 11 (66ch measure, UL-3 exemption) and App.md D-2. Cross-booked there — O's fresh `MT-W-SHELL-ADDR` name is retired. The audit-blindness arm BUILDS here as the π obligation (G-MD-8). |
| **MT-MD-12** | ABSENT — F read useSafeAccentFn closely enough to call its bump "the intended live-probe idiom, not a hazard," and missed that the current code never bumps | CONFIRMED new row: useMarkdownColors folds `resolveSurfaceLightnessLive` (:44-48) and never calls `bumpProbeEpochOnMount`, violating the helper's own stated contract (:69-78) | **O wins — row admitted, CONFIRMED by this seat's own reads** | The contract text is verbatim in source ("…must register the mount bump from its setup, or its first (possibly detached/pre-style) probe result caches until some OTHER consumer happens to bump the epoch" — written from a measured 3.59:1 failure); the four bump sites re-censused by this seat exclude useMarkdownColors; `useSafeAccentFn` discharges it at :348, which DECIDES the L-6 cure. O's own half-refutation adopted with the row: first evaluation is deferred past mount by the async doc load (skeleton branch while `isLoading`), so it files as a CONTRACT row with a predicted null DELTA — not inflated to availability. Sibling offender HeroBlob.vue (:42 import, :99 fold, no bump — re-verified by this seat) is booked as a rider so it is not lost. |

**Spot-checks of agreed verdicts (anti-rubber-stamp)**: **L-3** — this seat's grep: 11/11 docs carry the identical line-2 `import { Katex } from "../../demo/scenes/about/katex"`, `assets/` contains only `docs/`, AboutPane climbs back out ×11; sole consumer confirmed. **L-8** — installed Skeleton.vue.d.ts declares `class` only (0 `surface|variant` hits); `glass-ui.css` skeleton selector census = {skeleton, skeleton-scan, skeleton-title, skeleton[data-v]} — no `[surface=]`/`[variant=]`/`.shimmer`; this seat's compiler probe fired TS2353 ×3 at the exact positions both workers cited. **L-9** — exactly 4 runtime global-CSS import lines demo-wide: App.vue:199-200 (root, correct) + Markdown.vue:37-38 (the duplicate), with the correct `@reference` also present at :79.

## Verdict register — all 12 rows

Every CONFIRMED row carries a reproduction a fresh session can run. Line anchors are HEAD `c654824e`; re-resolve before executing any cure.

- **L-1 — RESCOPED** (mechanism CONFIRMED; priority headline REFUTED). Repro (fresh session): `node -e` set-diff of tsconfig.demo.json:42-49's 8 hand-written paths keys against package.json#exports' 7 → 3 phantom entries (`@mkbabb/value.js`→dist/index.d.ts, `/parsing`, `/units` — all three targets MISSING on disk, this seat's `test -e`) + 2 absent real keys (`/css`, `/value`); then a traceResolution probe importing `/css` and `/color` → `/css` resolves to the frozen 4.0.0 tarball with a Package ID, `/color` to local dist. `pretypecheck: npm run build` (package.json:64) proves the defeated intent — `/css` (10 sites / 9 files) escapes every rebuild. NOT epistemically contaminating today: symbol sets 52=52 identical (this seat's diff). Priority = cost + sequencing before V·π touches `src/css/`. **FOLD → the standing W-HYGIENE config-parity row** (one identity with the ColorSpaceSelector adjudication's L-14, which already carries the born-RED generator-parity command) — cross-booked, not re-minted.
- **L-2 — RESCOPED** (both dead layers CONFIRMED; F's cure REFUTED at 271). Repro: `ls -d demo/@` → ENOENT; `npx eslint --print-config demo/scenes/about/markdown/composables/useMarkdownColors.ts` → `no-restricted-imports: None` while `src/value.ts` → LIVE (both run by this seat); the TWO demo rule-objects (eslint.config.js:232→rule :241, :274→rule :279) glob the deleted `demo/@` tree and the dead `@components` alias (2 referents repo-wide, both prose: DESIGN.md:384 + a comment); `grep strictTemplates tsconfig*.json` → nothing. **The eslint re-key FOLDs → the standing L-5(a) lint-glob-liveness row** (ColorSpaceSelector adjudication; repo-wide, born-RED `--print-config` command). **The strictTemplates arm BUILDS here, SCOPED** (G-MD-1, `tsconfig.demo.strict.json` filtered to the scene — 3 errors today); the demo-wide 271 (263 TS2353 + 8 TS2322: palettes 134, workbenches 65, shell 44, scenes 14, shared 4, color-session 4, color-picker 4, picker 2) is **BANKED** with O's runnable widening trigger, because many rows are producer-boundary attrs-fallthrough decisions, not typos.
- **L-3 — CONFIRMED (MAJOR).** Repro: `grep -c 'demo/scenes/about/katex' assets/docs/*.md` → 1×11 (identical line 2); `grep -rn 'assets/docs' demo/ src/ test/ e2e/` → only AboutPane.vue:82-92's eleven `../../../assets/docs/*.md` climbs; package.json#files excludes assets/ from publish; `ls assets/` → `docs` only. Repo-root content names a scene's private subdirectory and the scene climbs back out — total, symmetric inversion with zero second consumers. Cure BUILDS here: move ×11 → `demo/scenes/about/content/`, line-2 import → `"../katex"` (G-MD-5).
- **L-4 — CONFIRMED (MAJOR; built whole here — see ruling).** Repro: `grep -rl '?source' demo/ src/ assets/` → 0; `grep -rn '@src' assets/ demo/` → 0; `ls test/docs-source-snippets.test.ts` → ENOENT while plugins/vite-source-export.ts:86-87 cites it as the golden; `sourceExportPlugin()` FIRST in defaultPlugins (vite.config.ts:160), imported :13; the `@src` alias survives (:75) on a justification comment (:70-74) naming `assets/docs` `?source` embeds — zero instances. Second harm (this component's own): useMarkdownHighlighting.ts:71-80 documents code-block behaviour entirely through the dead plugin + the W43-killed `@styles/hljs.css` alias, over a corpus with **0 fenced blocks ×11**. Corrections carried: test/ `@src` importers = 4 (vitest.config.ts's alias STAYS); the hole is a build-path hole (the hard typecheck gate would catch a demo `@src` type-import; the gh-pages BUILD would not). Cure: delete the plugin + its defaultPlugins slot + the vite `@src` alias + fix the prose; gate G-MD-9 asserts the deletions AND `npm run gh-pages && test -s dist/index.html` (product assertion, not exit code).
- **L-5 — RESCOPED** (facts CONFIRMED; count RULED **90 sites / 48 files**; cure FOLDed whole). Repro: `ls demo/ui` → 19 one-file pure re-export dirs; demo/ui/alert/index.ts names the shim contract verbatim ("The two consumers (ColorNutritionLabel.vue, Markdown.vue) import from this barrel unchanged" — the no-backwards-compat edict violated in writing); count command in G-header instruments (both methods → 90/48). **FOLD → the standing W-HYGIENE barrel extirpation** (ColorPicker adjudication L-6; also carried at CSS-adj L-4). Markdown.vue:35-36 (2 of 90) ride that wave; curing them alone would manufacture a dual path. F's 71 is REFUTED (his own regex yields 90).
- **L-6 — CONFIRMED (MAJOR).** Repro: useMarkdownColors.ts:18/:25/:44-48/:70-73 hand-assemble exactly `useSafeAccentFn("resting")`'s three steps (useContrastSafeColor.ts:346-361) via three 4-hop deep reaches (:5-7) into a color-session with **no index.ts seam** (this seat's `ls`); the widening's docstring (:363-368) names `useMarkdownColors` as its beneficiary — the defect's signed confession; three `throw new Error` (:33/:37/:41) inside a computed bound to `:style` (Markdown.vue:15) re-open the exception channel the library's Result contract exists to close. O's probe adopted as corroboration: `parseCssColor('notacolor')` → `{ok:false}` fires :33; the throw arm is DEFENSIVE today (cssColor arrives serialized from the pipeline) — filed as contract, not availability, and `parseCssColor("oklch()")`'s TypeError remains the V·π parser row (EXECUTION BLOCKED on OC-1/OC-2 + begin-word; named owner, standing gate — not re-booked). Cure BUILDS here (G-MD-3/G-MD-4): rewrite onto `useSafeAccentFn("resting").safeCss` with a local pure `withChromaFloor` preserving the full :31-53 arithmetic (parse → oklch → `max(C, 0.08)` → `H === "none"` zeroing → re-serialize — NOT a trivial string transform, O's correction), `{}` as the total degenerate, and delete the beneficiary docstring in the same commit so the widening loses its written justification.
- **L-7 — CONFIRMED (MAJOR; count RULED 146).** Repro: corpus census (this seat's): h2=53, h3=85, h1/h4/h5/h6=0, tables/fences/quotes/images 0×11, anchors=3 (lch/oklch/xyz.md:13); `Markdown({})` at vite.config.ts:162 = plain markdown-it, no plugins — footnotes/deflists/task-lists/toc/callouts CANNOT be generated. Rendered-tag union (O's instrument, adopted as the durable one): `a code em h2 h3 hr li ol p strong ul` (+ declared runtime injections `mark.cs-name`, Katex divs). Dead against the 331-line style block: 14 element selectors (blockquote dd dl dt h1 h4 h5 h6 img input pre table td th) + 7 class families (.contains-task-list .task-list-item .callout .footnotes .footnote-ref .footnote-item .toc) = 21 dead groups = **146 lines** (149 raw − 3 surviving `> h2 + h3` adjacency lines — this seat re-derived the arithmetic; the integer stays method-soft, the census gate is the measure). Also: `:deep(.markdown-body)` declared twice (:104/:198, no cascade reason); the :94-96 comment's "style.css :root" is FALSE (no demo style.css exists; rungs live at foundation.css:458+). The challenger's per-document `a:0` DOM census is REFUTED as an instrument (MT-F006's shape) — but `a` was never in the accused dead-set, so no row changes. Pollution point verified: AB-3 and F6-law comments in the file adjudicate markup that has never rendered. Cure BUILDS here (G-MD-2, bidirectional census golden).
- **L-8 — CONFIRMED (MAJOR) + one GLASS-OWNED arm.** Repro (this seat's compiler receipt): strictTemplates probe → TS2353 at Markdown.vue (4,19)/(6,23)/(7,23) — "'surface' does not exist in type '{ readonly class?: ClassValue; } …'"; installed glass-ui 7.0.0 Skeleton declares `class` only; `glass-ui.css` has no `[surface=]`/`[variant=]`/`.shimmer` selectors (this seat's census). CRITICAL for the wave: **TS2353 halts at the first excess property — `variant` is masked behind `surface`; delete BOTH** (O's reading note, verified in this seat's probe output: every row names only `surface`). The props read as intent, ship as inert HTML attributes, and only look correct because shimmer is glass's unconditional default. GLASS-OWNED arm: the `var(--muted)` skeleton fill — the exact tone Markdown.vue:229-232's own AB-3 comment forbids for content chips — is the producer's recipe: **relayed to the glass BH inbox per the standing owner edict, cites INBOX I-20; no local patch, no demo override, no masking fallback** (the row's own "if the skeleton genuinely must seat on --well-bg…" phrasing invites exactly the patch the boundary forbids — refused here in writing). BANK: O's B2 re-trigger (grep the compiled css for a skeleton surface API; fires above 0).
- **L-9 — CONFIRMED (MINOR).** Repro: `grep -rn 'import "[^"]*\(foundation\|utils\)\.css"' demo/` → exactly 4 lines: App.vue:199-200 (root, correct and sufficient) + Markdown.vue:37-38; Markdown.vue ALSO carries the correct `@reference` at :79 — one file, two mechanisms for one job; 17 `@reference` sites exist demo-wide and this is the only file that additionally runtime-imports. Harm honestly scoped (O): Vite dedupes, so today's damage is zero — what survives is the dual mechanism plus a leaf async chunk (defineAsyncComponent, usePaneRouter.ts:69) naming the global CSS layer: a cascade-ordering hazard the moment chunking changes. Cure: delete 2 lines (G-MD-6).
- **L-10 — RESCOPED** (every factual arm CONFIRMED; cure merged and largely cross-booked). Repro: index.ts:3 `DocModule = () => Promise<{ default: any }>` (`any` at the seam); :5-9 `DocItem` with exactly 1 repo hit (its own declaration — this seat's grep); AboutPane.vue:79 hand-copies an 11-member `MarkdownSpace` union (fourth copy; canonical `DisplayColorSpace` = 18); :97's `as MarkdownSpace` launders 7 spaces into `undefined`; :51's v-if silently hides the guide — **and the authored "Oh snap" Alert (Markdown.vue:19-31) is unreachable for a doc-less space because the v-if unmounts Markdown entirely** (F's verified insight; corroborated live by the CSS-seat's display-p3 probe). CURE: **the map's home, the cast's death, and explicit absence are owned by V·MT-W-CSS1** (already adjudicated: total catalog, `doc: DocModule | null`, the v-if becomes an authored empty state) — cross-booked, identity preserved, NOT re-authored here. What BUILDS here: DocItem deleted, DocModule typed to the real module shape (G-MD-7). The 7 missing guides stay ONE banked row at the catalog wave (O's B3). Honest limitation, recorded: if V·MT-W-MD1 were the only wave ever executed, the cast survives — its owner is named, adjudicated, and gated, which is what the no-deferral law requires.
- **L-11 — RESCOPED** (audit blindness CONFIRMED + built as π; two sub-claims REFUTED; addressability cross-booked). Repro: router/index.ts:22-37 = 14 real paths + catch-all, no `/about`, every one `component: Stub`; viewSchema.ts:110 seats About solely as the picker view's right pane ("About" rightLabel), picker declares no `defaultPaneIndex` (:122/:150 belong to other views), useViewManager.ts:65 `?? 0` → mobile lands pane 0; `find …/audit/visual/shots -name '*about*'` → nothing; the safari-desktop-light picker.png witness clips the About card at "Key Properties" — every pixel Markdown renders is uncaptured, so the sweep's `blankOrNearBlank:0 / pageErrors:0` rows are structurally silent about this component. REFUTED: "largest component in the demo tree" (third: 417/414/408, this seat's wc) and any implied mobile unreachability (Dock.vue:195-205 PaneSegmentedControl is a live toggle). **Addressability FOLDs → the standing W18 `/about` route identity** (ARCHITECTURE §3.1; layout-gestalt row 11: 66ch measure, UL-3 exemption; App.md D-2) — a new `ViewId` + VIEW_MAP entry is shell topology, not a route line. The π capture BUILDS here via the pane toggle, with that dependency stated, not hidden (G-MD-8).
- **MT-MD-12 — CONFIRMED (NEW, worker-O's; contract severity).** Repro: read useContrastSafeColor.ts:69-78 (the obligation, written from a measured 3.59:1 failure) then `grep -rn bumpProbeEpochOnMount demo/` → ConsoleRail.vue:133, useViewAccents.ts:89, internal :300/:348 — useMarkdownColors (which folds the referent at :44-48) is absent. Bounded: first evaluation defers past mount behind the skeleton branch + async doc load, so the predicted DELTA is null — filed as contract, never inflated. DISCHARGED STRUCTURALLY by the L-6 cure (useSafeAccentFn bumps at :348). Rider: **HeroBlob.vue (:42/:99, no bump — this seat re-verified) is the same shape**; booked to the ink/probe-contract sweep so it is not lost (no existing registry row holds it; it enters the ledger under this adjudication's name).

## The wave — V·MT-W-MD1 (re-authored: O's chassis, F's ordering + Alert insight, this seat's registry cross-bookings)

**Title**: The About guide becomes a module — one ink source, one live stylesheet, one honest seam, a committed witness.
**bornRed**: true. **Repo**: `/Users/mkbabb/Programming/value.js`, branch `tranche-u`. Supersedes both workers' drafts (F `MT-MD-1`, O `MT-W-MDGUIDE`).

**Scope — IN (edited)**:
- `demo/scenes/about/markdown/Markdown.vue` — the two dead Skeleton props deleted at :4/:6/:7 (**both `surface` AND the masked `variant`** — G-MD-1's reading note); runtime global-CSS imports :37-38 deleted (@reference :79 stays); the 146 dead theme lines excised and the two `:deep` blocks (:104/:198) merged; survivors (~185 lines: h2 h3 p a ul ol li code hr + mark.cs-name + the katex-display scroll box) move to `demo/styles/markdown.css` as one unscoped `.md-prose` class, no `:deep()`; the false `--phi-*` provenance comment (:94-96) corrected to `demo/styles/foundation.css:458+`.
- `demo/scenes/about/markdown/composables/useMarkdownColors.ts` — rewritten onto `useSafeAccentFn("resting")` + a local pure `withChromaFloor` carrying the whole :31-53 arithmetic; three deep reaches → zero; three throws → `{}` total degenerate; MT-MD-12 discharged structurally.
- `demo/color-session/useContrastSafeColor.ts` — the :363-368 beneficiary docstring deleted in the same commit (the widening loses its written justification; `resolveSurfaceLightnessLive` itself retires with the HeroBlob rider, not here).
- `demo/scenes/about/markdown/composables/useMarkdownHighlighting.ts` — the :71-80 dead-plugin/killed-alias paragraph deleted; surviving prose describes only what the function does.
- `demo/scenes/about/markdown/index.ts` — DocItem deleted; DocModule typed to the real module shape (no `any`).
- `assets/docs/*.md` ×11 — MOVED to `demo/scenes/about/content/`, line-2 import rewritten to `"../katex"`; `assets/` ceases to exist; whichever of {this wave, V·MT-W-CSS1} lands second updates the map/catalog import specifiers (a string-only touch — the coordination seam, stated).
- **Supply chain (built here per the L-4 ruling)**: `plugins/vite-source-export.ts` deleted; its defaultPlugins slot (vite.config.ts:160) and the vite `@src` alias (:75) + its false comment (:70-74) deleted. `vitest.config.ts`'s own `@src` STAYS (4 live test consumers).
- NEW: `tsconfig.demo.strict.json` (scoped strictTemplates program) · `demo/test/about/use-markdown-colors.test.ts` · `demo/test/about/guide-modules.test.ts` · `docs/tranches/V/megatranche/workflows/gates/{md-selector-census.mjs,md-ink-source.mjs}` · `docs/tranches/V/megatranche/audit/visual/shots/about-guide/` (force-added past `.gitignore` `*.png`).

**Scope — OUT, each with its standing owner (nothing re-minted, nothing deferred)**: the tsconfig paths↔exports parity → **W-HYGIENE config parity** (CSS-adj L-14, born-RED generator diff, RED today at FAIL=8/5-key drift) · the eslint lattice re-key → **L-5(a) lint-glob liveness** (CSS-adj; born-RED `--print-config` UNSET today) · demo/ui 90-site extirpation → **W-HYGIENE** (ColorPicker-adj L-6) · `/about` addressability → **W18 §3.1** (layout-gestalt row 11; born-RED `grep -c 'path: "/about'` → 0 today) · the AboutPane map→catalog + explicit authored empty state + 7 missing guides → **V·MT-W-CSS1** (+ its B3 bank) · the parser `oklch()` TypeError → **V·π** (blocked on OC-1/OC-2 + begin-word; standing gate) · the glass Skeleton `--muted` fill + any shimmer/surface variant desire → **glass BH inbox relay** (owner edict; INBOX I-20; masking-fallback ban — no local patch may be proposed) · demo-wide strictTemplates (271) → **BANK B1** (widen one scene at a time; re-trigger: the scoped program reports 0 scene rows) · HeroBlob probe-epoch omission → **ink/probe-contract rider** (this adjudication's booking).

**Structure over gates (formation law L-8)** — five cures are made unrepresentable: (1) the ink dual-path DIES by deletion — after the rewrite `useSafeAccentFn` is the only way the scene can mint a certified accent, and MT-MD-12 becomes unrepresentable (the bump rides :348); (2) the exception channel DIES at the type level — the composable returns a total `ComputedRef<Record<string,string>>`; (3) the content inversion DIES by the move — no path from `assets/` into `demo/` can exist because `assets/` does not; (4) the seam's `any` DIES by typing; (5) phantom-prop expressibility DIES for this scene under the scoped strict program (the producer boundary means demo-side unrepresentability is impossible — the gate is the only instrument, said plainly). Two cures genuinely need gates: CSS↔corpus correspondence (no type system reaches a stylesheet — G-MD-2's bidirectional census) and the committed witness (G-MD-8).

**Gates (all born-RED today; each names the input that makes it RED)**:
- **G-MD-1 · scoped strict templates** — `npx vue-tsc -p tsconfig.demo.strict.json --noEmit 2>&1 | grep '^demo/scenes/about/markdown/' | wc -l` → assert 0. RED NOW: 3 rows, TS2353 at Markdown.vue (4,19)/(6,23)/(7,23) (this seat's run). RED input: any non-`class` attribute on `<Skeleton>`. Reading note: TS2353 names only the FIRST excess prop — `variant` is masked; delete both. Blindness stated: the same program demo-wide reports 271 (banked B1).
- **G-MD-2 · styled ⊆ rendered, bidirectionally** — `node docs/tranches/V/megatranche/workflows/gates/md-selector-census.mjs`: renders all 11 guides through the repo's own markdown-it, unions emitted tags + declared runtime injections (mark.cs-name, Katex divs), diffs against every element/class selector in the component + markdown.css. RED NOW: 21 dead groups (14 elements + 7 class families). RED inputs, both directions: re-adding `blockquote { … }` (styled-never-rendered) OR deleting the `hr` rule (rendered-never-styled). Supersedes the per-document DOM census (the refuted `a:0` instrument).
- **G-MD-3 · the composable is total and never throws** — `npx vitest run demo/test/about/use-markdown-colors.test.ts`: `{}` for `'notacolor'` and `''`; three-key object for `'rgb(1 2 3)'`. RED NOW: :33 throws on `notacolor` out of a `:style` computed. RED input: any `throw` re-introduced. ENV honesty (L-12): jsdom has no canvas — the live probe yields null and the static model serves; the test asserts SHAPE only, never an ink value (a color assertion there would certify the model against itself); ink values are proved solely by DELTA (2) in a real browser. Does NOT cover `parseCssColor("oklch()")` — V·π's row.
- **G-MD-4 · one ink seam, zero deep reaches** — `node docs/tranches/V/megatranche/workflows/gates/md-ink-source.mjs`: (i) `../../../../color-session/` count under demo/scenes/about/ = 0 (today 3: :5/:6/:7); (ii) useMarkdownColors imports `useSafeAccentFn` (today no); (iii) `resolveSurfaceLightnessLive` importers under demo/scenes/ = 0 (today 1). RED input: re-adding any color-session deep import. This gate is MT-MD-12's mechanical trace.
- **G-MD-5 · content lives in its scene** — `test $(grep -rn 'demo/' assets/ 2>/dev/null | wc -l) -eq 0 && test ! -d assets/docs && npx vitest run demo/test/about/guide-modules.test.ts` (every guide value resolves to a module with a default export; every key ∈ DisplayColorSpace). RED NOW: 11 hits + the dir exists. RED input: a guide importing any `demo/` path, or a guide left under assets/.
- **G-MD-6 · one global-CSS mechanism at one place** — `test $(grep -rn 'import "[^"]*\(foundation\|utils\)\.css"' demo/ | grep -v '^demo/color-picker/App.vue' | grep -v '^demo/styles/' | wc -l) -eq 0`. RED NOW: Markdown.vue:37/:38. RED input: any non-root demo file runtime-importing the global sheets.
- **G-MD-7 · the module seam exports only what is consumed, typed** — `test $(grep -rn 'DocItem' demo/ src/ test/ e2e/ 2>/dev/null | wc -l) -eq 0 && test $(grep -c ': any' demo/scenes/about/markdown/index.ts) -eq 0`. RED NOW on both legs (DocItem's own declaration; `{ default: any }`). The `as MarkdownSpace`/absence legs are V·MT-W-CSS1's gates — cross-cited, born-RED there today, not double-owned here.
- **G-MD-8 · (π) the rendered guide exists as a committed witness** — capture + `git ls-files --error-unmatch docs/tranches/V/megatranche/audit/visual/shots/about-guide/*.png`. RED NOW twice: zero `*about*` shots exist; `git ls-files docs/tranches/V/megatranche | grep -c png` → 0. RED input: a capture not force-added past `.gitignore` (*.png) — a witness not committed does not exist. Until W18 lands, the capture drives the Dock pane toggle, a stated structural limitation.
- **G-MD-9 · the dead supply chain is gone and the build still ships** — `grep -n 'sourceExportPlugin\|"@src"' vite.config.ts | wc -l` → assert 0 (today 3: import :13, call :160, alias :75) **&&** `npm run gh-pages && test -s dist/index.html` (product assertion, never exit-code). RED input: the plugin or alias re-appearing, or the build emitting no artifact.

**π obligations** (I-20: cells never infer from one another): safari-desktop-light, safari-desktop-dark (real Safari 26.4 via safaridriver :4599), playwright-webkit-desktop-light, chromium-desktop-dark. Spaces: rgb (default), oklch, lch — two of the only three anchor-bearing guides + the widest KaTeX rows, exercising every surviving `.md-prose` selector class. Route: `/#/` hash root, About pane via the toggle (no URL exists until W18 — stated). Selector: the `.markdown-body` bounding box at full scroll height. Committed: `docs/tranches/V/megatranche/audit/visual/shots/about-guide/<cell>-<space>.png`, force-added. ENV: the About corpus is API-less-complete (static content — the one component this environment can witness whole); the close ALSO captures once from the built gh-pages artifact (the dev server is structurally blind to build defects), which additionally proves the `.md-prose` extraction bundled.

**Delta obligations** (three pairs, each proving a different claim): (1) STYLE-NEUTRALITY — the same 12 captures before/after the 146-line excision + `.md-prose` move; any non-anti-aliasing pixel delta means G-MD-2's census missed a live rule. (2) INK IDENTITY — computed `color` on the first `> h2` and a `p > code` chip, both schemes, before/after the useSafeAccentFn swap; byte-identical required — a delta means `withChromaFloor` failed to reproduce the :31-53 arithmetic. (3) THE HONEST NEGATIVE (MT-MD-12) — `getComputedStyle(h2).color` at first frame after `currentDoc` resolves, before/after; prediction: NO delta (the async load already defers evaluation past mount). A delta refutes the prediction and raises the row to availability in a written addendum — never quietly.

**Completable alone — the L-1 one-wave test, answered**: if V·MT-W-MD1 were the only wave this formation ever executed, the About guide owns its content (11 files moved, the inverted edge gone, `assets/` deleted), its stylesheet (one unscoped class, 146 dead lines excised under a bidirectional census, the duplicate :deep collapsed), its ink (three deep reaches → one public composable, the exception channel typed away, the probe-epoch contract discharged), its seam (DocItem gone, the `any` typed), its supply chain (a zero-consumer plugin + alias retired with the build product asserted), and the first committed visual witness this component has ever had. Nothing requires any other wave first: G-MD-1 rides its own scoped tsconfig; G-MD-3/5 ride vitest's existing demo/test config; G-MD-8 rides the pane toggle. Every folded row leaves with a standing registry owner and a runnable born-RED command; "next tranche decides" and its synonyms appear nowhere.

## Addendum clause — ADD-MDG (E-3: appended, never rewriting the challenger record)

**MDG-1 · Content lives in the scene that renders it.** No file outside `demo/` may name a path inside `demo/`. If the app is a document's only consumer, the document lives in the app's tree and imports downward. Enforced by command, then by `assets/` not existing.

**MDG-2 · A theme may only style markup its own corpus emits.** For a CLOSED corpus, styled-set = rendered-set, re-derived from the corpus by a command, bidirectionally — never asserted by a comment. Dead CSS is not harmless surface: 146 of 331 lines here were adjudicated by two prior rulings (AB-3, the F6 ONE-grammar law) against markup that has never rendered — dead CSS manufactures findings.

**MDG-3 · One concept, one composable — a widened surface is a confession.** When an export's docstring names its beneficiary (`resolveSurfaceLightnessLive` naming `useMarkdownColors`), that sentence is the defect's signed statement: fix the call site, delete the widening, never keep both. **Corollary (earned twice here)**: when a helper's docstring states a consumer obligation (`bumpProbeEpochOnMount`), a consumer that takes the referent and skips the obligation is defective even while the omission is dormant — the contract was written from a measured 3.59:1 failure. And a reviewer who quotes the docstring two lines below a contract while missing the contract has re-proven why seats are adversarial.

**MDG-4 · The Result contract is not re-openable at the render boundary.** A `computed` may not `throw`. Decoration has a degenerate — return it. An unreachable throw is a latent one.

**MDG-5 · A cast is not a type; a fourth copy is three unnoticed drifts.** `x as NarrowUnion` at a Record lookup converts a compile error into a silent render omission. Partial maps say `Partial<Record<Canonical, T>>` and handle `undefined` visibly; total maps make the compiler prove totality. And prefer `Partial<Record>` over an `Extract`-derived subset: `Extract` silently drops a typo'd member — the safer-looking shape is the weaker one.

**MDG-6 · A surface with no URL has no audit.** Route-driven gates are structurally blind to pane-only surfaces. Until addressable, the π obligation is discharged by an explicitly stated non-route capture, and any `blankOrNearBlank: 0` report states in the same breath which surfaces its matrix could not see.

**MDG-7 · A prop the producer does not declare is a lie, not a hint.** It reads as intent and ships as an inert attribute. Where the producer's API cannot express the intent, that is a producer change under the glass boundary — relayed (INBOX I-20), never locally overridden, never masked. And the gate that catches it reports only the FIRST excess property per site — delete what the design passed, not what the gate printed.

**MDG-8 · A closed-corpus claim is measured over the corpus, not over one document.** A DOM census of the selected item generalized to eleven is MT-F006 wearing new clothes. Render the corpus; take the union.

**MDG-9 · (arbiter's, earned in this tri-fold) A gate greps what it greps — and a fold names a standing owner.** Two lessons from the workers themselves: (a) worker-F piped a 271-error compiler run through a component-scoped grep and proposed the full run as a one-wave gate — scope every count to source means scope every GATE to what its command actually reads; (b) both workers minted fresh wave names (`MT-W-PATHS`, `MT-W-UISHIM`, `MT-W-SHELL-ADDR`) for identities this registry already adjudicates under standing names — before folding, grep the registry; a fold to a wave that does not exist is a deferral wearing a fold's name.

## DISSENT — preserved, not ruled away

1. **L-4 ownership (the arbiter's sole ruling against worker-O).** O's boundary argument is preserved verbatim: "The plugin and alias deletion belongs to a build-graph wave — removing a member of defaultPlugins is not the About scene's call." This seat ruled BUILD-here because no build-graph wave exists in the registry (O's fold named a non-existent owner), because the plugin's sole documented purpose is this wave's own corpus, and because G-MD-9 asserts the build product. If a build-graph wave is later formed, this stage may be re-homed there BEFORE execution — the disagreement is about ownership, not about the facts, which are unanimous (0 consumers, phantom golden, false comments).
2. **Worker-F's instrument failures, recorded for the formation's method ledger**: the 71-site count (his own regex re-run yields 90); the G3 demo-wide strictTemplates gate proposed while the same program reports 271 errors (an un-landable gate offered as this wave's own); and the MT-MD-12 miss while quoting the adjacent docstring — F's return simultaneously called the bump "the intended live-probe idiom, not a hazard." F's genuine contributions are equally recorded: the unreachable-Alert insight (verified: AboutPane's v-if unmounts Markdown, so the authored absence-state cannot render), the G1→G3 ordering argument (survives as sequencing inside W-HYGIENE config parity), and the 149-line range arithmetic both workers shared.
3. **Worker-O's L-1 dissent is adopted nearly whole and one sentence preserved verbatim**: "a formation that has manufactured seven false findings in one session cannot afford a BLOCKER whose headline its own measurement refutes." The mechanism is real, cheap, and sequenced before V·π; the headline was false.
4. **O's B4 bank preserved unmeasured, as filed**: the `content-visibility: auto` + `contain-intrinsic-size: auto 200px` rung (Markdown.vue:106-109) over KaTeX display blocks — re-trigger: a find-in-page + scroll-anchor probe over the oklch guide. Nobody has measured it; nobody asserts anything about it.
5. **The L-10 completability tension, stated plainly**: this adjudication cross-books the cast/absence cure to V·MT-W-CSS1 (which owns the catalog); if only V·MT-W-MD1 ever executes, `as MarkdownSpace` survives at AboutPane.vue:97. The alternative — both waves editing the same map — was judged the worse defect (double-booked identity, guaranteed merge conflict). The owner is named, adjudicated, and born-RED; that is what the law requires, and the residual risk is recorded rather than hidden.
6. **Verdict-distribution note (L-14 corollary)**: 7 CONFIRMED / 5 RESCOPED / 0 REFUTED is not rubber-stamping — every disputed number in this record was re-measured by this seat's own commands (the 271, the 90/48, the 52=52, the 146, the 408-third, the 4 test files, the 3 anchors, the TS2353 positions), and the incredulity changed outcomes in both directions: it refuted F's count, F's gate, and F's fold shapes; it refuted O's fold targets against the registry; and it admitted O's new row only after this seat re-read every cited line.
