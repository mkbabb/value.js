# ErrorBoundary — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · seat model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: apotheosis adjudicator (incredulity seat) · **Date**: 2026-08-04 · **HEAD**: `95cfbb15` · branch `tranche-u`.
**Subject**: `demo/color-picker/ErrorBoundary.vue` (87 lines) + its sole consumer `demo/color-picker/App.vue:50-140` and the containment/loader/reporting lattice around it.
**Corpus**: `audit/components/ErrorBoundary/` — r2 governs (`challenge-D-design.md` D2-1..D2-11 · `challenge-C-implementation.md` C-1..C-15/V-1..V-6 · `challenge-L-library.md` L2-1..L2-10), r1s preserved beside them; evidence in `evidence/`, `evidence-r2/`, `probes/`, `probes-L-r2/`. All three challenge files are `EXISTS-ORIGINAL` in `registry/HYDRATION-LEDGER.md:72-74`; `grep ErrorBoundary audit/codex-provenance/motion-quarantine.md` → zero rows, so **no M-xx FALSE row constrains any claim here** (checked; the D2-9 motion findings are unquarantined and stand on their own evidence).
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code) — two hostile passes over the full corpus, each returning confirmed/killed/missed/unproven/superlatives.
**Method (L-18)**: both returns were attacked before either was adopted. Every verdict-level disagreement (D2-3, L2-7, the D2-4 retry mechanism, reader-LC's M1/M7 missed finds, reader-DU's `/80` severity) was re-derived against the tree by this seat with its own instruments. Every load-bearing kill was re-proven, not spot-trusted.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony; product source READ-ONLY)**: full read of ErrorBoundary.vue (87 lines); App.vue:1-60/:125-168/:230-245; shell.css:19-95 (line-pinned: `.app-layout{position:relative}`:25, `.dock-band{position:relative}`:39-40, `.pane-main` positionless :50, `.pane-container` :59 with `max-width:min(…)` :69-73 + `position:relative` :74 + `max-height:var(--content-max-h)` :75, `.pane-wrapper--left{z-index:1}` :95); foundation.css:475 (`--content-max-h: clamp(34rem,86dvh,52rem)`) + :716 (the only `[tabindex]` arm, excluding `-1`); usePaneRouter.ts:55-100/:155-180; viewSchema.ts:40-67; useViewManager.ts:35-55; PaneSlot.vue:60-135 (whole template); useDevicePixelSnap.ts (whole, 98 lines); main.ts (whole, 7 lines) + index.html:205 + `git show c4af0ef9` (2026-07-29 13:50); router/index.ts:36-37; EmptyState.vue:14-30/:50-53/:70-75/:96-104; useContrastSafeColor.ts:280-330; useAtmosphereBoot.ts:85-115; status-lamp.ts:35-56; a11y-authed-admin.spec.ts:105-156; installed `@vue/runtime-core` dist: `handleError`:227-258 (raw hook invocation + early return), async-component factory :2700-2770 (`error.value` assignment sites; `handleError(err, instance, 13, !errorComponent)`), `filterSingleRoot`:4669-4690 + `getChildRoot`:4646; repo greps re-run: `.plate-ink` declarations (=5), `var(--ink-muted, var(--muted-foreground))` (=12 across 9 files), `text-destructive/` (=2), `font-display text-heading` (4 live sites), `variant="outline" size="sm" class="font-display` (=7), `role="alert"` (3 SFC + status-lamp), `vj-error-boundary` (=1), `demo/ui` (=19 dirs; label+switch importers = 0), `demo/@` (ENOENT), eslint.config.js:235-238/:275-276/:283-296, the 3 upward `color-picker/` imports + App.vue:164, tsconfig.demo.json:31-50, `ls dist/ dist/subpaths/` + exports (7 keys, no root), glass-ui exports (74 keys, no `./alert`, has `./surface`/`./card`/`./button`), `ls test/dist` (ENOENT) + `git log -- test/dist` (`6d6d3521`), .gitignore:15-26, vitest.config.ts:18-30, VISUAL-CONSTITUTION.md (`docs/tranches/V/`):19/:21/:28/:56/:73-78/:115/:141/:151-154, evidence-r2/R2-retry.json + R2-paint.json (read whole), challenge-L-library-r1.md:111-133, X wave chart `docs/tranches/X/waves/W0..W11` ownership greps.
**Wave authority for routing**: tranche X ratified (`docs/tranches/X/waves/`). **No X wave names ErrorBoundary** (repo-wide grep over `waves/*.md` → 0). Owners used below: **X-W5** owns the cure surfaces by file (`main.ts` "composition root" unit 1 + §files `main.ts`/`index.html`/`App.vue`/`PaneSlot.vue`/`usePaneRouter.ts`; hard gate = one H1/protagonist per route + PRM-guarded scene-swap motion) — rows landing on those files are marked **X-W5 (rider — surface owned, unit unnamed)**; **X-W3** items 15/16 own the not-found route + the `componentFor` fail-closed tail; **X-W7** owns the plate register (EmptyState-adj ES-6 already names the ErrorBoundary twin as cross-surface residue); **X-W8** owns `demo/ui` + the dead `demo/@` globs; **X-W9** carries the glass-ui relay packets; **X-W10** owns type/design canon; **X-W1** owns the oracle re-gate.

## Scoreboard

**36 CONFIRMED (2 BLOCKER · 15 MAJOR · 17 MINOR · 2 INFO) · 14 KILLED (12 reader kills ratified/refined + 2 arbiter-original kills/demotions of reader claims) · 3 arbiter-original evidentiary finds · 10 UNPROVEN-NEEDS-LIVE residue rows.**

Blocker-weight survivors: **EB-1** (the caught plate paints nothing but the button — zero-mutation three-arm identification, mechanism fully source-verified by this seat) and **EB-2** (one catch latches the whole application dead for the session while the dock keeps accepting navigation). Composed they are the component's own header claim inverted: the "NEVER a silent white-screen dead plate" file ships a silent near-blank dead plate that cannot be discharged.

## Ruled disagreements (the incredulity seat's actual work)

| # | question | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| **R-1** | D2-3 — "the boundary hand-rolls a primitive Vue already ships per-pane" (BLOCKER?) | CONFIRMED as part of the D2-3/L2-8/V-1 BLOCKER row | KILLED as framed: `errorComponent` covers a disjoint failure class | **reader-LC wins; D2-3 dies as an independent BLOCKER; residue = EB-4 (MAJOR)** — and this seat adds a cure-lock neither reader derived. runtime-core.cjs.js:2725-2770: `error.value` is assigned in exactly two places — the `load().catch` and the `timeout` branch — so `errorComponent` renders only for load rejection/timeout; a render/lifecycle/watcher throw from the resolved component (exactly what the sole e2e induces) never touches it. The boundary is therefore not a re-implementation of the async primitive. **Arbiter-original (cure-lock)**: the internal `onError` calls `handleError(err, instance, 13, !errorComponent)` and `handleError` walks the `instance.parent` chain **unconditionally** (`!errorComponent` only sets `throwInDev`) — so adding `errorComponent`/`onError` to the ten loaders does NOT stop the rejection from also reaching the App-altitude boundary's `onErrorCaptured` and latching the app. The loader-options cure and the containment transposition are jointly necessary; landing either alone leaves a live BLOCKER. Any wave binding EB-4 must state this. |
| **R-2** | L2-7 — "two masking fallbacks for one concept" | CONFIRMED (MAJOR, both mechanisms) | HALF-REFUTED: the `componentFor` tail is unreachable dead code | **reader-LC wins; EB-16 rescoped, MAJOR retained** for the missing not-found composition. My reads: both call sites (usePaneRouter.ts:164-165/:169-171) pass `currentConfig.value.left/right`, typed `LeftPane` (11 members, viewSchema.ts:52-63) / `RightPane` (4 + null, :66); `useViewManager.ts:43-45` clamps `currentView` through `isViewId(name) ? name : "picker"`; :82-93 handle all 16 cases; the tail cannot execute. **Arbiter-original**: the function's own doc comment (:80) reads "Maps a view-config slot name to its component. `` `null` `` for an unknown name" — the tail `return ColorPicker;` (:94) contradicts the function's own stated contract. The observed `/#/does-not-exist`→picker render is caused entirely by router/index.ts:36-37's catch-all redirect. |
| **R-3** | D2-4 retry mechanism — module-map cached rejection vs loader re-fetch | KILLED the r2 correction paragraph, citing R2-retry.json `networkAttempts.retry: 0` | listed UNPROVEN-NEEDS-LIVE (jsdom `loader_attempts_after_retry: 2` vs the live trace) | **Both refined: the artifact is INADMISSIBLE for either mechanism — it contradicts ITSELF.** My read of evidence-r2/R2-retry.json whole: `requests` records a **retry-phase** entry `AboutPane.vue?t=1785272919096` with a fresh cache-bust timestamp (evidence FOR a second fetch of a distinct URL) while `networkAttempts` in the same file records `{boot:1, retry:0, note: "the module map served the cached rejection; no fetch was made"}`. One artifact, both directions. Reader-DU's kill conclusion survives narrowly (r1's mechanism must not be recorded as withdrawn) but its premise ("the probe output asserts the opposite of the prose" — it asserts both) is corrected. The question is OPEN → U-3; the user-visible defect (retry does not recover) holds on every reading and is booked in EB-4. |
| **R-4** | reader-LC missed M1 — `useDevicePixelSnap` "permanently disarmed + retention leak" (MAJOR) | — | MAJOR, three limbs | **DEMOTED to MINOR (EB-32) with the mechanism corrected from source.** My read of useDevicePixelSnap.ts whole: limb (3) "the cure is silently dead for the rest of the session" is OVERSTATED — `sync()` re-reads `container.value` fresh on every call (:38), and the `transitionend`/`animationend` listeners (:84-85) and `window resize` (:83) are **document/window-level**, so after `reset()` the first settle inside the remounted `.pane-container` re-drives the snap through the live template ref. What is permanently lost after one catch: the ResizeObserver channel (`ro.observe(host/wrappers)` :74-78 binds the DEAD elements; `onMounted` cannot re-run) and the MutationObserver childList channel (:80-81) — element-resize- and child-swap-driven re-syncs are disarmed for the session. Limb (1) "holding the old pane subtree alive — a real retention leak" is UNPROVEN: both observer specs hold observation targets weakly; no retention was measured. Limb (2) verified (the settle handler no-ops against a null ref while caught — harmless). Real, narrow, MINOR. |
| **R-5** | reader-LC missed M7 — dev/prod fragment-root divergence, "attribute fallthrough silently disabled" (INFO) | — | INFO | **KILLED (K-13, arbiter-original).** runtime-core.cjs.js:4669-4690 `filterSingleRoot` explicitly skips Comment vnodes (`child.type !== Comment`) when locating the single element root, and `getChildRoot`:4646-4668 re-attaches fallthrough attrs to that filtered root via `setRoot`; the "Extraneous non-props attributes" warning fires only when MULTIPLE non-comment roots exist. A dev-only template comment (ErrorBoundary.vue:2-14) does not disable fallthrough — Vue ships this exact case handled. Residue: cosmetic only (rationale prose could live in `<script setup>`); no defect row is minted. |
| **R-6** | reader-DU missed — `text-destructive/80` as MAJOR | MAJOR | — | **DEMOTED to MINOR (EB-28).** The find is real and its provenance is exact (my greps: exactly 2 sites, EmptyState.vue:19 + ErrorBoundary.vue:23; useContrastSafeColor.ts:318-322 "post-hoc opacity de-emphasis is retired"; EmptyState.vue:100-101 "D6 retires the class by name" — the D6 cure reached the display line and left the glyph's `/80` in both files). But the user-visible weight (the glyph's sub-floor contrast) is already carried by EB-5/U-1; this row is a mechanism share and a 2-site canon breach — one family row, MINOR. |
| **R-7** | the L-1/L2-1 cure markup — `PaneErrorBoundary` as `KeepAlive`'s direct child, `:key="liveKey"` ("the whole fix for the latch") | inherited approvingly (C-2 cure: "one boundary per PaneSlot, keyed on the pane key") | KILLED as structurally unsound | **reader-LC's kill RATIFIED (K-6) with this seat's own PaneSlot read.** PaneSlot.vue:112-129: the live shape is `Transition > KeepAlive :max > component :is :key="liveKey"`. KeepAlive caches its child instance by key and DEACTIVATES rather than destroys — a boundary sitting as the cached child preserves `caught === true` across navigation away and restores the caught plate on return (until `:max` LRU eviction): the latch reborn at pane granularity. Binding constraint for any wave: the boundary sits OUTSIDE KeepAlive, or the seat itself renders the fallback (the D2-7 shape, which does not have this flaw). Reader-DU's C-2 cure sentence inherits the same trap and is corrected by this ruling. |

## Adjudicated defect roster

Severity · claim · proof standard · terminal disposition. Line anchors are HEAD `95cfbb15`; re-resolve before curing (D-19 law). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. "Successor surface" = the post-transposition boundary/fallback pair the BLOCKER cures produce.

**BLOCKER**

- **EB-1 · BLOCKER — the caught plate's ink paints under the atmosphere canvas; only the Button survives** (C-1 ≡ D2-2 ≡ D-1; both readers CONFIRMED). Mechanism fully re-verified from source by this seat: ErrorBoundary.vue:18 declares no positioning and the scoped block (:84-86) sets only `color` → `position: static`; App.vue:9-17 the canvas is `absolute inset-0 w-full h-full` inside shell.css:25 `.app-layout{position:relative}`; shell.css:50 `.pane-main` is positionless; the DISPLACED element carries the contract the boundary lacks — `.pane-container{position:relative}` :74 and `.pane-wrapper--left{z-index:1}` :95 — and the control case sits 35 lines above the defect: `.dock-band{position:relative}` :39-40, which is exactly why the dock paints in every frame and the plate does not. CSS 2.1 Appendix E: in-flow inline content of non-positioned descendants = step 7; positioned z-auto = step 8 (the r2 file's "step 5" is corrected — K-9). Zero-mutation shipped arm: 0 of 16,020 statement px / 0 of 784 glyph px, minLuma 128; two mutually exclusive interventions each restore ink (3,089 / 3,016 px) — counts are U-2 (live), the identification design and mechanism are certain. → **X-W5 (rider — App.vue/PaneSlot.vue owned)** jointly with EB-2's transposition; the plate body lands at **X-W7** (plate register). The cheap `position:relative` patch is BANNED by EB-8 (it ships a 1408px plate).
- **EB-2 · BLOCKER — one catch latches the whole application dead for the session; containment is simultaneously too coarse and too narrow** (C-2 ≡ L2-1 ≡ D2-4c ≡ L-1; both readers CONFIRMED). `caught` (:55) is component-local, written at :60, cleared only in `reset()` (:71-75); the slot sits in `v-else` (:35); App.vue:50-140 mounts ONE boundary with no `:key`/route coupling around the entire two-pane grid, so one pane's throw withholds both and route changes re-render nothing (R2-retry.json afterRouteChange: `#/gradient`, `boundaryUp true`, `paneContainerPresent false`). The narrow half: `<nav>` (:24-44), the canvas (:9-17) and `MigratePalettesDialog` (:152-157) are siblings — `grep onErrorCaptured demo/ src/` returns exactly one carrier — so the dock survives and keeps accepting navigation, reporting a scene that is not mounted. → **X-W5 (rider)** — the three-altitude transposition (pane containment in the seat, OUTSIDE KeepAlive per R-7; shell containment + reporting at the composition root main.ts, which X-W5 unit 1 owns by name). Containment census 90.6% = U-5.

**MAJOR**

- **EB-3 · MAJOR — terminal unreportable sink: `return false` halts Vue before `errorHandler` and `logError`, and no net exists anywhere** (C-3 ≡ L-3 + the L2-2 residual after K-1). runtime-core.cjs.js:238-241 early-returns on a `false` hook result, above the `errorHandler` branch (:245-254) and the tail `logError` (:257); the hook (:59) discards Vue's `instance`/`info` args; main.ts (7 lines, read whole) installs no `errorHandler`, no `unhandledrejection`, no `window.error`; repo grep → only ErrorBoundary.vue's own lines. R2-C: an installed handler was called **0** times. `return false` itself is the correct call for an owning boundary (both readers agree) — the defect is what the file does INSTEAD of reporting. → **X-W5 (rider — main.ts composition-root unit; the error-net lines are unowned by its text: named rider, ~5 lines)**. EB-35/EB-36 fold here.
- **EB-4 · MAJOR — all ten panes are bare one-argument `defineAsyncComponent`; the chunk-load class lands in a latching global boundary whose one verb cannot cure it; a chunk that HANGS is structurally unreachable** (L2-8 ≡ V-1 ≡ the D2-3 residue after R-1). usePaneRouter.ts:69-78: ten bare loaders, zero options (`errorComponent`/`loadingComponent`/`delay`/`timeout`/`onError` all absent from the file). The shipped detail string for this class is "Importing a module script failed." / "Failed to fetch dynamically imported module" — the stale-chunk event whose only cure is a reload, and no reload affordance exists (R2-retry `anyReloadAffordance: false`). **Carries R-1's cure-lock verbatim**: loader options alone do not stop parent-chain propagation into the boundary — EB-4 and EB-2 land together or the latch survives. Retry mechanism = U-3 (open). → **X-W5 (rider — usePaneRouter.ts owned; cross-note X-W3.16's fail-closed tail)**.
- **EB-5 · MAJOR — the plate has no material tier: its ground is the seed-derived ambient field, its ink scheme-derived, and the `--ink-muted` certification does not transfer** (D2-1 structural half ≡ C-6; both readers CONFIRMED the structure, magnitudes live-only). :18 carries no surface/card/glass material; useContrastSafeColor.ts:296-311 certifies against `surfaceLightnessNow("resting", …)` — the resting plate rung, a referent that does not exist here; the file's own :79-86 offer token names as evidence, against VC:82-83 verbatim ("a token name is not evidence" / "never color-only"). The cure's export premise is TRUE (glass-ui 74 exports include `./surface` and `./card`). D2-1's "no CSS can fix this" framing is killed (K-10) — the requirement "the plate must acquire a tier" survives. Per-seed magnitudes = U-1. → **X-W7** (plate register; cross-book EmptyState-adj ES-6's named cross-surface residue) + **GLASS-RELAY** via X-W9's packet.
- **EB-6 · MAJOR — focus is thrown unconditionally to an unnamed 1408×804 container that gets the UA ring, and is never restored on reset** (C-4 ≡ D2-10; + V-3's mid-typing theft, sequence = U-4). :21 `tabindex="-1"`, :64 `nextTick(() => alertRef.value?.focus())` fires on EVERY catch without reading `document.activeElement`; `reset()` destroys the focused Button and restores nothing (C-D: activeElement AFTER reset = BODY); foundation.css:716 is the house ring's only `[tabindex]` arm and it excludes `-1`, so the element is explicitly outside the house recipe; :20 `aria-live="assertive"` is redundant on `role="alert"`. The §5.1 canon citation is killed (K-8) — the behaviour stands on the ARIA alert/alertdialog pattern. → successor surface (**X-W5 rider** mechanics + **X-W7** body).
- **EB-7 · MAJOR — `role="alert"` wraps an interactive control: the wrong ARIA role for this composition, and the reason the focus hack exists** (reader-DU missed find; ADMITTED — this seat re-verified the structure: the sole recovery `<Button>` :30 sits inside the `role="alert"` region :19). WAI-ARIA reserves `alert` for content requiring no interaction and directs `alertdialog` when it does; `alertdialog` supplies by pattern exactly the modality/return-focus contract that :21/:64 hand-patch — which makes EB-6's "drop tabindex, focus the named control" half a cure while the role is wrong. → successor surface; the role decision is a canon act (**X-W10** note).
- **EB-8 · MAJOR — the boundary drops the seat's PROPORTION contract along with its stacking contract** (reader-DU missed find; ADMITTED — line-pinned by this seat). `.pane-container` carries THREE inherited declarations the `v-if` arm discards: `position:relative` (shell.css:74), the 2026-07-05 owner-ruled ≈1050px clamp `max-width:min(calc(100vw − pad), calc(--pane-max*2 + --pane-gap))` (:69-73), and `max-height:var(--content-max-h)` (:75; `clamp(34rem,86dvh,52rem)` foundation.css:475). ErrorBoundary.vue:18 is `h-full w-full min-h-0` and inherits none. Operationally decisive: the cheap EB-1 patch (`position:relative`) restores paint and ships a 1408px-wide plate where every healthy scene clamps to ~1050px — a proportion break recorded as "cured". **Supersession warning**: X-W5's hard gate retires the shell's block-axis cap — any wave binding this row re-derives the proportion contract against X-W5's post-state; the inline clamp is the owner-ruled invariant. → successor surface (**X-W5 rider**).
- **EB-9 · MAJOR — a fork of `EmptyState variant="error"` with five drifted magnitudes, a byte-identical CSS clone, two prop idioms — and no design-system home for the failure register at all** (D2-8 ≡ V-5 ≡ L2-5 ≡ L-2; both readers, diffed line-by-line, re-verified by this seat: `gap-2.5 py-8`/`gap-3 py-10 px-6`, `w-6`/`w-7`, `26ch`/`28ch`, `44ch`/`46ch`, `.plate-ink` byte-identical, `withDefaults`/destructure, `#action` slot/hard-coded Button). Three demo `role="alert"` homes + the dock lamp; glass-ui ships `Alert`/`AlertTitle`/`AlertDescription` from the root barrel only — no `./alert` among its 74 exports. → **X-W7** (ONE identity with EmptyState-adj ES-6 — cross-booked, never re-minted) + **GLASS-RELAY** (the producer plate + `./alert` subpath ask rides X-W9's standing packet, per the owner's BH/BI relay edict).
- **EB-10 · MAJOR — Fraunces on a control label is a seven-site pasted family; the plate's own Button carries THREE per-instance overrides in one line — the producer-variant signal, not three demo patches** (D2-6 + reader-DU missed find #7 + the RTL limb). My grep reproduces all seven `variant="outline" size="sm" class="font-display` sites verbatim (ErrorBoundary.vue:30 + six admin-panel sites); VC:75 assigns control labels to `text-small`/Plus Jakarta non-bold, VC:78 closes the matrix. On the one control: `font-display` (family), `mt-1` (breaks the plate's own gap-3 rhythm at exactly one interval — rect arithmetic checks), `mr-1.5` on the icon (physical, not logical — VC:151). One control needing three local overrides IS the evidence for a producer-owned failure-plate action variant; dispositioning them as three patches ratifies the per-instance-override habit edict 5 bans. → **X-W10** (role decision) + **X-W7** (admin sites) + **GLASS-RELAY** (Button variant); the boundary site rides the successor.
- **EB-11 · MAJOR — the plate that owns the whole `<main>` contains zero headings, and the sole e2e asserts the opposite of its own comment** (V-4 ≡ C-7; both readers). :24/:27 are `<p>`; no heading exists in the file; a11y-authed-admin.spec.ts:139-140 claims "NAMED (a heading a screen reader reads…)" while the assertion beneath (:143-144) is `textContent.length > 0` — fully satisfiable while the glyphs are 100% occluded, so the gate is blind to EB-1 too. All seven green-surviving mutations check out against the spec text; zero unit tests repo-wide. → heading decision **X-W5** (its one-H1-per-route hard gate must cover the caught scene or the gate has a blind arm); gate re-hardening **X-W1**.
- **EB-12 · MAJOR — the copy is wrong for the failure it is most often shown for: "This panel" understates an app-wide blast radius, the machine truth is unfiltered developer prose (unbounded, no clamp/disclosure/LTR isolation), and "Try again" is the wrong verb for a stale chunk** (D2-5, + the V-6 residue after K-4: unfiltered `err.message` as body copy is this row's second limb). App.vue:50 passes the message around the ENTIRE grid; :61 is unbounded; :27-29 center-set `text-mono-small` with no clamp (VC:154 requires LTR-isolated provenance). → successor surface (**X-W5 rider** / **X-W7** body); the reload affordance ties to EB-4.
- **EB-13 · MAJOR — an unratified nineteenth composition: no protagonist, no region ratio, no Card decision, no tier, no boundary/reserve inventory, no heading decision; 25% of the file is design prose** (D2-7; reader-DU CONFIRMED, reader-LC's roster consistent). VC:56 names the ratification list (including "unsupported/corrupt storage recovery") and the seven separately-measured properties; VC:28 caps empty secondary content at ≤15% of the stage — the plate is `h-full w-full` at ~87% of viewport carrying ~6.4% ink (magnitudes U-2). → ratification act **X-W10**; the composition lands wherever the successor lands (**X-W7** body). This row is the ROOT of EB-1/EB-5/EB-8/EB-24 (reader-DU's F1 family, adopted).
- **EB-14 · MAJOR — a live directory-level dependency cycle through the boot root, in the exact region whose ESLint guard is dead** (L2-3 ≡ L-5; both readers; every fact re-run by this seat). `ls demo/@` → ENOENT while eslint.config.js:235-238/:275-276 glob it and :286's G-DEMO-1 sits inside a dead object; three live upward edges (aurora-harmony-stops.ts:23, aurora-bracket.test.ts:14, ColorPicker.vue:129) close a cycle against App.vue:164's downward `import { ColorPicker } from "../picker"`; `demo/platform/` exists as the landing stratum. → dead-glob carve + boundary ban **X-W8** (W8.md:42-43 owns both by name); the two-file platform re-home is **NO-WAVE-OWNER in X** (named residue so the cycle cut is not silently half-done).
- **EB-15 · MAJOR — the demo's TypeScript view of the published surface is misdeclared: 3 of 8 `paths` targets do not exist, `./value`+`./css` (10 live imports) undeclared, exports = 7 keys with no root, the block's own "CLOSED 8-key set" comment false** (L2-4; both readers; re-verified: `dist/index.d.ts` missing, no `parsing.*`/`units.*` in dist/subpaths). The r2 cure's premise is killed (K-3 — `test/dist/` is ENOENT since `6d6d3521`); cure re-specified: generate `paths` from `exports` as vite.config.ts:28/:41 already does runtime-side. → **ONE identity with the standing W-HYGIENE config-parity row** (ColorSpaceSelector-adj L-14; EmptyState-adj ES-18) — cross-booked, never re-minted. **NO-WAVE-OWNER in X** (named residue; nearest mechanism X-W9.f's config carve, unowned by its text).
- **EB-16 · MAJOR — the third failure state has no plate: an unknown route silently renders the picker** (L2-7 as RESCOPED at R-2 + reader-LC missed M4 folded). ONE live mechanism: router/index.ts:36-37's catch-all redirect (verified verbatim). The `componentFor` tail (:94) is provably unreachable dead code contradicting its own :80 doc comment; narrowing the parameter from `string | null` to `LeftPane | RightPane` lets TS prove exhaustiveness and deletes it with zero behaviour change. The visual matrix's `notfound-redirect.png` records the consequence as clean — a correct render of the wrong thing. → **X-W3** (items 15/16 own the not-found route + the fail-closed tail by name; `NotFoundPane.vue` already in its create list).
- **EB-17 · MAJOR — `demo/ui/` is a 19-directory back-compat alias layer (edict 2); its 231 KB cost claim stays REFUTED; two of the nineteen barrels are outright dead** (L2-6 ≡ L-4 + reader-LC missed M2 folded; my census: 19 dirs; `demo/ui/button/index.ts` = one line; label + switch importers = 0 repo-wide). The production-cost refutation (15,665 B byte-identical both ways; 231 KB = dev prebundle) is carried WITH the finding and is U-9 (re-run before citing). → **X-W8** (owns the 19-barrel deletion + 92-import re-point by name; cross-booked with the standing W-HYGIENE extirpation identity).

**MINOR**

- **EB-18 · MINOR — the entire declared public surface is dead**: `emit("reset")` has no listener (my grep: no `@reset`/`onReset` in App.vue), `retryLabel` is never passed, and App.vue:50 re-passes the `message` default byte-identically (C-5 ≡ L-7 ≡ C-15 ≡ D-14). → dies with the successor (**X-W5 rider**).
- **EB-19 · MINOR — `vj-error-boundary` is a dead hook class** — one occurrence repo-wide, its own declaration; the repo convention is `data-slot` (C-8 ≡ L-9 ≡ D-12). → dies with the successor.
- **EB-20 · MINOR — `.plate-ink` ×5 byte-identical scoped rules / 12 `var(--ink-muted, var(--muted-foreground))` expression sites across 9 files — two quantities, both now pinned** (C-12, the corrected count, + K-5's re-count; the fallback arm is unreachable while useAtmosphereBoot.ts:100-106 stamps `--ink-muted` immediate at App setup, so it can only mask a boot regression). → plate-side dedup **X-W7** (ONE identity with EmptyState-adj ES-13 — cross-booked); the token's static declaration + mask retirement keep ES-13's **NO-WAVE-OWNER** residue status (candidate X-W10).
- **EB-21 · MINOR — up to four independently-owned `role="alert"` publishers and the sole spec binds `.first()` by role-and-hope** (C-11; the e2e-failure half correctly hypothesis — U-7). status-lamp.ts:53 mints `role:"alert"` in `<nav>`, which precedes `<main>`. → **X-W1** (bind by stable test id).
- **EB-22 · MINOR — the live region is created in the same commit as its content; the announcement that occurs is attributable to the focus move, not the WCAG 4.1.3 mechanism the header claims** (C-9). Before the throw the component renders only the comment + slot — no `[aria-live]` node exists for AT to observe. Safari/VoiceOver reliability = U-10. → successor surface.
- **EB-23 · MINOR — the machine-truth detail line is unbounded in height and can push the sole recovery control out of the box** (C-10; `max-w-[46ch]` bounds width only; the container has no overflow declaration). → successor surface; shares EB-12's clamp/disclosure cure.
- **EB-24 · MINOR — the plate has no motion at all: an unmanaged full-slab jump-cut in a fully choreographed app; PRM satisfied only vacuously** (D2-9; no `<Transition>` anywhere in the file — my read; VC:141 verbatim "No full-slab remount hole"; MT-F023's animations.css guard caveat carried; motion-quarantine: zero ErrorBoundary rows, nothing blocks this). → **X-W5** (its hard gate requires every pane swap to carry defined PRM-guarded motion — the caught/recovered swap is a pane swap).
- **EB-25 · MINOR — a non-`Error` throw renders the literal `[object Object]` as the only surviving diagnostic** (V-2; :61 `String(err)`; reachability of the throw shape correctly a hypothesis; the rendering behaviour measured). → successor surface, cured with EB-36's guarded derivation.
- **EB-26 · MINOR — the demo component-test home exists, is wired into `npm test`, and this core-shell component is absent from it** (L2-9 ≡ L-11; vitest.config.ts:21 + exactly 3 suites under demo/test — my find). The corpus's 9 passing jsdom probes are ready `demo/test/shell/` seeds. → **X-W1** (with the seeds named).
- **EB-27 · MINOR — a shell-level failure composition filed in the Vite boot root, in a tree where four directory names carry two distinctions** (D2-11 ≡ L2-10 ≡ L-6; `demo/ui` vs `demo/shared/ui`, `demo/color-picker` vs `demo/picker` — all verified on disk). → the file's re-home rides the successor (**X-W5 rider**); the `demo/app` rename is **NO-WAVE-OWNER** (named residue; nearest X-W8's graph subtraction).
- **EB-28 · MINOR (demoted at R-6) — `text-destructive/80` is the repo's twice-retired post-hoc-opacity idiom on the one non-textual failure register, at exactly 2 sites — one family row** (reader-DU missed find). → **X-W7** (both sites live in the plate register).
- **EB-29 · MINOR — the failure STATEMENT crosses the closed type matrix by the same mechanism as the button label: `font-display` forcing Fraunces onto `text-heading` (Plus Jakarta's rung, VC:73), 4 live sites** (reader-DU missed find). → **ONE identity with EmptyState-adj ES-7 (X-W10 + X-W7), which already enumerates the ErrorBoundary site — cross-booked, no new row minted.**
- **EB-30 · MINOR — the `<main>` landmark's accessible name goes stale on catch: `aria-label="Color tool panes"` (App.vue:47) is static markup naming a scene that no longer exists — landmark navigation and heading navigation (EB-11) are BOTH dead ends** (reader-DU missed find; verified). → **X-W5** (the "scene the shell can state out loud" jurisdiction).
- **EB-31 · MINOR — recovery is silent: assertive on failure, nothing on success — a one-directional status contract under a header claiming WCAG 4.1.3** (reader-DU missed find; `reset()` removes the region with no announcement; under EB-2/EB-4 the no-op retry is the common case, so the user cannot distinguish recovery from failure). → successor surface a11y contract.
- **EB-32 · MINOR (demoted + corrected at R-4) — one catch permanently disarms `useDevicePixelSnap`'s ResizeObserver/MutationObserver channels for the session (observers bound to dead nodes; `onMounted` never re-runs); the settle-listener and window-resize channels survive via the live ref** (reader-LC missed M1, mechanism corrected). → rides EB-2's transposition (**X-W5 rider** — App.vue/PaneSlot owned; re-wire observation to the element's lifetime, not App's).
- **EB-33 · MINOR — dead machinery in two config files preserves a `test/dist/` gate directory deleted at `6d6d3521`**: .gitignore:18-24's carve-out negations and vitest.config.ts:22-27's exclude filter (which now silently un-excludes any future `dist` under `test/**`) — the exact hygiene invariant that would have caught EB-15's drift (reader-LC missed M3; verified). → **NO-WAVE-OWNER in X** (named residue; nearest X-W8.b/X-W9.f config carves, unowned by their text).
- **EB-34 · MINOR — `PaneSlot` names two things in one directory: the component every cure routes containment through, and the exported interface usePaneRouter.ts:61-65** (reader-LC missed M5; verified). Rename the interface (`ResolvedPane`) before the transposition lands. → **X-W5 rider** (both files W5-owned).

**INFO**

- **EB-35 · INFO — the boundary cannot catch bare promise rejections (the API-bound failure class), and the header prose implies otherwise** — :4 scopes honestly to render/lifecycle; :2-3's "NEVER a silent white-screen" is a different promise (C-13). Cure = EB-3's `unhandledrejection` net; folds there.
- **EB-36 · INFO — Vue invokes `onErrorCaptured` hooks UNGUARDED (runtime-core :238-241, raw call — contrast `callWithErrorHandling` for ordinary hooks), so a throw inside the hook escapes the framework's net entirely; `String(err)` at :61 can itself throw for null-prototype/hostile objects** (reader-LC missed M6; invocation site verified by this seat). Cure: one try/catch around the detail derivation in the successor.

## Killed-claims register (refutations carried with receipts)

- **K-1 — L2-2 "there is no boot module, so error reporting has nowhere to live" (BLOCKER) — DEAD AGAINST THE LIVE TREE** (both readers; ratified with my own reads). `demo/color-picker/main.ts` EXISTS (7 lines, the only `createApp` in the repo); index.html:205 is verbatim `<script type="module" src="./main.ts"></script>`; landed at `c4af0ef9` 2026-07-29 13:50 — one day AFTER the r2 reports, which honestly recorded the branch drifting past their HEAD. The prescribed cure is already fully shipped; X-W2 even cites `c4af0ef9` as its template. The residual (nothing installed on the app config) is EB-3, MAJOR, ~5 lines in an existing file — not an "enabling condition", not a BLOCKER. A wave author must not be told to create a file that is already there.
- **K-2 — D2-3 "the boundary hand-rolls a primitive Vue already ships" — REFUTED AS FRAMED** (R-1; reader-LC's kill ratified + this seat's dist derivation). `errorComponent` and `onErrorCaptured` cover disjoint classes; the async primitive cannot replace the boundary for the render-throw class the sole e2e induces. Residue booked EB-4 with the arbiter's cure-lock.
- **K-3 — L2-4's cure premise "add a test/dist/ gate — that directory already exists" — REFUTED**: `ls test/dist` → ENOENT; pruned at `6d6d3521`. The finding stands (EB-15); the cure home is re-specified; the surviving dead machinery is EB-33.
- **K-4 — V-6 "the detail line publishes absolute developer filesystem paths" (production disclosure) — REFUTED FOR THE SHIPPED BUILD**: `/@fs/` is Vite's dev-server prefix; usePaneRouter's loaders are relative specifiers the production build rewrites to hashed asset URLs — no path, no username. Downgraded to the unfiltered-`err.message` limb already inside EB-12; not an independent row.
- **K-5 — D2-8/D-11 "eleven declarations across seven SFCs" — REFUTED BY COUNT** (re-run by this seat): 12 expression sites across 9 files; the distinct `.plate-ink` RULE count is 5. Two quantities conflated under one number; both now pinned in EB-20.
- **K-6 — the L-1/L2-1 cure markup (`PaneErrorBoundary` as KeepAlive's direct child, `:key="liveKey"` "the whole fix") — STRUCTURALLY UNSOUND** (R-7): KeepAlive deactivates rather than destroys, so the cached boundary restores `caught === true` on return — the latch reborn at pane granularity. The defect (EB-2) is untouched; the prescription is dead. Boundary outside KeepAlive, or seat-rendered fallback.
- **K-7 — the D2-4 correction paragraph ("my network trace shows the loader IS re-invoked… r1's module-map claim should not be carried forward") AND reader-DU's counter-kill of it — BOTH INADMISSIBLE** (R-3): evidence-r2/R2-retry.json asserts both directions of the mechanism in one file (a retry-phase request entry with a fresh `?t=` vs `networkAttempts.retry: 0` + the module-map note). Neither the withdrawal nor the counter-kill may be cited; the question is U-3. The user-visible defect stands regardless.
- **K-8 — D2-4a's canon citation (VC §5.1 focus-restoration) — DOES NOT REACH**: VC:115 is a row of the overlay table, scoped to Dialog/Drawer/Popover open/close; the boundary is neither. The behaviour survives in EB-6 on the ARIA pattern argument; a gate bound to the §5.1 citation would not hold.
- **K-9 — D2-2's mechanism citation "Appendix E: inline content paints at step 5" — WRONG STEP NUMBER**: Appendix E.2 step 5 is non-positioned floats; in-flow inline content of non-positioned descendants is step 7, positioned z-auto step 8 (challenge-C:239 has it right; the r2 D file GOVERNS its axis, so the correction is recorded here to stop the error propagating into a wave spec). The "step 5" figure is the §9.9.1 seven-layer list's numbering, mis-attributed to Appendix E. Conclusion unaffected — the three-arm experiment settles causation independently.
- **K-10 — D2-1's "No token, no alpha and no `@media` arm can close a contrast whose ground is unbounded user input" — SELF-UNDERCUTTING OVER-CLAIM**: the ground is unbounded solely because :18 declares no ground; a single material declaration bounds it — which is exactly what the same finding's cure prescribes. Requirement kept ("the plate must acquire a tier", EB-5); absolute dropped, so the cheapest structurally correct cure is not rejected by a literal reading.
- **K-11 — the state-coverage claim "the `v-if` at :27 is effectively dead: an `Error` always yields a non-empty message" — FALSE ON BOTH LIMBS**: `new Error()` / `new Error("")` yield `message === ""` (the own property is installed only for a non-undefined argument; the prototype default is `""`), and `throw ""` gives `String("") === ""`. The guard is live, correct code; filing it as edict-2 dead surface would push a wave author to delete a working guard.
- **K-12 — L2-7's second mechanism ("`componentFor`'s tail is a live masking fallback") — REFUTED** (R-2): the tail is provably unreachable through both typed call sites and contradicts its own :80 doc comment. It is dead code cured by signature narrowing (folded into EB-16), not a masking fallback; the router catch-all alone causes the observed symptom.
- **K-13 — reader-LC missed M7 "dev fragment root silently disables attribute fallthrough" — REFUTED, arbiter-original** (R-5): `filterSingleRoot` (runtime-core :4669-4690) skips Comment vnodes and `getChildRoot` re-targets fallthrough at the single element child; the warning requires multiple non-comment roots. No row minted.
- **K-14 — reader-LC missed M1's MAJOR framing ("the corner-AA cure is silently dead for the rest of the session" + "a real retention leak") — DEMOTED, arbiter-original** (R-4): `sync()` re-reads the live template ref and the document/window-level settle channels revive the snap after reset; only the RO/MO channels disarm permanently; the retention limb is unproven under weak-target observer semantics. Survives as EB-32, MINOR, mechanism corrected.

## Superlatives — preserved with provenance

1. **Causal identification, not inference** (reader-DU's method, adopted by both re-readers): evidence-r2/R2-paint.json runs three arms with the SHIPPED arm — zero page mutation — carrying the finding, and two mutually exclusive interventions each restoring the ink in the same rect; independently corroborated by a different metric in a different axis (challenge-C's distinct-colour census: 86/42 = gradient banding vs 1,323/499 = antialiased type). Two seats, two metrics, one conclusion. The best evidence work in this corpus.
2. **The source records the exact premise that produced the defect, and the corpus found what it missed**: shell.css:16-18 reasons "Occlusion of the dock by any card is impossible by construction — grid rows do not overlap and no z-index is minted anywhere in the shell" (verified verbatim) — true for cards and rows, silent about an `absolute inset-0` sibling; the control case (`.dock-band{position:relative}`) sits 35 lines above the defect.
3. **Both r2 seats refuted themselves in-band with receipts** — the E-1 behaviour the edict exists to produce: challenge-L kills its own r1's 231 KB cost claim with a real-bundler build ("a wave spec that carries it will be refuted at the gate"); challenge-C narrows `.plate-ink` 7→5 (verified: exactly 5) and replaces "the focused container is unstyled" with the sharper UA-ring-not-house-ring claim. Self-correction that makes findings sharper, not softer.
4. **Every canon and producer citation this seat spot-checked is verbatim and correctly located**: VC:19/:21/:28/:56/:73-78/:141/:151-154 read exactly as quoted; glass-ui 7.0.0 = 74 export keys, no `./alert`, `./surface`+`./card`+`./button` present; runtime-core early-returns on a `false` hook result exactly as cited. (The two citation errors that existed — the §5.1 reach and the Appendix E step — were caught by the re-readers and are K-8/K-9.)
5. **The component is genuinely well-made in the small, recorded as positive evidence**: `useTemplateRef` (:57), reactive props destructure with defaults (:43-51) — more current than the `withDefaults` in the atom it copies — typed `defineEmits` (:53); the `nextTick` callback `?.`-guarded and self-terminating; no timers, listeners, observers, or rAF, so the PRM-rAF epidemic does not touch this file; a three-edge import graph (`vue` / `@lucide/vue` / `../ui/button`) a real external consumer could write verbatim; and `return false` is the semantically correct call for an owning boundary — the defect is what it does instead of reporting.
6. **A real positive with provenance on a scarce surface**: the recovery control measures 159.2×54 CSS px at 390×844 with accessible name "Try again", against a whole-app matrix reporting `smallTapTargets` on all 60 captures — this control is not among them (R2-measurements `mobileTap`).
7. **PaneSlot.vue:78-108 is the discipline its blast-radius sibling lacks**: the deferred-commit rAF cancels at the watcher head AND on `onBeforeUnmount`, with the measured rationale in-file — the exact two axes `useDevicePixelSnap` gets wrong (EB-32).
8. **Single-source stamping done right, beside the drift it indicts**: useAtmosphereBoot.ts:87-105 stamps `--accent-live` + `--ink-muted` from ONE `useContrastSafeColor` instance with `{immediate:true}` and publishes the referent itself; vite.config.ts:28/:41 GENERATES the runtime alias set from the exports map "so the alias set can never drift" — the precise virtue whose absence on the TS half is EB-15.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs — task #4 feed)

- **U-1** — every contrast magnitude in the corpus: the four-seed table (statement 1.89-7.01, glyph 1.10-1.96 — measured with the plate runtime-LIFTED, i.e. describing the post-EB-1 state, never filable as a shipped contrast failure), the 3.70:1 detail reading, the 1.12/1.06 shipped figures. Mechanisms statically certain (EB-5); numbers live-only.
- **U-2** — the occlusion pixel counts (0/16,020 · 0/784 · minLuma 128 · 3,089/3,016 restored) and the 87.3%-viewport/6.4%-ink proportion figures. Mechanism proven from source; counts need a browser.
- **U-3** — the retry mechanism (module-map cached rejection vs loader re-fetch): the governing artifact is internally contradictory (K-7) and the jsdom harness disagrees with the live note. ONE live re-measure, with the network log keyed on URL-without-query, before any wave binds a cure to either mechanism. The defect (retry does not recover) holds on all readings.
- **U-4** — the focus/caret theft sequences: V-3's `selectionCollapsedInBoundary: true` out of a live text field; D2-4a's `activeIsButton: false` re-theft on failed retry. Code shape verified (unconditional :64); exact sequences live-only.
- **U-5** — L2-1's containment census (1,695/1,535 = 90.6%) and the teleport-host enumeration. Structural half verified from App.vue; percentage live-only.
- **U-6** — C-14 WebGL2 context churn on repeated reset — self-declared hypothesis; only the absence of `loseContext` handling is confirmed.
- **U-7** — C-11's e2e half: whether the dock lamp actually wins `.first()` on the e2e origin (playwright.config sets `VITE_API_URL`, so the lamp is likely absent there). Duplicate-role fact and DOM ordering confirmed; consequence not.
- **U-8** — "the only action that cures a stale-chunk failure is a page reload": standard, but the corpus's own `afterReload: {boundaryUp: true}` arm ran under a persistent route-abort and decides nothing. Verify before the successor's reload affordance is gated on it.
- **U-9** — the L2-6 cost REFUTATION (15,665 B byte-identical, 76 vs 13 modules): UNPROVEN-NEEDS-BUILD — re-run both `vite build` arms before a wave cites either number. Refutation direction is the safe one (retires a claim).
- **U-10** — C-9's Safari/VoiceOver announcement-reliability claim for same-commit live regions — the engine this tranche's matrix audits; needs an AT pass, not a DOM probe.

## Closing verdict

**DEFECTIVE — ADJUDICATED.** Two BLOCKERs survive hostile re-reading and this seat's re-derivation: the caught plate paints nothing but its button (EB-1), and one catch latches the entire application dead for the session while the shell keeps reporting a scene that is not there (EB-2) — together, the file's own header claim inverted. 36 rows confirmed (2 BLOCKER / 15 MAJOR / 17 MINOR / 2 INFO), 14 claims killed with receipts — including one BLOCKER killed against the live tree (the boot module the corpus said could not exist has existed since `c4af0ef9`) and one prescribed cure killed for structural unsoundness (the KeepAlive-cached boundary). No X wave names this component: the cure surfaces are owned (X-W5 files, X-W3 not-found, X-W7 plate register, X-W8 shims/globs, X-W10 canon, X-W1 gates, GLASS-RELAY via X-W9), but the boundary successor itself is **named cross-surface residue for the next formation boundary** — carried here so it cannot be silently dropped, with R-1's cure-lock and R-7's KeepAlive constraint binding on whoever picks it up. Dispositions are ADJUDICATED, not VERIFIED; the 10 U-rows are the visual-audit feed.

— end of apotheosis. The challenger records, both re-reader returns, and all dated evidence remain immutable beside this file per E-1/E-3.
