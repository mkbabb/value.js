claude-fable-5 (served model id)

# fr-EquationView — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-10 · **Subject**: `fourier-analysis/web/src/components/equation/EquationView.vue` (469 lines, DIRTY in the WT) + its first-party closure (`FunctionInput`, `EquationResult`, `EquationModeToggle`, `NotationPills`, `InfoCard` (dirty), `composables/{useEquationCache,useCoeffHover}`, `lib/equation/{api,types,notation}.ts`, `lib/{api,api-problem,colors}.ts`) and the wire peers (`api/{models,routers}/equations.py`, `api/services/{computation,rate_limiter}.py`, `api/main.py`, `src/fourier_analysis/symbolic/latex_rendering.py`) — the `/equation` route shell, census row lane-frontend:130.
**Substrate**: fourier `cd26c65` + dirty WT · installed glass-ui **4.0.0** · installed value.js **0.13.0** (exports = `['.']`, single root — seat node read) · installed keyframes.js **4.3.0** (hard dep `value.js ^0.13.0`, optionalDep `glass-ui ~4.0.0` — seat node read) · glass-ui producer **8.0.0 TODAY** (seat re-pin; tags v7.0.0 AND v8.0.0 both present) — one major past the 7.0.0 the D axis audited (fifth occurrence of the drift pattern; fr-EditorControlsDock L-7 / fr-ConvergencePlot SR-1).
**Corpus**: three axis challenges (`audit/fourier-components/EquationView/challenge-{D-design 29+5, L-library 25+5, C-consumption 37+6 (v2, v1 folded whole)}.md`, all read whole by this seat) × two hostile re-reader returns (**R-D** design-and-usage · **R-L** library-and-code), delivered as separable returns — so this record carries a ruled reader-vs-reader table.
**Method (M-23/L-18)**: every reader-vs-reader verdict conflict re-derived against the tree with this seat's own instruments before ruling. **One R-D confirmation struck as FALSE** (the 429 backoff falsifier — SC-1) and **one R-L kill struck as FALSE** (the C·D-16 byte-receipt kill — SC-2): each reader failed exactly where the other was strong, and this record is the join. One fold neither reader drew was caught by the identity guard (C·C-30 = banked fr-ConvergencePlot C-19 — SC-4). Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node; zero browser — probe parsimony; product source READ-ONLY; this file is the sole write)**: EquationView.vue read WHOLE (469: writer sets, both catch arms, whole template, both style blocks); EquationModeToggle.vue WHOLE (80: `overflow: hidden` :37, `height: 2.25rem` :47, `is-active` color-only :63-66); useEquationCache.ts WHOLE (two keys :7-8, keyless `CachedResult` :19-23, four wrapped accessors); useCoeffHover.ts WHOLE (dead `notation` :22, `amber || golden` :65, `\color{}` :79/:87/:92, `trust:true` :100, `>= 6`-after-push :81 vs `> 6` :89/:94); EquationResult.vue (useClipboard header :12-15, `trust:true`, unescaped `<code>` catch, `.eq-scroll-region` `padding: 2rem 1rem 1rem` / `overflow-x: auto` only, `.copy-pos` right 0.5rem z-controls); FunctionInput.vue spots (`parseDomainValue` no-ordering, Harmonics `:max="100"` + autoHarmonics-off-on-drag, Display-terms clamp-on-READ `:model-value="Math.min(budget, vizHarmonics ?? nHarmonics)"` vs unclamped WRITE `budget = $event`, nameless Wand2 `variant="glass" size="icon"`, Compute no `:disabled`); NotationPills (no aria-pressed); **api/services/rate_limiter.py read WHOLE — the RD-1 decider** (`check()` records-then-raises; `snapshot()` `reset = ceil(oldest + 60 − now)`; `RateLimitHeaderMiddleware.dispatch` = check → `call_next` → `_stamp` on EVERY non-OPTIONS response; `_limiter_for_path` routes `/api/equations/{compute,simplify}` → `compute_limiter`) + api/main.py:67 registration + computation.py:34-40 (30 s semaphore wait → 429) + lib/api.ts (`MAX_RATE_LIMIT_RESET_SECONDS = 30`, `waitSec = Math.min(reset ?? 2**attempt, 30)`, `retryOn429` on CoreFetchOptions only, ApiFetchOptions omits it); **installed dist token extraction — the RD-2 decider**: color-radius.css:263-267 (bare `oklch()` + `var(--section-color-4/5)`) AND light-dark.css:130-131/:145-147 (`light-dark(oklch(),oklch())` for section-4/5 + the viz trio, **verbatim as C quoted**, inside the `@supports (color: light-dark(white, black))` block at :71) AND dark-arm.css:113-115 — plus dark-arm.css:174-182's "PLAIN-hsl LITERALS … NEVER light-dark()" comment read in context: it governs the CONSTELLATION token set, not viz; light-dark.css:99 `--popover: light-dark(hsl(36 48% 97%), hsl(24 8% 16%))`, color-radius.css:74 `--popover: var(--card)`, dark-arm.css:65; a11y-overrides.css:6-31 (universal PRM reset + the `[data-allow-motion]` override arm) and :115-121 (coarse floor keyed `[data-size="icon"]`, "min-block/inline-size only RAISES"); button CVA base extracted (`"btn-pill tap-squish focus-ring whitespace-nowrap …"`), base.css:174-178 (`.focus-ring:focus-visible { outline: none; … box-shadow: var(--focus-ring-shadow) }`), scale-paper.css:65-68 (2px outset-only ring); scheme-motion.css z-ladder verbatim (`--z-hovercard: 120 · --z-modal: 140`); dist/tabs.js (`role: V.value ? "tablist" : "group"`, trigger `"tab"`, `grep -c keydown` → **0**); `dist/composables/dom/` listing + useResolveTokenColor.d.ts and useTokenColor.d.ts read (docs verbatim) + useClipboard.d.ts present; glass-ui@4.0.0 peer `value.js "^0.10.0 || ^0.11.0"` optional (node read); producer tag reads: v7.0.0 exports = **74 keys at the tag** (`./hover-popover` FALSE · `./hover-card` FALSE · `./popover`/`./fading-scroll`/`./dom`/`./metric`/`./tabs`/`./button` TRUE · `./metric-badge` FALSE), Button.vue (ButtonEmphasis/ButtonSize-no-icon/ButtonProps + `glassMaterial` computed), metric/types.ts (MetricValueProps {value,unit,placeholder,loading} → MetricProps — **no `color`, no `tone`**), `grep -c emphasis MIGRATION.md` → **0**, tap-toggle sentence at MIGRATION.md:866; api/models/equations.py `grep -n budget` → **:23 and :40 both `ge=2, le=50`** (readers' pins EXACT; this seat's own first sed-arithmetic was wrong and corrected by grep — recorded), SimplifyRequest :38-41 `coefficients` NO max_length, SimplifyResponse :44-47 three fields; routers/equations.py :91/:96/:111/:114 (`simplify_series(terms, req.budget, req.notation)`; `render_latex_sigma(terms, req.notation)`); visualization.py:186/:192 (the sibling contract's array caps); simplification.py `compute_effective_n(threshold=0.9999, minimum=3)` returns `minimum` even at `total_energy <= 0`; latex_rendering.py :159/:175-176/:216/**:248** (`\htmlClass{eq-coeff …}` incl. the `eq-An` site R-D's cite missed); style.css:112-127 (D.W4.d `--viz-amber`/`--section-color-5` overrides, light+dark arms); App.vue:9-18 (resolveVizColors + class MutationObserver); tsconfig `strict: true`, no `noUncheckedIndexedAccess`; router/index.ts:92-94 lazy route; e2e: 8 specs, `grep -rn equation web/e2e` → 1 hit (visual-baseline.spec.ts:34); keyframes consumption grep → 2 hits, both outside the closure; ConvergencePlot.vue :255-258 (`renderKatexInline` NO trust; catch returns raw latex), :263-264, :323-327; lucide info.js :10-14 (byte-identity with the inline SVG); **B-2 recurrence re-derived from the shipped defaults**: `budget(v) = v − 10` on a monotone up-drag (20→10 … 61→**51**, 100→90; a single track-click jump from defaults yields exactly 50); **D-B1 geometry re-derived**: coarse `--control-h-md` = max(2.5rem×1.5, 2.75rem) = 60px ⇒ copy 8→68 / info 52→112 ⇒ **16px coarse overlap, z-30 over z-20**; fine 40px ⇒ copy 8→48 / info 52→92 ⇒ **NO fine horizontal overlap (4px gap)**; vertical incursion vs the 32px `padding-top`: 16px fine / 36px coarse; registry sweeps: `computeKey|resolveTokenColor|useTokenColor` over adjudicated/*.md → **0** (both R-L missed rows genuinely new); fr-BasisCanvas D-1 + fr-App B-1 read (BOTH cite the `light-dark()` arms — the SC-2 corroboration); fr-AdminAuditLog AA-2, fr-AdminFlaggedPanel FR-AFP-2/-18, fr-AdminUserList FR-AUL-7/-16/-19/-24/-43/-55, fr-EditorControlsDock L-7/D-15/D-16/C-18, fr-BasisSelector M-7, fr-AnimationControls C-15/C-28, fr-CoefficientsSpectrum M-5/M-6, fr-ConvergenceLegend D-7/D-L5, fr-ConvergencePlot (read whole: L-M2/M-L1/L-M3+D-26/D-9+C-2/M-D1/C-19/C-20/C-22/L-i2/L-m12/RD-2/RD-3/SR-1), fr-CanvasControlsDock R-9/K-9 + D-2/C-2, fr-CanvasOverlayButton R-6/F8-REACH-01 (+ its v7-tag **74-key** re-measure, independently matching this seat's), kf-ChannelControls D-2/C-3, kf-KfPillTabs (N-1/C-3.2 clip class; :85 aria-controls kill; useTabRovingFocus note), CENSUS-2026-08-03:186-189.

## Scoreboard

**74 rows ADJUDICATED-CONFIRMED (2 BLOCKER booked · 19 MAJOR booked · 5 MAJOR folded to banked identities · 33 MINOR booked incl. 6 missed-row bookings · 4 reader-unattacked MINOR carried at axis grade, flagged · 5 MINOR folded · 6 INFO) · 19-entry killed-claims register — 17 kills RATIFIED on this seat's own instruments, plus one FALSE CONFIRMATION struck against R-D (SC-1) and one FALSE KILL struck against R-L (SC-2) · 11 reader "missed" filings adjudicated: 3 new MAJOR + 6 new MINOR + 2 INFO banked · 1 seat-caught identity fold neither reader drew (SC-4) · 11 superlatives ratified.**

The two surviving BLOCKERs are one shape — a control wired to an operation that cannot produce what the surface reads: **B-1** (the Notation control is inert in the DEFAULT sigma mode: `/simplify` has no `latex_sigma` field on either side of the wire, `displayLatexSigma`'s only writers are the cache init and `doCompute`, and the `:133` memo self-seals every retry — the user's selected notation and the rendered notation diverge permanently until a force-Compute) and **B-2** (one Harmonics drag from the shipped defaults walks `budget` to 51 — past `le=50` on both pydantic models — via the `:151` rescale recurrence `budget(v) = v − 10`, every hop is a verbatim pass-through, the 422 lands in doSimplify's `/* silent */` catch, and the escalated value is sessionStorage-persisted so the NEXT session's first compute 422s cold). The deepest single correction this seat minted: **the 429-retry magnitude that both axes and R-D carried ("1 s + 2 s backoff — no RateLimit-Reset header") is false at the server tree** — `RateLimitHeaderMiddleware` stamps every response including the compute-saturation 429, so the client sleeps `min(reset≈60, 30) = 30 s` per retry: the true worst case is ≈150 s with `computing` pinned true and no client timeout. The finding survives ~20× sharper than filed.

## Ruled reader-vs-reader disagreements (the incredulity seat's actual work)

| id | R-D (design-and-usage) | R-L (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **RD-1 · L·M-4/C·C-34 magnitude** | CONFIRMS the axes' falsifier cells: "the 429 sets no `RateLimit-Reset`; backoff is 1 s + 2 s" | KILLS from the middleware: every response is stamped; reset ≈ 60 ⇒ 30 s waits; ≈150 s worst case | **R-L RATIFIED; R-D's cell is a FALSE CONFIRMATION (SC-1)** | Seat read of rate_limiter.py WHOLE: main.py:67 registers the middleware; `dispatch` = `check()` (records the hit) → `call_next` → `_stamp(RateLimit-Limit/-Remaining/-Reset)` on every non-OPTIONS response — the in-route `HTTPException(429)` from computation.py:37-40 is converted by Starlette's inner exception middleware and returns THROUGH `call_next`, so it IS stamped; `snapshot()` reset = `ceil(oldest + 60 − now)`; `_limiter_for_path` routes both equation POSTs to `compute_limiter`. Client: `waitSec = Math.min(reset ?? 2**attempt, 30)`. The finding is SHARPER, the filed magnitude dies. R-D adopted the axes' claim without opening the one file that decides it — the citation-inheritance lesson, confirmation-flavoured, fourth fleet instance. |
| **RD-2 · C·D-16 byte receipt** | Cites BOTH shipped forms (color-radius bare `oklch()` + light-dark.css:130-131/:145-147 arms) | KILLS C's receipt: "THE QUOTED BYTES DO NOT EXIST … one of the two shipped forms is fabricated" | **R-L's kill STRUCK AS FALSE (SC-2); R-D's cell exact** | Seat extraction: every line C quoted exists VERBATIM in the installed dist — light-dark.css:145-147 (`--viz-fourier: light-dark(oklch(0.579 0.201 30.4), oklch(0.693 0.151 28.1));` …) and :130-131 (section-4/5), inside the `@supports (color: light-dark(white, black))` block at :71, which modern engines pass — so the light-dark form is the WINNING cascade value, with color-radius.css:263-267 as the fallback base. R-L over-generalised dark-arm.css:174-182's "NEVER light-dark()" comment, which governs the CONSTELLATION (Canvas2D-read) token set. Double bank corroboration: fr-BasisCanvas D-1 and fr-App B-1 both cite the `light-dark()` arms. The finding's conclusion was never in doubt (both forms fail all four `cssVarToHex` regexes); the kill of the receipt dies. A kill is a claim like any other. |
| **RD-3 · D·D-B1 fine-pointer arm** | Repeats D's "12px overlap on fine pointers (8+40=48 vs 32)" | Fine = NO horizontal overlap (copy 8→48, info 52→92); the collision is coarse-only; D's fine sentence is its vertical claim | **R-L wins; seat re-derives both arms** | Fine: 40px icons ⇒ spans [8,48] and [52,92] — a 4px GAP, no horizontal overlap. Coarse: 60px ⇒ [8,68] vs [52,112] — 16px overlap, info z-30 over copy z-20. Vertical incursion into the 32px `padding-top` content start: 16px fine / 36px coarse (D's "12px" is wrong arithmetic in either reading). MAJOR stands on the coarse arm (K-7/K-8 below). |
| **RD-4 · C·D-04 severity** | MAJOR (unvalidated parse; guarded object, dereferenced field) | MINOR (threat model = corrupted per-tab sessionStorage record; `-v2` is the schema guard) | **MINOR — R-L wins on banked precedent** | fr-ConvergencePlot RD-3 ruled exactly this threat model MINOR on exactly this file (`useEquationCache.ts`: sessionStorage ×4, localStorage ×0 — seat re-verified). No shipped writer produces a shapeless record. The `:61`-vs-`:72` guard asymmetry books; the grade follows the threat model. |
| **RD-5 · D·D-M2 severity** | MINOR | MAJOR ("the route opens with the equation hidden") | **MINOR — R-D wins** | The artifact is one tab away, auto-computed (`:174` fires with the shipped default), and the fleet's demotion law (fr-CT "a11y-critical, not functionally blocking"; fr-CL "the information survives") governs: a questionable default, not a capability loss. `mobileView` un-persisted (cache fields :10-17) books with it. R-L's MAJOR preserved in DISSENT. |
| **RD-6 · D·D-M14 severity** | MINOR (primitive limb banked ×2; desktop-only) | MAJOR | **MINOR — R-D wins** | The FadingScroll-shipped-at-both-pins identity is banked twice (fr-ConvergenceLegend D-7 — itself seat-DEMOTED to MINOR there — and fr-CoefficientsSpectrum M-6); `:371` scopes the defect desktop-only. The genuine increments (state-blind false affordance; fades to `--background` over a `--card` ground — dark smudge; 40px fade over a 32px `pb-8` runway) book at MINOR. |
| **RD-7 · D·D-M5 severity** | MINOR (orphan limb banked at the F8-REACH-01 standing demand) | MAJOR (+ the computed-pair duplication) | **MINOR — R-D wins; R-L's increment ADOPTED** | fr-CanvasOverlayButton R-6 established InfoCard is **disposed-in-demand** (F8-REACH-01: "exact KEEP_WITH_MOUNT, ISOLATED_HARNESS, or DELETE; HOLD is RED") and un-executed — and the frozen tree is spending edits on it right now (` M`, `:amount=`→`:value=`). The new facts book: template drift (`% energy` vs `% energy captured`; `size-3.5` vs `h-3.5 w-3.5`) AND R-L's computed duplication (EquationView :69-70 ≡ InfoCard :13-14), which doubles D-B3's and D-M10's blast radius at a zero-consumer site. |
| **RD-8 · the v7.0.0 export keyset count** | (not asserted) | "seat-enumerated: 66 keys" at the tag | **74 keys at the tag — seat's own node count, independently matching fr-CanvasOverlayButton's tag re-measure (74)** | R-L's "66" is the banked AA-2 number — measured against a LIVE producer HEAD (`62305f4a`), not the tag — quoted as its own tag enumeration: a citation-inheritance nick (SC-3). Every MEMBERSHIP fact R-L derived from the keyset holds (seat-verified: no hover-popover, no hover-card, no metric-badge; popover/fading-scroll/dom/metric/tabs/button present), so no verdict changes. |

**Seat corrections and strikes:**

- **SC-1 (against R-D — false confirmation, killed).** The 1 s + 2 s backoff cell (RD-1). R-D's return is otherwise the corpus's stronger severity architecture; this is its one inherited cell.
- **SC-2 (against R-L — false kill, struck).** The C·D-16 receipt kill (RD-2). A hostile reader that kills a true byte-quote on a mis-scoped producer comment manufactures doubt against the corpus's strongest evidence class. First kill-of-a-kill in this trio's records.
- **SC-3 (against R-L — count inheritance).** "66 keys" (RD-8). Membership conclusions unaffected.
- **SC-4 (seat's own identity-guard catch — neither reader drew it).** C·C-30 ("`useCurveTransition.ts` hand-rolls `lerp` beside the import") is the banked **fr-ConvergencePlot C-19** verbatim — same file, same lines, same cure (F.W2 rider). R-D confirmed it as a fresh row; R-L did not sweep it. FOLDED, not re-booked; R-D's K5 cure-kill (un-landable at installed 0.13.0 — single-root exports, FR-AUL-55) rides the banked row as its sequencing note.
- **SC-5 (seat method note).** This seat's own first line-arithmetic on api/models/equations.py was wrong (sed reconstruction); `grep -n` settled it at :23/:40 — the readers' pins were EXACT and L's originals one low. Recorded because an adjudicator's instrument errors are evidence too.

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty (subject and InfoCard both dirty); re-resolve before any cure (D-19). Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W2** value.js migration · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5-W8** CRUD/provenance union · **F.W9/W10** coverage · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Identity guards fold by reference, never re-book.

### BLOCKER (2 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **B-1** *(L·B-1 = C·D-05; v1 D-05 promotion ratified)* | The Notation control is inert in the DEFAULT display mode. `:43` `eqMode = "sigma"`; `:49-51` `activeLatex` prefers `displayLatexSigma`, whose only writers are `:39` (cache init) and `:114` (doCompute); `:138` doSimplify writes only `displayLatex`; the notation path `:177-181` routes to doSimplify only; SimplifyResponse = `{latex, energy_captured, term_count}` on BOTH sides (types.ts:42-46; models/equations.py:44-47) while the server proves sigma IS notation-dependent (routers/equations.py:96). Self-sealing: `:140` advances `lastDisplayKey` on success so `:133` suppresses every retry; the energy badge DOES move (`:139`), so the control looks alive. R-L's sharpening adopted: the divergence is observable in one screen — toggle to `expanded` (new notation) and back to `sigma` (old). | **F.W5-W8** — `latex_sigma` joins `SimplifyResponse` on both models (a contract decision, not a patch) + **F.W4** — write it in doSimplify. **CURE BOUND by M-CK below**: the alternative cure ("route notation through doCompute") walks into the under-determined `computeKey` and re-ships B-1 one layer down. |
| **B-2** *(L·B-2 = C·D-01)* | `budget` walks past `le=50` from the shipped defaults in ONE Harmonics drag. Defaults 20/10 (`:28-29`); drag flips `autoHarmonics` off (FunctionInput:186) so `vizHarmonics === nHarmonics` (max 100, :184); the `:151-159` watcher's second branch yields `budget(v) = v − 10` on a monotone up-drag (seat-re-derived: 20→10, 30→20, 61→**51**, 100→90; a single track-click jump from defaults yields exactly 50 — R-D's caveat, adopted); `:110`/`:137` and equation/api.ts pass it verbatim; both pydantic bounds `ge=2, le=50` (models :23/:40). Through doSimplify the 422 lands in `/* silent */` (`:142-144`); through doCompute it renders as C·D-02's empty-or-useless banner. Third entry: `budget` restores from sessionStorage (`:29`), so the NEXT session's first compute 422s before the user touches anything. | **F.W4** — clamp at the two request sites or the ref (NOT at the slider: the restore path never passes through it — the M-BR rider below) + **F.W5-W8** — one shared bound constant across the seam (the R6-8-correct form; C §8 repair 1's contract half). |

### MAJOR (19 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **L·M-4 + C·C-34** *(RD-1 re-magnituded)* | `retryOn429` is declared on `CoreFetchOptions` (:101) and absent from `ApiFetchOptions` (:205-212); `apiFetch` is the only exported wrapper, so the equation calls cannot opt out even in principle; `:162` defaults it TRUE with `MAX_RATE_LIMIT_RETRIES = 2`. The compute-saturation 429 is raised only AFTER a 30 s semaphore wait (computation.py:34-40) — a backpressure signal — and (seat correction) arrives STAMPED with `RateLimit-Reset ≈ 60` by the middleware, so each retry sleeps `min(60, 30) = 30 s`. Worst case ≈ 2×30 s client sleep + 3×30 s server wait ≈ **150 s** with `computing` pinned true, no client `AbortSignal.timeout`, and the spinner reading "Computing…" throughout — auto-amplified against the endpoint that just asked for relief. | **F.W4** — surface `retryOn429` on `ApiFetchOptions`, pass `false` for both equation ops, add a client timeout posture. |
| **D·D-B4 = L·M-5 = C·D-07** | The unified busy state was designed and never wired: `loading` (`:57`) has ZERO consumers (sole `simplifying` reader; template reads `computing`/`error`/`result` only — whole-template read), and doSimplify's failure path is a terminal no-op (`:142-144` `/* silent */`) — opposite error postures at two async seams 40 lines apart, and the silent one is the seam the user drives most (every notation click, every budget drag). B-2 supplies a one-drag 422 through exactly this seam; no toast surface exists in the subtree (seat grep). | **F.W4** — wire `loading` (dim + `aria-busy`) and give doSimplify doCompute's banner. **F.W1 benefit rider**: 7.0.0 `Metric` ships `loading` + `aria-busy` (seat-read at the tag). |
| **C·D-02** | The server's only field-level diagnostic is destructured away before any consumer can reach it: api-problem.ts:37 names `detail` in the destructure (removing it from `...extensions`), then `typeof detail === "string"` drops FastAPI's array-shaped `{"detail":[{loc,msg,type}]}`; `title` falls back to `response.statusText` (`""` over HTTP/2 ⇒ `error = ""` ⇒ both banners' truthiness gates fail — silence); `:123` reads `.message` only. main.py has ONE `@app.exception_handler(Exception)`; no problem+json on the equations router's own 422/504/500. R-L's narrowing adopted: the middleware's own rate-limit 429 IS problem+json (`rate_limited()`), so that one arm carries a non-empty title. | **F.W4** — stop discarding array `detail`; non-empty fallback at `:123` + **F.W5-W8** — the problem+json envelope on `/api/equations/*`. |
| **L·M-3 = C·C-29** *(C's BLOCKER demoted — K-16)* | Two independent sessionStorage records, two write cadences, no shared key: the input record rewrites on every keystroke (`:161-171`, un-debounced, `expression` v-model'd to a text input); the result record writes only on success (`:120`/`:141`); `CachedResult` carries NO compute key (:19-23). Restore seeds `lastComputeKey`/`lastDisplayKey` from the NEW inputs (`:79-80`) while `result` restores OLD; `:174` tests nullity only ⇒ no corrective recompute EVER fires; `:133` early-returns; `:38-39` restore `displayLatex` post-simplify but `latex_sigma` pre-simplify — three provenances, one screen. The quantitatively-wrong-plot half (old coefficients under a new ω; `minX = ox[0]` vs `maxX = domB`) is **banked at fr-ConvergencePlot L-M2 — folded, not re-booked**; EquationView owns the cache half. | **F.W4** — key the result record and compare at `:79`/`:174` (or one atomic record). Discharges D-06's poisoned-write consequence too. |
| **L·M-2** | Abort-race clobbers the loading flag: `abortable(key)` aborts the prior controller synchronously before the first await; fixed keys `eq-compute`/`eq-simplify`; A's `finally` (`:126`) clears `computing` while B is still in flight — a whole network round trip with the template fallen through to the EMPTY state (`:321-325`) mid-compute. Reachable from cold load (`:174`'s initial compute superseded by a preset/Enter/button). R-L's scope sharpening adopted: doSimplify's identical shape (`:135`/`:145`) is consequence-FREE today because `simplifying` has no template consumer (D-B4) — the two halves are not equal in blast radius. | **F.W4** — generation token or per-call guard in `finally`. The empty state is this defect's only visible symptom — see INFO-2 before any "unused branch" cleanup. |
| **L·M-6** | The `vizHarmonics`→`budget` rescale guard is a tautology: the comment says "at or near the old cap"; the condition `budget <= oldV` holds in EVERY reachable steady state (branch 1 forces `budget ≤ v`; the slider's own `:max` IS `vizHarmonics`), so exactly one branch fires on every change and the user's explicit Display-terms choice is rewritten on every step of a drag. This watcher is B-2's engine. | **F.W4** — make the guard say what the comment says (`budget >= oldV − ε`), or delete the rescale. |
| **L·M-7** | No teardown: zero `onUnmounted`/`onScopeDispose` (whole-file read) on a lazily-loaded route component (router:92-94). `abortInflight(keys)` exists at lib/api.ts:61-66 with exactly 4 call sites, all in stores/workspace.ts (the banked AA seat-measure) — the equation route is the one async surface not using it. A dead instance's resolved handler writes six refs and `saveCachedResult` (`:120`) AFTER a fresh instance's `loadCachedResult` — C-29's incoherence from the other side. VueUse disposes `:45`/`:177`; the gap is precisely the two fetches. | **F.W4** — `onScopeDispose(() => abortInflight(["eq-compute","eq-simplify"]))`. |
| **D·D-B1** *(BLOCKER→MAJOR — K-8; fine arm corrected — RD-3)* | `.info-anchor`'s hard `right: 3.25rem` (`:430-434`) encodes the geometry of a button owned by a DIFFERENT component (`.copy-pos`, EquationResult) and that width rides the comfort axis: coarse `--control-h-md` = 60px (offsets-sizing.css:151 + light-dark.css:17-21; the utility ships precompiled — no consumer Tailwind scan needed; zero `--ui-scale`-family overrides in web/src, seat grep). Coarse: 16px overlap, info z-30 stealing the copy button's rightmost 16px (44px retained = exactly the touch floor — the K-8 demotion). Fine: NO horizontal overlap. Vertical incursion into the stage's 32px content start: 16px fine / 36px coarse. Third-order: the mode toggle's raw `height: 2.25rem` (:47, specificity 0,2,0 beating the CVA utility) doesn't scale — a 36px control beside 60px controls in one band. | **F.W4** — delete the constant; one flex row `top-2 right-2 gap-2`, one owner. Painted witness coarse+fine → SS-13. |
| **D·D-B2** *(BLOCKER→MAJOR per fr-CT precedent — both readers concur)* | The info button has no accessible name: `:276-280` `<Button variant="glass" size="icon">` wrapping a bare `<svg>` — no aria-label/title/sr-only/labelledby; `HoverCardTrigger as-child` forwards no naming attribute (reka emits `data-state` + grace-area only — banked instrument). Hard WCAG 4.1.2, and an internal divergence: the convention exists 3 siblings over (`title="Copy LaTeX"`; FunctionInput's aria-labels). R-L's extension adopted: FunctionInput's Wand2 auto-harmonics Button (`:189-197`) is a SECOND nameless icon-only control in the same subtree — its Tooltip supplies a description, never a name. Identification failure, not lockout (focusable via reka's bare `onFocus`). | **F.W4** — `aria-label` ×2; one axe run on `/equation` rides F.W9/W10. |
| **D·D-B3** *(BLOCKER→MAJOR, same precedent)* | The tier pill and energy badge literals fail WCAG AA on the popover ground in LIGHT mode — against the repo's own written, executed contrast law. Seat accepts both readers' independent recomputation (converged to 3 decimals): green hsl(142 71% 45%) → **2.17:1**, amber hsl(38 92% 50%) → **2.02:1**, red hsl(0 84% 60%) → **3.57:1** vs `--popover` L=0.942, at 14px/600 (NOT large text); the `color-mix` 15% tint only lowers them. All sRGB-authored (no oklch caveat). The aggravator is in-tree: style.css:112-127 (D.W4.d) measured 3.54:1 as an AA failure and shipped a darkening override — then this view ships 2.02:1 from a module (notation.ts:26/:32/:38, :45-47) that bypasses the token layer. Dark arm passes. Blast radius doubles through the InfoCard duplicate (RD-7). | **F.W4** — tokenize the three literals through a semantic-status ramp that clears AA light-arm; **GLASS-RELAY** — the tokenized ramp ask (D's disposition, adopted). |
| **D·D-M10** | `MetricBadge :color` has NO successor on 7.0.0's `Metric` — the energy-tier colour encoding is DESTROYED by the uplift, not renamed. Seat-verified at the tag: metric/types.ts MetricValueProps→MetricProps = value/unit/placeholder/loading/label/context/class/size/orientation — no `color`, no `tone`; `./metric-badge` absent from the 74-key tag keyset; 4.0.0 d.ts documents `color` ("applied to the value"). Consumer sites `:292-297` + InfoCard:31-36. Registry-swept: the no-successor fact is NEW; its CLASS is banked at fr-BasisSelector **M-7** (prop-VALUE-axis breaks invisible to the keyset census — "F.W1 must re-derive the break surface as a prop-level diff") and the migration seat at FR-AFP-18. | **F.W1** — M-7's Metric instance: re-express the encoding (class → `.metric__value`, or tokens), design work not a rename. Rider: 7.0.0 Metric's `loading`/`aria-busy` is the D-B4 benefit. |
| **D·D-M3** *(absolute qualified — K-10)* | Coefficient inspection is mouse-only: `:252-253` binds mousemove/mouseleave; useCoeffHover requires MouseEvent + `.closest(".eq-coeff")`; KaTeX spans get `cursor: pointer` + hover recipe but no tabindex/role/:focus-visible (`:414-423`). The tier+energy block lives only in the HoverCard and FunctionInput's energy figure only in a Tooltip `#content` — on touch, the energy figure is unreachable on iOS Safari and engine-dependent on Android (reka binds bare `onFocus` beside `excludeTouch`-wrapped pointer handlers — the banked fr-CanvasControlsDock R-9/K-9 qualification, carried verbatim). | **F.W4** — a keyboard/touch inspection path; **F.W1 benefit** — the overlay union's coarse-pointer tap-toggle (MIGRATION.md:866, seat-verified pin). Both touch engines → SS-13. |
| **D·D-M6** | Zero live-region semantics on the async surfaces: spinner + "Computing…" (`:221-226`), "Recomputing…" (`:239-241`), "Error: …" (`:243-246`) — seat grep over the subtree for `role=|aria-live|aria-busy` → 0. Compute is user-initiated (Enter/button); a screen-reader user gets no announcement of start, completion, or failure; the spinner divs carry no `aria-hidden`. Family-adjacent to banked roleless-spinner notes (fr-AdminAuditLog; fr-ContourSettings K-2); no fourier row covers this file. | **F.W4** — `role="status"` / `role="alert"` + `aria-busy` (one pass with D-B4's wiring). |
| **D·D-M12** | Two mechanisms for one job in one file: `SegmentedTabs variant="underline"` for the 2-way mobile toggle (`:188-191`) vs the hand-rolled `EquationModeToggle` for the 2-way Σ/expanded toggle (`:270`) — and the hand-rolled one signals state by colour alone (`is-active` → `--viz-amber` + 8% wash, :63-66; WCAG 1.4.1) with no aria-pressed/radiogroup and `title=` as the only label. The house pattern is DOCUMENTED three files away (notation.ts:5-7 "aria-pressed driving an instance-scoped tint") and NotationPills (`:17-32`) also fails to honour it; the Button CVA ships `aria-pressed:` arms. | **F.W3/W4** — either retire the hand-roll onto SegmentedTabs or add the one attribute the sanctioned mechanism needs; NotationPills rides the same edit. |
| **D·D-M1** | The equation stage is a fixed 160px box with no vertical escape: `.eq-card { height: 10rem; overflow: hidden }` over a scroll region that is `overflow-x: auto` ONLY (padding-top 32px; `.katex-display` forced `overflow: visible !important`; `.katex` 1.4em/1.8em) — vertical overflow escapes the scroll region by design then clips at the card with no scrollbar, no scale-to-fit, no auto-height. `expanded` mode renders up to `budget` terms. Structural absence proven; the truncation threshold is SS-13. | **F.W4** — an escape hatch (auto-height mode or vertical scroll); threshold witness → SS-13. |
| **C·D-11** | The glass-ui→value.js peer edge is unmet TODAY (installed 0.13.0 vs peer `^0.10.0 || ^0.11.0`, optional ⇒ EBADPEER warning not failure — C's v1 correction ratified), and the 0.13.0 pin is held from BELOW: keyframes.js@4.3.0 hard-deps `value.js ^0.13.0` + optionalDeps `glass-ui ~4.0.0` (all three package.json facts seat-verified by node). The EBADPEER reproduction folds to the census risk-1 family (banked ×3: fr-CoefficientsSpectrum, fr-CanvasControlsDock, fr-CanvasOverlayButton ELSPROBLEMS); the BACKWARD pin is C's genuine increment — it is what makes F.W2 un-landable alone. | **F.W0/F.W2** — sequencing fact for the tri-package transaction; corrects the corpus's "latent, not live" framing. |
| **M-FR** *(missed, R-D — seat-ratified NEW)* | `.eq-toggle`'s `overflow: hidden` (EquationModeToggle:34-40, flush flex children, no wrapper padding) clips glass-ui's focus ring off both toggle buttons: the Button CVA carries `focus-ring`; the recipe is `outline: none; box-shadow: var(--focus-ring-shadow)` (base.css:174-178) and the token is purely OUTSET (2px + 8px glow, scale-paper.css:65-68) — an `overflow: hidden` ancestor clips descendant box-shadows and the native fallback is already deleted, so keyboard focus on Σ/a+b is invisible at the outer boundary (WCAG 2.4.7 AA). All three axes quoted lines inside this block and walked past it. Registry: the containment-clips-the-ring CLASS is banked in kf (kf-KfPillTabs N-1/C-3.2 — `contain: paint`, different repo AND mechanism); no fourier instance existed. | **F.W4** — wrapper padding ≥ the ring, or move the radius onto the children (`overflow: clip` does not help). Pixel witness both themes → SS-13. |
| **M-CK** *(missed, R-L — seat-ratified NEW; cure-constraint)* | `computeKey()` UNDER-DETERMINES the request: `:82-84` returns 4 fields while the POST carries 7 including `notation` (`:109`) and `budget` (`:110`) — both response-determining (equations.py:91 `simplify_series(terms, req.budget, req.notation)` → `latex`; :96 `render_latex_sigma(terms, req.notation)` → `latex_sigma`). No live symptom TODAY because the memo branch is dead (L·m-1) — but BOTH prescribed B-1 cures (route notation through doCompute; revive the memo) walk into it: `doCompute(false)` would see an unchanged key on a notation change and early-return into doSimplify at `:96`, reproducing B-1 one layer down. Registry-swept: no `computeKey` row anywhere. The M-D1 class of finding: fix the key FIRST or the B-1 repair ships broken. | **F.W4** (binds B-1's cure) + **F.W5-W8** note (the key IS the client's statement of the operation's identity — R6-8 adjacent). |
| **M-RTC** *(missed, R-L — seat-ratified NEW; cure re-homing)* | The installed glass-ui 4.0.0 ALREADY SHIPS the token-colour resolver `lib/colors.ts` hand-rolls and gets wrong: `resolveTokenColor(css, el)` + `createTokenColorCache` (paint-onto-a-real-property → `getComputedStyle(el).color` → cascade-resolved `rgb(...)`; `invalidate()` documented for dark-mode flips) and `useTokenColor` (reactive, re-resolves via `useGlobalDark`) at `@mkbabb/glass-ui/dom` — present in the 4.0.0 exports AND the v7 tag keyset (both seat-verified). It never PARSES token text — precisely the four-regex failure mode of `cssVarToHex` — and it IS the "used-value probe-element read, never string parsing" remedy the banked fr-App B-1 record demands. The consumer instead hand-rolls the resolver (~95 lines) AND the dark-mode re-resolve (raw MutationObserver, App.vue:11-17). Registry-swept: zero `resolveTokenColor|useTokenColor` hits anywhere in the bank or corpus. | **AMENDS the banked fr-BasisCanvas D-1/BC-1/C-1 cure angle** (→ F.W1) and corrects the census F.W2 booking: the sanctioned seat exists at the CURRENT pin, no uplift, no cross-package coordination. `getComputedStyle` oklch-vs-rgb serialization → SS-13 (decides drop-in vs format adapter). |

**MAJOR folded to banked identities (5)** — original ids for life, never re-booked: **D·D-B5** (Button `variant`/`size` definition-absent at 7.0.0 behind a surviving subpath; all facts re-verified at the tag by this seat incl. `grep -c emphasis MIGRATION.md` → 0 and the no-dev-warning fallthrough limb at the tag) → **fr-AdminAuditLog AA-2** (ADJUDICATED BLOCKER census-extension row, 37 files / 38 / 124, → F.W1; the FR-AFP-2 discipline: fold the identity, book only THIS file's nine prop sites — EquationView:276; EquationResult:39-40; EquationModeToggle:11-12/:21-22; FunctionInput:144-145/:159-160/:190-191; NotationPills:20-21) · **C·D-16** (four of five `--viz-*` tokens resolve to `#888888` at the installed pin; `VIZ_COLORS.amber` survives only by the unrelated D.W4.d WCAG override; `:65`'s `|| VIZ_COLORS.golden` unreachable dead code) → **fr-BasisCanvas D-1/BC-1/C-1** (BLOCKER, → F.W1) via the fr-App B-1 fold — booking only the two increments: the CENSUS CONTRADICTION (CENSUS:187-189 books F.W2 as "declared 3-line hex residual"; the deleted surface is colors.ts:22-117 with live wrong output on 4 of 5 tokens — needs a colour-token witness) and the consumer arm (the coefficient popover's `\color{}` ink is load-bearing on the override's accident) · **L·M-1 = C·C-32** (plot x-mapping bound to live refs; no domain precondition anywhere) → **fr-ConvergencePlot L-M2 + M-L1** (banked MAJOR, → F.W4, which names THIS callsite's live-ref binding verbatim) — one increment booked as a MINOR rider below (the server-side validator absence) · **D·D-M9** (`display:none` panel, clock runs on) → **fr-ConvergencePlot L-M3 + D-26** (banked, cites `EquationView:219/:437-440` by name; K-9 caps the burn at one gBCR + reactive cascade per frame, not a 60fps draw) · **D·D-M8's inherited rAF limb + C·C-31's PRM cell** (ungated autoplay clock this file mounts at `:309-316`) → **fr-ConvergencePlot D-9 + C-2 with the M-D1 cure constraint** (the PRM gate must seed t = 1, or it ships an empty plot).

### MINOR (33 booked · 4 carried · 5 folded)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D·D-M4** *(K-11 scope)* | `.info-hovercard` overpaints the shipped `glass-floating` surface with an opaque `var(--popover)` plate + a `rgba(0,0,0,0.12)` shadow invisible over the dark `--popover` hsl(24 8% 16%) — 66 lines after `:400` gets the same shadow token-correct. The literal is a directory idiom (ConvergencePlot:397 + FrequencyGraph byte-identical, banked fr-CP K-3), so the cure is a token family pass, not a one-line copy. | **F.W4** (family pass) + the GLASS-RELAY question D filed (is `glass-floating` reaching portaled content?). |
| **D·D-M2** *(RD-5)* | Mobile opens on Controls; the auto-computed equation/tier/energy/plot are one un-previewed tab away; `mobileView` not persisted. | **F.W4** — default-tab ruling + persist the choice. |
| **D·D-M5** *(RD-7)* | InfoCard orphan: drift ×2 (prose register; sizing idiom) + the duplicated computed pair (EquationView:69-70 ≡ InfoCard:13-14). Orphan disposition itself = the standing **F8-REACH-01** demand (banked, fr-CanvasOverlayButton R-6) — HOLD is RED there already. | **F.W3** — execute the standing demand; the drift facts are its new evidence. |
| **D·D-M14** *(RD-6)* | Desktop-only state-blind scroll-fade: unconditional `::after`, tests nothing, retracts never; fades to `--background` over a `--card` ground (dark smudge); 40px fade over a 32px runway. Primitive limb (FadingScroll at BOTH pins, zero imports) folds → fr-ConvergenceLegend D-7 / fr-CoefficientsSpectrum M-6. | **F.W3** — `<FadingScroll axis="y">`, available at the current pin. |
| **D·D-M7** | Error banner: raw `text-red-400` (Tailwind oklch literal) while glass-ui ships the semantic register — the palette-literal limb folds → **FR-AUL-7** (banked, → F.W4 riding SS-13); the increments book: `truncate` with no `title`/expand/wrap, and no retry affordance on either error surface. Seat adopts R-L's gating rider: this surface is REACHABLE only when C·D-02 yields a non-empty string — the two defects cannot both bite in one deployment, and either cure changes the other's reachability. | **F.W4** — one edit with C·D-02's client half. |
| **D·D-M11 = C·D-18** | `:11` imports lucide `Info` (used `:300`) while `:277-279` hand-inlines the byte-identical glyph (seat-diffed against info.js:10-14) — the copy the `@lucide/vue` sweep will miss. | **F.W1** — sweep amendment (+1 inline site). |
| **D·D-M13 + D·D-m2 + C·D-13** | Breakpoint bundle: mobile gutter inversion (4px outer vs 12px inner; the `px-3` tab strip mis-registers 8px against the cards; desktop proportioned); `isDesktop` is DEAD (the JS guard can never change the outcome — `.panel-inactive` exists only inside the media query); triple breakpoint authority with a real [1023, 1024) dead band (tab bar renders, tabs do nothing, panels stack — seat re-derived). | **F.W4** — one breakpoint authority; delete the composable. |
| **D·D-m1** *(K-3 floor limb killed)* | The toggle ignores the comfort axis: raw `height: 2.25rem` / 16px / 11px literals; the scoped selector (0,2,0) beats the CVA utility. The "11px violates a 12px floor" limb is DEAD — 11px IS the library's `sm` rung. | **F.W4** — rides D-B1's anchor edit. |
| **D·D-m3 / m4** | Bare `ease` keyword at `:418` in the file whose `:443` comment declares the token migration done; five raw durations (`0.1–0.3s`) while glass-ui publishes duration tokens. | **F.W4** — finish the pass; correct the comment. |
| **D·D-m5 + m6** | Partial restore silently changes the reading (`eqMode` and `autoHarmonics` not persisted ⇒ Σ + auto-on return, `vizHarmonics` recomputes, the slider shows a different number unflagged); Compute with empty expression is a dead button (`:92-93` bare return; no `:disabled`, no message). | **F.W4**. |
| **D·D-m7 / m8 / m9** | Double elevation on the coefficient popover (`glass-floating` + a second box-shadow); two content measures below 1024 (left capped 480px/centred, right uncapped); two `getBoundingClientRect` per qualifying mousemove, unthrottled, over a KaTeX subtree (cost rides the banked fr-CP F.W4 MEASURE-BEFORE). | **F.W4**. |
| **C·D-04** *(RD-4)* | Unvalidated `JSON.parse` under a `CachedResult | null` annotation; `:59-67` guards the OBJECT then dereferences the FIELD while `:72` does it right; `:174` gated on the same nullity — no recovery, no clear-cache affordance. | **F.W4** — match `:72`; shape-check on restore (threat model per fr-CP RD-3). |
| **C·D-03 residue** *(the correction ratified — see S-11)* | Both `trust: true` sites are server/numeric-fed; the one user-expression site (`renderKatexInline`) passes NO trust (seat-read). Surviving MINOR: two unescaped catch-paths — EquationResult:26 `` `<code>${props.latex}</code>` `` into `v-html`, and ConvergencePlot's `catch { return latex; }` (that half folds → fr-CP L-M8+C-9, banked MINOR). | **F.W4** — `escapeHtml` in both catches. Trigger rate → SS-13. |
| **C·D-22** *(seat-strengthened)* | `:disabled="!effectiveN"` can never fire: seeded to 20 (`:41`) AND `compute_effective_n(minimum=3)` returns ≥3 on every server path including `total_energy <= 0` (seat-read) — unreachable from BOTH ends. | **F.W4** — delete or re-condition. |
| **C·D-24** | `SegmentedTabsProps.responsive` exists and is unused at the one callsite that needs the mobile Select behaviour. | **F.W4**. |
| **C·D-27** | `lucide-vue-next` is a runtime import declared in devDependencies. | **F.W0** — manifest row. |
| **L·m-1** | The `force` memo branch is dead code: call sites are exactly `:174` (result null by construction) and `:210` (force=true) — seat grep; every Compute press re-runs symbolic integration. (L's spurious fourth site at `:97` — K-14.) | **F.W4** — revive AFTER M-CK's key fix, never before. |
| **L·m-2** | `useCoeffHover(coefficients, notation)` never reads `notation` (seat grep: doc comment + parameter only) — a dead parameter advertising a notation-sensitive contract. | **F.W4**. |
| **L·m-3** | The ±n→(a_n,b_n) fold implemented twice with epsilons four orders apart (`|val| > 1e-10` unordered vs `amp > 1e-14` sorted) — a harmonic in (1e-14, 1e-10) appears in the legend and not the popover. The O(n²) in-loop-`find` half folds → fr-CP L-m12. | **F.W4** — one fold, one epsilon. |
| **L·m-5** | `FourierTermDTO → BasisComponent → FourierTermDTO` identity round-trip on every simplify + the self-inflicted `as SimplifyRequest["notation"]` cast (api.ts:49). | **F.W4**. |
| **L·m-6 = C·D-14** | `reconstructed_points` (500+500 floats) fetched, sessionStorage-persisted whole, read by NOTHING (seat grep: 1 hit, the type declaration; sent at equations.py:114/:128). | **F.W5-W8** — drop the field, or consume it. |
| **L·m-7** | `TIER_INFO: Record<string, …>` though `EquationTier` exists; with `strict` and no `noUncheckedIndexedAccess` (seat-verified) the load-bearing `?? TIER_INFO.spline` is type-dead while runtime-live (backend `tier` is a bare `str`). | **F.W4** — key by `EquationTier`. |
| **L·m-8** | A second, UNSCOPED `<style>` block ships the global `.info-hovercard` from a lazy route chunk (necessary for the portal, hazardous as an unnamespaced global; 2 hits repo-wide today). | **F.W3** — dies with the hover-card migration (K-4's popover destination). |
| **L·m-10** *(R-D's mechanism correction adopted)* | `error` is cleared ONLY at `:101` (doCompute); doSimplify never touches it — a failed compute's banner survives every successful notation/budget re-render and every input edit until the next Compute press. (L's ":101 after the dead short-circuit" framing was moot — the branch is dead per m-1.) | **F.W4** — clear on any successful settle. |
| **L·m-11** | Fresh `:options` array per parent render + the `$event as 'controls' | 'canvas'` template cast. Seat adopts R-D's dist close-out: SegmentedTabs has no memo on `options` — cost is a computed re-eval, not a defeated memo. The producer-typed `modelValue: string` cast folds → C·D-23 (**GLASS-RELAY**, owed under the standing edict). | **F.W4** — hoist the literal. |
| **L·m-12** | `popoverHtml`'s `>= 6`-after-push with no lookahead: exactly six harmonics still render a trailing ⋮ while the `cn`/`An` branches use `> 6` — three branches, two truncation semantics, one computed. | **F.W4**. |
| **L·m-13** | 469 lines: six input refs, eight result refs, six computeds, two async orchestrators, two key builders, three watches — the orchestration is the ONE concern not extracted in a directory with three established composables. A `useEquationSession()` makes B-1/M-2/M-3/M-5/M-7 unit-testable without mounting. | **F.W4** — the extraction is the cure seam for half this roster. |
| **M-TL** *(missed, R-D — seat-ratified NEW)* | The mobile tab strip is an ARIA `tablist` whose tabs control nothing, name nothing, and cannot be arrowed between: `variant="underline"` selects `role="tablist"`/`role="tab"` + `aria-selected` in the installed dist (seat byte-read); the two panels are bare `<div>`s (no tabpanel/id/labelledby); no accessible name reaches the tablist (the producer's `ariaLabel` hangs off the responsive Select branch, not passed here); `grep -c keydown dist/tabs.js` → **0** at 4.0.0 — an ARIA tablist with no roving focus. Grade declined to MINOR per the ratified kf-KfPillTabs:85 kill (`aria-controls` is SUPPORTED, not required, on role=tab); the kf orphan-tabpanel identity (kf-ChannelControls D-2/C-3) is the CLASS precedent in another repo — the fourier instance is new. | **F.W4** — tabpanel roles + a tablist name; **F.W1 benefit** — 7.0.0 ships `useTabRovingFocus` (banked kf instrument). |
| **M-ZM** *(missed, R-D — seat-ratified NEW)* | `.info-hovercard` paints at `--z-modal` (140) on a ladder that ships an exactly-named `--z-hovercard` rung (120) two levels below (seat-read scheme-motion ladder) — a hover-opened, close-delayed surface stacked above every Popover and level with Modal can outlive and overlay a dialog. Registry: only the INVERSE (killed) claim exists (fr-AdminUserList). | **F.W4** — one token. |
| **M-US** *(missed, R-D — seat-ratified NEW)* | `:467` `user-select: none` on the hover card makes the component's best prose — the Conjectured tier description D-S5 nominates as copy that "should survive any redesign" — unselectable and uncopyable. Defensible for the producer's one-line-label register; wrong for an explanatory paragraph. | **F.W4** — delete the line. |
| **M-CP** *(missed, R-D — seat-ratified NEW)* | `.eq-coeff` promises activation to the only modality that can never activate it: `cursor: pointer` (`:414-419`) with no `@click`, no tabindex, no role anywhere on the selector — the conventional click signal on a hover-only span, and on coarse pointers it resolves to nothing at all. Consumer-authored twin of the banked fr-AnimationControls C-28 producer class (GLASS-RELAY there). | **F.W4** — rides D-M3's inspection-path cure. |
| **M-BR** *(missed, R-L — seat-ratified NEW; cure-constraint on B-2)* | No restore-time reconciliation of `budget` against `vizHarmonics`: the `:151` watcher has no `immediate: true`; `autoHarmonics` is not persisted and resets true, so after a reload `vizHarmonics` can collapse below the restored `budget` with no watcher fire — and the Display-terms slider clamps on READ (`:model-value="Math.min(...)"`) but not on WRITE (`budget = $event` raw), so the control DISPLAYS a number the component will not SEND. Kills the slider-side clamp as B-2's cure: the restore path never passes through the slider. | **F.W4** — clamp at the ref or the two request sites (bound into B-2's row). |
| **M-SB** *(missed, R-L — seat-ratified NEW)* | `SimplifyRequest.coefficients` is the ONE unbounded field in the equation contract — no `max_length` (models :38-41) beside fully-bounded scalars, while the sibling contract caps its arrays (visualization.py:186 `max_length=16`, :192 `max_length=10`); the client's 2N+1 ≤ 201 bound is a coincidence, not a contract term; the endpoint runs on the bounded thread pool. The R6-8 attributability asymmetry, exactly. | **F.W5-W8** — one `max_length`. |
| **M-DV** *(seat-booked rider on the folded L·M-1/C·C-32)* | The SERVER side of the domain seam has no validator either: no `model_validator`/cross-field constraint on ComputeEquationRequest (seat grep); `domain_start == domain_end` ⇒ period 0 ⇒ the catch-all 500 that C·D-02 renders as nothing; reversed domains succeed with a negative period. The client half is banked (fr-CP M-L1); the contract half is the increment. | **F.W5-W8** — pydantic `model_validator` + the client precondition rides the banked row. |

**Reader-unattacked MINOR carried at axis grade (4, flagged — neither reader attacked them; seat spot-checks noted)**: **C·D-17** (whole-response re-serialise per simplify — `:141` + cache:48, seat-read) · **C·D-19** (twin magic `500`s across the seam — `:108` + models default, seat-read) · **C·D-20** (one hand-rolled v-model among six — `:207-209`, seat-read) · **C·D-21** (unearned optionality on four always-passed props — unverified, carried on the axis's grep). All **F.W4**.

**MINOR folded (5)**: **C·C-33** (label smuggled through MetricBadge's `unit` axis — d.ts separates the axes, seat-read) → fr-EditorControlsDock D-15/D-16/C-18, second site · **C·D-09** (`.cartoon-card` ×5 — exactly 5, seat count) → the shim family at fr-AdminAuditLog AA-4/K-13 · **C·D-26** (`:61` vs `:59` citation drift) → fr-CP D-21+C-21 · **C·C-30** (hand-rolled `lerp`) → **fr-CP C-19** (SC-4 — the seat's identity-guard catch; K-5's not-landable-today note rides it) · **L·m-3's O(n²) half** → fr-CP L-m12.

### INFO (6)

| id | claim | disposition |
|---|---|---|
| **I-1** *(L·i-1 + D·i1 + C·D-03 posture)* | Fabricated pre-measurement defaults (`effectiveN` seeded 20; `displayEnergy` seeded 1 = "100.0% energy") in a component whose subject is numerical honesty; `trust: true` on two numeric-fed sinks with a runtime-resolved token interpolated into a `\color{}` command position. | **F.W4** model note. |
| **I-2** *(missed, R-D — the empty-state witness)* | The empty state (`:321-325`) is nearly unreachable in the shipped default (`:174` computes synchronously-flagged at first paint) — the one path that reaches it is L·M-2's abort race, making the branch that state's only visible symptom. Recorded so an F.W4 pass does not delete an "unused" branch that is a defect's witness. | Rider on L·M-2. |
| **I-3** *(missed, R-D — the trust cure-constraint)* | `trust: true` at EquationResult is LOAD-BEARING: the backend authors the hover hooks as `\htmlClass{eq-coeff …}` (latex_rendering :159/:175-176/:216/**:248** — the `eq-An` site extends R-D's cite) and KaTeX gates `\html*` behind `trust` — so C §8's "replace trust with a handler" and L·m-9's "trust: false" both silently delete the feature the component exists to provide. Correct cure: a trust HANDLER whitelisting `\htmlClass` + the class set. The M-D1 cure-constraint class. | **F.W4** — binds the C·D-03-residue cure. |
| **I-4** *(L·m-14 residue)* | The R5-7 native-loop instance (ConvergenceLegend:30) is the banked fr-CL D-L5 / fr-CP L§5.3 carry — L's filing was its THIRD (K-15). The one additive cell rides the banked carry: EquationView itself contributes zero loop evidence while transitively rendering the unbounded list — a per-component loop denominator reads "no loops" and is wrong about its own subtree. | Rides intake R5-7's standing F.W4 carry. |
| **I-5** *(C·C-31 grade — K-6)* | keyframes.js pinned, chunked, zero-consumed in a closure running two hand-rolled rAF engines — TRUE and INFO: banked at fr-CP C-22 as "F.W2 scope evidence, not a defect". The second-engine cell (+useCurveTransition) rides that row. | Fold → fr-CP C-22. |
| **I-6** *(C·C-35/C-36/C-37)* | Micro-info: C-35 self-killed by its own falsifier (recorded honesty); C-36 (triple-nested easeInOutSine) folds → fr-CP L-i2 with +1 level; C-37 (fresh `:domain` array per render beside a `deep: true` sibling watcher) stands as filed. | **F.W4** notes. |

## Killed-claims register — 19 entries

| # | killed claim | refutation (adjudicated) | seat instrument |
|---|---|---|---|
| K-1 | D·D-M8's premise — "no blanket exists: glass-ui gates PRM per recipe, never globally"; `.pop-*`/`.slide-down-*`/`tooltip-in` unguarded | Both readers killed; RATIFIED — the universal `*:not([data-allow-motion])` reset at a11y-overrides.css:6-16 (+ the :26-31 arm re-killing `[data-allow-motion]`) reaches the app through the style.css:3 import chain; transform is not whitelisted, so the tweens strip and `tooltip-in` snaps. Banked fr-CP RD-2/K-14; fourth application. | a11y-overrides read |
| K-2 | D·D-M8's praise-by-contrast (ConvergencePlot's own PRM block as "the discipline stops at this file") | Inverted — that block is REDUNDANT with the producer blanket ("cost and bought nothing", banked fr-CP M-L7). The real motion defect is the JS clock (folded above). | banked + K-1 |
| K-3 | D·D-m1's "11px is below the 12px floor the library documents" | The quotation refutes the claim: the ladder is "sm 11px / md 12px / lg 14px / xl 18px" — 11px IS the sm rung. Rest of the row survives. | MetricBadge d.ts |
| K-4 | C·D-08 / §8 repair 10 — "HoverCard + the global block → `<HoverPopover>`" | The destination is DELETED at the target: `./hover-popover` absent from the v7 tag keyset (seat-enumerated; present at 4.0.0); the overlay union folds both onto ONE sealed `<Popover>` (MIGRATION §BI.W-OVERLAY-UNION). Correct destination `<Popover trigger="hover">` + `aria-label`, marked primitive-class-change per banked fr-CanvasControlsDock D-2/C-2; corroborated at 8.0.0 by fr-EditorControlsDock L-7. | v7 keyset |
| K-5 | C §8 repair 8 — "three-line rewrite to `@mkbabb/value.js/easing` … the cheapest leg, landable today" | Unexecutable at the installed pin: value.js 0.13.0's exports = `['.']` (seat node read) — no `./easing`, no `./math`; bare-root is producer-imposed (banked FR-AUL-55). A post-F.W2 cure mis-scheduled as landable. The sting stands: C's own S-05 posture (no dist aliases) is what makes this fail loudly. | node read |
| K-6 | C·C-31's MAJOR grade ("supersedes v1's AMBER") | Facts true, grade ruled down by the bank: fr-CP C-22 = F.W2 scope evidence, INFO. | banked row |
| K-7 | D·D-B1's "36px of equation glyphs render behind the control band" | Overstated by construction: the equation is CENTRE-set (`text-align: center`, seat-read) and the controls corner-anchored — occlusion is the run under each corner, expression-width-dependent. The band geometry survives; the glyph figure → SS-13. | EquationResult styles |
| K-8 | D·D-B1's BLOCKER grade ("provable geometric collision on every touch device") | Real 16px coarse overlap, not a lockout: the copy button retains 44px = the WCAG 2.5.5 floor glass-ui independently guarantees (`[data-size="icon"]` coarse floor, "only RAISES" — seat-read). Banked altitude: fr-AdminFlaggedPanel ruling 2 / FR-AUL-19. MAJOR. | a11y-overrides:115-121 |
| K-9 | D·D-M9's "60fps no-op loop burning the main thread" + its novelty | The zero-rect early return spares the draw body (banked fr-CP L-B3+C-11 ratified kill); the residual is one gBCR + the reactive cascade; and the mechanism is ALREADY BANKED at fr-CP L-M3+D-26 citing these exact lines. A re-book at MINOR, folded. | banked rows |
| K-10 | D·D-M3's absolute — "on a touch device the energy percentage is rendered nowhere at all" | QUALIFIED, not absolute: reka binds bare `onFocus` beside `excludeTouch` pointer handlers — tap-focus engines (Android Chrome) can open the card; iOS Safari cannot. The banked fr-CanvasControlsDock R-9/K-9 qualification, verbatim. Both engines → SS-13. | banked instrument |
| K-11 | D·D-M4's "two shadows, one file" framing (one-line copy cure) | The literal is a directory-wide idiom (ConvergencePlot:397; FrequencyGraph byte-identical per banked fr-CP K-3) — a one-line fix mis-scopes the cure. Nick, not a kill; both other limbs book. | banked + seat read |
| K-12 | D's MIGRATION.md pin cluster (`:511` ButtonVariants · `:960-962` tap-toggle · `:2624` FadingScroll) + the axes' line-drift ledger | All three substances TRUE, all three pins wrong (tap-toggle at **:866**, seat-verified; the others per R-D's re-resolution, adopted). Companion drift recorded once across all three axes (notation.ts, light-dark.css, EquationModeToggle, Button.vue, metric/types.ts cites each ±1). The banked K-13 lesson: re-resolve every anchor before a cure. | seat + R-D |
| K-13 | L·B-2's path 2 — "programmatic, no slider involved; with `budget = oldV = 20` the ratio is exactly 1" | Unreachable from the shipped defaults: the up-drag recurrence is `budget(v) = v − 10`, so `budget === oldV` never occurs; the ratio-1 state needs a prior DOWN-drag saturating branch 1. And the write is slider-DRIVEN (only the slider or a compute moves `vizHarmonics`) — it merely bypasses the slider's clamp. The BLOCKER survives entirely on C·D-01's trace (seat-reproduced). | seat derivation |
| K-14 | L's backend pin cluster as verifiable anchors | models/equations.py pins :23/:40 are EXACT (seat grep — L's :22/:20 one low, R-L's correction right); routers pins off (`:92`→:96; `:133-161` span drift); the fourth `doCompute` site `:97` is spurious. Claims survive; the pins were unusable. | seat greps |
| K-15 | L·m-14 — "extends R5-7 with a second live instance" | Third filing of a banked instance (fr-CL D-L5: "+1 instance; the carry is already booked — never re-booked"; fr-CP L§5.3 re-records it). The additive denominator cell rides as I-4. | banked rows |
| K-16 | C·D-04's and C·C-29's BLOCKER grades | D-04 → MINOR (RD-4; fr-CP RD-3 threat model). C-29's plot half is fr-CP L-M2's banked MAJOR — grading it BLOCKER here double-counts a banked identity; the cache half books MAJOR. | banked precedent |
| K-17 | D §0's substrate line — "producer **7.0.0**" — and the wave arithmetic keyed to a 4→7 hop | Producer package.json reads **8.0.0** today; v8.0.0 tagged (seat). Every 7.0.0 fact D cites HOLDS at the tag (seat re-verified the load-bearing ones), so the findings survive; the pin and the hop arithmetic do not. Fifth occurrence — the re-trigger belongs in the F.W1 charter (fr-ECD L-7 / fr-CP SR-1). | seat re-pin |
| K-18 | **R-D's confirmation of the 1 s + 2 s backoff** (SC-1) | FALSE CONFIRMATION — struck per RD-1. The middleware stamps every response; the true wait is 30 s per retry. | rate_limiter.py |
| K-19 | **R-L's kill of C·D-16's byte receipt** (SC-2) | FALSE KILL — struck per RD-2. The quoted `light-dark(oklch())` arms exist verbatim at light-dark.css:130-131/:145-147 inside the `@supports` block; the "NEVER light-dark()" comment governs the constellation set. Bank corroboration ×2. | dist extraction |

## Superlatives — ratified with provenance (L-18 runs both ways)

- **S-1 · The `:116-119` ordering comment** (all three axes + both readers + this seat's own re-trace of the full chain `effectiveN → vizHarmonics → :151 watcher → budget → displayKey → :133 guard`) — capturing `lastDisplayKey` BEFORE the assignment is what lets the post-compute simplify detect the budget change; swapped, it is silently suppressed. The comment names the mechanism, not the symptom. The best line of engineering prose in the file, and load-bearing.
- **S-2 · `overflow-x: visible; overflow-y: clip`** (`:357-358`, D-S1 + both readers) — the ONE legal mixed overflow pair under CSS Overflow 3; the naive `visible`/`auto` degrades to `auto`/`auto` (spurious scrollbar + clipped popover). Genuinely expert.
- **S-3 · Flex hygiene** (`:239/:243 shrink-0` against `:308 flex-1 min-h-0` and `:380`) — the plot absorbs slack, the banners never squeeze; most stacked panels omit `min-h-0` and wonder why the canvas won't shrink.
- **S-4 · `useCoeffHover`'s DOM strategy** (L·S-5 = C·S-02, both readers) — ONE delegated mousemove + `closest()` (not N listeners on spans KaTeX destroys per render), card-relative coordinates, container as an ARGUMENT (never `document`) — survives scrolled/transformed ancestors and a second instance.
- **S-5 · The cross-seam hover contract** (R-D's find — NO axis noticed it existed) — one mechanism spanning four files and two languages with a docstring on the producing side: latex_rendering.py declares and emits `\htmlClass{eq-coeff …}`; CLASS_MAP is the exact inverse; `:414-423` styles it. Which is precisely why I-3's cure-constraint matters: all three axes prescribed a `trust` change that would sever a documented contract none of them saw.
- **S-6 · `useEquationCache` is storage-denial-proof and versioned** (L·S-4) — all four accessors totally wrapped, both keys `-v2`; a private-mode throw cannot white-screen the route. Correctly held orthogonal to the M-3 coherence defect.
- **S-7 · Abort discriminated from failure at BOTH seams** (`:122`/`:143`; L·S-2 = C·S-06) — the component never reports a self-inflicted cancellation as a user error. C's sting preserved: this correct-looking guard at `:143` is what makes the surrounding silence read as deliberate.
- **S-8 · The contract-v2 resolution posture** (C·S-05) — no `@mkbabb/*` dist aliases, `fs.allow` declined, the precept cited by section — and load-bearing beyond C's claim: it is exactly why K-5's un-landable cure fails loudly instead of resolving by accident.
- **S-9 · The prose refuses to overclaim** (D-S5) — "The formula matches but is not proven" is intellectual honesty most product copy never reaches; the recompute case gets its own verb. Two stings owed: `:467` blocks copying the very sentence (M-US), and the praised empty state is nearly unreachable (I-2).
- **S-10 · `EquationResult` retired a hand-rolled primitive ONTO the producer and recorded why** (R-L's find, missed by all three axes) — the P.W5 Lane B.2 header documents the `navigator.clipboard`+ref+setTimeout → `useClipboard({resetMs: 2000})` migration; the composable ships at the installed pin (seat-verified d.ts). The exact inverse of the two hardest-booked rows (colors.ts's hand-rolled resolver; the hand-rolled lerp) — the single place the pattern ran the right direction. Nick: imported from the root barrel, not `./dom`.
- **S-11 · Meta — the corpus's best cell is C killing its own v1 BLOCKER at the bytes** (D-03: `renderKatexInline` passes no `trust`; seat re-verified the two-hit trust census). Per the fleet standard (fr-AdminUserList): auditors who decline defects the tree refuses are worth more than ones who bank thirty. L's m-9 reached the same honesty independently ("I could NOT construct a payload").
- **Fold, not re-booked**: the endpoint-convention triptych (L·S-3 = C·S-04) is banked at fr-CP S-1 — corroboration recorded only.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **D-B1 painted overlap** — coarse arm (16px theft; does `(pointer: coarse)` match the tab bar's device class) + the fine 4px-gap null check; the token arithmetic is closed and needs no probe.
2. **D-M1 truncation threshold** — a stacked-fraction trig/polar expansion vs the 160px stage at 1.4em/1.8em, ≥768px and 375px.
3. **C·D-02's protocol arm** — HTTP/2 empty `statusText` ⇒ `error = ""` (both banners silent) vs HTTP/1.1 "Unprocessable Entity" (banner useless). Both defective; they route to different cures, and D-M7's whole surface is gated on which obtains. Probe the deployed API, not the dev proxy.
4. **The hover-card tap legs, BOTH engines** (Android tap-focus vs iOS Safari) — decides how much of D-M3 survives; the standing fr-CCD K-9 qualification, do not carry unqualified.
5. **M-FR pixel witness** — does any ring pixel survive `.eq-toggle`'s clip; keyboard-tab both buttons, light + dark. Mechanism fully proven from installed bytes.
6. **`.info-hovercard` fixed 300px at a 320px viewport** after reka collision shift (300+24 > 320; shift cannot shrink) — masked today by fine-pointer-only reachability, goes live at the F.W1 tap-toggle promotion.
7. **The `#888888` popover paint** — rides the banked fr-BasisCanvas D-1 witness; never re-commissioned.
8. **Mousemove forced-layout + hidden-panel per-frame cost** — rides fr-CP's F.W4 MEASURE-BEFORE demand.
9. **`vue-tsc -b` fallthrough admission of `variant="glass"` at the uplift** — decide by a scratch typecheck at F.W1, not a browser (FR-AFP-2's in-tree corroboration suggests it builds green).
10. **`getComputedStyle` oklch-vs-rgb serialization** — decides whether M-RTC's `resolveTokenColor` is a drop-in or needs a format adapter; the duplication and re-homing are proven from bytes.
11. **D-B3 composited corroboration only** — the ratios are closed arithmetic; the `color-mix` tint only lowers them.

## Identity guards (anti-rename) — folds by reference

- **Button prop-surface break** (D·D-B5) → fr-AdminAuditLog **AA-2** (census-extension BLOCKER, F.W1), with fr-ConvergenceTimeline D·D-2 and FR-AFP-2 as the sibling instances — this record contributes the nine file-local prop sites and the tag-verified no-dev-warning limb.
- **Viz-palette collapse** (C·D-16) → fr-BasisCanvas **D-1/BC-1/C-1** via fr-App **B-1** (F.W1) — this record contributes the census-contradiction increment, the popover consumer arm, the RD-2/K-19 receipt vindication, and the **M-RTC cure amendment** (the producer's `/dom` resolver at the current pin).
- **Plot/domain seam** (L·M-1, C·C-32, C·C-29's plot half) → fr-ConvergencePlot **L-M2 + M-L1** (F.W4) — increment booked as M-DV (the server-side validator).
- **Ungated-clock family** (D·D-M8 rAF limb, D·D-M9, C·C-31's PRM cell) → fr-CP **L-M3+D-26** and **D-9+C-2 under the M-D1 cure constraint**.
- **Hand-rolled lerp** (C·C-30) → fr-CP **C-19** (SC-4; K-5's sequencing note rides it). **Triple-easing** (C·C-36) → fr-CP L-i2 (+1 level). **Citation-drift** (C·D-26) → fr-CP D-21+C-21.
- **InfoCard orphan** (D·D-M5's orphan limb) → the standing **F8-REACH-01** demand (fr-CanvasOverlayButton R-6) — drift + computed-duplication are its new evidence.
- **MetricBadge label/`unit` contract** (C·C-33) → fr-EditorControlsDock D-15/D-16/C-18. **`cartoon-card` shim** (C·D-09) → AA-4/K-13. **Palette-literal error ink** (D·D-M7's limb) → FR-AUL-7. **Coverage void** (C·D-28 — zero functional e2e on `/equation`, seat re-verified 8 specs / 1 screenshot hit) → the banked coverage family (FR-AUL-16, census risk 10) — its `/equation` instance routes **F.W9/W10** + NO-WAVE-OWNER spec input: one interaction spec (compute → notation → budget → reload) would have caught B-1, B-2, C-29, D-07 and D-14 mechanically.
- **FadingScroll-shipped-unused** (D·D-M14's limb) → fr-CL D-7 / fr-CS M-6. **R5-7 native-loop carry** (L·m-14) → fr-CL D-L5 / fr-CP L§5.3 (K-15). **Touch-dead qualification** → fr-CCD R-9/K-9. **Hover-card primitive-class-change** (K-4) → fr-CCD D-2/C-2. **EBADPEER** (C·D-11's reproduction half) → census risk-1 family (×3 banked); the keyframes backward-pin books here.
- **fr-BasisSelector M-7** gains its Metric instance (D·D-M10); **kf-ChannelControls D-2/C-3** is the class precedent for M-TL (fourier instance new); **kf-KfPillTabs N-1/C-3.2** is the class precedent for M-FR (consumer-authored fourier instance new).

## DISSENT — preserved, not ruled away

1. **Each reader failed exactly once, in its off-axis** — R-D falsely confirmed a server-tree claim (SC-1, never opened the middleware); R-L falsely killed a dist-byte receipt (SC-2, over-read a comment about a different token set) and inherited a keyset count (SC-3). The citation-inheritance ledger now carries confirmation-, kill-, and count-flavoured instances in one record. Recommendation renewed from fr-CP DISSENT-1: hand readers the registry index AND the middleware/dist file inventory as challenge inputs.
2. **R-L's MAJOR grades on D-M2/D-M5/D-M14** are preserved: a forming X·F spec that weighs mobile-default-hides-the-artifact or orphan-drift more heavily may restore them; the mechanism cells need no re-derivation.
3. **Severity architecture credit**: R-D's demotion discipline (K-8, K-9's cap, the fr-CT precedent applications) was the corpus's best and carried its one false confirmation; R-L's server-tree and producer-seat sweeps (RD-1, M-CK, M-RTC) were the corpus's deepest and carried its one false kill. The seat adopted each where the other was blind — which is the M-23 design working as intended.
4. **The B-1/B-2 BLOCKER grades are unanimous** (axes, both readers, this seat). If the forming spec grades notation-inertness as MAJOR-not-BLOCKER because a force-Compute recovers it, the self-sealing memo (`:133`) and the sessionStorage persistence of B-2's escalated value are the facts that argue against it.
5. **Substrate drift, fifth occurrence** — every 7.0.0-derived figure re-pins at F.W1 open; the standing re-trigger belongs in the F.W1 charter, not in another apotheosis footnote.

---

**VERDICT — DEFECTIVE, ADJUDICATED**: 2 BLOCKERs booked (the Notation control inert in the default view with a self-sealing memo; a one-drag `budget` escalation past the contract bound, swallowed silent and persisted into the next session) + 19 MAJOR booked (led by the 20×-sharper-than-filed 429 amplification, the never-wired busy state over a silent seam, the destructured-away diagnostic, the two-record cache desync, and three genuinely-new missed rows — the clipped focus ring, the under-determined `computeKey` that booby-traps B-1's own cure, and the producer token-resolver that re-homes the banked palette cure to the current pin) + 5 MAJOR and 5 MINOR folded to banked identities + 33 MINOR + 6 INFO across 74 rows; 17 kills ratified and TWO reader cells struck — one false confirmation (R-D, the 429 backoff) and one false kill (R-L, the C·D-16 receipt) — with one identity fold neither reader drew (C·C-30 = fr-CP C-19). The file's small-scale reasoning is genuinely careful — the ordering comment, the overflow pair, the delegated hover, the storage-proof cache — and its failures are all one shape: two sources of truth never reconciled (sigma/expanded surfaces, live-refs/last-result, input-record/result-record, client-bound/server-bound), each invisible to every gate the repo runs. One F.W4 pass (the session extraction + error posture + a11y band), the F.W5 contract fields, and the already-banked F.W1/F.W2 identities dissolve the worst of it. Routings are M-25 agglomeration input; nothing here stamps a wave.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3. This document is the sole write.
