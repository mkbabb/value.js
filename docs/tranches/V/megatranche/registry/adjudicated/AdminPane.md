# AdminPane — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: incredulity adjudicator over two hostile re-readers (design-and-usage · library-and-code), both of whom re-read the full challenge corpus for `demo/palettes/admin/AdminPane.vue` and its five-panel closure.
**Date**: 2026-08-03 · **HEAD**: `c654824e` · branch `tranche-u` · **Corpus**: `audit/components/AdminPane/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md` (r2 governs; HYDRATION-LEDGER.md:33-35 marks all three `EXISTS-ORIGINAL`) + 5 committed probe artifacts.
**Method**: the two returns were attacked before either was adopted. Every verdict-level disagreement (D-09's pagination limb, C-5's live-region kill, D-08, D-10, C-2/C-9/C-11/C-12 severities, reader-1's unbooked L-2) was re-derived against the live tree with this seat's own instruments; the strongest agreed rows (C-1, the god-port census, the canon quotes) were re-measured, not spot-trusted. **Identity sweep run against the standing registry** — this corpus overlaps the already-adjudicated `AdminUsersPanel.md` (MT-AU1, §2+§3 delta-unified) on six rows; every overlap is cross-booked to the standing identity per anti-rename, never re-minted.
**Arbiter's own instruments (all static; zero browser probes — probe parsimony; dev truth not needed for any ruled row)**: full reads of AdminPane.vue (135 L), AdminUsersPanel.vue (391 L), AdminAuditPanel.vue, AdminFlaggedPanel.vue, AdminTagsPanel.vue, AdminNamesPanel.vue, AdminListItem.vue, AdminListSkeleton.vue, PaginationBar.vue, EmptyState.vue, useAdminUsers.ts (whole), usePalettePorts.ts (whole), usePaletteActions.ts:1-100(+writes grep), usePaneRouter.ts:60-190, viewSchema.ts:34-232, usePaletteWiring.ts:140-175, PaneHeader.vue:15-60/:135-145, App.vue:59/:77-110, PaneSlot.vue:115-127, shell.css:133-137, foundation.css:111-119/:152-156/:361-369, utils.css:130-156; canon: VISUAL-CONSTITUTION.md §3(:26-33)/§3.1(:38)/§4.1(:85)/§5(:101)/§5.1(:114)/§6.1(:151)/§7(:218), OPTICAL-BENCH-COMPOSITIONS.md:9/:51/:57-62/:90/:100(PR-35)/:106-108, PROPORTION-AUDIT.md:48-49(PR-04/PR-05)/§5 laws 5·6·11·13(:70-78), motion-quarantine.md §4 MQ-1(:131-168) + M-09..M-15 rows; gates: e2e/smoke/admin/ (5 specs + fixtures), a11y-battery.ts:60-107, a11y-authed-admin.spec.ts:20-55, api/src/modules/admin/service/users.ts:228-251; repo-scope greps: `<h1` in demo → **0**, `ms-[0-9]` → **0**, `ml-2"` → exactly 2, `searchQuery.value =` → **0**, `onActivated` → HeroBlob.vue:27/:246 only, onImpersonate/deletePaletteAdminBySlug/`.userPalettes` external → **0/0/0**, admin-spec assertion recount → **52**, badge/description assertions → **0/0**; package.json#exports (7 keys, no `.`), tsconfig.demo.json:30-50, `ls dist/` + `dist/subpaths/` (no index.d.ts, no parsing/units d.ts), demo/ui/{card,badge}/index.ts (one-line root-barrel re-exports), installed glass-ui 7.0.0 searchVariants.d.ts (`{inline,bare,floating}` — no `seated`) + SearchBar.vue.d.ts prop set (incl. `surface`), eslint.config.js:228-258, `ls demo/@` → No such file or directory, demo/palettes/export/ listing (12 modules, NO index.ts), registry/adjudicated/AdminUsersPanel.md §2/§3 (MT-AU1 coverage), V-PRIME.md:85-98 (W46/W47/W52 owners), CARRY-LEDGER.md:26.

## Scoreboard

**44 adjudicated rows: 2 BLOCKER-identities · 20 MAJOR · 18 MINOR · 4 INFO. 16 killed claims (10 reader-kills upheld, 3 kills extended by this seat, 3 fresh kills of the readers' own cells). 15 missed rows adjudicated in — 6 of them land on standing MT-AU1 identities rather than new bookings.**

Blocker survivors: **C-1** (the prune confirm sizes a server-global irreversible delete from the search-filtered list — ≡ MT-AU1 **Δ-1**, one identity for life) and the **D-03/C-4 family** (a fabricated authoritative zero in both the load-error and never-attempted states — ≡ MT-AU1 **L-5**, the AdminUsersPanel docket's ruled BLOCKER; AdminPane's badge arms fold into that identity).

The composition core (D-01/D-02/D-09/D-10/D-11 + the rhythm rows) is ratified-but-unexecuted forward spec owned by **V′ W52** (F6 Admin suite, absorbing W24 — V-PRIME.md:91; CARRY-LEDGER.md:26); the operative core (prune, results, counts, the instance handle, the port) is already owned by **MT-AU1**; the shell rows (H1, viewSchema typing, ViewId route) go to **W47**; two rows are GLASS-OWNED interims on the standing relay; the shared-ref class alone is NO-WAVE-OWNER at the next formation boundary.

## Ruled disagreements (this seat's actual work)

| row | reader 1 (design-and-usage) | reader 2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-09 pagination limb** | CONFIRMED whole ("Pagination controls across all five: zero") | KILLED the limb (PaginationBar on Audit) | **KILLED — reader 2 wins, and the kill EXTENDS** | My read: AdminAuditPanel.vue:83-90 **and** AdminFlaggedPanel.vue:126-133 both mount `PaginationBar` wired to their port facades; PaginationBar.vue:3 `v-if="pageCount > 1"` + :17 `aria-live="polite" aria-atomic="true"`. The probes measured empty fixtures (pageCount 1 → correctly suppressed), not a missing feature. TWO of five routes paginate. D-15's `paginating ❌ absent` row (challenge-D:470) falls doubly. D-09's grammar limb (three query registers) and capped-roster limb survive — see roster. |
| **C-5 "no live region"** | Killed narrowly (K9: Tags has `role=status`; "the users/names routes have none") | Killed broadly (EmptyState `role=alert/status`; AdminListSkeleton `role=status`) | **Reader 2's kill wins; K9's scoping limb is itself FALSE** | EmptyState.vue:17 `role="alert"` (error) / :28 `role="status"` (empty); AdminUsersPanel.vue:51-63 routes BOTH states through it, AdminNamesPanel.vue:30/:42/:80/:92 and AdminAuditPanel.vue:42/:56 likewise; AdminListSkeleton.vue:10-11 `role="status" aria-label="Loading"`; AdminTagsPanel.vue:54. K9's sentence "the users/names routes have none" does not survive a read of the users panel. The true residue is narrow and is booked: loading→populated transition, the badge, the filter result-count (VC:114), and the prune transient (VC:101). |
| **C-2 severity** | BLOCKER (C-1 coupling) | MAJOR | **MAJOR, blocker-coupling recorded** | The row's own harm is state-aliasing; its BLOCKER weight is entirely borrowed from C-1, which already carries it and names the cross-pane filter as amplifier. The leaked term IS visible in the admin field (same ref, v-model at AdminPane.vue:13) — the understatement is latent, not invisible. Severity follows the row's own harm; the C-1/MT-AU1 wave must gate the coupling (its G-10 already makes the fabrication unrepresentable). |
| **C-9/L-7/D-13 severity** | MAJOR | MINOR | **MAJOR — and the grade is already RULED at the standing identity** | This is MT-AU1's **L-1** (RESCOPED BLOCKER→MAJOR at the AdminUsersPanel adjudication, §2). One identity, one grade: MAJOR. All five imperative pokes re-verified (useAdminUsers.ts:99/:121/:136/:150 + usePalettePorts.ts:123); AdminUsersPanel.vue:389 `defineExpose`; AdminPane.vue:27/:134 the wiring. MT-AU1 §3 scope (7) deletes the handle everywhere. |
| **C-11 severity** | MAJOR | MINOR | **MINOR** | Mount-only loads confirmed (AdminAuditPanel.vue:119, AdminFlaggedPanel.vue:152, AdminTagsPanel.vue:125; PaneSlot.vue:120 KeepAlive; no admin `onActivated`). But every affected panel ships a visible labeled Refresh, no data is destroyed, and the cure is one `onActivated` line ×3 — an idiom the tree already knows (HeroBlob.vue:246). Grade follows consequence. The staleness-of-a-ledger aggravation is recorded in the row. |
| **C-12 severity** | MAJOR ("vacuous gate") | INFO | **MAJOR, as test-debt** | The vacuum is what let D-03/D-04/D-06 ship green: my greps — zero admin-spec assertions on the badge, any count, or any description string; Playwright `getByRole(name:)` substring-matches, so "Users 0" satisfies `{name:"Users"}` (a11y-authed-admin.spec.ts:39). Two sharpenings of my own: the corpus/reader count "58 assertions" re-counts to **52** at HEAD, and the a11y battery walks only **3 of 5** admin routes (ADMIN_VIEWS = Users/Names/Flagged, :27-31 — Audit and Tags are never batteried). |
| **D-08** | KILLED to INFO (K3: laws govern W18 ratification frames, not component topology) | CONFIRMED MAJOR | **KILLED — K3 upheld; INFO residue** | My read of VC:57: the "one Admin frame cannot stand in for its four siblings" sentence sits inside the W18 ratification-artifact paragraph ("W18 must ratify one low-fidelity and one real-rendered composition…separately named frame"); OBC:9 likewise. Neither reaches Vue topology. Reader 2's own confirmation self-corrects ("'zero runtime sharing' overstates") and its App.vue:97-100 evidence is a stated design decision, verified verbatim in my App.vue read. Five bodies under one chrome is the shipped shape D-08's own cure asks for. |
| **D-10** | CONFIRMED MAJOR (static corroboration) | UNPROVEN (is the tray↔panel boundary a "major section"?) | **CONFIRMED MAJOR** | OBC §4 names the Users anatomy "Full-width query/status tray **followed by** principal review rows" — the tray and the field are separate ratified anatomy members, so their separation is PR-35's ADMIN5 major-section interval `--spacing(6)` = 24px (OBC:100, verbatim re-read; foundation.css adds only `--spacing-phi-*` at :152-156, Tailwind's `--spacing` base untouched). Shipped: AdminPane.vue:7 `gap-3` = 12px. The same file demonstrably knows the rung: PaneHeader pb-2 (8) + body py-4 (16) = 24px at the header→tray boundary. Reader 2's interpretive reservation is preserved below but does not survive the anatomy sentence. |
| **L-2 (unbooked by reader 1)** | — (coverage gap) | CONFIRMED MAJOR | **CONFIRMED MAJOR** | My count: AdminPane.vue:28-40 forwards 13 bindings, 12 of them `pm.*`-sourced, into a child that ALREADY injects the same port — AdminUsersPanel.vue:200 imports ADMIN_PORT_KEY, :234 `inject(ADMIN_PORT_KEY)!`, :361 uses it. The dual idiom is exact: Audit/Flagged/Tags inject with zero props (:107/:149/:122) and own their loads; Users gets 12 forwarded members of the same object. MT-AU1 §3 scope (7) converges the subject with its inject-only siblings. |

## Adjudicated defect roster — terminal dispositions

Line anchors re-verified at HEAD `c654824e` by this seat; re-resolve before executing any cure (D-19). **Identity law**: rows marked ≡ are the SAME defect as a standing registry row and are never re-booked under a second name.

**BLOCKER (2 identities)**
- **C-1 · BLOCKER · ≡ MT-AU1 Δ-1/D-1/L-19/C-2 (one row for life).** The prune confirm sizes a server-global, irreversible, cross-collection delete from the SEARCH-FILTERED prop. Re-verified end-to-end: AdminPane.vue:28 `:users="pm.filteredAdminUsers.value"` vs :33 `:total-users="pm.adminUsers.value.length"`; AdminUsersPanel.vue:241 `emptyCount` over the filtered prop; :290-291 prints it twice into the destructive confirm; the filter is slug-substring (useAdminUsers.ts:32-34); the server takes no scope — api users.ts:228-251 `findEmptyUserSlugs()` whole-collection + transactional users+sessions delete; post-prune the client filters its ≤50-row page by guesswork (useAdminUsers.ts:59/:184-186). Understatement unbounded; also wrong in the inverse direction (Prune disabled at :25 while off-view empties exist). **Disposition: MT-AU1** (G-10: the request carries the slugs it names — the fabrication becomes unrepresentable). Never re-quote probe-D3f's "`pruned: 3`" as a server observation (ruled fixture-echo at the r3 delta).
- **D-03/C-4 · BLOCKER-identity · ≡ MT-AU1 L-5 (auth-costumed-as-empty).** The badge renders an authoritative `0` in BOTH non-states: useAdminUsers.ts:54-56 returns before touching `loadingUsers`/`usersLoadError` (never-attempted ≡ loaded-empty); :62-68 catch leaves `adminUsers` `[]` while `finally` drops the loading arm — AdminPane.vue:121-122 guards on loading ONLY, so `<Badge>` renders `0` beside the error EmptyState (AdminUsersPanel.vue:51-62) or beside `· roster clear ·` for an anonymous visitor (:63; reachability confirmed — the logout watcher usePaletteWiring.ts:167-171 is non-immediate). The A-3 comment at AdminPane.vue:119-120 states the rule and covers one of three violating states. Same shape on admin-names (`queueLoadError`, :126-128). **Disposition: MT-AU1** (viewSchema `auth:"admin"` + one guard site; "count guarded on error" is in the §3 scope). Reader 2's consequence kill stands: no live destructive affordance for the anonymous visitor (Prune disabled at :25; Refresh a guaranteed no-op).

**MAJOR (20)**
- **C-2/L-1 · one `searchQuery` ref, three ports, three simultaneously-mounted v-models.** usePalettePorts.ts:54 → :141/:183/:224; bound at AdminPane.vue:13, BrowsePane.vue:11, PalettesPane.vue:34; never reset anywhere (`searchQuery.value =` → 0 hits); every admin view mounts PalettesPane beside it (`right:"palettes"` ×5, viewSchema.ts:188-231); divergent consumers useAdminUsers.ts:29-34 + useFilteredList sites; the wiring guard usePaletteWiring.ts:159-164 exists solely to contain the aliasing. **Disposition: NO-WAVE-OWNER** — the port-split successor banked at MT-AU1 ("adminPort five-seam split beyond the users slice", re-trigger `grep -n 'adminColorQueue: colorQueue' demo/palettes/usePalettePorts.ts`); the next formation consumes it. Blocker-coupling to C-1 recorded above.
- **expandedId aliasing (missed, reader 2) · the SAME class on a second cell — with a namespace fork.** usePaletteActions.ts:24 one ref; writers :28/:76/:81/:129; handed to all three ports (:144/:177/:226). BrowsePane.vue:97 and AdminUsersPanel.vue:144 compare it to `palette.slug`; PalettesPane.vue:87 compares the SAME cell to `palette.id` — two namespaces on one ref across panes mounted side by side; picker save paths (:76/:81) silently collapse an open admin drill-down. **Disposition: same NO-WAVE-OWNER port-split row as C-2/L-1** — booked so the "zero shared refs survive" cure cannot close over searchQuery alone.
- **D-01 · every Admin route ships 50/50 beside the personal library the canon orders to 0.** Static: shell.css:133-137 equal-columns dual grid; App.vue:59 applies it whenever `right !== null`; viewSchema ×5. Law: VC:218 (verbatim re-read) + VC §3 law 2 (:27, unqualified) + OBC:51/:57-61 (`Companion rect 50%→0` ×5) + PR-04 (:48). Ruled MAJOR not BLOCKER: ratified-but-unexecuted forward spec, not a regression (reader 2's framing adopted). The law-1 pillar is killed (K2) — the cure is REMOVAL, never re-proportioning. **Disposition: W52** (V-PRIME:91; the cure is structurally sound — `right: null` drops the dual class, `rightProps` never fires, PaneSegmentedControl self-hides).
- **D-02 · the retired mobile pane selector exists only because admin views declare a right pane.** usePaneRouter.ts:175-182 mobile arm; viewSchema leftLabel/rightLabel ×5; VC:218 + §3 law 6 (:31) + §4.2 (:89, P092 retirement) + OBC:107. **Disposition: W52 (schema rows) + W47 (the PaneSegmentedControl/split-state retirement, V-PRIME:86 "1→0").**
- **D-04/C-3/L-10 · the two badge arms apply opposite denominators.** AdminPane.vue:122 unfiltered vs :128 filtered, while the rendered list is filtered (:28); the governing rule is written at :123-124 and violated two lines above. **Disposition: MT-AU1** (§3 scope: `RosterCounts` from the port, count guarded) **+ W52** (one status grammar).
- **D-05 · one number, three renderings on one card.** Badge (:5) + `{{ totalUsers }} user(s)` (AdminUsersPanel.vue:8-10, fed from :33) + the empty-state sentence (:63). Observation confirmed on source alone; D's law citation killed (K7) — the canonical hook is VC §5.1's owning-status-region row, not PA law 5. **Disposition: W52.**
- **D-06/C-6 · a live network counter welded into the route's ONLY heading.** PaneHeader.vue:22 `<h3>` slot; AdminPane.vue:4-5 puts title + Badge in it; `<h1>` in demo → **0** (my grep) — the mutating h3 is the route's sole heading, and the shell H1 belongs to W47 (VC:85). Aggravation found by this seat: **PalettesPane already ships the correct idiom one pane over** — its badge is `aria-hidden="true"` with an sr-only `({{n}} saved)` companion (PalettesPane.vue:19-25); AdminPane's is bare. The typography sub-claim is killed (K10 — mono-small is the canon-correct voice for a value, PA law 13). **Disposition: W52 (badge seat out of the naming slot) + W47 (shell H1).**
- **D-07 · physical `ml-2` — the title/badge gap collapses under RTL.** AdminPane.vue:5; `ms-*` in demo → 0; twin at PalettesPane.vue:22 (the only other `ml-2"` in the tree). VC §6.1 (:151). Mechanism statically decided; rects live-only (residue). **Disposition: W52** for the admin instance; the PalettesPane twin recorded as a named rider so it is not lost (two-site mechanical `ml-2`→`ms-2`; no producer change — KISS, no third site exists).
- **D-09 (rescoped) · three query registers where the canon binds one grammar; roster capped at 50 with no cursor.** SearchBar gated to users/names (AdminPane.vue:12); Audit ships two raw Input pills (:7-22); Tags two more (:20-35); Flagged none. VC:218. The pagination limb is KILLED (two live PaginationBars). The cap: useAdminUsers.ts:59 `listUsers(token, 50)`, `res.total` discarded. **Disposition: W52** (grammar) **+ MT-AU1** (§3 scope 1: page-aware roster; scope 5: PaginationBar mounted on Users).
- **D-10 · ADMIN5 major-section interval 12px where 24px is bound** — ruled above. **Disposition: W52** (PR-35 strip is W52's measured close).
- **D-11 · the console housing is a Card with an 8px cartoon caster where the register binds a structural review field.** AdminPane.vue:2 `<Card tier="resting"…>` carrying material+scroll-host+grid-cell at once; foundation.css:366/:368 traces the measured shadow exactly; OBC:57-61 housing column + VC §3.1 (:38). The OBC:90 pillar is killed (K8) — **but this seat restores a lawful hook for the caster arm: PR-05 (:49) names "caster shadows" as boundary-repetition, disposition REMOVE** ("only the five Admin fields retain one adjacent-row separator"). **Disposition: W52.**
- **L-5 · two live routes to one design system in eight lines.** AdminPane.vue:74-75 `../ui/{card,badge}` (both one-line ROOT-barrel re-exports, my read) vs :87 `@mkbabb/glass-ui/search`. **≡ the W-HYGIENE demo/ui extirpation identity** (ColorPicker L-6 / ColorSpaceSelector L-4 — ~90 edges repo-wide). **Disposition: FOLD → W-HYGIENE**, identity preserved; this component's two edges die with the console re-home (MT-AU1 §3 scope 6).
- **L-6 · adminPort is a 34-member aggregate over four domains; AdminPane touches 27.** Counted member-by-member by this seat against :195-231 — 14 users + 13 queue + 7 misc = **34 exact**. The RF-15 header (:22-30) claims dissolution; :245 re-aggregates. **Disposition: MT-AU1** (§3 scope 7: narrow `AdminUsersPort` split) **+ the standing BANK** (full five-seam split; re-trigger above).
- **L-7/C-9/D-13 · a composable owns a component INSTANCE; five imperative pokes; AdminPane exists partly to wire it.** ≡ MT-AU1 **L-1** (grade MAJOR, ruled). All sites re-verified (roster table above). Every `?.` silently discards the result. **Disposition: MT-AU1** (§3 scope 7 deletes the handle, the props, the emits, and the defineExpose).
- **L-2 · 12 forwarded port members into a port-injecting child; two boundary idioms fifteen lines apart** — ruled above. **Disposition: MT-AU1 (7).**
- **C-12 · vacuous gate** — ruled above (MAJOR, test-debt; recount 52; battery 3-of-5 routes). **Disposition: W52's measured close + MT-AU1's committed tests** (admin-mount-smoke/admin-prune-scope/admin-op-result et al. are already in the §3 scope).
- **MQ-1 inheritance (missed, reader 1) · the header choreography AdminPane hosts runs byte-identically under `prefers-reduced-motion: reduce`.** AdminPane.vue:2 carries `pane-scroll-fade` = the named-timeline host (PaneHeader.vue:54-57); motion-quarantine.md §4 **MQ-1 (HIGH, affirmative — NOT one of the 20 FALSE M-xx rows; carriable)**: both PRM guards clamp `animation-duration`, timeline-driven animations ignore it; measured byte-identical transform/height under `reduce` (:150-159); blast radius names **Admin** (:161-162); cure named (:164-168: PRM block setting `animation-timeline: none` in PaneHeader). This kills D-15's "reduced-motion OK (inherited, defensible)" row (K1) — D-15's exact reasoning is the documented blind spot. **Disposition: W47** (PaneHeader is shell chrome; routing is this seat's, flagged as such — the cure text is already written in the quarantine).
- **Two rhythms where one is bound (missed, reader 1).** PR-35 (OBC:100): row block inset `--spacing(4)` wide / `(3)` narrow; "the five Admin members share the one dense-row rhythm". Shipped: `px-3 py-2.5` (12/10px) at EVERY width — AdminUsersPanel.vue:80, AdminAuditPanel.vue:62, AdminFlaggedPanel.vue:50, AdminListItem.vue:11 — no responsive arm anywhere; inter-row gap 12px (Users/Audit/Flagged `gap-3`) but **8px for Names on both lists** (AdminNamesPanel.vue:43/:93 `gap-2`) — the forbidden per-route substitute. **Disposition: W52.**
- **The prune count's only truth is a 3-second vanishing flourish (missed, reader 1).** AdminUsersPanel.vue:301-309 `onPruneDone` → italic muted span inside `<Transition name="vj-celebrate">` (:16-20), no role, no persistence, `setTimeout` 3000 with no cleanup and no prior-timer cancel; VC:101 verbatim ("a transient flourish…never carries the only truth"). Compounds C-1: this line is the sole place the confirm/actual discrepancy could ever surface, after which :184-186 re-filters and no record remains. **Disposition: MT-AU1** (§3 scope 5: role=status beat, one home = moved ActionFeedback **gaining teardown**; §2 L-7's uncancelled-timer row is the standing identity for the timer half).
- **error ≠ empty inverted on the destructive paths (missed, reader 2) · ≡ MT-AU1 Δ-2/C-21.** useAdminUsers.ts:188-191 catch→`return 0` → "No empty users to prune" (success-register for a failed irreversible op); :157-166 `loadUserPalettes` []-on-failure → "No palettes." (AdminUsersPanel.vue:138); five further ops swallow to console.warn (:100-102/:111-113/:126-128/:139-141/:151-153) while the confirm closes regardless (:283-286). The file's own W5-5 law (:63) enforced only on loads. **Disposition: MT-AU1** (typed op-result channel, G-11) — booked once, at the standing identity.

**MINOR (18)**
- **L-8/C-7 · `cssColorOpaque` is a REQUIRED prop never read.** AdminNamesPanel.vue:140 sole occurrence (my grep); supplied at AdminPane.vue:52; one of two reasons for the CSS_COLOR_KEY inject (:94). → **MT-AU1 (6)** console re-home deletes it.
- **L-9 · `searchPlaceholder` has two implementations; the port's is dead.** usePalettePorts.ts:104-110/:225, zero consumers; AdminPane.vue:15 inlines the ternary; BrowsePane hardcodes a third string the port never produces. → rides the **port-split BANK** row.
- **D-14/L-13 · `.search-seated` — consumer recipe over a glass-ui root at three sites; the producer cure did not land in 7.0.0.** utils.css:132-156 (fill/1.5px border/`--shadow-cartoon-sm`/focus-ring recomposition/descendant font override); applied AdminPane.vue:14, BrowsePane.vue:12, PalettesPane.vue:35; installed searchVariants = `{inline,bare,floating}` (my read), no `seated`; SearchBar grew a `surface` axis the demo ignores. **Disposition: GLASS-OWNED interim — ASK-D on the standing BH relay (I-20)**; tracked, never expanded locally; the recipe dies whole at the adopt event.
- **L-12 · nine hand-copies of the pane-shell class string; PaneHeader owns an UNSCOPED style for a class it never applies.** AdminPane.vue:2; PaneHeader.vue:41-58 documents the inversion in prose; motion-quarantine:161-162 independently enumerates the nine. **Disposition: NO-WAVE-OWNER** — neighbors the V·L1..V·L4 layout family (layout-gestalt.md); named here so the next formation folds it.
- **L-3/C-8 · the title/description tables re-copy viewSchema.** AdminPane.vue:97-115 vs viewSchema labels (re-read: all five headerTitles equal `VIEW_MAP.label` EXACTLY; the "already drifted" claim is killed, K5 — `leftLabel` is a different field for a narrow control that D-01/D-02 delete anyway). PaneConfig has no `description` home. → **W47** (schema gains `description`; union becomes `Extract<ViewId, \`admin-${string}\`>`).
- **L-4/C-10 · prefix-routed `subView` never checked at its only call site — sharpened by the missed type row: the computed's type is affirmatively FALSE.** usePaneRouter.ts:93 prefix arm (against **ten** exact arms :83-92 — the "9" is killed) + :140 `{ subView: name }` from a plain string through `Record<string, unknown>` (:64, :128) spread at PaneSlot.vue:125; AdminPane.vue:97-105/:107-115 switches with no default — and because the switch is exhaustive over the DECLARED union, TS infers `ComputedRef<string>`, never `| undefined`, so no strictNullChecks diagnostic can ever fire while the runtime genuinely can return undefined. Latent today (isViewId gates the hash, viewSchema.ts:236). → **W47.**
- **L-11 · the declared top-level seam has zero importers and the lint cannot fire.** browser/index.ts self-declares the G-DEMO-3b seam; `from "./browser"|"../browser"` → 0 (my grep); AdminPane reaches the sub-barrels (:79-86); eslint.config.js:232-239 = 2 live color-picker globs + 4 dead `demo/@` globs (reader 2's correction verified), banned pattern spelled in a dead alias. **≡ ColorSpaceSelector L-5(a) lint-glob-liveness identity** for the glob half; **MT-AU1 §3 scope (9)** re-points the G-DEMO globs. Cross-booked to both; no new wave.
- **L-14 · ViewId has two import routes via a source-compat re-export for a deleted alias.** useViewManager.ts:13-19 (comment names the dead `@composables` path); usePalettePorts.ts:19 takes the shim route. → **W47.**
- **D-12 · `mx-auto` on the root is dead cargo** (with `w-full`, auto margins resolve 0; wrapper App.vue:96). Confirmed on the static limb; `min-h-0` limb stays live-residue. → rides **MT-AU1 (5)**'s AdminPane touch.
- **C-11 · KeepAlive staleness (ruled MINOR above).** Mount-only loads ×3 under `<KeepAlive :max>`; a revisited Audit Log predates the actions it logs until manual Refresh. → **W52** ("stable cursor" is in its close).
- **Two uncleaned timers (missed, reader 2).** The prune `setTimeout` (≡ MT-AU1 L-7, above) **and** AdminAuditPanel.vue:110-117's `filterTimeout` — cleared only on the next watcher tick; a pending 300ms debounce past unmount writes `audit.page.value = 1` and fires a network call into app-scoped port state. Challenge-C's "zero leak surface" (:70) was true of AdminPane.vue alone and false of the closure. Prune timer → **MT-AU1**; audit debounce → **W52** rider (unmount cleanup).
- **All five panels are statically imported (missed, reader 2).** AdminPane.vue:79-85 value imports; only AdminPane itself is async (usePaneRouter.ts:76); the corpus's "three lazy sibling panels" (challenge-C:61) is false — nothing about them is lazy; first admin navigation fetches the whole five-panel transitive closure. → **MT-AU1 (6)** (the re-home mints the seam; per-panel `defineAsyncComponent` rides it) / **W52**.
- **AdminPort ↔ AdminUsersPanel instance types are mutually recursive (missed, reader 2).** useAdminUsers.ts:14 type-import + :27 `InstanceType` → adminPort :197 → `AdminPort` (:268) → AdminUsersPanel.vue:234 injects it. The cycle is static fact; the it-breaks-vue-tsc-if-expose-mentions-the-port prediction is plausible-unverified (recorded, not booked). **Dissolves with MT-AU1 (7)** — the handle and the expose both die.
- **Dead surface (missed, reader 2) · ≡ MT-AU1 scope (1)/(2).** `onImpersonate` (:71-80) and `deletePaletteAdminBySlug` (:169-177) returned (:203/:211), on NO port, zero call sites (my grep); `onImpersonate` drags the `impersonateUser` client chain (api/admin-users.ts:59, api/index.ts:56); `defineExpose`'s `userPalettes` has zero external readers. Already owned: "dead exports deleted; impersonateUser deleted with its dead reader."
- **Post-prune roster repair is client-side guesswork (missed, reader 2) · ≡ MT-AU1 Δ-1's compounding leg.** :184-186 filters the held ≤50-row page; `result.pruned` and rows-removed-locally are independent quantities; the wiring reloads only when length===0 (usePaletteWiring.ts:148-150). Already owned (page-aware roster, `res.total` consumed).
- **The in-route filter announces nothing (missed, reader 1).** VC §5.1 (:114) ruled row: "changed result count/state through the owning status region." AdminPane owns the interaction (:11-16 → filteredAdminUsers → :28); no status region exists in AdminPane, and the natural host (the count line, AdminUsersPanel.vue:8-10) renders the UNFILTERED total (D-04), so it would announce the wrong number even if live. → **W52** (the obligation) with the mechanism landing via **MT-AU1 (5)**'s status-region home.
- **The admin search field's only accessible name is its placeholder (missed, reader 1).** AdminPane.vue:11-16, no aria-label — while all four sibling admin inputs carry explicit ones (AdminAuditPanel.vue:12/:20, AdminTagsPanel.vue:25/:33); the battery's INPUT arm resolves placeholder as name (a11y-battery.ts accName tail), so the gate is green by construction. Honest scoping preserved: an ACCNAME-legal fallback, an internal-consistency break, not a WCAG 4.1.2 failure. → **W52.**
- **Row action-gap rungs (missed, reader 1 — SHARPENED by this seat).** PR-35 action gap ADMIN5 `--spacing(2)` = 8px; toolbars comply (`gap-2` ×4); the row clusters do not — AdminUsersPanel.vue:106 `gap-1.5` (6px) on the destructive Palettes/Delete cluster, **and AdminFlaggedPanel's real action cluster is :85 `gap-1` (4px)** — the reader's cite of :106 there was the flag-details stack, not the actions; the true value is WORSE. → **W52.**

**INFO (4)**
- **D-08 residue.** Five KeepAlive entries of one component definition; `:max="6"` vs 11 distinct left keys (my count of viewSchema `left:` values) is a documented LRU sizing choice (App.vue:97-100). Observation, not defect.
- **L-15 · `export.ts` shadows `export/` (12 modules, NO index.ts — my listing); file-before-directory precedence binds the untested implementation.** Out of AdminPane's closure. → **W-HYGIENE.**
- **L-16 · exports-map/tsconfig drift · ≡ ColorSpaceSelector L-14 identity (already FOLDed to W-HYGIENE config parity).** Re-verified at HEAD: 7 exports keys, no `.`, main/module/types undefined; tsconfig.demo.json declares root + `/parsing` + `/units` (all three targets absent from dist — my `ls`) and omits shipped `/value` + `/css`; the "CLOSED 8-key set" comment false by one. Cross-booked; never re-minted.
- **The `ml-2` twin (missed, reader 1 — RESCOPED by this seat).** The visual/RTL twin at PalettesPane.vue:22 is real (the only other site; zero `ms-*` tree-wide = systemic gap confirmed) — but "byte-for-byte" fails: PalettesPane's badge is `aria-hidden` with an sr-only companion, so the heading-name half of the defect is AdminPane-only. Two-site mechanical fix; no producer relay needed unless a third site appears.

## Killed-claims register

1. **K1 upheld — D-15's reduced-motion row.** "Position-mapped therefore defensible" is the exact blind spot motion-quarantine MQ-1 measures (byte-identical scrub under `reduce`; guard fired at 1e-05s and was powerless). MQ-1 is affirmative HIGH, not a FALSE M-xx row — carriable, carried.
2. **K2 upheld — D-01's law-1 pillar.** VC:26 re-read whole: the golden/preview-dominant clause binds "A P122 instrument"; Admin sits in PR-35's non-P122 family (OBC:100 title) and in no P122 member table. Scope-stripping misquote. The finding stands on law 2 + §7 + OBC + PR-04; the ratified disposition is removal, never re-proportioning.
3. **K3 upheld — D-08 killed to INFO** (ruled above): both citations govern W18 ratification artifacts; the cure describes the shipped shape; the App.vue "concession" is a stated design decision.
4. **K4 upheld — the "module-scope" mechanism in C-2 and C-9 is false.** usePalettePorts.ts:43 and useAdminUsers.ts:16 open the functions; :54 and :27 sit inside them. Composition-root singletons, not module state — same observable lifetime, different repair site (they do NOT survive a composition-root remount).
5. **K5 upheld — "two of three already disagree" is fabricated.** `label` and `leftLabel` are disjoint PaneConfig fields with disjoint render targets; all five headerTitles equal `VIEW_MAP.label` exactly (my read); the "drift" pair is scheduled for deletion with D-01/D-02 anyway.
6. **K6 upheld — C-11's grep cell.** `grep -rn onActivated demo/` returns HeroBlob.vue:27/:246 (my run). The finding survives on the corrected statement (no ADMIN panel uses it); HeroBlob is the in-tree counter-example that strengthens the cure.
7. **K7 upheld — D-05's law citation is self-defeating.** PA §5 law 5 (:70) PERMITS data/status marks; it forbids decorative and nameless-operable marks and says nothing about redundancy. Correct hook: VC §5.1's owning status region.
8. **K8 upheld, with a replacement hook (this seat's).** OBC:90's enumeration is lexically a family of LINES terminating the separator table — "caster stroke" there is a hairline, not an 8px offset fill. **But PR-05 (:49) reaches the caster by name** ("Dividers, **caster shadows** and corner marks repeat a boundary — REMOVE") — D-11's shadow arm keeps a lawful basis, just not the one D cited.
9. **C-5's kill — reader 2's broad version adopted; K9's scoping limb itself killed.** "The users/names routes have none" is false: EmptyState (`role=alert`/`role=status`) and AdminListSkeleton (`role=status`) ship on those exact routes. The true residue is the four narrow gaps booked in the roster.
10. **K10 upheld — the badge's Fira/mono voice is canon-CORRECT for a value** (PA law 13); the defect is the LOCATION (the h3 naming slot). Restyling to Fraunces would newly violate law 13.
11. **D-09's pagination limb killed — and extended.** PaginationBar live on Audit (:83-90) AND Flagged (:126-133), correctly suppressed at pageCount 1, with a polite atomic live-region readout. "0 of 5" was measured against empty fixtures; the truth is 2 of 5 shipped, Users owed (MT-AU1 mounts it).
12. **C-4's "disarms Prune/Refresh" consequence killed.** `:disabled="emptyCount === 0 || pruning"` (:25) — an unauthenticated empty roster disables Prune; Refresh is an authenticated no-op (:55-56). No live destructive affordance for a stranger.
13. **C-10's `:107` citation killed.** The `Record<string, unknown>` sites are :64 (PaneSlot interface) and :128 (leftProps signature); :107 is another declaration. Claim true at the correct lines.
14. **L-4's "9 exact matches" killed.** componentFor holds TEN exact arms (:83-92) before the prefix arm (:93) and the ColorPicker fallthrough (:94) — my count. The finding (admin is the sole prefix-matched family) unaffected.
15. **The "58 assertions" cell killed (this seat's).** My recount at HEAD: **52** across e2e/smoke/admin/*.spec.ts. Both the corpus and reader 1's "re-ran, matching exactly" carry the stale number — a citation-inheritance marker recorded against the fleet, not the tree. C-12's substance (zero badge/count/description assertions) re-verified independently and stands.
16. **Two missed-row cells corrected (this seat's).** (a) The Flagged "action gap 6px at :106" cite — :106 is the flag-details stack; the real action cluster is :85 at `gap-1` = 4px (worse). (b) The `ml-2` twin's "byte-for-byte" — PalettesPane's badge is aria-hidden + sr-only-companioned; the a11y half of the defect does not transfer.

## Superlatives — provenance preserved

- **AdminUsersPanel.vue:70-89 + :337-350** — the conditional disclosure row: `role/tabindex/aria-expanded` only when genuinely interactive, and the `e.target !== e.currentTarget` keydown guard with its load-bearing reason written down (clicks are stopped, keydown bubbles). Born-RED under a named gate and cured (a11y-authed-admin.spec.ts records the original failure).
- **AdminUsersPanel.vue:311-314** — the shift-click confirm bypass EXCISED with its actual argument in the tree; every destructive path routes through one `showConfirm` (:263-278) with destructive tone and a slug pill. The grammar is right; only the number it quotes is wrong (C-1).
- **EmptyState.vue:14-28** — error (`role="alert"`, machine truth in Fira, real Retry) vs true-empty (`role="status"`, seeded specimen ghosts) as two genuinely distinct registers behind one atom, consumed by every admin panel — and it is what refutes C-5. AdminListSkeleton.vue:7-12 gives loading its own `role="status"`; per motion-quarantine **M-12 (TRUE)**, the producer's skeleton scan is the ONLY PRM-gated register in glass-ui.css — correct reduced-motion behaviour by construction on this exact surface.
- **AdminUsersPanel.vue:243-252** — tail-priority slug truncation (`…-33` vs `…-77`, never two identical stubs): a real user-safety insight on the exact failure mode that decides whether an irreversible action targets the right row.
- **api/src/modules/admin/service/users.ts:236-251** — the prune is a genuine cross-collection transaction with its invariant spelled out; the server half of C-1 is exemplary. Only the client's count is a lie.
- **PaginationBar.vue:17** — `aria-live="polite" aria-atomic="true"` on the page readout with both icon-only buttons named; 48 lines that get the announcement contract right in a corpus whose C axis reported the surface as having none.
- **AdminNamesPanel.vue:2-12** — the `min-w-0` cure carrying its full failure story (TabsContent min-width:auto propagating truncate min-content, approve/reject ~250px off-card at 390px); the raw Tabs assembly retired onto the P092-sanctioned SegmentedTabs.
- **PaneHeader.vue:140-142** — `tan(atan2())` as a CSS length-ratio identity so the shrink law survives the fluid clamp and self-neutralizes on phones; `@supports`-gated with its consumer. MQ-1 indicts this choreography's PRM arm, not its mathematics.
- **PaneSlot.vue:12-23** (adopted from reader 2) — the dev-only `mode="out-in"` failure documented with tranche and evidence path; the kind of note that saves the next reader a day.
- **Provenance quality of the corpus**: first-party, live-probed, committed reproductions (probe-C-*.mjs/.test.ts beside the challenges); of ~60 line-cites this seat re-checked, the misses are a handful of off-by-ones and two wrong-line-in-right-file — plus the three cells killed above. C-1 remains the find neither a design-only nor a library-only reading would have surfaced.

## UNPROVEN-NEEDS-LIVE residue — visual-audit inputs

1. **D-01/D-11 rendered geometry** — `splitPct [50,50]`, the ~158.78px (30.85%) empty plate, `scrollHeight 513 === clientHeight 513`. Mechanisms proven static (shell.css:133-137; AdminPane.vue:2; foundation.css:366/:368); pixels need a browser. The companion-emptiness limb of D-01 is a state claim (empty visitor library — the default and every shipped capture); the §7/OBC/PR-04 pillars are unconditional and need nothing.
2. **D-02 selector rects at 390/320.**
3. **D-07 RTL rect table** (8.00px → 0.00px) — mechanism certain, numbers live-only.
4. **D-12's `min-h-0` limb** — one computed-style read of the glass Card root's `display` decides it.
5. **`.search-seated` target node** — root `.input-bar` vs forwarded onto the inner input; not decidable from minified dist; D-15's forced-colors note assumes the root.
6. **Forced-colors, whole component** — D-15's own capture never entered forced-colors mode (chromatic aurora visible in the shot); treat as UNMEASURED. Note the Dock r3 precedent: forced-colors probes need the two-engine cell.
7. **C-9's KeepAlive-eviction prune-loss race** — all preconditions static-confirmed (`:max="6"` vs 11 left keys; the `?.` at usePalettePorts.ts:123); the window was never manufactured; **dissolves structurally when MT-AU1 (7) deletes the handle** — verify by absence after that wave.
8. **D-09's >50-user truncation** — needs a seeded backend; the missing cursor is proven.
9. **The type-cycle CI-breakage prediction** (one port-typed expose member breaks vue-tsc) — plausible, untested; moot after MT-AU1 (7).
10. **L-5's 43-module dev-fetch figure** — artifact sizes exact; the chunk count not re-derived.

## Identity guards & citation-drift ledger (addendum, E-3 register)

- **One identity each, never re-booked**: C-1 ≡ MT-AU1 Δ-1/D-1/L-19/C-2 · D-03/C-4 ≡ MT-AU1 L-5 · L-7/C-9/D-13 ≡ MT-AU1 L-1 · error-inversion ≡ MT-AU1 Δ-2/C-21 · post-prune-guesswork ≡ MT-AU1 Δ-1 leg · dead-surface ≡ MT-AU1 scope (1)/(2) · L-5 ≡ W-HYGIENE demo/ui extirpation · L-11's globs ≡ ColorSpaceSelector L-5(a) · L-16 ≡ ColorSpaceSelector L-14 (W-HYGIENE config parity).
- **Canon line-drift**: this seat's VC anchors run ~2-6 lines below the readers' (law 2 at :27 not :32; flourish law at :101 not :99; one-H1 at :85 not :83; direction at :151 not :150; Admin §7 at :218). Substance identical in every case; re-resolve anchors before quoting.
- **Both PRM guards' scroll-driven blind spot (MQ-1) binds every wave touching AdminPane's root class** — no wave may cite the guards as its reduced-motion evidence for scroll-driven motion.
- **The a11y gate's two structural blind spots are now written down**: heading-name substring match (lets "Users 0" pass `{name:"Users"}`) and the INPUT placeholder fallback (lets the nameless SearchBar pass). W52's close must not inherit them silently.
- **Wave-routing disclosure**: MT-AU1 and W52/W47 assignments follow the standing registry and V-PRIME owner table; the MQ-1→W47 and audit-debounce→W52 routings are this seat's own and are flagged as such for the next formation to ratify. These dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

## DISSENT — preserved, not ruled away

1. **Reader 2's D-10 reservation**: whether the tray↔panel boundary is a "major section" is an interpretation of PR-35's prose. Ruled against (the OBC anatomy sentence separates the members), but if W52's strip measurement adopts a different section taxonomy, the 12-vs-24 row re-opens at the row-rung instead — it fails there too (12 ≠ 16-wide).
2. **Reader 1's C-2 BLOCKER position**: preserved — "the filter that understates the prune count need never have been typed by the admin." The coupling is real; the ruling keeps the blocker weight on C-1 where the cure lives (G-10 makes the compound unrepresentable).
3. **Reader 1's K9 vs reader 2's C-5 kill**: the two "kills" of one claim disagreed on facts; the loser's false limb is recorded in the register (kill #9) as a caution that a kill's own evidence needs the same re-derivation as a find's.
4. **The "58 assertions" repetition** (corpus → reader 1 verbatim) is logged as a citation-inheritance event per the ColorSpaceSelector DISSENT-1 precedent — evidence about the fleet, not the tree.

## Closing verdict

**DEFECTIVE — ADJUDICATED.** 44 rows ruled: 2 BLOCKER identities (both already owned by MT-AU1 — the prune that lies about its own scope, and the console that costumes absence-of-authority as an empty roster), 20 MAJOR, 18 MINOR, 4 INFO; 16 claims killed with written refutations; 15 missed rows folded in, 6 onto standing identities. The operative core's cures are fully owned by **MT-AU1** (delta-unified, born-RED); the composition core (companion 50%→0, one grammar, one rhythm, honest housing) awaits **W52**; the shell rows (H1, schema typing, MQ-1's PRM arm) go to **W47**; `.search-seated`/ASK-D stays GLASS-OWNED on the standing relay; the shared-ref class (searchQuery + expandedId) is the sole NO-WAVE-OWNER row, pinned to MT-AU1's banked port-split re-trigger for the next formation. No wave is stamped by this record; every disposition carries its original id for life.

— end of apotheosis. The challenger corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
