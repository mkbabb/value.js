Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequencePlayhead; receipt read from this seat's own system prompt, not inherited).

# kf-SequencePlayhead — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-10 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (the SACRED STALE checkout, 41 commits behind `origin/master` `81a56990`), worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus** `audit/kf-components/SequencePlayhead/challenge-{D-design,L-library,C-consumption}.md` plus **two hostile re-reader returns** (design-and-usage = "RR-A"; library-and-code = "RR-B"), delivered as separate registers: RR-A 21 confirmations · 7 kills · 5 misses; RR-B 24 confirmations · 12 kills · 8 misses.

**Supersession note.** This file replaces a 2026-08-09 draft at the same path that adjudicated a *different, superseded* re-reader return (a single consolidated register). Nothing from that draft is inherited: every ruling below was re-derived by this seat against the bytes this session. Where this record and that draft converge (the two-clock MAJOR, the manifest triangulation), the convergence is independent re-derivation, recorded as corroboration only.

**Substrate (re-derived, not inherited).** `git diff origin/master -- demo/scenes/sequence/ demo/styles/ demo/DESIGN.md src/animation/orchestration/sequence/` → drift confined to `lifecycle.ts`/`transport.ts` (4 lines, export keywords/import path; the clamp lines cited below identical at both trees). **Every demo anchor below is origin-current**; the worktree-vs-local-HEAD drift on SequenceTarget.vue is the Glass-7 `MetricBadge`→`Metric` migration already landed at origin. **The manifest is where the trees disagree — triangulated by this seat**: worktree `package.json` has NO `@mkbabb/glass-ui` line and pins `@mkbabb/value.js: 4.0.0` (:69); local HEAD `8281638c` declares `@mkbabb/glass-ui: 6.0.0` (optionalDependencies) with lockfile entries at :61/:614/:616 (6.0.0 tgz) and `@mkbabb/value.js: ^3.1.0` — a range the installed 4.0.0 does not satisfy; `origin/master` declares `@mkbabb/glass-ui: 7.0.0` exact devDep (:77) and `@mkbabb/value.js: 4.0.0` (:70); installed `node_modules`: glass-ui 7.0.0, value.js 4.0.0. Decisive for rulings 2 and 3.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new MAJOR, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; sole write = this file. Instruments this session: whole reads of SequencePlayhead.vue (87 L), SequenceTarget.vue (252 L), SequenceTarget.css (259 L), SequenceAxis.vue (49 L), SequenceScrubber.vue (162 L), useSequenceDemo.ts (482 L), useSequenceInstrument.ts (45 L), useDragScrub.ts (150 L); sequence.ts:140-330 (`_duration`/`progress`/`add`/`seek`/`setTargets` — the decider for both MAJORs and the C-4-CURE kill); transport.ts grep (`applySequenceAt` :35 child-clock clamp, `foldPhase` :117 master clamp); design-idioms.css rail/ball/stage-field block; style.css z-contract (:18-40) + violet-authority block (:130, :155-163); DESIGN.md:48-60; R-tranche demo-styling.md F8 read verbatim (:134-151); the manifest triangulation above (`git show` ×3 trees); installed glass-ui 7.0.0: timeline barrel d.ts (exactly `GlassTimeline` + 3 types), exports map by node (**73 keys, sole wildcard `./fonts/*`**), `radius.css` (`--radius-pill: 9999px`, inside an `@theme` block — Tailwind-v4-processed, a nuance no axis stated), a11y-overrides.css read verbatim (PRM blanket = animation/transition ONLY; forced-colors block = focus rings + `.hairline-accent`/`.glass-dock`); installed value.js: math.js 1110 B (clamp = `Math.min(Math.max(e,t),n)`, NaN-transparent); **a node run of the product's own shipped converter** (`dist/subpaths/{css,color}.js`) for the D-7 decision; greps: `@property` demo census (exactly four — `--lit`, `--axis-active`, `--ball-p`, `--rail-width`; no `--playhead-p`, no `--seq-glow`), `--z-[a-z-]*:` demo census (exactly three), `--specular|--shade` (ONE hit tree-wide: the DESIGN.md prose itself), `--radius-pill` (zero demo declarations; 10 demo consumers, SpringHeatmap.vue:321 the only fallbacked one), `--stagger-max`/`--scrub-dir`/`setScrubDir` consumer censuses, `forced-colors` (0 tree-wide), `SequencePlayhead|seq-playhead` over src/+test/+scripts/ (**zero** — no test, no gate); banked registry reads: kf-AmigaScene (C-1 ≡ F-1 ruling + the recorded citation-inheritance class), kf-KeyframeTimeline (C-15), census lane-frontend.md:15/:54/:342/:398/:441; spot-reads of all three challenge files where the readers' quotations are load-bearing (D-1's BLOCKER text, D's superlative L-5, C's §1.1 provenance + §5 negative table + §6 repair order, L's :76-81/:313 cure text — all quoted accurately by the readers).

## Scoreboard

**32 adjudicated roster rows — 0 BLOCKER · 3 MAJOR · 12 MINOR · 17 INFO.** RR-A: 21/21 confirmations sustained in substance (2 cells corrected in place), 7/7 kills sustained, 5/5 misses admitted (one promoted, one rescoped-to-banked). RR-B: 24 confirmations — 22 sustained, **2 overruled** (D-7, the F-1 limb of L-13/C-9); 12/12 kills sustained; 8/8 misses admitted (two folded into existing rows, one demoted). **15 killed-claims register entries.** **8 superlatives** (one carrying a qualification). **8 UNPROVEN-NEEDS-LIVE residues** for SS-13. **2 fold-by-identity** to banked records.

**Headline.** The three challenge axes led with a 12px registration error; RR-A found the instrument's real defect an order larger and RR-B missed it entirely — this seat confirms it at the bytes: **the ruler and the playhead measure two different clocks.** The axis names the `[0, 1600ms]` handle domain (`STAGGER_MAX`, useSequenceDemo.ts:74; labels `Math.round(q × staggerMax)`, SequenceAxis.vue:12) while the playhead normalises over `Sequence.duration = 1940ms` (`add()`'s cursor fold, sequence.ts:277-278) — up to **~11.4% of the track width** (~67px), ~5.6× the corpus headline, asserted false by the scene's own prose ("the time grid is NAMED from the same domain the handles + playhead ride", SequenceTarget.vue:152-154). D-1 had both numbers in one sentence — "≈ 39ms of the default 1940ms Sequence duration" three lines under its quotation of the `q * staggerMax` labels — and never compared them (verified against challenge-D-design.md by this seat). And beneath it: **`_duration` has no writer outside `add()`** (exactly three references: :158/:203/:278), so the first handle drag stales the master clock's own denominator.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for the SS-1/SS-2 spec authoring, keyed to the census wave taxonomy (KF.W0/W6/W7/W9 per the banked kf records) with NO-WAVE-OWNER for the sequence scene-repair packet. Original ids for life.

## Ruled disagreements (the seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D-7 (the theme-blind bevel): RR-A KILLED it (wrong contrast pair; conclusion inverts) vs RR-B CONFIRMED at INFO (re-ran D's arithmetic)** | **RR-A WINS — D-7 KILLED (register K-1); RR-B's cell overruled** | Node run of the product's own shipped converter: `oklch(0.56 0.17 295)` → rgb(125.9, 89.6, 203.7) (RR-A's rgb(126,90,204) exact; D's rgb(126,90,216) blue-channel off ~12); bevel = 30% body + 70% white. **Bevel-vs-BODY (the pair that produces a "machined" read on a 1px top/left border of an 8px diamond): LIGHT 3.32:1, DARK 1.90:1** — the machined read is STRONGER in light mode, the exact inverse of D-7's claim, and the two border declarations are dead paint in neither theme. RR-B reproduced D's arithmetic faithfully (bevel-vs-plate 1.36:1 — my run agrees) but inherited D's wrong operative pair; correct arithmetic on the wrong comparison is still the wrong verdict. Survivors: the top-left SILHOUETTE softens against the light plate at 1.36:1 (percept → SS-13 #8); body-vs-plate 4.50:1, no WCAG 1.4.11 exposure; the raw `white` literal rides C-3. |
| 2 | **The glass-ui manifest: RR-A killed the "F-1 restatement" + filed lockfile-drift as a miss vs RR-B "independently re-verified" F-1 (grep package.json → no match) and repeated it in its SUP-E** | **RR-A WINS, then RESCOPED further by this seat — register K-4; RR-B's cell overruled and recorded as a citation-inheritance event** | My triangulation (substrate block): the absence is an **uncommitted worktree deletion**; local HEAD declares 6.0.0 (optional, locked at :61/:614/:616 — RR-A's line numbers verified exact); `origin/master` declares **7.0.0 exact devDep (:77)**. Census SCH-1 ruled F-1 **FALSIFIED at HEAD-of-origin** and banked kf-AmigaScene C-1 ≡ F-1 as RESCOPED-BY-SUBSTRATE (cure = **KF.W0 §B-12**) with a recorded citation-inheritance event of exactly this shape — days before either reader wrote. RR-B grepped the dirty worktree, called it "HEAD", and re-verified a banked-FALSIFIED row against the tree that manufactured it. RR-A's own miss is also incomplete: "a clean `npm ci` yields 6.0.0" is true only of the stale local checkout — at origin it yields 7.0.0, and RR-A never consulted the census substrate ruling either. Bonus wrinkle (this seat's own): RR-B's SUP-E claims the value.js `4.0.0` pin holds "in both HEAD and worktree" — false at local HEAD, which declares `^3.1.0`, a range the installed 4.0.0 does not even satisfy. **Any manifest claim in a kf corpus is inadmissible without a named tree** — now proven three times. |
| 3 | **D-8 (PRM opt-out for the comet): RR-A CONFIRMED at MINOR ("C's negative is under-reasoned") vs RR-B demoted to INFO siding with C ("a static gradient attached to essential motion; charging it under a motion query stretches the contract")** | **RR-B WINS — INFO (roster D-8 row); RR-A's dissent preserved** | Both readers' shared FACT verified verbatim at a11y-overrides.css: the PRM blanket neutralises `animation` + `transition` only, so a transform driven by a changing custom property genuinely survives PRM — banked as the tranche-level datum (KF.W9), it convicts elsewhere even though not here. On chargeability C's read is stronger: the file declares zero timed motion (my grep: 0 `transition`, 0 `@keyframes`); the sweep IS the clock (essential — D's own K-2 concedes); the comet is static paint rigidly attached to it. The scene's demonstrated PRM pattern (boot killed at SequenceTarget.css:238-243, JS-gated at useSequenceInstrument.ts:29-32) makes a one-rule `::after` degrade a reasonable *offer*, not a charge. |
| 4 | **RR-A's D-3/L-3 cell adopted D's "editing either token produces no visible change"; RR-B killed that limb (D-3-OVERREACH)** | **RR-B WINS — RR-A's cell corrected in place (register K-10)** | Inside `.seq-track`'s private context (container-type :114 → layout containment → stacking context), `.seq-handle`/`.seq-ball` at z:2 (:135/:200) order against `.progress-rail` (abspos, z-auto, design-idioms.css — a sibling in the same context, SequenceTarget.vue:93/:96/:114): set `--z-seq-handle: -1` and grips + travellers drop behind the rail — visible. Only the playhead↔handle RELATION is inert, which is L-3's exact phrasing. The distinction is load-bearing: L's :313 cure ("remove whichever member is inert") would, under D's over-broad reading, delete the member that is NOT inert. |
| 5 | **Severity contests** (C-3: RR-A MAJOR vs RR-B MINOR · D-3/L-3: MAJOR vs MINOR · D-12: MINOR vs INFO · L-9/C-6: MINOR vs INFO · L-10: INFO vs MINOR) | **MINOR · MINOR · INFO · INFO · INFO** | C-3: the codex lie is real and verbatim (DESIGN.md:52-57 names "the sequence playhead cap" as a `--specular` consumer; my grep: the token exists nowhere; :63-64 ship the forbidden literal) but it is prose-vs-stylesheet drift with zero user-facing break — MINOR pending an owner ruling on DESIGN.md's bindingness; RR-A's "strongest single finding" honored as prose, not as grade. D-3/L-3: a dead documented micro-contract whose visible consequence (playhead paints over grips) is arguably the conventional playhead behavior — MINOR. D-12: "brightens with --seq-glow" is satisfied by a step; no ramp is promised; cure trivial — INFO. L-9/C-6: a dead guard — INFO. L-10: idiom divergence, zero functional harm — INFO. |
| 6 | **RR-A's D-6 alpha cells ("8.75% at rest … ~28% while scrubbing")** | **Rest figure ADOPTED; scrub figure CORRECTED to ~37.5%** | Re-derived: at p=0 the trail spans padding-box x∈[48, 80]; label column x∈[16, 68] → 20px overlap (both readers agree, C-8's "32px" already killed). At the column's right edge (x=68): gradient fraction toward the hot end = 20/32 = 0.625; rest = 28% × 0.625 × opacity 0.5 = **8.75%**; scrubbing = (28%+32%) × 0.625 × opacity 1.0 = **37.5%**, falling to 0 by x=48. RR-A's ~28% and RR-B's carried "≈26%" both understate the scrubbing case. |
| 7 | **RR-A's missed-2 (`reseatRow` stales `_duration`) filed at MINOR** | **PROMOTED to MAJOR (roster N-2)** | Three grounds, all from the bytes: (a) the drag is the scene's headline interaction by its own declaration ("The headline Sequence refinement — the GSAP-timeline gesture", useSequenceDemo.ts:316-324); (b) the falsified state is terminal and visible — drag row 5 to 1600 and p=1 leaves its lane frozen at 340/900 = **37.8% of its glide** (child ends at 2500ms, clock caps at 1940: `foldPhase` clamp transport.ts:117, child clamp :35 — both verified); converse, all rows to 0 → the playhead sweeps **54% dead track** after the board settles; (c) the seam is engine-level, not demo-level — `entries` is a public field, `_duration`'s only writer is `add()` (:278, monotone), there is no recompute/re-time API, and the demo's direct mutation is the library's own dogfood. The `seek(progress × duration)` at :346 compounds with the stale denominator. |
| 8 | **RR-A's missed-1 (the two-clock domain error) — absent from RR-B entirely** | **ADMITTED at MAJOR — the record's headline (roster N-1); RR-B's silence recorded** | Re-derived whole: staircase delays 0/260/520/780/1040 (useSequenceDemo.ts:101-111) + `ROW_DURATION 900` (:62) → `add()` folds cursor to **1940ms** (sequence.ts:277-278); `progress = _time/duration` (:217-219). Axis labels 0/400/800/1200/1600 (SequenceAxis.vue:12 × SequenceTarget.vue:155/:66); handles AND gates drawn at `at/1600` (SequenceTarget.vue:98, :81). Consequences exact: row-5's lane fires with the playhead at 1040/1940 = 0.536W against a gate drawn at 0.650W (**Δ ≈ 0.114W ≈ 67px** at W≈592px — at the very moment the scene exists to demonstrate); at t=1600ms the playhead reads 0.825W under the tick labelled 1600; p=1 lands at a time the ruler cannot name. The prose denial is verbatim (SequenceTarget.vue:152-154; SequencePlayhead.vue:21-22 "the SAME axis the handles ride"; SequenceAxis.vue:2 "The master-clock axis ruler"). Crucially it **survives every cure the corpus prescribed** — the D-1/L-2/C-§6.1 grid-placement repairs fix the 12px origin and leave the clock untouched. |
| 9 | **RR-B's missed rows M-2/M-5 vs the existing roster** | **FOLDED into their parent rows, credited** | M-2 (ALL ten `var()` fallbacks unreachable, not just the six `--ball-tone` reads) is the full-strength form of D-14/L-5/C-2 — verified: `--ball-tone` + `--seq-glow` unconditional on `.seq-target` :7-11 (an ancestor; the per-row overrides sit on non-ancestor `.seq-row`s), `--scrub-dir` + `--playhead-p` written inline on every render (SequenceTarget.vue:63; SequencePlayhead.vue:10) — 10 dead fallbacked reads vs 3 bare load-bearing ones; the exact-move cure adopted. M-5 (delete `z-index: var(--z-seq-playhead)` — the track's own containment already makes it a stacking context, and z-auto tree-order puts the playhead UNDER `.seq-rows`' positioned descendants, restoring the documented order in one deletion) is the D-3/L-3 row's cure — verified: `.seq-playhead-track` (SequenceTarget.vue:70) precedes `.seq-rows` (:74). |

## Adjudicated defect roster

Severity · row (original ids for life; N-* = net-new from the re-read, provenance noted) · disposition. Anchors are origin-current; re-resolve before executing any cure.

### MAJOR

- **N-1 (RR-A missed-1) — the ruler and the playhead measure two different clocks: axis/handle/gate domain [0, 1600ms] vs master clock [0, 1940ms] — up to ~11.4% of track width, denied by the scene's own prose.** Ruling 8, all arithmetic re-derived by this seat. Scene-level identity (SequenceAxis + SequenceTarget + useSequenceDemo + the engine's duration fold), booked HERE — the playhead is the reading surface; any forthcoming kf-SequenceAxis/kf-SequenceTarget adjudication folds its domain finding to this row (anti-rename). Cure is spec-work, adjudicated separately from the defect: either the axis names the true duration domain, or the playhead normalises to the drawn `STAGGER_MAX` domain, or the design collapses the two (e.g. duration-derived labels); born-RED gate: assert the axis terminal label equals `sequence.duration`. **NO-WAVE-OWNER (sequence scene-repair packet, SS-1/SS-2 intake).**
- **N-2 (RR-A missed-2, promoted MINOR→MAJOR — ruling 7) — `Sequence._duration` is never recomputed on re-time; the first handle drag permanently stales the playhead's denominator.** `reseatRow` (useSequenceDemo.ts:325-349) mutates `entry.at` in place, re-sorts, and seeks by the stale product; `_duration` = exactly three references (sequence.ts:158/:203/:278), written only in `add()`, monotone non-decreasing, private, no recompute hook. Dual seam booked as one row: the engine's re-time invariant gap AND the demo's direct entry mutation ("dogfooding the engine's position model"). Witnesses: p=1 with a lane frozen at 37.8% of its glide; 54% dead sweep. **NO-WAVE-OWNER (scene-repair packet — with the engine-side rider named for the spec authoring: the repair needs a library-side seam, not just a demo patch).**
- **D-1/L-1/C-1 — the playhead and the ruler resolve two different horizontal origins: at p=0 the head stands 0.75rem (12px) right of the 0ms tick, decaying linearly to 0 at p=1.** Re-derived: `gap: 0.5rem 0` → stage column-gap 0 (SequenceTarget.css:44, no later re-declaration); axis = grid item at column 2 → padding-box + 1rem + 3.25rem = 4.25rem (SequenceAxis.vue:22); playhead = abspos, placement auto → padding-box + 1rem + `--track-inset`(4rem) = 5rem (SequencePlayhead.vue:24-28; the `--col-gap` term describes the `.seq-row` subgrid :73, not the stage); right edges agree (:29 vs padding-right), so origin AND geometric scale differ — RR-B's wording correction adopted (still an affine map; the point is that no single translation repairs it). BLOCKER demoted per ruling in K-6; whether the playhead at least matches the *handles* is the quarantined subgrid-gutter branch (SS-13 #1 — with RR-B's sharpening that under the split-gutter branch it matches NEITHER). **MAJOR. NO-WAVE-OWNER (one commit with the D-10/L-2 and D-2/L-4/C-5 rows; the corpus's own cure is killed at K-8 — use the K-8 sound forms).**

### MINOR

- **D-3/L-3 — the documented `--z-seq-playhead: 1` / `--z-seq-handle: 2` ordering contract is dead: `container-type: inline-size` on `.seq-track` (:114) traps the handles'/travellers' z:2 in a private stacking context; the playhead subtree (z:1, paint step 9) always paints over `.seq-track` (positioned z-auto, step 8) under the `.seq-stage.cascade-chase` root (`will-change: transform`, :217).** Chain verified from bytes; D's "both tokens do nothing" limb corrected (ruling 4). Cure = RR-B's M-5 one-deletion (fold, ruling 9): drop SequencePlayhead.vue:31 and tree-order restores the documented relation; `--z-seq-handle` keeps its live internal job. Live paint witness = SS-13 #3. **NO-WAVE-OWNER.**
- **C-3 — DESIGN.md:52-57 names the playhead cap a `--specular` consumer; the token was never defined anywhere (my grep: one hit tree-wide, the prose itself) and :63-64 ship the raw `white` the same paragraph forbids.** The U lane-25 F7 proposal landed in the codex and not in the stylesheet — a named-but-undefined material register. Held MINOR pending the DESIGN.md-bindingness ruling (ruling 5; RR-A's MAJOR preserved as dissent). One cure with the D-7 residue: define `--specular`/`--shade` as `light-dark()` pairs, repoint :63-64. **KF.W6 (material-register/token packet; cross-repo relay if the pair belongs in glass-ui).**
- **D-4 — "the LINE (full master red)" (:3) names a colour the token authority expelled**: `--ball-tone` → `--color-progress` → `--accent-kf` = `light-dark(oklch(0.56 0.17 295), oklch(0.74 0.13 305))` — violet in both arms (style.css:130/:155-163, read by this seat; the expulsion prose is explicit: "so red exits the chrome entirely"); no per-row override reaches the playhead. Family: "master red" survives at SequenceScrubber.vue:14/:132. **NO-WAVE-OWNER (prose-truth sweep).**
- **D-5/L-8/C-4 — "Pure CSS over the engine's `progress` — no per-frame JS" (:7-8) is false: `progress` is a Vue ref written every mirror frame (useSequenceDemo.ts:179-181, :193-195 via the engine's own RAFPlayback loop) and patched through an inline `:style` (:10) via a child re-render.** Comment-accuracy, not performance (SequenceTarget re-renders the Metric every frame regardless). The "sole/one motion element" framing is killed at K-2 — the playhead is one of at least four per-frame Vue consumers. **MINOR. NO-WAVE-OWNER (prose sweep; the "make it true" cure is killed at K-9).**
- **D-6/C-8 — the 32px comet `::after` escapes the track's own inset and washes the right 20px of the label column at the rest state the scene mounts in (p=0), unclipped** (no paint containment from `container-type`; no `overflow` until the Card). Alphas per ruling 6: **~8.8% at rest, ~37.5% while scrubbing** at the column's right edge, over the `@{{at}}ms` sub-line (opacity 0.8, SequenceTarget.css:100-103) — read exactly while scrubbing. `pointer-events: none` keeps it interaction-safe. Percept = SS-13 #2; one-line clip or fade-at-origin cure. **NO-WAVE-OWNER.**
- **D-2/L-4/C-5 — `top: calc(0.75rem + 1.25rem)` hardcodes the DESKTOP axis box; SequenceAxis compresses to 0.95rem below 1024px (:43-47) and the playhead has zero `@media`** — the same breakpoint is a first-class band (SequenceTarget.css:249-259). The coupling is the defect; the described symptom is refined by K-11: on mobile the LINE's top opens a 4.8px gap to the ruler while the diamond tip (reaching 4.66px above the line top) stays near-flush — a pair of slivers beside the tip, not a detached head, and desktop (not mobile) is the 2.26px-overlap case. **MINOR. NO-WAVE-OWNER (one commit with D-1).**
- **D-10/L-2 — `.seq-stage`'s `padding: 0.75rem 1rem 1rem` (:45) re-typed as four raw literals across a component boundary (:26-29), untokenized, untested — while the columns ARE tokenized four lines away; the identical pattern already broke on the fifth constant (the axis height above).** All four literals are CORRECT today (C's §5 padding-double-count refutation verified — the containing block is the padding box, crossing it is required). Latent coupling only; no gate exists (my grep). **NO-WAVE-OWNER (with D-1; RR-B's M-6 names the natural home — see N-8).**
- **D-11/L-6/C-7 — `--playhead-p` is unregistered while its structural twin `--ball-p` is `@property`-registered eight lines away with the rationale stated (:13-18); the inline write makes the `, 0` fallback at :46 unreachable, and the real failure mode is IACVT poisoning the whole transform (NaN passes the NaN-transparent clamp).** @property census re-run: four registrations, none `--playhead-p`. Exposure, not live (every writer clamps; `duration ≥ 900` guards the zero-division — C's concession adopted). L-6's stated rationale for the cure is killed at K-12: registration makes the `var()` fallback PERMANENTLY dead — register AND delete the `, 0` + the JS clamp together. **NO-WAVE-OWNER (property-hygiene family).**
- **D-14/L-5/C-2 + RR-B M-2 (folded, ruling 9) — fallback discipline is 100% inverted: all TEN fallbacked `var()` reads in the file are provably unreachable (`--ball-tone` ×6, `--seq-glow` ×4-overlapping, `--scrub-dir`, `--playhead-p`) while all THREE structurally load-bearing external reads are bare (`--track-inset` :28, `--z-seq-playhead` :31, `--radius-pill` :48 — each single-source; a rename yields IACVT → `left: auto`/`z-index: auto`/`border-radius: 0` with no build signal and no test).** The exact-move cure: put the three fallbacks where they are needed, delete the ten that cannot fire. **MINOR. NO-WAVE-OWNER.**
- **N-3 (RR-A missed-3 + this seat's wiring extension) — `--scrub-dir` is a raw per-pointermove sign test with no hysteresis, AND no writer exists on three of the four ways the playhead moves.** Verified: `onScrub` compares against the immediately-prior sample (SequenceScrubber.vue:69-73); `useDragScrub` fires on raw window `pointermove` — no rAF coalescing, no dead-band (read whole by this seat). A 1px trackpad jitter mirrors the 32px comet 64px across the line (transform-origin right + scaleX flip) in a single frame at peak glow. This seat's extension from the same bytes: `setScrubDir` has ONE caller (the drag path — grep verified); `reverse()` playback (useSequenceDemo.ts:261-272), keyboard scrubs (SequenceScrubber.vue:97-111), and gesture end (:89-92) never touch it — so under reverse playback the streak LEADS the head, and after any backward drag it sticks mirrored for the session, against the file's own ":68 always trails the thumb". The flip *mechanism* remains a superlative; its *driver* is under-wired. Witness = SS-13 #7. **MINOR. NO-WAVE-OWNER.**
- **N-4 (RR-B M-4) — the scene runs three inconsistent p→x maps and the travellers never rest on their drawn gates: handle centre = `at/1600 × W` exactly; traveller centre = `row-start × (W − 25.6px) + 12.8px` (ball-size 1.6rem, left-edge-mapped over a shortened domain) → **+12.8px off its gate at row-start 0, −12.8px at 1**, exact only at midpoint — against SequenceTarget.css:183-185/:200's "it RESTS at its start gate … the traveller rests ON its gate".** Verified from :187-199 + design-idioms.css (`.progress-ball` supplies no horizontal margin). Compounds N-1: the master playhead is the element the eye uses to relate all three maps, and the D-1 repair alone does not reconcile them. Scene-level; booked here with the same anti-rename guard as N-1. Percept = SS-13 #6. **MINOR. NO-WAVE-OWNER (instrument-truth packet with N-1/N-2/D-1).**
- **L-7 — the rule comment (:21-22) describes a `left: %` mechanism the file does not contain, contradicted ten lines below by the T.G4 comment that replaced it (:32-34/:41-43).** Two adjacent comments, two accounts, 87 lines. **NO-WAVE-OWNER (prose sweep).**

### INFO

- **D-12 — `--seq-glow` hard-steps 0→1 where :5/:67-69 promise "brightens": binary token (:10/:28-29/:216), zero `transition` in the file — blur 2px→10px and trail opacity 0.5→1.0 in one frame on pointerdown.** INFO per ruling 5. The mechanism claim is killed at K-13: the cure is a plain `transition: box-shadow/opacity` (ordinary interpolable computed values — registration of the custom property is irrelevant), and it survives glass-ui's PRM blanket (box-shadow + opacity are on the permitted transition-property list, my read). **NO-WAVE-OWNER.**
- **L-9/C-6 — `clamp(progress, 0, 1)` at :10 is a provable no-op against every writer (all clamp — useSequenceDemo :180/:199-200/:281/:328 + the engine setter sequence.ts:221-223 and zero-guarded getter :217-219) and passes the one breaking input (NaN) through.** Deleting it (with the D-11 registration) makes the component dependency-free. **NO-WAVE-OWNER.**
- **L-10 — two idioms for one job in sibling files** (scalar + CSS transform here vs finished transform string + hand-synced negative margin at SequenceScrubber.vue:34/:159), the same T.G4 rationale comment pasted on both. **NO-WAVE-OWNER.**
- **L-11/C-10 — unconditional, permanent `will-change: transform` (:51) in a card already holding 8 promoted layers** (5× `.seq-ball` :201, `.cascade-chase` :217, `.scrub-ball` :160 — D's count of 8 verified; C-10's "seven" undercounts); the element's other animated property (box-shadow) repaints the promoted layer regardless. Not a measured regression (both readers correctly decline); cost = SS-13 #5. **NO-WAVE-OWNER.**
- **D-13/L-12 — `grid-column: unset` (:25) is a dead declaration** (not inherited → `unset` = `auto`, already the value; grid-column census: five sites, none matching this element). The charitable reading (documents that placement must stay auto so the containing block stays the padding box) belongs in the comment — and is QUALIFIED by N-8 below: the "nothing CAN set it" reasoning in all three axes is wrong. **NO-WAVE-OWNER.**
- **L-13/C-9 — `border-radius: var(--radius-pill)` is an import-invisible token edge on glass-ui** (zero demo declarations; sole definition in the producer's `radius.css` `@theme` block — reached through the Tailwind-v4 build, a nuance this seat adds; consumed bare while SpringHeatmap.vue:321 shows the defended house form). The **F-1 limb is dead** (ruling 2 — fold to the banked substrate row, KF.W0 §B-12, never re-booked). The "essentially nil" self-demotion is killed at K-14: corner-overlap scaling gives the 2px line genuine ~1px semicircular caps that square silently on a producer rename. Surviving INFO: the version-sensitive edge no import graph or typecheck can see. **KF.W0 fold (F-1 limb) + NO-WAVE-OWNER residue (fallback rides the D-14 row).**
- **D-8 / C-§5 — the PRM contradiction, RULED (ruling 3): no charge here (the sweep is essential, the comet is static paint), but the FACT both readers verified is banked — glass-ui's PRM blanket cannot reach a custom-property-driven transform.** RR-A's dissent (the scene "demonstrably knows the pattern") preserved. **KF.W9 (PRM-unification datum).**
- **D-16 — forced-colors has zero handling tree-wide (my grep: 0), mitigated here by earned `aria-hidden` + two unconditionally-rendered accessible value carriers** (Metric, SequenceTarget.vue:18-24; `role="slider"` + live `aria-value*`, SequenceScrubber.vue:22-27). Datum, not a charge. **KF.W9.**
- **L-14 — the SFC boundary is ceiling-driven, not contract-driven: the header names "≤500L split" as the reason; the real contract is a five-token undeclared cascade** (`--track-inset`, `--ball-tone`, `--seq-glow`, `--scrub-dir`, `--z-seq-playhead` — all declared in a sibling's sheet, none documented as an interface). The correct root-cause naming for D-1 and D-2/L-4. **NO-WAVE-OWNER (framing input for the packet spec).**
- **D-9/C-11 — the `--z-seq-*` off-scale observation is ALREADY ADJUDICATED (R-tranche demo-styling F8, read verbatim by this seat: "No change to the style.css global contract is needed"); C-11 correctly declined to re-file, D-9's re-file is closed (K-15).** This corpus's contribution: the F8 ruling's own premise ("the storyboard creates its own stacking context") is FALSE — `.seq-storyboard` has no position/z-index/transform/containment (my read); the tokens are declared on an element that is not the context that orders them, and the ACTUAL context (`.seq-stage.cascade-chase`, via will-change) is an accident of the chase idiom. A reason to annotate F8, not to re-book D-9. **KF.W6 rider (census §6.3 / z-annotation).**
- **N-5 (RR-A missed-4) — `--stagger-max` is written inline on `.seq-stage` on every render and read by no stylesheet anywhere** (my grep: exactly two hits, the inline write :63 and the same value passed as a JS prop :66). A dead custom property in exactly the flat unprefixed namespace the census flags (lane-frontend:441 — 0 `--kf-*` tokens). **NO-WAVE-OWNER (dead-declaration sweep).**
- **N-6 (RR-B M-1) — `pointer-events: none` at :85 is a dead declaration** (inherited property; :30 on the ancestor track already covers the pseudo — my grep: exactly two hits, :30/:85), **and D's superlative L-5 credits the PAIR** ("interaction-transparent by construction rather than by patch" — quote verified against challenge-D-design.md by this seat): the construction is :30 alone; :85 is the patch. **NO-WAVE-OWNER (with D-13/L-12).**
- **N-7 (RR-B M-3, demoted MINOR→INFO for class-consistency) — `--scrub-dir: 1` at SequenceTarget.css:24 is entirely dead**: `.seq-storyboard`'s only child is `.seq-stage`, which carries an inline `--scrub-dir` binding on every render from a ref initialised to 1 and normalised to ±1 — the CSS declaration can never be observed by any element, and its comment ("the scrubber flips it") describes the inline value, not itself. Same class as the other dead declarations. **NO-WAVE-OWNER.**
- **N-8 (RR-B M-6) — the seam is already stylable from the owning side: Vue applies the PARENT's scope id to a child component's single root element, so `SequenceTarget.css` (loaded scoped at SequenceTarget.vue:252) can target `.seq-playhead-track` today with no API change.** Three consequences adopted: it is the natural home for the stage-geometry rules (killing D-10/L-2's cross-file literals AND D-2/L-4/C-5's mobile divergence — the media query already lives in that file); it sharpens L-14 from lament to move; it corrects the "nothing in the cascade CAN set grid-column here" reasoning in all three axes. **NO-WAVE-OWNER (packet-spec input).**
- **N-9 (RR-B M-7) — `var(--ball-tone, var(--color-progress))` is written out six times in 68 lines of CSS** (:47/:50/:62/:63/:64/:81 — verified) against the house idiom documented at design-idioms.css for exactly this shape; resolve once on the track, read one name six times. **NO-WAVE-OWNER.**
- **N-10 (RR-B M-8) — no test, no gate, no story names this component anywhere** (my grep over src/+test/+scripts/: zero) — the mechanical reason every finding above is silent, and the three axes' shared falsifier (rect-equality of `.seq-axis` vs `.seq-playhead-track`) is verbatim the regression test nobody proposed writing. **NO-WAVE-OWNER (the packet's born-RED gate list starts here).**
- **N-11 (RR-A missed-5, RESCOPED — ruling 2) — the manifest triangulation.** The surviving truth at INFO: local checkout stale (6.0.0 locked), origin migrated (7.0.0 exact devDep), worktree carrying an uncommitted deletion, installed 7.0.0 — so every glass-ui-sourced claim in this corpus happens to be checked against the origin-consistent producer, by luck, not by method. **Fold to the banked substrate row (census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1, KF.W0 §B-12)**; this record contributes RR-B's cell as a fresh citation-inheritance event + the value.js `^3.1.0`-vs-installed-4.0.0 range wrinkle at local HEAD.

**Folded-by-identity (not rows):** (1) both readers' **C-§4 export-surface correction** (lane-frontend S-3 overstates the glass-ui timeline surface; `ScrubberTimeline`/`ContinuousRail`/`ContinuousMarkers`/`geometry.*` exist on disk, unexported, no deep path) ≡ **banked kf-KeyframeTimeline C-15** (→ KF.W7), independently re-verified by this seat (barrel = `GlassTimeline` + 3 types; 73 exports keys; sole wildcard `./fonts/*`) — one identity, C-15 owns it; this corpus contributes corroboration only, and RR-B's SUP-G ("the single most valuable piece of work in this corpus") is re-graded accordingly: valuable, and already banked. (2) The **F-1 limb** everywhere it appears (L-13/C-9, C §1.1's contrast clause, RR-B's SUP-E) ≡ the banked substrate row, per ruling 2.

## Killed-claims register

1. **D-7 ("the machined read exists only in dark mode; the two border declarations are dead paint in light mode") — KILLED, and the kill VERIFIED with the product's own shipped converter (ruling 1).** Bevel-vs-body: light 3.32:1, dark 1.90:1 — the conclusion inverts. RR-B's confirmation cell overruled (correct arithmetic, inherited wrong pair). Residues: light-plate silhouette 1.36:1 (SS-13 #8); the raw `white` (rides C-3).
2. **D-5/C-4's "the one/sole motion element that round-trips through Vue every frame" — KILLED (RR-A).** The scrub-ball's inline transform (SequenceScrubber.vue:32-35), the `toFixed(3)` timecode (:16), and the header Metric (SequenceTarget.vue:18-24) are per-frame Vue consumers in the same scene; the sibling's own comment says so (:152-154). L-8 alone had it right. The core comment-falsity finding is unaffected.
3. **D-17 ("move SequencePlayhead into the S-3/S-4 evaluate bucket; `geometry.d.ts` is precisely the module that would own the p→x mapping") — KILLED twice over (both readers, converging independently):** unreachable (no barrel export, no deep path — re-verified) AND unfit (banked C-15: geometry is weighted-segment math with no percent↔position map). The census "Bespoke, no glass counterpart" bucket (lane-frontend:398) stands; D-17's real survivor is the census self-contradiction (TimelineCaret at :342 vs axis/playhead at :398 — same primitive, two verdicts), which rides the C-15 fold to KF.W7.
4. **The F-1 restatement (L-13's/C-9's "absent from both package.json and package-lock.json") — KILLED at every committed tree (ruling 2).** True only of the uncommitted worktree; local HEAD declares+locks 6.0.0; origin declares 7.0.0. RR-B's "independent re-verification" overruled and recorded as a citation-inheritance event against a banked-FALSIFIED row; RR-A's "npm ci yields 6.0.0" corrected (origin yields 7.0.0); RR-B's SUP-E manifest clause corrected (local HEAD declares `^3.1.0`, not 4.0.0).
5. **L-3's supporting sentence / C-11's parenthetical ("no stacking context interposes on .seq-stage") — KILLED (RR-A, verified):** `.seq-stage` carries `.cascade-chase` → `will-change: transform` (:215-218) — it IS the local context. Knock-on verified: the R.F8 ruling's own "storyboard creates its own stacking context" premise is equally false. Neither error unseats the D-3/L-3 conclusion (step-8-vs-step-9 holds under whichever ancestor roots the context).
6. **D-1's BLOCKER grade + "misses all four drawn quarter rules by 12/9/6/3px" — KILLED (both readers, verified).** `.stage-field-x` rides `.seq-axis` alone (height 1.1rem): the quarter rules occupy y∈[12, 29.6] while the playhead line starts at y=32 — no shared scanline except ~2.26px of rotated diamond tip. The visible signature is a 12px gap against tick LABELS, partly self-masked at p=0 by the first label's edge-hug (SequenceAxis.vue:36-38). MAJOR, as two of three axes graded; D's own text demanding "an explicit owner ruling" to downgrade is answered: this is that ruling.
7. **D-2's third limb ("the arithmetic never accounts for the 0.5rem row-gap — the line overhangs the first row by 8px") — KILLED (both readers).** Spanning the gap is the design: the playhead must run from the ruler it indexes through the lanes it crosses; `bottom: 1rem` is independently exact; the 0.5rem is copied nowhere and has nothing to drift against.
8. **The corpus's own headline cure (L-2's "grid-column: 2; grid-row: 1 / -1 … delete all four inset literals"; C-§6.1's "delete left, right, and the 1.25rem term") — KILLED AS WRITTEN (RR-B, verified against L's and C's actual text by this seat).** With `left`/`right` auto, the abspos track shrink-to-fits over an out-of-flow-only child → 0 inline size → `100cqw` = 0 → the playhead pins at the origin for every p; and `grid-row: 1 / -1` names a line the row-less explicit grid does not have. Sound minimal forms, adopted into the D-1 disposition: **(a)** keep abspos + `grid-column: 2; left: 0; right: 0` (grid-placement makes column 2 the inline containing block; `top`/`bottom` semantics unchanged), or **(b)** the one-token repair `left: calc(1rem + var(--label-col))`. The cure-adjudicated-separately law (the ColorSpaceSelector precedent) applies verbatim.
9. **C-§6.3's "make the claim true by giving the playhead an engine target" as an adjacent fix — KILLED (RR-B, verified):** `Sequence.setTargets` (sequence.ts:310-315) forwards to EVERY entry — it would re-target all five lanes onto the playhead and destroy the `--ball-p` fan-out; a sixth entry whose duration equals the sequence's own total is self-referential under `add()`'s duration fold. Real design work, not a three-line change. The defect (D-5/L-8/C-4) stands.
10. **D-3's "editing either token produces no visible change" — KILLED (RR-B, verified; ruling 4).** `--z-seq-handle` still orders handles/travellers against `.progress-rail` inside the track's context. RR-A's confirmation cell corrected in place; L's ":313 remove whichever member is inert" flagged as the hazard the kill exists to prevent.
11. **The D-2/L-4/C-5 symptom picture ("the cap-kisses-ruler read is lost on mobile" / "a 4.8px gap opens") — REFINED-AS-KILLED (RR-B, verified):** the diamond vertex reaches y=27.34 vs the mobile ruler bottom 27.2 — near-flush (0.14px), with two ~4.8px slivers beside the tapering tip; desktop is the 2.26px-overlap case, inverting D's incursion reading. The coupling row stands; file the coupling, not the picture.
12. **L-6's cure rationale ("registration … makes the `var()` fallback honest") — KILLED (RR-B):** a registered property with `initial-value: 0` always has a computed value, so the `, 0` fallback becomes PERMANENTLY unreachable. Register + delete the fallback + delete the JS clamp, as one edit (C-7 stated the mechanism correctly).
13. **D-12's mechanism clause ("unregistered custom properties are not interpolable, and with no transition there is nothing to interpolate anyway") — first clause KILLED as misdirection (RR-B):** a transition on `box-shadow`/`opacity` interpolates the computed values regardless of the feeding property's registration; a maintainer led by D's clause would add a pointless `@property --seq-glow`.
14. **L-13's "the CSS-layer consequence is essentially nil" — KILLED in the direction that STRENGTHENS the row (RR-B, arithmetic verified):** corner-overlap scaling (f = 2/19998 ≈ 1e-4) resolves the pill radius to ~1px semicircular caps on the 2px line; token loss squares them silently — small, real, invisible to every tool.
15. **D-9's re-file of the `--z-seq-*` scale observation — CLOSED (both readers agree; C-11 upheld):** adjudicated at R.F8, no drift since (z census: exactly three declarations, one sanctioned). The F8-premise correction is an annotation on the standing ruling, not a new booking.

## Superlatives (provenance preserved)

1. **The sweep transform** (:46 `translateX(calc(var(--playhead-p, 0) * 100cqw - 50%))`) — container on the parent, `cqw` consumed by the child (the self-container error checked and absent), self-centring `-50%` against the element's own 2px box, compositor-only; the hand-synced contrast case sits in the sibling (`.scrub-ball`'s `margin-left`). All three axes + both readers; spot-confirmed here.
2. **The diamond's transform order** (:61 `translateX(-50%) rotate(45deg)`) — translate resolves against the un-rotated box; the common inversion throws the head 4√2 ≈ 5.66px off diagonally. D's axis alone checked the composed matrix; both readers verified; adopted.
3. **`aria-hidden="true"` is earned, not merely present** (:9) — the value survives in two unconditionally-rendered accessible forms (Metric; `role="slider"` + live `aria-value*`), verified by this seat; the decision is load-bearing (it is what holds D-16 and the PRM row at INFO). All three axes.
4. **Zero teardown surface, by construction** — one import, one `defineProps`, two divs, one binding; nothing to leak in a 1400-line scene directory carrying pointer capture ×2, a rAF mirror, five engine targets, a machine subscription and a global keydown.
5. **The T.G4 compositor claim holds by enumeration** — :46 is the file's only per-frame-varying declaration; box-shadow varies once per gesture, `scaleX` once per direction change; nothing layout-inducing per frame. "The comment above it overclaims; the CODE does not" (D) — the sharpest sentence in the challenge corpus, and true.
6. **The comet flip mechanism** (`right: 50%` anchoring the trail on the 2px line's exact centre + `transform-origin: right center` + `scaleX(±1)`, with `scaleX(0)` closed at the writer) — the mechanism is excellent; **its driver is under-wired (roster N-3): the qualification is now part of the superlative.** D + L found the mechanism; RR-A found the driver; this seat found the third limb (no writer on reverse/keyboard/end).
7. **The one dependency edge the tools can see is the correct one** — `@mkbabb/value.js/math`, a declared subpath of a declared dependency, 1110 import-free bytes; the R1 `parseCssColor` crash class provably unreachable from this component (C's measurement, re-verified). Carried with ruling 2's caveat: the *manifest* half of C's sentence is tree-dependent and was stated without naming the tree.
8. **The corpus's kill sections survive hostile re-reading** — L's K-1/K-4, C's §5 negative table (including the padding-double-count refutation this seat re-verified), and D's K-2 all re-derive clean; the one inter-axis contradiction (D-8 vs C-§5) is ruled above rather than papered over. RR-A's closing observation adopted as this record's own: three axes produced 42 findings and 111KB against an 87-line file whose *shape* is right — every genuine defect is a failure of coupling, prose, or the numbers the file inherits, which is why the repair list is a packet and not a rewrite.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The three-rect measurement** — `getBoundingClientRect().left` of `.seq-axis`, `.seq-playhead-track`, and one `.seq-track` in a single mount: settles the 12px origin claim, the subgrid-gutter branch (4.25 / 4.625 / 5rem — under the split-gutter reading the playhead matches NEITHER the ruler nor the handles, RR-B's sharpening), and N-1's ~67px gate offset in one pass.
2. **The comet wash percept** over the `@{{at}}ms` sub-line at p=0 (geometry exact: 20px overlap at ~8.8% rest / ~37.5% scrubbing) — does it read as a smudge, especially light theme.
3. **Paint order at a playhead↔handle crossing** — scrub across a grip: does the comet tint it (spec-derived answer) — also the before-frame for the D-3 one-deletion cure.
4. **The diamond-tip vs quarter-label collision** — desktop tip incursion 2.26px into the tick band; horizontal near-miss ~9px at p≈0.25 against a ~26px label: one screenshot at a quarter position.
5. **The will-change layer cost** — DevTools layer count + scrub trace with/without the 8 hints.
6. **N-4's percept** — does a ±12.8px straddle at the rail extremes still read as "the ball rests on its gate" (decides whether the D-1 repair is the whole registration fix).
7. **N-3's witnesses** — trackpad micro-jitter mirror-flicker at peak glow; the streak LEADING the head under `reverse()`; the stuck-mirrored streak in forward play after one backward drag. Cheap, decisive, born-RED material for the wiring cure.
8. **D-7's residue** — the light-theme silhouette softness (1.36:1 top-left edge): a looking question only; the ratios are computed and closed.

## Identity guards (fold-by-reference — never re-booked)

- **The F-1 limb ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1** — one identity, FALSIFIED at HEAD-of-origin, discharged at KF.W0 §B-12. This record contributes RR-B's citation-inheritance event + the local-HEAD value.js range wrinkle to that row's pattern ledger.
- **The C-§4 export-surface correction ≡ kf-KeyframeTimeline C-15** (→ KF.W7) — one identity; this record contributes independent re-verification (73-key map, barrel enumeration) and D-17's census-contradiction observation.
- **N-1 and N-4 are scene-level**: any forthcoming kf-SequenceAxis / kf-SequenceTarget / kf-SequenceScrubber adjudication MUST fold its domain-mismatch and gate-offset findings here (anti-rename; this row is the home, the siblings are parties).
- **N-2 has no banked engine identity** — the adjudicated registry carries no `Sequence`/`_duration` row (checked); first booking is here, engine-side rider named.
- **D-9 stays closed under R.F8**; the F8-premise correction is an annotation riding KF.W6, not a booking.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold (§B-12 reconciliation; the worktree glass-ui deletion + the local-HEAD value.js range mismatch are that reconciliation's evidence).
- **KF.W6**: C-3 + the D-7 residue (one material-register cure: define `--specular`/`--shade` as `light-dark()` pairs per DESIGN.md:52-57, repoint :63-64; cross-repo relay if the pair belongs in glass-ui) · the R.F8-premise annotation · the flat-namespace (§6.3) datum.
- **KF.W7**: the C-§4/C-15 fold (it IS that wave's evaluation input); nothing new from this record.
- **KF.W9**: the PRM datum (glass-ui's blanket cannot reach custom-property transforms) · the forced-colors datum · the SS-13 list above.
- **NO-WAVE-OWNER — the sequence scene-repair packet (SS-1/SS-2 authoring must home it):** the **instrument-truth core N-1 · N-2 · D-1 · N-4** (three inconsistent maps + one stale denominator; one repair spec; born-RED gates: axis-terminal-label = `sequence.duration`, three-rect alignment (SS-13 #1), duration-recompute-on-reseat — N-2's cure needs the engine-side rider) · the geometry-coupling commit D-10/L-2 + D-2/L-4/C-5 (via N-8's owning-side home; the K-8 sound forms, never the killed cure) · the stacking row D-3/L-3 (the M-5 one-deletion) · the wiring row N-3 (SS-13 #7 witnesses) · the property-hygiene family D-11 + D-14(+M-2) + L-9 + the dead declarations D-13/L-12 · N-6 · N-7 · N-5 · the prose-truth sweep D-4 · D-5 · L-7 (+ the header's four false assertions as one edit) · D-6's clip one-liner · D-12's transition pair · INFO postures L-10 · L-11 · N-9 · L-14/N-8 (spec framing) · N-10 (the packet's test obligation: the falsifier-as-regression-test).

## Verdict

**ADJUDICATED.** The two hostile re-readers between them dismantled the challenge corpus's grades and cures while confirming nearly all its facts — and then contradicted each other at three points, all three ruled here with this seat's own instruments: the bevel kill stands (verified with the product's own shipped color converter — the "light-mode dead paint" claim inverts to 3.32:1), the manifest "re-verification" falls (a banked-FALSIFIED census row re-confirmed against a dirty worktree — the citation-inheritance class the registry has now recorded across multiple corpora), and the PRM charge resolves to a banked datum rather than a defect. The re-read's real contribution survives everything: a playhead has one job — relate a position to a named grid — and this one is 12px off its ruler's origin, up to 11.4% of the track off its ruler's clock, ±12.8px off its travellers' rests, stale-denominatored after the first drag of the scene's headline gesture, and mirror-stuck in three of its four motions; five independent falsehoods on one 2px line, none visible to any gate because nothing in the tree names `seq-playhead`. The code around those numbers remains better than its prose — the transform idiom, the earned `aria-hidden`, the zero teardown surface are real superlatives — which is exactly why the disposition is a repair packet and not a rewrite. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids (D/L/C for life; N-1..N-11 for the re-read's net-new rows), for life.

— end of apotheosis. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3; the superseded 2026-08-09 draft at this path is replaced, not amended, per the supersession note above.
