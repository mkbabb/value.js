Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequencePlayhead; receipt read from this seat's own system prompt, not inherited).

# kf-SequencePlayhead — adjudicated apotheosis r3 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-11 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale, 2026-07-28), `origin/master` `81a56990`, worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus (r3)**: two fresh hostile re-reader returns over `audit/kf-components/SequencePlayhead/challenge-{D-design,L-library,C-consumption}.md` — **reader-DU** (design-and-usage: 24 confirmed rows · 5 kills · 8 misses m1–m8 · 6 superlatives · 7 unproven) and **reader-LC** (library-and-code: 21 confirmed rows · 7 kills · 8 misses M-1..M-8 · 8 superlatives · 6 unproven). Both returns consumed whole; the C challenge file re-read where the readers' id mappings and the §6.1 cure text are load-bearing.

**Supersession (recorded, nothing lost).** This record supersedes the 2026-08-10 **r2** apotheosis at this path (which adjudicated a *different* pair of returns, RR-A 21c/7k/5m · RR-B 24c/12k/8m, and itself superseded the 2026-08-09 r1 single-register draft). **All r2 identities — the original D/L/C ids and N-1..N-11 — are preserved for life (anti-rename) and carried in full below**; r2's rulings 1–9 and killed-claims K-1..K-15 are carried in force, five of them re-fired against this corpus's readers. r3 repairs r2 in **exactly two places**: (a) **the D-1 cure preference** — reader-LC's M-2 root-cause derivation (verified at the bytes this session) re-homes the defect to `SequenceTarget.css:44` and supplies a one-token cure sound under BOTH subgrid-gutter branches, superseding r2's K-8 sound forms (which stay valid as alternates but bind the playhead to the axis only and leave the gutter branch live); (b) **D-15 (RTL posture) was silently absent from r2's 32-row roster** — neither booked, killed, nor folded (grep of the r2 record: 0 hits) — admitted here at INFO as a tranche-level datum. Where r2 content is restated it was re-verified this session except where marked *(r2-carried)*.

**Substrate (re-run by this seat).** `git log -1` → `8281638c`; `git rev-parse origin/master` → `81a56990`; `git diff origin/master --stat -- demo/scenes/sequence/ demo/styles/ demo/DESIGN.md src/animation/orchestration/sequence/` → drift confined to `lifecycle.ts`/`transport.ts` (4 lines, export keywords/import path) — **every demo anchor below is worktree ≡ origin-current**, reproducing r2's substrate exactly. The manifest triangulation (worktree deletion / local-HEAD 6.0.0+`^3.1.0` / origin 7.0.0-exact+4.0.0 / installed 7.0.0+4.0.0) is r2's ruling 2, carried; no reader in this corpus re-offended on it.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SequencePlayhead.vue (87 L), SequenceTarget.vue (253 L), SequenceTarget.css (259 L), SequenceAxis.vue (49 L), SequenceScrubber.vue (162 L), useSequenceInstrument.ts (45 L); useSequenceDemo.ts:55-120/:165-205/:280-350 (constants, mirror loop, `scrub`/`reset`/`reseatRow`); sequence.ts `_duration` census (exactly :158/:203/:278) + `progress` getter/setter (:217-223) + `add()` (:271-280); design-idioms.css `.stage-field-x` (:205-211); greps this session — `setScrubbing|setScrubDir` (writers: SequenceScrubber :70/:87/:91 only), `--col-gap` (exactly three: SequenceTarget.css:38/:39/:73), `--stagger-max` (exactly one: the inline write, SequenceTarget.vue:63), `dir="` demo-wide (0) + the inline-family logical-property grep (4 files — decisive against reader-DU's narrower zero), "D-15" over the r2 record (0 — the omission); installed glass-ui 7.0.0 `metric/types.d.ts` read whole (no aria/live-region prop — decisive for N-14); keyframes' own barrel probe (`src/animation/internal/leaves.ts:28` re-exports `clamp`; `src/animation/index.ts` does not re-export leaves — reader-LC's SUP-G detail verified); challenge-C-consumption.md §0/§2/§6 re-read (C-2's true two-limb identity; §6.1's "delete `left`, `right`" cure verbatim — decisive for K-20); a node gamma-encode of reader-DU's own linear triple (0.20842, 0.10128, 0.601977 → bytes **126/90/204**, not 216 — decisive for K-21); trail-alpha re-derivation at the label-column edge (~8.3-8.8% rest / ~35.6-37.5% scrubbing — ruling-6 re-fired). Registry instruments: the r2 record at this path (whole, twice), kf-MatrixEditor r3 (the supersession + booking-kill idiom), kf-KeyframeTimeline C-15 and the census F-1/SCH-1 line (carried folds).

## Scoreboard

**39 roster rows, original ids for life — 0 BLOCKER · 3 MAJOR · 14 MINOR · 22 INFO** (r2's 32 carried; **6 net-new admitted from this corpus's 16 filed misses** — N-12..N-17, each re-derived at the bytes; **8 of the 16 misses are booking-killed as pre-banked** (≡ N-1, N-2, N-4 ×2, N-5, K-6's geometry) and recorded as independent confirmations; 2 become riders; **+ the D-15 registry repair**). **Killed-claims register: 25 entries** — K-1..K-15 carried (five re-fired against this corpus) + **K-16..K-25 new**. **9 superlatives** (8 carried + 1 new; two carrying new qualifications). **10 UNPROVEN residues** → SS-13. **2 fold-by-identity** to banked records (carried).

**Headline.** The r3's structural yield is twofold. First, a **cure upgrade at the record's oldest MAJOR**: reader-LC found what five prior readers and three axes missed — the 12px misregistration's root cause is not in SequencePlayhead.vue at all but at `SequenceTarget.css:44`, where the stage declares `--col-gap: 0.75rem` (:38), derives `--track-inset` from it (:39), hands it to the row subgrid (:73), and then writes `gap: 0.5rem 0` — declining its own token six lines below declaring it. My census confirms exactly three `--col-gap` references, none consumed by the stage's own grid. The one-token cure (`gap: 0.5rem var(--col-gap)`) aligns axis, handles, and playhead simultaneously and **moots the subgrid-gutter branch** every prior record had to quarantine — the leaf's `--track-inset` was right all along; the parent regressed underneath it. Second, the **registry's recurring failure mode fired eight more times**: both readers filed the two-clock domain error and the stale-`_duration` row as "missed by all three axes" — true of the axes, false of the registry, which banked both as N-1/N-2 on 2026-08-10; four more misses were likewise pre-banked. Two and three independent derivations now stand behind N-1 and N-2 respectively — the strongest confirmation either will ever get, and zero of them may book a row (anti-rename). And one citation-inheritance event inside a correct kill: reader-DU re-ran D-7's colour chain, produced linear values that encode to rgb(126, 90, **204**), and still reported D's rgb(126, 90, **216**) as "byte-identical … EXACT" — identical-to-wrong is not exact; the kill survives on its ratios, the cell does not.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0/W6/W7/W9) with **NO-WAVE-OWNER** for the sequence scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **The D-1 cure: reader-LC M-2 (`gap: 0.5rem var(--col-gap)` at the parent; "sound under both branches") vs r2's K-8 sound forms vs reader-DU's endorsement of C-§6.1 ("C's version IS sound")** | **LC WINS — D-1's disposition REPAIRED; DU's endorsement KILLED (K-20)** | My `--col-gap` census: exactly three references (:38 declaration, :39 `--track-inset: calc(var(--label-col) + var(--col-gap))`, :73 `.seq-row { column-gap: var(--col-gap) }`); `.seq-stage`'s own `gap: 0.5rem 0` (:44) is the sole non-consumer. With the parent gutter = 0.75rem: axis column-2 start = 1rem + 3.25rem + 0.75rem = **5rem** = the playhead's `left: calc(1rem + 4rem)` exactly; the `.seq-row` subgrid's gap override equals the parent's → **no redistribution exists to be ambiguous about** — the unproven branch is moot for the repair (it survives only as SS-13 #1's witness). r2's K-8 forms (a)/(b) stay sound but bind the playhead to the axis alone, leaving playhead-vs-handles on the live branch — demoted to alternates. DU's cell ("C's version — `grid-column: 2`, **delete `left`/`right`** … IS sound") re-asserts the exact form K-8 killed: C-§6.1 verbatim says "delete `left`, `right`" (re-read this session); with both insets auto the abspos track shrink-to-fits over an out-of-flow-only child → 0 inline size → `100cqw` = 0 — and DU also dropped C's own "settle the subgrid branch first" hedge. Cure side-effect named: column 2 narrows 12px (SS-13 #10). |
| 2 | **Both readers file the two-clock error (DU m1 / LC M-1) and the stale-`_duration` row (DU m2 / LC M-3) as "missed by all three axes" / net-new MAJORs** | **BOOKING KILLS (K-16) ≡ banked N-1/N-2 — recorded as 2nd and 3rd independent derivations; two attached cells corrected** | True of the challenge axes, false of the registry: r2 banked N-1 (its headline) and N-2 (promoted MAJOR, ruling 7) on 2026-08-10. Both mechanisms re-verified again this session (`STAGGER_MAX = 1600` :74-region; staircase 0/260/520/780/1040; `add()` cursor fold → 1940; `_duration` = exactly three refs, written only at :278; axis `Math.round(q * staggerMax)`; handles `at/1600`; the :152-154 prose denial verbatim). Corrections: **(K-25)** LC M-1's rider "the mismatch is not even constant … duration moves with the authored `at:` values (see M-3)" is killed **by LC's own M-3** — `_duration` is frozen at 1940 precisely because no writer exists outside `add()`, so the drawn ratio is a constant 1940/1600 = 1.2125 until the engine gains a recompute (at which moment the rider revives); **(K-24)** DU m2's "Reset does not restore it either" is mechanically true (no `_duration` path; `Math.max` monotone) but its implication is corrected: `reset()` restores DEFAULT_DELAYS (:288-305 re-read), under which the frozen 1940 is **coincidentally correct again** — the stale-denominator harm is the mid-edit state, exactly as N-2 states it. |
| 3 | **Reader-DU m3 — `pointer-events: none` is not only a virtue: a pointerdown on the "master playhead" falls through to a 44px row-drag handle and silently re-times a row; "a handle is beneath it for most of the sweep"; graded MAJOR** | **ADMITTED at MINOR as N-12, scope corrected; DU's MAJOR preserved as dissent** | Chain verified whole: `.seq-playhead-track` `pointer-events: none` (:30, inherited); the ONLY pointerdown listener in the rows is the handle (SequenceTarget.vue:96-107 — the track div :89-92 is inert); `.seq-handle` = 44px × track-height box, `margin-left: -22px` (css :120-135); a drag → `onRowDown` → `useDragScrub` → `reseatRow` (mutating authored timing). Corrections from the same bytes: the handle boxes' union ≈ 22 + 4×44 = 198px of a ~592px field ≈ **⅓ of the sweep, not "most"**, and only when the pointer's y-band is the matching row; the DIAMOND — the actual grab signifier — sits at y≈[29,37], above `.seq-rows` (≥40px), so grabbing the head lands on the axis/stage and is **inert**; and the re-time is not fully silent (the grip and `@ms` label track the drag) — but it produces **no glow lift** (N-13), so the scene's own scrub feedback never fires. The affordance clause is adopted whole: two elements are named "master playhead" (the file header :2 + the scrubber eyebrow :11), and the one wearing the machined-diamond grab cue is the inert one. Witness → SS-13 #9. |
| 4 | **Reader-DU m5 — the glow architecture is unreachable from half the scene's gestures: `.is-scrubbing`/`--seq-glow` fires only for the master scrubber, never for a row-handle drag** | **ADMITTED at MINOR as N-13** | My census: `setScrubbing` writers = SequenceScrubber.vue:87/:91 (drag start/end) — nothing else tree-wide; the row path (SequenceTarget.vue:209-225) wires `project`/`onScrub`/`onEnd` and **no `onStart`**; the row keyboard path (:233-243) likewise; the scrubber's own keyboard path also bypasses it (:97-111 — concordant with banked N-3's third limb). So the gesture useSequenceDemo.ts calls "the headline Sequence refinement" leaves the playhead's comet (:81/:84), bloom (:49-50) and stage border (:29-30) cold while the secondary gesture lights all three. Motion-feedback inconsistency between two peer gestures; compounds N-12 (the fall-through's only feedback channel is the one that never fires). |
| 5 | **Reader-DU m6 — the accessible equivalents behind `aria-hidden` are thinner than every prior record states; graded MINOR** | **ADMITTED at INFO as N-14; superlative 3 now carries the qualification; scope corrected** | Verified: the scrubber slider (SequenceScrubber.vue:22-29) **has** an accessible name (`aria-label="Scrub the sequence master playhead"` — DU's "bare integer with no axis" understates the markup) plus valuenow/min/max — but **no `aria-valuetext`** (announces "45" of 0–100, unit unstated) and no orientation; the Metric renders static text with **no live-region path** (glass-ui `metric/types.d.ts` read whole this session: `value/unit/placeholder/loading/label/context/size/orientation` — no aria surface), so playback progress is never announced. The row handles, by contrast, are properly named (SequenceTarget.vue:100-104). `aria-hidden` on the playhead remains CORRECT; "demonstrated equivalent" is re-graded to "value not lost, announcement thin". Cure: one `:aria-valuetext` binding; a Metric live-region prop is producer-side (BH relay if pursued). |
| 6 | **Reader-LC M-7 / M-5 / M-8 — three new INFO mechanisms** | **ALL THREE ADMITTED (N-15 / N-16 / N-17), each verified** | **N-15**: `.seq-target` sets `--seq-glow: 0` (:10) on the Card (SequenceTarget.vue:8), an ancestor of `.seq-stage.cascade-chase` (:61); `.cascade-chase { --seq-glow: 0 }` (:216) is therefore a dead redeclaration whose only observable effect is the source-order-vs-specificity hazard L's K-4 had to adjudicate — delete :216, keep :217's `will-change` (the stacking root per K-5). **N-16**: `.stage-field-x` = `repeating-linear-gradient(… var(--border) 0 1px, transparent 1px calc(100%/4))` (design-idioms.css:205-211, read) → rules at 0/25/50/75% and none at 100%, while AXIS_QUARTERS renders FIVE labels incl. q=1 with a `:last-child` edge-hug (SequenceAxis.vue:39-41) — the terminal tick is unruled. **N-17**: row-5's boot animation spans [360, 780]ms (:224-227: 420ms + delay 4×60+120) while `powerOn` clears the class at **760ms** (useSequenceInstrument.ts:34) — a real 20ms truncation against the ":220 ~700ms" prose; LC's own sub-0.1px residual computation honoured (imperceptible; INFO). |
| 7 | **D-7's afterlife: reader-DU KILLED it claiming "I reproduced D's entire colour chain and every figure is EXACT … rgb(126, 90, 216) — byte-identical to D's figure"; reader-LC left the input UNPROVEN ("could not locate --neutral-0")** | **Kill SUSTAINED ≡ K-1; DU's cell KILLED (K-21) and recorded as a citation-inheritance event; LC's gap resolved by the carried r2 run** | My node encode of DU's OWN linear triple (0.20842, 0.10128, 0.601977) → bytes **126/90/204** — agreeing with r2's product-converter run (rgb ≈ 125.9/89.6/203.7) and refuting the 216 D published and DU "reproduced". A reader that reports its own intermediate values and a final byte those values cannot produce has inherited the byte from the text it was auditing — inside a kill whose conclusion is nonetheless right (DU light 3.24:1 / dark 1.91:1 vs r2's 3.32/1.90 — concordant; the machined read is stronger in LIGHT mode, D inverted). LC's honest UNPROVEN on the light plate is discharged by r2's resolved declaration chain (`--background: var(--neutral-0)`; `--neutral-0: light-dark(hsl(40 30% 98%), …)` → rgb(245,242,247)), which DU independently corroborated. Third inheritance-class event in this component's ledger (after r2's ruling-2 pair). |
| 8 | **Grade contests: D-3/L-3 MAJOR (both readers) · C-3 MAJOR (LC) · D-8 MINOR (LC) · D-12 MINOR (both) · L-9/C-6 MINOR (both) · L-10 MINOR (both)** | **r2 rulings 3/5 RE-FIRED (K-22): MINOR · MINOR · INFO · INFO · INFO · INFO — all banked grades stand** | No new evidence accompanies any of the six grade claims; the banked reasoning re-verified where touched (D-3's visible consequence is arguably conventional playhead behaviour; C-3 is prose-vs-stylesheet drift pending the DESIGN.md-bindingness ruling; D-8's comet is static paint on essential motion; D-12's "brightens" is satisfied by a step; L-9's clamp is a dead guard; L-10 is harmless idiom divergence). Grade-stability is the anti-rename law applied to severity. DU m3's MAJOR → MINOR is this record's own ruling 3. |
| 9 | **Reader-DU's D-3 and D-2 confirmation cells re-assert limbs the bank killed** | **K-10 and K-7 RE-FIRED (K-18, K-19); reader-LC concordant with the bank on both** | DU's D-3 cell: "editing either token produces no visible change" — killed at K-10 (inside `.seq-track`'s context, `--z-seq-handle` still orders grips/travellers against `.progress-rail`; only the playhead↔handle relation is inert). DU's D-2 cell: "the line overhangs `.seq-rows` by 8px … D's second limb also checks" — arithmetic true, defect status killed at K-7 (the comment at :26 states the computed intent exactly; spanning the row-gap is the design). LC killed the same D-2 limb in its own register — concordant. |
| 10 | **The comet-alpha cells: DU "0 → ~17.5% while scrubbing"; LC "~26%"** | **Ruling 6 RE-FIRED (K-23): ~8.3-8.8% rest / ~35.6-37.5% scrubbing at the label-column edge** | Re-derived from :79-84: gradient mix = `28% + glow×32%`, opacity = `0.5 + glow×0.5`; at p=0 the trail spans ≈[49, 81]px, label column [16, 68]px → ≈19-20px overlap; at x=68 the gradient sits ≈0.59-0.63 toward the hot end → rest ≈ 0.28 × 0.6 × 0.5 ≈ **8.3-8.8%**; scrubbing ≈ 0.60 × 0.6 × 1.0 ≈ **35.6-37.5%**. DU's 17.5% forgets the mix-percentage lift; LC's ~26% is carried from nowhere derivable. Third corpus in a row to understate the scrubbing case. |
| 11 | **The D-15 hole + reader-DU's RTL cell** | **REGISTRY REPAIR — D-15 admitted at INFO (r2 omission confirmed: 0 hits in the r2 record); DU's census cell corrected** | The component is all-physical (`left`/`right`/`translateX`/`transform-origin: right` — whole-file read) and the demo has zero `dir="` surface (my grep: 0). DU's "0 logical-property sites, narrower pattern" is an artifact of that narrowing: the inline-family grep (`inset-inline|inline-start|margin-inline|padding-inline`) hits **4 demo files** — D's original "5 incidental sites" was nearer the truth. Conclusion unaffected (posture-faithful, project-level), which is why this is INFO and tranche-routed. |

**Adjudicator's corrections register (against this corpus's readers):** (1) DU's C-§6.1-endorsement — ruling 1/K-20; (2) both readers' "missed by all axes" filings ×8 — rulings 2, and the booking-kill table below; (3) LC M-1's non-constant rider — K-25, killed by LC's own M-3; (4) DU m2's Reset implication — K-24; (5) DU m3's "most of the sweep" + silent-retime scope — ruling 3; (6) DU m6's "no axis" scope — ruling 5; (7) DU's D-7 "byte-identical/EXACT" cell — K-21; (8) DU's D-3/D-2 re-asserted limbs — K-18/K-19; (9) six grade cells — K-22; (10) both readers' alpha cells — K-23; (11) DU's RTL pattern-narrow cell — ruling 11. Reader-LC's L-3 sub-claim catch (challenge-L asserted `.seq-stage` has no containment; `.cascade-chase`'s `will-change` makes it the context) is concordant with banked K-5 and credited; LC's D-3 cite-slip note (:218 → :217) adopted.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the sequence scene-repair packet). Anchors bind to worktree ≡ `origin/master`; re-resolve before any cure.

### MAJOR

- **N-1** *(r2; provenance r2·RR-A missed-1; +2 independent derivations this corpus: DU m1, LC M-1)* — **the ruler and the playhead measure two different clocks**: axis/handle/gate domain [0, 1600ms] (`STAGGER_MAX`; labels `Math.round(q × staggerMax)`; handles+gates at `at/1600`) vs the master clock [0, 1940ms] (`add()`'s cursor fold, sequence.ts:277-278) — up to **~11.4% of track width (~67px)**: row-5's lane fires with the playhead at 0.536W against a gate drawn at 0.650W, denied by the scene's own prose (SequenceTarget.vue:152-154; SequencePlayhead.vue:21-22; SequenceAxis.vue:2). Re-verified whole this session. LC M-1's "not-even-constant" rider killed (K-25 — frozen denominator ⇒ constant 1.2125 ratio; the rider revives the day N-2's cure lands, and the two rows must be cured against each other for exactly that reason). Scene-level identity, booked HERE (anti-rename; forthcoming kf-SequenceAxis/kf-SequenceTarget/kf-SequenceScrubber adjudications fold to this row). Cure is spec-work: one named time domain (axis names the true duration, or the playhead normalises to the drawn domain, or duration-derived labels); born-RED gate: axis terminal label = `sequence.duration`. **NO-WAVE-OWNER (instrument-truth core).**
- **N-2** *(r2, promoted MAJOR at r2 ruling 7; +2 derivations: DU m2, LC M-3 — LC's engine-lens statement of the library-side half is the sharpest yet filed)* — **`Sequence._duration` is never recomputed on re-time; the first handle drag stales the playhead's denominator.** `reseatRow` mutates `entry.at` in place, re-sorts, and seeks by `sequence.progress × sequence.duration` against the frozen product; `_duration` = exactly three references (:158/:203/:278), written only in `add()`, monotone, private, no recompute/re-time API on the enumerated public surface (LC's enumeration adopted, concordant with the banked cell). Witnesses: drag row 5 to 1600 → child spans past 1940, `foldPhase`/child clamps cap the clock → p=1 with the lane frozen at 37.8% of its glide; all rows to 0 → ~54% dead sweep. DU's Reset cell corrected (K-24): defaults restore ⇒ the frozen 1940 is coincidentally right again; the harm is every mid-edit state. Dual seam, one row: the engine's re-time invariant gap AND the demo's dogfooded direct mutation ("the SAME re-sort `Sequence.add` runs internally" — half of `add()`'s work). **NO-WAVE-OWNER (engine-side rider named: derive `duration` from `max(at + child.duration)` or close `entries` mutability).**
- **D-1 / L-1 / C-1** *(all three axes + both r2 readers + both r3 readers)* — **the playhead and the ruler resolve two different horizontal origins: 12px at p=0, decaying to 0 at p=1** (axis grid-placed at padding-box+4.25rem; playhead abspos at padding-box+5rem; right edges agree — an affine, non-translational error). **r3 REPAIR (ruling 1): the root cause is `SequenceTarget.css:44`** — the stage declares `--col-gap` (:38), derives `--track-inset` from it (:39), grants it to the row subgrid (:73), and writes `gap: 0.5rem 0`, declining its own token; the leaf's `--track-inset` was authored correctly against the intended geometry. **Preferred cure: `gap: 0.5rem var(--col-gap)`** — aligns axis (5rem), subgrid track, and playhead (5rem) simultaneously; the `.seq-row` gap override becomes a same-value no-op, so the subgrid-gutter branch is moot for the repair; column 2 narrows 12px (witness SS-13 #10). K-8's forms (a) `grid-column: 2; left: 0; right: 0` and (b) `left: calc(1rem + var(--label-col))` demoted to axis-aligning alternates; the corpus's delete-the-insets cure stays killed (K-8, re-fired as K-20). **MAJOR. NO-WAVE-OWNER (one commit with D-10/L-2 and D-2/L-4/C-5).**

### MINOR

- **D-3/L-3** *(grade re-contested MAJOR by both r3 readers — K-22; stays MINOR)* — the documented `--z-seq-playhead: 1` / `--z-seq-handle: 2` ordering contract is dead: `container-type: inline-size` on `.seq-track` (:114) traps the handles'/travellers' z:2 in a private stacking context; the playhead subtree (z:1, step 9) paints over `.seq-track` (positioned z-auto, step 8) under the `.seq-stage.cascade-chase` root (`will-change: transform`, :217). D's "both tokens do nothing" limb stays killed (K-10, re-fired K-18 — `--z-seq-handle` still orders against `.progress-rail`); LC's catch of challenge-L's false "no containment on `.seq-stage`" sentence credited (≡ K-5). Cure = the one-deletion (drop :31; tree order restores the documented relation). *(r2-carried; chain re-walked.)* **NO-WAVE-OWNER.**
- **C-3** *(LC re-contested MAJOR — K-22; stays MINOR pending the DESIGN.md-bindingness ruling)* — DESIGN.md:52-57 names the playhead cap a `--specular` consumer; the token exists nowhere (one tree-wide hit: the prose itself) and :63-64 ship the raw `white` the same paragraph forbids; the name additionally sits beside glass-ui's live `--specular-x/y/intensity/angle` family. One cure with the D-7 residue: define `--specular`/`--shade` as `light-dark()` pairs, repoint :63-64 — the light-mode arm is doing real visible work (K-1), so the repair is definition, not deletion. *(r2-carried.)* **KF.W6 (material-register packet; cross-repo relay if the pair belongs in glass-ui).**
- **D-4** — "the LINE (full master red)" names a colour the token authority expelled (violet in both arms; "so red exits the chrome entirely"); family residue at SequenceScrubber.vue:11-16/:132-141. *(r2-carried.)* **NO-WAVE-OWNER (prose-truth sweep — now carrying DU m8's aggregate rider: six false load-bearing comments across the cluster, four inside this SFC's own 30 comment lines — :3-4 red, :7-8 no-per-frame-JS, :21-22 `left: %`, :21-22 same-axis, css:20 micro-stack, Target:152-154 same-domain — each re-verified; the cure is fewer, checkable comments, not more.)**
- **D-5/L-8/C-4** — "Pure CSS … no per-frame JS" is false: `progress` is a Vue ref written every mirror frame and patched through an inline `:style` via a child re-render. The "sole motion element" framing stays killed (K-2 — the scrub-ball, timecode, and Metric are per-frame Vue consumers; the scrubber's own :152-154 comment says so). *(r2-carried; chain re-read.)* **NO-WAVE-OWNER (prose sweep; the engine-target cure stays killed, K-9).**
- **D-6/C-8** — the 32px comet escapes the track's inset and washes the right ~20px of the label column at the mount state (p=0), unclipped; alphas **~8.8% rest / ~37.5% scrubbing** at the column edge (ruling 10, third re-derivation). Percept → SS-13 #2. *(r2-carried.)* **NO-WAVE-OWNER.**
- **D-2/L-4/C-5** — `top: calc(0.75rem + 1.25rem)` hardcodes the DESKTOP axis outer height; SequenceAxis compresses to 0.95rem under 1024px with zero `@media` here. Symptom picture stays as K-11 refined it (slivers by the tip; desktop is the 2.26px-overlap case); the overhang limb stays killed (K-7, re-fired K-19 against DU). *(r2-carried.)* **NO-WAVE-OWNER (one commit with D-1; the M-2 cure kills the horizontal literal, the vertical pair dies via N-8's owning-side home).**
- **D-10/L-2** — `.seq-stage`'s padding re-typed as four raw literals across the component boundary; all four correct today, untokenized, untested. *(r2-carried.)* **NO-WAVE-OWNER (with D-1).**
- **D-11/L-6/C-7** — `--playhead-p` unregistered while `--ball-p` is `@property`-registered with stated rationale; the `, 0` fallback unreachable; real failure = IACVT transform poisoning (NaN passes the NaN-transparent clamp). Cure per K-12: register AND delete the fallback + JS clamp as one edit. *(r2-carried.)* **NO-WAVE-OWNER (property-hygiene family).**
- **D-14/L-5/C-2 (+ r2·RR-B M-2)** — fallback discipline 100% inverted: all ten fallbacked `var()` reads provably dead; all three load-bearing external reads bare (`--track-inset`, `--z-seq-playhead`, `--radius-pill`). C-2's true two-limb identity confirmed at the C file this session (contract table + fallback inversion — one finding, both readers' divergent id mappings reconciled here). *(r2-carried.)* **NO-WAVE-OWNER.**
- **N-3** *(r2)* — `--scrub-dir` is a raw per-pointermove sign test with no hysteresis AND no writer on three of the four ways the playhead moves (reverse playback, keyboard, gesture end — writers census re-run this session: SequenceScrubber.vue:70 sole caller). *(r2-carried; census re-verified.)* **NO-WAVE-OWNER.**
- **N-4** *(r2; +2 derivations: DU m4, LC M-4 — booking-killed as new, K-17)* — three inconsistent p→x maps on one rail; travellers rest ±12.8px off their drawn gates (`left`-edge-mapped over `100cqw − ball-size` vs the handles' centre-mapped `at/1600 × W`), against the stylesheet's own "it RESTS at its start gate" (:183-186/:200). *(r2-carried.)* **NO-WAVE-OWNER (instrument-truth packet).**
- **L-7** — the rule comment describes a `left: %` mechanism the file doesn't contain, contradicted ten lines below by the T.G4 comment that replaced it. r3 cell addition (both readers, concordant): L-7's own falsifier as literally worded ("Dead if `left: %` appears anywhere in the file") is satisfied by the tree — :58 `left: 50%`, :75 `right: 50%` are static anchors, not the sweep; the substance survives, the falsifier text should not be relied on. **NO-WAVE-OWNER (prose sweep).**
- **N-12** *(NET-NEW r3 — DU m3, ruling 3; MINOR, DU's MAJOR preserved as dissent)* — **the affordance trap: the inert element wears the grab cue, and grabbing where it points can silently re-time a row.** `pointer-events: none` (:30) drops every pointerdown through the playhead; the sole listener beneath is the 44px `.seq-handle` (SequenceTarget.vue:96-107 → `reseatRow`), under the line for ≈⅓ of its travel (row-band conditioned); the diamond head — the canonical NLE grab signifier, on the element titled "THE PHOSPHOR MASTER PLAYHEAD" — is inert (it hangs above the rows), while the second element named "master playhead" (the scrubber eyebrow, :11) owns the actual gesture. A grab near a gate re-times that row with the grip/label as the only feedback — the glow channel never fires (N-13). Witness → SS-13 #9; cure rides the packet's interaction spec (either the playhead becomes a real scrub surface delegating to `demo.scrub`, or the diamond loses the grab styling). **NO-WAVE-OWNER.**
- **N-13** *(NET-NEW r3 — DU m5, ruling 4)* — **the scrub-heat architecture is unreachable from half the scene's gestures**: `setScrubbing` fires only on the master-scrub drag path (:87/:91); row drags, row keyboard nudges, and the scrubber's own keyboard path never lift `--seq-glow` — the scene's headline gesture (its own comment's words) leaves the storyboard cold while the secondary one lights it. Compounds N-12; pairs with banked N-3 (the same under-wiring class, `--seq-glow` edition). One-line cure per path (`onStart` on the row `useDragScrub`; wrap the keyboard branches). **NO-WAVE-OWNER (wiring row, with N-3).**

### INFO

- **D-12** — `--seq-glow` hard-steps 0→1 (binary token, zero `transition` in the file); grade re-contested MINOR by both readers, stays INFO (K-22); the registration-misdirection stays killed (K-13). *(r2-carried.)* **NO-WAVE-OWNER.**
- **L-9/C-6** — the `clamp(progress, 0, 1)` at :10 is a provable no-op passing the one breaking input (NaN); grade re-contested, stays INFO. *(r2-carried.)* **NO-WAVE-OWNER.**
- **L-10** — two idioms for one job in sibling files (scalar+CSS transform vs finished transform string + hand-synced margin), same T.G4 rationale pasted on both; grade re-contested, stays INFO. *(r2-carried.)* **NO-WAVE-OWNER.**
- **L-11/C-10** — unconditional permanent `will-change: transform` in a card holding 8 promoted layers; box-shadow repaints the promoted layer regardless. Cost → SS-13 #5. *(r2-carried.)* **NO-WAVE-OWNER.**
- **D-13/L-12** — `grid-column: unset` is a dead declaration; the charitable reading belongs in the comment; qualified by N-8. *(r2-carried.)* **NO-WAVE-OWNER.**
- **L-13/C-9** — `--radius-pill` is an import-invisible producer-token edge (sole definition in glass-ui's `@theme` radius.css); the F-1 limb stays dead (fold ≡ census F-1/SCH-1 → KF.W0 §B-12); the ~1px-cap arithmetic stays (K-14). *(r2-carried.)* **KF.W0 fold + NO-WAVE-OWNER residue.**
- **D-8 / C-§5** — the PRM contradiction, ruled at r2 ruling 3 (no charge here; the banked datum: glass-ui's PRM blanket cannot reach custom-property-driven transforms); LC's MINOR re-contest declined (K-22); DU's INFO concordant. **KF.W9.**
- **D-16** — forced-colors: zero handling tree-wide; mitigated by the earned `aria-hidden` + the two value carriers (now with N-14's thinness qualification). *(r2-carried.)* **KF.W9.**
- **L-14** — the SFC boundary is ceiling-driven, not contract-driven (the five-token undeclared cascade); the root-cause frame for D-1/D-2 — and now the frame ruling 1 vindicates: the interface the boundary hides is exactly where the regression lived. *(r2-carried.)* **NO-WAVE-OWNER (packet-spec framing).**
- **D-9/C-11** — the `--z-seq-*` off-scale observation stays CLOSED under R.F8 (K-15; both r3 readers concordant — LC's kill, DU's already-adjudicated filing). DU's cell enrichment adopted: F8's accepted remedy was "a comment naming the stacking semantics"; that comment now exists at css:20 and, per D-3, is false — **the discharged remedy is the defect**. **KF.W6 rider (F8 annotation).**
- **D-15** *(REGISTRY REPAIR r3 — ruling 11; absent from r2)* — every inset/offset is physical; under `dir="rtl"` the mapping inverts — a project-level posture, not a local defect (zero `dir=` surface demo-wide, my grep; the demo ships 4 files of incidental inline-family logical properties, correcting DU's zero-count). Correctly filed INFO by all who touched it; tranche-level. **KF.W9 (with the forced-colors datum).**
- **N-5** *(r2; +1 derivation: DU m7, booking-killed K-17)* — `--stagger-max` written inline on every render, read by no stylesheet (census re-run: exactly one hit, the write). *(r2-carried.)* **NO-WAVE-OWNER (dead-declaration sweep).**
- **N-6** *(r2)* — `pointer-events: none` at :85 is a dead re-declaration (:30 covers the pseudo); D's superlative credited the pair, the construction is :30 alone. *(r2-carried.)* **NO-WAVE-OWNER.**
- **N-7** *(r2)* — `--scrub-dir: 1` at css:24 can never be observed (the sole `.seq-stage` carries the inline binding on every render). *(r2-carried.)* **NO-WAVE-OWNER.**
- **N-8** *(r2)* — the seam is stylable from the owning side today (parent scope-id on the child's root); the natural home for the stage-geometry rules — and the M-2 cure's home file is exactly this one. *(r2-carried.)* **NO-WAVE-OWNER (packet-spec input).**
- **N-9** *(r2)* — `var(--ball-tone, var(--color-progress))` written out six times in 68 lines; resolve once, read one name. *(r2-carried.)* **NO-WAVE-OWNER.**
- **N-10** *(r2)* — no test, no gate, no story names this component (grep: zero over src/+test/+scripts/) — the mechanical silence behind every row above; the shared falsifier (rect-equality of axis vs track) is the unwritten regression test, and ruling 1's cure gives it a second assertion (axis rect == playhead-track rect == handle-track rect). *(r2-carried.)* **NO-WAVE-OWNER (the packet's born-RED gate list starts here).**
- **N-11** *(r2)* — the manifest triangulation (fold ≡ census F-1/SCH-1/kf-AmigaScene C-1, KF.W0 §B-12); no r3 reader re-offended; the pattern ledger keeps r2's two events + K-21's new inheritance event as this corpus's contribution. *(r2-carried.)* **KF.W0 fold.**
- **N-14** *(NET-NEW r3 — DU m6, ruling 5)* — the accessible equivalents are real but thin: the scrubber slider has a name but no `aria-valuetext`/unit (announces a bare 0–100 integer); the Metric has no live-region path (producer prop surface read: none exists), so playback progress is never announced; the row handles are the counter-example done right. `aria-hidden` stays correct; superlative 3 carries the qualification. **NO-WAVE-OWNER (one-binding cure; Metric live-region = BH relay if pursued).**
- **N-15** *(NET-NEW r3 — LC M-7, ruling 6)* — `.cascade-chase { --seq-glow: 0 }` (:216) is a dead redeclaration of the `.seq-target` default whose only effect is the K-4 source-order hazard; delete :216, keep :217. **NO-WAVE-OWNER (with N-6/N-7 — the dead-declaration family, now four strong).**
- **N-16** *(NET-NEW r3 — LC M-5, ruling 6)* — four quarter rules serve five tick labels; the 100%/1600ms tick is unruled (the repeating gradient ends where its period begins again); the axis even special-cases that fifth label's overflow while nothing paints its rule. **NO-WAVE-OWNER (rides the axis half of the packet).**
- **N-17** *(NET-NEW r3 — LC M-8, ruling 6)* — the power-on boot truncates row-5's lane-drop 20ms early (760ms timeout vs a [360, 780]ms animation); sub-0.1px residual under the front-loaded easing today, an off-by-one against the file's own "~700ms" that bites the day either constant moves. **NO-WAVE-OWNER (hygiene).**

**Folded-by-identity (carried, not rows):** (1) the **C-§4 export-surface correction** ≡ banked kf-KeyframeTimeline C-15 (→ KF.W7) — this corpus contributes two more clean handlings (DU confirmed-with-citation; LC used it to kill D-17's prescription — both concordant with K-3); (2) the **F-1 limb** everywhere it appears ≡ the banked substrate row (KF.W0 §B-12).

## Killed-claims register — 25 entries

K-1..K-15 *(r2, carried in force; compressed)*: **K-1** D-7's theme-blind bevel (conclusion inverts: light 3.32:1 > dark 1.90:1, product-converter run; re-engaged r3 at K-21) · **K-2** the "sole motion element" clause (≥4 per-frame Vue consumers; LC's kill this corpus ≡ this entry) · **K-3** D-17's geometry-delegation prescription (unreachable AND unfit; both r3 readers' kills ≡ this entry) · **K-4** the F-1 restatement at every committed tree · **K-5** "no stacking context on `.seq-stage`" (will-change IS the context; DU's C-11-parenthetical kill ≡ this entry; LC's L-3-sentence catch ≡ this entry) · **K-6** D-1's BLOCKER + the quarter-rules device (no shared scanline but ~2.26px of diamond tip; LC M-6 = an independent confirmation of this kill; DU's D-1-limb kill ≡ this entry) · **K-7** D-2's row-gap overhang limb (re-fired r3 → DU, K-19) · **K-8** the corpus's delete-the-insets cure (shrink-to-fit → `100cqw` = 0; grid-row 1/-1 names a line the row-less grid lacks; re-fired r3 → DU, K-20; forms (a)/(b) now alternates behind the ruling-1 cure) · **K-9** the give-it-an-engine-target fix (`setTargets` forwards to every entry) · **K-10** D-3's "either token" overreach (re-fired r3 → DU, K-18) · **K-11** the mobile detached-head picture (slivers by the tip; desktop is the overlap case) · **K-12** L-6's registration-makes-the-fallback-honest rationale (registration makes it permanently dead) · **K-13** D-12's interpolability misdirection · **K-14** L-13's "essentially nil" self-demotion (the ~1px caps are real) · **K-15** D-9's re-file of the R.F8-closed item (both r3 readers concordant).

New this corpus:

| # | killed claim | adjudication |
|---|---|---|
| **K-16** | Both readers' filings of the two-clock error and the stale-`_duration` row as new/"missed by all three axes" (DU m1/m2; LC M-1/M-3, incl. "5.6× the corpus headline" framing re-derived as if unbanked) | **BOOKING KILLS (ruling 2)** — true of the axes, false of the registry: banked as N-1/N-2 on 2026-08-10, one day before these returns. Recorded as the 2nd and 3rd independent derivations of each — confirmation, not booking. The recurring failure is an unread registry |
| **K-17** | DU m4 + LC M-4 (traveller ±12.8px off its gate) and DU m7 (`--stagger-max` dead write) as new findings | **BOOKING KILLS** ≡ banked N-4 and N-5; mechanisms re-verified and recorded as independent confirmations (N-4 now carries three derivations) |
| **K-18** | Reader-DU's D-3 cell: "editing either token produces no visible change" | **K-10 RE-FIRED (ruling 9)** — `--z-seq-handle` still orders grips/travellers against `.progress-rail` inside the track's context; only the playhead↔handle relation is inert. The distinction guards against L's "remove whichever member is inert" deleting the live member |
| **K-19** | Reader-DU's D-2 cell: "D's second limb also checks: … the line overhangs `.seq-rows` by 8px" (as defect substance) | **K-7 RE-FIRED (ruling 9)** — the arithmetic is true and the intent is stated at :26 (`frame pad-top + axis ruler height`); spanning the row-gap is the design. Reader-LC killed the same limb independently — concordant with the bank |
| **K-20** | Reader-DU's L-2-kill rider: "C's version of the same repair (`grid-column: 2`, **delete `left`/`right`** …) IS sound" | **K-8 RE-FIRED (ruling 1)** — C-§6.1 verbatim deletes both insets; with both auto, the abspos track shrink-to-fits over an out-of-flow-only child → 0 inline size → `100cqw` = 0 → the playhead pins at the origin for every p. DU also dropped C's own settle-the-branch-first hedge. The sound forms remain K-8(a)/(b), now behind the ruling-1 preferred cure |
| **K-21** | Reader-DU's D-7 cell: "I reproduced D's entire colour chain and every figure is EXACT … rgb(126, 90, 216) — byte-identical to D's figure" | **CELL KILLED; kill SUSTAINED ≡ K-1** — DU's own linear triple encodes to **rgb(126, 90, 204)** (node run this session; concordant with r2's product-converter 203.7). A final byte its own intermediates cannot produce is an inherited byte; identical-to-wrong ≠ exact. Recorded as this corpus's citation-inheritance event (third of its class in this component's ledger) |
| **K-22** | Six grade cells: D-3/L-3 MAJOR (both) · C-3 MAJOR (LC) · D-8 MINOR (LC) · D-12 MINOR (both) · L-9/C-6 MINOR (both) · L-10 MINOR (both) | **SEVERITY KILLS (ruling 8)** — r2 rulings 3/5 re-fired; no new evidence accompanied any re-grade; banked grades stand (MINOR/MINOR/INFO/INFO/INFO/INFO). Grade-stability = anti-rename applied to severity |
| **K-23** | Both readers' comet-alpha scrubbing cells (DU "0 → ~17.5%"; LC "~26%") | **Ruling 6 RE-FIRED (ruling 10)** — re-derived ≈35.6-37.5% at the label-column edge under scrub (mix lift AND opacity lift compound); the rest-state ~8.3-8.8% cells stand. Third corpus running to understate the scrubbing case |
| **K-24** | Reader-DU m2's "Reset does not restore it either" (as a live consequence) | **CORRECTED (ruling 2)** — mechanically true (no `_duration` path), but `reset()` restores DEFAULT_DELAYS, under which the frozen 1940 is coincidentally correct again; N-2's harm is the mid-edit state, exactly as banked |
| **K-25** | Reader-LC M-1's rider: "the mismatch is not even constant: it is `sequence.duration / STAGGER_MAX`, and duration moves with the authored `at:` values (see M-3)" | **KILLED BY THE READER'S OWN M-3** (ruling 2) — `_duration` has no writer outside `add()`; it is frozen at 1940, so the drawn ratio is a constant 1.2125. The rider revives the day N-2's recompute cure lands — which is why N-1 and N-2 must be cured against each other, a sequencing note now in the packet spec |

## Superlatives (provenance preserved)

1. **The sweep transform** (:46) — container on the parent, `cqw` consumed by the child, self-centring `-50%`, compositor-only; the hand-synced contrast case in the sibling. All three axes + all four readers across r2/r3. *(r2-carried.)*
2. **The diamond's transform order** (:61 `translateX(-50%) rotate(45deg)`) — translate against the un-rotated box; the inversion throws the head 4√2 ≈ 5.66px off. **r3 qualification (reader-LC SUP-E, logic verified): the guarantee is borrowed** — under content-box the 1px top/left borders make a 9×9 border box and `-50%` lands −4.5px (0.5px off the line's centre); Tailwind v4's preflight `border-box` reset, reaching this file through `style.css`'s import, is what makes the arithmetic exact — an unnamed cross-file dependency inside the file's best line.
3. **`aria-hidden` is earned** — the value survives in two unconditionally-rendered accessible forms — **now carrying N-14's qualification**: the forms are real but thin (bare-integer slider announcement, no valuetext; the Metric is static text with no live-region path — producer d.ts read). "Demonstrated equivalent" → "value not lost, announcement thin". *(r2 + r3 ruling 5.)*
4. **Zero teardown surface, by construction** — one import, one `defineProps`, two divs, one binding, in a scene directory carrying pointer capture ×2, a rAF mirror, five engine targets, a machine subscription and a global keydown. *(r2-carried; both r3 readers re-found it.)*
5. **The T.G4 compositor claim holds by enumeration** — :46 is the file's only per-frame-varying declaration. *(r2-carried.)*
6. **The comet flip mechanism** (`right: 50%` + `transform-origin: right center` + `scaleX(±1)`, `scaleX(0)` closed at the writer) — mechanism excellent, driver under-wired (N-3, and now N-13's gesture asymmetry beside it). *(r2-carried.)*
7. **The one visible dependency edge is the correct one** — `@mkbabb/value.js/math`, declared, 1110 import-free bytes, parser-crash class unreachable. **r3 addition (reader-LC, verified this session): the edge is forced, not lazy** — keyframes' own barrel does not re-export its `internal/leaves.ts:28` clamp, so the direct value.js subpath is the only route; and the edge is manifest-declared at every committed tree. *(r2-carried + r3 rider.)*
8. **The corpus's kill sections survive hostile re-reading** — now through a second full re-reader generation; the one new counter-example is DU's inherited byte (K-21), which proves the discipline's value rather than voiding it. *(r2-carried, extended.)*
9. **NEW (reader-DU, verified this session): state as parameter, not as selector.** `.is-scrubbing` appears nowhere in SequencePlayhead.vue; the entire scrub-heat response rides one inherited scalar through three `calc()` sites (:49-50 blur+tint, :81 gradient tint, :84 opacity), toggled one ancestor away — three visual layers respond to a gesture with zero extra selectors, zero class plumbing, zero duplicated colour values; the same discipline carries `--scrub-dir` and `--ball-tone`. The component's best architectural idea, unnamed by all three axes and both r2 readers — and it is exactly what makes D-12's cure a one-liner and N-13's cure a one-liner-per-path.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The three-rect measurement** — `.seq-axis` / `.seq-playhead-track` / one `.seq-track` `getBoundingClientRect().left` in one mount: settles the 12px origin, the subgrid-gutter branch (4.25/4.625/5rem — now a witness question only; the ruling-1 cure is branch-independent), and N-1's ~67px gate offset in one pass. *(r2-carried, re-scoped by ruling 1.)*
2. **The comet wash percept** at p=0 (~8.8% rest / ~37.5% scrubbing over the `@ms` sub-line), both themes. *(r2-carried; figures re-derived.)*
3. **Paint order at a playhead↔handle crossing** (D-3's before-frame). *(r2-carried.)*
4. **The diamond-tip vs quarter-label collision** (desktop 2.26px incursion; one screenshot at a quarter position). *(r2-carried.)*
5. **The will-change layer cost** (8 hints, trace with/without). *(r2-carried.)*
6. **N-4's percept** — does ±12.8px still read as "resting on the gate". *(r2-carried.)*
7. **N-3's witnesses** — jitter mirror-flicker; the streak leading the head under `reverse()`; the stuck-mirrored streak after one backward drag. *(r2-carried.)*
8. **D-7's residue** — the light-theme top-left silhouette softness (1.36:1), looking question only. *(r2-carried.)*
9. **NEW — N-12/N-13's compound witness**: grab the playhead line inside a row band within 22px of that row's gate → confirm the row re-times with no glow lift and no scrub affordance; separately confirm the diamond-grab is inert. Cheap, decisive, and the born-RED material for the interaction-spec cure.
10. **NEW — the ruling-1 cure's before/after**: the same three-rect probe after `gap: 0.5rem var(--col-gap)` — all three lefts equal, column 2 narrower by 12px, no other layout drift (the `.seq-rows` fallback branch for non-subgrid browsers included).

## Identity guards (fold-by-reference — never re-booked)

- **The F-1 limb ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1** → KF.W0 §B-12; pattern ledger gains K-21's inheritance event. *(carried)*
- **The C-§4 export-surface correction ≡ kf-KeyframeTimeline C-15** → KF.W7. *(carried; two clean r3 handlings recorded)*
- **N-1 / N-2 / N-4 are scene-level homes** — any forthcoming kf-SequenceAxis / kf-SequenceTarget / kf-SequenceScrubber adjudication folds its domain, denominator, and gate-offset findings HERE (anti-rename); this corpus's four booking-kills (K-16/K-17) are the law working.
- **N-2 remains the engine row's first booking** (no `Sequence`/`_duration` row elsewhere in the registry — re-checked); LC M-3's engine-lens cell is its best statement and is preserved inside the row.
- **D-9 stays closed under R.F8**; the annotation rider (the discharged remedy is the defect) rides KF.W6.
- **D-1 ≡ LC M-2** — one identity: M-2 is the root-cause half of the banked D-1 row, folded as its repair (ruling 1), never a second booking.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold (§B-12). — **KF.W6**: C-3 + the D-7 residue (the `--specular`/`--shade` material register; cross-repo relay if producer-owned) · the R.F8 annotation · the flat-namespace datum. — **KF.W7**: the C-15 fold. — **KF.W9**: the PRM datum · the forced-colors datum · **D-15 (RTL posture, admitted r3)** · the SS-13 list.
- **NO-WAVE-OWNER — the sequence scene-repair packet (SS-1/SS-2 authoring must home it):** the **instrument-truth core N-1 · N-2 · D-1 · N-4** — with two r3 obligations: the ruling-1 cure (`gap: 0.5rem var(--col-gap)`, one token, all origins; SS-13 #10 witness) and the K-25 sequencing note (N-1's domain cure and N-2's recompute cure must land against each other, or the constant ratio becomes a moving one) · the **interaction rows N-12 · N-13 · N-3** (one gesture spec: what the playhead affords, which gestures light the well, when the streak flips) · the geometry commit D-10/L-2 + D-2/L-4/C-5 (via N-8's owning-side home) · the stacking row D-3/L-3 (the one-deletion) · the property-hygiene family D-11 + D-14 + L-9 + the dead-declaration quartet D-13/L-12 · N-6 · N-7 · **N-15** · N-5 · the prose-truth sweep D-4 · D-5 · L-7 (+ DU m8's six-comment tally as the sweep's checklist) · D-6's clip one-liner · D-12's transition pair · **N-14's one-binding valuetext** · **N-16/N-17 hygiene** · L-10/L-11/N-9 postures · L-14/N-8 (spec framing) · N-10 (the packet's test obligation — now two born-RED assertions: the three-rect equality and the axis-terminal-label = duration gate).

## Verdict

**ADJUDICATED.** A third reading generation confirmed what the first two established — every load-bearing fact about this 87-line file survives hostile re-derivation, and nearly every grade, cure, and "first" attached to those facts does not. The r3's net yield: one cure upgraded at the root (the stage declined its own gap token; one token consumed, three origins align, the quarantined subgrid branch goes moot), six modest rows admitted (an affordance trap wearing the grab cue, a cold headline gesture, a thin announcement, three dead-paint/off-by-one hygiene items), one registry hole repaired (D-15), and ten more entries in the killed-claims register — eight of them the same lesson: **the readers re-derive brilliantly and do not read the bank**, so the two-clock MAJOR and the stale-denominator MAJOR were each "discovered" for the second and third time while their banked rows sat one directory away. Two and three independent derivations now stand behind the record's headline pair; the citation-inheritance class claimed its third event inside an otherwise-correct kill. The component itself is unchanged by all of this: a right-shaped leaf whose prose lies, whose numbers are inherited from four files that disagree, and whose repair remains a packet, not a rewrite. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life, N-1..N-17 — for life.

— end of apotheosis r3. The three challenger records and both r3 re-reader returns remain immutable beside this file per E-1/E-3; the superseded r2 record at this path is replaced whole with its identities, rulings, kills, superlatives and residues carried above, per the supersession note.
