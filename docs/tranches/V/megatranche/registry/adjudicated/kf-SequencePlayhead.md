Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequencePlayhead; receipt read from this seat's own system prompt, not inherited).

# kf-SequencePlayhead — adjudicated apotheosis r4 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-12 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale, 2026-07-28), `origin/master` `81a56990`; three sequence files dirty vs local HEAD (`SequenceTarget.vue`/`useSequenceDemo.ts`/`sequence.ts`) but **worktree ≡ origin/master on every demo sequence surface** (my `git diff origin/master --stat` this session: drift confined to `lifecycle.ts`/`transport.ts` export keywords/import path — r3's substrate reproduced exactly; the readers' `Metric`/`useSweepScene` cites are worktree-current). · **Registry tree** value.js `tranche-u` · **Corpus (r4)**: two fresh hostile re-reader returns over `audit/kf-components/SequencePlayhead/challenge-{D-design,L-library,C-consumption}.md` — **reader-A** (design-and-usage: 23 confirmed · 3 kills · 10 misses M-1..M-10 · 7 unproven · 9 superlatives) and **reader-B** (library-and-code: 21 confirmed · 6 kills · 6 misses · 7 unproven · 7 superlatives). Both returns consumed whole; the challenge files re-read where the readers disagree (D-1's coordinate frame, C-1's disjunction, C-2's contract table, C-§6.1, C-11).

**Supersession (recorded, nothing lost).** This record supersedes the 2026-08-11 **r3** apotheosis at this path (which adjudicated a *different* pair of returns, reader-DU 24c/5k/8m · reader-LC 21c/7k/8m, and itself superseded r2 of 2026-08-10 and r1 of 2026-08-09). **All prior identities — the original D/L/C ids and N-1..N-17 — are preserved for life (anti-rename) and carried in full below**; r3's rulings 1–11 and killed-claims K-1..K-25 are carried in force, six of them re-fired against this corpus's readers. r4 changes the bank in **exactly three places**: (a) three modest INFO rows admitted (**N-18** two-rails-one-name, **N-19** the header's wrong interface self-description, **N-20** the boot omission); (b) one banked census cell repaired (D-14's "ten fallbacked reads" → **12 reads across 9 declarations**, my whole-file count); (c) the killed-claims register gains its **first killed kill** — reader-B's D-1-SUBGRID/C-1-SUBGRID kill dies by frame conflation (K-29). Where r3 content is restated it was re-verified this session except where marked *(carried)*.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SequencePlayhead.vue (87 L), SequenceTarget.css (259 L), SequenceAxis.vue (49 L), SequenceScrubber.vue (162 L — `wc -l` decisive against reader-A's "163"), SequenceTarget.vue:40-139, useSequenceInstrument.ts (45 L); useSequenceDemo.ts:60-124/:280-349 (constants, staircase, `scrub`/`reset`/`reseatRow`); sequence.ts:115-169/:200-284 (`entries` binding-readonly :121, `_duration` :158, **`progress` getter :217-219 AND setter :221-223** — reader-B's fourth clamp site, verified, NaN-transparent, previously unbanked; `add()` :271-280 the sole `_duration` writer); challenge-D:20-95 (the coordinate table's **content-left frame** at :26-28 and the :75 subgrid branch — decisive for K-29), challenge-L:54-62 (padding-frame 4.25/5rem/4.625), challenge-C:99-111 (padding-frame table + the "exhaust the space" disjunction), challenge-C:121-188 (C-2's actual limbs — decisive for the N-19 novelty ruling; C-4's header quote), challenge-C:293-297/:410; style.css:126-165 (`--accent-kf` light `oklch(0.56 0.17 295)`; the violet-authority prose; the :19-38 z-contract head), design-idioms.css:158-168/:203-211 (`.progress-*` ancestor doc; `.stage-field-x` quarters), DESIGN.md:52-58 verbatim; my own gamma encode of the oklch light arm → linear (0.2083, 0.1013, 0.6020) → bytes **rgb(126, 90, 204)**, bevel mix → **rgb(216, 206, 240)** — decisive for K-30; the M-4 rail arithmetic re-derived from `px-4` paddings (playhead origin Card+6rem vs scrub-ball centre Card+1rem at p=0 → 80px; 16px at p=1); the M-7 vertical re-derivation (diamond vertex 27.34px vs axis box bottom 29.6px desktop / 27.2px mobile); the fallback census (12 fallbacked var() reads / 9 declarations / 3 bare — :46,:47,:49-50×3,:62,:63,:64,:78,:79-83×2,:84 vs :28,:31,:48). Registry instruments: the r3 record at this path (whole, twice), banked SS-13 #1's 4.25/4.625/5rem enumeration (decisive for K-29), kf-KeyframeTimeline C-15 and census F-1/SCH-1 (carried folds).

## Scoreboard

**42 roster rows, original ids for life — 0 BLOCKER · 3 MAJOR · 14 MINOR · 25 INFO** (r3's 39 carried; **3 net-new admitted from this corpus's 16 filed misses** — N-18/N-19/N-20, each re-derived at the bytes; **10 of the 16 misses are booking-killed as pre-banked** (≡ N-1 ×2, N-2 ×2, N-3, N-4 ×2, N-6, K-15's rider, K-8/K-20's cure critique) and recorded as independent confirmations — **N-1 and N-2 now stand at FIVE independent derivations each**; 3 become riders (M-5 → D-4, M-6 → N-14, M-7 → SS-13 #4)). **Killed-claims register: 33 entries** — K-1..K-25 carried (six re-fired) + **K-26..K-33 new, including K-29, the register's first killed kill**. **9 superlatives** (carried; two gaining generation notes). **12 UNPROVEN residues** → SS-13. **2 fold-by-identity** to banked records (carried).

**Headline.** The r4's structural yield is the incredulity seat doing its titular job in both directions. Against the readers: reader-B manufactured a **false kill** — it killed D-1's and C-1's subgrid numbers as "wrong arithmetic" while endorsing L-1's 4.625rem, not seeing that D-1's 3.625rem is stated in D's own **content-left frame** (challenge-D:26-28) and equals L's padding-relative 4.625rem **exactly**, and that C-1's 5rem is the absorbed-left *branch* in C's padding frame (the same branch D:75 names), not an arithmetic; B's kill sentence even mixes the two frames in one clause. The banked SS-13 #1 had already reconciled all three candidates (4.25/4.625/5rem) two generations earlier. And the wrong-byte ledger gained a **fourth entry**: B's "exact" recomputation of D-7 produced blue byte 229 against a chain whose true encode is 204 — while reader-A, in the same corpus, finally produced the **correct** 204 AND the first reader-generation comet-alpha figures concordant with banked ruling 6. Against the bank: the booking-kill lesson fired a second full generation — ten of sixteen filed "missed by all three axes" rows were banked one-to-two days before these returns (true of the axes, false of the registry, again) — and one banked census cell ("ten fallbacked reads") is repaired to the true 12/9+3 by this seat's own count. Three honest admissions survive the sieve: the two-rails-one-name datum quantified (80px→16px), the header comment that enumerates 4 of its 7 external reads and misattributes an owner, and the boot that assembles every stage element except the one painted over them.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0/W6/W7/W9) with **NO-WAVE-OWNER** for the sequence scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **Reader-B kills D-1's and C-1's subgrid-branch numbers ("both arithmetics are wrong … L-1 is the only axis that states it correctly"); reader-A's U-1 restates the centred branch at content+3.625rem** | **B's KILL IS KILLED (K-29) — the first false kill in this component's ledger; A's U-1 CONCORDANT with the bank** | Frames re-read at the sources: challenge-D's coordinate table is **content-left**-framed (:26-28 "`.seq-axis` … content-left + 3.25rem"), so D:75's centred-branch "3.625rem" = padding-box 4.625rem ≡ challenge-L:62's 4.625rem ≡ **the number B endorses**. B's refutation ("would place it LEFT of the parent column, impossible") compares D's content-frame number against B's padding-frame 4.25rem — and B's own gloss "4.25rem (content-left + 3.25rem, column-gap 0)" equates a padding-frame number with a content-frame expression in a single clause, the exact conflation it accuses D of. C-1's "5rem" (challenge-C:108-111, padding frame) is the absorbed-left BRANCH — track at content+4rem, "playhead agrees with the handles" — the same branch D:75 names second; a branch statement cannot be killed as an arithmetic error. C-1's real defect is its "the two branches exhaust the space" (4.25/5rem, omitting centred 4.625) — which reader-A's D-1 cell states correctly and banked SS-13 #1 already enumerates as **4.25/4.625/5rem**. Nothing changes in the roster; the branch stays a witness question (SS-13 #1); B's kill enters the register dead. |
| 2 | **Both readers file the two-clock error (A M-1 / B MISSED-1) and the stale-`_duration` row (A M-2 / B MISSED-2) as "missed by all three axes" MAJORs** | **BOOKING KILLS (K-26) ≡ banked N-1/N-2 — recorded as the 4th and 5th independent derivations of EACH; three new cells adopted into the banked rows** | K-16's lesson re-fired a second generation: banked 2026-08-10 (r2), re-derived by both r3 readers, re-derived again by both r4 readers, never once read from the bank one directory away. Mechanisms re-verified again at the bytes (`STAGGER_MAX = 1600` :74; staircase 0/260/520/780/1040 :101-108; `add()` cursor fold → 1940 :277-278; `_duration` written nowhere else; axis `Math.round(q·staggerMax)`; handles `at/1600`; `reseatRow` in-place `entry.at` mutation :341 + stale-product seek :346). Adopted cells: **A's error function** E(p) = 371.8p − 31.8 ms (compounding the 12px origin with the domain scale — with the divisor note that a RULER reading converts over the axis field, endpoints −31.8/+340 ms); **B's per-row divergence table** 0/17/34/50/67 px (rows 1..5 at the default staircase) into N-1; **B's one-keypress trigger** (focus a handle, press `End` → `at = 1600`, span [1600, 2500] vs frozen 1940 → p=1 with the lane ≈38% mid-glide — the sharpest witness statement yet) into N-2. A's "38% through its glide" ≡ banked 37.8% — concordant. |
| 3 | **Reader-A M-3 + reader-B MISSED-4 (ball ±12.8px off its gate); B MISSED-3 (`--scrub-dir` never reset / wrong under reverse); B MISSED-5 (`pointer-events` :85 dead)** | **BOOKING KILLS (K-27) ≡ banked N-4 / N-3 / N-6 — derivation counts now 5 / 4 / 2; one cell adopted** | K-17 re-fired. N-4's mechanism re-verified (`.seq-ball { left: 0; transform: … (100cqw − var(--ball-size)) }` :193-199 vs the handle's centre-anchor :128-132 + `left: at/1600 %` Target:98; the stylesheet's own ":183-186 it RESTS at its start gate" denial verbatim); **B's per-row offset table** (+12.8/+8.6/+4.5/+0.3/−3.8 px, rows 1..5) adopted into N-4. B's MISSED-3 limbs (persistent mirror after a backward-final-sample drag; leading streak under `reverse()`) ≡ banked N-3's writer census, re-run: `setScrubDir` sole caller SequenceScrubber:70; `onEnd` :89-92 restores scrubbing only; `reverse()` never consults it — and B's cell that this falsifies :67-68's "always trails the thumb" is the banked row's own reading. B's MISSED-5 ≡ N-6 verbatim (inherited property; :30 covers the pseudo; D's superlative credited the dead pair). |
| 4 | **Reader-A M-9 (mark R.F8 SUPERSEDED; C-11's deference unsound) vs reader-B's C-11 row ("prior ruling stands … the correct disposition"); A also re-files D-9 as a confirmed INFO row** | **Banked disposition RE-AFFIRMED (K-15 re-fired, K-28): D-9/C-11 stays CLOSED under R.F8 with the annotation rider; A's M-9 booking-killed ≡ that rider; B's endorsement cell corrected** | The bank already threaded this needle at r3: F8's two factual premises are refuted by the live tree (`.seq-storyboard` :21-25 creates no stacking context — no position/z/transform/containment; the handles' z:2 is trapped by `.seq-track`'s `container-type` :114 = confirmed D-3), F8's accepted remedy (a comment naming the stacking semantics) now exists at css:20 and is false — **the discharged remedy is the defect** — and the rider rides KF.W6. A's M-9 is that rider re-derived (booking-kill, concordant; its "should not be carried downstream as settled" is the rider's exact function). B's cell quotes F8's rationale approvingly without the annotation — corrected: the CONCLUSION (1/2 are context-relative orderings, not global rungs) survives on re-derived reasoning; the AUTHORITY does not. A's D-9 re-file dies as it did in r2/r3 (K-15, third firing). |
| 5 | **Reader-A M-10 — the corpus's `grid-column: 2; grid-row: 1 / -1` cure is "structurally underspecified in three ways"** | **BOOKING KILL (K-28) ≡ banked K-8/K-20 + ruling-1 supersession; the implicit-grid mechanism cell CORRECTED** | All three limbs are banked: (a)'s vertical hazard ≡ K-8's grid-row limb — but M-10's law is wrong: css-grid-1 §8.3 counts negative indices from the end of the **explicit** grid (never the implicit); with no `grid-template-rows` the explicit grid has one row line, `1 / -1` = `1 / 1` → end removed → `1 / auto` — the containing block still starts at the content top (M-10's conclusion, by a different mechanism). (b) ≡ K-7/K-19 (the below-ruler start and the 2.26px tip overlap are stated intent). (c) — "fixing to column 2 may CREATE playhead-vs-handles at 6px or 12px" — is exactly why r3's ruling 1 superseded the whole cure family: **`gap: 0.5rem var(--col-gap)` at SequenceTarget.css:44** aligns axis, handles, and playhead simultaneously and moots the branch (the `--col-gap` census re-confirmed: three references, :38/:39/:73, the stage's own grid the sole non-consumer). M-10's closing frame ("a decision about which of four domains is canonical, not one commit") is adopted into the packet-spec framing — it is N-1/N-2/N-4/D-1 read as one sentence, and it is right. |
| 6 | **D-7's afterlife, generation four: reader-A recomputes the chain to rgb(126, 90, 204); reader-B recomputes it to rgb(126, 90, 229) — both filing MINOR-confirmed** | **Row stands at MINOR ≡ K-1; A's chain ADOPTED (3rd concordant derivation); B's cell KILLED (K-30)** | My own gamma encode this session: b_linear 0.6020 → byte **204** (bevel 0.3·204 + 0.7·255 = **240**) — concordant with A (204/240), r2's product-converter run (203.7), and r3's K-21 node run; discordant with D's published 216, DU's "byte-identical" 216, and now B's 229/247. Four wrong-byte events, one component, three generations — B's is a fresh arithmetic slip wearing "exact recomputation" clothes rather than K-21's inheritance class, and its ratio conclusion (1.347:1 ≈ banked 1.35) survives untouched, which is precisely why the byte cell must die: a conclusion this robust does not need a fabricated precision. A's cell correctly flagged D's 216 as a slip — the first reader to do so. |
| 7 | **Grade re-contests: A's D-3 MAJOR ("the wash sweeps the exact objects the scene exists to demonstrate") · B's D-8 MINOR · B's D-11 INFO (downward) · B's L-10 MINOR · vs the banked MINOR/INFO/MINOR/INFO** | **ALL DECLINED (K-31; K-22 re-fired, third generation) — banked grades stand** | No new evidence accompanies any re-grade. D-3: restated impact, not new mechanism — a playhead painting over content is conventional instrument behaviour, `pointer-events: none` caps function, and the percept is SS-13 #2/#3's question; MINOR. D-8: B's own cell argues the demotion ("a static smear riding essential motion"); INFO. D-11: B's "exposure, not a live bug" understates the banked row — the asymmetry against the deliberately-registered `--ball-p` (:13-18, with stated rationale) plus B's OWN open NaN path make MINOR right; the downward contest dies on the contester's evidence. L-10: harmless idiom divergence; INFO. Grade-stability is the anti-rename law applied to severity — three generations running. |
| 8 | **Reader-B's C-6-REACH kill (the zero-width-rect NaN path) + the fourth clamp site** | **KILL SUSTAINED; two cells ADOPTED into banked rows** | Verified at the bytes: `project()` = `clamp((e.clientX − rect.left) / rect.width, 0, 1)` (SequenceScrubber:81) → 0/0 = NaN on a zero-width rect; `clamp` is NaN-transparent; and the previously-unbanked **fourth clamp site** — `set progress(p) { this.seek(clamp(p, 0, 1) * this.duration) }` (sequence.ts:221-223, reached via `scrub`'s `sequence.progress = clamp(p,0,1)` at useSequenceDemo:281) — is equally NaN-transparent, so C-6's "not reachable in this scene today" closed only the getter door. The bank never endorsed C-6's reachability cell (L-9/C-6 stays INFO precisely as "passing the one breaking input"); B's kill formalises it and the setter-site cell enriches both D-11 and L-9/C-6. B's cell credit line ("none of the three axes noticed this fourth site") is TRUE of the axes and — for once — true of the registry as well. |
| 9 | **The comet-alpha cells, generation four: A "0 → 8.75% rest / 0 → 37.5% scrubbing"; B "≈6% rest / ≈26% scrubbing" (restating D-6's originals)** | **A CONCORDANT with banked ruling 6 — the first reader generation to state the scrubbing case right; B's cell corrected (K-32, K-23 re-fired)** | Banked derivation (re-fired r3 ruling 10): mix = 28% + glow·32%, opacity = 0.5 + glow·0.5 → ≈8.3-8.8% rest / ≈35.6-37.5% scrubbing at the label-column edge. A's figures are the exact upper edge of that band; B's are D-6's uncorrected band averages, the fourth corpus running to understate the scrubbing case by ~30%. |
| 10 | **New-miss admissions: A M-4 (two "master playheads", ~80px apart) · A M-8 (the header's interface list) · B MISSED-6 (the boot omission) · riders M-5/M-6/M-7** | **THREE ADMITTED (N-18/N-19/N-20, all INFO — A's M-4 MINOR preserved as dissent); THREE RIDERS ADMITTED; each verified at the bytes** | **N-18**: rails re-derived from `px-4` paddings — playhead field origin Card+6rem (storyboard 1rem + stage padding 1rem + `--track-inset` 4rem), scrub-ball centre Card+1rem (rail `px-4`, ball `margin-left −18px` off `left:0`) → **80px apart at p=0, 16px at p=1** for the same scalar; the naming limb ≡ N-12's cell (carried, not re-booked); the third centring convention ≡ L-10's cell. **N-19**: C-2 re-read whole (challenge-C:121-141) — its limbs are the undeclared-cascade count and the fallback inversion; it quotes the header only at C-4 for the per-frame-JS clause; NO axis audited the header's own enumeration. Verified: :6-8 lists 4 of 7 external reads (omitting `--z-seq-playhead` — the silent structural failure, `--radius-pill` — the cross-package edge, `--color-progress` — the terminal fallback ×6) and attributes all four to "the parent .seq-stage cascade" when `--ball-tone` is declared on `.seq-target` (css:7-8, the Card) and `--scrub-dir`/`--z-seq-playhead` on `.seq-storyboard` (:22-24; `--scrub-dir` also inline on `.seq-stage`, Target:63 — half-right). **N-20**: boot selectors `.seq-stage.is-powering-on .seq-axis` / `.seq-row` only (css:220-227); `.seq-playhead-track` matches neither and the file has no animation; `powerOn` fires on mount for 760ms at p=0 — the state where the comet overhangs the labels (D-6). Scope corrected: "sole fully-painted mark" holds **within the stage plate** (the scrubber below is outside the assembling frame). Riders: **M-5** → D-4 (the "clock green" head at css:5-6, verified verbatim — the OLDEST wrong name, in the stylesheet that owns the tone; the family is now green → red → violet across four sites, and it was absent from DU-m8's six-comment tally); **M-6** → N-14 (the ms master-clock domain has NO accessible OR visual-numeric exposure — Metric %, slider valuenow %, timecode a unitless 0-1 scalar (:16, a third normalized exposure A's cell undercounts); the axis that names ms is itself `aria-hidden` (SequenceAxis:6) while the row handles announce ms correctly (Target:101-103) — with this seat's own sequencing note: **the `aria-valuetext`-in-ms cure is BLOCKED on N-1's domain decision** — there is no honest ms to announce until the scene picks a clock); **M-7** → SS-13 #4 enrichment (the p=1 terminal-'1600' case: last tick right-hugged via `translateX(-100%)` :39-41, vertex 27.34px vs axis box bottom 29.6/27.2px, p=1 = every play-through's rest state; glyph question stays UNPROVEN). |
| 11 | **Small-cell corrections both directions** | **RECORDED** | Against A: SequenceScrubber.vue is **162 lines** (`wc -l`), not A's "163" — while A's :33→:34 binding-drift catch stands (K-33); A's D-1-kill divisor wobble (32.4ms over the 592px playhead field — a ruler reading converts over the 604px axis field → 31.8ms; both bury D's 39). Against the bank: D-14's census cell "all ten fallbacked var() reads" repaired to **12 fallbacked reads across 9 declarations + 3 bare** (this seat's whole-file count; mechanism and verdict unchanged). Coverage note: reader-B filed no cell at all on D-10, D-12, D-13-adjacent N-8, D-15, D-16 — no consequence (banked rows carried; A covered all five concordantly). |

**Adjudicator's corrections register (against this corpus's readers):** (1) B's subgrid kill — ruling 1/K-29 (killed kill); (2) both readers' "missed by all axes" filings ×10 — rulings 2-3/5, K-26..K-28; (3) B's D-7 byte cell — ruling 6/K-30; (4) four grade re-contests — ruling 7/K-31; (5) B's alpha restatement — ruling 9/K-32; (6) B's C-11 authority endorsement + A's D-9 re-file — ruling 4/K-15 re-fired; (7) A's M-10 implicit-grid law — ruling 5; (8) A's line-count rider — ruling 11/K-33; (9) A's M-6 "two exposures" undercount (the timecode is a third) — ruling 10. Credits: A's D-7 chain (204) and comet-alpha figures (first-generation concordance); A's D-1-conversion correction (the 39ms cell converted against the wrong clock — N-1's domain law applied to D's own rhetoric); B's fourth clamp site (sequence.ts:221-223) and per-row divergence/offset tables; both readers' clean handling of the C-§4 fold and the D-17/K-3 kill.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the sequence scene-repair packet). Anchors bind to worktree ≡ `origin/master`; re-resolve before any cure.

### MAJOR

- **N-1** *(r2; 5 independent derivations: r2·RR-A, r3 DU-m1, r3 LC-M-1, r4 A-M-1, r4 B-MISSED-1)* — **the ruler and the playhead measure two different clocks**: axis/handle/gate domain [0, 1600ms] (`STAGGER_MAX`; labels `Math.round(q × staggerMax)`; handles+gates at `at/1600`) vs the master clock [0, 1940ms] (`add()`'s cursor fold, sequence.ts:277-278) — up to ~11.4% of track width; **r4 cells adopted**: per-row gate divergence 0/17/34/50/67 px (rows 1..5), the compound error function E(p) = 371.8p − 31.8 ms, and A's correction of D-1's own "12px ≈ 39ms" cell (a ruler reading converts over the ruler's 1600ms domain → ≈32ms — the wrong-clock reflex that concealed this row from all three axes). Denied by the scene's own prose (Target:152-154 region; Playhead:21-22; Axis:2-5). Scene-level identity, booked HERE (anti-rename). Cure is spec-work: ONE named time domain; born-RED gate: axis terminal label = `sequence.duration`. K-25's sequencing note carried (cure N-1 and N-2 against each other). **NO-WAVE-OWNER (instrument-truth core).**
- **N-2** *(r2, promoted r2-ruling-7; 5 derivations: r2·RR-A, r3 DU-m2, r3 LC-M-3, r4 A-M-2, r4 B-MISSED-2)* — **`Sequence._duration` is never recomputed on re-time; the first handle drag stales the playhead's denominator.** `reseatRow` (and `reset`, :297-301) mutate `entry.at` in place and re-sort; `_duration` is written only in `add()` (:278), monotone, private; `entries` is binding-readonly with mutable contents (:121); no re-time API exists on the public surface. **r4 cell adopted (B)**: the one-keypress witness — focus a handle, press `End` → `at = 1600`, child spans [1600, 2500] against frozen 1940 → p=1 with the lane ≈38% mid-glide; symmetric all-to-0 → ~54% dead sweep. K-24 carried: `reset()` restores DEFAULT_DELAYS so the frozen 1940 is coincidentally right again — the harm is every mid-edit state. Dual seam, one row (engine invariant gap + demo direct mutation; ":320 the SAME re-sort `Sequence.add` runs internally" is half of `add()`'s work). **NO-WAVE-OWNER (engine-side rider: derive duration from `max(at + child.duration)` or close `entries` mutability).**
- **D-1 / L-1 / C-1** *(all three axes + all four re-readers across r3/r4)* — **the playhead and the ruler resolve two different horizontal origins: 12px at p=0, decaying to 0 at p=1** (axis grid-placed at padding-box+4.25rem; playhead abspos at padding-box+5rem = `1rem + var(--track-inset)`; right edges agree — non-affine against its own ruler). Root cause (r3 ruling 1, re-verified): `SequenceTarget.css:44` — the stage declares `--col-gap` (:38), derives `--track-inset` from it (:39), grants it to the row subgrid (:73), and writes `gap: 0.5rem 0`, declining its own token. **Preferred cure: `gap: 0.5rem var(--col-gap)`** — aligns axis, handles, playhead simultaneously; moots the subgrid branch; column 2 narrows 12px (SS-13 #10). K-8 forms (a)/(b) remain demoted alternates; the delete-the-insets cure stays killed (K-8/K-20); the BLOCKER grade stays killed (K-6 — both r4 readers concordant at MAJOR); the subgrid branch stays a witness question ONLY (SS-13 #1; B's attempt to adjudicate it statically died at K-29). **MAJOR. NO-WAVE-OWNER (one commit with D-10/L-2 and D-2/L-4/C-5).**

### MINOR

- **D-3/L-3** *(grade re-contested MAJOR by r4 reader-A — K-31; stays MINOR, third generation)* — the documented `--z-seq-playhead: 1` / `--z-seq-handle: 2` ordering contract is dead: `container-type: inline-size` on `.seq-track` (:114) traps the handles'/travellers' z:2 in a private stacking context; the playhead subtree (z:1, step 9) paints over `.seq-track` (positioned z-auto, step 8) under the `.seq-stage.cascade-chase` root (`will-change: transform`, :217). K-10/K-18 carried (`--z-seq-handle` still orders against `.progress-rail`); K-5 carried (the context owner — both r4 readers state it correctly; r3's LC caught challenge-L's false sentence, ≡ B's L-3-WALK kill this corpus, concordant). Cure = drop :31; tree order restores the documented relation. *(carried; chain re-walked.)* **NO-WAVE-OWNER.**
- **C-3** *(stays MINOR pending the DESIGN.md-bindingness owner ruling — both r4 readers concordant at MINOR, B noting C's MAJOR was conditional)* — DESIGN.md:53-57 names the playhead cap a `--specular` consumer; the token exists nowhere (the prose is the sole tree-wide hit; glass-ui's `--specular-x/y/intensity/angle` are positional registers, not a colour mix) and :63-64 ship the raw `white` the same paragraph forbids. One cure with the D-7 residue: define `--specular`/`--shade` as `light-dark()` pairs, repoint :63-64. *(carried.)* **KF.W6 (material-register packet; cross-repo relay if producer-owned).**
- **D-4** *(+ r4 rider M-5, verified)* — "the LINE (full master red)" names a colour the token authority expelled ("the motion-color authority = the VIOLET … red exits the chrome entirely", style.css:155-163). **The family is now THREE names across FOUR sites**: `SequenceTarget.css:5-6` "ONE clock green" (the OLDEST wrong name, verified verbatim this session, heading the stylesheet that owns the tone — absent from DU-m8's tally), Playhead:4 "full master red", Scrubber:14 + :132 "master red"; the vocabulary drifted green → red → violet and the head of the family sits in the css file, not the SFCs. DU-m8's six-comment aggregate rider carried; the tally is now eight (with N-19's list). **NO-WAVE-OWNER (prose-truth sweep).**
- **D-5/L-8/C-4** — "Pure CSS … no per-frame JS" is false: `progress` is a Vue ref written every mirror frame, patched through `:style` via a child re-render; the five travellers are engine targets, this is the one motion element Vue delivers. K-2 carried (the "sole motion element" clause stays dead — scrub-ball/timecode/Metric are per-frame Vue consumers); K-9 carried. *(carried; chain re-read at worktree names — `useSweepScene`, `Metric`.)* **NO-WAVE-OWNER (prose sweep).**
- **D-6/C-8** — the 32px comet escapes the track's inset and washes the right ~20px of the label column at the mount state (p=0), unclipped (`container-type` grants no paint containment; first clip = the Card's `overflow-hidden`); alphas ≈8.8% rest / ≈37.5% scrubbing at the column edge (ruling 9 — reader-A's figures are the first reader concordance). Percept → SS-13 #2. *(carried.)* **NO-WAVE-OWNER.**
- **D-2/L-4/C-5** — `top: calc(0.75rem + 1.25rem)` hardcodes the DESKTOP axis outer height; SequenceAxis compresses to 0.95rem under 1024px (:43-47) with zero `@media` here → 4.8px drift where the layout is tightest. K-7/K-19 carried (the row-gap overhang limb stays intent, not defect); K-11 carried. *(carried.)* **NO-WAVE-OWNER (one commit with D-1; vertical pair dies via N-8's owning-side home).**
- **D-10/L-2** — `.seq-stage`'s padding re-typed as four raw literals across the component boundary; the fifth constant already broke (D-2) — the empirical proof. *(carried.)* **NO-WAVE-OWNER (with D-1).**
- **D-11/L-6/C-7** *(B's downward INFO re-contest declined — K-31; **r4 cell adopted**: the fourth clamp site)* — `--playhead-p` unregistered while `--ball-p` is `@property`-registered with stated rationale (:13-18); the `, 0` fallback unreachable (written inline on every render against a required prop); real failure = IACVT transform poisoning — and the NaN feed is FOUR-times clamped yet never guarded (`Math.min(Math.max(v))` passes NaN at useSequenceDemo:180/:281/:328, at `set progress` sequence.ts:221-223, and at `project()`'s 0/0 Scrubber:81). Cure per K-12: register AND delete the fallback + JS clamp as one edit. *(carried + enriched.)* **NO-WAVE-OWNER (property-hygiene family).**
- **D-14/L-5/C-2** *(census cell REPAIRED this session)* — fallback discipline exactly inverted: **12 fallbacked `var()` reads across 9 declarations, all provably dead** (`--ball-tone` ×6 — `.seq-target` declares it unconditionally on an ancestor; `--seq-glow` ×4; `--scrub-dir` ×1; `--playhead-p` ×1) **vs 3 bare reads, all load-bearing and externally owned** (`--track-inset` :28, `--z-seq-playhead` :31, `--radius-pill` :48 — the last defined only in glass-ui's `@theme`, and `SpringHeatmap.vue:321` proves the defensive form is house idiom). The banked "ten reads" count corrected; mechanism, grade, and disposition unchanged. C-2's two-limb identity carried. *(carried + repaired.)* **NO-WAVE-OWNER.**
- **N-3** *(r2; 4 derivations after B-MISSED-3)* — `--scrub-dir` is a raw per-pointermove sign test with no hysteresis AND no writer on three of the four ways the playhead moves (reverse playback, keyboard, gesture end) — B's limbs verified again (sole caller Scrubber:70; `onEnd` restores scrubbing only; `reverse()` never consults it); falsifies :67-68 "always trails the thumb". *(carried.)* **NO-WAVE-OWNER (with N-13).**
- **N-4** *(r2; 5 derivations after A-M-3 + B-MISSED-4)* — three inconsistent p→x maps on one rail; travellers rest ±12.8px off their drawn gates (left-edge-mapped over `100cqw − ball-size` vs the handles' centre-mapped `at/1600 × 100%`), against ":183-186 it RESTS at its start gate". **r4 cell adopted (B)**: per-row offsets +12.8/+8.6/+4.5/+0.3/−3.8 px at the default staircase. *(carried.)* **NO-WAVE-OWNER (instrument-truth packet).**
- **L-7** — the rule comment describes a `left: %` mechanism the file doesn't contain, contradicted ten lines below by the T.G4 comment that replaced it; the falsifier-text caveat carried. *(carried; both r4 readers concordant at MINOR.)* **NO-WAVE-OWNER (prose sweep).**
- **N-12** *(r3 — the affordance trap; DU's MAJOR dissent preserved)* — the inert element wears the grab cue: `pointer-events: none` (:30) drops every pointerdown through the playhead; the sole listener beneath is the 44px `.seq-handle` → `reseatRow` (≈⅓ of the travel, row-band conditioned); the diamond — the canonical NLE grab signifier, on "THE PHOSPHOR MASTER PLAYHEAD" — is inert; the second element named "master playhead" owns the actual gesture (→ N-18). Witness SS-13 #9. *(carried.)* **NO-WAVE-OWNER.**
- **N-13** *(r3)* — the scrub-heat architecture is unreachable from half the scene's gestures: `setScrubbing` fires only on the master-scrub drag path (:84-92); row drags, row keyboard, scrubber keyboard never lift `--seq-glow`. *(carried; writer census re-run.)* **NO-WAVE-OWNER (wiring row, with N-3).**

### INFO

- **D-12** — `--seq-glow` hard-steps 0→1, zero `transition` in the file; stays INFO (K-13/K-22 carried; reader-A concordant this corpus; reader-B filed no cell). *(carried.)* **NO-WAVE-OWNER.**
- **L-9/C-6** *(+ r4: B's C-6-REACH kill sustained, ruling 8)* — the `clamp(progress, 0, 1)` at :10 is a provable no-op that passes the one breaking input (NaN); now with the full four-site clamp census incl. the engine setter. Stays INFO — "keep it" remains a reasonable ruling; the real cure is D-11's registration. *(carried + enriched.)* **NO-WAVE-OWNER.**
- **L-10** *(B's MINOR re-contest declined — K-31)* — two idioms for one job in sibling files (scalar+CSS transform vs finished string + hand-synced `margin-left`), the same T.G4 rationale pasted on both; the playhead's is the drift-proof form. *(carried; A concordant at INFO.)* **NO-WAVE-OWNER.**
- **L-11/C-10** — unconditional permanent `will-change: transform`, 8 promoted layers in one card (C-10's "seven" undercount noted by both r4 readers — concordant with the bank); box-shadow repaints the promoted layer regardless. Cost → SS-13 #5. *(carried.)* **NO-WAVE-OWNER.**
- **D-13/L-12** — `grid-column: unset` is a dead declaration (`unset` = `initial` = `auto`, nothing to override); the charitable containing-block note belongs in the comment; A's rider (it is the one declaration the killed cure would flip) folds to K-8's record. *(carried.)* **NO-WAVE-OWNER.**
- **L-13/C-9** — `--radius-pill` is an import-invisible producer-token edge; the F-1 limb stays a fold (≡ census F-1/SCH-1 → KF.W0 §B-12); K-14 carried (~1px caps are real); B's parser-free `/math` subpath cell concordant with superlative 7. *(carried.)* **KF.W0 fold + NO-WAVE-OWNER residue.**
- **D-8 / C-§5** — the PRM gap: glass-ui's blanket cannot reach a custom-property-driven transform; ruled no-charge with the one-rule nicety recorded (B's MINOR re-contest declined — K-31; A concordant INFO, adopting C-§5's CORRECT-absence ruling). *(carried.)* **KF.W9.**
- **D-16** — forced-colors: zero handling tree-wide; mitigated by the earned `aria-hidden` + the value carriers (with N-14's thinness qualification). *(carried; A concordant.)* **KF.W9.**
- **L-14** — the SFC boundary is ceiling-driven, not contract-driven (the undeclared token cascade is the real interface) — the frame that made D-1/D-2 possible, vindicated by ruling 1's root cause; now compounded by N-19 (the one written statement of that interface is wrong). *(carried.)* **NO-WAVE-OWNER (packet-spec framing).**
- **D-9/C-11** — stays CLOSED under R.F8 with the annotation rider (**the discharged remedy is the defect**); ruling 4 this corpus: A's M-9 ≡ the rider, B's endorsement cell corrected, A's re-file killed a third time (K-15). **KF.W6 rider (F8 annotation).**
- **D-15** *(r3 registry repair)* — all-physical geometry; project-level RTL posture, zero `dir=` surface; correctly INFO. *(carried; A concordant.)* **KF.W9.**
- **N-5** *(r2; 3 derivations)* — `--stagger-max` written inline on every render, read by no stylesheet. *(carried.)* **NO-WAVE-OWNER (dead-declaration sweep).**
- **N-6** *(r2; 2 derivations after B-MISSED-5)* — `pointer-events: none` at :85 is a dead re-declaration (inherited from :30); the "interaction-transparent by construction" superlative is :30 alone — A's S-4 re-credited the dead pair and inherits the qualification. *(carried.)* **NO-WAVE-OWNER.**
- **N-7** *(r2)* — `--scrub-dir: 1` at css:24 can never be observed (the sole `.seq-stage` carries the inline binding on every render). *(carried.)* **NO-WAVE-OWNER.**
- **N-8** *(r2)* — the seam is stylable from the owning side; the natural home for the stage-geometry rules and the D-2 vertical cure. *(carried.)* **NO-WAVE-OWNER (packet-spec input).**
- **N-9** *(r2)* — `var(--ball-tone, var(--color-progress))` written out six times in 68 lines. *(carried.)* **NO-WAVE-OWNER.**
- **N-10** *(r2)* — no test, no gate, no story names this component; the packet's born-RED list starts with the three-rect equality + the axis-terminal-label = duration gate. *(carried; C-§5's "no gate" row remains the corpus's best explanation of WHY.)* **NO-WAVE-OWNER.**
- **N-11** *(r2)* — the manifest triangulation (fold ≡ census F-1/SCH-1, KF.W0 §B-12); the pattern ledger now carries r2's two inheritance events + K-21 + K-30's arithmetic-slip variant. *(carried.)* **KF.W0 fold.**
- **N-14** *(r3; + r4 rider M-6, verified)* — the accessible equivalents are real but thin: no `aria-valuetext` (a bare 0-100 integer), no Metric live-region path. **Rider: the thinness is domain-deep** — the ms master-clock domain has no accessible OR visual-numeric exposure anywhere (Metric %, slider valuenow %, timecode a unitless 0-1 scalar — a THIRD normalized exposure; the axis naming ms is itself `aria-hidden` with no counterpart; the row handles announce ms correctly, the counter-example done right). **Sequencing note (this seat's):** the valuetext-in-ms cure is BLOCKED on N-1's domain decision — there is no honest millisecond to announce until the scene picks a clock. *(carried + rider.)* **NO-WAVE-OWNER (one-binding cure, sequenced after N-1; Metric live-region = BH relay if pursued).**
- **N-15** *(r3)* — `.cascade-chase { --seq-glow: 0 }` (:216) is a dead redeclaration of the `.seq-target` default; delete :216, keep :217. *(carried.)* **NO-WAVE-OWNER.**
- **N-16** *(r3)* — four quarter rules serve five tick labels; the 100%/1600ms tick is unruled. *(carried.)* **NO-WAVE-OWNER (axis half of the packet).**
- **N-17** *(r3)* — the boot truncates row-5's lane-drop 20ms early (760ms timeout vs [360, 780]ms animation). *(carried.)* **NO-WAVE-OWNER (hygiene).**
- **N-18** *(NET-NEW r4 — A M-4, ruling 10; INFO, A's MINOR preserved as dissent)* — **two elements in one card are named "master playhead" and sit on two differently-inset rails ~80px apart at p=0 for the same value** (playhead field Card+6rem → Card−2rem; scrub rail Card+1rem → Card−1rem; Δ = 80px at p=0 shrinking to 16px at p=1 — re-derived by this seat from the `px-4` paddings and the ball's `−18px` centring). A transport control legitimately owns a full-width rail — which is why this is INFO — but it is a strictly larger same-value cross-element disagreement than the 12px the corpus headlined, it is the right calibration datum for D-1's grade, and it multiplies N-12's affordance trap (the naming limb lives THERE, not re-booked here). Witness → SS-13 #12. **NO-WAVE-OWNER (packet naming decision: one element gets the name).**
- **N-19** *(NET-NEW r4 — A M-8, ruling 10; INFO)* — **the one written statement of the component's real contract is wrong on both axes**: the header (:6-8) enumerates 4 of the 7 external reads (omitting `--z-seq-playhead`, whose failure is silent and structural; `--radius-pill`, the cross-package edge; `--color-progress`, the six-site terminal fallback) and attributes all four to "the parent .seq-stage cascade" when `--ball-tone` is owned by `.seq-target` (css:7-8) and `--scrub-dir`/`--z-seq-playhead` by `.seq-storyboard` (:22-24). Novelty verified against C-2 (whose limbs are the undeclared-cascade count and the fallback inversion — it never audited the header's own list) and L-14 (the abstract frame). This is where a maintainer would actually look; it compounds L-14 and feeds the D-4/D-5/L-7 prose sweep (checklist entry #8). **NO-WAVE-OWNER (prose sweep; the cure is the L-14 interface block, stated once, correctly).**
- **N-20** *(NET-NEW r4 — B MISSED-6, ruling 10; INFO, scope corrected)* — **the playhead is the one stage element with no power-on treatment**: the boot wipes the axis in and drops the rows (css:220-227) while `.seq-playhead-track` matches neither selector — so for the ~760ms ignition (fired on mount, at p=0) the fully-painted line, diamond, AND the label-overhanging comet (D-6's state) sit over an assembling frame. Scope: holds within the stage plate (the scrubber section is outside the assembling frame). Cheap cure inside the boot's existing idiom (one added selector arm); PRM posture inherits the boot's existing kill (:238-243). **NO-WAVE-OWNER (with N-17, the boot pair).**

**Folded-by-identity (carried, not rows):** (1) the **C-§4 export-surface correction** ≡ banked kf-KeyframeTimeline C-15 (→ KF.W7) — this corpus contributes two MORE clean handlings (A's S-7 re-verification; B's re-enumeration killing D-17 a second time, ≡ K-3); (2) the **F-1 limb** ≡ the banked substrate row (KF.W0 §B-12; B's lockfile grep concordant).

## Killed-claims register — 33 entries

K-1..K-15 *(r2, carried in force; compressed)*: **K-1** D-7's theme-inversion (light 3.32:1 > dark 1.90:1; re-engaged r3 K-21, r4 K-30) · **K-2** the "sole motion element" clause · **K-3** D-17's geometry-delegation prescription (killed again by BOTH r4 readers — four generations concordant) · **K-4** the F-1 restatement law · **K-5** "no stacking context on `.seq-stage`" (≡ B's L-3-WALK kill this corpus, concordant) · **K-6** D-1's BLOCKER + the quarter-rules device (≡ B's D-1-SEV kill this corpus, concordant — with B's good strip-scoping cell adopted: the `.stage-field-x` rules live only in the axis's 17.6px band the line never enters) · **K-7** D-2's row-gap overhang limb · **K-8** the delete-the-insets cure (shrink-to-fit → `100cqw` = 0; re-fired r3 K-20, r4 K-28) · **K-9** the give-it-an-engine-target fix · **K-10** D-3's "either token" overreach · **K-11** the mobile detached-head picture · **K-12** L-6's registration-makes-the-fallback-honest rationale · **K-13** D-12's interpolability misdirection · **K-14** L-13's "essentially nil" self-demotion · **K-15** D-9's re-file of the R.F8-closed item (re-fired r4 against reader-A, ruling 4).

K-16..K-25 *(r3, carried in force; compressed)*: **K-16** the booking-kill of the r3 readers' "missed by all axes" filings (re-fired r4 as K-26) · **K-17** ditto N-4/N-5 (re-fired r4 as K-27) · **K-18/K-19** DU's re-asserted D-3/D-2 limbs · **K-20** DU's endorsement of C-§6.1's delete-the-insets · **K-21** DU's inherited D-7 byte ("identical-to-wrong ≠ exact") · **K-22** the six r3 grade re-contests (re-fired r4 as K-31) · **K-23** the comet-alpha understatements (re-fired r4 as K-32) · **K-24** DU-m2's Reset implication (defaults restore ⇒ frozen 1940 coincidentally right) · **K-25** LC-M-1's non-constant rider (frozen denominator ⇒ constant 1.2125; revives with N-2's cure).

New this corpus:

| # | killed claim | adjudication |
|---|---|---|
| **K-26** | A M-1/M-2 + B MISSED-1/MISSED-2 filed as new/"missed by all three axes" MAJORs | **BOOKING KILLS (ruling 2)** ≡ banked N-1/N-2 — recorded as the 4th and 5th independent derivations of each; three cells adopted into the banked rows. The recurring failure fired its second full generation: the readers re-derive brilliantly and do not read the bank |
| **K-27** | A M-3 + B MISSED-4 (ball-vs-gate), B MISSED-3 (`--scrub-dir` reset/reverse), B MISSED-5 (dead `pointer-events`) filed as new | **BOOKING KILLS (ruling 3)** ≡ banked N-4 (now 5 derivations, one cell adopted) / N-3 (4) / N-6 (2) |
| **K-28** | A M-9 (mark F8 superseded — filed as a missed corpus-hygiene defect) + A M-10 (the corpus cure underspecified three ways) | **BOOKING KILLS (rulings 4-5)** — M-9 ≡ K-15's standing annotation rider ("the discharged remedy is the defect", KF.W6); M-10 ≡ K-8/K-20 + the ruling-1 cure that moots limb (c). M-10's limb-(a) mechanism corrected: css-grid-1 §8.3 counts negative indices from the **explicit** grid, never the implicit — `1 / -1` collapses to `1 / auto`; same conclusion, wrong law |
| **K-29** | **Reader-B's D-1-SUBGRID / C-1-SUBGRID kill** ("Both arithmetics are wrong … 3.625rem would place it LEFT of the parent column, impossible … 5rem would require the whole gutter absorbed on one side. L-1 is the only axis that states it correctly") | **THE KILL IS KILLED (ruling 1) — the register's first false kill.** D-1's 3.625rem is content-left-framed (its own table, challenge-D:26-28) ≡ padding-box 4.625rem ≡ the number B endorses from L-1; C-1's 5rem is the absorbed-left BRANCH in C's padding frame (challenge-C:110), the same branch D:75 names — a branch statement, not an arithmetic; and B's own kill sentence conflates the frames ("4.25rem (content-left + 3.25rem …)"). C-1's real defect — the false "exhaust the space" omitting the centred branch — was already stated by reader-A and already banked (SS-13 #1: 4.25/4.625/5rem). The branch remains UNPROVEN-NEEDS-LIVE; no static seat, including this one, adjudicates it |
| **K-30** | Reader-B's D-7 chain cell: "My OkLab→linear-sRGB→sRGB chain gives the light arm rgb(126, 90, **229**) … Bevel = rgb(216, 206, **247**)" | **CELL KILLED (ruling 6)** — this seat's own encode: b_linear 0.6020 → byte **204**; bevel **240** — concordant with reader-A, r2's product-converter run, and r3's K-21 node run. A fresh arithmetic slip claiming recomputational precision (distinct from K-21's inheritance class); the 1.347:1 ratio conclusion survives, which is exactly why the byte had to die. Fourth wrong-byte event in this component's ledger (D 216 → DU "exact" 216 → truth 204 → B 229) |
| **K-31** | Grade re-contests: A's D-3 MAJOR · B's D-8 MINOR · B's D-11 INFO (downward) · B's L-10 MINOR | **SEVERITY KILLS (ruling 7; K-22 re-fired, third generation)** — no new evidence in any cell; banked MINOR/INFO/MINOR/INFO stand. B's D-11 downward contest dies on B's own open-NaN-path evidence |
| **K-32** | Reader-B's comet-alpha cell ("≈6% at rest, ≈26% while scrubbing") | **K-23 RE-FIRED (ruling 9)** — D-6's uncorrected band averages restated; banked ≈8.3-8.8% / ≈35.6-37.5% stand; reader-A's 8.75%/37.5% is the first reader-generation concordance and is adopted |
| **K-33** | Reader-A's L-10 rider: "the file is 163 lines not 162" | **CELL KILLED (ruling 11)** — `wc -l` → 162; L-10's original count was right. A's :33→:34 binding-drift catch stands. Recorded because a correction that introduces the error it corrects is this register's smallest recurring failure mode |

## Superlatives (provenance preserved)

1. **The sweep transform** (:46) — container on the parent, `cqw` consumed by the child, self-centring `-50%`, compositor-only; the hand-synced contrast case in the sibling. All three axes + all six re-readers across r3/r4. *(carried.)*
2. **The diamond's transform order** (:61) — translate against the un-rotated box; the inversion throws the head ≈5.66px off. **Qualification carried (r3, LC): the guarantee is borrowed** — Tailwind's preflight `border-box` is what makes `-50%` exact over the 1px borders; reader-A's r4 restatement omits the qualification and inherits it. *(carried.)*
3. **`aria-hidden` is earned** — the value survives in accessible forms — **with N-14's thinness qualification, now domain-deep via the M-6 rider**: every exposure is normalized (%, 0-100, 0-1); the ms domain the ruler names is exposed nowhere; the row handles are the counter-example done right. *(carried + deepened.)*
4. **Zero teardown surface, by construction** — one import, one `defineProps`, two divs, one binding. *(carried; both r4 readers re-found it.)*
5. **The T.G4 compositor claim holds by enumeration** — :46 is the file's only per-frame-varying declaration (reader-B independently re-enumerated: box-shadow varies per-gesture, `scaleX` per-direction-change). *(carried.)*
6. **The comet flip mechanism** (`right: 50%` + `transform-origin: right center` + `scaleX(±1)`; `scaleX(0)` closed at the writer, useSequenceInstrument:22) — mechanism excellent, driver under-wired (N-3/N-13); reader-B's r4 superlative self-qualifies with exactly the banked narrowing ("does not close the reset/reverse modes") — the first reader to file the superlative and its limit in one cell. *(carried.)*
7. **The one visible dependency edge is the correct one** — `@mkbabb/value.js/math`, declared, 1110 import-free bytes, parser-crash class unreachable; the edge is forced, not lazy (the barrel does not re-export the leaf clamp). *(carried; B's C-9 cell concordant.)*
8. **The corpus's kill sections survive hostile re-reading — now through a THIRD full generation, with the discipline's two counter-examples both caught by adjudication**: r3's inherited byte (K-21) and r4's false kill (K-29) + fresh slip (K-30). The lesson sharpens rather than voids: kills must carry their reference frame and their arithmetic, because the register will re-derive both. *(carried, extended.)*
9. **State as parameter, not as selector** — the entire scrub-heat response rides one inherited scalar through three `calc()` sites; zero extra selectors, zero class plumbing; reader-A's r4 "token discipline above the demo's mean" cell (six of eight colour productions tokenized; percentages parameterized by `--seq-glow`) is this superlative restated and is folded here, not double-booked. *(carried.)*

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The three-rect measurement** — `.seq-axis` / `.seq-playhead-track` / one `.seq-track` `getBoundingClientRect().left` in one mount: settles the 12px origin, the subgrid-gutter branch (**4.25 / 4.625 / 5rem — the ONLY seat that may rule it; K-29 stands as the warning against ruling it statically**), and N-1's ~67px gate offset in one pass. *(carried, re-armed by ruling 1.)*
2. **The comet wash percept** at p=0 (≈8.8% rest / ≈37.5% scrubbing over the `@ms` sub-line), both themes. *(carried.)*
3. **Paint order at a playhead↔handle crossing** (D-3's before-frame; capture at `--seq-glow: 1`). *(carried.)*
4. **The diamond-tip vs tick-label collision — ENRICHED (r4, M-7): capture at p=1 specifically**, where the head shares the axis's right edge with the right-hugged '1600' label (`translateX(-100%)`, Axis:39-41) and p=1 is every play-through's rest state; desktop vertex incursion 2.26px, mobile vertex 0.14px BELOW the axis box; whether glyph bottoms are reached stays open. Both breakpoints. *(carried + enriched.)*
5. **The will-change layer cost** (8 hints, trace with/without). *(carried.)*
6. **N-4's percept** — does ±12.8px still read as "resting on the gate". *(carried.)*
7. **N-3's witnesses** — jitter flicker; the streak leading under `reverse()`; the stuck mirror after one backward drag. *(carried.)*
8. **D-7's residue** — the light-theme silhouette softness (≈1.35:1), looking question only; the byte ledger is CLOSED (204/240, three concordant derivations + this seat's). *(carried.)*
9. **N-12/N-13's compound witness** — grab the line inside a row band near a gate → row re-times, no glow, no affordance; the diamond-grab is inert. *(carried.)*
10. **The ruling-1 cure's before/after** — the three-rect probe after `gap: 0.5rem var(--col-gap)`; all lefts equal, column 2 narrower by 12px, the non-subgrid fallback branch (`grid-template-columns: var(--label-col) 1fr`, css:63/:70) included. *(carried.)*
11. **NEW — N-2's one-keypress witness**: focus row 5's handle, press `End`, play to p=1 → photograph the lane frozen ≈38% mid-glide with the playhead parked at the track's right edge; then Reset → confirm the coincidental re-truthing (K-24). The born-RED material for the engine-side rider.
12. **NEW — N-18's side-by-side frame**: one screenshot at p=0 capturing both "master playheads" 80px apart; the packet's naming decision wants this single image.

## Identity guards (fold-by-reference — never re-booked)

- **The F-1 limb ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1** → KF.W0 §B-12; the pattern ledger now holds four wrong-byte/inheritance events (r2 ×2, K-21, K-30). *(carried)*
- **The C-§4 export-surface correction ≡ kf-KeyframeTimeline C-15** → KF.W7. *(carried; two more clean r4 handlings recorded — both readers killed D-17 on it independently)*
- **N-1 / N-2 / N-4 are scene-level homes** — forthcoming kf-SequenceAxis / kf-SequenceTarget / kf-SequenceScrubber adjudications fold their domain, denominator, and gate-offset findings HERE (anti-rename); this corpus's ten booking-kills (K-26..K-28) are the law working, twice over.
- **N-2 remains the engine row's only booking** (no other `Sequence`/`_duration` row in the registry — re-checked); B's fourth-clamp-site cell now lives inside it and D-11.
- **D-9 stays closed under R.F8**; the annotation rider rides KF.W6; neither endorsement (B) nor re-file (A) moves it.
- **D-1 ≡ LC-M-2 (root cause)** — one identity, ruling-1's repair; **N-18's naming limb ≡ N-12's cell** — one identity, the geometric datum lives at N-18, the affordance trap at N-12.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold (§B-12). — **KF.W6**: C-3 + the D-7 residue (the `--specular`/`--shade` material register; cross-repo relay if producer-owned) · the R.F8 annotation rider · the flat-namespace datum. — **KF.W7**: the C-15 fold. — **KF.W9**: the PRM datum · the forced-colors datum · D-15 (RTL posture) · the SS-13 list.
- **NO-WAVE-OWNER — the sequence scene-repair packet (SS-1/SS-2 authoring must home it):** the **instrument-truth core N-1 · N-2 · D-1 · N-4** — with the ruling-1 cure (`gap: 0.5rem var(--col-gap)`; SS-13 #10), the K-25 sequencing note (N-1's domain cure and N-2's recompute cure land against each other), and M-10's adopted frame (the repair is a decision about which of four domains is canonical — one spec, not four patches) · the **interaction rows N-12 · N-13 · N-3 (+ N-18's naming decision)** — one gesture spec: what the playhead affords, which gestures light the well, when the streak flips, which element carries the name · the geometry commit D-10/L-2 + D-2/L-4/C-5 (via N-8's owning-side home) · the stacking row D-3/L-3 (the one-deletion) · the property-hygiene family D-11 + D-14 (census: 12/9 + 3) + L-9 + the dead-declaration family D-13/L-12 · N-6 · N-7 · N-15 · N-5 · the prose-truth sweep D-4 (family of four sites, three names) · D-5 · L-7 · **N-19** (the checklist is now eight comments incl. the header's interface list) · D-6's clip one-liner · D-12's transition pair · **N-14's valuetext cure, SEQUENCED AFTER N-1** · N-16/N-17/**N-20** boot-and-axis hygiene · L-10/L-11/N-9 postures · L-14/N-8 (spec framing) · N-10 (the packet's test obligation: the three-rect equality + the axis-terminal-label = duration gate, both born-RED).

## Verdict

**ADJUDICATED.** A fourth reading generation and the record finally cut both ways: the readers re-derived every load-bearing fact and the register caught the readers — one false kill killed (a reference-frame conflation dressed as arithmetic hygiene), one "exact" byte that its own chain cannot produce, four grade re-contests declined on their own evidence, and ten of sixteen "missed by all three axes" rows booking-killed against a bank the readers never opened — while the same corpus delivered the first correct reader derivations of the D-7 byte and the comet alphas, the engine's fourth NaN-transparent clamp site, and three honest INFO admissions (two rails wearing one name, a header that misdescribes its own interface, a boot that assembles everything except the element painted over it). N-1 and N-2 now stand at five independent derivations each — confirmation compounding, never re-booked. The component is unchanged by all of this: a right-shaped 87-line leaf whose prose lies in eight places, whose numbers are inherited from four files that disagree about two clocks and one gutter, and whose repair remains a packet — one canonical domain, one gesture spec, one token consumed by its own declarer — not a rewrite. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life, N-1..N-20 for life.

— end of apotheosis r4. The three challenger records and both r4 re-reader returns remain immutable beside this file per E-1/E-3; the superseded r3 record at this path is replaced whole with its identities, rulings, kills, superlatives and residues carried above, per the supersession note.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/sequence/SequencePlayhead.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SequencePlayhead.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

