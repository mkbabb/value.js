SEAT RECEIPT — served model id: `claude-fable-5` (Fable 5), FRESH ADJUDICATOR (M-23/L-18), observed in this seat's own system prompt, not inherited.

# EmptyState — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Date**: 2026-08-04 · **HEAD at this seat's reads**: `ddf2d5f8` · branch `tranche-u` · **Seat**: `demo/shared/ui/EmptyState.vue` (105 L, read whole this session; every cited anchor re-resolved at this HEAD — the corpus's `80fc5c40`/`c654824e` anchors drift by a few lines in consumer files, e.g. the PalettesPane badge now sits at `:19-25`; nothing material moved).
**Corpus**: `challenge-{D-design,C-implementation,L-library}.md` + both `.pass1.md` files + `evidence/resolution-trace.txt` + `probes/` (30 committed PNG witnesses). **r2 governs** the D and L axes. Motion law: `audit/codex-provenance/motion-quarantine.md` — **grep run this session: the quarantine contains ZERO EmptyState rows**, so no FALSE row constrains this corpus; the nearest law (M-09/M-13) classifies a species-swap contract as state, not motion, and D-21 is carried under that classification.
**Re-readers adjudicated**: design-and-usage · library-and-code. Both were challenged back: one of their kills was killed (R-3), one of their CONFIRMED collaterals was killed (R-3), one of their new MAJOR finds was killed as a registry re-mint (K-16), their shared cure for the weight breach was killed (R-7), and their shared UNPROVEN filing on the corpus's lead BLOCKER was overruled to CONFIRMED (R-1).
**Method**: every verdict-level disagreement and every load-bearing cell re-derived with this seat's own instruments — whole-file reads: EmptyState.vue (105 L), PaletteCardGrid.vue (whole), ErrorBoundary.vue (:1-30/:43-51/:59-70/:84-86), Markdown.vue:15-40, Dock.vue:260-290, CurrentPaletteEditor.vue:90-110, MixSourceSelector.vue:160-180/:235-245, BrowsePane.vue:38-90, AdminTagsPanel.vue:62-90, AdminUsersPanel.vue (seat grep), AdminNamesPanel.vue (seat grep), useAdminTags.ts:48-62, usePalettePorts.ts:105-118, PalettesPane.vue:15-30/:70-85, viewSchema.ts:185-232, foundation.css:218-232/:376-384/:644-700, o9-shadow-palette.spec.ts:60-80/:230-270, challenge-C-implementation.md:24-104/:345-360. Installed glass-ui 7.0.0 dist bytes: `WatercolorDot.vue.d.ts` full prop union (no `tag`, `{}` slots); compiled `watercolor-dot.js` (`renderSlot|$slots|slots` → **0**, `tag` → **0**, `aria-hidden:"true"` root); `glass-ui.css` ghost-stroke rule order at char ~69023 (the container rule then the `border` shorthand, byte-witnessed); `var(--watercolor-dash` → **0** consumptions; `styles/typography/semantic.css` (`text-heading` = font-text + `var(--type-weight-heading)` + `line-height: var(--type-leading-heading)` + `text-wrap: balance`; every `text-display-*` = `font-weight: var(--type-weight-display)`); `utilities.css` (`text-mono-caption` = mono + `var(--type-tracking-caps)` + `uppercase`); `scale.css` (`--type-weight-heading: 700`, `--type-tracking-caps: 0.1em`, producer `--type-weight-display: var(--font-display-weight)` = 600). Censuses re-run: `import EmptyState` → **8 files**; `dots` consumers → **0**; `emptyAction` → declaration only (PaletteCardGrid:28-29); `.plate-ink` → **5 files**; `--ink-muted` → 33 raw hits / 12 files, **zero `--ink-muted:` static declarations**, sole writer `useAtmosphereBoot.ts:103`, two bare no-fallback reads (`ParseEchoReadout.vue:38/:44`); `tag="` in demo → **26 raw / 21 WatercolorDot bindings** (3 TransitionGroup + 1 SearchBar `tag="form"` + 1 comment excluded — the reader-2 kill arithmetic reproduced exactly); `role="listitem"` → **0 repo-wide**; PaletteCard root `role="article"` (:22); `@vue/test-utils` consumers → **0** (devDep `^2.4.10`); vacuity greps (`Add colors above`, `Failed to load palettes`, `plate-ink` in test/ e2e/) → **0/0/0**; `withDefaults(` → 4 files; `<Transition` census over all 8 hosts → **only BrowsePane.vue:40 wraps a plate**; all five admin views `right: "palettes"` (viewSchema.ts:188-231); `tsconfig.base.json:11` `exactOptionalPropertyTypes: true`; package.json `dependencies` = glass-ui ^7 + keyframes ^6 with `files:["dist",…]`, glass-ui peer `@mkbabb/value.js ^4.0.0`; canon lines VC 82/101/113/114/141/154/184/186/218 and PA 48/49/52/68/70/71/76/78 all read verbatim. Zero browser tooling (law); every live-render residue is declared, never silently absorbed.
**Wave authority for routing**: tranche X ratified (`docs/tranches/X/waves/W0..W11`). **X-W7** owns `demo/shared/ui/EmptyState.vue` by name (modify-carve at W7 §files; X.W7.g deletes the `eyebrow` prop *and* element and re-rules `o9-shadow-palette.spec.ts` in the same commit — W7.md:62/:268/:522). **X-W4.g** (trigger-gated on the X-W0.j census) holds the WatercolorDot impostor seats and the `WatercolorSwatch.vue` successor, and cross-declares `EmptyState.vue (X-W7)`. **X-W5** owns viewSchema's physical pane axis and Dock. **X-W8** owns `demo/ui` + the dead `demo/@` globs (W8.md:42). **X-W9** owns `package.json` (W9 §files) and already carries a standing glass-ui relay packet (W9.md:318-324). **X-W10** owns type/design canon. **X-W1** owns the oracle re-gate.

## Scoreboard

**36 CONFIRMED (2 BLOCKER · 14 MAJOR · 17 MINOR · 3 INFO) · 17 KILLED/DEMOTED (incl. one kill-of-a-kill reversal, one killed reader-CONFIRMED collateral, one reader "missed" find killed as registry re-mint, and one shared reader cure) · 8 UNPROVEN-NEEDS-LIVE + 1 OWNER-QUESTION · 9 superlatives.**

Blocker pair: **ES-1** (the plate states a falsehood — "No saved palettes yet." over a search-filtered zero while the same screen's badge and sr-only span say "2 saved", in the accessibility tree as well as the pixels) and **ES-2** (activating Retry on the Browse error plate leaves a blank pane with no error, no invitation, no control — measured cross-engine by the C axis and structurally re-derived here; both re-readers under-filed it as unproven). The gravest pattern-level truth: the failure register carries the product's only actions while the empty register — the one place the user can actually do something — is a text-only dead end at all nine source sites (ES-16).

## Ruled disagreements (the incredulity seat's actual work)

| id | design-and-usage | library-and-code | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **R-1 · C-1's status** | UNPROVEN-NEEDS-LIVE ("decidable only in a browser"), attribution note to BrowsePane | UNPROVEN-NEEDS-LIVE ("structural half confirmed; outcome unverified at this seat") | **OVERRULED → CONFIRMED (BLOCKER), homed on `BrowsePane.vue:40`** | The original C axis MEASURED it: challenge-C-implementation.md:34-60 pastes deterministic before/after DOM state in Chromium **and** WebKit (`alerts:1→0`, `retryButtons:1→0`, guard at :62 still TRUE, transition host zero element children, stable at +4s) with a second cross-engine probe (`probe-deadlock.mjs`). This seat re-derived every structural precondition at live HEAD: `:40` `mode="out-in"`, `:62` guard re-satisfied post-retry, `:63` re-entering vnode same `key="error"`, `:73` the handler inside the branch it removes. The re-readers' demurral confused "I could not re-run it" with "unproven" — and reader-1 applied the opposite standard one row over, using the SAME axis's live probe as decisive grounds to kill D-19 (R-4). Corpus measured evidence + structural re-derivation = CONFIRMED; the un-banked probe script is a re-run residue (U-3), not a verdict downgrade. Attribution adopted: the mechanism is BrowsePane's transition, not the atom — cross-booked accordingly. |
| **R-2 · "three implementations of the error register" (L-N2 vs D-14)** | CONFIRMED as "third geometry" at Markdown.vue:19-30 | KILLED ("renders the glass-ui Alert primitive; no glyph, no detail, no action, no geometry, no plate-ink; cure inverted") | **reader-2's kill STANDS; reader-1's surviving limb adopted into ES-6** | My read of Markdown.vue:15-31: `<Alert><AlertTitle class="font-display text-heading">Oh snap...</AlertTitle><AlertDescription>` — a producer primitive carrying only the TYPE PAIRING, none of the five plate rows. The clone count is TWO (EmptyState + ErrorBoundary, four unexplained numeric drifts, byte-identical `.plate-ink`). What survives from reader-1: the file's own comment (:21-24) claims it "joins the EmptyState error-statement register **verbatim**" — false in geometry, true only in the (itself off-matrix) type pairing; that false register-claim rides ES-6/ES-7. L-N2's prescribed cure (compose a demo `ErrorPlate` in place of Alert) stays killed — it inverts the glass-ui-first edict. |
| **R-3 · the trio wrapper's `aria-hidden`** | CONFIRMED as D-15 collateral ("now redundant — glass-ui sets it") | KILLED the collateral ("wrapper `<div>` ≠ the producer `<span>`s; load-bearing under o9:74") | **reader-2 wins — the collateral is KILLED** | Live bytes: the attribute sits at EmptyState.vue:43 on the wrapper `<div>` (`data-slot="empty-state-trio"` :41), a different element from the three producer spans that carry their own `aria-hidden` (compiled root, my byte-read). And `o9-shadow-palette.spec.ts:74` asserts `toHaveAttribute("aria-hidden","true")` on that exact wrapper — acting on reader-1's "redundant" note reddens a standing CI gate. Defense-in-depth on a decorative subtree is not a defect. |
| **R-4 · D-19 ("the failure species has zero rendered evidence anywhere in the audit corpus")** | KILLED (refuted twice from inside the corpus) | UNPROVEN at its seat (would require reading captures) | **the kill STANDS — three ways** | (1) challenge-C renders and measures the real error plate live on `#/browse`, both engines. (2) `o9-shadow-palette.spec.ts:238-263` is a standing CI gate that DRIVES the deterministic failure and asserts the visible `role="alert"` + "The commons is unreachable." + trio count 0 + the My Palettes positive control (:263). (3) This corpus's own `probes/` directory holds `probeD-error-browse.png` and `probeD-errorspecies-{light,dark}.png` — the species is photographed IN the very corpus the claim indicts. A universal negative dies on one counterexample; no capture needs to be *read* to count three. Reader-2's demurral is overruled on the same evidence-standard ruling as R-1. |
| **R-5 · the hard-cut host census (D-21)** | "7 of 8 hosts hard-cut" | "6 of 7 hard-cut, not 4" | **RULED: 1 of 8 importing files wraps its species swap; both correction-counts stand, both original table-✔ rows false** | My Transition census over all 8 importers: BrowsePane:40 (`vj-morph out-in`, wraps the chain) is the ONLY plate-enclosing transition; AdminUsersPanel's sole `<Transition>` (:16, `vj-celebrate`) wraps the pruneResult toolbar text; MixSourceSelector's only wrapper is the swatch `<TransitionGroup>` (:120); the five other hosts have none. D-21's table credited the two exceptions to exactly two hosts that hard-cut. The finding survives WORSE than filed; the table may not be carried as evidence (K-3). |
| **R-6 · D-6's canon inference (the mark FORBIDDEN on Admin routes)** | CONFIRMED MAJOR (canon scope) | Facts confirmed; canon inference filed as an owner ruling, not a measurement | **reader-2 wins — dead-axis facts CONFIRMED (ES-4), the Admin-prohibition inference goes to the OWNER (OQ-1)** | VC:186's grant sits under the `### Palette library and Browse` heading (:184, my read); VC:218 governs Admin by "labeling and scope, not … a fourth visual system"; PA:49 (PR-05)'s subject row is boundary-repeating dividers/ornament. Whether an empty-plate mark on an Admin route is PR-05's "other divider/ornament" or outside its jurisdiction is a canon-interpretation call the corpus cannot decide. The defect that needs no ruling: `dots` defaults `true`, has zero consumers, is unreachable through the only wrapper, and a standing oracle pins the state — that is confirmed at MAJOR regardless of where the owner lands. |
| **R-7 · the weight-breach cure (both readers: `--type-weight-heading: var(--type-weight-display)` at `:root`)** | proposed as "the correct one-line cure" | proposed as "closes the previous finding for all four sites at once" | **CURE KILLED; defect confirmed (ES-8)** | The cure is adjudicated separately from the defect (the CSS-adj law, earned again here): `text-heading` is the matrix's SECTION-HEADING role (PA:78 — Plus Jakarta), consumed demo-wide; globally re-pinning its weight token to 400 de-bolds every legitimate section heading to cure four mis-paired lines. The root defect is ES-7's off-matrix pairing — re-pair the role (a display-family utility that already rides `--type-weight-display`) and the 700 dies at all four sites with zero collateral. |
| **R-8 · the ink-muted census width** | 5 `.plate-ink` blocks + the boot asymmetry | (missed find) 13 masked-fallback declarations / 10 files + 2 bare reads as the counterexample | **reader-2's widening ADOPTED with my own numbers** | My grep: 33 raw `--ink-muted` hits across 12 files; zero static declarations anywhere; sole writer `useAtmosphereBoot.ts:103`; the masked-fallback idiom spans ~10 consumer files; `ParseEchoReadout.vue:38/:44` read the token BARE — the in-tree proof the mask is optional; `ExtractControls.vue:124` shows the graphics-floored alternative (`safeCss(cssColor, GRAPHICS_CONTRAST_FLOOR)`). All folded into ES-13. |

## Adjudicated defect roster

Severity · claim · proof standard · terminal disposition. Line anchors are HEAD `ddf2d5f8`; re-resolve before curing (D-19 law). X-W7 rows land on the SUCCESSOR surface X.W7 defines (the eyebrow element is already scheduled to delete under X.W7.g; the o9 premise is re-ruled in the same commit). Dispositions are ADJUDICATED, not VERIFIED.

- **ES-1 · BLOCKER — the plate states a falsehood over a filtered-zero collection, and the contradiction is in the accessibility tree** (D-1; both readers CONFIRMED). `PalettesPane.vue:77` `:empty="pm.filteredSaved.value.length === 0"` (search-filtered — `usePalettePorts.ts:112` `useFilteredList(savedPalettes, searchQuery, …)`) against the badge (:20-24, UNfiltered `savedPalettes.length`) and the sr-only span (:25 "(N saved)") on the same screen — AT hears both "2 saved" and "No saved palettes yet." Same mechanism at `BrowsePane.vue:83` and `AdminUsersPanel.vue:63` (`users` is the query result). The API offers one `message`/one `variant`, so the filtered-empty state is unexpressible; canon puts it in a different jurisdiction entirely (VC:114 verbatim: "changed result count/state through the owning status region"). Witness: `probes/probeD-filtered-zero-populated.png`. → **X-W7** (the species/predicate contract at the successor surface).
- **ES-2 · BLOCKER — activating Retry annihilates the Browse pane; the error plate never returns** (C-1; RULED CONFIRMED at R-1, over both readers' unproven filings; homed on `BrowsePane.vue:40`, not the atom). Measured cross-engine by the C axis (before/after DOM state pasted, stable at +4s); every structural precondition re-derived at this HEAD: `mode="out-in"` over a chain whose error vnode re-enters with the SAME `key="error"` while the leave is pending, triggered from `:73` inside the branch itself. The pane body degrades to a search bar over a blank strip — no error, no invitation, no control, no announcement. The prescribed cure is separately KILLED (K-8). → **X-W7** (BrowsePane is a `demo/palettes` surface; the species-swap transition must be redesigned with ES-10/ES-11's constraints). Re-run residue U-3.
- **ES-3 · MAJOR — the failure species exists only as a content REPLACEMENT, so a failed refresh hides retained data at 6 sites / 5 routes** (D-2; reader-1's demotion from BLOCKER adopted: rows are hidden, not lost — one successful retry restores them). `useAdminTags.ts:53-61` catch sets `loadError` and leaves `tags.value` untouched (same shape re-verified by readers at useAdminFlagged/useAdminAudit); every panel orders `v-else-if="loadError"` BEFORE the list; every panel ships a real Refresh control, so the state is reachable. `BrowsePane.vue:62` is the lone site that guards with `&& displayedBrowse.length === 0`. → **X-W7**.
- **ES-4 · MAJOR — `dots` is a dead configuration axis locked in place by a standing oracle** (D-6 ≡ C-6 ≡ L-N1; facts unanimous). Default `true` (:90); consumers **0** (my grep); unreachable through the only wrapper (PaletteCardGrid:24-26/:38-44 forwards message/eyebrow/hint only); the co-seating it models no longer exists (ShadowPalette's one seat is Extract; no extract file imports EmptyState); and `o9:262-263` asserts the My Palettes plate KEEPS its trio, so `dots=false` at the one place with a second ghost register reddens CI. The mark ships unconditionally on all five Admin routes; whether canon FORBIDS it there is **OQ-1** (R-6). → **X-W7** (X.W7.g re-rules o9's premise in the same commit — the wave already owns exactly this collision).
- **ES-5 · MAJOR — the canon names `EmptyPaletteMark`; it does not exist, so the mark is hand-rolled with per-instance sizing/opacity and a stroke that does not scale** (D-7, minus its killed dash-token limb K-5). VC:186 names the primitive verbatim; glass-ui's export map has no such key; EmptyState.vue:39-48 inlines `flex items-end gap-2` + three dots at `w-8/w-11/w-6` with per-instance `opacity-80/—/opacity-60`. Producer bytes: `.watercolor-ghost-stroke` = fixed `var(--watercolor-ghost-weight,2px) dashed` against Ø24/32/44 (8.3%/6.25%/4.5% of diameter), and the producer's `container-type: inline-size` seam is pinned shut by the consumer's fixed `w-*`. → **X-W7** (mark as a named atom at the successor surface); the producer questions (stroke scaling, a first-class mark atom) ride **GLASS-RELAY** with ES-30.
- **ES-6 · MAJOR — "the ONE shared empty atom" is false: the error plate is cloned with four unexplained numeric drifts, and a third file falsely claims register-membership** (D-14 ≡ L-3, as RESCOPED at R-2). Exact drift re-measured: `gap-2.5 py-8` vs `gap-3 py-10 px-6`; `w-6` vs `w-7` glyph; `26ch` vs `28ch`; `44ch` vs `46ch`; `.plate-ink` byte-identical at EmptyState.vue:102-104 and ErrorBoundary.vue:84-86; ErrorBoundary's header comment (:12-15) declares the mirroring intentional. Markdown.vue:21-24's comment claims verbatim register-join through an Alert that shares only the type pairing. The :98 comment's "ONE shared empty atom (8 consumers)" is true as an import count, false as a register claim. → **X-W7** for the plate register; the ErrorBoundary twin is a `demo/color-picker` shell surface held by no X wave by name — **named cross-surface residue** so the unification is not silently half-done.
- **ES-7 · MAJOR — `font-display text-heading` is a family↔role pairing the closed type matrix does not contain, live at four sites, standing only on cascade emission order** (D-13, minus its killed citation limb K-6). VC §4 + PA:78 close the matrix (`text-heading` → Plus Jakarta section heading; Fraunces owns display/identity; P019 the sole exception); producer bytes: `text-heading` itself declares `font-family: var(--font-text)`, so the pairing exists only because the Tailwind `font-display` utility outranks it in emission order. Sites: EmptyState.vue:20/:58, ErrorBoundary.vue:25, Markdown.vue:27. → **X-W10** (canon) with the surface edits riding **X-W7**.
- **ES-8 · MAJOR — the plate's protagonist line renders Fraunces at weight 700, breaching the owner's pinned non-bold display edict** (NEW — both readers found it independently in missed; admitted after this seat's byte-derivation; the shared cure is KILLED at R-7). Producer: `text-heading` rides `--type-weight-heading: 700`; demo overrides it nowhere (my grep: 0); foundation.css:376-384 pins `--type-weight-display: 400` and states "every demo title surface carries `font-weight: var(--type-weight-display)` on its OWN class list" — PaneHeader.vue:110 and ColorSpaceSelector.vue:213/:301 comply; EmptyState.vue:20/:58 (and the ES-6 clones) carry no weight pin, so the display voice re-enters at 700 across all 16 call sites. The D axis MEASURED `fontWeight 700` and filed it as neutral matrix data. Cure = ES-7's re-pairing, never a global `--type-weight-heading` re-pin. → **X-W10** + **X-W7**.
- **ES-9 · MAJOR — `role="list"` is malformed in EVERY state, and the empty state makes EmptyState its only owned child** (C-3, WIDENED by reader-2's missed limb, verified). PaletteCardGrid.vue:3 `role="list"`; populated children are `role="article"` cards (PaletteCard.vue:22); `role="listitem"` occurs **zero times repo-wide** (my grep); at true-empty the `role="status"` plate (:28) is the list's sole child (axe `aria-required-children`, serious). The C-axis cure (EmptyState renders a plain div) leaves the populated-state violation standing — the defect is the `list` role itself. → **X-W7**.
- **ES-10 · MAJOR — no focus contract: the slot action lives inside the branch its own handler removes, and the repo owns the correct pattern one directory away** (C-2; structural half byte-dispositive, focus-destination measurement = U-4). All seven `#action` Retries sit inside their self-removing branches (BrowsePane:68-77 and the six admin seats, re-verified at the tags panel); EmptyState exposes no focus target; ErrorBoundary.vue:16-22/:64 does it right (`role="alert"` + `tabindex="-1"` + `nextTick(() => alertRef.value?.focus())`, WCAG 2.4.3 rationale in-file). Canon VC:113: "no focus on removed content." → **X-W7**.
- **ES-11 · MAJOR — the decorative eyebrow is announced verbatim inside an atomic polite region, the authoritative count is silent, and Admin routes structurally seat two identical live plates** (C-4 ≡ D-22; announcement reliability = U-2). :43 hides the trio from AT while the eyebrow `<p>` (:55-57) sits exposed inside `role="status"` (implicit polite + atomic); the count readout (AdminUsersPanel:8-10) is a plain span — VC:114 inverted; PA:76 lands harder than the axis's own citation ("A display-sized readout is not therefore a … live status"). Stacking is structural: all five admin views declare `right: "palettes"` (viewSchema.ts:188-231, my read) — but that companion is ALREADY ruled out by VC:218 + PA:48 (PR-04), so the stacking limb discharges at **X-W5** (reader-1's mis-homing note adopted: deleting the companion deletes both symptoms without touching EmptyState). Region semantics → **X-W7**.
- **ES-12 · MAJOR — vacuous gate: four named mutations keep the suite green; no unit test; Retry is never pressed; the harness is installed with zero consumers** (C-10 ≡ L-N8; blast-radius limb narrowed at K-15). My greps: hint strings, error strings, `.plate-ink` in test/+e2e → 0/0/0; `@vue/test-utils` → devDep, zero consumers repo-wide; only o9 references the component at all. → **X-W7** (component gates at the successor surface) with the harness re-hardening under **X-W1** (CC-031 oracle-slate).
- **ES-13 · MAJOR — `--ink-muted` has no declaration in the stylesheet layer at all; a leaf reads it through a masking fallback whose masked value is the sub-floor ink the class exists to retire; the rule is copy-pasted five times** (L-5 ≡ L-N3 ≡ C-11 + reader-2's widening, R-8; contrast numbers = U-1). My census: zero `--ink-muted:` declarations; sole writer `useAtmosphereBoot.ts:103`; the sibling `--accent-live` IS declared statically (foundation.css:231) — the asymmetry is the finding; `.plate-ink` ×5 byte-identical; the masked-fallback idiom spans ~10 files; two bare reads (ParseEchoReadout) prove the mask optional; the component's own :49-54 comment records the fallback's 3.84:1 history. → plate-side dedup **X-W7**; the token's static declaration + fallback-mask retirement have **NO-WAVE-OWNER in X** (named residue for the next formation boundary; candidate canon home X-W10).
- **ES-14 · MAJOR (repo-level) — the published library declares runtime dependencies on its own consumer's design system: a declared manifest cycle invisible to the import guard** (L-2; both readers). package.json `dependencies: { "@mkbabb/glass-ui": "^7.0.0", "@mkbabb/keyframes.js": "^6.0.0" }` + `files:["dist",…]`; installed glass-ui declares `peerDependencies["@mkbabb/value.js"]: "^4.0.0"` (both read by this seat); zero source imports of either package in `src/` (readers' grep, spot-confirmed); eslint's inv-K-1 polices specifiers, not the manifest. → **X-W9** (owns `package.json`; its W9.i glass-ui/keyframes relay packets are the coordination channel).
- **ES-15 · MAJOR — WatercolorDot 7.0.0 renders NO slot, so every child passed to it silently drops — three shipped sites lose their glyph, including the collapsed dock's view-identity icon** (reader-2's missed find, admitted after this seat's own byte-proof; rendered symptom = U-8). Compiled `watercolor-dot.js`: `renderSlot|$slots|slots` → 0 occurrences; `inheritAttrs:!1` forwarding class/style only — children, `@click`, `aria-label`, `:disabled`, `tag` all die at the boundary. Sites re-read live: `Dock.vue:271-283` (the `#collapsed` seal wraps the view icon + admin gold-shimmer + its own `vj-morph` Transition inside the dot — the :260-268 comment describes a GLYPH morph that cannot be rendering), `CurrentPaletteEditor.vue:95-105` and `MixSourceSelector.vue:164-176` (both add-slot ghosts with `tag="button"`, `@click`, `aria-label`, a `Plus` child — the "impostor seats"). REGISTRY CATCH: this is substantially a standing identity — **X-W4.g** already enumerates the impostor seats by line (Mix `:168`/`:215`, CurrentPaletteEditor `:98`, GenerateControls `:203`, SwatchHoverMenu `:17`/`:32`) and builds `WatercolorSwatch.vue` + `a11y-swatch-seat.spec.ts`; the genuinely new limbs are the static no-slot proof (which strengthens X-W4.g's born-RED) and the Dock `#collapsed` child-swallow. → **X-W4.g** (trigger-gated; cross-declared) with the Dock limb noted to **X-W5**; the "should the dot ever host children/a seat?" question rides **GLASS-RELAY**.
- **ES-16 · MAJOR — the empty species has zero actions at every one of its 9 source call sites; the invitation is a text-only dead end whose three navigations ride as monospace prose; the one slot forward that could cure it has no consumer** (reader-1's missed find — pass-1 D-1 dropped unadjudicated by pass 2; admitted on this seat's greps). All seven `#action` fills are error-variant; `emptyAction` exists only as PaletteCardGrid:28-29's declaration (my grep); the three how-to sentences (PalettesPane:80, BrowsePane:86, Mix:243) do buttons' work as `text-mono-small` prose. PA:71 verbatim: "Add affordance when the surviving action/state is otherwise undiscoverable … Subtraction precedes explanation." → **X-W7** (the .g copy abrogation + .c/.d action-ownership units are the cure surface).
- **ES-17 · MINOR — inverted `ui/` ownership: 19 pure re-export barrels, zero components; the demo's real atoms live in `demo/shared/ui/`** (L-N4; pass-1's two-Button-sources limb killed at K-13). → **X-W8** (owns `demo/ui` + the graph subtraction; cross-booked with the standing W-HYGIENE 90-site extirpation identity, ColorPicker-adj L-6).
- **ES-18 · MINOR — the hand-rolled `tsconfig.demo.json` export surface has drifted from the generated one; three targets missing after a full build; currently inert (trace banked)** (L-N5; `evidence/resolution-trace.txt` re-cited, inertness proof stands). ONE identity with the standing **W-HYGIENE config-parity row** (ColorSpaceSelector-adj L-14, born-RED generator-parity command; cross-booked again at Markdown-adj L-1) — never re-minted. **NO-WAVE-OWNER in X** (named residue; nearest mechanism X-W9.f's config carve, unowned by its text).
- **ES-19 · MINOR — the e2e oracle asserts glass-ui private internals (`[data-variant="ghost"]`, `.watercolor-ghost-stroke`) one line past the component's own published seam** (L-N6; line cite corrected to o9:75-76; the good seam `data-slot="empty-state-trio"` :41). → **X-W1** (oracle re-gate) coordinated with **X.W7.g**'s same-commit o9 re-rule.
- **ES-20 · MINOR — `tag="div"` ×3 passed to a prop glass-ui 7.0.0 deleted; silently swallowed** (D-15 ≡ C-5 ≡ L-1 core; count and cure ruled at K-9: **21** WatercolorDot bindings / 26 raw hits / 11 files — mechanical deletion "at all 18" would strip a Vue built-in from three TransitionGroups and a real SearchBar prop; the aria-hidden collateral killed at R-3; the "`div` host" comment note stands corrected to the `<span>` root). The component's own 3 die in any X-W7 edit; the repo-wide sweep is the standing glass-7 `tag=` residue identity (CSS-adj L-8) now embodied in **X-W4.g**.
- **ES-21 · MINOR — `tracking-[0.18em]` is an arbitrary literal at 1.8× the producer's certified caps tracking, and two dead utility classes ride the same two lines** (D-16 + the readers' dead-class missed finds, folded). Producer bytes: `text-mono-caption` already sets `uppercase` + `--type-tracking-caps: 0.1em` (so :55's `uppercase` is a no-op); `text-heading` already sets `text-wrap: balance` (so `text-balance` at :20/:58 is a no-op ×2, ×2 more in the clones). → **X-W7** (dies in the carve) under **X-W10** canon.
- **ES-22 · MINOR — `justify-center` is an inert no-op duplicated in both branches; `py-8` and the `w-8/w-11/w-6` trio are fixed sizes in a container-scaled system** (D-11 ≡ D-12; VC:33). → **X-W7**.
- **ES-23 · MINOR — `break-words` guards one of three text rows; the clipping ancestor (`contain: content`, PaletteCardGrid:51) exists only in the two grid hosts, so the failure differs by host** (D-17 + reader-1's containment refinement; correctly latent — all 16 sites pass literals, and the one row receiving unbounded machine strings IS the guarded one). → **X-W7**.
- **ES-24 · MINOR — RTL: the mark's 24/44/32 composition mirrors, trailing punctuation reorders, and the machine `detail` string has no LTR isolation** (D-18; structural, no measurement needed; VC:154 verbatim). → **X-W7**.
- **ES-25 · MINOR — the `26ch` measure is authored in a display serif whose `ch` far exceeds its average advance; inert at today's 462px pane, it arms the moment X-W5 lands Admin full-width** (D-10; the 46%/38-char figures = U-6; reader-1's pass-1 inertness refinement adopted). → **X-W7** with the X-W5 arming condition stated.
- **ES-26 · MINOR — the component owns no motion contract; the species swap is left to each host, and 7 of 8 importing files hard-cut a ~200px slab** (D-21 core, census RULED at R-5; the quarantine holds no EmptyState rows — carried as a state contract per the M-09/M-13 classification, never as a reduced-motion claim). → **X-W7** under **X-W10**'s motion canon.
- **ES-27 · MINOR — the Retry affordance is re-authored at every error site with a per-instance `class="font-display"` override, seven times** (L-8 bullet 1; reader-2's count correction adopted — includes BrowsePane:72). → **X-W7**.
- **ES-28 · MINOR — flat prop bag, not a discriminated union: `eyebrow`/`hint`/`dots` are inert on `error`, `detail` inert on `empty`, no compile error; the file's own header says "TWO species, never conflated" and the type conflates them** (C-7 ≡ L-4; latent — no call site crosses today). → **X-W7** (the successor's typed species contract).
- **ES-29 · MINOR — the P10 weight-token retirement is due and unverified: glass-ui 7.0.0 SHIPPED the seam foundation.css:376-384 says does not exist** (reader-2's missed find; my byte-proof: every `text-display-*` utility rides `var(--type-weight-display)`, producer default 600 via `--font-display-weight`, so the demo's `:root` 400 pin now flows through automatically and the per-surface pins are dead scaffolding on a false premise). ONE identity with the CSS-adj/Markdown-adj **producer-state-correction** law (stale-cell repetition is itself evidence). → **X-W10** (the comment correction + BOOKED verify), with the ES-7/ES-8 sites cured by re-pairing, not by this seam.
- **ES-30 · MINOR (producer) — the small-dot legibility affordance is dead by source order: the `@container (width<=48px){…border-style:solid}` rule is immediately followed by the `.watercolor-ghost-stroke` `border` shorthand at equal specificity, so it can never win; all three EmptyState dots are ≤48px** (reader-1's missed find; byte-witnessed by this seat at glass-ui.css char ~69023; pass-1's N-6 measured the symptom and left the mechanism open — it is the former). → **GLASS-RELAY** (same letter as the `tag=` migration and ES-15's slot question; standing BH channel, owner edict — no local patch, no masking override).
- **ES-31 · MINOR — the forced-colors tier-1 roster is keyed on a producer CLASS, so an aria-hidden ornament inherits an exemption whose written charter is color CONTENT** (reader-1's re-aim of the killed D-20; verified: foundation.css:653-657's charter, `.watercolor-swatch` in the :678-698 roster, `[data-color-surface]` :697 as the opt-in the trio could have been kept out of). → **NO-WAVE-OWNER in X** (named residue; candidate X-W10 canon; the WHCM engine specifics stay U-7).
- **ES-32 · MINOR — `text-destructive/80` on the failure glyph is the guard-then-alpha species the component's own comment says D6 retired, kept on the error register's one chromatic signal and copied into the clone** (reader-1's missed find; verified :19 + :99-101 + ErrorBoundary:24; VC:82). → **X-W7**.
- **ES-33 · MINOR — the `hint` renders help copy in Fira Code, a matrix role reserved for value/code/provenance** (reader-1's missed find, pass-1 D-14.2 dropped by pass 2; verified :61 + PA:78; the eyebrow's mono is separately warranted by the matrix's mono-caption clause, and the error `detail` in Fira is exactly right). → **X-W7** (the .g copy abrogation rewrites these exact strings) under **X-W10**.
- **ES-34 · INFO — off-idiom props declaration: `withDefaults` against the tree's dominant 3.5 reactive-destructure form** (C-9 ≡ L-N7a; my counts: 4 `withDefaults` files vs ~60 `defineProps<` files; the twin uses the 3.5 form at ErrorBoundary:43-51). → **X-W7** (dies in the carve).
- **ES-35 · INFO — the two roots carry a byte-identical class string, and the duplication is LOAD-BEARING** (L-N9 + challenge-C's N-4 guard, adopted by both readers and this seat): collapsing to one root with `:role="…"` patches the attribute in place and silences the species change to AT. The cure is a shared class; the roots stay separate. → **X-W7** rider (a written guard on the successor, so a polish pass cannot regress it).
- **ES-36 · INFO — guard asymmetry: the eyebrow is the only caption row that is unguarded AND non-nullable — `:eyebrow="undefined"` restores the default, `eyebrow=""` renders an empty caption plus its gap band** (reader-2's missed find; verified :55-57/:61/:23/:90; latent — all 10 configurations pass literals). → **X-W7** — DISCHARGED STRUCTURALLY by X.W7.g's deletion of the prop and element; recorded so the deletion is understood as also curing this.

## OWNER-QUESTION

**OQ-1 · Does the empty-plate mark fall under PR-05's ornament-zeroing on Admin routes, or outside its jurisdiction?** VC:186 grants the mark under the "Palette library and Browse" heading; VC:218 rules Admin's register ("labeling and scope, not … a fourth visual system"); PA:49's subject is boundary-repeating ornament. The corpus cannot decide this; ES-4's dead-axis facts do not depend on it. Route with X-W7's OM-15 receipts to the owner sitting.

## Killed-claims register

1. **D-19** ("the failure species has zero rendered evidence anywhere in the audit corpus") — **KILLED, R-4**: challenge-C's cross-engine live measurement, the standing o9 CI gate (:238-263), and this corpus's own `probes/probeD-error-browse.png` + `probeD-errorspecies-{light,dark}.png`. A universal negative with three corpus-internal counterexamples.
2. **D-23's universal quantifier** ("Not one eyebrow carries information the message does not") — **KILLED** by two of its own table rows read live: BrowsePane:84 `· the commons ·` names the place its message omits; Mix:241 `· nothing to mix ·` names the operation. Redundancy holds for ~7 of 10 rows; the MAJOR promotion rested on the universal. **Recorded tension**: X.W7.g's G19 born-RED text asserts "in all 11 the eyebrow is a verbatim restatement" — the abrogation DISPOSITION survives on the owner's OM-15 census authority, but its receipt must not repeat the false universal; the two informative eyebrows' data folds into message/hint at the rewrite.
3. **D-21's evidence table** (AdminUsersPanel ✔, MixSourceSelector ✔, "4 of 7 hard-cut") — **KILLED as evidence, R-5**: both ✔ rows false by this seat's Transition census; the finding survives worse (1 of 8 wraps).
4. **D-20 / pass-1 D1-20** ("no forced-colors design at all; inherits whatever the producer leaks") — **KILLED**: foundation.css:644-698 is an explicit, documented two-tier policy (U-F57 · BR-5) executing exactly as written, roster by name. The true residue is re-aimed as ES-31.
5. **D-7 item 2** (the `--watercolor-dash`/`--watercolor-gap` dash-rhythm arithmetic, both passes) — **KILLED**: both tokens are declared once and `var()`-consumed **zero** times (my count); the ghost outline is a CSS `border: 2px dashed` whose dash geometry is UA-derived and not addressable by those properties. The conclusion survives on border-width grounds only; every computed number dies.
6. **D-13's leading citation** ("foundation.css:379 notes text-heading's leading is producer-hardcoded") — **KILLED**: :376-384 is about font-WEIGHT, and it is stale besides — the producer tokenized both weight and leading (my bytes; feeds ES-29).
7. **The D file's self-arithmetic** ("all 10 empty-variant call sites"; "16 across 7 files") — **CORRECTED**: 9 empty SOURCE sites / 10 configurations (PaletteCardGrid's one site serves two panes' strings) / 16 sites across **8** files. Trivial, but it is the file that grades other counts.
8. **C-1's prescribed cure** ("stable root that never unmounts, swap only the interior") — **KILLED, reader-1's kill upheld**: it contradicts its own N-4 — a stable root forces either a static role (wrong for one species) or an in-place role mutation (the state N-4 proves silent); and per-species interior mounting is the populated-insertion pattern whose announcement is itself unreliable (U-2). The defect stands (ES-2); the cure may not be handed to a wave author unamended.
9. **L-1's count and cure** ("18 `tag=` bindings across 9 files; delete at all 18, zero-delta at the 15 decorative ones") — **KILLED, reader-2's kill reproduced exactly**: 26 raw / 21 WatercolorDot / 11 files (my census); the literal cure strips `TransitionGroup tag="div"` ×3 and `SearchBar tag="form"`; and the "zero-delta decorative" premise is false at the three ES-15 child-bearing sites.
10. **L-1's collateral** ("EmptyState's own `aria-hidden` is now redundant") — **KILLED, R-3**, against reader-1's confirm: different element (wrapper div vs producer spans), and o9:74 asserts it — acting on the note reddens a standing gate.
11. **L-N2** ("the error register has THREE implementations; Markdown.vue should compose the same ErrorPlate") — **KILLED as clone-claim and as cure, R-2**: Alert primitive, none of the five plate rows; the cure inverts the glass-ui-first edict. The false register-comment limb survives inside ES-6.
12. **L-N7 limb 2** ("`?: string | undefined` re-admits explicit undefined and cancels `exactOptionalPropertyTypes`; drop every `| undefined`") — **KILLED**: the unions are REQUIRED at the component's own consumer — PaletteCardGrid's bare `?: string` props forward as `string | undefined` under base:11 (verified), which is unassignable to a bare `?: string` target. Both other axes ruled correctly; the L axis was the outlier. (Flag location also mis-cited: base:11, not :13.)
13. **L pass-1 L-8 bullet 3** ("Two Button sources coexist; the ~178-file shadcn tree is a standing sanctioned structure") — **KILLED** (r2's own correction upheld): `demo/ui` holds 19 one-line re-export barrels and **zero** `.vue` files; the "~178 files" premise is a stale memory of a pre-restructure tree.
14. **D-5's canon citation** (PA:70 quoted as forbidding the mark) — **KILLED as quoted**: the full sentence forbids "Decorative **controls** and **operable** ornaments without names" — the mark is a static, aria-hidden, `pointer-events:none` span (producer bytes), and VC:186 affirmatively grants it. D-5's rendered-salience measurement survives only as U-1/U-5 material.
15. **C-10's blast-radius limb** ("deleting the hint kills the how-to line on EVERY empty plate app-wide") — **NARROWED**: 3 of 10 configurations pass a hint (my grep); the vacuity core is untouched.
16. **library-and-code's missed #8** (Markdown.vue's leaf global-CSS imports, offered as a new find) — **KILLED as a registry RE-MINT** (anti-rename): the fact is standing identity **Markdown-adj L-9**, already CONFIRMED with cure G-MD-6 inside V·MT-W-MD1. Cross-booked, not re-admitted. A re-reader that greps the tree must also grep the registry.
17. **Both readers' one-line weight cure** (`--type-weight-heading: var(--type-weight-display)` at `:root`) — **KILLED, R-7**: it de-bolds every legitimate section heading in the demo to fix four mis-paired lines; the root cure is ES-7's re-pairing. The cure is adjudicated separately from the defect — the corpus's own recurring law, applied to its re-readers.

## GLASS-RELAY (standing BH/BJ channel, owner edict — producer items, never local patches; rides X-W9.i's existing glass-ui packet)

1. **ES-30**: the `@container (width<=48px) { border-style: solid }` legibility rule is dead by source order against the `.watercolor-ghost-stroke` `border` shorthand — byte-witnessed; fix is producer-side rule ordering or longhand.
2. **ES-15 question**: WatercolorDot 7.0.0 renders no slot and forwards only class/style — is child/seat hosting (the pre-7 idiom four demo sites still write) intended to be unsupported forever? If yes, a d.ts-level slots type or doc note would make the swallow visible at authoring time; the consumer-side cure is X-W4.g's `WatercolorSwatch`/seat split either way.
3. **ES-5 question**: the ghost stroke is fixed `2px` against Ø24-44 consumers and the `container-type: inline-size` seam invites size-relative weights (`--watercolor-ghost-weight` exists but no size-scaled recipe) — cite the 8.3%→4.5% drift as motivating data; and whether `EmptyPaletteMark` (VC:186's named primitive) should be a producer atom.

## Superlatives (provenance preserved)

- **S-1 · The Q6 species separation is real, gated, and held** (both readers; re-verified whole): error renders no eyebrow, no trio (:14-27); empty renders both (:28-65); the ruling is quoted in the file's own header comment and o9:238-263 asserts both directions with a positive control. A design ruling that survived from prose into a standing CI assertion.
- **S-2 · The component refuses to own loading, and every host honours it** (reader-1): "loading ≠ empty, error ≠ empty" (:2-3) is enforced by real skeleton grammars at every seat, with the reason written where it matters (useAdminTags:57 — "a dead backend must never read as 'no tags yet'"). The industry's most common empty-state defect was designed out on purpose and stayed out, five routes, zero drift.
- **S-3 · The one row that can carry hostile text is the one row that is guarded** (both readers): `break-words` sits exactly on `detail` — the only row receiving unbounded machine strings. D-17 is correctly about scope, not instinct.
- **S-4 · The `exactOptionalPropertyTypes` unions are load-bearing, not noise** (challenge-C N-5 + challenge-D N-4, upheld here against the L axis): the live consumer settles it (K-12).
- **S-5 · `data-slot="empty-state-trio"` (:41) is a real contract seam, published before it was needed** (both readers): the oracle's private-selector reach (ES-19) is wrong precisely because the right hook already exists one line up.
- **S-6 · challenge-C's N-4 is a genuine anti-simplification guard rail** (both readers; adopted as ES-35's standing rider): a negative finding aimed at a future editor — and it caught its own file's cure (K-8).
- **S-7 · challenge-L r2's N-5 is model falsification done right** (reader-1): dangerous hypothesis → `--traceResolution` → self-refutation → banked re-runnable artifact (`evidence/resolution-trace.txt`, re-cited by this seat) → honest positional caveat. Its pass-1 self-corrections (L-8) show the same discipline.
- **S-8 · challenge-D's citation hygiene** (reader-1, corroborated on this seat's 17 canon-line reads): every VC/PA reference this seat checked lands on the sentence quoted, and the 16-site inventory is exact site-for-site — in a corpus where citation drift is the usual failure mode. (Its arithmetic is another matter — K-7.)
- **S-9 · The repo already owns ES-10's cure, one directory away** (reader-1): ErrorBoundary:16-22/:59-69 — role, tabindex, focus-on-catch, rationale in-file. The repair is a pattern this codebase already wrote correctly once.

## UNPROVEN-NEEDS-LIVE (visual-audit inputs — the next probe session's worklist)

- **U-1** · The boot-computed ink quantities: D-3's 6.4× dark de-emphasis collapse, D-4's 8.95× chroma loss, D-5's ornament-outranks-message ordering, L-N3's 5.213 vs 3.753 pair. `--ink-muted`/`--accent-live` exist only as boot output (structurally confirmed), so every ratio is live-only. The design principle (a de-emphasis rung needs a separation ceiling, not only a contrast floor) carries regardless.
- **U-2** · Whether ANY screen reader announces the plate at all: the region mounts with its content already inside it (`v-if` + `role="status"`), the one insertion pattern with unreliable announcement across NVDA/JAWS/VoiceOver. Decides ES-11's cure form (persistent region vs assertive) and whether C-4's "the eyebrow leads every announcement" is even the right defect. Needs an AT transcript, not a DOM read.
- **U-3** · ES-2's probe re-run: the outcome is quoted cross-engine in the immutable corpus but `probe-state.mjs`/`probe-deadlock.mjs` are not banked in `probes/` (only PNGs are). Re-script and re-run at the next live session; also the SPA-nav non-recovery claim.
- **U-4** · ES-10's measured focus destination after slot-action activation (structural half is dispositive).
- **U-5** · D-8's 2.8-3.5px optical offset and D-9's ink-rhythm scan — pass 1 and pass 2 disagree by up to 25% in both directions, so the "inverted rhythm" conclusion is irreproducible as filed; only the structural half (one `gap-2.5` serving four semantic ranks against PA:68) is carried, inside ES-22's cure.
- **U-6** · ES-25's 46%/38-character figures (the structural claim is certain; measure when X-W5 arms it).
- **U-7** · ES-31's engine-specific WHCM pixel behavior (policy half settled statically; the corpus's capture is Chromium-emulation with a void WebKit arm).
- **U-8** · ES-15's rendered symptom: collapse the dock and look — the seal should be missing its view icon and admin shimmer; likewise the two add-slot `Plus` glyphs. Statically certain as non-render; witness it anyway (the M-23 method: a byte-proof predicts, the probe confirms, the delta teaches).

## Closing verdict

**ADJUDICATED — RED, and redder than the re-readers filed it: the corpus's lead BLOCKER was under-filed as unproven by both hostile seats while its measured cross-engine evidence sat in the corpus, and the second-worst truth (the empty species' action vacuum, ES-16) had been dropped by the governing pass and rescued only in a reader's missed list.** 36 rows confirmed (2 BLOCKER · 14 MAJOR), 17 claims killed or demoted — including one reader kill killed in reverse (R-3), one reader "missed" find killed as a registry re-mint (K-16), and the readers' own shared cure (R-7). Terminal dispositions: **26 → X-W7** (the named surface owner; X.W7.g already deletes the eyebrow and re-rules o9), **1 → X-W4.g** (trigger-gated impostor/no-slot boundary, Dock limb noted to X-W5), **1 limb → X-W5** (the viewSchema companion discharge inside ES-11), **1 → X-W8**, **1 → X-W9**, **3 → X-W10** (with X-W7 surface edits), **2 → X-W1-coordinated gates**, **3 → GLASS-RELAY**, **3 NO-WAVE-OWNER in X** (the `--ink-muted` declaration, the tsconfig-parity standing identity, the forced-colors roster granularity — named residues for the next formation boundary), **1 → OWNER-QUESTION** (OQ-1), **8 → the visual-audit worklist**. Dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

— end of apotheosis. The five challenger files, both re-reader returns, `evidence/`, and `probes/` remain immutable beside this file per E-1/E-3.
