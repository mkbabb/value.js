# ColorInput — Adjudicated (M-12 tri-fold apotheosis)

**Component**: `demo/shell/dock/ColorInput.vue` (377 lines) + its parse cone (`demo/color-session/useColorParsing.ts`, `demo/color-session/picker-color.ts`, `demo/shell/dock/ParseEchoReadout.vue`, `demo/shell/dock/layers/ActionBarLayer.vue`)
**Date**: 2026-07-27 · **HEAD**: `c654824e` · branch `tranche-u`
**Seats**: worker-F (claude-fable-5) · worker-O (claude-opus-5[1m]) · **arbiter-F (claude-fable-5 — this record; model id observed in this seat's own runtime, not inherited; L-11)**
**Corpus**: 25 defects (L-1 ×2 passes, L-1-suppl, L-1-harness, L-12..L-17, D-1..D-15)

**Arbiter's own instruments this session (all receipts mine, none inherited)**: full source read of ColorInput.vue (all 377 lines) / useColorParsing.ts (all 137) / picker-color.ts (facade :109-113, serialize :206-211, buildColor :123-144, CSS_PICKER_SPACES :92-95) / ParseEchoReadout.vue / ActionBarLayer.vue bindings / foundation.css forced-colors roster / shared/utils.ts:8-21 / grammar.ts:75-185 / App.vue:310-328+:38; node battery against **today's** `dist/subpaths/css.js` (mtime **Jul 27 11:52**): all 8 degenerate bodies THROW `TypeError: Cannot read properties of undefined (reading 'replace')`, `oklch(`/`)`/`notacolor` → clean `ok:false`, valid → `ok:true`; node export census of `dist/subpaths/color.js` (makeColor/SPACE_SCHEMA/SPACE_IDS/isAnyColor all undefined; the 23 real exports match both workers verbatim); `@vue/compiler-sfc` parse+compileStyle oracle (scoped block emits `@keyframes crown-appear-test123`, rewrites the class-driven `input-mode-flash` sibling, leaves the template's inline `animation: crown-appear` untouched); probe harness re-run → **5/5 green, 8ms** (signature read first: jsdom, aliases to the real dist subpaths, real composable + real facade, no mocks); `git status`/`git ls-files`/`git cat-file -e` over `docs/tranches/V/megatranche/` (untracked, 0 tracked files, cat-file fails); greps: btn-interactive (0 in the WHOLE installed glass-ui 7.0.0 package, 0 in demo CSS, 8 template/comment hits), crown-appear (exactly 3 demo hits, 0 in animations.css), `]!` density loop (six-for-six: 113/609, 70/483, 35/564, 18/139, 12/899, 11/377), `export function parse` census over src/ (9 entry points across grammar.ts/stylesheet.ts:743/timeline.ts:17+:66), parsePickerColor call sites (exactly 8), dead-surface greps (0 external readers), `<form>` ancestors (0 in all three files), test/ degenerate-body greps (0 hits; six parseCssColor files); package.json (7 exports keys, no `.`; deps = glass-ui+keyframes only; @vueuse/core in devDependencies); tsconfig.demo.json:42-49 + `ls dist/index.d.ts` (missing); registry cross-reads: ColorPicker.md (W-LIB identity + the SPACE_SCHEMA-has-no-bounds correction), ColorSpaceSelector.md (L-14 → W-HYGIENE config parity), parser-band.md (G1 = `r1-published-totality.mjs` unmodified, RED 102/172; cand-O winner), App.md:146 (MT-DOCK-LAYERS-1). **Zero browser probes needed** — every verdict decided from source, dist, git, the installed toolchain, and the repo's own compiler; worker-O's live chromium 1440×900 cells are carried labeled as such where geometry is cited.

---

## §1 Ruled disagreements

| id | worker-F | worker-O | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-1-harness** | RESCOPED — "committed" is FALSE; force-add now | CONFIRMED — "committed beside the report" | **F WINS** | `git ls-files docs/tranches/V/megatranche/` → **0**; `git cat-file -e HEAD:…/parse-swallow.probe.ts` → fatal, exists on disk NOT in HEAD; whole tree is `??`. Committed is a git predicate (L-7). O verified existence+signature+execution but never the git object — the exact failure class the row was filed to end. |
| **L-1-suppl** | CONFIRMED (superlative kept, scoped "among them") | RESCOPED — superlative REFUTED; band = `src/css/**` | **O WINS** | My census: `parseStylesheet(source: string)` at stylesheet.ts:**743** (12 `]!`) and `parseAnimationTimeline`/`parseAnimationRange` at timeline.ts:**17**/**66** all consume arbitrary user strings. grammar.ts is NOT the only adversarial surface — even inside F's own six-file density list, stylesheet.ts is one. The eslint no-non-null-assertion band must cover `src/css/**`; O's rescope makes the cure stronger. Folds to the parser band. |
| **L-15** | RESCOPED — split: doctrine sentence BUILDs here; config arm = existing W-HYGIENE identity | CONFIRMED — fold whole to freshly-minted MT-LIB-SURFACE-1 | **F WINS on booking** | ColorSpaceSelector.md L-14 already books the exports↔tsconfig parity at **W-HYGIENE** with a born-RED generator-parity command, and already records the 3 dead targets + 2 omissions + false "CLOSED 8-key set" comment. Re-booking under a minted name is the anti-rename disease (L-5). O's 4-row drift widening is REAL (my read: `/parsing`+`/units` declared-unexported; `/value`+`/css` exported-undeclared) and corroborates the existing record — adopted as corroboration, not as a new booking. The `utils.ts:16-17` false-doctrine sentence BUILDs in this wave. |
| **L-17** | CONFIRMED — fold to V·MT-W-RESULT-2 | RESCOPED — behavioral arm refuted-as-unsubstantiated; structural half BUILDs in-wave | **SPLIT: O's verdict label, F's disposition** | O's refutation work stands (nine constructible degenerate colours all serialize cleanly — the double-append must not enter as measured fact). But O's in-wave cure is **unwritable as scoped**: the predicate rewrite `s.ok && s.value === newStr` requires `serializePickerColor` to return a Result, and O's own wave Result-ifies ONLY `parsePickerColor` (serialize stays `valueOrThrow`, :210). A gate the wave cannot turn green is L-2/L-3-banned. Verdict RESCOPED; disposition FOLD to **V·MT-W-RESULT-2** (serialize/convert Result-ing + the defensive-catch census) with F's born-RED. |
| **D-7 innerText count** | SIX write sites | four write sites | **F WINS** | My grep of the file: `:191, :240, :260, :265, :272, :278` — six programmatic `innerText` writes (O missed the propose-mode clear at :260 and the formattedCurrentColor watch at :272). The defect is worse than O filed. |
| **L-14 fold home** | minted "V·MT-W-LIBSURFACE-1" | minted "MT-LIB-SURFACE-1" | **BOTH REFUTED on the name** | The export-surface row is ALREADY BOOKED: ColorPicker.md carries it as **W-LIB "publish the abstraction"** (its L-1) with the born-RED node census and the SPACE_SCHEMA-has-no-bounds correction. Both workers minted a new id for an existing identity — the exact anti-rename failure both of them policed elsewhere. Fold home = **W-LIB**; both minted aliases recorded and retired. |
| **debounce edge** | 2000→150 **trailing**, Enter/blur immediate | ~150ms **leading** | **F WINS** | A leading debounce fires on the FIRST keystroke — `parseAndSetColor("o")` → instant error state on every entry's first character, then suppression. Trailing-after-pause is the preview semantics; commitment is Enter/blur, immediate. O's own G6 asserts only "≤ 250ms", consistent with trailing. |
| **D-14 label** | CONFIRMED as filed-scope | RESCOPED (inventory confirmed / reordering = hypothesis) | **O's LABEL adopted** | Facts unanimous (five physical offsets, no `dir`, no bidi isolation, text-center — all re-read by me). A row whose consequence arm is an unwitnessed prediction must not carry an unqualified CONFIRMED banner; the inventory BUILDs, the reordering banks on the first RTL locale arm. |

---

## §2 Per-defect verdicts (25 rows)

Severity headline: **2 BLOCKER identities** (D-1; L-1≡D-6-boundary≡MT-F001≡MT-F024≡R1). The fleet's "3 BLOCKER" count double-counted the twice-filed L-1 — both workers agree, arbiter confirms.

| id | Verdict | Deciding evidence (fresh-session reproducible) |
|---|---|---|
| **L-1** (pass-1 / seat L-b) | **CONFIRMED — BLOCKER** | Arbiter's node battery vs dist (Jul 27 11:52): `oklch() rgb() hsl() lab() lch() color() oklab() "hsl(  )"` all THROW `TypeError: …reading 'replace'`; `oklch(`/`)`/`notacolor` → `ok:false`; valid → `ok:true`. Throw site read: grammar.ts:**181** `slash[0]!` after `splitTopLevel("", "/")` → `[]` (empty loop + falsy tail, :75-87). Facade read: picker-color.ts:109-113 converts only `ok:false` → PickerColorError; a raw TypeError escapes untyped. Swallow: useColorParsing.ts:84 bare `catch {`; constant at ColorInput.vue:88. Diagnostics readers in demo = 3 internal sites + the capture-and-drop pair. Repro: `node -e "import('<repo>/dist/subpaths/css.js').then(m=>m.parseCssColor('oklch()'))"` → throws. |
| **L-1** (pass-2 / seat L-c) | **CONFIRMED — MERGED** | Same defect identity, independently re-filed blind (the L-14 verifier working as designed). ONE blocker identity, five names (MT-F001 / MT-F024 / R1 / D-6-boundary / L-1×2), all ids retained, ONE build entry. Seat L-c's distinct contributions verified: swallow-site precision, demo-dogfood negative proof. |
| **L-1-suppl** | **RESCOPED** (ruled §1 — O wins) | Density six-for-six re-run by arbiter; `noUncheckedIndexedAccess: true` at tsconfig.base.json:10 — the compiler answered `string \| undefined` and was overridden by hand at the exact adversarial boundary. Superlative refuted (stylesheet.ts:743, timeline.ts:17/:66). FOLD → parser band; eslint `@typescript-eslint/no-non-null-assertion` ban over `src/css/**`. |
| **L-1-harness** | **RESCOPED** (ruled §1 — F wins) | Real, honest (signature read; drives real composable+facade against real dist; its own :88-92 comment argues against its own strongest leg), re-run 5/5 green in 1.02s — and **UNCOMMITTED** (`??`, ls-files 0, cat-file fatal). Cure: force-add + commit the harness in this wave's FIRST commit; at landing promote `parse-swallow.probe.ts` INVERTED into `test/colorinput-parse-result.test.ts`, delete the private config. |
| **L-12** | **CONFIRMED** | Arbiter's compiler oracle (zero-browser): `compileStyle(scoped)` emits `@keyframes crown-appear-test123` + rewrites the class-driven `input-mode-flash` sibling; the template inline `animation: crown-appear …` (:38) is untouched by construction. Repo hits: exactly :38, :369, DESIGN.md:213 (prose); 0 in demo/styles/animations.css. A scoped keyframe is consumable only from a class rule in its own block; an inline style resolves only globally. **The crown entrance has never run anywhere.** O's live 54-name keyframe census corroborates (chromium cell). |
| **L-13** | **CONFIRMED** | Arbiter greps: 0 degenerate-body hits anywhere in test/; exactly six files mention parseCssColor (v4-c1, v4-css-public, gradient-v4-consume, ink, preview-chips, view-accents). package.json:4 "failure-explicit" + the ParseResult type = a public contract with zero negative-space coverage for the shipped crash class. Disposition (O's precision): FOLD → parser band, SUBSUMED by its pinned G1 `audit/probes/r1-published-totality.mjs` (verified present; parser-band.md G1 runs it unmodified, RED 102/172). A narrower duplicate here would be a gate this wave cannot turn green. |
| **L-14** | **CONFIRMED** | Arbiter's node census: makeColor/SPACE_SCHEMA/SPACE_IDS/isAnyColor all `undefined` in `dist/subpaths/color.js`; 23 real exports match. Source exports exist (model.ts :56/:76/:127/:136), re-exported by neither barrel. buildColor = 17-arm makeColor reconstruction (read :123-144); 13-member CSS set has three underived homes (model.ts css flags · grammar.ts:161-164 · picker-color.ts:92-95 — read all three). Disposition (ruled §1): FOLD → **W-LIB** (ColorPicker-adj L-1 identity) incl. the bounds-must-be-ADDED correction (SPACE_SCHEMA holds `{channels, hueIndex?, css}` only). Born-RED: the W-LIB node census exits 1 today (re-run by arbiter). |
| **L-15** | **RESCOPED** (ruled §1 — F wins booking) | 7 exports keys, no `.` (arbiter read); tsconfig.demo.json:42 → `./dist/index.d.ts` (my `ls`: No such file); doctrine sentence verbatim at utils.ts:16-17 ("the library's root-barrel export stands for external consumers" — unwritable by any consumer). **Correction confirmed**: @vueuse/core ^14.3.0 is a devDependency (`dependencies` = glass-ui + keyframes.js only). BUILD here: rewrite the doctrine sentence honestly. FOLD: config-parity arm keeps the CSS-adj L-14 → W-HYGIENE identity (O's 4-row drift adopted as corroboration of that record). |
| **L-16** | **CONFIRMED** | useColorParsing.ts:94-105 `astEcho` reads `model.value.color` exclusively — no source string, no ParseResult, no diagnostic; ParseEchoReadout.vue:**31** injects it (challenger's :29 = trivial anchor drift, F's correction adopted); labeled "the Parse-Lab echo (AST + gamut verdict)" at :107-109. On exactly the failure it exists to explain, it echoes the PREVIOUS committed colour. The gamutVerdict-throws-in-computed arm (:107-117, unguarded valueOrThrow hops) stays hypothesis → bank. Cure rides G1 (astEcho takes the last ParseResult). |
| **L-17** | **RESCOPED** (ruled §1 — split) | Predicate read verbatim at usePaletteWiring.ts:85-89: `catch { return false; }` answering "not a duplicate" inside a correctness predicate — a masking-fallback shape forced by `valueOrThrow(serializeCssColor(…))` (:210). Behavioral double-append UNSUBSTANTIATED (O's nine-colour construction attempt). FOLD → **V·MT-W-RESULT-2** with born-RED (a node assertion that serializePickerColor returns `{ok:…}` — structurally RED today, it returns a bare string or throws). Fuzz-openness bank recorded (O's dissent (d)). |
| **D-1** | **CONFIRMED — BLOCKER** | Source (arbiter): :16 `focus-visible:outline-none`; :162-166 error branch (:163) precedes focus branch (:164 `borderColor: cssColor.value` — raw, alpha-bearing, no floor: alpha-0 → invisible by construction; error+focus → focus indicator erased). Forced-colors roster read (foundation.css ~:702-720): `button`, `input`, `[tabindex]:not([tabindex="-1"])` present; **no `[role=textbox]`, no `[contenteditable]`**, and the span carries no tabindex → matches nothing. SAFE_ACCENT_KEY injected (:157) and spent only on the arrow stroke (:74/:81). O's live cells corroborate (outline-style `none` measured; `el.matches(roster)` false; chromium 1440×900). WCAG 2.2 SC 1.4.11: the sole indicator can reach 1.0:1 and 0 opacity via the app's own alpha slider. |
| **D-2** | **CONFIRMED** | Arbiter greps: `btn-interactive` = **0 in the entire installed glass-ui 7.0.0 package** (not just dist/ — O's widening confirmed), 0 in demo CSS; consumers = ColorInput.vue:69/:78 + CurrentPaletteEditor.vue:75/:78/:100 + 3 comment blocks; DESIGN.md:**247** records the row `landed` — a false record of shipped state (read by arbiter). Sole disabled treatment: :344-346 `cursor: not-allowed`. Cure viable: producer ships tap-squish + focus-ring (O's package census). |
| **D-3** | **CONFIRMED** (O's rescope nuance adopted) | Template :67-82: both branches icon-only, no aria-label/text/title, no `type` (submit default), Loader2 branch no aria-busy; 24×24 by arithmetic. **No `<form>` ancestor in ColorInput/ActionBarLayer/Dock (arbiter grep: 0/0/0)** → type=submit is a latent hazard, not a live failure; the accessible-name defect is the substance, unqualified. REPORT.md namelessButtons `/#/: 1` cross-checked by both workers. PR-12: ≥44px invisible seat. |
| **D-4** | **CONFIRMED** | Statically certain: `.send-btn { right: 0.25rem }` (:336-337) vs `.error-badge { right: 0.5rem }` (:348-350), both absolute vs the same edge, badge later in DOM (:86-90 after :67-82), z-index auto both (O measured), pointer-events none; **zero field-error semantics in the file** (no aria-invalid/-describedby/-errormessage — arbiter read). Geometry: 20.0px overlap = **83% of the 24px button** (O's chromium cell, arithmetically self-consistent); badge-over-field 85% at 1440×900 (O's correction — the filing UNDERSTATED at 66%; viewport-dependent, labeled cell). |
| **D-5** | **CONFIRMED** | Source (arbiter): `debounce(parseAndSetColor, 2000)` :92 (shared/utils.ts trailing-edge — read); `@input` is the only fast path (ColorInput.vue:199); previousInvalid early-return :62; error truth on a 2000ms setTimeout :54-58. Sub-2s typing cadence provably never fires. Retry total-no-op PROVEN mechanically: the committed probe's test 5, re-run green by arbiter today. PR-08 ADD-AFFORDANCE. |
| **D-6** | **CONFIRMED — MERGED with L-1's boundary arm** | Crash arm = L-1 (re-proven vs today's dist); swallow :84 + user-blaming constant :88 read; console/window.onerror silence structural to the bare catch → the visual audit's `pageErrors: 0` is manufactured. Nuance adopted (both workers + the harness's own honesty): ColorPicker.vue's boot-time inputColor watcher burns `initialParse`, so **`previousInvalid` is the silencer that bites a real user**. Seat-level copy arm (internal fault reported distinctly, never blaming the user) = this wave's typed seam. |
| **D-7** | **CONFIRMED — worse than filed** | contenteditable span + role=textbox (:11-27); glass-ui Input already in-tree (`demo/ui/input/index.ts` = one-line re-export; O read InputProps: type/inputmode/enterkeyhint/maxlength/pattern/placeholder/invalid…); `:empty[data-placeholder]::before` (:321-325) defeated by the browser's residual `<br>` (standard :empty semantics; O measured both directions); markup survives in the value; **SIX programmatic innerText writes** (:191/:240/:260/:265/:272/:278 — arbiter grep; F right, O undercounted) defeating native undo/redo. |
| **D-8** | **CONFIRMED** | Source-certain: element-level mask (:299-302) fades border+background with the ink; `:focus` snaps it off in one frame (:303-306); three unrelated gutter numbers (mask 40px vs pr-9 36px vs 24px button); `text-ellipsis` (:16) stacks a second truncation idiom; `text-center` fights the physical-right fade. No browser needed. |
| **D-9** | **CONFIRMED** (scope caveat baked in) | Arbiter greps: editTarget only :139 (parent passes at ActionBarLayer:118); zero external readers of copyAndSetInputColor/onSubmitColor/submitProposedName; colorInputRef declared :28 + bound :116, never dereferenced → the whole 5-member defineExpose (:282-288) is dead; cssColorOpaque (:146) + canProposeName (:150) destructured, never used; copyAndSetInputColor (:219-223) = an uncounted third Copy path vs PR-13's 2→1. **CAVEAT (the "sole/only" law)**: the parent's `actionBar.canProposeName` is LIVE (ActionBarLayer :37/:49/:106/:131 — arbiter read) — the dead thing is this component's destructure; a naive sweep would break mode cycling. File = exactly 377 lines; proposeMode forks :196-201/:203-211/:252-267. |
| **D-10** | **CONFIRMED** | Tailwind v4 `hover:scale-110` emits the **`scale` longhand** (O extracted from the served sheet; consistent with the installed lib) which `transition-[opacity,transform]` (:35) does not cover — opacity eases, magnification snaps (the same F3 stray the :327-334 comment claims retired); `@media (hover: hover)` kills the touch affordance; Crown = lucide `<svg>`, no tabindex, carrying name/contributor/CSS in its tooltip (:42-57) — keyboard+touch unreachable. Compounds with L-12: the entrance never runs AND the transition is broken. **UNVERIFIABLE-HERE arm**: the live `transitionProperty` reading on a rendered crown — dev :9000 is API-LESS, `currentColorMeta` never populates, the crown never mounts; needs a seeded fixture or the API-up stack. Verdict independent of that arm. |
| **D-11** | **CONFIRMED** (one leg dropped) | `.color-input` carries the `fira-code` family class with NO size-role utility (inherited 18.608px/27.912px root — the number that drives the 45.91px field height); `.error-badge` `@apply text-xs` (:353) is no matrix row; VISUAL-CONSTITUTION §4 closed-matrix sentence verified by both workers. **O's correction adopted**: the challenger's "mono-caption = Plus-Jakarta-18.608px matrix row" leg is an artifact (a bare `mono-caption` class does not resolve in the demo; the measurement was the inherited default) — dropped from the record. Pixel cells = O's chromium 1440×900, labeled. |
| **D-12** | **CONFIRMED** | `catch (e: any) { console.warn(…) }` :242-244 in a strict:true codebase (should be `unknown` + narrowing); success path :237-241 clears + repaints with zero confirmation; only op-state `proposing` (:227) has no visual because btn-interactive resolves to nothing (D-2). **UNVERIFIABLE-HERE arm**: the real proposeColorName round-trip (API-LESS env) — and for the failure path the blindness IS the test input (unreachable API must yield a visible persistent message, witnessable exactly here); success path asserted at vitest level with a mocked proposeColorName, labeled as such. |
| **D-13** | **CONFIRMED — FOLD → MT-DOCK-LAYERS-1** | Mechanism statically re-derived by arbiter: App.vue :310-312 `isDesktop` = `(min-width: 1024px) and (min-aspect-ratio: 1.1)`; `onDesktopLeftMount` (:323-328) is the sole setter of `colorPickerRef`, wired to desktop slots only; App.vue:38 `:action-bar="colorPickerRef?.actionBarContext ?? null"` → mobile Dock gets null → the action-bar layer never mounts ColorInput. O's adversarial mobile probe (drove the full disclosure at 390×844 with isMobile+hasTouch: no gesture reaches a free-text colour entry) + REPORT.md's missing mobile `/#/` row corroborate. Desktop rest-state: hidden AND inert/aria-hidden behind two disclosures (O's ancestor walk). Fold home verified: **MT-DOCK-LAYERS-1** named at App.md:146. Born-RED: 390×844 probe asserting a reachable free-text colour entry on `/#/` — RED today by the desktop-only capture mechanism. If MT-DOCK-LAYERS-1 does not carry it verbatim, the row returns here (O's dissent (e) preserved). |
| **D-14** | **RESCOPED** (ruled §1) | Inventory source-exact (arbiter): pr-9 :18, right-2 :35, right:0.25rem :337, right:0.5rem :350, to-right mask :300; no `dir`, no bidi isolation, `text-center` on a technical-LTR value vocabulary; VISUAL-CONSTITUTION §6.1 quoted accurately. Inventory BUILDs (rides the Input adoption free: `dir="ltr"` + logical properties). RTL visual-reordering = correct bidi prediction, unwitnessed → **bank, re-trigger = the first RTL locale arm**. |
| **D-15** | **CONFIRMED** | :3-8 `<Popover trigger="hover" :close-delay="0" :open-delay="300">`, whole field as trigger (:9); content (:94-110) = serialized colour + ParseEchoReadout = instrument data. O's refutation attempt is the decider: producer's `PopoverTriggerMode = Extract<Trigger, "click" \| "hover">` — **no focus mode exists**, so no keyboard path by the producer's own contract; no touch path. GLASS BOUNDARY HELD: a value.js COMPOSITION defect (trigger="click" exists; the Picker has a persistent readout region) — no producer ask, no local glass patch. Compounds with L-16: the gated content is WRONG on exactly the failure it explains. |

**Glass-owned rows: 0 of 25.** Every row is value.js-owned. The spectrum prefix seam (glass 07-25 outbound §3, cured in 8.0.0) is untouched by this corpus and stays GLASS-OWNED/unbooked per I-20; D-2/D-15 were checked against the producer's installed surface and ruled consumption/composition defects, with no local patch proposed.

---

## §3 The wave — MT-COLORINPUT-1

*(Registry-convention id; worker-F's minted alias `V·MT-W-CINPUT-1` recorded and retired. Born-RED. HEAD `c654824e`; re-resolve all line anchors before executing.)*

**Title**: ColorInput becomes a real field — Result-true parse boundary, house focus, named actions; the contenteditable, the constant string, the retry silencer, and the never-run keyframe all die.

### Scope (exact files)
- **`demo/color-session/picker-color.ts`** — ONE function: `parsePickerColor(source): ParseResult<CssColor>`, passing the library Result through UNCONVERTED. ONE typed internal-fault seam at this single choke point: a library THROW (alive until the parser band lands MT-F024) is caught HERE ONLY → `{ok:false, diagnostics:[{code:'internal_error',…}]}` AND `console.error` with the input — typed, loud, never user-blaming, bank-triggered for deletion (BANK-1). `valueOrThrow`/`PickerColorError` survive untouched for the conversion/serialize helpers (out of scope → V·MT-W-RESULT-2).
- **`demo/color-session/useColorParsing.ts`** — `parseAndSetColor` branches on `result.ok`; DELETE `previousInvalid` (:49/:62/:74/:85), `initialParse` (:50/:86/:88), the bare parse-path `catch {` (:84), the 2000ms flash timer (:54-58). `parseError` → `parseIssue: Ref<ParseIssue | InternalFault | null>` derived from the current value, cleared only by a valid parse, persistent while invalid. Debounce 2000 → **≤150ms TRAILING** for preview (ruled §1); Enter/blur commit immediately. `astEcho` takes the last ParseResult — channels on ok, `diagnostics` on failure (L-16). The 8 mechanical call sites (arbiter census: useColorParsing.ts:26, useColorUrl.ts:33, useCustomColorNames.ts:28, color-model.ts:47, color-utils.ts:12, usePaletteWiring.ts:81, hydrate.ts:82+:116) are found by the compiler — the signature change makes every stale site a `vue-tsc` error (L-8's answer to the blast radius).
- **SPLIT `demo/shell/dock/ColorInput.vue` (377 lines)** → `ColorInput.vue` (colour) + `ProposeNameInput.vue` (name), each <150 lines, each on the glass-ui `Input` (via the existing `demo/ui/input` re-export): `type="text"`, `inputmode`/`enterkeyhint`/`spellcheck="false"`, `:invalid`, real placeholder, `dir="ltr"` + logical properties (D-14 inventory), `text-mono-small` value role + a real size role for the field (D-11), inline-start alignment. DELETE: `focus-visible:outline-none`, the `borderColor: cssColor.value` focus branch, the mask-image pair (house `.focus-ring`/producer register; the live colour appears as a swatch if at all); the six imperative innerText writes die with the primitive (v-model). Error = field state: `aria-invalid` + `aria-errormessage` → in-flow message region rendering `diagnostics[0].expected` with `[start,end]` marking, text-small role — the `.error-badge` overlay dies; the same region carries propose success/failure persistently (catch typed `unknown` + narrowed; console.warn dies; API-unreachable = a visible persistent message). Buttons: `aria-label` per branch ("Apply color"/"Submit name"), `type="button"`, `aria-busy` in flight, ≥44px invisible seat over the 24px glyph (PR-12), consuming atoms that exist (tap-squish, focus-ring — verified in the producer dist); every `btn-interactive` reference + its two comment blocks deleted from this file; **DESIGN.md:247 corrected** (a false `landed` record). Crown: `@keyframes crown-appear` PROMOTED to `demo/styles/animations.css`, driven from a `.crown-badge` class (the inline style attribute dies); the transition names the `scale` longhand on a house token; the crown becomes a focusable named control keeping its tooltip OR aria-hidden decoration losing `cursor:help` — **decided in-wave**. ParseEchoReadout: astEcho rewired; the hover-only popover replaced by a named focusable disclosure or persistent instrument placement — **decided in-wave** (D-15).
- **`demo/shell/dock/layers/ActionBarLayer.vue`** — drop the never-read `colorInputRef` (:28/:116) and `:edit-target` (:118); mount the two components on the existing toolbarMode cycle. **`actionBar.canProposeName` consumption (:37/:49/:106/:131) is LIVE and survives the sweep** (D-9 caveat).
- Dead surface deleted: editTarget prop, defineExpose members except `focus()`, copyAndSetInputColor (the third Copy path), unused cssColorOpaque/canProposeName destructures.
- **`demo/shared/utils.ts:16-17`** — the false root-barrel doctrine sentence rewritten honestly (L-15 BUILD arm).
- **Harness (L-1-harness cure)**: this wave's FIRST COMMIT force-adds `docs/tranches/V/megatranche/audit/components/shell-dock-colorinput/` (probe + config + frames — a witness with no git object does not exist, L-7); at landing, `parse-swallow.probe.ts` is promoted INVERTED into `test/colorinput-parse-result.test.ts` and the private vitest config deleted.

**OUT OF SCOPE, each named so nothing is lost**: grammar.ts:181 root fix + L-1-suppl eslint band (`src/css/**`) + L-13 negative-space coverage → **parser band** (MT-F024/R1 identity; its pinned G1 `r1-published-totality.mjs` is RED today 102/172 — verified present); SPACE_SCHEMA/SPACE_IDS/makeColor/isAnyColor + the channel-domain/bounds table → **W-LIB** (ColorPicker-adj identity; born-RED node census exits 1 today — arbiter re-run); serialize/convert Result-ing + the defensive-catch census + L-17 → **V·MT-W-RESULT-2** (born-RED: serializePickerColor returns `{ok:…}` — structurally RED, it returns a bare string or throws); mobile composition D-13 → **MT-DOCK-LAYERS-1** (App.md:146; born-RED 390×844 reachability probe); exports↔tsconfig parity → **W-HYGIENE** (CSS-adj L-14 identity, generator-parity command — NOT re-booked); the glass spectrum prefix seam — GLASS-OWNED, untouched.

### Structure over gates (L-8) — four classes made unrepresentable
1. `parsePickerColor: ParseResult` makes throw-swallowing unspellable at every one of the 8 call sites — no exception exists to catch; diagnostics reach the surface because they are the only thing the function returns; the compiler enumerates the blast radius.
2. A real `<input type=text>` makes markup-in-value, the `<br>`-defeated placeholder, the missing native affordances, imperative innerText, and the forced-colors roster miss (`input` IS a roster member by element name) all nonexistent, not fixed.
3. The typed `internal_error` Result at one choke point makes an internal fault loud by construction — `pageErrors: 0` stops being manufacturable.
4. The two-component split makes the proposeMode fork (two value types, two submit semantics, two error vocabularies on one boolean) unrepresentable, and dead exposed members lose their home.
Gates remain only for rendering truth (focus visibility, geometry, keyframe census), naming, and the timing constant.

### Gates (all born-RED today; failing inputs pasted; 10 runnable this minute + 1 rendered-truth gate)
- **G1 — Result-total boundary, diagnostics reach the surface**: `npx vitest run test/colorinput-parse-result.test.ts`. RED: the spec (the committed probe INVERTED) asserts for all 8 crashers + `notacolor`: no throw, `ok:false`, diagnostics non-empty, `internal_error` distinctly coded for the 8, AND astEcho reflects diagnostics after a failed parse. Today `parsePickerColor("oklch()")` THROWS (arbiter re-proven vs dist Jul 27 11:52); the current probe's 5/5-green proves the exact inverse.
- **G2 — no retry ever silenced**: `test "$(grep -c 'previousInvalid' demo/color-session/useColorParsing.ts)" = 0 && test "$(grep -c 'initialParse' demo/color-session/useColorParsing.ts)" = 0 && test "$(grep -cE 'catch \{' demo/color-session/useColorParsing.ts)" = 0`. RED: :49/:62/:74/:85, :50/:86/:88, three bare catches (:44/:81/:84). Failing gesture: type `oklch()`, wait, type `oklch()` again — a total no-op today (probe test 5). *(Note: the two hue-guard catches :44/:81 become typed or die with the Result plumbing; the gate holds the composable to zero bare catches.)*
- **G3 — error is a semantic field state in an honest vocabulary**: `node -e "const s=require('fs').readFileSync('demo/shell/dock/ColorInput.vue','utf8');process.exit(/aria-invalid/.test(s)&&/aria-errormessage/.test(s)&&!/not a valid color/.test(s)&&!/error-badge/.test(s)?0:1)"`. RED on all four clauses today. Failing input: `lab(#)` focused.
- **G4 — focus from the house register, never from user data**: `node -e "const s=require('fs').readFileSync('demo/shell/dock/ColorInput.vue','utf8');process.exit(!/focus-visible:outline-none/.test(s)&&!/borderColor: cssColor.value/.test(s)?0:1)"`. RED (:16, :164). Failing input: alpha 0 via the app's own slider + click the field.
- **G5 — every action named, typed, honest in-flight**: `node -e "const s=require('fs').readFileSync('demo/shell/dock/ColorInput.vue','utf8');const b=s.match(/<button[^>]*>/gs)||[];process.exit(b.length>0&&b.every(x=>/aria-label/.test(x)&&/type=\"button\"/.test(x))?0:1)"` (+ its twin over ProposeNameInput.vue at landing). RED: both branches nameless/untyped; Loader2 no aria-busy. Enumeration witness: exactly 1 nameless button on `/#/` (both workers, REPORT.md). 
- **G6 — no class contract resolving to nothing**: consumed-implies-defined check for `btn-interactive` over the producer dist + demo CSS vs this file's template. RED: 0 definitions anywhere (arbiter: 0 in the whole glass-ui package), 2 template consumers here. Failing input: hover/press/disable the send button — no rule exists; disabled renders pixel-identical.
- **G7 — keyframe references satisfiable by construction (repo-law form)**: in-wave gate script over `demo/**/*.vue` via `@vue/compiler-sfc`: every `animation:` in a template style attribute names a keyframe defined in `demo/styles/` (global); every scoped `@keyframes` is referenced only by class rules in its own block. Minimal runnable-today form: the compiler check on this file — RED (inline `crown-appear` :38 vs scoped-only definition :369; arbiter's oracle: emitted as `crown-appear-test123`, inline ref untouched). The instrument note: compiler-sfc is a zero-browser oracle for every scoped-CSS accusation.
- **G8 — a real input; the mask hack is gone**: `test "$(grep -c 'contenteditable' demo/shell/dock/ColorInput.vue)" = 0 && test "$(grep -c 'mask-image' demo/shell/dock/ColorInput.vue)" = 0`. RED: 1 + 4 hits. Failing inputs: propose→type→select-all→delete leaves `<br>` (no placeholder ever again); pasted styled HTML survives; focus snaps the border back in one frame.
- **G9 — dead surface gone**: `test "$(grep -c 'copyAndSetInputColor\|cssColorOpaque\|canProposeName\|editTarget' demo/shell/dock/ColorInput.vue)" = 0 && test "$(grep -c 'edit-target' demo/shell/dock/layers/ActionBarLayer.vue)" = 0`. RED today. **Sweep caveat baked in: ActionBarLayer's own `actionBar.canProposeName` reads are LIVE and must survive.**
- **G10 — feedback inside the interaction threshold**: `node -e "const s=require('fs').readFileSync('demo/color-session/useColorParsing.ts','utf8');const m=s.match(/debounce\(parseAndSetColor,\s*(\d+)\)/);process.exit(m&&Number(m[1])<=250?0:1)"`. RED: the constant is 2000, trailing — sub-2s cadence provably never fires. Failing input: `oklch(0.7 0.1 200)` at 1 char/s → zero preview for the entire entry.
- **G11 — rendered truth (the one browser gate)**: in-wave `gates/a11y.mjs`, chromium 1440×900, dev `:9000`, `/#/` hash only (MT-F006), drives the disclosure: (a) nameless-button count on `/#/` = 0 (today 1 of 26 — O's cell); (b) focused field's outline-style ≠ `none` and `el.matches(<foundation roster>)` = true (today `rgb(0,95,204) none 1px` + false); (c) error-node/action horizontal overlap = 0 with `aria-errormessage` IDREF resolving (today 20.0px = 83% of the button, all ARIA null); (d) action hit-rect ≥44×44 (today 24×24). Cells: playwright-webkit and safari-app (safaridriver :4599) are SEPARATE (I-20), never inferred; committed `safari-*` shots are webkit captures and cited as webkit.

### π obligations
Chromium 1440×900 primary (this adjudication itself needed ZERO browser probes — verdicts decided from source/dist/git/toolchain/compiler; all inherited pixel cells labeled with their cell); 390×844 only for the D-13 fold's evidence. Route: `http://localhost:9000/#/` hash only. Selectors pinned: `.color-input` (pre-cure) → `input[aria-label="Enter a CSS color"]`; `.send-btn` → `button[aria-label="Apply color"]`; `.error-badge` → `[id=colorinput-error]`; `.crown-badge`. Committed captures force-added past `.gitignore *.png` into `audit/components/shell-dock-colorinput/witness/`: the existing `frames/B-error-badge-over-send.png` ADOPTED as the BEFORE frame and force-added in the wave's FIRST commit with the probe harness, plus after-{error-state,focus-ring,crown-entrance}.png and the 390×844 pair for the D-13 fold. **ENV**: dev :9000 is API-LESS — the propose-name FAILURE path's blindness IS the test input (unreachable API → visible persistent message, witnessable here); propose SUCCESS is a vitest assertion with mocked proposeColorName, labeled; the crown's rendered arm needs a seeded fixture or API-up (UNVERIFIABLE-HERE today); no gate witnesses the production bundle — bundle truth rides the standing built-bundle harness.

### Delta obligations (before → after)
1. **Error**: badge over 83% of the send button, zero ARIA → in-flow region, `aria-invalid="true"`, overlap 0, same input `lab(#)`.
2. **Retry**: probe test 5 green (second `oklch()` = no-op) → inverted spec green (every attempt yields rendered diagnostics).
3. **Focus**: `outline none` + raw alpha-bearing border, roster match false → house ring visible at every colour incl. alpha 0, roster match true (element = `input`).
4. **Crown**: compiler proof of an unresolvable keyframe → entrance fires from animations.css (two-frame witness, seeded fixture or API-up cell).
5. **Naming**: nameless-button enumeration on `/#/` 1 → 0; action seat 24×24 → ≥44×44.
6. **Copy honesty**: `not a valid color` grep 1 → 0, `diagnostics[0].expected` rendered; internal fault reaches window.onerror (pageErrors becomes a real green).
7. **Feedback**: 2000ms trailing + timer-cleared error → ≤150ms trailing + value-derived persistent error; Enter immediate.
8. **Surface**: 377 lines / 5 dead exposed members / 1 unread prop / 2 unused injections / six innerText writes → two components <150 lines, every member read, zero imperative writes.

### Banks (swept at every wave open; each with its re-trigger command)
- **BANK-1 (delete the seam)**: `node -e "import('<repo>/dist/subpaths/css.js').then(m=>{try{m.parseCssColor('oklch()');process.exit(1)}catch{process.exit(0)}})"` — exits 0 today (arbiter re-run). The day it exits 1 (parser band lands), DELETE the internal-fault seam + its distinct copy; the parser-band regression guard takes over. **If this bank is never swept, the wave will have replaced a bare swallow with a polite one that outlives its reason (O's dissent (c), adopted as a standing warning).**
- **BANK-2 (glass 8)**: producer major-version check → re-derive Input/field styling against the new producer; the spectrum prefix seam stays GLASS-OWNED and unbooked.
- **BANK-3 (RTL)**: first RTL locale arm → witness D-14's reordering hypothesis.
- **BANK-4 (serialize fuzz)**: at V·MT-W-RESULT-2's open, fuzz serializePickerColor over the AnyColor space — O's nine constructible candidates all serialized; the space is not proven closed (O's dissent (d)).
- **BANK-5 (gamutVerdict)**: the unguarded valueOrThrow hops in the :107-117 computed — hypothesis today; asserted or retired when the Result plumbing lands.

### Completable alone?
**YES.** If this is the only wave that ever executes: both blocker identities die at this surface (a library crash can no longer wear a typo's face — every parse attempt yields visible, persistent, semantically-wired diagnostics; focus becomes visible for every reachable colour in every contrast mode); the retry silencer, the constant string, the phantom class, the never-run animation, the nameless button, the occluding badge, the contenteditable, and the dead API surface are gone; zero capability lost (sliders + URL remain full editors); no dependency on any other wave landing (the typed seam keeps the boundary total meanwhile; BANK-1 deletes it). Ten gates runnable this minute as node/grep/vitest, each re-verified RED by two workers and this arbiter; one rendered-truth gate authored in-wave with its RED inputs already measured.

---

## §4 Addendum — MT-CINPUT-A (E-3: appended, never rewriting the challenger records)

1. **Committed is a git predicate** (`git cat-file -e`), not a filesystem predicate. The row created to END ephemeral evidence (L-1-harness) itself asserted "committed" about an untracked tree — and one adjudicator seat then repeated the error. Verify the git object, not the `ls`.
2. **Anti-rename cuts three ways here**: (a) the single parse-boundary blocker wears FIVE names (MT-F001 / MT-F024 / R1 / D-6 / L-1×2) — one identity, one demo-half cure here, the root stays π's; any close row claiming this wave cured MT-F001 is false. (b) The component's true headline is **2 blocker identities**, not the fleet's 3. (c) **Both workers minted fresh wave names for already-booked identities** (LIBSURFACE/LIB-SURFACE vs the ColorPicker adjudication's existing **W-LIB**; O also re-booked the W-HYGIENE config-parity arm) — the arbiter's registry cross-read caught both. Before minting a wave id, grep `registry/adjudicated/`.
3. **Dependency-class precision**: @vueuse/core ^14.3.0 is a devDependency (`dependencies` = glass-ui + keyframes.js only — arbiter re-read). A doctrine correction resting on a mis-classed dependency inherits the disease it cures.
4. **The internal-fault seam is not a masking fallback**: the ban is on SILENT conversion; the seam is typed (`internal_error`), loud (console.error with the input), honest in copy, single-choke-point, and carries its own deletion bank. It exists to end the silence while the root fix rides with its owner.
5. **The "sole/only" scope law, twice in one corpus**: D-9's canProposeName is dead as a destructure, live as the parent's concept (a naive sweep breaks mode cycling); L-1-suppl's "only arbitrary-string consumer" superlative was false (stylesheet.ts:743, timeline.ts:17/:66) and would have scoped the eslint cure too narrowly. Superlatives must be enumerated, not asserted.
6. **Instrument notes for future seats**: `@vue/compiler-sfc` parse+compileStyle is a zero-browser oracle for every scoped-CSS/keyframe-resolution accusation — cheaper and stronger than curling the dev server, works offline (both this arbiter and worker-F used it decisively). A cure whose gate needs a symbol another wave owns (O's in-wave `s.ok` predicate rewrite while serialize still throws) is un-completable as scoped — adjudicate cures separately from defects, and check every proposed edit's type dependencies against the wave's own scope.
7. **Evidence freshness is load-bearing**: `dist/subpaths/css.js` was rebuilt Jul 27 11:52, AFTER every challenger cell (Jul 24); all three tri-fold seats re-measured against the new bytes independently (unchanged behavior). Any parser claim must name WHICH artifact it measured; the parser band separately flags that this repo's dist differs from what 4.0.0 ships.
8. **Corrections ran AGAINST the accusers' favor in three places and FOR them in three** — against: the mono-caption "matrix row" was a resolution artifact (dropped); type=submit is latent, not live (no form ancestor); the double-append is unsubstantiated. For: badge-over-field 85% not 66%; tsconfig drift 4 rows not 1 (already known to W-HYGIENE); innerText writes six not four. A jury assuming uniform challenger inflation would mis-set severity in both directions.

---

## §5 DISSENT (preserved verbatim in substance; unresolved by this arbiter where noted)

1. **Severity ordering, D-1 vs L-1 (both workers, opposite emphases — UNRESOLVED, both preserved)**: worker-F ranks the D-5+L-1 previousInvalid compound (the retry total-no-op) as the worst LIVED harm ("hits every user of the feature within seconds"); worker-O ranks L-1 first over D-1 ("a library CRASH delivered as copy blaming the user's own typing, on the one surface where a human types CSS into a CSS library's own demo"). Neither moved D-1's BLOCKER class (accessibility law); both BUILD here. The arbiter records both orderings and moves neither: the wave cures all three in one landing, so the ordering has no execution consequence — it matters only if the wave is ever split, in which case the parse boundary goes first (both workers agree on that much).
2. **One wave or two (worker-O's (b), preserved)**: a reasonable arbiter could split the field substitution (13 rows) from the parse boundary (6 rows), accept a temporary generic error string, and get two smaller certainly-completable waves — relevant against a measured 38% landing rate across 22 tranches. Both workers answered with the coupling (D-4's cure cannot render `diagnostics[0].expected` through a bare catch that destroyed it); the arbiter concurs but does not claim the answer is unique.
3. **The interim catch as a debt (worker-O's (c), adopted into BANK-1's text)**: "if that bank is never swept, this wave will have replaced a bare swallow with a polite one, and the polite one will outlive its reason." Standing warning, sweep obligation at every wave open.
4. **L-17's behavioral arm may yet be true (worker-O's (d))**: nine constructible degenerate colours all serialized — but the AnyColor space is not proven closed; a fuzz could find a throw and raise the row from latent hazard to live data corruption. BANK-4.
5. **D-13's ownership, softly contested (worker-O's (e))**: "a control that does not exist on mobile is a fact about the control." Folded to MT-DOCK-LAYERS-1 on cure locus; the born-RED reachability gate is the hedge; **if MT-DOCK-LAYERS-1 does not carry it verbatim, the row returns here.**
6. **Suspicious-agreement disclosure (L-14 corollary)**: 20 CONFIRMED / 5 RESCOPED / 0 REFUTED across three seats is high agreement. The arbiter records why it is evidence, not inheritance: the dist was rebuilt after every challenger cell and all three seats re-measured independently; the three seats used partially disjoint instruments (F: compiler-sfc + git predicates; O: live chromium cells + producer d.ts reads + construction-based refutation; arbiter: registry cross-reads that caught BOTH workers' fold-name errors, the entry-point census that decided L-1-suppl, and the six-write innerText count that decided D-7); and the disagreement ledger (§1) contains eight substantive rulings — four decided against worker-F's positions, four against worker-O's. Uniform deference would have produced zero.
