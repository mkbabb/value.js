claude-fable-5 (served model id)

# fr-FourierMorphSvg — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **Subject**: `fourier-analysis/web/src/components/decorative/FourierMorphSvg.vue` (41 lines) — the path-only SVG renderer terminating the morph chain, with consumers `layout/DarkModeToggle.vue` + `morph/MorphShapePreview.vue`, driver `morph/FourierMorphDemo.vue` (330, read whole this session), composables `useFourierMorph.ts` (:55-214 re-read whole this session) + `useMorphConfig.ts` (97, read whole this session), producer module `lib/svg-fourier.ts` (155, read whole this session), and siblings `HarmonicLevelGrid.vue` (:28-137 re-read) + `lib/easings.ts` (:1-70 re-read).
**Substrate**: fourier `cd26c65` + dirty WT (28 porcelain rows, re-verified this session; every subject file clean) · installed glass-ui **4.0.0** · keyframes.js **4.3.0** · value.js **0.13.0** · glass-ui producer **8.0.0**, HEAD `4a86570b` (seat pin this session — **TEN commits past** the `074a3d0e` r1 pinned on 08-10, not reader-1's "two"; drift-in-two-days is itself the FM-2 lesson) · value.js producer **4.0.0** re-probed in-repo (branch `tranche-u`: exports = 7 subpaths, no `"."`, no main/module/types; `timingFunctions` src grep = 0 — seat runs).
**Corpus adjudicated (r2)**: the same three axis challenges (`audit/fourier-components/FourierMorphSvg/challenge-{D-design,L-library,C-consumption}.md` — re-read by this seat at every disputed cell) × two FRESH hostile re-reader returns, both of which had read the r1 record and folded onto it: reader-1 (design-and-usage, slug `FourierMorphSvg`: 19 confirmation cells · 14 kill entries · 2 misses · 8 superlatives) and reader-2 (library-and-code, slug `fr-FourierMorphSvg`: 14 confirmation cells · 8 kills **including four of its own hypotheses** · 5 misses · 7 superlatives).
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-10 r1 apotheosis at this same path, which adjudicated a *different* reader corpus. The r1 identities **FM-1..FM-19, K-1..K-16, SC-1..SC-4** are preserved for life (anti-rename); every r1 ruling is carried where this corpus does not touch it, and is **corrected in exactly one region**: the absolute-px cells inside K-9/K-10/K-14, FM-6, and D-9's "40 px" — all felled by one new find (FM-21, ruling 2). Where r1 content is restated it was re-verified, not copied.
**Method (incredulity)**: every load-bearing NEW claim was re-derived against the bytes with this seat's own instruments before adoption — the FM-20 level-domain mechanics by an independent node replication, the FM-21 ladder by re-reading both breakpoint pairs and sweeping for third writers, the FM-2 tag receipt by re-running `git merge-base`/`git show` at the producer, M-2 by reading the producer's `cartoon-surface` body whole. Reader kills were challenged back (one clause inside a reader's kill is itself corrected — SC-6); reader mechanism cites were traced to their writers (SC-5). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings are M-25 agglomeration input and stamp nothing.
**This seat's own instruments (static + node/git greps only; zero browser probes — probe parsimony; product source READ-ONLY; this file is the sole write)**: fourier — `--toggle-size` census (**exactly two setters**: AppHeader.vue:242 `2.5rem`, :256 `2.75rem` inside `@media (min-width: 640px)` at :245; DarkModeToggle.vue:78-79 is the `var(--toggle-size, 5rem)` reader); root-font census (**exactly two** `html { font-size }` rules: style.css:41 `1.125rem`, :47 `1rem` inside `@media (min-width: 768px)` at :45; the :61/:72 hits are non-root `em` rules); MorphShapePreview.vue:91-106 (`.morph-button` width 120px/pad 0.625rem, then 180px/1rem at min-width 640 — the FM-21 box-model decider; transition :97, hover :108-112, active :114-116 re-confirmed); FourierMorphDemo.vue whole (chip :15 `Math.round(morph.harmonicLevel)`, grid wiring :58-67 with unguarded bound writes :64-65, export :71, unguarded Reset :75-78, `syncWith` :111, `nearestActiveLevel` :115-120 **over `currentShape.value.data.levels`**, `isMoon` flip :132 BEFORE the await :134, `handlePreviewClick` guard :138 + `setLevel` :172, `handleReset` :175-178); useFourierMorph.ts (harmonicLevel writers censused: init :82, setShape :92, **setLevel :101**, settle-out tick :174 with `Math.max(lowLevel, level)` clamp-at-paint :175, **settle-in tick :203** with :204's `Math.min`; morphProgress writers :176/:190/:205/:211; `fromLowPoints/toLowPoints` cross-asset lerp :183-189; `getIdlePoints` :110-113); useMorphConfig.ts whole (candidates literal :30 **taking no shape argument**, `updateField` :64-67, `toJSON`/`copyToClipboard` :69-75, `syncWith` :78-84 — fresh-object getter + `deep: true`); svg-fourier.ts whole (import :11, `closed = true` :49, dead branch :52, `" Z"` :72, `if (ps)` :82-84, **`lerpPoints` `Math.min(a.length, b.length)` :99**, `nearestLevel` :112-119, clamp :130-131, bracket :136-142, degrade :146-148); **node replication of the FM-20 mechanics** — both assets' `levels` = `[1,2,3,5,8,12,18,25,35,50]` with 512 points at every level of both files, and targets 50/75/100 ALL resolve `{clamped:50, lo:35, hi:50, t:1}` with `nearestLevel(75)=nearestLevel(100)=50` (this seat's own run, reproducing reader-2 exactly); `pointsToSvgPath` caller census (**definition :47 + imports useFourierMorph.ts:19/HarmonicLevelGrid.vue:93 + calls :81/:131, both single-argument** — the FM-22 decider); HarmonicLevelGrid.vue:28-137 (High `max="100"` :36, Slider `:max="100"` :44, `emitHigh` clamp `Math.min(100, …)` :115, interlock :110/:115, `active` vs `is-bound` :60-63); easings.ts (bare root specifiers :9/:16; the `as EasingFn` cast **:58 exact**; `getEasingFn` fallback); `getIdlePoints` consumer census (**declaration :110 + return :227, zero external consumers** — a second `updateField`); `useClipboard` probe (fourier root-specifier import useMorphConfig.ts:9 — and the producer's root `"."` export SURVIVES at 8.0.0 with `useClipboard` live at src/composables/dom/useClipboard.ts: **a fired falsifier, declared**); glass-ui producer — `git merge-base --is-ancestor 0078e508 v7.0.0` → **true** and `git show v7.0.0:src/styles/theme/bridges.css` carrying the "DELETED, clean break… `--focus-ring-color`" comment **verbatim at the tag** (seat re-run of reader-2's receipt); `cartoon-surface` at HEAD relocated to `src/components/card/styles.css:256-260` with body exactly `position: relative; border-width: 2px; box-shadow: var(--shadow-cartoon-md)` under a docblock calling it "a public **STATIC** decoration" (the M-2 decider, seat-read); registry identity sweeps — `previewLevels|75, 100|candidate set|Math.min(a.length|catmullRomToBezier|syncWith` over `adjudicated/*.md` → **zero defect bookings** (mentions are counting instruments and fr-AppHeader:55's mechanism cite only); targeted re-reads of fr-EqCoefficientsPanel (FR-EQC-3/-5 + K-7 — the 8.0.0 `cartoon-surface`/Card facts ARE banked there, honouring reader-1's fold), fr-EasingPicker.md:52/:89 (the L/B-1 + duplicate-import rows, verbatim as reader-2 quoted), fr-AppHeader.md:55 (FR-AH-9 home), fr-DarkModeToggle.md:79/:97/:143 (M-6, N-11, and **superlative 8's "45 px mobile / 44 px desktop"** — the cell FM-21 three-arms); challenge-file re-reads at the disputed cells (challenge-L:41-53 — **the L-08 `5rem` premise is real at :46**; challenge-D:7-8/:110-187/:307/:467-472/:599/:628; challenge-C:73/:92); glass-ui log positioning (`074a3d0e` at log position 11 → ten commits behind HEAD — the SC-7 decider).

## Scoreboard

**43 adjudicated roster rows — 1 BLOCKER · 19 identity folds · 3 MAJOR · 16 MINOR · 4 INFO** (r1's 37 identities preserved and re-affirmed; **one new MAJOR admitted** — FM-20, the level-domain truth failure every prior seat walked past; three new MINORs, one new INFO, one new fold row). **20 killed-claims register entries** (K-1..K-16 carried, all re-ratified where this corpus touched them; **K-17..K-20 new** — including K-17, which corrects px cells inside three of r1's own kills at the bytes: the pt-leaf K6 idiom, r2-corrects-r1). **3 new seat corrections (SC-5..SC-7)** — two against reader-2, one against reader-1, all inference/citation, none arithmetic. **7 reader-missed finds adjudicated** (1 MAJOR, 3 MINOR, 2 INFO/riders, 1 fold-rider) **+ 2 seat additions** (the `getIdlePoints` dead export; the `useClipboard` fired falsifier). **12 superlatives** (r1's 8 sustained + 4 new). Both readers' registry discipline RATIFIED — every fold they claimed held under re-check, and reader-2's four self-kills are adopted into the register so no repair ticket ever "fixes" them.

The r2 headline is **FM-20**: the `/morph` UI's harmonic-level domain (candidates to 100, input/slider max 100) is decoupled from the asset level table (max 50), so two grid cells are byte-identical silent duplicates, the two highlight systems disagree about the same click, the `n=` readout reports levels the SVG is not rendering, and Export serialises a config no consumer can honour — all reachable in one click, all static fact, and all three axes plus both r1 readers opened these exact files without checking the domain against the data. The r2 corrections headline is **FM-21**: the rendered pixel ladder is three-armed and NON-MONOTONIC (45 → 49.5 → 44 px) because the toggle's breakpoint (640) and the root-font breakpoint (768) disagree — which retires every absolute-px figure in the corpus AND in three of r1's own kill cells, while the one banked conclusion it might have felled (fr-DarkModeToggle superlative 8's 2.5.5-AAA) **survives strengthened** (all three arms ≥ 44 px).

## The incredulity seat's own work — ruled disagreements and corrections

1. **FM-20 severity + mechanism — reader-2's missed #1 ADMITTED at MAJOR; SC-5 minted on its mechanism cite.** Every arm re-derived by this seat: (a) grid cells n=75/n=100 render byte-identical `d` to n=50 — my node run over both assets reproduces `{clamped:50, lo:35, hi:50, t:1}` for all three targets (svg-fourier :130-131/:136-142), so 2 of the 12 cells are silent duplicates and the grid's "more harmonics = closer fit" pedagogy is false above 50; (b) clicking n=75/100 puts `active` on the n=50 cell (`nearestActiveLevel` = `nearestLevel(currentShape.data.levels, …)` — demo :115-120; `nearestLevel(75)=nearestLevel(100)=50`, my run) while `is-bound` simultaneously marks the clicked cell (grid :60-63) — two highlight systems disagreeing about one click; (c) the `n=` readout lies — **but not via the line reader-2 cited**: `setShape`'s :92 write (`harmonicLevel = config.highLevel`) is unreachable with a >50 value (setShape runs only onMounted and post-`reset()`, both at defaults — seat trace); the REAL writers are `setLevel` :101 (the click path writes the raw 75/100 while :102's interpolate clamps at 50) and the settle-in tick :203 (with `highLevel: 100`, the readout animates 50→100 over a frozen glyph; symmetrically :174/:175 on settle-out). **SC-5 (against reader-2)**: the K-2-class law — the conclusion folds, but a cure ticket written against `setShape:92` would fix nothing. (d) Export serialises `{"highLevel": 100}` (useMorphConfig :69-75, demo :71) — a config the render path cannot honour. Severity: MAJOR on the house M-2 test (capability survives — the morph itself works; what fails is the route's *truth*), reachable in one click on the pedagogy route. Cure: derive the candidate set and the High bound (`max` :36, `:max` :44, `emitHigh`'s 100 :115) from `shape.data.levels` — one argument to `computePreviewLevels` — **with this seat's rider**: the source-of-truth shape is ambiguous mid-transit (`isMoon` flips at demo :132 BEFORE the `await` at :134), benign today only because both assets share one table, which is exactly the unguarded assumption FM-19 books; the cure must name its shape.
2. **FM-21 — reader-1's missed M-1 ADMITTED at MINOR, and its corrections cascade RATIFIED against r1's own cells (the pt-leaf-K6 idiom); SC-6 minted against reader-2.** Seat-verified whole: exactly two `--toggle-size` setters (AppHeader :242/:256, breakpoint **640**) and exactly two root font rules (style.css :41/:47, breakpoint **768**), no third writer of either (my sweeps). `rem` resolves against the root, so the header toggle renders **45 px (<640) → 49.5 px (640-767) → 44 px (≥768)** — NON-MONOTONIC, largest in the one band nobody measured; and `/morph`'s `.morph-button` mixes px widths with rem padding (:92/:95, :102/:104), so its content boxes are **93.5 / 140 / 144 px**, not K-14's "96/144". Consequences ruled precisely: **D-9's "40 px" header cell dies entirely** (40 px never renders at any band — it needs 2.5rem under a 16px root, which never co-occur); **banked K-9's clause "the same corpus's D-9 table has 40 px right" is CORRECTED** (the contradiction r1 recorded was between two wrong numbers); **banked K-10's frame "at the rendered 40/44 px … ~2.9-3.2 px" re-corrects to ~3.2-3.6 px** across the true bands (14.49 u × 45/49.5/44 ÷ 200 = 3.26/3.59/3.19); **banked K-14's "96×1.5=144 exactly" invariance is falsified** (93.5×1.5=140.25≠144) — the honest statement is a three-armed ladder with ~1.5× at the 640 crossing (140/93.5 = 1.497) and **1.54× extremes** (144/93.5), reader-1's "1.54×, not 1.5×" being itself the extremes figure, not the crossing; FM-6's absolute-px cell is re-stated accordingly (the 7.00%/2.25% = 3.11× RATIOS are viewport-invariant and untouched). **The banked conclusion that SURVIVES**: fr-DarkModeToggle superlative 8's 2.5.5-AAA claim — its "45/44" ladder was two-armed, but the missing 49.5 arm also clears 44 px, so AAA holds at all three; recorded so nobody "fixes" it. **SC-6 (against reader-2)**: its L-08 kill text endorses "the D axis's own D-9 table has 40 px right" — banked K-9's clause inherited without re-derivation, inside a kill, while its co-reader was falsifying it: the third premise-inheritance event in this registry family.
3. **FM-2's label-vs-aim — reader-2's tag receipt ADOPTED as the K-15 refinement (K-19).** Both readers re-killed the corpus's "glass-ui 7.0.0 (producer latest)" label (producer is 8.0.0; seat re-pin `4a86570b`). Reader-2 alone produced the receipt no prior seat had: **the ring deletion is verifiable AT the v7.0.0 TAG** — `git merge-base --is-ancestor 0078e508 v7.0.0` → true, and `git show v7.0.0:src/styles/theme/bridges.css` carries the "DELETED, clean break" comment verbatim (both re-run by this seat). The label dies; the AIM was right — 7.0.0 is the census's uplift target and the deletion is already in it, which closes the one soft spot in FM-2's evidence (it no longer rests on HEAD alone). FM-2's grade (MAJOR, BLOCKER-on-execution) and booking-locus ruling are carried unchanged. **SC-7 (against reader-1)**: its "two commits past the 074a3d0e" is TEN commits (log position 11, seat count) — trivial, but the drift RATE is FM-2's own argument, so the number should be right.
4. **M-2 (cartoon-surface de-sprung at 8.0.0) — reader-1's fold-by-reference HONOURED; riders booked, nothing re-booked.** Seat-read at the producer: the utility has MOVED (`src/components/card/styles.css:256`, not under `src/styles/`) and its body is three static declarations — no `translate`, no `transition`, no `:hover`; the docblock says "STATIC". The 8.0.0 body fact is banked at fr-EqCoefficientsPanel FR-EQC-5/K-7 (seat re-read — reader-1 cited honestly rather than re-booking). What is NEW here is the collision with THIS component's two motion rows, booked as riders: (a) the de-sprung-lift rider (r1's fold of reader-1-missed-#4 onto FR-AH-11's leg) describes a lift that **will not exist post-uplift** — the F.W4 ticket must be sequenced AFTER F.W1 or dropped; (b) the L-04/D-3 PRM rider's "THREE ungated motion layers" at the `/morph` host becomes **two** at F.W1 — the PRM ticket's scope note; (c) the converse — a genuinely interactive cartoon site silently LOSING its lift affordance at F.W1 with no typecheck signal — is the FM-2 defect class and is already banked (FR-EQC-5's F.W1 note + FR-EQC-13), cited not re-booked. Reader-1's own method note is ratified with it: its opening hypothesis (utility deleted) was refuted by its own wider grep — the scope of a producer grep is itself a stale-dist trap.
5. **PRM severity (L-04/D-3)** — reader-1 files MAJOR against the banked MINOR home (FR-AH-31); reader-2 holds the banked grade while recording the dissent. **Fold stands at the banked home's MINOR**; the MAJOR dissent ledger grows to FOUR reader-seats deep across two corpora (both axes' + both r1 readers' + reader-1 here). Recorded, not re-ruled — the home row owns the grade.
6. **Reader-2's four self-kills ADOPTED (K-20).** A reader that publishes the hypotheses it executed to death is the §D discipline applied to oneself; each is recorded so no later seat re-mints the defect: SELF-1 (the `type AnimationCtor = typeof Animation` alias is SOUND under `verbatimModuleSyntax` — killed by running the repo's compiler flags in isolation; TS1361 fires only in value position), SELF-2 (no module-level Animation registry in the shipped engine — completed animations are GC-able; L-01's post-unmount work is real, a retention leak on top of it is not), SELF-3 (the `lowLevel > highLevel` inversion is structurally unreachable — the grid interlocks the bounds; re-graded to superlative, seat-concordant at :110/:115), SELF-4 (the SvgFilters PRM precedent is DOUBLY gated — the pencil-boil producer gates `start()` itself — so D-3's contrast is stronger than the axis argued).

## Adjudicated defect roster

Line anchors are live-WT at fourier `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W0** substrate/assets · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Identity folds are riders on banked rows — never re-bookings.

### Carried from r1 — re-affirmed by this corpus (compressed; full adjudicated forms in the r1 record, superseded here but preserved in git history at this path)

| id | grade | claim (one line) + r2 touch |
|---|---|---|
| **FM-1** · D-1⊕C-1 | **BLOCKER** | The `/morph` route's only interactive control has an EMPTY accessible name; the 41-line leaf offers no affordance to fix it (WCAG 2.2 §4.1.2 Level A, nav-visible, never axe-scanned). Both r2 readers re-confirmed at BLOCKER with fresh censuses (8 specs, AxeBuilder ×2 both `/visualize`). **F.W4** (leaf `title?` prop + `aria-hidden` default; name the button) · axe run → SS-13. |
| **FM-2** · D-2 ⊕ cure-collision | **MAJOR**, BLOCKER-on-execution | glass-ui's producer deletes BOTH ring registers; six read sites / five files lose focus indication at F.W1, invisible to the all-import/type break surface; falsifies banked fr-AppHeader killed-item-11 at the producer; strands banked N-6's cure. **r2 strengthens**: the deletion is proven AT the v7.0.0 tag (ruling 3/K-19) — no longer HEAD-dependent. **F.W1** (token-level break-surface column) + **F.W4** rider (six sites → `--focus-ring-color`, one edit). |
| **FM-3** | **MAJOR** | HarmonicLevelGrid rebuilds all twelve 512-point paths on every re-render (~168 identical ~57 K-char rebuilds/toggle + unguarded slider-path churn). **r2 compounds via FM-20**: three of the twelve cells provably rebuild the SAME string. **F.W4** memo edit · jank witness → SS-13. |
| **FM-4..FM-16** | 13 MINOR | As ruled in r1 (chip contrast demoted-with-rationale · always-inline color style · no proportion contract (px cell re-stated per FM-21) · fallback-less `var()` · 4-site primitive bypass · no live region + focus-theft · zero output assertions · false scheduler invariant · morphProgress cure-completion · config mutation mid-flight · bespoke info-chip (F.W1 caution) · ring-sweep omission · unrolled chip loop). All re-affirmed where this corpus touched them; no regrades. **F.W4** (FM-14 → F.W3/W4). |
| **FM-17..FM-19** | 3 INFO | Dead defaults + 6-site viewBox literal · latent `overflow:visible` (~10-unit margin) · frozen assets with no regeneration pipeline + 5 orphan JSONs. Re-affirmed; FM-19's unguarded-drift exposure is now ALSO the reachability frame for FM-23 and the assumption FM-20's cure must not inherit silently. **F.W4 / — / F.W0**. |

### Identity folds (19 — 18 carried from r1 with r2 riders where marked, 1 new)

Carried unchanged: **L-01**→FR-AH-9 (+unguarded-Reset rider; both r2 readers re-proved at the shipped engine bytes — `stop()` resolves `play()`, :938/:883-885; the BLOCKER dissent recorded again) · **L-02**→FR-AH-8 · **L-04/D-3**→FR-AH-31 (**r2 rider**: the third motion layer EVAPORATES at F.W1 — ruling 4b) · **D-4**→fr-DarkModeToggle M-2/D-B1 (2.50:1 re-derived by reader-1 to 2.504, concordant) · **D-6**→N-1 (transposed table re-killed, K-4 concordant at 7.487/2.504) · **D-7**→M-3/L-4+M-1 (step-2 mechanism re-killed, K-2 third minting) · **C-2**→N-12 (+FR-AH-49) · **C-3**→M-6/D-M3 (drift band re-stated per FM-21) · **C-4**→M-1/D-B2/L-6 · **C-5**→FR-AH-23 (+fr-App m-9/C-9) · **C-10**→N-14 (tuple-widening premise-kill re-ratified) · **C-13-half**→N-14+N-15 · **L-06**→N-15 (latent re-confirmed: 512 pts every level, both assets — seat run; **FM-23 adds the sixth link its cure misses**) · **L-12⊕D-1-leaf**→N-4 · **D-11-leg-2**→N-9 · **L-13**→intake R5-7 carry (12+2=14 re-derived by reader-2, seat-concordant) · **updateField**→FR-AH-37 (**r2 rider: `getIdlePoints` joins the batch** — declaration :110 + return :227, zero consumers, seat census; a second dead public API in the same composable) · **reader-1-missed-#4 (de-sprung lift)**→FR-AH-11-leg (**r2 rider: sequence AFTER F.W1 or drop — the lift dies at the uplift**, ruling 4a) · **reader-1-missed-#5**→N-3+N-5.

**NEW fold (r2)** — **easings/value.js seam** → **banked fr-EasingPicker L/B-1 + C/B-1** (F.W1; + lane-docs O-14/I-2): no `"."` export at value.js 4.0.0, `timingFunctions` deleted, 14/22 presets drift analytic→CubicBezier under a CORRECT rewrite — all re-verified in-repo by this seat. **The one unbanked arm, booked as a rider on that F.W1 ticket (reader-2 missed #5, INFO)**: `easings.ts:58`'s `as EasingFn` cast erases the shape change, so an INCORRECT re-point at `bezierPresets` (4-tuples, not functions) typechecks clean and throws on the first rAF tick inside the frame callback (:135-138 → :172) — the awaited `play()` never resolves, `phase` sticks at `"settle-out"`, and the `!== "idle"` guard closes the app-wide theme toggle PERMANENTLY. Rider text: **the F.W1 rewrite must be verified by execution, not typecheck.** Nothing is broken today (all 22 keys resolve at installed 0.13.0 — banked 55-key probe). |

### New rows (this corpus)

| id | grade | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|---|
| **FM-20 · reader-2 missed #1** | **MAJOR** | The harmonic-level domain is decoupled from the asset level table and `/morph` lies about it in four places: candidates hardcode 75/100 (useMorphConfig :30, no shape argument) and the High bound is a literal 100 (grid :36/:44/:115) while both shipped assets top out at 50 — so (a) cells n=75/n=100 paint byte-identical paths to n=50 (silent duplicates; the pedagogy is false above 50), (b) `active` lands on n=50 while `is-bound` marks the clicked 75/100 (two highlight systems, one click, two answers), (c) the `n=` readout reports 75/100 while the SVG renders 50 — writers `setLevel:101` and the settle ticks :174/:203, NOT `setShape:92` (SC-5), and with `highLevel:100` the readout animates over a frozen glyph for the >50 span of both settle phases, (d) Export serialises `{"highLevel": 100}` — unhonourable by construction. All static fact; every arm reproduced by this seat's own node run. | useMorphConfig :30/:69-75 · grid :36/:44/:60-63/:115 · demo :15/:64-66/:71/:115-120/:172 · svg-fourier :112-119/:130-142 · seat node replication (`{clamped:50,lo:35,hi:50,t:1}` ∀ targets ≥50, both assets) | **F.W4** — derive candidates + High bound from `shape.data.levels` (one argument to `computePreviewLevels`; clamp the input/slider/emitHigh trio in the same edit), **naming the source-of-truth shape** (mid-transit ambiguity rider, ruling 1) and pairing with FM-19's golden-file ask so a regenerated asset re-derives the UI domain. Duplicate-cell perceptibility + the var()-substitution dependency → SS-13. |
| **FM-21 · reader-1 missed M-1** | **MINOR** | The rendered pixel ladder for BOTH hosts is three-armed because the toggle/morph breakpoint (640) and the root-font breakpoint (768) disagree by 128 px of viewport: header toggle **45 → 49.5 → 44 px** (NON-MONOTONIC — it grows past 640 then shrinks past 768, largest in the band nobody measured); `/morph` content boxes **93.5 / 140 / 144 px** (px widths × rem padding). One control scaled by two incompatible systems; the header's optical rhythm inverts mid-ladder. Corrections cascade ruled at ruling 2 (D-9's 40 px dies; K-9 clause, K-10 band, K-14 invariance all corrected; fr-DarkModeToggle superlative 8 survives STRENGTHENED — all arms ≥ 44 px). | AppHeader :242/:256 (@640 :245) · style.css :41/:47 (@768 :45) · MorphShapePreview :92/:95/:102/:104 · seat sweeps: no third writer of either register | **F.W4** — one decision: align the component breakpoints with the root-font breakpoint, or size the control against ONE system (px, or rem-with-aligned-breakpoints); document it where D.W4.d documented the amber darken. `getComputedStyle` at a 700 px viewport + perceptibility → SS-13. |
| **FM-22 · reader-2 missed #2** | **MINOR** | The `closed = false` branch of `pointsToSvgPath` is unreachable — the tree-wide caller census is exactly two, both single-argument — so the `catmullRomToBezier` import (svg-fourier :11) is dead code, and deleting the parameter + branch + specifier is the FREE half of banked FR-AH-53's cure (one fewer file in the F.W1 pencil-boil `0.4.1 → ^0.11.2` migration surface). Honest limits carried from the reader, seat-trusted as concordant with banked fr-App: the vendor chunk stays eager via SvgFilters/App.vue regardless, and the symbol still ships at the producer — dead code, not a latent break. | svg-fourier :11/:49/:52 · caller census (seat): calls :81 (useFourierMorph) + :131 (grid), imports :19/:93, no second argument anywhere | **F.W4** one-liner, riding banked **FR-AH-53** (which books the eager edge but not the dead branch — registry-swept, zero prior bookings). |
| **FM-23 · reader-2 missed #3** | **MINOR** | `lerpPoints` silently truncates on length mismatch (`Math.min(a.length, b.length)` :99) — the SIXTH link in L-06's silent-degradation chain and the one link banked N-15's `size === 0` cure does NOT catch: a length-mismatched-but-non-empty asset pair cross-fades only the common prefix, closed by `" Z"` into a chord across the figure, no throw, no warn. Reachable exactly where the two arrays come from DIFFERENT assets (morphTo phase 2, :183-189). Latent today — 512 points at every level of both assets (seat run) — and the exposure is precisely FM-19's unguarded regeneration drift. | svg-fourier :94-106 (:99) · useFourierMorph :183-189 · seat asset sweep | **F.W4** — rider ON banked **N-15**'s cure: a uniform-length assertion at `prepareFourierShape`, not an emptiness check; pairs with FM-19's golden-file diff. |
| **FM-24 · reader-2 missed #4** | **INFO** | `syncWith`'s watcher can never bail out: the getter allocates a fresh `{ ...config }` per run so reference comparison always reports change, and `deep: true` then traverses the throwaway clone of a flat 8-scalar object for nothing — a new SITE in the thrice-banked `deep: true` family (FR-CP-29, C-20, I-6/C-37) and the delivery mechanism for FM-13 (every slider step reaches `updateConfig` → `Object.assign` through this always-firing path). | useMorphConfig :78-84 · MorphConfig = 8 scalars (useFourierMorph :59-68) · updateConfig :106-108 · call site demo :111 | **F.W4** — drop `deep` (the getter's own object identity suffices) or watch `config` directly; the SAME edit as FM-13's decision point, so both rows close together. |

**Seat additions (this corpus, beyond both readers)**: (i) **`getIdlePoints` is dead public API** — declaration + return only, zero consumers (seat census) — the second `updateField`-class corpse in the same composable; folded as a rider onto the FR-AH-37 batch (folds table). (ii) **Declared negative, fired falsifier**: `useClipboard`'s ROOT-specifier import at useMorphConfig:9 was this seat's own F.W1-break hypothesis — REFUTED: the producer's `"."` export survives at 8.0.0 and `useClipboard` is live in the export closure; recorded so the F.W1 census doesn't re-derive it.

## Killed-claims register — 20 entries

**K-1..K-16 (r1) — carried whole**, and re-ratified by this corpus wherever touched: both r2 readers independently re-killed D-5 (commit≠paint), the D-6 transposed table (reader-1 re-derived all four cells by hand: 7.487/2.504), D-7's step-2 mechanism (third minting), S-2's phantom clip, K-5's OS-preference bootstrap, K-6's inverted inference (with the fr-EasingPicker:138 readback re-raised), K-7/K-12's double-announce and eight-nodes-per-frame arithmetic, K-8/K-9's byte figures and dead-`5rem` premise, K-11's forced-cast ruling, K-13's three-files count, K-14's border-box (now itself corrected — below), K-15's stale producer label (now refined — below), and K-16's drift bundle. No r1 kill was overturned; three r1 kills are **corrected in their px cells** by K-17.

| # | killed claim | adjudication |
|---|---|---|
| **K-17** | The corpus's absolute-px column, THIRD minting — challenge-L L-08's "`14` (toggle, into a `5rem` box)" (:46, seat-read); **plus the "40 px" cells banked K-9/K-10 endorsed and D-9 printed** | **Reader-2's L-08 kill SUSTAINED** (the L axis independently repeated the dead-fallback premise the C axis died on twice — three axes, one never-rendered literal). **AND the seat rules further, per FM-21**: 40 px never renders either — D-9's header cell dies whole; banked K-9's "D-9 has 40 px right" clause, K-10's "rendered 40/44 px" frame (→ ~3.2-3.6 px band), and K-14's "96×1.5=144 exactly" invariance (→ 93.5/140/144, 1.497× at the crossing, 1.54× extremes) are **corrected at the bytes** — r2-corrects-r1, the pt-leaf K6 idiom. L-08's structural survivors (user-space stroke, no `vector-effect`) unaffected (FM-6). SC-6 records reader-2's inheritance of the corrected clause. |
| **K-18** | C-2's anchor + figures, re-killed with a new cell: the `d +=` emission line is svg-fourier **:69**, not :68 (:68 is `cp2y` — seat-read); byte figures 57,027/56,571 (dropped `" Z"` :72 → 57,029/56,573, K-8 concordant); ~0.4 px/unit precision premise (dead `5rem` → the true quantum is band-dependent ~0.20-0.25 px/unit under FM-21's ladder) | **SUSTAINED** (reader-2's kill, seat re-verified at the file). The per-frame invalidation mechanism itself is confirmed and stays banked at N-12. |
| **K-19** | "glass-ui 7.0.0 (producer latest)" (challenge-D:7-8 et al.) — the label | **SUSTAINED as a label kill, REFINED in aim (ruling 3)**: producer is 8.0.0 HEAD `4a86570b` (seat pin), but reader-2's tag receipt — `merge-base --is-ancestor 0078e508 v7.0.0` true + the deletion comment verbatim at `git show v7.0.0:` (both seat re-runs) — proves the ring deletion is IN the census's named uplift target. K-15's re-read order is discharged; FM-2's evidence no longer rests on a moving HEAD. Drift-bundle additions: reader-1's "two commits past" → **ten** (SC-7); reader-2's grid Slider anchor ":43" → **:44**; its interpolate clamp ":129-131" → **:130-131**; its FM-20 mechanism cite `setShape:92` → `setLevel:101`/tick `:203` (SC-5). |
| **K-20** | Reader-2's four SELF-kills, adopted (ruling 6): the sound `AnimationCtor` alias (TS1361 is value-position-only — compiler-executed); the phantom engine registry (no module-level Animation list — dist read); the unreachable bounds inversion (grid interlock :110/:115 — seat-concordant, re-graded superlative); the under-credited SvgFilters precedent (producer-gated `start()` — doubly gated) | **ADOPTED into the register** so no repair ticket "fixes" the alias, hunts the leak, guards the unreachable inversion, or weakens the precedent D-3 cites. A reader publishing its own dead hypotheses with the killing instrument named is the §D discipline turned inward — see superlative 9. |

## Superlatives — 12, ratified with provenance

**1-8 sustained from r1, re-affirmed by this corpus**: the default-outperforms-its-consumer inversion (S-3⊕S-5 — and the token survives 8.0.0 byte-identical, the falsifier that makes FM-2 a specific deletion); the `currentColor` primitive's survivable failure mode (S-1⊕C-S-3, with FM-2 as the control experiment one file over); the 41 lines themselves (zero imports/hooks/refs/timers/`any` — re-verified line-by-line by reader-2); the L axis's §D falsifier discipline (five withdrawals, one inversion — re-run at the shipped engine by both r2 readers and holding); L-14's survival under a strictly stronger instrument; C-5's disposition discipline (declining the right fix at the wrong time — now vindicated twice: this seat's re-probe confirms a 4.0.0 adoption landed today would be rewritten again at F.W1); the D §5 method note proven by its own author twice; the declared negatives.

**9 (new) — reader-2's self-kill discipline.** Four hypotheses formed, four executed to death with the killing instrument named (one by running the repo's own compiler in an isolated repro), one re-graded into a superlative rather than dropped. The exact §D virtue this registry keeps ratifying in axes, now demonstrated by a re-reader — and the reason this seat could adopt its five missed finds after verifying only their bytes, not their honesty.

**10 (new) — the bounds interlock at the input seam (SELF-3's regrade, seat-concordant).** `emitLow` clamps to `highLevel−1` and `emitHigh` to `lowLevel+1` (grid :110/:115), with inputs and Sliders binding the same reciprocal bounds — the one place in the morph feature where a whole defect class (the settle-phase collapse under `lowLevel > highLevel`) was designed out. FM-20 makes the contrast pointed: the same file that interlocks the bounds' RELATION never checks their DOMAIN.

**11 (new) — the producer's cancellation contract is documented, honest, and correct.** `stop()` says it resolves the pending `play()` and the shipped bytes do exactly that (:938, both readers + seat-concordant across two corpora). L-01/FR-AH-9 is entirely a caller defect; recorded because the folder's severest fold could otherwise be mis-routed as a keyframes coordination ask.

**12 (new) — DarkModeToggle labels the ACTION, not the state** (`:aria-label="isDark ? 'Switch to light mode' : …"`, :5) — the APG-endorsed mode-switch treatment on the BUTTON, not the SVG; the repo contains FM-1's cure one directory away, which is precisely what makes FM-1 an oversight rather than an unknown.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **FM-1** — axe's `button-name` firing on `/morph` (statics complete; the run is the witness).
2. **FM-2 publication state** — whether the PUBLISHED 7.0.0/8.0.0 artifacts match the tag/working tree read here; cuts symmetrically at S-5, and — reader-2's honest addition, adopted — at this seat's own value.js folds (the in-repo `dist/` may lag `src`; confirm at the F.W1 re-baseline). Banked N-6's rebuild probe remains the browserless decider.
3. **FM-20** — duplicate-cell perceptibility (byte-identity is static fact); AND the finding's grid-visibility arm depends on presentation-attribute `var()` substitution — **fr-EasingPicker.md:138's open readback now bites twice** (K-6 safety + FM-20 arm (a)): one `getComputedStyle(document.querySelector('.grid-svg path')).stroke` on `/morph` decides both. Arms (b)/(c)/(d) survive regardless.
4. **FM-21** — `getComputedStyle(document.querySelector('.sun-moon-toggle')).width` at a 700 px viewport (the arithmetic needs no browser; the witness is one call), and whether the 45→49.5→44 inversion is perceptible in use.
5. **FM-3 jank** — ~168 rebuilds/toggle + slider churn; FM-20 compounds the count without changing the question.
6. **FM-6** — device-pixel strokes, now across **three** bands per host (ratios browserless).
7. **De-sprung lift (FR-AH-11 rider)** — perceptibility + built-sheet `@layer` check, **now scoped by M-2 to the pre-uplift tree only**.
8. **Banked residue re-cited, not duplicated**: D-4/M-2 translucent-header backdrop; M-3 cold double-click window; C-2/N-12 perceived jank; I-4 suspended-mount exposure; N-4/L-12 AX exposure of a nameless `<svg>` per engine.

## DISSENT — preserved, not ruled away

1. **Severity dissents, cumulative ledger**: L-01 BLOCKER (axis + reader-2's recorded dissent, two corpora); PRM MAJOR (four reader-seats deep — ruling 5); FM-2 BLOCKER-now (r1 reader-1; this record holds MAJOR-with-named-escalation, now tag-proven); FM-4 MAJOR (r1 reader-1, phase-transience demotion stands); FM-11 MAJOR (axis + r1 reader-1). None re-ruled here; homes own grades.
2. **The r2 corpus's residual failure mode is premise-inheritance, not measurement** — SC-5/SC-6/SC-7 are all inference/citation slips inside otherwise-verified cells, matching the fleet pattern banked at fr-DarkModeToggle superlative 9 and pt-leaf ruling 3. Both readers' arithmetic reproduced wherever this seat re-ran it.
3. **FM-20's booking locus** — arguably a HarmonicLevelGrid/useMorphConfig defect, not the leaf's; booked here because this component's corpus is where the domain was first checked against the data, and the forming F.W4 spec should lift it whole with this record as provenance (the FM-2 precedent).
4. **FM-21's cure is a design decision, not a patch** — which scaling system owns control sizes is an F.W0-adjacent question the F.W4 ticket should pose, not answer.

## Closing verdict

**ADJUDICATED-DEFECTIVE-AT-THE-SEAMS, LEAF RATIFIED — r2 SUSTAINS the r1 verdict and widens the seam inventory**: the 41 lines remain among the cleanest in the fourier registry (re-verified by both fresh readers line-by-line; zero `any` against six in the chain; the survivable-failure-mode primitive), and every defect this corpus adds lives in what surrounds them — **FM-20** (MAJOR: the level-domain truth failure — duplicate cells, disagreeing highlights, a lying readout, an unhonourable export — reachable in one click and missed by three axes and two prior readers), **FM-21** (the non-monotonic three-armed pixel ladder that retires every absolute-px figure in the corpus and corrects three of r1's own kill cells while leaving one banked AAA conclusion strengthened), **FM-22/23/24** (the dead branch that is FR-AH-53's free half; the sixth silent-blank link N-15's cure misses; the always-firing `deep` watcher), plus the tag-level proof that FM-2's deletion is already in the census's named uplift target and the M-2 riders that re-sequence two F.W4 tickets behind F.W1. 43 rows, 20 register kills (four of r1's cells corrected at the bytes, none of its rulings overturned), 3 new seat corrections, 12 superlatives. Routings are M-25 agglomeration input for the forming X·F specs; nothing here stamps a wave.

— end of apotheosis r2. The original challenge corpus and both r2 reader returns remain immutable beside this file per E-1/E-3; the r1 record this supersedes is preserved in git history at this path.
