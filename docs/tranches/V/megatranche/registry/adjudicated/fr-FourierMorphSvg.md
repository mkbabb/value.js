claude-fable-5 (served model id)

# fr-FourierMorphSvg — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-10 · **Subject**: `fourier-analysis/web/src/components/decorative/FourierMorphSvg.vue` (41 lines; read whole by this seat) — the path-only SVG renderer terminating the morph chain, with its two live consumers `layout/DarkModeToggle.vue` (109, read whole) and `morph/MorphShapePreview.vue` (175, read whole), their driver `morph/FourierMorphDemo.vue` (read to :200), the producing composables `useFourierMorph.ts` (230, read whole) + `useMorphConfig.ts` (97, read whole), and siblings `HarmonicLevelGrid.vue` (:40-139) + `FourierShapeExtractor.vue` (svg census).
**Substrate**: fourier `cd26c65` + dirty WT (28 porcelain rows — the M.W1a pin-bump family per banked fr-AppHeader/fr-DarkModeToggle; every subject file clean) · installed glass-ui **4.0.0** · keyframes.js **4.3.0** · value.js **0.13.0** · glass-ui producer at **8.0.0**, HEAD `074a3d0e` (seat-read `package.json` + git pin) — the corpus's "7.0.0" framing re-killed per banked SR-2/K-15.
**Corpus**: three axis challenges (`audit/fourier-components/FourierMorphSvg/challenge-{D-design,L-library,C-consumption}.md`) × two hostile re-reader returns (reader-1 design-and-usage, slug `FourierMorphSvg` · reader-2 library-and-code, slug `fr-FourierMorphSvg`).
**Method (M-23/L-18)**: every reader-vs-reader disagreement and every load-bearing verdict was re-derived against the tree with this seat's own instruments before ruling. The readers ran real registry sweeps this time (an improvement on the fr-DarkModeToggle corpus) and their folds all held under my re-checks; the seat's own work here is severity arbitration on four contested rows, four seat corrections (two against each reader), one severity-anchor kill, and independent reproduction of every disputed number. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node/grep only; zero browser probes — probe parsimony; product source READ-ONLY; this file is the sole write)**: whole reads of the six subject-chain files named above; `index.html:15-35` (the pre-paint predicate, verbatim — the D-4-parenthetical decider); `style.css:105-145` (cartoon-card shim :107-111 · the D.W4.d `--viz-amber` block :113-127 · the D.W4.d ring block: comment :129-135, selectors :136-139, declarations :140-142); `scheduler.ts:1-20` (the L-05 sentence, verbatim); ring-consumer census re-run (`grep -rn "var(--ring)\|var(--color-ring)" src/` → **exactly 6 sites / 5 files**: style.css:140, ImageUpload.vue:200, GalleryCard.vue:222+:223, AppHeader.vue:189, DarkModeToggle.vue:99 — byte-matching both readers); `updateField` census (2 hits: declaration :64 + return :92 — zero consumers); `FourierShapeExtractor.vue` svg census (`<svg viewBox="0 0 200 200">` at :9-12 and :72-75, paths :36/:82/:90, no FourierMorphSvg import; router lazy route :112-114); installed-pin token reads — color-radius.css:40 `--neutral-0: hsl(40 30% 98%)`, :256 `--accent-pink: oklch(0.613 0.197 353.8)`, :258 `--accent-red: oklch(0.574 0.216 27.5)`, dark-arm.css:109/:111, bridges.css:80 `--color-ring: var(--ring)`; cards.css:33-49 `@utility cartoon-surface` whole (border-width 2px · `translate: 0` · `transition: translate var(--duration-normal) var(--spring-smooth), box-shadow …` · hover `translate: var(--lift-sm)` · **no outline declaration**); tailwindcss **4.3.1** (my read); PRODUCER 8.0.0 reads — `grep -rnE '^\s*--(color-)?ring\s*:' src/ dist/` → **EMPTY both trees (my run)**, bridges.css:93-97 deletion comment read verbatim ("…is DELETED, clean break. The house focus accent is the `--focus-ring-color` token / the `.focus-ring` box-shadow utility…"), `--focus-ring-color` present (color-radius.css:161, dark-arm.css:133); exports probes — installed `{./icon-chip, ./metric-badge, ./toggle-chip, ./badge}` vs producer `{./badge, ./chip}` (metric-badge/icon-chip/toggle-chip GONE — my node runs); `metric-badge` importer census → **7 files** (EquationView, InfoCard, AnimationControls, EditorControlsDock, EquationPanel, GalleryAdminBanner, GalleryDraftsSection — the census's ×7 exact; reader-1's 5-file enumeration incomplete by 2, its ×7 arithmetic right); e2e — AxeBuilder in exactly 2 specs, both `goto("/visualize")` (ux :28/:47 re-read); `/morph` visited only by visual-baseline.spec.ts:35, sole expect the overflow gate (~:65); root font `style.css:41` `1.125rem` → `:47` `1rem` at the breakpoint (the SC-3 decider); **the full contrast suite re-derived by my own node run (OKLab/HSL→sRGB→WCAG 2.x)** — SUN `#E88845` vs light `rgb(251,250,248)` = **2.50**, MOON `#c084fc` vs dark `rgb(11,10,9)` = **7.49**, SUN-on-dark = **7.57**, MOON-on-light = **2.53** (the transposition decider), morph midpoint 2.61, `--accent-pink` light → `rgb(215,66,140)` on its 12% chip plate = **3.42** (3.99 on bare page), `--accent-red` light → `rgb(219,36,36)` on plate = **3.89** (4.69 on page), dark arms 5.73/4.93 (pass), S-3's 4.62 light-card / 4.07 dark-card / 4.69 light-page — every reader figure reproduced to the hundredth; registry identity sweeps — `grep` over `adjudicated/*.md` for MorphShapePreview/info-chip/morph-button/HarmonicLevelGrid/FourierShapeExtractor/updateField → **zero defect bookings at any of these sites** (mentions are instrument lists and fr-EasingPicker.md:138's open question only); targeted reads of fr-DarkModeToggle.md (N-1, N-3, N-4, N-5, N-6 at :92, N-9, N-10, N-12 — **in the MINOR table**, N-14, N-15, M-1, M-2, M-3, M-6, I-4, K-2, SR-2/K-15), fr-AppHeader.md (FR-AH-8/-9/-11/-23/-24/-31/-37/-49, **killed item 11 read verbatim**), fr-App.md, fr-EasingPicker.md:138, CENSUS-2026-08-03.md:100-104 (the metric-badge ×7 break-surface row); git pins both repos.

## Scoreboard

**37 rows ADJUDICATED-CONFIRMED (1 new BLOCKER · 18 identity folds · 2 MAJOR booked · 13 MINOR booked · 3 INFO booked) · 16 killed-claims register entries (14 reader kills RATIFIED — four of them decided by this seat's own node runs — plus 1 reader-vs-reader premise conflict ruled and 1 new seat severity-anchor kill) · 4 seat corrections minted (2 per reader) · 10 reader-missed finds adjudicated (2 booked MAJOR, 1 demoted MAJOR→MINOR with rationale, 3 MINOR, 4 folded as riders) · 8 superlatives ratified · both readers' registry discipline RATIFIED (every fold they claimed held under re-check — the fr-DarkModeToggle SC-2 lesson landed).**

The component-owned headline is **FM-1** — the `/morph` route's primary and only control is a `<button>` whose accessible name computes to the empty string, and the 41-line leaf offers no affordance to fix it: the one genuinely new BLOCKER in the folder. The registry-level headline is **FM-2** — glass-ui's producer deletes BOTH ring registers (`--ring` and the `--color-ring` bridge), which falsifies banked fr-AppHeader killed-item-11 at the producer, strands banked fr-DarkModeToggle N-6's cure on a token that will not survive the uplift, and is invisible to the census's all-import/type-level break surface. The leaf itself is close to exemplary — its default stroke out-performs the colour its app-wide consumer substitutes for it (S-3's inversion, re-derived by this seat).

## The incredulity seat's own work — ruled disagreements

| topic | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **D-1/C-1 severity** (nameless `/morph` button) | MAJOR, on the fr-DarkModeToggle M-2 house test ("failure with surviving capability grades MAJOR") — while conceding "capability does NOT survive for AT users here" and remitting the ruling to this seat | BLOCKER ("the strongest new row in the folder") | **BLOCKER — reader-2 wins, by reader-1's own concession** | The M-2 test's operative prong is *surviving capability*, and for the affected population it does not survive: the button announces as a bare "button" with no name (MorphShapePreview.vue:4-10 read — sole child is the SVG; no text, no aria-*, no sr-only span), so an AT user cannot know what the route's only control does. WCAG 2.2 §4.1.2 Level A — the floor — on a shipped, nav-visible route (router/index.ts:29/:102-104) that axe never scans (2 AxeBuilder specs, both `/visualize` — my re-run). The leaf half (no `aria-hidden`/`role`/labelling prop on a `decorative/` primitive) folds to banked fr-DarkModeToggle N-4; the consumer half is unbanked and is the booking. |
| **D-2 severity + booking** (ring-register deletion) | BLOCKER; folds the DarkModeToggle:99 line to banked N-6, books the uplift arm; separately files the cure collision as missed find #2 (MAJOR) | BLOCKER as filed but dissents twice: (a) belongs on the F.W1 break-surface row, not booked twice on a component; (b) uplift-conditional → MAJOR until F.W1 lands | **ONE row, MAJOR with a named BLOCKER-on-execution escalation — reader-2's booking, reader-1's merge** | My own producer runs: `grep -rnE '^\s*--(color-)?ring\s*:' src/ dist/` over glass-ui 8.0.0 → EMPTY; the deletion comment at src/theme/bridges.css:93-97 read verbatim; `--focus-ring-color` present (:161/:133). At the installed pin the ring resolves (bridges.css:80, my read) — nothing is broken TODAY, so MAJOR is the honest grade; the day F.W1 lands without a CSS-custom-property column, six focus sites across five files (my census, byte-matching both readers) go dark app-wide with zero typecheck/build/test/axe signal — BLOCKER at execution. The uplift arm and reader-1's cure-collision find are the SAME identity (the deletion is what breaks the cure) — merged into FM-2, folded onto N-6's line for the :99 site. |
| **L-03 severity + scope** (primitive bypassed) | Duplication CONFIRMED at HarmonicLevelGrid; the "indirection buys nothing" inference KILLED; severity not restated | MINOR on its own, but EXTENDED: FourierShapeExtractor hand-rolls the gestalt twice more → 4 bypass sites, primitive serves 2 of 6 | **MINOR at the 4-site scope — reader-2's extension adopted (the L-9.1 scope law)** | My svg census of FourierShapeExtractor.vue: `<svg viewBox="0 0 200 200">` at :9-12 and :72-75 with paths :36/:82/:90, no component import (the tree-wide `FourierMorphSvg` grep is 4 lines); routed at `/demo/shape-extractor` (router :112-114, my read). Every leaf-level cure (FM-1's aria default, FM-5's custom-property hook, FM-6's vector-effect) reaches 2 of 6 sites — that reach argument, not the 8 duplicated lines, is the row's weight. The inference kill is ratified (see K-6). |
| **L-05 severity** (false scheduler invariant) | "The most valuable un-folded L row"; holds the axis's MAJOR (severity is for the false documented invariant) | MINOR (impact bounded ≤2.4 s of rAF; what survives is a false comment) | **MINOR — reader-2 wins, on house precedent** | scheduler.ts:13-15 read verbatim by this seat; useFourierMorph's import list (:13-27) confirms no store/observer/visibility gate. The runtime harm is bounded and the cure is a comment correction plus adding the third clock to the census's PRM/gating book — the fr-DarkModeToggle N-5 family (false/stale documented claims) books at MINOR. The MAJOR dissent (a future wave relying on the sentence would skip gating a real ungated clock) is recorded and is exactly why the row must not be dropped. |
| **D-10/L-15 double-announce parenthetical** | KILLED it: `display: none` removes the subtree from the AX tree; a live region inside it does not announce — one arm renders at any viewport, so a naive `aria-live` on both announces once | CONFIRMED L-15/D-10 *including* "a naive `aria-live` fix would double-announce because both arms are always in the DOM" | **reader-1 WINS; SC-2 minted against reader-2** | MorphShapePreview.vue:124-145 read: `.desktop-info{display:none}` / `.mobile-info{display:flex}`, flipped at the 640px query — exactly one arm is ever rendered. Excluding `display:none` subtrees from the accessibility tree is defined behaviour, not an edge case. Reader-2 carried the axis's false compounding premise inside a confirmation — the premise-inheritance pattern this registry has recorded twice before (fr-DarkModeToggle SC-3, fr-AppHeader DISSENT 1). The substantive halves (no live region at all; the duplicated markup means any fix lands twice) survive in FM-9/FM-16. |
| **HarmonicLevelGrid recompute severity anchor** | (not filed) | MAJOR — "the /morph route pays ~8× the header's per-toggle string churn **that N-12 books as MAJOR**" | **MAJOR on its own magnitude; the anchor is KILLED (SC-1)** | fr-DarkModeToggle N-12 sits in that record's **MINOR (17 booked)** table — my read. The severity anchor is false; the row survives without it: ~168 identical ~57 K-char rebuilds per toggle (~9.5 MB transient text — my re-derivation of the 14-re-render × 12-cell cadence from `nearestLevel` over the 10-level table) plus 12 rebuilds per slider step-change with no `isAnimating` guard (FourierMorphDemo :64-65, my read) is an order of magnitude past the banked MINOR *and* rides the input path. Booked FM-3. |

**Seat corrections minted against the readers:**

- **SC-1 (against reader-2)**: the FM-3 severity anchor — banked N-12 is MINOR at its home, not MAJOR. The find is ratified at MAJOR on magnitude, not on the anchor (table above).
- **SC-2 (against reader-2)**: the killed double-announce premise carried inside the L-15/D-10 confirmation while its sibling reader was killing it — premise-inheritance inside a confirmation (table above).
- **SC-3 (against reader-1, trivial)**: the info-chip find's "text-sm (14px)" cell — fourier's root is `1.125rem` below 768px (style.css:41/:47, my read), so mobile text-sm renders 15.75px. Immaterial: both sizes stay below the 18.66px-bold/24px large-text thresholds, the 4.5:1 floor holds.
- **SC-4 (against reader-1)**: holding D-1/C-1 at MAJOR under a house test whose operative prong reader-1 itself conceded fails here — the concession decides the grade (ruled BLOCKER, table above). Also its metric-badge enumeration names 5 of the 7 importer files (EquationView.vue, InfoCard.vue omitted — my census); its ×7/"widens to 8" arithmetic used the census figure and is right.

## Adjudicated defect roster

Line anchors are live-WT at fourier `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W0** substrate/assets · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **GLASS-RELAY** rides the standing glass-ui BH inbox · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Identity folds are riders on the banked rows — never re-bookings.

### BLOCKER (1 new)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **FM-1 · D-1 ⊕ C-1 (consumer half)** | The `/morph` route's primary — and only — interactive control has an EMPTY accessible name: `MorphShapePreview.vue:4-10` renders `<button class="morph-button cartoon-card" @click :disabled>` with `FourierMorphSvg` as its sole child — no text node, no `aria-label`, no `title`, no sr-only span — and the 41-line leaf offers no affordance (no `role`, no `aria-*`, no `<title>`, no slot, prop set exactly `path|viewBox|strokeColor|strokeWidth` at :21-27) by which any caller could fix it at the leaf. WCAG 2.2 §4.1.2 Name/Role/Value, **Level A**, on a shipped nav-visible route (`router/index.ts:29`, `:102-104`) that is never axe-scanned (AxeBuilder in exactly 2 of 8 specs, both `goto("/visualize")` — seat re-run); the sibling consumer proves the correct pattern exists in-tree (DarkModeToggle.vue:5's live `:aria-label` on the **button**). Severity ruled in the table: capability does not survive for AT users, so the M-2 house test's demotion prong does not apply. | MorphShapePreview.vue:4-10 + FourierMorphSvg.vue:2-16/:21-27 (both read whole); e2e census (seat) | **F.W4** — leaf: optional `title?` prop emitting `role="img"` + `<title>` when present, `aria-hidden="true"` when absent (the honest default for `decorative/` — closes it for all future callers, incl. banked N-4's header half); consumer: name the button. Two lines. Axe run over `/morph` → SS-13. The leaf-side gap itself FOLDS to banked fr-DarkModeToggle **N-4** — the booking here is the unbanked consumer half. |

### Identity folds (18 — the banked row owns the defect; this corpus contributes riders)

| our id(s) | banked identity (route) | rider contributed by this corpus |
|---|---|---|
| **L-01** (stopAnim is a fast-forward; teardown starts new work post-unmount) | **fr-AppHeader FR-AH-9** (F.W4 epoch/cancellation edit; fr-DarkModeToggle M-3 is the family's fourth arm) | GENUINELY NEW: the unguarded `handleReset` (FourierMorphDemo.vue:75-78 Reset `<Button>` with no `:disabled` where siblings carry it at :18; handler :175-178 — seat-read) is a reachable, NON-unmount trigger that silently discards a user action and then reports `phase:"idle", morphProgress:1`. The L axis's BLOCKER framing is a severity dissent on the fold (banked home holds it below BLOCKER). Engine-quote hygiene pre-ruled: the consumed class is `Animation` (:937-939), not AnimationGroup. |
| **L-02** (engine promise caches its rejection forever) | **fr-AppHeader FR-AH-8** (F.W4 — clear the memo in `.catch`) | None — same file, same lines (:38-44/:149/:169, seat re-read), same cure. Both consumers' bare `await morph.morphTo` confirmed. |
| **L-04 / D-3** (no PRM gate anywhere in the morph stack) | **fr-AppHeader FR-AH-31** (MINOR, demoted with rationale; both hostile readers' MAJOR dissent already banked at fr-DarkModeToggle) | GENUINELY NEW: `MorphShapePreview.vue` has ZERO `prefers-reduced-motion` blocks yet stacks THREE ungated motion layers — its own `transition …transform .15s` (:97), hover/active transforms (:108-116), and glass-ui's `cartoon-surface` lift (cards.css:33-49, seat-read whole) — and the corpus's PRM inventory (lane-frontend.md:616-619) lists this host nowhere. `createTweenAnimation` (:127-133) declines the engine's shipped `respectReducedMotion` opt-in (banked aggravator). DarkModeToggle:104-108 decoy confirmed. |
| **D-4** (SUN literal fails §1.4.11 at 2.50:1 light; token bypass) | **fr-DarkModeToggle M-2 · D-B1** (MAJOR · F.W4, sequenced behind the FR-AH-23 palette guard; worst-case backdrop SS-13) | None new — my node run reproduces 2.50 (banked 2.51 = rounding across the `#fbfaf8` pin note). The "light is the default arm / index.html defaults to light" parenthetical is KILLED (K-5): the bootstrap arm is the OS preference (index.html:21-32, seat-read verbatim). "Light is what a majority of visitors see" survives as prose, not mechanism. |
| **D-6** (first toggle paints outgoing glyph in incoming colour) | **fr-DarkModeToggle N-1** (MINOR merge of C-7 ⊕ L-10) + rides **FR-AH-8** | The contrast column is TRANSPOSED — killed by this seat's own node run (K-4): light→dark paints MOON-on-dark = **7.49** (challenge says 7.57 = SUN-on-dark); dark→light paints SUN-on-light = **2.50** (challenge says 2.53 = MOON-on-light). The conclusion (the dark→light frame is sub-3:1) survives at the corrected figure. MAJOR filing is a severity dissent on the banked MINOR. |
| **D-7** (guard not armed across the lazy await; durable false indicator) | **fr-DarkModeToggle M-3 · L-4** (MAJOR · F.W4, the FR-AH-9/-20/-21 epoch family) + **M-1** (no re-sync watch) | Step-2 MECHANISM killed (K-2 re-ratified with this seat's own trace): in the only reachable window `currentAnim` is still `null` (first assignment :171, after the :149 await), so the second chain's `stopAnim()` is a no-op behind the :116 guard — the real failure is assignment-overwrite orphaning animA, exactly as banked K-2 ruled against the sibling corpus's C-6. The conclusion folds; the causality must not travel into a repair ticket. |
| **C-2** (~57 KB full-precision `d` rebuilt per frame) | **fr-DarkModeToggle N-12** (MINOR · F.W4 exact-hit + fixed-precision edit) + **FR-AH-49** | Byte figures corrected (K-8): the axis's replay dropped the closed-path `" Z"` terminator (svg-fourier.ts:72) — true lengths **57,029 / 56,573** (banked N-12 concurs); its precision premise dies with the never-rendered `5rem` fallback (K-9, banked N-10) — the real quantum is ~0.20-0.22 px/unit, the over-precision TWICE as severe as claimed. |
| **C-3** (assets don't respect the box; moon off-centre, 33% narrower) | **fr-DarkModeToggle M-6 · D-M3** (MAJOR · F.W4 re-centre at prepare) | Drift magnitude corrected (K-10): ~2.9-3.2 px at the rendered 40/44 px, not the claimed 5.8 px (same dead-`5rem` premise); banked M-6 carries the right figure. The axis's honest optical-centring falsifier note survives. |
| **C-4** (shape never resyncs to `isDark`) | **fr-DarkModeToggle M-1 · D-B2/L-6** (MAJOR · F.W4 — `watch(isDark)` → `setShape`, SEQUENCED with-or-after the FR-AH-9 cancellation edit) | None — the banked row is strictly broader (it names the second live external writer, cross-tab `storage` under vueuse 14.3.0's `listenToStorageChanges: true`, which the C axis missed) and carries the load-bearing sequencing constraint the C axis does not. Seat re-ran `grep -n watch DarkModeToggle.vue` → no match. |
| **C-5** (MOON_COLOR provenance comment false three ways) | **fr-AppHeader FR-AH-23** (+ fr-App m-9/C-9) (F.W4, delete-or-correct now; palette route sequenced after fr-BasisCanvas D-1) | None new — and C-5's disposition discipline (declining the value.js adoption, sequencing behind value.js's own R1 oklch crash, refusing to undo the keyframes lazy boundary) is ratified as a superlative below. |
| **C-10** (two `as any` casts erase `FourierPathData` at the seam) | **fr-DarkModeToggle N-14** (F.W4 typed loader / one shared prep module) | The "casts are unnecessary today" premise KILLED (K-11): TypeScript never infers tuples from JSON modules — `domain: [number, number]` (and `coefficient`) vs the widened `number[]` FORCES an assertion, exactly as banked N-14 ruled; the surviving defect is the over-breadth of `any`, which N-14's cure already names. |
| **C-13 (drift-guard half)** | **fr-DarkModeToggle N-14 + N-15** (typed loader + throw-at-prepare) | The unbanked half — no in-tree regeneration pipeline + the five orphaned ~271 KB route JSONs (1.358 MB, zero importers — reader re-measured, seat-trusted as concordant with banked "context only") — survives as **FM-19** (F.W0). |
| **L-06** (five-link silent-blank chain → invisible control) | **fr-DarkModeToggle N-15** (F.W4 `size===0` throw at prepare) + rides **K-9** | None — every link re-verified against the live files by both readers and spot-checked by this seat (`if (ps)` :79-85 no-else; `:146-148` bracket degrade; `:51` `""`; `:9` `<path :d>`; the transparent borderless host). Latent today (asset sweep concordant). |
| **L-12 ⊕ D-1 (leaf half)** (decorative primitive declares no decorative semantics) | **fr-DarkModeToggle N-4** (F.W4, rides N-2) | The cure unifies with FM-1's leaf edit — one default (`aria-hidden="true"` absent a `title` prop) discharges both the banked header half and the new `/morph` half. AX-tree exposure stays SS-13. |
| **D-11 (second leg)** (hover `outline:none` vs focus-visible ring at equal (0,2,0) specificity — order-only survival) | **fr-DarkModeToggle N-9** (F.W4 — delete the dead declaration) | None; seat re-read :89-101. First leg is booked new (FM-15). |
| **L-13** (2-vs-14 painted-instance gap on `/morph`) | **intake R5-7 standing F.W4 carry** (+ fr-DarkModeToggle L-16 fold) | The 12 re-derived by this seat from `computePreviewLevels` (:27-39 — candidate set of 12 literals; defaults 5/50 already members, Set stays 12) + the grid's import list (:87-93, no component import). Rides FM-3 (the cost of those 12) and the fr-EasingPicker:138 open question (whether they paint at all — SS-13). |
| **reader-2 missed #4** (`updateField` is dead public API and the file's only `any`) | **fr-AppHeader FR-AH-37** (F.W4 dead-export deletion batch) | `updateField` added to the batch: 2 tree-wide hits (declaration useMorphConfig.ts:64-67, return :92 — seat census), zero consumers; deleting it removes the `(config as any)[field]` cast at :66 for free — a dead function causing a live cast. |
| **reader-1 missed #4** (the `/morph` control's glass lift is DE-SPRUNG) | **fr-AppHeader FR-AH-11 (folded M-3 leg)** (F.W4 — one cascade decision for the override family, with FR-AH-30's cure caution) | New-site rider: MorphShapePreview.vue:97's unlayered scoped `transition` shorthand omits `translate`, so `cartoon-surface`'s spring-timed −1px lift (cards.css:33-49, seat-read; Tailwind 4.3.1 emits `@utility` into `@layer utilities`, SFC scoped styles are unlayered) fires instantly under the animated local scale — the identical mechanism at a site FR-AH-11 does not reach. INFO magnitude (the 1px snap is likely masked by the 1.2-3.6px scale); built-sheet layer check → SS-13. |
| **reader-1 missed #5** (bare `<button>` no `type`; motion literals vs shipped tokens) | **fr-DarkModeToggle N-3** (type="submit" default) + **N-5** (hardcoded `--duration-fast`/`--ease-standard` values) (F.W4 one-liners) | Second-site riders only: MorphShapePreview.vue:4 (no `type=` — seat grep) and :97/:111/:115 literals (`0.2s`/`0.15s`/`scale(1.02)`/`scale(0.98)`) — the F.W4 tickets written from fr-DarkModeToggle name one file; this consumer needs the identical edits. |

### MAJOR (2 booked)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **FM-2 · D-2 uplift arm ⊕ reader-1 missed #2 (cure collision)** | The glass-ui producer deletes BOTH ring registers — `grep -rnE '^\s*--(color-)?ring\s*:' src/ dist/` over glass-ui 8.0.0 (`074a3d0e`) is EMPTY (this seat's run); the deletion is stated in the producer's own words at src/theme/bridges.css:93-97 ("DELETED, clean break… the house focus accent is the `--focus-ring-color` token / the `.focus-ring` box-shadow utility"); the replacement register is populated (color-radius.css:161, dark-arm.css:133). Consequences, in escalating order: (1) six read sites across five files (seat census, exact: DarkModeToggle.vue:99 · style.css:140 — the D.W4.d block whose four beneficiary classes die with it · AppHeader.vue:189 · ImageUpload.vue:200 · GalleryCard.vue:222-223) lose their focus indication at F.W1 — IACVT on the `outline` SHORTHAND unsets to `outline-style: none`, total loss not degradation, with `:focus{outline:none}` already surrendering the UA fallback; (2) **banked fr-DarkModeToggle N-6's cure ("adopt `var(--ring)`") migrates onto a register that also dies** — the cure collision; (3) **banked fr-AppHeader killed-item-11 ("a naming normalisation, not an uplift risk") is TRUE at the pin and FALSE at the producer** — read verbatim by this seat; a banked kill is falsified by producer drift, the K-13 class ("parity with a banked row is not a cure") applied to a banked KILL; (4) the census break surface (CENSUS:100-104, seat-read) is composed exclusively of import/type-level breaks — a CSS-custom-property deletion is invisible to vue-tsc, the build, all 8 specs, and axe. | producer greps + bridges comment (seat); installed bridges.css:80 (seat); ring census (seat); fr-DarkModeToggle.md:92 + fr-AppHeader.md killed item 11 (seat-read) | **MAJOR (uplift-conditional; nothing is broken at the pin today) with a named escalation: BLOCKER the day F.W1 executes without a CSS-custom-property column.** Route: **F.W1** — the break surface gains a token-level column and a sweep of every glass token fourier reads (line-diffing is not a sweep: `--accent-red` moved lines and kept its value while `--ring` kept nothing); **F.W4 rider** — the N-6 edit and all six sites retarget to `--focus-ring-color` / the `.focus-ring` utility, ONE edit, never `var(--ring)`. Reader-1's BLOCKER held as dissent. Published-artifact state → SS-13 (rides the F.W1 re-baseline, as banked SR-2 already notes). |
| **FM-3 · reader-2 missed #1** | `HarmonicLevelGrid` recomputes all twelve 512-point paths from scratch on every re-render: `:d="getPath(level)"` (:68) inside `v-for` (:55) calls an un-memoized function (:129-132 — `interpolateAtHarmonicLevel` + `pointsToSvgPath`, no computed/cache in the file, seat-read) whose output depends only on `shape`+`level`. Cadence re-derived by this seat: `:active-level` = `nearestLevel(...)` over the 10-level table takes 7 distinct values per settle sweep → ~14 re-renders × 12 cells ≈ **168 rebuilds of identical ~57 K-char strings (~9.5 MB transient text + 513 allocations each) per toggle** — the paths change only when `shape` flips (once), so ~156 of the 168 are pure waste; and the level sliders write through FourierMorphDemo :64-65 with NO `isAnimating` guard (unlike the `@select` path, :138), so every slider step-change forces 12 more rebuilds (~684 KB) on the input path. All three axes opened this file (L-03 diffed it, L-13 counted it, D-10 read its siblings); none costed it. Registry: no fr-HarmonicLevelGrid row; banked N-12 scopes the churn to the single header glyph. | HarmonicLevelGrid.vue :55/:68/:129-132 + FourierMorphDemo :61/:64-65/:115-120 + svg-fourier `nearestLevel` (all seat-read) | **MAJOR on magnitude (SC-1: reader-2's "N-12 books as MAJOR" anchor is false — N-12 is MINOR at its home; this row stands without it). F.W4** — `computed` map keyed on (shape, levels) or per-cell memo; one edit, kills both the toggle-path and slider-path waste. Dropped-frame witness → SS-13 (pair with the FR-AH-1 boot trace). |

### MINOR (13 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **FM-4 · reader-1 missed #1 (DEMOTED MAJOR→MINOR with rationale)** | The `/morph` phase chips fail §1.4.3 AA in light mode — `.info-chip.morph` **3.42:1**, `.settle-out/.settle-in` **3.89:1** against their own 12% color-mix plates (MorphShapePreview.vue:165-174, seat-read; figures re-derived by this seat's own node run to the hundredth; dark arms pass 5.73/4.93; sizes below the large-text thresholds at the true 1.125rem root — SC-3). DEMOTION RATIONALE (seat's own exposure analysis, which no axis or reader ran): the failing arms are **phase-transient** — they paint only while `:class="phase"` is a non-idle phase (150/50/150 ms default), while the persistent idle chip renders `--foreground` on muted and passes; capability survives (the FR-AH-31/M-2 demotion family). AGGRAVATORS recorded: durations are user-raisable to 800 ms each (a legible 2.4 s failing readout), the route exists to be stared at, the repo hand-remediated a LESS severe 3.54:1 at D.W4.d (style.css:113-127, seat-read), and axe cannot see it (`/morph` never scanned). Reader-1's MAJOR preserved as dissent. | **F.W4** — the pure-CSS darken (the `--viz-amber` precedent) is available now, needs no palette sequencing; rendered-surface identity → SS-13. |
| **FM-5 · D-8/L-09** | `withDefaults` guarantees `strokeColor` is never undefined, so `:style="{color: strokeColor}"` (:6) ALWAYS emits an inline style — no author stylesheet (hover, `.dark`, `@media`, consumer rule) can recolour the stroke without `!important`. Consequence verified in-tree: hover/active/disabled feedback at both hosts is confined to the frame, never the figure; a disabled `/morph` control is visually identical to enabled but for `cursor: wait` (:118-120). | **F.W4** — `--fourier-morph-color` custom property with the prop as fallback (D-8's shape): restores CSS reach, keeps the prop API. |
| **FM-6 · D-9/L-08 ⊕ D-12** | No proportion contract: `strokeWidth` is a user-space ratio (no `vector-effect="non-scaling-stroke"`, no doc line) — 14/200 = 7.00% at the header vs 4.5/200 = 2.25% on `/morph` = **3.11× relative-weight incoherence** from an identical viewBox, plus a 1.5× painted swing within `/morph`'s own breakpoint from one unchanged prop; and `strokeWidth?: number` (:26) forbids the tokenisation the spread most wants (SVG accepts string). The absolute-px column is corrected (K-14): `cartoon-surface`'s 2px border under border-box makes the content boxes 96/144 px, strokes 2.16/3.24 px — the ratios are invariant. | **F.W4** — publish the size↔weight contract (or `vector-effect`) + widen to `number \| string`. Device-px witness → SS-13. |
| **FM-7 · C-6/L-10** | `strokeColor: "var(--accent-red)"` (:30) is a fallback-less `var()` on a third-party token carried as a JS string default — token not fourier-local (seat-concordant greps), arrives via `style.css:3`; an unresolved token makes `color` IACVT → inherit, silent. Latent (the token survives at producer 8.0.0 byte-identical — S-5, seat-re-verified at color-radius.css:319). In-repo precedent: the `.cartoon-card` resurrection shim (style.css:105-111, seat-read). | **F.W4** — `var(--accent-red, currentColor)`, one token. |
| **FM-8 · L-03 ⊕ reader-2 missed #2** | The 41-line primitive is bypassed at FOUR sites: HarmonicLevelGrid.vue:66-75 (identical element/attribute tree, same producers imported at :93) and FourierShapeExtractor.vue ×2 (:9-12/:36, :72-75/:82/:90 — seat census; routed at `/demo/shape-extractor`). FourierMorphSvg serves 2 of 6 svg+path sites in its own feature; every leaf-level cure reaches a third of its surface. The "indirection buys nothing" inference is KILLED (K-6) and must not travel — the grid's direct presentation-attribute `var()` is the UNVERIFIED idiom (fr-EasingPicker.md:138). | **F.W4** — adopt the leaf at the four bypass sites (which also multiplies FM-1/FM-5/FM-6's cures). Ruled MINOR at the 4-site scope (table above). |
| **FM-9 · D-10** | The live `phase` chip has no `aria-live`/`role="status"` (grep over morph/*.vue: aria only on the two slider labels + MorphPhaseConfig:27) — the demo's entire state readout is silent to AT, and the duplicated markup (:13-26/:30-43, byte-identical modulo wrapper) means the fix lands twice; AND the button binds `:disabled="isAnimating"` so activating it disables it — keyboard focus drops to `<body>` per interaction. The double-announce parenthetical is killed (K-7). | **F.W4** — one live region (on the rendered arm only), and prefer `aria-disabled` or a guard over `:disabled` for the busy window. Reader-2's arguably-MAJOR note on the focus-theft half recorded — it compounds FM-1 on the same control. |
| **FM-10 · L-11** | Nothing anywhere asserts anything about this component's output: no vitest (package.json scripts/devDeps, seat-concordant), the only `/morph` e2e is the capture harness whose sole expect is the overflow gate (visual-baseline.spec.ts:35/:65-68 — seat spot-read), and paper-performance's toggle click asserts theme colours only (banked K-1's "incidental coverage" framing correctly adopted by reader-2). A blank path, a wrong stroke, and L-01's discarded reset all pass CI. | **F.W4** — assertion rider on whichever F.W4 edit lands first; the "29 Playwright tests" census cell corrected to 8 spec files (concordant with banked). |
| **FM-11 · L-05 (DEMOTED MAJOR→MINOR, table above)** | `lib/scheduler.ts:13-15` states the morph render loop is "already rAF-paced AND off-screen-gated (I.γ, stores/animation.ts)" — read verbatim by this seat; false of the morph clock, whose file imports no store, no observer, no visibility gate (:13-27, seat-read). A third ungated clock the census's reduced-gap book does not name; a false documented invariant a future wave would rely on. | **F.W4** — correct the comment + add the morph clock to the gating/PRM book in the same commit (pairs with the FR-AH-31 fold). MAJOR dissent (axis + reader-1) recorded. |
| **FM-12 · reader-2 missed #3** | Banked N-1's one-line cure is incomplete: `setShape` (:89-95) and `setLevel` (:98-104) reset `phase`/`harmonicLevel`/`currentPoints` but NOT `morphProgress` (:211 writes 1, nothing writes it back — seat-read), so after any Reset or grid-select the composable reports `phase:"idle", morphProgress:1` — a terminal progress for a morph that no longer relates to the display, and DarkModeToggle's non-idle colour branch keys off a value with two meanings. The FR-AH-24 cure-completeness precedent applies. | **F.W4** — rider ON banked N-1's cure: reset `morphProgress` in `setShape`/`setLevel` too, not only at morph start. |
| **FM-13 · reader-2 missed #5** | `morphTo` snapshots the whole config ONCE (:151-160, seat-read) but nothing disables the parameter controls mid-flight: `MorphPhaseConfig` receives no `disabled` prop (FourierMorphDemo :26-54, seat-read) and the level sliders write straight through (:64-65, no guard — unlike `@select`'s early-return :138), then `syncWith` → `updateConfig` mutates config the in-flight morph has already read past. Observable: the `{{ totalMs }}ms` chip and the `is-bound` highlight move while the running animation does not — the guard protects the trigger, not the parameters. | **F.W4** — either extend the `isAnimating` guard to the parameter surface or re-read config per phase; one decision, stated. |
| **FM-14 · reader-1 missed #3** | `.info-chip` (MorphShapePreview.vue:147-163 + tinted arms, instantiated ×8) is an unbooked bespoke re-implementation of a producer primitive live at the pin and imported by **7** sibling files (seat census — the census's metric-badge ×7 exact; reader-1's 5-file list incomplete, SC-4); two of the four chips are exact `value`+`unit` MetricBadge shapes. CURE CAUTION (the FR-AH-30 law — cure adjudicated separately): `./metric-badge` is IN the F.W1 break surface (CENSUS:102) and my producer probe confirms `./metric-badge`/`./icon-chip`/`./toggle-chip` GONE at 8.0.0 while `./badge`/`./chip` survive — adopting MetricBadge here would widen the break surface to 8 files; the durable target is `./chip`. | **F.W3/W4** — bespoke-shadow list extension (the fr-AdminFlaggedPanel FR-AFP-61 treatment), target `./chip` post-uplift; do NOT adopt `./metric-badge` pre-F.W1. |
| **FM-15 · D-11 (first leg)** | The D.W4.d focus-ring sweep enumerated four classes by hand (style.css:136-139, seat-read) and `.morph-button` — the primary control of `/morph` — is not among them and declares no `:focus-visible` of its own. Today benign (the UA ring draws: `cartoon-surface` carries no outline declaration — seat-read whole; falsifier held), but inconsistent with the house 2px+2px ring — and FM-2 INVERTS it: post-uplift the four enumerated beneficiaries lose their rings while the omission keeps its UA default. | **F.W4** — add `.morph-button` to the ring set in the SAME edit that retargets the set to `--focus-ring-color` (FM-2's rider), so the sweep and its omission are cured together. |
| **FM-16 · L-15** | MorphShapePreview renders the four info-chips TWICE as eight hand-written divs toggled by `display` media queries (:13-26 vs :30-43, byte-identical modulo wrapper — seat-diffed) — a manually unrolled loop invisible to R6-5's `NATIVE_TEMPLATE_LOOP` cure, and the reason FM-9's fix must land twice. The per-frame cost arithmetic is KILLED (K-12): of the four bindings only the rounded `harmonicLevel` is near-tick-cadence and `phase` changes 4×/toggle; `shapeName`/`totalMs` don't move mid-morph (FourierMorphDemo :13-18, seat-read). | **F.W4** — one block with responsive flex-direction, or one `v-for` over a 4-tuple. |

### INFO (3 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **FM-17 · D-13/L-07/C-8 (⊕ D-12's asymmetry context)** | Two of three defaults are dead at every call site (viewBox 0/2, strokeWidth 0/2 — call-site census 4 lines, thrice-concordant), and the `"0 0 200 200"` literal lives at **6 sites / 5 files** (FourierMorphSvg:29 · DarkModeToggle:11 · MorphShapePreview:8 · HarmonicLevelGrid:66 · FourierShapeExtractor:12+:75 — seat census; D-13's "three files" killed, K-13). The one default documenting a real decision (the asset grid) is restated everywhere instead of inherited. | **F.W4** — dies inside FM-6/FM-8's contract work + the C-3/C-8 F.W0 asset-convention carry. |
| **FM-18 · L-14** | `overflow: visible` (:39) on hit-testable painted stroke with no `pointer-events: none` at either consumer — LATENT: nothing overflows today. Ratified at INFO on the axis's own fired falsifier, which reader-2 then survived at a strictly stronger instrument (the Catmull-Rom control-hull sweep adds nothing: moon hull y ≤ 183.04, +7 half-stroke = 190.04 < 200). The all-level-envelope vs level-50 attribution cell is in the drift bundle (K-16). | No action; the ~10-unit margin is recorded for any future wider-stroke consumer. |
| **FM-19 · C-13 (unbanked half) ⊕ C-11/C-12 declared negatives** | The shape assets are frozen decomposition output with NO in-tree regeneration pipeline (generator absent; `git log` shows the pipeline once existed) and five of seven JSONs in the directory are orphans (~1.358 MB, zero importers). The declared negatives are recorded with the positives: zero module-level consumption (C-11) and R6-8 operation↔client coupling UNREACHABLE from this chain (C-12) — both verified. | **F.W0** — restore or document the pipeline; adopt the 2026-06-16 audit's golden-file diff; disposition the orphans. Drift-guard half already folded (N-14/N-15). |

## Killed-claims register — 16 entries

| # | killed claim | refutation (adjudicated) | seat instrument |
|---|---|---|---|
| **K-1** | **D-5** — "the app-wide toggle PROVABLY renders blank on first paint" (MAJOR) | Commit ≠ paint: `onMounted` runs in the mount task before the browser's rendering steps; the reactive write flushes on the pre-paint microtask checkpoint. The L axis withdrew the identical claim under its own falsifier (challenge-L §D-5) and the registry had already bounded it to INFO (banked fr-DarkModeToggle **I-4**). The D axis's r2 revived, at MAJOR, a claim its sibling axis had killed and the registry had ruled — the folder contained its own refutation, unreconciled. Survivors: the type gap (banked N-15), the loading/error halves (banked FR-AH-8, M-7). Both readers' kills RATIFIED. | registry + L §D read |
| **K-2** | **D-7 step 2** — "the second click's `stopAnim()` calls `currentAnim.stop()`… resolves the pending play(), pipeline A advances" | Cannot occur in the only reachable window: `currentAnim` is first assigned at :171, AFTER the :149 await both chains are suspended on — the second chain's `stopAnim()` is a no-op behind the :116 null guard. Real failure = assignment-overwrite orphaning animA (banked M-3), exactly as banked K-2 ruled against the sibling corpus's C-6. Second minting of a kill this registry has already issued. Both readers RATIFIED + this seat's own trace. | useFourierMorph.ts :146/:149/:169/:171 (seat trace) |
| **K-3** | **D · S-2** — "`overflow: visible` is precisely the fix… 3.5% of the box clipped on every edge" | Nothing is clipped, with or without the rule: worst excursion + 7-unit half-stroke = 190.04, inside 0-200 with ~10 units of headroom (both readers' concordant asset sweeps; L-14's fired falsifier published the same result inside the same corpus). The "3.5%" is a per-edge stroke-halfwidth ratio mistaken for a clip. Superlative re-granted only as future-proofing. | readers' concordant measurements + L §D-3 |
| **K-4** | **D-6 contrast column** — "light→dark … 7.57:1" / "dark→light … 2.53:1" | TRANSPOSED — each row computed against the OTHER row's stroke. This seat's own node run: MOON-on-dark = **7.49** (the light→dark frame), SUN-on-light = **2.50** (the dark→light frame); the challenge's 7.57/2.53 are SUN-on-dark/MOON-on-light — the pair its own table says is NOT rendered. Conclusion survives at 2.50. | seat node run |
| **K-5** | **D-4 parenthetical** — "light is the default arm (`index.html:22-32` defaults to light absent a stored preference)" | The bootstrap defaults to the OS preference: `if (saved === "dark" \|\| (!saved && matchMedia("(prefers-color-scheme: dark)").matches))` — seat-read verbatim at index.html:21-32. A first-time dark-OS visitor boots dark. The 2.50 figure and the light-majority prose survive; the mechanism dies. Concordant with banked fr-App MG-γ / the darkModeSyncScript facts. | index.html read |
| **K-6** | **L-03 inference** — "[the grid's direct `stroke=var()`] demonstrates that L-09's indirection buys nothing" | Inverted three ways: banked fr-EasingPicker.md:138 books presentation-attribute `var()` substitution as an OPEN invisible-preview risk and names "the twin uses the safe `currentColor` idiom" as the good precedent; the same corpus's D S-1 (inherited `color` degrades survivably) and C S-3 (token stays in the cascade with the right arm applied) both reach the opposite conclusion and are right. Must not travel into a ticket that deletes the indirection. | fr-EasingPicker.md:138 (seat-read) + corpus |
| **K-7** | **D-10 parenthetical** — "a naive aria-live on both would double-announce (both arms are in the DOM…)" — AND reader-2's confirmation carrying it | `display: none` removes the subtree from the accessibility tree; a live region inside it does not announce — defined behaviour. Exactly one arm renders at any viewport (:124-145, seat-read). Announces once. Reader-1's kill RATIFIED; reader-2's repetition inside a confirmation → SC-2. The substantive halves survive (FM-9/FM-16). | MorphShapePreview :124-145 read |
| **K-8** | **C-2 byte figures** — 57,027 / 56,571 chars | The replay dropped the closed-path `" Z"` terminator (svg-fourier.ts:72 `return d + " Z"`): true figures **57,029 / 56,573** — banked N-12 concurs (twice-reproduced). Trivial in magnitude; the "measured, not estimated" replay did not run the function it claimed to replay. Reader-2's kill RATIFIED. | svg-fourier :72 + banked N-12 |
| **K-9** | **C-2 precision premise** — "rendered into a 5rem box… ~0.4 px per user unit" | `5rem` is the `var()` FALLBACK and has never rendered (sole consumer sets 2.5/2.75rem — banked N-10, and the same corpus's D-9 table has 40 px right: a corpus-internal contradiction on one line). Real quantum ~0.20-0.22 px/unit — the over-precision is TWICE as severe as claimed; direction unchanged. | banked N-10 + AppHeader :242/:256 (banked seat-read) |
| **K-10** | **C-3 drift figure** — "~5.8 px of horizontal drift" | Same dead-`5rem` premise: at the rendered 40/44 px the 14.49-unit offset paints ~2.9-3.2 px — banked M-6 carries the correct "~3 px diagonal at 44 px". Defect survives at half the amplitude. | banked M-6 |
| **K-11** | **C-10 premise** — "the JSON does structurally satisfy [FourierPathData]… the casts are unnecessary today" | Key-presence ≠ type shape: TS never infers tuples from JSON modules; `domain: [number, number]` and `coefficient: [number, number]` vs widened `number[]` make SOME assertion mandatory — banked N-14 ruled precisely this ("the cast is FORCED"). The surviving defect is `any`'s over-breadth, which is N-14's own cure text. Reader-2's kill RATIFIED. | banked N-14 + svg-fourier interface |
| **K-12** | **L-15 cost claim** — "all four [bindings] update on every morph tick — eight text nodes patched per frame" | Seat-read FourierMorphDemo :13-18: `phase` changes 4×/toggle; rounded `harmonicLevel` is the only near-tick binding; `shapeName` flips once per toggle; `totalMs` is config-keyed and static mid-morph. At most two per frame. Structural finding untouched (FM-16). Reader-2's kill RATIFIED. | FourierMorphDemo :13-18 read |
| **K-13** | **D-13 count** — "the magic constant now lives in three files" | Five files / six sites (seat census incl. FourierShapeExtractor :12/:75). The L axis had it right in the same corpus; two axes disagree on a number either could have grepped. Reader-2's kill RATIFIED. | seat grep |
| **K-14** | **D-9 px column** — content boxes "100 px"/"148 px", strokes 2.25/3.33 px | `cartoon-surface` adds a 2px border (cards.css:34, seat-read) under border-box: 96/144 px, strokes 2.16/3.24 px. The 3.11× and 1.5× ratios are invariant (96×1.5=144 exactly). Presented as "worked from the tree", worked from a border-box the tree does not use. Reader-2's kill RATIFIED. | cards.css read + arithmetic |
| **K-15** | The corpus-wide **"glass-ui 7.0.0 (producer latest)"** framing + S-5's `:321` anchor | Producer is **8.0.0**, HEAD `074a3d0e` (seat pin) — banked SR-2/K-15 already ordered the folder-wide re-read. D-2's substance SURVIVES re-verification at 8.0.0 unchanged (this seat's greps); S-5's `--accent-red` anchor drifts to color-radius.css:**319**, values byte-identical, bridge survives. The stale-pin half of the very trap the D axis's own method note warns about. Both readers RATIFIED. | producer package.json/git + greps |
| **K-16** | **Citation-drift bundle** (killed as citable cells; substance unaffected — the D-19 re-resolve law) | Ratified from both readers + seat additions: D-3 `router:9-13` → **:15-17**; the D.W4.d ring block `:133-143` → comment :129-135 / selectors :136-139 / decls :140-142 (seat-read); `--viz-amber` carry `:114-131` → **:113-127**; `.cartoon-card` `:108-112` → **:107-111** (seat-read); L-06's `:144-147` → **:146-148**; L-01's handleReset `:176-178` → **:175-178** (seat-read); L-14/L §D-3's moon `y∈[36.48,183.04]` attributed to "the two shapes rendered" is the ALL-LEVEL envelope (level-50 rest shape is y∈[36.55,174.73] — banked M-6's figure); SEAT ADDITIONS: reader-2's "N-12 books as MAJOR" anchor (SC-1) and reader-1's "text-sm (14px)" at a 1.125rem root (SC-3); reader-1's 5-of-7 metric-badge enumeration (SC-4). | seat re-resolutions |

## Superlatives — ratified with provenance

1. **S-3 ⊕ S-5 — the default is better than what its consumer substitutes, and it survives the uplift byte-identical.** Re-derived by this seat's own node run: `var(--accent-red)` clears §1.4.11 in BOTH arms against the surfaces it actually paints on (4.62 light-card / 4.07 dark-card / 4.69 light-page) — so MorphShapePreview, which takes the default, is contrast-correct while DarkModeToggle, which overrides it with JS literals, is banked-MAJOR wrong (M-2). And the one glass token the leaf names survives 8.0.0 byte-identical (producer color-radius.css:319 / dark-arm.css:154 — seat-read; `--accent-pink` likewise), which is precisely the falsifier that makes FM-2's opposite finding a specific deletion rather than a misread reorganisation: the same instruments checked the sibling tokens and found them intact.
2. **S-1 ⊕ C-S-3 — `:style="{color}"` + `stroke="currentColor"` is the right primitive, three ways.** Token resolution stays IN the cascade with the correct arm applied (the exact inverse of `cssVarToHex`'s pull-into-JS mistake and its banked `#888888` family); the failure mode is survivable (`color` inherits — IACVT degrades to ambient text, the exact inverse of FM-2's non-inherited `outline` total loss); and banked fr-EasingPicker.md:138 independently names the alternative idiom as the risky one. Only L-03's inference dissents, and it is killed (K-6).
3. **THE LEAF ITSELF — three verified properties of the 41 lines.** Zero imports/hooks/refs/timers (leak-free by construction; the teardown burden correctly located one layer up where `onUnmounted` can see it); a genuinely tight per-frame surface (only `:d` + the colour move per tick; the four static path attributes hoist); zero `any` against six `any` sites in its surrounding chain — the one place the repo's only gate retains full information, and banked N-14 shows that is a design outcome (the casts elsewhere are forced), not luck. The correct props-down split is what lets a stateful 60fps consumer and a stateless preview share it free.
4. **The L axis's §D falsifier discipline — the corpus's own load-bearing corrective.** Five withdrawals, each naming its instrument, one INVERSION (the pending-promise theory read against the engine d.ts and inverted into L-01 — the truth banked at FR-AH-9). Two of the five withdrawals were correct rulings the D axis's r2 then RE-ASSERTED at higher weight (D-5 at MAJOR, the overflow clip as a superlative rationale) — §D is not bookkeeping, it is the folder refuting itself, and this record's K-1/K-3 simply enforce it.
5. **L-14 survived a strictly stronger test than the one it ran.** Reader-2 reproduced the asset sweep to the hundredth, then ran the instrument neither axis did — the Catmull-Rom CONTROL-hull overshoot — and it added nothing. "Nothing overflows today" now holds under a harder instrument than the one that produced it.
6. **C-5's disposition is the most disciplined judgement in the folder.** It identifies value.js as the obvious remedy, then declines to file it on two tree-supplied grounds (the eager-header would undo the documented keyframes lazy boundary; value.js's own R1 `parseCssColor("oklch()")` crash means the pinned surface would not survive the motivating input) and sequences the carry behind the parser repair. Recommending the right fix at the wrong time is the commonest cross-repo failure; this row refuses it in writing — and its five self-repo citations all land on the nose (rare enough that banked I-3 books the drift as a standing hazard).
7. **The D §5 method note — "this class of defect is invisible unless both pins are read" — is correct, and its own author proves it twice.** The same ring claim aimed at the installed pin is false (bridges.css:80, seat-read) and survives only at the producer; the axis wrote the law, then read a stale second pin (7.0.0 — K-15). The note is more valuable for the irony, not less: FM-2's F.W1 ask (a token-level break-surface column) is the operationalisation.
8. **The declared negatives.** L §C's R5-7 negative (zero v-for, zero native loops in the leaf) and C-12's R6-8 negative (no `lib/api.ts` edge anywhere in the chain — seat-concordant) are stated on the record before generalising. Declared negatives are what let this seat trust the positives, and they are the cheapest thing for an axis under scope pressure to drop.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **FM-1** — axe's `button-name` rule firing on `/morph` (the static facts are proven; the run is the witness).
2. **FM-2 publication state** — whether the published glass-ui 8.0.0 artifact matches the producer working tree read here (`074a3d0e`); rides the F.W1 re-baseline exactly as banked SR-2 notes, and the caveat cuts symmetrically at S-5. Banked N-6's rebuild probe (drop `AdminUserList.vue:248`, rebuild, grep for `--color-ring`) remains the browserless decider for the pin-side coupling.
3. **FM-3 jank** — whether ~168 rebuilds/toggle and the per-step slider churn drop frames on `/morph`; pair with the FR-AH-1 boot trace already queued.
4. **FM-4 surface identity** — `.demo-page` declares no background (seat-read to :200), so the chips sit over `--background` by inheritance; one `getComputedStyle` on the rendered stack confirms the plate arithmetic's substrate.
5. **fr-EasingPicker.md:138's open readback, re-raised here because this folder is where it bites** — `getComputedStyle(document.querySelector('.grid-svg path')).stroke` on `/morph`: if presentation-attribute `var()` does not substitute, twelve of L-13's fourteen instances are invisible and K-6's kill becomes safety-critical for any repair ticket.
6. **FM-6** — exact device-pixel strokes at both hosts/breakpoints (the ratios need no browser).
7. **Banked residue re-cited, not duplicated**: D-4/M-2 worst-case translucent-header backdrop (fr-DarkModeToggle SS-13 #1); M-3 cold double-click window width (#5); C-2/N-12 perceived jank; I-4 suspended-mount exposure; N-4/L-12 AX exposure of a nameless `<svg>` per engine.
8. **reader-1 missed #4** — perceptibility of the de-sprung 1px lift under the 0.15s scale, and the built-sheet `@layer` confirmation (`npm run build` + grep around `.morph-button`) — decidable without a browser.

## DISSENT — preserved, not ruled away

1. **Severity dissents on folds and bookings.** The L axis holds L-01 at BLOCKER against its banked FR-AH-9 home; both axes hold the PRM row at MAJOR against banked FR-AH-31's MINOR (that dissent is already banked twice and is now three readers deep); reader-1 holds FM-2 at BLOCKER (this record: MAJOR with a named BLOCKER-on-execution escalation); reader-1 holds FM-4 at MAJOR (demoted here on the phase-transience analysis — if the F.W4 cure does not land with the D.W4.d-style darken, the regrade question reopens at the user-raisable 2.4 s exposure); the D axis + reader-1 hold FM-11 at MAJOR (demoted on the N-5 family precedent).
2. **The D axis's r2 is the corpus's cautionary instrument.** Its re-derivation discipline was real (every figure this seat re-ran reproduced, or failed in a way its sibling axis had already published) — yet it revived two claims its own corpus had killed (D-5, the overflow clip) and shipped a transposed table whose numbers were internally accurate for the wrong cells. Measurement fidelity ≠ premise fidelity, third occurrence in this registry family.
3. **Reader discipline improved and should be recorded as such**: both readers ran registry sweeps, folded rather than re-booked, published their folds' banked homes, and each independently caught kills the other missed. The residual failures are the two premise-inheritance events (SC-1, SC-2) and one severity-anchor misquote — all inference, none arithmetic, matching the fleet pattern banked at fr-DarkModeToggle superlative 9.
4. **The FM-2 booking-locus question.** Reader-2's position — that a plan-level defect should live on the F.W1 break-surface row, not on a component apotheosis — is structurally right and is honoured by the routing (F.W1 owns the column; F.W4 owns the six-site retarget). It is booked HERE because this component's corpus is where the producer's deletion was first read against the banked kill it falsifies; the forming F.W1 spec should lift FM-2 whole and this record should then be its provenance, not its home.

## Closing verdict

**ADJUDICATED-DEFECTIVE-AT-THE-SEAMS, LEAF RATIFIED — the 41 lines are among the cleanest in the fourier registry (right primitive, right split, contrast-correct and uplift-stable default, zero `any`) and every heavy defect lives in what surrounds them**: 1 new BLOCKER (FM-1 — the nameless `/morph` control, Level A on a nav-visible route axe never visits); 2 MAJORs booked (FM-2 — the producer's ring-register deletion that falsifies a banked kill, strands a banked cure, and is invisible to an all-import/type break surface, MAJOR-with-named-escalation routed F.W1; FM-3 — the twelve-cell path-rebuild churn, MAJOR on magnitude with its false severity anchor killed); 13 MINORs and 3 INFOs, dominated by F.W4 with the asset-pipeline half on F.W0 and the bespoke-chip row on F.W3/W4 behind an explicit F.W1 caution; 18 identity folds honoured with genuinely new riders (the unguarded Reset trigger, the three-layer PRM-free host, the morphProgress cure-completion, the de-sprung lift's second site); 16 register kills of which four were decided by this seat's own node runs (the transposed contrast table above all) and one is a new seat kill of a severity anchor; 8 superlatives ratified, the highest being the inversion no banked row states — the component's default is better than what its app-wide consumer substitutes for it. Routings are M-25 agglomeration input for the forming X·F specs; nothing here stamps a wave.

— end of apotheosis. Original challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.
