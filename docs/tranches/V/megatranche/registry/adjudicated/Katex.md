# Katex — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · served model id: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: L-18 adjudicator (incredulity seat over two hostile re-readers; the apotheosis seat for component Katex).
**Date**: 2026-08-05 · **HEAD**: `b9955f9c` · branch `tranche-u` · **Component**: `demo/scenes/about/katex/` (Katex.vue 49 lines + index.ts barrel) + its 64 call sites in `assets/docs/*.md`, its box model in `demo/scenes/about/markdown/Markdown.vue`, and its vendor supply chain.
**Corpus (r2 governs)**: `audit/components/Katex/challenge-D-design.md` (D-1..D-17) · `challenge-C-implementation.md` (K-1..K-12) · `challenge-L-library.md` (L-1..L-12), with committed `frames/`, `probes/`, `evidence/`.
**Re-readers adjudicated**: `L-18-hostile-reread-Katex-design-and-usage` · `L18-katex-library-and-code` — both Fable-class hostile re-reads of the full challenge corpus. Both were attacked before either was adopted: every cross-reader conflict and every load-bearing verdict was re-derived against the tree with this seat's own instruments.
**Method**: the two re-readers disagree at six verdict-level points (L-11's ground of death, D-1's severity, D-2/K-2's severity, K-1's severity, L-5's severity, L-9's fate) and each shipped kills and missed-findings the other did not. All six were ruled below with fresh evidence. One evidence class sits between static and live and was used deliberately: the committed, dated PNG frames are readable artifacts, and this seat READ `frames/D-mobile-390.png` directly — four adjudications below rest partly on that witness and say so.
**Arbiter's own instruments this session (all static + committed-artifact reads; zero browser tooling per law; zero writes outside this file; no builds run)**: whole-file read of Katex.vue (49 lines — :2 root div, :8-19 AB-1 comment, :20 import, :22 default, :33 throwOnError, :37-38 the `clip` mis-statement, :41 output, :48 watch); Markdown.vue :104-109/:121/:163-168/:194/:249-253/:286-306/:389 all verbatim at HEAD; vendored `katex.min.css` greps — `annotation` rules **0**, `.katex-mathml{…clip-path:inset(50%)…}` verbatim, **20× `font-display:block`**, `.katex{font:normal 1.21em …;line-height:1.2;…}`, `.katex-display>.katex{…white-space:nowrap}`, genuine global `body{counter-reset:katexEqnNo mmlEqnNo}` (the `position:relative` hit is `.accent-body` tail), `direction:` only flex; corpus census — `<Katex` **64**, `:display-mode="false"` **27**, identical line-2 import **11/11**, inline `\text{}` **21**, punctuation-adjacent `/>[.,]` sites **4** (hwb.md:77 ×2, lab.md:74, hex.md:62), `class="…"` in assets/docs **0**, bare `<script setup>` no `lang`; dual-voice spot-verification at rgb.md:45-46, hsl.md:42-43, lab.md:49-56 (loose-list blank-line structure read directly); package.json:90 `"@types/katex": "^0.16.8"` / :104 `"katex": "^0.16.47"`; installed katex package.json — `types: types/katex.d.ts` (file exists), exports = `.` + **five contrib subpaths incl. `./contrib/copy-tex`** + `./*`; dist sizes — katex.min.js **272,537 B**, katex.mjs **601,155 B**, katex.min.css **23,827 B**, fonts **1.1 MB / 60 files**; tsconfig.demo.json:57 `include: ["demo/", "src/vite-env.d.ts"]`; eslint.config.js `**/*.md` ignore + `no-unused-vars: off` (:71, :185) + zero `noUnusedLocals/noUnusedParameters` in any tsconfig; foundation.css :75 marker / :91-92 @source pair / :458-462 φ ladder; vite.config.ts :68-76 (the false `@src` comment verbatim) / :161-163 (`Vue({include:[/\.vue$/,/\.md$/]})`, `Markdown({})`) / vendor-katex `codeSplitting` group verbatim; plugins/vite-defer-glass-fonts.ts:72 dead `demo/@` guard; plugins/vite-source-export.ts:14-21; installed FadingScroll dist — `tabindex: "0"` unconditional, `role: o.value ? "region" : void 0`; EasingSpecimenStrip.vue:84-89 (bare FadingScroll + inner `role="group" aria-label`); viewSchema.ts:107 `right: "about"`; visual REPORT.json `katex|about` hits **0** against **92** shots (re-counted); `overscroll-behavior` in demo/ **0**; `katex` in test/+e2e/+demo/test/ **0**; demo/color-picker/index.html:2-11 (U-F58 RTL-readiness comment + `dir="ltr"`) and post-fix external `main.ts` entry (7-line bootstrap read whole); **`git show c4af0ef9`** — the commit exists (2026-07-29, `fix(demo/boot): expose the Vue mount as Vite's production entry`, its message quoting the exact 698-byte figure, +main.ts −inline-script); App.vue:50→:140 `<ErrorBoundary>` wrapping the whole two-pane grid; VISUAL-CONSTITUTION.md :78/:83/:99/:124/:154/:218 read verbatim; CARRY-LEDGER §F gh-pages empty-mount row located (:116); CARRY-CUT-LEDGER X-wave routing rows (CC-062 About→X-W6, CC-079 Markdown SCC→X-W8, X-W1/X-W2/X-W4/X-W10 charters read at head); adjudicated/Markdown.md and adjudicated/ColorSpaceSelector.md read whole for identity guards.
**Environment disclosure**: no dev server touched, no build run (write-nothing law) — the library re-reader's HEAD-`b9955f9c` rebuild receipts are adopted as corroboration only where this seat's static derivation already decides (L-11 is decided by the commit + tree shape, not the rebuild). The two frames read by this seat are committed dated evidence, never rewritten.
**Motion/PRM law**: `audit/codex-provenance/motion-quarantine.md` was swept — its sole `about` hit is a pane-name in prose; **no M-xx motion row touches this corpus; nothing is carried from FALSE rows.**

## Scoreboard

**33 adjudicated rows survive: 1 BLOCKER · 13 MAJOR · 13 MINOR · 6 INFO. 16 claims killed (including one re-reader kill killed back and one re-reader overbreadth corrected). 11 findings the three axes missed are admitted, all verified or witnessed by this seat. 12 residue items are UNPROVEN-NEEDS-LIVE and become visual-audit inputs.**

Blocker-weight survivor: **D-2/K-2** — display math clips at the card edge with zero affordance and zero keyboard path on the declared truth engine; this seat witnessed the shear directly in the committed 390px frame (three of four display blocks cut mid-glyph, one mid-bracket, no edge signal of any kind).

Headline correction to the corpus: its other BLOCKER (K-1) holds at a **MAJOR floor** — the structural premise is proven from source but the engine differential rests on one unreplicated live measurement — and its third BLOCKER (L-11) is **dead at HEAD** (`c4af0ef9`).

## Ruled cross-reader conflicts (the adjudication's actual work)

| point | design-and-usage | library-and-code | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L-11 ground of death** | Kill by SCOPE (relocate to the W44 §F carry; artifact gone, not re-checkable) | Kill as **DEAD AT HEAD** (commit `c4af0ef9`; rebuilt at `b9955f9c`: full asset graph incl. `vendor-katex-*.js` 258,876 B) | **Library reader wins; both kills upheld** | `git show c4af0ef9` verified: the commit exists (Jul 29), its message quotes the challenge's own 698-byte figure, the diff moves the inline bootstrap to `demo/color-picker/main.ts` (7 lines, read by this seat) and index.html now references the external entry. The design reader's relocation was correct law at its date; the carry itself is now DISCHARGED-BY-`c4af0ef9` — the §F row (CARRY-LEDGER:116) should be marked so at its next sweep. Either way the row was never a Katex defect: its own body says "not caused by Katex.vue". |
| **D-1 severity** | MAJOR (canon-anchored) | MINOR (canon anchors misapplied; RTL unreachable; the booked U-F58 register owns it) | **Library reader wins — MINOR** | This seat read the canon lines verbatim: VISUAL-CONSTITUTION.md:124 is a cell in the slider keyboard-axis table (arrow-key semantics, not glyph rendering); :154 names "CSS strings, hex, slugs, IDs and provenance" — mathematics is an extension by analogy, and D concedes it. index.html:2-11 is an explicit owner scope decision ("RTL mechanical readiness… Per-component physical-property → logical conversions are BOOKED follow-ups — docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md"). No code path sets `dir` anywhere. The rendering corruption is real (the design reader witnessed `*L`/`ₙY` in the committed rtl frame) and the cure is one attribute — a readiness-register row, not a shipped defect. |
| **D-2/K-2 severity** | BLOCKER (kept) | MAJOR ("pointer-reachable, nothing destroyed; the severe member is the keyboard half") | **Design reader wins — BLOCKER; the downgrade is killed** | This seat read `frames/D-mobile-390.png` itself: at 390px the default About render shows `a* = 500 [` sheared mid-bracket and two further display blocks cut mid-formula, with no fade, no scrollbar, no signal — and only the bracket row even *looks* truncated; a reader who does not already know the formula cannot tell the others are amputated. Undisclosed-gesture reachability does not restore content for the touch cohort, and for the keyboard cohort on the declared truth engine (S-22 WebKit) the content has NO path at all (structural absence proven from source: no tabindex/role/name on the port, `.katex-html` aria-hidden, no focusable descendant). "Loses content in a shipped, default configuration" is met. |
| **K-1 severity** | BLOCKER kept, with an honest caveat (differential not re-measured) | MAJOR floor (`ariaSnapshot` is not a screen-reader run; "hears it twice" is inference) | **Library reader wins — MAJOR floor, BLOCKER pending live** | The structural premise is proven by this seat: `<annotation>` carries raw TeX, zero `annotation` rules exist in the vendored sheet, `.katex-mathml` is `clip-path:inset(50%)` visually-hidden-but-exposed, and the app hides nothing. The escalating half (WebKit computes the annotation `display:block`) rests on one unreplicated dual-engine run of `probes/KTX-probe7.mjs` — methodology sound (spot-read by the library reader), evidence unreplicated. The one-rule cure is adopted regardless of the live outcome; severity follows the evidence, not the cure. |
| **L-5 severity** | MAJOR ("strongest structural finding") | MINOR (architectural half only; magnitudes killed; mitigations elided) | **Library reader wins — MINOR (rescoped)** | This seat verified the mitigations in source: the `vendor-katex` Rolldown `codeSplitting` group (vite.config.ts, verbatim) plus the two async boundaries already booked at the Markdown adjudication mean the typesetter is code-split and off the critical path; production ships 272,537 B min (not 588 kB — that is the unminified dev mjs, 601,155 B); katex's exports map has five contrib subpaths (this seat read it), so "single monolith, nothing droppable" is false as stated. The dual-path architecture observation SURVIVES (vite-source-export.ts:14-21's "No runtime formatting… libraries are needed" is the repo's own written idiom) and is the corpus's best formation input — see R15. The user-visible harm the MAJOR grade was reaching for lives in R14 (FOIT), booked separately. |
| **L-9 fate** | Kill ("outlier" evidence inverted: 2-of-2 members of demo/scenes/about/ are folder+barrel) | CONFIRMED MINOR (the barrel exists solely to service L-1's edge) | **Both, merged — MINOR on the library reader's ground; the "outlier" evidence arm is killed** | This seat confirms the barrel is one line servicing the eleven `assets/docs` directory-specifier imports. The convention comparison is dead (the design reader's census stands) but the row survives on its true ground: the barrel dies free the day L-1's edge dies. |

**Cure-level rulings inside agreed verdicts**: the C-axis gestalt cure for K-6 (`computed` + `v-html` + `throwOnError: import.meta.env.DEV`) is **REFUTED** — this seat verified App.vue:50→:140 wraps the ENTIRE two-pane grid in one `<ErrorBoundary>`, so a throw inside a render-evaluated computed escalates one malformed formula to a whole-app error plate in DEV, and forks DEV/PROD render paths — the exact divergence class this corpus indicts. The transposition half (computed render) remains right for K-3/K-4/K-8; the error policy must be the corpus gate (R6) + an explicit `typeof expression === "string"` guard + try/catch inside the render call. The D-axis gestalt cure's aria shape is REFUTED as written — see R13.

## Adjudicated defect roster

Every row: id · severity · claim · proof class · terminal disposition. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `b9955f9c`; re-resolve before executing any cure.

### BLOCKER

- **R1 · D-2 / K-2 (composite) — BLOCKER.** Display math clips at the card edge with zero affordance (no scrollbar, mask, fade, ::after, scrollbar-gutter — proven absent from Markdown.vue's whole sheet and `evidence/p5.json`) while `.katex-display>.katex{white-space:nowrap}` forbids wrapping (vendored sheet, this seat's grep); the port (Markdown.vue:301-306) carries no tabindex/role/name and `.katex-html` is aria-hidden, so on WebKit — the declared S-22 truth engine — the hidden content has no keyboard path (live leg: 90 presses, unreplicated; structural absence: proven). **Witnessed by this seat** in `frames/D-mobile-390.png`: three of four display blocks sheared mid-glyph at 390px, one mid-bracket, zero edge signal. Corpus magnitude (19-of-37 at 332px; 316/428/446px tables) stays live-only. → **X-W6** (the port cure: FadingScroll per the EasingSpecimenStrip shape — R13's rider is mandatory; scroll-margin/centering/overscroll riders R18/R19/R22 ride the same edit; the keyboard/name contract cross-cites X-W4).

### MAJOR

- **R2 · K-1 + D-4 (one defect, two sides) — MAJOR (floor; BLOCKER pending live re-run).** The un-hidden `<annotation encoding="application/x-tex">` node: nothing in the vendored sheet or the app hides it (this seat: 0 `annotation` rules; `.katex-mathml` is clip-path-hidden, which suppresses PAINT only), so it sits in the text/selection/find/AT trees. Engine-independent half (D-4, selection/copy/find triple-payload) proven structurally + `evidence/p5.json`; AT-name half (K-1) needs the live WebKit differential (`probes/KTX-probe7.mjs`, methodology verified sound). Cure: `.katex-mathml annotation{display:none}` (one rule, harmless under Chromium) — and note the vendor already ships `katex/contrib/copy-tex` (this seat read the exports map) as the first-class clean-copy answer for D-4. → **X-W4**.
- **R3 · D-11 / K-8 / L-3 / L-4 / K-4 (root-contract cluster) — MAJOR.** The root ignores its one prop (`class="inline-block"` both modes, Katex.vue:2 vs `displayMode = true` at :22); 100% of the display box model lives in a sibling's scoped `:has()` keyed on a Tailwind utility used as a structural API (Markdown.vue:286-289, :301-306 — read verbatim by this seat); the root is `<div>` even inline — flow content inside `<p>` at ≥8 sites (hex.md:62, hsl.md:75, kelvin.md:65, hwb.md:77, lab.md:74 re-verified; the loose-list floor question stays open, R-residue). Riders binding on the cure: the mx-1 rule MOVES into the component, never deletes (kill #9); the K-3 watch fix lands in the same commit (R17 — a mode-dependent root remounts an empty element no watcher refills); the aria shape follows R13. L-3's `inP` evidence arm is dead (kill #8) — the finding stands on the `outside` arm + this cluster. → **X-W6**.
- **R4 · K-6 — MAJOR.** Non-`ParseError` throws bypass `throwOnError:false` AFTER `katex.render` has wiped the node (`baseNode.textContent = ""` first — vendor source verified by both re-readers, node-repro'd by the library reader with the component's exact options: undefined/null → TypeError, deep nesting → RangeError). Katex.vue:29-44 has no guard, no try/catch. Cure: `typeof expression === "string"` boundary + try/catch inside `renderKatex` — NOT the refuted computed-throw (see cure-level ruling above). → **X-W6**.
- **R5 · K-5 / L-8 / D-5 / K-9 (error-surface cluster) — MAJOR (latent; all 64 expressions parse today).** `throwOnError:false` is a masking fallback with no console signal, rendering `span.katex-error` with hover-only `title`, no role, no accessible name, and hard-coded inline `#cc0000` (canon: VISUAL-CONSTITUTION.md:83, read verbatim — never color-only, role/name/status explicit). Corrections adopted: the citation is Katex.vue:**33** (L-8's :36 is a comment line); "un-overridable" is killed — `errorColor: "var(--destructive)"` is a one-line vendor option (kill #10). → **X-W6** (surface) + the corpus gate is R6's.
- **R6 · K-7 / L-2 / K-12 / D-16 / L-6 (vacuous-gate family) — MAJOR.** Zero tests (`katex` in test/+e2e/+demo/test/ = 0, re-run by this seat); 64 call sites outside the typecheck program (tsconfig.demo.json:57 excludes assets/), the lint program (eslint ignores `**/*.md`), and the style program (`class=` in assets/docs = 0 — the Tailwind third is inert, confirmed); no unused-import net (no `noUnusedLocals` anywhere; `no-unused-vars` off — the K-7 exact mutation genuinely keeps every gate green); zero visual coverage (REPORT.json `katex|about` = **0** against **92** shots, both re-counted; denominators 80/60 killed, #4). Structural cause verified: About is a pane (viewSchema.ts:107), unreachable by a route matrix. → **X-W1** (re-gate: the ~20-line corpus vitest that `renderToString`s all 64 extracted expressions; the visual oracle already names About at 390/1024/3440 ×2 schemes — X-W1:230; cross-book the Markdown-adjudication G-MD-8 committed-witness identity, one identity, not re-minted).
- **R7 · D-3 — MAJOR.** A fourth typographic voice and a rogue 1.21× rung: `.katex{font:normal 1.21em …;line-height:1.2}` — the shorthand imposes leading; 20 `@font-face` rules; on no rung of the φ ladder (foundation.css:458-462, read) and outside the closed matrix (VISUAL-CONSTITUTION.md:78, read verbatim: the matrix is closed; math has no row). → **X-W10** (the canon row, with D-17/R31) + X-W6 implementation echo.
- **R8 · D-6 — MAJOR.** Chromatic hierarchy inverted: headings and inline code recolored to the live specimen (Markdown.vue:163/:168/:252 + useMarkdownColors bindings), the formulas that ARE the argument left undifferentiated `--foreground` — nothing in the sheet touches `.katex` for color (verified). Compounds R12. → **X-W6**.
- **R9 · D-7 — MAJOR.** Display-block vertical rhythm off-ladder and proximity-inverted (intra-derivation gap ~1.47× the prose→math gap; four owners, one gap — the design re-reader re-derived the box arithmetic from source independently of the live measurement; ink-gap magnitudes stay live-only). → **X-W6**.
- **R10 · D-10 — MAJOR.** Edict-4 bypass: glass-ui ships FadingScroll (dist read by this seat: `tabindex:"0"` unconditional; `role:"region"` only when named), this repo consumes it for the identical problem (EasingSpecimenStrip.vue:84), and the math port hand-rolls `overflow-x-auto`. This row IS R1's cure vehicle. → **X-W6**.
- **R11 · L-1 (+ L-9 rider) — MAJOR.** The inverted content edge: 11/11 docs import a scene-private leaf (`../../demo/scenes/about/katex`), the scene imports the docs back. **Identity guard (anti-rename): this is one identity with the Markdown adjudication's L-3**, already CONFIRMED there with the cure built (content moves to `demo/scenes/about/content/`, line-2 import becomes `"../katex"` — G-MD-5); the barrel (L-9) dies free with it. Rider recorded (library re-reader's missed-cure, premise verified by this seat: `main.ts` now exists post-`c4af0ef9`, and vite compiles doc SFCs through the Vue compiler): one-line `app.component("Katex", Katex)` additionally deletes the 11× duplicated import header — compatible with, not competing with, the move. → **X-W8** (CC-079's measured Markdown SCC row owns cycle subtraction; cross-booked with Markdown-adj L-3/G-MD-5, one identity).
- **R12 · MISSED (design re-reader) — dual-voice same-referent — MAJOR, admitted.** The same variable typeset in two competing voices one line apart at ~19 of 27 inline sites: an accent Fira Code chip on the label line, undifferentiated 1.21em KaTeX serif on the definition line (spot-verified by this seat at rgb.md:45-46, hsl.md:42-43, lab.md:49-56 verbatim; both voices in ONE sentence at hex.md:62). The accent is spent on the label and withheld from the definition — R8 sharpened into a content-shape law. → **X-W10** (one referent, one voice) + X-W6 implementation.
- **R13 · MISSED (design re-reader) — the prescribed cures' aria shape — MAJOR as a cure-rider, admitted WITH a precision correction (this seat's own).** D's gestalt cure (`<FadingScroll … role="group" :aria-label>`): verified against the dist — supplying `ariaLabel` flips the primitive to `role="region"`, a LANDMARK, ×37 identically-named on one pane, AND the fallthrough `role="group"` fights the component's own binding. CONFIRMED. But the claim "BOTH cures ship 37 landmarks" is overbroad: C's cure puts `role="group"` on the component's own element — `group` is NOT a landmark role; that arm ships 37 identically-named groups (redundant-name noise, not landmark pollution). The correct shape is the repo's own precedent, verified: bare `<FadingScroll axis="x">` (tabindex supplied unconditionally, no manufactured region) + role/name on inner content only where a UNIQUE name exists (EasingSpecimenStrip.vue:84-89). → binding rider on R1/R10's X-W6 cure.
- **R14 · MISSED (design re-reader) — `font-display: block` ×20 — MAJOR, admitted.** Verified by this seat: 20 of 20 KaTeX `@font-face` rules declare `font-display:block` — every formula renders INVISIBLE during the block period on cold load of the pane, then reflows; inherited unexamined through the side-effect import (Katex.vue:20); not overridable without re-declaring the faces (descriptor, not property); lands on the route with the open Q14 LCP ruling. → **X-W2** (eager-payload/Q14 chain; the re-declaration or subsetting is asset-pipeline work).

### MINOR

- **R15 · L-5 (rescoped) — MINOR.** The runtime-vs-build-time dual path at architectural altitude (vite-source-export.ts:14-21 is the repo's own written build-time idiom; all 64 expressions are compile-time literals; `Markdown({})` already sits in the chain with its `markdownItSetup` hook unused). Magnitudes killed (#3); mitigations verified (vendor-katex split; two async boundaries; "one tap away" on mobile, not "not unconditional" — kill #12). The named cure candidate — render KaTeX at build time via markdown-it plugin options on the EXISTING `Markdown({})` — would structurally retire R4, R5's runtime surface, R14, and most of R3 in one architectural move. → **NO-WAVE-OWNER — flagged for the next formation boundary as a formation input** (no X wave owns the content build pipeline).
- **R16 · D-1 (ruled down) — MINOR.** RTL mirrors math (`*L`, `ₙY`, reversed cases-brace — witnessed by the design re-reader in the committed rtl frame; premises proven: no `dir` on the root, vendor sheet has only flex `direction:`). Unreachable in the shipped product (index.html `dir="ltr"`, no switcher — verified); canon anchors misapplied (see ruled conflicts). Cure: `dir="ltr"` on the KaTeX root (math never mirrors). → the standing **U-F58 register** (`docs/tranches/U/audit/w-a11y/rtl-logical-property-audit.md` booked follow-ups), echo at **X-W4** beside CC-046.
- **R17 · D-15 / K-3 / L-7 — MINOR today, with a MANDATORY rider.** `watch(() => expression, renderKatex)` (Katex.vue:48) tracks only `expression` while renderKatex reads `displayMode` (:32). Latent (all 64 sites pass literals — 27 `:display-mode="false"` verified). The rider is the sharp edge: it flips MAJOR the moment R3's cure gives the root a mode-dependent element — the fix (watch both, or render-as-computed) lands in the SAME commit. D-15's API rider adopted: the `displayMode = true` default inverts the boolean-attribute idiom (27 of 64 sites forced to write `:display-mode="false"`; `<Katex inline>` unwritable). → rider on **X-W6**.
- **R18 · D-12 — MINOR.** Zero `scroll-margin` on the math wrapper while headings carry `scroll-m-20`; a scrolled-to formula lands under the sticky pane header. **Witnessed by this seat**: in `frames/D-mobile-390.png` the cases block sits ghosted under the header title. → **X-W6** (rides R1's edit).
- **R19 · D-13 — MINOR.** Asymmetric `padding: var(--phi-1) 0 var(--phi-1) var(--phi-3)` (Markdown.vue:304, verbatim) displaces the centering axis 12.944px (arithmetic, no measurement needed); overflowing blocks abandon `text-align:center` entirely (nowrap + overflow, structural). → **X-W6**.
- **R20 · D-14 — MINOR.** `contain-intrinsic-size: auto 200px` (Markdown.vue:106-108, verbatim) over-reserves for 76-110px math blocks (heights live-only). The derived measurement-trap is worth more than the defect — see superlatives; the trap canon rides **X-W1**; the sizing rides **X-W6**.
- **R21a · MISSED (design re-reader) — `mx-1` orphaned punctuation — MINOR, admitted.** 4 sites verified by this seat (`/>[.,]` grep: hwb.md:77 ×2, lab.md:74, hex.md:62); **witnessed** in the 390 frame: `…κ = 24389/27 .` with the gap before the period. Binds R3's cure: inline spacing must be word-boundary-aware. → rider on **X-W6**.
- **R21b · MISSED (design re-reader) — `mx-1` off the φ ladder — MINOR, admitted.** 0.25rem = 4px, below the smallest rung (0.382rem — ladder read at foundation.css:458-462); the sole off-ladder spacing declaration in the sheet, governing the component under audit. → **X-W6**.
- **R22 · MISSED (design re-reader) — no `overscroll-behavior-x: contain` — MINOR, admitted.** Verified: 0 hits in all of demo/. 37 ports chain overscroll to the card scroller and (iOS) the back-gesture — precisely on the arm where clipping is worst. → rider on R1's **X-W6** cure.
- **R23 · MISSED (design re-reader) — `\text{}` prose in math serif — MINOR, admitted.** 21 of 27 inline formulas verified by this seat; "otherwise" in serif **witnessed** in the 390 frame. English words ("black", "diffuse white", "kelvin", "otherwise") typeset as mathematics at 1.21em inside Plus Jakarta Sans paragraphs. → **X-W10** (voice policy: `\text{}` re-mapped or the prose leaves the math) + X-W6.
- **R24 · MISSED (library re-reader) — `@types/katex` fully dead — MINOR, admitted and re-verified.** package.json:90 carries `@types/katex@^0.16.8` while katex 0.16.47 ships first-party types (`types/katex.d.ts` exists; declared in `types` and every exports condition — this seat read the installed package.json) and `moduleResolution: bundler` never consults `@types/`; 39 patch versions stale, wrong contract if it ever won. All three axes read the line and certified "clean". One-line deletion. → **X-W8**.
- **R25 · L-10 — MINOR.** vite.config.ts:70-74's `@src` justification comment is factually false at HEAD (read verbatim by this seat; `@src|?source` in assets/ = 0 — the Markdown adjudication's census). **One identity with Markdown-adj L-4**, whose cure already deletes plugin + alias + comment. → cross-book **Markdown-adj L-4** (X-W8 vicinity).
- **R26 · L-12.1 — MINOR.** `vite-defer-glass-fonts.ts:72` guards on dead `demo/@/styles/style.css` (verified verbatim; marker actually at foundation.css:75) — the transform can never fire. The library re-reader's sharpening is ADOPTED as the row's real weight: dev serves metric-compat fallback faces forever, so **every dev-server typography measurement in this challenge corpus was taken without the production font corpus** — an instrument caveat X-W1 must inherit. → **X-W8** (fix) + **X-W1** (instrument caveat).
- **R27 (counted under MINOR band close) — see INFO for L-12.2.**

### INFO

- **R28 · L-12.2 — INFO.** foundation.css:91 `@source "../../color-picker/**"` resolves to nonexistent `<repo>/color-picker` (verified); harmless only because :92 covers the tree. → **X-W8**.
- **R29 · MISSED (library re-reader) — Katex.vue:37-38 misnames its own mechanism — INFO, admitted and verified.** The comment says the MathML layer is hidden by "`.katex-mathml { clip; position: absolute }`"; the live rule is `clip-path:inset(50%)` — and the distinction (paint-only suppression, subtree fully in text/selection/AT trees) is exactly what produces R2. A comment that misnames its safety mechanism is how the next seat re-derives the wrong cure — and it already did: the D axis certified the layer "SOUND" by reasoning from this comment (kill #13). Correct the comment in R3's rewrite. → **X-W6**.
- **R30 · K-11 — INFO (confirmed negative).** Exactly one genuine global from the vendored sheet: `body{counter-reset:katexEqnNo mmlEqnNo}` (re-verified; the `body{position:relative}` hit is the `.accent-body` tail). AB-1's script-side import decision is correct and is NOT a finding. No action.
- **R31 · K-10 — INFO.** Asset figures re-verified exactly (272,537 / 23,827 / 1.1 MB / 60 files / 20 faces). "KEEP does not mean UNGATED" adopted — discharged through R6.
- **R32 · D-17 — INFO.** The canon has no jurisdiction for mathematics (VISUAL-CONSTITUTION.md:78 and :218 read verbatim: only the Conversion-paths graph and fenced code may escape the 66ch measure; display math — 64 instances, the largest content species on the route — is neither). → **X-W10** formation input, beside R7/R12/R23.
- **R33 · MISSED (design re-reader) — FadingScroll ×37 instance cost — INFO, admitted.** On engines without native scroll timelines: 37 ResizeObservers + 37 scroll listeners on the default right pane of the Q14 route; teardown verified complete (not a leak); interacts with R20's content-visibility skips. A sizing question the X-W6 cure must ANSWER, not a reason to refuse the cure. → rider on **X-W6**.

## Killed-claims register

1. **L-11 (challenge-L BLOCKER: gh-pages emits fonts and no application) — KILLED: DEAD AT HEAD.** `c4af0ef9` (2026-07-29) verified by this seat: commit exists, message quotes the challenge's own 698-byte figure, `demo/color-picker/main.ts` (7 lines) is the external entry, index.html carries the module reference. The library re-reader's HEAD-rebuild (122 assets incl. `vendor-katex-*.js`) adopted as corroboration. Never a Katex defect by the finding's own admission. The W44 §F carry (CARRY-LEDGER:116) is DISCHARGED-BY-`c4af0ef9` — record for that ledger's next sweep.
2. **D-1 as BLOCKER (and as MAJOR)** — killed to MINOR. RTL is not a shipped configuration (index.html `dir="ltr"` + U-F58 owner scope decision, read verbatim; zero dir-setting code paths); canon anchors :124/:154 verified misapplied (slider keyboard-axis cell; a class list that does not name math). The defect and its one-attribute cure survive in the readiness register.
3. **L-5's magnitudes** — killed. 588 kB is the unminified dev ESM (601,155 B measured by this seat); production minified is 272,537 B (~2.2× overstatement, more on the wire); "no tree-shakeable subpath / single monolith" is false as evidence (five contrib subpaths read from the installed exports map — including `copy-tex`, which is R2's D-4 cure). The architectural core survives as R15.
4. **D-16's "0 of 80" / L-6's "60 captures"** — killed arithmetic, twice wrong for one corpus: **92** shots (re-counted). Substance CONFIRMED (0 `katex|about` hits in the 4,678-line REPORT.json, re-run). A ruling quoting 80 or 60 cites a number that does not exist.
5. **D-8's universality ("unreachable without a pointer" absolutely)** — killed. The sibling axis measured Chromium reaching the scroller at Tab press 17 (keyboard-focusable-scrollers). The correct statement is the engine differential; WebKit is the truth engine, so the finding stands at R1 without the false universality.
6. **L-9's "outlier" evidence** — killed. 2 of 2 members of demo/scenes/about/ are folder+barrel; katex/ conforms to its directory's convention. The row survives on the barrel-services-the-edge ground only (R11 rider).
7. **The library re-reader's D-2/K-2 downgrade to MAJOR** — killed back (a kill of a kill). Ground: this seat's own read of the committed 390 frame — three display blocks sheared with no signal; undisclosed-gesture reachability is not content availability for the touch cohort and is nothing at all for the keyboard cohort on the truth engine. BLOCKER stands (see ruled conflicts).
8. **L-3's `inP` evidence arm** — killed. Every display-mode call site sits at column 0 surrounded by blank lines (design re-reader's 37-site census; spot-confirmed by this seat at lab.md), so markdown-it emits them as direct children — the `inP` measurement was manufactured by hand-relocating a node and proves nothing about the shipped tree. The finding survives via the `outside` arm and R3.
9. **D-11/K-4's unqualified deletion cure ("Markdown.vue:286-306 then deletes")** — killed. Lines 286-289 are the inline formula's horizontal spacing, not compensation; a `<span>` root still needs word-boundary-aware spacing at 27 sites (and 4 of them currently show the R21a gap — witnessed). The cure MOVES and FIXES the spacing; verbatim application ships a regression.
10. **D-5's "un-overridable without !important" framing** — corrected. `errorColor` is a first-class vendor option written verbatim into the inline style; `errorColor: "var(--destructive)"` tokenizes the ink in one line. The substance (color-only, no role/name/message) is untouched at R5.
11. **The C-axis gestalt K-6 cure (computed + v-html + DEV-only throw)** — REFUTED by this seat. App.vue:50→:140 verified: one ErrorBoundary wraps the entire two-pane grid; the cure escalates one malformed formula to a whole-app error plate in DEV and forks DEV/PROD render paths.
12. **L-5's mobile mitigation ("not unconditional")** — corrected to "deferred by one tap": the About pane is the picker view's right pane, one segmented-control tap away at 390 (control visible in the frame this seat read).
13. **The D axis's negative-proof "SOUND" on the MathML layer** — killed. A cross-axis contradiction the corpus never reconciled: D certified the exact node C proved defective, reasoning from the component's own comment — which misnames the hiding mechanism (R29). C's two-engine measurement wins on the merits; recorded as fleet-level evidence of citation-inheritance from source comments.
14. **K-1's "a VoiceOver user hears it twice" as measured fact** — demoted to inference (Playwright a11y-tree serialization is not a screen-reader run); with it the BLOCKER grade, to a MAJOR floor pending the live re-run (R2).
15. **L-8's citation `Katex.vue:36`** — corrected to :33 (verified; :36 is inside the comment block). Recorded so a repair seat editing by line number does not patch a comment.
16. **The design re-reader's "BOTH cures ship 37 identically-named ARIA landmarks"** — corrected for overbreadth (this seat's own): true for the FadingScroll+ariaLabel arm (`role:"region"` computed — landmark ×37, plus the role-merge fight); C's arm ships 37 identically-named GROUPS — redundant-name noise, not landmark pollution. The corrected finding stands as R13.

## Superlatives — with provenance

- **The AB-1 comment (Katex.vue:8-19)** — mechanism, cure, and the invariant that makes recurrence impossible ("A script-side import is unscopeable by construction"); independently re-audited by the C axis (K-11), both re-readers, and this seat. Now carries ONE attached defect: its sibling comment at :37-38 misnames `clip-path` as `clip` (R29) — the best artifact in the component contains the seed of its worst finding's mis-certification (kill #13). Provenance: all three axes + both re-readers + this seat's read.
- **The Markdown.vue:291-300 post-mortem** — the 10-line history of the first time this coupling broke, honest enough to indict its own construct; the reason three axes independently found L-4. Provenance: C/D/L; selectors re-verified by the design re-reader against real KaTeX output.
- **C's measurement trap (challenge-C:149-154)** — `content-visibility:auto` makes `scrollWidth` report the `contain-intrinsic-size` placeholder; the seat caught its own probes' false green and wrote the rule. Belongs in the gate-design canon (routed with R20 to X-W1), not just this ledger. Provenance: C axis; adopted by both re-readers; mechanism verified from Markdown.vue:104-109 by this seat.
- **K-1's dual-engine method** (`probes/KTX-probe7.mjs` + `KTX-cure.mjs`, BEFORE/AFTER in one harness) — the only construction under which the claim is decidable; survives hostile re-reading precisely because its method is re-runnable. Provenance: C axis; spot-read sound by the library re-reader.
- **K-7's exact-mutation argument** — strengthened under attack twice: the design re-reader closed the unused-import escape (no `noUnusedLocals` anywhere, `no-unused-vars` off — re-verified by this seat), the library re-reader re-ran the greps. "A gate that cannot distinguish 'renders 64 formulas' from 'renders 64 empty divs' is not a gate." Provenance: C axis + both re-readers + this seat.
- **glass-ui's FadingScroll** — unconditional `tabindex="0"`, `role="region"` only when named, native-timeline feature gate, complete teardown, tri-convention RTL scrollLeft normalizer. Verified in dist by the design re-reader and (role/tabindex computation) by this seat. It is exactly the primitive R1 should ride — used in the EasingSpecimenStrip shape, not the gestalt-cure shape.
- **`c4af0ef9` itself** — a fix commit whose message quotes the measured defect figure it cures (698 bytes), states its evidence (module count, entry size, typecheck, dual-viewport renders), and routes the residual warning honestly. The reason L-11's death is adjudicable statically. Provenance: the owner's tree; read by this seat.
- **The three axes' negative-proof discipline** — D's withdrawn `\frac`-rule hypothesis and forced-colors refusal; L's self-labeled hypothesis rows and self-falsified `sideEffects` theory; C's split of proven vs hypothesis arms inside single findings. Three voluntary disclosures of weakest evidence; the library re-reader's observation stands: L's clean-negative table pointed at exactly the right line (package.json:90) and asked the wrong question — which is what let R24 be found. Provenance: all three axes; adopted with attribution by both re-readers.

## UNPROVEN-NEEDS-LIVE residue — these become visual-audit inputs

1. **K-1's engine differential** (WebKit `annotation{display:block}` vs Chromium `none`) — re-run `probes/KTX-probe7.mjs` dual-engine; decides R2's escalation MAJOR→BLOCKER. The corpus's headline a11y claim rests on this one unreplicated measurement.
2. **WebKit Tab-unreachability** (90 presses) + **the arrow-key-scroll half of the cure** (`KTX-cure.mjs`: focus reached at press 10, but ArrowRight left scrollLeft 0 in headless WebKit) — needs real Safari; the cure may require an explicit keydown handler.
3. **The pixel tables**: 316/428/446px hidden; 19-of-37 overflowing at 332px (clipping itself is arbiter-witnessed from the committed frame; magnitudes are not).
4. **D-9's line-box ruptures** (101.5px `.katex` in a 28px-leaded paragraph at 200% zoom).
5. **D-7's rendered ink gaps** (46.49 vs 60.25px; ×2 at 200% zoom) — box arithmetic verified; sums live-only.
6. **D-13's rendered displacement** (12.94px derived; rendered confirmation live-only) and the two-axes adjacency claim.
7. **D-14's real block heights** (76-110px vs the 200px reservation) and **whether `content-visibility:auto` preserves a nested port's `scrollLeft` across skip/unskip** — if not, it compounds R1.
8. **D-5's contrast ratios** (2.37:1 dark / 5.45:1 light) — depend on live-resolved `--card`.
9. **D-3's loaded-face census** (5 of 20) — AND all dev-server typography measurements in this corpus carry R26's caveat (glass fonts never load in dev): re-measure against the BUILT artifact.
10. **K-4's div-in-p floor** (8 verified; the 19 loose-list sites may also wrap in `<p>` — markdown-it loose-list semantics vs C's `divInsideP:2` probe conflict, unresolved).
11. **Forced-colors** — no axis claims it; this seat concurs with the refusal.
12. **Post-`c4af0ef9` gh-pages artifact** — the library re-reader's rebuild receipts (122 assets, vendor-katex emitted, not in the modulepreload set) are adopted as its receipts; this seat ran no build (write-nothing). One fresh `npm run gh-pages` + artifact census at the next X-W1/X-W2 sitting closes it.

## Law compliance

- Sole write: this file. Product source untouched; no browser tooling; no builds. Dated evidence (challenge files, frames, probes, evidence JSON) read, never rewritten — this record sits beside them per E-1/E-3.
- Motion/PRM: no motion-quarantine.md row touches this corpus; no FALSE row carried; no M-xx motion id is cited by any adjudicated row above.
- Status verbs: every disposition above is **ADJUDICATED**. Nothing here is VERIFIED and nothing stamps a wave; X-wave routings are claims about surface ownership for the next formation's fold, made against CARRY-CUT-LEDGER at HEAD `b9955f9c`.
- Identity guards honored: R11 = Markdown-adj L-3 (one identity, G-MD-5); R25 = Markdown-adj L-4; R6's witness leg = G-MD-8. Nothing re-minted.

## Verdict

**ADJUDICATED — 33 rows survive hostile re-reading (1 BLOCKER · 13 MAJOR · 13 MINOR · 6 INFO), 16 claims killed, 11 missed findings admitted.** The true BLOCKER is the display-math port (clip + zero affordance + no keyboard path on the truth engine — arbiter-witnessed from committed frames); the corpus's headline a11y BLOCKER (K-1) holds at a MAJOR floor pending one live dual-engine re-run; its build BLOCKER (L-11) is dead at HEAD by `c4af0ef9` and its §F carry is discharged. The component's rewrite cluster and port cure route to **X-W6** (with binding riders R13/R17/R21a/R22), its a11y contract to **X-W4**, its gates to **X-W1**, its font/FOIT payload to **X-W2**, its graph rows to **X-W8** (cross-booked to the standing Markdown-adjudication identities), its canon rows to **X-W10**, and the one genuinely architectural question — build-time KaTeX on the repo's own `?source` idiom, which would retire four MAJOR rows at once — carries **NO-WAVE-OWNER** and is flagged for the next formation boundary.

— end of apotheosis. The challenger corpus and both hostile re-reader returns remain immutable beside this file per E-1/E-3.
