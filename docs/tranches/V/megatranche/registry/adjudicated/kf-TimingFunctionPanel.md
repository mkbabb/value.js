claude-fable-5

# kf-TimingFunctionPanel — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/components/instrument/transport/channel-controls/TimingFunctionPanel.vue` (166 L, worktree) — reader-1 ("design-and-usage": 18 confirmation cells · 10 kills · 4 misses · 9 superlatives) and reader-2 ("library-and-code": 24 confirmation cells · 5 kills · 5 misses · 7 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/TimingFunctionPanel/challenge-{D-design,C-consumption,L-library}.md`, 400/313/265 L, all opus-5[1m]).
**Method (incredulity)**: every reader-vs-reader head-on contradiction (the L-11/C-7 "never unmounted" absolute; D-4 killed-vs-confirmed; D-6 killed-vs-confirmed; the D-3/C-10/L-6 grades; D-2's booking), every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption. **Zero browser tooling** (law); product source read-only; the irreducibly visual residue rides to SS-13/KF.W9. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only + node-against-dist)**: TimingFunctionPanel.vue whole (166 L, worktree — anchors below are worktree coordinates); `git show origin/master:…TimingFunctionPanel.vue :138-165` (**the EE-02 css twin present at the frontier**, `{ fn: cubicBezierEasing(...pts), css: cubicBezierToString(...pts) }` + its rationale comment); `git show origin/master:…useTimingFunctionEditor.ts` (**`setAnimationTimingFunction(timingFunction, css?)` builds `{fn, css}` at `:109` and is called with `timingFunctionLiteralFor(key)` at `:170`** — the winning write carries the twin, closing a C-5×C-3 interaction gap neither reader closed); worktree useTimingFunctionEditor.ts `:45-70/:95-110/:120-170/:185-212` (the four literal-classifier computeds; the css-less worktree setter; the `TypeError` receiver `:141-145`-region; `onEasingLabelClick`'s `kind === "cubic-bezier"` early-return + the `[0,0,1,1]` fallback + the literal persist); ChannelControls.vue `:90-140` (**the `v-if="hasSurface('controls') && selectedControlSurface === 'controls'"` gate wrapping the whole ChannelOptions subtree — the L-11 decider** — and the `:inert="!keyframesActive"` cure with its five-line aria-hidden-focus rationale — the D-3-evidence decider); ControlsPaneWrapper.vue `:40-55` (per-channel **`v-show`** keyed by animation); animationOptionsStore.ts `:40-75` (`timingFunction: "ease-in-out"` · `steps: 100` · `controlPoints [0.2,0.65,0.6,1]` · `useStorage` with **no options** = deep); animationDescriptions.ts `:65-107` (`timingFunctionState` → `parseTimingFunction` + registry `easing()` on miss; `timingFunctionKind`; `isDetailTimingFunction` funnels through the same classifier); AnimationControlsGroup/useControlsKeyboardShortcuts.ts `:50-66` (Escape→`reset()` "Stop animation"; ArrowLeft/Right→scrub ∓0.01 `preventDefault:true`; Shift ±0.1; Home→0; End→1); glass-ui 7.0.0 dist read raw: `keyboard.js` (window keydown dispatcher; skip predicate `f(e)` = exactly `INPUT|TEXTAREA|SELECT|isContentEditable|.monaco-editor`; **`defaultPrevented` never consulted** — my grep = 0) and `easing.js` (`Ze` switches ArrowLeft/Right/Up/Down/Home/End → `preventDefault(); N(…)`; **`stopPropagation` = 0 hits file-wide** — my grep; `Ye` gates on instance-local pointer state; `setPointerCapture` on the svg); node probes against installed `@mkbabb/value.js@4.0.0`: `cubicBezierToString(0.431,0.008,0.58,1)` → `"cubic-bezier(0.43, 0.01, 0.58, 1.00)"` and `(0.175,0.885,0.32,1.275)` → `"…(0.17, 0.89, 0.32, 1.27)"` — **2-dp confirmed**; git coordinates: HEAD `8281638c` (dirty, 252 rows), frontier `origin/master 81a56990`, `HEAD..origin/master` = 41, not-ancestor; package.json at three coordinates (**HEAD: `optionalDependencies` `@mkbabb/glass-ui: "6.0.0"` · frontier `:77` exact devDep `"7.0.0"` = installed · worktree: block deleted, grep = 0**). Vendor-internal cells not re-derived here (the `We` write-through, `setHandle`'s `+a.toFixed(3)` + `"custom"` stamp, `initialPreset ?? "ease-out-back"`, the six-field model with **no `preset`**, the sole `mode` prop-watch, `min:1 max:12`) are adopted from the **banked** EasingSidebar seat's own pretty-print (kf-EasingSidebar.md §instruments) + both readers' convergent independent reads — three-way convergence, zero inheritance from the challengers alone.
**Substrate disclosure (SCH-1, re-run by this seat)**: checkout = the sacred stale `8281638c`, 41 behind the frontier, worktree dirty. On THIS corpus's blast surface exactly three cited files diverge from `origin/master`: **TimingFunctionPanel.vue** (worktree lacks the frontier's EE-02 css twin), **useTimingFunctionEditor.ts** (worktree setter takes no `css`), and **package.json** (worktree deletes the frontier's exact 7.0.0 pin). The baseline law applied throughout: the product baseline is the frontier; worktree-only mechanisms are killed, frontier-only defects are booked (KF-TFP-18).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file.**

## Scoreboard

**29 adjudicated roster rows — 2 BLOCKER · 9 MAJOR · 11 MINOR · 7 INFO**, of which **14 fold by reference or fold-extend banked identities (KF-ES-12 · KF-CO-17≡KF-ES-4 · KF-CO-40·N-10 · KF-CO-9 · KF-CO-5+KF-CO-46 · KF-ES-7 · KF-CO-16 · ACG M-1/M-4+KSM R-1 · KF-ES-18 · KF-ET-1 · KF-AV-16 · KF-ES-28 · KF-ES-22 · census SCH-1/F-1) — zero re-bookings**. **11 killed-claims register entries, all SUSTAINED** — chief among them the **frontier-baseline kills** (C-3's WAAPI-disqualification and L-3/C-14's phantom-dep both measured a locally-reverted worktree; this seat re-proved the frontier bytes itself, including the parent-write leg neither reader closed) and the **one-prop cure-kill** (the corpus-unanimous `:model-value` repair cannot seat a preset display — third sustaining of the KF-ES-4 cure-kill). **8 distinct missed rows ADMITTED** (4 from reader-1, 5 from reader-2, one identity shared) — the largest a source-decided MAJOR keyboard-collision third site. **7 adjudicator corrections against the readers**, including one reader-2 confirmation-cell absolute reversed at the bytes ("the panel is never unmounted" — a `v-if` sits two components up) and one D-axis superlative neither reader challenged, demoted (S-C's token-derived contrast over a composited surface). **10 superlative cells sustained.**

Blocker-weight survivors: **KF-TFP-1** (the self-triggering `:key` destroys the first authored drag and desyncs the canvas from the animation, on the dominant entry path) and **KF-TFP-2** (every post-edit reopen seats the editor on `ease-out-back` and its readout/copy export the wrong literal) — both frontier-durable (the EE-02 hunk does not touch the seeding stack), both cure-routed to the SS-1/SS-2 seat-composable spec under the banked split-seam cure-lock.

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L-11/C-7 mount scope** | KILLED the absolute: `ChannelControls.vue:98` `v-if` gates the subtree; surviving scope = per-channel `v-show` | Confirmed C-7 with "the panel is never unmounted… No `v-if` above the panel" | **Reader-1's KILL SUSTAINED; reader-2's absolute REVERSED — the KF-CO-16 fold survives at its banked scope** | My ChannelControls.vue read: the tabpanel div wrapping `<ChannelOptions>` carries `v-if="hasSurface('controls') && selectedControlSurface === 'controls'"` — every Controls→Keyframes/Timeline switch tears the picker down (a fact load-bearing elsewhere in the bank: KF-CO-4, and the KF-CO-15 correction). My ControlsPaneWrapper.vue:49 read: per-channel gating is **`v-show`** keyed by animation — so every channel whose controls surface is selected does hold a live `useEasingPicker`, which is precisely KF-CO-16's banked scope ("every inactive channel"), not "never unmounted". |
| **D-4** | KILLED the headline (overflow-hidden zeroes the 1fr minimum → CRUSH, right column never clipped); survivor folds to KF-ES-7 | CONFIRMED MAJOR (structural viewport-vs-container leg; pixel arithmetic self-marked unproven) | **RECONCILED — both partially right; the headline dies, the structural row survives as a KF-ES-7 FOLD (MAJOR, GLASS-OWNED)** | The crush branch is the bank's own resolution, ruled at the EasingSidebar seat from its pretty-print (`glass-card … overflow-hidden` canvas card zeroes the grid item's automatic minimum — kf-EasingSidebar.md KF-ES-7). D-4's "Preset/steps/term/readout/copy unreachable and unscrollable" is the wrong branch; D-4's own hedge is what ships. The producer defect (a viewport `lg:` governing a `38cqi` container-sized primitive inside a 376–488px rail) survives whole and is already relayed. |
| **D-6** | KILLED (inverted by the bank: KF-CO-42 rules the SIBLING wrong and names this panel's Button the donor); residue = position, MINOR | CONFIRMED MAJOR (all four divergence legs), noting KF-CO-42 covers the component/label legs | **Reader-1's KILL SUSTAINED on 3 of 4 legs; MINOR residue booked (KF-TFP-12)** | My grep of the bank: kf-ChannelOptions.md:199 KF-CO-42 (MAJOR) prescribes exactly this panel's `Button emphasis="quiet" icon-only` + `aria-label` for both sibling call sites, with KF-CO-27 (the `title` fall-through) and KF-CO-24 (the compact tap-floor exclusion) booking the sibling's other two legs. Charging this panel MAJOR for the shape the bank rules correct would be an inversion. Un-adjudicated residue: POSITION (title-left/dismiss-RIGHT vs back-LEFT) + glyph rung (icon-sm vs icon-md) — and D-8's own magnitude makes this panel's box the LARGER (40/60px vs 24/28px), inverting D-6's implicit direction. |
| **D-3 grade** | MAJOR (fold to KF-CO-5) | BLOCKER | **FOLD — the grade lives at banked KF-CO-5; reader-2's BLOCKER preserved as the escalation hook** | KF-CO-5 books BOTH collapsed rows at the host seat with the KF-CO-46 cure rider (bare `inert` incomplete; `content-visibility` incompatible with the `0fr↔1fr` morph). One identity, one grade home; SS-13's focus witness (banked residue #5 there) is the escalation trigger. |
| **D-2's booking** | Merged into D-1's terminal state; not booked separately | Booked as its own BLOCKER | **Reader-2 WINS — separate BLOCKER (KF-TFP-2)** | Distinct manifestation with its own reachability chain (my reads: the `cubic-bezier` early-return in `onEasingLabelClick` preserves the authored quad; the literal persist survives reload; the shipped default matches no preset) and its own harm class (wrong-literal export = the KF-ET-1 class). Mid-drag destruction and reopen-on-wrong-curve fail at different times for different users; one row would under-count the surface. |
| **C-10 grade** | MINOR ("the SHIPPED DEFAULT state") | INFO (masked by a strictly worse banked defect) | **INFO — reader-2's downgrade adopted; reader-1's MINOR as dissent** | The `undefined` arm is masked by the banked unguarded-reapply `TypeError` (kf-ChannelOptions L·B-4/L·I-2 — the throw fires before the wrong header can render), and the default-state exposure ("cubic-bézier" in the AT tree while the animation runs `ease-in-out`) is a consequence of KF-CO-5's mounted-collapsed row, graded there. What remains chargeable to THIS file alone is the missing self-guard — INFO. |
| **L-6 grade** | MINOR (leg 1 only) | MAJOR (both legs) | **SPLIT — MINOR for the constant coupling (KF-TFP-15); leg 2 relocated** | Leg 2 (store-anchored, not seed-anchored) is the enabling condition of KF-TFP-1's lock-in and KF-TFP-3's standing filter — booking it MAJOR here as well would double-count one mechanism across three rows. The unexported-`DEFAULT_BEZIER_PRESET` coupling stands alone at MINOR. |
| **D-9/C-7/L-8 grade** | MINOR | MAJOR | **FOLD — grade lives at banked KF-CO-16** (C·m-6's decline was already OVERRULED there; "two deletions + `v-if` the detail panel" is the banked disposition). |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md` §(a): W0 Substrate Settle · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**; producer defects are **GLASS-OWNED**, riding the standing glass-ui BH relay (owner edict 2026-07-12). Routings are bookings, not acts. Line anchors are worktree-at-`8281638c`; the frontier shifts post-`:144` anchors by ~5 lines (the EE-02 hunk) — re-resolve before any cure (D-19).

### BLOCKER

- **KF-TFP-1 · D-1 = C-1 = L-1 — the `:key` is derived from the state the handler writes: the first drag off any preset-matched quad remounts EasingPicker mid-gesture, kills the pointer capture, and teleports the canvas to `ease-out-back` while the store and the live animation hold the dragged quad.** Cycle closed in one file at my own read: `:143` writes `controlPoints` → `:92-99` `seedPreset` reads them → `:101` `pickerKey` → `:33` `:key`. Store deep-reactive (`useStorage`, no options — my read). Vendor: emits per `setHandle` with `+a.toFixed(3)` quantisation (any real drag breaks the 5e-4 `quadEq`); the rebuilt instance seeds `initialPreset ?? "ease-out-back"`; the panel's own `!seed` arm (`:121-128`) swallows the rebuild's echo, locking the desync in — vendor cells three-way convergent (both readers + the banked EasingSidebar pretty-print). Entry path DOMINANT: both readers independently reproduced the 29/29 `NAMED_EASING_BEZIER` ⊆ `bezierPresets` measurement (the L-17 probe), and `onEasingLabelClick`'s unnamed fallback writes `[0,0,1,1]` = `bezierPresets.linear` (my read) — every pencil entry arrives preset-matched. Identity guard: the MECHANISM is recorded in the bank as a rider inside the FORK row — KF-ES-12(b) ("the FIRST drag away from a seeded preset flips seedPreset → key → remount mid-drag") — this row is the **component-primary booking**; the fork identity stays KF-ES-12's; zero re-book of the fork. **BLOCKER (both readers; sustained).** → **NO-WAVE-OWNER** (SS-1/SS-2): the `useEasingPickerSeat` composable (KF-ES-12) under the KF-ES-4/KF-CO-17 **split-seam cure-lock** — the one-prop cure is dead (register #3). Visible half → SS-13.
- **KF-TFP-2 · D-2 — a custom stored curve opens the editor on someone else's curve: every reopen after any authored edit seats `ease-out-back`, the echo-swallow preserves the divergence permanently, and the vendor readout + copy affordance export the wrong literal.** Reachability at my own reads: `onEasingLabelClick` early-returns for `kind === "cubic-bezier"` WITHOUT rewriting `controlPoints`, and the persist seam stores the full literal — an authored quad survives reload; the shipped default `[0.2, 0.65, 0.6, 1]` (my read) matches zero of the 30 presets (both readers' probes) → `seedPreset === undefined` on the default state of every channel → the mount seeds `ease-out-back` → `:121-128` swallows the echo, preserving store AND divergence; the readout/copy derive from the picker's own points. The panel's own comment (`:28-31`) records the failure as accepted. Class-sibling: KF-ES-6 (the sidebar's rebuild-fallback wrong-seed, MAJOR) — graded BLOCKER here because the trigger is not a viewport crossing but EVERY post-edit reopen on the demo's primary authoring surface, and the wrong-literal export is the KF-ET-1 harm class. **BLOCKER (reader-2's booking adopted; ruled above).** → **NO-WAVE-OWNER**, same spec as KF-TFP-1: the `cubic-bezier` seed must carry the LIVE quad (KF-ES-6's cure sentence, verbatim).

### MAJOR

- **KF-TFP-3 · C-4 (+ L-6 leg 2) — `isSeedEcho`'s `!seed` arm is a STANDING, not one-shot, by-value filter: whenever the stored quad matches no preset, picking `ease-out-back` from the picker's own Preset Select is classified as a mount echo and discarded — picker and animation diverge permanently, silently.** `:120-128` runs on EVERY emission (`:134`); reachable from the SHIPPED DEFAULT state (the default quad matches no preset — probes above); the Preset Select renders in bezier mode regardless of `playback` (vendor cells convergent). The predicate is store-anchored rather than seed-anchored (L-6 leg 2, booked here as the enabling condition). Class banked: **KF-ES-1 kill #9 names exactly this arm** ("a hard-coded `ease-out-back` standing filter of the same class") — the sidebar's row stays KF-ES-1's; this books the panel instance at its own reach. **MAJOR (unanimous).** → **NO-WAVE-OWNER**: the echo predicate must compare LIVE state with provenance — one spec with KF-ES-1/KF-ES-12.
- **KF-TFP-4 · D-§0 = L-2 = C-2 — the load-bearing comment (`:25-31`, "glass-ui 4.0.1's modelValue is emit-only — no external write-through / points-in prop") is FALSE against the installed-and-frontier-declared 7.0.0, which ships a deep+immediate inbound applier with its own by-value echo suppressor — and the cure all three axes prescribe on that premise is KILLED.** Comment verbatim at my read; vendor applier three-way convergent; glass-ui unaliased in vite.config (both readers). = **KF-CO-17 ≡ KF-ES-4 identity — FOLD-EXTENDED, zero re-book.** **MAJOR (grade at the banked rows).** → fold to KF-CO-17/KF-ES-4: the **SPLIT seam** (model write-through for the steps arm; remount-or-producer-`preset`-seam for named-preset seating) + the GLASS-OWNED preset-seam rider already riding the BH relay.
- **KF-TFP-5 · C-5 = L-4 — the bezier arm writes `options.timingFunction` plus every frame, then emits, and the parent synchronously rebuilds and overwrites both: two easing constructions and two full `frames[]` walks per pointermove, one discarded — while the steps arm of the same handler performs no engine write at all.** Verified end-to-end at my reads: `:144-148`; ChannelOptions binds the emit to `updateTimingFunctionFromName`, which unconditionally reaches `setAnimationTimingFunction` for cubic-bezier, rebuilding from the array the panel wrote one line earlier. = **KF-CO-40·N-10 identity ("the drag path writes options+frames twice per pointermove — verified in both files") — FOLD.** **MAJOR.** → fold to the OPTIONS-UNIT: child owns the store, parent owns the engine — the steps arm is the correct shape.
- **KF-TFP-6 · D-7 = C-6 = L-15 — the stored `steps` default (100) is 8× outside the vendor authoring domain [1,12], forwarded raw on the unclamped initial-prop path, with no out-of-range state.** My reads: `defaultStepOptions = { steps: 100, … }`; `:36` binds raw; vendor clamp exists only on the model path (convergent). = **KF-CO-9 identity — FOLD.** **MAJOR.** → fold to KF-CO-9 (OPTIONS-UNIT); the 100-tread staircase, the "Steps (n) — 100" readout, and the mount-clamp leg → SS-13.
- **KF-TFP-7 · D-3 — the collapsed detail row keeps six-plus focusable controls (back Button; 2× `role="slider"` handles; Preset trigger; readout `<code tabindex="0">`; copy button; steps mode adds the Slider thumb + term trigger) in the tab order and the AT tree; arrow keys on a focused, invisible handle rewrite the animation's easing.** Host mechanism = class toggle only (`0fr` + `overflow:hidden` + `opacity:0` + `pointer-events:none` — none removes focusability). = **KF-CO-5 identity (both collapsed rows) — FOLD**, with the **KF-CO-46 cure rider** (bare `inert` is structurally incomplete; the sibling's `content-visibility` half is incompatible with the height morph). Evidence correction folded: the house cure IS written 30 lines from the mount — `ChannelControls.vue` `:inert="!keyframesActive"` + the aria-hidden-focus rationale (my read; register #5). Grade lives at the bank; reader-2's BLOCKER preserved as the SS-13 escalation hook. → fold to KF-CO-5/KF-CO-46 (OPTIONS-UNIT a11y leg).
- **KF-TFP-8 · D-5 — net-new: the panel header renders `text-title` (2.058rem / 700 ≈ 33px) — the demo's hero/poster rung — inside a 376–488px control rail whose sibling sub-pane header is `text-small font-medium` and whose picker section labels are `text-mono-caption`; the only other `text-title` class site repo-wide is a scene-hero specimen (`StartingStyleTarget.vue:33`).** Both readers' independent censuses converge (2 call sites; no `--type-title` override anywhere in demo). **MAJOR.** → **NO-WAVE-OWNER** (SS-1/SS-2 type-register spec, one motion with the copy rows); wrap behavior at minimum rail width → SS-13.
- **KF-TFP-9 · reader-1 miss #1, ADMITTED and re-verified whole at this seat's own bytes — the panel's ONLY keyboard authoring path double-fires against the demo's global playback shortcuts, and the key a user presses to leave a sub-panel stops the animation instead: ArrowLeft/Right on a focused bezier handle moves the handle AND scrubs the playhead ∓1%; Home/End sets x=0/1 AND slams the animation to t=0/t=1; Escape stops playback and leaves the panel open.** Three links, all mine this session: (1) vendor `Ze` handles ArrowLeft/Right/Up/Down/Home/End, ends `preventDefault(); N(…)`, **never** `stopPropagation` (my grep: 0 hits file-wide), bound per `<circle role="slider" tabindex="0">`; (2) glass-ui `keyboard.js` dispatches from a window keydown listener whose skip predicate accepts only true form elements/contentEditable/monaco (an SVG circle is none) and **never consults `defaultPrevented`** (my grep); (3) the demo registers Escape→`reset()` ("Stop animation"), ArrowLeft/Right→`scrubActive(±0.01)` `preventDefault:true`, Shift±0.1, Home→0, End→1 (my read, `:50-59`). The steps-mode reka-Slider half is by-mechanism-parity, not independently source-decided — carried at the M-1 precedent's PLAUSIBLE tier. = **fold-extension of ACG M-1 (mechanism banked, NO-WAVE-OWNER + GLASS relay) + M-4 (Escape) under KSM R-1's LIFO/FIFO cure-shape law.** This site's increments: the THIRD collision site; the colliding widget is **VENDOR-owned** (the demo cannot fix it at the widget); and where M-1's non-tablist half was PLAUSIBLE, this one is source-decided. **MAJOR.** → fold to the M-1 keyboard spec + the GLASS-OWNED relay rider (a `defaultPrevented` check / scope affordance in the registry — the relay must carry R-1's LIFO-for-Escape-only asymmetry). Rendered double-effect → SS-13.
- **KF-TFP-10 · D-4 as-rescoped — the vendor's root grid switches on the VIEWPORT (`lg:grid-cols-[1fr_18rem]`) inside a rail that sizes in container terms (376–488px), and the canvas card's own `overflow-hidden` zeroes the `1fr` item's automatic minimum — the canvas track CRUSHES; the fixed 18rem column is never clipped.** = **KF-ES-7 identity (MAJOR, GLASS-OWNED, banked with exactly this crush resolution; the BH letter already sent from the sidebar seat) — FOLD.** D-4's "right column unreachable" headline is killed (register #4); the producer defect (a media query contradicting the primitive's own `38cqi` container sizing) survives whole; this seat adds the second consumer site to the relay. → fold to KF-ES-7; used-width at 1024/1600px → SS-13.
- **KF-TFP-11 · D-9 = C-7 = L-8 — `progress` is declared (`:69`), parent-bound to a rAF-polled computed, read nowhere (my grep: exactly one occurrence in the file), and unwireable (the vendor has no progress prop) while `:playback="false"` disables the one affordance it would drive.** = **KF-CO-16 identity ("the sole per-frame render dependency of every inactive channel"; C·m-6's decline overruled there; disposition "two deletions + `v-if` the detail panel") — FOLD, grade at the bank.** Scope CORRECTED against reader-2 (ruled above): the panel IS unmounted on surface switches (`ChannelControls.vue` `v-if` — my read); the true always-live scope is per-channel `v-show` while the controls surface is selected (`ControlsPaneWrapper.vue:49` — my read). → fold to KF-CO-16; magnitude → SS-13.

### MINOR

- **KF-TFP-12 · D-6 residue — the two back affordances in one panel-row stack disagree on POSITION (title-left/dismiss-RIGHT here; back-LEFT/title-right in the advanced sub-pane) and glyph rung (icon-sm 16px vs icon-md 20px).** The component/label/tap-floor legs are already ruled the OTHER way — KF-CO-42 names this panel's `Button emphasis="quiet" icon-only` + `aria-label` as the donor (register #8). **MINOR.** → **NO-WAVE-OWNER**: rides the KF-CO-42 swap — when both call sites become Button, pick ONE position and ONE glyph rung.
- **KF-TFP-13 · D-8 — every one of the back Button's five utility classes is inert or redundant (the vendor's `min-block-size`/`inline-size` floors survive `h-auto`/`p-1`; both color arms restate `[data-emphasis="quiet"]` verbatim): the rendered control is a 40×40 box — 60×60 on coarse pointers (reader-1's increment: `--ui-coarse-scale` 1.5 with no demo override) — holding a 16px glyph.** **MINOR.** → **NO-WAVE-OWNER** hygiene: delete the class string; if a smaller chip is truly intended, that is a producer size-variant ask, not utility overrides.
- **KF-TFP-14 · D-11 — two orthographies of one identifier in one card: the `<h3>` renders `cubic-bézier` (accented — the spelling that is not valid CSS) while the vendor readout ~200px below prints `cubic-bezier(…)` and the emit sends `"cubic-bezier"`; plus the all-lowercase `aria-label="back to controls"` against the vendor's sentence-case AT labels in the same subtree.** The lowercase VISIBLE register is house idiom and correctly uncharged. **MINOR.** → **NO-WAVE-OWNER** (copy spec with KF-TFP-8); the aria-register counter-census → SS-13.
- **KF-TFP-15 · L-6 leg 1 — `isSeedEcho` hard-codes `"ease-out-back"`, the vendor's private `DEFAULT_BEZIER_PRESET`, deliberately not re-exported from any public glass-ui entry — a forced literal no type or test pins, on an unpinned-at-HEAD producer.** **MINOR (reader-2's MAJOR split — leg 2 lives at KF-TFP-1/2/3).** → **NO-WAVE-OWNER** + rides KF-ES-4's GLASS rider (export the constant, or ship the preset seam that retires it).
- **KF-TFP-16 · L-7 = L-12 = D-10 = L-13 — the seed/echo protocol is a copy-paste fork across the two EasingPicker consumers (divergently-typed `quadEq`; store- vs seed-anchored `isSeedEcho`; computed key vs ref+counter; byte-identical scoped CSS), both copies carrying the same dead global `container-name: easing-editor`, plus this file's inert `justify-items-center` over a single `w-full` child.** = **KF-ES-12 (the fork — grade lives there; neither seat is a clean donor) + KF-ES-18 (the container-name, incl. the flat-global-namespace hazard D-10 waved off) — FOLD.** Local crumbs die with the composable. **MINOR here.** → fold.
- **KF-TFP-17 · L-14 = C-8 — the emit type collapses to `string` (`| string` absorbs the union; the `TimingFunctionNames` import is inert; `"banana"` typechecks into a receiver that throws `TypeError` — my read of `updateTimingFunctionFromName`), while the adjacent seam keeps the tight union.** Contingent: nothing typechecks demo templates today (= KF-CO-7 ≡ KF-ES-34, banked). **MINOR.** → **NO-WAVE-OWNER** hygiene, discharged with the KF.W4 gate.
- **KF-TFP-18 · reader-1 miss #2, ADMITTED and re-probed — the FRONTIER's own EE-02 cure ships a 2-dp-LOSSY css twin at a WAAPI compositor-delegation site, and it desynchronises the store's two representations of one curve.** My probe: `cubicBezierToString` emits 2 dp against the vendor's 3-dp authored points (Δ up to 0.005/coordinate — 10× the file's own `quadEq` tolerance); my frontier reads confirm BOTH writes carry it (the panel's `css: cubicBezierToString(...pts)` AND the parent's winning `setAnimationTimingFunction(timingFunction, timingFunctionLiteralFor(key))` — so the surviving object's `css` is 2-dp lossy in all cases); the persisted literal (2 dp) and `controlPoints` (3 dp) disagree in localStorage and re-diverge on reload. Invisible to the whole corpus because all three axes read the twin-less worktree. = **fold-extension of KF-ET-1 (BLOCKER, cure-locked: "emit the exact bezier at full precision … never a silent approximation") ≡ GV-12 ≡ KF-ES-16 — the cure-lock gains a second, higher-stakes consumer (the compositor runs a curve 0.005 off the rAF curve).** **MINOR at this site (grade lives at KF-ET-1); the vendor's discarded `reparseOk`-verified `v.css`/`v.fn` (L-5's surviving half) is the honest source.** → fold to KF-ET-1's spec; the value.js lossless-serializer rider letter is already booked there.
- **KF-TFP-19 · reader-1 miss #3, ADMITTED — every pointermove on a bezier handle pays a synchronous whole-bucket `JSON.stringify` + `localStorage.setItem`: both written slots (`controlPoints` + the persisted literal) are nested in the ONE deep `useStorage` ref (no options — my read), the bucket spans every scene and channel, and the vendor emits once per move — the demo's only direct-manipulation authoring surface, with no debounce anywhere on the path.** = **fold-extension of KF-AV-16's banked mechanism (≡ the KF-APP-35 store family), graded by the KSC L-M-10 precedent ("cost, not breakage").** **MINOR with an escalation hook.** → fold to the KF-AV-16 debounce-at-the-machine-seam spec; felt INP cost → SS-13.
- **KF-TFP-20 · reader-2 miss M-1, ADMITTED and re-grounded — `kind` runs value.js's full timing-function grammar once per pointermove, one of FOUR classifier funnels invalidated by the same per-move literal write (`kind`, `isDetailEasing`, `showDetailPanel`, `activeCurvePath`) — a parser recovering a two-valued tag the handler already holds verbatim as `v.mode`.** My reads: `timingFunctionState` calls `parseTimingFunction` (+ registry `easing()` on miss); `isDetailTimingFunction` funnels through the same classifier (`:105-106` — my read); the persist writes a NEW literal string per move; all four computeds read that field. Registry-swept by the reader (KF-CO-10 and the R1 class are different mechanisms). **MINOR.** → **NO-WAVE-OWNER**: the seat composable carries mode as data, never by re-parsing its own output.
- **KF-TFP-21 · reader-2 miss M-2, ADMITTED — the direct engine write bypasses the library's own normalize/validate path, and NEITHER public path is complete: `animation.setTimingFunction` normalizes but never reaches compiled frames (a playback no-op until re-parse); the demo's direct write reaches frames but never normalizes.** Completes S+3's one-sided account — the dogfooding surfaced a TWO-sided option-setter gap. **MINOR.** → **NO-WAVE-OWNER** for the demo half (route through the composable); the library-contract half is a **KF.W5 rider** (the option-setter gap letter, carrying S+3's provenance and KF-TFP-27).
- **KF-TFP-22 · reader-1 miss #4 = reader-2 miss M-4, ONE identity, ADMITTED — two competing accessible names with no programmatic relationship: the vendor applies `label="Easing curve editor"` as the inner SVG's `aria-label` (its only binding — the banked pretty-print), while the `<h3>` names the surface "cubic-bézier" — wrong for the default state of every channel (KF-TFP-23) — with no `id`, no `aria-labelledby`, no region.** = **fold-extension of KF-ES-28** (the inverse case banked at the sidebar; its cure — restore the vendor's narrower canvas default + give the panel a landmark tie — applies unchanged). **MINOR (grade at the bank).** → fold to KF-ES-28's cure row.

### INFO

- **KF-TFP-23 · C-10 as-ruled — `kind` collapses the five-valued classifier to a two-valued mode: every non-`"steps"` value, including `undefined`, renders "cubic-bézier" and mounts bezier mode; for the shipped default (`"ease-in-out"`) the always-mounted panel carries the wrong-named `<h3>` in the AT tree.** INFO per the ruling (the `undefined` arm is masked by the banked reapply `TypeError`; the AT exposure rides KF-CO-5). Also consumes register #10: the D axis's §7 "verified NOT a defect" ruling silently swapped the VISIBLE state for the MOUNTED state. → **NO-WAVE-OWNER** rider on the seat spec: an always-mounted component carries its own guard.
- **KF-TFP-24 · D-15 = L-16 — `cubicBezierEasing` throws a bare `Error`, unguarded, inside the per-pointermove handler; unreachable today only because the vendor clamps x∈[0,1] — the invariant lives entirely in the vendor.** Reachable neighbour already banked: KF-ES-13 (`steps(1, jump-none)` render throw, MAJOR GLASS-OWNED). **INFO (posture, both axes' own grade).** → dies with L-5's `v.fn` consumption in the seat spec.
- **KF-TFP-25 · L-9 = C-9 + L-10 = C-11 — the cross-vendor jump-term bridges are inert (one four-member union, three spellings — both readers re-executed the tuple); `String(cur.jumpTerm)` is a no-op residue (reader-2's provenance: HEAD carried a genuine `jump-${…}` normalization the worktree correctly removed); `quadEq`'s length check guards fixed 4-tuples.** = **KF-ES-22 identity — FOLD (INFO-as-folded; the KF-ES-34 typecheck contingency).** The outbound annotation `:137` is the correct shape (SUP-2). → fold.
- **KF-TFP-26 · C-13 + C-15 + D-12 residue + D-13 — import-idiom crumbs (root barrel beside `/easing` while `./button` ships; split `import type` from one specifier, the second line deletable with KF-TFP-17); the transport tree's only heading, tied to nothing (the document-scope leg killed, register #9); zero RTL surface (recorded, not charged — the KF-ES-31 precedent).** **INFO.** → **NO-WAVE-OWNER** hygiene riders.
- **KF-TFP-27 · reader-2 miss M-3, ADMITTED — `animation.frames` is a compiler-delegated getter over `_frames`: pre-parse it yields `[]` and the panel's per-frame write is a silent no-op reporting success — the live-edit visibility rests on a precondition the panel neither asserts nor can observe (and any later `parse()` erases the writes — S+3's own caveat).** **INFO (reachable paths are parsed).** → **KF.W5** rider with KF-TFP-21 (one option-setter letter).
- **KF-TFP-28 · reader-2 miss M-5, ADMITTED — the `:key`'s own STATED purpose (`:25-27`, "re-seats the picker when the KIND flips") is void twice over: `mode` IS a live watched prop at the installed vendor (a kind flip needs no remount), while `steps`/`term` are initial-only AND absent from the key (a store-side change would silently fail to re-seat) — the key is wired to the one input that must never trigger it.** Decisive for the split-cure shape. **INFO.** → rides KF-TFP-1/KF-TFP-4.
- **KF-TFP-29 · L-3/C-14's surviving residue = census SCH-1/F-1 — the glass-ui pin at three coordinates (this seat's own reads: HEAD `optionalDependencies` `"6.0.0"` — one major behind the installed 7.0.0; frontier exact devDep `"7.0.0"` = installed, +3 lockfile rows; worktree deletes the block).** = **census SCH-1/F-1 identity (banked at kf-App ruling 1; the kf-ChannelOptions L·B-3 kill) — FOLD, INFO-as-folded.** The panel-local rider: the 4.0.1-era prose (`:25-31`) is unpinned-rationale drift = the KF-CO-17 row, not a dependency defect. → fold to SCH-1 → **KF.W0**.

## Killed-claims register — 11 entries, all SUSTAINED

1. **C-3 (MAJOR, + L-5's `css` half + D-15's no-css premise share) — "the panel throws the faithful CSS twin away; `eligibility.ts:175` permanently disqualifies every authored curve from WAAPI compositor delegation."** KILLED AT THE PRODUCT BASELINE, re-proved by this seat's own `git show`: the frontier's panel builds `{ fn: cubicBezierEasing(...pts), css: cubicBezierToString(...pts) }` with the EE-02 rationale comment, AND — the leg neither reader closed — the parent's synchronously-winning overwrite (confirmed KF-TFP-5) ALSO carries the twin (`setAnimationTimingFunction(timingFunction, css?)` builds `{fn, css}` at `:109`, called with `timingFunctionLiteralFor(key)` at `:170` — my reads). So `firstTF.css !== undefined` on every arm of the shipping path; C-3's own kill-condition is met twice. The mechanism exists only in the locally-reverted worktree — banked as substrate reversions at kf-ChannelOptions:13-16 (SCH-1). C-3's chain reading was correct; its SUBJECT was stale. Survivors: the `fn` half of L-5 (rides KF-TFP-24's cure) and the 2-dp lossiness of the twin itself (KF-TFP-18 — the kill births a sharper frontier defect).
2. **L-3 (MAJOR) = C-14 = D-§6's F-1 corroboration — "nothing pins the vendor; zero `glass-ui` hits in package.json/lockfile; no artifact to review, no CI signal."** FRONTIER-STALE AND HEAD-STALE, at this seat's own three-coordinate reads: HEAD declares `@mkbabb/glass-ui: "6.0.0"` (optionalDependencies), the frontier declares an exact `"7.0.0"` devDep equal to the installed version, and the zero-hit grep reproduces ONLY against the worktree's local deletion. Every downstream inference is void. Residual preserved: HEAD's pin was one major behind the installed tree (reader-2's residual), dead at the frontier. Folded → KF-TFP-29.
3. **THE ONE-PROP CURE (D §0/§Summary + D-1/D-2's mitigation + C-2/C §Disposition + L-2's replacement) — "bind `:model-value`, delete `:key`/`seedPreset`/`pickerKey`/`isSeedEcho`/`quadEq`; ~45–55 lines out, one prop in."** KILLED — the third sustaining of the banked cure-kill (KF-ES-4 ruled at that seat's own pretty-print; KF-CO-17 "spec item, NOT a mechanical swap"): `EasingPickerValue` carries NO `preset` field; the inbound applier seats points only through `setHandle`, which unconditionally stamps the preset display `"custom"`; `preset`/`steps`/`term` are consumed once as `initial*` and only `mode` is watched. A repair executed as written trades a blocker for a silent regression (every named-preset display lost). All three axes prescribed it; both readers independently killed it; the premise (KF-TFP-4) survives, the cure does not.
4. **D-4's headline (MAJOR) — "528px required vs 376–488px available … the clipped-away region is the right column: Preset, step count, Jump term, readout, copy — unreachable and unscrollable."** WRONG BRANCH: the `1fr` grid item is the canvas card, whose `overflow-hidden` zeroes the item's automatic minimum — the canvas track CRUSHES below content size; the fixed 18rem column is never clipped; every control D-4 lists remains rendered and reachable. This is the bank's own resolution (KF-ES-7), and D-4's own hedge is what ships. The structural producer defect survives as KF-TFP-10.
5. **D-3's falsifier evidence — "`grep -rn inert demo/` returns only `inertia*` identifiers."** FALSE AT THE TREE, my read: `ChannelControls.vue` ships `:inert="!keyframesActive"` with a five-line rationale naming the exact defect class (aria-hidden-focus), ~30 lines above the mount that contains this panel. The FINDING survives and is strengthened — the house cure is written and applied one component away (and KF-CO-46 already rules why it cannot be transplanted verbatim).
6. **D-14's evidence — "glass-ui's only forced-colors block covers aria-state borders."** FALSE: `dist/styles/accessibility.css` line 1 imports `glass/a11y-fallback.css`, whose `forced-colors: active` block borders every glass tier (both of the picker's cards) with `1px solid CanvasText` and zeroes the glass — the same byte that was "the D-17 decider" at the EasingSidebar seat. The genuinely-unproven residue survives untouched: the border lands on the CARD, not the SVG strokes → SS-13.
7. **L-11's absolute (MINOR) — "the panel is never unmounted … Falsifier: a `v-if` above the panel in the parent. The class-toggle is the only gate."** THE FALSIFIER IS MET, at this seat's own read: `ChannelControls.vue` gates the entire `<ChannelOptions>` subtree behind `v-if="hasSurface('controls') && selectedControlSurface === 'controls'"` — every surface switch tears the picker down (a fresh-mount fact load-bearing elsewhere in the bank: KF-CO-4, the KF-CO-15 correction). The correct surviving claim — per-channel `v-show` keeps a live `useEasingPicker` per selected-surface channel — is exactly KF-CO-16's banked scope (KF-TFP-11).
8. **D-6 (MAJOR) — "two back buttons, one navigation stack, inverted" with this panel as the deviant on component/glyph/label.** INVERTED BY THE BANK: KF-CO-42 (MAJOR) rules the SIBLING's `DockControl shape="icon" compact` + `title` wrong and prescribes exactly this panel's shape ("as the sibling TimingFunctionPanel's Back button proves in the SAME card"), with KF-CO-27 and KF-CO-24 booking the sibling's label and tap-floor legs. Surviving residue MINOR: position + glyph rung (KF-TFP-12) — and D-8's own magnitude makes this panel's box the LARGER of the two, inverting D-6's implicit direction.
9. **D-12's document-scope leg — "the panel's `<h3>` is plausibly the document's first and only heading."** NOT DECIDABLE AS FILED and false in at least one shipping configuration (a scene `<h2>` mounts on stage beside the rail — reader-1's `EasingTarget.vue:24` witness); heading order is scene-dependent, not a property of this component. The surviving half (sole heading in the transport tree, no region tie) books at KF-TFP-26/KF-TFP-22.
10. **The D axis's §7 non-defect row 1 — "`kind` could be `undefined`/wrong → NOT a defect; only cubic-bezier/steps reach the panel."** SELF-REFUTED by the same document's D-3: the gate governs the VISIBLE state; the panel is MOUNTED regardless, computing and rendering in states its own logic does not model, keyboard-reachable while collapsed. Recording it as "verified not a defect so it is not re-raised" is the harmful half. Consumed by KF-TFP-23.
11. **D-axis anchor drift (method entry, not a defect kill).** Anchors slip up to 6 lines against the very tree the axis names (five instances re-verified by reader-1; consistent with this seat's own reads). Every cited FACT is present — no finding dies of it — but the corpus's fold-by-reference machinery keys on anchors, so D-axis anchors are not inherited without re-grounding; the L/C axes are materially tighter.

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-2's mount-scope absolute** — its C-7 confirmation cell asserts "the panel is never unmounted … No `v-if` above the panel" while the `v-if` sits two components up (my read); simultaneously reader-1 had killed L-11 on exactly that byte. The one reversed confirmation-cell of this adjudication; the KF-CO-16 fold survives at its banked scope (KF-TFP-11).
2. **Reader-2's D-3 BLOCKER** — regraded to the banked KF-CO-5 identity's grade; the escalation hook (SS-13 focus witness) preserved.
3. **Reader-1's D-2 non-booking** — the reopen-on-wrong-curve manifestation was left inside D-1's terminal-state prose; reader-2's separate BLOCKER booking adopted (KF-TFP-2), with the distinct reachability chain verified at this seat's own reads.
4. **The D axis's S-C contrast superlative — DEMOTED by this seat (neither reader ruled on it).** The 5.01/5.43 ratios are token-derived over a flat `--card` assumption whose falsifier S-C itself names (a glass wash shifts the effective background); the bank's manufactured-ratio refusal (the EasingSidebar seat's residue #8, "both readers correctly refused") applies. Neutral, not sustained → SS-13 measures the real composited surface.
5. **Reader-2's L-6 MAJOR** — split: the seed-anchor leg is the enabling condition of three separately-booked rows; the standalone unexported-constant coupling books MINOR (KF-TFP-15).
6. **Reader-1's C-10 MINOR** — overruled to INFO (the masking `TypeError` is banked; the AT exposure rides KF-CO-5); preserved as dissent on the row.
7. **Reader-1's D-1 severity-vs-fold tension** — "folds by reference to KF-ES-12(b)" and "I sustain BLOCKER at this component" cannot both be the booking: resolved as a component-primary BLOCKER row here (the registry is per-component) with the FORK identity remaining KF-ES-12's — the sub-clause in the bank was a rider inside a different identity, not a booking of this component's roster.

## Superlatives — sustained, with provenance (L-18 runs both ways)

1. **S-A / SUP-4 — the shadow component was genuinely DELETED, not shimmed**: the 1,082-line `instrument/easing/` cluster is gone (both readers' `find` = nothing), both authoring modes covered by one published primitive through the `/easing` subpath. The worked example the S-1..S-8 census asks for — sustained at both consumer seats now, and it survives every defect above it: consuming the right primitive and consuming it wrongly are independent.
2. **S-B / SUP-3 — `container-type: inline-size` is load-bearing to the digit**: the vendor's `clamp(200px, 38cqi, 320px)` requires exactly this environment; the comment names the mechanism AND retires the predecessor's px-arithmetic. Third sustaining (banked at KF-ES #2). The D-4 irony indicts the vendor's media query, not this declaration. Redundancy probe → SS-13.
3. **S-D — reduced-motion honesty is complete, by inheritance and by omission**: the component's only transition is `transition-colors`; `:playback="false"` silences the vendor's rAF travel clock; the host's `grid-template-rows` collapse SNAPS under PRM (excluded from glass-ui's forced transition-property list — reader-1's re-read). Nothing originating here survives PRM as motion.
4. **S-E — zero contribution to the flat `--kf-*` namespace hazard**: no custom property, no colour or length literal; every visual value is a vendor token or utility; the file needs no migration if the hazard is remediated. (Its one namespace surface — the unprefixed global `container-name` — is KF-ES-18's, folded.)
5. **S+1 / SUP-1, SCOPED — echo-recognition BY VALUE is the right AXIS** (order-free, idempotent, undefeatable by lost/duplicated/reordered echoes) and the comment states it as an invariant. Carried as principle, never as predicate: the anchor is wrong (store, not seed), the `!seed` arm is a standing filter (KF-TFP-3), and the vendor now ships its own structural suppressor. The KF-ES-1 class ruling binds the replacement.
6. **S+2 — `quadEq`'s 5e-4 tolerance is derived from the PRODUCER's quantum** (`+a.toFixed(3)` → half-width 5e-4: distinct authored quads can never alias; equal quads survive the float round-trip) — the non-obvious, correct derivation, with a live unpinned falsifier (`toFixed(4)` breaks it silently). The counterweight is five lines away: the same file's persist path serializes at 2 dp (KF-TFP-18) — **the file gets its epsilon right and its serializer wrong**.
7. **S+3 — the single-`{fn}`-shared-across-frames discipline compensates a real engine gap**: the sanctioned setter never reaches compiled frames (a playback no-op until re-parse), so the demo's two-write idiom is what makes a live edit visible — dogfooding surfacing a library defect. Completed, not contradicted, by KF-TFP-21/27: the gap is two-sided (setter normalizes but doesn't propagate; direct write propagates but doesn't normalize), and the indicted redundant COPY is KF-TFP-5 — the shape belongs in the composable, where it already lives.
8. **SUP-2 — the outbound bridge is an annotation, not a cast** (`const jumpTerm: JumpPosition = v.term`): a zero-cost compile-time tripwire at the exact point where vendor state enters persistence — the same bridge built to opposite standards in the inbound direction (KF-TFP-25).
9. **PROVENANCE (reader-2's, adopted with this seat's counterweight)** — the shipping product is better than the corpus measured, and says why: the frontier's EE-02 twin was authored with a comment naming the failure it prevents, making the corpus's staleness detectable in one diff. Counterweight: the same twin is 2-dp lossy (KF-TFP-18) — the repair-with-rationale still violates the `Easing.css` faithfulness contract it cites.
10. **METHOD — two probes that earned their keep**: the C axis's residual-honesty paragraph (`:94`) narrowed the vendor ask one field short of the cure-kill decider while its own disposition still prescribed the swap — the only place any axis approached the kill; and the L axis's L-17 executed the published artifacts as data, falsifying in-tree prose in the SIBLING file and simultaneously upgrading the blocker from "can happen" to "fires on the dominant entry path" — one measurement, two payloads, reproduced independently by both readers.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **KF-TFP-1/2's visible half** — the teleport to `ease-out-back` on drag frame 1; the dead pointer capture; whether the small preview (`activeCurvePath`) and the big editor visibly render two different curves simultaneously; the readout/copy exporting the wrong literal on reopen.
2. **KF-TFP-9's witness** — one keypress, two effects (Home/End sharpest: handle to x=0/1 AND playhead to t=0/1); whether any focus-visible cue survives; the reka-Slider steps-mode half (the parity leg).
3. **KF-ES-7 geometry** — used width of the crushed canvas track at 1024/1600px; the 200px-floor letterboxing (banked residue restated; second consumer site).
4. **KF-CO-9 rendering** — the 100-tread staircase through 240 samples; "Steps (n) — 100" beside a thumb pinned at 12.
5. **The reka Slider mount-clamp leg** — if the Slider emits a clamped correction on mount, `isSeedEcho`'s steps-equality fails and merely OPENING the panel persists 100→12 as an authored edit.
6. **KF-CO-16 magnitude** — per-frame re-render cost of the dead `:progress` across a multi-channel scene (banked as kf-ChannelOptions SS-13 item 8).
7. **KF-CO-5 focus behavior** — where the viewport scrolls when focus lands in a 0-height `opacity:0` row; what AT announces (the KF-TFP-7 escalation trigger).
8. **Forced-colors strokes** — curve `stroke-(--easing-curve-accent)` vs grid `stroke-border/40` vs guide `stroke-muted-foreground/30` (the D-14 residual; card borders are covered, strokes are not — vendor-owned).
9. **The header at minimum rail width (400px − 24px padding)** — `text-title`'s "cubic-bézier" (`text-wrap: balance`, no `min-w-0`/`truncate`) wrapping vs pushing the 40/60px Button out of the row.
10. **S-B redundancy probe** — whether a nearer ancestor already establishes inline-size containment (downgrades the superlative to neutral, never to a defect).
11. **S-C's real contrast** — `text-muted-foreground` measured over the actual composited pane surface (this seat's demotion, correction #4).
12. **D-11's aria-label register counter-census** — the demo's other `aria-label`s (would weaken the register leg to INFO).
13. **KF-TFP-19's felt INP cost** — the per-pointermove whole-bucket serialize; the MINOR grade carries the escalation hook.

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c` against frontier `origin/master 81a56990` under the baseline law: the seeding stack, its host wiring, and every roster row above are frontier-durable; the three divergent files on this blast surface (the panel's EE-02 twin, the setter's `css` thread, the package.json pin) were re-read at BOTH coordinates by this seat and decide four kills and one frontier-only defect. 29 roster rows — 2 BLOCKER · 9 MAJOR · 11 MINOR · 7 INFO, with 14 folds/fold-extensions to banked identities and zero re-bookings. 11 kills sustained — the frontier-baseline kills re-proved at this seat's own git reads (including the winning-write leg neither reader closed), the one-prop cure dead for the third time, and two reader-vs-reader contradictions settled at the bytes (the `v-if` exists; the canvas crushes). 8 missed rows admitted, the largest a source-decided keyboard-collision third site whose colliding widget is vendor-owned. The component is the census's best shadow-component citizen — the cluster deleted, the container supplied, the clock refused, zero token surface — wearing the demo's worst seeding contract: a `:key` wired to the value its own handler writes, an echo filter anchored to the wrong reference with a standing hard-coded arm, and a stale vendor premise whose tempting one-prop cure would trade the blocker for a silent regression. Routings are M-25 agglomeration input: the behavioral core rides NO-WAVE-OWNER to the SS-1/SS-2 seat-composable spec under the banked KF-ES-4/KF-ET-1 cure-locks; producer rows ride the standing GLASS BH relay (the viewport grid, the preset seam, the keyboard registry's missing `defaultPrevented`/scope with R-1's LIFO/FIFO law); structural folds to KF.W0/W4/W5; 13 visual rows to SS-13/KF.W9. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.
