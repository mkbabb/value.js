Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequenceScrubber; receipt read from this seat's own system prompt, not inherited).

# kf-SequenceScrubber — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c`, `origin/master` `81a56990`; **worktree ≡ origin/master on every audited surface** (this seat's `git diff --name-only origin/master -- demo/scenes/sequence/ demo/composables/useDragScrub.ts demo/styles/design-idioms.css demo/utils/gestureSelectSuppression.ts demo/scenes/spring/SpringTarget.vue demo/styles/style.css` → EMPTY this session). The ONLY corpus-relevant divergence is `package.json` + `package-lock.json`, both worktree-dirty — the substrate of the F-1 kill (K-1). · **Registry tree** value.js `tranche-u` · **Corpus**: three challenge axes (`audit/kf-components/SequenceScrubber/challenge-{D-design,L-library,C-consumption}.md` — D: 12 defects incl. 1 BLOCKER; L: 12 defects, 5 superlatives; C: 13 defects, 5 superlatives) re-read whole by two hostile re-readers — **reader-A** (design-and-usage: 17 confirmed · 10 kills · 5 misses · 10 unproven · 7 superlatives) and **reader-B** (library-and-code: 24 confirmed · 9 kills · 4 misses · 8 unproven · 8 superlatives). Both returns consumed whole; the three challenge files re-read by this seat; live source re-read wherever the readers disagree or a verdict is load-bearing.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SequenceScrubber.vue (162 L — every cited anchor checked: `:7` wrapper `px-4 py-3`, `:11` eyebrow class list, `:16` timecode, `:20-28` rail attrs, `:34` transform, `:47` `inject(...)!`, `:62` `scrubbing`, `:69-73` `onScrub`, `:75-93` seam wiring, `:95` wrapper, `:97-111` keyboard, `:114-161` scoped block incl. `:127` `0.18em`, `:145` the `:584` citation, `:148-150`, `:158-159`), useDragScrub.ts (151 L — `dragging` `:98` the sole gesture identity; `onPointerDown` `:112-126` **no re-entrancy guard**, `acquireSelectSuppression()` `:116` unconditional; `endGesture` `:100-110` release-then-hooks; window handlers `:131-147` gated only on `dragging.value`; `pointerId` exactly once `:120`; `releasePolicy`/`onRelease` `:52,:76-82,:96,:109`; docblock claims `:14` MotionPath, `:26-27` "rail scenes leave them unset", `:36-38` "Nesting-safe"), gestureSelectSuppression.ts (24 L — unguarded increment `:12`, `Math.max(0, …−1)` + remove-at-zero `:19-22`); SpringTarget.vue:60-75 (`focus-ring … h-12 cursor-pointer select-none` on the twin rail `:63`), :228-240 (`useDragScrub({el: railEl})`), :330-352 (the protagonist-ball posture comment verbatim + `.sampler-ball { --ball-glow: 0% }` `:350` **and a third writer `--ball-glow: 30%` at `:438`**); SequenceTarget.vue:8/:18-25/:39/:55/:62-63/:66/:70/:105/:127 and :203-243 (ONE `useDragScrub` instance for all five handles via `activeRowEl`; `onRowDown` latch; `ROW_AT_STEP = 40` named+commented); useThrottledReadout.ts:1-14 (the docstring verbatim — decisive for K-11); useSceneMachine.ts:76/:169-181 (`useStorage` with NO options; echo-guard + fresh `persisted.value` object); design-idioms.css `wc -l` → **300**; :289-300 (`html:has(body.is-dragging), body.is-dragging, body.is-dragging * { user-select: none !important }` verbatim); the `touch-action` census re-run (12 declaration lines; nothing reaches `.seq-scrub` or `.spring-rail`); installed glass-ui 7.0.0: fonts.css — **4 `@font-face` (2× Fira Code, 2× Plus Jakarta Sans), ALL `font-style: normal`, `grep italic` → 0**; typography/semantic.css `@utility text-caption { … font-style: italic }` + `@utility text-admin-label { … font-size: var(--type-admin-label) … }` (python extraction); typography/utilities.css `@utility text-mono-caption` (no line-height) + `.tabular-nums { font-variant-numeric: tabular-nums lining-nums }` + `.section-label { @apply text-mono-caption; color: var(--muted-foreground) }`; scale.css **`--type-admin-label: 0.625rem` (fixed) vs `--type-caption: clamp(0.75rem, 0.71rem + 0.21vw, 1rem)` (fluid) vs `--type-display-3: clamp(2.618rem, …)`**; tokens/scheme-motion.css `--type-tracking-caps: 0.1em`, `--type-leading-caption: 1.3`, `--type-leading-body: 1.5`, `--font-stack-text: "Plus Jakarta Sans", …`; theme/bridges.css `--text-caption: var(--type-caption)` (the ordering caveat's other definition); `--radius-pill` demo grep → 0, sole definer glass theme/radius.css; demo/styles/style.css:50/:100 (`font-synthesis: none` at `:root`, authored for Instrument Serif) — glass dist declares `font-synthesis` NOWHERE (grep → 0 files); demo/app/index.html font links = preconnect + Instrument Serif only; `releasePolicy` census → sole consumer SquareScene.vue:258 passing the default `"persist"`; `ls demo/scenes/` → no motion-path; useSceneMachine.ts:255 "a PRUNED scene — compose/morph/motion-path per OD-1" verbatim. **Git substrate probes**: worktree package.json `@mkbabb/*` = value.js 4.0.0 only; `git show HEAD:package.json` → `"@mkbabb/glass-ui": "6.0.0"` (:71); `git show origin/master:package.json` → `"@mkbabb/glass-ui": "7.0.0"` (:77, exact); origin/master lockfile glass-ui hits = 3, worktree lockfile = 0; installed = 7.0.0. Registry instruments: kf-SequencePlayhead.md r4 (whole — N-3/N-13/N-14/N-18/K-6/ruling 8/identity guard :144), kf-AnimationVisualizer.md (:9 census cell, KF-AV-13/15/16/17/28), kf-ChromeDock.md:40 (frontier-CURED), kf-OrbitalDrag.md:31 + kf-CubeTarget.md:54-55 (the useDragScrub exonerations), kf-AmigaScene.md:28 (the house-wide multi-pointer class), kf-SequenceAxis.md:52/:60 (legend rider; N-14 concordance; `@800MS` relay), kf-EditorStartScreen.md:22/:59 (KF-EST-2), kf-MbabbMenu.md:80 (MM-29), kf-EasingSidebar.md:46 (KF-ES-8), kf-MatrixEditor.md (ME-44 CardHeader-nowhere); registry sweeps: `gestureSelectSuppression|releasePolicy|text-caption` across `kf-*.md` → the suppression class only at kf-AnimationVisualizer/kf-OrbitalDrag (useDragCapture/useOrbitalPointer, never this seam); `releasePolicy` → 0; `text-caption` → 0; no existing `kf-SequenceScrubber*` file (this record is r1).

## Scoreboard

**29 adjudicated roster rows — 0 BLOCKER · 3 MAJOR · 16 MINOR · 10 INFO** (heavy fold-by-reference: ten rows fold whole or in part to banked kf-SequencePlayhead N-rows, KF-AV rows, or census rows — never re-booked). **Killed-claims register: 14 entries**, including the challenger's sole BLOCKER (dead twice over), the corpus-wide F-1 premise (the sixth one-baseline-short event on record), D-2's headline italic mechanism (dead at the font inventory), and two whole defect rows (D-9/C-9). **6 net-new admissions** from the readers' misses (KF-SCR-1..6, each re-verified at the bytes by this seat — one MAJOR), plus five cells adopted into banked rows. **8 superlatives** (merged, two strengthened by this seat's own instruments). **12 UNPROVEN-NEEDS-LIVE residues** → SS-13.

**Headline.** The corpus graded loud and the tree graded quiet — and the biggest defect in the component's orbit was found by nobody until the second reading generation. All four MAJOR filings on the D axis and two of C's deflate: the BLOCKER dies on booking (≡ banked N-18) AND severity; the phantom-dep gate that D §8 and L's D-2 built sequencing on exists only in an uncommitted worktree edit (`origin/master` declares glass-ui 7.0.0 exact, lockfile concordant, installed concordant); the "UPPERCASE ITALIC" faux-small-caps mechanism dies at a font inventory D never ran (four faces, all `font-style: normal`, under `font-synthesis: none`); and the three-axis "sole drag surface without touch-action" rhetoric dies on a comparator none of the three axes opened — `SpringTarget.vue`, the component's near-byte-identical twin, which also kills the cursor row, re-frames the ball-anchoring row as deliberate cross-scene posture, and *upgrades* the focus-ring row ("the one rail that dropped the class its own twin wears"). Meanwhile reader-B's MISS-1 — verified whole at the bytes by this seat — is the record's sharpest new find: `useDragScrub` acquires the global select-suppression token unconditionally per `pointerdown` but releases it only behind the `dragging` boolean, so **two fingers on the master rail strand `body.is-dragging` forever** — `user-select: none !important` document-wide for the life of the page, no reload-free recovery — in the very seam the registry twice praised as "the honest form," under a docblock that claims "Nesting-safe." The component itself remains what the superlatives say it is: a 162-line leaf that consumes its idiom by parameter, carries no clock, no teardown surface, and no literal colour — whose prose lies about its own font, its own brightness, its own line citation, and whose gesture contract is pointer-only in four independent ways.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0/W6/W7/W9) with **NO-WAVE-OWNER** for the sequence scene-repair packet and the drag-seam packet. Anchors bind to worktree ≡ `origin/master`; re-resolve before any cure.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **Reader-B MISS-1 (the suppression-token strand) filed MAJOR as "all three axes missed it, and challenge-C filed the same seam as a superlative (S-E)"; reader-A did not find it** | **ADMITTED — KF-SCR-1, MAJOR.** B's framing softened in one place: the banked exonerations are EXTENDED, not corrected | Whole-file reads this session. `onPointerDown` (useDragScrub.ts:112-126): `dragging.value = true` then `acquireSelectSuppression()` — **no `if (dragging.value) return` anywhere in the file**; the window `pointerup`/`pointercancel` handlers early-return on `!dragging.value` BEFORE `endGesture` (:136-139, :144-147); `releaseSelectSuppression()` decrements with `Math.max(0, …)` and removes `body.is-dragging` only at 0 (gestureSelectSuppression.ts:19-22). Trace: finger A down (count 1) → finger B down on the same rail (per-pointerId capture does not stop pointer B hit-testing the element; `@pointerdown` fires again → count 2, `onStart` re-fires, `lastP` reset mid-drag) → first up runs `endGesture` (count 1, dragging false) → second up **early-returns, no release** → count pinned ≥ 1 forever → `html:has(body.is-dragging), body.is-dragging, body.is-dragging * { user-select: none !important }` (design-idioms.css:292-297, verified verbatim) for the session. Second trigger: two-finger pinch → both pointers `pointercancel` → same imbalance (`.seq-scrub` has no `touch-action`; inherited `manipulation` PERMITS pinch — the compound with D-5). Third limb: mid-drag unmount — vueuse teardown removes the only listeners that could release; unlike banked kf-CubeTarget #54 it cannot self-heal. **The docblock's own ":36-38 Nesting-safe: a small reference count guards against two concurrent gestures clearing the token early" is false in the opposite direction** — the count survives early-clear and dies on under-release. Registry: the mechanism CLASS is banked at **KF-AV-15** (kf-AnimationVisualizer.md:55) for `useDragCapture` — a DIFFERENT seam; kf-OrbitalDrag.md:31 ("`useDragScrub` remains the honest form") and kf-CubeTarget.md:54-55 exonerate THIS seam on **listener lifecycle** and **capture-acquire guarding** — axes on which the exoneration stays TRUE (setup-scope registration; try/catch on `setPointerCapture`). Neither row ruled on re-entrancy; KF-SCR-1 extends them. Cure family shared with L·D-1: `if (dragging.value) return` + a `pointerId` latch + an `onScopeDispose` that releases an in-flight token, one edit at the seam. |
| 2 | **L·D-1 (multi-pointer cross-talk) — reader-A: "seam instance of a banked house-wide class, not a fresh discovery"; reader-B: "MAJOR — the first filing against `useDragScrub` itself"** | **BOTH HALF-RIGHT — booked MAJOR here as the seam's first filing, with the class cross-referenced; B's six-hosts counting cell corrected (both readers concordant on the correction)** | `pointerId` occurs exactly once in the file (:120, the capture call); move/up/cancel gate only on the shared boolean; `project` reads `e.clientX` for whatever pointer arrived (Scrubber:77-82) and `endGesture` kills the live gesture on any pointer's up. The class is banked house-wide (kf-AmigaScene.md:28: zero `isPrimary`/`e.button` guards in any scene; KF-AV-15's sibling seam `useDragCapture`) — but no registry row books `useDragScrub` itself (my sweep), and the scene stacks the demo's densest exposure: **TWO seam instances** (the master rail + ONE shared instance for all five row handles — SequenceTarget.vue:209-230, `activeRowEl` computed; NOT six instances) → 6 window listeners. Cross-instance theft needs the pointerId latch; same-instance double-down needs KF-SCR-1's re-entrancy guard — one cure, two limbs. |
| 3 | **The F-1 kill — both readers kill L·D-2 MAJOR + C-13's F-1 limb + D §7/§8's "blocking" gate as ONE-BASELINE-SHORT** | **KILL CONFIRMED (K-1) on this seat's own three-substrate probe — the sixth one-baseline-short event in this program's ledger** | My git probes this session: worktree package.json carries NO glass-ui and its lockfile 0 hits — but the worktree is DIRTY on exactly those two files; local HEAD `8281638c` declares `"@mkbabb/glass-ui": "6.0.0"` (:71); **`origin/master` declares `"@mkbabb/glass-ui": "7.0.0"` exact (:77) with 3 lockfile hits, matching the installed 7.0.0**. The "absent from package.json AND package-lock.json" premise exists only in an uncommitted local edit. Precedent already banked: kf-ChromeDock.md:40 ("frontier-CURED … both re-readers booked it BLOCKER without the substrate triage"). Every "blocking"/"F-1 must land first"/"post-`npm ci` degradation" consequence in all three axes dies with it — including D's repair-order step 7 gate and C's §3 sequencing caveat. **What survives**: the baseline-independent cascade-coupling refinement (row C·C-13, INFO, folds to the banked kf-SequenceAxis legend rider). This also corrects the carried census cell `glass-ui in package.json/package-lock.json = 0/0 (F-1)` at kf-AnimationVisualizer.md:9 — true of the dirty worktree, false of the frontier. |
| 4 | **D·D-2's headline: reader-A KILLS the italic mechanism outright; reader-B holds the italic limb UNPROVEN (Tailwind v4 utility-ordering) while confirming the row at MAJOR; A lands MINOR** | **A's KILL SUSTAINED at this seat's own bytes; row CONFIRMED at MINOR (B's MAJOR preserved as dissent); B's ordering caveat kept as an SS-13 courtesy check — immaterial to any surviving limb** | My font inventory: glass-ui dist/styles/fonts.css ships exactly **4 `@font-face` — 2× "Fira Code", 2× "Plus Jakarta Sans" — all `font-style: normal`; zero `italic` occurrences**. index.html's only external font is Instrument Serif. `font-synthesis: none` at demo style.css:100 (`:root`, authored for Instrument Serif per :50's comment); glass dist declares `font-synthesis` in zero files. Per CSS Fonts 4 §5 a matched family is not skipped for a missing style, and `font-synthesis-style: none` forbids the compensating oblique — so whether semantic.css's `@utility text-caption { … font-style: italic }` or bridges.css's font-size-only `--text-caption` wins the ordering, **the eyebrow paints UPRIGHT Plus Jakarta, never italic, never Fira Code**. D's "textbook faux-small-caps failure" mechanism is dead; D's stated falsifier searched only demo sheets and could not see it. SURVIVING LIMBS (all verified): false "Fira Code" prose twice (:9-10, :124-125); the demo's **sole** hand-rolled micro-cap (`grep "text-transform: uppercase" demo/` → exactly SequenceScrubber.vue:127); `0.18em` off-ladder (token `--type-tracking-caps: 0.1em`); the system ships the exact intended idiom (`text-mono-caption`, consumed by THIS FILE five lines later at :16; `.section-label` a one-class match) and a sibling in the same Card wears `text-admin-label` (SequenceTarget.vue:39). With the mechanism dead this is a one-class hygiene row: **MINOR**. Mechanism-law precedent ≡ banked KF-EST-2 (same law, different family). |
| 5 | **D·D-2's prescribed cure ("`text-admin-label`, delete `.seq-eyebrow`") — reader-A kills it OFF-RUNG; reader-B's proof cites `text-admin-label` approvingly as "the system idiom"** | **A's CURE KILL SUSTAINED (K-4); B's cell narrowed — the idiom exists, but it is the wrong rung** | Verified at scale.css: `--type-admin-label: 0.625rem` — a FIXED 10px rung; `--type-caption: clamp(0.75rem, 0.71rem + 0.21vw, 1rem)` — fluid 12-16px. D's cure silently drops the eyebrow 2-6px and off the fluid ladder. The rung-exact cure is `text-mono-caption` (`font-family: var(--font-mono); font-size: var(--type-caption); letter-spacing: var(--type-tracking-caps); text-transform: uppercase` — verbatim the comment's stated intent) or `.section-label` (`@apply text-mono-caption; color: var(--muted-foreground)` — the whole span in one class). Note the interlock with KF-SCR-4: caps tracking is CORRECT on the eyebrow (real uppercase letters) and questionable on the digits-only timecode — same utility, two verdicts, which is exactly why the cure is a packet decision, not a drive-by. |
| 6 | **D·D-1's grade — reader-A rules MINOR on the header-prose evidence; reader-B endorses the banked INFO (+ r4 MINOR dissent), killing only the BLOCKER** | **BLOCKER DEAD (both readers + banked K-6 lineage, concordant — booking kill ≡ N-18 + severity kill). Grade RULED **MINOR** on evidence not before the r4 seat; N-18's identity retained (anti-rename), the movement recorded here as evidence-backed, never a bare re-contest** | Geometry re-anchored at the bytes this session (paddings, `--track-inset`, centre-anchor all confirmed in my reads; the full Δ(p) = −81 + 98p, W-independent, sign flip at p≈0.827 — both readers re-derived it independently and concordantly; the 81/17 vs banked 80/16 delta is the `.seq-stage` 1px border, adopted as the finer figure). The banked N-18 (kf-SequencePlayhead.md:88) ruled INFO on the rationale "a transport control legitimately owns a full-width rail" — **but this component's own header denies that premise**: ":2-4 the storyboard's editable CONTENT (the playhead the user scrubs), not transport chrome (the bottom TransportDock IS the transport)" — read verbatim by this seat. That prose was not in evidence at the r4 seat (a SequencePlayhead corpus). K-31's severity law kills re-grades *without new evidence*; this one carries it. Adopted into N-18: the sign-change cell (the thumb OVERTAKES the playhead it drives at p≈0.827 — no banked row states it), the 1px-border refinement, and the header-prose cell. The prose limb itself (a file claiming content-status while riding a transport-anchored rail) routes to the prose-truth sweep. The identity guard at kf-SequencePlayhead.md:144 pre-routes this fold by name; honored. |
| 7 | **C·C-1's throttle-precedent cell — reader-A ADOPTS it ("useThrottledReadout.ts:9 names useSequenceDemo as a lapsed consumer"); reader-B KILLS it as a misquote** | **B WINS at the docstring — and A's adoption is a citation-inheritance event ACROSS re-readers, recorded (K-11)** | My read of useThrottledReadout.ts:5-10: "(`useEasingDemo` / `useSpringHotPath` / `useSpringDemo`, and **historically `AmigaScene` / `useSequenceDemo` before their readouts were removed**)" — the docstring records the readouts as REMOVED, not the throttle as lapsed. C misread it; A inherited C's misreading verbatim while stamping it "verified"; B caught it and also caught the sharper true fact (the docstring's "removed" is itself stale prose — `useSequenceDemo` still writes `progress.value` per rAF at :179-181/:188-190). The C-1 chain verdict is untouched: **MAJOR at the seam, FOLD ≡ banked KF-AV-16** (kf-AnimationVisualizer.md:56, whose own scoping clause — "the per-SCRUB-dispatch persist is a chain property that pointer scrubs pay too" — this corpus independently derives from the pointer end). Chain nodes re-anchored by this seat: `useStorage(SCENE_MACHINE_PERSIST_KEY, {…})` with NO options (useSceneMachine.ts:76); the echo-guard that cannot fire + the fresh `persisted.value` object (:169-181). Magnitude stays UNPROVEN → SS-13. |
| 8 | **D·D-7's contrast — reader-A refuses to certify the table (four wrong-byte events on this chain's ledger); reader-B independently re-derives 4.606:1** | **B's DERIVATION ADOPTED as the adjudicated cell — the first contrast derivation in this component family to survive independent re-derivation; A's abstention recorded as the correct posture, not an error** | B re-derived from the banked byte ledger (kf-SequencePlayhead K-1/K-21/K-30 fixed the light arm at rgb(126, 90, 204)): Y_fg 0.1611 / Y_bg 0.9222 / **4.606:1**, concordant with D's published 4.63:1 to 0.6% with the background luminance exact — against a family ledger holding FOUR wrong-byte events (D 216 → DU "byte-identical" 216 → B-r4 229 → truth 204). Row CONFIRMED MINOR: the ungated dark-idiom `text-shadow` over 12-16px tabular figures in a light-default theme (`color-scheme: light` at style.css:92; chain `--ball-tone → --color-progress → --accent-kf` verified); percept → SS-13. |
| 9 | **Severity contests: C-2's MAJOR (keyboard bypass) · C-3/D-5's MAJORs (touch-action) · C-4's MAJOR (inject-only) · B's MINOR vs A's INFO on C-7 and L·D-12 · B's MINOR vs A's INFO on C-4** | **Keyboard bypass MINOR (banked N-13/N-3 fold; a severity contest with no new mechanism — the K-31 law, third generation). Touch-action MINOR (the corpus refutes itself: L's own ancestor-overflow walk + C's concession; the roster cells die separately at K-6). C-4 MINOR narrowed (the undeclared three-way contract asymmetry + the `inject(...)!` posture; the MAJOR and the prop/emit cure die at K-8; SUP-5 sustained — A's INFO preserved as dissent). C-7 INFO locally (the durable divergence half is BANKED — N-4/N-18/KF-AV-17; the local residue is a statically-proven 2px clip of a 36px ball — B's MINOR as dissent, B's static discharge of the Card-padding unproven ADOPTED: `.card` declares only `--card-pad-*`, padding lives on unused CardHeader/-Content/-Footer, `grep -rln CardHeader demo/` → 0 ≡ banked ME-44). L·D-12 INFO (≡ banked N-10 class, second instance in the same scene; B's MINOR as dissent)** | Each re-derived: keyboard path `:97-111` calls `demo.scrub()` bare (my read) — the mechanism is banked at N-13 (heat) + N-3 (direction); the net-new witness cells adopt into the banked rows (ruling 10). Touch: `.seq-scrub` declares nothing; `html,body` `manipulation` permits pan/pinch; no pannable ancestor (L's walk verified by the overflow chain); the manifestation is live-only. C-4: the scrubber consumes THREE writer verbs the parent does not own — the flip is re-plumbing ceremony, not honesty (C's own §3 names the eventual target's contract, which is a swap decision = KF.W7's, not this row's). |
| 10 | **Net-new admissions and cell adoptions from the misses** | **SIX ADMITTED (KF-SCR-1..6, one MAJOR · three MINOR · two INFO); FIVE CELLS adopted into banked rows; one reader miss re-classed as corpus-method datum** | **KF-SCR-1** (B-MISS-1) — ruling 1. **KF-SCR-2** (A-M-1) — `font-synthesis: none` voids glass-ui's `text-caption` italic register demo-wide: semantic.css declares `font-style: italic` on the utility; no italic face exists in the shipped inventory; synthesis is forbidden at `:root`; 8 consumer sites re-grepped (SequenceScrubber:11, SquareInstrument ×3, CubeScene:136, StartingStyleTarget:72, SpringPhysicsFacet:105, SpringHeatmap:50). Mechanism law ≡ banked KF-EST-2 (Instrument Serif — transient swap-window there; PERMANENT here); unbanked for this family (`text-caption` registry grep → 0). MINOR. Fleet consequence rides the glass-ui BH relay (owner edict): either the caption register depends on synthesis and must say so, or the demo owes an italic-declared face. B's ordering caveat (bridges vs semantic definition) noted: under EITHER resolution the render is upright — the verdict does not depend on it; one `getComputedStyle` read at SS-13 is the courtesy check. **KF-SCR-3** (A-M-3) — the 36px protagonist ball seated in a 36px `h-9` row with zero vertical clearance, against the demo's own written rule one scene over ("The h-12 rail row seats the 36px ball with breathing room; at the former 1.75rem it 'read as a footnote'" — SpringTarget.vue:334-339 verbatim, rail `h-12` at :63); plus the idiom's `0 2px 10px` glow reaching ~12px below its box = exactly the Card's clip edge (wrapper `py-3`, last child of `overflow-hidden` Card). Source-decided asymmetry MINOR; the glow-clip percept → SS-13. **KF-SCR-4** (A-M-4) — `text-mono-caption` on the digits-only timecode: `text-transform: uppercase` inert on `0.470`; `--type-tracking-caps: 0.1em` (+1.2-1.6px per glyph at the caption rung) on a readout the file twice insists should "click like a counter"; and the utility declares NO `line-height` while the eyebrow's `text-caption` declares 1.3 — two spans in one `flex items-center` row resolving different line boxes (1.3 vs inherited 1.5). Mechanism family BANKED (MM-29 caps-tracking-on-lowercase; KF-ES-8 mono-caption-on-a-sentence; kf-SequenceAxis's `@800MS` relay) — the numeric-readout instance and the line-height limb are new; MINOR, cure with the family. **KF-SCR-5** (B-MISS-2) — `releasePolicy`/`onRelease` is dead API on the shared seam: sole call site passes the DEFAULT (SquareScene.vue:258 `"persist"`); `"recenter"` has zero consumers; the `:109` branch is unreachable; the docblock names MotionPath three times as a live consumer of a scene the tree records as PRUNED (useSceneMachine.ts:255 verbatim), and its ":26-27 the rail scenes leave them unset" is false about this file (:84-92 sets both). INFO → prose-truth sweep, seam tier. **KF-SCR-6** (B-MISS-4) — the keyboard step is a bare `0.05` twice (:99, :102) against the same Card's named, commented `ROW_AT_STEP = 40` (SequenceTarget.vue:232 verbatim); the two sliders' granularities (5% vs 2.5%) silently disagree. INFO; the cure is the same `applyScrub` helper N-13's cure family already wants. **CELLS ADOPTED into banked rows**: the sign-change + border-refinement + header-prose cells → N-18 (ruling 6); the fresh-page-first-ArrowLeft comet witness (A) + the deadband cure (C-12) → N-3; the keyboard-bloom rider (A-M-5: `.seq-scrub.is-scrubbing .scrub-ball { --ball-glow: 60% }` hangs off the LOCAL `scrubbing` ref — a second pointer-only consumer banked N-13 never names, making the master control's entire live-gesture affordance keyboard-unreachable) → N-13; the NaN cell (B-MISS-3: `project()`'s 0/0 → NaN-transparent clamps → `aria-valuenow="NaN"` on a live `role="slider"` (an `aria-valid-attr-value` violation), the string `"NaN"` as the LIT timecode, IACVT transform — changing the cure's shape: guard at the projector, not only at the custom property) → the banked ruling-8 property-hygiene family (D-11/L-6/C-7 + L-9/C-6); the per-frame `aria-valuenow` ATTRIBUTE-write limb stays in L·D-6's row (the per-frame-Vue limb folds ≡ banked D-5/L-8/C-4 + K-2). **A-M-2** (the un-audited SpringTarget twin) is re-classed: a corpus-method datum, not a tree defect — its yield is already distributed (kills K-5/K-6, the D-6 proof upgrade, the D-4 re-frame); recorded in the corrections register with A's MINOR as dissent. |

**Adjudicator's corrections register:** (1) against reader-A — the C-1 throttle-precedent adoption inherited C's misquote (ruling 7 / K-11; the cross-reader inheritance event this program's DISSENT sections exist to catch); A-M-2 re-classed method-datum. (2) against reader-B — MISS-1's "corrects those two banked cells" softened to EXTENDS (the exonerations ruled on lifecycle and capture-guarding, axes on which they remain true); the six-hosts cell in L·D-1 (B self-corrected; verified: ONE shared handle-seam instance). (3) against the challengers — fourteen kills below. (4) against the bank — the kf-AnimationVisualizer.md:9 F-1 census cell corrected to worktree-only (ruling 3); N-18 enriched and re-graded on new evidence (ruling 6). Credits: B's suppression-strand find and its 4.606:1 contrast derivation (both survived this seat's re-derivation); A's italic-mechanism kill and off-rung cure kill (both survived); both readers' concordant twin-comparator kills; C's ScrubberTimeline contract table (verbatim-accurate, superlative 8); L's two published self-downgrades (superlative 8).

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the drag-seam packet + the sequence scene-repair packet). KF-AV-28's standing rider applies: the KF.W7 ScrubberTimeline evaluation gates the component-shape rows.

### MAJOR

- **KF-SCR-1** *(NET-NEW — reader-B MISS-1, ruling 1; verified whole at the bytes by this seat)* — **two pointers on the master rail permanently strand the demo's global select-suppression token**: `acquireSelectSuppression()` unconditional per `pointerdown` (useDragScrub.ts:113-116, no re-entrancy guard) vs release gated behind the shared `dragging` boolean (:136-147 early-returns) → acquire ×2 / release ×1 → `activeGestureCount` pinned ≥ 1 → `user-select: none !important` + transparent `::selection` document-wide, forever (design-idioms.css:292-300). Triggers: two-finger touch or pinch on `.seq-scrub` (pinch PERMITTED — the D-5 compound); mid-drag unmount (scope teardown removes the only release path — non-self-healing, unlike banked kf-CubeTarget #54). The seam's docblock claims the opposite ("Nesting-safe", :36-38). Class ≡ banked **KF-AV-15** (`useDragCapture`); this seam twice exonerated on OTHER axes (kf-OrbitalDrag.md:31, kf-CubeTarget.md:54-55) and never on this one. Cure: one guard line + `pointerId` latch + `onScopeDispose` release, at the seam. Live one-gesture probe → SS-13 #1. **NO-WAVE-OWNER (drag-seam packet, spec'd with KF-AV-15).**
- **L·D-1** *(first filing against `useDragScrub` itself; class banked house-wide — kf-AmigaScene.md:28, KF-AV-15)* — **multi-pointer cross-talk**: no `pointerId` latch (`:120` is the file's sole occurrence), so while one pointer holds a rail, any second pointer's `pointermove` is projected onto that rail (`e.clientX` against the rail rect, Scrubber:77-82) and its `pointerup` silently ends the first gesture. Exposure: two live seam instances in one viewport (master rail + ONE shared instance for five handles — SequenceTarget.vue:209-230; B's "six hosts" cell corrected, both readers concordant). One cure family with KF-SCR-1. **NO-WAVE-OWNER (drag-seam packet).**
- **C·C-1** — **the scrub drives a localStorage-persisting state machine at raw pointer-sample frequency** — one synchronous `JSON.stringify` + `setItem` per admitted `pointermove` (chain re-walked: Scrubber:69-73 unthrottled → useDragScrub:131-134 → useSequenceDemo:279-286 `machine.dispatch({type:"SCRUB"})` → sceneMachine `scrub()` fresh object per call → useSceneMachine:169-181 unreachable echo-guard → `useStorage` no-options `flush:'pre' deep:true`). **FOLD ≡ banked KF-AV-16** (whose scoping clause pre-names the pointer path); this corpus is that clause's independent second derivation. C's throttle-precedent cell KILLED (K-11 — misquote); the honest cure statement: `useDragScrub` offers no throttle and `useThrottledReadout` gates readout mirrors, not machine dispatches — the throttle/decouple decision is the KF-AV-16 spec's. Magnitude → SS-13 #7. **NO-WAVE-OWNER (≡ KF-AV-16's row; one spec, both consumers).**

### MINOR

- **D·D-1** *(BLOCKER killed twice — booking ≡ banked N-18, severity ≡ K-6 lineage; grade ruled MINOR on new evidence, ruling 6)* — the master ball and the phosphor playhead ride two differently-inset rails: Δ(p) = −81 + 98p px, W-independent, **sign-flipping at p≈0.827** (the thumb overtakes the playhead it drives). Cells adopted into N-18: sign-change, 1px-border refinement (97/33 insets), the header-prose contradiction of the banked INFO rationale. Fold home = kf-SequencePlayhead N-18 (anti-rename, per its own identity guard); witness = banked SS-13 #12's side-by-side frame. **NO-WAVE-OWNER (sequence packet, with the instrument-truth core).**
- **C·C-2 / L·D-4** *(C's MAJOR killed — severity, no new mechanism, third generation of the K-31 law)* — the keyboard path is a second, divergent scrub: four bare `demo.scrub()` calls (:97-111), no `setScrubDir`/`setScrubbing`/`lastP` — the ignition cascade and comet direction are pointer-exclusive. **FOLD ≡ banked N-13 + N-3.** Adopted cells: the fresh-page witness (default `--scrub-dir: 1` → the FIRST ArrowLeft renders the comet leading a backward move) → N-3; the keyboard-bloom rider (the local `--ball-glow: 60%` consumer N-13 never names — A-M-5) → N-13. Cure = one `applyScrub(p, {gesture})` helper, landing once for all four consumers (incl. KF-SCR-6's step literal). **NO-WAVE-OWNER (gesture spec).**
- **D·D-2 (core)** *(italic mechanism KILLED at the font inventory — ruling 4; cure KILLED off-rung — ruling 5; B's MAJOR as dissent)* — the eyebrow's twice-stated "Fira Code, letter-spaced" rationale is false about its own file (renders upright Plus Jakarta); the demo's sole hand-rolled micro-cap; `0.18em` off-ladder vs `--type-tracking-caps: 0.1em`; the shipped idiom (`text-mono-caption` / `.section-label`) is consumed by this very file five lines later. **NO-WAVE-OWNER (prose-truth sweep + one-class swap; rung decision per ruling 5).**
- **D·D-3** *(narrowed to the prose; C's §4 dismissal half-killed — K-10)* — the file's twice-stated "brightest number on the page" is falsified by its own parent's unconditional `<Metric size="xl">` at `--type-display-3` (41.9-67.8px vs caption 12-16px — non-overlapping at every viewport). The three-notation census folds ≡ banked N-14's M-6 rider. The register split itself is defensible (C right); the prose is not (C wrong). Register percept → SS-13 #10. **NO-WAVE-OWNER (prose sweep).**
- **D·D-5 / L·D-5 / C·C-3** *(two MAJORs killed; L's self-downgrade adopted as the corpus's best-reasoned severity; the "only drag surface" cells killed at K-6)* — `.seq-scrub` declares no `touch-action`; inherited `manipulation` permits pan AND pinch; `pointercancel` → silent mid-drag abort; the sibling handle declares it with rationale 20 lines away. The honest census: TWO rails without it (the SpringTarget twin also lacks it) vs handles that declare it — an internal-consistency gap whose manifestation is live-only, **compounded by KF-SCR-1** (the permitted pinch is the strand trigger). → SS-13 #1. **NO-WAVE-OWNER.**
- **D·D-6 / C·C-8** *(proof upgraded by the twin)* — `tabindex="0" role="slider"` with neither `.focus-ring` nor a scoped `:focus-visible`, outside the design system's enumerated forced-colors guarantee — and the near-byte-identical twin rail (SpringTarget.vue:63) **carries `focus-ring` in its class list**: this is the one rail in the demo that dropped the class its twin wears. UA-ring legibility → SS-13 #6. **NO-WAVE-OWNER.**
- **D·D-7** *(B's 4.606:1 derivation adopted — ruling 8)* — a dark-substrate phosphor `text-shadow` ships ungated into a light-default theme (`color-scheme: light`), blurring 12-16px tabular figures at ~4.6:1 (AA on thin headroom). Chain verified to `--accent-kf`'s `light-dark()`. Percept → SS-13 #8. **NO-WAVE-OWNER (with the banked D-7/K-1 family's material-register decision, KF.W6-adjacent).**
- **D·D-8** — the live-scrub state is signalled by box-shadow strength alone (35%→60%, +71%, zero `transition` — a step, not the prose's "bloom"), nulled under forced-colors. Forced-colors limb **FOLD ≡ banked D-16 (KF.W9)**; hard-step limb classed with banked D-12; keyboard-unreachability rider → N-13. **KF.W9 + NO-WAVE-OWNER (transition pair).**
- **D·D-10 / L·D-7 / C·C-6** — the load-bearing idiom citation points past EOF: `design-idioms.css:584` in a **300-line** file (`--ball-glow` at :185). Five concordant derivations (three axes + both readers); unbanked; the comment is doing governance work with a dead pointer. **NO-WAVE-OWNER (prose sweep).**
- **C·C-5 / L·D-3** — one boolean, three homes: the local `scrubbing` ref is a byte-exact synchronous shadow of the seam's own returned `dragging` (set/cleared before the hooks fire — timing identity verified), with `demo.isScrubbing` a third copy of the same edge — the exact duplication the seam's docblock says it was extracted to end (:14-19). Unbanked. **NO-WAVE-OWNER.**
- **L·D-6** — the component re-renders every animation frame during autoplay: `toFixed(3)` string alloc + inline-style patch + **an `aria-valuenow` attribute write at 60-120Hz on a focusable `role="slider"`** — the per-frame-Vue limb **FOLDS ≡ banked D-5/L-8/C-4 + K-2**; the aria-attribute limb is net-new (no banked row states it) and is why a bare custom-property cure would not suffice here. AT announcement storm → SS-13 #5. **NO-WAVE-OWNER.**
- **L·D-10 (+ D·D-11 / C-aria)** — clamp-ownership asymmetry inside one 20-line template (`aria-valuenow` bare at :24, transform clamped at :34 — net-new) + no `aria-valuetext` (what you see, `0.470`, is not what AT hears, `47`) — the valuetext half **FOLDS ≡ banked N-14** including its sequencing law: **the cure is BLOCKED on N-1's domain decision** (no honest millisecond to announce until the scene picks a clock). **NO-WAVE-OWNER (sequenced after N-1).**
- **C·C-4** *(MAJOR + prop/emit cure killed at K-8; SUP-5 sustained head-on; A's INFO as dissent)* — narrowed to: the three colocated sub-units split by one rule landed on two contracts with the divergence nowhere declared, and `inject(SEQUENCE_DEMO_KEY)!` erases the missing-provider signal (runtime `TypeError` in a render function — the class the tree carries a scar comment about). **NO-WAVE-OWNER (one-line guard; the contract question belongs to KF.W7's evaluation).**
- **KF-SCR-2** *(NET-NEW — A-M-1, ruling 10)* — **`font-synthesis: none` (style.css:100) permanently voids glass-ui's `text-caption` italic register across the whole demo** (8 consumer sites; no italic face shipped; synthesis forbidden; glass declares `font-synthesis` nowhere). Mechanism law ≡ banked KF-EST-2 (transient there, PERMANENT here); unbanked for this family. Rendered witness (glyph comparison / `document.fonts.check`) + the utility-ordering courtesy check → SS-13 #3. **KF.W6 (the font decision: declare the synthesis dependency, ship an italic face, or retire italic from the register) + glass-ui BH relay (owner edict).**
- **KF-SCR-3** *(NET-NEW — A-M-3, ruling 10)* — the 36px protagonist ball seated in a 36px row with zero clearance, against the written h-12 rule one scene over (SpringTarget.vue:334-339); the idiom glow reaches exactly to the Card's clip edge. Percept → SS-13 #9. **NO-WAVE-OWNER (geometry, with the sequence packet).**
- **KF-SCR-4** *(NET-NEW — A-M-4, ruling 10)* — `text-mono-caption` on a digits-only readout: inert `uppercase`, caps tracking (0.1em) loosening a "counter", and no `line-height` → two different line boxes in one flex row (1.3 vs 1.5). Family banked (MM-29 / KF-ES-8 / kf-SequenceAxis relay) — instance + line-height limb new; cure with the family. **NO-WAVE-OWNER (typography family row).**

### INFO

- **C·C-7 / D·D-4 / L·D-11** *(D's MAJOR + "the sibling wrote the fix down" frame KILLED at K-5 — the centre-anchor is deliberate cross-scene protagonist posture, byte-identical + documented at SpringTarget.vue:333-346, and the registry already rejected the reverse swap at KF-AV-17's K-12; B's MINOR as dissent)* — one idiom, two anchoring models (centre-anchored master over bare `100cqw` vs inset-anchored travellers); the divergence half **FOLDS ≡ banked N-4/N-18**; the local residue — a now **statically-proven** ~2px clip at p=0/p=1 (B's discharge: Card padding = 0, `.card` declares only `--card-pad-*` vars, `CardHeader` consumed nowhere ≡ banked ME-44) plus the amputated outboard glow — stays INFO; percept → SS-13 #2. **NO-WAVE-OWNER (residue rides the packet's geometry decision).**
- **C·C-11** *(C's "provably in [0,1]" justification cell killed at K-12 — provably [0,1] ∪ {NaN}, banked ruling 8)* — the template re-clamp is a redundancy; the `clamp` import stays load-bearing for `project()`. Family ≡ banked L-9/C-6. **NO-WAVE-OWNER (property-hygiene family).**
- **C·C-12** — `p >= lastP` reports FORWARD on a zero-delta sample ≡ **banked N-3** (no hysteresis); the deadband cure cell (`if (p !== lastP)`) adopted into N-3. **NO-WAVE-OWNER (with N-3).**
- **L·D-8 / C·C-10** *(C's WebKit-focus cure rationale killed at K-13 — uncited, and not ruled inversely: no static seat rules a UA focus behaviour)* — `onScrubDown` is a pure identity wrapper (the sibling's same-shaped wrapper earns its seat with a latch); whether the seat NEEDS a `focus()` is SS-13 #4's question, tested on both rails. **NO-WAVE-OWNER.**
- **L·D-9** — the `project` null-ref fallback is unreachable defensive code whose failure mode, if reached, is to PAUSE playback (`demo.scrub` opens with `if (isPlaying) pause()`). Adjacent to the banked four-site NaN census. **NO-WAVE-OWNER.**
- **L·D-12** *(B's MINOR as dissent)* — zero automated coverage: no spec mounts this component, no gate names it (`grep test/ scripts/` → empty; sequence-scene.test.ts covers the instrument refs only); the keyboard divergence is exactly what a mount test reds on run one. Class ≡ **banked N-10**, second instance in the scene. Rider: the sibling's `proof:sequence-rows-draggable` clause's liveness is baseline-dependent (`proof:structure` deleted in the worktree edit) → the KF-EST-1 dead-`proof:` family, not this row. **NO-WAVE-OWNER (the packet's born-RED mount test).**
- **C·C-13 / L·D-2 (surviving refinement)** — a component with ZERO glass-ui imports is fully glass-coupled through the `@import` cascade (`text-caption`/`text-mono-caption`/`text-muted-foreground`/`border-border` + `--radius-pill`, sole definer glass theme/radius.css) — an import-graph census scores it clean and is wrong. **FOLD ≡ banked kf-SequenceAxis L-5/C-7 legend rider → KF.W0 §B-12** (the import-coupled vs cascade-coupled census column). The F-1 consequence is dead (K-1). **KF.W0 fold.**
- **D·D-12 (residue)** *(both original limbs killed at K-7)* — `tnum` is authored twice (`tabular-nums` class + `font-feature-settings: "tnum" 1`) with identical value; `lnum` survives via the class (feature-settings overrides only the tags it names), so the class is NOT inert and nothing is dropped — the residue is one duplicate declaration. **NO-WAVE-OWNER (hygiene).**
- **KF-SCR-5** *(NET-NEW — B-MISS-2, ruling 10)* — `releasePolicy`/`onRelease` is dead API on the shared seam (sole consumer passes the default; `"recenter"` zero consumers; the branch unreachable) and three docblock paragraphs describe the PRUNED MotionPath scene as a live consumer, with ":26-27 the rail scenes leave them unset" false about this very file. **NO-WAVE-OWNER (prose-truth sweep, seam tier).**
- **KF-SCR-6** *(NET-NEW — B-MISS-4, ruling 10)* — the keyboard step is a bare `0.05` literal twice, against the same Card's named `ROW_AT_STEP = 40`; two undeclared arrow-key granularities (5% vs 2.5%) in one card. Cure inside C-2's `applyScrub` helper. **NO-WAVE-OWNER.**

## Killed-claims register — 14 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **L·D-2 (MAJOR) + C-13's F-1 re-confirmation + D §7's "Unchanged and blocking" + D §8 step-7's F-1 gate** — "`@mkbabb/glass-ui` is a phantom dependency, absent from package.json AND package-lock.json" | **ONE-BASELINE-SHORT — the sixth on record.** All three axes read the DIRTY worktree. This seat's probes: origin/master declares 7.0.0 exact (:77) with 3 lockfile hits, matching installed; HEAD declares 6.0.0; the absence exists only in an uncommitted local edit. L·D-2's own falsifier (c) fires. Precedent ≡ kf-ChromeDock.md:40 frontier-CURED. Survives: the cascade-coupling refinement (C·C-13). Dies: every "blocking"/sequencing/post-`npm ci` consequence in all three axes, and the banked kf-AnimationVisualizer.md:9 census cell is corrected to worktree-scope |
| **K-2** | **D·D-1's BLOCKER** ("the component's primary affordance failing its stated purpose") | Booking kill ≡ **banked N-18** (same two elements, same insets, same magnitudes; the SequencePlayhead identity guard pre-routes this component's fold by name) + severity kill ≡ the K-6 lineage — both readers concordant. The arithmetic SURVIVES and enriches N-18 (sign-change cell, 1px-border refinement); the grade lands MINOR on the header-prose evidence (ruling 6). BLOCKER stands nowhere |
| **K-3** | **D·D-2's italic mechanism** ("UPPERCASE ITALIC Plus Jakarta at 0.18em — the textbook faux-small-caps failure") | **THE EYEBROW IS NOT ITALIC** — this seat's own inventory: 4 shipped faces, all `font-style: normal`, zero italic; no external Jakarta sheet; `font-synthesis: none` at `:root`; per CSS Fonts 4 §5 + `font-synthesis-style: none` the eyebrow paints upright under EITHER `text-caption` resolution. D's falsifier searched only demo sheets; the killer was a font-face inventory + one `:root` declaration. Mechanism law ≡ banked KF-EST-2. (Reader-A's kill, sustained on this seat's bytes) |
| **K-4** | **D·D-2's cure** ("`text-admin-label`, delete `.seq-eyebrow`") | **OFF-RUNG**: `--type-admin-label: 0.625rem` FIXED 10px vs `--type-caption` fluid 12-16px (both read at scale.css this session) — the swap silently drops the eyebrow off the fluid ladder. The rung-exact cure is `text-mono-caption` (verbatim the comment's stated intent, already consumed at :16) or `.section-label`. Reader-B's approving `text-admin-label` cell narrowed (the idiom exists; the rung is wrong) |
| **K-5** | **D·D-4's MAJOR + "the scene already knows the fix and wrote it down"** (adopt the travellers' inset formula) | The centre-anchored master ball is a **deliberate cross-scene protagonist posture** — SpringTarget.vue:340-346 is byte-identical under a comment stating it deliberately (:333-339); the inset form is scoped to the small travellers; the registry already rejected the reverse swap (KF-AV-17's K-12: the full-width mapping is "the K-8 decider", not portable). D's cure would break the protagonist idiom in two scenes to fix a 2px clip in one. The 2px arithmetic survives at INFO (statically discharged) |
| **K-6** | **D-5's "the ONLY drag surface without touch-action" / C-3's "7 of 8" / L-D-5's "every other pointer-capture host declares its posture"** | **ALL THREE ROSTERS OMIT THE SAME FILE**: SpringTarget.vue:62-72 is a `useDragScrub` rail with NO `touch-action` (my census re-run: nothing under demo/scenes/spring/). Two rails without it vs handles with it = the rail-vs-handle split, which also kills K-9's row. The absence itself survives at MINOR — where L had already landed it |
| **K-7** | **D·D-12's "silently drops lnum" + L's-implied "the class is inert"** | Wrong per CSS Fonts 4 feature resolution: `font-feature-settings` overrides `font-variant-*` only for the tags it names; `"tnum" 1` names nothing else, so `lnum` from `tabular-nums`' `lining-nums` survives — and because the class is `lnum`'s sole source it is NOT inert; the axis's own removal-falsifier is unsatisfiable. Residue: one duplicate `tnum` (INFO). Both readers concordant |
| **K-8** | **C·C-4's MAJOR + "A `{progress}` prop + scrub/scrub-start/scrub-end emits is the honest contract"** | The scrubber consumes THREE writer verbs the parent does not own; the flip relocates coupling into pass-through ceremony the KISS posture forbids; C's own §3 gates the eventual contract on a KF.W7 swap decision. Head-on with L's SUP-5, which is PRESERVED. Row survives narrowed at MINOR (`!` posture + undeclared asymmetry); A's INFO as dissent |
| **K-9** | **D·D-9 / C·C-9** — "cursor semantics contradict the affordance and the same scene's other slider" | **CATEGORY ERROR** — a consistent RAIL-vs-HANDLE cursor convention: both `useDragScrub` rails are `cursor-pointer select-none` (spring rail verified :63); `grab`/`grabbing` appears only on `.seq-handle`. 2-for-2 rails, 1-for-1 handles; both rails support click-to-seek, which is what `pointer` announces. Whole rows dead |
| **K-10** | **C §4's dismissal cell** — "the two-units-in-one-Card observation … a deliberate register split, not a defect" | HALF RIGHT, wrong half kept: the register split is defensible, but C declined without reading the prose that claims the apex for the SMALLER register ("the brightest number on the page", twice) — falsified at every viewport by non-overlapping type ranges. D-3 confirmed on that narrowed ground |
| **K-11** | **C·C-1's "its own docstring names `useSequenceDemo` as a lapsed consumer" — AND reader-A's adoption of it as "one adoptable new cell"** | **MISQUOTE**, verified at useThrottledReadout.ts:5-10: "historically … before their readouts were removed" records removal, not lapse. B's kill sustained; A's adoption is a citation-inheritance event ACROSS re-readers — the same failure class the ColorSpaceSelector DISSENT documented, now observed reader-to-reader. B's sharper residue adopted: the docstring's "removed" is itself stale about the live per-rAF mirror |
| **K-12** | **C·C-11's justification cell** — "Provably in `[0,1]` at the source; every other reader trusts it" | Provably `[0,1] ∪ {NaN}` — banked ruling 8's four NaN-transparent clamp sites include this component's own `project()` 0/0. Right verdict (redundant), wrong reason; the cell dies so the wrong reason is not cited downstream |
| **K-13** | **C·C-10's cure rationale** — "a `<div tabindex=0>` does not receive focus on mouse-down in Safari (the documented WebKit behaviour)" | Killed as **uncited** (C's stated falsifier tests a different proposition) and contested by reader-B's reading of the WebKit rule — but NOT ruled inversely: no static seat rules a UA focus behaviour. One live keypress-after-mouse-scrub, both rails → SS-13 #4. The identity-wrapper row survives at INFO |
| **K-14** | **The MAJOR grades on C-2 (keyboard bypass) and C-3/D-5 (touch-action)** | Severity kills, no mechanism disturbed: C-2 is banked at MINOR (N-13/N-3) with only restated impact — the K-31 law, third generation; the touch row's consequence is stipulated-unproven by two of the three filing axes (L's own ancestor-overflow walk + C's concession), and a finding whose consequence its own filers stipulate unproven cannot carry MAJOR |

## Superlatives (provenance preserved; L-18 both ways)

1. **Parameter consumption instead of idiom re-authoring — the corpus's cleanest instance** (D S+1 / L SUP-1 / C S-C; all three axes + both readers). Three lines lift `--ball-glow` to 60%; the idiom keeps authority over the shadow shape (design-idioms.css:185, promoted "was authored four ways"). **STRENGTHENED by this seat**: C's exclusivity cell is dead THREE times over — `--ball-glow` has writers at SequenceScrubber:149 (60%), SpringTarget:350 (**0%**, "the sweep sampler is a quiet translucent marker") and SpringTarget:438 (**30%**, this seat's own find beyond A's correction) — the parameter is genuinely generic, which is what a promoted parameter is supposed to look like.
2. **Leak-free by construction** (L SUP-2; both readers) — zero disposable acquisitions in the leaf; teardown lives at the layers that can discharge it. **Mechanism corrected by reader-B's dist read and adopted**: `useEventListener` = `watchImmediate` with `onCleanup`, scope-registered at setup — real teardown by a different mechanism than the comment claims (reconciling the banked kf-AnimationVisualizer doubt, which applies to registration inside event callbacks, ≡ kf-CubeTarget #54). **Qualification (B's, adopted)**: this same honest cleanup is what makes KF-SCR-1's unmount limb unrecoverable.
3. **Compositor-only travel via container queries, scene-wide, zero drift** (D S+3 / L SUP-3 / C S-D) — `container-type: inline-size` + `translateX(cqw)` + an EARNED `will-change`. Plus L's uncredited containment bonus, verified reasoning: `inline-size` containment computes `contain: layout style inline-size`, making `.seq-scrub` the containing block for its abspos children — the anchoring is well-defined by containment; `relative` is belt-and-braces. No other reader in the corpus reached it.
4. **Reports the GESTURE, never the motion — inv ζ honoured at the leaf** (L SUP-4 / C S-B) — a 162-line file drives a five-lane detonation and contains no clock; the one grep hit for a timing primitive is the comment naming the invariant (:59). The temptation refused (a chase animation, an eased catch-up) is named in the comment that refuses it.
5. **Zero literal colour, and the inheritance claim survives audit** (D S+2) — `--ball-tone → --color-progress → --accent-kf` terminates cleanly; the per-row overrides live on a sibling subtree and cannot leak. The class of comment that is usually wrong, and it is right.
6. **The micro-cap is CSS, not shouted into the DOM** (D S+4) — source text `master playhead`, presentation-layer uppercase; AT receives natural casing; survives every charge D-2 brings (D-2 indicts the family and tracking, never the mechanism).
7. **Hit-target ≡ measurement frame ≡ capture target, by construction** (reader-A's uncredited find, verified) — both idiom children are `pointer-events: none`, so every `pointerdown` lands on the element `project()` measures and the seam captures; no path on which the rect read and the event target disagree. The registry rates the alternative: KF-AV-13 books the sibling's dead press-to-seek and names THIS file the counter-example done right.
8. **The corpus itself, both directions.** C's ScrubberTimeline contract table is verbatim-accurate against the installed d.ts (routes via **banked KF-AV-28**, the KF.W7 intake that already names this component: EVALUATE, not swap; C's `Slider`-strike concurs with the banked reasoning). L's two published self-corrections — the touch-action MAJOR→MINOR downgrade with the falsified reasoning left visible, and the 17-path falsifier repair — are the epistemics the K-29/K-30-class ledgers exist to reward, and L's D-5 severity was the one this seat adopted over two MAJORs. D's contrast table is the family's first to survive independent re-derivation (ruling 8). And the failure mode is symmetrically visible: D built its BLOCKER and its §8 gate on an unread baseline and an unrun font inventory; C filed the seam that strands the page as a superlative (S-E); all three axes reached past the decisive twin comparator to a handle with a different role — A-M-2's method lesson, adopted: **a "sole/only/every-other" claim is a claim about a roster, and the roster is part of the claim** (≡ the ColorSpaceSelector L-9.1 law, second program-level confirmation).

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The one-gesture strand probe** — two-finger pinch on `.seq-scrub` at 375×667 iOS Safari: does `body.is-dragging` stick (KF-SCR-1's witness — the highest-value probe on this list), and does visual-viewport rubber-band fire `pointercancel` mid-drag (D-5's residue)? One session settles both.
2. **The 2px clip + amputated outboard glow** at p=0/p=1 — `getBoundingClientRect()` on `.scrub-ball` vs the Card's padding edge; percept only (the clip is statically proven). Same frame as banked SS-13 #12 (N-18's side-by-side).
3. **`text-caption` paints upright** (KF-SCR-2) — glyph comparison or `document.fonts.check('italic 1rem "Plus Jakarta Sans"')`; computed `font-style` will still report `italic`, so the pixel is the witness. Plus the Tailwind v4 ordering courtesy check (which `.text-caption` definition wins) — immaterial to the verdict.
4. **Safari mousedown focus on `tabindex="0"` divs** — keypress-after-mouse-scrub on BOTH `.seq-scrub` and `.spring-rail`; a shared gap changes the cure's home (decides K-13's open question).
5. **AT announcement churn** — does a screen reader announce the 60-120Hz `aria-valuenow` writes on a focused slider during autoplay (L·D-6's sharpest edge)?
6. **UA default focus-ring legibility** on the radius-less rail, both themes (decides whether D-6/C-8 stays MINOR).
7. **`setItem` magnitude** during a full-rail master scrub (C-1, under banked KF-AV-16's trace obligation).
8. **D-7's light-theme percept** — the 8px glow over 12-16px glyphs at 4.606:1 (the byte ledger is closed; the looking question is not).
9. **KF-SCR-3's bottom-glow clip** — capture at rest and at `.is-scrubbing` (`--ball-glow: 60%`).
10. **D-3's register question** — do `47%` at display-3 and `0.470` at caption read as one clock or two (sizes source-decided; the reading is not).
11. **N-3's first-press witness** on this rail — fresh page + ArrowLeft: comet leading a backward move (the adopted cell's photograph).
12. **The `proof:sequence-rows-draggable` liveness** — baseline-dependent (`proof:structure` present in origin/master's `check`, deleted in the worktree edit) → KF-EST-1 family, flagged for whichever seat rules that family next.

## Identity guards (fold-by-reference — never re-booked)

- **D·D-1 ≡ banked N-18** (kf-SequencePlayhead.md:88; its :144 identity guard pre-routes this component's geometry findings there by name) — cells adopted, grade moved INFO→MINOR on new evidence (ruling 6), id and home unchanged.
- **C·C-2/L·D-4 ≡ banked N-13 + N-3**; **C·C-12 ≡ N-3**; **the valuetext half of L·D-10/D·D-11 ≡ N-14** (with the N-1 sequencing law); **L·D-6's per-frame limb ≡ banked D-5/L-8/C-4 + K-2**; **L·D-12 ≡ N-10's class**; **C·C-7's divergence half ≡ N-4/N-18 + KF-AV-17's K-12**; **the NaN cell ≡ banked ruling 8's family (D-11/L-6/C-7 + L-9/C-6)**.
- **C·C-1 ≡ banked KF-AV-16** (second independent derivation of its own scoping clause). **KF-SCR-1's class ≡ KF-AV-15** — one seam-packet spec covers both seams; the kf-OrbitalDrag/kf-CubeTarget exonerations of `useDragScrub` remain true on their own axes and are EXTENDED, not corrected.
- **C·C-13 ≡ banked kf-SequenceAxis L-5/C-7 legend rider → KF.W0 §B-12**; the F-1 substrate ≡ census F-1/SCH-1 ≡ kf-ChromeDock L/B-1 (frontier-CURED — this record is that correction's second firing).
- **KF-SCR-2's mechanism law ≡ banked KF-EST-2** (new family instance, permanent variant). **KF-SCR-4's family ≡ MM-29 / KF-ES-8 / the kf-SequenceAxis relay.** **The Card-padding discharge ≡ banked ME-44** (CardHeader consumed nowhere).
- **The ScrubberTimeline question ≡ banked KF-AV-28 (KF.W7 intake, names this component)** — no row of this corpus re-opens it; C's §3 table rides it as evidence.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold, corrected to frontier-CURED (§B-12) · the cascade-coupled census column (C·C-13 via the banked legend rider).
- **KF.W6**: **KF-SCR-2** (the italic/synthesis decision — declare, ship, or retire; + the glass-ui BH relay per the owner edict) · D·D-7's material-register adjacency.
- **KF.W7**: the ScrubberTimeline evaluation ≡ KF-AV-28 (C's contract table + `Slider`-strike as evidence; C-4's contract question and the D-5/D-6/D-11 "the primitive already solves it" arguments discharge or persist with its verdict — the standing rider).
- **KF.W9**: D·D-8's forced-colors fold (≡ banked D-16) · the SS-13 list · the AT churn row.
- **NO-WAVE-OWNER — the drag-seam packet (SS-1/SS-2 authoring must home it):** **KF-SCR-1 + L·D-1 as ONE cure family** (re-entrancy guard + `pointerId` latch + scope-disposal release + the "Nesting-safe" docblock corrected), spec'd with KF-AV-15 across both seams; **C·C-1** spec'd with KF-AV-16 (throttle-vs-decouple at the machine seam, one decision for pointer + coast); **KF-SCR-5**'s dead-API/prose cleanup rides the same seam edit.
- **NO-WAVE-OWNER — the sequence scene-repair packet (joins the banked kf-SequencePlayhead packet):** the **gesture spec** — one `applyScrub(p, {gesture})` landing C-2/L-D-4 + KF-SCR-6 + the N-13 bloom rider + C-12's deadband at once · the **geometry residue** — D·D-1's N-18 cells, C-7's clip decision, KF-SCR-3's vertical seat · the **typography pair** — D·D-2's one-class cure (rung per ruling 5) + KF-SCR-4 (with its banked family) · the **prose-truth sweep** — the two Fira-Code comments, the two brightest-number claims, the `:584` citation, KF-SCR-5's three MotionPath paragraphs + "rail scenes leave them unset", the header's "editable CONTENT" claim (with N-18's naming decision) · **hygiene** — C-5/L-D-3's one-boolean-three-homes, L-D-8's wrapper (pending SS-13 #4), L-D-9's fallback, C-11's re-clamp, D-12's duplicate `tnum`, D-8's transition pair · **a11y** — L-D-10's clamp-ownership + the N-14-sequenced valuetext + L-D-6's aria-write limb + the NaN-projector guard (ruling-8 family cure shape) · **coverage** — L·D-12: the packet's born-RED mount test (keyboard parity + direction latch + projection; N-10's class).

## Verdict

**ADJUDICATED.** The incredulity seat cut in all three directions this generation. Against the challengers: fourteen kills — a BLOCKER dead twice over, a cross-axis F-1 premise that existed only in an uncommitted worktree (the sixth one-baseline-short event), an italic mechanism dead at a four-face font inventory, two whole rows dead on a rail-vs-handle convention, and three "sole/only/every-other" rosters that all omitted the same twin file none of the three axes opened. Against the readers: one citation-inheritance event caught crossing between them (the throttle-precedent misquote, adopted by one reader as "verified" after the other had killed it at the docstring), one counting cell and one over-claimed correction repaired. Against the bank: one census cell corrected to its true scope, one banked row enriched and re-graded on evidence its seat never saw. And for the tree: the single largest defect in the component's orbit — a two-finger gesture that freezes text selection document-wide for the life of the page, inside the seam the registry twice praised, under a docblock that promises the opposite — entered the record only at the second reading generation, verified whole at the bytes by this seat. The component underneath remains the corpus's best-behaved leaf: idiom consumed by parameter, no clock, no teardown surface, no literal colour, hit-target and measurement frame one element by construction — with prose that lies about its font, its brightness, and its citations, and a gesture contract that is pointer-only four separate ways. The repair is two packets and a sweep, not a rewrite. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life, KF-SCR-1..6 for life.

— end of apotheosis r1. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/sequence/SequenceScrubber.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SequenceScrubber.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**K-1's ONE-BASELINE-SHORT kill is a C-19 firing and its COROLLARY is executed**: the banked `kf-AnimationVisualizer.md:9` census cell is corrected to **worktree-scope** by appended note at that bank (correction (i) of the six).

**C-9's provenance is corrected at the wave and recorded here**: the banked row is `:71` *"C·C-13 / L·D-2 (surviving refinement)"*, restated at `:126`. The earlier "D-11/C-13" tag named a different family in this record — `D·D-11` at `:57` and `L·D-11` at `:65`, routed to KF.W7/W9 and never to KF.W0.

**`L·D-2`'s MAJOR half is BOOKED, not dropped**: *"a component with ZERO glass-ui imports is fully glass-coupled through the `@import` cascade … an import-graph census scores it clean and is wrong."* That is the surviving half; the phantom-dependency arm died at this record's own K-1.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

