claude-fable-5 (served model id)

# fr-AnimationControls — adjudicated apotheosis (M-23/L-18 fresh-adjudicator seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject**: `fourier-analysis/web/src/components/visualization/AnimationControls.vue` (224 lines) · fourier `m/w1-bump-migration @ cd26c65` + the frozen M.W1a dirty WT (re-confirmed dirty by this seat) · value.js `tranche-u @ c654824e` · installed glass-ui 4.0.0 / keyframes.js 4.3.0 / value.js 0.13.0; producer glass-ui at 7.0.0.
**Corpus adjudicated**: three axis challenges (`audit/fourier-components/AnimationControls/challenge-{D-design,C-consumption,L-library}.md`, all read whole by this seat) × two hostile re-reader returns (design-and-usage · library-and-code, both `claude-fable-5`-era returns received in-brief).
**Method (M-23)**: every verdict-level disagreement between the two re-readers was re-derived against the tree with this seat's own instruments before ruling; every load-bearing kill was re-attacked (one kill-of-a-kill probe fired and did NOT land — see the D-6 row); two re-reader claims were killed by this seat itself (KX-16, KX-12). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings below are agglomeration input (M-25), never wave stamps.
**This seat's own instruments (all static + read-only; zero browser — probe parsimony)**: full reads of AnimationControls.vue, GlassTimeline.vue (127), EasingPicker.vue (98), SpeedSelect.vue (70), stores/animation.ts (146); ranged reads BasisCanvas.vue:130-244, FullscreenViewer.vue:80-160, VisualizationView.vue:95-124 + :206-250, ConvergenceTimeline.vue:36-62, useWorkspaceLoader.ts:55-60 + :112-121, canvas-drawing/trail.ts:40-75, svg-fourier.ts:125-145. Producer reads at the installed pin: dock.js:578-584 (`inheritAttrs:!1`) + :676-703 (frame root vs `$attrs` inner + the `position` ternary), glass-ui.css `.slider-thumb` rules (standard = `width:0;opacity:0`; only `[data-variant=spectrum]` paints one), dropdown-menu-BJ7E9js_.js (`forceMount:{type:Boolean}` ×2, `DropdownMenuPortal`), reka MenuContent.js (Presence-gated), reka SliderRoot.js:104-178 (commit gating + keyboard `commit:true` routes), SliderImpl.js (keydown/pointerdown/pointermove/pointerup ONLY — no `pointercancel`/`lostpointercapture`), ui/slider/index.d.ts (`variant?: "standard"|"spectrum"`, "NO VISIBLE THUMB AT ALL"), custom/timeline/GlassTimeline.vue.d.ts:30-85 (scrubber props + **`scrubStart`/`scrubEnd` emits at :84-85**), styles/index.css:195-224 (the `@source` block whole), typography/utilities.css:50, styles/dock/{layers,overflow,shell}.css (`--dock-max-inline-size` 2+4+2 occurrences). Measurements run by this seat: `npm ls @mkbabb/value.js` → ELSPROBLEMS exit 1 (verbatim reproduction); `npx vue-tsc --noEmit -p tsconfig.json` → **exit 2**, exactly one diagnostic (`PaperView.vue(12,8) TS2882`); `prefers-reduced-motion` census 15 occurrences / 12 files; `"fourier-epicycles"` fanout 28; FA pause-path literal in exactly 2 files; `dock-max-inline-size` in src → 0.

## Scoreboard

**54 corpus rows ADJUDICATED-CONFIRMED (21 MAJOR · 36 MINOR · 12 INFO counting the 15 missed rows below) · 17 killed-register entries · 15 missed rows booked · 0 BLOCKERs survive as booked.**

All four BLOCKER demotions are RATIFIED: C-1 and C-2 (both re-readers) plus **D-1 and D-4/L-2/C-3 (this seat's rulings — see the disagreement table)**. Two blocker-weight clusters are named so severity arithmetic does not hide them: **transport-truth** (L-1 freeze-while-painting-pause + C-2 direction reset + MISS-1's fabricated epicycle `N`) and **dead-contract** (D-1's unreachable width rule + D-4's four dead fullscreen bindings, shipped ~4.5 months through a same-block edit and a green-gate commit).

## Ruled disagreements (the adjudication's actual work)

| seat | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-1 severity** | BLOCKER | MAJOR | **MAJOR** | Mechanism unanimous and re-sealed by this seat (dock.js:582 `inheritAttrs:!1`; :678-681 root = `.glass-dock-frame`; :683-696 inner `.glass-dock` takes `t.$attrs` + `{expanded}`; Vue scope-id lands on the child's subtree root only → the compound matches nothing). Harm: a proportion contract that never applies — the dock still renders, functions, and is not lying about data. Blocker-weight by load (it invalidates C-S-2 and C-10 and both mount sites), MAJOR by consequence. |
| **D-4/L-2/C-3 severity** | BLOCKER | MAJOR | **MAJOR** | Unreachability re-verified whole (FullscreenViewer.vue:131-139 binds 4 undeclared; :107 `v-if="show"` opaque `.fs-backdrop` :148-153; the non-editing ghost seat is CanvasControlsDock at VisualizationView.vue:210-226). Capability loss confined to fullscreen with an exit-fullscreen workaround → MAJOR. The horn is closed by this seat's own vue-tsc run: exit 2 on ONE unrelated diagnostic → horn (b), silent runtime feature loss. |
| **L-1 mechanism** | thumb-tap route DEAD (no thumb at this pin); freeze survives via track press ±half-step + pointercancel | thumb-tap route cited as the confirmed mechanism | **reader-1 wins** | glass-ui.css: `.slider-thumb[data-v-534634a7]{width:0;…opacity:0;background:0 0;border:none}` is the base rule; only `[data-variant=spectrum]` paints a thumb, and GlassTimeline.vue:67 passes `variant="standard"`; d.ts prose: "NO VISIBLE THUMB AT ALL". A zero-area element is not a hit target. The surviving routes are this seat's re-derivation of reader-1's: (i) track press-and-release within ±half a 1% step — reka `updateValues` snaps to step, `hasChanged` is string-equality on an unchanged model, so `handleSlideEnd` never emits `valueCommit` while GlassTimeline's native `@pointerdown` already ran `startScrub()`→`stopRAF()`; (ii) pointercancel — SliderImpl registers no `pointercancel`/`lostpointercapture` (this seat's grep). |
| **L-1/L-1b permanence** | "confirmed as defect, primary route refuted" | "permanently freezes" and "no reset path" KILLED — next value-changing drag re-enters `onValueCommit` (local flag still true) → `endScrub()` | **reader-2 wins on permanence** | GlassTimeline.vue:53-57 + stores/animation.ts:121-126, read whole by this seat. Additional store nuance neither reader wrote: after a freeze `playing===true`, so `play()` EARLY-RETURNS (:85) — the recovery gesture is pause-then-play, and `scrubbing` strands TRUE across exactly that route (`reset()` :132-135 clears `t`, never the flag). |
| **L-1b severity** | MAJOR | MAJOR (with scope corrections) | **MINOR — this seat demotes against both** | Reachability is triple-conditional: L-1 freeze first, then the pause+play recovery route specifically, and it self-heals on the next value-changing drag. The cost while stranded is real (trail.ts:48 rebuild branch; :60-67 `evaluateFourier` over ≤600 samples/frame when `preX/preY` absent — verified) but a compound-conditional, self-healing perf cliff is MINOR. Both readers' MAJOR preserved in provenance. |
| **C-11/L-4 severity** | MINOR (caret `opacity:0` at rest — its own missed row) | MAJOR "with qualification" | **MINOR** | GlassTimeline.vue:95 `opacity: 0`, :102-105 hover/`data-held`-only reveal — this seat's read. A one-level divergence visible only on hover/drag is MINOR. The always-visible half of the family is MISS-1 (the CANVAS label), which is where the MAJOR lives. |
| **D-6 vs C-14 (direct contradiction)** | D-6 killed, C-14 confirmed | same | **KILL RATIFIED — with a falsifier probe this seat fired and survived** | The `@source` comment at styles/index.css:203-210 says the directive "is a no-op" — a potential kill-of-the-kill. Read whole (:195-224): the no-op clause scopes to glass-ui's OWN source build; in the shipped dist context `../*.js` = `dist/*.js` (the compiled chunks carrying the utility strings), explicitly "a BACKSTOP for a consumer that imports this exact cascade file", with the P9 `components.css` path as primary. `@utility text-mono-micro` exists at utilities.css:50. D-6 stays dead: the size axis lives in child-span utility classes, not in a root CSS rule `text-base` could beat. |
| **D-16 arithmetic** | confirmed "token present at :166, :176, :196" | that cell KILLED — :166 is raw `ease` | **reader-2 wins — verified against the live file by this seat** | AnimationControls.vue:166 reads `transition: opacity 0.3s ease;` (also what both readers' D-17 rows quote — reader-1's D-16 cell contradicted its own D-17). Corrected arithmetic: `var(--ease-standard)` at exactly 2 sites (:176, :196); absent at :155, :166, :191. The drift finding is CONFIRMED and worse than the axis booked. |
| **D-24** | confirmed INFO via "dock is bottom-anchored (`position="fixed"` … `bottom-(--dock-pos)`)" | mechanism KILLED — `position` never passed | **reader-2 wins on mechanism; residue INFO** | AnimationControls.vue:58-63 passes only `class`/`:collapse-delay`/`:start-collapsed`/`:style`; dock.js:699 ternary resolves to `dock-inline`. The dock's bottom placement is the consumers' wrappers (`.controls-overlay`, `.fs-controls`). Residue survives trivially: no explicit `side` at :109, placement rides reka's default + collision flip. Reader-2's sharper `avoidCollisions` boolean-trap chase also dies (useForwardProps forwards only present keys) — recorded. |
| **L-12 runtime half vs reader-1's MISS "two always-mounted SpeedSelects"** | (asserted the two-mounted claim inside its M-5 missed row) | runtime half killed (portal + Presence) | **reader-2 wins — and this seat kills reader-1's sub-claim (KX-16)** | This seat's own dist reads: glass-ui `DropdownMenuContent` declares `forceMount:{type:Boolean}` (unset by this consumer) and composes `DropdownMenuPortal`; reka `MenuContent` wraps content in `Presence` — the menu subtree, including the :117 SpeedSelect and EasingPicker, is UNMOUNTED while closed. At rest exactly ONE SpeedSelect is mounted. The dock-layer half of M-5 (both layers always mounted, `inert`-toggled; dock.js:696-703) survives. |
| **severity ladder on the adoption cluster** (C-5, C-6, D-10/C-16, D-11/L-7/C-8, L-9/C-7) | MAJOR | MINOR | **MINOR across the cluster** | None is a functional loss: C-5 renders 20px not 24px; C-6 dies wholesale in the F.W3 adoption; D-10's tooltip text duplicates accessible names already present (the a11y loss is redundancy, and the double-announcement sub-claim is killed); D-11/C-8 and L-9/C-7 are duplication/grammar drift whose user-facing harms are separately booked (D-2/D-3). Reader-1's MAJORs preserved in provenance; the A-tranche "ornament" ledger contradiction is retained on the L-9 row as a record correction. |

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure. Dispositions: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring.

### MAJOR

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **D-1** | The scoped `.animation-dock:where(.expanded)` width rule matches nothing at either mount site — scope-id on `.glass-dock-frame`, class+state on the `$attrs` inner `.glass-dock`; the inline `--animation-dock-max-width` lands and is read by nobody. Survives the 7.0.0 uplift (producer keeps the frame + `inheritAttrs:false` and claims byte-transparency that is true for global selectors, false for scoped ones). | AnimationControls.vue:62, :130, :134-136; dock.js:582, :678-696 (this seat) | **F.W4** — the consumer cure is MISS-6 (rename the existing inline binding to the sanctioned `--dock-max-inline-size`, delete :133-136, fix D-21 in the same edit) + **GLASS-RELAY** for the producer's false byte-transparency claim (breaks every scoped-block dock consumer at both pins) |
| **D-4 / L-2 / C-3** | FullscreenViewer binds 2 props + 2 emits AnimationControls does not declare; ghost + image-overlay toggles unreachable in fullscreen (~4.5 months, survived a same-block edit + green-gate commit); vue-tsc silent (this seat's run: exit 2, one unrelated diagnostic). Non-editing ghost owner = CanvasControlsDock (D's EditorControlsDock attribution killed). | FullscreenViewer.vue:131-139, :148-153; AnimationControls.vue:15-30; VisualizationView.vue:210-226 | **F.W4** — 4-line FullscreenViewer repair + an explicit decision where the ghost/overlay toggles live in fullscreen; the vitest-absence lesson folds to census §5 risk 10 (identity stays there) |
| **L-1** | Scrub session opens on unconditional native `pointerdown`, closes only on reka's `hasChanged`-gated `valueCommit`; a track press-and-release within ±half a 1% step (≈±3px/600px) or a cancelled pointer freezes the clock while the pause glyph + `is-playing` rainbow keep painting. NOT permanent (next value-changing drag self-heals); recovery gesture is pause→play. Thumb-tap route dead at this pin. | GlassTimeline.vue:43-57, :73-74; animation.ts:116-126; reka SliderRoot.js:114-134 + SliderImpl.js (this seat) | **F.W3** — dies structurally in the producer-GlassTimeline adoption: the shipped `./timeline` scrubber emits first-class `scrubStart`/`scrubEnd` (d.ts:84-85, this seat) — the exact session pair the fork hand-rolled wrong. Acceptance: freeze-gesture + pointercancel probes (SS-13) |
| **L-3** | Keyboard scrub (←/→/Home/End) never opens a scrub session — reka routes all three through `updateValues(...,{commit:true})` → `valueCommit`, GlassTimeline's `!scrubbing` guard eats it; the rAF's latched `startTime` (:63) clobbers the seek next frame. `aria-label="Timeline"` advertises a keyboard slider that is not one while playing. | GlassTimeline.vue:49, :53-54; animation.ts:63-69; SliderRoot.js:165-176 (this seat) | **F.W3** — same adoption; MUST carry an explicit keyboard-seek acceptance test (adoption alone is not proven to cure it: whether the upstream scrubber opens a session on keyboard commits is unverified) |
| **C-2** | Every transport verb restarts the clock forward: direction is derived from a local `cycle` off a re-anchored `startTime`; first tick after any restart has `cycle=0` for all t∈[0,1). Four doors, all owned by this dock: speed watcher, `endScrub`, `setCanvasVisible(true)`, `play()`. Trail kinks mid-stroke on the reverse leg. | animation.ts:55-75, :84-88, :99-106, :121-126, :138-143 (read whole by this seat) | **F.W4** — store repair (persist direction across restarts); the durable seat is C-13's engine decision at **F.W1** (keyframes 4.3 ships `alternate` + velocity-continuous interruption, unused by the transport that needs exactly that) |
| **C-1** | `npm ls @mkbabb/value.js` exits ELSPROBLEMS — glass-ui 4.0.0 peers `^0.10.0 || ^0.11.0`, keyframes 4.3.0 hard-depends `^0.13.0`; disjoint; installs only because the peer is optional. Reproduced by this seat (third independent run). Tree-level, not component-level. | this seat's run, verbatim axis block | **F.W1** — the uplift closes the constraint graph; identity = census §1 pins row AMENDED + the G33 `npm ls` gate (RED now, before the uplift); never re-book per-component |
| **C-4** | Local GlassTimeline.vue is a name-identical fork of the shipped `./timeline` GlassTimeline whose scrubber variant is a prop-level superset (`modelValue` 0..1 float, `label` caret) — AND ships the `scrubStart`/`scrubEnd` emits whose absence is L-1's mechanism. Fork is not merely redundant; it is the reason L-1 exists. | timeline/GlassTimeline.vue.d.ts:29-34, :84-85 (this seat); `grep glass-ui/timeline src` → 0 | **F.W3** — identity = census §3a HARD-shadow row (folded, this record adds the API-level + emits proof) |
| **D-9 / C-9** | A `role="menu"` hosting a combobox + six hand-rolled `role="menuitemradio"` `<Button>`s reka never registers (registration is by CollectionItem, not role) — exactly one of the menu's interactive children participates in the menu's keyboard model, while `DropdownMenuRadioGroup`/`DropdownMenuRadioItem` ship on the imported subpath. The shape (two grouped settings + one command) is a Popover. | AnimationControls.vue:103-125; EasingPicker.vue:16-36; ui/dropdown-menu/index.d.ts | **F.W3** — primitive adoption or Popover re-home; keep the correct `aria-required-children` reasoning (superlative S-1) when re-homing. Arrow-traversal pixel proof → SS-13 |
| **D-12 / C-20** | `currentLevel` fabricates `N = 1` when both payloads are null, and the dock DOES mount in that state — `hasData` includes `|| store.computing`, so mid-compute the dock presents an enabled transport over nothing; zero of the store's three status flags consulted. | AnimationControls.vue:39-54; VisualizationView.vue:121, :235 | **F.W4** (consult `computing` regardless) + **F.W1 rider** (7.0.0 `Metric` gains `loading?: boolean` — the free affordance) |
| **D-2** | `.play-btn`'s `#fff` glyph sits on an always-full-strength 7-stop rainbow `::before` (stacking context via backdrop-filter; App.E step-2 paint) ≈1.14–1.19:1 in the light arm vs 3:1 (SC 1.4.11); `is-playing` scrolls the ramp so the glyph cyclically fades while playing. Only hard-coded colour family in the file, in a repo that documents light-arm contrast repair. | AnimationControls.vue:151, :153, :158-167, :176 | **F.W4** — tokenise with D-3/D-13/D-17 in one edit; composited ratios → SS-13 |
| **D-3** | `:focus-visible` ring is 60% white on a near-white light-arm surface (≈1:1) while the app's canonical `var(--ring)` pattern exists globally with four adopters. | AnimationControls.vue:183; style.css:136-142 | **F.W4** |
| **D-5** | `.summary-speed` at `--foreground` 35% alpha cannot reach 4.5:1 in either theme (ceiling argument holds: 35% black-on-white maxes ≈2.47:1); it is the collapsed dock's only text. | AnimationControls.vue:192 | **F.W4** |
| **D-7 / L-14** | The collapsed progress bar is two bare divs — no role/name/value/aria-hidden — at ≈1.45:1 fill-vs-track; the sole position readout in the collapsed state. | AnimationControls.vue:74, :190-191 | **F.W4** — with D-23 (INFO) as the design consideration: surface position, demote the bar to `aria-hidden` decoration |
| **D-8 / C-19** | `prefers-reduced-motion` gates 1 of 6 animated surfaces; the 60fps clock is ungated — AND the clock AUTO-STARTS on data arrival (`useWorkspaceLoader.ts` `anim.reset(); anim.play()` + `else if (!anim.playing) anim.play()`, verified by this seat), so reduced-motion users receive unrequested continuous motion. WCAG 2.2.2 citation killed (the pause mechanism exists — it is superlative S-2); the true map is SC 2.3.3/platform practice. | AnimationControls.vue:178-180, :155, :181-182, :191, :196-197; animation.ts (whole); useWorkspaceLoader.ts:112-121 | **F.W4** — identity = census FE §8 (folded; this record adds the auto-start hardening); `DockBackgroundToggle` remains the canonical seat at both pins |
| **C-12** | `exportFrame` means options-modal at one mount and direct no-options call at the other; `Record<string,boolean>` over a closed two-key vocabulary lets `{withLabel:false}` compile and do nothing. | VisualizationView.vue:98, :236; FullscreenViewer.vue:138; BasisCanvas.vue:462-469 | **F.W4** — one payload-typed contract |
| **L-6** | The broken scrub pairing is forked verbatim into ConvergenceTimeline (same local flag, same guards, emits for sink) — the defect is already duplicated, and that file also proves the counter-example (`<Button variant="glass" class="play-btn">` for the same control). | ConvergenceTimeline.vue:36-56, :61 (this seat) | **F.W3** — both forks die in the same adoption |
| **MISS-1** (reader-2) | In epicycle mode the canvas's `N = …` readout is FABRICATED: `level = ceil(easedT · components.length)` feeds ONLY the label; the geometry uses the full chain at raw `t` and a constant `maxCircles` budget. AnimationControls.vue:47 renders the same fabricated quantity. On a tool whose claim is "the partial sum at N terms", the always-visible number announces an order the picture never had. | BasisCanvas.vue:135, :140, :144, :196-197 (verified whole by this seat) | **F.W4** — label-honesty repair, co-owned with BasisCanvas; the pixel ramp → SS-13 |
| **MISS-2** (reader-2) | The declared build gate is RED: `vue-tsc` exits 2 on `PaperView.vue(12,8) TS2882` (`@mkbabb/latex-paper/theme` exports no types condition) — WT-attributable regression; the uplift wave would open from a red gate. Tree-level. | this seat's own run (exit 2, single diagnostic) | **F.W0** — substrate must be green before F.W1 opens |
| **MISS-3** (reader-1) | `caretLabel` — the readout the whole derivation exists to produce — is `opacity:0` at rest and unreachable on touch (hover/`data-held` reveal only). The expanded dock at rest shows no numeric position; collapsed shows only D-7's bar. | GlassTimeline.vue:90-105 (this seat) | **F.W3** — the adoption wave must decide an at-rest/touch-reachable readout |
| **MISS-4** (reader-1) | No visible thumb exists at this pin (`variant="standard"` paints `width:0;opacity:0`) and GlassTimeline's own header claims "track + thumb + halo paints" — false against the installed dist; :120-123 names a "glass-scrubber variant" that is not one of the two shipped slider recipes. The counter-example to the "comments are accurate archaeology" superlative. | GlassTimeline.vue:6-11, :67, :120-126; glass-ui.css + slider/index.d.ts (this seat) | **F.W3** — same adoption; correct the prose wherever it survives |
| **MISS-5** (reader-1) | D-2's defect class recurs inside the surface this dock owns: EasingPicker's selected-easing label is 9.0px type at ≈3.4:1 in the light arm from a hard-coded `--easing-accent: hsl(248 88% 71%)` with no light/dark arm split. Context this seat adds: the hard-code carries its own upstream-filing comment (:44-47, `--viz-easing` filed per CONSTELLATION.md) — a documented carry, but the carry never addressed contrast. | EasingPicker.vue:44-49, :88-97 (read whole by this seat) | **F.W4** (consumer arm split now) + **GLASS-RELAY** rider on the already-filed token ask; ratio → SS-13 |

### MINOR

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **L-1b** | Stranded `anim.scrubbing=true` forces the trail's full-rebuild branch (≤600-sample `evaluateFourier`/frame un-precomputed) — DEMOTED by this seat: triple-conditional reachability, self-heals on next drag; bites on the pause→play recovery route only. | **F.W3** (dies with L-1) + the cheapest interim cure is MISS-12 |
| **C-11 / L-4** | Dock caret rounds where the canvas floors-and-lerps — one-level disagreement whenever frac(pos)>0.5; DEMOTED (exposure is hover/drag only, per MISS-3). | **F.W4** — cured by the L-5 composable |
| **L-5** | The `easedT → level` map: THREE sites, two rules (reader-2's kill of the fourth site ratified by this seat: svg-fourier brackets by VALUE, zero `easedT` — my grep). `composables/` exists and holds no level seat. | **F.W4** — one `levelAt` home |
| **C-24 / L-16** | Data-flow split: `activeBases` by prop, payloads by 471-line god-store reach-in — the structural cause of the L-4/L-5 family; untestable without router-bearing Pinia. | **F.W4** |
| **C-5** | `--slider-scrub-track-height` does not exist at 4.0.0 (dist grep 0) — dead CSS, track renders 20px not 24. The axis's implied cure is ALSO dead (reader-1's kill adopted: producer sets `--slider-track-height` at `(0,3,0)` on the same element; sound cures are `size="lg"`/inline style/upstream ask). | **F.W3** |
| **C-6** | The fork introduces a 1% quantization (200ms steps at 20s duration) the upstream float scrubber does not have; caret (raw float) desyncs from fill (rounded int); the fork's own reka-integers comment is contradicted by the upstream composing the same slider over a float model. | **F.W3** |
| **L-9 / C-7** | Two native `<button class="play-btn">` hand-roll ~48 declarations of `variant="glass"` chassis; both in-tree siblings (fs-close; ConvergenceTimeline's literally-same control) already use the variant. Record correction retained: the A-tranche ledger's "ornament" rationale is contradicted — this is the primary transport control. | **F.W4** + **GLASS-RELAY** if the rainbow needs a variant extension (premise → SS-13) |
| **D-11 / L-7 / C-8 / C-26** | Two icon grammars in one control row; the FA path literals are byte-identical pairs ×2 files (ConvergenceTimeline is the third copy — my grep: exactly 2 files); lucide ships both glyphs and glass-ui's own dock chunk already carries lucide play/pause; C-26's twin-block copy is the duplication mechanism. | **F.W4** — one edit also cures MISS-11 and shrinks the F.W1 lucide rename to ≤1 symbol |
| **D-10 / C-16** | 2 of 4 Tooltip callsites anchor on non-focusable nodes (`div`, `svg`) — hover-only (SC 1.4.13); the :94 trigger is display:none below `sm`. Double-announcement sub-claim killed; describedby lands inert on the svg. | **F.W3** — folds to intake **R3-7a**'s 35-callsite migration (identity stays with the intake; rider: do NOT port these two as-is; `icon-tooltip` is the displaced producer seat; shim-hardening row per D §7) |
| **C-13** | Two animation engines: keyframes.js drives the secondary morph, a hand-rolled rAF drives the primary transport — and lost direction doing it (C-2's cost). | **F.W1** — the engine decision |
| **C-14** | `.summary-speed { @apply text-base }` is inert (child spans own their font-size; utilities really emitted — this seat's `@source` probe). The surviving half of the D-6 contradiction. | **F.W4** — delete with D-5's recolour |
| **C-15** | The CSS `color` route bypasses MetricBadge's typed `color` prop; reaches the amount by inheritance, never the unit span. | **F.W4** |
| **C-17** | `linear: (t) => t` hand-rolled in a file that imports `timingFunctions` from value.js nine lines up; third identity in the store's `?? ((x)=>x)`. | **F.W4** |
| **C-21** | `epicycleData.components.length` read where the payload ships `n_components` — a field with zero client readers (declaration-only, verified). | **F.W4** |
| **C-22** | `activeBases?: string[]` — open type over a closed vocabulary, consumed as one bit; typos pass silently. | **F.W4** |
| **L-10 / C-23** | `maxWidth` default written twice (prop + provably-unreachable CSS fallback); unvalidated CSS-length string. Doubly moot under D-1; activates the moment D-1 is cured — fix in the same edit. | **F.W4** (rider on D-1's cure) |
| **L-11** | Persisted `speed` restored unvalidated into a 5-literal option set (`if (as?.speed)` also drops legit 0); off-set value renders an empty trigger while the collapsed badge shows it. | **F.W4** — with MISS-13 as the same input surface's second hole; production reachability → SS-13 |
| **L-12 / C-25** | Source half only (runtime half killed): hand-expanded `v-model` written twice; `speed`/`easing` are the two template-written store fields that carry side effects (the speed watcher IS C-2's trigger) — precisely the two that should have been actions. | **F.W4** |
| **C-27** | `"fourier-epicycles"` magic string — fanout corrected UP to **28 occurrences / 12 files** (this seat's grep confirms reader-2), ~4.7× the axis's booking; no exported constant; compounded by C-22. | **F.W4** |
| **D-13** | `::after` sheen paints above the glyph (positioned, z-auto, App.E step 6 vs in-flow svg) — invisible now, exposed the moment D-2 is cured. | **F.W4** (same edit as D-2) |
| **D-15** | Three un-tokenised icon sizes (17/14/16px) in one component; 17px maps to no scale step in the tree. | **F.W4** |
| **D-16** | Motion-token drift, CORRECTED arithmetic: `var(--ease-standard)` at 2 of 5 transition sites (:176, :196); raw `ease` at :166, UA-default at :155, `linear` at :191 — against the :195 comment claiming "named properties + canonical token". | **F.W4** |
| **D-17** | Dead declarations: `::before` opacity transition never fires (nothing changes its opacity — also why the rainbow is permanently full-strength); `border-radius:9999px` on both pseudos redundant under `overflow:hidden`. | **F.W4** |
| **D-18** | `mode="out-in"` at 0.15s each way leaves the primary transport with no glyph ~300ms per toggle — illegible exactly during its own state change. | **F.W4** |
| **D-19** | Collapsed tooltip 'Pause'/'Play' vs accessible name '…animation' — visible label and accessible name disagree (SC 2.5.3 smell); expanded twin is consistent. | **F.W4** |
| **D-20** | Second style block is unscoped and ships `.menu-popup`/`.menu-item` app-wide with zero external reuse. The unscoped MECHANISM is correct (portaled content — `:deep()` cure killed); the defect is the two generic NAMES. | **F.W4** — namespace or `data-*` hook, never `:deep()` |
| **D-21** | `calc(100dvw - 1rem)` measures including a classic scrollbar — adjudicated by this seat (neither re-reader contested; source-decidable, correct, moot under D-1, live the moment it is cured). | **F.W4** (rider on D-1's cure) |
| **MISS-6** (reader-1) | D-1's cure is half-written in the file: the inline binding already lands on `.glass-dock`, and `--dock-max-inline-size` is real, consumed (dock/{layers,overflow,shell}.css — my count 8 occurrences), and unused in src (my grep: 0). One token rename + one dead-rule deletion restores the contract at both pins, no producer coordination required. | **F.W4** — the D-1 consumer cure; `getComputedStyle` before/after → SS-13 |
| **MISS-7** (reader-1, corrected by this seat) | Both dock layers are always mounted (`inert`-toggled, dock.js:696-703), so the collapsed bar's per-frame `:style` width write patches an inert subtree while expanded, and the GlassTimeline slider stays mounted while collapsed. The "two always-mounted SpeedSelects" sub-claim is EXCISED (KX-16 — menu content is portal+Presence unmounted when closed). | **F.W4** — pair with MISS-8; measurable cost → SS-13 profiler question |
| **MISS-8** (reader-1) | `.mini-fill { transition: width 0.1s linear }` on a property rewritten every rAF tick — the bar renders a permanently-lagged damped copy of `t` and forces per-frame style recalc on a JS-animated value; removing one line improves correctness AND performance. | **F.W4** |
| **MISS-9** (reader-1) | One control, three labels in eight lines: mobile group `aria-label="Speed"`, a visible unassociated `<span>Speed</span>`, and the trigger's own `aria-label="Playback speed"` — the one VISIBLE label is the one not programmatically attached. | **F.W4** |
| **MISS-10** (reader-2) | The slider announces `aria-valuenow` on a 0..100 int scale while the visible caret reads `t = 0.42` / `N = 26`, with no `aria-valuetext` — accessible value and visible value are different quantities. (Single-reader derivation; the no-valuetext forwarding is d.ts-plausible, adopt with attribution.) | **F.W3** — the adoption must carry `aria-valuetext` |
| **MISS-11** (reader-2) | The two transport glyphs use different viewBox aspects (320/512 vs 384/512) letterboxed into one square box — rendered glyph width jumps ~20% every toggle. Subsumes and corrects D-14 (whose optical-centroid mechanism is retired). | **F.W4** (dies with the lucide adoption) |
| **MISS-12** (reader-2) | The scrub flag is triplicated with the store as odd one out: `anim.scrubbing` is what the renderer reads, but each timeline keeps a private `scrubbing` ref that actually gates session close — redundant by construction (the store already returns it, :145) and the drift IS L-1/L-1b's mechanism. Cheapest interim cure: read `anim.scrubbing`, delete the locals. | **F.W3** (interim **F.W4**-eligible) |
| **MISS-13** (reader-2) | `SpeedSelect` emits `parseFloat(v)` with no NaN guard → `speed=NaN` → `dur=NaN` → `t=NaN`, clock silently dead; with L-11, two independent unguarded holes on one input surface. | **F.W4** |

### INFO

| id | claim | disposition |
|---|---|---|
| **D-22** | Import-block drift with dangling `, }` (:5-6) — verified verbatim. | **F.W4** sweep |
| **D-23** | The collapsed summary surfaces the least dynamic value (speed) and omits position — adjudicated by this seat as the D-7 cure's design consideration. | folds into D-7's **F.W4** row |
| **D-24** | No explicit `side` on the menu content — placement-by-collision-flip; mechanism corrected (dock is `dock-inline`; bottom placement is the consumers' wrappers). One `side="top"` makes it placement-by-design. | **F.W4** |
| **C-18** | Zero value.js colour consumption in the tree's most colour-dense transport surface (honestly scoped by the axis itself: colors.ts is NOT in this import graph). | **NO-WAVE-OWNER** — observation; the colors.ts deletion identity stays with census W.L5 |
| **C-28** | MetricBadge ships `cursor-pointer` + focus ring unconditionally, no `interactive` prop — false affordance on a static readout; producer seam. | **GLASS-RELAY** |
| **C-29** | The one e2e keystone over this menu is `test.fixme` — every finding inside the menu is un-gated in CI by construction; web/ has no unit-test runner at all. | **F.W4** — gate re-enable rides the cure wave |
| **C-30** | Fold of intake **R6-8** (operation↔client leaves non-isolable); this component is the reader that will display the ambiguity, not the defect site. | fold by reference — identity stays with R6-8's F.W5-W8 carry |
| **L-8** | R5-7's blind-spot class transposed: two `v-if/v-else` icon twins invisible to an `:is`-keyed budget (R3-10). Scope-of-budget claim, not runtime defect. | **NO-WAVE-OWNER** — F.W4 budget note for the exhaustiveness sweep |
| **L-13** | The 4→7 break surface (`./metric-badge`, `DockDropdownTrigger`, lucide rename) is a DEFERRAL, not a live defect — both exports present at the installed pin; correction to the census row's per-component reading. | **F.W1** — identity stays with census FE §5/C-4 |
| **L-15** | The 60fps watcher depends on both `t` and `easedT` (pure derivation — one flush, no double draw); the sharper aside survives: `easing` changes are NOT observed, so changing easing while paused does not redraw. | **F.W4** note |
| **MISS-14** (reader-1) | Axis C's style-block citations are systematically displaced 2–3 lines (script cites exact); its "48 declarations" count is therefore unaudited. Spot-verified by this seat (`.summary-speed` is :192, not :194/:195; `::after` is :168-175, not :172-178). | **NO-WAVE-OWNER** — remediation reads C's style ranges as approximate |
| **MISS-15** (reader-2) | FullscreenViewer ships a no-op `onAfterLeave` bound to a live event whose own comment is false for this template (`v-if` unmounts the subtree) — the same seam as the corpus's shared headline, unread twice. Verified :87-89, :106-107. | **F.W4** sweep |

## Killed-claims register (17)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| KX-1 | **C-S-2** (superlative): the `.animation-dock:where(.expanded)` hook is "correct at 4.0.0" | The corpus's worst error — a superlative certifying the top structural defect as sound. C quoted the inner div's class list without the two lines above it: dock.js:678-681 shows the subtree ROOT is `.glass-dock-frame`; the quoted element is its child receiving `$attrs` under `inheritAttrs:!1`. Scope-id and class are on different elements. Killed by both readers; re-sealed by this seat's own dist read. |
| KX-2 | **C-10** mechanism: "two authorities write `width` on the same element; the consumer value steps mid-spring" | Same evidence: the consumer rule matches nothing, so there is exactly one width authority and no fight. The surviving half (sanctioned `--dock-max-inline-size` unused — src grep 0, this seat) is re-booked constructively as MISS-6, D-1's cure. |
| KX-3 | **D-6**: "`.summary-speed { @apply text-base }` beats the library's single-class size rule and forces 1rem" | No such rule exists — the size axis is child-span utility classes (`text-mono-micro`/`text-micro`), and children that declare font-size never inherit the root's. D-6 searched `dist/glass-ui.css` for a `.metric-badge` font-size rule — the wrong place. This seat additionally fired the one remaining falsifier — the `@source` "no-op" comment — and it does not land (the clause scopes to the producer's own build; the consumer backstop + P9 components.css path is explicit at index.css:195-224). C-14 is the surviving reading. |
| KX-4 | **L-1 route 1** (thumb tap): "SliderImpl thumb pointerdown → focus, no slideStart → no commit" | No thumb to tap at this pin: `variant="standard"` paints `.slider-thumb` at `width:0;opacity:0;background:0 0;border:none` (this seat's dist read); d.ts states "NO VISIBLE THUMB AT ALL"; the touch shim is `pointer-events:none`. The DEFECT survives via the track ±half-step route + pointercancel; only this mechanism is dead. Reader-2's L-1 row cited this dead route as primary — corrected here. |
| KX-5 | **L-1/L-1b permanence**: "permanently freezes"; "no reset path"; "recovery requires an unrelated action" | The next value-changing drag re-enters `onValueCommit` with the local flag still true → `endScrub()` clears both flags and restarts. The freeze is real, self-healing, and its stranded-flag penalty is confined to the pause→play recovery route (`play()` early-returns on `playing===true` — this seat's store read). |
| KX-6 | **D-8's WCAG 2.2.2 citation** | 2.2.2 requires a pause mechanism; it exists, is prominent, and is this file's own play/pause button (superlative S-2). The real gap maps to SC 2.3.3 / platform reduced-motion practice. Coverage arithmetic and the ungated-clock fact survive separately. |
| KX-7 | **D-4's attribution**: "EditorControlsDock owns the ghost toggle" | In the only state where AnimationControls mounts (`!isEditing`), the ghost toggle belongs to **CanvasControlsDock** (VisualizationView.vue:210-226 — this seat's read). Conclusion unaffected; proof chain corrected; C-3's version is the accurate one. |
| KX-8 | **D-20's prescribed cure**: "`:deep()` … would be exact" | The menu body is portaled (`DropdownMenuPortal` — this seat's dist grep); `:deep()` compiles to a descendant selector rooted at a non-ancestor. The unscoped block is the correct mechanism (the file's own comment says why); the fixable defect is the generic NAMES. Prescribing `:deep()` would ship a silently broken edit over a working one. |
| KX-9 | **C-5's implied cure**: "the intended knob is `--slider-track-height`" | Insufficient as written: the producer sets that token at `(0,3,0)` (`.glass-slider[data-size=md][data-v-…]`) against the consumer's scoped `(0,2,0)` on the same element — the corrected token would be dead for the identical reason. Sound cures: `size="lg"`, inline style, or upstream ask. (Reader-1 derivation; adopted with attribution.) |
| KX-10 | **D-10's compounding sub-claim**: "aria-describedby produces 'More options, More options'" | The tooltip is nested INSIDE the trigger; `TooltipTrigger as-child` merges onto the `<svg>` — an unnamed, role-less node that is not announced. No double announcement; the describedby is inert (a different, lesser defect). The trigger's own `aria-label` does forward (reader-2's compatible observation, retained). |
| KX-11 | **L-S-3's scarcity premise**: "1 of only 8 `prefers-reduced-motion` blocks in the tree" | Stale against the live WT: this seat measures 15 occurrences / 12 files. The substance (kill-not-shorten, landed with the token migration) survives; the rarity framing is ~2× inflated. |
| KX-12 | **Reader-1's D-16 cell**: "`var(--ease-standard)` present at :166" | Killed by reader-2 and re-verified by this seat: :166 is `transition: opacity 0.3s ease` — raw keyword. The cell contradicted reader-1's own D-17 row eight lines away. Token count corrected to 2-of-5. |
| KX-13 | **D-24's mechanism** (+ its sharper variant) | `position` is never passed (AnimationControls.vue:58-63 — this seat); dock.js:699 resolves `dock-inline`; bottom placement is the consumers' wrappers. The `avoidCollisions` boolean-trap chase also dies: reka's useForwardProps forwards only keys present in vnode props, so reka's own `true` default holds. Residue = trivial INFO. |
| KX-14 | **L-5's fourth site**: "svg-fourier.ts is a fourth implementation of the same clock map" | It brackets by VALUE (takes a harmonic level in, finds `levels[i] ≤ v ≤ levels[i+1]`) and never touches `easedT` (this seat's grep: 0 hits in the file). The falsifier's own test fails on this site. Three sites, two rules — C-11's count is the correct one. |
| KX-15 | **L-12's runtime half**: "two live Select popovers / two mounted listbox subtrees at every viewport" | The :117 instance lives inside `DropdownMenuContent` → `DropdownMenuPortal` + reka `Presence` with `forceMount` unset — UNMOUNTED while closed (this seat's dist + reka reads). Source-duplication half stands. |
| KX-16 | **Reader-1's MISS-7 sub-claim**: "two always-mounted SpeedSelect instances (:96, :117)" | This seat's own kill, same evidence as KX-15: only the :96 instance is always mounted. The dock-layer half of the row survives and is booked as MISS-7-corrected. |
| KX-17 | **D-14** (optical-centroid mechanism) | Retired as subsumed: the measurable effect is the viewBox aspect delta (MISS-11), decidable from the two attributes alone; the centroid framing named the wrong mechanism and offered no measurement. |

## Superlatives (provenance: axes → re-readers → this seat)

1. **The `aria-required-children` reasoning** (AnimationControls.vue:110-114 + EasingPicker.vue:10-15) — all three axes and both readers independently confirm; structure re-verified. Correct on the spec (`group` is an allowed owned element of `menu`), correct on the deeper point (stamping `menuitem` on a combobox passes the same automated check with a worse tree). Operationally defeated by D-9/C-9 — keep the reasoning when the surface re-homes.
2. **State-swapped accessible name in both dock branches** (:67, :82) — the APG transport pattern, held identically where the visible copy drifted (D-19); it is also the WCAG 2.2.2 mechanism whose absence D-8 wrongly claimed (KX-6).
3. **The reference-counted off-screen rAF gating** (animation.ts:34-53, :95-113 — read whole by this seat): `visibleCanvases` is a credit count because inline+fullscreen canvases co-mount; `playing` is user INTENT, visibility parks the CLOCK. Every §2-class defect confirmed here (C-2, L-1) is a bug inside good architecture, not instead of it.
4. **Zero teardown surface** — no lifecycle hooks, timers, observers, listeners, or `defineExpose` in a component commanding a 60fps loop; the store cancels the rAF on every exit path (five, enumerated in this seat's store read); BasisCanvas releases its visibility credit under a `lastVisible` guard.
5. **The typed `maxWidth` prop's intent** — retiring an invisible scoped-CSS-var handshake between two components for a typed, defaulted, documented prop, recorded from both ends. The delivery fails (D-1) for a producer-owned reason; cure it (MISS-6) and this is the pattern to copy.
6. **`@click.stop` vs bare `@click` two lines apart, both correct** — the collapsed layer carries the dock's own click-to-expand; the mini transport must not expand, the expanded twin has nothing to expand. C-26 books the real cost: nothing says so.
7. **The one live internals selector is a good coupling**: `.timeline-row:has(.glass-slider[data-held])` keys off a documented producer data-attribute (Slider d.ts dock-held reflection) — better than most consumers manage, in the same file as the dead C-5 token.
8. **Comments as checkable archaeology, with the exception named**: the DropdownMenu migration note, the unscoped-block boundary notes, and the CR-2/dock-context history all verify; the exception is MISS-4 — GlassTimeline's header describes a thumb the installed pin does not paint. Four of five verify; the fifth is now a booked defect, not a hedge.
9. **The multi-lens corpus itself**: on the highest-stakes structural question the axes SPLIT (D derived D-1 correctly; C certified the opposite as superlative S-2), and at MetricBadge D-6 and C-14 assert opposite readings of the same compiled lines. Both splits were what made the truth findable — and both re-readers then split again on L-1's mechanism and L-12's runtime half, which is what this seat's dist reads settled. Single-lens auditing would have shipped KX-1 as a green mark on the top defect.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **D-1/MISS-6** — `getComputedStyle($('.animation-dock')).width` at both mounts, before and after the `--dock-max-inline-size` rename (the cure's acceptance test). Source derivation complete.
2. **D-2/D-3/D-5/D-7/MISS-5** — the five re-derived contrast ratios (≈1.19, ≈1.0, ≈2.17, ≈1.45, ≈3.4 : 1) computed against tokens with the glass surface approximated; sample composited pixels in the light arm. Direction and pass/fail verdicts are robust; second decimals are not.
3. **L-1** — the live gesture: track press-and-release within ±3px, and a pointercancel; confirm the pause glyph + rainbow persist over a frozen clock, and that the next drag self-heals (the KX-5 correction).
4. **L-3** — keyboard seek snap-back within one frame during playback.
5. **D-9/C-9** — ↑/↓ walk only the Export item and skip the six chips (reka 2.9.x roving-focus behaviour on unregistered children).
6. **C-6** — the caret/fill 1%-step desynchronisation during playback.
7. **C-2** — the visible trail kink on a speed change during the reverse leg.
8. **MISS-1** — the on-canvas `N` visibly ramps while the drawn circle count does not (epicycle mode).
9. **C-11/L-4** — whether a user ever sees the dock/canvas divergence on hover with `levels.length > 1` (decides the practical severity floor).
10. **L-9/C-7** — whether `<Button variant="glass">` can host the animated `::before` rainbow (the variant-extension question).
11. **L-11** — whether an off-set persisted `speed` exists in production data (the unguarded path is source-closed).
12. **MISS-7** — whether the inert-layer per-frame style patch is measurable (profiler question).

## Identity guards (folds by reference — never re-booked)

- **R3-7a** (intake, TRUE, CARRY→F.W3): the 4 Tooltip callsites — D-10/C-16 ride that migration as riders (2-of-4 malformed, icon-tooltip seat, shim-hardening), identity stays with the intake.
- **R3-10 / R5-7** (intake): budget classes — L-8 is a scope note on them, not a new row.
- **R6-8** (intake, ADOPT-AS-FACT, CARRY→F.W5): C-30 folds; this component is the display surface of that ambiguity, not its defect site.
- **Census §1 pins row + G33 gate**: C-1 is that row AMENDED (versions agree, constraint graph does not close) — one identity, F.W1.
- **Census FE §5 / C-4-census**: the 4→7 break table (metric-badge ×7 incl. this file, DockDropdownTrigger ×1 = this file, lucide ×35) — L-13 is the per-component deferral correction; no re-book.
- **Census FE §8**: the ungated rAF clocks — D-8/C-19 localize and harden (auto-start), identity stays with the census row; `DockBackgroundToggle` remains the named seat.
- **Census §3a HARD shadows**: GlassTimeline/EasingPicker/easings.ts — C-4/C-5/C-6/MISS-4 are that row's API-level proof and residue, identity stays census; F.W3.
- **Census W.L5 item 2** (colors.ts deletion): C-18 scoped out, no claim on this component's graph.
- **Constellation memory** (value.js `ComponentSliders.vue` pointercancel cure): L-1's route (iii) is the same producer defect class, cured downstream once already and never carried here — pattern echo recorded, not a re-book.
- No prior `fr-*` apotheosis exists; `kf-AnimationControlsGroup` is a different product's component — no identity overlap.

## Closing verdict

**ADJUDICATED — DEFECTIVE, zero BLOCKERs as booked but two blocker-weight clusters (transport-truth; dead-contract): 54 corpus rows confirmed (21 MAJOR · 33 MINOR · 12 INFO after merges) + 15 missed rows booked · 17 claims killed (including one superlative that certified the top defect as sound, two re-reader cells killed by this seat, and one kill-of-a-kill probe fired that did not land) · routings: 1×F.W0 · 4×F.W1 · 13×F.W3 · the balance F.W4, with 4 GLASS-RELAY riders and 4 NO-WAVE-OWNER records — all as agglomeration input to the forming X·F specs (M-25), stamping nothing.**

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
