claude-fable-5 (served model id)

# fr-AnimationControls — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject**: `fourier-analysis/web/src/components/visualization/AnimationControls.vue` (224 lines) + its three local children (`GlassTimeline.vue` 127 · `EasingPicker.vue` 98 · `SpeedSelect.vue` ~70) and the transport store.
**Substrate**: fourier `m/w1-bump-migration @ cd26c65` + the frozen M.W1a dirty WT (re-pinned by this seat: `git log -1` + porcelain, subject-adjacent rows modified) · value.js `tranche-u @ c654824e` · installed glass-ui 4.0.0 / keyframes.js 4.3.0 / value.js 0.13.0 / reka-ui 2.9.x · producer glass-ui at 7.0.0 (read at `/Users/mkbabb/Programming/glass-ui`).
**Corpus**: three axis challenges (`audit/fourier-components/AnimationControls/challenge-{D-design 24 rows, L-library 16+1, C-consumption 30}.md`, all read whole by this seat) × two hostile re-reader returns (**reader-DU** design-and-usage · **reader-LC** library-and-code), received in-brief.
**SUPERSESSION NOTE**: a prior draft existed at this exact path (same date, same seat-class). Its reader-attribution cells could not be reconciled with the returns delivered to this seat (it attributes to its reader-1 a "thumb-tap route DEAD" position and to its reader-2 a D-1-at-MAJOR booking — positions **neither** of this corpus's readers took; both of mine assert the thumb-tap mechanism and both book D-1 BLOCKER). It adjudicated a different reader pair and is replaced wholesale; the three challenge files beside this record remain immutable per E-1/E-3. Where the prior draft's seat-verifications overlap mine (the zero-width thumb, the `:166` raw `ease`), this record's cells were **re-derived independently against the tree**, not inherited.
**Method (M-23/L-18)**: every verdict-level disagreement between the readers was re-derived against the tree before ruling; every load-bearing kill was re-attacked; **four kills were minted by this seat against the readers themselves** (K-5, K-15, K-18, K-19) — including one cell BOTH readers got wrong twice over. Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched)**: full reads of AnimationControls.vue, GlassTimeline.vue, EasingPicker.vue, stores/animation.ts (146), useWorkspaceLoader.ts (136); ranged reads SpeedSelect.vue:1-45, BasisCanvas.vue:44-48+:376-400, FullscreenViewer.vue:104-155, ConvergenceTimeline.vue:36-64, canvas-drawing/trail.ts:1-72, style.css:40-49+:128-143, lib/easings.ts:1-90. Installed-dist reads: dock.js:582 (`inheritAttrs:!1`) + :676-700 (frame root static-class vs `$attrs` inner + the `position` ternary) + :715-730 (both layers always mounted, `inert`-toggled, summary-layer onClick); glass-ui.css `.slider-thumb` ladder (base `width:0;opacity:0;background:0 0;border:none`; only the spectrum arm paints `opacity:1`); slider/index.d.ts (standard = "NO VISIBLE THUMB AT ALL … you pull the TRACK"); timeline/GlassTimeline.vue.d.ts:29-34 + **ScrubberTimeline.vue.d.ts (emits `scrubStart`/`scrubEnd` at :19-20)**; dropdown-menu-BJ7E9js_.js (`forceMount:{type:Boolean}` ×2, `DropdownMenuPortal` imported); reka MenuContent.js (`Presence` gated `forceMount || open`), MenuContentImpl.js:153/:190/:199/:205 (collection-keyed nav + `Tab → preventDefault`), SliderRoot.js:104-178 (`hasChanged`-gated commit; keyboard routes `{commit:true}`), SliderImpl.js:34-63 (keydown/pointerdown/pointermove/pointerup ONLY; thumb-target → focus-not-slideStart); MetricBadge compiled chunk (amount/unit child spans, `text-mono-micro` ×4, root `cursor-pointer`); styles/index.css:195-224 (the `@source` block read whole — the "no-op" clause scopes to the producer's own build); typography scale/utilities (`--type-micro` 11px); glass/a11y-fallback.css (`prefers-reduced-transparency` → `--glass-level:0`; `prefers-contrast:more` → 0.3+ink+AA; forced-colors arm); dock/shell.css:18-23 (`--dock-max-inline-size` consumer-override doc) + :79-83 (the nested-backdrop containment comment naming "the Button-in-glass-in-dock stack"). Producer 7.0.0 reads: `src/components/timeline/index.ts` (**exports `Timeline` ONLY** — "role=progressbar, never role=slider … the commanding surface is `<Slider :marks>`"); `dock/GlassDock.vue:38-45` (byte-transparency claim + `inheritAttrs:false` kept). Measurements run by this seat: `npm ls @mkbabb/value.js` → ELSPROBLEMS exit 1 (third independent reproduction); `vue-tsc --noEmit -p tsconfig.json` → exactly one diagnostic, `PaperView.vue(12,8) TS2882` (third independent run); greps: `easedT` fanout (5 derivation sites; **`svg-fourier.ts` = 0**), `--slider-scrub-track-height` writers = **3**, `"fourier-epicycles"` = **28 occurrences / 11 files** (correcting LC's "12 files" by one), FA path literals = **6 hits / 2 files**, `dock-max-inline-size` in src = 0, `max_circles`/`fps` client readers = 0, `duration` store writers = 0, root font 1.125rem/1rem, `trail.precompute` = one call site.

## Scoreboard

**68 rows ADJUDICATED-CONFIRMED (20 MAJOR · 34 MINOR · 14 INFO, counting the 15 missed rows banked) · 20 killed-register entries (4 minted by this seat against the readers) · 2 further reader-missed rows FOLDED to banked fr-BasisCanvas identities without re-booking · 0 BLOCKERs survive as booked.**

All five axis BLOCKERs are demoted or re-scoped (C-1, C-2, C-3/D-4, D-1, L-1/L-1b — rulings below), but two blocker-weight clusters are named so severity arithmetic does not hide them: **transport-truth** (L-1 freeze-while-painting-pause + C-2 always-resumes-forward + M-9's data-arrival play-override) and **keyboard-reachability** (D-9/C-9: with the menu open, Tab is preventDefault'd and arrow-nav walks only the one registered item — easing selection is pointer-only at every viewport and speed below `sm`; blocker-class if the SS-13 keystroke confirms).

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L-1 mechanism** | thumb-tap cited as confirmed route ("a thumb tap changes no value") | same thumb-tap route cited, with reka line proof | **BOTH CORRECTED — seat-minted kill K-5.** Freeze survives on two different routes | GlassTimeline.vue:67 passes `variant="standard"`; installed `glass-ui.css` base rule paints `.slider-thumb{width:0;…opacity:0;background:0 0;border:none}` and only the spectrum arm makes it visible; the d.ts is explicit ("NO VISIBLE THUMB AT ALL … the reka `<SliderThumb>` STAYS MOUNTED … paints INVISIBLE"). A zero-area element cannot be `event.target`, so `SliderImpl`'s thumb branch is unreachable — **there is no thumb to tap at this pin**. The freeze is real via (i) **track press-and-release within ±half a 1% step of the playhead** — `handleSlideStart→updateValues` snaps the press position; if it equals the current int, no model write, `handleSlideEnd`'s `hasChanged===false`, no `valueCommit`, while GlassTimeline's native `@pointerdown` already ran `startScrub()→stopRAF()`; and (ii) **pointercancel** — SliderImpl registers no `pointercancel`/`lostpointercapture` (my read: exactly 4 handlers), so a cancelled captured pointer never reaches `slideEnd`. Route (i) is the *natural* gesture — pressing where the invisible "thumb" would be. |
| R-2 | **L-1 severity + permanence** | BLOCKER; "tapping the timeline freezes the transport" | MAJOR; permanence KILLED ("recovery is the most natural retry") | **MAJOR — LC wins; kill RATIFIED (K-4) with a store nuance neither reader wrote** | After a freeze `playing===true`, so `play()` **early-returns** (animation.ts:85) — the play button itself needs TWO presses (pause→play) to recover. But `SliderRoot` re-latches `valuesBeforeSlideStartRef` on every pointerdown (:158-161), so the next value-changing drag emits `valueCommit`, passes the `:54` guard (local flag still true), and `endScrub()` restarts the loop; a **speed change** also recovers it (`watch(speed)` fires `if (playing.value)` → true → `startLoop()`, :138-143 — my read). Self-healing on the natural retry ⇒ not a BLOCKER. |
| R-3 | **D-1 severity** | BLOCKER | BLOCKER | **MAJOR — seat demotes against both** | Mechanism unanimous and re-sealed by this seat (dock.js:582 `inheritAttrs:!1`; :677-681 root `.glass-dock-frame` with a STATIC class list; :683-696 inner `.glass-dock` takes `t.$attrs` + `{expanded}`; Vue's scope-id lands on the child's subtree root only → the compound matches nothing; producer 7.0.0 keeps the frame and claims byte-transparency that is true for global selectors, false for scoped ones — my read of GlassDock.vue:38-45). Severity follows consequence: a proportion preference that never applies, on a dock that still renders, functions, and lies about no data — the producer's own `--dock-max-inline-size` cap still governs. Blocker by *load* (it invalidates C-S-2 and C-10 and both mount sites); MAJOR by consequence. |
| R-4 | **D-4/L-2/C-3 severity** | BLOCKER | MAJOR (as L-2) | **MAJOR — LC wins** | Unreachability re-verified whole (FullscreenViewer.vue:104-141: children are the close Button, ContourEditorCanvas/BasisCanvas, AnimationControls; `.fs-backdrop` `position:fixed;inset:0;background:var(--background)` :146-153 — my read). Capability loss is confined to fullscreen with an exit-fullscreen workaround. The C-3 horn is closed by **three** independent `vue-tsc` runs (both readers + this seat): exactly one diagnostic, `PaperView.vue(12,8) TS2882`, nothing on either file → horn (b), silent runtime feature loss, ~4.5 months (C's git provenance adopted). DU's mis-attribution killed at K-11. |
| R-5 | **D-2 severity** | MAJOR | BLOCKER | **MAJOR — DU wins** | The glyph legibility failure is real (both readers' independent composites agree ≈1.19:1 / ≈2.01:1 worst-stops vs the 3:1 floor; direction arithmetically forced), but the control stays discoverable — the rainbow pill itself is the highest-salience object on the dock, the accessible name is state-swapped (superlative S-2), and the tooltip carries the verb. A whole-theme SC 1.4.11 failure on the primary control = MAJOR, cured with D-3/D-13/D-17 in one edit; exact composited ratios → SS-13. |
| R-6 | **D-9/C-9 severity** | MAJOR | BLOCKER (escalated: WCAG 2.1.1 Level A) | **MAJOR booked; escalation ADJUDICATED-PLAUSIBLE, blocker-class if the SS-13 keystroke confirms** | LC's mechanism is re-verified by this seat's own reka read: MenuContentImpl.js:205 `if (event.key === "Tab") event.preventDefault()` (gated only on `isKeyDownInside`, :190), arrow nav + `focusFirst` walk `"[data-reka-collection-item]:not([data-disabled])"` (:153/:199) — a hand-written `role="menuitemradio"` registers nothing. So the six easing chips are pointer-only at EVERY viewport and the in-menu SpeedSelect (the only speed control below `sm`, `:95` `hidden sm:block`) is pointer-only on mobile. C-9's "reachable by Tab" mitigation is dead (K-8). No keystroke was fired against the open menu; severity floor MAJOR now, re-opens on live confirmation. |
| R-7 | **D-10/C-16 severity** | MAJOR | MINOR (inverted: "the duplication is a mitigation, not an aggravator") | **MINOR — LC wins** | Both malformed tooltip anchors (`div`, `svg`) duplicate accessible names already present (`aria-label="More options"` on the trigger; `SpeedSelect`'s own `aria-label="Playback speed"`), so keyboard users lose zero information; the real cost is dead weight — two reka popper roots buying nothing, one `display:none` below `sm`. Folds to intake **R3-7a** (identity stays there): do NOT port these two as-is in the F.W3 migration; `icon-tooltip` is the displaced producer seat. |
| R-8 | **L-5 count** | four sites incl. `lib/svg-fourier.ts` ("bracket search") | fourth site KILLED ("padded with an unrelated module") | **LC wins — my own grep** | `grep -c easedT src/lib/svg-fourier.ts` → **0**. Its bracket search takes a harmonic *value* over the statically-shipped morph JSON (`FourierPathData`), not the animation clock over `runComputeBases` levels — DU's own falsifier ("all four map the same normalized clock") is factually false at the fourth site. Real count: the `ceil` expression byte-identical ×3 (AnimationControls:47, BasisCanvas:196, :239) + the `pos` mapping ×2 with divergent rounding. (`ConvergencePlot.vue` has its own local `easedT` over its own clock — different quantity, correctly not counted.) |
| R-9 | **L-12 runtime half** | "both instances mount and two reka listbox subtrees live at every viewport" | KILLED ("at most ONE SpeedSelect is live at rest") | **LC wins — my own dist reads** | The `:117` instance is a child of `DropdownMenuContent`; the glass-ui chunk declares `forceMount:{type:Boolean}` (unset by this consumer) and reka `MenuContent` wraps content in `Presence` with `present: forceMount || open` — the menu subtree, EasingPicker included, is **unmounted while closed**. Only the `:96` instance (CSS-hidden below `sm`) is always mounted. The source half (hand-expanded `v-model` written twice; `speed`/`easing` the two side-effect-bearing raw writes — C-25) survives. |
| R-10 | **C-4 prescription** | "two-line substitution" KILLED at 7.0.0; keep the local Slider composition | SHARPENED toward upstream adoption ("one migration, not three patches") | **BOTH halves adopted with DU owning the route — K-14** | LC's root-cause synthesis is right and is the corpus's best line: the installed `ScrubberTimeline.vue.d.ts` declares emits `scrubStart`/`scrubEnd` (my read, :19-20) — an *unconditional* session pair owned by the primitive's own pointer machine, so the fork is the single common cause of L-1, L-3 and C-6. But DU's producer-forward check is decisive for the ROUTE: at 7.0.0 `timeline/index.ts` exports `Timeline` ONLY — `role="progressbar"`, "a timeline REPORTS … the commanding surface is `<Slider :marks>`" (my read). A 4.0.0 `GlassTimeline` adoption is stale-on-arrival at F.W1. The durable cure: keep the Slider composition and re-derive the unconditional session pairing on the Slider's **own** pointer events including `pointercancel` — or an upstream ask for a scrub-session contract on `Slider`. |
| R-11 | **C-5 count** | confirmed at one site | corrected to **3 writers** | **LC wins — my grep**: GlassTimeline.vue:125 (24px) · `ui/SliderControl.vue:144` (16px) · `ConvergenceTimeline.vue:136` (20px). Dead-token residue of the retired `glass-scrubber` variant; booked at the scope where the count lives (the ColorSpaceSelector L-9.1 scope law). |
| R-12 | **D-16 arithmetic** | "token at :166, :176, :196 — 3-of-5" | same cell, verbatim | **BOTH KILLED — seat-minted K-15.** `AnimationControls.vue:166` reads `transition: opacity 0.3s ease;` — the raw keyword. `var(--ease-standard)` appears at exactly **:176 and :196 — 2-of-5**; absent at :155 (UA default), :166 (raw `ease`), :191 (`linear`). Both readers inherited the D axis's D-16 cell while their own D-17 rows quote :166 correctly — a **dual citation-inheritance event, each reader self-contradicting eight lines apart** (DISSENT §1). The drift finding is CONFIRMED and worse than booked. |
| R-13 | **L-4 co-visibility + severity** | "co-visible by construction" (its own MISS-3 contradicts this) | co-visibility KILLED (caret `opacity:0` at rest); MAJOR kept | **MINOR — seat demotes against both** | GlassTimeline.vue:95 `opacity:0`; :102-105 reveal only on `.timeline-row:hover` / `:has(.glass-slider[data-held])` (my read). A one-level rounding disagreement exposed only during hover/scrub is MINOR. DU asserted both the L-4 by-construction cell and the M-3 invisibility cell in one return — the intra-reader contradiction is recorded; M-3 is the true one. |
| R-14 | **C-1 altitude** | killed at component altitude (INFO here, book at repo) | MINOR, no live break (all imported symbols present at 0.13.0) | **Compatible — both demotions from the axis BLOCKER ratified; fold** | Reproduced by this seat (third run): ELSPROBLEMS exit 1, `"^0.10.0 || ^0.11.0"` vs installed 0.13.0. Identity = census §1 pins row AMENDED + the G33 gate — already FOLDED to the F.W1 charter by the banked **fr-BasisCanvas** record (its C-8). ONE identity, component-altitude INFO, never re-booked per component. |
| R-15 | **L-1b severity** | MAJOR | MAJOR | **MINOR — seat demotes against both** | Reachability is compound: an L-1 freeze first, then specifically a non-drag recovery (pause→play or speed change — the natural drag retry clears the flag via `endScrub`). Cost while stranded is real (trail.ts:48 rebuild branch; the un-precomputed `evaluateFourier` fallback — my read) and the loader's recompute path can restart the clock over the stranded flag, but it self-heals on the next value-changing drag. A compound-conditional, self-healing perf cliff is MINOR. Both MAJORs preserved in provenance; cross-ref fr-BasisCanvas BC-20/M-β2 (distinct identities: unbounded append / draw-path mutation). |
| R-16 | **C-13 severity** | (not booked) | MAJOR | **MINOR — seat demotes** | Two-engines is the structural seat, but its user-facing harm is C-2's row, booked MAJOR — double-charging one harm across two rows inflates the component. The engine decision (keyframes 4.3 ships `alternate` + the velocity-continuous interruption contract the transport needs) rides **F.W1**. |

**Citation-correction rulings inside agreed verdicts**: D-3 survives on SC 1.4.11 alone — the "SC 2.4.11" citation is wrong in WCAG 2.2 (2.4.11 = Focus Not Obscured; Focus Appearance = 2.4.13, AAA); LC killed it and DU's cell *repeats* it (second inheritance event, DISSENT §1). D-19's SC 2.5.3 framing killed (DU's own kill, ratified — "Pause" ⊂ "Pause animation" runs the conforming direction): pure microcopy row. D-18's "~300ms with no glyph" killed by both (opacity+scale fades; glyph-free only at the crossover instant): INFO. D-24's D-axis mechanism ("dock is bottom-anchored `position=fixed`") seat-corrected (K-19): no `position` prop is passed (:58-63), dock.js's ternary resolves `dock-inline`; bottom placement belongs to the consumers' wrappers; the trivial residue (no explicit `side`) survives at INFO.

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure. Routes: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5–W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring.

### MAJOR (20)

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **D-1** | The scoped `.animation-dock:where(.expanded)` width rule matches nothing at either mount site — scope-id on `.glass-dock-frame`, class+state on the `$attrs` inner `.glass-dock`; the inline `--animation-dock-max-width` lands and is read by nobody; `maxWidth` inert. Survives the 7.0.0 uplift (frame + `inheritAttrs:false` kept; the byte-transparency claim is false for scoped selectors). | :62, :130, :134-136; dock.js:582/:677-696; producer GlassDock.vue:38-45 (all this seat) | **F.W4** — consumer cure is one rename: the inline binding already lands on `.glass-dock`, and `--dock-max-inline-size` is the documented consumer-override (shell.css:18-23, my read) with 0 src consumers (my grep) — rename the custom property, delete :133-136, and fix L-10/C-23 + D-21 in the same edit. + **GLASS-RELAY**: the frame silently broke every scoped-block dock consumer at both pins. |
| **D-4 / L-2 / C-3** | FullscreenViewer binds 2 props + 2 emits the component does not declare; ghost + image-overlay toggles unreachable in fullscreen (~4.5 months, survived a same-block edit and a green-gate commit); typechecker silent (this seat's run: one unrelated TS2882). View-mode toggle owner = `CanvasControlsDock`, behind the opaque backdrop (DU's EditorControlsDock attribution killed, K-11). | FullscreenViewer.vue:126-139, :146-153 (this seat); AnimationControls.vue:15-30 | **F.W4** — 4-line FullscreenViewer repair + an explicit decision where the ghost/overlay toggles live in fullscreen; the no-unit-test lesson folds to census §5 risk 10 (identity stays there). |
| **L-1** | Scrub session opens on unconditional native `pointerdown`, closes only on reka's `hasChanged`-gated `valueCommit`; a track press-and-release within ±half a 1% step at the playhead (the natural grab gesture — no visible thumb exists at this pin, K-5) or a cancelled pointer freezes the clock while the pause glyph + `is-playing` rainbow keep painting. NOT permanent (K-4): next value-changing drag or speed change self-heals; the play button needs pause→play. | GlassTimeline.vue:43-57, :73-74; animation.ts:85, :116-126; SliderRoot.js:104-161 + SliderImpl.js (this seat) | **F.W3** — dies in the M-1 re-derivation (unconditional session pair incl. `pointercancel`); freeze-gesture + cancel probes → SS-13. Constellation echo: value.js's `ComponentSliders.vue` cured the same reka pointer-capture class downstream — pattern recorded, not re-booked. |
| **L-3** | Keyboard scrub (←/→/Home/End) never opens a session — reka routes all three through `updateValues({commit:true})` → `valueCommit`; the `!scrubbing` guard eats it; the rAF's latched `startTime` (:63) clobbers the seek next frame. `aria-label="Timeline"` advertises a slider that is keyboard-inert while playing. | GlassTimeline.vue:49-54; animation.ts:63-69; SliderRoot.js:165-176 (this seat) | **F.W3** — same re-derivation; MUST carry an explicit keyboard-seek acceptance test. |
| **C-2** | Every transport verb resumes forward: direction is derived from `cycle` off a re-anchored `startTime`; first tick after any restart has `cycle=0` for all t∈[0,1). Four doors, all owned by this dock (speed watcher, `endScrub`, `setCanvasVisible(true)`, `play`). Mid-stroke reversal on the descending leg; trail kinks. | animation.ts:51-75, :84-88, :99-106, :121-126, :138-143 (read whole by this seat) | **F.W4** store repair (direction becomes state); durable seat = C-13's engine decision at **F.W1** (keyframes 4.3 ships `alternate` + velocity-continuous interruption, excised by the store's own header). |
| **C-4** | Local `GlassTimeline.vue` is a name-identical fork of the shipped `./timeline` `GlassTimeline` whose scrubber variant is a prop superset (float 0..1 `modelValue`, `label` caret) — and whose `ScrubberTimeline` ships the `scrubStart`/`scrubEnd` emits whose absence is L-1's mechanism. The fork is not merely redundant; it is the reason L-1 exists. Route ruling K-14: the 7.0.0 seat is `<Slider :marks>`, not the timeline. | timeline d.ts (this seat); `grep glass-ui/timeline src` → 0; producer timeline/index.ts (this seat) | **F.W3** — identity = census §3a HARD-shadow row (folded; this record adds the emits-level proof + the 7.0.0 route correction). |
| **D-9 / C-9** | A `role="menu"` hosting a combobox + six hand-rolled `role="menuitemradio"` `<Button>`s reka never registers — one of eight interactive children participates in the keyboard model; Tab is preventDefault'd inside the content (this seat's MenuContentImpl read), so the chips are pointer-only at every viewport and speed is pointer-only below `sm`. `DropdownMenuRadioGroup`/`DropdownMenuRadioItem` ship on the imported subpath, unused; the shape (two grouped settings + one command) is a Popover. | :103-125; EasingPicker.vue:16-36; MenuContentImpl.js:153/:190/:199/:205 (this seat) | **F.W3** — primitive adoption or Popover re-home; preserve the correct `aria-required-children` reasoning (S-1). Live keystroke → SS-13 (severity re-opens to blocker-class on confirmation). Contradicts lane-frontend:629's superlative — rationale right, seat wrong (C's own contradiction, upheld). |
| **D-12 / C-20** | `currentLevel` fabricates `N = 1` (or `t = 0.00`) when both payloads are null, and the dock DOES mount in that state — `hasData` includes `|| store.computing`; zero of the store's three status flags consulted; a fully enabled transport over nothing. | :39-54; VisualizationView.vue:121, :235 | **F.W4** (consult `computing`) + **F.W1 rider** (7.0.0 `Metric` gains `loading?: boolean` — D's own §3 I-a). |
| **D-2** | `.play-btn`'s `#fff` glyph sits on an always-full-strength 7-stop rainbow `::before` (backdrop-filter stacking context; App.E step-2 paint; no opacity rule ever fires — D-17) at ≈1.19–2.01:1 across the light arm vs the 3:1 SC 1.4.11 floor; `is-playing` scrolls the ramp so the glyph cyclically fades exactly while playing. Sole hard-coded colour family in the file, in a repo that documents light-arm contrast repair (`--viz-amber`). | :151, :153, :158-167, :176 (this seat); both readers' independent composites | **F.W4** — tokenise with D-3/D-13/D-17 in one edit; composited ratios → SS-13. (R-5: MAJOR, not LC's BLOCKER.) |
| **D-3** | `:focus-visible` is a literal 60%-white outline offset onto the near-white light-arm glass (≈1:1) while the canonical `var(--ring)` pattern exists at the global layer with four adopters and a written scoped-styles rationale — the same trap D-1 documents, already solved in-tree. SC 1.4.11 alone (K-9: the 2.4.11 citation is dead; do not carry it into a gate). | :183; style.css:128-143 (this seat) | **F.W4**. |
| **D-5** | `.summary-speed` at `--foreground` 35% alpha cannot reach 4.5:1 in either theme (arithmetic ceiling ≈2.2:1) — and it is the collapsed dock's only text, at 11px (`text-mono-micro`, unambiguously normal-size — the D-6 kill makes this stronger, no large-text argument exists). Scope: the rule reaches the amount span by inheritance, not the unit (LC's correction). | :192; MetricBadge compiled chunk (this seat) | **F.W4** — delete with C-14/C-15. |
| **D-7 / L-14** | The collapsed progress bar is two bare divs — no role/name/value, not even `aria-hidden` — at ≈1.5:1 fill-vs-track; the sole position readout in the collapsed state (the caret lives in the expanded branch). The producer's own 7.0.0 `Timeline` is emphatic that this shape is `role="progressbar"`. | :74, :190-191 | **F.W4** — with D-23 (INFO) as the design consideration and fr-BasisCanvas D-27 (the `N = k` DOM hoist) as the sibling cure. |
| **D-8 / C-19** | `prefers-reduced-motion` gates 1 of 6 animated surfaces in the file; the 60fps clock is entirely ungated (store read whole — zero references) and AUTO-STARTS on data arrival; keyframes 4.3's honoring flag unused. Decoration gated, substance not — CSS habit, not contract. | :178-180, :155, :181-182, :191, :196-197; animation.ts (whole); useWorkspaceLoader.ts:99-122 (this seat) | **F.W4** — identity = census FE §8 (folded); `DockBackgroundToggle` remains the canonical seat at both pins; M-7 extends the inventory into the children. |
| **C-12** | `exportFrame` means options-modal at one mount and direct no-options call at the other; `Record<string,boolean>` over a closed two-key vocabulary lets `{withLabel:false}` compile and do nothing. | VisualizationView.vue:98/:236; FullscreenViewer.vue:138; BasisCanvas.vue:462-469 | **F.W4** — one payload-typed contract (fr-BasisCanvas's export-truth cluster is the co-owner; its BC-13 cure-law applies: a named interface alone re-ships the drop class). |
| **L-6** | The broken scrub pairing is forked verbatim into `ConvergenceTimeline.vue` (same flag, same guards, emits sink — my side-by-side read), which also carries the same two FA glyphs at a fourth size and the same dead token — so L-1/L-3/C-6 are properties of TWO timelines and a one-file fix leaves the other. | ConvergenceTimeline.vue:36-64, :136 (this seat) | **F.W3** — both forks die in the same re-derivation; M-14 names the coupling that forced the fork. |
| **M-1** (both readers, merged: DU-M1 + LC-M3) | The defect is a reimplementation of a solved problem: the installed `ScrubberTimeline` declares an *unconditional* `scrubStart`/`scrubEnd` session pair owned by the primitive's own pointer-capture machine ("pointer-capture drag, keyboard a11y … `label` tooltip caret" — my d.ts read), which is precisely what GlassTimeline.vue:43-57 hand-rolls wrong in the three booked ways. All three axes cited the fork; none noticed the solved contract. | ScrubberTimeline.vue.d.ts:11-22 (this seat) | **F.W3** — with K-14's route: at 7.0.0 the pairing is re-derived on `<Slider>`'s own `@pointerdown`/`@pointerup` + `pointercancel`. |
| **M-2** (DU) | `.play-btn`'s hand-rolled glass is OFF the `--glass-level` accessibility escape: under `prefers-reduced-transparency: reduce` and `prefers-contrast: more` the dock flattens/firms (a11y-fallback.css, my read) while the literal `rgba(255,255,255,…)`/raw-`hsl()` pill keeps its translucent blur and full-strength rainbow — the user's explicit contrast request never reaches the one element that fails contrast. Strongest available argument for the `<Button variant="glass">` adoption; the axes argued from duplication only. | a11y-fallback.css:12-38; :149-167 (this seat) | **F.W4** (with L-9/C-7); rendered appearance under both media queries → SS-13. |
| **M-3** (DU) | `caretLabel` — the value half the component's logic exists to produce — is `opacity:0` at rest, revealed only on `:hover`/`[data-held]`, with no touch equivalent: on a touch device the dock never shows position or level at rest in either state (combined with D-7). The corpus audited the label's correctness (L-4) without noticing it is largely unseen. | GlassTimeline.vue:90-105 (this seat) | **F.W3** — the re-derivation must decide an at-rest, touch-reachable readout; D-23's swap is the ready design answer. |
| **M-9** (LC) | `useWorkspaceLoader`'s data watcher force-resurrects playback: `else if (!anim.playing) anim.play()` on every subsequent data arrival — pause the animation, change `n_harmonics`, and playback restarts against the explicit user pause, violating the store's own documented `playing`-stays-user-intent invariant (the S-4 architecture). Also the most reachable path that restarts the clock over an L-1b-stranded flag (`reset()` never clears `scrubbing`). | useWorkspaceLoader.ts:117-119; animation.ts:34-42, :132-135 (this seat) | **F.W4** — cross-ref fr-BasisCanvas D-8/BC-25 (auto-start's reduced-motion face; identities distinct: this row is intent-override on ANY recompute). |
| **M-15** (LC) | The declared build gate is RED at the audited substrate: `vue-tsc` fails on `PaperView.vue(12,8) TS2882` (`@mkbabb/latex-paper/theme` side-effect import) — measured three times independently (both readers + this seat); `build` = `vue-tsc -b && vite build`. Tree-level; the reason the L-2 seam has no live gate. Provenance caveat: `web/package.json` is in the frozen M.W1a modified set. | this seat's run; package.json:8 | **F.W0** — substrate green before F.W1 opens. |

### MINOR (34)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **L-1b** | Stranded `anim.scrubbing===true` (writers exactly :117/:122; `reset()` never clears it) forces the trail's full-rebuild branch per frame — DEMOTED by this seat (R-15): compound-conditional, self-heals on the next drag. | **F.W3** (dies with L-1); cheapest interim: timelines read `anim.scrubbing` instead of triplicating private flags. |
| **L-4 / C-11** | Dock caret `Math.round` vs canvas floor-and-lerp — one-level disagreement whenever frac(pos)>0.5; DEMOTED (R-13: exposure is hover/drag-only per M-3). | **F.W4** — one `levelAt` composable (with L-5). |
| **L-5** | The `easedT → level` map: **three** sites, two rules (fourth site killed, K-6); `composables/` exists and holds no level seat. | **F.W4**. |
| **L-16 / C-24** | Data-flow split: `activeBases` by prop, payloads by 471-line god-store reach-in — the structural cause of the L-4/L-5 family; untestable without router-bearing Pinia. | **F.W4**. |
| **C-5** | `--slider-scrub-track-height` does not exist at 4.0.0 (dist grep 0; the real surface ends at `--slider-track-height`) — dead CSS at **3 writer sites** (R-11), residue of the retired `glass-scrubber` variant; GlassTimeline's :120-123 comment names a variant that no longer exists. | **F.W3** — 3-line budget; correct knob or `size`. |
| **C-6** | The fork introduces 1% quantization (200ms steps at the hard-coded 20s duration) the upstream float scrubber does not have; caret (raw float, :62) desyncs from the range (rounded int); the fork's own reka-integers comment is refuted by the upstream composing the same reka slider over a float. | **F.W3** — desync pixels → SS-13. |
| **L-9 / C-7** | Two native `<button class="play-btn">` hand-roll ~48 declarations of `variant="glass"` chassis; the in-tree counter-examples are exact (`FullscreenViewer` fs-close; `ConvergenceTimeline.vue:60-61` — `<Button variant="glass" size="icon" class="play-btn">` for the LITERALLY same control, my read). Record correction retained: the A-tranche ledger's "ornament" rationale is contradicted — this is the primary transport control (LC verified the ledger lines). | **F.W4** + **GLASS-RELAY** if the rainbow needs a variant extension (premise → SS-13). |
| **D-11 / L-7 / C-8 / C-26** | Two icon grammars in one four-control row; the FA path literals are byte-identical pairs at **6 sites / 2 files** (K-17; my grep) across **four** un-tokenised sizes (17px/14px/`h-4`/`size-3`); lucide ships both glyphs; C-26's twin-block copy is the mechanism. | **F.W4** — the lucide swap also shrinks the F.W1 icon rename (census FE §5 ×35) and cures D-14's residue. |
| **D-10 / C-16** | 2 of 4 Tooltip callsites anchor on non-focusable nodes (`div`, `svg`) — hover-only (SC 1.4.13); the `:94` trigger is `display:none` below `sm`. DEMOTED (R-7). | **F.W3** — folds to intake **R3-7a** (identity stays there; riders: don't port as-is; `icon-tooltip` is the displaced seat; D §7's shim-hardening row). |
| **C-13** | Two animation engines: keyframes.js drives the secondary morph, a hand-rolled rAF drives the primary transport — and lost direction doing it. DEMOTED (R-16: harm is C-2's row). | **F.W1** — the engine decision. |
| **C-14** | `.summary-speed { @apply text-base }` is inert — the size axis lives in child-span utilities really emitted in the consumer build (this seat's `@source` probe: the "no-op" clause scopes to the producer's own build; the dist context is the documented BACKSTOP + the P9 components.css primary). The surviving half of the D-6 contradiction. | **F.W4** — delete with D-5. |
| **C-15** | The scoped `color:` bypasses MetricBadge's typed `color` prop — reaches the amount span by inheritance, never the unit (`text-muted-foreground` hard-coded in the chunk — my read). | **F.W4**. |
| **C-17** | `linear: (t) => t` hand-rolled at easings.ts:77 in a file importing `timingFunctions` at :9 (my read); third identity in the store's `?? ((x)=>x)` at animation.ts:28. Neither reader returned it; remainder-adjudicated by this seat. | **F.W4**. |
| **C-21** | `epicycleData.components.length` read where the payload ships `n_components` — a field with zero client readers (declaration-only). | **F.W4**. |
| **C-22** | `activeBases?: string[]` — open type over a closed vocabulary, consumed as one bit; typos pass silently. | **F.W4** — a `BasisKey` union; pairs with C-27. |
| **L-10 / C-23** | `maxWidth` default written twice (prop + provably-unreachable CSS fallback); bare `string` accepts `"banana"`. Moot under D-1; live the moment it is cured. | **F.W4** — rider on D-1's cure. |
| **L-11 + M-10 (LC)** | Persisted `speed` AND `easing` restored unvalidated (`if (as?.speed)` drops a legit 0; `as.easing as EasingName` asserts an open string into a 6-key union → silent linear degradation + no active chip — my loader read :56-57). Off-set speed renders an empty trigger beside a live `3×` badge. | **F.W4** — validate at the restore seam; production reachability → SS-13. |
| **L-12 / C-25** (source half) | Hand-expanded `v-model` written twice; `speed`/`easing` are the exactly-two template-written store fields that carry side effects (the speed watcher IS C-2's trigger) — the two that should have been actions. Runtime half killed (K-7). | **F.W4**. |
| **C-27** | `"fourier-epicycles"` magic string — **28 occurrences / 11 files** (my count; corrects the axis's 6/3 ~4.7× low and LC's "12 files" by one); no exported constant; `lib/bases.ts` the natural home. | **F.W4**. |
| **D-13** | `::after` sheen is positioned, z-auto → App.E step 6, above the in-flow svg — invisible today (white on white), exposed the moment D-2 is cured. | **F.W4** — same edit as D-2. |
| **D-15** | Four un-tokenised icon sizes for one glyph family (17/14/16/12px — the fourth found in ConvergenceTimeline by the K-17 sweep); 17px maps to no scale step. Remainder-adjudicated (neither reader returned it). | **F.W4**. |
| **D-16** | Motion-token drift at the CORRECTED arithmetic (K-15): `var(--ease-standard)` at 2 of 5 transition sites; raw `ease` at :166, UA default at :155, `linear` at :191 — against the :195 comment claiming "named properties + canonical token". | **F.W4** — with M-7. |
| **D-17** | Dead declarations: the `::before` opacity transition never fires (nothing changes its opacity — also why the D-2 rainbow is permanently full-strength); `border-radius:9999px` on both pseudos redundant under `overflow:hidden`. | **F.W4**. |
| **D-19** | Collapsed tooltip 'Pause'/'Play' vs expanded '…animation' and the constant '…animation' accessible name — microcopy drift only (K-10: the 2.5.3 citation runs the conforming direction; never carry it into a gate). | **F.W4**. |
| **D-20** | The second style block is unscoped and ships `.menu-popup`/`.menu-item` app-wide with zero external reuse (grep: this file only). The unscoped MECHANISM is correct — the content is **portaled** (`DropdownMenuPortal` in the chunk — my read), so DU's ":deep() would be exact" prescription is killed (K-18); the defect is the two generic NAMES. | **F.W4** — namespace or a global `data-*` hook, never `:deep()`. |
| **D-21** | `calc(100dvw - 1rem)` measures the viewport including a classic scrollbar. Moot under D-1, live when cured. Remainder-adjudicated. | **F.W4** — same edit as D-1. |
| **M-4** (DU) | `.play-btn` nests a second `backdrop-filter` inside the dock's own — defeating the producer's containment mitigation, which names this exact stack ("the Button-in-glass-in-dock stack re-samples the already-blurred buffer at each level" — shell.css:79-83, my read) — and then animates `transform: scale()` on that element per hover. | **F.W4** — dies with L-9/C-7's variant adoption. |
| **M-5** (DU) | The collapsed speed badge has no accessible name — AT hears a bare "1 ×"; `MetricBadgeProps` ships `label`/`abbreviation` as the sanctioned slots. With D-7, the collapsed a11y tree is: one correct button, two role-less divs, an unlabelled number. | **F.W4** — a prop, not CSS. |
| **M-6** (DU) | EasingPicker chip labels render at **9px desktop / 10.1px mobile** (`font-size: 0.5625rem` at :89 against the 1rem/1.125rem roots — my reads), below the system's smallest rung (`--type-micro` 11px, "fixed sub-control micro"), in muted foreground, inside the menu this component owns. | **F.W4**. |
| **M-7** (DU) | The motion-token drift continues into the children under the same banner: EasingPicker.vue:93 `transition: color 0.15s` (bare) twelve lines below its A.W3.d comment (:78 is correct); GlassTimeline.vue:96 `opacity 0.2s ease` — and all three child transitions are additionally reduced-motion-ungated (extends D-8's inventory to 8 across the mounted subtree). | **F.W4**. |
| **M-8** (both readers, merged) | `.mini-fill { transition: width 0.1s linear }` on a property rewritten every rAF tick — a transition retargeted every 16ms with a 100ms window never completes: the collapsed dock's only position readout is structurally ~100ms behind the clock, and the browser runs a live interpolation for the whole playback. Deleting one line fixes correctness AND cost, and removes one D-8 surface. | **F.W4**. |
| **M-11** (LC) | `tArr`'s getter mints a fresh array identity on every `anim.t` write (60/s), so the Slider subtree receives an always-unequal `modelValue` and re-renders every frame for a value that changes ~5×/s at the 1% step. | **F.W3** — dies with the M-1 re-derivation (float model or stable array); magnitude → SS-13. |
| **M-12** (LC) | Both dock layers are always mounted (`inert`-toggled — dock.js:715-730, my read) and the template reads `anim.t` directly (:74, :91→:53), so the component's render effect subscribes to the 60Hz clock and re-runs the whole two-branch vnode tree every frame beside two Canvas2D surfaces. | **F.W4** — isolate the two 60Hz readouts; magnitude → SS-13 profiler question. |
| **M-14** (LC) | `GlassTimeline` hard-wires `useAnimationStore()` behind a one-prop surface — unusable anywhere the store isn't the source, which is precisely why `ConvergenceTimeline` (props-in/emits-out) had to fork it. The upstream it shadows is store-agnostic by construction. | **F.W3** — the coupling correction collapses L-6's two files. |

### INFO (14)

| id | claim | disposition |
|---|---|---|
| **C-1** | Peer graph does not close (glass-ui 4.0.0 peers `^0.10.0 || ^0.11.0`; keyframes hard-depends `^0.13.0`); `npm ls` ELSPROBLEMS — reproduced by this seat, third run. No live break (LC's symbol audit). Component-altitude INFO (R-14). | fold → **F.W1** census §1 pins + G33 (one identity with fr-BasisCanvas's C-8; never re-book per component). |
| **D-14** | Perceived-centre shift on toggle — mechanism corrected by this seat: the two FA glyphs have different viewBox aspects (320/512 vs 384/512) letterboxed into one square, so rendered width jumps ~20% per toggle; the centroid framing was unmeasured. Remainder-adjudicated. | **F.W4** — dies with the lucide swap (D-11). |
| **D-18** | `mode="out-in"` at 0.15s each way = ~300ms of degraded-opacity glyph per toggle ("no glyph at all" killed, K-12). | **F.W4** sweep. |
| **D-22** | Import-block drift with the dangling `, }` (:5-6) — verified verbatim by this seat. Remainder. | **F.W4** sweep. |
| **D-23** | Collapsed summary surfaces the least dynamic value (speed) and omits position — the D-7 cure's design consideration; the M-3/M-13 evidence is its argument. | folds into D-7's **F.W4** row (cross-ref fr-BasisCanvas D-27). |
| **D-24** | No explicit `side` on the menu content — placement-by-collision-flip; D-axis mechanism corrected (K-19: dock is `dock-inline`; bottom placement is the wrappers'). One `side="top"` = placement-by-design. | **F.W4**. |
| **C-18** | Zero value.js colour consumption in the tree's most colour-dense transport surface (honestly scoped by the axis: colors.ts is NOT in this graph). | **NO-WAVE-OWNER** observation; the colors.ts deletion identity stays with census W.L5. |
| **C-28** | MetricBadge ships `cursor-pointer` + focus ring unconditionally, no `interactive` prop (my chunk read) — false affordance on a static readout; producer seam. | **GLASS-RELAY**. |
| **C-29** | The one e2e keystone over this menu is `test.fixme` — every finding inside the menu (D-9 included) is un-gated in CI by construction. | **F.W4** — gate re-enable rides the cure wave. |
| **C-30** | Fold of intake **R6-8** (operation↔client leaves non-isolable); this component is the display surface of the ambiguity, not the defect site. | fold by reference — identity stays with R6-8's **F.W5–W8** carry. |
| **L-8** | R5-7's blind-spot class transposed: two `v-if/v-else` icon twins invisible to an `:is`-keyed budget (R3-10). Scope-of-budget claim, not a runtime defect; neither reader returned it — remainder-upheld (the six R3-10 sites are all literal `:is`). | **NO-WAVE-OWNER** — F.W4 exhaustiveness-sweep budget note. |
| **L-13** | The 4→7 break surface (`./metric-badge`, `DockDropdownTrigger`, the lucide rename) is a DEFERRAL, not a live defect — both exports present at the installed pin; the `:value` rename already landed. | **F.W1** — identity stays with census FE §5 / C-4-census. |
| **L-15** | The 60fps watcher depends on both `t` and `easedT` (pure derivation — one flush); the sharper inverse survives: `easing` changes are unobserved, so changing easing while paused does not redraw. | fold by reference → **fr-BasisCanvas BC-18** (one identity). |
| **M-13** (LC) | In the DEFAULT configuration the caret reports `t = 0.42` while the canvas paints `N = 34` — different quantities, no legend on either; there is no configuration in which the two labels agree (the other mode disagrees by rounding, L-4). | fold-rider on **fr-BasisCanvas D-9/BC-8** (the fabricated-`N` family; this record adds the dock-side face) → F.W3/W4 with that row. |

**Reader misses folded WITHOUT re-booking (identity guard)**: LC's "write-only `AnimationSettings`" (duration/fps/max_circles round-tripped and discarded; renderer hardcodes `ref(80)`/20000ms — re-verified by this seat's greps) **= fr-BasisCanvas BC-9/C-6/D-20**, already banked MAJOR → F.W5–W8 with server-side units; LC's "fullscreen BasisCanvas never precomputes its trail" (one `precompute` call site inside a non-immediate watcher; fullscreen mounts warm — re-verified) **= fr-BasisCanvas M-β3**, already banked MAJOR → F.W3/W4. Two independent re-derivations of banked rows are corroboration, not new bookings.

## Killed-claims register (20)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| K-1 | **C-S-2** (superlative): the `.animation-dock:where(.expanded)` hook is "correct at 4.0.0" | The corpus's worst error — a superlative certifying the top structural defect as sound. C quoted the inner div's class list without the two lines above it: the subtree ROOT is `.glass-dock-frame` with a static class list; the quoted `.glass-dock` is its child receiving `$attrs` under `inheritAttrs:!1`. Scope-id and class land on different elements. Killed by both readers; re-sealed by this seat's dist read. |
| K-2 | **D-6**: the scoped `text-base` "wins" and the `size` prop is inert, badge ~half the rail | No such contest exists — the size axis is child-span utilities (`text-mono-micro` = 11px), children owning `font-size` never inherit the root's, and the utility IS emitted in the consumer build (the `@source "../*.js"` backstop + P9 `components.css`; this seat probed the "no-op" falsifier — it scopes to the producer's own build and does not land). C-14 is the surviving reading; D-5 gets STRONGER (no large-text argument). Killed by both readers, ratified. |
| K-3 | **C-10** failure scenario: "two authorities write `width` on the same element; the consumer value steps mid-spring" | The consumer rule matches nothing (D-1) — exactly one width authority. Surviving half (sanctioned `--dock-max-inline-size` unused; src grep 0 — this seat) re-booked constructively as D-1's cure. Killed by both, ratified. |
| K-4 | **L-1 permanence**: "permanently freezes"; "recovery requires an unrelated action; none is discoverable" | The next value-changing drag re-enters `onValueCommit` with the stranded flag still true → `endScrub()` → restart; a speed change also restarts (`watch(speed)` fires on `playing===true`). LC's kill ratified; seat adds: `play()` early-returns on `playing===true`, so the play button specifically needs pause→play. |
| K-5 | **L-1 thumb-tap mechanism** (BOTH readers) | **Seat-minted.** `variant="standard"` paints the thumb `width:0;opacity:0;background:0 0;border:none` (dist css) and the d.ts states "NO VISIBLE THUMB AT ALL"; a zero-area element cannot be a pointer target, so `SliderImpl`'s thumb-focus branch is unreachable. The DEFECT survives via track-press-within-±half-step at the playhead + pointercancel; only the cited mechanism is dead. Both readers cited reka's thumb branch as the confirmed route — neither checked which variant the consumer mounts. |
| K-6 | **L-5's fourth site**: `svg-fourier.ts` as a fourth implementation | It brackets by VALUE over the statically-shipped morph JSON and never touches `easedT` (this seat's grep: 0 hits). DU's falsifier ("all four map the same normalized clock onto the same levels") is false at this site. Three sites, two rules. |
| K-7 | **L-12's runtime half**: "two live Select popovers / two mounted reka listbox subtrees at every viewport" (DU's cell) | The `:117` instance lives inside `DropdownMenuContent` → `DropdownMenuPortal` + reka `Presence` with `forceMount` unset — **unmounted while closed** (this seat's dist reads). Source-duplication half stands. |
| K-8 | **C-9's mitigation**: "they remain reachable by Tab" | `MenuContentImpl.js:205` preventDefaults Tab for any keydown inside the content; arrow nav walks only the reka collection. The chips are pointer-only — the finding gets STRONGER (2.1.1-class), the mitigating cell is dead. LC's kill, re-verified by this seat. |
| K-9 | **D-3's "SC 2.4.11" citation** (the axis's, repeated by DU) | In WCAG 2.2, 2.4.11 is *Focus Not Obscured* (AA); *Focus Appearance* is 2.4.13 (AAA). The row survives on SC 1.4.11 alone; a false SC attribution in an audit record later gets cited as a gate. |
| K-10 | **D-19's SC 2.5.3 framing** | 2.5.3 requires the visible label be CONTAINED IN the accessible name; "Pause" ⊂ "Pause animation" — the containment runs the conforming direction. Microcopy row only. DU's kill, ratified. |
| K-11 | **D-4's attribution**: "EditorControlsDock owns the ghost toggle" | In the only state where AnimationControls mounts (`!isEditing`), the owner is **CanvasControlsDock** (mounted in VisualizationView, behind the opaque fullscreen backdrop). Conclusion unaffected; a repair wave following D's provenance would edit the wrong file. Both readers, ratified. |
| K-12 | **D-18**: "~300ms with no glyph at all, every toggle" | `out-in` sequences two 150ms opacity+scale fades — a glyph is partially painted through nearly all of it; fully-empty only at the crossover instant. Residual: ~300ms of degraded legibility on a transport control. Both readers, ratified. |
| K-13 | **C-1 as a component BLOCKER** | The fact is thrice-reproduced; the scope is the tree. Nothing in this component causes, worsens, or can repair it; glass-ui's three value.js-importing chunks resolve every symbol at 0.13.0 (LC's audit). Booked at repo altitude (census §1 pins + G33, F.W1); component-altitude INFO. |
| K-14 | **C-4's prescription**: "the migration is a two-line substitution" onto the 4.0.0 `GlassTimeline` | TRUE at the installed pin, STALE across F.W1: producer 7.0.0 exports `Timeline` only — `role="progressbar"`, "a timeline REPORTS"; the sanctioned commanding seat is `<Slider :marks>` (this seat's producer read). The de-fork must not target a primitive the uplift deletes. DU's kill, ratified. |
| K-15 | **D-16's ":166 token" cell** (BOTH readers) | **Seat-minted.** The live file: `:166` is `transition: opacity 0.3s ease` — raw keyword. Token at exactly :176/:196 = 2-of-5. Both readers inherited the D axis's cell verbatim while their own D-17 rows quote :166 correctly — each self-contradicting within one return. Recorded as a dual citation-inheritance event (DISSENT §1). |
| K-16 | **C-5 as a single site** (DU confirmed it as booked) | Three writers (this seat's grep): GlassTimeline 24px · SliderControl 16px · ConvergenceTimeline 20px. The remediation budget is three lines; the row is booked at the count's scope. |
| K-17 | **L-7/C-8's "4 copies" bound** | Six hits, two files (this seat's grep): ConvergenceTimeline pastes the same two literals at a fourth size. Both axes bounded the blast radius to one file; DU's sharpening ratified by measurement. |
| K-18 | **D-20's prescribed cure**: "`:deep()` from the scoped block … would be exact" (DU's cell) | **Seat-minted.** The menu body is portaled (`DropdownMenuPortal` imported and composed in the shipped chunk — this seat's read); `:deep()` compiles to a descendant selector rooted at the component's scope attribute, which no body-teleported ancestor carries. The prescription would ship a silently broken edit over a working one; the unscoped mechanism is correct, the generic NAMES are the defect. |
| K-19 | **D-24's mechanism**: "the dock is bottom-anchored (`position="fixed"` … `bottom-(--dock-pos)`)" | **Seat-minted** (against the axis cell; LC's confirmation avoided it). No `position` prop is passed (:58-63); dock.js's ternary resolves `dock-inline`; the bottom placement belongs to `.controls-overlay`/`.fs-controls`. Residue (no explicit `side`) survives at INFO. |
| K-20 | **C-27's "6 sites / 3 files"** (and LC's "12 files") | 28 occurrences / **11** files (this seat's count — LC listed 11 names and wrote "12"). The axis under-counted ~4.7×; the correction is booked at the measured scope. |

## Superlatives — with provenance (L-18 runs both ways)

1. **The `aria-required-children` reasoning** (:110-114 + EasingPicker.vue:10-15) — all three axes and both readers independently; structure re-verified by this seat (`group` IS an allowed owned element of `menu`; `aria-checked`+`menuitemradio` right where `aria-pressed` would lie; `anim.easing` a single ref so the set is genuinely exclusive). Operationally defeated by D-9 — correct reasoning inside the wrong container stays correct; preserve it when the surface re-homes.
2. **State-swapped accessible name, identical in both branches** (:67, :82) — the APG transport pattern, held even where the visible copy drifted (D-19). It is also the pause mechanism that keeps D-8 out of WCAG 2.2.2 territory.
3. **`@click.stop` vs bare `@click`, two lines apart, both correct** — the summary layer carries the dock's own click-to-expand (dock.js:727 `onClick` on `dock-layer--summary` — this seat's read), so the mini transport must stop propagation and the expanded twin must not. C-26 books the real cost: nothing says so.
4. **The reference-counted off-screen rAF gating with `playing` as user intent** (animation.ts:34-53, :96-113 — read whole by this seat): the credit count exists because inline+fullscreen canvases co-mount; the clock parks, the intent survives; every exit path cancels the rAF; the component itself has zero teardown surface. C-2, L-1 and M-9 are bugs *inside* and *against* good architecture — M-9 is the one seat that violates the documented invariant, from outside the store.
5. **Comments as checkable archaeology — now with two named exceptions**: the DropdownMenu migration note, the unscoped-block boundary notes, the CR-2/dock-context history and the store's excision note all verify (both readers + this seat). The exceptions are load-bearing: GlassTimeline's header still names the retired `glass-scrubber` variant and a thumb the standard variant does not paint (C-5/K-5's upstream source), and the :195 A.W3.d banner overstates its own token discipline (K-15). Comments this good are why D-1 and D-9 are findable; comments this stale are why K-5 and K-15 were needed.
6. **The typed `maxWidth` prop's intent** (:18-25 + FullscreenViewer's headstone) — retiring an invisible scoped-CSS-var handshake for a typed, defaulted, documented prop, recorded from both ends. The delivery fails for a producer-owned reason (D-1); cure it and this is the pattern for the rest of the de-shadowing programme.
7. **The one live producer-internals selector is a good coupling**: `.timeline-row:has(.glass-slider[data-held])` keys the cva base class + the documented dock-held reflection (LC re-derived from the compiled chunk) — in the same file as the dead C-5 token, which is the contrast worth teaching.
8. **The dock transport props are exactly right** (C-S1; `collapse-delay`/`start-collapsed` both real `DockProps` members) — checked precisely because C-3 proved `$attrs` swallows inventions silently.
9. **The multi-lens corpus worked**: on the highest-stakes structural question the axes split (D derived D-1; C certified its opposite as a superlative), the readers then split on L-1's permanence and L-12's runtime — and BOTH readers shared two inherited false cells (K-5, K-15) that only a fresh instrument pass caught. Single-lens auditing would have shipped K-1 as a green mark on the top defect; single-round re-reading would have shipped a dead gesture as the headline repro.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **D-1** corroboration — `getComputedStyle($('.animation-dock')).width` at both mounts; then the same after the `--dock-max-inline-size` rename (the cure's acceptance). Source derivation complete.
2. **D-2 / D-3 / D-5 / D-7 / M-6** — composited contrast ratios over the live glass in the light arm (≈1.19–2.01, ≈1.0, ≈2.2, ≈1.5 : 1; 9px chips). Directions arithmetically forced; decimals are not.
3. **L-1** — the corrected gesture: track press-and-release within ±half a 1% step (≈±3px on a 600px track) at the playhead, and a `pointercancel`; confirm the pause glyph + rainbow persist over a frozen `t`, and that the next drag or a speed change self-heals (K-4/K-5).
4. **L-3** — keyboard-seek snap-back within one frame during playback.
5. **D-9** — the keystroke walk: ↑/↓ reach only the Export item, Tab is eaten, the six chips + mobile SpeedSelect are pointer-only. On confirmation the severity re-opens to blocker-class (WCAG 2.1.1 A).
6. **C-2** — the mid-stroke direction flip + trail kink on a speed change during the reverse leg.
7. **C-6** — the caret/fill 1%-step desync during playback.
8. **L-4 / M-13** — the hover-window label divergence, and the default-config `t =` vs `N =` disagreement.
9. **M-2** — rendered appearance under emulated `prefers-reduced-transparency: reduce` and `prefers-contrast: more`: the dock flattens, the pill does not.
10. **C-5** — the delivered track height (20px vs the authored 24px).
11. **L-11 / M-10** — whether an off-set persisted `speed`/`easing` exists in production data.
12. **M-11 / M-12** — profiler magnitudes of the 60Hz Slider re-render and whole-dock vnode churn.

## Identity guards (folds by reference — never re-booked)

- **R3-7a** (intake, TRUE, CARRY→F.W3): the 4 Tooltip callsites — D-10/C-16 ride as riders; identity stays with the intake.
- **R3-10 / R5-7** (intake): budget classes — L-8 is a scope note, not a new row.
- **R6-8** (intake, ADOPT-AS-FACT, CARRY→F.W5): C-30 folds; this component displays the ambiguity, does not own it.
- **Census §1 pins + G33**: C-1 is that row AMENDED — one identity with **fr-BasisCanvas C-8**, F.W1.
- **Census §3a HARD shadows**: GlassTimeline/EasingPicker/easings — C-4/C-5/C-6/M-1 are that row's API+emits-level proof; F.W3.
- **Census FE §5 / C-4-census**: the 4→7 break table — L-13 is the per-component deferral correction; F.W1.
- **Census FE §8**: ungated rAF clocks — D-8/C-19 localize + M-9 hardens; identity stays census; `DockBackgroundToggle` the named seat.
- **Census W.L5 item 2**: colors.ts deletion — C-18 scoped out, no claim on this graph.
- **fr-BasisCanvas (banked)**: BC-9/C-6/D-20 absorbs LC's write-only-settings miss; M-β3 absorbs LC's fullscreen-precompute miss; BC-18 absorbs L-15; D-9/BC-8 absorbs M-13 as its dock-side rider; D-27 is D-7/D-23's sibling cure. One identity each.
- **Constellation memory** (value.js `ComponentSliders.vue` pointercancel cure): L-1's cancel route is the same producer defect class cured downstream once already — pattern echo recorded, not re-booked.
- **kf-AnimationControlsGroup** is a different product's component — no identity overlap.

## DISSENT — preserved, not ruled away

1. **Dual citation-inheritance events (evidence about the fleet, not the tree).** Both readers repeated the D axis's D-16 ":166 token" cell verbatim while their own D-17 rows quoted :166 correctly (K-15); DU additionally repeated the D axis's dead "SC 2.4.11" citation that LC had killed in the same round (K-9). Same signature as the ColorSpaceSelector L-10 and fr-BasisCanvas D-12 events: stale-cell repetition without re-derivation is detectable, and here it crossed BOTH hostile readers at once — the strongest argument yet recorded for the fresh-instrument adjudication seat.
2. **Both readers' shared thumb-tap cell (K-5)** is the same failure shape at the mechanism level: each read reka's `SliderImpl` correctly and neither read which `variant` the consumer mounts. The freeze verdict survived because the corrected route is *easier* to hit than the cited one — but a repair wave writing a regression test against "tap the thumb" would have authored an untestable repro.
3. **Reader-DU's severity dissents preserved**: BLOCKER on D-1, D-4 and L-1 (this seat ruled MAJOR on all three) on the argument that a dead structural contract, a lost fullscreen capability, and a transport that lies about its own state are each disqualifying on a component whose single purpose is that transport. If SS-13's live pass shows the L-1 freeze is hit routinely in ordinary scrubbing, L-1's severity re-opens.
4. **Reader-LC's escalation preserved**: D-9 at BLOCKER (WCAG 2.1.1 Level A). This seat booked MAJOR solely because no keystroke was fired; the static mechanism is verified and the seat expects the live pass to confirm.
5. **This seat's own scope notes**: the contrast composites (D-2/D-3/D-5/D-7) were adopted from the readers' independent, mutually-agreeing arithmetic and re-checked for direction only, not recomputed to the decimal; the C-3 git provenance (2f53d5d/ca58321/504844a) is the C axis's archaeology, uncontested by either reader and not re-walked here.

## Closing verdict

**ADJUDICATED — DEFECTIVE, zero BLOCKERs as booked but two blocker-weight clusters (transport-truth; keyboard-reachability, the latter one live keystroke from re-opening): 68 rows confirmed (20 MAJOR · 34 MINOR · 14 INFO) including 15 missed rows banked · 20 kills registered (4 minted by this seat against the readers, including one cell both readers inherited falsely twice over) · 2 reader misses folded to banked fr-BasisCanvas identities · routings: 1×F.W0 · 5×F.W1 · 13×F.W3 · the balance F.W4, with 3 GLASS-RELAY riders, 2×F.W5–W8 folds and 3 NO-WAVE-OWNER records — all agglomeration input to the forming X·F specs (M-25), stamping nothing.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above; the prior draft at this path is superseded per the header note.
