claude-fable-5 (served model id)

# fr-DarkModeToggle — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/layout/DarkModeToggle.vue` (109 lines; read whole by this seat) — the app's theme control: one bare `<button>` wrapping `FourierMorphSvg` (41 lines, read whole), glyph = a live Fourier partial-sum reconstruction morphing sun↔moon through `useFourierMorph` (230 lines, read whole) over `lib/svg-fourier.ts` (154 lines, read whole).
**Substrate**: fourier `cd26c65` + dirty WT (re-pinned by this seat: 28 porcelain rows; per the banked fr-AppHeader re-confirmation the dirt is the M.W1a pin-bump family — `web/package.json`/lock + gallery/equation SFCs — and the subject file itself is clean) · installed glass-ui **4.0.0** (peers value.js `^0.10.0 || ^0.11.0` — seat-read) · keyframes.js **4.3.0** (deps value.js `^0.13.0`) · value.js **0.13.0** · @vueuse/core **14.3.0** · glass-ui producer at **8.0.0** (seat-read `package.json`; exports probe: `./dark` TRUE · `./dark-mode-toggle` TRUE · `./controls` **FALSE** · `./hover-card` FALSE) — **one major PAST the 7.0.0 the corpus audited** (SR-2).
**Corpus**: three axis challenges (`audit/fourier-components/DarkModeToggle/challenge-{D-design, L-library, C-consumption}.md`) × two hostile re-reader returns (reader-1 design-and-usage · reader-2 library-and-code), delivered as two separable JSON returns — this record carries a reader-vs-reader ruled-disagreements table.
**Method (M-23/L-18)**: every reader-vs-reader disagreement was re-derived against the tree with this seat's own instruments before ruling; **two seat kills were minted that BOTH readers missed** (K-12 — the L-3 light-only scoping is stale-artifact-derived; K-13 — the D-m15 cure direction collides with a banked posture); **the identity guard caught FOURTEEN rows the readers adjudicated as new that were already banked** at fr-AppHeader (2026-08-07, whose subject subtree WAS this component's library closure), fr-App, fr-BasisCanvas, fr-AnimationControls, and the intake carries — folded by reference, never re-booked. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node/grep only; zero browser probes — probe parsimony; product source READ-ONLY; this file is the sole write)**: whole reads of the subject, `useFourierMorph.ts`, `svg-fourier.ts`, `FourierMorphSvg.vue`, `easings.ts:1-70`, `useMorphConfig.ts:1-20`, `index.html:15-40` (the pre-paint predicate, verbatim), `App.vue:1-30` (observer + boot `resolveVizColors()`), `colors.ts:10-25/:22-53/:75-96` (four parse arms — hex/hsl()/bare-triplet/rgb(), zero `oklch`, zero `light-dark`; `#888888` fallthrough at :53; `rainbow[4]="#c084fc"`; `legendre:"#9545b8"` seed; `resolveVizColors` assigns exactly five), `AppHeader.vue:135-145/:240-260` (`--toggle-size` 2.5rem/2.75rem; the toggle is the sole consumer), `DESIGN.md:25` (the `aria-pressed` house rule, verbatim), `e2e/paper-performance.spec.ts:320-345` (**the K-1 decider**: `:328 getByRole("button", { name: /switch to dark mode/i }).click()` + `:329 waitForTimeout(250)` + `:339-340` colour assertions) and the re-run of BOTH killed falsifiers (`grep -rn -i "dark" web/e2e/*.ts` → **4 matching lines, all paper-performance :328/:331/:339/:340**; `ls e2e` → **8** `*.spec.ts` + README + screenshots); the bracket walk of `interpolateAtHarmonicLevel` re-derived at clamped=5 (**i=2 matches first → lo=3/hi=5/t=1 — the K-4/K-11-family decider**) and at clamped=50 (lo=35/hi=50/t=1 — the `lo===hi` fast path unreachable for the shipped `[1,2,3,5,8,12,18,25,35,50]`); `lerpPoints` allocation count by construction (1 outer + 512 tuples = **513**/call); asset sizes on disk (sun.json **225,687 B** / moon.json **224,944 B** — byte-exact to both readers; plus the five orphaned ~271 KB route JSONs, context only); installed-dist reads — glass-ui `dist/dark.js` WHOLE (`darkModeSyncScript` handles `m==="auto"` AND writes `colorScheme`), `useGlobalDark-C28t0VWJ.js` WHOLE (five-member return `{isDark, toggleDark, disableTransitions, setDisableTransitions, onFlipSettled}`; `toggleDark`'s `no-transition` suppression gated on `setDisableTransitions(true)`; `colorScheme` watcher `{immediate:true}`; `onFlipSettled` = rAF-coalesced set; the one-shot `initialValue` seed contract), `tokens/color-radius.css:40` light `--neutral-0: hsl(40 30% 98%)` ≈ `#fbfaf8` (**Δ1 blue channel from the corpus's `#fbfaf9` — D-B1's light figure robust**) and `:263` **`--viz-fourier: oklch(0.579 0.201 30.4)` — the K-12 decider**; @vueuse/core dist: `useStorage` `writeDefaults = true` (:1908) + the `:1981` write-initial-on-absent line, `useColorMode` `initialValue = "auto"` / `storageKey = "vueuse-color-scheme"` / `listenToStorageChanges = true` (:2027); keyframes dist: `respectReducedMotion: !1` defaults (engine + instance) + `setRespectReducedMotion` d.ts:332, **8** dynamic `import(` in `dist/keyframes.js`, both `stop()` bodies present (AnimationGroup `…this.reset(), this._resolvePlay()…` vs Animation `…_cancelWAAPI…settle…_resolvePlay` — the banked fr-AppHeader two-class ruling re-confirmed); producer 8.0.0 reads — `package.json` exports probe (above) + `src/components/dark-mode-toggle/DarkModeToggle.vue` (`aria-pressed` :21-in-range, `type="button"`, `aria-hidden` svg, `disableTransitions` wired via `watchEffect`); seat censuses — `color-ring` = **exactly 1 hit tree-wide** (subject :99) with `AdminUserList.vue:248 focus:ring-ring` the sole ring-keyed utility, `hover: hover` guards = **0 tree-wide**, hover-scale register top = the subject's 1.12 (then 1.1 GalleryCard, 1.08 AnimationControls), `EASING_PRESETS|EASING_PRESET_NAMES` consumers of the `:29` re-export = **0** (useMorphConfig imports them from `@/lib/easings` directly, seat-read), `getIdlePoints|xyToPoints` external consumers = 0, duplicate prep site = `FourierMorphDemo.vue:95-96/:99-100` (seat-located; reader-1's `:93` off by two); prior-art verification — `DA-design-A4-equation-morph-chrome.md:56` row 9 read verbatim (**carries BOTH the `aria-pressed` half AND the tooltip half**), `raw-findings.json:1393` (the false-PRM-compliance census row, verbatim) / `:2268` (the "genuinely elegant DarkModeToggle" verdict) / `:3624` (the sRGB-mudding prior art); **registry identity sweep** — `grep -ln` over `adjudicated/*.md` + targeted reads of fr-App.md (B-1 fold, MG-γ, m-2/L-3/C-7, R-7/R-8, m-9/C-9, MG-ζ), fr-AppHeader.md (FR-AH-1/-7/-8/-9/-20/-21/-22/-23/-24/-30/-31/-37/-53/-55, the L-6 two-class quote ruling, the C-1 module-eval correction, the peers fold), fr-ConvergenceLegend.md (SR-1 precedent); git pins both repos.

## Scoreboard

**43 rows ADJUDICATED-CONFIRMED (0 new BLOCKERs booked — 1 BLOCKER-weight identity folded · 8 MAJOR booked + 6 MAJOR-or-weightier folded to banked identities · 17 MINOR booked + ~5 folded · 4 INFO booked) · 15 killed-claims register entries (11 reader kills RATIFIED, 2 reader-vs-reader kill conflicts MERGED, 2 NEW seat kills minted against BOTH readers) · 14 identity-guard folds (the readers ran ZERO registry sweeps — the largest single correction this seat makes) · 9 superlatives ratified (one with its banked falsifier attached) · SR-2 route-sharpening (producer 8.0.0; `./controls` GONE).**

The component-owned headline is **D-B2/L-6** — the glyph is bound to `isDark` only at mount and at click; every externally-driven flip (OS scheme under the stored `"auto"`, cross-tab storage — both live writers at installed vueuse 14.3.0) leaves the app's only theme indicator painting the WRONG state while its colour follows correctly. The consumption-weight headline (the 450 KB eager payload, 89%+ of the entry chunk) is real, re-derived, and **already banked as fr-AppHeader FR-AH-1 (BLOCKER)** — both readers re-booked it without sweeping the registry. The deepest seat finding: **both readers' L-3 adjudications (light-only grey-out, "dark mode is correct") are measurements of a STALE COMMITTED BUILD** — at the installed glass-ui 4.0.0 the `--viz-*` tokens are `oklch()`/`light-dark(oklch(),oklch())` and `cssVarToHex` has no `oklch` arm, so HEAD greys the palette in **both** themes (K-12; identity = banked fr-BasisCanvas D-1).

## The incredulity seat's own work — ruled reader-vs-reader disagreements

| topic | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **L-1/C-1 payload — severity + booking** | CONFIRMED MAJOR, booked new | CONFIRMED BLOCKER, booked new | **BOTH OVERRULED ON BOOKING — FOLD → banked fr-AppHeader FR-AH-1 (BLOCKER)**, banked 2026-08-07 with this exact identity (`App.vue → AppHeader:6/:141 → DarkModeToggle:23-24`, 450,631 B, 82.5% of entry chunk) | Registry sweep. FR-AH-1 also CORRECTS both readers' dead-payload arithmetic: levels 1-2 are user-reachable app-wide via `/morph` `setLevel` and level 3 is read on every morph (my bracket walk: clamped=5 → lo=3/hi=5), so the dead regions are the unread FIELDS (`original`+`decomposition`+`eval_points` = 75,210 B, 16.7%) — reader-1's "93,654 B/shape incl. levels 1/2/3" dies, reader-2's per-key census matches the banked one. Severity follows the banked row. |
| **L-3 grey-out — scope + severity** | CONFIRMED MAJOR: "light mode fails, dark passes", 11 byte offsets into `index-57FkGzlZ.css` | CONFIRMED BLOCKER (static half), same offsets, 4-of-5 light-only asymmetry; killed the sole-trigger arm | **FOLD → banked fr-BasisCanvas D-1/BC-1/C-1 (via fr-App B-1) — and BOTH readers' light-only scoping is KILLED (K-12)** | Both readers reproduced a **stale committed dist** (the 2026-06-12 build). This seat's read of the INSTALLED 4.0.0: `color-radius.css:263 --viz-fourier: oklch(0.579 0.201 30.4)`; `cssVarToHex` (:22-53, seat-read whole) has hex/hsl/bare-triplet/rgb arms ONLY. At HEAD the fallthrough fires in **both** themes — exactly what the banked fr-App B-1 record states ("every `--viz-*` token at installed 4.0.0 is oklch() or light-dark(oklch(),oklch())"). The readers' byte-exact offsets were real measurements of the wrong artifact. Reader-2's sole-trigger kill is RATIFIED on top (boot call in App's `onMounted` + external `isDark` writers — seat-read). |
| **C-7 vs L-10 — the destination-colour flash** | KILLED C-7's warm arm (microtask drain pre-paint), CONFIRMED L-10 at MINOR | CONFIRMED C-7 at MAJOR, KILLED L-10's scoping | **MERGED into ONE row at MINOR (N-1); both partial kills RATIFIED** | Seat trace against the whole files: cold path — `toggleDark()` (:69) flips `isDark` while `phase` is still `"idle"` (first write :169, after the 8-chunk engine await :149), so the idle branch (:47-50) paints the DESTINATION colour over the SOURCE glyph for the whole first-toggle fetch window; warm path — the idle-branch window closes inside one microtask checkpoint, but `morphProgress` is stale at 1 (:211, never reset before :176), so the animating branch paints destination for one frame, then the first rAF tick rewinds to ~0. One defect, two mechanisms, transient and cosmetic → MINOR. Neither reader's severity survives alone; each reader's kill of the other's scoping is right. |
| **level-3 reachability** | Kill text asserts "only 1, 2, 3 are unreachable, clamped out" | Kill: "level 3 IS dereferenced on every morph" (lo=3/hi=5/t=1) | **reader-2 WINS; reader-1's cell overcorrects** | My bracket walk: the `:136-142` loop matches INCLUSIVELY at i=2 for clamped=5 → `pointsByLevel.get(3)` runs with lerp weight 1. And BOTH are superseded by banked FR-AH-1: levels 1-2 are reachable via `/morph` `setLevel` on the same shared JSON. "Weight-zero, still read" is the only defensible phrasing. |
| **D-m8 premise** | CONFIRMED the axis cell "outline: none duplicates the base state" | KILLED the premise: no `outline` declaration exists on the base rule | **reader-2 WINS (K-8); seat correction SC-3 minted against reader-1** | Seat read of `:76-87`: position/width/height/cursor/border/padding/border-radius/background/transition/flex-shrink — **no outline**. The `:hover` declaration is dead because the UA default outline renders only on focus, which `:94-96`/`:98-101` handle separately. Reader-1 reproduced the axis's false premise inside a confirmation — premise-inheritance, second instance in this corpus family (cf. fr-AppHeader's C-1 module-eval event). Row survives at its conclusion (dead declaration + cascade-order-only ring survival). |
| **D-B1 severity** | MAJOR (nominal 2.51:1; translucent header ⇒ not worst-case-proven) | BLOCKER (sole indicator, whole morph path fails, precedent remediation exists) | **MAJOR — reader-1 wins, on house precedent** | fr-ConvergenceLegend precedent: a contrast failure with surviving capability (the control remains findable/operable; the aria-label carries state) grades MAJOR. Seat robustness check: installed light `--neutral-0 hsl(40 30% 98%)` ≈ `#fbfaf8`, Δ1 channel from the corpus's `#fbfaf9` — the 2.51:1 light figure survives the stale-artifact problem that killed L-3's scoping. Worst-case backdrop → SS-13. |
| **PRM row (D-M1/L-7/C-5)** | CONFIRMED MAJOR | CONFIRMED MAJOR | **BOTH OVERRULED ON BOOKING — FOLD → banked fr-AppHeader FR-AH-31, which adjudicated THIS row MAJOR→MINOR** | Identity guard: FR-AH-31 is this exact defect (JS morph unreachable by PRM; cure `gate morphTo → setShape(to)`), demoted with rationale (350 ms user-initiated decorative motion, not continuous). The two readers' joint MAJOR is recorded as a severity dissent on the fold — with their genuinely new aggravator: the engine ships `respectReducedMotion` (default `!1`, seat-verified in dist) so the cure is ONE OPTION away, and `raw-findings.json:1393` (seat-read) lists this component among PRM-honouring components — false compliance in a signed census. |
| **C-3 peer graph** | CONFIRMED MAJOR | CONFIRMED MAJOR (self-demoted from BLOCKER; latent — glass-ui's value.js-bearing modules unreachable from consumed subpaths, own BFS) | **FOLD → banked fr-AnimationControls C-1** (the `npm ls` ELSPROBLEMS identity; census §1; G33 gate) | Already ruled once at fr-AppHeader against a re-booking attempt ("never re-book per-component"). Seat re-verified the metadata triple (peers `^0.10.0 || ^0.11.0` / installed 0.13.0 / keyframes deps `^0.13.0`). Reader-2's dark-subpath BFS (dark.js reaches only vue + @vueuse/core) rides the identity as a scoping rider. |
| **engine `stop()` quote** | Quotes `stop() { return this.playback.stop(), this.reset(), this._resolvePlay(), this; }` | Quotes `stop(){ this._cancelWAAPI(), this.playback.stop(), this.settle(), this._resolvePlay(); }` | **reader-2 quotes the consumed class** — the banked fr-AppHeader L-6 ruling re-applies verbatim | Both bodies exist (seat grep): reader-1's is **AnimationGroup**; the `Animation` class `useFourierMorph` consumes is reader-2's. Second occurrence of the identical wrong-class grab this registry has already ruled on. Nothing turns on it — `_resolvePlay()` fires in both — but the citation must not survive into a repair ticket. |
| **C-17 falsifier hit count** | "FOUR hits — :328/:331/:339/:340" | "exactly one hit and it is line 328" | **reader-1 exact (SC-5, trivial, against reader-2)** | Seat re-ran the falsifier verbatim: 4 matching lines, all paper-performance. One REFERENCE, four lines. The kill (K-1) is unaffected. |

**Seat corrections minted against the readers:**

- **SC-1 (against BOTH — the stale-artifact event, K-12).** Both readers "re-verified" L-3's cascade at eleven byte offsets into `web/dist/assets/index-57FkGzlZ.css` and adopted its light-only conclusion. The dist is a committed build from 2026-06-12; the installed 4.0.0 tokens are oklch-spelled (seat-read), so the asymmetry they confirmed does not exist at HEAD — the grey-out is both-themes. Byte-exact reproduction of the wrong artifact is not verification; the fr-App record (same registry, two days older) already carried the correction. Measurement fidelity ≠ premise fidelity.
- **SC-2 (against BOTH — identity-guard failure).** Fourteen rows in the two returns re-adjudicate banked identities: FR-AH-1 (payload), FR-AH-7 (vendor-math), FR-AH-8/-20 (L-5's two arms), FR-AH-9 (stop-resolves), FR-AH-23 (MOON_COLOR comment + the cure-sequencing guard both readers "discovered"), FR-AH-24 (the `:29` re-export reader-2 filed as a new missed find), FR-AH-31 (PRM), FR-AH-37 (dead exports), FR-AH-53 (pencil-boil eager edge), fr-App MG-γ (the `"auto"` pre-paint hole reader-2 filed as its top missed MAJOR), fr-App m-2 (the MutationObserver hand-roll), fr-BasisCanvas D-1 (#888888), fr-AnimationControls C-1 (peers), intake R5-7 (declared by L-16 itself). The apotheosis registry is the first grep of the M-23 seat, not the last.
- **SC-3 (against reader-1)**: the D-m8 confirmation cell inherited the axis's false base-rule premise (table above).
- **SC-4 (against reader-1)**: the level-reachability kill overcorrected (level 3 is read; levels 1-2 are `/morph`-reachable) and the dead-region derivation (93,654 B/shape) dies with it.
- **SC-5 (against reader-2, trivial)**: falsifier hit count (4 lines, not 1); `FourierMorphDemo` prep sites are `:95-96/:99-100` (reader-1's `:93` off by two — reader-2 had it right).
- **SR-2 (route sharpening — producer drift, second occurrence after fr-ConvergenceLegend SR-1).** Producer glass-ui is at **8.0.0**; this seat's exports probe: `./controls` is **GONE** (with `./hover-card`, `./hover-popover`, `./metric-badge`), `./dark` and `./dark-mode-toggle` remain. Consequences: (a) fr-AppHeader FR-AH-1's F.W3 rider ("`./controls` exports DarkModeToggle at the installed 4.0.0 — the reconcile seat is live today") remains true of the PIN but the reconcile TARGET is now `./dark-mode-toggle`; (b) C-16's "not gated on the tri-package transaction" survives only pin-side; (c) every "at 7.0.0" disposition in this folder re-reads as "at 8.0.0". All producer source cites both readers checked still land at 8.0.0 (seat spot-read: `aria-pressed`, `type="button"`, `aria-hidden`, `disableTransitions` wiring).

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5-W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** → SS-3/SS-4 spec authoring. Identity folds are riders on the banked rows — never re-bookings.

### BLOCKER (0 new)

None booked here. The corpus's BLOCKER-weight facts live in banked identities: the 450 KB eager payload (**fr-AppHeader FR-AH-1**, BLOCKER, → F.W4 + F.W3 rider + SS-13) and the `#888888` palette collapse (**fr-BasisCanvas D-1/BC-1/C-1**, → F.W1). No crash path ships from this file today; the closest (L-5's brick + unhandled rejection) is error-path degradation, banked at FR-AH-8/-20.

### Identity folds (14 — the banked row owns the defect; this record contributes riders)

| our id(s) | banked identity (route) | rider contributed by this corpus |
|---|---|---|
| **L-1 / C-1 / D-M5** | **fr-AppHeader FR-AH-1** (BLOCKER · F.W4 boundary+prune, F.W3 shadow rider, SS-13 LCP) | Reader-2's six-literal `JSON.parse` offset table (439,100 B / 89.4%) corroborates the banked 82.5%-long-float scan on the same artifact; D-M5's convergence asymmetry (moon L18/25/35 pixel-identical to L50, sun L35 still 10.1 u off) sharpens the generator-side level-prune arm — the prune is per-shape, not symmetric. |
| **L-2 / C-2** | **fr-AppHeader FR-AH-7** (MAJOR · F.W4 + F.W1 fold at `./easing`) | Reader-2's independent eager-BFS (26 eager local modules; easings reachable by exactly one chain; glass-ui dist entries carry zero value.js edges) is the strongest sole-cause proof yet on this identity; `timingFunctions.linear(0.5)===0.5` run pins the benefit at the identity function. |
| **L-3** | **fr-BasisCanvas D-1/BC-1/C-1** via fr-App B-1 (→ F.W1, with fr-App's remedy facts) | K-12: the light-only scoping dies; HEAD greys BOTH themes. The corpus's contribution shrinks to: this component's click is A trigger (not THE trigger — K-10) and the four-arm enumeration of `cssVarToHex` re-confirmed at :22-53. |
| **L-5 (both arms)** | **fr-AppHeader FR-AH-8** (memo poisoned on existence; theme flips before the await) + **FR-AH-20** (post-:169 throw bricks the guard; `try/finally` cure) (F.W4) | Reader-1's near-miss bound (a value.js key drop would surface at :172 via `getEasingFn`'s soft-fall returning… no — the soft-fall PREVENTS the throw; the reachable-throw question stays open exactly as FR-AH-20 booked it, SS-13). |
| **L-15** | **fr-AppHeader FR-AH-9** (anti-teardown; stop RESOLVES; epoch/cancellation cure) (F.W4) | The warning both readers issue — the D-B2 cure (`watch(isDark) → setShape`) trips stop-advances-the-chain — becomes a SEQUENCING CONSTRAINT on M-1 below: the cancellation-token edit lands with-or-before the watcher. |
| **D-M1 / L-7 / C-5 (PRM)** | **fr-AppHeader FR-AH-31** (MINOR, demoted with rationale · F.W4 `matchMedia` short-circuit → `setShape(to)`) | Both readers' MAJOR dissent recorded; new aggravators ride: `respectReducedMotion` ships in the installed engine (default false, `setRespectReducedMotion` public — one property is an alternative cure) and `raw-findings.json:1393` books this component as PRM-COMPLIANT — a false census cell any F.W4 wave must correct in the same commit. |
| **C-3** | **fr-AnimationControls C-1** (F.W1 · G33 `npm ls` gate) | Fifth independent confirmation; reader-2's dark-subpath BFS scoping rider (exposure is metadata/latent — none of glass-ui's three value.js-bearing dist modules is reachable from this app's imported subpaths). |
| **D-m13 / L-13 / C-9** | **fr-AppHeader FR-AH-23** (+ fr-App m-9/C-9) (F.W4, sequenced AFTER the D-1 resolver cure) | The idle-branch dead arithmetic (`lerpColor(SUN,MOON,1)`/`(…,0)` recomputing two constants per reactive read, :47-50) and the colocation fact (the tree's only surface freezing viz-palette channels into literals) ride as cure-shape inputs. |
| **BOTH READERS' co-discovered "cure collision" missed row** | **fr-AppHeader FR-AH-23's own parenthesis** — "route through the resolved palette — AFTER fr-BasisCanvas D-1's resolver cure lands, else it lerps `#888888`" — the sequencing guard was banked two days before either reader "found" it | Two genuinely new arms ride: (1) the D axis's OWN repair order (challenge-D-design.md:508 item 2) prescribes the broken cure and calls it "theme-aware" — a corpus-internal contradiction with its own L-3 that neither axis nor reader linked to the banked guard; (2) the false-GREEN gate caution: `#888888` on the light page measures ~3.4:1, so a bare 3:1 contrast gate would PASS a dead indicator — any D-B1 gate must assert identity-colour, not ratio alone. |
| **r2 missed #1 (the pre-paint `"auto"` hole)** | **fr-App MG-γ** (MAJOR · F.W3/W4, one item with m-2/L-3/C-7 + the R-8 colorScheme/observer coupling rider) | Fully banked, including the vueuse `writeDefaults` mechanics this seat re-verified (:1908/:1981/:2027) and the `darkModeSyncScript` correct-generator fact. Reader-2's second-visit framing ("light first paint on EVERY load until hydration") is adopted onto the banked row as the sharper user-story. Reader-1 never found it — recorded. |
| **D-I2 / C-13 / L-17 (hand-rolled fork + missing colorScheme)** | **fr-App MG-γ + m-2/L-3/C-7 bundle** (F.W3/W4) | The `DarkModeToggle.vue:18` import edge proves the producer subpath is already a dependency — the adoption is free. |
| **C-15 + r2 missed #8 (pencil-boil edge)** | **fr-AppHeader FR-AH-53** (F.W4) | Reader-2's strengthening RATIFIED and ridden: the `!closed` branch is dead APP-WIDE (both call sites — `useFourierMorph.ts:81`, `HarmonicLevelGrid` — take the default `closed=true`), so `svg-fourier.ts:11` can be DELETED outright, removing one of the two eager vendor-paper edges rather than merely re-bounding it. |
| **r2 missed #5 (the `:29` re-export)** | **fr-AppHeader FR-AH-24** (F.W1 fold; colocation re-export dies in F.W4) | The genuinely new arm: CURE COMPLETENESS — C-2's cheapest-cure and L-2's falsifier both name only the `:21-27` import; leaving `:29` keeps the static easings binding and the eager vendor-math edge SURVIVES the repair. Seat-verified zero consumers of the re-export (useMorphConfig imports from `@/lib/easings` directly). The repair must strike `:21-27` AND `:29`. |
| **r2 missed #7 (dead public surface)** | **fr-AppHeader FR-AH-37** (F.W4 deletion batch) | `xyToPoints` added to the batch (exported at svg-fourier.ts:38, used only internally at :83 — seat grep); `currentPoints`/`getIdlePoints` already banked. |
| **L-16 (R5-7 class declaration)** | **intake R5-7 standing F.W4 carry** | +1 declared-negative instance, with the axis's correct adjacent booking: the only working element is the native `<path>` whose `:d` rewrites every frame — invisible to component-callsite-keyed derivations. |

### MAJOR (8 booked)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **M-1 · D-B2/L-6** | Nothing watches `isDark`: the glyph is written exactly twice — `onMounted` (:58-60) and `handleToggle` (:71) — while the owner is a global singleton whose value has two live external writers at the installed pins (cross-tab `storage` under `listenToStorageChanges: true`; OS scheme change under the stored `"auto"` mode — both seat-verified in @vueuse 14.3.0 dist). An external flip leaves the app's ONLY theme indicator painting the wrong state indefinitely — purple sun on a dark page — while `strokeColor`'s idle branch (:47-50) correctly follows, so colour and geometry contradict each other; and the next click computes `from` off the flag (:65-66), degrading a shape the user is not looking at. | subject read whole (109 lines — no `watch`); useGlobalDark chunk read whole; vueuse defaults :2027/:1908 | **F.W4** — `watch(isDark, …) → setShape` guarded on idle, **sequenced with-or-after the FR-AH-9 cancellation-token edit** (L-15's stop-advances hazard, both readers' warning ratified). SS-13: none needed — fully static. |
| **M-2 · D-B1** | The light-mode glyph — the button's only visual content and only sighted state channel — measures **2.51:1** against the page (WCAG 1.4.11 requires 3:1), and every point of the colour-morph path also fails in light; dark passes (7.5:1-class). Both readers reproduced the ratio table independently; this seat verified the premise survives the installed tokens (light `--neutral-0` = `hsl(40 30% 98%)` ≈ `#fbfaf8`, Δ imperceptible) — unlike L-3, this row is robust to the stale-artifact problem. The house precedent (`style.css` `--viz-amber` remediation, a signed fix of a LESS severe 3.54:1) makes it a known-class defect. Axe coverage cannot see it (`color-contrast` is text-node-only). | seat token read; both readers' concordant WCAG arithmetic (adopted as corroborated cells); style.css precedent (reader-verified, seat-spot-read at fr-App's record) | **MAJOR · F.W4** — sequenced BEHIND the palette identity per FR-AH-23's guard (a token-routed cure before the resolver fix ships a grey sun that PASSES 3:1 — the false-GREEN caution). Worst-case translucent-header backdrop → SS-13. |
| **M-3 · L-4** | Guard TOCTOU: `:63` reads `phase` which is first written at `:169` — strictly AFTER the `await getAnimationCtor()` (:149, genuinely 8 dynamic imports, seat count). Two clicks inside a cold engine-load window both pass, both flip the theme (net-zero, UI lies twice), and on resume chain A assigns and plays `animA`, then chain B's assignment overwrites the ONLY handle without stopping it — `stopAnim` (:115-120) and `onUnmounted` (:215) can never reach `animA` again: an orphaned, unstoppable ticker writing `currentPoints`/`phase`/`morphProgress` past unmount. Reader-2's mechanism (= the corpus's L-4) is the correct one; C-6's inverted causality is killed (K-2). | useFourierMorph.ts:63→:149→:169 chain, :115-120, :215 — all seat-read; keyframes dist import count | **F.W4** — same epoch/cancellation edit as banked FR-AH-9/-20/-21 (this row is that family's fourth arm, component-triggered); the guard must re-check after the await. Window width → SS-13. |
| **M-4 · D-M4/C-12 (unused flip-batching API)** | The installed singleton returns FIVE members; the subject destructures two (:33). `setDisableTransitions` — which arms `toggleDark`'s `no-transition` add/remove-on-rAF suppression (seat-read in the chunk: gated on `s.value`) — is never called anywhere (seat grep: zero hits for `no-transition|setDisableTransitions|onFlipSettled` in web/src + index.html), so every theme flip cross-fades every transitioned surface in the app at once; and `onFlipSettled` (the rAF-coalesced settle hook, doc'd for exactly this) is re-implemented in App.vue as an unfiltered-callback MutationObserver. The producer promotes the same switch to a public prop and wires it by `watchEffect` (seat-read at 8.0.0). | useGlobalDark chunk whole; subject :33; App.vue:10-18 | **F.W4** — one dark-sync item WITH the banked fr-App bundle (MG-γ + m-2 + R-8): adopt `darkModeSyncScript` + `installDarkModeSync`/`onFlipSettled` + `setDisableTransitions(true)` in a single transaction. Smear severity → SS-13. (The observer HALF is banked at fr-App m-2 — this booking is the unused-API half only.) |
| **M-5 · D-M2** | Motion budget inverted: defaults (:59-68, bound bare at subject :37) spend 300 of 350 ms (86%) on settle phases whose mean per-point travel is 18.1/5.0 u, and 50 ms — ~3 frames — on the identity-changing morph whose mean travel is 55.6 u (max 127.5 u): 9.3% of the viewBox per 60 Hz frame. Both readers reproduced the travel table to the hundredth from the shipped arrays; figures adopted as twice-corroborated cells. | useFourierMorph.ts:59-68; subject :37; readers' concordant re-derivations | **F.W4** — re-time (`morphMs` ≥ ~180 per the D axis; a design judgement, flagged as such). "Reads as a pop" → SS-13. |
| **M-6 · D-M3** | The two states share neither optical centre nor mass, and neither centres in the circular hit-box: sun centre (95.5, 96.0), moon (85.5, 105.6) — a 10.0×9.6 u jump (~3 px diagonal at 44 px) — with moon bbox area 59% of sun's; the hover-scale origin, the 50% border-radius plate, and the `outline-offset` focus ring all stay centred on (100,100), so the ring sits visibly off the moon's ink. Nothing in the prep path re-centres (seat-read svg-fourier whole). | readers' concordant bbox re-derivations; subject :83/:91/:100 | **F.W4** — translate each shape's bbox centre to (100,100) at prepare time (pure data fix). Perceptibility at 44 px → SS-13. |
| **M-7 · D-M6** | Input is silently discarded for the full 350 ms (:63) with zero state affordance: no `:active` rule anywhere in the scoped block (seat-read whole), no `aria-disabled`, no `[data-state]`, while `cursor: pointer` (:80) and the hover scale (:91) keep advertising interactivity through the dead window. Compounds M-3 (the guard is also the only queueing policy). | subject :63, :75-108 read whole | **F.W4** — either queue the toggle (latest-wins with the M-3 epoch edit) or surface the busy state; producer contrast: no lockout at all (`@click="toggleDark"`). |
| **M-8 · C-4** | keyframes 4.3 is consumed as a bare rAF ticker with its engine switched off: dummy `{v:"0px"}→{v:"1px"}` vars exist only to make it run, the interpolated value is discarded (`_vars` declared, never read — :135), `timingFunction` pinned `"linear"`, `useWAAPI: false`, and `t` is re-derived (`time / a.options.duration`) with easing re-applied from a SECOND value.js path (:162-164) — the structural reason FR-AH-7's 349 KB is unavoidable rather than incidental, and a per-phase `new Animation` + `parse()` per toggle besides. | useFourierMorph.ts:122-143, :162-164 — seat-read | **F.W4** — co-designed with FR-AH-1's boundary: either use the engine's own vars/easings (making the eager easings import deletable) or drop to a plain rAF tween and delete the engine dependency from this path entirely. Either arm satisfies FR-AH-7. |

### MINOR (17 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **N-1 · C-7 + L-10 (merged)** | The flash-forward-then-rewind (merged mechanisms per the ruled table: cold = idle-branch destination hold for the engine-fetch window, with the glyph still the source shape — a purple sun for a full network round-trip on first toggle; warm = stale `morphProgress=1` one-frame destination flash then rewind on every subsequent toggle). | **F.W4** — reset `morphProgress` to 0 at morph start (one line) + the FR-AH-8 catch posture covers the cold hold. |
| **N-2 · D-m1/C-17(a11y)** | No `aria-pressed` — against the project's OWN `DESIGN.md:25` rule (seat-read verbatim), the producer's toggle (`"aria-pressed": isDark.value`, seat-read at 8.0.0), and the 2026-05 D-audit row still open (DA-A4:56 row 9, seat-read). The swapping label announces the next action only. **Rider (r2 missed #3, ratified)**: the aria-label is a load-bearing e2e selector — `paper-performance.spec.ts:328` locates the control ONLY by `/switch to dark mode/i` (matches only the light state) and waits 250 ms against a 350 ms morph (:329, seat-read) — so the a11y repair and the producer-contract adoption are silently coupled to a spec none of the axes found; label changes must land with the spec edit. | **F.W4** — `aria-pressed` (+ consider `role="switch"` per the producer) in the same commit as the spec's locator update. |
| **N-3 · D-m2/L-12** | Bare `<button>` defaults `type="submit"` — latent (no form ancestor today, seat-informed by reader greps), producer sets it. | **F.W4** one-liner. |
| **N-4 · D-m3** | The SVG is not `aria-hidden` (seat-read FourierMorphSvg whole — no role/aria at either level); producer marks its glyph decorative. | **F.W4** — rides N-2. |
| **N-5 · D-m4 + D-m16** | `transition: transform 200ms ease` hardcodes the exact values the installed system tokenizes (`--duration-fast: 0.2s`, `--ease-standard` — reader-verified in the installed dist, seat-trusted as concordant); and the composable's provenance comment cites keyframes **2.2.0** against the installed 4.3.0 — two majors stale (the comment's CLAIM is already falsified by banked FR-AH-7; the version cite is its own hygiene line). | **F.W4** — token swap + comment correction (the comment edit is FR-AH-7's cure text anyway). |
| **N-6 · D-m5/C-18 + r1 missed #4 (ring dialect + coupling)** | `outline: 2px solid var(--color-ring)` (:99) is the tree's ONLY `--color-ring` reference (seat grep: 1 hit) in an app whose canon is `var(--ring)`; the ring is NOT dead (the glass bridge emits `--color-ring: var(--ring)`) — but its survival in the BUILT sheet is co-caused by the sole ring-keyed Tailwind utility in the tree, `AdminUserList.vue:248 focus:ring-ring` (seat grep): if that class dies, a used-keys emission can drop the variable and this focus ring silently vanishes — the exact BLOCKER the corpus's S-5 congratulates itself for refuting, currently masked by an unrelated admin file. | **F.W4** — adopt `var(--ring)` (one token, deletes the coupling). Rebuild-without-the-class probe → SS-13 (decidable by one `npm run build`, no browser). |
| **N-7 · D-m6** | 1.12 hover scale is the tree's largest (seat census: 1.12 > 1.1 > 1.08 > …) on an untokenized register, on the control with the smallest ink tolerance. | **F.W4** — register pass. |
| **N-8 · D-m7** | The `reduce` block (:104-108) nulls only `transition`, leaving `:91`'s transform: PRM users get an instantaneous 12% snap — motion reduction inverted into motion sharpening. | **F.W4** — rides the FR-AH-31 PRM edit. |
| **N-9 · D-m8 (rescoped per K-8)** | `outline: none` inside `:hover` is a dead declaration (no base-rule outline exists — the premise correction), and the focus-visible ring survives it only by cascade ORDER at equal (0,2,0) specificity — a reorder away from a hover-focus ring trap. | **F.W4** — delete the declaration. |
| **N-10 · D-m9/D-m10** | The 5rem `--toggle-size` default has never rendered (sole consumer always sets it — seat-read AppHeader :241-243/:255-257); mobile 2.5rem sits between the producer's sm/md/lg rungs (1.75/2.25/2.75 — reader-verified ladder) while desktop 2.75rem is an exact lg. | **F.W3/W4** — resolves inside the shadow-reconcile (SR-2: target `./dark-mode-toggle` at 8.0.0). |
| **N-11 · D-m11 (figures corrected per K-7)** | Ink spans **89.2%** of the box (39.25 px in 44 px, from the shipped L50 sun + 7 u stroke half-width — the corrected arithmetic; the axis's "92% / 40.6 px" was origin-measured) against a producer register of 63.6-66.7% and its SIGNED "too large" post-mortem (reader-verified comment block). | **F.W3/W4** — same reconcile. |
| **N-12 · D-m12/L-8/C-8 + r2 missed #4** | A 57,029-char, 17-significant-digit `d` string is rebuilt and re-parsed every frame (~21 frames × ~57 KB ≈ 1.2 MB transient text per toggle) for a ≤44 px glyph whose device-pixel quantum is 1.5 viewBox units; **513** allocations per tick (K-11's corrected count); and — the ratified r2 find — the `lo === hi` fast path (:150) is UNREACHABLE for the shipped levels (inclusive bracketing always yields distinct neighbours), so even the two exact levels this component requests (5 → lo=3/hi=5/t=1; 50 → lo=35/hi=50/t=1, seat-derived) fall through to a full 513-allocation copy of an array `pointsByLevel` already caches. | **F.W4** — exact-hit `pointsByLevel.get(clamped)` before bracketing + fixed-precision emission (2 dp ≈ 20.3 K chars) in one edit. |
| **N-13 · D-m14/C-11** | The sRGB channel lerp collapses chroma ~37% at t=0.5 between two near-isoluminant hues ~250° apart, passing through a dusty rose belonging to neither state — while value.js (already on the critical path, C-11's line-verified export roster) ships perceptual mixing. Prior art: M-audit `raw-findings.json:3624` (seat-read head). Reader-1 re-derived the OKLab figures independently; adopted as corroborated. | **F.W4** — perceptual mix (`mixColors`/OKLab) — sequenced with the FR-AH-23 palette family so the endpoints are token-derived before the interpolation is upgraded. |
| **N-14 · L-9/C-14** | Shape prep runs per-mount in `setup()` (10,240 tuple conversions before first paint of the never-unmounting header — cadence corrected per the banked FR-AH C-1 ruling: per mount, NOT per route), duplicated character-for-character (`prepareFourierShape(sunData as any)` ×2) in `FourierMorphDemo.vue:95-96/:99-100` (seat-located), and both sites launder the Python-generated asset through `as any` at the exact seam where regeneration can drift — the cast is FORCED by a real type mismatch (`domain: [number, number]` vs the JSON module's `number[]`, seat-read interface) and erases checking of everything else too. | **F.W4** — one shared prep module + a typed loader (`satisfies`/parse) at the seam; rides FR-AH-1's boundary edit. |
| **N-15 · r2 missed #6 (ratified NEW)** | Silent-degradation error posture: a renamed/dropped `partial_sums` key is swallowed (`if (ps)` :82), bracketing degrades (`loPoints ?? hiPoints ?? []` :146-148), `pointsToSvgPath` returns `""` (<2 points, :51), and the bound `:d=""` renders an INVISIBLE theme control — no throw, no warn, no degraded-mode signal, on an out-of-band Python drift surface the module's own header documents. (This replaces L-11's killed runtime-throw prediction — K-9.) | **F.W4** — `if (pointsByLevel.size === 0) throw` (or dev-warn) at prepare; pairs with N-14's typed loader. |
| **N-16 · r1 missed #2 (ratified NEW)** | Zero `@media (hover: hover)` guards exist tree-wide (seat grep: 0) and this file owns the tree's largest hover transform: on touch UAs that latch `:hover` after tap, every mobile theme flip leaves the toggle 12% enlarged until the user taps elsewhere. Producer sidesteps the class (background/opacity hover, no transform). | **F.W4** — capability-guard the hover block (tree-wide pattern candidate → note for SS-3/SS-4). UA latch on device → SS-13. |
| **N-17 · r1 missed #3 (ratified NEW)** | The toggle is the header's only interactive control with NO sighted-user affordance beyond position — no `title`, no tooltip — for an abstract morphing glyph; every sibling carries something (admin badge `title`, logo HoverCard, nav trigger visible label). The 2026-05 D-audit filed EXACTLY this (DA-A4:56 row 9 carries BOTH halves — seat-read verbatim: "…and no tooltip; a first-time user may not read the crescent as 'theme toggle'"), and the corpus folded forward only the `aria-pressed` half — a dropped prior-art arm. `TooltipProvider` is already mounted app-wide (seat-read App.vue). | **F.W4** — one glass-ui Tooltip wrapper; restores the dropped half of a 15-month-old Low. |

### INFO (4 booked)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **I-1 · D-m15 (RESCOPED per K-13)** | The tap-highlight inconsistency is real (sibling sets `-webkit-tap-highlight-color: transparent`; the toggle doesn't) — but the FILED cure (add the suppression) collides with banked **fr-AppHeader FR-AH-55**, which adjudicates that same suppression as a DEFECT on a control with no other press feedback. The toggle has no press feedback either (M-7). Surviving truth: the toggle lacks ANY touch press channel — cured by M-7's affordance work, not by suppression parity. | Rides M-7. The cure-direction kill is registered (K-13). |
| **I-2 · D-I5/C-16 + SR-2** | Uplift disposition — reconcile, don't replace — RATIFIED; the component's own glass seam (`./dark` only) is clean and is NOT in the break surface (its consumer AppHeader is — banked FR-AH-22). C-16's "actionable at the installed 4.0.0" survives pin-side; the 8.0.0 target moves the reconcile subpath to `./dark-mode-toggle` (`./controls` GONE — seat probe). | **F.W1/F.W3** — re-baseline note; every "7.0.0" cell in this folder re-reads at 8.0.0. |
| **I-3 · provenance-hygiene method note (r2 missed #9 + K-14)** | The C axis's self-repo citations drift ~10-11 lines while its node_modules citations are byte-exact — the drift pattern (plus the K-14 bundle) is a repair-time hazard: agents following `vite.config.ts:47` land in the wrong block. | **NO-WAVE-OWNER** — spec-input for SS-3/SS-4: re-resolve all corpus line anchors at wave open (the D-19 law, restated for this folder). |
| **I-4 · D-I4** | Empty-first-paint is BOUNDED as the axis states (`currentPoints=[]` → `d=""`; `onMounted` normally beats paint); only a suspended mount exposes it. | Booked as posture context for N-15; exposure → SS-13. |

## Killed-claims register — 15 entries

| # | killed claim | refutation (adjudicated) | seat instrument |
|---|---|---|---|
| **K-1** | D-I3 ("zero test coverage; 9 specs; grep → 0") + C-17's coverage half ("8 specs, none referencing the toggle"; falsifier "matches only unrelated toggle words") — both readers' kills **RATIFIED** | The control IS driven in CI by its accessible name: `paper-performance.spec.ts:328` clicks `getByRole("button", { name: /switch to dark mode/i })` and `:339-340` assert the theme actually changed. The axes' greps were case-sensitive (D-I3) or misreported (C-17 — its own falsifier, re-run verbatim by this seat, returns 4 matching lines all in paper-performance, none at the lines it cites, none in gallery.spec.ts). There are **8** specs, not 9. What survives: the coverage is incidental — nothing asserts the toggle's own state, contrast, or morph. | seat re-run of both greps + spec read :320-345 |
| **K-2** | C-6's mechanism ("chain B's `stopAnim()` cancels chain A's in-flight animation") — both readers' kills **RATIFIED** | In the only reachable race window both chains are suspended at `:149`; `currentAnim` is still null; `stopAnim()` is a no-op behind its `:116` guard. The real failure is assignment-overwrite orphaning `animA` (M-3). C-6's conclusion stands; its causality cannot occur. | seat trace :63/:146/:149/:169/:171 |
| **K-3** | L-1's byte measurement (449,770 B / 91.6%; offsets 25,916/251,160/…) — reader-1's kill **RATIFIED** | C-1's competing table (439,100 B / 89.4%; six literals) is the one that reproduces; L-1's figures overstate by ~10.7 KB / 2.2 pts and its anchors are off by 3-7 bytes. The structural conclusion (blobs and render fn share a module group) survives. | reader-1's re-measurement adopted (consistent with the banked FR-AH-1 scan of the same artifact) |
| **K-4** | L-1's "8 of 10 levels are pure ballast (~80% of partial_sums)" — reader-1's kill **RATIFIED, then CORRECTED** | The sweep brackets through levels 5-50 (7 of 10 read); level 3 is ALSO read on every morph (lo=3/hi=5/t=1 — reader-2's refinement, seat-derived); and levels 1-2 are user-reachable app-wide via `/morph` `setLevel` (banked FR-AH-1). The truly deletable payload from ANY consumer is the unread FIELDS (16.7%), plus per-shape level pruning per D-M5's asymmetry. Reader-1's own kill text ("only 1, 2, 3 are unreachable, clamped out") is itself wrong twice. | seat bracket walk + registry |
| **K-5** | C-7 as a universal per-toggle MAJOR — reader-1's kill **RATIFIED into the N-1 merge** | Warm-path idle-branch flash never reaches paint (one microtask checkpoint); the warm flash that IS painted is L-10's stale-progress mechanism. C-7 survives as the cold-start hold only. | seat trace (ruled table) |
| **K-6** | L-10's "every toggle but the first / ~16 ms" scoping — reader-2's kill **RATIFIED into the N-1 merge** | The first toggle flashes too (C-7's mechanism) and the cold window is the whole engine fetch, not a frame. One defect, two mechanisms, one booking. | seat trace |
| **K-7** | D-m11's "92% ink-to-box / 40.6 px" — reader-1's kill **RATIFIED** | Origin-measured extent, not an ink ratio: correct figures 89.2% / 39.25 px (the axis's own bracket states them one line later). Row survives at corrected figures (N-11). | reader-1 arithmetic adopted; consistent with reader-2's independent ink-bounds |
| **K-8** | D-m8's premise ("outline is already none on the base rule") — reader-2's kill **RATIFIED; reader-1's confirmation cell corrected (SC-3)** | Seat read `:76-87`: no outline declaration exists. Dead-for-a-different-reason; conclusion survives (N-9). | subject read |
| **K-9** | L-11's "renamed key … fails at runtime inside prepareFourierShape" — reader-2's kill **RATIFIED** | `if (ps)` swallows it; the chain degrades to an invisible glyph with zero diagnostics — silent, which is worse (booked as N-15). Only a missing `levels` array throws. | svg-fourier :79-85/:146-148/:51 seat-read |
| **K-10** | L-3's "this component is the only runtime trigger" — reader-2's kill **RATIFIED** | `resolveVizColors()` runs at boot in App's `onMounted` before any click, and external `isDark` writers flip the class through vueuse with no click; the grep enumerated API callers, not class writers. | App.vue:10-18 seat-read; useGlobalDark chunk |
| **K-11** | C-8's "1,024 array allocations" per frame — reader-2's kill **RATIFIED** | One `lerpPoints` call per tick = 1 outer + 512 tuples = **513**. The 57,029-char string is exact (twice reproduced). | lerpPoints seat-read |
| **K-12** | **BOTH READERS' L-3 scoping — "light mode fails, dark mode is correct" — SEAT KILL (new)** | The eleven byte offsets both readers reproduced live in the committed 2026-06-12 dist. The INSTALLED 4.0.0 spells `--viz-*` in `oklch()`/`light-dark(oklch(),oklch())` (seat read of `color-radius.css:263`; concordant with banked fr-App B-1/R-5), and `cssVarToHex` has no oklch arm — HEAD greys BOTH themes. The light-only asymmetry, and every disposition premised on "auditing in dark hides it", dies at HEAD. (D-B1 is unaffected — its token survived the retune, seat-checked.) | installed token read + colors.ts read + registry |
| **K-13** | **D-m15's cure direction ("add `-webkit-tap-highlight-color: transparent` for parity") — SEAT KILL (new)** | Banked fr-AppHeader FR-AH-55 adjudicates that exact suppression as a DEFECT on the sibling ("kills the last touch feedback channel"). Parity with a banked defect is not a cure; the surviving truth is M-7's missing-feedback row (I-1). Neither reader checked the collision. | registry (FR-AH-55) |
| **K-14** | Provenance-drift bundle (killed as citable cells; substance unaffected) | Reader-1's 11-cite list adopted (vite.config :47→:57 / :34→:45; package.json :15→:18; colors.ts :52→:53, :81→:80; App.vue/SvgFilters drift; katex importer count 2→4; D-B1's dark-arm 7.32→7.57 at the installed token) + seat additions: reader-1's `FourierMorphDemo.vue:93` → `:95-96/:99-100`; reader-1's L-15 `stop()` quote is the **AnimationGroup** class (the banked two-class ruling, second occurrence); reader-2's "exactly one hit" falsifier count → 4 lines. | seat greps |
| **K-15** | The corpus-wide "producer at 7.0.0" framing — reader-2's kill **RATIFIED + EXTENDED (SR-2)** | Producer at **8.0.0**; `./controls` gone from exports (seat probe), so C-16's ↯2 reconcile argument survives pin-side only and the target subpath is `./dark-mode-toggle`. All checked producer source cites still land. | producer package.json + exports probe |

## Superlatives — ratified with provenance

1. **THE COMPONENT — the concept is load-bearing, in writing, three audits deep.** A theme control whose glyph is a live Fourier reconstruction makes the app's thesis into its chrome. Provenance verified by this seat at the heads: `raw-findings.json:2268` ("the genuinely elegant DarkModeToggle that morphs a sun↔moon as a live Fourier epicycle path"), DA-A4:56 ("keep the lovely morph"), and the A.W3 button-ledger retirement (reader-verified). Every defect in this record is repairable without touching the idea; nothing here argues for deletion.
2. **The closed-path seam is exactly right.** `svg-fourier.ts:58-62` modular-indexes all four Catmull-Rom control points so tangents wrap at the seam — seat-read; the common clamped implementation leaves a corner at `points[0]` on every closed frame. (Both readers + all three axes concur.)
3. **The animation provably cannot escape the level table.** Three clamps in the right order (`:175` max, `:204` min, `:131` both-ends) over a degrade-don't-throw guard (`:146-148`) — seat-read. The same guard that enables N-15's silent failure is what keeps `/morph` green under overshooting easings: one posture, correctly priced in one place and mispriced in another.
4. **Index-aligned point arrays, and the naive lerp is provably near-optimal.** Reader-1 reproduced the axis's exhaustive 512-offset rotational search (best offset improves mean pairing distance 0.03%); adopted as a corroborated cell. The Python pipeline emitted matched parameterisations and the runtime is right to trust them.
5. **The lazy-engine boundary is the right instinct, correctly built — and its defeat is one edge, not a philosophy.** `loadAnimationEngine()` is 8 real dynamic imports (seat count); the memo is written synchronously before any await (:38-44). The banked FR-AH-7 framing stands: one static sibling import undid an otherwise-correct boundary — keep the boundary, fix the edge (and strike `:29` with it).
6. **The producer seam is granular and singly-owned.** `@mkbabb/glass-ui/dark` (not the barrel), sole `useGlobalDark` caller in the repo — one owner for a global boolean across a pre-paint script, an observer, and a control. Reader-2's dist-BFS (zero value.js edges from any consumed glass subpath) is the strongest version of this credit.
7. **The accessible name is real product infrastructure.** Action-phrased, state-derived, synchronous with the flip — good enough that CI's only handle on the control IS the label (K-1). Ratified WITH the C-17 note attached: the label leads the glyph by the full 350 ms morph, so the "never lies" claim holds for state, not for the visual.
8. **44×44 by construction (r1's find, adopted).** At the header's own sizing the toggle is the only WCAG 2.5.5-AAA-conformant control in it (45 px mobile / 44 px desktop — seat-verified the `--toggle-size` sets; sibling arithmetic adopted as reader cells). No axis measured it.
9. **THE CORPUS + READERS — measurement fidelity is extraordinary; the failures are inference.** Every numeric cell this seat re-derived (bracket pairs, allocation counts, JSON byte sizes, the falsifier greps, token values) reproduced exactly; the corpus's arithmetic reproduced across two independent readers to the hundredth. The kills in this record are all inference kills — a case-sensitive grep, an inverted causality, a stale artifact trusted byte-for-byte, a cure colliding with a banked posture. K-12 is the canonical lesson: **byte-exact reproduction of the wrong artifact is still the wrong answer.**

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **D-B1 worst case** — the true backdrop behind the glyph through the translucent header (`bg-background/60` + blur) at several scroll positions on /paper, /visualize, /gallery; the nominal 2.51:1 is adjudicated static.
2. **The L-3/K-12 readback, re-aimed at HEAD** — `getComputedStyle(documentElement).getPropertyValue("--viz-fourier")` in BOTH themes at the installed 4.0.0: expected `oklch(…)` (dark) / `light-dark(oklch(),oklch())` (light), `VIZ_COLORS.*` = `"#888888"` in both. One console evaluation; also the load test for this record's K-12 against the banked fr-App expectation.
3. **M-5/M-6 perceptual halves** — whether 50 ms reads as a pop; whether the 3 px centre jump and 41% mass drop are noticed at 44 px.
4. **M-4 smear severity** — how objectionable the unsuppressed all-surface cross-fade is per flip.
5. **M-3 window width** — cold-cache duration of the 8-chunk engine fetch (human reachability of the double-click).
6. **N-16 UA latch** — whether iOS Safari / Chrome Android hold the 1.12 scale after tap on this control (pairs with the I-1/FR-AH-55 touch-feedback pass).
7. **N-6 rebuild probe** — `npm run build` with `AdminUserList.vue:248`'s `focus:ring-ring` removed; grep the fresh sheet for `--color-ring`. Decidable without a browser; not decidable from the stale artifact.
8. **I-4 exposure** — whether a suspended/slow mount ever paints the empty button.
9. **C-1 byte-share on a FRESH build** — the import topology is proven from source; the 89.4% figure inherits the stale-dist caveat and should be re-measured post-FR-AH-1.
10. **Producer 8.0.0 registry state** — the version/exports facts are producer-worktree reads; publication state unverified (rides the F.W1 re-baseline).

## DISSENT — preserved, not ruled away

1. **Severity dissents.** Reader-2 holds L-1/C-1, L-2/C-2, and L-3 at BLOCKER (this record folds all three to banked identities — the first carries BLOCKER at its banked home, so the dissent is partially honoured by reference). Both readers hold the PRM row at MAJOR against banked FR-AH-31's MINOR; their joint conviction, plus the false census cell at `raw-findings.json:1393`, is recorded on the fold — if the F.W4 PRM edit does not land with FR-AH-31's cure, the regrade question reopens.
2. **Reader-1's kill of C-7 and reader-2's kill of L-10 were each half right** — preserved as the cleanest demonstration in this corpus family that two hostile readers attacking the same two rows from opposite ends can BOTH be correct about the other's scoping error while both mis-scoping their own confirmation. The N-1 merge is the union.
3. **Citation-inheritance events, tallied for the fleet record**: reader-1 inherited the axis's false D-m8 premise inside a confirmation (SC-3) and repeated the AnimationGroup `stop()` wrong-class quote this registry has already ruled on once (K-14); both readers inherited the stale-dist cascade premise (K-12) while performing genuinely independent arithmetic on top of it. The pattern matches the fr-App/fr-AppHeader DISSENT records: verification effort concentrates on arithmetic, premise provenance goes unchecked.
4. **The identity-guard gap is systemic, not individual.** Fourteen banked-row rediscoveries across two independent readers on one component says the M-23 reader brief does not currently mandate a registry sweep. Recorded for SS-3/SS-4: a reader return SHOULD carry a "registry collisions checked" cell, the way axis challenges carry falsifiers. (NO-WAVE-OWNER — spec-authoring input.)

## Closing verdict

**ADJUDICATED-DEFECTIVE, CONCEPT RATIFIED — the signature control is sound as an idea and encircled by banked identities as an implementation**: 0 new BLOCKERs (the two blocker-weight facts — the 450 KB eager payload and the palette collapse — are banked at fr-AppHeader FR-AH-1 and fr-BasisCanvas D-1, folded with riders, never re-booked); 8 MAJORs booked, led by the component-owned D-B2/L-6 watch gap and the L-4 guard TOCTOU (both dying in or beside the banked FR-AH-9 cancellation edit), plus the light-mode contrast failure sequenced behind the palette cure per the banked FR-AH-23 guard; 17 MINORs and 4 INFOs, dominated by F.W4 with the sizing/shadow rows on F.W3/W4 re-baselined to producer 8.0.0 (SR-2, `./dark-mode-toggle` not `./controls`); 15 register kills of which two are this seat's own against both readers — the stale-artifact kill (K-12: HEAD greys BOTH themes) being the single most disposition-changing fact in the folder; 14 identity folds honoured; 9 superlatives ratified. Routings are M-25 agglomeration input for the forming X·F specs; nothing here stamps a wave.

— end of apotheosis. Original challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.
