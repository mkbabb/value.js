# App — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed, not inherited — recorded against L-11.
**Date**: 2026-07-27 · **Corpus**: 39 accusations (CHALLENGE-L 15 · CHALLENGE-D 15 · CHALLENGE-C 9), the three App-subject seats in `registry/harvest/area-core.json`.
**Workers adjudicated**: worker-F (Fable 5, `app-shell-adjudication-worker-f`) · worker-O (Opus 5 [1m], `worker-o-app-shell-adjudication`).
**Method (L-14)**: both workers were attacked before either was adopted. Every disagreement was re-derived against the tree by this seat; every agreed BLOCKER was independently re-measured. Arbiter's own instruments this session: full source reads of App.vue / index.html / PaneSlot.vue / usePaneRouter.ts / useViewManager.ts / router/index.ts / useColorUrl.ts / useOverture.ts / hydrate.ts / ground.ts / usePalettePorts.ts / usePaletteWiring.ts / ActionBarLayer.vue / shell.css / foundation.css / VISUAL-CONSTITUTION.md (all contested clauses verbatim); one run of the committed probe `audit/probes/app-wave/app-shell-truth-probe.mjs` (signature read before output believed); one real `npm run gh-pages` build with the bundle assertion.
**Environment disclosure (L-12)**: dev server :9000 UP and API-LESS — no data-backed surface was judged; probe cell = playwright-chromium only (WebKit/safari-app are separate cells, I-20 — nothing here transfers); build facts from the BUILT artifact, never the dev server.

## Scoreboard

**26 CONFIRMED · 11 RESCOPED · 2 REFUTED (L-9, D-15) · 0 UNVERIFIABLE-HERE at row level · 0 GLASS-OWNED.**
Blocker-weight survivors: **L-1** (inline entry → empty prod bundle, MT-F012), **C-1** (route-untrue boot → terminal overture stall on 10/14 routes), **C-2/D-1** (entire mobile action surface inert), **D-4 H1 arm** (zero H1 repo-wide, canon-hard).

Arbiter triple-measurement record (my run, byte-identical to worker-F's committed BORN-RED and worker-O's independent oracles — three executions, deterministic):
- Cold `/#/generate` 1440×900 → marks `[b0,b1,b2]`, blob absent; hash-nav home after 8s → STILL `[b0,b1,b2]`, terminal. Control `/#/` → `[b0,b1,b3,b2,b4]`, blob present.
- 390×844 `/#/generate` dock Regenerate → `dockChanged:false` (button found, dispatched); in-pane control `paneChanged:true`; desktop control both true.
- Cold bare load settles at `?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` — both route classes.
- `h1: 0` live everywhere; `gridTemplateColumns: "512px 512px"` (exact 50.0000%); `docScrollable:false`; `mountHostIsBody:true`.
- Build (this seat, this session): `dist/gh-pages/assets` = `{"js":["index-Dezn_h7o.js","quantize-worker-xMwe415C.js"],"sizes":[698,11774],"tot":12472,"hasApp":false}` — the same 698-byte application-free entry, the same content hash `Dezn_h7o`.

## Ruled disagreements (the tri-fold's actual work)

| id | worker-F | worker-O | RULING | Deciding evidence |
|---|---|---|---|---|
| **L-9** | CONFIRMED | REFUTED | **REFUTED — O wins** | The cited edict (`feedback_preserve_animations`) prohibits REMOVING animations, not colocating them; no canon clause mandates `demo/styles/` for keyframes. App.vue:207-210 + :413-417 document the overture.css colocation deliberately, with cascade rationale ("moves, not removals" — PP-8). A documented, rationale-bearing choice is not a defect. Struck; the wave does NOT move the file — its import merely rides to main.ts preserving order. |
| **D-15** | UNVERIFIABLE-HERE (banked) | REFUTED | **REFUTED — O wins** | foundation.css:678-696 read by this seat: the `@media (forced-colors: active)` tier-1 roster explicitly names `.atmosphere-canvas` (:683) AND `[data-glass-field-canvas]` (:684) — the exact stamps App.vue:11/:16 applies. The challenger's premise ("the atmosphere canvas has no forced-colors arm") is false in the bytes. Worker-F under-read the block (stopped at the first three selectors). The two-tier policy is deliberate and commented (U-F57/BR-5) and right for a colour tool. No bank: a struck row earns no re-trigger, and a forced-colors gate authored green is vacuous under L-3. |
| **D-6** | RESCOPED (split) | REFUTED | **RESCOPED — F's split, O's strike** | The NONDETERMINISM clause is struck: O's 6/6 cold-load determinism plus this seat's three deterministic runs show the challenger read C-1's deterministic route boundary across mixed route classes as chance. But refuting the whole row would strike two true, C-1-independent source facts this seat re-verified: `grep -rln contextlost demo/ src/` → ONLY `useAtmosphere.ts` (the blob has NO loss contract — mid-session context loss has no handler regardless of `isReady`), and b4 has only a success edge (useOverture:154 watches `b3Complete && b2`, no failure arm). Split: b4's terminal `unavailable` arm folds into MT-APP-1's useOverture item; the blob `webglcontextlost/restored` contract goes to the blob/picker component wave. |
| **D-4** | CONFIRMED (incl. skip link) | RESCOPED (skip link refuted) | **RESCOPED — O wins** | This seat grepped the constitution for `skip.?link\|skip to\|bypass`: ZERO hits. The only "skip" string in demo/ is a code comment (useColorUrl.ts:60). No skip-link obligation exists in canon; `<nav>`+`<main>` landmarks satisfy bypass-blocks. The H1 arm is canon-hard (§4.1: "Each route has one H1 and exactly one stable main landmark, owned by the shell"; §5.1 targets the destination H1 in 4/7 origin rows and requires "the single **visible** H1 exist before app-ready") and BUILDS here. Note against F's cure: a *visually-hidden* H1 would violate §5.1's "visible H1" — the wave lands a **visible** shell-owned H1. |
| **C-4** | CONFIRMED | RESCOPED | **RESCOPED — O wins** | The "no h2" clause is false: `grep -c '<h2'` → AboutPane.vue 1 + ColorNutritionLabel.vue 5 (O measured 10 rendered on `/#/`, About being picker's right pane). Struck so no close can cite it. What survives folds into D-4: h1=0, first heading an h3 carrying a numeric readout. |
| **C-3** | CONFIRMED | RESCOPED | **CONFIRMED — F's label** | Both arms stand by construction, re-derived by this seat: PaneSlot.vue:124 `:ref="onMount ? (el: any) => onMount!(el) : undefined"` is an inline arrow rebuilt every render (Vue re-invokes setRef on every parent patch — App.vue re-renders per colour tick), and PaneSlot's documented one-rAF commit lag (:95) against App.vue:324's call-time `currentConfig.value.left` sampling gives the mis-attribution window. O's own note confirms both arms; his label was evidentiary bookkeeping (frame not captured). Reproduction: read the two lines; or count `onMount` invocations across a colour tick. Severity per F: identity-model defect; user-visible window ~1 frame. Moot under `bindPane` — stable identity, slot-keyed. |
| **C-5** | CONFIRMED (narrowed) | RESCOPED | **RESCOPED — O's label, both workers' substance** | App.vue:164 value-imports ColorPicker, used only in a type position (:223) — a real verbatimModuleSyntax hygiene miss. The stated bundle harm is FALSE: usePaneRouter.ts:21/:83 imports and returns ColorPicker as a value (the deliberate eager default), so the fix's bundle delta is zero. Survives as a one-line `import type` while the wave rewrites the import block. |
| **C-7** | CONFIRMED (mechanism part-refuted) | RESCOPED | **CONFIRMED — F's label + F's mechanism discipline + O's charge-narrowing** | Phenomenon triple-measured (bare cold load gains `?space=&color=`). Mechanism pinned by this seat from source: useColorUrl.ts:79-82's Model→URL watch has NO `immediate` flag — F is right that the pre-registration pipeline clamp (App.vue:245, before useColorUrl at :377) cannot trip it; the writer is a post-setup model write (O's "first model tick") that the wave must pin before curing. O's charge-narrowing adopted: NOT booked against §6.2 — the `value-ground/3` seed-latch contract is unimplemented (0 hits for `data-ground-schema\|SeedToken\|--ground-seed` in index.html); the charge is the shell violating its own one-commit law (App.vue:225-230/:369-376). |
| **D-5** | RESCOPED | CONFIRMED | **CONFIRMED — O's label, F's wave shape** | §6:145 verified verbatim by this seat: "terminates within five seconds or exposes one persistent keyboard-operable still/pause control whose state is announced and remembered." Both arms fail: renderers are continuous by design; the pause-affordance grep is empty repo-wide. O's rAF numbers stand with his own honesty caveat (global counter; the gate must scope to the renderer). Disposition: named sibling MT-APP-MOTION-1 (feature-shaped, not shell-boot-shaped). |
| **D-7** | RESCOPED | CONFIRMED | **CONFIRMED — O's label, shared routing** | Statically airtight (re-derived): `(min-width:1024px) and (min-aspect-ratio:1.1)` at App.vue:310-312 consumed as a hard v-if/v-else fork (:77/:94) — 200% zoom at 1440×900 → 720×450 CSS viewport → mobile IA + destruction of all three KeepAlive caches and both WebGL contexts; the X6 comment (:62-70) shows the single-context trade was deliberate, the total state loss at a zoom step was not. O crossed the breakpoint live. Cure is member-scale: the zoom-IA arm travels with D-2's owner; the state-loss evidence rides the registration seam's record. |
| **D-8** | RESCOPED | CONFIRMED | **CONFIRMED — O's label, F's inseparability rider** | shell.css:19-28 `height:100dvh; overflow:hidden` — viewport-frame at every size; canon §3 law 6 requires mobile document-scroll in so many words. F's two corrections both verified by this seat: the challenger's foundation.css:481 cite is the 21:9 ULTRAWIDE arm (not mobile), and document-scroll cannot land by deleting overflow alone (every pane's inner scroll-well reflows with it) — so the row travels to the member program's mobile recomposition carrying O's route-named born-RED gate (at 390×844 the document scrolls; RED today). |
| **D-9** | CONFIRMED | RESCOPED | **RESCOPED — O wins on the cure arms** | Diagnosis confirmed (= L-2/C-8 family; `mountHostIsBody:true` triple-measured). But the challenger's cure arms are struck: App.vue's template is a TWO-element-root fragment (comments are not element roots), `data-paper-field` must stay on body to dominate the two live `<Teleport to="body">` sites (index.html:216-224 says exactly this), and `class="relative"` stays — this seat found PointerDebugOverlay is `position:fixed` (body-relative-independent), so the load-bearing claim is unproven either way, and deleting a body stamp is a behavior risk no gate requires. Cure: `<div id="app">` INSIDE the existing body; both stamps untouched; the D-E portal-integrity delta guards it. |
| **D-10** | CONFIRMED | RESCOPED | **RESCOPED — O's facts, F's cheap fold** | Verified: index.html:11 pins `dir="ltr"` with the RTL follow-up booked — latent, not live; the grid itself flips CORRECTLY under RTL (canon §6.1 satisfied for layout); the defect is physical NAMES (`pane-wrapper--left/--right`, shell.css:94-96's directional z-index rationale) and physical DELAYS (`--overture-left-delay` 40ms per slot inline). F's cheap half folds into MT-APP-1 (role-named classes + role-keyed stagger survive even when the slots die under the member program); the choreography residue travels with D-2. |
| **D-14** | RESCOPED | CONFIRMED | **CONFIRMED — O's label, F's sequencing bind** | §7:224 verified verbatim: recovery "is never rendered as an empty library, silent reset, **migration choice**, **overlay**, or companion pane" — App.vue:152-157's unconditional shell-root dialog is two of the five at once. Canon names the owners (W15 semantics / W22 composition). BINDING SEQUENCE per F: the mount is removed only WITH the W15/W22 recovery article — removing it first deletes the only affordance. O's gate (`MigratePalettesDialog` 0 mount sites) fires at that wave's close, not before. |
| **L-14** | CONFIRMED (fold param drop) | RESCOPED | **RESCOPED — O wins on subject** | Facts exact (this seat): ActionBarLayer.vue's local `useLayerTransition` with `void opts.containerEl; // signature parity with the retired producer composable`; glass 7.0.0 dist has NO `useLayerTransition` (0 d.ts hits) and DOES ship `DockCrossfade.vue.d.ts`. A defect filed against the wrong component is one the right component's wave never sees: the param-drop belongs to MT-DOCK-LAYERS-1 (named, born-RED gate below); MT-APP-1 carries only the runnable glass-successor bank. No local patch is proposed for the glass boundary (the shim is value-local; its relay is the standing BH fond). |
| **L-15** | CONFIRMED → own micro-wave | CONFIRMED-INFO → retire | **CONFIRMED (INFO) — F's disposition** | Fact agreed by all three seats: `demo/color-picker/` holds the shell, `demo/picker/` holds the picker; the name invited shell concerns into the wrong roof (visible in the tree). F's micro-wave MT-APP-RENAME-1 (gate `test -d demo/app` — RED today) quarantines the churn and satisfies no-deferral; O's retire position is preserved in DISSENT. |

Arbiter corrections applicable to BOTH workers:
- **C-1 mechanism nuance**: b3 has a second arming path — the PRM instant path (useOverture onMounted, `prm` only). "B3 has no state-checked floor" is scoped to the non-PRM majority path. The cure's state-checked arm must not double-fire against the PRM path.
- **D-11 scoping**: `role="status"` regions DO exist in demo/ (GradientCodeEditor.vue:111, EmptyState.vue:28). The true claim is shell-scoped: no polite live region exists in demo/shell or demo/color-picker, and none announces navigation. Worker-F's phrasing was properly scoped; worker-O's addendum phrase "zero polite live regions exist" over-claims and is corrected here.
- **Worker-F integrity notes**: (1) the BORN-RED witness files EXIST on disk (`audit/probes/app-wave/`, mtime Jul 27 10:41) but sit in the UNTRACKED megatranche tree — "committed" was overstated; the wave's π obligation includes actually committing them. (2) F under-read foundation.css:678's selector roster (D-15). (3) F's proposed visually-hidden H1 contradicts §5.1's "visible H1" — corrected in the wave.
- **Worker-O integrity notes**: (1) the unscoped "zero polite live regions" claim, above. (2) O's harvest-enumeration claim ("exactly those three seats") is axis-scoped; a string-level grep matches 6 harvest files — accepted as his enumeration method, not re-derived here.

## Final verdicts — all 39 rows

Reproductions: `PROBE` = `node docs/tranches/V/megatranche/audit/probes/app-wave/app-shell-truth-probe.mjs` against the dev server (run three times total across seats, deterministic; output committed beside it). `BUILD` = `rm -rf dist/gh-pages && npm run gh-pages` + the G1 assertion below.

| id | VERDICT | Deciding evidence (fresh-session reproduction in parentheses) |
|---|---|---|
| C-1 | **CONFIRMED · BLOCKER** | Route-untrue boot: index.html:205-213 mounts without `router.isReady()`; useViewManager:44-46 `isViewId(name) ? name : "picker"` seeds the wrong pane; PaneSlot's one-rAF commit (:95) swaps the child, cancelling `appear`; `@after-appear` (:118) is the SOLE non-PRM caller of `noteLeftPlateSettled` (App.vue:90/:109); b3 never fires, terminal for the session. 10/14 routes affected. (PROBE: deeplinkGenerate.marks=[b0,b1,b2], blob:false, still true after hash-nav home.) |
| C-2 | **CONFIRMED · BLOCKER** | One defect with D-1, both ids preserved. App.vue:83-91 mobile PaneSlot passes no `:on-mount` vs :105/:131 desktop; :323-332 are the sole ref writers; Dock.vue:41 gates on metadata and draws the layer anyway. (PROBE: mobileRegen390 dockChanged:false / desktop control true.) |
| C-3 | **CONFIRMED** | Inline-arrow `:ref` (PaneSlot:124) rebuilt per render + call-time `currentConfig` sampling (App.vue:324) vs the one-rAF trailing child. Identity-model defect; ~1-frame visible window; cured structurally by `bindPane`. (Read the two lines; count onMount calls across a colour tick.) |
| C-4 | **RESCOPED** | Folds into D-4. The "no h2" clause is STRUCK — 6 `<h2>` sources in the About tree, ~10 rendered on `/#/`. Keyboard friction (16 dock buttons first) is real but not a conformance failure given landmarks. |
| C-5 | **RESCOPED** | Hygiene only: App.vue:164 value import, type-only use at :223 → `import type`. Bundle harm FALSE (picker eager via usePaneRouter:21/:83 by design). One line, rides the wave's import-block rewrite. |
| C-6 | **CONFIRMED** | App.vue:168-175 books a residual on "the root package.json marks ./demo/** side-effecting"; package.json:20 is `"sideEffects": false`; no demo/package.json exists. The booking is unfounded — comment AND booking deleted together (a corrected comment is still an ungated claim). (grep both files.) |
| C-7 | **CONFIRMED** | Boot rewrites the address bar with unrequested state, violating the shell's own one-commit law. Mechanism: useColorUrl:79-82 Model→URL watch (no `immediate`) tripped by a post-setup model write — the writer is UNPINNED and the wave pins it before curing (the challenger's setup-clamp trigger is refuted: the clamp at App.vue:245 precedes watch registration at :377). NOT booked against §6.2 (contract unimplemented, 0 tokens in index.html). (PROBE: href gains `?space=lab&color=…` on bare cold load.) |
| C-8 | **CONFIRMED** | `<body class="relative" id="app" data-paper-field>` (index.html:225) is mount container AND portal host; app.mount clears body innerHTML; a static in-body boot-error region is structurally impossible. The entry cure alone does NOT fix this — the `<div id="app">` move is coupled to the same index.html edit. (PROBE: mountHostIsBody:true.) |
| C-9 | **CONFIRMED** | Zero unit tests mount any component (`grep -rln 'mount(' test/` → 0); the only overture oracles (o4:47, o11) `goto("/")` exclusively — the one route class where C-1 hides. The wave's gates ARE the missing oracles; the e2e O-4 gains a non-picker-route arm. (Run the greps.) |
| L-1 | **CONFIRMED · BLOCKER** | The spine. No main.ts exists; the entry is 8 inline HTML lines; App.vue carries five undeclared boot contracts (router plugin, fouc-guard lockstep, body mount, CSS document root :199-210, two app-level installs :214/:219). Build half re-run BY THIS SEAT: entry `index-Dezn_h7o.js` 698 B, `hasApp:false` — MT-F012 reproduces byte-for-byte. (BUILD.) |
| L-2 | **CONFIRMED** | Body = mount container + portal host + field-contract holder. Cure constraint binding (see D-9): div#app inside body; both stamps stay. |
| L-3 | **CONFIRMED** | vite.config.ts:15 imports `injectGroundTokens` from `demo/color-picker/composables/boot/ground`; ground.ts carries browser contract (:51-:103) AND node-only HTML transform (:131) in one module. Cure: split by lifetime → `plugins/vite-ground-tokens.ts` (the plugins/ home has two siblings). FOLDED into MT-APP-1 (the boot folder is already open). |
| L-4 | **CONFIRMED** | 14 bare name literals in router/index.ts:22-35; viewSchema.ts:7-10's header CLAIMS the router copy was retired — it was not; `meta:{admin:true}` ×5 has ZERO consumers (grep 0 outside the router); useViewManager:44-46 masks typos. Cured in MT-APP-ROUTER-1. |
| L-5 | **CONFIRMED** | `Stub = {render:()=>null}` ×14; no `<router-view>` in the tree (sole grep hit is the comment); componentFor's doc-comment says "null for an unknown name" while the code ends `return ColorPicker` (:94) — a masking fallback under a lying comment, discarding the closed LeftPane/RightPane unions. Cured in MT-APP-ROUTER-1. |
| L-6 | **RESCOPED** | Direction+membership confirmed (App.vue:188 → usePaletteWiring → providePalettePorts → 4 admin composables + useAdminAuth eager on an anonymous first paint while AdminPane is async). Counts rescoped to O's re-derivable walker (71 eager source files; the challenger's numbers not re-derivable, and barrel `export…from` edges make all counts under-counts). Cure: MT-APP-PORTS-1. |
| L-7 | **RESCOPED** | Fact exact (15 composables imported+constructed in usePalettePorts.ts:4-18, verified); SUBJECT is usePalettePorts.ts, not App — App owns the eager call site (:351). Same sibling wave. |
| L-8 | **CONFIRMED** | Three `ref<any>` + nine `?.method?.()` dispatches (usePaneRouter:196-222): a workbench rename is a silently dead dock button; `vue-tsc` exits 0 over a measured-inert action bar. Typed-record half folds HERE; the workbench-provided-action-bar inversion rides the member program. |
| L-9 | **REFUTED — STRUCK** | See ruled disagreements. The edict forbids removal, not colocation; the colocation is documented with rationale (App.vue:207-210/:413-417). No successor close may cite this row. |
| L-10 | **CONFIRMED** | `picker-shell` applied at exactly one site (usePaneRouter:134), defined nowhere (demo/, src/, glass dist). O's nuance adopted: it IS the audit probes' oracle selector — cure is rename to `data-testid`, not a styling rule. |
| L-11 | **CONFIRMED** | App.vue:190-192: three consecutive glass-ui imports at three depths; dist `composables/dom/index.d.ts:6` exports useClipboard — `/dom` is the published home. One-line fold. |
| L-12 | **CONFIRMED** | Two URL-read owners; useColorUrl's boot apply always false (async initial navigation → empty `route.query` at setup — C-1's stall is the empirical proof); App.vue:378's left conjunct constant-false; hydrate.ts:50's rationale ("runs before router installation") is FALSE — install precedes mount; the true fact is the async initial navigation. Fold: delete the dead conjunct + boot apply; correct the rationale. |
| L-13 | **CONFIRMED** | `grep -c 'watch(' App.vue` = 0, yet BlobPane.vue:9, aurora-atoms.ts:6/:16/:18, AuroraPane.vue:6 name App.vue as watcher-owner (real owner: useAtmosphereBoot, whose header records the move); App.vue's comments order imports "AFTER style.css" — no style.css exists in demo/styles/; plus the C-6 false manifest prose. Cure altitude right (F): make the atmosphere provides declared surface so prose pointers become deletable. |
| L-14 | **RESCOPED** | See ruled disagreements. Subject = ActionBarLayer → MT-DOCK-LAYERS-1; MT-APP-1 carries the runnable bank only. |
| L-15 | **CONFIRMED (INFO)** | Naming inversion real and consequence-bearing (shell concerns accreted under `demo/color-picker/`). → MT-APP-RENAME-1 micro-wave; O's retire dissent preserved. |
| D-1 | **CONFIRMED · BLOCKER** | = C-2, one carry, both ids. Aggravations verified by this seat: usePaletteWiring's `whenColorPickerReady` 40×50ms poll then give-up (mobile edit journey dead), emitApply degradation, Dock.vue:72's mobile-edit layer routing Save/Cancel to `colorPickerRef?.commitEdit()` — null at exactly that viewport. |
| D-2 | **RESCOPED** | All six canon citations verified verbatim by this seat (§4.2:89 retirement · §3 law 6, O's mobile-scope correction stands · :218 admin full-width · §3.1 "About is a quiet trailing destination" + eleven-member inventory incl. `/easing`,`/about` absent from the tree · W18 ratification bind). The accusation is true; the SHAPE is refused — an eleven-member transposition is an arc (L-1; 38% landing rate). → the constitution's own owner: §3.1 W18 + the member waves. Axis bank below. |
| D-3 | **RESCOPED** | `512px 512px` = exact 50.0000% triple-measured; shell.css:133-138 ships it by written intent ("the kept 1fr↔1fr invariant"); §3 law 1 permits only golden/preview-dominant. Folds into D-2's owner (ratio is a property of the route composition). The empty-library sub-arm is UNVERIFIABLE-HERE (API-less server — environment fact, not confirmation). |
| D-4 | **RESCOPED** | H1 arm CONFIRMED and BUILT here (h1=0 repo-wide + live; §4.1/§5.1 canon-hard; must be VISIBLE per §5.1). Skip-link clause STRUCK (zero canon obligation; landmarks satisfy bypass). |
| D-5 | **CONFIRMED** | §6:145 disjunction verified verbatim; both arms fail (continuous renderers; pause-affordance grep 0 repo-wide). → MT-APP-MOTION-1 with the renderer-SCOPED gate (O's own caveat: his rAF counter was global). |
| D-6 | **RESCOPED** | Nondeterminism clause STRUCK (route-class confound; 6/6 + 3/3 deterministic). Survivors: b4 terminal `unavailable` arm → MT-APP-1's useOverture item; blob contextlost contract (grep: only useAtmosphere handles loss) → blob/picker wave. |
| D-7 | **CONFIRMED** | Zoom crosses the compound breakpoint → different IA + all three KeepAlive caches and both WebGL contexts destroyed (hard v-if fork; X6 trade documented, the state loss not). Zoom-IA arm travels with D-2's owner; the fork dies with the axis. |
| D-8 | **CONFIRMED** | shell.css:19-28 viewport-frame at every size vs §3 law 6's mobile document-scroll law. Challenger's foundation.css:481 cite corrected (21:9 ultrawide arm — F right); the magic-depth number corrected to route-dependent (O right). → member program's mobile recomposition, carrying O's gate: at 390×844 the document scrolls (RED today: scrollHeight 844 = clientHeight 844). |
| D-9 | **RESCOPED** | Diagnosis = L-2/C-8 (confirmed). Corrections binding: two-element-root fragment (not three); BOTH body stamps stay (`data-paper-field` must dominate the body-portalled dialogs — its move would re-arm the producer's orphan-card field-floor fallback; `class="relative"`'s load-bearing status unproven either way and no gate requires its deletion). Cure: `<div id="app">` inside body; D-E portal-integrity delta. |
| D-10 | **RESCOPED** | Latent (dir="ltr" pinned, index.html:11, RTL booked); grid flips correctly; defect = physical class names + physical stagger delays + shell.css:94-96's directional z-index rationale. Cheap half (role-named classes, role-keyed stagger) FOLDS here; choreography residue → D-2's owner. |
| D-11 | **CONFIRMED** | router/index.ts:36 catch-all silent redirect; `/#/does-not-exist` renders byte-identical to `/#/`, same title; NO shell-owned polite region (scoped claim — role=status exists in workbenches, none in demo/shell / demo/color-picker, none announcing navigation); §5.1 row 4 requires redirect reason in one polite status + destination title/H1. FOLDS here beside the H1 (same route-settlement commit installDocumentTitle hooks). |
| D-12 | **CONFIRMED** | App.vue:41-42: two sequenced statements in a template expression, foreign ref mutation, magic index `1` documented three files away. mobilePaneIndex is now a tagged computed (well-defined write — F's nuance) but the template-level reach stands. Diagnostic value (O): the second statement masks the first's silent mobile failure. Cure: named `returnToInspector()` intent. |
| D-13 | **CONFIRMED** | Three `ref<any>` + `onMount?: (instance:any)` optional — while colorPickerRef (:223) is properly InstanceType-typed, proving the pattern was known and skipped. `vue-tsc` green over a measured-inert action bar = the enabling condition for D-1's silence. Structure over gates: typed generic onMount + InstanceType record. |
| D-14 | **CONFIRMED** | §7:224 verbatim: the unconditional shell-root MigratePalettesDialog (App.vue:152-157) is a "migration choice" AND an "overlay" — two of the five forbidden presentations. → canon-named W15/W22, SEQUENCED: the mount is removed only WITH the recovery article (F's bind); O's 0-mount-sites gate fires at that close. |
| D-15 | **REFUTED — STRUCK** | See ruled disagreements. foundation.css:683-684 names the atmosphere stamps in the forced-colors tier-1 roster; premise false; no gate, no bank. |

## The re-authored wave — MT-APP-1 §app-shell (BUILD)

**Title**: The shell declares what it owns — composition root, route-true boot, slot-truth registration, and the shell's own voice.
**Born-RED**: yes — all eight gates measured RED this session by this seat (third independent measurement).
**Alias note**: supersedes worker-F's "MT-APP-1" and worker-O's "MT-W-APP-1" specs; this is the single adjudicated spec.

### Scope (writes, exact end state)

1. **NEW `demo/color-picker/main.ts`** — the composition root: the four CSS imports lifted from App.vue:199-210 IN ORDER (utils → foundation → focus-ring → overture — cascade preserved; overture.css does NOT move, L-9 struck), `createApp(App)`, `app.use(router)`, `await router.isReady()`, the dark-mode init and the API-client provide installed at app level, `app.mount('#app')`.
2. **`demo/color-picker/index.html`** — inline module (:205-213) → `<script type="module" src="./main.ts"></script>`; `<body class="relative" id="app" data-paper-field>` → `<body class="relative" data-paper-field><div id="app"></div></body>`. BOTH stamps stay on body (D-9 ruling; the two live `Teleport to="body"` sites are the div's siblings).
3. **`demo/color-picker/App.vue`** — drops: `useGlobalDark()` (:214), `provideApiClient()` (:219), all four CSS imports, `ref<any>` ×3 (:317-319), both mount callbacks (:323-332), the ColorPicker value import (:164 → `import type`), the false sideEffects prose+booking (:168-175), the two nonexistent-style.css rationales, the inline compound handlers (:41-42 → `viewManager.returnToInspector()`). Gains: `bindPane(...)` on ALL THREE PaneSlot call sites; ONE **visible** `<h1>` inside `<main>` bound to `VIEW_MAP[currentView].label` with `tabindex="-1"` (§5.1 focus target); one shell-owned polite `role="status"` region (announces redirects; written by the same route-settlement commit installDocumentTitle hooks); pane-wrapper role rename (`--left/--right` → role-named) + role-keyed stagger (D-10's cheap half).
4. **`demo/shell/PaneSlot.vue`** — `onMount` becomes REQUIRED and generic over the mounted component's instance type, invoked with `(instance, liveKey)` — registration rides the slot's OWN identity; the inline arrow at :124 → a stable bound capture (kills C-3's re-fire and mis-attribution).
5. **`demo/shell/usePaneRouter.ts`** — gains `bindPane(slot: 'left'|'right'|'mobile')`, owns the instance refs it dispatches onto; `PaneActionRefs` re-typed `Ref<InstanceType<typeof GeneratePane>|null>` etc.; `picker-shell` class token → `data-testid` (L-10). `componentFor`'s lattice is NOT unified here (→ MT-APP-ROUTER-1); the isReady await removes its boot-time masking.
6. **`demo/shell/useViewManager.ts`** — named `returnToInspector()` intent (D-12).
7. **`demo/color-picker/composables/boot/useOverture.ts`** — b3 gains a mount+settle STATE-CHECKED arm beside the cancelable `@after-appear` event (careful of the existing PRM instant path — arbiter nuance); b4 gains the terminal `unavailable` arm (D-6 fold half).
8. **`demo/color-session/useColorUrl.ts`** — the always-false boot apply + `appliedFromUrl` deleted (live sync only); the post-setup boot writer PINNED, then the boot URL rewrite made a no-op (C-7). **`boot/hydrate.ts`** — rationale corrected to the async-initial-navigation fact (L-12).
9. **`boot/ground.ts` split by lifetime** — browser contract stays; `injectGroundTokens` → `plugins/vite-ground-tokens.ts`; vite.config.ts:15 rewired (L-3).
10. **Glass import fold** — `useClipboard` via `@mkbabb/glass-ui/dom` (L-11).
11. **NEW gate scripts** under `docs/tranches/V/megatranche/workflows/gates/` (these ARE C-9's missing oracles) + the e2e O-4 spec gains a non-picker-route arm.

READ-ONLY, explicitly NOT in scope: `overture.css` location (L-9 struck), `ActionBarLayer.vue` (L-14 → sibling), viewSchema's left/right axis + shell.css's equal columns (D-2/D-3 → member program), `MigratePalettesDialog` mount (D-14 → W15/W22 sequenced), foundation.css's forced-colors block (D-15 struck), anything glass-owned (spectrum seam prefix trap: glass-owned, cured in 8.0.0 — NOT re-booked, no local patch; INBOX I-20 discipline for cells).

### Structure over gates (L-8)

Unrepresentable after this wave: (a) boot logic in HTML — the entry is a compiler-checked module, the router dependency an explicit awaited line, so the C-1 class cannot recur; (b) an unregistered slot — `onMount` required + `bindPane` the only capture, keyed by the slot's own liveKey, so no breakpoint branch can omit registration; (c) any-typed/wrong-instance dispatch — InstanceType unions + slot-keyed record make the next workbench rename a compile error (kills C-2/C-3/D-13's silence class); (d) build↔app inversion — ground's node half lives in plugins/. Gates remain only where the property is behavioral.

### Gates (all RED today; command + the input that makes it RED)

- **G1 · build-contains-app** — `rm -rf dist/gh-pages && npm run gh-pages && node -e "const fs=require('fs'),p='dist/gh-pages/assets';const js=fs.readdirSync(p).filter(f=>f.endsWith('.js'));const tot=js.reduce((a,f)=>a+fs.statSync(p+'/'+f).size,0);const hasApp=js.some(f=>fs.readFileSync(p+'/'+f,'utf8').includes('pane-container'));if(!hasApp||tot<300000){console.error('RED',JSON.stringify({js,tot,hasApp}));process.exit(1)}console.log('GREEN')"` — RED input: the inline HTML entry. Arbiter-measured: `{"js":["index-Dezn_h7o.js","quantize-worker-xMwe415C.js"],"tot":12472,"hasApp":false}`. Built artifact only; the dev server is structurally blind to this class.
- **G2 · prod-mounts-nonempty** — serve `dist/gh-pages` + `prod-mount-probe` asserting main≥1 and text≥200 chars — RED input: a bundle that builds, serves, runs the inline classic script (b0 still marks!) and never mounts; blank AND console-silent.
- **G3 · deeplink-overture** — the committed probe, asserting `deeplinkGenerate.marks ⊇ {b3,b4}` AND blob present, across `/#/generate,/#/browse,/#/gradient,/#/extract,/#/atmosphere` — RED today: `[b0,b1,b2]`, blob absent, terminal (triple-measured).
- **G4 · dock-action-parity** — the probe's mobile/desktop Regenerate pair asserting PARITY — RED today: `{mobileChanged:false, desktopChanged:true}`; covers the nine handlers; palette-library arms excluded (API-less = environment, not defect).
- **G5 · typed-registration-bite** — `npx vue-tsc -p tsconfig.demo.json --noEmit` against a tree with ONE bindPane argument deleted must exit non-zero — RED reading today per L-3: the checker is GREEN over a measured-inert action bar (the gate does not bite; the wave's job is to make it bite).
- **G6 · route-H1** — iterate all fourteen route names from viewSchema: `h1===1 && main===1 && h1.text===VIEW_MAP[view].label`, H1 visible — RED today: h1=0 everywhere; first heading an h3 carrying `92.0%,88.8,20.0`.
- **G7 · boot-url-silence** — fresh context, bare `/#/` and `/#/generate`: href contains no `?space=` — RED today: settles at `?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` (triple-measured).
- **G8 · announced-redirect** — `/#/does-not-exist` → the shell status region carries the redirect reason, title/H1 distinct, `grep -rn 'role="status"' demo/shell/ demo/color-picker/` ≥1 — RED today: 0 shell hits; byte-identical render to `/#/`.

### π obligations

BEFORE: `audit/probes/app-wave/BORN-RED-2026-07-27.json` + probe source — **must be `git add`-ed with the wave's first commit** (they exist untracked today; a witness that is not committed does not exist — L-7). AFTER: the matching GREEN JSON + one desktop-1440 and one mobile-390 capture pair on `/#/generate` under `audit/probes/app-wave/pi/`, `git add -f` (the `.gitignore *.png` trap), plus the H1 accessibility-tree dump pair. Every capture accompanied by the machine-readable oracle output it was taken with.

### DELTA obligations

(1) deep-link marks `[b0,b1,b2]` → `[b0,b1,b2,b3,b4]` + blob present, b3 timestamp recorded; (2) mobileRegen `dockChanged` false → true with the desktop control unchanged (parity proves desktop not broken); (3) h1 0 → 1 on all fourteen routes with accessible names against `VIEW_MAP[view].label`; (4) cold-load href bare → stays bare; (5) bundle 698 B application-free entry → entry referencing the app graph, tot >300 kB, hasApp:true (both assertion runs pasted); (6) **D-E portal-integrity guard**: body-direct-children positioning census + `[data-paper-field]` ancestry of a portalled dialog, identical before/after (proves the mount-target move re-armed nothing).

### Carries — all 39 dispositioned, original ids preserved (L-5), "next tranche decides" appears nowhere

- **BUILD here (MT-APP-1)**: L-1 (spine), L-2, L-3, C-1, C-2/D-1 (one carry, two ids), C-3, C-5, C-6, C-7, C-8, C-9 (gates=oracles), L-8 (typed-record half), L-10, L-11, L-12, L-13, D-4 (H1 arm), D-6 (b4 unavailable arm), D-9 (div#app form), D-10 (role-rename half), D-11, D-12, D-13.
- **Named siblings, each born-RED**:
  - **MT-APP-ROUTER-1** ← L-4, L-5. Gate: a route `name` absent from `ViewId` fails `vue-tsc`, AND `! grep -q 'return ColorPicker;$' demo/shell/usePaneRouter.ts`, AND meta.admin deleted — RED today (RouteRecordRaw accepts any string; the masking tail exists; 5 dead meta rows).
  - **MT-APP-PORTS-1** ← L-6, L-7. Gate: the entry's eager closure contains zero `useAdmin*` modules — RED today (4 admin composables + useAdminAuth measured eager by O's committed walker).
  - **MT-APP-MOTION-1** ← D-5. Gate: renderer-scoped — on an idle `/#/`, the metaball/aurora renderers schedule 0 frames between t=20s and t=24s unless a named persistent keyboard-operable still control exists with announced+persisted state — RED today (≈190 frames, 0 controls).
  - **MT-DOCK-LAYERS-1** ← L-14 (the param drop + shim review). Gate: `grep -c 'void opts\.' demo/shell/dock/layers/ActionBarLayer.vue` == 0 — RED today (line 68).
  - **MT-APP-RENAME-1** ← L-15. Gate: `test -d demo/app` — RED today. (O's retire dissent preserved below.)
- **The constitution's own member-composition program (§3.1 W18 + member waves)** ← D-2, D-3, D-7 (zoom-IA arm), D-8 (mobile document-scroll; O's gate travels: at 390×844 `document.documentElement.scrollHeight > clientHeight` — RED today), D-10 (choreography residue). Ids travel un-renamed.
- **Canon-named W15/W22** ← D-14, SEQUENCED (recovery article lands WITH the mount removal, never before).
- **Blob/picker component wave** ← D-6's blob `webglcontextlost/restored` contract half.
- **BANKS (runnable, swept at wave-open)**:
  - Glass successor (L-14): `grep -rn 'useLayerTransition\|ContentSwap\|content-swap' node_modules/@mkbabb/glass-ui/dist --include='*.d.ts'` — fires on a public composable; plus `node -e "const g=require('@mkbabb/glass-ui/package.json');process.exit(g.version.startsWith('8')?0:1)"` — Glass 8 re-triggers the shim deletion AND the spectrum-seam prefix-trap re-read (glass 07-25 outbound §3; glass-owned, no local patch ever).
  - D-2 axis bank: `node -e "const s=require('fs').readFileSync('demo/shell/viewSchema.ts','utf8');const n=(s.match(/right:\s*\"/g)||[]).length;process.exit(n===0?0:1)"` — GREEN only when the left/right axis is gone.
- **RETIRED/STRUCK**: L-9, D-15 (refuted, struck — no successor close may cite them); C-4's "no h2" clause; D-4's skip-link clause; D-6's nondeterminism clause; D-9's delete-the-stamps cure arms.

### Completable alone (L-1 test)

If MT-APP-1 were the only wave ever executed: production ships the application instead of a 698-byte blank (the next green master push currently deploys nothing to color.babb.dev); ten of fourteen routes stop terminally stalling their boot on cold loads and shared links; the entire mobile action surface (nine controls + the edit journey + palette-apply) becomes live; every route carries a visible H1 and an announced 404; a boot no longer rewrites the address bar; and roughly ten classes of silent drift become unrepresentable. Eight gates, all RED today with committed evidence; no dependency on glass-ui, the API, the member program, or any sibling. One architectural seam — the shell's truth.

## ADDENDUM — binding on any close that cites this adjudication

(1) The application entry is a module file, never inline HTML: it installs the router, **awaits `router.isReady()`**, and mounts into a dedicated container that is not `<body>`; no component renders a route-derived pane before the route is true, and no boot beat is armed solely by a single cancelable DOM event without a state-checked arm. (2) Pane-instance registration rides the slot's own identity (liveKey) on every call site at every viewport; a slot that can mount without registering, or a breakpoint branch that can omit registration, is malformed; all pane-instance plumbing is typed — `any` in the shell seam is a defect. (3) The shell owns exactly one `<main>`, one **visible** document H1 carrying route identity, and one polite `role="status"` region that announces redirects; a boot may not write the address bar with state no user action produced. (4) BOTH body stamps (`class="relative"`, `data-paper-field`) survive the mount-target move; the D-E portal-integrity delta is mandatory evidence on any change to index.html's body. (5) The route/view/component lattice gets exactly one home (MT-APP-ROUTER-1); unknown names become navigation events, never silent substitutions. (6) The dual-pane retirement, ratio law, document-scroll, zoom-IA and chassis adoption remain governed by the VISUAL-CONSTITUTION's member-composition program; D-2, D-3, D-7, D-8, D-14 travel there un-renamed and are NOT discharged by MT-APP-1. (7) STRUCK AND UNCITABLE: L-9; D-15; C-4's "no h2"; D-4's skip link; D-6's nondeterminism; D-9's stamp-deletion cure. (8) No local patch anywhere for a glass-owned surface; the spectrum-seam range is glass-owned (8.0.0), never re-booked here; WebKit/safari-app remain separate evidence cells (I-20) and nothing in this document claims them. (9) The BORN-RED witness must enter git with the wave's first commit — it exists untracked today and an uncommitted witness does not exist.

## DISSENT — preserved unresolved disagreement

1. **Worker-O's independence challenge, verbatim**: "My conclusions agree closely with the two juror seats already in area-core.json on C-1, D-1, D-2's citations and D-15. I read those seats only after completing my own derivation… The places where I differ from them are the places to examine: I refute D-6 outright (neither juror addresses it), I strike C-4's h2 clause and D-4's skip-link clause (neither juror separates them), and I decline to book C-7 against §6.2 because that entire contract is unimplemented. **If worker-F converges with me on those four, the arbiter should ask whether we were genuinely independent before adopting either of us.**" — ARBITER'S ANSWER: worker-F did NOT converge on those four (F confirmed L-9, kept D-6 as a split, marked D-15 unverifiable, kept the skip link, and part-refuted C-7's mechanism from the opposite side). The workers disagreed on precisely the rows O flagged as the independence test, and each disagreement decomposed under source re-derivation with a determinate winner. Independence is evidenced by the disagreement pattern itself; the agreements are on mechanical facts this seat re-measured (a build hash, a mark list, a click that changes nothing).
2. **L-15 disposition, unresolved in principle**: O — "Not a defect with a gate; a naming observation… recorded because MT-W-APP-1's `main.ts` is the first file in that directory whose name is honest about what it is." F — a rename micro-wave with `test -d demo/app` born-RED. The arbiter ruled for F's micro-wave (no-deferral discipline), but O's position — that booking churn-heavy renames as waves dilutes the formation — is preserved as a live objection for the formation owner.
3. **Fold-vs-sibling breadth**: F folded the route lattice, ground split, D-10 rename and D-11 status region into the App wave (14 files); O held the wave to 6 files and spawned siblings. The arbiter split the difference (lattice+ports+motion+dock-layers out; ground+rename+status in) — the resulting ~11-file wave is larger than O wanted and smaller than F wrote, and both original shapes are on the record above if the executing session finds this one wrong at contact.
4. **Worker-F's dissent on CHALLENGE-D's gestalt, preserved**: "the two defects that actually break the product for a real user are NOT consequences of the two-pane premise… a transposition-first program would leave both live for however long eleven route compositions take, against a repository whose measured delivery rate is 38% and whose standing 193-wave formation sits at 2%." (O wrote the same dissent independently, near-verbatim in substance — adopted as the ruling on D-2's shape.)
