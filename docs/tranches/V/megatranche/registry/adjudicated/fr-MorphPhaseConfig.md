claude-fable-5 (served model id)

# fr-MorphPhaseConfig — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/morph/MorphPhaseConfig.vue` (212 lines, read whole by this seat — working tree) with its closure `useMorphConfig.ts` (97, read whole), `lib/easings.ts:85-127`, `useFourierMorph.ts` (defaults + `createTweenAnimation` anchors), `FourierMorphDemo.vue` (three callsites + composable wiring), `ui/SliderControl.vue:33-110`.
**Substrate**: fourier HEAD `cd26c65` + dirty WT — `git status` shows `M` on **both** morph slider files (MorphPhaseConfig.vue + HarmonicLevelGrid.vue); the subject's WT diff is exactly one hunk, `variant="glass-scrubber"` → `variant="standard"` at :23 (seat-run `git show HEAD` + `git diff`). Installed glass-ui **4.0.0** (node-read), keyframes.js **4.3.0** (`dependencies` node-read: `{"@mkbabb/parse-that":"^0.9.0","@mkbabb/value.js":"^0.13.0"}`), value.js pin 0.13.0. glass-ui producer at **8.0.0**, HEAD `407de2d3`; tag reads `v7.0.0`/`v8.0.0` by this seat. value.js producer 4.0.0 (this repo; exports + `./easing` surface node-run by this seat).
**Corpus**: three axis challenges (`audit/fourier-components/MorphPhaseConfig/challenge-{D-design 24+5, L-library 16+4, C-consumption 20+7}.md`, all read whole by this seat) × two hostile re-reader returns (reader-1 design-and-usage · reader-2 library-and-code, both slug `MorphPhaseConfig`).
**Method (M-23/L-18)**: every reader-vs-reader disagreement and every load-bearing verdict re-derived against the tree with this seat's own instruments before ruling. Both readers were attacked: one reader-2 CONFIRMED is overturned into a fold by identity (the FMD-25 ruling), one fold neither reader made is minted by this seat (C-13), the headline new find's corrected cure is PRICED by this seat's own contrast run (a cell neither reader computed), and two reader evidence cells are corrected (forced-colors enumeration; the FMD substrate line). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node/git/grep only; zero browser — probe parsimony; product source READ-ONLY; this file the sole write)**: whole read of the subject + closure files above; `--slider-scrub` census `grep -rn` over `web/src` → **23 declarations / 7 files** (BasisSelector 4 · EditorControlsDock 4 · GlassTimeline 1 · SliderControl 5 · ConvergenceTimeline 1 · HarmonicLevelGrid 4 · MorphPhaseConfig 4) and **0** hits in the installed glass-ui dist; brace-split extraction of the pinned slider focus rules → **`.glass-slider:not([data-variant=spectrum]):focus-within .slider-track[data-v-534634a7]{box-shadow:var(--focus-ring-shadow)}` EXISTS at 4.0.0** (the D-3 decider) beside the spectrum-only `:focus-visible` thumb rule; the `standard` thumb rule verbatim (`width:0;…;opacity:0;background:0 0;border:none`); slider chunk reads — `C.variant ?? "standard"` fallback + `"aria-label": n.$attrs["aria-label"] ?? void 0` thumb forwarding; a11y-overrides.css — the forced-colors class allow-list verbatim (`.focus-ring:focus-visible … .input-pill:focus-visible { outline: 2px solid Highlight }`), the `@media (pointer: coarse)` three-selector floor verbatim, `forced-colors` string in **4** dist files but **3** @media blocks (ladder.css hit is comment-only — reader-1's "exactly THREE" survives at the block level) and **0** slider mentions; glass-ui git — `v7.0.0:src/components/slider/types.ts:5` `SliderVariant = "standard" | "spectrum"` + `marks` :23 + **no `valueText`**, `v7.0.0` Slider.vue `variant: "standard"` :19 + `:focus-within .slider-track` :416, `v7.0.0:…/styles.css` **does not exist** ("exists on disk, but not in 'v7.0.0'"), `v8.0.0:types.ts:10` `"scrubber" | "spectrum"`, `v8.0.0` Slider.vue default `variant: "scrubber"`, `v8.0.0:styles.css:145/:153` authored `:not([data-variant="spectrum"])`; reka bytes — SelectItemText registers `textContent: itemTextElement.value?.textContent ?? …` (a STRING) and SelectValue renders `createTextVNode(toDisplayString(slotText.value))` from `getOption(modelValue)?.textContent ?? ""` (the teleported-clone decider); glass select.js export list (SelectGroup + SelectLabel present); SelectTrigger compiled recipe (`… focus-ring flex w-full …` — `w-full` in base); `./labeled-field` + `./number-field` in the 4.0.0 export map (node-read); value.js 4.0.0 — 7 subpath exports, no `"."`, `dist/subpaths/easing.js` key list with `timingFunctions` **false**; web scripts (`dev/build/preview/test:e2e/test:e2e:ui` — no unit runner) + `e2e/visual-baseline.spec.ts:35` `{ slug: "morph", path: "/morph" }`; **the MPC-10 contrast run** (own oklch→srgb + WCAG implementation over the shipped literals): track `#f8f6f1` · range TODAY `#363331` = **11.51:1** · range CURED-as-banked (`--accent-red` at 0.30×0.88=0.264) `#f1bebb` = **1.51:1** vs track / 1.55:1 vs card · **range at FULL-strength `--track-color` under glass's own 88% = 4.00:1 PASS** — the corrected-cure pricing cell; registry identity sweeps — targeted reads of fr-FourierMorphDemo (FMD-3/-4/-8/-11/-12/-14/-15/-18/-19/-25/-26/-31/-32/-33/-34, m-5/K-4/K-7/K-8, superlatives), fr-BasisSelector (B-2/M-6/M-7/K-11), fr-EasingPicker (L/B-1+C/B-1, C/B-2, the 55-key/5-of-5 node re-run, producer doctrine), library-band RD-5 (**RULED RESTORE**, 0.192 twice-measured), fr-ConvergenceTimeline (23/7 recount, M-2, SR-1), fr-EasingCurvePreview (GEO-1/WRONGPAD/BARREL/FORK/K-GEOM), ConfigSliderPane (the `:focus-within` corroboration :33; the slider forced-colors gap → RELAY I-20 :60), fr-EqCoefficientsPanel FR-EQC-5, fr-ContourPreview row 23.

## Scoreboard

**29 rows ADJUDICATED-CONFIRMED (3 BLOCKER-tier, all folds · 8 MAJOR · 15 MINOR · 3 INFO; 15 of the 29 are folds-by-reference with banked identities preserved; 8 are reader-missed finds booked) · 10 killed-claims register entries (8 reader kills RATIFIED by this seat's own instruments · 1 reader confirmation OVERTURNED into a fold by identity · 1 reader framing killed) · 9 superlatives ratified (3 corpus/substrate-level) · 1 fold minted that neither reader made (C-13) · 2 evidence-cell corrections · 1 bank substrate-cell correction.**

The component-owned headline is **MPC-10** — the banked cure for the fleet's dead `--slider-scrub-*` family is **not visually neutral**: applied verbatim it drops the `standard` slider's only affordance (the fill edge, per the producer's own d.ts contract) from 11.51:1 to **1.51:1** against its own track — a WCAG 1.4.11 regression shipped under a green token-sweep gate. Both readers found the arithmetic independently; this seat priced the corrected cure (full-strength `--track-color`, glass's 88% as the sole dilution) at **4.00:1 PASS**. Everything else BLOCKER-shaped here is already banked (FMD-4 starved band · fr-EasingPicker/RD-5 catalog bump · B-2/FMD-3 dead retint); the fresh registry-clean books are three design MAJORs (type-scale inversion, dead-domain-boots-at-floor, forced-colors num-input) and a connective MINOR tail.

## The incredulity seat's own work — ruled disagreements

| topic | reader-1 | reader-2 | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **D-6 · L-D1 · C-4** (number field desyncs at clamp fixed points) | FOLD → banked FMD-25, MINOR-as-banked | CONFIRMED **MAJOR**, "not banked anywhere — grep for `emitDuration` over all 204 rows → no fourier hit" | **FOLD → FMD-25, MINOR-as-banked — reader-1 wins; reader-2's registry-clean claim OVERTURNED by identity** | fr-FourierMorphDemo.md:72 (FMD-25) books "the clamp funnel's three edges: no-op clamps desync the input's DOM (`:value`+`@change`, no force-write); **two of three emitters** lost `Math.round`…" — the third emitter, the one that HAS `Math.round`, is exactly `emitDuration` (MorphPhaseConfig.vue:93-96, seat-read; the FMD corpus read this file whole). A literal-string grep is not an identity sweep — the anti-rename lesson, again. Reader-2's sharpenings FOLD IN: boot-state reachability (`morphMs: 50` = the clamp floor, useFourierMorph.ts DEFAULT seat-read — the Morph card boots ON a fixed point) and the `shouldUpdateComponent` short-circuit mechanism (props unchanged ⇒ no re-render even while the parent renders every frame). Reader-2's MAJOR (echoing the original axes) preserved as dissent. |
| **MPC-10 severity** (the banked cure's paint) | MAJOR, registry-clean | INFO rider on a banked cure ("perceptibility → SS-13") | **MAJOR — reader-1 wins; seat adds the pricing cell** | Per the ColorSpaceSelector addendum law ("the cure is adjudicated separately from the defect; every adopting wave states the property the cure could break and gates it in the breaking direction"): the arithmetic is fully token-decidable and this seat re-ran it end-to-end (11.51 → **1.51** light; reader-1's dark cells 5.32 → 1.42 adopted as reader-cell, direction concordant). The producer's own d.ts declares the fill edge THE handle ("its leading EDGE IS the handle — the only affordance is the fill edge") with the thumb `width:0; opacity:0` (seat dist extraction) — a 1.51:1 edge is not a control boundary. Not INFO: it is a repair-blocking amendment to a BLOCKER-tier banked cure whose acceptance gate (token-family sweep → 0) would pass GREEN over the regression. Seat's new cell: dropping the consumer's 30%/45% wrappers and passing `--track-color` full-strength yields **4.00:1 PASS** under glass's own 88% — the corrected cure is now priced, not just prescribed. Rendered-hairline residue (`--slider-range-blur`, `--glass-material-rim`) → SS-13. |
| **C-5 severity** (HEAD does not typecheck; migration half-applied) | MAJOR ("the sharpest framing") | MINOR (runtime graceful via `data-variant` fallthrough) | **MINOR with the sequencing rider carried at cure-order weight** | Facts unanimous and seat-verified (`git show HEAD…:23` = `glass-scrubber`; pinned d.ts `variant?: "standard" | "spectrum"`; `build` = `vue-tsc -b && vite build`). The repo-state fact is census-recorded (mid-bump, 28 uncommitted paths); the LOAD-BEARING content is C-5's sequencing insight — landing the one-line flip alone turns the build green and hides the dead-token half — which rides MPC-3/FMD-3's cure ordering as a hard constraint: **the variant fix and the token fix land in one cut.** |
| **L-D8 · C-8** (leaf coupling: glass root barrel + keyframes in the static graph) | KILLED-in-part (eager-value.js half banked at FMD-20/FR-AH-7; residue folds) | CONFIRMED MINOR (facts verified; bundle delta zero conceded) | **Reconciled: facts CONFIRMED, eager-narrative KILLED, residue FOLDS → FMD-34 / fr-EasingCurvePreview BARREL** | Seat-read: useMorphConfig.ts:9 root-barrel `useClipboard`, :14-18/:20 pure re-export of the easings catalog (one catalog, three public surfaces incl. useFourierMorph's re-export); the value.js-bearing engine sits behind `getAnimationCtor()`'s dynamic boundary. Narrating the engine as eager is the killed half — the REAL eager edge (static `easings.ts` → value.js) is banked at FMD-20/FR-AH-7. C-20's re-attribution carried: glass-ui 4.0.0 ships **no** subpath exporting `useClipboard` — the root-barrel edge is producer-forced; a `./clipboard`/`./composables` subpath ask rides the standing glass BH relay. |
| **C-13** (keyframes 4.3.0 hard-deps value.js `^0.13.0`) | CONFIRMED MINOR, standalone | not booked | **FOLD — seat's own minting; neither reader folded it** | The identity is banked: fr-EasingPicker C/B-2 already routes the keyframes-hard-dep/peer-contract fact **FOLD BY REFERENCE → fr-AnimationControls C-1 = fr-BasisCanvas C-8, one identity, F.W1**. Facts re-verified by this seat's node read (byte-identical dependencies object). C-13's contribution survives as the reason MPC-2's repair cannot be a one-file specifier rewrite — carried as a note on the fold, not a fresh row. |
| **D-9 / D-10 / D-11 / D-12** (design rows reader-2 did not book) | CONFIRMED (2 MAJOR + 1 MAJOR + 1 MINOR) | silent (in-lane: library axis) | **CONFIRMED as rated — seat spot-verified every load-bearing literal** | Type-scale inversion exact in the live file (:120-126 title `text-lg`/400 · :128-131 desc `text-sm` · :148-154 label `text-base`/500 · :167-180 input `text-base`/600 — top-down 18→14→16px with the tertiary element heaviest); D-10's boot-at-floor (min=50=Morph default; zero-extent range; thumb `width:0;opacity:0` seat-extracted); D-11's forced-colors hole (`outline: none` :177 + border/box-shadow-only focus :188-191; the allow-list is closed and `.num-input` is bespoke scoped — seat-read verbatim; 3 blocks, 0 slider or num-input arms); D-12's unused affordances (SelectGroup/SelectLabel exported; `description` slot; the taxonomy latent in data order). Reader-1's slider-arm rider on D-11 ratified: the slider's WHCM gap is glass-owned, banked ConfigSliderPane:60 → RELAY I-20, not re-booked. |

## Adjudicated defect roster

Line anchors are the fourier working tree over HEAD `cd26c65`; re-resolve before executing any cure. Folds carry the banked row's identity — original ids for life; nothing here re-books a banked defect.

### BLOCKER tier (all folds — identity preserved, no re-book)

| id | claim · proof | terminal disposition |
|---|---|---|
| **MPC-1 · D-1** | Two misaligned breakpoint systems (grid/card/page at 640 vs root-font at 768) starve the duration slider to ~0px across 640-767px and over-subscribe the 3-up grid; `overflow-x:hidden` CLIPS the excess. Every literal re-verified by both readers against the live tree; the slider's min-content ≈ 0 because the `standard` thumb is `width:0` (seat dist extraction). | **FOLD → banked fr-FourierMorphDemo FMD-4 (BLOCKER · F.W4).** Reader-1's sharpening ADOPTED as a rider on the banked row: `.config-grid` uses bare `1fr` (= `minmax(auto,1fr)`), so the grid box itself over-subscribes and the page-level `overflow-x:hidden` clips rather than scrolls — a WCAG 2.2 **1.4.10 Reflow** arm FMD-4 does not record. Exact band edges/clip counts → SS-13 (already queued as FMD input #2). |
| **MPC-2 · C-2 ⊕ L-D16** | The easing catalog cannot resolve at the value.js bump: `lib/easings.ts:9-16` are bare-root specifiers; 4.0.0 publishes **no `"."` export** (7 subpaths — seat node-run) and `timingFunctions` is absent from `./easing` (seat node-run: `false`) and every published entry point; the successor is `easing(): Result`. | **FOLD → banked fr-EasingPicker L/B-1 + C/B-1 (census C-7 identity) · F.W1.** The 22/22-name survival through `easing(name).value` is bank-ratified; the Result-shape rewrite of `EASING_PRESETS` is the booked repair. C-13's hard-dep fact (fold above) is why this is a lockstep tri-package bump, never a specifier rewrite. |
| **MPC-3 · D-4 ⊕ L-D2 ⊕ C-1** | The `--slider-scrub-*` retint block (:205-211) is dead — `sliderColor` (:85), the `--track-color` feed (:29) and the parent's red/pink/red accents (FourierMorphDemo.vue:31/:41/:51, seat-read) deliver zero pixels; every card paints `var(--primary)` at 88%. Seat census: **23 declarations / 7 files**, 0 producer hits at 4.0.0. | **FOLD → banked fr-BasisSelector B-2 (fleet BLOCKER) / fr-FourierMorphDemo FMD-3 · F.W1 (execution F.W3/W4).** All three axis blast-radius figures are KILLED (register K-5); the bank's 23/7 stands, seat-recounted a third time. **The cure is AMENDED by MPC-10 below** — rename alone is no longer the accepted repair. Sequencing per MPC-13: lands in the same cut as the variant flip. |

### MAJOR tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **MPC-4 · D-2 ⊕ D-7 ⊕ C-15 ⊕ L-D12** | Both `<label>`s are inert (:8/:35 — no `for`, wrap nothing); the number input's complete attribute set (:10-18) carries no name; the SelectTrigger (:37) carries only `class="w-full"` — the Easing select is named by its own value. | **FOLD → banked FMD-14 (MAJOR · F.W4; `LabeledField`/`./number-field` ship at the pin — seat-verified in the export map).** D-2's BLOCKER corrected down to the adjudicated severity. The D-7 "teleported clone" second-order arm is KILLED (register K-4); the primary claim is unaffected. |
| **MPC-5 · C-3** | 8 of 22 presets silently change curve shape at the bump (max Δ 0.192, `ease-out-circ`); reader-2 reproduced the table to four significant figures with its own 1001-point sampling. | **FOLD → banked fr-EasingPicker CC-2 sharpening + library-band RD-5 · F.W1.** RD-5 is **RULED RESTORE** — the analytic arms return producer-side, closing the drift leg without a marker vocabulary; the F.W1 acceptance re-runs the sampling. Denominators reconciled (bank's 14-drift vs C-3's 8-MATERIAL are one fact: 6 of the 14 were already bezier ⇒ solver noise). |
| **MPC-6 · D-8** | The number field's border is 1.36:1 against its own fill on a fill ≈1.01:1 against the card (WCAG 1.4.11); token literals byte-verified by reader-1 against the installed dist. | **FOLD → banked FMD-19 (the contrast suite, MAJOR · F.W4).** D-8's dark-arm "UNPROVEN" caveat is already discharged at the bank (borders 1.36/1.28 light/dark, with the recorded 1.40-not-1.53 drift note — both FAIL; no re-derivation needed). |
| **MPC-7 · D-9** | The type scale is foreign to the system and internally inverted: reading top-down the card steps 18 → 14 → **16px** with the tertiary label at weight 500 and the input at 600; glass's φ ladder (`--type-subheading/-heading/-title`, seat-cited from the pinned scale.css via reader verification) is entirely unused; no project `--text-*` override exists (falsifier grep 0). | **MAJOR · registry-clean · books HERE → F.W4** — three token substitutions restore ordering and commensurability; rides the FMD-19/FMD-13 token cleanup. |
| **MPC-8 · D-10** | The slider domain is 89% dead space and the Morph card boots AT the floor: min=50=default (seat-read), so `.slider-range` has zero extent and the `standard` thumb paints nothing — the boot state is an empty grey capsule with no position indicator, on a control that is also 0px wide in MPC-1's band. Producer intent corroborates: the fill edge IS the handle (index.d.ts, seat-read via extraction). | **MAJOR · registry-clean · books HERE → F.W4** — re-domain toward the used 50-300 band or add `marks` (the D-24 `marks` half SURVIVES at v7 :23 AND v8 :28 — seat git-read); zero-width seam question → SS-13. |
| **MPC-9 · D-11** | The number field's focus indicator vanishes entirely under `forced-colors: active`: `outline: none` (:177) + border-color (forced to system palette, indistinguishable from resting) + box-shadow (stripped). The producer's antidote is a closed class allow-list (seat-read verbatim) that a bespoke scoped class cannot join — the cost of hand-rolling instead of consuming `.input-pill`/`./number-field`. | **MAJOR · registry-clean for `.num-input` · books HERE → F.W4** (dies structurally in FMD-12's `./number-field` adoption). The slider half is glass-owned and banked: ConfigSliderPane:60 → **RELAY I-20** — not re-booked. Seat cell correction: the `forced-colors` string appears in 4 dist files; 3 are @media blocks (ladder.css comment-only) — reader-1's enumeration survives at the block level. |
| **MPC-10 · NEW (reader-1 missed-1 ⊕ reader-2 missed-2, merged)** | **The banked B-2/FMD-3 cure is not visually neutral.** glass-ui composites `--slider-range-bg` a second time (`color-mix(in oklab, var(--slider-range-bg, var(--primary)) 88%, transparent)` — seat dist extraction); the consumer authors 30%. Rename-only ⇒ net alpha 0.264 ⇒ the fill drops **11.51:1 → 1.51:1** vs its own track (seat's own end-to-end run: track `#f8f6f1`, today `#363331`, cured `#f1bebb`; dark 5.32 → 1.42 reader-cell, concordant) — a 1.4.11 failure on the `standard` recipe's only affordance, shipped under B-2's green token-sweep gate. Registry-clean (reader-1's "88%" sweep over the bank → 0; the bank's own K-11 "all sliders render identically today" is exactly why the cure's paint was never priced). Applies at all five tint-dropping sites plus SliderControl's 25%. | **MAJOR · CURE-RIDER amending banked B-2/FMD-3 · F.W1.** Corrected cure, seat-priced: rename AND re-author — drop the 30%/45% `color-mix` wrappers, pass `--track-color` full-strength, glass's 88% the sole dilution → **4.00:1 PASS** (seat's cell). Per the adjudication-of-cures law, B-2's acceptance gate gains a leg in the breaking direction: a contrast assertion on the cured fill (G6(ii) template), not the token grep alone. Rendered hairlines (`--slider-range-blur`, rim shadow) + red/pink discriminability at full strength → SS-13. |
| **MPC-11 · L-D3a ⊕ D-8-chassis** | The duration row re-implements the in-tree `ui/SliderControl.vue` chassis and loses on four axes (label association, `Number.isFinite` clamp, `@input` cadence, `:aria-label` binding) — correspondence table re-verified line-by-line by both readers; seat re-read :40-49/:53-56/:66-80/:87-89. | **FOLD → banked FMD-12 + fr-BasisSelector M-6 (MAJOR · F.W3).** The prescribed substitution recipe is KILLED (register K-6) — the duplication survives; the recipe must not travel into a ticket. Note the chassis carries the SAME dead tokens (SliderControl.vue:144-148, in the seat census) — MPC-3/MPC-10 sweep first or the regression travels. |

### MINOR tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **MPC-12 · D-6 ⊕ L-D1 ⊕ C-4** | Uncontrolled `:value`+`@change` number input desyncs whenever the clamp is a fixed point; boot-reachable (Morph card boots at the floor); the Export button copies the model while the screen shows the rejected text. | **FOLD → banked FMD-25 (MINOR-as-banked · F.W4)** — ruled above; reader-2's MAJOR recorded as dissent; its reachability + `shouldUpdateComponent` mechanism folded into the bank's cure text (force-write on no-op clamp). |
| **MPC-13 · C-5** | The committed state does not typecheck (`glass-scrubber` ∉ pinned `SliderVariant`; `build` = `vue-tsc -b`); the WT one-liner is the cosmetic half of the migration. | **MINOR (ruled above) · F.W1** — with the sequencing rider at cure-order weight: the variant flip lands WITH MPC-3/MPC-10's token cure in one cut, never before. **Bank substrate-cell correction minted**: fr-FourierMorphDemo's header cell "every subject file clean" is contradicted by its own FMD-11 citation of WT-only content (`variant="standard"` at :23) and by today's porcelain (`M` on both morph slider files) — the WT flip predates/spans that adjudication; the cell, not the verdicts, is wrong. |
| **MPC-14 · C-13** | keyframes.js 4.3.0 hard-depends on value.js `^0.13.0` (seat node-read, byte-identical). | **FOLD → banked fr-AnimationControls C-1 (= fr-BasisCanvas C-8; via fr-EasingPicker C/B-2) · F.W1** — seat's own minting; carried as the why-lockstep note on MPC-2. |
| **MPC-15 · D-13** | Heading skip h1 → h3; no h2 anywhere on the route. | **FOLD → banked FMD-31 (MINOR · F.W4).** |
| **MPC-16 · D-21** | Hover-lift affordance (`cartoon-card` → `cartoon-surface`, −1px) on a non-interactive card. | **FOLD → banked fr-EqCoefficientsPanel FR-EQC-5 (class row) + fr-ContourPreview row 23 · F.W1 self-cures** (8.0.0 `cartoon-surface` has no hover). |
| **MPC-17 · D-22 ⊕ C-9 ⊕ L-D14** | `easingCurvePath` uncached in the 22-item `v-for` while `_svgCache` sits in the same module — 550 evaluations per dropdown open (correctly derated per-open by all three axes). | **FOLD → banked FMD-32 → fr-EasingCurvePreview BARREL + FORK · F.W3.** |
| **MPC-18 · L-D9 ⊕ L-D10 ⊕ L-D5 ⊕ C-12** | `updateField` dead + the module's only `as any` (seat-read :64-67/:92); `syncWith` no disposer + wasted `deep: true` over a spread source (:78-84); `arr[0]` vs the producer's `number[] | undefined` (:99-102). | **FOLD → banked FMD-26 (→ FR-AH-37 deletion batch) + FMD-33 · F.W4.** L-D10's leak framing KILLED (register K-9). |
| **MPC-19 · L-D8 ⊕ C-8 ⊕ C-20** | Pure constants imported through a composable barrel, three export surfaces deep; root-barrel `useClipboard` producer-forced (no subpath exists). | **FOLD → banked FMD-34 → fr-EasingCurvePreview BARREL · F.W3** (ruled above); the `./clipboard` subpath ask rides the glass BH relay. |
| **MPC-20 · C-14** | Both emits terminate in `createTweenAnimation`, which hardcodes `timingFunction:"linear"` + `useWAAPI:false` and discards the engine's interpolation (seat-read :127-133) — the leaf emits correctly; the seam discards. | **FOLD → banked FMD-8 (MAJOR at its home; MINOR at this leaf) · F.W4** — the `NumericAnimation` cure with its banked caveats. |
| **MPC-21 · NEW (reader-2 missed-1)** | `variant="standard"` is redundant at BOTH ends of the uplift: 4.0.0 computes `C.variant ?? "standard"` with CVA default `standard` (seat chunk-read); 8.0.0 defaults `"scrubber"` with every non-spectrum leg authored `:not([data-variant="spectrum"])` (seat git-read :145/:153). **DELETING the prop at all 9 sites is a no-op today and needs no second edit at 8.0.0** — strictly better than the rename every axis and the bank prescribe. Registry-swept clean by the reader. | **MINOR (defect) / repair-value AMENDMENT to banked FMD-11 + D-5's cure · F.W1.** |
| **MPC-22 · NEW (reader-1 missed-2)** | The number field's focus tint hard-codes `var(--accent-red)` (:189-190, seat-read) while the slider one gap away retints per-instance via `--track-color` (:29) — invisible today (retint dead), guaranteed two-colour Morph card (pink track, red focus ring) the moment MPC-3/MPC-10 lands. FMD-18 books the ADJACENT inter-sibling divergence, not this intra-card one. | **MINOR · books HERE, riding the MPC-3/MPC-10 edit → F.W1** — one token: `border-color: var(--track-color, var(--accent-red))`. |
| **MPC-23 · NEW (reader-1 missed-3)** | Every dropdown preview is drawn 2.25× wider than tall relative to its own unit square: `x = 2 + t·36; y = 18 − v·16` (seat-read in easings.ts) bakes the anisotropy into the path data — a linear ramp renders at 23.96°, and the axis compressed is exactly the one distinguishing 22 near-identical presets (compounds D-12/MPC-25). The undistorted sampler sits nine lines earlier (`generateCurveSVGPath`, normalized [0,1]² — seat-read); the producer doctrine is "THE FRAME IS A CONSTANT" (banked fr-EasingPicker cite). Registry-clean: WRONGPAD is clipping magnitude, GEO-1 is the sibling's letterbox — neither is this sampler's aspect. | **MINOR · books HERE → F.W3** — dies by construction in the FMD-32/EasingCurve consolidation (constant square frame); perceptibility + the K-GEOM `--ui-glyph` question → SS-13. |
| **MPC-24 · NEW (reader-1 missed-4)** | The compound: in MPC-1's 640-767px band the slider is 0px (banked), the native steppers are removed by author decision (:179-186 — the S-5 superlative's other edge), and `.num-input` (~31-34.5px computed) is outside glass's coarse-pointer floor (a closed three-selector allow-list, seat-read verbatim) — on a coarse pointer in that band the Morph card has **no usable duration control at all**. No banked row joins FMD-4 and FMD-12. | **MINOR · books HERE → F.W4**, riding FMD-4's restructure + FMD-12's `./number-field` adoption; computed block-size on a real device → SS-13. |
| **MPC-25 · D-12** | 22 options, one flat list, eight families; SelectGroup + SelectLabel + the `description` slot all present at the pin (seat export-read); the taxonomy already latent in data order. | **MINOR · registry-clean · books HERE → F.W4.** |
| **MPC-26 · the tail — D-14(frame) · D-15 · D-16 · D-17 · D-18 · D-19 · D-20 · D-23 · L-D6 · L-D7 · C-6 · C-10 · C-11 · C-16 · C-17** | Source-verified batch, none contradicted: no drawn reference frame on the preview (the 0- and 1-lines land at y=18/y=2, neither drawn); two label geometries in one card; seven off-grid spacing literals; `:focus` not `:focus-visible`, tinted off-system; no error/invalid/placeholder/disabled surface (`<SelectValue />` bare; reka default placeholder `""`; `isAnimating` never reaches the cards); unaligned rows across the grid; non-parallel description prose (three grammatical shapes, seat-read at the callsites); unbounded `title`/`description`; no step quantisation (rounds to integer, never snaps to 10); the [50,800,10] domain quadruplicated with no shared constant; `easing` typed bare `string` where `keyof typeof EASING_PRESETS` is free — which would also make C-6's `String($event)` laundering a compile error at every writer. Citation drifts recorded, verdicts unaffected: D-16's L126→L125; D-17's style.css:133-137→:136-141; C's useFourierMorph anchors :62/:66-68→:61/:65-67. | **MINOR/INFO batch · F.W4** (the typing leg + placeholder guard are two-token edits; the frame leg rides MPC-23's consolidation). |

### INFO tier (new finds)

| id | claim · seat verification | disposition |
|---|---|---|
| **MPC-27 · NEW (reader-1 missed-5)** | `class="w-full"` on the SelectTrigger duplicates the recipe base (`… focus-ring flex w-full …` — seat-read in the compiled chunk); the sole attribute on the trigger is inert, and the attribute that belongs there is `aria-label` (MPC-4). | **INFO · F.W4** (dies in the FMD-14 edit). |
| **MPC-28 · NEW (reader-2 missed-4)** | The pinned focus ring paints on `.slider-track` via a spread-only shadow — in MPC-1's band the track's grown width is 0, so the ring degenerates to a ~2px sliver: D-3's conclusion is accidentally right in one narrow band for a reason neither the killed claim nor FMD-4 names. | **INFO · rider on FMD-4 · SS-13** (sliver perceptibility). |
| **MPC-29 · NEW (reader-2 missed-5)** | `/morph`'s only automated gate is one screenshot row (`visual-baseline.spec.ts:35`, seat-read; no unit runner in `scripts`, seat-read) — and that screenshot is precisely the instrument that catches MPC-5's 3.1px preview drift, **iff the baseline regeneration straddles the value.js bump** (pre-bump baseline held, post-bump build diffed against it). The no-unit-tests mechanism is broadly banked; the sequencing rider is new. | **INFO · F.W1 sequencing note.** |

## Killed-claims register

Refutations ruled by this seat; original ids for life. A killed claim is not re-mintable without new evidence.

1. **K-1 · D-3 (BLOCKER: "no visible keyboard focus indicator under the pin; no `:focus-within` … anywhere in the 4.0.0 slider block")** — **KILLED BY THE PIN ITSELF; both readers concordant, seat-ratified by its own extraction**: the installed 4.0.0 dist ships `.glass-slider:not([data-variant=spectrum]):focus-within .slider-track[data-v-534634a7]{box-shadow:var(--focus-ring-shadow)}` — non-spectrum, targeting the 20px track (at `data-size=md`), firing for the invisible thumb's focus because `:focus-within` matches ancestors. D-3's `}`-split enumeration missed the rule its own falsifier named; ConfigSliderPane:33 had already quoted it as verified. Consequential collapse: "the uplift CURES this" inverts the truth — the cure is at the pin; 8.0.0 merely re-spells it (`:has(:focus-visible)`, with the authored comment explaining WHY it replaced the 4.0.0-era `:focus-within` — the producer's own text confirming the killed claim false). The surviving WHCM residue is banked glass-owned (ConfigSliderPane:60 → RELAY I-20). |
2. **K-2 · D-5/P-2 ("the 7.0.0 Slider's vocabulary is `scrubber | spectrum`; `standard` is gone; types.ts:10 / Slider.vue:26/:258")** — **KILLED at its named baseline; seat git-run**: `v7.0.0:types.ts:5` reads `"standard" | "spectrum"`; v7 defaults `variant: "standard"` at :19. Every cited line number is an 8.0.0/working-tree fact stamped 7.0.0 — the K-12/SR-2 baseline-law failure, now carried by its **FOURTH** corpus (FMD's seat counted three). The SUBSTANCE survives at 8.0.0 and is banked (FMD-11 slider leg / fr-ConvergenceTimeline K-8 / fr-BasisSelector M-7); "contradicts the census by addition" collapses — it is a re-book of an adjudicated row, and MPC-21 supersedes the rename cure anyway. |
3. **K-3 · D-24's `valueText` leg + D-3's cited 7.0.0 cure file** — **KILLED; seat git-run**: `v7.0.0:src/components/slider/styles.css` does not exist at the tag; v7 `SliderProps` carries `marks`/`keepDockOpen`/`motion` and **no `valueText`**. The `marks` half survives at v7 AND v8 and is adopted into MPC-8's cure. |
4. **K-4 · D-7's second-order arm / L §4.2 ("the selected item's entire content — including the SVG — is projected into the trigger; one extra live SVG per card")** — **KILLED IN REKA'S OWN BYTES; seat-read**: SelectItemText registers a `textContent` STRING into the option map; SelectValue renders `createTextVNode(toDisplayString(…))` — a text node, never a subtree; no second `<svg>` mounts and nothing joins any accname computation. Both axes independently invented the same DOM-clone mechanism. D-7's primary claim unaffected (MPC-4). |
5. **K-5 · the blast-radius figures (D-4 "10/5 files" · L-D2 "21/7" · C-1 "23/6")** — **ALL THREE KILLED; seat grep**: 23 declarations / 7 files, distribution matching the bank (fr-BasisSelector B-2 / fr-ConvergenceTimeline recount) exactly. The F.W1 acceptance is a token-family sweep to zero + MPC-10's contrast leg, never a per-file count. |
6. **K-6 · L §9.2's prescribed cure (`<SliderControl … :format-value="v => v + 'ms'">` "kills the desync…")** — **KILLED, both readers concordant, seat-verified in SliderControl.vue**: `formatValue` makes `displayValue` non-numeric ⇒ `isNumericDisplay` false ⇒ the input degrades to `type="text"` with `min`/`max`/`step` bound `undefined` — the nominated prop is the switch that disables the numeric contract; AND `onInput` clamps-and-emits with no DOM write-back, so the fixed-point trap survives the substitution (`@input` changes how often it escapes, not whether it forms); AND the chassis geometry is a column, not the row — a layout change, not a drop-in. The duplication finding survives as MPC-11's fold. |
7. **K-7 · L-D8/C-8's eager-coupling framing** — **KILLED-in-part (ruled above)**: the value.js-bearing engine is behind a real dynamic boundary; the true eager edge is banked (FMD-20/FR-AH-7). Residue folds (MPC-19). |
8. **K-8 · D-14's "shaved" half · L-D13 · C-18 (back-curve clipping)** — **SPENT ROW; re-book killed**: adjudicated at fr-FourierMorphDemo m-5/K-4 (seat node-run there: extrema inside the box, ≤0.3px of outer stroke edge), magnitude banked at fr-EasingCurvePreview WRONGPAD; the residue (`overflow: visible` absent) is an INFO note inside FMD-32. All three axes graded honestly; the row is simply not re-mintable. |
9. **K-9 · L-D10's leak framing ("two calls create two permanent watchers…")** — **KILLED (reader-2, ratified; seat-verified the callsite)**: `syncWith` is invoked exactly once, synchronously in `FourierMorphDemo`'s setup, so the watcher binds the component effect scope and auto-disposes. The residue (no stop handle; `deep: true` pure waste over a spread source) is already banked at FMD-33 — fold, not re-book. |
10. **K-10 · reader-2's "registry-clean" cell on the desync row** — **KILLED by identity (ruled above)**: FMD-25's three-emitter clamp funnel includes `emitDuration`; a literal-string grep over the bank is not an identity sweep. The row folds; reader-2's mechanism sharpenings survive inside the fold.

## Superlatives — ratified, with provenance

1. **S-1 (D), NARROWED per banked FMD-15 — both readers concordant, seat-verified**: `aria-label="Duration (ms)"` (:27) is placed with knowledge of where reka mints `role="slider"` — the pinned chunk re-projects `$attrs["aria-label"]` onto each thumb (seat chunk-read), and the unit reaches AT only through this string. The superlative holds for the **placement leg only**: the same byte-identical string names all three mounted cards, which is banked MAJOR FMD-15 — a superlative may not celebrate an adjudicated defect's content leg (reader-2's corpus-hygiene catch, adopted).
2. **One clamp funnel, two input paths (D S-4 · C S-3/S-4 · L S2)** — seat-verified: `Math.max(50, Math.min(800, …))` exactly once (:94), `emit("update:duration"` exactly once (:95), `durationModel.set` delegates (:101). The writable computed is the right adapter, not the mirror-ref-plus-watch; the parent stays authoritative. L's framing is the sharpest: the desync (MPC-12) is a DOM defect, and the reactive adapter beside it is exactly correct. Concordant with FMD superlative 3.
3. **Reduced-motion-safe by construction (D S-2), with reader-1's correction adopted**: the scoped block declares exactly one transition (`border-color 0.15s`, :178), on glass's PRM allow-list — no vacuous PRM arm shipped because none is needed. Correction: the RENDERED tree still inherits `cartoon-surface`'s hover translate and the producer's active-scale, both PRM-neutered; the superlative is about the authored surface, where it holds.
4. **The catalog is complete and the iteration set is derived (D S-3 · C S-1/S-2)**: `EASING_PRESET_NAMES = Object.keys(EASING_PRESETS)` over the object being indexed — `presets[name].label` structurally cannot miss; 22/22 resolve at 0.13.0 (bank re-run: 55 keys, 5/5 object identity) and 22/22 still resolve at 4.0.0. Reader-2's sharpening adopted: with `strict: true` and no `noUncheckedIndexedAccess`, the construction is doing work the compiler flag is not.
5. **Zero teardown surface (L S1)** — seat-verified: :64-106 carries no lifecycle hook, watch, timer, listener, template ref, or subscription; the producer disposes its own touch listeners (readers' chunk reads, concordant). In a subtree whose banked headline family is FR-AH-8/-9/-20/-21, a leaf with nothing to leak is worth naming.
6. **Complete spinner suppression across engines (D S-5)** — seat-read :179-186: `-moz-appearance: textfield` + both webkit pseudo-elements with the usually-forgotten `margin: 0`. (Its other edge is priced honestly at MPC-24.)
7. **A genuinely presentational leaf, zero API reach (C S-6)** — reader greps concordant: nothing in the 45-operation surface can touch it; the emission path terminates in an SVG sink. All three axes correctly declined to manufacture a canvas/WebGL finding.
8. **Corpus superlative — measurement fidelity**: ~15 token line-citations resolved byte-exact; two off-by-one drifts and zero arithmetic errors across ~60 defects; every kill in this record is inference, baseline, identity, or mechanism — not measurement. L §7's candidates-checked-and-cleared section (six self-kills including an executed twMerge probe) is the page every seat should ship, and reader-2 re-derived its hardest cell independently.
9. **Substrate superlative (reader-1's, ratified)**: the pinned `dist/components/ui/slider/index.d.ts` documents WHY the thumb is invisible, why reka's thumb stays mounted, and that "keyboard focus rings the TRACK" — producer prose that pre-answers the corpus's most confident BLOCKER (killed K-1). Rare enough to bank.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **MPC-1/FMD-4** — rendered `.duration-slider-track` width at 640/700/768/960; overflow-vs-clip manifestation; clipped pixel count at the third card's right edge (already queued as FMD input #2).
2. **MPC-10** — rendered appearance of the cured range fill: apply the corrected (full-strength) cure in a scratch build, screenshot the three cards at 1280px light + dark; decide whether `--slider-range-blur`/`--glass-material-rim` contribute a hairline the flat-composite model misses; confirm red/pink/red discriminability at glass's 88%.
3. **MPC-8/D-10** — whether the zero-extent range paints a rim-shadow seam or nothing at boot (direction unaffected).
4. **MPC-23** — whether the 2.25× anisotropy defeats preset discrimination in practice (open the dropdown, compare Ease In Quad/Cubic/Sine); pair with the banked K-GEOM instrument to settle whether the Button-cva glyph rule reaches a SelectItem-hosted `<svg>`.
5. **MPC-24** — computed `.num-input` block size on a real coarse-pointer viewport (~31/34.5px is derived, not measured).
6. **MPC-28** — perceptibility of the ~2px focus-ring sliver where the track is width-0.
7. **C-7** — whether 22 curves are distinguishable at the Morph card's 50ms (~3-frame) default; frame count source-derived, perception not.
8. **D-19** — the widths at which the three description wrap-counts diverge and knock the Duration rows out of alignment.
9. **L-D5/C-12 residue** — whether `vue-tsc -b` flags the `number[] | undefined` variance (C measured no diagnostic with a harness; either branch is a defect, so the hazard stands on the d.ts).
10. **Provenance rider** — every producer-8.0.0 fact here is a tag/working-tree read at `407de2d3`; re-baseline at F.W1 per banked SR-2/FM-2.

## Verdict

**ADJUDICATED (M-25 agglomeration input — stamps nothing): 29 rows confirmed (3 BLOCKER-tier, all folds with identities preserved · 8 MAJOR · 15 MINOR · 3 INFO; 15 folds-by-reference; 8 reader-missed finds booked, all seat-verified where load-bearing) · 10 killed claims (incl. one reader confirmation overturned into a fold by identity) · 9 superlatives ratified · 1 seat-minted fold (C-13 → fr-AnimationControls C-1) · 1 bank substrate-cell correction (fr-FourierMorphDemo "every subject file clean") · the headline is a CURE-RIDER: the fleet's banked dead-token repair, applied verbatim, would ship a 1.51:1 fill edge on the slider's only affordance under a green gate — the corrected cure is seat-priced at 4.00:1 and B-2's acceptance gains a contrast leg in the breaking direction.** The leaf itself is a well-mannered prop→emit component whose authored surface earns real superlatives; its debt is overwhelmingly design-local (type scale, dead domain, forced-colors, naming — F.W4) and fleet-inherited (dead tokens, catalog bump, chassis duplication — F.W1/F.W3), and both of its corpus's most confident BLOCKERs died against the pin's own bytes.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
