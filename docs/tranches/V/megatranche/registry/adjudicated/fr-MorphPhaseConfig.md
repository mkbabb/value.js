claude-fable-5 (served model id)

# fr-MorphPhaseConfig — adjudicated apotheosis, PASS 2 (M-23/L-18 hostile re-read)

**Seat**: fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Supersession**: this file replaces-in-place the 2026-08-24 pass-1 apotheosis, which remains immutable in git at `dd218c3a` (E-1/E-3 — dated evidence is never rewritten; this file is new). All pass-1 ids (MPC-1..29, K-1..10, S-1..9) are carried with their identities; pass-2 touches are marked ►.
**Date**: 2026-08-25 · **Subject**: `fourier-analysis/web/src/components/morph/MorphPhaseConfig.vue` (212 lines, re-read whole by this seat — working tree).
**Substrate**: fourier HEAD `cd26c65`, WT dirty on both morph slider files (`M` MorphPhaseConfig.vue + HarmonicLevelGrid.vue — seat porcelain, unchanged since pass 1). Installed glass-ui **4.0.0**; glass producer HEAD `8a96868d` (package 8.0.0), tags `v7.0.0`/`v8.0.0` seat-read. value.js producer 4.0.0 · pin 0.13.0 · keyframes 4.3.0.
**Corpus**: the three axis challenges (immutable beside this file) × the pass-1 bank × two hostile re-reader returns, both slug `MorphPhaseConfig`, both of which re-derived the bank's arithmetic from independently written instruments.
**Method (M-23/L-18)**: the readers were challenged back, not transcribed. Every reader-vs-reader disagreement, every new kill, and every new find's load-bearing cell was re-derived against the tree by this seat before ruling. One reader consequence is **overturned** (reader-1's "pixel-unchanged at boot" — falsified by this seat's read of `DEFAULT_MORPH_CONFIG`), one reader "new kill" has its novelty corrected (the bank had already discharged D-8's dark arm), one cross-lane "baseline correction" is re-ruled as a **portability guard** (KF-SST-30 is true at its own pin), and five reader-missed finds are booked after seat verification. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node/git/grep only; zero browser — probe parsimony; product source READ-ONLY; this file the sole write)**: whole re-read of MorphPhaseConfig.vue (every pass-1 anchor re-resolved: :8/:13/:23/:27/:29/:36-38/:47/:94-95/:99-102/:121/:167-191/:205-211); `useFourierMorph.ts` DEFAULT_MORPH_CONFIG read (**settleOutMs 150 · morphMs 50 · settleInMs 150** — the MISS-1 corrector); `easings.ts:85-127` both path builders verbatim (`toFixed(3)` in `generateCurveSVGPath` vs NO rounding in `easingCurvePath`; `x = 2 + t*36; y = 18 − v*16`); installed glass dist — slider chunk thumb class `"slider-thumb glass-specular-track touch-hit-area"`, scoped `.slider-thumb.touch-hit-area[data-v-534634a7]:before{pointer-events:none}`, `@utility touch-hit-area` at 4.0.0 declaring **`pointer-events: auto`** with the authored halo comment, coarse allow-list = exactly `[data-size="icon"] / .expandable-container__trigger / .segmented-tabs__trigger`, `--touch-target: 2.75rem` (offsets-sizing.css:461), `[data-size=md]{--slider-track-height:1.25rem}`, `--accent-pink: oklch(0.613 0.197 353.8)` / `--accent-red: oklch(0.574 0.216 27.5)` (color-radius.css:256/:258), `cartoon-surface` `border-width: 2px` (cards.css:33-34), `--font-serif: var(--font-stack-text)` → Plus Jakarta Sans (bridges.css:68 + scheme-motion.css:43), `valueCommit: (payload: number[]) => any` in the pinned Slider.vue.d.ts:20; fourier style.css:40-49 root ladder (1.125rem < 768px ⇒ **22.5px** track; 20px ≥ 768); `@value-commit` consumers = GlassTimeline.vue:74 + ConvergenceTimeline.vue:81, **0 in the morph directory**; reka bytes — `role: "option"` the sole role in SelectItem.js, `role: "slider"` minted ONLY in SliderThumbImpl.js, useTypeahead.js:14 `textValue ?? textContent?.trim()`; glass producer git — `v7.0.0` a11y-overrides utility `pointer-events: none` verbatim with the authored generalization + WCAG-readback prose, `v8.0.0:src/components/slider/styles.css:85-93` `--slider-target-floor: max(track-height, --slider-touch-target 1.5rem)` coarse block, `v8.0.0` Slider.vue `touch-hit-area` count **0** (thumb class `"slider-thumb glass-specular-track"`) + four aria-attr forwards onto every thumb + the DEV nameless-thumb warn prose, `v8.0.0:src/components/card/styles.css:256-261` `cartoon-surface` = three declarations no hover, `v8.0.0:package.json` exports = **70 subpaths** with `./select` `./slider` `./labeled-field` `./number-field` all live; **the discriminability run** (own oklch→oklab→LMS³→linear-sRGB pipeline, gamma-space composite of `color-mix(in oklab, accent 88%, transparent)` over track `#f8f6f1`): fillRed **`#df3d3c`** = 4.004:1 vs track · fillPink **`#db5799`** = 3.327:1 vs track · **red-vs-pink = 1.203:1** — reproducing reader-1's every cell to the thousandth; registry identity sweeps — `slider-target-floor|slider-touch-target` → **0** adjudicated hits (the uplift cell is unbooked), `accent-pink` discriminability rows → **0**, `valueCommit` rows = fr-SliderControl R-6/R-7 (wrapper narrowing; "the settle event is the cure") + fr-AnimationControls/fr-GlassTimeline/fr-ConvergenceTimeline scrub usage + kf lane — none this component, kf-StartingStyleTarget KF-SST-30 read verbatim (correctly stamped **7.0.0** at its own pin), fr-FourierMorphDemo K-9 read verbatim ("the four morph `var(--font-serif)` declarations resolve to the sans register; FR-AH-2/FR-AH-11 own the identity"), fr-EasingCurvePreview SAMPLE row (F.W3 sweep rider, FACET on `n`), kf-CopyButton children-presentational precedent present.

## Scoreboard

**The bank HOLDS under hostile re-read: 0 of 29 pass-1 rows overturned; 0 of 10 pass-1 kills revived. Pass 2 adds: 5 reader-missed finds booked after seat verification (MPC-30..34: 3 MINOR · 2 INFO) → roster now 34 rows (3 BLOCKER-tier folds · 8 MAJOR · 18 MINOR · 5 INFO) · 4 new killed-register entries (K-11..14) → 14 · 1 reader consequence OVERTURNED by this seat (MISS-1's "pixel-unchanged") · 1 cross-lane portability guard minted (KF-SST-30) · 3 evidence-cell corrections adopted (MPC-1 border-box arithmetic; MPC-7 font-serif rider; MPC-12 sanitization mechanism) · 2 self-cure/provenance cells verified at v8.0.0 (MPC-16; MPC-21 both-baselines).**

The pass-2 headline is the **cure conjunction (MPC-31)**: MPC-3 (dead retint, BLOCKER fold), MPC-10 (corrected full-strength cure, 4.00:1) and MPC-8 (Morph boots at the floor, zero-extent range) each own one term of a conjunction no row prices — after the corrected cure, the grid's only *discriminating* fill is the Morph card's pink bar at **1.203:1** against its red neighbors (seat-reproduced), and that bar has **zero extent at boot**. The repaired colour scheme communicates nothing on the boot render. Consequence: MPC-8's re-domain/`marks` leg is the precondition under which MPC-3/MPC-10's acceptance is *witnessable* — the MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 edits land as **one cut**.

## Ruled disagreements and challenges-back (the pass-2 seat's actual work)

| topic | reader-1 | reader-2 | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **MISS-1 consequence** ("the boot render of the three-card grid is pixel-unchanged by the BLOCKER cure") | asserted | silent | **OVERTURNED — the arithmetic survives, the consequence does not** | `DEFAULT_MORPH_CONFIG` (seat-read): `settleOutMs: 150, settleInMs: 150` — NOT the floor. At boot the two settle fills have (150−50)/750 ≈ **13.3% extent** and the cure turns them visibly red (`#363331` → `#df3d3c`). The TRUE statement, adopted into MPC-31: the cured boot grid shows two byte-identical red slivers and **no pink** — its chromatic *discrimination* content is zero before AND after the cure, because the sole discriminating bar (Morph, `#db5799`, 1.203:1 from red) is zero-extent at boot (`morphMs: 50 === min`, banked MPC-8). Every colour cell reproduces in this seat's own pipeline to the thousandth. |
| **MISS-2** (coarse-pointer: bare 22.5px track; 44px halo pointer-inert) + "baseline correction to KF-SST-30" | asserted, MINOR | silent | **ADMITTED as MPC-32; the KF-SST-30 arm re-ruled a PORTABILITY GUARD, not a correction** | All dist/git facts seat-verified (instruments above). KF-SST-30 is stamped **7.0.0** and is TRUE at its own kf-lane pin — the utility carries `pointer-events: none` at v7 verbatim. What reader-1 found is that the mechanism is **epoch-local**: 4.0.0 utility = `auto` (the neutering lives ONLY in the slider's scoped override), 7.0.0 = utility-level `none` (generalized, with the producer's own prose conceding the WCAG-readback framing), 8.0.0 = ghost retired + a real `--slider-target-floor` coarse floor (seat git-reads, all three). KF-SST-30's verdict stands at its pin; its mechanism cell must not travel across pins — recorded here as the guard, relayed via the standing glass BH relay the kf row already rides. The **8.0.0-cures-it** cell is new and unbooked (registry sweep 0). |
| **MISS-1 (reader-2)** (`valueCommit` shipped at the pin, unconsumed; two siblings wire it) | silent | asserted, MINOR | **ADMITTED as MPC-30** | Pinned `Slider.vue.d.ts:20` declares `valueCommit` (seat-read); GlassTimeline.vue:74 + ConvergenceTimeline.vue:81 wire `@value-commit` (seat grep); the morph directory has **0** occurrences (seat grep). This leaf consumes `<Slider>` DIRECTLY, so fr-SliderControl R-6's wrapper-narrowing (which destroys the event for that chassis's consumers) does not apply — nothing is in the way, and R-7's banked doctrine ("the settle event is the cure") is the precedent. Registry-clean for this component. |
| **D-14's `aria-hidden` leg** (a leg pass 1 left undispositioned) | KILLED, three mechanisms | silent | **K-11 — kill RATIFIED** | Seat-read reka bytes: `role: "option"` is SelectItem.js's sole role; ARIA 1.2 marks `option` Children-Presentational (banked precedent: kf-CopyButton killed-register, re-ratified kf-StartingStyleTarget) — the `<svg>` is pruned regardless; a `<title>`-less, text-less `<svg>` contributes `""` to name-from-contents anyway; and typeahead reads `textValue ?? textContent?.trim()` (useTypeahead.js:14, seat-read) — nothing the omission would expose is ever exposed. MPC-26's frame leg and K-8 stand untouched. |
| **L-D12's second arm** ("aria-label lands twice — two accessible names") | silent | KILLED | **K-12 — kill RATIFIED** | Seat grep: `role: "slider"` is minted ONLY in reka's SliderThumbImpl.js; SliderRoot/SliderImpl render role-less generics, and `aria-label` on a role-less `span` joins no accname computation. Exactly one name reaches the tree — the mechanically-true fallthrough is consequentially inert. S-1's placement superlative is what survives. |
| **D-6/C-4's failure computation** (`Number("abc")` → NaN → 50) | silent | mechanism corrected | **K-13 — mechanism KILLED, outcome intact** | HTML value-sanitization for `input[type=number]`: non-numeric text leaves the `value` IDL attribute as the **empty string** (`validity.badInput` true) — the handler at :13 never sees `"abc"`; the evaluated expression is `Number("") || 50` = falsiness on `0`, not NaN. Adopted into MPC-12's cure text so no repair encodes a NaN guard that guards nothing; the desync verdict (FMD-25 fold) is untouched — the `||`-swallows-0 idiom (MPC-26's C-16 leg) is what actually fires. |
| **D-8's dark-arm UNPROVEN label** | "new kill" (killed as classification) | silent | **K-14 — classification-kill ratified; NOVELTY CORRECTED** | The bank's MPC-6 had already discharged the caveat via FMD-19's cells (1.40-not-1.53 drift note). Reader-1's genuine contribution is the independent reproduction (dark border 1.40:1 vs fill / 1.03:1 vs card, own pipeline) — adopted as corroboration, not a fresh discharge. The lesson survives: the cell was one token-hop from decidable and the axis wrongly sent it to SS-13. |
| **MISS-2 (reader-2)** (unrounded IEEE doubles in `d`; ~1.67× text inflation) | silent | asserted, INFO, fold-flagged | **MPC-33 — FACET rider, not a fresh row** | Seat-read easings.ts:115-127: `${x},${y}` with no quantisation, beside `generateCurveSVGPath`'s `toFixed(3)` nine lines up. Same module, opposite polarity (no rounding vs over-fine rounding), same F.W3 sweep — folds onto banked fr-EasingCurvePreview **SAMPLE**'s FACET decision as a second input (ONE `d`-precision policy covers both builders). Reader-2's own fold-if-module-scoped instinct is the correct booking. |
| **MISS-3 (reader-2)** (resting-state divergence: glyphs only-in-popover vs EasingPicker's persistent grid) | silent | asserted, INFO | **MPC-34 — ADMITTED as a FORK-consolidation sharpener** | The unbooked *consequence* of ratified K-4: SelectValue renders a text node by construction (seat-read reka bytes), so the curve affordance structurally cannot reach the trigger's resting state, while the census's fourth fork shows six glyphs permanently. Divergence of behaviour, not merely duplication — sharpens banked MPC-17/MPC-25 → FMD-32/FORK. |
| **MPC-3 tier label** | BLOCKER (fold) | MAJOR | **Banked tier stands — BLOCKER-tier fleet fold** | A fold's tier lives with the banked identity (B-2/FMD-3), not with a re-reader's label; no re-adjudication of an adjudicated row by drift. |
| **MPC-7 cure-completeness rider** (title's `--font-serif` is inert) | asserted, fold-by-reference | silent | **RATIFIED — fold correct, consequence adopted** | Seat-read: bridges.css:68 + scheme-motion.css:43 resolve `--font-serif` to Plus Jakarta Sans. The identity is banked EXACTLY where reader-1 pointed: fr-FourierMorphDemo **K-9** / FR-AH-2 / FR-AH-11 (seat-read verbatim) — no new mechanism row. The NEW consequence amends MPC-7's cure: after its three token substitutions the title's only surviving differentiation from the description is +0.25rem, because the declared face delta was always a no-op — the F.W4 edit must decide the title's voice, not just its size. |
| **MPC-1 arithmetic** | corrected (128.33 not 132.33) | silent | **Evidence-cell correction ADOPTED** | `cartoon-surface` `border-width: 2px` seat-verified in the installed dist; under preflight border-box the card content budget loses 4px both edges' worth — the axis's derivation was 4px too generous; direction unaffected, deficit worse. Rider on MPC-1's banked fold (FMD-4). |
| **MPC-16 / MPC-21 provenance** | v8 self-cure verified / — | — / both-baseline delete-the-prop verified | **ADOPTED** | Seat git-reads: `v8.0.0` `cartoon-surface` = three declarations, no hover/translate/transition (MPC-16's F.W1 self-cure now tag-verified, not just claimed); 4.0.0 chunk computes `variant ?? "standard"` with empty-string variant classes and v8 authors every non-spectrum leg `:not([data-variant="spectrum"])` — MPC-21's delete-don't-rename holds at both ends. |

## Adjudicated defect roster

Line anchors are the fourier working tree over HEAD `cd26c65`; re-resolve before executing any cure. Folds carry the banked row's identity — original ids for life; nothing here re-books a banked defect. Rows unchanged from pass 1 are carried in compressed form; ► marks a pass-2 touch. Full pass-1 prose: git `dd218c3a`.

### BLOCKER tier (all folds — identity preserved)

| id | claim (compressed) | terminal disposition |
|---|---|---|
| **MPC-1 · D-1** | Misaligned breakpoint systems (640-grid vs 768-root-font) starve the duration slider to ~0px across 640-767px; `overflow-x:hidden` clips (1.4.10 Reflow arm). ► Pass-2 correction: true card content budget at vw=640 is **128.33px** (the axis omitted `cartoon-surface`'s 2×2px border under border-box) — deficit 4px worse, direction unchanged. | **FOLD → fr-FourierMorphDemo FMD-4 · F.W4**; band edges/clip counts → SS-13. |
| **MPC-2 · C-2 ⊕ L-D16** | Easing catalog cannot resolve at the value.js bump: bare-root specifiers, no `"."` export at 4.0.0, `timingFunctions` absent from every entry point. | **FOLD → fr-EasingPicker L/B-1 + C/B-1 · F.W1** (lockstep tri-package bump per MPC-14, never a specifier rewrite). |
| **MPC-3 · D-4 ⊕ L-D2 ⊕ C-1** | The `--slider-scrub-*` retint block (:205-211) is dead; `sliderColor`, the `--track-color` feed (:29), and the parent's red/pink/red accents deliver zero pixels; every card paints `var(--primary)` at 88%. Census **23/7** (seat-recounted a FOURTH time this pass; both readers concordant). | **FOLD → fr-BasisSelector B-2 / FMD-3 · F.W1 (execution F.W3/W4).** Cure AMENDED by MPC-10; **sequencing hardened by MPC-31: lands in one cut with MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22.** ► Tier label drift (reader-2 "MAJOR") ruled against — banked tier stands. |

### MAJOR tier

| id | claim (compressed) | terminal disposition |
|---|---|---|
| **MPC-4 · D-2 ⊕ D-7 ⊕ C-15 ⊕ L-D12** | Both `<label>`s inert (:8/:35 — no `for`, wrap nothing; the input is a *sibling*, no implicit association); the number input's attribute set (:10-18) carries no name; the SelectTrigger (:37) is named by its own value. ► Both readers re-verified; cure surfaces (`./labeled-field`, `./number-field`) confirmed live at 4.0.0 AND v8 (70-subpath parse, seat-run). | **FOLD → FMD-14 · F.W4.** D-7's clone arm stays killed (K-4); L-D12's second arm newly killed (K-12). |
| **MPC-5 · C-3** | 8 of 22 presets materially change curve shape at the bump (max Δ 0.192 `ease-out-circ`); reader-2 reproduced the table to four significant figures — the row's THIRD independent derivation. | **FOLD → fr-EasingPicker CC-2 + library-band RD-5 (RULED RESTORE) · F.W1**; acceptance re-runs the sampling. |
| **MPC-6 · D-8** | Number-field border 1.36:1 vs own fill, fill ≈1.02:1 vs card — 1.4.11 fail, no other cue; spinner suppression removes the last native one. ► Dark arm now TWICE independently computed (1.40/1.03, reader-1's own pipeline concordant with FMD-19's drift note). | **FOLD → FMD-19 · F.W4.** UNPROVEN classification killed at K-14. |
| **MPC-7 · D-9** | Type scale foreign and inverted: 18 → 14 → 16px top-down, tertiary label 500 / input 600 heaviest; φ ladder unused; no project override (falsifier 0). ► **Cure-completeness rider adopted**: the title's second declared distinction, `font-family: var(--font-serif)` (:121), is INERT — resolves to the sans register (fold-by-reference → FR-AH-2/FR-AH-11/FMD K-9, identity NOT re-booked). After the three token substitutions the title's only differentiation is +0.25rem; the F.W4 edit must decide the title's voice explicitly. | **MAJOR · books HERE → F.W4.** |
| **MPC-8 · D-10** | Slider domain 89% dead space; Morph boots AT the floor (min=50=default): zero-extent range, invisible thumb, no position indicator — an empty grey capsule. `marks` survives at v7 :23 AND v8 :28. | **MAJOR · books HERE → F.W4** — re-domain toward the used band or add `marks`. ► **MPC-31 makes this row the witnessability precondition for the MPC-3/MPC-10 cure — pulled into the one-cut sequencing.** |
| **MPC-9 · D-11** | Number-field focus indicator vanishes under `forced-colors: active` (`outline: none` :177; border/box-shadow-only focus :188-191); glass's antidote is a closed allow-list a bespoke scoped class cannot join (seat-read verbatim, re-confirmed pass 2). | **MAJOR · books HERE → F.W4** (dies structurally in the `./number-field` adoption). Slider half glass-owned → RELAY I-20, not re-booked. |
| **MPC-10 · NEW (pass 1), CURE-RIDER** | The banked B-2/FMD-3 cure is not visually neutral: rename-only ⇒ net α 0.264 ⇒ fill drops **11.51:1 → 1.51:1** vs its own track (dark 5.32 → 1.42) — a 1.4.11 regression on the `standard` recipe's only affordance, shipped under a green token-sweep gate. Corrected cure (drop the 30%/45% wrappers; `--track-color` full-strength; glass's 88% sole dilution) = **4.00:1 light / 3.71:1 dark PASS**. ► Pass 2: BOTH readers reproduced every cell to the hundredth from independently written pipelines; this seat's third pipeline agrees to the thousandth (4.004). | **MAJOR · CURE-RIDER amending B-2/FMD-3 · F.W1.** B-2's acceptance gains a contrast leg in the breaking direction. Hairline residue → SS-13. ► Payoff priced at MPC-31. |
| **MPC-11 · L-D3a** | The duration row re-implements `ui/SliderControl.vue` and loses on four axes; the chassis carries the SAME dead tokens (5 of the 23). | **FOLD → FMD-12 + fr-BasisSelector M-6 · F.W3.** Substitution recipe stays killed (K-6). |

### MINOR tier

| id | claim (compressed) | terminal disposition |
|---|---|---|
| **MPC-12 · D-6 ⊕ L-D1 ⊕ C-4** | Uncontrolled `:value`+`@change` number input desyncs at clamp fixed points; boot-reachable (Morph boots ON the floor); parent's `Object.is` no-op semantics + `shouldUpdateComponent` short-circuit are the mechanism. ► **Mechanism correction adopted (K-13)**: the DOM never delivers `"abc"` — sanitization yields `""`, and `0` falsiness (not NaN) reaches the fallback; the banked force-write cure must not grow a NaN guard. | **FOLD → FMD-25 (MINOR-as-banked) · F.W4.** Reader-2's MAJOR (echoing the axes) preserved as dissent, second recording. |
| **MPC-13 · C-5** | HEAD does not typecheck (`glass-scrubber` ∉ pinned vocabulary; `build` = `vue-tsc -b`); the WT one-liner is the cosmetic half. ► WT state re-confirmed this pass (same one hunk; porcelain unchanged). | **MINOR · F.W1** — sequencing rider at cure-order weight: the flip lands WITH the token cure, never before (now subsumed into MPC-31's one-cut). Bank substrate-cell correction (pass 1) stands. |
| **MPC-14 · C-13** | keyframes 4.3.0 hard-deps value.js `^0.13.0`. | **FOLD → fr-AnimationControls C-1 (via fr-EasingPicker C/B-2) · F.W1.** |
| **MPC-15 · D-13** | Heading skip h1 → h3, no h2 on the route. | **FOLD → FMD-31 · F.W4.** |
| **MPC-16 · D-21** | Hover-lift on a non-interactive card (`cartoon-surface` −1px lift at 4.0.0). ► **Self-cure now TAG-VERIFIED**: `v8.0.0` `cartoon-surface` = `position: relative; border-width: 2px; box-shadow: var(--shadow-cartoon-md)` — no hover, no translate, no transition (seat git-read :256-261). | **FOLD → FR-EQC-5 + fr-ContourPreview row 23 · F.W1 self-cures.** |
| **MPC-17 · D-22 ⊕ C-9 ⊕ L-D14** | `easingCurvePath` uncached in the 22-item `v-for` (550 evaluations/open) while `_svgCache` sits 15 lines up; per-open derating confirmed (no forceMount). | **FOLD → FMD-32 → fr-EasingCurvePreview BARREL + FORK · F.W3.** ► MPC-33 and MPC-34 ride the same consolidation. |
| **MPC-18 · L-D9 ⊕ L-D10 ⊕ L-D5 ⊕ C-12** | `updateField` dead + the module's only `as any`; `syncWith` returns no disposer + wasted `deep: true`; `arr[0]` vs `number[] | undefined`. | **FOLD → FMD-26 (FR-AH-37 batch) + FMD-33 · F.W4.** Leak framing stays killed (K-9; reader-2 re-ratified at the callsite). |
| **MPC-19 · L-D8 ⊕ C-8 ⊕ C-20** | Constants through a composable barrel, three surfaces deep; root-barrel `useClipboard` producer-forced (no subpath at 4.0.0 — reader-2 re-verified). | **FOLD → FMD-34 → BARREL · F.W3**; `./clipboard` subpath ask rides the glass BH relay. |
| **MPC-20 · C-14** | Both emits terminate in `createTweenAnimation`'s hardcoded `timingFunction:"linear"` + `useWAAPI:false` — the leaf emits correctly; the seam discards. | **FOLD → FMD-8 · F.W4.** |
| **MPC-21 · NEW (pass 1)** | `variant="standard"` redundant at BOTH baselines — deleting the prop at all 9 sites is a no-op today and correct at 8.0.0; strictly better than every axis's rename. ► Both ends re-verified this pass (4.0.0 `?? "standard"` + empty variant classes; v8 `:not([data-variant="spectrum"])` :145/:153). | **MINOR / repair-value AMENDMENT to FMD-11 + D-5's cure · F.W1.** |
| **MPC-22 · NEW (pass 1)** | Focus tint hard-codes `var(--accent-red)` (:189-190) beside the per-instance `--track-color` slider — invisible today, guaranteed two-colour Morph card the moment MPC-3/MPC-10 lands. | **MINOR · rides the one-cut → F.W1** (one token: `var(--track-color, var(--accent-red))`). |
| **MPC-23 · NEW (pass 1)** | 2.25× baked anisotropy in every dropdown preview (`x = 2 + t·36; y = 18 − v·16` — seat re-read verbatim this pass); the compressed axis is the discriminating one; the undistorted sampler sits nine lines up. | **MINOR · books HERE → F.W3** (dies in the FMD-32 consolidation; constant square frame). Perceptibility + K-GEOM instrument → SS-13. |
| **MPC-24 · NEW (pass 1)** | The compound: in MPC-1's band the slider is 0px, steppers author-removed, `.num-input` (~31-34.5px) outside the coarse floor — no usable duration control at all on a coarse pointer in that band. ► MPC-32 extends the coarse-pointer story OUTSIDE the band (the surviving control is a 22.5px strip). | **MINOR · books HERE → F.W4** (rides FMD-4 + FMD-12). |
| **MPC-25 · D-12** | 22 options, one flat list, eight families; SelectGroup/SelectLabel/`description` slot all shipped and unused (re-verified live at v8 exports too). | **MINOR · books HERE → F.W4.** |
| **MPC-26 · the tail** (D-14 frame · D-15..D-20 · D-23 · L-D6/L-D7 · C-6 · C-10/C-11 · C-16/C-17) | Source-verified batch carried whole: no reference frame; two label geometries; off-grid literals; `:focus` not `:focus-visible`; no error/placeholder/disabled surface; unaligned rows; non-parallel prose; unbounded strings; no step quantisation; [50,800,10] quadruplicated; `easing` typed bare `string` (the typing leg would make C-6's `String($event)` laundering a compile error). ► D-14's `aria-hidden` leg now dispositioned: KILLED (K-11). | **MINOR/INFO batch · F.W4.** |
| **MPC-30 · NEW (reader-2 MISS-1, pass 2)** | **The producer ships this component's commit-semantics cure at the pin and the component consumes none of it.** Pinned d.ts declares `valueCommit: (payload: number[]) => any` (:20, seat-read); the slider commits per reka step through `v-model` (:21-30 → :99-102 → :93-96) while the number field commits on change (:13) — the split MPC-26 books — and `@value-commit` is the settle event that unifies them without the hand-rolled force-write. In-tree precedent at the same pin: GlassTimeline.vue:74 (+ authored rationale :16) and ConvergenceTimeline.vue:81 (seat grep). Direct `<Slider>` consumption ⇒ fr-SliderControl R-6's wrapper-narrowing does not apply; R-7's "the settle event is the cure" is the banked doctrine. Registry-clean for this component (seat sweep). | **MINOR · books HERE → F.W4**, as cure-input to FMD-25/MPC-12 and MPC-26's cadence leg. Live-drag feedback question (GlassTimeline pairs `@pointerdown` + `@value-commit` to keep both) → SS-13. |
| **MPC-31 · NEW (reader-1 MISS-1, pass 2 — consequence CORRECTED by this seat)** | **The corrected cure's perceptual payoff is 1.203:1, and it lands on the one card whose bar is zero-extent at boot.** Seat's own pipeline: corrected-cure fills `#df3d3c` (red, 4.004:1 vs track) and `#db5799` (pink, 3.327:1) — **1.203:1 apart**, hue-only, on bars ≤90px; cards 1 and 3 are byte-identically `var(--accent-red)` (FourierMorphDemo.vue:31/:51), so the pink Morph bar carries the grid's ENTIRE discrimination content — and it has zero extent at boot (MPC-8). **Seat correction**: the settle cards boot at 150ms (13.3% extent, seat-read DEFAULT_MORPH_CONFIG) so the cured boot render IS visibly changed (two red slivers) — reader-1's "pixel-unchanged" is false; what is unchanged is the *information*: zero phase-discrimination before and after. Answers the static half of SS-13 item #2 by arithmetic. | **MINOR · CONJUNCTION rider · F.W1 sequencing law**: MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 land as ONE CUT, and the acceptance witness for the colour scheme is taken at a non-boot state (or after MPC-8's re-domain). Whether 1.203:1 hue-only reads as phase-coding at all → SS-13. |
| **MPC-32 · NEW (reader-1 MISS-2, pass 2)** | **At the pin the `standard` slider's coarse-pointer target is the bare track — 22.5px below 768px (1.25rem × 18px root, seat-verified), 20px above — and the 44px halo glass composes onto the thumb is pointer-inert** (`.slider-thumb.touch-hit-area::before{pointer-events:none}`, slider-scoped at 4.0.0; the utility itself declares `pointer-events: auto` at this pin — seat-read verbatim). The package's only coarse floor is a closed three-selector allow-list the slider is not on (seat-read). Both figures sit under the producer's own later-declared 24px floor. Compounds MPC-24 outside its band: on touch, the "surviving control" is a 22.5px strip carrying 76 stops. **8.0.0 CURES it**: `--slider-target-floor` coarse block (styles.css:85-93, seat git-read) + the decorative ghost retired (thumb class carries no `touch-hit-area` at v8, seat-read) — an uplift cell no bank row carries (registry sweep 0). Honest bound: WCAG 2.5.8's spacing exception plausibly rescues strict conformance; stated against the producer's own floor and acquisition ergonomics. | **MINOR · books HERE → F.W1** (the uplift cell joins the F.W1 benefit ledger) **+ F.W4** site note; **KF-SST-30 portability guard** recorded (ruling above) and rides the standing glass BH relay alongside kf-StartingStyleTarget's existing routing; measured acquisition → SS-13. |

### INFO tier

| id | claim (compressed) | disposition |
|---|---|---|
| **MPC-27 · NEW (pass 1)** | `class="w-full"` on the SelectTrigger duplicates the recipe base; the trigger's sole attribute is inert and the one that belongs there is `aria-label`. | **INFO · F.W4** (dies in the FMD-14 edit). |
| **MPC-28 · NEW (pass 1)** | The pinned focus ring degenerates to a ~2px sliver where MPC-1 zeroes the track. | **INFO · rider on FMD-4 · SS-13.** |
| **MPC-29 · NEW (pass 1)** | `/morph`'s only automated gate is one screenshot row — which catches MPC-5's drift iff baseline regeneration straddles the bump. | **INFO · F.W1 sequencing note.** |
| **MPC-33 · NEW (reader-2 MISS-2, pass 2)** | `easingCurvePath` emits unrounded IEEE doubles into every `d` (~11.2KB → 6.7KB at `toFixed(2)`, reader-measured ~1.67×; 17-significant-digit coordinates for a 20px glyph), while the sibling builder nine lines up rounds to three decimals (both seat-read verbatim). Opposite defect, same module, as banked fr-EasingCurvePreview SAMPLE (over-fine `toFixed(3)`). | **INFO · FACET rider on fr-EasingCurvePreview SAMPLE · F.W3** — ONE `d`-precision decision for the module's two builders; never booked twice. |
| **MPC-34 · NEW (reader-2 MISS-3, pass 2)** | The 22 curve glyphs are visible only while the popover is open — the resting trigger is a text node **by construction** (ratified K-4's mechanism, seat-read reka bytes) — while the in-tree fourth fork (EasingPicker) shows six glyphs permanently: the forks diverge in behaviour, not just duplication. | **INFO · rider sharpening FMD-32/FORK · F.W3.** Whitespace-collapse residue in the trigger text → SS-13. |

## Killed-claims register

Refutations ruled across both passes; original ids for life. A killed claim is not re-mintable without new evidence. K-1..K-10 carried from pass 1 (full prose at `dd218c3a`); ► marks pass-2 deepening.

1. **K-1 · D-3** (BLOCKER "no visible keyboard focus indicator at the pin") — KILLED by the pin's own bytes: the non-spectrum `:focus-within .slider-track` rule EXISTS at 4.0.0 (three independent extractions now: pass-1 seat, both pass-2 readers). ► Deepened: the consequential claim inverts truth in BOTH directions — `v7.0.0` Slider.vue :410-417 carries the identical rule with the authored "rings the TRACK" note; `v8.0.0` styles.css *narrows* it to `:has(:focus-visible)` with authored rationale (kills pointer-focus false-positives). The uplift removes a false positive; it never supplies a missing indicator.
2. **K-2 · D-5/P-2** ("7.0.0 vocabulary is `scrubber|spectrum`") — KILLED at its named baseline (`v7.0.0:types.ts:5` = `"standard" | "spectrum"`); the substance is an 8.0.0 fact already banked (FMD-11 / fr-ConvergenceTimeline K-8 / fr-BasisSelector M-7). ► Now independently re-derived by BOTH pass-2 readers — the corpus's **fifth** instance of the K-12/SR-2 baseline-law failure; MPC-21 supersedes the rename cure regardless.
3. **K-3 · D-24's `valueText` leg + D-3's cited v7 styles.css** — KILLED: the file does not exist at the tag; `valueText` first appears at v8 :35. `marks` survives at both tags and rides MPC-8's cure. ► Re-ratified by reader-2's own git runs.
4. **K-4 · D-7's clone arm / L §4.2** ("the selected item's subtree — including the SVG — is projected into the trigger") — KILLED in reka's own bytes: SelectItemText registers a `textContent` STRING; SelectValue renders `createTextVNode(toDisplayString(…))`. Two axes independently invented the same non-existent mechanism. ► Both readers re-read the bytes; MPC-34 books the true consequence.
5. **K-5 · the blast-radius figures** (D "10/5" · L "21/7" · C "23/6") — ALL KILLED; truth **23 declarations / 7 files**, now **four** concordant recounts. F.W1 acceptance = token-family sweep to zero + MPC-10's contrast leg, never a per-file count.
6. **K-6 · L §9.2's SliderControl substitution recipe** — KILLED three ways in the chassis's own source (`formatValue` disables the numeric contract; the fixed-point trap survives; the geometry is a column). Duplication survives as MPC-11's fold.
7. **K-7 · L-D8/C-8's eager-coupling narrative** — KILLED-in-part: the engine sits behind `getAnimationCtor()`'s real dynamic boundary; the true eager edge is banked (FMD-20/FR-AH-7). Residue folds (MPC-19).
8. **K-8 · D-14's "shaved" half · L-D13 · C-18** — SPENT ROW (adjudicated FMD m-5/K-4 + WRONGPAD); not re-mintable.
9. **K-9 · L-D10's leak framing** — KILLED at the callsite: one synchronous setup invocation, effect-scope auto-disposal. Residue banked FMD-33.
10. **K-10 · pass-1 reader-2's "registry-clean" cell on the desync row** — KILLED by identity: FMD-25's clamp funnel includes `emitDuration`; a literal-string grep is not an identity sweep.
11. **K-11 · D-14's `aria-hidden` leg (pass 2, NEW)** — KILLED on three independent mechanisms, all seat-verified: (i) `role="option"` is Children-Presentational — every descendant pruned (banked precedent kf-CopyButton, re-ratified kf-StartingStyleTarget); (ii) a `<title>`-less, role-less, text-less `<svg>` contributes `""` to any name computation anyway; (iii) typeahead reads `textValue ?? textContent?.trim()` — the SVG feeds no search key. Nothing the omission would expose is ever exposed. MPC-26's frame leg and K-8 stand.
12. **K-12 · L-D12's second arm (pass 2, NEW)** — "aria-label lands twice ⇒ two names" KILLED: `role: "slider"` is minted only in SliderThumbImpl (seat grep); the root's fallthrough copy sits on a role-less generic and joins no accname computation. Mechanically true, consequentially inert; exactly one name reaches the tree — which is S-1's superlative, not a defect.
13. **K-13 · D-6/C-4's failure computation (pass 2, NEW)** — `Number("abc")` KILLED as mechanism (sanitization delivers `""`; the fallback fires on `0`-falsiness, the C-16/L-D7 idiom); the outcome and the FMD-25 fold are untouched. Recorded so no repair encodes a NaN guard that guards nothing.
14. **K-14 · D-8's dark-arm `UNPROVEN-NEEDS-LIVE` label (pass 2, NEW)** — killed as a *classification*: the chase is one token hop, fully static (dark border 1.40:1 vs fill / 1.03:1 vs card — reader-1's independent pipeline, concordant with FMD-19's banked drift note). Novelty corrected: the bank's MPC-6 had already discharged the caveat; the axis's real error was routing a decidable cell to SS-13.

## Superlatives — ratified, with provenance

S-1..S-9 carried from pass 1; ► marks pass-2 provenance additions.

1. **S-1 — the `aria-label` placement (NARROWED per FMD-15)**: placed where reka mints `role="slider"` (the pinned chunk re-projects it onto each thumb). ► **Vindicated at v8**: the producer now forwards `aria-label`/`aria-labelledby`/`aria-describedby`/`aria-errormessage` onto every thumb (seat git-read :363-369) and ships a DEV-only warn for exactly the omission this author avoided ("a role=slider thumb nameless to a screen reader") — the consumer got there two majors before the producer's diagnostic. The narrowing stands: the byte-identical string across three cards remains banked MAJOR FMD-15.
2. **S-2 — one clamp funnel, two input paths, the right adapter**: clamp once (:94), emit once (:95), writable computed not mirror-ref-plus-watch — the desync is a DOM defect beside an exactly-correct reactive adapter.
3. **S-3 — reduced-motion-safe authored surface** (one `border-color` transition, PRM-allow-listed), with pass-1's rendered-tree caveat intact.
4. **S-4 — the iteration set derives from the object being indexed** (`Object.keys(EASING_PRESETS)`; 22/22 resolve at both majors; total fallback) — doing work `noUncheckedIndexedAccess` is not.
5. **S-5 — complete spinner suppression across engines** (`-moz-appearance` + both webkit pseudos + the forgotten `margin: 0`); its other edge priced at MPC-24.
6. **S-6 — zero teardown surface**: no hook, watch, timer, listener, ref, or subscription; the producer disposes its own machinery. In the FR-AH-8/-9/-20/-21 subtree, a leaf with nothing to leak.
7. **S-7 — genuinely presentational, zero API reach**: seat/reader closure greps concordant (0 hits for api/store/fetch across the morph closure); R6-8's client↔operation coupling cannot touch it; R5-7's primary class does not apply (the only `v-for` is a component callsite).
8. **S-8 — corpus measurement fidelity, now thrice-tested**: ► both pass-2 readers re-derived every load-bearing number with independent instruments and found **zero arithmetic errors** (eight contrast cells to the hundredth; the 23/7 census twice more; C-3's table to four significant figures; the easing-geometry constants byte-exact) — and this seat's third-generation pipeline reproduced the headline cells to the thousandth. Every kill in this record is inference, baseline, identity, or mechanism; not one is measurement. L §7's candidates-checked-and-cleared page remains the fleet exemplar.
9. **S-9 — the substrate documents itself against its auditors**: the pinned slider d.ts pre-answers the corpus's most confident BLOCKER. ► Deepened: `v7.0.0` Slider.vue and `v8.0.0` styles.css each carry authored rationale for their focus predicate — the 8.0.0 comment explains the `:focus-within` → `:has(:focus-visible)` switch by the exact false-positive it cured. A producer whose comments falsify consumer-side audits, twice.
10. **S-10 (pass 2, NEW — reader-2, seat-ratified) — subpath-clean consumption, uplift-durable**: imports only `@mkbabb/glass-ui/select` + `/slider`, zero reka imports, zero local copies; and the entire import surface survives v8 (70 subpaths; all five Select members + Slider live — seat parse). Neither the corpus nor pass 1 had verified the 8.0.0 durability.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **MPC-1/FMD-4** — rendered `.duration-slider-track` width at 640/700/768/960; clip counts (the 128.33px corrected budget sharpens, does not measure).
2. **MPC-10 ⊕ MPC-31** — the corrected cure in paint: three cards at 1280px, light + dark; hairline contributions of `--slider-range-blur`/`--glass-material-rim`; and whether a 1.203:1 hue-only delta on ≤90px bars reads as phase-coding at all (the arithmetic half is now closed statically — 4.004/3.327/1.203, thrice-derived).
3. **MPC-8/D-10** — whether the zero-extent range paints a rim seam or nothing at boot.
4. **MPC-23** — whether 2.25× anisotropy defeats preset discrimination (Ease In Quad vs Cubic vs Sine); pair with the K-GEOM `--ui-glyph` instrument.
5. **MPC-24 ⊕ MPC-32** — computed `.num-input` block size on device; measured acquisition/mis-tap rate on the 22.5px (<768) / 20px (≥768) track vs the producer's own 24px v8 floor; any dead zone from the pointer-inert halo.
6. **MPC-28** — perceptibility of the ~2px focus-ring sliver at track width 0.
7. **MPC-30** — whether `@value-commit` alongside `v-model` preserves live-drag preview feedback or introduces perceptible commit lag (GlassTimeline's `@pointerdown` pairing is the in-tree pattern to test).
8. **C-7** — 22 curves' distinguishability at the Morph card's 50ms (~3-frame) default.
9. **D-19** — the widths at which description wrap-counts knock the Duration rows out of alignment.
10. **MPC-34 residue** — whether the trigger's text node inherits template inter-element whitespace through white-space collapsing.
11. **Provenance rider** — producer-8.0.0 facts are tag/HEAD reads at `8a96868d`; re-baseline at F.W1 per banked SR-2/FM-2.

## Verdict

**ADJUDICATED, PASS 2 (M-25 agglomeration input — stamps nothing): the bank HOLDS under hostile re-read — 0 of 29 rows overturned, 0 of 10 kills revived, and every load-bearing number in the corpus now reproduces across three independently written instrument sets. Pass 2 adds 5 verified reader-missed finds (→ 34 rows), 4 new kills (→ 14), 1 overturned reader consequence (the settle cards do NOT boot at the floor — the cured boot grid changes pixels but carries zero discrimination), 1 cross-lane portability guard (KF-SST-30 is version-true at 7.0.0; its mechanism is epoch-local: scoped-override at 4.0.0, utility-level at 7.0.0, cured at 8.0.0), and 3 evidence-cell corrections. The operative headline is now a sequencing law: MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 land as ONE CUT, because the corrected BLOCKER cure's entire communicative payoff is a 1.203:1 pink bar that has zero extent at boot — a repair whose witness does not exist until the domain row lands with it.** The leaf remains a well-mannered prop→emit component whose authored surface earns real superlatives; its debt is design-local (F.W4) and fleet-inherited (F.W1/F.W3), and every BLOCKER-shaped claim against it has now died twice against the pin's own bytes.

— end of pass-2 apotheosis. The three challenge files, both pass-2 reader returns, and the pass-1 record (git `dd218c3a`) remain immutable beside this file per E-1/E-3.
