# AdminUsersPanel — r3 DELTA adjudication (worker-F)

**Seat**: worker-F (r3 delta) · model receipt: `claude-fable-5` (Fable 5), observed directly by this seat in its own runtime, not inherited from the spawn label.
**Date**: 2026-07-28 · **HEAD**: `c654824e` (branch tranche-u) · **Merge target**: `registry/adjudicated/AdminUsersPanel.md` (r2, arbiter-F, 2026-07-27) — per L-14 this file is the worker's delta record, NOT an overwrite of the r2 apotheosis; agglomeration belongs to the arbiter/root.

## Corpus census (L-9 discipline — my own numbers, scoped)

The brief said "25 defect accusations (6 BLOCKER)". Measured against `registry/harvest/area-palettes.json` (runId `wf_22b7a7b7-97b`): **eight** fresh seats carry an AdminUsersPanel subject — idx 3 (CHALLENGE-L, 14), idx 6 (CHALLENGE-D pass-1/probe-D2, 19), idx 7 (CHALLENGE-C pass-1/probe-impl, 14), idx 22 (CHALLENGE-C pass-2/probe-C2, 22), idx 24 (L pass-3, 18), idx 25 (CHALLENGE-D pass-2/probe-D3, 22), idx 32 (CHALLENGE-L r3-prior, 15), idx 35 (CHALLENGE-D pass-3, 15) — **139 raw rows**, heavily self-carrying across passes. Deduped by identity they collapse to the cluster set ruled below; the **six fresh BLOCKER identities exactly match the brief's 6**. The "25" is reachable as latest-pass-per-axis (22 C + 15 D + 18 L minus carried duplicates); no verdict turns on it. r2's own corpus (idx 0, 11 rows) is excluded — already adjudicated.

**Environment disclosure (L-12)**: no browser probe was run by this seat. Dev server :9000 is UP but API-LESS; every challenger probe cited stubs its fixtures via `page.route` (signatures read: probe-D3f.mjs, probe-C2b-controls.mjs), so their recorded JSON cells (`probe-C2b-controls.json`, `probe-C2c-window.json`, `probe-D3.json`, `probe-D3b/c/d.json`, `probe-D3f.json`, `probe-C2-implementation.json` — all on disk, re-read this session) are believed as chromium/webkit cells where my static re-derivation independently reached the same mechanism. Per I-20 nothing here is transferred to real Safari. All PNG frames remain UNTRACKED (r2 gate G-8 still RED).

Arbiter-grade instruments this session (all re-runnable): full reads of AdminUsersPanel.vue (392 lines), useAdminUsers.ts (222), AdminPane.vue:15-135, usePalettePorts.ts:100-135+190-235, demo/palettes/api/admin-users.ts (whole), api/src/modules/admin/service/users.ts:215-250, AdminListSkeleton.vue (whole), vitest.config.ts (whole), eslint.config.js:195-303, e2e/smoke/admin/a11y-authed-admin.spec.ts:1-33 + fixtures/admin-populated.ts imports; scoped greps recorded inline below.

## The six fresh BLOCKER identities — all CONFIRMED

### Δ-1 · Prune's stated scope is a fabrication (D-1 ≡ C-2 ≡ L-19 ≡ D-1 pass-2 CF) — CONFIRMED · BLOCKER
Statically certain end-to-end, every link re-read at HEAD: panel :241 `emptyCount` over the `users` PROP; AdminPane.vue:28 binds `:users="pm.filteredAdminUsers.value"` (search-filtered, useAdminUsers.ts:29-34) while :33 binds `:total-users="pm.adminUsers.value.length"` (unfiltered); dialog copy :290-291 quotes `emptyCount` twice; api/admin-users.ts:69-71 `pruneEmptyUsers(token)` POSTs **no body**; the server (api/src/modules/admin/service/users.ts:228-249) runs `users.findEmptyUserSlugs()` — **whole collection, no scope argument** — then deletes users + sessions in a transaction. Compounding: useAdminUsers.ts:59 `listUsers(token, 50)` means even the unfiltered number is a ≤50-row page count (`res.total` discarded, :60). Probe cell (both engines, disk-verified probe-D3f.json): dialog "Prune 1 empty users?" → `prune_request.postData: null` → toolbar after: "Pruned 3 users". Inverse arm (probe-impl.json C2_filteredNoEmpty): Prune DISABLED (`:disabled="emptyCount === 0"`, :25) while 3 empty users exist server-side — the enablement is wrong in **both directions** from the same source. Riders confirmed at source: the title never pluralises (:290) while the description does (:291). In-wave cure (demo-only): the dialog may not state a number fabricated from a filtered client page — the scope statement comes from the executing authority's semantics ("every user with 0 palettes across the whole roster"), the button gates on authority not on the filtered page; the CORRECT cure (server preview `GET /admin/users/prune-empty` → `{count}` the dialog quotes) needs api/ and is BANKED (rider MT-AU2-API, re-trigger below).

### Δ-2 · Five of six mutations have no result channel; refusal renders as success (D-2 ≡ C-21 ≡ D-2 pass-2 CF; absorbs and ELEVATES r2 L-7's thrown-prune arm) — CONFIRMED · BLOCKER
useAdminUsers.ts — every mutation `catch → console.warn` and nothing else: :96-98 (feature), :107-109 (admin delete), :122-124 (admin delete user palette), :135-137 (delete user palettes), :147-149 (delete user). Only prune has a channel and it is broken twice: :186-198 `if (!token) return 0` AND `catch → return 0`, both flowing into panel :303-308 where `0` renders **"No empty users to prune"** — refusal, failure, and nothing-to-do are one sentence, in the success register, while :49-50 states the file's own opposite invariant for the load path. Probe cells (disk-verified): DELETE fulfilled 500 → dialog closes, rows unchanged, alertRoles 0, console.warn only (probe-D3.json P8); prune 500 → "No empty users to prune" beside a toolbar reading "· 2 empty" (probe-D2d). r2 booked the thrown-prune arm inside L-7's rescope; the delta corpus proves it is the **family** defect (5/6 actions), so it takes its own row and gate. Cure: the port owns a typed operation-result (`{ok:true, n} | {ok:false, reason}`) for all six actions, rendered in the error register already ratified at :51-62; `onPruneDone(count: number)` is the wrong signature for the family.

### Δ-3 · The destructive confirm double-fires inside the close window (C-15) — CONFIRMED · BLOCKER (severity note in dissent)
Statically certain mechanism, all three preconditions at HEAD: `onConfirm()` :283-286 fires `confirmAction.value?.()` then sets `confirmOpen=false` — the closure is **never cleared** (grep `confirmAction.value = null` → 0 hits); the footer confirm Button :175 has **no `:disabled`**; the dialog leaves by transition so the button stays mounted and hit-testable. Probe cells (disk-verified probe-C2b-controls.json, both engines byte-identical): double-click Prune → `prunePosts: ["POST /admin/users/prune-empty", "POST /admin/users/prune-empty"]`, `atSecondClick.elementText: "Prune"`, beat "Pruned 2 users" — ONE receipt for TWO executions; same shape on per-user DELETE (probe-C2-implementation.json N2); single-click control → exactly one request (K2a). Live window measured (probe-C2c-window.json): chromium lastLiveAtMs 271, webkit 159. Cure: single-fire by construction — clear the closure on fire, disable the confirm control while the action is in flight (composes with the r2 wave's shared `useConfirm`).

### Δ-4 · The expand race puts one user's palettes in another user's row, and the wrong-owner destruction is reachable (C-1 ≡ C-19 ≡ the C3-race rows) — CONFIRMED · BLOCKER
Statically certain: `toggleUserExpand` :352-365 writes `userPalettes.value = await pm.loadUserPalettes(slug)` with **no generation guard**; the collapse arm :353-356 returns early without cancelling or invalidating the in-flight request; `finally` clears `loadingUserPalettes` for whichever settles first; ONE shared `userPalettes` ref serves whatever row is open. :150 then binds the card's admin delete to the ROW's `user.slug` — so alpha's palette rendered in beta's open row wires its destruction to beta as owner (badge decrements the wrong user, useAdminUsers.ts:118-121). Probe cells (both engines): expand alpha (slow) → beta (fast) → alpha's payload lands in beta's open row (`RACE_REPRODUCED: true`); the destroying DELETE issued from beta's row hit `alpha-p-1`, betaBadge 2→1 alphaBadge 4→4 — both wrong, opposite directions; C-19's collapse-midflight gesture reproduces it after an EXPLICIT collapse. Cure: the r2 wave's `useAdminUserPalettes()` owns the slice keyed by generation (`if (slug !== expandedUserSlug.value) return` on settle), and the card's owner comes from the palette's own `userSlug`, never the row.

### Δ-5 · The keyboard focus ring renders zero pixels (D-3 ≡ D-33 second mechanism) — CONFIRMED · BLOCKER
Static mechanism certain at HEAD: the row carries `focus-visible:ring-2 focus-visible:ring-ring` (:82) — a box-shadow at the row's border box — while the wrapper :68 is `rounded-md border overflow-hidden`; the row fills the wrapper, so the 2px shadow falls entirely in the clipped region. Second mechanism (D-33): the only other focus-visible signal is `focus-visible:bg-accent/50` — a low-contrast wash. Probe cells (4 cells on disk, probe-D3b/c/d.json): WebKit light 1.034:1 / dark 1.166:1, Chromium light 1.024:1 / dark 1.202:1; changed-pixel bbox exactly equals the row interior (the ring contributed ZERO pixels); control native sibling button maxΔ 177/290. This is the ONLY keyboard path to a user's palettes (the file's own :70-77 comment) — WCAG 2.4.7 total loss on the primary disclosure. Cure: the ring moves to an element that owns its own paint bounds (ring inside via `ring-inset`, or the focus style on the wrapper) — one class-list edit, asserted by a probe gate, never by eye.

### Δ-6 · RTL fabricates identifiers (D-4 ≡ D-20 rider ≡ D-4 pass-2 CF) — CONFIRMED · BLOCKER
Static: the slug is split across two sibling spans :97-101 (`slugHead`/`slugTail`, :246-252) with **zero bidi isolation** — `grep -c '<bdi\|dir=' AdminUsersPanel.vue` → 0; the confirmation dialog quotes the raw unsplit slug :169, so row and dialog show **two identities for one account** under RTL. Probe cells: 4/4 long slugs render tail-left-of-head (`f9a-33an-extremely-long-…`), page bdi count 0; the count line reorders ("empty 3 · users 6"); visible without data in the tracked-shot claim — NOT relied on (shots untracked, G-8). Rider confirmed: `mr-1` physical margin on the Trash glyph :119 in a logical-properties row (D-20). Cure: the slug value renders inside one LTR-isolated span (`<bdi>` or `dir="ltr"` on the pill), the split stays a visual truncation inside it; `mr-1` → `me-1`. Law: VISUAL-CONSTITUTION.md:133/:154 as cited by the fleet — re-read not required, the DOM facts decide.

## Refiles into standing r2 rows (books ONCE — no new rows)

- **L-1 refile ×4 at BLOCKER (idx 3/24/32 + ledger L1819) → RESCOPED into r2 L-1 (MAJOR structural spine STANDS)**. Every cited line re-verified at HEAD (useAdminUsers.ts:14/:27/:95/:117/:132/:146; usePalettePorts.ts:123 + :197 + the `onPrune` bridge :121-124 region; AdminPane.vue:27/:134; panel :389, 5 members). The r3 corpus adds ONE genuinely new confirmed arm: **the remotePalettes divergence** — `onAdminDeleteUserPalette` :112-125 updates the panel copy + the owner badge but NEVER `deps.remotePalettes`, while sibling `onAdminDeletePalette` :101-110 filters it; the codebase itself asserts list overlap (`onFeaturePalette` :87 searches remotePalettes for a palette sourced from this panel's card). Consequence: Browse renders a deleted palette until reload. This is **staleness, not corruption** — MAJOR-grade — so the r2 BLOCKER-retirement is NOT overturned; the arm folds into the same cure (one module owning both lists makes the divergence unrepresentable). The cure ALIGNS with the r1 precedents as instructed: App.md's W-PICKER-2 ("the picker stops being a service" — the shared machine moves into a PROVIDED composable, defineExpose → 0) and ColorPicker.md L-8/D-16 (cross-layer coupling through a parent-held template ref, exposed-not-provided). Here: `useAdminUserPalettes()` PROVIDED at the composition layer; the port carries data+commands downward; the instance handle, all four `?.` swallows, and the type cycle delete together (r2 gates G-1/G-2 unchanged).
- **C-17 ≡ L-12 ≡ D-9 ≡ D3f `signed_out` cell → r2 L-5 (the standing wave BLOCKER)** — three more seats and one more probe cell confirm the auth-costumed-as-empty-roster row; nothing new to rule; corroboration recorded.
- **D-23 ≡ L-4(idx24) → r2 L-3** (dead `variant`); **L-3(idx24) ≡ L-2(idx3) ≡ L-22(idx32) → r2 L-2** (34-member port / three simultaneous wiring channels); **L-5(idx24/32) → r2 L-4** (two dialects); **L-6/L-7(idx24) ≡ D-28 ≡ D-14/L-17 → r2 L-6** (row-grammar forks + the skeleton's lie — re-verified: AdminListSkeleton.vue:13 draws the w-8 swatch no real row has); **L-6(idx3)/idx32 → r2 L-8** (one confirm home of three panels; the ConfirmDialog-preset rider stays GLASS-OWNED via BH relay); **L-18 → r2 L-9** (safeAccent two transports); **L-14 → r2 L-10** (Transition import); **L-10(idx24) ≡ D-24 → r2 L-11** (h-7 rungs).
- **L-8(idx24) ≡ D-16(idx25) ≡ C-7's timer arm → r2 L-7, WIDENED with two NEW verified facts**: (1) glass-ui SHIPS `./toast` (this seat's exports-map read: `['./dropdown-menu', './toast']`) — the beat has a producer home the demo never adopted, so the ActionFeedback move must first answer "why not toast?" (one line in the wave, BH question if the answer is producer-shaped); (2) the beat is a flex sibling inside the wrapping toolbar row (:4 `flex-wrap`, :16-20), so acknowledging a destructive action displaces the very controls that caused it (probe: 105px). The uncancelled-timer + missing-teardown arms stand as r2 ruled.

## New non-BLOCKER identities (fresh, no r2 home)

- **Δ-7 · Pagination + search are theatre (C-3 ≡ C-16 ≡ L-20 ≡ N4) — CONFIRMED MAJOR.** api/admin-users.ts:21-33 declares `limit/offset/q` and forwards them; the sole call site :59 is `listUsers(token, 50)` — `res.total` discarded (:60), `q` NEVER sent, filter client-side over the 50-row page. Probe N4 (both engines): 60-user roster, type an off-page name → 0 rows, "· ROSTER CLEAR ·", zero requests issued — a false negative rendered as truth, beside a DISABLED Prune while empty users exist off-page. Siblings consume `res.total` and mount PaginationBar (useAdminAudit/useAdminFlagged); PaginationBar.vue sits unused in the subject's own directory. Feeds Δ-1.
- **Δ-8 · Failed disclosure costumes as "No palettes." (C-4 ≡ D-27) — CONFIRMED MAJOR.** :153-161 `catch → return []` meets panel :138; the loading→empty ladder has no third branch while the roster above has a full error path (:51-62) and the file's own :49-50 invariant forbids exactly this. Probe C4 both engines.
- **Δ-9 · "0 users" above "The roster is unreachable." (C-5) — CONFIRMED MAJOR.** :8 guards the count on `!loading` only, so the error state renders `totalUsers`=0 (adminUsers emptied) above the error plate; AdminPane :117-122 has the same fork on the header badge. The file's own :5-7 comment states the violated principle.
- **Δ-10 · Nested-interactive rows (C-6 ≡ D-29) — CONFIRMED MAJOR.** :85-86 role=button/tabindex on a row CONTAINING two focusable Buttons (:112-130); no `aria-controls`/region id for the :134 disclosure; accName = full row concatenation. Structural DOM fact, both engines (C5_a11y).
- **Δ-11 · The destructive receipt is announced to no one (C-7 ≡ C-20 ≡ D-13's announce arm) — CONFIRMED MAJOR.** :16-20 bare span, no role/aria-live anywhere in the file; each `onPruneDone` arms a NEW 3s timeout without clearing the last, so an earlier timer truncates a later beat; the loading containers :46/:135 put `aria-label` on role-generic divs (name discarded per ARIA; the skeleton's own role=status is fine). Folds into Δ-2's typed-result rendering (the result surface becomes a status region).
- **Δ-12 · One malformed row denies the console (C-8; C-9/C-10/C-11/C-22 riders) — CONFIRMED MAJOR (rescoped: caught, not white-screened).** :247-251 `slug.length` unguarded; a null slug throws in render and the panel-level ErrorBoundary replaces the ENTIRE admin console (probe c10: alert=true, rows=0, every control gone) — announced, but total denial with zero row-level tolerance. Riders (boundary family, MINOR): empty slug → accessible name "Delete user " (:126) and an empty pill; `paletteCount` truthiness at :81/:85/:113/:241 (-1 = interactive row, non-empty); duplicate slugs collide in `:key` and expansion state (:67, :87); `tier as` unchecked casts (:377, useAdminUsers :92).
- **Δ-13 · Refresh never invalidates the open disclosure (C-18 ≡ N3/K3) — CONFIRMED MAJOR.** Nothing watches the `users` prop or `loading`; probe K3 control proves collapse/re-expand fetches GEN-2 while Refresh alone leaves GEN-1 rendered — staleness at the exact moment the operator asked for truth. Folds into `useAdminUserPalettes` (refresh clears/reloads the slice).
- **Δ-14 · Test truth (C-12 ≡ C-13) — CONFIRMED MAJOR, gate-bearing, with a correction to r2.** `vitest.config.ts` has **no `@vitejs/plugin-vue`** (plugin IS in devDeps; environment jsdom; `mount(` count in test/ + demo/test/ = 0) — the vitest tier cannot compile a single SFC today, so **r2's G-6/G-4 unit-test gates were unbuildable as specced**; the delta adds the precondition gate. C-13 verified: `e2e/smoke/admin/fixtures/admin-populated.ts:21-28` type-imports from `demo/@/lib/palette/types` — `demo/@` DOES NOT EXIST (this seat's ls); invisible because e2e runs transpile-only and no tsconfig covers it.
- **Δ-15 · The injection key lives in the wiring module (L-2 idx24) — CONFIRMED MAJOR.** Panel :200 value-imports `ADMIN_PORT_KEY` from `usePalettePorts` (272-line module composing 15 composables + auth) — the leaf drags the whole wiring closure and completes a value-level edge into the type cycle's territory. The repo already owns the cure pattern: `color-session/keys.ts` (:187 imports from it). Keys move to `palettes/keys.ts`.
- **Δ-16 · The demo's boundary lint is dead (L-15) — CONFIRMED MAJOR (precision corrected).** eslint.config.js:274-277 scopes G-DEMO-1/3a to `demo/@/composables/**` — **a tree that does not exist** (zero files matched); the G-DEMO-3b object :232-238 globs `demo/@/components/**`+`demo/@/lib/**` (dead) plus `demo/color-picker/**` (alive, ~4 files) but its banned patterns name `@components/custom/palette-browser/**` — a dead alias. Net: the boundary regime that would have caught this component's import sins guards nothing in the tree where it lives. "Match ZERO files" is literally true for two of three rules; the third matches files and bans ghosts.
- **Δ-17 · Global expandedId (L-11 idx24) — CONFIRMED MINOR.** usePaletteActions.ts:24 single ref republished on THREE ports (usePalettePorts :144/:177/:226), written from both id (:76) and slug (:81) namespaces. Recorded; folds into the port-slimming carry.
- **Δ-18 · Dead exports (L-23) — CONFIRMED MINOR.** `featurePaletteBySlug`/`deletePaletteAdminBySlug`/`onImpersonate`: zero consumers outside useAdminUsers.ts (this seat's grep); none published on adminPort. Delete in-wave.
- **Δ-19 · Design-canon cluster (D-5/D-6/D-7/D-8/D-10/D-11/D-15/D-17/D-18/D-25/D-26/D-30/D-31/D-32/D-19) — CONFIRMED as a class (MAJOR), four members statically certain this session:** the two-denominator sentence (:8 unfiltered `totalUsers` · :11 filtered `emptyCount`, one "·" apart); the duplicated datum (AdminPane badge :122 and toolbar :8, same expression); the disclosure with zero visual state (no open marker anywhere in the template — aria-expanded is the only difference); two irreversible actions sharing Trash2 6px apart (:119/:129, and again :176). The remainder (Fraunces-register controls, PR-35 insets, toolbar void, empty-arm proportion, disabled-colour-only, accent-painted identity :99, sort-by-invisible-createdAt :40-42 vs a row that renders neither) are adopted as the fleet's chromium/webkit cells and land as the wave's design pass under the π obligations — separate from the six blockers, never gating on eyeball re-measurement.
- **L-16 (User-type divergence) — RESCOPED to its verifiable half**: types.ts:88-95 read; the optional-field masking (`?? 0` at :103/:241 etc.) is real in source; the wire-shape divergence half is UNVERIFIABLE-HERE (API-less) and stays out of the gates.
- **L-21 (admin console homed under browser/) — RESCOPED to a structure note** in the wave scope; no gate — re-homing trees is not this wave's cure and "next tranche decides" is not invoked: the note binds the MT-AU1 executor to keep `browser/admin/` internals behind the barrel, and the homing question rides the banked port-split re-trigger.
- **L-13 (export.ts + export/ coexist) — CONFIRMED INFO** (both exist, this seat's ls). Rename rides MT-UI1.

## Verdict on the r2 standing rulings
The r2 scoreboard (7 CONFIRMED / 4 RESCOPED), the L-1 BLOCKER→MAJOR retirement, and L-5 as wave BLOCKER all **STAND** — no r3 evidence overturns any of them; the no-harm KeepAlive mechanism was not challenged by the fresh corpus (idx 22's C-14 records the fleet's own refuted hypothesis on the same arm, converging with r2). The delta ADDS five blockers beside L-5 and corrects one r2 defect: G-6/G-4's test arms lacked their build precondition (Δ-14).

## Dissent (preserved)
1. **C-15 severity (Δ-3)**: filed BLOCKER by the fleet; I sustain BLOCKER on the family ground (unguarded destructive re-entry, two POSTs measured on two engines) while recording it is the least consequential of the six — the second prune is server-idempotent-ish and the second DELETE 404s; the harm is duplicate destructive dispatch + audit noise, not compounding data loss. If the arbiter regrades, MAJOR is defensible; the cure is identical either way.
2. **Corpus census**: the brief's "25 accusations (6 BLOCKER)" vs my measured 139 raw rows / 8 seats. The 6 matches exactly at identity grain; the 25 does not correspond to any single seat or raw sum I can reconstruct. Recorded under L-9: decorative counts must not be repeated as evidence — mine are above, scoped.
3. **Δ-1's full cure is api-shaped**: the in-wave honesty cure removes the lie but cannot give the operator a true count; the preview endpoint is banked, not deferred — re-trigger is a grep, the wave is completable alone without it.
