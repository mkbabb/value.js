claude-fable-5

# kf-KeyframeCardList — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt — recorded against L-11/L-14.
**Date**: 2026-08-09 · **Substrate**: keyframes.js `master` HEAD `8281638c` (re-confirmed by this seat; same HEAD all three axes read) · **Corpus**: 3 challenge documents (D-1..D-17+3S · L-1..L-13+3S · C:D-1..D-10+3S, all opus-5[1m]) against `demo/components/instrument/keyframes/components/KeyframeCardList.vue` (82 L) and its composed child `KeyframeCard.vue` (81 L), plus two hostile re-reader returns (reader-DU, design-and-usage · reader-LC, library-and-code).
**Method (L-14)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (the D-5 per-edit blank, the L-4 causal chain, the L-13 write mechanism, the D-10 relay disposition, the L-2/C-D-3 crash reachability, the D-8 and M-4 severities) was re-derived against the tree with this seat's own instruments; both readers' independently-found missed defects (the frozen-slider write, the dead structural watch) were reproduced, not spot-trusted; one confirmed cell of reader-LC's was killed by this seat outright.
**Arbiter's own instruments (all static + node; zero browser probes — probe parsimony, read-only law)**: full reads of KeyframeCardList.vue (82 L), KeyframeCard.vue (81 L), useKeyframesParsing.ts (whole), useKeyframeOps.ts:100-215, useKeyframesState.ts:20-30, KeyframesEditor.vue:30-59/120-284; **node against the installed `@mkbabb/value.js@4.0.0`**: `parseKeyframeSelector('50%')` → `ok:true`, value `{kind:"percent",value:0.5}`, `Object.isFrozen` → **true**, `value.value = 0.9` → **`TypeError: Cannot assign to read only property 'value'`**, `String(value.toString())` → **`"[object Object]"`** (the KC-1/KC-2 decider); installed `@vue/reactivity` `doWatch`: plain-array source → `isMultiSource` branch, each plain-object element → `warnInvalidSource`, zero deps tracked (the KC-6 decider); `package.json` scripts + deps (no `vue-tsc` anywhere; deps = `{"@mkbabb/value.js":"4.0.0"}` only; `check` = plain `tsc`), `grep -c glass-ui package-lock.json` → 0, ci.yml:41-42 = `check:lib` (the KC-13/F-1 deciders); installed glass-ui **7.0.0** dist: `.field-control:focus-visible{border-color:…;box-shadow:…;outline:none}`, `--separator-ink: color-mix(in srgb, var(--foreground) 22%, transparent)`; sibling glass-ui checkout at `e73ec80c` (2026-08-09), **version 8.0.0**: `src/styles/tokens/color-radius.css:183` "`--separator-ink` (0.22) **DIED** at BK #87 W-MARKS" → `--ink-seam: 0.08`, and `src/components/_shared/field/control.css:221-223` focus = `outline: var(--focus-ring-width) solid var(--field-control-ink)` (the KC-5/KC-20 disposition deciders); greps: `removeKeyframeData` callers = KeyframesEditor.vue:163/:249 **only**; `templateFrameStrings.value` writers = useKeyframesParsing.ts:50/:52 **exactly two**; PRM arms in 8 `demo/scenes/` files; `respectReducedMotion = false` (group.ts:57); `warpLeft` = 700 ms `ease-in-bounce` (catalog.ts:120-123); KeyframeCard sole consumer; `.tap-floor` 44px at design-idioms.css:81-85; the demo z-contract `--z-modal: 140 "modal dialogs — above everything"` (style.css:21-35) vs `sticky z-modal` at KeyframeCard.vue:4.

## Scoreboard

**37 adjudicated rows: 3 BLOCKER · 10 MAJOR · 17 MINOR · 7 INFO. 12 killed-claim register entries (incl. 1 kill of a re-reader's confirmed cell by this seat). 13 corpus-missed defects adjudicated IN (9 from reader-DU, 4 more from reader-LC, 1 this seat's own).**

Blocker-weight survivors: **(1)** every stop's offset reads `[object Object]` in the editable field and its caption — proven at the shipped artifact by this seat's own node run; **(2)** the offset **Slider — the editor's primary timing control — throws a `TypeError` on every drag tick**, because it writes into a value.js deep-frozen selector (write verified thrown by this seat; read side pins every thumb at ≈0 on a −10…110 track); **(3)** the sole keyframe-removal affordance is a bare `<svg>` with a click listener — no tab stop, no role, no name, no keyboard path. The offset authoring loop is dead end-to-end three independent ways, and the shared enabler is the same one the kf-CSSCodeEditor record established: **no `.vue` file in this repository is type-checked by anything.**

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-5 per-edit blank** | KILLED D-5(b) (no visible jolt post-cache; all microtasks), kept the mount blank at MINOR | CONFIRMED MAJOR — "PROVEN unconditional… microtask FIFO guarantees an intermediate zero-row render on every regeneration" | **BOTH HALF-RIGHT — fused at MAJOR (KC-8)** | The two claims live at different layers. The zero-row **RENDER** is unconditional: the `= []` write at useKeyframesParsing.ts:50 schedules `flushJobs` on the resolved-promise microtask before the refill continuation can queue — reader-LC's derivation adopted; it also underwrites the C-D-4/L-3 kill and the D-17 masking. The per-edit **PAINT** is NOT established: post-cache, every step between the two writes is plausibly microtask-only, so reader-DU's kill of the "layout jolt on every keystroke" stands — with the honest residue that nobody (readers included) has proven `prettier.format` never yields a macrotask (→ UNPROVEN #5). The mount blank is certain (real chunk load). Severity MAJOR on the composite: the unconditional full unmount/remount of every card on every regeneration is the component's structural tax — it is what voids the id-keying benefit (see superlatives) and what grounds KC-9. |
| **L-4 causal chain** (F-1 → CI narrowed → L-1 shipped) | CONFIRMED the chain ("closing L-9→L-4→L-1 as a causal chain, not a coincidence") | REFUTED causation: plain `tsc` cannot read `.vue` at all; the un-narrowed `check` never inspected this template either | **reader-LC WINS** | My scripts read: `check` = `tsc --noEmit` ×2 — no vue-tsc, no `@vue/language-tools` anywhere in package.json. Even repo-wide `tsc` is structurally blind to `.vue`; the tsconfig.lib.json narrowing (real, documented, F-1-motivated) removed the demo **TS project** from CI — a separate fact that never protected this template. Consequence adopted: the demo type gate is blocked on **adding vue-tsc**, not on F-1; reader-DU's confirmation of the chain over-credited the tsconfig comment. |
| **L-13 write mechanism** (the Slider write) | KILLED the write ("throws — frozen; verified by evaluating the installed artifact"); refiled the row as missed **M-1 BLOCKER** | CONFIRMED L-13 MAJOR with the axis's mechanism: "a drag writes raw integers back, after which `selectorText` would emit `5000%`" | **reader-DU WINS — this seat's own node run is the decider** | `Object.isFrozen(parseKeyframeSelector('50%').value)` → `true`; the assignment **throws** `TypeError: Cannot assign to read only property 'value'` (my run, header block). SFC render code is ESM → strict → the throw is real and aborts the `forEach` at the first still-frozen frame. `5000%` is never produced. Reader-LC's confirmed cell carries the axis's false write-corruption mechanism — a citation-inheritance event inside an otherwise-verified row (see DISSENT). One fused row survives at **BLOCKER** (KC-2): write side throws; read side pins ≈0 ([0,1] fractions on a −10…110/`step=1` track) and reads `undefined` for `{kind:"named"}`. Nuance recorded: `onUpdateStart` (KeyframesEditor.vue:206-209) replaces a frame's frozen `start` with a **plain** literal, so a hand-edited frame's slot becomes writable — the throw then lands on the first untouched frame; the control is dead either way in the shipped seeding path (frame-compiler stores the frozen parse verbatim). |
| **D-10 disposition** | KILLED D-10(b): the relay-as-written targets a token upstream already **deleted** in the opposite direction | CONFIRMED with "correctly routed upstream via the BH/BI relay" | **reader-DU WINS — my own 8.0.0 read is the decider** | glass-ui `e73ec80c` (8.0.0): `--separator-ink` "DIED at BK #87 W-MARKS", replaced by `--ink-seam: 0.08` — **fainter** (~1.20:1 ceiling) than the 22%/1.69:1 the axis objected to, and still no `forced-colors` arm for `.separator`. A relay sent as the axis wrote it would request a change that already landed with the opposite sign. KC-20's disposition: re-scoped relay against 8.0.0's `--ink-seam` + a forced-colors arm; nothing here regression-testable until F-1 lands (demo pins 7.0.0). |
| **L-2 / C-D-3 crash reachability** | Confirmed the hazard half MINOR; named surviving triggers (onUpdateStart, the 1000 ms debounced op) overlapping the 700 ms removal window | Downgraded: asymmetry CONFIRMED MINOR, TypeError PLAUSIBLE-UNPROVEN; also killed axis-D's own "impossible" reasoning | **ALIGNED at MINOR + UNPROVEN — both adopted, both extremes of the original axes rejected** | The guard asymmetry (`:5` `frames[i]?.id` vs `:11` `frames[i].start`) is unconditional and needs no race. The crash needs a stale **longer** pass's final write (:52) to land after a removal — the blanking cannot serialize passes it does not own (axis-D's D-15 negative result mis-reasoned, killed #8), but no reader exhibited the interleave either (axes C/L asserted it; their named slider trigger is dead — it throws first, killed #3). → UNPROVEN #3. |
| **D-8 severity** | MAJOR | MINOR | **MAJOR — reader-DU's rating** | The 1.84:1-ceiling block is not decoration: `f {index}` is the row's **only visible identity**, exposed to AT, and `pointer-events-none` additionally makes it unselectable/uncopyable. Token-independent, statically decidable, both readers' arithmetic agrees to the second decimal. |
| **M-4 severity** (`z-modal` on the offset field) | MAJOR ("token-contract breach, decidable statically") | — (not adjudicated) | **DEMOTED to MINOR by this seat** | The breach is real and doubly documented (style.css:21-35 `--z-modal: 140 "modal dialogs — above everything"`; the only other consumer is an actual overlay). But the statically-proven harm is contract erosion alone: every pixel consequence (sticky inputs at 140 punching over the `--z-popover` 130 SharePopover, the z-auto sticky bottom slider panel, or the dock) is stacking-context/DOM-order contingent. → SS-13 probe with an explicit promotion trigger: a witnessed punch-through re-prices this row MAJOR. |

## Adjudicated defect roster — 37 rows

Ids are the corpus's own, for life; `KC-n` is this record's roster index. Dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input (M-25). **CARD-UNIT = NO-WAVE-OWNER**: a proposed single wave-surface ("the keyframe authoring loop made honest" — offset readout + offset writer + delete affordance + remount discipline + the a11y triad + the ref bookkeeping), spanning `KeyframeCardList.vue` + `KeyframeCard.vue` + `KeyframesEditor.vue`'s card seam, for SS-1/SS-2 spec authoring to consume; no census KF.W0–W10 row names this repair, and scattering it would orphan its one authoring loop. Sequenced **after** the KF.W4 vue-tsc gate (nothing in it is defensible until a `.vue` file can fail a build) and internally ordered by KC-34's edict.

### BLOCKER
- **KC-1 · D-1 = C-D-1 = L-1 — `:frame-start="frames[i].start.toString()"` renders `[object Object]`** in the offset `<Input>` (KeyframeCard.vue:5) and its caption (:37), on every card. Proven at the shipped artifact by this seat's node run (`toString` → `"[object Object]"`; union at css.d.ts:210-217; frozen literal construction at css.js; engine stores it verbatim, frame-compiler.ts:145-150). The correct serializer is used by the library one function away (`selectorText`, format.ts:131) and by the sibling write path (useKeyframeOps.ts:110-111) — four correct call sites, one `.toString()`, and the one is in the file nothing type-checks. Named selectors additionally un-editable (the write path accepts `%`-scalars only, KeyframesEditor.vue:196-203). Cure: `selectorText(frames[i].start)` — one line, one import; **prerequisite for KC-27's offset-bearing aria names, land together**. → **CARD-UNIT**.
- **KC-2 · corpus-missed (reader-DU M-1; fuses L-13; kills L-13(b)/C-D-3(b)) — the offset Slider is dead: the write throws, the read pins.** KeyframesEditor.vue:40-47 `frame.start.value = starts![i]` writes into a value.js deep-frozen parse result → `TypeError` on the first frame (this seat's run), aborting the forEach; `updateAllStringsAndAnimation()` at :45 never fires. Read side: `[0,1]` fractions rendered against `:min="-10" :max="110" :step="1"` — all thumbs ≈0; `undefined` for named selectors. All three axes walked past the line (axis L filed it INFO with the wrong mechanism). The same line is TS2339+TS2540 under the repo's own tsconfig (KC-37) — the vue-tsc day-one artifact. Parent-owned, same authoring loop. → **CARD-UNIT**; the type witness attaches to **KF.W4**.
- **KC-3 · D-2 — the sole keyframe-removal affordance is a bare `<svg>` with a click listener**: no tabindex, no role, no accessible name, no keyboard activation (KeyframeCard.vue:20-25; lucide forwards attrs onto `<svg>`, synthesizes nothing — both readers' dist reads). Sole path verified (my grep: `removeKeyframeData` reachable only via KeyframesEditor.vue:249 ← :232 ← the list's :15). WCAG 2.1.1 + 4.1.2 (A). The house-correct idiom exists one instrument over (KeyframeTimeline's `<Button icon-only aria-label>` wrap of the same `X` — reader-LC), and the correct naming idiom sits 3 lines away on CopyButton — which is what makes this inexcusable rather than unfortunate. → **CARD-UNIT**, first of the a11y triad.

### MAJOR
- **KC-4 · D-3 — the offset `<Input>` has no accessible name of any kind** (KeyframeCard.vue:3-8; installed 7.0.0 `Input` derives none — reader-DU's dist read; the two adjacent `<Label>`s are unbound siblings in a `pointer-events-none` block). The sibling `<pre>` at :47-49 carries the full role/multiline/label idiom — the author knew it. → **CARD-UNIT** (with KC-1: name = "Keyframe offset at 50%"-class).
- **KC-5 · D-4 — the demo's utilities defeat glass-ui 7.0.0's entire keyboard-focus affordance on the offset field** (`focus:border-transparent focus:shadow-none` from `@layer utilities` over the components-layer `.field-control:focus-visible{border-color;box-shadow;outline:none}` — my 7.0.0 dist read; the same rule's `outline:none` forecloses the native fallback, so keyboard focus changes literally nothing outside forced-colors). **Already cured upstream**: 8.0.0's control.css:221-223 paints focus as an `outline`, which these utilities cannot defeat (my 8.0.0 read) — the cheapest correct repair is the glass-ui 8 adoption riding F-1; else strip the `focus:*` overrides now. → **CARD-UNIT** + version-bump note to **KF.W6**.
- **KC-6 · corpus-missed (BOTH readers independently) — the `templateFrames` structural watch can never fire, and the 10-line comment above it documents a projector that does not exist.** useKeyframesParsing.ts:96-103 `watch(animation.templateFrames, …)` — `animation` is `markRaw`, the source is a **plain array**: Vue's `doWatch` takes the `isArray` multi-source branch and every plain-object element hits `warnInvalidSource`, zero deps tracked (verified by this seat against the installed reactivity — the decider). Independently fatal: the array is captured by reference while `removeKeyframeData` **reassigns** `animation.templateFrames` (useKeyframeOps.ts:202, my read), so even a live watch would bind a dead array after the first removal. Dev logs one warning per element per mount. The surface survives only because every mutation site also calls the explicit updaters — the "edit projector" does 100% of the work the comment splits 50/50. Axis C cited these exact lines *approvingly* (killed #11). Cure: `watch(() => animation.templateFrames, …)` getter (or length getter) — or delete watch + comment together. → **CARD-UNIT**.
- **KC-7 · corpus-missed (reader-DU M-3; replaces killed D-6(b)) — deleting the last remaining keyframe is a total silent no-op**: KeyframesEditor.vue:232-235 bare-returns before everything; the only explanatory toast (useKeyframeOps.ts:195) is unreachable from this surface (my sole-caller grep); the `X` keeps its full live-affordance styling unconditionally. The list is the natural owner of the disabled state (it already computes `frameStrings.length - 1` at :21). → **CARD-UNIT**.
- **KC-8 · D-5 fused (mount blank + unconditional remount; per-edit paint claim killed) —** the list has one rendering mode (N rows), no skeleton/empty/reserved height; a zero-row **paint** on first mount is certain (real prettier chunk load); and **every** regeneration renders N → 0 → M by microtask ordering (reader-LC's proof, adopted; two-writer grep re-run by this seat), full-unmounting and remounting every card — the structural tax behind KC-9, D-17's masking, and the id-keying demotion. The per-edit visible jolt is NOT established (killed #6). glass-ui `Skeleton` unreached (census S-6 context). → **CARD-UNIT**, strictly **after** KC-34's highlight repair; repair shape = keep the single writer, add a generation token (reader-LC's honest note adopted: the blanking is the *price* of the two-writer discipline, not a separate sin).
- **KC-9 · THIS SEAT'S OWN (adjudicator-added) — the debounced write-back destroys the focused contenteditable mid-session.** Chain, all verified reads: typing in a card's `<pre>` → `updateCSS` → `updateAnimationFromKeyframeString` (1000 ms debounce, useKeyframeOps.ts:108-144) → on success `updateAllStringsAndAnimation()` (:136) → `updateAllStrings` → the proven unconditional N→0→M remount (KC-8) → the `<pre>` the user is editing is unmounted; focus falls to `<body>`, caret/selection/undo gone, ~1 s after they pause; resumed keystrokes land nowhere. Content survives (the S-B round-trip commits the vars first); the editing *session* does not. Both readers held the remount proof and the write-back chain in the same hand and neither composed them. The felt symptom (and whether the identical-text remount is perceptible) → SS-13. → **CARD-UNIT** — this row plus KC-8 makes the generation-token/keep-mounted repair a correctness requirement, not polish.
- **KC-10 · D-6(a) — removal restores no focus and announces nothing**: the whole path contains no focus call; the demo's only live regions are CopyButton and App.skeleton (reader-DU's exhaustive grep); the clicked element leaves the DOM → focus to `<body>`. The neighbour index is already computed for the animation (KeyframesEditor.vue:239-240) — reuse it for focus. → **CARD-UNIT**.
- **KC-11 · D-7 (+ M-8 context) — the removal motion ignores prefers-reduced-motion and gates the data mutation behind a 700 ms bounce** (`respectReducedMotion = false` at group.ts:57 — my read; `warpLeft` 700 ms `ease-in-bounce` — my read; glass-ui's CSS-side PRM arm structurally cannot reach a rAF loop). Upgraded by M-8: the demo has a PRM house pattern in 8 scene files + 2 JS matchMedia guards (my grep corroborates) — the editor is the one rAF surface that skipped the demo's own idiom, and the repair template exists in-tree. → **CARD-UNIT** (`respectReducedMotion: true` or the matchMedia guard; and consider un-gating the data write from the animation).
- **KC-12 · D-8 — the row-metadata block paints at a 1.84:1 theoretical ceiling** (`opacity-25`, arithmetic re-derived independently by both readers to agreement), is exposed to AT, holds the row's only visible identity, and `pointer-events-none` makes that identity unselectable. Token-independent. Severity per the ruled table. → **CARD-UNIT** + real muted-token swap coordinates with **KF.W6**.
- **KC-13 · L-4 — nothing type-checks this file (or any `.vue` file)** — no vue-tsc anywhere; `check` = plain tsc; CI = `check:lib` (src/ only) — all re-verified by this seat. Causal chain corrected per the ruled table: the gate is blocked on **vue-tsc**, not F-1. **IDENTITY GUARD**: this is the same gate hole the kf-CSSCodeEditor record booked (KF-CE-16 → KF.W4) — **FOLD by reference, never re-booked**; this corpus contributes two new day-one witnesses: KeyframesEditor.vue:43 (TS2339 + TS2540, KC-37) and the `any[]` prop (KC-17). → **FOLD → KF.W4** (KF-CE-16 identity).

### MINOR
- **KC-14 · L-2 = C-D-3 rescoped — the guard asymmetry** (`:5` guarded, `:11` not) stands unconditionally at MINOR; the render TypeError is UNPROVEN (ruled table). The structural dissolution is KC-29. → **CARD-UNIT**.
- **KC-15 · reader-missed (DU M-9 = LC #2; the surviving remnant of killed L-3/C-D-4) — `cardInstances` is never truncated**, so `cardRefs.length` is the historical maximum; after any prior removal, deleting the last row takes the `frameIx + 1` arm against the stale tail and hands `null` to `presets.jumpUp().setTargets(null)` — a silently half-played exit choreography for the session's remainder (null-tolerance verified by both readers at element-resolve). `getPreElements()` unaffected (it filters). Reader-LC's repair correction adopted: truncating **inside** `setCardRef` is unsound (patch order non-monotonic; sync-from-end seats high indices first) — the `onBeforeUpdate` rebuild is the safe cure, which also cures KC-36. Whether `.play()` on a null-targeted group settles (gating whether `removeKeyframeData` is even reached) → UNPROVEN #12. → **CARD-UNIT**.
- **KC-16 · L-6 = C-D-5 + L-7 — the per-site async engine loader defends a transient that cannot occur** (the parent reads the throwing-sync `kfEngine()` at `<script setup>` top level; the app mounts inside the warm's `.finally` — both readers, consistent with my KeyframesEditor.vue:127-130 read), and carries no `.catch` (latent — the parent throws first). Adopting `kfEngine()` deletes the ref, the promise, the null branch, the `?? s`, and the 6-line comment together. Provenance defect recorded: axis L cited `main.ts:74-78` — stale against HEAD (:50-52); axis C cited it correctly. → **CARD-UNIT**.
- **KC-17 · L-5 = C-D-2 — `frames: any[]` declines the library's own zero-cost light-barrel type** (`TemplateAnimationFrame`, index.ts:165, type-only pure) and disables `noUncheckedIndexedAccess` in the one file whose job is projecting the union. Both axes' honest disclaimer stands: a right type would NOT have caught KC-1 (`toString` exists on `object`). → **CARD-UNIT** (lands with the KF.W4 gate).
- **KC-18 · L-8 — `ref<any[]>` of component public instances is deep-reactive**, safe only by accident of the child's `defineExpose` (`markRaw`'d expose proxy vs the un-markRaw'd `instance.proxy` fallback — both readers verified the installed 3.5.35 branch). `shallowRef` is already imported one line away. → **CARD-UNIT**.
- **KC-19 · D-9 = C-D-6 — N−1 unnamed `role="separator"` nodes over rows with zero grouping semantics** (`decorative` unset → reka emits `role="separator"`; the list root is `display:contents`, the card root a bare `div.grid`; no list/group/setsize anywhere). One fix, two halves: name the rows, then mark the rules `decorative`. → **CARD-UNIT**; AT announcement → UNPROVEN #8.
- **KC-20 · D-10(a) rescoped — the 7.0.0 divider paints at a ~1.69:1 ceiling with no forced-colors arm** (my dist reads; the only visual is `background`, overridden to Canvas under forced-colors → the dividers vanish). Disposition per the ruled table: **re-scoped glass-ui BH relay** against 8.0.0's `--ink-seam: 0.08` (fainter still) + a `forced-colors` arm for `.separator` — never the axis's original request; nothing regression-testable until F-1. → **glass-ui BH relay** (owner edict, standing invariant).
- **KC-21 · D-11 — the rhythm is double-encoded (2 rem void + hairline) and 4.1× its 8 px frame**, and by choosing `display:contents` the list owns no layout authority over its own dividers (the sharper half). Ratio statically certain; rendered pixels → UNPROVEN #10. → **KF.W6**.
- **KC-22 · D-12 — `class="w-full"` on `Separator` is dead twice over** (dist sets `inline-size:100%`; grid stretch) and re-states a logical property physically. Deletes free with any KC-19 touch. → **CARD-UNIT**.
- **KC-23 · D-13 — 24 px destructive target, zero gap from its neighbour, under the demo's own documented 44 px `.tap-floor`** (my read, design-idioms.css:81-85). House-standard miss, not a 2.5.8 failure. Rides KC-3's button promotion. → **CARD-UNIT** + **KF.W6**.
- **KC-24 · C-D-8 — three payload key names for one positional concept across four sibling emits, the fourth carrying no index** (`{val,index}`/`{value,index}`/`{event,index}`/bare — my read of :13-16/:54-58; the child names all three uniformly). The wrapper's one contract job is adding `index`, and it renames en route. → **CARD-UNIT**.
- **KC-25 · corpus-missed (reader-DU M-4; DEMOTED per ruled table) — the offset field claims `z-modal` (140), the demo's documented top rung, for a sticky in-card input** (`sticky z-modal` at KeyframeCard.vue:4 vs the style.css:21-35 contract "modal dialogs — above everything"; sole other consumer is an actual overlay; nothing between the input and root opens a stacking context — reader-DU's walk, consistent with my reads). Statically proven: contract breach. → **CARD-UNIT** (a `--z-controls`-class rung); punch-through probe → SS-13 with MAJOR-promotion trigger.
- **KC-26 · corpus-missed (reader-DU M-5) — the form-label primitive is spent on decoration in the same card whose real control has no label**: two orphan glass-ui `<Label>`s (real `<label>` elements via reka, with the multi-click preventDefault behavior) wrapping nothing, `pointer-events-none`, while the `<Input>` twelve lines up is nameless (KC-4). The glass-conformance inversion running backwards: a primitive conscripted for a job it does not do. → **CARD-UNIT** + **KF.W6**.
- **KC-27 · corpus-missed (reader-DU M-6) — every accessible name on the surface is positional** (`CSS for keyframe ${index}` — shifts on every non-tail removal, silently re-pointing AT landmarks; removal itself unannounced, KC-10) and the identifying value a name should carry is the one KC-1 breaks. KC-1's fix is the prerequisite; land together. → **CARD-UNIT**.
- **KC-28 · corpus-missed (reader-DU M-7) — the child-ref contract comment overstates the child's declaration**: KeyframeCardList.vue:61-63 promises `$el` + `preEl` "via defineExpose"; the child exposes `{ preEl }` only (my reads of both files) — `$el` arrives from Vue's instance surface regardless, and that accidental half is the one feeding the remove animation with no single-root guarantee anywhere (a fragment root turns `$el` into a comment-node anchor and the null-tolerant engine no-ops silently). → **CARD-UNIT** (fix the prose with the KC-15 repair).
- **KC-29 · corpus-missed (reader-LC #3) — the architectural root: `frames` is non-reactive data read inside a render function**, correct today only because a *different* ref (`templateFrameStrings`) churns on the same events. Two index-parallel projections of one source, one reactive, one not — the shared cause of KC-14's asymmetry, KC-31's dead fallback, and a staleness class (mutations that don't churn the strings are invisible until the async pass lands). The one repair in the corpus that removes a class rather than a line: one `ref` of `{ id, start, raw, formatted }[]`. → **CARD-UNIT** (the spec's recommended shape).
- **KC-30 · L-10 — the child lives one directory above its sole consumer** (my grep: KeyframeCardList is KeyframeCard's only importer). → **KF.W8** (colocation settle).

### INFO
- **KC-31 · L-11 = D-14 = C-D-10 — `formattedStrings[i] ?? s` is runtime-dead**; axis L's framing wins (compile-required under `noUncheckedIndexedAccess`, so "delete it" is the wrong cure) — the real signal is KC-29's one-array shape. C-D-10's `?? c` note adopted. → **CARD-UNIT** (dissolves with KC-29).
- **KC-32 · L-12 — the exposed contract mixes shapes** (`cardRefs` ComputedRef beside a plain function). KC-15 makes it load-bearing rather than cosmetic: the *filtered* member is correct and the *unfiltered* one is not. → **CARD-UNIT**.
- **KC-33 · D-16 — the header comment asserts "never a blank" of a surface that blanks unconditionally** (KC-8). One clause repairs it; rewrite rides the KC-8/KC-16 edit (E-3: prose is part of the defect surface). → **CARD-UNIT**.
- **KC-34 · D-17 — the blanking currently masks a highlight/vdom divergence** (`el.innerHTML = …` on the Vue-owned `<pre>` detaches the `<code>` text node; today's full remount clears the marker and rebuilds). **THE SEQUENCING EDICT — the single most valuable ordering constraint in the corpus, now doubly load-bearing**: the highlight path must stop replacing Vue-owned DOM **before** KC-8/KC-9's keep-mounted repair lands, or an invisible coupling becomes a visible stale-content bug. Post-repair divergence → UNPROVEN #11. → **CARD-UNIT sequencing law**.
- **KC-35 · corpus-missed (reader-DU M-8) — the PRM house pattern exists in 8 scene files + 2 JS guards**; context row, folds into KC-11's cure. → with **KC-11**.
- **KC-36 · corpus-missed (reader-LC #4) — a fresh ref closure per card per render, re-invoked across every patch**, on a list that fully re-renders per regeneration (KC-8); also the very staleness both dead traces were built on. The `onBeforeUpdate` rebuild (KC-15) cures it; the sibling already uses `useTemplateRef`. → **CARD-UNIT**.
- **KC-37 · corpus-missed (reader-LC #5) — KeyframesEditor.vue:43 is statically ill-typed twice** (TS2339 on the `named` arm; TS2540 on `Readonly`) — the concrete falsifier L-4 asked for, converting KC-2 into a one-command type-gate catch. → attaches to **KF.W4** (KC-13's fold).

## Killed-claims register — 12 entries, refutations preserved

1. **L-3 / C-D-4(b) (both axes, MAJOR) — "every non-tail removal nulls a live ref; D's card is never highlighted again"** — KILLED (both readers, upheld): the required partial keyed diff never runs. Exactly two writers of `templateFrameStrings`, both in one function (re-grepped by this seat); the `= []` write's flush is FIFO-guaranteed to render before the refill continuation queues, so every length change renders N → 0 → M — all-unmount (each closure nulls its own index) then all-mount fresh. `getPreElements()` is never short and never duplicated. The axes' Vue readings were impeccable; the premise they operated on is foreclosed by the data flow. Residue promoted: KC-15.
2. **C-D-4(c) — "a detached instance is retained"** — KILLED (reader-DU, upheld): even under the axis's own trace the stale slot holds the SAME live instance as the re-seated one — a duplicate, never a corpse; a leak-hunt would find nothing.
3. **L-2(b) / C-D-3(b) — "the Slider drives the race: it fires `updateAllStringsAndAnimation()` un-debounced per drag tick"** — KILLED (reader-DU) and **VERIFIED KILLED by this seat's node run**: the handler throws `TypeError` on the first frozen `frame.start.value` write; the update call after the forEach is unreachable. The race survives only through slower triggers; the frozen write itself is KC-2.
4. **L-13(b) — "the Slider writes raw integers back; `selectorText` would emit `5000%`"** — KILLED, same run: there is no write; there is a throw. The axis had the right line and the right unit mismatch on the READ side, and understated a BLOCKER as INFO by missing the freeze.
5. **Reader-LC's confirmed L-13 cell ("a drag writes raw integers back…") — KILLED BY THIS SEAT.** The reader confirmed a mechanism the other reader had already executed to refutation; my own run settles it. The row's substance survives fused into KC-2; the cell did not. Recorded in DISSENT as a citation-inheritance event.
6. **D-5(b) — "a layout jolt on every keystroke-driven regeneration"** — KILLED (reader-DU, upheld with the layer split): the zero-row RENDER is unconditional (reader-LC, adopted into KC-8) but a PAINT between the two writes is not established post-cache; the axis's own hedge should have been the claim. Honest residue: prettier's macrotask behavior unproven either way (UNPROVEN #5).
7. **D-6(b) — "the failure case is announced visually as a toast"** — KILLED (reader-DU) and verified by this seat's sole-caller grep: KeyframesEditor.vue:232-235 bare-returns before `removeKeyframeData`, so useKeyframeOps.ts:195's toast is dead code on the only removal path. The truth is worse: KC-7.
8. **D-15's negative result ("the v-for body never executes against a mismatched pair — latent, not live")** — KILLED (reader-DU, upheld): the `= []` preceding a refill belongs to the SAME pass; a stale longer pass's final write is itself the mismatched render trigger. The blanking cannot serialize passes it does not own. (The crash nonetheless remains UNPROVEN — both axes' extremes rejected, ruled table.)
9. **D-10(b) — "fix belongs upstream; relay as written"** — KILLED (reader-DU) and **VERIFIED by this seat's 8.0.0 read**: `--separator-ink` is deleted upstream in the *fainter* direction (`--ink-seam` 0.08); the relay must be re-scoped (KC-20) or it requests a landed change with the opposite sign. Same provenance hazard noted for the axis's other `glass-ui/src` citations — re-verified substantively against the installed 7.0.0 by the readers; they hold.
10. **C-D-1 blast-radius item 3 — "the write path is severed; the round trip breaks both halves at once"** — KILLED (reader-LC, upheld): the field is uncontrolled after seeding; select-all + type `50%` parses clean and commits. The BLOCKER is the readout, not the writer — which matters for triage: the instrument is authorable, just unreadable.
11. **C-D-3's supporting praise — "the author saw this hazard and defended the watch against it (flush:'post' + nextTick)"** — KILLED (reader-LC) and **VERIFIED by this seat against the installed reactivity**: the praised watch can never fire (plain-array multi-source → `warnInvalidSource`; and reference-capture vs the :202 reassignment). The evidence cell inverted into KC-6.
12. **L-4's causal chain ("F-1 → CI narrowed to src/ → demo untyped → L-1 ships") — PARTLY KILLED** (reader-LC; ruled for LC against reader-DU's confirmation): plain `tsc` cannot read `.vue` with or without the narrowing; the gate was never there to lose. The narrowing is real and documented; the causation and the repair-blocking edge were wrong. The demo type gate is blocked on vue-tsc, an independent and cheaper move.

## Superlatives — provenance preserved (L-18 runs both ways)

- **THE COMPONENT · S-A, the declared child-ref contract** (all three axes + both readers; survives adjudication STRONGER than anyone argued): `defineExpose({ preEl })` → `getPreElements()` → `useCodeHighlight(() => cardList.value?.getPreElements() ?? [])`, with the retired bug named in prose at both ends (`D.W3.S1: the global querySelectorAll("pre") was the bug`). Both axes that charged its bookkeeping with corrupting its payload were killed; the falsifier grep (no content sweep survives) was re-run by the readers and stands. The mechanism is not just well-designed — at runtime it is *right*.
- **THE COMPONENT · the two-writer discipline on `templateFrameStrings`** (reader-LC's own find; re-grepped by this seat: exactly two assignments, one function). The single most load-bearing property on the surface: it made the runtime statically decidable and killed two MAJORs. Honest ledger kept: it also *causes* KC-8 — the repair keeps the single writer and adds a generation token; it does not split the writes.
- **THE COMPONENT · S-B (axis C), the engine-purity comment that survives its own audit**: `formatCSSKeyframeString` really is a value.js-free three-regex trim exported only on the heavy surface; both halves verified, the routing conclusion correct even though the accessor choice was not (KC-16). Contrast the OTHER long comment in this cluster (useKeyframesParsing.ts:86-95), refuted outright into KC-6. Same author, opposite outcomes — the difference between prose that describes code and prose that describes intent.
- **THE COMPONENT · S-B (axis L), the raw/formatted closed round-trip**: the contenteditable carries only the declaration body; the selector re-attaches on write-back from the authoritative in-memory value; Copy gets the complete rule. Three consumers, three correct projections, one source — and precisely why KC-1 is a display bug, not a data-loss bug.
- **THE COMPONENT · S-B (axis D), id-keyed rows — ADJUDICATED DOWN to conditional**: the id-keying is the correct, non-obvious choice, and ids are verifiably stable across removal. But under KC-8's unconditional full remount, the caret/selection/undo preservation the axis praised **never operates at steady state** — every regeneration destroys all rows regardless of key. The superlative's benefit is real only after the CARD-UNIT repair keeps rows mounted; record it as latent correctness, and keep the axis's `?? i` alias hairline.
- **THE COMPONENT · CopyButton.vue beside the `<pre>`'s complete naming idiom** (both readers): a real `<button>` with state-dependent aria-label and an sr-only live region, three lines from the nameless destructive `<svg>`; a fully-named textbox `<pre>` four lines from the nameless `<Input>`. The row contains both the standard and its violation — which converts KC-3/KC-4's repair from instruction to imitation.
- **THE COMPONENT · S-C (axes L+C), Goldilocks + barrel purity + zero token debt**: 82 lines, one responsibility, zero dead imports, one published-light-barrel specifier, zero `@src/*`/value.js deep imports (stricter than its own sibling composables), zero `--kf-*` hits demo-wide, no `<style>` block. Every confirmed defect is a line or a seam, never the decomposition.
- **THE LIBRARY · value.js 4.0.0's frozen parse boundary** (reader-DU): every parse result deep-frozen in a frozen envelope — which is exactly why KC-2 is a loud TypeError instead of silent corruption, and why this record could be settled by one node run. The immutability contract did its job; the consumer never read the contract. The counter-witness set (four correct `selectorText` call sites; the boundary test pinning the shape) dates KC-1 as a regression in the one unwatched file, not a design error.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (route with KF.W9)

1. KC-1's glyph: the literal `[object Object]` pixel in the card (string proven statically + by the repo's own test).
2. KC-2's moment of death: whether reka's Slider emits `update:modelValue` during mount normalization (0…1 values vs `step=1`), moving the frozen-write TypeError from first-drag to first-render. The control is dead either way.
3. KC-14's TypeError: a stale-longer-pass interleave (drag on a cold prettier import, then delete a middle stop) — the only decider left after both axes' named triggers died.
4. KC-8's mount blank duration — bug or beat.
5. KC-8's per-edit paint: whether `prettier.format` ever yields a macrotask post-cache (the sole remaining path to a visible per-edit blank).
6. KC-9's felt symptom: the caret/focus loss ~1 s after pausing mid-edit; whether the identical-text remount is itself perceptible.
7. KC-25's punch-through: the `z-modal` sticky inputs vs the SharePopover (130), the z-auto sticky bottom slider panel, and the dock — **promotion trigger to MAJOR if witnessed**.
8. KC-19's AT behavior: the N−1 unnamed separators interleaved with N textboxes, per screen reader.
9. KC-20's forced-colors vanish — and re-witness against 8.0.0's `--ink-seam` once F-1 + the version bump land.
10. KC-21's measured rhythm under the shipped `framed=false` wrapper.
11. KC-34's post-repair divergence: whether a model-driven `formattedCSS` update reaches a highlighted `<pre>` without a remount once the blanking is removed — gates the KC-8 repair order.
12. KC-15's settlement: whether `.play()` on a null-targeted group resolves (reaching `removeKeyframeData`) or wedges the delete — decides that row's true severity.

## Identity guards (anti-rename) & folds

- **L-9 = C-D-7 = C-D-9 = census F-1** (phantom `@mkbabb/glass-ui` dep; re-verified at HEAD by both readers and this seat) — **FOLD by reference**; identity owned by lane-frontend §10 step 1, exactly as kf-CSSCodeEditor's KF-CE-32 folded it. Never re-booked. The barrel-vs-subpath inconsistency inside the 2-file pair is a named instance of lane-frontend §3.1 — extends that census row, no new booking; the subpath swap rides the F-1 declaration.
- **KC-13 = KF-CE-16** (the vue-tsc gate hole) — one identity, folded to **KF.W4**; this corpus adds the KC-37 and KC-17 witnesses to that row, plus the causal-chain correction (killed #12).
- **Axis C's S-C R1 note** (the parser-crash class unreachable from this component; the parse seam sits at KeyframesEditor.vue:123 `parseCssScalar`) — folds to the megatranche **R1** identity in the parser lanes (KF.W2/KF.W3), matching the kf-CSSCodeEditor precedent; no new row.
- **KC-20 → glass-ui BH relay** (owner edict, standing formation invariant), carrying this seat's producer-state correction: request against `--ink-seam` 0.08 + a `.separator` forced-colors arm, NOT against the deleted `--separator-ink`.
- **Census S-6/S-7 context** (Skeleton/Button+Tooltip unreached) — extended exactly as axis D recorded (KC-8, KC-23); no contradiction of the census survives adjudication.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet)**: reader-LC's confirmed L-13 cell repeats the challenger's "writes raw integers back / would emit 5000%" mechanism — a mechanism reader-DU had already executed to refutation, and which this seat's own node run settles (the write throws). LC's surrounding work on the same surface (the D-5 microtask proof, the L-4 causal-chain kill, the ill-typed :43 find) was genuinely its own; the half-inherited cell inside an otherwise-strong return remains detectable. The epoch rule stands.
2. **Reader-DU's M-4 MAJOR** is the one severity this seat demoted against a reader (KC-25, MINOR + promotion trigger). DU's position — a documented single-source contract breached at its top rung is MAJOR regardless of pixels — is preserved here; if SS-13 witnesses the punch-through, DU was right and the row re-prices.
3. **The D-5 severity split** (DU MINOR-as-narrowed vs LC MAJOR-as-proven) dissolved into the render/paint layer split rather than being ruled — but the readers' *methods* disagreed more than their facts: DU falsified a paint, LC proved a render. Both were correct about their own layer. Preserved as a method lesson: "blank" is two claims wearing one word.
4. **Unresolved severity ordering**: DU ranks the a11y triad (KC-3/4/5) as one cluster headed by KC-3; LC's return implies the type gate (KC-13) outranks everything as the sequencing head. The CARD-UNIT + KF.W4 split honors both; the spec author should read KC-34's ordering edict first regardless.

## Closing verdict

**ADJUDICATED — RED.** 37 rows stand (3 BLOCKER · 10 MAJOR · 17 MINOR · 7 INFO); 12 claims killed with refutations preserved, two of them verified dead by this seat's own node runs and one of them a re-reader's confirmed cell; 13 corpus-missed defects adjudicated in, including one of this seat's own (KC-9). The offset authoring loop is dead three independent ways — readout (`[object Object]`), writer (a frozen-selector TypeError on the primary control), and removal (a mouse-only `<svg>`) — while the component's architecture (the child-ref contract, the closed round-trip, the two-writer discipline) is repeatedly *right*; every defect is a line, and every line sits in the one surface nothing type-checks. Routings: the repair coheres as ONE proposed wave-surface (**CARD-UNIT, NO-WAVE-OWNER** — SS-1/SS-2 spec authoring to consume, sequenced after the KF.W4 vue-tsc gate, internally ordered by KC-34's highlight-before-blanking edict), with discrete rows to **KF.W4** (the KF-CE-16 gate identity + two new witnesses), **KF.W6** (focus-contract/version-bump note, rhythm, tap-floor, muted token, Label conformance), **KF.W8** (colocation), **KF.W9/SS-13** (twelve live witnesses, one carrying a severity-promotion trigger), one re-scoped **glass-ui BH relay**, and two folds by reference (census F-1; megatranche R1). Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.

— end of apotheosis. The three challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/keyframes/components/KeyframeCardList.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **KeyframeCardList.vue=+14/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**THE UPSTREAM RELAY PREMISE IS 8.0.0-SOURCE, NOT ARTIFACT — re-derived against the CONSUMED dist** (C-13's law, G-0.10). D-10(b)'s verification rests on an 8.0.0 read: *"`--separator-ink` is deleted upstream in the fainter direction (`--ink-seam` 0.08)."* At the consumed 7.0.0 artifact: ⟨`grep -rl -- '--separator-ink' node_modules/@mkbabb/glass-ui/dist`⟩ → **2 files** (it SHIPS), ⟨`… '--ink-seam' …`⟩ → **0 files** (the successor is ABSENT). **The KILL of D-10(b) stands; the relay must be re-scoped (KC-20) before any consume slice**, because as written it requests a landed change whose sign the consumed artifact cannot see.

**F-12 invokes the KF-CE-32 precedent by name**, so the precedent is made legible at `kf-CSSCodeEditor`'s bank: identity ownership stays with `lane-frontend` §10 step 1; what KF.W0 discharges is the **F-1 substrate leg**.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

