# fr-FullscreenViewer — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Date**: 2026-08-24 · **Registry HEAD**: value.js `053f7d59` (branch `tranche-u`) · **Subject tree**: fourier-analysis `cd26c65` (branch `m/w1-bump-migration`) · **Installed producer**: glass-ui **4.0.0** (`web/node_modules/@mkbabb/glass-ui/package.json`, re-read this seat) · **Component**: `web/src/components/visualization/FullscreenViewer.vue` (245 lines, read whole this seat).
**Corpus**: 3 challenge axes at `docs/tranches/V/megatranche/audit/fourier-components/FullscreenViewer/` (challenge-D 643 L · challenge-L 293 L · challenge-C 194 L, immutable per E-1/E-3) · **Re-readers adjudicated**: design-and-usage (return 1) · library-and-code (return 2), both hostile full-corpus re-reads.
**Method (M-23/L-18)**: every verdict-level disagreement between the two re-readers was re-derived against the tree with this seat's own instruments before ruling; every load-bearing kill was re-attacked. **One re-reader verdict is OVERRULED on seat evidence (return 2's D-D-4 CONFIRMED-MAJOR, killed at K-1), one re-reader superlative ratification is OVERRULED (return 1's S-5 keep of C-S1, killed at K-11), one re-reader mechanism is corrected into a severity ruling (both returns' D-13 "lands on a hidden control" → the seat's inert-no-op → body derivation, R-3), and one seat-minted kill lands against return 2's own confirmation cell (the L-M6 z-140-dissolves-z-130 arithmetic, K-12).** Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**Seat instruments (all static + node against installed dist; zero browser probes — probe parsimony)**: FullscreenViewer.vue whole; `dist/styles/utilities/a11y-overrides.css` whole (the file BOTH corpus axes and return 2 failed to open — decider for K-1/K-2); `dist/styles/utilities/base.css:245-285` (tap-squish + its PRM `scale:1` reset); `dist/styles/tokens/scale-paper.css` (`--scale-hover-btn:1.05`, `--scale-press:0.96`, `--scale-press-btn`→`--scale-press-sm` 0.97), `scheme-motion.css:336-348` (z ladder byte-verified: controls 20 … popover 130 · modal 140 · fullscreen 150 · toast 160), `offsets-sizing.css:136-151` (`--ui-scale:1` / `--ui-coarse-scale:1.5` / `--control-h-md: max(calc(2.5rem*var(--ui-scale)), var(--control-floor))`), `light-dark.css:15-21`; node extraction of `dist/button-BNDWhAZb.js` (base = `btn-pill tap-squish focus-ring … active:scale-(--scale-press-btn) …`; `icon: "h-(--control-h-md) w-(--control-h-md) p-0"`; `default`/`glass` arms identical; `data-size` reflected ×2); portal wrappers confirmed in `TooltipProvider-B3MkB_8P.js` (TooltipPortal + `z-tooltip …`), `SelectScrollDownButton-C1jb3b3K.js` (SelectPortal + `z-popover min-w-32 …`), `dropdown-menu-BJ7E9js_.js` (DropdownMenuPortal + `z-popover min-w-32 max-h-[60vh] …`); `dist/dock.js` layer render node-extracted (`dock-layer--full` at char 22371 BEFORE `--summary` at 22520; `inert: !expanded||void 0` on full); `dist/styles/dock/layers.css:118-155` (inactive layer `opacity:0;visibility:hidden;pointer-events:none` + the docblock rejecting `display:none` BY NAME); consumer: `useCanvasSetup.ts` whole, `useViewState.ts` whole, `App.vue:22-31`, `AnimationControls.vue:1-80` (declared surface = `{activeBases?, maxWidth?}` props + `exportFrame` emit ONLY; Tooltips :66/:81/:94/:105; SpeedSelect :96/:117; `DropdownMenu :modal="false"` :103; `:collapse-delay="2000" :start-collapsed="true"` :60-61), `BasisCanvas.vue:515-546`, `ContourEditorCanvas.vue:225-303`, `VisualizationView.vue:72/:203-207/:282-292` + `.editor-shell` grep (:203/:393/:405), `style.css` (`:3` imports glass styles whole; `:25` body safe-area; `--viz-*` overrides = **amber only**, :120/:125), `index.html:5` (`viewport-fit=cover`), `grep -rn ullscreen e2e/` → 1 comment (gallery.spec.ts:113). Banked-registry sweeps: fr-AnimationControls (R-3/R-4/D-1/D-4/K-11/C-12), fr-BasisCanvas (D-1·BC-1·C-1 / BC-10·C-9 / D-4·BC-2·C-2a), fr-ContourEditorCanvas (D/B-3 · D/B-5 · C-9·L-12), fr-CanvasControlsDock (D-1 / M-2 / M-3 / M-9), fr-AdminAuditLog (AA-2), fr-CanvasOverlayButton (FR-COB-2 + the K-12 budget correction) — all row-verified by grep this seat.

## Scoreboard

**40 adjudicated-confirmed rows (2 new BLOCKERS + 4 blocker-weight FOLDS · 8 new MAJORS + 4 MAJOR folds · 13 MINOR · 9 INFO) · 12 claims KILLED (incl. one re-reader verdict overruled and one re-reader confirmation cell corrected) · 9 corpus misses banked (5 from return 1, 3 from return 2, 1 seat-minted) · 10 UNPROVEN-NEEDS-LIVE residues → SS-13.**

The component in one sentence: a hand-rolled modal that fails precisely as a modal — every floating affordance it owns paints behind its own opaque backdrop, its focus trap leaks in one dock state and dead-keys in the other with the two states flipping on a 2-second timer, one Escape closes two layers, its edit seat silently discards work (banked), and its close ritual dumps keyboard focus at `<body>` in the resting case — while its internal listener bookkeeping, degenerate-case handling, and motion-property discipline are genuinely above the tree's median.

## Ruled disagreements (the seat's actual work)

| # | Question | return 1 (design-and-usage) | return 2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|---|
| R-1 | **D-4** — does the scoped `width/height: 2.5rem` break the WCAG 2.5.5 coarse floor (40×40 exit)? | KILLED (a11y-overrides floor; MINOR residue) | **CONFIRMED MAJOR** ("the scoped rule pins 40px") | **KILLED — return 1 wins; return 2 OVERRULED** | `dist/styles/utilities/a11y-overrides.css:106-122`, read whole this seat: `@media (pointer: coarse) { [data-size="icon"], … { min-block-size: var(--touch-target, 2.75rem); min-inline-size: var(--touch-target, 2.75rem); } }` under the comment "`min-block/inline-size` only RAISES — a larger explicit size is never lost". Button reflects `data-size` (×2 in the dist chunk, node-extracted); reachability `style.css:3` → glass styles whole. `min-*` beats `width`/`height` at **used-value time**, cascade-independent — the coarse exit is **44×44**, never 40×40. Return 2's D-D-4 cell walked `--control-h-md`/`light-dark.css` and never opened the overrides file; challenge-D's own falsifier list (:178-189, re-read) names only `--ui-scale` overrides and specificity — the floor is a third path neither ran. Residue re-booked at FV-11 (MINOR): the scoped size DOES defeat `--ui-scale`, so the exit sits at 44px while every sibling icon control grows to `max(2.5rem×1.5, 2.75rem)` = **60px** — proportion discordance, not an accessibility failure. |
| R-2 | **C-S1 / D-S5** — is the `:deep()` block's `flex:1; min-height:0` the file's hardest correct decision? | RATIFIED (its S-5: "the best single superlative in the corpus") | KILLED (children already declare the pair) | **KILLED — return 2 wins; return 1's S-5 ratification OVERRULED** | Seat reads: `BasisCanvas.vue:531-536` root `.canvas-container { … flex: 1; min-height: 0; … }` and `ContourEditorCanvas.vue:285-297` root `.editor-shell { flex: 1; min-height: 0; … }` — both children declare the pair on their own roots in their own scoped blocks. C-S1's stated falsifier ("delete :169 and the canvas overflows") **fails**: the children re-assert identical values. The `:deep()` flex pair is literal cargo; credit survives only for the chrome strip (border/radius/shadow + the `:hover` reset). Booked as FV-14 (MINOR duplication) and S-5-halved. |
| R-3 | **D-13 severity** — unguarded focus restore | MINOR ("restore lands on a hidden control — a lesser defect than filed") | MAJOR ("focus lands on an inert/hidden control rather than on `<body>`") | **MAJOR — return 2's severity, on a mechanism NEITHER reader completed** | Both readers stopped at "lands on a hidden control". Spec: `focus()` on a non-focusable element is a no-op, and `inert` elements are not focusable. The trigger (`CanvasControlsDock`'s DockIconButton) lives in the inline GlassDock's **full layer**, which carries `inert` whenever the dock is collapsed (`dock.js` `inert:!expanded`, node-extracted) — and collapsed is the RESTING state (`:start-collapsed` + 2000ms idle-collapse; banked fr-CanvasControlsDock **M-2** proves it's also keyboard-sealed). During fullscreen the inline dock idles behind the opaque backdrop → collapsed. So at close, `lastFocused?.focus?.()` (FullscreenViewer.vue:82) **no-ops**, focus stays inside the leaving subtree, and the `v-if` teardown 200ms later drops it to `<body>` — the restore silently fails **in the common case**, not the edge case. challenge-D's original "can dump the user at `<body>`" is resurrected by a mechanism it didn't have. FV-10, MAJOR. |
| R-4 | **C-B1's blast-radius limb** — does a click at the occluded menu's coordinates close the whole viewer? | KILLED (swallowed by `.fs-container`) | Re-asserted inside its confirmed[1] ("its clicks land on the backdrop's close handler") | **KILLED — return 1 wins; return 2's cell is internally inconsistent** | `.fs-container` (:108, `width:100%;height:100%`, :155-161, no box-model offsets) exactly covers the `position:fixed;inset:0` backdrop and paints above it in the backdrop's own stacking context — the hit target at any coordinate is the container or a descendant, `event.target` can never equal the `.self` node (:107). The occluded click is **swallowed** (a no-op), the viewer does not close. Return 2 confirms exactly this at its own C-M-1/D-D-10 row ("structurally unreachable dead code") three rows below re-asserting the opposite in its B-1 cell. The occlusion BLOCKER itself is unaffected (FV-1). |
| R-5 | **D-2's scroll-lock limb** — does the page behind scroll under the fixed layer? | KILLED (App.vue topology) | Listed "no body scroll lock" inside its confirmed D-D-2 without engaging the kill | **LIMB KILLED — return 1 wins** | `App.vue:24` `h-dvh … overflow-hidden` shell, `:26` `<main class="flex-1 min-h-0 … overflow-y-auto">` — seat-read. The document never scrolls; `<main>` is the sole scrollport and is not an ancestor of the body-teleported backdrop, so the wheel chain (backdrop → body → html) finds nothing scrollable. Banked corroboration: fr-AppHeader FR-AH-10 / fr-App MG-ε establish the identical topology. The other three limbs of FV-2 (role / aria-modal / accessible name / background inert) stand at BLOCKER. |
| R-6 | **Return 2's L-M6 "dissolution" arithmetic** — "adopting a z-modal(140) chassis dissolves L-B1 because a 140 layer no longer occludes a 130 popover" | (not addressed) | Asserted inside its confirmed D-D-2 cell | **SEAT-MINTED KILL (K-12)** | 140 > 130: a z-modal layer still occludes a body-teleported z-popover sibling — the arithmetic is backwards. Adopting the glass Dialog chassis does not dissolve FV-1 by z-number; it makes FullscreenViewer share the SAME nested-popover tier question that C-B1's own widening flags app-wide (SelectContent 130 inside DialogContent 140 at AdminUserList.vue:12,19 / GallerySearchBar.vue:5,12 — unproven, the highest-leverage live probe). The occlusion cure is portal targeting into the layer or a producer tier re-base (GLASS-RELAY), not the chassis's z-number alone. The chassis adoption remains the right cure for FV-2/FV-3 regardless. |

## Adjudicated defect roster

Line anchors are fourier-analysis `cd26c65`; re-resolve before any cure (D-19). Every fold cites its banked identity; **no fold is re-booked**.

### BLOCKERS — booked here

- **FV-1 = D-1 · L-B1 · C-B1 — ADJUDICATED BLOCKER.** Every glass-ui floating primitive opened inside fullscreen paints BEHIND the opaque backdrop: `<Teleport to="body">` (:105) + `.fs-backdrop { position:fixed; inset:0; z-index:var(--z-fullscreen); background:var(--background) }` (:148-153) at **150**, over body-teleported portals at ≤130 — and the conclusion is branch-invariant on whether the consumer's Tailwind emits `.z-popover` (auto still paints below a positive-z sibling). Blast radius at FULL scope, both re-readers' correction of challenge-L's carve ratified by this seat's own dist extractions: the ⋮ DropdownMenu (DropdownMenuPortal + `z-popover…`, `dropdown-menu-BJ7E9js_.js`), **all four transport Tooltips** (AnimationControls.vue:66/:81/:94/:105 → glass TooltipContent composes reka TooltipPortal, `TooltipProvider-B3MkB_8P.js`), and the **SpeedSelect popup** (:96/:117 → glass SelectContent composes SelectPortal, `SelectScrollDownButton-C1jb3b3K.js`). `data-glass-dock-portal` CSS consumers = 0; reka `Teleport.js` defaults `to:"body"`; no override anywhere in the chain. Blast-radius corrections sealed both ways: occluded clicks are **swallowed** by `.fs-container`, they do NOT close the viewer (R-4); and the fullscreen transport's Export / Easing / mobile-Speed affordances are invisible-but-focusable. Not banked anywhere in the 32-record fr-registry (`occlud|backdrop|z-fullscreen` sweep adopted from return 1, re-spot-checked). **→ F.W3/W4** (consumer: portal the transport's floaters into the layer, or adopt the Dialog chassis WITH the tier question resolved) **+ GLASS-RELAY** (producer: portal tiers are not re-based per nesting level — one letter with the C-B1 widening probe, per the standing BH-relay edict). Rendered paint → SS-13.
- **FV-2 = D-2 · C-M3 · L-M6 — ADJUDICATED BLOCKER (3 of 4 limbs; scroll-lock limb killed, R-5).** Full dialog behaviour, zero dialog semantics: zero `role=`/`aria-`/`inert` tokens in the file (grep 0); `.fs-container` carries only `tabindex="-1"` (:108); body is the sole ancestor; no accessible name, no `aria-modal`, no background inert. The primitive is in-tree one directory over (`ExportModal.vue` imports `@mkbabb/glass-ui/dialog`). **→ F.W3/W4** — adopt the dialog chassis; FV-3/FV-5/FV-6/FV-10/FV-17 all dissolve into the same adoption; the z-tier caveat of R-6 rides.

### BLOCKER-weight FOLDS — by reference, never re-booked

- **FB-1 = L-B2 · C-B2 (+ the fr-CanvasControlsDock M-3 statement of the same identity)** → **fr-ContourEditorCanvas D/B-5 · L-2 · C-3 (ADJUDICATED BLOCKER → F.W3/W4)**: fullscreen edit mode mounts a second `<ContourEditorCanvas>` with three props, no ref, no listeners (:115-120); the rail is `v-if="!isEditing"` (:130); saves read only the inline `editorRef` — edits silently discarded. Re-verified live this seat (:115-120, :130; VV:203-207 inline `ref="editorRef"` hidden by class not v-if).
- **FB-2 = L-B3** → **fr-ContourEditorCanvas D/B-3 · L-1 · C-1 (ADJUDICATED BLOCKER)**: the duplicate editor installs a second unguarded `window` keydown; ⌘Z/Delete in fullscreen mutate the hidden inline editor.
- **FB-3 = C-M6** → **fr-BasisCanvas D-1 / BC-1 / C-1 (ADJUDICATED BLOCKER → F.W1)**: the fullscreen canvas draws `#888888` — `colors.ts` cannot parse the oklch `--viz-*` tokens. Correction ratified (return 2, seat-verified): `style.css` overrides **only `--viz-amber`** (:120/:125) — the corpus's "--viz-green survives" sub-claim is false and the row is UNDER-claimed; that correction is itself already banked (fr-App B-1).
- **FB-4 = D-5 / D §5** → **fr-AdminAuditLog AA-2 (ADJUDICATED BLOCKER, census-extension → F.W1)** + fleet identity **fr-BasisSelector M-7** via **fr-CanvasOverlayButton FR-COB-2** (which names FullscreenViewer.vue:105-125 as a live site and corrects the budget to 9 `variant="glass"` sites / 36 `size="icon"` sites under K-12): `<Button variant="glass" size="icon">` (:110) is a hard break at the glass 7.0.0 pin (both readers re-proved it at the v7.0.0 TAG — baseline law satisfied; challenge-D's working-tree citation is the K-12 provenance kill, that tree now reads 8.0.0). This file contributes exactly its two prop sites. **The FR-COB-2 re-key limb rides**: 7.0.0 keys the coarse floor on `[data-control-target]`, so a mechanical `size="icon"`→`size="md"` sweep silently drops the 44px floor — only `size="md" iconOnly` preserves it; FV-11's scoped-size cure must land with that limb in view.

### MAJORS — booked here

- **FV-3 = L-M1 — ADJUDICATED MAJOR. The corpus's strongest un-banked find; every link re-derived this seat.** The trap's sole visibility predicate `el.offsetParent !== null || el === document.activeElement` (:43-45) is blind to `visibility:hidden` + `inert`: glass-ui hides the inactive dock layer with `opacity:0;visibility:hidden;pointer-events:none` (`layers.css`, docblock rejecting `display:none` BY NAME to keep FLIP geometry), which retains a layout box → non-null offsetParent → the hidden layer's controls pass the filter. dock.js renders `--full` BEFORE `--summary` (chars 22371/22520, node-extracted), so with the dock EXPANDED the DOM-last "focusable" in `.fs-container` is the **inert collapsed-slot mini play button** (AnimationControls.vue:66-73, a real `<button class="play-btn play-btn--mini">`): forward Tab matches neither branch (:64) → native Tab **escapes to the background document**; Shift+Tab from `.fs-close` (`first`) → `last.focus()` on an inert node → spec no-op → **dead key**. Collapsed happens to work; `:collapse-delay="2000"` + `:start-collapsed="true"` flip correctness on a timer. Not banked (fr-CanvasControlsDock M-2 is the dock's own keyboard-sealing — different mechanism). **→ F.W3/W4** (dies with the FV-2 chassis adoption); observed destinations both states → SS-13.
- **FV-4 = L-M4 — ADJUDICATED MAJOR (unbanked; fr-BasisCanvas read the same files and did not book it).** The fullscreen canvas is measured under the enter transform and permanently sized at 95%: `useCanvasSetup.setupCanvas()` reads `containerRef.getBoundingClientRect()` and writes **inline** `canvas.style.width/height` (seat-read whole); the child's `onMounted` measurement runs before the backdrop's post-render `transition.enter` removes `.fs-enter-from { transform: scale(0.95) }` (:237-240); ResizeObserver reports the untransformed layout box so the 0.95→1 settle fires no callback; the only other `setupCanvas()` calls are `if (!surface.value)`-guarded. `.canvas-el { position:absolute; inset:0 }` is over-constrained by the inline size → pins top-left with a ~5% dead band. ContourEditor branch correctly exempt (viewBox-driven). **INFO rider (return 2's, ratified)**: fullscreen `exportFrame()` (:138) copies `canvasRef.width/height` → PNGs at 95% of inline-seat resolution — a third silent divergence on the already-banked forked export contract; rides fr-BasisCanvas D-4/BC-2/C-2a + fr-AnimationControls C-12, no third booking. **→ F.W3/W4** (`@after-enter` re-measure or transition-aware setup); band fraction → SS-13.
- **FV-5 = D-6 · L-M3 · C-M2 — ADJUDICATED MAJOR.** Containment is physical DOM ancestry only (:60/:64 `containerRef.contains(active)`; no `composedPath()`, no layer registry), so every body-teleported descendant (proven at FV-1) reads as "outside": Tab inside the open ⋮ menu is preventDefault'd and focus is pulled out of the menu (reka MenuContentImpl preventDefaults Tab but does not stop propagation). Return 2's refinement adopted: the Shift+Tab arm targets `last`, which under FV-3's expanded state is inert → dead key rather than yank — direction-dependent misbehaviour, one mechanism. `:modal="false"` (:103) is precisely the mode where reka does not compensate. **→ F.W3/W4** (same chassis cure).
- **FV-6 = D-7 · L-M2 (C-m8's hedge killed, K-9) — ADJUDICATED MAJOR.** One Escape closes two layers: `:93-95` emits `close` on any Escape with no `defaultPrevented` check, no layer stack, listener on `document` for the component's whole life (:100-101). reka's DismissableLayer listens on **window** (VueUse `onKeyStroke` default), CONSUMES `defaultPrevented` and never sets it; document precedes window in the bubble path unconditionally — the ordering is spec-determined, not registration-dependent. **→ F.W3/W4**.
- **FV-7 = D-10 · C-M1 — ADJUDICATED MAJOR.** `@click.self` dismissal is structurally dead: `.fs-container` covers the backdrop exactly (:107/:108/:148-161, seat-read — no margin/padding/inset/max-width anywhere in the box chain); `.self` can never match. A documented affordance that does not exist; also the premise of R-4's kill. **→ F.W3/W4** (the chassis's outside-click semantics replace it).
- **FV-8 = D-9 · C-m4, press mechanism CORRECTED (return 1's miss, ratified) — ADJUDICATED MAJOR.** `.fs-close:hover/:active` (:198-206) re-implements the glass chrome with ad-hoc `color-mix()` literals over `--glass-bg/border-resting` (unlayered scoped CSS beats layered utilities — C-m4's cascade mechanism is the precise one) — a `feedback_glass_ui_first_class` violation. **The press does not replace the token, it COMPOUNDS with it**: the local rule sets the `transform` property (`scale(0.95)`) while the library presses via the independent `scale` property (`.tap-squish:active { scale: var(--scale-press) }` = 0.96, base.css seat-read; the cva base also carries `active:scale-(--scale-press-btn)` = 0.97) — the properties compose, so the used press is ≈ **0.96×0.95 ≈ 0.912** (0.97-arm: ≈ 0.9215), ~2.4× the house depth. All three axes read it as a 0.97→0.95 substitution; a token-swap cure would leave the compounding intact. The only correct repair deletes the local `transform` press/hover entirely and lets the library govern. **→ F.W4**.
- **FV-9 = D-11 — ADJUDICATED MAJOR (declaration gap source-certain; device render → SS-13).** No `env(safe-area-inset-*)` on the one layer that escapes the shell's inset: `index.html:5` ships `viewport-fit=cover` (seat-read — insets are LIVE), `style.css:25` pads only `body`, which a fixed body-teleported layer ignores; `.fs-close` `top/right:0.75rem` and `.fs-controls` bottom padding declare no `env()`; glass dock css supplies none (`safe-area` grep 0, adopted from return 1). Composes with FV-11 on the same corner. **→ F.W4**.
- **FV-10 = D-13 · L-m7 · C-m6 — ADJUDICATED MAJOR (R-3: seat mechanism).** The restore path `lastFocused?.focus?.()` (:82) has no `isConnected` guard, no fallback (asymmetric against the open path's `els[0] ?? containerRef` :75-78), fires at leave START under the still-painted opaque layer (the empty `onAfterLeave` :87-89 is the correct hook slot, holding a false comment — see FV-17). Seat completion: in the resting case (inline dock auto-collapsed → trigger's layer `inert`) the restore is a spec **no-op** and teardown drops focus to `<body>` — silent failure in the COMMON case. **→ F.W3/W4** (chassis restore semantics + connected/fallback guard); live destination → SS-13.

### MAJOR FOLDS — by reference

- **FM-1 (return 2's banked-miss, ratified against live source)** → **fr-AnimationControls D-4 / L-2 / C-3 (ADJUDICATED MAJOR → F.W4, "4-line FullscreenViewer repair")**: FullscreenViewer binds `:show-ghost`, `:show-image-overlay`, `@toggle-ghost`, `@toggle-image-overlay` (:131-137) on a child whose ENTIRE declared surface is `{activeBases?, maxWidth?}` + `exportFrame` (AnimationControls.vue:15-30, seat-read) — all four fall to `$attrs`, the two FullscreenViewer emits (:22-23) are unreachable, VV:285's handlers are dead, and in fullscreen the ONLY route to those toggles (CanvasControlsDock, per the banked K-11 ownership correction) sits behind the opaque backdrop at z-20. Fullscreen has **no ghost toggle and no image-overlay toggle at all**. All three corpus axes read the props block two lines below the missing declarations and missed it.
- **FM-2 = D-12 · C-M4 (conclusion) — mechanism SUPERSEDED (K-8)** → **fr-AnimationControls D-1 (ADJUDICATED MAJOR per its R-3 demotion — note: banked severity is MAJOR, not the BLOCKER challenge-D filed)**: `max-width="60rem"` (:135) is inert because the scoped `.animation-dock:where(.expanded)` width rule matches nothing at either mount site (scope-id on `.glass-dock-frame`, class+state on the `$attrs` inner `.glass-dock`) — NOT because of rem arithmetic; the inline `--animation-dock-max-width` is read by nobody; the banked cure is a property rename + rule deletion, not a value change. The headstone comment at :225-227 asserting the prop works is false.
- **FM-3 = L-M5 · C-M5** → **fr-BasisCanvas BC-10 / C-9 (ADJUDICATED MAJOR → F.W3/W4)**: fullscreen doubles per-frame canvas work — the inline canvas stays mounted (VV:282 no v-if) under an opaque backdrop IntersectionObserver `{threshold:0}` cannot report as occluded; `visibleCanvases = 2`, both draw. The banked row already cites this seat's exact lines.
- **FM-4 = D-3** → **fr-CanvasControlsDock M-9 (banked, rides D-1's family → F.W4)**: the sole dismiss control has an empty accessible name — no `aria-label`, lucide glyph `aria-hidden` (:110-112). challenge-D filed it as a novel blocker; it is a banked row. Compounds with FV-25 (autofocus announces the nameless control first).

### MINOR — booked here

- **FV-11 (D-4 residue, R-1)** — the scoped `width/height:2.5rem` (:185-186) defeats `--ui-scale`: the coarse exit holds the 44px floor but sits at 44px while every sibling `size="icon"` control grows to 60px (`--control-h-md` = max(3.75rem, 2.75rem)) — proportion discordance on the layer's most important control. Cure: delete the scoped size (the icon arm already sizes via `--control-h-md`); land WITH FB-4's re-key limb in view. **→ F.W4**; side-by-side render → SS-13.
- **FV-12 (return 1's miss, ratified; the true residue of the killed D-8)** — `.fs-close` re-introduces a spatial hover/press `transform` that glass-ui deliberately RETIRES under PRM: base.css's reset targets the `scale` longhand (`.tap-squish:active { scale: 1 }` under "I retire the press transform entirely", seat-read) and cannot reach the local `transform`; the PRM blanket strips `transform` from the transitioned set, so the jump is un-ANIMATED but still APPLIES instantly — under `reduce`, this one control still jumps while every glass control has surrendered its press. Same cure as FV-8 (delete the local transform). **→ F.W4**.
- **FV-13 (return 1's miss, ratified)** — the fullscreen transport hard-inherits `:collapse-delay="2000"` + `:start-collapsed="true"` (AnimationControls.vue:60-61; no consumer override exists) on a surface where it is the ONLY chrome: 2s after every interaction the whole transport reduces to a mini play button + speed chip, and the collapse timer is exactly what flips FV-3's trap membership. Seat-level design decision no corpus axis examined; fr-AnimationControls audits the dock's own props, not the fullscreen-appropriateness of the contract. **→ F.W4** (seat decision: expose/override the collapse contract in the fullscreen seat).
- **FV-14 (return 2's miss, ratified; R-2)** — the `:deep()` block re-declares `flex:1; min-height:0` both children already declare on their roots — no-op duplication that two corpus superlatives certified as craft (C-S1 killed, D-S5 halved). **→ F.W4** hygiene (delete the pair; keep the chrome strip).
- **FV-15 (return 1's miss, ratified)** — `.editor-shell` is one class name owned by two independent scoped stylesheets (child root ContourEditorCanvas.vue:231/:285 vs VV's wrapper :203/:393/:405 — seat grep), resolving to different elements per seat; and the chrome strip removes border/radius/shadow but NOT the child's `var(--card)` ground + 28px grid (`:291-296`, seat-read) — the "edge-to-edge" fullscreen editor paints a card-tinted grid inside a `var(--background)` layer. **→ F.W4**; appearance → SS-13.
- **FV-16 = D-19 · C-m5** — `:deep()` reach-ins couple the file to two children's private class names; neither child exposes a presentational contract (props seat-verified). C-m5's altitude (the absent contract is the finding) adopted. **→ F.W4**.
- **FV-17 = L-m1 · C-m1 · C-m2 · D-21** — `show` duplicates `props.visible` (written :73/:80, read :107 only); the trap/Escape gate on `visible` (:92) while render gates on `show` — disagreeing for the whole 200ms leave window; `onAfterLeave` (:87-89) is an empty handler whose comment ("the teleport stays in DOM but invisible") is factually false against the `v-if` at :107 — and that falsehood is load-bearing: the v-if teardown is what makes FB-1's data loss possible. **→ F.W3/W4** (dies with the chassis).
- **FV-18 = L-m3** — `{ immediate: true }` (:85) is inert: sole call site `VisualizationView.vue:72` boots `showFullscreen = ref(false)`; the immediate run takes the else-branch whose whole effect is writing `false` over `false`. **→ F.W4** (falls out of FV-17's cure).
- **FV-19 = L-m5** — an in-flight overlay image load survives unmount (`useImageOverlay` raw `img.onload`, guard passes post-unmount because `useCanvasSetup.onUnmounted` only disconnects the RO and never nulls `surface` — seat-read) → full `drawFrame()` against a detached canvas. Correctly bounded to cold-open (module-scoped cache). **→ F.W3/W4**.
- **FV-20 = D-16** — three inset rhythms on one full-bleed surface (0.75rem close corner; 1rem→2rem controls gutters), none token-derived (:179-180/:214/:219-223). **→ F.W4**.
- **FV-21 = D-17** — durations off the token ladder (0.15/0.2/0.25/0.3s at :191-195/:232-235 vs `--duration-instant/fast/normal` 0.1/0.2/0.3 — two literal duplicates, two between-rung) under A.W3.d provenance comments claiming the cure. Easings DO resolve (`--ease-standard`/`--ease-out-expo` live at :root). **→ F.W4**.
- **FV-22 = D-18** — `calc(var(--z-fullscreen) + 10)` (:181/:213) mints an unnamed rung ≡ `--z-toast` (160) inside the backdrop's own stacking context, where any positive value orders identically — buys nothing, collides nominally with a named tier. **→ F.W4**.
- **FV-23 = C-M7** — `imageSlug` duplicates `contour.image_slug`: the overlay URL and its placement rectangle ride unbound sources (`useImageOverlay` derives `resize` from `store.contour.image_bounds`, URL from `store.imageSlug`). Attribution per return 2, ratified: the split is ContourEditorCanvas's contract — this file mirrors VV:283-284's same pair and is the SECOND instance. **→ F.W3/W4** with the ContourEditorCanvas contract cluster; book against the child.

### INFO — confirmed

- **FV-24 = D-20** — `variant="glass"` ≡ `default`, byte-identical cva arms at 4.0.0 (node-extracted this seat); the distinction dies in FB-4's F.W1 migration anyway.
- **FV-25 = D-22** — autofocus targets the dismiss control (`els[0]` = the close Button, first in DOM order); entry announces "close" first — nameless, per FM-4.
- **FV-26 = D-23 · C-m7** — zero automated coverage: the only e2e "Fullscreen" hit is a comment (gallery.spec.ts:113, seat grep); every finding above sits in the gates' blind spot.
- **FV-27 = L-i1** — Teleport census exact: 2 sites tree-wide (PaperSearchModal.vue:41, FullscreenViewer.vue:105); R3-11/X-6 intake ratified.
- **FV-28 = L-i2** — `canvasComponent` latently undefined in the edit branch (:26/:123 v-else-only binding/:138); becomes a live silent no-op the moment FB-1's cure shows controls in edit mode.
- **FV-29 = L-i3** — 245 lines is Goldilocks-OK; the problem is seat, not size — the 40-line trap (:30-98) extracts to the primitive and takes six defects with it.
- **FV-30** — fullscreen export bypasses ExportModal AND (FV-4 rider) writes at 95% resolution; rides banked fr-BasisCanvas D-4/BC-2/C-2a + fr-AnimationControls C-12 — no new booking.
- **FV-31 (return 1's miss, ratified)** — the fullscreen layer is the app's only surface without the house ground: `bg-background paper-texture` lives on the App shell (App.vue:24); the sibling backdrop declares bare `var(--background)` — visible exactly during the 0.95 enter/leave reveal and along FV-4's dead band. One class/token to close; recorded so SS-13 reads the band correctly. **→ F.W4**.
- **FV-32 (return 2's correction, ratified)** — the resize-default count on the overlay operation is FOUR, not three (`lib/api.ts:296` 1024 · `images.py:169` 1024 · `ContourEditorCanvas.vue:94` `?? 768` · `useImageOverlay.ts:17` `?? 768` — the one the fullscreen BasisCanvas actually rides); rides banked fr-ContourEditorCanvas C-9·L-12.

## Killed-claims register

| K | Claim (axis · filed severity) | Refutation (deciding evidence) |
|---|---|---|
| K-1 | **D-4 as filed** (challenge-D BLOCKER "40×40 exit under the 44px floor") **and return 2's D-D-4 CONFIRMED-MAJOR** | R-1. `a11y-overrides.css:106-122` coarse `[data-size="icon"]` `min-block/inline-size: var(--touch-target, 2.75rem)` — used-value floor, cascade-independent, Button reflects `data-size`, reachability sealed. The exit is 44×44 on every coarse pointer. Residue re-booked FV-11 MINOR. Corroborated by banked FR-COB-2 limb (b) reading the same rule. |
| K-2 | **D-8 · L-m4** (MAJOR/MINOR — no PRM gate on the viewport scale transition) | `a11y-overrides.css:6-31` (read whole): PRM rewrites `transition-property: opacity, color, background-color, border-color, box-shadow !important` on `*:not([data-allow-motion])` — `transform` is globally removed from the transitioned set; `.fs-enter-active` renders ≤1 frame at 0.95 and snaps. Both axes named the falsifier and checked only `view-transition.css`/`style.css:92`. True residue re-booked FV-12. |
| K-3 | **D-14** (MINOR — `isEditing && !contour` renders the wrong instrument) | Its own nominated falsifier is satisfied one composable up: `useViewState.ts:37-40` `watch(() => store.contour, (c) => { if (!c && isEditing.value) isEditing.value = false })` (seat-read verbatim; pre-flush), `isEditing` boots false, restore gated on contour. The state is actively closed, not merely unvisited. |
| K-4 | **D-2/C-M3's scroll-lock limb** ("the page behind keeps scrolling") | R-5. App.vue `h-dvh overflow-hidden` shell; `<main>` is the sole scrollport and not an ancestor of the teleported layer. Banked topology: fr-AppHeader FR-AH-10 / fr-App MG-ε. |
| K-5 | **C-B1's blast-radius limb** ("a click at the menu's coordinates closes the whole viewer") + return 2's re-assertion | R-4. Hit target is always `.fs-container` or a descendant; `@click.self` can never match; the click is swallowed. Self-contradictory with C's own M-1 and return 2's own C-M-1 row. |
| K-6 | **L-B1's "Not overclaimed" carve** ("Tooltips are safe … SpeedSelect is likewise safe") | Both re-readers, ratified by seat dist extraction: glass-ui's wrappers compose reka's `TooltipPortal`/`SelectPortal` OUTSIDE the files challenge-L read; all four transport Tooltips and the SpeedSelect popup are body-teleported at ≤130 and occluded. The best-argued paragraph in the corpus is the wrong one. |
| K-7 | **D §5's census-amendment novelty + 27/38 budget + "producer latest 7.0.0" provenance** | Banked three records deep (AA-2 census-extension BLOCKER → F.W1; fleet identity M-7; FR-COB-2 with the K-12-corrected 9/36 budget and the re-key limb challenge-D missed entirely). Provenance is the K-12 trap: the cited path is the producer WORKING TREE (8.0.0 at v8.0.0 tag 2026-08-09); the break itself re-proven at the v7.0.0 tag by both readers — fact survives inside FB-4, amendment is a re-book. |
| K-8 | **D-12/C-M4's mechanism** (rem arithmetic; the `calc(100dvw - 1rem)` clamp; "960px dock in 1600px of backdrop") | Banked fr-AnimationControls D-1: the scoped width rule matches NOTHING at either mount site — both axes reason about a clamp that never runs, predict a widening that cannot occur, and prescribe a value change where the banked cure is a rename + deletion. Conclusion (prop inert) survives as FM-2. |
| K-9 | **C-m8's hedge** ("UNPROVEN — registration-order dependent") | reka registers on `window` (VueUse `onKeyStroke` default), not `document`; document precedes window in the bubble path unconditionally; reka CONSUMES `defaultPrevented` and never sets it. The ordering is spec-determined — the row belongs in the confirmed column (FV-6), not the live queue. |
| K-10 | **C-M6's `--viz-green` sub-claim** ("survives via the app's hsl override") | `style.css:113-128` seat-read: only `--viz-amber` is overridden. `--viz-green` misses all four parser arms too — the row is UNDER-claimed; correction already banked at fr-App B-1. Row folds to FB-3. |
| K-11 | **C-S1 superlative** ("delete `min-height:0` and the canvas overflows — load-bearing, not cargo") + return 1's S-5 ratification of it | R-2. Both children declare `flex:1; min-height:0` on their own roots (BasisCanvas.vue:531-536, ContourEditorCanvas.vue:285-297, seat-read); the falsifier fails; the pair is cargo. Chrome-strip credit survives, halved (S-5 below). |
| K-12 | **Return 2's L-M6 dissolution arithmetic** ("a 140 layer no longer occludes a 130 popover") — seat-minted | R-6. 140 > 130; the chassis adoption inherits, not dissolves, the nested-popover tier question (C-B1's app-wide widening). The chassis remains the right cure for FV-2/FV-3/FV-5/FV-6/FV-10; FV-1's cure is portal targeting or producer tier law. |

## Misses banked (beyond the challenge corpus)

Return 1: press compounding (→FV-8), PRM press residue (→FV-12), fullscreen collapse contract (→FV-13), `.editor-shell` dual ownership + incomplete strip (→FV-15), missing house ground (→FV-31). Return 2: the dead ghost/overlay bindings fold (→FM-1, the corpus's most consequential miss — all three axes cited AnimationControls.vue:25 two lines below the absent declarations), the `:deep()` flex duplication (→FV-14), the 95%-export rider (→FV-30). Seat: the inert-no-op → `<body>` completion of the focus-restore mechanism (→FV-10/R-3) — both re-readers stopped one spec fact short of the severity-deciding consequence.

## Superlatives — with provenance

- **S-1 (C-S3; both returns, ratified)** — `focusableEls()`'s `|| el === document.activeElement` clause (:44): keeps the focus anchor when a transition transiently zeroes `offsetParent` — the guard most hand-rolled traps omit. Load-bearing, not cargo. The sharp irony stands: the trap's internal arithmetic is more careful than the median hand-roll; its failure (FV-3) is entirely in the visibility PREDICATE.
- **S-2 (D-S1)** — the zero-focusables degenerate branch (:51-55): preventDefault + focus the container rather than letting Tab escape; paired with an 8-line stated threat model and no-new-dependency constraint (:30-34). Intent-recording most files skip.
- **S-3 (L-S2; seat re-verified in the whole-file read)** — exactly symmetric listener bookkeeping: hoisted named `onKeydown` (:91), same reference added/removed (:100-101), no options mismatch. Worth stating precisely because every composition defect in this file involves `window`/`document` listeners it does NOT own — its own are clean.
- **S-4 (D-S2/S-3 · L-S3 · C-S2 — all four axes, ratified with bounds)** — motion property discipline: every transitioned property enumerated, both easings live tokens resolving at `:root` (seat-verified), the enter/leave asymmetry (0.25s/0.3s expo in, 0.2s standard out) a considered "arrival is an event, departure is a chore". Bounded by FV-21 (durations off-ladder) and FV-12 (the PRM residue) — NOT by the killed D-8.
- **S-5 (D-S5, HALVED per R-2)** — the fullscreen chrome strip (:163-175) is genuine design judgment (a card frame is meaningless when the surface IS the viewport; the `:hover` reset shows someone chased the residual affordance) — credit confined to the four chrome declarations; the flex pair is duplication (FV-14), and C-S1 is killed (K-11).
- **S-6 (seat-minted in both returns; ratified and extended)** — the corpus's falsifier discipline is what made it cheap to adjudicate AND what killed it: five of this record's kills (K-1, K-2, K-3 across both returns) were obtained by running the corpus's OWN named falsifiers against files their authors never opened (`a11y-overrides.css` twice, `useViewState.ts` once), and the corpus's single best-argued passage (L's carve) is also its wrongest (K-6). A corpus that states its falsifiers can be audited for the price of one file read; publish the branch you could not close (C's B-1 falsifier 2 is the template).
- **S-7 (C-M4's method, credit with the arbiter downgrade both returns agreed on)** — the two-sided CSS-var→typed-prop documentation instinct (:135 + :225-227 + AnimationControls.vue:18-26) is exactly the contract-v2 pattern the constellation wants — and the banked fr-AnimationControls D-1 shows it moved a dead contract onto a dead prop. The matching corpus superlatives (D-S4/L-S1/C-S4) are DOWNGRADED to direction-only accordingly. Diagnosis exemplary; outcome inert.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **FV-1 rendered occlusion** — enter fullscreen; open the ⋮ menu; hover Play; open SpeedSelect; screenshot all three. Expect three focusable, SR-reachable, visually absent surfaces. Source-certain up to paint.
2. **C-B1's app-wide widening (highest leverage; carries the GLASS-RELAY)** — SelectContent (z-130) inside DialogContent (z-140) at AdminUserList.vue:12,19 and GallerySearchBar.vue:5,12. Broken → glass-ui tier law, FV-1 is one instance; working → FV-1's derivation needs the observed mechanism. Decides R-6's residual question.
3. **FV-3 focus destinations** — Tab and Shift+Tab through the layer in BOTH dock states (expanded and idle-collapsed); the cheapest witness that the 2s timer flips the trap.
4. **FV-4 dead band** — `document.querySelector('.fs-backdrop .canvas-el').style.width` vs `innerWidth`; expect ≈0.95×. Corroborative only.
5. **FV-10 restore destination** — close fullscreen after >2s idle (inline dock collapsed); predict `document.activeElement === body`. The seat's inert-no-op derivation wants one witness.
6. **FV-11 discordance** — coarse-pointer emulation: close at 44px beside 60px siblings; invisible to the chromium-desktop-only e2e project.
7. **FV-9 device render** — notched viewport, portrait + landscape: home-indicator vs bottom dock, notch vs close corner (`viewport-fit=cover` + absent `env()` are source-certain).
8. **FM-3 cost multiple** — one trace, fullscreen open, epicycle mode (banked BC-10 arm; milliseconds are the only open fact).
9. **Tailwind emission of `.z-popover`/`.z-tooltip`** — decides only the reported z-number (130 vs auto); FV-1 is branch-invariant. **STALE-DIST WARNING (both returns, ratified)**: `web/dist/assets/*.css` predate the installed producer and contain zero `.z-popover{` — that artifact must NOT be quoted either way.
10. **FV-15 appearance** — the fullscreen editor's card-tinted 28px grid against the bare `var(--background)` layer; FV-31 tells the auditor what the band is so it is not read as a rendering artifact.

## Closing verdict

**GRAVE, WITH CLEAN HANDS INSIDE THE TRAP.** Two new blockers (FV-1 portal occlusion at full blast radius; FV-2 dialog-shaped, semantics-free) and four blocker-weight folds land on a 245-line file whose central defect is seat-choice: it hand-rolled a modal beside the primitive its own sibling imports, and every one of its eight new MAJORs — trap predicate, transform-time measurement, ancestry containment, double-Escape, dead outside-click, compounding press, missing safe-area, focus dumped to `<body>` — either dissolves into the chassis adoption or is a one-declaration deletion. The corpus was strong (L-M1 and L-M4 are the two best un-banked finds in the fourier fleet) and its falsifier discipline let this seat kill twelve claims — including one hostile re-reader overruled on the touch floor and one corrected on its own z-arithmetic — for the price of three file reads. Routing: FV-1/FV-2 cluster + trap family **→ F.W3/W4**; chrome/token/inset hygiene **→ F.W4**; Button-break fold rides **→ F.W1** (AA-2/M-7); export riders stay with their banked owners; producer tier law + collapse-contract letters ride the **GLASS-RELAY**. Nothing here stamps a wave — agglomeration input under M-25.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
