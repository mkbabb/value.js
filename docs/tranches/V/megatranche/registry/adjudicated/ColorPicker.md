# ColorPicker — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed, not inherited — recorded against L-11.
**Date**: 2026-07-27 · **HEAD**: `c654824e` · **Corpus**: 52 accusations (L-1..L-13 · C-1..C-19 · D-01..D-20) against `demo/picker/ColorPicker.vue` and its colocated tree.
**Workers adjudicated**: worker-F (Fable 5, `colorpicker-worker-f-adjudication`) · worker-O (Opus 5 [1m], `picker-colorpicker-adjudication-worker-O`).
**Method (L-14)**: both workers were attacked before either was adopted. All four verdict-level disagreements (C-6, C-7, D-10, D-15) were re-derived against the tree by this seat with its own instruments; two BLOCKER agreements (C-1/D-01, L-2) and a spread of file-level agreements were independently re-measured, not spot-trusted.
**Arbiter's own instruments this session**: full source reads of ColorPicker.vue (whole) / ColorComponentDisplay.vue (template + the `.readout` CSS mechanism) / useHeaderCondense.ts (whole) / PaneSlot.vue (whole) / usePaletteWiring.ts (whole) / overture.css + animations.css transition families / seat.css:87-90 / header.css:14-28 / shell.css equal-columns / useColorPipeline.ts / useColorParsing.ts / shared/utils.ts debounce / picker-color.ts / color-model.ts:55-72 / valueDomain.ts + readoutReservation.ts docstrings / src/color/model.ts SPACE_SCHEMA / usePaneRouter.ts / App.vue:36-44,:83-91,:323-328 / SpectrumCanvas.vue:1-12 / ComponentSliders.vue:260-290,:340-395 / test/picker-blob-config.test.ts (whole) / W46-W48.md anchors / ARCHITECTURE.md:39,:52,:58; node probes of `dist/subpaths/color.js` (export census) and `dist/subpaths/css.js` (hex battery + `oklch()`); full interrogation of `audit/visual/REPORT.json` (60 rows); five live chromium probes on dev :9000 (C-1 end-to-end, D-15 40ms cold-load sampling, C-7 390×480 real-wheel, L-2 390×844 nav census, C-6 thumb geometry + track click); direct pixel inspection of `shots/safari-desktop-dark/picker.png`.
**Environment disclosure (L-12)**: dev server :9000 UP and API-LESS — no data-backed arm was judged (C-4's stored-palette end-to-end stays a stated hypothesis inside its row); live cell = playwright-chromium ONLY (WebKit and safari-app are separate evidence cells, I-20 — nothing measured here transfers to them); the committed `safari-*` shots are playwright-**webkit** captures (`audit/visual/capture.mjs:10` imports `webkit`; verified) and are cited as webkit, never as Safari-app.

## Scoreboard

**46 CONFIRMED · 3 RESCOPED (L-13, C-6, C-19) · 2 REFUTED (C-7, D-15) · 1 GLASS-OWNED (D-10) · 0 UNVERIFIABLE-HERE at row level.**

Blocker-weight survivors: **C-1/D-01** (one keystroke permanently orphans the hero readout from the model — reproduced end-to-end by this seat), **C-2** (the multiplexed 500ms debounce silently discards a channel edit — structural from the closure, twice live-reproduced by the workers), **L-2** (the entire picker action surface unreachable at mobile — re-measured by this seat), **C-4 call-site arm** (an unparseable palette string wedges the edit machine outside ErrorBoundary reach; the parser crash root = π's R1, not re-booked), **C-3/L-5** (the deactivated picker consumes Cmd+K on foreign routes).

Arbiter re-measurement record (my run, this session, chromium 1440×900 dev :9000 unless stated):

- **C-1/D-01**: `.readout-fig[0]` before = `<span class="fig-int">50</span><span class="fig-frac">.0</span>`; click → Cmd+A → insertText `45` → innerHTML = **`<b>45</b>`** (Vue's two owned children destroyed, rich text accepted), hash → `lab(45%+20+30)`; slider ArrowLeft×12 → hash `lab(43.8%+20+30)`, title `lab(43.8% 20 30)`, meter `43.8%`, readout **still `<b>45</b>`** — permanent orphaning, display/URL/title/AT in three-way disagreement.
- **L-2**: 390×844 (`data-layout="mobile"`) nav aria-labels = `[Save edit, Cancel edit, Switch to slug, Generate new slug, Cancel, Select view, Toggle action bar, Menu]` — **zero** of the six picker actions present at 1440×900.
- **C-7**: 390×480 → card overflow **160** > gate 107.98 (expandedH 183.95 → half-header estimate 91.98 + threshold 16); 8 real wheel ticks → `.picker-header.is-condensed` applied, height 183.95 → **87.29px**. The condense apparatus is live.
- **D-15**: cold-load 40ms sampling of `.pane-shell`: t=225ms classes `overture-appear-from overture-appear-active`, transform `matrix(0.999945, -0.0104718, …, 0, -12)` interpolating toward none, computed transition-duration **0.35s throughout**; classes fully removed by t=590 with the matrix **still settling** (−0.36px) — carried by the element's own `:399` declaration. `--overture-plate-land` = 440ms, unconsumed on this element.
- **C-6/D-10**: four `[role=slider]` thumbs exactly **12×24**; `.channel-slider` box **339.4×24**; a click 20px from the track's left edge moved `aria-valuenow` **0.5 → 0.048** (URL followed to `lab(4.8% 20 30)`) — the operable pointer target is the full track.
- **Node**: `dist/subpaths/color.js` → makeColor/SPACE_SCHEMA/SPACE_IDS/isAnyColor all ABSENT; `parseCssColor`: `#12345`/`#1234567` → `ok:false` (clean), `#abc/#abcd/#abcdef/#abcdef12` → `ok:true`, `oklch()` → **THROWS** `TypeError: Cannot read properties of undefined (reading 'replace')` (= π row R1).
- **REPORT.json**: 60 rows · **15 distinct routes** (hash URLs) · **0** `navError` keys · `h1` = **0 on all 60** · `main` = 1 on all 60 · settleMs min/median/max = 3139/3427/**18905** · the sole console-error row = `safari-desktop-light /#/` `"WebGL: context lost."` · `/#/does-not-exist` allElements **1741** vs `/#/` 1738 (the `componentFor` masking default renders the picker).
- **Counts (scoped to source)**: `demo/ui` = 19 dirs / 29 total lines / 90 import sites; debug scaffold = 281+286+136 = **703** lines; `isTransitioning` = exactly 2 hits (ColorPicker.vue:330/:333); `CSS_NATIVE_SPACES` = declaration only; `src/units` does not exist; `COLOR_SPACE_RANGES|getColorSpaceBound|COLOR_SPACE_DENORM_UNITS` = **zero** hits in `src/`.

## Ruled disagreements (the tri-fold's actual work)

| id | worker-F | worker-O | RULING | Deciding evidence |
|---|---|---|---|---|
| **C-7** | CONFIRMED (dead surface, delete 127 LoC) | REFUTED | **REFUTED — O wins** | This seat's own probe: at 390×480 the card produces overflow **160** (> the 107.98 gate) and 8 REAL wheel ticks condense the header 183.95 → 87.29px. The accusation's universal ("the gate demands more overflow than the picker card can EVER produce") is false; F confirmed it from bands where the card barely scrolls (390×600 overflow 64) or not at all (1440×900, 390×844 — the documented dormant intent, useHeaderCondense.ts:25-34). F's arithmetic was right; the quantifier was wrong. **Replacement finding booked in its place**: ColorPicker.vue:181-185's comment claims the contraction is reachable because "threshold sits below the 390 card overflow (~32px at the tightest witnessed phone band)" — but the SUFFICIENCY gate needs overflow > ~108 at that band, so the comment's reachability claim is arithmetically false even though the mechanism is live at shorter viewports. Correct the comment; delete nothing. Probe note for fresh sessions: an assigned `scrollTop` did not exercise the IO gate for worker-O; use a real wheel/touch scroll. |
| **D-15** | CONFIRMED (dead `transition` declaration at :399) | REFUTED | **REFUTED — O wins** | Source: PaneSlot.vue:112-118's `<Transition>` applies `overture-appear-from/active/to` to the pane component's ROOT element — for the picker that IS `.pane-shell` (ColorPicker.vue:5) — and view swaps apply the `vj-enter` family the same way (animations.css:93-97, :228-234, transforms on both). Live (this seat): the shell carries the appear classes with a live interpolating matrix at 0.35s throughout, and keeps interpolating AFTER Vue removes the classes — only the element's own :399 declaration can carry that tail. F's grep-for-`.pane-shell`-selectors methodology structurally cannot see Vue-applied transition classes. **Replacement finding booked in its place**: the scoped `.pane-shell[data-v-…]` rule (specificity 0,2,0) out-specifies `.overture-appear-active` (0,1,0), so the plate-land arrival runs at `--spring-smooth-duration` (0.35s) instead of the overture's intended `--overture-plate-land` (440ms) — a silent clock override, which also falsifies the hand-derivation behind D-17's 850ms literal. |
| **C-6** | CONFIRMED (WCAG 2.2 SC 2.5.8 failure, 24×24 floor) | RESCOPED (not a conformance failure) | **RESCOPED — O wins** | SC 2.5.8's subject is the region that accepts the pointer action. This seat measured it: a click on the bare track 20px from its left edge (far from the 12px thumb) moved `aria-valuenow` 0.5 → 0.048 and the model followed. The operable target is the full `.channel-slider` box at **339.4×24 ≥ 24×24** — conformance met; the 12px thumb is the drag affordance, and the track click is the SC 2.5.7 non-dragging alternative. What survives: the thumb's narrow grab affordance (ergonomics/taste, PR-12 TIGHTEN) and the demo's own coarse-pointer rung (ComponentSliders.vue:345-357) growing the block axis only — the thumb's inline grab seat is untouched on coarse pointers. The "60 captures" inflation died with L-13. |
| **D-10** | CONFIRMED (ours to book, producer-routed cure) | GLASS-OWNED | **GLASS-OWNED — O wins** | The 12px geometry ships in the producer's own scoped rule — `.slider-thumb[data-v-4f4cab01]{width:calc(var(--slider-thumb-size,1rem)*.75)}` in `node_modules/@mkbabb/glass-ui/dist/glass-ui.css` (extracted verbatim by this seat) — and grep finds NO demo rule setting any thumb width/inline-size (`.channel-slider .slider-thumb` sets cursor/position/needle only, verified :363-395). Both workers agree the cure lands in glass-ui's Slider and rides the standing BH relay; the verdict label follows the geometry's owner. Glass boundary per INBOX I-20: this is a producer seat defect, NOT the I-20 blur-over-ramp prefix seam (which stays unbooked here); the masking-fallback ban forbids a demo `--slider-thumb-size` override as a cure. Disposition: BH-inbox relay with the measured geometry + the track-target counter-measurement; no local patch. |

**Severity dissent recorded, not ruled away (L-1)**: both workers CONFIRMED; worker-F contests the BLOCKER ranking ("this is duplication, not user-facing breakage — I would rank MAJOR"). The row stands CONFIRMED; the ranking dispute is preserved in DISSENT.

## Verdict register — all 52 rows

Every CONFIRMED row carries a reproduction a fresh session can run. Line anchors are HEAD `c654824e`; re-resolve before executing any cure (D-19's lesson).

### L-band (structure)

- **L-1 — CONFIRMED.** `node --input-type=module -e "import('./dist/subpaths/color.js').then(m=>console.log('makeColor' in m,'SPACE_SCHEMA' in m,'SPACE_IDS' in m,'isAnyColor' in m))"` → all false, while `src/color/model.ts` exports all four (:56/:76/:127/:136) and `src/color/index.ts` re-exports none; `demo/color-session/picker-color.ts` buildColor is the 17-case makeColor reconstruction and `CSS_PICKER_SPACES` duplicates the unexported `CSS_COLOR_SPACES`. **O's correction adopted, verified by this seat**: `SPACE_SCHEMA` holds `{channels, hueIndex?, css}` ONLY — no per-channel bounds; the library's sole numeric bound is kelvin 1000..40000 in `createColor` (model.ts). The cure must ADD the bounds to the library, not merely publish the schema. Severity contested (F: MAJOR) — see DISSENT.
- **L-2 — CONFIRMED.** Re-measured by this seat: at 390×844 the nav exposes zero of the six picker actions present at 1440×900 (census above). Source chain verified: App.vue:83-91 mobile PaneSlot has no `:on-mount`; :105/:131 desktop slots carry it; PaneSlot.vue:124 renders `:ref` only when `onMount` exists; App.vue:323-328 `onDesktopLeftMount` is the sole writer of `colorPickerRef`; App.vue:38 feeds `colorPickerRef?.actionBarContext ?? null`. O's imprecision noted: the `nav [id=action-bar]` sub-probe is layout-dependent and not load-bearing; the parity claim is.
- **L-3 — CONFIRMED.** All four edges literal (ColorPicker.vue:108 `../ui/card`, :129 `../color-picker/…/useOverture`, :130 `../shell/useViewManager`, :131 `../palettes/usePalettePorts`); law verified verbatim (ARCHITECTURE.md:39/:52/:58); the App.vue:164 ↔ :129 directory cycle holds. **Scope note (O, verified)**: ARCHITECTURE.md is tranche V's TARGET tree (`demo/app/` does not exist) — a measured gap-to-target with a named authority; may not be closed by editing the doc.
- **L-4 — CONFIRMED.** usePaletteWiring.ts read whole by this seat: 40×50ms poll + give-up warn (:37-58); emitApply's two semantically different arms (:65-73); emitSetCurrentColor's throw-vs-swallow fork (:121-127); emitStartEdit through bare `setTimeout` + retry (:106-119). With L-2, every fork takes its degraded arm at mobile and `emitStartEdit`'s picker call can never run. **O's correction adopted, verified**: emitAddColor (:75-104) reaches the picker only in its `catch` arm.
- **L-5 — CONFIRMED.** Source certain: window keydown bound `onMounted` (:377), released `onUnmounted` (:381) under `<KeepAlive :max>` (PaneSlot.vue:120) — deactivation never unbinds. Both workers proved the behavioral arm live (F: defaultPrevented=true on `#/gradient`; O: CDP listener census 3→3→3 across routes). Repro: SPA-nav to `#/gradient`, dispatch/press Cmd+K (warm the chord first — see C-5), observe defaultPrevented with the picker out of the DOM. The correct `onActivated` idiom sits ten files away (HeroBlob.vue:246).
- **L-6 — CONFIRMED.** This seat's counts: 19 dirs / 29 lines / 90 import sites; every barrel a bare glass-ui re-export; ARCHITECTURE.md:39 forbids the level by name. Picker's own 4 sites sit beside direct producer imports in the same files (ColorPicker.vue:108 vs :124/:138) — dual spelling decided by kind. 90-site mechanical rewrite → its own hygiene wave.
- **L-7 — CONFIRMED.** This seat's parser battery: `#12345`/`#1234567` pass the :227 regex `/^#[0-9a-fA-F]{3,8}$/` and return `ok:false` — the guard's language is wider than the grammar it guards, tripping the 2s `flashParseError` (useColorParsing.ts:54-57/:86). The scattered hex special-casing corroborated (useColorPipeline.ts:118 hex arm verified by this seat).
- **L-8 — CONFIRMED.** defineExpose = 12 members (:331-344, read); this seat's grep: `.parseColor`/`.setCurrentColor`/`.editTarget` zero external consumers, `isTransitioning` never written. Divergent double-wiring verified: App.vue:41-42 commit/cancel ALSO sets `mobilePaneIndex=1`; usePaneRouter's palettes rightProps (`onCommit-edit`/`onCancel-edit`, :156-157 region) does not. usePaletteWiring.ts:21 imports the component type solely for `InstanceType` — the service-locator shape.
- **L-9 — CONFIRMED.** 281+286+136 = 703 lines (this seat's `wc`); static imports :133/:141 + unconditional `<PointerDebugOverlay/>` :103 (read); the deliberate `defineAsyncComponent` contrast for HeroBlob :157 with its rationale is eleven lines away. Coupling arm: ComponentSliders.vue:117 / SpectrumCanvas.vue:52 `inject(POINTER_DEBUG_KEY)!` non-null-asserted. Runtime gates are correct — the defect is chunk weight + coupling.
- **L-10 — CONFIRMED.** color-model.ts:58 `CSS_NATIVE_SPACES` alias (declaration-only, this seat's grep); :61-65 `colorToHexString` pure delegate; :67-72 `toCSSColorString` with dead `_digits=2` (all call sites pass one argument); valueDomain.ts = 49 lines around one delegating arrow. The rename-not-move shim class the no-backwards-compat edict forbids.
- **L-11 — CONFIRMED.** `ls src/units` → No such file or directory (this seat); zero hits for the three cited symbols in `src/` (this seat); the false-provenance docstrings verbatim at valueDomain.ts:7/:12/:43 and readoutReservation.ts:21/:26. The derivation actually reads `PICKER_CHANNELS` — L-1's demo-local duplicate. Same class as the 4c1e9270 cure; these two modules were missed.
- **L-12 — CONFIRMED.** Two live `ColorSpaceSelector` writers of `model.selectedColorSpace` at desktop: ColorPicker.vue:39-44 and AboutPane.vue (demo/scenes/about/, `inline`, direct model-spread write mechanic — verified by this seat); both workers measured 3 comboboxes with two labeled "Select color space" at 1440, one at 390; visible in the committed dark shot (two "Lab ⌄"). `selectedColorSpaceOpen` is component-local (:243), so Cmd+K drives only one.
- **L-13 — RESCOPED** → the `componentFor` masking default + W48's absent structural clauses; **the harness-artifact arm is DEAD on today's bytes** (superseded by re-capture, this seat's REPORT.json interrogation: 15 distinct routes, distinct payloads, 0 navError). Surviving arms verified: usePaneRouter.ts:94 `return ColorPicker;` (unknown pane silently renders the flagship — `/#/does-not-exist` = 1741 elements vs 1738); W48's Work sections carry no clause on defineExpose/colorPickerRef/import edges/mobile action-bar parity (this seat's grep) — W48 as written closes GREEN with L-2 intact.

### C-band (correctness)

- **C-1 — CONFIRMED (BLOCKER; reproduced end-to-end by this seat).** One keystroke into the contenteditable headline destroys Vue's owned children (`<b>45</b>`), and every subsequent model write no-ops against the orphaned vnodes — readout 45 vs URL/title/meter 43.8, for the life of the session. Source: ColorComponentDisplay.vue:21-39 puts `contenteditable="true" role="textbox"` on the span whose `.fig-int`/`.fig-frac` children Vue re-renders. No patch makes a Vue-controlled subtree safely contenteditable; the write path must be deleted. Repro: gate G1/G2 commands below.
- **C-2 — CONFIRMED (BLOCKER; structural).** `updateColorComponentDebounced = debounce(updateColorComponent, 500)` (useColorPipeline.ts:209) over the single-timer, last-args-win closure (shared/utils.ts:22-44, read whole by this seat) — the `component` argument makes each call a distinct intent, so coalescing is data loss by construction. Both workers reproduced live (`l:=30` then `b:=10` 120ms later → `lab(80% 20 10)`, the `l` write gone). Only the readout path routes through it; ComponentSliders calls the undebounced writer — why dragging never shows it and typing always does.
- **C-3 — CONFIRMED.** = L-5's mechanism with the behavioral proof: the deactivated picker preventDefaults Cmd+K on `#/gradient` (F live; O by CDP census + real keys). The same unreached `onUnmounted` leaves both debounce cancels and both stray timers unreached on every route change (C-8/C-14).
- **C-4 — CONFIRMED (call-site arm ours; parser root π-owned).** Parser arm re-run by this seat: `parseCssColor('oklch()')` THROWS TypeError — the pinned π row R1, NOT re-booked here. Call-site arm verified in source: picker-color.ts:110 calls `parseCssColor` with no try/catch; ColorPicker.vue:286 `parseColor(target.originalCss)` unguarded, reached from usePaletteWiring:115-118 inside a bare `setTimeout` — outside Vue error propagation, so ErrorBoundary structurally cannot catch; the throw exits with `preEditModel` set and `editTarget` null — the edit machine wedges. Guarding inconsistent by call site (applyExternalColor unguarded; onPaletteAddColor/onPaletteApply/applyColorString try/catch). **UNVERIFIABLE-HERE inside the row**: the API-sourced end-to-end (stored palette with a crashing string) — API-less environment; `originalCss` also arrives from localStorage-backed palettes, so the trigger is not exotic.
- **C-5 — CONFIRMED.** The predicate at :263-266 tests sampled global STATE (`keys.cmd?.value && keys.k?.value`), not the event, no `e.repeat` guard — while the chord is held, ANY keydown enters the branch, preventDefaults (Cmd+A swallowed), and re-toggles the Select. Both workers reproduced with trusted events once the chord was warm. **Adopted second defect from the same lines (O's re-derivation, corroborated by F's independent probe anomaly)**: the FIRST chord of a session is inert — @vueuse 14.3.0 `useMagicKeys` creates refs lazily in its Proxy getter and ColorPicker touches `keys.cmd`/`keys.k` first inside the handler, so press 1 creates them at `false`, press 2 opens, press 3 closes. Cure: one line of event inspection (`e.key==='k' && (e.metaKey||e.ctrlKey) && !e.repeat`), after which `useMagicKeys` has no consumer and both defects leave together.
- **C-6 — RESCOPED** (ruled above). The 12×24 thumbs reproduce at both bands, but the SC 2.5.8 conformance claim dies on the track-target measurement (339.4×24 operable; this seat: track click moved aria-valuenow 0.5→0.048). Survives: the demo-owned coarse-pointer rung grows the block axis only (:345-357); the thumb's grab-affordance narrowness is PR-12 TIGHTEN territory, producer-routed (→ D-10 relay).
- **C-7 — REFUTED** (ruled above). The condense fires at 390×480 under a real wheel (this seat: overflow 160, header 183.95→87.29). Replacement finding: the :181-185 comment's reachability arithmetic is wrong at the band it cites. Delete nothing; correct the comment.
- **C-8 — CONFIRMED.** :383-384 `.cancel()`s two debounces created by useColorParsing (:92, 2000ms) / useColorPipeline (:209, 500ms), provided app-wide, with live sibling consumers (dock ColorInput). A child disposes state it does not own; cleanup belongs in the owning composable's `onScopeDispose`. Blast radius honestly narrow (KeepAlive means the cancel fires only on real LRU eviction — desktop-left `:max` over more panes); the observable drop stays a stated escape path, NOT promoted (O's methodological line honored).
- **C-9 — CONFIRMED.** test/picker-blob-config.test.ts read whole by this seat: `readFileSync`s ColorPicker.vue and asserts two regexes over its text (:44 no `<HeroBlob @click=`; :49 `writeClipboard(` count 1) — no mount, no behavior. C-1 and C-2 reproduce with this suite fully green: the gate is vacuous. This is the retired `proof:*` grep idiom under a new filename (owner: "overfit junk").
- **C-10 — CONFIRMED.** Child declares `update:[number,string]` (:92-95) but its sole emit is `emit('input', …)` (:33); parent holds the dead `@update` binding at :51 — notably the binding that carries C-2's debounce, so the debounce reaches the model solely through :233. Both delete.
- **C-11 — CONFIRMED.** This seat's grep: exactly 2 hits repo-wide — :330 `ref(false)` (never written) and :333 (its own defineExpose entry). Dead exposed state; the canonical L-8 illustration.
- **C-12 — CONFIRMED.** = L-7's measurement, guard-causes-the-error framing: the guard's accepted language exceeds the parser's, so the guard itself produces the spurious 2s error flash. Dies with the write path (this wave).
- **C-13 — CONFIRMED.** This seat's grep: `parseError` renders only in `demo/shell/dock/ColorInput.vue`; the only aria-live in demo/picker is `aria-live="off"` (ComponentSliders.vue:84). A picker-raised parse failure surfaces on a different component in a different region and never reaches AT. Residual after this wave's deletion: the picker still needs its own `role="status"` channel — booked to the spectrum/axis wave.
- **C-14 — CONFIRMED.** :288 `setTimeout(…,120)` and :378 `window.setTimeout(…,850)`, both ids discarded, neither cleared in `onUnmounted` — which instead cancels the two FOREIGN debounces (C-8): cleanup pointed exactly backwards. The post-eviction write is a stated escape, not driven (honored).
- **C-15 — CONFIRMED.** :198 `inject(COLOR_TARGET_PORT_KEY)` without `!` while siblings :172/:197 (and both other consumers of the key) assert; the port is structurally always provided (App setup → usePaletteWiring → providePalettePorts). Worse than useless: commitEdit's guard (:292) early-returns WITHOUT clearing `editTarget` — a wiring break would freeze edit mode silently. Dead defensive code masking any future break.
- **C-16 — CONFIRMED (picker-owned half), h1 arm re-scoped to the shell.** The route's first heading is `H3[data-slot=card-title]` whose accessible text is the number tuple with three `[contenteditable][role=textbox]` descendants (both workers measured; CardTitle→h3 producer default). **Scope correction verified by this seat**: `h1` = 0 on ALL 60 captures across ALL 15 routes — the missing H1 is shell-owned (the App register's row), not the picker's. V-A137/V-A138 companions separately true on disk.
- **C-17 — CONFIRMED.** :43 `(colorSpace: any)` erases the sole checkpoint of `selectedColorSpace` against `DisplayColorSpace`; repeated at AboutPane. INFO; free with a typed parameter.
- **C-18 — CONFIRMED.** = L-9 on the bundle axis; runtime gates correct, so the cost is chunk weight + policy incoherence (the async-split rationale for the blob argues the opposite policy eleven lines away). Bundle delta not measured by anyone — needs the built artifact.
- **C-19 — RESCOPED** → on TODAY's bytes (this seat's re-derivation): zero `navError` rows; `/#/` safari-desktop-light is the sole console-error capture (`WebGL: context lost.`) and the sole settle outlier (18905ms vs median 3427); the cited 38454ms/`[vite]`-module-failure evidence belongs to a superseded run — recorded as SUPERSEDED-BY-RECAPTURE inside the row, identity preserved. Causal attribution stays open: the dev-server witness is structurally blind (L-12). **Bank (runnable)**: re-capture `/#/` on the BUILT artifact ×2 matrices (`npm run gh-pages && node e2e/smoke/perf/serve-built.mjs` + the capture harness); if GL loss survives HMR-free capture, relay to the glass producer with the trace (HeroBlob wraps glass Blob; glass 7.0.0 ships a `webglcontextlost` handler) — no demo patch.

### D-band (design)

- **D-01 — CONFIRMED.** = C-1 on the design axis, reproduced by this seat: display/URL/title/AT disagree simultaneously after one keystroke — the lying instrument the canon forbids (VISUAL-CONSTITUTION §7 read-only readout; §4 W21-fields-only editing; PROPORTION-AUDIT §5.10 no duplicate contenteditable path — all verified by the workers against canon). The mid-typing reflow arm accepted on the fleet's measurement, not re-driven (not load-bearing — the agreement failure alone is terminal).
- **D-02 — CONFIRMED.** Independently measured twice (F 69.92px, O 70.42px on different live colors) against the canon ceiling φG ≈ 14.87px (PROPORTION-AUDIT §2.2 gap law, verified verbatim by both); mechanism re-derived from source by both and re-read by this seat: `.readout` `min-height: calc(var(--readout-lines,1)*1.12em)` + `align-content: flex-end` bottom-anchor the single painted lab line under a 2-line reservation — the unused line renders as air above the numbers. The :150s comment documents the air as designed; the canon now condemns that design.
- **D-03 — CONFIRMED.** SpectrumCanvas.vue:6-9 verified by this seat: `role="img"` on the pointer-only 2D field, no tabindex, no keyboard path, the defending comment in place. = V-A137, still born-RED; the flagship spatial control is mouse-only. Ours — distinct from the I-20 glass blur seam.
- **D-04 — CONFIRMED.** Both workers measured `grid-template-columns: 512px 512px` (exact 50/50) with a character-identical 8px 8px 0 0 shadow on both plates; shell.css:134 comments "Equal columns always (the kept 1fr↔1fr invariant)" (verified by this seat); canon §3 laws 1/2/8 verified by both. Evidence drift recorded, not verdict-bearing: shadow alpha 0.8 at HEAD (both workers this session) vs the fleet's pasted 0.5 — identical between plates, which is the claim.
- **D-05 — CONFIRMED.** Both measured the same pair independently: 390 → 0.7861 (= 1/√φ to 4dp; floors bind) · 1440 → 0.9750 (ceiling vs cqi bind) — two independently clamped sources with different binding arms cannot hold the ratio. Canon requires the ONE paired P019 clamp (V-A140, in W48's Current RED).
- **D-06 — CONFIRMED.** `.readout` computes Fraunces at both bands (both workers); the `.fira-code` class is gated on `monospace` — false for every numeric channel (source :28, verified by this seat). Canon names Fira Code for the headline arm at exact 1/√φ, three places. Family and ratio are one decision; land atomically with D-05.
- **D-07 — CONFIRMED (picker half), h1 arm shell-scoped.** = C-16 design framing: the readout is expressed with a heading primitive carrying three interactive descendants — both die with C-1's cure. h1=0 is the shell's row (all-60 proof above).
- **D-08 — CONFIRMED.** Two nested `[data-slot=card]` measured live (both workers); `--instrument-title-gap` computes to the empty string; glass-ui 7.0.0 ships `./instrument-chassis` (package exports verified by this seat) unconsumed. **The consequential arm**: PROPORTION-AUDIT §2 ruling 2 opens "Because Picker no longer nests a Card" — FALSE at HEAD; the ruling must be re-premised before any wave executes it. Upstream of D-02/D-04/D-05.
- **D-09 — CONFIRMED.** The token cured a non-governing variable: header.css:14-25's own comment claims 1440/390 now "share ONE rhythm SOURCE" (verified by this seat), yet the label→readout seam diverges ~2.7× with the loose arm at DESKTOP (F 69.92 vs 25.40; O 70.42 vs 25.75) — because the governing quantity is D-02's reservation one file away. The row-gap itself is well-behaved (O measured it), which is the proof.
- **D-10 — GLASS-OWNED** (ruled above; INBOX I-20 boundary). Producer geometry, producer cure, BH-inbox relay with the measurement; masking-fallback ban forbids a local override. Not the I-20 blur-over-ramp prefix seam — that stays unbooked and unpatched here.
- **D-11 — CONFIRMED.** = L-12 design axis: two peer controls over one state at two type treatments (53.28px vs 41.888px Fraunces, both "Lab"), visible in the committed shot and in this seat's pixel inspection. Dies with D-04's About-companion retirement; until then the duplication stands.
- **D-12 — CONFIRMED.** All three sub-defects verified (undiscoverable — no affordance names Cmd+K anywhere in demo/picker; unscoped — :377 window listener with no target/activeElement guard, fires while focus is in the picker's own editable cells or the dock input; held-state re-toggle — C-5). Plus the adopted fourth: first-chord inert (C-5). "Subtraction precedes explanation" applies with force.
- **D-13 — CONFIRMED.** The 3-tuple breaks 2+1 at 390 with the last line 27% filled (both workers; O by Range.getClientRects); mechanism verified by this seat in source: flex-wrap over atomic `white-space: nowrap` `.readout-cell`s with no authored break policy — the break is inherited from the layout algorithm, not designed.
- **D-14 — CONFIRMED.** = C-11 (this seat's grep: 2 hits). One defect, two challenger ids — folded to ONE carry, both ids preserved.
- **D-15 — REFUTED** (ruled above). `.pane-shell` IS transformed by both transition families and its own :399 declaration carries the animation's tail. Replacement finding booked: the scoped rule silently governs the plate-land clock at 0.35s instead of `--overture-plate-land` — which also falsifies D-17's derivation basis.
- **D-16 — CONFIRMED.** The :311 comment states the mechanism in its own words ("exposed, not provided — TopDock is a sibling") + 12 members (this seat's read). Cross-sibling coupling through a parent-held template ref — invisible to types and consumer-side grep; how D-14's corpse survived and L-2's hole was produced. = L-8 design axis; folded, both ids kept.
- **D-17 — CONFIRMED (sharpened by the D-15 ruling).** :288 (120ms) and :378 (850ms, hand-derived at :365-371 from "440ms plate-land + 220ms stagger + slack") re-derive token-owned quantities as literals; neither reads a token or listens for completion; the same file does it correctly at :97 (`@vue:mounted`). Sharpened: the 440ms plate-land the 850 derives from is NOT the clock in effect (0.35s wins by specificity — this seat's measurement), so the literal is already wrong today.
- **D-18 — CONFIRMED.** This seat inspected `shots/safari-desktop-dark/picker.png` directly: the ambient field is the same bright pink→peach gradient as light mode; only the plates flip (seed-tinted brown-maroon, not the canon's restrained-neutral pole); the Blob is a near-white mass — among the brightest objects in the dark frame — while canon classes it inert; the cartoon shadow's legibility collapses against the bright ground. **Evidence-cell note (I-20, verified)**: capture.mjs launches playwright-**webkit** while naming matrices `safari-*` — these are webkit captures; a dark-scheme repair must be re-witnessed in both cells.
- **D-19 — CONFIRMED.** This seat's line-level verification: seat.css:88 = `padding-right` (horizontal Blob clearance), :89 = `min-height` (the vertical reservation); W46-W48.md:161/:176 anchor "the Blob-derived .title-row reservation" on **:88** — a horizontal line named for a vertical cure; the spec's own "re-resolve post-W43/W44" flag never discharged. Both softenings adopted: W48 Work-2 ALSO orders the readoutReservation two-line deletion (the true desktop cure — F), and at 1440 line :89 contributes 0px anyway (O), so the consequence is a wrong-axis deletion plus a mis-attributed cure, not a wholly missed PR-01.
- **D-20 — CONFIRMED.** Source verified by this seat: useColorPipeline.ts:117-122 builds `colorComponents` from `PICKER_CHANNELS` only (alpha never a headline cell) while readoutReservation.ts:96 injects an alpha entry the line-count/fit functions can never receive; the inspected dark shot shows the rail at α 82.7% beside a three-cell headline. Under-report and over-reserve from two unreconciled sources.

## Corrections entered against the worker record (so no wave is authored on them)

1. **L-1 (both workers' cure premise, O caught it)**: `SPACE_SCHEMA` owns NO per-channel bounds — the library cure ADDS bounds, it does not merely re-export.
2. **L-2 (O's own sub-probe)**: `nav [id=action-bar]` absence is not the load-bearing evidence; the six-action parity census is.
3. **L-13/C-19 (fleet evidence both workers rescoped, this seat re-proved)**: the visual matrix at HEAD has 15 distinct hash routes, 0 navError — the 60×-single-route and 30s-timeout arms are stale; re-booking them would manufacture the MT-F006 class again.
4. **C-16/D-07**: h1=0 is `{0}` across all 60 captures — shell-owned; a picker wave claiming the H1 closes on a row it cannot verify.
5. **D-19 (challenger overstatement, F's softening adopted)**: W48 Work-2 does also order the true desktop cure; the off-by-one anchor is real, the "wholly misses PR-01" consequence was overstated by half.
6. **C-7/D-15 (worker-F's confirmations, overruled)**: single-band generalization and selector-grep-blindness respectively — the two refutations each replace a false row with a true booked finding (comment-arithmetic drift; the silent plate-land clock override).
7. **C-6/D-10 (worker-F's WCAG booking, overruled)**: SC 2.5.8 governs the pointer-accepting region; the full track qualifies (measured). The producer relay survives; the conformance citation does not.

## THE WAVE — MT-PICKER-1 (re-authored: worker-O's subtraction shape adopted, worker-F's structural gates distributed to the named successors)

**waveId**: MT-PICKER-1 · **title**: "The readout stops writing — one writer, one formatter, one truth (delete the contenteditable headline path, its hex guard, and the multiplexed channel debounce)" · **bornRed**: true.

**Why this shape won (L-14)**: worker-F's MT-PICKER-1 bundled four transpositions (service, write-path, keyboard, parse boundary) into one wave — each individually right, together an arc wearing a wave's name. Worker-O's W-PICKER-1 is pure subtraction, completable alone in one session, kills both data-integrity BLOCKERs on the flagship route, loses no capability (the four sliders + dock ColorInput already ship as editors), and leaves each remaining transposition as a named successor with its own born-RED gate. F's best content — the mobile-parity gate, the foreign-route gate, the edit-machine gate, the W48-A addendum — is preserved verbatim in the successors and the addendum below.

**scope (exact files, exact end state)**:
1. `demo/picker/display/ColorComponentDisplay/ColorComponentDisplay.vue` — delete `contenteditable="true"`, `role="textbox"`, the per-cell `:aria-label`, and the `@input` handler (:21-39); delete the never-emitted `update` declaration (:92-95, C-10); replace the `<CardTitle>` host (:13) with `<output class="readout" :aria-label="…">` — non-heading, non-live, one labelled domain output; the component becomes a pure projection with zero emits.
2. `demo/picker/ColorPicker.vue` — delete `onComponentInput` whole (:223-235), retiring the `/^#[0-9a-fA-F]{3,8}$/` guard (L-7/C-12) with it; delete the dead `@update` (:51) and `@input` (:52) bindings; drop `updateColorComponentDebounced` from the destructure (:215) and from onUnmounted (:384).
3. `demo/color-session/useColorPipeline.ts` — delete `updateColorComponentDebounced` (:209) and its export (:326); `updateColorComponent` stays as the ONE channel writer.
4. `demo/picker/display/ColorComponentDisplay/readoutReservation.ts` — one shown-channel source: the alpha entry (:96) either renders (alpha ≠ 1) or is deleted; headline cells ≡ rail cells (D-20).
5. NEW `e2e/smoke/oracles/o28-readout-projection.spec.ts` — the six gates below.
6. `test/picker-blob-config.test.ts` — delete the two ColorPicker regex assertions (C-9's picker arm); the replacement behavioral coverage IS gates G1–G5.
NO new controls: the surviving channel editors are the four sliders (keyboard + pointer, all four channels incl. alpha — track-click verified this session) and the dock ColorInput, both shipped — pure subtraction, no capability lost.

**structure (L-8: structure over gates)**: C-1/D-01 — the writability is DELETED, not validated: after this wave the readout has no `@input`, no emits, no editable descendants; the defect class is unrepresentable. C-2 — the multiplexed debounce is deleted, not keyed: no coalescing window exists in which one channel's intent can cancel another's; `debounce` keeps exactly one demo consumer (`parseAndSetColorDebounced`, single-argument, where coalescing IS the intent). C-10/C-12/L-7-hex-arm — die as unreachable code with the handler. Only projection agreement (G2) and shown-channel parity (G5) need standing gates — cross-file invariants no single type carries today.

**gates (all RED today; failing inputs pasted; each runnable by a fresh session)**:
- **G1 · THE READOUT IS NOT A WRITER** — no descendant of `.readout` is user-writable; a keystroke aimed at it changes neither its DOM nor `location.hash` nor `document.title` nor any `.channel-meter`. `npx playwright test e2e/smoke/oracles/o28-readout-projection.spec.ts --project=smoke -g "readout is not a writer"`. RED input (arbiter-measured this session): navigate 1440×900 `#/?space=lab&color=lab(50%25%2020%2030)`, focus `.readout-fig[0]`, Cmd+A, insertText `45` → innerHTML `<b>45</b>` (Vue children destroyed) and hash `lab(45%+20+30)` — both clauses fail.
- **G2 · PROJECTION AGREEMENT** — after any independent model write, readout text ≡ meters ≡ hash ≡ title. Same spec, `-g "readout agrees with the model after an independent write"`. RED input (arbiter-measured): after G1, slider ArrowLeft×12 → readout `45` vs meter `43.8%` vs hash `lab(43.8%+20+30)` vs title `lab(43.8% 20 30)`, persisting for the session. The gate MUST contain an independent write — W48's single-frame agreement check passes with C-1 fully present (L-13's lesson).
- **G3 · NO CHANNEL WRITE DISCARDED BY ANOTHER CHANNEL'S WRITE** — two writes to different channels 120ms apart from the picker's channel editors both land. `-g "cross-channel commit independence"`. RED input (workers' twice-reproduced): `l:=30` then `b:=10` 120ms later → `lab(80%+20+10)`, the `30` gone silently. The asserted property names the channel editors by role, not markup — after the cure the same test drives the sliders (driver-independent); any relanding of a channel-multiplexed shared debounce (including under W48's new numeric fields) re-reds it.
- **G4 · THE READOUT IS A PROJECTION, NOT A HEADING OR A CONTROL** — not h1..h6, no `data-slot="card-title"`, zero `[role=textbox]`/`[contenteditable]` descendants; the route keeps exactly one `<main>`. `--project=smoke --project=smoke-safari -g "readout is a projection, not a heading"` (both engines — I-20 forbids inferring webkit from chromium). RED input: `document.querySelectorAll('h3[data-slot=card-title] [contenteditable="true"]').length` = 3 today at both bands. The h1 clause is deliberately EXCLUDED (shell-owned, all-60 proof — carried to W-SHELL).
- **G5 · SHOWN-CHANNEL PARITY** — the headline projects exactly the rail's channel set, alpha present iff alpha ≠ 1, and the reservation table budgets no unrenderable cell. `-g "headline and rail agree on the shown channels"`. RED input: shipped default `lab(92% 88.8 20 / 82.7%)` → rail `['92.0%','88.8','20.0','82.7%']` vs headline `['92.0%,','88.8,','20.0']`; readoutReservation.ts:96 budgets an alpha entry `readoutLineCount`/`readoutFit` can never be called with.
- **G6 · BUILT-ARTIFACT WITNESS (L-12)** — G1/G2/G4 hold against the BUILT bundle, and the bundle contains an application. `npm run gh-pages && npx playwright test e2e/smoke/oracles/o28-readout-projection.spec.ts --project=smoke-perf` (`node e2e/smoke/perf/serve-built.mjs` on :8091). RED today: the contenteditable ships in the built bundle. First assertion under this project: the served root mounts `.pane-shell` at all (MT-F012 proved `gh-pages` can exit 0 with an application-free bundle; the dev witness is structurally blind).

**piObligations**: MATRIX — chromium 1440×900 + 390×844 (dev :9090/:9000) · webkit 1440×900 (smoke-safari) · chromium 1440×900 built (:8091). safari-app is a SEPARATE cell (I-20): nothing inferred across engine cells. ROUTES — `#/?space=lab&color=lab(50%25%2020%2030)` (G1/G2) and `#/` at the shipped alpha default (G5) — hash URLs only (MT-F006 was manufactured by path URLs at a hash router). SELECTORS — `.readout`, `.readout-cell`, `.readout-fig`, `.channel-meter`, `.channel-slider [role=slider]`, `h3[data-slot=card-title]`. COMMITTED CAPTURES (force-added past `.gitignore` `*.png`): `docs/tranches/V/megatranche/audit/components/picker-colorpicker/witness/{before,after}-{1440-readout-orphan,1440-readout-projection,390-readout-lines,webkit-1440-roles,built-1440-readout}.png`. A witness not in the wave's own commit does not exist.

**deltaObligations**: four before/after pairs, each one measurement re-run by the same command, before-frames committed at wave OPEN pinning the RED: (D1) `.readout-fig[0].innerHTML` after insertText — before `<b>45</b>`, after unchanged spans; (D2) the (readout, meter, hash) triple after ArrowLeft×12 — before `('45','43.8%','43.8')`, after all `43.8`; (D3) hash after the l→b 120ms pair — before `lab(80%+20+10)`, after `lab(30%+20+10)`; (D4) `h3[data-slot=card-title] [contenteditable]` count — before 3, after 0, in BOTH engines.

**carries — every one of the 52 leaves with exactly one disposition; original challenger ids preserved for life (anti-rename)**:
- **FOLD INTO THIS WAVE** (closed by its own gates): C-1, C-2, C-10, C-12, D-01, D-20, L-7 (hex-guard arm), the picker-owned half of C-16/D-07 (tuple-as-heading + textbox descendants), C-9's picker arm (the two regex assertions die; G1–G5 are the behavioral replacement).
- **BUILD — W-PICKER-2 "the picker stops being a service"** (L-2, L-4, L-8, D-16, C-15, C-11+D-14 as ONE row, C-17, C-4's call-site arm): ActionBarContext + the commit/cancel edit machine move into a color-session composable PROVIDED by App beside COLOR_MODEL_KEY; defineExpose → 0 members; colorPickerRef + onDesktopLeftMount capture + the 40×50ms retry loop + all masking forks die; the parse seam returns the library Result so an unparseable string is a branch, not a wedge (editTarget/preEditModel invariants restored on failure). Born-RED gates: (a) the 390×844 nav exposes the SAME six picker actions as 1440×900 (today: six vs zero — arbiter-measured); (b) `onStartEdit('oklch()')` leaves editTarget null, preEditModel null, no unhandled throw, a parse verdict on the picker's own status region (today: wedges — parser throw measured, call-site unguarded at :286 inside a setTimeout outside ErrorBoundary reach).
- **BUILD — W-PICKER-3 "one keyboard owner"** (C-3, C-5, D-12, L-5, C-8, C-14, D-17): the shortcut moves to a shell-owned keymap testing the EVENT (`e.key==='k' && (e.metaKey||e.ctrlKey) && !e.repeat`, editable-target-guarded, registered once at the root) or is deleted; `useMagicKeys` leaves with it (kills the first-chord-inert defect too); onActivated/onDeactivated replace onMounted/onUnmounted; the two escaped timers are captured and cleared; the two foreign debounce cancels move into their owners' `onScopeDispose`; the C-7 replacement finding lands here (correct the :181-185 comment — the condense apparatus is NOT deleted); the D-15 replacement finding lands here (either the :399 scoped rule scopes to exclude the appear phase or `--overture-plate-land` governs; D-17's 850ms is re-derived from the ACTUAL clock or replaced by the animationend/`@vue:mounted` idiom already in the file). Born-RED gate: with the picker deactivated on `#/gradient`, no picker-registered handler preventDefaults Cmd+K (today: defaultPrevented=true) AND the first chord of a session behaves identically to the second (today: press 1 inert — measured).
- **BUILD — W-PICKER-4 "the seam has one owner"** (D-02, D-05, D-06, D-08, D-09, D-13, D-19, + re-premising PROPORTION-AUDIT §2 ruling 2): consume InstrumentChassis + the single P019 paired clamp; delete `--readout-lines`/`align-content:flex-end`/the static line table; retire `--picker-header-rhythm`; re-resolve the W48 anchors (seat.css:**89**, not :88 — and at 1440 the true cure is the ColorComponentDisplay reservation, not seat.css at all); authored break policy for the tuple (D-13). Born-RED gates: gap law `I_after ≤ min(φG, I_before − G)` ±0.5px at 1440/390/320/400%-zoom (today ~70px against a 14.87px ceiling) AND label/headline = 1/√φ at BOTH band ends (today 0.9750 vs 0.7861). D-18's dark-scheme recomposition rides here, re-witnessed in BOTH engine cells (the shipped `safari-*` shots are webkit). C-19's bank re-fires under this wave's π run.
- **BUILD — W-PICKER-5 "the spectrum becomes a control"** (D-03/V-A137, V-A138's tablist linkage, C-13's local `role="status"` region, C-6's surviving coarse-rung arm): two named numeric S/V axes over a shared coordinate model. Born-RED gate: `.spectrum-picker` appears in the card's focusable enumeration (today: absent from all twelve stops).
- **BUILD — W-SHELL "one route H1 + exhaustive pane dispatch"** (the shell-owned h1 arm of C-16/D-07; L-13's surviving arm): `componentFor()` exhaustive over LeftPane, throws on unknown (usePaneRouter.ts:94). Born-RED gate: `#/does-not-exist` no longer renders the picker (today: 1741 vs 1738 elements — arbiter-measured).
- **BUILD — W-LIB "publish the abstraction"** (L-1, L-11): re-export SPACE_SCHEMA/SPACE_IDS/makeColor/isAnyColor (ADDITIVE, addenda-not-patch, no signature changes); **ADD the per-channel bounds the library does not yet own** (the O-correction — SPACE_SCHEMA has none today); delete demo buildColor + CSS_PICKER_SPACES; rewrite the two false-provenance docstrings so the prose becomes true because the derivation becomes true. Born-RED gate: the L-1 node census above exits non-zero while any of the four is absent.
- **BUILD — W-HYGIENE** (L-6's 90-site barrel deletion — the picker's own 4 sites may ride any picker wave touching those files; L-10's four shims inlined; L-9/C-18's 703-line debug scaffold behind `import.meta.env.DEV` + dynamic import + optional inject).
- **RELAY, NO LOCAL PATCH — D-10** to the active glass-ui BH inbox (standing fond): the measured thumb geometry (`.slider-thumb{width:calc(var(--slider-thumb-size,1rem)*.75)}` → 12×24) + the track-target counter-measurement (339.4×24 operable; aria-valuenow 0.5→0.048 on a track click) + PR-12's TIGHTEN disposition (invisible grab seat at the producer root). The masking-fallback ban forbids a demo `--slider-thumb-size` override. Distinct from the I-20 GLASS-OWNED blur-over-ramp prefix seam — unbooked here, unpatched locally, cured in glass 8.0.0.
- **FOLD → the V·π library band by original identity**: C-4's parser crash root (= R1, already that band's row — not re-booked, not renamed).
- **RETIRE — C-7 and D-15 (refuted)**, each replaced by its booked standing correction (the :181-185 comment arithmetic; the plate-land clock override) — the corrections are carried in W-PICKER-3 above, so the refutations delete nothing true.
- **BANKS (runnable re-triggers, swept at every wave open)**: (1) glass 8.0.0 adoption re-fires the D-10 relay check — `node -e "const v=require('fs').readFileSync('node_modules/@mkbabb/glass-ui/package.json','utf8'); process.exit(JSON.parse(v).version.startsWith('8')?1:0)"` exits 1 when it lands; (2) the V·π parser gate re-fires C-4's library half — `node --input-type=module -e "const {parseCssColor}=await import('./dist/subpaths/css.js'); try{parseCssColor('oklch()'); process.exit(0)}catch{process.exit(1)}"` exits 1 while R1 stands; (3) C-19's built-artifact GL re-capture (named above).

**completableAlone**: YES — the L-1 one-wave test answered on both clauses. BETTER: the flagship instrument stops lying (both data-integrity BLOCKERs die with the deletion) with zero capability loss. CLOSED ON ITS OWN EVIDENCE: six gates, all RED today with pasted failing inputs (four of them re-measured by this arbiter, not inherited), runnable by one session against dev + webkit + built cells; no other wave lands first. The wave deliberately does NOT touch the chassis, clamps, import graph, or keyboard owner — each is a named successor with its own born-RED gate, so this wave cannot become an arc wearing a wave's name.

## Addendum clause — W48-A (per E-3 addenda-not-patch; appended to `docs/tranches/V/reformation/waves/W46-W48.md`, never rewriting it)

1. **Anchor re-resolution before W48 opens**: the Blob-derived VERTICAL reservation is `seat.css:89` (min-height), not `:88` (padding-right); executing :88 literally deletes the horizontal clearance (D-19). At 1440 neither seat.css line is the cure — the 70px void is owned by the ColorComponentDisplay reservation (`min-height` × `align-content: flex-end`), which W48 Work-2's readoutReservation clause must name as the desktop cure.
2. **Structural completion clauses** (L-13's surviving arm — so W48 cannot close GREEN with L-2 intact): action-bar parity desktop==mobile falsifiable by the 390 nav aria-label census; ColorPicker defineExpose member count 0 achieved by consumer moves and asserted behaviorally, never by source-grep; picker import edges legal per ARCHITECTURE §1; V-A137's two named numeric axes and V-A138's tab→tabpanel linkage asserted by focusable enumeration.
3. **Per-channel commit**: W48 Work-3's "W21 numeric fields the sole editors" MUST land on per-channel commit, never a shared single-timer debounce, or C-2 reships in new markup (G3's driver-independent assertion is the tripwire).
4. **Producer boundary**: the C-6/D-10 thumb-seat cure lands in glass-ui's Slider (producer root), relayed via the standing BH fond; never a per-instance demo override; distinct from the I-20 blur-over-ramp prefix seam (unbooked, unpatched locally).
5. **C-19 attribution** only from the production-build capture named in the bank; the dev-server witness is structurally blind (L-12); the `safari-*` shot directories are playwright-webkit and may not be cited as Safari-app evidence (I-20).
6. **Single-frame checks are insufficient**: any W48 readout-agreement completion check must contain an independent model write (the C-1 orphaning passes single-frame checks).
7. **PROPORTION-AUDIT §2 ruling 2 is re-premised before execution** — its opening condition ("Because Picker no longer nests a Card") is false at HEAD (two nested `[data-slot=card]` measured; `--instrument-title-gap` empty).

## DISSENT — preserved unresolved disagreement, verbatim

1. **L-1 severity (worker-F, unresolved)**: "Dissent on severity only: this is duplication, not user-facing breakage — I would rank MAJOR, but the defect stands as stated." The arbiter does not rule severity ladders; the row carries both rankings.
2. **C-6/D-10 booking (worker-F, overruled but preserved)**: "WCAG 2.2 SC 2.5.8 floor is 24x24. Cure is producer-level (glass-ui Slider seat) and must ride the standing BH relay — NOT the I-20 glass prefix seam; this row is ours to book, producer-routed to cure." Overruled on the conformance citation by the track-target measurement (this seat: aria-valuenow 0.5→0.048 on a bare-track click 20px from the edge; worker-O: 0.92→0.048) and on the booking by the producer-owned geometry (no demo width rule exists). F's relay routing survives intact — only the label moved.
3. **C-7 (worker-F, overruled)**: "The predicate is self-defeating by arithmetic: … the gate outruns the residual overflow at every witnessed band. 127 LoC + sentinel + $el computed + header.css rules are dead surface." Overruled by measurement at an unwitnessed band (390×480: overflow 160, condense fires, 183.95→87.29). The arithmetic was correct; the universal quantifier was not. Worker-O's probe-artifact caveat is preserved for fresh sessions: "an IntersectionObserver gate must be exercised by a real scroll, not an assigned scrollTop."
4. **D-15 (worker-F, overruled)**: "re-grepped every pane-shell selector — seat.css:42 and overture.css target descendants/first-child (the Card), no rule or class toggle sets transform on the shell itself … Dead declaration." Overruled: Vue's `<Transition>` applies its classes to the pane root — no `.pane-shell` selector ever appears, which is exactly why selector-grep missed it. Method lesson recorded for the formation: transition-class liveness is invisible to selector greps; sample the element.
5. **D-04 evidence drift (unresolved, not verdict-bearing)**: the fleet's pasted shadow alpha 0.5 vs both workers' measured 0.8 at HEAD (identical between the two plates — which is the claim). Recorded so the delta pair is authored against 0.8.
6. **Worker-O's methodological line, honored in full**: "I have recorded those as measured-mechanism / undriven-consequence rather than upgrading them on plausibility, and I would resist any arbiter move that promotes them to CONFIRMED-with-repro on the strength of how convincing the source reads." C-8's and C-14's observable drops and C-4's API-sourced end-to-end remain stated escape paths in their rows; no promotion occurred.
7. **C-5 probe nuance (both workers, convergent, preserved)**: worker-F: "my synthetic-dispatch probe reproduced the handler firing with held meta+k state on a later phase but not on the first fresh-page chord — an untrusted-event/lazy-ref artifact of useMagicKeys' proxy, which does not refute the source predicate and is itself a second fragility of the same mechanism." Worker-O derived the mechanism (lazy Proxy-getter ref creation) and measured press-1-inert with trusted events. The two observations corroborate one adopted defect (first-chord inert), booked to W-PICKER-3.
8. **Scope discipline (worker-F, adopted as standing practice)**: ledger rows citing ColorPicker.vue but subjected elsewhere (App.vue's import-type row, area-core's updateToColorSpace, the serializer-digits row, the router-stub row, ConfigSliderPane's contrast citation) were disambiguated by harvest provenance and left to their own components' juries — adjudicating them here would double-book.
