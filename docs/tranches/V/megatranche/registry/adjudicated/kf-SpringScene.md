claude-fable-5

# kf-SpringScene — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Registry tree**: value.js `tranche-u` · **Subject tree**: keyframes.js HEAD `8281638c`, worktree DIRTY (incl. package.json/lock and an in-flight glass-7 prop sweep — see K-1 and the D-17/D-5 worktree riders); `origin/master` `81a56990` pins `@mkbabb/glass-ui: "7.0.0"` as an exact devDependency (this seat's own `git show`, line 77).
**Corpus (r2)**: two fresh hostile re-reader returns over the full three-axis challenge corpus for `demo/scenes/spring/SpringScene.vue` (205 L) + its consumption closure — **reader-1** (design-and-usage: 27 confirmed cells · 12 kills · 4 misses · 9 superlative cells) and **reader-2** (library-and-code: 11 confirmed cells · 12 kills · 4 misses · 5 superlative cells). All three challenge axes (`audit/kf-components/SpringScene/challenge-{D-design,L-library,C-consumption}.md`) consulted directly at every disputed cell (D-1, D-2, D-6, D-23, the D §4 INFO table, L-1, L-3, C-B1/C-B2, C-M3/C-M4/C-M5, C-m3..m6, C-i1/i2).

**Method (incredulity).** Every reader-vs-reader verdict conflict (five head-ons), every kill of a reader confirmation, every claimed-new miss, and every load-bearing fold was re-derived at the bytes before adoption. Zero product-source writes, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SpringScene.vue (205 L — every anchor checked: :29/:55/:56/:67/:81/:83-91/:93-96/:119-122/:136/:138-157/:159-177/:180-200/:191-193), useCompiledEntry.ts (85 L), RibbonBar.vue :1-125 (the three-arm chain :13/:69/:108 + the teleport sink :6-9), StartingStyleTarget.vue :1-80, useSpringDemo.ts :60-75/:185-305/:355-500 (frame gate :197-206, scrubTo :281-289, reseat :294-300, **`toggleTarget = () => reseat(target.value > 0.5 ? 0 : 1)` :303-305 — the D-23 decider**, reset :355-370, facility :404-431, return bag :433-499), useAnimationGroupActions.ts :40-70 (reset = `getGroup().stop(); syncPlayState(false)`), AnimationControlsGroup.vue :106/:140-141/:302-303/:324, TransportDock.vue :158/:347, useControlsKeyboardShortcuts.ts :50-58 (R :52, Escape :51, arrows :53-56), App.vue :29/:214-232 (**the empty fallback AnimationGroup — the Reset-miss decider**), SpringPhysicsFacet.vue :17-20/:119, SpringTarget.vue :448-455, test/demo/state/control-surface-dfa.test.ts :81-86 (verbatim), installed glass-ui 7.0.0 `dist/styles/typography/utilities.css` (**`@utility text-mono-caption { … text-transform: uppercase }` — the uppercase-miss decider**); **executed censuses**: `demo.reset|springDemo.reset` → **0** consumers demo-wide · `dispatch({type:"RESET"})` → **3** sites, all inside orphaned scene `reset()` bodies + the type decl · `samplerCss` → 2 hits, both its own file · `SpringSidebar` → **12 hits / 8 files**, all comments · `contractAnim` → **4** hits, all comments · playback-idiom.css = **87 L** (`.btn-playback-accent` :28, hover :44, `[aria-pressed]` :84 — the K-5 drift confirmed) · AnimationVisualizer.vue = **256 L** (the K-6 out-of-file cites confirmed) · glass-ui at `origin/master` package.json:77 = 7.0.0 exact devDep / HEAD:71 = 6.0.0 optionalDep / worktree = **deleted** (the K-1 decider); **registry cross-reads (identity-guard sweep)**: kf-PlaybackRibbon (D-10+DU-M-3, **C-2 with its :138 pre-assignment "any future kf-SpringScene adjudication folds its raw-feed row to C-2 here"**, N-2, C-11, D-3+DU-M-1, C-14), kf-EasingScene (KF-ES-2/-5/-6/-11/-12/-13/-16/-17/-18/-40/-41/-43 + killed-register #1), kf-EasingSidebar (its own KF-ES-8 — the uppercase row), kf-AnimationVisualizer (KF-AV-1/-2/-15; its instrument list already reads "SpringScene.vue:95-125 (raw-scene latch)"), kf-ControlsPaneWrapper (D-M11/D-m12/M-4/MISS-3, the btn-interactive census naming SpringScene.vue:167), kf-CubeScene (D-2/C-3 naming "SpringScene ×2", D-11), kf-KeyframesStringControls (the three-closures chain, "folded, not re-booked"), kf-AnimationControlsGroup (D-1 "cube 3 · amiga 3 · spring 2", D-11/C-8 clear-arm-only, M-4 Escape), kf-ChannelControls (D-7/L-4, L-3, UNPROVEN #1 "one screenshot of spring"), kf-KfPillTabs (:39), kf-App.skeleton (K11 — F-1 killed at origin/master), kf-SequenceAxis (D-12 `@800MS` + aria-valuetext relay), census CENSUS-2026-08-03 (SCH-1; KF.W0–W10 taxonomy).

**Law compliance**: product source read-only; no browser tooling; dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.

## Scoreboard

**41 adjudicated roster rows — 0 new BLOCKER · 9 MAJOR · 16 MINOR · 7 INFO · 9 folds by reference into banked identities (two blocker-weight at their banked homes) — ZERO re-bookings.** All **five filed BLOCKERs die or fold**: L-1/C-B1 killed baseline-short (the program's sixth-and-seventh occurrence, in one corpus), D-1 and C-B2/D-15 fold to banked kf-PlaybackRibbon rows (one of them **pre-assigned by name** to this adjudication), D-2/L-2 regraded MAJOR (blocker-in-first-impression, four dissents preserved), L-3 split into two folds. **14 killed-claims register entries** — the readers' convergent kills all sustained, two reader confirmations REVERSED at the bytes (D-23; L-10's "paints nothing" arm), one reader kill-of-a-grade sustained, one grouped BOOKING-KILL covering **fourteen** registry-blind identities. **8 of the readers' 8 filed misses ADMITTED** (4 become roster rows — one of them, the unreachable Reset, this corpus's best net-new find; 2 fold as riders; 1 folds into an existing row; 1 lands in the corrections register as the corpus-integrity event). **10 superlative cells sustained** (one sentence struck against the bank). **12 UNPROVEN residues → SS-13.** Three **cross-registry corrections** recorded: the kf-EasingScene killed-#1 rationale, the kf-PlaybackRibbon C-14 baseline note, and a registry id-space collision (two different banked rows both named `KF-ES-8`).

**Headline.** Four results. (1) **The scene's advertised thesis is inert at birth and its transport is wired to clocks the user did not select**: the one on-stage instruction does nothing in the born state (D-2/L-2, the false comment live at :191-193), dock Play on the Entry channel advances every clock EXCEPT the selected channel's (L-10-narrowed), and — the corpus-wide miss — **dock Reset, `R`, and `Escape` can never reach the spring at all**: the scene's 25-line `reset()` is unreachable dead code, the dock's handler stops the App's EMPTY fallback AnimationGroup (App.vue:214-219, this seat's read), and the comment "the dock owns Reset" (:72-73) is false. (2) **The copy-fidelity kill**: the scene's headline dogfood — the copy-pasteable `compileToEntry()` artifact whose stated purpose is "a designer pastes it verbatim" — renders ALL-CAPS through `text-mono-caption`'s `text-transform: uppercase`, in a scene whose author demonstrably knew the hazard (`.derby-lane-tag { text-transform: none }` at SpringTarget.vue:454). (3) **The registry lesson, again at scale**: fourteen of this corpus's rows were banked before it was written — including one filed BLOCKER whose fold destination was **named in advance** by kf-PlaybackRibbon's identity guard — and the three axes contain no registry sweep at all; reader-2 swept and killed, reader-1 swept partially and re-confirmed one banked row as a BLOCKER anyway. (4) **The bank is corrected back twice**: the dead `ribbonContent` guard is genuinely dead for every derived-triad scene (RibbonBar's own arms claim keyframes/timeline before the slot arm — kf-EasingScene's killed-#1 rationale is wrong in mechanism), and kf-PlaybackRibbon's C-14 "0/0 re-verified" cell was a worktree reading (F-1 is a worktree-only fact; both committed baselines declare AND lock glass-ui).

## Ruled disagreements (the incredulity seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D-1 (accent-CTA contrast): reader-1 CONFIRMED BLOCKER with a full independent re-derivation vs reader-2 BOOKING-KILL** | **Reader-2 WINS — FOLD ≡ banked kf-PlaybackRibbon D-10+DU-M-3 (MAJOR at its home, one day older, identical figures 3.58/3.70/4.02 rest · 3.03/3.12/3.19 hover, identical rule, and the one-token cure `--accent-kf-foreground` already identified there)** | The banked row is verbatim the same mechanism and arithmetic. What this corpus ADDS is carried as fold riders: three further consumer sites of the same rule (SpringScene.vue:144, StartingStyleTarget.vue:42, plus the ribbon mount at :108-129), reader-1's method-grade independent re-derivation (byte-identical #7E5ACC; 3.576/3.034 to 3 s.f.), and the K-5 line-drift correction (the axis's playback-idiom cites are shifted 3-6 lines; one points past the 87-line EOF — seat re-verified :28/:44/:84). Reader-1's BLOCKER preserved as dissent at the banked home. |
| 2 | **D-2 ≡ L-2 grade: BLOCKER (both axes + reader-1) vs MAJOR (reader-2)** | **MAJOR — blocker-in-first-impression; four BLOCKER dissents preserved** | Chain re-walked whole by this seat: :194 `autoPlays: false` → machine born `paused` → `reseat()` (:294-300) writes targets + `startLoop()` and nothing else → `frame()`'s first statement (:197-206) returns `false` before any `tickDt`/`repaintSprings`. One dead tick, then stop; the ghost marker moves (reactive `:style`), the ball never does; the falsified comment is live at :191-193 and the on-stage copy inherits it. Demotion grounds: no state corrupts, dock-Play cures instantly, and the mechanism class (one-dead-tick under a paused machine) is banked INFO at kf-EasingScene KF-ES-41 — but the INTERACTION consequence is spring-owned and net-new (easing has no reseat gesture), which is exactly the MAJOR. |
| 3 | **L-3's shape: reader-1's single structural BLOCKER vs reader-2's three-way split** | **Reader-2 WINS — the row splits into two folds plus the root fold, and reader-1's K-9 correction is adopted into all three** | (a) L-3b/C-M1 (ungated `tabsContent` :67 against ChannelControls' written contract; the dead `CONTROL_SURFACES` premise at :34-38/:58-66) **FOLDS ≡ kf-EasingScene KF-ES-11** (same identity, EasingScene.vue:57; spring is the second witness) + banked kf-ChannelControls D-7/L-4 (`isSingleSurfaceScene` unreachable — its own rationale already names spring). The tree's receipt verified verbatim by this seat: control-surface-dfa.test.ts:81-86 asserts `["controls","keyframes","timeline","spring"]`. (b) L-3a/C-M2 (two live facet instances) **FOLDS ≡ kf-ControlsPaneWrapper M-4 + MISS-3** (v-show mount-all + forwarded-slot re-render), with kf-KeyframesStringControls' SHARPER banked count (three closures over one `springEditAnim`, not two) and kf-AnimationControlsGroup D-1's enumeration ("spring 2"). (c) K-9 sustained: a scene-side `v-if` kills only 3b — both hosts render the slot, so only the upstream `v-show→v-if` cure or a per-host key kills 3a. |
| 4 | **C-M3 (both facility channels non-painting): reader-1 CONFIRMED MAJOR standalone vs reader-2 FOLD-EXTEND ≡ KF-ES-2** | **FOLD-EXTENDED ≡ banked kf-EasingScene KF-ES-2 (BLOCKER at its home) — the spring witness, which is HOTTER: two decoy channels, not one** | Reader-2's 40-hit census adopted after spot-verification: neither `springEditAnim` (built targetless, `buildSpringKeyframesCSS` emits only percentage rows, never `.play()`ed) nor `entryAnim` (compile input only; the card animates by pure CSS) paints; the contract clause (controlSurfaces.ts:68 "Present ⇒ this channel PAINTS") is violated twice, which is precisely why spring is the only scene that also spawns a duplicate host (ruling 3b). The Entry channel's live `setProgress` writes a number nothing paints (arrow-scrub reachable — seat-verified shortcut registrations :53-58). Rides KF-ES-2's NO-WAVE-OWNER facility-honesty spec as its second witness; zero re-booking. |
| 5 | **D-15 ≡ L-5 ≡ C-B2 (Reverse mirror): reader-1 CONFIRMED MAJOR vs reader-2 BOOKING-KILL** | **Reader-2 WINS decisively — the bank PRE-ASSIGNED this fold by name** | kf-PlaybackRibbon.md's identity-guard section states verbatim: "C-2's easing manifestation ≡ KF-ES-5 …; **any future kf-SpringScene adjudication folds its raw-feed row to C-2 here**" — and its ruling 4 already cites the spring links by line (SpringScene :119-120, useSpringDemo :240/:288, AnimationVisualizer :246). The scene-side decoy bundle (raw `.reversed =` skipping `reverse()`'s startTime shift; the never-reversing stage) ≡ banked KF-ES-5. Reader-1's full re-derivation is recorded as an independent confirmation of the banked BLOCKER; nothing is booked here. |
| 6 | **D-23 ("Re-seat calls the wrong verb"): reader-1 CONFIRMED INFO vs reader-2 KILLED** | **Reader-2 WINS at the bytes — reader-1's confirmation REVERSED** | useSpringDemo.ts:303-305, this seat's own read: `const toggleTarget = () => { reseat(target.value > 0.5 ? 0 : 1); }`. The button labelled Re-seat re-seats — `toggleTarget` IS a delegating call to `reseat`, seating the same target/liveSpring/track targets as the rail path. Reader-1 quoted both functions and missed the delegation. Residue (the label is jargon the on-stage copy never teaches) survives at INFO (KF-SS-38). |
| 7 | **L-10/D-27's "burns a rAF and paints nothing" arm: reader-1 confirmed as filed vs reader-2 refuted-and-narrowed** | **Reader-2 WINS — the arm is KILLED, the narrowed row is booked MAJOR** | The Physics facet lives in the controls pane, not the stage; on the `spring` surface it is the visible panel and its registered painter writes `translateX(<cqw>)` onto four visible `.preset-ball`s that DO move under Play. The narrowed, verified defect: with Entry selected, `frame()` never touches `entryAnim.t` (census: its only writer is the channel's own `setProgress` :422-425), so the transport advances the live spring, four presets, the sampler, and `springEditAnim` — everything except the selected channel. Booked as KF-SS-8, riding the KF-ES-2 fold's spec (same facility-honesty root as ruling 4). |
| 8 | **The dead `ribbonContent` guard (C-m4 / L·N-1) vs banked kf-EasingScene killed-#1 ("the guard is live")** | **Both readers' factual half CONFIRMED (MINOR) — and a CROSS-REGISTRY CORRECTION is recorded against the banked rationale** | RibbonBar.vue re-read whole by this seat: the slot (:111-114) sits inside `v-else-if="selectedControl !== 'controls'"` (:108), AFTER the keyframes (:13) and timeline (:69) arms have claimed those values — the slot is only ever invoked with a value outside {controls, keyframes, timeline}, i.e. exactly `spring` (or `easing`). The guard at :136 can never fire, for spring AND for easing. kf-EasingScene killed-#1's stated reason ("`selectedControl` takes keyframes/timeline" ⇒ guard live) is wrong in mechanism; its substance survives intact — the transport IS absent on those two surfaces, but via RibbonBar's own arms replacing the slot, not via the guard's null return (KF-ES-12's booking is unaffected, its attribution line should be read with this correction). |
| 9 | **Reader-2's missed Reset row: net-new or banked?** | **NET-NEW — ADMITTED at MAJOR (KF-SS-2) after this seat re-walked the whole chain and swept the bank** | Every link re-verified: TransportDock.vue:158 emits `reset(false)` (aria-label "Reset animation"); :51-52 binds `Escape` AND `R` to the same `reset()`; AnimationControlsGroup.vue:106 is the sole handler → useAnimationGroupActions.ts:55-61 `getGroup().stop(); syncPlayState(false)` → :303 `getGroup: () => animationGroup` → App.vue:29 `:animation-group="currentAnimationGroup"` → :214-219 **the born-empty fallback group, never rebound for a facility scene** (spring exposes no group — SpringScene.vue:181-183). `demo.reset` = 0 consumers; `dispatch({type:"RESET"})` = 3 sites, all inside orphaned scene reset() bodies (my greps) ⇒ sceneMachine's RESET case is UI-unreachable. Net observable: Reset/R/Escape on the spring is at most a pause; targets, positions, velocities, sweep phase, editor clock all untouched; the :72-73 comment is false. Bank swept: kf-AnimationControlsGroup D-11/C-8 adjudicates the same handler line but ONLY its `clear()` arm; ACG M-4 covers only Escape's overlay scoping. The `reset()` arm's inertness for facility/raw-rAF scenes is unbooked anywhere. |
| 10 | **C-M6 grade: the C axis's MAJOR vs reader-1's MINOR regrade** | **MINOR — reader-1's regrade adopted; the premise kill is registered (K-14)** | The two reverse authorities are mutually exclusive by construction (ChannelOptions' teleported ribbon renders only on the `controls` surface — seat-verified RibbonBar :6-9 v-show + ChannelControls :98; the scene's ribbon only on `spring` — ruling 8). The real defect is state divergence across a tab switch (two `userReversed` refs over one non-reactive flag), the spring instance of the banked KF-ES-6 family (cross-ref, not merged — the easing row's third contradiction, wake-wiring, is C-M4's fold here). |

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0–W10** per CENSUS-2026-08-03; behavioral repairs with no census owner = **NO-WAVE-OWNER (SS-1/SS-2 spec input — the spring scene-repair packet, riding the transport/ribbon packet and the KF-ES-2 facility-honesty spec where named)**. Demo anchors are worktree anchors; the worktree is dirty in this scene's neighborhood (StartingStyleTarget's `emphasis` migration observed live) — **re-resolve every anchor at cure time (D-19), and re-baseline the D-17/D-5 riders first**.

### MAJOR (booked here)

- **KF-SS-1 · D-2 ≡ L-2 — the scene's one on-stage instruction ("Tap or drag the rail — the ball springs to the new target") is inoperative in the born state; the comment asserting otherwise (:191-193) is false and the on-stage copy inherits it.** Chain seat-verified whole (ruling 2). MAJOR, blocker-in-first-impression; BLOCKER dissent ×4 (both axes, reader-1) preserved. Mechanism class ≡ banked KF-ES-41 (INFO at easing); the interaction consequence is spring-owned. Cure space for the spec: `reseat` participates in the machine (dispatch PLAY, or a one-shot settle tick), never a comment-only fix. → **NO-WAVE-OWNER** (spring packet); first-impression witness → SS-13.
- **KF-SS-2 · reader-2 MISS-1 (admitted, ruling 9) — dock Reset, `R`, and `Escape` can never reach the spring scene: `useSpringDemo.reset()` — the only code that rewinds the spring — is unreachable dead code; the dock's handler stops the App's empty fallback AnimationGroup; "the dock owns Reset" (:72-73) is false.** Every link this seat's own (ruling 9). MAJOR. Cross-refs (arms, not identities): kf-ACG D-11/C-8 (clear-arm), kf-ACG M-4 (Escape scoping). Cure space: facility-aware reset routing (the facility already carries `playback`), or wire `demo.reset` into the scene's expose contract. → **NO-WAVE-OWNER** (spring packet + the ACG facility-routing seam).
- **KF-SS-3 · C-M5 (+ reader-2's interleave rider, folded — one identity; + C-m6 as its library-API satellite) — a 0.01-step slider drag drives `compileToEntry` undebounced, uncancelled, last-completed-wins; and `recompile` mutates the SHARED facility-channel `entryAnim` (`setTimingFunction` :71) AFTER an await, so two in-flight recompiles interleave a mutation with a compile over one markRaw object.** useCompiledEntry.ts read whole by this seat (:65-77 verbatim; watch :79-81 `{immediate:true}`, no debounce/flush/abort); three uncoordinated watchers per tick (useSpringDemo :341 rebuild; useCompiledEntry recompile; useSpringLinearStops — a fourth solve) with SpringScene as the composition root owning none of the coordination. NET-NEW (both readers' registry sweeps concur; seat sweep concurs). Satellite (C-m6, INFO): `springTimingFunction` has no way to ask for `.fn` without paying for eager `.css` — a library-API signal for the packet. MAJOR; measured cost → SS-13. → **NO-WAVE-OWNER**.
- **KF-SS-4 · reader-1 MISS-1 (admitted; seat-verified at the dist and both sites) — the copy-pasteable `compileToEntry()` artifact and the `springLinearStops()` identifier render ALL-CAPS: `text-mono-caption` carries `text-transform: uppercase` (installed utilities.css, my read).** StartingStyleTarget.vue:61 (the artifact `<code>`, whose :50-55 charter is "a designer pastes it verbatim" — class selectors and custom idents are case-sensitive, so what is read is not what pastes; the CopyButton :59 copies the raw string, making read ≠ copy) and :17-19 (`SPRINGLINEARSTOPS()`). The author knew the hazard: SpringTarget.vue:454 `text-transform: none` (seat-verified). **Mechanism FOLDS ≡ banked kf-EasingSidebar KF-ES-8** (the uppercase text-mono-caption family; + kf-SequenceAxis D-12's `@800MS` relay) — **new sites, and the hardest instance in the tree** (a fidelity-charter surface). All three axes walked past it (D-4 graded the register without opening the utility; D-14/C-i1 quoted line :61 for other reasons). MAJOR. Cure = the banked one (chip the identifier, case-preserving mono rung for the artifact). → **NO-WAVE-OWNER**; rides the KF.W5 prose/typography sweep with the banked family.
- **KF-SS-5 · D-7 — derby lane tags fail AA by up to ~2.4×: `color-mix(90% ball-tone, foreground)` at `opacity: 0.85`, 4/4 light · 2/4 dark.** Rule seat-verified verbatim (SpringTarget.vue:448-455); reader-1's independent recomputation (1.87 vs the axis's 1.92 — 3% agreement) adopted; no opaque scrim under `.derby-lanes`. MAJOR; composited ratios → SS-13. → **NO-WAVE-OWNER**.
- **KF-SS-6 · D-8 (prose half) — six comment sites in the scene's read set describe `--color-progress` as green or red; only violet is true (style.css :163 → :130, oklch hue 295/305).** All six sites verified by reader-1 (one off-by-one corrected); the token-level lane pairing (gentle = `--color-progress`, bouncy = `--rainbow-violet`, the two physics extremes on the two nearest hues) holds at design-idioms.css:26-31. The perceptual "collapse" half stays UNPROVEN (ΔE_OK 0.15-0.19 ≈ 7-9× JND) → SS-13. Pattern kin: KF-ES-43 / KF-ET-37 (the false-comment shell) — kin, not identity; the color-prose content is new. MAJOR. → **NO-WAVE-OWNER** prose sweep (KF.W5).
- **KF-SS-7 · D-10 — non-text contrast: the rail groove, the y=1 target line, and the ghost target marker sit far below 3:1 — and the marker (~1.99:1) is the SOLE feedback for the born-state tap gesture (compounds KF-SS-1).** Source rules all verified at the cited lines by reader-1; its contrast engine validated on two other cells to ≤3%. The `.stage-field-x` rescue holds for the TRACK only; marker and target line encode state. MAJOR; composited ratios → SS-13. → **NO-WAVE-OWNER**.
- **KF-SS-8 · L-10/D-27 as-narrowed (ruling 7) — with the Entry channel selected, the transport's Play advances every clock EXCEPT the selected channel's; the visible discrete card is pure CSS and `entryAnim.t` is never advanced by the loop.** The "paints nothing" arm is K-13. Same facility-honesty root as the KF-ES-2 fold (KF-SS-10); booked as its spring-transport consequence. MAJOR. → **NO-WAVE-OWNER**, rides the facility-honesty spec.
- **KF-SS-9 · D-4 — the two faces of one scene wear two different type systems in the identical structural slot (`text-display` vs `text-heading` on the face titles: different family, rung, weight, one fluid one not; `gap-8` vs `gap-6`), and the scene carries four registers for code identifiers.** Reader-1 verified every byte incl. the installed-utility comparison; the identifier-register limb is sharpened by KF-SS-4. MAJOR. → **NO-WAVE-OWNER** (one register decision per role; rides KF.W6's typography discipline).

### MAJOR/BLOCKER-weight folds by identity (banked homes; this corpus = independent confirmation, zero re-booking)

- **KF-SS-10 · C-M3 ≡ kf-EasingScene KF-ES-2 (BLOCKER at home) — FOLD-EXTENDED (ruling 4): spring declares TWO non-painting channels against the decoy-zero clause, earning 4 surfaces and the duplicate host; the Entry channel's reachable `setProgress` writes a number nothing paints.** The spring witness rides KF-ES-2's NO-WAVE-OWNER spec; the DFA test (:81-86, verbatim) is the executable receipt.
- **KF-SS-11 · D-1 ≡ kf-PlaybackRibbon D-10+DU-M-3 (MAJOR at home) — FOLD (ruling 1)**, riders: +3 consumer sites, reader-1's re-derivation, the K-5 anchor corrections. The mobile-drawer plate residue is already that row's SS-13 item.
- **KF-SS-12 · D-15 ≡ L-5 ≡ C-B2 ≡ kf-PlaybackRibbon C-2 (BLOCKER at home; fold PRE-ASSIGNED by name) + KF-ES-5 (the scene-side raw-setter/decoy bundle) — FOLD (ruling 5).** Reader-1's full mirror re-derivation recorded as independent confirmation.
- **KF-SS-13 · L-3a ≡ C-M2 ≡ kf-ControlsPaneWrapper M-4 + MISS-3, with kf-KeyframesStringControls' banked THREE-closures count and kf-ACG D-1's "spring 2" — FOLD (ruling 3b).** K-9 rides: only the upstream `v-show→v-if` or a per-host key cures the duplication. Rendered stack + stale-write → SS-13 (kf-ChannelControls UNPROVEN #1 names the screenshot).
- **KF-SS-14 · L-3b ≡ C-M1 ≡ kf-EasingScene KF-ES-11 + kf-ChannelControls D-7/L-4 — FOLD (ruling 3a)**, spring as second witness; the dead `CONTROL_SURFACES` premise comment dies with the cure.
- **KF-SS-15 · L-4 ≡ kf-KeyframesStringControls' banked chain (2× KeyframesEditor slot copies + 1× force-mounted KeyframesStringControls over one `springEditAnim`) — FOLD.** Rider carried: the facet's own terminal-date comment (:17-20, seat-verified) declares its editor already due dead post-T.B2 — the cure has a written owner. `keyframesWarmed` liveness → SS-13.
- **KF-SS-16 · C-M4 (freeze half) ≡ kf-AnimationVisualizer KF-AV-1 (BLOCKER at home) — FOLD; the cure half is K-12 (the prescribed `onScrubbed` wire is structurally inert at this call site — reader-2's derivation adopted: SpringScene owns no `useAnimationSync`, and a supplied guard makes `useDemoTicker`'s start/stop dead).** Scene-side residue class ≡ kf-EasingScene KF-ES-8 (the easing scene's row); rides KF-AV-1's guard cure.
- **KF-SS-17 · L-6 ≡ KF-AV-2 (+ KF-AV-15, the seam's non-reentrancy) + KF-ES-13 (the byte-identical transport-arming quartet EasingScene/SpringScene both carry) — FOLD.** kf-AV's own instrument list already reads the spring latch; KF-AV-15 rules the fix lands once, at `useDragCapture`.
- **KF-SS-40 · D-19 ≡ KF-CE-13 (categorical demo-wide forced-colors gap) + kf-ControlsPaneWrapper D-3+M-1 (the authored defeat, two-deletion cure) — FOLD.** → KF.W9 with the banked sweep.

### MINOR

- **KF-SS-18 · D-5 — the same domain verb (Dismiss/Reveal: same label, same icon pair, same handler) is mounted twice in the discrete face's persistent layout, in two diverging skins.** Both sites re-read by this seat (SpringScene :138-156 `variant:"outline"`+class string vs StartingStyleTarget :40-47 `emphasis="secondary"` — the worktree has already migrated the stage twin, so the pair is now skin-divergent too; re-baseline before cure). The elision-law contradiction is live at :64-70. MINOR; reader-1's MAJOR preserved as dissent pending the SS-13 co-visibility witness.
- **KF-SS-19 · D-6 as-rescoped — the Re-seat cell breaks the transport band's own one-voice law (PlaybackRibbon.vue:38-52) three lines from its accent-skinned sibling, and misses the `.btn-playback` hover family; the height-break and focus/press-loss arms are DEAD (K-3/K-4).** `h-8` unreachable ≡ kf-CPW D-m12; focus/press producer-supplied ≡ kf-CS D-11; `btn-interactive` ≡ kf-CPW D-M11 (all folds). MINOR (both readers convergent post-kill).
- **KF-SS-20 · D-9 as-regraded — prefers-reduced-motion is honored for three cosmetic CSS sites only; the 60 Hz physics is ungated while three siblings gate in JS (cites all seat-spot-verified via reader-1's exact-line census).** Regraded MINOR: `autoPlays:false` means all motion is user-initiated and pausable (no AA criterion engaged — the axis's own honest mitigation); the defect is convention-divergence from the three gated siblings. MAJOR dissent (axis + reader-1) preserved. → **NO-WAVE-OWNER**; PRM data rides KF.W9.
- **KF-SS-21 · D-11 + reader-1's both-ends sharpening (folded in) — the solver face clips under compression with no scroll seam, and because the column is `justify-center` inside `overflow-hidden`, it clips at BOTH ends and neither is recoverable even by scroll; the sibling face solves it with `flex-1 min-h-0`.** K-10 rides (four children, not five). Regraded MINOR pending the SS-13 threshold; MAJOR dissent preserved.
- **KF-SS-22 · C-M6 as-regraded (ruling 10) — two reverse authorities, mutually exclusive by construction, diverge silently across a tab switch (two `userReversed` refs over one markRaw flag).** MINOR; the spring instance of the KF-ES-6 family (cross-ref).
- **KF-SS-23 · C-m4 ≡ L·N-1 — the `ribbonContent` guard (:136) is dead and reads as the live gate, masking C-M1's missing gate (ruling 8, with the cross-registry correction).** MINOR.
- **KF-SS-24 · D-16 — `title` as the sole description carrier for the preset blurbs and the re-sample button, 30 lines from the correct `tooltip` seam used in the same file.** MINOR.
- **KF-SS-25 · D-17 — `background: … !important` twice against the consumed Chip's state paint.** WORKTREE RIDER (reader-2, adopted): the worktree ADDS `mode="selectable"` at the call site vs HEAD — the premise must re-baseline before cure; the parameterize-vs-override question needs the producer's Chip source (BH-relay question, not yet a relay item). MINOR.
- **KF-SS-26 · D-18 — the headline derby egg is pointer-only and keyboard-unreachable, and the shared `useDragScrub` seam fires `reseat` unconditionally on EVERY pointer-down, so both taps of the double-tap re-seat before `derby()` overrides.** Reader-1 verified the seam no axis but D checked. MINOR.
- **KF-SS-27 · D-13 + reader-1's rider — dead layout classes on the scene's only markup (`items-center`/`justify-center` no-ops; `px-6 lg:px-8` repeated verbatim on both stage Cards), and the same disease in the render-fn half (`grid grid-cols-1 gap-2 w-full` at :139 vs `grid gap-2 w-full` at :159 for the same construct — seat-verified both).** MINOR.
- **KF-SS-28 · D-14 — two scroll regions with no keyboard path (the artifact `<code>` :61 — the content the user is explicitly meant to read — and `.keyframes-editor-scroll`).** MINOR; focusability probe → SS-13.
- **KF-SS-29 · C-m3 — root-barrel glass-ui import at :18 where `./button` exists (71-byte re-export) and the sibling file already uses subpaths; RESOLVES L-1's explicitly-unverified subpath half.** Per-file instance of the kf-PlaybackRibbon C-11 class (cross-ref). MINOR.
- **KF-SS-30 · C-m5 — keyboard collision: the rail's `role="slider"` handles Arrow/Home/End with `preventDefault` and no `stopPropagation`, while the global transport binds the same keys and the glass dispatcher's exemption list does not exempt `div[role=slider]`.** SEAT CITE CORRECTION: the live registrations sit at :53-58 — the C axis's original cite was EXACT and reader-1's ":54-59, off by one" correction is itself off by one (my grep: ArrowLeft :53 … Home :57). Adjacent banked collision: kf-KfPillTabs :39 (reached through this scene's own facet). Co-fire → SS-13. MINOR.
- **KF-SS-31 · D-20 ≡ L-9 ≡ C-i1 — comment rot with load-bearing misinformation: routes to a file that does not exist, a deleted authority table cited as live, a phantom identifier narrated, and the `defineExpose` comment (:195-197) orphaned above `tabsContent`.** SEAT CENSUS RE-PIN: `SpringSidebar` = **12 comment sites / 8 files** (both readers' counts corrected: D's "seven", reader-1's "eleven across nine"); `contractAnim` = **4** hits, all comments, zero declarations (reader-2's correction of L-9's "0 hits" confirmed). Pattern ≡ KF-ES-43/KF-ET-37 (kin); content new. MINOR. → **NO-WAVE-OWNER** prose sweep (KF.W5).
- **KF-SS-32 · reader-1 MISS-4 (admitted) — the rail announces a 0-100 scale (`aria-valuenow` = `round(target*100)`) that appears nowhere in the visual UI ([0,1] everywhere), with no `aria-valuetext`; keyboard step ±0.1 = 10 announced units, no fine step/Page keys per APG.** Mechanism class-folds to the banked aria-valuetext family (kf-PlaybackRibbon N-2 GLASS-OWNED seam + kf-SequenceAxis D-12 relay); the site and the two-scales mismatch are new. MINOR. → **NO-WAVE-OWNER** demo half; producer half rides the recorded kf-PR relay.
- **KF-SS-33 · reader-2 MISS-2 (admitted; seat spot-verified `samplerCss` 2-hits + `reset` 0-consumers) — `useSpringDemo`'s public surface is one-fifth dead: `samplerCss` (live computed, rendered nowhere — its consumer was the deleted SpringSidebar), `reset` (KF-SS-2), `scenePlayback` (duplicate of `facility.playback`; the App binds the facility), `repaintSprings`, `progress` — zero component consumers each.** Detector gap ≡ KF-ES-16's toolchain rider (no `noUnusedLocals`, `lint` = `depcruise src`). MINOR. → **NO-WAVE-OWNER** hygiene.

### INFO

- **KF-SS-34 · D-21 ≡ L-8 ≡ C-m1 + D-22 ≡ L-7 ≡ C-m2 — the dead-surface bundle, FOLD ≡ kf-EasingScene KF-ES-40 (which pre-names "SpringScene carries the twin") + KF-ES-18 (`isStarted` write-only vs the hardcoded `isAnimStarted: true` literal) + KF-ES-16 (the unchecked h()-bag enabling it), logged per-site per the KF-AV-38 precedent**: `computed` imported unused (:17), `isPlaying` alias unread (:55), `SCENE_ID` one-use (:29→:185), the shell genuinely writing the never-read `isStarted` (:56/:186). All seat-verified in the whole-file read. Zero re-booking.
- **KF-SS-35 · D-24 — `ribbonContent` reads the per-frame `scrubberPhase` ref, re-vnoding PlaybackRibbon + the static Re-seat Button 60×/s while playing; MECHANISM CORRECTED (reader-1, adopted): App renders it as `<component :is>` — a functional component with its own render effect, so the invalidation is scoped to the ribbonContent subtree, not the host.** Magnitude → SS-13. INFO.
- **KF-SS-36 · D-25 — all positional properties physical; the demo has no RTL surface; latent.** → KF.W9 (with kf-PR D-24's RTL datum). INFO.
- **KF-SS-37 · D-26 — `.spring-pane`: one usage, zero definitions anywhere.** INFO.
- **KF-SS-38 · D-23 residue (post-K-2) — "Re-seat" is jargon the on-stage copy never teaches.** INFO; rides the prose sweep.
- **KF-SS-39 · reader-2 MISS-4 (admitted; seat-verified constructor :55 + guards :417/:423) — two dead `?? 500` guards duplicate the Entry constructor's literal in a second file, and diverge from the sibling channel's bare idiom.** INFO.
- **KF-SS-41 · C-i2 — the scrub handler narrows the emit payload to `{ t }` and discards `animation`, hard-wiring every scrub to the Sweep clock; the correct discriminating shape ships in-tree (AnimationControlsGroup :244-251).** Harmless today, a mis-route the day the second ribbon binds (KF-SS-22's family). Unchecked-`.vue` enabling condition ≡ KF-ES-16 (fold). INFO.

## Killed-claims register — 14 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **L-1 / C-B1 (both BLOCKER) + D-§6-F1's load-bearing fold** — "glass-ui is a phantom dependency; `npm ci` from a clean checkout cannot build the spring chunk" | **BASELINE-SHORT — killed by both readers convergently, re-verified at this seat's own git instruments**: worktree DELETED the declaration; `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep (+3 lock rows); HEAD:71 = 6.0.0 optionalDep (+3 lock rows). `npm ci` from either committed baseline resolves. The program's sixth-and-seventh baseline-short occurrences, in one corpus; kf-App.skeleton K11 had already killed F-1 at origin/master. D's contingency ("re-measure D-1/D-12 if the pin differs") is DISCHARGED — origin/master pins exactly the installed 7.0.0. Residue (INFO, repo-level): the worktree drift itself (HEAD 6.0.0 vs installed 7.0.0; the uncommitted edit deletes the declaration). **Cross-registry note**: kf-PlaybackRibbon's C-14 cell ("0/0 re-verified") was a worktree reading — F-1 is a worktree-only fact and the KF.W0 §B-12 routing consumes it WITH the baseline correction, per kf-EasingScene KF-ES-15's dating. |
| **K-2** | **D-23** — "the button labelled Re-seat calls `toggleTarget`, not the function named `reseat` — the label names the wrong verb" (reader-1 CONFIRMED at INFO) | **SEAT-RATIFIED KILL (reader-2's; ruling 6)**: `toggleTarget` IS `reseat(target.value > 0.5 ? 0 : 1)` at :303-305 — my own read. Reader-1's confirmation REVERSED; the jargon residue survives as KF-SS-38. |
| **K-3** | **D-6a** — "`h-8` = 32px vs `h-10` = 40px — an 8px height break in the stack" | SUSTAINED (both readers convergent): `.button { min-block-size: var(--button-size) }` floors both cells at 40px (60px coarse); `height` vs `min-block-size` is no cascade contest. ≡ banked kf-CPW D-m12 (the same class-string family). No height break exists in either mode. |
| **K-4** | **D-6b** — "without `.btn-playback` the Re-seat button forgoes the focus ring and press affordance" | SUSTAINED: the producer stamps `"button tap-squish focus-ring"` and ships the ring + the liquid-press spring itself (reader-1's dist extraction; concord kf-CS D-11 "no focus contract lost"). Survivors (accent-hover family absent; voice split) live in KF-SS-19. |
| **K-5** | **Axis D's playback-idiom.css citation layer** — :31-33/:47-52/:88-91 et al. | SUSTAINED AND SEAT-CONFIRMED: the file is 87 L; live anchors :28-37/:44-50/:84-87 (my grep); D-15's cite points past EOF. Every substantive claim survives; a repair wave following D's numbers would edit the wrong declarations. Carried as a rider on every D-anchored row. |
| **K-6** | **C-M4/L-6's `AnimationVisualizer.vue:305`/`:318` cites** — shared across two independent axes | SUSTAINED AND SEAT-CONFIRMED: the file is 256 L (my `wc`). Two axes converging on the same out-of-file line pair is itself evidence of a shared upstream reference. The topology survives; the anchors do not. |
| **K-7** | **D·S-2's sentence** — "`.focus-ring` → design-idioms.css:76-79 is the demo's single documented focus contract" (praised) | SUSTAINED (reader-1's K-8): the bank books exactly that rule as kf-PR D-3+DU-M-1 — MAJOR, blocker-in-mode, cure = delete BOTH demo copies. The rail (SpringTarget.vue:63 `focus-ring`) is a consumer of the banked defect. S-2's remaining content stands (see superlatives). |
| **K-8** | **L-3's falsifier clause (d)** — "a `v-if` at SpringScene.vue:67 would kill both halves" | SUSTAINED (reader-1's K-9, ruling 3c): both v-show hosts render the slot; a scene-side gate true for both kills only the surface bleed. Only the upstream mount-policy cure or a per-host key kills the duplication. |
| **K-9** | **D-11's "five children"** | SUSTAINED: four (header/rail/sweep/SpringTrace). Immaterial to the mechanism; the intrinsic-floor arithmetic must be re-run at cure time. |
| **K-10** | **Both readers' SpringSidebar censuses as stated** (D "seven"; reader-1 "eleven across nine files") | **SEAT RE-PIN**: 12 comment sites / 8 files (my grep, enumerated in the instruments). The prose-rot row (KF-SS-31) gets stronger; every stated count was wrong, including the correction. |
| **K-11** | **Fourteen registry-blind filings** (the axes; reader-1 for D-1) | **GROUPED BOOKING-KILLS — every identity re-verified at the bytes and recorded as an independent confirmation of its banked row**: D-1 ≡ kf-PR D-10+DU-M-3 · D-3 ≡ kf-CPW D-M11/kf-CS D-11 (the banked census names SpringScene.vue:167, SpringPhysicsFacet.vue:74/:105) · D-12 ≡ kf-CS D-2/C-3 ("SpringScene ×2 … rides the same commit") · D-15/L-5/C-B2 ≡ kf-PR C-2 (+KF-ES-5) — the pre-assigned fold · D-19 ≡ KF-CE-13/kf-CPW D-3+M-1 · D-21/L-8/C-m1 + D-22/L-7/C-m2 ≡ KF-ES-40/-18/-16 · L-6 ≡ KF-AV-2/-15 (+KF-ES-13) · C-M4 ≡ KF-AV-1 (+kf-EasingScene KF-ES-8) · L-3a/C-M2 ≡ kf-CPW M-4/MISS-3 (+kf-KSC, +kf-ACG D-1) · L-3b/C-M1 ≡ KF-ES-11 (+kf-CC D-7/L-4) · C-M3 ≡ KF-ES-2 · L-4 ≡ kf-KSC's chain · D-6a ≡ kf-CPW D-m12 · L-1/C-B1 ≡ census SCH-1 as already falsified at kf-App.skeleton. The axes contain no registry sweep at all; reader-2 swept and killed; reader-1 swept six and still confirmed D-1 as a BLOCKER — registry-blindness inside a return that itself performed booking-kills. |
| **K-12** | **C-M4's cure half** — "wire `onScrubbed`" as remediation item 6 | SUSTAINED (reader-2; adopted at ruling table): `scrubbed` exists to wake `useAnimationSync`'s guard ref, which SpringScene does not own; and a supplied guard makes `useDemoTicker`'s returned start/stop dead — the wire would wake nothing. The defect folds to KF-AV-1; the cure lands at the guard, at its banked home. |
| **K-13** | **L-10/D-27's "burns a rAF and paints nothing" arm** (reader-1 confirmed as filed) | **SEAT-RATIFIED KILL (reader-2's; ruling 7)**: the facet's four preset balls are visible in the controls pane on the `spring` surface and its registered painter moves them under Play. Reader-1's confirmation of this arm REVERSED; the narrowed row is KF-SS-8. |
| **K-14** | **C-M6's MAJOR grade premise** — two simultaneously-live reverse authorities | SUSTAINED (reader-1's severity correction, ruling 10): the two ribbons are mutually exclusive by construction (RibbonBar :6-9/:108 + ChannelControls :98). The defect is divergence-across-a-switch, MINOR (KF-SS-22). |

## Adjudicator corrections register

1. **Reader-1's D-23 confirmation** — reversed at the bytes (K-2): the return quoted both function bodies and missed that one delegates to the other. A verb-mismatch claim must execute the handler chain, not describe the two ends.
2. **Reader-1's D-1 confirmation as a BLOCKER booking** — the derivation is the corpus's best instrument (method superlative sustained) and the booking is wrong: the row was banked one day earlier with identical figures. Brilliance of re-derivation is not novelty; the same return performed six booking-kills and did not sweep for its own headline confirmation.
3. **Reader-1's census corrections, corrected**: SpringSidebar is 12/8, not "eleven across nine" (K-10); and its C-m5 line-range correction (":54-59, off by one") is itself off by one — the C axis's original :53-58 was EXACT (my grep: ArrowLeft :53 … Home :57).
4. **Reader-2's L-9 arithmetic correction adopted over reader-1's cell**: `contractAnim` greps 4 (all comments), not 0 — reader-1 inherited the axis's false falsifier count while confirming the row's substance.
5. **The corpus-integrity event (reader-1's second miss, homed here per precedent)**: the three axes contain **no registry sweep**; six-plus of their rows — one filed BLOCKER and both headline transport MAJORs among them — were banked before the corpus was written, and both axes' ranked repair item #1 ("declare + lock glass-ui") is a no-op against origin/master. Acting on this corpus alone would also have produced per-scene patches of seam defects the bank rules must be fixed once (KF-AV-15's `useDragCapture` law). REGISTRY-FIRST is a law because of exactly this file's arithmetic: 14 booking-kills : 9 booked majors.
6. **Cross-registry (recorded here; dated banked files are not rewritten)**: (a) **against kf-EasingScene killed-register #1** — the "guard is live" rationale is mechanically wrong (ruling 8); the guard is dead for every derived-triad scene; KF-ES-12's substance unaffected. (b) **against kf-PlaybackRibbon C-14's cell** — "0/0 re-verified" was a worktree reading; add the baseline qualifier when consuming the F-1 fold. (c) **registry id-space collision** — kf-EasingScene and kf-EasingSidebar both mint `KF-ES-*` ids; two DIFFERENT banked rows are both named `KF-ES-8` (the pause-on-scrub row vs the uppercase row). Both readers cited "KF-ES-8" for different identities and both were right. Future records must qualify the file; a registry-wide prefix de-collision is flagged for the next census sweep.

## Superlatives — sustained, with provenance

1. **The compositor-only value axis, with the split-cadence channel that makes it legible** (D·S-1 / L·S-1, both readers + this seat's spot-reads) — painter-registry `translateX(<cqw>)` against `container-type: inline-size` rails kills per-frame layout AND the per-frame width read; the position channel (60 Hz `scrubberPhase`) is split from the text channel (6 Hz readouts) with the reasoning written down; the in-repo contrast (AnimationVisualizer's per-frame `clientWidth`) is 100 lines away. Distinct from banked kf-AV SUP-1 (the component doing the opposite).
2. **Velocity-continuous re-parameterisation** (L·S-3) — `rebuildLiveSpring` carries value AND velocity across `dispose()` into the fresh `SpringProgress`; the from-rest snap every other demo ships is absent.
3. **Teardown is structural, not per-author discipline** (L·S-2) — `onScopeDispose` + bound visibility-pause in useSweepScene (docstring naming the exact historical crash), derby timer drain, both painter unregisters, and the engine's arrow-class-field bind-proofing. Neither reader found a leak; nor did the axes.
4. **The `@starting-style` triad including the part everyone gets wrong** (D·S-3) — `transition-behavior: allow-discrete` split into its own declaration with the reason written, independent `translate`/`scale` properties, full base/@starting-style/.is-hidden triple.
5. **A documented contrast invariant that survives independent measurement** (D·S-4) — the `.status-badge` family's ≥4.5:1-by-construction claim re-derived twice (4.87 vs 4.94). In a scene with six false color comments (KF-SS-6), this one is load-bearing and true.
6. **Measure discipline without exception** (D·S-5) — `max-w-3xl` on all eight content bands across both faces, zero exceptions, intent comment honored.
7. **The provide+prop dual delivery is forced and correct** ≡ **banked SUP-3 / KF-ES-38 — booked once, now confirmed by a third corpus**: the render-fn slot teleports the facet across the hierarchy, so the prop is load-bearing and `inject` would miss; the standing residue (the comment never names the trap) is unchanged.
8. **Pointer-based double-tap over `dblclick`** (D·S-6 / C·S-1) — one code path for mouse/pen/touch, drag-disjoint by tolerance, reason documented; zero shadow components anywhere in the scene.
9. **The tree ships the receipt that refutes its own comment** (reader-2's find, adopted; seat-verified verbatim) — control-surface-dfa.test.ts:81-86 is an executable, currently-green assertion of exactly the fact that falsifies SpringScene.vue:34-38/:58-66. The cheapest cure path for KF-SS-14 is already written and passing.
10. **Method superlative, axis D's contrast engine** (reader-1's award, ratified) — token anchors EXACT, conversions byte-identical under two independent re-derivations, validated to ≤3% on every re-run cell — computed from the resolved token chain even while its rule-line citations drifted (K-5): the numbers were derived, not transcribed. **D·S-2's rail** survives minus the struck focus-contract sentence (K-7): tabindex, aria-label, valuenow/min/max, four arrow branches, Home/End, 48px hit box all verified.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **KF-SS-1's first-impression witness** — born state, tap the rail: ghost moves, ball frozen, headline `x` readout frozen.
2. **KF-SS-2's witness** — press Reset (or `R`/`Escape`) mid-derby: nothing rewinds.
3. **D-1's mobile-drawer plate** — already banked kf-PR D-10's SS-13 item; not duplicated, cross-filed.
4. **KF-SS-18's co-visibility** — both Dismiss/Reveal twins in one short-mobile viewport.
5. **KF-SS-5/KF-SS-7's composited ratios** — derby tags, rail groove, target line, ghost marker on the live plate, both themes.
6. **D-8's perceptual half** — bouncy vs gentle lane balls at 0.8rem with the glow.
7. **KF-SS-21's threshold** — the stage height at which the header top and trace bottom first amputate.
8. **KF-SS-13's rendered stack** — two SpringPhysicsFacet bodies; whether a hidden-editor edit clobbers the visible one on channel switch (kf-ChannelControls UNPROVEN #1 names the screenshot); plus `keyframesWarmed` liveness on the spring route (KF-SS-15's honest gap).
9. **KF-SS-3's magnitude** — the ~130-recompile damping sweep traced; the interleave witnessed.
10. **KF-SS-30's co-fire** — one ArrowRight on the focused rail: reseat AND transport scrub?
11. **KF-SS-16's freeze magnitude** — rides KF-AV-1's banked SS-13 obligations.
12. **KF-SS-28's focusability + KF-SS-35's re-render cost** — axe `scrollable-region-focusable`; the 60 Hz vnode churn profiled.

## Routing summary (M-25 agglomeration input)

- **KF.W0 §B-12**: the K-1 substrate reconciliation (F-1 consumed WITH the baseline correction; the worktree drift INFO).
- **KF.W4**: the KF-SS-34 toolchain riders (≡ KF-ES-16's banked routing).
- **KF.W5**: the prose sweeps — KF-SS-6 (six false color comments) · KF-SS-31 (the 12-site SpringSidebar rot + the dead-table premise + the orphaned expose comment) · KF-SS-38 · KF-SS-4's typography family (with banked kf-EasingSidebar KF-ES-8 / kf-SequenceAxis D-12).
- **KF.W6**: KF-SS-9's register decision · the banked btn-interactive/variant folds ride their existing kf-CPW/kf-CS routings.
- **KF.W9**: KF-SS-40 (forced-colors fold) · KF-SS-36 (RTL) · KF-SS-20's PRM data · the SS-13 list above.
- **GLASS-OWNED (standing relays; nothing new relayed by this seat — sole-write law)**: KF-SS-32's producer half rides kf-PR N-2's recorded `aria-valuetext` relay; KF-SS-25's Chip-surface-token question is flagged for the next relay session.
- **NO-WAVE-OWNER — the spring scene-repair packet (SS-1/SS-2 authoring must home it, sequenced with the transport/ribbon packet and the KF-ES-2 facility-honesty spec)**: the **interaction-truth core** — KF-SS-1 (reseat participates in the machine) · KF-SS-2 (facility-aware Reset) · KF-SS-8 (the transport drives the selected channel) · KF-SS-10-as-witness (two channels become honest: paint or declare light) — · the **coordination core** — KF-SS-3 (one debounced, cancellable recompile seam; the library `.fn`-without-`.css` API signal) · KF-SS-16/17's banked-cure riders — · the **surface sweep** — KF-SS-4 · KF-SS-5 · KF-SS-7 · KF-SS-18 (one verb, one skin; re-baseline first) · KF-SS-19 (one voice) · KF-SS-21 (`flex-1 min-h-0`, the sibling's own cure) · KF-SS-22..30 · KF-SS-33 · the INFO tail. Born-RED candidates the packet inherits: the DFA-test-vs-comment assertion (superlative 9 inverted into a gate), a born-state reseat witness (KF-SS-1), a Reset-reaches-the-scene assertion (KF-SS-2), a recompile-count ceiling under a scripted sweep (KF-SS-3), and a case-fidelity assertion on the artifact (rendered text ≡ copied text, KF-SS-4).

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c` (dirty; anchors re-resolve at cure — the worktree's glass-7 sweep has already reached one of this scene's two Button twins): 41 roster rows — 0 new BLOCKER · 9 MAJOR · 16 MINOR · 7 INFO · 9 folds into banked identities, ZERO re-bookings. All five filed BLOCKERs die or fold: the phantom-dependency pair killed baseline-short at this seat's own git instruments (origin/master pins the exact installed 7.0.0); the accent-CTA and Reverse-mirror rows fold to kf-PlaybackRibbon — the latter to a fold the bank had PRE-ASSIGNED to this adjudication by name; the inert-gesture row survives as the headline MAJOR with four dissents; the ungated-facet row splits into two banked folds. Two reader confirmations reversed at the bytes (the Re-seat button re-seats; the Entry-channel loop paints the visible facet), one grouped booking-kill covering fourteen registry-blind identities, and the corpus's best find was missed by all three axes and found by one reader: dock Reset, R, and Escape have never been able to touch this scene — the only rewind code is unreachable, the dock stops an empty group, and the comment says the dock owns it. The component's measured shape: a genuinely excellent hot path, teardown, and physics core (ten sustained superlatives, one of them the tree's own green test refuting the file's own comment) wearing a transport, facility, and prose layer in which the instruction is false at birth, the Reset is a decoy, the artifact uppercases what it promises verbatim, and six comments name the wrong color. Nothing here stamps a wave; every routing is M-25 agglomeration input for the forming X·KF specs. Original ids — D/L/C and the readers' rows for life; KF-SS-1..41 minted here.**

— end of apotheosis r2. The three challenge records and both reader returns remain immutable beside this file per E-1/E-3.
