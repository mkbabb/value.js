claude-fable-5

# kf-KfPillTabs — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ HEAD `8281638c` (branch `master`; working tree dirty only at `.github/workflows/*` + `CHANGELOG.md`/`README.md` — nothing under `demo/` or the installed dist, so every anchor below is HEAD-clean) · registry branch `tranche-u` (value.js).
**Corpus**: three challenge axes — `challenge-D-design.md` · `challenge-L-library.md` · `challenge-C-consumption.md` under `audit/kf-components/KfPillTabs/` — attacked by two hostile re-readers: **RR-1** (design-and-usage) and **RR-2** (library-and-code). Both re-reader returns were themselves challenged by this seat before adoption; one jointly-confirmed cell was killed against the installed producer (see Killed #1).
**Method (L-18/M-23)**: every verdict-level disagreement between the re-readers (the D-2 grade, the L/D-6 thesis, the C-3.2 clip scope, the D-3/D-4 grades) was re-derived against the tree with this seat's own instruments; every kill was re-executed at its decisive file; RR-2's live-MAJOR net-new find (the toolbar leak) was third-derived end to end before banking; and the whole roster was run through the **identity guard** against banked `kf-ChannelControls` (2026-08-07), `kf-AnimationControlsGroup` (2026-08-07), `kf-App`, and census rows — **this corpus is fold-heavy: 15 of 27 confirmed rows were already banked**, including three rows both re-readers filed as net-new "missed" finds. **No browser tooling** (law); all rulings static + installed-dist reads; the irreducibly visual residue rides to SS-13.
**Arbiter's own instruments this session (all read-only)**: `KfPillTabs.vue` WHOLE (124 L); `KfPillTabs/useKfPillTabs.ts` WHOLE (93 L); `useToolbarKeyboard.ts` WHOLE (111 L — zero `stopPropagation`, `preventDefault` at :83/:87/:91/:95); `AnimationControlsGroup/useControlsKeyboardShortcuts.ts` WHOLE (72 L — Arrow/Home/End at :53-58 all `preventDefault: true`; `Mod+Z` :70 bound to timeline state, not playhead); `KeyframesEditor.vue` :54-69 (`role="toolbar"` :65, `@keydown` :69) + :121/:267-272/:281; `SpringPhysicsFacet.vue` :7/:119/:135/:226; `SpringScene.vue` :67/:198; `KeyframesEditor.vue` importer census (SpringPhysicsFacet + the dead `keyframes/index.ts` barrel only); installed glass-ui 7.0.0: `dist/keyboard.js` (`defaultPrevented` grep = **0**; skip predicate = INPUT/TEXTAREA/SELECT/isContentEditable/.monaco-editor, one hit each; `space: [" "]`; window `useEventListener` keydown :76-78), **`dist/styles/utilities/a11y-overrides.css` WHOLE — the corpus decider**: `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; } *:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important } }`, `dist/styles/accessibility.css` head (FIRST import = `./utilities/a11y-overrides.css`; both `prefers-contrast: more` and `forced-colors: active` `[aria-selected="true"]` 2px-border twins present), `dist/styles/index.css` import census (`./accessibility.css` last), `dist/styles/utilities.css` head (does NOT carry the blanket — it rides accessibility.css), PRM file census over `dist/styles/` = **23 files** (RR-2 enumerated 8), `glass/material.css` (`contain: paint` set = wash/quiet/resting/card; `::before` specular selector list includes `.glass-wash`, `z-index: 1`, `inset: 0`; `.glass-capsule-track` absent from BOTH lists), `glass/glass-capsule.css` (`.glass-capsule-track` body: no border, no contain), `glass/ladder.css` (`.glass-wash` plate = `var(--glass-plate-tinted)`, NOT `--card`; the `--muted-foreground → --on-glass-muted` repoint = 1 hit; `@container style(--glass-backdrop: light)` present), `theme/radius.css` (`--radius-strip: 0.75rem`, `--radius-control: var(--radius-pill)`, `--radius-tab: var(--radius-pill)`), `dist/tabs.js` (`semantics === "tabs" ||` + `"tablist"` present); demo: `styles/style.css:3` `@import "@mkbabb/glass-ui/styles"`; PRM census demo-side = **16 files / 18 hits, zero in `demo/styles/`** (both readers' counts EXACT — and immaterial, see Killed #1); `tab-idiom.css` :1-28/:53-79; `design-idioms.css` :7 + `@keyframes enter` :275-288; `font-roles.json` :14-34; `useScrollFade.ts` :90-100 (x-branch `scrollLeft > threshold` at **:96**); `ChannelControls.vue` :29-31/:56/:74/:219/:229-230/:277; `App.vue:169` (`provide(TABS_EXTERNALLY_MANAGED_KEY, true)`); `transport/index.ts` :1-14; barrel-importer greps (instrument umbrella = **0** importers; transport barrel referenced only by it); phantom-file find (`*SpringSidebar*` / `AnimationControls.vue` = **0**); `aria-controls|aria-labelledby` grep = **0** app-wide; `@mkbabb/glass-ui` specifier census in demo = **82**; `test/demo/instrument/KfPillTabs.test.ts` :13-30/:93-95 + full `it(` census (7 cases at :130/:138/:153/:162/:173/:186/:195) + `onClick` grep = **0**; registry cross-reads: `kf-ChannelControls.md` WHOLE, `kf-AnimationControlsGroup.md` WHOLE, `kf-ChromeDock.md` (identity-guard precedent), `ColorSpaceSelector.md` (format exemplar).

## Scoreboard

**27 ADJUDICATED-CONFIRMED rows (7 MAJOR · 16 MINOR · 4 INFO) — 15 FOLDED by reference to banked kf-ChannelControls / kf-AnimationControlsGroup / kf-App / census identities (zero re-bookings) · 12 booked here (1 LIVE MAJOR · 9 MINOR · 2 INFO) · 12 KILLED (with executed refutations, incl. ONE KILL-OF-A-JOINT-CONFIRM decided against the installed producer) · 8 re-reader "missed" finds adopted (2 booked, 4 as riders, 2 refiled) · 8 UNPROVEN-NEEDS-LIVE rows to SS-13.**

The governing fact is banked, not new: **the strip never renders** (kf-ChannelControls §0/C-1 — `App.vue:169` provides `TABS_EXTERNALLY_MANAGED_KEY` `true` unconditionally to the only app root; `ChannelControls.vue:56`'s `v-if="!tabsExternallyManaged"` is permanently false; census S-1 is a **DELETE**, not a replace). Every visual/behavioral row in this corpus is latent under it except two: the banked orphan-tabpanel defect (kf-ChannelControls D-2/C-3) and **this record's one net-new LIVE MAJOR — N-KPT-1, the KeyframesEditor toolbar's window-shortcut scrub leak**, which is the leak the L axis filed against the dead strip, found alive one component over.

The seat's headline adjudication act: **both re-readers jointly confirmed a false cell** — "no prefers-reduced-motion guard reaches this component" — against an installed glass-ui 7.0.0 whose `accessibility.css` FIRST LINE imports a universal PRM blanket (`utilities/a11y-overrides.css`). Killed #1; the banked D-10 framing ("PRM users spared by the substrate blanket; nobody else") was already on the record two days before their returns.

Status verbs per M-25: dispositions are **ADJUDICATED**, not VERIFIED — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets from the census lane taxonomy (`lane-frontend.md` §10): **KF.W1-DEP** (F-1) · **KF.W2-TABS** (S-1) · **KF.W3-SHIM** (F-5) · **KF.W4-PROSE** (S-2). Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Ruled disagreements (the seat's actual work)

| id | RR-1 (design-and-usage) | RR-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D:D-7 / L:D-7 / C:C-9 / C:C-10 — the PRM cell** | CONFIRMED MINOR incl. "no PRM guard anywhere that reaches it … glass-ui carries no universal reduce rule; accessibility.css has none" | CONFIRMED MINOR incl. "none can reach it: every PRM block in glass-ui/dist/styles/ is class-scoped … C/C-10's stated condition is discharged" | **JOINT CELL KILLED — this seat's own dist read; row survives ONLY as banked D-10** | `dist/styles/accessibility.css` opens `@import "./glass/a11y-fallback.css"; @import "./utilities/responsive.css"; @import "./utilities/a11y-overrides.css";` — and `a11y-overrides.css` is a UNIVERSAL blanket: under PRM, `*:not([data-allow-motion])` gets `transition-property: opacity, color, background-color, border-color, box-shadow !important` (font-weight EXCLUDED → the 500→600 step SNAPS, no animated reflow) + `transition-duration: 0.1s !important`, and `animation-duration: 0.01ms !important; animation-iteration-count: 1 !important`. Chain: demo `style.css:3` → `@mkbabb/glass-ui/styles` → `dist/styles/index.css` → `./accessibility.css` (last import). RR-1's cell "accessibility.css contains ZERO prefers-reduced-motion occurrences" is literally true of that file's own bytes and materially false — its first import IS the PRM file. RR-2 enumerated 8 of the **23** PRM-bearing files in dist/styles (my census). The surviving defect is exactly the banked kf-ChannelControls **D-10**: font-weight is a metric-affecting channel in an enumerated transition list, biting NON-PRM users only, latent under §0. |
| **L:D-6 thesis (loud→silent)** | ENDORSED inside its F-1 row ("converted a loud build-time import failure into a silent runtime CSS one, as L:D-6 argues") | KILLED (demo carries dozens of build-time glass-ui module specifiers; `npm ci` fails loudly long before any `var()` resolves to nothing) | **RR-2 wins; RR-1's endorsement struck** | My census: **82** `@mkbabb/glass-ui` specifiers across demo `*.vue/*.ts/*.css`, including `ChannelControls.vue:219` (`import { TooltipProvider, Button } from "@mkbabb/glass-ui"`) in the very file that renders the strip — the same import the banked F-1 rider already names as "the first import that fails on a clean npm ci." Under F-1 the failure mode is loud module-resolution death, not quiet CSS decay; the fork's CSS-only coupling never gets to be the silent failure. The token inventory (7 glass-ui-owned custom properties + `.glass-wash`; only `--color-progress` demo-owned) and the inconsistent fallback posture (:84/:98/:121 carry fallbacks; :100/:103-105/:112-118 do not) are CONFIRMED and ride the F-1 fold as the component rider. |
| **D:D-2 grade** | BLOCKER killed → MINOR (SC 1.4.11 excludes text; challenge-D's own §5 concedes "the label carries the state when the chip cannot"; the 5%/8% recipe is house idiom, not this file's invention) | CONFIRMED MAJOR (arithmetic re-derived to 4 decimals; "the WCAG mapping is contestable even though the arithmetic is not") | **MINOR — RR-1's grade adopted; RR-2's arithmetic adopted with a token correction** | The backdrop-independent ceiling is bankable: an 8% same-family overlay cannot exceed ≈1.25:1 against its own track at ANY backdrop (both readers derived the extrema independently; I re-checked the bound analytically). But BOTH sets of specific ratios (1.2433/1.1725 "vs `--card`") measure the wrong track: my ladder.css read — `.glass-wash`'s plate is `var(--glass-plate-tinted)` with a backdrop-filter, NOT `--card`; the composited plate is not statically computable (the banked S-B lesson, re-learned). Severity: latent under §0; the selected state is also carried by a label colour-token change + weight step (challenge-D §5's own 7.22/11.18/6.06 table — numbers themselves S-B-void, mechanism real); not a WCAG 1.4.11 failure. MINOR, carried as a design-quality input for any reinstated pill idiom. RR-2's MAJOR preserved in DISSENT. |
| **C:C-3.2 clip scope** | Geometric correction: clip = top/bottom of EVERY tab + outer edge of first/last only; interior ring edges overlap the sibling instead (D:D-14's horizontal half) | Same conclusion, filed as a partial refutation of C-3.2's "all four edges" | **CONVERGENT — unified geometry ADOPTED as a rider on banked N-1** | My reads: `material.css` `.glass-wash { contain: paint }` on the strip's own root (`KfPillTabs.vue:17`); strip padding 2px (:85); ring 2px + 1px offset = 3px (:120-122); 2px inter-tab gap (:83) is INSIDE the containment box. Two failure modes, not one: containment clip on the strip perimeter, sibling overpaint interior. D:D-14's vertical half ("5px of room — the ring survives") is KILLED — it totaled slack to the wrapper's boundary and never accounted for the strip's own `contain: paint`, the exact wrong-boundary error the bank's Killed #4 prosecuted against the same axis in the sibling corpus. |
| **D:D-3 / D:D-4 grades** | D-3 MAJOR; D-4 not filed | D-3 MAJOR; D-4 MAJOR | **Both demoted to MINOR (grade coherence with the bank)** | The banked convention grades latent-under-§0 strip defects MINOR (D-8, D-9, D-10, N-1, L-17 all MINOR at the bank). D-3 is latent-squared: the strip never mounts AND the sole call site (`ChannelControls.vue:74-82`) never binds `:orientation`; my grep confirms zero CSS consumes `aria-orientation` anywhere (demo + dist styles). D-4's "indistinguishable" is overstated by its own sibling row: hover-inactive vs active differ by the 500→600 weight step — the corpus cannot hold that step imperceptible (D-4) and simultaneously a perceptible jitter defect (D-7); if either is true the other weakens. Both MINOR, both die with KF.W2-TABS; both readers' MAJOR preserved in DISSENT. |
| **Both readers' "missed" M2 (orphan tabpanels)** | Filed as the corpus's top missed find, MAJOR | Filed as missed M2, MAJOR, "strictly worse than the association gap" | **IDENTITY GUARD — already banked 2026-08-07 as kf-ChannelControls D-2/C-3 (+N-4, the CubeScene `h()` panel), MAJOR, NO-WAVE-OWNER** | Every fact both readers derived (sole `role="tablist"` emitter never mounts; four `role="tabpanel"` sites DO mount incl. `CubeScene.vue:172`; `aria-labelledby|aria-controls` = 0 app-wide — my re-run; the `ChannelControls.vue:29-31` comment naming the rule then breaking it — my re-read) is the banked row verbatim, down to the comment citation. Independent re-derivation from two more seats is corroboration of its reality, not a new booking. The bank-miss event itself is recorded in DISSENT. |

## Adjudicated defect roster

Ids keep their axis names for life; this record's net-new bookings are N-KPT-1/N-KPT-2. Line anchors are keyframes HEAD `8281638c`; re-resolve before executing any cure.

### MAJOR

- **N-KPT-1 (RR-2 net-new "missed" M1; third-derived by this seat end to end) — ADJUDICATED MAJOR · LIVE · Arrow/Home/End traversal of the KeyframesEditor action toolbar also scrubs the animation playhead — the exact leak the L axis filed against the dead strip, alive one component over.**
  Proof (this seat's own): `useToolbarKeyboard.ts:80-101` — `event.preventDefault()` on ArrowRight/ArrowLeft/Home/End (:83/:87/:91/:95), zero `stopPropagation` in the file (whole read). Bound live: `KeyframesEditor.vue:65` `role="toolbar"` + `:69` `@keydown="onToolbarKeydown"`, wired :270-272. Mounts in the shipped app: `SpringPhysicsFacet.vue:119` `<KeyframesEditor :animation="demo.springEditAnim">` → `SpringScene.vue:67` `tabsContent = () => h(SpringPhysicsFacet, …)` → the controls pane; my importer census confirms this is the SOLE live `KeyframesEditor` render site (the other importer is the dead `keyframes/index.ts` barrel). Colliding registry: `useControlsKeyboardShortcuts.ts:53/:54/:57/:58` bind ArrowLeft/ArrowRight/Home/End → `scrubActive(t∓0.01 / 0 / 1)` with `preventDefault: true`, registered window-level by `AnimationControlsGroup` unconditionally (banked group read); glass-ui `dist/keyboard.js`: `defaultPrevented` = 0 occurrences (my grep), window bubble-phase listener (:76), skip predicate covers only INPUT/TEXTAREA/SELECT/isContentEditable/`.monaco-editor` — a toolbar `<button>` is not skipped (Monaco itself IS, which is why only the toolbar bites). FAILURE: spring scene → Tab into "Keyframe actions" → End = focus moves to the last button AND the playhead slams to t=1; Home → t=0; each arrow nudges ∓0.01; `Mod+Z` (:70) covers timeline keyframe state, not playhead position — no undo.
  Identity note: NOT a re-booking of banked group **M-1** (that row is the tablist half, latent under §0, + the reka-Select PLAUSIBLE half) — this is the same producer seam's **first CONFIRMED-LIVE carrier**, which the group DISSENT #2 predicted the BH relay needed ("the inventory argument, not just the two symptoms").
  Terminal disposition: **NO-WAVE-OWNER** (one keyboard-scope spec row with banked group M-1/M-3/M-4: scope the registry or stop propagation at composite-widget roots) **+ glass-ui BH relay rider** (the registry's missing `defaultPrevented` check now has a live, shipped victim — the relay carries this as the inventory's first confirmed member). Cheapest live witness: a 30-second keyboard trace in the spring scene (SS-13 #1).
- **D:D-1 — MAJOR · FOLD → banked kf-ChannelControls §0/C-1/D-3/L-5 (identity guard; readers' BLOCKER grade corrected to the bank's MAJOR — C-1's BLOCKER was killed there on unreachable-code-is-not-breakage grounds).** Both readers re-derived the full chain independently (provide → inject-default → dead `v-if` → single-createApp/single-site mount chain) and every falsifier fired negative — adopted as corroboration. **Riders banked here (net-new corroborations the bank lacked)**: `SpringPhysicsFacet.vue:7` + `useSpringDemo.ts:61` in-tree prose ("the KfPillTabs strip is gone"); `font-roles.json:28-33` — the demo's own typography registry records the component as "a T.H gated-on-publish excision" (with D:D-15's citation drift corrected: the row is :28-33, not :31-34). Census repricing (S-1 replace → **DELETE**) is already the bank's. **FOLD → KF.W2-TABS.**
- **C:C-1 / L:D-3 / D:D-10 — MAJOR · FOLD → banked kf-ChannelControls C-6 (the fork's rationale void against installed 7.0.0; `SegmentedTabsSemantics = "toggle" | "tabs"`; conditional `aria-orientation` at `dist/tabs.js:232` kills the 4.0.1 complaint; the :45-46 contingency already met — the bank's arbiter rider).** My own corroboration: `dist/tabs.js` carries `semantics === "tabs" ||` and emits `"tablist"`. **Riders banked here**: `KfPillTabs.vue:4-5` names two consumer files that DO NOT EXIST (`find` = 0 for `*SpringSidebar*` / `AnimationControls.vue`) while never naming the one real (dead) consumer; the header argues against "glass-ui 4.0.1" with 7.0.0 installed. lane-frontend:613's open design leg is answered at the contract level (both readers + the bank agree). **FOLD → KF.W2-TABS + KF.W4-PROSE.**
- **D:D-6 / C:C-2 / L:D-5 + both readers' missed-M2 — MAJOR · FOLD → banked kf-ChannelControls D-2/C-3 (+N-4) — the orphan-tabpanel/association-gap identity, one row, already NO-WAVE-OWNER as the KF.W2-TABS a11y spec input.** My re-runs: `aria-controls|aria-labelledby` = 0 app-wide; the `ChannelControls.vue:29-31` self-indicting comment verbatim. C:C-2's APG-as-MUST inflation is killed (Killed #9); L:D-5's grading-against-the-file's-own-standard is the honest form and matches the bank's grade.
- **L:D-16 — MAJOR · FOLD → banked kf-ChannelControls L-7/C-4 ≡ kf-App KF-APP-13 (the SURFACE_META fork: two byte-equivalent registries + duplicated interface + duplicated `extraTabsFrom`, split-brain consumers, both "THE ONE" prose blocks false; value-identity re-measured at the bank with the Curve/Physics near-miss on record).** RR-2's INFO→MAJOR upgrade is moot — the bank already grades MAJOR. NO-WAVE-OWNER (collapse copy B into copy A) — the bank owns it.
- **F-1 — MAJOR · FOLD → census F-1 (KF.W1-DEP), banked with component riders at kf-ChannelControls/kf-AnimationControlsGroup.** This component's rider (confirmed): its consumption surface is one class + eight custom properties, seven glass-ui-owned, with fallback posture inconsistent within one file (:84/:98/:121 yes; :100/:103-105/:112-118 no). The L:D-6 thesis built on that inventory is killed (ruling above); the inventory itself rides the fold.
- **L:D-1 / L:D-2 (mechanism) — MAJOR-latent · FOLD → banked kf-AnimationControlsGroup M-1 (the window-registry collision, tablist half).** Both readers KILLED the live framing (BLOCKER/MAJOR) — ratified: no pill can hold focus in the shipped app (§0). The mechanism facts (no `stopPropagation` in `useKfPillTabs`; registry ignores `defaultPrevented`; Space registered `preventDefault: true` and BUTTON not skipped) are all bank-verified and re-verified here; they transfer to N-KPT-1's live carrier and to the KF.W2-TABS acceptance criteria (any successor tablist — incl. glass-ui `useTabRovingFocus` — must be audited against the same registry). Zero re-booking.

### MINOR

- **D:D-2 — MINOR (adjudicated per ruling) · the active chip is an 8% same-family tint, structurally incapable of 3:1 against its own track at any backdrop (ceiling ≈1.2:1); the state is text-carried, so this is design-quality, not a WCAG 1.4.11 failure.** Token correction banked: the track is `var(--glass-plate-tinted)` under two washes, not `--card` — no specific ratio in this corpus is bankable (S-B lesson); the ceiling argument is backdrop-independent and stands. House-idiom provenance (tab-idiom.css's identical 5%/8% pair) kills "the component's own invention, twice." Latent under §0; dies with **KF.W2-TABS**; carried as spec input if any pill idiom is reinstated.
- **D:D-3 — MINOR (demoted per ruling) · `orientation="vertical"` ships correct ARIA + correct remapped keyboard (`useKfPillTabs.ts:67-69`) over a layout that stays horizontal (`.kf-pill-tabs` declares no flex-direction; zero `aria-orientation`-consuming CSS rules anywhere — my greps), and a green test (:186) certifies the working half.** Latent-squared (dead strip + the sole call site never binds `:orientation`). The test's disposition rides Killed #7 (migrate, don't delete). **KF.W2-TABS.**
- **D:D-4 — MINOR (demoted per ruling; RR-2 sole filer) · hover-inactive vs active differ only by a 3% tint delta (≈1.09:1 — RR-2's re-derivation, adopted as mechanism not measurement) + the 500→600 weight step.** Compounds D:D-2; latent. **KF.W2-TABS.**
- **L:D-9 / C:C-11 + RR-2's missed-M6 (joined here) — MINOR · the selection guard exists three times; the SFC's own `select()` (:63-65) — the ONLY copy not backed by the composable — has ZERO test coverage** (my grep: `onClick` = 0 in the test host; the harness never renders the SFC), **so the duplicated branch is precisely the untested branch.** The honest shape: one guarded `select` passed to both the click path and the composable. **KF.W2-TABS** (spec input for the swap's contract).
- **L:D-14 — MINOR · `focusTab` hard-codes an undocumented host shape (direct-children crawl :52-62; handler must sit one level below the tablist :89) into a composable exported at two public paths; neither the params type nor the 17-line docblock states it; the test harness reproduces the flat shape by hand, RATIFYING the assumption rather than checking it.** Any wrapping host (tooltip trigger, icon span, fade track) silently reopens the a12-F1 regression class by markup alone. **KF.W2-TABS** (acceptance criterion for the `useTabRovingFocus` swap).
- **L:D-13 — MINOR · the disabled branch is unreachable in-tree (no supplier sets `disabled` — SURFACE_META carries none; extras are projected to `{value,label}`), and the permitted selected∧disabled state is internally inconsistent and untested: `aria-selected="true"` on a native-disabled (unfocusable) button while the roving stop sits on an `aria-selected="false"` sibling** (my rovingValue + template reads). **KF.W2-TABS.**
- **D:D-21 — MINOR · `border: 0` (:91) reserves no space for the producer's forced-colors/prefers-contrast override — both twins verified in my accessibility.css read (`[aria-selected="true"] { border: 2px solid !important }`) — so selecting a tab grows the content-sized pill 4px per axis inside a 2px-padded strip.** Cure recorded: `border: 2px solid transparent` at rest. Latent; the successor rung must reserve. **KF.W2-TABS.**
- **N-KPT-2 (RR-2's missed-M4, refiled from its L/D-10 kill; verified by this seat) — MINOR · the ENTIRE instrument barrel chain is dead, not just the KfPillTabs row: `demo/components/instrument/index.ts` (four `export *`s under a 22-line laziness docblock) has ZERO importers (my greps), and `transport/index.ts` (the `defineAsyncComponent` wrappers for AnimationControlsGroup/TransportDock/KfPillTabs at :8-12, my read) is referenced only by that dead file.** The code-split both docblocks promise is collected by nobody; the Monaco-laziness guarantee is actually delivered by ChannelControls' own `defineAsyncComponent` panes. Subsumes L:D-10/C:C-7 (the KfPillTabs row alone). **FOLD → KF.W3-SHIM** (one deletion family with the F-5 shim; L:D-11's shim row rides the census F-5 fold below).
- **L:D-11 (+D:D-17's shim half) — MINOR · FOLD → census F-5 / banked kf-ChannelControls L-15/C-10 (the 4-line back-compat shim, sole consumer `ChannelControls.vue:230` — the census §10 step 3 names the repoint; lane F-5's "0 consumers" headline vs its own §7.3 body corrected by both readers, consistent with the bank; `KfPillTabs.vue`-beside-`KfPillTabs/` duplicate-name hazard rides §7.4).** Violates `feedback_no_backwards_compat`; the whole triple deletes together under §0.
- **D:D-5 / L:D-4 / C:C-3.1 — MINOR · FOLD → banked kf-ChannelControls D-9 (`.glass-wash` nested in `.glass-wash` — same rung twice: plate, blur, border, rim, grain, `contain: paint`, specular `::before`, each doubled; glass-ui's own pill strip composes `.glass-capsule-track` instead — my capsule read: no border, no contain).** Rider: no de-duplication rule exists in `glass/*.css` and the component offers no material opt-out prop — "no way to be right" (both readers, confirmed). Composited magnitude → SS-13 (bank's residue). **KF.W2-TABS.**
- **C:C-3.2 (+D:D-14's surviving horizontal half) — MINOR · FOLD → banked kf-ChannelControls N-1 (the focus ring clipped ~1px at the strip's own `contain: paint` padding box).** Unified geometry rider per ruling: perimeter clip top/bottom every tab + outer edges of first/last; interior edges overlap the sibling across the 2px gap and are overpainted. **KF.W2-TABS.**
- **D:D-9 / C:C-4 + RR-2's missed-M5 — MINOR · FOLD → banked kf-ChannelControls D-8 + the L-5 runs-vs-renders corrections (the scroll seam probes a box that can never scroll; `.tabs-overflow-left/-both` unreachable; the apparatus executes forever, effect-free, ×N hosts).** RR-2's sharper cause adopted as rider: `tabsHeaderEl` is permanently null in the shipped app (the ref lives only inside the dead `v-if`), so the seam is UNREACHABLE, not merely mis-pointed — the cure is deletion of the composable + the three mask rules + the ref with the branch (already the bank's disposition); provenance correction: the x-branch is `useScrollFade.ts:96` (my read; C cited :97). **KF.W2-TABS.**
- **D:D-16 / C:C-5 / L:D-12 — MINOR · FOLD → banked kf-ChannelControls L-17/C-11 (icon metadata structurally accepted and silently dropped; the sibling dock host renders it; asymmetric projection inside one computed; capability regression vs `SegmentedTabOption`).** **KF.W2-TABS** (census S-1 capability record).
- **D:D-7 / L:D-7 / C:C-9 / C:C-10 — MINOR · FOLD → banked kf-ChannelControls D-10, WITH THE JOINT PRM CELL KILLED (ruling above).** What survives is exactly the bank's row: `transition: font-weight` on a variable face (bank-verified `font-weight: 200 800`) under a comment claiming narrowness — a metric-affecting channel biting non-PRM users only, latent under §0. All three axes' PRM censuses and both readers' "16 files / 18 hits, none in demo/styles" are EXACT (my re-count) — and immaterial: the blanket rides in from the producer. **KF.W2-TABS.**
- **D:D-8 (+both readers' missed "tab-idiom corpse") — MINOR · FOLD → banked kf-ChannelControls C-15 + N-3 (the `.kf-pill-tab` skin is a second authored copy of the DEAD `.tab-trigger-*` rules — zero appliers tree-wide; both copies unreachable; one pill skin deleted across two files; the consequence clause "every edit made twice" killed ≡ the bank's zero-consumer fact).** New rider banked (RR-2's missed-M3 detail, spot-verified in my font-roles read): `font-roles.json:16-26`'s `filing-tab`/`filing-tab-active` rows select `.tab-trigger-base[data-state=…]` — selectors that match nothing, a green registry gate over an empty set; dies with the C-15 delete. **KF.W2-TABS + KF.W4-PROSE.**
- **D-minor cluster (D:D-15 · D:D-19 · D:D-20 · D:D-22 · D:D-24) — MINOR (cluster row; each verified in my whole-file reads) ·** :80-123 exposes zero component-scoped theming custom property and `font-roles.json` selects on the `.kf-pill-tab` vanity class (D-15, the de-vanitising is a registry migration); `ariaLabel?` has no default and no `aria-labelledby` alternative — an unlabeled tablist is one omitted prop away, though the live (dead) call site does pass one (D-19); arrow mapping is hardcoded physical-LTR with no `dir` read (D-20 — cross-ref banked ChannelControls D-12's `dir=` grep = 0, latent); the `v-for` renders empty/single-option strips unguarded (D-22 — the empty case is Tab-reachable by the rovingValue superlative, the single-option tablist is semantic noise); `@container style(--glass-backdrop: light)` re-points `--muted-foreground` inside wash rungs — unreachable today, a real repaint hazard the moment any `glass-floating/overlay` ancestor appears (D-24, rule verified in my ladder read). All die with **KF.W2-TABS**.

### INFO

- **L:D-8 / C:C-6 — INFO · FOLD → banked kf-ChannelControls N-7 (the :41-42 comment promises a `<script setup>` re-export that cannot exist; zero consumers attempt the advertised path).** RR-2's load-bearing correction banked as rider: because the advertised path was never implemented, the F-5 shim is the only working type path — its deletion must repoint `ChannelControls.vue:230` at `../KfPillTabs/useKfPillTabs`, NOT at the `.vue`. **KF.W4-PROSE + KF.W3-SHIM.**
- **Header mangle (RR-1's missed-M5; verified in my whole read) — INFO · `KfPillTabs.vue:2-5` is broken text: an unterminated code span opening at `` `<SegmentedTabs `` and a dangling `:aria-orientation=` split across lines — the first thing any reader meets, parseable only by reconstruction, on a file whose comment-to-code ratio the axes measured at ~36%.** Neither axis nor the bank observed it; both readers quoted the block for content without noticing. **KF.W4-PROSE.**
- **RR-2's missed-M7 (verified :70-74) — INFO · `Math.max(0, findIndex(...))` coerces a contract-violating -1 to 0, converting an unmatched `value` into a plausible-but-wrong traversal (ArrowRight from nowhere lands on `en[1]`) instead of a detectable miss; the :70 comment ASSERTS the invariant rather than enforcing it.** Unreachable through today's template; the same silent-degradation shape as L:D-14. Cheap cure: early-return on -1. **KF.W2-TABS** (spec note).
- **D:D-11 / D:D-12 (+RR-1's missed radius rung) — INFO · FOLD → banked kf-ChannelControls D-11 (fluid `--type-small` in a fixed-padding chip; the `line-height: 1.75rem` pin dropped from the fork) + D-9/N-3's token rider (glass-ui ships `--radius-strip` AND `--radius-tab` — named rungs for exactly this element — while the strip takes the panel/lg pair; my radius.css read re-confirms all three tokens).** Rider adopted from RR-1's missed find: the INNER pick (`--radius-lg`) is off-ladder too, not just the outer — both rungs wrong, which the bank's "right arithmetic, wrong register" already implies. **KF.W2-TABS.**

## Killed-claims register

1. **The joint PRM-gap cell (both re-readers; premise of the axes' D-7/L-7/C-9/C-10 framings) — KILLED BY THIS SEAT (kill-of-a-joint-confirm).** Installed glass-ui 7.0.0 ships `dist/styles/utilities/a11y-overrides.css` — a universal `*:not([data-allow-motion])` PRM blanket (transitions restricted to an opacity/color/background/border/shadow allow-list at 0.1s; animations clamped to 0.01ms×1) — imported as the FIRST line of `dist/styles/accessibility.css`, which `dist/styles/index.css` imports last, reached by the demo via `style.css:3`. RR-1 grepped `accessibility.css` for the @media string (0 hits — true, useless); RR-2 enumerated PRM blocks per-file and covered 8 of 23 files. Both inherited the axes' search shape; neither checked the banked D-10 ("PRM users spared by the substrate blanket"), on the record since 08-07. Survivor: banked D-10 exactly as written.
2. **RR-1's missed-M2 ("the only LIVE motion in this cluster is unguarded: the tab-panel `enter` slide fires with no PRM guard") — KILLED by the same blanket** (`animation-duration: 0.01ms !important; animation-iteration-count: 1 !important` neuters `@keyframes enter` under PRM). The re-fire COST over the restored Monaco subtree for non-PRM users stays banked as kf-ChannelControls C-14; nothing of the "unguarded" charge survives.
3. **L:D-1 as a live BLOCKER ("every keyboard traversal of the strip ALSO scrubs the playhead … live data-corruption") — KILL RATIFIED (both readers).** No pill can hold focus in the shipped app (§0). Mechanism folds to banked group M-1; the CLASS is real and live at N-KPT-1 — the L axis's own five-falsifier list omitted the one that fires ("the strip never mounts"), and its consequence was hiding one component over.
4. **L:D-2 as a live MAJOR (Space swallowed on a focused tab) — KILL RATIFIED** for the same reason; registry facts confirmed and folded; under automatic activation Space is not part of the APG tablist contract anyway (the L axis's own concession).
5. **D:D-2's BLOCKER grade + WCAG 1.4.11 framing — KILL RATIFIED (RR-1's kill; ruling above).** The axis asserts a BLOCKER in §1 and refutes it in §5 ("the label carries the state when the chip cannot"); 1.4.11 excludes text; the recipe is house idiom (tab-idiom.css:56-61). Survives at MINOR.
6. **D:D-14's vertical half ("2+1+2 = 5px of room — vertically the ring survives") — KILL RATIFIED (both readers; my derivation).** The nearer clip is the strip's OWN `contain: paint` at its 2px padding box; the wrapper's budget is never reached. The same wrong-boundary error the banked record killed (its #4) against the same axis in the sibling corpus — the D axis committed it twice across two corpora in one formation.
7. **D:D-1's test-scoping clause ("…and a 7-case test file — all maintained for a branch that cannot execute") — KILL RATIFIED (RR-1).** The test drives the COMPOSABLE through its own `createApp` host on `document.body` (my reads: :93-95; 7 cases; `onClick` = 0 — it never touches the SFC or the dead branch) and documents its own bite (:20-22). It is a live falsifiable gate and the fork's most transferable asset; disposition = migrate onto `useTabRovingFocus` (concord with banked SUP-4), never delete-with-the-rest.
8. **D:D-8's consequence clause ("every future edit to the house pill skin must now be made twice") — KILL RATIFIED ≡ the bank's C-15 zero-consumer fact.** There is no second painter; both sides of the alleged fork are dead code.
9. **C:C-2's normative inflation ("ARIA 1.2 makes the association a SHOULD; the APG makes it a MUST") — KILL RATIFIED (RR-1).** The APG is non-normative; `aria-controls` is SUPPORTED, not required, on `role=tab`. L:D-5's grading against the file's self-declared "complete contract" is the honest form and is what the banked row carries.
10. **L:D-6's thesis ("the fork traded a loud build-time dependency for a silent runtime CSS one") — KILLED (RR-2's refutation adopted over RR-1's endorsement; ruling above).** 82 build-time specifiers, including in the strip's own consumer file; `npm ci` dies loudly at module resolution first. Inventory survives on the F-1 fold; thesis does not.
11. **C-axis provenance pair — KILL RATIFIED**: the read-list path `demo/components/instrument/composables/useScrollFade.ts` does not exist (real: `…/transport/composables/…`), and the start-branch citation is :96, not :97 (my read). Conclusions unaffected; recorded because this corpus grades on file:line provenance.
12. **L/D-10's mechanism note ("`instrument/index.ts:24` keeps the dead factory reachable from the wider barrel") — KILL RATIFIED (RR-2) and REFILED STRONGER as N-KPT-2**: the wider barrel is itself importer-less, so nothing is kept reachable — the whole chain is dead. A kill that produced a sharper row (the RR-D-D-15 precedent from the sibling corpus).

## Superlatives (provenance preserved; L-18 runs both ways)

- **The §0 walk (D:D-1) — the corpus's most valuable row, and it was reached correctly twice more.** Both readers re-walked the render chain (provide-site, inject-default, single-createApp, single-site chain) rather than grepping the tag, and ran the falsifier lists to extinction. The identity is the bank's (kf-ChannelControls §0, 08-07); two independent convergences on it from hostile seats is the strongest corroboration a banked row has received in this program.
- **Getter-thunk composable params (both readers; both files whole-read by this seat).** `KfPillTabs.vue:68-73` passes options/orientation/modelValue as thunks; `useKfPillTabs.ts` invokes them per-call (:38/:48/:67/:87) — the correct Vue 3.5 reactive-destructure cure, zero toRef ceremony; value.js's own census names it the demo's reference implementation.
- **`rovingValue` (:46-50) — four lines that make the strip structurally Tab-unreachable-proof** (selected-enabled, else first-enabled, undefined only on an empty enabled set), explicitly gated by the test's :195 case. The edge hand-rolled forks in this census routinely miss.
- **`test/demo/instrument/KfPillTabs.test.ts` is a real interaction gate with a recorded bite** — document.body mount for live activeElement, genuine bubbling KeyboardEvents from the focused element, focus AND selection asserted together across 7 cases, kill condition documented (:20-22). A component that got MORE rigorous after shipping a HIGH regression (a12 F1). Preservation spec: migrate the gate onto whatever occupies the seam (banked SUP-4 concord); RR-2's missed-M6 caveat rides it — the gate's blind spot (the SFC's own click path) is part of what migration must close.
- **Zero teardown surface, by construction (RR-2; re-verified by this seat's whole reads)** — no listeners, timers, observers, or watches in either file; the single deferred call (`nextTick(() => btn?.focus())`) resolves its target before the tick. On the axis whose first question is "what leaks," the correct answer here is "nothing."
- **The focus ring rides the demo's one motion-colour authority** (`--color-progress` → `--accent-kf`), `:focus-visible`-gated, currentColor fallback (:120-122). Let down only by the extrinsic geometry (banked N-1) — the choice itself is right.
- **Challenge-D §0.2's token table is exactly reproducible and is why disagreeing with it was cheap** (RR-1's praise, endorsed with a caveat): nine independent token reads, zero drift — but its composition premise (`--card` as the chip's track) inherits the S-B lesson; a reproducible table can still be a table about the wrong stack. Both facts recorded.
- **Negative results at full evidentiary standard**: challenge-C's §4 killed-hypotheses register (attribute fallthrough works; focusTab's crawl is correct against today's template; all eight custom properties resolve — with the two fallback-less criticals named) — RR-1's endorsement ratified. The counter-lesson is Killed #1: the corpus's one joint false cell was also a negative claim ("no PRM rule reaches this"), stated without walking the producer's import graph.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs, ranked)

1. **N-KPT-1's live trace** — spring scene, Tab into the keyframe-actions toolbar, press End/Home/arrows; witness playhead motion beside focus motion. Thirty seconds; also calibrates the severity of banked group M-1's whole family. The FIRST keyboard item on the SS-13 slate.
2. **The one-probe §0 witness (both readers converged)** — `document.querySelectorAll('[role=tablist]').length === 0 && [role=tabpanel] ≥ 1` in the running demo: simultaneously re-witnesses banked §0, banked D-2/C-3 (orphan panels), and the L:D-1/L:D-2 kills. Everything else in this corpus is downstream of it.
3. **Doubled-wash magnitude** — banked at kf-ChannelControls (residue #6); pointer only, not double-booked.
4. **Specular-over-labels** — banked residue #2, now carrying this corpus's source-derived mechanism rider (contain:paint makes the strip a stacking context; the `z-index: 1` `::before` paints above non-positioned button ink; `.glass-capsule-track` sits outside both the contain set and the `::before` list — my material.css read). Only the magnitude remains live-dependent.
5. **Font-weight jitter (non-PRM users)** — banked D-10 residue; face-is-variable already settled at the bank.
6. **Ring clip / sibling overpaint rendered form** — banked N-1's conditional row; only if a strip is ever reinstated.
7. **Forced-colors 4px growth (D:D-21)** — conditional on reinstatement; the reservation cure is static.
8. **Right-edge overflow existence** (whether `scrollWidth > clientWidth` ever exceeds on the overflow-visible box) — moot in the shipped app; noted for completeness.

## DISSENT — preserved, not ruled away

1. **The bank-miss event, now from BOTH hostile seats.** Both re-readers filed as net-new "missed" finds three rows banked on 2026-08-07 (the orphan tabpanels = D-2/C-3; the tab-idiom corpse = C-15/N-3; the named radius rungs = N-3's rider), and jointly confirmed a cell the banked D-10 had already contradicted in writing. Whether or not the registry was in their brief, this is the third consecutive record to log the class (kf-ControlsPaneWrapper's DISSENT, kf-ChromeDock's five confirm-side events, now this) — the identity guard lives at the adjudication seat and nowhere else, which is an argument for keeping the seat, not for trusting the readers less: their independent re-derivations made every fold cheap to ratify.
2. **RR-2's D-2/D-3/D-4 grades (MAJOR) — preserved against my demotions.** RR-2's position: certain arithmetic deserves the grade regardless of latency. This seat ruled grade-coherence with the bank's latency convention. If KF.W2-TABS ever reinstates a pill idiom instead of deleting, the D-2 chip ceiling and the D-3 half-feature revert to MAJOR inputs at that spec's open.
3. **RR-1's F-1 row carried the killed L:D-6 thesis inside an otherwise-correct confirmation** — struck per Killed #10 without prejudice to the row's inventory, which was accurate and is banked on the fold. Recorded as this corpus's instance of a true row transporting a false rider.
4. **Both readers' PRM demo-census exactness (16 files / 18 hits, zero in demo/styles) is preserved** — and is the sharpest available illustration that an exact census of the wrong scope is worth less than one `@import` walk of the right one.
5. **HEAD note**: keyframes HEAD `8281638c` is unchanged since both sibling records (08-07); zero line drift at every site this seat re-opened. Anchors remain re-resolve-before-cure (D-19 lesson).

## Closing verdict

**ADJUDICATED at keyframes HEAD `8281638c`: the census DELETE stands and hardens — 27 confirmed rows (7 MAJOR · 16 MINOR · 4 INFO), of which 15 fold by reference to banked kf-ChannelControls / kf-AnimationControlsGroup / kf-App / census identities with zero re-bookings; 12 booked here, headed by the corpus's one LIVE net-new MAJOR (N-KPT-1: the KeyframesEditor toolbar's Arrow/Home/End traversal scrubs the playhead through the guardless window registry — the leak the L axis filed against the dead strip, found alive one component over; NO-WAVE-OWNER keyboard-scope spec row + glass-ui BH relay); 12 claims killed with executed refutations, headed by a kill-of-a-joint-confirm — both hostile re-readers certified a PRM gap that the installed producer's `a11y-overrides.css` universal blanket closes, one `@import` below the file both of them grepped; the strip's whole latent cluster routes to KF.W2-TABS as a DELETE with a preservation spec (migrate the 7-case keyboard gate onto `useTabRovingFocus`, carry the guard/host-shape/forced-colors acceptance criteria, close the untested click path), the barrels and shim to KF.W3-SHIM, the mangled header and phantom-consumer prose to KF.W4-PROSE, F-1 to KF.W1-DEP; routings are M-25 agglomeration input for the forming X·KF specs, and 8 residue rows ride to SS-13 with the thirty-second toolbar trace ranked first.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
