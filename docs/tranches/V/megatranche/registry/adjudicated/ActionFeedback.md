SEAT RECEIPT — served model id: `claude-fable-5` (Fable 5), FRESH ADJUDICATOR (M-23/L-18), observed in this seat's own system prompt, not inherited.

# ActionFeedback — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Date**: 2026-08-03 · **HEAD**: `64aca0a2` · branch `tranche-u` · **Seat**: `demo/palettes/browser/card/PaletteCard/ActionFeedback.vue` (58 L, SHA `9b5cbe86…` — the corpus receipt, unverified byte-for-byte this session but the live file read whole matches every cited line).
**Corpus**: `challenge-{D-design,C-implementation,L-library,L-library-r2}.md` + `evidence-r2/` (5 probes, 4 witnesses). **r2 governs** the L axis. Motion law: `audit/codex-provenance/motion-quarantine.md` rows M-01..M-08 (FALSE rows may not be carried).
**Re-readers adjudicated**: design-and-usage (`ActionFeedback`) · library-and-code (`actionfeedback-l18-library-and-code`). Both were challenged back; two of their kills were themselves killed or demoted, one of their MAJOR new finds was demoted, and their one direct verdict-level collision (the ApiOfflineChip genus fight) is RULED below.
**Method**: every load-bearing or disputed cell re-derived by this seat's own instruments — full source read (58 L whole); PaletteCard.vue mount/expose/copy/rename sites; BrowsePane.vue:88-98/:225-270; PalettesPane.vue:78-90/:172-212; useBrowsePalettes.ts:120-220; useDialogBrowseActions.ts:66-72; usePaletteActions.ts:42-60; eslint.config.js:225-300 (both G-DEMO-3b declarations); demo/palettes/browser/index.ts:1-9; card/index.ts; ApiOfflineChip.vue whole; api-problem.ts:28-42; animations.css:138-196; foundation.css well tokens; utils.css:9-11; DESIGN.md:384; demo/ui/alert/index.ts; installed glass-ui 7.0.0 dist bytes: `_shared/axes.d.ts` ("NEVER a `variant` member", TONES 5-seal), `alert/{Alert.vue.d.ts,index.d.ts}` (tone + `announce`), the compiled announce→`role="status"|"alert"` + `aria-live` mapping in `dist/glass-ui.js` (grep-witnessed), `chip/types.d.ts` (`tone?: string`), `chipVariants` sm, `toast/Toaster.vue.d.ts` (ToasterPosition-only), `typography/{utilities,semantic,scale}.css` (fira-code liga/calt; text-caption ITALIC; `--type-small`/`--type-caption` fluid clamps), token files (`--success`/`--destructive` light-dark arms; light `--card` hsl(30 85% 96%) / `--foreground` hsl(24 10% 10%); dark-arm `--card` hsl(26 22% 17%) / `--foreground` hsl(30 14% 90%)); @lucide/vue defaultAttributes.mjs; useClipboard.d.ts. Censuses re-run: `@components` specifiers → 0; raw `browser/*.vue` reaches from outside the feature → 0; template `text-xs` → 4 hits / 3 files / 1 in a template class (this file); `text-mono-small` → 28 files; `aria-hidden="true"` → 71; `--type-mono-caption:` definition → nowhere; ActionFeedback/showFeedback/feedback-chip in test/ e2e/ src/ → 0; `console.warn|error` in demo/palettes tree → 38. **Decider instrument**: a full independent WCAG recompute (oklch→oklab→sRGB + oklab color-mix + sRGB alpha compositing, node, this session) of every contrast cell — reader-1's grounds and all six ratios reproduced DIGIT-EXACT: wells `#e9e1d9`/`#42372f`; light success 2.31 / cure 1.65 / light destructive 3.27; dark success 5.64 / dark destructive 2.78; plus one NEW fact neither reader had: `--success` dark arm as ink = **5.85** (passes; the r2 cure's dark half is fine — the regression is light-mode only). Zero browser tooling (law); the live-render residue is declared, not silently absorbed.
**Wave authority for routing**: tranche X ratified — `docs/tranches/X/waves/W0..W11`. X-W7 (Palette Specimen and Domain Split) owns `demo/palettes` whole: X.W7.c-3 rules the catch→console.warn table, X.W7.c-4 creates a props-only `PaletteSpecimen` and moves feedback out, X.W7.d moves feedback to the selected-entity inspector and **deletes PaletteCard.vue**. Every component-local cure below must therefore be authored against the SUCCESSOR surface, not this file — the adjudicated requirements transfer to whatever renders the verdict.

## Scoreboard

**25 CONFIRMED (8 MAJOR · 13 MINOR · 4 INFO) · 13 KILLED/DEMOTED (incl. 2 of the re-readers' own rulings) · 6 UNPROVEN-NEEDS-LIVE · 8 superlatives.**

Blocker-weight cluster: **AF-1/AF-2/AF-3/AF-5 together** — the palette feature's only user-visible failure channel is unannounced, self-erasing on one clock, identity-blind, and addressed through a registry that retains corpses. A publish verdict can be silently lost, silently replaced, silently truncated to 701 ms, and was never announced to AT in any branch. All four are measured or byte-dispositive; none is speculative.

## Ruled disagreements (the incredulity seat's actual work)

| id | design-and-usage | library-and-code | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **R-1 · L-2 severity** | MAJOR (found MORE dead surface: the 2nd declaration at :293-299 over the dead composables glob) | MINOR (re-ran the breach test: zero live violations — harm prospective + documentary) | **MINOR — library-and-code wins; design's extra limb adopted** | My greps: `@components` specifiers = 0, raw cross-feature `.vue` reaches = 0 — no shipping breach, so MAJOR overstates. All four limbs of the inert seam verified (dead `demo/@` globs in :232-238; unresolvable banned specifier; the second inert declaration; index.ts:6-7 still asserting "enforces it standing"). **Cross-corpus identity (this seat's own catch, anti-rename)**: this is fact-identical to ColorSpaceSelector L-5(a) — the SAME eslint objects, the same dead `demo/@` globs, already FOLDed there to a lint-glob-liveness repair with a born-RED `--print-config` command. ONE identity, not two bookings. Also adopted: library-and-code's route correction — `no-restricted-imports` matches SPECIFIER strings, so r2's `**/browser/**/*.vue` re-point under-specifies (intra-feature relative reaches never match). |
| **R-2 · r1 #16 "local duplicate" vs the ApiOfflineChip partial-kill** | Upheld r2's kill: "duplicate of nothing… singleton fork" | Partially killed r2's kill: ApiOfflineChip.vue is a second hand-rolled status chip in the same feature; "two chips should land on one producer atom" | **r2's kill STANDS as scoped; the partial-kill is DEMOTED to an out-of-seat observation** | My whole-file read of ApiOfflineChip.vue: it is a PERSISTENT availability lamp, not a transient action verdict — different genus, different lifetime, different addressee. It carries ratified design provenance in its own comments (K-INV5 R.W3; S.W0 W0-1 — "a DESIGNED state in the instrument's own register… never an apologetic toast"), correct roles on both rungs (`role="alert"` :13 / `role="status"` :21 — the very thing ActionFeedback lacks), and a correct eliminate-not-clamp PRM posture (animation INSIDE `no-preference`, :81-90 — the quarantine's own S-3 pattern). Forcing both onto one atom is the consolidation the KISS edict bans, against a ratified design. r1's sentence, even read charitably in context, adds nothing r2's "singleton fork" doesn't route better. **Salvage adopted**: `--type-mono-caption` is defined NOWHERE (my grep over demo + dist) so ApiOfflineChip's `0.6875rem` fallback always wins — a real INFO defect on a DIFFERENT seat, relayed to ApiOfflineChip's own record, not booked here. |
| **R-3 · the cure atom** (r1: toast/`ActionResult` · r2: `Chip mode="static"` · design: producer ink-rung ask + local re-tokenize · library: glass-ui `Alert`) | ink-rung producer ask (MAJOR miss) | `Alert` (MISS-1 MAJOR), Chip killed | **`Alert` is the atom — library-and-code wins; the ink-rung ask is DEMOTED to a relay question; toast and Chip stay killed** | All four candidates re-derived from dist bytes. Toaster: `ToasterPosition`-only prop — viewport-anchored, severs verdict from card; KILLED (both readers agree). Chip: `tone?: string` UNSEALED, no announce surface, `text-caption` is ITALIC prose (semantic.css byte-read) vs the shipped mono, pill vs band — KILLED as the route. `ActionResult`: a contrivance one import away from the shipped thing — KISS-banned. Alert: `tone?: AlertVariants["tone"]` over the sealed 4-member TONE, `announce?: "off"|"polite"|"assertive"` compiled to `role="status"/"alert"` + matching `aria-live` (my grep of dist/glass-ui.js), house rule DESIGN.md:384 verbatim ("**No hand-rolled Alert**"), live barrel demo/ui/alert with 2 consumers and the B.W2 precedent in its own header. **And Alert settles the ink question the design reader raised**: its tone classes ink text as `text-card-foreground` on a tone-tinted wash, coloring only the svg with `--tone` — the producer's shipped answer to tinted-surface legibility is foreground ink, NOT a tone-ink rung. The design reader's root-cause analysis (fill + ink-on-fill pairing, no on-surface ink) is correct and travels on the relay as a QUESTION; its prescription (mint `--success-ink`) is contradicted by the producer's own idiom and is not adopted as a defect. Residue: wash-on-well fit is U-3; the announce default is "off" and must be set explicitly. |
| **R-4 · C#1** (mount-with-visible-true) | KILLED (unreachable; consumer read) | CONFIRMED at INFO ("code fact, unreachable in the live tree") | **RESCOPED to INFO-LATENT — no substantive conflict** | Both readers derived the same facts (no `v-if` at the mount, `ref(false)` init, stable keys, KeepAlive deactivates ≠ unmounts). The difference is bookkeeping. Ruling: not a kill (the contract gap is real on a 4-prop public surface) and not a live defect — a latent contract note that goes LIVE the moment the AF-3/AF-5 cure hoists the verdict into pane/inspector state, so it is fixed inside that repair, booked AF-25. |
| **R-5 · the contrast corpus-miss** (design reader MAJOR; library reader silent; r2 explicitly disclaimed it at :110) | derived 6 ratios from token bytes | — | **CONFIRMED — this seat's independent recompute reproduces every digit** | My own oklch/oklab/WCAG pipeline against the token bytes: wells `#e9e1d9` / `#42372f`; light success (green-600 on green-500/10) **2.31:1**, light destructive **3.27:1**, dark success (green-400) **5.64:1** (sole pass), dark destructive **2.78:1** — against 4.5:1 at the shipped fixed 12 px. Three of four rungs fail AA; the dark destructive pair r2 canonised as superlative S-4 is the component's WORST reading. New fact: `--success` dark as ink = **5.85** — so the r2 cure (`text-success`) regresses ONLY the light arm (1.65:1, a further −29%). Both the finding and the cure-kill are adopted; exact served values stay U-1. |
| **R-6 · the typography target** (design: `text-mono-small`; library: cites the caption-family utilities) | text-mono-small, fluid `--type-small` | text-mono-caption et al., fluid `--type-caption` | **`text-mono-small` — design wins on the landing; both censuses verified** | semantic/scale byte-reads: `text-mono-caption` is UPPERCASE (`text-transform: uppercase` + caps tracking) — adopting it would shout the verdict; `text-caption` is italic prose. `text-mono-small` = `var(--font-mono)` + fluid `--type-small` (`clamp(0.875rem, 0.8rem + 0.25vw, 1.25rem)`) — the register the card's own siblings use (PaletteCard.vue:64/:72, my read; 28-file census re-run). Moot if Alert adoption lands with its own type, but the adjudicated fact stands: the chip is the card's only non-fluid text and the direct aggravator of R-5's 12 px failures. |

## Adjudicated defect roster

Severity · claim · proof standard · terminal disposition. Line anchors are HEAD `64aca0a2`; re-resolve before curing (D-19). "X-W7" rows land on the SUCCESSOR verdict surface (PaletteSpecimen / entity-inspector rail), per X.W7.c-4/.d — PaletteCard.vue is scheduled to delete.

- **AF-1 · MAJOR — no accessible announcement, no severity role, no recovery action** (D#2 ≡ C#6b; r2 out-of-seat :259). Byte-dispositive: :4-15 one `:class` binding, no `role`/`aria-live`/`aria-atomic`/button/tabindex in 58 lines; publish/save/delete/fork/visibility verdicts never announced. The repo knows the idiom (15 aria-live/role hits incl. same-feature ApiOfflineChip:13/:21, PaginationBar.vue:17 — my reads). **Cure-constraint rider (library MISS-3, adopted)**: `v-if` inserts region + content in one commit — a polite region injected populated is unreliable; the house-correct forms are the persistent PaginationBar region or `role="alert"`/assertive for errors. Alert's `announce` (default OFF — must be set) is the shipped mechanism; per-SR behavior = U-2. → **X-W7** (.c-4/.d successor surface).
- **AF-2 · MAJOR — one lifetime and one motion grammar for both outcomes** (D#1; quarantine M-02 TRUE). :30 single 2500 default, :39-47 variant-blind watcher, :3 one Transition. Every error producer's raw failure text self-erases in 2500 ms with no dismiss/retry/persist path. → **X-W7** (per-tone lifetime + dismiss affordance at the successor surface; D's target matrix :19 adopted minus its quarantined motion cells).
- **AF-3 · MAJOR — no verdict identity; a second verdict inherits the first one's clock** (C#3+C#4 ≡ r2 L-4, measured 701/2500 ms). Byte-dispositive independent of the probe: watch source is the boolean `props.visible`; `showFeedback` sets `true` unconditionally (PaletteCard.vue:238-242, my read); `true→true` never fires the watcher. Reachable from three post-`await` call sites. → **X-W7** (identity-carrying verdict object in pane/inspector state, transition keyed on id).
- **AF-4 · MAJOR — the success rung forks the producer's SEALED tone register three ways** (r2 L-1 + its C3; name `variant`, vocabulary `error` ∉ TONES, rendering raw green literals — axes.d.ts seal byte-read by this seat; singleton fork, census re-run). Defect CONFIRMED; **the r2 one-line cure is KILLED for light mode** (R-5: 1.65:1) — the fix ships only with a legibility answer (Alert's foreground-ink idiom or an ink decision under canon). → **X-W7** under **X-W10** canon; producer questions ride the **GLASS-RELAY** (see below).
- **AF-5 · MAJOR — the imperative registry retains unmounted cards; verdicts silently lost** (r2 L-3a/b). Byte-dispositive: `:ref="(el: any) => el && (cardRefs[…] = el)"` (BrowsePane.vue:94, PalettesPane.vue:84, my reads) — the `el &&` swallows Vue's null-on-unmount; no delete path exists; `?.` guards absence, never deadness; `noUncheckedIndexedAccess` proves nothing about liveness. Aggravators adopted from BOTH readers: KeepAlive caches the panes (PaneSlot.vue:120, my read) so the registry survives navigation, not merely filtering. Runtime symptom rests on r2's probe = U-5; mechanism needs no probe. → **X-W7** (.c-4/.d deletes the channel: `defineExpose`, both registries, both `(el: any)` casts).
- **AF-6 · MAJOR — three of four tone rungs fail WCAG AA at the shipped fixed 12 px; the r2-canonised dark-destructive pair is the worst (2.78:1)** (design reader's corpus-miss; R-5 — INDEPENDENTLY RECOMPUTED, digit-exact). The axis-crossing failure: D owned contrast and never looked; L looked at the exact lines and disclaimed it. → **X-W7** cure at the successor surface; **X-W10** books the canon law (any tone-on-tint pairing states and meets its contrast floor). Exact served values = U-1.
- **AF-7 · MAJOR — a hand-rolled Alert, in violation of the repo's own standing rule** (library MISS-1; R-3). DESIGN.md:384 verbatim; barrel live with 2 consumers and the B.W2 precedent in-file; producer Alert supplies sealed tone + compiled announce mapping (all byte-verified this session). Caveats carried: announce defaults off; wash-on-well fit = U-3; rule standing = U-6. → **X-W7** (adoption decision inside the PaletteSpecimen/inspector restructure — the FORM is Alert's; the HOME is X-W7's to choose).
- **AF-8 · MAJOR — error-posture asymmetry: the feature's only failure channel is wired to 5 of ~38 failure paths** (library MISS-2). Verified: onRename and onVote swallow to console.warn (useBrowsePalettes.ts, my read) while their sibling handlers return `{success,message}` and are surfaced; PaletteCard's rename submit closes the input unconditionally — a rejected rename (412 If-Match) vanishes without signal. Censuses: mine 38, reader 37, X.W7.c-3's "31 catch sites" — different rulers, same magnitude. → **X-W7** (.c-3 owns it BY NAME: the console.warn table + surface-to-user).
- **AF-9 · MINOR — empty message renderable AND reachable** (C#6a, promoted from assertion to proof by the design reader — chain verified: api-problem.ts:40 `response.statusText` fallback (my read; empty on HTTP/2) → `e?.message ?? …` non-nullish → icon-only red chip). → **X-W7** (message contract at the successor surface).
- **AF-10 · MINOR — timer never cleared on unmount** (C#2; both readers' honest scoping adopted: parent-coupled mount makes the stray emit harmless; the residue is a ≤2500 ms closure over a dead instance, compounding AF-5's corpse). → **X-W7** (dies with the restructure).
- **AF-11 · MINOR — name/location/coupling disagree** (r2 L-5; both readers re-verified the table; rename `PaletteCardFeedback.vue` per KISS, mooted if X.W7.c-4 renames the world anyway). → **X-W7**.
- **AF-12 · MINOR — copy governance entirely caller-dependent; WCAG 1.4.1 limb RESCOPED** (D#3; design reader's scope-down adopted: the icon fork is a genuine non-color channel, so use-of-color is SATISFIED — the defect is unpoliced free-form copy: literals beside raw transport text). → **X-W7** (one copy register at the verdict seam).
- **AF-13 · MINOR — the barrel seam is enforced by prose only; BOTH declarations inert** (r2 L-2 as re-graded in R-1). → dead-glob deletion **X-W8** (W8.md:42 owns "delete the six dead `demo/@/**` file globs" by name); the live re-point of G-DEMO-3b at `demo/palettes/**` (specifier-aware, per R-1's correction) and the index.ts:6-7 prose correction are in NO wave's mechanism — **NO-WAVE-OWNER, named residue for the next formation boundary**. Identity cross-ref: ColorSpaceSelector L-5(a).
- **AF-14 · MINOR — typography fork: `text-xs fira-code` hand-pairs a fixed 12 px with a font alias where the producer ships `text-mono-small`** (both readers, merged; R-6 rules the target). The chip is the card's only non-fluid text; direct aggravator of AF-6. → **X-W7** under **X-W10** canon.
- **AF-15 · MINOR — `--vj-celebrate-expanded: 2.5rem` hard-caps unbounded content** (both readers, merged). Mechanism byte-verified this session: animations.css maps the var onto `max-height` at enter-to/leave-from with `overflow: hidden` on both actives — a two-line message (44 px vs 40) is clipped through the enter, pops open when classes drop, snaps down before the leave. Wrap threshold = U-4. → **X-W7** (intrinsically-sized successor).
- **AF-16 · MINOR — a replacement verdict mutates in place with no perceptual event of ANY modality** (design reader's perceptual half of AF-3: the Transition fires only on the `v-if` toggle; success→error swaps tint/icon/text with zero motion, zero announcement, zero focus event — then dies on the inherited clock). → **X-W7** (falls out of the identity-keyed transition + AF-1). 
- **AF-17 · MINOR — both lucide icons break the repo's 71-instance `aria-hidden` convention, 6 instances in this very feature** (design reader; lucide defaults byte-verified: no aria attrs shipped). Cosmetic today only because nothing announces; becomes a regression the day AF-1's region lands with an unnamed graphic inside it. → **X-W7** (with AF-1).
- **AF-18 · MINOR — the element's material register is unreconciled: named a chip, dressed as a panel, laid out as a band** (both readers, merged: block-level `flex`, no width constraint, full-card tint in a `grid-cols-1` wall (grid byte-verified); `rounded-panel` 12 px on a 28 px band butting a `py-2.5` row). A full-bleed verdict band may be the RIGHT gestalt (closer to D's result-ledger than a pill) — the defect is that the three disagree, undecided. → **X-W7** under **X-W10** canon.
- **AF-19 · MINOR — no overflow discipline on the message span, in a card that explicitly refuses to clip** (the one true residue of D#4, rebooked; card comment byte-verified: "NO overflow-hidden (S.W5-10 / S-15-A)"; flex `min-width: auto` + unbreakable transport tokens; every sibling rendering foreign text guards it). → **X-W7**.
- **AF-20 · MINOR — the card discards the producer's discriminated clipboard result at both of its own fallible actions** (design reader; verified: two `void writeClipboard(...)` sites in PaletteCard, `Promise<CopyResult>` signature + the d.ts's own anti-lossy doc, Safari/permission failures silent by nature, the verdict surface mounted 170 lines above). → **X-W7** (.c-8: "each palette mutation… a visible result").
- **AF-21 · MINOR — zero test coverage on a component with a measured clock bug and an imperative expose channel** (both readers, merged; my grep: 0 references in test/ e2e/ src/, while e2e/smoke/flows ships palette-save/delete/fork/flag specs — the exact flows whose verdicts this renders, blind to four MAJORs). → **X-W7** sub-gates (G8-G13 + the n-fixture battery), harness re-hardened by **X-W1**.
- **AF-22 · INFO — `autoDismissMs` is dead configuration** (r2 L-7 + library compounding: read inside the callback, so even a binder cannot retime a live chip). → **X-W7** (delete or make load-bearing with the verdict object).
- **AF-23 · INFO — `autoDismissMs: 0` mints an immortal chip** (library MISS-8; :43 guard is also the file's only exit — latent trap with exactly the semantics D prescribes for errors). → **X-W7** (dies with AF-2's design).
- **AF-24 · INFO — `.fira-code` defined twice; the demo copy drops the producer's ligature settings** (library MISS-7; both bytes verified: producer `@utility` with `"liga","calt"` vs utils.css:9-11 family-only; load-order-dependent winner, currently coincident). → **X-W10** (canon fold of duplicate style authorities).
- **AF-25 · INFO-LATENT — the dismissal watcher is not immediate** (C#1 as ruled in R-4). Unreachable today (no `v-if` at mount, `ref(false)` init, stable keys, KeepAlive deactivates); goes live under the verdict-hoist and is fixed inside it, not before. → **X-W7** (rider on AF-3/AF-5).

## Killed-claims register

1. **D#5** ("no explicit reduced-motion design in this file") — **QUARANTINED FALSE, M-03**; may not be carried (binding law). Independently corroborated by both readers from bytes: producer guard strikes `transform`/`max-height` outright; "in this file" is scope-shopping against a correct central architecture.
2. **C:18 + D:19 reduced-motion obligation cells** — **struck, M-01/M-04**: already discharged by measurement at both preference states.
3. **D#4 first limb** ("absolute/inline placement… can overlap card content") — **REFUTED on fabricated mechanism**: zero positioning declarations in the file or at the mount; an in-flow block displaces, never overlaps. True residues rebooked as AF-15/AF-19 with their own mechanisms.
4. **C#5** ("parent cannot distinguish expiry from acknowledgement") — **VACUOUS**: exactly one emit source exists (:44); a discriminator with one value loses nothing. The real gap (no dismiss affordance at all) lives in AF-2.
5. **r1 L#10** (`type: "success" | "error"` too small) — **KILLED as written**: no prop named `type` exists. Re-founded as AF-4's sealed-axis violation (r2's C3, verified).
6. **r1 L#11 first limb** ("visibility and lifetime controlled internally") — **half false**: `visible` is a parent-owned prop. The surviving, worse statement (no party owns verdict identity) is AF-3.
7. **r1 L#16** ("this is a local duplicate") — **KILLED as scoped** (R-2): no second verdict chip exists; singleton fork. The ApiOfflineChip partial-kill of this kill is itself demoted (see 12).
8. **r1 L#5 + target #16** (route to toast / mint a shared `ActionResult`) — **KILLED both halves**: Toaster is viewport-anchored (d.ts byte-read) — a UX regression sold as conformance; `ActionResult` is a KISS-banned contrivance one import from the shipped Alert (R-3).
9. **r1 L#12** ("not scoped to a producer motion token or reduced-motion policy") — **QUARANTINED FALSE, M-07** (net); 6/6 tokens producer-defined, specificity-decided PRM. Only the non-defect "globally named" limb is true.
10. **r2 L-1's prescribed cure and its scoping note** ("`bg-success/10 text-success`, drop the `dark:`"; "not a contrast regression") — **KILLED for light mode by this seat's recompute**: the cure drops 2.31 → 1.65 (−29%); the note compared fork-vs-token and never either-vs-ground. NARROWED by my new fact: the dark arm (5.85) is fine. Ships only with AF-6's answer.
11. **r2 L-6's atom and premise** (`Chip mode="static"`, "near-transcription") — **KILLED as the route**: italic `text-caption` vs shipped mono (byte-read), pill capsule vs block band, `tone?: string` UNSEALED (defeats the conformance goal), no announce surface (leaves AF-1 standing). The under-adoption observation survives as context; the BJ relay item survives (below).
12. **library-and-code R2-C7** (the ApiOfflineChip partial-kill-of-kill: "two hand-rolled status chips… both land on one producer atom") — **DEMOTED, R-2**: different genus, ratified design provenance, correct roles, correct PRM posture; consolidation would be the contrivance the KISS edict bans. Salvage: the undefined `--type-mono-caption` token, relayed out-of-seat.
13. **design-and-usage's second MAJOR miss** (producer ink-rung gap; "add `--success-ink` to the sealed cohort") — **DEMOTED to a relay QUESTION, R-3**: the root-cause reading of the fill+ink-on-fill pairing is correct, but the producer's own Alert idiom (foreground ink on tone wash, tone on the icon only) is the shipped answer the prescription ignored. Not adopted as a defect; travels on the relay as a question.

## GLASS-RELAY (standing BH/BJ channel, owner edict — producer items, never local patches)

1. `ChipVisualProps["tone"]` is `string` against the producer's own sealed `Tone` (r2's ask, byte-verified) — narrow it.
2. QUESTION (demoted ask from R-3): is foreground-ink-on-tone-wash the intended TOTAL answer for tinted status surfaces, or is an on-tint ink rung planned? Cite AF-6's measured failures as the motivating data, Alert's TONE classes as the observed idiom.
3. Out-of-seat salvage: `--type-mono-caption` is consumed (ApiOfflineChip.vue:47) but defined nowhere in dist — either ship the token or the consumer's fallback is the de-facto spec.

## Superlatives (provenance preserved)

- **S-1 · The scoped style block** (r2; both readers; re-read whole by this seat): three geometry vars only, reasoning stated in-file, zero durations/easings/keyframes/PRM branches — local geometry, producer choreography. Well-built precisely where the corpus graded it red.
- **S-2 · Pure-sink dependency direction** (r2; both): two external imports, zero relative; cannot cycle. r1 documented this exact property as a fault.
- **S-3 · Barrel discipline** (r2): named re-exports with PI-6 stated in-file; ActionFeedback correctly NOT exported. Intent right; enforcement rotted (AF-13).
- **S-4 · The error rung knows the idiom** (r2, **caveat now load-bearing**): `bg-destructive/10 text-destructive` is ownership-exact — and in dark mode it is the component's WORST contrast (2.78:1, my recompute). A superlative of OWNERSHIP only; never cite it as the legibility template.
- **S-5 · The feature already contains the correct a11y patterns** (library reader; verified): PaginationBar.vue:17's persistent polite region; ApiOfflineChip's role-per-rung. AF-1's cure has an in-feature template.
- **S-6 · challenge-L-library-r2.md is the document of record** (both readers; this seat concurs after re-deriving its load-bearing cells): killed four of its own axis's r1 claims, corrected a provenance mechanism against itself, scoped its own finding DOWN, receipts re-runnable. Its one blind spot (absolute contrast) is now AF-6 — and it is a blind spot BECAUSE it stayed inside its axis.
- **S-7 · The C axis reached AF-3 statically** (library reader): report-authored, zero probes, and still stated both the un-retriggered watcher and the stale-timer consequence that r2 later measured at 701 ms. Two more of its six findings survived where no other axis reached them.
- **S-8 · The K-PALID comment** (design reader; PalettesPane.vue:202-204 re-read): the one place the addressing hazard was reasoned about in writing — the registry-key/entity-identity divergence visible at compile time. AF-5 is the same instinct's blind side; the team is one step from seeing it.

## UNPROVEN-NEEDS-LIVE (visual-audit inputs — the next probe session's worklist)

- **U-1** · Exact served contrast ratios, both schemes, `getComputedStyle` on a mounted chip. My derivation (and the design reader's, independently) is digit-stable and the margins are wide (2.31/2.78/3.27 vs 4.5) — the verdicts are not in doubt; the fix spec should pin served numbers (browser gamut-mapping + `color-mix(in oklab)` rounding move the third decimal).
- **U-2** · Whether an `aria-live` region inserted already-populated announces in the targeted SR/browser pairs — decides whether AF-1's cure is one attribute (assertive/`role="alert"`) or the persistent-region form. Do not ship on the one-liner assumption.
- **U-3** · Alert's wash-tier tone surface (`--glass-bg-wash` + backdrop-filter) rendered inside the rung-2 WELL card — the AF-7 adoption's one open render question.
- **U-4** · The AF-15 wrap threshold at shipped breakpoints (mechanism proved from bytes; the wrapping string length is not).
- **U-5** · AF-5's runtime symptom (no-throw/no-log on a dead instance) rests on r2's probe, un-re-run by every seat since; the source mechanism is dispositive regardless. Re-run `evidence-r2/af-L-r2-registry-leak.mjs` when a stack is next up.
- **U-6** · Standing of DESIGN.md:384 ("No hand-rolled Alert" — cites a W43-killed alias path; the barrel and both consumers survive, so substance clearly lives) — an owner/ledger question, not a bytes question.

## Closing verdict

**ADJUDICATED — RED, with the red in different places than the corpus put it.** 25 rows confirmed (8 MAJOR), 13 claims killed or demoted — including one kill-of-a-kill and one demoted MAJOR from the re-readers themselves; the motion grade the original corpus led with was its falsest claim (M-03/M-07), and the gravest true defect (AF-6, three AA failures) appeared in no axis file at all. Every confirmed row carries exactly one terminal disposition: 21 → **X-W7** (whose PaletteSpecimen/inspector restructure is the cure surface — the component as sited is scheduled to dissolve), 1 → **X-W8** + a named **NO-WAVE-OWNER** residue (the G-DEMO-3b re-point + index.ts:6-7 prose), 2 → **X-W10** canon, 3 producer items → **GLASS-RELAY**, 6 live questions → the visual-audit worklist above. Dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave.

— end of apotheosis. The four challenger files, both re-reader returns, and `evidence-r2/` remain immutable beside this file per E-1/E-3.
