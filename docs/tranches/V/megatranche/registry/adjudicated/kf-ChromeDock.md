# kf-ChromeDock — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-07 · **Subject repo**: `/Users/mkbabb/Programming/keyframes.js` at HEAD `8281638c` (same HEAD as the kf-App / kf-ChannelControls / kf-ControlsPaneWrapper adjudications — anchors directly comparable). **Substrate caveat carried from kf-App ruling 1**: `ChromeDock.vue` and `MbabbMenu.vue` are DIRTY at the worktree and MOVED at the frontier (`origin/master` is 41 commits ahead; MbabbMenu −146 lines there) — **every cure re-resolves anchors at `origin/master` first** (D-19 law + the kf-App substrate law).
**Corpus**: the three challenge axes at `docs/tranches/V/megatranche/audit/kf-components/ChromeDock/` (challenge-D-design.md 711 L incl. the R2/R3 merged passes; challenge-L-library.md 285 L incl. its §0 self-retraction; challenge-C-consumption.md 687 L incl. the Round-3 integrity note), as re-read by two hostile re-readers — **RR-1 (design-and-usage)** and **RR-2 (library-and-code)** — whose returns this record adjudicates.
**Method (L-14/L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (D-2 kill-vs-confirm, D-13/m-10's `startCollapsed` default, the L/S★-2 necessity cell, the M-5/C-6 "no bypassing path" universality, RR-1's MISSED #4 duration) was re-derived against the tree with this seat's own instruments; the two kills that fell BLOCKER-weight claims (R3-1, D-19) were third-derived rather than adopted. Both re-readers' "confirmed" rosters were then run through the identity guard — and **five of their jointly-confirmed rows turned out to be already banked** (D-1 → kf-App KF-APP-6; the SURFACE_META fork → KF-APP-13 / kf-ChannelControls L-7·C-4; D-10/m-11/C-9 → KF-APP-26; D-17 → KF-APP-24; the D-9 announcement half → KF-APP-33) — the same not-checking-the-bank event the kf-ControlsPaneWrapper DISSENT recorded, now from the confirm direction.
**Arbiter's own instruments this session (all static + git + one tsc oracle; ZERO browser tooling — probe parsimony)**: full read of ChromeDock.vue (385 L); surfaceTabs.ts WHOLE (42 L); controlSurfaces.ts:40-46/:96-125; useEasingDemo.ts:345-365 (the facility channel); scenes.ts:1-30/:100-140; TransportDock.vue:43/:183-214/:236-237/:357; MbabbMenu.vue:4/:96; layout.css:105-170; design-idioms.css:19-21/:96-120; style.css:1-5/:28-36/:160-166/:286-294; all six icon assets read at `assets/icons/` (3× 32×32 `image-rendering="pixelated"` rasters; easing hardcoded `hsl(248,88%,71%)`; spring/sequence `var(--rainbow-*/--color-progress, currentColor)` with every token DEFINED); **installed producer bytes**: dock.js:265-381 (`useDockState` WHOLE — every collapse route), :615-641 (`Ue`, the touch composable BOTH readers missed), :694-843 (props / setup / layers render), useTouchGate-B4mzQcHJ.js WHOLE (the module-level document-touchstart deactivator), adaptive-legibility.css WHOLE (the D-2 decider: `:where(.glass-dock){--muted-foreground: var(--foreground)}` + the `contrast-color()` arm), layers.css WHOLE (the stagger cascade + `--dock-layer-gap`), triggers.css (the 4-state ink ladder), layer-group.css (`.dock-separator` margin), density.css (trigger-padding rungs + `--dock-control-size` scoping), sizing.css (`--icon-xs..lg` = 12/14/16/20 vs demo 14/16/20/24; `--dock-selected-accent` present; `--dock-icon-glyph` ratio), icon-button.css (`[data-active]` seat + `> svg` glyph width), bridges.css head (`@theme inline` → utilities resolve `var(--muted-foreground)` AT THE ELEMENT), useDockShellProps.d.ts:120/:124/:158-160 (the three doc-drift cells), dock index.d.ts (exports incl. `useOptionalDockContext`; NO `DockSelectTrigger`), DockControl.vue.d.ts:21-39 (`active` prop), feedback.d.ts:4 (STATUS_DOT_STATES health axis), SelectItem.vue.d.ts (`hideIndicator` real), select.js (`SelectLabel` exported), reka SelectGroup.js:15-30 + SelectLabel.js:25-27 (the always-emitted dangling `aria-labelledby`); **executed oracles/greps (exit-checked)**: a THIRD-independent `InstanceType` probe — scratchpad tsconfig path-mapping `@mkbabb/glass-ui/dock` to the byte-unmodified dist, repo's own tsc 6.0.3: sole diagnostic = the deliberate `boolean→number` TS2322; `IsAny` false, `@ts-expect-error` on a phantom member satisfied, `expanded/expand/keepOpen/release` all typed (kills R3-1 thrice over); `--dock-margin *:` definitions = **0** (11 consumption refs) + `git log -S"--dock-margin:"` → **`c7f7c96d` "refactor(demo): modernize CSS tokens…"** body "Remove redundant color token declarations now in glass-ui defaults" (the NEW provenance rider); `data-glass-dock-portal` = **2 hits in select-DD6Ly6xg.js** (the S★-2-cell decider); `aria-hidden` in ChromeDock.vue = **0** across 9 glyph sites; status-dot.js `aria-hidden": e.label ? void 0 : "true"`; dock.js `data-morphing` = 6; `keydown` case-insensitive = **3 `onKeydown`** (:1039/:1185/:1203 — DockLayer roving focus + search mode, neither on this consumer's path; both readers' "grep keydown → 0" cell corrected, substance intact); `CONTROLS_PANE_HOVER_KEY` writers = ChromeDock SOLE; `dockCardinality` in demo/state/index.ts = 0 (the barrel omission); `surfaceTabs` in test/ = 0; channel-level `surfaces:` = 0; useControlsKeyboardShortcuts.ts:45-75 WHOLE (no scene-switch, no dock action); glass-ui exports map = 73 subpaths, `./select`/`./dock`/`./status-dot` all present; the style import chain `style.css:3 → @mkbabb/glass-ui/styles → styles/index.css → components/dock/styles/index.css → adaptive-legibility.css` (each edge grepped); **registry cross-reads**: kf-App.md (KF-APP-4/-6/-13/-24/-26/-33/-42 + ruling 1's three-substrate map), kf-ChannelControls.md (L-7/C-4), kf-ControlsPaneWrapper.md (the ChromeDock.vue:330-331 aria-expanded row; D-m9), kf-AnimationControlsGroup.md (D-3), CENSUS-2026-08-03 §(a) lane taxonomy.
Status verbs per M-25: dispositions are **ADJUDICATED, not VERIFIED** — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets follow the census lane taxonomy (CENSUS-2026-08-03 §(a)): **KF.W0** substrate settle · **KF.W4** gates/hygiene · **KF.W6** glass suffusion incl. the phantom-token audit · **KF.W9** Safari/visual audit (owns ALL SS-13 residue) · **KF.W10** fold discharge; folds to banked rows carry the bank's own routing. Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Scoreboard

**52 rows stand: 1 BLOCKER · 13 MAJOR · 22 MINOR · 6 INFO booked here, plus 10 FOLDS to banked identities (three of them blocker-weight at their banks). 7 corpus claims KILLED with executed refutations — two of them BLOCKER-grade (R3-1, D-19), both third-derived by this seat before ratification. 9 net-new finds adjudicated in: 8 from the re-readers (one MAJOR pin-demotion defect no original axis reached) and ONE from this seat — a hold-bypassing touch-axis collapse path in the installed producer that REVERSES the corpus's and both re-readers' unanimous M-5/C-6 verdict: the 15-line watchdog all five prior readers sentenced as dead code is live and load-bearing on every touch device. Identity guards held with zero re-bookings — and caught FIVE jointly-confirmed rows as already banked.**

Blocker-weight center of mass: the collapsed dock is the app's ONLY scene navigation and contains zero tab stops — 2.5 s after any pointer idle the entire scene graph is keyboard-unreachable, while the producer's `#persistent` escape hatch and the sibling dock's focusable-Button house pattern both sit unused one file away. The two banked blockers this component rides (the `--dock-margin` IACVT chain voiding its only positioning declaration; the no-SFC-typecheck gate concealing a real TS2322 at :128) were both re-confirmed by both readers without checking the bank.

## Ruled disagreements (the seat's actual work)

| question | RR-1 (design-and-usage) | RR-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-2 (trigger glyph ink)** | KILLED on resolved values + direction | CONFIRMED MAJOR (rescoped: glyph can't follow hover ink) | **KILLED as filed — RR-1 wins; a MINOR rescope is booked in its place** | My bytes, whole chain: adaptive-legibility.css `:where(.glass-dock){ --muted-foreground: var(--foreground) }` (+ `@supports (color: contrast-color(white))` arm → `contrast-color(var(--card))`), loaded via the style.css:3 import chain (each edge grepped); bridges.css is `@theme inline` so `text-muted-foreground` emits `color: var(--muted-foreground)` resolved AT THE ELEMENT. Inside the dock the glyphs render at FULL `--foreground`, not the 5.21:1 muted rung D-2 tabulates; triggers.css rest ink is `color-mix(… var(--dock-fg-on-aurora …) calc(var(--opacity-icon-muted)*100%) …)` — the glyph is BRIGHTER than its label at rest and equal on hover/open: the split CLOSES, it does not widen to 2.8×. What survives (booked MINOR): the glyph ink is a state-invariant direct declaration (the mechanism half was always sound), it opts OUT of the `--dock-fg-on-aurora` adaptive flip in the base arm, and the SAME class renders a different ink in the portalled menu (RR-1's missed find, adopted). RR-2's MAJOR grade and its D-3 cell "one grey glyph … on the landing scene's row" both die for the in-dock sites — the grey reading is true ONLY in the portal. |
| **D-13 + L/m-10 (`startCollapsed` default)** | m-10 KILLED both halves — runtime default is EXPANDED; `:start-collapsed="true"` is a real override | CONFIRMED incl. "`true` is already the default ChromeDock re-pins" (citing dock.js:519 `?? !0`) | **RR-1 wins; RR-2's cell corrected** | Both citations are real bytes but only one is live: dock.js:703 declares `startCollapsed: { type: Boolean }` with NO default, so Vue's Boolean cast resolves an absent attribute to `false` — and :520's `e.startCollapsed ?? !0` then sees `false`, never `undefined`; the `?? !0` arm is DEAD on the SFC path (live only for a raw-options composable caller). :722 seeds `initialExpanded: !a.value`. So `:start-collapsed="true"` at :224 is a genuine behavioural override — the one that creates the D-18/C-1 first-idle keyboard hole — and TransportDock (no attribute) seeds `"hover"`: the two co-visible docks mount in OPPOSITE states (RR-1's MISSED #4, adopted with correction below). useDockShellProps.d.ts:124's "default true" joins :120's "default 2000" (vs compiled 3600, my :270 read) and :158-160's CSS-selector claim (vs the :714-717 resolver that nulls strings) as the THIRD doc/code divergence in one file — the drift ledger books at D-13. |
| **M-5 / C-6 (the re-expand watchdog defends a nonexistent path)** | CONFIRMED ("E() reachable only from I() or an imperative call") | CONFIRMED ("while the hold is applied no collapse is reachable") | **REVERSED IN PART — this seat's own find; the watchdog is LIVE on the touch axis** | Both readers enumerated T/A/M/I in `useDockState` and stopped. GlassDock ALSO wires `Ue` (dock.js:752-761) with `collapse: L` — and `Ue`'s watcher (:633-634) `!isActive && expanded && !isPinned && !alwaysExpanded && collapse()` consults NEITHER the hold counter (never passed in) NOR the teleport stamp. `useTouchGate` (my whole-file read): a module-level document `touchstart` listener deactivates any active gate whose `controlEl` does not `.contains(e.target)` — raw containment, no `data-glass-dock-owner` exemption — and a `collapseDelay`-long inactivity timer deactivates too. `E()` itself checks only `alwaysExpanded`. FAILURE SCENARIO (touch device): touch-expand the dock (gate active) → open the scene Select (hold > 0; menu PORTALLED to body) → tap any menu item → document touchstart fires, target is outside the dock el → gate deactivates → watcher collapses the dock UNDER THE OPEN MENU, hold bypassed — precisely the hazard class ChromeDock.vue:197-202 re-expands out of. The 9-line rationale's NAMED mechanism ("document-pointerdown … bypassing the hold") is still false — `I()` is hold-guarded AND stamp-exempt (`Fe`, :347) — so the row survives as a prose-mechanism defect PLUS a producer defect, but the cure both readers implied (delete the watchdog) is BLOCKED: it would regress touch. Producer half → glass-ui BH relay (the touch gate should honor the hold + the portal stamp). Live witness → KF.W9 SS-13 #1. |
| **R3-1 (BLOCKER: `InstanceType` = slot bag; 5 latent TS2339s; C-2 gated on it)** | KILLED (probe vs real package) | KILLED (probe vs real package) | **KILL RATIFIED — third-derived by this seat** | My own scratchpad tsconfig path-maps `@mkbabb/glass-ui/dock` → the byte-unmodified `dist/components/dock/index.d.ts`; repo's tsc 6.0.3. Sole diagnostic: the deliberate `const bad: number = x.expanded` → TS2322 boolean-not-number. `IsAny<Inst> = false` compiles; `@ts-expect-error` over a phantom member is satisfied (TS2339 fires); `x.expanded: boolean`, `x.expand()/keepOpen()/release()` clean. R3-1's own text concedes it probed "a faithful mirror of that declaration shape" — the exact harness fault axis L's §0 had ALREADY retracted for pass-1's M-4. Three concordant real-package probes vs one hand-mirror: the five call sites (:161/:200/:206/:207/:208) are typed today; C-2's cure is NOT gated; the `ComponentExposed<>`/`Pick<>` remedies must not land. |
| **D-19 (the inline arm renders on easing/spring; D-8 goes live)** | KILLED (non-sequitur + live-tree enumeration) | KILLED (same, incl. the knock-on kills) | **KILL RATIFIED — enumeration re-derived** | `dockCardinality` (surfaceTabs.ts:29-41, my whole read) decides `controlZone.kind` by COUNT alone; `controlLabelRedundant` REQUIRES `kind === "inline"` already — D-19's "redundant === false → kind === inline" inverts the dependency, inheriting `length === 1` from the file's own stale :118-120 comment. Live: easing's facility (useEasingDemo.ts:349-363, my read) is ONE channel carrying `animation: previewAnim` + `facets:[{surface:"easing"}]` → `surfacesFor` (controlSurfaces.ts:107-120, my read) grants the triad + facet = 4 tabs → the `select` arm; channel-level `surfaces:` grep = 0 demo-wide. `allControlTabs.length ∈ {0,3,4}` — never 1. The knock-ons die with it (D-8 stays MINOR-latent; D-17's inline-tab rider void) and D-19's "cheap live kill" is a FALSE-CONFIRMATION TRAP for SS-13 (both arms render a Curve-labelled node; the discriminating observation is combobox-vs-bare-div — handed to KF.W9). D-19's one true sub-claim — Curve/Physics vs Easing/Spring means the redundancy predicate can NEVER fire — books inside M-2/C-4. |
| **C/S-2 + C-17 (inject resolves against authoring scope; the `#items` prop round-trip is necessary)** | KILLED (SSR probe + in-tree stamp proof) | KILLED (same) | **KILL RATIFIED** | Vue resolves `inject` along the runtime parent chain, and the tree itself proves it inside this very render: glass-ui's `SelectContent` — authored by ChromeDock at :246/:295 as slot content inside GlassDock's default slot — calls `useOptionalDockContext()` and stamps `data-glass-dock-portal`/`data-glass-dock-owner` (my grep: 2 hits in select-DD6Ly6xg.js), a stamp unreachable under lexical resolution; `<DockSeparator>` at :283/:326 rides the same DI. `useOptionalDockContext` is exported from the `/dock` subpath already imported (my index.d.ts read). So MbabbMenu CAN hold the dock itself and M-4's five-site round-trip is removable. A superlative was awarded for a false statement about Vue; it is withdrawn, not qualified. |
| **L/S★-2's necessity cell ("SelectContent carries no portal stamp")** | Endorsed the cell | KILLED the cell | **RR-2 wins — cell dead, superlative core intact, and the mutex is MORE necessary than either reader argued** | The stamp exists (my grep). But the stamp only exempts the pointerdown path `I()`; the idle/mouseleave routes still need the HOLD (Select has no `keepDockOpen` — that half of the cell is true), and per the touch ruling above the gate path ignores stamp AND hold — only the watchdog covers it. The component's defensive posture is vindicated for a reason none of its own comments (and no prior reader) names. |

**Cure-level rulings inside agreed verdicts**: RR-2's MISSED #1 (adopted MAJOR below) — `expand()` at :207 writes `"hover"` unconditionally (my dock.js:299-305 read; `x()` re-pins only under `alwaysExpanded`), so a user-pinned dock (`N()`, :329-335 — the collapsed pill's click PINS under `:start-collapsed="true"`) is silently DEMOTED by the keep-open watcher and then auto-collapses ~3.3 s after the menu closes (`F()` :339-343 → `T()` → 2500 ms). The corrected cure: drop `expand()` from :204-208 (`keepOpen()` suffices there — the popup can only open from the non-inert full layer), KEEP the :197-202 watchdog for the touch path, and consolidate the two `expanded` watchers (C-14). RR-1's MISSED #4 duration cell corrected: a `"hover"`-seeded TransportDock does NOT stay expanded "indefinitely" — the document-pointerdown listener attaches whenever expanded (:349-357, `immediate`), so the first outside click collapses it; the asymmetric first paint (top pill collapsed, bottom dock fully expanded) stands until that click. R3-2's duration correction (RR-1) ratified on my own read: `I()`'s guard excludes only `alwaysExpanded`/held/transitioning — a PINNED dock collapses on outside pointerdown, so the idle-dim defeat ends there.

## Adjudicated defect roster

Line anchors are HEAD/worktree `8281638c`; ChromeDock.vue is dirty and moved at the frontier — **re-resolve at `origin/master` before any cure**. Dispositions are ADJUDICATED (M-25); routings are agglomeration input for the forming X·KF specs.

### BLOCKER (booked here)

- **D-18 / C-1 — ADJUDICATED BLOCKER · the collapsed dock contains zero tab stops, so the app's only scene navigation is keyboard-unreachable 2.5 s after any pointer idle.** My bytes: dock.js:819-828 renders the full layer `inert` when not active; :829-838 renders the summary as a bare `<div>` with `onClick` only — no role, no tabindex, no name; the case-corrected keyboard census is 3 `onKeydown` sites, ALL in DockLayer roving-focus/search paths this consumer never mounts (both readers' "grep keydown → 0" cell corrected, substance intact). ChromeDock.vue:363-366 fills `#collapsed` with bare SVGs; `#persistent` (rendered OUTSIDE the layers, never inert — :813) is unused; the house pattern is one file away (TransportDock.vue:183-213: a real focusable `Button` with a disambiguated aria-label in the same slot); the shortcut registry (useControlsKeyboardShortcuts.ts:45-75, my whole read) has no scene-switch and no dock action. `onFocusIn` can never fire — the only focusable subtree is inert. WCAG 2.1.1. Compounds the banked kf-ControlsPaneWrapper aria-expanded row and the KF-APP-5 inert-chrome family — ONE a11y spec surface, three components' edges. Disposition: **NO-WAVE-OWNER** (a11y spec row; the consumer half is a `#persistent` or focusable-`#collapsed` fit) **+ glass-ui BH relay** (producer half: a summary layer that is click-expandable but not focusable-by-construction, `onClickCollapsed` pointer-only).

### Blocker-weight FOLDS (identity guard; zero re-booking)

- **D-1 — FOLD → kf-App KF-APP-6 (KF.W6 phantom-token audit; rendered offset → KF.W9).** `--dock-margin` phantom at all three substrates; `top: var(--dock-top-anchor)` (:216, no fallback) → IACVT → `auto`; my re-run: 0 definitions / 11 demo refs. **Riders added to the bank**: (a) NEW provenance — my `git log -S`: the deleting commit is `c7f7c96d` "refactor(demo): modernize CSS tokens and remove redundant styles", body "Remove redundant color token declarations now in glass-ui defaults" — the token died on a false belief about producer ownership (glass-ui 7.0.0 ships zero `--dock-margin`); (b) the `@supports (anchor-name)` arm (layout.css:159-170, my read) carries the same dead term and loses to the inline style regardless. **Identity-guard event: BOTH re-readers filed this as a fresh confirmed BLOCKER; it was banked eleven days of registry ago.**
- **L/B-1 — FOLD → census SCH-1/F-1 (KF.W0 substrate settle), per kf-App ruling 1.** Phantom `@mkbabb/glass-ui` at worktree/HEAD — but **frontier-CURED** (`origin/master` declares 7.0.0 as an exact devDependency; kf-App's three-substrate map). Both re-readers booked it BLOCKER without the substrate triage; carried corrected. Component rider: ChromeDock is the densest consumer — three subpaths + a type edge (:6-11/:22-28/:29/:158) — so at the two stale substrates the graph dies at :11, mounted unconditionally from App.vue.
- **C-2 / B-2 — FOLD → kf-App KF-APP-4 (KF.W4 gates lane).** No SFC typecheck anywhere (`check` = bare tsc ×2, CI runs `check:lib`, my package.json read; vue-tsc grep = 0). **Riders this component contributes to the banked born-RED**: the concealed TS2322 at :128 (`allControlTabs` is `{value: string}[]` by the :49/:72 widened annotations, passed into `dockCardinality`'s `readonly ControlSurfaceTab[]` whose `value: ControlSurface` — both readers measured it with real compilers, my type-read of both files concurs); C-8 (the DFA alphabet widened to `string` at :49/:62/:66/:71/:72/:154 while both source modules export the union — booked MAJOR below); m-8 (`String(id)` at :239/:289 launders `AcceptableValue` into `"[object Object]"` scene ids); m-1/C-7 (+m-9, unadjudicated by either reader, ratified by this seat's :292 read) — the unguarded `TAB_ICONS[…find(…)…]` trigger lookup is `Component | undefined` under `noUncheckedIndexedAccess`, its guarded twins 7 lines below prove the intent, and the inline `find` belongs in a computed: one repair.

### MAJOR (booked here)

- **M-4 — MAJOR · the `itemsPopupOpen` prop and its five-site round-trip rest on a provably false Vue mechanism claim (:73-78).** Ruled above (S-2/C-17 kills are its mirror). Cost: a prop, an `isAnyOpen` term (:172), `v-model:open` in MbabbMenu.vue:96, a ref + two bindings in App.vue — all reaching a `keepOpen()` MbabbMenu can call directly via `useOptionalDockContext()` on the subpath it already imports. Disposition: **NO-WAVE-OWNER** (delete the round-trip; MbabbMenu self-holds).
- **M-5 / C-6 — MAJOR (RESCOPED per ruling) · the watchdog's 9-line rationale names a false mechanism, but the watchdog itself is LIVE on the touch axis.** The named path (document-pointerdown bypassing the hold) does not exist — `I()` is hold-guarded and stamp-exempt; the REAL path (useTouchGate deactivation → `Ue`'s unguarded `collapse()`) exists and neither the comment nor any prior reader names it. Disposition: **NO-WAVE-OWNER** (correct the prose to the touch-gate truth; keep the watchdog; drop the :207 `expand()`; consolidate C-14's twin watchers) **+ glass-ui BH relay** (the touch gate should consult the hold counter and the `data-glass-dock-owner` stamp — a portalled-menu tap currently collapses the dock under the menu on every touch device, masked in this app ONLY by ChromeDock's watchdog). Live witness → KF.W9 SS-13 #1.
- **RR-2 MISSED #1 (adopted) — MAJOR · `dockRef.value?.expand()` at :207 silently demotes a user-pinned dock to `hover`, converting a pin (documented exit: click-outside) into a timed auto-collapse ~3.3 s after the menu interaction.** My bytes: `D()` :299-305 writes `"hover"` unconditionally; `N()` :329-335 pins on the collapsed pill's first click; `F()` :339-343 schedules `T()` at ≤800 ms once holds drain. The call's only reachable effect is the demotion (a popup can only open from the non-inert full layer, so the dock is never collapsed at :207). Also compounds R3-2. Disposition: **NO-WAVE-OWNER** (one batch with M-5's prose cure and R3-2).
- **R3-2 — MAJOR · `expanded` (= hover ∪ PINNED) is mirrored into `controlsPaneHover`, whose sole reader treats it as hover — a pinned dock defeats the F9 idle rest-dim for the life of the pin.** ChromeDock is the SOLE writer (my grep); duration bounded at the next outside pointerdown (my `I()`/`E()` read — pinned is not exempt). Cross-ref: the banked kf-ControlsPaneWrapper S-5 superlative praises the reader's `isPaneIdle` derivation — the defect is on the writer side, here. Disposition: **NO-WAVE-OWNER** (mirror hover only, or expose/consume `isPinned`; one batch with the row above).
- **M-2 / C-4 — MAJOR · the `inline` control-zone arm is structurally unreachable, `controlLabelRedundant` can never be true, and four comment blocks assert the opposite.** My enumeration ruling (D-19 kill) is the proof; D-19's true sub-claim folds in as the sharpest cell: the T.E8 relabel (Curve/Physics vs Easing/Spring) permanently disarmed the equality predicate the comments were written against. `inlineControlTab` (:144-146) is permanently `undefined`; :307-318 never renders; the `.dock-inline-tab` block (:380-384) is unreachable CSS. D-8/m-5/C-11 (ARIA-prohibited `aria-label` on the role-generic div) stays MINOR-LATENT inside this arm and dies if the arm is deleted; D-15 (easing/spring share the `Activity` glyph) rides as INFO on the same registry surface. Disposition: **NO-WAVE-OWNER** (decide: delete the arm + dead CSS + false prose, or make the predicate live; the decision seat is the SAME registry cure as the KF-APP-13 fold).
- **M-3 — MAJOR · `currentLabel` is a required prop whose only consumer is the dead predicate, and its :359-361 comment describes a render that does not happen** (the trigger renders `currentIcon`/`<Home>` + `<SelectValue/>`; no `{{ currentLabel }}` interpolation exists — my template read). Disposition: **NO-WAVE-OWNER** (dispose WITH M-2 — the prop lives or dies with the predicate).
- **D-3 — MAJOR · ChromeDock.vue:36-37's "every survivor themes via currentColor" is false for all six scene glyphs, and one dropdown column mixes three theming regimes** (raster-pixelated ×3 / hardcoded-hsl ×1 / token-var-with-dead-fallback ×2 — every asset read by this seat; every named token DEFINED so no `currentColor` fallback ever fires). CORRECTED CELL: inside the dock the `text-muted-foreground` intent is not "muted grey" (the D-2 ruling) — the grey-among-saturated reading is live only in the PORTALLED menu rows. Disposition: **NO-WAVE-OWNER** (prose truth + an icon ink/format policy decision; batch with D-4/D-5).
- **D-4 — MAJOR · the three 32×32 pixel-art glyphs are nearest-neighbour scaled at non-integer factors (icon-sm 0.5×/1.0× at DPR1/2; icon-md 0.625×/1.25× — fractional at BOTH common DPRs) and the same `currentIcon` jumps +25% (16→20px) across the collapse crossfade** on the element the file calls "the scene's identity moment". Geometry static-certain (my asset + design-idioms reads); perceived shear → KF.W9. Disposition: **NO-WAVE-OWNER**.
- **D-5 — MAJOR · four glyph rungs in one dock row, none the dock's own; the demo `icon-*` utilities shadow glass-ui's `--icon-*` tokens by a uniform +1 rung under identical stems** (demo 14/16/20/24 vs producer 12/14/16/20 — both sides read by this seat), and `icon-lg` (24px, utilities layer) defeats `--dock-icon-glyph`'s invariant 0.5 ratio (my sizing.css + icon-button.css reads), drifting it 0.60/0.545 with pointer type. The identity glyph is the SMALLEST in the row, the nav toggle the LARGEST — the reverse of the file's own :226-232 grammar. Disposition: **KF.W6** (token-alignment family with KF-APP-25/-26).
- **D-22 + RR-1 MISSED #1 (merged; adopted) — MAJOR · the `gap-2` wrapper div at :225 is the single child of GlassDock's default slot: it substitutes 8px for the token'd 6px (D-22), silently DEFEATS the producer's entire staggered morph choreography, and latently defeats `overflow="wrap"`.** My layers.css read: the reveal cascade rides `.glass-dock[data-morphing] .dock-layer.is-active > *` with `nth-child(2 of *)/nth-last-child(2 of *)/…` onset steps — with ONE direct child every arm is a no-op and the whole row fades/scales as a single block; `data-morphing` is live (dock.js ×6). Same one-line root cause, strictly more consequential than the 2px: the primitive's signature expand animation is lost on the app's most-seen chrome. Cure: delete the wrapper (children directly into the slot); retune `--dock-layer-gap` if 8px is wanted. Disposition: **KF.W6** (glass consumption correctness); magnitude → KF.W9.
- **D-23 / C-5 — MAJOR · the dock floats over the animated aurora `<canvas>` and never tells the legibility observer — the sampler runs (both defaults ON, my :694/:705-717 reads) and resolves `null`, falling to the static stack-walk on the landing route.** CURE LAW (RR-1's missed caveat, adopted; my resolver read): the d.ts advertises "element, getter, or CSS selector" but the installed resolver returns `null` for a string — the remedy MUST pass a getter or element; a selector-string diff would ship green and change nothing. The doc cell joins D-13's drift ledger. Disposition: **KF.W6**; contrast delta + observer cost → KF.W9.
- **C-8 — MAJOR · the DFA alphabet is widened to `string` at every prop boundary, from the very modules ChromeDock already imports** — the direct enabling condition of the :128 TS2322 and the silent-typo drop-out at :98. Disposition: **NO-WAVE-OWNER** (type the boundary `ControlSurface`/`ControlSurfaceTab`; lands under the KF-APP-4 gate).
- **D-20 — MAJOR (the component-owned half) · the panel toggle declines DockControl's `active` prop — no `aria-pressed`, no `data-active`, no selected seat — encoding state solely by mutating its accessible name.** My DockControl.vue.d.ts:21-39 + icon-button.css reads: the producer ships exactly this affordance, ChromeDock passes only the ternary aria-label (:327-332). The name-mutation/aria-expanded identity is **ALREADY BANKED** at kf-ControlsPaneWrapper's MISSED-MAJOR row (which cites ChromeDock.vue:330-331) — folded by reference, NOT re-booked; what books HERE is the declined-`active`/no-seat half, whose one-prop cure lives in this file. Disposition: **NO-WAVE-OWNER** (same a11y spec surface as D-18).

### FOLDS to banked identities (continued; zero re-booking)

- **M-1 / C-3 / D-14 + R3-7 — FOLD → kf-App KF-APP-13 ≡ kf-ChannelControls L-7/C-4 (NO-WAVE-OWNER at the bank: collapse copy B into copy A, repoint ChromeDock/TransportDock, correct the invariant comments).** ChromeDock's split-brain import block (:15-21 — BUILT_IN_SURFACES from copy A, SURFACE_META + dockCardinality from copy B) is already the bank's named cell. **Riders NEW here**: (a) R3-7's test-net split — `control-surface-dfa.test.ts` imports ONLY the canonical copy while BOTH docks render the fork (my greps: `surfaceTabs` in test/ = 0; TransportDock.vue:237); the gate is green and pointed at the file neither dock renders; (b) the mechanical cause — `demo/state/index.ts` exports no `dockCardinality` (my grep = 0); (c) the fork is the weaker copy (drops the return annotation).
- **D-17 — FOLD → kf-App KF-APP-24 (≡ kf-ControlsPaneWrapper D-m9): zero `forced-colors` demo-wide.** The producer mitigation for StatusDot survives (both readers); the inline-tab rider died with D-19.
- **D-10 / m-11 / C-9 — FOLD → kf-App KF-APP-26 (KF.W6 phantom-token audit): `--dock-label-padding-inline` phantom; real seam `--dock-trigger-padding-inline` per density rung** (my density.css read: 0.4375/0.5/0.625/1rem × `--dock-scale`). Riders: `DockSelectTrigger` does not exist in 7.0.0 (my index.d.ts read — the :375 rationale names a retired primitive); latent-only while M-2's arm is dead.
- **D-27 / m-7 — FOLD → kf-AnimationControlsGroup D-3 (the one-boundary-many-declarations identity; kf-ControlsPaneWrapper added a 6th site).** ChromeDock.vue:148's `useMediaQuery("(max-width: 1023px)")` is the SEVENTH site (third JS instance), and the un-annotated one; both readers count 18 `1023px` sites demo-wide.
- **D-9 (announcement half) — FOLD → kf-App KF-APP-33 (banked: `aria-label="Scene"` overrides `<SelectValue/>` per accname §4.3.1-2C, same shape ×3 — :241/:291/:309; AT utterance → KF.W9).** Struck from this corpus's UNPROVEN list — the bank adjudicated the mechanism statically; the policy-inconsistency half rides the same row.
- **C-12 — FOLD → kf-App KF-APP-42 (warmScene worth), rider added: the warm is `@pointerenter`-only (:261, the sole emitter — my SFC read), so keyboard traversal of the same list never prefetches** — sharpening the bank's "reachable only from inside the open dropdown" cell. Home's missing warm stays correct (no loader — my scenes.ts read).

### MINOR (booked here)

- **D-2-RESCOPED — MINOR (ruled)** · the trigger/collapsed glyph ink is a state-invariant direct declaration: it cannot follow triggers.css's rest→hover ladder (brighter at rest, equal on hover — the lockup breathes only on one side), and in the base arm it opts OUT of the `--dock-fg-on-aurora` adaptive flip its label follows (the two converge only under `@supports contrast-color`). NO-WAVE-OWNER (icon ink policy with D-3); over-aurora visibility → KF.W9.
- **RR-1 MISSED #2 (adopted) — MINOR · one class, two inks: `text-muted-foreground` resolves to full `--foreground` on the trigger (inside `.glass-dock`) and to muted grey in the portalled menu row (root scope)** — the colour half of C-10's portal finding; on the nav root the Home menu glyph reads as the disabled affordance beside five saturated icons. NO-WAVE-OWNER.
- **RR-1 MISSED #3 (adopted) — MINOR · zero `aria-hidden` on nine decorative glyph sites (my grep = 0) against the producer's own convention** (dock.js ×7 stamps; status-dot.js conditional-and-correct). NO-WAVE-OWNER (a11y hygiene).
- **RR-1 MISSED #4 (adopted, duration corrected) — MINOR · the two co-visible docks mount in OPPOSITE states** (ChromeDock `:start-collapsed="true"`; TransportDock seeds `"hover"` per the m-10 ruling) — the asymmetric first paint lasts until the first outside pointerdown (NOT "indefinitely"; my `I()` read). NO-WAVE-OWNER (decide the pair's boot posture; batch with D-13).
- **RR-2 MISSED #2 (adopted) — MINOR · both Select open bindings hand-roll `:open` + `@update:open` for a WritableComputedRef built for `v-model:open`** (:237-238/:287-288), while the sibling MbabbMenu.vue:4 uses the shorthand. NO-WAVE-OWNER (hygiene; the longhand also hides the mutex's guarded setter at the call site).
- **D-13 — MINOR · two docks, two collapse clocks (2500 vs the compiled 3600 default) with no shared token — plus the producer doc-drift ledger: useDockShellProps.d.ts claims delay "default 2000" (:120), `startCollapsed` "default true" (:124, false at the SFC layer per the ruling), and a `backgroundCanvas` selector form (:160) the resolver nulls.** The 2500 also sets D-18's unreachability window. NO-WAVE-OWNER (consumer); the three-cell drift ledger → **glass-ui BH relay**.
- **D-6 / m-6 — MINOR · `[&>span]:line-clamp-none` is a no-op cargo-cult (DockTrigger renders reka's SelectTrigger, never glass-ui's clamped wrapper — dock.js:22, both readers; consistent with my import-head read) and the dock label has no truncation contract** (`white-space: nowrap`, no overflow terms — my triggers.css read). NO-WAVE-OWNER; live overflow → KF.W9.
- **D-7 / C-15 / m-13 — MINOR · the two adjacent dropdowns invert glyph/marker order (StatusDot-icon-label vs icon-StatusDot-label), and :265 alone lacks the `<Home v-else>` fallback its two siblings carry.** My template read. NO-WAVE-OWNER.
- **D-8 / m-5 / C-11 — MINOR-LATENT (per the D-19 kill) · `aria-label` on a role-generic `<div>` (:307-311), ARIA 1.2-prohibited — inside the arm M-2 recommends deleting.** Dies with M-2's cure; repair-order note recorded so no wave "fixes" ARIA inside a branch being deleted.
- **D-11 / m-4 / R3-5 — MINOR · the :326 `DockSeparator` is unconditional while its :283 twin is zone-gated — the file violates its own elision doctrine on the nav zone; latent in-tree (App always fills `#items`) but the component is publicly exported with a non-App-host contract.** NO-WAVE-OWNER.
- **D-12 — MINOR · `shrink-0` on 3 of 9 glyph sites (RR-1's broadened census adopted: absent at :243/:251/:292/:299/:313/:365 incl. the controls TRIGGER glyph).** Shrink pressure → KF.W9. NO-WAVE-OWNER.
- **D-24 — MINOR · a health-status primitive carries the selection signal (`online` green / dashed `unknown` ring per non-current row) against the dock's own "never a saturated brand hue" register, while `--dock-selected-accent` — built for the job — sits unused (my sizing.css read confirms it ships).** Disposition: **KF.W6** (consume the producer's selection vocabulary). The filled/hollow shape contrast is the mitigating half.
- **D-25 — MINOR (sharpened) · both `SelectGroup`s ship `role="group" aria-labelledby="reka-select-group-N"` pointing at NOTHING** — my reka reads: the id is ALWAYS emitted (SelectGroup.js:22-26) and only `SelectLabel` (exported by glass-ui's select.js — my grep) ever stamps it. An axe `aria-valid-attr-value` failure ×2. NO-WAVE-OWNER.
- **D-26 — MINOR · `py-2 px-3` overrides the design system's item padding at three sites, in physical properties, reading no density rung** — while the file's own :381 correctly uses logical `padding-inline`. NO-WAVE-OWNER.
- **C-10 — MINOR · `dock-label` on portalled menu content falls to the 2.5rem `--dock-control-size` literal (the token is `.glass-dock[data-size]`-scoped — my density.css read); divergence bounded to coarse-pointer ≥1024px by the style.css:288-292 flatten (my read).** Computed-style witness → KF.W9. NO-WAVE-OWNER.
- **m-2 / C-13 — MINOR · `dockCardinality` is called with a literal `channels: []` here and a literal `tabs: []` in TransportDock (:357 — my grep): the shared module is real, the shared CALL is fictional; no invocation exercises the conflated signature.** Rides the KF-APP-13 registry cure. NO-WAVE-OWNER.
- **m-12 — MINOR · `:model-value="selectedControl ?? 'controls'"` hardcodes a member the rendered set need not contain (non-App hosts); the set-derived fallback is one expression away.** NO-WAVE-OWNER.
- **m-3 / R3-6 — MINOR · three import granularities in one 24-line block; `./select` exists among the 73 export subpaths (my exports read).** NO-WAVE-OWNER (hygiene).
- **R3-4 — MINOR · Home's menu label is a hard-coded literal beside six single-sourced siblings; `homeScene.label` exists (my scenes.ts read) and App passes only the id — a rename desyncs trigger from menu row, the exact drift class :33-38 claims eradicated for icons.** NO-WAVE-OWNER.
- **D-21 — MINOR (grade per RR-2's note, both readers concur) · one toggle, two icon grammars (mobile state-chevrons vs desktop action-panels), and the mobile open-arm's up-chevron sits under `aria-label="Close controls"` for a bottom sheet whose dismiss gesture is downward.** NO-WAVE-OWNER (with the banked CPW toggle row's cure).
- **m-1 / C-7 + m-9 — MINOR · booked as the KF-APP-4 rider above** (listed here for count integrity: the unguarded trigger lookup + the inline `find`; one repair).
- **m-8 — MINOR · booked as the KF-APP-4 rider above** (the `String()` laundering at the emit boundary; App hands the result to `runSceneSwitch` unvalidated).

### INFO

- **D-16 + L/i-1 — INFO · `--z-dock` documented as "the bottom dock band" (style.css:32, my read) while ChromeDock rides it on the top band; plus the stale-prose ledger: :13 (deleted dockZones.ts), :109-113 (dead clause), :122 (deleted branch), :220 ("3.3.0" vs installed 7.0.0), :348 ("4.0.0").** NO-WAVE-OWNER (prose sweep; every M-row above corrects its own comments in the same motion).
- **D-15 — INFO · easing and spring share `TAB_ICONS["Activity"]` (my surfaceTabs read) — zero-information glyph column wherever both could co-occur; latent today.** Rides the KF-APP-13 registry cure.
- **C-14 — INFO (rescoped by the M-5 ruling) · two watchers on `dockRef.value?.expanded` could be one — but the watchdog member STAYS; only the consolidation and the :207 `expand()` removal land.** Rides the M-5 batch.
- **R3-8 — INFO · App.vue:171-172's "both ChromeDock and TransportDock share the same ref" is stale — ChromeDock is the sole writer (my grep).** NO-WAVE-OWNER (one comment).
- **i-2 — INFO (uncharged, preserved) · the unpaired `release()` on a mount with `itemsPopupOpen === true` is safe only by the undocumented `Math.max(0, …)` clamp (my `F()` read)** — dies with M-4's round-trip deletion.
- **D-28 — INFO · RTL unsupported demo-wide; ChromeDock's three exposures recorded (physical `px-3` ×3; symmetric centering safe; direction-named PanelLeft* glyphs).** NO-WAVE-OWNER (a11y spec input).

## Killed-claims register (executed refutations; nothing re-litigable without new evidence)

1. **D-19 (MAJOR, whole finding) — KILLED, ratified by this seat's enumeration.** "controlLabelRedundant === false → kind === inline → the arm renders on easing and spring." Non-sequitur (the predicate REQUIRES kind === inline; kind is count-decided) + false on the live tree (easing/spring derive 4 tabs each — my facility + `surfacesFor` reads; `allControlTabs.length ∈ {0,3,4}`). Knock-ons dead: D-8 stays latent; D-17's inline-tab rider void; the "cheap live kill" is a false-confirmation trap (both arms render a Curve-labelled node) — KF.W9 receives the discriminating test (combobox vs bare div) instead. The true sub-claim (the T.E8 relabel disarmed the predicate forever) survives INSIDE M-2.
2. **R3-1 (BLOCKER) — KILLED, third-derived.** `InstanceType<typeof GlassDock>` is the fully-typed public instance, not the slot bag: three independent probes against the byte-unmodified package (RR-1, RR-2, this seat — sole diagnostic the deliberate TS2322) vs R3-1's hand-mirrored declaration, the exact harness fault axis L's §0 had already retracted once. The five "latent TS2339s" do not exist; C-2's cure is ungated; the `ComponentExposed<>`/`Pick<>` remedies must not land.
3. **C/S-2 (superlative) + C-17's endorsement — KILLED.** Vue resolves inject along the runtime parent chain; the tree proves it inside this component's own render (the portalled SelectContent's dock stamp — my grep — and DockSeparator's DI, both authored as GlassDock slot content). MbabbMenu can self-hold via the already-imported subpath; the five-site round-trip is M-4's defect, not a design achievement.
4. **D-2 (MAJOR, as filed) — KILLED on resolved values and direction; MINOR rescope booked.** The dock deliberately remaps `--muted-foreground` to `var(--foreground)` (adaptive-legibility.css, reached via the verified import chain; `@theme inline` bridge resolves at the element): the glyphs render at full foreground, the rest split runs the OTHER way (glyph brighter than the 0.8α label), and it closes on hover. The mechanism half (utilities-layer direct color beats the inherited ladder) survives into the rescope.
5. **C-16 — KILLED for the two in-dock sites** (a future `currentColor` glyph on trigger or pill renders full foreground, not muted grey); the hazard survives ONLY for the portalled rows and is booked as the portal-ink-split MINOR.
6. **L/m-10 — KILLED both halves, inverted** (with RR-2's re-confirmation cell corrected in the same ruling): `startCollapsed`'s effective SFC default is FALSE (props `{type: Boolean}` + Vue Boolean cast; the `?? !0` arm dead), so `:start-collapsed="true"` is a genuine override — the one that creates D-18's window; and the delay default is 3600, not the d.ts's 2000. Both cells re-filed constructively (D-13's ledger; MISSED #4).
7. **L/S★-2's "SelectContent carries no portal stamp" cell — KILLED** (my grep: 2 stamps in the select chunk). The mutex superlative core and the hold-necessity half stand — strengthened by the touch ruling.

**Reader-cell corrections by this seat (recorded against the returns, not the corpus)**: (a) BOTH readers' "every collapse route honours the hold / no such path exists in 7.0.0" — false on the touch axis (the M-5 reversal; the single most consequential correction in this record); (b) both readers' "grep keydown → 0" — 3 `onKeydown` sites exist, none on this consumer's path; (c) RR-1's MISSED-#4 "stays expanded indefinitely" — collapses at the first outside pointerdown; (d) RR-2's D/D-3 "grey glyph on the landing scene's row" — true only in the portal.

## Superlatives (provenance preserved; L-18 runs both ways)

- **S-1 (both readers; core third-verified) · the popup mutex (:166-186)** — mutual exclusion from the state's TYPE (`Ref<PopupKey | null>`), with the `else if (openPopup.value === key)` stale-close guard defeating reka's late losing-close. AMENDED per kill 7: its click-away-necessity cell was false, but the touch ruling makes the surrounding defensive posture (hold + watchdog) MORE necessary than the file itself knows — vindicated for a reason no comment names.
- **S-2 (D-axis + RR-1; verified) · total motion delegation** — the entire scoped block is three declarations; zero local transition/animation/JS tween; PRM honored wholesale through the producer.
- **S-3 (L S★-1 + RR-2; my SFC read concurs) · zero teardown surface by construction** — every side effect is a setup-scope `watch` + one VueUse composable; no manually-registered subscription anywhere.
- **S-4 (L S★-3 + C S-5; both readers) · kf-CONSUME at the collapsed slot** — the consumer shrank to the circle rather than patching GlassDock; survived two producer majors. D-18 is its a11y COST and outranks it; the direction was still right.
- **S-5 (L S★-4; my layout.css read) · the explicit `data-dock-tether` opt-in** — layout bound to declared intent, not class soup; the one part of the anchoring system KF-APP-6 does not void.
- **S-6 (L S★-7; my read) · conservative DFA degradation** — an absent projection yields the COMPLETE triad, never a subset, with the regression it cured documented in place.
- **S-7 (C S-4; my d.ts reads) · `hide-indicator` consumed as the producer's own opt-out**, replacement marker another glass primitive correctly un-labelled (`aria-hidden` stamped by status-dot.js). D-24's objection is to the chosen VOCABULARY, not the consumption.
- **S-8 (D S-4 + C S-3; my scenes.ts read) · Home structurally excluded from the scene loop** — declared outside the array; the fallback falls through by construction, not by special case.
- **S-9 (C S-1; amended by R3-5/R3-6) · the clean glass boundary at the leaf** — zero reka imports, zero vendored primitives; the two amendments (unguarded :326 separator; root-barrel Select) are booked above.
- **S-10 (L §0 + both readers + this seat) · axis L's self-retraction is the most valuable page in the corpus** — it diagnosed the mirror-harness fault, re-probed in situ, published its falsifier, and was vindicated THREE more times this cycle (RR-1, RR-2, this seat) against R3-1's re-commission of the retracted error. The discipline is the finding: probe the tree, not a mirror of it.
- **S-11 (RR-1's echo; my layout.css read) · layout.css:105-136 is the best proportional reasoning in the read set** (one φ token, a deliberate /4-vs-/φ asymmetry, the anchor ceiling, notch-wins nesting) — every term currently IACVT because one token died in c7f7c96d on a false producer-ownership belief. The tragedy is banked at KF-APP-6; the craft is recorded here.

## UNPROVEN-NEEDS-LIVE residue (SS-13 inputs → KF.W9, ranked)

1. **The touch-gate collapse witness (NEW, this seat)**: on a touch device, expand the dock by touch, open the scene Select, tap a menu item — expected: a collapse-then-re-expand flicker as `Ue` fires and the :197-202 watchdog recovers. Decides the producer-relay urgency and the M-5 prose rewrite's wording.
2. **D-1's rendered `top: auto` offset** — rides KF-APP-6's banked KF.W9 item (390×844 first).
3. **D-23/C-5's contrast delta + observer cost over the live aurora** (wiring static-decided; magnitudes not).
4. **D-4's perceived shear** at DPR 1/2, collapsed and across the 16→20px crossfade.
5. **The stagger-loss magnitude** (defeat structural-certain; whether the block-reveal reads flat is a visual call).
6. **The M-2 discriminating observation** (combobox vs bare inline div on /easing — the corrected D-19 test; a Curve-labelled node proves NOTHING).
7. **D-2-residue**: base-arm divergence (label on `--glass-tint-ink`, glyph on `--foreground`) visibility over the aurora, in a non-`contrast-color()` browser.
8. **C-10's coarse-pointer ≥1024px portalled type-size read**; **D-6's label overflow** (longest live string "Matrix Controls"); **D-12's shrink pressure**.
9. **R3-3's transient `false` on the pane-hover mirror** — REFRAMED: with C-6 rescoped, the touch-axis collapse makes the transient plausibly reachable on touch; one paint-trace decides.
10. **RR-2's unproven pair**: same-scene re-pick firing a full View Transition (no same-id guard in `runSceneSwitch`); `#items`-dropdown + Select simultaneity (the mutex covers only the two Selects).
11. **KF-APP-33's AT utterance** (banked routing; listed for the ChromeDock sites :241/:291).

## DISSENT — preserved, not ruled away

1. **RR-2's D-2 MAJOR posture**: "the glyph cannot follow the trigger's own state ink — an icon+label lockup split by mechanism regardless of direction." Preserved; the ruling grades the surviving asymmetry MINOR because the resolved values contradict the filed harm and the closing-on-hover direction is arguably the intended design.
2. **The identity-guard event (fleet evidence, both directions now)**: BOTH re-readers re-confirmed five already-banked rows as fresh finds (KF-APP-6, -13, -24, -26, -33). The kf-ControlsPaneWrapper DISSENT recorded the same failure for "missed" claims; this record extends it to "confirmed" claims. The M-25 agglomeration must run EVERY re-reader row — confirmed or missed — through the bank before booking.
3. **The M-5/C-6 unanimity lesson (method)**: five readers across three passes proved "no collapse path bypasses the hold" by exhaustively enumerating ONE composable's routes — and the wiring diagram one screen below handed `collapse` to a second composable none of them opened. An enumeration is complete over a FILE, never over a CLAIM; the claim's scope was the component graph. (Corollary recorded: the ChromeDock comment was RIGHT that a bypassing path exists and WRONG about which — dead-code verdicts on defensive code demand the same burden as defect verdicts.)
4. **The C-axis arithmetic (corpus integrity, carried from both readers)**: challenge-C-consumption.md's header tally (25 defects / 6 superlatives) still counts eight defects and two superlatives that were never written (its own Round-3 note concedes it), and with S-2 and R3-1 both killed on the merits the axis's arithmetic must be restated from scratch by any downstream consumer — never quoted.
5. **Severity ordering (RR-1, implicit)**: D-18/C-1 first and alone. This record concurs and adds the batch law: the M-5 prose rewrite + :207 `expand()` removal + R3-2 mirror fix + C-14 consolidation are ONE edit (four rows, one watcher cluster); the registry cure (KF-APP-13 fold + M-2 + M-3 + D-15) is ONE edit; the wrapper deletion (D-22/stagger) is ONE line.

## Closing verdict

**ADJUDICATED at HEAD `8281638c` (worktree; anchors re-resolve at `origin/master` before any cure) — RED.** 52 rows stand: 1 BLOCKER (the only scene navigation keyboard-unreachable 2.5 s after pointer idle, booked here) · 13 MAJOR · 22 MINOR · 6 INFO booked, plus 10 folds to banked identities of which three are blocker-weight at their banks (the `--dock-margin` IACVT chain at KF-APP-6 with new deletion-provenance `c7f7c96d`; the phantom producer dependency at census SCH-1/F-1, frontier-cured; the typecheck hole at KF-APP-4 concealing a real TS2322 at :128). Seven corpus claims killed on executed refutations — both BLOCKER-grade kills (R3-1's slot-bag, D-19's inline-arm) third-derived before ratification — and the seat's own producer-bytes find REVERSES the corpus's only unanimous dead-code verdict: the re-expand watchdog every prior reader sentenced defends a real hold-bypassing touch-axis collapse in installed glass-ui 7.0.0, so its cure is a prose rewrite plus a BH relay, never a deletion. The component's chrome is well-made where it is structural (the mutex, the tether, the conservative DFA fallback, the kf-CONSUME slot fit) and wrong where prose stands in for measurement — five stale-comment clusters, a doc-drift ledger three cells deep in one producer d.ts, and two dropdown columns that mix three theming regimes over a remap none of its authors re-read. Routings are M-25 agglomeration input for the forming X·KF specs: KF.W0/W4/W6 own the folds, KF.W9 owns the eleven-item SS-13 residue (touch-gate witness first), everything behavioral is NO-WAVE-OWNER for SS-1/SS-2. Nothing here stamps a wave.

— end of apotheosis. The three challenge axes and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/app/dock/ChromeDock.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **ChromeDock.vue=+4/-4**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**C-20 RESOLVED BY MEASUREMENT — this bank's cell and kf-CubeScene's are BOTH TRUE, of DIFFERENT coordinate pairs, and the wave has now re-anchored.** This record says MbabbMenu *"MOVED at the frontier (−146 lines)"*; kf-CubeScene says it is *"byte-identical between the worktree and origin/master"*. Measured at this seat: ⟨`git diff --numstat 6d280ee7 81a56990 -- demo/app/dock/MbabbMenu.vue`⟩ → **EMPTY** (the pre-settle worktree WAS byte-identical to the frontier) and ⟨`git diff --numstat 8281638c 81a56990 -- …`⟩ → **`13  133`**, LOC **241 → 121**. **The DIRECTION of this cell holds; its MAGNITUDE does not** — the measured motion is **+13/−133, net −120**, and **−146 reproduces at no coordinate this wave can name**. E-3: the dated cell is not rewritten; the measurement is appended.

**The doc-drift ledger's pattern is now at its FOURTH instance** and is delivered to the producer with BOTH repo-qualified letters named (C-14's rider; the FYI packet is `.b`'s single `INBOX.md` append).

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

