claude-fable-5 (served model id, observed in this seat's own system prompt — M-23/L-18 receipt)

# fr-FunctionInput — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/equation/FunctionInput.vue` (261 lines, read whole by this seat) — the `/equation` route's input rail: expression + domain fields, Compute, eight preset pills, Harmonics/Display-terms sliders, the Parseval wand, NotationPills.
**Substrate**: fourier HEAD `cd26c65` (re-pinned this session, `git log -1`; byte-identity confirmed at every anchor this seat checked) · installed `@mkbabb/glass-ui@4.0.0` (80 export keys, version re-read from the installed package.json) · installed Vue **3.5.38** · producer repo `/Users/mkbabb/Programming/glass-ui` at **8.0.0** HEAD with `v3.1.0`/`v7.0.0`/`v8.0.0` all tagged (my `git tag` read — the banked K-17 re-pin governs all tag-anchored claims).
**Corpus adjudicated**: two fresh hostile re-reader returns over the FULL three-axis challenge corpus (`challenge-D-design.md` 55K · `challenge-L-library.md` 29K · `challenge-C-consumption.md` 29K) — **reader-1** (design-and-usage: 20 confirmation cells · 11 kills · 3 misses · 8 superlatives) and **reader-2** (library-and-code: 33 confirmation cells · 9 kills · 3 misses · 8 superlatives). Axis files spot-read verbatim by this seat at every disputed cell (C:118 · L:153/:297/:310 · D:180-184/:432/:729).
**Method (incredulity)**: every verdict-level disagreement between the readers, every kill, every net-new missed row, and every fold into a banked identity was re-derived against the tree with this seat's own instruments before adoption. Both readers were corrected at least once each on their own evidence (corrections register below). One reader-1 kill-replacement cell was **overturned by this seat's own execution** (the `(1e9+0.5)π` cell — ruling 6). Zero browser probes (probe parsimony); product source READ-ONLY; **the sole write is this file**.
**This seat's own instruments (all static + node, read-only)**: FunctionInput.vue WHOLE (261 — every cited anchor checked: `:25-30` six defineModels · `:36-41` applyPreset three-writes+compute · `:44-55` parseDomainValue · `:57-60` onDomainInput · `:62-78` formatDomain · `:80-87` activePreset · `:94`/`:176` BOTH `:default-open="true"` · `:99`/`:117` the only two `id=`s · `:102` sole `@keydown.enter` · `:103-107`/`:122-125`/`:134-137` the triple input recipe · `:143-151` Compute `variant="default"` no-disabled · `:157` sole `preset.description` consumer · `:162` class-only `is-active` · `:183-186` Harmonics read/write bindings + the `update:autoHarmonics false` side effect · `:189-197` nameless Wand2 `:disabled="!effectiveN"` · `:200-208` Auto tooltip copy · `:216-219` Display-terms clamp-on-read/raw-write · `:235-261` scoped block incl. the `!important` trio); **node executions of the live functions verbatim**: `".pi"`→NaN · `"pi/0"`→Infinity · `"-.pi"`→NaN · `"2pi/"`→**2** · `"1..5"`→1 · `"3.14abc"`→3.14 · `"2x"`→2 · `"5degrees"`→5 · `"0x10"`→**0** · `"1 2"`→**12** · `"PI"/"Pi"/"e"/"tau"/"pi*2"/"pi/2.5"`→null · formatDomain(NaN)→`"NaN"` · (Infinity)→`"Infinity"` · (100)→`"100"` · (10/1000/30.5/0) all correct · (π/5)→`"0.6283"` with **Δ=1.853e-5** · **exhaustive round-trip over the declared table: 101 cases, 0 fails** · `formatDomain((1e9+0.5)·π)`→`"3141592655.1606"` (decimal fallback — NOT `"2000000001π/2"`); installed Vue bytes — runtime-core.cjs.js:5788 `if (next !== prev || key === "value")` force-patch + runtime-dom.cjs.js:589 `const oldValue = tag === "OPTION" ? … : el.value` live-DOM diff (the D-3/C-4 decider); EquationView.vue spot-reads (`:26-27` domain defaults incl. `domainEnd ?? Math.PI` · `:41` `effectiveN = ref(cachedRes?.result?.effective_n ?? 20)` unconditional · `:53-55` vizHarmonics · `:57` `loading` = the file's ONLY `loading` hit (my grep) · `:92-93` bare empty-expression return · `:104-111` verbatim pass-through POST · `:139` displayEnergy overwrite · `:151-159` the rescale watcher, no `immediate` · **the initial compute is GUARDED: `if (!result.value) doCompute()`** · `:187-192` always-visible SegmentedTabs sub-lg · `:199-210` prop list, no loading/error · `:229-234` error banner); SliderControl.vue :35-90 (clamp non-finite→lo · onInput per keystroke · getter `[props.modelValue]` raw · one-way `:value` onto native `type=number` with `:min/:max`); NotationPills.vue WHOLE (ONE `<Button>` in a `v-for` over 3 · no aria-pressed · `.notation-pill` min-width 4.5rem + `--pill-color`); CollapsibleSection.vue (withDefaults `defaultOpen: true` · `watch(open)` NOT immediate · 250ms uncancelled timer · `el = rootEl.value?.$el ?? rootEl.value` resolves while mounted · `scrollIntoView({behavior:'smooth'})` ungated · span-not-heading title); Tooltip.vue adapter (TooltipTrigger as-child, no name, no touch path); presets.ts (8 descriptions; domains at :7/:13/:19/:25/:31/:37/:43/:49 — all 0/π/2π/−π); notation.ts:5-17 (the aria-pressed doc-comment + 3 options); python — parsing.py:17-45 SAFE_NAMESPACE WHOLE (**`pi`,`e`,`E`,`I`,`oo` — NO `PI`**), simplification.py:55-98 (`energy_fraction = kept_energy/total_energy` over `truncate_by_budget`; `compute_effective_n(threshold=0.9999, minimum=3)` — all three exits ≥3), api/models/equations.py:14-41 (**`expression: str` bare** · n_harmonics `ge=1,le=200` · budget `ge=2,le=50` both models · notation pattern both models · `# harmonics needed for ~99.5% energy`); installed glass-ui — export map enumerated by node (**80 keys; `./forms` TRUE · `./chip` FALSE · `./toggle-chip` TRUE · `./collapsible`/`./label`/`./number-field` TRUE**), button CVA extracted (**`default` and `glass` are BYTE-IDENTICAL glass-wash strings; `solid` = the relocated `bg-primary` recipe; `outline` carries `aria-pressed:` paint; sm/icon ride `--control-h-*`; `size-(--ui-glyph)` present ×1**), a11y-overrides.css:6-16 universal `*:not([data-allow-motion])` PRM reset (transition-property excludes `translate`), light-dark.css:17-22 the coarse `--ui-scale`/`--control-floor` lift (reader-1's provenance correction CONFIRMED), scale.css:64-72 the `"Fira Code Fallback"` @font-face (local SF Mono/Menlo/Consolas… + ascent/descent/size-adjust overrides, no unicode-range), surfaces.css:187+ `.input-pill` (`height: var(--control-h-md)` + the AX.W51 comment); producer git-shows — **v3.1.0 button `default` = `'bg-primary text-primary-foreground hover:bg-primary/90 active:bg-primary/80 aria-pressed:bg-primary/85'` and `grep -c solid` → 0**; CHANGELOG.md:571 verbatim **"Clean break, no aliases (the Button `default` is now glass; `solid` is the opaque escape)"**; v7.0.0 `grep -c variant` Button.vue → 0, ButtonEmphasis/iconOnly/loading present, `grep -c emphasis` → **0 in CHANGELOG AND 0 in MIGRATION at the tag**, `./chip` at v7.0.0 package.json:322; fourier-side greps — `variant="solid"` → **0**, `variant="default"` → **4 files** (FunctionInput · ExportModal · FourierMorphDemo · FourierShapeExtractor), DESIGN.md:23 verbatim **"`.btn-solid` → `<Button variant=\"default\">` (canonical primary CTA via `--primary` token)"** + :25 the aria-pressed law, aria-pressed → 0/0 in FunctionInput+NotationPills, maxlength → 0 in equation/, fonts.css `03C0|0370-03FF` → 0, style.css:40-50 html **1.125rem BELOW 768px** + :107-111 cartoon-card no-padding + :112-127 the D.W4.d `--viz-amber` darken comment, `grep -rn equation web/e2e/*.spec.ts` → 1 (visual-baseline.spec.ts:34), axe → visualization-ux + visualization-crud only, `preset.description` → 1 hit, EquationPanel.vue **:100** `:min="2" :max="20"`; census cells — CENSUS-2026-08-03.md:100 "NotationPills→`./chip`" unqualified, lane-frontend.md:441 verbatim (the version split AND "currently 6× Button"); registry sweeps — `parseFloat` over adjudicated/*.md → fr-EquationPanel (M-N8) + pt-leaf only, `applyPreset` → 0, `variant="solid"|btn-solid` → 0 (all three missed finds genuinely NEW); identity-guard re-reads at the cited rows: fr-EquationView (B-2 incl. FunctionInput:184/:186 by name · M-BR · M-DV · M-SB · **R2-N2 whole** incl. its runtime-core:5788 cite · D·D-B2 incl. the Wand2 extension by name · D·D-B4=L·M-5=C·D-07 · D·D-M2/RD-5 · D·D-m5+m6 · C·D-22 · D·D-M12 incl. "NotationPills rides the same edit" · K-1 · K-8 · K-13 · K-17 · I-1), fr-ConvergencePlot M-L1+L-M2 (both citing FunctionInput:44-60 for ordering only), fr-CollapsibleSection M-1/M-3/M-4/M-7/m-2/m-8/m-12, fr-EquationPanel M-N8 (+ its r2-a re-derivation), fr-AdminAuditLog AA-2 (66-key correction noted), fr-DarkModeToggle N-2 (DESIGN.md:25), fr-ConvergenceTimeline D·D-5+C·C-5 (literal-geometry class; fourier root 1.125rem member), fr-EqCoefficientsPanel FR-EQC-5 (the non-control hover-lift — the D-19-headline home), fr-CanvasControlsDock M-1 (the destructive-gesture class precedent).

## Scoreboard

**47 adjudicated rows — 0 own-BLOCKER (the two BLOCKER-weight identities live in banked folds: fr-EquationView B-2 · fr-AdminAuditLog AA-2) · 14 MAJOR · 15 MINOR · 5 INFO · 13 folded identities.** Killed-claims register: **14**. **Three net-new MAJORs admitted** (one found by both readers independently — the strongest convergence event in this corpus) **+ two net-new MINORs**. Two banked-row amendments relayed beyond this file (fr-EquationView M-SB's "ONE unbounded field" is false by one; fr-BasisSelector-class census cell "6× Button" corrected to 1 callsite/3 instances — NO-WAVE-OWNER). Each reader was load-bearing-wrong at least once: reader-1 filed a false replacement-evidence cell inside its own kill and inherited the axis's `PI` cell; reader-2 re-affirmed the axis's wrong arithmetic (5.3e-5) and confirmed D-15 without engaging the falsifier its counterpart had already killed. Every such point in this file rests on this seat's own instruments.

## Ruled disagreements (this seat's actual work)

| # | contest | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **L-B2 severity** (NaN/Infinity enter the model) | BLOCKER | MAJOR (fleet demotion law: escapable by retyping) | **MAJOR — reader-2 wins** | My executions: `".pi"`→NaN, `"pi/0"`→Infinity, and formatDomain renders `"NaN"`/`"Infinity"` INTO the field — the anomaly is VISIBLE (unlike N-2's plausible wrong number), retyping escapes it, and the reload self-heal (`null ?? 0` via the cache + EquationView:26-27) is real. The banked demotion altitude (fr-EquationView K-8; fr-CT "a11y-critical, not functionally blocking") governs. |
| 2 | **D-1 severity** (fields imperceivable as controls) | BLOCKER | MAJOR (banked contrast precedent) | **MAJOR — reader-2 wins** | Both readers' contrast recomputes agree to the hundredth (1.02/1.34/2.02 light) and I adopt them as twice-independently-derived; the fields remain fully OPERABLE (focus border moves 3.29:1 vs fill — above the 3:1 graphical bar), so the banked still-operable demotion law applies. The cure is shipped at the installed pin (my `.input-pill` + `./forms` reads) — one repair unit with C-5. |
| 3 | **D-2 — confirmed MAJOR (first half) vs killed as a three-way re-book** | CONFIRMS "first half" MAJOR (dead Compute + dead `loading` computed) | KILLS: every limb banked at its own grade | **KILLED — reader-2 wins; FOLD, no new row** | My own reads land every limb in the bank: empty-expression bare return (EquationView:92-93) = banked **D·D-m5+m6 (MINOR)**; `loading` declared-never-read (my grep: exactly one hit, :57) = banked **D·D-B4=L·M-5=C·D-07 (MAJOR)**; the mobile arm = banked **D·D-M2, RULED MINOR at RD-5** ("one tab away, auto-computed") — and my read confirms the always-visible SegmentedTabs at :187-192 that D-2's framing omitted. Reader-1's one increment survives as a rider on the D·D-B4 fold: v7.0.0 Button ALSO ships `loading`/aria-busy (my tag read), joining the banked Metric rider. |
| 4 | **D-13 — decided vs undecided** (h-3.5/h-4.5 inert at 4.0.0; icon-shrink at 7.0.0) | CONFIRMED MAJOR ("scanner reach is real") | NOT DECIDED — hostage to the banked fr-CollapsibleSection M-7 F.W1 PRE-GATE | **UNPROVEN — reader-2 wins** | The specificity arithmetic is settled ((0,2,1) vs (0,1,0), my CVA extraction confirms `size-(--ui-glyph)` in the dist string), but whether fourier's Tailwind build EMITS the dist-JS arbitrary variant through `@source "../*.js"` is exactly the extraction link banked M-7 declares UNPROVEN — one `npm run build` + grep, which this write-nothing seat cannot run. web/dist is Jun 12/pre-4.0.0 (both readers; banked M-7/K-4). Reader-1's cure (`size-3.5`/`size-4.5`) is right in both worlds and adopted as the row's disposition; D-18's "not load-bearing" cell rides the same hinge. → UNPROVEN #1. |
| 5 | **D-15 — MINOR vs INFO** (π outside the served Fira Code unicode-range) | KILLS falsifier, demotes INFO (the metric-calibrated Fallback face exists) | CONFIRMS MINOR (never engages the Fallback face) | **INFO — reader-1 wins** | My read of installed scale.css:64-72: `@font-face { font-family: "Fira Code Fallback"; src: local("SF Mono")… ascent-override: 99.0161%; descent-override: 32.2052%; size-adjust: 99.9837% }` — no unicode-range, so it covers U+03C0, and the substitution is deliberately metric-calibrated. The core fact survives (fonts.css grep → 0 coverage of π — my run) but the filed harm ("different width, weight, x-height") is defeated by the producer's own calibration. Which local face resolves per-OS → UNPROVEN #4. |
| 6 | **The D·S-1 falsifier kill's replacement evidence** (r = 1e9+0.5) | "that input yields `2000000001π/2`, exactly correct, round-trips exactly" | (silent) | **KILL SUSTAINED; reader-1's evidence cell OVERTURNED by execution** | My run: `formatDomain((1e9+0.5)·π)` → `"3141592655.1606"` — the float error in `val/π` (≈1.7e-7) defeats the 1e-10 epsilon, the π-table DECLINES, and the toFixed fallback prints a decimal that round-trips as a decimal (Δ vs true value 1.1e-5, an L-m7-class truncation). So D's "mis-rounds" claim still dies — nothing mis-rounds, the table abstains — but reader-1's replacement output is equally false at the bytes. Both the `100→1` kill (my run: `"100"`) and the conclusion stand. |
| 7 | **N-2 severity — the parseFloat prefix-commit** (both readers found it independently) | MAJOR | MINOR (no demotion rationale stated) | **MAJOR — reader-1 wins** | My executions: `"2pi/"`→2, `"1..5"`→1, `"0x10"`→0, `"3.14abc"`→3.14 — a PLAUSIBLE wrong number is committed, the model write triggers a re-render, and formatDomain repaints the field with `"2"` — no signal ever exists. Parity ruling: the banked R2-N2 (silent discard + unattributable revert) is MAJOR; silent COMMIT of wrong data that feeds `omega`, the POST, and the LaTeX with nothing anomalous on screen is not less severe. The demotion law's "escapable" arm needs a visible anomaly (ruling 1's `"NaN"`); here there is none. |
| 8 | **The `PI` cell** (L-m9/C-9's "PI is valid in the sibling expression field per parsing.py:37-39") | Carried the axis cell | Nick: SAFE_NAMESPACE has no `PI` | **reader-2 wins** | My whole read of parsing.py:17-45: `"pi"`, `"e"`, `"E"`, `"I"`, `"oo"` — **no `"PI"`**. `PI` is invalid in BOTH fields; `e` is valid in one. The case-sensitivity mechanism survives; the axis exemplar and reader-1's repetition of it die. |
| 9 | **L-m7's arithmetic** | Corrects to 1.853e-5 | Re-affirms the axis's 5.3e-5 | **reader-1 wins** | My run: π/5 − 0.6283 = 1.8531e-5. Reader-2 adopted the axis figure without recomputing — recorded as fleet evidence (the same one-hop-short class the corpus's own killed cells embody). Finding survives MINOR with the corrected number. |
| 10 | **D-14 — own MAJOR vs fold** | CONFIRMS MAJOR | FOLDS to banked fr-CollapsibleSection M-1 | **FOLD — reader-2 wins** | Banked M-1 already counts "four presentationally-styled section headings" and this file's two mounts (:94/:176) are two of the four (my read of the banked row). Identity guard: one identity, never re-booked. |

**Adjudicator's corrections register**: **reader-1** — (a) the `2000000001π/2` cell (ruling 6, overturned by execution); (b) the inherited `PI` cell (ruling 8); (c) "EquationView.vue:174 fires doCompute on first paint" — the live call is `if (!result.value) doCompute()` (my read): with a cached RESULT present nothing auto-fires, so L-B1's cold-open-422 arm requires cached inputs WITHOUT a cached result — narrower than stated (the banked B-2 wording "the NEXT session's first compute 422s" is the correct form); (d) L-m6 mis-mapped into the aria-pressed grouping — the axis file's L-m6 is the `!important` row (my header read of challenge-L :287); (e) D-13 confirmed past the banked M-7 hinge (ruling 4). **reader-2** — (a) the 5.3e-5 re-affirmation (ruling 9); (b) D-15 confirmed without engaging the already-killed falsifier (ruling 5). Reader-2's pin nicks on D-1's token indirections and reader-1's two D-8 provenance corrections (coarse lift lives at light-dark.css:17-22, NOT offsets-sizing.css; sub-768px root is 1.125rem so the absolute px table shifts) were all VERIFIED by this seat and adopted.

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave. Line anchors are HEAD `cd26c65`; re-resolve before any cure (D-19/K-13 law).

### BLOCKER-weight, folded to the bank (never re-booked)

- **L-B1 = C-1 → fr-EquationView B-2 (ADJUDICATED BLOCKER).** Display-terms/budget walks past `le=50` via the `:151-159` rescale recurrence; both pydantic bounds re-read by this seat (`ge=2, le=50` at :23/:40); the banked row already names FunctionInput:184 (`:max="100"`) and :186 (the auto-off flip) as members. Reader-2's nick RATIFIED: L-B1's "without ever touching the offending slider" headline rides the ratio-1 state banked **K-13** killed as unreachable-from-defaults; C-1's no-touch path is the true one. C-1's third-ceiling cite corrected `:99`→**`:100`** (my grep). → **F.W4** (clamp at ref/request sites per B-2+M-BR) + **F.W5-W8** (one shared bound constant).
- **D-12 → fr-AdminAuditLog AA-2 (ADJUDICATED BLOCKER, census-extension).** All 13 Button instances in this subtree definition-absent at 7.0.0 (`grep -c variant` → 0 at the tag, my run; ButtonEmphasis/iconOnly/loading present); the doc gap re-proven by this seat (`emphasis` → 0/0 in CHANGELOG+MIGRATION at v7.0.0). This file's prop sites (FunctionInput:144-145/:159-160×8/:190-191; NotationPills:20-21) are already enumerated inside banked fr-EquationView D·D-B5's fold. Reader-2's BASELINE-LAW note ADOPTED: banked **K-17** re-pins the producer at 8.0.0 (my read: package.json 8.0.0, v8.0.0 tagged) — D's 4→7 hop arithmetic is stale; the membership facts survive. Reader-1's four doc-line mis-citations → killed register #10. → **F.W1**.

### MAJOR (14)

- **D-1 (+ C-5 as its cure surface) — MAJOR (demoted per ruling 2) — F.W3/W4.** The three text inputs are imperceivable as controls: fill-vs-card 1.02:1 light/1.07 dark, border-vs-fill 1.34/1.46, `outline-none` on all three (`:103-107`/`:122-125`/`:134-137`, my read), while the installed producer ships the cure whole — `./forms` exports `Input` (+ `useUserInvalidAria`), `.input-pill` carries `--control-h-md` height, 1.5px `--control-surface-border`, tokened placeholder, focus-visible ring, `[aria-invalid]` destructive ring, forced-colors arm (my dist reads). `grep -rn glass-ui/forms web/src` → 0 (readers, concordant). One repair unit with C-5.
- **C-5 — MAJOR — F.W3/W4 (one edit with D-1).** Three hand-rolled `<input>`s carry the same 6-declaration recipe core; the shipped `Input` types the exact needed surface (modelValue/type/placeholder/pattern/inputmode/required…, my d.ts-adjacent export-map read). Reader-2's nick ADOPTED: "byte-identical ×3" overstates — the lists differ in `rounded-lg` vs `rounded-md`, `text-center`, and the `placeholder:` arm; the RECIPE CORE is what triplicates.
- **D-4.2 + C-6 (aria-pressed) — MAJOR — F.W3/W4, partial folds.** Twelve toggle-shaped controls signal state by class/colour alone with zero `aria-pressed` (my grep: 0/0 across both files) while BOTH consumed variants ship `aria-pressed:` paint (my CVA extraction) and the project's own law demands it twice over — DESIGN.md:25 (my verbatim read) and notation.ts:5-7's doc-comment IN THE FEEDING FILE. Folds: fleet-law identity → banked **fr-DarkModeToggle N-2**; the NotationPills limb → banked **fr-EquationView D·D-M12** ("NotationPills rides the same edit", my read). The INCREMENT booked here: the 8 preset pills (:162) + the wand (:192) — one attribute per site, riding D·D-M12's edit.
- **D-5 (contrast half) — MAJOR — F.W3/W4.** Preset selection is colour-only AND under-contrast for its own register: active label on its 12% tint 3.77:1 light/4.17 dark against a 4.5:1 bar (`--control-text` = the text-sm rung, reader-1's cva-base derivation adopted); 12% tint vs card 1.19:1; 40% border vs card 1.83/1.91 (< 3:1). Live rule :244-248 (my read). Reader-1's refinement adopted: `variant="outline"` paints `bg-background` at rest (my CVA read) — background/card differ 1.02:1 so every verdict is insensitive.
- **D-6 — MAJOR — F.W3/W4.** The placeholder is the component's only syntax documentation at 2.02:1/2.58:1 (twice-independently recomputed to three significant figures — adopted); `Piecewise`/`Heaviside` appear nowhere else user-facing (presets.ts:6/:42); no `aria-describedby`/hint/help (grep → 0).
- **D-7 — MAJOR — F.W3/W4, bound into N-1's repair unit.** Compute's hover tint is its only branded state and misses AA in both arms (4.00/4.44, reader-1's recompute adopted); rest state is the neutral glass wash (my CVA read: `default` ≡ `glass` byte-identical); no `:focus-visible` twin in the scoped block (:231-261, my read). N-1 is the root cause; D-7 is the symptom and dies with N-1's cure.
- **D-8 — MAJOR — F.W3/W4.** The form band opts out of the `--ui-scale` comfort axis the three Buttons ride: inputs literal-pinned (`py-2`/`py-1` at :103/:122/:134) vs `--control-h-sm/md` = `max(scaled, floor)` (my token reads); `grep -rn 'ui-scale|control-floor|touch-target' web/src` → 0 (readers, concordant). BOTH corrections adopted, mutually consistent at the two root sizes: coarse lift lives at **light-dark.css:17-22** (my read; NOT offsets-sizing.css as D cited); at 16px root the wand is **60px** coarse (max(3.75rem, 2.75rem)) per the banked fr-EquationView D·D-B1 re-derivation, and sub-768px fourier's root is **1.125rem** (my style.css:40-50 read) so Compute ≈60.75px vs domain fields ≈31.5px. The `.auto-calc-tip` literal (`font-size: 12px; max-width: 220px`, :256-260) is a member of the banked **fr-ConvergenceTimeline D·D-5+C·C-5** literal-geometry class — folded as member, not re-booked. Absolute px table → UNPROVEN #5.
- **D-9 = L-M2 — MAJOR — F.W3/W4.** Degenerate range: at nHarmonics=1 (reachable, `:min="1"` at :184) the Display-terms slider gets `:max = Math.max(2,1) = 2 = :min` (zero travel) and `:model-value = Math.min(budget,1) = 1 < min`; the chassis forwards raw (getter `[props.modelValue]`, my SliderControl read) so the inline native `type=number` renders 1 against min=2/max=2 — HTML-constraint-invalid on its face, with no invalid styling in the chassis. The display-only-clamp leg (C-3) folds → banked **fr-EquationView M-BR**. Rendered outcome at min===max → UNPROVEN #3.
- **D-11.1 — MAJOR — F.W3/W4, one repair unit with N-3.** The eight preset descriptions — the pedagogic payload of an educational tool — surface ONLY through hover tooltips: `preset.description` has exactly one consumer (:157, my grep), the local adapter adds no touch path (my whole read), reka's trigger is pointer/focus-driven. Two Tooltip callsites mounting nine instances (my read). The second limb (disabled-arm unreachability) → killed register #2.
- **L-B2 — MAJOR (demoted per ruling 1) — F.W3/W4 (one edit with N-2).** `parseDomainValue` guards finiteness on one of two return paths: `".pi"`→NaN (regex group `"."`, unguarded return at :51), `"pi/0"`→Infinity (denom 0) — MY EXECUTIONS; formatDomain has no non-finite arm so the field renders `"NaN"`/`"Infinity"` (my runs); `JSON.stringify` emits null; pydantic floats reject explicit null; reload self-heals. NOT banked (registry sweep: the bank's instrument lines record only "no ordering check"). The single-finiteness-exit cure closes this and N-2 together.
- **L-M3 — MAJOR — F.W4 (copy) + F.W5-W8 (seam).** The Auto tooltip prints a budget-truncated energy fraction directly beneath its own ≥99.99%-at-N_eff assertion: `energy_fraction = kept_energy/total_energy` over `truncate_by_budget(terms, budget)` (my simplification.py:55-65 read) lands via SimplifyResponse → `displayEnergy` (:139) → `:energy-captured` (:206) → `:207` — the top-budget terms' energy, not the energy at N_eff; defaults (budget 10, effectiveN 20-40) make the mismatch the common case. The best library-axis find in the corpus (both readers concur; I concur).
- **N-1 (reader-1 missed #1, every leg re-proven by this seat) — MAJOR — F.W1 (uplift semantics) + F.W3/W4 (this file's CTA) + NO-WAVE-OWNER rider (DESIGN.md correction).** The route's primary CTA was silently demoted by the in-flight 3.1→4.0 bump: `variant="default"` meant `bg-primary` at v3.1.0 (my git-show: the exact recipe, `solid` count 0) and means the neutral glass wash at the installed pin (my CVA extraction: **`default` ≡ `glass`, byte-identical strings; `solid` = the relocated primary recipe**). The producer documented the break (CHANGELOG.md:571 verbatim, my read); the consumer never absorbed it: `variant="solid"` → **0 adopters** (my grep), 4 explicit `variant="default"` + every bare `<Button>` inheriting it, and the house law is now false — DESIGN.md:23 still names `variant="default"` the "canonical primary CTA via `--primary` token" (my verbatim read). THIS IS D-7's ROOT CAUSE: the hand-rolled sub-AA hover tint (:235-239) compensates for a demotion nobody booked. Forward: `solid` is itself retired upstream, so the 7.0.0 landing is `emphasis="primary"` — the F.W1 cure target. A value-axis redefinition under an unchanged name — invisible to vue-tsc and to the census's removed-subpath break model. Registry-swept clean (my greps → 0). Painted delta → UNPROVEN #9.
- **N-2 (BOTH readers independently; severity per ruling 7) — MAJOR — F.W3/W4 (one edit with L-B2).** `parseDomainValue` does not merely reject malformed input — it SILENTLY COMMITS A WRONG NUMBER via parseFloat prefix-parsing: `"2pi/"` (the natural mid-typing state of `2pi/3`) → **2**, committed at :59; the model write re-renders; formatDomain(2) repaints `"2"` over the user's text. Also `"1..5"`→1, `"3.14abc"`→3.14, `"0x10"`→**0** (a degenerate-domain seed straight into banked fr-CP M-L1). ALL MY EXECUTIONS. The opposite arm from banked R2-N2 (reject/revert) and from L-B2 (non-finite): here plausible wrong data enters `omega`, the POST, and the LaTeX with zero signal. The axes had it backwards (D:182 claims `"2pi/"`/`"1..5"` return null — two of four don't, my run against the verbatim axis text). Cure: full-match numeric anchor before parseFloat — the same edit gives L-B2 its single finiteness exit. Genuinely new (registry sweep: parseFloat rows are fr-EquationPanel M-N8's SliderControl only).
- **N-3 (reader-1 missed #3, chain re-verified) — MAJOR — F.W3/W4 (one repair unit with D-11.1).** On touch, the only gesture that discloses what a preset IS is the gesture that destroys the user's work: `applyPreset` (:36-41, my read) writes expression + BOTH domain endpoints and fires a network compute — no confirm, no undo, no dirtiness gate — while the descriptions are hover-only (D-11.1). The script holds no history (whole read: six defineModels, two emits, nothing else) and the sessionStorage layer overwrites within a tick (input-state watcher, my EquationView read). The file already computes `activePreset` (:80-87) — the dirtiness gate is one comparison away. Class precedent (cited, NOT folded — different component/mechanism): banked fr-CanvasControlsDock M-1. Felt severity on a real coarse device → UNPROVEN #10.

### MINOR (15)

- **L-M4 — MINOR — F.W4.** Three in-template spellings of "effective N" (`:183` honours autoHarmonics; `:216`/`:217` ignore it) agree only via EquationView:53-55's invariant, invisible to this file; the sole doc-comment (:16) documents the opposite half; the four props are optional yet always passed (EquationView:205-208, my read).
- **L-M5 — MINOR — F.W3/W4.** ~36 lines of pure domain-notation library trapped in the SFC, zero runner tree-wide, named home exists (lib/equation/ with two precedents); now proven fallible four ways (L-B2, N-2, L-m7, L-m9) — extraction + the missing unit tests are one motion.
- **L-m5 — MINOR — F.W3 + NO-WAVE-OWNER (census cell).** `./chip` ABSENT at the installed 4.0.0; `./toggle-chip` present (my node enumeration of the 80-key map); CENSUS:100 routes NotationPills to `./chip` unqualified (my read) while lane-frontend.md:441 carries the correct version split (my read) — the census dropped the qualifier. `.preset-pill` is an unbudgeted copy of the active-pill recipe; reader-2's nick adopted (shared shape is the active-tint triple, not byte-equivalence).
- **L-m6 = D-17 = C-12 — MINOR — F.W4.** Three unnecessary `!important` (:251-253) that also suppress the glass variant's own hover/active feedback exactly while auto is engaged (per-longhand outranking, my CVA read of the plain-utility hover arms). C-12's compiled receipt STRUCK (killed #14 — stale dist); the conclusion survives on cascade semantics alone (unlayered SFC scoped styles vs `@layer utilities`), which is how the two sibling rules in the same block win without `!important`.
- **L-m7 = C-15 — MINOR (number corrected per ruling 9) — F.W4.** Off-table round-trips are lossy: π/5 → `"0.6283"` → Δ=**1.853e-5** into omega and the server grid (my run). The 1e-10/1e-6 epsilon asymmetry is deliberate and stays.
- **L-m8 = D-20 — MINOR — F.W4.** Two `id=`s for three inputs (my grep: :99, :117); the Domain-end field can never be label-associated; `aria-label` overrides the visible `<label for>` on the start field; the "to" separator is a bare span; no fieldset/group.
- **L-m9 = C-9 — MINOR (exemplar corrected per ruling 8) — F.W4.** `"1 2"`→12 (whitespace-strip concatenation, my run); case-sensitive rejections reproduce (`Pi`/`PI`→null); the docstring grammar is narrower than advertised. `PI` is invalid in BOTH fields (SAFE_NAMESPACE, my whole read).
- **L-m10 = D-22 — MINOR — F.W4.** Three commit contracts on one form: Enter computes from one field of three (:102 sole hit); presets compute implicitly; domain edits commit on `change` and never compute — the plot-side half is banked **fr-ConvergencePlot L-M2** (folded; "which may be never" verbatim there).
- **C-7 — MINOR — F.W4.** `autoHarmonics` is the seventh two-way value and the only hand-rolled one (optional prop + emit + `!props.autoHarmonics`) beside six defineModels; the parent round-trips it long-hand.
- **C-8 — MINOR — F.W5-W8.** Harmonics `:max="100"` forfeits half the operation's range (`le=200`, my pydantic read) with no documented rationale; rides B-2's shared-bound-constant cure.
- **C-10 (energy limb) — MINOR — F.W4.** `v-if="energyCaptured"` (:207) hides a genuine `0`; no `ge=` on the field. The effectiveN limb is banked **fr-EquationView C·D-22** (folded — see killed #2).
- **D-16 — MINOR — F.W4.** 8px block padding framing 12px internal gaps; the cartoon shim contributes no padding (my style.css:107-111 read); three intervals doing one job.
- **D-18 — MINOR — F.W4 (rides the D-13 hinge for load-bearing-ness).** Hard `9999px` restates `rounded-pill`/`--radius-pill` in both pill classes — a token bypass that survives a retune; whether the CVA's own radius currently reaches the DOM is UNPROVEN #1's question.
- **N-4 (reader-2 missed #2, re-verified) — MINOR — F.W5-W8 + AMENDMENT RELAY.** `ComputeEquationRequest.expression: str` is bare (my read) — no max_length/pattern — above six fully-bounded scalars, and this component is its sole editor with no `maxlength` (my grep → 0); flows through the seven-keyword denylist into `parse_expr` + symbolic integration. **Corrects banked fr-EquationView M-SB** ("the ONE unbounded field in the equation contract" — there are two, and the free-text one is the harder half of the R6-8 asymmetry). Relay recorded here; the banked file is never rewritten. Damage bounded by the banked 30s-semaphore/429 row.
- **N-5 (reader-2 missed #3, re-verified) — MINOR — F.W4.** The Harmonics slider's read axis and write axis are different quantities: `:183` displays the DERIVED count while `:186` writes the AUTHORED one — with auto on and effectiveN below authored N, one track click writes the derived value over the user's `nHarmonics` and flips auto off, with no undo and a colour-only signal (the wand tint — itself the aria-pressed row). Structurally banked M-BR's shape on the sibling slider, but M-BR's cure does not reach this instance; the corpus came within one line twice (L-M4, D-10) and missed the write.

### INFO (5)

- **D-15 — INFO (demoted per ruling 5) — F.W4 note.** π falls outside every served unicode-range (my grep) but lands in the producer's metric-calibrated local fallback face. Per-OS face resolution → UNPROVEN #4.
- **C-14 — INFO — F.W4 note.** Six dead defineModel defaults; the two default sets already disagree (`domainEnd` 2π here vs π at EquationView:27 — my read of both).
- **C-13 = D-24 — INFO — F.W9/W10 (fold: banked fr-CollapsibleSection m-8 class).** `/equation` has screenshot coverage and nothing else (my greps: 1 hit; axe wired on two non-equation specs); the nameless wand's survival is the corollary. The unused `actions` slot sits ready (my CollapsibleSection read).
- **C §0 row 7 census correction — INFO — NO-WAVE-OWNER (SS-3/SS-4 spec authoring consumes).** lane-frontend.md:441's "currently 6× Button" is false: ONE `<Button>` callsite in a `v-for` over 3 options (my whole read) — the F.W3 budget is 1 line, not 6.
- **L-i1 — INFO — NO-WAVE-OWNER.** The R5-7 native-template-loop class does not apply here (both v-fors are component callsites; native loops → 0) — a positive control, recorded with fr-CollapsibleSection m-14's inversion rider.

### Folded identities (by reference, never re-booked)

L-B1/C-1 → **fr-EquationView B-2** (BLOCKER) · D-12 → **fr-AdminAuditLog AA-2** (BLOCKER; K-17 rider) · D-4.1/C-2(a) the nameless wand → **fr-EquationView D·D-B2** (MAJOR; the banked row adopted this exact extension by name — my read) · C-3 → **fr-EquationView M-BR** · L-B3 → **fr-ConvergencePlot M-L1** (client) + **fr-EquationView M-DV** (server) — L-B3's increments (useCurveTransition.ts:70's second divisor; the rAF visibility arm) are banked at fr-CP D-9+C-2/C-19 · D-3/L-M1/C-4's surviving residue (no error affordance/aria-invalid/blur-resync + stale-model compute) → **fr-EquationView R2-N2** (MAJOR, banked WITH the corrected force-patch mechanism — my byte-read matches its cite) · D-14 → **fr-CollapsibleSection M-1** (two of its four mounts) · D-10 → **fr-EquationPanel M-N8** (the chassis's clamp-per-keystroke/NaN→lo) **with this file's increment riding it**: the `:186` `emit('update:autoHarmonics', false)` makes a cleared inline field silently cancel Parseval auto-mode AND snap N to 1 · D-2 (all three limbs) → **fr-EquationView D·D-m5+m6 (MINOR) + D·D-M2/RD-5 (MINOR) + D·D-B4 (MAJOR)** with the v7-Button-loading rider · L-m1 dead guards → **fr-EquationView C·D-22** · C-11 → **fr-CollapsibleSection m-12** · D-19's headline (hover-lift affordance lie on a non-control container, two mounts :93/:175) → **fr-EqCoefficientsPanel FR-EQC-5** (MAJOR class row; F.W1 self-cures — 8.0.0 `cartoon-surface` has no hover) · D-8's `.auto-calc-tip` extension → **fr-ConvergenceTimeline D·D-5+C·C-5** member · L-m10's plot half → **fr-ConvergencePlot L-M2**.

## Killed-claims register (14)

1. **D-3 / L-M1 / C-4 — the Vue-patch mechanism ("rejected text stays permanently; patchDOMProp diffs vnode-vs-vnode").** FACTUALLY INVERTED at the installed runtime, MY BYTES: runtime-core.cjs.js:5788 `if (next !== prev || key === "value")` force-patches `value` on every re-render; runtime-dom.cjs.js:589 diffs against **el.value — the live DOM** (C:118's sentence is its exact opposite, spot-read verbatim). The rejected text survives only until any of the component's reactive inputs ticks; the corrected shape + the true harm are ALREADY BANKED as fr-EquationView R2-N2. "Permanently"/"indefinitely"/"no signal" die; the residue folds (see roster). L-M1's identical-string sub-claim reaches a true conclusion by a different route (no model change → no re-render).
2. **D-11.2 / C-2(b) — "the auto tooltip is unreachable exactly when needed."** The premise state cannot occur: `effectiveN = ref(cachedRes?.result?.effective_n ?? 20)` — the `?? 20` is UNCONDITIONAL (my read; D's falsifier (d) at D:432 misreads it as cache-dependent), and `compute_effective_n`'s three exits all pass `max(minimum=3, …)` (my whole read). Banked from the other side as C·D-22. The preset limb of D-11 is unaffected (confirmed MAJOR).
3. **D-23 — ":176 omits `:default-open` — sibling call sites disagree."** FALSE at the tree: both :94 AND :176 state it (my whole-file read). The redundancy against `withDefaults({defaultOpen: true})` is real; the drift is not. L-m12's "dead at BOTH call sites" is the correct form.
4. **L-m12 — both mechanism halves.** (a) Mount arms no timer: `watch(open, …)` carries no `immediate` (my read) — the timer exists only after a user close/reopen. (b) "Damage is nil" is contradicted by the bank: while mounted, `rootEl.value?.$el ?? rootEl.value` resolves, and the callback runs the class-attribute probe + ungated smooth `scrollIntoView` — banked fr-CollapsibleSection **M-3 (MAJOR)** + M-4; the timer itself is banked m-2. The redundant-prop half survives inside kill #3's residue.
5. **D-19 falsifier (a) — "none of the producer's PRM blocks is a universal reset."** FALSE: a11y-overrides.css:6-16 IS universal (`*:not([data-allow-motion])`, my verbatim read) and its forced transition-property list excludes `translate`, so the cartoon hover lift SNAPS under PRM. Banked fr-EquationView K-1, fourth-plus application. D-19's headline survives and FOLDS to FR-EQC-5 (roster).
6. **D·S-1's falsifier — "`/\.?0+$/` mangles 100 → 1" and "r = 1e9+0.5 mis-rounds."** Both dead by MY EXECUTION: formatDomain(100) → `"100"` (leftmost-match anchoring); the (1e9+0.5)π input produces the decimal fallback `"3141592655.1606"` — the table abstains (float error > 1e-10), nothing mis-rounds. Reader-1's replacement cell (`"2000000001π/2"`) equally false — ruling 6. The superlative it attacked stands (superlative 1).
7. **D-21 bullet 1 — "the placeholder's double-spaces collapse as HTML attribute whitespace."** Mechanism wrong twice (both readers, concordant; adopted): attribute values are not whitespace-collapsed at parse, and UA stylesheets give `input` `white-space: pre`. Visual survival → UNPROVEN #6. D-21's other bullets stand inside the roster's copy rows ((a+b) at :215 vs expanded/sigma — reader-1's grep adopted).
8. **D-2 as a composite BLOCKER.** Ruled — all three limbs banked at MINOR/MINOR/MAJOR (ruling 3); the composite grade was unsupported by any part. Nothing lost: the rider is recorded on the D·D-B4 fold.
9. **D-15 falsifier (a) — "no fallback face exists."** The producer declares `"Fira Code Fallback"` with local() monos and metric calibration at scale.css:64-72 (my read), on fourier's import path. D-15 demoted INFO (ruling 5).
10. **D-12's doc provenance cluster** (CHANGELOG:63-64 / MIGRATION:167/:445 / :1183-1204). Four mis-citations at tag AND HEAD (reader-1's git-show sweep, adopted; substance — the 0/0 `emphasis` grep — re-proven by this seat at the tag). The F.W1 relay must cite lines that exist.
11. **L-B1's "reachable without ever touching the offending slider" headline.** Contradicted by its own worked example; the ratio-1 variant is banked-killed (K-13). C-1's no-touch path carries the row (reader-2's nick, ratified).
12. **The axes' `PI` cell** ("parsing.py:37-39 legitimises PI/E"). SAFE_NAMESPACE has no `PI` (my whole read; L:153 spot-read verbatim). The case-sensitivity mechanism survives; the exemplar dies, and reader-1's repetition of it with it.
13. **L-m7's 5.3e-5** (and reader-2's re-affirmation). True Δ = 1.853e-5 (my run) — ~2.9× overstatement. Finding survives corrected.
14. **C-12's compiled receipt** (`web/dist/assets/EquationView-*.css` as "proof"). The tree is Jun 12 / pre-4.0.0 — the exact staleness banked at fr-CollapsibleSection M-7/K-4; inadmissible. The conclusion survives on cascade semantics (roster row L-m6); D-13's same-corpus refusal to cite it is the model (superlative 8).

## Superlatives (adjudicated, provenance preserved)

1. **RATIFIED BY EXHAUSTIVE EXECUTION (L·S-4 / both readers)** — formatDomain/parseDomainValue are an exact inverse pair over the entire declared table: my node run over all 101 table members → **0 round-trip failures**; all eight shipped preset domains sit inside the table (my presets.ts read), so every shipped path is lossless. QUALIFIED: the same pair carries L-B2 and N-2 — the praise is the π table; the defects live in its fallback arms.
2. **RATIFIED (L·S-2 / C-15)** — `activePreset` is a derived, tolerance-aware reflection (:80-87, my read): no stored selection state exists to go stale; the highlight self-clears on any edit. The pattern most preset UIs get wrong, done right — and it is N-3's cheapest cure hook.
3. **RATIFIED (L·S-1/S-2, my re-greps)** — zero ambient registrations and zero type escapes in 261 lines (no hooks/timers/listeners/observers/watchers; the sole cast is the irreducible event-target narrowing at :58) — in a tree censused at 20 rAF sites/8 files with an ungated clock in this component's own sibling.
4. **RATIFIED AT BOTH ENDS (L·S-3)** — the component's physics copy is right where the backend's own comment is stale: `:202-203`'s "≥99.99% of total energy" matches `compute_effective_n(threshold=0.9999)` exactly (my read) while api/models/equations.py:34's `# ~99.5%` comment is the stale authority. Disciplined attribution — the defect files against the backend.
5. **RATIFIED (D·S-4, numbers adopted from reader-1's recompute)** — the fourier-local `--viz-amber` darken (style.css:112-127, the signed D.W4.d comment — my read) is load-bearing IN THIS FILE: the wand's active glyph clears the 3:1 graphical bar only because of it.
6. **RATIFIED (C·S-1/S-2, reader re-greps adopted)** — the component contributes NOTHING to the F.W2 value.js migration surface: zero value.js imports, zero hex/VIZ_COLORS, every colour a token composed with `color-mix()` (my scoped-block read) — already at the F.W2 target posture.
7. **RATIFIED (C·S-3)** — `notation` is the one field whose client type, option table, and server contract agree exactly (my reads at all three: types.ts union · notation.ts table · pydantic pattern on BOTH models). Against budget's three disagreeing ceilings and n_harmonics' halved range, the R6-8 lesson stated exactly: this codebase keeps an operation leaf honest where the contract is a TYPE and loses it where the contract is a NUMBER.
8. **CORPUS-QUALITY (both readers, ratified)** — D's contrast arithmetic reproduced to the hundredth by two independent recomputes; D-13's stale-dist refusal is the model of an honest undecidable row (its same-corpus counterexample is killed #14); L-m5's export-map census correction is the corpus's most operationally valuable cell (re-proven here by my own enumeration); C's §0 fold table corrected a census downward against its own tally interest. The standing fleet lesson re-instanced twice in this corpus: a superlative's (and a kill's) falsifier must be RUN, not admired — rulings 6 and 9 are this file's two instances.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs · one build-gate)

1. **D-13 both arms + D-18's load-bearing question — NOT a browser item**: the banked fr-CollapsibleSection **M-7 F.W1 PRE-GATE** (one `npm run build` + grep for `size-\(--ui-glyph\)` / `\.h-3\\.5` / `rounded-pill`) decides whether the glyph classes are inert today and whether they shrink at 7.0.0. This seat's law is write-nothing; the gate is one command at F.W1 open.
2. **L-B1/C-1's 422 surface text** — rides the banked fr-EquationView C·D-02 chain (array-detail discard; empty statusText over HTTP/2). Probe the deployed API, not the dev proxy.
3. **D-9/L-M2's rendered outcome at min===max with modelValue<min** — reka clamp-and-correct loop vs degenerate thumb paint; the native number field's `:invalid` presentation with no invalid styling in the chassis.
4. **D-15 residue** — which local() face resolves for U+03C0 per OS, both themes; the calibration makes this narrower than filed.
5. **D-8 absolute control heights** — measure the four controls at fine/coarse × 16px/18px roots; the DIVERGENCE is token-decided and closed, only the absolutes are open.
6. **D-21 residue** — do the placeholder's double-spaces visibly survive (UA `white-space: pre` says yes)? One screenshot.
7. **D-2 mobile arm (felt half)** — the SegmentedTabs carries no badge/state cue when Compute fires from the Controls tab; the absence is static, the felt discoverability is not. Rides the banked D·D-M2 default-tab ruling.
8. **L-B3 backend arms** — sympy at period 0 (`zoo` vs raise); the tier-3 spline over a degenerate linspace; the inverted-domain NET visual (negative omega × backwards x-grid). Frontend chain needs no probe (banked M-L1).
9. **N-1 painted witness** — Compute's rest state at the installed pin vs the v3.1.0 `bg-primary` frame; the before/after for the F.W1 `emphasis="primary"` landing.
10. **N-3 felt severity** — the tap-to-learn-destroys-work flow on a real coarse device, with the D-2-banked tab split in play.

## Closing verdict

**ADJUDICATED**: 47 rows — 0 own-BLOCKER (both BLOCKER-weight identities were already banked, and this corpus's readers correctly folded rather than re-booked them) · 14 MAJOR · 15 MINOR · 5 INFO · 13 folds; killed register 14; **three net-new MAJORs** (N-1 the silently-demoted primary CTA — the corpus's deepest find, a value-axis redefinition under an unchanged name that three axes and one banked census model all missed; N-2 the parseFloat prefix-commit — found independently by BOTH readers, the corpus's strongest convergence; N-3 the touch-side learn-gesture that destroys work) and **two net-new MINORs** (N-4 correcting banked M-SB by relay; N-5 the read/write axis mismatch). The component's portrait: a 261-line panel with genuinely excellent bones — an exhaustively-verified π-notation inverse pair, derived selection state, zero leak surface, one type-honest contract leaf — wearing its defect mass at exactly two seams: the hand-rolled input band that opted out of the design system's perceivability, scale, and naming machinery, and the numeric fallback arms where three different silent-wrong-data paths (reject-and-revert R2-N2, non-finite L-B2, prefix-commit N-2) converge on one two-function library that one extraction-plus-guard edit cures whole. Ten of the corpus's rows were already owned by the bank — the identity guard held: nothing was re-booked, two banked rows gained members, two gained corrections by relay. Routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this file per E-1/E-3.
