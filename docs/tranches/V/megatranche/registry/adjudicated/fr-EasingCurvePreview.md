claude-fable-5 (served model id)

# fr-EasingCurvePreview — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Subject**: `fourier-analysis/web/src/components/visualization/EasingCurvePreview.vue` (41 lines, read whole) + its dependency chain (`stores/animation.ts` barrel · `lib/easings.ts` 127 · sole consumer `EasingPicker.vue` 98, read whole) and the in-tree comparanda (`MorphPhaseConfig.vue`, `ui/PathPreview.vue`).
**Substrate**: fourier `m/w1-bump-migration @ cd26c65` + the frozen M.W1a dirty WT (re-pinned by this seat: 28 porcelain rows) · value.js `tranche-u @ 1994eb53` · installed glass-ui **4.0.0** / value.js 0.13.0 / reka-ui 2.9.x / tailwindcss **4.3.1** · producer glass-ui at **8.0.0** (`v8.0.0`, HEAD `e73ec80c`, CHANGELOG "## 8.0.0 — 2026-08-09" — published the day of this adjudication; read at `/Users/mkbabb/Programming/glass-ui`).
**Corpus**: three axis challenges (`audit/fourier-components/EasingCurvePreview/challenge-{D-design 332, L-library 232, C-consumption 273}.md`, all read whole by this seat) × two hostile re-reader returns (**reader-DU** design-and-usage · **reader-LC** library-and-code), received in-brief.
**Method (M-23/L-18)**: the readers' one verdict-level structural collision (reader-DU confirms the 28×19.6 attribute geometry with full re-run; reader-LC kills the entire premise as **K-GEOM**) was re-derived by this seat with its own instruments — and **closed beyond what either reader achieved**: the one link reader-LC left UNPROVEN-NEEDS-LIVE is now static (see R-1). Two kills were minted by this seat against reader-DU (K-10, K-11). Dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; product source untouched; scratchpad-only scripts)**: full reads of the subject, EasingPicker.vue (98 — reader-DU's EOF-check confirmed), lib/easings.ts (whole; duplicate `@mkbabb/value.js` import specifiers at :9/:10-16; `description` required at :76, authored :78-83), animation.ts:1-30, MorphPhaseConfig.vue:44-56/:70-90/:195-205, PathPreview.vue:1-20/:48-68, AnimationControls.vue:5-12/:115-125, style.css:1-15/:110-122, api/models/shared.py (grep: the one `easing: str = "sine"` line), defaults.ts:18-25, types.ts:44-51, loader path + :56, vite.config.ts:35/:45-58, e2e/visualization-ux.spec.ts:27-34/:185-200, producer EasingCurve.vue (header + :85-190), easing/index.ts, constants.ts:1-50, producer package.json exports (70 keys; `./dropdown-menu` ABSENT, `./menu`/`./easing` PRESENT; `./metric-badge`/`./hover-card`/`./hover-popover` still absent). **K-GEOM instruments**: installed `dist/button-BNDWhAZb.js` read (Primitive `as:"button"`, slot rendered INSIDE the class-carrying root; CVA base carries `[&_svg:not([class*=size-])]:size-(--ui-glyph) [&_svg]:shrink-0` byte-exact; zero collection registration); shipped `dist/styles/index.css:190-222` `@source "../*.js"` block read whole; `components.css` (48 lines) grepped — the svg rule is NOT precompiled; **`@tailwindcss/oxide` Scanner run over `glass-ui/dist/*.js` → the candidate IS extracted (among 5 239)**; **the candidate compiled under fourier's own installed tailwindcss 4.3.1 → `& svg:not([class*=size-]) { width: var(--ui-glyph); height: var(--ui-glyph) }`** (and `[&_svg]:shrink-0` → `flex-shrink: 0`); `src/style.css:3` `@import "@mkbabb/glass-ui/styles"` through `@tailwindcss/postcss` (vite.config.ts:35); `--ui-glyph: calc(1rem * var(--ui-scale))` at offsets-sizing.css:177, `--ui-scale: 1` at :136, coarse-pointer 1.5 at light-dark.css:17-19. **Numeric runs (installed value.js 0.13)**: all six catalog ranges = [0.000000, 1.000000] at 2001 samples; sine↔quad peak Δ 0.0280 / linear↔expo 0.2882; back extrema −0.0927/1.0927 · −0.0969 · 1.0869 (the producer's VIEW_PAD annotation reproduced); WCAG runs — accent 3.46:1 vs page, 3.13:1 vs its own 8% bed, `--muted-foreground` 5.21:1, `--viz-amber` original 3.56:1. Greps: `248 88% 71%` = exactly 2 · `errorCaptured|errorHandler` = 0 · `EasingCurvePreview` = 2 (both EasingPicker) · defineProps census **0 destructured / 27 `const props =` / 9 `withDefaults`** · `EASING_OPTIONS` = 3 lines, loop reads `.label` only · `generateCurveSVGPath` = declaration + 1 internal caller · the third barrel `useMorphConfig.ts:20` + fourth `useFourierMorph.ts:29` · reka `MenuContentImpl.js` `data-reka-collection-item` ×2 + `:205` Tab preventDefault (corroboration of the banked identity) · `--viz-chebyshev` at installed light-dark.css:146.

## Scoreboard

**29 rows ADJUDICATED-CONFIRMED (9 MAJOR · 11 MINOR · 9 INFO, counting 11 reader-missed rows banked) · 13 killed-register entries (2 minted by this seat against reader-DU; 1 reader kill upgraded from conditional to unconditional by this seat's instruments) · 1 reader miss FOLDED to the banked fr-AnimationControls keyboard identity without re-booking · 0 BLOCKERs booked.**

The corpus's central event: **all three axes and one hostile reader computed 27+ pages of geometry on SVG width/height attributes that no browser in this tree ever uses** — the glass Button's CVA base sizes every descendant `<svg>` to a square `--ui-glyph` box, and this seat closed the delivery chain statically. Every px figure in the D/L/C challenges is void; the defect *directions* largely survive at corrected magnitudes.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **K-GEOM** — is the element 28×19.6 px (attributes) or a `--ui-glyph` square (CSS override)? | CONFIRMS the attribute geometry across D-1/D-2px/D-8/D-12/D-13, "re-ran the arithmetic and the sampler" | KILLS the premise; final link (consumer build emits the utility) honestly marked UNPROVEN-NEEDS-LIVE | **LC wins — and the seat CLOSES the chain statically.** K-GEOM is unconditional. | Four links, all this seat's own runs: (1) the CVA base string byte-exact in installed `button-BNDWhAZb.js`, slot rendered inside the class-carrying `Primitive as="button"`; (2) the rule is NOT precompiled in shipped CSS (components.css grep 0) — delivery is the JIT scan, and **the real `@tailwindcss/oxide` Scanner over `glass-ui/dist/*.js` (the exact dir the shipped `@source "../*.js"` targets) extracts `[&_svg:not([class*=size-])]:size-(--ui-glyph)`**; (3) fourier's installed tailwindcss 4.3.1 compiles it to `& svg:not([class*=size-]) { width/height: var(--ui-glyph) }` at specificity (0,2,1) — presentation attributes lose to ANY author CSS per SVG2; (4) `src/style.css:3` imports the cascade through `@tailwindcss/postcss` (vite.config.ts:35), so the `@source` is honored. `class="easing-preview"` contains no `size-`, so the `:not()` never excepts it. **Live geometry: a SQUARE `--ui-glyph` box — 16 px desktop (1rem × scale 1), ~27 px coarse-pointer (× 1.5 on the 1.125rem mobile root the fr-AnimationControls seat measured) — scale = min(16/1.1, 16/1.6) = 10 px/unit, ink 10×10 px, stroke 1.50 px, 2.5 px dead gutter per side.** SS-13 keeps one `getBoundingClientRect` as corroboration only. |
| R-2 | **D-1/L-2/C-D-3 survival under K-GEOM** | MAJOR at 43.7%-ink | letterbox *direction* survives; magnitude/cause/repair-arithmetic die | **MAJOR — the composite survives RESCOPED** | The dead ±0.3 headroom is unconditionally proven (this seat's range run: six ranges exactly [0,1]); the aspect mismatch letterboxes in ANY box (attribute 28×19.6 → 12.25 px/u; live 16×16 → 10 px/u — the glyph is *smaller* than any axis computed). Both prescribed repairs die: D's `-0.08 -0.08 1.16 1.16` and C's two-attribute edit both bind on attributes the cascade overrides — the height is not the tree's to set. The cure is the F.W3 re-home. |
| R-3 | **D-2 severity** | MAJOR (ratio column scale-invariant) | MAJOR (same; px column corrected) | **MAJOR — unanimous, ratified** | The Δ/stroke ratios are geometry-independent (stroke and curve share the user-unit scale). This seat re-ran the endpoints: sine↔quad 0.0280 (0.19×), linear↔expo 0.2882 (1.92×) — exact. At live geometry the px column is 0.28 px on a 1.50 px stroke. 10 of 15 pairs below 1× stands. Perceptual half stays U-4 → SS-13. |
| R-4 | **D-8/C-D-2 (`size` = weight knob)** | CONFIRMED MINOR with tables | KILLED — unreachable; both tables unproducible | **LC wins (K-12)** — residue INFO | Under K-GEOM no value of `size` moves the box, the scale, or the stroke: the prop is INERT, not a weight knob. The `vector-effect` absence survives as a contract-portability rider on the F.W3 target (which ships it — producer EasingCurve.vue:12-14/:166, this seat's read). |
| R-5 | **D/S-3 superlative (`flex-shrink: 0` load-bearing)** | adopted approvingly | KILLED (redundant under `[&_svg]:shrink-0`; `display: block` blockified anyway) | **LC wins (K-9)** | This seat compiled `[&_svg]:shrink-0` → `& svg { flex-shrink: 0 }` under the consumer's own Tailwind; the chip is `display: flex` (EasingPicker.vue:69), which blockifies its items. Both scoped declarations are inert at the sole callsite. |
| R-6 | **reader-DU's M-5 (px-attribute-sized, inert to zoom/rem)** | filed as missed MINOR | (not filed) | **SEAT-KILLED (K-10)** | Inverted by K-GEOM: the glyph IS CSS-sized — by the producer, in rem (`--ui-glyph: calc(1rem × var(--ui-scale))`), so it is zoom-responsive and coarse-pointer-adaptive. DU's mechanism ("no CSS is written") is false — producer CSS is. The surviving content (the component's own contract offers no CSS sizing; a third party it cannot name controls it) is reader-LC's M1, banked as GEO-2. |
| R-7 | **reader-DU's missed M-1 (six chips keyboard-unreachable, BLOCKER)** | fresh BLOCKER, "no axis holds it" | (not filed) | **FACT UPHELD, BOOKING KILLED (K-11) — folds to banked fr-AnimationControls D-9/C-9** | True within this corpus, false within the fleet: the identity is already banked (MAJOR, blocker-class pending the SS-13 keystroke, F.W3) with the same MenuContentImpl mechanism this seat re-corroborated (`data-reka-collection-item` ×2; `:205` Tab preventDefault). DU's *new* evidence — the glass Button chunk registers no collection item (this seat's grep: 0) — is banked as corroboration on that row. One identity, never re-booked; DU's BLOCKER severity preserved in DISSENT. |
| R-8 | **D-6 (zero prose) severity** | MINOR | MAJOR | **MINOR — seat rules** | The magic numbers' causal weight is real (the undefended `-0.3` is GEO-1's authoring cause) but the harm is already booked in the geometry rows; double-charging one harm across two rows inflates the component (the fr-AnimationControls R-16 precedent). LC's MAJOR preserved in DISSENT. |
| R-9 | **reader-DU's M-3 (no reference frame) severity** | MAJOR | (not filed) | **MINOR — seat demotes** | Verified (the template emits one `<path>` and nothing else; the producer's frame furniture — unit rect, dashed diagonal, grids, endpoints, HTML axis captions — read by this seat at EasingCurve.vue:~118-190). But its harm is D-2's row; it books as D-2's design cause, cured free by the F.W3 adoption. |
| R-10 | **C/D-5 (motion-curves at 4.0.0) severity** | MAJOR ("best census amendment") | MINOR | **MINOR at component altitude, census-amendment weight in the routing — LC wins** | The fact is verified (this seat read the installed exports map: `./motion-curves` present at 4.0.0; both readers verified the value re-export + the `timingFunctions` exception). Nothing in this component causes or repairs it; the row's weight lives at the census (the K-13 altitude precedent). |
| R-11 | **L-1 / D-§5 kills (producer supersession)** | killed, with fairness note (STALE not fabricated) | killed, same route | **RATIFIED against the live producer (K-3/K-4)** — this seat's own reads: 8.0.0 LIVE today; `EasingCurve` takes `EasingStroke { d: string; tone? }` ("`d` is a PATH, not a callable, and that is deliberate"); index.ts:1-3 "three downstream repositories forked the display half; it is addressable now". Re-homing THIS component never touches `EasingPickerMode` or the persisted `animation_settings.easing`. L-1's algebra (no cubic-bezier equals cos or 2^x) stays sound and binds only the *editor* half. INFO residue for the named 6-chip grid (EasingPicker), which has no producer counterpart. |

**Citation-correction rulings inside agreed verdicts**: the D axis's provenance is systematically mis-anchored (reader-DU's M-6 ≡ reader-LC's K-CITE, one substance) — seat spot-checks confirm: EasingPicker.vue is 98 lines; D's `:103-105` (inside its highest-severity carry) is **past EOF** (real `:95-97`); `:98-102`→`:88-94`, `:76-78`→`:81-83`, `:47`→`:49`, `:34`→`:33`; `ladder.css:257,266`→`tokens/light-dark.css:146` (this seat verified the token there). The C axis mis-paths the loader (real: `components/visualization/composables/`) and `vite.config.ts:57`→`:51` (this seat: pinia in `vendor-vue` at :51). Symmetry note minted by this seat: reader-LC's own escape-hatch cite `UserSlugBar.vue:124 size-5 sm:size-3.5` is actually `:94`/`:106` `size-5 rounded-full` — right fact, drifted anchor. All findings survive; booked as CITE (MINOR, NO-WAVE-OWNER).

## Adjudicated defect roster

Line anchors are live-WT at `cd26c65`+dirty; re-resolve before any cure (D-19). Routes: **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5–W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring.

### MAJOR (9)

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **GEO-1** (D-1 · L-2 · C-D-3, RESCOPED by K-GEOM) | The viewBox reserves ±0.3 vertical headroom that is provably unreachable (six ranges exactly [0,1] — this seat's run) around an aspect (0.6875) that letterboxes in any box it lands in; at the LIVE square `--ui-glyph` box the curve inks 10×10 px inside 16×16 with 2.5 px dead gutters — smaller than any axis computed. Both prescribed attribute repairs are unexecutable (the cascade overrides the attributes). | :19-22; oxide/tailwind runs (R-1); easings ranges run | **F.W3** — dies whole in the `<EasingCurve>` adoption (constant square frame, letterbox zero by aspect identity — producer constants.ts:6-12, this seat's read). No interim attribute surgery: the box is not the tree's to set. |
| **GEO-2** (reader-LC M1, banked) | The design system silently overrides the component's entire size contract: `[&_svg:not([class*=size-])]:size-(--ui-glyph)` from the Button CVA base sizes the svg; BOTH `:width` and `:height` are inert at the only callsite; the tree knows the escape hatch (`size-5` at UserSlugBar.vue:94/:106) and does not use it here (`h-4 w-4` at AnimationControls.vue:106/:121 also loses at (0,1,0) vs (0,2,1)). The `size` prop is dead API. | button chunk; compile + scan runs (R-1) | **F.W3** — the re-home deletes the prop (EasingCurve is CSS-sized, `block aspect-square w-full`); **F.W4** rider: sweep the other `[&_svg]`-governed icon sites for the same silent override. |
| **D-2** (ratio column; px re-derived) | 10 of 15 catalog pairs separated by less than one stroke width — scale-invariant, endpoints re-run by this seat (0.0280/0.2882 exact); at live geometry, sine↔quad = 0.28 px on a 1.50 px stroke (0.19×). The three-way stroke-convention divergence (0.15/unit vs twin 9.4% vs producer whole-px `vector-effect`) stands. | :29; seat's separation run | **F.W3** — the producer's px-stroke + frame furniture is the cure; perceptual half → SS-13 (U-4). |
| **TOKEN** (D-3 · L-3 · C-D-1) | `color: "hsl(248 88% 71%)"` — theme-blind brand literal, byte-duplicated into `--easing-accent` (grep: exactly 2 sites), dead at the sole callsite (`:color` unconditional at :33), and the EasingPicker.vue:44-47 carry-note ("sole in-tree consumer") is false by the file's own :12. A conforming light/dark token ships at the INSTALLED 4.0.0 (`--viz-chebyshev`, light-dark.css:146 — this seat's read; 6.40:1 light per D, direction seat-ratified). Producer 8.0.0 supersedes the cure target: the consumer override point is **`--motion-accent`** (EasingCurve.vue template, this seat's read), not D's predicted `--easing-curve-accent`. | :12; EasingPicker.vue:49, :33, :44-47 | **F.W3** (currentColor + token in the re-home) + **GLASS-RELAY**: re-scope the filed `--viz-easing` ask to the `--motion-accent` seam; correct the false carry-note in the same relay (C-e adopted). |
| **CARRY-1.4.3** (D-3 CARRY, re-anchored) | The same literal colours the active chip's 9 px / weight-500 label at **3.46:1 vs page and 3.13:1 over its own `color-mix` 8% bed** (both this seat's WCAG runs) — below the 4.5:1 AA floor for normal text — in a tree that remediated `--viz-amber` at a *better* original ratio (3.56:1, seat-measured; style.css:113-121 verbatim). Real anchors `:95-97` over `:88-94` (D's cited lines are past EOF — see CITE). | EasingPicker.vue:86-97; seat's contrast runs | **F.W3/F.W4** — one edit with TOKEN; attributed to EasingPicker (the literal's second home), cross-ref banked fr-AnimationControls M-6 (same element, distinct defect: size vs contrast). U-5 (axe would flag if keystone 4 un-`fixme`'d) → SS-13. |
| **INVERT** (reader-DU M-2, banked) | Selection inverts the contrast hierarchy: inactive stroke `--muted-foreground` = 5.21:1 (the producer's own annotated figure, seat-reproduced), active = 3.46/3.13:1 — choosing a curve makes it the faintest mark in the grid, compounding D-2 on exactly the pair the user is comparing. No axis set the two ratios side by side. | :33; seat's contrast runs | **F.W3/F.W4** — same edit as TOKEN/CARRY; the D-2 SS-13 render doubles as its witness. |
| **FORK** (D-5 · L-6 · C-D-6) | The 6-member prop domain cannot serve the tree's 22-member second catalog, so a total fork exists with a byte-identical scoped class name (`.easing-preview` — subject :23/:37 vs MorphPhaseConfig.vue:47/:198-202, both seat-read): second sampler (easings.ts:115-127, own affine map, uncached), `stroke="currentColor"`, `stroke-width="1.5"`. Correction adopted from reader-LC: the twin imports through a THIRD barrel (`useMorphConfig.ts:20`, seat-verified), not `@/lib/easings`. The cure shape (`d: string`) is now producer-ratified. | :6; easings.ts:71,115-127; MorphPhaseConfig.vue:47-54 | **F.W3** — identity = census §3a HARD-shadow row + intake **R3-12** (folded, never re-booked); this record adds the geometric-incompatibility and third-barrel proof. |
| **CCOLOR** (D-4 · L-3(b) · C-D-1(b)) | A `color` prop where `currentColor` belongs: a CSS var smuggled through a JS ternary, restating state the cascade already expresses (`is-active` class :26; the sibling label gets both states from one rule :95-97), blind to the chip's own hover ground change (:81-83). Two in-tree existence proofs for the idiom (MorphPhaseConfig.vue:51; PathPreview.vue:16 `strokeColor: "currentColor"` — both seat-read). | :8, :12, :28; EasingPicker.vue:33 | **F.W3** — dies in the re-home (EasingCurve strokes `currentColor` off one channel). |
| **BREAK-8.0** (reader-DU M-4, banked) | The uplift target moved under the corpus: glass-ui **8.0.0** (published 2026-08-09, seat-verified: tag, CHANGELOG:3, 70 export keys) **retires `./dropdown-menu`** (`./menu` is the successor) — a new break row on `AnimationControls.vue:9` (seat-read), the direct host of all six previews, absent from the census break surface; F.W1's "4→7" framing is stale by a major. Seat re-checked the survivors at 8.0.0: `./metric-badge`/`./hover-card`/`./hover-popover` still absent — the existing rows hold; this is strictly additive. | producer package.json; AnimationControls.vue:9 | **F.W1** — census FE §5 / C-4-census break-table AMENDED (one identity with the banked fr-AnimationControls L-13 deferral row; the version target re-pins 7.0.0 → 8.0.0). |

### MINOR (11)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **CENSUS-MC** (C-D-5) | `@mkbabb/glass-ui/motion-curves` exists at the PINNED 4.0.0 (seat-read exports map) and value-re-exports every easing symbol `lib/easings.ts` imports bare-root (both readers verified the dist module; exception `timingFunctions` holds) — the value.js-specifier half of the scheduled migration is NOT blocked on the uplift. | **F.W1** — census §1/§3 rows (`CENSUS:94-95,188,191`) AMENDED; sequencing input only (component-altitude ruling R-10). |
| **RESOLVER** (D-10 · L-4 · C-D-4) | `easings.ts:105` is the tree's only unguarded catalog index (three siblings guard — :66, :116, animation.ts:28, all seat-read), called from a template expression with zero error boundaries (grep 0); the corridor is a bare `str` at both seam ends (shared.py:69 sole line; types.ts:48) and the loader cast is the whole validation (:56). Latent, unanimously and correctly (the sole callsite iterates the catalog). | **F.W4** (one `?? linear` guard-parity line) + **F.W5–W8** (C-b adopted: hoist the union to the operation record as `Literal[…]`); the restore-seam cast identity = banked **fr-AnimationControls L-11/M-10** — folded, not re-booked. |
| **BARREL** (L-5 · C-D-7 + reader-LC M4) | A stateless leaf imports a pure function through a Pinia store's self-described compat barrel (animation.ts:9-11, the shim shape the standing precept forbids) — and the pattern is 3×, not 1×: `useMorphConfig.ts:20` and `useFourierMorph.ts:29` also front the same 127-line lib (seat-verified); the two preview forks reach it through two different barrels; zero direct importers. Bundle half correctly declined by both axes (pinia in `vendor-vue`, vite.config.ts:51 — seat-read). | **F.W3/F.W4** — colocation repair has three seams; the alias `EasingName` (dominant 3-vs-2) reconciles in the re-home. |
| **GATE** (D-11) | No observing gate: vitest ABSENT (seat: 0 in package.json, no config), the only keystone over this surface is `test.fixme` (:192, seat-read with its honest booked-baseline comment), axe blocks on serious/critical (:27-34) and cannot audit graphic-stroke contrast by construction. | **F.W4** — gate re-enable rides the cure wave; identities = census §5 + banked **fr-AnimationControls C-29** (folded). |
| **FACET** (L-7) | 32 uniform-`t` samples flatten `circ` at its vertical tangent (~0.55 px at attribute scale; direction seat-ratified — infinite derivative at t=0.5); masked by today's fat stroke, WORSENS under any repair that raises the scale. | **F.W3** rider — re-derive `n` (or adaptive sampling near the tangent) at the adopted geometry; fourier keeps generating `d`, so the sampler survives the re-home. |
| **PROSE** (D-6, ruled R-8) | Zero comments of any kind across 41 lines; four consequential magic numbers (:11, :19, :22, :29) undefended in a repo that annotates two-line overrides with measured ratios and wave ids (style.css:113-121 et al., seat-read) — the sentence nobody wrote is the one that would have caught GEO-1 at authoring time. | **F.W3** — the file dies in the re-home; any surviving remnant takes a header stating coordinate space + pad derivation. |
| **A11Y** (D-9 · L-9 · C-D-8) | The `<svg>` carries no accessible-name posture at all and no prop to choose one; the tree `aria-hidden`s its lucide icons and none of its 12 hand-written svg surfaces. Producer cure shipped (`label` → `role="img" :aria-label`, EasingCurve.vue — seat-read). | **F.W3** — the adoption resolves it; accname effect (U-1) → SS-13. |
| **DEADCOPY** (reader-LC M2, banked) | `ANIMATION_EASINGS.description` is REQUIRED on the type (:76), authored six times (:78-83), read ZERO times (seat grep: 3 `EASING_OPTIONS` lines, loop body reads `.label` only) — dead data that is exactly the per-curve copy A11Y needs. | **F.W3** — wire as the `label` copy in the adoption, or delete with rationale. |
| **WRONGPAD** (reader-LC M3, banked) | The pad is on the wrong preview: this component (which cannot overshoot) pads 37.5% of its height for overshoot, while the morph twin (whose catalog DOES overshoot — seat-measured back extrema ±0.097/1.093) maps `y = 18 − v·16` into a 0..20 box and CLIPS the overshoot plus stroke (paints to y≈20.23/−0.23). The strongest single argument for the one-renderer repair. | **F.W3** — both previews onto `<EasingCurve>`; its `clipped` excursion contract (seat-read) owns the overshoot honestly. |
| **NOFRAME** (reader-DU M-3, demoted R-9) | No reference frame — no unit box, axes, baseline, or diagonal — the design cause under D-2's separation arithmetic; the producer's display unit draws exactly the missing furniture first. | **F.W3** — free in the adoption; MINOR to avoid double-charging D-2's harm. |
| **CITE** (reader-DU M-6 ≡ reader-LC K-CITE, merged) | The D axis's provenance is systematically mis-anchored, including one citation past EOF inside its highest-severity carry (seat spot-checks in the ruling block above); L and C drift by a line or a path; reader-LC drifts once itself (UserSlugBar). Findings survive; a repair agent following D's anchors lands outside the file. | **NO-WAVE-OWNER** — SS-3/SS-4 spec authoring re-resolves every anchor before consuming (the D-19 law); recorded as corpus-hygiene evidence. |

### INFO (9)

| id | claim | disposition |
|---|---|---|
| **VEFF** (D-8/C-D-2 residue, R-4) | No `vector-effect="non-scaling-stroke"` — pure contract-portability under K-GEOM (the prop cannot be exercised); in-tree cure exists (PathPreview.vue:56 `strokeWidth / size`, seat-read); producer names the defect class in prose. | **F.W3** — the adoption ships it. |
| **DEGEN** (D-12 · L-8 · C-D-11, height half killed K-13) | `:size="28"` restates the default; of three props one is load-bearing, one its own default, one a dead default; the 0.7 factor produces no box at all. Subsumed by GEO-2. | **F.W3** — props die in the re-home. |
| **FLOOR** (D-13) | The 9 px, 11-character unbreakable caption — not the svg — sets the grid column's min-content floor (`repeat(3, 1fr)`; `1fr` floors at auto/min-content). Survives K-GEOM untouched. | **F.W3/F.W4** — layout decision in the re-laid-out host; U-3 (menu overflow) → SS-13. |
| **SAMPLE** (D-14 · C-D-9, bezier sub-claim killed K-6) | ~4× oversampled for the rendered size (33 pts, 461 chars ×6, `toFixed(3)` quantum ~80× finer than a device px; `linear` = 33 collinear points). Cache makes it one-time cost. | **F.W3** sweep rider with FACET (one `n` decision). |
| **NAMES** (C-D-10.1/.3; .2 killed K-5) | `color` where the sibling says `strokeColor`; scoped block body-identical to PathPreview's (both seat-read). | **F.W3** sweep. |
| **PARDEF** (reader-LC M5, banked) | `preserveAspectRatio="xMidYMid meet"` is the SVG initial value — a no-op declaration two axes credited as a deliberate decision (C/S-4's cell corrected in place; substrate praise survives). | **F.W3** sweep. |
| **ONECALL** (reader-LC M6, banked) | `generateCurveSVGPath` is exported with exactly one caller three lines below it (seat grep); `easings.ts:9`/`:10-16` are duplicate `@mkbabb/value.js` import specifiers no axis flagged. | **F.W3/F.W4** sweep. |
| **S1-INFL** (reader-DU M-7, banked) | D/S-1's "motion-free by construction … do not regress by adopting a playback dot" is doubly wrong live: the producer dot is opt-OUT (`travel: null` default + `v-if`, seat-read) with `motion-reduce` arms; meanwhile the chips' own colour transitions are PRM-ungated. | superlative corrected in place; the ungated-transitions half = banked **fr-AnimationControls M-7** (folded, not re-booked). |
| **PICKER-RES** (L-1 residue, R-11) | The named 6-chip grid (EasingPicker) still has no producer counterpart — that half of census row `CENSUS:191` remains a product decision; the row should be SPLIT (D's ask, corrected reason). | **F.W3** — census row split note; preview half = mechanical de-dup, picker half = decision. |

## Killed-claims register (13)

| # | killed claim | refutation (adjudicated) |
|---|---|---|
| K-1 | **K-GEOM's target: the 28×19.6 attribute geometry shared by ALL THREE axes and CONFIRMED by reader-DU** (D-1/L-2/C-D-3 magnitudes, D-2 px column, D-7's viewport arithmetic base, D-8/C-D-2 tables, D-12/L-8 height, D-13's "12.25 px ink" cell) | Reader-LC's kill, **upgraded from conditional to unconditional by this seat** (R-1: oxide-scanner extraction + tailwind compile + import-chain reads, all seat's own). Every px figure in the corpus is void; the letterbox direction, the dead headroom, and the ratio column survive at the corrected 10 px/unit. Reader-DU's meticulous re-runs were correct arithmetic on an inert premise — recorded in DISSENT as the corpus's central method event. |
| K-2 | **D-7** — "both endpoint round caps are clipped; the round terminals render as flats" | Killed by both readers, ratified under BOTH geometries by this seat's arithmetic: the viewBox is not a clip; the horizontal letterbox leaves the caps ~7 px (attribute) / ~2.25 px (live) inside the viewport; vertically the caps sit 0.225 u off the edge. Mutually exclusive with D-1 (one needs the letterbox, the other its absence). Rider retained: C's prescribed square-at-0.05-pad repair would have created the clip for real (0.05 < 0.075) — moot under K-GEOM, preserved as the cure-adjudicated-separately lesson. |
| K-3 | **L-1 (the axis's sole BLOCKER)** — F.W3 re-home breaks the persisted easing contract; migration necessarily lossy | SUPERSEDED at the producer's head, seat-verified live: 8.0.0 ships display-only `<EasingCurve>` taking `d: string` — re-homing this component touches no persisted value. STALE not fabricated (correct when authored against 7.0.0's editor-only subpath — seat confirmed the fairness note stands). The transcendental algebra survives, binding only the editor half. Residue banked PICKER-RES (INFO). |
| K-4 | **D §5 CONTRADICT** — "no counterpart to re-home onto; product decision, not de-duplication" | Same supersession: the producer ask D proposed landed as `EasingCurve`, whose header names fourier's 41-line preview (seat-read). D's diagnosis was exact at 7.0.0 and its prescription is what shipped; the live verdict is the opposite of the one it reached. Its `--easing-curve-accent`/`--viz-chebyshev` cure cells superseded by `--motion-accent`. |
| K-5 | **C-D-10.2** — "`withDefaults` is the minority idiom (9:27 against the 3.5 destructure form)" | Reader-DU's kill, CONFIRMED by this seat's own census: destructured-props = **0** files; the 27 are `const props = defineProps` (cannot express defaults); among default-declaring files `withDefaults` is **9-for-9** — the house rule, not the minority. C's own headline comparand (PathPreview.vue:4) uses it — seat-read. Inference inverted, cell dead. |
| K-6 | **C-D-9 sub-claim** — "five of the six curves have exact 1-segment `C` representations" | Reader-DU's kill upheld: sine and expo are transcendental (the corpus's own L axis proves no algebraic curve equals them on an interval); circ is degree-wrong; at most linear exactly + quad/cubic at two segments per curve. `CSSCubicBezier` is an evaluator over the standard *approximations*. The oversampling finding survives separately (SAMPLE). |
| K-7 | **C-D-4 sub-claim** — the no-active-chip desync "is live today, no cast-crash needed" | Reader-DU's kill upheld: identical precondition to the throw C itself grades latent — a persisted out-of-catalog value nothing in the tree produces (API default `"sine"` shared.py:69; client default `"sine"` defaults.ts; sole writer keyed off the catalog `v-for` — all seat-read). Both latent; the corridor row survives as RESOLVER. |
| K-8 | **L-2's causal cell** — the 0.3 headroom "is vestigial, inherited from the MORPH catalog" | Reader-LC's kill upheld: the morph renderer pads 2/16 = **0.125** curve-units (easings.ts:123, seat-read); the actual back extrema are ±0.097 (seat-measured); the producer's tight bound is 0.1. No in-tree source yields 0.3 — the causal story is invention; the defect it decorated is confirmed (GEO-1). Worse, the implied inheritance runs backwards (WRONGPAD: the overshooting catalog's own preview pads nothing and clips). |
| K-9 | **D/S-3 superlative** — "`flex-shrink: 0` is load-bearing … two declarations, both load-bearing" | Reader-LC's kill, seat-ratified by compile: the parent CVA base ships `[&_svg]:shrink-0` targeting this exact node; `display: block` is a no-op under the chip's `display: flex` blockification. Both scoped declarations inert in the shipped composition — the opposite of the claim. (They matter standalone; the superlative was stated about the tree.) |
| K-10 | **reader-DU's missed M-5** — "the component cannot be CSS-sized … inert to zoom, rem, container queries" | **Seat-minted** (R-6). The glyph is rem-sized by the producer override — zoom-responsive and coarse-adaptive by construction; "no CSS is written" is false. Surviving content re-homed into GEO-2 (the override is silent and the component's own contract cannot name it). |
| K-11 | **reader-DU's missed M-1 as a NEW BLOCKER booking** — "six chips keyboard-unreachable … no axis holds it" | **Seat-minted booking kill** (R-7). The mechanism is real, re-corroborated, and **already banked** at fr-AnimationControls D-9/C-9 (MAJOR, blocker-class pending SS-13, F.W3) — the fleet holds it even though this component's corpus does not. Identity guard: fold with the new Button-non-registration proof as corroboration; DU's severity preserved in DISSENT. |
| K-12 | **D-8/C-D-2 as booked** — "`size` is silently a weight knob; both ends of its range are bad" | Reader-LC's kill upheld (R-4): under K-GEOM no value of `size` reaches the render; both tables are unproducible in the shipped composition. Residue banked VEFF (INFO). |
| K-13 | **L-8/D-12 height half** — "19.6 px fractional layout box, engine-dependent rounding" | Reader-LC's kill upheld: the height attribute is overridden; the box is a whole 16 px (desktop). The `0.7` produces no box at all — sharper than the killed claim. |

## Superlatives — with provenance (L-18 runs both ways)

1. **C/S-1 — the glyph and the clock read the same function object** (easings.ts:105 · animation.ts:28 — both seat-read): one table, one row, one reference; the preview provably cannot lie about what will play. Both readers verified independently; the sibling (different generator, different catalog) proves the property is earned, not automatic. The corpus's best observation, unanimously.
2. **S-2 — the bounded module cache, filed defensively** (easings.ts:99; closed 6-member key union): all three axes and both readers converge, and L records it explicitly "so another lane does not re-file it" — the rarest kind of superlative. C/D-12's forward carry (key-or-clear the moment curves become configurable) rides F.W3 with it.
3. **C/S-4 — the normalized [0,1]² substrate, defended beside the defect filed against its frame** — vindicated in the strongest way: `generateCurveSVGPath` already emits exactly the SVG space `EasingCurve` declares for `d` (seat-read both ends). The substrate C defended is what makes the re-home a one-liner. (Its `preserveAspectRatio` credit cell corrected — PARDEF.)
4. **D/S-4 — zero glass-ui specifiers, with the consequence attached** — verified at 8.0.0 (imports: none; the repair is pin-independent while the host is hit by BREAK-8.0). **Seat sharpening**: K-GEOM adds the irony that the "uplift-inert" file is nonetheless glass-*governed* through the Button's CSS — true for imports, false for rendering; both halves now recorded. |
5. **S-3 — zero teardown surface, provably** (41 lines; `defineProps` and nothing else): the one member of the family the F.W3 lift moves without untangling state.
6. **L §R5-7 — the corpus's only stated negative that runs its own falsifier** (zero `v-for`; the parent loops a registered component) — seat-confirmed; the discipline the rest of the corpus applies only to positives.
7. **The producer's independent convergence** — `EasingCurve.vue:5-9` names fourier's "41-line preview" and `constants.ts` derives VIEW_PAD from measured extrema this seat reproduced to four decimals: two teams, opposite sides, same diagnosis within days. The strongest available evidence that GEO-1, FORK, VEFF and A11Y are real.
8. **The corpus's falsifiers bit** — L-4/D-10/C-D-4 each independently downgraded their own crash to latent; L-5 declined its own bundle inflation; C-D-7 refused an unbuildable byte figure. That posture is why the surviving rows are trustworthy even though the shared geometric premise beneath them was wrong — and why the one thing nobody falsified (the attributes' own effectiveness) is the method lesson.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **K-GEOM corroboration** (now corroboration ONLY — the chain is static): `getBoundingClientRect` on `.easing-preview` → expect ~16×16 desktop, ~27×27 coarse-pointer; a stale-built `web/dist` (2026-06-12) witnesses nothing either way.
2. **Chip overflow** (reader-LC): ~40 px of stacked chip content inside the Button's fixed `--control-h-sm` 36 px box — needs a measured box.
3. **D-2 perceptual half (U-4)** at the LIVE 10 px/unit geometry, 1× and 2× DPR; NOFRAME is the variable most likely to move it.
4. **U-1/C-D-8** — accname of the untitled `<svg>` inside `role="menuitemradio"` (engine/AT-dependent).
5. **U-3** — 3-column grid vs real `DropdownMenuContent` width (min-content ≈198 px statically derivable; the content width is not).
6. **U-5** — axe `color-contrast` (impact `serious`, harness-blocking) on the active label if keystone `:192` is un-`fixme`'d; ratio 3.13–3.46:1 measured, the run is the proof.
7. **U-6** — dark-arm ratios (light figures exact; dark resolves through `light-dark()`/`contrast-color()` chains not statically decidable).
8. **C-D-7** — bundle byte delta of the store-barrel import (plausibly zero; needs a build, write-forbidden here).
9. **R5-7 adjacent** — whether callsite-keyed leaves under-count this component's 6 runtime instances 6:1 (carries F.W4; the deriver was not read by any seat).

## Identity guards (folds by reference — never re-booked)

- **fr-AnimationControls D-9/C-9** (banked MAJOR, blocker-class pending SS-13): reader-DU's M-1 folds; the glass-Button non-registration grep banked as corroboration (K-11).
- **fr-AnimationControls L-11/M-10**: the loader `as EasingName` seam — RESOLVER books only the guard-parity line + the F.W5 Literal hoist; the seam identity stays banked.
- **fr-AnimationControls M-6**: the 9 px label size — CARRY-1.4.3 is the same element's *contrast* defect; cross-referenced, not merged.
- **fr-AnimationControls M-7**: ungated child transitions — reader-DU's S1-INFL observation folds.
- **fr-AnimationControls C-29 / census §5**: the `fixme` keystone + vitest absence — GATE localizes, identity stays.
- **Census §3a HARD shadows + intake R3-12**: FORK is that row's geometric/third-barrel proof; one identity.
- **Census FE §5 / C-4-census + fr-AnimationControls L-13 + fr-BasisCanvas C-8/G33**: BREAK-8.0 AMENDS the one break-table identity (7.0.0 → 8.0.0; `./dropdown-menu` row added; survivors re-verified).
- **CENSUS:94-95/:188/:191 + lane-frontend:409-413/:641**: CENSUS-MC amends the motion-curves cell; PICKER-RES books the row-split note.
- **R6-8** (intake): RESOLVER's seam framing displays the ambiguity; identity stays with the intake's F.W5–W8 carry.
- **kf-** rows: different product; no overlap. **pt-/value.js** rows: none touched (the easing fns consumed here are value.js exports, defect-free in this corpus).

## DISSENT — preserved, not ruled away

1. **The shared-blind-spot event (evidence about the fleet, not the tree).** Three axis challenges and one hostile re-reader (DU) computed exhaustive, internally-flawless geometry — re-running samplers, re-deriving `meet` arithmetic to four decimals — on SVG presentation attributes that the shipped composition overrides wholesale. Reader-DU even *re-verified* the arithmetic while confirming it. This differs from the fleet's recorded citation-inheritance events: nothing was inherited stale — every seat independently verified the same wrong frame. Verification-without-composition is the failure mode; the K-GEOM kill was findable only by asking what the *parent's* stylesheet does to the child. SS-3/SS-4 spec authoring should treat "attribute claims about a component rendered inside a design system" as unverified until the cascade is read.
2. **Reader-DU's BLOCKER severities preserved**: M-1 (keyboard, WCAG 2.1.1 A — the banked row re-opens blocker-class on SS-13 confirmation) and its M-3 at MAJOR (this seat ruled MINOR, R-9).
3. **Reader-LC's MAJOR on D-6 preserved** (R-8): "a repo that annotates everything shipped its most consequential numbers naked" — the seat agrees on the fact and demotes only the double-charge.
4. **This seat's scope notes**: the full D-2 pair table and L-7 deviation table are adopted from the readers' mutually-agreeing independent runs (this seat re-ran the endpoints and the six ranges only); the coarse-pointer 27 px figure rides the 1.125rem mobile root measured by the fr-AnimationControls seat, not re-measured here; the producer-side dist reads (`motion-curves.js` value re-export byte detail) are reader-verified twice and spot-confirmed via the exports map only.
5. **Reader-LC's kill discipline is the corpus's model** — K-GEOM was filed WITH its own unproven link named. This seat's closure of that link (oxide scan + compile) is the tri-fold working as designed: the kill was right, and it still needed a fresh instrument to become unconditional.

## Closing verdict

**ADJUDICATED — DEFECTIVE, zero BLOCKERs booked: 29 rows confirmed (9 MAJOR · 11 MINOR · 9 INFO) including 11 reader-missed rows banked · 13 kills registered (K-GEOM closed unconditional by this seat's own oxide/tailwind instruments; 2 kills minted against reader-DU, including one booking-altitude kill folding a claimed BLOCKER to the banked fleet identity) · 1 reader miss folded to fr-AnimationControls without re-booking · routings: 3×F.W1 census/break amendments · the geometry/fork/token/a11y core to **F.W3** (the `<EasingCurve>` adoption retires GEO-1, GEO-2, D-2's frame, TOKEN, CCOLOR, FORK, VEFF, A11Y, NOFRAME, WRONGPAD in one re-home — producer-ratified, live at 8.0.0 as of today) · guard/gate/layout rows to F.W4 · the domain hoist to F.W5–W8 · 1 GLASS-RELAY (re-scoped to `--motion-accent`) · 1 NO-WAVE-OWNER (CITE) — all agglomeration input to the forming X·F specs (M-25), stamping nothing.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
