# wb-extract-workbench — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **HEAD**: `1994eb53` · branch `tranche-u` · **Corpus**: three challenge axes against `demo/workbenches/extract/ExtractWorkbench.vue` (293 lines) and its subtree, authored at corpus HEAD `c654824e` — C-implementation (r1/r2/r3; the unsuffixed r3 file governs, C-1..C-35 + CORRECTION-C), D-design (pass1 D-1..D-28 / pass2 D2-01..D2-23 / governing pass3 D3-01..D3-02 + re-measurement register), L-library (pass1/pass2 / governing pass3 P3-1..P3-5 + §0 reconciliation).
**Re-readers adjudicated**: design-and-usage · library-and-code (two hostile re-reads of the full corpus). Both were attacked before either was adopted; every kill re-derived against the tree, every confirm spot-audited at its sharpest line, every reader-found miss re-walked in source by this seat.
**Sibling record**: `registry/adjudicated/wb-extract-pane.md` (banked `0afc25c3`) covers the same subtree from the pane seat. Its identities XP-1/XP-2/XP-7/XP-8/XP-9/XP-11/XP-12/XP-13/XP-14/XP-19/XP-20/XP-21 recur in this corpus and are **cross-booked, never re-minted** (the anti-rename law).
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling per the commission)**: full reads of ExtractWorkbench.vue (293), useExtractSession.ts (223 — return literal counted: **19 members**), useImageQuantize.ts (161), ExtractControls.vue (151), ImageDropZone.vue (113), PaletteColorStrip.vue (73 — `WEIGHT_FLOOR` at :48, floor+renormalise at :63-67), useLoupeCanvas.ts (75), quantize-worker.ts (44, whole), ImageEyedropper.vue (:1-248) + useInertiaGesture.ts onTap/return blocks; src/quantize.ts :48-66/:124-140 (**the sort at :128; the alpha<10 skip at :57; `dominantColor` k:5 at :132-139**); ShadowPalette.vue :1-50 (the R12/genesis header read whole); PaletteCard props/emits counted (**10 props :183-195, 16 emits :200-218**); usePalettePorts.ts (`createPalette` destructured :62, exposed :95/:142 — the same function object); demo/palettes/utils.ts :15-31. Repo censuses run by this seat: **`<DockControl` = 19 instances / 11 `title`-only (all under demo/workbenches/**) / 8 `aria-label`-only (all under demo/shell/dock/**) / 0 both**; **`isLocal: true` = exactly 3 sites** (store ×2, extract ×1; zero `gen-`/`mix-` id mints); **`tracking-[0.18em]` = 4 sites** (ExtractWorkbench ×2, ImageDropZone ×1, EmptyState ×1) against installed glass-ui's `text-mono-caption` (`letter-spacing: var(--type-tracking-caps)` = **0.1em**, `text-transform: uppercase` already applied — dist utilities.css + scheme-motion.css read); `--btn-hover-color` = exactly 3 (ExtractControls); DockControl.vue.d.ts hit-cell doc (":14-15 — HIT CELL stays the full `--dock-control-size` (≥44px on coarse via the density clamp)"). Canon re-read at the cited lines: VISUAL-CONSTITUTION.md §4 type matrix + "Live numbers use tabular figures and reserve their widest legal representation", §4.1 never-color-only, §4.2 "every other composition retains no divider", §5 palette-card law, §5.2 "numeric/scientific sign never mirrors" + image-sampler x/y row, Extract section ("not a giant shadow placeholder"; "Eyedropper and sampler have keyboard/numeric alternatives"); OPTICAL-BENCH-COMPOSITIONS.md:42 Extract row (narrow order "source/drop; result; controls/commit") + :76 boundary inventory (`[]`/`none`/none); demo/DESIGN.md:60-75 card-lock law ("Two mechanisms, **both required**"). **X wave files read for routing**: X-W7.md — ExtractWorkbench.vue is `modify-carve` (:124); **W7 item 13 / G20 deletes the `ExtractWorkbench.vue:163-166` caption and cuts the plate mass on BOTH ShadowPalette + PaletteCardSkeleton (:64-65, :536-550)**; W7 item 10 creates `format-color.ts` with four registers over a per-channel decimal table (:58, :251); W7 item 4/:206 creates the props-only `PaletteSpecimen` (zero CRUD imports); **W7:523-527 already names the `uppercase`/`tracking` eyebrow override family** — plus the sibling's pinned CC-108/A-17 (chassis dead) and X-W5 item 5 (golden-ratio numerics = fabricated tolerance).

## Scoreboard

**36 adjudicated defect rows: 3 BLOCKER · 17 MAJOR · 13 MINOR · 3 INFO** — 24 corpus-claim rows confirmed (several consolidated/rescoped, 8 of them cross-booked identities with wb-extract-pane), 12 reader-found misses admitted after re-derivation (2 corrected in framing by this seat). **23 rows in the killed register**: the readers' 19 kills ALL UPHELD on re-derivation (several sharpened by this seat's own censuses), plus 4 kills/demotions minted by this seat — one against a reader confirm (P3-2's MAJOR grade), one against the governing L-r3 file's own §0 cure position, two framing corrections against reader-found misses. Blocker-weight cluster: **the instrument disagrees with itself about its own input and output** — the cropped preview vs the uncropped quantizer/sampler (XW-1), and the whole teardown family that never runs under the shell's KeepAlive (XW-2, = XP-1).

## Ruled disagreements and reader-challenges (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| DockControl accessible-name cure | C-r3: route to glass-ui (`aria-label` from `title` default) · L-r3 §0: "three aria-labels in ExtractControls, no glass-ui change, no BH relay" (adopting pass-2's "sole divergence") · reader-2: killed the "sole divergence" premise, filed the 11/8 layer fork as a new MAJOR | **reader-2's kill UPHELD by this seat's census; L-r3's §0 position KILLED (K-21); routed per the sibling XP-8** | My own 19-instance census: 11 `title`-only, every one under `demo/workbenches/**` (ExtractControls ×3, capture chip ×1, ImageEyedropper ×3, MixResultDisplay ×3, GradientVisualizer ×1); 8 `aria-label`-only, every one under `demo/shell/dock/**`; **0 carry both**. Pass-2's live sample (two routes at rest, where Mix's gated controls and Gradient's Copy CSS are not in the DOM) generalised from an unrepresentative frame, and L-r3 §0 withdrew a correct draft on that refuted premise. The true shape is a clean **layer fork** — a convention decision across 11 sites/4 files/3 features, not one file's lapse and not a producer defect. Disposition = the sibling's XP-8: **X-W4** (its accname law `aria-label > … > title`), the producer `label`-prop ask rides the standing glass-ui BH relay. `title` is a legitimate last-resort accname source (pass-2 CORRECTION-A, upheld by both readers): weakly-named + touch-unavailable, not nameless. |
| D3-01's cure ("sort `extractedPalette` by population descending") | governing D-r3 ranks it cure #1 | **KILL UPHELD (K-1) — the cure is a no-op; C-r3's cure governs** | src/quantize.ts:128 `output.sort((a,b) => b.population - a.population)` is the last statement of `quantizePixels` — the payload already arrives sorted; useExtractSession.ts:85-89 maps in return order. The measured divergence lives entirely in the tiebreak (:132-137), which fires only on `population === best.population` — the exact case a population sort cannot break (and why the `>` branch at :133 is unreachable, per L's M-1). Sound cure = `dominant = presented.value[0]` (C-r3, matching the library's own `dominantColor()` shape) or a re-sort by the tiebreak's own comparator. The defect itself stands at MAJOR (tie-only — C-r3's live non-tie verification adopted over D-r3's BLOCKER grade). |
| Reader-1 miss #1 (strip floor vs headline %) | filed MAJOR, "no axis checked the readout against the picture beneath it" | **ADMITTED at MAJOR — verified whole by this seat** | PaletteColorStrip.vue:48 `WEIGHT_FLOOR = 0.08`; :63-67 floors then **renormalises** (`x/flooredTotal*100`); the extract card feeds weights implicitly (useExtractSession.ts:88 `weight: population/total`; PaletteCard mounts the strip with no explicit `:weights`, so the `own.some(w => w > 0)` branch at :57 takes them). Any k where a cluster sits under 8% (routinely k≥6) makes `flooredTotal > 1`, so the dominant cell renders `w/flooredTotal` while ExtractWorkbench.vue:123 prints `Math.round(dominantShare*100)` — the true share — on the line seated on the same plate. The session's own comment (:78-80 "the card's OWN strip is population-proportional") is false as stated; the strip's floor doc (:39-43) is honest, the co-seating is not. Same disease as D3-01, wider blast radius. One saving grace verified: flooring is monotonic, so the widest cell is still the max-population colour (the surviving half of K-3). |
| Reader-2 miss #1 (loupe blank on touch) | filed MAJOR, source-entailed | **ADMITTED at MAJOR — mechanism re-derived exactly; photograph is residue** | useLoupeCanvas.ts:55-60 `showLoupeAt` flips `loupeVisible` then calls `drawLoupe` **synchronously**; the canvas is behind `v-if` (ImageEyedropper.vue:79-86) and Vue flushes on the next job, so `loupeCanvasRef.value` is null and `drawLoupe` exits at :30. Mouse survives by accident: pre-pin hovers (`onHover`, :164-172) mount the canvas and redraw each move. Touch never does: the only touch caller is `onTap` (useInertiaGesture.ts pointerup → `options.onTap`), `onHover` is hover-only, and the capture-phase pointerdown (:176-185) runs `hideLoupe()` — unmounting the canvas — before every re-pin. Every pinned loupe on touch is an empty styled ring. |
| P3-2 (K-PALID defeated) grade | reader-1 carried MAJOR; reader-2's kill register called the remainder "INFO-grade" | **DEMOTED to MINOR by this seat (K-20)** | My census: `isLocal: true` = 3 sites, two of which ARE the store's minting functions; the sole outside mint is extract's, and the forged object never enters `getStore().value.palettes` (`onSave` → `createPalette`, which re-mints). The predicate's doc-vs-truth gap is real and structural, but with one minting site and zero runtime path it rides P3-1's union-tag cure rather than standing at MAJOR. Revives with a crash witness the day any workbench stores its display object. |
| Reader-1 miss #2 (tracking-override) framing | "went unrecorded by all three axes" | **ADMITTED at MINOR — framing corrected (K-22): the program already owns it** | Verified: 4 sites × `uppercase tracking-[0.18em]` over a producer utility that already sets both (`--type-tracking-caps: 0.1em` — an 80% per-instance increase). But **X-W7:523-527 already records this exact family** ("in all 11 the eyebrow is a `uppercase`/`tracking` that `text-mono-caption` already applies") — unrecorded by the corpus, pre-owned by the formation. The readers' systematic weakness, again: nobody read the X wave files (the sibling's DISSENT item 1 extends to this corpus). |
| Golden-ratio garnishes (D-19's "50/50 where canon requires 61.8/38.2"; D-5's ratio cell) | carried by both readers as canon | **NUMERIC LIMB KILLED (K-23); sequence/composition limbs survive** | The sibling's RULING PIN: X-W5 scope item 5 marks "61.8033989%/38.1966011%" as fabricated tolerance to be replaced, and CC-108/A-17 killed the P122 chassis those rows assume. OPTICAL-BENCH:42's **narrow order** ("source/drop; result; controls/commit") and :76's boundary inventory are composition statements that survive as authored intent — the numbers do not, and the canon rows carrying them are stale against A-17 (canon correction = X-W10, already booked at the sibling's XP-25). |

## Adjudicated defect roster

Line anchors re-verified at HEAD `1994eb53` (docs-only commits since corpus HEAD; the extract tree is untouched); re-resolve before executing any cure (the D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: a wave id means that wave's bounds/scope own the surface today; **NO-WAVE-OWNER** rows are booked for the next formation boundary. Two standing clusters from the sibling record: the **extract-session correctness cluster** (XP-2/XP-3/XP-4/XP-23 — no X wave's file bounds touch the two composables) and this record's **camera-mode cluster** (the camera lives in ExtractWorkbench.vue:228-281, inside W7's `modify-carve` file but beyond its declared scope — addendum candidate at the formation boundary, riding the session cluster's booking).

### BLOCKER

- **XW-1 — BLOCKER — D-1/D3-02 (confirmed on three instruments): the preview is silently centre-cropped ~30% and the instrument disagrees with itself about its own input.** Mechanism source-certain: ImageDropZone.vue:41 `w-full h-full object-contain` inside :8's flex + `overflow-hidden`, height-capped by ExtractWorkbench.vue:20 `max-h-[min(320px,40dvh)]` — percentage height against an indefinite containing block resolves to auto, the img takes intrinsic-ratio height (measured 458px in a 320px box, `croppedFraction 0.301`, twice independently). The quantizer reads the original file (useImageQuantize.ts:19) — D-r3's rails probe proved the palette renders black bands the preview does not contain; the eyedropper receives the **uncropped** dataURL (ExtractWorkbench.vue:175) — a third geometry. Cure = the governing D-r3's declared-ratio stage, ONE geometry for preview/sampler/quantizer; the pass-1 `max-height:100%` limb stays killed (K-19). → **X-W7** (the stage seat's sizing lives at ExtractWorkbench.vue:17-21, in the modify-carve; ImageDropZone.vue is not in W7's file list — **addendum candidate** for the zone's own `h-full`/`min-h` lines).
- **XW-2 — BLOCKER — C-29/C-2/C-31 + B-1 (confirmed): every teardown in the subtree is `onBeforeUnmount`, which never fires under the shell's KeepAlive — camera stream, quantize worker, debounce timer, and the eyedropper's `window` keydown all survive the view change.** IDENTITY = **wb-extract-pane XP-1** (cross-booked, not re-minted; the four orphaned teardowns, the `:max="6"` never-evicted cache, and C-r3's P7/P8 measurements are recorded there). This corpus adds C-r3's CORRECTION-C sharpening: the leak is **unconditional, no race** — pass-2's mid-flight framing was a second door — and the pass-2 negative ("worker termination … Clean") is formally withdrawn. → **X-W5** (the sibling's addendum candidate: deactivation-contract gate — navigate away from an active camera/quantize → zero live tracks, worker settled).
- **XW-3 — BLOCKER — D-11/D2-11/C-6 (confirmed, third instrument): the sampler is keyboard-unreachable by construction.** ExtractWorkbench.vue:25 makes a click on the drop zone the SOLE eyedropper opener; :22-23 bind `preview` and `disable-click` to the same value, so exactly when the zone becomes the sampler it takes `tabindex="-1"` (ImageDropZone.vue:19), its keydown gates off (:22), and `role="button"` + a "tap to sample" aria-label (:18/:20) co-occur with inertness. D-r3's proof shape adopted: absent from the whole tabbable set; programmatic focus + Enter → `eyedropperOpen: false`. Canon requires the alternative twice (VISUAL-CONSTITUTION Extract section; §5.2 image-sampler x/y row — re-read by this seat). Adjudicated BLOCKER: a first-class capability with zero non-pointer path, both canon-named. → **X-W4** — **addendum candidate** (ImageEyedropper is in W4's bounds as "import + face only"; the opener lives in ExtractWorkbench/ImageDropZone, beyond the carve).

### MAJOR

- **XW-4 — MAJOR (D filed BLOCKER; C-r3's measured grade adopted) — D-2/C-14: the `isProcessing` skeleton never paints; the only accessible progress signal is absent during the one phase that freezes the main thread.** useImageQuantize.ts:87 arms the flag only after the decode awaited at :105-107; C-r3's P5: `longTasks [517,63]`, `anySkeleton false`, a 519ms sampler hole on a 12MP PNG; D's occupancy probe agrees (`[[0,"ghost"],[2894,"card"]]`). The comment block at ExtractWorkbench.vue:87-101 asserts the opposite of what ships. Cure = decode in the worker (post the `File`), folding C-5/C-7/C-25. → **NO-WAVE-OWNER** (extract-session correctness cluster).
- **XW-5 — MAJOR — D3-01/C-8 (confirmed, tie-scoped): "the card's first swatch IS the dominant specimen" (ExtractWorkbench.vue:113-117) is false on population ties, and nothing binds the readout to its referent.** The comparator divergence verified by this seat: the palette arrives population-sorted (src/quantize.ts:128), so useExtractSession.ts:133's `>` branch is dead and only the chroma tiebreak (:134-136) is live — firing exactly on ties, which flat-colour graphics produce on the first upload (D-r3's four-band witness: readout green, swatch 0 crimson). C-r3's non-tie live verification honours the invariant, so MAJOR not BLOCKER. Cure per K-1's ruling: `presented.value[0]` or the tiebreak's own comparator as the sort — never the no-op population re-sort. → **X-W7** (the dominance row is in the carve; the comment deletes with the cure — the D-r3 gestalt law adopted: *relocate the invariant into structure or delete the claim*).
- **XW-6 — MAJOR — reader-1 miss #1 (admitted, verified whole by this seat): the strip and its own headline number disagree by construction on nearly every real image.** PaletteColorStrip's 8%-floor renormalisation vs the true-share numeral one line above — full derivation in the rulings table. The defensible strip floor is documented; the co-seated contradiction and the session comment (:78-80) are not. → **X-W7** (`PaletteSpecimen` owns the strip contract; `format-color` owns the numeral; the invariant-into-structure law applies to :78-80's prose).
- **XW-7 — MAJOR — D-8/D2-04/C-35 (confirmed thrice): the dominant readout is a 54-58-char unrounded float clipped to ~30% desktop / ~17% mobile, and the `title` escape hatch is the identical string.** useExtractSession.ts:54 serializes with no presentation rounding; ExtractWorkbench.vue:134-141 pre-excuses it ("never a lying readout") — the sixth unbound comment-invariant in D-r3's table. → **X-W7 — explicitly owned**: item 10's `format-color.ts` four registers (`compact`/`caption`/`interchange`/`exact`) over a per-channel decimal table (:58, :251) is the program's own cure.
- **XW-8 — MAJOR — D-12/D2-08 (confirmed live): `disabled` is computed and threaded, then honoured by 1 of 5 controls — and the camera is a one-way door.** ExtractControls.vue: `:disabled` consumed only at :84 (Reset); k/kC sliders, Upload, Camera never receive it (D-r3 drove k 5→6 with the viewfinder open). The viewfinder holds exactly one control ("Capture frame"); no Escape, no Cancel; the only exits are capture, a file drop, and the dead unmount (XW-2). → **NO-WAVE-OWNER** (camera-mode cluster; the cure sentence both axes converge on — the camera becomes a mode of the stage with an idempotent `useCameraCapture` — exceeds W7's declared carve).
- **XW-9 — MAJOR — D2-09/C-3 (confirmed, measured `streamCount: 2, ["live","live"]`): a second Camera press overwrites `cameraStream` with no re-entry guard and no prior teardown; `stopCamera` can only ever know the last assignment.** ExtractWorkbench.vue:228/:241-248/:257-263 re-verified. The Camera button is one click from re-entry because XW-8 never disables it. → **NO-WAVE-OWNER** (camera-mode cluster).
- **XW-10 — MAJOR — P3-3 + D2-06 + D2-07/C-5-extension (consolidated): the failure register is one mutable slot shared by three producers, colour-only, unannounced, and the stale result is never invalidated.** Camera faults are written through the writable computed's setter (ExtractWorkbench.vue:252 → useExtractSession.ts:66-73 → the quantizer's private ref), erased unconditionally by the next quantize (useImageQuantize.ts:86); the error line (:80-85) has no role/aria-live/dismiss and prints raw `${err}`; on failure the previous palette stays fully rendered (`palette` never cleared — C-r3's P4: broken `<img>` beside a byte-identical rail + readout, `destructiveLines: []` — **stale attribution**, the sharpest form). Stale-half IDENTITY = **wb-extract-pane XP-2**. → **NO-WAVE-OWNER** (session cluster; the `set:` half of `quantizeError` deletes with the camera's own error ref).
- **XW-11 — MAJOR — D2-16/L-2 (confirmed): the camera is implemented twice; the dead composable copy already returns the `stop()` disposer whose absence is XW-9.** IDENTITY = **wb-extract-pane XP-13** — cross-booked. → **X-W8** (G-10 dead-public-surface census extension).
- **XW-12 — MAJOR — D-17/D2-17 (confirmed): the result plate is an operable ornament** — unconditional `cursor-pointer`, `role="article"`, press choreography, `@click="() => {}"` (ExtractWorkbench.vue:152), `editable-name` against the canon's card law (§5: "no inline rename in the card body"; the palette-card seat law re-read by this seat). → **X-W7 — explicitly owned**: the props-only `PaletteSpecimen` (item 4, :206) deletes the CRUD surface from this seat.
- **XW-13 — MAJOR — D2-10 + reader-1 miss #4 (consolidated, verified): one predicate drives two facts, deleting click-to-replace and killing the announce chip in all three registers.** `:preview` and `:disable-click` bound to the same value (ExtractWorkbench.vue:22-23) make the `'Replace image…'` aria-label branch and the `'replace'` chip text unreachable (ImageDropZone.vue:20/:61); the chip that remains is `aria-hidden`, hover-only, and its `group-focus-visible` arm is dead because the group carries `tabindex="-1"` whenever the chip renders — leaving `cursor-crosshair` as the sole signal, absent on the touch platform the label addresses. → **X-W7** (one binding decision in the carve) — the chip's register rides XW-3's X-W4 addendum.
- **XW-14 — MAJOR — D-5 (confirmed, needs no probe): the binding narrow sequence is source → result → controls; the shipped order is source → controls → result, structurally, at every viewport.** ExtractControls (:64) precedes the result Transition (:102) in one column; the sole mount is `layout="column"`. The sequence limb of OPTICAL-BENCH:42 survives K-23; the ratio limb does not. → **X-W5** (the bespoke scene composition, per the sibling's XP-25 routing).
- **XW-15 — MAJOR — D-3 arm 1 (confirmed, measured): the empty arm breaks the NORMATIVE card-lock — dragging k 5→16 moves the pane rect (+128px height, −64px y).** ShadowPalette renders live-k swatches in `flex-wrap` with no fixed-row reserve; the developed arm is clean (img-k1/k5/k16 identical — the probe's own scoping honoured). → **X-W7 — explicitly owned**: G20 (:536-550) cuts the plate's mass and gates it at max k on both plate species.
- **XW-16 — MAJOR — D-7 + D-3 arm 2 (consolidated: one numeral, two violations): the derived stat renders at byte-identical size/family to the pane's identity (41.888px Fraunces, thrice-measured), in a closed type matrix that assigns `text-display` to "route H1 or major argument"; and it declares no `tabular-nums` and reserves no width where DESIGN.md:60-75 requires **both** mechanisms.** The tabular-only cure stays killed (K-7): the measured 21.88px jitter is digit-count, which only the `ch` reserve cures. → **X-W7** (the carve owns the row; the text-abrogation item owns the register).
- **XW-17 — MAJOR — P3-1 (confirmed, corrected): the forged `Palette` is a cycle — palettes parses `"__extracted__"` back out (utils.ts:20-30) and drives nine `PaletteCardMenu` branches on it.** Corrections folded (K-13/K-14, upheld by my census): only extract has ever minted an outside id — the registry's `gen-`/`mix-` thirds were written speculatively and have always been dead (P3-5 admitted as the tell, MINOR, riding this row). The structural argument (a fourth workbench edits a foreign feature's file) survives as hypothetical, not track record. → **X-W7** (the `PaletteSpecimen` split removes the menu from THIS seat; the `PaletteDraft` union-tag cure for utils.ts/types.ts is an **addendum candidate** — utils.ts is not in W7's file list).
- **XW-18 — MAJOR — C-9/D-23/D2-13 (confirmed as-rescoped): no gate has ever handed this component a file, and `#/extract` is in none of the special-state capture matrices.** Sharpened per the sibling's K-1: the route is NOT test-invisible (o18 born-RED-gates both slider tracks under `?space=lab`; walk.spec asserts the heading; o9 certifies the empty state + PRM stillness) — but `setInputFiles` = 0 repo-wide, no test imports either composable, and forced-colors/RTL/zoom-200/keyboard-focus have zero tracked evidence here. C-r3's three vacuous-gate mutations (empty `stopCamera`; delete both dead teardowns — **zero behavioural change**; delete the whole result branch) all stay green. IDENTITY = **wb-extract-pane XP-7**. → **X-W1** (the born-RED flow spec — still the highest-leverage single artifact either record proposes).
- **XW-19 — MAJOR (reader-2 filed BLOCKER; adjudicated MAJOR) — C-1: `lastFile` is written before an await and read after, so two rapid intakes can leave the preview showing image A while the palette describes image B — permanently if the FileReader reads settle out of order.** useExtractSession.ts:164-168 re-verified; no request identity anywhere in the chain. Requires two racing intakes → MAJOR, same family as the sibling's XP-3 (single-slot resolver race) and XP-23 (readAsDataUrl rejection). → **NO-WAVE-OWNER** (session cluster).
- **XW-20 — MAJOR — the DockControl accessible-name layer fork (reader-2 miss #3, admitted; ruled in the disputes table): 11 `title`-only under workbenches (7 in this subtree), 8 `aria-label`-only under the shell, 0 both.** IDENTITY = **wb-extract-pane XP-8**. D-r3's additions carried: the capture chip and the `<video>` itself are nameless even by `title`'s standard, and `/#/extract` owns 12 of the app's 26 flagged instances (46%). → **X-W4** + the producer `label`-prop ask on the glass-ui BH relay.
- **XW-21 — MAJOR — reader-2 miss #1 (admitted, ruled in the disputes table): the loupe — the eyedropper's whole precision affordance — renders as an empty ring on every touch pin.** → **X-W4 addendum candidate** (with XP-9's modality contract; the fix is one `nextTick`/`v-show` decision in the eyedropper).
- **XW-22 — MAJOR — C-24 (confirmed, measured with a transparent PNG): a successful extraction yielding zero opaque pixels renders the pre-image ghost ("feed it an image") beside the image just fed.** `[]` and `null` collapse at useExtractSession.ts:75-77; src/quantize.ts:64 returns `ok: true, value: []`. The state-union cure (pass-2's `barren` kind) owns it. → **NO-WAVE-OWNER** (session cluster).
- **XW-23 — MAJOR — D2-01 + D-r3 §5b (confirmed; magnitude is a range, not a decimal): the undeveloped ghost occupies 95.3–97.0% of the stage against canon that demands a contextual, collapsing empty** — and it renders beside Palettes' dashed dot-trio empty in one frame, two empty grammars as peers. The delete-the-species cure stays killed (K-8: two owner rulings, R12 + R7, in the file's own header — re-read by this seat); **X-W7's G20 mass-cut + caption deletion is the program's own, ruling-compatible cure**. → **X-W7** (:64-65, :536-550; o9 re-ruled there).
- **XW-24 — MAJOR — L-8+M-1 (confirmed): the published `./quantize` surface ships `dominantColor` with zero production consumers, a hardcoded second full `k:5` re-quantize, and an untyped population-descending guarantee the demo re-derives in 22 lines** (useExtractSession.ts:8-10 telling the library its export is the wrong tool — simultaneously a superlative and an indictment). → **X-W9** (the library apotheosis owns published-surface truth; adjacent to the sibling's XP-28).
- **XW-25 — MAJOR — L-5/N-6 (confirmed): the demo import-boundary law does not reach `demo/workbenches/**` — the barrel's "enforces it standing" claim is false** (`no-restricted-imports = null` on every file of this subtree; the live boundary object covers 17 of 251 demo files; `demo/@` does not exist). IDENTITY = **the ColorSpaceSelector apotheosis's L-5(a)** — ONE identity, already FOLDed to the lint-glob-liveness wave with a born-RED `--print-config` command. Cross-booked, not re-minted.

### MINOR

- **XW-26 — MINOR (demoted by this seat, K-20) — P3-2: the K-PALID predicate is satisfied by the forgery by construction.** Rides XW-17's union-tag cure; revives with a crash witness if a workbench ever stores its display object.
- **XW-27 — MINOR — L-3-as-rescoped (the readers' kill remainder): the leaf composable imports a foreign feature's store singleton instead of injecting the named port.** Behavioural half killed (K-11 — `libraryPort.createPalette` IS the store's function, verified at usePalettePorts.ts:62/:142); the module-graph fact stands. → **X-W7** rider (the specimen split re-draws this edge anyway).
- **XW-28 — MINOR — L-14 (confirmed): the subtree's `inject(CSS_COLOR_KEY, undefined)` is the codebase's only defaulted inject of that key, and the `?? ''` mask (ExtractWorkbench.vue:69/:149) silently degrades the certified track ink to `var(--ink-muted)` instead of failing loud.** → **X-W7** addendum (one binding in the carve; the 8-vs-1 house-contract question follows the ColorSpaceSelector L-5(b) precedent).
- **XW-29 — MINOR — L-9/L-10 (confirmed): tsconfig.demo.json's phantom/omitted paths + the stale self-install of `@mkbabb/value.js@4.0.0`.** IDENTITY = the sibling's XP-21 / ColorSpaceSelector L-14 — W-HYGIENE config parity, **NO-WAVE-OWNER in X** (X-W8 explicitly declined it). Cross-booked.
- **XW-30 — MINOR — N-3 (confirmed): both glass-ui import spellings live in this one subtree** (narrow subpaths in the workbench, alias-hop root-barrel widenings in ExtractControls/ExtractPane, adjacent lines). IDENTITY = the sibling's XP-11. → **X-W8**.
- **XW-31 — MINOR — L-7/C-32 (confirmed): `DisplayColorSpace` ×4 over a canonical export, coincidentally identical, crossing the untyped router seam.** IDENTITY = the sibling's XP-14. → **X-W5**.
- **XW-32 — MINOR — the colocation/per-instance family (consolidated, all grep-verified by this seat):** (a) `.plate-ink` ×5 — IDENTITY = XP-19 → **X-W10**; (b) the `uppercase tracking-[0.18em]` eyebrow override ×4 — **X-W7, pre-owned at :523-527** (framing corrected, K-22); (c) `--btn-hover-color` ×3, the only sites in demo — one declaration on the row does the job (owner edict 5) → **X-W7** rider; (d) dead scoped `.touch-gate-target` (C-33) whose comment names behaviour the file lacks → **X-W7** rider; (e) EmptyState's caption re-implemented as a bare `<p>` where the shared atom's `dots={false}` affordance was built for this exact seat (L-12) → **X-W7** (:270 consolidates the caption dialects); (f) two `DockSeparator`s bracketing the kC axis against VC §4.2 / OPTICAL-BENCH:76's `none` — carried WITH the readers' honest caveat that a Dock-internal group separator is arguably Dock grammar; the ambiguity itself needs the cure wave's ruling → **X-W7** face carve.
- **XW-33 — MINOR — the redundancy/idiom family (consolidated):** D-16 three renderings of one palette in ~320px (k-rail gradient + strip + swatches, plus the duplicated cardinality token); D-18 two accent families ~140° apart in one card (static `border-primary` zone edge vs live certified `useSafeAccentFn` rail — probe-pinned oklab values); D-27 two template-ref idioms on adjacent lines (:222-223); reader-2's `@click` fallthrough onto an undeclared-emit child whose root already handles clicks with the inverse gate (:25 vs ImageDropZone:21 — verified: the child declares only `file`); reader-1's orphan `<label>`s (no `for`, no labelable descendant — the real names live on the Sliders' `aria-label`) + the `min-h-[140px]`/`min-h-[180px]` double-declaration whose winner is Tailwind emission order (fragility, currently correct). → **X-W7** riders, all in or adjacent to the carve.
- **XW-34 — MINOR — the worker-seam pair (reader-2 misses, verified):** (a) `QuantizeWorkerRequest` is declared and consumed only by the worker's own `onmessage` annotation — the producer posts an untyped literal (`postMessage` takes `any`), while the response direction is typed end-to-end with three `satisfies`; renaming `pixels` compiles clean and fails at runtime with a raw code the user reads (C-26). (b) Every worker-reported failure ALSO raises an unhandled rejection — `pendingReject` fires into a discarded promise (useExtractSession.ts:153-157, no await/catch) — distinct from C-5's pre-promise throw; the env-noise `pageerror` filter + walk.spec's clean-console assertion would catch it the moment any test drove a failing quantize, which per XW-18 none does. → **NO-WAVE-OWNER** (session cluster; both die in the decode-in-worker + typed-seam cure).
- **XW-35 — MINOR — the camera-face pair (reader misses, verified at the contract level):** (a) the capture chip takes `compact` + a hand-painted face (`bg-on-stage-chrome/20`, 18px glyph), forfeiting the producer's documented coarse-pointer hit-cell guarantee ("≥44px on coarse via the density clamp — hit box ≠ paint box", DockControl.vue.d.ts:14-15 read by this seat) on the component's most phone-shaped surface — the action-row triad correctly takes the default 40×40; (b) glyph and plate are the same token (`--on-stage-chrome` at 20% alpha) over unbounded live video with only a fading veil — content-dependent, uncertifiable contrast that the repo's contrast oracle does not cover, filed as design-risk with token provenance (foundation.css:336-337) so the cure wave measures rather than inherits. Also carried here: reader-1 miss #7 (verified in source) — **opening the camera stacks two specimens** (the viewfinder mounts below a preview `startCamera` never clears, displacing the result column ~208px; no state row for `camera + image` exists in any pass). → **NO-WAVE-OWNER** (camera-mode cluster; the mode-of-the-stage cure owns all three).
- **XW-36 — MINOR — reader-2 miss #2 (verified): the eyedropper's entrance transition never runs and its refit safety net is dead code.** `<Transition name="vj-enter" @after-enter="onTransitionEnd">` with no `appear` over a child with no `v-if` — enter hooks never fire on initial render, so `onTransitionEnd` (:206-208, the `fitToViewport` re-run) never executes; the leave direction is equally impossible (the parent destroys the component via `v-if`). The :2 comment documents motion that does not exist. Consequence bounded (loadAndFit + the ResizeObserver still fit). Alongside: reader-2's dead-returns generalisation (verified for `clientToViewport` and the loupe returns; 8 unread exports across 4 one-consumer composables, extending P3-4's corrected 4-of-19) → **X-W4 addendum** (transition/face) + **X-W8** (G-10 census extension).

### INFO

- **XW-37 — INFO — reader-1 miss (verified): the k-rail gradient is direction-hardcoded (`to right`, useExtractSession.ts:111) while both peer renderings of the same ordered data mirror with writing direction.** Held at INFO because RTL is a documented seam, not a shipped mode (`documentElement.dir` writes = 0; the sole reference is an index.html comment). Recorded beside K-5: pass-1 spent a MAJOR on RTL sliders and got the direction law backwards while this genuine RTL incoherence sat unseen in the same frame. → rides the RTL residue.
- **XW-38 — INFO — the C-23 kill remainder (this seat's framing): ExtractControls.vue:75 pins the kC track to a SOLID ink while requesting `variant="spectrum"`, whose whole contract is "the track carries the data".** A variant-semantics observation, not a defect — the solid pin is the documented, born-RED-certified T-44a cure. → note for the X-W4/W7 face work.
- **XW-39 — INFO — compliance cross-reference, not a row: ExtractPane's `pane-scroll-fade` PRM survival is the sibling's XP-12 (MQ-1, HIGH, quarantine-authored), already owned at X-W10:202.** Carried here only because this corpus's D axis asserted the route motion-clean as a negative proof — true for THIS component's own two Transitions (see superlative 1), false for the inherited header. **Motion-law compliance**: no motion-quarantine FALSE row is carried anywhere in this record; M-33 is cited only in its own direction; MQ-1/M-34 are the carryable survivors and both ride X-W10.

## Killed-claims register

Reader kills re-derived and UPHELD by this seat (evidence at the readers' rows unless sharpened):

- **K-1 — D3-01's #1 cure ("sort by population desc") — no-op**: the sort exists upstream (src/quantize.ts:128, my read); only the tiebreak diverges. Sharpened into XW-5's disposition.
- **K-2 — D3-01's "strip order is arbitrary" garnish**: order is population-descending end-to-end; no third defect, no free win.
- **K-3 — D3-01's "no marker of any kind"**: the weight-sized strip cell IS a population marker (monotonic under the floor — my derivation); the sharp claim is that the marker tracks population while the label tracks population-with-tiebreak, guaranteed to disagree exactly on ties.
- **K-4 — D-9's RTL cure (`dir` through to the Slider)**: canon-violating — VC §5.2 "numeric/scientific sign never mirrors" (re-read by this seat). Diagnosis inverted; the genuine unclaimed defect (container mirrors, axis correctly does not) rides the RTL residue.
- **K-5 — D-10's bare WCAG 2.5.8 verdict**: producer-owned geometry (one-line re-export seam), spacing exception never analysed; the thumb rides XP-8's residue at the producer.
- **K-6 — D-6's "ShadowPalette's header concedes the rule"**: the header draws the distinction it is accused of conceding (:14-26, re-read whole by this seat); pass-3's honest handling governs.
- **K-7 — D-3's tabular-only cure**: DESIGN.md:60-75 names two mechanisms "both required" (re-read); tabular figures cannot make three digits the width of two.
- **K-8 — D2-01's "delete ShadowPalette" cure**: re-litigates the R12 and R7 owner rulings recorded in the artefacts themselves; needs a ruling, not a wave — and X-W7's G20 mass-cut is the ruling-compatible cure that already exists.
- **K-9 — C-30's "the only dismissal mechanism the keyboard has"**: the overlay ships a named native close button (ImageEyedropper.vue:11, re-read); the leak and the missing modality stand on their own evidence.
- **K-10 — D-28's "no accessible name" headline**: `title` is a last-resort accname source; "weakly named" (pass-2 CORRECTION-A) is the accurate word — folded into XW-20.
- **K-11 — L-3's consequence limb**: `libraryPort.createPalette` is the same function object as the store's (usePalettePorts.ts:62/:142, my read); extract loses nothing. Remainder = XW-27 (MINOR).
- **K-12 — pass-2's "sole divergence" DockControl claim (and L-r3 §0's adoption of it)**: refuted by my 19-instance census — 11 title-only across two features, split cleanly by layer. See the disputes table.
- **K-13 — P3-1's "this has happened three times"**: `isLocal: true` = 3 sites, two ARE the store; the registry was written speculatively, two-thirds always dead (my grep; reader-2's `git log -S` dating adopted).
- **K-14 — P3-2's "three features mint ids outside the store"**: same census — one feature.
- **K-15 — P3-4's arithmetic**: 19 returned members, 15 read, 4 dead (my count of the return literal) — not 17/13.
- **K-16 — L-13's prop/emit counts**: 10 props, 16 emits (my count) — both inflated by one; the god-component finding stands.
- **K-17 — C-23 first half (the "solid slab" rail)**: the at-rest value is `var(--muted)` — a pending-substitution shorthand whose longhands serialize empty; the developed state paints the gradient (C-r3's own P4 witness). Residue = pass-1's smaller true finding (the gradient leg is dead pre-image only).
- **K-18 — C-23 second half (kC transparent range)**: producer-declared spectrum-variant behaviour (foundation.css:559-574's byte-level restatement); both sliders opt in. Residue = XW-38 (INFO).
- **K-19 — D-1's pass-1 cure limb (`max-height:100%`)**: inherits the bug's precondition — a percentage max-height against the same indefinite containing block computes to none; the governing D-r3 stage cure supersedes.

Kills/corrections minted by this seat:

- **K-20 — this seat's demotion of P3-2 (MAJOR → MINOR)**: one minting site, zero runtime path, the forged object never stored; rides XW-17's union-tag cure (the XP-27 demotion precedent). Reader-1 carried the MAJOR while its own register accepted the premise's refutation — an inconsistency this record resolves.
- **K-21 — this seat's kill of L-r3 §0's cure position ("three aria-labels; no glass-ui change; no BH relay")**: rests on K-12's refuted premise; the cure is an 11-site convention decision + a producer `label`-prop relay, per the sibling's XP-8 and X-W4's accname law. The withdrawal of the drafted glass-ui finding was right for the wrong reason — the producer's surface is a relay ask, not a masking fallback, when the fork spans three features.
- **K-22 — this seat's correction of reader-1 miss #2's framing ("unrecorded by all three axes")**: true of the corpus, false of the program — X-W7:523-527 already owns the eyebrow-tracking family. Row admitted (XW-32b); framing corrected so the wave does not double-book.
- **K-23 — this seat's kill of the golden-ratio numeric garnishes** (D-19's pass-2 "50/50 should be 61.8/38.2"; D-5's ratio cell): X-W5 item 5 marks the numbers fabricated tolerance; CC-108/A-17 killed the chassis carrying them. Sequence and boundary-inventory limbs survive (XW-14, XW-32f) with the canon-staleness note routed to X-W10.

## Superlatives — provenance verified by this seat

1. **Motion is the healthiest register in the component, proved twice over**: both Transitions key legal house families and no fourth name exists (`vj-enter` :34, `vj-morph` :102 — my read); ShadowPalette's PRM claim is true as written; and the guards are proved in served bytes by the quarantine's §1. D-r3 records it as a negative result and is right to — with XW-39's inherited-header exception honestly fenced.
2. **The certified-ink work carries its own pre-cure measurement in source**: ExtractControls.vue:113-125 derives the track from the live pick certified at `GRAPHICS_CONTRAST_FLOOR` against the exact tier it seats on, names the degenerate fallback, and pins the born-RED numbers ("light 1.88 / dark 1.85 — the owner's 'un-readable' sliders, measured") beside the cure.
3. **A documented refusal of the library's own API**: useExtractSession.ts:7-10 — dominance derived from the RETURNED palette, "the library's `dominantColor()` re-quantizes and is the wrong tool" — verified true by this seat (src/quantize.ts:132-139 is a second full pass at hardcoded k:5). Simultaneously the evidence cell for XW-24: the consumer's comment is the library's sharpest indictment.
4. **The presentation boundary is a real discriminated union**: `PalettePresentation` (:25-27) makes serialization failure a value, checked at :51-64 and folded into the visible register — the same instinct as the library's `Result`, and the standing contrast with the camera path's raw `${err}` one file over.
5. **ShadowPalette gets a hard AT judgment exactly right and says why**: `aria-hidden`, deliberately NO `role="status"`, NO "Loading" — "a shimmering aria-hidden plate does not lie to AT" — with the host seating the readable caption. Refusing the easy wrong label, and documenting the refusal, outclasses most of the roster's defects.
6. **The quantize worker's total error boundary**: quantize-worker.ts:20-44 (read whole) — try/catch around the entire handler, typed `satisfies` responses for both the Result-fail and throw paths. The worker is better defended than its consumer (XW-34's rejection leak is the consumer's).
7. **Published-surface discipline, structurally guaranteed**: nine `@mkbabb/value.js` imports, three specifiers, all real exports keys; zero `src/` reaches; vite generates the demo alias set FROM the exports map so an unpublished subpath cannot resolve.
8. **The adversarial record audited itself**: two genuine self-corrections against own earlier work (L-r3's §0 withdrawal — premise later refuted, but the act was real; C-r3's CORRECTION-C overturning its own pass-2 negative on measurement) and one seat (D-r3) that filed two rows then spent its sections confirming other seats' numbers and recording negatives. This seat's censuses subsequently corrected three of their counts and one premise — which is the system working, not failing.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **D2-23 — viewfinder `object-cover` ≠ full-frame capture (~43% of the captured frame never shown)**: source-entailed and deterministic (:44 vs :270-272), never rendered — no probe environment in any of nine passes had a real camera grant. With XW-35's capture-chip contrast (~30px measured cell; glyph-over-video contrast) and any real-hardware nuance of XW-9's double-start beyond the emulated `streamCount: 2`.
2. **Forced colors**: the probe matrix proves nothing by construction (webkit launch; `forcedColors: "active"` applies no UA overrides — every colour byte-identical to desk-light in the probe JSON). The static half (no `forced-colors` query in the subtree's five files; the ghost's information rides `background-color` alone) is verified; the rendered consequence needs a Chromium/WHCM arm.
3. **The whole RTL family**: unshipped mode (zero `documentElement.dir` writes; D-r3's own RTL arm failed to take). Includes K-4's substituted defect (the container mirrors while the axis lawfully does not — label lands opposite the fill's origin) and XW-37.
4. **C-34 — dragleave strobing**: no depth counter is source-true; whether the false state is one frame or a visible flicker was never reproduced (its own author labels it hypothesis).
5. **Zoom-200 clipping** (D-r3 only instrument: `code.fira-code` clipped in all four state arms; the pane chassis joins at zoom-200): WCAG 1.4.4 consequence sound if the measurement holds; needs one confirming instrument — an X-W1 golden input alongside XW-18's matrix absence.
6. **XW-21's photograph**: the blank pinned loupe on a real touch device (mechanism source-certain; the witness needs a touch-emulated run).
7. **D2-01's exact magnitude**: cite the range 95.3–97.0% (two instruments, two engines), and the k-driven arm grows without bound (empty-k16 ghost h 278) — a cure wave should gate the range, not a decimal.
8. **C-22 — the isProcessing latch** under a contended server: mechanism source-true (no timeout/cancel/identity), reproduction contaminated by sibling seats; note XW-2 means the sibling's XP-4 poisoned-worker arm produces the same permanent skeleton by a proven path.
9. **Coverage note**: of the 36 rows, today's suite can see only the two o18 contrast legs — the census that keeps XW-18's flow spec the record's highest-leverage artifact.

## DISSENT / method notes — preserved, not ruled away

1. **Both re-reader rounds were strong and complementary**: all 19 kills survived independent re-derivation; 11 of 12 admitted misses survived attack (one demoted, two reframed). Their shared blind spot repeats the sibling record's exactly — **neither read the X wave files** — so one "unrecorded by all axes" row was already program-owned (K-22), one killed cure was already superseded by the program's own (K-8/XW-23), and the governing L-r3 file still carries a cure position built on a premise its own re-reader had refuted (K-21). The standing law extends: *a reader's return must be re-derived against the program's own formations, not only against the tree.*
2. **Reader-1's inconsistency, recorded**: it carried P3-2 at MAJOR in its confirmed list while its killed register adopted the refutation of P3-2's load-bearing sentence. Resolved here by K-20; recorded because a merged return that disagrees with itself is exactly what the apotheosis seat exists to catch.
3. **Severity instinct, unresolved and preserved**: a case exists that XW-1 (the self-disagreeing instrument) and XW-5/XW-6 (the unbound/contradicted readouts) are one identity — "the instrument lies about its own data" — with three witnesses. Kept at the readers' grain so each mechanism keeps its own witness; the next formation may merge without loss (ids preserved).
4. **What this seat could not do under the commission**: no browser, so no residue row moved to CONFIRMED here. Every disposition above is decidable from source + the tree + the program's own records, and was.

## Closing verdict

**ADJUDICATED RED — the corpus's direction confirmed on every axis; its cures corrected in five places; its two sharpest new rows were the readers', verified here.** 36 rows survive (3 BLOCKER · 17 MAJOR · 13 MINOR · 3 INFO): 24 corpus claims confirmed (8 cross-booked identities with wb-extract-pane, honoured, never re-minted) + 12 reader-found admissions (2 reframed). 23 killed claims with written refutations — 19 reader kills upheld, 4 minted by this seat, including one against the governing challenge file's own cure position. Terminal dispositions: **14 rows to X-W7** (the modify-carve plus its explicitly pre-owned cures: format-color, PaletteSpecimen, G20's plate mass + caption deletion, the eyebrow family — three of them flagged addendum-candidate where the cure exceeds the declared carve: ImageDropZone geometry, utils.ts's sentinel registry, the `?? ''` mask), **4 to X-W4** (the accname fork = XP-8, the sampler keyboard path, the loupe, the eyedropper transition — two as addendum candidates beyond the "face only" carve), **2 to X-W5** (the KeepAlive contract = XP-1's addendum, the narrow sequence), **3 to X-W8** (dead camera copy = XP-13, dual import routes = XP-11, the dead-returns census extension), **1 to X-W9** (`dominantColor`), **1 to X-W10** (`.plate-ink` = XP-19; plus the canon-staleness notes riding XP-25's correction), **1 to X-W1** (the flow spec = XP-7 — still the single highest-leverage artifact), **2 already-FOLDed identities** (lint-glob liveness; tsconfig parity), and **8 to NO-WAVE-OWNER** across two named clusters: the extract-session correctness cluster (XW-4/XW-10/XW-19/XW-22/XW-34, joining the sibling's XP-2/XP-3/XP-4/XP-23) and this record's **camera-mode cluster** (XW-8/XW-9/XW-35 + the teardown half already at X-W5) — the camera that cannot be cancelled, cannot be safely reopened, files its faults in a foreign channel, and stacks two specimens on one stage lives in code no X wave's declared scope owns, and that cluster, beside the sibling's session cluster, is this apotheosis's headline for the next formation boundary.

— end of apotheosis. The challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
