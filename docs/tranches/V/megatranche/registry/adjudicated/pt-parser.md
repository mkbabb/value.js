claude-fable-5

# pt-parser — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ master `ef10d5b` (re-verified this session; dirty rows are `rust/**`, `README.md`, `.cargo/config.toml`, `.gitmodules`, `docs/**` — `typescript/` has **zero** dirty rows) · `typescript/src/parse/parser.ts` = 711 lines, read **whole** by this seat · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two fresh reader returns over the module's two challenge files — reader-α (`L-18-hostile-reread-parse-that-parser-library-correctness`: 33 confirmation cells · 14 kills · 10 misses · 8 superlatives) and reader-β (`L-18-hostile-reread-parse-that-parser-consumption-api`: 27 confirmation cells · 7 kills · 8 misses · 10 superlatives). Both challenge files (`audit/parse-that-modules/parser/challenge-L-library.md`, 634 lines; `challenge-C-consumption.md`, 838 lines) were re-read **whole** by this seat.
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes before adoption. Nothing in parse-that was armed (no `enableDiagnostics`, no `memoize`, no `mergeMemos`); **zero parse-that code was executed by this seat** — every adjudication below is static (read + grep + artifact byte-reads); the readers' probe outputs are corroboration only. One consequence-cell that **both readers and both challenges** carried was corrected by this seat's own derivation (ruling 1). Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (all read-only)**: `parser.ts` whole (711 — every cited anchor checked: `:74` `return state`, `:61-66` errorState build/assign, `:67-69` the console gate, `:82-97/:190-205/:213-227` the then/skip/next triplet, `:131-138` chain, `:146-154` map/mapError, `:162-187` mapState + `Object.create` at `:177-179`, `:251-303` not/negate (merge-free arm `:261-265`; `parser ? not : negate` at `:300` with no type argument), `:309-331` minus (merge-free arm `:314-319`; **value leak** — `:313` runs `excluded`, the failure arm never touches `state.value`), `:339-357` peek (`:350` direct write), `:392-431` wrap (three failure arms; **only** `:417-423` slices, at `:419`), `:437-478` call + FLAG tier, `:480-521` trim (`:488` name gate, `:492-496` discarded flaggedParser, sole `.flags =` at `:496`, `:520` wrap fallback), `:523-636` many/sepBy (`:526/:572` `new Array(est)`, `:538/:605` zero-width guards, `:555/:628` `unsafeSetValue([])`), `:638-642` eof context clobber, `:653-688` recover, `:690-696` debug (`logger = console.log`), `:702-707` lazy, `:711` `_initWhitespace()`); `utils.ts` whole (186 — `:6-18` flag trio **including `disableDiagnostics` at `:12-14`**, `:28-49` mergeErrorState (`:33` gated, `:34-35` ungated), `:71` reporter early-return, `:95` the module-global buffer, `:102-128` collectDiagnostic (**spreads `suggestions`/`secondarySpans` at `:120-122` with NO flag guard**, `:127` → `:131-136` `furthest = -1`), `:138-148` get/clear/pop); `state.ts` whole (189 — `:2` **value import of `statePrint`** — the independent printer edge, `:43-53` constructor field initializers, `:86-99` the three choke points, `:101-109` **clone()** (copies 5 fields; `expected`/`suggestions`/`secondarySpans` reset by construction while `furthest` is preserved), `:111-134` the 0-based/1-based line-number split, `:136-138` toString, `:141-171` parserNames (`"opt"` `:163`, `"eof"` `:164`)); `debug.ts` whole (383 — `:12` debugDepth (`++` `:358`, `--` `:379`, **no try/finally** around `:361`), `:141-190` statePrint (offset only; `furthest` grep = `:202/:207`, both on the Diagnostic printer), `:245` PARSER_STRINGS, `:261-343` parserPrint (destructure `:261`, `"unknown"` `:262-265`, `default: undefined` `:330-331`, **`throw new Error("parserPrint: missing parser context name")` `:336-338`**, id-0 truthiness `:320/:339`, lazy arm `:316-327` → `getLazyParser` `:318`), `:351-383` parserDebug (**default `logger = console.error` at `:355`**)); `lazy.ts` whole (43 — the **two disjoint caches**: WeakMap `:5-15` (fn re-run on miss `:12`) vs closure memo `:18-24`); `leaf.ts:1-25` (eof `:13` compare, `:16` merge), `:40-80` (any arity-2/general — restore **then** unlabelled merge `:54/:69`), `:150-273` (all arity-1 short-circuit `:161`; drop sites `:199/:207/:209`, `:230/:238/:246/:248`, `:267/:270`; packed literals `:191/:222` vs `new Array(n)` `:257`), `:280-369` (string fast paths + labels; **regex EOF bail `:327-330` with no merge**, empty-match `:355`, pattern-miss merge `:360`), `:372-399` (trimStateWhitespace; `export let whitespace` `:395`, `_initWhitespace` `:396-399`); `packrat.ts:60-105` (getCijKey + budget throw `:90-98`), `:150-300` (latch `:156`, epoch `:216-231/:243-250`, resetPackrat `:262-272`, **`PACKRAT_ARMED = true` at `:290`** first statement of makeMemoized, applyAnswer `:256-260` and evalParser `:295-299` direct `state.value` writes), `:340-407` (eval-set re-entry `scratch = live.clone()` `:351-355`; growLR try/finally `:390-406`); barrels whole: `index.ts` (mergeErrorState + enable/disable at `:5`; `export *` `:14`; **`isDiagnosticsEnabled` absent**), `core.ts` (the three-denial docblock `:3-6`), `diagnostics.ts` (**`isDiagnosticsEnabled` absent**); gates: `proof-no-dead-combinator.mjs` (two-name `banned` list; dispatch vacuity guard; three-tree sweep), `test/subpath-gate.mjs` (existence + `typeof` only), `test/manifest-gate.mjs:37-41` (Gate 2); `test/debug.test.ts:105-120` (the vacuous flag test, verbatim); dist: `parser.d.ts` whole (**`:12` `parse(val: string): T`**, `:21` `not → Parser<string>`, `:8` flags, `:46` call, 24 public methods), chunk layout **`class ParserState {` at `:286` / `class Parser {` at `:854`**, `head -285 | wc -c` = **9,359**, bytes **1,336 + 40,576 + 3,516 = 45,428**, `jsonParser|csvParser` grep over the `/core` graph = **0/0/0**, `PACKRAT_ARMED` dist rows `:678/:682/:714/:722`; `diff -rq` band-installed `@mkbabb/parse-that/dist` vs `typescript/dist` → **zero differences** (ANCHOR-A upheld); casts re-counted **exactly**: `as Parser` 91 occurrences of which `as Parser<` 37 · `as ParserState` 37 · `as ParserFunction` 17 (sites incl. the corrected `:387`) · `as unknown as` 5; censuses re-run: `.flags =` → `:496` only · `Parser.call(` → `state.ts:93` + `parser.ts:493` + **`test/debug.test.ts:115`** · `unsafeCall` callers **0** · `mapState/minus/peek/lookAhead/debug/recover/not` → **0/0/0** over pt src (excl. parser.ts)/test/scripts · `this.state` → `:66` (write) `:68` (**read**) `:71` (write) · ParserState `err/save/restore/getColumnNumber/getLineNumber` → **0**, `.from(` → Array.from only; tsconfig: `strict` only, **no** `noUnusedLocals`/`noUnusedParameters`; band: `idiom/example.ts` `resilientArguments = calcValue.trim().recover(regex(/[^,)]+/), INVALID_ARGUMENT).sepBy(comma).wrap(...)` + `example.test.ts:248-271` **two passing assertions over `getCollectedDiagnostics()` with diagnostics OFF** (`enableDiagnostics` in the band = 4 doc comments + one **arity read**, `parsethat-surface-gaps.mjs:26` — never a call); subpath importers = **3** (`parsethat-surface-gaps.mjs:13` /diagnostics, `fixtures/g16-replay.test.ts:22` + `g16-accepted.ts:1` /core); band census **with declared scope+filter+unit** (see ruling 7); value.js: `src/css/types.ts:10-24` ParseIssue (8-code union incl. `"trailing_input"` `:13`; `expected` `:22`) + failure arm `:25-27` non-empty tuple; `src/css/index.ts` = 60 lines; `trailing-content` grep → **exactly** `state.ts:26` + `parser.ts:470`; sibling-roster identity scan: pt-core rows 3/5, pt-diagnostics rows 2/3/4/6/7/9/10/11/14 (+ its live P3/P4/P5 witnesses), pt-debug B-3/M-4/m-8/m-10/MISS-10, pt-lazy LZ-1/LZ-2/LZ-17/M5, pt-leaf PT-LEAF-1/-2/-3/-5/-7/-14/-15/-16/-20/-21/-25/-27/-28, pt-index, pt-packrat C-7.
**STOP/law compliance**: `/Users/mkbabb/Programming/parse-that-css-totality-p2` → `No such file or directory` (re-verified this session; not created). parse-that touched read-only, main checkout only. **Sole write = this file.**

## Scoreboard

**41 adjudicated roster rows — 5 BLOCKER · 12 MAJOR · 19 MINOR · 5 INFO** (36 defect rows + 5 INFO), under a routing law where **nine rows are cross-registry identities** (anti-rename: booked once at their primary roster, carried here by reference). **20 killed-claims register entries, all SUSTAINED — including one kill that is this seat's own and lands against both challenges AND both readers** (ruling 1: the memo-key-perturbation consequence). **17 distinct missed rows ADMITTED** (10 from reader-α, 8 from reader-β, one duplicate): **8 become NEW roster rows**, 9 fold into existing identities here or in sibling rosters — four of which are re-discoveries of rows already adjudicated at pt-leaf/pt-lazy/pt-debug/pt-diagnostics, caught by this seat's identity scan. **11 superlatives sustained.**

Blocker-weight survivors of record: **PT-PARSER-1** (`.parse()` returns a stale, type-plausible value on failure, published as `parse(val: string): T` — the library's ergonomic front door is unsound at both the runtime and the 1.0.0 `.d.ts` layer) and **PT-PARSER-3** (`recover()`'s positional pop over a module-global buffer — and it is **LIVE**: the band's only real parse-that consumer wires `.recover()` and asserts `getCollectedDiagnostics()` unarmed). Realized in-constellation impact today is zero at every hop (value.js does not consume the package — the consume edge is the band workspace, pinned 1.0.0, byte-identical dist); every blocker is a defect of the **published 1.0.0 contract** and of the **X·P adoption premise**.

## The incredulity seat's own work — ruled disagreements and corrections

1. **The memo-key-perturbation consequence — KILLED BY THIS SEAT, against the challenges AND both readers (SEAT-K1).** C-B3(a) ("a Parser's memo identity depends on how many Parsers the process constructed before it"), C-M3(iii) ("bumps the global `PARSER_ID` and therefore *shifts every subsequently constructed parser's packrat memo key*"), and reader-α's lazy-print miss ("thereby shifts every later parser's packrat memo key") all carry the same consequence cell — and it is **memo-inert at the bytes**. `packratEnter()` (packrat.ts:216-231) runs on **every** `parseState` (parser.ts:43) and, armed, **installs fresh empty tables**; `packratExit` (:243-250) restores the parent's. Memo cells therefore live exactly one top-level parse: keys need uniqueness only within an epoch, and `id` is unique per instance by construction. Construction-order id drift changes the key *value*, never its uniqueness, and no cell survives to be mis-hit. **Surviving consequences, re-scoped**: (i) the `MEMO_MAX_ID` budget RangeError at ≈2.1M constructions (packrat.ts:90-98) — real, and the lazy-print id-doubling (pt-lazy LZ-1) burns it; (ii) the O-8 posture breach as such; (iii) the dual-package twin counters (pt-core row 5); (iv) printer identity staleness (pt-debug B-3/m-8). **Honest bound**: the derivation holds for the supported `parseState` entry; a consumer driving a memoized parser via raw `.parser()` on a hand-built state opens no epoch, and the hazard revives — recorded as a W2 entry-discipline clause, not as the incumbent's defect. This re-grades C-B3 BLOCKER → **MAJOR** (PT-PARSER-6) and strikes the AC-3 "scan-time construction perturbs memo keys" hazard down to budget-burn.
2. **The recover-path liveness contradiction — RULED for reader-α; reader-β's cell corrected (SEAT-K2).** Reader-β's L-M1 confirmation carried the challenge's "utils.ts:121-122 (the recover path only — dead, L-M11)"; reader-α killed it. At the bytes: `idiom/example.ts` builds `resilientArguments` on `.recover(regex(/[^,)]+/), INVALID_ARGUMENT)` and `example.test.ts:248-271` runs two **passing** vitest assertions over `getCollectedDiagnostics()` — with diagnostics OFF (the band's only `enableDiagnostics` touch is an arity *read* at `parsethat-surface-gaps.mjs:26`, plus doc comments). `collectDiagnostic` (utils.ts:102-128) has **no** `diagnosticsEnabled` guard and spreads both arrays at `:120-122`. So the recover path is live, unarmed, and asserted; L-M1's "write-only" premise is dead; and L-B3's severity **raise** (the challenge's own falsifier called it unreachable) is sustained. Convergent with pt-index's C-B1 overrule (21 band files import the package) and pt-diagnostics' live P5 witness.
3. **L-B4 grade — the readers split (α: MAJOR-lowered · β: BLOCKER). RULED MAJOR at this module; β's dissent preserved.** Grounds: the arming-at-construction posture is *declared* twice (packrat.ts:284-289 verbatim; CHANGELOG's "the latch never disarms" note reader-α cites), the 1.47× magnitude is O-15's N=1 and stays MEASURE-AT-OPEN, no live consumer constructs a memoizer today, and ruling 1 removes the correctness limb — what remains is a declared, unmeasured, cross-module perf posture. The **harness law lands regardless of grade**: W1 G-4's predicate becomes "no memoizer was **CONSTRUCTED**" (the corpus-new half, right in both readers). Primary latch identity: **pt-packrat**; this roster carries the parser-side facts (parser.ts:43 unconditional entry; null-latch fast path).
4. **The C-B1 sub-claim kills — BOTH SUSTAINED at the artifact by this seat's own bytes.** (a) Printer-tier provenance: `state.ts:2` is a **value** import of `statePrint`, load-bearing for `ParserState.toString()` (:136-138) — ParserState is a first-class `/core` export, so the §7 rows 19/20 disposition (retire `debug`/`toString`) removes **zero** bytes of the ANSI/printer tier from the `/core` graph; `head -285 | wc -c` = 9,359. (b) Chunk layout: `class ParserState {` at dist `:286`, `class Parser {` at `:854` — the "Parser does not begin until :286" sentence overstates the printer tier ~2×. (c) β's K-3: the third denial of core.ts:3-6 is TRUE (`jsonParser|csvParser` = 0 hits across all three `/core` chunks) — "/core loads the entire library" must not propagate into an X·P footprint number. The **parent blocker survives whole** (packrat + diagnostics statically linked; 45,428 B exact; gate has no exclusion clause) and its primary booking is **pt-core row 3** — cross-booked here, never re-booked.
5. **The dead-flag-tier bundle — kills sustained; identity ruled to pt-leaf PT-LEAF-20.** `Parser.call()` has a **third** live caller (`test/debug.test.ts:115` — C-M3's "no live caller" and C-M5's `call | pt test 0` cell are both false); it exercises only the `:438-440` fast path (`pEof.flags === 0`; sole `.flags =` is `:496` on the discarded flaggedParser), so the non-zero-flag branches stay unreachable and the test is **vacuous coverage** — the comment claims the EOF flag path, the asserted label comes from `leaf.ts:16`. The trim two-ids claim is whitespace-branch-only (`:488` gates; `:520` allocates one Parser). All previously adjudicated at pt-leaf (PT-LEAF-20, kill K4 there); this corpus's independent convergence is recorded, the published-contract limb (`dist/parser.d.ts:8` `flags`, `:46` `call`) books here as PT-PARSER-13.
6. **Cross-registry identity sweep (anti-rename) — four reader "misses" are re-discoveries.** (i) regex-EOF-no-merge + the whitespace-vs-`trimStateWhitespace` divergence = **pt-leaf PT-LEAF-3** (both readers' filings sharpen it: the expected-set silently omits regex terminals at truncation points — folded as a rider there, and into PT-PARSER-12's conservation law). (ii) The lazy print-re-runs-the-thunk row = **pt-lazy LZ-1** (already demoted BLOCKER→MAJOR there with the sharper trigger bound: explicit stringification only, NOT default `.debug()`; the id-burn consequence now reads through ruling 1 as budget-burn). (iii) PARSER_STRINGS/debugDepth = **pt-debug m-8/M-4**; the id-0 printer truthiness = **pt-lazy M5** (with its own bound: `whitespace` consumes id 0 at module eval). (iv) The `getCollectedDiagnostics` live-handle/rebind pair + the unexported `isDiagnosticsEnabled` = **pt-diagnostics rows 10/11/14**. Each is carried by reference; nothing is double-booked. Reader-α's "six process-global mutables" correction of L-B3's "three, not one" is SUSTAINED with the note that #5/#6 are pt-debug's rows.
7. **Census portability — both readers cautioned by this seat's own instrument variance.** α measured 111 `.trim(` / 54 `.parseState(` / 26 `.parse(` over the five candidate dirs; β measured 65/29 band-wide; the challenges wrote 95/72/33. This seat re-ran both scopes and got α's 54/26 exactly — and `.trim(` = **126** over the five dirs *without* a file-type filter (markdown hits) vs **95** band-wide `*.ts`+`*.mjs`. A subset scoping *above* its superset is proof the numbers are filter-artifacts. Register law (extends pt-leaf ruling 6): every census states **scope + file filter + counting unit** beside the pattern, or the number is not portable. The direction (`parseState` ≫ `parse`) holds under every instrument.
8. **Severity contests inherited from the challenge pair — ruled.** map(mapError) L:MAJOR vs C:MINOR → **MINOR** (nothing reaches it by accident; zero consumers). L-M1/L-M2/L-M3 → **MINOR** each (reader-α's kills verified at the bytes: the reader set includes unguarded `collectDiagnostic`; only the close-delimiter arm slices; one ParserState-per-root holding a reference). C-B3 → **MAJOR** (ruling 1). L-B3 → **BLOCKER raised** (ruling 2).

**Adjudicator's corrections register**: (1) both challenges + both readers — the memo-key consequence cell (ruling 1); (2) reader-β's "dead recover path" cell (ruling 2); (3) both readers' census magnitudes stated without filters (ruling 7); (4) reader-α's lazy-print miss filed as corpus-new when pt-lazy LZ-1 already adjudicated it — citation-independence confirmed but registry-awareness failed; identity guard applied (ruling 6). Per the ColorSpaceSelector precedent these are fleet evidence; the readers' disagreements were exactly where a third instrument was needed.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/corpus/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec · **NO-WAVE-OWNER** for incumbent parse-that repairs (owner-held O-15 channel, **semver-classed per pt-leaf PT-LEAF-27**: 1.1.0-eligible vs 2.0.0 event). X·P executes nothing until the owner's begin-word; every routing is a booking. Line anchors are `ef10d5b`; re-resolve before any cure (D-19). Rows marked **[identity → …]** are booked ONCE at the named primary roster.

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-PARSER-1** | L-B1 / C-M8 + β-miss 1 | `.parse()` returns a **stale, type-plausible value** on failure — `"a"` for `string("a").then(string("b")).parse("ax")`, `[]` for `many(min>0)` — because `parseStateInner` returns the working `state` (:74), not the errorState built at :61-66, and no failure path clears `value`; AND the shipped 1.0.0 declaration is **unsound**: `dist/parser.d.ts:12` `parse(val: string): T`, non-optional, so under strictNullChecks `const c: CssColor = root.parse(bad)` type-checks. Cannot express value.js's `ParseResult` failure arm (src/css/types.ts:25-27, non-empty tuple). Root erasure: `state.ts:49` `value: T = undefined as T`. The band's `.parse()` ban (parser-band.md:108, G8 :134) is a HAND gate doing the type system's job | parser.ts:74/:77-79/:82-97; :555/:628 (the `[]` written INTO the error state); dist/parser.d.ts:12 re-read byte-identical in the installed tarball; 55 `.parse(` test sites, none asserting a failure return | **W2** — `parse` RETIREs (C §7 row 16 sustained); the successor entry returns a typed result. **REFINEMENT FOLD → W1**: the JS-boundary invariant is `parseState` + `isError` only, with this row as the written reason (same law as pt-leaf PT-LEAF-16 — **one identity, primary HERE**: the mechanism is parser.ts:74). Incumbent d.ts truth-telling (`T \| undefined`) = NO-WAVE-OWNER, **2.0.0** |
| **PT-PARSER-2** | L-B2 / C-M9 | The non-string boundary is a **three-way split** — throw (`charCodeAt`/`startsWith`/`substring`) / silent failure (`eof`: `0 >= undefined` → false) / **silent success** (`regex(/a*/)`: `test()` coerces the number, empty-match arm returns `isError:false`) — contradicting the corpus premise (O-15 PT-07 "5/5 throw") that the debt-4 guard was specified from. No `typeof` anywhere on the parse path; a consumer try/catch protects against neither non-throwing regime | parser.ts:34/:52; state.ts:47-53; leaf.ts:13, :327, :344, :355-357 (each re-read); `grep "typeof.*string" src/parse/*.ts` → nothing | **REFINEMENT FOLD** — W1 §3.9's boundary invariant asserts a **typed failure**, never "it throws"; the corpus gains the eight-probe row; W2's boundary clause = positional `typeof` at entry. **[identity → pt-leaf PT-LEAF-5** for the leaf postures; the unguarded entry books here**]** |
| **PT-PARSER-3** | L-B3 / C-M4 / L-M11-recover-half (killed) | `recover()` pushes into a module-global `Diagnostic[]` with **no identity on the record** and pops **positionally** on sync failure (:674 → `collectedDiagnostics.pop()`) — a nested successful recover inside a failed `sync` pops the WRONG record; the buffer is outside the packrat epoch (PackratEpoch has five members, not six); and the pop path leaves the outer parse's `furthest` **destroyed** (`resetErrorState` → `-1`). **SEVERITY RAISED vs the filing — the combinator is LIVE**: the band's sole real consumer wires it and asserts the buffer unarmed (ruling 2); the challenge docs contradict each other on this (L: "zero callers in the entire repository"; C-M5: band 1) | parser.ts:653-688 (:666/:670/:674); utils.ts:95/:115/:127/:131-136/:146-148; packrat.ts:196-202; utils.ts:20-26's own reentrancy law as the in-file contradiction; pt-diagnostics **P3/P4/P5 live witnesses** (wrong-pop journal=1; furthest 2→0; unarmed growth) | **W2** — R-LAW-4/O-8: `D` is a per-parse append-only **value** of the parse; **W3.b** re-authors recovery (mark/rewind snapshots the four state fields, not just journal length). **[identity → pt-diagnostics rows 2/7/9/10/11** own the buffer/reset/handle; the parser.ts combinator mechanism books here**]**. Incumbent NO-WAVE-OWNER, **2.0.0** |
| **PT-PARSER-4** | C-B1 (narrowed per ruling 4) | The subpath export map is not a zone map: `/core` statically links the packrat tier AND the diagnostics module — parser.ts:4 (six diagnostics symbols) and :7 (packratEnter/Exit) make it unshakeable by construction; `dist/core.js:1` → packrat chunk `:1` → diagnostics chunk; 45,428 B exact; the O-15 latch rows sit inside the `/core` graph (dist :678/:722); core.ts:3-6 and CHANGELOG r6 #6 both contradicted. Narrowings bound in: json/csv truly absent (0 hits — the third denial is TRUE); the printer tier is ~9,359 B reachable through `state.ts:2` regardless of any method retirement | this seat's byte re-reads (ruling 4); test/subpath-gate.mjs = existence + `typeof`, no exclusion clause | **[identity → pt-core row 3 — PRIMARY THERE**, BLOCKER, cross-booked; pt-ansi row 11 the other cross-book**]**. W4 RC-P chunk topology + the W1/D-17 module-graph gate; W2 owns the import-discipline successor |
| **PT-PARSER-5** | C-B2 / L-M7 (narrowed per β-K4) | The sole entry point writes `console.error` when armed (:67-69, the only gate — no sink, no else-arm), and `state.expected` is **unobtainable without arming** (utils.ts:33/:38) — no configuration yields structured expectations without console writes; R-LAW-3 unpassable by construction for any source-direct lowering. Narrowing bound in: `disableDiagnostics` EXISTS (utils.ts:12-14, both barrels) — the flag is two-way, unlike the latch; the absorbing-state framing must not extend here by analogy. Aggravation: the library ships **two disagreeing sink defaults** (parser.ts:693 `console.log` vs debug.ts:355 `console.error`) for the one thing it parameterized | all cites re-read at source + dist :881-882; `isDiagnosticsEnabled` exported from NEITHER entry (pt-diagnostics row 14) | **[identity → pt-diagnostics row 4 — PRIMARY THERE** (= O-15 PT-01 = pt-core row 9)**]**. W2 R-LAW-3 (diagnostics are values, never effects); W3's "labelled diagnostics without arming" condition is the named discharge; the sink-disagreement limb = pt-debug m-10 |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-PARSER-6** | C-B3, **re-graded BLOCKER→MAJOR by SEAT-K1** | `PARSER_ID` (parser.ts:18/:25) is a process-global mutable minting every Parser identity, input to the memo key (packrat.ts:99) and the budget throw (:90-98); `this.state` (:26) is a public mutable field written every parse (:66/:71), read only inside the console branch (:68), retaining the last input's whole `src` per root Parser for process life. **Correction of record (ruling 1)**: the memo-key-perturbation consequence is memo-inert — epoch-fresh tables per top-level parse; surviving limbs are the O-8 posture, the ≈2.1M id budget, the ESM/CJS twin counters, and printer identity | .state census 66/68/71 (challenge's "exactly one hit" killed); dist/parser.d.ts:6-7 publish both fields; resetPackrat touches neither | **W2** — O-8: identity becomes a construction-scoped parameter; `this.state` RETIREs (C §7 rows 21/22). W2 rider (this seat's): the entry-discipline clause covering raw-`.parser()` driving. Incumbent NO-WAVE-OWNER, **2.0.0**. Dual-package limb **[identity → pt-core row 5]** |
| **PT-PARSER-7** | L-B4 (grade ruled 3; β's BLOCKER dissent preserved) | The packrat latch arms at **construction** — `PACKRAT_ARMED = true` is makeMemoized's first statement (:290) — one-way, process-wide, cross-module; `parseState` calls `packratEnter()` unconditionally (:43); `resetPackrat` never disarms (:262-272). Declared posture (packrat.ts:284-289; CHANGELOG), magnitude unmeasured | grep exhaustive: four source rows + four dist rows, no assignment back | **REFINEMENT FOLD → W1 G-4**: the predicate is "no memoizer was **CONSTRUCTED**" (the corpus-new half, both readers right). Blast radius = MEASURE-AT-OPEN #1. **[identity → pt-packrat — latch primary there]** |
| **PT-PARSER-8** | C-M1 + C-M6 / L-m7 | `not()` publishes `Parser<string>` regardless of `T` in the shipped .d.ts (:21) — runtime value is `T` (:260/:286) — because `new Parser(parser ? not : negate, …)` (:299-302) has no inference source: `ParserFunction<T>` (:13-16) declares `T` and never references it, so all **17** `as ParserFunction<X>` casts assert nothing and the class default `Parser<T = string>` wins. The eslint justification ("any required for variance") is self-refuting | dist/parser.d.ts:21 vs threading siblings :26/:33/:40; cast census re-counted exact (91/37/37/17/5 — the challenge's 86+37 double-count killed) | **W2** — type-soundness law: the algebra's function type carries `T` on both sides; casts a counted budget. **[identity → pt-leaf PT-LEAF-15** for the phantom; the not() artifact lie + census book here**]**. d.ts repair 2.0.0 |
| **PT-PARSER-9** | L-M8 / C-M10 | `parseState()` returns the state at the **backtracked** offset while the correctly-positioned error view is stashed on the public mutable `this.state` — `statePrint` renders from `state.offset` and never consults `furthest` (grep: :202/:207 only, both on the Diagnostic printer). The natural idiom `if (s.isError) s.toString()` renders the cursor at the wrong position; the correct render is reachable only through the field row 6 condemns. Shares the :74 root with PT-PARSER-1 | parser.ts:60-66 vs :74; state.ts:136-138; debug.ts:161/:168 | **W2** — the entry returns the error view (one object, one truth); **NO-WAVE-OWNER** one-line incumbent (return errorState) — **2.0.0** (it also changes `.parse()`'s failure return) |
| **PT-PARSER-10** | L-M9 / C-M11 | No recursion ceiling anywhere: `Parser.lazy` arity-1 (:702-707), `createLazyCached` a bare closure memo (lazy.ts:18-24), no depth/MAX_ anywhere, the only try in parser.ts is the epoch's finally; exhaustion crosses the public boundary as a **throw** (O-15 PT-04: 7,761/7,762 — cited, correctly not re-measured). No grammar can bound its own recursion while the signature is arity-1 — partially adjudicates the band's try/catch DISSENT (parser-band.md:142) in cand-O's favour at the library altitude | greps re-run; W2 K-7's shield note (W2.md:738) verbatim | **W2** — depth bound as an algebra parameter; W1 G-9 owns the ceiling row (MEASURE-AT-OPEN #5) |
| **PT-PARSER-11** | C-M5 + L-M11-mapState + α-miss 7 | **Dead published surface at both classes**: six of 24 Parser methods with zero call sites in pt src/test/scripts (`mapState`/`minus`/`peek`/`lookAhead`/`debug`/`recover`-in-pt — census re-run 0/0/0; `.not()` is band-exercised, untested in-tree) **plus six ParserState members** (`err`/`from`/`save`/`restore`/`unsafeCall`/`getColumnNumber`/`getLineNumber` — 0 callers re-verified) ≈ **12 of 38 published members dead**, with a bonus convention split: `getLineNumber` 0-based first line vs `getLineAndColumn` 1-based, one class, undocumented. The dead-surface gate is a hardcoded two-name ban list with a vacuity guard for its own grep and none for its own coverage | proof-no-dead-combinator.mjs:29-32 re-read; dist/parser.d.ts + dist/state.d.ts publish all | **W2** — operators enter the algebra **specified, never inherited** (C §7 rows 11/17 sustained); **W4 rider**: release gates enforce standing properties, not enumerated corpses (shared with pt-leaf PT-LEAF-18/-20). Retirements 2.0.0, O-15 channel |
| **PT-PARSER-12** | C-M7 + L-m2 (sharpened) | The two boundary combinators contribute nothing to error position or expected-set: `not()`'s negate arm (:261-265) and `minus()` (:314-319) fail with no merge, no label — not curable from inside (the failure cause is a sub-parser SUCCEEDING, so nothing merged anywhere); the disciplined siblings all merge (8 sites re-read). Sharpening sustained: `or()` (:105-116) has no exhausted-path merge while `any()` does (leaf.ts:54/:69) — and any's merge is UNLABELLED and fires after the offset restore, so when `furthest < savedOffset` (reachable via merge-free failure arms incl. regex-at-EOF) it takes the utils.ts:29 branch and **CLEARS** the accumulated expected-set: the divergence can destroy diagnostics, not merely omit them | all arms re-read this session; ParseIssue.expected = src/css/types.ts:22 has nothing to carry | **W2** — labelled zero-width failure family (band debt 1) + the **diagnostic-conservation law** (merge may never destroy D — one law with pt-leaf C-miss-3) |
| **PT-PARSER-13** | C-M3 / L-M5 (narrowed per ruling 5) | The dead flag tier ships as 1.0.0 contract: `FLAG_EOF` never assigned (sole `.flags =` = :496, on the discarded flaggedParser whose comment :497 inverts the code), `call()`'s non-zero-flag branches (:442-477) unreachable, `unsafeCall` zero callers — while `dist/parser.d.ts:8` publishes `flags` (constants module-private: visible, unusable) and `:46` publishes `call`. The one test touching it is vacuous coverage (ruling 5) | greps re-run this session | **[identity → pt-leaf PT-LEAF-20 — PRIMARY THERE]**; the published-contract limb books here. NO-WAVE-OWNER deletion (internals 1.1.0-eligible; the export removals **2.0.0**); W4 rider shared with row 11 |
| **PT-PARSER-14** | **NEW** (α-miss 2) | `Parser.prototype.toString()` **THROWS a raw Error** for any Parser built through the public constructor: `context` defaults to `{}` (:29-32; published `context?` at d.ts:9), parserPrint destructures it (debug.ts:261), falls to `default: undefined` (:330-331), `result = s ?? name` = undefined, and :336-338 throws `"parserPrint: missing parser context name"`. `String(p)`, a template literal, or `debug(recursivePrint=true)` on any hand-built leaf throws — exactly the construction posture the band credits cand-F with | derived line-by-line at debug.ts by this seat; no "eof"/"dispatch" cases either (the eof node prints its bare name, discarding the computed inner string) | **W2** — EQ-4: rendering is non-normative AND total-or-absent; a printer that can throw on a public construction is neither. Incumbent guard = NO-WAVE-OWNER, 1.1.0-eligible. Cross-note: pt-debug B-3 owns the printer's cache defects; this row is the *totality* defect |
| **PT-PARSER-15** | **NEW** (α-miss 4) | `ParserState.clone()` drops the entire diagnostic substate while **preserving `furthest`**: :101-109 passes 5 constructor args; the field initializers (:43-45) reset `expected` to undefined and both arrays to fresh empties — so a clone inherits a furthest offset with no labels, and `mergeErrorState(clone, label)` below that furthest is a silent no-op. **On a live path**: packrat's eval-set re-entry (`scratch = live.clone()`, :351-355) discards every diagnostic the re-evaluation accumulates. Also invalidates L-m6's proposed cure (clone is not a faithful copy) | state.ts:101-109 read against :43-45 by this seat; packrat.ts:351-356 re-read | **W2** — state-copy law: a `(V,C,P,D)` copy is total or forbidden. Incumbent field-copy = NO-WAVE-OWNER, 1.1.0-eligible |
| **PT-PARSER-16** | **NEW** (β-miss 2) | The adoption arithmetic for the 52-export surface: parse-that's default-path failure state can produce **at most one of value.js's five frozen ParseIssue fields**. `start` derivable (parser.ts:60); `end` has **NO producer anywhere** (ParserState carries offsets only; SecondarySpan is a point; the 15 `*Span` builders were excised at 1.0.0 — a KNOWN delegation per CHANGELOG r6 #8, but no corpus row priced the frozen field); `expected` is armed-only (PT-PARSER-5); `actual`/`found` exists only via `collectDiagnostic` — an O-8 breach to obtain | src/css/types.ts:10-24 re-read (5 fields; 8-code union); X.P.W2 §3b's D-channel row makes ParseIssue normative | **REFINEMENT FOLD** — W2 §3b + W3's typed-recovery unit each carry the three missing producers as explicit priced obligations, not discoveries-at-open |
| **PT-PARSER-17** | L-M4 | `all()` drops `undefined` positionally so `opt()` inside `all()` silently shifts arity; parser.ts's contribution is the producer (`opt` → `ok(undefined)`, :241) and the published fixed-length tuple type; the band's graph-walking G8 gate is the external cost of the library-side decision | drop sites + producers re-read | **[identity → pt-leaf PT-LEAF-2 (+ the positional law PT-LEAF-28) — PRIMARY THERE]**; W2 sequence decision; carried here so the parser-side producer list is not lost |

### Minors (19) and info (5)

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| **PT-PARSER-18** (L-M1′, re-graded MAJOR→MINOR) | `mergeErrorState` allocates two ungated arrays per furthest-advance (utils.ts:34-35; :33 correctly gated); the "write-only under the default" premise is DEAD (ruling 2: `collectDiagnostic` spreads both, unguarded, band-asserted); three of the 21 call sites sit in dead `call()` (:447/:460/:468), deflating the hotness argument | W2 R-LAW-3 delivery side; alloc counts MEASURE-AT-OPEN #3; incumbent guard 1.1.0-eligible — but the naive gate cure is unsound (mis-attribution; a frozen sentinel throws under armed `addSuggestion`) — cure design is W2's, not a patch |
| **PT-PARSER-19** (L-M2′, MAJOR→MINOR) | `wrap()` eagerly slices the source ONLY on the close-delimiter arm (:419; the reporter discards it behind `utils.ts:71`) — the reject-leg/DEBT-2 framing killed: a rejected alternative fails at the open delimiter, which does not slice | incumbent hoist 1.1.0-eligible; magnitude MEASURE-AT-OPEN #4 |
| **PT-PARSER-20** (L-M3′, MAJOR→MINOR) | Every root Parser retains its last input's ParserState (a reference, not a copy) for process life via `this.state` — K-6 residue, not an unbounded leak | dies with PT-PARSER-6's RETIRE; 2.0.0 |
| **PT-PARSER-21** (L-M6) | `many`/`sepBy` pre-allocate `new Array(est)` (:526/:572) and fuseAll's general arm `new Array(n)` (leaf.ts:257) against the deliberate packed literals 66 lines away; min=0 (the dominant band value) takes the `[]` path, so the unconditional exposure is all() arity ≥4 | **[identity → pt-leaf PT-LEAF-25]**; elements-kind predicate = its MEASURE-AT-OPEN #1; 1.1.0-eligible |
| **PT-PARSER-22** (L-M10 / C-m1; L:MAJOR vs C:MINOR ruled MINOR) | `map(fn, mapError=true)` ERASES the error via `ok()` and maps the stale error-path residue through `state.value as T`; undocumented, published (d.ts:16), zero consumers | W2 — the parameter retires |
| **PT-PARSER-23** (L-m1) | then/skip/next are three copies of one closure differing in which value survives (and `.parser()` vs `unsafeCallRaw` — policy drift, not behavior) | W2 — one sequence factory |
| **PT-PARSER-24** (L-m4) | `chain()` neither restores the offset nor merges on continuation failure (:131-138) — the one committed-continuation arm with no failure discipline | W2 — monotonicity + failure-arm law (with PT-PARSER-12) |
| **PT-PARSER-25** (L-m5 / C-m3, corrected per K15) | The declared `unsafeSetValue` choke point (state.ts:86-89) is **broadly** bypassed, not once: peek :350, mapState :179, state.restore :81, packrat :258/:298/:354 (three on hot paths) — the "one-line inconsistency" framing killed | W2 — policy is either enforced or not declared |
| **PT-PARSER-26** (L-m6) | `mapState()` hands the callback an `Object.create(state)` view — foreign hidden class, prototype-shadowing write hazard, type it was not constructed by; unreachable today (row 11); its suggested clone() cure is unsound (row 15) | dies with mapState's RETIRE |
| **PT-PARSER-27** (L-m8) | many/sepBy allocate a fresh `[]` INTO the error state (:555/:628) — the value PT-PARSER-1 then returns | folds into row 1's cure |
| **PT-PARSER-28** (L-m9 / C-m2) | `eof()` clobbers the composed context (:640 — args [], name "eof" over skip's closure); the printer has no "eof" case and prints the bare name, and the graph-walk instrument all four band roots end in is lied to; `"eof"` is registered at state.ts:164 (challenge's :163 = "opt") | W2 — context is construction-time truth; **[cross-note → pt-debug MISS-10** (post-construction context mutation vs id-keyed cache)**]** |
| **PT-PARSER-29** (L-m3 / C-m4) | Dead `ExtractValue` type (:9-11, re-declared in leaf) + dead `Span` import (:2; :64 is a different identifier); root cause = no `noUnusedLocals`/`noUnusedParameters` (verified) | NO-WAVE-OWNER, 1.1.0-eligible; the tsconfig gap is the general cure |
| **PT-PARSER-30** (C-m5) | `sideEffects:false` gated GREEN (manifest-gate :37-41) while parser.ts:711 `_initWhitespace()` is a load-bearing top-level side effect mutating leaf's `export let whitespace` — latent, honestly bounded | **[identity → pt-leaf PT-LEAF-14 / pt-lazy LZ-17 — PRIMARY THERE]** |
| **PT-PARSER-31** (C-m6) | many() and sepBy() diverge on zero-width elements (clean stop :538 vs backtrack-past-separator :605), undocumented in either doc comment or the d.ts | W2 — a DECLARED zero-width policy (C §7 row 7 sustained) |
| **PT-PARSER-32** (**NEW**, α-miss 10) | `minus()` leaves the EXCLUDED parser's value in `state.value` on failure (:313 runs it; :314-319 restores only offset) — the PT-PARSER-1 stale-value class carrying a **foreign parser's output** through `.parse()` | folds into rows 1/12's cures; W2 stale-value law |
| **PT-PARSER-33** (**NEW**, β-miss 5) | `.trim(sep)` for any non-whitespace separator routes through `wrap()` (:520), so a close failure emits a structurally WRONG armed diagnostic: `kind:"unclosed-delimiter"` + "unclosed \`X\` opened here" for a trim separator that is not a delimiter (utils.ts:66-80's closeText falls through to openText) — R-LAW-4-adjacent: a diagnostic describing a construct absent from the grammar | W2 — diagnostic fidelity clause on the trivia/wrap split; MINOR (armed-only; the eager slice is row 19's) |
| **PT-PARSER-34** (**NEW**, β-miss 6) | The ONLY producer of `"trailing-content"` (one of the two frozen Suggestion kinds, state.ts:26) sits inside the unreachable FLAG_EOF branch (parser.ts:469-472) — and `"trailing_input"` is one of value.js's eight frozen ParseIssue codes; the one library diagnostic mapping onto a frozen consumer code is entirely dead machinery (the live `.eof()` path yields only an armed `<end of input>` label) | rider on row 13's RETIRE: W2 re-founds trailing-content in the algebra; recorded so the deletion does not orphan the capability |
| **PT-PARSER-35** (**NEW**, α-miss 7 residue) | `getLineNumber()` returns 0 for the first line while `getLineAndColumn()` returns 1 at the same position — two conventions on one published class, neither documented | dies with row 11's ParserState surface work; NO-WAVE-OWNER doc note 1.1.0 |
| **PT-PARSER-36** (L-m10) | Goldilocks: 711 lines / 24 published methods, four concerns on one class (algebra · entry+error render :34-79 · the dead flag tier · print delegation :690-700) | W2 — the tier split IS the algebra redesign; no incumbent act |
| **INFO-1** (C-i1) | The root barrel is a strict superset of all four subpaths; `mergeErrorState` has two homes (index.ts:5, diagnostics.ts:7); the four candidate grammars import bare while **3** band files import subpaths (β-K5's narrowing) | **[identity → pt-index — PRIMARY THERE]** |
| **INFO-2** (C §0 correction) | The commission's `value.js src/parsing/` does not exist; consume edges are `src/css/**`; value.js consumes zero parse-that (2 absence-asserting comments); sole live consumer = the band workspace, pinned 1.0.0 | frame of record for every severity above |
| **INFO-3** (β ANCHOR-A) | The band-installed dist is **byte-identical** to `typescript/dist` (re-diffed this session: zero differences, identical content-hashed chunk names) — every dist cite in the corpus is an assertion about the published 1.0.0 artifact | chunk names are content-addressed: re-derive from `dist/core.js:1` on any re-run (pt-leaf MEASURE #11's law) |
| **INFO-4** (STOP/law) | `parse-that-css-totality-p2` ABSENT (re-verified); nothing armed; no bench run; read-only throughout | compliance row |
| **INFO-5** (semver pricing) | pt-leaf **PT-LEAF-27**'s law applied to this roster: 1.1.0-eligible = rows 14 (printer guard), 15 (clone fields), 18 (alloc guard, W2-designed), 19 (hoist), 29 (dead imports), 35 (doc), 13's internal deletions; **2.0.0 events** = rows 1, 6, 9, 11's retirements, 13's export removals, and every entry-semantics change. The standing O-15 ask is a minor; none of the 2.0.0 rows may ride it | REFINEMENT FOLD → W4 release condition (shared law) |

## Killed-claims register — 20 entries, all SUSTAINED

| # | killed claim | adjudication |
|---|---|---|
| K1 | C-B1 sub-claim: the printer tier is present "solely because parser.ts:3 imports parserDebug/parserPrint for two zero-call-site methods"; §7 row 19's "~9,354 bytes dragged by `debug`" | **SUSTAINED (α; re-derived ruling 4a)** — `state.ts:2` is an independent value edge via `ParserState.toString()`; retiring debug/toString frees zero printer bytes from `/core`; 9,359 B re-measured |
| K2 | C-B1 sub-claim: "The `Parser` class does not begin until `:286`" | **SUSTAINED (α; re-measured)** — `:286` is `class ParserState`; `class Parser` at `:854`; the printer tier is overstated ~2× |
| K3 | C-B1 headline: "/core loads the **entire library**" / "Both sentences are false" | **SUSTAINED-IN-PART (β; re-grepped)** — json/csv = 0 hits in all three chunks; the third denial of core.ts:3-6 is TRUE; the blocker's body survives |
| K4 | C-B2: "no return-value alternative: the parameterless `enableDiagnostics()` is the **only switch**" | **SUSTAINED (β; re-read)** — `disableDiagnostics` at utils.ts:12-14, published from `.` and `/diagnostics`; the flag is two-way; everything else in C-B2 intact |
| K5 | L-M1's "write-only" reader set + its gate-the-arrays cure | **SUSTAINED (α; re-read + ruling 2)** — `collectDiagnostic` spreads both arrays unguarded (:120-122), called from :666, asserted by the band unarmed; the cure converts an allocation into either mis-attribution or an armed throw |
| K6 | L-M2's scope: "every failing wrap allocates … one throwaway substring per rejected alternative" | **SUSTAINED (α; re-read :400-426)** — three failure arms, ONE slices; a rejected alternative fails at the open delimiter, which does not slice; MAJOR→MINOR |
| K7 | C-M3 claim 3 "call() has no live caller" + C-M5's `call \| pt test 0` cell | **SUSTAINED (α+β convergent; re-grepped)** — `test/debug.test.ts:115` is a third, executing caller (fast-path only); "the flag tier is dead" survives, "the method is uncalled" does not |
| K8 | C-M3 (iii): "Every `.trim()` call allocates a wasted Parser … two ids per trim" as a universal | **SUSTAINED (α+β; re-read :488/:520)** — whitespace-branch-only; a custom-separator trim allocates one Parser via wrap |
| K9 | C-B3(b)'s census: "`.state` … returns exactly one hit, parser.ts:71" | **SUSTAINED (α; re-grepped)** — three hits: :66 write, :68 READ, :71 write; the doc's own falsifier contradicts its body |
| K10 | L-M11's recover half + L-B3's "unreachable in practice" falsifier | **SUSTAINED (α; ruling 2)** — the band consumer is wired, tested, and asserts the buffer; the two challenge docs contradict each other on the load-bearing mitigation |
| K11 | C-M8's headline: "parse() returns `undefined` on failure" | **SUSTAINED (α; β concurs)** — stale last-arm value / `[]`, undefined only for bare-leaf failures; a wave guarding on `=== undefined` would be misled; the ParseResult conclusion survives strengthened |
| K12 | C-M9's characterization: "non-string dies as a raw TypeError … 5/5" | **SUSTAINED (α)** — the lower bound restated as the class; two non-throwing regimes exist (PT-PARSER-2); a boundary try/catch protects against neither |
| K13 | L-B3's framing: "three [process-global mutables], not one … the one the corpus has not yet named" | **SUSTAINED+EXTENDED (α; re-verified)** — at least six across the import closure (+ PARSER_ID, PARSER_STRINGS, debugDepth); #4 was already C-B3a's, #5/#6 are pt-debug m-8/M-4's — the primacy claim and the count both fail |
| K14 | L-m7's cast arithmetic: "86 + 37 + 5 + 1 ≈ one cast every 5.5 lines" | **SUSTAINED (α; re-counted EXACT)** — the prefix `as Parser` = 91 occurrences, and that 91 already DECOMPOSES as 37 `as Parser<` + 37 `as ParserState` + 17 `as ParserFunction` (the latter two contain the prefix); adding 37 to 86 double-counts. Distinct casts ≈ 91 + 5 `as unknown as` + 1 `!.` = 97 in 711 lines ≈ one per 7.3 lines; the qualitative claim (the :13-16 erasure makes them uncheckable) stands |
| K15 | C-m3's "one-line inconsistency" framing for peek's direct write | **SUSTAINED (α; re-read)** — six bypasses, three on hot paths (packrat :258/:298/:354, mapState :179, state.restore :81); the policy-drift finding is larger, the framing wrong |
| K16 | C-M2's arity witness: "whitespace … yields a length-1 array" via leaf.ts:355 | **SUSTAINED (β K-2; re-derived)** — regex's EOF bail (:327-330) precedes the empty-match arm, so the trailing whitespace at a consumed offset FAILS the whole all(); the arity claim survives on a non-EOF witness only; exposes PT-LEAF-3's identity |
| K17 | C-i1's "Every band prototype imports from the bare specifier" | **SUSTAINED (β K-5; re-grepped)** — three subpath importers; the narrower true statement (all four candidate GRAMMARS import bare) still supports the finding |
| K18 | **SEAT-K1** (this seat's own): the memo-key-perturbation consequence in C-B3a, C-M3(iii), and reader-α's lazy-print cell | **KILLED (ruling 1)** — epoch-fresh tables per top-level parseState make construction-order id drift memo-inert; survivors: id budget, O-8 posture, dual-package, printer identity; honest bound: raw-`.parser()` driving revives it |
| K19 | **SEAT-K2**: reader-β's "utils.ts:121-122 (dead recover path)" confirmation cell | **CORRECTED (ruling 2)** — the path is live, unarmed, and asserted in the band; β inherited the challenge's L-M11 framing that α had killed in the same corpus round |
| K20 | Provenance-drift bundle (α's a-g + β's K-7, deduped) | **ALL SUSTAINED** — spot re-derived by this seat: `"eof"` = state.ts:164 (:163 is `"opt"`); the 17th cast at :387 (not :388); `createLazyCached` = lazy.ts:18-24; ParseResult failure arm = types.ts:25-27, `expected` = :22; `export *` = index.ts:14; `mergeErrorState` = index.ts:5 / diagnostics.ts:7; `minus` arm opens :314; `export let whitespace` = leaf.ts:395; band-census magnitudes filter-dependent (ruling 7). None load-bearing; an epoch-rule corpus carries exact provenance |

## Superlatives — 11 sustained (L-18 runs both ways)

1. **The PT-Q1 epoch at the one correct boundary, finally-guaranteed** (parser.ts:34-49 + packrat.ts:216-250): five globals snapshotted, fresh tables installed, restored even on throw, one-call-one-comparison unarmed. Honest bound: it snapshots five packrat globals and NOT `collectedDiagnostics` — correct shape, incomplete membership (PT-PARSER-3). **Elevated by ruling 1**: the epoch is also what makes the id-drift class memo-inert — a soundness dividend neither challenge credited.
2. **growLR's second unwind layer** (packrat.ts:390-406, α's find): try/finally restores per-head GROWING/HEADS bookkeeping even when a user `.map` throws mid-grow — defence in depth exactly where it is needed.
3. **getCijKey** (packrat.ts:79-100): float64-exact `id * 2^32 + offset` replacing an aliasing int32 shift, arithmetic and envelope stated in-comment, RangeError at the budget edge rather than a wrong answer.
4. **sepBy refuses trailing separators by construction** (parser.ts:565-568 + :594-611): checkpoint before the separator, restored on element failure; `rgb(1,2,3,)` cannot silently parse; deleting :608 admits it — load-bearing, not decorative.
5. **The zero-width progress guards** (:538/:605): one comparison closes the classic combinator hang; demonstrably load-bearing (csv.parse("") green because of it).
6. **Backtracking preserves the furthest record**: every disciplined backtracking arm merges BEFORE restoring (8 parser.ts sites + the leaf arms re-read) — the single most consumer-visible quality difference between combinator libraries, done uniformly, with PT-PARSER-12's arms as the exceptions that prove the rule.
7. **dispatch()'s construction-time label** (leaf.ts:127-132/:142): the only combinator delivering a labelled failure at zero per-parse cost — precisely DEBT-1's asked-for shape; the C-B2 coupling still gates its delivery.
8. **The in-source provenance habit** (C-16 at parser.ts:124-130 with the zero-caller scan and semver consequence; PT-Q1 at :35-42; PT-Q5's RETRACT note): decisions traceable to ledgers, including a REMOVED optimization documented with its re-introduction condition. Debit sustained: bench numbers no test pins (packrat.ts:141-143, parser.ts:397-398) drift into unfalsifiable authority.
9. **The leaf fast paths are real work** (string single-char charCode; regex `test()` + sticky lastIndex, no RegExpMatchArray on the default path; wrap/fuseAll frame elimination) — bounded honestly to the unrolled arms and success paths (PT-PARSER-19/-21 are the reject/general-arm edges).
10. **β's S-A — the two things that most often break at adoption are both right**: every exports-map entry orders types → import → require with no bare `default` escape, and the shipped artifact is publish-faithful (byte-identical dist, re-verified). Unpinned discipline — nothing in proof:all asserts either — but held.
11. **β's S-C — a gate that changed polarity with a ruling instead of being deleted**: dist-surface.test.ts flipped from asserting the `*Span` builders' presence to asserting their ABSENCE at the 1.0.0 excision — rarer and strictly better than deletion; its bound (barrel-only, one direction) is exactly row 11's gap.

## MEASURE-AT-OPEN residue — bench-dependent rows for the lane's W1 harness

Inherited law: one fresh process per cell; nothing armed; every arming/probing PID disposable and never reused in a timing cell; census instruments state scope + filter + unit (ruling 7); chunk names re-derived from `dist/core.js:1` at measurement time.

1. **Latch blast radius (PT-PARSER-7)** — construct `memoize(p)` in one module, never call it, measure an unrelated grammar's parse in the same process vs a fresh process; the corpus's 1.47×/139.3 figures are O-15's N=1. Poison cell by definition.
2. **Diagnostic realloc counts (PT-PARSER-18)** — arrays-per-furthest-advance on the reject leg, disarmed; mechanism adopted at the bytes, magnitude unowned.
3. **wrap() slice magnitude (PT-PARSER-19)** — genuinely-unclosed-input corpus leg only (the reject-alternative leg provably does not slice).
4. **Elements-kind predicate (PT-PARSER-21)** — shared cell with pt-leaf MEASURE #1 on the lane's pinned node.
5. **Recursion ceiling (PT-PARSER-10)** — W1 G-9 owns the row; 7,761/7,762 cited as a class, never a constant; fused-vs-chained depth from pt-leaf MEASURE #5 rides the same rig.
6. **The armed expected-set probes (PT-PARSER-12)** — C-M7's falsifier (`enableDiagnostics(); …skip(x.not()).parseState(…)`) and the destructive any()-merge witness both require arming: UNRUN-BY-LAW this session; static derivation stands; the harness runs them in disposable cells.
7. **Retention magnitude (PT-PARSER-20)** — root-parser count × retained-src size over the real grammar; one reference per root is the static truth, aggregate unowned.
8. **Raw-`.parser()` epoch bypass (ruling 1's bound)** — one cell demonstrating memo persistence when a memoized parser is driven without `parseState`; decides whether W2's entry-discipline clause is a law or a footnote.
9. **Printer-throw sweep (PT-PARSER-14)** — `String(p)` over every public construction form; belongs in the W1 boundary corpus beside the eight non-string probes (PT-PARSER-2).

## Closing verdict

Both fresh reader returns **survive the incredulity seat with corrections that sharpen rather than reverse** — and this corpus needed a third instrument at exactly the points the readers split: the recover-path liveness contradiction (ruled for α at the bytes; β's cell inherited the challenge's framing its own sibling had killed), the L-B4 grade (ruled MAJOR, β's BLOCKER preserved as the dissent that keeps W1 G-4 honest), and one consequence cell that **nobody in the corpus** — two challenges, two readers — had re-derived against the epoch machinery: the memo-key-perturbation claim is memo-inert under the supported entry (SEAT-K1), which re-grades C-B3 and defangs the AC-3 hazard down to budget-burn. Four of the seventeen admitted misses were re-discoveries of rows already adjudicated in sibling rosters — caught by the identity scan, folded, never re-booked — and eight are genuinely new, the sharpest being the published `parse(val: string): T` unsoundness, the throwing `toString()`, the diagnostic-dropping `clone()` on packrat's live re-entry path, and the ParseIssue five-field adoption arithmetic that prices the D-channel before a wave discovers it mid-flight.

The module's adjudicated shape: **a genuinely well-disciplined backtracking core — uniform furthest-merging, a correctly-placed and finally-guarded epoch, honest in-source provenance — wearing a 1.0.0 public contract that is wrong about itself at every layer**: an entry pair whose failure returns a plausible value under an unsound declaration, a printed error view divorced from the returned one, a type parameter its own function type erases, a dead 40-line flag tier and ~12 dead published members with a gate that cannot see them, a recovery combinator whose journal is a module global popped positionally — live at the package's one real consumer — and boundary combinators that fail label-free in exactly the grammar positions CSS needs. Every consumer-visible defect routes: **W2** owns the algebra-class rows (entry/typed-result PT-PARSER-1/-9, identity/state O-8 PT-PARSER-6, type-soundness PT-PARSER-8, recovery-as-value PT-PARSER-3, conservation + labels PT-PARSER-12, depth PT-PARSER-10, the specified-not-inherited operator discipline PT-PARSER-11/-13); **W3** carries the recovery re-authoring and the labelled-without-arming condition; **W4** carries the chunk-topology and standing-property release gates plus the semver-pricing law; the **REFINEMENT FOLD** sharpens W1's boundary invariant (typed failure, `.parse()` banned, printer-throw and non-string probes in the corpus, G-4's constructed-not-called predicate) and W2's D-channel pricing; **NO-WAVE-OWNER** carries the incumbent repairs on the owner-held O-15 channel, split 1.1.0-eligible vs 2.0.0. Nine identities live in sibling rosters and are carried by reference; nine dynamic rows are banked to MEASURE-AT-OPEN. Nothing lost: head, tail, or interval.

— end of apotheosis. Both challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
