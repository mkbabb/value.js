# wb-extract-controls — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **HEAD**: `1994eb53` · branch `tranche-u` · **Subject**: `demo/workbenches/extract/ExtractControls.vue` (151 lines) + its closure (ExtractWorkbench.vue, useExtractSession.ts, useImageQuantize.ts, quantize-worker.ts, ImageDropZone.vue, ink.ts, useContrastSafeColor.ts, foundation.css, utils.css, the glass-ui 7.0.0 dist).
**Pin re-verified this session**: `shasum -a 256` → `71aa0a65873c367ae3ae393283d4e81bcfc9cbb57b6f232eec9f930264d46c28` — byte-identical to the glass BJ W4 receiver pin, at a HEAD five-plus revisions past the corpus brief's `c654824e` (the brief-generation staleness defect the L axis names at :1739-1740 is still live and still unactioned).
**Corpus**: challenge-D-design.md (pass 4, Opus 5[1m]) + its pass-1/2/3 priors · challenge-L-library.md (r1–r5 folded) · challenge-C-implementation.md (pass 4) + priors · evidence/ (4 PNGs, 2 probe scripts). No r2 axis-promotion file exists for this component (AXIS-PROMOTIONS-2026-08-03 table read); the newest pass of each axis governs, and §EC-20 below records that the D axis's own supersession chain is false.
**Re-readers adjudicated**: design-and-usage · library-and-code. Both were attacked before either was adopted: every kill re-derived against the tree, every load-bearing confirm re-walked at its sharpest line, every reader-found miss re-verified in source by this seat.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling per the commission)**: full reads of ExtractControls.vue (151), ExtractWorkbench.vue (293), useExtractSession.ts (223), useImageQuantize.ts (161), quantize-worker.ts (44); targeted reads ImageDropZone.vue:14-32, ink.ts:10-20/:126-176, useContrastSafeColor.ts:344-362, useAtmosphereBoot.ts:96-108, useColorPipeline.ts:100-108/:276-282, picker-color.ts:203-213, useMarkdownColors.ts:23-27, GenerateControls.vue:63-74/:282-306, ComponentSliders.vue:176-208, spectrumLuma.ts:1-33, src/quantize.ts:36-52/:128-140, App.vue:268-274, foundation.css:260-268/:395-410/:520-528/:648-700/:810-840, utils.css:1-15, vite.config.ts:22-28, ErrorBoundary.vue grep; producer dist bytes: glass-ui.css spectrum block (`.slider-range{…background:0 0}`, `.slider-track{…background:var(--slider-track-bg,var(--secondary))}`, thumb `background:var(--slider-thumb-bg,transparent);border:…var(--slider-thumb-border-color,var(--background))`), dock layer-group.css + shell.css (`--dock-separator-height` authored ONLY inside `.glass-dock{}`; **0** `:root` blocks in shell.css; foundation.css authors only `--dock-h`, the separator var appears there in a comment alone), DockControl.vue.d.ts:24-49 (shape/compact/**active**/type/disabled/as/asChild/class — no title, no label), slider-\*.js aria forwarding census (**exactly four**: label/labelledby/describedby/errormessage — no `aria-valuetext`), typography tokens (`--type-small: clamp(0.875rem, 0.8rem + 0.25vw, 1.25rem)` · `--type-micro: 0.6875rem` · `@utility fira-code{…font-feature-settings:"liga","calt"}`), reka-ui SliderImpl.js:40-60 (pointerdown on the ROOT emits slideStart for any non-thumb target); repo censuses: `find demo/ui -type f` = **19**, all index.ts; `.plate-ink` = 5 files with ErrorBoundary at **:84**; `data-o18` = 3 files, GenerateControls absent; ParseEchoReadout.vue:38/:44 both bare `var(--ink-muted)`; eslint.config.js:235-238/:275-276 `demo/@` globs with `demo/@` ABSENT; package.json dependencies `{glass-ui:^7.0.0, keyframes.js:^6.0.0}` + exports = 7 keys no root; installed `node_modules/@mkbabb/value.js` = 4.0.0; dist/subpaths/quantize.d.ts:42-48 five bare numbers; **X wave files W0/W1/W4/W5/W7/W8/W9/W10 + CARRY-CUT-LEDGER read for routing** — including CC-105 (`ExtractControls.vue:32,75` --slider-track-bg sites BLOCKED-ON the X-W0.j Glass-8 repin census, receiving surface **X-W4.g**) and the fact that **no X wave's file bounds hold ExtractControls.vue itself**; motion-quarantine.md M-31/M-32/M-33/M-34 re-read; corpus priors: pass-1 D-1/D-3/D-4/D-6/D-18 located at their cited lines, pass-3 state register :512-545 read (no lying-readout row, no blend row; forced-colors demoted to "undesigned"+hypothesis at :534/:557-562), L-file citations :168, :326-327, :444, :831-845, :1869-1875 re-read against the tree.

## RULING PINS (this seat's own derivations)

1. **The pinned file has no owner in tranche X.** ExtractControls.vue appears in no X wave's file-bounds table (grep across W0–W11). The only named receiving surface for ANY of its bytes is CC-105 (the two `--slider-track-bg` sites :32/:75) → **X-W4.g**, trigger-gated on the X-W0.j Glass-8 repin census, and W4.g's own bounds table omits the file — a spec inconsistency the formation boundary must reconcile. Every in-file cure below is therefore pin-gated AND unowned: the densest defect surface in this corpus is banked behind a release event whose receiving wave does not name it.
2. **CC-105/X-W4.g is the receiving surface for the whole track-material cluster** (rail div, ring, thumb tokens, tier differentiation) — the readers' producer-ask framings are re-pointed there and at the standing BH relay, never at a consumer patch under the pin.
3. **The spectrum contract is the decider for the corpus's largest D claim**: `.slider-range{background:0 0}` in the shipped dist bytes. There is no range fill to composite, by producer design; the value carrier is the thumb. Every argument built on "fill ≡ track" is dead (K-A), and the producer ask P2 ("a colour-bearing track must still yield a legible filled range") is struck — it would break the hue/spectrum idiom at its five app-wide consumers.

## Scoreboard

**47 adjudicated rows: 0 BLOCKER · 20 MAJOR · 24 MINOR · 3 INFO.** All three of the corpus's BLOCKER claims failed adjudication *at BLOCKER weight* (D4-1 rescoped, D4-2 mechanism killed, XC4-3 demoted) — and the MAJOR band is the densest of any workbench corpus. **19 rows in the killed register**: 16 reader kills all UPHELD on re-derivation (most re-verified in the tree by this seat), plus 3 corrections of this seat's own against the readers themselves. 13 roster rows originate in reader-found misses. Headline: **the in-file defect mass (separators, disabled wiring, both readouts, naming, ring, reservation) is pin-gated with NO wave owner**, and the session-correctness cluster extends wb-extract-pane's NO-WAVE-OWNER finding.

## Ruled disagreements and reader-challenges (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| D4-2 (fill≡track) | reader-1 KILLED the mechanism; reader-2's confirm cites "L-17 + D4-2" as one row | **KILL UPHELD; reader-2's citation corrected** | Dist bytes read by this seat: spectrum `.slider-range` is `background:0 0; box-shadow:none` — no fill exists in either axis; the thumb is the value carrier (`opacity:1`, 12×24, 2px border). Pass-1 D-3 measured this correctly (range bg transparent, width 26.7% = (5-1)/(16-1)); pass 4 replaced a correct mechanism with a false one and minted a false novelty claim. Reader-2's row is really L-17 (ring==fill, sound on its own — EC-2); the D4-2 id inside it is struck. The surviving DESIGN truth re-forms as EC-3. |
| D4-1 (focus ring "wholly gated" on producer) | reader-1 KILLED attribution; reader-2 silent | **KILL UPHELD** | foundation.css:264 `--focus-ring-color: var(--accent-live)` and the :522-526 dark re-pin, both read by this seat — the ring's accent binding is DEMO-authored, in an unpinned file; `--accent-live` itself is demo-stamped (useAtmosphereBoot:96-99). "Cannot be discharged by a consumer edit" and "the consumer wave becomes wholly gated" are false. The measured 1.03–1.14:1 stays UNPROVEN-NEEDS-LIVE; the hue-collision mechanism survives as EC-19. |
| D4-6 (readouts overflow at 1440) | reader-1 KILLED the adjudication; corpus pass-2/pass-4 assert overflow, pass-3 denies | **KILL UPHELD — pass 3 agrees with the type system** | Re-derived from shipped tokens: `w-5` = 20px; k at `--type-small`(1440) = 16.4px → 2×0.6em = 19.68px; kC at `--type-micro` = 11px → 3×0.6em = 19.80px. Both FIT; pass-3's measured 19.74 matches to 0.3%; pass-4's 20.185/20.308 sit a **constant** ~+0.5px above the arithmetic across two font sizes and glyph counts — the signature of `Range`-rect instrument bias, not two independent overflows. The REAL defect is EC-23: a fixed 20px box against a viewport-fluid clamp — genuine overflow above ≈1547px (+4.0px by 2880px). One live font-metric read settles the residual (advance ≠ 0.6em would revive pass 4). |
| L-10 (12×24 thumb vs SC 2.5.8) | reader-1 demote-to-INFO; reader-2 "well-attested, glass-owned, do not re-book" | **BOTH ADOPTED — INFO · GLASS-OWNED · already relayed** | reka-ui SliderImpl.js:46-53 read by this seat: pointerdown on any non-thumb target captures and emits `slideStart` — the 2.5.8 TARGET is the root (full inline-size × 24), not the painted 12px glyph. The repo's `smallTapTargets` probe measures the paint — an instrument defect of the same species as the `namelessButtons` accname miss (L-5), recorded as input to X-W1's gate re-authoring. Seated in GLASS-UI-COMMUNIQUE §1; not re-booked (EC-47). |
| XC4-3 (BLOCKER "rail paints nothing") | both readers demote to MINOR | **CONCORDANCE VERIFIED, DEMOTION UPHELD** | App.vue:271 provides CSS_COLOR_KEY unconditionally; useColorPipeline:104 builds it from `serializePickerColor` → picker-color.ts:210 `valueOrThrow` — an empty string is unconstructible, so `cssColorOpaque ?? ''` is truthy on every shipped route and ExtractControls:124's bare-token branch never runs live. Reader-1's kill of the "sole outlier" count also verified: ParseEchoReadout.vue:38/:44 are two more unguarded `var(--ink-muted)` sites. Merged row EC-21. |
| K2 (the L axis's inset-ring producer cure) | reader-2 kill; reader-1 concordant via XC-1 | **KILL UPHELD — the cure preserves the defect** | The proposed `--slider-track-ring` is still `inset`; CSS Backgrounds 3 §5.3 paints an inner shadow over the element's background **image**, so the ring lands on the ramp while its ink is certified against the plate (`useSafeAccentFn("resting")`, :118 — read); and `contrastInkFor` (ink.ts:158-174, read) certifies ONE opaque fill — a single sampled `gradientMid` cannot certify a ring over a multi-stop ramp. XC-1's direction (the ring seats on the material its ink was certified against) governs EC-2's cure; the ColorSpaceSelector law applies: *the cure is adjudicated separately from the defect.* |
| L-24 (two inject contracts) | reader-2 CONFIRMED MAJOR; reader-1 silent | **DEMOTED to observation inside EC-21 (precedent)** | Facts verified (3 asserting incl. useMarkdownColors:25 vs 2 defaulting; sole provider at boot; + ExtractPane.vue:**36** — reader-2's ":35" off by one). Under the ColorSpaceSelector L-5(b) and wb-extract-pane COLOR_TARGET_PORT_KEY precedents, a hard-assert under a boot-guaranteed provider is house contract, not defect; K-J killed the axis's reachability premise besides. Revives with a crash witness if any consumer mounts outside App.vue's subtree. |
| Reader-2's dead-camera / unhandled-rejection / promise-slot / route-change-leak misses | filed as new MAJORs | **ADMITTED AS IDENTITIES — folded, not re-minted** | All four are already adjudicated TODAY at wb-extract-pane: dead `quantizeFromCamera`/`quantizeFromCanvas` = **XP-13** (→ X-W8 G-10); decode-path rejection + stale palette = **XP-2** (BLOCKER, session cluster) with `readAsDataUrl` = **XP-23**; single promise slot = **XP-3**; KeepAlive/onBeforeUnmount camera+worker teardown = **XP-1** (→ X-W5 addendum). Anti-rename law: one identity each; reader-2's sharpenings (the dead impl carries the correct `stop()` teardown; the un-caught `onFile` template handler) travel to those rows as annotations. |

## Adjudicated defect roster

Line anchors at HEAD `1994eb53`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: a wave id = that wave's bounds/ledger row owns the surface today; **X-W4.g/CC-105** = banked, census-gated; **NO-WAVE-OWNER** = booked for the next formation boundary; **PIN** flags rows whose cure edits the pinned file (ruling pin 1).

### MAJOR

- **EC-1 — L-11/D-5/P2-4: both `<DockSeparator/>` render at height 0 and paint nothing — a `.glass-dock`-scoped primitive on a card plate.** Verified whole by this seat: `.dock-separator{height:var(--dock-separator-height)}` with no fallback; the var's ONLY authoring site is inside `.glass-dock{}` (shell.css; zero `:root` blocks); foundation.css authors only `--dock-h` (:400-405, the separator var appears in its comment alone). Off-dock: invalid-at-computed-value-time → `auto` → 0 on an empty div. Corpus pixel scan corroborates (3 colours, all plate ground). Two `role="separator"` nodes asserted to AT, withheld from everyone else. OBC gives Extract retained-line **none** — the canonical cure is deletion. → **NO-WAVE-OWNER · PIN**; producer fallback-hygiene ask (a var-consuming primitive that dies silently off-scope) rides the BH relay.
- **EC-2 — L-17 + XC-1: the ring is the fill, and when it isn't, it paints on the wrong referent.** ExtractControls:22 feeds ONE expression (`trackInk`) to both `backgroundColor` and the `inset` box-shadow — ring==fill 1.00:1 by construction whenever the gradient is absent (pre-image: useExtractSession:103 returns `"var(--muted)"`, overwritten by key order — EC-25). Once the ramp paints, the INSET ring paints over the ramp while certified against the resting plate (:118) — certification referent ≠ paint referent, statically decidable from line 22. Falsifies the file's own :8-11 claim ("survives OUTWARD … in every state"; the keyword is inward). Cure direction per K2. → **X-W4.g via CC-105**.
- **EC-3 — pass-1 D-3 as-rescoped + reader-1 miss M2: the sole value carrier of both axes is a producer-default thumb — transparent fill, `--background`-coloured border — floated over a ramp derived from the user's own image.** Producer contract verified in dist: no range fill exists (ruling pin 3); thumb tokens `--slider-thumb-bg`/`--slider-thumb-border-color` unset at both instances (:24-34, :68-77). The app already owns the cure two directories away: ComponentSliders.vue:193-206 feeds both tokens from `thumbInk` keyed to `spectrumFieldIsLight` — whose doc-block states "Share the function, never copy the constant" (spectrumLuma.ts:1-33, read). A near-white swatch in the extracted ramp (visible in the developed capture) collides with the default border. This is the true form of the defect D4-2 mis-mechanised. → **X-W4.g via CC-105**.
- **EC-4 — D-4: the kC axis is unoperable at 320px** — one non-wrapping `flex items-center gap-2` row (:39-91) carrying 3 DockControls + 2 separators + `w-5` label + `w-5` readout, with the kC group the ONLY `flex-1 min-w-0` child: all shrink lands on the one element that must not shrink. Corpus: 22.5px track, 0.70px/step; rendered witness D-narrow-320-light.png. → **NO-WAVE-OWNER · PIN**; the P5 producer ask (a named minimum operable inline size) rides the BH relay.
- **EC-5 — D4-5 + XC-2/C-1: the camera is a one-way door and a stream leak.** No off control exists (`stopCamera` reachable only via onFile/onBeforeUnmount — :235/:281, read); `disabled` never reaches Camera, so a second press re-enters startCamera (:239-250), which assigns `cameraStream` without stopping the previous stream — orphaned live tracks. The producer lever exists and is unused: DockControl `active` stamps `aria-pressed`+`data-active` (d.ts read). The route-change leak arm is XP-1's (folded). → **NO-WAVE-OWNER** (leak guard + off-path live in unpinned ExtractWorkbench; the `active` binding half is **PIN**).
- **EC-6 — D-7/L-3 (denominator corrected, K-O): `disabled` is a whole-component contract honoured by 1 of FIVE controls** (:110 declared; :84 sole reference; Upload/Camera/both Sliders ignore it) — and the parent's intent is "camera running / quantize in flight, stand down" (:70). Live consequence beyond the door: both sliders keep re-arming the 300ms debounce mid-flight. → **NO-WAVE-OWNER · PIN**.
- **EC-7 — D-8/§4: neither axis can carry a unit or `aria-valuetext`.** Verified in dist bytes: the thumb forwards exactly four aria attrs — `aria-valuetext` is structurally unreachable from any consumer; the protagonist control announces the bare string "5". The demo already books the ask: foundation.css:648-651 names "the Slider aria-valuetext prop-through" as a standing BH-relay item. → **GLASS-OWNED (BH relay, already seated)**; receiving demo surface = X-W4's accname/announcement law; the visible-unit half is **PIN** (see EC-44, K-R).
- **EC-8 — reader-1 miss M1: the forced-colors roster protects an element that never paints, and both actual colour carriers die in WHCM.** Source-certain, verified whole by this seat: foundation.css:678-694 tier-1 lists `.glass-slider[data-variant="spectrum"] .slider-range` — which the producer sets to `background:0 0`; the carriers (`.slider-track` fed by `--slider-track-bg`, and the rail div whose whole identity is an inline background + inset shadow — the two things WHCM strips) are ABSENT from the roster; the print block (:815-840) adds blanket `box-shadow:none !important` and its smaller roster omits them too. Scope is app-wide (every spectrum ramp incl. ComponentSliders:197). Corpus history: pass-1 D-1 BLOCKER → pass-3 hypothesis (:534, :557-562, WebKit-emulation caveat — an honest demotion) → pass-4 dropped it entirely. The roster mis-aim needs no browser; the rendered blank needs a real-WHCM witness (residue). → roster cure **NO-WAVE-OWNER** (foundation.css's forced-colors block exceeds X-W10's declared carve); witness matrix → **X-W1** goldens.
- **EC-9 — reader-1 miss M3 / pass-1 D-4 (restored): the k readout is a lying readout — it shows the request, not the result.** `{{ k }}` (:16) is `session.colorCount` (the ask); the returned palette can be SHORTER — src/quantize.ts dedupes at `dedupeThreshold 0.02` (read) — and the card badge ~90px below renders the truth (corpus capture: 16 vs 10). The governing rule is written in the parent at :134-136: "never a lying readout." Dropped by pass 3 AND pass 4; restored here. → **NO-WAVE-OWNER · PIN**.
- **EC-10 — reader-1 miss M4 / pass-1 D-6 (restored): the rail renders a discrete k-means result as a continuous interpolation** — soft stops at `i/(n-1)` (useExtractSession:104-111, read), so most rail pixels are colours the quantizer never returned, while PaletteCard renders the same dataset as crisp bands in the same viewport; `rounded-full` additionally pinches both terminal stops. The most product-relevant D finding in the corpus, absent from the axis of record. Cure (hard stops) lives in the UNPINNED composable. L-19 folds here: the twin builder in GenerateControls:65-73 (verified; same singleton guard, same `.toFixed(0)`, same `"var(--muted)"`) must move in the same step, and its empty arms are NOT equivalent (the generate rail's `var(--muted)` actually paints — reader-2's sharpening, adopted). → **NO-WAVE-OWNER (session cluster)**.
- **EC-11 — reader-1 miss M5: the eyedropper is keyboard-unreachable in exactly the state it exists for.** Verified: sole activation is ExtractWorkbench:25's click on ImageDropZone's root; the instant an image exists, `:tabindex="disableClick ? -1 : 0"` (:19) removes it from the tab order while `role="button"` (:18) and `aria-label="…tap to sample colors"` (:20) persist — an AT promise on an unfocusable element — and its own `@keydown.enter.space` (:22) is gated off by the same flag. Neither file is pinned. → **X-W4** (keyboard-operability family, addendum candidate beside XP-9).
- **EC-12 — L-16 (+ D-2 folded): the parameter named `floor` is not the floor.** Verified at :17/:130-137: `certify(accent, surfaceL, floor + CERTIFY_HEADROOM)` — this component's documented 1.4.11 floor of 3 ships as a 4.25:1 walk (and the default text path as 5.75), against a doc-comment (useContrastSafeColor:350-352, read) that promises a plain override. D-2's floor-only/no-ceiling scheme asymmetry (3.333 light vs 6.385 dark, rendered) folds here as the same walk's other face — numbers residue, mechanism source-consistent. NOTE the cross-axis tension (§residue 3): D's rendered 3.333 vs the code's requested 4.25 implies the modelled referent misses the painted ground by ~0.9 — one live A/B decides whether every quantitative claim in both axes shares a biased referent. → **X-W8 (addendum candidate — ink.ts is already in W8's G-13 surface)**.
- **EC-13 — L-18: the library's contrast machinery is module-private and the demo infers ratios by oracle-abuse** — `luminance`/`contrast` unexported (axis greps; consequences verified by this seat: ink.ts:138-142's certify-then-diff-every-channel with 1e-9 epsilons, :158-174's endpoint-walk inference). → **X-W9** (published-surface apotheosis).
- **EC-14 — L-20: the published manifest declares runtime deps on the design system that peer-depends on it** (`dependencies: {glass-ui, keyframes.js}` — verified; src/dist import neither per the axis's re-run greps; eslint inv-K-1 defends the same topology one level down). → **X-W9**.
- **EC-15 — L-21: every module-lattice lint rule is dead** — globs name the deleted `demo/@` tree (:235-238, :275-276 verified; `demo/@` ABSENT). **FOLD — one identity**: ColorSpaceSelector L-5(a) + AXIS-PROMOTIONS' ActionFeedback L-2 headline, already carried → **X-W0/X-W1**. Not re-booked.
- **EC-16 — L-22: the O-18 cure landed on one of two rail copies, and the gate hook exists only on the cured copy.** Verified: GenerateControls:282-306 admits the clone ("the extract k-slider pattern verbatim"), still ships the retired `text-muted-foreground` (:288 — the exact token ExtractControls:144-147 names as the measured failure), carries no certified fill and no ring; `data-o18` census = 3 files, the twin absent, so o18 cannot fail on it. → **X-W4** (GenerateControls.vue is in W4.b's bounds; pin-note travels).
- **EC-17 — L-23: the quantize domain has four disagreeing homes and the published d.ts declares none of it** — verified: guard k∈[1,64] int / chromaWeight≥0 (src/quantize.ts:39-50), `dominantColor` hardcodes k:5 (:132-139), demo defaults 5/0.5, UI offers 1..16 & 0..1.5, GenerateControls offers 1..12, quantize.d.ts publishes five bare `number`s. → **X-W9** (surface/brands) + the demo range-coherence arm rides the session cluster.
- **EC-18 — XC4-1: KeepAlive-parked, the component re-renders on the app's hottest signal.** Mechanism verified: PaneSlot KeepAlive; `trackInk` → `safeCss` reads `ambient.value` + `isDark.value` inside the computed (useContrastSafeColor:353-358, read); and the rAF-coalesced `cssColorOpaqueFrame` EXISTS one line beside the raw computed (useColorPipeline:279) while App.vue:271 provides the RAW one. Rates residue. → **X-W5** (the deactivation-contract addendum, XP-1's family); cure candidates (PANE_ACTIVE_KEY gate / coalesced provide) touch only unpinned files.
- **EC-19 — D4-1 as-rescoped (K-D): the accent spent on a surface collides with the accent-derived focus ring.** Source-certain half: `--focus-ring-color` = `--accent-live` (demo-pinned, twice) and `trackInk` = the certified live pick with hue preserved — state signal and surface share one hue axis by construction. Rendered 1.03–1.14:1 and the 1.976 producer-default counterfactual: UNPROVEN-NEEDS-LIVE. The "wholly gated on Glass 8" wave-shape conclusion is killed; the demo can re-derive its ring today. → **NO-WAVE-OWNER** (no X wave owns the focus-ink derivation) + BH-relay ask P1 (focus ink certified against the RESOLVED track) survives on its merits.
- **EC-20 — reader-1 miss M7 (softened, K-S): the governing D file's supersession chain is false and drops pass-1 rows from the axis of record.** Verified: pass-4:3-4 claims pass 3 "folded passes 1 and 2"; pass-3's register (read :512-545) carries no lying-readout row, no blend row, no D-18 — and pass-4 restates only deltas, so the drops propagate; the "five findings no prior pass carries" claim is false for D4-2 (≡ pass-1 D-3, with worse mechanics). Softening: pass 3 did NOT wholly drop forced-colors — it demoted it to an honest emulation-bounded hypothesis; pass 4 dropped it. Any wave authored from the governing text alone builds on an incomplete record. → **formation-boundary process row** (this file restores EC-8/EC-9/EC-10/EC-46).

### MINOR

- **EC-21 — XC4-3 + L-24 merged (K-I/K-J/K-P): the unguarded token and the assert-inject, one latent portability fact.** :124 emits bare `var(--ink-muted)` (zero CSS declarations repo-wide; sole definition is the boot-stamp, immediate watcher — verified) while :149 in the same file guards it; the same component's chain hard-asserts INK_AMBIENT_KEY. No live break on any shipped route (K-J); the component simply cannot mount outside App's provider subtree without painting a hole. House-contract precedent applied. → **NO-WAVE-OWNER · PIN** (the :124 guard) — revives with a crash witness.
- **EC-22 — D4-3 as-rescoped (K-E): no tier differentiation between the protagonist and the support axis** — one `trackInk` feeds both with no semantic split; kC (a neutral scalar weight) is the most saturated object on the card in the developed state. The "byte-identical in every state" evidence was pre-image-only (killed); the design conclusion survives on the developed capture. → **X-W4.g via CC-105** (+ X-W10 canon awareness).
- **EC-23 — D4-6-as-corrected + reader-1 M6: a fixed 20px reservation against a viewport-fluid font.** The 1440 overflow claim is KILLED (K-F); the real defect is static: `w-5` is a constant, `--type-small` is a clamp — two Fira digits exceed 20px once the clamp passes 16.667px (viewport ≈1547px), +4.0px at ≥2880, with `text-right` sending the excess back across the cluster. VC §4: "reserve their widest legal representation" — a constant against a function. → **NO-WAVE-OWNER · PIN**.
- **EC-24 — L-26 + reader-2's widening: "ONE mono voice" is two type recipes whose ratio is itself viewport-dependent** — 1.24× (320) → 1.49× (1440) → 1.82× (clamp saturation ≈1800px), re-derived from the tokens by this seat. The :12-13 comment parenthetically narrows to weight (which does match); size is the visible half. → **NO-WAVE-OWNER · PIN**.
- **EC-25 — D4-7/L-12-tail: the cured born-RED `var(--muted)` pin is live and neutralised only by JS object key order** (:22 enumerates `background` before `backgroundColor`; useExtractSession:103 returns a colour token from a prop typed `gradient: string`). A 1.4.11 certification resting on property order, with no test. → **NO-WAVE-OWNER** (session arm unpinned; the :22 write is **PIN**); dies structurally with EC-10's `string | null` re-type IF the generate consumer moves in the same step (reader-2's coupling, adopted).
- **EC-26 — L-7/C-4: dead scoped `.touch-gate-target`** (:140-142; class absent from the 93-line template — full read; scoped compilation could not reach producer internals anyway). → **NO-WAVE-OWNER · PIN**.
- **EC-27 — L-13: `.plate-ink` ×5 byte-identical scoped copies** (verified; ErrorBoundary at **:84**, K-N). **FOLD — identity = wb-extract-pane XP-19 → X-W10.** Not re-booked.
- **EC-28 — L-14: `cssColorOpaque` reaches this subtree by two mechanisms with three stacked dead degenerates** (inject-default undefined → `?? ''` → the `:124` falsy arm — the last two unreachable per K-J). Demoted from the reader's MAJOR: dead defensive layers, no live break. → **X-W7** (ExtractWorkbench modify-carve owns the `?? ''` sites).
- **EC-29 — L-6: the WCAG floor constants have four homes, two identically named in sibling files** (ink.ts trio verified; view-accent.ts/palettes-ramp.ts per the axis's greps). → **X-W8** (one-home family, rides with EC-12).
- **EC-30 — L-8: tsconfig.demo.json↔exports desync** (3 phantoms, `/css`+`/value` omitted, "CLOSED 8-key set" false by one). **FOLD — identity = wb-extract-pane XP-21 (X-W8 explicitly declines it) → NO-WAVE-OWNER**, generator-parity cure standing.
- **EC-31 — L-1: `demo/ui/` is 19 index-only barrels (K-L) and this file takes both spellings on adjacent lines** (:98 `/dock`, :99 `../ui/slider`). **FOLD — identity = wb-extract-pane XP-11 → X-W8** (which owns the import-line repoint even in the pinned files, W8:106).
- **EC-32 — L-9: the vite dogfood-alias comment is factually false** — "A package does not install itself" vs the installed transitive 4.0.0 (verified). → **NO-WAVE-OWNER** (one-line truth fix at the next config-touching wave).
- **EC-33 — L-19: the palette→gradient builder exists twice at two altitudes** — verified; corroborated by the GenerateControls apotheosis R-F ("2 structural twins"). Folded into EC-10's cure step; booked here so the id survives.
- **EC-34 — C2-3/XC-7: `onFile` and `onReset` dispatch directly and never cancel the pending debounce** (:159-162 is the only clearing site — read) — one intent, two identical worker jobs, feeding XP-3's race window. → **NO-WAVE-OWNER (session cluster)**.
- **EC-35 — C2-4: the single-slot pending-resolver.** **FOLD — identity = wb-extract-pane XP-3** (reachability already corrected there). Not re-booked.
- **EC-36 — reader-2 miss: raw library enum codes render as user-facing text** — worker posts `result.error.code` and `String(err)` (quantize-worker.ts:26-42, read); ExtractWorkbench:80-85 renders the slot verbatim in the destructive register. → **NO-WAVE-OWNER (session cluster — presentation contract)**.
- **EC-37 — reader-2 miss: the `@reference` directive is dead in this file** (:137; the block contains only plain declarations — no `@apply`, no theme function; full read) — and per the reader's census, dead in 15 of 17 carrying files, at a per-block foundation.css re-processing cost. → **NO-WAVE-OWNER** (styles-hygiene sweep).
- **EC-38 — both readers independently (blind cross-corroboration): the demo re-declares the producer's `fira-code` utility, unlayered, dropping `font-feature-settings: "liga","calt"`** — both definitions read by this seat; utils.css enters via a plain App.vue:199 import, so the demo copy outranks every `@layer`; consumed at :66/:78. Harmless today (same token), the producer's recipe is no longer retunable. → **X-W4** (demo/styles/utils.css is in W4's bounds, modify-carve).
- **EC-39 — reader-2 miss (count corrected, K-Q): the session publishes dead surface — 4 of NINETEEN members unread** (`palette`, `lastFile`, `paletteName`, `totalPopulation`; return literal + sole-consumer enumeration both read by this seat; the reader's "21" is wrong, its list is right). `palette` is an escape hatch around the composable's own presentation contract. → **X-W8** (G-10 dead-surface census family, beside XP-13).
- **EC-40 — D4-4 (rescoped MINOR): two controls, one function, in the boot state** — the labelled well and the icon both invoke `openFilePicker` (verified :24-25 / ImageDropZone:21-22); PR-13/PR-06 name the pattern. Rescoped from the axis MAJOR: both owners are named and functional; the defect is a missing state-aware action region (the icon is redundant empty, load-bearing populated), not a broken path. → **NO-WAVE-OWNER · PIN** (the icon lives in the pinned file).
- **EC-41 — D-15 (static half): `text-right` on the k label is a physical alignment in a bidirectional app** (:15, source-certain; the corpus's 8.0→17.9px RTL delta is its live face). → **NO-WAVE-OWNER · PIN**.
- **EC-42 — XC4-2: one `:style` object welds three keys whose lifetimes differ by orders of magnitude** (gradient per-image, trackInk per-frame; Vue patchStyle re-sets unconditionally). → **NO-WAVE-OWNER · PIN** rider on EC-2's cure.
- **EC-43 — XC4-4: two paint clocks in one row** — the producer track transitions `background` over `--duration-fast` while the bare rail div snaps (no transition source anywhere — :19-23 classes read; no matching demo rule). The "uncertified mid-flight intermediate" hypothesis is decidable STATICALLY (both endpoints computable from `certifyAccentInk`; a pure unit test over the sRGB interpolation — no seat proposed it, this record does). No motion-quarantine row is touched. → **NO-WAVE-OWNER**, rides the track cluster.
- **EC-44 — reader-1 miss M9 (corrected, K-R): the protagonist axis has no visible name at all; kC's only expansion is a hover `title` on a non-focusable `<label>` with no `for`.** Verified (:14-36, :66). Correction of the reader: the natural cure edits the PINNED file — "unblocked today" is false in-file (a parent-composed label is the unpinned alternative). **FOLD → X-W4** (XP-8's accname identity), pin-note attached.

### INFO

- **EC-45 — L-15/D4-8: producer defaults restated per-instance ×3 (`w-5 h-5 transition-colors`) + `--btn-hover-color` pinned identically ×3** (:42/:45/:51/:54/:86/:89 — verified). → **NO-WAVE-OWNER · PIN** rider.
- **EC-46 — pass-1 D-18 (restored): `:model-value="[k]"` mints a new array identity per parent render** (:27, :70) — defeats producer prop-identity bailouts; dropped by passes 3 and 4. → **NO-WAVE-OWNER · PIN** rider.
- **EC-47 — L-10 as-demoted (K-G): the 12×24 thumb** — INFO · **GLASS-OWNED**, already seated in the glass communiqué; do not re-book. The repo-side `smallTapTargets` paint-vs-target instrument defect → input to **X-W1**'s gate re-authoring.

**Not carried**: L-25 (palettes `export.ts` vs `export/` dual — out of this component's cone; recorded for the palettes-export seat). XC-3/C2-1, XP-2-family rejections, XP-3, XP-13 — folded identities (rulings table). D-3 (10px thumb overhang on coarse pointers) — rides EC-47's producer geometry family + P4 relay ask, no separate consumer row.

## Killed-claims register

Reader kills — **all 16 UPHELD**, the load-bearing ones re-derived in the tree by this seat:

- **K-A — D4-2 mechanism ("fill colour ≡ track colour")**: dist bytes — spectrum `.slider-range{background:0 0}`; no fill exists to composite. Pass-1 D-3 had it right.
- **K-B — D4-2 novelty ("no prior pass carries")**: ≡ pass-1 D-3, identical severity, better evidence.
- **K-C — D4-2's rhetorical capture claim**: the developed capture shows a full palette ramp (a spectrum, not a progress bar); the flat-capsule reading holds only pre-image.
- **K-D — D4-1 attribution ("cannot be discharged by a consumer edit … wholly gated")**: `--focus-ring-color: var(--accent-live)` authored by the DEMO at foundation.css:264 and re-pinned at :522-526 (both read); foundation.css is unpinned.
- **K-E — D4-3's "identical to the byte in every state"**: sampled pre-image only; the developed state is plainly differentiable. Conclusion survives rescoped (EC-22).
- **K-F — D4-6's "confirmed overflow" adjudication**: token arithmetic (19.68/19.80 vs 20px boxes) matches pass-3's measurement to 0.3%; pass-4's constant +0.5px across two sizes/glyph-counts is instrument bias. Real defect re-formed at EC-23.
- **K-G — L-10's SC 2.5.8 conformance failure**: the target is the slider ROOT (reka SliderImpl.js:46-53, read) — ≥24px both axes; spacing exception besides. Demoted (EC-47).
- **K-H — D-10's "named only by hover title" framing**: the governing file's own a11y tree prints computed names for all three; residue = weak last-resort naming, carried in the X-W4 family.
- **K-I — XC4-3's "ten guarded sites; line 124 the sole outlier"**: ParseEchoReadout.vue:38/:44 (read) are two more bare sites. The BLOCKER framing rode this count.
- **K-J — L-24's reachability premise ("gated on cssColor truthy")**: `serializePickerColor` ends in `valueOrThrow` — "" is unconstructible; the provide is unconditional. The real gate is provider presence.
- **K-K — the L axis's inset-ring cure (L-12 P2 / L-17 A3)**: still inset → still paints on the ramp against a plate-certified ink; `contrastInkFor` certifies one fill, not a ramp; the born-RED gate would go green on a ring failing along most of its length. XC-1's direction governs.
- **K-L — r5's "demo/ui is 20 files"**: 19, all index.ts (this seat's find). Load-bearing for the A11 gate as written.
- **K-M — L-16's provenance block ("ink.ts:107-112")**: a fabricated composite — CERTIFY_HEADROOM at :17 and the defect at :137, 113 lines apart (read); re-cited by r4/r5 without re-derivation. The FINDING is confirmed at :137 (EC-12). Citation-inheritance event, recorded.
- **K-N — L-13's ErrorBoundary citation (:104)**: live at :84; re-asserted unchanged r2→r5 — the §18 grep was not re-run.
- **K-O — L-3's denominator ("1 of 3")**: five interactive controls; only Reset honours it (EC-6).
- **K-P — XC4-3's BLOCKER severity**: the branch is unreachable on every shipped route (K-J's chain); latent-portability MINOR (EC-21).

This seat's own kills/corrections — **3**, all against the readers:

- **K-Q — reader-2's "21 return members"**: the literal returns **19** (read whole); 15 consumed, 4 dead — the finding survives with its numbers fixed (EC-39).
- **K-R — reader-1's "the visible-label half … is not blocked … by the pin"**: the in-file label cure edits the pinned bytes (hash re-verified this session). The unpinned alternative is parent-composed labelling; the row routes with that correction (EC-44).
- **K-S — reader-1's corpus-integrity claim, softened in one arm**: pass 3 did not "downgrade despite the PNG" so much as honestly bound it — the PNG is WebKit *emulation*, and pass-3:557-562 says so. The drop is pass 4's. Row survives (EC-20) with the fairness note.

**Motion-law compliance**: no motion-quarantine FALSE row is carried. M-32/M-33 concern other components; M-31 likewise; M-34/MQ-1 live at the pane level (wb-extract-pane XP-12 → X-W10) and no row here re-books them. The D axis's own reduced-motion negative (§6) stands unchallenged.

## Superlatives — provenance verified by this seat

1. **spectrumLuma.ts:1-33** — one function, one threshold, three named consumers, and the rule in the doc-block: "Share the function, never copy the constant." The exact instrument EC-3 needed and did not reach for — its existence converts a judgement into a defect with a named cure.
2. **useExtractSession.ts:7-10 + src/quantize.ts:132-139** — a documented refusal of the library's own API ("the library's `dominantColor()` re-quantizes and is the wrong tool" — and it does: a second full k-means at k:5, read). A consumer that reads its dependency's implementation, declines the convenience, and records why.
3. **quantize-worker.ts:20-44** — a failure-total, typed worker boundary: whole-handler try/catch, a discriminated response union, `satisfies` at BOTH postMessage sites. The worker is better defended than its consumer (the reason wb-extract-pane's K-12 exists).
4. **ink.ts's failure posture** — every Result unwrapped with an explicit coded throw; `contrastInkFor` returns `null`, never a guess (:158-174, read). The opposite of the masking-fallback idiom, in the same directory that gets criticised for it.
5. **foundation.css:648-700** — the two-tier forced-colors POLICY is right and self-documenting (colour surfaces keep colour, chrome adopts system colours, `:where()`-zero-specificity outline restore). EC-8 is a roster-membership bug inside a well-designed policy — the architecture is the superlative, the row is the bug.
6. **ExtractWorkbench.vue:87-101/:158-167** — the empty state is designed, not absent: the ghost shows the instrument's output SHAPE, re-segments live under the k slider before any image exists, swaps to skeleton IN PLACE, and carries its AT caption separately from the error register.
7. **challenge-D-design.pass-1** as a document — D-3's range-element probe, D-4, D-6: the strongest D pass in the corpus, and the one the governing file dropped (EC-20's sting).
8. **challenge-L-library.md:458-509 (L-5)** — a seat retracting its own headline after computing the accname, then converting the retraction into an instrument defect (the `namelessButtons` predicate). The best single act of judgement in the corpus; its sibling instrument catch now extends to `smallTapTargets` (EC-47).
9. **The dogfood closure** (reader-2, spot-verified): 49 demo imports across exactly five published subpaths, zero deep reaches, no root export to cheat with, and vite.config.ts GENERATING the alias set from the exports map with the prefix-match bug documented at the site. Drift is impossible by construction — even as its neighbouring comment (EC-32) tells one lie.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **D4-1's rendered focus ratios** — 1.03–1.14:1 across four arms; the 1.976:1 producer-default counterfactual. Mechanism source-certain (EC-19); numbers live-only.
2. **D-2's rendered rail-vs-ground** — 3.333:1 light / 6.385:1 dark; dark chroma collapse C 0.218→0.021.
3. **THE CROSS-AXIS TENSION (highest-value single probe)**: D renders 3.333 where L-16 proves the code requests 4.25 — if both hold, `surfaceLightnessNow`'s modelled referent misses the painted ground by ~0.9 ratio points and EVERY quantitative ink claim in both axes shares the bias. One live A/B: modelled referent vs sampled ground.
4. **XC4-1/C2-2 rates** — 225 detached style mutations, 7.7 re-renders/s, 17,435 chars/s, 1.30ms/eval. Mechanism confirmed (EC-18); magnitudes live-only.
5. **XC-1's 401-column ring-vs-ramp sweep** (1.002–2.673 light / 6.992–1.845 dark) — paint-order mechanism confirmed; values live-only; note the sweep interpolated in sRGB.
6. **EC-43's mid-flight intermediate** — decidable WITHOUT a browser: a pure unit test over the sRGB interpolation of two `certifyAccentInk` endpoints. Proposed here; no seat had.
7. **The D4-6 decider** — one live font-metric read: is the resolved Fira Code advance 0.6em (pass 3 + the type system) or not (pass 4's Range figures)?
8. **EC-8's rendered blank under REAL WHCM** — the pass-1 PNG is WebKit emulation (pass-3's honest caveat); a real forced-colors cell belongs in X-W1's golden matrix.
9. **L-5's instrument scope** ("18 rows / 7 routes") — REPORT.md uncorroborated by any reader or this seat.
10. **L-10's `--slider-thumb-size` unset premise** — well-attested (3 probes + REPORT.json), glass-owned, not re-proven statically here.
11. **L-22's computed-colour half** — the twin's `rgb(112,89,66)` vs the subject's `oklch(0.447121…)`; structural half fully verified.
12. **Pin-hold provenance**: the hash is re-verified at `1994eb53`, but whether the glass BJ W4 hold's ruling authority has been formally transposed into the X formation (X-W0.j census as the SOLE release event) is a formation-boundary question this seat flags, not answers.

## DISSENT / method notes — preserved, not ruled away

1. **Both reader rounds were strong and complementary**: reader-1 (design-and-usage) owned the corpus-vs-corpus work — every one of its nine kills survived re-derivation, and its four restored pass-1 rows are the roster's product heart (EC-8/9/10 + EC-3). Reader-2 (library-and-code) owned the tree — its seven kills all verified, three by this seat's own re-runs, and its citation-forensics (K-M, K-N) caught a citation-inheritance class the fleet should treat as systemic. Their shared weakness mirrors wb-extract-pane's readers: **neither read the X wave files**, so neither knew CC-105 already owns the track sites, that X-W4.g is census-gated, or that the pinned file has no owner at all — the routing facts that decide what any of this is FOR.
2. **The D pass-4 seat's pattern, named**: pixel readback is a genuinely better instrument, and it is what produced D4-1's causal counterfactual — but three of its five "new" findings degraded under adjudication (mechanism false, novelty false, adjudication inverted), each time by preferring its own live numbers over the shipped bytes and the prior passes' record. An instrument upgrade is not an evidence upgrade when the mechanism is legible in source.
3. **Severity instinct, unresolved and preserved**: a case exists that EC-1+EC-6+EC-9 together constitute one BLOCKER-class "the instrument lies at rest" identity (asserted-but-invisible grouping, a standing-down contract that stands up, a readout that shows the ask). This seat kept the corpus grain — three MAJORs, ids preserved — and notes the merge option for the next formation.
4. **What this seat could not do under the commission**: no browser, so nothing in §residue moved to CONFIRMED here. Every disposition above is decidable from source + the shipped dist, and was.

## Closing verdict

**ADJUDICATED RED — the corpus's direction confirmed, its three BLOCKERs all cut down, its dropped best findings restored.** 47 rows survive (0 BLOCKER · 20 MAJOR · 24 MINOR · 3 INFO); 19 killed claims with written refutations (16 reader kills upheld, 3 arbiter corrections against the readers). Terminal dispositions: **4 to X-W4** (eyedropper keyboard family, the rail twin, the fira-code shadow, the naming fold) + **3 to X-W4.g via CC-105** (the track-material cluster: ring, thumb/value-carrier, tier split — census-gated), **2 to X-W5** (parked-hot subscriber beside XP-1), **1 to X-W7**, **5 to X-W8** (ink headroom + floor homes as addendum, dead surface, demo/ui fold, tsconfig fold-decline), **3 to X-W9** (module-private contrast, manifest cycle, quantize surface), **1 to X-W10** (plate-ink fold), **2 to X-W0/X-W1** (dead lint lattice fold; forced-colors witness + instrument corrections), and **17 to NO-WAVE-OWNER** — of which ELEVEN are pin-gated edits to a file no X wave owns. That compound fact — the corpus's densest consumer-defect surface is byte-frozen behind a census event whose receiving wave does not name the file, while its session composables extend the cluster no wave's bounds touch — is this apotheosis's headline for the next formation boundary.

— end of apotheosis. The challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
