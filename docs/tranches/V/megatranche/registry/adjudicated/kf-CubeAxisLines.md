claude-fable-5

# kf-CubeAxisLines — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Substrate**: keyframes.js `master` HEAD `8281638c` (re-confirmed this session; **worktree DIRTY, 252 rows** — cube-scene dirty set re-measured: `matrix-editor/MatrixEditor.vue`, `matrix-editor/transformMath.ts`, `matrix-editor/useTransformState.ts`, `useCubeDemo.ts`, plus `package.json`/`package-lock.json`; the target file and the rest of the primary read set — `CubeAxisLines.vue`, `CubeTarget.vue/.css`, `OrbitalDrag.vue`, `useOrbitalPointer.ts`, `style.css`, `layout.css`, `DESIGN.md` — are CLEAN; every HEAD-load-bearing cite on a dirty file was read via `git show HEAD:`) · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two fresh hostile re-reader returns over the component's three challenge documents — reader-α (**design-and-usage**: 20 confirmation cells · 6 kill groups · 5 misses · 8 superlatives · 7 unproven) and reader-β (**library-and-code**: 22 confirmation cells · 7 kill groups · 5 misses · 7 superlatives · 7 unproven). The challenge documents (`audit/kf-components/CubeAxisLines/challenge-{D-design,L-library,C-consumption}.md`) were re-read by this seat at every disagreement zone and every load-bearing cite (D:5-7, D:53-84, C:120-131, L:5, L:665-674 verbatim this session).
**Supersession (recorded, nothing lost)**: this record supersedes the same-day r1 apotheosis at this path, which adjudicated a **different** reader pair (r1's reader-A CONFIRMED the D-2 restart mechanism and killed C·D-4; r1's reader-B CONFIRMED F-1 on contaminated greps; r1's readers discovered the registry root and the graph rotation). The r1 roster identities **KF-AX-1..30** are preserved for life (anti-rename); r1's rulings, kills (register #1-12), superlatives, folds and SS-13 queue are carried where this corpus does not touch them, and are **corrected in exactly two cells** (rulings 3 and 8 below: the forced-colors keying claim and KF-AX-9's cure-benefit sentence). Two new rows are admitted (KF-AX-31/32).
**Method (incredulity)**: both readers were attacked before either was adopted. Every verdict-level disagreement (the D-4 grading, the D-3 and C·D-2 severities, the forced-colors keying, the fallback count, the D·S-3 mechanism-as-superlative) was re-derived against the bytes with this seat's own instruments; the r1 record's two solo discoveries (registry root, graph rotation) were re-reproduced at HEAD before being used as instruments against this corpus's readers; suspiciously-familiar cells (the ×4 fallback count, the "byte-identical" prediction, the F-1 greps) were re-measured, not spot-trusted. Zero browser probes (probe parsimony; read-only law). Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are forming; routings are agglomeration input (M-25). **Sole write = this file.**
**This seat's own instruments (all static + git)**: `wc -l CubeAxisLines.vue` → **90**; `var(--axis-active, 0)` → exactly **3** (:53/:55/:56); **HEAD** `useCubeDemo.ts:104-128` (`changeGraphPerspectiveAnim` = 650 ms ease-out-back `rotate3d(0,0,0,0deg)` → `rotate3d(-1,1,0,30deg)`, `.setTargets(graphEl)`, PRM branch writes the attitude inline at :124 — the KF-AX-4 decider, re-verified whole); `CubeScene.vue:214-216` (onBeforeUnmount stops **only** `animationGroup` — KF-AX-19 re-verified); `demo/components/instrument/transport/AnimationControlsGroup/useControlsKeyboardShortcuts.ts` docblock verbatim ("Every binding routes through the ONE existing glass-ui `registerShortcut` registry (not a second window listener) … editable-target skip + … KeyboardShortcutsModal" — KF-AX-1's root re-verified); `useOrbitalPointer.ts` ~:117 (drag-path latch) + ~:155 (**wheel**-path latch — the sixth operation) + `updatePressedKeys` unguarded; **installed glass-ui 7.0.0** `dist/styles/utilities/a11y-overrides.css` whole (universal `*:not([data-allow-motion])` PRM rule; **class-keyed** forced-colors block `.focus-ring/.dock-*/.field-control/.input-pill/.hairline-accent/.glass-dock`) **and** `dist/styles/accessibility.css` whole (a **second, ARIA-keyed** forced-colors block `[aria-current]/[aria-selected]/[aria-pressed]/[aria-checked]/[data-state]` + a prefers-contrast twin — the ruling-3 decider); git state: HEAD `package.json:70-72` = `"@mkbabb/glass-ui": "6.0.0"` under **`optionalDependencies`**, HEAD lock glass-ui ×3, worktree diff removes both, installed = **7.0.0**; `layout.css:8-11` (tokens "OUTSIDE @layer so they override glass-ui's incidental same-named tokens (authoritative for the demo)" — verbatim) + the `--z-behind` decl; `style.css:20-42` whole (the self-contradiction, the stale raw-−10 sentence, AND the `proof:brittleness` citation in one block); `ls scripts/gates/` → `surface`,`visual` only + `proof:brittleness` grep → 3 stale prose sites + dissolution commit `70b32501` confirmed (KF-AX-31 decider); `DESIGN.md:87-93` (§4 dock antecedent + "when that move lands in U.B"), `:149-155` (§7), `:160-175` (**§8 OD-U9 verbatim: "Cube exposes the active channel plus a live matrix cell or Euler triple"** — the α-miss-2 decider); census `lane-frontend.md:253`/`:442` (z-index rows) + `§6.5` header/Gaps verbatim ("defers to glass-ui's `transitions.css` PRM block … unverified statically" — KF-AX-32 decider); `OrbitalDrag.vue:320` `{ deep: true }` (both challenge docs' `:321` corrected — β right); `occlusion.mjs:33-37` verbatim (incl. "extend toward the vanishing point" — the repo's own script describing the settled 3D frame); `SquareScene.css:110` (second `--z-behind` consumer); tabindex census → only `App.vue:87` (programmatic host, :381 comment); `CubeTarget.css:144-145` (`.face-numeral { z-index: var(--z-content) }` **inside** the self-context `.cube-side` faces at :86-103 — the ruling-7 decider); `CubeTarget.vue:12` (static `preserve-3d` class, kill-#4 re-verified) + `:224-226` (S.G3-S2 verbatim); `App.vue:365-371` (the backdrop-root keystone verbatim — α-miss-4's cite); MatrixEditor **HEAD**:14 `<Input` (from `@mkbabb/glass-ui/forms`, :108).

## Scoreboard

**32 adjudicated roster rows — 0 BLOCKER · 4 MAJOR · 17 MINOR · 11 INFO** (r1's 30 identities preserved, zero re-graded; two new MINOR rows admitted from this corpus's misses). **18 killed-claims register entries** — r1's 12 carried and re-affirmed where touched, plus 6 from this corpus (two against reader-α, one against reader-β, one against BOTH readers' top geometric grading, and **two against r1 itself**). **10 missed rows ADMITTED** (5 α · 5 β: two become new roster rows, one re-affirms the F-1 fold with fresh instruments, seven sharpen existing rows or the SS-13 queue). **Superlatives sustained** (r1's ten) plus two reader-credit entries. The corpus's only BLOCKER (D·D-1) stays dead — **both** readers of this corpus killed it independently and correctly, an improvement over the r1 corpus, where one reader confirmed it.

The r2 headline: **this reader generation fixed r1's transition-mechanism split (both readers now carry the correct after-change-exclusion model) and independently reproduced the F-1 substrate correction — but repeated the corpus's two deepest blindnesses whole.** Neither reader found the registry root (KF-AX-1) or the mount-time `rotate3d(-1,1,0,30deg)` (KF-AX-4); both re-confirmed D-4 as MAJOR "source-certain" in an untransformed frame — while both had `useCubeDemo.ts` open, citing it for its matchMedia line, sixty lines below the rotation. Four hostile re-readers and three challenge axes have now missed the same two truths, each sitting in a file the misser had already opened.

## Ruled disagreements & corrections (this seat's actual work)

1. **D-4 / L·D-4 grading — BOTH readers' MAJOR rescoped; KF-AX-4 re-verified at the bytes.** Reader-α confirmed "the projection is source-certain" (MAJOR, first-listed); reader-β confirmed the same algebra ("Every point of the rotated line is then (0,0,t) → projects to (0,0)", MAJOR). This seat re-read **HEAD** `useCubeDemo.ts:104-128`: `.graph` — the lines' parent, containing block (by `perspective`), and perspective root at once — is driven to `rotate3d(-1,1,0,30deg)` on **every mount** (650 ms sweep from identity; the PRM branch writes the attitude inline), wired at `CubeScene.vue` onMounted. In the settled attitude the composite `M_graph · P(1200) · rotateY(90°)` does not collapse the line to the view ray; the degeneracy algebra is source-certain **only for the transient mount frames**. Both readers' cells rescope exactly as r1's ruling did: **KF-AX-5 MINOR + fully UNPROVEN-NEEDS-LIVE**, the Z-triptych screenshot first in the SS-13 queue. The α cell's grading language ("MAJOR … source-certain") and the β cell's are both **killed as graded** (register #13); the substance survives frame-stamped. Corroborating detail this seat adds: the demo's own observability script (`occlusion.mjs:34-35`) describes the lines as "extend[ing] toward the vanishing point" — the settled-frame behaviour, written down by the house and cited by nobody in either generation.
2. **F-1 — reader-β's kill SUSTAINED ≡ r1's fold; and this time the reader did the arbiter's work.** β killed F-1 at `git show HEAD:` (optionalDependencies `6.0.0` at :71; lock ×3; the absence is an uncommitted worktree edit) and independently derived the sharper truth (β-miss-1): declared **6.0.0-optional** vs installed **7.0.0**, so every vendor fact in the corpus — the PRM kill-rule, `.preserve-3d`, `--z-behind`, the ease token, the duration ladder — was read from an unpinned tree, and `npm ci --omit=optional` still breaks the build at `style.css:3`. All re-reproduced by this seat's own git reads this session. **One identity, owned by census F-1 = kf-CSSCodeEditor KF-CE-32, folded, never re-booked** (r1's booking to **KF.W0** §B-12 stands, with the optionalDependencies failure-shape note). β also correctly convicted the D challenge's provenance header (":6 'file clean' — false as stated; 252 dirty rows") and flagged the two dirty cited files (`useCubeDemo.ts`, `MatrixEditor.vue`) — both confirmed by this seat's `git status`; β's practice of quoting MatrixEditor at HEAD is the correct discipline and is adopted as the r2 norm.
3. **Forced-colors keying — reader-β RIGHT, r1 CORRECTED (r1 correction #1).** r1's KF-AX-17 cell ruled "class-keyed, not aria-keyed; D's aria-keyed citation was of a different-era file, immaterial." This seat read both vendor files whole: `a11y-overrides.css` carries the **class-keyed** block AND `accessibility.css` carries a **second, ARIA-keyed** forced-colors block (`[aria-current]:not([aria-current="false"]), [aria-selected="true"], [aria-pressed="true"], [aria-checked="true"], [data-state="checked"|"on"]` → `border-color: Highlight !important`, plus an `[aria-invalid]` arm and a prefers-contrast twin) — in the installed 7.0.0, not a different era. Reader-β's cell listed both surfaces and is exact. **Verdict unchanged** — the three bare `.axis-line` divs match neither aria states nor glass classes, so KF-AX-17 stands as booked — but the r1 evidence cell is corrected, and the correction is a fresh reader out-reading the r1 arbiter on one cell: recorded as the anti-inheritance counter-example.
4. **D-3 severity — α MAJOR vs β MINOR: r1's MAJOR SUSTAINED, β's demotion preserved as DISSENT.** Both readers reproduced the arithmetic independently to the digit (α: 2.689/2.977; β: 2.68/2.99 — third and fourth independent reproductions). β's ground for MINOR ("contestable for a demo stage guide"; the decorative-exemption reading) is r1's own recorded contestability — which is why KF-AX-3's severity rides the **token-discipline gap at the information-carrying state** (sole declaration site, no dark arm, `--accent-kf` nineteen lines later using `light-dark()`), not a bare WCAG conviction. No single-theme fix exists; each theme fails on a different axis. MAJOR stands; the effective-backdrop measurement stays SS-13.
5. **C·D-2 severity — β's MINOR grade CORRECTED; KF-AX-2 stays MAJOR.** β graded the false-semantics row MINOR ("C's MAJOR is inflated for a decorative demo egg; D's INFO is closer"). β's cell examines only the modifier branch — it never reached the **wheel** path (`useOrbitalPointer.ts` ~:155 routes wheel through the same latch: the sixth operation, re-verified this session) or the armed-vs-active limb (the line lights on bare keydown at idle). One lit line meaning any of six operations, against prose that says "single-axis rotation" twice, is the fully-informed row r1 booked; β's grade was made against a third of it. C's fair-counter ("lock = which axis") remains preserved in DISSENT.
6. **The fallback count — reader-α's cell is a THIRD-generation citation-inheritance event.** α's confirmed cell reads "Four `var(--axis-active, 0)` fallbacks" while its own proof text lists exactly three anchors (:53, :55, :56). The tree says **3** (this seat's grep, twice-run). Lineage: L's challenge wrote ×4 → r1's reader-A repeated it → this corpus's α repeated it again, two corpora after the correction was first published. β's cell carries no count and is clean. Substance unaffected (KF-AX-21); the event is fleet evidence per the ColorSpaceSelector DISSENT-1 precedent.
7. **β-miss-3 — the stacking-context proof: ADOPTED; KF-AX-6's cited-contract half moves from needs-live to source-decided.** β's derivation, re-verified by this seat at the bytes: the comment (:64-67) cites `--z-behind < --z-content`, but the only `--z-content` consumer in the die is `.face-numeral` (`CubeTarget.css:144-145`), which sits inside `.cube-side` faces that each carry a `transform` (:86-103) and are therefore their own stacking contexts — a −10 in `.graph`'s context and a 10 nested two contexts deeper are **not comparable quantities; the cited inequality is ill-formed before any 3D depth-sorting argument**, and within `.graph`'s own context the sole competitor (OrbitalDrag's container) declares no z-index at all. This argument is robust to the KF-AX-4 frame question (it is about the comparison, not the paint outcome). C·D-6's `z-index: 999` probe stays SS-13 **for the render only**.
8. **β-miss-2 — the opacity-grouping cure rider: ADOPTED; the corpus's unanimous cure claim AND r1's own cure sentence corrected (r1 correction #2).** All three axes and r1's KF-AX-9 prescribe gating `filter` under `.axis-line--locked` and state the benefit as removing the resident grouping/stacking cost ("zero cost, and the mobile fleet stops paying"). β's point, spec-verified: `opacity: calc(0.45 + …)` computes to **0.45 at rest** (:53), and `opacity < 1` is itself a grouping property and stacking-context creator (css-transforms-2) — so gating the filter removes **only the drop-shadow pass**; the stacking context, the containing block, and the forced used `transform-style: flat` persist, and cannot be removed, because the 0.45 rest register is the design. KF-AX-9's cure text now reads: gate the filter (still worth doing — the pass and the three permanent shadow buffers go; the touch fleet stops paying **for the filter**), and any wave reporting the fix must not claim the de-grouping benefit. Second-order consequence adopted with it: the line is composited as a plane in **both** states regardless of the filter cure, which feeds ruling 9's coupling.
9. **β-miss-4 — the grouping×geometry coupling: ADOPTED as a KF-AX-5 rider, with its own frame-blindness corrected.** β joins the resident grouping property to the Z degeneracy: a grouped `.axis-line` rasterises into a buffer composited as a plane, so edge-on the mapped quad has zero area and the likely residue is **nothing**, not the "sub-pixel needle"/"vertical flare" the D and L challenges predict from border-edge algebra — and gating the filter changes what `.z` renders, so the D-4 and D-8/D-1 cures cannot be scheduled independently. Adjudicated in — **but β's "likely nothing at all" is itself computed in the untransformed frame** (edge-on holds only at the mount frames; in the settled 30° attitude the plane has projected area). Post-ruling-8, the plane-compositing premise survives the filter cure via opacity, so the coupling is durable. Net: the SS-13 #1 triptych now decides **three** rows (KF-AX-5 residue, KF-AX-6 render, the coupling), and both this rider and the challenge predictions are frame-stamped.
10. **D·S-3 — β's kill convergent with r1 register #9; reader-α BANKED the killed mechanism as a superlative.** β independently refuted "one declaration between working and catastrophically broken" (hit-testing walks paint order; OrbitalDrag wins over its own footprint; the residue is stage-outside capture, not a dead drag) — the same outcome r1 ruled. α's superlative list carries the catastrophe verbatim ("would intercept every pointer event in the cube scene and kill the orbit gesture outright"). α's cell is corrected; the superlative stands at r1's reduced strength (stage-wide input discipline), with reader-B-r1's revival condition intact.
11. **Convergences recorded without ruling (both readers + r1 agree)**: D·D-1 dead by its own falsifier (both readers re-ran the 23-file grep and walked the cascade end-to-end — and both flagged the 6.0.0/7.0.0 caveat r1 booked); the D-2 restart mechanism dead on the after-change exclusion — **this corpus's cleanest improvement over r1's**, both readers citing the correct clause, C·D-4 restored at MINOR (KF-AX-8) with C's "byte-identical" falsifier prediction re-endorsed; D-5's state-dependence dead (static `preserve-3d`, re-verified :12); L·D-12's sole-consumer census dead (`SquareScene.css:110`, re-verified); the L header's 91 lines and the 55 %-prose arithmetic dead (90 / 24 comment lines); D-9/D-10's codex grounds dead (§4's dock antecedent + "when that move lands in U.B", re-read this session — demoted to taste/INFO exactly as r1 booked at KF-AX-20 and register #10/#11); the `{deep:true}` seam liveness non-finding (C·§0) re-endorsed, with β's line correction (:320, not :321) adopted.

## Adjudicated defect roster — 32 rows (identities for life; r2 status per row)

Routing law unchanged from r1: **AXISLINE-UNIT = NO-WAVE-OWNER** — one proposed wave-surface ("the axis-lock reveal made honest") for SS-1/SS-2 spec authoring to consume, sequenced after the **KF.W4** vue-tsc gate, every geometric assertion written against the settled `rotate3d(-1,1,0,30deg)` frame; discrete rows to **KF.W0** (census/records re-anchor), **KF.W6** (tokens/z-contract), **KF.W9/SS-13** (live witnesses). Line anchors are HEAD `8281638c`; re-resolve before any cure (D-19).

| id | sev | row (compressed) | r2 status | disposition |
|---|---|---|---|---|
| KF-AX-1 | MAJOR | window-latch bypasses the demo's ONE-registry law; registry ships both cures (`allowInInput`, keyup events, modal) | defect re-confirmed MAJOR by both readers; **root missed by both** — re-verified verbatim by this seat | AXISLINE-UNIT (first; cure upstream in orbital-drag/) |
| KF-AX-2 | MAJOR | reveal's semantics false three ways (rotate/translate/scale; armed-vs-active; the **wheel** sixth op) | both re-confirmed the modifier limb; β's MINOR grade corrected (ruling 5); wheel re-verified | AXISLINE-UNIT + prose/prop decision; labels rider (α-miss-2's deuteranopia note: X-red/Y-green with no text labels) rides J-lane C6's ask |
| KF-AX-3 | MAJOR | `--axis-*` palette theme-blind; LOCKED state fails 3:1 in each theme on a different axis | 3rd+4th independent arithmetic reproductions; MAJOR sustained over β's demotion (ruling 4) | KF.W6 (token cure); backdrop → SS-13 #5 |
| KF-AX-4 | MAJOR | corpus-method defect: all geometry argued in a frame the app shows only during the mount sweep | **missed by BOTH readers; re-verified at HEAD by this seat (ruling 1)** — now a 2-generation, 7-document miss | booked; every AXISLINE-UNIT geometric assertion + SS-13 capture frame-stamped |
| KF-AX-5 | MINOR | Z-line degeneracy real only at mount frames; cure rider (tilt would make Z lie) | both readers' MAJOR rescoped (ruling 1); + β-miss-4 grouping×geometry rider with frame stamp (ruling 9) | SS-13 #1 FIRST (now decides 3 rows), then AXISLINE-UNIT design decision |
| KF-AX-6 | MINOR | `z-index: var(--z-behind)` + 4-line contract citation inert | **sharpened: cited inequality ill-formed across stacking contexts — source-decided (ruling 7)** | AXISLINE-UNIT (truthful comment or removal); render probe SS-13 #2 |
| KF-AX-7 | MINOR | ":58-59 PRM wrapper" false; real guard = vendor universal rule, which SPLITS the reveal | both re-confirmed; both flagged the 6.0.0-vs-7.0.0 dependency of the guard (rides the F-1 fold) | AXISLINE-UNIT (author the block or state the reliance) |
| KF-AX-8 | MINOR | C·D-4 restored: driver + derived channels all transitioned; driver feeds nothing; `--lit` house idiom deviated from | **both readers derived the correct mechanism independently** (vs r1's split); falsifier prediction re-endorsed | AXISLINE-UNIT (driver-only list); settle-time SS-13 #4, prediction ~180 ms |
| KF-AX-9 | MINOR | resident non-`none` filter ×3 at 1000vw; 100 % cost / 0 % function on touch | **cure-benefit corrected (ruling 8, r1 correction #2)**; + α-miss-4 scope sharpening (1000vw guarantees intersection with every glass backdrop footprint; App.vue:365-371 keystone) | AXISLINE-UNIT (gate filter — pass only); magnitude SS-13 #3 |
| KF-AX-10 | MINOR | touch-parity gap + discoverability mis-attribution + unresolved egg-vs-affordance intent | **doubly sharpened**: α-miss-2 — DESIGN.md:172-174 (OD-U9) already ASSIGNS "the active channel" to the ruled `.stage-whisper`; the reveal is a second encoding shipped ahead of the ruled one, uncited in 24 comment lines; α-miss-3 — the affordance needs keyboard AND pointer simultaneously (latch consumed only in drag/wheel paths — re-verified; nothing in the subtree focusable — tabindex census run), so keyboard-only users lose the operation itself, WCAG 2.1.1 shape | AXISLINE-UNIT + explicit product-intent decision for SS-1/SS-2; §7 legend rider stays U.B8's; reconcile the two encodings when U.B8 lands |
| KF-AX-11 | MINOR | `--z-behind` demo re-declaration vs vendor token; T-F3 relocated-not-applied; style.css self-contradiction | **mitigation rider adopted (β)**: `layout.css:8-11` documents the shadow as deliberate override policy ("OUTSIDE @layer … authoritative for the demo") — re-verified verbatim; the row becomes a policy-contradiction to RESOLVE (two written laws disagree), not a stray to delete | KF.W6, sequenced with the F-1 reconciliation |
| KF-AX-12 | MINOR | census wrong about this file at its own HEAD twice (`raw z-index:-10` ×2), inherited from stale prose | both readers re-confirmed independently; census rows re-read this session | KF.W0 (census re-anchor); prose sites ride KF.W6 |
| KF-AX-13 | MINOR | `border: 1px dashed` on height:0 = 2 px double-stroke + end caps; carries KF-AX-3's arithmetic | both re-confirmed (α adds the box-sizing derivation) | AXISLINE-UNIT (`border-block-start`) |
| KF-AX-14 | MINOR | one boolean, two channels; inline `--axis-active` un-overridable by any media query — obstructs the PRM/forced-colors cures | both re-confirmed; α carries D's obstruction limb (the sharper one) | AXISLINE-UNIT (`&.axis-line--locked { --axis-active: 1 }`) |
| KF-AX-15 | MINOR | 180 ms ×3 off the vendor duration ladder; same declaration consumes the ease token | both re-confirmed (β enumerated the full ladder; C's two-member-pattern caveat carried) | KF.W6 |
| KF-AX-16 | MINOR | silent-drift bundle: structural prop re-declaration vs `Pick<PressedKeys,…>`; hand-unrolled divs vs the `axes` tuple; `--axis-w` live; zero coverage | both re-confirmed (β adds the KISS counter-argument, preserved) | AXISLINE-UNIT after KF.W4 |
| KF-AX-17 | MINOR | no forced-colors handling: hue coding collapses while `filter` haloes survive | both re-confirmed; **evidence cell corrected (ruling 3)** — vendor has BOTH aria-keyed and class-keyed blocks; `.axis-line` matches neither | AXISLINE-UNIT; render SS-13 #6; demo-wide row stays KF-CE-13's |
| KF-AX-18 | MINOR | dead scoped-slot seam; comment claims the emit's job; slot structurally unreachable for its stated use | both re-confirmed (full-tree greps); β's `:320` line correction adopted | AXISLINE-UNIT (delete the seam) |
| KF-AX-19 | MINOR | `changeGraphPerspectiveAnim` = the scene's one engine animation with no teardown | untouched by this corpus; re-verified this session (:214-216 stops only the group) | AXISLINE-UNIT (scene-scoped) |
| KF-AX-31 | MINOR | **NEW (β-miss-5)** — the z-contract's cited enforcement is a dissolved gate: `style.css:38-39` says "`proof:brittleness` gates against drift"; `ls scripts/gates/` → `surface`,`visual` only; dissolution commit `70b32501`; the same three-line block also carries the stale raw-−10 sentence (KF-AX-12's) — a comment chain pointing to a contract pointing to a deleted enforcement, two falsehoods deep | verified whole by this seat | KF.W6 (recut the z-contract block in one motion with KF-AX-11/12's prose); KF.W0 rider (gate-inventory truth) |
| KF-AX-32 | MINOR | **NEW (α-miss-5)** — census §6.5 "13 PRM enforcement sites" is a roster of LOCAL guards misread as COVERAGE: the vendor's universal `*:not([data-allow-motion])` rule reaches every demo element, so the roster's two "delegation … unverified statically" gap rows (TypingDots, KeyframeTimeline — the latter citing the wrong vendor file) actually hold, and D·D-1's BLOCKER ground ("this component is the regression, not the norm") was manufactured by the same mis-scoping | census text re-read this session; the misreading is the exact mechanism of the corpus's only false BLOCKER | KF.W0 (re-scope §6.5 in the same motion as the z-index rows) |
| KF-AX-20 | INFO | 1000vw: codex charge dead; taste residue | both readers killed the codex ground convergently (ruling 11) | AXISLINE-UNIT if touched |
| KF-AX-21 | INFO | fallbacks unreachable on a registered property; file written for two worlds at once | count **3** re-verified; α's ×4 cell corrected (ruling 6) | AXISLINE-UNIT |
| KF-AX-22 | INFO | 3-root fragment, no attrs contract; latent until J-C6 | β re-confirmed at INFO | AXISLINE-UNIT |
| KF-AX-23 | INFO | aria-hidden inconsistency with `face-relit` | untouched; carried | AXISLINE-UNIT |
| KF-AX-24 | INFO | the D-16/C-2 containing-block correction stands; its "every engine" hardening rescoped by KF-AX-4 | both readers adopted the correction inside their D-4 cells — and inherited its frame problem (ruling 1) | recorded; method lesson carries |
| KF-AX-25 | INFO | scoped-`@property` registers document-wide; `--color` flat-namespace pair; initial-value poisons future fallbacks app-wide | both re-confirmed (β re-ran the compiler-sfc proof independently — second mechanical confirmation) | KF.W6 (namespace audit) |
| KF-AX-26 | INFO | reveal is a NAMED EXCLUSION from the demo's only visual gate; `e2e/` does not exist so L's coverage grep half-fired | α's cell independently caught the `e2e/` absence (convergent); exclusion re-read verbatim | KF.W9 surface list; test row rides KF-AX-16 |
| KF-AX-27 | INFO | `var(--ease-standard, ease)` guards only the undefined half (IACVT hazard) | untouched; carried as the S-4 rider | recorded |
| KF-AX-28 | INFO | four verbatim thesis restatements; duplication is the drift mechanism; dramatising arithmetic dead | both re-confirmed the table; both killed the arithmetic (ruling 11) | AXISLINE-UNIT prose collapse |
| KF-AX-29 | INFO | `rotateX(0deg)` identity: two live readings, file silent | β re-confirmed | AXISLINE-UNIT (one comment) |
| KF-AX-30 | INFO | props not reactively destructured per §9.2's positive form | β re-confirmed (correctly notes: deviation, not the banned form) | AXISLINE-UNIT if touched |

## Killed-claims register — 18 entries (r1's 12 carried; 6 from this corpus)

r1 register #1-12 (D·D-1's BLOCKER; the restart mechanism + r1-reader-A's confirmation of it; reader-A-r1's kill-of-C·D-4; D-5's state-dependence; D-4's "PROVEN" grading; L·D-4's frame claims; L·D-12's inversion + census; the arithmetic cluster; D·S-3's mechanism; D-9's codex limb; D-10's codex limb; reader-B-r1's F-1 re-book) — **carried whole**; every entry this corpus touched was re-affirmed at the bytes (rulings 1, 2, 10, 11).

13. **Both r2 readers' D-4 grading cells** ("source-certain"/MAJOR; α's "The projection is source-certain", β's settled algebra) — killed as graded via KF-AX-4, re-verified at HEAD by this seat (ruling 1). Substance survives frame-stamped at KF-AX-5. The miss now spans two reader generations with the deciding file open in both.
14. **Reader-α's ×4 fallback cell** — the tree says 3; third-generation citation inheritance; α's own anchor list contradicts its own count (ruling 6).
15. **Reader-α's D·S-3 superlative rationale** ("one declaration between working and catastrophically broken … would kill the orbit gesture outright") — banked a mechanism r1 had killed and β re-killed; corrected to the reduced-strength form (ruling 10).
16. **Reader-β's C·D-2 MINOR grade** — made against a third of the row's surface; the wheel path and armed-vs-active limb unexamined (ruling 5). The row's MAJOR stands.
17. **r1's KF-AX-17 keying cell** ("class-keyed, not aria-keyed; different-era file") — the ARIA-keyed forced-colors block exists in installed 7.0.0 `accessibility.css`, read whole this session (ruling 3). Verdict intact; cell corrected; credit to reader-β.
18. **r1's KF-AX-9 cure-benefit sentence** ("zero cost, and the mobile fleet stops paying") — overstated: `opacity: 0.45` at rest is itself a grouping property/stacking context, so the prescribed gate removes the filter pass only (ruling 8, β-miss-2). Any wave reporting this cure must not claim the de-grouping benefit.

## Corpus-missed adjudications — 10 rows admitted

- **α-1 (perceptual premise)** — three demotions (D-3 off blocker, D-13 MINOR) and three superlatives across the corpus rest on the dashed→solid tell's perceptibility, unbounded, with the two channels **confounded** (stroke style and opacity flip together). ADMITTED → sharpens SS-13 #8: measure with `--axis-active` pinned so the channels separate; if sub-perceptible, KF-AX-3/17 escalate.
- **α-2 (§8 assignment)** — DESIGN.md:172-174 (OD-U9, ruled direction) already assigns "the active channel" readout to `.stage-whisper`; the reveal is a bespoke second encoding shipped before the ruled one, never citing §8. Verified verbatim. ADMITTED → KF-AX-10 sharpening; the deuteranopia/no-labels limb → KF-AX-2's J-C6 rider.
- **α-3 (modality intersection)** — the affordance requires keyboard AND pointer at once; no key-only constrained-rotation path exists; nothing in the subtree is focusable. Verified (latch consumption sites; tabindex census). ADMITTED → KF-AX-10 widened; WCAG 2.1.1 shape; cure upstream with KF-AX-1's registry adoption.
- **α-4 (paint scope)** — a 1000vw element intersects every glass surface's backdrop-root footprint by construction, so the 180 ms bloom dirties paint under all of them at once — the exact mechanism `App.vue:365-371` names as VERDICT #19 root cause #1 (verified verbatim). ADMITTED → KF-AX-9/SS-13 #3 sharpening; magnitude stays measured-not-assumed.
- **α-5 (census §6.5)** — ADMITTED as **KF-AX-32** (roster above).
- **β-1 (optionalDependencies substrate)** — ADMITTED as the F-1 fold's re-affirmation with fresh instruments (ruling 2); no new row (identity guard).
- **β-2 (opacity grouping)** — ADMITTED as the KF-AX-9 cure rider; r1 correction #2 (ruling 8).
- **β-3 (stacking-context proof)** — ADMITTED into KF-AX-6; cited-contract half now source-decided (ruling 7).
- **β-4 (grouping×geometry coupling)** — ADMITTED as the KF-AX-5 rider, frame-stamped, its own "likely nothing" prediction corrected (ruling 9).
- **β-5 (dissolved gate)** — ADMITTED as **KF-AX-31** (roster above).

## Superlatives — r1's ten sustained; two reader-credits added

r1's superlative register (the dashed→solid tell; `@property` correctly parameterised; the zero-coupling leaf; scene-tier fallback discipline with the KF-AX-27 rider; `pointer-events: none` at reduced strength; codex-measured colocation; D·D-16-rev-2's self-correction; the L·K-4/C·X-3 self-kill pair; C·§4's scoped R1 negative, folded; C·§0's discharged BLOCKER candidate) — **carried whole**; the tells this corpus re-verified (the S-1 channel, the `--lit` idiom, the colocation pointer) all held.

- **READER-β · the `git show HEAD:` discipline** — the only participant across two reader generations and three challenge axes to read the committed tree on a dirty worktree before ruling on packaging, thereby killing F-1's re-book, convicting the D header, and deriving the 6.0.0/7.0.0 drift in one motion. The r2 substrate norm.
- **READER-α · reproducible arithmetic, again** — independent re-derivation of the D-3 contrast rows to the digit from the tree's own tokens (the fourth reproduction), plus the honest refusal to claim the rest register. The confound catch (α-1) is the best new SS-13 input in this corpus.

## UNPROVEN-NEEDS-LIVE — SS-13 inputs (route with KF.W9; every capture frame-stamped per KF-AX-4)

r1's nine-item queue carries with three sharpenings: **#1** (the Z triptych: settled attitude / mount frame / PRM) now decides **three** rows — KF-AX-5's residue, KF-AX-6's render, and the β-miss-4 coupling — and must be shot before AND after the filter gate lands, since the cure changes what `.z` composites; **#3** (resting filter cost) gains α-4's cross-surface framing — probe whether the 180 ms bloom triggers re-rasterisation in the glass chrome's backdrop root, not merely the lines' own raster; **#8** (dashed→solid perceptibility) gains α-1's methodology — pin `--axis-active` so stroke style is measured in isolation from opacity; three demotions and three superlatives re-price if it fails. Items #2, #4 (prediction ~180 ms, falsifier direction flipped), #5, #6, #7, #9 carry unchanged.

## Identity guards (anti-rename) & folds

Carried whole from r1: **F-1 → census F-1 = KF-CE-32** (this record adds reader-β's independent re-derivation — the reconciliation at **KF.W0** §B-12 now has two seats' git reads); **C·§4's R1 negative → megatranche R1** (KF.W2/W3); **KF-AX-3 sharpens J-lane STY-8** (booked once, here); **KF-AX-17 references KF-CE-13**; census §6.3/§6.5 — §6.5's treatment UPGRADED from "sharpened, not contradicted" to a booked correction (KF-AX-32). Fold map for this corpus's readers: α-1→SS-13#8 · α-2→KF-AX-10/2 · α-3→KF-AX-10 · α-4→KF-AX-9/SS-13#3 · α-5→KF-AX-32 · β-1→F-1 fold · β-2→KF-AX-9 · β-3→KF-AX-6 · β-4→KF-AX-5 · β-5→KF-AX-31. Nothing re-booked.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance ledger (fleet evidence)**: α's ×4 fallback cell (third generation, past a published correction); α's banking of the killed D·S-3 catastrophe as a superlative. Against it, the anti-inheritance events: β re-deriving F-1's substrate from git rather than repeating either the census or r1, and β out-reading r1 on the forced-colors keying. Half-inherited cells inside strong returns remain detectable; so does the opposite.
2. **The shared-instrument blindness, distinguished from inheritance**: both readers' untransformed-frame D-4 cells are not citation inheritance — each derived the algebra independently and correctly *for the frame it assumed*. The failure class is assumption-scoping, and it survived a full generation change. The lesson (r1 DISSENT #5) is re-affirmed at n=4 with a sharpening: **both readers had the deciding file open** (each cites `useCubeDemo.ts:164` for its matchMedia line); the rotation was sixty lines above the cited line. Exhaustiveness inside a file is not even exhaustiveness about the file you are citing.
3. **β's D-3 MINOR** (decorative-exemption reading) — preserved; if SS-1/SS-2 rules the stage guide decorative-first, KF-AX-3 re-prices toward β's grade and the token-discipline row survives alone.
4. **β's C·D-2 counter-reading** ("lock = which axis" is exact in all six operations) — preserved with r1's DISSENT #4; the two-word prose edit remains the minimal cure.
5. **r1-reader-A's severity condition** (a measured ~360 ms chase re-prices KF-AX-8 toward MAJOR) and **r1-reader-B's S-A revival condition** (a live hit-test showing a line topmost over the die) — both carried; the falsifiers are written in their directions in the SS-13 queue.

## Closing verdict

**ADJUDICATED — AMBER (r2; supersedes same-day r1, identities preserved).** 32 rows stand (0 BLOCKER · 4 MAJOR · 17 MINOR · 11 INFO) — r1's 30 carried without a single severity change plus two new MINORs (the dissolved `proof:brittleness` citation chain; the census §6.5 coverage misreading that manufactured the corpus's only false BLOCKER). 18 register entries killed with refutations preserved, six new — among them both r2 readers' top geometric grading (rescoped by the mount-time `rotate3d(-1,1,0,30deg)` this seat re-verified at HEAD), and two corrections against r1 itself (the forced-colors keying cell, to reader-β's credit; KF-AX-9's overstated cure benefit, per the opacity-grouping rider). This reader generation improved on the last — both readers killed D·D-1 and the restart mechanism correctly, and β did the arbiter's git work unprompted — yet neither found the registry root nor the rotated frame, the two largest truths about this component, both sitting in files the readers had open. Routings unchanged: **AXISLINE-UNIT (NO-WAVE-OWNER)** for the coherent repair, sequenced after **KF.W4**, frame-stamped per KF-AX-4; discrete rows to **KF.W0** (census re-anchor + §6.5 re-scope + F-1 reconciliation + gate-inventory truth), **KF.W6** (dark arm, ladder, the `--z-behind` policy contradiction, the z-contract recut, namespace audit), **KF.W9/SS-13** (nine witnesses, the Z triptych first — it now decides three rows). Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.

— end of apotheosis r2. The three challenge documents and both r2 reader returns remain immutable beside this file per E-1/E-3; the r1 record's substance is carried inside this one (supersession note, header).
