claude-fable-5

# kf-CubeAxisLines — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt — recorded against L-11/L-14.
**Date**: 2026-08-07 · **Substrate**: keyframes.js `master` HEAD `8281638c` (re-confirmed by this seat; **worktree DIRTY** — `package.json`, `package-lock.json`, and four cube-scene files carry uncommitted edits; every HEAD-load-bearing cite below was read via `git show HEAD:` where the file is dirty) · **Corpus**: 3 challenge documents (D-1..D-20+C-1/C-2/K-1..K-5 · L·D-1..D-16+K-1..K-6 · C·D-1..D-13+X-1..X-3, all opus-5[1m]) against `demo/scenes/cube/CubeAxisLines.vue` (90 L), plus two hostile re-reader returns (reader-A: design-and-usage · reader-B: library-and-code).
**Method (L-14)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (the D-2/L-D-7/C-D-4 transition mechanism; the D-4 projection grading; the D·S-3 pointer-events mechanism vs reader-B's S-A elevation; reader-B's F-1 re-book; the L-D-8 fallback count) was re-derived against the tree with this seat's own instruments; both solo-reader MAJOR discoveries (reader-A's registry root, reader-B's M-1 graph rotation) were independently reproduced at HEAD, not spot-trusted; three suspiciously-repeated cells (the ×4 fallback count, the L-D-12 inversion limb, the F-1 phantom greps) were re-measured and all three turned out inherited.
**Arbiter's own instruments (all static + git; zero browser probes — probe parsimony, read-only law)**: full read of CubeAxisLines.vue (90 L; `wc -l` → 90; `var(--axis-active, 0)` → exactly **3**, at :53/:55/:56); `--z-behind` demo grep → **7 lines / 5 files** incl. the second consumer `SquareScene.css:110`; **HEAD** `useCubeDemo.ts` ~:98-128 (`changeGraphPerspectiveAnim` = CSSKeyframesAnimation 650 ms ease-out-back, `fromVars` `rotate3d 0,0,0,0deg` → `rotate3d(-1,1,0,30deg)`, `.setTargets(graphEl)`, PRM branch writes `graphEl.style.transform = "rotate3d(-1, 1, 0, 30deg)"` inline); CubeScene.vue:204-218 (onMounted `setTargets(cubeEl, graphEl)`; onBeforeUnmount stops **only** `animationGroup`, which HEAD :90-96 builds from rotation/matrix/hover — the graph sweep excluded); CubeTarget.vue:9 (`.graph preserve-3d grid items-center…`), :12 (**OrbitalDrag's static class list carries `preserve-3d` unconditionally**), :67-70 (`face-relit` + `aria-hidden`), :93/:101 (OrbitalDrag closes; the lines render as its **sibling**), :150-163 (axisLock mirror), :195-236 (roll teardown ×2 + the S.G3-S2 touch-parity comment); OrbitalDrag.vue:2-7 (dead slot seam + comment), :40 (emit), :63-76 (`containerStyle` — the inline `transformStyle` merely duplicates the static class), :112-158 (`applyRotation` pre-multiply + `updateAxisRotation` world-basis vectors), :193-212 (the shift/ctrl/meta branch), :265-321 (window keydown/keyup at :276-277; wheel on container; the `{deep:true}` emit watch); useOrbitalPointer.ts:96-129 (drag gated on `isDragging`; ctrl-drag z-roll), :150-176 (the **wheel** path through `handleAxisSpecificInput`; `updatePressedKeys` with no guard); useControlsKeyboardShortcuts.ts:30-33 (the ONE-registry law, verbatim); installed glass-ui `dist/keyboard.js` (`isContentEditable || e.closest(".monaco-editor")` guard) + `dist/composables/keyboard/useKeyboardShortcuts.d.ts` (`allowInInput?: boolean`; `event?: ShortcutEventType` = `"keydown" | "keyup"`, both read verbatim); EditorShell.vue:106 (`KeyboardShortcutsModal` mounted); `scripts/observe/demo/occlusion.mjs:33-37` (the axis-line EXCLUSION note, verbatim); `dist/styles/utilities/a11y-overrides.css` whole (the universal PRM rule verbatim; the forced-colors block keyed on `.focus-ring`/`.dock-*`/`.field-control`/`.input-pill`/`.hairline-accent`/`.glass-dock` — class-keyed, not aria-keyed); `grep -rl "prefers-reduced-motion" node_modules/@mkbabb/glass-ui/dist/styles/` → **23 files** incl. a11y-overrides.css; DESIGN.md:85-99 (§4: the viewport-literal ban's antecedent is the **dock** token set; "design-idioms.css **when that move lands in U.B**"), :127-146 (§6 partition at :131-132; prose-ownership tail at :140-146), :149-155 (§7 touch-discoverability law); viewport-literal census `demo/scenes demo/components` → **45**; style.css:20-42 (the z-contract incl. the stale ":39 raw −10" sentence and the ":24-27" self-contradiction), :105-135 (`--axis-x/y/z/w` fixed literals; `--accent-kf` `light-dark()` 19 lines later); MatrixEditor **HEAD**:14 (`<Input`); lane-frontend.md:253 + :442 (the census's raw-`z-index:-10` rows) + :15/:54/:612 (F-1); **git state**: `master`@`8281638c`; HEAD `package.json:71` = `"@mkbabb/glass-ui": "6.0.0"` under **`optionalDependencies`**; HEAD lock carries glass-ui ×3; the worktree diff REMOVES it; `add20b7e` ("consume Glass 7… registry-only lock") exists on a non-master ref; installed 7.0.0; `e2e/` does not exist, no `playwright.config.*`.

## Scoreboard

**30 adjudicated rows: 0 BLOCKER · 4 MAJOR · 15 MINOR · 11 INFO. 12 killed-claim register entries (incl. 2 kills of this corpus's own readers by this seat, one of them a kill-of-a-kill). 11 corpus-missed items adjudicated IN (5 from reader-B, 6 from reader-A, folded where identity demands). 1 fold to census F-1, 1 fold to megatranche R1 — neither re-booked.**

The corpus's ONLY BLOCKER (D·D-1, the "no PRM guard anywhere" claim with a rev-2 exhaustiveness certificate) **does not exist** — both readers killed it, and this seat re-verified the kill end to end. The severest survivors are the ungoverned window-listener latch (with its registry root that all three axes missed), the semantically false reveal, the theme-blind axis palette at the information-carrying state, and the corpus-wide untransformed-frame blindness that reader-B alone caught.

## Ruled disagreements (this seat's actual work)

| id | reader-A | reader-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D·D-2 + L·D-7 vs C·D-4** (the transition mechanism) | CONFIRMED MAJOR as one row — per-frame cancel-and-restart, "materially longer than 180 ms, doubly eased"; killed C·D-4's mechanism and falsifier | KILLED the restart mechanism; CONFIRMED C·D-4 MINOR ("the driver feeds nothing") | **B wins on the spec; C·D-4 RESTORED at MINOR; A's row and A's kill both die** | css-transitions-1 §3: the **after-change style excludes any styles from CSS Transitions in the cascade**. Reader-A's confirmation quotes the *before-change* clause ("declarative animations… updated to the current time") as if it defined after-change. The transitioning `--axis-active` is a Transitions-origin style, so `opacity`'s/`filter`'s after-change values derive from the CASCADED driver — already at its terminal 0/1 from the inline binding (:13/:18/:23) — the end value never moves, the restart condition never fires. (If it did, every transition would restart itself every frame; the exclusion is what makes the model coherent.) While the derived transitions run, the Transitions origin supplies their values: one 180 ms `var(--ease-standard)` ramp, exactly as declared, and the driver's interpolation is unobserved — C's mechanism, verbatim. C's one-edit falsifier (delete :62 + the `@property` block) then predicts **no visible change** in normal motion, since both channels are affine in the driver at the same duration/ease — the prediction A called refuted is in fact the expected outcome under the correct model. Surviving defect = redundancy + the false `:35-36`/`:58-59` prose + the `--lit`-idiom deviation (CubeTarget.css:79, verified) + the PRM split consequence — **MINOR** (KF-AX-8). Live settle-time stays an SS-13 item with the prediction now FLIPPED: expect ~180 ms; a measured ~360 ms chase would convict the engine, not the spec reading. |
| **D·D-4 grading** ("PROVEN for the projection") | CONFIRMED MAJOR — algebra "source-certain and unconditional" | KILLED the grading via M-1 — the frame is never displayed | **B wins; demoted to MINOR + fully UNPROVEN-NEEDS-LIVE** | This seat's HEAD read of `useCubeDemo.ts` ~:104-127: `.graph` — simultaneously the lines' parent, containing block (by `perspective`), and perspective root — is driven to `rotate3d(-1,1,0,30deg)` on every mount (650 ms sweep from identity; PRM writes it inline), wired at CubeScene.vue:204-211. In the settled attitude the composite `M_graph · P(1200) · rotateY(90°)` does not in general collapse the line to a point (the graph rotation moves the line's homogeneous points off the view ray *before* the divide); where the divide falls relative to the establishing element's own transform is precisely the css-transforms-2 corner engines diverge on. The degeneracy algebra holds source-certain **only for the transient mount frames** (the sweep starts at identity). D·D-16-rev-2/C-2's "unconditional, in every engine" hardening dies with it; the containing-block *correction* itself stands. Not one axis doc cites the transform — reader-B's M-1 is the corpus's largest single miss and is booked MAJOR (KF-AX-4). |
| **D·S-3's mechanism** (`pointer-events: none` = "one declaration between working and catastrophically broken; would kill OrbitalDrag outright") | KILLED the mechanism (negative-z paints below in-flow content; residue = stage-wide hazard) | ELEVATED it (S-A: "without :69 the drag affordance dies outright") | **A wins on outcome; B's elevation inherits the overstatement** | Over the die, pointer events land on the die's own subtree — descendants of OrbitalDrag's container — and reach the :280 `pointerdown` handler by bubbling; for the lines to steal the event they must be hit-test-topmost there, and they are not under EITHER stacking model (z-index: negative-z children paint below in-flow content; depth sort: the near faces at +`--side-offset` beat the z≈0 lines). So "dies outright" is false twice over. What survives is real and worth the superlative at reduced strength: outside `.graph` the 1000vw boxes overlay stage surfaces painted earlier, and no ancestor supplies `pointer-events` (verified: the only other site is CubeTarget.css `.cube--rolling`, transient). Revival condition preserved for B: a live hit-test showing a line topmost over the die re-opens the row. |
| **L/§4 F-1** (glass-ui phantom dep) | folded (via L·D-2's "undeclared, unlocked" framing) | CONFIRMED MAJOR with fresh greps ("package.json → no match; lock → 0") | **FOLD to census F-1 — never re-booked — and BOTH readers' verifications are worktree-contaminated** | This seat's git reads: HEAD `8281638c` `package.json:71` declares `"@mkbabb/glass-ui": "6.0.0"` under **`optionalDependencies`**; HEAD `package-lock.json` carries glass-ui ×3; the DIRTY worktree's uncommitted diff removes both, and that is the tree both L·§4 and reader-B grepped. Installed = 7.0.0 (≠ declared 6.0.0); `add20b7e` on a non-master ref declares Glass 7. So the program now holds **three mutually contradictory records** — lane-frontend F-1 "phantom, absent from both", CENSUS-2026-08-03 SCH-1 "FALSIFIED: package.json:77 = 7.0.0 exact devDep", and this seat's HEAD read "6.0.0 optionalDep at :71 + lock ×3" — and none of the three describes the working tree. One identity, owned by census F-1 / kf-CSSCodeEditor's KF-CE-32; the reconciliation (incl. the optionalDependencies question — an *optional* design-system dep means `npm ci` can succeed WITHOUT glass-ui and then `style.css:3`'s `@import` fails at build, a sharper failure shape than "npm ci broken") routes to **KF.W0** §B-12. L's component-level rider survives inside the fold: a zero-import leaf still carries four vendor edges, one of them (its entire PRM behaviour) unguardable from a token — and the PRM rule verified in `node_modules` is the **7.0.0** surface, not the declared 6.0.0. |
| **L·D-8's count** | repeated "the four `var(--axis-active, 0)` fallbacks" | corrected to three | **B wins: exactly 3** (:53, :55, :56 — this seat's own grep). A's cell is a citation-inheritance event (DISSENT #1). Substance confirmed at KF-AX-21. |
| **L·D-12's inversion limb** (missing `--z-behind` → lines paint OVER the die) | KILLED (inconsistent with L's own D-3 inertness; antecedent unreachable) | **repeated it** inside the confirmed C/D-7 cell ("token absence inverts the stacking") | **A's kill stands; B's cell corrected** | A declaration the corpus's own confirmed core (KF-AX-6) argues the 3D context does not honour cannot invert stacking by going missing — the two claims cannot both hold. And the antecedent needs both the vendor token AND the demo copy gone, at which point `style.css:3`'s `@import` has already failed. C·D-7 survives WITHOUT the limb (KF-AX-11). |

## Adjudicated defect roster — 30 rows

Ids are the corpus's own, for life; `KF-AX-n` is this record's roster index. Dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input (M-25). **`AXISLINE-UNIT` = NO-WAVE-OWNER**: one proposed wave-surface — "the axis-lock reveal made honest" (the latch's registry adoption, the transition list, the filter gate, the dual encoding, the PRM/forced-colors decision, the prop type + tuple, the prose brought back to the tree, the scene-scoped teardown) — for SS-1/SS-2 spec authoring to consume; no census KF.W0–W10 row owns the cube scene's repair, and scattering one feature's fifteen small cures across four waves would orphan their shared seams. It sequences AFTER the KF.W4 vue-tsc gate (the type rows are unverifiable until a `.vue` file can fail a build) and its every geometric assertion is written against the **settled `rotate3d(-1,1,0,30deg)` frame** (KF-AX-4), not the local one.

### MAJOR

- **KF-AX-1 · D·D-7 = L·D-1 + corpus-missed root (reader-A) — the X/Y/Z latch is the exact "second window listener" the demo's own registry law bans, and the registry already ships both cures the corpus hand-rolls.** The bare `window` keydown/keyup pair (OrbitalDrag.vue:276-277 → useOrbitalPointer.ts:169-176) has no target/`isContentEditable`/drag-state guard and no blur/visibilitychange reset (grep empty, re-run by this seat): typing `x`/`y`/`z` into any field (glass-ui `<Input>` at MatrixEditor HEAD:14; SharePopover; the Monaco host) lights an axis line, and a keyup lost to a tab switch leaves one permanently lit. The root all three axes missed: useControlsKeyboardShortcuts.ts:30-33 states the law verbatim — "Every binding routes through the ONE existing glass-ui `registerShortcut` registry (**not a second window listener**), so they inherit the **editable-target skip** + surface in the **KeyboardShortcutsModal**" — and the registry's shipped surface covers this latch completely: `dist/keyboard.js` guards `isContentEditable || e.closest(".monaco-editor")`; `useKeyboardShortcuts.d.ts` declares `allowInInput?: boolean` AND `event?: "keydown" | "keyup"` (both read verbatim by this seat), so a hold-latch is fully expressible; the modal is live at EditorShell.vue:106. The false-lights bug is a self-inflicted regression against a working house guard, and the registry cure also feeds the discoverability limb of KF-AX-10 (a labelled "constrain to X axis" row in the shipped modal). → **AXISLINE-UNIT**, first in its repair order (the fix is upstream in `orbital-drag/`; the unit owns the feature).
- **KF-AX-2 · C·D-2 + D·D-19 + corpus-missed (reader-A) — the lit line's semantics are false three ways.** (1) It lights identically for rotate-, translate- and scale-lock (OrbitalDrag.vue:197-211: `keys.shift → updateTranslation`, `keys.ctrl||meta → updateScale`, else rotation; the prop projects the modifiers away at CubeTarget.vue:158-163) while the prose names *rotation* twice (:3-4, :71-73). (2) It lights on bare keydown at idle, when nothing is being constrained — `handleAxisSpecificInput` has exactly two call sites, both gesture-gated (drag, `isDragging` early-return at :97; wheel at :155-160) — the honest state is "armed", not "active". (3) The **wheel** is a sixth operation no axis enumerated: :155-160 routes it through the same modifier branch, so one lit line means any of six operations. All three verified by this seat. → **AXISLINE-UNIT**: widen the prop (the `Pick<PressedKeys,…>` row, KF-AX-16, gives it the type for free) or make the two-word prose edit, and DECIDE armed-vs-active; C's fair-counter (the prop is named `lock` = "which axis") is preserved as the losing-but-live reading.
- **KF-AX-3 · D·D-3 = C·D-8 (sharpens J-lane STY-8; identity noted, numbers new) — the axis palette has no dark arm, and the LOCKED state — the one that carries information — fails 3:1 in each theme on a different axis.** style.css:109-112 is the sole declaration site (fixed light-mode `hsl()` literals; `--axis-w: var(--foreground)`), no `.dark`/`light-dark()` arm — while `--accent-kf` nineteen lines later (:130, read by this seat) uses `light-dark()` for exactly this reason. Both readers re-derived the arithmetic independently and reproduce: locked green 2.69:1 (light), locked blue 2.98:1 (dark). 1.4.11 applicability is contestable (the dashed→solid tell is a redundant non-colour channel — the S-1 superlative), so the severity rides the token-discipline gap landing at maximum emphasis, not a bare WCAG conviction. → token cure **KF.W6**; effective-backdrop measurement (the graph-paper field) **SS-13/KF.W9**.
- **KF-AX-4 · corpus-missed (reader-B M-1) — the whole corpus reasons in an untransformed `.graph` frame; the shipping app rotates `.graph` itself on every mount, and no axis doc cites the file that does it.** Verified at HEAD by this seat (header block): `changeGraphPerspectiveAnim` sweeps `rotate3d(0,0,0,0deg)` → `rotate3d(-1,1,0,30deg)` over 650 ms ease-out-back, targeted at `graphEl`, played from CubeScene's onMounted; the PRM branch writes the attitude inline. `.graph` is simultaneously the lines' parent, containing block, and perspective root — so every geometric verdict in the corpus (D-4, D-5/L-D-3/C-D-6, D-16/C-2's "by construction in every engine") was computed against a frame the app shows only during the first sweep frames. The three axes cite `useCubeDemo.ts` **only** for its matchMedia line. → booked as the corpus's method-defect row; every AXISLINE-UNIT geometric assertion and every SS-13 capture below is frame-stamped because of it.

### MINOR

- **KF-AX-5 · D·D-4 + L·D-4, RESCOPED — the Z-line degeneracy is real only in a frame the app barely shows, and the corpus's prescribed cure is unsound.** Source-certain: `rotateY(90deg)` on a `1000vw × 0` box whose centre coincides with the perspective origin (the D-16/C-2 correction, verified: CubeTarget.css carries `position` only at :109) degenerates **at the mount frames** (the sweep starts at identity). In the settled 30° attitude the projection is engine-territory (ruled above). Cure rider (reader-A, adjudicated in under the ColorSpaceSelector "cure adjudicated separately" law): an off-normal tilt or `translateZ` sweep would make Z the one line that lies about its axis — `updateAxisRotation` builds **world** basis vectors and pre-multiplies (OrbitalDrag.vue:112-158, verified), so line orientation is truth-telling; the honest alternatives are a ring/screen-plane gizmo for the view-adjacent axis or a declared triad — and this rider is itself frame-dependent (in the settled attitude the graph-local z is no longer the view ray). → **SS-13 screenshot FIRST** (settled attitude + mount frame + PRM), then **AXISLINE-UNIT** design decision. The single highest-value capture in the set — both readers agree, and after M-1 it is worth even more than they graded it.
- **KF-AX-6 · D·D-5 core + L·D-3 + C·D-6 — `z-index: var(--z-behind)` carries a four-line contract citation for a declaration the 3D rendering context does not honour.** C's MINOR severity adopted ("documentation-and-idiom defect" — the geometry already delivers the intended look); D's state-dependence escalation is killed (register #4). → **AXISLINE-UNIT** (truthful comment or removal); C·D-6's decisive one-edit probe (`z-index: 999`, several orbit angles) → **SS-13**.
- **KF-AX-7 · L·D-2 = C·D-3 (the residue of dead D·D-1) — ":58-59 PRM-respecting via the wrapper below" is false; the real guard is glass-ui's universal `*:not([data-allow-motion])` rule, and it SPLITS the reveal.** Verified whole by this seat: the rule replaces `transition-property` with `opacity, color, background-color, border-color, box-shadow`, so under PRM the driver and `filter` snap while opacity ramps 0.1 s — bloom on frame 0, brightening ~100 ms later. The behavioural dependency rides the installed-7.0.0-vs-declared-6.0.0 vendor surface (see the F-1 fold). → **AXISLINE-UNIT**: either author the local block the comment promises or state the vendor reliance truthfully; the driver-only cure (KF-AX-8) makes the PRM degradation coherent either way.
- **KF-AX-8 · C·D-4 RESTORED — the driver and both derived channels are transitioned; the driver's interpolation feeds nothing while they run; the colocated `--lit` idiom (CubeTarget.css:79, derived gradients deliberately unlisted — verified) is the house form this file alone deviates from; ":35-36/:58-59"'s "the registration lets both channels interpolate" is false as written.** The compounding MAJOR (D·D-2/L·D-7) and reader-A's kill of this row are both dead (ruled table). Cure = the driver-only list, which also un-splits PRM and makes the `@property` block load-bearing instead of ornamental. → **AXISLINE-UNIT**; measured settle → **SS-13** with the flipped prediction.
- **KF-AX-9 · C·D-1 = D·D-8 = L·D-5 + corpus-missed join (reader-A) — a resident non-`none` `filter` on three 1000vw elements inside the T.G1 backdrop root, against T.A1/G5 discipline — and on the entire touch fleet the component is 100 % cost, 0 % function.** The lock is keyboard-only (no gesture/UI path — verified), so on touch `--axis-active` is permanently 0 while the scene explicitly grows the die for that fleet (CubeTarget.css touch media query) and the sibling egg was explicitly retrofitted for touch (S.G3 S2, verified :224-226). D·D-6 held the unreachability, C·D-1/L·D-5 held the cost; nobody multiplied them. Cure: move `filter` under `.axis-line--locked` (:73-75) — zero cost, and the mobile fleet stops paying. → **AXISLINE-UNIT**; magnitude (raster is viewport-clipped — both readers' K-1/§5 correctly bound it) → **SS-13**.
- **KF-AX-10 · D·D-6 RESCOPED + corpus-missed (reader-A) — the touch-parity gap is real; the discoverability half is mis-attributed; beneath both sits an unresolved product intent.** D pinned the whole discovery story on the unlanded U.B8 `.stage-legend` rider — but a live, shipped, grouped shortcuts surface exists (EditorShell.vue:106 + `useRegisteredShortcuts`) and was simply never fed; KF-AX-1's registry cure discharges that limb. What stands: no touch path to a first-class scene affordance, against DESIGN.md §7's "remains discoverable on touch" (verified :149-155) and the in-file Roll precedent — and the file never chooses between ":2 EGG" and CubeTarget.vue:151-153's "a powerful affordance… completely invisible". The egg-vs-affordance category error (KF-AX-28's INFO body) is why three independent surfacing mechanisms were all skipped rather than any one argued about. → **AXISLINE-UNIT** + an explicit product-intent decision for SS-1/SS-2; the §7 legend rider stays U.B8's, not re-booked.
- **KF-AX-11 · C·D-7 (folds L·D-12's substance) — T-lane F3's prescribed deletion was never applied, only relocated: `layout.css:25` re-declares `--z-behind: -10`, a token glass-ui ships; the docs' "the ONE element that uses it" is stale (second consumer `SquareScene.css:110`, verified); `style.css:24-27` contradicts itself in one comment block ("There is NO demo-local z-scale" vs "demo-owned, defined in layout.css").** The inversion limb and the sole-consumer census are killed (ruled table). → **KF.W6** (the token settle), sequenced with the F-1 reconciliation (deleting the demo copy makes the vendor cascade load-bearing).
- **KF-AX-12 · D·C-1 = C·X-1 — the formation census is wrong about this file at its own HEAD, twice**: lane-frontend.md:253 (`raw z-index:-10`) and :442 ("One acknowledged exception…") — the tree reads `z-index: var(--z-behind)` (verified both sides by this seat). Inherited from live stale prose: style.css:39 (ambiguous-at-best — reader-A's caveat that "reconciles to" can be read as past-tense is adopted; the sentence must be recut, not merely branded false) and DESIGN.md:99. → census rows **KF.W0** (the re-anchor wave); the two prose sites ride **KF.W6**'s z-contract touch.
- **KF-AX-13 · D·D-15 = L·D-11 — `border: 1px dashed` on a `height: 0` box paints a 2 px double-stroke band plus 1 px end caps** — the weight that carries KF-AX-3's arithmetic, and it makes the dashed→solid tell MORE perceptible, not less. → **AXISLINE-UNIT** (`border-block-start`, one line).
- **KF-AX-14 · D·D-14 (folds L·D-6) — one boolean encoded twice, and the inline `--axis-active` outranks every stylesheet rule** — no PRM/forced-colors/prefers-contrast block can pin the reveal without `!important`, so the dual encoding actively obstructs the cures for KF-AX-7/17. `types.ts` types all six slots boolean; no continuous source justifies it. Cure: `&.axis-line--locked { --axis-active: 1 }`, minus three bindings. → **AXISLINE-UNIT**.
- **KF-AX-15 · D·D-10 duration limb + C·D-5 — `180ms` ×3 matches no rung of the ladder the same declarations reach into for the ease** (ladder re-verified by this seat; sibling `var(--duration-fast, 160ms)` consumers verified); tempo-deaf (`--motion-tempo: 1`, never PRM-scaled — which is also why tokenising the duration does NOT discharge the PRM decision). The codex/token-home limb is killed (register #11). → **KF.W6**.
- **KF-AX-16 · L·D-9 + C·D-13 + corpus-missed (reader-B M-5) — the silent-drift bundle**: the prop re-declares the x/y/z third of `PressedKeys` structurally instead of `Pick<PressedKeys,…>` (the parent already imports the type); the three divs are hand-unrolled against the exported `axes` tuple; `--axis-w` ships with a live consumer, so the fourth axis lands with vue-tsc green — and the OTHER half of the green is upstream: `useOrbitalPointer.ts:173-175`'s `slot in pressedKeys.value` (an `in` against a reactive proxy that narrows nothing) + the unchecked `as keyof PressedKeys` assertion. Zero coverage confirms nothing catches a transposition (and see KF-AX-26 for why the coverage grep under-measured). → **AXISLINE-UNIT** (type + tuple + a three-assertion mount test) enabled by the **KF.W4** vue-tsc gate.
- **KF-AX-17 · D·D-13 (a D-only catch, correct) — no forced-colors handling: `border-color` is forced to one system colour so the X/Y/Z hue coding collapses, while `filter` — outside the forced-color-adjust property set — keeps painting authored red/green/blue haloes into a monochrome UI.** Glass-ui's forced-colors block is class-keyed (`.focus-ring`/`.dock-*`/`.field-control`/`.input-pill`/`.hairline-accent`/`.glass-dock` — read verbatim by this seat; D's aria-keyed citation was of a different-era file, immaterial) and reaches none of three bare divs. The **demo-wide** categorical forced-colors gap is already banked at kf-CSSCodeEditor KF-CE-13 → referenced, not re-booked; this row is the component's own absence. → **AXISLINE-UNIT**; render → **SS-13**.
- **KF-AX-18 · L·D-16 = C·D-12 — OrbitalDrag publishes the latch through two seams; the scoped-slot seam has zero consumers, snapshots `.value`, and its comment claims the live emit's job — and the lines are rendered OUTSIDE `<OrbitalDrag>` (verified :93/:101), so the slot prop is structurally unreachable for the one use case it was authored for.** → **AXISLINE-UNIT** (delete the slot seam; one line).
- **KF-AX-19 · corpus-missed (reader-B M-2) — `changeGraphPerspectiveAnim` is the cube scene's one engine animation with no teardown**: excluded from `animationGroup` (HEAD :90-96, verified) while CubeScene's onBeforeUnmount stops only the group (:214-218, verified); the sibling roll pays its teardown twice. Bounded (650 ms, one iteration) — hence MINOR — but it is the exact engine-consumption idiom the corpus graded on the pair while the third consumer in the same scene went unexamined, and it owns the axis lines' parent frame. → **AXISLINE-UNIT** (scene-scoped: add the sweep to the group or stop it explicitly).

### INFO

- **KF-AX-20 · D·D-9 DEMOTED (register #10)** — `width: 1000vw`: the §4 codex charge dies (the viewport-literal ban's antecedent is the dock token set; 45 literals across scenes/components incl. the sibling sheet's own `--side-size: min(25vh, 25vw, 15rem)`, none flagged); residue = §4's geometry-tokens-live-in-`layout.css` sentence + the 10×-for-a-stroke magnitude taste. → **AXISLINE-UNIT** if touched.
- **KF-AX-21 · L·D-8 (count corrected: **3**) = C·D-10's fallback limb** — the fallbacks are unreachable on a registered property, and C's sharper framing stands: the file is written for both worlds at once (registration real → fallbacks are noise; registration absent → `transition: --axis-active` does nothing). → **AXISLINE-UNIT**.
- **KF-AX-22 · C·D-11 (C-only)** — three sibling roots, no `inheritAttrs: false`, no `$attrs` target: the next consumer's `class` is silently dropped with a dev warning; latent until J-lane C6's per-axis labels. → **AXISLINE-UNIT**.
- **KF-AX-23 · D·D-12 = C·D-9 vs L·K-3** — the aria-hidden inconsistency with `face-relit` (verified :67-70): L is right that empty generic containers are AT-inert and the attribute is not a defect-cure; the inconsistency with a sibling that took the opposite decision is real and free. INFO, per reader-A's demotion. → **AXISLINE-UNIT**.
- **KF-AX-24 · D·D-16 rev-2 / C-2** — the containing-block correction (perspective establishes one; "nearest positioned ancestor" is the wrong test) stands and remains the corpus's best self-correction; its "unconditional in every engine" hardening of D-4 is RESCOPED by KF-AX-4. → recorded; the method lesson ("don't use the positioned-ancestor test in a scene full of perspective/transform/filter boxes") carries.
- **KF-AX-25 · D·D-11 + L·D-15 + C·D-10 + corpus-missed (reader-A) — the flat-namespace pair, plus its inverse hazard**: `--color` unregistered/inheriting/collision-prone; `@property --axis-active` in `<style scoped>` registers document-wide (reader-B's mechanical compiler-sfc proof adopted — emitted verbatim un-scoped while every selector gains `[data-v-…]`); and the hazard all three axes filed backwards: registration with `initial-value: 0` makes the property universally PRESENT, so any future `var(--axis-active, <fallback>)` anywhere in the app silently resolves to "at rest" instead of taking its fallback or failing loudly — the same mechanism that deadens this file's own three fallbacks, promoted to app scope, in a demo with zero `--kf-*` prefixing (census §6.3, re-verified). → **KF.W6** (the token-namespace audit named in its scope).
- **KF-AX-26 · corpus-missed (reader-B M-3) — the reveal is a NAMED EXCLUSION from the demo's only visual gate** (occlusion.mjs:33-37, verified verbatim), and L·D-14's coverage grep could not have found it: `e2e/` does not exist (verified; no playwright.config), so the grep silently covered half its intended surface. Zero coverage AND out-of-gate BY DECISION — stronger than filed. → **KF.W9** (the visual audit scopes its own surface list; this exclusion is on it) + the test row rides KF-AX-16.
- **KF-AX-27 · corpus-missed (reader-B M-4) — the banked `var(--ease-standard, ease)` guard covers only the undefined half**: inside the `transition` shorthand it is a pending-substitution value, and a resolvable-but-invalid `--ease-standard` (two hops of indirection, verified: bridges.css + scheme-spring.css → scheme-motion.css) yields IACVT → `transition: initial` — the reveal snaps in both motion registers with no protection. Filed as the rider on the S-4 superlative, which all three axes banked unqualified. → recorded.
- **KF-AX-28 · D·D-17 + L·D-10 residue** — the four verbatim thesis restatements are real (verified sites) and the duplication is D-1's mechanism (prose minting authority drifts; one copy was strong enough to mislead an audit); the arithmetic that dramatised it is dead (register #8). → **AXISLINE-UNIT** prose collapse to one copy + pointers (§6's sanctioned form, which CubeTarget.css:11-12 already models).
- **KF-AX-29 · D·D-18 = L·D-13 vs C's cleared row** — `transform: rotateX(0deg)`: behaviourally consistent (keeps `.x` under the same transformed-element treatment), documentation absent; C's "intentional" reading and D/L's "null declaration" reading are both live because nothing in the file says which. → **AXISLINE-UNIT** (one comment).
- **KF-AX-30 · D·D-20 (uncontested)** — props not reactively destructured per DESIGN.md §9.2's positive prescription (not the banned legacy forms). → **AXISLINE-UNIT** if touched.

## Killed-claims register — 12 entries, refutations preserved

1. **D·D-1 (the corpus's ONLY BLOCKER)** — "zero PRM guard; nothing upstream rescues it; [REV 2] exhaustive over glass-ui's entire PRM surface." DEAD by its own stated falsifier: `dist/styles/utilities/a11y-overrides.css` ships the universal `*:not([data-allow-motion])` PRM rule (this seat re-read it whole); reachability end-to-end via `style.css:3` → `exports["./styles"]` → index.css → accessibility.css; `.axis-line` carries no `data-allow-motion`; author-`!important` outranks the scoped author-normal list. The rev-2 exhaustiveness grep actually returns **23 files** (re-run by this seat) and a11y-overrides.css is among them — the file is single-line minified, the exact trap rev 2 itself documented at K-4 and then fell into. Both readers killed it; axis L killed it against itself (K-4) and axis C against its own prior pass (X-3); the D axis is the outlier. Survivor: the false comment, MINOR (KF-AX-7).
2. **D·D-2 + L·D-7's compounding mechanism — and reader-A's confirmation of it as a MAJOR** — "a running transition whose after-change style no longer matches its end value is cancelled and restarted… materially longer than 180 ms, doubly eased." Refuted by the transitions model: the after-change style **excludes CSS Transitions styles**, so the derived properties' end values derive from the cascaded (terminal) driver and never move; the quoted "updated to the current time" clause belongs to the *before-change* definition. Killed BY THIS SEAT against a reader's confirmed cell. The redundancy/idiom substance survives as KF-AX-8; the live prediction flips (~180 ms expected).
3. **Reader-A's kill of C·D-4** — "Perform C's edit and the two routes diverge sharply… visibly different, not byte-identical; a wave that ran the prescribed test and saw a change would wrongly retire a true finding." A kill premised on the dead restart mechanism (#2): under the correct model both routes render one 180 ms ramp and C's byte-identical prediction is the expected outcome. Kill-of-a-kill; C·D-4 restored (KF-AX-8).
4. **D·D-5's state-dependence limb** ("not started → the die subtree flattens coplanar at z = 0 → the comment holds; playing → depth sorting engages") — `preserve-3d` is in OrbitalDrag's STATIC class list (CubeTarget.vue:12, verified), unconditional; the inline `transformStyle` at :72 merely duplicates it; `.idle-hover` and `.cube` carry it too. There is no coplanar regime. Both readers killed it; confirmed. C·D-6's flat reading is the surviving core (KF-AX-6).
5. **D·D-4's "PROVEN for the projection" + D·D-16-rev-2/C-2's "unconditional, in every engine"** — killed via KF-AX-4 (the ruled table): the algebra is `.graph`-local and the shipping frame is `rotate3d(-1,1,0,30deg)`; source-certain only for the transient mount frames. Demoted, not deleted — the screenshot decides it.
6. **L·D-4's "X and Y both stay in the screen plane and read correctly" + D·S-2's horizontal/vertical orientation channel as stated** — both describe a frame the app never settles in (KF-AX-4). Residue preserved: in the tilted frame the three lines still carry three distinct orientations — the identity channel plausibly survives and may improve; that is an SS-13 observation, not a source claim.
7. **L·D-12's inversion limb + its "sole consumer" census — and reader-B's repetition of the inversion inside its confirmed C/D-7 cell** — internally inconsistent with the confirmed inertness core; antecedent unreachable (vendor token + demo copy + a cascade that has already failed at the `@import`); the census grep returns 7 lines / 5 files incl. `SquareScene.css:110` (this seat's run). Substance survives as KF-AX-11.
8. **The arithmetic cluster: L's "91 lines"/"55 % prose", L·D-8's "×4" (repeated by reader-A), D·D-17's "26 of 90"** — the file is 90 lines (`wc -l`, twice-confirmed); comment lines 24 (≈26.7 %); `var(--axis-active, 0)` ×**3**. The duplicated-rationale finding survives on the four verbatim thesis copies (KF-AX-28); every number that dramatised it dies.
9. **D·S-3's mechanism ("would intercept every pointer event… kill OrbitalDrag outright") + reader-B's S-A elevation ("without :69 the drag affordance dies outright")** — false under both stacking models over the die (ruled table): events on the die bubble to the container's own :280 handler; the lines are not hit-test-topmost there. Survives at reduced strength as the stage-wide discipline superlative. Revival condition: a live hit-test showing a line topmost over the die.
10. **D·D-9's codex-violation limb** — DESIGN.md:87-92's viewport-literal ban is scoped to the dock-geometry token set by its own antecedent (verified); enforcement is absent tree-wide (45 literals, 13 files, incl. this scene's own sibling sheet); the file contributes one. Demoted MINOR → INFO (KF-AX-20).
11. **D·D-10's token-home/codex limb + its citation** — the partition lives at DESIGN.md:131-132, not :143-146 (verified); §4's appearance clause is explicitly "**when that move lands in U.B**" (verified :92-93) and the U.B stray list (:134-137) names six tokens, none this component's. A colocated appearance magnitude does not violate a rule that has not landed. The off-ladder-180ms limb is unaffected (KF-AX-15).
12. **Reader-B's F-1 re-book as MAJOR, and the grep evidence beneath it (shared with L·§4)** — worktree-contaminated: at HEAD `8281638c`, `package.json:71` declares `"@mkbabb/glass-ui": "6.0.0"` under `optionalDependencies` and the lock carries it ×3 (this seat's `git show` reads); the "absent from both" state exists only in the uncommitted worktree diff. Identity guard: census F-1 / KF-CE-32 — folded, never re-booked; the three-way record contradiction (phantom / 7.0.0-devDep / 6.0.0-optionalDep) routes to **KF.W0** with the optionalDependencies failure-shape note (ruled table).

## Superlatives — provenance preserved (L-18 runs both ways)

- **THE COMPONENT · the dashed→solid motion-free tell** (D·S-1 = L·L-5 = C·SUP-4, both readers, re-verified ends): a shape channel independent of colour, opacity and motion; survives the vendor PRM property-replacement (`border-style` is discrete and unlisted — flips on frame 0), survives forced-colors, survives every failing contrast row; the 2 px stroke (KF-AX-13) makes it MORE perceptible. This one declaration is why KF-AX-3 and KF-AX-17 are not blockers.
- **THE COMPONENT · `@property` is the right primitive, correctly parameterised** (D·S-4 = L·L-1): `syntax: "<number>"`, `inherits: false` (right for a per-element state scalar), `initial-value: 0` (rest = identity); the house instinct is consistent (`--lit`, `--ball-p`). The mis-wiring (KF-AX-8) is a separate matter from the choice — and the driver-only cure is what finally makes the registration load-bearing.
- **THE COMPONENT · a genuinely zero-coupling contract with a vacuously perfect leak profile** (L·L-2/L-4 = C·SUP-3 = reader-B S-D, whose executed falsifier is adopted: the one plausible vector — OrbitalDrag's window listeners — is VueUse-registered inside onMounted with a live scope, verified against installed Vue). One prop, zero imports, zero lifecycle. Set against the sibling that reaches for the engine and pays teardown twice, the pair draws the seam exactly right — timeline → engine, two-state → platform (C·SUP-1 = reader-B S-E; inv ζ stated in the tree, not inferred).
- **THE COMPONENT · scene-tier `var()` fallback discipline** (L·L-3 = C·SUP-2 = D·S-6): the one foreign token guarded, the owned tokens bare — a 6-of-6 scene-tier convention, token resolution verified to `cubic-bezier(0.4, 0, 0.2, 1)`. **Carries KF-AX-27 as its permanent rider**: the guard covers only the undefined half of the failure space.
- **THE COMPONENT · `pointer-events: none` — load-bearing at reduced strength** (D·S-3 as killed-and-rescoped; reader-B S-A's tree-facts adopted, its headline not): not the drag's lifeline, but the one declaration keeping three viewport-spanning boxes from occluding the rest of the stage's input; same discipline as `face-relit`.
- **THE COMPONENT · colocation measured against the codex, with the pointer left behind** (D·S-5 = reader-B S-F): 57-line style block / 90-line SFC against the §9.1 100L/300L law; CubeTarget.css:11-12 leaves exactly the §6-sanctioned pointer — the discipline the rationale prose (KF-AX-28) fails to imitate.
- **THE CORPUS · D·D-16 rev 2** — a second pass that re-derived its own finding, discovered its premise wrong, demoted itself, propagated the correction (C-2), showed it hardened a sibling finding, and recorded the method lesson. Still the corpus's best moment even though KF-AX-4 later re-scoped the hardening — the correction's *direction* (down on severity, up on rigour) is the model.
- **THE CORPUS · the self-kill pair on the PRM trap** — L's K-4 (its OWN first-pass MAJOR, retracted in place with the lesson: "when the claim is 'X does not exist anywhere', the grep must cover the whole resolved cascade") and C's X-3 (the same correction against its own prior pass). Two of three axes disarmed the same trap in public; the third (D·D-1) walked into it with an exhaustiveness certificate.
- **THE CORPUS · C·§4's scoped negative on value.js exposure** — the R1 crash class certified unreachable through four independent closures; the six safe subpath edges re-verified by reader-B. **Folds by reference to the megatranche R1 identity (KF.W2/W3)** — a certified negative with its falsifier, on the axis where a false negative is cheapest to ship.
- **THE CORPUS · C·§0's discharged BLOCKER candidate + the §5 cleared table** — the `{deep:true}` seam checked FIRST and recorded as a non-finding instead of a false BLOCKER; thirteen more candidates buried with reasons, two of them genuinely subtle. The denominator made visible.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (route with KF.W9; every capture frame-stamped per KF-AX-4)

1. **The Z line, three frames** (KF-AX-5): settled `rotate3d(-1,1,0,30deg)` attitude (the shot that decides the row), the mount frame (where the degeneracy algebra is source-certain), and under PRM (inline attitude from first paint). The highest-value screenshot in the set.
2. **z-index governs/inert** (KF-AX-6): C·D-6's `z-index: 999` probe at several orbit angles — complicated by the element's own two grouping properties, which is why no static ruling was issued.
3. **Resting filter cost** (KF-AX-9): does Chromium allocate a filter surface for `drop-shadow(0 0 0px transparent)` ×3, and does it intersect the glass chrome's live `backdrop-filter` footprint (T.G1)? Raster region viewport-clipped — magnitude expected small, measured not assumed.
4. **Reveal settle time** (KF-AX-8): instrument `getComputedStyle(el).opacity` keydown → steady. Prediction (this seat's, against the corpus's): **~180 ms, one ease**. A ~360 ms exponential chase would convict the engine of deviating from the after-change exclusion and re-price the row — the falsifier now runs in the opposite direction from the one the corpus wrote.
5. **Effective backdrop / contrast** (KF-AX-3): sample the composited ground under a locked line, both themes, graph-paper field included; the 2.69/2.98 figures are flat-token arithmetic (twice-reproduced).
6. **Forced-colors render** (KF-AX-17): hue collapse + authored filter haloes — the source side is closed; the pixels are not.
7. **PRM split-reveal perceptibility** (KF-AX-7): bloom + stroke on frame 0, brightening ~100 ms later — incoherent or unnoticeable is a judgment only pixels settle.
8. **The dashed→solid flip at 2 px against the 0.45 rest register** (the S-1 tell): if sub-perceptible, the redundancy story collapses and KF-AX-3/17 both escalate.
9. **`--ease-standard` resolved value live** (D·S-6's residue + KF-AX-27): one `getComputedStyle` check that the two-hop indirection bottoms out.

## Identity guards (anti-rename) & folds

- **F-1 → census F-1 = kf-CSSCodeEditor KF-CE-32** — ONE identity, folded; this record's contribution is the three-state substrate correction (worktree: absent · HEAD: 6.0.0 optionalDep + lock ×3 · `add20b7e` ref: 7.0.0 registry-locked) and the optionalDependencies failure-shape note → **KF.W0** §B-12. KF-CE-32's "npm ci broken until it lands" cell inherits the same worktree contamination and should be re-derived there, not here.
- **C·§4's R1 negative → megatranche R1** (parser crash surface) — folds to the R1 identity in the parser lanes (**KF.W2/W3**); no new row.
- **KF-AX-3 sharpens J-lane STY-8** (the `--axis-*` contrast gap) — lineage noted, numbers and the locked-state framing are this corpus's; booked once, here.
- **KF-AX-17 references kf-CSSCodeEditor KF-CE-13's demo-wide forced-colors gap** — the categorical row stays banked there; this row is the component's own absence.
- **Census §6.3 (flat namespace, zero `--kf-*`) and §6.5 (PRM roster)** — sharpened, not contradicted (KF-AX-25; the roster's omission of this file was the census being right about D-1's *absence*, wrong only in the two `z-index` rows corrected at KF-AX-12).
- **Folds within this roster**: D·D-19 → KF-AX-2; L·D-6 → KF-AX-14; L·D-12's substance → KF-AX-11; L·D-14 → KF-AX-16/26; reader-A's missed #1/#2/#3/#4/#5 → KF-AX-1/5/9/2/25 respectively; reader-B's M-5 → KF-AX-16.

## DISSENT — preserved, not ruled away

1. **Three citation-inheritance events (evidence about the fleet, not the tree)**: (a) reader-A's confirmed L·D-8 cell repeats L's "four fallbacks" — the tree says three; (b) reader-B's confirmed C/D-7 cell repeats L·D-12's inversion limb that reader-A had killed with an argument reader-B's own confirmed inertness row endorses; (c) both readers' F-1 cells repeat the census's phantom-dep greps without a `git show HEAD:` read, on a tree whose dirty status both had themselves recorded. Half-inherited cells inside otherwise-excellent returns remain detectable; the epoch rule stands.
2. **Severity ordering, preserved**: reader-A holds that if the live probe shows the ~360 ms chase, the transition row re-prices toward its MAJOR; this seat agrees the probe is the arbiter and has written the falsifier in that direction (UNPROVEN #4).
3. **Reader-B's S-A elevation, preserved with its revival condition**: a live hit-test showing an axis line topmost over the die restores "load-bearing for the drag" and demotes this seat's kill to a mechanism correction.
4. **The C·D-2 counter-reading, preserved**: "lock = which axis is constrained" is semantically exact in all six operations; if SS-1/SS-2 adopts that reading, KF-AX-2 resolves by the two-word prose edit and the prop width stays.
5. **Method note carried from both readers, endorsed**: the corpus's three axes produced 49 raw findings on a 90-line file and the two largest truths about it (the registry root, the rotated frame) were in neither the findings nor the kills — they were in files one seam away that every axis had already opened for other reasons. Exhaustiveness inside the file is not exhaustiveness about the file.

## Closing verdict

**ADJUDICATED — AMBER.** 30 rows stand (0 BLOCKER · 4 MAJOR · 15 MINOR · 11 INFO); 12 claims killed with refutations preserved — among them the corpus's only BLOCKER, a reader's confirmed MAJOR (killed on the after-change exclusion), and that same reader's kill of the one row that had the mechanism right (restored). 11 corpus-missed items adjudicated in, two to MAJOR weight: the latch that bypasses the demo's own single-registry law while the registry ships both hand-rolled cures, and the mount-time `rotate3d(-1,1,0,30deg)` that every geometric verdict in the corpus was computed without. The component's code is small and mostly right; what it does not survive is its own narration — the PRM wrapper that isn't there, the rotation that isn't the only operation, the interpolation story the transitions model contradicts, the depth rung the rendering model ignores, the egg that is also documented as an affordance. Routings: the repair coheres as ONE proposed wave-surface (**AXISLINE-UNIT, NO-WAVE-OWNER** — SS-1/SS-2 spec authoring to consume, sequenced after the KF.W4 vue-tsc gate, frame-stamped per KF-AX-4), with discrete rows to **KF.W0** (census re-anchor + the three-way F-1 substrate contradiction), **KF.W4** (type/test gates), **KF.W6** (the `--axis-*` dark arm, the duration ladder, the `--z-behind` duplicate + z-contract prose, the token-namespace audit), and **KF.W9/SS-13** (nine live witnesses, the Z-line triptych first); two folds by reference (census F-1, megatranche R1) and one banked-row reference (KF-CE-13) leave nothing re-booked. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.

— end of apotheosis. The three challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.
