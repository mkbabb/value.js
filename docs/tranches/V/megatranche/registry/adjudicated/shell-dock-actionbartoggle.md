# shell-dock-actionbartoggle — adjudicated apotheosis · seat model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited — M-23/L-18 receipt)

**Component**: `demo/shell/dock/ActionBarToggle.vue` (159 lines) + its presence/contract cone (`Dock.vue`, `App.vue`, `usePaneRouter.ts`, `color-session` ink seam).
**Date**: 2026-08-07 · **HEAD at adjudication**: `ee9474e4`, branch `tranche-u` (corpus anchors: challenger HEAD `c654824e`/`5c13465d`; re-resolve every line anchor before executing any cure — the D-19 lesson).
**Corpus (r2 governs)**: `audit/components/shell-dock-actionbartoggle/challenge-{C-implementation,D-design,L-library}.md` + colocated probes (`abt-probe2.json` et al., `probe/`) — all three axis seats Opus 5 [1m], receipts declared.
**Re-readers adjudicated**: two hostile Fable re-readers (design-and-usage · library-and-code), both returns attacked before either was adopted. Their one direct conflict (the C-7 coarse-floor dismissal) is RULED below; one row BOTH confirmed (L-6's binding mechanism) is REFUTED below by this seat's own compiler run.

**This seat's own instruments (all receipts mine, this session; ZERO browser probes — probe parsimony; every operative mechanism decided from source, dist bytes, the committed probe corpus, and the compiler API):**
full source reads of `ActionBarToggle.vue` (159), `Dock.vue` (359), `usePaneRouter.ts:40-231`, `App.vue:30-115/:296-350`, `GenericActionBar.vue:1-30`, `shell/dock/index.ts` (whole), `viewSchema.ts` VIEW_MAP census (**14 views**; `left:"color-picker"` at :106/:115/:143/:180), `useContrastSafeColor.ts` (rung + floor doc-blocks), `ink.ts:92-126`, `useAtmosphereBoot.ts` (SAFE_ACCENT provide :91, `--accent-live` mirror :96), `foundation.css` `--focus-ring-color: var(--accent-live)`, `demo/styles/animations.css:184-193` (Guard 1); **producer bytes** (installed glass-ui 7.0.0): `a11y-overrides.css` (PRM property-list REPLACEMENT, verbatim), `accessibility.css` (forced-colors `[aria-pressed="true"]` 2px Highlight border; `@import` chain), `icon-button.css` (base + `--compact` + hover `scale: var(--scale-hover-dock)` + `[data-active]` arm), `glass-capsule.css` (`.glass-dock .dock-icon-button { padding: … }` in `@layer components`; PRM scale-neutraliser scoped to `.glass-capsule-hover` at (0,2,0)), `tab-button.css` (hover block INCLUDES `scale: var(--scale-hover-dock)`), `touch-floor.css` (`:not(:where(.glass-dock *))`), `density.css` (`--dock-control-safe-inset = size×0.1`; `--dock-tab-h` 32/38/44px rungs; tab padding tokens override the fallbacks), `overflow.css` (`@media (pointer: coarse)` `--dock-control-floor: 2.75rem` on `.glass-dock[data-size]`; `.dock-overflow-wrap .dock-separator { display: none }`), `DockControl.vue.d.ts` (shape/active/compact contract verbatim) + `dock.js` `as: { default: "button" }`; **corpus re-reads**: `abt-probe2.json` whole (prmRoundTrip[0..4], normalRoundTrip, siblings, focus — `compactPadUsed: "4px"` on ALL seven samples; `toolsW: 115` vs `104.5` UNDER `reducedMotion:"reduce"`); `o15-dock-register.spec.ts:100-168`; **CI truth**: grep over `.github/workflows/{ci,release,deploy-pages}.yml` — zero Playwright steps anywhere; **THE DECIDING RUN**: `ts.resolveModuleName` via the installed TypeScript against the SHIPPED `tsconfig.demo.json` from `demo/shell/dock/` — `@mkbabb/value.js/css → <repo>/dist/subpaths/css.d.ts` (LOCAL, package self-reference), `/value` and `/color` likewise LOCAL; **registry/formation cross-reads**: `adjudicated/Dock.md` (§1 L-18/L-22, §6 DELTA rows D2-03/D2-04/D2-07/D2-08/D2-09, C-25/C-27/C-28, R-13, structures (f)/(h), gates G-B/G-M/G-Q), `adjudicated/ColorSpaceSelector.md:25/:47` (L-14 config-parity identity), `CARRY-CUT-LEDGER.md` CC-080/CC-083, `X/waves/{W1,W4,W5,W8,W9,W10}.md` (CC-031 · CC-040 · CC-043 D1–D4 · W5 A3/C3 · CC-083 · CC-099), `motion-quarantine.md` (EXISTS, tracked; Guard-2-wins ruling :43-58; **zero M-xx rows touch this component** — nothing here is quarantined, and every PRM fact below was re-derived from producer bytes by this seat regardless).

---

## Scoreboard

**Corpus of 33 axis findings + 9 re-reader missed-finds → 17 consolidated roster rows: 4 BLOCKER · 8 MAJOR · 3 MINOR · 2 INFO-cluster. Killed-claims register: 12 rows (11 re-reader kills UPHELD · 1 unanimous re-reader confirmation KILLED by this seat — L-6's binding mechanism). One re-reader/re-reader conflict RULED (C-7 coarse floor — the design-and-usage reader wins). Two cross-record corrections FILED (Dock-adj G-M's token-seam premise; the o15 separator oracle's collision with D2-08).**

The through-line: **most of this corpus was already owned before it was filed.** The X formation and the sibling Dock adjudication carry the mobile no-op (X.W4.d gate D4, X-W5 gate A3 — "RED of record `{mobileChanged:false}`"), the CI-orphaned oracle (X-W1 CC-031, B7: "6 of 6 CI-orphaned"), the breakpoint fork (X-W5.c gate C3: "38 occurrences across 13 files"), the false 2.5rem comment (Dock-adj D2-09/G-M), the grid-transition ban (Dock-adj G-B amended: ".action-bar-toggle-slot animates transform/opacity only or not at all — RED"), and the present-and-inert cure (Dock-adj structure (f): "an absent capability is ABSENT"). This apotheosis's work is therefore mostly ACCRETION under identity guards — plus three genuinely new mechanisms (the PRM property-list kill of the settle channel, the resting-rung ink certificate, Label-in-Name) and one refutation of a unanimous confirmation.

---

## Ruled disagreements (the seat's actual work)

### R-A · L-6 "the demo types 2 of 7 subpaths against a DIFFERENT physical copy" — **REFUTED IN MECHANISM, both re-readers overruled; RESCOPED to the standing config-parity identity**

Both re-readers CONFIRMED L-6 at MAJOR, both re-verified the md5s (`e0968b8d…` vs `4309648d…` — real, files differ) and the 7-key exports census (real) — and **neither ran the resolver**. This seat did, against the SHIPPED `tsconfig.demo.json` (paths present, no `/css` entry), from this component's own directory:

```
@mkbabb/value.js/css   → /Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts   (LOCAL)
@mkbabb/value.js/value → /Users/mkbabb/Programming/value.js/dist/subpaths/value.d.ts (LOCAL)
@mkbabb/value.js/color → /Users/mkbabb/Programming/value.js/dist/subpaths/color.d.ts (LOCAL)
```

Under bundler resolution the repo root's own `package.json` (`name: "@mkbabb/value.js"` + exports) is a **package self-reference** that resolves `/css` and `/value` to the working tree's `dist/` — the same copy the Vite aliases serve. Nothing typechecks against the installed 4.0.0 tarball; the md5 divergence is real but INERT; the "breaking change invisible to typecheck" consequence and challenge-L's `--traceResolution` reading (a candidate-probe line mistaken for the winner) both die. This exactly reproduces the **Dock-adjudication L-22 deciding run** ("all eight entries are INERT … refutes the filing's 'second instance' consequence") — which both re-readers had available and neither consulted: a citation-inheritance event on a unanimous row, recorded as evidence about the fleet. **What survives**: 3 dead `paths` targets (`dist/index.d.ts`, `subpaths/{parsing,units}.d.ts` — none exists), 2 omissions, the "CLOSED 8-key set" comment false by one. **Identity**: ColorSpaceSelector-adj L-14 ≡ Dock-adj L-22 ≡ this L-6 — ONE identity, already FOLDed to W-HYGIENE config parity with the cure superseded to **DELETE the inert block**; this corpus is that identity's sixth+seventh confirmation and FIRST mechanism refutation at the type-binding level. Not re-booked (anti-rename). No X-formation CC row surfaced under this seat's grep — **flag at the next formation boundary** so the identity is not orphaned by the V→X transposition.

### R-B · The re-reader conflict on C-7's coarse-floor dismissal — **the design-and-usage reader WINS; `compact` is chargeable**

Reader 1 killed C-7's dismissal ("producer-wide register question, not charged"); reader 2 endorsed it ("C-7 got this exactly right"). Producer bytes decide it: `touch-floor.css`'s quoted rule is indeed dead for the whole dock (`:not(:where(.glass-dock *))` — reader 2's half-truth), **but** `overflow.css` `@media (pointer: coarse) { .glass-dock[data-size] { --dock-control-floor: var(--dock-touch-target, 2.75rem); … } }` feeds `density.css`'s `--dock-control-size: max(calc(…), var(--dock-control-floor, 0px))` — the 44px floor REACHES every non-compact dock control (C's own measurement: Back/Save at 44×44 in the same coarse run), and `icon-button.css`'s `.dock-icon-button--compact { width: var(--dock-compact-control-size, auto); height: …auto; min-width: 0 }` is what DISCARDS it. Choosing `compact` (:87) forfeits the floor through the width/height override — a local, chargeable consumer decision. Reader 2's kill of D-07's quoted-rule mechanism stands; its adoption of C-7's no-charge conclusion is OVERRULED.

### R-C · Cross-record correction: **Dock-adj DELTA-6/G-M's token-seam premise is half-dead**

Dock-adj D2-04's interim cure routes SlugEditLayer's floor "via the producer's own token seam (`--dock-compact-control-*`) … that its OWN sibling already uses (ActionBarToggle.vue:155 — T-36)". This corpus proves the sibling's seam NEVER WORKED for padding: `--dock-compact-control-padding` is structurally unreachable inside `.glass-dock` (glass-capsule.css (0,2,0) beats `--compact` (0,1,0), same layer — probe: `compactPadVar "0.5rem 0.75rem"` beside `compactPadUsed "4px"` on all seven samples). The SIZE tokens (`--dock-compact-control-size`) remain live (nothing two-class overrides width/height). Any G-M execution must ride the size tokens or the BH-relayed producer fix, never the padding token. Filed per E-3 as an addendum-beside — Dock.md is not rewritten.

### R-D · The o15 separator oracle collides with Dock-adj D2-08

Both re-readers carried the separator fold as a superlative ("a register decision that ships WITH its oracle", o15:161-165 asserting exactly one separator inside the inner box). TRUE today — and Dock-adj D2-08 (CONFIRMED, G-M) rules **main-layer visible separators → 0**. The day D2-08 lands, o15's separator case goes RED by design. The superlative stands with this caveat attached; the wave that executes D2-08 retires the o15 case in the same commit or the suite manufactures a false alarm.

---

## Adjudicated defect roster

Severities re-ruled by this seat; every row carries its original ids for life. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

| # | ids (identity) | sev | claim (as adjudicated) | deciding proof (this seat's) | terminal disposition |
|---|---|---|---|---|---|
| **ABT-1** | D-01 ≡ L-7(stuck) + C-3 + C-6 · upgrades Dock-adj **C-28** (was hygiene) | **BLOCKER** | The settle machine's only restoration channel after first arm is a `grid-template-columns` transitionend — a property glass-ui's PRM guard REMOVES (`a11y-overrides.css` rewrites `transition-property` to `opacity, color, background-color, border-color, box-shadow !important`, (0,1,0) beating the demo's `*` guard) — so under PRM one route round-trip latches `overflow: hidden` forever and the T-29 amputation the file exists to cure returns permanently. **NOT PRM-specific** (D-01-scoping killed): any `visible` false→true flap with no committed track change (Vue-coalesced flush) reaches the same stuck state — L's gradient→about cell + statics. C-3/C-6 are the same machine's unguarded limb: the double-rAF re-reads nothing, has no id, no cancel, no scope teardown, and stamps `settled=true` after presence dropped (measured resting class `is-live is-settled` with no `is-visible`; A/B-controlled 45.4px overpaint). The SFC header's PRM premise (:28-29) is a FALSE RECORD — it describes Guard 1 only. | Producer bytes read verbatim by this seat; `abt-probe2.json` prmRoundTrip[2-3] (`is-visible is-live` / `hidden`, slotTransProp = the rewritten list); motion-quarantine.md:43-58 Guard-2-wins independently ratified; writers census of `settled` (:56/:63/:70-72); `:59` early-return read | **X-W8** (CC-083 consumes Dock.md): G-B AMENDED already bans the grid transition ("transform/opacity only or not at all — RED") and G-Q already gates the `slotLive` early-return gone; this row's PRM evidence travels as the severity upgrade (C-28 was filed "recovers today" on the non-PRM path only). The machine deletes whole — `slotLive`/`settled`/`onSlotSettled`/double-rAF/three `is-*` classes die with the producer-presence transposition; any surviving completion signal must not key on a property a producer stylesheet may remove |
| **ABT-2** | L-1 + the generic-bar no-op (re-reader missed #2) + the Save/Cancel discard (reader-2 promotion of L's HYPOTHESIS) | **BLOCKER** | On mobile the ColorPicker-sourced action bar has NO entry point (sole writer of `colorPickerRef` is the desktop-only `:on-mount`, App.vue:105/:323-325; mobile PaneSlot :83-91 passes none) — AND where the generic bar DOES open (gradient/mix/generate, tappable at 390w), all nine actions dispatch onto pane refs only desktop populates: silent no-ops with `disabled` never set (usePaneRouter.ts:196-222, GenericActionBar `:disabled="act.disabled"` = undefined). Worst limb: mobile edit Save/Cancel — `colorPickerRef?.commitEdit()` no-ops while `mobilePaneIndex = 1` still runs (App.vue:41-42), so the UI signals success while the edit is DISCARDED. | Whole chain re-read: App.vue:38/:41-42/:83-91/:101-110/:317-332/:347; all nine handlers; Dock.vue:72/:135-145 (mobile-edit renders ONLY on mobile — the dead path is the ONLY path) | **X-W5.a** (bindPane; gate A3's RED of record IS this row: `{mobileChanged:false, desktopChanged:true}`) + **X.W4.d** (CC-043 gate D4 "no silent no-op" carries the mechanism as its measured RED premise; the `unavailable` state modelling). ALREADY BANKED at formation — the corpus's three axes and the program converge; accretes, not re-booked. The Save/Cancel discard limb is named here so D4/A3's close must witness it explicitly |
| **ABT-3** | C-1 (+ Dock-adj structure (f) identity; R-13's "becomes real the day the clip lifts" residual) | **BLOCKER** | The invisible Tools control is AT/programmatically activatable on bar-less routes (`tabindex` gates tab order only; `@click` emits unconditionally; `toggleActionBar` unguarded; reset watch not `{immediate:true}`), switching DockLayerGroup to a `v-if`-absent layer: every face inert+aria-hidden, `shouldKeepOpen` pins the emptied dock. Cross-engine repro in corpus (24×62 husk, 0 reachable buttons). "Unrecoverable" scoped: an out-of-band hash change to a bar-carrying route escapes; no in-page control survives, so the user-facing claim holds. | Dock.vue:48-49/:86-87/:108-117/:153-154 re-read; producer `inert: !isActive` stamping confirmed in dist/dock.js; probe artefacts carried labeled | **X-W8** (Dock-adj structure (f): the typed DockCommand/provide registry — "a view providing no commands renders no Tools trigger; present-and-inert becomes unspellable") with X.W4.d D1's one-render-path as co-anchor. Interim belt if any wave touches the file first: `:inert="!visible"` on the slot root + `if (!hasAnyActionBar.value) return;` in `toggleActionBar` — one declaration, producer's own idiom |
| **ABT-4** | C-2 ≡ D-06 (count corrected by the D-06-count kill) | MAJOR | On every bar-less route the control is a fully-named ghost: complete name/role/state in the AX tree (`ignored: false`, `pressed="false"`), programmatically focusable, 0 painted px. **Corrected census (this seat's, static)**: VIEW_MAP = 14 views; 4 picker-sourced (:106/:115/:143/:180) + 3 generic − mix overlap = **6 bar-carrying desktop routes; the ghost set is 8 of 14**, not D's 11-of-15 (kill upheld). Neither axis's live denominator is static-checkable; the structural exposure is. | ActionBarToggle absent-state vocabulary census (opacity/0fr/overflow/tabindex — none prunes AX); no `inert`/`aria-hidden`/`v-if` anywhere in the file or at the mount site; viewSchema census mine | **X-W8** (dies with ABT-3's structure (f); the `:inert` interim closes it in the same declaration). The two-uses-of-one-predicate observation (Dock.vue:153 `v-if` vs :183 styling prop) travels as the cure's design note |
| **ABT-5** | C-4 ≡ D-05 (+ reader-2's tabindex-dead-branch INFO folded in) | MAJOR | The toggle's ON state is unobservable in BOTH channels: `aria-pressed="true"`/`.is-active` exist only while the host layer is inert+aria-hidden+opacity-0 (the exact ref that sets them also swaps the layer out); on Back the state clears before re-entry, so the active register never paints in either direction. Focus is destroyed on activation (lands on a roleless div reading a raw colour string) with no restoration contract. Structurally a disclosure, not a toggle: `aria-expanded` + `aria-controls` + a focus contract. The `:tabindex="visible ? 0 : -1"` ternary's `0` branch is a no-op on a native button (`as: { default: "button" }` — my dist grep): the line reads as a presence guard and is one unconditional removal — precisely why under-scoped cures happen. | Dock.vue:113/:154/:184 ref-identity trace; producer face stamping; corpus AX/activeElement cells carried labeled (D-05 geometry figures live-only → residue) | **X-W4** (semantic controls — NEW ROW at wave open; no CC row names it). The reshape rides whichever structure survives: under Dock-adj (f) the trigger is absent without commands and the pressed/expanded semantics attach to the surviving seat |
| **ABT-6** | D-03 ≡ L-2 ≡ Dock-adj D2-09 (+ the C-negproof-T36 kill; + reader-2's oracle/CI limb) | MAJOR | T-36's "true-button box-model" has NEVER applied: `--dock-compact-control-padding` is structurally unreachable inside `.glass-dock` ((0,2,0) vs (0,1,0), same `@layer components`); computed padding = the 4px safe-inset (40px×0.1); the SFC's 12-line "never a specificity fight" comment is a false record of the fight it lost. The guarding oracle asserts the comment's wish (`o15:126 expect "8px 12px"`) — RED by construction — **and no CI workflow runs Playwright at all** (my grep: zero steps across ci/release/deploy-pages), so the RED gate has never been able to fail a build. Reader-2's kill of D-03's "the comment's number is right" limb UPHELD: compact has no fixed height (`height: var(--dock-compact-control-size, auto)`); the forced-padding 40px is 24px glyph + 2×8px coincidence; no shipped or proposed mechanism lands 2.5rem. Reader-2's tab-numbers kill UPHELD with my bytes: density.css tokens override the fallbacks — the `shape="tab"` cure yields 12px/7px on a 38px `--dock-tab-h` box at md, NOT "12px/6px", and `margin-inline` survives it. | glass-capsule/icon-button/density bytes; probe `compactPadUsed "4px"` ×7; o15:105-130 read; workflows grep | Register/mechanism → **X-W8** (Dock-adj G-M already gates the false comment; the D-03 cascade mechanism travels as G-M's addendum per R-C). Oracle/CI limb → **X-W1** (CC-031 — B7 already measures 6/6 projects CI-orphaned; accretes). BH-relay item (the padding token as a documented trap inside its only shipped container) rides the standing fond |
| **ABT-7** | D-04 ≡ L-3 (+ D-13 dead class; hover-scale limb corrected by the D-04-hoverscale kill) | MAJOR | `DockControl` ships the exact primitive hand-rolled here — `shape="tab"` (auto-sized text tab) and `active` (stamps `aria-pressed` + `data-active`, composes the `.glass-capsule` selected seat) — and both are bypassed: hand-written `.is-active` + `:aria-pressed` land the legacy selector arm; `data-active` never exists; the sanctioned selected face never composes. D-13 upheld: the class and the attribute hit the same `:is(…)` arm — dual spelling of one state is why the missing `data-active` looked handled. **Kill upheld**: `shape="tab"` does NOT cure the 10% hover inflation — `tab-button.css`'s hover block carries the SAME `scale: var(--scale-hover-dock)` (my bytes read; the axis stopped one declaration short); the §6 gestalt cure inherits that defect and must say so. | DockControl.vue.d.ts verbatim; dock.js class/attr synthesis; probe siblings[1] class list (no `glass-capsule`); tab-button.css hover block | **X-W8** (Dock-adj scope additions name `ActionBarToggle.vue` — the register rewrite seat). The `:active` swap is mechanical; the shape decision rides the same wave with ABT-6's corrected numbers |
| **ABT-8** | D-02 + D-15 + C-7(inline-ink) + **the rung-mismatch root cause (reader-2 missed #1)** + the identity-authored-twice MINOR | **BLOCKER** | Two per-instance inline `:style="{ color: accent }"` bindings paint seed ink on resting dock chrome — breaching the seed-tint jurisdiction, defeating the producer's entire pressed-ink treatment (inherited property, two child declarations), and failing AA at 16.4px/w400 in the measured cells. **Root mechanism (adopted from reader 2, verified by this seat)**: `SAFE_ACCENT_KEY` carries ink certified against the RESTING rung (`useContrastSafeColor.ts:302-306`) while the dock is FLOATING chrome — the codebase's own documented contract (":337-338 — 'floating' for menu/dock chrome") with two obeying siblings in the same directory (`MobileMenuDropdown:23`, `ProfileSection:28-29`) and three violators (`Dock.vue:35` → this component, `ActionBarLayer:24`, `ColorInput:157`). The exact ratios are seed-variable and live-only; the unguarded variability IS the finding. D-15 upheld: `accentColor` set by NONE of the three bars (my read of all three) — the `??` chain paints a default through three files; and the Tools identity (`"Tools"` + `Paintbrush`) is authored independently in two modules, so the chain reconciles a duplication that need not exist. | useContrastSafeColor/ink.ts/useAtmosphereBoot reads mine; usePaneRouter :191-225 mine; VISUAL-CONSTITUTION §2 jurisdiction quoted by both axes | Ink canon + rung → **X-W10** (canonical design; the one-call contract-restoring cure — route the dock's accent through `useSafeAccentFn("floating")` — fixes all three violators and is stated so the "delete the prop" design ruling doesn't erase the rung lesson). The prop/thread deletion falls out of **X.W4.d**'s contract collapse (D-15 dies with it) |
| **ABT-9** | L-4 ≡ C-5 ≡ Dock-adj **D2-03** (BLOCKER there; this file's two witnesses) + the `lg:block` layer-ownership INFO (reader-1 missed #4) | MAJOR | "Desktop" has three mechanisms and two inequivalent predicates; this file uses two on adjacent lines (`hidden lg:block` :82/:103 beside the `isDesktop` prop :96) against App.vue's own MOB-1 ruling and its "can never disagree" comment. The missed limb verified: Tailwind utilities live in `@layer utilities`, structurally above the producer's `@layer components` display rules — including `overflow.css`'s wrap-mode `.dock-overflow-wrap .dock-separator { display: none }` — so the consumer owns a producer state axis it doesn't know exists (INFO: wrap reachability unestablished today, `:fit-content` only). | All three sites re-read; overflow.css bytes; App.vue:305-312 | **X-W5.c** (the fork-death gate C3: "38 occurrences across 13 files" == 0 — these two witnesses are inside that census) ≡ Dock-adj D2-03/structure (h). One identity, accretes |
| **ABT-10** | D-07 (label + zoom limb CONFIRMED; touch-target mech corrected per R-B) | MAJOR | The visible wordmark is deleted by VIEWPORT WIDTH (`v-if="isDesktop"`, a width query), so 200% zoom and every phone get an unlabelled, tinted, plate-less glyph — content lost as a consequence of an accessibility accommodation (SC 1.4.4). The coarse-target symptom (33.3×33.3) is real; its mechanism is the `compact` width/height override discarding the producer's coarse floor (R-B) — NOT the touch-floor rule D quoted (dead for the whole dock) — and the cure D prescribed (`shape="icon"` sans `compact`) restores the floor only via the size clamp, not via the quoted rule. | R-B bytes; corpus zoom/mobile rects carried labeled (residue) | **X-W4.a** (CC-040 — the shell target-size wave; its A2 gate already measures dock controls under coarse) with the label-survival arm attached; the label-bearing register itself rides ABT-7's X-W8 rewrite |
| **ABT-11** | D-08 (mechanism limb + reader-2's floor-knob miss; outline narrative KILLED) | MAJOR | The only focus indicator is a 2px ring at 30% alpha of the LIVE SEED (`--focus-ring-color: var(--accent-live)`, foundation.css — a deliberate global re-point; `--accent-live` mirrors the same resting-certified computation), i.e. user-variable by construction, judged against the wrong floor semantics: a focus ring is non-text ink (1.4.11, 3:1) and `useSafeAccentFn`'s own documented `GRAPHICS_CONTRAST_FLOOR` (3) parameter is never used by any ring. Kill upheld: "an outline was declared at 3px and then killed" is a getComputedStyle misreading (`outline-width` initial `medium` serializes 3px; `outline: none` itself produces it). Forced-colors corrected per reader 1: pressed gets a 2px Highlight border (accessibility.css) and `:focus-visible` gets a Highlight outline (a11y-overrides.css) — only the REST-state boundary and the seed-derived ring are genuinely open. | foundation.css read; accessibility.css + a11y-overrides.css bytes; probe focus cell (boxShadow = seed at 0.3/0.15) | **X-W10** (ink/focus canon; shares ABT-8's rung cure — a floating-rung, 3:1-floored ring certificate is one `useSafeAccentFn("floating", GRAPHICS_CONTRAST_FLOOR)` away). The 1.65:1 composite figure stays residue |
| **ABT-12** | **Label in Name — SC 2.5.3 (reader-1 missed #1, NEW)** | MAJOR | Accessible name "Toggle action bar" (:90, overrides content) does not contain the visible label "Tools" (:96-98; every call site literal) — voice-control users cannot activate the control by saying what they read. All three axes HELD the proof (the printed AX snapshot; the CDP name cell) and read it only for ghost-presence; D even filed the aria-label as a clean negative proof. Desktop/≥1024 only (below lg no visible text exists). Cure is a subtraction: let "Tools" name the button; the verb moves to ABT-5's `aria-expanded`. | ActionBarToggle :90/:96-98 + Dock.vue:186-187 + usePaneRouter literals — all mine | **X-W4.a** (the naming gate family; CC-041's nameless-control kin). NEW ROW at wave open |
| **ABT-13** | L-6 as RESCOPED (R-A) | MAJOR→**RESCOPED (INFO facts)** | Surviving facts: 3 dead paths targets, 2 omissions, the false "8-key" comment. Binding mechanism REFUTED (R-A); no live break; severity follows Dock-adj L-22's INFO. | R-A resolver run | FOLD → the standing **W-HYGIENE config-parity identity** (ColorSpaceSelector-adj L-14; cure = DELETE per Dock-adj L-22). **No X CC row found — surface at the next formation boundary** (NO-WAVE-OWNER guard so the V→X transposition doesn't orphan it) |
| **ABT-14** | L-8 ≡ Dock-adj D3-09 | MINOR | The dock barrel re-exports 3 producer symbols consumed both ways on adjacent lines (Dock.vue:4 `"./"` vs :5 direct); Dock-adj found it the tree's ONLY runtime cycle. `Dock` re-export earns its keep. | index.ts + Dock.vue:4-5 mine | **X-W8** (explicit file bounds: `demo/shell/dock/index.ts · Dock.vue` modify-carve, W8.md:84; G-Q's `from "./"` grep already RED) |
| **ABT-15** | L-10 | MINOR | `@mkbabb/glass-ui ^7.0.0` + `@mkbabb/keyframes.js ^6.0.0` in `dependencies` while `src/` imports neither (2 doc-prose hits) — every library consumer installs a Vue design system for zero runtime reason, contradicting inv-K-1 at the distribution level. | Both readers' greps consistent; package surface is X-W9's file | **X-W9** (`package.json` in its §Files; the 4.1 cut) → `devDependencies` |
| **ABT-16** | D-09 + D-10 ≡ L-9 + D-12 (tier limb; separator limb ≡ Dock-adj **D2-08**) + PRM-hover-step (reader-1 missed #3, NEW) | MINOR cluster | Type: a control label in Fraunces 16.4/w400 against the canon's closed matrix (dock-wide habit, honestly scoped). Glyphs: 24px/12px Tailwind utilities in `@layer utilities` structurally beating `--dock-icon-glyph` (20px) — three rungs in one dock, two declared here. Tier: no plate at rest yet ×1.1 hover inflation beside a non-scaling Select trigger. Separators: 3 lines for 4 seats — D2-08 already rules main-layer count 0 (R-D's oracle collision attaches). **NEW, proven from the corpus's own unread data**: under PRM the ×1.1 hover SURVIVES as an instantaneous step — the only scale-neutraliser (`.glass-capsule-hover:hover { scale: 1 }`, (0,2,0)) loses to `.dock-icon-button:hover:not(:disabled)` (0,3,0), while the PRM guard strips `scale` from the transition list; `prmRoundTrip[4]` measures `toolsW: 115` vs `104.5` UNDER `reducedMotion:"reduce"`, amputated on three sides (compounds ABT-1). | Bytes + probe, all mine | Type/tier → **X-W10**; separators → **X-W8** (D2-08/G-M); glyph utilities die with ABT-7's register rewrite (X-W8); the PRM scale step is producer-owned mechanism → **BH RELAY** (standing fond) with the consumer's register choice charged at X-W8 |
| **ABT-17** | D-14 + D-11 + L-11 + false-record-header class (reader-1 missed #5) | INFO cluster | D-14: `label` is a constant wearing a variable's type — no overflow design; dies with X.W4.d's typed contract. D-11: a directional arrow on a toggle whose motion is inward; RTL half UNPROVEN-NEEDS-LIVE at the render, source-certain (physical glyph, no logical handling) — ≡ Dock-adj D2-07 (G-Q one-logical-chevron). L-11: shell/feature directory inversion — recorded context, no locus (Dock-adj carries it). Header class: two of the three header invariants are false records with no re-measuring gate (the PRM premise :28-29; the T-36 promise :147-153) and the one gate that existed asserted the comment, not the producer — any cure retires prose-as-record or reproduces both defects at the next producer bump. | Reads mine; identity anchors cited | D-14 → **X-W4.d**; D-11 → **X-W8** (D2-07); L-11 → recorded, NO-WAVE-OWNER; header-class → rides ABT-6's G-M comment-truth arm (X-W8), and the class-level law is offered to the formation addendum |

---

## Killed-claims register (refutations upheld or issued by this seat)

1. **D-04-hoverscale** (reader 1) — UPHELD. `tab-button.css`'s hover block carries `scale: var(--scale-hover-dock)` (my bytes read); `shape="tab"` does not cure the inflation; the §6 gestalt cure inherits it. The Select-trigger half survives for a different reason (different primitive, no scale).
2. **D-07-mech** (both readers) — UPHELD. The quoted touch-floor rule is dead for the entire dock (second `:not()`); the real forfeit is `compact`'s width/height override discarding `--dock-control-floor` (R-B). D's cure-as-written delivers nothing through the rule it cites.
3. **C-7-dismissal** (reader 1; reader 2 overruled per R-B) — UPHELD. The floor reaches non-compact dock controls (C's own 44px measurement); `compact` is a chargeable local choice, not a producer-wide excuse.
4. **C-negproof-T36** (both readers) — UPHELD, the corpus's largest single error: "padding 8px 12px … holds" contradicts the cascade (my re-derivation), all seven probe samples (`4px`), L's pasted RED, and its own arithmetic (8px around a 24px glyph ≠ a 32px box). Copied from o15's expectations, not measured; it is why challenge-C never reached D-03/L-2.
5. **D-06-count** (reader 1) — UPHELD with my static census: 14 views, 6 bar-carrying desktop routes, ghost set 8 — D's "eleven of fifteen" wrong in both directions; the defect itself stands.
6. **D-01-scoping** (reader 1) — UPHELD. The stuck state needs only an uncommitted flap; L measured it without PRM; "PRM-specific" and D's negative-proof row both die; the two machines merge into ABT-1.
7. **D-08-outline** (reader 1) — UPHELD. `outline-width: medium` serializes as 3px with no declaration anywhere (my grep of the dock styles found none outside forced-colors); "declared then killed" is a getComputedStyle misreading.
8. **L-2/D-04 tab-cure numbers** (reader 2) — UPHELD with bytes: density tokens override the quoted fallbacks (12px/**7px**, `--dock-tab-h` **38px** at md); "the Button-primitive scale T-36 asked for" is not what the cure delivers; `margin-inline` survives the "only `gap` survives" claim.
9. **D-03's correction to Dock-adj D2-09** (reader 2) — UPHELD in the "number is right" limb: no shipped or proposed mechanism produces 2.5rem (compact height is `auto`; the tab lands 38px). "Mechanism dead" survives; the 40.0px counterfactual may not be carried into the register as vindication of the comment.
10. **L-6's binding mechanism** (BOTH readers confirmed; killed by THIS seat, R-A) — `@mkbabb/value.js/css` and `/value` typecheck against the LOCAL working tree via package self-reference (my `ts.resolveModuleName` run with the shipped config); nothing binds the installed 4.0.0; the md5 divergence is inert; the traceResolution excerpt was a candidate probe, not the winner. Matches Dock-adj L-22's deciding run, which both readers had and neither consulted.
11. **C-1's "unrecoverable without reload"** — SCOPED, not killed: an out-of-band hash navigation to a bar-carrying route restores the layer; no in-page affordance survives, so the user-facing claim holds and the absolute word does not (both readers agree; recorded so the wave's gate asserts the right thing).
12. **L-7's route label** — `/#/about` is not a VIEW_MAP view (about is picker's right pane); the repro ran the unknown-route fallback. Mechanism confirmed independently of the label; the transcript must be re-taken against a named route before citation as canon.

---

## Superlatives (provenance preserved)

- **`ActionBarToggle.vue:137-142`** — the recorded REJECTION of `overflow: clip` + `overflow-clip-margin` with its named cross-engine cause ("Safari ships no overflow-clip-margin"). Correct, non-obvious, written at the point of decision — the idiom the rest of the header's prose should have followed; the contrast with the T-36 comment is the whole lesson. (All three seats + both readers concur.)
- **`:69-72` + `:79`** — the two-layer transitionend filter: `.self` rejects bubbled child transitions AND the `propertyName` guard rejects the co-transitioning `opacity` on the same node, which `.self` alone cannot. C measured ~7 bubbled events correctly rejected. Exactly right — and it is the channel ABT-1 proves a producer stylesheet can silently remove.
- **`:12-17`** — the S.W7-6 problem statement: causal, quantified, falsifiable; the boot cure WORKS (probe: boot lands seated with `overflow: visible`, PRM included). The component's one fully-discharged claim.
- **The separator fold (`:81-82`) + its oracle (o15:161-165)** — a register decision shipped WITH its gate. **Caveat attached (R-D): D2-08 rules main-layer separators → 0; the oracle case must retire with that landing or it manufactures a false alarm.**
- **`Dock.vue:78-89`** — the U-F48 declarative-hold collapse with the equivalence argument in the comment. Sound abstraction; its only sin is faithfully propagating ABT-3's unsound input.
- **`:38` + `:53`** — reactive-props destructure paired with the getter watch — the exact pairing that silently breaks under Vue 3.5, right here; unanimous negative proof across five seats.
- **The corpus's evidence discipline** — every seat colocated runnable probes; `abt-probe2.json` alone PROVES ABT-1 (prmRoundTrip[2-3]), PROVES the PRM hover step nobody filed (prmRoundTrip[4]), and REFUTES two of its own authors' claims (`compactPadUsed`, the hover-scale limb). Reproducible probes that outlive and correct their authors are the highest form this audit takes.
- **Cross-seat correction culture** — D-03's explicit correction of D2-09; C-8's mutation table proving the gate vacuous and catching o10d STEERING AROUND C-1's condition; reader 2's structural promotion of L's labelled HYPOTHESIS. (And the counter-lesson: L-6 shows unanimous confirmation is still not derivation — the resolver was one command away.)
- **`demo/color-session/ink.ts:92-126`** (reader 2's find) — the parameterised material-rung ladder is genuinely excellent library design; the dock consuming the wrong rung is a defect precisely because the right call was one string away.
- **`usePaneRouter.ts:126-136`** (reader 2's find) — `leftProps` really is one path for both breakpoints, which is what makes the ONE un-unified channel (`:on-mount`) stand out and reduces the whole mobile defect family to a single asymmetry.

---

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

All mechanisms below are source/dist-certain; the FIGURES are live-only. Every pixel claim in this record is chromium-cell unless noted; webkit/safari are separate cells.

1. D-02/ABT-8 rendered ratios (4.04:1 light / 4.12:1 dark / sibling 7.97:1) — seed-variable by construction; re-measure across a seed sweep, floating-rung referent.
2. D-08/ABT-11 composited focus-ring figure (1.65:1) + the forced-colors REST-state render (the pressed/focus-visible arms are producer-covered; only rest is open).
3. D-05/ABT-5 geometry jolt (dock x/w step, toggle 82.6px outside its dock) + `pressedInAx: false` — re-witness after any layer-swap change.
4. C-3/ABT-1 stale-boot hit rate (12/12, two engines) + overpaint geometry (45.4px/121.5px) + its USER-VISIBILITY (the axis's own honest concession: opacity is near 0 at t=30ms).
5. C-2/D-06/ABT-4 per-route AX censuses — the two live denominators disagree (4/11 vs 11/15); this seat's static census (8 of 14) is the reference; a live sweep should reconcile all three.
6. L-1/ABT-2 widths (0px @390 vs 125.5px @1440, 12 routes) + the safari-mobile corroboration shot + a LIVE witness of the Save/Cancel discard (tap Save on mobile edit; assert the edit persisted — it does not).
7. L-7/ABT-1 non-PRM stuck repro — re-take against a NAMED route (the gradient→about transcript ran the unknown-route fallback) before citation as canon.
8. D-01/ABT-1 PRM amputation geometry (1.23/1.60px) — in-corpus chromium only; webkit PRM cell unwitnessed.
9. D-07/ABT-10 zoom-200 (32×32, label null) and mobile-390 (33.3×33.3) rects; note X.W4's A2 measured 23.3×23.3 for dock controls in its own cell — the discrepancy itself is a visual-audit input (probe conditions differ; both are sub-floor).
10. D-12 separator positions (x=625.8/757.3/864.8) and the divider-count design judgment — the disposition is Dock-adj D2-08's, already ruled; the visual pass witnesses the AFTER state.
11. D-11 RTL render (`shots/rtl-desktop/*.png` unopened by any adjudicating seat); whether the app ships an RTL surface at all is unestablished.
12. L-2's o15 RED run — structurally certain (4px vs "8px 12px"), never re-executed by an adjudicating seat, and CI cannot execute it (X-W1's premise); the first HARD e2e run after CC-031 lands is the witness.

---

## Verdict

**DEFECTIVE — 4 BLOCKER · 8 MAJOR, adjudicated (not verified).** The component's three documented invariants are one-third true: the boot seat works; the settle machine's restoration channel is removed by the producer's own PRM guard (and by any coalesced flap); the box-model comment records a specificity fight it lost invisibly, guarded by an oracle no CI has ever run. The deepest pattern is inherited state at the wrong altitude — presence, breakpoint, pressed-state, boot-phase, ink-rung, and the action contract each re-derived one level below their rightful owner — and the formation already knows: nearly every terminal disposition above lands on a gate that is ALREADY RED in X-W1/X-W4/X-W5/X-W8/X-W10's own ledgers. What this corpus adds that the program did not have: the PRM property-list kill (upgrading Dock-adj C-28 to BLOCKER), the resting-rung certificate under the dock's floating chrome, Label-in-Name, the PRM hover step, two cross-record corrections (G-M's half-dead token seam; the o15/D2-08 collision) — and one discipline lesson, that a unanimous confirmation died to a single compiler call. Original challenger records, probes, and both re-reader returns remain immutable beside this file per E-1/E-3.
