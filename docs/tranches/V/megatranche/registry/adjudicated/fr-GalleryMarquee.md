# fr-GalleryMarquee — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · served model id: `claude-fable-5`

**Seat**: fresh Fable adjudicator (incredulity seat), model receipt `claude-fable-5` observed in this seat's own system prompt.
**Date**: 2026-08-24 · **Subject tree**: fourier-analysis @ `cd26c65` (working tree) · **Corpus**: challenge-D-design.md (312) · challenge-L-library.md (233) · challenge-C-consumption.md (233), each re-read at the disputed rows by this seat.
**Re-readers adjudicated**: reader-1 (design-and-usage return, slug `fr-GalleryMarquee`) · reader-2 (library-and-code return, slug `GalleryMarquee`). Both were attacked before either was adopted; every verdict-level disagreement was re-derived against the tree with this seat's own instruments.
**This seat's own instruments (static + node, read-only, zero browser — probe parsimony)**: full read GalleryMarquee.vue (134, whole); GalleryView.vue :60-71 / :215-226 / :255-290; GalleryCard.vue :60-190 + :210 (`translateY(-4px) scale(1.02)` line-exact); App.vue :18-32; style.css :40-51 (root 1.125rem → 1rem at 768px, verified); stores/gallery.ts greps — `softDelete` = definition :165 + return :286 only, `restore` = return :287 only (every other `restore(` is `ctx.restore()`), `deleteEntry` :150-155 → `resetAndFetch`, `splice` :171 inside softDelete, `unshift` :182 inside restore. Installed-pin reads (glass-ui **4.0.0** by node): `dist/styles/utilities/a11y-overrides.css:1-20` (the universal PRM blanket, `animation-duration: 0.01ms !important; animation-iteration-count: 1 !important` on `*:not([data-allow-motion])`, reached via style.css:3); `dist/styles/utilities/base.css:326-328` (`.scroll-fade-mask` tokenized gradient); `dist/styles/tokens/offsets-sizing.css:18-26` (`--mask-fade-width: 1rem`, RETIRE-COORDINATED annotation verbatim); `dist/FadingScroll-DwNnvKMs.js` prop census — `grep -c ariaLabel` → **0**, symbols present = `axis`/`fadeStart`/`fadeEnd` only. **The L-16 decider, run by this seat**: GalleryMarquee's template compiled with the repo's own `@vue/compiler-sfc` (`cacheHandlers: true`, script bindings supplied) — output quoted in the kill register. Registry sweeps: `marquee` → fr-GalleryFeaturedCarousel.md + fr-GalleryCard.md only; `nested-interactive|presentational` → GalleryDraftsSection avoided-credit only; `keydown` → no GalleryCard booking; `softDelete|scroll-fade-mask|mask-fade-width` → **0**; `timeAgo` → FR-AUL-17 / FR-AFP-32 / fr-GalleryCard L·M-4 ("FOLDS BY REFERENCE, nothing new to book").

**Anti-rename event discharged**: banked FR-GFC-9 (fr-GalleryFeaturedCarousel.md:40) named this seat in advance — "when the GalleryMarquee/GalleryView seats' apotheoses land, their independently-derived instances of this defect fold to THIS row, one identity." This record is that fold. Nothing in §A is re-booked.

## Scoreboard

**32 CONFIRMED (14 folds honored · 18 booked net-new) · 12 KILLED cells/rows · 7 MISSED FINDS banked (2 of them LIVE MAJORs on GalleryCard) · 7 superlatives ratified · 9 UNPROVEN-NEEDS-LIVE residues → SS-13.**

The load-bearing fact of the whole corpus is banked and holds: **GalleryMarquee has never rendered and cannot render** (FR-GFC-9 / G-LC1 — mount predicate false for all inputs; re-verified at GalleryView.vue:266 ∧ :270 via :64-66 by this seat). Every §B row is therefore **latent** — graded as-if-live, effective only under the FR-GFC-9 delete-or-revive ruling. The two findings that matter TODAY (§C GM-M1/GM-M2) live in GalleryCard, on both live hosts.

## Ruled disagreements (the seat's actual work)

| # | dispute | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L-2b viewport escape** — does the band ever tile? | Adopted the conditional framing ("where 472k−16 ≤ A"); ratified D-6's self-healing note — void closes at k≥6 at A=1400 | Killed the branch: min-content lock, V = 472n−16 unconditionally | **reader-2 WINS; reader-1's ratified self-healing threshold OVERRULED** | Re-derived twice over: (a) CSS Sizing 3 §5.2.1 — `.marquee-inner`'s computed `width: max-content` (:94) makes its min-content contribution EQUAL its max-content contribution; (b) independently, a nowrap flex row of `flex-shrink: 0` definite-width items (:110-112) has min-content = sum of items. Either route gives `.marquee-container` fit-content = 472n−16 at every viewport. And sharper than either reader: since travel = exactly half the inner (W/2 = 236n−8), the void ∩ the centered visible window = **A/2 at end-of-cycle for EVERY viewport width A** — half of whatever is visible is blank, viewport-invariantly. The void never closes at any k. |
| R-2 | **L-16** — do the `(h,t) =>` handlers defeat `cacheHandlers`? | KILLED AND INVERTED (compiled it) | CONFIRMED MINOR ("arrows closing over entry/emit defeat cacheHandlers") | **reader-1 WINS — kill RATIFIED; reader-2 OVERRULED** | This seat's own compile (repo's `@vue/compiler-sfc`, cacheHandlers on): `onSetTier: _cache[1] \|\| (_cache[1] = (h, t) => $setup.emit('set-tier', h, t))` — **CACHED** (and `_cache[4]` in the duplicate loop); `onLike`/`onDelete` cached at `_cache[0]/[2]/[3]/[5]`. The ONE uncached handler is `onClick: $event => ($setup.emit('card-click', entry))` — the `$event`-form sibling L-16 exonerates — uncached because it closes over loop-scoped `entry` (it is the sole listener in the dynamic-props list `["entry","admin-mode","liked-hashes","onClick"]`). `cacheHandlers` bails on scope references, not arity. Residue (the uncached onClick) FOLDS to banked FR-GFC-19 as the marquee instance. |
| R-3 | **L-6/C-5 mechanism** — what strands PRM users? | Confirmed w/ condition A < 472k−16, V-formula from the dead branch | Confirmed "rests at 472n−16 px inside a clipped track" — one paragraph after proving the box never shrinks | **BOTH CORRECTED — row survives RESCOPED (GM-5)** | At rest the inner sits EXACTLY inside its track box (box = inner = 472n−16, the R-1 lock), so `overflow: clip` (:81) clips **nothing** at translateX(0) — the clip only bites transformed overflow mid-animation. The true stranding mechanism is GM-M3: the box overflows the PANE (GalleryView.vue:220 `overflow-y-auto` ⇒ computed `overflow-x: auto` — a scroll container!), centered by :267's `items-center`, so the LEFT overflow ≈ (472n−16−A)/2 is in the unreachable scroll region (LTR) and the right side is reachable only via an accidental pane scrollbar the design never intended. Reader-2's cure stands (overflow-x:auto + snap idiom, duplicate hidden); reader-2's blanket correction also stands — the local `animation: none` (:129-133) is belt-and-braces under the pin's `!important` PRM blanket (verified at a11y-overrides.css by this seat), so no animation-cascade cure is viable (fr-AdminFlaggedPanel rulings 1/3). |
| R-4 | **D-11 severity** | MINOR (latent, different host than FR-GFC-7) | MAJOR | **MAJOR as-if-live, latent — family parity** | Banked FR-GFC-7 books the same absence on the LIVE sibling at MAJOR. This corpus grades as-if-live (challenge-D-design.md:13 discloses the convention); latency is FR-GFC-9's property, not the defect's. Reader-1's MINOR preserved as dissent. |
| R-5 | **L-20 (timeAgo)** — book or fold? | (absent) | Booked MINOR | **FOLD — already banked** | fr-GalleryCard L·M-4 rules the whole timeAgo family "FOLDS BY REFERENCE, nothing new to book" → FR-AUL-17 (F.W3) + FR-AFP-32, both naming GalleryCard:53-61. Identity guard holds; reader-2's registry sweep missed the fold. |
| R-6 | **PRM superlative scope** | Ratified S-3 whole ("best-documented three lines") | Killed the uniqueness/coverage framing (the pin blanket) | **SPLIT — discipline survives, coverage framing KILLED** | The blanket is real at the installed pin (this seat's read): every animation in the app is already PRM-clamped with `!important`; the local block is redundant belt-and-braces (consistent with banked fr-GalleryFeaturedCarousel K1). The citation discipline (wave + SC + audit row + reason, scoped to the sole animation host) survives as S-3. Any census counting PRM blocks as coverage is measuring the wrong thing. |

## §A — FOLDS BY REFERENCE (identity guard; nothing re-booked) — 14

- **GM-F1 = D-1/L-1/C-1 — the dead mount → FR-GFC-9 + fr-GalleryCard G-LC1 (MAJOR banked; D/C BLOCKER dissents preserved).** Re-verified by this seat: GalleryView.vue:266 `!gallery.entries.length && !gallery.loading` ∧ :270 `featuredEntries.length >= 4` with :64-66 `featuredEntries = entries.filter(tier==="featured")` — |filter(S)| ≤ |S| ⇒ 0 ≥ 4, false always; GalleryMarquee.vue:27 duplicates the same dead test. → **F.W4** (the one authored decision: delete or revive; every §B row is that decision's cure list).
- **GM-F2 = D-3/L-4/C-2 — `aria-hidden` over a focusable Delete-bearing subtree.** Structure ADJUDICATED real (:55 wraps `<GalleryCard>` :57-65; card root `role="button" tabindex="0"` :72-73; Trash2 `emit('delete', entry.slug)` :180; no `inert` anywhere in the file). Live arm KILLED by the bank (fr-GalleryCard R-1/K-9: "no aria-hidden-focus violation, ever" — unreachable). Booked as a **latent rider on the FR-GFC-9 revival ruling**; the axe question stays moot (see UNPROVEN). C-2's scope note is the honest form. → rider on **F.W4**.
- **GM-F3 = D-10/L-5/C-7 — `adminMode` forwarded while `selected`/`toggle-select` dropped → FR-GFC-2** (MAJOR banked; its fan-out correction already scopes the cure to the two live hosts and excludes the marquee copies).
- **GM-F4 = L-7/C-11 — `GalleryTier` retyped inline → fr-GalleryCard G-LC3** (MINOR; census law = **9 retype lines / 8 files**, re-run by this seat's readers and matching the bank). Corrections carried: FR-GFC-24's count of 8 is one short (omits stores/gallery.ts:33); C-11's own "8 sites" headline contradicts its 9-item enumeration (cell killed, §KILLS).
- **GM-F5 = L-8 — slugs named `hash` across the whole emit/prop surface → FR-GFC-17** (MINOR; the marquee :9/:14-16 is one of the named leaf/relay holdouts; every payload is a slug — GalleryCard :141/:162/:171/:180 re-read).
- **GM-F6 = L-19 — dead imports `VIZ_COLORS`/`PathPreview` in GalleryCard :9-:10 → FR-GFC-22** (MINOR, F.W0 rider: `strict` without `noUnusedLocals` cannot catch it) + fr-GalleryCard K-13 (`.path-preview` reaches no built sheet while the dead marquee's CSS ships).
- **GM-F7 = C-8 — `cssVarToHex` has no `oklch()`/`light-dark()` arm → fr-BasisCanvas D-1/BC-1/C-1 (banked BLOCKER → F.W1). DO NOT RE-BOOK** (guard already stamped at fr-GalleryCard C-4). Riders carried: `--viz-green` limb dead (K-10); true symptom is chrome-vs-canvas divergence (fr-App B-1); `--viz-amber` survives only via fourier's own hsl override.
- **GM-F8 = C-9 — `basisDisplay` module-eval snapshot of the reactive palette → fr-BasisCanvas D-2/BC-5/C-3 + FR-GFC-8.** Both banked scope corrections carried: path-dependence (FR-GFC R-3/K4 — deep link vs in-app nav capture different values) and fr-App R-1 (default `/paper` entry freezes post-poison grey); the durable half is the theme-flip desync on every path.
- **GM-F9 = C-10 — glass-ui 4.0.0 peer `^0.10.0 || ^0.11.0` vs installed value.js 0.13.0 → FR-GFC FOLD-2 / fr-EquationPanel.md:117** (MINOR). Reader-2's scoping riders ADOPTED: the peer is `optional: true` (no npm hard-error), the drift is value.js-only (keyframes 4.3.0 satisfies; @lucide/vue installed), and the F.W1 uplift RELOCATES the violation (v7/v8 peer → ^4.0.0), never discharges it silently.
- **GM-F10 = L-9 — 100%-native-loop component; census 16 native / 16 component / 1 `<template v-for>` = 33 → FR-GFC-28 + fr-GalleryCard L·I-2.** Reader-2's independent walk-back re-derivation RECONCILES the banked 17/16 vs 16/16/1 as one measurement under two bucketings of MobileFloatingToc:149 — **the FR-GFC-28 re-derivation ask is DISCHARGED**. The sharper indictment stands: a callsite-keyed deriver scores two callsites (:40/:57) that expand to 2·entries instances in a subtree that can never render.
- **GM-F11 = D-17a/L-21 — `--deferred-section-size: 17rem` calibrated for a ~15rem grid cell, imposed on a 13.75rem/12.2rem marquee cell → FR-GFC-25** (INFO per ruling R-6, self-correcting `auto` prefix). The per-cycle relevance-churn arm (infinite translate crossing the relevance boundary twice per cycle forever) → UNPROVEN #3. Width is safe: `.marquee-item` fixes 220px, a skipped card cannot perturb the −50% travel (reader-2's L-21 cell, adopted).
- **GM-F12 = D-18 — zero automated coverage → FR-GFC-27** (INFO). Re-verified: `grep -rni marquee web/e2e/` → 0 across all 8 specs; the repo owns `@axe-core/playwright` + a checkA11y zero-serious/critical helper never pointed at a gallery card; the booked ConfiguratorLayer `aria-hidden-focus` `test.fixme` exists verbatim at visualization-crud.spec.ts.
- **GM-F13 = D-8 width-census limb — 3rem fade = 6× the sibling's 0.5rem, 28.8% of a 375px viewport → FR-GFC-5** (MAJOR banked; "three fade widths across two adjacent strips"). The R8-08 "Shy" rationale verifies AT THE PIN (base.css). The contrast/PRM-parked limb is net-new → **GM-18**.
- **GM-F14 = L-20 — `timeAgo` render-frozen, no ticker → FR-AUL-17 (F.W3) + FR-AFP-32** via fr-GalleryCard L·M-4's explicit "nothing new to book" (R-5 ruling above).

## §B — BOOKED here (net-new; ALL LATENT behind FR-GFC-9, graded as-if-live) — 18

- **GM-1 = D-2/L-2a/C-3 — MAJOR.** `translateX(-50%)` against a gapped flex row under-translates by exactly gap/2 per cycle: W = 2n·220 + (2n−1)g, travel = W/2 = 236n − g/2, tiling period = 236n ⇒ shortfall **g/2, n-independent** = 8px (≥768px) / 9px (<768px) under the inverted-responsive root (style.css:40-51, this seat's read). Proven by construction; all three axes agree; C-3's cure-is-the-proof falsifier (`gap: 0`) is exact. → **F.W4**.
- **GM-2 = L-2b + D-6-conclusion — MAJOR, STRENGTHENED by R-1.** One copy can never cover the track: the min-content lock fixes the band at 472n−16 at every viewport, travel = exactly half of it, so **half of whatever is visible is blank** — at end-of-cycle on the left track and at FIRST PAINT on the right track (:121-123 runs −50% → 0). At the shipped minimum (entries=4, n=2): 928px band, 464px blank. No viewport escape exists (R-1). → **F.W4**.
- **GM-3 = D-4/L-14/C-6 — MAJOR.** WCAG 2.2.2 (Level A): the only pause is `:hover` (:98-100, sole `animation-play-state` in web/src) — unreachable by keyboard, touch, AT; autostart, infinite, >5s, parallel with the empty-state CTA. The PRM block discharges 2.3.3 (AAA), a different criterion, only for users who opted out. Every card is a tab stop (GalleryCard:73) riding a strip at 10–260 px/s. Cure is one selector (`:focus-within`) — plus GM-14's `!important` removal so a JS pause can ever win. → **F.W4**.
- **GM-4 = D-5/L-3/C-4 — MAJOR.** Duration hard-coded (45s/50s, :102-108; no `:style`/`--dur` anywhere) while travel is ∝ n ⇒ velocity linear and unbounded in |entries| (GalleryView:271 passes `featuredEntries` uncapped): v = (236n−8)/45 — n=2 → 10.3 px/s (functionally static at the guard's own minimum; a card takes ~21s to advance its own width), n=50 → 262 px/s. Reader-1's arithmetic correction adopted (L-3's 10.1/9.1 are 2% low). The producer idiom pins px/s (`ScrollingText`), not seconds. → **F.W4**.
- **GM-5 = L-6/C-5 — MAJOR, RESCOPED (R-3).** Reduced-motion reachability: under PRM both tracks rest at translateX(0) (local `animation: none` + the pin's `!important` blanket — no cascade cure is viable), the duplicate copy stays visually present (each card SEEN twice), and where A < 472n−16 the content past the viewport is reachable only through GM-M3's accidental pane scrollbar, with the left overflow unreachable in LTR. The track's own `overflow: clip` clips nothing at rest (box = inner width) — both readers' clip-stranding framings corrected. Cure = the reachability half the sibling already ships (`overflow-x: auto; scroll-snap-type: x mandatory`) + hide the duplicate under PRM. → **F.W4**.
- **GM-6 = D-11 — MAJOR as-if-live (R-4; reader-1 MINOR dissent preserved).** No accessible name, no region, no heading — bare `<div class="marquee-container">` root; both live siblings self-name (carousel Crown+"Featured"; grid `{{ entries.length }} loaded` at :27); GalleryCard :74 names the ITEM, never the SET; the ancestor is an unlabelled flex div. Family precedent FR-GFC-7 (different host — books, not folds). **The producer-cure cell is KILLED at the pin** (§KILLS #7): `FadingScroll` region-naming props are 7.0.0-only — an accessible name has NO producer vehicle until **F.W1**. → **F.W4** with F.W1 rider.
- **GM-7 = D-7/C-13 — MINOR.** `width: 220px` is the only hard-pixel card width in the gallery family under a root font that GROWS 12.5% below 768px — grid 224→252, carousel 256→288, marquee 220→220: the card shrinks relative to its siblings exactly where space is scarcest. Reader-1's cell correction adopted: D-7's mixed-root arithmetic understates it — mobile well = 220 − 27 − 6.75 = 186.25px against a 15.75px `text-sm`; the claim is strictly stronger than filed. → **F.W4**.
- **GM-8 = D-12/L-12/C-14 — MINOR.** `will-change: transform` static + permanent on two multi-thousand-px elements (472n−16 each; ~11.8k px at n=50), no `contain`, and it survives PRM (the reduce block touches `animation` only; the blanket touches animation/transition only — nothing resets will-change), where it becomes the SOLE promoter, pure cost for the user who asked for less. Redundant while running (UAs auto-promote an infinite compositor animation). Third time of asking (M-deep A8-11 / M-critique E5-12). → **F.W4**.
- **GM-9 = D-13/L-11 — MINOR.** The mask's `0 -50% / 100% 200%` geometry is rendering-inert (a `to right` gradient is Y-invariant; the shorthand resets `mask-repeat` to `repeat`, whose tile fills the box) and the :84 comment documents a mechanism that does not exist (the vertical clipper is `overflow: clip` + the :83 padding). Latent trap ratified: introduce `mask-repeat: no-repeat` anywhere and the top half of both tracks silently disappears. This row is the direct refutation of C·S-1 (§KILLS #1). → **F.W4**.
- **GM-10 = D-14/L-18/C-12 — MINOR.** One state, no loading/error surface (a failed fetch renders identically to a healthy 3-entry set), and the `>= 4` invariant is a bare literal duplicated across a module boundary (:27 vs GalleryView:270) with the :262-264 comment documenting the delegation while re-implementing the test one line later. Neither exported. → **F.W4**.
- **GM-11 = D-15 — MINOR.** Phantom hit targets: `mask` affects compositing only (only `clip-path`/`overflow` clip hit-testing), so a card at alpha ~0.05 inside the 3rem ramp is invisible and fully clickable (opens the modal), and `:hover` pauses the track from a region the user reads as outside the component. Compounded on mobile (ramps = 28.8% of viewport). → **F.W4**.
- **GM-12 = D-16 — MINOR.** Three of the file's four comments misstate their own mechanism (:50 "seamless" — falsified by GM-1/GM-2; :82 "inside the mask" — means the clip; :84 — GM-9), the fourth (:126-128) is exact, plus GalleryView:262-264 describes the only case as an edge case. The self-checked NON-defect ratified: hover headroom ~6.6px needed vs 16px available (~2.4× margin), the shadow token line-exact at the pin (`--shadow-cartoon-hover: 4px 4px 0 0`, no blur/spread). → **F.W4** (comments corrected by whatever wave touches the file).
- **GM-13 = L-10 — MINOR.** A 20-line card invocation duplicated byte-identically but for `:key` and `aria-hidden` (~44% of the template) — verified at the compiled level by this seat (both `_renderList` blocks emit identical prop objects and mirrored cache-slot shapes `_cache[0..2]`/`_cache[3..5]`). The duplication is demonstrably the mechanism that produced GM-F3's dropped contract. The single-loop cure (`[...track, ...track]` + `:inert="i >= track.length || undefined"`) also closes GM-F2 — one structurally sound edit. → **F.W4**.
- **GM-14 = L-13/C-15 — MINOR, both cited mechanisms corrected.** The `!important` on :99 is cargo — but not for either reader's stated reason: `.marquee-track:hover .marquee-inner` = (0,3,0) beats the direction rules' (0,2,0) outright (no tie — L-13's cell killed), AND a competing declaration DOES exist (the `animation` shorthand at :103/:107 resets `animation-play-state` to `running` — C-15's "no competing declaration" cell corrected) — it just loses on specificity. The `!important` will suppress the JS/`:focus-within` pause GM-3 requires. Two prior audits proposed the cure; neither landed. → **F.W4**.
- **GM-15 = C-16 — MINOR.** Odd N: tracks split ceil/floor (:19-23) under fixed asymmetric durations ⇒ at N=5, 15.6 vs 9.3 px/s — the authored 45:50 = 1.11× parallax becomes ~1.67× by parity accident. Arithmetic exact. Compounds GM-4; the same parity rule's second, bigger consequence is GM-M5. → **F.W4**.
- **GM-16 = L-17/C-17 — INFO.** Dead generality around a hardcoded 2 spelled three ways (`[[], []]` / `i % 2` / `tIdx % 2 === 0` over a known-length-2 array), plus `:key="tIdx"` where the file's other loops key on slugs. Idiom drift, harmless while positional. → **F.W4**.
- **GM-17 = L-22/C-18 — INFO.** Zero direct API consumption (`thumbnailUrl` is a pure string builder, not one of the 45 operations); the duplicate track doubles `<img>` count to 2·entries with `loading="lazy"` largely defeated inside `width: max-content`; identical URLs share the decoded bitmap so the cost is DOM/layout, not network — stated honestly by both axes. No canvas coupling. → **F.W4**.
- **GM-18 = D-8 contrast limb — MINOR (net-new arm of GM-F13).** A mask multiplies composited alpha, so contrast → 1:1 as alpha → 0: a sub-AA sub-band necessarily exists inside each 48/54px ramp at all times — and under PRM the strip parks at translateX(0) with the first card inside the leading ramp PERMANENTLY, while `calc(100% − 3rem)` feathers the LAST card too (the shrink-to-fit box has no overflow to justify it). Monotone argument decidable from geometry; measured ratios → UNPROVEN #2. → **F.W4**.

## §C — MISSED FINDS banked (ratified by this seat; registry-swept clean) — 7

- **GM-M1 (reader-2) — MAJOR, LIVE TODAY on both hosts.** GalleryCard's root is `role="button" tabindex="0"` (:70-79) wrapping up to six focusable controls — `<Checkbox>` :90-95, like `<Button>` :135-145, three admin Buttons :156-183 (all re-read line-exact by this seat). ARIA 1.2 gives `role=button` Children-Presentational: True — the textbook axe `nested-interactive` (serious) shape, fires on the ORIGINAL cards in GalleryFeaturedCarousel + GalleryInfiniteGrid right now, a DIFFERENT identity from the dead marquee's aria-hidden row (GM-F2). Registry-swept: the only registry hit is fr-GalleryDraftsSection:115 crediting the trap as AVOIDED; the value.js-repo bookings (AdminUsersPanel/AdminListItem) are a different repo, declared not re-booked. The irony ADJUDICATED with it: the :65-69 D.W4.c comment names this construction as the canonical cure and banked FR-GDS B-1 prescribes copying it — **the cure template propagates the defect; correct the template before it is copied again**. → **F.W3/W4**.
- **GM-M2 (reader-2) — MAJOR, blocker-class pending SS-13 keystroke; LIVE TODAY.** `@keydown.enter.prevent` / `@keydown.space.prevent` on the card root (:78-79, no `.self`) run on keydowns BUBBLING from the nested native buttons (glass Button renders `<button>` — pin dist verified by reader-2) and `preventDefault()` cancels the activation click while `emit('click')` opens the modal — **every per-card action (Like, Feature, Save, Delete, Select) is keyboard-dead**; the nested `@click.stop`s stop clicks, not keydowns. WCAG 2.1.1 Level A. Grading precedent: fr-AnimationControls R-6 (MAJOR, "blocker-class if the SS-13 keystroke confirms"). Registry `keydown` sweep → no booking. The highest-value SS-13 probe on this surface (UNPROVEN #5). → **F.W3/W4**.
- **GM-M3 (reader-2) — MAJOR (latent).** The min-content lock (R-1) means the band can NEVER shrink to the viewport: 928px at the shipped minimum (2.5× a 375px phone), 4704px at 20 entries. GalleryView:220's `overflow-y-auto` computes `overflow-x: auto` (CSS Overflow 3 §3.1 — verified in source by this seat), so the gallery pane grows a horizontal scrollbar sized to the marquee, and the centered box splits its overflow both ways — the left half unreachable by scrolling in LTR. This is the fact that kills D-6's mechanism and L-2b's branch, and it is what a naive FR-GFC-9 revive ships. Mirror of banked FR-GFC-6's overflow-axis computation on the other axis. Painted consequence → UNPROVEN #6. → **F.W4**.
- **GM-M4 (reader-2) — MINOR, live dead-code.** `softDelete` (:165-176: ETag capture, `api.deleteVisualization(slug, etag)`, splice) and `restore` (:178-186: unshift) are exported store actions with ZERO callers — this seat's grep: definition + return-object entries only (:286/:287; every other `restore(` is `ctx.restore()`). They are the store's only splice/unshift mutators and its only ETag/If-Match round-trip on the gallery path; everything reachable funnels through `resetAndFetch()`. The fact that kills L-15's scenario, and an independent dead-surface finding (the :162-164 contract prose documents a round-trip no user can trigger). → **F.W5-W8** (CRUD/provenance union).
- **GM-M5 (reader-1) — MINOR (latent).** Odd N leaves track 1's inner exactly 2·(220+16) = **472px narrower than its own stretched box** (column-flex `align-items: stretch` gives both tracks the wider track's width) — bare background at its trailing edge at EVERY phase, on top of GM-2's half-band loop void; the 3rem ramp attenuates only 48-54px of it. All three axes read the parity rule; only its velocity consequence (GM-15) was filed. → **F.W4**.
- **GM-M6 (reader-1) — MINOR (latent).** At the guard's own minimum the "seamless loop" trick is VISIBLE as literal repetition: n=2 ⇒ all four `.marquee-item`s of a track on-screen simultaneously — A, B, then A′, B′ 472px to their right; the author guarded against the duplicate being ANNOUNCED twice (:55) and never checked that at the minimum it is SEEN twice; and the right track's first frame is half background (GM-2's first-paint arm) under the copy "No visualizations yet." (Non-PRM mode; under PRM both tracks rest at 0.) The perception twin of GM-2. → **F.W4**.
- **GM-M7 (reader-1) — INFO (latent).** The marquee hand-rolls a utility and a token the producer ships AT THE INSTALLED PIN: `.scroll-fade-mask` (base.css:326-328, same gradient tokenized — this seat's read) with `--mask-fade-width: 1rem` (offsets-sizing.css:26, RETIRE-COORDINATED), at 3× the token width, including a `-webkit-mask` companion the producer's own single-source policy drops (base.css AV.W16 TW7 clause; fr-GalleryFeaturedCarousel:101 already proved the marquee's `-webkit-mask` is the redundancy). Width census folds to FR-GFC-5; the utility/companion arms were unbooked and unnamed by all three axes. → **F.W3/W4**.

## Killed-claims register — 12

1. **C·S-1 — KILLED (both readers; verified by this seat).** The superlative credits a "vertical midband" that cannot exist: `linear-gradient(to right, …)` is Y-invariant, so `0 -50% / 100% 200%` is rendering-identical to `0 0 / 100% 100%` and the falsifier's promised regression cannot occur. A superlative awarded to dead bytes, contradicting two axes that are right (GM-9). C's superlative count drops to 2; no repair wave preserves the geometry on C·S-1's authority.
2. **L-16 as filed — KILLED AND INVERTED (R-2; this seat's own compile is the decider).** `(h,t) =>` handlers ARE cached (`_cache[1]/[4]`); the exonerated `$event`-form `onClick` is the ONLY uncached one (loop-scoped `entry`). As filed, a repair wave would rewrite three cached handlers and leave the uncached one alone. Reader-2's confirmation OVERRULED. Residue (uncached onClick) folds to banked FR-GFC-19.
3. **D-6 mechanism + arithmetic — KILLED; conclusion survives in GM-2.** Inner at k=2 is 928px, not 944 (gap double-count); the track is sized by the min-content lock, not the "≥ ~900px gallery pane"; the "container is narrow — mobile only" falsifier branch is void (the container never shrinks). Evaluated against the wrong box throughout.
4. **L-2b narrow-viewport branch + reader-1's ratified self-healing threshold — KILLED (R-1).** `V = min(max-content, A)` is the wrong formula (min-content = max-content here); there is no viewport at which the marquee tiles; the "void closes at k≥6 / A=1400" claim is dead — the visible void is A/2 at end-of-cycle at EVERY viewport. Arbiter-level correction of a reader-1 confirmed cell.
5. **L-13 specificity cell — KILLED (conclusion survives as GM-14).** No tie: `:hover` counts in the class column, (0,3,0) vs (0,2,0); L-13's own falsifier condition is the one that actually holds. **C-15's "no competing declaration" cell corrected** in the same stroke: the `animation` shorthand resets `animation-play-state`.
6. **L-15 trigger path — KILLED (verified by this seat).** The marquee's delete reaches `deleteEntry` (:150-155) → `resetAndFetch()` — full teardown + refetch (the banked FR-GFC-1 mechanism), no splice. The cited splice/unshift live in `softDelete`/`restore`, which have ZERO callers (GM-M4). The parity-churn mechanism is sound in the abstract and unreachable in fact.
7. **D-11/D·L-1 `./fading-scroll` producer cell — KILLED AT THE PIN (K-12 class; re-verified at the bytes by this seat).** `dist/FadingScroll-DwNnvKMs.js` declares exactly `axis`/`fadeStart`/`fadeEnd`; `ariaLabel` grep → 0. The region-naming props exist at v7.0.0 only (D read a 7.0.0+ tree, named no baseline; producer working tree is now 8.0.0). D's disposition item 4 ships a cure that cannot supply GM-6's accessible name until F.W1. Rider sharpening banked FR-GFC-5/R-7: at 4.0.0 the COMPONENT supplies the state-driven fade but not the naming.
8. **D·L-1 Checkbox provenance cell — CITE KILLED, substance survives.** `src/index.ts:124` is an 8.0.0 working-tree anchor, not the pin or the uplift target; the claim is already discharged at the tags by FR-GFC-29 (v7.0.0 root barrel re-exports Checkbox; `./checkbox` subpath absent at 4.0.0 AND 7.0.0, appearing at 8.0.0 — no subpath to "tighten to" during uplift).
9. **C-11 count cell — KILLED.** "8 sites" headline over a 9-item enumeration; the law is G-LC3's **9 lines / 8 files** (both readers' independent greps + the bank agree). FR-GFC-24's 8 also corrected. A repair wave quoting either 8 leaves one file behind.
10. **K10-class line-drift, extended.** The banked "challenge-D systematic sibling-file line drift" recurs (GalleryCard :78→:71, :136-144→:135-145, :178-183→:175-183, :196-206→:199-206; GalleryInfiniteGrid :23→:27, :27→:29) and is NOT D-only in this corpus: L-4 cites :167-192 (actual :157-183), L-5 cites :90 (actual :88), C-7 cites :34/:40 (actual :36/:41). The C axis's GalleryCard anchors are the only line-exact set (spot-verified by this seat: :70-80, :90-95, :135-145, :156-183) — **repair waves quote C's anchors or THIS record, never D's**.
11. **S-1-as-PRM-uniqueness — KILLED (R-6).** "One of only 8 PRM blocks / the seed the rest grew from / the only PRM handling" is over-credit: the installed pin ships a universal author-origin `!important` PRM blanket (a11y-overrides.css, read by this seat) reaching every animation end-to-end. The local block is belt-and-braces; the documentation discipline survives as S-3. Corollary: no animation-cascade cure for GM-5 is viable — the escape hatch is welded by design (fr-AdminFlaggedPanel rulings 1/3).
12. **L-6/C-5 track-clip mechanism cell — KILLED in both filings; row survives RESCOPED (R-3/GM-5).** At rest the box equals the inner width, so `overflow: clip` clips nothing; reader-2 confirmed "inside a clipped track" one paragraph after proving the box never shrinks. The stranding that exists is GM-M3's centered pane overflow.

## Superlatives — ratified, with provenance

1. **S-1 — `overflow: clip` not `overflow: hidden` (:81).** The correct, non-obvious primitive: `clip` establishes no scroll container, so programmatic/keyboard focus can never scroll the box and desynchronize transform from layout — the classic hand-rolled-marquee bug, avoided. It is also what makes GM-5 a design consequence rather than a scroll bug: the author bought the right primitive and never paid for the affordance it implies. (D·S-2; both readers; ratified.)
2. **S-2 — the dual-track parallax (:19-23/:32/:102-108).** `i % 2` PARTITIONS rather than duplicates; 45:50 = 9:10 ⇒ phase relationship repeats only at lcm = 450s = 7.5 minutes — the eye never resolves the pair into a repeating grid. Three declarations, zero JS, zero clocks (verified: no lifecycle/listener/rAF/observer greps hit). (D·S-3/L·S; ratified, arithmetic checked.)
3. **S-3 — the PRM block's citation discipline (:126-133).** Names the wave, the SC, the originating audit row, and the reason; scoped to the sole animation host. Ratified WITH R-6's limit: the coverage/uniqueness framing is dead (the pin blanket), and all three axes credited the discipline while faulting the same three lines without self-contradiction.
4. **S-4 — two independent correct REFUSALS.** D·L-1 declined to claim a `FadingScroll` shadow ("the marquee has no scroll port") — proven exactly right at the bytes (fade driven by `scrollLeft`/`scrollWidth − clientWidth`, quantities a transform strip never produces); C·S-2 declined `ScrollingText` ("finite overflow cannot express a seamless tile"). Convergent correct refusal, the corpus-level signal the bank prizes (cf. fr-GalleryFeaturedCarousel:100).
5. **S-5 — the C axis's evidence hygiene.** Cure-is-the-proof falsifiers (C-3's `gap: 0`), the exact source-vs-live line (C-2's UNPROVEN-NEEDS-LIVE self-mark), and the only line-exact sibling anchors in the corpus (kill #10) — quote C's anchors, per banked K10.
6. **S-6 — D-16's verified negative.** The hover-headroom arithmetic run, recorded as a NON-defect so it is never re-flagged, with the load-bearing producer value line-exact at the installed pin (`--shadow-cartoon-hover`, no blur/spread) — one of D's very few baseline-clean producer cites.
7. **S-7 — nothing to leak, and running falsifiers.** 134 lines, one `defineProps` + one `defineEmits` + one 5-line computed reading `props.entries` inside itself; zero teardown obligations — which is exactly why every defect here is contract or arithmetic, never lifecycle. And the corpus's falsifier discipline is load-bearing: two of this adjudication's kills (C·S-1, L-13) came from EXECUTING falsifiers their authors wrote. Plus zero producer coupling — the file's first-order exposure to the tri-package uplift is nil (D·S-5; ratified).

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs — 9

1. Perceived magnitude of the 8/9px seam snap once per 45s/50s (geometry proven; perception is not).
2. Measured contrast ratios inside the 3rem ramps, both themes, three sample points (GM-18's monotone argument is decided; the pixels are not) — and the specific card parked in the ramp under PRM.
3. `content-visibility: auto` relevance churn per cycle + first-cycle row-height wobble against the 17rem hint at a 13.75/12.2rem cell (GM-F11; carried as SS-13 item 7 on banked FR-GFC-25).
4. The axe `aria-hidden-focus` run on the duplicate subtree — moot while FR-GFC-9/G-LC1 hold; re-opened only by the revival ruling (C-2's own self-mark).
5. **The GM-M2 keystroke witness — the highest-value probe on this surface (LIVE hosts, not the dead marquee)**: on /gallery, Tab to a card's Like button, press Enter → confirm the modal opens and no like registers; repeat with Space, the admin Crown/Bookmark/Trash, and the Checkbox. Severity re-opens to blocker-class on confirmation (fr-AnimationControls R-6 precedent). Pair with the axe `nested-interactive` run (GM-M1) — the repo's own checkA11y helper has never been aimed at a gallery card.
6. GM-M3's painted consequence: whether the pane's horizontal scrollbar renders or App.vue:24's `h-dvh overflow-hidden` wrapper swallows it (the sizing derivation is proven; the paint is not). Gated on a reachable marquee existing.
7. L-12's VRAM figure for the permanently promoted 472n−16 px layers (width arithmetic exact; memory cost not statically decidable).
8. Whether the shipped `dist/assets/GalleryView-*.css` proving the dead `.marquee-*` block ships is current with HEAD, and whether the dead marquee's JS survives tree-shaking in a fresh production build — carried from fr-GalleryCard's own UNPROVEN #4; bears directly on what the FR-GFC-9 delete-or-revive ruling saves.
9. First-paint layout settle from the 17rem `--deferred-section-size` hint (banked FR-GFC-25 rider, restated here for the marquee cell).

## Closing verdict

**ADJUDICATED**: a 134-line component whose five best decisions are real and whose geometry has never been watched by anyone — every arithmetic and a11y defect in it is latent behind a mount predicate false for all inputs (FR-GFC-9, the fold this record was named in advance to perform); the band cannot tile at any viewport (the min-content lock, this seat's own re-derivation, kills both readers' escape branches in opposite directions); the two findings that ship TODAY live in the card it instantiates — GalleryCard's nested-interactive root and the keydown swallow that makes every per-card action keyboard-dead on both live hosts (GM-M1/GM-M2 → F.W3/W4, keystroke witness → SS-13) — and the marquee's own fate remains FR-GFC-9's one authored decision at F.W4: **delete, or revive with §B as the price of admission**. Dispositions are ADJUDICATED, not VERIFIED; X·F specs are forming and consume this record as agglomeration input (M-25).

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
