**Served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited (M-23/L-18 receipt, line 1 by commission).**

# shell-dock-actionbutton — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Date**: 2026-08-08 · **HEAD**: `8d7e17fe` · branch `tranche-u` · subject `demo/shell/dock/ActionButton.vue` (137 L, last touched `f2c8f565` glass-7 adoption) + consumers `ActionToolbar.vue` (92 L) / `layers/GenericActionBar.vue` (33 L).
**Corpus**: `audit/components/shell-dock-actionbutton/` — challenge-C (13 rows) · challenge-D (17 rows + D-0) · challenge-L (11 rows), r2 governs; one probe frame (`probe-actionbar-open-desktop.png`).
**Re-readers adjudicated**: design-and-usage (r-DU) and library-and-code (r-LC), both hostile, both challenged back by this seat. Where they disagreed I re-derived with my own instruments; where they agreed suspiciously I spot-re-measured; two of their kills I killed or trimmed, one of their misses I rescoped, and one of r-LC's severity raises I refused on identity grounds.
**This seat's own instruments (all static, zero browser probes — probe parsimony; every load-bearing cell re-derived, not inherited)**: full reads of ActionButton.vue / ActionToolbar.vue / GenericActionBar.vue (whole ×3 — every cited anchor exact, incl. the blank lines at ActionToolbar 21/32/43/56); Dock.vue:30-190; App.vue:70-140 + :300-350; usePaneRouter.ts:30-120 + :185-229; demo/styles/focus-ring.css (whole); e2e/smoke/oracles/o10d-display-voice-census.spec.ts:340-374 (the C-10 decider); eslint.config.js:225-280 + `ls -d demo/@` → ENOENT; demo/DESIGN.md:245-270; motion-quarantine.md rows M-03/M-29/M-32; ColorPicker.vue:320-328; demo/shell/dock/index.ts; demo/ui/{popover,tooltip}/index.ts; producer bytes: `dist/popover-BQGYXZyO.js` whole head (latch `x`, `onScopeDispose(S)`, the emit-less prop watch, `inheritAttrs:!1` + attr-less render, and the ONCE-evaluated `matchMedia("(pointer: coarse)")` → reka `PopoverRoot` click branch), `dock/styles/{density,overflow}.css` (`--dock-control-floor: var(--dock-touch-target, 2.75rem)` under `@media (pointer: coarse)`; every `--dock-control-size` clamped by it), `DockControl.vue.d.ts` + `dockContext.d.ts` docblocks verbatim, `components/tooltip/` (4 d.ts incl. TooltipProvider); reka `HoverCardTrigger.js:30-49` (`handleLeave` closes only when NOT open; `onFocus→onOpen`, `onBlur→onClose`) + `HoverCardRoot.js:30-70` (`useVModel(props,'open',emit,{passive: props.open===void 0})`, `handleClose → open.value=false`); `@vueuse/core/dist/index.js:7671-7708` (non-passive branch `set(value){ triggerEmit(value) }` — NO equality check); tailwindcss dist (`a("rotate"` — the utility emits the standalone `rotate` property, the D-10 promotion decider); registry cross-reads: Dock.md (DELTA-5, DELTA-7 C-35, DELTA-13 C-25/C-26/C-27/D3-09/L-36), shell-dock-actionbarlayer.md (AB-25, AB-30, AB-31, AB-32, K-4, MISS-1/2/6), shell-dock-actionbartoggle.md (ABT-8, ABT-11), ColorSpaceSelector.md (L-4, L-5(a), L-14 — three standing identities this corpus re-files), ActionFeedback.md (X-map precedent); wave authority `docs/tranches/X/waves/W4.md` (X.W4.d mechanism + file bounds + gate D4 verbatim) and `W5.md` (gate A3 = L-2's measurement, RED of record).
**Law compliance**: motion-quarantine FALSE rows not carried (M-03/M-29/M-32 cited at the kill); dated evidence untouched; this file is the seat's only write; dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

## Scoreboard

**32 adjudicated rows: 2 BLOCKER · 10 MAJOR · 10 MINOR · 10 INFO. 11 kills sustained (one of them this seat's own, against a re-reader's miss). 13 corpus-missed findings adopted (2 of them already booked elsewhere — cross-cited, not re-counted). 11 UNPROVEN-NEEDS-LIVE residues → visual-audit inputs.**

The gestalt both re-readers converged on holds and is hereby adjudicated: **ActionButton is a hand-rolled parallel dock control** — seat, hold, hover, press, selection, focus, disabled and tooltip all re-implemented one layer below their producer owners, and most of them wrong. Nearly every terminal disposition below lands on a gate that is **already RED in X-W4.d / X-W5.a's own ledgers**; what this corpus adds that the program did not have: the both-directions hold corruption proven from source, the coarse-pointer contract swap, the click-recoil composition, and one registry correction — **X.W4.d's file bounds do not name `ActionButton.vue`, yet its D1 single-render-path collapse cannot land without rewriting or deleting it.** That bounds gap is this record's actionable carry to the wave file.

## Ruled disagreements (the adjudicator's actual work)

| dispute | r-DU | r-LC | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| C-1/L-1 fold severity | MAJOR | BLOCKER | **BLOCKER** | One leaked token permanently disables auto-collapse AND outside-click dismissal (dock.js early-returns on `keepOpenCount>0`, three paths), and the unmatched release eats the shell's own U-F48 hold (Dock.vue:87 fires on predicate EDGES only — it can never re-acquire); both directions now STATIC: the emit-less prop watch (leak) + `handleLeave`/`onBlur` → `handleClose` → `open.value=false` → non-passive `set(value){triggerEmit(value)}` unconditional (over-release). Masked only by `:always-expanded="!isDesktop"` — desktop-biting by construction. |
| C-10 ("zero tests"; mutation row 5) | headline CONFIRMED, row 5 killed | headline AND row 5 killed | **r-LC WINS — both killed; r-DU's confirmation cell demoted** | o10d:350 clicks `getByRole("button", {name: "Toggle action bar"})`; :356 `getByRole("button", { name: "Copy color" }).hover()` — a role+accessible-name locator resolving THROUGH `:aria-label="title"`, then :358 asserts the card became visible. That is behaviour: the name, the layer-open path, the hover-open contract. Mutating the aria-label breaks the locator. Surviving form: coverage is THIN (the keep-open and flash mutations do pass green), not zero. |
| D-2 cure provenance | narrative KILLED ("wrong cure; ≥3 registers, this is a 4th") | narrative CONFIRMED ("ratified cure sits unadopted") | **SPLIT — r-DU wins the cure, r-LC keeps one clause** | focus-ring.css read whole: :4-7 scopes today's adopters ("the gradient stop handles + remove chip today") and its recipe is the NEUTRAL dual-contrast hairline pair (:33-36) — NOT the register on the adjacent DockControl (producer `.focus-ring` + `--focus-ring-color: var(--accent-live)` retint, ABT-11's adjudicated ground). Prescribing this file's recipe to a dock seat is the wrong cure; the re-seat inherits the producer register. BUT the law clause ":4 ONE token recipe … **never a per-control literal**" survives and still indicts ActionButton.vue:15 verbatim. Register census stands: the house has ≥3 focus registers and this seat authored a 4th (an accidental one). |
| D-10 rotate limb | PLAUSIBLE, needs live check | PROMOTED to CONFIRMED from source | **r-LC's promotion RATIFIED** | My own grep of tailwindcss dist: the `rotate`/`-rotate` functional utilities emit `a("rotate", …)` — the standalone CSS `rotate` property; ActionButton.vue:25's `transition-[transform,stroke]` does not list it, so ActionToolbar.vue:9's `hover:-rotate-180` jump-cuts while the sibling `scale(1.2)` sweeps. The live check is now a formality (U-8). |
| L-11 (value.js exports↔tsconfig) | omitted | CONFIRMED, raised to MAJOR | **RAISE REFUSED — FOLD on identity** | Fact-identical to ColorSpaceSelector.md L-14, already RESCOPED program-wide → the W-HYGIENE exports↔tsconfig config-parity identity with a born-RED generator command. Anti-rename: ONE identity, never re-booked under a second name or a second severity. This corpus's genuinely new datum — the live byte divergence (`dist/subpaths/css.js` 43973 B vs installed 43972 B, `cmp` differs at char 141) — is APPENDED to that identity as corroboration. The subject imports nothing from value.js (both readers agree; my grep concurs). |
| r-LC missed #2 (cross-route hoverKey collision) | — | filed as MAJOR miss with an unmatched-release limb | **RESCOPED — half killed BY THIS SEAT** | Premise verified (Dock.vue:157 un-keyed `GenericActionBar`; :49 resets only when `hasAnyActionBar` falls; all three action sets share key `copy` at usePaneRouter.ts:198/:209/:222; keyed `v-for` reuses the child). But keyed reuse preserves the INSTANCE — the seat that took the token at hover time is the same seat that eventually releases it, so the pairing holds and no unmatched release occurs on this path (the leak direction is ABN-1's unmount arm, not new). What survives: a hover card that stays OPEN across a route swap with no pointer on it, re-labeled with the next route's copy at a stale anchor. MINOR, not MAJOR. |

## Adjudicated defect roster

Format: id · severity · claim · proof · **terminal disposition**. Line anchors are HEAD `8d7e17fe`; re-resolve before curing (D-19). Wave authority: tranche X ratified, `docs/tranches/X/waves/W0..W11`. Rows marked FOLD keep their original ids for life inside the owning identity.

- **ABN-1 · BLOCKER — the hand-rolled dock keep-open is unbalanced in BOTH directions and corrupts a shared refcount it does not own** (C-1 ≡ L-1 ≡ D-16, ONE identity — D-16 was the same defect filed two grades low with "Reproduction: NONE"). ActionButton.vue:55/:57/:81-88: no latch, no dispose, zero lifecycle hooks in 137 lines. Producer ships the whole protocol (`keepDockOpen` on Popover.vue.d.ts:16; dockContext.d.ts docblock names `<Popover trigger="hover" keep-dock-open>` verbatim; the bundle carries latch + `onScopeDispose`). LEAK path (static): glass's `open`-prop watch syncs WITHOUT emitting, so `handleClick` → `activeHover=null` → prop-close never reaches :86. OVER-RELEASE path (static, this seat's chain): glass always passes a defined `open` → reka `useVModel` non-passive → `set(value){triggerEmit(value)}` UNCONDITIONAL → a brush-and-leave before the 300 ms openDelay (`handleLeave` fires `onClose` exactly when the card is NOT open) or a bare blur emits `update:open(false)` → :86 releases a token never taken → eats Dock.vue:87's edge-driven hold → the dock idle-collapses UNDER ITS OWN OPEN ACTION BAR (r-LC's measured trace: keepOpen ×2 / release ×4, first call a bare release; tallies = U-1). Counter clamps at 0 — silent corruption, never an error. **→ X-W4.d** (the re-seat composes the producer prop; interim minimal cure = C-1's — keep `:open`/`@update:open`, add `keep-dock-open`; L-1's cure-as-written is KILLED, see K-9). **Registry correction: X.W4.d must add `ActionButton.vue` to its file bounds** (today absent — its D1 collapse cannot land without it). Cross-book: Dock-adj DELTA-13 C-25 (the edge-only watch) and C-27 (the :95 timer) are already BUILT there — not re-booked here.
- **ABN-2 · BLOCKER — all nine generic dock actions are structurally dead on the mobile layout while the button plays its 400 ms success animation** (L-2). App.vue:83-91 mobile `<PaneSlot>` binds NO `:on-mount`; only :105/:131 (desktop) do; :315 says so in prose; :317-319 refs written nowhere else; usePaneRouter.ts:196-222 = nine `paneRefs.X.value?.verb?.()` hops over `Ref<any>` (:107-111); GenericActionBar mounts on mobile ungated (Dock.vue:157, :183); ActionButton.vue:94 flashes unconditionally BEFORE :98's emit — the control confirms an action that provably did not run. Portrait-tablet aggravator (App's compound aspect query vs Dock.vue:71's width-only query) re-verified. **→ FOLD — this is the program's own born-RED**: X.W4.d gate **D4** ("RED by construction, source-measured" — the exact same mechanism, same anchors) + X-W5.a gate **A3** (`{mobileChanged:false, desktopChanged:true}`, RED of record) own the silence and the parity cure respectively; the predicate divergence is Dock-adj **DELTA-5** (BUILD there, collision frame committed). This corpus adds the flash-lie limb — carried into X.W4.d's "unavailable is surfaced, never silence" clause — and a fourth/fifth independent confirmation.
- **ABN-3 · MAJOR — on a coarse pointer the hover contract silently becomes a CLICK contract** (r-LC missed #1, ADOPTED after this seat's own bundle read). `popover-BQGYXZyO.js`: `matchMedia("(pointer: coarse)").matches` evaluated ONCE at setup into a plain `let`; when it matches, `trigger="hover"` renders reka's `PopoverRoot`/`PopoverTrigger` — CLICK semantics — so one tap on a seat both fires the action (`handleClick`) and toggles the description card, acquiring a keep-open token whose prop-driven close emits nothing (ABN-1's leak arm, fully live because coarse+≥1024 disables the always-expanded mask). The probe is non-reactive: a hybrid laptop is locked to its mount-time input mode. All three axes assumed HoverCard universally. **→ X-W4.d** cure clause (the successor seat states its coarse contract explicitly); the once-evaluated coarse probe is a producer trait — **one line on the standing glass-ui BH relay** (owner fond). Live tap behaviour = U-9.
- **ABN-4 · MAJOR — selection is an inline stroke only: no `aria-pressed`, a stroke-width delta of exactly zero, and an ink bit-identical to hover ink** (D-1 + D-3 + L-4, ONE fold; the corpus's BLOCKER label on D-1 demoted per r-DU's own fusion note). ActionButton.vue:30 writes `--hover-color` and ActionToolbar.vue:46 writes the selected `stroke` from the SAME `cssColorOpaque`; lucide ships `stroke-width: 2` so `strokeWidth:'2'` changes nothing; no state attribute anywhere in the file. Producer contract on the shelf: DockControl `active` stamps `aria-pressed` + `data-active`, "never a saturated brand hue" (d.ts read). VC:83-84 verbatim. **→ X-W4.d** — AB-32's rider governs verbatim (the SceneActionSet must carry an active/selected member; DockControl `active` then renders it for free) **+ X-W10** (two-channels-one-axis ink canon, AB-31/MISS-2 kin). Exact serialized pair = U-2.
- **ABN-5 · MAJOR — the focus ring is a token-fallback accident and a fourth house focus register** (D-2; corpus BLOCKER demoted by both readers, ratified). `focus-visible:ring-ring/40` (:15) reaches `--color-ring`, defined NOWHERE in demo/ or the producer CSS (the only grep hit is focus-ring.css:11's prose ABOUT its emptiness) — the utility never generates and the ring falls to opaque `currentColor`; no forced-colors posture anywhere in demo/shell (`grep forced-colors` → 0) under an authored `outline-none`. Provenance narrative killed-in-part (see ruled table + K-5); the ":4 never a per-control literal" clause survives as the indictment. **→ X-W4.d** (the re-seat inherits the producer focus register; the literal deletes) **+ X-W10** (focus canon — ABT-11's identity; shares the rung/floor cure). Forced-colors arm = U-11.
- **ABN-6 · MAJOR — a hardcoded 32 px box beside 40 px DockControls, opted out of the ≥44 px coarse floor** (D-5 + L-3 — **FOLD → AB-30**, the same fact already adjudicated MAJOR at the ActionBarLayer seat). Producer clamp re-verified in bytes this session: `@media (pointer: coarse) { --dock-control-floor: var(--dock-touch-target, 2.75rem) }` and every `--dock-control-size` is `max(…, var(--dock-control-floor))` — a plain `<button>` with `width: 2rem` (:109-110) reads none of it. Adjacency literal (Dock.vue:154-157). D-5's smallTapTargets corroboration limb killed (K-7); 32×32 clears WCAG 2.5.8's 24 px — coherence + coarse-floor defect, not a WCAG failure. **→ X-W4.d (AB-30's clause: the collapse must seat actions on DockControl or carry the floor) + X-W10** (canon half, with AB-25/MISS-2). Identity preserved; not re-booked.
- **ABN-7 · MAJOR — a Popover impersonates a Tooltip: `description` reaches no assistive technology, no touch user, and no disabled seat** (D-6 + C-4 fold; the keyboard limb KILLED — K-6 — reka's `onFocus→onOpen` opens on focus, as C-4 itself measured). The sole copy of title+description lives in PopoverContent (:35-44); the trigger carries `aria-label` alone — no `aria-describedby` (reka hover surfaces are deliberately unannounced). glass ships Tooltip/TooltipTrigger/TooltipContent/TooltipProvider (dist dir read); demo/ui/tooltip already barrels all four — the cure is one import away. **→ X-W4.d** (the successor seat adopts the producer primitive and wires the description into `aria-describedby` or the tooltip body) **+ X-W10** rider (PR-16 "no tooltip proliferation" canon).
- **ABN-8 · MAJOR — the click flash cannot restart, a stale timer truncates it, and the two clocks can drift** (C-2 + D-9 + L-10 fold). `isClicked.value = true` over `true` is not a reactive change — the class never leaves, the animation never restarts (Vue fact, no probe needed); the first press's unowned `setTimeout` clears the flag mid-flight (measured 228 ms of 400 = corroboration); the `400` triplicates the CSS `0.4s` (:122/:125/:126). The CLEANUP limb (no clearTimeout/dispose) is **already BUILT at Dock-adj DELTA-13 (C-27 names `ActionButton:95`)** — cross-booked, not double-counted. The restart/truncation/dual-clock limbs: **→ X-W4.d** (press feedback re-authored on the producer's interruptible press register; edict 6 — the keyframes move, they do not die).
- **ABN-9 · MAJOR — the success flash is the only truth of Copy, fires before the result is known, and is neutralised under reduced motion** (D-8). ColorPicker.vue:323-326 verbatim: `void writeClipboard(…)` — no await, no catch, no status; :94 flashes before :98 emits; `ActionFeedback` has exactly one consumer (PaletteCard) so nothing else reports the copy. The PRM limb is carried ONLY as a feedback-truth defect on VC:101 grounds ("a transient flourish … never carries the only truth"), corroborated by quarantine M-32's own probe (`animation-duration: 1e-05s`, pulse+spin neutralised, the 100% frame = rest) — NOT as a missing local PRM arm, the pattern M-03/M-29 rule FALSE. **→ X-W4.d rider** (ColorPicker.vue is in its file bounds; D4's no-silence law extends naturally from unregistered-action to failed-action — a rejected clipboard write may not render identically to success). If the wave declines the extension, this row is a named formation-boundary residue.
- **ABN-10 · MAJOR — the disabled arm sets the native attribute, so the only explanation of WHY is unreachable, the seat leaves the tab order, and `cursor: pointer` keeps promising** (D-4 limbs 1/2/4 + C-9; limb 3 KILLED — K-4). :17-18 set BOTH `aria-disabled` and `disabled`; a natively disabled button is non-focusable and dispatches no pointer activation, so neither hover NOR focus can open the card on exactly the two seats disabled on `isEditing` (ActionToolbar.vue:44/:57); :111 `cursor: pointer` has no disabled arm. DockControl.vue.d.ts names the four-state contract verbatim. **Cure rider (the cure-is-adjudicated-separately law)**: any `aria-disabled`-without-native cure MUST carry `if (disabled) return` in `handleClick` — `handleClick` has no guard today and D-4's cure-as-written would make disabled seats firable (r-LC's prescription rider, adopted). **→ X-W4.d.** Disabled-hover behaviour = U-6.
- **ABN-11 · MAJOR — four live implementations of "exactly one of a set is open", the protocol defined in the LEAF with every parent re-implementing its half** (L-6 + D-6's mutex half). `hoverKey`/`activeHover`/`update:activeHover` (:59-88) re-bound on all five ActionToolbar seats and in GenericActionBar; `usePopupMutex` (same directory, an honest "local fork" of a retired producer composable — its BH relay is Dock-adj C-26's, already booked); `useHoverPopover`; and the producer's own cluster transit, which dockContext.d.ts names as the owner. **→ X-W4.d** (the protocol deletes with the re-seat; `keep-dock-open` + reka cadence own it; if a hard mutex is genuinely wanted it is `usePopupMutex`, once).
- **ABN-12 · MAJOR — the demo's structural import guard matches zero files; `demo/shell/` has no boundary enforcement at all** (L-7 — **FOLD → the ColorSpaceSelector-adj L-5(a) lint-glob-liveness identity**, already carrying a born-RED `eslint --print-config` command; X-W8 owns the re-point per the standing X-map, and X.W4.d's own scope adds `no-explicit-any` over the action-path files). Re-verified this session: `demo/@` ENOENT; four dead `demo/@/{components,lib}` globs + the two-glob composables object; the sole ban pattern keys on a W43-retired `@components` alias. Recorded here as the PERMISSIVE CONDITION for ABN-1 (a leaf reaching producer DI) and ABN-2 (the shell reaching pane verbs). Identity preserved; not re-booked.
- **ABN-13 · MINOR — `class="pointer-events-auto"` on `<Popover>` never reaches the DOM** (C-3; the corpus's MAJOR demoted by both readers, ratified — inert today, prospective hazard only). `inheritAttrs:!1`, render binds no `$attrs` (bundle re-read; PopoverContent's copy at :35 does land). **→ X-W4.d** (delete :9 with the re-seat).
- **ABN-14 · MINOR — the rotate composition discards the authored pulse curve, the flash lands BELOW the hover scale, and the booking that exempts the literals cites drifted anchors** (C-8 + r-DU missed #1 + r-DU/r-LC missed DESIGN.md drift, one motion fold). (a) `action-rotate` lists two `forwards` animations both driving `transform`; per CSS Animations §3 `action-spin` wins and the pulse's 1.3→1.15→1 curve is dead weight (C-8). (b) NEW, statically certain (this seat re-derived): the pulse's 100% frame is `transform: scale(1)` held by `forwards` for 400 ms, but the pointer is still over the control, whose hover base is `scale(1.2)` — animation origin outranks the normal declaration, so every press goes 1.2 → 1.3 → **1.0 (below rest-hover)** → hold → snap back to 1.2. The celebration reads as a recoil. (c) demo/DESIGN.md's bespoke-keep row cites `ActionButton.vue:117,120,121`; the live literals are :122/:125/:126 — the exemption has silently stopped covering what it exempts. **→ X-W4.d** (one animation, one curve, moved with the seat — edict 6) **+ X-W10** rider (the DESIGN.md anchor correction; DOC-1 kin).
- **ABN-15 · MINOR — the hover affordance covers 24×24 of the 32×32 hit box** (C-7). `.action-icon:hover` targets the glyph, not the wrapper — a 4 px clickable dead ring. Compounds ABN-6. Kin: AB-31's three-hover-grammars row (X-W10). **→ X-W4.d** (dies at the re-seat).
- **ABN-16 · MINOR — six dead public members across the leaf, its parent, and the contract one file away** (D-13 + C-5 + L-8 + r-DU missed #4 + r-DU missed #7). `label` (+`.action-label` with zero CSS rules anywhere), `hidden` (never passed; drives the DOM-absent `v-if` the producer contract forbids; ADDITIONALLY shadows the HTML global attribute — a consumer writing `hidden` destroys the subtree, possibly with a token outstanding — the reason the name may never return), `canProposeName` (declared ActionToolbar:72, referenced nowhere in that SFC), `clearHover`+`defineExpose` (zero callers), and `DockAction.iconClass` (usePaneRouter.ts:44 — declared, forwarded by GenericActionBar:26, set by NONE of the nine actions; missed by all three axes' dead-surface censuses). The parent's dead template refs (`actionToolbarRef`/`colorInputRef`) are **already booked at Dock-adj C-35** — cross-cited, not re-counted. **→ X-W4.d** (the contract collapse deletes them; gate D1's member surface is the hook).
- **ABN-17 · MINOR — two custom properties carrying one value, stamped inline per instance, after a four-hop drill of an injectable** (D-12 + L-9). :30's identical expression twice; `SAFE_ACCENT_KEY` inject → prop → prop → leaf when Dock.vue:35 injects it directly; the `?? 'currentColor'` doubly redundant on the flash channel (:129-130 already carries the fallback). **→ X-W4.d** (the leaf loses the colour prop; one token at the row root) **+ X-W10** (ABT-8's one-call `useSafeAccentFn("floating")` cure names the route).
- **ABN-18 · MINOR — untokened interaction timings, and the Reset seat's distinguishing hover rotation snaps instead of sweeping** (D-10, delay + rotate limbs; rotate limb PROMOTED — see ruled table; the R1/`duration-normal` limb is a re-filing of DESIGN.md:250's producer-gated EXPECTED-RED row, not double-counted; the PRM limb KILLED — K-3). `:open-delay="300"` / `:close-delay="0"` bare and unbooked. **→ X-W10** (motion token canon) — the values themselves die or get tokened at the X-W4.d re-seat. Served GenericActionBar transition duration = U-7.
- **ABN-19 · MINOR — no gap token in the action row; the gutter is a by-product of `justify-around` at content width, beside a 1.25× larger sibling** (D-15). Both consumers render the identical bare `flex items-center justify-around flex-1` div. Mechanism static; the 0 px / 6 px per-route numbers = U-3. Kin: MISS-1's `gap-0` row (X-W10). **→ X-W4.d** (the re-seat inherits `--dock-layer-gap`) **+ X-W10** (proportion canon).
- **ABN-20 · MINOR — one row, two hover tempos** (r-DU missed #2, adopted; verified). ActionToolbar:9 gives the Reset seat alone `duration-normal`; its four siblings ride the bare-utility default (itself DESIGN.md:250's producer-gated row). Five identical-role controls, one answering the pointer at a different speed. **→ X-W10** (one hover grammar per run — AB-31's exact clause); dies at X-W4.d.
- **ABN-21 · MINOR — the action row has no group semantics** (r-DU missed #3, adopted; verified). No `role="toolbar"`, no group name, no roving focus on either container; 3-5 sequential tab stops with nothing announcing the group; the repo knows the pattern (ConsoleRail's managed-tabindex rail). **→ X-W4.d** (the successor row states its group semantics — a cure clause on the same surface as ABN-7).
- **ABN-22 · MINOR — a keyed-reuse hover card survives a route swap: open under no pointer, re-labeled with the next route's copy at a stale anchor** (r-LC missed #2, RESCOPED — the unmatched-release limb KILLED by this seat, K-10). Premise verified (un-keyed GenericActionBar; shared `copy` key across all three scenes; `actionBarLayerActive` survives the swap). **→ X-W4.d** (key the bar per scene, or reset hover state on scene swap — one line either way; the gate is a spec assertion that no popover is open after a route change with no pointer over the dock). Reachability window = U-10.
- **ABN-23 · INFO — behavioural coverage is thin, not zero** (C-10 as corrected by the ruled table). The o10d locator + hover-open assert IS behaviour; the keep-open and flash mutations still pass green; `picker-blob-config.test.ts` regex-reads source as a string. X.W4.d's D1-D4 + `scene-action-contract.spec.ts` are the arriving coverage — no separate booking.
- **ABN-24 · INFO — the tracked visual matrix contains zero pixels of this component** (D-0 + C-11; three axes concur, REPORT.json re-read by r-LC). All 60 captures show the dock's summary face. Harness carry: **capture.mjs opens the Tools layer on the workbench routes** — this row plus the U-residues below are this record's inputs to the visual audit. NO-WAVE-OWNER (harness, not product source).
- **ABN-25 · INFO — partial reactive-props destructure, 2 of 12** (C-12 + D-17; D's "eleven-member"/"six, from eleven" counts corrected to 12/6-from-12). Dies at the X-W4.d rewrite; no separate booking.
- **ABN-26 · INFO — the tooltip title takes the palette-identity type role, and the exact line is oracle-locked** (D-14; r-DU's INFO calibration adopted over r-LC's MINOR). :41 `font-display … text-subheading` vs VC:72/:75; but o10d:359-363 asserts DISPLAY_FACE + weight ≤ 500 on this very node — a unilateral cure turns a standing oracle red. **→ X-W10** as a matrix-vs-oracle OWNER ruling; no local change.
- **ABN-27 · INFO — the inline `stroke` outranks the hover rule; the spread order makes the API asymmetric** (C-6, cascade limb only — consequence KILLED, K-2). Record-only; `activeStyle` dies whole at ABN-4's cure.
- **ABN-28 · INFO — the same verb ships two names and two affordances by route; one seat is a bare noun among verbs** (r-DU missed #5, adopted; verified: "Reset color"+rotation on the picker vs "Reset" plain at usePaneRouter:208; "Palettes" nounal). `title` feeds both `aria-label` and the card heading, so the split is simultaneously visual and accessible-name voice. **→ X-W4.d** (SceneActionSet titles get one home) **+ X-W10** voice canon (o10d is the standing oracle).
- **ABN-29 · INFO — the W5-a11y comment certifies a capability the code does not deliver** (r-DU missed #6, adopted). :12 promises "focus-visible ring"; ABN-5 proves that clause false; the comment is the first thing a repairer reads. Corrected whenever the file is next touched; dies with the re-seat.
- **ABN-30 · INFO — four stray blank attribute lines in ActionToolbar** (r-DU missed #8; verified at 21/32/43/56 — the first instance has none, so the file is internally inconsistent too). Dies at X-W4.d.
- **ABN-31 · INFO — `close-delay="0"` on the sole carrier of the description prose** (r-DU missed #10, adopted). Help text vanishes the instant the pointer leaves a 32 px target; mitigated by reka's grace area. **→ X-W4.d** (cadence decision at the tooltip adoption, with ABN-7/ABN-18).
- **ABN-32 · INFO/FOLD — value.js typecheck-vs-runtime artifact divergence** (L-11 — **FOLD → ColorSpaceSelector-adj L-14 / W-HYGIENE config parity**, raise refused per the ruled table). New corroborating datum appended to that identity: the two css.js builds are live byte-divergent. The subject component is clean on this axis.

## Killed-claims register

Sustained kills, with refutations; K-10 is this seat's own.

- **K-1 — C-10's "zero tests" headline + mutation row 5** ("change `:aria-label` to a literal → nothing fails"). o10d:356 is `getByRole("button", { name: "Copy color" }).hover()` — resolved through the aria-label BEFORE the text assert; the nominated mutation is the one the suite catches, and the hover-open assert is behaviour. r-DU's partial confirmation demoted with it.
- **K-2 — C-6's consequence limb** ("the Palettes button loses its hover stroke exactly when most used"). The clobbered declaration would paint the identical colour — both channels are the same `cssColorOpaque` expression (D-1's own central finding turned against C-6); `scale(1.2)` is unaffected. Mechanism survives as ABN-27 INFO.
- **K-3 — D-10's PRM limb** ("the 1.2× jump still happens as a jump-cut … §6 violation"). The citation refutes the finding: VC:144 specifies resolving DIRECTLY to final geometry — the instant jump IS compliance; and the quarantine measured glass's a11y-overrides REMOVING `transform` from the transition set on this very element (M-32). Scope-shopping pattern per M-03/M-29 — may not be carried.
- **K-4 — D-4's limb 3** (2.97:1 disabled composite vs the 3:1 floor). Category error: WCAG 1.4.11/1.4.3 exempt inactive components; the repo's own `GRAPHICS_CONTRAST_FLOOR` uses are on ACTIVE ink (ExtractControls' live track, with its BORN-RED record). The alpha-only-state half survives inside ABN-10's fold.
- **K-5 — D-2's cure-provenance narrative** ("this exact defect was already adjudicated and cured at U-F25; this file did not adopt the cure"). Wrong cure: focus-ring.css's recipe is the neutral dual-contrast pair scoped to the stop handles; the register adjacent to this seat is the PRODUCER's retinted ring (ABT-11's adjudicated ground). Survivors: the undefined-token defect itself, and the ":4 never a per-control literal" clause. r-LC's confirmation of the narrative corrected.
- **K-6 — D-6's keyboard limb** ("the description is unreachable by keyboard, since focus does not open the popover"). reka binds `onFocus: … onOpen()` on the trigger (HoverCardTrigger.js:48, read this session); C-4 in the same corpus measured `dataState: "open"` after `.focus()`. Asserted against a sibling's measurement without measuring.
- **K-7 — D-5's corroboration limb** (REPORT.md `smallTapTargets: 8` as support). Those rows are SlugEditLayer buttons and channel spans, captured with the action bar closed; both sibling axes enumerate them. The 32-vs-40 measurement needs no borrowed prop.
- **K-8 — challenge-D's honesty-section bullet 1** ("the W5-a11y comment's stated goal is met"). Self-contradicted seven lines away by D-2: the focus-ring clause of that comment is precisely what does not ship. The element choice survives — in superlatives, not as an endorsement of the comment.
- **K-9 — L-1's cure-as-written** (`<Popover trigger="hover" keep-dock-open :open-delay :close-delay>` with `:open`/`@update:open` deleted). Dropping the controlled open silently deletes the single-open mutex in the same edit — a behaviour change L-1 does not claim. C-1's shape (keep the controlled open, add the prop) is the sound minimal cure; the producer watches `resolvedOpen = props.open ?? internal`, so a controlled popover still latches and disposes. Prescription defect, not a defect-claim refutation.
- **K-10 — r-LC missed #2's unmatched-release limb** (this seat's own kill). Keyed `v-for` reuse preserves the component INSTANCE across the route swap, so the seat that took the token on the original hover is the same seat that eventually releases it — the pairing holds. The genuinely unpaired paths are ABN-1's (unmount-while-open; prop-close), already carried. The stale-open-card limb survives as ABN-22.
- **K-11 — L-11's severity raise to MAJOR** (identity refusal, not a fact refutation — every count r-LC reproduced is accepted byte-exact). One identity, one booking, one severity home: ColorSpaceSelector-adj L-14 / W-HYGIENE config parity. Anti-rename law.

## Superlatives — with provenance

1. **ActionButton.vue:12-23 — the host element** (both re-readers, concurred by this seat's read): a real `<button type="button">` under `<PopoverTrigger as-child>`, accessible name via `:aria-label="title"`, `aria-hidden` on the glyph. Keyboard reach, tab order, Enter/Space all free. Every cure in this record preserves it; it is the one thing the hand-roll got structurally right.
2. **ActionButton.vue:37-41 — the T.W4-6 provenance comment** (r-DU): names the tranche, the finding ids, the mechanism, and the reason — and its claim is LOCKED by a live oracle (o10d:359-363). The standard :12's false clause (ABN-29) falls short of, stated in the same file.
3. **Dock.vue:79-88 — the U-F48 hold predicate** (both): three imperative watchers collapsed to one declarative disjunction with the equivalence argument written inline. Its presence one file away is what makes ABN-1 legible as a defect rather than taste — ActionButton is the fourth watcher U-F48 never converted.
4. **DockControl.vue.d.ts:12-31 + dockContext.d.ts — producer contract prose that carries its own enforcement** (r-DU; bytes re-read by this seat): "hit box ≠ paint box", "≥44px on coarse via the density clamp", "never a saturated brand hue", and the sanctioned `keep-dock-open` composition — three independent axes each derived a finding from these docblocks alone.
5. **ActionBarLayer.vue:88-94 — `subLayerProps` pairing `inert` with the leaving class** (r-LC): layer activeness owned by the PARENT — the exact inversion the leaf-owned mutex (ABN-11) gets wrong two files away.
6. **usePopupMutex.ts:80 `onUnmounted(clearSwapTimer)` + its candid fork header** (r-LC): the same directory, the same class of one-shot timer, disposed correctly and honest about provenance — the in-repo proof that ABN-8's missing teardown is an omission, not a house convention.
7. **App.vue:305-312 — the one-compound-query discipline, with the irony recorded** (r-LC): the comment says the JS mount condition and CSS grid share one query "so they can never disagree" — and Dock.vue:71's different query breaks parity from the other side (DELTA-5's ground).
8. **The challenge corpus's epistemic discipline** (both re-readers; ratified): explicit negative-results sections in all three files, hypotheses labelled as hypotheses, "Reproduction: NONE" where true, and challenge-L's arithmetic reproduced to the byte — which is exactly why eleven claims could be killed cleanly from source instead of re-measured.

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

Mechanisms above are CONFIRMED static; these numbers/behaviours are live-only. With ABN-24's harness carry (capture.mjs opens the Tools layer), these are the component's visual-audit worklist.

- **U-1** — the counter traces: C-1's `keepOpenCount` 0→1→2 with all popovers closed; L-1's keepOpen ×2 / release ×4 log and the 8 s collapse under an open bar. The BLOCKER does not rest on them.
- **U-2** — D-1's serialized pair (`oklab(0.454082 …)` ≡ `oklch(0.454991 0.15 30)`); identity is a source fact, the triples are a capture's.
- **U-3** — D-15's per-route geometry (0 px gutters on `/#/gradient`; 38 px pitch / 6 px gaps on `/#/`).
- **U-4** — D-11's contrast table (4.90:1 light / 1.26:1 dark) — seed-variable by construction (ABT-8's residue kin); the rung mechanism is adjudicated there.
- **U-5** — D-4's 2.97:1 disabled composite (arithmetic re-derived sound; the ground is a sample; moot for the floor claim per K-4).
- **U-6** — whether `:hover` matches a natively-disabled seat's glyph in served browsers (would compound ABN-10's cursor lie with a full hover response on a dead control).
- **U-7** — the served 0.2 s GenericActionBar icon transition (dist `:root` clobber vs plain default — DESIGN.md R1's producer gate decides).
- **U-8** — the rotate-snap formality: sample `getComputedStyle(svg).rotate` at 50/250 ms on the Reset seat (source-confirmed; one line closes it forever).
- **U-9** — ABN-3's real-device tap behaviour on a coarse ≥1024 px surface (the leak arm and the double-fire race).
- **U-10** — ABN-22's reachability window (pointer parked on a seat across a keyboard-driven route change).
- **U-11** — D-2's forced-colors arm (Chromium UA outline rescue; Safari has no forced-colors at all — separate cells).

## Closing verdict

**DEFECTIVE — 2 BLOCKER · 10 MAJOR · 10 MINOR · 10 INFO, adjudicated (not verified).** The component is a hand-rolled parallel dock control: it drives a shared refcount it does not own with an acquire/release pair proven unbalanced in both directions from source, confirms actions that are structurally dead on an entire layout, and re-implements — smaller and wrong — the seat, floor, hold, mutex, tooltip, focus, selection and disabled contracts its producer ships one import away. Almost nothing here needs a new wave: the roster lands on X-W4.d (whose D1/D4 gates are already RED on these exact mechanisms), X-W5.a (A3, already RED of record), X-W8 (two standing fold identities) and X-W10 (four canon riders), with three cross-books into Dock-adj and two into ActionBarLayer/ActionBarToggle-adj identities. What this adjudication adds beyond the corpus: the static both-directions proof of the hold corruption, the coarse-pointer contract swap, the recoil composition, one kill of a re-reader's own limb, one severity-raise refusal on identity grounds — and the registry correction that **X.W4.d's file bounds must name `ActionButton.vue`** before its collapse can be real. Original challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
