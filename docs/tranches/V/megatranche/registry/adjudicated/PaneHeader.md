**Served model: `claude-fable-5` (Fable 5)** — observed in this seat's own system prompt, DECLARED not inherited (M-23/L-18 receipt law).

# PaneHeader — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator over two hostile re-readers)

**Seat**: fresh Fable adjudicator — the incredulity seat, not a transcriber.
**Date**: 2026-08-06 · **HEAD at adjudication**: `8eca29f0` · branch `tranche-u`. Challenge corpus authored at `c654824e`→`fe8785e5`; `demo/shared/ui/PaneHeader.vue` byte-identical across the interval (re-read whole, 224 lines, this session).
**Corpus (r2 governs)**: `audit/components/PaneHeader/` — `challenge-C-implementation.md` (r2, Opus 5 [1m]), `challenge-D-design.md` (r2 with §0.1 self-correction), `challenge-L-library.md` (pass 2, Opus 5 [1m]) + the two preserved pass-1 records + 29 probes + 7 committed shots.
**Re-readers adjudicated**: reader-1 (design-and-usage) · reader-2 (library-and-code). Both returns were attacked before either was adopted; every verdict-level disagreement was re-derived against the tree with this seat's own instruments.

**This seat's own instruments (all static + node; zero browser probes — probe parsimony)**: full read of PaneHeader.vue (224 lines); `grep -n no-restricted-imports eslint.config.js` + full read of the :200-290 region (the K-3 decider); `demo/ui/card/index.ts` + producer `dist/components/card/index.d.ts` (the L2-5 decider); `tsconfig.base.json:11` + `tsconfig.demo.json:42-49` + `package.json#exports` (7 keys, no `.`) + `ls dist/subpaths` (no parsing, no units, no index.d.ts); `EmptyState.vue:73-90` (all six optional props widened `| undefined`); `ConfigSliderPane.vue:45-125` (Card `overflow-hidden` at :99, inner `.pane-scroll-fade` host at :106, ternary at :107 — the K-9 decider); ConfigSliderPane consumer census (exactly 2: `AuroraPane.vue:115`, `BlobPane.vue:128`, both string literals); node re-derivation of the ENTIRE C-1 pole series from the shipped tokens (`--type-heading: 1.618rem`, `--type-display-1: clamp(1.618rem, 1.2rem + 1.6vw, 2.618rem)`, both read from installed `scale.css`): 1440-cap → 0.310808 · 1024 → 10.868 · 1280 → −7.3909 · phone-floor → 1.619775, each matching the corpus to 5-6 figures; repo greps — `scroll-padding|scroll-margin` demo-wide → **0**, `px-4 sm:px-6` → **10 files/10 hits**, `text-pane-title` demo → **0**, `<h1` → **0** / `<h2` → **6** / `<h3` → **4**, `ScrollCardHeader` in producer → **0**, `card-scroll-host` in demo → **0**; producer reads — `card-scroll.css` whole (the shrink grammar incl. the `prefers-reduced-motion: reduce` arm AND the `opacity: 0` rest `::before`), `CardHeader.vue.d.ts` (`shrink?: boolean`), `semantic.css` (`text-display` strict superset; `text-heading` rides `var(--type-weight-heading)`, NOT a hardcoded 700; `text-caption` italic), `utilities.css` (`.text-pane-title` = display-2 base, display-3 at ≥40rem container), `a11y-fallback.css` (`--glass-level: 0`); `demo/styles/foundation.css:733` (`--glass-level: 0.1`) and `:767` (`0`); `demo/styles/animations.css:184-193` (duration/iteration/transition/scroll-behavior only); `useHeaderCondense.ts:1-40` (the false docblock, whole); `demo/DESIGN.md:375/:388`; `demo/shared/utils.ts:10-22`; `AdminPane.vue:1-10` + `AboutPane.vue:12-28` + `ColorSpaceSelector.vue:42-53` (the slot/accname/Badge chain); producer badge chunk root = `"div"`; `App.vue:399-408` (ghost pane); `o11-header-gates.spec.ts` gates 3+5 + `playwright.config.ts` project census (smoke = chromium 1280×720; smoke-safari testDir = `./e2e/smoke/safari`, disjoint); `motion-quarantine.md` MQ-1 (HIGH, new) / M-33 (FALSE — the "bottom fade") / M-34 (TRUE); `CARRY-CUT-LEDGER.md` CC-099/CC-101 rows; X-wave ownership reads — `X/waves/W10.md` §X.W10.d + G-4/G-5 whole, `W8.md:42-43/:76-79/:163`, `W5.md:164/:249` (A5), `W1.md` (CC-031); the VISUAL-CONSTITUTION §4 matrix / §4.1 origins table / §6 final-geometry clause / §6.1 direction law, all read at source.

**Motion-law compliance**: the only quarantine rows cited are **MQ-1** (the sweep's own new HIGH defect) and **M-34** (ruled TRUE). **M-33** is cited solely AS a FALSE ruling — evidence that the `.pane-scroll-fade` name already misled one seat — never carried as a defect.

## Scoreboard

**36 rows CONFIRMED (2 BLOCKER · 12 MAJOR · 12 MINOR · 9 INFO, +1 identity-fold) · 9 kills sustained in the register (all re-derived, none spot-trusted; 2 carry NEW charges against a reader's own evidence cell) · 13 missed-by-the-axes findings adjudicated (12 confirmed new, 1 already-booked identity) · 12 UNPROVEN-NEEDS-LIVE residue items → visual audit.**

Blocker-weight survivors: **PH-1** (the WebKit `tan(atan2())` shrink "ratio" is a pole-crossing function of viewport width — re-derived by this seat from pure arithmetic, matching three independently authored probe families to six figures) and **PH-2** (third parallel header-condense fork; the installed producer ships the complete seam with zero consumers, and both demo forks' justifying docblocks are false against it).

**The single most consequential thing this adjudication found is not in either reader's return**: `X/waves/W10.md` **G-4 pins "0.311 WebKit" as the POST-CURE per-engine value** ("Post-cure values pin per engine (0.311 WebKit / 0.618 Chromium)"). 0.310808 IS the PH-1 defect — the deg-consumed-as-radians pole arithmetic at the 1440 cap, not an honest engine difference. Executed as written, X-W10.d would certify the BLOCKER as expected behavior. The G-4 pin must become **0.618 on BOTH engines** (the token ratio 25.888/41.888) once the ratio cure lands. No CARRY-CUT-LEDGER row exists for PH-1 at all (`grep atan2|shrink-ratio` over the ledger and all X waves → 0) — a ledger gap flagged for the formation boundary.

## Ruled disagreements between the re-readers (the seat's actual work)

| Dispute | Ruling | Deciding evidence (this seat's own) |
|---|---|---|
| **eslint boundary-object count** — reader-1's K-3 says THREE `no-restricted-imports` objects (one fully dead); reader-2's L2-1 confirm cell says "three of the four … the fourth also covers `demo/color-picker/**`", presented as re-run | **Reader-1 SUSTAINED.** My grep: rule declarations at `eslint.config.js:206` (src, live inv-K-1), `:241` (color-picker ×2 live globs + 4 dead `demo/@` globs), `:279` (composables, fully dead) — `:230`/`:267` are comment lines. Three objects; exactly ONE matches zero files; six dead globs total — matching the ColorSpaceSelector arbiter's independent census AND X-W8:42's "six dead `demo/@/**` file globs". **New charge**: reader-2's cell repeats L2-1's original four-object arithmetic under a "re-run by me" label — a citation-inheritance event (the ColorSpaceSelector DISSENT-1 species), recorded. The FINDING (PH-10) survives both readers intact. |
| **L2-5 barrel arithmetic** — reader-1 "six of seven"; reader-2 "6 of 12" | **Reader-2 SUSTAINED.** My reads: producer `card/index.d.ts` exports 7 components + 5 types (`CardProps`,`CardTier`,`CardSize`,`CardVariant`,`CardMetal`); `demo/ui/card/index.ts` re-exports 6 value names, 0 types. Under `verbatimModuleSyntax` the type drop is the sharper half. Reader-1's cell inherited the challenge's undercount. |
| **L2-3 framing** — reader-1 confirms a "false contract" lump incl. consumer drift; reader-2 kills the drift/decay narrative | **Reader-2 SUSTAINED; the comment-falsity limb survives under C-10.** My read: `ConfigSliderPane.vue:99` is `<Card … overflow-hidden>` — it CANNOT host the scroll timeline; the class MUST sit on the inner `overflow-y-auto` div at `:106` or the timeline never resolves. Nothing drifted; the comment at `PaneHeader.vue:43-45` was never true of that consumer. The code is correct at all nine sites; the prose is the defect. |
| **The PRM cure shape** — both readers kill the bare `no-preference` wrapper (K-4); CC-099/X-W10.d prescribes exactly that wrapper | **Kill SUSTAINED and PROMOTED to a binding rider on X-W10.d.** My read of `challenge-D-design.md:495-519` (r2): D-7's PRIMARY prescription is the D-16 transposition — discrete-state **transitions**, which `animations.css:184`'s guard DOES reach and the producer belt-and-braces (`card-scroll.css`'s own `reduce` arm, read by this seat) — so MT-F023 "ceases to exist". The wrapper is D-7's INTERIM only, and then only **plus a static `--pane-veil-rest: 1` under `reduce`** (VISUAL-CONSTITUTION §6: "final geometry", not rest — read at source). CC-099's wrapper-alone, and MQ-1's `animation-timeline: none` equivalent, both land the reduce cohort at the veil's WEAKEST state while content scrolls beneath — D-7 measured the regression on both engines. X-W10.d may not execute CC-099 as written. |
| **tsconfig `paths` drift** — reader-2 files it as a NEW missed finding | **REAL but NOT NEW — identity-fold.** Fact-identical to the ColorSpaceSelector adjudication's **L-14** (3 dead targets incl. the phantom root `./dist/index.d.ts`, 2 omissions, the "CLOSED 8-key set" comment false by one), already RESCOPED → W-HYGIENE config parity with a born-RED generator-parity command. My own `ls` re-confirms every fact at HEAD. Anti-rename law: ONE identity, never re-booked under a second name. Reader-2's registry-awareness miss is recorded. |
| **`.text-pane-title` = "exactly this job"** — reader-1's missed-#2 limb | **DEMOTED on this seat's own read.** `.text-pane-title` (producer `utilities.css`, `@layer components`) rests on `--type-display-2` and steps to `--type-display-3` at ≥40rem container — a LARGER rung pair than PaneHeader's `--type-display-1`. It is a pane-title-role class at different rungs, not a drop-in. The byte-faithful swap is the **`text-display` utility** (my read: a strict superset of the five hand-written declarations at `:106-112`, including the non-bold `var(--type-weight-display)` the comment claims it had to hand-roll). The finding (PH-14) survives at full severity; the "exactly this job" cell does not. |

## Adjudicated defect roster

Line anchors are HEAD `8eca29f0` (file byte-identical since T.W4); re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, never VERIFIED** — nothing here stamps a wave.

**BLOCKER**

- **PH-1 (C:C-1 / D:D-1,D-2 / L:L-1) — `--pane-title-shrink-ratio: calc(tan(atan2(var(--type-heading), var(--type-display-1))))` at `PaneHeader.vue:140-142` is not a ratio on the WebKit cell; it is a pole-crossing function of viewport width.** Re-derived by this seat from pure arithmetic against the installed tokens: deg-magnitude-consumed-as-radians reproduces 1440→0.310808, 1024→10.868, 1280→−7.3909, phone→1.619775 — digit-identical to three independently authored probe families and to the two committed WebKit shots (title painted as a full-pane slab at 1024; title and slotted selector gone from the band at 1280). Chromium computes the designed 0.618029. The `@supports` gate at `:177` cannot detect a wrong ANSWER, only a missing feature — the file's own defence at `:138-139` is the design error. **Disposition: BUILD → X-W10.d** (owns the lines) with TWO mandatory riders: (i) the cure is the direct type-ratio `calc(var(--type-heading) / var(--type-display-1))` (`<length>/<length>` → `<number>`, CSS Values 4; two independent probes measured it correct in both engines; degradation is graceful — invalid-at-computed-value-time → `transform: none`); (ii) **G-4's per-engine pin "0.311 WebKit" is struck and replaced by 0.618 both-engines** — as specified it enshrines this defect. **Ledger gap**: no CC row exists; flagged for the formation boundary.
- **PH-2 (D:D-16 / L:L-2) — third parallel header-condense choreography in one app; the installed producer ships the complete seam (`CardHeader shrink?: boolean` + `card-scroll.css` + `.card-scroll-host`) with ZERO demo consumers (my grep), and the premise justifying both demo forks is FALSE against glass-ui 7.0.0.** `card-scroll.css` (read whole by this seat) transitions `padding-block-start`, steps `font-size` display-2→display-1, and `display: none`s the description — three LAYOUT channels — refuting `useHeaderCondense.ts:9-15`'s "compositor-only BY ARCHITECTURAL COMMITMENT … structurally cannot satisfy §0.8/BR-9" verbatim; `<ScrollCardHeader>` (cited by both forks) returns 0 hits in the package. PaneHeader (`:205-223`) animates `transform`/`opacity` only. **Disposition: BUILD → X-W10.d**, with three riders this seat adds: (i) **the transposition is NOT verbatim adoption** — the producer's `::before` rests at `opacity: 0` (zero-at-rest), which the component's own T.W3-4 ruling explicitly SUPERSEDED (`:2-10`: the constitutive veil, rest floor 0.52, Q9 bracket); any adoption must carry the ratified rest floor or it silently reverts an owner-ruled design; (ii) CC-101's "glass DECLINE-ON-RECORD / consumer-side ratified" cell predates the shipped 7.0.0 layout lane and must be re-derived at wave open (the same stale-producer species as ColorSpaceSelector addendum item 3); (iii) the false docblocks (`useHeaderCondense.ts:9-15`, `PaneHeader.vue:157`) are corrected by any wave touching the files — **named file-list gap**: `useHeaderCondense.ts` is in NO X-wave file list; degrades to NO-WAVE-OWNER at execution if unadopted.

**MAJOR**

- **PH-3 (C:C-3) — the three animation ranges (`0px 64px`/`0px 120px`/`0px 80px`, `:181/:187/:192`) are absolute pixel constants with no relation to pane travel; `fill: both` parks short panes at fractional poses no input can resolve** (corpus: Gradient caption at 0.2375 forever at 1440×900). Structural from the declarations; every reported terminal value re-derives exactly from range arithmetic (reader-1, six of six). The AB-2 contrast certification at `:118-124` is silently void at every fractional park. **Disposition: BUILD → X-W10.d** — dissolves under PH-2's discrete transposition; if any scrub survives, ranges must derive from travel. Rider: G-5 measures at `scrollTop ≥ 120` — on a short pane that state is unreachable; the gate must guard travel or it inherits C-8(ii)'s viewport luck.
- **PH-4 (C:C-8 i–v) — the certifying oracle is vacuous**: gate 5 compares REST states only (never calls the `scrubTo` sitting in the same file — my read of `:278-300`), so it returns the same verdict for the PRM defect and its cure; gate 3's `at64.opacity === 1` (`:154`) has no travel guard and the sole running project pins 1280×720 (my read of `playwright.config.ts:142-159`); nothing anywhere asserts the shrink ratio (grep 0 over `e2e/ test/`); the oracles subtree is structurally unreachable from any WebKit project (`smoke-safari` testDir = disjoint `./e2e/smoke/safari`); unit coverage 0. **Disposition: split** — the no-orphaned-cell arm → **X-W1** (CC-031 oracle-slate); the motion-gate reform rides **X-W10.d**'s G-4/G-5 (with PH-1's pin correction); `o11-header-gates.spec.ts` itself is in NO X file list — **partial NO-WAVE-OWNER**, flagged.
- **PH-5 (C:C-2 / MT-F023 / MQ-1, M-34 TRUE) — the scroll choreography runs at FULL amplitude under `prefers-reduced-motion: reduce`; the global guard is categorically incapable of reaching it** (progress comes from the timeline, not the clock — `animations.css:184-193` overrides durations only, my read; MQ-1 measured `animation-duration: 1e-05s` with identical transforms both states). The file's own `:148-151` "needs no PRM gate" is the design error; the producer contradicts it in code (its own `reduce` arm). **Disposition: BUILD → X-W10.d (CC-099) under the K-4 binding rider** — the D-16 transposition as primary (MT-F023 ceases to exist), or interim wrapper **plus `--pane-veil-rest: 1` under `reduce`**; never the bare wrapper CC-099 currently names.
- **PH-6 (C:C-4 / D:D-4) — the collapse reclaims ZERO scrollport; band, padding (`pt-4 pb-2`, constant) and description box are unchanged rest→stuck, and the `opacity: 0` description keeps its box, hit target and accessibility-tree entry** (no `display`/`visibility`/`aria-hidden`/`inert` arm anywhere — my read). This is exactly X-W10 **G-5**'s RED baseline (OM-18, the owner's own mark). **Disposition: BUILD → X-W10.d (CC-101)**; C-4's `grid-template-rows` scrub cure is KILLED (K-6), the discrete toggle is the shape.
- **PH-7 (C:C-5 / D:D-11 / L:L-4 + L2-3-as-rescoped) — a leaf publishes an unscoped global class onto nine sibling pane roots; its `contain` rationale (`:50-53`) is false by spec (named-timeline lookup is ancestor-chain + `timeline-scope`; `contain` plays no role); `contain: layout` makes every pane a `position: fixed` trap; and the mechanism dictates route-level DOM lifetime** (`App.vue:399-408` ghost panes retained "to preserve scroll-timeline state" — my read). First declaration byte-identical to the producer's `.card-scroll-host`. **Disposition: X-W10.d rider** for the comment corrections + whatever containment survives the PH-2 transposition; the class-architecture arm (typed host contract) **NO-WAVE-OWNER**.
- **PH-8 (C:C-6 / D:D-6-as-corrected / L:L-5) — the pane title is a hardcoded `<h3>` (`:21`), the app has ZERO `<h1>` (my census: h1×0, h2×6, h3×4), on About the `<h3>` is outranked by its own five `<h2>` children (heading-order, WCAG 1.3.1), and the producer's `CardTitle as` seam ships unconsumed.** The constitution's seven focus origins targeting "destination H1" are structurally unimplementable today. **Disposition: split** — the shell H1 → **X-W5 (A5)**, whose gate is RED on exactly my census; the PaneHeader heading-level seam (`as`/`level` prop or CardTitle adoption) is in no wave's mechanism — **NO-WAVE-OWNER**, flagged with X-W5 as the named candidate.
- **PH-9 (C:C-7) — `transform: scale()` on the `<h3>` scales every slotted descendant; About slots a real `<button>` (`ColorSpaceSelector inline`, aria-label "Select color space" — my read of the chain) which drops below WCAG 2.5.8's 24×24 when scrubbed** (and leaves the viewport entirely at the 1280 pole — visible in the committed shot). **Disposition: X-W10.d rider** (the font-size-step transposition changes but does not remove the geometry question for slotted content); the structural cure — an adornment SLOT beside the heading, not inside it — rides PH-26/PH-27's arm, **NO-WAVE-OWNER**.
- **PH-10 (L:L2-1, as rescoped by K-3) — the demo's import-direction lattice is enforced by nothing**: of three `no-restricted-imports` objects, one guards `src/` (live, load-bearing), one reaches 17 of 251 demo files and bans a specifier with zero occurrences since W43/RF-15, one matches zero files; six dead `demo/@` globs. **Disposition: FOLD → X-W8** (`:42-43`: delete the six dead globs, add the boundary ban that keeps the barrels dead).
- **PH-11 (D:D-3 / C:C-9, as rescoped by K-2) — at DEFAULT media the veil cannot occlude: `--glass-bg-resting` resolves alpha 0.65 light / 0.72 dark, so ~35% of underlying luminance transmits at terminal opacity 1, and gate 3 measures pseudo-element opacity, not composited coverage.** Producer-inherited (the producer's own shrink veil rides the same token). Universal quantifier KILLED (K-2): under `prefers-reduced-transparency` `--glass-level: 0` (BOTH producer `a11y-fallback.css` AND `foundation.css:767` — my greps) the fill is 100% card, fully opaque; under `prefers-contrast: more` (`:733`, 0.1) 96.5%. The lever D-3's cure asks the producer for already ships as `--glass-level`. **Disposition: X-W10.d** (the stuck-material decision belongs to the condense register); legibility limb → residue #4.
- **PH-12 (D:D-5 / L:L-3) — `transform-origin: left top` (`:184`) is a physical keyword pair with no `:dir()` arm; under RTL the shrinking title detaches from its inline-start edge** (the two axes' independent measurements reconcile to the tenth — reader-1's arithmetic, adopted). Constitution §6.1 requires logical direction for chrome (read at source). **Disposition: X-W10.d rider** — neither sibling has a transform; dissolves under PH-2's transposition, else `:dir()` pair.
- **PH-13 (reader-1 missed #1, CONFIRMED by this seat) — no `scroll-padding-block-start` on any `.pane-scroll-fade` host and no `scroll-margin` anywhere in the demo (my grep: 0 rows), so the 69–137px sticky band occludes anything the browser scrolls into view — including keyboard-focused controls. WCAG 2.4.11 Focus Not Obscured (Minimum), AA.** The producer's `.card-scroll-host` has the same gap, so PH-2's transposition does NOT dissolve this. Cure is one declaration on the host rule. **Disposition: BUILD → X-W10.d rider** (owns the host rule's file); no X gate covers it — gate gap flagged.
- **PH-14 (reader-1 missed #2, CONFIRMED with this seat's rung correction) — `.pane-header-title` (`:106-112`) hand-restates the producer's `text-display` utility property-for-property on a FALSE premise: the comment at `:101-104` claims `text-heading` "hardcodes 700", but the installed producer rides `font-weight: var(--type-weight-heading)` (my read) — the fifth false rationale comment, and the one that licensed the local rule.** Same stale-producer species as the ColorSpaceSelector text-title cell (addendum item 3 there; citation-inheritance is detectable). `text-display` is the byte-faithful swap (strict superset incl. the non-bold weight token); `.text-pane-title` is NOT (different rungs — see Ruled disagreements). Zero rendered-pixel delta; the defect is structural. **Disposition: X-W10.d** (one class edit + comment correction under E-3).

**MINOR**

- **PH-15 (reader-1 missed #3, CONFIRMED) — `useHeaderCondense.ts:9-15`'s docblock — the entire justification for fork B, quoted as authority by two axes — is FALSE against the installed producer** (the three layout channels; the phantom `<ScrollCardHeader>`; it even cites the owner-retired `proof:*` idiom). The load-bearing false claim of the whole triplication, in live shipping source, licensing fork C. **Disposition: rider on PH-2**; file-list gap named there.
- **PH-16 (reader-1 missed #4, CONFIRMED) — `.pane-scroll-fade` is a misnomer: two properties, no fade of any kind; the quarantine's own M-33 (FALSE) records that the NAME already produced one wrong audit finding downstream.** Hand-typed into 9 consumer files with no type or warning; the producer's equivalent is honestly named. **Disposition: NO-WAVE-OWNER** (rename ×10 files; candidate X-W10.d).
- **PH-17 (reader-1 missed #5, CONFIRMED) — the header/body alignment contract is the raw literal pair `px-4 sm:px-6` hand-mirrored across 10 files (my grep: 10/10)** where the producer demonstrates container-scaling and ships `--card-pad-inline`. **Disposition: NO-WAVE-OWNER** (spacing tokenization; candidate X-W10 under its canon).
- **PH-19 (D:D-9) — the description wears `text-caption`, an eighth role outside §4's closed seven-row matrix, and the producer utility renders it ITALIC (my read); no §4 row specifies italic.** **Disposition: BUILD → X-W10.a** (the canon re-author must either legislate the caption row or the class dies).
- **PH-20 (C:C-10) — four load-bearing rationale comments measurably false** (`:43-45` root-contract; `:50-53` contain-isolates-timeline; `:136-138` "degenerates to exactly 1" — refuted by the pole arithmetic, phone tan(45 rad)=1.6198; `:148-151` "needs no PRM gate" — refuted by MQ-1) **plus PH-14's fifth**. **Disposition: X-W10.d rider** (E-3: any wave touching the file corrects all five).
- **PH-21 (D:D-8 / L:L-6) — three desynchronized un-tokenized ranges on one block; raw `-0.25rem`; the `14px` feather literal ×3 code sites (`:85/:91/:95`) + prose, where the producer tokenizes the same quantity as `--card-pad-title-gap` (consumed in its own inset AND mask stop — my read).** Range anchors corrected to `:181/:187/:192` (reader-1's off-by-one catch on D-8 adopted). **Disposition: X-W10.d (CC-101's register)**.
- **PH-22 (L:L2-2 + reader-2 missed #7 folded) — under `exactOptionalPropertyTypes` (base:11, hard demo gate) the public `description?: string` (`:35-37`) cannot be passed optionally with a `string | undefined` source; the SOLE consumer with such a source carries the repo's only `v-bind`-ternary workaround, while the sibling `EmptyState.vue` widens all six props `| undefined` (house convention, my read).** Two cures, both adjudicated sound: widen the prop (house convention), AND/OR reader-2's strictly smaller one — `ConfigSliderPane`'s own `description?` has exactly 2 consumers, BOTH passing string literals (my census), so making IT required collapses the ternary with zero other edits. **Disposition: X-W10.d rider** (one-line prop widening travels with any file touch).
- **PH-23 (D:D-13) — no measure, no truncation, no overflow disposition on the description; the sticky band grows with any caller string** (constitution bounds prose at 66ch). **Disposition: X-W10.d rider** (the condense register must state the caption's measure).
- **PH-24 (L:L-8) — two dangling `ScrollCardHeader` references in shipping source (`PaneHeader.vue:157`, `useHeaderCondense.ts:11`); the real surface is `CardHeader shrink?: boolean`.** **Disposition: rider on PH-2's comment corrections.**
- **PH-25 (reader-2 missed #5, CONFIRMED) — `demo/DESIGN.md:375` certifies this component "reduced-motion-correct" (falsified by MQ-1) via two DEAD paths (`panes/PaneHeader.vue`, `style.css` — my reads confirm both gone), and `:388` offers `.pane-scroll-fade` as an exemplar of the no-global-utility-class rule it violates** (the block is UNSCOPED; `foundation.css` describes the same move correctly). **Disposition: BUILD → X-W10.a/.c** — the full DESIGN.md re-author; G-3 (CANON-ANCHORS-LIVE) is RED on these very anchors.
- **PH-26 (reader-2 missed #2, CONFIRMED) — `AdminPane.vue:5` slots a `<Badge>` into the `<h3>`; the producer Badge root is a `<div>` (my read of the chunk) inside phrasing-only content — an HTML content-model violation that parses without auto-closing, which is why nothing surfaced it.** **Disposition: rides the adornment-slot arm (PH-9) — NO-WAVE-OWNER, candidate = the wave that lands PH-8's seam.**
- **PH-27 (reader-2 missed #3, CONFIRMED) — the slot-into-`<h3>` design lets a descendant `aria-label` rewrite the heading's accessible name: About's computed name is "About the color spaces, Select color space" (accname descendant traversal), not the visible text — and C-6's own report cell printed `textContent` as the accessible name.** Structural; the adornment-slot cure fixes it for a reason C-6 never stated. **Disposition: with PH-26.**

**INFO**

- **PH-18 (reader-1 missed #6) — no programmatic heading↔description association (`hgroup`/`aria-describedby`); AT hears an orphan paragraph — including at `opacity: 0`, where it announces a caption sighted users cannot see (compounds PH-6).** **Disposition: with PH-2's transposition** (the producer's `data-slot` pairing is the discarded seam).
- **PH-28 (reader-2 missed #4) — all three declarations depend on undocumented ORDER: the `animation` shorthand resets `animation-timeline`/`animation-range-*` (CSS Animations 2), so any reorder silently kills the choreography with a rest state indistinguishable from correct — the failure mode C-8 proves no gate can see.** Verified in source (`:179-192`, shorthand-then-longhands). **Disposition: dissolves under PH-2/PH-5; else X-W10.d rider** (producer idiom: `animation: <name> auto linear` with explicit duration).
- **PH-29 (reader-2 missed #6) — the caption animation binds `.pane-header-desc-wrap > p` (`:189`) while the `<p>` carries a component-authored class (`:29`); the wrap node's only other job is a 2px margin — a structural-selector coupling whose failure is silent.** **Disposition: X-W10.d rider.**
- **PH-30 (reader-2 missed #7, the census half) — recorded under PH-22's fold.**
- **PH-31 (L:L2-5, arithmetic per K-8) — `demo/ui/card/index.ts` re-exports 6 of the producer's 12 card exports (0 of 5 types); the barrel tier is the concrete mechanism by which `CardHeader shrink` and `CardTitle as` went unnoticed.** **Disposition: FOLD → X-W8** (19 barrels deleted, 92 imports re-pointed).
- **PH-32 (L:L2-6) — `demo/shared/utils.ts:17` justifies the local `debounce` on "the library's root-barrel export stands for external consumers" — false (no `.` export, no root d.ts — my reads).** **Disposition: FOLD → X-W8** (`:163` deletes utils.ts and re-homes debounce; the false comment dies by deletion).
- **PH-33 (C:C-13 / L:L-7) — `@reference` at `:61` is a dead edge (zero `@apply`/`theme()` in 164 style lines); systemic (17 demo SFCs carry it, 15 dead).** **Disposition: NO-WAVE-OWNER** (systemic hygiene; not charged to this component).
- **PH-34 (D:D-15) — empty-title/loading/error/focused states never designed; `<slot />` with no fallback inside a display-rung `<h3>` reserves a full line box and yields empty-heading.** **Disposition: NO-WAVE-OWNER**; the empty-title guard may ride any X-W10.d touch.
- **PH-35 (L:L-9/L-10) — the 19-directory alias tier and the three-file `demo/shared/` residue colliding with it.** **Disposition: FOLD → X-W8.**
- **PH-36 (reader-2 missed #1) — tsconfig `paths` drift: ALREADY BOOKED, identity = ColorSpaceSelector L-14 → W-HYGIENE config parity.** Not re-booked (anti-rename). Recorded here so the next formation does not re-litigate.

## Killed-claims register (all re-derived by this seat)

1. **K-1 — C-12 whole + D-12's contrivance limb: KILLED (both readers, SUSTAINED).** "The ternary guards nothing … collapses to `:description="description"`". Under EOPT (base:11, my read) that collapse is a TYPE ERROR for the one consumer whose source is `string | undefined`; the ternary is the standard EOPT workaround FORCED by PaneHeader's own narrow prop. The consumer census proves the direction (8 definite-value consumers bind directly; the sole optional-source consumer carries the sole ternary; EmptyState's widening is why `PaletteCardGrid.vue` forwards `string | undefined` with no ceremony). C-12's `v-if` cure does not touch the caller-side error. L2-2 is the correct diagnosis; C-12/D-12(b) invert causality and prescribe a CI regression. (C-12's separate empty-string/`" "` observation survives inside PH-34.)
2. **K-2 — D-3's universal quantifier ("at every state, forever … occludes nothing"): KILLED (reader-1, SUSTAINED).** Under `prefers-reduced-transparency: reduce` `--glass-level: 0` (producer a11y-fallback AND `foundation.css:767` — my greps) → the mix resolves 100% card → at terminal veil the header occludes COMPLETELY; `prefers-contrast: more` → 96.5%. The default-media defect stands (PH-11); the quantifier and the "needs a new producer token" cure do not — the lever ships and is wired.
3. **K-3 — L2-1's enumeration ("four objects, three dead"): KILLED (reader-1, SUSTAINED + NEW charge).** Three objects (my grep: `:206/:241/:279`), exactly one fully dead, six dead globs. The finding survives; the arithmetic presented as its proof was wrong — fatal irony for a thesis about unchecked enumerations decaying. NEW: reader-2's confirm cell repeated the wrong count under a "re-run by me" label — citation-inheritance, recorded in Challenges.
4. **K-4 — the bare `no-preference` wrapper as "the only available cure" (C-2, L pass-2, and MQ-1's `animation-timeline: none` shape): KILLED (both readers, SUSTAINED + PROMOTED).** The wrapper alone parks the reduce cohort at the veil's weakest state while content scrolls beneath — strictly worse than the shipped defect on every leak metric, both engines (D-7's own simulation, the only seat that tested its cure). Constitution §6 requires FINAL geometry (read at source). Binding rider on X-W10.d/CC-099: transposition primary; wrapper+`--pane-veil-rest: 1` interim only.
5. **K-5 — D-6's "h2: 0, measured 30/30": KILLED as a false negative (reader-1, SUSTAINED).** The probe's route matrix excluded `/#/`, where all six `<h2>`s live (my census). The correction produced the sharper About-inversion finding (PH-8).
6. **K-6 — C-4's `grid-template-rows` scrub cure: KILLED (reader-2, SUSTAINED).** A layout property scrubbed per frame is the F3 fork the file retired (`:153-157`) and requires disarming O-11 gate 4 — the one gate in that file measuring the component. The discrete toggle achieves the collapse at one layout per crossing. (Reader-2's git-archaeology limb — the wrap div predating F3 — is reader-2's own measurement, adopted unrelitigated; the kill stands without it.)
7. **K-7 — L pass-2's "forbidden by construction, proved twice": KILLED as stated (reader-2, SUSTAINED).** The runtime alias set is generated; the TYPE side is hand-maintained and drifted in both directions, and its bare-root key restates the phantom barrel L2-6 disproves. Folds to the L-14 identity (PH-36).
8. **K-8 — L2-5's "six of seven" arithmetic: CORRECTED (reader-2, SUSTAINED).** 6 of 12 (7 components + 5 types — my read). The narrowing is twice what the challenge charged.
9. **K-9 — L2-3's "contract drifted on 18% of sites" decay narrative: KILLED (reader-2, SUSTAINED).** The code is correct at all nine sites — `ConfigSliderPane`'s Card is `overflow-hidden` and CANNOT host the timeline (my read); the inner placement is structurally forced. The defect is a never-true comment (PH-20), not consumer decay.

## Challenges to the re-readers (recorded, not ruled away)

1. **Citation-inheritance event (reader-2)**: the eslint four-object arithmetic repeated under a "re-run by me" label while reader-1's independent grep and the ColorSpaceSelector arbiter's census both say three. The same reader's OTHER counts (251 files, 17 color-picker, the barrel 6-of-12) were genuinely its own. Evidence about the fleet, recorded per the ColorSpaceSelector DISSENT-1 precedent.
2. **Registry-awareness miss (reader-2)**: filing the tsconfig `paths` drift as a NEW find when the adjudicated registry already books it (ColorSpaceSelector L-14). The anti-rename law exists precisely for this.
3. **Rung overstatement (reader-1)**: `.text-pane-title` as "exactly this job" — different rungs (display-2/-3 vs display-1). The correct swap is `text-display`.
4. **Both readers missed the G-4 enshrinement**: neither return reads `X/waves/W10.md`'s gate as specified, yet it is the single artifact most likely to convert PH-1 from a defect into certified behavior. Wave-spec cross-reads are part of the adjudication surface.
5. **Both readers treated producer adoption as clean (PH-2)**: neither noticed the producer's zero-at-rest `::before` contradicts the component's owner-ruled constitutive veil (T.W3-4, `:2-10`). The cure is adjudicated separately from the defect (ColorSpaceSelector addendum item 2); this is that law's next test case.

## Superlatives (provenance preserved)

- **The from-state = base-state construction across all three keyframes** (`:196-223` vs the base rules) — engine-, PRM- and timeline-invariant rest; what makes every cure free at rest. C's N-1/N-2 verified rather than assumed it; both readers elevate it; this seat concurs from source.
- **The veil rides producer tokens and inherits three accessibility escape hatches for free** (reader-1's superlative, re-verified by this seat's greps): `--glass-level: 0` under reduced-transparency → fully opaque stuck veil; 0.1 under `prefers-contrast: more`; zero local media queries. The `:68` comment is the one architectural claim in the file that fully pays out.
- **The rest floor is a closed-form design argument with a stated bracket** (`:127-129`): 0.52 × 0.65/0.72 = 0.3380/0.3744, inside the ratified [27%, 39%] — re-checked by reader-1, exact. The standard the file's motion numbers fail, met by its material number.
- **The caption ink measures the composite, not the token** (`:118-124`, the 4.29:1 rejection on the TRUE ground) — constitution §4.1 honored exactly where a shortcut would be invisible.
- **`challenge-D-design.md` §0.1** — a seat re-ran its own negative, published the wrong version verbatim, and let the correction generate the corpus's largest finding (D-16). The best epistemic hygiene in the corpus, about exactly the failure mode (an unverified negative hiding a producer seam) the corpus indicts.
- **D-7** — the only seat in the entire program (including the binding motion sweep) that SIMULATED its own prescribed cure, measured the regression it would ship, and amended. Its amendment is now a binding rider on X-W10.d.
- **`useHeaderCondense.ts:18-34`'s sufficiency gate** — reasons aloud about the condense-removes-its-own-overflow oscillation and gates on it: the failure class PaneHeader has no defence against (PH-3). The best-argued scroll logic in the demo, under the worst docblock (PH-15).
- **`EmptyState.vue:73-89`** — the sibling that got the EOPT contract right, six props widened with per-prop docblocks; the in-repo proof that PH-22's cure is house convention, and the decider for K-1.
- **The corpus's verifiability** — this seat reproduced the complete pole series, the parked-pose predictions, and the token derivations from closed-form arithmetic with no browser; three seats' independently authored probes and two readers' re-derivations agree digit-for-digit. That is what an auditable audit looks like.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

1. **Engine attribution of PH-1** — mechanism certain (arithmetic); whether shipping Safari (vs the Playwright WebKit build) exhibits it is live-only. This is also C-8(iv)'s point: no gate can currently see it.
2. **PH-3's specific parked poses** (0.9775/0.2375/0.806 at 1440×900; 0.5425 at 1512×982) and the maxScroll figures — the class is structurally certain; the numbers are probe-cell.
3. **C-8(ii)'s rescue overflow** (~211px Gradient at 1280×720) — the gate's structural defect is confirmed by reading; the luck is not.
4. **D-3's legibility limb (QUALIFIED)** — the committed WebKit frames render band content SHARP (backdrop-filter plausibly inert in that headless build), so `--glass-blur-resting` may do occlusion work the leak numbers do not credit; the Chromium arm still shows mean channel delta ~21. Severity of PH-11 rides this.
5. **C-11's scrub-cost delta** (~18% main-thread attributable to the veil; honest headless + GooBlob caveats). The structural half (opacity on a backdrop-filter element is not compositor-pure) needs no probe.
6. **D-14's forced-colors computed values** — locally unhandled is confirmed in the negative; the measured values need a live forced-colors context.
7. **D-15's zoom-200% row** — arithmetically supported (pole at 768px → −6.38, my sweep family), rendered consequence unobserved.
8. **C-4's dead-band extent** (63.8px / 7.1% viewport) and hit-test sampling — mechanism certain, extent live.
9. **L2-3's route-instance arithmetic** (2-of-11; ghost double-mounts on /#/browse and /#/generate) — source facts confirmed; per-route element counts live.
10. **The exact resolved veil alphas** (0.65/0.72) — the FORM guarantees alpha < 1 at any `--glass-level > 0`; the numbers are token-derivation, worth one live confirmation.
11. **The division cure's cross-engine safety** (`calc(len/len)` → number) — two independent probes measured it GREEN both engines; graceful degradation if rejected; one live WebKit confirmation closes it (feeds PH-1's cure gate).
12. **PH-13's occlusion geometry** — the band heights (68.97–136.6px) are corpus figures; the zero-`scroll-padding` fact is this seat's own grep.

## Closing verdict

**ADJUDICATED (never VERIFIED — no wave is stamped by this file): 36 rows CONFIRMED — 2 BLOCKERs (a shrink "ratio" that is a pole-crossing function of viewport width on the WebKit cell, painting the About title as a slab at 1024 and erasing it at 1280; and a third parallel condense fork whose producer seam ships complete and unconsumed while both demo forks stand on docblocks that are false against the installed 7.0.0) · 12 MAJOR · 12 MINOR · 9 INFO (+1 already-booked identity); 9 kills sustained, every one re-derived, two carrying new charges against a reader's own evidence cell; 13 missed-by-the-axes findings adjudicated; dispositions routed X-W10.d ×14 (with THREE binding riders: the G-4 "0.311 WebKit" post-cure pin is struck for 0.618-both-engines; CC-099 may not ship the bare no-preference wrapper — D-7's amended shape or the transposition; producer adoption must carry the owner-ruled constitutive veil floor) · X-W8 ×5 · X-W5 ×1 · X-W1 ×1 · W-HYGIENE identity ×1 · 8 NO-WAVE-OWNER rows/arms flagged for the formation boundary, with two named file-list gaps (`useHeaderCondense.ts`, `o11-header-gates.spec.ts` in no X wave) and one named ledger gap (PH-1 has no CC row); 12 residue items feed the visual audit.** The component's signature is a file whose material decisions are closed-form, token-derived and bracket-checked — and whose motion decisions are hand constants certified by gates that measure the config, wrapped in five rationale comments that are measurably false. Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
