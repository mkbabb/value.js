claude-fable-5

# fr-BasisCanvas — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-07 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/BasisCanvas.vue` (547 lines) + its drawing/composable/consumer chain.
**Substrate**: fourier HEAD `cd26c653` / tree `9a66411d` — re-pinned by this seat (`git log -1`), identical to the coordinate all three challenges pin. **Dirty-tree disclosure**: 28 dirty rows in the fourier working tree at adjudication time; **the subject file and its entire `lib/canvas-drawing` + composables subtree are CLEAN at HEAD**. Three subject-adjacent consumers are drifted (`VisualizationView.vue`, `BasisSelector.vue`, `AnimationControls.vue`) by six equal-length content-substitution lines — glass-ui API renames only (`MetricBadge :amount→:value`, `Slider glass-scrubber→standard`, `UnderlineTabs→SegmentedTabs variant="underline"`); **all line anchors in this record are stable across the drift** and none of the six lines is an adjudicated citation.
**Corpus**: 70 accusation rows across three axes — challenge-D (design, 27 rows, opus-5[1m]), challenge-L (library/code, 25 rows BC-1..BC-25), challenge-C (consumption, 18 rows) — at `docs/tranches/V/megatranche/audit/fourier-components/BasisCanvas/`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Their returns arrived through the orchestration channel; the full proof cells live in the immutable challenge files beside this record, and every load-bearing or disputed cell was **re-derived by this seat against the tree with its own instruments** before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. Every kill was re-derived (a weak kill gets un-killed); every missed find was re-proven before banking; every reader-vs-reader conflict was ruled with a fresh read. Two shared reader sub-claims died under this seat's own reads (see K-11, K-12) — the incredulity seat's actual work. One citation-inheritance event was detected and is recorded in DISSENT (the ColorSpaceSelector L-10 precedent, reproduced here at D-12).
**Arbiter's own instruments this session (all static, zero browser probes — probe parsimony; product source read-only)**: full reads of BasisCanvas.vue (547), useCanvasHover.ts (202), useWorkspaceLoader.ts (136), trail.ts (96), labels.ts (103), colors.ts (117), golden-shimmer.ts (68), epicycles.ts (297), basis-display.ts (7), transforms.ts (39), animation.ts (146), VisualizationView.vue (486), FullscreenViewer.vue (245), useCanvasSetup.ts, useViewTransform.ts; targeted reads of workspace.ts:40-110, defaults.ts, types.ts, ContourSettings.vue error region, BasisSelector.vue toggle, ExportModal.vue:15-45, AnimationControls.vue width chain, SpeedSelect.vue options, placeholder/grid/ghost-path constants, bases.ts:27-45, App.vue boot, web/tsconfig.json (whole — `strict` present, `noUnusedLocals`/`noUncheckedIndexedAccess` absent), e2e axe harness; installed glass-ui 4.0.0 dist: `--viz-*` token forms (oklch-only + `light-dark()`), `@property` registration census (progress/phase/specular/glass-level/ui-scale only — no color), full `--duration-*` and `--ease-*` token enumerations, `cartoon-surface` utility body, `package.json` peer map; **producer** glass-ui 7.0.0 `./axes` export map + `src/components/_shared/axes.ts` source (the D-26 decider); repo-scope greps: `spectrumColor` (4 implementations / 6 consumer files), `VIZ_COLORS.green|rainbow|pink|emerald` readers (0), `.trace` readers (0), `getPathBounds` callers (0), `drawEpicycleLabel` callers (0), `withEpicycles|withTrail` (ExportModal-only), `prefers-reduced-motion` file census (12 files, none of them the canvas chain), `anim.duration` writers (0), `expand-pop` Transition consumers (0), `--ease-apple-spring` consumers (5 files), `toHaveScreenshot` in e2e (0).

## Scoreboard

**64 rows ADJUDICATED-CONFIRMED (2 BLOCKER · 21 MAJOR · 30 MINOR · 11 INFO) · 12 KILLS in the killed register (incl. 2 minted by this seat against the readers themselves) · 16 missed finds banked · 11 superlatives with provenance.**

Blocker-weight survivors: **D-1/BC-1/C-1** (the entire `--viz-*` basis palette resolves to the parser fallback `#888888` in both themes — the canvas has no colour system at runtime) and **D-3** (the product's central artifact is absent from the accessibility tree and every affordance it draws is pointer-only, behind a green axe gate that cannot see it).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **D-12 falsifier** ("the chrome is host-supplied by construction") | KILLED it — the inline wrapper carries no `cartoon-card` | CONFIRMED D-12 while **repeating the corpus falsifier verbatim** ("Both consumers already wrap the component…so the chrome is host-supplied by construction") | **DU wins.** LC's cell is a citation-inheritance event (DISSENT §1) | My read of `VisualizationView.vue:198`: the wrapper carries `canvas-container` + two conditional modifiers — no `cartoon-card`, and the scoped block (:389-409) sets only position/inset/opacity/z-index/transition. The canvas's entire card identity is the child's own `:521` class; deleting it leaves the inline stage chromeless until the host is edited. The cure is a **two-file** change; a repair wave reading LC's cell would ship a one-file deletion and regress the inline stage. |
| R-2 | **D-26** (`grid.ts` vs glass-ui `./axes`) | KILLED — `./axes` is the type-axis grammar | Confirmed-as-filed at INFO ("honest caveat holds", API unread) | **KILLED — DU wins.** LC confirmed a row whose API it admits it could not read | My own reads: producer `package.json` maps `./axes → dist/axes.js`; source is `src/components/_shared/axes.ts` — "the ONE grammar home", exporting `SURFACES`/`SURFACE_TIERS`/`SIZES`/`ORIENTATIONS`/`MOTIONS`/`TONES`/`PLACEMENTS`/`TRIGGERS`/`BACKDROPS` string tuples and derived unions. **Zero rendering code.** Nothing in `./axes` can replace `grid.ts`. The genuine convergence candidates on that export map are `./fourier-field`/`./fourier-math` — the F.W1 study must not be pointed at `./axes`. |
| R-3 | **BC-13** (untyped export-options bag) | KILLED the prescribed cure ("a declared interface would have made the drop compile-visible" — no such TS diagnostic exists) | CONFIRMED BC-13 as "the wrong type and the mechanism by which BC-2 shipped" | **SPLIT — both partially right.** The *defect fact* (untyped `Record<string, boolean>` on both sides of the emit contract, `BasisCanvas.vue:462` + `ExportModal.vue:19`) is CONFIRMED MINOR. The *causal/cure claim* is KILLED (K-7) | `noUnusedLocals`/`noUnusedParameters` never report unread members of an object type; no TS flag errors on an undestructured optional property. My tsconfig read: neither flag is set anyway. Cure adjudicated separately from defect (the ColorSpaceSelector addendum-item-2 law): the catching cures are a required destructure of every member, an exhaustiveness test, or a lint rule — a named interface alone re-ships the drop class. |
| R-4 | **The error-surface seam** (D-10 kill fallout) | Missed-find: the `ContourSettings.vue:311` retry banner is structurally unreachable | Kill-cell claims `VisualizationView.vue:162` ("`store.error && !store.imageSlug`") is "itself effectively unreachable" | **DU wins; LC's sub-cell KILLED (K-12).** | The toast watcher (`useWorkspaceLoader.ts:125-133`, read whole by this seat) nulls `store.error` **only when `store.imageSlug` is truthy**. With no slug the error survives, and `:162` renders — it is the live load-failure surface. What IS dead is the post-load persistent surface: `ContourSettings` renders only under `v-if="hasImage"` (VV:260/:270 ⇒ slug present ⇒ the pre-flush watcher consumed the error before any render effect ran). DU's version banked as M-α1 (MAJOR). |
| R-5 | **Severity conflicts** (D-3: DU BLOCKER / LC MAJOR · D-2: DU BLOCKER / LC MAJOR · D-13: DU MAJOR / LC MINOR · D-12: DU MAJOR / LC MINOR · D-23: DU MINOR / LC INFO) | — | — | **D-3 BLOCKER · D-2 MAJOR · D-13 MINOR · D-12 MINOR · D-23 MINOR** | D-3: hard 2.1.1/1.1.1-class failure on the sole product surface with **no in-product alternative** (the legend toggles exist only as canvas hit regions) and a green gate structurally blind to it — blocker-weight capability absence for a user class. D-2: severe visible incoherence, but the palette-death headline is D-1's; D-2 is the second, independent staleness defect and ships WITH the D-1 cure — MAJOR. D-13: consistency drift whose live-bug arm is already BC-7's own row — MINOR, with the M-α2 mechanism attached. D-12: chrome composition + a 1px hover-lift; no capability loss — MINOR, DU's severity dissent preserved. D-23: the `anim` store leak through a component ref is the exact idiom `VisualizationView.vue:75-78` records as deliberately retired — MINOR, not INFO. |
| R-6 | **The pinned-shimmer mechanism** (shared by both readers) | D-8 cell: shimmer repaints "while a basis is hovered **or pinned** (…`onMouseLeave` never clears `pinnedBasis`)" | M-β2 cell: a pinned row "holds startShimmer alive **indefinitely**, appending 60 zero-length segments per second" | **BOTH CORRECTED (K-11) — this seat's own kill.** | My read of `useCanvasHover.ts`: `shimmerTick` (:69-73) exits when `hoveredBasis` is null — and **both** `onMouseLeave` (:125-126) and any `onMouseMove` off-label (:110-114) null `hoveredBasis` without clearing `pinnedBasis`. On a mouse-driven session the shimmer rAF stops the moment the pointer leaves the label; the *latch* survives, its *observable* does not (that desync is BC-16's row, confirmed separately). The indefinite-shimmer regime is real but narrower: the **touch path** (no trailing mouse events after the synthetic click) or a pointer parked on the label. M-β2's core finding survives on its own legs — see the register. |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim (one line) · decisive anchor · terminal disposition → route**. Full proof cells live in the challenge files and are incorporated by reference; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **D-1 / BC-1 / C-1 — BLOCKER.** Every `--viz-*` basis token collapses to `#888888` in both themes: `cssVarToHex` (`web/src/lib/colors.ts:22-53`) matches four forms (hex/hsl()/bare-triplet/rgb()) and falls through at `:53`; the installed producer authors the three basis tokens **only** in `oklch()` (`dist/styles/tokens/color-radius.css:263-265`, `dark-arm.css:113-115`) and `light-dark(oklch(),oklch())` (`light-dark.css:144-147`) — all re-read by this seat; no `@property` color registration exists (my registration census: progress/phase/specular/glass-level/ui-scale only). `resolveVizColors()` overwrites the seeded brand hexes on mount and on every root class flip (`App.vue:10-18`, read). Live blast radius **trimmed per K-10**: `.fourier` (trail :127, multi :326, connector `epicycles.ts:258`, tip glow :283, dead label `labels.ts:89`), `.chebyshev`, `.legendre`; `.golden` is static and correct; `.green/.rainbow/.pink/.emerald` have zero readers. Why CI is blind: zero `toHaveScreenshot` in e2e (my grep). **ADJUDICATED → F.W1** (the resolver/token seam is the tri-package uplift surface; the cure must survive glass-ui 7.0.0's token forms and heed value.js's own parser-proof R1 caveat — route through glass-ui's `cssToOklch` arm, per C's unproven note).
- **D-3 — BLOCKER** (R-5 ruling). `<canvas>` at `:526` is self-closing — no role, no name, no fallback children; the container `:519-525` is a bare `<div>` with mouse handlers, no `tabindex`/`role`/`@keydown`; the legend toggles exist **only** as canvas hit regions (`labels.ts:64` → `useCanvasHover.ts:103-109`/`:154-160`) so no focus target can exist; no `aria-live` mirror for `N = k`. The green gate is real and blind: my read of the e2e harness confirms AxeBuilder wcag2a/aa/21a/21aa filtered to serious/critical — axe has no rule for an unnamed canvas. **ADJUDICATED → F.W3/W4** (DOM-mirror legend + named canvas + keyboard path; D-27's MetricBadge hoist is the ready-made cure for the `N = k` third of this).

### MAJOR

- **D-2 / BC-5 / C-3 — MAJOR** (R-5). `basis-display.ts:3-7` (7 lines, read whole) is a bare object literal snapshotting `VIZ_COLORS.*` at module eval — reached statically from `BasisCanvas.vue:8`/`labels.ts:2` before `App.vue`'s `onMounted`. Frozen consumers (legend fill `labels.ts:39`, multi stroke `:257` → seed `#bf4040`) vs live consumers (trail/glow/connector → `#888888`): one basis, two colours, one frame. Independent of D-1; survives any resolver fix. **ADJUDICATED → F.W1**, ships with D-1.
- **C-4 — MAJOR.** No draw path holds a reactive dependency on `VIZ_COLORS` (my grep: 6 occurrences in the component, all inside watch/rAF callbacks — untracked reads; no watcher source mentions it, `:379/:403/:419` read). A theme flip never repaints a paused or off-screen canvas; becomes newly visible the moment D-1 is cured. **ADJUDICATED → F.W1**, same wave as D-1/D-2.
- **D-4 / BC-2 / C-2a — MAJOR** (corpus BLOCKER demoted by both readers; ratified). `ExportModal.vue:36-41` emits four keys; `BasisCanvas.vue:466-469` destructures two; repo-wide `withEpicycles|withTrail` hits are ExportModal-only (my grep). `FullscreenViewer.vue:138` calls `exportFrame()` bare — a second undeclared contract (my read). **ADJUDICATED → F.W3/W4** (export-truth work item, with BC-3/D-11/D-22/D-24/D-25/BC-12/BC-22).
- **BC-3 / C-2b — MAJOR.** `exportFrame` is a hand-forked `drawFrame` that lost the overlay layer: draw composite at `:104-107` gates `drawImageOverlay` on the prop; the export block `:484-501` never calls it (my read: `drawImageOverlay` appears at `:81`, `:106`, `:515` only). The reference photograph silently exits every PNG. **ADJUDICATED → F.W3/W4** (same item; the structural cure is one draw pipeline with an options parameter, not two forks).
- **D-11 / BC-4 / C-12(vertical) — MAJOR.** `withLabels:false` is `offCtx.clearRect(0,0,200,100)` **after** the full draw (`:498-500`) over a never-filled canvas — erases grid/ghost/curve in the corner to transparency, and still under-covers: 3 reachable rows put the `N` row top at y=90 (labels geometry re-derived by this seat: yOff 16 + 26/row, N row at `yOff-4`). The horizontal and fourth-row halves are killed (K-5). **ADJUDICATED → F.W3/W4.**
- **D-5 — MAJOR (sign only; K-4 trims the arithmetic).** The inset centre is memoised at base scale (`epicycles.ts:75-86`, pad 12) and hover applies ×1.447 about that fixed centre (`:89-92`) — left/bottom clipping is unconditional for baseScaled dims > ~54px; the paired pixel figures are over-determined (at most one of W/H binds, `bfs = min(...)` at `:78`). Hard cut is the canvas bitmap bound, not CSS overflow. **ADJUDICATED → F.W3/W4** (re-fit about a corner-anchored origin on hover, or clamp).
- **D-6 — MAJOR.** Inset pinned bottom-left at 12px (`epicycles.ts:82-83`); the transport dock rides `.controls-overlay { position:absolute; bottom:0.75rem; left/right:0.375rem; justify-content:center }` (`VisualizationView.vue:412-422`, read) hosting `AnimationControls` (default `maxWidth` 960px, expanded width `min(var(--animation-dock-max-width,960px), calc(100dvw - 1rem))` — my reads of `:25`/`:62`/`:135`). Horizontal overlap is arithmetically certain on every real stage; the dock's shrink bound is keyed to the **viewport** while it lives in the stage cell (DU's strengthening, adopted; realised layout at 1024-1280 stays UNPROVEN). **ADJUDICATED → F.W3/W4.**
- **D-7 — MAJOR.** Every neutral canvas text fails AA on `--card` in both themes — `placeholder.ts:55-56` `rgba(150,150,150,0.6)` → ~1.77:1 light / ~2.71:1 dark; `labels.ts:70-71` `rgba(150,150,150,0.7)` bold 16px (below the 18.66px large-text floor) → ~1.98:1; `grid.ts:90` 0.35 alpha worse. Both readers recomputed the ratios independently to three digits; anchors re-read by this seat. The passing token exists (`--muted-foreground`, producer-annotated 5.21:1) and the tree already fixed `--viz-amber` for exactly this class (`style.css:113-126`, read — the D.W4.d axe carry that could not reach canvas pixels). Formal 1.4.3 scope stays arguable (UNPROVEN cell); the illegibility is not. **ADJUDICATED → F.W3/W4.**
- **D-8 / BC-25 — MAJOR (K-3 kills the 5 Hz sub-claim; R-6 corrects the pinned arm).** Zero `prefers-reduced-motion` consultation anywhere in the canvas chain (my 12-file PRM census: the idiom exists in-tree — `SvgFilters.vue`, `router/index.ts`, `PaperView.vue`, even VV's own view-transition block `:310-316` — and stops at this component's door); playback auto-starts (`useWorkspaceLoader.ts:99-122`, read); the ungated oscillators run at ≈0.80 Hz / ≈0.53 Hz with a 0.70→1.00 alpha swing (my derivation from `golden-shimmer.ts:12`, `epicycles.ts:280`). **ADJUDICATED → F.W3/W4** (copy the in-tree JS idiom; cite 2.3.3/2.2.2, not 2.3.1).
- **D-9 / BC-8 — MAJOR.** In epicycle-only mode the geometry is unconditionally full-order (`:140` full chain; `trail.ts:20` no maxTerms) while `:196-197` mints and prints `N = level` — decorative; in multi mode the same string indexes real levels (`:229-241`) — one legend, two meanings. The honest `drawEpicycleLabel` (`labels.ts:81-103`) is exported and has zero callers (my grep). **ADJUDICATED → F.W3/W4** (pair with D-27's DOM hoist).
- **BC-7 — MAJOR.** `epicycleBounds` zeroing exists only on the single-basis path (`:176-185`); `drawMultiBasesFrame:358-366` has no else (my read; five occurrence sites confirmed). Stale rect feeds the hover machine → phantom scale tween + alpha jump for an inset that is not drawn. **ADJUDICATED → F.W3/W4.**
- **BC-20 — MAJOR.** `trail.ts:48`'s loop-back predicate `t < lastT - 0.01` is unreachable at every selectable speed (Δt ≤ 0.0033 at the 5000ms fastest regime — `duration` ref(20000) with **zero writers**, my grep; `SpeedSelect` offers exactly {0.25,0.5,1,2,4}, my read; the ping-pong seam `animation.ts:66-69` never jumps backward >0.01). The else-branch appends unboundedly (`:70-71`, no eviction — my read). **ADJUDICATED → F.W3/W4** (with M-β2 — same cure family: bound the trail and move the mutation out of the draw path).
- **BC-9 / C-6 / D-20 — MAJOR.** Half the persisted `AnimationSettings` contract is write-only and three-way divergent **including units**: server `fps 30 / duration 30.0 s / max_circles 80` (`api/models/shared.py:65-71`, read), client defaults `60 / 5000 ms / 100` (`defaults.ts`, read), renderer hardcodes `ref(80)` (`:47`, unwritten, unexposed) and `20000 ms` (`animation.ts:23`, unwritten). Only easing/speed/active_bases round-trip. A gallery replay cannot reproduce its own frame. **ADJUDICATED → F.W5–W8** (settings round-trip is a persistence-contract row; the CRUD/provenance union owns the three-way reconciliation).
- **BC-10 / C-9 — MAJOR (K-8 kills the tab-hide third).** The I.γ gate's own comment promises occlusion parking that `IntersectionObserver {threshold:0}` cannot report (`:425-451` + `animation.ts:34-42`, read); fullscreen teleports a second canvas over an opaque fixed backdrop while the inline one stays mounted (`FullscreenViewer.vue:105-127`/`:148-153`, VV`:282` no v-if — read) → `visibleCanvases` = 2, both draw. And two rAF drivers in the component's own composable (`useCanvasHover.ts:52-63`, `:67-75`) consult no visibility state at all (read whole — no reference). The reference **counter** is airtight (S-1); the **sensor** under-reports. **ADJUDICATED → F.W3/W4.**
- **C-5 — MAJOR · FOLD BY REFERENCE (identity guard).** The multi-basis fallback re-derives the server's `partial_sums` operation on the main thread (`:300-312`: nEval 800 × O(nTerms≤200) per frame ≈ 160k iterations, in the same tick as everything else), reachable in the ordinary bases-in-flight window. This is the **R6-8** intake-row shape (client re-implements a server operation) — ONE identity, folded to the R6-8 census row, never re-booked. **→ F.W5–W8** with that row.
- **C-7 — MAJOR.** Two `AnimationData` types share a name and a field with incompatible shapes: server `partial_sums: dict[str, Point2D]` whose docstring **names `BasisCanvas.vue`** and prescribes typed access over a shape the client cannot use (`api/models/visualization.py:55-79`, read verbatim by this seat); client `Record<string, Record<number,{x:number[];y:number[]}>>` (`types.ts:17`, read); the component's `:271-273` comment misdescribes even the client type. Entity `animation_data` is written/read by nobody (LC, adopted). **ADJUDICATED → F.W5–W8.**
- **C-8 — MAJOR · FOLD to the F.W1 charter (identity guard).** glass-ui 4.0.0 declares `"@mkbabb/value.js": "^0.10.0 || ^0.11.0"`; installed value.js is 0.13.0, hoisted, single copy (my `node -p` on the installed package.json + both readers' lockfile walks). The coupling is live (color/aurora chunks import value.js at runtime). This IS the tri-package-uplift surface the challenge headers already pin (installed 4.0.0 vs producer 7.0.0) — one identity, **→ F.W1**, not a second booking.
- **M-α1 — MAJOR (missed find, reader-DU; re-proven by this seat; see R-4).** The `ContourSettings.vue:311` retry banner — a designed error-recovery affordance with a Retry button — is structurally unreachable: the pre-flush toast watcher nulls `store.error` when `imageSlug` is set, ahead of render effects, and `ContourSettings` renders only when an image exists. Net post-load error posture = one transient toast + the canvas frozen on "Computing…" (`placeholder.ts:59`). **ADJUDICATED → F.W3/W4** (one error-posture row with the D-10 remnant).
- **M-β1 — MAJOR (missed find, reader-LC; re-proven).** `spectrumColor` exists **four times** (my grep: `canvas-drawing/transforms.ts:3-8` with `pow(t,0.6)`; `equation/lib/harmonics.ts:81-88`, `FrequencyGraph.vue:42-45`, `CoefficientsSpectrum.vue:47-50` all WITHOUT the pow — the latter two byte-identical, all re-read). Same harmonic index paints ~213° on the canvas vs ~262° in the co-visible coefficients panel (arithmetic re-derived: 0.1266^0.6 ≈ 0.2894). Six consumer files ride the four copies. **ADJUDICATED → F.W3/W4** (one ramp home; pairs with D-18's substrate question).
- **M-β2 — MAJOR (missed find, reader-LC; mechanism corrected per R-6/K-11).** `trail.update()` is called from the **draw** path (`:144`, `:329`), so every non-clock redraw mutates model state: hover-scale tween frames, the shimmer loop while it runs, ResizeObserver re-setup, overlay loads, toggle watchers, and exportFrame each append a point — with the clock paused, `t` is unchanged, the `:48` predicate is false, and `:70-71` pushes a duplicate. Corrected scope: the 60/s append regime holds while a label is hovered (or touch-pinned); it does **not** survive mouse-out (the shimmer rAF keys on `hoveredBasis`, which mouse-out nulls). The root shape — `draw()` mutating persistent state — is what makes BC-20 and D-22's trail-push inevitable. **ADJUDICATED → F.W3/W4** (with BC-20).
- **M-β3 — MAJOR (missed find, reader-LC; re-proven).** No watcher is `{immediate:true}` (my grep of the component) and `trail.precompute` is called only from watcher A (`:386`); a `BasisCanvas` that mounts with a warm store — the fullscreen instance (`v-if="show"`, mounts after data by construction — my read) and any gallery→workspace remount — never precomputes: every scrub falls to the `trail.ts:60-68` O(600·N) fallback at a different sampling resolution than the inline instance. **ADJUDICATED → F.W3/W4.**

### MINOR

- **D-12 — MINOR** (R-1/R-5; falsifier killed, defect confirmed). Baked-in `cartoon-card` chrome (`:521`) + five `:deep()` resets in `FullscreenViewer.vue:163-175` (read) + the `cartoon-surface` hover-lift (installed dist read: 2px border, offset shadow, `translate` lift) firing whenever the pointer crosses the busiest repaint surface. Two-file cure. → **F.W3/W4**; the shim's cross-repo re-publish stays a coordination ask (style.css's own comment) — glass-ui BH relay rider.
- **D-14 / BC-6 — MINOR.** The caller-set shimmer `globalAlpha` (`:168-173`) is clobbered by all six of `drawEpicycleCircles`' per-op alpha assignments (my read of `epicycles.ts:151-232` — six of six, each resetting to 1). Dead statement. Cure = pass shimmer as a parameter, never remove the internal resets (S-8). → **F.W3/W4**.
- **D-13 / BC-11 — MINOR** (R-5) + **M-α2 — MINOR (missed find, reader-DU; re-proven)**: the two draw modes drift ({4,3.5} vs {5,4.5} weights, tip-dot z-order, sentinel `undefined` vs `null`), and the mechanical explanation nobody in the corpus offered — `lineWidths` are assigned **inside** the fit transform (`epicycles.ts:151-156` then `:197`/`:209`), so inset stroke weight = `weight × fitScale`, swinging ~45% on hover, while the connector (1.5, post-restore) is unscaled. Any constant repair must decide whether to divide by `fitScale`. → **F.W3/W4** (one shared frame body).
- **D-15 — MINOR.** 16/12/0.5rem insets on the same edges, no shared step (anchors re-read). → **F.W3/W4**.
- **D-16 — MINOR.** `image-rendering: crisp-edges` (`:545`) on smooth-curve content whose backing store rounds against a fractional CSS box (`useCanvasSetup.ts:25-28`, read). → **F.W3/W4**. Visible artefacts UNPROVEN.
- **D-17 — MINOR.** Canvas requests CM-Serif/Fira-Code with zero `document.fonts` hooks (my grep: no match) — no redraw on `fonts.ready`. → **F.W3/W4**.
- **D-18 / C-13 — MINOR.** The 300° full-saturation constant-L rainbow is theme-blind and bypasses both installed colour substrates (value.js `mixColorsN`/`sampleColorRamp`; glass-ui `cssToOklch`/`oklchStopToHex` — adopted from both readers' d.ts reads). With M-β1 the ramp gets ONE home when it gets a better formula. → **F.W1** (substrate adoption question) with an F.W3/W4 execution rider.
- **M-α5 — MINOR (missed find, reader-DU; re-proven).** The focal mark mixes colour systems: tip glow `hexToRgba(VIZ_COLORS.fourier,…)` (`epicycles.ts:283` — grey under D-1) around a hardcoded `#ff3b3b` core (`:289`) + white highlight (`:295`). A third divergence site that survives any `basisDisplay` fix. → **F.W1** rider on the palette wave.
- **D-19 — MINOR.** The `"touches" in e` arm is uninhabited at the only call site (`@click` only — my template read); the "Touch / click toggle for mobile" comment misleads. → **F.W3/W4**.
- **D-21 / BC-17 — MINOR.** `768` container-width literal duplicated (`:151`, `:339`), named `isDesktop` (it measures the container — `useCanvasSetup.ts:18-23`), silently swapping the presentation (fit/connector/bounds all gated); third breakpoint `useMediaQuery(1024px)` under a similar name in the same feature. → **F.W3/W4**.
- **D-22 — MINOR (hit-region half killed, K-9).** Export re-enters the live draw path: pushes a live trail point, bakes `hover.getScale()`/pinned golden recolour at an arbitrary shimmer phase into the PNG. → **F.W3/W4** (export-truth item).
- **D-23 / BC-21 / C-14 — MINOR** (R-5). `defineExpose({ anim, exportFrame, drawImageOverlay })`: one consumer-used member; `drawImageOverlay` structurally uncallable (neither argument constructible from outside); `anim` re-exports the global store through a component ref — the retired out-of-band idiom. → **F.W3/W4**.
- **D-24 — MINOR.** No opaque base is ever painted before `toDataURL` — every export carries alpha out; the greys were tuned against `--card`. → **F.W3/W4** (export-truth).
- **D-25 — MINOR.** `fourier-frame-${Date.now()}.png` — slug/bases/N/t all in scope and unused. → **F.W3/W4** (export-truth).
- **BC-12 — MINOR.** `s.ctx` swap across ~40 lines with no try/finally (`:479-504`); any throw strands the live surface on a detached buffer. → **F.W3/W4** (export-truth).
- **BC-14 / C-10 — MINOR.** `basesData?.partial_sums?.fourier` vs `basesData?.partial_sums[basisName]` (`:268-270`) over one uniform index signature — vacuous ternary; exactly one of "dead `?.`"/"latent TypeError" is true and the source doesn't say which. → **F.W3/W4** (dies with the C-7 reconciliation).
- **BC-15 / C-16 — MINOR.** `data: typeof store.epicycleData & {}` (`:122`) where `EpicycleData` is imported two lines up — Pinia-coupled signature on a pure draw function. → **F.W3/W4**.
- **BC-16 — MINOR.** Pin/hover desync: `onMouseLeave` touches everything but `pinnedBasis` (`:118-131`, read) → `{pinned, hovered:null}` → next tap unpins invisibly; two taps to re-engage. → **F.W3/W4** (one latch, one observable).
- **BC-18 — MINOR.** Watchers A and B both fire per `activeBases` reassignment (VV`:267` wholesale) — two full frames + a redundant 33-sample bbox rebuild; render watcher carries both `anim.t` and its own computed `easedT`. → **F.W3/W4**.
- **C-11 — MINOR.** `currentScale += diff * 0.12` per **frame** (`:52-63`, no clock — read): ~2× faster at 120Hz; keyframes.js 4.3.0 — a satisfied hard dependency — ships the scalar-tween family incl. `reducedMotionScale`. → **F.W1** (keyframes substrate adoption; also serves D-8).
- **C-15 — MINOR.** `activeBases?: string[]` over a closed in-file enumerated set; zero `defineEmits` while siblings recompute `hasEpicycles`. A `BasisKey` union retires the `if (!cfg) continue` guards. → **F.W3/W4**.
- **D-10-remnant — MINOR** (the surviving residue of K-2). `placeholder.ts:59`'s two-string vocabulary keys on `hasImage`, not compute state — the canvas asserts "Computing…" after failure, forever; the component reads neither `store.computing` nor `store.error` (adopted, anchors read). → **F.W3/W4** (error-posture row, with M-α1).
- **M-α4 — MINOR (missed find, reader-DU; re-derived).** Legend hit regions carry `y: yOff - iconYAdj` with `iconYAdj` 8-vs-2 by basis under a comment claiming uniformity (`labels.ts:49`, `:60-65` — read): fourier→poly ordering leaves a 6px dead band inside the legend; poly→fourier a 6px silently-shadowed overlap (earlier row wins via `break`). Both orderings reachable (loader puts fourier first; the selector appends it last). → **F.W3/W4**.
- **M-α6 — MINOR (missed find, reader-DU; SCOPE-CORRECTED by this seat per the L-9.1 scope law).** `--duration-mid` does not exist in the installed producer (my full enumeration: instant/fast/normal/panel/shimmer/shimmer-fast/slow/xl/xxl) nor locally — VV`:402`'s crossfade always rides the bespoke 0.24s fallback with bare `ease`. `--ease-apple-spring` likewise does not exist (producer ships `--ease-apple` and `--ease-spring*`) — and my repo grep finds it consumed in **five files** (VV:426, GallerySearchBar:188-189, GalleryCard:196/:295, AppHeader:280), several under "A.W3.d — bezier→canonical token" comments asserting it is canonical; with no fallback the whole declaration is invalid at computed-value time. DU booked only VV's dead `.expand-pop-*` block (confirmed dead — zero `<Transition name="expand-pop">`, my grep); the row is booked here at the scope where the count lives. → **F.W1** (token-parity with the producer).
- **M-α7 — MINOR (missed find, reader-DU; re-proven).** `canvas-container` is claimed by two components on nested elements in one scoped namespace (VV`:198` wrapper; BasisCanvas `:521` root — parent scope id lands on both); masked only by the `>` combinators at `:392`/`:404`; `FullscreenViewer`'s `:deep(.canvas-container)` means the *child's* root. One name, two owners, two meanings. → **F.W3/W4**.
- **M-α8 — MINOR (missed find, reader-DU; re-proven).** The canvas click-to-upload affordance is a bare `<div>` handler (VV`:198`, hidden input `:201`, `cursor:pointer` the only signal `:477-479`) with no role/name/keyboard path — and below 1024px the controls aside is `display:none` (`:443-447`), so a keyboard user on the Canvas tab has **no reachable upload control**. The consumer-side twin of D-3. → **F.W3/W4** (a11y row).
- **M-β4 — MINOR (missed find, reader-LC; re-proven).** `EpicycleData.trace` — a 3000-sample polyline the API computes per response — has **zero readers** in the web tree (my grep: the `types.ts:25` declaration only) and rides `structuredClone` into every IndexedDB draft (`workspace.ts:95-104`, read). The R6-8 seam's converse: server computes, nobody consumes. → **F.W5–W8**.
- **M-β5 — MINOR (missed find, reader-LC; re-proven).** `hexToRgba`/`hexToRgb` (`colors.ts:101-117`, read) do zero format validation — a 3/4/8-digit hex yields `rgba(…, NaN, …)`, which Canvas2D silently ignores on assignment (previous fill/stroke style survives). Masked today by the 7-char fallback; detonates the moment D-1 is cured with a hex-emitting branch. → **F.W1**, same wave as D-1 (the cure's own gate).
- **M-doc — MINOR (merged doc/dead-surface hygiene row; parts found by both readers, all re-read).** (a) `labels.ts:12` says "top-right corner"; the implementation is top-left — load-bearing misinformation three findings compute against. (b) `golden-shimmer.ts:10`'s docstring misstates its own period by 2π (~200ms vs ≈1257ms — the upstream source of the killed 5 Hz claim) AND its range (0.85→1.0 claimed; 0.70→1.00 actual, sin is signed). (c) Both label functions destructure `width`/`height` unused (`:20`, `:85`); no `noUnusedLocals` to catch it (my tsconfig read). (d) `getPathBounds` — the superseded variadic hot-path hazard — still barrel-exported with zero callers (my grep): one import away from reintroduction. → **F.W3/W4**.

### INFO

- **BC-22 — INFO.** Draw guarded, download unconditional (`:484` vs `:506-512`) — transparent PNG on empty state; upstream-gated. → **F.W3/W4** rider (export-truth).
- **BC-23 — INFO.** `needsMove || i === 0` — second disjunct unreachable-true (`:280`/`:293-295`, read). → **F.W3/W4** rider.
- **BC-24 — INFO.** `fourierPositionsAt` allocates 201 tuples + array per call, twice per frame, plus `slice` (`bases.ts:27-45`, read; `TrailManager` demonstrates the scratch pattern). → **F.W3/W4** rider.
- **C-17 — INFO.** Overlay fetched unconditionally, then refused when `image_bounds` is null; second correctly-sized fetch follows — the first is waste (adopted; race discipline itself is S-6). → **F.W5–W8** rider.
- **C-18 — INFO.** `overlayUrl` raw string-concat outside `coreFetch`; `/api/api/...` under the Dockerfile default; compose passes empty — config-gated. → **F.W5–W8** rider.
- **D-27 — INFO (convergence).** The `N = k` readout hand-drawn into the bitmap while `MetricBadge` renders on the dock positioned over this very canvas (drift note: the dirty tree renames its prop `:amount→:value` — the import is alive). Hoisting to DOM cures thirds of D-3 and D-7 at once; `withLabels` already exists to keep it out of exports. → **F.W1** (glass adoption) with the D-3 cure.
- **D-corpus-C-1 — INFO (corpus correction UPHELD).** `cartoon-surface` survives the 4→7 uplift (producer 7.0.0 still declares the utility and imports it into the styles entry — adopted from DU's producer reads, consistent with my installed-dist read of the 4.0.0 body). The shim is a design ask, not a build break. → **F.W1** epistemic carry.
- **L §R5-7 — INFO (census generalisation UPHELD).** The component's 9-line template hides N interactive controls with 0 DOM nodes — any instance denominator built on this file reads "1 element, 0 loops, 0 interactive descendants". `ConvergencePlot.vue`/`FrequencyGraph.vue` must be checked before publishing any percentage. → **NO-WAVE-OWNER** (SS-3/SS-4 spec-authoring input; census methodology, not a repair).
- **M-β6 — INFO (missed find, reader-LC; re-proven).** `resolveVizColors` makes five separate `getComputedStyle(document.documentElement)` round-trips per invocation (one for the reader-less `green`), re-run on **any** root class mutation (`App.vue:12-17` — no value filter; read). → **F.W1** rider.
- **M-β7 — INFO (missed find, reader-LC; re-proven).** Empty `activeBases` — deliberately supported by the selector ("canvas handles it gracefully", `BasisSelector.vue`, read) — paints an orphan animated `N = k` at y=12 over an empty grid (`labels.ts:68-73` draws the N row unconditionally; routing re-walked). → **F.W3/W4** rider.
- **M-β8 — INFO (missed find, reader-LC; re-proven).** `isInEpicycleRegion` closes over `hover` inside `hover`'s own initialiser (`:66-72`) — works only because the predicate is never invoked during setup; a constructor-time probe is a TDZ ReferenceError. Also redundant (mouse coords are in scope at the call site). → **F.W3/W4** rider.

## Killed-claims register

- **K-1 · D-26 (whole row) — KILLED** (R-2 ruling; this seat's own producer read). glass-ui `./axes` is the type-axis grammar — string tuples, zero rendering. reader-LC's confirmed-INFO cell is overruled: it admitted the API was unread and confirmed anyway. Nothing in `./axes` can replace `grid.ts:46-105`; the F.W1 convergence study is pointed at `./fourier-field`/`./fourier-math` instead.
- **K-2 · D-10 / BC-19 headline — KILLED** (both readers; ratified by this seat's read of `useWorkspaceLoader.ts:124-133` — the `// Error toast` watcher is verbatim as claimed, `useToast` wired, `<Toaster/>` mounted at App root, read). BC-19's own falsifier check ("a toast covers it — DEAD") was explicitly wrong. Remnant booked at MINOR (D-10-remnant) + M-α1 for what the kill exposes.
- **K-3 · D-8's "≈5 Hz" sub-claim — KILLED** (both readers; ratified: `sin(now/200)` ⇒ period 2π·200 ≈ 1257ms ⇒ ≈0.80 Hz; companion ≈0.53 Hz). Matters because 5 Hz would misroute the repair into 2.3.1 flash territory. Upstream cause = the docstring itself (M-doc b).
- **K-4 · D-5's paired pixel figures — KILLED** (reader-DU; ratified from `epicycles.ts:78` — `bfs = min(...)` means at most one axis binds; the "~90px left AND ~62px below" pair cannot co-realise). The unconditional inequality survives and is booked. Secondary: the hard cut is the bitmap bound, not `.canvas-container{overflow:hidden}`.
- **K-5 · C-12's horizontal + fourth-row halves — KILLED** (both readers; ratified: `fourierModes` cycle makes the two fourier rows mutually exclusive — 3-row hard max, my read of `BasisSelector.vue`; widest reachable row ≈134-150px < 200px). The vertical under-coverage survives inside D-11.
- **K-6 · D-12's falsifier ("host-supplied by construction") — KILLED** (reader-DU; ratified at VV`:198` — no `cartoon-card` on the wrapper). See R-1 and DISSENT §1.
- **K-7 · BC-13's cure-efficacy claim — KILLED** (reader-DU; ratified: TypeScript has no unread-member diagnostic; tsconfig sets neither unused flag anyway). The untyped-bag defect fact survives at MINOR. Law reaffirmed: the cure is adjudicated separately from the defect.
- **K-8 · BC-10b's tab-hide third — KILLED** (reader-LC; platform fact: rAF is suspended for hidden documents in every current engine). Scroll-away and fullscreen-occlusion arms survive.
- **K-9 · D-22's hit-region sub-claim — KILLED** (reader-LC; ratified: `exportFrame` passes the same surface dims and `labels.ts` derives regions purely from constants + `measureText` under fonts it sets itself — the write is a no-op of an equal value).
- **K-10 · D-1's `.green` blast-radius arm — KILLED** (reader-LC; ratified by my grep: `VIZ_COLORS.green|rainbow|pink|emerald` have zero readers repo-wide — the assignment at `colors.ts:95` is the only hit). Four of nine palette keys are dead; the BLOCKER stands on fourier/chebyshev/legendre alone.
- **K-11 · The shared "pinned basis holds the shimmer rAF alive indefinitely" mechanism — KILLED BY THIS SEAT against both readers** (R-6). `shimmerTick` exits when `hoveredBasis` is null; every off-label mousemove and mouseleave nulls it while leaving `pinnedBasis` set. The indefinite regime is the touch/parked-pointer path only. M-β2 and D-8 are re-scoped accordingly; BC-16 (the latch/observable desync) is the defect this mechanism actually evidences.
- **K-12 · reader-LC's "VV:162 is itself effectively unreachable" cell — KILLED BY THIS SEAT** (R-4). The toast watcher nulls the error **only when `imageSlug` is truthy**; `:162` (`store.error && !store.imageSlug`) is the live load-failure surface. The correct narrowing is M-α1's (the ContourSettings banner).

## Superlatives — with provenance (L-18 runs both ways)

1. **The reference-counted visibility credit** (`BasisCanvas.vue:432-459` + `animation.ts:96-106`) — both readers; all four mutation sites re-traced by this seat and balanced: IO-absence floor, `lastVisible` dedupe, conditional unmount release, underflow clamp; `playing` stays intent while visibility parks the clock. BC-10 attacks the sensor; the counter is airtight.
2. **The three-way watcher split with its deliberate absences written at the site** (`:374-423`) — both readers; comments state negative design constraints ("basesData is intentionally NOT here"), buying the trail-survives-overlay-arrival motion invariant; both watchers defer to `setupCanvas()` when unmounted. (BC-18 is a trigger-narrowing fix, not an argument to merge.)
3. **`useViewTransform.ts` — diagnose, fix, justify, and leave the superseded implementation as the diff** — both readers; the memo's dependency set verified against the store's `shallowRef`+`markRaw` discipline (this seat re-read both). Debit stands: the superseded `getPathBounds` is still barrel-exported (M-doc d).
4. **DC-term suppression argued from the physics** (`epicycles.ts:164-190`) — both readers; the non-obvious half (why the arm still draws) is the half written down; re-read verbatim by this seat.
5. **`computeStableEpicycleBbox` chooses stability over tightness and names the trade** (`:27-53`) — reader-DU; 32-phase union pins the inset geometrically; D-5 is a multiplier applied to this foundation, not a fault in it.
6. **`useImageOverlay`'s async-race discipline on both handlers** — both readers; the `onerror` cache-key re-check is the omission everyone else makes.
7. **Split revision counters + the compute depth counter** (`workspace.ts:53-69`) — re-read verbatim by this seat; the comment states cause→symptom→consequence, and the asymmetry is load-bearing; `beginCompute`/`endCompute` keeps `computing` truthful across sequential async steps and cannot underflow.
8. **`drawEpicycleCircles` is defensively re-entrant in exactly the way the export path needs** (`epicycles.ts:143-232`) — reader-LC, un-credited by the corpus; the `nVis<=0` guard exits **before** `save()`, and every paint op owns its alpha — which is simultaneously why the export can borrow a foreign context cleanly AND why D-14's caller-set shimmer is dead. The repair is a shimmer **parameter**; removing the internal resets would reintroduce state bleed across the export boundary. Ratified by this seat's read.
9. **The FullscreenViewer focus trap** (`FullscreenViewer.vue:30-101`) — reader-DU, un-credited by the corpus; re-read whole by this seat: the `offsetParent !== null || el === document.activeElement` filter, empty-ring container focus, open/close focus restoration, lifetime-scoped listener. The layer *around* the canvas is properly trapped even though the canvas is opaque to AT — the counterweight that keeps D-3 honest.
10. **The stage-collapse forensics comments** (`VisualizationView.vue:336-347`, `:381-387`) — reader-DU; re-read: twelve lines naming the exact substrate-seam failure each `:deep` rule cures — precisely the notes the F.W1 4→7 uplift needs.
11. **The trail scrub-buffer discipline** (`trail.ts:12-38`, `:51-68`) — credited and then narrowed twice, correctly: LC narrowed it off the loop-back regime (BC-20); the second narrowing stands (M-β3: the buffers are never built for warm-store mounts). The backwards-scrub correctness itself is real and re-read.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. The rendered swatch + exact `getPropertyValue('--viz-fourier')` string in the shipping engine (`light-dark()` arm vs bare `oklch()` floor). Immaterial to the D-1 verdict (all forms fail all four regex arms); it is the 30-second live confirmation.
2. Screen-reader announcement transcript for the `/w/:slug` canvas subtree (D-3's DOM claim is static and exhaustive; the transcript is not).
3. D-5/D-6 realised magnitudes: clipped-pixel counts per contour aspect; the dock's rendered height (GlassDock collapse state) and whether `min(…, 100dvw-1rem)` actually overflows the 1024-1280px stage cell or flex-shrink absorbs it.
4. D-7's formal 1.4.3 citation scope for canvas-rendered text (the 1.77/1.98/2.71:1 ratios are exact and thrice-derived; only the citation is soft).
5. D-11's surviving glyph band below y=100 under real font metrics.
6. D-16 resampling artefacts at fractional DPR/zoom; D-17's first-frame font substitution under a throttled load.
7. BC-10/BC-20/M-β2 magnitudes: dual-canvas frame cost under fullscreen; trail heap-growth and frame-time curves; the paused-hover append steady-state.
8. D-22's pinned-export appearance (golden recolour at a frozen shimmer phase) — the state coupling is static.
9. C-8's semantic blast radius (0.11→0.13 signature diffs — the 0.11 typings are not in this tree); plus the C-1 cure caveat that value.js's parser-proof gate records R1 (`parseCssColor("oklch()")` live crash) — the reason the repair targets glass-ui's `cssToOklch` wrapper, taken from the value.js corpus and not verifiable from the fourier tree.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet, not the tree).** reader-LC's D-12 cell repeats the corpus falsifier "the chrome is host-supplied by construction" verbatim — false at VV`:198` by this seat's read, and already killed with the correct read by reader-DU in the same corpus round. LC's *other* D-12 facts (the dist `cartoon-surface` body, the hover-lift consequence) were genuinely its own. The verdict survives; the cell did not. Same signature as the ColorSpaceSelector adjudication's L-10 event: stale-cell repetition without re-derivation is detectable and is itself evidence of inheritance.
2. **reader-DU's severity dissents preserved**: DU graded D-12 and D-13 MAJOR (this seat ruled MINOR) on the argument that a 1px lift + shadow transition on the busiest repaint surface, and a visible z-order/weight swap keyed to an unrelated toggle, are user-visible design incoherences on the flagship surface. If SS-13's live pass shows either is prominently visible in ordinary use, the severity re-opens.
3. **reader-LC's verification lapse recorded**: confirming D-26 while marking its API unread (R-2) and the ":162 unreachable" overreach (K-12) are the same failure shape — extending a true neighbouring fact past what was read. Both of LC's cells sat beside genuinely excellent original work (M-β1..β8, five of eight banked at MAJOR/MINOR).
4. **This seat's own scope note**: M-α6's five-file `--ease-apple-spring` correction was derived from a repo grep, not from reading all five consumers; the two gallery files and AppHeader were not read whole. The token's absence from the producer and the local tree is exhaustively derived; the per-site consequence (which declarations invalidate) is anchor-level only at VV:426.

## Closing verdict

**ADJUDICATED — the grey-canvas/blind-canvas BLOCKER pair stands (D-1/BC-1/C-1 · D-3); 64 rows confirmed (2 BLOCKER · 21 MAJOR · 30 MINOR · 11 INFO), 12 kills banked (2 minted by this seat against the readers), 16 missed finds adopted after re-proof; identities folded, never re-booked (C-5→R6-8 · C-8→F.W1 tri-package charter · L§R5-7→census); routings are M-25 agglomeration input for the forming X·F specs — F.W1 owns the colour/token/package substrate cluster, F.W3/W4 owns the frontend truth-and-a11y cluster, F.W5–W8 owns the settings/AnimationData/trace contract cluster; nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
