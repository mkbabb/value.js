claude-fable-5

# fr-CoefficientsSpectrum — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-08 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/shared/CoefficientsSpectrum.vue` (168 lines) + its tooltip-shim / `#graph` / host / producer chain.
**Substrate**: fourier HEAD `cd26c65` — re-pinned by this seat (`git log -1`). **Dirty-tree disclosure**: the subject is HEAD-clean; the working tree carries 5 modified rows including `web/package.json`, `web/package-lock.json` and **`EquationView.vue`** — every EquationView anchor in this record was re-read against the WORKING tree (the state that ships), not trusted from the corpus. **Producer disclosure**: glass-ui is LIVE and unpinned — `v7.0.0-393-g7d9abe08` at this seat's read; the readers measured 322-past-tag; 71 producer commits landed between their pass and this one. Every producer-HEAD claim below is re-run at `7d9abe08`.
**Corpus**: 87 accusation rows across three axes — challenge-D (design, 38 rows, opus-5[1m], with a folded prior D pass it self-corrects), challenge-L (library, 20 rows, opus-5[1m]), challenge-C (consumption, 29+7 rows, opus-5[1m], second pass folding its first) — at `docs/tranches/V/megatranche/audit/fourier-components/CoefficientsSpectrum/`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Immutable challenge files sit beside this record; every load-bearing or disputed cell was re-derived by this seat with its own instruments before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. Every kill was re-derived (a weak kill gets un-killed — none needed un-killing, but three reader-DU confirmation cells died under reader-LC's kills and this seat's reads); every missed find was re-proven before banking; all seven reader-vs-reader conflicts were ruled with fresh reads; the identity guard was run over the whole `adjudicated/` registry before any row was booked — it caught **four** identities the readers would have re-booked.
**Arbiter's own instruments this session (all static + node, zero browser probes — probe parsimony; product source read-only)**: whole-file read of CoefficientsSpectrum.vue (168 — every template/script/style anchor in this record is this seat's own); VisualizationView.vue:270-276 (**`:273` `v-if="store.epicycleData || store.computing"` — the gate that decides R-1**); EquationView.vue:210-216 (**`:213` `v-if="components.length"`**); App.vue:20-30 (**`:23` `<TooltipProvider :delay-duration="400" :skip-delay-duration="200">`**); CoefficientsPanel.vue (whole, 26) + EqCoefficientsPanel.vue (whole, 17); FrequencyGraph.vue:40-50 (`spectrumColor` byte-twin + the 0.008 fractional floor) / :70-115 (**`getComputedStyle(canvas)` INSIDE the per-bar loop at :108**) / :135-160 (**the `{deep:true}` watcher at :157**; `draw()` re-run per hover-move) / :190-210 (**the tooltip grid: Amplitude + optional log₁₀ + Phase — NO Re/Im row**, the R-7 decider); installed reka-ui@2.9.10 dist: TooltipProvider.js:47-59 (**`isOpenDelayed = ref(true)`, skip-delay timer, `onOpen → false`**), TooltipRoot.js:82-115 (**`:103-104` `isOpenDelayed ? handleDelayedOpen() : handleOpen()`**), TooltipTrigger.js:60/:70-74 (**touch early-return; `handleFocus → rootContext.onOpen()` — the IMMEDIATE path, the R-2 decider**), Primitive.js:24 (`asTag = props.asChild ? "template" : props.as`); installed glass-ui **4.0.0** (exports map re-read: `./animated-digit` **YES**, `./icon-tooltip` `./progress` `./fading-scroll` `./metric-stack` all YES; `dist/animated-digit.js` props block — `grep -c initial` → **0**); **producer glass-ui 7.0.0 @ HEAD `7d9abe08`**: exports map — `./animated-digit` **NO** / `./progress` YES / `./motion` YES; `src/components/` has no `animated-digit` dir; deletion commit **`4bf53962` (2026-08-04, "BK #18 W-DELETE — the Φ5 deletion spine") verified to exist**; **`git show v7.0.0:package.json | grep -c animated-digit` → 5 — the subpath EXISTS at the tag** (the R-5 honesty caveat, confirmed); Button.vue:126-146 (**the "THE SILENT BREAK GETS A VOICE" DEV `watchEffect` read verbatim** — detects `"variant" in attrs`, names the `emphasis`/`tone` migration); `_shared/floating.ts` = **15 lines**, `grep -rn 'RETIRED_FLOATING|floatingContentAttrs' src/` → **∅** (the K-1 ratification); `src/composables/motion/index.ts:56` re-exports `useAnimatedNumber` (the cure path survives); metric/README.md tail ("Everything here is noninteractive…") verbatim; **MIGRATION.md:738-739 still homes `AnimatedDigitMode`/`AnimatedDigitProps` at `/animated-digit` with no removal marker** (the readers' :721-722 has drifted — the doc grew — the staleness itself is re-confirmed); fourier producers: epicycles.py:78-95 (**DC + both ±n under the `neg_idx != n` guard = 2N+1**), defaults.ts:7 (**`n_harmonics: 200`**); receipts re-run in `web/`: **`npm ls @mkbabb/value.js` → the byte-identical `invalid: "^0.10.0 || ^0.11.0"` tree + `ELSPROBLEMS`**; installed versions glass-ui 4.0.0 / keyframes 4.3.0 / value.js 0.13.0; identity-guard sweep over `adjudicated/*.md` for `text-admin-label` / `ELSPROBLEMS` / `variant="ghost"` / `lucide-vue-next` / `animated-digit` — **`animated-digit` returns ∅: the B-4 banking is genuinely new**.

## Scoreboard

**47 adjudicated register rows (4 BLOCKER · 16 MAJOR · 22 MINOR · 5 INFO), covering the overwhelming mass of the 87-row corpus · 4 identities FOLDED BY REFERENCE (peer graph → fr-AnimationControls C-1 · Button `variant` → fr-BasisSelector M-7/fr-AdminAuditLog AA-2 · `text-admin-label` deletion → fr-App MG-β · lucide → census+fr-BasisSelector m-9 class) · 15 kills in the killed register · 10 missed finds banked (3 folded into composite rows) · 9 superlatives + 2 rescissions.**

Blocker-weight survivors: **B-1** (the four per-coefficient physics values are pointer-only under a docblock claiming the gap discharged), **B-2** (7-of-12 / 24-of-40 bars under the 1.4.11 3:1 floor on a 1.03:1 track), **B-3** (hue encodes rank-in-slice — one harmonic wears three colours in one panel and recolours itself on "Show more"), **B-4** (NEW, both readers convergent, neither axis filed it: **`@mkbabb/glass-ui/animated-digit` is deleted at the producer HEAD the F.W1 uplift will land on — line `:19` is the file's only build-time break, it is uncensused, and the corpus affirmatively cleared it**).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **D-M6/C-21's failure scenario** ("a failed first compute renders the compute instruction") | CONFIRMED the row whole, folding the VV gate as a "sharpening" | KILLED the named limb; banked the inverse (in-flight = instruction) as a missed find | **LC wins.** | My read of VisualizationView.vue:273: `v-if="store.epicycleData || store.computing"`. Failed FIRST compute → `epicycleData` null ∧ `computing` false → the panel UNMOUNTS; the misleading copy never paints. The reachable defect is the inverse: during the in-flight first compute the panel is mounted with `components = []` and renders `0 / 0` + "Compute epicycles to see coefficients" — instructing the user to start the compute already running. DU read the same gate and did not draw the kill from it — DISSENT 1. The contract defect (M-5) survives whole. |
| R-2 | **D-M10's keyboard/touch extension** ("the honest sentence is unavailable to keyboard and touch entirely, gated behind 400 ms") | CONFIRMED as filed | KILLED the keyboard+delay limbs | **LC wins.** | My reka reads: the `:124` trigger is glass-ui `Button` (native `<button>`, focusable); TooltipTrigger.js:70-74 `handleFocus → rootContext.onOpen()` where `onOpen: handleOpen` (TooltipRoot.js:110) is the IMMEDIATE path — no 400 ms, no `isOpenDelayed` consult. `ignoreNonKeyboardFocus` is unset at App.vue:23. Keyboard reaches the honest copy instantly; only touch remains excluded (TooltipTrigger.js:60). B-1 stays correctly scoped to the row `<div>`. |
| R-3 | **C-18** (`w-8` overflows at ±3-digit indices) | KILLED (unit-mixing: mobile type size against desktop box) | UNPROVEN (real Fira Code advance decides) | **DU's kill stands as filed.** | C-18's own arithmetic mixes roots — both quantities are root-relative and at EITHER root the box exceeds the 0.6 em estimate (28.8<32 desktop, 32.4<36 mobile; D's F-2 performed exactly this check). The claim as authored is dead. A thin font-metric residue rides SS-13 (residue 9); a residue is not a finding. |
| R-4 | **B-2's confirmation strength** (WCAG 1.4.11 sweep) | CONFIRMED — independently recomputed to 4 decimals, worst corrected 1.27→**1.21** | UNPROVEN-NEEDS-LIVE (glass/paper-texture compositing not statically decidable) | **CONFIRMED BLOCKER with an SS-13 exactness rider — the FR-AUL-7 precedent.** | Two independent derivations against two candidate surfaces agree within 0.06 on the same verdict; the track is a 50%-alpha neutral OVER the page so compositing can only raise backdrop luminance on the light arm; direction and mass (7/12, 24/40) are static-certain. What is live-only is the third digit behind `glass-floating`/`paper-texture` (App.vue:24, my read). Worst-case figure corrected to 1.21 per DU's full-sweep instrument (adopted). |
| R-5 | **B-4's severity** (the animated-digit deletion) | BLOCKER, flatly | MAJOR, with the tag-vs-HEAD honesty (`git show v7.0.0` carries the subpath) | **BLOCKER-at-uplift, with LC's caveat carried as a load-bearing F.W1 rider.** | My own runs at `7d9abe08`: exports map has no `./animated-digit`; no component dir; deletion commit `4bf53962` real; **and the tag DOES carry it (grep -c → 5)**. The census (F.W1 "glass 4→7") targets the 7.0.0 LINE, whose only live artifact is HEAD — the fr-AdminAuditLog precedent already ruled producer drift is the target's motion, not worker error. If F.W1 pins the dead tag exactly, the break defers one pin — which is precisely why the row's first obligation is RE-PIN, then size. It is the file's sole build-time (module-resolution) failure; everything else in the corpus fails at paint time. BLOCKER. |
| R-6 | **The MAJOR/MINOR ladder on M-10 (label triple) and L-M6 (phantom tooltip)** | MAJOR ×2 | MINOR ×2 | **MINOR ×2.** | M-10 after R-2: what survives is wrong copy (promises N, delivers 40; the tooltip false for 13≤N≤39) with the honest sentence keyboard-reachable — no capability loss, `Math.min(40, totalComponents)` in scope at `:41` (my read). L-M6: reka survives the empty slot without throw or warn (both readers' Slot.js reads agree), content is Presence-gated, cost is instance-level — one-line `v-if` hoist. DU's dissents preserved; re-open on SS-13 evidence. |
| R-7 | **D-m16's cross-file limb** ("FrequencyGraph.vue:200 uses the same Re/Im block") | CONFIRMED the limb inside its m-16 cell | KILLED the limb | **LC wins — this seat's own read.** | FrequencyGraph.vue:200-209 (read whole): Amplitude, an optional log₁₀ row, Phase. **No `Re / Im` row exists.** The two files share the grid container class, not the notation. m-16's subject-local claim survives (m-11 below); DU's confirmation cell carried a false limb — DISSENT 1. |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · ADJUDICATED → route**. Full proof cells live in the challenge files and the reader returns, incorporated by reference; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **B-1 = D:B-1 / L-B2 / C-5 — BLOCKER.** The per-row Tooltip trigger is a non-focusable `<div>` (`:85`, no tabindex/role; `:165-167` `cursor: default`), so amplitude-4dp / phase / relative / Re-Im (`:110-119`, their only surface) are pointer-only — touch independently excluded (TooltipTrigger.js:60, my read) — while the docblock (`:13-15`) claims the L5 §5 A8 gap DISCHARGED. Mechanism certain at the installed primitives: `Primitive.js:24` discards `as:"button"` under `asChild` (my read); `Slot` writes no tabindex. Uplift does not cure (7.0.0 forces `type:"button"` only in the non-asChild branch — adopted, twice-agreed). WCAG 2.1.1/1.3.1. **ADJUDICATED → F.W3/W4** (focusable named trigger — `tabindex="0"` + list semantics with M-9, or drop `as-child`; prerequisite for the R3-7a 35-callsite tooltip disposition landing as an improvement; the docblock's false discharge sentence must be corrected by whatever wave touches the file).
- **B-2 = D:B-2 — BLOCKER (R-4).** 7 of 12 collapsed and 24 of 40 expanded bars fail WCAG 1.4.11's 3:1 non-text floor in light mode (worst **1.21:1**, corrected); the track (`bg-muted/50`, `:89`) sits at 1.03:1 and supplies no reference frame; dark arm 1/12 and 3/40, worst 2.59. The bar is the sole graphical encoding of amplitude. Twice-derived against two surfaces (agreement ≤0.06); exact composited ratios ride SS-13. The falsifier chain holds: axe has never seen this DOM — both hosts collapse by default and the e2e layer stays closed (i-2). The house precedent (`--viz-amber` axe carry, style.css) and the resolver (`resolveVizColors`) exist and are bypassed. **ADJUDICATED → F.W3/W4** (one repair with B-3: the ramp moves to the palette owner and must clear 3:1 in both arms; SS-13 confirms the composite).
- **B-3 = D:B-3 / L-B1 / C-1 — BLOCKER.** `spectrumColor(i, topComponents.length)` (`:94`/`:107`, my read) keys hue to rank-in-the-current-slice: the same harmonic wears different colours collapsed (total=12) vs expanded (total=40) vs the byte-identical FrequencyGraph fork fed 40 (`:42-45`, my read; `:max-bars="40"` at CoefficientsPanel.vue:20) vs the gamma-curved canvas fork — three surfaces co-rendered in one panel, and "Show more" recolours every visible row, ANIMATED over 500 ms by M-3's `transition-all`. Colour is the only channel binding row↔bar↔circle (the hover channel is designed and dropped — m-17). Both surfaces present the dot/bar as bound to `n` (`:107-108`; FrequencyGraph labels bars `String(comp.index)`, my read), so rank-in-view-as-intent is dead. **ADJUDICATED → F.W3/W4** (one ramp, one owner, keyed on the datum, theme-routed — with M-10; the value.js `sampleColorRamp` angle rides F.W2).
- **B-4 = readers' convergent missed find — BLOCKER-at-uplift (R-5); the genuinely new banking of this adjudication.** `CoefficientsSpectrum.vue:19` `import { AnimatedDigit } from "@mkbabb/glass-ui/animated-digit"` — the subpath is an exports key at the installed 4.0.0 (my read) and **does not exist at producer HEAD `7d9abe08`** (my export-map read; component dir gone; deleted at `4bf53962`, whose body names the relay to "fourier CoefficientsSpectrum"). The file's only break that fails at BUILD time rather than paint time; uncensused (no row in the CENSUS §5 break table); affirmatively cleared by the D axis (S-4 "PRESENT at 7.0.0", killed at K-2) on citations to a file that does not exist at HEAD (K-3), with the producer's own MIGRATION.md:738-739 still homing the symbols at the dead subpath (i-4) as the proximate cause. **Honesty caveat carried (LC)**: the subpath exists at the v7.0.0 TAG — the corpus was right when authored (2026-08-04 morning) and wrong by that evening. **The cure is not a rename**: `Metric` is explicitly noninteractive-static (README tail, my read); the structurally sound cure is hand-wiring `useAnimatedNumber` (still exported via `./motion` at BOTH pins — my reads) + a `tabular-nums` span — exactly the pattern D:S-4 credited the adoption for retiring, inverted; and the composable's `initial` option simultaneously discharges m-21. **ADJUDICATED → F.W1** (first obligation: RE-PIN the producer target, then size; the census break-surface model must add the export-map diff at the ADOPTED commit, not the tag — composes with fr-BasisSelector M-7's prop-VALUE amendment).

### FOLDED BY REFERENCE (identity guard — banked elsewhere, never re-booked)

- **C:B-1 (npm ls `invalid` / ELSPROBLEMS; glass-ui peers `^0.10.0 || ^0.11.0` vs flat 0.13.0)** **= fr-AnimationControls C-1** (census §1 pins identity, G33 gate, "never re-book per-component"). This seat's run today is the **fifth** independent reproduction, byte-identical. Component-altitude: INFO. → F.W1, by reference.
- **D:M-4 / C-26 (`variant="ghost"` deleted at 7.0.0)** **= fr-BasisSelector M-7 + fr-AdminAuditLog AA-2** (the prop-VALUE break-surface F.W1 amendment; 37-file/124-site blast radius already measured there). The subject's `:127` is one instance; `size="sm"` survives (adopted, my Button.vue read consistent). What is NEW here rides as a rider on that identity: **the "silent break" framing is dead** — see K-4. → F.W1, by reference.
- **D:M-8 (`text-admin-label` deleted at 7.0.0; the tooltip grid at `:110` loses its register)** **= fr-App MG-β** (banked → F.W1 census gate, 7 sites / 4 files including this `:110`, cross-booked against fr-AdminAuditLog AA-15's collision). One identity; the subject's stake (the 10 px grid silently becoming ambient `text-sm`+fluid at uplift, D's i-1/m-8 interactions) is recorded here as a consumer note on that identity, not a row. → F.W1, by reference.
- **C-6 (lucide-vue-next from devDependencies; 1 of 35)** **= the census `:105` fleet row + the fr-BasisSelector m-9 class** (which also carries the dev-class misfiling). `:20` books as a site enumeration only. → F.W1, by reference.

### MAJOR

- **M-1 = D:M-1 / L-M1 / C-3 — MAJOR.** The whole motion contract is dead code: `<Tooltip>` bottoms out in a PopperRoot Fragment, so no TransitionGroup enter/leave/move applies — 17 of 168 lines (`:147-163`) unreachable, one Vue dev-warn per row per render (both readers verified the full five-hop chain in the installed dists; LC closed C-3's enter/leave hedge statically). A regression introduced by the B.W2.c lift itself. **→ F.W3/W4** (wrap the row in a real element; one edit with M-11's key fix; multiplicity of the warn = SS-13 residue 3).
- **M-2 = D:M-2 + DU-missed-4 (folded) — MAJOR.** Zero `prefers-reduced-motion` on the one LIVE motion path — `:91`'s 500 ms width+colour on 12-40 elements — while ten sibling files carry the guard AND **the component's own host region does**: ConfiguratorLayer's compiled body class carries `motion-reduce:transition-none` (adopted, twice-agreed dist read) one DOM level up. The cheapest cure shape in the fleet: one `motion-reduce:` token on `:91`. **→ F.W3/W4**.
- **M-3 = D:M-3 / L-M2 / C-9,C-10 — MAJOR.** The file contradicts its own covenant: `:146` "named properties + canonical tokens, no `transition: all`" vs `:91` `transition-all duration-500`; `:162` bare `ease` and `:148/:151/:162` literal durations where the exact token values ship (`--duration-normal`/`--duration-fast`). Only `width`/`backgroundColor` change — the named form is a drop-in, and narrowing to `transition-[width]` de-animates B-3's recolour as a side effect. **→ F.W3/W4**.
- **M-4 = D:M-5 / L-M3 / C-12 — MAJOR.** Visible column `toFixed(2)` (`:61-63`→`:101`) vs tooltip `toFixed(4)` (`:112`): the expanded 40-row state — the state "Show more" exists to produce — degenerates to a column of `0.00` exactly where the bars are least visible (B-2) and the colours most broken (B-3). Amplitudes are unnormalised; `maxAmplitude` is already computed and already used. LC's narrowing adopted: `snapThreshold` (0.05 absolute, dom-scale) snaps sub-0.05 tails on frame 1, so the damping-window disagreement is a head-row phenomenon. **→ F.W3/W4** (`toPrecision(3)` or a max-derived scale; one repair with m-9).
- **M-5 = D:M-6 / C-21, as corrected by R-1 — MAJOR.** The prop surface (`:24-33`) collapses four realities (never / computing / failed / stale-after-failure) onto two branches. Reachable scenarios, both verified by this seat: (a) **in-flight first compute** — VV:273 mounts the panel on `store.computing`, so the "empty" branch IS the loading branch, rendering `0 / 0` + an instruction to do what is already happening (LC-missed-3, folded here); (b) **failed RECOMPUTE** — neither route clears prior data (workspace.ts catch sets only `error`; EquationView's `doCompute` never clears `result` — corpus cells, adopted), so stale coefficients render as current, wrong-coloured per B-3. The killed limb (failed FIRST compute shows the instruction) is in K-8. Producer seat: 7.0.0 `Metric` ships the `loading`/`aria-busy` contract (adopted). **→ F.W3/W4** (a `state` prop or `#empty`/error surface; the Metric seat rides F.W1's host re-home).
- **M-6 = D:M-7 / L-m11 — MAJOR.** The 300 px port (`:78`) has no tabindex, no role, no name — keyboard users can press "Show more" and reach nothing it reveals; no scroll affordance, no `overscroll-behavior` (chaining into App.vue:26's `<main>` scroller = SS-13 residue 4); the magic pixel doesn't scale with the root fork. Producer ships FadingScroll at BOTH pins ("Unnamed ports remain ordinary focusable scroll containers"), zero imports. **→ F.W3/W4**.
- **M-7 = D:M-9 — MAJOR.** The expand control is buried inside the port it fills: 40 rows ≈ 2.5× the 300 px cap put "Show less" far below the fold of its own container; no sticky footer, no header toggle, and per M-6 no keyboard scrolling at all — a one-way door. **→ F.W3/W4** (hoist the toggle to a sibling of `:78`, or sticky-bottom it).
- **M-8 = D:M-11 / C-17, arithmetic corrected per K-9 — MAJOR.** A 40-row ceiling over a payload of **1001 at the slider max (2·500+1) and 401 at the shipped default (`n_harmonics: 200`, defaults.ts:7 — my reads of epicycles.py:78-95 and defaults.ts)** — expanded coverage 4%, not the corpus's 8%; the default-N=50 sizing premise is false (the `?? 50` fallbacks never bind). No filter, no virtualisation, no third step; FrequencyGraph is pinned below its own default. **→ F.W3/W4** (the ceiling decision must be sized against 401/1001, and every worked example in the corpus's M-10/M-11 cells carries the dead numbers — do not inherit them).
- **M-9 = D:M-12 / L-m10 + DU-missed-2 (folded as the cure) — MAJOR.** A three-column data table as unlabelled divs: Fragment list root, div rows, a bare `12 / 401` readout with no referent (`:72-76`), a bar whose inline width IS the datum with no `role`/`aria-valuenow` (`:90-97`), no live region; AnimatedDigit supplies no ARIA (adopted, dist-verified). **The cure is adoption, not authorship**: `./progress` is an exports key at BOTH pins (my reads) — 7.0.0's Progress is purpose-engineered for announced values (≤2dp `announcedValue`, name-channel discipline — adopted) — with the honest caveat that a static magnitude is semantically a `meter` vs Progress's `progressbar`; the wave states which, either beats none. Zero `glass-ui/progress` imports tree-wide (adopted). **→ F.W3/W4**.
- **M-10 = L-M4 / D-m11 / C-2 — MAJOR.** `spectrumColor` is the FOURTH fork of a symbol the tree exports twice (transforms.ts gamma; harmonics.ts linear-hsla ≡ the subject's at α=1), byte-identical to FrequencyGraph's private third — and it bypasses the repo's palette owner (`VIZ_COLORS`/`resolveVizColors`, re-run on theme flip) entirely: frozen `hsl(h,85%,55%)` in both themes, invisible to the house's own AA carry. The ownership/theming limb of the B-3 repair, booked separately so the fork census survives the cure. C-2's F.W2 reading adopted: the file's EMPTY value.js migration surface is the wrong reading — the ramp is where the bump pays for itself. **→ F.W2 + F.W3/W4** (one shared, theme-routed ramp in the palette owner; reconcile the gamma fork explicitly).
- **M-11 = L-M5 / D-m12 / C-4 — MAJOR.** `:key="`${comp.index}-${i}`"` (`:82`) couples identity to slice position though `comp.index` is unique by construction (epicycles.py's `neg_idx != n` guard — my read); any reorder remounts every row and every AnimatedDigit restarts from 0 (`initial ?? 0`, `.play()` at setup — adopted, dist-verified). Same edit as M-1's wrapper. **→ F.W3/W4**.
- **M-12 = C:B-2, demoted from BLOCKER by both readers, ratified — MAJOR.** The DC term is the normalization basis: `maxAmplitude = topComponents[0].amplitude` (`:43-45`, my read) and on the visualization route element 0 is `frequency 0` — the repo's own canvas testimony documents ("a stationary disc … potentially the size of the whole figure") and cures (`index === 0` suppression) the exact hazard; no cure exists on this path (adopted, grep-verified by both readers). Structure certain; **rendered magnitude is genuinely open** — LC's counter-scenario (centred figure → row 2 ≈ 41%) is why this is MAJOR-with-residue, not BLOCKER. **→ F.W3/W4** (the normalization decision: suppress, separate, or annotate DC — decided WITH the 2px-floor row m-7; magnitude = SS-13 residue 6).
- **M-13 = C-7 — MAJOR.** The epicycle operation's response is `data: dict`; the only typed component model (`EpicycleComponentDTO`) is dead (one self-hit) and disagrees with the wire; `BasisComponent` is hand-maintained against a Python dict literal — no derivable join, and R6-8's remedy presupposes the typed payload this operation denies. The equation route does it right, in the same API. (Adopted — LC re-verified all three shapes verbatim.) **→ F.W5–W8** (the R6-8 companion clause: an operation's response model must be structurally typed or the leaf is unjoinable in principle).
- **M-14 = DU-missed-3 ∘ D-m3, composed — MAJOR.** `skipDelayDuration: 200` (App.vue:23, my read) turns the ranked list into a zero-delay tooltip cascade: after the first tooltip closes, `isOpenDelayed` is false for 200 ms and every subsequent row opens INSTANTLY (TooltipProvider.js:47-59 + TooltipRoot.js:103-104, my reads) — and each opens `side="bottom"` (`:83`), occluding the ~4 rows beneath at ~16 px pitch, exactly the comparative context of a ranked list. The corpus costed a single 400 ms-delayed tooltip; the live behaviour on this surface is neither delayed nor single. One cure retires both limbs: `side="right"`/`"left"` (`avoidCollisions` hard-true at both pins — adopted). **→ F.W3/W4**.
- **M-15 = LC-missed-2 — MAJOR (attribution: FrequencyGraph.vue, the subject's `#graph` payload and render path).** `getComputedStyle(canvas)` executes INSIDE the per-bar draw loop (`:108` within the `:75` `for` — my read): up to 40 forced style recalculations per draw on a loop-invariant value; `draw()` is driven by a `{deep:true}` watcher over the whole coefficient array (`:157`, my read — 401 objects at the default) and re-runs per hover-move (`:139`); ConfiguratorLayer keeps the slot mounted while collapsed, so this executes behind a closed, `inert` panel. LC's "every store tick" phrasing is overstated (DISSENT 3); the forced-read-in-loop limb is certain. Hoist = one line. **→ F.W3/W4**.
- **M-16 = LC-missed-6, ratified and extended by this seat — MAJOR (method/structural).** The corpus's entire producer layer is pinned to a producer that MOVED: v7.0.0-322 at the readers' pass, **v7.0.0-393 at this seat's** — with three verified tag→HEAD inversions already banked in this record (animated-digit B-4; `RETIRED_FLOATING_ATTRS` K-1; Button's DEV voice K-4), all landing 2026-08-04, hours after the challenges were authored. Any F.W1 sizing built on the tag is sizing a target that no longer exists. **→ F.W1** (re-pin gate: derive the break surface at the ADOPTED commit) **+ NO-WAVE-OWNER** (SS-3/SS-4: producer-side evidence must carry the producer commit hash, not the version string — the fr-AdminAuditLog drift note, promoted to method).

### MINOR

- **m-1 = D:M-10 / L-m4 / C-27, demoted per R-2/R-6 — MINOR.** Three statements about one control, two wrong: `:133` promises `(N total)` and delivers 40; `:124` promises 40 unconditionally, false for 13≤N≤39. The keyboard-unreachability limb is DEAD (K-6); the honest copy opens instantly on focus. `Math.min(40, totalComponents)` in scope at `:41`. **→ F.W3/W4**.
- **m-2 = L-M6 / D-m1 / C-11, demoted per R-6 — MINOR.** Phantom tooltip: `v-if` on the inner Button (`:126`) not the Tooltip (`:124`) — full TooltipRoot machinery around a comment node for every N≤12, `:text` evaluated for a tooltip that can never open. Non-fatal (Slot returns children unchanged — both readers). One-line hoist. **→ F.W3/W4**.
- **m-3 = L-m1 / C-13 — MINOR.** Guard asymmetry: `formatPercent` guards zero (`:56-58`), the width binding performs the identical division unguarded (`:93`) → `width: NaN%` dropped, leaving the 2px floor. **→ F.W3/W4**.
- **m-4 = D-m4 / C-15 — MINOR.** One coefficient, three notations across two files: `+0` (`:87`), `n = 0` (`:108`), `0` (FrequencyGraph bar label — my read). `comp.index > 0` fixes the sign. **→ F.W3/W4**.
- **m-5 = D-m5 / L-m6 — MINOR.** A label register on a data grid: `text-admin-label` (`:110`) uppercases authored sentence-case prose, inherits caps tracking onto values, sets a 12 px pitch under a 14 px header in a transient overlay. The 4.0.0 utility facts are dist-verified (adopted); L-m6's producer-path cites are dead (K-11). Successor question rides the fr-App MG-β fold. **→ F.W3/W4** (`text-mono-micro` at both pins).
- **m-6 = D-m6 / L-m2 / C-8 — MINOR.** The descending-amplitude precondition three behaviours depend on is stated nowhere in the contract; all producers comply today (corpus-verified ×5); C-8's replay path (persisted `markRaw` draft) makes it reachable without a code change. `Math.max(...)` is one line. **→ F.W3/W4**.
- **m-7 = D-m13 — MINOR.** `minWidth: '2px'` (`:95`) on an `h-3 rounded-full` bar: radius exceeds width, the whole tail below ~0.7% renders as one identical dot — while FrequencyGraph one file away uses a fractional floor + optional logScale (`:47-49`, my read). Compounds M-4 and M-12 (the floor is what hides the DC pathology). **→ F.W3/W4** (decided WITH M-12).
- **m-8 = D-m8 — MINOR.** Uplift: tooltip padding proportion inverts 2.0→0.79 (both class strings dist-read — adopted); helps the grid, hurts the one-liner; budget both callsites. **→ F.W1 note**.
- **m-9 = D-m15 — MINOR.** Three type systems in 168 lines (`text-xs` ×4 · `text-admin-label` ×1 · hand-composed `fira-code` ×7); the two micro scales CROSS under the root fork (13.5 px mobile `text-xs` vs fixed 10 px). **→ F.W3/W4** (one repair with M-4/m-5).
- **m-10 = D-m16, cross-file limb killed per R-7 — MINOR.** `Re / Im` as a bare slash pair (`:117-118`): a negative imaginary reads `0.123 / -0.456` with the value separator ≡ the label separator, in a file that knows notation (`n =`, `°`). Subject-local only. **→ F.W3/W4**.
- **m-11 = C-19, axe limb killed per K-13 — MINOR.** Mount lifecycle diverges by host: ConfiguratorLayer keeps the body mounted-collapsed (`aria-hidden` + `inert` — no aria-hidden-focus violation); CollapsibleSection Presence-unmounts. Nothing records the asymmetry; it decides where M-15 runs and where m-19 latches. **→ F.W3/W4** (record it; L-M3(b)'s inverted version is K-7).
- **m-12 = C-23 — MINOR.** The primitive publishes `--digit-count` for exactly this problem; the consumer pins `w-16` with unbounded `toFixed(2)` and no truncate. Post-B-4 this books against the hand-wired successor: the replacement span must own its width. Rendered overflow = SS-13 residue 10. **→ F.W3/W4** (rides B-4's cure).
- **m-13 = L-m7 + LC-missed-4, composed — MINOR.** `emptyText` is worse than duplicated — on the equation route it is DEAD: EquationView:213 gates the panel on `components.length` (my read), and `components.length ≥ 1 ⇒ topComponents.length ≥ 1`, so the `v-else` can never render there; the byte-identical default restated at EqCoefficientsPanel.vue:14 (my read) decorates unreachable code. Rescinds half of D:S-2 (see superlatives). **→ F.W3/W4**.
- **m-14 = L-m8 — MINOR.** Colocation: `components/shared/` holds exactly one file; the `#graph` payload lives under `equation/` with zero equation-route consumers, forcing the cross-feature import at CoefficientsPanel.vue:5 (my read). **→ F.W3/W4**.
- **m-15 = L-m9 — MINOR.** The extraction left the third co-rendered tooltip-body copy standing in FrequencyGraph (same container classes, phase conversion inlined vs `formatPhase`) — one inside the other's `#graph` slot. **→ F.W3/W4**.
- **m-16 = C-14 / L-m3 — MINOR.** `12`/`40` authored across three sites incl. one template literal, with a two-site sync invariant and no prop — while the trivial knob (`emptyText`) IS a prop; the shared component is less configurable than the sibling it was extracted beside (`maxBars` exists there). **→ F.W3/W4**.
- **m-17 = C-16 — MINOR.** The cross-highlight seam is designed and dropped: FrequencyGraph declares 2 emits + `activeIndices`/`logScale`; its sole consumer binds neither; the subject offers no landing point (bare slot, zero emits — my read). `activeIndices`/`logScale` are dead tree-wide; every bar permanently "active". The interaction that would make B-3's colour channel redundant. **→ F.W3/W4** (slot props + a `hover-harmonic` emit).
- **m-18 = LC-missed-5, narrowed by this seat — MINOR.** `expanded` is latched: `ref(false)` at `:35`, no watcher anywhere (my whole-file read), no reset on payload identity change; the only toggle unmounts at N≤12 (`:126`) — expand to 40, recompute to ≤12, and the instance is stuck expanded with no affordance; the next N>12 payload opens at 40 unasked. **Narrowing (this seat's)**: on the equation route CollapsibleSection's Presence-unmount resets the ref per panel-open; the durable latch is the ConfiguratorLayer route, where the body never unmounts (m-11). **→ F.W3/W4**.
- **m-19 = C-22 — MINOR (latent; demoted from the corpus's MAJOR).** The subject accepts basis-agnostic `BasisComponent` and renders a Fourier-only readout (`n =`, Phase, Re/Im unconditional; no `basis` prop — my whole-file read confirms zero `basis` hits); for real-coefficient bases three of four tooltip rows become noise. No live break (both call sites Fourier); graded latent per the aliasing/contract precedent — but it is the named blocker for the lane-frontend §4 convergence this file is rowed for. Producer-side cites adopted from the corpus unverified. **→ F.W3/W4** (rider on the convergence decision).
- **m-20 = C-24, as transformed by B-4 — MINOR.** No consumer could seed the damper (no `initial` prop at 4.0.0 — my `grep -c` → 0), so every mount counts up from 0.00 behind a collapsed panel. The corpus's producer ask ("AnimatedDigit should forward `initial`") is DEAD — the component is deleted — and the cure inverts into a free rider on B-4: `useAnimatedNumber` itself accepts `initial` (adopted, dist-verified), so the hand-wired successor seeds for free. **→ F.W1** (inside B-4's repair unit; the BH relay item is retired WITH rationale).
- **m-21 = C-25, as re-derived at HEAD — MINOR.** The local shim re-monolithizes the decomposed tooltip and is the proximate cause of M-1's Fragment; the corpus's comparison target (`./icon-tooltip`) is itself deleted at 7.0.0 (fr-BasisSelector R-1, corroborated). The producer ask — a rich-body single-root tooltip monolith — survives and rides the standing glass BH relay; meanwhile the shim must either render a single element root or document that it may never be a transition child (L's §8 adapter note, adopted). **→ F.W3/W4 + glass BH relay**.
- **m-22 = D-m7 / D-i7 — MINOR.** The empty state leads with an unlabelled `0 / 0`: `:72-76` sits outside the `v-if`/`v-else` pair (my read); moving it inside `:78` costs nothing (no consumer reads the count when empty — both consumer files read whole) and dissolves the flex-wrapper-for-one-span while it's there. **→ F.W3/W4**.

### INFO

- **i-1 = D-m10 / L-i1 / C-I5 — INFO (intake fold).** R3-10's dynamic-`:is` registry drop is live byte-for-byte at `:132` (my read) — already adjudicated TRUE and carried → F.W4 at the intake; never re-booked. Design note preserved: the swapped icon is redundant reinforcement worth keeping through the codemod. R3-7a's "CoefficientsSpectrum 2" callsites confirmed at `:80`/`:124`, of which one is m-2's phantom — the F.W3 budget counts 2, deletes 1.
- **i-2 = C-I6 — INFO.** Zero test coverage: two comment-only e2e hits, no unit tree; and the specific mechanism that let B-2 survive — axe runs with the coefficients layer collapsed at both hosts. Any F.W3/W4 cure lands with the layer OPENED in at least one axe pass. **→ F.W3/W4 rider + SS-13**.
- **i-3 = C-20 / D-i5 / L-m5 — INFO.** `tabular-nums` hand-wired onto the primitive adopted to own it (`:102`); harmless (tailwind-merge dedupes), booked because the file's own docblock cites the register as the adoption's purpose — and because B-4's successor must now genuinely own it. **→ F.W1** (inside B-4).
- **i-4 = DU-missed-5, re-anchored by this seat — INFO (producer-doc defect; BH relay).** glass-ui MIGRATION.md **:738-739** (drifted from the readers' :721-722 — re-found by this seat) still homes `AnimatedDigitMode`/`AnimatedDigitProps` at `/animated-digit` with no removal marker, in a table that marks removals for its neighbours — the stale row that seeded D:S-4's false clearance and four fabricated cites (K-3). Measurable downstream cost inside this corpus alone. **→ glass BH relay** (standing formation invariant), riding B-4's F.W1 row.
- **i-5 = L §7 RH-5 inverse + D-i4/i-6 + C-I4 band — INFO.** (a) Callsite-keyed instance budgets understate this file ~20× (2 callsites → up to 41 live tooltip roots) — census methodology, **→ NO-WAVE-OWNER (SS-3/SS-4)**. (b) The row grid is hand-repeated flex literals while the tooltip two lines below uses real grid — of two minds (dies inside M-9's semantics repair). (c) `.fira-code`'s feature half is defeated on the AnimatedDigit host, family half applies — nil net effect on digits, recorded against comprehension. (d) `@reference "tailwindcss"` at `:145` is inert build cost. All → F.W3/W4 as riders, none needs its own item.

## Killed-claims register

- **K-1 · D-m9 (`collision-padding` on 7.0.0's "retired list", `floating.ts:28,30`) — KILLED, FABRICATED PROVENANCE** (reader-DU's kill; ratified by this seat's own reads at `7d9abe08`: `floating.ts` = 15 lines; `grep RETIRED_FLOATING|floatingContentAttrs` → ∅; TooltipContent forwards `{...attrs}` wholesale). LC's honesty adopted: the set DID exist at the v7.0.0 tag — true-at-tag, false-at-HEAD, the M-16 pattern. Nothing survives of the finding.
- **K-2 · D:S-4's uplift-clean superlative ("`./animated-digit` … PRESENT at 7.0.0, none in the removed set") — KILLED** (both readers; ratified by my export-map read). The superlative is the exact inverse of the live risk: the adoption created the file's ONLY hard uplift row, and the hand-wired predecessor would have created none. Replaced by B-4.
- **K-3 · Four line-cites across two axes into `glass-ui/src/components/animated-digit/AnimatedDigit.vue` (D:i-3, D:S-4, D:S-6, C-24) — KILLED: the file does not exist at producer HEAD** (my `src/components/` listing). C-24's substance survives against the installed 4.0.0 dist (my `grep -c initial` → 0) — only the 7.0.0 provenance is dead. Proximate cause = the stale MIGRATION.md rows (i-4).
- **K-4 · D:M-4's falsifier + "silent break" framing (and C-26's "no compat shim") — KILLED** (both readers; ratified by my verbatim read of Button.vue:126-146: the DEV `watchEffect` under "THE SILENT BREAK GETS A VOICE" detects `"variant" in attrs` and `console.error`s the exact migration). The break survives (folded to the banked identity); it is loud in dev, self-documenting, and stripped in production. The falsifier as printed ("grep → the string does not appear") is false on its own grep.
- **K-5 · D:M-4 / C-26 line cites into Button.vue (`:15-31`, `:33-40`, `:23`, `:37`) — KILLED as anchors** (reader-LC; ~23 lines of drift on a load-bearing uplift cite; correct anchors carried in the fold row). Claims survive.
- **K-6 · D:M-10's keyboard/touch extension ("unavailable to keyboard and touch entirely … gated behind delayDuration 400") — KILLED for keyboard AND for the delay** (reader-LC; ratified by my reka reads — `handleFocus → onOpen()`, the immediate path; native `<button>` trigger). Touch exclusion stands. m-1 is the surviving remainder.
- **K-7 · L-M3(b) ("this window recurs on EVERY panel open" via Presence-unmount, naming the visualization route first) — KILLED for the route it names** (reader-LC; ConfiguratorLayer renders the body unconditionally behind `aria-hidden`+`inert` — CSS reveal, no unmount; the damping runs once, invisibly). Only the equation route remounts. C-19 (m-11) holds the correct version.
- **K-8 · D:M-6's named scenario ("a failed first compute renders 'Compute epicycles to see coefficients'") + C-21's "identical to never-computed" — KILLED** (reader-LC; ratified by my VisualizationView:273 read — the panel UNMOUNTS on failed-first). The reachable scenario is the inverse and is banked inside M-5. Reader-DU's confirmation of the row carried the same gate as a "sharpening" without drawing the kill — DISSENT 1.
- **K-9 · D:M-11/M-10's arithmetic ("501 components", "default N = 50", "8%", "80% coverage") — KILLED as numbers, finding strengthened** (reader-LC; ratified by my epicycles.py 2N+1 read and defaults.ts:7 `n_harmonics: 200`). True figures: 1001 max / 401 default / 4% expanded coverage. Every worked example in those cells carries the dead numbers; M-8 carries the live ones.
- **K-10 · D:§A's "worst 1.27:1" — CORRECTED, not killed** (reader-DU's own full-sweep recompute: true minimum 1.21:1 at hue≈62°; every other figure reproduces to 4 decimals). Adopted into B-2; the drift strengthens the finding.
- **K-11 · L:S4 / L:RH-4 / L-m6's producer-path cites (`semantic.css:238`, 7.0.0 `scale.css:86` as the admin token) — KILLED** (reader-DU; corroborated by the banked fr-App MG-β roster read: no `text-admin-label` in 7.0.0's 25-name utility roster). The utility is live at the installed 4.0.0 pin — L's runtime conclusion right for today, its citation pointing at the tree where the opposite is true. D's M-8 self-correction independently confirmed.
- **K-12 · C-18 (`w-8` overflow at ±3 digits) — KILLED, UNIT-MIXING** (reader-DU; R-3 ruling — C-18 evaluated mobile type against the desktop box; both roots clear at the 0.6 em estimate; D's F-2 had already killed the hypothesis correctly). Thin font-metric residue → SS-13 residue 9.
- **K-13 · C-19's axe framing ("focusable Button inside an `aria-hidden` subtree") — KILLED** (reader-DU; the region carries `inert` WITH `aria-hidden` — no aria-hidden-focus violation, nothing focusable behind the collapse). The mount asymmetry survives as m-11.
- **K-14 · D-m16's cross-file corroboration ("FrequencyGraph.vue:200 uses the same block") — KILLED by this seat's own read** (R-7): the FrequencyGraph grid has NO Re/Im row. Subject-local claim survives (m-10).
- **K-15 · Cite-drift band — KILLED as anchors, none load-bearing** (reader-DU): `--tooltip-text` offsets.css:82→**:94**; class-names dist hash rebuilt; `--ease-standard` bridges.css:356→**:355**; `CoefficientsPanel.vue:13`→**:14** and `:19`→**:20** (my reads confirm :14/:20); VV `:274`→**:273** (my read). Plus MIGRATION.md `:721-722`→**:738-739** (this seat's own re-anchor — the readers' cite had itself drifted by publication time). Recorded because L-18 verdicts are re-checked by line.

## Superlatives — with provenance (L-18 runs both ways)

1. **The `#graph` slot is the right seam, and it is provably route-blind** (all three axes + both readers; re-verified in this seat's whole-file read — the subject's only FrequencyGraph knowledge is two comments, `:8`/`:68`; props = `components`+`emptyText`, no route flag, no store handle). A `:show-graph` boolean would have dragged 247 lines of Canvas2D into the equation route. **Preserve through every F.W3/W4 rework**; C-1's lesson is that the seam under-specified its payload (slot props), not that the seam is wrong.
2. **Host-side state discipline at the right altitude** (reader-LC's superlative; this seat's own VV:273 read is the proof): the route owns the store and gates the panel on `epicycleData || computing` — the one carried piece of the state contract, and the line that killed two corpus failure scenarios (K-8). The M-5 cure should extend this posture, not re-home it.
3. **Teardown is clean and the obvious leak hypothesis is false** (L:S1; both readers re-verified in the installed chunk — scope-disposed, generation-guarded, self-terminating; zero timers/listeners/rAF in the subject itself). A refuted hypothesis recorded as a refutation is worth more than a found defect.
4. **The tooltip escapes its own clipping container for free** (L:S2) — portal'd out of `:78`'s scroll port and `:89`'s `overflow-hidden` at both pins; the strongest argument FOR the B.W2.c lift, notwithstanding B-1.
5. **Zero hardcoded text colours in 168 lines, and the tokens carry their own contrast receipts** (D:S-3, both readers) — the sharpest sentence in the corpus stands: the single hardcoded colour in the file is the one that carries the data, and it is the one that fails.
6. **D's M-8 is a self-correction done right** (both readers; corroborated by the fr-App MG-β banked identity): the fold note opens by contradicting its own prior pass's clearance, then proves the deletion exhaustively across src+dist. An axis that opens by killing its predecessor's finding is doing L-18 correctly.
7. **C's §0.1 revision discipline** (reader-DU): first pass folded whole, ids preserved, corrections marked inline, four self-kills including K-4 — which demolished its OWN prior blocker's mechanism on receipts and re-filed the identity at the right altitude. Re-verified by reader-LC chunk-by-chunk.
8. **The producer voiced its own break before any consumer tripped it** (reader-LC; this seat read the block verbatim): Button.vue:126-146 is the correct posture for a breaking rename — loud in dev, exact migration named, non-fatal, stripped in production — and it re-prices 22 fourier files the census never enumerated.
9. **D's F-2 is a model killed-candidate** (root-consistency arithmetic, both breakpoints) — and the corpus then contained its own refutation of its own C-18. The cross-axis read did exactly what it exists to do; the C axis re-raising a hypothesis the D axis had killed is the anti-pattern (DISSENT 4).
   **Rescissions (this seat's)**: **D:S-2 RESCINDED-IN-PART** — "both consumers override correctly and differently" loses a leg: the equation-route override decorates an unreachable branch (m-13; my EquationView:213 read). **D:S-4 RESCINDED WHOLE** (K-2) — replaced by B-4, its exact inverse.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. B-2's exact composited ratios behind `glass-floating` / `paper-texture` / `.cartoon-card`, both themes — direction and mass static-certain, third digit live-only. Run axe with the coefficients layer OPEN (i-2).
2. The tooltip's own type: `text-muted-foreground` at fixed 10 px caps over the `glass-floating` backdrop-filter composite — WCAG 1.4.3 at 4.5:1 is not statically decidable; no static receipt covers this surface.
3. M-1's dev-warning multiplicity per session (path proven statically; count and re-fire behaviour on tooltip open/close are not).
4. Scroll chaining from the 300 px port into `<main overflow-y-auto>` — no `overscroll-behavior` anywhere in web/src.
5. Post-uplift `vue-tsc` behaviour on the fallthrough `variant="ghost"` — with the note that Button's DEV `console.error` gives a runtime signal regardless (K-4).
6. M-12's rendered magnitude: whether |c₀| in fact dominates on a real uploaded contour (LC's centred-figure counter-scenario: row 2 ≈ 41%, not the 2 px floor) — one live compute decides BLOCKER-vs-MAJOR pricing.
7. Whether `space-y-1`'s rhythm survives the Teleport comment anchors interleaved between rows (should hold — comments invisible to CSS — cheap to confirm, not worth guessing).
8. The 12→40 expand frame cost: 28 AnimatedDigit mounts + 12 simultaneous 500 ms width+colour transitions in one frame — both halves proven individually, the frame budget not.
9. Fira Code's real advance width at both roots (the K-12 residue; max label 4 glyphs either way).
10. m-12's rendered `w-16` overflow at pixel-scale amplitudes (the ignored `--digit-count` knob is proved; the clip is not).

## DISSENT — preserved, not ruled away

1. **Reader-DU's confirmation cells carried three killed limbs** — D-m16's Re/Im cross-file corroboration (R-7; this seat's own FrequencyGraph read), D-M10's "per B-1 unreachable by keyboard" (R-2), and D-M6's named failure scenario, where DU quoted the very gate (VV's `epicycleData || computing`) as a "sharpening" without noticing it killed the scenario it was confirming (R-1). Same fleet lesson as fr-BasisSelector DISSENT-1: a confirmation cell needs the same receipt discipline as an accusation cell, and quoting the refuting evidence inside a CONFIRMED verdict is the failure at its most visible.
2. **Severity dissents preserved** (R-5, R-6): reader-LC's MAJOR on B-4 (the tag honestly carries the subpath; the corpus was right-when-written) — overruled on the live-target argument but the caveat is load-bearing and rides the row; reader-DU's MAJOR on m-1/m-2 — re-opens if SS-13 shows the wrong copy or the phantom machinery prominently costly in ordinary use.
3. **Reader-LC's M-15 phrasing** ("runs on every store tick behind a closed panel") is overstated — the deep watcher fires on identity/deep change of the components array, and `markRaw`-replayed drafts are skipped by watch traversal; the forced-style-read-inside-the-loop limb is the certain one and is what M-15 books. A missed-find cell inherits the accusation cell's discipline too.
4. **The C axis re-raised a hypothesis the D axis had already killed** (C-18 vs F-2) and got it wrong by mixing roots — the cross-axis corpus contained its own refutation. Recorded as the method-lesson twin of superlative 9.
5. **This seat's scope notes**: the reka Primitive/Slot merge behaviour, ConfiguratorLayer's compiled `inert` emission, AnimatedDigit's dist class/feature output, Metric/Progress prop contracts, and the 4.0.0 typography utilities were verified by targeted grep/read against dist or adopted as twice-agreed reader extractions — not whole-chunk re-extractions; producer cites inside m-19 (bases_fitting.py) are adopted from the corpus unverified; the B-2/§A contrast sweep is reader-DU's instrument adopted after cross-consistency checks (two surfaces, two readers, ≤0.06 agreement), not re-run.

## Closing verdict

**ADJUDICATED — the pointer-only-physics / contrast / rank-indexed-hue BLOCKER triple stands (B-1 · B-2 · B-3) and gains a fourth: the `@mkbabb/glass-ui/animated-digit` import at `:19` is a build-time module-resolution failure at the producer HEAD the F.W1 uplift will actually land on — deleted in a commit that names this component as its relay target, uncensused, and affirmatively cleared by the corpus on citations into a file that no longer exists (B-4, → F.W1, re-pin before sizing). 47 register rows confirmed (4 BLOCKER · 16 MAJOR · 22 MINOR · 5 INFO); 4 identities FOLDED BY REFERENCE (peer graph · Button `variant` · `text-admin-label` · lucide — one booking each, forever); 15 kills banked, including the corpus's fabricated `RETIRED_FLOATING_ATTRS` provenance, the "silent" Button-break framing refuted by the producer's own DEV voice, the 501/default-50 arithmetic (true figures 1001/401/200), and three reader-DU confirmation cells that died under cross-examination; 10 missed finds banked after re-proof — the skip-delay tooltip cascade, the `getComputedStyle`-per-bar draw loop, the in-flight-equals-empty loading branch, the dead equation-route `emptyText`, the `expanded` latch, and the moving-producer method row among them. Routings are M-25 agglomeration input for the forming X·F specs — F.W1 owns the re-pin gate + B-4's replacement (with m-20/i-3 riding) + the four folded identities; F.W2 owns the ramp-to-value.js angle; F.W3/W4 owns the a11y/motion/state/semantics/format mass; F.W5–W8 owns the untyped-operation contract clause; the census-methodology rows go to SS-3/SS-4 with NO-WAVE-OWNER; nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
