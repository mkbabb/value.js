# PaginationBar — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (the incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **HEAD**: `983967da` · branch `tranche-u` · **Corpus**: 15 accusations (C-1..C-5, D-1..D-5, L-1..L-5) against `demo/palettes/browser/admin/PaginationBar.vue` (48 lines, SHA-256 `3006c9b9…` per all three axis receipts; anchors re-verified at this HEAD).
**Re-readers adjudicated**: design-and-usage (`L-18-PaginationBar-design-and-usage`) · library-and-code (slug `PaginationBar`). Both were attacked before either was adopted; every verdict-level disagreement (C-1, C-3, C-4 severity, L-2, D-3 lens, D-5/L-1 severity, the focus-loss severity split) was re-derived against the tree by this seat with its own instruments.
**SUPERSESSION (recorded, not hidden)**: this path previously held a same-titled record (2026-08-04, HEAD `95cfbb15`) whose reader-position cells do not match the re-reader returns delivered to this seat — it had design-and-usage CONFIRMING D-1 at MINOR (with a "~60px pop-in" citation appearing in neither return), library-and-code at D-1 MAJOR where that reader in fact KILLED D-1, and both readers at BLOCKER on the stranding trap where both actually graded MAJOR. The returns adjudicated here are the ones delivered; the prior version's *tree-facts* that this seat re-verified are carried below as continuity riders so nothing consumed by `X/COHESION.md:109` is lost — including its 5-row NO-WAVE-OWNER envelope, which this record preserves exactly.
**Provenance law (binding on the whole corpus)**: all three challenge files are **REPORT-AUTHORED** — `registry/HYDRATION-LEDGER.md:264-266`, payload 0, closure-manifest-2026-08-03; per the ledger header (:5-8) these are "source-review findings — NOT challenge coverage." No challenge seat ever ran against this component; the three SOURCE-RED stamps are unwitnessed prose. Both readers knew this; the kills below are kills of descriptions, and every confirmation stands only on re-derived mechanism.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony)**: whole-file reads of PaginationBar.vue (48 L), useAdminFlagged.ts (150 L), useAdminAudit.ts (103 L), AdminAuditPanel.vue (120 L), AdminFlaggedPanel.vue (153 L), `demo/ui/button/index.ts` (1 line), all three axis files; producer bytes (installed glass-ui **7.0.0**, my own node read of its package.json): `dist/components/button/Button.vue.d.ts` whole prop union (`emphasis/tone/size/iconOnly/loading/type/disabled/class` — **no `variant`**; `ButtonEmphasis = primary|secondary|quiet|text`), compiled `dist/button-Bu9F4uU6.js` (`grep -c variant` → **0**; `emphasis: { default: "secondary" }`; guard `m = loading || disabled===true || disabled==="true"` with `preventDefault()` + `stopImmediatePropagation()`; native branch `disabled: h.value ? m.value : void 0`, `aria-disabled` emitted **only** on the non-native branch; `"data-loading"`/`"aria-busy"` off `loading`; `"data-control-target": iconOnly ? "" : void 0`), `dist/components/button/styles.css` (`.button:disabled, .button[aria-disabled="true"]` styled as a **pair**; `pointer-events:none` reserved to `:disabled` alone; `[data-icon-only]` square geometry off `--button-size`; `.button > svg:not([class*="size-"])` → `--ui-glyph`; one PRM guard), `dist/styles/tokens/sizing.css` (`--control-h-sm: max(calc(2.25rem*var(--ui-scale)), var(--control-floor))`, `--ui-glyph: calc(1rem*var(--ui-scale))`), coarse tokens (`--ui-scale: 1.5`, `--control-floor: 2.75rem`), `dist/styles/typography/semantic.css` `@utility text-caption` (**`font-style: italic`**; demo override census → 0), exports map (`./pager-dots` IS first-class — see DISSENT #2). Censuses: `aria-live` in demo = **4 sites exactly** (ComponentSliders :84 `off`, ErrorBoundary :6 comment + :20 `assertive`, PaginationBar :17 — both readers' census exact); `variant="` in demo = **103** vs ` emphasis="` = **2**; `vueCompilerOptions` across tsconfigs = **0**; `PaginationBar` consumers = exactly 2 panels + 2 imports; pagination terms over test/ + e2e/ = **0** (the admin battery — walk/populated/a11y/flows — exists and never clicks the bar); `usePalettePorts.ts` singleton truth = audit :72, flagged :73, `provide(ADMIN_PORT_KEY…)` :245; in-repo cure precedent `useBrowsePalettes.ts:98` = `if (!hasMore.value || nextCursor.value == null || loadingMore.value) return;` **plus** a `loadGeneration` token at :112 — the busy guard AND the sequencing cure, both already shipped one directory over. Routing authorities read: `X/COHESION.md` §4 (:109 = this file's pointer row), `X/waves/W8.md` :158-170 (same-touch: "delete the dead `variant`… attributes" on this file) + :305-320 (G-9 **null-DELTA** contract; `PaginationBar.vue:8,22` named RED), `X/waves/W7.md` :41/:75/:182/:309-311 (strictTemplates), `X/waves/W4.md` :20/:30/:380 (A3/CC-045), sibling adjudications `AdminFlaggedPanel.md` (AF-5, AF-27, AF-28, AF-30, AF-40, AF-53) and `AdminAuditPanel.md` (AAP-2, AAP-11, AAP-12, AAP-19, AAP-20, AAP-21, AAP-22) — every cross-book below hangs on a row this seat re-read.
**Motion/PRM law**: `audit/codex-provenance/motion-quarantine.md:274` lists `PaginationBar | C · D · L` in the no-motion-content table — **vacuously cleared; no M-xx row exists to carry and none is carried.** The component authors zero motion; the producer sheet carries the PRM guard.

## Scoreboard

**8 of 15 survive in some part (0 BLOCKER · 1 MAJOR · 6 MINOR · 1 RESCOPED-INFO; two pairs folded to one identity each) · 7 KILLED (4 unanimous kills sustained; 3 split rows ruled) · 11 missed rows adjudicated (3 MAJOR · 6 MINOR · 2 INFO), of which 5 are cross-books into standing sibling identities (AF-28/AF-53/AF-27, AAP-2, AAP-11, AF-30/AAP-12, AF-40) and 6 are NEW to the program's record. NO-WAVE-OWNER envelope: 5 rows (C-4, D-3, M3, M7, M8) — unchanged against `COHESION.md:109`.**

The corpus's headline is dead and its verdict survives anyway: all three axes stamped SOURCE-RED on a *prop-contradiction* theory ("carries pagination truth as four unrelated scalars") that both readers independently refuted and this seat re-refuted — the two live consumers derive all four props from one computed graph (`useAdminFlagged.ts:55-57`, `useAdminAudit.ts:44-46`, byte-identical) and no third consumer exists (`admin/index.ts` exports exactly the five panels; my grep = 2 template sites + 2 imports). Every terminal defect proven against this surface lives **one layer up** — mutators that never clamp (`dismiss`/`deletePalette`, flagged :86-87/:98-99), never guard (`nextPage` :105-110 / audit :73-78 never consult the `loading` refs their own files export), never sequence (no token, no AbortSignal — `getAuditLog` verified signature-bare) — or **one layer down**, in producer vocabulary the call sites no longer speak (`variant` dead at 7.0.0, `iconOnly`/`loading` unused, italic `text-caption` on a numeric readout). Weight-bearing survivor: the **AF-28 stranding trap** (missed M1), found independently by both readers with correct mechanics, graded MAJOR by both — concurring with the standing AF-28 booking, and *not* re-escalated (the superseded record's BLOCKER rested on grades these readers did not give).

## Ruled disagreements (this seat's actual work)

| row | design-and-usage | library-and-code | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-1** | CONFIRMED-IN-PART at INFO (fourth limb reachable; three limbs refuted) | KILLED as authored ("right symptom from the wrong cause"), mechanism re-filed under missed | **RESCOPED → INFO** — the surviving limb is real and stays on this row's id | Both readers agree on every fact; the split is bookkeeping. `page > pageCount` IS reachable — my arithmetic re-run: total 41→40 by `deletePalette` (flagged :98-99, no clamp, no refetch) with page=3 → pageCount=2 → the bar renders the literal "Page 3 of 2" (:18), Prev enabled, and `v-if="pageCount > 1"` (:3) still passes. But the axis's stated mechanism (contradictory *inputs* at the boundary) is dead — the cause is a parent mutator omitting a clamp. Per the ColorSpaceSelector precedent, right-symptom/wrong-mechanism = RESCOPE, not kill: the row survives at INFO, riding M1's X-W7 clamp cure; its three other limbs (page=0/negative/non-integer) are REFUTED by construction (seeded 1, mutated ±1 behind guards, reset-to-1 only at AdminAuditPanel.vue:114). |
| **C-3** | KILLED (destination payload would be discarded at every zero-arity call site and would encode a stale view) | CONFIRMED inside a merged "C-3+C-4+D-2" row (MAJOR) | **KILLED — design reader wins; the merge is unpicked** | The library reader's proof for its merged row is entirely C-4/D-2 substance (missing busy guard, missing `loading` prop, no sequencing); not one sentence of it establishes that the *events* should carry a destination. My reads: handlers bound as bare refs (`@prev="audit.prevPage"`, panels :88-89/:131-132), `nextPage()`/`prevPage()` zero-arity, parents re-read their own authoritative `hasNext`/`hasPrev` refs inside the handler — a child-carried destination is structurally inert AND would manufacture the stale-view race it purports to cure. Attribution corrected: the merged row's evidence is credited to C-4/D-2 below; C-3 dies. |
| **C-4 severity** | MINOR | MAJOR (merged row) | **MAJOR** | Two compounding consequences, both statically certain: (a) double-activation inside the first fetch's latency passes the `hasNext` re-check (page already incremented, pageCount unchanged) → `page` advances TWICE on two clicks — the user skips a page; (b) two in-flight loads, `entries.value = res.data` unconditional on settle (audit :61-62, flagged :69-70), first `finally` clears the one shared `loading` for both — last-writer-wins can put page N-1's rows under a "Page N" label until the next load. The repo itself grades this class of guard as mandatory: `useBrowsePalettes.ts:98` ships the exact one-line cure plus a generation token (:112). Design's narrower window-width hedge is preserved in UNPROVEN #1. |
| **L-2** | "CONFIRMED as a type-design observation only — INFO, not a defect" | KILLED | **KILLED — the library reader wins; the observation survives as prose, not as a row** | A row that its own author says is "not a defect" is a note. The widened 4-scalar contract admits no live contradiction (both consumers derive from one graph; no third consumer can exist — hardened barrel verified), the one reachable contradiction (C-1's limb) is a parent mutation bug an ADT would faithfully transport, and the prescribed `PaginationState` union is the invented-shared-abstraction move `feedback_kiss_no_contrivance` bans. Latent-width note recorded here; no roster row. |
| **D-3 lens** | CONFIRMED MINOR (unbounded append-only ledger; 24 sequential round-trips to the oldest of 500) | UNPROVEN — "outside my library/code lens… leaving to the D axis; no defect asserted" | **CONFIRMED MINOR — the design axis owns it; abstention is not dissent** | Facts verified: two commands + a label, no branch on pageCount (:7-29), `pageSize = 20` fixed (audit :38, flagged :51), audit total append-only. Mitigation the design reader itself supplied and I verified: both toolbars print the labeled total (audit :26-28 with correct pluralisation, flagged :5-7), so magnitude is legible while position is not. The *paradigm* half (offset-vs-cursor) is already adjudicated — **AAP-22, NO-WAVE-OWNER** — cross-booked, never re-booked. |
| **D-5/L-1 severity** | MINOR (hierarchy inversion; hedge discharged to definite) | MAJOR (dead intent + invalid DOM attr + fleet scope) | **MINOR at this component; the fleet identity keeps its own grade** | Scope law (ColorSpaceSelector addendum §1): the count lives repo-wide — my census **103 `variant="` vs 2 `emphasis="`** — and the fleet row is already claimed (X-W8 G-9 names `PaginationBar.vue:8,22` as RED inputs; `W8.md:313`). The component-local instance is cosmetic: two arrows render at the producer default `emphasis="secondary"` (compiled default verified) instead of the authored outline register — functional, admin-only, MINOR. **Routing nuance this seat adds**: W8's G-9 is a *null-DELTA* gate ("the only legal delta is the attribute's absence from the DOM", W8:317-319) — deleting the dead attr is proven paint-neutral, which means the *register decision* (should these be `emphasis="quiet"`?) is deliberately NOT cured by W8. That limb goes to the visual audit (UNPROVEN #5) and the X-W10 canon — G-9 green must never be read as "D-5 cured" (DISSENT #6). |
| **focus-loss severity (M3)** | MAJOR ("structurally unfixable through the producer's current Button API") | MINOR ("the fix is available in-producer") | **MINOR — with the design reader's premise corrected** | The severity hung on curability, and both readers got the cure half-wrong. My dist reads settle it: `aria-disabled` is emitted only on the non-native branch (compiled `!h.value && m.value`), so no *prop* reaches it with default `as="button"` — design is right there. But Button declares no `inheritAttrs:false`, attrs fall through to the root, and the stylesheet styles `.button[aria-disabled="true"]` as a full pair with `:disabled` — so the consumer cure compiles today: omit `:disabled`, pass `:aria-disabled` as a fallthrough attr, and the activation is already inert because the composables' own `hasNext`/`hasPrev` guards no-op the handler. Deterministic focus-to-`<body>` at every terminal-page keyboard activation is real (blur-on-disable is specified behavior); one Tab recovers it, the surface is admin-only, and the cheap cure exists — MINOR, with design's MAJOR preserved (DISSENT #4). |

## Verdict register — the original 15

Line anchors are HEAD `983967da`; re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, never VERIFIED** — nothing here stamps a wave.

- **C-1 — RESCOPED (INFO)** — ruled above. Surviving limb ("Page 3 of 2" rendered verbatim) rides M1's X-W7 reload-with-clamp cure; three limbs refuted by construction; the "contradictory inputs" mechanism retired with rationale.
- **C-2 — KILLED** (both readers, unanimous; re-verified). `hasPrev = page > 1`, `hasNext = page < pageCount`, `pageCount = Math.max(1, Math.ceil(total/pageSize))` — byte-identical computeds at both and only call sites; the named contradictions ("Page 1 of 2 with Prev enabled") require `1 > 1`. The axis mistook a permissive prop signature for an unsound derivation.
- **C-3 — KILLED** — ruled above. A stateless 48-line leaf with zero-payload emits and a single writer cannot host a parent/child race; the prescribed cure is structurally inert and ownership-inverting.
- **C-4 — CONFIRMED (MAJOR)** — ruled above; D-2's composable arm co-locates here. The guard is missing where the state lives: neither `nextPage` consults the `loading` ref its own file declares, exports (flagged :138, audit :92), and never reads. **Disposition: NO-WAVE-OWNER** — `useAdminAudit.ts`/`useAdminFlagged.ts` sit in no X wave's file list (the AAP-11 precedent); booked to the COHESION §4 intake register, extending **AAP-11**'s identity to the flagged twin by name. One machine with M8's sequencing — whichever formation adopts AAP-11 takes both.
- **C-5 — CONFIRMED (MINOR), D-4 folded in (one identity).** The polite region (:17-19) mirrors `page`, mutated synchronously (flagged :107, audit :75) strictly before the async fetch; rows commit on settle (:69-70 / :61-62). The announcement always precedes the rows it names — reader-1's sharpening of D-4's "could" to "always" adopted; nothing announces the commit. **Disposition: X-W4 rider on the AAP-21 announcement machine** (announce on commit, not intent), the commit-signal arm flagged beside C-4's NO-WAVE-OWNER booking since it must originate in the composables.
- **D-1 — KILLED** (both readers, unanimous; re-verified — and INVERTED). The bar does NOT disappear on loading or error: `total` is never cleared in-flight (:51 audit / :62 flagged set only `loading`) and the catch writes only `loadError` (:64-67 / :72-75), so the bar persists, interactive, beside the skeleton and the error plate. Negative/zero totals never reach the guard (`Math.max(1, …)`). The surviving first-paint limb is occupied by the skeleton branch. The real defect is the exact opposite of the one alleged — re-homed as M2.
- **D-2 — CONFIRMED (MINOR at the bar).** Four props in, no busy input (:37-42); neither call site passes one; the producer documents `loading?: boolean` ("suppresses activation until it settles") and ships the full register (`data-loading`, `aria-busy`, `cursor: progress`, and the compiled preventDefault/stopImmediatePropagation guard — all verified in dist). A bespoke-vs-producer gap, not a missing capability. **Disposition: X-W7** (the AF-5 machine — "used zero times" — the `:loading` pass-through lands in the same panel touches).
- **D-3 — CONFIRMED (MINOR)** — ruled above. **Disposition: NO-WAVE-OWNER, cross-booked to AAP-22**; the affordance question (first/last/jump at a threshold) rides whichever formation adopts AAP-22's paradigm cure.
- **D-4 — folded into C-5** (one identity, counted confirmed).
- **D-5 — folded into L-1** (one identity, counted confirmed; the naming limb survives as superlative #2's evidence).
- **L-1 — CONFIRMED (MINOR), D-5 folded in** — ruled above. Dead-attribute limb → **X-W8 G-9** (this file's :8/:22 already named RED; the barrel edge dies in the same touch, `W8.md:164`); register limb → UNPROVEN #5 + X-W10 canon. The barrel itself is NOT carried as a defect (one-line producer seam, the repo's declared idiom — design reader's decline adopted).
- **L-2 — KILLED** — ruled above.
- **L-3 — KILLED** (design reader's refutation adopted; the library reader never defended it). Literally true (:44-47 `prev: []; next: []`), names no defect: the emitter owns no page, no query, no request — identity is a property of the fetch (AAP-11's), and threading it through a presentational bar inverts the panel's ownership design (`pm.audit`/`pm.flagged` as the single authority, usePalettePorts :72-73/:245).
- **L-4 — KILLED** (both readers, unanimous; re-verified by census). 4 `aria-live` sites in the demo; this is the only polite content region; the only paginated surface is this one. A single implementation cannot diverge from itself — the phase it picks is wrong (C-5, confirmed) but not inconsistent. **ASIDE for the next formation boundary**: the public browse wall's keyset load-more announces nothing to AT — a browse-surface gap, outside this corpus's subject.
- **L-5 — KILLED** (both readers, unanimous; re-verified). Premise false: ONE 48-line file, TWO importers, zero duplication — the demanded extraction already happened. The producer ships no data pager (`PagerDots` = a dot rail keyed on slide `count`/`pattern: "tabs"|"group"` — with the export-status correction in DISSENT #2). The homing residue (a generic control living in `admin/`) is already adjudicated as **AF-40 (NO-WAVE-OWNER)** — cross-referenced, not re-booked.

## Missed-defect roster — 11 rows, adjudicated

Cross-booked rows carry their prior id and add no new booking (anti-rename); NEW rows are this corpus's first record.

| id | sev | claim (compressed) | adjudication + route |
|---|---|---|---|
| **M1** (both readers, independently, with matching mechanics) | **MAJOR** | Emptying the last flagged page strands the operator: mutators decrement `total` without clamping `page` (flagged :86-87/:98-99, no refetch), `pageCount` collapses, the `v-if` (:3) unmounts the ONLY rescue control (Prev, whose `hasPrev = 2 > 1` is still true — a `hasPrev \|\| hasNext` guard would self-heal, design's derivation verified) *and the live region with it*, EmptyState lies "No flagged palettes." (:37-41) beside a toolbar reading "20 flagged" (:5-7), Refresh re-fetches offset `(2-1)*20 = 20` forever (:67), and the app-root singleton (usePalettePorts :73/:245) survives every pane switch and remount (onMounted :152 refetches the same dead offset). Arithmetic re-run by this seat: total 21→20 at page 2. Only a full reload recovers. | **= AF-28 (+ AF-53 navigation arm, + AF-27 server half). ONE identity → X-W7** (reload-with-clamp machine). Severity: both readers MAJOR; AF-28's standing grade MAJOR; this seat CONCURS — the superseded record's BLOCKER escalation is not carried (DISSENT #1). Reachability data-gated (≥21 flagged) — UNPROVEN #6 feeds the fixture. |
| **M2** (design M2 + library "D-2 second limb" — one identity) | **MAJOR** | The bar survives loading AND error: both `v-for`s carry no `v-else` (audit :59, flagged :44) and both bars sit outside the state chain entirely (audit :83, flagged :126), so mid-load the NEW page label paints over the OLD rows under skeletons, and after a failed load "The ledger is unreachable." renders beside a live "Page 2 of 5" with working arrows over a dead backend — clicking Next then increments `page` with no data and no rollback. Violates the panels' own codified law in the same files (audit :40-41, useAdminAudit :65 "never costume a dead backend…"). | **= AAP-2** (whose own text already names "the pager stays operable over the dead ledger"; two-member family incl. AdminFlaggedPanel — verified) **→ X-W7 BUILD** (terminal branch owns rows + pager + count). Cross-booked. |
| **M3** (design M3 MAJOR · library #3 MINOR) | MINOR | Activating the arrow that reaches the terminal page disables the focused element in the same tick (native `disabled`, compiled `disabled: h.value ? m.value : void 0`) — focus drops to `<body>`, deterministically, both directions; `.button:disabled { pointer-events: none }` additionally swallows the pointer follow-up. Severity ruled above with the cure-availability correction: a compiling consumer-side cure exists (fallthrough `aria-disabled` + already-guarded handlers + the stylesheet's styled pair). | **NEW. NO-WAVE-OWNER** — no X wave owns this file's disabled semantics; adjacent to but NOT inside X-W8's same-touch, whose G-9 null-DELTA contract a behavior change would breach. COHESION §4 intake. |
| **M4** (design) | MINOR | Bare `<div>` (:2-5): no `<nav>`/`role`/`aria-label`, no group semantics, and the live span (:17) carries no `id` so neither button can be described by the sentence between them — the pagination control is invisible to the AT rotor and Next announces name-only, positional context arriving only after activation. House discipline exists (App.vue's real landmarks) and the producer's own pager takes `ariaLabel` + a declared register. | **NEW. X-W4 rider on the AAP-21 machine** (AAP-21's own evidence cites this file as the house grammar next door; the `<nav>` arm is the coordinated edit in this file; both parents are W4 modify-list). |
| **M5** (design M5 + library #2 — one identity, first limb) | MINOR | `iconOnly` never passed though this is the producer's documented exact case (":6 W5-a11y" self-identifies the shape): `data-icon-only` square geometry and the `data-control-target` coarse hook are withheld (compiled `iconOnly ? "" : void 0` verified), so the arrows render as `sm` lozenges with `padding-inline`, off-register against the producer's 36×36 square. **Tap-target limb affirmatively NOT a defect** — both readers independently: coarse tokens (`--ui-scale: 1.5`, `--control-floor: 2.75rem`) clear 44px by another route; recorded so it is never re-raised. | **NEW at this site. FOLD → X-W4 A3/CC-045 machine** (the AAP-19 precedent: cure class = `size`+`iconOnly` consumer one-liner), with an explicit **coordination note**: X-W8's same-touch edits these exact attribute lists — sequence the two waves' touches; the iconOnly arm carries an INTENDED geometry delta and may not ride under W8's null-DELTA gate. |
| **M6** (design M6 + library #4 — one identity, second limb) | MINOR | `class="h-4 w-4"` (:14/:28) does not trip the producer's sanctioned `size-*` escape hatch, so `.button > svg:not([class*="size-"])` still matches and the winner falls to cascade-layer accident (utilities vs components; `@import "tailwindcss"` at foundation.css:1 — verified); under coarse pointer the producer intends 24px glyphs (`--ui-glyph`) while the pin holds 16px in a 54px control. Fleet-wide: ~20 `h-N w-N` icon sites, zero `size-N` (library census; idiom confirmed by my spot greps). The contract violation is static-certain; the painted consequence is layer-order-derived, unmeasured. | **NEW. X-W4 rider beside M5** (same file, same touch, same geometry machine); consequence limb → UNPROVEN #3. Fleet scope recorded for the formation boundary — this component is one instance of a demo-wide idiom. |
| **M7** (design) | **INFO** (design filed MINOR, self-flagged the downgrade; this seat takes it) | `justify-center` + natural-width label: a digit-count boundary (9→10) widens the sentence ~3.3px and slides each arrow ~1.65px outward mid-repeat-click. `tabular-nums` holds digit width, not count. A one-rung-down echo of DESIGN.md's card-lock law (:67-78 — tabular figures AND `ch` reservation; only the first is present), below the law's literal display-scale trigger. Cure: one `min-w-[Nch]`. | **NEW. NO-WAVE-OWNER** — candidate X-W10 canonical-design rider at the formation boundary. |
| **M8** (library, inside its merged row; unpicked here to its own identity) | **MAJOR** | No request identity anywhere on the surface: `entries.value = res.data` unconditional (audit :61-62; flagged :69-70), no generation counter, no AbortSignal (`getAuditLog` signature verified bare), first settle clears the shared `loading`, and the audit surface has FOUR overlapping entry points (onMounted :119, debounce :110-117, Refresh :30, pager). Last-writer-wins on an audit ledger presents the wrong 20 rows under the right page number. The cure pattern ships in-repo at useBrowsePalettes :98/:112 (guard + generation token). | **= AAP-11 → NO-WAVE-OWNER** (composables in no wave's file list). Cross-booked; this record extends the identity to `useAdminFlagged.ts` by name. One machine with C-4 — adopted together or not at all. |
| **M9** (library #5) | MINOR | The hard CI typecheck is structurally blind to the whole dead-prop family: `vueCompilerOptions` = 0 hits across tsconfigs (my grep), strictTemplates defaults off, so 103 stale `variant=` attrs pass vue-tsc green. This component's green gate is not evidence of health. | **ALREADY CLAIMED → X-W7 G3** (`W7.md:41/:75/:182/:309-311` turn on `strictTemplates` by name, born-RED with this exact grep). Cross-referenced, no new booking. |
| **M10** (library #6; design flagged the same fact in its residue) | MINOR (as this corpus's instance; the identity's grade lives at AF-30 MAJOR) | Zero automated coverage: no unit test touches the component or either composable, and the admin e2e battery (walk/populated/a11y/flows — exists, verified) never clicks pagination; the vitest program reaches demo code and `demo/test/` holds three suites, so the idiom is available — this is omission, not infrastructure. M1/M2/M8 shipped through this hole. | **= AF-30/AAP-12 vacuous-gate machine → X-W1** (fixture/CI half) **+ X-W7 rider** (behavioral battery: the trap, the double-click, the out-of-order case are its named RED cells). Cross-booked. |
| **M11** (library #7) | INFO | Colocation nit — the only defensible residue of killed L-5: a fully generic component homed in feature-specific `admin/` while its generic sibling EmptyState lives in `demo/shared/ui/`. Correctly withheld from the hardened barrel; two consumers both inside admin make the homing defensible under KISS. Move only on a third, non-admin consumer. | **= AF-40 → NO-WAVE-OWNER, already booked** at the sibling — cross-referenced, adds no new row to the envelope. |

## Superlatives — with provenance, so the good bones survive the cures

1. **`tabular-nums` on the page sentence (:17)** — deliberate, correct, uncommon: constant glyph advance under a changing numeral, applied at the caption rung where the DESIGN.md law does not even require it. Its one blind spot (digit count) is M7 — a refinement of a good instinct, not an absence of one.
2. **The icon-button a11y trio, complete and dated**: `aria-label` on the command (:11/:25), `aria-hidden` on the glyph (:14/:28 — the half everyone forgets; design verified it survives lucide's class rebuild to the rendered svg), and the provenance comment naming the wave that required it (:6 "W5-a11y"). Even CHALLENGE-D concedes the naming.
3. **The demo's ONLY polite navigational live region** (my 4-site census), with the right register — `polite` + `aria-atomic="true"`, an atomic sentence that never interrupts a moderator mid-row. The phase is wrong (C-5); the instinct is the rarest one in this tree.
4. **The derivation discipline that killed a third of the corpus**: `Math.max(1, Math.ceil(total/pageSize))` + two comparison computeds, byte-identical at both composables — one scalar pair, three views, no drift. The nine-token `Math.max(1, …)` floor alone pre-refuted C-1's negative limbs and D-1's invalid-totals limb.
5. **It already IS the abstraction the L axis demanded, correctly private**: one 48-line file, two importers, zero duplication, and `admin/index.ts` ("NAMED re-exports only, PI-6") deliberately withholds it. The same fact kills L-5 and evidences the cluster's encapsulation discipline.
6. **48 lines, zero state, zero lifecycle, zero style block, payload-less tuple emits** — a pure function of four props, vacuously clean on all three motion axes (quarantine :274). This purity is precisely what made C-2/C-3/L-2/L-3 refutable in one reading, and several of the corpus's prescriptions (PaginationState ADT, destination-bearing events) would have destroyed it to fix defects that live one layer up. The presentational split is the file's thesis, and it is correct.
7. **Plain prose in the plain register**: "Page {{ page }} of {{ pageCount }}" — no ornament on a moderation surface, matching the siblings' labeled machine readouts ("N entries", correctly pluralised).

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

1. **Out-of-order resolution in anger** (C-4/M8): the missing guard and missing identity are static-proven; whether real interleaving occurs against the live Hono/Mongo backend, and how wide the double-activation window is inside one Vue tick before `:disabled` lands, are network/frame questions. Do not carry "out-of-order responses" as a *demonstrated* failure without a probe.
2. **AT experience of the premature announcement** (C-5): DOM ordering proven (sync `page++` flushes the region before the async row write); whether NVDA/JAWS/VoiceOver voice it before the rows settle, and whether the polite queue coalesces, needs an AT probe.
3. **Coarse-pointer rendered geometry** (M5/M6): the 54px-capsule/16px-glyph figures are token-derived by hand (sizing.css + coarse block, verified) — a single getComputedStyle on coarse emulation settles the glyph freeze and the lozenge box.
4. **The `variant="outline"` attribute in the served DOM** (L-1's secondary limb): derivation sound (no `variant` prop, no `inheritAttrs:false`, Primitive hand-applies attrs); W8's own dist-CSS grep corroborates inertness. Secondary either way — the load-bearing limb (emphasis fallthrough) is proven from compiled dist.
5. **Whether the default `secondary` glass-capsule register actually reads heavier than the authored outline intent** (D-5's design limb): the static derivation says two blurred capsules flanking a muted caption; eyes on the served page decide, and X-W8's null-DELTA gate deliberately leaves this uncured (DISSENT #6).
6. **M1's data dependence**: the code path is unconditional; whether production has crossed 20 flagged palettes is not statically decidable. Data-residue for X-W7's fixture design (≥21 rows, delete on the last page).
7. **Recorded NOT-defects, so they are never re-raised**: caption contrast passes (design's token computation, 5.21:1 ≥ 4.5:1); coarse tap-target passes by token floor (both readers, independently); the one-line barrel is the house seam, not a stale axis.

## Continuity riders — carried from the superseded record, RE-VERIFIED by this seat (else dropped)

- **CR-1 (verified, carried)**: the page sentence wears `text-caption` = producer-declared **`font-style: italic`** proportional type (dist semantic.css, my read; zero demo override, my grep) while the panels' own in-source law two scroll-heights away declares "a machine string is a READOUT: Fira, never italic display type (W5-12 / F-9)" (AdminAuditPanel.vue:74-75). Neither current reader raised it; the fact is bytes. MINOR → **X-W10** (type canon).
- **CR-2 (verified, carried)**: the live region is conditionally *removed*, not hidden — :3's `v-if` wraps :17, so the 2→1 transition (the M1 moment) unmounts region and content together, and `AdminFlaggedPanel.md`'s AF-5 cell ("mounts one, hidden at pageCount≤1") is corrected: REMOVED, not hidden. The AF-5 kill that cell supported still stands. AT consequence rides UNPROVEN #2; the mount-vs-content split belongs to the AAP-21/X-W4 machine with M4.
- **CR-3 (verified, cross-note only)**: the 300ms filter debounce with no teardown (AdminAuditPanel.vue:110-117) is already booked — **AAP-20 → X-W7 BUILD** — no row owed here.
- Everything else in the superseded record that this seat could not ground in the delivered returns or its own instruments is NOT carried; the prior file's reader-position cells are superseded by this record (DISSENT #1).

## DISSENT — preserved, not ruled away

1. **The supersession itself**: the prior record at this path asserted reader positions (D-1 confirmed at MINOR/MAJOR by the two readers; both readers at BLOCKER on M1; a "~60px pop-in" evidence cell) that appear nowhere in the returns delivered to this seat — both delivered readers KILLED D-1 and graded M1 MAJOR. Whether the prior seat adjudicated an earlier, different pair of runs or inherited unwitnessed cells is not decidable from here; either way, per the epoch rule the delivered returns govern this record, and the divergence is itself recorded as evidence about the fleet (the ColorSpaceSelector citation-inheritance precedent). Its NO-WAVE-OWNER envelope (5 rows) and its verified tree-facts are preserved above so `COHESION.md:109` loses nothing.
2. **Library reader's factual cell corrected, kill unharmed**: "glass-ui 7.0.0 exports no Pagination component… PagerDots is an unexported internal chunk" — the exports map ships **`./pager-dots` first-class** (my node read). The kill of L-5 survives on the true ground both readers share: PagerDots is a carousel dot rail (`count`/`orientation`/`windowFit`/`pattern`), not a prev/next data pager.
3. **Design reader's cure-availability cell corrected, defect unharmed** (M3, ruled above): "the focus-preserving pattern is NOT reachable from the consumer" is false — fallthrough `aria-disabled` + the already-guarded handlers + the stylesheet's `.button[aria-disabled="true"]` pair compile and paint today. The MAJOR grade partially rested on that premise.
4. **Design reader's M3-MAJOR position preserved**: deterministic, every terminal-page keyboard activation, aggravated under M1 where the focused element leaves the DOM entirely. If X-W7's fixture work witnesses the compound live, the next formation may re-grade; the M1 booking carries the aggravation meanwhile.
5. **C-4 severity split preserved**: design graded MINOR on window-width uncertainty; ruled MAJOR on the statically-certain double-increment compound. UNPROVEN #1 is the live cell that would vindicate design's caution or this seat's grade.
6. **The G-9 trap, recorded for the wave**: X-W8's null-DELTA proof is the RIGHT gate for deleting inert attributes and simultaneously a guarantee that the authored outline intent stays unrestored. Three records now touch these two lines (this file, W8's G-9, the X-W10 canon candidate) — whichever wave lands last must re-read the other two, and a green G-9 is never citable as the cure of D-5's design limb.
7. **Unresolved lens philosophy on D-3** (design: worst-case user cost → MINOR-tending-MAJOR; library: defect-vs-decision taxonomy → abstention): ruled on the design axis's ownership with the library's abstention recorded as abstention, not dissent. AAP-22's eventual paradigm cure is where the grade gets re-litigated.

## Closing verdict

**ADJUDICATED: SOURCE-RED SUSTAINED AT THE EDGES, HEADLINE OVERTURNED — ON A CORPUS THAT WAS NEVER CHALLENGE-WITNESSED.** 8 of 15 rows survive in some part (1 MAJOR · 6 MINOR · 1 RESCOPED-INFO; C-5+D-4 and D-5+L-1 folded to one identity each) — none by the axes' own "four unrelated scalars" theory, which is dead on the tree; 7 killed with refutations sustained under this seat's pressure; 11 missed rows adjudicated (3 MAJOR · 6 MINOR · 2 INFO), 5 cross-books into standing identities (AF-28/AF-53/AF-27 · AAP-2 · AAP-11 · AF-30/AAP-12 · AF-40) and 6 NEW (M3, M4, M5, M6, M7, plus CR-1's canon rider). Routing: **X-W7 ×4 (M1, M2, D-2, M10-rider) · X-W8 ×1 (L-1/D-5 dead-attr limb, already RED-named) · X-W4 ×3 (C-5/AAP-21, M4, M5+M6) · X-W10 ×2 (CR-1; M7/D-5-register candidates) · X-W1 ×1 (M10 fixture half) · X-W7-G3 cross-ref (M9) · NO-WAVE-OWNER ×5 (C-4, D-3, M3, M7, M8) for the COHESION §4 intake register — envelope unchanged.** The component itself remains the cleanest 48 lines in its cluster: a pure presentational leaf whose every terminal defect was injected by its parents' composables or by vocabulary drift against its producer. The six words the next formation should carry are unchanged and re-earned: **clamp the page, gate the phase, speak the producer's tongue.** Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3; the superseded version of this file is reconstructible from git history and its verified facts are carried above.
