# PaginationBar — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (the incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **HEAD**: `95cfbb15` · branch `tranche-u` · **Corpus**: 15 accusations (C-1..C-5, D-1..D-5, L-1..L-5) against `demo/palettes/browser/admin/PaginationBar.vue` (48 lines, SHA-256 `3006c9b9…` per all three axis receipts). No r2 corpus exists for this component — the three axis files govern as read; both re-readers and this seat read the same bytes.
**Re-readers adjudicated**: design-and-usage (`L-18-PaginationBar-design-and-usage`) · library-and-code (slug `PaginationBar`). Both were attacked before either was adopted; every verdict-level disagreement (D-1 severity, D-3 severity, L-3 disposition, L-4 disposition, the M1 severity vs the AF-28 precedent) was re-derived against the tree by this seat with its own instruments.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony; no repro re-runs where source construction decides)**: full reads of PaginationBar.vue (whole, 48 L), useAdminFlagged.ts (whole, 150 L), useAdminAudit.ts (whole, 103 L), AdminAuditPanel.vue (whole, 120 L), AdminFlaggedPanel.vue (whole, 153 L), `demo/ui/button/index.ts` (1 line), all three axis files (whole); producer bytes (installed glass-ui **7.0.0**, my own `node -e` on its package.json): `dist/components/button/Button.vue.d.ts` whole prop union (`emphasis/tone/size/iconOnly/loading/type/disabled/class` — **no `variant`**), compiled `dist/button-*.js` (`grep -c variant` → **0**; `emphasis: { default: "secondary" }`; the `loading || p.disabled` guard with exactly one `preventDefault` + one `stopImmediatePropagation`; `"data-control-target": a.iconOnly ? "" : void 0`), `dist/components/button/styles.css` whole (selector set keys on `data-emphasis/data-tone/data-size/data-icon-only/data-loading`; `.button:disabled, .button[aria-disabled="true"]` styled as a pair, `pointer-events: none` reserved to `:disabled` alone; `[data-icon-only]` square geometry; `.button > svg:not([class*="size-"])` → `--ui-glyph`; the closing PRM guard), `dist/styles/typography/semantic.css` `@utility text-caption` (**`font-style: italic`**, no demo override — grep over `demo/styles/` → 0), `dist/styles/tokens/sizing.css` (`--control-h-sm: max(calc(2.25rem*var(--ui-scale)), var(--control-floor))`, `--ui-glyph: calc(1rem*var(--ui-scale))`), `dist/styles/tokens/light-dark.css` coarse block (`--ui-scale: 1.5`, `--control-floor: 2.75rem`); censuses run by this seat: `aria-live` in demo = **4 sites exactly** (ComponentSliders.vue:84 `off`, ErrorBoundary.vue:6 comment + :20 `assertive`, PaginationBar.vue:17 — the readers' census is exact), `variant="` in demo = **103** vs `emphasis="` = **2**, `vueCompilerOptions` in `tsconfig*.json` → **0 hits**, unit tests touching `useAdminAudit|useAdminFlagged|PaginationBar` → **0** (sole demo unit test = `test/demo/palettes/api/admin-palettes.test.ts`; the admin e2e battery exists — walk/populated/a11y/6 flows — and none clicks pagination; `pagination|nextPage|Next page` over e2e/ hits only the unrelated `browse-palettes.ts` fixture), `usePalettePorts.ts` instantiation truth = audit at **:72**, flagged at **:73**, `provide(ADMIN_PORT_KEY…)` at **:245**. Routing authorities read: `docs/tranches/X/waves/W1|W4|W7|W8|W10` heads, W4 gate A3 (:380), W7 strictTemplates (:41/:75/:182), W8 :164 + :313, `docs/tranches/X/COHESION.md` §4 intake register; sibling adjudications `registry/adjudicated/AdminAuditPanel.md` (AAP-2, AAP-7, AAP-11, AAP-19, AAP-20, AAP-21, AAP-22) and `AdminFlaggedPanel.md` (AF-5, AF-27, AF-28, AF-30, AF-40, AF-53) — the identity guards below hang on these.
**Motion/PRM law**: `audit/codex-provenance/motion-quarantine.md` lists `PaginationBar | C · D · L` under "no motion content" (re-read by this seat at the :270-278 table) — **vacuously cleared; no M-xx row exists to carry and none is carried**. The component authors zero motion; the producer's own sheet closes with `@media (prefers-reduced-motion: reduce) { .button { transition: none } }`.

## Scoreboard

**9 of 15 CONFIRMED (0 BLOCKER · 1 MAJOR · 8 MINOR, two pairs folded to one identity each) · 6 KILLED (both readers' four unanimous kills sustained; both split rows — L-3, L-4 — ruled KILLED) · 13 missed rows adjudicated (1 BLOCKER · 3 MAJOR · 8 MINOR · 1 INFO), of which 7 are cross-books of identities already adjudicated at the sibling panels (anti-rename: cross-booked, never re-booked) and 6 are NEW to the program's record.**

The corpus's headline is dead and its verdict survives anyway: all three axes stamped SOURCE-RED on a *prop-contradiction* theory (`carries pagination truth as four unrelated scalars`) that both readers independently refuted and this seat re-refuted — the two live consumers derive all four props from one computed graph and no third consumer can exist (`browser/admin/index.ts` exports exactly the five panels; verified). Every terminal defect proven against this surface lives **one layer up** — in `useAdminFlagged.ts`/`useAdminAudit.ts` mutators that never clamp, never guard, never sequence — or **one layer down**, in producer vocabulary the call sites no longer speak (`variant`, missing `iconOnly`, missing `loading`, italic `text-caption`). Blocker-weight survivor: the **AF-28 stranding trap** (missed-roster M1), which both readers found independently and which the AdminFlaggedPanel adjudication had already booked — this seat escalates its severity (see DISSENT #4).

## Ruled disagreements (this seat's actual work)

| row | design-and-usage | library-and-code | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-1** | CONFIRMED-IN-PART, MINOR (loading-conflation limb real; invalid-totals limb refuted; names the ~60px pop-in) | CONFIRMED, MAJOR — "only because the same v-if makes the deletion trap unrecoverable" | **MINOR — reader-1 wins; the escalation is refused as double-counting** | The v-if's role in the unrecoverable trap is the AF-28 identity (missed M1, BLOCKER, X-W7) — grading D-1 MAJOR *for that* books one mechanism twice under two ids. What D-1 owns standalone: first-paint absence → post-fetch insertion (total=0 → pageCount=1 → hidden; then pops in), mitigated by the panels' skeletons. Precision correction against reader-1: `Math.max(1, NaN)` = **NaN**, not 1 — the "floors it for every input" cell is wrong; reader-2's version (NaN pageCount → `NaN > 1` false → hidden) is the correct mechanism, same outcome. |
| **D-3** | CONFIRMED MAJOR (unbounded audit ledger; 49 sequential clicks + round-trips to entry #1000) | CONFIRMED INFO (axis self-hedged; design-decision gap, not defect) | **MINOR — between the seats, with reasons** | Both facts verified: three children only (PaginationBar.vue:1-31); `pageSize = 20` fixed (useAdminAudit.ts:38) against an append-only `total` (:62). Not MAJOR: no state is corrupted, the surface is admin-only, and the two filters (:41-42, applied :56-57) are real narrowing tools. Not INFO: the growth vector is live, not hypothetical — reader-1's application split (right for the drainable flagged queue, wrong for the ever-growing ledger) is adopted. The *paradigm* half is already adjudicated: **AAP-22** (offset-where-cursor-is-bound, NO-WAVE-OWNER) — cross-booked, not re-booked. |
| **L-3** | KILLED (destination limb refuted — the payload-less emit is the more race-resistant shape; identity limb misfiled) | CONFIRMED at INFO (the FACT is exact; consequence separately refuted) | **KILLED — reader-1 wins** | A true fact with a refuted consequence is not a finding; it is a description. The empty emits are this component's best property (see superlative #6): the parents re-read `hasNext.value`/`hasPrev.value` inside the handler (useAdminFlagged.ts:106/:113, useAdminAudit.ts:74/:81), so a child-carried destination would encode a possibly-stale view and *manufacture* the race. The real request-identity gap is **AAP-11**'s (missed M8, cross-booked). Recording residue-INFO rows for consequence-free facts inflates rosters — the kill is clean. |
| **L-4** | CONFIRMED WEAKLY at INFO (the browse wall announces at NO phase → policy inconsistency) | KILLED ("no referent" — no second polite live region exists anywhere) | **KILLED — reader-2 wins** | My census: 4 `aria-live` sites in the demo, PaginationBar.vue:17 the only polite content region. The claim as authored ("other paginated surfaces **can** announce at a different phase") is a hypothetical with zero live instances; the one real fact it gestures at — the browse wall's keyset load-more announces nothing (useBrowsePalettes.ts) — is a *browse-surface* defect, out of this corpus's subject, and reader-1 itself conceded the implied one-shared-sentence cure over-unifies across paradigms. The browse-wall silence is recorded as an ASIDE for the next formation boundary, not as a row. |
| **D-2 vs C-4 anatomy** | kept separate (D-2 MINOR at the boundary; C-4 MAJOR in the composables) | merged (one busy-gating defect) | **KEPT SEPARATE — reader-1 wins** | The cure sites differ: C-4's guard is a composable edit (`if (loading.value) return` — the exact shape the sibling already ships at useBrowsePalettes.ts:98, reader-1's decisive precedent); D-2's cure is a prop pass-through (`:loading` from the exported-but-unread `loading` refs, useAdminAudit.ts:92 / useAdminFlagged.ts:138) into the producer's documented suppress-until-settled guard (compiled: `loading || disabled` → preventDefault + stopImmediatePropagation, verified in dist by this seat). One family, two anatomical rows, cross-referenced. |
| **M1 severity** | BLOCKER | BLOCKER | **BLOCKER at this altitude — recorded as an ESCALATION against AF-28's MAJOR** | See DISSENT #4. The identity is AF-28's (one identity, X-W7); the grade in this record is this seat's. |

## Verdict register — the original 15

Line anchors are HEAD `95cfbb15`; re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, never VERIFIED** — nothing here stamps a wave.

- **L-1 — CONFIRMED (MINOR), D-5 folded in (one identity).** `variant="outline"` at PaginationBar.vue:8/:22 is not a prop of glass Button 7.0.0 — verified by this seat at the d.ts (no `variant`), the compiled bundle (`grep -c variant` = 0), and the stylesheet selector set; the attribute falls through inert and both buttons render at the producer default `emphasis:"secondary"` (compiled default read by this seat), not the authored outline register. D-5's naming limb is also verified correct (`aria-label` :11/:25, `aria-hidden` :14/:28). Scope law (ColorSpaceSelector addendum §1): the count lives repo-wide — **103 `variant="` sites vs 2 `emphasis="`** (my census) — and the class identity is already adjudicated as **AAP-7 / the AdminUsersPanel L-3 dead-variant family**. X-W8 already names this file's two sites as RED inputs (`W8.md:313`) and claims the file in the barrel touch (`W8.md:164`). **Disposition: FOLD → X-W8** (CC-081 limbs; cross-booked, not re-booked).
- **C-1 — CONFIRMED IN PART (MINOR).** One limb live: `page > pageCount` is reachable because `dismiss`/`deletePalette` decrement `total` (useAdminFlagged.ts:87/:99) with no clamp and no reload — total 41 → 40 on page 3 renders the literal "Page 3 of 2" (:18) with Prev enabled. Three limbs REFUTED by both readers and re-verified: `page=0`/negative/non-integer are unreachable (seeded 1, guarded ±1, reset-to-1 only at AdminAuditPanel.vue:114; no third consumer exists — `admin/index.ts` verified). The axis's "contradictory inputs" mechanism is dead; the reachable path is a mutator omitting a clamp, whose worst form is M1. **Disposition: X-W7** — dies in the AF-27/AF-28 reload-with-clamp machine.
- **C-2 — KILLED** (both readers, unanimous; re-verified). The flags ARE derived, byte-identically, at both — and only — call sites (useAdminFlagged.ts:55-57, useAdminAudit.ts:44-46), passed from one synchronous computed graph (AdminAuditPanel.vue:83-90, AdminFlaggedPanel.vue:126-133); the named symptom is unreachable. Survives only as unexercised type-level looseness in a private, unexported component.
- **C-3 — KILLED** (both, unanimous; re-verified). Zero-payload emits + zero child state (48 L, no ref/computed/watcher — whole-file read) + parents that re-read their own authoritative refs at call time. The prescribed cure (carry the destination) would *create* the race it alleges. Reader-2's synchronous-dispatch mechanism adopted as corroboration.
- **C-4 — CONFIRMED (MAJOR), mislocated by the axis and relocated by both readers.** The child cannot guard what it does not own; the guard is missing in the composables: `nextPage()` at useAdminFlagged.ts:105-110 / useAdminAudit.ts:73-78 never consults the `loading` ref each file declares and exports. On pageCount≥3, a second activation before nextTick's `:disabled` passes both `hasNext` re-checks and issues overlapping loads; both `finally` blocks clear one shared boolean. In-repo precedent for the one-line cure: useBrowsePalettes.ts:98 already guards on `loadingMore`. The producer's own suppress-until-settled guard (Button `loading`) sits unused. Identity adjacency: harmful *because* of AAP-11's missing sequencing (M8). **Disposition: NO-WAVE-OWNER** — `useAdminAudit.ts`/`useAdminFlagged.ts` sit in no X wave's file list (the AAP-11 precedent); booked to the COHESION §4 intake register, extending AAP-11's audit-only booking to the flagged twin.
- **C-5 — CONFIRMED (MINOR), D-4 folded in (one identity).** The polite region (:17-19) is bound to `page`, which mutates synchronously *before* the fetch (useAdminFlagged.ts:107→108, useAdminAudit.ts:75→76); rows commit only on settle (:69-70 / :61-62). A screen-reader user hears the new page number, then silence, then unannounced row replacement. Reader-1's sharpening adopted: not "could" — always. **Disposition: X-W4 rider on the AAP-21 announcement machine** (announce on commit, not on intent), the composable arm flagged alongside C-4's NO-WAVE-OWNER booking since the commit signal must originate there.
- **D-1 — CONFIRMED IN PART (MINOR)** — ruled above. Standalone content: first-load absence/pop-in conflation, mitigated by the panels' skeletons; invalid-totals limb refuted (with reader-1's NaN cell corrected). The v-if's trap role belongs to M1/AF-28; the complete-cure spec is AAP-2's ("the terminal branch must own rows + pager + count"). **Disposition: X-W7** (rides AAP-2's cure; cross-booked).
- **D-2 — CONFIRMED (MINOR)** — ruled above. Four props in, no busy input; neither call site passes one; both composables export `loading` unread; the producer documents `loading?: boolean` precisely for this and ships `[data-loading] { cursor: progress }`. AF-5 already recorded "used zero times" for this family. **Disposition: X-W7** (with AF-5's machine; the `:loading` pass-through lands in the same panel touches).
- **D-3 — CONFIRMED (MINOR)** — ruled above. **Disposition: NO-WAVE-OWNER, cross-booked to AAP-22** (offset-vs-cursor is the adjudicated paradigm row; the affordance question — first/last/jump at a threshold — rides whichever formation adopts AAP-22).
- **D-4 — folded into C-5** (one identity, counted confirmed).
- **D-5 — folded into L-1** (one identity, counted confirmed; the naming limb survives as superlative #2's evidence).
- **L-2 — KILLED** (both, unanimous; re-verified). Same evidence as C-2 plus the category error: a private twice-consumed child is not a library boundary, and the prescribed `PaginationState` ADT would pack/unpack fields the parents already hold as refs while fixing nothing this corpus proved — every proven defect lives in the composables, not the prop shape.
- **L-3 — KILLED** — ruled above.
- **L-4 — KILLED** — ruled above. ASIDE for the next formation: the public browse wall's load-more announces nothing to AT; the demo's only navigational live region is this component's.
- **L-5 — KILLED** (both, unanimous; re-verified). The premise is false on fact: ONE file, TWO importers, zero per-panel duplication — the demanded extraction already happened; the other three admin panels paginate nothing; glass-ui 7.0.0 exports no pager (PagerDots is an unexported internal chunk serving carousels). The *homing* residue (a generic control living in `admin/`) is already adjudicated as **AF-40 (NO-WAVE-OWNER)** — cross-referenced, not re-booked.

## Missed-defect roster — 13 rows, adjudicated

Cross-booked rows carry their prior id and add no new booking (anti-rename); NEW rows are this corpus's first record.

| id | sev | claim (compressed) | adjudication + route |
|---|---|---|---|
| **M1** (both readers) | **BLOCKER** | Emptying the last flagged page strands the operator: mutators decrement `total` without clamping `page` (useAdminFlagged.ts:86-87/:98-99), `pageCount` collapses, the `v-if` (:3) unmounts the ONLY navigation control + the live region, EmptyState lies "No flagged palettes." over N remaining (AdminFlaggedPanel.vue:37-41), Refresh re-fetches the stale offset forever (:67), and the app-root port (usePalettePorts.ts:73, provided :245 — line truth verified by this seat; reader-1's `:69` cite corrected) survives remount. | **= AF-28 (+AF-53 navigation arm, +AF-27 server half). ONE identity → X-W7** (reload-with-clamp machine). Both readers found it independently with correct mechanics; severity ESCALATED by this seat over AF-28's MAJOR — DISSENT #4. |
| **M2** (reader-1) | **MAJOR** | Native `:disabled` (:10/:24) drops keyboard focus to `<body>` at exactly the terminal page; the producer's stylesheet is authored for the focus-preserving alternative (`.button[aria-disabled="true"]` styled everywhere, `pointer-events:none` reserved to `:disabled` alone — verified in dist by this seat). Compounds with M1: the focused element can leave the DOM entirely. | **NEW. NO-WAVE-OWNER** (no X wave names this file's disabled semantics; adjacent to — but not inside — the X-W8 same-touch). Cure: `aria-disabled` + no-op guard, both arrows stay focusable and announced. |
| **M3** (reader-1 + reader-2 #6, merged) | MINOR | `iconOnly` never passed though it is the producer's exact case (square geometry + the `data-control-target` hit-target hook, compiled `a.iconOnly ? "" : void 0` — verified); `class="h-4 w-4"` (:14/:28) pins the glyphs against `--ui-glyph`, so under `pointer: coarse` the control grows to 54px while the chevron freezes at 16px. In-repo idiom precedent: 8 files already pass icon-only (my census). Tap target itself clears WCAG 2.5.8 — not a defect. | **NEW at this site** (AAP-19 named the cure class for the h-7 siblings, not these two buttons). **FOLD → X-W8 same-touch rider**: the `variant→emphasis` swap (AAP-7/W8:164) edits these exact two call sites; splitting `iconOnly` into another wave manufactures a double-touch. Height half already gated at X-W4 A3/CC-045 — cross-noted. |
| **M4** (reader-1) | MINOR | The control is a bare `<div>` (:2-5): no `<nav aria-label="Pagination">` landmark, no group name, no `aria-controls` — and the driven lists carry no id to point at (both panels open their row loops on unidentified divs). | **NEW. X-W4 rider on the AAP-21 machine** (container semantics; both parents are in W4's modify list, the `<nav>` arm is the coordinated edit in this file). |
| **M5** (reader-1) | MINOR | `justify-center` + natural-width label: crossing a digit boundary (9→10, 99→100 — live via D-3's unbounded ledger) widens the label and slides both arrows under a repeatedly-clicking pointer. `tabular-nums` holds digit *width*, not digit *count*. Cure: one `min-w` declaration. | **NEW. NO-WAVE-OWNER** (no wave owns this file's layout; a candidate X-W10 canonical-design rider at the formation boundary). |
| **M6** (reader-1) | MINOR | Three control heights in one rendered column: these arrows at the honest `sm` rung (36/54 coarse) vs the siblings' hand-clamped `h-7 px-2` 28px (AdminFlaggedPanel.vue:10/:90/:96, AdminAuditPanel.vue:30). PaginationBar is the CONFORMANT party; the repair belongs to the siblings. | **= AAP-19 → X-W4 gate A3/CC-045** (already cites AdminAuditPanel.vue:30 verbatim and both failure modes). Cross-booked; zero edits owed in this file. |
| **M7** (reader-2 #2) | **MAJOR** | Both panels render stale rows UNDER the skeletons — the `v-for` sits outside the v-if/v-else-if chain (AdminAuditPanel.vue:36/:42/:56 then :59; AdminFlaggedPanel.vue:16/:22/:37 then :44 — re-verified by this seat against both templates), so mid-load the bar reads the NEW page over the OLD rows. | **= AAP-2 (two-member family incl. AF-4/AF-53) → X-W7** (BUILD, complete-cure spec incl. pager + count ownership). Cross-booked. |
| **M8** (reader-2 #3) | **MAJOR** | No request generation/sequencing anywhere: `entries.value = res.data` unconditionally on settle (useAdminAudit.ts:61-62; flagged :69-70), first settle clears the shared `loading`, the filter debounce (AdminAuditPanel.vue:111-117) races the pager; last-writer-wins can put page-2 rows under a page-3 label. | **= AAP-11 → NO-WAVE-OWNER** (composables in no wave's file list). Cross-booked; this record extends the identity to `useAdminFlagged.ts` by name. C-4's guard and M8's sequencing are one machine — whichever formation adopts AAP-11 takes both. |
| **M9** (reader-2 #4) | MINOR | The live region is conditionally MOUNTED with its content (:3 wraps :17): it does not exist before first content, and the transitions that matter most (2→1 pages after deletion; 1→2 after refresh) remove/insert the region wholesale — the classic no-announcement pattern. Also a precision correction to a sibling record: `AdminFlaggedPanel.md:120` says the region "mounts unconditionally … hidden at pageCount≤1" — it is REMOVED, not hidden. | **NEW. X-W4 rider on the AAP-21 machine** (mount the region unconditionally; gate its *content*). AT-specific behavior stays in the UNPROVEN residue. |
| **M10** (reader-2 #5) | MINOR | "Page N of M" wears `text-caption` — producer-defined ITALIC proportional type (`font-style: italic` verified in dist semantic.css, no demo override) — while the panels' own in-source law says a machine string is a READOUT, "Fira, never italic display type (W5-12 / F-9)" (AdminAuditPanel.vue:74-75), and every sibling readout uses the mono register. | **NEW. X-W10** (type canon; the AdminFlaggedPanel X-map already routes type-canon rows there). |
| **M11** (reader-2 #7) | MINOR | The hard CI typecheck gate is structurally blind to the entire dead-prop family: no tsconfig declares `vueCompilerOptions` (my grep → 0 hits), vue-tsc defaults unknown-prop checking off, so `variant="outline"` passes green. | **ALREADY CLAIMED → X-W7 G3** (`W7.md:41/:75/:182` turn on `strictTemplates` by name). Cross-referenced; recorded here so this component's green gate is never cited as evidence of health. |
| **M12** (reader-2 #8) | MINOR | The 300ms filter timer (AdminAuditPanel.vue:110-117) has no teardown; a pending timer fires after unmount and mutates the app-lifetime port (`audit.page.value = 1` + a fetch). The watcher itself dies with the scope — only the already-scheduled timer leaks; one late fire, not a stream. | **= AAP-20 → X-W7** (BUILD; `watchDebounced` already a dependency). Cross-booked with this seat's scope-precision note. |
| **M13** (reader-2 #9) | INFO | Zero unit coverage for the component and both composables (verified); the admin e2e battery never clicks pagination (verified — no pagination term anywhere under e2e/smoke/admin). This is what made M1/M7/M8 shippable. | **= AF-30's vacuous-gate machine → X-W1** (fixture/CI half) **+ X-W7 rider** (behavioral battery: the trap, the double-click, the out-of-order case are its named RED cells). Cross-booked. |

## Superlatives — with provenance, so the good bones survive the cures

1. **`tabular-nums` on the page sentence (:17)** — an uncommon, deliberate, correct choice: constant glyph advance under a changing numeral. Its one blind spot (digit count) is M5 — a refinement of a good instinct, not an absence of one.
2. **The icon-button a11y trio is exactly right and self-documenting**: `aria-label` on the command (:11/:25), `aria-hidden` on the glyph (:14/:28), provenance comment naming the wave that required it (:6 "W5-a11y"). Majority idiom in the cluster, verified, not an accident.
3. **The demo's ONLY navigational live region** (my 4-site census). Whoever wrote :17 chose `polite` + `aria-atomic="true"` — the right pair for a short atomic sentence. Only the phase (C-5) and the mounting (M9) are wrong; the instinct is the rarest one in this tree.
4. **Cluster-privacy discipline observed**: `admin/index.ts` ("NAMED re-exports only (PI-6)") deliberately withholds PaginationBar — one shared child, two consumers, zero leaked surface. Simultaneously the fact that kills L-5 and the reason L's whole boundary frame never applied.
5. **Within its own panel it is the only Button site that respects the producer's size rung untouched** (`size="sm"`, no class override) while every sibling hand-clamps `h-7 px-2` (M6/AAP-19). On the one axis where the surface is inconsistent, this file is the conformant party.
6. **48 lines, zero refs, zero computeds, zero watchers, zero lifecycle, payload-less emits** — a pure function of four props. This is what made C-2/C-3/L-2/L-3 refutable in a single reading: a component that owns no state cannot race. Several of the corpus's prescriptions (PaginationState ADT, request identity on events) would have destroyed exactly this property to fix defects that live one layer up. The presentational split is the file's thesis, and it is correct.
7. **Motion hygiene clean by construction**: no authored motion; producer PRM guard verified in dist; motion-quarantine lists this subject as vacuously cleared on all three axes. No M-xx carried, none owed.
8. **The producer defends the consumer's omission**: Button defaults `type` to `"button"` when rendering a real button (compiled, verified) — the absent `type` attr at :7/:21 is a non-defect, recorded so no future reader files it.

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

1. **AT behavior of the v-if-inserted polite region** (both readers): whether NVDA/JAWS/VoiceOver announce a live region inserted together with its content, and whether the 1→2 pageCount transition announces at all. The ORDERING defect (C-5) and the EXISTENCE defect (M9) are source-proven and do not depend on this; only the exact per-AT behavior does.
2. **Served-cascade order for `h-4 w-4` vs `--ui-glyph`** (reader-1): whether Tailwind's `utilities` layer lands after glass-ui's `@layer components` in the built page (reasoned from `demo/styles/foundation.css` import order; unmeasured). M3 stands regardless via the absent `iconOnly`.
3. **Rendered pixels of the fallthrough buttons** (reader-1): the default `emphasis="secondary"` plate is proven from compiled dist; whether it accidentally reads as the intended bordered control needs eyes on the served page.
4. **Rendered geometry of the two chevron buttons at coarse pointer** (reader-2): the missing `data-control-target` hook is proven from dist bytes; computed sizes need a live page (probe parsimony honored — zero browser tooling this session).
5. **Frame-level reachability of the double-activation window** (reader-1): whether a human double-click lands inside one Vue tick before `:disabled` applies. The missing guard (C-4) and missing sequencing (M8) are proven statically.
6. **Data-dependence of the M1 trap** (reader-2): reachable only at total > pageSize (≥21 flagged). The code path is unconditional; whether production has crossed 20 flagged palettes is not statically decidable. (Data-residue, not visual — carried for the wave's fixture design.)

## DISSENT — preserved, not ruled away

1. **Reader-1's NaN cell** (`Math.max(1, NaN) = 1`) is arithmetically false — `Math.max` propagates NaN. Reader-2's mechanism was correct; the shared conclusion (bar hidden) survives either way. Recorded as a citation-precision event, weight-bearing on nothing.
2. **Reader-1's `usePalettePorts.ts:69` anchor** for the flagged singleton is off by four lines (truth: audit :72, flagged :73, provide :245 — my grep). Mechanism unharmed; recorded per the anchor-drift law.
3. **A sibling record's cell is corrected by this corpus**: `AdminFlaggedPanel.md:120` describes the live region as "mounts unconditionally … hidden at pageCount≤1"; the truth is v-if REMOVAL (M9). The AF-5 kill that cell supported still stands — the categorical "no live region" claim it refuted remains refuted — but the cell itself should not be cited forward.
4. **Severity split with the standing AF-28 booking**: the AdminFlaggedPanel seat graded the stranding trap MAJOR among its rows; both of this corpus's readers independently graded it BLOCKER, and this seat concurs at this altitude — a moderation queue that reports itself permanently empty over N remaining items, with every escape hatch (bar, refresh, remount) proven dead, is an operator-facing lie of the exact species the file's own comments forbid. One identity, one cure, one wave (X-W7); the grade disagreement is recorded here rather than silently harmonized. If the wave must triage, it should triage at BLOCKER.
5. **Reader-2's D-1 MAJOR** is refused above as double-counting, but its underlying instinct — that the v-if is the single point where three defects meet (D-1, M1, M9) — is preserved as cure guidance: whatever X-W7 does to the state chain, the bar's mount condition is the load-bearing line.
6. **Unresolved severity philosophy on D-3** (MAJOR vs INFO, ruled MINOR): reader-1 grades by worst-case user cost, reader-2 by defect-vs-decision taxonomy. The ruling is a compromise with stated reasons, not a synthesis; the next formation may re-grade when AAP-22's paradigm cure is scoped.

## Closing verdict

**ADJUDICATED: SOURCE-RED SUSTAINED, HEADLINE OVERTURNED.** 9 of 15 confirmed (1 MAJOR · 8 MINOR; two folds) — none by the corpus's own "four unrelated scalars" theory, which is dead on the tree; 6 killed with refutations sustained against both readers' pressure; 13 missed rows adjudicated (1 BLOCKER · 3 MAJOR · 8 MINOR · 1 INFO), 7 of them cross-books into standing identities (AF-28/AF-53, AAP-2, AAP-11, AAP-19, AAP-20, AF-30, X-W7-G3) and 6 NEW. Routing: **X-W7 ×6 · X-W8 ×2 · X-W4 ×3 · X-W10 ×1 · X-W1 ×1 (shared) · NO-WAVE-OWNER ×5** (C-4, D-3/AAP-22, M2, M5, M8/AAP-11 — for the COHESION §4 intake register). The component itself is the cleanest 48 lines in its cluster — a pure presentational leaf whose every terminal defect was injected by its parents' composables or by vocabulary drift against its producer — and the six-word summary the next formation should carry is: **clamp the page, gate the phase, speak the producer's tongue.** Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
