# UserSortMenu — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: adjudicator-F (incredulity seat over two hostile re-readers).
**Date**: 2026-08-06 · **HEAD**: `3487d73e` · branch `tranche-u` · **Subject**: `demo/palettes/browser/search/UserSortMenu.vue` (58 L; sha256 `de6cc257db85223fea80f45823f6df07af08b7bfc8d2e7f44bee3e92bfa1cf9a` re-computed by this seat, byte-identical to all three axes' pins) + its 1-consumer closure (AdminPane.vue → useAdminUsers.ts → api/admin-users.ts → AdminUsersPanel.vue; sibling SearchFilterBar.vue).
**Corpus (r2 governs — none exists)**: `audit/components/UserSortMenu/challenge-{C-implementation,D-design,L-library}.md` — all three **REPORT-AUTHORED** (`registry/HYDRATION-LEDGER.md:277-279`: source `closure-manifest-2026-08-03`, payloads **0**); per the ledger header these are "Source-review findings — NOT challenge coverage." No `-r2.md` exists for any axis; `AXIS-PROMOTIONS-2026-08-03.md` carries **0** UserSortMenu rows (my grep). Integrity re-verified by this seat: all three file hashes re-computed and byte-identical to the ledger (`4a7e023d…637ff` / `10df5f8c…f4f1` / `a16e5c4cc3…3d31e` — reader-1's prose receipt "a16e5c43…" is a transcription slip of its own note; the file matches the ledger).
**Motion-quarantine bar**: `codex-provenance/motion-quarantine.md:278` lists `UserSortMenu | C · D · L` under §7.4 (no motion content — vacuously cleared); §7.5 (:297-298) records this component's only two keyword hits as collisions ("transition" = emit type-safety; "scanning" = reading). No M-xx FALSE row constrains any claim here and none is carried.
**Readers adjudicated**: reader-1 (design-and-usage) · reader-2 (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement (L-1 grade, L-4 kill-vs-confirm, L-3 grade, reader-2's barrel-necrosis missed-find, both readers' "novel" rows) was re-derived against the tree by this seat with its own instruments.
**Arbiter's own instruments (all static, zero browser tooling per law)**: full read of UserSortMenu.vue (whole, 58 L) + `shasum -a 256`; useAdminUsers.ts:1-80; AdminPane.vue:1-120; api/admin-users.ts:1-45; AdminUsersPanel.vue field greps (`createdAt|lastSeenAt|totalUsers|paletteCount` — zero date hits, `totalUsers: number` at :219, rendered :9); types.ts PaginatedResponse `{data,total,limit,offset}` + User `{slug, createdAt, lastSeenAt?, status?, paletteCount?}`; SearchFilterBar.vue:1-40; browser/index.ts:1-40 (header verbatim) + top-barrel importer census (**0** direct / **13** sub-barrel) + `UserSortMenu` consumer census (exactly AdminPane.vue:86 + the two barrels; **0** hits in test/ e2e/); `userSortMode` census (10 hits, no watcher, no storage, no route); usePaletteWiring.ts:145-152 (the only other `loadAdminUsers` caller, guarded on empty); eslint.config.js:221-232 (G-DEMO-3b comment verbatim); `strictTemplates` grep across all tsconfigs → **0**; `@lucide/vue` demo-SFC census → **45** files; installed glass-ui 7.0.0: Button.vue.d.ts whole (`emphasis|tone|size|iconOnly|loading|type|disabled|class`; `ButtonEmphasis = "primary"|"secondary"|"quiet"|"text"` — no `variant`); dropdown-menu/styles.css (`--dropdown-text`, `--dropdown-text-secondary`, `cursor: default`, `.dropdown-menu__radio-dot`, `[data-indicator="start"]` gutter); the compiled `dropdown-menu-BlbnvMaZ.js` setups for DropdownMenuRadioGroup AND DropdownMenuLabel (both `inheritAttrs: !1` **with useAttrs-forwarding via mergeProps** — the US-9 cure decider; no `id`, `aria-labelledby` count **0** in the whole chunk); `dist/search.js` (`surface: { default: "glass" }`, `data-surface: e.surface`, `input-bar`/`input-bar-field` — the US-6 nesting decider); typography/semantic.css verbatim (`@utility text-small { font-family: var(--font-text); … }`, `@utility text-micro` declares no family — the D-4 kill decider); ledger and wave cross-reads for identity guards: DEFECT-LEDGER.md:23779-23790 (the 51-site `variant` family + its `emphasis="quiet"` proposed cure), :25830-25840 (`w-48` vs `min-w-menu`, 178 px measured), :26296-26310 (the sort-key-invisible row), :6283-6291 + :4009 (page-as-corpus/prune rows); AdminUsersPanel.md Δ-1/Δ-7/D-25 + MT-AU1 scope items (1)/(5)/(6) + G-16; X/waves/W7.md:170-200 (M-23 design-content law + X.W7.a strictTemplates) + :220-245 (W7.d/e files); X/waves/W8.md:155-170 (X.W8.b names `UserSortMenu.vue` in the same-touch sentence) + :305-320 (G-9 **names `UserSortMenu.vue:8 variant="ghost"` RED today** with the null-DELTA law); CARRY-CUT-LEDGER.md §1 (X-wave fold rows; MT-AU1 absent — sibling-docket wave); GenerateControls.md row 5 + G5 (ghost→quiet mapping); ColorSpaceSelector.md L-4 (demo/ui identity); PaletteColorStrip.md PCS-9 (area-seam identity).
**Environment disclosure**: no dev server touched; every ruling below is decidable from source + the installed dist. The corpus itself ran **0** runtime commands (ledger payloads 0), so no live cell exists anywhere in this docket to adopt — everything pixel- or AT-dependent is quarantined into the UNPROVEN residue.

## Scoreboard

**13 adjudicated rows: 2 MAJOR sustained (US-5, US-7) · 1 MAJOR-candidate pending live (US-6) · 7 MINOR · 2 INFO · 1 FOLD-to-home-docket (US-8, grade lives at AdminUsersPanel Δ-7) — 0 BLOCKER in-file. 8 kills sustained, including one reader missed-find killed by this seat (US-K8 barrel necrosis). 9 of the readers' 10 missed-findings adopted (all re-verified; five folded into sibling rows) + 2 rows of this seat's own (US-11, the axis clause both readers dropped; the US-6 cure collision).**

**Dispositions**: X-W8.b/G-9 ×2 FOLD (US-1, US-11) + ×2 same-touch named-adopt riders (US-9, US-12) · X-W7.a named-adopt ×1 (US-2) · **X-W10 ×5** (US-3, US-4, US-5, US-6, US-10) · MT-AU1 (AdminUsersPanel docket) ×1 FOLD (US-8) + ×2 named-adopt riders (US-7, US-13). Every named-adopt row degrades to **NO-WAVE-OWNER at execution** if the named wave does not adopt it explicitly (the PCS-1 precedent).

The headline of this docket is not a defect in the file — it is that **the axes indicted the healthiest 58 lines in the closure while every grave row lives in the seam above it** (the page-as-corpus class, already adjudicated at AdminUsersPanel) **or in the canon around it** (register/glyph/idiom rulings, all X-W10's). 5 of the axes' 14 rows are dead on re-derivation; the corpus's REPORT-AUTHORED thinness (0 runtime commands, no r2) shows exactly where the ledger header said it would.

## Ruled disagreements (the adjudication's actual work)

| # | Question | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-A | **L-1 grade** (dead `variant="ghost"`) | MAJOR (double-glass consequence) | MINOR (systemic, 19 ghost sites; still renders) | **SPLIT — both partly right, neither's booking survives whole.** Mechanical arm MINOR-as-FOLD (US-1); register arm split out as its own row (US-6), MAJOR-candidate pending live | The grade of the *class* lives at its home: DEFECT-LEDGER:23784 books the **51-site** `variant` family (UserSortMenu.vue:6 enumerated) and X-W8 G-9 names `UserSortMenu.vue:8` RED today — per the scope law a repo-wide class is never re-graded per docket. Reader-1's consequence chain is FACT (my own reads: Button d.ts has no `variant`; compiled defaults `emphasis:"secondary"` → `glass-wash glass-capsule`; SearchBar `surface:{default:"glass"}` and AdminPane passes no `surface` → the capsule provably nests inside a glass field) — but its *loudness* is undecidable without pixels, and the corpus has zero live cells. So the fact is confirmed, the MAJOR is conditional, and the row that carries it is US-6, not US-1. |
| R-B | **L-4**: killed vs confirmed | KILLED ("REFUTED as stated"; residual → its missed-2) | CONFIRMED MINOR (duplication real; registry cure over-built) | **RESCOPED — both readers' facts, neither's booking.** The axis's cure is REFUTED; the surviving facts split by identity into US-2 and US-5 | Both readers agree on every fact: the union is authored twice (UserSortMenu.vue:52, useAdminUsers.ts:26), cast twice (:20 `any`, :51), and 2 of the axis's 4 named registry consumers **do not exist** (my reads: listUsers sends only `limit/offset/q` — no sort parameter; `userSortMode` census shows no URL/storage hydration). A kill that then re-files the residue as a "missed finding" (reader-1) and a confirm that strikes the cure (reader-2) are the same verdict wearing two labels. One identity each: the type seam → US-2; the sibling divergence → US-5. |
| R-C | **L-3 grade** (option glyphs lack `aria-hidden`) | INFO (no accname contribution in mainstream engines) | MINOR (three bare svgs inside menuitemradio rows) | **INFO — reader-1 wins** | Reader-2's own UNPROVEN note concedes the deciding fact: whether any AT voices a role-less, title-less inline `<svg>` is engine-dependent and unwitnessed. A grade may not exceed its proven consequence. The within-file inconsistency (trigger glyph carries the attribute, :23/:27/:31 do not) is real and one-line-curable → US-12, same-touch rider. The import half of the axis sentence is killed (R-D). |
| R-D | **L-3a / direct lucide imports** | "not a defect — fleet idiom" | KILLED (45-SFC census; eslint silent on packages) | **KILLED — unanimous, re-verified** | My own census: **45** demo SFCs import `@lucide/vue` directly; demo/ui has no icon barrel; G-DEMO-3b's ban keys on raw `.vue` reaches only (comment read verbatim). → US-K6. |
| R-E | **Reader-2 missed-4: "the TOP-LEVEL SEAM is dead — no obeying call site"** | (not filed) | MINOR (0 importers of browser/index.ts; 13 sub-barrel reaches "bypass" it) | **KILLED BY THIS SEAT — the reader read the export list and not the contract** | The zero-direct-importer count is TRUE (my census). But browser/index.ts:5-6 states the contract verbatim: consumers reach the feature "through THIS seam **(or a sub-barrel it re-exports)**", and eslint.config.js:221-232's G-DEMO-3b comment repeats it ("the top-level index.ts **or a sub-barrel it re-exports**… only paths ending in a raw `.vue` are banned"). The header's PI-6 block even explains WHY the sub-barrel is the preferred reach (SFC scoped styles are side-effecting imports; named sub-barrel reaches tree-shake). The 13 sub-barrel imports are the documented design *working*, not rot routing around a corpse. Residual INFO fact (an aggregation layer with zero direct reaches) is the authored intent; the adjacent area-seam question is **already booked** at PaletteColorStrip PCS-9 (NO-WAVE-OWNER) — identity guard, not re-booked. → US-K8. |
| R-F | **Novelty of the gravest rows** | missed-1 "the finding the corpus most conspicuously missed" (invisible sort key) | missed-1/-2 "all three axes missed the real ordering-contract defect" (50-row page; total discarded) | **Both readers' facts CONFIRMED; both novelty claims CORRECTED — the program already owns every one of these rows** | Invisible sort key ≡ **DEFECT-LEDGER:26296-26310** (":36-46 sorts by … createdAt … the row renders slug + count only … A sort with no visible column is an unfalsifiable ordering" — same lines, same cure options). Page-as-corpus ≡ **AdminUsersPanel.md Δ-7** ("pagination/search/sort are theatre … sole call `listUsers(token, 50)`; `res.total` discarded; **sort reorders an arbitrary page**", gate G-16) and the total-discard limb ≡ **Δ-1/L-19 leg + D-25** ("the 'total' is itself a page"), all routed to wave **MT-AU1** (scope item 1: "roster becomes page-aware — `res.total` consumed, `q`/offset forwarded, the client-side filter dies"). True against the three axes, wrong against the ledger. Identity guard: US-7 and US-8 are FOLDs/riders on those homes — one identity each, for life, never re-graded here. |
| R-G | **The US-6 cure collision** (this seat's own derivation — neither reader saw it) | prescribed the quiet register implicitly | cited G-9 without noticing the conflict | **NAMED AND ROUTED — X-W10 must rule; the two standing cures are incompatible** | Three authorities claim `UserSortMenu.vue:8`: (a) **X-W8.b/G-9** — *delete* the dead attribute, with a committed null-DELTA pair proving **zero pixel change** (the register stays secondary-wash); (b) **DEFECT-LEDGER:23784's proposed cure** — `emphasis="quiet"` ("repo-wide the mapping is ghost→quiet"), which *changes* paint to the transparent register; (c) **V·MEGA-W-GEN-1 G5** (GenerateControls) — the same ghost→quiet migration at the ~48 non-pinned sites. (b)/(c) satisfied would FAIL (a)'s gate, and (a) satisfied preserves the possibly-wrong register forever. The tiebreak is in the X formation's own law: X-W7.md:170-173 — "No design content is authored here — the canonical design and motion law is X-W10." The register decision (quiet vs wash inside a glass field) is design content → X-W10 rules; X-W8's deletion may land first (mechanically safe either way); **if quiet wins, G-9's null-DELTA baseline must be re-stamped in the same commit** — otherwise the gate outlaws the cure. |

**Reader corrections of record**: (1) reader-1's hash transcription slip (`a16e5c43` for `a16e5c4c`) — cosmetic, its verification was real; (2) reader-1's US-9 mechanism implied a producer dependency ("passes no aria-labelledby… inheritAttrs: false") — my read of the compiled setups shows BOTH Label and RadioGroup forward `useAttrs()` via mergeProps, so `id` + `aria-labelledby` pass through TODAY and the cure is two consumer attributes, no producer change; (3) both readers dropped L-1's first clause (the demo forwarding barrels) entirely — restored as US-11.

## Verdict register — the adjudicated defect roster

Line anchors are HEAD `3487d73e`; the component pin `de6cc257…cf9a` is the durable anchor — re-resolve before any cure (D-19).

- **US-1 — dead `variant="ghost"` (axis L-1 mechanical arm; C both readers) — CONFIRMED MINOR-as-FOLD.** `UserSortMenu.vue:8` passes a prop the producer never declared (Button.vue.d.ts read whole by this seat — no `variant`; zero attribute selectors match it, per X-W8's own grep law, so it is inert DOM fallthrough). Grade lives at the class home: DEFECT-LEDGER:23784 (51 sites) and X-W8 G-9, which names this exact line RED today. **Disposition: FOLD → X-W8.b (G-9)** — the deletion is already specified, gated, and null-DELTA-proven there. The register question is NOT this row — see US-6.
- **US-2 — the untyped sort seam (axis C-1 + C-2 + L-2 + L-4 type limb + reader-2 missed-3, ONE identity) — CONFIRMED MINOR (latent).** Four holes on one 3-value seam: `:20` gratuitous `(v: any)` discarding the producer's own `SelectionValue` typing (`_shared/selection.d.ts:2`); `:56` emit widened to `string`; useAdminUsers.ts:51's unchecked double cast; and the three `value=` template literals checked only against `string|number`, so `value="newst"` compiles green, never matches the model, and — clicked — degrades the roster to raw server order with no throw (reader-2's trace, mechanism verified by this seat: the switch at :36-46 has no default and falls through to unsorted). **Severity MINOR because no out-of-union producer exists** (my census: `userSortMode` has one writer, one caller, one emitter, three literals; no storage/route/API path). **X-W7.a's repo-wide `strictTemplates` will NOT catch the literal typo** (the attr type is legally `SelectionValue`) — the cure is the union: `export type UserSort` from useAdminUsers.ts:26, consumed at :52/:56 and (ideally) an options array typed `{ value: UserSort; … }[]`, the shape the sibling already demonstrates. The four-layer "registry" cure stays struck (R-B). **Disposition: X-W7.a named-adopt** (it is the template-strictness/drift-class unit and already touches this directory) — its mechanism text names only Checkbox today; **must adopt this row explicitly or it degrades to NO-WAVE-OWNER at execution**.
- **US-3 — the trigger carries no active criterion (axis D-2) — CONFIRMED MINOR.** `:10`'s `aria-label="Sort users"` is static and the sole trigger content is the state-free ellipsis; the `sort` prop reaches only the closed menu's radio model. Reader-2's scope correction adopted: inside the OPEN menu the state IS drawn and announced (producer ItemIndicator + `menuitemradio`/`aria-checked` — indicator machinery read in dist by this seat); the gap is trigger-only. The house cure idiom sits one file over (SearchFilterBar's count badge, :7-12). Compounds US-7: with the sort key invisible in the list AND the criterion invisible on the trigger, the panel offers zero in-place confirmation of ordering state. **Disposition: X-W10** (trigger-state affordance is a design-canon composition; W7.md:170-173's law).
- **US-4 — "Newest first" is ambiguous, and the code silently picks one meaning (axis D-3) — CONFIRMED MINOR.** The domain model carries BOTH candidate fields (types.ts: `createdAt`, `lastSeenAt?`); the implementation sorts `createdAt` (useAdminUsers.ts:41) while the label says only "Newest first" (:24). Both readers' refinements adopted: "Most palettes" and "Alphabetical" are unambiguous, so the axis's three-hint cure over-reaches (KISS edict; the `text-micro` description idiom is reserved for opaque option sets) — the cure is ONE disambiguated label. Reader-2's bonus verified: the `?? ""` comparator sorts missing values LAST in both positions — correct as-is. **Disposition: X-W10** (label copy is design content).
- **US-5 — two sort UIs, one directory, two sign systems (axis D-1 + L-4 divergence limb + reader-1 missed-2, merged) — CONFIRMED MAJOR (reader-1's grade sustained).** The glyph limb (D-1, MINOR as filed, folded in): `EllipsisVertical` means "overflow" at every other site in the fleet (ActionBarLayer, MobileMenuDropdown, PaletteSlugBar, PaletteCard — reader-1's census) — and, decisively, the SAME glyph in the SAME trailing slot of the SAME search-row composition means **"Filters"** in BrowsePane (SearchFilterBar.vue:5-6, my read) and **"Sort"** in AdminPane. The structural limb: the two siblings agree on the job, the title, the Clock icon, and the `update:sort` event — and on nothing else (Popover+RadioGroup+`<label>` rows+array-driven options+`String(v)` vs DropdownMenu+RadioGroup+menuitemradio+hard-coded options+`v as any`; hard-pinned `h-8 w-8` vs `size="xs"`). An admin who learns one pane's sort affordance has learned the wrong thing about the other. **Disposition: X-W10** — the canonical sort-menu idiom is a design ruling (this component is the better half on every measurable axis — see superlatives — so the likely ruling direction is SearchFilterBar converging onto THIS idiom, not the reverse; recorded as input, not authored here). The "Sort: Newest" desktop text-capsule half of the axis cure stays UNPROVEN-NEEDS-LIVE (residue 3).
- **US-6 — the register arm: a glass capsule nested in a glass field (reader-1's L-1 consequence, split from US-1) — CONFIRMED as an open design decision; MAJOR-candidate pending live.** Proven statically end-to-end by this seat: dead `variant` → compiled defaults `emphasis:"secondary"`/`tone:"neutral"` → `glass-wash glass-capsule glass-capsule-hover` → nested inside SearchBar's `data-surface="glass"` input-bar (`surface: { default: "glass" }` in dist/search.js; AdminPane passes no `surface`). The intended register — per the S.W5-4 migration intent, the sibling, ledger 23784's proposed cure, and G5's mapping — is `quiet` (transparent, no plate). What is undecidable from source is loudness at rest (residue 1). **Disposition: X-W10, carrying the R-G cure-collision rider verbatim** — X-W8's null-DELTA deletion and the quiet migration are mutually exclusive on this line; X-W10 rules the register; if quiet wins, G-9's baseline re-stamps in the same commit.
- **US-7 — "Newest first" orders the roster by data the roster never renders (reader-1 missed-1 + reader-2's D-3 sharpening) — CONFIRMED MAJOR ≡ DEFECT-LEDGER:26296-26310, one identity for life.** My own greps: zero `createdAt`/`lastSeenAt` hits in AdminUsersPanel.vue — the row renders slug and palette-count only, so "Alphabetical" and "Most palettes" are user-verifiable and "Newest first" is an unfalsifiable ordering (the ledger row's own phrase; VISUAL-CONSTITUTION:218 cited there). Compounds US-3 (no trigger state either — rows shuffle with nothing on screen accounting for it). The ledger's cure options stand: render the provenance the sort acts on, or drop the key. **Disposition: MT-AU1 named-adopt rider** (the panel rebuild — AdminUsersPanel.md scope item 5 — is the surface owner; the row is not named there today) — **else NO-WAVE-OWNER at the formation boundary**.
- **US-8 — sort/search/count over a hard-capped 50-row page (reader-2 missed-1 + missed-2, merged) — CONFIRMED, FOLD; grade lives at the home docket.** ≡ **AdminUsersPanel Δ-7** ("sort reorders an arbitrary page"; gate G-16 born-RED: fixture `total: 400` rendered as "5 users") **+ Δ-1/L-19 + D-25** (`res.total` discarded, `totalUsers` = page length ≤ 50). My own re-verification at HEAD: useAdminUsers.ts:59 `listUsers(token, 50)`, `total` never referenced in the file, AdminPane.vue:33 `:total-users="pm.adminUsers.value.length"`, and — sharpening adopted from my own read — `listUsers` *declares* `q?`/`offset` (api/admin-users.ts:21-26) and the composable forwards neither: the client-side slug filter (:32-34) is the same page-local disease wearing a search costume. **Disposition: FOLD → AdminUsersPanel Δ-7 / MT-AU1 G-16** — already specified ("roster becomes page-aware… the client-side filter dies"). This menu is blameless: it emits a criterion; the seam above it lies about the corpus. Manifestation (live row count vs the cap) is residue 4.
- **US-9 — the radio group is unlabelled for AT (reader-1 missed-3) — CONFIRMED MINOR, cure-path corrected by this seat.** The `:17` "Sort" label is a plain `div` with no `id`; `aria-labelledby` occurs **zero** times in the compiled dropdown chunk (my grep); the visual grouping is presentational only. Mitigation (why MINOR): the invoking trigger is named "Sort users", establishing context on open. **Correction of record**: both Label and RadioGroup are `inheritAttrs:!1` *with useAttrs-forwarding through mergeProps* (compiled setups read by this seat), so the cure is two consumer attributes today — `id` on the label, `aria-labelledby` on the group — no producer change required; a producer auto-wire affordance may ride the glass BH relay as garnish, never as a blocker. **Disposition: X-W8.b same-touch named-adopt rider** (the wave already edits this file) — else NO-WAVE-OWNER.
- **US-10 — per-instance typography and tokens against the producer's menu scale (reader-1 missed-4 + reader-2 missed-5, merged) — CONFIRMED MINOR.** Three limbs, one identity: (i) `:16 font-display` is near-dead — every item's `text-small` re-declares `font-family: var(--font-text)` (semantic.css read verbatim by this seat), so the display face reaches exactly the 4-character label — a trap for the next editor; (ii) `text-small`/`text-micro`/`cursor-pointer` on every row override the producer's own `--dropdown-text`/`--dropdown-text-secondary`/`cursor: default` (styles.css read); (iii) `w-48` where the demo defines `min-w-menu` for exactly this ≡ **DEFECT-LEDGER:25836** (UserSortMenu.vue:16 enumerated; 178 px consequence measured at the family) — identity preserved, not re-booked. **Disposition: X-W10** (the menu-family typography/width ruling; per reader-2's D-4 kill, 3 of 4 DropdownMenuContent sites carry `font-display`, so charging any single file would move it AWAY from its siblings — the ruling is family-scoped by construction).
- **US-11 — the demo forwarding-barrel edges (axis L-1's FIRST clause, dropped by both readers; this seat's restoration) — CONFIRMED MINOR-as-FOLD.** `:40` `../../../ui/button`, `:41-48` `../../../ui/dropdown-menu` — the axis filed it ("Button and dropdown pieces arrive through demo forwarding barrels") and neither reader adjudicated it. Both families have first-class per-component subpaths at glass-ui 7.0.0 (X-W8.b's own census). Identity: the repo-wide demo/ui extirpation (ColorSpaceSelector L-4 / AdminUsersPanel L-4 / MT-UI1 — anti-rename, one identity). **Disposition: FOLD → X-W8.b** (the 92-import rewrite; the executing owner of this file's touch).
- **US-12 — option glyphs lack `aria-hidden` (axis L-3b) — CONFIRMED INFO (ruled at R-C).** `:13` carries it; `:23`/`:27`/`:31` do not; the producer supplies none (`@lucide/vue` defaultAttributes read — both readers, consistent) while glass-ui's own CheckboxItem indicator models the correct behaviour. One attribute ×3. **Disposition: X-W8.b same-touch rider with US-9** — else NO-WAVE-OWNER. AT consequence is residue 2.
- **US-13 — colocation inversion (reader-2 missed-6) — CONFIRMED INFO.** An admin-only control filed in the palette-browser search cluster; its only consumer (AdminPane.vue:86) and only state owner (useAdminUsers.ts) are both in the admin capsule; it is the only member of its barrel no browser view uses (BrowsePane imports the other two — my read of the census). **Sharpened by this seat**: MT-AU1 scope item 6 already re-homes the whole admin console ("`demo/palettes/admin/` becomes the whole home… `browser/admin/` ceases to exist") and does NOT name UserSortMenu — executed as written, this file strands in `browser/search` reaching across a feature boundary that just got cleaner everywhere else. **Disposition: MT-AU1 item-6 named-adopt rider** (move beside its consumer during the re-home) — else NO-WAVE-OWNER.

## Killed-claims register — 8 kills, every one re-derived by this seat

- **US-K1 · C-5 / D-5 (no loading/busy state for the in-flight request)** — KILLED, premise false, both readers concordant and re-verified: sorting is a synchronous computed over an already-fetched array (useAdminUsers.ts:29-48); the only fetcher is `loadAdminUsers`, called from the view-switch guard (usePaletteWiring.ts:148-150, gated on `length === 0` — my read) and the refresh button (AdminPane.vue:40). A sort change issues zero requests; there is no in-flight window, no ordering contract, nothing to disable. (Had one existed, the producer ships `loading` — one prop.)
- **US-K2 · C-4 (no equality guard; parent may refetch)** — KILLED twice over: no watcher on `userSortMode` exists anywhere (my 10-hit census), the sink assigns a plain ref (Object.is-guarded — identical assignment notifies nothing), and no fetch is wired to sort at all. The prescribed guard costs more than the zero it saves.
- **US-K3 · C-3 (stale/unknown sort; radio group opens unselected)** — KILLED as unreachable: the state is typed to the union, initialized in-union, and has exactly one writer whose only caller is this menu's emit, which can carry only the three hard-coded literals. No URL, no storage, no API path (census). The latent type-hole is already booked once at US-2 — re-filing it as an unhandled state double-counts.
- **US-K4 · C-2 (parent state can become a value the menu cannot show selected)** — KILLED as a separate row: this component is the tree's only `update:sort` emitter for this seam and emits only its own three literals. Double-counts US-2.
- **US-K5 · D-4 (`font-display` competes with fast scanning)** — KILLED twice over, by two independent mechanisms both verified here: reader-1's cascade proof (`text-small` re-declares the family — semantic.css verbatim: the three option rows render in the text face; only the 4-char "Sort" label inherits the display face) and reader-2's scope proof (3 of 4 DropdownMenuContent sites in demo/ carry `font-display` — a family convention, not a local choice). The axis read the class list and not the cascade. Residue booked honestly at US-10.
- **US-K6 · L-3a (direct lucide imports as a boundary problem)** — KILLED: the universal house idiom (45 SFCs, my census); no icon barrel exists; the import law (G-DEMO-3b) is silent on third-party packages.
- **US-K7 · L-4-as-cure (typed shared registry "consumed by query, menu, URL, and API layers")** — KILLED: two of the four named consumers do not exist (no URL layer, no API sort parameter — my read of admin-users.ts), the "query layer" is one in-file switch, and a registry for a three-option client-side sort is the contrivance the KISS edict bans. The true facts fold to US-2 (type seam) and US-5 (sibling divergence).
- **US-K8 · reader-2 missed-4 (top-level seam necrosis)** — **KILLED BY THIS SEAT** (ruling R-E): the seam's own contract text and the eslint boundary's own comment both sanction sub-barrel reach as the compliant form, and the header's PI-6 block documents why the top barrel is deliberately not the routing surface. The zero-direct-importer fact is the authored design functioning. The area-seam question adjacent to it is already NO-WAVE-OWNER at PCS-9 — identity guard, never re-booked.

## Superlatives — with provenance (both readers concordant; spot-verified by this seat)

1. **THE CONTROLLED-COMPONENT DISCIPLINE — the reason 3 of C's 5 findings are false.** 58 lines, zero local state, zero watchers, zero lifecycle, zero dead imports: a pure props-down/events-up leaf over one synchronous computed in one file. Because sorting never touches the network, the race, the generation counter, the loading state, and the equality guard the C axis demanded are all *unnecessary by architecture* — the defects it forecloses cannot occur, so they never get filed. This is what a correctly-factored seam looks like from the accusation side.
2. **REFERENCE IDIOM FOR THE FLEET.** The program's own DEFECT-LEDGER cites this file as the *cure* for four other components (:25823 PaletteCardMenu's hand-rolled visibility rows; :25931 SearchFilterBar's label-wrapped list; :26039 "adopt UserSortMenu's exact idiom… that one move deletes the `.section-label` and `.filter-option` scoped block, the double rhythm, the missing focus register on two lists and the dead `h-8`"; :28103). Few files in a corpus this size are named as the answer rather than the problem.
3. **THE RIGHT PRIMITIVE FAMILY, TAKEN WHOLE — AND ALONE.** The tree's only `DropdownMenuRadioGroup` consumer (:18-34): `menuitemradio`/`aria-checked`/roving focus/type-ahead arrive free, and the producer reserves the selection gutter by construction (`data-indicator="start"` → `padding-inline-start: 1.75rem`, styles.css read here) so the leading Clock/ArrowDownAZ/Palette glyphs cannot collide with the indicator dot — a layout class the component gets right in zero lines of CSS, while both hand-rolled siblings are booked as defects.
4. **ICON-COMMAND CONTRACT, FOUR FOR FOUR.** `icon-only` + `aria-label="Sort users"` + `aria-hidden` glyph + load-bearing `shrink-0` (the slot is a flex sibling of `input-bar-field` — dist read) — and `size="xs"` consumed as producer geometry with no `h-*`/`w-*` override, inheriting the coarse-pointer floor (28 px fine / 44 px coarse) that its folder-mate's hard-pinned `h-8 w-8` opts out of. The ledger cites this trigger as the cure at three other dockets (:26034, :28080, :25949).
5. **PROVENANCE IN THE FILE, MOTION SILENCE EARNED.** The `:4-5` comment names what S.W5-4 killed and what it added ("+ the missing name") — three independent auditors reconstructed the migration from it. And the component declares no motion because every moving part is producer-owned and PRM-guarded (`tap-squish` + `.button` reduced-motion arms in the producer stylesheets), corroborating motion-quarantine §7.4's vacuous clearance. No M-xx row exists to carry.

## UNPROVEN-NEEDS-LIVE residue — the visual-audit inputs

1. **US-6 magnitude**: how loud the secondary-wash capsule reads nested in the glass field, at rest, both themes, both pointer classes — the class/token derivation is proven statically end-to-end; only the pixels are missing, and X-W10's register ruling should sit with them in hand. Chromium, WebKit, and safari-app are SEPARATE cells (I-20); nothing inferred across them.
2. **US-12 consequence**: whether VoiceOver/NVDA actually voice the three bare option `<svg>`s inside `menuitemradio` rows. Static half proven (producer emits no `aria-hidden`/`role`/`focusable`); rated INFO on the assumption they are not voiced — an AT pass decides it.
3. **US-5 cure geometry**: the axis's "Sort: Newest" desktop capsule — the compiled SearchBar makes the slot a flex sibling of the growing input; measure input shrink at narrow admin panes before adopting any text-bearing trigger.
4. **US-8 manifestation**: whether the live corpus currently exceeds the 50-row cap (needs the API). The code-path defect is proven from source alone and adjudicated at its home docket regardless.
5. **X-W7.a interaction**: whether `strictTemplates` fires on `:8`'s `variant` (TS2353 class). Strongly corroborated by the sibling probe (registry/harvest/area-workbenches.json:6970) — not re-run here (the `pretypecheck` chain forces a full library build; probe parsimony). Note it CANNOT catch US-2's literal typo either way — the union seam is the cure there.

## Closing verdict

**ADJUDICATED (not VERIFIED — no wave is stamped by this record).** The axes' three "SOURCE-RED" verdicts are **overturned in part**: 5 of their 14 rows are dead on re-derivation, and the 58-line subject is among the healthiest leaves in the corpus — a ledger-cited reference idiom whose architecture forecloses the C axis's entire failure class. What survives red is real but lives almost entirely OFF this file: the two MAJORs are the seam above it (the unfalsifiable "Newest first" — ledger 26304 — and its sibling the 50-row page-as-corpus class, both already owned at the AdminUsersPanel docket/MT-AU1) and the canon around it (the two-sort-UIs sign-system collision, X-W10's). The one genuinely new hazard this adjudication adds is procedural: **three standing authorities prescribe incompatible cures for `UserSortMenu.vue:8`** (X-W8 G-9's paint-neutral deletion vs ledger-23784/G5's quiet migration) — named here as the R-G collision rider so X-W10 rules the register before any wave's gate outlaws the other's cure. Dispositions: X-W8.b ×4 · X-W7.a ×1 · X-W10 ×5 · MT-AU1 ×3 · kills ×8 · NO-WAVE-OWNER contingencies named on every named-adopt row. Original challenge records and the two reader returns remain immutable beside this file per E-1/E-3.
