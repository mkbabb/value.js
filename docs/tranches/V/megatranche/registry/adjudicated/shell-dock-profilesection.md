Model receipt: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited (L-11/L-14).

# shell-dock-profilesection — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: incredulity/apotheosis · **Date**: 2026-08-08 · **HEAD**: `8d7e17fe` · branch `tranche-u`.
**Corpus (r2 governs)**: `audit/components/shell-dock-profilesection/` — challenge-C (11 rows, Opus 5 [1m]), challenge-D (20 rows, Opus 5 [1m]), challenge-L (10 rows) + 7 evidence PNGs. Component: `demo/shell/dock/menus/ProfileSection.vue` (181 lines, read whole) and its twin `MobileMenuDropdown.vue` (115 lines, read whole).
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted: every verdict-level disagreement (C-1 severity/durability, L-2 severity, C-2/D-3 severity, D-4, D-6, D-8, D-12, D-14, D-16, D-19-split, the C-4 rider where the two readers CONFLICT, the L-3 rider, both readers' missed-finding slates) was re-derived against the tree with this seat's own instruments; agreed rows with load-bearing bytes (the strip set, the menu.css layer seam, the dock paint topology, the regenerate ordering, the o18 walk) were re-measured, not spot-trusted.
**Arbiter's own instruments (all static — product source read-only, zero browser probes, per law)**: full reads of ProfileSection.vue, MobileMenuDropdown.vue, useContrastSafeColor.ts (377 lines), ink.ts:10–145, useUserAuth.ts:100–136, useSlugMigration.ts:89–115, SlugEditLayer.vue:66–72, Dock.vue:30–82/:205–226, o18-contrast-census.spec.ts:142–180/:585–645, foundation.css:250–256/:372–376/:583–590/:726–770, eslint.config.js glob census, VISUAL-CONSTITUTION.md:73–80/:154, DESIGN.md:9–13/:35–39, X.W4/W8/W10 wave specs, motion-quarantine.md (grep for this component: zero rows). Installed glass-ui 7.0.0 dist: DropdownMenuItem.vue.d.ts (whole), primitive-B2t2jdqM.js (whole — the `{"as","asChild","as-child"}` strip set verbatim), `as: "div"` ×8 in dropdown-menu chunk, button/styles.css (`border: 0`, secondary wash, quiet/text hover pair, `> svg` glyph rule, tone-border rule), _shared/menu.css (the unlayered `.dropdown-menu-content` rule — the D-11 decider), styles/index.css import list (no `layer()` on menu.css), dock paint census (`.dock-plate` paints; NO `.glass-dock` background rule anywhere in dist), shell.css `--dock-expand-t` crossfade + morph.css `[data-held] .dock-plate`, tokens (`--glass-bg-dock` = the ONE tinted rung token; `--glass-bg-floating` untinted; `--control-h-xs`; `--ui-glyph`; the `pointer: coarse` `--ui-scale: 1.5` ramp; `--control-floor: 0px`), ladder.css `--muted-foreground` re-points ×3 + the floating-rung `--glass-tint-source: var(--glass-tint-ink)` re-point, useGlobalDark.d.ts `onFlipSettled`, AvatarImage.vue.d.ts (`referrerPolicy`/`crossOrigin` present), gold-shimmer in base-misc.css. Repo counts (mine): `useGlobalDark(` = 10 invocations / 9 files; `dir="rtl"|direction: rtl|useRtl` in demo = 1 hit (an index.html comment, no live RTL arm); `dropdown-menu-bg` in demo = 0; `ls demo/@` = No such file or directory; demo/ui = 19 dirs, input barrel = `@mkbabb/glass-ui/forms`.
**Environment disclosure**: every ruling below was decidable from source + the shipped dist. The readers' live cells and the corpus PNGs are adopted as corroboration only, never load-bearing where a static derivation exists; each pixel number that could not be re-derived statically is in the UNPROVEN residue.

## Scoreboard

**31 corpus rows CONFIRMED in some form (2 at BLOCKER weight · 15 MAJOR · 8 MINOR · 6 INFO; several RESCOPED or DEMOTED below) · 11 claims/limbs KILLED (one of them this seat's own, against both readers) · 12 reader-missed findings ADOPTED (5 MAJOR) · 2 identity-guarded rows NOT re-booked (anti-rename) · 14 items to the live pass.**

Blocker-weight survivors: **C-5/L-1(a)** — the `chrome` contrast probe reads an element glass-ui 7 never paints, so the guard silently serves a stale static model to the exact control it was built for (X.W8 G-13 already names this defect and its falsifier); and **D-2** — the GitHub row ships visibly broken in every scheme and both twins because `as-child` lands in a producer strip set that discards it silently, leaving a focusable anchor inside `[role=menuitem]`.

## Ruled disagreements (the seat's actual work)

| dispute | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-1 severity + durability** | BLOCKER, "permanently, until reload" | BLOCKER, but flags that L-8's own trace shows the ink settled correct at +600 ms | **DEMOTED to MAJOR; the durability limb is UNPROVEN and this seat KILLS it as stated (K-11)** | Mechanism CONFIRMED from source: useContrastSafeColor.ts:242 keys cache validity on the DOM effect (`classList.contains("dark")`), :68–78 shows `probeEpoch` bumped only from `onMounted`, and nothing re-drives on a flip — so the pre-flush recompute stamps the OUTGOING scheme. But duration is structurally bounded: the stamped entry carries the outgoing `darkClass`, so the FIRST recompute after the class lands (any ambient tick, any consumer mount, any epoch bump) mismatches the key and re-probes fresh. "Permanent" requires a document in which no ink computed ever re-evaluates again — and L-8's measured trace (`settled dark=true ink=oklch(0.848…)` at +600 ms) witnesses the opposite. The real defect is that nothing GUARANTEES a recompute (an idle document stays wrong indefinitely), the cure is the producer's shipped `onFlipSettled` hook (useGlobalDark.d.ts:39, verified), and the window's true length is a live question (U-2). A BLOCKER needs the permanent limb; the permanent limb is unproven and half-contradicted by the corpus's own instruments. |
| **L-2 severity** | BLOCKER | MAJOR (over-certification raises contrast; harm is voice, not AA) | **MAJOR — LC wins** | Dock.vue:220/:209 `:css-color-opaque="safeAccent"` verified; SAFE_ACCENT_KEY = the resting-certified output (useContrastSafeColor.ts:302–311). The composition direction only ever ADDS contrast, so no accessibility floor is at risk; the defects are the lying prop name and two owners of one concept in series. Blocker-weight is reserved for shipped user harm. The +26.3 % figure stays live-labelled (U-4). |
| **C-2/D-3 severity** | MAJOR | BLOCKER | **MAJOR — DU wins** | The dead `variant` props are structurally certain (Button.vue.d.ts has no `variant`; my read) and three sites pass them — but the rendered harm they cause is carried by other rows already booked (D-1's uncertified wash layer, D-15's material clash, D-17's missing hover). A dead prop whose consequences are each independently indicted is a MAJOR cause, not a second BLOCKER. The repo-wide `variant=` blast count (103) stays live-labelled (U-12). |
| **C-4 rider ("withdraw mark M1")** | endorsed the rider ("sound") | KILLED it as overreach | **KILLED — LC wins (K-8)** | `DropdownMenuCheckboxItem` cures THIS row's semantics (verified exported by the demo barrel, my read). Mark M1 concerns the producer's `DarkModeToggle` primitive lacking a `passive` mode — a different component with its own consumers; the W44 comment's double-toggle hazard (ProfileSection.vue:168–173) is untouched by this consumer edit. Whether M1 dies is the BH relay's decision, not a corollary of a consumer swap. |
| **D-11 disposition** | CONFIRMED MAJOR (cascade-certain inertness) | killed the framing/cure ("already the shipped design"), left inertness "plausible" | **BOTH partly right → RESCOPED to MINOR, and the inertness is now CASCADE-CERTAIN by this seat's own read** | menu.css closes its `@layer components` block after `.glass-menu-row` and declares `.dropdown-menu-content { font-family: var(--dropdown-menu-font, inherit) }` OUTSIDE any layer; styles/index.css imports it with no `layer()` (both read by this seat). Unlayered author declarations beat every layered one, so Tailwind's `font-display` utility loses regardless of order — DU's upgrade adopted. But the FAMILY is a documented root decision (foundation.css:374 + DESIGN.md's sanctioned override list, both read) — LC's kill of the "set the token" cure adopted (K-7). Surviving work: delete one dead class at three sites. That is hygiene = MINOR. |
| **D-16 cause/cure** | killed the size-rung diagnosis (K-4), kept MAJOR, re-diagnosed as wrong control family | left the geometry unproven | **DU's rescope ADOPTED — MAJOR as re-diagnosed** | triggers.css read by this seat: dock triggers are content-and-padding sized (`--dock-trigger-min-height, auto`), consuming NO `--control-h-*` rung — so `size="sm"` (36 px) would overshoot, K-4 stands. The 28 px seat height is token-derivable (`--control-h-xs` = 1.75rem at `--ui-scale: 1`, `--control-floor: 0px`, both read). The true conformance gap: this component drops page `Button` primitives into the dock band while its own twin uses `DockTrigger` (MobileMenuDropdown.vue:11/:40, read). Separator air numbers stay live (U-6). |
| **D-8 severity** | MAJOR | MINOR | **MAJOR — DU wins** | The slug is the CREDENTIAL in this auth model (slug-based sessions; losing it is losing the account). "Copy slug" is the credential-preservation affordance, `@select.prevent` deliberately holds the menu open — and then no state exists for success OR failure (`void writeClipboard(...)`, SlugEditLayer.vue:68–70, read; rejection discarded, empty-slug silent no-op). A user who trusts a silent copy before Regenerate/Switch can lose the account. Silent failure on a credential action outranks cosmetic severity. |
| **D-12/C-8 severity** | MAJOR | MINOR | **MINOR — LC wins, with the N-11 rider folded in** | The avatar is `decorative` by the component's own (correct) declaration; its failure state is an empty 28 px circle on a vanity block. The third-party fetch is real but mitigable by a one-word prop the producer already ships (`referrerPolicy`, AvatarImage.vue.d.ts, read). Decorative failure + one-line cure ≠ MAJOR. |
| **D-6 severity** | MAJOR | MINOR | **MINOR** | One glyph misdrawn (16×24, mechanism certain from lucide presentation attributes + the definite-both-axes `aspect-ratio` rule) in one menu row. The SYSTEMIC glyph problem — nine absolute pins per file defeating the producer's coarse `--ui-scale` ramp — is N-2's, adopted at MAJOR; D-6 folds into its cure. |
| **D-14 severity** | MINOR (latent — no RTL arm ships) | MAJOR | **MINOR — DU wins** | My grep: one `rtl` hit in demo and it is an index.html COMMENT; no live `dir="rtl"` arm exists. The bidi derivation is sound (both readers re-derived it independently) and the constitution's LTR-isolation law (:154, read) makes it real debt — but shipping severity follows shipping exposure. Filed at X-W10 so the law lands with the conformance pass. |
| **D-19/C-6 shape** | one MAJOR row | C-6 MAJOR + D-19 MINOR | **ONE MAJOR row (DU's shape)** | The two facts are one defect: the only automated instrument that ever looks at this component (o18) keys on a hook the shipped majority state doesn't carry (:629–630, read) AND composites an ancestor walk that can never contain the sibling `.dock-plate` (:147–160, read — the band paint is a z-index:-1 SIBLING). A gate blind twice over is one vacuous gate, not a MAJOR and a MINOR. |
| **LC's missed N-6 (untinted rung probes)** | — | filed MAJOR with foundation.css:253 as the tint driver | **ADOPTED MAJOR, with this seat's correction** | The core is verified: the probe comment promises "color-mix, TINT KNOB AND ALL" (useContrastSafeColor.ts:44, read) while `--glass-bg-floating` carries no tint term and the tint mixes one level up in `--glass-plate-tinted` (both read from dist). Correction: at the floating/overlay rung, ladder.css RE-POINTS `--glass-tint-source` to `var(--glass-tint-ink)` (read) — so the missing term is not simply the demo's root `--accent-live` feed; the painted tint at that rung is the producer's ink-tint axis. The finding survives; its magnitude model needs the corrected source. |
| **LC's missed N-7 (state-dependent band paint)** | — | filed MAJOR citing dock.css | **ADOPTED MAJOR, citation corrected** | The quoted `--dock-expand-t` crossfade and `[data-held]` rules are REAL but live in `shell.css` and `morph.css`, not `dock.css` (my greps: `dock-expand-t` = 0 hits in dock.css; the crossfade in shell.css; `[data-held] .dock-plate { background: color-mix(...--glass-bg-floating...) }` in morph.css). The `.collapsed:hover` background limb did not reproduce in my dist read and is struck from the proof (the surviving two mechanisms suffice). Consequence intact: any single-shot band probe caches one expand-state; the cache has no invalidation for any of them. |

## Adjudicated defect roster

Severities are this seat's rulings. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `8d7e17fe`; re-resolve before any cure (the D-19 lesson).

| id | sev | claim (as survives adjudication) | proof (arbiter-verified core) | terminal disposition |
|---|---|---|---|---|
| **C-5 / L-1(a)** | **BLOCKER** | The `chrome` ink probe reads `.glass-dock`, which paints nothing under Glass 7; the guard silently degenerates to the static model for every `useSafeAccentFn("chrome")` consumer, and no test can notice. | useContrastSafeColor.ts:151–153 (read); dist-wide census: NO `.glass-dock` background rule, band painted by the z-index:-1 child `.dock-plate` (read); alpha-0 → `undefined` at :217 → ink.ts:118–122 static arm (read). Sole consumer: ProfileSection.vue:28. | **X-W8** — G-13 names this row verbatim, with the type-level cure (the static `chrome` rung becomes unrepresentable) and the CC-107 bank for the probe's retirement. Cross-ref N-6/N-7/N-12 as design inputs to that gate. |
| **D-2** | **BLOCKER** | `as-child` on `DropdownMenuItem` is silently discarded — the producer's strip set filters `{as, asChild, as-child}` and hard-codes `as: "div"` — so the GitHub anchor nests inside `[role=menuitem]`, breaks to two lines in every scheme, and is a rogue Tab stop inside a roving-focus widget. Both twins. | DropdownMenuItem.vue.d.ts: `{disabled, textValue, inset, class}` only (read); primitive-B2t2jdqM.js strip set verbatim (read); `as: "div"` ×8 in the dropdown chunk (grep). Corpus PNGs corroborate the two-line break in both schemes. Sites: ProfileSection.vue:158–165, MobileMenuDropdown.vue:92–99. | **X-W4** (semantic control in the persistent shell) + **glass-ui BH relay** (owner edict): either DropdownMenuItem gains the PrimitiveProps pair / a `DropdownMenuLinkItem` ships, or the row becomes the navigator via `@select` with the anchor removed. The `as="a"` cure is K-2, dead. |
| **C-1** | MAJOR (demoted) | The tint cache's validity key is the DOM effect read at a moment only the reactive cause has changed, so a runtime flip re-stamps certified ink against the outgoing scheme; nothing guarantees the correcting recompute, and this component owns the flip control. | :242 + :68–78 read; no `onFlipSettled`/`installDarkModeSync` consumer in demo (LC's grep, spot-trusted against my read of the d.ts). Durability limb KILLED as stated (K-11) → U-2. Cure exists in the producer: `onFlipSettled` (read). | **X-W8** (the ink instrument carve; same file as G-13's surface). |
| **L-2** | MAJOR (demoted) | Dock injects the resting-certified ink and passes it down a prop named `cssColorOpaque`; ProfileSection certifies the already-certified — two owners in series, the name asserting the opposite. | Dock.vue:34–35/:209/:220 read; useContrastSafeColor.ts:302–311 read. Magnitude → U-4. | **X-W8** (one certification owner; the prop renames or dies in the carve). |
| **C-2 / D-3** | MAJOR (demoted from LC's BLOCKER) | `variant="outline"`/`variant="ghost"` are not Glass 7 vocabulary; three sites pass them as inert DOM attrs; both band controls resolve to default `secondary` and paint the identical wash capsule. The `emphasis="quiet"` cure is real at rest (transparent + a real hover) — with LC's caveat that quiet's HOVER re-introduces a wash. | Button d.ts prop union (no `variant`) + styles.css secondary/quiet rules, all read. | **X-W8** (dead-surface subtraction) for the dead props; **X-W10** for the emphasis register decision. |
| **D-1** | MAJOR | The certified trigger ink's referent omits a layer: the glyph composites over band + the button's own wash capsule; `chrome` models one alpha layer. The file's own dependency already diagnosed and cured this exact two-layer class for the veil. | Mechanism chain verified: dead `variant` → secondary → `glass-wash glass-capsule` (compiled class computed, LC's read corroborated by styles.css bytes); veil precedent at useContrastSafeColor.ts:154–206 (read). Ratios → U-1. | **X-W8** with C-5 (same referent repair); the emphasis-quiet arm rides C-2/D-3's X-W10 decision. |
| **D-4** | MINOR (demoted) | The `borderColor: triggerInk` binding is cascade-dead on both triggers (`.button { border: 0 }`, tone `neutral` never re-adds it) — three lines below the comment retiring exactly this class of dead accent. The same idiom is LIVE on `.slug-pill` (its recipe declares `border`). | styles.css `border: 0` + tone-border rule read; foundation.css:585–587 read. Vestigial by the file's own :103–110 account. | **X-W8** (dead-surface subtraction). |
| **D-5** | MAJOR | The admin gold shimmer is dead — inline `color: var(--color-gold)` outranks the text-clipped gradient — while the infinite `metal-shimmer-sweep` keeps animating invisibly. Only this consumer kills it (the two siblings set no inline color). | base-misc.css `.gold-shimmer` bytes read (background-clip:text + `color: transparent`; animation PRM-gated). ProfileSection.vue:96 read. **Motion law**: no motion-quarantine row exists for this component (my grep; the nearest shell-dock row M-32 belongs to actiontoolbar and is not carried); the animation is producer-gated, so this is waste + dead identity, not a PRM defect. | **X-W10** (canonical design and motion). |
| **D-6** | MINOR (demoted) | The theme glyph renders 16×24 (`w-4` overrides width; the 24 px height presentation attr stands; `aspect-ratio` is ignored when both axes are definite) — the one class written to prevent it is the one that cannot. | Mechanism certain from lucide defaultAttributes + CSS sizing rules (readers' derivation, re-checked against the spec logic); rect → U-7. | Folds into **N-2**'s glyph-rule cure at **X-W10**. |
| **D-7 / C-4** | MAJOR | Dark mode is a checkbox shipped as a command: `role="menuitem"`, no `aria-checked`, state carried only by `aria-hidden` glyphs — while `DropdownMenuCheckboxItem` (role `menuitemcheckbox` + `aria-checked`) sits exported in the very barrel the file imports. Both twins. | ProfileSection.vue:167–177 + MobileMenuDropdown.vue:101–111 read; demo/ui/dropdown-menu/index.ts exports `DropdownMenuCheckboxItem` (read). Rider KILLED (K-8). | **X-W4** (semantic controls). |
| **D-8** | MAJOR (ruled) | Two copy actions, two state designs; the one attached to the user's CREDENTIAL has none — rejection `void`-discarded, empty-slug silent, menu held open showing nothing, while the sibling Share row has a full success state. | SlugEditLayer.vue:68–70 verbatim (read); Dock.vue:62 chain read; ProfileSection.vue:77 `@select.prevent` read. | **X-W4** — the wave's goal criterion names this class verbatim: "an action that silently does nothing becomes unrepresentable". |
| **C-3 / D-9** | MAJOR | Regenerate slug: the zero-palettes branch skips the confirm dialog entirely, the port clears storage BEFORE creating the replacement while its own doc-comment claims "no null gap … atomically", the call site is a floating promise, and no pending/error/result state exists; `tone="destructive"` ships unused. The "no confirm ever" limb is K-6 (the >0-palettes branch DOES route through the migrate dialog — read). | useSlugMigration.ts:91–104 read; useUserAuth.ts:109–127 read (comment + clear-then-create ordering verbatim); ProfileSection.vue:87 read. | **X-W4** (destructive action ownership; X-W6's b4 precedent — "one removal owner; the floor is an explicit disabled state" — cited as the house pattern). |
| **D-10** | MAJOR | The constitution's closed type matrix is broken: `text-mono-small font-bold` on the Profile/Login CONTROL labels (:62/:114), and the tagline rides `text-micro` italic — a rung the matrix does not contain. The menu-family charge is struck (K-7: the mono menu is a sanctioned root decision). | VISUAL-CONSTITUTION.md:75–78 read verbatim ("closed across all eighteen compositions", P019 sole exception); component classes read. | **X-W10** (the wave that owns the constitution fold). |
| **D-11** | MINOR (rescoped) | `font-display` on menu content is a dead class at all three sites — now CASCADE-CERTAIN (the producer's unlayered `.dropdown-menu-content` font rule beats any layered utility; my read of menu.css + index.css). The family itself is deliberate (K-7). | See ruled-disagreements row. Sites: ProfileSection.vue:70/:143, MobileMenuDropdown.vue:43. | **X-W8** (dead-surface subtraction). |
| **D-12 / C-8** | MINOR (demoted) + N-11 rider | No `AvatarFallback` (exported by the imported barrel, unused) → an errored image leaves an empty 28 px circle; the chrome fetches a third-party host per menu open; and the producer's `referrerPolicy` prop (shipped, read) is unpassed, so the full document URL leaks to GitHub. Both twins. | ProfileSection.vue:145–147 + MobileMenuDropdown.vue:80–82 read; avatar barrel read; AvatarImage.vue.d.ts read. Offline render → U-8. | **NO-WAVE-OWNER** — booked for the next formation boundary (a two-line cure: `AvatarFallback` + `referrer-policy="no-referrer"`). |
| **D-13 / C-9 / L-6** | MAJOR | One menu hand-written twice, already drifted three ways (admin identity gold-shimmer-in-band vs muted-pill-in-menu; the Regenerate separator present vs absent; Login button-in-band vs menu row) — under a twin whose own comment claims "verbatim". The 1024 px seam is crossed by a desktop at 200 % zoom. The zoom-LOSS limb is K-5 (everything relocates into `⋮`; nothing is unreachable). | Both files read line-by-line; all three drifts confirmed; MobileMenuDropdown.vue:21–22 comment read. | **NO-WAVE-OWNER** — the consolidation (one content model, two presentations) is named for the next formation; the drift rows individually ride their owning waves above. |
| **D-14** | MINOR (ruled) | Wordmark/handle/slug are not LTR-isolated; in an RTL context the wordmark renders `mbabb@` (bidi-derivable, both readers independently). LATENT: no RTL arm ships (my grep — one comment hit). Constitution :154 legislates LTR-isolated spans. | Component sites :75/:140/:149 + twin :50/:84 read. | **X-W10** (lands with the constitution conformance pass). |
| **D-15** | MAJOR | A desaturated wash slab sits inside a chromatic capsule on a chromatic band, and the control boundary is stated three times (capsule ring + wash edge + `DockSeparator` each side). | Mechanism certain from styles.css secondary rules (read) + ProfileSection.vue:49/:123; pixels → U-6. | **X-W10**. |
| **D-16** | MAJOR (rescoped per K-4) | The component seats page `Button` primitives in the dock band (28 px via `--control-h-xs`) while the band's own family is content-sized and its own twin already uses `DockTrigger` — wrong control family, not wrong size rung. Separator asymmetry → U-6. | sizing.css + triggers.css + both components read (see ruled row). | **X-W4.a** — "Shell control geometry and naming" owns this surface by name. |
| **D-17** | MINOR | Two adjacent controls, two hover languages; Login's entire affordance is the capsule's 1.5 % scale (`.glass-capsule-hover:hover { scale: 1.015 }`, read) because secondary ships no hover rule and the inline color would outrank one anyway. | styles.css hover census read. | **X-W10** (rides the emphasis decision of C-2/D-3). |
| **D-18** | MINOR | One file, two sibling roots, four jobs — the app's only theme control lives as the last row of a personal credit menu, at both breakpoints. | Template read; Dock.vue:217–226 single-unit mount read. | **NO-WAVE-OWNER** (component split; sequenced with D-13's consolidation). |
| **D-19 / C-6** | MAJOR (one row) | Nothing tests this component, and the one instrument that looks at its seat is blind twice: keyed on the logged-in-only `data-o18` hook, and compositing an ancestor walk that can never contain the sibling `.dock-plate` — so the census ground is unpainted and scheme-invariant. | o18 spec :147–160/:585–645 read; ProfileSection.vue:64 vs :111–120 read; the sibling-not-ancestor topology confirmed by the dock CSS census. | **X-W1** (re-gate: the visual/contrast oracle). |
| **C-7** | MINOR | `text-foreground/70` on the wordmark is the retired post-hoc-alpha de-emphasis idiom; the certified replacement (`mutedInkCss` → `--ink-muted`) exists in the file this component imports. | useContrastSafeColor.ts:317–329 read; ProfileSection.vue:138 read. | **X-W10**. |
| **C-11 / L-7** | INFO | Two `useSafeAccentFn` calls → two mount-epoch bumps of the module-global cache key every ink consumer shares; `inject(SESSION_PORT_KEY)!` renders a missing provider as a TypeError despite the sibling docblock's "surfaced loudly" promise. | :28–29/:42 + :69–78/:345–348 read. | **X-W8** (record; folds into the instrument carve). |
| **C-10** | INFO | Three dialects of one design system in one 44-line script block (two producer subpaths + three shim barrels). | Imports read. | Folds into **L-3 → X-W8.b**. |
| **L-3** | MAJOR (rider rescoped per K-9) | `demo/ui/` is a 19-barrel pure-alias shim; 18 of 19 reach the root barrel — `input` is the counter-example (`@mkbabb/glass-ui/forms`, read), so the producer-subpath ask is FIVE (avatar, alert, checkbox, radio-group, skeleton), not six. | Barrels read; LC's 48-file/79-file counts adopted as corroboration (identity: the W-HYGIENE `demo/ui` extirpation, per the ColorSpaceSelector precedent). | **X-W8.b** — "Barrel and configuration subtraction" owns it by name. |
| **L-4** | MAJOR | The shell imports a feature module (`palettes/usePalettePorts`) to learn who is logged in; identity's owners live in `platform/auth/*` — shell → feature → platform. Three edges (both twins + Dock). | usePalettePorts.ts imports read. | **X-W8** (SCC closure) — with L-5's guard as the reason it was never caught. |
| **L-5** | MAJOR — **IDENTITY GUARD, not re-booked** | All demo import-boundary lint globs target the deleted `demo/@` tree; zero structural enforcement. | eslint.config.js:235–238/:275–276/:286 + `ls demo/@` re-verified by this seat. FACT-IDENTICAL to ColorSpaceSelector's L-5(a), already FOLDed to the lint-glob-liveness wave with a born-RED command. ONE identity (anti-rename law); this corpus adds the L-4 consequence as evidence. | Cross-ref only. |
| **L-8** | INFO | MT-F009 re-scope CONFIRMED: exactly one live classList reader (:242); the named `useDark` stores are removal-comments; count corrected — **10 invocations / 9 files** (my grep; the "21" was import-specifier conflation, K-10). `--accent-live` desync is transient by its own trace. | My greps. | **X-W8** record (MT-F009 re-scoped to its single surviving instance). |
| **L-9** | INFO — **IDENTITY GUARD, not re-booked** | tsconfig.demo.json paths: 8 keys / 7 exports, 3 dead targets, 2 unmapped subpaths, false "closed set" comment; plus LC's verified rider — vite.config's "bare specifier" prose is false (glass-ui dist imports only published subpaths). | Fact-identical to ColorSpaceSelector's L-14, already FOLDed to W-HYGIENE config parity (born-RED generator command). | Cross-ref only. |
| **L-10 / D-20** | INFO | The component proves nothing about the public API (its library reach is transitive through ink.ts's published subpaths — a clean fact, see superlatives); D-20's undesigned-state table is adopted as the fixer's checklist (no `loading`, no error paths, no truncation rule, admin-without-slug has NO logout affordance). | ink.ts imports + Button d.ts `loading` read. | Record; the state rows ride their owning waves above. |

### Adopted reader-missed findings (N-rows — new defects neither challenge axis filed)

| id | sev | finding (provenance) | arbiter verification | disposition |
|---|---|---|---|---|
| **N-1** | MAJOR | A SECOND focusable anchor is a direct non-menuitem child of `role="menu"`: the credit block is a bare `div` child of `DropdownMenuContent` containing a Tab-reachable `<a>` the roving order never visits. Both twins. (reader-DU) | ProfileSection.vue:143–152 + MobileMenuDropdown.vue:79–87 read — structurally certain. Same wound-class as D-2, one seat over; cheap cure (`DropdownMenuLabel` is already imported and used at :71). | **X-W4** with D-2. |
| **N-2** | MAJOR | Nine hand-pinned absolute glyph sizes per file silently defeat the producer's `pointer: coarse` ramp (`--ui-scale: 1.5`, `--ui-glyph` 16→24 px, menu rows to 44 px) — and the always-coarse mobile twin is the worst-hit surface. Sharpens D-6's cure: the producer's `> svg` rule covers only Button hosts; menu.css ships NO svg sizing → demo-root rule or BH-relay mark. (reader-DU) | light-dark.css coarse block + sizing.css `--ui-glyph` + menu.css `min-block-size` all read; the pins enumerated in both files by this seat's reads. Coarse-cell render → U-13. | **X-W4** (legal target size is the wave's goal text) + **BH relay** for the menu-glyph rule. |
| **N-3** | MAJOR | The menu's row labels are the ONE ink population with no contrast guard, compositing over live page content that reads through the translucent panel — while the same file certifies the pill and the trigger. The producer seam for exactly this (`--dropdown-menu-bg` via `--glass-bg-rung`) is unused: my grep = 0 hits, one property away from the pinned `--dropdown-menu-font`. (reader-DU) | menu.css `--glass-bg-rung: var(--dropdown-menu-bg, var(--glass-bg-floating))` read; foundation.css translucency defaults read; rows carry no ink binding (template read). Ratios → U-10; the blur question → U-9. | **X-W10** (design) + **X-W1** (the census learns the rows). |
| **N-4** | MINOR | Four `target="_blank"` anchors, zero new-window indication (WCAG 3.2.5/G201), two of them inside an open menu. (reader-DU) | All four sites read (:149/:159; twin :84/:93). | **X-W4**. |
| **N-5** | INFO | `--muted-foreground` is re-defined PER RUNG by ladder.css (three re-points + a backdrop-container escalation) — the fixer's map for every muted seat here, and the fact that killed the D-3 rider. (reader-DU) | ladder.css re-points read by this seat, token values confirmed in dark-arm.css/on-glass-fg.css. | Record — attach to X-W10's design-law fold. |
| **N-6** | MAJOR | The `floating`/`resting`/`well` probes read UNTINTED rung tokens while the file's own contract comment promises "tint knob and all" — the same defect-class as the dead chrome probe, on three more rungs, with the comment as counter-evidence. **Arbiter's correction**: at the floating rung the producer re-points `--glass-tint-source` to `--glass-tint-ink` (ladder.css), so the missing term is the producer's ink-tint axis, not the root `--accent-live` feed as filed. (reader-LC) | :41–54 read; `--glass-bg-floating` (no tint term) vs `--glass-plate-tinted` (tint mix) read; ladder.css re-point read. | **X-W8** (G-13's repair must not stop at `chrome`). |
| **N-7** | MAJOR | The band's paint is a FUNCTION OF DOCK STATE (`--dock-expand-t` crossfade in **shell.css**; `[data-held]` re-paint in **morph.css** — citations corrected, the `.collapsed:hover` limb struck) and the tint cache has no invalidation for any of it — which bounds every single-shot probe cure, including C-5's own. (reader-LC) | shell.css + morph.css rules extracted by this seat; cache keys :242–249 read; Dock.vue:78 expanded-watch read. | **X-W8** (G-13 design input: the fix is an invalidation signal or a producer-published value, not a better selector). |
| **N-8** | MAJOR | `liveTintCache` is keyed by `InkSurface` alone with a first-match `querySelector('[data-surface="veil"]')` host — directly contradicting the 12-line subtree-locality argument (with measured evidence) the same file makes for the veil; the first veil probed becomes every veil's referent until the next epoch. (reader-LC) | :158–172/:239–249 read — structurally certain. | **X-W8**. |
| **N-9** | MINOR | `resolveCssColorAlpha` fabricates a confident OPAQUE BLACK for an unparseable color (canvas ignores invalid `fillStyle`; both grounds return black; α computes 1) while every sibling failure path degrades honestly to null/undefined. Latent — current callers feed computed values. (reader-LC) | :100–117 read — derivation checked line-by-line by this seat. | **X-W8**. |
| **N-10** | MINOR | The instrument leaks by design: a hidden probe `div` appended to `document.body` with no removal path, a memoized canvas context, and two more unowned module-global maps — surviving unmount and HMR, littering every jsdom suite. (reader-LC) | :80–92/:121/:134–145/:239 read. | **X-W8**. |
| **N-11** | MINOR | `referrerPolicy`/`crossOrigin` are declared by the producer's `AvatarImage` and unpassed — every menu open sends the full document URL to GitHub; the mitigation is one token. (reader-LC) | d.ts read. | Folded into **D-12** (NO-WAVE-OWNER). |
| **N-12** | INFO | `--glass-bg-dock` is the ONE rung token that carries the tint term — `chrome: "--glass-bg-dock"` in `TIER_BG_TOKEN` plus deleting the selector branch is the cheapest correct-DIRECTION repair for C-5, bounded by N-7 (still not the state-dependent whole). (reader-LC) | Token definition read: the tint mix is inside `--glass-bg-dock`, absent from every sibling rung token. | **X-W8** (G-13 cure note, beside the CC-107 bank). |

## Killed-claims register

| id | killed claim | refutation (arbiter-held) |
|---|---|---|
| **K-1 · L-1(b)** | "`chrome` and `floating` are the same case body; therefore `triggerInk === menuInk`, always — the two-referent apparatus is inert." | Refuted by the line the claim itself cites: ink.ts:109 short-circuits on a live tint BEFORE the switch, and `floating` HAS a live tint (`TIER_BG_TOKEN.floating` → a real token the probe element resolves — :50–54/:208–213, read; L-1's own table measured it at α 0.88). Only `chrome`'s probe dies. The apparatus is HALF-BROKEN, not inert — and the distinction is load-bearing: "inert" invites deleting the two-referent design whose principle every axis affirmed (see superlatives). Both readers killed this; this seat re-derived and concurs. |
| **K-2 · D-2 cure** | "Use the producer's supported `as=\"a\"` on the item." | `as` sits in the SAME strip set as `as-child` (primitive-B2t2jdqM.js, read verbatim) and the Item hard-codes `as: "div"` (×8 in the compiled chunk, my grep); DropdownMenuItemProps declares no polymorphic prop (d.ts read). The prescribed edit is the indicted no-op wearing a different spelling. Cure = producer relay or `@select` navigation. |
| **K-3 · D-3 rider** | "`--muted-foreground` is floor-raised in foundation.css, so `emphasis=\"quiet\"` also retires the wordmark's 4.18:1 dark failure." | The sole demo `--muted-foreground:` write sits inside `@media (prefers-contrast: more)` (foundation.css:728–740, read) — no default-path raise exists; and ladder.css re-points the token ON `.glass-capsule` elements to `--on-glass-muted-strong` (read), which no `:root` write can reach. The cure may still be right; its offered guarantee is false and would ship an unverified dark ratio. |
| **K-4 · D-16 cause/cure** | "`size=\"xs\"` is the wrong rung; the dock family is `--control-h-*` one step up." | Dock triggers consume NO `--control-h-*` rung — triggers.css sizes them content-and-padding (`min-height: var(--dock-trigger-min-height, auto)`, read). `size="sm"` = 36 px would overshoot the 32 px band. The real gap is the control FAMILY (page Button vs the twin's `DockTrigger`), adopted into the D-16 roster row. |
| **K-5 · D-13 limb** | "At 200 % zoom the entire ProfileSection is gone, account affordance and theme control with it." | Overstated: every affordance relocates into the `⋮` twin (Login :71–73, Copy/Switch/Logout/Regenerate :52–63, Share :88–91, GitHub :92–99, Dark mode :101–111 — all read). No WCAG 1.4.4 loss; the real seam defect is the DRIFT, which the roster keeps. |
| **K-6 · D-9 limb** | "`pm.onRegenerateSlug()` fires on a single click. No confirm step." | False for the majority branch: useSlugMigration.ts:91–104 (read) routes any user with ≥1 saved palette through the migrate dialog with a publish/transfer/discard choice. Only the zero-palettes `else` is unguarded — which is exactly the user with nothing saved. C-3 stated it correctly; the surviving defect set is in the roster. |
| **K-7 · D-11 cure + D-10's menu-family charge** | "Set `--dropdown-menu-font` once, at the shell root" / the mono menu as a unilateral break. | Already the shipped, DOCUMENTED design: foundation.css:374 pins it at `:root` under a rationale comment, and DESIGN.md lists `--select-font`/`--dropdown-menu-font` "pinned to mono" in the demo's sanctioned narrow override surface (both read). Surviving work: delete the dead class ×3. |
| **K-8 · C-4 rider** | "mark M1 (passive `DarkModeToggle`) can be withdrawn rather than folded." | Ruled against reader-DU's endorsement: the checkbox-item swap changes no producer API and does not touch the double-toggle hazard M1 answers (the W44 comment, still true — see superlatives). M1's fate belongs to the BH relay, not to this consumer edit. |
| **K-9 · L-3 rider** | "SIX missing subpaths … close the last root-barrel reach." | Five, not six: `demo/ui/input/index.ts` re-exports from `@mkbabb/glass-ui/forms` — a published subpath (read) — and is the counter-example to L-3's own "every file" claim. Ask = avatar, alert, checkbox, radio-group, skeleton. |
| **K-10 · L-8 count** | "21 `useGlobalDark()` call sites." | My grep: **10** invocations across **9** files. The 21 conflates import specifiers with invocations. The singleton claim and the MT-F009 re-scope survive with the corrected number. |
| **K-11 · C-1 durability limb** (this seat's own, against both readers) | "…permanently, until reload." | The stale entry is stamped with the OUTGOING `darkClass`; the first recompute after the class lands mismatches the key and re-probes (:242–249, read). L-8's own trace witnesses the ink correct at +600 ms. What is true: nothing GUARANTEES that recompute (an idle document stays wrong indefinitely). "Permanent" and "transient" are both unproven as stated → U-2; severity demoted to MAJOR. |

## Superlatives — with provenance, arbiter-checked

1. **`.slug-pill` is the file's one genuinely correct piece of system work** (reader-DU): one root recipe (foundation.css:585–587, read) with provenance naming the 5+ sites it replaced — and the ONLY place the `:style="{color, borderColor}"` idiom is live, precisely because the recipe declares `border`. Owner edict 5, executed.
2. **The dock popup mutex** (reader-DU): Dock.vue:65–69 (read) calls `usePopupMutex` exactly once and derives all four popup models; this component gets a four-way exclusion for free with zero coordination code.
3. **`Avatar decorative`** (both readers): the one producer API in the file consumed exactly as designed, at both sites.
4. **Zero imperative surface** (reader-DU): no listener, timer, rAF, watcher or observer in the component; in a repo whose constellation audit found a ~40-site rAF epidemic, a dock component with nothing to leak is a result. The only animation it hosts is producer-PRM-gated (base-misc.css, read).
5. **`rel="noopener noreferrer"` intact ×4 across both twins with zero drift** (reader-DU) — in a file where three other things drifted between the same two files.
6. **The W6-8/V-W44 dark-mode comment is true and load-bearing** (reader-DU; re-checked against the producer): the no-`passive`-mode double-toggle rationale is the correct call correctly recorded — the exact contrast to the :103–110 comment sitting above a live instance of the defect it declares retired (D-4).
7. **The two-referent principle is right and already proven executable** (both readers): ink-certifies-against-the-surface-it-composites-over is the correct law, and the veil's mounted-element two-layer probe (useContrastSafeColor.ts:154–206, read) is the in-repo exemplar every cure above should generalize — the BLOCKER is a broken referent, not a broken idea.
8. **`demo/color-session/ink.ts` is a clean consumer/producer boundary** (reader-LC): all color math from published `@mkbabb/value.js/{color,css}` subpaths, no deep `src/` reach on this component's graph (imports read), explicit throws naming producer error codes; and **`certifyAccentInk`'s identity short-circuit** returns the author's own serialization when certification changed nothing (ink.ts:138–144, read).
9. **`bumpProbeEpochOnMount`'s `getCurrentInstance()` guard** (reader-LC): composable-safe outside a component, with the comment saying why (:69–78, read) — the idiom is right even where the epoch design around it is not.
10. **The producer's strip set is a principled refusal** (reader-LC): glass-ui declining to let consumers re-root menu items protects reka's roving-focus model; the defect is that the refusal is SILENT rather than typed — which is what turned one attribute into a shipped BLOCKER. That observation is the BH-relay ask in one sentence.
11. **The o18 census exists, settles, and composites a real model** (reader-LC): blind in two nameable ways (both now booked at X-W1) — and a census that can be shown wrong by construction is worth more than no census; it is the only reason C-5's ground defect was auditable at all.

## UNPROVEN-NEEDS-LIVE residue (inputs to the visual-audit pass)

- **U-1** — D-1's ratios: LIGHT Login 3.43:1 · DARK @mbabb 4.18:1 (+ the passing pair). Mechanism confirmed; numbers capture-derived.
- **U-2** — C-1's staleness DURATION (the C-1 "permanent" vs L-8 "reconverges" conflict, K-11). Decider: flip the scheme with the ambient frozen (no color drag, aurora idle) and sample the trigger ink at +0/+1f/+5s/+60s.
- **U-3** — L-1's magnitudes: model α 0.88 vs band α 0.4544; delivered 5.365/8.259 vs the 5.75 target. (The dead-probe half is CONFIRMED statically.)
- **U-4** — L-2's +26.3 % over-certification table and the painted ink's byte-identity with `--accent-live`.
- **U-5** — D-2's row geometry (Δy 15.9 px; 48.9 vs 44.0 px; 55.4 px iPhone 14). The two-line break itself is corpus-visible in both schemes.
- **U-6** — D-15/D-16/D-17 pixel geometry: band chroma collapse, the four seat rects (28 px is token-derived), separator air 15.8/10.2 · 9.8/12.2, hover deltas.
- **U-7** — D-6's rendered 16×24 rect (mechanism certain).
- **U-8** — D-12's offline avatar render (the corpus's `evidence-avatar-offline.png` is the D seat's witness; re-take under the formation harness).
- **U-9** — whether `backdrop-filter` actually reaches the portaled menu subtree: the corpus captures show near-unblurred backdrop text, inconsistent with a live blur at default `--glass-level: 1`. Bears directly on U-10.
- **U-10** — N-3's per-label contrast ratios over the bleed-through backdrop, both schemes.
- **U-11** — D-19's "all 60 captures are logged-out" + `STATES.json` present-but-hidden corroboration.
- **U-12** — C-2's repo-wide `variant=` count (103) and C-3's live regeneration trace (localStorage null; no live region).
- **U-13** — N-2's coarse cell: 14 px glyphs inside 44 px rows beside 24 px producer chrome, playwright-webkit iPhone-14 arm.
- **U-14** — D-13's 200 %-zoom rendered arm (the seam is source-certain; the reflow landing is capture-derived).

## Closing verdict

**ADJUDICATED — the corpus substantially survives hostile re-reading: 31 rows confirmed (2 BLOCKER-weight: the dead `chrome` probe C-5/L-1(a), already named by X.W8's G-13, and the strip-set-broken GitHub row D-2), 12 reader-missed defects adopted (5 MAJOR, all arbiter-verified in producer/consumer bytes), 11 claims or limbs killed with written refutations (including one BLOCKER durability limb this seat killed against both readers), 2 rows identity-guarded to their prior folds rather than re-booked; every disposition routed (X-W1 ·X-W4 · X-W8 · X-W10 · BH relay · 4 NO-WAVE-OWNER rows named for the next formation boundary) and 14 numeric residues handed to the live pass — nothing verified, nothing stamped, the dated evidence untouched beside this file.**

— end of apotheosis. The r2 challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
