claude-fable-5

# kf-KeyframeCard — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/components/instrument/keyframes/KeyframeCard.vue` (81 L, one mount path — the spring scene) — reader-1 ("design-and-usage": 24 confirmation cells · 3 kills · 12 misses · 9 superlatives) and reader-2 ("library-and-code": 33 confirmation cells · 4 kills · 13 misses · 10 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/KeyframeCard/challenge-{D-design,L-library,C-consumption}.md`, 507/451/341 L, all opus-5[1m]).
**Method (incredulity)**: every cross-reader disagreement (D-11, L-2, D-5's severity+backdrop, the D-10 paint consequence, C-9-vs-L-14) and every load-bearing miss was re-derived against the bytes with this seat's own instruments before adoption. Two reader confirmation cells were **REVERSED** (reader-2's D-11; reader-1's L-2). **Zero browser tooling** (law); all rulings static + installed-dist + git reads; the irreducibly visual residue rides to SS-13. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only)**: full read of KeyframeCard.vue (whole, 81 — every cited anchor checked: `:2` bare `grid`, `:3-8` the Input with all 18 classes, `:11-13` the overlay **without** `pointer-events-none`, `:14` gapless flex, `:15-19` the T.D7 comment citing `proof:accent-census`, `:20-25` the bare `<X @click>`, `:26` CopyButton fed `frameString` no label, `:28-39` the ghost stack with `pointer-events-none` at `:29`, `:41-50` the `<pre>` incl. the `:50` `><code>` broken start tag, `:62-67` props, `:69-74` emits, `:76-80` expose); KeyframeCardList.vue (whole, 82 — `:5` guarded `frames[i]?.id` vs `:11` unguarded `.toString()`, `:35` `frames: any[]`, `:44-52` formatFn, `:64-67` index-assigned `cardInstances`, `:70-72` null-preserving `cardRefs`, `:76-79` type-guarded `getPreElements`); KeyframesEditor.vue (whole, 284 — `:15/:27` the `:frames` bindings (corpus cites `:14,26` — corrected), `:36-51` the Slider triple, `:87-95` the correct button idiom, `:182-211` onUpdateStart, `:213-216` undebounced animateProgressBar, `:218-230` onKeyDown incl. the `:219` `"Ï"` swallow and `:229` highlightAll-per-keydown, `:232-250` removeKeyframe incl. the `:233-235` silent guard and the `:244-247` awaited AnimationGroup, `:254-258`, `:275-277`, `:279-283`); useKeyframesParsing.ts (whole, 118 — `:48-54` the blank-then-refill, `:62` `debounce(…, 100)`, `:71` the **single** useKeyframeOps invocation, `:86-103` the structural-watcher docblock + `watch(animation.templateFrames, …)`); useKeyframeOps.ts (whole, 220 — `:25-40` withErrorToastAsync, `:87-106`/`:108-144` the two `debounce(…, 1000)` wrappers, `:111` selectorText wrap, `:194-197` the shadowed toast, `:202-204` the array-**replacing** filter); src/animation/internal/helpers.ts:15-24 (**one closure-held `timer`, cleared regardless of arguments** — the shared-timer decider); useHighlightCSS.ts (whole, 148 — `:94-105` ensureThemeStyle **unconditionally** falling through to setCodeTheme at `:104`, `:90` the full-theme textContent write, `:108-113` setHighlightingString's **falsy `""` marker**, `:116-126` the guard/write/marker at `:117/:123/:124`, `:132-138` highlightAll opening with ensureThemeStyle); contenteditable.ts (whole — 4×U+00A0 `insertNode`, no input event); format.ts:100-146 (CSSKeyframesToStrings emits `selectorText(start)\n{body}`; formatCSSKeyframeString strips `^[^{]*{`, indents, `}\s*$` — **structural, not whitespace**); SpringPhysicsFacet.vue `:98/:113/:119` mount + `:230-241` scoped CSS verbatim (`.keyframes-section { min-width: 0 }`; `.keyframes-editor-scroll { max-height: 26rem; overflow-y: auto; container-type: inline-size }` — the containment decider); useSpringKeyframesEditor.ts:57 `markRaw(...)`; demo/app/index.html:6 (viewport meta clean — no `user-scalable=no`/`maximum-scale`); KeyframesAddDialog.vue `:33-39` (the ARIA-stripped `<pre>` twin) + `:92/:103-104/:122/:152/:158` (setHighlightingString + its own highlightAll paths); instrument/keyframes/index.ts (the 3-export lazy barrel) + importer grep → **0**; useTimeline.ts:68-88 (the "100ms debounce" docblock + `debounceFilter(100)`); installed vue **3.5.35** `@vue/reactivity/dist/reactivity.cjs.js:1844-1856` read verbatim (plain-array source → multi-source; plain-object element → `warnInvalidSource`, no dep — the dead-watcher decider); value.js src/css/types.ts:42-44 + grammar.ts:409/:415 (bare-literal `KeyframeSelector` — the D-1 root); kf src/animation/constants/types.ts:64-67; `respectReducedMotion` grep (group.ts:57 default false · lifecycle.ts:80 the group-path consult · **view-transition.ts:197 defaults TRUE** — a fact neither reader nor the banked KF-CB-6 carried · demo consumers **0**); `data-destructive` grep → exactly 1 (`:22`); `e.key === "Ï"` grep → **2 sites** (KeyframesEditor.vue:219 + KeyframesStringControls.vue:91 = banked KF-CE-35); installed glass-ui 7.0.0: field-control.css focus-visible/background-shorthand/backdrop-filter lines read verbatim, `glass-ui.css` byte-scan reproduced (`@layer components{` at **2531**, `.glass-label[data-v-87831917]` at **21050**, net brace depth **0** — genuinely unlayered), `styles/index.css` import order (defined.css early via glass.css; `../glass-ui.css` near the tail), and one precision correction to reader-1: `glass-defined` has **1** occurrence in glass-ui.css — the custom property `--glass-defined-plate: var(--control-surface-bg)`, not a selector, which *supports* the D-14 plate-reset mechanism rather than weakening it.
**Substrate disclosure — the four-layer ruling (re-probed this session; the kf-App/SCH-1 and kf-CopyButton precedents consulted BEFORE ruling)**: local HEAD `8281638c` on `master` is the **same divergent stale fork** the kf-CopyButton apotheosis banked — 41 commits behind `origin/master` `81a56990`, 252 dirty rows. (a) **HEAD**: value.js `^3.1.0`; glass-ui `optionalDependencies: 6.0.0` **with 3 consistent lock entries** — reader-1's "at HEAD grep → 0" is a worktree measurement wearing HEAD's name; per the banked K3 correction, `npm ci` at HEAD silently **downgrades** the installed 7.0.0, it does not fail. (b) **Worktree** (what all three axes and both readers audited): the in-flight Value-4 migration — value.js pinned 4.0.0, the glass-ui declaration **deleted**, lock hits 0. KeyframeCard.vue and KeyframeCardList.vue are byte-identical HEAD↔worktree; the emit-sink chain (KeyframesEditor.vue, useKeyframeOps.ts, useKeyframesParsing.ts) is **dirty** — those anchors are worktree-only. (c) **Frontier `origin/master`**: glass-ui **7.0.0 exact** + value.js **4.0.0**, **and the FE-3 repair is already landed** (commit `add20b7e`): KeyframeCardList `:11` reads `startScalar(frames[i].start)` with a comment naming the `[object Object]` leak verbatim ("10 shipped labels on /#/spring"), and KeyframeCard itself carries a defensive `displayStart` computed. **The corpus's №1 blocker is partially cured upstream and neither reader ran the one `git diff origin/master` that shows it.** (d) Frontier durability probed row-by-row: the `:50` blank-then-refill **survives** at the frontier; the Slider unit disagreement (`:38/:43/:48/:208`), the `"Ï"` swallow (`:219`), highlightAll-per-keydown (`:229/:276`), and the bare `<X>` all **survive**; the FE-3 cure itself is **partial** — `String(selector.value)` renders the **fraction** `"0.5"` where the commit path demands `"50%"`, a named selector still falls through to `"[object Object]"`, and `selectorText` (the tested, canonical renderer) remains bypassed at both frontier seams. All line anchors below are worktree-at-`8281638c`; re-resolve before any cure (D-19).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**53 adjudicated roster rows — 6 BLOCKER · 21 MAJOR · 21 MINOR · 5 INFO.** **7 killed-claims register entries, all SUSTAINED** — two of them **reversing a reader's confirmation cell** (reader-2's D-11, on a CSS-Grid containing-block error; reader-1's L-2, on worktree-as-HEAD provenance). **17 missed rows ADMITTED into the roster** (+2 admitted as pure identity folds), chief among them the shared-debounce data-loss bug, the dead structural watcher with a docblock asserting the opposite, the dead click zone over the code's corner, and the second PRM-unreachable engine animation gating the destructive path. **Identity guards held — zero re-bookings**: the bare-X destructive control = **KF-CB-11**; the CopyButton family (L-6/D-19/L-8/D-26) folds to **KF-CB-2/6/16/22/3/9/12**; the glass-ui dependency family = census **SCH-1** (kf-App ruling 1 → KF.W0); the `"Ï"` dead-key = **KF-CE-35** second site; forced-colors = **KF-CE-13**; the KeyframesAddDialog twin rides **kf-CSSPasteDialog R-7**; D-24's ".tap-floor house idiom" premise corrected by banked **KF-CB-25** (the utility is dead CSS).

Blocker-weight facts: **KF-KC-1** (`[object Object]`) is real at the audited bytes and **PARTIALLY CURED AT THE FRONTIER** — the terminal risk inverts from "fix the string" to (i) the fork reconciliation must not re-land `.toString()` over FE-3 and (ii) FE-3 itself is a half-cure (fraction-for-percent, named-selector fallthrough, canonical renderer still bypassed). The live frontier-frontmost defects are **KF-KC-6** (the focused editor is unmounted mid-edit, frontier-durable), **KF-KC-2** (the keyboard-unreachable delete, frontier-durable), and the editing-integrity cluster KF-KC-20/23 (a keystroke can destroy the caret; a second card's edit can silently discard the first's).

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-11 (sticky glyph collision)** | KILLED — grid row auto-sizes to the Input; sticky travel is zero | CONFIRMED MAJOR — "the containing block is div.grid, which spans BOTH rows" | **Reader-1 SUSTAINED; reader-2's cell REVERSED** | css-grid-1 §9.1: an in-flow grid item's containing block is **its grid area**, not the grid container. `:2` is a single-column, auto-row grid with exactly two items; row 1 auto-sizes to the Input's own outer height, so the sticky constraint rectangle equals the box and travel is **zero**. The mechanism cannot occur; `sticky top-0` is a dead utility (rostered, KF-KC-29) whose only live effect is making the element positioned so `z-modal` applies. A 30-second SS-13 glance is kept as courtesy confirmation of a spec-decided fact. |
| **L-2 (phantom glass-ui imports)** | CONFIRMED BLOCKER "at HEAD" | KILLED — the absence exists only in the uncommitted worktree; HEAD declares optionalDep 6.0.0 | **Reader-2 SUSTAINED; reader-1's cell REVERSED** | My git reads: HEAD `package.json:71` `"@mkbabb/glass-ui": "6.0.0"` (optionalDependencies), HEAD lock glass-ui count **3**; worktree deletes both. Identical to the banked kf-CopyButton K3/SCH-1 ruling, which also corrects reader-2's residue mechanism (npm ci at HEAD **downgrades**, does not fail). Frontier: 7.0.0 exact — cured. ONE identity = census SCH-1 → **FOLD by reference to kf-App ruling 1 / KF.W0**, zero re-booking. Reader-2's separately-filed optionalDep-drift missed row is the **same identity** and is folded, not rostered. |
| **D-10/L-10/C-5 — the z-modal paint consequence** | KILLED via `container-type: inline-size` at the mount site; contract breach demoted MINOR | contract breach MINOR; paint consequence left UNPROVEN-NEEDS-LIVE | **Reader-1 SUSTAINED and EXTENDED; the SS-13 probes are STRUCK** | SpringPhysicsFacet.vue:233-240 read verbatim: `container-type: inline-size` ⇒ `contain: layout style inline-size`; **layout containment creates a stacking context and a containing block for positioned descendants** (css-contain-2 §3.2). z-index 140 is scoped inside the 26rem band and cannot paint over KeyframesAddDialog, the Drawer, SharePopover, or the dock. Both readers' open live probes resolve statically to NO. The token-contract breach survives at MINOR (KF-KC-28), consistent with the kf-DemoGlobalChrome D-6/L-9 latent-breach precedent. |
| **D-5 — severity and backdrop** | MAJOR; composited against the **card** plate | BLOCKER; backdrop corrected to the **hljs** plate (`#ffffff`/`#0d1117`) | **Backdrop correction ADOPTED (reader-2); severity RULED MAJOR (reader-1); reader-2's BLOCKER preserved as dissent** | Structure: the ghost stack lives inside the `:11-13` overlay, absolutely positioned over the `<pre>` — the hljs plate is the real ground (D-7's own hexes). Both arithmetic sets fail 4.5:1 and the 3:1 floor regardless of ground (25% ink caps the achievable ratio ≈4:1) — the FAIL verdict is token-decided. Severity: the readout is authored as a watermark (`opacity-25 italic pointer-events-none`), its `s` line mirrors the Input, and its one unique datum (`f {index}`) is itself the mis-designed 0-based ordinal (KF-KC-32) whose cure is the same metric/chip redesign (KF-KC-30). One spec, MAJOR weight. |
| **D-15 — what a long line actually breaks** | CONFIRMED with corrected consequence (band-wide horizontal scrollbar) | KILLED as filed ("escapes the card"), residue rebooked | **CONVERGENT — CONFIRMED-AS-RESCOPED (MAJOR)** | The two returns agree at the bytes and differ only in bookkeeping. My read: `overflow-y: auto` forces `overflow-x` visible→auto (CSS Overflow 3) and inline-size containment pins the band, so nothing escapes; the true consequence is a **two-axis scroll of the entire 26rem editor band**, dragging the sticky footer (slider + toolbar) and — reader-2's residue, adopted — pinning the `right-4` control overlay to the **widened** containing block so delete/copy scroll out of view. One row, both consequences (KF-KC-14). |
| **C-9 vs L-14 (what copy copies)** | filed both as minors, L-14 framing | KILLED C-9's mechanism; L-14 supersedes | **Reader-2 SUSTAINED** | format.ts:135-146 read verbatim: `.replace(/^[^{]*{/, "")` deletes the **selector**, plus both braces — a structural difference, not the "pure-string trim" C-9's hedge leaned on. L-14 is the correct booking (superset copied under a generic name); C-9's independent debounce-window half (copy within 1000 ms yields pre-edit text) survives folded into it (KF-KC-40). |
| **D-8/D-18 provenance** | flagged D's line cites as "off" | proved them wrong (`:94-105` = ensureThemeStyle; write `:123`, marker `:124`) | **CONVERGENT — provenance VOID, substance intact** | My read of useHighlightCSS.ts confirms: axis D cited the head-`<style>` singleton function for the innerHTML write, twice. Any repair driven off D's numbers edits the wrong function. Both readers' `:116-126` cites are exact. Recorded in the killed register (#7); the file is clean in the worktree, so this is citation error, not drift. |
| **L-4's practical exposure** | adopted L-4's "interlock" framing whole | scoped it down per C-6 (the remount pre-empts most orphan writes; the durable half is the one-shot marker) | **Reader-2's scoping ADOPTED** | The L-3 blank-and-remount hands the highlighter a fresh unmarked `<pre>` roughly once per commit cycle, so the orphaned-vnode half is mostly masked; what is permanently live is the marker making every keydown/`watch`-driven highlightAll a no-op per element, plus the first-keystroke caret destruction this corpus's reader-1 newly found (KF-KC-23). L's R-5 kill (formatFn resolves off the warm engine before bootHighlighter's four imports) re-endorsed by both readers and left standing. |
| **respectReducedMotion grep cell** | (did not run it) | "grep returns ONLY the declaration — zero consumers" | **Substance SUSTAINED, cell CORRECTED** | My grep: ~10 hits — the declaration, lifecycle.ts:80 (the group-path consult), constants/defaults.ts:87, types.ts:152/:202, and **view-transition.ts:197 where the engine's newer organ defaults it TRUE**. The true claim is "zero **demo** consumers set the group flag" — verified. The view-transition default-true fact **sharpens** the dogfood indictment: the library already knows the honest default and its flagship demo uses neither (KF-KC-27; identity family KF-CB-6). |
| **D-16/D-17 coverage** | absent from reader-1's return entirely (neither confirmed, killed, nor missed) | both confirmed at demoted MINOR | **Reader-2 ADOPTED; reader-1's silent drop recorded as a correction** | D-16 (physical `right-4`, nothing mirrors): real, latent — `<html lang="en">`, no `dir`, no i18n layer; the axis's own MINOR-if-LTR-scoped falsifier effectively fires. D-17 (root grid gap 0 against gap-4/gap-1 neighbours): real proportion inversion, no functional consequence. Both MINOR (KF-KC-46/47). |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md:179-206`: W0 Substrate Settle §B-12 · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**. Glass-ui **producer** defects ride the standing glass-ui BH relay (owner edict), never a local patch. Routings are bookings, not acts.

### BLOCKER

- **KF-KC-1 · D-1 = L-1 = C-1 — the primary datum renders `[object Object]`; the stringly-typed prop is the second half of the hole.** Chain re-derived whole by this seat: value.js types.ts:42-44 (plain Readonly union, no toString) ← grammar.ts:409/:415 (bare literals) → kf types.ts:64-67 → KeyframesEditor.vue:15/:27 (raw `templateFrames`; the corpus's `:14,26` cites corrected) → KeyframeCardList.vue:35 `frames: any[]` + `:11` `.toString()` → KeyframeCard.vue:65 `frameStart: string`, rendered `:5` and `:37`. The canonical renderer `selectorText` is shipped, tested green (`value4-editor-boundary.test.ts`), and bypassed. **TRUE at the audited bytes; PARTIALLY CURED AT `origin/master`** (this seat's probe, commit `add20b7e`): frontier list `:11` = `startScalar(frames[i].start)` + a `displayStart` guard in the card — but the cure renders the **fraction** `"0.5"` where the commit parser (`:206-209`) demands `"50%"`, a **named** selector still string-coerces to `"[object Object]"`, and `selectorText` remains uncalled at both seams. → **FOLD the substrate face to KF.W0** (the fork reconciliation must not re-land `.toString()` over FE-3 — low collision risk since neither file is locally dirty, but the guard is the same one EE-01 carries) **+ NO-WAVE-OWNER** for the terminal cure: `frameStart: KeyframeSelector` (or a `selectorText`-fed label), the `any[]` killed, L-15's three-line test as the acceptance gate.
- **KF-KC-2 · D-2 = C-2 = L-13 = banked KF-CB-11 (identity fold — never re-booked).** The card's only destructive control is a click handler on a bare Lucide `<svg>` — unfocusable, roleless, unnamed, outside the toolbar cohort's `button` query. Already rostered from the CopyButton corpus as KF-CB-11 with the same anchors (`:20-25`); this corpus adds the three-axis proof (lucide defaultAttributes verbatim; the self-condemning `:87-95` sibling; reader-1's verification that class/@click DO reach the svg via functional-component fallthrough, so the affordance is live for mouse users only). Frontier-durable (template untouched in the FE-3 diff). L's MINOR filing under-priced it; C/D's BLOCKER stands. → **KF-CB-11's NO-WAVE-OWNER routing carries**; the cure (a real `<button class="focus-ring">`) also discharges KF-KC-34 free.
- **KF-KC-3 · D-3 = C-4a — the offset Input has no accessible name at all.** `:3-8` passes only class/model-value/handler; glass's Input synthesizes nothing (dist read); the two `<Label>`s carry no `for` and the input no `id`. WCAG 4.1.2/1.3.1 (A). Both readers convergent; verified. → **NO-WAVE-OWNER** (SS-1/SS-2: `aria-label` or the `labeled-field` primitive glass already ships).
- **KF-KC-4 · D-4 = C-3 — no visible focus indicator in any ordinary mode.** Three `@layer utilities` classes (`shadow-none`, `border-none`, `focus:*`) cancel the whole `@layer components` focus affordance; `outline: none` from field-control survives uncontested. Cascade decided by layer order (utilities last, style.css:1), landing site verified (`cn("field-control glass-defined", h.class)` on the `<input>`). The same file pairs `outline-none focus-ring` correctly on the `<pre>` 40 lines away — omission, not house style. The forced-colors-only survivor arm folds to **KF-CE-13**. → **KF.W6** (delete the negating utilities / request the bare variant from glass rather than erasing field-control per the root-styling law) — one cure with KF-KC-13.
- **KF-KC-5 · D-6 — Tab (and Shift+Tab) trapped in the contenteditable, no advertised escape — WCAG 2.1.2 (A).** `:44` → KeyframesEditor.vue:224-227 `e.preventDefault()` + 4×U+00A0 insertion; no Escape/release path anywhere on the chain (verified). Only axis D saw it; both readers confirmed. Class note: KeyframesAddDialog carries its own Tab trap (banked in kf-CSSPasteDialog's instruments, `:117-120`) — the cure should be one shared idiom. → **NO-WAVE-OWNER** (Escape-releases-focus + on-screen advertisement, both editing hosts).
- **KF-KC-6 · L-3 — the focused card is unmounted mid-edit: the list is emptied and refilled on every commit cycle.** useKeyframesParsing.ts:48-54 verbatim: the `:50` ref-blank queues Vue's flush strictly before the `:51` await's continuation; the v-for renders zero rows; every card unmounts; N prettier passes later fresh cards mount. Keys cannot save a child across an empty render; the card (`:55-81` = the whole script) has no defense and, stateless, cannot build one. **Frontier-durable** (`:50` survives at origin/master — probed). Interlocks: the only reason KF-KC-15's orphan half stays masked, and the amplifier for KF-KC-20/23 and the CopyButton churn (KF-CB-16/22). → **NO-WAVE-OWNER** (single-assignment cure: build the new strings, assign once; the blink and the unmount die together). Perceptual blink duration → SS-13.

### MAJOR

- **KF-KC-7 · D-5 as-ruled — the ghost readout fails contrast ~2.6× (≈1.70-1.71:1 light / 2.00-2.04:1 dark vs 4.5:1), composited against the hljs plate.** Backdrop corrected per the ruled table; both readers' independent arithmetic adopted as concordant bounds; the failure is token-decided, the second decimal SS-13. Reader-2's BLOCKER as dissent. → **NO-WAVE-OWNER** — one readout-redesign spec with KF-KC-30/31/32 (metric/chip at honest opacity, or delete the mirror).
- **KF-KC-8 · D-7 — the unlayered hljs `<style>` defeats `bg-transparent`: hardcoded `#ffffff`/`#0d1117` plates inside the warm card.** Mechanism verified (bare head `<style>`, textContent = raw theme; `.hljs` + `bg-transparent` on one element at `:45`; zero `.hljs` re-declarations in demo). D's line cites corrected to `:94-105/:90` (injection) — the killed register #7. → **NO-WAVE-OWNER** (layer the theme injection — `@layer` wrapper or tokenized theme), one spec with KF-KC-9.
- **KF-KC-9 · D-8 — the code editor renders CSS in proportional sans once highlighted.** `text-small` (utilities) beats preflight's base-layer mono on the `<pre>`; the base-layer-mono `<code>` is destroyed by the `:123` innerHTML write (no `<code>` in hljs output); highlighting is not opt-in (`:229`, `:275-277`). Reader-2's strengthening adopted: the themes' `pre code.hljs { overflow-x: auto }` never applied even pre-highlight — the `<code>` never carries `.hljs`. Falsifier discharged (0 font-family declarations in either theme). → **NO-WAVE-OWNER** (a winning mono rule on the `<pre>`; with KF-KC-8).
- **KF-KC-10 · D-9 — glass-ui's unlayered `.glass-label` SFC-scoped rule defeats 3 of the 4 typography utilities on the ghost.** Byte-scan reproduced by this seat: single `@layer components{` 2531-18827; `.glass-label[data-v-87831917]` at 21050, net depth 0. `--font-text` beats `text-mono-small`, 500 beats `font-light`, 1.4 beats `leading-none`; only `tabular-nums` survives, on a proportional face. **GLASS-OWNED** — the producer emits scoped component styles unlayered, silently outranking every consumer utility; this is the cascade-layer face of the census token-collision lane. → **glass-ui BH relay** (owner edict; request layered emission of scoped styles) + consumer residue rides **KF.W6**; no local `:deep` patching meanwhile.
- **KF-KC-11 · D-12(a)(b)(c) + reader-2's undo sharpening — the destructive X floats over the code's first line, has no confirm and no undo, and is silently inert at n=1.** (a) `absolute top-2 right-4` over the `<pre>`'s line 1 (subsumed by KF-KC-22's sharper container finding); (b) no dialog/undo on the chain `:21` → `:232-250` → ops`:193-210` — **and the demo DOES own an undo** (`useRefHistory` at useTimeline.ts:77-88, Mod+Z wired) that is bound to the timeline's own state, not `templateFrames`, so the card's delete sits outside the app's only undo trail (verified at the docblock); (c) KeyframesEditor.vue:233-235 returns early **before** ops:194-197's "Cannot remove last keyframe" toast can fire — the codebase has the message and shadows it — and the card's props can't express a dead state. → **NO-WAVE-OWNER** (`canRemove` prop + confirm-or-undo decision); the undo-trail unification question routes **KF.W7**.
- **KF-KC-12 · D-13 = C-4b (+ reader-1's numeric-affordance miss merged) — glass's entire invalidity channel is shipped on the exact subpath the card imports, and none of it is used; errors land in a detached toast.** `invalid` → aria-invalid/data-state + destructive border + `--invalid-ring`; `useUserInvalidAria` in forms.d.ts; the card passes nothing, and KF-KC-4's utilities would cancel the invalid paint even if it did. Merged facet: no `inputmode`/`pattern`/`min`/`max`/step on a percentage field — mobile gets a full alphabetic keyboard; the only coarse control is a Slider whose units disagree (KF-KC-19). → **NO-WAVE-OWNER** (one field-contract spec: name + invalid + inputmode + described-by diagnostic).
- **KF-KC-13 · D-14 — every plate token is stripped but `backdrop-filter` and the pill radius survive: a filtered, edgeless, padding-less smudge with no editable affordance.** Reader-1's attack-and-fail adopted with one precision correction (this seat's): `glass-defined` occurs **once** in glass-ui.css as the custom property `--glass-defined-plate: var(--control-surface-bg)` — set inside the very rule whose `background` shorthand resets `background-image`, then killed by `bg-transparent`; the plate is doubly dead, confirming the kill-refutation. Blur real (`--glass-blur-quiet`, `@property --glass-level` initial 1). With KF-KC-3/4 the field is undiscoverable by sight, keyboard, and AT simultaneously. Perceived strength → SS-13. → **KF.W6** (one cure with KF-KC-4: stop erasing the control, or request the variant).
- **KF-KC-14 · D-15 as-rescoped — `white-space: pre`, no `overflow-x`, no `min-w-0`: a long declaration turns the whole 26rem band into a two-axis scroller.** Containment verified at the mount site (ruled table); consequences merged: the sticky footer + toolbar scroll off horizontally, and the `right-4` overlay pins to the widened containing block so delete/copy leave the visible band. → **NO-WAVE-OWNER** (`overflow-x-auto` + `min-w-0` on the `<pre>`, or wrap). Live paste-check → SS-13.
- **KF-KC-15 · D-18 = L-4 = C-6 — Vue and highlight.js co-own one subtree; the one-shot marker makes the first highlight permanent and orphans Vue's patch target.** `:117/:123/:124` verified; exposure scoped per the ruled table (the durable half is the marker + KF-KC-23's caret kill; the orphan half is mostly pre-empted by KF-KC-6). The card's `defineExpose` contract says nothing about write authority — the structural fact underneath all of it. → **NO-WAVE-OWNER** (ownership spec: either hljs owns the subtree via `setHighlightingString`-on-change with the marker cleared on edit, or Vue owns it and highlighting re-keys the node; one decision shared with KF-KC-23/26's caret and KF-KC-41's marker constant).
- **KF-KC-16 · L-5 — iOS Safari auto-zooms on focusing the `<pre>`: `--type-small` floors at 14px < 16px; the repo's own clamp has exactly one consumer and it is the sibling editor.** Token chain verified to scale.css; the last falsifier closed by this seat and both readers independently: index.html:6 has no `user-scalable=no`/`maximum-scale`. **Fully decided statically.** → **NO-WAVE-OWNER** (route the editing host through `clampIOSNoZoomFontSize` or a `.ios`-scoped 16px floor).
- **KF-KC-17 · L-7 — the keydown pass-through injects U+00A0 into CSS and bypasses the card's only model channel.** `range.insertNode` fires no `input` event, so `:43` never runs — the tab exists only in the DOM until the next remount erases it; the next real keystroke hands 4×NBSP (not CSS whitespace) to `parseAnimationCSS` via ops:111. Repo grep: one hit = the insertion site. → **NO-WAVE-OWNER** (parent dispatches a synthetic InputEvent, or the card exposes `syncFromDOM()`; the NBSP choice itself re-examined — real spaces survive `white-space: pre`).
- **KF-KC-18 · C-7 — no commit seam in the emits contract; the one consumer builds three inconsistent settling policies.** Per-keystroke emits (`:6`, `:43`); consumer: 1000ms-debounced commit, **undebounced** `animateProgressBar` constructing a fresh engine animation per keystroke (`:213-216`, `:254-258`), undebounced parse+toast (`:182-211` — typing `50%` toasts twice en route). Input-side falsifier discharged at dist (`useVModel` passive, no internal debounce). → **NO-WAVE-OWNER** (commit/blur seam in the card's contract; the progress-bar animation moves behind it), with KF-KC-38/39.
- **KF-KC-19 · C-12 — one field, two units: the Slider reads a 0..1 fraction onto a −10..110 rail and writes 0..110 back; the card's commit path writes fractions; both mutate a `Readonly` in place; `.value` does not exist on the named variant.** Verified at `:36-51`/`:206-209`; `percentSelector`/`selectorPercent` shipped and bypassed at both seams. **Frontier-durable** (probed: `:38/:43/:48/:208` all survive — FE-3 did not touch the slider). Thumb-position visual → SS-13. → **NO-WAVE-OWNER** (route both seams through the total pair; same family as KF-KC-1's cure).
- **KF-KC-20 · reader-2 miss M1, ADMITTED — the 1000 ms commit debounce is ONE shared timer for every card: editing card A then card B inside a second silently discards A's edit, and A's `<pre>` is then repopulated from the unmodified model.** Verified end-to-end by this seat: helpers.ts:15-24 is a single closure-held `timer` cleared regardless of arguments; `updateAnimationFromKeyframeString = debounce(fn(keyframeString, frameIx), 1000)` (ops:108-144) is created **once per editor** (parsing.ts:71 → useKeyframesEditor → one editor). The classic debounce-over-an-identity-argument data-loss bug; all three axes saw the 1000 ms and none saw the sharing. Cross-ref: banked KF-CE-16 indicts the same helper's arity from CSSCodeEditor — one helper, two banked defect classes. → **NO-WAVE-OWNER** (per-frame timers keyed by `frameIx`, or flush-on-switch).
- **KF-KC-21 · reader-2 miss M2, ADMITTED — the declared "structural projector" watcher can never fire, and its 10-line docblock asserts the opposite contract.** parsing.ts:96-103 `watch(animation.templateFrames, …)` over a **plain** array (markRaw seed verified at useSpringKeyframesEditor.ts:57): vue 3.5.35's doWatch (my read, reactivity.cjs.js:1844-1856) treats a plain array as multi-source and maps each plain-object element through `warnInvalidSource` — no dependency, never fires, dev-warns per frame. Doubly dead: ops:202-204 **replaces** the array reference the getter closed over. Harmless today only because every mutation site also calls updateAllStrings explicitly — exactly what the docblock says the watcher exists to complement. → **NO-WAVE-OWNER** (delete the watcher + correct the docblock, or watch a real source; the dev-console warning count → SS-13 as a one-glance confirmation).
- **KF-KC-22 · reader-1 miss #1, ADMITTED — the control overlay is a dead click zone over the code's top-right corner, far larger than the two icons, and it swallows caret placement.** `:11-13` carries **no** `pointer-events-none`; only the ghost child (`:29`) does. The overlay is a positioned sibling painting above the in-flow `<pre>`; hit-testing lands on a handler-less `<div>` that is not the `<pre>`'s ancestor, so the click dies — no focus, no caret. The ghost's own guard is the tell: the right instinct applied one level too deep. Zone extent depends on the rendered `s …` label (currently the 15-char literal) → measure at SS-13 before/after KF-KC-1. → **NO-WAVE-OWNER** (`pointer-events-none` on the container + `pointer-events-auto` on the icon row).
- **KF-KC-23 · reader-1 miss #2, ADMITTED — the first keystroke in any freshly-mounted card destroys the caret, and KF-KC-6 makes every card fresh roughly once per commit.** `:229` highlightAll on every keydown; a just-remounted `<pre>` has no marker, so `:120-124` replaces the focused host's entire subtree (`innerHTML`) — selection collapses mid-typing. Static mechanism; the keystroke-vs-`.then()` ordering is warm/cold-dependent → SS-13 (type one character into a fresh card). → **NO-WAVE-OWNER** (same ownership spec as KF-KC-15; never innerHTML a focused editing host).
- **KF-KC-24 · reader-1 miss #3, ADMITTED — highlightAll rewrites the full hljs theme `<style>` on every keystroke.** Verified: `:132-138` opens with `ensureThemeStyle()`, whose existing-element branch still falls through to `setCodeTheme()` (`:104`), which writes the entire theme string into `textContent` (`:90`) — a stylesheet re-parse + document-wide recalc per keydown on the demo's heaviest editing surface (`:229`, `:275-277`). Wall-clock → SS-13. → **NO-WAVE-OWNER** (write only on theme change; the `watch(isDark)` at `:140` already covers the legitimate case).
- **KF-KC-25 · reader-1 miss #4, ADMITTED — the contenteditable sets no `spellcheck="false"`, `autocorrect="off"`, `autocapitalize="off"`.** `:41-50` verified — red squiggles under every CSS ident; on iOS, autocorrect/autocapitalize rewrite the text before `@input` reads it, compounding KF-KC-16/17 on the same element. The Monaco sibling sidesteps the class entirely. → **NO-WAVE-OWNER** (three attributes).
- **KF-KC-26 · reader-1 miss #5, ADMITTED (CopyButton leg = banked KF-CB-10) — no set-level grouping or naming: N stops produce N unnamed inputs, N unnamed graphics, N identically-named copy buttons.** Card root has no `role="group"`/label; only the `<pre>` carries a per-instance name; the Input (KF-KC-3) and X (KF-KC-2) have none; CopyButton's default "Copy to clipboard" ×N is **KF-CB-10, folded not re-booked** — the group-level consequence (an AT user cannot tell which keyframe any control belongs to) is the new row. → **NO-WAVE-OWNER** (group role + `Copy keyframe ${index+1}`-style names; one spec with KF-KC-3).
- **KF-KC-27 · both readers' misses MERGED, ADMITTED — a second engine-driven, PRM-unreachable animation sits on the destructive path, and the data mutation is awaited on it.** KeyframesEditor.vue:244-249: `await AnimationGroup.of(presets.warpLeft()…, presets.jumpUp()…).play(); removeKeyframeData(frameIx)` — same cascade-unreachability the banked KF-CB-6 proves (identity family; folded, not re-booked), plus three aggravations verified here: the deletion is **sequenced behind** the decorative animation (a reduced-motion user waits out the full warp; a play() failure silently drops the delete — no catch anywhere on the template chain); the targets come from the never-compacted `cardRefs` (KF-KC-51) so a stale index can hand `setTargets` a null; and — this seat's addition — the engine's own view-transition organ defaults `respectReducedMotion: true` (view-transition.ts:197) while `AnimationGroup` defaults false and **zero** demo consumers set it: the honest default exists in-house and is used nowhere on this path. → **NO-WAVE-OWNER** (`g.respectReducedMotion = true`, mutation not gated behind the animation, a catch); the default-inversion letter rides **KF.W5**.

### MINOR

- **KF-KC-28 · D-10 = L-10 = C-5 as-demoted — `z-modal` (140) stamped on an in-card numeric field, against the demo's documented rung semantics.** Letter honoured (semantic utility), intent inverted; paint consequence KILLED (ruled table) — the breach is contractual only, scoped inside the contained band. Precedent: kf-DemoGlobalChrome D-6/L-9 MINOR. → **NO-WAVE-OWNER** hygiene (`z-controls`, decided together with deleting the dead `sticky`).
- **KF-KC-29 · D-20 EXTENDED — the dead/self-cancelling utility census, corrected and enlarged.** Reader-1's row-by-row re-check adopted (aspect-square inert vs explicit block-size; border-transparent/focus:border-transparent cancelled by border-none; focus:shadow-none redundant; font-semibold/stroke-2/m-0 restatements) **plus** reader-1's arithmetic corrections against axis D (18 classes not 20; 11 not 12; 8 rows under a "Seven" heading) **plus** three the whole corpus missed until this round: `sticky top-0` (zero travel — the D-11 ruling), `z-0` (`:29`, statically-positioned element), `justify-center` (`:12`, shrink-to-fit track). The noise is why KF-KC-4/13 survived review. → **NO-WAVE-OWNER** hygiene (delete with the KF-KC-4/13 cure).
- **KF-KC-30 · D-21 = C-8 — two glass `<Label>`s label nothing; reka's Label even suppresses double-click selection; `./metric` and `./chip` ship for exactly this shape.** Verified at dist. → **NO-WAVE-OWNER** (the KF-KC-7 readout spec).
- **KF-KC-31 · D-22 — cryptic `f`/`s` glyphs duplicating the datum in a clashing register.** No legend/title/aria anywhere (grep). The instrument register is spent on the read-only mirror, the body register on the live control. → with KF-KC-7/30.
- **KF-KC-32 · D-23 — the 0-based index published to AT ("CSS for keyframe 0") and in the ghost.** `:49`, `:33`; `:index="i"` 0-based. → with KF-KC-7/30 (name by offset, not ordinal).
- **KF-KC-33 · D-24, premise corrected — 24px destructive target flush against the copy target in a gapless flex.** The "demo's own 44px idiom" leg is CORRECTED by banked **KF-CB-25**: `.tap-floor` has zero adopters — dead CSS, not house practice; what survives is the 2.5.8-boundary math on an irreversible/reversible pair with no gap, and the in-house 32px answer (SharePopover). CopyButton's own target sizing = banked **KF-CB-14** (folded). → **KF.W6** (with the KF-CB-14 family floor).
- **KF-KC-34 · D-25 — hover-only affordance on the X (no focus/active, nothing to attach them to).** Rider on KF-KC-2's cure. → **NO-WAVE-OWNER**.
- **KF-KC-35 · L-9 — `data-destructive` is inert (exactly 1 occurrence repo-wide — re-grepped) and its 5-line justification cites `proof:accent-census`, which does not exist; collides with the owner's standing proof-idiom retirement.** Also kills axis D's S-2 second leg (see killed register #6). → **NO-WAVE-OWNER** hygiene (delete attribute + comment, or land a real consumer — the owner's law says delete).
- **KF-KC-36 · L-11 — `(e.target as HTMLElement)` cast while the typed `preEl` is in hand; repeated across the boundary at `:226`; `useTemplateRef<HTMLElement>` widens an HTMLPreElement.** → **NO-WAVE-OWNER** hygiene.
- **KF-KC-37 · L-12 — the 24 KB root barrel for `Label` beside a subpath import for `Input`, with `./label` at 69 B in the 73-entry exports map.** → **KF.W6** (import-granularity sweep).
- **KF-KC-38 · C-10 — the `remove` emit carries a payload the sole consumer discards (`_e`) and omits the index the consumer reconstructs by closure.** Verified `:72`/`:21`/list `:15`/editor `:232`. → **NO-WAVE-OWNER** (with KF-KC-18's contract fix).
- **KF-KC-39 · C-11 — `formattedCSS`/`updateCSS` hyphenate to `formatted-c-s-s`/`update-c-s-s` at every call site and the deformity has crossed one boundary already.** → **NO-WAVE-OWNER** (rename `formattedCss`/`updateCss`).
- **KF-KC-40 · L-14 (supersedes C-9) — the clipboard receives a structural superset of what the card displays (selector + braces vs body), under the generic default name; a copy inside the 1000 ms window yields pre-edit text.** Strip verified at format.ts:135-146. → **NO-WAVE-OWNER** (`label="Copy keyframe block"` + a feed decision; the label leg lands with KF-CB-10's cure).
- **KF-KC-41 · reader-2 miss, ADMITTED — `setHighlightingString` marks with the FALSY empty string, so the shared idempotence guard does not protect it: the next highlightAll re-highlights and clobbers pre-built markup.** `:110` vs `:124` vs the truthiness reads at `:117/:121` — verified; live consumer KeyframesAddDialog `:103-104` calls both in sequence. The latent opposite half of the one-shot marker. → **NO-WAVE-OWNER** (one marker constant; with KF-KC-15).
- **KF-KC-42 · reader-2 miss, ADMITTED — KeyframesAddDialog ships a near-verbatim clone of the card's editing host with the entire ARIA contract stripped** (`:33-39` verified: no role, no aria-multiline, no aria-label, no focus-ring). The celebrated SUP-5 is a one-off, not a house pattern. Duplication family = banked **kf-CSSPasteDialog R-7** (fold); the ARIA-strip delta books here. → rides the **R-7 consolidation (KF.W8)**.
- **KF-KC-43 · reader-2 miss, ADMITTED — `frames[i]` dereferenced unguarded at list `:11` six lines after `:5` guards the same expression; the two arrays are written by different code at different times.** Honest scope preserved: no proven crash window (the markRaw shrink triggers no flush; the `:50` blank pre-empts). It is also the exact line every axis quoted without seeing the missing `?.`. → **NO-WAVE-OWNER** (dies inside KF-KC-1's typed-prop cure).
- **KF-KC-44 · reader-1 miss, ADMITTED — `min-h-32` reserves 128px of plate per card inside a 416px band: barely three stops visible, most plate empty — the inverse of D-17's proportion complaint.** → **NO-WAVE-OWNER** (size decision at SS-13).
- **KF-KC-45 · reader-2 miss, ADMITTED — `instrument/keyframes/index.ts` is a dead barrel: three `defineAsyncComponent` exports, zero importers (re-grepped), every consumer re-implementing the lazy wrap by hand.** → **KF.W8** (adopt-or-delete).
- **KF-KC-46 · D-16 as-demoted — physical `right-4`, nothing mirrors; latent (lang="en", no dir, no i18n), not shipping.** → **NO-WAVE-OWNER** hygiene (`end-4`).
- **KF-KC-47 · D-17 as-demoted — the root grid declares no gap; intra-card rhythm 0 against gap-4/gap-1 neighbours.** → **NO-WAVE-OWNER** (gap-1/2 with the KF-KC-44 sizing pass).
- **KF-KC-48 · L-15 — zero test coverage in a suite that tests siblings; the L-1 three-line unit test is the causal proof.** Ruled MINOR for consistency with the banked KF-CB-19 precedent (a shared editing leaf with destructive side effects). → **NO-WAVE-OWNER** (the SS-1/SS-2 spec's acceptance test — it gates KF-KC-1).

### INFO

- **KF-KC-49 · reader-1 miss, ADMITTED — an unchecked mutation layer sat between the source and the cascade in all three axis analyses: glass's bespoke `cn` conflict-resolver can silently drop classes** (last-token-wins per group; `text-ellipsis` classified under the `text-color` catch-all; `border-none` escaping conflict resolution by accident). Reader-1 walked all 18 classes and confirmed none is dropped **here** — recorded as corpus method law + a **glass BH relay** note (the lossy group table is producer surface).
- **KF-KC-50 · reader-1 miss, ADMITTED — the `css` class on the `<pre>` is dead**: highlighting is imperative (`language: "css"` at `:122`), auto-detection deliberately abandoned, neither theme selects `.css`. → **NO-WAVE-OWNER** hygiene.
- **KF-KC-51 · reader-2 miss, ADMITTED — `cardInstances` is index-assigned and never compacted; `cardRefs` (`:70-72`) preserves nulls that `getPreElements` (`:76-79`) filters — the null-target root of KF-KC-27's animation leg.** → **NO-WAVE-OWNER** (with KF-KC-27).
- **KF-KC-52 · reader-2 miss, ADMITTED — useTimeline's docblock cites "the existing useKeyframesEditor 100ms debounce" as its correctness keystone; the commit debounce is 1000 ms — the 100 ms it matched is the string-reprojection throttle** (parsing.ts:62), not an edit commit. Verified at both files. → **KF.W7** (docblock correction inside the undo-trail evaluation).
- **KF-KC-53 · C-7's deliberate negative finding, UPHELD and PREDICTIVE — the R1 `parseCssColor` crash class is NOT reachable from this component**: no colour parse; `parseCssScalar` is result-typed and read under `if (!parsed.ok)` with a non-empty diagnostics tuple. Both readers re-verified; this seat concurs from the value.js types. Same posture as banked KF-CB-33 → recorded as a **KF.W3** scoping input.

**Identity folds (zero re-bookings, original ids for life)**: L-6 (unawaited copyText + false announce) → **KF-CB-2** · D-19 (engine bounce PRM-unreachable) → **KF-CB-6** · L-8 (no teardown; 2N parses per remount; uncancelled rAF) → **KF-CB-16 + KF-CB-22** · D-26 (name-swap double announcement; no focus-ring on CopyButton) → **KF-CB-3 + KF-CB-9 + KF-CB-12** · N-identical copy names → **KF-CB-10** · D-24's CopyButton half → **KF-CB-14** (and its .tap-floor premise → **KF-CB-25**) · glass-ui dependency family (L-2's residue, optionalDep drift, 6.0.0-declared-vs-7.0.0-installed) → census **SCH-1** / kf-App ruling 1 → **KF.W0** · the `"Ï"` swallow at KeyframesEditor.vue:219 → **KF-CE-35** second site (the early return additionally skips highlightAll — noted there) · forced-colors residue → **KF-CE-13** · KeyframesAddDialog duplication family → **kf-CSSPasteDialog R-7**.

## Killed-claims register — all SUSTAINED, refutations re-executed by this seat

1. **L-2 (reader-1's BLOCKER confirmation) — KILLED.** "At HEAD" is false: HEAD declares optionalDep 6.0.0 with 3 consistent lock entries (my `git show` probes); the phantom is worktree-only; the frontier carries 7.0.0 exact. One identity with census SCH-1 — folded, never re-booked. Reader-1's cell is the third occurrence of the worktree-as-HEAD provenance failure in this program (after the CopyButton corpus's K1/K2).
2. **D-11 (reader-2's MAJOR confirmation) — KILLED; reader-1's kill sustained.** A grid item's containing block is its grid area (css-grid-1 §9.1); the single-column auto row equals the Input's own height; sticky travel is zero, the collision unreachable. Reader-2's "containing block = div.grid spanning both rows" is spec-false. Courtesy SS-13 glance retained.
3. **D-10/L-10/C-5's paint consequence — KILLED (reader-1 sustained, extended by this seat).** `container-type: inline-size` ⇒ layout containment ⇒ stacking context + containing block at the mount site; 140 is scoped inside the band. Both readers' open live probes resolved NO statically and are struck from the SS-13 list. The MINOR contract breach survives (KF-KC-28).
4. **D-15's "escapes the card / pushes past its container" — KILLED as filed; rescoped row survives** (KF-KC-14: the band becomes a two-axis scroller; footer and overlay consequences adopted from both readers).
5. **C-9's mechanism ("unformatted twin", "whitespace-only divergence") — KILLED.** formatCSSKeyframeString deletes the selector and both braces (format.ts:135-146, my read) — structural difference; L-14 supersedes; the debounce-window half survives inside KF-KC-40.
6. **Axis D's S-2 superlative, second leg ("marked so the red-census can see the role") — KILLED (reader-1's kill sustained at my grep).** `data-destructive` = 1 occurrence, no consumer; `proof:accent-census` is not a script; the justifying comment cites a retired gate and collides with the owner's proof-idiom retirement. The first leg (token-routed destructive colour) stands as a superlative.
7. **Axis D's D-8/D-18 line-cite provenance (`useHighlightCSS.ts:96-104`/`:99-102`) — VOID.** Those lines are ensureThemeStyle; the write is `:123`, the marker `:124` (my read; file clean in the worktree — citation error, not drift). Substance fully intact; recorded because a repair driven off D's numbers would edit the head-stylesheet singleton.

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-2's D-11 confirmation cell — REVERSED** (grid containing-block spec error; killed register #2).
2. **Reader-1's L-2 confirmation cell — REVERSED** (worktree-as-HEAD; killed register #1; the banked SCH-1 ruling was in the registry reader-1 cites elsewhere and was not consulted).
3. **Both readers' frontier blindness — the governing correction.** The corpus's №1 blocker is partially repaired at `origin/master` (FE-3, commit `add20b7e`, with a comment naming the defect verbatim), reachable by one `git diff origin/master -- <file>`; reader-2 established the fork layers and still never probed the frontier; reader-1 probed neither. Second consecutive corpus in this program (after kf-CopyButton) where the most consequential fact lay one git command away.
4. **Reader-1's D-5 backdrop (card plate) — corrected by reader-2's overlay-over-`<pre>` reading; adopted** (the ghost composites against the hljs plate; both failure verdicts unchanged).
5. **Reader-2's respectReducedMotion grep cell ("returns ONLY the declaration") — corrected**: ~10 hits including the group-path consult and view-transition.ts:197's **default-true**; the substantive claim (zero demo consumers) survives and is sharpened by the default-inversion fact.
6. **Reader-1's D-14 "zero occurrences of .glass-defined in glass-ui.css" — corrected to 1** (the `--glass-defined-plate` custom property, not a selector) — a precision fix that *strengthens* the plate-reset refutation it was part of.
7. **Reader-1's silent omission of D-16 and D-17** — two axis rows absent from its return entirely (neither confirmed, killed, nor addressed); reader-2's demoted-MINOR confirmations adopted (KF-KC-46/47).
8. **Both readers' `:14,26` cites for the `:frames` bindings — corrected to `:15,27`** (reader-2 self-corrected; reader-1 inherited the axis numbers in one cell).

## Superlatives — sustained, with provenance

1. **S-1/SUP-1/S★1 · the declared child-ref contract** (`:76-80` → list `:76-79` → editor `:176-178` → useHighlightCSS `:78-80/:132-138`) — re-walked by this seat; the docblock names the bug it killed (D.W3.S1's document-wide sweep); zero `querySelectorAll` in the instrument; the consumer's null-filtering type guard makes it type-honest. The citable template for scoped DOM ownership — even as KF-KC-15 shows the contract's missing clause is *write* authority, not *read*.
2. **SUP-2 · Goldilocks shape** — `:55-81` is the entire script: four props, four emits, one expose, zero lifecycle/state/teardown. Why the KF-KC-6 churn is cheap rather than catastrophic; also, neutrally, why the card cannot defend its own focus — the shape is right and the defense belongs to the parent.
3. **SUP-3 · `innerText`, not `textContent`** (`:43`) — contenteditable materializes line breaks as elements; textContent would destroy the user's structure on the first Enter. The non-obvious correct read; a "reviewer optimization" here is a data-loss bug.
4. **SUP-4 · `String(val)`** (`:6`) — necessary narrowing of glass's `string | number` emit, not nullish laundering; L raised and killed its own hypothesis (R-3), the right discipline.
5. **SUP-5/S★2/S-3 · the complete ARIA textbox contract on a hand-rolled editing host** (`:46-49`) with a per-instance name — rare in the wild; made conspicuous by the naked X six lines up and un-shared with its own sibling (KF-KC-42, the sibling's defect not this card's).
6. **S-4 · `outline-none focus-ring` paired correctly on the `<pre>`** — the demo's single sanctioned idiom, unlayered so it wins, first selector in glass's forced-colors list (per the banked KF-CE-13 census). The direct proof KF-KC-4 is an omission, not a style.
7. **SUP-6/S-2 (first leg) · destructive colour routed through the token bridge** (`text-accent-red` → the demo's one sanctioned red); L's kill-attempt failed and became the superlative. The gate leg is dead (killed register #6).
8. **S-5/S★3 · zero bespoke CSS at the leaf** — no `<style>`, no local cn/cva, no hex, no raw `z-[N]`, all typography semantic rungs; the reason a three-axis static audit was possible. That the shared vocabulary loses four cascade contests is the vocabulary's defect (KF-KC-8/9/10/13), not the card's.
9. **Reader-1's missed superlative, ADOPTED (verified at `:41-50`) — the deliberately broken `<pre>` start tag**: the closing `>` at the head of line 50 (`><code>…`) keeps whitespace out of `white-space: pre` content and survives both prettier plugins. Unremarked by all three axes, each of which quoted the lines.
10. **Reader-1's missed superlative, ADOPTED — the ghost's `pointer-events-none`** shows the author knew floating overlays must not eat clicks; the guard is one level too deep (KF-KC-22) — a near-miss, not ignorance.
11. **CORPUS · axis D's §0.1 byte-offset cascade method** — reproduced exactly by this seat (2531/18827/4325/21050); D-4/7/9/14 all fall out of it. **CORPUS · axis L's killed-hypotheses table (R-1..R-5) and axis C's R1-unreachable negative finding** — both survived both readers and this seat; recording verified negatives is the posture this program pays for.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **The dead click zone's extent** (KF-KC-22): measure the overlay box before AND after the KF-KC-1 repair — the zone shrinks when `s [object Object]` becomes a short scalar.
2. **First-keystroke caret destruction ordering** (KF-KC-23): type one character into a freshly mounted card, warm and cold highlighter.
3. **The KF-KC-6 blink duration** (unmount is static; prettier is module-cached after first pass).
4. **D-14's perceived smudge** (KF-KC-13): the offset field unfocused, light and dark.
5. **KF-KC-14's corrected consequence**: paste one long `transform:` declaration; watch the band's two-axis scroll, the footer, and where the delete/copy overlay pins.
6. **The exact D-5 composite** over the real translucent backdrop (verdict already token-decided; KF-KC-7).
7. **The dead watcher's dev-console `Invalid watch source` emissions** (one glance at mount; KF-KC-21).
8. **Theme-rewrite wall-clock per keystroke** (KF-KC-24): a perf trace while typing.
9. **iOS device confirmation** of the focus zoom + autocorrect rewriting (KF-KC-16/25 — mechanisms closed statically).
10. **Slider thumb positions** for a 50% stop on the −10..110 rail (KF-KC-19).
11. **One AT pass** (VoiceOver): the unnamed Input, the invisible X, the group-less card set (KF-KC-2/3/26) — the highest-value probe.
12. *Courtesy only*: scroll the card list and confirm the offset field never detaches from its own card (the D-11 kill is spec-decided). **STRUCK**: the former L-10/C-5 dialog-and-drawer probes (resolved statically by containment).

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c` (the same stale fork, 41 behind `origin/master`, that the kf-App/kf-CopyButton records banked — re-probed this session): 53 roster rows — 6 BLOCKER (the `[object Object]` datum PARTIALLY CURED AT THE FRONTIER by FE-3, which neither reader found: fraction-for-percent, named-selector fallthrough, and the canonical renderer still bypassed survive upstream; the other five frontier-durable) · 21 MAJOR · 21 MINOR · 5 INFO; 7 kills sustained, two of them reversing reader confirmation cells (a CSS-Grid containing-block error and a worktree-as-HEAD attribution); 17 missed rows admitted — chief among them the one-timer-for-N-cards data-loss debounce, the dead watcher with the false docblock, the dead click zone, the per-keystroke theme rewrite, and the awaited PRM-unreachable delete animation; identity guards held across two banked corpora and the census (KF-CB-2/3/6/9/10/11/12/14/16/22/25, SCH-1, KF-CE-13/35, R-7 — zero re-bookings). The component is the best-shaped 81 lines in the instrument wearing the worst seams in it: its ref contract, ARIA triple, and token discipline are genuinely exemplary, and nearly every failure is a boundary it cannot police alone — a prop typed too weak to refuse poison, a list that unmounts it mid-edit, a highlighter that overwrites its subtree, a cascade that discards its intents, and a destructive control the same author demonstrably knew how to build correctly six lines away. Routings are M-25 agglomeration input: behavioral rows NO-WAVE-OWNER for SS-1/SS-2; substrate/glass/structure folds to KF.W0/W5/W6/W7/W8; the glass-ui producer rows (unlayered scoped styles; the lossy cn table) ride the standing BH relay; visual residue (12 rows) to KF.W9/SS-13. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/keyframes/KeyframeCard.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **KeyframeCard.vue=+16/-3**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

