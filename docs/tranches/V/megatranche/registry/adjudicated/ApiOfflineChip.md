# ApiOfflineChip — adjudicated apotheosis · seat model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited — recorded per M-23/L-18)

**Seat**: fresh-Fable adjudicator (the incredulity seat) over two hostile re-readers.
**Date**: 2026-08-04 · **HEAD at this seat's reads**: `03e4bfef` (branch `tranche-u`; the corpus was authored 2026-07-29 against `c654824e` ancestry — every anchor below was re-resolved by this seat against the working tree, and my own reads are the anchor authority).
**Corpus**: 3 challenge axes against `demo/palettes/browser/status/ApiOfflineChip.vue` (91 L) + its transport/sibling cone — `challenge-C-implementation.md`, `challenge-D-design.md`, `challenge-L-library.md`. **r2 note**: the brief says "r2 governs"; this seat's `ls` of the component directory finds NO r2 files — only the three r1 challenges + `evidence/` (3 PNGs). All three are **EXISTS-ORIGINAL** per `registry/HYDRATION-LEDGER.md` (self-authored seats, not the thin Codex batch), so AXIS-PROMOTIONS does not apply and the r1 corpus IS the governing text. Both readers reached the same conclusion independently.
**Readers adjudicated**: reader-1 (design-and-usage) · reader-2 (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement (8) was re-derived by this seat with its own instruments; one reader-2 census was found undercounted against the producer dist, one reader-2 headline count was refuted by `wc -l`, and one reader-1 provenance cell was corrected against the shipped token files.

**Arbiter's own instruments (all static — zero browser probes, probe parsimony; one fontTools run against shipped producer bytes)**: full reads of ApiOfflineChip.vue (whole, 91 L) and demo/platform/transport/availability.ts (whole, 195 L); DockStatusLamp.vue (whole, 123 L); status-lamp.ts:40-66; client.ts:30-50 (BASE_URL at :37, top-level `initApiEnvironment(BASE_URL)` — reader-2's citation correction confirmed); CurrentPaletteEditor.vue:110-120 + :190-196; the status barrel index.ts (whole, 7 L — incl. the dead `@components/`/`@lib/` doc specifiers); eslint.config.js:228-280 (my census: object-1 = **6** file globs, 2 live `demo/color-picker/**` + 4 dead `demo/@/**`; object-2 = **2** globs, both dead; banned pattern = the W43-killed `@components/custom/palette-browser` alias; `ls demo/@` → ENOENT); o22-status-lamp.spec.ts:8-40 (the `.dev-misconfig-banner` negative watch + the harness-bound concession); test/status-lamp.test.ts:44-48/:115-120/:137-152; utils.css:9-11 (`.fira-code` → `--font-mono`) + :88-108 (`.dashed-well` = flex column, opaque `--well-bg`); foundation.css:328 (`--well-bg` mint), :676-698 (forced-colors tier-1 roster — chip classes ABSENT), :726-740 (`prefers-contrast: more` arm raising `--muted-foreground` to 88% + `--card-edge` to 55%); usePaletteActions.ts:58-78 (the save-P0 unconditional-createPalette comment — the copy-truth decider). Producer bytes: `grep -c -- "--type-mono-caption" dist/glass-ui.css` = **1** (the `.timeline-popover-state` rule, verbatim: `font-size:var(--type-mono-caption,.75rem);text-transform:uppercase;letter-spacing:.06em`); `--type-mono-caption:` declarations across demo/ + src/ + the whole glass dist = **0**; scale.css `:root` declares `--type-admin-label: 0.625rem` AND `--type-micro: 0.6875rem`; `--type-micro` demo consumers = **0**; utilities.css `@utility text-mono-micro { font-family: var(--font-mono); font-size: var(--type-micro); line-height: 1.25; letter-spacing: 0.025em }` and `@utility text-mono-caption { … var(--type-caption) … uppercase … var(--type-tracking-caps) }`; `--type-tracking-caps: 0.1em` declared in **scheme-motion.css** (reader-1 said scale.css — corrected); `--destructive` declared in 3 token files; `--ease-standard → --motion-ease-standard: cubic-bezier(0.4, 0, 0.2, 1)` (scheme-spring.css); `SURFACES: readonly ["glass", "veil", "opaque"]` (axes.d.ts); exports incl. `./badge` `./chip` `./pulse` `./status-dot`; EasingSpecimenStrip.vue:14 `import { Chip } from "@mkbabb/glass-ui/chip"`. **fontTools run (mine)**: `fira-code-latin.woff2` GSUB features = `['calt','dnom','frac','locl','numr']` — **no smcp, no c2sc**. Censuses: `"is unreachable\."` in demo `*.vue` = **7**; chip/lamp/test coverage grep over test/ + e2e/ = **0** hits for `ApiOfflineChip|api-offline-chip|api-misconfig-chip`; `wc -l` over the six-file cone = 91+7+123+65+195+62 = **543**. Canon: VISUAL-CONSTITUTION.md:101/:145/:154, PROPORTION-AUDIT.md:52 (PR-08), ROOT-FINDINGS.md:1442-1457 (**MT-F031** verbatim: the surface "is REMOVED, not restyled and not gated"). Registry/routing cross-reads: Dock.md (MT-DOCK-LAYERS-1 — scope names `DockStatusLamp.vue` + `status-lamp.ts`; D2-14 re-roles the lamp; C-34 books the production zero-live-region truth; G-K(iii)/G-Q), ColorSpaceSelector.md (L-5(a) lint-glob-liveness identity), X/waves/W5.md:274 (C7 counts `DockStatusLamp:70` in the viewport-query subtraction), X/waves/W10.md:295-320 (ApiOfflineChip.vue named in the CONFORMING no-preference baseline), CARRY-CUT-LEDGER X-map, motion-quarantine.md §1 (Guard 1 = animations.css:184-193).

## Scoreboard

**33 rows CONFIRMED (1 BLOCKER · 16 MAJOR · 10 MINOR · 6 INFO) · 13 claims KILLED (incl. one of each reader's own cells, killed by this seat's instruments) · 12 reader-missed rows adjudicated in · 8 UNPROVEN-NEEDS-LIVE residue items → visual-audit inputs.**

Blocker-weight survivor: **AP-1** — the owner-ordered-dead `misconfigured` surface (MT-F031 / OM-5, owner's words "this nonsense needs to be removed", disposition "REMOVED, not restyled and not gated") has a second, live, byte-identical seat in this component, and the component's own barrel comment documents a death that never happened (it was copied, not moved). Both readers, all three axes, and this seat's reads agree on every limb. The deepest truth in the corpus is reader-1's inversion find: **in a production build this suppressed, swatch-gated chip is the app's ONLY backend-offline affordance** — `status-lamp.ts:48` darkens BOTH lamp faces in production, so the dev environment where every axis measured is the only place the redundancy exists, and production is where the hole is.

## Ruled disagreements (the seat's actual work)

| # | question | reader-1 | reader-2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| 1 | `--type-mono-caption` consumer census | 4 sites (3 demo + 1 producer `.timeline-popover-state`) | 3 sites ("re-run over demo/, src/ and glass dist") | **reader-1 — 4 sites** | My grep: `grep -c -- "--type-mono-caption" dist/glass-ui.css` = **1**, rule pasted in the instruments block. The file is minified to one line; reader-2's re-run missed the hit exactly as reader-1 predicted the original axes had. Declarations = 0 everywhere (both readers right). Consequence adopted whole: the phantom is PRODUCER-side, the chip's recipe is a hand-copy of the producer's own broken rule, and a demo-only cure leaves glass-ui broken → the GLASS-OWNED half rides the BH relay. |
| 2 | D-7(b) — the 30 s live-region "flap" | UNPROVEN-NEEDS-LIVE | KILLED from the bytes | **reader-2 — KILLED** | My read of availability.ts: `assertApiAttemptAllowed` (:188-195) never mutates the cell; the failure path re-enters `markApiUnreachable` which re-arms the clock (:170) and assigns `"unavailable"` to a ref already holding `"unavailable"` — Vue's same-value ref write does not trigger; and the chip's `v-else-if="offline"` predicate (:20) stays true regardless, so no unmount/remount exists to announce. The only exit is a genuine recovery (:175-179) — one correct transition. No probe needed; reader-1's agnosticism was over-cautious. |
| 3 | Duplication row severity (C-7/D-5/L-2) | MAJOR | BLOCKER ("531 lines for a read of one enum") | **MAJOR** | The Dock adjudication's R-19 precedent: BLOCKER is reserved for capability loss and silent data-loss-class failures. Both registers render and operate; the defect is structural duplication + drift. The BLOCKER in this cone is AP-1 (owner-ruling violation), not the copying that produced it. Reader-2's headline count also dies — see killed #13. |
| 4 | Phantom-token severity (C-6/L-5/D-3 core) | MINOR | MAJOR | **MINOR** | The rendered output is currently correct (the 0.6875rem fallback is byte-identical to the producer's own `--type-micro` rung), the cure is one line with zero pixel delta, and the class is masking-fallback hygiene. What earns the row its record space is not severity — it is that all three axes AND their unanimous cure were wrong (killed #5). |
| 5 | Dark misconfig ink figure | 3.22:1 (computed from tokens) | 2.42:1 (measured, brightest-2.5% decile) | **BOTH DISPLACED — specified-color band ≈2.7–3.2:1, sub-AA either way** | Consistency the corpus itself demands: reader-1's kill of D §1.3 correctly rules WCAG 1.4.3 is evaluated against SPECIFIED colours, not antialiasing deciles — then reader-2's 2.42 uses the same decile method on the other register. Specified arithmetic: ink `hsl(0 80% 60%)` = (235,71,71) over fill = 0.12·ink + 0.88·ground; reader-2's measured dark ground (66,55,47) → fill (86,57,50) → **CR ≈ 2.7:1**; reader-1's token-derived ground → **≈3.2:1**. Every derivation fails the 4.5:1 floor; the finding stands with an honest band, and the register dies under AP-1 regardless. |
| 6 | L-4 glob counts | "six globs, four dead" (object-1) | "3 of the 4 named dirs gone" | **This seat's census governs: 8 globs across two objects, 6 dead** | eslint.config.js:232-239 = 6 globs (4 dead `demo/@/**`, 2 live `demo/color-picker/**`); :275-276 = 2 globs, both dead. Both readers' carves were partial; the substantive claim is untouched and stronger than either stated: the banned pattern (:248) targets a W43-killed alias so the rule can NEVER fire, and the live consumer (`demo/palettes/browser/card/CurrentPaletteEditor.vue`) matches NO glob in either object — three independent layers of dead law. |
| 7 | "531 lines exactly" (reader-2's re-run) | — | "LIVE `wc -l` re-run this session … = **531 exactly**" | **REFUTED — 543** | My `wc -l`: 91 + 7 + 123 + 65 + **195** (availability.ts) + **62** (useApiClient.ts) = **543**. Reader-2's 160/85 for the two transport files match no file in the tree — a stale or fabricated cell inside a sentence claiming a live re-run. The direction (two full implementations of one affordance) survives on the true numbers; the "exactly" does not. Recorded in DISSENT as an echo-integrity event. |
| 8 | `--type-tracking-caps` provenance | "declared … (dist/styles/typography/scale.css)" | — (not filed) | **Corrected: scheme-motion.css** | My grep: the sole declaration `--type-tracking-caps: 0.1em` lives in `dist/styles/tokens/scheme-motion.css`. The finding (0.06em off-ladder with a named token unused) is confirmed; the cell is fixed before it enters the registry. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. X-map per the CARRY-CUT-LEDGER: X-W1 re-gate (CI/e2e/oracles) · X-W5 one-route-one-scene (C7 owns `DockStatusLamp:70`'s viewport query by name) · X-W7 palette domain/CRUD · X-W8 graph/config subtraction (barrels, lint globs, module homes) · X-W10 canonical design+motion (its §G-4 baseline already names ApiOfflineChip.vue as CONFORMING no-preference motion). The lamp-side files (`DockStatusLamp.vue`, `status-lamp.ts`) are owned BY NAME by the standing **MT-DOCK-LAYERS-1** (Dock adjudication scope additions; D2-14/C-34/G-K/G-Q). `demo/platform/transport/**` is claimed by NO wave — its rows go NO-WAVE-OWNER at the formation boundary. GLASS-OWNED rows leave by the standing BH relay. Line anchors re-resolve before any cure.

### Blocker-class

| id | sev | claim (adjudicated) | disposition |
|---|---|---|---|
| **AP-1** (C-1/D-1/L-1) | **BLOCKER** | The owner-ordered-dead `misconfigured` surface has a second live seat: ApiOfflineChip.vue:11-18 renders `` dev misconfigured — run `npm run dev` `` under `role="alert"`; status-lamp.ts:54 holds the byte-identical literal; the barrel (index.ts:2-6) records a death that never happened. Both pills render simultaneously on one frame (evidence/desktop-1440-dark-misconfigured.png — reader-2's visual read). MT-F031's disposition is categorical: REMOVED, not restyled, not gated; the console diagnosis (availability.ts:163) is the owner-sanctioned home and already exists. | **MT-F031 executor** — this adjudication pins the exact files the ruling asked for: chip template :11-18 + `misconfigured` computed :37 + `.api-misconfig-chip` :68-72 + `.misconfig-dot` :74-79 + comment :8-10; lamp arm status-lamp.ts:50-55 + DockStatusLamp.vue:88-107. Lamp-side files = **MT-DOCK-LAYERS-1** (owns them by name). Chip-side: no X wave claims the file by name → **X-W7 rider (palette-tree home, unclaimed by name), else NO-WAVE-OWNER at the formation boundary**. MT-F031's born-RED registry grep gate stands until both seats die. |

### Major

| id | sev | claim (adjudicated) | disposition |
|---|---|---|---|
| AP-2 (C-7/D-5/L-2) | MAJOR (ruled, R-19 precedent — see disagreement #3) | The entire visual register — pill, dot, ink, keyframe body — is duplicated between the twins, already drifted (radius fallback spelling; label fold; inert `@reference`; two names for one keyframe body). DockStatusLamp.vue:42 names it itself: "one status language, two seats." True cone = 543 lines (my wc) for a read of one enum. | The misconfig half dies with AP-1; the offline half consolidates to ONE affordance in the AP-9 rebuild → **X-W7 rider**. |
| AP-3 (C-3/D-6 + reader-1 missed #3 folded) | MAJOR | The honest transport truth is gated on an unrelated swatch count (CurrentPaletteEditor.vue:116 `v-if="savedColorStrings.length > 0"` ANDed over the self-gating chip) — the exact failure the T-9 re-home comment forbids (DockStatusLamp.vue:4-5 "never gated on a palette save"). **Production inversion (the fold)**: status-lamp.ts:48 `if (!isDev) return null;` precedes the switch and darkens BOTH faces in any built artifact, so in production the chip is the SOLE offline affordance — and it is suppressed until a colour is staged. A production user with the backend down and an empty draft strip gets no indication anywhere. | Chip gate + the one-owner affordance decision → **X-W7 rider**. The lamp-side blanket-gate truth is the ALREADY-BOOKED Dock **C-34** identity (production zero live regions, MT-DOCK-LAYERS-1 DELTA-7/G-K(iii)) — cross-booked, never re-booked (anti-rename). |
| AP-4 (C-5) | MAJOR | Zero test coverage (my grep: 0 hits across test/ + e2e/), and the e2e resurrection guard watches `.dev-misconfig-banner` — a selector that exists nowhere; the live resurrection wears `.api-misconfig-chip` and walks past it. o22's own header concedes the misconfigured variant cannot fire under the harness. | Guard repair (watch the STRING per MT-F031's born-RED grep, not a dead selector) → **X-W1 rider (oracle-slate successor, CC-031 family)**; behavioral coverage rides the AP-9 rebuild (X-W7 rider). |
| AP-5 (C-4/D-10) | MAJOR | `white-space: nowrap` (:54), all dimensions rem/em-derived, no max-width, no responsive arm — and the host `.dashed-well` is a flex COLUMN (utils.css:90-92, my read), so the chip is a flex item whose min-content width is the entire un-wrappable sentence: it cannot shrink, only overflow. The sibling's relief (:67-74) was not copied. | **X-W7 rider** (the AP-9 rebuild on producer Chip largely cures); exact clip geometry stays residue U-4. |
| AP-6 (D-2 ink limb, magnitudes corrected — disagreement #5) | MAJOR | The misconfig register's text fails WCAG AA in both schemes: ≈3.8:1 light / ≈2.7–3.2:1 dark by specified-colour arithmetic. Mechanism confirmed in bytes (:69/:71 put the ink's own hue at 12% under the ink). The 2.08:1 headline was the LAMP over the overture's pink ambient (killed #2). | Moot-on-AP-1 (the register is ordered dead) — RECORDED so no restyle is ever attempted instead of the kill. |
| AP-7 (D-8, light limb only) | MAJOR | The chip claims a surface and delivers ≈1.13:1 against its well: 55%-alpha `--background` over the opaque `--well-bg` computes to (243,239,234) vs D's measured (243,239,235) — a 1-unit cross-validation of mechanism and sampling. Dark limb killed (#1). | **X-W7 rider** — the rebuilt affordance takes a real producer surface (`glass`/`veil`/`opaque`). |
| AP-8 (D-7(a)) | MAJOR | No recovery arm: `markApiReachable()` flips the latch and the chip unmounts silently; nothing in demo/ ever states "synced"; VC:101 (verified verbatim) requires persistent operation state with the entity. The flap half (D-7(b)) is killed (#7) — recovery is ONE correct transition that no surface narrates. | **X-W7 rider** (the rebuilt affordance states recovery once). |
| AP-9 (D-4/L-8) | MAJOR | glass-ui 7.0.0 ships the primitives this file hand-rolls — `./chip` (modes/shapes/sizes/tones/surfaces), `./status-dot` (with the honest docstring "Omit when adjacent text already names the state"), `./pulse` — and the demo already consumes Chip (EasingSpecimenStrip.vue:14, verified). ApiOfflineChip re-derives pill+dot+pulse in 39 lines of scoped CSS. Cure corrections bank: `surface="quiet"` is illegal (killed #9); StatusDot may be correctly OMITTED per its own docstring; size govern per killed #5. | **X-W7 rider** — ONE rebuilt offline affordance on producer primitives. |
| AP-10 (L-3) | MAJOR | Ownership inversion: a pure platform/transport concept homed three levels inside the palettes mega-feature; sole import edge is `../../../platform/transport/useApiClient` (:31); zero palette types touched; `demo/platform/transport/` already exists — the cure is a move. | **X-W8** (module-graph re-home; rides with AP-11 so the seam death and the move land together). |
| AP-11 (L-4a/L-4b) | MAJOR | The barrel seam is dead (zero entrants; the live consumer raw-imports the .vue) and the lint law defending it cannot fire — three dead layers (my census, disagreement #6). | Glob-liveness = the ALREADY-FOLDED ColorSpaceSelector **L-5(a)** identity (born-RED `eslint --print-config` command lives there) — cross-booked, not re-booked. The NEW facts (dead banned-pattern alias; the unmatched live consumer; the dead barrel) → **X-W8**. |
| AP-12 (L-7) | MAJOR | Third parallel ownership of "the backend is not answering": 7 hand-written `is unreachable.` sentences (my census = 7 exact) beside the latch, with two DIFFERENT trip conditions (latch = fetch rejection only, availability.ts:167; refs = any thrown error) — reader-2 re-read browse.png and confirmed the live contradiction (plate says unreachable, band empty). | Surface vocabulary → **X-W7 rider** (one transport-truth voice); latch/ref reconciliation lives in transport → **NO-WAVE-OWNER**. |
| AP-13 (D-9 — out-of-component, lamp seat) | MAJOR | Below 1024px the lamp is a `role="alert"` with an EMPTY accessible name: `.lamp-label { display: none }` (:67-69) removes the node from the a11y tree while the comment two lines above (:64-66) claims "visually-hidden, not v-if'd" — false as written. The only other child is aria-hidden. | **CROSS-BOOK → MT-DOCK-LAYERS-1** (owns DockStatusLamp.vue by name; announcement family DELTA-7/G-K; D2-14 already re-roles the lamp) — with the coupling note that **X-W5 C7** counts `DockStatusLamp:70` in its viewport-query subtraction census, so the fold mechanism itself is scheduled to die there. The false comment must be corrected by whichever wave touches the file first. |
| AP-14 (reader-1 missed #1 — **confirmed by this seat's own fontTools run**) | MAJOR | `font-variant: small-caps` is BROWSER-SYNTHESIZED in every code path: the shipped `fira-code-latin.woff2` GSUB carries `['calt','dnom','frac','locl','numr']` — no `smcp`, no `c2sc` (my run); the metric fallbacks carry none either. The browser scales capitals to sub-pixel stem weight at 11px — the true mechanism behind the "partial coverage on every glyph pixel" the D axis measured and mis-attributed to ink opacity. The producer's own register recipe uses REAL capitals (`text-transform: uppercase` in `@utility text-mono-caption`). Present in both twins. | **X-W10** (type canon: the register spells uppercase, never synthetic small-caps) + a **BH relay note** (the producer's Fira Code subset cannot honour `smcp` — any producer surface spelling small-caps has the same disease). |
| AP-15 (reader-1 missed #2 — confirmed by this seat, disagreement #1) | MAJOR | The phantom token is PRODUCER-born: glass-ui's own `.timeline-popover-state` rule consumes undeclared `--type-mono-caption` with a THIRD divergent fallback (.75rem), and the chip's recipe is a hand-copy of it (same var, same 0.06em, small-caps for uppercase). Four consumers, zero declarations, three fallback realities. | Producer half (declare beside `--type-micro`/`--type-caption`, or retire from the producer too) = **GLASS-OWNED → BH relay**. Demo half = **X-W10** with the size-preserving cure (`var(--type-micro)` bare / `text-mono-micro`) per killed #5. |
| AP-16 (reader-1 missed #4) | MAJOR | The chip is the one text in its well that ignores `prefers-contrast: more`: foundation.css:726-740 raises `--muted-foreground` to 88% and `--card-edge` to 55% (my read); the sibling labels in the same well consume the token and get louder; the chip's hand-mixed 72% ink (:52) is untouched — the decorative count responds to the user's contrast request, the data-loss message does not. Its own border (:51, `--card-edge`) DOES participate — the correct line that makes the wrong one legible. | **X-W10** (ink roles consume tokens, never hand-mixes) — also cured structurally by the AP-9 rebuild. |
| AP-17 (reader-2 missed #2 — confirmed by this seat's read) | MAJOR | The cooldown gate admits an UNBOUNDED burst, not the "ONE probe" its docs promise twice (:9-11, :185-186): the allow branch (:193) neither re-arms `unavailableSince` nor holds an in-flight flag, so every caller between window-open and first-failure-resolution passes — N doomed requests + N console errors per cycle against a slow-failing backend, the exact scenario the latch exists for. Re-arm happens only on failure (:170). | **NO-WAVE-OWNER** (transport; no X wave claims `demo/platform/transport/**`) — flagged for the next formation boundary with the one-line cures named (re-arm on allow, or a `probeInFlight` flag). |

### Minor

| id | sev | claim (adjudicated) | disposition |
|---|---|---|---|
| AP-18 (C-2/D-14) | MINOR (both readers converged down from the axes' MAJOR — adopted) | The chip's dev-only instruction is not dev-gated; the trigger keys on loopback hostname, never build mode, so a production BUILD served on loopback (`vite preview`, no VITE_API_URL) latches misconfigured, the lamp goes dark, and the chip alone shouts. Only reachable population: a developer previewing a local build. | Moot-on-AP-1 (the branch deletes). Recorded; the reachability truth also kills L-1's stronger framing (killed #8). |
| AP-19 (C-6/L-5/D-3 core — disagreement #4) | MINOR | `--type-mono-caption` masking fallback (owner edict 2): font-size always resolves to the hardcoded fallback; three consumers, three divergent fallbacks. | **X-W10**: `font-size: var(--type-micro)` bare — zero pixel delta, on-ladder, mask gone. Producer half = AP-15's relay. |
| AP-20 (L-6) | MINOR | Six dead `var(--destructive, oklch(0.58 0.19 25))` fallbacks across the twins encode a drifted second definition of the destructive ink (`--destructive` is declared threefold in the producer — my grep); plus the `--radius-pill` vs `--radius-pill, 9999px` spelling drift. | The misconfig blocks die with AP-1; surviving fallback hygiene → **X-W10**. |
| AP-21 (D-11.1, severity per reader-2 — the D-11 bundle is unbundled) | MINOR | `2.4s` untokenized, duplicated in the twin; the producer duration ladder has NO 2.4s rung, so "tokenize it" is a value change, not a rename — any cure states the motion-design decision explicitly. | **X-W10** (motion canon — noting W10's G-4 baseline already counts this file's `no-preference` block as CONFORMING; only the literal is at issue). |
| AP-22 (C-8) | MINOR | small-caps renders the literal shell command as `` RUN `NPM RUN DEV` `` (visually confirmed by reader-2 in the dark evidence frame) — capitals the user must not type; no `<code>`-register escape. AP-14's synthesis makes it worse: the capitals are fake. | Moot-on-AP-1 (the copy dies with the branch). |
| AP-23 (D-13 + reader-1's sharpening) | MINOR | Forced-colors: the ring-vs-filled register split is carried by colour/fill alone; chip classes absent from foundation.css's tier-1 roster (my read of :676-698); the borderless filled dot plausibly VANISHES (background substitution with nothing to draw) while the ring survives. | Structurally moot-on-AP-1 (only one register remains); rendered outcome stays residue U-3 for the surviving dot. |
| AP-24 (L-9, structural limb; citation corrected to client.ts:37/:43) | MINOR | `initApiEnvironment(BASE_URL)` runs at import time: importing the transport module for any reason mutates global state and may console.error. App.vue's `provideApiClient()` is the natural explicit seat. | **NO-WAVE-OWNER** (transport) — coupled to AP-25: the move makes repeat invocation reachable, so the guard lands first. |
| AP-26 (reader-2 missed #3) | MINOR | Three doc cross-references in the component's own cone point at W43-killed aliases (`index.ts:5` `@components/custom/dock/…`, `:6` + `status-lamp.ts` `@lib/palette/api/availability.ts`) — the barrel documenting MT-F031's history sends the reader to files that do not exist. | Dies with the barrel (**X-W8**); the status-lamp.ts copy corrected by whichever wave touches it (MT-DOCK-LAYERS-1 owns the file). |
| AP-27 (reader-1 missed #5) | MINOR | The loud register beats the base register by SOURCE ORDER alone (:41 vs :68, both (0,1,0)); the sibling solved it with `[data-variant]` at (0,2,0). A third load-bearing twin-divergence the axes' byte-diffs missed. | Moot-on-AP-1; preserved as a method lesson (declaration diffs miss cascade semantics). |
| AP-28 (reader-1 missed #6, provenance corrected — disagreement #8) | MINOR | `letter-spacing: 0.06em` is off-ladder with a named producer token unused (`--type-tracking-caps: 0.1em`, **scheme-motion.css**); the sixth off-ladder literal D-4's inventory stopped short of. Adopting the token is a rendered change — same caveat as AP-21. | **X-W10**. |

### Info

| id | claim (adjudicated) | disposition |
|---|---|---|
| AP-29 (reader-2 missed #4) | `ApiClient.baseUrl` is a dead member of the otherwise-exemplary DI seam (sole external "consumer" is a comment). | NO-WAVE-OWNER (transport hygiene). |
| AP-30 (C-9) | `detectDevMisconfig`'s third conjunct is a tautology at its only call site (VITE_API_URL unset forces the remote constant; cross-origin-vs-loopback always true once legs 1–2 pass) — the unit test proves a matrix the wiring cannot produce. | NO-WAVE-OWNER; moot-on-AP-1 in effect (the detector's product consumer dies; the console diagnosis keeps it). |
| AP-31 (C-10) | The misconfigured invariant is defended on one side only: `markApiUnreachable` refuses to relabel (:169); `markApiReachable`'s `!== "available"` guard (:176) does NOT protect misconfigured and would silently clear it. Unreachable today (the gate throws first); test/status-lamp.test.ts:46 already calls it in the exact future-caller shape. | NO-WAVE-OWNER. |
| AP-32 (L-1 attribution / C-2 §2) | **The owner's OM-5 witness is DockStatusLamp, not this chip** — settled by geometry (pad 4.8/8.8px = lamp :56; `position:absolute` = :44; vs chip :49/:42), reached independently by both readers and re-verified from this seat's reads. The chip is nonetheless a live second copy — the trap is real. | Feeds AP-1's file-pinning; no separate cure. |
| AP-33 (reader-2 missed #5) | `initApiEnvironment` is documented "Idempotent" (:148) with no idempotence guard — benign at module-eval-once, load-bearing the day AP-24's cure moves the call. | NO-WAVE-OWNER, paired with AP-24 (guard lands with the move). |
| AP-34 (reader-1 missed #7) | The chip's placement is RIGHT: emitted after the swatch strip, immediately before the save affordance — the status precedes the control it qualifies. D-15 argued hierarchy from size/ink alone and never read reading order. | Observation; preserved so the rebuild keeps the seat. |

## Killed-claims register

1. **D-8 dark limb / D §1.2 — "CR fill/well = 1.00 … There is no chip."** Killed twice independently and adopted with reader-1's algebraic lock: `0.12·ink + 0.88·G = G` iff `ink = G`, for ANY ground — and the dark ink demonstrably resolved (235,71,71). Reader-2's re-measurement of the axis's own PNG found ground (66,55,47) vs chip (86,57,50) → CR 1.11, and the composite arithmetic reproduces the measured chip to the unit. The axis sampled the chip's own fill for both operands. Light limb (1.13:1) SURVIVES (AP-7).
2. **D-2 provenance — "2.08:1 … the least legible text on the screen."** The pixels are the LAMP's, over the overture's pink ambient — proven three ways by the corpus itself (L's geometry, C's probe, D's own :129-132) and re-verified from source geometry by this seat. The AA failure survives at the chip's true grounds (AP-6); the headline and superlative die.
3. **D §1.3/D-15 — "3.58:1 is the honest read" for the offline text.** The offline register PASSES AA: specified colours compute (88,85,82) on (243,239,234) → 6.47:1 — a value D measured and discarded in favour of an antialiasing-decile mean. WCAG 1.4.3 evaluates specified colours. **Extension by this seat**: reader-2's 2.42:1 dark misconfig figure uses the same decile method and is displaced by the specified-colour band (disagreement #5). The perceptual washout D saw is real — it is AP-14's synthesized small caps, not contrast.
4. **D-3(a) — "11px is below the floor of the type scale."** The same `:root` block declares `--type-micro: 0.6875rem` (exactly 11px) and `--type-admin-label: 0.625rem` (my read). The chip sits ON a named rung.
5. **The unanimous tri-axis cure — `var(--type-caption)` bare / `text-mono-small` / Chip `size="sm"`.** All three silently resize the component 1.3–1.8× (caption = 12–16px fluid; small = 14–20px), directly worsening the C-4/D-10 overflow the SAME corpus files as MAJOR. The size-preserving producer role exists: `text-mono-micro` / `var(--type-micro)` — byte-identical to today's render. Three independent seats prescribed a regression because none grepped the scale for the VALUE. (The ColorSpaceSelector addendum's law restated: the cure is adjudicated separately from the defect.)
6. **D-11.2 — the "asymmetric tick" easing claim.** CSS Animations applies the timing function per keyframe interval; the 0/50/100 stops make the loop symmetric by construction; and `--ease-standard` resolves to `cubic-bezier(0.4, 0, 0.2, 1)` (my grep) — an ease-in-out, the right shape for a breath.
7. **D-7(b) — the 30 s live-region flap.** Killed from the bytes (disagreement #2): no state oscillation, no trigger, no unmount. Reader-1's UNPROVEN posture is overruled — no live run is needed to refute a mechanism that cannot occur.
8. **L-1(note) — "provably unreachable in production — pure dead code."** Conflates deployment with build: the detector keys on page hostname, so a production bundle on loopback latches it (AP-18's population). A kill on this premise would license the deletion for the WRONG reason; MT-F031 is the right one.
9. **D-4's cure props — `surface="quiet"`.** `SURFACES: readonly ["glass","veil","opaque"]` (my read of axes.d.ts). The quoted cure does not typecheck.
10. **D-11.3's canon citation.** VC:145 (verified verbatim) governs "Continuous Aurora/Blob ambient motion" — two named systems, not a 0.4rem dot. The WCAG 2.2.2 limb survives alone as residue U-5.
11. **D-3(c)'s census — "0 declarations, 3 consumption sites" — AND reader-2's identical re-run.** Four consumption sites; the fourth is the producer's own minified rule (disagreement #1). The undercount repeating across the axis and a reader claiming a fresh re-run is an echo event (DISSENT #1).
12. **L-4's "three of four globs" and reader-2's "3 of the 4 named dirs."** My census: 8 globs, 6 dead, across two objects — and the sharper fact both underplayed: the live consumer's path matches NO glob at all. Substance survives on stronger legs.
13. **Reader-2's "531 lines exactly."** My `wc -l` = **543** (disagreement #7). A "live re-run" cell that does not reproduce is worse than no cell; the direction survives, the digit dies.

## Superlatives (with provenance)

1. **The PRM idiom is exemplary and now quadruply attested**: ApiOfflineChip.vue:81-90 puts the `@keyframes` AND the `animation` declaration inside `@media (prefers-reduced-motion: no-preference)` — under reduce, no animation EXISTS, strictly stronger than the app's blunt Guard 1 (`animations.css:184-193`, motion-quarantine §1). All three axes credited it; both readers verified it; **X-W10's own G-4 baseline census names this file among the six conforming blocks** (W10.md:305-307). Motion-law compliance note: this corpus is EXISTS-ORIGINAL (outside the 46 REPORT-AUTHORED files), so no quarantine row governs it directly; no motion-quarantine **FALSE** row (M-26, M-32 class) is carried anywhere in this record, and the TRUE rows' pattern (M-10: the producer ELIMINATES rather than clamps) is exactly the pattern this file exhibits.
2. **Opacity-only, compositor-only, zero cleanup surface**: the animated property is `opacity` alone (:83-84); no rAF, timer, listener, or observer in the SFC — leak-free by construction in a repo whose constellation audit recorded a ~40-site PRM-RAF epidemic. (Both readers; verified in my full read.)
3. **The latch contract is real engineering**: availability.ts:20-35 states the CORS-vs-network indistinguishability honestly; :112-116 detects the precondition rather than mislabelling the symptom; :169 refuses to relabel; :189-191 fails loud and synchronously; and `devMisconfigMessage` (:119-130) names origin, base, both remedies, the cause, and explicitly denies the wrong diagnosis. The component's only sin is giving that distinction two visible surfaces where the owner ruled it deserves zero.
4. **The DI seam is exemplary and survives every cure**: the chip reads the latch through `useApiClient()` (:31, :35), never the module singleton; 3 consumers, 1 provider, fail-fast injector (reader-2's census, my chip-side read). AP-29's dead member is the seam's one blemish.
5. **`resolveLampState` is the model the chip should have been** (reader-2): pure, total, `isDev` threaded as an argument for closed-form testability, with the full (availability × dev-gate) matrix asserted in test/status-lamp.test.ts — two files from a sibling that inlines the same decision with zero tests.
6. **The copy tells the truth, and the truth was engineered**: "saved locally" is backed by usePaletteActions.ts:66-77 — `createPalette` runs UNCONDITIONALLY, with the comment recording that the prior `await ensureUser()` inverted the contract and silently destroyed palettes on an unreachable backend (verified verbatim by this seat). Reader-1 alone checked this; it is the strongest thing in the component's cone.
7. **One line gets accessibility for free by consuming its token**: `border: 1px solid var(--card-edge)` (:51) inherits the `prefers-contrast: more` lift (foundation.css:736) — the counter-example that makes AP-16 and AP-20 legible as defects rather than style.
8. **Self-gating absence-as-design**: `unknown` and `available` render nothing — no shell, no skeleton, no reserved space (:12, :20).
9. **Cross-corpus: L's geometry attribution proof** (pad/position uniquely identify the lamp) settled the owner's OM-5 question by measurement, not assertion — adopted by both readers and this seat as the load-bearing fact under AP-1/AP-32.
10. **The corpus polices itself**: D's open retraction of its own stale probe artifact, C's byte-exact state restoration, L's bolded refusal to assert an unmeasured timing limb — and both readers correctly answered the attribution question against their own axes' interest.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **U-1 · AT announcement of mount-with-content live regions** (C §4) — plus the confirmed adjacent fact to test WITH it: in dev-misconfigured, TWO live regions carry the same sentence simultaneously. Needs VoiceOver/NVDA. (Largely moot if AP-1 lands first — then verify the single surviving `role="status"` mount.)
- **U-2 · RTL isolation** of backticked provenance strings (VC:154 verified binding) — no RTL frame of this component exists in the 60-capture corpus (reader-2's precondition check). Moot for the command string under AP-1; the surviving copy still carries no `<bdi>`.
- **U-3 · Forced-colors rendered outcome** for the surviving dot (ring survives / filled vanishes hypothesis) — CSS basis source-certain, substitution behaviour engine-dependent.
- **U-4 · The 200%-text clip geometry in the real `.dashed-well`** — mechanism source-confirmed (flex-column min-content); the 427/508px numbers were synthetic-host measurements and stay uncited.
- **U-5 · WCAG 2.2.2** on a 2.4s opacity pulse of a 6.4px aria-hidden dot — genuinely borderline; this seat, like reader-1, does not decide it from the armchair.
- **U-6 · The production-build-on-loopback repro** (AP-18) — sound from source, never executed; one `vite preview` run decides it (moot on AP-1).
- **U-7 · One live latch cycle** witnessing AP-17's burst (N parallel doomed requests past the cooldown against a hanging backend) — the mechanism is source-certain; the magnitude is not.
- **U-8 · One honest dark-mode capture of the well** — reader-2's measured dark ground (66,55,47) vs reader-1's token derivation diverge; both readers' dark numbers ride the `classList.add('dark')` hack the D axis itself flagged. Decides nothing structural (AP-6/AP-7 stand either way) but pins the band.

## DISSENT — preserved, not ruled away

1. **Echo-integrity events, both directions.** Reader-2's "3 consumption sites" repeated the D-3(c) undercount inside a sentence claiming a fresh re-run (disagreement #1), and its "531 exactly" did not reproduce (disagreement #7) — two cells where a claimed live measurement was inherited or stale. Reader-1's tracking-token provenance (scale.css for scheme-motion.css) is the same class at lower stakes. All three findings survive on corrected cells; the lesson is the Dock adjudication's restated: adoption without re-derivation is where false cells enter the registry — including inside the seats hired to catch it.
2. **Reader-2's BLOCKER instinct on AP-2 is preserved**: "two full implementations of one availability affordance" is arguably the root that produced AP-1, and a future formation may reasonably rank the disease above its worst symptom. This seat holds the R-19 severity line (BLOCKER = capability/data loss) but records the opposing view.
3. **The transport rows' NO-WAVE-OWNER cluster is the record's largest routing risk**: AP-17 (a real doomed-request burst), AP-24/AP-33, AP-29/AP-30/AP-31 all land in `demo/platform/transport/**`, which no X wave claims by name. If the next formation boundary does not mint an owner, five adjudicated rows strand. Flagged deliberately loud.
4. **This seat's own least-certain ruling**: folding reader-1's production-inversion find INTO AP-3 rather than minting it as a separate BLOCKER. The argument for BLOCKER: a production user with an empty draft strip and a dead backend gets zero indication anywhere — arguably a silent-data-loss-adjacent hole. The argument that held: the data itself is NOT lost (save-P0 is unconditional, superlative #6), the copy when visible is honest, and the hole's cure is identical to AP-3's (one ungated owner). If the visual-audit's production pass witnesses a user-confusion event on this hole, the severity re-opens.

## Closing verdict

**ADJUDICATED — BLOCKER SUSTAINED.** One component, one honest state, wearing an owner-ordered-dead second face that the repo's own paperwork says was already buried: AP-1 executes MT-F031 across BOTH seats (chip branch + lamp arm, files pinned above), the offline affordance is rebuilt ONCE on producer primitives with an owner and no swatch gate (X-W7 rider), the module goes home (X-W8), the type/motion register joins the canon (X-W10 + BH relay for the producer-born phantom), the lamp's a11y truths cross-book to MT-DOCK-LAYERS-1, and the transport cluster is flagged NO-WAVE-OWNER before it strands — 33 rows confirmed, 13 claims killed (three of them the readers' own), 8 residue items handed to the live pass. Nothing here stamps a wave; everything here is runnable by the session that does.

— end of apotheosis. The three challenge records and both reader returns remain immutable beside this file per E-1/E-3.
