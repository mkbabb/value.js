# ConfigSliderPane — ADJUDICATED (M-12 tri-fold apotheosis)

**Date**: 2026-07-27 · **Component**: `demo/scenes/ConfigSliderPane.vue` + consumers `demo/scenes/blob/BlobPane.vue`, `demo/scenes/atmosphere/AuroraPane.vue`
**Seats**: worker-F (claude-fable-5, self-observed) · worker-O (claude-opus-5[1m], self-observed) · **arbiter-F (claude-fable-5, self-observed — this document's author)**
**Corpus**: C-1..C-13 (code seat) + D-1..D-21 (design seat) + CSP-X1 (shell-band carry) = 35 rows, both workers blind to each other.
**Arbiter evidence**: every disputed number re-derived by this seat against the live tree (dev :9000, API-LESS — immaterial: both consumers are local reactive configs, full render verified) and the installed dist (`@mkbabb/glass-ui` **7.0.0**, version read from package.json by this seat). Arbiter probes committed:
`docs/tranches/V/megatranche/audit/components/ConfigSliderPane/probes/arbiter-paint-probe.mjs` (the reachability battery) and `arbiter-settle-probe.mjs` (the settle-time correction — the deciding run). Worker-O's battery `csp-gates.mjs` sits beside them.

**Headline tally**: 21 CONFIRMED · 13 RESCOPED · 1 REFUTED (D-21) · 0 UNVERIFIABLE-HERE rows (one *arm* of D-15 is, labelled below) · 4 glass-owned cure-arms riding one BH relay.

---

## §1 · THE DECIDING MEASUREMENT — the paint is reachable; worker-O's central refutation falls to a transition-window instrument error

The corpus's most consequential disagreement: worker-F (with both challengers and both prior jurors) prescribed *"retire `variant="spectrum"`; feed the producer's `--slider-range-bg` seam; gate the painted extent demo-side, born-RED"*. Worker-O refuted that cure with three experiments and re-routed C-3/D-3/D-4/D-14 into a glass-owned BANK B-1 ("the fill is NOT reachable from the demo at glass-ui 7.0.0 at any specificity").

**This seat re-ran all three experiments with the transition settled and worker-O's refutation is itself refuted.** The mechanism worker-O missed is in the producer's own paint rule (`dist/styles/glass/liquid-fill.css`, loaded via `foundation.css:56` → `@mkbabb/glass-ui/styles` → `glass.css` — import chain verified by this seat):

```
.glass-liquid-fill { … background: color-mix(in oklab, var(--liquid-fill-tint, var(--glass-capsule-warm)) …);
                     transition: background var(--duration-fast) var(--ease-standard), … }
```

A `background` **transition** rides every change. A computed-style read inside the transition window reports the *from*-value — transparent. Worker-O's two-rAF waits (and this seat's own first run, `arbiter-paint-probe.mjs`, which reproduced O's numbers exactly: `oklab(0 0 0 / 0)` at flip, alpha 0.57/0.78 mid-climb on the feeds) all measured the window, not the destination. Settled at 700 ms (`arbiter-settle-probe.mjs`, chromium 1440×900, `/#/blob`):

| Experiment | Worker-O (2-rAF) | **Arbiter (700 ms settle)** |
|---|---|---|
| (a) flip `data-variant` → `standard`, no feed | `oklab(0 0 0 / 0)` "still transparent" | **`oklab(0.88 0.0259 0.0966 / 0.88)`** — the `--glass-capsule-warm` (`oklch(0.88 0.1 75)`, defined in `styles/tokens/glass-fx.css`, import verified) mix at the default 88% strength. **The standard variant paints its range bare.** |
| (b) feed documented `--slider-range-bg: oklch(0.55 0.19 25)` on the standard slider | "moves `--liquid-fill-tint`, background does not move" | **`oklab(0.55 0.1722 0.0803 / 0.88)`** — the seam paints exactly the fed colour. |
| (c) consumer `!important` background on a spectrum slider | "STILL computes transparent" | **`oklch(0.55 0.19 25)`** — paints, as cascade theory requires (no competing `!important` exists; unlayered spectrum `background:0 0` is normal-register). |

**What survives of worker-O's position — three real salvages, all adopted:**
1. **The thumb collapses on retirement** — byte-certain and settled-verified: base `.slider-thumb[data-v-4f4cab01]{width:0;…;opacity:0}`; only `[data-variant=spectrum]` gives it width/opacity. The standard variant is a **thumbless filled-capsule design** — the painted extent IS the value read, which is exactly what D-3 demands. And retirement is focus-safe in the normal register: `.glass-slider:not([data-variant=spectrum]):focus-within .slider-track{box-shadow:var(--focus-ring-shadow)}` (dist, verified) — the track focus ring *returns* when spectrum leaves.
2. **The o18 LOCK is real** (this seat read `e2e/smoke/oracles/o18-contrast-census.spec.ts:1158-1214`): the config-track leg waits for the `--ink-muted` stamp and asserts the INERT track ≥ GRAPHICS_FLOOR — green *only because of* SFC:202's re-ink. Deleting :202 without re-aiming o18 in the same cut turns the suite RED. Sequencing adopted (gate order below).
3. **The `foundation.css:571-574` shim** (owner-ruled MARKER, verified: restates the dist's dropped unprefixed `backdrop-filter:none` on `[data-variant="spectrum"] .slider-range`) is keyed to the variant — but this seat corrects O's "silently orphans a live bank": the shim serves **every** spectrum slider, and the **picker population keeps spectrum legitimately** (D-14 returns the meniscus to chromatic surfaces). The shim **stays, untouched**, serving the picker; the cut records that disposition explicitly (CSP-O9 honoured, application corrected).

Consequence: **C-3, D-3, D-4, D-14 are demo-curable and demo-gateable in this wave.** O's BANK B-1 is dissolved; its single-cut discipline (paint + retirement + :202/:204-206 deletion + o18 re-aim + shim disposition, together) survives as the shape of the paint cut. The spectrum range's glass-material blur seam (the 07-25 §3 prefix trap, cured in glass 8.0.0) stays **glass-owned** — cited against INBOX I-20; not re-booked; no local patch (masking-fallback ban).

---

## §2 · PER-DEFECT VERDICTS

Every verdict below was re-derived by this seat: source line-reads of the SFC/consumers (full files read), dist byte-reads (rules quoted from the installed 7.0.0), and the two committed arbiter probes. Live numbers cited are this seat's own runs. "Repro" = what a fresh session runs.

| ID | Verdict | Deciding evidence (arbiter's own) | Disposition |
|---|---|---|---|
| **C-1** reset wipes nested live state | **CONFIRMED** | SFC:92-94 `Object.assign(config, structuredClone(defaults))`; dist `blob-config.js` defaults carry the canned triad `["#b5947f","#d4b27d","#dad6b1"]` (this seat's node import); `useAtmosphere.ts:389-403` watch keyed on colour change; `aurora-atoms.ts` header: DEFAULT_AURORA_ATOMS "Carries NO seed" — the depth-decided asymmetry is structural. Repro: `csp-gates.mjs --gate=G1`. | BUILD — declared-key reset (structure) + G1 |
| **C-2** no aria-valuetext | **RESCOPED** (O's form) | Live (arbiter): 31/31 thumbs `aria-valuetext === null`, `aria-valuenow="0.22"`. ARIA announces valuenow when valuetext is absent — the "17-digit float" harm framing is a CDP AXValue float32 artifact, refuted. Surviving defect: unit-bearing (`Merge (ms)`, `Hue Range`) and formatted (`0.220` vs `0.22`) domains announce wrong; the in-repo formatter (`useSliderAnnouncements.ts`, U-F27) sits unconsumed. | BUILD G2 (equality with the rendered cell) + glass RELAY B-3 (prop-through; `valuetext` absent from `slider/types.d.ts` — verified) |
| **C-3** spectrum variant defeated by own override | **CONFIRMED — cure ruled per §1** | Dist rule 8 `[data-variant=spectrum] .slider-range{…;background:0 0}` (quoted whole by this seat); SFC:202 feeds the flat solid; N.W13 lineage per both workers. Cure demo-reachable (§1). | BUILD G-PAINT (the single cut) |
| **C-4** copy Result discarded, UI-silent | **CONFIRMED** | SFC:88-90 awaits and discards; no status element anywhere in the template (full read); producer Result contract (never throws) verified by both workers independently — O live-forced both failure modes, 0 unhandled rejections. `vite.config.ts server.host:true` + secure-context gating makes the LAN arm real. | BUILD — status region + G1's result leg |
| **C-5** unguarded dot-path → `.toFixed` crash | **CONFIRMED** (latent-labelled) | `atoms.d.ts:145 zones?: AuroraZones` (verified); AuroraPane:103 def `zones.count` vs :74/:82 `?.` guards three lines away (verified — the internal inconsistency); SFC:76-78 `as number` + :84-86 `.toFixed`. Latent through shipped UI (injected-only); LIVE as authoring hazard — `SliderDef.key` is bare `string`, a one-char typo compiles and crashes first render. | BUILD G8 — typed lens makes it unrepresentable |
| **C-6** 31 orphan labels, zero grouping | **CONFIRMED** | Live (arbiter): labels 31, `labelsWithFor` 0, `role=group` 0. `ConfiguratorRow.vue.d.ts:44` "No a11y for/id wiring" (verified). `for` cannot work against `span[role=slider]` → aria-labelledby is the durable form. | BUILD G3 (role=group + aria-labelledby on structure the pane draws) + RELAY B-3 |
| **C-7** clamp + coarse ::before both inert | **CONFIRMED** | Clamp: ceiling 42px vs rendered row ~61px — arithmetic, cannot bind. Coarse: the producer ships `@media (pointer: coarse){[data-control-target]{min-block-size:var(--touch-target,2.75rem);…}}` at `dist/styles/utilities/responsive.css:1` — **quoted verbatim by this seat**; worker-F's mechanism find confirmed. Both blocks delete with their comments. | BUILD G5a + G4-truth |
| **C-8** 12px thumbs under a "≥44px" comment | **RESCOPED** (both workers) | Geometry: 12×24 live (arbiter baseline). **No WCAG 2.5.8 violation** — O's spacing exception (67px pitch, one target/row) and F's operable-target (full-track click) independently kill the SC framing; booking it would be a false finding three prior seats made. Demo defect = the false SFC:186-187 claim. | BUILD G5b (house floor, honestly named) + G5c (claim leaves) + glass RELAY B-2 (glyph geometry) |
| **C-9** hand-rolled copy/reset vs producer machine | **RESCOPED** (O's form) | `useConfiguratorState.d.ts` read whole by this seat: `presets: readonly ConfiguratorPreset<T>[]` is **required**, `resetCurrent()` "Restore config to the **active preset's** baseline" — the ADOPT prescription changes the pane's model to a preset model and does not express reset-to-defaults. Hand-roll + unguarded `structuredClone` confirmed. C10-A4 [P1] book stays OPEN, re-scoped (row-level `canReset`/`reset` adoption — verified shipped in the d.ts — plus a guarded clone; the state machine refused). | BUILD G1 (declared-key reset) + per-row canReset |
| **C-10** vacuous gate | **RESCOPED** (O's form) | Zero behavioural coverage + all 4 green-keeping mutations survive: confirmed (grep re-run, two e2e files only). The STRONG claim ("delete the file → all green") is FALSE: o18:934/:950/:959/:1170 await visibility. **Adverse-oracle LOCK adopted**: o18's track leg certifies the loudness of the inert surface (§1.2). | BUILD G9 — lands FIRST in build order |
| **C-11** dead branches, false JSDoc, child-mutates-prop | **CONFIRMED** | All in this seat's full SFC read: :44 empty-state promise, :119/:163 v-if no v-else (sections:[] also silently loses Copy/Reset), :3 phantom `extraControls?` vs :41-52 props, :80-82 writePath into the prop object against `aurora-atoms.ts:11`'s stated injection idiom (verified). | BUILD G11 + G8 |
| **C-12** render-function evaluation | **RESCOPED → FOLD** | Mechanism visible at :141/:147; challenger's own 43µs/row ceiling; no product property violated; L-2 forbids an ungateable gate. | FOLD into G8's per-def lens, no standalone change |
| **C-13** 390 unmount + unreachable coarse block | **RESCOPED** | Cross-cited: `registry/adjudicated/layout-gestalt.md` already carries the row by name (":150 …ConfigSliderPane.vue:218; G-4 producer ask" — verified). Deletion grounds corrected per worker-F: the block is **inert everywhere** (producer `[data-control-target]` rung delivers 44px in every coarse cell), not "unreachable where it matters" — F's coarse+wide-cell probe stands un-contradicted by O's width sweep. | Cross-cite V·L2/V·L4; coarse-block residue BUILD G4-truth |
| **D-1** forced-colors: single-channel + vanished focus | **RESCOPED — split; O wins causation** | This seat verified in bytes: `foundation.css:678` `@media (forced-colors: active)` with (i) tier-1 roster placing `forced-color-adjust:none` on the **transparent** spectrum range (dead declaration) and (ii) the `:where(… [role="slider"] …):focus-visible{outline:2px solid Highlight}` bind — **specificity (0,1,0) by deliberate design** ("keeps the specificity at 0 so a control's own focus rule still wins the normal register") — defeated by the producer's `[data-variant=spectrum] .slider-thumb[data-v-4f4cab01]:focus-visible{…;outline:none}` (verified, quoted). The FOCUS arm is **demo-owned**: a WHCM-scoped specificity bump inside the demo's own policy block (not a masking fallback — the demo owns this policy). The TRACK/RANGE arm: glass-ui ships forced-colors arms for switch/checkbox/toggle/radio/disclosure/skeleton and **none for slider** (7 blocks enumerated by this seat) — glass-owned, INBOX I-20, relay with O's control-element probe as acceptance test. The demo's solid feed (F's half) also lands — it dies in the paint cut. | BUILD G4 (demo focus fix) + G-PAINT (solid feed dies) + RELAY (slider forced-colors arm) |
| **D-2** <1024 the configurator leaves the DOM | **RESCOPED** | Both workers agree; "unreachable" framing dead (one dock tap mounts 31 rows at 390 — F measured; O concurs). Band-owned: layout-gestalt V·L2 (DOM half) / V·L4 (composition half) carry it by name (registry verified). This wave ships only the `rank` schema field the band consumes. | Cross-cite; no competing cure |
| **D-3** no slider indicates its value | **CONFIRMED — cure per §1** | Range laid out to the value fraction and painting zero alpha (both workers, arithmetic agreeing to 1e-4); figure/ground inversion (inert track at text-ink loudness, extent at 1.00:1). Twice-diagnosed lineage (U-CONTROLS:11-18, N.W13:28) per both. **Closes in this wave** — §1. | BUILD G-PAINT |
| **D-4** text ink as ~23,000px² area fill | **CONFIRMED + LOCK** | SFC:202 feeds `--ink-muted` to the track area; PROPORTION-AUDIT law 8. o18:1158-1214 read by this seat: the leg waits for the `--ink-muted` stamp — born to certify this exact re-ink. **:202 deletion and o18 re-aim land in one cut** (CSP-O8). | BUILD G-PAINT (same cut re-aims o18 onto the painted extent) |
| **D-5** thumbs + coarse-only hit extension | **RESCOPED** | = C-8 + C-7. One addition (O): the guard covers nobody in either pointer register. | G5b/G5c + RELAY B-2 |
| **D-6** 8 dividing lines vs canon "none" | **RESCOPED** | Count CONFIRMED from source (:232-235 ×7 sections on /#/blob + :245-251 action bar). **Scheme-asymmetry sub-claim STRUCK**: O's two-ground α-solve (1.24:1 light / 1.29:1 dark) exposed the fill-black-then-fill canvas premultiplication artifact; F's caveat flagged the same fragility independently; the challenger's 1.08/5.03 and O's own first pass (5.22/1.19 — the inverse!) are both artifacts of the same broken helper. Row stands on count-vs-canon alone; deletion cures. **Harness sweep obligation recorded** (§5, CSP-O4). | BUILD G6 (deletion) |
| **D-7** heading recipe == row-label recipe | **CONFIRMED** | Byte-identity verified by this seat: SFC:237-243 and AuroraPane:194-200 — five identical declarations, same order. Closed type matrix breach per canon. | BUILD G10a/G10b |
| **D-8** Card-in-Card sub-well | **CONFIRMED** | :99-101 Card tier=resting + :122 `.console-well`; `foundation.css:339-354` concedes INTERIM/P3-BOOKED (per both workers). Chassis composition → V·L4/W28-29 (no competing cure); `.console-well` swap stays BANKED — re-trigger verified by this seat: `grep -c 'glass-well' dist/glass-ui.css` → **0** (not fired). | Pane slice BUILD; chassis FOLD; BANK B-4 |
| **D-9** essentials/advanced/compare unrepresentable | **CONFIRMED** | SliderDef/SliderSection (:27-39) carry no rank/collapsed/compare/disabled/unit/format — no consumer CAN express the bench composition. Schema field built here; disclosure surface → band. | BUILD G8 (`rank` axis) + fade with G4-truth |
| **D-10** zero result state; Reset unconfirmed | **RESCOPED** | Core CONFIRMED (no status element, no aria-live — full template read). **Unhandled-rejection clause REFUTED** (both workers converged; O's live listener: 0 events; `writeClipboard` returns Result). | BUILD — status region + confirm/compare grammar, minus the false clause |
| **D-11** readout: value-derived precision, no reserved width | **CONFIRMED** | `fmt` at :84-86 derives precision from the VALUE (`Number.isInteger` flip); 8 step-0.1 rows render 3 false decimals; no width reservation. F's defusal adopted: the row renders in fixed-pitch Fira Code, so the missing `tabular-nums` is inert — the swing is char-COUNT, cured by step-derived precision + reserved `min-inline-size` (the load-bearing assertions); `unit` axis absent from SliderDef. | BUILD G7 |
| **D-12** inert "ONE RHYTHM SOURCE" + size-API bypass | **CONFIRMED** | Clamp inert (C-7a); `size?:` verified in ConfiguratorRow.vue.d.ts, `data-size` null live (workers, consistent); SFC:142 `gap-1.5 py-1` overrides the producer ladder. | BUILD G5a — pass the producer size, delete clamp + claim |
| **D-13** bare slot licenses a second row species | **CONFIRMED** | AuroraPane:118-181 hand-rolls `.aurora-row` (this seat's full read); label recipe byte-copied (D-7); `extraControls?` phantom confirms API drift. Enum defs into sections → slot fill + style block delete. | BUILD G10b + G8 (enum/select def) |
| **D-14** reserved meniscus on 34 non-chromatic axes | **CONFIRMED — closes here** | :146 `variant="spectrum"` unconditional; VISUAL-CONSTITUTION:7 reservation (both workers verified verbatim). Per §1 retirement is demo-reachable; the picker population keeps spectrum (legitimately chromatic), so the gate asserts `data-variant != spectrum` **inside `.config-console` only**, and the foundation:571 shim STAYS serving the picker. | BUILD G-PAINT |
| **D-15** dependent-control state never designed | **RESCOPED** | Model gap CONFIRMED (no disabled/enabledWhen axis; live `aria-disabled` 0/31 — arbiter). The "five sliders drive nothing at Satellites=0" arm is **UNVERIFIABLE-HERE** — it asserts glass-ui's metaball shader behaviour, which no seat instrumented; it rides the BH relay as an evidence request (O's discipline adopted). | BUILD G8 (`enabledWhen`) + G2's disabled leg; relay evidence request |
| **D-16** prop contract erases types | **CONFIRMED** | 4 launder-casts (AuroraPane:111/:113, BlobPane:124/:126) + `as number` (:77) + the 13-line NumericAtomPath under its 24-line comment (BlobPane:36-48) — all in this seat's full reads. ~30 net lines delete. | BUILD G8 |
| **D-17** pane-scroll-fade promises a mask that does not exist | **CONFIRMED** | `PaneHeader.vue:54-57` sets ONLY `contain` + `scroll-timeline` (read by this seat); SFC:103-105 false; the class name mis-describes 9 consumer panes. Doc/name truth here; the fade itself designed with disclosure (D-9/band). | BUILD G11 family |
| **D-18** RTL half-mirror + promoted destructive verb | **CONFIRMED (latent)** | Both workers' independent RTL measurements agree (thumb at the LTR fraction from visual left; Reset promoted; readout not LTR-isolated). Producer hooks verified by this seat: `[data-inverted], :dir(rtl) { --slider-range-origin: right center }` in the dist. No RTL surface ships → schema fold (declared axis direction + isolated readout + action order by role), no born-RED gate (L-2). | FOLD into G8 schema + RELAY (axis-direction decision) |
| **D-19** truncating label, no fallback | **RESCOPED (latent)** | Structural facts certain (producer `truncate`, no `title`, full string only in :145 aria-label). The two seats' substitution measurements CONFLICT (challenger: clipped; O: 389==389 not clipped) — unresolved, preserved in §6; irrelevant to disposition since today's longest label (13 chars) does not fire it. | FOLD — schema label budget (authoring-time beats render-time) |
| **D-20** documented empty state with no branch; phantom prop | **CONFIRMED** | = C-11's doc half, filed by the design seat; adjudicated once, gated once. | BUILD G11 |
| **D-21** dead `relative`×2 + `mx-auto` | **REFUTED — O wins; F overturned** | Arbiter live: 64 absolutely-positioned elements inside the pane root (the row's stated "no absolutely-positioned child" evidence is FALSE), `containedByOuterOrCard: 0` (so neither relative is load-bearing — the conclusion half-survives), and **removing the `relative` class from both elements leaves both computing `position: relative`** (another source supplies it) — the cure is a measured no-op. An INFO row with false evidence and an ungateable no-op cure is RETIRED with rationale (L-5), not carried. Repro: `arbiter-paint-probe.mjs` (d21 block). | RETIRED |
| **CSP-X1** second `.glass-dock` makes the shell tint-reader order-dependent | **CONFIRMED** | Arbiter live: 2 docks — y=10 (shell) and y=810 (`inPane: true`, the SFC:164 action bar). Subject = the shell band; this file recorded as co-cause; D-10's action-region redesign knows it is deleting the second dock. | Cross-cite shell band |

---

## §3 · DISAGREEMENT RULINGS (who won, and the evidence that decided it)

1. **Value-paint cure (C-3/D-3/D-4/D-14)** — F: demo-gateable retirement + `--slider-range-bg`; O: unreachable → BANK B-1. **RULED: F**, on this seat's settled measurements (§1). O's three refuting experiments shared one instrument error (computed-style reads inside the `.glass-liquid-fill` background-transition window). O's salvages (thumb collapse, o18 lock, shim coupling) all adopted into the cut's shape. Note the irony preserved for the record: O's dissent documented catching a canvas-premultiplication instrument error in its own first contrast pass — and the seat's central refutation rested on a different instrument error of the same genus.
2. **D-1 causation** — F: glass-owned material + demo solid feed; O: the demo's own `:where()` WHCM bind defeated by producer specificity. **RULED: O** — foundation.css:678/:702-720 and the producer's `(0,4,1)` `outline:none` verified in bytes by this seat. The single most valuable find in the corpus; converts a BLOCKER-attributed-to-glass into a demo-owned fix that lands today. F's solid-feed half is also true and lands with the paint cut. The slider's missing forced-colors arm stays a glass relay (I-20).
3. **C-2 harm framing** — F: "AX tree announces the raw float structurally"; O: `aria-valuenow="0.22"` is what ARIA announces; the float is a CDP artifact. **RULED: O** — arbiter live read: `firstValuenow: "0.22"`. Defect (missing valuetext/units/format parity) stands.
4. **D-21** — F: CONFIRMED, delete; O: REFUTED, retire. **RULED: O** — arbiter live probe: class removal is a computed no-op; the row's evidence is false as written.
5. **C-9 / C10-A4 book** — F: adopt the producer machine, book DISCHARGED; O: API is preset-shaped, book stays OPEN re-scoped. **RULED: O** — `presets` is a required option and `resetCurrent()` is preset-baseline, verified in the d.ts. F's per-row `canReset` adoption survives (shipped, verified).
6. **C-10 strong claim** — F adopted "vacuous gate" wholesale; O refuted "delete the file → all green" and found the adverse oracle. **RULED: O** — o18's visibility awaits + the `--ink-muted` stamp poll verified by this seat.
7. **D-6 scheme asymmetry** — F: flagged measurement-fragile; O: refuted by α-solve. **RULED: O** (struck), with F's caveat recorded as independent convergence on the harness defect.
8. **C-8/D-5 SC framing** — both refuted the 2.5.8 booking on different grounds. **RULED: no SC violation is booked**; O's spacing-exception arithmetic is the primary ground (it needs no cross-component precedent), F's operable-target measurement the secondary.
9. **C-13 deletion grounds** — F: "inert everywhere" (coarse+wide cell probe); O: "unreachable where it would matter". **RULED: F's corrected grounds** — O's width sweep (fine-pointer cells) never contradicted F's coarse+wide cell, where the pane mounts AND the producer rung already delivers 44px. Immaterial to the disposition (both delete).

---

## §4 · THE WAVE — MT-CSP-1

**Title**: The configurator tells the truth — painted value, honest reset domain, one voice, one row grammar, and the deletion of every lying byte.
**Born-RED**: yes, every gate, verified against today's tree by three seats independently.

### Scope
**IN (all demo-owned, all closable without any producer release):**
- `demo/scenes/ConfigSliderPane.vue` — rewritten in place, net-negative: generic `SliderDef<T>` typed lens (keyof-derived dot-path `key`, `unit`, precision-from-`step`, `rank: "essential"|"advanced"`, `enabledWhen?`, label budget, enum/select defs); declared-key reset; **retire `variant="spectrum"`** + feed `--slider-range-bg` a certified area fill (≥3:1 vs track); DELETE :202/:204-206 (track re-ink), :208-230 (clamp + coarse blocks + their comments), :232-235/:245-251 (divider rules), both dead v-if guards' false promise, the four lying strings (:3 `extraControls?`, :44 empty state, :103-105 fade mask, :186-187 ≥44px); role=group + aria-labelledby sections; aria-valuetext from the one `fmt()` cell (interim thumb write, per the ComponentSliders precedent); pass the producer `size` rung (delete `gap-1.5 py-1`); status region for copy/reset results + confirmed Reset.
- `demo/scenes/blob/BlobPane.vue` — 2 casts + NumericAtomPath (13 lines + 24-line comment) DELETE.
- `demo/scenes/atmosphere/AuroraPane.vue` — 2 casts + the `.aurora-row` species + whole `<style>` block DELETE; enum atoms become section defs.
- `demo/styles/foundation.css` — the WHCM focus bind (:702-720) gains a WHCM-scoped high-specificity arm for the slider focus target (the `:where()` register stays for everything the design intended it for); the dead tier-1 roster line for the now-retired config spectrum range is dispositioned. **The :571-574 spectrum shim is NOT touched — it serves the picker population.**
- `e2e/smoke/oracles/o18-contrast-census.spec.ts:1158-1214` — **re-aimed in the same cut** as the :202 deletion: the leg asserts the PAINTED extent (range non-transparent, ≥3:1 vs track) instead of the inert track's loudness (CSP-O8).
- NEW `demo/test/config-slider.test.ts` (anti-vacuity suite) + NEW schema module if the lens outgrows the SFC.

**OUT, each with a named home:** narrow-width mount + disclosure composition (layout-gestalt V·L2/V·L4 — carried there by name); InstrumentChassis adoption (V·L4/W28-29); `.console-well` P3 swap (BANK B-4); thumb/grab glyph geometry (RELAY B-2); ConfiguratorRow aria-labelledby / Slider valuetext prop-through (RELAY B-3); slider forced-colors arm (RELAY, I-20); D-15's shader-effect evidence request (RELAY); glass-material blur seam (glass 8.0.0, I-20, not re-booked); `docs/tranches/V/vnext/**` (Codex-owned); `scripts/dev/dev.sh` (unowned, never touched).

### Structure (L-8 first; gates only where structure cannot reach)
1. `SliderDef<T>` typed lens → C-5/D-16 casts + crash unrepresentable; D-11 precision, D-15 dependency, D-9 rank, D-18 direction, D-19 budget become declared-domain facts.
2. Declared-key reset → C-1 unrepresentable, not merely tested.
3. Enum defs → D-13's second species unrepresentable (slot survives only for genuinely non-row content).
4. Spectrum retirement + token feed → C-3/D-14's declared-variant-defeated state deleted, not gated.
**Build order is load-bearing**: G9 (anti-vacuity) FIRST; the paint cut (G-PAINT + o18 re-aim) is one atomic commit.

### Gates (each: command · RED today · the input that makes it RED)
- **G9 · ANTI-VACUITY** — `npx vitest run demo/test/config-slider.test.ts` — RED today (file absent). Once landed: M1 `update()`→no-op, M2 reset→no-op, M3 copy→no-op, M4 `fmt`→`String(v)` must each turn it RED; plus the C-1 fixture (nested live key preserved across Reset), the C-5 fixture (`zones:undefined` render does not throw), the C-4 fixture (both `{ok:false}` arms reach the status region).
- **G-PAINT · PAINTED EXTENT** (C-3/D-3/D-4/D-14/D-1-demo-half) — `node docs/tranches/V/megatranche/audit/components/ConfigSliderPane/probes/csp-gates.mjs --gate=PAINT` (extend the battery; the arbiter settle probe is the reference measurement — **assert with the background transition settled, ≥600 ms, per §5 clause 1**). Asserts inside `.config-console`: `data-variant !== "spectrum"` on 31/31 AND settled `.slider-range` background non-transparent AND range-vs-track ≥3:1 AND `grep -c 'ink-muted\|slider-track-bg' ConfigSliderPane.vue` = 0 AND o18's re-aimed leg green. RED today on every leg (arbiter: 31/31 spectrum, range `rgba(0,0,0,0)`, :202 present). Asserts the painted PRODUCT only — material quality stays glass-owned (I-20).
- **G1 · RESET-DOMAIN** — `csp-gates.mjs --gate=G1` — RED today: one Reset click replaces live `color.paletteStops` `["#ffbde0",…]` with the canned triad. Green only when reset writes declared def paths.
- **G2 · ONE VOICE** — `csp-gates.mjs --gate=G2` — RED today: 31/31 `aria-valuetext` null vs rendered `0.220`. Asserts EQUALITY with the rendered cell incl. unit; at satellites=0 the five dependent thumbs report `aria-disabled="true"` (RED today: 0/31).
- **G3 · GROUPING** — `csp-gates.mjs --gate=G3` — RED today: groups 0-of-7, orphan labels 31/31 (arbiter-verified).
- **G4 · FORCED-COLORS FOCUS** (chromium ONLY; `matchMedia('(forced-colors: active)').matches===true` asserted FIRST, fails-not-passes when false — WebKit ignores the emulation, I-20/L-12) — RED today: focused config slider outline-style "none" while the same-register action-bar button control gets "solid" 2px. The control element is the gate's teeth (absent vs defeated).
- **G5a · DEAD CURE (rhythm)** — RED today: rendered row 60.97px at both 512px and 1042px containers under the "ONE RHYTHM SOURCE" comment. Passes by deleting :215-217/:142 and stamping the producer `size` (data-size non-null).
- **G5b · HANDLE FLOOR** (house 24px inline floor, PROPORTION-AUDIT law 7 — **explicitly NOT a WCAG 2.5.8 claim**) — RED today: 12×24. Cure is producer geometry (B-2); reported RED while the bank stands, never weakened; a demo ::before re-appearing fails it on principle.
- **G5c + G11 · DOC TRUTH** — `grep -nE '≥44px|fade mask|empty state|extraControls' ConfigSliderPane.vue` = 0 — RED today: 4 lying strings.
- **G6 · NO LINE** — RED today: 8 computed dividing lines on /#/blob, 2 on /#/atmosphere; want 0 (canon "none"). Deletion, not contrast repair (the α-correct ratios ~1.24:1 are both forbidden AND too weak to group).
- **G7 · RESERVED READOUT** — RED today: precision from value not step (step-0.1 rows render 3 decimals), no `min-inline-size`, 40.6px swing. Want: step-derived precision, declared unit, reserved widest-legal width, ≤0.5px swing.
- **G8 · TYPE LENS** — `grep -cE 'as unknown|as number' demo/scenes/**` = 0 + `npm run typecheck` green with generic props — RED today: 4+1 casts + NumericAtomPath.
- **G10a/b · ONE RECIPE PER ROLE** — RED today: 6/6 computed identity heading==row-label; `aurora-row-label` ×5 in AuroraPane. Want: heading one real size step above rows; 0 occurrences.

### Banks & relays (re-triggers verified by this seat TODAY)
- **B-2** thumb/grab glyph geometry (glass) — re-trigger: G5b green with no demo ::before.
- **B-3** a11y prop-through (glass) — re-trigger: `aria-labelledby` in ConfiguratorRow.vue.d.ts / `valuetext` in slider/types.d.ts — **both absent today (verified)**.
- **B-4** `.console-well` → `.glass-well` (P3) — re-trigger `grep -c 'glass-well' dist/glass-ui.css` — **0 today (verified)**.
- **RELAY letter (ONE, standing BH invariant, owner edict 2026-07-12)**: B-2 + B-3 + the slider forced-colors arm (with O's control-element probe as acceptance test) + D-15's shader evidence request + D-18's axis-direction decision + the ConfiguratorRow label-id exposure. Every glass item cites INBOX I-20.
- ~~B-1~~ **dissolved** (§1) — its contents are this wave's G-PAINT cut.

### Carries (every row, exactly one disposition; "next tranche decides" appears nowhere)
BUILD: C-1→G1 · C-2→G2(+B-3) · C-3→G-PAINT · C-4→status+G9 · C-5→G8 · C-6→G3(+B-3) · C-7→G5a/G11 · C-8→G5b+G5c(+B-2) · C-9→G1+canReset (C10-A4 book OPEN, re-scoped) · C-10→G9 · C-11→G11+G8 · D-1(focus)→G4, (solid feed)→G-PAINT, (slider arm)→RELAY · D-3/D-4/D-14→G-PAINT(+o18 re-aim same cut) · D-5→G5b/c · D-6→G6 · D-7→G10a · D-8→pane slice + B-4 + V·L4 · D-9→G8 rank + band · D-10→status (minus refuted clause) · D-11→G7 · D-12→G5a · D-13→G10b+G8 · D-15→G8+G2(+relay) · D-16→G8 · D-17→G11(+band) · D-20→G11. FOLD: C-12→G8 · D-18→schema+relay · D-19→schema label budget. CROSS-CITE: C-13/D-2→V·L2/V·L4 · CSP-X1→shell band (this file co-cause; D-10's redesign deletes the second dock). RETIRE: D-21 (rationale §2).

### Completable alone
**YES** (L-1): all gates are demo-owned; the paint cut needs no producer release (§1 proved the seam live at 7.0.0); banks have runnable re-triggers, verified not-fired today; the band consumes (not blocks) the rank API. One session: one SFC rewrite (net-negative), two consumer simplifications (net-negative), one test file, the battery extension, one o18 re-aim, one letter.

### π / Δ obligations
π: before/after committed captures (force-added past .gitignore, L-7) — `{before,after}-blob-1440-{light,dark}.png` (`.config-console`), `{before,after}-forced-focus-chromium.png` (labelled chromium, never "Safari" — a WebKit forced-colors frame is void, I-20), settled-paint pair (§1's measurement beside each). Δ: range `rgba(0,0,0,0)`→painted ≥3:1 (settled) · valuetext null→cell-equal 31/31 · reset wipe→preserve · dividers 8→0 · casts 5→0 · lying strings 4→0 · `.aurora-row` 4→0 · data-size null→stamped · o18 leg re-aimed and green against the cure, RED against the old defect.

---

## §5 · ADDENDUM CLAUSE — ADD-CSP (merged, arbiter-corrected)

1. **THE SETTLE LAW (new, from the deciding evidence).** A computed-style assertion on a property the matched rules TRANSITION is taken after the transition window (or with transitions disabled) — a mid-window read reports the *from*-value and can refute a working mechanism. Before booking "X does not paint / does not move", read the matched rules for a `transition` naming X's property, and state the settle discipline in the probe. Two seats in this corpus refuted a live producer seam with 2-rAF reads; the settled value overturned an entire bank.
2. **PAINTED-EXTENT OBLIGATION (F).** Every bounded numeric control renders its value as a painted extent ≥3:1 against its own track; token-presence on an information-free surface never closes a value-legibility row (PROPORTION-AUDIT law 8 applied to controls).
3. **A CURE IS ADMISSIBLE ONLY IF ITS PATH IS REACHABLE — MEASURED SETTLED (O, corrected).** Before prescribing "consume the producer's X", measure X in the installed dist and on the live page — attribute flip, token feed, `!important` control — **each read settled per clause 1**. A refutation produced by an unsettled read is itself refuted.
4. **TWO CHANNELS + A SAME-REGISTER CONTROL (O).** Every forced-colors assertion asserts `matchMedia` truth FIRST (fails-not-passes when false; WebKit frames are void, I-20) and carries a control element the policy is expected to reach — without it, a *defeated* rule is indistinguishable from an *absent* one and the cure gets booked against the wrong owner.
5. **A `:where()` POLICY IS A SUGGESTION (O).** A cross-cutting a11y/modality rule at specificity 0 declares, beside itself, the highest specificity it must beat and its control element. A policy nobody can observe applying is a dead declaration with a live comment.
6. **COMPOSITE HONESTLY OR DO NOT QUOTE A RATIO (O).** Fill-black-then-fill canvas helpers premultiply and fabricate luminance — symmetrically, producing a finding or its exact inverse (three seats hit it here). Recover α from two grounds and composite over the actual ground. **Registry ratios produced by such helpers are re-measured or struck — standing sweep obligation.**
7. **NAME THE FLOOR (O).** Never borrow an SC's authority for a house floor; an undersized target satisfying 2.5.8's spacing exception is a house-floor row, not an SC violation (three seats booked the false SC here).
8. **RESET RESTORES A DECLARED DOMAIN (F+O merged).** A reset affordance writes ONLY the keys its surface declares, announces its result, pairs with compare. Whole-object restore — `Object.assign` and preset-baseline `resetCurrent()` alike; the semantics, not the API, is the defect — is forbidden wherever a sibling system holds live state under the same root. Reset semantics decided by nesting depth is a defect even when the prose declares intent correctly.
9. **DECLARED DOMAIN OVER CURRENT VALUE (O).** Precision, unit, disclosure rank, dependency and label budget derive from the declared schema, never the live value; the schema, not the stylesheet, is where this row-class closes.
10. **AN ADVERSE ORACLE IS A LOCK (O).** Enumerate the gates asserting a surface before curing it; a gate born green against the defect's own remedy is re-aimed in the same cut, never preserved — preserving it is how the T.W8 cure produced D-3/D-4.
11. **A RETIRED VARIANT'S SHIMS ARE ENUMERATED WITH ALL THEIR CONSUMERS (O, corrected).** Before a consumer stops declaring a producer variant, every rule keyed to it is enumerated **across every population that wears it** — a shim serving a surviving population (here: foundation:571 serving the picker) STAYS, and the cut records that disposition; only a shim with zero surviving matches dies with the variant.
12. **COMMENT-PARITY IS GATED (F).** A CSS block whose stated cure a measurement refutes is deleted WITH its comment in the same cut — a lying comment is a doc-truth defect of the same rank as the dead code it defends.
13. **THE COARSE RUNG IS THE PRODUCER'S (F).** `@media(pointer:coarse){[data-control-target]{min-block-size:var(--touch-target,2.75rem)}}` (dist/styles/utilities/responsive.css:1) rides every control root; consumers never re-mint per-instance hit-area extensions; a wanted rung beyond 44px is a relay, never a ::before.

---

## §6 · DISSENT — preserved disagreement, verbatim

**Worker-O, dissent (1), overturned by this seat's settled measurement but preserved whole as the record of the corpus's sharpest disagreement** (the seat cannot respond; its salvages were adopted):

> "AGAINST BOTH CHALLENGER SEATS AND BOTH PRIOR JURORS, ON THE VALUE-READ CURE — the most consequential disagreement in this adjudication. C-3, D-3, D-14 and JUROR-3's J3-01 all prescribe "retire variant='spectrum' so the producer's .slider-range paints the value", and JUROR-3's G1 gates on it demo-side. I measured the proposition three independent ways and it is false at glass-ui 7.0.0: (a) flipping data-variant to the real default "standard" … leaves rangeBg `oklab(0 0 0 / 0)` — still transparent — and collapses the thumb to width 0 … (b) `.glass-slider:not([data-variant=spectrum]) .slider-range` painting rules in dist/glass-ui.css = 0; (c) an injected consumer rule `.config-console .slider-range{background:oklch(0.55 0.19 25)!important}` STILL computes transparent … The fill is unreachable from the demo at any specificity. … Consequence: a wave that gates the paint demo-side CANNOT CLOSE (L-1), and worse, retiring the variant today would strictly worsen the pane — the last remaining value cue, the thumb, disappears."

*Arbiter ruling on the preserved text*: (a)'s transparency and (c) were unsettled transition-window reads (§1 — the settled values paint); (b) is true of `background` declarations but the paint lives in the layered `.glass-liquid-fill` rule, which IS loaded by the demo (import chain verified); the thumb collapse is true and adopted — with the corrective fact that the standard variant's track focus-ring returns on retirement, and the painted extent replaces the thumb as the value cue by design.

**Worker-F, dissent (5), on D-1 cure ownership — superseded by O's specificity find but preserved as the position that correctly identified the demo's causal half:**

> "D-1's cure ownership is split, not producer-only as the filed cure implies: the demo's solid --slider-track-bg feed is causally implicated TODAY (a gradient survives forced-colors as background-image; a solid is forced to Canvas), so the demo half lands with G1 now, while the slider's forced-colors arm is glass-owned and rides the relay with a banked, runnable acceptance test — the wave must not present the relay as a completed cure."

**Genuinely unresolved (no seat can settle it from here):** D-19's substitution measurements conflict — the challenger measured a 47-char label clipping (392/413) where worker-O measured it fitting (389/389, `clipped:false`), with the challenger's substitution string unstated. Immaterial to the disposition (the row is latent either way; the schema label budget closes it at authoring time), but the conflict is recorded rather than harmonised.

**Recorded against the harness, jointly:** the fill-black-then-fill 1×1-canvas contrast helper fabricated three mutually-contradictory ratios across three seats (challenger 1.08/5.03; O's first pass 5.22/1.19; F's caveat flagged the fragility without a number). §5 clause 6 carries the standing sweep.
