# AdminListItem — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · served model: `claude-fable-5` (Fable 5), observed in this seat's own system prompt

**Seat**: apotheosis adjudicator (incredulity seat) over two hostile re-readers — reader-A (design-and-usage) and reader-B (library-and-code).
**Date**: 2026-08-03 · **HEAD**: `64aca0a2` · branch `tranche-u` · **Subject**: `demo/palettes/browser/admin/AdminListItem.vue` (24 lines, SHA-256 `a15213…c7a` — re-hashed by this seat, matches all three axis receipts, so the corpus is against current bytes).
**Corpus provenance (governs the whole record)**: all three challenge files are **REPORT-AUTHORED with 0 payloads** (`registry/HYDRATION-LEDGER.md:258-260`, source `closure-manifest-2026-08-03`; hashes verified by this seat). Per the ledger header these are *source-review findings, NOT challenge coverage* — no challenge seat ever ran, **no r2 file exists for any axis**, so the r1 prose is the entire corpus and no AXIS-PROMOTIONS override applies. Both readers reported this independently; this seat re-read the ledger rows itself.
**Method**: both readers were attacked before either was adopted. Every verdict-level disagreement (D-5, C-4, D-4, C-5, L-2, L-4, plus two severity splits L-1/L-3) was re-derived against the tree with this seat's own instruments. The two reader-vs-reader contradictions hiding inside agreed material (announced-loading superlative vs triplication defect; skeleton "zone-for-zone" superlative vs parity defect) were ruled, not averaged.
**Arbiter's own instruments (all static; zero browser probes — probe parsimony)**: full reads of AdminListItem.vue (whole), AdminListSkeleton.vue (whole), AdminNamesPanel.vue (whole), AdminAuditPanel.vue:30-90, AdminFlaggedPanel.vue:10-125, AdminUsersPanel.vue:40-150, `browser/admin/index.ts` (whole), AdminPane.vue:45-100, AdminTagsPanel.vue:45-60; canon reads `docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md:80-92` (the five Admin separator rows + the "Any additional line … terminal row rule, caster stroke or corner rule is a defect" sentence), `VISUAL-CONSTITUTION.md:218` (one review-row anatomy) and `:186` (no listbox/option fiction), `PROPORTION-AUDIT.md` law 4; installed glass-ui 7.0.0 `dist/styles/typography/semantic.css` (`text-caption` = italic; `text-small` → `--type-small`/`--type-leading-small`), `utilities.css` (`text-mono-small` → the SAME `--type-small`/`--type-leading-small`), `scale.css` (`--type-caption` clamp min 0.75rem vs `--type-small` clamp min 0.875rem), `dist/components/button/Button.vue.d.ts:4-19` (emphasis/tone/size/iconOnly/loading — **no `variant`**); `foundation.css:585-587` (`.slug-pill` = `text-mono-small font-bold …`); censuses run by this seat: frame string `flex items-center gap-3 px-3 py-2.5` = **5 sites** (Item:11, Skeleton:9, Audit:62, Flagged:50, Users:80; full bordered form verbatim at the first three); `AdminListItem` importers = **1** (AdminNamesPanel.vue:128, call sites :44/:94); `cssColorOpaque` = declaration-only in the panel (:140) + AdminPane.vue:52/:94 feed; `role=` census over `browser/admin/*.vue` = status ×2 + conditional button ×1, **zero list semantics**; script-less SFCs in demo = exactly 1 (AdminListItem.vue); `defineSlots` in demo = 0; `grep "AdminListItem\|AdminNamesPanel" test/ e2e/` = 0; `aria-label="Loading…"` wrapper census = 7 plain-div sites vs AdminTagsPanel's single correct `role="status"` container (:54-55); motion-quarantine §7.4 + exclusion table + M-09/M-10/M-12 rows; `HYDRATION-LEDGER` rows; the **AdminNamesPanel challenge corpus** (probe-backed, payload-bearing) rows C-2/C-9/C-11/L-4 — decisive for two identity guards below; tranche-X wave bounds: X-W4.md:121-125, X-W7.md:100-120/:225-260/:455-470, X-W8.md:87/:161-165/:291/:311-315, X-W10.md:120-143; the AdminUsersPanel adjudication (MT-AU1 §255 item 6: `browser/admin/` re-homes to `palettes/admin/` **including AdminListItem/Skeleton**).
**Environment disclosure**: nothing in this corpus is data-backed; every ruling was decidable from source + installed producer bytes + canon. No browser tooling was used (law).

## Scoreboard

**18 corpus rows → 5 CONFIRMED identities (2 duplicate rows folded in) · 11 KILLED/REFUTED · 8 fresh rows booked from the readers' missed rosters (adjudicated by this seat) · 2 cross-books to the AdminNamesPanel corpus (anti-rename) · 1 cross-ref (Button `variant`, already banked).**

Severity of what stands: **3 MAJOR** (D-5 canon border · L-3 five-copy anatomy · AL-M6 four identity registers) · **6 MINOR** · **4 INFO**. All three axis verdict lines (**SOURCE-RED ×3**) are REFUTED as written: the one invariant the file actually promises — the S.W5-12/F-1 min-width truncation chain — is enforced structurally and repaired a documented P0. The component's real defects are canon defects of its *anatomy's suite*, not implementation failures of its 13 lines.

**Id mapping** (the readers numbered differently): canonical ids here are axis-ordinal (C-1..C-5+C-V, D-1..D-5+D-V, L-1..L-5+L-V). Reader-A cited challenge-L bullets by file line (their L-9..L-13 = canonical L-1..L-5) and their "L-11/L-12" = canonical L-3/L-4. Reader-B's ids match canonical.

## Ruled disagreements (this seat's actual work)

| id | reader-A (design/usage) | reader-B (library/code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-5** | CONFIRMED MAJOR — the per-row border is the canon's named "terminal row rule" defect | KILLED — "repeated everywhere" refuted by enumeration; remainder unfalsifiable | **RESCOPED-CONFIRMED MAJOR — A wins the material ground; B's kill survives only on the limbs the ground doesn't need** | This seat's canon read settles it: OPTICAL-BENCH-COMPOSITIONS.md's Admin table grants all five Admin routes exactly "**one low-emphasis separator between adjacent review rows; none after the final row**", and the very next sentence names the defect classes: "Any additional line, automatic P122 divider, consumer-hidden producer line, **terminal row rule, caster stroke or corner rule** is a defect." AdminListItem.vue:11 ships `rounded-md border border-card-edge` — a four-sided stroke + corner rule per row, whose last row's bottom edge IS the terminal row rule. Compounding (verified): the field separates by interval already (AdminNamesPanel.vue:43 `grid gap-2`) and the swatch adds a fifth stroke (:46/:96 `border border-card-edge`) — one boundary expressed three ways, the exact PROPORTION-AUDIT law-4 case. And it is not one row's sin: **all five routes** ship the bordered-card-per-row shape (Item:11, Skeleton:9, Audit:62, Flagged:47 wrapper, Users:68 wrapper), so the canon defect is suite-wide. B's kill engaged only the "generic card repeated everywhere" spreading limb (correctly refuted — one importer, not exported, cannot spread) and the "signature connection" vagueness limb (correctly unfalsifiable) — B never opened the optical-bench table. A kill that refutes two limbs while the third is a named canon violation does not kill the row. |
| **C-4** | CONFIRMED MINOR — "detached siblings" realised at Users:134 and Flagged:105 | KILLED — requirement-invention; zero consumers of AdminListItem did it; a detail region = KISS-banned | **KILLED — B wins; A's evidence transfers to L-3** | The axis's claim is about *consumers* of this component. Actual consumer set = one file, two call sites, zero disclosure needs (name + CSS literal + commands). The routes that hand-rolled row+detail shapes (AdminUsersPanel.vue:68/:134 — a true conditional disclosure; AdminFlaggedPanel.vue:47/:105 — a *permanent* `border-t` detail section, not a disclosure, a nuance A elided) **never import AdminListItem** — my grep: importers = 1. Charging this component's API with defects manifested by files that refused to consume it is misattribution; the true fact those files evidence is **non-adoption**, which is L-3's identity and is counted there, once. Adding an unused detail region to the 24-line shell is the feedback_kiss_no_contrivance case verbatim. |
| **D-4** | PARTIALLY CONFIRMED — hover/focus-within divergence across the three row renderings | KILLED — inverted: row-level states on an inert row would BE the defect; Audit's hover is the bug | **KILLED AS FILED — B wins; the true defect books as fresh row AL-M8 with A's census as evidence** | The claim prescribes adding state design to AdminListItem. The row is deliberately inert (no handler/role/tabindex at :11) — a row-level hover would advertise an interaction that does not exist. The tree supplies the controlled experiment: the one copy that took this advice, AdminAuditPanel.vue:59-63, paints `hover:bg-accent/50` on a div with no click/keydown/role/tabindex and no interactive child (:64-79) — a phantom affordance — while AdminUsersPanel.vue:78-90 models the correct discipline (hover/focus/role/tabindex strictly conditioned on `paletteCount`). So the axis's prescription would mint the bug the suite already demonstrates. What survives is A's *divergence census* (three shells, three state treatments under the one-anatomy canon), which is real, and its locus is Audit's phantom hover — booked as AL-M8, routed to the wave that owns that file. |
| **C-5** | KILLED — focus rings escape (clip is on the content column only; actions cluster outside it); no focusable content exists; re-filed the text-loss with the correct mechanism | CONFIRMED-latent, INFO — `overflow-hidden` at :17 will clip future focusable slot content | **KILLED AS FILED — A's refutation adopted; B's latent note preserved inside it** | Verified: `overflow-hidden` exists once (:17, the content column); the root (:11) and the actions cluster (:20, a sibling flex item) are unclipped, so every live focus ring (the two Buttons) escapes freely; both call sites slot only non-focusable spans (:50/:52, :100/:102). The axis's distinctive mechanism — the *wrapper* clipping rings or text — has zero live instances, and the real text loss rides the spans' own `truncate`, not the wrapper (A's re-file, booked as AL-M3). B's forward-looking hazard (a future focusable/overflowing content child) is recorded as an INFO latent note inside this kill, not as a standing defect: "a consumer could someday" with one private importer is not a finding. |
| **L-2** | CONFIRMED MINOR on the typography limb ("the comment is factually contradicted by every shipped implementation") | KILLED — "neither supplies" is false: :13 supplies the 8×8 box; the real smell is the caller re-declaring it | **KILLED AS FILED; the surviving truths re-home to fresh rows AL-M1 and AL-M9** | The row as written asserts the component "neither supplies nor validates" the promised primitives. Half false by my read: :13 is a fixed `shrink-0 w-8 h-8` centering box — the component supplies exactly the geometry the comment promises, and B is right that the comment's :3 instruction then induces the sole consumer to *re-declare* `w-8 h-8` redundantly (:46/:96, unclipped in the failure direction — AL-M9, INFO). The typography limb is real but is **not what the axis claimed**: the axis said *unvalidated*; the truth is *factually wrong* — `text-caption` is italic by producer definition (my semantic.css read) and every shipped secondary line refuses it for `text-mono-small` under the recorded F-9 ruling (AdminNamesPanel.vue:11-12/:52/:102; AdminAuditPanel.vue:74-76). A defect the axis gestured past gets booked at its true strength as AL-M1, not credited to this row. |
| **L-4** | CONFIRMED MINOR — four semantics under one visual shell (across the copies) | KILLED — delegation IS a layout shell's contract; the actual consumer set produced identical, well-labeled rows | **KILLED — B wins; same misattribution law as C-4** | "Consumers can produce incompatible or inaccessible rows": the consumer set (n=1 file, 2 byte-identical call sites) produced rows *more* accessible than most of the tree (item-scoped aria-labels :55/:65/:110, aria-hidden glyphs :56/:68/:113). The four-semantics divergence A cites (Users = real disclosure button; Audit = hover-lit inert; Flagged = fully inert; Item = inert) is real and canon-relevant — but it is divergence across the *hand-rolled copies*, i.e. L-3's identity again, not a property of this component's slot API. One defect, one booking. |
| **L-1 severity** | INFO (KISS-correct untyped leaf; the defect is what the prose *says*) | MINOR (sole script-less SFC in demo; `defineSlots` = 0; cheap cure) | **INFO — A wins** | Facts unanimous and re-verified by this seat (sole script-less SFC in the demo tree; defineSlots census 0). With one private importer and two identical call sites, the missing type surface has no live blast radius; the one casualty the unenforced contract has actually produced is the stale-comment drift, which is AL-M1's identity. The `defineSlots<{swatch?, content, actions?}>()` one-liner is recorded as a free rider for any wave touching the file. |
| **L-3 severity** | MAJOR (canon-bound: 1 of 5 routes consumes the component that names the anatomy) | MINOR (structural duplication, divergence begun) | **MAJOR — A wins** | The count is B's own (undercount corrected: full bordered frame verbatim ×3, geometry prefix ×5, content-column grammar re-typed ×2 more — all re-run by this seat), but the severity follows the canon: VISUAL-CONSTITUTION.md:218 binds "one review-row anatomy" across the five-route suite, and the anatomy exists as five *diverging* copies (Audit gained hover+transition; Users gained focus rings; Skeleton diverged in gap and metrics) with exactly one route consuming the named component. Divergence-under-a-binding-canon is MAJOR; mere repetition would have been MINOR. |

## Verdict register — all 18 corpus rows

- **C-1 — CONFIRMED (INFO, latent).** Unconditional wrappers verified: empty `#swatch` still costs 32px + `gap-3`; empty `#actions` collapses to 0 width but leaves the flex gap; no `v-if="$slots.…"`. Zero live reach — both call sites supply all three slots. Latent API-robustness note, nothing more; reader-B's SOURCE-RED-feeding weight rejected.
- **C-2 — KILLED.** "Conflicting row behavior" presupposes row behavior; :11 has none (no handler/role/tabindex), and no consumer adds any. The one real nested-interactive hazard in the suite lives at AdminUsersPanel.vue:78-131 (role=button row containing buttons, with a deliberate `@click.stop` at :106) — a file that does not consume this component. The residual "slot content can violate dimensions" is true of every slot ever shipped and predicts nothing; its one concrete instance (unclipped swatch box) books as AL-M9.
- **C-3 ≡ D-1 — CONFIRMED-RESCOPED (MINOR), one identity, role limb only.** My census: zero list semantics anywhere in `browser/admin/*.vue` — AT announces N unrelated groups instead of a list of N. The house precedent exists and was not followed (PaletteCardGrid.vue `role="list"`). All other limbs refuted: no selected/disabled/expanded field exists on `ProposedColorName`, the row is non-interactive by design, and its commands carry their own accessible names. **Cure mislocated by the axis, corrected by both readers convergently**: `role="list"` belongs on the panel containers (AdminNamesPanel.vue:43/:93) with `listitem` on the rows — a leaf-only `role="listitem"` would orphan; and the canon (VISUAL-CONSTITUTION.md:186) forbids listbox/option *fiction*, not list semantics, so the cure never reaches for listbox. **Route: X-W7** (owns AdminNamesPanel + AdminPane).
- **C-4 — KILLED** (ruled above). Evidence transferred to L-3.
- **C-5 — KILLED AS FILED** (ruled above); B's latent-clip note preserved as INFO inside the kill; the true text-loss defect = AL-M3.
- **C-V ("SOURCE-RED: cannot enforce the semantic or layout invariants its comments promise") — REFUTED as written.** The layout invariant — the only one the comments *assert* (:6-10) — is enforced completely and structurally: `min-w-0` on the row (:11), `flex-1 min-w-0 overflow-hidden` on the column (:17), `shrink-0` flanks (:13/:20); a slot child cannot raise its ancestor's min-width; the chain closes through the consumer (grid `min-w-0` containers :43/:93, `truncate` leaves) and repaired a documented P0 (moderation INOPERABLE at 390px, AdminNamesPanel.vue:2-9). The semantic half survives only as narrow C-3.
- **D-1 — duplicate of C-3**, counted once (both readers agree; recorded so the D axis is not credited independently).
- **D-2 — duplicate**: limb 1 (unlabeled slots) = L-1; limb 2 (reserved space) = C-1. Reader-A's kill of the "three slots reserve space" over-generalization adopted as a fact correction: only the swatch box is hard-sized; content absorbs; empty actions collapse to 0 + gap.
- **D-3 — KILLED (unanimous, ratified).** An explicit, documented width policy exists and is the file's reason for being; the squeeze direction is deliberately *inverted* from the claim (identity truncates so commands stay on-card — the S.W5-12/F-1 cure); live actions ≈ 66px worst case. Applying the axis's cure would regress the recorded P0. The truncated-identity fallback gap books as AL-M3, not here.
- **D-4 — KILLED AS FILED** (ruled above) → replacement row AL-M8.
- **D-5 — RESCOPED-CONFIRMED (MAJOR)** (ruled above). Surviving row: the per-row four-sided stroke + corner radius violates the optical-bench separator grant for all five Admin routes; the named cure altitude is one class change on the anatomy (border/rounding → single inter-row separators) applied suite-wide, amending nothing in the canon. The "repeated everywhere"/"no signature connection" limbs die with reader-B's enumeration. **Route: NO-WAVE-OWNER** — AdminListItem.vue sits in no tranche-X File Bounds (verified across W0-W11), and no X unit claims the separator grammar; the sibling copies' files are X-W4/X-W7/X-W8-owned, so the cure is *reachable* but *unclaimed*. Must be booked at the next formation boundary; re-resolve paths after MT-AU1's `browser/admin/` → `palettes/admin/` re-home.
- **D-V ("SOURCE-RED … only geometry: semantics, states, density, selection, mobile action behavior are absent") — REFUTED as written.** Two of the five absences are correct-by-design (states on an inert row; selection with no selectable model), one is the narrow C-3, "density" has no truth conditions (see UNPROVEN), and "mobile action behavior" is the file's proven strength. What the verdict should have said — the anatomy it names is canon-noncompliant and largely unadopted — appears in no axis sentence.
- **L-1 — CONFIRMED (INFO)** (severity ruled above). Sole script-less SFC in demo; slot contract is prose only; `defineSlots` one-liner recorded as a rider.
- **L-2 — KILLED AS FILED** (ruled above) → AL-M1 (the comment is *wrong*, not merely unvalidated) + AL-M9 (double-spec, unclipped).
- **L-3 — CONFIRMED (MAJOR)** (severity ruled above). The five-copy census with begun divergence is this record's structural spine; C-4's and L-4's transferred evidence lives here. **Route: NO-WAVE-OWNER** for the consolidation (same bounds analysis as D-5); the ALTITUDE of the cure — demo-local shared component vs glass-ui compound primitive — is an **owner call** (both readers concur; feedback_kiss_no_contrivance vs feedback_glass_ui_first_class are in genuine tension), recorded, not decided here.
- **L-4 — KILLED** (ruled above).
- **L-5 — REFUTED AS FILED (prescriptive; the conditional's premise is false).** "If this pattern is common, it belongs in … glass-ui as a compound primitive" — the pattern has one importer and is deliberately private (`index.ts` exports five panels, PI-6 named re-exports only — my read); a producer-versioned `as`/role/density/state API for two call sites is the textbook banned contrivance, and its `detail` slot dies with C-4. Only the word "untyped" survives, as L-1. L-3's confirmation *does* argue for consolidation at the next formation — at an altitude the owner picks.
- **L-V ("a reusable primitive in practice") — REFUTED by enumeration (unanimous, ratified).** One importer, two call sites, withheld from the public surface. The library-boundary frame inverts the true defect: not unconstrained reuse but **non-adoption** (L-3).

## Fresh rows — adjudicated from the readers' missed rosters

Every row re-verified by this seat before booking; severities are this seat's, not the reporting reader's.

- **AL-M1 — MINOR — the component's only contract prose is factually wrong and instructs violation of a later ruling** (reader-A; verified). :4-5 prescribes `text-caption` for the secondary line; `text-caption` is *italic by producer definition* (semantic.css, my read), and all three shipped secondary-line implementations refuse it for `text-mono-small` under the recorded F-9 readout ruling (AdminNamesPanel.vue:11-12/:52/:102; AdminAuditPanel.vue:74-76). Three implementations, zero honour the comment; the L axis even quoted the comment without checking it. Cure: correct the comment to the F-9 truth (and restate the intended hierarchy — see AL-M2). **Route: NO-WAVE-OWNER** (file unowned); per the ColorSpaceSelector precedent, *any* wave touching the file must correct it.
- **AL-M2 — MINOR — the designed primary/secondary size step was silently deleted by the F-9 register swap** (reader-A; verified from producer bytes). `text-small` and `text-mono-small` resolve to the identical `--type-small`/`--type-leading-small` (semantic.css + utilities.css, my reads), so the shipped two-line hierarchy rests on family/weight/color with a **zero size delta**, whereas the comment's design (`text-small` over `text-caption`) carried a real clamp-floor step (0.875rem vs 0.75rem, scale.css). Caveat ruled in: F-9's register choice was deliberate; the size-step loss was its *unruled consequence*. The cure is a deliberate design restatement (the zero delta may be ruled acceptable) — the defect is that it was never ruled. **Route: NO-WAVE-OWNER**, folds into AL-M1's comment correction.
- **AL-M3 — MINOR — truncated identity text has no accessible full value; the sibling row already ships the cure** (reader-A, correcting C-5's/D-3's wrong mechanisms; verified). All four content spans set `truncate` with no `title`/aria alternative (AdminNamesPanel.vue:50/:52/:100/:102); a long name or CSS literal is unrecoverable in any modality, and the identity column is the designed flex loser. AdminUsersPanel.vue:100 ships `:title="user.slug"` on its truncating span for exactly this reason. **Route: X-W7** (owns AdminNamesPanel; kindred to X.W7.f's "exact number preserved in `title`" pattern). Identity guard: if the AdminNamesPanel adjudication books this from its own corpus, ONE identity, cross-booked.
- **AL-M4 — MINOR — the skeleton is not the row's shape: metrics diverge, so the queue shifts when data lands** (reader-A; divergence source-certain, magnitudes live-residue). The grammars literally disagree on the internal gap — `gap-0.5` (Item:17) vs `gap-1.5` (Skeleton:14) — and the skeleton's fixed `h-3.5`/`h-3` bars cannot equal two `--type-small × --type-leading-small` lines across the clamp range; the action lozenge (`h-7 w-14`) matches neither the pending (~66px) nor approved (~30px) cluster. Reader-B's "mirrors the row anatomy zone-for-zone" superlative is *narrowed*, not adopted: the zones mirror, the metrics do not. Pixel deltas (~9-16px/row derived) are UNPROVEN-NEEDS-LIVE. **Route: NO-WAVE-OWNER** — X-W8 holds AdminListSkeleton.vue only for the inert-attr deletion (W8.md:165) under a committed **null-DELTA** witness (W8.md:311), and a parity cure is a deliberate delta that must NOT ride silently inside that unit; book at the formation boundary beside the L-3 consolidation.
- **AL-M6 — MAJOR — four Admin routes ship four different primary-identity registers under the one-anatomy canon** (reader-A; every cell verified). Names `text-small font-medium` Jakarta (:50) · Audit Badge + `text-small text-muted-foreground tabular-nums` (:67-72) · Flagged `font-display font-medium text-subheading` Fraunces 1.272rem (:65) · Users `.slug-pill` = `text-mono-small font-bold` pill (foundation.css:585-587, AdminUsersPanel.vue:97). Four families/sizes/weights for one semantic slot against VISUAL-CONSTITUTION.md:218; AdminListItem.vue:2-5 is the anatomy's only written typography statement and is honoured by zero of the four (and is itself wrong — AL-M1). Suite-scoped; surfaced here because this file names the contract. This is the typographic face of L-3's structural defect — one consolidation cure discharges both plus D-5. **Route: X-W7** for the four panel surfaces (all in its File Bounds) **as a rider — no X-W7 unit claims register unification**; if unclaimed at execution, it travels with the L-3/D-5 NO-WAVE-OWNER block to the formation boundary.
- **AL-M8 — MINOR — phantom affordance: AdminAuditPanel's row paints hover on a fully inert div** (reader-B; verified :59-63 — `transition-colors duration-fast hover:bg-accent/50`, no click/keydown/role/tabindex, no interactive child :64-79). Pointer users get a false interaction signal; no other modality gets anything. The same directory models the discipline (AdminUsersPanel.vue:78-90 conditions all of it on `paletteCount`). This is D-4's replacement row and its concrete refutation. **Route: X-W7** (owns AdminAuditPanel; coherence unit .d).
- **AL-M9 — INFO — swatch sizing specified twice, enforced zero times in the failure direction** (reader-B; verified). :13 pins the 8×8 zone; :3 instructs callers to size it anyway; the consumer duly re-declares `w-8 h-8` (:46/:96); the wrapper carries no `overflow-hidden`, so a non-conforming swatch would overflow silently. Redundant-but-harmless today. **Route: NO-WAVE-OWNER**, folds into AL-M1's comment correction.
- **AL-M10 — INFO — zero test coverage for the component, its consumer, and the one invariant the file exists to hold** (reader-B; my grep: 0 hits across test/ + e2e/). The min-w-0 cure that fixed the 390px P0 has no regression net. X-W7's G13/G14 e2e battery will put the admin panels under test; a single narrow-viewport row assertion is recorded as a **rider** on that battery (not spec'd there today). **Route: X-W7 (rider).**

## Cross-books and cross-refs (anti-rename — one identity each, counted where the payload lives)

- **AL-X1 — the triplicated loading announcement** (reader-A filed as missed; **already booked with a probe payload as AdminNamesPanel corpus C-9 MAJOR**, my read of that file's :400-418, with the exact cure: one `role="status"` container owning the label, skeleton drops its per-instance `role="status"`). Facts verified here independently: Skeleton :10-11 stamps `role="status" aria-label="Loading"` per row; every panel wrapper is a role-less div whose `aria-label` AT ignores (7 sites, my census); AdminTagsPanel.vue:54-55 is the in-suite correct shape. Recorded so AdminListItem/Skeleton are not read clean; **counted at the AdminNamesPanel adjudication**. Surface split: panels = X-W7, skeleton = X-W8's named AdminListSkeleton edit. Reader-A's "no axis saw it" and reader-B's announced-loading superlative are both corrected by this one row: the announcement exists (good instinct), is triplicated and generic (the defect), and the suite corpus already knew.
- **AL-X2 — the dead required `cssColorOpaque` prop** (reader-B filed as missed; **already booked as AdminNamesPanel corpus C-11 MINOR + L-4 MAJOR** — my read; declaration-only at :140, fed by AdminPane.vue:52 from `inject(CSS_COLOR_KEY)!` at :94 — an inject wired into a black hole, and a MEMORY-contract violation per that corpus). Verified here by my own grep. **Counted at the AdminNamesPanel adjudication**; surface = X-W7 (owns both files); the AdminUsersPanel adjudication's adminPort five-seam BANK already carries "the AdminNamesPanel-convergence question".
- **AL-X3 — inert `variant=` props on the row's action Buttons** (:55/:61/:106): glass-ui 7.0.0 `ButtonProps` has **no `variant`** (my d.ts read — emphasis/tone/size/iconOnly/loading). **Already banked MAJOR with a rendered receipt at AdminNamesPanel corpus C-2** (repo count 66; the MT-BTN1 22→0 carry in the AdminUsersPanel adjudication is the same program). Recorded only so this file is not read clean on the axis; never re-booked.

## Killed-claims register (refutations, terminal)

| id | refutation in one line |
|---|---|
| C-2 | No row behavior exists to conflict with; the real nested-interactive case lives in a non-consumer (Users, with `@click.stop` handled); "arbitrary content can violate dimensions" predicts nothing. |
| C-4 | Zero consumers created detached siblings; the routes that hand-rolled row+detail never import the component — that fact is L-3's, and a pre-emptive detail slot is KISS-banned. |
| C-5 | The clip is on the content column only; the actions cluster and its focus rings sit outside it; no focusable content exists in any call site; the true text loss is the spans' `truncate` (→ AL-M3). |
| C-V | The promised layout invariant is structurally enforced and repaired a documented 390px P0; SOURCE-RED indicts the one thing the file provably delivers. |
| D-3 | `shrink-0`/`min-w-0`/`flex-1` is a complete, documented, deliberately *inverted* squeeze policy — the axis's cure would push approve/reject back off-card at 390px. |
| D-4 | Row-level state design on an inert row manufactures phantom affordances; the suite's one compliant-with-this-advice copy (Audit) is the bug (→ AL-M8). |
| D-V | Two absences are correct-by-design, one is narrow C-3, "density" has no truth conditions, and mobile behavior is the file's proven strength. |
| L-2 | :13 supplies the promised 8×8 primitive, so "neither supplies" is false; the surviving typography truth is a *wrong* comment, not an *unvalidated* one (→ AL-M1). |
| L-4 | The actual consumer set produced identical, well-labeled rows; the four-semantics divergence lives across non-consuming copies = L-3's identity. |
| L-5 | The "if common" conditional fails (one private importer); a producer compound API for two call sites is the banned contrivance; consolidation altitude is an owner call. |
| L-V | "Reusable primitive in practice" refuted by enumeration (1 importer, 2 call sites, unexported); the true defect is non-adoption, booked at L-3. |

## Superlatives (provenance: reader consensus + this seat's re-reads)

1. **AdminListItem.vue:6-10 — the finest engineering comment in the cluster** (both readers, ratified). It names the exact CSS mechanism (grid-item `min-width:auto` resolving to the inner flex row's min-content), the measured symptom (~850px track at 390), and the cure, under a ledger id (S.W5-12 F-1), corroborated by the consumer's own account of the P0 it fixed (AdminNamesPanel.vue:2-9). Causal prose, not decoration.
2. **:11/:13/:17/:20 — the three-zone truncating-row recipe, textbook and minimal** (both readers, ratified). `shrink-0` flanks, `flex-1 min-w-0` middle, `min-w-0` on the row itself; the invariant is unfalsifiable from inside a slot; 13 lines, zero script, zero dependencies.
3. **RTL-clean by construction, at zero cost** (reader-A; verified — no physical-direction property in the file; every inset and gap symmetric). Discharges the D axis's own RTL audit item statically.
4. **Honest non-interactivity** (both readers). No dead control ever enters the tab order; the sibling Users row documents the same discipline from the interactive side (role/tabindex conditioned on `paletteCount`).
5. **Consumer a11y, genuinely good** (reader-B; verified). Every icon-only command carries an item-scoped accessible name and every glyph is `aria-hidden` (:55/:56, :65/:68, :110/:113) — the delegation the L axis feared produced the opposite of its prediction.
6. **Destructive restraint (F-8), cluster-consistent** (reader-B; verified :64/:109 + Flagged:96 + Users:125): red arrives on hover/focus, never as resting beacons — the "no row-level destructive design" complaint names a deliberate, documented, suite-wide design.
7. **Disciplined public surface** (reader-B; verified `index.ts`, PI-6): the leaf stays directory-private, so any change's blast radius is one file — the exact boundary the L axis misread as the defect.
8. **PRM-inert by construction** (reader-A; motion-quarantine §7.4 lists AdminListItem C·D·L "no motion content"; the only keyword hits are excluded as structural language). Zero motion, nothing owed to either reduced-motion guard.
- *Narrowed, not adopted*: reader-B's "loading state is announced" superlative — the per-row `role="status"` + zone mirroring is the right instinct with the wrong multiplicity and drifted metrics (AL-X1, AL-M4). The **M-12** quarantine citation it leaned on remains valid for what M-12 actually says (the producer's scan is PRM-gated; the corpus's one correct reduced-motion row).

**Motion/PRM law compliance**: rows cited here are **M-09** (TRUE — skeleton swap contract parent-owned) and **M-12** (TRUE — producer PRM gate; also establishes the skeleton's `surface`/`variant` attrs are inert, cured at X-W8). The FALSE rows touching this family — **M-11, M-13** (AdminListSkeleton D axis) — are **not carried** anywhere in this record, per the quarantine law.

## UNPROVEN-NEEDS-LIVE (visual-audit inputs)

1. **Skeleton↔row pixel delta** at 390×844 and 1440×900, both schemes: divergence is source-certain (`gap-0.5` vs `gap-1.5` alone), the ~9-16px/row magnitudes are derived from clamp interpolation, never measured. Probe: mount the Names queue, capture skeleton→data swap, measure row heights (AL-M4's gate-shaped fact).
2. **Secondary-line contrast**: `text-mono-small text-muted-foreground` over the row ground resolves through `color-mix()` in both schemes — not decidable from source. No axis raised it; this seat could not close it statically.
3. **AT behavior of the triple announcement** (AL-X1): the DOM shape is source-certain; actual VoiceOver/NVDA utterance counts and the ignored generic-div label need live cells. Rides the AdminNamesPanel adjudication's probe set.
4. **200%-text and Safari cells** (the C:15/D:19 matrices): RTL is discharged statically (superlative 3); the truncation consequence is booked (AL-M3); the *rendered* result at 200% text, phone/desktop Safari, remains live-only.
5. **Empty-slot forward rendering** (C-1): no consumer triggers it; INFO stands on source alone.
6. **D-V's "density … absent"** — left unadjudicated by both readers and this seat concurs: a single fixed `px-3 py-2.5` rung with no size axis and no consumer requesting a second rung has no truth conditions to test. Recorded as not-truth-apt, not as a defect or a kill.

## Routing summary (dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave)

| row | severity | terminal route |
|---|---|---|
| D-5 canon border (suite) | MAJOR | **NO-WAVE-OWNER** (AdminListItem.vue outside all X bounds; separator grammar unclaimed; re-resolve after MT-AU1 re-home) |
| L-3 five-copy anatomy / non-adoption | MAJOR | **NO-WAVE-OWNER** (consolidation unclaimed; altitude = owner call) |
| AL-M6 four identity registers | MAJOR | **X-W7 (rider — surfaces owned, unification unclaimed)**; travels with L-3/D-5 if unclaimed |
| C-3 list semantics | MINOR | **X-W7** (containers at AdminNamesPanel.vue:43/:93) |
| AL-M3 truncate/no-title | MINOR | **X-W7** (AL-namespane identity guard) |
| AL-M8 phantom hover (Audit) | MINOR | **X-W7** |
| AL-M1 wrong comment · AL-M2 flattened step · AL-M9 double-spec | MINOR/INFO | **NO-WAVE-OWNER** — one comment-correction edit; mandatory for any wave touching the file |
| AL-M4 skeleton parity | MINOR | **NO-WAVE-OWNER** (must not ride X-W8's null-DELTA attr deletion) |
| AL-M10 coverage | INFO | **X-W7 (rider on G13/G14 battery)** |
| C-1 · L-1 | INFO | free riders on any future wave touching the file (`v-if="$slots.…"`, `defineSlots`) |
| AL-X1 · AL-X2 · AL-X3 | — | cross-booked: counted at the AdminNamesPanel adjudication (C-9, C-11/L-4, C-2) |

## DISSENT — preserved, not ruled away

1. **Reader-A's C-4/L-4/D-4 confirmations vs reader-B's kills**: ruled for B on all three under one law — *a defect manifested only by files that do not consume the component is not a defect of the component's contract; it is evidence for the non-adoption row*. A's censuses were accurate and are preserved as L-3/AL-M8 evidence; A's verdict labels sat on the wrong identity.
2. **Reader-B's D-5 kill**: ruled against. B's enumeration was correct and its two killed limbs stay killed, but B adjudicated a canon question without opening the canon — the optical-bench separator table names the shipped border a defect in so many words. A kill must engage the strongest surviving limb of the row it kills.
3. **Suite-blindness, both directions (method lesson)**: both readers declared discoveries ("none saw it") that the payload-bearing AdminNamesPanel corpus had already booked with probes (C-9, C-11/L-4) — and reader-B praised as a superlative (announced loading) what that corpus had already indicted. Component-scoped adjudication must sweep the sibling corpora of the component's own consumers before claiming novelty or cleanliness.
4. **Reader-A's MAJOR on the triplication and dead prop**: not ruled here — severity for cross-booked rows belongs to the adjudication that owns the identity (AdminNamesPanel's, where the corpus already splits C-11 MINOR / L-4 MAJOR).
5. **Both readers' provenance caveat stands verbatim**: 10-11 of 15-18 rows died on contact with the tree — consistent with a REPORT-AUTHORED corpus that no challenge seat ever defended. The kill rate is evidence about the corpus's provenance class, recorded for the formation's calibration.

## Closing verdict

**SOURCE-AMBER, CANON-RED-AT-THE-SUITE.** The three SOURCE-RED axis verdicts are refuted: this 13-line shell enforces the one invariant it promises, repaired a documented 390px P0, and is the best-commented, most restrained file in its directory. What is actually wrong is above the file's pay grade and below the axes' sightline: its row anatomy violates the optical-bench separator canon on every Admin route (D-5, MAJOR), exists as five diverging hand-copies with one consumer of the component that names it (L-3, MAJOR), and carries four typographic identities for one semantic slot (AL-M6, MAJOR) — one consolidation cure, owner-priced, discharges all three, and **no tranche-X wave owns it**. 5 corpus rows confirmed, 11 killed, 8 fresh rows booked, 2 identities cross-booked to the AdminNamesPanel corpus, 6 live residues filed for the visual audit.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.
