claude-fable-5 (served model id)

# fr-PaperSidebar — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · model receipt `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/paper/PaperSidebar.vue` (283 lines) · installed pins re-probed by this seat: glass-ui **4.0.0**, latex-paper **0.2.1**, producer glass-ui HEAD **8.0.0**.
**Corpus**: 3 challenge axes (L·library 15 rows / C·consumption 20 rows / D·design 33 rows, all `claude-opus-5[1m]`) → 2 hostile re-readers (design-and-usage · library-and-code) returning one merged verdict set: 33 confirmed identities · 17 kills · 7 new finds · 10 superlatives · 8 unproven.
**Method (L-18 both ways)**: this seat re-derived every kill whose refutation was load-bearing and every confirmed row whose proof chain crossed a producer boundary, with its own instruments, before adopting anything. Where the re-readers and the axes disagree, the tree decided; where this seat and the re-readers disagree, it is recorded below (SEAT CORRECTIONS).
**Arbiter's own instruments this session (all static + node against the installed dists; zero browser probes — probe parsimony)**: `postcss.parse` over glass-ui 4.0.0 `dist/styles/index.css` (35 top-level nodes; **zero** valid `@source`; one garbage at-rule literally named `` source` `` spanning :203–222 — the M1 decider, run by this seat, not inherited); producer source `/Users/mkbabb/Programming/glass-ui/src/styles/index.css` :285–312 read unspliced (dist-generation bug confirmed); emission census `grep -c` over `dist/styles/components.css` + `dist/glass-ui.css` — `ui-glyph` 0/0 · `animate-collapsible` 0/0 · `transition-collapse` 0/0 vs `whitespace-nowrap` 1 · `control-h-md` 1 (the M3 live/dead line); `@utility transition-collapse` found at `dist/styles/utilities/btn.css:67` (D-B1-leg-2 kill confirmed); tw-animate-css collapsible keyframes present; `dist/CollapsibleContent-C_s6fG7r.js` dumped whole (1916 bytes): `role` 0 / `aria-labelledby` 0 / `provideDisclosureIds` 0 (L-5(b) kill) and `Collapsible` **declares** `unmountOnHide: { type: Boolean }` (producer-note kill); `dist/sidebar.js` `useSidebarState` extracted verbatim — `l(e)` isExpanded and the **symmetric** `d(e)` toggle, ten-member return, raw `sections` snapshot (L-1 core + scope-kill + L-4 + L-9); `.btn-pill` home = `dist/styles/glass/surfaces.css`, `.tap-squish` = `dist/styles/utilities/base.css:258` with the PRM squish-reset at :273 (D-M6 chain); latex-paper `dist/vue.js` `handleSidebarPointer` with the hard-coded `closest("[data-toc-id], .sidebar-top-btn")` (M2) and 0 `matchMedia|prefers` hits (L-11); reka `TooltipRoot` state domain `closed|delayed-open|instant-open` + `TooltipTrigger.js:89` stamping (C-M4); e2e guard re-read at `paper-performance.spec.ts:~118–129`; template lines :51/:65–117 all re-read (click contract, dead class, chain gate, three unrolled blocks, `v-html`, `var(--section-color-${si})` ×3); ramp census `--section-color-N:` = exactly **13** definitions; IconChip fallback form confirmed; `style.css:113–127` `--section-color-5` fork in `:root` + `.dark`; coarse arm `--ui-scale:1.5` / `--control-floor:2.75rem` under `(pointer:coarse)`; `fonts.css` — CM Serif ships exactly normal+bold (×roman, ×italic); contrast **triple-derivation** (axis, re-reader, this seat): label 2.39/3.00 · hover 1.022 · glyph 1.87 — reproduced to the digit; `\chapter{` count 11 + 34 `\bibitem` corroborating the 13-root arithmetic; `treeIndex` 1 hit · `is-active-sub` 1 hit · tsconfig no unused-flags · lucide in devDependencies only · scripts = dev/build/preview/test:e2e/test:e2e:ui (no lint, no vitest).

## Scoreboard

**40 adjudicated rows: 1 BLOCKER · 20 MAJOR · 11 MINOR · 8 INFO** — 33 confirmed challenge identities + 7 re-reader finds, all adopted after this seat's re-derivation. **17 killed** (all 17 kills upheld; one corrected in detail — see SEAT CORRECTIONS). **10 superlatives.** **8 UNPROVEN-NEEDS-LIVE** (SS-13 inputs).

Blocker survivor: **M1** — the installed glass-ui 4.0.0 ships a syntactically corrupt `dist/styles/index.css` (dist-emission spliced two `@import`s into the middle of the BA.W-EMISSION comment; CSS comments do not nest; the shipped `@source "../*.js"` backstop is swallowed into a garbage at-rule and **never registers**). Re-proved from scratch by this seat: `postcss.parse` → 35 nodes, 0 valid `@source`, the one source-shaped node named `` source` ``. Every glass utility that is neither pre-shipped in `components.css`/`glass-ui.css` nor independently present in fourier `src/` paints nothing; Tailwind's own stylesheet loader hard-errors on the file (the `'s own` apostrophe at :203 lives outside any comment), so the `@tailwindcss/vite` / CLI paths are broken at this pin — fourier survives only because Vite 8's `@tailwindcss/postcss` route inlines with postcss's lenient parser first. **This DECIDES the banked fr-CollapsibleSection M-7 emission hinge, negative, and corrects banked K-5** (the directive is shipped but inert — both prior framings were wrong). Producer source at HEAD is intact: a dist-generation bug, not an authoring one.

The component itself carries no blocker: L's "mediocre, not broken" holds. What is blocker-weight *in aggregate* is the a11y quartet on the flagship reading route — D-B2 (label 2.39:1) + D-B3 (20×20px / sub-3:1 / title-only name) + L-5(a)=D-B4 (a disclosure with zero ARIA) + D-M4-extended (4 live ramp stops under AA, worst 3.58:1) — none of it visible to any gate in the repo (M7).

## Seat corrections (where this seat challenged the re-readers)

1. **D-N6 kill upheld, timing corrected.** The re-reader killed D-N6's "font-weight transition is inert" with "the interpolation crosses the 500/501 boundary partway through → a ~75ms-delayed hard face swap." The mechanism is right, the arithmetic is not: interpolating 500→600, the computed value exceeds 500 at t≈0⁺, and under CSS font-matching (desired ≤500 resolves down to the 400 face; >500 resolves up to 700) the bold-ON swap is effectively **instant**, while the reverse 600→500 stays above 500 until t≈150ms — so the observable artifact is a **full-duration lagging un-bold**, not a delayed bold-on. The kill's substance (the leg is observable, not inert; and the weight channel is a real regular→bold face swap, killing D-B4's "sub-perceptual" sub-claim) stands on this seat's read of `fonts.css` (CM Serif: exactly normal+bold faces). Detail corrected for the repair's acceptance test.
2. **D-M7(a) is DEMOTED-TO-UNPROVEN, not killed-with-prejudice.** The re-reader's production-bundle order argument (route-chunk sheet injected after the entry sheet → the 8px radius holds) is plausible and probably right, but bundler injection order is exactly the kind of coin-flip the corpus's own falsifier discipline refuses to settle statically. Booked in the UNPROVEN register with the re-reader's prediction attached. D-M7(b) — the stacked second ring — is independent of the tie and CONFIRMED.
3. **Everything else adopted after independent re-derivation.** This seat attacked the seventeen kills expecting at least one weak refutation; all seventeen held (fifteen re-derived directly, two — the Transformer-walk kills D-M4-premise and D-M11-sliver — adopted on the re-reader's in-memory derivation, which this seat corroborated at its edges: 11 `\chapter` + orphan `\section` + bbl-gated Bibliography = the 13-root arithmetic; 34 `\bibitem` counted; 13-stop ramp counted; and the same derivation independently reproduced L-7's single empty-preview node). The corpus's failure modes are exactly two — BASELINE (producer-source facts asserted at the adopted pin) and EMISSION (assuming a cva string paints) — and the re-readers named both.

## Adjudicated defect roster (id · sev · claim · disposition — all dispositions ADJUDICATED, routes are agglomeration input for the forming X·F specs, M-25)

| id | sev | claim (adjudicated form) | route |
|---|---|---|---|
| **M1** | **BLOCKER** | glass-ui 4.0.0 `dist/styles/index.css` is syntactically corrupt; the `@source` backstop never registers; strict Tailwind loaders hard-error on the sheet. Decides banked fr-CollapsibleSection M-7 negative; corrects banked K-5. | **F.W0 substrate pre-gate + glass-ui BH relay** (dist-generation bug; producer source intact). F.W1 pre-gate: one `npm run build` + `grep -c animate-collapsible` on emitted CSS — prediction: build succeeds, grep 0. |
| L-1 = C-M3 = D-M5 | MAJOR | Root-row click couples navigate+toggle → clicking the chapter you are reading collapses it; scroll-follow dies while `activeId` names an unmounted row and re-dies on every return (scope as corrected — recoverable, not session-terminal; toggle re-verified symmetric by this seat). Also writes disclosure state for the 3 childless roots. e2e receipt in-tree. | F.W4 — split navigate from toggle (sibling `MobileFloatingToc.vue:155` is the reference); acceptance test must use the corrected scope. |
| L-2 = C-B3 | MAJOR | Two `PaperSearch` trees over one `PaperSearchState` → two `<Teleport to="body">` modals, document-global `querySelector(".search-modal-results")` (`PaperSearchModal.vue:32`), shared-ref focus race. Mount topology re-verified (`PaperSidebar.vue:51` unconditional; `MobileFloatingToc.vue:111` v-if). | F.W4 — hoist one modal to `PaperView` or gate the sidebar's instance. Visual stacking = SS-13. |
| L-3 = C-M6 = D-N9 | MINOR | `treeIndex: Map<string, any>` — dead prop, sole `any`, parent pays to compute it (1 grep hit re-verified). | F.W4 — one edit with M5: delete the map, add the preview map. |
| L-4 = C-B2 = D-N10 (+M6) | MAJOR | Three parallel derivations of one 98-node tree across two producers; 8 of 10 `useSidebarState` members unused (ten-member return re-verified from dist); both producers export `useSidebarFollow` (export line re-verified) and the app picks latex-paper's follow + glass-ui's expand with nothing choosing a winner; the arbitration point is the untyped `defineExpose` seam (M6). | **F.W1 — OWNER-RULING escalation** (which producer owns the ToC model), then F.W4 executes the collapse (delete `paperTree.ts` adapter + the losing index). |
| L-5(a) = C-M2 = D-B4 | MAJOR | No `CollapsibleTrigger`: the disclosure control announces no state (no `aria-expanded`/`aria-controls`/`aria-current` anywhere in the file — re-verified: state reaches the DOM only as reka `data-state`); `@update:open` at :68 is dead and becomes a double-toggle the day a trigger lands. At the 4.0.0 pin the harm is silence, not a dangling IDREF (L-5(b) killed); D-I1's 7.0.0-era IDREF break must be re-derived at whatever tag F.W1 adopts. | F.W4 — `CollapsibleTrigger as-child` restructure, a **prerequisite** of F.W1 (D-I1). |
| L-6 = C-m1 = D-M9(a) | MINOR | `is-active-sub` dead class (1 hit repo-wide, re-verified; 0 in built CSS), costing an `isInActiveChain` recursion per row per render. | F.W4 (dies inside M4's single-row-component cure). |
| L-7 | MINOR | Exactly one of 98 nodes yields an empty tooltip bubble (`sturm-liouville-completeness`) — the corpus's most precisely-evidenced row, from-scratch re-derived by the re-reader. | F.W4 — guard the shim's content on `text`. |
| L-8 = C-m3 = D-N5 | MINOR | Depth-2 rows: no tooltip, no class binding; three levels, three state-conveyance strategies. | F.W3 — rides R3-7a's Tooltip migration budget (+1 callsite, which M4's row component makes free). |
| L-9 = C-m7 | MINOR | `sections` snapshotted while sibling options are getters; internal index can never rebuild (construction-walk re-verified in dist). Latent today (build-time constant). | F.W4. |
| L-10 = C-M5 | MAJOR | `var(--section-color-${si})`, unbounded index, no fallback, ramp exactly saturated **13/13** (both counts re-verified); producer's own IconChip uses a fallback (re-verified); fourier forks stop 5 app-wide in `:root`+`.dark` (re-verified) — the only theme-varying, only-hsl stop in an oklch ramp. | F.W4 (fallback + fork repair in the ramp's own model) + **glass-ui BH relay** (headroom/rebaseline ask, with D-M4). |
| L-11 (+M2 rides) | MINOR→relay | latex-paper's damped rAF sidebar-follow: no PRM gate anywhere in dist/vue.js (0 hits re-verified), no motion option; a third ungated clock missing from lane-frontend §8's two-clock inventory. | **latex-paper relay** (one relay carries L-11 + M2); amend lane-frontend §8. |
| L-12 | INFO | Mounted-but-invisible below 1024px, honestly bounded by reka `unmountOnHide` default true (bound mechanism re-proved at the pin). | F.W4, low priority. |
| L-13 | INFO | Dead `props` + two dead `select` emits in the search stack (0 `emit(` calls re-verified). | F.W4. |
| L-14 = C-m10 | INFO | Three `v-html` sinks, no escaping, opposite trust posture to the sibling derivation; not a vulnerability today (build-time frozen content). | Book only. |
| L-15 | INFO | Idiom drift with `MobileFloatingToc` on four axes over the same composable. | F.W4 (largely dies inside M4). |
| C-M1(a) = D-M2ⁿᵒʷʳᵃᵖ | MAJOR | `whitespace-nowrap` from the Button base is live (pre-shipped, emission re-verified) and never reset — ToC titles cannot wrap; `line-height:1.35` is the authored-to-wrap tell. | F.W4. |
| C-M1(b) = D-M2ᵒᵛᶠ | MAJOR | `.sidebar-nav` `overflow-y:auto` + unset `overflow-x` computes `overflow-x:auto` → horizontal scroll axis. Amount = SS-13. | F.W4. |
| D-M1 | MAJOR | Every row locked to 40px (`--control-h-md` pre-shipped, re-verified) against CSS authored for ~24–31px; `display:block` defeats `.btn-pill`'s centring (~9.4px dead space, arithmetic-certain); the three-level density gradient is inert. Coarse-pointer arm: 60px nominal (re-verified `--ui-scale:1.5`), device matrix = SS-13. | F.W4 — `height:auto` / restore flex; three declarations. |
| D-M3 | MAJOR | Hover surface 1.022:1 light / 1.08 dark (triple-derived); the active row — the likeliest pointer target — has zero hover response (inline style pins color; `.is-active{background:none}` wins by order). | F.W4. |
| D-B2 | MAJOR (blocker-weight in the a11y aggregate) | "Contents" heading 2.39:1 / 3.00:1 vs a 4.5 floor (triple-derived to the digit); D.W4.d remediated twelve lines away and never measured it — no axe pass visits `/paper`. | F.W4 (full-strength token) + F.W9/W10 (axe on `/paper`). |
| D-B3(a)(b)(c) | MAJOR (aggregate as above) | Scroll-to-top: 20×20px (SC 2.5.8: 20<24 stands; the "defeats the touch floor" framing killed — desktop floor is 0px, coarse box would be 60px, both re-verified); glyph/border never reach 3:1 in any state (1.87/1.27/1.90 re-derived); accessible name = native `title` only, the one control denied the project Tooltip. (d) icon-half killed — the chevron renders at the authored 12px; `--ui-glyph` never emits (M1). | F.W4 — ≥24px, full-strength tokens, real name. |
| D-M4 extended | MAJOR | Light-arm ramp failures are **4 stops, not 3** — 4.36/4.48/4.33/**3.58** — and the worst (stop 11, appendix B, live at 13/13) was missing from the axis's own verdict table because of the killed `^\chapter` premise. Numbers validated (re-reader matched the axis's raw output; this seat re-derived the method's other cells to the digit). Dark arm rests on the axis's own numbers (SS-13). | F.W4 (fourier-side overrides in oklch) + **glass-ui BH relay** (rebaseline ask, with L-10). |
| D-M6 | MAJOR | Scoped `transition` shorthand clobbers the `.btn-pill`/`.tap-squish` coherent set (both homes re-verified as shipped plain rules; PRM squish-reset at base.css:273 survives by accident) — press-squish snaps; 0.25s vs `--duration-fast`; `--ease-out-expo` on a colour cross-fade against the producer's own written doctrine. | F.W4. |
| D-M7(b) | MINOR | Two focus rings stack — the global rule overrides outline+radius, says nothing about glass's ring `box-shadow`. (a) radius-square = order-dependent, demoted to UNPROVEN. | F.W4. |
| D-M8 | MAJOR | Hand-rolled weaker copy of `cartoon-card` (1.36:1 edge vs the shim's ~1.9:1); census corrected to 25 occurrences / **15** files (re-reader's re-measure; the in-tree comment is stale the other way). | F.W4 — adopt the shim. |
| D-M9(b)(c) | MAJOR | In-chain state has no visual expression at all (only `isActive` paints); level 3 mounts only when already active — the tree cannot be surveyed; no wrapper, so no transition even in principle. | F.W4. |
| D-M10 | MAJOR | Both sidebar actions smooth-scroll PRM-ungated (`useScrollNavigation.ts:191,:242` re-verified; far-jump paths already `instant`) — one conditional from correct, fifteen lines from the in-repo idiom. | F.W4. |
| D-M11 (type-level) | MINOR | No empty/loading/error state; `v-if="section.subsections"` truthy for `[]`. The observable sliver does NOT occur in this paper (killed — 0 nodes with `subsections: []`); the contract hole survives. | F.W4 — `?.length`. |
| C-M4 | MAJOR | The e2e "expand-if-closed" guard reads the **Tooltip's** `data-state` (domain re-verified: never `"open"`), so it is a no-op and the suite churns exactly as its comment forbids — a structural consequence of stacking tooltip-trigger and would-be disclosure-trigger on one element. Corrected cite :124–129. | F.W4 (dies with the `CollapsibleTrigger` restructure) + F.W9/W10 (guard keys on the Collapsible root). |
| C-m8 | MINOR | `.sidebar-sublist-wrapper{overflow:hidden}` restates a producer invariant (genuinely duplication — `.overflow-hidden` emits via fourier's own usage; its two class-string siblings do not). | F.W4 — resolve with the D-B1 animation decision. |
| C-m9 | MINOR | `lucide-vue-next` runtime-imported from devDependencies (re-verified: devDeps only; Dockerfile full `npm ci` masks it); also 1 of 35 sites on the `@lucide/vue` rename surface. | F.W0 (dep placement) + F.W1 (rename). |
| D-B1 (conclusion) | MAJOR | The subsection disclosure has **no animation** while two comments assert it does; the chapter subtree pops. Chain corrected by the re-readers (legs 2 and 5 killed; the true mechanism is M1); the retired `0fr→1fr` shim was the only thing that ever animated it. | F.W4 — decide at the uplift (fix emission or adopt the 7.x+ `disclosure-content` route), and correct both false comments in the same commit. |
| M2 | MAJOR | latex-paper's shipped `useSidebarFollow` hard-codes fourier's private scoped class `.sidebar-top-btn` in its pointer-exemption (re-verified verbatim in dist/vue.js `handleSidebarPointer`) — dependency inverted; any rename/restyle (which D-B3's and D-M8's cures both touch) silently converts scroll-to-top clicks into sticky manual-override suspensions. | **latex-paper relay** (same relay as L-11) — replace with a data-attribute hook; F.W4 must not rename the class before the relay lands. |
| M3 | MAJOR | The emission law of this substrate: glass recipes expressed as **arbitrary variants are dead**, **pre-shipped recipes are live** (line re-verified by this seat's greps), and the corpus's Button-chassis verdicts split along it by luck. Repair prescriptions inherit the error: `size-3` is a no-op; a naive `@source` widening resurrects the 40px icon box + `--ui-glyph` override simultaneously — three findings flip state together. | **F.W1 pre-gate law** + SS-3/SS-4 spec input: the one-command discriminator (`grep -c '<class>' components.css glass-ui.css`; 0/0 ∧ absent from `web/src` ⇒ not painting) goes into the wave spec. |
| M4 | MINOR | The ToC row is hand-unrolled ×3 (×5 counting the sibling) — L-6/L-8/D-M9/D-N1/D-N2/C-m5/L-15 are symptoms of one cause; a single recursive row component makes the divergences unrepresentable and R3-7a's +1 tooltip free. | F.W4 — the structural cure the roster's row-level fixes hang off. |
| M5 | MINOR | `getPaperPreview` uncached in the render path, invoked per row per `activeId` tick, and is the sole reason the sidebar pins the full content payload; the cure is a preview `Map` in exactly the slot the dead `treeIndex` prop vacates — two findings cancel in one edit. | F.W4 — with L-3. |
| M6 | INFO | The only `defineExpose` is an untyped seam feeding a third package's rAF clock; both producers ship `useSidebarFollow`; the two-producer split runs through this seam. | Folded into L-4's OWNER-RULING row (F.W1). |
| M7 | INFO | None of the corpus's dead surface is reachable by any gate: no `noUnusedLocals`/`noUnusedParameters` (re-verified), no lint script, no vitest, no axe on `/paper` (all re-verified). Two of the four needed gates are one config line each. | F.W0 (tsconfig flags) + F.W9/W10 (axe pass on `/paper`, lint script). |
| R5-7/R6-5/R3-7a/R6-8 | INFO | The three inherited Codex coordinates reproduce verbatim; the API leaf is genuinely absent (0 of 45 operations); the instance denominator is now pinned exactly: **13/51/34 = 98** vs 9 counted callsites. | Fold by reference to the intake rows (identity guard) — F.W4 adopts the iterable-provenance recommendation. |

## Killed-claims register (17 — all upheld by this seat; refutations condensed, each re-derived or corroborated as noted)

1. **L-5(b)** (dangling `aria-labelledby` landmark ×10) — KILLED, baseline-law (K-12): producer-source-at-7/8.0.0 asserted against the 4.0.0 pin. This seat dumped the installed chunk whole: zero ARIA. Survives only as D-I1's forward row, re-derived at whatever tag F.W1 adopts.
2. **L-5 producer note** (`unmountOnHide` stripped, mount-while-closed impossible) — KILLED: the installed pin **declares** the prop and forwards it (re-verified). Same baseline substitution.
3. **D-B3(d) = C-M7-icon = C-m2** (`h-3 w-3` inert under `--ui-glyph`) — KILLED: the variant is emitted by NO stylesheet (0/0 re-verified; root cause M1). The chevron renders at the authored 12px; the prescribed `size-3` cure is a no-op. Three rows across two axes invert on one emission fact.
4. **D-M4 live-set premise** (11 chapters → stops 0–10) — KILLED by the Transformer re-derivation: **13 roots**, ramp exactly saturated, stop 11 (3.58:1, the worst failure) live and missing from the axis's own verdict table. `grep -c '^\chapter'` is the wrong instrument for this parser. Corroborated by this seat's arithmetic (11 `\chapter` + orphan §Introduction + bbl-gated Bibliography).
5. **D-B1 leg 5** (no `@source` anywhere) — KILLED: the directive IS shipped (glass's own cascade file) — and re-asserting the banked fr-CollapsibleSection K-5 premise is a re-run of a killed row. M1 corrects both: shipped but syntactically dead.
6. **D-B1 leg 2** (`transition-collapse` not defined by the producer) — KILLED: `@utility transition-collapse` at `dist/styles/utilities/btn.css:67` (re-verified) + tw-animate-css keyframes. Definition exists; **emission** fails. Mis-locating this mis-routes the relay.
7. **D-N7** (`touch-action: pan-y` dead below-desktop) — KILLED: `min-width:1024px` is a width query, not a pointer query; the coarse arm (re-verified) has no width qualifier; on coarse ≥1024px devices the declaration is load-bearing. The axis contradicted its own device model.
8. **D-B4 weight sub-claim** (500→600 sub-perceptual) — KILLED and inverted: CM Serif ships exactly normal+bold (re-verified), so the delta is a full regular→bold face swap — a real non-colour channel.
9. **D-N6** (`transition: font-weight` inert) — KILLED (observable lagging face swap), with this seat's timing correction: bold-on ≈ instant, un-bold lags the full 150ms (see SEAT CORRECTIONS). Four faces declared, not three.
10. **D-M11 live sliver** — DECIDED negative without a browser: 0 of 98 nodes carry `subsections: []`; the three childless roots omit the key. Type-level hole survives (roster).
11. **L-1 scope** ("sticky forever; follow dies for the session") — KILLED as overstated: toggle symmetric (re-verified verbatim), follow self-recovers outside collapsed chapters. Still MAJOR; the repair's acceptance test changes.
12. **C-B1** (oklch-blind `colors.ts`, `#888888` palette) — killed as a NEW FILING, not on fact: it is banked **fr-BasisCanvas D-1/BC-1/C-1** (BLOCKER, routed → F.W1), re-affirmed at fr-App and fr-BasisSelector. REGISTRY-FIRST: folds by reference; booking it here would double-count F.W1's budget. Provenance corrections carried (the `light-dark(oklch(),oklch())` light-arm form; the pre-bump hex history).
13. **C-M7 / D-B3(a) touch-floor framing** — KILLED: desktop floor is 0px (nothing to defeat); coarse box would be 60px, not 44px (re-verified). The SC 2.5.8 failure stands; cite the SC, not the token.
14. **C/D substrate baseline "producer 7.0.0"** — KILLED as a stale name: producer HEAD is **8.0.0** (re-verified). The forward export-map claim survives at 8.0.0; every "7.0.0" label and D-I1's source-based prediction must be re-derived at the tag F.W1 actually adopts.
15. **Provenance drift** (C-M4 `:105-108`→`:124-129`; D-M7 `style.css` cites; L-2 modal cites; D-N9 `:21`→`:20`) — corrected; findings unaffected; recorded so no reader chases ghosts.
16. **D-M8 site census** (25 sites / 13 files) — corrected to 25 / **15** files; the in-tree comment is stale in the third direction; finding untouched.
17. **D-M7(a)** (focus squares the corners) — NOT ESTABLISHED; production bundle order probably resolves against it; demoted to UNPROVEN (SEAT CORRECTIONS 2).

## Superlatives (provenance preserved)

1. **Zero teardown surface, affirmatively proven** — no listener/timer/observer/rAF/lifecycle in the file; the composable is pure (re-verified: two reactive Sets + closures). Contrast the sibling's scroll-lock restore obligation. (L S-1.)
2. **The W3.5.c de-duplication comment survives its own grep** — `userExpanded|userCollapsed` = 1 hit repo-wide, the comment itself; the state lives once, in the producer. (L S-2.)
3. **L-7 is the best-evidenced row in the fourier corpus** — a build-artifact claim that reproduced node-for-node under a hostile from-scratch re-derivation (98 nodes, exactly one empty preview).
4. **Stable domain keys on all three native loops** — `:key` on ids, never the index, with `si` in scope; R5-7's blind spot is a counting problem, not a correctness problem. (L S-4.)
5. **The viewport-height plumbing is correct on a genuinely hard problem** — scroller-derived `--paper-scroll-viewport-height` with a `100dvh` fallback; the classic nested-scroller bug is absent. (D S-2.)
6. **`overscroll-behavior-y/x: contain` + `scrollbar-gutter: stable`** — both the fastidious choice, neither common. (D S-3.)
7. **Token-pure styling with named-property transitions** — zero literals, zero `transition: all`; the discipline is what made D-M6 findable, wave id attached. (L S-3 + D S-4.)
8. **Correct semantics where they are cheapest to get wrong** — `<aside>`→`<nav aria-label>`→nested `<ol>`; `data-toc-id` as a presentation-free hook shared by e2e and the producer. The missing ARIA is the widget-pattern kind; the free kind is all present. (D S-5/S-6.)
9. **The falsifier discipline bit on both sides** — the axes killed five of their own hypotheses each; the re-readers killed seventeen more and re-verified the axes' kills; this seat re-verified the re-readers'. Where the corpus fails it fails on BASELINE and EMISSION, never on the component's own bytes.
10. **D's contrast arithmetic is fully reproducible** — now triple-derived (axis → re-reader → this seat), every shared cell matching to the digit.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. Two teleported `.search-modal-overlay` plates visibly stacked <1024px (L-2/C-B3). Recipe: 375px viewport → scroll inline ToC out → floating search → type → expand → `document.querySelectorAll('.search-modal-overlay').length`.
2. Measured horizontal overflow of `.sidebar-nav` at 1024–1280px (C-M1(b)/D-M2) — cascade settled; only `scrollWidth > clientWidth` and the amount need a live read.
3. D-M7(a) production-order tie on `border-radius` at `:focus-visible` — prediction: route-chunk sheet wins, radius holds, claim fails.
4. C-B1's engine half (`getPropertyValue('--viz-fourier')` returns token text) — folds into banked fr-BasisCanvas D-1 either way.
5. Perceived motion of the ungated damped follow under PRM (L-11) — the missing gate is static-certain.
6. Whether a real `vite build` completes at the pin (M1's soft half) — in-memory replication compiles clean at 366,937 bytes; strict loaders hard-error. F.W1 pre-gate: `npm run build` + `grep -c animate-collapsible` → predicted GREEN build / 0.
7. Dark-arm contrast of ramp stops 0–4, 6–12 (rests on the axis's own numbers).
8. Rendered row height on a coarse-pointer device ≥1024px (60px arithmetic certain; device matrix not settled statically).

## Identity guards (anti-rename, fold-by-reference)

- **C-B1** → banked **fr-BasisCanvas D-1/BC-1/C-1** (+ fr-App B-1/L-1/C-1, fr-BasisSelector C-2/L-B1). ONE identity, already BLOCKER-routed → F.W1. Never re-booked here.
- **M1** → **decides** banked fr-CollapsibleSection **M-7** (emission hinge, negative) and **corrects** banked **K-5** (the `@source` backstop is shipped-but-inert, not absent, not functional). One identity across three records; the correction is booked here, the defect stays banked at its original id.
- **R5-7 / R6-5 / R3-7a / R6-8** → intake `lane-fourier-r3-r6.md` rows, folded by reference; this record contributes the exact denominator (13/51/34 = 98) and the +1-tooltip budget note to F.W3.
- **tsconfig no-unused-flags** → banked at fr-BasisSelector; referenced by M7, not re-booked.
- **L-1's e2e workaround** and **C-M4's broken guard** are two defects on one test block — one restructure (CollapsibleTrigger) discharges both; the specs must not book them twice.

## Closing verdict

**ADJUDICATED: DEFECTIVE-ON-A-CORRUPT-SUBSTRATE.** The component's own 283 lines are careful and mediocre — zero teardown surface, correct semantics, token-pure styling, and a click contract, cascade posture, and a11y surface that are wrong in ways every gate in the repo is blind to (M7). The corpus's decisive fact is not in the component at all: the installed producer's cascade sheet is syntactically corrupt (M1, this seat's own postcss derivation), which single-handedly explains the dead-animation blocker, kills three findings, corrects two banked rows, and invalidates two of the corpus's prescribed repairs. 40 rows adjudicated (1 BLOCKER · 20 MAJOR · 11 MINOR · 8 INFO), 17 kills upheld (one timing-corrected), 10 superlatives, 8 SS-13 residues. Routes: F.W0 ×3 · F.W1 ×5 (incl. the OWNER-RULING escalation and the M3 pre-gate law) · F.W3 ×1 · F.W4 the bulk · F.W9/W10 ×3 · producer relays ×2 (glass-ui BH: M1+L-10/D-M4; latex-paper: L-11+M2). Nothing here stamps a wave — every disposition is agglomeration input for the forming X·F specs (M-25).

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
