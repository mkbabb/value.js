# wb-mix-resultdisplay — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **HEAD**: `4ddf11d1` · branch `tranche-u` · **Corpus**: `demo/workbenches/mix/MixResultDisplay.vue` (159 lines) plus the contract surfaces it rides (MixPane.vue · composables/useMixingState.ts · MixSourceSelector.vue · demo/styles/animations.css · demo/styles/utils.css `.swatch-row` · installed glass-ui 7.0.0 dock/watercolor-dot/useClipboard). Challenge axes on disk, r6 governing: **challenge-C-implementation.md** · **challenge-D-design.md** · **challenge-L-library.md** — all three **EXISTS-ORIGINAL** (HYDRATION-LEDGER.md:249-251, my read; no UNWITNESSED-DIRECT axis in this corpus, unlike the sibling seat's L).
**Re-readers adjudicated**: TWO hostile returns (design-and-usage = "reader-DU": 20 confirmed · 6 killed · 4 missed; library-and-code = "reader-LC": 19 confirmed · 8 killed · 5 missed). Both were attacked before adoption; every verdict-level disagreement re-derived against the tree with this seat's own instruments; all nine reader misses re-walked in source and **all nine admitted** (two as limbs, one as the governing mechanism over a corpus row); one corpus consequence-limb **corrected by this seat** (the stale-gradient retention arm — a third instance of the injection-apparatus lesson).
**Anchor stability**: `git diff --stat c654824e 4ddf11d1 -- demo/workbenches/mix/ demo/styles/ demo/shell/usePaneRouter.ts demo/palettes/browser/card/ e2e/smoke/views/mix.spec.ts e2e/smoke/safari/mix-flow.spec.ts eslint.config.js tsconfig.demo.json` → **EMPTY** (my run). Every line anchor in the corpus and in both reader returns re-resolves unchanged at HEAD.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling per law)**: whole reads of MixResultDisplay.vue, useMixingState.ts, MixPane.vue; animations.css:50-139 (the three-family law + both morph legs); MixSourceSelector.vue:1-10/:115-125/:180-190/:244-270; PaletteColorStrip.vue head (root `:class` = `'flex h-10 w-full'`, props `{colors, orientation?, weights?}` — the K-9 decider); foundation.css:315-332 (the `--well-bg` D1 block naming this plate a consumer), :670-696 + :828-843 (both color-surface roster arms), :76 (`@import "./animations.css"` bare); layer census: **zero** `@layer` in animations.css, foundation.css:590 the sole demo `@layer` (utils.css:127 is a comment) — so the corpus's specificity arithmetic is the correct and only mechanism; usePaneRouter.ts:213-226 (Clear/Mix/Copy rows); `grep -rn '\.focus()' demo/workbenches/mix/` → **0**; `grep -rn 'aria-live|role="status"' demo/workbenches/mix/` → **0**; MixPane `computed` census → 1 import, 0 calls; glass-ui import census at HEAD: root-barrel files = **36**, subpath files = **54** (my own grep — confirms reader-LC's K7 against L's 37/82); demo/shared/utils.ts:8-18 read whole (doctrine is about bare `@mkbabb/value.js`, not glass-ui); canon verbatim: OPTICAL-BENCH-COMPOSITIONS.md:43 (decided sequence "source mode/rack; **result**; method/provenance/commit"; "no shadow Card/local grid"; close 2/3/**12**), :77 (Mix boundaries `[]` · retained line `none` · "distinct **through geometry**"), :108 (P051), VISUAL-CONSTITUTION.md:48 ("operand rack, result, controls"; `InstrumentChassis`); motion-quarantine grep for mix rows → **0**; arithmetic re-derived by this seat: the wrap threshold (6×40+5×8 = 280 ≤ 292 < 328 = 7×40+6×8), the triple cascade collision (scoped `(0,2,0)` × unlayered family `(0,1,0)` on `transition` AND `opacity`, both directions), and the same-tick ghost mount (useMixingState.ts:79-101 assigns `mixResult` and `animationPhase="mixing"` in one synchronous call → MixPane.vue:111-119's enter always lands on a `.mix-plate--ghost` root). Producer-dist facts (inheritAttrs:!1, six props, touch-floor selector, useClipboard failure-arm-no-timer, separator no-fallback) adopted from the readers' byte-level probes + the fifth/sixth independent verifications banked at wb-mix-animationcanvas.md — not re-probed a seventh time.

## RULING PINS (this seat's own derivations)

1. **The plate's arrival is motion-dead on the ONLY path the shipped app can take — reader-DU's MISS-1 is admitted as the sharpest limb of a three-limb collision inside one nine-line `<style scoped>` block.** (a) `.mix-plate[data-v-…] { transition: opacity … }` (:152-154, `(0,2,0)`) replaces the whole `.vj-morph-enter-active` shorthand (`(0,1,0)`, animations.css:104-110) — transform, max-height and the snappy spring deleted (R6-1, confirmed). (b) The same inversion truncates `.vj-morph-leave-active` (animations.css:111-117) — every exit snaps geometrically too (reader-LC M2, admitted). (c) `.mix-plate--ghost[data-v-…] { opacity: 0.55 }` (:155-157, `(0,2,0)`) outranks `.vj-morph-enter-from` (`(0,1,0)`, :118-123) — and because `startMix()` sets result and phase in ONE synchronous call, the plate ALWAYS mounts ghosted, so on every shipped enter the opacity is pinned at 0.55 from frame one: no fade at all, plus a snapped 0.97-scale transform from limb (a). All unlayered (my census), so specificity is the sole mechanism. C's pasted enter capture (`opacity:"0"`, no `--ghost` class) is from a non-ghost mount its own `setupState` apparatus manufactured — the shipped app cannot produce that frame (reader-DU's kill, upheld as K-5). L:504-506 certified the two scoped rules "legitimately scoped… Nothing deleted" — the exact area its axis's largest miss lives in.
2. **`vj-enter` is structurally dead for a reason independent of `:key`, so R6-3 is RESCOPED and its cure is insufficient — reader-LC's M1 governs.** The swatch `<TransitionGroup>` (:92-96) sits in the `v-else` branch of an inner `mode="out-in"` `<Transition>` (:60) keyed `well`/`content`; every re-mix flips `ghost` (both writes in `startMix()`'s single synchronous frame), so the content subtree unmounts and fresh-mounts with the new result — and the group carries **no `appear`**, so Vue runs no enter on initial render. Patch-in-place (`sameDomNodeReused: true`) is reachable only through C's injection apparatus, which mutates `mixResult` while `content` stays mounted — an arm the phase machine forbids. The index `:key` survives as a latent defect (it goes live the day any path replaces a result without a ghost window); the cure is `appear` + identity keys, and the identity expression **must preserve the index-0 `mix-result` seed** (superlative 1).
3. **Severity rulings.** DockSeparator (:135) = **MINOR against reader-LC's MAJOR**: the box paints 0×1 px, so the canon retained-line row is morally satisfied by accident; what survives is a phantom `role="separator"` in the AT tree + 12 px of unexplained rhythm — real, low-blast. Eyebrow (:58) = **MINOR against reader-LC's MAJOR**: Fraunces-italic-bold-uppercase is visible drift with a one-class cure (`.section-label`, which routes through `text-mono-caption` and kills the italic for free). D-46 = **MINOR as reader-DU graded (axis MAJOR overruled)**: OBC:77's own preamble classes a specimen-well edge as housing-not-divider; the defect is the geometry job never done, not a missing boundary — and the axis's cure is killed outright (K-8). D-47 = **MAJOR as filed**, with the axis's "wraps at 8" corrected to **7** by this seat's arithmetic (280 ≤ 292 < 328) — the decoupling begins one colour earlier than filed, which strengthens the row.
4. **D-45 is CONFIRMED MAJOR and routed X-W6.j — distinguished from, not folded into, the sibling record's MX-28 INFO ruling.** MX-28 ruled "built against the superseded composition" INFO because the recomposition is X-W6.j's programmed work. D-45 is narrower and survives that logic: the decided sequence (OBC:43 + VC:48, both re-read verbatim) is violated by source order alone (MixPane.vue:80 rack → :97 method/commit → :112 result), the in-use consequence is stated (the answer renders below the verb that produced it; re-tuning requires scrolling back up past the answer), and the cure is a one-line child move separable from the full chassis adoption. The `<Card tier="resting">` limb (MixPane.vue:62 vs OBC:43/:77) FOLDS to MX-28's identity and stays INFO.
5. **One corpus consequence corrected by this seat — the stale-gradient arm is apparatus-only.** C measured an empty re-mix retaining the PREVIOUS gradient (CSSOM rejecting the invalid assignment). On the shipped path PIN 2's fresh-mount applies: a new element has no prior inline background, so the shipped consequence of `linear-gradient(to right, )` / `(…, red)` is a **blank 16 px bar**, not a stale one — plus the zero-dot rack and the `"" → "Copied!"` copy. The defect (truthiness-for-length over an optional bag) stands MAJOR either way, and the N=1 arm is fully reachable shipped: Save a single-colour mix twice ("Mixed Color" creates 1-colour palettes, MixPane.vue:41-43), select both, Mix → a 1-colour palette result → invalid gradient. Third instance of the injection-apparatus lesson (after K-5 and PIN 2): **every runtime JSON in challenge-C was taken through `setupState` injection and must be re-taken through `startMix()` before it is trusted as a shipped frame.**
6. **The fold discipline held**: MX-1, MX-3, MX-4, MX-8, MX-32, MX-35, MX-20≡L-14, L-5(a), PC-7/PCS-2, PCS-8, PCS-10/R-F, GAB-12, GAB-17, XP-8, PS-29≡GenerateControls-23, AdminPane-L-5/SwatchHoverMenu-K-5 — sixteen banked identities touched by this corpus, all folded by reference, **zero re-booked**. Both readers practiced this correctly; the roster below marks every fold.
7. **The round's blind-spot symmetry is itself a finding about the fleet.** Reader-DU found the ghost-pins-enter limb (MISS-1) and missed the leave side; reader-LC found the leave side (M2) and missed the ghost limb — the same nine lines of CSS. Two hostile readers each stopped at the first collision that satisfied its thesis; the sibling record logged the identical pattern (its DISSENT 2). Recorded so the next seat audits BOTH legs of any family it finds truncated.

## Scoreboard

**33 adjudicated rows: 2 BLOCKER (both folds) · 10 MAJOR · 17 MINOR · 4 INFO — 12 folds to banked identities, 21 rows/limbs live at this docket.** 13 killed claims with written refutations; **all 9 reader misses admitted** (M1 promoted to governing mechanism; MISS-1/M2 merged as limbs of one row); 2 corpus errata corrected by this seat (wrap-at-7; the stale-gradient arm). Headline: **the announced destination of the mix convergence is motion-dead in every direction on the only reachable arm — its anchor cannot render (banked MX-1), its arrival is cascade-pinned at 0.55 opacity with a snapped transform, its per-swatch entrance never fires for want of `appear`, its exit snaps, and the region itself renders on the wrong side of the verb that produces it — while no tranche-X wave owns this file beyond a face carve.**

## Ruled disagreements (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **R6-3 vs M1** (why `vj-enter` never fires) | DU: R6-3 confirmed as filed (index keys); LC: M1 — group fresh-mounts, no `appear`; keys latent | **M1 GOVERNS; R6-3 RESCOPED latent** | PIN 2 — same-tick `startMix` writes + `mode="out-in"` keyed branches make patch-in-place unreachable shipped; C's `sameDomNodeReused` is apparatus-arm. Cure = `appear` + identity keys preserving the index-0 seed. |
| **plate enter** (what actually paints) | DU MISS-1: pops at 0.55, no fade; LC: silent on the ghost limb (found M2 instead); C r6: measured `opacity:"0"` fading | **MISS-1 + M2 ADMITTED, C's frame KILLED (K-5)** | PIN 1 — triple collision derived statically; C's capture lacks `--ghost`, an arm the synchronous phase write forbids. |
| **separator severity** | DU: MINOR; LC: MAJOR | **MINOR** | PIN 3 — paints nothing; phantom `role="separator"` + 12 px gap survive; producer no-fallback/JS-defensive asymmetry rides the BH relay as INFORMATION (P051 posture — never a restore ask). |
| **eyebrow severity** | DU: MINOR ×2 rows; LC: MAJOR merged | **MINOR, merged** | PIN 3 — one-class cure; sibling already on `.section-label` (MixSourceSelector.vue:183, my read; third variant at :119). |
| **D-46 grade + cure** | axis: MAJOR + retire-the-well/≥3:1; DU: MINOR, cure killed; LC: ratio unproven | **MINOR; cure KILLED (K-8)** | foundation.css:321-328 names this plate a `--well-bg` consumer in the D1 consolidation ("the six parallel hand-mints D1 retires") and fixes the bracket at 8 % in [6, 10] — the axis's ≥3:1 prescribes ~5× the ratified step and re-opens the parallel-mint pathology. Geometry cure rides D-45/D-29's region ownership. |
| **D-45 vs banked MX-28** | both readers: MAJOR; sibling precedent: conformance class = INFO | **MAJOR here, Card limb folds to MX-28** | PIN 4 — separable one-line cure + stated in-use consequence distinguish the sequence inversion from programmed-adoption conformance. |
| **28 px mechanism** | L N-1: producer dock-scoping; C R6-4: consumer `compact` opt-out | **C GOVERNS; L's mechanism KILLED (K-2)** | touch-floor.css's `:not(:where(.glass-dock *))` is authored FOR out-of-dock controls; `:not(.dock-icon-button--compact)` is what excludes these three — the component's own prop at :122/:129/:137. Changes the cure from producer rider to `--dock-compact-control-size` at the plate root (ActionBarToggle.vue precedent) or dropping `compact`. |
| **empty-palette consequence** | C: stale gradient retained; LC: "structurally certain" | **CORRECTED — blank bar shipped; stale arm apparatus-only** | PIN 5. |
| **strip duplication cure** | L N-2: "one import" of PaletteColorStrip; DU: registers not interchangeable; LC: cure killed | **duplication FOLDS to PCS-10/R-F; cure KILLED (K-9)** | My read of PaletteColorStrip.vue: root hard-codes `'flex h-10 w-full'`, no size/radius/gradient register; a fall-through `h-4` merges at equal Tailwind specificity (source-order roulette the sibling file documents at :134-145). PC-7 coupling mandatory (K-9 rider). |

## Adjudicated defect roster

Line anchors are HEAD `4ddf11d1` (tree byte-identical to the corpus base `c654824e`); re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend per the sibling record (bounds re-read there at `9c779130`, tree unchanged since): **MX-CLUSTER** = NO-WAVE-OWNER booking (mixStage.ts · useMixingState.ts · **MixResultDisplay.vue beyond the X.W4.g import+face carve** · ErrorBoundary.vue · demo/palettes/mix.ts · the two RED mix specs); **X-W6.j** owns MixPane/MixConfigBar/MixSourceSelector/the canvas pair; **X-W1** CI e2e + witness matrices; **X-W7/.c** strip consolidation + export unit; **X.W4.d** shell dispatch deaths; **X-W10** register canon.

### BLOCKER (both folds — grade lives at the banked home)

- **MR-1 — FOLD → MX-1 (wb-mix-animationcanvas.md:43): `data-mix-target` (:69) is stamped on a WatercolorDot and dropped (`inheritAttrs:!1`, class/style only, six declared props); the convergence anchor cannot exist in the DOM; `mixStage.ts:121-124`'s fabricated target runs on every mix.** This corpus adds the sixth/seventh independent verifications (C's in-page `querySelector` patch proving the lookup executed and returned null — the tightest form yet; L's live attribute census) and one micro-fact of this seat's: the `:72 aria-hidden="true"` is ALSO dropped, harmless only because the producer hardcodes it. Cure road banked (consumer-owned anchor OUTSIDE the out-in Transition; delete the fallback; NEVER a producer-forwarding ask — P051/K-3). → **MX-CLUSTER · face/seat X.W4.g · canvas-side X-W6.j**.
- **MR-2 — FOLD → MX-3 (:46): both e2e specs structurally unsatisfiable (role-query a name the producer drops, then assert `[data-mix-target]` visible); NO CI job runs playwright; zero unit tests import the SFC (test/mix-v4.test.ts is math-only).** C's pasted RED run is the round's live witness; the "delete the whole `<template>`, vitest stays green" mutation restated. → **MX-CLUSTER · CI gating X-W1**.

### MAJOR

- **MR-3 — R6-1 + LC-M2 + DU-MISS-1, ONE row (PIN 1): the nine-line scoped style block defeats the `vj-morph` family in all three directions — enter list truncated to opacity-only with the spring curve swapped, leave list truncated identically so every exit snaps, and the ghost's `(0,2,0)` opacity pin deletes the enter fade outright on every shipped mount.** Violates the family law stated in prose at animations.css:60-65 ("the family owns the CURVE + TOKEN pairing; a consuming site may parameterise only GEOMETRY"). Cure sharpened per DU: delete :152-157 and express the ghost step through `--vj-morph-*` geometry (or an opacity **longhand**, which merges instead of replacing) — a cure designed from C's capture alone would aim at a frame the app cannot produce (K-5). → **MX-CLUSTER** (the style block is beyond the face carve).
- **MR-4 — LC-M1 governing + R6-3 rescoped (PIN 2): the `vj-enter` TransitionGroup is structurally dead shipped — fresh-mounted on every settle with no `appear`; the index `:key` (:99) is a latent second kill.** The `.swatch-row` recipe (utils.css:167-179) exists precisely to animate this row and names this file a consumer; it has never run here. Cure: `appear` + identity keys preserving the `i === 0 ? 'mix-result' : …` seed (:104). → **MX-CLUSTER**.
- **MR-5 — R6-2: Reset unmounts the control that was just activated; focus falls to `<body>` (WCAG 2.4.3).** Chain fully static: :136-142 → MixPane.vue:117 → useMixingState.ts:108-111 → `v-if` unmount at MixPane.vue:111-119; `grep '\.focus()' demo/workbenches/mix/` = **0** (my census) — no restoration exists anywhere on the path. Cure in the parent (owns both the unmount and the successor: focus the Mix verb). → **MX-CLUSTER + X-W6.j** (handler side).
- **MR-6 — R6-5 ≡ LC-N-3 ≡ D-9/D-35: `useClipboard` half-consumed — `CopyResult` discarded (:46), no `onCopyError`, `"failure"` has NO reset timer (byte-level: the failure arm sets no `setTimeout`) so a failed copy is permanently pixel-identical to idle, and `invalidate()` is never called with no watcher on `result`, so a stale "Copied!" outlives its payload.** Four declared states projected onto one boolean (:31-32). The failure arm is live on the LAN-http origin `server.host: true` exists to serve. Adjacent banked rows cover the discard family elsewhere (AT-1, GAB-18, GenerateControls row 15); the failure-never-resets + invalidate limbs at THIS file were unbanked — booked here. → **MX-CLUSTER**; coupled to MR-13's one-seat consolidation.
- **MR-7 — D-8/16 ≡ F-5/F-C (consequence corrected, PIN 5): `MixResult` is an optional bag (useMixingState.ts:32-36), so `:91` tests truthiness where length was meant — `[]` renders a zero-dot rack, an INVALID `linear-gradient(to right, )` (blank bar shipped; C's stale-retention arm apparatus-only), and `:44-45`'s `?? ""` copies the empty string while `:32` flips to "Copied!".** The N=1 arm is reachable shipped via two saved "Mixed Color" palettes (MixPane.vue:41-43). Root cure: a true discriminated union — five masking guards in this file (:37/:39/:44/:45/:78/:91) delete themselves. → **MX-CLUSTER** (the type lives in useMixingState.ts).
- **MR-8 — D-45 (PIN 4): the result region renders AFTER the method/commit region, inverting the decided sequence in both canon documents (OBC:43; VC:48 — both re-read verbatim this session); source-order certain from MixPane.vue:80/:97/:112.** In-use consequence stated by the axis and adopted: the answer lands below the verb; re-tuning scrolls back past the answer. The 126.2 px magnitude is live residue (R-3). Card limb folds to MX-28 (INFO). → **X-W6.j** (MixPane is in its bounds; pairs with MR-23 so the region becomes permanent AND correctly placed in one edit).
- **MR-9 — D-47 (erratum corrected): at the canon's own named count of 12 the rack wraps to two rows (at SEVEN faces per 292 px row, my arithmetic — not the axis's eight) while the strip stays one row by construction, so the two depictions of one ordinal decouple before any overflow.** Canon: VC §7 Mix legibility at 2/3/12; OBC:43 close criterion names 12. Cure = one owner for ordinal depiction (rides the PCS-10 consolidation, MR-17); if the result becomes one ordered strip, MR-27's ghost can announce it honestly. → **MX-CLUSTER, coupled to X-W7.c**.
- **MR-10 — FOLD → MX-32 (:57): the palette result — the only arm a user can reach — has NO text alternative; its entire visible+accessible text is the word "Result"; `:title` (:103) dies on the same producer mechanism; the strip is explicitly `aria-hidden`.** :54 is the element that must carry the cure. → **MX-CLUSTER / X.W4.g**.
- **MR-11 — FOLD → MX-8 (:53): the settle of a ≥900 ms async operation is announced to nobody — zero `aria-live`/`role="status"` in the mix tree (my census re-run); MixPane.vue:107-110's "the animation IS the progress" leaves no non-visual channel.** → **X-W6.j**.
- **MR-12 — FOLD → PCS-8 ≡ CurrentPaletteEditor L-10 ≡ PalettesPane PP-8: `demo/palettes/export/` (the certified byte-exact set) has one importer and it is a test; the app runs legacy `export.ts`.** In this component's blast radius through Save→createPalette→export. → **X-W7 export unit** (banked).

### MINOR

- **MR-13 — FOLD → GAB-17 (shell-dock-genericactionbar.md:32): two implementations of the result-copy command with divergent feedback postures; the dock's "Copy result" (usePaneRouter.ts:222, my read) routes to MixPane's silent `writeClipboard` path (:49-55) while the plate owns the honest seat.** GAB-17's route stands: the shell dispatch dies at **X.W4.d**; the plate's seat remains sole owner — then MR-6's full-contract consumption lands on it.
- **MR-14 — R6-4 ≡ D-12 (mechanism = C's; L's killed at K-2): the three actions are 28×28 on coarse and fine because `compact` IS the producer's own exclusion from its out-of-dock touch floor — an opt-out, not a clamp failure.** 28 ≥ WCAG 2.5.8's 24; below the system's own 44. Cure idiom real and in-tree: `--dock-compact-control-padding` at a root, ActionBarToggle.vue:155-157. Also recorded: capture.mjs's `< 24` threshold is why REPORT.md is silent — a threshold artifact, not a clean bill. → **MX-CLUSTER**.
- **MR-15 — D-11 ≡ N-1 separator limb (severity ruled, PIN 3): `<DockSeparator />` (:135) renders 1×0 px (unset `--dock-separator-height` → invalid at computed-value time → `height: auto` → 0) yet stamps `role="separator"` — a phantom in the AT tree plus 12 px of stray margin, producing the 4 px/21 px action-rhythm asymmetry the axis measured.** Cure: delete it (canon forbids a line here — OBC:77 retained line `none` — so swapping to the real `./separator` subpath export would mint a NEW defect; reader-DU's caveat adopted). Producer asymmetry (defensive JS, non-defensive CSS, no fallback) → BH relay, informational only. → **MX-CLUSTER**.
- **MR-16 — D-6 + L-F-16 merged (severity ruled, PIN 3): the "Result" eyebrow renders Fraunces ITALIC bold uppercase — `text-caption` carries `font-style: italic` at the installed pin and :58 overrode family and weight but never style — and the five-utility recipe re-mints `.section-label`, which the sibling already uses (:183) against a third variant (:119).** One-class cure kills both limbs. → **MX-CLUSTER**.
- **MR-17 — FOLD → PCS-10 ≡ R-F (grade MAJOR at home; cure killed here, K-9): the hand-rolled gradient strip (:109-116) is the over-threshold arm of the five-parallel "render a palette compactly" family; the sibling file imports `PaletteColorStrip` seven lines away in the module graph (MixSourceSelector.vue:8, my read).** The registers are NOT interchangeable (hard segments vs continuous ramp; hard-coded `h-10`); consolidation must land as ONE decision with PC-7's roster cure (MR-18). → **X-W7.c**.
- **MR-18 — FOLD → PC-7/PCS-2 (BLOCKER at home; member surface added, not re-graded): the strip is a color-display surface absent from BOTH roster arms (forced-colors :679-694, print :830-841 — my reads) — it is a SIBLING of `.swatch-row`, so `.swatch-row > *` does not reach it, while every other color surface in this plate is covered (the dots via `.watercolor-swatch` AND `.swatch-row > *`).** WHCM and print treat the result's only palette-wide depiction as chrome. → **X-W7.c**, coupled per K-9's rider.
- **MR-19 — D-41 (new limb; naming class folds → GAB-12/XP-8): `title="Copy color"` is unconditional and WRONG across the entire palette half of the state space (:123 never branches on `result.type` while :44-45 joins N colours); `title` is also the sole accessible name on all three controls (banked class, XP-8 counts these ×3).** → naming register **X-W10/X-W4** (banked routes); the wrong-label limb **MX-CLUSTER**.
- **MR-20 — D-7(rtl): the rack is writing-mode aware (`flex flex-wrap`, :95) and the strip hard-codes the physical `to right` (:112), so under RTL the two orderings contradict.** Cure `to inline-end`/inline-direction interpolation — or MR-17's consolidation makes it moot. → **MX-CLUSTER / X-W7.c**.
- **MR-21 — D-17: the value artifact (:85-87) wears `break-all` (breaks mid-token inside `oklab(63.37…%)`) and no LTR isolation for a machine string in a possibly-RTL paragraph.** Cure `break-words`/`overflow-wrap:anywhere` + `dir="ltr"`. → **MX-CLUSTER**.
- **MR-22 — D-46 (graded down, cure killed — PIN 3/K-8): the plate declares its region solely through the `bg-well` material step (:55) — an 8 % tone step BY DESIGN (foundation.css:328) that the axis measured at ~1.076:1 in dark — while OBC:77 assigns Mix's grouping to GEOMETRY.** The failure is the geometry job never done; the well is sanctioned housing. Cure rides MR-8/MR-23's region ownership; the ratio is live residue (R-1). → **MX-CLUSTER + X-W6.j**.
- **MR-23 — D-29: no pre-result state — the region simply does not exist until the first mix (`v-if`, MixPane.vue:113), leaving the pane's lower third unowned void, against VC:48's permanent "result/provenance inspector" (absent OPERANDS occupy no filler; absent RESULT is not the same statement).** Same edit as MR-8. → **X-W6.j**.
- **MR-24 — D-10/R4-2: Save emits into a void — no pending/success/failure register, no de-duplication; five clicks make five "Mixed Palette" rows, and (with MR-7) an empty one is representable.** The in-file precedent for the cure sits one line above at :31. → **X-W6.j** (MixPane.onSave) **+ MX-CLUSTER** (plate-side register).
- **MR-25 — LC-M3: the masking fallback's `r: 28` encodes the 56 px COLOUR-branch well (`w-14` ⇒ r 28) while the only reachable arm (palettes, banked MX-3) renders `w-10` ⇒ r 20 — the fabricated target is branch-blind and 40 % wrong for every real mix, feeding `tr: Math.max(target.r, 14)`.** A silent duplicate of a Tailwind class in a different module; strengthens the banked delete-the-fallback PIN. → **MX-CLUSTER rider** (mixStage.ts).
- **MR-26 — DU-MISS-3: verb-scope inversion — the plate's "Reset" (:138) discards only the result (`reset()`, useMixingState.ts:108-111; operands survive), while the command that actually clears is named "Clear" and exists only on the dock (usePaneRouter.ts:220 → `clearSelection()`, :113-117 — my reads).** The loud in-plate control is a no-op on operands; worsens banked MX-19's recovery story (the plate now offers a control that LOOKS like the unadvertised recovery affordance and is not). Cure subtraction-first: one honest verb, correctly scoped, in one place. → **MX-CLUSTER + X.W4.d coupling**.
- **MR-27 — DU-MISS-4: for the palettes arm the announced destination under-declares the answer by a factor of N — the ghost is ONE 40 px well (:63-74, coloured `colors[0]`), the result is N faces plus a strip; the docblock's silhouette-continuity promise (:12-18) is true for face 0 and silent about faces 1..N-1 (seeds `:104` never announced).** Distinct from banked MX-37 (anchor-contract prose) — this is the silhouette-continuity contract, and it survives MX-1/MX-2's cure. Cure rides MR-9's: a single ordered strip lets the ghost announce that strip honestly. → **MX-CLUSTER**.
- **MR-28 — FOLD → ColorSpaceSelector L-5(a) + MX-35: no import-boundary rule reaches this file (`no-restricted-imports: undefined`, reader-run; dead `demo/@` globs).** L's "100 %" quantifier killed (K-10) — one live glob object exists (`demo/color-picker/**`) — AND MX-35's sharper limb stands: its only pattern keys a dead alias shape, so enforcement is still effectively zero. Neither over-claim survives; the identity is banked. → **NO-WAVE-OWNER (banked)**.
- **MR-29 — FOLD → MX-20 ≡ ColorSpaceSelector L-14 (W-HYGIENE, another owner): tsconfig.demo.json#paths vs package.json#exports drift, in this component's transitive closure.** Reader-LC's five-key sharpening (3 phantom specifiers + 2 omissions vs L's one) adopted as the identity's note. → **banked NO-WAVE-OWNER**.

### INFO

- **MR-30 — R5-6/L-F-9′ + LC-M4: the `TransitionGroup` named import is dead (compiler resolves built-ins before setup bindings — LC's `compileTemplate` run; the in-file asymmetry with the un-imported `<Transition>` at :60 is the tell) — at TWO sites: MixResultDisplay.vue:4 AND MixSourceSelector.vue:2 (my read).** A cure following the ledger as filed would leave the twin. → **MX-CLUSTER rider + X-W6.j** (sibling file).
- **MR-31 — LC-M5: MixPane.vue:2 imports `computed` and never calls it (my census: 1 hit = the import).** Lint-green and typecheck-green through the same holes MR-28 files; previously sighted only in a banked record's METHOD section, never filed. → **X-W6.j rider**.
- **MR-32 — FOLD → PS-29 ≡ GenerateControls row 23 (banked, DEMOTED): `useClipboard` imported from the root barrel (:5) while the same file rides two subpaths.** Reader-LC's corrected census confirmed by this seat's own grep at HEAD: **36 root / 54 subpath** files (L's 37/82 dead — K-11); the `demo/shared/utils.ts` "doctrine" is about a different package (my read). → **banked fold**.
- **MR-33 — FOLD → AdminPane L-5 (W-HYGIENE demo/ui extirpation); count killed at SwatchHoverMenu K-5 (17 one-line bare-root, not 19): MixPane imports `Card` through `demo/ui/card` while this file imports glass-ui directly — two conventions in one folder.** → **banked fold**.

## Killed-claims register

- **K-1 — L §5.2 conclusion "`canMix` can never become true … the component has never rendered in the shipped app" — KILLED (both readers; premise re-read by this seat).** The palettes arm is a NATIVE `<button type="button" :aria-pressed :aria-label @click>` carrying real `data-mix-*` stamps (MixSourceSelector.vue:246-268, my read); `canMix`'s palettes arm needs only `selectedPalettes.length >= 2` (useMixingState.ts:50-53). Identity of the kill banked (wb-mix-animationcanvas.md K-2). L also self-contradicts its own §3.1 (the dock Copy route through this component's payload).
- **K-2 — L N-1's mechanism "the 44 px coarse floor … is behind `.glass-dock`" — KILLED (C's byte-read governs).** touch-floor.css's floor is authored FOR non-dock hosts (`:not(:where(.glass-dock *))`); the exclusion is the consumer's own `compact`. The 28×28 fact and the separator limb survive (MR-14/MR-15); L's severity ("17 px short" MAJOR) and cure direction die with the mechanism.
- **K-3 — L F-1's producer cure ("`as`/`asChild` + drop `inheritAttrs:false`", BH relay) — KILLED by ratified canon (P051, OBC:108 re-read verbatim this session) and the banked terminal ruling (wb-mix-animationcanvas.md PIN 2).** The attribute/interactivity drop is the decided outcome; the relay informs, never asks to restore.
- **K-4 — C's #1-ranked ledger row (R5-1, the `parseCssColor` throw "destroys the whole Mix pane … ranked BLOCKER #1 on THIS component's ledger") — KILLED as ranked (reader-LC; banked).** The throw class is real and banked as MX-4 with the consequence corrected to an ANNOUNCED, recoverable, grid-wide boundary (banked K-1); none of the four throwing sites is in MixResultDisplay.vue (zero parser imports, my whole-file read). Mis-homing the #1 row mis-homes the cure.
- **K-5 — C's R6-1 evidence frame (the pasted enter capture `opacity:"0"`, no `--ghost` class) — KILLED (reader-DU; my same-tick derivation).** The shipped plate ALWAYS mounts ghosted (`startMix` writes result + phase synchronously); the captured non-ghost mount is the seat's `setupState` apparatus. The truncation MECHANISM stands (MR-3); the numbers are from an arm the app cannot produce, and on the real path opacity is pinned at 0.55 (MISS-1), not fading from 0.
- **K-6 — D-47's "the rack wraps at 8 colours on this arm" — CORRECTED to 7 (this seat's arithmetic: 6×40+5×8 = 280 ≤ 292 < 328 = 7×40+6×8).** Correction only; the finding strengthens (MR-9).
- **K-7 — D-48's displacement-to-diameter mechanism — KILLED as self-refuting (reader-DU, adopted).** A constant 1.3 px displacement is a LARGER fraction of a 40 px box, so the axis's own ratio predicts the opposite of its conclusion. The candidate mechanism worth testing is turbulence periods per silhouette (`baseFrequency 0.05` is absolute: ~2 periods at 40 px vs ~2.8 at 56 px). The perceptual observation survives as residue (R-4).
- **K-8 — D-46's cure ("retire the well from this plate; any survivor needs a measured ≥3:1 boundary") — KILLED (reader-DU, verified against the token doc by this seat).** foundation.css:321-328 names this plate a consumer of the D1 `--well-bg` consolidation and ratifies the 8 % bracket in [6, 10]; a ≥3:1 step is ~5× the bracket and reads as a second Card, which OBC:43/:77 forbid (Mix Card count 0); the ≥3:1 framing also over-claims WCAG 1.4.11 for a decorative ground OBC's preamble classes as housing. Finding survives MINOR (MR-22); the cure must be geometry.
- **K-9 — L N-2's "the KISS cure is one import" — KILLED (reader-LC, verified by this seat's read of PaletteColorStrip.vue).** The component hard-codes `'flex h-10 w-full'` with no size/radius/gradient register; a drop-in swaps a 16 px rounded continuous ramp for a 40 px square hard-segment strip, and a fall-through `h-4` merges at equal utility-layer specificity (source-order roulette). L then proposes the register-prop move it called contrivance in r4. Duplication survives as MR-17's fold.
- **K-10 — L F-2's "100 % of the demo import-boundary rules glob `demo/@`" — KILLED (reader-LC; my config knowledge via the banked re-read).** One live glob object exists; MX-35's sharper limb (its only pattern keys a dead alias) is recorded beside the kill so the kill itself does not over-claim enforcement exists.
- **K-11 — L F-8's census (37 root/82 subpath) + the `demo/shared/utils.ts` "doctrine against exactly this" — KILLED (reader-LC; INDEPENDENTLY RE-MEASURED by this seat: 36/54 at HEAD; the doctrine text is about the bare `@mkbabb/value.js` specifier — my read of :8-18).** Finding class banked-and-demoted (PS-29); folds as MR-32.
- **K-12 — L F-10's "19 pass-through `demo/ui/*` barrels" — banked-KILLED (SwatchHoverMenu K-5: 17 one-line bare-root; alert 11-line; input via `/forms`).** `ls demo/ui | wc -l` counts directories, not pass-throughs. Identity folds as MR-33.
- **K-13 — R6-3's shipped-path mechanism ("same-length re-mix patches the existing nodes") — RESCOPED by LC-M1 (PIN 2).** Patch-in-place is unreachable through the phase machine; the capture that showed it rode the injection apparatus. The index-key defect survives LATENT inside MR-4; C's cure alone (identity keys) would leave the animation exactly as dead.

## Superlatives (provenance preserved)

1. **The seed contract is honoured end to end — and C's r6 read the decisive line backwards** (reader-LC, adopted; d.ts facts banked). `variant` and `seed` are DECLARED props, so they survive `inheritAttrs:!1`; ghost and landed dot share `color + seed` in BOTH branches (`wellColor` :36-40; seed `mix-result` at :68/:83/:104-index-0), so the silhouette the pigment poured into IS the silhouette face 0 wears. The `:104` special case C cited as proof that "position is the wrong identity" is the one line keeping the design promise — any MR-4 cure must preserve it.
2. **The ghost's geometry tracks the branch it announces**: `:71`'s ternary matches `:82` and `:102` exactly — three literals, zero drift (reader-DU). MR-27 is the cardinality axis of the same promise; the geometry axis is kept.
3. **This file owns no clock** (reader-LC): zero rAF/timeout/listener/observer; the only timer is the producer's, scope-owned and torn down by `useClipboard`'s own `onScopeDispose`. In a repo whose memory records a ~40-loop PRM-RAF epidemic, a 159-line animated result surface with no clock is the correct shape.
4. **The demo's only honest copy seat** (banked at wb-mix-animationcanvas.md:70; census 3 `useClipboard` vs 13 `writeClipboard` files banked at picker-pointerdebugoverlay.md:30): MR-6 is a criticism of the BEST clipboard implementation in the tree — under-consumption of a good dependency, not a re-mint.
5. **The strip's a11y posture is exactly right** (`aria-hidden` + `role="presentation"`, :114-115, reason recorded at :107-108) — both axes attacked its geometry; neither could fault its semantics (reader-DU; challenge-D §4 says so explicitly).
6. **The `swatch-row` adoption is precise and load-bearing** (:95): one of the recipe's three declared consumers, buying the faces membership in BOTH color-surface rosters — the exact contrast that makes MR-18's hand-rolled sibling visible (reader-DU).
7. **`wellColor` writes down a falsifiable spatial claim** ("the pool lands on slot 0 — spatially true", :35) — which is why the sibling apotheosis could catch its twin (`convergeCss`, banked MX-18) diverging (reader-DU).
8. **Motion is family-named and tokenised throughout** (`vj-morph` :60, `vj-enter` :92, tokens at :153; no fourth family) — with this record's irony noted: every one of the three motion declarations is then defeated by cascade (MR-3) or mount structure (MR-4). The names are right; the wiring kills them.
9. **Craft edicts clean, re-checked**: `import type` (:7), reactive props destructure with default (:20-23), no god module, no shim, no back-compat branch, no new `shared/`. The failure here is design and contract, not craftsmanship (both readers; this seat concurs from the whole-file read).

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — D-46's rendered pair (rgb(66,55,47)/rgb(59,51,45) → 1.076:1; arithmetic internally consistent per reader-DU's re-derivation; tokens boot-stamped, so the served pair is live-only).
- **R-2** — MR-3's painted result: that no fade is visible on the shipped enter and that exits snap (cascade statically certain; frames are not).
- **R-3** — D-45's 126.2 px verb→answer offset (order source-certain; magnitude live).
- **R-4** — D-48's perceptual claim (40 px dot stops reading as the species); test the turbulence-period mechanism, not the killed ratio.
- **R-5** — D-47's exact wrap viewport (wrap-at-7 arithmetic certain for the 292 px plate; other arms vary) — the **400 %-zoom arm** both canon docs mandate remains the one arm with ZERO coverage across seven reads of this directory; it would move this row first.
- **R-6** — every runtime JSON in challenge-C r6 (focus probe, node-identity probe, copy-timing probe): mechanisms confirmed static; captures must be re-taken through `startMix()` per PIN 5 before any is treated as a shipped frame.
- **R-7** — D-3/D-40's +68 px/+75 % ghost→settled height jump and the 224-281 ms `mode="out-in"` dead window (mechanism entailed; magnitudes live).
- **R-8** — R5-1's throw: NOT re-probed by any seat this round; banked as MX-4 with corrected blast; neither corroborated nor disputed here.
- **R-9** — F-15's "0 of 60 captures contain this component" (structurally certain via the cold-route `v-if`; the enumeration itself unverified).
- **R-10** — L's module-graph figure (66/224 KB vs 8/12.6 KB): honest as a graph figure, not load-bearing anywhere in this record.

## Motion-law compliance

No `motion-quarantine.md` FALSE row is carried. The quarantine's subject set is the 46 REPORT-AUTHORED rows; all three axes of this corpus are EXISTS-ORIGINAL (HYDRATION-LEDGER.md:249-251, my read), and my grep of the quarantine for mix/resultdisplay/vj-morph rows returns **none**. Every motion claim in this record (MR-3, MR-4) is this seat's own static cascade/mount derivation against animations.css and the SFC, not a carried live cell.

## DISSENT — preserved, not ruled away

1. **Reader-LC holds MAJOR on the separator and the eyebrow**; ruled MINOR (PIN 3). If a live witness shows the phantom `role="separator"` materially disordering AT navigation of the action row, MR-15 revives at LC's grade.
2. **Reader-DU confirmed R6-3 as filed**; ruled RESCOPED under LC's M1 (PIN 2). DU's return did not contain the fresh-mount mechanism at all — recorded beside PIN 7's symmetric observation that LC missed MISS-1: each reader owned exactly one half of the motion-death story.
3. **D-45's MAJOR sits beside the sibling record's MX-28 INFO** (PIN 4). If the next formation rules that ANY composition delta at an un-migrated pane is forward-conformance-only, MR-8 demotes to that class; the one-line separability argument is this seat's, preserved for that ruling.
4. **C's apparatus produced three unreachable-arm captures this round** (K-5, K-13, PIN 5) while remaining the corpus's most falsifiable artifact — same verdict as the sibling record: where its findings died, framing died, never a measurement. The `setupState` method note (R6-6) is genuinely valuable AND is the source of every killed frame; both facts stand.
5. **K-10 cuts both ways**: LC's kill of L's "100 %" is upheld, but LC's own "the G-DEMO-3b ban is enforced there today" over-claims against banked MX-35 (the live glob's only pattern keys a dead alias). Recorded so neither record is quoted for more than it proved.

---

**ADJUDICATED RED — the corpus's direction confirmed, both re-readers adopted only where they survived attack, three cures killed, two errata corrected, one consequence-limb corrected by this seat's own derivation.** 33 rows (2 BLOCKER-folds · 10 MAJOR · 17 MINOR · 4 INFO); 13 killed claims with written refutations; all 9 reader misses admitted. Terminal dispositions: **the live weight of this docket — the style-block motion kill, the dead `vj-enter`, the focus drop, the half-consumed clipboard, the optional-bag type, the ordinal decoupling, the verb inversion, the ghost cardinality — lands on MX-CLUSTER, NO-WAVE-OWNER**, re-establishing the sibling record's PIN 1 for a second file in the cluster; **4 rows/limbs to X-W6.j** (sequence + permanent region + save handler + sibling riders) · **3 to X-W7/.c** (strip consolidation coupled to PC-7, export unit) · **2 riding X.W4.d/X.W4.g** · **1 to X-W1** · **6 banked-identity folds**. The compound headline for the next formation boundary: the component whose entire purpose is to be the announced, animated destination of the product's one narrated convergence cannot be found by the canvas, cannot fade in, cannot stagger its swatches, cannot speak to AT, and renders below the verb that produces it — and the file it lives in is owned by no wave.

— end of apotheosis. The three challenge records (r1-r6 priors preserved beside them) and both re-reader returns remain immutable beside this file per E-1/E-3.
