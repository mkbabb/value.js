# wb-mix-resultdisplay — adjudicated apotheosis, ROUND 8 (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **HEAD**: `053f7d59` · branch `tranche-u` · **Corpus**: `demo/workbenches/mix/MixResultDisplay.vue` (159 lines) + its contract surfaces (MixPane.vue · composables/useMixingState.ts · MixSourceSelector.vue · **demo/palettes/mix.ts, opened by a reader for the first time this round** · demo/styles/{animations,utils,foundation}.css · installed glass-ui 7.0.0 dock/watercolor-dot/useClipboard). Challenge axes on disk unchanged since 2026-07-29, r6 governing (unsuffixed files; r1–r5 priors preserved beside them).
**SUPERSESSION**: this file replaces the 2026-08-12 apotheosis IN PLACE per the round-8 commission. The prior record is preserved verbatim at `git show 053f7d59:docs/tranches/V/megatranche/registry/adjudicated/wb-mix-resultdisplay.md`; every id in it (MR-1..MR-33, K-1..K-13, superlatives 1–9, R-1..R-10) is carried here UNCHANGED IN IDENTITY. Line-number citations *into* the prior record resolve at that commit, not at this file.
**Re-readers adjudicated**: TWO hostile round-8 returns against the full corpus + the banked record (design-and-usage = "r8-DU": 10 confirmed-as-folds · 5 killed · 2 missed · 6 superlatives · 6 unproven; library-and-code = "r8-LC": 12 confirmed-as-folds · 8 killed · 5 missed/4-filed · 5 superlatives · 4 unproven). Both were attacked before adoption. This round's confirmed columns are overwhelmingly RE-VERIFICATIONS of banked rows (correct fold discipline, zero re-books); the round's live matter is **two new kills of previously-unrefuted corpus claims (D-33's two limbs), two retirements (R4-5, F-7-as-diagnosed), one killed cure (N-4), one banked-kill addendum (K-2 limb b), and six new roster rows** — one of which this seat DEMOTED by killing three of its quantifiers with its own census.
**Anchor stability**: `git diff --stat 4ddf11d1 053f7d59 -- demo/workbenches/mix/ demo/styles/ demo/palettes/mix.ts demo/palettes/browser/card/ eslint.config.js tsconfig.demo.json` → **EMPTY** (my run). The tree the banked record adjudicated is byte-identical at this HEAD; every banked line anchor re-resolves.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling per law)**: whole reads of MixResultDisplay.vue, useMixingState.ts, MixPane.vue, **demo/palettes/mix.ts (147 lines, whole)**; MixSourceSelector.vue:2/:8/:119/:123/:129/:183/:203/:246-268; MixAnimationCanvas.vue:28-35; PaletteColorStrip.vue:25-50 (the per-COLOR `weights` docblock, T19); demo/styles/animations.css:180-212 (Guard 1 + the `[data-state]` carve-out with its "fade communicates state" rationale); foundation.css:50-80 (glass `./styles` imported BEFORE `./animations.css` — order confirmed), :310-360 (D1 rung-2 WELL token block naming "the mix result plate" among "the six parallel hand-mints D1 retires" + `.console-well` "ONE home for the console species"), :723-750 (`prefers-contrast: more` — bump roster = `.console-well, .app-layout .glass-resting, .app-layout .console-well, .slug-pill, [role="tab"]`), :815-845 (print — ruled-box roster = `.glass-resting, .console-well, .veil-surface, [data-surface="veil"]`; color roster `.swatch-row > *` at :833); demo/styles/utils.css:88-138 (`.dashed-well` 1.5px dashed `--card-edge` + `--shadow-cartoon-sm`; `.search-seated` 1.5px solid + stamp), :160-179 (`.swatch-row` recipe + the `position: absolute` leave limb + its three-consumer comment); **`bg-well`/`var(--well-bg)` demo-wide census (my grep — the MR-34 decider)**: beyond the three edge-bearing wells, GradientEasingEditor.vue:176 (`rounded-md bg-well`, no edge, no stamp), ColorNutritionLabel.vue:110 (`bg-well rounded-panel`, no edge), Markdown.vue:235/:241/:251/:275/:346/:386 (six edgeless `bg-well` fixtures), EasingAuthoringStage.vue:94, GradientStopEditor.vue:288 (WITH border), GenerateControls.vue:132 (WITH border+stamp), ShadowPalette/PaletteCard(+skeleton)/VersionHistoryDrawer (all WITH border); registry sweep `grep -rln 'console-well' registry/adjudicated/` → **5 files** (ConfigSliderPane.md committed Jul 27; wb-gradient-easingauthoringstage.md committed `1b7816d4` Aug 12 — **EAS-12 banks the well-recipe-divergence CLASS with owner X-W10**); installed-pin bytes re-read this session: `dist/styles/utilities/a11y-overrides.css` (Guard 2 verbatim: `*:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important }`), `dist/components/dock/styles/controls/touch-floor.css` (`:not(.dock-icon-button--compact):not(:where(.glass-dock *))`), `icon-button.css` (`--compact` block; `--dock-compact-control-size` grep over dist → **declared nowhere**), `dist/components/dock/styles/index.css` (`.dock-icon-button:focus-visible … box-shadow: var(--dock-ring); outline: none` UNSCOPED), `dist/styles/tokens/sizing.css` (`--dock-ring: var(--focus-ring-shadow)`), `dist/styles/theme/radius.css` (`--radius-panel: var(--radius-xl)` = 12px = `rounded-xl` — r8-DU's radius equivalence confirmed), `dist/useClipboard-D36OTaeT.js` head (`no-api` reason + writeText catch); `@vue/runtime-dom/dist/runtime-dom.cjs.js:1336-1450` (TransitionGroup FLIPs `prevChildren` via `recordPosition`/`applyTranslation` transforms; NO positioning ever written to a leaving element); motion-quarantine.md §1 "Guard 2" + "Cascade winner (measured, not reasoned)" + rows M-01/M-03/M-17 (the FALSE-row precedent class) + wb-mix-configbar.md R-4 verbatim; OPTICAL-BENCH-COMPOSITIONS.md Mix rows re-read verbatim (regions "source mode/rack; result; method/provenance/commit"; boundaries `[]` · retained line `none` · "rack, result and action regions remain distinct through geometry"); challenge-D-design.r5-prior.md:193-224 (D-33 original) + challenge-D-design.md §4 (the r6 "I do not contest it" endorsement + the focus-ring derivation); challenge-C-implementation.md ledger (R4-5 INFO row); challenge-L-library.md:415-425 (§5.2) + :486-492 (F-3/F-7/F-8/F-9′/F-10/F-15/F-17 rows); eslint.config.js:232-238/:274-277; tsconfig.demo.json:37-50 + `package.json#exports` keys (7, no bare root — the five-key drift re-derived); `mixPalettes`/`mixColorSequence`/`palettes/mix` importer census (3 importers; `weights` passed by NO app caller; test/mix-v4.test.ts exercises the mixColorSequence parameter only); `demo/workbenches/mix/` published-surface grep (bare `@mkbabb/value.js` = 0, `@src/` = 0); demo/ui/card/index.ts (one line, root barrel) + `ls demo/ui` = 19 dirs.

## RULING PINS (this seat's own round-8 derivations)

1. **D-33 dies twice, and the record finally holds its written refutation — r8-DU's kill CONFIRMED with the quarantine's own precedent class.** (a) Limb 1 (the PRM "escape"): the measurement was right, the mechanism wrong in both halves. The demo's `*` guard (animations.css:184-192) DOES reach `.mix-plate` — it just LOSES: Guard 2 (`a11y-overrides.css`, reached at foundation.css's glass `./styles` import, before `./animations.css`) declares `*:not([data-allow-motion])` = (0,1,0) over `*` = (0,0,0) at equal `!important`, so `transition-duration: 0.1s` + the five-property allowlist win app-wide — the exact "Cascade winner (measured, not reasoned)" ruling of motion-quarantine.md §1, and the identical class already carried as INFO-consistent-NOT-a-defect at wb-mix-configbar.md R-4. And because Guard 2 FORCES `transition-property` to an allowlist containing `opacity`, deleting the scoped shorthand (:152-154) changes the PRM computed value by exactly nothing — D-33's attribution ("the exposure exists because the component authored its own scoped transition") prescribes a void cure. The 0.1s opacity/colour fade is the producer's deliberate non-vestibular accommodation, the same posture the demo itself carves out at animations.css:196-210. Quarantine precedent: **M-17** (same claim shape, FALSE), **M-03** ("motion is governed centrally by design … not a gap", FALSE), **M-01** (discharged obligation). The r6 D-axis §4 endorsement ("a residual escape via the scoped shorthand … I do not contest it") dies with the limb. → **K-14**. (b) Limb 2 (`ghostFrames: 0` as "a second, undrawn design"): canon-satisfied — VISUAL-CONSTITUTION §6 demands reduced motion "resolve directly to the final geometry and stable chromatic state", useMixingState.ts:9-15 states the complete-immediately contract in-tree, and wb-mix-animationcanvas superlative 3 rules the posture "the CURE for the repo's PRM-RAF epidemic". A PRM user losing the narration window is the accommodation working. The one true residue — nobody is TOLD the settle happened — is not a PRM defect: it is banked MR-11 (MX-8), for all users. → **K-15**. Consequence for the visual audit: **the plate's PRM probe is RETIRED — do not spend it** (see residue).
2. **r8-DU's first missed find is ADMITTED at MINOR after this seat killed three of its quantifiers — and its "REGISTRY-FIRST sweep → ZERO files" premise is FALSE on the record.** Facts that survive my re-derivation: MixResultDisplay.vue:55 is `bg-well rounded-xl` — token + radius, no `--card-edge`, no stamp; `--radius-panel = var(--radius-xl)` = the same 12px, so the plate is arithmetic-identical to `.console-well` minus the border limb; the plate is absent from BOTH the `prefers-contrast: more` border-bump roster (foundation.css:728-750 — nothing to thicken, because nothing exists) and the print ruled-box roster (:822-828 — the region prints boundary-less while its dots survive via `.swatch-row > *`). Quantifiers KILLED by my census (**K-19**): (i) "*every sibling well is inside* both rosters" — FALSE: `.dashed-well` and `.search-seated` are in NEITHER roster (their only foundation.css occurrence is the :325 consumer comment); the rosters key on the `.console-well` CLASS, not on wellness. (ii) "*the only rung-2 well with neither an edge nor an elevation*" — FALSE: GradientEasingEditor's readout-rail (:176), ColorNutritionLabel's rows (:110), and six Markdown code-plate surfaces are all edgeless `bg-well` fixtures; edgeless wells are a common, apparently sanctioned register. (iii) "*not banked … grep → ZERO files*" — FALSE at the reader's own run time: ConfigSliderPane.md (Jul 27) and **EAS-12** (wb-gradient-easingauthoringstage.md, `1b7816d4`, Aug 12) already carried `console-well`; EAS-12 BANKS this exact class — "the well recipe re-mint loses its contextual arms" including the SAME two roster blocks — with disposition "X-W10 owns foundation.css's survivor recipe — cite, don't fork". What survives is the mix-plate MEMBER of that banked class, with one Mix-specific twist the gradient seat did not have: **OBC:77 sets Mix boundaries `[]` · retained line `none` · "distinct through geometry"** — so r8-DU's cure ("the geometry already exists in-tree as ONE class", i.e. take `.console-well`'s border) is NOT free: a drawn hairline on this plate is a canon decision, the same trap this corpus already ruled once at MR-15 (a real separator would MINT a defect) and at K-8 (the tone-step cure). The edge-vs-geometry question rides MR-8/MR-23's region-ownership edit. → **MR-34, MINOR** (the reader's provisional MAJOR dies with the uniqueness quantifiers; the live cell R-12 can promote).
3. **The `.swatch-row` leave recipe's static-position hazard is ADMITTED as a cure rider on MR-4 — mechanism ruled CERTAIN, magnitude ruled LIVE; the readers' split is adjudicated, not averaged.** r8-DU filed it MINOR with a spec proof; r8-LC filed the same facts UNPROVEN ("engine-dependent; I will not rule it from source"). My ruling: utils.css:177-179 `.swatch-row > .vj-enter-leave-active { position: absolute }` with no positioned ancestor established by any of the three declared consumers (MixResultDisplay.vue:95, MixSourceSelector.vue:123, CurrentPaletteEditor.vue — per the recipe's own :167-172 comment); Vue supplies no coordinates (my runtime-dom read: FLIP transforms on `prevChildren` only — `recordPosition`/`applyTranslation`; a leaving element is never positioned); CSS Flexbox L1 §4.1 resolves the abspos child's static position *as if it were the sole flex item* — the container's content-box start under default `justify-content`, NOT the item's former slot. That is a spec mandate, not an engine coin-flip; what IS live-only is the rendered jump distance and any engine's non-conformance, so the visible consequence stays residue (**R-11**) and must be measured in the two consumers that run real leaves TODAY (identity keys at MixSourceSelector.vue:129) **before** MR-4's `appear`+identity-keys cure activates leaves in THIS file. Fix the recipe once (record the leaving element's offsets, or drop the abspos limb and let `.vj-enter-move` carry the reflow), never per consumer. → **MR-35, MINOR (latent here, live in the siblings)**.
4. **`demo/palettes/mix.ts` — opened for the first time in eight reads — yields four rows; F-7's diagnosis is killed, its instinct honoured.** F-7 claimed the module's LOCATION forces the `as unknown as` at :37 and prescribed promotion into `@mkbabb/value.js/color`. My whole-file read: the cast is forced by `mixedOrThrow`'s OWN declared return type `PickerColorIn<S>` — a narrowing BOTH call sites discard on the next line (:57's `let mixed: AnyColor` re-widens at :63; :102 returns into `getColorAtIndex(…): AnyColor`), and the module depends on demo-only types (`Palette`, `PaletteColor`, `PickerSpace`), so it cannot move as written. Delete the unconsumed generic return type and the repo's loudest type-escape idiom deletes itself (**MR-36**). The provenance check r8-LC ran is confirmed by my read of the superseded record: F-7 was the one challenge-L row the banked adjudication neither adopted nor killed. → **K-17** + MR-36/-37/-38/-39.
5. **K-2 gains limb (b), r8-LC's own addition, byte-verified by this seat**: `.dock-icon-button--compact { width: var(--dock-compact-control-size, auto); height: …; padding: var(--dock-compact-control-padding, 0.25rem) }` and `--dock-compact-control-size` is declared NOWHERE in the shipped package — so a `compact` control is ~28px INSIDE a dock too, and the L-axis's probe never measured the one cell (compact-inside-dock) its causal claim required. The banked kill's cure menu is unchanged and now doubly grounded: set the declared-but-unset `--dock-compact-control-*` at the plate root (ActionBarToggle.vue precedent) or drop `compact`. r8-LC's killed-cure companion — N-4's "swap the family to `<Button iconOnly>`" — dies for the same reason: the component already in use exposes the exact knob the diagnosis needs; swapping component families is the largest available change for the smallest defect. → **K-2b addendum + K-18**.
6. **The fold discipline held for a second consecutive round — and the round's real product is written refutations, not new weight.** 22 of this round's 22 confirmed rows across both readers fold to banked identities (MR-1/MR-3..MR-10/MR-12/MR-14..MR-18/MR-28..MR-33 + K-1/K-2/K-9..K-12 re-verifications), several with sharper instruments than any prior seat (r8-LC's `dist/watercolor-dot.js` render-function read — one `span`, `renderSlot` grep = 0 — is the tightest MX-1 witness yet; its five-key exports-drift re-measurement independently reproduces the banked MR-29 note). Zero re-books. The six new rows are all MINOR/INFO: the corpus's centre of gravity was found in rounds 1–7 and has not moved — what moved is that D-33, R4-5 and F-7 now carry refutations on the record instead of silence.

## Scoreboard

**39 adjudicated rows: 2 BLOCKER (both folds) · 10 MAJOR · 21 MINOR · 6 INFO — 13 folds to banked identities + 1 fold-to-class (EAS-12), 25 rows/limbs live at this docket.** 19 killed claims with written refutations (13 carried + 6 new). All 6 reader missed-finds this round admitted — one demoted by quantifier-kill, one ruled against a reader's own excess caution. Headline unchanged from the superseded record and now harder: **the announced destination of the mix convergence is motion-dead in every direction on the only reachable arm — its anchor cannot render, its arrival is cascade-pinned, its per-swatch entrance never fires, its exit snaps, it renders below the verb that produces it — while its reduced-motion posture, twice accused, is ruled CORRECT twice, and the only unowned math module it runs carries a dead generic, an inverted `weights` contract, and a misleading home.**

## Ruled disagreements (the seat's round-8 work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **D-33 limb 1** (PRM escape) | r5-D + r6-D §4: real residual escape, component-authored; r8-DU: killed | **KILLED (K-14)** | PIN 1a — Guard 2 bytes + cascade winner + void-cure derivation; motion-quarantine M-17/M-03 precedent; wb-mix-configbar R-4 identity. |
| **D-33 limb 2** (ghostFrames:0) | r5-D: "a second, undrawn design", MAJOR; r8-DU: killed | **KILLED (K-15)** | PIN 1b — VC §6 satisfied; in-tree contract useMixingState.ts:9-15; banked animationcanvas superlative 3; residue routes to banked MR-11, not to PRM. |
| **missed find 1** (well-recipe limb) | r8-DU: MAJOR-provisional, cure = `.console-well`; this seat: quantifiers false | **ADMITTED DEMOTED — MR-34 MINOR; cure demoted to canon question (K-19)** | PIN 2 — my `bg-well` census (readout-rail, nutrition rows, Markdown plates all edgeless); dashed-well/search-seated outside BOTH rosters; EAS-12 banks the class; OBC:77 boundaries `[]`. |
| **missed find 2** (abspos leave) | r8-DU: MINOR, mechanism certain; r8-LC: UNPROVEN, engine-dependent | **ADMITTED — MR-35 MINOR; mechanism CERTAIN, magnitude LIVE (R-11)** | PIN 3 — Flexbox §4.1 sole-flex-item static position is spec-mandated; runtime-dom read; the rendered jump stays live-only. |
| **F-7** (mix.ts homing forces the cast) | L-axis: MINOR-hypothesis, promote to library; r8-LC: diagnosis killed | **KILLED (K-17); residue booked as MR-36 + MR-38** | PIN 4 — the unconsumed generic return type is the cause; both call sites re-widen; demo-only type deps forbid the prescribed move. |
| **N-4 cure** (swap to Button) | L-axis: replace the three DockControls; r8-LC: killed | **KILLED (K-18)** | PIN 5 — the declared-but-unset `--dock-compact-control-*` knobs + ActionBarToggle precedent; K-2's mechanism owns the cure. |
| **R4-5** (doubled decorative hiding) | C-axis: INFO defect; r8-DU: killed | **RETIRED (K-16)** | `role="presentation"` on a `<div>` is a no-op; `aria-hidden` alone suffices; banked superlative 5 rules the posture "exactly right"; redundancy-without-consequence is not a row. |

## Adjudicated defect roster

Line anchors are HEAD `053f7d59` (tree byte-identical to the banked base `4ddf11d1`); re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend per the superseded record (unchanged): **MX-CLUSTER** = NO-WAVE-OWNER booking (mixStage.ts · useMixingState.ts · **MixResultDisplay.vue beyond the X.W4.g import+face carve** · ErrorBoundary.vue · **demo/palettes/mix.ts** · the two RED mix specs); **X-W6.j** owns MixPane/MixConfigBar/MixSourceSelector/the canvas pair; **X-W1** CI e2e; **X-W7/.c** strip consolidation + export unit; **X.W4.d** shell dispatch deaths; **X-W10** register canon + foundation.css survivor recipes (EAS-12).

### Carried rows (identities unchanged; this round's re-verifications noted)

- **MR-1 — BLOCKER, FOLD → MX-1**: `data-mix-target` (:69) stamped on a WatercolorDot and dropped (`inheritAttrs:!1`, six declared props, class/style only); the convergence anchor cannot exist. *Round 8 adds the seventh/eighth independent verifications — r8-LC's render-function read (one `span`, zero slots) is the tightest yet.* → MX-CLUSTER · X.W4.g · X-W6.j.
- **MR-2 — BLOCKER, FOLD → MX-3**: both e2e specs structurally unsatisfiable; no CI playwright; zero unit tests import the SFC. → MX-CLUSTER · X-W1.
- **MR-3 — MAJOR**: the nine-line scoped block (:149-158) defeats `vj-morph` in all three directions (enter list truncated, leave list truncated, ghost `(0,2,0)` opacity pin from frame one — `startMix()` writes result+phase in ONE synchronous call). *Re-derived by r8-DU including the zero-`@layer` census; unchanged. NOTE: this is the NO-PREFERENCE cascade kill — it does not revive D-33 (K-14): under PRM the producer's Guard 2 governs and is correct.* → MX-CLUSTER.
- **MR-4 — MAJOR**: `vj-enter` structurally dead (fresh-mount on every settle, no `appear`); index `:key` latent. *Now carries the MR-35 cure rider: the recipe's abspos leave limb must be fixed BEFORE the cure activates leaves here.* → MX-CLUSTER.
- **MR-5 — MAJOR**: Reset unmounts the activated control; focus → `<body>`; `.focus()` census 0 (WCAG 2.4.3). *Re-walked by r8-DU.* → MX-CLUSTER + X-W6.j.
- **MR-6 — MAJOR**: `useClipboard` half-consumed — CopyResult discarded, no `onCopyError`, failure arm timer-less (terminal), `invalidate()` never called. *r8-LC re-verified at the settle-arm bytes.* → MX-CLUSTER.
- **MR-7 — MAJOR**: `MixResult` optional bag; five masking guards; blank-bar shipped consequence (PIN 5 of the superseded record). *r8-LC re-verified.* → MX-CLUSTER.
- **MR-8 — MAJOR**: result region renders AFTER method/commit, inverting OBC:43 + VC:48 (source-order certain, MixPane.vue:80/:97/:112). *Canon re-read verbatim this session.* → X-W6.j (pairs MR-23).
- **MR-9 — MAJOR**: rack wraps at SEVEN faces per 292px row while the strip stays one row — the two depictions of one ordinal decouple at the canon's named count of 12. *r8-DU independently reproduces the wrap-at-7 erratum.* → MX-CLUSTER + X-W7.c.
- **MR-10 — MAJOR, FOLD → MX-32**: the palette arm has NO text alternative; `:title` dies on the producer surface; entire accessible text = "Result". → MX-CLUSTER / X.W4.g.
- **MR-11 — MAJOR, FOLD → MX-8**: the ≥900ms settle announced to nobody (zero `aria-live`/`role="status"`). *K-15's PRM residue routes HERE, for all users.* → X-W6.j.
- **MR-12 — MAJOR, FOLD → PCS-8**: certified `demo/palettes/export/` has one importer (a test); the app runs legacy `export.ts`. *Re-verified: single-hit census.* → X-W7.
- **MR-13 — MINOR, FOLD → GAB-17**: two result-copy implementations; the dock routes to the silent one (MixPane.vue:49-55). → X.W4.d.
- **MR-14 — MINOR**: three 28×28 actions; `compact` IS the producer's own touch-floor opt-out. *K-2b makes the mechanism total: compact is ~28px inside a dock too.* → MX-CLUSTER.
- **MR-15 — MINOR**: `<DockSeparator />` renders 1×0 yet stamps `role="separator"` + 12px rhythm; cure = deletion (OBC:77 retained line `none`). *Mechanism re-verified in kind by r8-DU.* → MX-CLUSTER.
- **MR-16 — MINOR**: the "Result" eyebrow — Fraunces ITALIC bold uppercase, five-utility re-mint of `.section-label` against two sibling variants. *Both readers re-verified at the installed semantic.css.* → MX-CLUSTER.
- **MR-17 — MINOR, FOLD → PCS-10/R-F**: hand-rolled strip duplicates `PaletteColorStrip` (imported by the sibling); registers not interchangeable (K-9). → X-W7.c.
- **MR-18 — MINOR, FOLD → PC-7/PCS-2**: the strip is outside BOTH color-surface roster arms (:679-694/:830-841) — a SIBLING of `.swatch-row`, unreached by `.swatch-row > *`. *Distinct subject from MR-34: different selectors, different rosters.* → X-W7.c.
- **MR-19 — MINOR**: `title="Copy color"` unconditional and wrong across the palette half; `title` = sole accessible name ×3. → X-W10/X-W4 + MX-CLUSTER.
- **MR-20 — MINOR**: rack is writing-mode aware, strip hard-codes `to right` — RTL contradiction. → MX-CLUSTER / X-W7.c.
- **MR-21 — MINOR**: value artifact `break-all` + no LTR isolation. → MX-CLUSTER.
- **MR-22 — MINOR**: region declared solely through the sanctioned 8% tone step; the geometry job never done (K-8: the cure must be geometry). *MR-34 is the recipe/roster face of this same surface; they land in one edit.* → MX-CLUSTER + X-W6.j.
- **MR-23 — MINOR**: no pre-result state — the region does not exist until the first mix, against VC:48's permanent inspector. → X-W6.j.
- **MR-24 — MINOR**: Save emits into a void — no register, no de-dup; five clicks, five rows. → X-W6.j + MX-CLUSTER.
- **MR-25 — MINOR**: mixStage's fabricated target `r: 28` encodes the colour-branch well; 40% wrong for every reachable mix. → MX-CLUSTER rider.
- **MR-26 — MINOR**: verb-scope inversion — plate "Reset" discards only the result; the clearing verb exists only on the dock. → MX-CLUSTER + X.W4.d.
- **MR-27 — MINOR**: the ghost under-declares the palette answer by a factor of N (one well vs N faces + strip). → MX-CLUSTER.
- **MR-28 — MINOR, FOLD → L-5(a)+MX-35**: no import-boundary rule reaches this file. *r8-LC re-verified the glob census; both over-claims stay dead (K-10).* → banked NO-WAVE-OWNER.
- **MR-29 — MINOR, FOLD → MX-20**: tsconfig `paths` ↔ exports drift. *r8-LC's independent re-measurement reproduces the banked five-key sharpening exactly (3 phantoms: bare root, /parsing, /units; 2 omissions: /css, /value; the "CLOSED 8-key set" comment false) — confirmed by this seat's own key-list run.* → banked (W-HYGIENE).
- **MR-30 — INFO**: dead `TransitionGroup` import at TWO sites (:4 + MixSourceSelector.vue:2). *r8-LC re-confirmed via the in-file `<Transition>` asymmetry.* → MX-CLUSTER rider + X-W6.j.
- **MR-31 — INFO**: MixPane.vue:2 imports `computed`, never calls it. *Re-confirmed, my read.* → X-W6.j rider.
- **MR-32 — INFO, FOLD → PS-29**: root-barrel `useClipboard` import beside two subpaths (36/54 census stands). → banked fold.
- **MR-33 — INFO, FOLD → AdminPane L-5 / SwatchHoverMenu K-5**: `Card` through the demo/ui barrel vs direct glass-ui in one folder (17 one-line pass-throughs, not 19 — re-confirmed: `ls demo/ui` counts directories). → banked fold.

### NEW rows (round 8)

- **MR-34 — MINOR (demoted from the reader's provisional MAJOR — PIN 2/K-19) · FOLD-TO-CLASS → EAS-12 (wb-gradient-easingauthoringstage.md, `1b7816d4`): the mix plate is the well-recipe-divergence class's Mix member — `bg-well rounded-xl` (:55) is `.console-well` minus its border limb (radius arithmetic-identical: `--radius-panel = var(--radius-xl)` = 12px), and having NO edge of any kind it is unreachable by the `prefers-contrast: more` border bump (foundation.css:728-750) and prints boundary-less (:822-828) while its dots survive via `.swatch-row > *`.** Corrections carried in the row so it is never quoted for more than it proved: the plate is NOT the only edgeless well (readout-rail, nutrition rows, six Markdown fixtures), `.dashed-well`/`.search-seated` are ALSO outside both rosters (the rosters key on the class, not on wellness), and the D1 comment (:315-328) records a completed token consolidation, not an outstanding hand-mint. The Mix-specific twist that keeps the row alive rather than a pure fold: **OBC:77 assigns Mix boundaries `[]` / retained line `none`**, so whether this plate may take an edge at all is a canon decision, not a class application — the same trap already ruled at MR-15 and K-8. Cure: decided WITH MR-8/MR-22/MR-23's region-ownership edit; if canon rules an object-containment edge legal for the result plate, the recipe comes from X-W10's survivor row (cite, don't fork — EAS-12's own law); if not, the contrast/print story must come from geometry. → **MX-CLUSTER (the plate's line) · recipe X-W10 · canon question X-W6.j**; live cell **R-12** decides promotion.
- **MR-35 — MINOR (PIN 3) · CURE HAZARD on MR-4: the shared `.swatch-row` leave recipe (utils.css:177-179 `position: absolute`) has no static-position guarantee — no consumer establishes a positioned ancestor, Vue writes no coordinates to leaving elements (runtime-dom read), and Flexbox §4.1 resolves the abspos child as if it were the SOLE flex item: the departing swatch resolves to the row's leading edge, not its former slot.** Latent in THIS file (MR-4: no leave has ever run here); live TODAY in the two identity-keyed consumers (MixSourceSelector.vue:129, CurrentPaletteEditor). Banked MR-4's cure (`appear` + identity keys), applied alone, activates this in the plate. Cure: fix the recipe ONCE — record the leaving element's offsets, or drop the abspos limb and let `.vj-enter-move` carry the reflow — never per consumer. Rendered magnitude = **R-11**, to be measured in the live consumers BEFORE the MR-4 cure lands. → **MX-CLUSTER (coupled to MR-4) + flag to the siblings' waves (X-W6.j owns MixSourceSelector)**.
- **MR-36 — MINOR (PIN 4): `mixedOrThrow` (demo/palettes/mix.ts:28-38) carries a dead type parameter and a gratuitous double-cast — `PickerColorIn<S>` is a narrowing BOTH call sites discard on the next line (:57/:63 re-widen to `AnyColor`; :102 returns into `getColorAtIndex(…): AnyColor`), so the generic, the `PickerColorIn` import and the `as unknown as` at :37 exist to produce a type nobody consumes.** In a repo whose L.W4 close drove `as unknown as` to 1 across `api/`, the demo's only Mix math module carries an unexamined one. Cure: declare `AnyColor`, delete the cast. *F-7's location diagnosis and library-promotion cure are killed (K-17); this row is its sound residue.* → **MX-CLUSTER** (mix.ts is in the cluster's named list).
- **MR-37 — MINOR: `PaletteMixOptions.weights` (mix.ts:25) is unexercised dead API with a contract INVERTED against the identically-named prop one directory away, and its first misuse throws synchronously into the grid-wide boundary.** `mixPalettes` forwards it to a per-PALETTE cardinality (:130-141: one `colors` entry per palette), while `PaletteColorStrip.weights` is per-COLOR ("per-segment weights … quantizer populations — T19", my read of :38-44); no app caller passes it (census: the sole consumer useMixingState.ts:92-96 passes `{space, hueMethod, leftoverStrategy}`); a wrong-length array reaches the `"Each color requires a weight"` throw (:47-49) inside `startMix()` — the announced, recoverable, grid-wide boundary banked MX-4 books. Cure: delete the option or document + type the cardinality. → **MX-CLUSTER**.
- **MR-38 — INFO: `demo/palettes/mix.ts` is homed in a feature that does not consume it** — zero importers inside `demo/palettes/`; both runtime consumers live in `demo/workbenches/mix/` (useMixingState.ts:21, MixConfigBar.vue:14 type-only) which already owns a `composables/` directory; the third importer is test/mix-v4.test.ts:4 (the root-test-into-demo inversion already noted in the sibling record). The mis-homing is what made a demo-only helper look like a library candidate to the F-7 axis. → **MX-CLUSTER**.
- **MR-39 — INFO: `mixPalettes`' single-palette early return (:119) is dead on the only route that reaches it** (`canMix` requires ≥2, useMixingState.ts:50-53, checked before :92) — and if ever reached it silently returns a byte-copy of the input's colors, which MixPane.vue:44-45 would save as "Mixed Palette": a duplicate presented as a result, the exact masking-fallback shape MR-7 objects to, in the module the corpus never opened. → **MX-CLUSTER**.

## Killed-claims register

Carried kills (full refutations at the superseded record, `git show 053f7d59:…`): **K-1** (L §5.2 "never rendered" — re-killed by BOTH round-8 readers; the palettes arm is a native `<button>`), **K-2** (L N-1's dock-scoping mechanism — *now with limb (b), PIN 5: `--dock-compact-control-size` declared nowhere, so compact is ~28px inside a dock too; the axis's probe never measured compact-inside-dock*), **K-3** (producer-forwarding ask vs P051), **K-4** (R5-1's throw as ranked; zero parser imports here — re-confirmed by r8-DU's whole-file read), **K-5** (C's non-ghost enter capture — apparatus arm), **K-6** (wrap-at-8 → 7), **K-7** (D-48's self-refuting ratio — re-killed by r8-DU with the turbulence-period mechanism restated), **K-8** (D-46's ≥3:1 cure), **K-9** (N-2's "one import" strip cure — re-derived by r8-LC from its own PaletteColorStrip read), **K-10** (F-2's "100%" quantifier — re-killed; both over-claims still dead), **K-11** (F-8's 37/82 census + the mis-read `debounce` doctrine — r8-LC's independent third measurement reproduces 36/54 exactly), **K-12** (F-10's 19-barrels count → 17), **K-13** (R6-3's patch-in-place mechanism).

New this round:

- **K-14 — D-33 limb 1 (r5-prior :193-224; r6 §4 endorsement) — KILLED (r8-DU; ratified by this seat at the bytes + the quarantine).** The demo `*` guard reaches `.mix-plate` and LOSES to Guard 2's `*:not([data-allow-motion])` (0,1,0 vs 0,0,0 at equal `!important`) — app-wide, producer-designed; and Guard 2's forced five-property allowlist (containing `opacity`) makes the prescribed cure (delete :152-154) a no-op under PRM. The 0.1s opacity/colour fade is the deliberate non-vestibular accommodation the demo itself mirrors at animations.css:196-210. Precedent: motion-quarantine §1 "Cascade winner (measured, not reasoned)"; FALSE-row class M-17/M-03/M-01; wb-mix-configbar R-4 (identical class, INFO-consistent). The r6 endorsement ("I do not contest it") dies with the limb.
- **K-15 — D-33 limb 2 ("ghostFrames: 0 … a second, undrawn design") — KILLED (r8-DU; ratified).** VC §6's "resolve directly to the final geometry" is precisely what complete-immediately does; the contract is stated in-tree (useMixingState.ts:9-15) and ruled the PRM-RAF epidemic's CURE at the banked animationcanvas superlative 3. The lost narration window is the accommodation working; the surviving "nobody is told" residue is banked MR-11 (all users), not a PRM row. The limb also self-undercuts: it grades an exposure and a missing-state as one MAJOR while its own numbers show the exposure is 0.1s of opacity.
- **K-16 — R4-5 (C's INFO ledger row, "doubled decorative hiding at :114-115") — RETIRED (r8-DU; ratified).** `role="presentation"` on a `<div>` is a no-op (implicit generic role); `aria-hidden` alone removes the subtree; the pair is belt-and-braces on a surface banked superlative 5 already ruled "exactly right". Redundancy without behavioural consequence does not earn a docket row.
- **K-17 — F-7's diagnosis + cure (location forces the cast; promote into `@mkbabb/value.js/color`) — KILLED (r8-LC; ratified by this seat's whole-file read, PIN 4).** The cast is forced by the function's own unconsumed generic return type; both call sites immediately re-widen; the module's demo-only type dependencies forbid the prescribed move as written. Sound residue booked as MR-36 (the dead generic) + MR-38 (the real homing defect, correctly stated).
- **K-18 — N-4's cure ("replace the three `DockControl compact` with `<Button iconOnly emphasis="quiet">`") — KILLED (r8-LC; ratified, PIN 5).** The producer's own `--dock-compact-control-{size,padding}` are declared, unset, fallback-carrying knobs — one declaration at the plate root resizes all three seats (ActionBarToggle.vue precedent, cited by banked MR-14), and dropping `compact` is the other one-token cure once K-2 is understood. The delete-DockSeparator half of N-4 stands inside MR-15.
- **K-19 — three quantifier limbs of r8-DU's missed find 1 — KILLED by this seat's own census (PIN 2), the find itself ADMITTED as MR-34.** (i) "every sibling well is inside" both rosters — `.dashed-well`/`.search-seated` are in neither; (ii) "the only rung-2 well with neither edge nor elevation" — readout-rail/nutrition rows/Markdown plates; (iii) "REGISTRY-FIRST sweep → ZERO files / not banked" — 5 files carry `console-well`, two predating the reader's session, and EAS-12 (committed `1b7816d4`, 2026-08-12) banks the CLASS with owner X-W10. Recorded with the method lesson: a registry sweep that returns zero must be re-run with a second instrument before "not banked" is filed — the sweep IS a claim, and this one was false.

## Superlatives (provenance preserved)

Carried 1–9 (full text at the superseded record): the seed contract honoured end to end (1) · ghost geometry tracks its branch (2) · the file owns no clock (3) · the demo's only honest copy seat (4) · the strip's a11y posture exactly right (5) · `.swatch-row` adoption precise and load-bearing (6) · `wellColor`'s falsifiable spatial claim (7) · motion family-named and tokenised, then defeated by wiring (8) · craft edicts clean (9).

Round-8 additions and enrichments:

- **S-1 enrichment (r8-LC)**: the seed contract survives on BOTH inputs — the producer hashes `color + seed`, and `wellColor` (:36-40) selects exactly the value the landed face 0 wears (:80 colour arm / :100+:104 index-0 palette arm), which is what makes the `:104` special case load-bearing rather than an oddity. Any MR-4 identity-key cure must preserve both halves.
- **S-4 enrichment (r8-LC, bytes)**: what makes `useClipboard` the right primitive — the discriminated `CopyResult` with NAMED failure reasons (`"no-api"` vs `"clipboard-api"`, verified at the dist head this session), the generation counter that lets no stale attempt repaint a newer state, the optional `timeoutMs` watchdog, and `onScopeDispose` teardown. None of these appears in any axis document; MR-6 is under-consumption of exactly this.
- **S-10 (r8-DU; D-axis derivation §4; both byte locations re-verified by this seat)**: the producer's focus ring survives out-of-dock mounting — `.dock-icon-button:focus-visible { box-shadow: var(--dock-ring); outline: none }` ships UNSCOPED in `dist/components/dock/styles/index.css`, and `--dock-ring: var(--focus-ring-shadow)` is a root token (`dist/styles/tokens/sizing.css`); the forced-colors arm is separately covered (`a11y-overrides.css` `outline: 2px solid Highlight`). The one dock a11y affordance that does NOT die with the dock tokens — and why MR-14 is a sizing defect, not a keyboard-access one.
- **S-11 (r8-DU; my read of MixAnimationCanvas.vue:28-35)**: the decorative canvas cannot occlude the plate — `absolute inset-0 … pointer-events-none z-controls` + `aria-hidden="true"`. In a component whose docket is motion that lies, the motion surface's containment is exact. Recorded so no future round spends the probe.
- **S-12 (r8-LC)**: the cleanest available statement of the P051 divide is inside the package itself — `DockControl` (same folder, same pin) does NOT set `inheritAttrs:false`, declares its interaction props, and lets `$attrs` fall through, which is exactly why `@click`/`title` work at :121-142 while all six attributes handed to `WatercolorDot` twenty lines earlier die. Two components, one package, opposite attribute postures — the producer demonstrates both sides of the argument the corpus and canon have been having.
- **S-13 (r8-DU)**: the prose this component was suspected of lying in is accurate — :51-53's "floating-rung glass … drops to the opaque well tone-step" is an explanation of why there is NO glass, matching foundation.css:317-322 verbatim (RC-3). The counter-case that makes MX-37's anchor sentence a lapse, not a habit.
- **S-14 (r8-LC; my clean grep)**: the published-surface keystone holds in this workbench — bare `@mkbabb/value.js` = 0, `@src/` = 0, aliases generated from `package.json#exports`; every import an external consumer could write verbatim.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — D-46's rendered tone-step pair (tokens boot-stamped; served pair live-only). **Take together with R-12 — same capture session.**
- **R-2** — MR-3's painted result: no visible fade on the shipped enter; snapped exits (cascade statically certain; frames are not). *No-preference cell ONLY — the PRM cell is retired, below.*
- **R-3** — D-45's 126.2px verb→answer offset (order certain; magnitude live).
- **R-4** — D-48's perceptual claim; test turbulence periods per silhouette (`baseFrequency 0.05` absolute: ~2 at 40px vs ~2.8 at 56px), not the killed ratio.
- **R-5** — the **400%-zoom arm** both canon docs mandate: still ZERO coverage across EIGHT reads of this directory; it moves MR-9/D-47 first (wrap-at-7 is arm-specific to the 292px row).
- **R-6** — every runtime JSON in challenge-C r6 (focus, node-identity, copy-timing probes) remains PIN-5-bound: taken through `setupState` injection; must be re-taken through `startMix()` before any is a shipped frame. Re-affirmed by both round-8 readers; none re-taken (static-only law).
- **R-7** — D-3/D-40's +68px ghost→settled height jump and the 224–281ms `out-in` dead window (mechanism entailed; magnitudes live).
- **R-8** — R5-1's throw: still neither corroborated nor disputed at this component; banked as MX-4 with corrected blast; zero parser imports here (re-confirmed).
- **R-9** — F-15's "0 of 60 captures" (cold-route mechanism certain; enumeration unverified).
- **R-10** — L's module-graph figure (66/224KB vs 8/12.6KB): honest as a graph figure; not load-bearing anywhere in this record.
- **R-11 — NEW**: MR-35's visible teleport in the two LIVE consumers (MixSourceSelector, CurrentPaletteEditor) — the spec mechanism and Vue's non-intervention are certain; the jump distance is not. **Sequencing obligation: measure BEFORE MR-4's cure lands**, not after.
- **R-12 — NEW**: MR-34's rendered consequence — one computed-style read under `prefers-contrast: more` + one print-preview capture of the result region decides MINOR→MAJOR promotion. Same live cell as R-1.
- **RETIRED PROBE — do not spend**: the plate's reduced-motion cell. K-14/K-15 close both PRM accusations; the served PRM behaviour (0.1s opacity/colour fade, immediate settle, no ghost window) is ruled the correct, producer-designed posture. A PRM capture of this plate would witness a non-defect.

## Motion-law compliance

No `motion-quarantine.md` FALSE row is carried — and this round the quarantine is load-bearing in the opposite direction: **K-14 cites its §1 Guard-2 ruling and the M-17/M-03/M-01 FALSE-row precedent class as the ground for killing D-33's limb 1**, aligning this record with the quarantine instead of merely not violating it. My grep of the quarantine for mix/resultdisplay/vj-morph subject rows → none (its :162 Mix mention is MQ-1, a PaneHeader scroll-motion defect outside this component — noted, not booked here). Every motion claim carried in this record (MR-3, MR-4, MR-35) is a static cascade/mount/spec derivation of this seat or a prior seat, never a carried live cell.

## DISSENT — preserved, not ruled away

1. **r8-LC declined to rule the MR-35 static-position mechanism from source** ("engine-dependent; I will not rule it"); this seat ruled it spec-certain (Flexbox §4.1) with the magnitude live. If any target engine is shown non-conformant on sole-flex-item static positioning, the row's mechanism reverts to r8-LC's caution and R-11 becomes the whole finding.
2. **r8-DU graded its well-recipe find MAJOR-provisional with the live cell deciding**; this seat demoted to MINOR by killing the uniqueness quantifiers (K-19). If R-12 shows the boundary loss materially illegible under `prefers-contrast: more` or in print, MR-34 promotes at the reader's grade — the reader's framing of the live cell as the decider was correct even where its census was not.
3. **Carried dissents from the superseded record remain live**: LC-prior's MAJOR on separator/eyebrow (revives on an AT-navigation witness); the MX-28-vs-MR-8 conformance-class question (a formation ruling that composition deltas at un-migrated panes are forward-conformance-only demotes MR-8); the C-apparatus double verdict (most falsifiable artifact, source of every killed frame).
4. **Method note recorded against BOTH round-8 readers**: each filed at least one census this seat had to correct (r8-DU's zero-files registry sweep — false at run time; r8-LC's line anchors into mix.ts drift by 1–3 lines throughout). Neither error was load-bearing; both are the third and fourth instances of the round's standing lesson that a sweep or an anchor IS a claim and inherits the evidence bar.

---

**ADJUDICATED RED — the corpus's direction confirmed for the eighth time; this round's product is refutation coverage and one newly opened module, not new weight.** 39 rows (2 BLOCKER-folds · 10 MAJOR · 21 MINOR · 6 INFO); 19 killed claims with written refutations — including, at last, D-33 (both limbs, with the quarantine's own precedent), R4-5, F-7-as-diagnosed, and N-4's cure; 6 reader missed-finds admitted, one demoted by this seat's census, four of them from `demo/palettes/mix.ts`, the module seven prior reads never opened. Terminal dispositions: the live weight of the docket is UNCHANGED and lands on **MX-CLUSTER, NO-WAVE-OWNER** (now including the mix.ts quartet MR-36..MR-39 and the MR-35 recipe rider); **X-W6.j** carries the sequence/region/save/sibling rows plus MR-34's canon question; **X-W7/.c** the strip family; **X-W10** the survivor-recipe coupling (EAS-12) and register canon; **X-W1/X.W4.d/X.W4.g** as banked. The compound headline for the next formation boundary stands as written on 2026-08-12, with one addition: **the component's twice-accused reduced-motion posture is now ruled CORRECT on the record — the defects are everywhere else.**

— end of apotheosis. The three challenge records (r1–r6 priors), both round-8 re-reader returns, and the superseded 2026-08-12 apotheosis (at `git show 053f7d59:…`) remain immutable beside/behind this file per E-1/E-3.
