claude-fable-5

# kf-CSSPasteDialog — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Anchors**: value.js @ `c654824e` (branch `tranche-u`) · keyframes.js @ `8281638c` (branch `master`, working tree = the audited tree) · installed producer `node_modules/@mkbabb/glass-ui` 7.0.0 (dist is the contract of record).
**Corpus**: three axis challenges (D design · L library · C consumption, all authored by `claude-opus-5[1m]`, 2026-08-04) at `docs/tranches/V/megatranche/audit/kf-components/CSSPasteDialog/`, re-read by two hostile re-readers (design-and-usage = **R-A**; library-and-code = **R-B**), both returns adjudicated here.
**Method (L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (the two BLOCKER↔MAJOR demotions, the D-M4 rung, the D-M3 rung, the L-9 kill-vs-confirm split, the S-9 site count, and the readers' directly contradictory focus-on-open mechanisms) was re-derived against the tree by this seat with its own instruments. One reader-vs-reader factual contradiction was settled by artifact read and one re-reader row is **killed by this seat** — see K-9.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony; product source READ-ONLY)**: CSSPasteDialog.vue read whole (80 lines); KeyframesAddDialog.vue read whole (161 lines — `collapseToEnd` at :106 inside `reformat()`, `onInput` :96-98, Tab trap :117-120, nested DialogDescription-in-DialogTitle :24-31); KeyframeCard.vue:35-59 (`focus-ring hljs css …` :45, `role="textbox"` :47, `aria-multiline` :48, `:aria-label` :49); KeyframeTimeline.vue mounts + handlers (the two `<CSSPasteDialog>` mounts with `description="…merge…"`/`button-label="Add"`; `doImport`/`doAddCSS` — the close ref is set **inside** `if (text.trim())`, a precision correction to both readers' "unconditionally"); useTimelineBuild.ts:140-161 (`state.value.keyframes = imported` — whole-array replace; success toast reports the NEW count); iosTextEntry.ts:1-15 (`clampIOSNoZoomFontSize = … Math.max(fontSize, 16)`) + its consumers (CSSCodeEditor.vue:39/:137) + test/demo/instrument/ios-text-entry.test.ts:88-97 (asserts 14→16); demo/styles/design-idioms.css:73-79 (the `.focus-ring` "SINGLE keyboard-focus affordance" docblock, verbatim); shipped `dist/dialog-BKSTfmIQ.js` — `scroll:{type:Boolean,default:!1}`, the unbounded centred base class, DialogDescription `"text-sm text-muted-foreground"` (present) vs `"text-small text-muted-foreground"` (**0 hits**), and the **decisive slot-order read**: `default: C(() => [b(e.$slots, "default"), d.showClose ? (close X) : …])` — the default slot renders BEFORE the close X; shipped `dist/styles/typography/scale.css` — `--type-small: clamp(0.875rem, 0.8rem + 0.25vw, 1.25rem)`, `--type-body: clamp(1rem, 0.92rem + 0.27vw, 1.375rem)`, `--type-subheading: 1.272rem` (fixed; crossover W = 2085.9px re-derived); `grep -c ink-perimeter` over shipped `dist/styles/tokens/color-radius.css` → **0**; KF wave taxonomy read from `formation/keyframes/CENSUS-2026-08-03.md:179-206`; census rows re-read at `formation/keyframes/lane-frontend.md:15` (F-1 RED), `:105` (root-barrel symbol list incl. Dialog*), `:204` (row 80 `G`), `:264-395` (S-1..S-8 — **S-9 does not exist in the census**; it is corpus-minted, adopted below as an extension row).

## Scoreboard

**27 CONFIRMED (0 BLOCKER · 10 MAJOR · 17 MINOR) · 10 KILLED · 7 MISSED-then-adjudicated (6 component rows + 1 corpus-integrity row) · 9 INFO/fold-by-reference · 7 superlatives · 8 UNPROVEN-NEEDS-LIVE residue rows for SS-13.**

Both axis BLOCKERs are **demoted to MAJOR-with-escalation**: each rests its BLOCKER rung on a mechanism half that is marked UNPROVEN-NEEDS-LIVE by its own author (caret landing; overflow threshold). A BLOCKER may not rest on an unproven half. Each carries a named SS-13 probe whose positive witness promotes it back — recorded per-row.

The component **stays** (the parameterised shell is the right de-duplication — D-S3, upheld by both readers and this seat); the load-bearing cure is the **S-9 Textarea swap (KF.W6)**, which kills R-1/R-6/R-8/R-9/R-12/R-14/R-20 in one move, plus the **twin fold (KF.W7)**.

## Ruled disagreements (the seat's actual work)

| id | R-A | R-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **R-1 severity** (L-1/C-3/D-B1 contention) | BLOCKER "stands on all three axes" — while itself killing L-1's certainty language | MAJOR — kills the "destroyed on EVERY keystroke" proof with the value-identity third case | **MAJOR-with-escalation — R-B wins the rung; R-A's position is internally inconsistent** | R-A cannot both kill the certainty ("Selection placement … engine-defined") and keep the rung that certainty carried. R-B's third case is airtight at the DOM level and none of the three axes saw it: `onInput` writes back `pre.innerText` — the string the browser just put in the DOM — so in the ordinary post-edit shape (`<pre><code>TEXT</code></pre>`) Vue's `el.textContent = v` assigns a value **already present**; the VDOM guard `n1.children !== n2.children` passes (old state ≠ new state) but the DOM write is value-identical, and whether a value-identical `textContent` write destroys the node is engine-defined, not DOM-mandated. The proven hazard core is the **divergent shapes**: the shipped empty-`<code>` default (`initialText: ""` at :53, taken by both mounts) where first insertion may land as a sibling invisible-to-Vue-forever → compounding visible duplication; Enter (`<br>`/`<div>`); rich paste. Escalation: SS-13 probe #1 (first-paste `childNodes` read) — a witnessed sibling-branch duplication on the primary flow re-promotes to BLOCKER. |
| **R-2 severity** (C-1/D-B3/L-2 no scroll owner) | BLOCKER | MAJOR (Esc dismisses; Tab+Enter reaches the Button; not an unreachable state) | **MAJOR-with-escalation — R-B wins** | The threshold is UNPROVEN-NEEDS-LIVE by R-A's own row. Keyboard activation does not require visibility (focus + Enter fires the off-viewport Button); Esc and outside-tap dismiss. The severe compound is with R-4: every escape route **destroys the paste** (the open-watch wipe) — recorded on both rows. Escalation: SS-13 probe #4; a witnessed state where neither submit nor close is reachable by ANY input modality promotes. |
| **Focus-on-open DOM order** (R-A missed-3 vs R-B missed-4 — direct contradiction) | slot renders BEFORE close X → `<pre>` is the first tabbable | close X renders BEFORE slot → focus lands on the close X ("verified in the 7.0.0 dist chunk") | **R-A is RIGHT; R-B's row is KILLED (K-9)** | This seat's read of the shipped chunk: `default: C(() => [b(e.$slots, "default"), d.showClose ? (v(), o(x(w), { key: 0, class: "focus-ring absolute right-…" }) …])` — slot **first**, close **after**, verbatim. R-B's citation into "glass-ui src at b155ca4c~1:DialogContent.vue:482-489" is the exact source-over-dist error R-B convicted axis-D of (K-10) — committed by the convicting reader in the same return. R-A's mechanism (title/description not focusable; a contenteditable host is tabbable; nothing prevents `openAutoFocus`) stands statically; the landing itself remains SS-13 probe #3. Booked as R-27. |
| **L-9/D-m3 font-family fragility** | KILLED whole ("structurally impossible … no live check was ever needed"; survives as INFO dead-weight) | CONFIRMED MINOR (emission-order fragility; byte offsets re-measured) | **KILLED — R-A wins; R-B overruled** | R-B never engaged R-A's structural argument, and the argument is decisive by the cascade, not by bytes: every visible glyph lives inside `<code>`, and Tailwind preflight rules `code,kbd,samp,pre{font-family:var(--default-mono-font-family,…)}` **directly on the element**. A direct declaration on `<code>` beats ANY inherited font-family from `<pre>` — no emission reorder between `.text-small` and `.font-mono` (both on `<pre>`) can change the rendered face. R-B's byte-offset re-measure answers a question that does not bind (and rides a possibly stale witness — see residue #8). Surviving residue, INFO: `font-mono` in `preClass` is dead weight; **one genuine footnote from this seat**: text that lands OUTSIDE `<code>` (R-1's sibling branch only) inherits `<pre>`'s face and would render proportional — the fragility is real precisely and only where R-1 is live. |
| **D-M4 rung** (text-body override) | MAJOR (inversion half confirmed; √φ half killed) | MINOR (re-base on the inversion alone) | **MINOR — R-B wins; sharpened by this seat** | Inversion needs ≥2085.9px CSS width (re-derived from shipped tokens) and peaks at 22px vs 20.35px — a ~1.6px inversion, color-rank (`text-muted-foreground`) intact. This seat's sharpening, which neither reader framed: the override compresses the title:description ratio at ORDINARY widths too — at 1440px the description renders ~18.6px against the 20.35px title (ratio ≈1.09, vs the shipped default's 1.45) — the hierarchy defect is viewport-independent even where the inversion is not. Still MINOR: rank survives on color + position. R-A's MAJOR preserved in DISSENT. |
| **D-M3 rung** (border-border perimeter) | MINOR ("file the measurement, not the ruling") | INFO ("cannot violate a ruling that has not shipped") | **MINOR — R-A wins** | The measurement stands on its own legs without the unshipped ruling: ~1.87:1 over opaque `--card` (both readers' recompute), direction robust to the anchor, on a control whose perimeter is doing real work because the SAME element also strips its focus ring (R-6) — the box's only remaining boundary signals are sub-3:1 ink and a half-muted fill. The shipped cure exists today (`--control-surface-border` at 1.5px in `dist/components/_shared/field-control.css`). The authority half stays killed (K-2). |
| **S-9 site count** (L-5 "3 sites" vs C-2 "1 site") | 3 — "the divergence is a system defect a per-site fix cannot close" | 1 — the other two surfaces carry hljs highlighting a native textarea cannot hold | **1 site for the SWAP row (C/R-B win); the 3-surface divergence is its own row (R-25) — R-A's substance preserved, at the right identity** | Verified by this seat: KeyframesAddDialog.vue:37 (`hljs css` + `useCodeHighlight` + per-keystroke `highlightAll`) and KeyframeCard.vue:45 (`focus-ring hljs css`) both buy syntax highlighting with the contenteditable cost; CSSPasteDialog pays the cost and buys **nothing** (no hljs class, no highlight composable, no keydown — all 80 lines re-read). A row's scope is part of its claim (the ColorSpaceSelector L-9.1 law): the swap row books at 1; the "one widget, three looks / three a11y postures / triplicated innerText-mirror" facility defect books separately at demo scope. |

**Cure-level ruling inside an agreed verdict**: R-3 (D-B2 merge-lie) — both readers confirm; R-A filed BLOCKER-weight, R-B MAJOR. **MAJOR**: the destruction is reversible — `useRefHistory(state, { capacity: 50, debounce 100ms })` (verified by R-A at useTimeline.ts:82-88) means Cmd+Z recovers the replaced timeline, and the timeline binds + surfaces Mod+Z discoverably (KeyframeTimeline.vue:117-140). A silent, misdescribed, but recoverable destruction is MAJOR. The success toast reporting the NEW count ("Imported N keyframes") as pseudo-confirmation of an "Add" is part of the row.

## Adjudicated defect roster — 27 rows

Dispositions are **ADJUDICATED**, not VERIFIED — X·KF specs are forming; these routings are agglomeration input (M-25). Wave targets from `CENSUS-2026-08-03.md:179-206`. Line anchors are keyframes.js `8281638c` working tree; re-resolve before any cure (D-19).

### MAJOR (10)

- **R-1 — MAJOR (escalatable) · contenteditable ↔ VDOM contention** (L-1 / C-3 / D-B1). The `<pre contenteditable>` whose text is also `{{ text }}` (:13-18) puts the browser and VDOM in contention; `onInput` mirrors `innerText` into state (:71-73). Proven core: the shipped empty-`<code>` default maximises the sibling-insertion branch, and a browser-inserted sibling is invisible to Vue forever (conclusion survives K-6's mechanism correction) while its text is simultaneously mirrored — compounding visible duplication; Enter and rich paste genuinely restructure. NOT proven: destruction on every ordinary keystroke (K-5's value-identity third case). Escalates to BLOCKER on SS-13 probe #1. **→ KF.W6** (the S-9 Textarea swap deletes the defect class — `value` channel, VDOM never touches the caret's node; L's I-4 jsdom-untestability argument corroborates the swap).
- **R-2 — MAJOR (escalatable) · no scroll owner, no height bound** (C-1 / D-B3 / L-2). `DialogContent` mounted without `scroll` (:3-10; shipped default `!1`, verified), centred base class carries no height bound (verified), `preClass` has no max-h/overflow (:54), no demo stylesheet rescues. A tall paste pushes the footer out of pointer reach with nothing to scroll; every escape route destroys the paste (compound with R-4). L-2's enumeration adopted: `scroll` absent on all three demo DialogContent mounts. **→ KF.W6** (`scroll` + a bounded, overflow-owning field — the Textarea recipe carries `overflow: auto` natively).
- **R-3 — MAJOR · "Add" promises merge, performs replace** (D-B2; L's I-7 hand-off). `description="Paste CSS @keyframes to merge into the timeline"` + `button-label="Add"` + FilePlus2 (verified at the mount) → `doAddCSS` → `importCSS` → `state.value.keyframes = imported` (verified :152) — whole-array replace; `importCSSToTimeline` never reads existing state. Mitigation (undo, capacity 50) adjudicates the rung, not the row. Surfaced through this component's props; the code fix lands in the consumer/composable. **→ KF.W7** (timeline import semantics: either implement merge or tell the truth; the copy rides the same commit).
- **R-4 — MAJOR · submit has no acknowledgement channel; failed parse destroys the paste** (L-3 / C-4). `(e:"submit", text:string): void` (:61-63); open-watch wipes text on reopen (:65-69 with `initialText:""`); consumer fires un-awaited `importCSS` and closes regardless of parse outcome (precision correction: the close sits inside `if (text.trim())` — conditional on non-empty text, unconditional on parse result); `importCSS` consumes every failure into a toast (:157). A sixth failure posture for the lane-library §7.5 registry (cite verified). **→ KF.W7** for the component/consumer contract (busy/error/awaitable-close or `v-model:text` hoisting — the twin's shape), cross-referenced to **KF.W2**'s posture registry so the façade counts it.
- **R-5 — MAJOR · S-9: bespoke shadow of shipped `Textarea`, census-invisible** (D-M1 / L-5 / C-2). `Textarea` is real, exported on `/forms` (the subpath the sole consumer already imports — KeyframeTimeline.vue imports `Input` from `@mkbabb/glass-ui/forms`, verified), zero mounts in demo; nine ruled field behaviours forfeited (D-M1's table verified by both readers against `field-control.css`). Census row 80 classified `G` on import-list evidence alone (lane-frontend.md:204 verified) — S-9 is a corpus-minted EXTENSION of S-1..S-8, sized at **1 site** (ruled above). **→ KF.W6** (suffusion; and the census's S-table gains the row).
- **R-6 — MAJOR · no accessible name, no role, no focus indicator — against the repo's own precedent** (D-M2 + C-2 sub-row + both readers' convergent missed rows, merged). `:54` ships `outline-none` with nothing put back; :13-18 carries none of role/aria-multiline/aria-label; the dialog's only un-ringed focusable (the shipped close X carries `focus-ring` — verified in the chunk). The kill-shot evidence is in-repo: KeyframeCard.vue:45-49 applies `focus-ring` + `role="textbox"` + `aria-multiline="true"` + `:aria-label` to the IDENTICAL idiom (verified verbatim), and design-idioms.css:73-79 declares `.focus-ring` "the SINGLE keyboard-focus affordance" (verified verbatim). No "no established pattern" defence survives; the non-swap fallback cure is four attributes + one class. **→ KF.W6** (swap = labelled control by construction; else the four attributes).
- **R-7 — MAJOR · near-twin duplication with KeyframesAddDialog** (L-4, extended). Byte-identical `@interact-outside` guard (:4-9 vs :17-22 — verified side-by-side), same `onInput` body, same flush `<pre…><code>{{ text }}</code></pre>` shape, `footer-extra` slot (:20) shaped exactly for the twin's `progressBarEl` (:48-51), and two OPPOSITE state-ownership contracts for one widget (internal ref + submit vs `props.text` + `update:text`). Fold direction constraints recorded: adopt THIS file's a11y composition (siblings under DialogContent — the twin nests DialogDescription inside DialogTitle, :24-31 verified) and the twin's text-hoisting contract; the fold experiment owns L-4's open falsifier (residue #9). **→ KF.W7**.
- **R-8 — MAJOR · `text-small` bypasses `--control-text`** (D-M5). Reads the raw type token; on a coarse pointer every sibling control scales 1.5× (`--ui-scale` under `pointer: coarse`, verified) while the code box stays at the clamp floor. Demo does not neutralise (both readers' grep; trusted on convergence). **→ KF.W6** (swap: `field-control` fonts ride `--control-text`).
- **R-9 — MAJOR · iOS: 14px text in a text-entry surface, below the repo's own tested 16px no-zoom floor** (R-B missed, adopted). `clampIOSNoZoomFontSize = Math.max(fontSize, 16)` exists (iosTextEntry.ts:10-11, verified), is consumed by this component's OWN consumer's other editor (CSSCodeEditor.vue:39/:137, verified) and unit-tested asserting 14→16 (ios-text-entry.test.ts:88-97, verified). `text-small` at a 390px viewport = the 14px clamp floor; the R-8 bypass is exactly why the coarse-pointer 1.5× never rescues it. A first-class ruled repo behaviour, absent here. Zoom itself = residue #7. **→ KF.W6** cure; **KF.W9** (Safari visual audit) owns the live witness.
- **R-26 — MAJOR · the dialog keeps the PROSE measure for a source-code surface** (R-A missed, adopted). `max-w-lg` (512px − 2×24px pad − p-3) ≈ 440px text column ≈ ~52 monospace columns at the 14px floor; `white-space: pre` never overridden; a single multi-value `transform:` line exceeds the measure; the `<pre>` is a grid item with `min-width:auto` so the track overflows the plate (R-B's independent confirmation adopted). Horizontal overflow is the DEFAULT case for real `@keyframes` bodies, not the edge case all three axes filed. One-token widening (`class="sm:max-w-2xl"` — DialogContent accepts `class`) plus an overflow owner. **→ KF.W6**.

### MINOR (17)

- **R-10 — MINOR · `p-3` / `gap-2` / `min-h-[20vh]` are `--ui-scale`-blind; vh sizes a text box where the system rules line-height** (D-M7). System recipes verified by both readers against dist. `min-h-[20vh]` is ALSO a named T-tranche carry — identity fold: L-12 (17-styles-idioms.md:345 + T.D.md:606), never re-booked. **→ KF.W6**.
- **R-11 — MINOR · `text-body` on the description: hierarchy compression at all widths, inversion ≥2086px** (D-M4, re-based per the ruling above; √φ half dead — K-1). **→ KF.W6**.
- **R-12 — MINOR · perimeter ink measures sub-3:1** (D-M3, re-based per the ruling above; authority half dead — K-2). Cure available in the shipped dist (`--control-surface-border`); composited figure = residue #5. **→ KF.W6**.
- **R-13 — MINOR · no DialogHeader: four peer grid children at uniform gap-4; responsive header alignment forfeited** (D-M6 / C-7 — C's MINOR adopted by both readers; presentational, a11y wiring unaffected). **→ KF.W6**.
- **R-14 — MINOR · `rounded-lg` (10px) is the button rung; the multi-line-field rung is `--radius-field` (16px)** (D-m1, dist tokens verified by readers). **→ KF.W6** (swap cures).
- **R-15 — MINOR · `text-subheading` / `text-muted-foreground` re-passed to primitives that already apply them** (D-m2 / C-6 — C's `text-sm` baseline is the accurate one; both readers' independent dedup re-derivations agree the duplicates are deterministic no-ops). **→ KF.W6**.
- **R-16 — MINOR · `icon-md` namespace collision (demo 20px vs glass `--icon-md` 16px) and the only 20px button icon in the instrument** (D-m4). **→ KF.W6** (the W6 token-namespace audit names exactly this class of collision).
- **R-17 — MINOR · whitespace-only submit is a silent dead click; no placeholder/empty/pending/disabled state** (D-m5 / C-8). `if (text.trim())` with no else (verified); Button never disabled; contenteditable has no `::placeholder` and none is faked. **→ KF.W7**.
- **R-18 — MINOR · no Cancel paired with a destructive primary; DialogFooter's ≥2-child machinery inert** (D-m6; `DialogClose` is exported). **→ KF.W6**.
- **R-19 — MINOR · three of seven configuration surfaces dead (`initialText`, `preClass`, `footer-extra`) + `defineExpose({ textEl })` publishes an internal DOM handle, structurally null while closed** (D-m7 / L-6 / L-7 / C-5). `initialText` is the poignant one — it is exactly the channel R-4 needed, minted and never wired. R-A's correction to D-m7's spring-chain adopted (the unmount-on-close conclusion right, the `springPreset` gating chain wrong). **→ KF.W7**.
- **R-20 — MINOR · no `spellcheck="false"` / autocorrect/autocapitalize suppression / `dir` on a source-code region** (D-m8; iOS half live today, RTL half latent). **→ KF.W6** (swap: attrs on Textarea).
- **R-21 — MINOR · `preClass` replaces rather than merges; the house `cn()` idiom exported from the barrel this file already imports** (L-8). **→ KF.W7** (dies in the fold or the swap).
- **R-22 — MINOR · unguarded `EventTarget → Element` casts, charged asymmetrically** (L-10 + R-B's parity miss, merged). toastGuard.ts:26-28 charged by L; the identical cast sits at CSSPasteDialog.vue:72 (`e.target as HTMLElement`).innerText, uncharged — either both are MINOR or neither; both booked here at MINOR (reka dispatches on `detail.originalEvent.target`, an Element, for every path in play — L's own falsifier holds). **→ KF.W7**.
- **R-23 — MINOR · no keyboard submit path** (R-A missed, adopted). No `@keydown` in all 80 lines; Enter is a newline in the host; no Mod+Enter — while the repo treats shortcuts as first-class (KeyboardShortcutsModal; Mod+Z/Mod+Shift+Z bound and surfaced). A keyboard user must Tab out of a multi-line editor to commit. **→ KF.W7**.
- **R-24 — MINOR · `bg-muted/50` is ~2.8× the system's READ-ONLY muting, on an editable surface** (R-A missed, adopted). The system reserves the `--muted` mix (18%) for `[data-state="readonly"]`; the editable well is `--input-on-glass`. Affordance semantics inverted, not merely off-token. **→ KF.W6**.
- **R-25 — MINOR · one widget, three looks, three a11y postures, three copies of the innerText-mirror** (the L-vs-C scope remainder + R-B's triplication miss, merged; ruled above). Three `<pre contenteditable>` surfaces ship three visual recipes in two unit families (20vh / 25vh / min-h-32; filled-and-bordered vs transparent-borderless ×2), three a11y postures (full / none / none), and the `innerText`-mirror idiom is TRIPLICATED (KeyframeCard.vue:43 is the third copy — verified) — any fold that touches only the two dialogs leaves the third. Demo-scope facility row. **→ KF.W6**.
- **R-27 — MINOR (escalatable) · opening focus lands (statically) on the un-ringed empty editing host** (R-A missed-3, adopted; R-B's contrary row killed — K-9). Slot-before-close verified by this seat in the shipped chunk; title/description not focusable; a contenteditable host is tabbable; no `@open-auto-focus` handler, no autofocus management anywhere in the file. Consequences: the shipped primary flow (open → Cmd+V, no click) is aimed at exactly R-1's sibling-insertion trigger, and the dialog's opening focus is invisible (R-6). Landing = residue #3; a positive landing witness promotes this and feeds R-1's escalation. **→ KF.W6** (focus-ring + swap) with the probe in SS-13.

### INFO / fold-by-reference (identity guard — never re-booked)

- **F-1 phantom dependency** (C-11 / L-I1 / D fold-forward): census row `lane-frontend.md:15` **RED**, already ruled "F-1 first" (:612). All six of this file's component imports ride it. Fold by reference → the census's own F-1 row (KF.W0/W6 precondition). Not re-booked here.
- **L-12 `min-h-[20vh]` carry**: T-tranche carry (named at 17-styles-idioms.md:345, T.D.md:606) — identity preserved inside R-10.
- **C-9 root-barrel import**: the demo's standing pattern (lane-frontend.md:105 verified — Dialog* drawn from the root barrel demo-wide); L-11's weaker reading noted, its alias sub-claim killed (K-7). Non-defect.
- **C-10 zero test coverage** + L's I-4 (vitest registers jsdom projects with no `@vitejs/plugin-vue`; the SFC is untestable as-is — itself an argument for the swap). **→ KF.W8** (test-tree isomorphism).
- **C-4 containment**: the value.js R1 throw class is CONTAINED on this path (every throwing call awaited inside `importCSS`'s try; the R1 crash surface at lane-library.md:243 is NOT on this path). Cleared — do not re-file.
- **D-i1**: `--kf-*` flat-namespace hazard ABSENT from the demo entirely (0 hits) — the icon-* family (R-16) is the collision that does bite.
- **L-9 residue**: `font-mono` in `preClass` is dead weight (preflight already rules pre/code mono) — see the ruled kill; INFO only.
- **DialogFooter `sm:`-only gap** (R-B missed-6, adopted as INFO): confirmed in the shipped dist; already cured in glass-ui source HEAD but UNRELEASED — the relay is live, not discharged. **GLASS-OWNED** — rides the standing glass-ui BH relay (owner edict), discharging when keyframes.js consumes the release. No demo edit.
- **Corpus-integrity row (MAJOR, process — NO-WAVE-OWNER, for SS-1/SS-2 spec authoring)**: challenge-D's evidence root (`challenge-D-design.md:7`, read by this seat: "verified 7.0.0 both sides") matched version STRINGS, not artifacts — the glass-ui source tree was weeks of unpublished work ahead of the consumed 7.0.0 dist. Roughly a third of D's file:line provenance does not resolve in the artifact under audit (both readers convergent; R-B adds the dating: 7.0.0 cut 2026-07-16; `--ink-perimeter` landed 2026-08-04). Consequence for spec authoring: every D citation into `glass-ui/src/**` must be re-derived against `node_modules` before any wave acts on it; L and C, which read the dist, are the durable records on every overlapping row. Companion caveat: the built witness `dist/gh-pages/assets/index-CL_QYCiO.css` is dated Jul 16 — possibly pre-glass-7 — so every byte-offset receipt in the corpus is provisional until a fresh `build:gh-pages` (residue #8).

## Killed-claims register — 10 rows

- **K-1 · D-M4's √φ centrepiece + its evidence table** (both readers; re-verified by this seat: the shipped DialogDescription carries `"text-sm text-muted-foreground"`, `"text-small …"` = 0 hits in the chunk). The shipped system pairing is 20.352/14 ≈ 1.454 — not √φ; every cell of the "desc @ text-small (system)" column is fabricated against the consumed artifact. The inversion arithmetic survives and is re-booked as R-11. Corollary adopted (R-B): against the shipped package, `text-body` arguably moves the description ONTO the fluid ladder rather than off it — the defect is hierarchy compression, not ladder violation.
- **K-2 · D-M3's authority** (both readers; re-verified: `ink-perimeter` = 0 hits in the shipped `color-radius.css`). A component cannot violate a ruling that has not shipped in the package it consumes; D also published two inconsistent figures (1.28:1 / 1.87:1) without adjudicating. The measurement half survives as R-12.
- **K-3 · L-9 whole + D-m3's fragility clause** (R-A's kill adopted; R-B's MINOR confirmation OVERRULED — ruled above). Structurally impossible via the cascade: preflight's direct element rule on `<code>` beats any inherited face from `<pre>`; no reorder binds. Survives as INFO dead-weight + the sibling-branch footnote.
- **K-4 · C-3's corroboration clause** (R-A; re-verified by this seat in source). `collapseToEnd()` sits inside `reformat()` (:100-107, the Shift+Alt+F path), NOT in `onInput` (:96-98) and NOT in `onKeyDown` (:109-123, which calls `highlightAll()` per keystroke and repairs nothing). The twin is equally unrepaired on the per-input seam; the repo does NOT "already carry the repair idiom" there; C-3's differential framing ("unlike its sibling") dies while its mechanism row lives inside R-1.
- **K-5 · L-1's stated certainty ("no third case"; caret destroyed on every keystroke) + C-3's "Proven: destroyed and recreated on every keystroke" + D-B1's unconditional remove-and-insert** (R-B's value-identity third case adopted; R-A's engine-heuristic point adopted). None of the three established `code.textContent !== pre.innerText` at write time for the ordinary shape; the DOM-level write is value-identical there and engine behaviour on a value-identical `textContent` write is not DOM-mandated. The disjunction survives on the divergent shapes only; the BLOCKER rung fell with the universal (ruled above).
- **K-6 · L-1's runtime patch-path mechanism ("Neither arm runs; only the class prop is patched")** (R-B). The dialog body is a FORWARDED slot; `renderSlot` tags the wrapper Fragment BAIL for non-stable slots; `patch()` nulls `dynamicChildren` and clears `optimized` → the `<pre>`'s full-diff arm DOES run. Taken literally, L-1's mechanism would also imply `<code>`'s text never updates — contradicting its own first limb. The conclusion (vnode-array diff never enumerates real DOM children; a browser-inserted sibling is invisible) survives intact inside R-1.
- **K-7 · L-11's alias sub-claim** ("the `./dialog` subpath does not avoid the demo↔glass-ui cycle that vite.config.ts:25-42 aliases shut") (R-B). No glass-ui alias exists; the one alias (`@mkbabb/keyframes.js` → engine) dedupes the ENGINE for glass-ui's bare import — a different problem. C-9's read of the seam is the record.
- **K-8 · L-5's "3 sites" sizing for S-9** (ruled above; C-2's 1-site adopted; the divergence substance re-homed at R-25 rather than lost).
- **K-9 · R-B's missed-4 mechanism ("DialogContent renders RekaDialogClose BEFORE the slot … focus lands on the close X"), killed by THIS SEAT** — falsified against the very dist chunk the row claims to have verified: the shipped render order is slot-first, close-after (verbatim excerpt in the header). A re-reader row, not an axis row — recorded because it is the corpus's second source-over-dist error, committed by the reader that convicted axis-D of the first. Its residual observation (the component installs no focus management) is true and lives inside R-27.
- **K-10 · D's Evidence-Roots premise ("version-matched … verified 7.0.0 both sides")** (both readers). Version strings are not artifacts. Consequences booked in the corpus-integrity row; D's citations were honest against its tree (R-B spot-verified them at the historical commit) — perishable, not fabricated. L and C's dist-first method is the durable one.

## Superlatives — 7, with provenance

1. **Whitespace-tight authoring of the editing host** (R-A, missed by all three axes; verified in this seat's full read). The five-line `<pre>` opening tag closes flush against `<code>` — under `white-space: pre` any seam whitespace would render as a phantom blank line/indent on every open. A house idiom correctly applied at all three sibling sites.
2. **Zero motion authorship, with a documented reason to know better** (D-S1, confirmed by both readers). No transition/animation/rAF anywhere in 80 lines; every frame belongs to PRM-bracketed DialogContent. The tree itself records the sibling's deleted hand-rolled transition ("a near-exact re-author MISSING the PRM guard", KeyframeTimeline.vue:89-95). This file never made the mistake.
3. **The `@interact-outside` toast guard survived three independent attacks and two hostile re-reads** (D-S2 / L-SUP-2 / C-SUP-1; the reka seam re-walked by R-B down to `handleAndDispatchCustomEvent`). Single-sourced (`[data-sonner-toaster]` at exactly one selector site), 17-line docblock naming the private attribute, the pinned dependency, and the adoption path. Textbook discharge of D-tranche brittleness B3 / U lane-20 F-5.
4. **The a11y composition beats its own twin** (C-SUP-2; both readers re-verified; this seat re-verified both files). DialogTitle and DialogDescription as SIBLINGS — what reka's labelledby/describedby wiring wants; the twin nests the description INSIDE the title (:24-31), folding it into the accessible name. Binding constraint on the R-7 fold: adopt the 80-line member's structure.
5. **Correct export posture — contained speculative generality** (C-SUP-4 / L-SUP-1). The timeline barrel publishes only the lazily-loaded KeyframeTimeline + the type; CSSPasteDialog is reachable only by relative import. Three dead configuration surfaces stayed PRIVATE. The U-tranche module census reached KEEP independently (pass1-research-demo-module-census.md:78).
6. **The parameterised shell is the right de-duplication, and D said so against its own BLOCKER** (D-S3). Two dialogs differing in prose and glyph share one shell; `defineModel<boolean>("open", { required: true })`; the open-watch re-seat is NECESSARY (portal content unmounts, instance persists). "The shell is not the defect; the defect is the copy poured through it" — the best adjudication sentence in the corpus, and it is right.
7. **The corpus published its own failed attacks** (D §5 Killed Claims — six self-falsified hypotheses; L's I-6 reversal on the Tab handler, correctly refusing to demand a keyboard trap; C's checked-and-cleared R1 containment). Both re-readers then killed more of each other and of themselves — R-A killing its own axis-mate's certainty, R-B convicting D of source-drift and then committing it (K-9). The record's trustworthiness comes precisely from this visible mortality.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **The first-paste branch into the empty host** — inside `<code>` (clean) or beside it (whole block rendered twice while `text.value` stays correct). **The sharpest single probe for this component and R-1's escalation decider**: open Import, do NOT click, Cmd+V; read `pre.childNodes.length` and the rendered text. Also the R-27 compound: repeat WITH a click first.
2. **Caret behaviour on ordinary typing** — type `abc` one character at a time; `pre.childElementCount === 1` + anchor inside `<code>` + in-order text kills the every-keystroke half for that engine (the K-5 third case predicts exactly this on Chromium); per-engine matrix (chromium / webkit / safari-app are separate cells).
3. **Which element receives focus on open** (R-27) — statically the `<pre>` (slot-first, verified); reka FocusScope's actual landing + whether a bare insertion caret reads as focus on that surface.
4. **Overflow thresholds** (R-2/R-26) — lines-until-footer-exits at given window heights; whether the close X is symmetrically unreachable; the horizontal paint of a long single declaration.
5. **Composited perimeter contrast over the live glass plate** (R-12) — both static anchors (<1.87:1) fall short of 3:1 so direction is robust; the figure of record is live-only.
6. **The ≥2086px title/description inversion's perceptual read** (R-11) — arithmetic proven from shipped tokens; severity perceptual.
7. **iOS focus-zoom on the 14px surface** (R-9) — mechanism and the repo's own 16px rule proven; the zoom needs real iOS Safari (KF.W9's cell).
8. **Witness refresh** — re-run `npm run build:gh-pages` before trusting ANY byte-offset receipt in this corpus (the Jul-16 witness may predate glass-7 adoption); one computed-style read on the `<pre>` supersedes the whole byte-offset debate.
9. *(Not an SS-13 input — a build experiment)*: whether KeyframesAddDialog folds onto this shell across the `footer-extra` seam without losing `<pre>` identity for `useCodeHighlight` (L-4's open falsifier). Owned by the R-7 fold in KF.W7.

## DISSENT — preserved, not ruled away

1. **R-A's BLOCKER positions** (R-1, R-2): preserved. If SS-13 probe #1 witnesses sibling-branch duplication on the shipped primary flow, R-1 re-promotes and R-A's rung is vindicated; probe #4 likewise for R-2. The demotions are burden-of-proof rulings, not mechanism refutations.
2. **R-A's MAJOR on D-M4's inversion**: preserved against this seat's MINOR; the compression sharpening partially concedes R-A's instinct that the defect is not confined to >2086px.
3. **R-B's MINOR confirmation of L-9**: overruled (K-3) but recorded — R-B's byte-offset re-measurement was honest work answering a non-binding question; its staleness caveat about the witness (residue #8) is adopted even as its row falls.
4. **Reader self-error, recorded as fleet evidence**: K-9 (R-B's dist-order misread while convicting D of the same class) joins the ColorSpaceSelector record's citation-inheritance event as evidence that verification claims ("verified in the dist") require artifact excerpts, not assertions. Spec authoring should require a verbatim excerpt beside every "verified" cell.

## Closing verdict

**ADJUDICATED — 27 confirmed (0 BLOCKER · 10 MAJOR · 17 MINOR, both axis BLOCKERs demoted to MAJOR-with-escalation on SS-13 probes #1/#4) · 10 killed · 7 missed rows folded in · component STAYS as the shell; the cure spine is the S-9 Textarea swap (KF.W6, kills 7 rows in one move) + the twin fold (KF.W7, structure constraints pinned) + the merge-lie truth-telling (KF.W7) + the failure-posture row joining KF.W2's registry; F-1/L-12/R1-containment folded by reference, never re-booked; one GLASS-OWNED relay row; one NO-WAVE-OWNER corpus-integrity row for SS-1/SS-2 to consume.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/timeline/CSSPasteDialog.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **CSSPasteDialog.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-13's corpus-integrity row is DISCHARGED IN ITS OWN TERMS.** Its companion caveat — *"the built witness `dist/gh-pages/assets/index-CL_QYCiO.css` is dated Jul 16 — possibly pre-glass-7 — so every byte-offset receipt in the corpus is provisional until a fresh build"* — is answered: **one** `npm run gh-pages` ran at this seat on 2026-09-17 13:37 (R-11: the banked command name `build:gh-pages` exists at no coordinate; the real script is `package.json:43`). The Jul-16 artifact is gone; the receipts now ride `index-CBB2Hr7m.css`, 571 192 B, with the build hash banked at `artefacts/W0/glass-citation-rederivation.txt` §5.

**The `--ink-perimeter` dating is CONFIRMED BY MEASUREMENT against the consumed artifact**: ⟨`grep -rl -- '--ink-perimeter' node_modules/@mkbabb/glass-ui/dist`⟩ → **0 files**. It landed 2026-08-04, after the 7.0.0 cut of 2026-07-16, exactly as this record's re-readers dated it. **A version string is not an artifact** — the law this row states, now with its own receipt.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

