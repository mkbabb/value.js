claude-fable-5

# kf-AnimatedText — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ `8281638c` (2026-07-28) with a **CHURNING working tree — 252 dirty rows**. `AnimatedText.vue` itself (126 lines, `demo/components/instrument/shell/`) is **CLEAN** (this seat's scoped `git status`), as are `EditorStartScreen.vue`, `usability.mjs`, `font-roles.json`, `style.css`, `segment.ts`; **`split-text.ts`, `stagger.ts`, `TypingDots.vue`, `package.json`, `package-lock.json` DIRTY** — every load-bearing anchor in those files was re-derived at today's bytes by this seat, never inherited. **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two hostile re-reader returns (reader-DU, design-and-usage · reader-LC, library-and-code) over the component's three challenge files (`audit/kf-components/AnimatedText/challenge-D-design.md` 566 lines · `challenge-L-library.md` 276 · `challenge-C-consumption.md` 398). All three challenge files were re-read by this seat at the ruling anchors; the two returns were attacked before either was adopted.
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, and every admitted miss was re-derived against the bytes with this seat's own instruments. Zero browser probes (probe parsimony; the lane's no-browser law holds — every dynamic claim routes to SS-13). Two compiler receipts were run fresh by this seat (`@vue/compiler-sfc` `compileScript` + `compileStyle` on the real file). One suspiciously-agreed cell **failed under this seat's re-derivation** (ruling 1 — the F-1 fold both readers confirmed is falsified in a third direction at HEAD). Dispositions are **ADJUDICATED, not VERIFIED**; X·KF specs are still forming — every routing below is agglomeration input (M-25), not an act.
**This seat's own instruments (all read-only + node against the working tree)**: full read of AnimatedText.vue (whole, 126); compileScript receipt (destructure kept verbatim, `_unref(cycleMs)`/`_unref(offsetMs)` vs `__props.text`, patch flag `17 /* TEXT, FULL_PROPS */`, 2 keyed fragments) + compileStyle receipt (`@keyframes charLift-abc123` renamed; the comma inside `var(--wave-cycle, 3.6s)` survives the shorthand rewriter; `.wave-char[data-v-abc123]` scope selector); split-text.ts whole at the dirty bytes (`applyA11y` = `hasAttribute("role")` + `role="img"` default; `buildLines`-only empty refusal; `el.replaceChildren` before the `isLaidOut` refusal; `revert()` unconditional `removeAttribute("aria-label")`; `fragmentClass = "kf-split"`; buildUnits emits a FLAT run with live whitespace text nodes); segment.ts:1-20/:55-80; usability.mjs:150-300 (heroReady poll `h1 .wave-char >= 2`; `.wave-word` gap clause; `glyphCount: mirrorText.replace(/\s+/g,"").length`; `charCount === glyphCount`; strict-monotone delays); font-roles.json:34-38 (`h1.hero-display .wave-char` selector contract); EditorStartScreen.vue:20-35/:60-120/:150-170 (h1 classes, `title?: string` default "Select an animation", token-ban comment, `.hero-dots { display:inline-block; white-space:nowrap }` at :160-163, BG-6 comment at :100); TypingDots.vue:10-16/:80-100 at the dirty bytes (`aria-hidden` :14 region; `respectReducedMotion: true`; glyph "." ×3); stagger.ts:96-168 at the dirty bytes (`distance * each` identity; string-ease throw); reduced-motion.ts:107/:125 (`ReducedMotionPolicy = boolean | number | undefined`); shell/index.ts (4 of 8 exported; AnimatedText absent); demo-roster.mjs:8; package.json:37/:52 (`check` = tsc-only; `demo:correctness` — reader-LC's `:47` cite drifted to `:52` under the dirty tree); ci.yml:65-80 (chromium install → gh-pages build → `demo:correctness -- --timeout=600` HARD); repo greps: `grep -rnE "splitText|split-text" demo/` → **exit 1, zero hits**; `--wave-cycle` → exactly 2 hits, both AnimatedText.vue; `AnimatedText` in demo/ → **4** hits; `liftDown` → 8 in docs/frontend-design/demo/home.md, **0** in demo/+src/; `proof:` per shell file → AnimatedText **0** / TypingDots 3 / HeroAurora 3 / EditorStartScreen 5; App.vue:50 ZWJ sequence `&#x1F642;&#x200D;&#x2194;&#xFE0F;` verbatim; split-text.test.ts:72-95 (role pin uses an EXPLICIT `setAttribute("role","heading")`; revert covers authored-none only); split-a11y-oracle.test.ts:51/:97/:173 `PRE_SPLIT = "Select an animation"`; installed glass-ui 7.0.0: semantic.css `text-display-mega { … font-weight: var(--type-weight-display); text-wrap: balance; }` verbatim, components.css `.sr-only{clip-path:inset(50%);…}` with **0 `@layer` declarations in the file**, scale.css `--type-display-mega: clamp(5.382rem, 4rem + 9vw, 11.089rem)` / `--type-display-4: clamp(3.33rem, 2.5rem + 4vw, 5.382rem)` / **`--font-display-weight: 600; --type-weight-display: var(--font-display-weight)`**, scheme-motion.css `--motion-weight: 0.618/1/0/0` + `--motion-stagger-default: 80ms`, typewriter/types.d.ts `TypoState = "normal"|"typo_injected"|"typing_past"|"noticed"|"correcting"|"resuming"` + `ngramSize`/`errorRate`/`deletingSpeed`; **the dependency decider**: `git show HEAD:package.json` → `:71 "@mkbabb/glass-ui": "6.0.0"` under **optionalDependencies**, working-tree package.json → **zero** glass-ui hits (deleted in the dirty tree, 3+/5− diff), installed `node_modules` → **7.0.0**, census SCH-1 → claims 7.0.0 EXACT devDep at its own HEAD (commits `add20b7e`/`ebb08948` exist in this repo); corpus authorities: G.md:405-412 (the X-5 hard-gate spec), r-animation-sota.md:107-111/:251-255 (the false DISCHARGED cells), U.B.md:135 (U.B5 move-to-app/ carry), lane-frontend.md:353-363 (census S-5 verbatim incl. its own "verify per-char granularity" hedge), CENSUS-2026-08-03.md:32 (SCH-1) /:137 (98 unprefixed, zero `--kf-*`) /:179-205 (the KF.W0..W10 taxonomy).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**28 adjudicated roster rows — 0 BLOCKER · 11 MAJOR · 13 MINOR · 4 INFO.** **11 killed-claims register entries, all SUSTAINED** (two of them correct cells the readers CONFIRMED). **14 distinct admitted misses across the two readers, all adjudicated real** (one found independently by both — the strongest convergence in the corpus); **plus one discovery of this seat's own that both readers missed** (ruling 1). **14 superlatives sustained.** **5 adjudicator corrections against the readers.**

MAJOR-weight survivors of record: **KF-AT-4** (the component's only live gate computes its oracle with the component's own wrong counter — a formally-discharged defect survived a dedicated browser gate because gate and subject share one defect) and the **KF-AT-6 adoption cluster** (the library primitive extracted from this component by name is consumed zero times — and every axis's prescribed migration is unexecutable as written: it violates Vue scoped CSS, trips a library `role="img"` override on the page's only `<h1>`, and REDs the live gate by selector). Realized shipping impact today is **latent at every MAJOR**: the sole consumer binds only `:text` with a static ASCII string. The defects are contract defects of the component's public knobs and of the X·KF adoption premise, not outages.

## The incredulity seat's own work — ruled disagreements and corrections

1. **The F-1 fold — BOTH readers corrected; this seat's own discovery.** Reader-DU folded L:N-1/C-12/C-16 as "census F-1" (glass-ui absent from package.json + lock); reader-LC confirmed the same cell ("`grep -n glass-ui package.json package-lock.json` → NO HITS"). Both greps are TRUE of the working tree — and **both readers missed that census SCH-1 had already FALSIFIED F-1 at its own HEAD** ("package.json:77 = `@mkbabb/glass-ui: "7.0.0"` EXACT devDep … **FALSIFIED at HEAD.** True only of the stale checkout; the cure is DOCS §B-12 reconciliation, not a dependency wave"). This seat's third instrument settles it: the truth is a **three-way churn** — HEAD `8281638c` declares `"@mkbabb/glass-ui": "6.0.0"` under **optionalDependencies** (`git show HEAD:package.json:71`), the census's W2 commits declare 7.0.0 devDep, the dirty working tree **deletes the declaration entirely** (3+/5− diff), and the installed artifact is 7.0.0. Every one of the four states disagrees with at least two others. RULING: the declaration leg is **RESCOPED and FOLDED BY REFERENCE to census F-1/SCH-1 → KF.W0** (§B-12 substrate reconciliation), never re-booked, with this seat's three-way-drift evidence attached as the sharpening input. The component-level finding that survives on its own (KF-AT-7): three cascade legs (`text-wrap: balance` · the type rungs · `.sr-only`) cross a boundary no import-graph instrument can see — the component's import graph is `vue` only.
2. **L:D-6, the nowrap cure — RULED for reader-LC's kill; reader-DU's confirmation-with-cure CORRECTED.** The mechanism is unanimous and normative (CSS Text 3 §5.1: soft wrap opportunity around every atomic inline; `.wave-word`'s min-content is one glyph; the `:88-89` comment "keeps each word unbreakable" is false as written while the template comment `:14-16` states the correct weaker form — two comments in one file disagree). The CURE the L-challenge prescribes and reader-DU endorsed ("the codebase already knows the missing declaration", `.hero-dots`' nowrap) is REJECTED per the cure-separation law (ColorSpaceSelector addendum item 2): with `overflow-wrap` unset anywhere in the chain (this seat's grep: zero hits in demo/styles/ + shell/), `white-space: nowrap` on `.wave-word` converts a graceful mid-word wrap into **horizontal overflow on the LCP node** — D's own X-2 states the correct order ("the split degrades *better* than unsplit text — it wraps instead of forcing overflow"), and the `.hero-dots` precedent is a false analogy (three fixed one-char dots vs an arbitrary word at up to 177px/glyph). Row survives at **MINOR** as a comment-truth + unenforced-stated-invariant defect (KF-AT-13); the cure decision belongs to the KF.W6 word-tier design; trigger widths → SS-13.
3. **L:D-3's "the delay index over-counts" arm — RULED for reader-LC's kill; reader-DU's cell CORRECTED (citation-inheritance).** The phrase originates in the challenge itself (challenge-L-library.md:174) and reader-DU confirmed it verbatim. At the bytes: `chars: w.split("")` yields exactly `w.length` entries, so `index += w.length` advances by precisely the number of spans emitted; `(startIndex + ci) * offsetMs` is the exact running span index, strictly monotone, uniform step — which is what usability.mjs's monotone clause measures and passes. The counter is internally CONSISTENT and only grapheme-INCORRECT — that is KF-AT-3's one charge. There is no second, independent stagger defect; the arm would send a repairer hunting an off-by-N that does not exist. NARROWING SUSTAINED (K9).
4. **D:X-3's drop — reader-DU's partial overturn SUSTAINED.** The challenge's own drop text (challenge-D-design.md:499-500, re-read) concedes "Real in principle — a line-terminal margin is not hung the way a line-terminal space is," then drops the row solely on the CENTRING ground (`text-align: start`, ragged edge). But `text-wrap: balance` does not align — it **chooses break points by comparing line widths**, and C-5 item 2 (re-read, :165) files exactly that arm: a phantom 0.25em (44.4px at mega) rides every non-final line's measured width into the very algorithm the two-tier split exists to serve. The centring arm stays dropped; the balance-bias arm REVIVES as UNPROVEN-NEEDS-LIVE → SS-13 P-9.
5. **Severity contests.** (a) KF-AT-6 non-consumption: **MAJOR sustained** — both readers adopted C's explicit dissent (BLOCKER→MAJOR, "nothing fails at runtime") against L's BLOCKER; concordant, adopted. (b) The `~2.7s` header arithmetic: reader-DU MINOR, reader-LC INFO — **INFO ruled** (a comment-figure defect in a header that is otherwise 13-for-14, no behavior consequence; corrected by any wave touching the file; reader-DU's MINOR preserved as dissent). (c) D-13 (WCAG 2.2.2): **MINOR sustained** with D's own stated calibration caveat — whether OS-level PRM discharges the "mechanism" requirement is genuinely contested. (d) All three convergent MAJORs (KF-AT-1/2/3) sustained at MAJOR-latent: the sole call site binds only `:text`.
6. **The convergence event (recorded as fleet evidence, positive direction).** Reader-DU's M-2 and reader-LC's first miss are the SAME finding — `applyA11y` tests `hasAttribute("role")` and silently stamps `role="img"` over any IMPLICIT role, including the hero `<h1>` — reached independently from opposite lenses (D-repair-table scrutiny vs library-defect hunting). This seat re-derived it at the dirty bytes: the guard, the `role = "img"` default, the docblock reasoning only about div/span→generic, and the UNTESTED hole (split-text.test.ts:72-76 pins the guard with an EXPLICIT `setAttribute("role","heading")` — exactly the case the implementation already handles). Per the ColorSpaceSelector DISSENT-1 precedent, independent convergence on a novel row is the anti-signature of citation-inheritance; it is why KF-AT-8 is booked at MAJOR with confidence.
7. **Citation-drift register (sustained against the corpus, plus one against each reader).** Reader-LC's kill of C's three drifted citations is SUSTAINED by this seat's own instruments (`grep -rn AnimatedText demo/` → **4** hits, not 3; the `.hero-dots` rule at :160-163, not :151-154). Against reader-LC itself: `package.json:47 demo:correctness` sits at **:52** at today's bytes (the dirty tree moved it — the reader's own corpus-churn warning applied to the reader). Against reader-DU: its D-3/N-3/C-5 cell asserts "`<h1>` `textContent` = `Select an animationSelectananimation` … TypingDots contributes none — aria-hidden" — **`aria-hidden` prunes the a11y tree, not `textContent`**; TypingDots renders three literal `.` glyphs, so the h1's true textContent carries a trailing `"..."`. The doubling substance survives untouched; the exact string and the mechanism attribution are corrected (the correct half — the dots are silent to AT — is the accessible-name claim, which stands).

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0..W10** per the census taxonomy (W0 substrate settle · W4 gate law · W5 D/L/C library tri-fold · W6 glass suffusion/demo incl. the S-5 slot · W8 structure & colocation · W9 Safari visual audit = the SS-13 surface · W10 fold discharge). Specs are forming; routings are agglomeration input (M-25). Line anchors are the 2026-08-07 working tree; **re-resolve before any cure** — split-text.ts/stagger.ts/TypingDots.vue/package.json are dirty and churning (D-19).

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **KF-AT-1** | D:D-7 / L:D-2 / C:C-3 | `const { offsetMs, cycleMs } = props` (:69) is NOT Vue 3.5 reactive-props-destructure — the initializer is the identifier `props`, not the macro, so both JSDoc'd public knobs (:58/:60) freeze at setup while `text` stays live | this seat's compileScript receipt: destructure kept verbatim; render emits `_unref(cycleMs)`/`_unref(offsetMs)` (identity on a number) vs `__props.text`; latent — EditorStartScreen.vue:28 binds only `:text` | **KF.W6** (the S-5 component-repair slot). KF-AT-12 is the why-it-shipped |
| **KF-AT-2** | D:D-8 / L:D-4 / C:C-4 + reader-LC miss (FULL_PROPS) | `v-bind="$attrs"` inside the char `v-for` (:37) replicates every fallthrough attribute onto N glyphs inside the `aria-hidden` subtree (`id`→N duplicates, listeners→N dead, `aria-*` unreachable) while the `.sr-only` mirror — the accessible name — receives nothing; AND imposes a permanent `17 /* TEXT, FULL_PROPS */` patch on all 17 glyph vnodes though no consumer passes one attribute | compileScript receipt (this seat): the flag present; source order :37 before :38 puts the local style LAST in mergeProps, so `animationDelay` wins — C-4's sub-claim correct | **KF.W6**. Latent; the `:51-52` justification covers only class/style |
| **KF-AT-3** | D:D-5 / L:D-3 / C:C-2 — index arm NARROWED (K9) | `w.split("")` (:82) is a UTF-16 code-unit split the library's own segmenter names as the anti-pattern (segment.ts:4-6 "shreds into mojibake"; even code-point iteration is "still better than `.split(\"\")`"); the sibling prop already ships a 5-code-unit ZWJ sequence verbatim (App.vue:50) one binding away from `text` | AnimatedText.vue:82; segment.ts re-read; App.vue:50 re-read. The "delay index over-counts" arm is KILLED (ruling 3): the counter is internally consistent per span | **KF.W6** — the grapheme decision IS the S-5 slot's work; MUST land in the same wave as KF-AT-4's oracle repair (C-15's operational law, adopted) |
| **KF-AT-4** | C:C-15 (unique to axis C) | The one live gate guarding this component computes its oracle with UTF-16 `.length` — the SAME wrong counter as the subject — so `charCount === glyphCount` holds by construction on shredded text; this is why a formally-discharged defect (r-animation-sota.md:109) survived a dedicated browser gate | usability.mjs re-read this seat: `glyphCount: mirrorText.replace(/\s+/g, "").length` + the (2c) equality + strict-monotone clause; the gate runs HARD in CI (roster :8 → `demo:correctness` → ci.yml chromium step) | **KF.W4** — the exact instance of the carried gate-authoring law G-L7d ("a gate may not re-derive its own oracle"); the counter cure (grapheme count or `fragments.length`) lands WITH KF-AT-3, same wave |
| **KF-AT-5** | D:D-3 / L:N-3 / C:C-5 | The in-file X-5 invariant (:10-13 "the naive-split 'Selectananimation' cannot recur") is falsified in every non-rendered text path: the visual layer's textContent IS `Selectananimation` (gap = `margin-inline-end`, box-model; tag-hugging guarantees zero whitespace text nodes), the h1's full textContent is the doubled mirror+visual run (+ the dots' `"..."` — this seat's correction, ruling 7) | AnimatedText.vue:10-13 vs :29-32/:33/:41-43; glass-ui components.css `.sr-only` = clipped 1×1, present in DOM; textContent arm source-certain | **KF.W6**. Clipboard + find-highlight arms → SS-13 P-2/P-11 |
| **KF-AT-6** | D:D-4 / L:D-1 / C:C-1 + C:C-11/D:C-1/L:C-1 (census S-5 correction) | `splitText` — extracted from this component by name (split-text.ts:9 "the AnimatedText.vue precedent"; segment.ts:13 "the AnimatedText X-5 lesson"), barrel-exported LIGHT, gated by a browser a11y oracle pinned to THIS component's default string — is consumed **zero** times by the demo (`grep -rE` exit 1, this seat). And census lane-frontend S-5's proposed counterpart (`TypewriterText`) is a category error: the installed d.ts is a human-typing SIMULATOR (`TypoState`/`errorRate`/`deletingSpeed` — reveals text over time) where AnimatedText LIFTS text already present; the S-5 hedge ("verify per-char granularity") is answered NO | barrel :107; tests re-read (`PRE_SPLIT = "Select an animation"`); typewriter/types.d.ts read by this seat; timeline (split-text 2026-07-03 < AnimatedText touch 07-12) adopted from convergent readers | **KF.W6** — the S-5 slot RE-HOMED from TypewriterText-swap to the kf-dogfood decision (adopt `splitText` OR rule the hero template-only, written either way), **under the KF-AT-8/-9/-10/-11 constraint set below — the migration is NOT a two-line swap and every axis's repair order is unexecutable as written**. MAJOR (C's calibration; L's BLOCKER dissent recorded) |
| **KF-AT-7** | L:N-1 / C:C-12+C-16 — **RESCOPED, folds census F-1/SCH-1** | Three cascade legs (`text-wrap: balance` via `@utility text-display-mega`; the mega/display-4 rungs, quoted figures exact to <1px; unlayered `.sr-only` beating Tailwind's layered copy) reach this component from a package whose DECLARATION is in three-way churn (ruling 1) — across a boundary invisible to every instrument in the repo (import graph = `vue` only) | this seat: semantic.css/scale.css/components.css verbatim; the `git show HEAD:package.json:71` decider | **Declaration leg → KF.W0** (census F-1/SCH-1 §B-12 reconciliation, fold-by-reference, three-way-drift evidence attached). Cascade-invisibility leg → KF.W4 (instrument law) |
| **KF-AT-8** | reader-DU M-2 ≡ reader-LC miss-1 (independent convergence, ruling 6) | `applyA11y` tests only `hasAttribute("role")` and silently stamps `role="img"` over any IMPLICIT role — `<h1>`, `<button>`, `<a href>` — so aiming `splitText` at the hero deletes the page's only h1 from the heading hierarchy (`role="img"`, WCAG 1.3.1/2.4.6, axe `page-has-heading-one`); the hole is untested (the role-pin test uses an explicit attribute) | split-text.ts at the dirty bytes: the guard + `role = "img"` default + the div/span-only docblock; split-text.test.ts:72-76 | **KF.W5** (library tri-fold pre-seeded roster row) — and a NAMED BLOCKER-of-the-migration inside KF.W6's spec: the cure (implicit-role awareness or a `role: null` opt-out) precedes any h1 adoption |
| **KF-AT-9** | reader-LC miss-2 | `splitText` mutates before it can refuse: `buildLines` does `el.replaceChildren(...)` BEFORE the `isLaidOut` check throws, and the throw propagates before the result object (whose `revert` closes over `originalHTML`) exists — a refused `by:"line"` split leaves the DOM permanently rewritten with no way back; the ResizeObserver catch comment ("keep the last good map") is falsified by the same path | split-text.ts re-read at the dirty bytes: snapshot taken, then buildLines' replaceChildren, then the refusal; the revert handle unreachable on throw | **KF.W5** — with KF-AT-8, the two library rows the demo audit surfaced in the primitive it was told to adopt |
| **KF-AT-10** | reader-LC miss-3 | A faithful `splitText` migration REDs the repo's only live gate BY SELECTOR: heroReady hard-polls `h1 .wave-char` (30×300ms then FAIL), clause (2a) enumerates `.wave-word` boxes — `splitText` emits `kf-split` fragments and NO word tier (`buildUnits` returns one flat run; `SplitBy` offers no grouping under grapheme — L:S-G's genuine library gap, sustained) | usability.mjs re-read this seat; split-text.ts `fragmentClass = "kf-split"`; ci.yml HARD | **KF.W4** (gate migration is spec'd WITH the component migration, one landing order) + KF.W6 sequencing note |
| **KF-AT-11** | reader-DU M-1 | The migration all three axes prescribe is structurally unsound under Vue scoped CSS: `splitText`'s `createElement` fragments carry no `data-v-*` attribute, so `.wave-char[data-v-hash]` cannot match them and the scope-renamed `charLift-hash` keyframe is unreachable — the prescribed repair ships a motionless, un-inline-blocked hero; second arm: `replaceChildren` on a Vue-rendered container desynchronizes the vdom | this seat's compileStyle receipt (keyframe renamed, scope selector emitted, the `var()` comma surviving the rewriter — D:X-5 extended); split-text.ts fragment construction re-read | **KF.W6 spec input** — the cure needs `:deep()`/an unscoped block/a per-fragment engine animation on an inert host, named in the spec, or the S-5 slot rules the hero template-only |

### Minors

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **KF-AT-12** | L:N-2 | No static gate can reach this file: no vue-tsc, no eslint-plugin-vue, no eslint config; `check` = plain tsc (cannot parse .vue), `lint` = depcruise scoped to src/ — the why behind every latent contract MAJOR above | **KF.W4** refinement fold (the instrument gap named in the gate law) + KF.W5 premise |
| **KF-AT-13** | L:D-6 as rescoped (ruling 2) + reader-DU M-7 | The `:88-89` comment ("inline-block keeps each word unbreakable") is false per CSS Text 3 §5.1; the stated invariant is enforced by no declaration; and under PRM the exposure survives untouched (`animation: none` only — `display:inline-block` remains, the one persisting PRM cost that can move ink). Nowrap cure REJECTED (K8) | **KF.W6** (word-tier decision); trigger widths → SS-13 P-8 |
| **KF-AT-14** | reader-DU M-3 | The `.hero-dots` seam C graded superlative is breakable: last `.wave-word` and `.hero-dots` are two ADJACENT ATOMIC INLINES — CSS Text 3 §5.1 puts a soft wrap opportunity between them that `white-space: nowrap` (interior-only) does not suppress; the "dots never wrap apart" contract (EditorStartScreen.vue:156-159) is prose, not a declaration | **KF.W6** (a wrapper nowrap spanning both, or a word-joiner); trigger → SS-13 P-8 |
| **KF-AT-15** | C:C-7 / L:D-11 | `cycleMs` reaches the cascade unvalidated (:24, no clamp/`Number.isFinite`); negative/NaN → invalid `<time>` → IACVT → `animation-name: none`, a silently dead poster the `3.6s` fallback cannot rescue (the property IS set, to garbage); contrast stagger.ts's fail-explicit throw | **KF.W6**; exact UA coercion → SS-13 P-10 |
| **KF-AT-16** | C:C-13 + C:C-14 + D:D-8-engine-pair — folds census §6.5 (sharpest instance) | Five raw motion constants (:64 `55`, :65 `3600`, :106/:110 two beziers, :109 `-0.09em`) bypass glass-ui's shipped registers (`--motion-stagger-default: 80ms`, `--motion-weight: 0.618` — verified by this seat) in the one hero whose geometry is fanatically tokenized; and the PRM guard is a hardcoded binary mirror (`animation: none`) of two NON-binary authorities (`ReducedMotionPolicy = boolean | number`; `--motion-weight`) — the un-migrated half of a pair inside one `<h1>` (TypingDots already routed engine-side) | **KF.W6** (suffusion is the wave's name) |
| **KF-AT-17** | D:D-13 | WCAG 2.2.2: infinite auto-starting motion on the LCP element, no in-content pause/stop/hide; only OS-level PRM relief. D's MINOR-with-stated-contest calibration adopted | **KF.W6** — `--motion-weight` is the ready-made app-drivable mechanism |
| **KF-AT-18** | D:D-10 + K1's extension | Empty/whitespace `text` → an `<h1>` with zero accessible name (`""` → `[]` → empty mirror; `title?: string` is public; `required: true` does not reject `""`); AND the prescribed cure does NOT close it — on the word/grapheme path `splitText` never refuses, and `applyA11y(el,"","img")` yields `role="img"` with NO name, a HARDER violation (axe `role-img-alt`) | **KF.W6** (an explicit empty-state decision); the false repair-premise is K1 |
| **KF-AT-19** | reader-DU M-5 (admitted over D's struck instruction, K3) | Find-in-page on the LCP heading is degraded: multi-word queries can only match the 1×1 clipped mirror (the dots contribute no spaces); the visible 177px poster matches no spaced query at all | **KF.W6** (same X-5/mirror decision); highlight rendering → SS-13 P-11 |
| **KF-AT-20** | reader-DU M-4 | The word gap is computed in JS and emitted as N inline styles (`:29-32`, patch flag STYLE per word, re-evaluated per render) where `.wave-word:not(:last-child) { margin-inline-end: 0.25em }` is exactly equivalent — a stylesheet concern in the render function, and the only home where the demo's own token discipline could reach it | **KF.W6** (cheapest item on the list) |
| **KF-AT-21** | C-contra-G-lane / L:C-2 | r-animation-sota.md:109's "the F26-4 demo grapheme-bug is DISCHARGED … the old raw-UTF-16 per-char split is gone" and its zero-hit grep are BOTH false at the tree (the per-char split returned under the owner's ruling; split-text/ exists, 4 files); ledger row :253 needs re-opening | **KF.W10** (doc-truth re-open; dated evidence never rewritten — an addendum row) |
| **KF-AT-22** | reader-LC miss-6 | The BG-6 blocker rationale is STALE: glass-ui 7.0.0 SHIPS `--font-display-weight` (`scale.css`, verified this seat: `--font-display-weight: 600; --type-weight-display: var(--font-display-weight)`) — style.css:48's "there is no --font-display-weight token" is false; setting it to 400 retires the weight arm of `@layer demo-typography` + the scoped belt-and-braces in one line (the tracking arm survives) | **KF.W6** (one-line suffusion retire + correct both stale comments) |
| **KF-AT-23** | reader-LC miss-8 | `splitText().revert()` unconditionally deletes a pre-existing author `aria-label` (snapshot = innerHTML only; `removeAttribute("aria-label")` unguarded, role guarded), contradicting its documented "restore … + a11y attributes"; untested (revert spec covers authored-none only) | **KF.W5** (library roster, with KF-AT-8/-9) |
| **KF-AT-24** | reader-DU M-6 + reader-LC miss-4 | The gate/contract coupling is one-way and partly unenforced: usability.mjs hard-depends on the private class strings `.wave-char`/`.wave-word` + the mirror, and font-roles.json:34-38 pins role `hero-display` to `h1.hero-display .wave-char` (DESIGN.md's "selector contract") — yet AnimatedText.vue is the ONLY shell file with **zero** `proof:` marks (0/3/3/5, this seat's count) and no runner reads font-roles; a rename to `kf-split` voids both silently | **KF.W4** (the coupling declared in-file per the house `proof:` idiom; the font-roles row either gains a runner or is folded into the gate) |

### Info

| id | merged ids | claim (adjudicated) | disposition |
|---|---|---|---|
| **KF-AT-25** | D:D-14 / L:N-4 — INFO ruled (ruling 5b; reader-DU's MINOR dissent recorded) | ":96-97 the poster holds still ~2.7s" is ~23% high — true rest = 3600 − (16×55 + 0.14×3600) = **2216ms**; the header is otherwise 13-for-14 (17 glyphs, ~0.9s sweep, 177/54px all verify) | corrected by any wave touching the file (KF.W6) |
| **KF-AT-26** | D:I-1/I-2 / L:D-10/D-11(validation half in KF-AT-15)/D-12/D-15 / C:C-8 — the INFO cluster | (a) `--wave-cycle` unprefixed (2 hits, both this file; **folds census §6.3** — 98 unprefixed/zero `--kf-*` — by reference, never re-booked; the library's own idiom is `kf-split`); (b) the `var(--wave-cycle, 3.6s)` fallback is dead code (:24 unconditional, properties inherit); (c) U.B5's move-to-app/ carry unlanded AND the file absent from its own barrel (4 of 8 exported — both re-verified); (d) three `.sr-only` mechanisms in force repo-wide (glass-ui unlayered + Tailwind layered + `.sr-only-slider`); (e) home.md cites a `liftDown` keyframe that does not exist (8 doc hits, 0 in code) | (a)(b) KF.W6 · (c) **KF.W8** (colocation settle) · (d) KF.W6 · (e) **KF.W10** |
| **KF-AT-27** | L:R-1 / D:X-1 / C:SUP-2 | The corpus's self-correction is UPHELD as a load-bearing verdict: r1's "`text-wrap: balance` never applied" was retracted after the five-hop cascade chain was found; three axes converged on the retraction from three directions; chain re-walked by this seat (style.css:3 → exports → semantic.css `text-wrap: balance`, the demo's @layer override touching only weight/tracking) | recorded; the falsifier discipline is real (see superlatives) |
| **KF-AT-28** | reader-LC's corpus-hazard row, ADOPTED as this record's evidence law | The working tree churns under the corpus's citations (252 dirty rows; split-text.ts/stagger.ts/TypingDots.vue/package.json dirty; `respectReducedMotion` at :91 working-tree vs :96 HEAD; `demo:correctness` :47→:52). Any consuming wave re-anchors every line cite before landing | pinned: this record measured the 2026-08-07 working tree over HEAD `8281638c` (D-19) |

## Killed-claims register — 11 entries, all SUSTAINED

| # | killed claim | adjudication |
|---|---|---|
| K1 | D:D-4's "splitText refuses degenerate input (`:147`) … **D-10 closed (name never empties)**" + repair order §11 step 4 | **SUSTAINED (both readers convergent; deciders re-read at the dirty bytes)** — the `:147` refusal is INSIDE `buildLines` (`by:"line"` only); the prescribed `by:"grapheme"` path never refuses: `segmentGraphemes("") → []` → `replaceChildren` empties the element → `applyA11y` sets `aria-label=""` **+ `role="img"`** — a nameless image role where today is a mere empty heading. The repair order must not execute on this premise (→ KF-AT-18) |
| K2 | D:I-3 + D:S-5's "backwards *is* doing real work … no glyph pre-jumps at t=0" | **SUSTAINED** — the 0% frame `translateY(0)` is identical to the untransformed state (`.wave-char` sets only display+animation); backwards-fill substitutes an indistinguishable value; there is no pre-jump to prevent. D's own S-7 proves the identity three paragraphs later without reconciling. L:D-14 ("a no-op token") is the correct cell |
| K3 | D:143's inline instruction: "Find-in-page is not affected — Chrome matches clip-path-hidden text. **I checked; do not file that.**" | **STRUCK AS AN INSTRUCTION** — (a) the lane's own law forbids browser tooling, so "I checked" cannot be a live probe; (b) even granted, only the invisible 1×1 mirror can match a spaced query — the visible poster never does. The suppressed finding is admitted as KF-AT-19; highlight behavior → SS-13 P-11. A do-not-file directive resting on an unlawful probe must never be inherited as settled |
| K4 | L:D-9's "zero test coverage on the LCP node" conclusion + "the G-tranche standing guard never landed (G.md:409)" | **SUSTAINED, both halves** — G.md:409's three assertions (route-reachability · inter-word-gap > 0 · unique-aria-label) are ALL implemented in usability.mjs and run HARD in CI (roster → `demo:correctness` → ci.yml; all re-read this seat). L's grep was test/-only; C-9 had already corrected it and the axes do not read each other. Survivors: zero VITEST coverage (C-9's narrow row) and KF-AT-4 (the gate cannot bite) |
| K5 | C:C-9's "the grep above is exhaustive over test/ and scripts/" | **SUSTAINED** — a third contract artifact names the component's internals outside both scopes: font-roles.json:34-38 (`h1.hero-display .wave-char`), unread by any runner (this seat's grep). The falsifier as written would have missed KF-AT-24 |
| K6 | D:X-3's DROP of the trailing-margin row | **PARTIALLY OVERTURNED (ruling 4)** — the centring arm stays dropped (`text-align` never set); the balance-line-width arm, conceded "real in principle" in the drop's own text and filed by C-5(2), revives → SS-13 P-9. D dropped a live claim by answering a different question |
| K7 | D:D-11's sr-only-rhythm half ("the poster's word rhythm and the sr-mirror's differ") | **SUSTAINED** — the mirror is a 1×1 clipped, absolutely-positioned box outside the h1's inline formatting context; it has no rendered rhythm. The poster-vs-deck comparison survives (→ SS-13 P-3) |
| K8 | L:D-6's cure: `white-space: nowrap` on `.wave-word` ("the codebase already knows the missing declaration") | **SUSTAINED (ruling 2; reader-DU's endorsement corrected)** — with `overflow-wrap` unset, nowrap converts graceful mid-word wrap into horizontal overflow on the LCP node; the `.hero-dots` precedent is a false analogy (3 fixed dots vs arbitrary words). Cure severed; defect survives as KF-AT-13 |
| K9 | L:D-3's "`index += w.length` counts code units so the delay index over-counts" (confirmed by reader-DU, inherited from the challenge) | **SUSTAINED AS NARROWING (ruling 3)** — the counter is internally consistent: spans emitted = `w.length`, delay = exact span index × step, strictly monotone (the gate's monotone clause passes). Grapheme-incorrectness is KF-AT-3's one charge; no second stagger defect exists |
| K10 | D:§9's state row: "`text` changes at runtime … restarting all N animations from phase 0" | **SUSTAINED** — the keys (`${wi}-${word.text}` / `${ci}-${ch}`, both KEYED_FRAGMENT per this seat's compile) make Vue REUSE surviving spans: a reused element's animation is not restarted, its inline delay change is a phase JUMP; only key-changed spans restart at 0. The true behavior is a torn two-population wave — worse and different from the cell a future repairer would read. Unreachable today; corrected for the record |
| K11 | C's three drifted citations (`:151-154` hero-dots · TypingDots `:99`/`:88-92` · "3 hits") | **SUSTAINED (reader-LC; re-derived this seat: rule at :160-163, 4 hits, TypingDots anchors at :91/:83-85 working-tree)** — substance survives in all three; provenance did not. Folded into KF-AT-28's re-anchor law |

## Superlatives — 14 sustained (L-18 runs both ways; provenance per row)

1. **The two-layer a11y mirror became a shipped library default with its own browser oracle** (D:S-1 · L:S-A · C:SUP-1 — all three axes independently; both readers; this seat re-walked all three files). One `.sr-only` span carrying the whole phrase (:21) + `aria-hidden` on the entire visual tier (:23) + `inheritAttrs:false` (:53), with TypingDots ALSO aria-hidden — the h1's accessible name is exactly `text`. Codified as `splitText`'s default `a11y: true` citing this file by name; gated in a real browser against the COMPUTED accessible name (`PRE_SPLIT = "Select an animation"`). Demo idiom → library default → browser oracle is a complete provenance chain — and it is precisely why KF-AT-6 has teeth.
2. **Per-keyframe `animation-timing-function`, used correctly** (:106 rise / :110 settle — an asymmetry no element-level curve can express), and **the inline per-glyph delay correctly survives the `animation` shorthand's delay-reset** (style attribute beats author-origin). L's detail adopted: the 216ms rise deliberately exceeds the 55ms step, so glyphs overlap into one travelling wave (D:S-6 · L:S-D · both readers; receipts re-run).
3. **The PRM guard rests at an identity frame** — bare `animation: none` is safe ONLY because both keyframe boundaries are `translateY(0)` and no static transform exists; zero residue, zero compensating declaration. The same identity that makes K2 correct (D:S-7).
4. **One clock, N phases** — `--wave-cycle` written once, inherited by all 17 glyphs; the only per-node value is the delay; transform-only on the LCP node whose fallback face is metric-matched so it lands in its final box from first paint (D:S-5 first clause · L:S-F; style.css:52-55 re-read).
5. **Em-relative amplitude with its work shown** (:109 `-0.09em`; ":19 the old −10px was rung-blind at 177px") — verified against the installed clamps: a 3.3× rung ladder across which any px amplitude is a heave or a twitch (D:S-4 · L:S-C · C:SUP-4).
6. **Two RTL decisions nobody would have faulted it for skipping**: `marginInlineEnd` not `margin-right`; delay = source-order global index, so the sweep runs WITH the reading direction (D:S-3).
7. **Zero teardown surface** — one `computed`, no lifecycle, no listener, no rAF, nothing to leak; the contrast (TypingDots' unmount guards) is 60 lines away. The property the prescribed migration would destroy — which is why it is a constraint on KF-AT-6, not decoration (L:S-E; reader-LC's pricing miss adopted into the KF.W6 spec input).
8. **The last-word margin conditional is a real honored cross-file seam** — `undefined`, not `0`, so no trailing declaration exists and `.hero-dots` sits flush (C:SUP-5, substance confirmed; its citation corrected in K11; its breakability filed as KF-AT-14 — both verdicts held on the same four lines).
9. **C-15 is the finding of the audit** — not "is the component wrong" but "why did a discharged defect survive a dedicated live gate": subject and oracle share one defect, and any remediation that does not fix the oracle in the same wave leaves the gate green on both behaviors. Adopted as KF-AT-3/-4's binding sequencing law (C, unique).
10. **Axis L led with its own retraction and was right to** — r1's D-7 killed in full after its own falsifier fired, independently corroborated by C (SUP-2) and D (X-1); this seat re-walked the chain. Three axes converging on one self-correction is the corpus's best evidence that the falsifier discipline is live (KF-AT-27).
11. **Axis D's contrast arithmetic is exact and labeled at its own boundary** (16.77:1 / 15.85:1 recomputed by reader-DU digit-for-digit; the one approximate row marked approximate with method stated). The standard the rest of the corpus is held to.
12. **L:S-G finds a real defect in the LIBRARY through the demo lens** — `buildUnits` emits a flat run, no word grouping, no grouping knob: under `by:"grapheme"` the primitive inherits the mid-word break exposure with no wrapper to suppress it. The migration is a MERGE, not a replacement — adopted verbatim into KF-AT-6/-10's constraint set.
13. **C-10 proves a negative properly** — the R1 crash-class exposure delta of adopting `splitText` is exactly zero: one value.js edge, allow-listed with a specifier-end lookahead, 1110-byte artifact with zero import specifiers; re-run by reader-LC, adopted (stagger identity re-verified by this seat at the dirty bytes: `distance * each`, string-ease throws).
14. **Every axis recorded its clean probes** — D's thirteen-state table including the clean rows, §8's falsified-and-dropped register ("each is an easy false-positive the next auditor will re-derive"); two of its drops re-derived and confirmed here (X-2's shrink-to-fit arithmetic; X-5 extended by reader-LC's comma-hazard check, re-run by this seat). An unrecorded clean probe is re-run forever; this corpus does not make that mistake — and where it dropped wrongly (K6), the drop's own text preserved the evidence to overturn it.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (→ KF.W9)

All mechanisms below are source-certain or normative; only magnitudes/UA behavior need a rendered viewport. One pass, one pinned commit, re-anchored lines (KF-AT-28).

- **P-1 · Kerning-loss magnitude** (D:D-1/L:D-13): no shaping run spans two atomic inlines — normative, no probe needed; whether Instrument Serif ships `kern` pairs across {Se, el, ec, an, ti, io, …} needs the remote woff2's GPOS. The team's demonstrated visible-defect threshold (the `@layer demo-typography` tracking correction) is real and small.
- **P-2 · The clipboard arm** (KF-AT-5): what select-all + copy yields over `mirror + Selectananimation + "..."` — UA selection over `clip-path:inset(50%)` content.
- **P-3 · Space-advance vs 0.25em** (D:D-11 surviving half): Instrument Serif's U+0020 advance ∈ 0.245–0.255em kills the poster/deck rhythm mismatch; outside it, the mismatch stands.
- **P-4 · Ink clearance at the wave peak** (D:D-12): em-box arithmetic exact (re-derived by reader-DU); real descender ink needs a 1440px render with a descender-bearing first line. The default copy is safe by letter choice — the sharpest part of the finding.
- **P-5 · Backdrop luminance under the h1** (D:S-2 residual): Aurora bounded (0.1 ceiling verified); the WebGL cube layer is not.
- **P-6 · Compositor cost** (D:I-4): ~17 perpetually-animating transformed inline-blocks on the LCP node; layer count + commits during the 2216ms rest window.
- **P-7 · Does `text-wrap: balance` engage over a run of atomic inlines at all?** The library and the component disagree IN PROSE about the same named lesson (segment.ts:11-13 live-whitespace answer vs AnimatedText.vue:14-16 inline-block-wrapper answer). The highest-value single probe on the component (both readers concordant); if the library is right, the word tier's stated benefit is nominal.
- **P-8 · Trigger widths** for the mid-word `.wave-word` break (KF-AT-13) and the `.hero-dots` orphan (KF-AT-14) — 320px render, `--type-display-4` floor, long-word `title`.
- **P-9 · Trailing-margin bias on balance break choice** (K6's revived arm): the chosen break vs the same string unsplit at the same width.
- **P-10 · UA behavior for `--wave-cycle: -100ms`/`NaNms`** reaching the shorthand (KF-AT-15): IACVT vs coercion — both give a dead poster; only the console signature differs.
- **P-11 · Find-in-page highlight on clip-path-clipped text** (KF-AT-19): whether Chromium paints the match, and what the user sees when the highlight lands on an invisible box.

## Identity guards (anti-rename; fold-by-reference, never re-book)

- **Census F-1/SCH-1** — KF-AT-7's declaration leg folds there (KF.W0 §B-12), carrying this seat's three-way-drift decider (`HEAD:package.json:71` = 6.0.0 optionalDependency). ONE identity.
- **Census §6.3** (98 unprefixed / zero `--kf-*`) — KF-AT-26(a) folds by reference; `--wave-cycle` is one instance, not a new row.
- **Census §6.5** (PRM conscientious-but-inconsistent, 13 sites) — KF-AT-16's PRM half is its sharpest instance, folded.
- **Census S-5** (`AnimatedText → TypewriterText`, AMBER) — CONTRADICTED and RE-HOMED, identity preserved: the S-5 slot survives in KF.W6 with its subject corrected to the splitText-dogfood decision; the census hedge is answered NO on this seat's own read of the installed d.ts.
- **G.md X-5 / T.D10 / OD-4 lineage** — referenced as the component's charter; the gate that implements G.md:409 is usability.mjs (K4); nothing here re-books X-5.
- No banked kf-* apotoheosis rows exist yet in the registry; this is the first — successor kf-* records fold against THESE ids (KF-AT-1..28).

## Closing verdict

Both hostile re-reader returns **survive the incredulity seat with corrections that sharpen rather than reverse** — and the seat earned its keep at three points the readers could not settle between themselves: the two genuinely disagreed on L:D-6's cure and L:D-3's index arm (both ruled for reader-LC's kills, reader-DU's inherited cells corrected), and **both** confirmed a fold (census F-1) that the census itself had already falsified at ITS head — the true dependency state is a three-way churn only a third instrument could see (HEAD 6.0.0 optionalDependency ≠ census 7.0.0 devDep ≠ working-tree none ≠ installed 7.0.0), now attached to KF.W0's reconciliation. All 14 distinct admitted misses are adjudicated real; the strongest (the `role="img"` implicit-role override) was found independently by both readers and is the anti-signature of citation-inheritance. The eleven kills all stand, including two that struck instructions rather than claims — a do-not-file directive resting on an unlawful probe, and a repair premise (`splitText` closes D-10) that would have shipped a harder violation under a green gate.

The component's adjudicated shape: **a small, genuinely excellent piece of craft — the a11y mirror that became a library default, an identity-frame PRM guard, one clock with N phases, em-relative amplitude with its work shown — wrapped in three latent contract MAJORs no instrument in the repo can see (frozen props, N-way `$attrs` fan-out, a UTF-16 split the library's own segmenter names as the anti-pattern), guarded by the one gate that shares its defect, and shadowed by the library primitive extracted from it that nobody consumes and that none of the three prescribed migrations could actually land** (scoped CSS, the role override, the gate selector, the missing word tier, the zero-teardown price — five constraints, zero of them named by the corpus's repair orders, all named now). Every row leaves with exactly one disposition: **KF.W0** takes the dependency reconciliation (folded), **KF.W4** the gate/instrument law rows (the oracle that cannot bite, the selector coupling, the migration's gate work, the static-instrument gap), **KF.W5** the three library-side split-text defects as pre-seeded tri-fold roster rows, **KF.W6** the component repairs + the re-homed S-5 dogfood decision under the five-constraint spec input, **KF.W8** the colocation carry, **KF.W9** the eleven SS-13 probes, **KF.W10** the two stale doc authorities. Nothing lost: head, tail, or interval.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; the X·KF specs are forming and these routings are their agglomeration input (M-25); execution awaits the owner's begin-word.
