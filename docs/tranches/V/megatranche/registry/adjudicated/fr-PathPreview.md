`claude-fable-5` — served model id, observed in this seat's own system prompt (recorded per M-23/L-18).

# PathPreview — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code), the apotheosis seat for fourier frontend component `web/src/components/ui/PathPreview.vue`.
**Date**: 2026-08-24 · **Substrate**: fourier HEAD `cd26c6533adc32dfe1453d74117d3cb73b89ea16` (re-run by this seat: `git rev-parse HEAD`), 28 dirty paths, subject and sole importer CLEAN.
**Corpus**: 3 challenge files (D: 12 defects/2 BLOCKER · C: 13/1 · L: 17/2 — **42 accusation rows against a 69-line zero-instance file**) + both re-reader returns, adjudicated against each other, neither adopted whole.
**Registry census**: 206 adjudicated records at write time; `fr-PathPreview.md` absent — this file claims the slug new. Identity guard ran against fr-GalleryCard, fr-GalleryFeaturedCarousel, fr-GalleryMarquee, fr-BasisCanvas, fr-BasisSelector, fr-ContourPreview, fr-App, fr-GalleryCardModal, fr-AnimationControls, fr-EasingCurvePreview, fr-FourierMorphDemo, fr-ConvergenceLegend, fr-CanvasOverlayButton.
**Method (L-18 both ways)**: both re-readers were attacked before either was believed. Every verdict-level conflict between them was re-derived by this seat with its own instruments; suspiciously-unanimous cells (the fabricated D-2 receipt, the getPathBounds zero-caller count, the 13-key tsconfig) were accepted only where BOTH readers reported independent execution and the banked registry corroborates.
**Arbiter's own instruments this session (all static + node, read-only; zero browser — probe parsimony)**: PathPreview.vue whole (69 lines — every load-bearing anchor re-confirmed: `:23` guard, `:29-30` `|| 1`, `:33` scale, `:43` `M…Z`, `:50-53` square viewport/viewBox, `:56` stroke divisor, `:65-68` scoped pair; single root, no `inheritAttrs: false`); GalleryView.vue `:60-70` + `:260-275` (the L-14 decider); tailwindcss installed **4.3.1**, `preflight.css:200-222` read verbatim (`img, svg, video, canvas … { display: block; vertical-align: middle; }`) + `src/style.css:1` `@import "tailwindcss"` (the S-4 decider); `git show HEAD:web/package-lock.json` → typescript **5.9.3** / vue-tsc **2.2.12** / latex-paper 0.2.1, vs installed **6.0.3 / 3.3.5**, vs the uncommitted `package.json` hunk `^5.8→^6.0.3` / `^2.0→^3.3.5` (the L-2 decider); `optionDeclarations.noUncheckedSideEffectImports.defaultValueDescription` read from three installed compilers — **true** at 6.0.3 (fourier AND value.js), **false** at 5.9.3 (atlas); challenge-file heads read for tally/register truth; registry `ls` (206).

## Scoreboard

**29 adjudicated rows · 11 killed claims · 7 missed finds adopted · 0 BLOCKER sustained.** All three axes' unanimous BLOCKER on the deadness is DOWN-GRADED: the identity was already banked MINOR, twice. Both re-readers lost a headline each to this seat's re-derivation: reader B's L-14 confirmation dies (the marquee never mounts), reader A's F.W0 routing of the typecheck RED dies (the RED is the uplift's, not the substrate's). The severity spine of the roster is **MINOR-with-a-fork**: PP-DEAD's delete-or-wire decision at F.W3/W4 is the gate through which every remount-conditional MAJOR becomes live or moot.

## Ruled disagreements (the seat's actual work)

| id | reader A (design-and-usage) | reader B (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-14 (marquee phantom-count)** | KILLED (K3): GalleryMarquee never mounts | CONFIRMED MINOR: three native loops + aria-hidden duplicate verified line-by-line | **A WINS — KILLED-as-booked** | My read of GalleryView.vue: the marquee sits at `:269` inside `v-if="!gallery.entries.length && !gallery.loading"` (`:266`) under its own guard `v-if="featuredEntries.length >= 4"`, and `featuredEntries` (`:64-66`) filters **the same** `gallery.entries` — `entries.length === 0 ⟹ featuredEntries.length === 0`; the two conditions are jointly unsatisfiable. B verified the loop anatomy inside GalleryMarquee.vue and never checked the mount site. The comment block above `:266` even documents the false intent ("a true cold-empty DB renders the CTA alone"). Banked: fr-GalleryCard.md:19/:27 (R-1, K-9) /:53 (G-LC1, MAJOR → F.W3/W4); fr-GalleryMarquee.md is the component-owned record. Surviving grain (both readers agree): GalleryFeaturedCarousel.vue:27-32 is ONE native-element loop wrapping GalleryCard — banked, folds by reference. L-14's P-2 "2× network multiplier" dies with it. |
| **L-2 (typecheck RED at HEAD)** | CONFIRMED MAJOR → F.W0, per banked M-15; killed only the provenance guess (K7) | KILLED: the RED is manufactured by the uncommitted toolchain bump | **B WINS — substrate-blocker framing KILLED, fact re-routed** | Four receipts, mine: (1) committed lock (`git show HEAD:web/package-lock.json`) resolves typescript **5.9.3** / vue-tsc **2.2.12**; (2) the uncommitted `package.json` hunk carries `typescript ^5.8→^6.0.3`, `vue-tsc ^2.0→^3.3.5` — the same hunk L-2 quoted for the latex-paper line; (3) `noUncheckedSideEffectImports` is UNSET in the 13-key tsconfig, so the compiler default governs — and that default is **false at 5.9.3, true at 6.0.3** (read from `optionDeclarations` of three installed compilers); (4) B's flag-isolation run (the identical vue-tsc invocation with the flag forced off emits **zero** diagnostics) adopted as corroboration. Therefore CI (`npm ci` from the committed lock, `ci.yml:95`) is **GREEN at HEAD-as-committed**; the sole TS2882 on committed `PaperView.vue:12` exists only inside the uplifted working tree. Reader A's own unproven list conceded exactly this question open; B closed it. See PP-REDGATE for the routing and the M-15 errata. |
| **D S-4 (`display: block` load-bearing)** | Missed find: INERT under Tailwind preflight | Endorsed D's S-4 ("neither is removable") | **A WINS — the superlative is half-dead** | My read: installed tailwindcss **4.3.1** ships `preflight.css:210-219` `img, svg, video, canvas, audio, iframe, embed, object { display: block; vertical-align: middle; }`, and `web/src/style.css:1` imports tailwindcss — every `<svg>` in the application is already blockified, no parent required, so D's falsifier ("remove it and the component gains phantom bottom space") FAILS. `flex-shrink: 0` (`:67`) survives as genuinely load-bearing — preflight sets no flex property. REGISTRY-FIRST: distinct from banked fr-EasingCurvePreview.md:96 K-9, which killed the same superlative by a COMPOSITION mechanism scoped to one callsite; this is the GLOBAL mechanism, the only one available to a zero-callsite component. |
| **PP-DEAD severity** | MINOR (identity banked at MINOR) | MAJOR (census-integrity half survives) | **SPLIT** | The deadness identity folds MINOR by reference — banked twice, both post-dating the challenges (fr-GalleryCard.md:63; fr-GalleryFeaturedCarousel.md:56 FR-GFC-22). B's MAJOR-carrying half — four lane-frontend rows describe the component as live/bespoke — is booked as its own row (PP-CENSUS) at MINOR: the registry already corrects the census in two records, so the residual formation risk is a reading-order hazard, not an open falsehood. The axes' remount severity law (challenge-D-design.md:17-22, declared honestly) is preserved as a CONDITION on the MAJOR rows, not as their present grade. |

## Adjudicated defect roster — 29 rows, every row one disposition, original axis ids for life

Dispositions are ADJUDICATED, not VERIFIED — X·F specs are forming; routings are M-25 agglomeration input. Line anchors are fourier HEAD `cd26c65`; re-resolve before any cure.

**The fork (reads first).**
1. **PP-DEAD** (D-1 · C-1 · L-1) — **MINOR · FOLD → F.W3/W4** (delete-or-complete, a design decision) **+ F.W0 `noUnusedLocals` rider**. Zero render sites; sole tree reference = the dead import `GalleryCard.vue:10`, dead since `a459a56` (2026-03-16). Both readers re-proved by grep AND compiler (TS6133 at `GalleryCard.vue(10,1)`, plus the equally-dead `VIZ_COLORS` at `(9,1)`). Identity BANKED — folds by reference to fr-GalleryCard.md:63 and fr-GalleryFeaturedCarousel.md:56; the unanimous BLOCKER is NOT sustained. Zero users harmed, zero bytes ship (banked K-13). The fork's wire branch is expensive, not cheap: `Visualization` (types.ts:207-239) carries `contour_hash` and no path arrays — wiring costs one `getContour` fetch per card in an infinite-scroll grid (PP-SEVLAW).
2. **PP-CENSUS** (L-1, census-integrity half) — **MINOR → NO-WAVE-OWNER** (M-25 errata; SS-3/SS-4 must read the registry over `lane-frontend.md:183/:366/:369/:444/:565`, which describe a dead component as a live bespoke surface — the census audited the file, not the graph).

**Remount-conditional MAJORs — live ONLY on the wire branch of PP-DEAD's fork; moot on delete.**
3. **PP-CLOSE** (D-2 · C-5 · L-5(1)) — **MAJOR → F.W3/W4**. `:43` appends `Z` unconditionally, no `closed` prop; both in-tree serializers parameterize closure (`ghost-path.ts:12` `closePath = false`; `svg-fourier.ts:47` `closed = true`). B's correction adopted: BasisCanvas renders the SAME arrays closed (`:131`) and open (`:220`) — closure is per-mode presentation, not a data property, so D-2/C-5's "the data is not always closed" framing overstates; the missing-prop verdict stands. D-2's numeric receipt is fabricated (K1); grade MAJOR, not BLOCKER.
4. **PP-A11Y** (D-3 · C-9) — **MAJOR → F.W3/W4**. Ten attributes, zero a11y marking (`:48-61`); the hard axe keystones are structurally blind (installed axe-core `svg-img-alt` scopes to `svg[role="img"]` etc. — a bare `<svg>` is out of scope), so the gate is compliant-by-omission and adding the correct role WITHOUT a name turns it RED. Tree denominator exact: 17 `<svg>` / 12 files, zero `role="img"`, zero `<title>`, one `aria-hidden` (SvgFilters.vue:64). C-9's "no way to name it" framing KILLED (K6). Per-engine AX exposure → SS-13.
5. **PP-STROKE** (D-4 · C-2 · L-10) — **MAJOR → F.W3/W4**. `:stroke-width="strokeWidth / size"` (`:56`) is cascade-fragile: `:width`/`:height` are presentation attributes any author rule outranks — and the only production caller that EVER existed did exactly that (`git show 4340ac8:…GalleryView.vue` :388-396 + `.card-path-primary { width:100%; height:100% }` :850). In-tree cure verified: `vector-effect="non-scaling-stroke"` (ContourPreview.vue:47). Four incompatible stroke laws across the preview family, verified line-for-line by both readers.
6. **PP-LEN** (L-4 · C-4) — **MAJOR → F.W3/W4 + F.W0 rider**. Independent `pathX`/`pathY`; `:23` checks non-emptiness, never length equality; the failure is ASYMMETRIC — X-longer emits literal `NaN`, Y-longer emits a syntactically perfect, silently mis-framed path (probes C1/C1b, byte-reproduced by BOTH readers independently). The sharpest row in the corpus; catchable only by a unit test (PP-NOTEST). Type-system rider banked, folds by reference: `noUncheckedIndexedAccess` absent (fr-BasisSelector.md:94 i-3; fr-ContourPreview.md:61 row 28).
7. **PP-SHAPE** (C-3) — **MAJOR → F.W3/W4**. Split-array props mirror none of the five `{x: number[]; y: number[]}` carriers the API emits (types.ts:15/:17/:25/:26/:80); one `path: {x,y} | null` prop makes PP-LEN and PP-NULL impossible by construction; `xyToPoints` (svg-fourier.ts:38) already performs the zip.
8. **PP-NULL** (reader-B missed find, adopted) — **MINOR → F.W3/W4** (folds into PP-SHAPE's cure). Absence is unrepresentable: required non-nullable props over a nullable-only real producer (`4340ac8` `getPathData(): {x,y} | null`, paid for with 3 calls + 2 `!` assertions per card); a null-valued binding produces zero Vue warnings and a raw render-time TypeError — mechanism BANKED at fr-ContourPreview.md:82 KILL-1 (`validateProps` warns only on absent KEYS), folds by reference.

**Framing/robustness cluster — mostly F.W4 via the banked contourBounds extraction.**
9. **PP-PAD** (D-6 · C-7 · L-8) — **MINOR → F.W4** (rider on fr-ContourPreview.md:73 row 40's `contourBounds(points, margin)` framing-cluster cure). `padding` is a fraction of pre-scaled content: 0.1 delivers an 8.33% margin (`p/(1+2p)`), and `:32`'s comment encourages the box reading. Three preview surfaces, three framings, two wearing the same literal 0.1. D-6's one-line cure `(1 - 2*padding)/max(range)` verified sound by B; D-6's failure-mode string killed (K10).
10. **PP-SPREAD** (D-8 · C-8 · L-11) — **MINOR → F.W3/W4**. Four variadic `Math.min/max` spreads (`:25-28`) over an API-controlled array, violating the repo's own written Invariant 20 (useViewTransform.ts:15-22/:35, verbatim); ceiling measured on the host runtime by both readers (n=125000 → RangeError); `n_points` bare/unbounded on BOTH sides of the seam (shared.py:13, computation.py:45/:50, types.ts:34). L-11's mitigation is KILLED (K2): the live canvas path already retired the spread — this is the only variadic bbox left in the frontend outside dead code, which sharpens the row.
11. **PP-GUARD1** (L-7) — **MINOR → F.W3/W4** (framing cluster). `|| 1` (`:29-30`) substitutes a synthetic unit range that COMPETES in `:33`'s `Math.max`, halving an axis-degenerate path (probes D1/D1b/D2, reproduced by both). Real teeth = the NaN-swallowing arm (PP-POISON). Same guard cited as in-tree precedent at banked fr-ContourPreview.md:50 row 17.
12. **PP-DOMAIN** (L-9 · D-11) — **MINOR → F.W3/W4**. Unvalidated numeric domains reach rendered attributes: `padding=-0.5` → `±Infinity` coordinates; `padding=-1` → a valid silently-MIRRORED path (the insidious arm); `size=0` → `stroke-width="Infinity"` on the DOM. Python contrast real but the receipt corrected: `visualization.py:76` is `le=256`, the `le=4096` sites are `:126/:187/:281` (K10; corroborated by banked fr-ConvergenceLegend.md:10).
13. **PP-POISON** (L-16 · L-15 · D-9 empty arm) — **INFO → F.W3/W4** (wire branch). NaN/Infinity swallowed twice (the range guard hides corruption; `cx` poisons every coordinate); 1- and 2-point inputs render `M…Z` where both library alternatives refuse (`svg-fourier.ts:51`, pencil-boil `path.ts:57`, identical guard); empty input renders a full-size childless box (`:23` → `:60` false, `:50-51` unconditional). D-S6's counter-reading (the guards keep this a finite dot, not `MNaN,NaN`) is equally true of the same lines — both preserved.

**Gate/substrate rows.**
14. **PP-GATE** (D-1 gate half · C-10 · L-3) — **MINOR · FOLD → F.W0** (banked: fr-GalleryFeaturedCarousel.md:56 FR-GFC-22 `noUnusedLocals` rider). 13 compilerOptions, no linter at any depth, scripts = dev/build/preview/test:e2e ×2. The 16-findings/12-files yield is now TWICE-executed (L's probe T1 + reader B's independent run, identical file list); labels.ts rows are destructured locals, not params (honest count stays 16/12). C-10's suppression-mechanism hedge KILLED by execution (K9).
15. **PP-REDGATE** (L-2, as re-adjudicated above) — **MAJOR → F.W1/W2 + M-25 errata**. The TS2882 on committed `PaperView.vue:12` is real and thrice-reproduced, but it is the UPLIFT'S: committed substrate + committed lock = GREEN gate; the uncommitted TS 6 bump flips `noUncheckedSideEffectImports` default false→true. The cure (ambient `declare module "@mkbabb/latex-paper/theme"` in env.d.ts, or a latex-paper types fix) lands WITH the toolchain bump, owned by F.W1/W2 — NOT as an F.W0 precondition. ERRATA, fold-forward, dated evidence unedited: banked fr-AnimationControls.md:69 M-15's F.W0 "substrate green before F.W1 opens" routing must be re-read against this finding at intake.
16. **PP-TSSCOPE** (reader-B missed find, adopted) — **MINOR → F.W0**. The only type gate never checks its own test suite or build config: `include` = `src/** + env.d.ts`, exactly one tsconfig in `web/`; `vite.config.ts`, `playwright.config.ts`, all 8 `e2e/*.spec.ts` sit outside `vue-tsc -b` entirely — a SCOPE gap all three axes missed while litigating the FLAGS gap. Instrument-only precedent disclosed (fr-CanvasOverlayButton.md:10 observed the glob; no banked row books the consequence).
17. **PP-NOTEST** (L-13) — **MINOR → F.W0**. No vitest/jest anywhere in `web/`; zero e2e references to PathPreview or gallery-card; probe-C1b-class defects (valid-and-wrong output) are catchable by nothing else.

**Seam rows (off-component or cross-component, honestly attributed).**
18. **PP-COLORSEAM** (C-6) — **MAJOR · FOLD by reference → F.W1** (banked fr-BasisCanvas.md:43 D-2/BC-5/C-3; re-cited fr-App.md:57, fr-BasisSelector.md:46, fr-GalleryCardModal.md:67 — and fr-GalleryCardModal.md:123 K-7 already SUSTAINED a REGISTRY-FIRST violation against re-booking this mechanism). `basis-display.ts` value-copies three reactive properties at module eval; the theme MutationObserver can never reach it. Determinism cell KILLED (K8): entry-route conditional (default `/paper` entry snapshots the post-resolve poisoned `#888888`, not the authored hex — banked fr-App.md:103 K-5). Colour radius = 6, not 7 (GalleryDraftsSection.vue:44 reads `.label` only).
19. **PP-DEADSEAM** (C-12) — **INFO → F.W5-W8**. Mutual dead capability: `preview_path` written as `""` at all three server sites (assets.py:85, image_storage.py:318, responses.py:22) while the client leaf is unreachable — zero implementation on either side. The LIVENESS predicate (beside R6-8's identity/isolation predicate) is genuinely new to the registry (both readers' sweeps + mine over 206 records) and is this row's value.
20. **PP-IMPORTSHAPE** (C-13 · D-12b) — **INFO · off-component, recorded-not-booked** → folds to fr-GalleryCard + census break surface (CENSUS-2026-08-03.md:102-105). GalleryCard.vue:3-5 mixes barrel and subpath specifiers in three adjacent lines and is a lucide-rename site; PathPreview's only import is `{ computed } from "vue"` — zero on every F.W1 break-surface row.
21. **PP-PLATE** (reader-A missed find, adopted) — **INFO · off-component correction** → folds by reference to banked fr-GalleryCard.md:51 D-6/L·D-7 (MAJOR, R-5). D-9's comparand is false: the GalleryCard grid plate is a backdrop, not a fallback — the bare `<img>` (`:98-105`) has no `@error`/`@load`/placeholder, so the "house standard, which is higher" that D-9 measured PathPreview against does not exist. Filed only as the comparand correction; the defect is GalleryCard's, already banked.

**Design/duplication rows.**
22. **PP-NOSHADOW** (D-5) — **MAJOR (design-fork input) → F.W3/W4**. No producer analogue for a path-thumbnail primitive — re-proven at BOTH defensible baselines after D-5's receipt died to baseline law (K4): installed **glass-ui 4.0.0 = 80 export keys**, `git show v7.0.0:package.json` = **74 keys**, no sparkline/path/thumbnail/polyline-fit at either; `./handmark` opened and rejected on the merits. The LOCAL shadow half folds by reference: fr-ContourPreview.md:43 row 10 rules PathPreview the SANCTIONED implementation and ContourPreview the weaker re-derivation; ContourPreview's own anisotropic-pad bug (`:26` X-derived, applied to both extents) confirmed by both readers. F.W1 rider real: `./easing` exists at v7.0.0, not at the installed 4.0.0.
23. **PP-DUP** (L-5(ii) + L-6-as-rescoped) — **MINOR → F.W3/W4**. The component hand-rolls a serializer pencil-boil exports (`pointsToLinear`, verified at installed 0.4.1 AND surviving at producer 0.12.0 — reader B closed L's open uplift falsifier) and re-derives line-for-line a bounds+fit law that exists at `transforms.ts:10-38` — but that law is DEAD (zero callers, K2), so this is duplication-of-dead-code, not divergence from the live law. BOTH prescribed cures are KILLED: P-5(a) would resurrect the Invariant-20-violating `getPathBounds` the fleet routed for deletion (fr-BasisCanvas.md:95 M-doc(d) — "one import away from reintroduction", and P-5(a) is precisely that import); P-5(b) collides un-adjudicated with banked fr-FourierMorphDemo.md:73 FMD-26 (the `closed:false` branch + the pencil-boil edge are queued for deletion, FR-AH-37/FR-AH-53). The sound target is the fr-ContourPreview row-40 `contourBounds` extraction.
24. **PP-KIT** (L-12) — **MINOR → F.W3/W4** (rides the fork). A zero-consumer component in the 5-file shared `ui/` kit, named by DA6 guidance as a surface every design agent must account for; 69 lines is right-sized (Goldilocks concession endorsed) — the defect is placement + three borrowed library jobs inlined in one computed.
25. **PP-DECIMATE** (D-7) — **MINOR → F.W3/W4** (wire branch). No decimation: 1024 samples across a ~53 px glyph; `d` = **14,337 chars** (measured independently three times — L, reader A, reader B — to the character). Perceptual round-join half → SS-13, moot until the fork is decided.
26. **PP-PAR** (D-10) — **INFO → F.W4** hygiene. `preserveAspectRatio="xMidYMid meet"` is inert: square viewBox, square viewport (both bound to one `size` prop), and it is the SVG default; becomes load-bearing only in the CSS-override regime PP-STROKE condemns.
27. **PP-PREFLIGHT** (reader-A missed find, adopted; **this seat's own re-verification**) — **MINOR → F.W4** hygiene (rider on the framing cluster). `display: block` (`:66`) is INERT: tailwindcss 4.3.1 preflight blockifies every `<svg>` tree-wide (my read, `preflight.css:210-219`; `style.css:1`). `flex-shrink: 0` (`:67`) remains load-bearing. Kills half of D S-4 and its falsifier; L-10's mitigating "no global svg rule" receipt also dies (its conclusion survives — preflight constrains `max-width` on `img, video` only, not `svg`). Distinct-by-mechanism from banked fr-EasingCurvePreview.md:96 K-9 (composition, callsite-scoped); this is the global mechanism, cited as such.

**Meta rows.**
28. **PP-AGGLOM** (reader-A missed find, adopted) — **INFO → NO-WAVE-OWNER** (M-25 intake strike-list, SS-3/SS-4). At least eight rows booked against this file belong to OTHER components (C-6, C-11, C-12, C-13, L-2, L-3's yield, L-14, D-12b), five banked elsewhere, yet all counted in the headline tallies — 42 accusation rows against a zero-instance component vs 39 confirmed for its live 309-line host. Three of the five bankings post-date the challenges: a fold-forward obligation on intake, not authorial misconduct. Intake MUST strike at agglomeration or F.W4 double-books. (C-11's glass-ui optional-peer range row rides here: INFO, off-component, verified from the installed package.)
29. **PP-SEVLAW** (reader-A missed find, adopted) — **INFO → NO-WAVE-OWNER** (spec-authoring input). The remount severity law (D declared it honestly) must not be inherited by F.W4 as present grades: the correct reading is MINOR-with-a-fork, and the wire branch is EXPENSIVE (one `getContour` fetch per card in an infinite-scroll grid — `Visualization` carries `contour_hash`, no path arrays). Every row above marked "wire branch" prices in only there.

## Killed-claims register — 11 rows, refutations attached

- **K1 — D-2's falsifier receipt** (challenge-D-design.md:116-119): the quoted `d`-string is FABRICATED — verbatim re-execution (by both readers independently) yields `M0.0833,0.7083L0.5000,0.2917L0.9167,0.7083Z`, not D's `0.9167/0.0833`, which are what a PER-AXIS fit would emit — the exact behaviour D's own S-2 credits the component with NOT having, in a cell advertised as browser-free arithmetic. The row's substance (PP-CLOSE) stands; the receipt dies.
- **K2 — L-6's premise + P-5(a) cure + L-11's falsifier (b)**: `getPathBounds` has ZERO callers (definition + barrel re-export only; both readers' greps; banked fr-BasisCanvas.md:95 M-doc(d)). It is not "the single fit law for the Canvas2D render path" — `useViewTransform.ts` is, and it is the Invariant-20-compliant scan. P-5(a) would re-import a function adjudicated for deletion; L-11's "the live canvas carries the same spread" is false, which STRENGTHENS PP-SPREAD.
- **K3 — L-14 wholesale, overruling reader B's confirmation** (THIS SEAT'S ruling; see disagreements table): GalleryMarquee never mounts — the outer empty-state guard and the inner `>= 4` guard are jointly unsatisfiable over the same `gallery.entries`. Banked fr-GalleryCard.md:53 G-LC1. No phantom instances exist for a derivation model to over-count at this site.
- **K4 — D-5's producer receipt** (baseline law, the K-12 class): "glass-ui@7.0.0 … 63 subpaths" matches NO baseline (v7.0.0 tag = 74; v8.0.0 = 70; working tree = 69; adopted pin 4.0.0 = 80); the enumeration lists `./music-staff` (absent at v7.0.0, first appears v8.0.0) and omits twelve keys present. Conclusion independently re-proven at named baselines (PP-NOSHADOW); this receipt must never be quoted downstream.
- **K5 — C-1's bundle hedge**: "the `.path-preview` CSS rule ships" is backwards — the class appears in NO built sheet and NO chunk (both sibling axes measured it; banked K-13 at fr-GalleryCard.md:63 on a whole-sheet read). Honest rider preserved: `web/dist` is dated 2026-06-12 and gitignored; currency at HEAD is SS-13 residue.
- **K6 — C-9's "no way to name it"**: Vue fallthrough attributes land `role`/`aria-label`/`aria-hidden` directly on the `<svg>` — single root element, no `inheritAttrs: false` (this seat's own 69-line read). `<PathPreview role="img" aria-label="…" />` works today with zero code change. D-3's narrower default-state claim is what survives (PP-A11Y); C-9's framing would mislead a repair into a redundant `label` prop.
- **K7 — L-2's BLOCKER framing, reader A's F.W0 confirmation of it, and L-2's provenance hedge** — all three die together on this seat's receipts (disagreements table row 2): the committed substrate's gate is GREEN; the RED is the uncommitted TS-6 bump's default-flip; the offending import is COMMITTED and four months old (so "the import is new work" was wrong in the other direction too). The fact survives as PP-REDGATE, re-routed F.W1/W2.
- **K8 — C-6's determinism cell** ("permanently `#bf4040`"): entry-route conditional — the default `/paper` entry evaluates `basis-display` AFTER `resolveVizColors()`, snapshotting the poisoned `#888888`, not the authored hex (banked fr-App.md:103 K-5). Any SS-13 witness must pin the entry route (banked fr-GalleryCardModal K-9). The freeze mechanism itself is confirmed (PP-COLORSEAM).
- **K9 — D-1's and C-10's `<script setup>` suppression mechanism**: REFUTED BY EXECUTION at exactly the named version — vue-tsc 3.3.5 emits TS6133 for the component import at `GalleryCard.vue(10,1)` (L's probe T1, independently re-run by reader B). `noUnusedLocals` alone is sufficient; C-10's UNPROVEN-NEEDS-LIVE item is CLOSED, do not route to SS-13.
- **K10 — numeric-receipt bundle** (each a "whole-file-read" claim with a wrong number): D-1's "14 compilerOptions" → **13** (banked corroboration fr-BasisSelector.md:11); D-5's "ContourPreview 59 lines" → **62**; L-9/L-11's "`:76 le=4096`" → `:76` is **`le=256`** (banked corroboration fr-ConvergenceLegend.md:10); D-1's "ShareButton, the only file that ever rendered it" → GalleryView rendered it at `4340ac8:388-396` (C-1's fossil record is the accurate one; D-1's timing conclusion survives); D-6's `"MNaN,NaN…"` failure string → re-execution yields `M-Infinity,InfinityL…` with NaN only where `x === cx`, and the "renders nothing" claim contradicts L-4's render-up-to-error rule (that half → SS-13); L-11's falsifier (c) mislabels destructured locals as parameters (honest count stays 16/12).
- **K11 — D S-4's "two declarations, both load-bearing" and reader B's endorsement of it**: half-dead — `display: block` is redundant under Tailwind preflight, tree-wide (this seat's own read; disagreements table row 3). `flex-shrink: 0` survives.

## Superlatives — with provenance, each re-verified

1. **`currentColor` default stroke (`:16`)** — the best colour decision in fourier's SVG cohort; zero colour literals in the file (grep: 0 hits); siblings hardcode (`ContourPreview.vue:45`, `EasingCurvePreview.vue:12`). Banked provenance: fr-EasingCurvePreview.md:51 CCOLOR cites this exact line as one of two in-tree existence proofs for the idiom. Structurally immune to the `--viz-amber` WCAG-darken liability style.css already carries. The `strokeColor` PROP is the redundant surface, not the default.
2. **`viewBox="0 0 1 1"` + normalized path** — cleanest of the tree's four viewBox idioms; the computed's dependency set pointedly excludes `size` (no re-derivation on resize); `size` leaks into geometry at exactly one point (`:56`), which is what makes PP-STROKE a single seam.
3. **The isotropic fit (`:33`) + arithmetic Y-flip (`:39`)** — provably the identical IEEE double as the canvas law it re-derives (`0.11904761904761904`, `===` true, re-run by both readers), with the API structurally foreclosing the non-square regime where the equivalence breaks. Banked: fr-ContourPreview.md:43 row 10 rules this the sanctioned implementation.
4. **`.toFixed(4)` (`:40`)** — the only rounding discipline in the repo's path-producing code, measured (14,337 vs 39,378 chars, 2.75×) and proven safe (coordinates algebraically bounded `0.5 ± 0.5/(1+2p)`; exponential notation unreachable). External corroboration: fr-FourierMorphDemo.md:93 FMD-N6 cites this line as the tree's prior art.
5. **Zero teardown surface** — no lifecycle, no watcher, no rAF, no observer, no timer across 69 lines (re-read whole by this seat); the only SVG surface in the tree that can say so, earned by contrast (the same probe run caught FrequencyGraph.vue importing `onUnmounted` and never calling it). The `:22` destructure sits INSIDE the getter — reactivity tracks (CF-2 correctly not a defect).
6. **The `|| 1` guards (`:29-30`) in the degenerate-INPUT direction** — delete them and `0 * Infinity = NaN` invalidates the whole path; with them a single point renders as a dot. D-S6 honestly names the asymmetry against itself (rigorous on degenerate input, absent on degenerate props — PP-DOMAIN). Cited as precedent at fr-ContourPreview.md:50 row 17.
7. **Zero break surface (C S-4)** — nil on every F.W1 census row (no glass-ui, no lucide, no pencil-boil, no ToastVariant); one import total. Of ~51 files carrying live glass-ui specifiers, this is one F.W1 need not open.
8. **L's probe discipline (evidentiary superlative)** — eleven probes reproduced TO THE BYTE by two independent hostile seats; probe C1b (valid-and-wrong output, found against the axis's own interest) is the finest single row in the corpus. Honoured falsifiers repeatedly cost the axes their own headlines (D-5's concession, C-8's self-demotion, L's six contra-findings and its receipted supersession note) — the mark of a challenge, not a prosecution.

**Corrected superlative**: D S-4 survives only as `flex-shrink: 0` (K11/PP-PREFLIGHT).

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **NaN-bearing `d` UA behaviour** — L-4 (renders up to the erroneous command) vs D-6 (discards + console error) assert OPPOSITES for the same string; the SVG error-handling rule favours L-4. Decorative to both rows (C1b needs no browser). SS-13.
2. **`stroke-linecap="round"` dot paint** for a zero-length subpath — specified (SVG 1.1 §11.4), unwitnessed; low value (the real producer resamples to exactly `n_points`). SS-13.
3. **D-7's perceptual round-join blob-up** (1024 segments / ~53 px) — string half proven (14,337 chars); raster diff vs an RDP-decimated ~120-point path needed. Moot until the PP-DEAD fork is decided. SS-13.
4. **Per-engine AX-tree exposure** of a bare role-less `<svg>` — the rule-scope half is proven from installed axe-core; what engines announce is live. SS-13.
5. **WCAG 1.4.11 (3:1)** for inherited `currentColor` against the `card-image-frame` plate, BOTH themes, on the wire branch — nobody computed it; must pin the entry route per banked fr-GalleryCardModal K-9. SS-13.
6. **`dist/` currency at HEAD** — thrice-corroborated absence of `path-preview` from every built artifact, but dist is dated 2026-06-12 and gitignored; one `npm run build` settles it (not run — writes). SS-13/F.W0.
7. **The committed substrate's gate under its OWN toolchain** — no seat has run vue-tsc 2.2.12 against the committed lock (install = a write). The narrower proven statement (the diagnostic is exclusively the flag's; the flag defaults false at 5.9.3) suffices for the K7 ruling; F.W1/W2 should confirm at execution. 
8. **L-9(c)** — `size` bound to a container measurement reading 0 pre-layout (the ordinary route to `stroke-width="Infinity"`); unreachable today. SS-13.

**Closed, not residue** (removed from the axes' open lists): C-10's suppression mechanism (K9, closed by execution); C-1's bundle question (K5, closed against dist with the currency rider); L-5's pencil-boil uplift survival (closed by reader B at producer 0.12.0, `pointsToLinear` present and exported).

## Closing verdict

**ADJUDICATED.** A dead 69-line component whose design is, on the merits, the best of fourier's three preview-SVG implementations — and the one that was orphaned. The corpus's unanimous BLOCKER is not sustained: the identity folds MINOR to two banked rulings, and everything sharp in the roster (PP-CLOSE, PP-A11Y, PP-STROKE, PP-LEN, PP-SHAPE) is remount-conditional on F.W3/W4's delete-or-wire fork, whose wire branch is expensive by data reachability. Both hostile re-readers were themselves caught once each by this seat — the marquee that never mounts and the RED gate that belongs to the uplift — which is the incredulity seat doing what it is for. 29 rows routed (F.W0 ×4 · F.W1/W2 ×2 · F.W3/W4 ×15 · F.W4 ×3 · F.W5-W8 ×1 · NO-WAVE-OWNER ×3 · off-component folds ×1 counted in their home records); 11 claims killed with receipts; 7 missed finds adopted, three of them this corpus's only genuinely new registry contributions (the liveness predicate, the tsconfig scope gap, the preflight inertness). Nothing here stamps a wave — every routing is M-25 agglomeration input for the forming X·F specs.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
