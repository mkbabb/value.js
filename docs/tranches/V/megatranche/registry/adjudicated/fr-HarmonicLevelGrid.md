claude-fable-5 (served model id)

# fr-HarmonicLevelGrid — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/morph/HarmonicLevelGrid.vue` (286 lines, read whole by this seat) — the harmonic-level bounds + 12-tile preview strip on `/morph` — with `useMorphConfig.ts` (97, whole), `svg-fourier.ts` (154, whole), `colors.ts` (117, whole), and targeted reads of `FourierMorphDemo.vue`, `MorphPhaseConfig.vue`, `SliderControl.vue`, `style.css`.
**Substrate**: fourier HEAD `cd26c65` + the frozen dirty WT (subject file `M`, re-verified by this seat — the corpus's X-4 coordinate holds) · installed glass-ui **4.0.0** · vue **3.5.38** · reka-ui **2.9.10** (all node-read) · glass-ui producer at **8.0.0**, HEAD `407de2d3` (moved again since the readers' `dc4267fc`; every producer fact in this record is a **v7.0.0 / v8.0.0 tag read**, immune to the drift). Challenge files committed `a8e593cf` **2026-08-06 11:10** (value.js repo, seat-verified) — the pivot date for every baseline-law ruling below.
**Corpus**: three axis challenges (`audit/fourier-components/HarmonicLevelGrid/challenge-{D-design,L-library,C-consumption}.md`, all three read whole by this seat) × two hostile re-reader returns (design-and-usage · library-and-code), delivered merged.
**Method (M-23/L-18)**: every reader kill and every load-bearing fold was re-derived against the trees with this seat's own instruments before adoption. All ten reader kills are RATIFIED — none on trust: the two baseline kills by my own `git show`/`git log -S` against the glass-ui tags; the Vue-mechanism kill by reading the installed runtime's own bytes; the C:S-5 kill by re-deriving the bracket loop. Two reader cells are STRUCK by this seat (MISSED-2's novelty claim; MISSED-3's extrema), and three corpus rows the readers dropped without disposition are restored and ruled (D-11⊕L:D-12 · C-10 · C-13). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node/git/grep only; zero browser — probe parsimony; product source READ-ONLY; this file the sole write)**: whole reads of the five subject-orbit files above; **glass-ui tag reads** — `git show v7.0.0:src/components/slider/types.ts` (line 5 `SliderVariant = "standard" | "spectrum"`; line 10 `defaultValue?: number[]`), `git log --all -S'"scrubber" | "spectrum"'` → exactly `9b4c15d8` (2026-08-08 22:46) released at v8.0.0 `17a11bc5` (2026-08-09), v7.0.0 `Slider.vue` token census → **15 names incl. `--slider-thumb-bg`/`--slider-track-bg`**, 8.0.0 census → the six-name set; **installed-runtime reads** — vue `runtime-core.cjs.js:5788` (`next !== prev || key === "value"`) + `:5864` (`"value" in newProps`) + `runtime-dom.cjs.js:587-597` (live-DOM `el.value` compare), reka `Slider/utils.js` `convertValueToPercentage` + `shared/clamp.js` + `SliderRange.js:25-27` style binding + `linearScale`'s degenerate guard, **executed**: `cvp(1,1,1) → NaN`, `cvp(100,100,100) → NaN`, control `cvp(5,1,49) → 8.33`; **installed glass-ui 4.0.0** — `dist/styles/index.css:201` `@import "./components.css";` (no `layer()`), components.css `@layer` count **0**, `.h-\(--control-h-md\){height:var(--control-h-md)}` pre-compiled (the reader's `.h\:\(…\)` transcription corrected), `.active\:scale-\(--scale-press-btn\):active{…}` + `[aria-pressed=true]` twin extracted verbatim, base.css `@layer components` at :43 / `.focus-ring:focus-visible` :174-178 / `.tap-squish` six-leg list + `:active` + PRM reset :255-279 / `.fading-scroll` :355, surfaces.css `.btn-pill` :119-120 + the cascade-bug doctrine comment + six-leg transition :143-155 + `.btn-pill > *` flex-shrink-0, scale-paper.css :26/:40-41 (0.96 / 0.97 / alias), offsets-sizing.css :136/:148/:151, light-dark.css coarse block + `--viz-chebyshev` :146, color-radius.css :72/:258/:264/:266 (`--viz-amber: var(--section-color-5)`), dark-arm.css :111/:114, `@property`∩viz → 0, `slider-scrub` in dist → **0**, `fira-code` in dist/glass-ui.css → **0** with `@utility` at typography/utilities.css:69 and typography.css:13 a comment, exports map carries `./fading-scroll ./labeled-field ./number-field ./badge ./metric-badge`, button chunk `default:"h-(--control-h-md) px-4 py-2 has-[>svg]:px-3"` + base `btn-pill tap-squish focus-ring … active:scale-(--scale-press-btn)` + outline `border-input bg-background hover:bg-accent … aria-pressed:bg-accent …`, slider chunk `inheritAttrs` **0** + `class: { type: [Boolean,null,String,Object,Array] }` **a declared prop** + `"aria-label": n.$attrs["aria-label"] ?? void 0` thumb forwarding + `.slider-range` `var(--slider-range-bg, var(--primary))` + `--slider-thumb-bg` read exactly once under `[data-variant=spectrum]`; **fourier repo runs** — `--slider-scrub` census **23 declarations / 7 files** (HLG 4 · MorphPhaseConfig 4 at **:207-210** · SliderControl 5 · BasisSelector 4 · EditorControlsDock 4 · GlassTimeline 1 · ConvergenceTimeline 1), `-webkit-overflow-scrolling` = 3 sites, `--ui-scale|--control-floor` in src → 0, heading census (h1 :5 / h3 ×2 / h2 only in FourierShapeExtractor), style.css :3/:41/:47 + the stale D.W4.d `--viz-amber` comment block, e2e spec `morph` row + `fullPage: true` + the horizontal-only occlusion gate, J-baseline PNG IHDRs **1440×900 / 375×667 / 1280×800**, the 2026-06-01 audit's contradictory parenthetical reproduced verbatim, node over sun/moon JSON: `levels [1..50]`, `n_harmonics 50`, `partial_sums` keys exactly ten, **GLOBAL all-level extrema sun x 13.316–177.753 / y 16.626–175.467 · moon x 29.488–140.961 / y 36.484–183.044** (the MISSED-3 decider), WCAG run `#60a5fa` lum 0.36299 vs `--card` 0.94212 → **2.40:1**; **registry identity sweeps** — fr-FourierMorphDemo.md (FMD-1/3/5/10/11/12/13/14/16/17/18/19/22/24/25/26/28/29/31/32/33/34/N6 + K-11/K-12 + UNPROVEN #5), fr-BasisSelector.md (**B-2 = 23/7 confirmed** · M-6 · R-7), fr-FunctionInput.md (D-9/L-M2 + UNPROVEN #3 verbatim), **fr-CanvasOverlayButton.md (R-7 · FR-COB-8 · FR-COB-12 — the MISSED-2 decider)**, PaletteCardSkeleton.md (R-2: 7.0.0 imports components.css `layer(components)`), fr-EasingCurvePreview.md (K-GEOM/WRONGPAD), fr-BasisCanvas.md D-1, fr-App.md B-1, `grep 'NaN%\|min===max'` across adjudicated/ → no prior derivation of reka's percentage NaN and no prior HarmonicLevelGrid booking.

## Scoreboard

**28 corpus rows ADJUDICATED-CONFIRMED (1 BLOCKER-tier fold · 12 MAJOR · 13 MINOR-tier · the rest INFO; 23 of 34 roster rows are folds-by-reference to banked identities — this component re-books almost nothing) · 12 killed-claims register entries (10 reader kills RATIFIED, every load-bearing one re-derived by this seat's own instruments · 2 seat strikes AGAINST the readers' missed-cells) · 3 reader omissions restored and ruled (D-11⊕L:D-12 dropped whole; C-10 and C-13 left dispositionless) · 6 reader-missed finds adjudicated (1 new MAJOR ratified · 1 fold-not-new · 1 evidence-corrected · 3 booked as filed) · 10 superlatives ratified (2 restored by this seat) · 1 banked-residue caution minted (FMD-32's m-5 note must not cite `.grid-svg` as craft).**

The component-owned headline is not new — **FMD-1** (the phantom 75/100 ladder) and **FMD-16** (the height-pinned clipped cells) own the two worst truths, and both were already banked. The one genuinely new MAJOR is the readers' find, ratified by this seat's own execution: **a two-drag-reachable degenerate slider range whose reka percentage math is `NaN`** — the Low track loses its fill and goes inert with no cue (HLG-13). The seat's own yield is corrective: all ten reader kills hold, two reader cells do not (a false "unbanked" claim and an under-measured extrema table), and three corpus rows the merge silently dropped get their dispositions back.

## The incredulity seat's own work — rulings

| topic | readers' merged position | RULING | Deciding evidence (seat's own) |
|---|---|---|---|
| **The two baseline kills** (slider `variant` + token census "at 7.0.0") | KILLED as 8.0.0-facts-labelled-7.0.0 | **RATIFIED — with this seat's own git** | v7.0.0 tag: `SliderVariant = "standard" \| "spectrum"` (types.ts:5); the fifteen-name token census incl. the thumb/track pair; `"scrubber" \| "spectrum"` enters at `9b4c15d8` 2026-08-08, ships at v8.0.0 `17a11bc5` 2026-08-09 — two/three days AFTER the challenges' `a8e593cf` 2026-08-06. Substance survives at the destination and FOLDS → banked **FMD-11** (which already orders F.W1 to budget from 8.0.0). Fourth corpus carrying the stale-pin class (banked K-7/K-8/SR-2/K-15). |
| **The Vue force-patch kill** (L:D-4 / C:C-5's "permanent desync") | KILLED (mechanism), defect survives at FMD-25 magnitude | **RATIFIED — from the installed runtime's own bytes** | vue 3.5.38: runtime-core :5788 force-patches `value` on the dynamicProps fast path, :5864 unconditionally on full diff; runtime-dom :587-597 compares against the **live DOM** `el.value`. "Permanently displays `abc`" is unsupportable; the desync is bounded by the next re-render (frequent here — every `activeLevel` crossing re-renders). Surviving defect = the no-re-render-scheduled fixpoint window, already banked **FMD-25 (MINOR)**. The wrong mechanism must not travel into the repair ticket: the cure is a `@blur` normalizer or version-keyed `:value` — Vue already force-writes. |
| **C:S-5's kill** (the `lo === hi` "cached fast path") | KILLED — unreachable, all 12 calls allocate | **RATIFIED — seat re-derivation** | svg-fourier.ts:136-142: for on-grid `clamped = levels[k]` (k≥1) the loop breaks at i=k−1 with `lo=levels[k-1], hi=levels[k], t=1`; for `clamped=1`, `lo=1, hi=2, t=0`. `lo === hi` (:150 — not the C axis's :146) requires `levels.length === 1`. Concordant with banked FMD-10 and the corpus's own L axis. The superlative dies; its corpse yields HLG-25 (the ownership asymmetry on that very branch). |
| **The compose kill** (C:C-7 b4 / C:C-10: local `transform` "kills" `tap-squish`) | KILLED — backwards; D-5's compose account ratified | **RATIFIED** | The scoped block declares `transform`, never `scale` (:239/:244/:248 — whole-file read); `transform` and `scale` are independent composing properties. The unlayered `.active\:scale-\(--scale-press-btn\)` rule (extracted verbatim) survives untouched → net press ≈ 0.97 × 0.96 = **0.931**, two registers deep. Consequence for the cure carried: deleting the local `transform` restores ONE register, it does not remove the press. |
| **MISSED-2's "unbanked" claim** (glass 4.0.0's unlayered sheet defeats its own PRM press guard) | filed as NEW (INFO, producer-owned) | **STRUCK — the mechanism is BANKED; FOLD, not new** | fr-CanvasOverlayButton **FR-COB-8** already books the producer CLASS verbatim ("components.css, 0 `@layer`, imported unconditionally at index.css ~:201 … defeats glass-ui's own `@layer components` accessibility resets") and **FR-COB-12** the PRM-squish instance — MINOR, with the identical instantaneous-not-animated narrowing via a11y-overrides :12-16, NO-WAVE-OWNER, standing BH relay. The readers' registry sweep named PaletteCardMenu K-4 / PaletteCardMeta M-16 and missed the actual owner. Substance RATIFIED (this seat's bytes concur exactly); novelty struck. Surviving new cell: the **PaletteCardSkeleton R-2 cross-pin corroboration** — 7.0.0 imports components.css `layer(components)`, so the class is pin-specific and partially discharged at the destination (where PCS-3 shows the layered import minted a different defect). |
| **MISSED-3's evidence table** (`.grid-svg { overflow: visible }` is a no-op) | filed with per-level extrema + "strictly tighter at lower n" | **VERDICT RATIFIED · EVIDENCE CORRECTED** | This seat's GLOBAL all-level run: **moon y-max = 183.044 at a sub-50 level** — above the readers' n=50 figure (174.7), so "strictly tighter at lower n" is FALSE and their table is not the worst case. Corrected worst extent with half-stroke (+2): y ≈ 185.04, x ≈ 179.75 — still inside [0, 200] on both axes, both shapes, every level. The declaration has never painted; margin ≈ 15 viewBox units, not "comfortable". Bank consequence stands: FMD-32's m-5 residue must justify the `.easing-preview` cure on its own arithmetic (K-8/WRONGPAD supplies it), never by analogy to this dead rule. |
| **D-11 ⊕ L:D-12 — DROPPED by the merge** (no `:disabled` posture; parent swallows clicks) | absent from confirmed, killed, and missed | **READER OMISSION RULED AN ERROR — restored, adjudicated MINOR rider** | Seat-verified: `handlePreviewClick` opens `if (isAnimating.value) return;`; the sibling `MorphShapePreview` takes `:disabled="isAnimating"` (:18); the twelve cells bind no `:disabled` and keep `cursor: pointer` + hover lift + press squish. The availability contract is enforced in the consumer while the affordance asserts in the producer. Rides banked **FMD-5's component-new arm + FMD-34's three-busy-postures edit** (one `:disabled` binding lands with that family cure) · F.W4. The D axis's MAJOR preserved as dissent; severity attaches to the family's one cure. |
| **C-10 and C-13 — left dispositionless** | engaged only via the kill (C-10) or not at all (C-13) | **restored** | C-10's omission arm: FOLD → banked **FMD-22's narrowing** — glass's PRM blanket rewrites `transition-property` (allow-list omits `transform`/`scale`), so the local motion is instantaneous under PRM, not animated; the residual instantaneous squish is **FR-COB-12**'s row. Its "net regression" arm stays killed (the compose ruling). C-13 (mixed `$emit` vs typed `emit`): INFO, one-line normalization in the F.W4 residue batch. |
| **D:D-1's severity** (BLOCKER vs the bank's MAJOR) | fold at MAJOR, "over-rated against the bank" | **MAJOR-as-banked — with D's BLOCKER dissent and its receipt RECORDED** | The fold discipline holds (severity attaches to banked FMD-16). But the D axis brought evidence the FMD corpus lacked: the tranche-J baseline PNG (`57624fa`, 2026-06-04, nine days after the Button migration) showing ten ~40px cells, curves bleeding through borders, zero `n=` labels — a shipped-broken receipt, IHDR re-read by this seat. And the emission half is now CLOSED (below), so the defect is fully proven, not half. If SS-13's pixel run confirms at today's tree, the wave may re-rate; this record files the dissent beside the fold. |

**Cell corrections in passing**: the merged return's `.h\:\(--control-h-md\)` transcription → the shipped selector is `.h-\(--control-h-md\)`; reka's `convertValueToPercentage` sits at utils.js:10-15 (reader cited 10-14); C's fast-path line :146 → :150.

## Adjudicated defect roster

Line anchors: fourier `cd26c65` + frozen WT / installed 4.0.0 / glass tags v7.0.0·v8.0.0 — re-resolve before any cure. Folds carry the banked row's identity, original ids for life; nothing here re-books a banked defect. Routing per the census lane taxonomy (F.W0 substrate · F.W1 tri-package uplift · F.W3/W4 shadow retirement + frontend audit · F.W5–W8 CRUD/provenance union · F.W9/W10 close · NO-WAVE-OWNER = producer relay or SS-3/SS-4 spec input).

### BLOCKER tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **HLG-1 · L:D-1 ⊕ C:C-3 ⊕ L:D-6 (⊕ D:§0)** | The n=75/n=100 tiles are byte-identical clones of n=50 (`d(75)===d(50)===d(100)`, readers' execution; the clamp-to-`maxLevel=50` + bracket `t=1` mechanism re-derived by this seat from svg-fourier.ts:131/:136-142/:152); the High control's 51–100 travel is inert; `.active` structurally unreachable on the clones; and the component HOLDS the antidote — `props.shape` (:96) carries `data.levels`, consulted at exactly one site (:130) while `100` is a literal at :36/:44/:115 (`useMorphConfig` cannot fix it: constructed shapeless, FourierMorphDemo.vue:107). | **FOLD → banked fr-FourierMorphDemo FMD-1 (BLOCKER · F.W4)** — the bank's cure text already names L:D-6's fix ("derive the ladder from `shape.data.levels` … cap the High range at the decomposition's own maximum"). No re-book; phantom-tile pixels → SS-13. |

### MAJOR tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **HLG-2 · D:D-1** | `size="default"` pins each cell to `--control-h-md` while ~2× that stacks inside; the scoped block (:228-240, seat-read) declares no height/min-height; `justify-content:center` survives → symmetric overflow, sheared by `.grid { overflow-y: hidden }` (:222). **Band corrected by the readers, re-verified**: 2.5rem is ROOT-relative and fourier's root is 1.125rem below 768px (style.css:41/:47) → cells are **45px at 640-767** and 40px only ≥768; the coarse arm is 3.75rem. Receipt: the J-baseline PNG (see ruling). | **FOLD → banked FMD-16 (MAJOR · F.W4; D's BLOCKER dissent + receipt recorded).** **CONTRIBUTION: closes banked FMD-16's UNPROVEN #5** — `.h-\(--control-h-md\)` ships **pre-compiled and unlayered** in the producer's own components.css, reached via style.css:3 → index.css:201; no K-GEOM build instrument needed. Bank's "grows with `--ui-scale`" framing gains the root-font-size leg. Rendered clip → SS-13. |
| **HLG-3 · D:D-3 ⊕ L:D-3 ⊕ C:C-1** | The four `--slider-scrub-*` declarations (:210-213) have **zero readers** in glass-ui 4.0.0 (`grep -rc slider-scrub dist/` → 0, seat re-run); both tracks paint `var(--slider-range-bg, var(--primary))`'s fallback; the whole `VIZ_COLORS` binding chain is inert; `--slider-thumb-bg` is read only under `[data-variant=spectrum]` so the thumb pair is doubly dead. L's flex-1-proves-plumbing falsifier is INVALID (`class` is a **declared prop** in the chunk, seat-read) — the conclusion survives on the right reason: `inheritAttrs` absent + single root ⇒ `style` falls through. Repo count **23 declarations / 7 files** (seat grep; C's 20 and L's six-file heading both wrong; spans corrected to MorphPhaseConfig :207-210). | **FOLD → banked FMD-3 → fr-BasisSelector B-2 (fleet row, 23/7) · F.W1.** Cure as bank-corrected: rename ONLY the range pair, DELETE the thumb pair (my v7.0.0/8.0.0 censuses re-prove R-7 leg (b)'s correction). Corpus closure kept: M-bump-migration.md:37's UNVERIFIED → VERIFIED DEAD. |
| **HLG-4 · D:D-4 ⊕ C:C-2** | `cssVarToHex` (colors.ts:22-54, seat-read — C's :26-58 cites drifted, D's exact) has no `oklch()`/`light-dark()` arm → `VIZ_COLORS.chebyshev` → `#888888` (fallback :53); the installed token is oklch in all three arms (light-dark.css:146, color-radius.css:264, dark-arm.css:114), zero `@property`. The `--viz-amber` asymmetry confirmed: `color-radius.css:266` is `var(--section-color-5)` while fourier re-authors it in `hsl()` — and the style.css D.W4.d justification comment ("glass-ui ships light `--viz-amber` at hsl(35 70% 42%)") is **stale at the installed pin** (seat-read both ends). | **FOLD → banked FMD-17 → fr-BasisCanvas D-1 / fr-App B-1 (+K-12: HEAD greys both themes) · F.W4/F.W2.** Sequencing carried: moot for these two call sites if FMD-3's dead channel deletes first. |
| **HLG-5 · D:D-2 ⊕ C:C-8 (Button legs)** | `variant="outline"` + `size="default"` ×12 (:57-58) cease to exist at the producer's current release — v7.0.0 Button already has `emphasis`/`tone`/`size` with no `variant`, no `"default"` (tag-read); true through 8.0.0. `variant` would fall through as a bogus DOM attribute. | **FOLD → banked FMD-11 (MAJOR · F.W1, budgeted from 8.0.0).** The slider leg of the same corpus rows is KILLED at the named baseline (killed register) and survives only inside FMD-11's own slider leg. |
| **HLG-6 · D:D-5 ⊕ D:D-6 ⊕ C:C-7 override half (⊕ D:D-18)** | The unlayered scoped block strips the Button recipe: :239's three-leg `transition` shorthand replaces both six-leg coherent lists (surfaces.css:148-155 ≡ utilities/base.css:261-267, seat-read — the producer names this exact failure class verbatim at surfaces.css:143-149); a SECOND press register composes (`transform: scale(0.96)` × unlayered `scale` 0.97 ≈ **0.931**); `outline`'s `hover:bg-accent hover:text-accent-foreground` fires unopposed (:242-245 declares no background) so background/color SNAP while border eases. | **FOLD → banked FMD-13 (the FR-AH-11 single cascade decision) + FMD-28 (motion literals) · F.W4.** C's inverted compose account killed; D-5's ratified. |
| **HLG-7 · D:D-10** | The state box-shadows on `.active` (:251-254) and `.is-bound` (:256-259) — unlayered scoped — replace `.focus-ring:focus-visible`'s entire focus paint (`@layer components`, base.css:174-178: `outline:none` + `box-shadow: var(--focus-ring-shadow)`), and the layered `outline:none` still applies ⇒ tabbing to exactly the three cells that matter produces **no visible focus change**. Distinct mechanism from FMD-18 (which owns the top-edge clipping). WCAG 2.4.7. | **MAJOR — books here**, riding FR-AH-11/FMD-13's one cascade decision · **F.W4**. Focus-state readback → SS-13. |
| **HLG-8 · D:D-7 ⊕ L:D-13** | Both number inputs are nameless to AT: bare `<label>`s :7/:30, no `for`/`id`/`aria-*` on :8-16/:31-39 (seat-read; file-wide `aria-` = exactly :23/:46, both Sliders). The corpus contradiction reproduced by this seat: `2026-06-01-modern-web-audit/fourier.md` claims "the number/text inputs in HarmonicLevelGrid do carry `aria-label`" and cites :23 — which is the Slider. The P0 headline stands; the mitigating parenthetical is false. | **FOLD → banked FMD-14 (MAJOR · F.W4** — the `./labeled-field` cure ships at the installed pin, seat-verified in the exports map). Audit-erratum recorded for the wave that touches the doc. |
| **HLG-9 · D:D-8 ⊕ D:D-9 ⊕ C:C-9 (⊕ L:D-10's carry)** | Selection/bound state carries zero ARIA (:60-63 classes only) while the producer's `outline` recipe already styles `aria-pressed:` (chunk-read verbatim); `.is-bound` is colour-only at **2.40:1** vs `--card` (this seat's run; D said 2.41, bank 2.45/2.40 — drift-proof FAIL of the 3:1 floor); four `#60a5fa`/`rgba(96,165,250,…)` literals (:203-204/:257-258) diverge from the sibling's tokenized recipe, bound to `:focus` not `:focus-visible`; `--accent-red` passes at 4.63. M-design-language C2-11 filed these exact lines in June — still unfixed, line-identical. | **FOLD → banked FMD-18 + FMD-19 (MAJOR · F.W4).** C2-11 adopted as unfixed carry inside the fold. Ring locatability after the clip + overpaint perceptibility → SS-13. |
| **HLG-10 · D:D-21 ⊕ L:D-2 ⊕ C:C-4** | `getPath` is a bare template call in the 12-cell `v-for` (:68/:129-132); `previewLevels` returns a fresh identity per compute (useMorphConfig.ts:38); every re-render rebuilds ~12 × 57 KB `d` strings; slider drags re-render per tick and mutate the cell count mid-drag (:35-36). All 12 calls allocate (the C:S-5 kill). | **FOLD → banked FMD-10 → fr-FourierMorphSvg FM-3 (MAJOR · F.W4, corrected cadence ~168 rebuilds ≈9.5 MB/toggle) + FMD-N6 (resident ~667.6 KiB · `.toFixed` prior art).** L's BLOCKER over-rated; banked K-12 already killed the ≥12/≈8 MiB figure class. INP/heap → SS-13. |
| **HLG-11 · C:C-6** | The tree's own labeled-slider chassis (`SliderControl.vue`, 150 lines — label + inline input + `<Slider variant="standard">` + the same scalar↔array computed + the same `--track-color` projection + the same dead quad + a STRONGER clamp `Number.isFinite(v) ? … : lo`, seat-read :40-42) is bypassed and re-forked inline, twice, regressing the clamp to `\|\| 1`. | **FOLD → banked fr-BasisSelector M-6 (MAJOR · F.W3)** — which already books the two hand-copies and the clamp regression; banked K-11 there killed the visible-divergence half (all 23 declarations dead ⇒ every slider renders identically today). |
| **HLG-12 · D:D-12** | The strip is a bare `<div class="grid">` (:53) with `overflow-x: auto`, no mask/arrows/role/aria-label (:218-226); `computePreviewLevels(5,50).length === 12` vs 10 visible in the widest checked-in baseline; the remedy ships at the installed pin (`./fading-scroll` export + base.css:355 recipe, both seat-verified). | **FOLD → banked FMD-12 (MAJOR · F.W3** — the hand-rolled-overflow-strip arm). Today-fixable, not F.W1-gated. |
| **HLG-13 · MISSED-1 (readers' find — RATIFIED NEW)** | **A degenerate slider range (`min === max`) is reachable in TWO pure drags from default** — Low fully left (`lowLevel=1`) then High fully left (clamps to `lowLevel+1=2`) ⇒ Low slider `min=1, max=1`; symmetrically via the n=100 tile + Low fully right ⇒ High slider `min=100, max=100`. reka's `convertValueToPercentage` at `min===max` → `Infinity × 0 = NaN` (**executed by this seat**: `cvp(1,1,1) → NaN`, control 8.33); `clamp` propagates NaN; `SliderRange` binds `right: "NaN%"` — invalid, dropped ⇒ zero-width fill; `linearScale`'s degenerate guard maps every pointer sample to `min` ⇒ inert; `variant="standard"`'s thumb is already invisible ⇒ an EMPTY capsule, no `disabled`, no cue, no guard in component, chassis, or producer. All twelve slider rows across three axes missed the derived bounds at their own boundaries. Note: this does NOT breach superlative S-4 — `low < high` still holds (1 < 2); the collapse is of the DERIVED range, not the band. | **MAJOR — books here · F.W4**: floor the derived ranges (`max = Math.max(min+1, …)`) or disable at collapse. CLASS identity honoured: banked **fr-FunctionInput D-9/L-M2** (different component, different path); this filing also **PARTIALLY closes fr-FunctionInput's UNPROVEN #3** — the degenerate-paint half is now decided statically (NaN% dropped ⇒ empty fill; per-variant thumb moot); the reka clamp-correct-loop half (modelValue<min) stays live. Painted witness → SS-13. |

### MINOR tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **HLG-14 · L:D-5 ⊕ C:C-11 ⊕ D:D-16** | `Number(raw) \|\| 1` is the wrong identity for the UPPER bound (clear High + blur ⇒ 50 → `lowLevel+1` = 6 at default); no `Number.isFinite`, no integer gate despite `step="1"` (`"3.7"` → a tile labelled n=3.7); the fixpoint desync is real but bounded (the Vue kill). The chassis does it right forty lines away. | **FOLD → banked FMD-25 (MINOR · F.W4)** — which already owns all three legs incl. the lost `Math.round`. Cure text corrected per the kill: `@blur` normalizer / version-keyed `:value`, never "force-write". |
| **HLG-15 · D:D-13** | Desktop `margin-bottom: 1rem` (:145; mobile arm is 0 at :139) double-counts the parent flex `gap: 1rem` + `.export-row`'s `padding-top: 0.5rem` ⇒ 1rem above / 2.5rem below, the sole break in a uniform stack (siblings carry no margin — seat-read both files). | **MINOR — books here · F.W4.** Delete :145; the mobile arm encodes the author's own answer. |
| **HLG-16 · D:D-14** | Card anatomy diverges from all three siblings: bare `<h3>` title-only (:3, margin 0.75rem) vs title+`<p class="config-card-desc">` with real gloss prose (seat-read MorphPhaseConfig :3-4); "Low"/"High" never glossed as the degrade/restore bookends. | **MINOR — books here · F.W4** (one description line; also touches the docs surface). |
| **HLG-17 · D:D-15 ⊕ L:D-11** | h1→h3 skip with no `<h2>` on the route (seat heading census; the repo knows the pattern — FourierShapeExtractor pairs h2 under its own h1); `class="grid"` shadows the Tailwind utility on a `display:flex` element, winning only because SFC styles are unlayered. | **FOLD → banked FMD-31 (MINOR · F.W4).** |
| **HLG-18 · D:D-17 ⊕ C:C-14** | Two mono mechanisms for one concern — the `fira-code` class (:15/:38; **0 hits in dist/glass-ui.css**, seat-run; `@utility` at typography/utilities.css:69, resolved only through the consumer's Tailwind build, which fourier has via style.css:3) vs `var(--font-mono)` (:275) — and neither sets `tabular-nums` on values that change per slider tick, while the producer ships `.animated-digit` with `tnum`. C's `typography.css:13` cite is a comment (killed register). | **MINOR — books here · F.W4.** |
| **HLG-19 · D:D-19** | Responsive proportion inverted: title ~20.25px mobile vs 18px desktop (the 1.125rem root × `text-lg`) while padding shrinks on the narrow arm. | **FOLD → banked FMD-29 (MINOR · F.W4** — two breakpoint systems 128px apart; the ratio itself banked as sound). |
| **HLG-20 · D:D-22** | The route's only automated gate asserts HORIZONTAL overflow ≤2 — absorbed by construction by `.grid { overflow-x: auto }`; `fullPage: true` requested yet the checked-in captures are exactly viewport-sized (**1440×900 / 375×667 / 1280×800**, IHDRs re-read by this seat — the app scrolls in an inner port). GREEN while HLG-2 clips and HLG-12 hides two of twelve. | **FOLD → banked FMD-31's test-posture arm → FM-10's e2e row (MINOR · F.W4).** Argues the vertical-clip assertion; feeds SS-13's probe list. |
| **HLG-21 · L:D-7** | `computed<number[]>` setters typed `(arr: number[])` narrow away the producer's declared `number[] \| undefined` emit; `arr[0] ?? 1` guards the element, not the array. Type lie static; throw unreachable single-thumb. | **FOLD → banked FMD-33 (MINOR · F.W4** — the adapters' setter-type divergence, persists at HEAD). |
| **HLG-22 · L:D-17 (⊕ C:C-4 precision leg)** | 512 cubic segments + full-float64 coordinates into a 48/64px box — ~10 segments per rendered pixel, 17 significant digits per point, no stride/precision parameter expressible (svg-fourier.ts:47-73, seat-read). | **FOLD → banked FMD-N6 (MINOR · F.W4** — resident-cost framing + `PathPreview.vue:39 .toFixed(4)` prior art, 57,002→20,323 chars at 2dp). |
| **HLG-23 · C:C-12** | `/morph` reaches zero of the 45 API operations; the static-JSON seam is `as any`-cast (FourierMorphDemo.vue:99-100, seat-read) — voiding the one interface whose invariant HLG-1 violates. | **FOLD → banked FMD-24 → N-14 (cast FORCED; cure = narrower typed assertion) + N-15 + FR-AH-1 (MINOR · F.W4).** The R6-8 **clean-seam control** observation is genuine and unbanked — kept as **F.W5 evidence**, not a defect. |
| **HLG-24 · D:D-11 ⊕ L:D-12 (seat-restored)** | No `:disabled` posture on the twelve cells while `handlePreviewClick` silently swallows clicks during a morph; the sibling in the same template takes `:disabled="isAnimating"`; the producer ships the disabled paint in the CVA base (chunk-read). | **MINOR rider → banked FMD-5's component-new arm + FMD-34's busy-posture edit · F.W4** (one binding). D's MAJOR preserved as dissent. |
| **HLG-25 · MISSED-4 (readers' find — booked)** | The `lo === hi` fast path (:150) and the `??` fallback (:147) return the SHARED cached arrays from `pointsByLevel` while every other exit allocates fresh — an ownership asymmetry on the branch nobody can reach (the C:S-5 kill), latent until a single-level shape or widened bracket appears; a mutating caller would corrupt the cache for the hero path too. | **MINOR — books here · F.W4** (return a copy, or document ownership). Library-correctness lens; L:D-14 enumerated the postures and missed this one. |
| **HLG-26 · MISSED-5 (readers' find — booked)** | Both bounds bind the SAME colour (`VIZ_COLORS.chebyshev`, :25/:48 — seat-read), so the corpus's own prescribed cures (rename the quad + retire cssVarToHex) restore a colour system that cannot distinguish the two things the component exists to distinguish — while the sibling already parameterises per-instance (`sliderColor ?? 'var(--accent-red)'`, driven with two distinct tokens — seat-read all three mounts). | **MINOR — books here · F.W4**, sequenced as the third edit of the FMD-3 → FMD-17 cure chain (two tokens, one binding each). Narrows superlative S-1. |

### INFO tier

| id | claim · proof | terminal disposition |
|---|---|---|
| **HLG-27 · L:D-14** | svg-fourier's uniformly silent error postures: `lerpPoints` truncates; `interpolateAtHarmonicLevel` → `[]` → `""` → `d=""` with no trace; `prepareFourierShape` :82 silently drops absent levels; `nearestLevel` assumes sorted input undocumented. Third leg (exact-level hits pay a full lerp) re-derived — it is the C:S-5 decider. | **FOLD → banked N-15 (silent-degradation chain) via FMD-24 · F.W4.** |
| **HLG-28 · L:D-15** | `pointsToSvgPath`'s closed branch verbatim-reimplements the `catmullRomToBezier` it imports; `closed` defaults true and no caller passes false ⇒ the import is unreachable tree-wide (both call sites seat-verified). | **FOLD → banked FMD-26 → FR-AH-53 (pencil-boil, deletable outright) · F.W4.** |
| **HLG-29 · L:D-16 ⊕ D:D-20 ⊕ L:D-8** | Contract residue: `:key="level"` assumes a de-duplicated prop the type doesn't carry; no empty arm though `levels: number[]` admits `[]` (unreachable today — `computePreviewLevels` ≥12 via a Set); the stringly emit seam (`String(arr[0] ?? 1)` → `Number(raw)`) is precisely what forced the shared `\|\| 1` identity behind HLG-14. | **INFO — books here · F.W4** (typed `clampLow/High(n: number)` core + thin DOM edge; honest severities as filed). |
| **HLG-30 · C:C-10 (seat-restored)** | No PRM guard in-file — but glass's a11y blanket rewrites `transition-property` under PRM (allow-list omits `transform`/`scale`), so the local motion is instantaneous, not animated; the surviving instantaneous press is the producer's own defeated guard. | **FOLD → banked FMD-22's narrowing + FR-COB-12 · F.W4.** The "net regression" arm stays killed. |
| **HLG-31 · C:C-13 (seat-restored)** | `$emit('select', level)` at :64 vs typed `emit()` at :111/:116 — stylistic, non-greppable emit surface. | **INFO — F.W4 residue batch**, one-line normalization. |
| **HLG-32 · MISSED-2 (seat-corrected)** | Glass 4.0.0's unlayered components.css defeats its own `@layer components` PRM press guard — every fact re-verified by this seat (index.css:201 unlayered import; `@layer` count 0; the `.active\:scale…` rule; base.css:273-279; the doctrine comment). | **FOLD → banked fr-CanvasOverlayButton FR-COB-8 (producer CLASS, NO-WAVE-OWNER · standing glass BH relay) + FR-COB-12 (the PRM-squish instance, MINOR-as-banked).** Novelty struck (ruling above); this component recorded as a further instance site (12 Buttons compose `tap-squish` + the unlayered twin). Surviving new cell folded into the relay: PCS R-2's cross-pin fact — 7.0.0 layers the sheet (`layer(components)`), so the class is pin-specific. |
| **HLG-33 · MISSED-3 (seat-corrected)** | `.grid-svg { overflow: visible }` (:264) is a no-op for every reachable shape — GLOBAL all-level extrema (this seat's run, superseding the readers' n=50 table): sun x 13.3–177.8 / y 16.6–175.5; **moon y-max 183.044**; worst extent with half-stroke ≈ [11.3, 185.0] ⊂ [0, 200]. Never painted a pixel; margin ≈ 15 units. | **INFO — books here · F.W4** deletion batch with HLG-34. Bank caution MINTED: FMD-32's m-5 residue must not cite this rule as deliberate craft — the `.easing-preview` cure stands on K-8/WRONGPAD's own arithmetic. |
| **HLG-34 · MISSED-6 (readers' find — booked)** | `-webkit-overflow-scrolling: touch` (:225) — dead since iOS 13 (2019); 3 sites repo-wide (seat grep: + MobileFloatingToc.vue:304, PaperView.vue:549). | **INFO — FOLD → banked FR-AH-37's deletion batch** (three-line repo row). |

## Killed-claims register

Original ids for life; a killed claim is not re-mintable without new evidence. K-1…K-10 are the readers' kills, ALL RATIFIED — the load-bearing ones by this seat's own instruments (rulings table). K-11/K-12 are this seat's, against the readers.

1. **K-1 · D:D-2 (slider row) ⊕ C:C-8 (slider row)** — "`variant="standard"` is gone at 7.0.0; `SliderVariant = "scrubber" | "spectrum"`" cited to the 7.0.0 tree. **FALSE AT THE NAMED BASELINE** (my tag read: `"standard" | "spectrum"` at types.ts:5; the scrubber union enters 2026-08-08, ships v8.0.0 2026-08-09; challenges committed 2026-08-06). TRUE at 8.0.0/HEAD ⇒ substance folds → FMD-11. Fourth corpus in the K-7/K-8/SR-2/K-15 stale-pin class — and FMD-11's disposition text already orders the 8.0.0 budget.
2. **K-2 · D:D-3 (post-uplift arm) ⊕ C:C-8 (token-narrowing row)** — the six-name "7.0.0" token census is the **8.0.0** set; v7.0.0's census is fifteen names including the thumb/track pair (my tag grep). The corrected cure (rename range pair, delete thumb pair) stands on 8.0.0 evidence — as banked at B-2/R-7's corrected leg.
3. **K-3 · L:D-4 (falsifier rebuttal) ⊕ C:C-5 (stated mechanism)** — "Vue never compares against the live DOM / patchProp is skipped regardless of renders." **FLATLY FALSE** at installed 3.5.38 (runtime-core :5788/:5864; runtime-dom :587-597 — my reads). "Permanently displays abc" refuted; the bounded fixpoint window survives at banked FMD-25's magnitude; the wrong mechanism must not enter the repair ticket.
4. **K-4 · C:C-7 (bullet 4) ⊕ C:C-10 ("Compounding")** — "the local `transform` kills `tap-squish`/`active:scale`". **BACKWARDS**: independent properties compose; the scoped block never declares `scale` (whole-file read); net press ≈ 0.931. D-5's compose account ratified.
5. **K-5 · C:S-5 (superlative)** — the `lo === hi` "zero-allocation fast path for ten of twelve levels" is **UNREACHABLE** for any ladder of length ≥2 (bracket re-derivation, ruling table; live line :150 not :146). All twelve calls allocate. The C axis's own load-bearing rebuttal of C-4's cost dies with it; corroborated by banked FMD-10 and the corpus's own L axis.
6. **K-6 · C:C-1 (blast radius)** — "20 dead declarations across 7 files" → **23 / 7** (my grep; bank's B-2 concurs). L's "six files" heading also short.
7. **K-7 · D:D-3 ⊕ L:D-9 (provenance)** — the sibling quad sits at MorphPhaseConfig.vue **:207-210** (my grep), not :204-211. C's anchors exact; cite C's when the cure is written.
8. **K-8 · D:D-1 (arithmetic band)** — "40px at ≥640" is off by 5px across the 640-767 band: the root is 1.125rem below 768 (style.css:41/:47, seat-read) ⇒ **45px** cells against a 64px SVG arm — worse than computed. Verdict unchanged; the figure is not citable.
9. **K-9 · L:D-3 (falsifier reasoning)** — "`flex: 1` visibly works ⇒ the plumbing is live" is INVALID as proof: `class` is a **declared prop** on the 4.0.0 Slider (chunk read). Conclusion survives via `inheritAttrs` absent + single root (style falls through). The right reason recorded before the cure.
10. **K-10 · C:C-2 (line provenance) ⊕ C:C-14 (definition site)** — `cssVarToHex` is :22-54 with fallback :53 (two `#888888` returns, :26/:53); `typography.css:13` is a COMMENT; the definition is `@utility fira-code` at typography/utilities.css:69 (all seat-read). Substance unaffected; anchors were not.
11. **K-11 · seat vs readers — MISSED-2's "it is unbanked" cell** — **STRUCK**: fr-CanvasOverlayButton FR-COB-8 (class) + FR-COB-12 (instance) already book the mechanism at the same pin with the same narrowing; the readers' registry sweep cited the wrong rows. Substance ratified; FOLD (HLG-32).
12. **K-12 · seat vs readers — MISSED-3's extrema cells** — "moon y∈[36.547, 174.728]" and "strictly tighter at lower n" are **UNDER-MEASURED/FALSE**: the global worst case is moon y = 183.044 at a sub-50 level (my run). Verdict survives on the corrected arithmetic (HLG-33); the readers' table is not citable.

## Superlatives — ratified, with provenance

1. **The `low < high` invariant is stated four times in agreement and cannot be violated from any entry point** (D:S-4; L's sibling argument). Re-verified: each bound clamps against the OTHER (:109-117), mirrored onto native `min`/`max` and slider `:min`/`:max`, respected by `handlePreviewClick`'s both branches. Banked as fr-FourierMorphDemo superlative 1. **Survives HLG-13 precisely stated**: the degenerate range collapses a DERIVED slider range at the band's boundary — the band itself never inverts.
2. **The writable-`computed` array adapter is the minimal correct bridge, one clamp funnel, two input paths** (D:S-5 ⊕ L:S-1 ⊕ C:S-2) — no mirror ref, no watch, no stale window; the constellation's own `useColorModel` counter-example named by L. Banked superlative 3; HLG-14/HLG-21/HLG-29 are refinements inside it, not refutations.
3. **`stroke="var(--accent-red)"` is the correct colour idiom, twenty lines from the code that gets it wrong** (D:S-2 ⊕ C:S-1) — token straight to CSS, both arms shipped, zero JS; the F.W2 remedy hierarchy (CSS-var passthrough first, `parseCSSColor` only where JS must see colour) adopted.
4. **Both Sliders are accessibly named on the element that carries the role** (C:S-4) — the chunk forwards `$attrs["aria-label"]` onto the thumb (seat-read); the discipline the grid Buttons fail, sharpening HLG-9. Banked superlative 8.
5. **Zero lifecycle surface — leak-free by construction in a tree that is not** (L:S-2) — `import { computed }` and nothing else; nothing to tear down; the reason this component contributes nothing to the FR-AH-8/-9/-20/-21 epoch family that owns the rest of the route.
6. **The label column is measured, not defaulted** (D:S-3) — `min-width: 2.5rem; nowrap` aligns both rows to one optical edge; a proportion decision this file alone had to author.
7. **The import graph is exemplary and it is exactly why the uplift stays cheap** (C:S-3) — five imports, narrow subpaths, correct `import type` split; `./button`/`./slider` survive to 8.0.0 while the props do not — the shape of this component's migration, banked at FMD-11.
8. **The `--track-color` indirection is the right shape** (D:S-1 — **restored by this seat, NARROWED**): one custom property in, a considered 30/45% ramp derived in CSS, character-identical contract with the sibling. Narrowed twice: both legs are dead today (HLG-3/HLG-4), and one value flows through it twice (HLG-26) — the design is one binding short of its own purpose.
9. **R5-7-immune, with the honest cardinality caveat** (L:S-3 — **restored by this seat**): the sole `v-for` sits on a component callsite, the populated side of the banked R5-7 split; the residual (one SITE ⇒ 12-14 INSTANCES, data-dependent mid-drag) is a different blind spot `NATIVE_TEMPLATE_LOOP` does not cure, and C's K-8 extension (fan-out to 36 native nodes under one component loop) rides with it as F.W4 denominator law.
10. **Corpus-level: the D axis's §4 self-kills are why its D-5 is right where C's C-7 is wrong** — four candidate findings died to their own falsifiers in the challenge itself; the same discipline got the compose semantics right. Every kill in this record is provenance, baseline, or mechanism — not one surviving arithmetic error, matching banked fr-FourierMorphDemo superlatives 9/10. The mirror image stands too: the C axis's S-5 contradicted its own sibling axis and the bank, which is why the incredulity seat re-runs the numbers.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

Static work is exhausted on each; every probe named.

1. **HLG-2 / banked FMD-16 — the rendered clip.** Emission half CLOSED statically this record (pre-compiled unlayered utility). Remaining pixel: `getComputedStyle(cell).height` vs `scrollHeight` on `/morph` at 1440 (expect 40 / ≈98) and at 700px (expect **45** / ≈101 — the corrected band), + whether any `n=` label renders.
2. **HLG-13 — the degenerate slider's painted result.** Mechanism executed to `NaN%`; the paint (empty capsule, no fill, no thumb) is one `getComputedStyle(range).right` + screenshot away. Repro: Low fully left, High fully left. Also the PARTIAL remainder of fr-FunctionInput UNPROVEN #3 (the clamp-correct loop at modelValue<min).
3. **HLG-7 — focus indication on the three state cells.** Tab to `.active` and either `.is-bound` bookend; predict computed `box-shadow` identical focused/unfocused, `outline` none.
4. **HLG-10 / banked FMD-10+N6 — wall-clock.** INP of a Low-slider drag + heap witness of the resident ~667.6 KiB; pair with the queued FR-AH-1 boot trace.
5. **C:C-5 slider half** — whether reka's SliderRoot retains internal drag state when the clamp is a fixpoint and the getter never invalidates (the `<input>` half is DECIDED — the Vue kill).
6. **HLG-9 / banked FMD-18/19 — perceptibility.** Whether the 2.40:1 bookend rings are locatable at all at 48/64px after the top-edge clip; `.is-bound`-over-`.active` in the deterministic boot state.
7. **FR-COB-12 (via HLG-32)** — the instantaneous PRM squish: already SS-13 #4 at fr-CanvasOverlayButton; cross-cite, do not duplicate the probe.
8. **Provenance rider** — producer facts here are v7.0.0/v8.0.0 TAG reads (drift-immune); the 8.0.0-HEAD identity re-baselines at F.W1 exactly as banked SR-2/FM-2 order. Fourier anchors are `cd26c65` + frozen WT; re-resolve before any cure (D-19).

## Verdict

**ADJUDICATED (M-25 agglomeration input — stamps nothing): 28 corpus rows confirmed across a 34-row roster (1 BLOCKER-tier fold · 12 MAJOR · 13 MINOR-tier · 8 INFO; 23 folds-by-reference, identities preserved — this component re-books almost nothing because fr-FourierMorphDemo already banked its two worst truths) · 12 killed-claims entries (10 reader kills RATIFIED, the baseline pair by this seat's own git, the Vue pair by the runtime's own bytes · 2 seat strikes against reader missed-cells: a false "unbanked" claim folded to FR-COB-8/12 and an under-measured extrema table corrected by a global run) · 3 dropped corpus rows restored and ruled · 1 new MAJOR booked (HLG-13, the two-drag NaN-range collapse — executed, classed under fr-FunctionInput D-9/L-M2, partially closing its UNPROVEN #3) · 10 superlatives ratified (2 restored).** The component is a small, largely well-mannered control strip whose genuinely new defect surface is thin: derived slider bounds that can collapse to a NaN-painted point, an unlayered focus-paint replacement on exactly the three cells that matter, and a colour indirection that ships one value where its purpose requires two — while everything heavy (the phantom ladder, the clipped cells, the dead retint quad, the 8.0.0 prop break, the cascade re-skin) folds into banked FMD/B-2/FR-COB identities, and the readers' one systemic weakness this session was registry recall, not measurement.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
