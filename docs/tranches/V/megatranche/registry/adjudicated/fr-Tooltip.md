claude-fable-5

# fr-Tooltip — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · model receipt `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/ui/tooltip/Tooltip.vue` (38 L) + barrel `index.ts` (1 L) · installed pin `@mkbabb/glass-ui@4.0.0` (my `node -p` on the installed package.json) · F.W1 target `v7.0.0` · producer HEAD `407de2d3` = **8.0.0** (my git reads).
**Corpus**: three challenge axes at `audit/fourier-components/Tooltip/` — D (claude-opus-5[1m], 17 defects / 2 BLOCKER) · L (claude-opus-5[1m], 11 / 0) · C (claude-opus-5[1m], 11 / 0) — plus the merged return of the two hostile re-readers (design-and-usage · library-and-code): 19 confirmed cells, 12 kills, 7 missed finds, 9 unproven, 7 superlative cells.
**Method (M-23/L-18)**: the re-readers were themselves attacked before adoption. Every FOLD verdict was re-opened at the banked file; every kill's refutation was re-derived where decidable; every missed find's mechanism was re-proven at this seat's own reads. One re-reader cell was KILLED by this seat against a banked kill they failed to sweep (ruling 1), and one routing increment was minted that neither re-reader stated (ruling 2). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings are agglomeration input (M-25); line anchors re-resolve before any cure (D-19/K-13 law).

**Arbiter's own instruments this session (all static + git + grep; ZERO browser tooling — probe parsimony; product source untouched)**: Tooltip.vue WHOLE (docblock `:2-12` · `defineProps` `:19-22` · `as-child` `:27` · `side ?? 'top'` `:31` · `side-offset 6` `:32` · `collision-padding 8` `:33` · slot-fallback `:35`); consumer censuses re-run — EditorControlsDock.vue (`:collapse-delay="2000"` :56 · Tooltips :62/:73/:78/:86/:91/:96/:142/:147/:156/:165 · aria-labels :105/:121/:136 only · `:disabled` :74/:79/:97 · HoverPopovers :103/:134), CanvasControlsDock.vue (Tooltips :53/:58/:70/:76/:86/:92 — D-1's :42/:49 are a `template v-if` and a closing tag · aria-label :46 only · HoverPopover :44 containing :53/:58), AnimationControls.vue (`:collapse-delay="2000"` :60 · Tooltips :66/:81/:94/:105 · play buttons DO carry aria-label :67/:82 · "Playback speed" wraps `<div class="hidden sm:block">` :95 · Tooltip-inside-DockDropdownTrigger :104-106 · DropdownMenuContent `:side-offset="8"` :109), FunctionInput.vue:188-211 (`<Tooltip side="bottom">` wrapping `<Button :disabled="!effectiveN">` :193, `effectiveN?: number` prop :13), ContourSettings.vue:265-300 + **:377-385** (`.advanced-grid` = `grid-template-columns: 1fr 1fr`, `gap: 0.625rem 0.75rem`, `> :last-child:nth-child(odd) { grid-column: 1/-1 }` — Min Area %/Max Contours are ROW-1 HORIZONTAL siblings; Smoothing spans row 2), CoefficientsSpectrum.vue:75-130/:145-163 (TransitionGroup :79 over `<Tooltip v-for>` :80-84 · `.coeff-row` :85 · rich grid `text-admin-label` :110 with muted labels :111/:113/:115/:117, `font-semibold` :108 · "Show top 40 of N" :124 + `Button v-if` :126 · five `.coeff-list-*` rules), PaperSidebar.vue (deep SFC import :2 · Tooltips :70/:88 inside `li v-for` :65/:87 · lazy CollapsibleContent), App.vue:4/:23/:30 (sole provider, 400/200); `grep -rn '<Tooltip' web/src | wc -l` → **39**; tex counts `grep -c '^\\section{'` → **51**, `'^\\subsection{'` → **31**, `'^\\section\*'` → **0**; tex:2687 DCT subsection read (definition env + four `\paragraph` prose blocks); paperTree.ts:11-21 WHOLE (101-char cap; truthy-gated parts). **reka-ui dist bytes**: TooltipTrigger.js:50-95 (`handlePointerDown` onClose+isPointerDown · `handlePointerMove` `pointerType === "touch"` return · `handleFocus` isPointerDown early-return · **`handleClick` onClose** · trigger attrs = conditional `aria-describedby` + `data-state` + `data-grace-area-trigger` ONLY), TooltipContentImpl.js:85-140 (`ariaLabel = props.ariaLabel || textContent` :87 · defu floor `side:"top"`, `sideOffset:0`, **`collisionPadding:0`**, `avoidCollisions:true`, `collisionBoundary:[]` · VisuallyHidden role=tooltip `toDisplayString` · `--reka-tooltip-content-available-width` published), TooltipContent.js:80-95 (**Presence gates on `forceMount || open`; Hoverable chosen by `disableHoverableContent`; NO portal in reka's content** — the portal is glass's), TooltipContentHoverable.js:80 (`useGraceArea` instantiated HERE only), useGraceArea.js:40-62 (document pointermove gated on `pointerGraceArea` non-null · watchEffect cleanup · `isAnotherGraceAreaTrigger` closes the prior hint), Popper/PopperRoot.js:8-19 (**bare `renderSlot`, unconditional**). **Installed glass 4.0.0 bytes**: TooltipProvider-B3MkB_8P.js (`grep -c "dock\|glass-dock-portal"` → **0** · class string verbatim, no `max-w-*` · props `ariaLabel: {}`, `sideOffset: { default: 4 }`, `collisionPadding: {}`), dock.js:164-168 (`be(e,t)` = `closest("[data-glass-dock-portal]")`), :226-236 (`M(e)` mouseleave → contained-in-dock / stamped-portal / morph-edge, else `D()`), :196-200 (`D()` schedules collapse), :470 (`collapseDelay ?? 2e3`), portal-stamp census (PopoverContent ×4 · SelectScrollDownButton ×2 · dropdown-menu ×2 · HoverPopover ×1 · **Tooltip chunk ×0**), src/styles/hover-popover.css:1-33 (docblock "IconTooltip's tooltip register was too quiet…" + **`max-width: 16rem`** :28), src/styles/dock.css disabled cohort (opacity + cursor ONLY — no pointer-events), button-BNDWhAZb.js CVA (`disabled:pointer-events-none` present), scheme-motion.css:343-344 (`--z-tooltip: 120` / `--z-popover: 130`), exports (`./icon-tooltip` + `./tooltip` both present at 4.0.0). **Producer git-shows**: `v7.0.0:src/components/tooltip/TooltipContent.vue` (props = class/surface/ariaLabel + FloatingPlacementProps · `sideOffset: 4` · golden-ladder class string `[--overlay-pad-inline:--spacing(2)]…*1.272` + `text-(length:--tooltip-text)` · template = RekaTooltipPortal + `:aria-label` + `data-material="overlay"` + `data-reveal="tooltip"` — **NO dock participation, NO portal stamp**), `v7.0.0:src/components/_shared/floating.ts` (65 lines · `FloatingPlacementProps` = side/sideOffset/align/alignOffset · **`RETIRED_FLOATING_ATTRS` carries `"collision-padding"` AND `"collisionPadding"`** · `floatingContentAttrs` filters :59-65), `v7.0.0:src/styles/typography/semantic.css` grep → **`@utility text-admin-label` at :238**, `v7.0.0:src/styles/tokens/offsets.css:78-82` (`--tooltip-text: var(--type-caption)` under the "token-backing of the bare text-sm" comment), `v7.0.0:package.json` (`icon-tooltip` → **0** · `"./tooltip"` → 1), `v7.0.0:src/styles/glass/reveal.css` (`[data-reveal="tooltip"]` at **:112**); HEAD `407de2d3` (8.0.0): TooltipContent.vue:13-15 (**props = class only — `ariaLabel` DELETED**), :34 (`forwardedAttrs = {...attrs}` wholesale — filter gone), :43-48 verbatim ("**THE HINT PARTICIPATES IN THE DOCK.** It emitted no portal stamp at all, so a dock-anchored icon's own tooltip read as 'the pointer left the dock' and collapsed the thing the pointer was still pointing at…"), :69-73 verbatim ("NO `aria-label`. The content-side override used to replace the very node the trigger's `aria-describedby` relation reads… an icon-only trigger labels ITSELF."). **Registry cross-reads (identity guard)**: fr-EditorControlsDock (C-3 + touch/XOR riders, D-22, D-23/C-19, "C-3's shim leg → intake R3-7a + X-11"), fr-CanvasControlsDock (D-1/L-3/C-1 · M-2 · D-7 · C-13 · superlative 2's portal-stamp cell · R-2 panel arithmetic), fr-EquationView (D·D-B2 incl. the Wand2 extension), fr-FunctionInput (D-11.1 · **killed register #2** = D-11.2/C-2(b) with the `?? 20` decider · instruments line "effectiveN = ref(cachedRes?.result?.effective_n ?? 20) unconditional" — re-proven by this seat at EquationView.vue:41 + FunctionInput.vue:13/:193, and `compute_effective_n(minimum=3)` all-exits-≥3 per the banked instruments), fr-CoefficientsPanel (**FR-CP-3 anti-cure lock** · FR-CP-21 · **FR-CP-22**), fr-FrequencyGraph (FR-FG-15 → SS-13 residue 9 · **K-1 tag-decides**), fr-App (MG-β 7 sites/4 files), fr-CoefficientsSpectrum (**K-1 collision-padding kill at HEAD `7d9abe08`** with the true-at-tag concession), fr-AdminUserList (80-key export census), fr-FullscreenViewer (FV-1 — the fullscreen-backdrop tooltip occlusion, a DISTINCT z-order identity), kf-TimelineHoverPreview (MISSED-1 ariaLabel cure @ KF.W7), registry-wide `grep -rn glass-dock-portal` → fr-CanvasControlsDock + kf-ChromeDock + fr-FullscreenViewer only — **no Tooltip-stamp booking anywhere**.

## Scoreboard

**24 adjudicated roster rows — 2 BLOCKER-weight (both banked FOLDs) · 7 MAJOR · 10 MINOR · 5 INFO. Killed-claims register: 13 (12 re-reader kills ratified — several with my own re-derivation — + 1 seat-minted kill OF a re-reader cell). 7 missed finds banked (1 MAJOR net-new: the unstamped hint collapses its own dock; 1 demoted by ruling 1). 10 UNPROVEN → SS-13. 7 superlatives ratified.** The component survives: KEEP-AND-REPARAMETERISE — and after F.W1 it is not merely kept but **forced** (no single-component tooltip exists in glass-ui at v7.0.0; `./icon-tooltip` is deleted from the export map).

## Seat rulings (the incredulity work — where this seat changed the re-readers' return)

1. **The L-5/C-2 census is 3, not 4 — the "substantive" fourth site is a banked-killed premise; SEAT-MINTED KILL of a re-reader cell.** The re-readers confirmed C-2's four-site disabled-trigger census and named `FunctionInput.vue:193` "the substantive one (the Parseval explanation is disabled exactly when the user needs it)", then minted a CSS instrument (`disabled:pointer-events-none` in the Button CVA — real, my read) to close its browser half. But banked **fr-FunctionInput killed register #2** (= D-11.2/C-2(b), banked from the other side as fr-EquationView C·D-22) already proved the premise state CANNOT OCCUR: `effectiveN = ref(cachedRes?.result?.effective_n ?? 20)` — the `?? 20` is unconditional (re-proven by this seat at EquationView.vue:41; the prop chain FunctionInput.vue:13 → :193 re-read), and `compute_effective_n`'s three exits all pass `max(minimum=3, …)`, so `!effectiveN` is never true and the wand is never disabled. The re-readers policed exactly this failure shape in others (fr-CanvasControlsDock K-2/K-10: a row published on a premise a banked sibling defeats) and then committed it. The row folds to **fr-EditorControlsDock D-23/C-19 at the dock trio only** (:74/:79/:97 — `canUndo`/`canRedo`/`canDelete`, genuinely reachable states); missed-find 7's instrument survives as fleet knowledge, its consequence clause ("the Parseval tooltip is provably unreachable") dies with the premise. SS-13's engine question narrows to the dock three.
2. **Missed-find 1's routing is incomplete — the F.W1 target does NOT carry the cure; seat increment.** The re-readers proved the producer "named and fixed exactly this at 8.0.0" but never checked the uplift target: my `git show v7.0.0` of TooltipContent.vue shows **no `useDockParticipation`, no stamp, no dock comment** — the dock-participation machinery arrives only at HEAD/8.0.0. So F.W1-at-v7.0.0 inherits the collapse-under-the-hint defect unchanged, and FR-TT-9 discharges only under an 8.0.0 re-pin — which is the same fork missed-find 4 already forces for `collision-padding`. The two rows are one gate: **F.W1 must name its baseline before either can be sized** (see FR-TT-5/FR-TT-9).
3. **Ratifications with my own instruments where load-bearing**: the D-4 named-instance kill (my `.advanced-grid` read: 2-col, row-1 horizontal siblings, `:last-child:nth-child(odd)` full-width); the D-12(ii) kill (my TooltipTrigger.js read: `handleClick` AND `handlePointerDown` both call `onClose` — the hint closes before the menu paints); the D-6 kill (banked twice: fr-FrequencyGraph K-1 upholds the utility ALIVE at v7.0.0 — my own `git show` finds `@utility text-admin-label` at semantic.css:238 — and fr-App MG-β banks the 7-site/4-file census); the L-2 magnitude kill (my reads: `useGraceArea` lives only in TooltipContentHoverable:80, mounted only inside `Presence present={forceMount || open}`, document listener gated on a non-null polygon with watchEffect cleanup — at most ONE machine, open-lifetime only); the D-7 upgrade (my v7.0.0 floating.ts read: `collision-padding`/`collisionPadding` both in `RETIRED_FLOATING_ATTRS`, stripped by `floatingContentAttrs` — the value is deleted, not merely un-typed); the FR-CP-3 anti-cure lock (my PopperRoot.js read: `renderSlot` unconditional — single-rooting the shim cures nothing).

## Adjudicated defect roster

### BLOCKER-weight, folded to the bank (never re-booked)

- **FR-TT-1 = D-1 / L-4 — BLOCKER (FOLD).** The shim is the only label on **17** icon-only triggers and wires a DESCRIPTION (`aria-describedby`, only-while-open), never a NAME — trigger aria re-proven at my own TooltipTrigger.js read (the conditional describedby is the ONLY aria); text lands in a VisuallyHidden role=tooltip. Census: L-4's table is the citable one (Editor ×10 · Canvas ×6 · FunctionInput Wand2 ×1 = 17); D-1's 16 and its :42/:49 anchors are killed (register #8). FOLDS → **fr-EditorControlsDock C-3** (tooltip-XOR-label + touch rider; shim leg already routed "→ intake R3-7a + X-11") · **fr-CanvasControlsDock D-1/L-3/C-1** · **fr-EquationView D·D-B2** (the Wand2 as second nameless control). Producer corroboration at 8.0.0 (`:69-73`): "an icon-only trigger labels ITSELF" — the banked trigger-side-naming cure is the producer's own ruling. **→ F.W4** (naming legs, all 17) **+ F.W3** (the shim leg keeps its R3-7a/X-11 identity). Promotion note stands: BLOCKER at any F.W9/W10 axe close-gate.
- **FR-TT-2 = D-2 — BLOCKER (FOLD).** No touch path at any of the 35 callsites — three independent arms at my own reads (`pointerType === "touch"` return · pointerdown onClose · focus suppressed while isPointerDown) — and keyboard structurally dead at 8 (as-child onto non-focusable roots; reka binds `focus`, not `focusin`). Touch rider BANKED at fr-EditorControlsDock C-3 and fr-FunctionInput D-11.1 (the preset pedagogy is hover-only); the keyboard limb books with FR-TT-12; the "+3 by disablement" arm rescoped per ruling 1 (dock trio, banked D-23/C-19). **→ F.W3/W4** (the shim re-parameterisation must state its input-modality contract; touch affordance is a design decision, not a prop).

### MAJOR (7)

- **FR-TT-3 = D-3 + missed-find 6 — MAJOR.** No `max-width` anywhere in the chain (4.0.0 chunk class string verbatim, no `max-w-*` · v7.0.0 likewise · style.css no tooltip rule · reka publishes `--reka-tooltip-content-available-width` and both pins discard it — all my reads), against unbounded input (`preview ≤101 chars` + `summary` unbounded, paperTree.ts my read). STRENGTHENED inside the design system: the sibling floating register in the SAME package bounds itself — `hover-popover.css:28 max-width: 16rem`, in a sheet whose own docblock says it exists as a reaction to the tooltip register — and EditorControlsDock mounts both registers side by side. D-3's arithmetic framing killed (register #9); magnitudes → SS-13. **→ F.W3** (the re-parameterised shim carries a measure — reka's available-width var is the free ceiling) **+ GLASS-RELAY** (the internal bound inconsistency; standing owner edict).
- **FR-TT-4 = D-5 / C-3 + missed-find 3 — MAJOR, cure RETIRED.** Rich `#content` is announced as one unpunctuated `textContent` run (`ariaLabel = props.ariaLabel || textContent`, `toDisplayString`, no separators — my ContentImpl read; live at CoefficientsSpectrum:105-120 and FunctionInput:198-210). The defect stands; the corpus's unanimous cure ("forward `ariaLabel`") is **repudiated by the producer**: declared at 4.0.0 and v7.0.0, DELETED at 8.0.0 with a standing comment (`:69-73`) — the content-side override replaced the very node the describedby reads, announcing text it did not paint. Prescribing it routes F.W3/W4 at a prop the adoption chain is retiring. Re-targeted cure: trigger-side names (FR-TT-1's seam, the producer's own ruling) + authoring the two rich bodies so their textContent reads correctly. **→ F.W3/W4.** Cross-repo relay note (not a re-adjudication): banked kf-TimelineHoverPreview MISSED-1 prescribes the same `ariaLabel` cure at KF.W7 against glass 7.0.0, where it still exists — that formation should be told the prop dies at 8.0.0.
- **FR-TT-5 = D-7 (upgraded) + missed-find 4 — MAJOR.** `Tooltip.vue:33`'s `:collision-padding="8"` at the F.W1 target is not merely un-typed — it is **STRIPPED** (`RETIRED_FLOATING_ATTRS` :28/:30 + `floatingContentAttrs` filter, my v7.0.0 read), with `avoidCollisions: true` hardcoded and reka's `collisionPadding: 0` floor reasserting; at producer HEAD the filter is gone and `{...attrs}` forwards wholesale. D-7's "runtime survives" consequence clause and D·S-2/L·S-2/C-4's durability assumption are killed (register #2/#11). AND the corpus already contradicts itself on this exact line: banked **fr-FrequencyGraph K-1** rules tag-decides (K-12 baseline law) while banked **fr-CoefficientsSpectrum K-1** killed the same retirement finding on HEAD-`7d9abe08` evidence while conceding "true-at-tag" — both re-proven by this seat. **→ F.W1 re-pin gate**: name the baseline; re-resolve the fr-CoefficientsSpectrum K-1 kill at whichever baseline is named; re-express the padding as a declared prop (or accept the 0-floor deliberately) at uplift. Joint gate with FR-TT-9 (ruling 2).
- **FR-TT-6 = D-8 (a,b,c) — MAJOR.** At the uplift the chip's padding ratio inverts (px-3/py-1.5 = 2.00 → √φ-ladder 0.79 — class strings read at both pins), the `text-sm` line-height pairing is dropped (`text-(length:--tooltip-text)` is font-size-only; leading falls to fourier's `html` rule), and the type rung is demoted (`--tooltip-text: var(--type-caption)`, floor 0.75rem) under a producer comment calling itself "the token-backing of the bare `text-sm`" — whose backing is `--type-small` (floor 0.875rem). Import-safe, visually material. **→ F.W1** (budget a tooltip visual re-baseline; nothing breaks at the import boundary) **+ GLASS-RELAY** (the offsets.css:78-82 prose defect).
- **FR-TT-7 = L-1 / C-1 — MAJOR (FOLD → fr-CoefficientsPanel FR-CP-3).** Fragment root ⇒ the entire `coeff-list` TransitionGroup register is dead (FLIP's `instanceof Element` gate; one dev warn per row at the PopperRoot boundary). The banked row carries the **anti-cure lock** this seat re-proved (PopperRoot's `renderSlot` is unconditional — C-1's "make the shim single-root" arm is killed, register #6): the cures are keying the `v-for` on the `.coeff-row` element (Tooltip inside) or an element-root primitive. C-1's portal provenance corrected (reka's TooltipContent renders NO portal — the portal is glass's own; my TooltipContent.js read). Attrs-dropped limb (L-1c/C-6) latent, rides FR-TT-15. **→ F.W3/W4 by reference.**
- **FR-TT-8 = L-3 — MAJOR.** 35 authored callsites ≥86 mounted instances **cross-route** (never simultaneous — the re-readers' nuance adopted); the load-bearing figure is **51 TooltipRoots on first paint of `/paper` from ONE authored callsite** (my tex counts: 51 `\section{`, 31 lazy subsections, 0 starred; PaperSidebar :65/:70/:87/:88 re-read). Portal + 6 listeners + timer paid per instance while closed; only content is lazy. R3-7a's F.W3 budget restates as 35 callsites / 9 consumers / ≥86 mounted; X-9's scope law applies first. **→ F.W3 + F.W4**; exact denominator → SS-13.
- **FR-TT-9 = missed-find 1 + ruling 2 — MAJOR (NEW — the re-readers' one net-new MAJOR, ratified with a seat increment).** The glass 4.0.0 Tooltip is the ONLY floating surface in the package that emits no `data-glass-dock-portal` stamp (stamp census my own: HoverPopover/Popover/Dropdown/Select all stamp; the tooltip chunk greps 0 for `dock`), so for the **20 tooltips mounted inside a GlassDock** (Editor ×10 + Anim ×4 at `:collapse-delay="2000"`, Canvas ×6 at default 2000), moving the pointer onto the hint reads to the dock as "the pointer left the dock" (`M(e)` → not-contained / not-stamped / not-morph-edge → `D()` → collapse at 2000ms — my dock.js reads) and the dock collapses under the very control being inspected. Compounds FR-TT-10 (hoverable-on invites the pointer into the content). Producer corroboration verbatim at 8.0.0 (`:43-48`); **v7.0.0 does NOT carry the cure** (ruling 2 — my tag read). Not banked anywhere (registry sweep; fr-CanvasControlsDock:122 books the stamp as HoverPopover's superlative and never asks about the Tooltip; fr-FullscreenViewer FV-1 is a distinct z-identity). **→ GLASS-RELAY** (live-at-4.0.0 exposure; cured 8.0.0) **+ F.W1 re-pin gate** (joint with FR-TT-5: v7.0.0 inherits it) ; observed collapse timing → SS-13.

### MINOR (10)

- **FR-TT-10 = L-2 (rescoped) — MINOR.** Hoverable content is hard-wired on for all 35, but the standing-cost claim is killed (register #7, my reads): the grace machine exists only while content is mounted, at most ONE at a time, torn down on unmount. Survives as: the default serves 2 rich callsites and taxes 33 one-worders, cure is one line — **but the cure conflicts with D §6's banked WCAG 1.4.13 CLEARED pass** (hoverable is the *requirement*; `disable-hoverable-content` would delete the pass). Cure adjudicated separately from defect (ColorSpaceSelector addendum-2 law). **→ F.W3** — only with a 1.4.13-preserving design; otherwise a deliberate no-op, recorded.
- **FR-TT-11 = L-5 / C-2 (rescoped by ruling 1) — MINOR (FOLD → fr-EditorControlsDock D-23/C-19).** Disabled triggers are unreadable tooltips at the dock trio (:74/:79/:97) only; the FunctionInput site is a banked-killed premise. Dock family CSS is opacity+cursor only (no `pointer-events` rule — my dock.css read), so the engine residue is real for the three → SS-13, scoped.
- **FR-TT-12 = L-7 + D-2(keyboard) + C-8 + D-12(i) — MINOR.** The `as-child` contract silently accepts non-focusable (`SliderControl` div root ×5, `.coeff-row` ×12–40), `display:none` (`hidden sm:block`), and SVG (`EllipsisVertical` inside DockDropdownTrigger — where the tooltip's `data-state` is stamped on the `<svg>`, inert today per the banked fr-CanvasControlsDock instrument) triggers; multi-node/text slots merge onto nothing with no warning; the docblock never states the one-focusable-element-child contract. **→ F.W3** (documented contract + DEV guard in the re-parameterised shim) **/ F.W4** (per-site: focusable hosts or naming).
- **FR-TT-13 = D-10 / C-4 / L-8(offset) + D-15 — MINOR.** Three uncoordinated anchor gaps in one dock (house 4 / tooltip 6 / menu 8 — AnimationControls:109 my read); no offset token exists in either pin; the `?? 'top'` coalescer is the one place the shim editorialises about defaults and it chose the redundant knob. **→ F.W3** (the literals become declared, rationalised parameters) **+ GLASS-RELAY** (the missing floating-offset token).
- **FR-TT-14 = D-13 — MINOR.** At 4.0.0 the tooltip's reduced-motion end-state is a blanket 0.01ms truncation (a11y-overrides.css read whole — the guard also clamps transitions, D's partial cite corrected by the re-readers); glass 7 authors it properly (`[data-reveal="tooltip"]` reveal.css **:112** — D's :118-127 was a 6-line drift — + `glass-reveal-out-reduced`). The uplift's clearest design win here; no shim change. **→ F.W1** (discharged at uplift; record only).
- **FR-TT-15 = D-14 / L-8(amputation) + L-1(c)/C-6 — MINOR.** The shim exposes 2 knobs and amputates the ones that fix its own defects; `class` is worse than absent (fragment root drops it with a dev warn — latent, 0 live sites). With FR-TT-4's ruling the `ariaLabel` leg is DROPPED from the re-parameterisation; the surviving list: measure (FR-TT-3), `align`/`class`-with-element-root-decision, hoverable policy (FR-TT-10), offset rationale (FR-TT-13), contract guard (FR-TT-12). **→ F.W3** — this row IS the re-parameterisation surface; S-1's conditional-forwarding law (bind only what the consumer passed) governs the implementation.
- **FR-TT-16 = L-6 / C-7 / D-11(structural) — MINOR (FOLD → fr-CoefficientsPanel FR-CP-22).** `v-if` on the trigger, not the tooltip — a mounted popper tree around a comment node; the banked row also carries D-11's copy limb in its sharper form (`Math.min(40, total)`), so the whole row is a re-book. **→ F.W3/W4 by reference.**
- **FR-TT-17 = L-9 / C-5 / C-11 + D-9(contract) — MINOR (latent, 0 live violations).** `text?` and `#content` independently optional (empty glass chip legal); `text` silently ignored under `#content`. D-9's "live" claim killed (register #4 — my tex sweep concurs: the sole no-top-level-prose subsection has four `\paragraph` prose blocks); the contract hole survives at the type level. **→ F.W3** (discriminated contract in the re-parameterised shim); the parser enumeration → SS-13.
- **FR-TT-18 = missed-find 2 — MINOR (NEW).** Four tooltips whose triggers live INSIDE a HoverPopover panel paint beneath the panel that contains them — `--z-tooltip: 120` under `--z-popover: 130` (my token read), both body-portalled fixed siblings, the panel's `backdrop-filter` blurring the hint behind it; `avoidCollisions` measures the viewport, never the containing panel. Sites re-verified: CanvasControlsDock :53/:58 inside the :44 panel; EditorControlsDock :142/:147 inside the :134 panel — all four also among FR-TT-1's 17 nameless. Not banked (fr-CanvasControlsDock D-7 = padding register, C-13 = token scope; fr-FullscreenViewer FV-1 = fullscreen backdrop — distinct identities). **→ F.W3/W4** (side-flip the nested four, or carry the tier question on the FV-1 relay letter); chip-in-panel-rect geometry → SS-13.
- **FR-TT-19 = D-4 (rescoped) — MINOR.** The named instance is killed by its own falsifier (register #3 — my grid read: horizontal siblings). Survives: the generic mechanism (34px chip + 6 offset = 40px upward reach against a 10px row gap) with exactly one candidate instance (row-2 Smoothing over row-1), unproven; and the design judgment that 26-of-35 callsites accept a `top` default in vertically dense stacks. **→ F.W3** (per-density side defaults are a re-parameterisation input); the overlap measurement → SS-13.

### INFO (5)

- **FR-TT-20 = L-10 / C-9 — INFO.** Two import identities (8 barrel + PaperSidebar:2 deep SFC — my grep); the deep importer contributes ~60% of mounted instances. Same module id, zero runtime cost, pure census hazard. **→ F.W3** (normalize before the migration executes; the choke-point count is 2, not 1).
- **FR-TT-21 = L-11 — INFO (corpus correction).** R3-7a's "+1/+3" parenthetical is a grep artefact (App hit = `<TooltipProvider`; adapter's 3 = docblock + two differently-named components); the 35/9 budget stands — my own 39-hit grep concurs. **NO-WAVE-OWNER** (SS-3/SS-4 spec authoring consumes the correction).
- **FR-TT-22 = C-10 — INFO.** Zero coupling to value.js / keyframes.js / the 45-op API; the chain terminates at reka + Vue (my whole-file read: one import). A zero-cost row on the F.W2 ledger — a negative budget statement. **NO-WAVE-OWNER.**
- **FR-TT-23 = D-17 — INFO (FOLD → fr-FrequencyGraph FR-FG-15).** Muted second-tier foreground inside translucent `glass-floating` has no contrast floor by construction; anchors corrected (labels :111/:113/:115/:117; :108 is `font-semibold` — my read). Already routed as SS-13 residue 9 by the banked row.
- **FR-TT-24 = missed-find 5 — INFO.** `./icon-tooltip` does not exist at v7.0.0 (my `git show` grep → 0; both keys present at 4.0.0 per the banked 80-key census) — so the F.W3 hazard L·S-1/D·S-3 warn against cannot compile at the uplift target, and the load-bearing statement is simpler: **after F.W1 there is no single-component tooltip in the package at all; the local shim is the only possible home for the `side` + `#content` + provider-non-nesting contract.** R3-7a's "barrel → `@mkbabb/glass-ui/tooltip`" framing restates as re-parameterisation of the shim, not retirement. **→ F.W3** (framing) **+ F.W1** (break-surface note).

## Killed-claims register (13)

1. **D-6 (both limbs — `text-admin-label` "deleted at 7.0.0" + "an ADDITION to the break surface").** The utility EXISTS at the tag (`semantic.css:238`, my git show); D-6 ran its falsifier against a version-lying working tree — the kill is already banked as **fr-FrequencyGraph K-1** (tag-decides, with the 08-04→08-09 suspicion window) and the census as **fr-App MG-β** (7 sites/4 files — D-6's own list missed FrequencyGraph:200). A kill that itself folds; nothing new books.
2. **D-7's "Consequence" clause ("runtime survives; Vue camelizes the attr").** FALSE at v7.0.0: `RETIRED_FLOATING_ATTRS` contains both spellings and `floatingContentAttrs` strips them (my read) — deleted, not un-typed. Props up D·S-2; see #11.
3. **D-4's named instance ("Max Contours' hint paints across Min Area %").** Killed by D-4's own stated falsifier, run by this seat: `.advanced-grid` is a 2-col grid; the two are row-1 horizontal siblings; Smoothing spans row 2 (`:377-385`). The premise "stacks three SliderControls" is false.
4. **D-9's "this is live" clause.** My tex sweep concurs with the re-readers': the single candidate (`\subsection{The Discrete Cosine Transform (DCT)}`, tex:2687) carries four `\paragraph` prose blocks the parser emits as string blocks. The contract hole survives at MINOR (FR-TT-17); the MAJOR-with-live-claim dies. Parser run → SS-13.
5. **D-12(ii) ("nothing closes the hint when the menu opens").** Refuted twice in the file D-12 cites, at my own read: `handleClick` and `handlePointerDown` both call `rootContext.onClose()` (nothing sets `disableClosingTrigger`); the hint closes on pointerdown, before the menu paints. The z-arithmetic is correct and moot.
6. **C-1's prescribed cure, first arm ("make `<Tooltip>` single-root").** Killed by the banked FR-CP-3 anti-cure lock, re-proven here: `PopperRoot` returns `renderSlot` unconditionally — a Fragment regardless of child count. Only the move-the-transition-onto-the-row arm survives. C-1's portal provenance also corrected (the portal is glass's, not reka's — my TooltipContent.js read).
7. **L-2's magnitude ("35 grace-area machines" / standing document-listener cost).** `useGraceArea` is instantiated only inside `TooltipContentHoverable`, mounted only inside `Presence(open)`; the document listener is polygon-gated and cleanup-torn; `isAnotherGraceAreaTrigger` closes the prior hint — at most ONE machine, open-lifetime only (all my reads). Mechanism survives at FR-TT-10 with the 1.4.13 cure-conflict recorded.
8. **D-1's CanvasControlsDock census (:42/:49; total 16).** :42 is a `template v-if`, :49 a closing tag (my grep); the six real Tooltips are :53/:58/:70/:76/:86/:92 and the true total is 17 (L-4's table cites).
9. **D-3's arithmetic framing ("≥103 characters and unbounded above").** A non-sequitur from an upper-bound expression; paperTree caps the preview at 101 and pushes parts only if truthy (my read). The absence-of-any-bound survives (FR-TT-3); the floor does not.
10. **D-11's copy limb ("a hint that contradicts its trigger").** The weak reading of a defect banked in sharp form at FR-CP-22 (`Show top 40 of N` false for 12<N<40; cure literal `Math.min(40, total)`). Re-book, both limbs.
11. **D·S-2's durability clause ("survives the uplift at runtime") — and L·S-2/C-4's inherited assumption.** Same evidence as #2: the refinement is pin-local, silently revoked at v7.0.0, restored by accident at HEAD. Restated in FR-TT-5 and superlative 6.
12. **D·S-3's `side` count (9).** Eight: PaperSidebar :70/:88 · Canvas :70/:76/:86/:92 · FunctionInput :188 · CoefficientsSpectrum :83 — L·S-1's enumeration is the citable one.
13. **[SEAT-MINTED] The re-readers' L-5/C-2 fourth census site + missed-find 7's consequence clause.** Ruling 1: `FunctionInput.vue:193`'s disabled state is unreachable (`effectiveN ?? 20` unconditional, backend exits ≥3 — banked fr-FunctionInput killed #2 / fr-EquationView C·D-22, re-proven at my own reads). The census is 3; "the substantive one" and "the Parseval tooltip is provably unreachable" die; the CVA `disabled:pointer-events-none` instrument survives as fleet knowledge with no live subject in this app.

## Superlatives (adjudicated, provenance preserved)

1. **L·S-1 — the IconTooltip decode (per-instance provider, 250ms, no `side`, no `#content`) is correct and byte-verified by the re-readers; ratified.** Under FR-TT-8's fan-out it would be 51 private providers destroying App.vue:23's grouping. With FR-TT-24 the migration it warns against also cannot compile at v7.0.0 — the warning was right and is now moot; the superlative's residue is the shim-is-forced framing.
2. **C·S-1 — the zero-prop pass-through at `Tooltip.vue:26` is the non-obvious right call** (declared-but-unset booleans would have pinned tooltips closed via `passive: props.open === void 0`); C alone saw it, recorded its own tension with C-2, and named the correct cure shape (conditional forwarding). Governs FR-TT-15's implementation.
3. **C's §4 falsifier ledger** — four self-killed high-severity drafts published with decoders shown (incl. the mangled-re-export scare, independently re-derived by the re-readers). The best epistemic artefact in the three axes; the SS-3/SS-4 spec authors should require the practice (the fr-CanvasControlsDock K-2/K-10 lesson from the other direction).
4. **L-11 — model corpus hygiene**: a correction that reproduces exactly (my 39-hit grep concurs) and disturbs nothing load-bearing.
5. **The subject's own docblock (`Tooltip.vue:2-12`) is load-bearing and drift-free** (my whole read: `text`/`side`/`#content` all present and as documented) — it is why the census could classify the file without reading 35 callsites. Its one omission is the FR-TT-12 contract, which the F.W3 edit adds.
6. **`:collision-padding="8"` — a real refinement, durability struck**: the only layer in the chain keeping a hint off the viewport edge at 4.0.0 (reka floor 0, my read); silently revoked at v7.0.0; must be re-expressed as a declared prop at F.W1 (FR-TT-5).
7. **L·S-3 / C·S-3/S-4 — Goldilocks**: 38 lines, one import, no state, nothing to tear down (my whole read); the reason the census's "0 direct reka-ui imports in web/src" row is true — the primitive triple absorbed once, not 35 times; `as-child` over a wrapper div is why no dock flex row broke.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **FR-TT-3 magnitudes** — rendered hint widths (font-metric dependent; note fourier's 18px root <768px ⇒ `text-sm` ≈15.75px there). The absence of any bound is proven; the pixels are not.
2. **FR-TT-19's surviving instance** — the row-2 Smoothing tooltip's measured overlap of the row-1 pair against the 10px `.advanced-grid` row gap (40px reach is exact).
3. **FR-TT-9 observed** — hover a dock icon, move onto its hint, time `.glass-dock` collapse against the 2000ms delay on `/visualize` (Editor + Anim) and Canvas (default). The stamp absence and scheduler path are static-proven; the user-visible timing is not.
4. **FR-TT-18 geometry** — whether the nested four's chips land inside their containing panel rect (z-order 120<130 and the shared body stacking context are proven).
5. **FR-TT-10 residual** — pointermove cost with a live grace polygon during a 60Hz contour drag, now scoped to the open-tooltip window only.
6. **FR-TT-8 denominator** — mount `/paper`, count `[data-grace-area-trigger]`; predicted 51 at first paint (tex-derived).
7. **FR-TT-17 data sweep** — enumerate built `paperSections`; confirm no empty `getPaperPreview` (tex-level sweep found no candidate; the parser must run).
8. **D-16** — whether `data-material="overlay"` is truly inert on the v7.0.0 tooltip (attribute confirmed at the tag; `material-roles.css`/`resolveSurfaceClass` not re-read there). PLAUSIBLE; producer-relay candidate if confirmed.
9. **FR-TT-23 ratios** — muted-on-glass contrast over canvas/paper-texture/spectrum backdrops = banked FR-FG-15's SS-13 residue 9.
10. **FR-TT-11 engine residue** — does any engine dispatch pointermove to a disabled `<button>`: dock trio only (`.dock-icon-button:disabled` has no `pointer-events` rule; the Button family is CSS-closed and its sole site is premise-dead per ruling 1).

## Closing verdict

**KEEP-AND-REPARAMETERISE — ADJUDICATED.** The 38-line shim survives all three axes and both re-readers, and after F.W1 it is *forced* (no single-component tooltip exists in the package at v7.0.0): the correct disposition is to harden this one seam — trigger-side naming (the producer's own 8.0.0 ruling), a measure, the documented focusable-single-child contract, rationalised offsets, the discriminated body contract — **before** F.W1 re-proportions the surface underneath it (FR-TT-6), with the F.W1 baseline named first (FR-TT-5/FR-TT-9's joint re-pin gate: `collision-padding` and the dock-stamp cure sit on opposite sides of the v7.0.0/8.0.0 fork, and two banked K-1 rulings currently disagree about which side governs). Both BLOCKER-weight identities are banked folds executing at F.W4; the one net-new MAJOR (the unstamped hint collapsing its own dock) rides the standing glass-ui BH relay with its 8.0.0-only cure named. 24 rows, 13 kills, 7 missed finds, 10 SS-13 residues — every row leaves with exactly one disposition, original ids preserved.

— end of apotheosis. The three challenge files and the re-readers' return remain immutable beside this record per E-1/E-3.
