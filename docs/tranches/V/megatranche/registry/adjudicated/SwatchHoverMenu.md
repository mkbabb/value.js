# SwatchHoverMenu — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Seat**: fresh Fable adjudicator (M-23/L-18 apotheosis seat) — the incredulity seat, not a transcriber. **Date**: 2026-08-06 · **HEAD**: `3487d73e` · branch `tranche-u`.
**Subject**: `demo/palettes/browser/card/SwatchHoverMenu.vue` (93 L) + the composable spine it fronts (`useHoverPopover.ts` 67 L · `useLeaveTimer.ts` 17 L) and its two consumers' call surfaces.
**Corpus**: `audit/components/SwatchHoverMenu/` — three canonical challenge files (challenge-D-design · challenge-C-implementation · challenge-L-library, all **EXISTS-ORIGINAL** in `registry/HYDRATION-LEDGER.md:127-129`, area-palettes.json, 1 payload each — re-read by this seat) + `evidence/` (5 PNGs, IHDR headers read by this seat). **No r2 challenge file exists on any axis** (`ls` re-run: 3 files + evidence only) — the canonical r1 files govern. Challenge anchors were minted at HEAD `c654824e`; every cited SwatchHoverMenu/useHoverPopover/useLeaveTimer anchor re-resolves byte-true at `3487d73e` (this seat's whole-file reads).
**Workers adjudicated**: hostile re-reader **R1** (design-and-usage: 14 confirmed · 7 kills · 8 missed · 6 superlatives) · hostile re-reader **R2** (library-and-code: 24 confirmed · 7 kills · 8 missed · 6 superlatives).
**Sibling-record identity guard (anti-rename law) — the decisive context NEITHER re-reader consulted**: this component's spine is already adjudicated three times over — `adjudicated/PaletteCard.md` (PC-2 floating-panel · PC-3 swatch-not-a-control · PC-21 clipboard · PC-23 timers · PC-38 size triplication · PC-39 PaletteDialog ghost), `adjudicated/PaletteCardSwatches.md` (whose declared subject *includes* SwatchHoverMenu.vue + both composables: PS-1/PS-2/PS-3/PS-15/PS-17/PS-22/PS-28/PS-29/PS-34/PS-36/PS-37/PS-38 + kills K-4/K-8/K-9), and `adjudicated/CurrentPaletteEditor.md` (A-1/A-3/C-6/C-9/C-12/ADJ-m5 + the U-5/U-7/U-8 residue). **One identity each — this record folds onto those rows and never re-books them**; it adds only what this corpus alone establishes, and it demotes two of the re-readers' "missed" findings that the registry had already banked.
**Method**: both returns were attacked before either was adopted. Seven verdict-level questions (D-11, L-6 severity, the L-3 enumeration, M-A severity, R2's clipboard-rejection missed-find, R1's M-D/M-F/M-H novelty claims, the D-8 tally) were re-derived against the tree and ruled; two kills were sealed by this seat's own executions; two of the readers' missed findings were killed or demoted by the registry itself.
**Arbiter's own instruments (all static + node + PNG-header reads; zero browser tooling — probe parsimony + task law)**: whole-file reads of SwatchHoverMenu.vue, useHoverPopover.ts, useLeaveTimer.ts; PaletteCard.vue:244-258/:318-336, useSwatchActions.ts:30-95 (+ the un-quoted pruning watch :54-59, last touch `f2c8f565` 2026-07-17), PaletteCardSwatches.vue:10-35, CurrentPaletteEditor.vue:283-292, MixSourceSelector.vue:274-283; **producer bytes**: WatercolorDot.vue.d.ts (whole docstring + 6-prop union, no `tag`), `dist/watercolor-dot.js` `renderSlot` count **0**, `dist/popover-BQGYXZyO.js` **whole** (:39 `matchMedia("(pointer: coarse)")` captured ONCE in a `let`; trigger defaults `click`/250/150; `side:{default:"bottom"}`, `sideOffset:{default:4}`, `avoidCollisions:!0`; the glass PopoverTrigger wrapper recomputing attrs + `as:"button"` on BOTH branches; `:145` `let { role: e, "aria-modal": t, ...r }` stripping consumer role; `role:"group"`+`aria-label` on the **hover branch only**, bare `aria-label: t.ariaLabel` on the click branch), Popover.vue.d.ts + PopoverContent.vue.d.ts (`ariaLabel?: string` — "Accessible name for the hover group or click dialog"), `dist/popover.js` (exports exactly Popover/PopoverContent/PopoverTrigger; `./popover` in exports map; **no** `./hover-card` key; `HoverCard` = 0 in index.d.ts), `useClipboard-*.js` head (**cannot reject** — every failure path returns `{ok:!1,reason}`); **executed**: the producer's own merger `t()` from `class-names-Cpy5eaBk.js` on the real strings → `w-72` DROPPED, `px-(--overlay-pad-inline)`/`py-(--overlay-pad-block)`/`p-1.5` ALL SURVIVE (padding-x/padding-y/padding = three separate conflict groups, read raw); tailwindcss **4.3.0** registration order `p`@106031 < `px`@106047 < `py`@106071 in dist/lib.js (and mjs @95459+) — px/py emit later, win the (0,1,0) tie; **censuses re-run exit-checked**: `floating-panel` (3 repo hits = 1 use + 2 prose; **0** in the whole glass dist), producer `@keyframes` enumeration = **46 names, none** of floating-panel-in/dialog-*/card-menu-in, `git show c84504d3^:demo/@/styles/animations.css` keyframe list (floating-panel-in :7, dialog family :22-49, edit-drawer-in :57, card-menu-in :71, golden-shimmer :86), zero live refs to any deleted name in demo (blast-radius grep), `tag="button"` = **6 live + 1 comment** (GenerateControls:193), the 11-utility recipe = exactly **6** across 2 files, demo/ui = **19 dirs: 17 one-line bare-root + alert 11-line bare-root + input 1-line `/forms`**, "Add current color" e2e locators = **6 sites/5 specs** + palette-save, `.github/workflows/*.yml` playwright/e2e = **0**, tsconfig.base.json:11 `exactOptionalPropertyTypes: true`; **canon verbatim**: VISUAL-CONSTITUTION.md:102, PROPORTION-AUDIT.md:51 (PR-07) + :56 (PR-12) + :70-79 (§5.5/.7/.9 and the :77=item-12 / :79=item-14 anchor truth), ADDENDA.md:24 (V-A142) + DISPOSITION-LEDGER.md:20; motion-quarantine grep (**no row** names SwatchHoverMenu/floating-panel; the M-26 hit is another component's); evidence PNG IHDRs: floating-panel-orphan **1440×40**, hover-fullpage **1440×940** vs hover-viewport **1440×900**, mobile-tap 1170×2532.

## Scoreboard

**30 rows survive: 3 BLOCKER · 6 MAJOR · 15 MINOR · 6 INFO** — of which **13 are identity-folds** onto PC-/PS-/A-/ColorSpaceSelector rows (cross-referenced, never re-booked) and **8 are genuinely new** to the registry. **14 claims KILLED** (12 reader kills upheld after re-derivation — two sealed by this seat's own executions · 2 arbiter's own, one against a reader's missed-find, one against a novelty claim). **16 missed findings adjudicated** (7 promoted as new rows/riders · 6 identity-folded · 2 killed/demoted · 1 folded as evidence). **Zero NO-WAVE-OWNER rows** — every surviving identity has a ledger owner.

Blocker-weight sentence: **the component is inoperative on every input modality it claims to serve (hover panel unstyled/off-flow SH-1; touch trigger structurally discarded SH-2), and the ratified canon already carries a terminal REMOVE for its concept by name (SH-3) — everything else is the receipt.**

## Ruled disagreements and arbiter corrections (the seat's actual work)

| # | Question | R1 | R2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| **R-A** | D-11 (tap targets "below the floor") | CONFIRMED MINOR (28px real; house floor can't reach) | KILLED (28×28 > WCAG 24px; REPORT rows captured with zero swatches mounted; buttons authored by consumers) | **R2 wins — KILLED as filed (K-6)** | 28 = 6+16+6 arithmetic checks but breaches no WCAG 2.2 AA floor; the REPORT.md smallTapTargets attribution is impossible (both readers + L-12 establish zero swatches in every capture); ownership sits in the consumers' `#actions` slots. R1's narrow residue (no seat/hit-area hook) is not an independent row — the X-W7.c named seat spec (≥44px, GenerateControls B1) already IS that obligation. Anchor slips recorded: PR-12 is :56, §5 item 12 is :77, item 14 is :79 (my reads — R1's K-6 correction exact). |
| **R-B** | L-6 severity (tsconfig paths drift) | MAJOR | MINOR (no live break; off-component) | **R2 wins — MINOR-as-fold** | Fact-identical to ColorSpaceSelector-L-14 ≡ CurrentPaletteEditor-L-11 ≡ GenerateControls row 20 — a quadruple-banked identity already FOLDed to X-W8 config parity with a born-RED generator gate. Severity follows the banked identity. **R1's M-H ("the comment authorising the block is itself false") is DEMOTED from missed-find to already-on-record**: ColorSpaceSelector.md L-14 already books the "CLOSED 8-key set … false by one" comment verbatim (K-13). |
| **R-C** | L-3's enumeration (19 shims) | "18 of 19 bare aliases" (killed only the alert row) | "17 of 19; alert 11-line; input via `/forms`" | **R2 wins — sealed by my census** | My per-file census: 17 × 1-line bare-root + alert (11 lines, bare-root, 2 statements incl. a type re-export) + input (1 line, **`@mkbabb/glass-ui/forms`** subpath). Root-barrel reach = 18/19 — exactly the census PaletteCardSwatches.md banked. R1 missed input's subpath; the core finding (popover shim = wrong channel beside a correct subpath in one file) is untouched. |
| **R-D** | M-A severity (PANEL_LAYOUT padding inert on touch) | MAJOR | MINOR | **R2 wins — MINOR (latent)** | The touch panel cannot open today (SH-2), so the divergence has no live witness; the const and both panels die under the one-Popover cure (PS-15). Booked SH-11 with the const's comment correction as a rider. Mechanism is REAL and sealed by my own execution of the producer's merger + the tailwind 4.3.0 registration order (cascade half carries a declared static-derivation caveat, U-6). |
| **R-E** | R2's missed #8 (clipboard "unhandled promise rejection … not even void-ed") | — (did not file) | filed INFO | **ARBITER KILL of the mechanism (K-12)** | `useClipboard-*.js` head, my bytes: missing API → `{ok:!1,reason:"no-api"}`; rejection → caught, `console.warn`, `{ok:!1,reason:"clipboard-api"}`. **`writeClipboard` cannot reject** — there is no unhandled-rejection hazard at PaletteCardSwatches.vue:16 or anywhere. Identical to PaletteCardSwatches.md R-2/K-9, which R2 did not consult. The discarded-result substance is the PS-23 ≡ PC-21 identity — folded, not re-booked. |
| **R-F** | R1's M-D (spread-in-v-for re-renders all N) and M-F (six-fold icon-button recipe) as "missed by every axis" | filed MINOR / MINOR | — | **Facts CONFIRMED; novelty KILLED (K-14)** | M-D is **PS-28** (ratified upgrade, PaletteCardSwatches.md R-3, 2026-08-06 — Vue semantics, no measurement required); M-F is **PS-8** (my recipe grep: exactly 6, same six anchors). True of the *challenge axes*, already banked by the *registry*. Both fold at their banked severities; R1's independent convergence is corroboration, recorded as such. |
| **R-G** | The trigger-chain trace (C-3/L-2 vs R1's K-4) | killed the chain (wrong by one hop — glass wrapper omitted) | confirmed the conclusion WITH the correct chain | **Both adopted — no conflict** | My popover bytes: the glass PopoverTrigger wrapper (`inheritAttrs:!1`, attrs recompute, `as:"button"`, `as-child` forwarded to reka on both branches) sits between reka and the consumer — the corpus's three axes traced reka directly and analysed the wrong producer. Conclusion (contract dies at WatercolorDot's `inheritAttrs:!1`) intact; the mis-trace is explained by SH-27's wrong-producer comment (M-G). |

## Adjudicated defect roster

Line anchors are HEAD `3487d73e`; re-resolve before any cure. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Wave vocabulary per `registry/CARRY-CUT-LEDGER.md` and the two sibling palette records (X-W1 gates · X-W7.c specimen/named-seat/one-Popover · X-W7.d verbs-to-inspector · X-W8 graph/config subtraction · X-W10 design/motion/canon law · CC-044/CC-003 blocked bank · glass-ui BH relay).

### BLOCKER

- **SH-1 — D-1/C-1/L-1 — CONFIRMED (BLOCKER), IDENTITY ≡ PC-2 ≡ PS-1.** `.floating-panel` (SwatchHoverMenu.vue:42) matches zero CSS rules anywhere (my grep: 1 use + 2 prose comments repo-wide; 0 in the entire glass 7.0.0 dist) → the teleported hover panel computes `position: static`, its inline `top`/`left` (useHoverPopover.ts:22-23) are inert per CSS 2.1 §9.4.3, and it lays out as a full-width block at document end. Provenance re-verified: `c84504d3` (2026-03-25) deleted `demo/@/styles/floating-panel.css` (`position:fixed; z-index:var(--z-overlay); …; animation: floating-panel-in …` — recovered via `git show c84504d3^:`) on a commit message claiming glass-ui provides it; it never has. R1's addition adopted: the deleted rule sat in `@layer components` and took `.floating-panel-item` with it. Live geometry corroborated in bytes without a browser: evidence IHDRs — orphan strip **1440×40**, fullpage **1440×940** vs viewport **1440×900** (the claimed 40px document growth). **Terminal: X-W7.c** (the hover path dies with the specimen split; PS-1's cure riders govern — `glass-floating` substitution stays REFUTED, restore-the-class stays refused). Exact rect numbers remain U-1.
- **SH-2 — D-2/D-3/C-2/C-3/C-4/L-2 — CONFIRMED (BLOCKER), IDENTITY ≡ PC-3 ≡ PS-2 ≡ CurrentPaletteEditor A-1 → CC-044.** `tag="button"`, `:aria-label`, `@click.stop` (:17-18/:32-35) and the entire `PopoverTrigger as-child` contract are silently discarded: producer d.ts declares `{color, variant?, animate?, cycleDuration?, range?, seed?}` — no `tag`; compiled dot is `inheritAttrs:!1`, forwards class/style only, renders an `aria-hidden` span with hard-coded `pointerEvents:"none"`, zero `renderSlot` (all my bytes). The touch Popover can therefore never open (R2's exhaustive openIndex-setter enumeration adopted: every writer is unreachable on a coarse pointer), the `click` emit (:90) is dead in both consumers, and the corrected trigger chain runs consumer → **glass wrapper** → reka Primitive/Slot → dead `$attrs` (ruling R-G). `f2c8f565` touched both siblings but not this file — the producer moved underneath an unmigrated call site. **Terminal: CC-044** ("six WatercolorDot impostors", one atomic cut, **BLOCKED-ON CC-003's Glass ≥8 trigger**; no pre-8 wrapper) **+ X-W7.c/d**; my census re-seals 6 live `tag="button"` + 1 comment canonising the phantom (GenerateControls:193); the 21-site `tag=` sweep identity stays banked at ColorSpaceSelector. Causation = consumer debt (GenerateControls R-D precedent); producer questions ride the standing **glass-ui BH relay**.
- **SH-3 — D-13 — CONFIRMED (BLOCKER, canon).** The ratified canon forbids the concept by name, verified verbatim by this seat: VISUAL-CONSTITUTION.md:102 ("The card body owns no … **hover-only swatch-action path** … the card's compact swatch strip remains noninteractive data with zero activation/focus/drag semantics"); PROPORTION-AUDIT.md:51 row **PR-07** ("Hover-only/unlabeled controls … REMOVE … palette hover paths retire into selected inspector"); ADDENDA.md:24 **V-A142** names `SwatchHoverMenu.vue:13-50` ("WatercolorDot is face-only and hover-only action panels are deleted. Born RED"), DISPOSITION-LEDGER.md:20 "BUILD W22/W23; RETAIN BORN RED" — still open. C reached V-A142 independently of D (different sections, same citation) — real convergence. **Terminal: X-W7.d** (the terminal REMOVE: per-colour verbs to the inspector; the strip becomes inert data). This row is why every cure below is a *transposition*, not a repair.

### MAJOR

- **SH-4 — D-4/C-5 + L-5 — CONFIRMED, IDENTITY ≡ PS-3 ≡ CurrentPaletteEditor C-6.** `aria-hidden="true"` (:45) over three natively-focusable, labelled `<button>`s (CurrentPaletteEditor:46/:49/:52, PaletteCardSwatches:41/:49/:56 — no tabindex, no disabled, no `inert`): axe `aria-hidden-focus`, WCAG 4.1.2. The :38-39 comment's stated mitigation ("the reka-ui Popover (touch path) is the accessible route") names the route SH-2 proves dead — so an entire action surface (the ONLY route to per-colour Add/Edit/Copy/Remove) has no keyboard/AT path on either branch, a product-level boundary decision made and documented inside a 93-line leaf (L-5's ownership arm, folded here). Regression provenance re-verified: `bda38b6c` ("fix(tranche-b/w1): W5 a11y corrections…") replaced A.W5's `role="toolbar"` + label with this attribute; the ledger still records the remediation as done. **Terminal: X-W7.d.**
- **SH-5 — D-5/C-7/C-14/L-4 + D-7 — CONFIRMED, IDENTITY ≡ PS-15 (fork) ≡ PS-17 (centring drift) ≡ PS-38 (five-file seam).** Two parallel implementations of one affordance branched on `canHover` (:8-25 vs :28-52), with the two consumers already drifted on the centring transform (PaletteCardSwatches:31 spreads `translateX(-50%)`; CurrentPaletteEditor:35 passes the bare reactive — both fed `left = rect.left + rect.width/2`). The producer already ships the fork correctly: glass Popover `trigger="hover"` + `openDelay` 250/`closeDelay` 150, pointer-adaptive via `(pointer: coarse)`, PopoverContent with portal/collision/dismissal (my d.ts + compiled reads) — and :61 already imports it. The double `useHoverPopover()` instantiation is **not** a defect (R2's kill upheld: per-instance composable state is the design). Ad-9 provenance (the A-era "collapse to one" recommendation answered by deduping only the padding string) stands. **Terminal: X-W7.c** (the one-Popover cure, buildable today — PS-15's decider) with SH-12/SH-13/SH-14 as its riders and SH-30's producer caveat on the relay.
- **SH-6 — D-8/C-6/L-11 — CONFIRMED and STRONGER THAN FILED (R2's K-tally adopted, sealed by my own enumeration).** `@keyframes floating-panel-in` was deleted at `c84504d3`, not moved or tokenized (edict 6) — and so were the **dialog family (4 blocks + media dups), card-menu-in and golden-shimmer** (my `git show` of the pre-image). The installed producer ships **46** keyframes, **none** of them floating-panel-in/dialog-*/card-menu-in — of the four families the surviving comment names (demo/styles/animations.css:2, echoed at demo/DESIGN.md:273), only `shimmer` is genuinely producer-supplied. Mitigation verified: zero live references to any deleted name in demo (my blast-radius grep) — the damage is confined to `.floating-panel`, dead for SH-1's separate reason. The false attribution has masked the regression for four months. **Terminal: X-W7.c** (motion returns via the producer's `glass-reveal`, inheriting PRM for free) **+ X-W10** (the two stale prose attributions die in the canon pass).
- **SH-7 — C-8 — CONFIRMED, IDENTITY ≡ CurrentPaletteEditor C-9-as-rescoped.** `${color}::${i}` puts the index inside the key identity (useSwatchActions.ts:45-53): removing index 0 misses every survivor's mapKey → N teardowns + N mounts under `TransitionGroup name="vj-enter"`; the reorder branch (:62-70) does the same; the computed side-effects the map/counter. Scope narrowing adopted (R1): confined to the CurrentPaletteEditor site — PaletteCardSwatches keys `${color.css}-${i}` outside any TransitionGroup. **Arbiter precision no corpus document carries**: a stale-key pruning `watch` sits at :54-59 (present since `f2c8f565`, 2026-07-17 — before the challenges) — it bounds map growth and changes nothing about the remount mechanism. **Terminal: X-W7** (identity-keying inside the specimen split).
- **SH-8 — C-9 + R2-missed#5 (e2e/CI legs) — CONFIRMED, IDENTITY ≡ CurrentPaletteEditor A-3 → CC-030/CC-031.** Zero tests reference the component (my grep); vue-tsc is green over the dead props by construction (R2 ran it; unknown component attrs are legal fallthrough); palette-save.spec.ts:35 targets an accessible name the tree cannot render — and R2's sharpening, re-sealed by my greps: **five more spec files** bind the same impossible locator family ("Add current color to the mix": o14×2, o15, safari/mix-flow, views/mix), while `.github/workflows/*.yml` contain **zero** playwright/e2e invocations, which is why six unresolvable specs have never gone red. **Terminal: X-W1** (CC-031 CI cliff + CC-030 visual gate; the seeded populated-state capture is that wave's matrix input — ≡ CurrentPaletteEditor U-9).
- **SH-9 — L-3 + R2-missed#6 — CONFIRMED, IDENTITY ≡ CurrentPaletteEditor L-4 (W-HYGIENE demo/ui extirpation) + PS-29/GenerateControls-23 (root-barrel `writeClipboard`).** `demo/ui/popover/index.ts` = one line re-exporting from the **root barrel** while glass publishes `./popover` (my exports read), and :61/:62 reach one package through two channels in adjacent lines; R2's four-channel census of the cluster (shim→root · watercolor-dot subpath · `/dom` subpath used correctly at useHoverPopover.ts:3 · bare root for `writeClipboard`) confirmed as a sharpening on the banked identities. Census per ruling R-C. The PI-6 contrast (card/index.ts reasons about barrels; demo/ui aliases by reflex) stands. **Terminal: X-W8** (one fold; byte figures stay unproven, U-8 — the cost is ownership, not weight).

### MINOR

- **SH-10 — R2-missed#3, PROMOTED (new to the registry — verified in my own source reads).** PaletteCard destructures `useHoverPopover()` **without `close`** (:246-255) and then hand-writes `openPopoverIndex.value = null` in all three action handlers (:321-333) — the ref half of `close()` without the `cancelLeave()` half, leaving the 250ms leave timer armed after every action — while the sibling consumer takes `close: closeCurrentSwatchPopover` and calls it at all three verbs (useSwatchActions.ts:37/:78/:86/:92, my read). Same three verbs, two close disciplines, only one disarms the timer SH-16 shows is never disposed. **Terminal: X-W7.c** (dies with the composable; until then the fix is one destructure).
- **SH-11 — R1-missed-1 ≡ R2-missed-1 (independent convergence), PROMOTED (new).** `PANEL_LAYOUT` (:64-66) fails on the one axis all three challenge axes concede it succeeds at: its `p-1.5` never paints on the touch branch. Sealed by this seat's own execution of the producer's merger (`px-`/`py-`/`p-` are three separate conflict groups — all survive; only `w-72` dies to `w-auto`) + tailwind 4.3.0's registration order (px/py emit after p, win the tie): the touch panel pads at the producer's 1rem × 1.272rem while the hover panel — were it ever painted — pads 0.375rem isotropic. The anti-drift const conceals a >3× divergence on exactly the axis it was written to hold. **Terminal: X-W7.c** (the const dies with the fork; severity per ruling R-D; cascade caveat U-6).
- **SH-12 — R1-missed-2 ≡ R2-missed-2 (independent convergence), PROMOTED (new) — the cure rider every axis's cure needs.** The touch panel is an accessible-name-less `role="dialog"`: the click branch takes reka's dialog role, glass strips any consumer `role` from attrs (:145, my bytes), and `ariaLabel` — the prop the producer publishes for exactly this ("Accessible name for the hover group or click dialog") — is passed nowhere (:22). Every cure written in the challenge corpus keeps `<PopoverContent …>` without `ariaLabel` and would ship a keyboard-reachable unnamed dialog; A.W5 once had the name. **Terminal: X-W7.c CURE-RIDER** — any Popover-based successor MUST pass an accessible name (and D-5's drift table's touch-cell is killed with K-2, so no seat may cite `role="group"` for this branch).
- **SH-13 — R1-missed-3, PROMOTED (new) — second cure rider.** The two paths open on OPPOSITE SIDES: the touch panel defaults `side:"bottom"` (my compiled read; :22 does not override) while the hover panel opens 42px ABOVE the swatch top (`offsetY = -42`). Deleting the fork silently flips every desktop user's menu from above to below. **Terminal: X-W7.c CURE-RIDER** — the successor states its `side` deliberately.
- **SH-14 — D-9 — CONFIRMED (new; mechanism static-certain, magnitudes live-only).** Block wrapper (`div.relative`, :2-3) hosting the producer's inline-block span builds a line box and reserves baseline descender space under every swatch; the span is the only in-flow child (the overlay slot is absolute, the Teleport leaves flow). PROPORTION-AUDIT §5.7's reservation-vs-glyph distinction applies. Magnitudes (48×54 / 40×46 / lineHeight 27.912) are U-3. **Terminal: X-W7.c** (dies with the seat rebuild).
- **SH-15 — D-10/C-13/L-7 — CONFIRMED; two arms fold, one sharpens.** (a) `swatchExtraClass` dead public surface (my grep: 3 hits, all internal) — **IDENTITY ≡ CurrentPaletteEditor ADJ-m5**. (b) The unreachable `click` emit is SH-2's arm, not a second row. (c) `sizeClass` has **FOUR** authoring sites, not three (R1's K-7 correction adopted, sharpening **PC-38**'s banked triplication): :82 default ≡ PaletteCard.vue:194 doc-comment ≡ :198 withDefaults value, contradicted by CurrentPaletteEditor:36's `w-11 h-11 sm:w-12 sm:h-12`. **Terminal: X-W7.c** (via the PC-38 fold, count corrected on the record).
- **SH-16 — C-10 — CONFIRMED, IDENTITY ≡ PC-23 ≡ CurrentPaletteEditor C-12.** useLeaveTimer (17 lines, my read) registers no `onScopeDispose`/`onUnmounted` anywhere in the cluster; a leave scheduled within 250ms of unmount fires against a disposed scope. One line closes it; the cure deletes it. **Terminal: X-W7.c.**
- **SH-17 — C-11 — CONFIRMED, folds onto SH-1's cure constraints (≡ CurrentPaletteEditor U-8 + PS-1's rider).** Positioned once at hover; no scroll/resize/ResizeObserver/autoUpdate in the 67-line file; `-42` is measured against the swatch top, not panel height, with no flex-wrap; no collision handling — all producer-solved on the branch 20 lines above. Standing law preserved: never restore `position: fixed` assuming the maths is complete. **Terminal: X-W7.c.**
- **SH-18 — C-12/D-12 — CONFIRMED (MINOR-latent; the corpus's own honest downgrades ratified).** `nextTick(() => positionPanel(e.currentTarget as Element))` (useHoverPopover.ts:30) reads `currentTarget` after dispatch — crashes under programmatic dispatch (all three axes reproduced), survives trusted hovers (all three re-tested and downgraded; L declined to file, naming its own probe noise). The `as Element` cast suppresses the `EventTarget | null` the compiler would force. **Terminal: X-W7.c** (dies with the composable; until then capture synchronously).
- **SH-19 — L-8 — CONFIRMED, IDENTITY ≡ PS-22.** One position object, three type spellings across one hop (`reactive({top,left})` → `Record<string, string|number>` → `CSSProperties | undefined`), no owning module — the door the SH-5 transform drift walked through. **Terminal: X-W7.c.**
- **SH-20 — L-9 — CONFIRMED (new).** The file sits outside `card/index.ts`'s hardened public surface (6 exports, not this one) yet is reached relatively from two depths (CurrentPaletteEditor:192 `./`, PaletteCardSwatches:73 `../` climbing out of the PaletteCard/ sub-cluster) — neither cluster-internal nor cluster-public. **Terminal: X-W7.c** (the rebuild homes it or deletes it).
- **SH-21 — L-6 — CONFIRMED, QUADRUPLE-BANKED IDENTITY-FOLD** (ruling R-B): tsconfig.demo.json paths ↔ package.json exports drift (3 phantoms, 2 omissions, the false "CLOSED 8-key set" comment). **Terminal: X-W8** via the standing fold; nothing new booked here.
- **SH-22 — R2-missed#4, PROMOTED (new — verified in my own reads).** Dead layout CSS written for a slot child the producer never renders, **duplicated verbatim with its false comment in two SFCs**: CurrentPaletteEditor.vue:285-290 and MixSourceSelector.vue:276-281 both declare `.add-slot-ghost { display:inline-flex; align-items:center; justify-content:center }` "hosts a centred Plus glyph in its default slot" — the compiled dot has zero `renderSlot` (my count), so there is no child to centre; both rules also override the producer's `inline-block`. The surviving evidence that the author believed the slot worked — C-16's collateral, previously unfiled. **Terminal: rides the CC-044 atomic cut** (the seat cure deletes both) — recorded here so the MixSourceSelector copy is not lost.
- **SH-23 — R1-missed-M-E + R2-missed#5(tag arm) — CONFIRMED, IDENTITY-FOLD.** The `tag=` phantom family: 6 live `tag="button"` + 1 comment canonising it + 18 `tag="div"` lines — already banked as the ColorSpaceSelector 21-site glass-7 residue sweep + GenerateControls R-D migration input. My census re-seals the 6+1. **Terminal: via the banked sweep + CC-044**; no cure may size itself to this component's two sites alone.
- **SH-24 — R1-missed-M-D — CONFIRMED, IDENTITY ≡ PS-28 (ruling R-F).** The spread `{ ...floatingStyle, transform }` inside the v-for re-renders the whole swatch list on every hover-position write while CurrentPaletteEditor's by-reference pass re-renders nothing — reactivity-cost drift between the same two call sites. Already ratified at PaletteCardSwatches.md; per-frame cost stays live-only (their V-7). **Terminal: X-W7.c** via the fold.

### INFO

- **SH-25 — C-15 (D-6's surviving leg) — CONFIRMED.** `class="w-auto"` deletes the producer's `w-72` (same width group — my cn run) + `:side-offset="8"` over default 4: per-instance layout re-specification on a producer root (edict 5), correctly filed soft by C after D-6's flattening half died (K-1). **Terminal: X-W7.c** (the producer owns the surface; a denser action row is a producer variant).
- **SH-26 — R1-missed-M-G, PROMOTED (new).** The :38-39 comment says "the **reka-ui** Popover (touch path)" but :61 imports the **glass** Popover wrapper — the component that owns the click/hover decision and the cure. The mis-attribution propagated into all three challenge axes' wrong-producer analysis (ruling R-G). **Terminal: X-W7.c** — one-line comment correction rider (or death with the file).
- **SH-27 — R2-missed#7, PROMOTED (new — verified in my template read).** `<Teleport to="body">` (:37) is unconditional inside the hover branch; only the inner div is `v-if`-gated (:41) — one Teleport + body anchor mounts per swatch per hover-capable render, N anchors per row across every expanded card. One-token fix; the cure removes it entirely; not counted by om-16 row 7. **Terminal: X-W7.c.**
- **SH-28 — L-12/D-coverage — CONFIRMED, IDENTITY ≡ CurrentPaletteEditor A-3/U-9.** The 60-shot matrix contains zero captures with this component mounted; its silence is absence of evidence. Both axes labelled it honestly; D's seeded-localStorage prescription is already the X-W1 matrix input. **Terminal: X-W1** via the fold.
- **SH-29 — useHoverPopover.ts:8's "PaletteDialog" ghost consumer — CONFIRMED, IDENTITY ≡ PS-37 ≡ PC-39.** Fold; nothing new.
- **SH-30 — R1's D-7 cure caveat, PROMOTED (producer-owned).** `(pointer: coarse)` is read **once** into a `let` at module setup (popover-BQGYXZyO.js:39, my bytes) — `trigger="hover"` is not reactive to pointer-capability change on hybrid devices (docking a tablet, attaching a mouse). Kin to PS-34's two-oracle hybrid gap. **Terminal: glass-ui BH relay** (owner edict) as a note on the trigger contract; the consumer carries nothing.

## Killed-claims register

- **K-1 — D-6's flattening mechanism ("p-1.5 flattens the √φ ladder to 0.375rem isotropic") — KILLED (both readers, SEALED by this seat's own executions).** The producer's merger keeps `px-`/`py-`/`p-` (three conflict groups — chunk read raw; merger executed on the real strings), and tailwind 4.3.0 registers/emits px/py after p (byte offsets measured in both bundles), so the producer's 1rem × 1.272rem wins the cascade. Only the `w-72` deletion survives (SH-25). Inverted into SH-11.
- **K-2 — D-5's touch-cell a11y row (`role="group"`, named) — KILLED (R1, sealed by my bytes).** `role:"group"`+label sit on the **hover** branch only; the click branch this component takes renders reka's `role="dialog"` with `aria-label` bound to an unpassed prop → an UNNAMED dialog. Consequence promoted as SH-12.
- **K-3 — C-1's cited proof (`grep dist/glass-ui.css → 0`) — KILLED as method (R1, upheld).** `dist/glass-ui.css` is not the stylesheet the demo imports; `glass-floating`/`glass-reveal`/`popover-content` all also grep 0 there while being real. Only the whole-dist form is sound (my re-run: 0 across the package). Finding untouched.
- **K-4 — C-3/L-2's traced trigger chain — KILLED as traced, conclusion intact (R1 + R2 concordant; ruling R-G).** The glass wrapper hop was omitted by all three axes; it matters because that same file implements the recommended cure.
- **K-5 — L-3's "19 identical … every one a 1-line pass-through" — KILLED (both readers; my census seals R2's version: 17 one-line bare-root, alert 11-line, input via `/forms`).** Core finding intact.
- **K-6 — D-11 as filed ("tap targets below the floor", REPORT attribution) — KILLED (ruling R-A; R2's kill adopted over R1's confirmation).** 28×28 exceeds the WCAG 2.2 AA floor; the cited REPORT rows were captured with zero swatches mounted; the buttons are consumer-authored. The seat-floor obligation lives in the X-W7.c seat spec, not in a row. Anchor slips (PR-12 :56 / item-12 :77 / item-14 :79) recorded.
- **K-7 — D-10's sizeClass anchor and count ("second default at :197 … three authoring sites") — KILLED (R1, upheld by my reads: :194 doc-comment + :198 value; FOUR sites total).** Finding worse than filed; PC-38 sharpened (SH-15c).
- **K-8 — D-8's four-way tally ("dialog ✓") — KILLED (R2, sealed by my 46-name enumeration + the `git show` pre-image).** No dialog-* keyframes exist in the producer; the dialog family, card-menu-in and golden-shimmer were all deleted on the same false premise. Finding STRONGER (SH-6).
- **K-9 — L-4's cure prescription ("or HoverCard, from @mkbabb/glass-ui/popover"; reka HoverCard directly) — KILLED (R2, sealed by my reads: `dist/popover.js` exports exactly three symbols; no `./hover-card` key; HoverCard = 0 in index.d.ts; direct-reka is the exact L-3 breach).** The correct cure is the producer's own `trigger="hover"` (SH-5). The cure is adjudicated separately from the defect — ColorSpaceSelector addendum item 2, standing law.
- **K-10 — L-4's double-instantiation sub-claim ("two live copies … is itself the defect") — KILLED (R2, upheld).** Per-component-instance composable state is what a composable is; sharing one instance across all cards would be the bug. The parent finding needs no such leg.
- **K-11 — D's edict-7 cell ("`| undefined` on an optional prop is redundant") — KILLED (R2, sealed by my read of tsconfig.base.json:11 `exactOptionalPropertyTypes: true`).** The `| undefined` is load-bearing repo law; consumers pass explicit undefined. The prescribed "cleanup" would introduce type errors.
- **K-12 — R2's own missed#8 mechanism ("unhandled promise rejection out of a template handler") — ARBITER'S OWN KILL (ruling R-E).** `writeClipboard` cannot reject (my byte read of the producer's try/catch discriminated result; ≡ PaletteCardSwatches K-9). Substance folds to PS-23 ≡ PC-21.
- **K-13 — R1's missed-M-H as a new find — ARBITER'S OWN DEMOTION (ruling R-B).** The false "CLOSED 8-key set" comment is already on the record at ColorSpaceSelector.md L-14. Fact true, novelty dead.
- **K-14 — R1's missed-M-D and missed-M-F as "missed" — ARBITER'S OWN DEMOTION (ruling R-F).** Both already banked (PS-28, PS-8) the day before this corpus's re-read. True of the challenge axes; false of the registry. Folded with credit for independent convergence.

## Superlatives (provenance: R1 · R2 · A = re-verified by this seat)

1. **The ghost-variant transposition, in this same file** (R1+R2, A — d.ts docstring read whole): :76-79's `ghost` rides a real producer prop tracing the SAME seeded silhouette through the SAME displacement filter — a per-instance CSS fork retired into a producer axis with the receipt left in the tree (CurrentPaletteEditor:280-283 names the deleted forks). The one binding in the component that is a typed contract — and therefore the one that works. The proof SH-1/SH-2 are migration failures, not competence failures.
2. **The producer is the innocent party and says so** (R2, A — ≡ CurrentPaletteEditor superlative #1): WatercolorDot.vue.d.ts documents why it is paint (internalised filter, rasterize-once, seeded no-twelve-clones, PRM-gated liveness). Every fact needed to predict SH-2 was published in the type the consumer imports.
3. **The slotted content is better than its container — which inverts the cure** (R1, A — WITH one correction): all six action buttons carry per-colour `:aria-label`s and `aria-hidden` icons; the container's single `aria-hidden` throws that work away, so the fix is subtraction at the container, not addition at the leaves. **Correction (PS-9, banked)**: the `focus-visible:ring-ring/40` register R1 praised reaches a token that does not exist in the served cascade — the labels are real, the ring is not.
4. **card/index.ts:1-3's PI-6 doctrine** (all axes + both readers, A): named re-exports with the tree-shake/side-effect rationale recorded at the point of use — beside demo/ui's reflexive barrels three directories away.
5. **The adjacency proof** (R1, A): CurrentPaletteEditor:58 consumes a REAL producer class (`glass-floating`) with a careful var-override block, twelve lines from SwatchHoverMenu:42's dead `.floating-panel` — same cluster, same author, one class alive and one dead: silent-handoff rot, not carelessness.
6. **The corpus's epistemic discipline** (R1, A): all three axes reproduced the `currentTarget` crash under synthetic dispatch, re-tested under trusted hover, and DOWNGRADED their own finding — three seats, three chances to inflate, zero taken. It is why the surviving BLOCKERs deserve belief.
7. **Coverage honesty as a deliverable** (R1+R2, A): D and L independently established the 60-shot matrix never mounts this component and labelled its silence "absence of evidence" — then prescribed the exact seeding fix that becomes X-W1's matrix input.
8. **Small correctnesses that survived the rot** (R1, A-spot): the S.W5-7 singular-form guard; animations.css:11-16's media-query keyframes-replacement note; `.vj-enter-move` present so FLIP reorders are real; `schedule()` cancelling before arming; the :68-74 btn-interactive retirement narration (its target's death is PS-6's business, already banked).

## UNPROVEN-NEEDS-LIVE residue (the next visual audit's input list)

- **U-1**: SH-1's live rects (panel [0,900,1440,40] · anchors · docScrollHeight 900→940 · inline `top:307.18px`) — mechanism static-certain; every number live-only. The two axes report different `left` for "the same" probe (791.5 vs 907.5) — reconcilable as different swatch indices, unreconciled in the corpus. PNG IHDRs corroborate the panel strip and the 40px growth in bytes (my read).
- **U-2**: the touch tap non-event. CONFIRMED-BY-CONSTRUCTION from three byte-verified premises; D claims a live 390×844 tap, C explicitly did not run one — the axes disagree about whether it was ever observed. No browser was run by this seat or either re-reader.
- **U-3**: SH-14's magnitudes (48×54 / 40×46 / lineHeight 27.912px).
- **U-4**: the 28×28 rendered action-button rects (post-K-6, relevant only to the X-W7.c seat spec).
- **U-5**: D-3's a11y-snapshot claims (zero swatch nodes; `elementsLabelledColorSwatch: 0`) — follow from the bytes, never re-observed.
- **U-6**: SH-11's cascade half — merge half EXECUTED (producer's own cn), order half derived from tailwind registration bytes; one `getComputedStyle` on an open touch popover would seal it. No stylesheet was built.
- **U-7**: whether the six e2e specs actually fail today — follows by construction; CI runs none of them (my workflow grep), so no run exists either way.
- **U-8**: L-3's esbuild delta (+969B) and L-6's css.d.ts two-line diff — carried on the banked identities' verification, not re-run this session.
- **U-9**: the shipped-capture visual claims (the glyph-less add-slot in safari-*-light shots) — slot-drop confirmed in bytes; PNGs opened only to IHDR depth by this seat.
- **U-10**: `vue-tsc` exit 0 over the dead props — R2's run, accepted; not re-executed this session.

## Corpus-hygiene and law notes

1. **r2 governance**: no r2 challenge file exists on any axis; the canonical r1 files govern. All three are EXISTS-ORIGINAL live-seat returns (HYDRATION-LEDGER:127-129) — not the REPORT-AUTHORED Codex batch; nothing here is UNVERIFIED-UNTIL-RE-PROVEN.
2. **Motion/PRM**: no motion-quarantine row names SwatchHoverMenu or floating-panel (my grep; the adjacent M-26 row belongs to picker-componentsliders-consolerail and is not carried). SH-6 is a deletion-of-source claim, not a reduced-motion behaviour claim; no M-xx FALSE row is carried anywhere above.
3. **Identity guards (anti-rename), one identity each for life**: SH-1 → PC-2 ≡ PS-1 · SH-2 → PC-3 ≡ PS-2 ≡ A-1 → CC-044 · SH-4 → PS-3 ≡ C-6 · SH-5 → PS-15/PS-17/PS-38 · SH-7 → CurrentPaletteEditor C-9 · SH-8 → A-3 → CC-030/CC-031 · SH-9 → CurrentPaletteEditor L-4 (W-HYGIENE) + PS-29 ≡ GenerateControls-23 · SH-15a → ADJ-m5 · SH-15c → PC-38 (count corrected 3→4) · SH-16 → PC-23 ≡ C-12 · SH-19 → PS-22 · SH-21 → ColorSpaceSelector-L-14 (quadruple fold) · SH-23 → the 21-site tag sweep · SH-24 → PS-28 · SH-29 → PS-37 ≡ PC-39.
4. **Cure law**: the cure is adjudicated separately from the defect (ColorSpaceSelector addendum item 2) — enforced here four times: K-9 (HoverCard), PS-1's banked glass-floating refusal, SH-12 (unnamed dialog reproduced by every corpus cure), SH-13 (silent side flip). DR-19/CC-019: no phantom-class cure may take the shape of a proof script — the family dies structurally.
5. **Dispositions are ADJUDICATED, not VERIFIED.** X-wave routing follows CARRY-CUT-LEDGER + the two sibling palette records' vocabulary; SH-2 sits in the CC-044/CC-003 blocked bank by that ledger's atomic-cut ruling; producer questions (SH-2 API, SH-30 trigger reactivity, the C4-3 hash kin) ride the standing glass-ui BH relay; **zero rows land NO-WAVE-OWNER**.

## Closing verdict

**ADJUDICATED — GRAVE, AND ALREADY SENTENCED.** 30 rows survive two hostile re-readings and this seat's counter-attack (3 BLOCKER · 6 MAJOR · 15 MINOR · 6 INFO) against 14 killed claims — two of the kills sealed by this seat's own executions of the producer's merger and the popover bytes, two more this seat's own against the re-readers themselves. The component is inoperative on every input modality it claims to serve: the hover panel is an unstyled static block at document end (SH-1), the touch trigger is structurally discarded before it reaches the DOM (SH-2), and the ratified canon already carries a terminal REMOVE for the concept by name with an open born-RED addendum (SH-3, V-A142). Thirteen rows fold onto identities the registry banked at PaletteCard, PaletteCardSwatches, CurrentPaletteEditor, ColorSpaceSelector and GenerateControls — this record adds eight genuinely new rows, of which three are cure riders (the unnamed dialog, the silent side flip, the padding the anti-drift const cannot hold) that every cure written in the challenge corpus would have shipped. The spine dies at X-W7.c/d with the seat transposition banked at CC-044; the gates that let a born-dead component ship green die at X-W1; and the one thing in the file that ever worked — the ghost variant — is the proof that the producer's typed surface is the only channel that survives a major.

— end of apotheosis. The challenger corpus and both hostile re-reader returns remain immutable beside this file per E-1/E-3; dated evidence was not rewritten.
