Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SquareInstrument; receipt read from this seat's own system prompt, not inherited).

# kf-SquareInstrument — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`; three square files dirty vs local HEAD (`SquareScene.vue` / `useSquareDemo.ts` / `useSquareTumble.ts`) but **worktree ≡ origin/master on every `demo/scenes/square/` surface** (my `git diff origin/master --stat` this session: empty) — the readers' cites are worktree-current AND baseline-current. `package.json` is worktree-dirty in the F-1-relevant way (see K-5). · **Registry tree** value.js `tranche-u` · **Corpus (r1)**: two fresh hostile re-reader returns over `audit/kf-components/SquareInstrument/challenge-{D-design,L-library,C-consumption}.md` — **reader-A** (design-and-usage: 29 confirmed · 8 kills · 6 misses · 7 unproven) and **reader-B** (library-and-code: 28 confirmed · 6 kills · 9 misses · 7 unproven). Both returns consumed whole; all three challenge files re-read whole by this seat.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every load-bearing cell was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole read of SquareInstrument.vue (212 L); useSquareDemo.ts:60-66/:235-252/:374-402 (TRAVEL, the frame/onTick seam, `startLoop` census, `paintRest`, dispose); SquareScene.vue:8-14/:44-52/:140-160/:255-310 (Card class chain, demo-box `text-display`, the onTick mirror + `tetherActive`, `anim.name = "Transform"`, readout writes, `hasDragged` 3-site census, useDoubleTap comment); useSquareTumble.ts whole (the `onScopeDispose` at :48, the guarded token reads, `asColor` throw); useSquareKeyboard.ts:13-96 (ENVELOPE_LEGS, `setTimeout(step, 520)`, the c/arrow/Home-End bindings, **the header's paced-by-settle claim**); SquareScene.css:1-10/:30-44/:68-72/:98-102/:134-140; T/audit/owner-review/VERDICT.md:29 (row 11 verbatim); T/stage-manifests/square.json (whole); design-idioms.css:185-245 (`.readout-accent`, `.stage-field-{x,y}`, the `.status-badge` family); layout.css:24-40 (the five graph tokens + the in-comment 10%-floor law); style.css:255-274 (the BG-6 display-weight block). Producer instruments: glass-ui 7.0.0 `dist/styles/glass/ladder.css` (the `:where(.glass-card,.glass-resting,.glass-quiet,.glass-wash){ --muted-foreground: var(--on-glass-muted) }` re-point — **the D-3 decider**), `tokens/on-glass-fg.css` (light `hsl(30 26% 35%)`) + `tokens/dark-arm.css` (dark `hsl(34 16% 72%)`), `Surface-*.js` (`material:{default:"elevated"}` → map `elevated→resting`), `tokens/scheme-motion.css` (`--duration-fast: 0.2s`, `--z-content: 10`), `typography/scale.css` (`--font-display-weight: 600` + `--type-weight-display`), `typography/semantic.css` (`text-display` glass-ui-owned, 1 hit), `dist/components/metric/` (ls: Metric/MetricCell/MetricRow/MetricStack ship). Executions: **node against the installed `@mkbabb/value.js` 4.0.0** — `parseCssColor("oklch(0.684 0.250 327.9)")` → `ok:true`, `mixColors(...,{space:"oklab"})` → ok, serialize → ok, AND `parseCssColor("oklch()")` → **uncaught TypeError** (the R1 class reproduced at its true degenerate input — the C-13 decider). Git: `-S "press C to trace the field" -- demo/scenes/square/` → single commit `fd058fd2` (R.W5); `--all -S ".square-live-caption {"` → **empty** (the class never had a rule in any revision); `-S "square-live-caption" -- demo/` → single commit `021f0eba`; `show 5bf99ca7:…:41` → the span then carried `text-mono-caption`; `show origin/master:package.json:77` → `"@mkbabb/glass-ui": "7.0.0"` + the lockfile entry at :612-617. Greps: `viewBox` demo-wide → exactly 3 hits, none in square/; `matchMedia|prefers-reduced-motion` in `demo/scenes/square/` → CSS blocks only, **zero** in `*.ts` (siblings gate: useCubeDemo.ts:164, useSequenceInstrument.ts:31); `registerShortcut` → **3 consuming files**, zero in square/; `--ball-tone` in square/ → 0 (siblings declare: EasingTarget.css:7, SequenceTarget.css:8); `--graph-pitch|--graph-major|--stage-field-tint` in square/ → 0; `stage-field` consumers → SequenceAxis.vue:6, SpringTarget.vue:63/:141; `duration-fast, 150ms` in playback-idiom.css → 8; `glass-ui/metric|chip` → 3 demo consumers.

## Scoreboard

**40 roster rows — 1 BLOCKER · 10 MAJOR · 16 MINOR · 13 INFO.** Nine net-new rows admitted (**N-SQ-1..N-SQ-8** from the readers' 15 filed misses — the rest adopted as cells into existing identities or booking-killed — plus **N-SQ-9**, this seat's own find). **Killed-claims register: 13 entries**, including two reader-cells corrected against a reader's own CONFIRMED list (reader-B's D-2 "grown" limb and D-3 substrate — both inherited challenge cells that reader-A had independently killed and this seat re-killed at the bytes). **9 superlatives** (merged, two scoped down). **10 UNPROVEN residues** → SS-13. **3 fold-by-identity** to banked records.

**Headline.** The two readers split the corpus's four load-bearing errors between them and each inherited what the other caught. Reader-A killed the challenge's substrate (glass-ui's ladder re-points `--muted-foreground` on the very plate the component renders in — D-3's "three of four fail" becomes **two of four, both light**), killed D-2's "the block has GROWN" limb by chronology the owner's own quote refutes, killed D-9's invented provenance (the class was **born orphaned** — no rule in any revision), and killed C-13 **by execution**; reader-B confirmed all four of those dead limbs — three verbatim from the challenge — while contributing the corpus's sharpest new find (the settled snapshot omits `springSpin`: the badge reads "settled" and the tether stays hidden through the entire advertised 360° tumble — **the loop running, the snapshot narrowed**, a mechanism C-3/L-D3 does not cover), the joint-unsatisfiability proof that no stage size makes TETHER_REACH correct, and the correct severity calibration on D-2 (the manifest's own `pendingOwner` records the prune as scheduled-and-unexecuted, not defied). This seat re-derived every one of those disputes at the bytes and executed the parser test itself — including the bonus reproduction of R1 at its true input (`oklch()` empty-args throws; every real token value parses clean). The component itself is unchanged by all of it: a 212-line leaf of genuine discipline (zero declarations, zero literals, zero teardown surface, complete pointer/AT transparency) whose one graphical thesis is drawn in a coordinate system that does not exist, whose telemetry tells three different lies in three different modes, and whose owner already ruled half its DOM off the stage.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0/W6/W9) with **NO-WAVE-OWNER** for the square scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D-2's "the block has grown since the rejection" limb: reader-A kills it; reader-B CONFIRMS it ("has GROWN since the ruling")** | **LIMB DEAD (K-1); reader-B's cell corrected — a limb-inheritance event** | VERDICT.md:29 read verbatim this session: the owner's quoted rejected block **includes** `PRESS C TO TRACE THE FIELD` — a line cannot be both inside the ruling's quotation and added after it (the challenge's own mapping table at challenge-D:68-73 lists it as row four). My `git log -S "press C to trace the field" -- demo/scenes/square/` → single commit `fd058fd2` (R.W5), pre-review. Only the `, or press Play to tour it` re-wording is post-verdict (`5bf99ca7`). The block did not grow; one line was re-worded. D-2's core is untouched. |
| 2 | **D-2 severity: challenge + reader-A file BLOCKER; reader-B demotes to MAJOR** | **MAJOR (K-2) — reader-B's calibration adopted; reader-A's BLOCKER preserved as dissent** | square.json read whole this session: the `_doc` states "#11 is a ruled removal whose on-stage DOM prune **rides T.B (OD-5)**"; `pendingOwner[0]` = "square-instrument-disposition … which of the SquareInstrument layers survive the caption-block prune is a T.M2 design sign-off; **default = the caption/telemetry #11 removal, the tether kept**"; `status` repeats it. The manifest itself records the removal as SCHEDULED-AND-UNEXECUTED behind an open sign-off — an unexecuted ruling plus **false closure records** (T/FINAL.md:36 "caption removed … LANDED"; U/lane-03:29 "LANDED | REAL"), which is a MAJOR records-and-product defect, not a shipping blocker. Reader-A's counter ("the caption prune is not open") is half-right — the prune is the recorded *default* — but a default pending sign-off is still pending. |
| 3 | **D-3's substrate: reader-A corrects the ink to `--on-glass-muted` (2 of 4 fail, both light); reader-B reproduces the challenge's `--neutral-5` table and confirms "three of four"** | **READER-A WINS (K-3); reader-B's cell corrected — the second inheritance event** | Verified at the producer this session: `dist/styles/glass/ladder.css` contains `:where(.glass-card, .glass-resting, .glass-quiet, .glass-wash) { … --muted-foreground: var(--on-glass-muted); --muted-foreground-strong: var(--on-glass-muted-strong); }`; the component renders inside exactly that class (`<Card :shadow="false">` SquareScene.vue:10-13 → `Surface-*.js` `material:{default:"elevated"}` → map `elevated→resting` → `.glass-resting`); `--on-glass-muted` = light `hsl(30 26% 35%)` (on-glass-fg.css) / dark `hsl(34 16% 72%)` (dark-arm.css) — reader-A's exact values. D-3's own falsifier ("glass-ui's tier blocks do not touch it in 7.0.0") is false at the producer; its §0 grep was scoped to `demo/` and could not see it. Corrected ratios at 0.8 alpha: light 3.88/3.98 ✗✗, dark 5.20/6.78 ✓✓ — **the light theme fails at both endpoints under BOTH candidate inks** (challenge 3.38/3.47; corrected 3.88/3.98), so the MAJOR verdict and sole-cause diagnosis are ink-independent; only the dark half of the claim dies. Residual: ladder carries further branches (`@container style(--glass-backdrop: light)` → `--foreground`; `@supports contrast-color`) that push ink STRONGER — the exact resolved plate ink is SS-13 #10, and it can only improve the dark side further. |
| 4 | **C-13: reader-A kills by execution; reader-B confirms MINOR ("safe only by cascade order")** | **KILLED (K-6) — reader-A's execution reproduced by this seat; reader-B overruled** | My own node run against the installed value.js 4.0.0: `parseCssColor("oklch(0.684 0.250 327.9)")` → `{ok:true, space:"oklch", channels:[0.684,0.25,327.9]}`; the cross-producer mix and serialize both ok. The hypothesised cascade flip hands the parser an input it **handles**. Bonus: `parseCssColor("oklch()")` — the true R1 degenerate form — throws an uncaught TypeError in my run, confirming R1 is real AND unreachable from any token (`getPropertyValue` yields `""` for undefined tokens; useSquareTumble.ts:17 `if (value)` skips empties; no CSS token can resolve to empty-args `oklch()`). "Safe only by cascade order" is false: safe regardless. Residue folded, not lost: the two-producer `--rainbow-*` collision (glass-ui `scale-paper.css` oklch vs design-idioms.css:18-21 hsl) is a real namespace-hygiene datum ≡ the census flat-namespace row → KF.W6; the unguarded computed-style reads stay a useSquareTumble note for that sibling's lane. |
| 5 | **C-5: reader-A confirms MINOR; reader-B kills (provenance note, not contract claim)** | **KILLED (K-9) — reader-B's refutation adopted; a one-line prose rider survives** | Verified: the props at SquareScene.vue:23-24 bind the same `springReadout` members that :63/:73 bind to `aria-valuetext` — "the aria-coupled telemetry numerals" is true of the NUMERALS (the data), false only if read as a claim about this file's render. The genuinely charged version is L-D5 (confirmed). Rider to the prose sweep: the JSDoc should say the coupling lives in the parent (one clause), because within this file the props reach `aria-hidden` subtrees only. |
| 6 | **Reader-B's missed #1: "BASELINE: glass-ui declared and locked at 7.0.0 on origin/master" filed as a new MAJOR** | **BOOKING KILL (K-5) ≡ census F-1 ≡ SCH-1, banked FALSIFIED-at-HEAD five-plus times; cells adopted** | The identity guard exists for exactly this row: F-1 is ruled FALSIFIED at HEAD (CENSUS-2026-08-03:32) and banked at kf-AmigaScene/kf-CubeScene/kf-CubeTarget/kf-AnimatedText/kf-App.skeleton, carried as the standing restatement law at kf-SequencePlayhead K-4. My own probe reproduces reader-B's cells: `origin/master:package.json:77` = exact 7.0.0 devDep + lockfile entry :612-617; the worktree deletes the declaration (my grep: only `@mkbabb/value.js` remains); local HEAD declares 6.0.0. All three axes measured the dirty worktree — the **sixth and seventh** restatement events (the axes, then reader-B re-booking it). Adopted into the fold, never re-booked. Reader-A folded correctly. |
| 7 | **Reader-B's missed #2 (the settled snapshot omits `springSpin`) — claimed distinct from C-3/L-D3** | **ADMITTED as N-SQ-1, MAJOR — the corpus's sharpest new find, verified at the bytes** | useSquareDemo.ts:239 `const live = !(springX.settled && springY.settled && springSpin.settled)` vs :243-247 `onTick?.({ …, settled: springX.settled && springY.settled })` — the third spring is in the liveness test and out of the snapshot, at the same call site, under a comment reading "the tether + the settled/tracking badge are derived reads of THIS". SquareScene.vue:147 `tetherActive = dragging || !isSettled` → during a from-rest tumble the loop RUNS, onTick FIRES every frame, and the instrument renders "settled" with the tether hidden through the whole 360° roll + rainbow sweep. Distinct mechanism from C-3/L-D3 (whose whole proof is "the loop is stopped"): here the feed is live and narrowed. The cheapest of the three telemetry lies to fix (one token). |
| 8 | **Reader-A's missed #1 (`.square-field` bypasses the published `.stage-field-x/y` idiom + the five graph tokens) filed MAJOR** | **ADMITTED as N-SQ-2 but DEMOTED to MINOR — the outcome is already booked at D-7; this row is the mechanism** | Verified whole: design-idioms.css:195-211 publishes the idiom at FULL `var(--border)` strength with three live consumers (SequenceAxis.vue:6, SpringTarget.vue:63/:141 — my grep); SquareInstrument.vue:129-139 re-authors the identical structure (1px lines, `calc(100%/4)`≡25% pitch, both axes) diluted to 30%; layout.css:28-36 publishes `--graph-pitch/--graph-major/--graph-opacity/--graph-major-opacity/--stage-field-tint` with the in-comment law "held STRICTLY ABOVE the 10% legibility floor", and square/ consumes none (grep → 0). The idiom-bypass would be a MINOR shadow-duplication (the C-11 genus) even if the contrast passed; the sub-perception OUTCOME is D-7's MAJOR. One repair identity, severity booked once. Cell adopted into D-7: the 30% dilution of an idiom that exists at full strength is the proximate cause of D-7's worst numbers (idiom bare: 1.87/1.94/1.99/2.82 vs the square's copy 1.19-1.24). Fold-by-reference for the idiom's own properties: banked kf-SequenceAxis D-4 (different file, different mechanism — mis-consumption there, bypass here; the shared-idiom cure serves both). |
| 9 | **D-13 severity: reader-A raises to MAJOR; reader-B holds MINOR (and files the guard-granularity superlative)** | **MAJOR at the SCENE level — reader-A's raise sustained; reader-B's superlative survives SCOPED** | No banked grade exists (r1) — ruled fresh. Verified: `matchMedia` in `demo/scenes/square/*.ts` → 0 while both siblings gate (useCubeDemo.ts:164, useSequenceInstrument.ts:31); the ungated set is the advertised verbs themselves — the 5-leg `setTimeout(step, 520)` tour, the +360° tumble, the post-release chase, and the engine's Play tour (±90px + 360° + colour sweep). A vestibular user pressing the PRIMARY transport verb gets full motion in the one scene family that opted out of the house idiom. The file-local four lines remain correct for the one transition the file owns (S-8) — the charge lands on the scene composables, and the row is booked here as the scene-level identity (anti-rename; the forthcoming SquareScene/useSquareDemo adjudications fold in). |
| 10 | **Grade calibrations: C-9 (C/reader-A MINOR vs reader-B INFO) · D-15 (reader-A MINOR vs reader-B INFO) · D-12 (MINOR-as-authored vs INFO) · C-12 + reader-B's shape-incompleteness miss** | **C-9 → INFO · D-15 → INFO · D-12 → INFO · C-12 → INFO with the shape cell adopted** | C-9: latent (sole call site passes declared props; Vue dev-warns) — the banked latent-no-harm standard. D-15: its strongest limb (the 1.67× "vertical rhythm between axis rows") is dead — the grid has four children in four explicit columns, ONE row, the row-gap never applies (K-10; reader-A killed it, reader-B independently derived the row-gap-inert cell) — what survives is two sub-3px off-grid literals, an INFO. D-12: ≡ the banked kf-SequencePlayhead D-15 posture (project ships no `dir`; all-physical geometry latent) — INFO by precedent, the mixed-coordinate-system authoring note recorded. C-12: *evaluate* stands (Chip's tone pipeline vs the measured AA guarantee is SS-13 #7); reader-B's corrective cell adopted — C-12 read the three-scene sharing as encapsulation while the idiom (design-idioms.css:218-229) owns tint/text/font but NOT shape, so all three call sites re-type `px-2 py-0.5 rounded-full` byte-identically (my read confirms the identical four-class string; reader-A's superlative cell confirms zero drift TODAY; both are true — unguarded, undrifted). |
| 11 | **Small-cell corrections, both directions** | **RECORDED (K-13)** | Against BOTH readers + the challenge: the tether `<svg>` carries **FOUR** attributes (`class`, `:class`, `aria-hidden` :15, `preserveAspectRatio` :16) — challenge-C:58 wrote "Three attributes", and both readers repeated "exactly three attributes" while each listing four — a verbatim citation-inheritance signature crossing both returns, zero verdict consequence, recorded per the DISSENT-1 precedent. Against reader-B: the svg's `aria-hidden` is :15, not ":13". Against reader-A: `registerShortcut` is consumed by **3** demo files (useControlsKeyboardShortcuts.ts, useAnimationGroupPlayback.ts, EditorShell.vue — my grep), not 1; the square-has-zero fact is unchanged. Against the challenge (both readers already caught it): D·S-1's "`var(--border)` ×4" is **×6** (:113/:114/:119/:120/:132/:137). Reader-B's D-9 cell "orphaned by f9752c58" corrected under K-4 (born orphaned at `021f0eba`; `f9752c58` swapped the sibling utility `text-mono-caption`→`text-caption` — my `git show 5bf99ca7:…:41`). |

**Credits.** Reader-A: the ladder substrate correction (the corpus-level catch of this adjudication), the C-13 execution kill, the D-2 chronology kill, the D-9 provenance kill, the stage-field bypass find, the cure-unsoundness find, the 160ms/200ms/150ms token find. Reader-B: the springSpin snapshot MAJOR, the joint-unsatisfiability cell, the tick-frame arithmetic, the C-5 kill, the D-2 severity calibration, the `Transform`-literal find, resolving L§7(a)'s referral, and the F-1 baseline cells (adopted into the fold it should have cited).

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the square scene-repair packet). Anchors bind to worktree ≡ `origin/master` `81a56990`; re-resolve before any cure.

### BLOCKER

- **D-1 ≡ L-D1 ≡ C-1** — **the tether `<svg>` carries `preserveAspectRatio="none"` and no `viewBox`; the documented 0..100 user space does not exist** — one user unit = one CSS px, so the path (`M 50 50 Q …`, :83-97) renders in a ~100×100px patch at the stage's top-left, connecting nothing to nothing, while the comment at :78-81 asserts the space in so many words. The demo's only SVG that gets the idiom wrong (viewBox census: exactly 3 hits demo-wide — EasingTarget.vue:97, SpringTrace.vue:18/:80 — my grep); never present in any revision. Both readers concordant; three axes converged by three independent roads. Enriched: C-1's corroboration #1 is void (K-8 — `vector-effect: non-scaling-stroke` :165 pins the painted width either way, so `stroke-width: 0.8` is neutral evidence; the other two corroborations stand). Rendered result = SS-13 #1, the single highest-value probe. Cure ranking REORDERED by N-SQ-4: the one-attribute cure all three axes lead with is itself unsound — see the MINOR roster. **NO-WAVE-OWNER (tether-truth commit, with D-6 + D-16 + N-SQ-4).**

### MAJOR

- **D-2** *(severity ruled 2; "grown" limb killed, ruling 1)* — the owner-ruled-dead caption/telemetry block (VERDICT #11, "superfluous nonsense — remove") renders unconditionally at HEAD (:21-33, :35-44 carry no `v-if`; only the two hints are gated), while THREE records disagree about reality: T/FINAL.md:36 and U/lane-03:29 assert LANDED (false), square.json honestly lists it `forbidden` with the prune riding the OPEN `pendingOwner` sign-off T.B/OD-5 (default: caption/telemetry removed, tether kept). The T-era gate inversion (29-gate-oracle-gap.md:109 — `proof:square-honest` REQUIRED the caption the owner struck) explains the survival; my git check: the named script no longer exists, no gate reads these classes — the cures are unblocked. **Disposition: the OD-5/T.M2 owner sign-off is the gate; the false closure records take addenda (E-3 — dated evidence is never rewritten), and lane-04's #5 same-genus ruling (the x/y strip) rides the same sign-off. NO-WAVE-OWNER (owner-decision packet; nothing lands before the sign-off).**
- **D-3** *(substrate corrected, ruling 3)* — `.square-legend-hint { opacity: 0.8 }` (:203-205) is a WCAG 1.4.3 failure **in the light theme at both substrate endpoints** (3.88/3.98 corrected ink; 3.38/3.47 challenge ink — ink-independent), and the single declaration is the whole failure (the sibling caption in the same colour at full opacity passes everywhere). Dark passes under the corrected `--on-glass-muted` (5.20/6.78). Compounded: `text-caption` is italic, 400, 12px floor. Cure: delete one line. **NO-WAVE-OWNER (with the D-7 field commit or standalone; also rides D-2 — the hints may not survive the prune at all).**
- **D-4 ≡ L-D7 ≡ C-4a** — every keyboard affordance (`c` tour, arrows ±0.25, Home/End) is documented only inside `aria-hidden="true"` roots (:10/:15/:21/:35); the box's accessible name is a pointer verb (SquareScene.vue:50); no `aria-describedby`, no sr-only twin; and the square registers **nothing** in the glass-ui shortcut registry the demo consumes in 3 files (my grep) — `c`/arrows/Home/End are absent from the shortcuts modal, the one place a keyboard user looks. **NO-WAVE-OWNER (disclosure commit, with D-5/C-6; registry entries + describedby/sr-only twin).**
- **D-5 ≡ C-4b** — the disclosure is pointer-gated: `hasDragged` has exactly three sites (SquareScene.vue:130 decl / :149 read / :230 sole write inside `useDragScrub.onStart` ← `@pointerdown`), and the keyboard paths (`reseat`/`onTarget`) never touch it — keyboard-only users can never mount the hints that name their only verbs, even visually. Inverted disclosure by construction. **NO-WAVE-OWNER (one commit with D-4).**
- **D-6 ≡ L-D2 ≡ C-2** *(+ reader-B's cell adopted)* — `TETHER_REACH = 38` (percent-of-stage) duplicates `TRAVEL = 110` (px, useSquareDemo.ts:64) with zero coupling; the endpoints coincide only at a 289.5px-square stage, and under `preserveAspectRatio="none"` the miss is anisotropic. **Adopted cell (reader-B): the coincidence size is unsatisfiable** — at 289.5px the 192px box driven ±110px overruns the stage by 61px into `overflow: hidden` clipping, so there is NO stage at which the constants agree while the scene works; the "stage pinned square at 289px" falsifier is closed permanently, and the only sound cure is derive-from-travel (the parent already holds `travel`, destructured SquareScene.vue:132, passed to nothing). **NO-WAVE-OWNER (tether-truth commit).**
- **D-7** *(+ N-SQ-2's root-cause cell adopted, ruling 8)* — the coordinate field — the component's stated thesis ("the missing axes", :101-102) — is drawn at 1.19-1.95:1 against a 3:1 WCAG 1.4.11 floor (crosshair `--border` at 70%: 1.52/1.56/1.61/1.95; quarter ticks at 30%: 1.19-1.24; both readers' arithmetic concordant, reader-B re-derived all 22 corpus ratios to 2dp). Both horns of the challenge's dilemma stand. **Root cause adopted: the file bypasses the demo's own published frame idiom** — `.stage-field-x/y` at full `--border` (3 live consumers) — and consumes none of the five tokenized graph values whose own comment states the 10% floor law. Cure = consume the idiom/tokens, not invent numbers. **NO-WAVE-OWNER (field commit, with N-SQ-2 + N-SQ-3).**
- **D-8** — the chrome sits inside the subject's ±206px travel envelope at the box's own z-rung (`--z-content` = 10 on both; box later in DOM → paints OVER the readout), and the `c` egg scripts the collision: ENVELOPE_LEGS leg 2 `[1,1]` parks the box on the legend, leg 4 `[-1,-1]` on the telemetry (my read of useSquareKeyboard.ts:24-30 + the inset anchors). `pointer-events: none` keeps it legibility-only (S-4). Exact rects = SS-13 #2. **NO-WAVE-OWNER (chrome/occlusion commit — reserve a gutter or shrink chrome; the `c` tour is the reproducer).**
- **D-13** *(ruled 9 — MAJOR at scene level)* — PRM is honoured cosmetically: the file's entire concession is the 160ms fade snap (:207-211, correct for what the file owns — S-8), while the scene's advertised motions all run ungated (5-leg `setTimeout(step,520)` tour; +360° tumble spring; post-release persist-chase; the engine Play tour) with **zero** `matchMedia` in `demo/scenes/square/*.ts` against both siblings gating (my grep). The scene family that opted out of its own house idiom. **KF.W9 (PRM posture) + NO-WAVE-OWNER (the scene-level gate lands in the composables; booked here as the scene identity, anti-rename).**
- **L-D3 ≡ C-3** *(+ N-SQ-8's evidence cell adopted)* — all seven props source from the spring authority only; during engine `playback` the loop is stopped (`onTick` fired only inside `frame()`; `startLoop` callers = :159/:275/:289, never the group — my census), so the strip freezes: "settled" + stale x/y + stale tether while the box tours ±90px/360°/colour-sweep. The strip has no `mode` prop, so it cannot even suppress what it knows is stale. **Sharpest evidence adopted: the strip is TITLED with the engine animation's own name** — `anim.name = "Transform"` (SquareScene.vue:154, my read) — while every numeral under that title is spring-fed. Visual = SS-13; data flow proven. **NO-WAVE-OWNER (instrument-truth commit, with N-SQ-1 + L-D5).**
- **N-SQ-1** *(NET-NEW, reader-B; ruled 7)* — **the settled snapshot omits the third spring in its own authority**: liveness counts `springSpin` (useSquareDemo.ts:239), the snapshot does not (:243-247) — so through the entire advertised tumble the loop runs, `settled:true` feeds every frame, the badge lies and `tetherActive` stays false (SquareScene.vue:147). Not covered by C-3/L-D3 (loop-stopped mechanism); this is a live feed deliberately narrowed at the snapshot site. One-token cure; the only telemetry lie fixable without a new prop. Witness = SS-13 #3. **NO-WAVE-OWNER (instrument-truth commit).**

### MINOR

- **N-SQ-2** *(NET-NEW, reader-A; DEMOTED from MAJOR, ruling 8)* — the field layer hand-rolls the published `.stage-field-x/y` idiom at 30% strength and ignores the five-token graph vocabulary — the shadow-duplication defect independent of its contrast outcome (which lives at D-7). The centred crosshair is genuinely new; only the quarter-tick layer is the duplicate. Fold-by-reference: kf-SequenceAxis D-4 for the idiom's own properties. **NO-WAVE-OWNER (field commit).**
- **D-9 (core) ≡ C-7** *(provenance limb killed, K-4)* — `.square-live-caption` ships in the DOM with zero rule definitions anywhere in either producer's cascade — **born orphaned** (my git: no rule in any revision; single introducing commit `021f0eba`). The T-era census's voice row is stale (the span's mono voice came from `text-mono-caption`, swapped to `text-caption` at `f9752c58`); its selector enumeration was correct at its time. No gate reads the class (reader-B's self-refuted hypothesis, K-12). Cure safe: delete the class (or the whole span, per D-2). **NO-WAVE-OWNER (hygiene).**
- **D-10** — the two halves of one instrument case the axes differently: telemetry lowercase `x`/`y` (`text-mono-small`, no transform) vs legend `X · Y ∈ [-1, 1]` (`text-mono-caption` binds `text-transform: uppercase`); every other surface is lowercase. **NO-WAVE-OWNER (chrome commit; moot if D-2's prune takes the legend).**
- **D-11** *(+ reader-B's ownership cell adopted)* — four-`auto` readout grid re-measures on sign changes (`tabular-nums` reserves no sign column; bare `toFixed(2)` strings). Adopted rider: the fix cannot land in this component — the format lives in the parent (SquareScene.vue:266-267/:279-280/:305-306), so the cure is a signed formatter at the write sites or a `ch` track here, stated as one cross-file edit. **NO-WAVE-OWNER.**
- **D-14 ≡ L-D4 ≡ C-8** — SquareScene.css:136-140 duplicates the tether PRM rule across the boundary and is DEAD: the parent scope id never reaches a 4-root fragment child (both readers verified against installed vue 3.5.35 `setScopeId`/`filterSingleRoot`; the file's own :8-10 comment states the handover intent it violates). Corrects the PRM census: two listed sites, one live enforcement. **NO-WAVE-OWNER (hygiene — delete the dead rule).**
- **D-16** — the tether stroke composites at 1.85-2.47:1 (sub-3:1 all four ways) at 0.8px; `vector-effect: non-scaling-stroke` inert today (live-but-width-only under any viewBox cure); and the "red motion-authority" prose names the colour the owner expelled (ruling #16; the token is violet `--accent-kf`) at :143 + SquareScene.css:70/:100-101. **NO-WAVE-OWNER (tether-truth + prose sweep).**
- **D-17 ≡ C-10** *(+ a fourth site, this seat)* — "double-click to tumble" names the mouse-only verb the handler was deliberately migrated off (useDoubleTap is pointer-based; the scene's own S.G3 comment says so; the primitive's header closes the falsifier). **Seat's addition: useSquareKeyboard.ts:20 repeats the stale verb** ("one is keyboard, one is dblclick") — the family is four sites. **NO-WAVE-OWNER (prose sweep; one word).**
- **D-18** — the settled⇄tracking flip is a WCAG 4.1.3 status message rendered inside `aria-hidden`, no live region in the scene; held MINOR (derivable from the slider `aria-valuenow` stream; both exclusions verified — not colour-only, badge text contrast passes with margin). **NO-WAVE-OWNER (rides the D-2 disposition — the badge may not survive).**
- **L-D5** — numerals show the spring TARGET, tether shows the spring VALUE, side by side under bare `x`/`y` with no commanded-vs-actual distinction (write sites verified). **NO-WAVE-OWNER (instrument-truth commit).**
- **L-D6** — per-frame Vue re-render on the paint hot path (deflX/deflY refs at loop cadence → `tetherPath` computed) against the law the same scene states verbatim (:182-183) and the direct-write idiom it already uses (`--spring-tilt`/`--spring-squash` straight to el.style); `useThrottledReadout` ships unused. Cost = SS-13 #8; the INconsistency is the defect. **NO-WAVE-OWNER.**
- **L-D8 ≡ C-14** *(+ reader-A's token-value cell adopted)* — two fallback postures in one block: :153 alone carries fallbacks (for the two tokens guaranteed present) while `--border` ×6, `--color-progress`, `--z-content` ×2 (IACVT = drops the z-rung), `--foreground` ride bare. **Adopted: the one fallback present is also WRONG** — `var(--duration-fast, 160ms)` misstates the shipped `0.2s` = 200ms by 25%, and playback-idiom.css carries a THIRD value (`150ms` ×8, my count) for the same token; three numbers, one token, zero demo declarations reconciling them. `--ease-standard` clean. **NO-WAVE-OWNER (hygiene).**
- **C-6** — `tumbleHintShown` gates two unrelated affordances (pointer tumble egg + keyboard tour egg); the prop name is false for one; no independent disclosure. **NO-WAVE-OWNER (with D-4/D-5).**
- **C-11** — shadow **S-9**: the telemetry grid hand-rolls glass-ui `/metric` (Metric/MetricCell/MetricRow/MetricStack ship — my ls; the sibling scene already imports the subpath at SequenceTarget.vue:138). Census §5 addition, verdict *replace*. **NO-WAVE-OWNER (census S-9 intake; glass-adoption packet).**
- **N-SQ-3** *(NET-NEW, reader-B)* — the quarter-tick frame is off-by-one and self-swallowing: `repeating-linear-gradient(… 0 1px, transparent 1px 25%)` on the `inset: 12.5%` ::before puts 1px lines at 0/25/50/75% of the box with the 100% line clipped (opening edges ticked, closing edges bare, both axes), and the symmetric inset lands the 50% tick on the exact pixel of the parent's crosshair — one of four ticks invisible under a stronger line. Stop arithmetic spec-certain (re-derived by this seat); percept = SS-13 #9. **NO-WAVE-OWNER (field commit — moot if N-SQ-2's cure adopts the idiom).**
- **N-SQ-4** *(NET-NEW, reader-A)* — **the corpus's lead cure is geometrically unsound**: `viewBox="0 0 100 100"` + the existing `preserveAspectRatio="none"` is a non-uniform (non-conformal) map, so the bow constructed perpendicular in user space (:94-95, `(-dy/len, dx/len)`) is not perpendicular after it — verified by this seat: the mapped dot product is `dx·dy(sy²−sx²) ≠ 0` on any non-square stage, so the slingshot curvature points off-normal to the pull it expresses, and `non-scaling-stroke` pins width only, never direction. All three axes discussed anisotropy for the endpoint alone. Consequence: the sound cures are drop-the-`none` (default `xMidYMid meet` + reach derived from travel) or author the tether in the box's own px space. The cure-adjudicated-separately law (ColorSpaceSelector addendum §2) fires again. **NO-WAVE-OWNER (tether-truth commit — this row ORDERS it).**
- **N-SQ-5** *(NET-NEW, reader-A)* — the chrome's title and the subject's own label share the SAME display rung: `text-display` on both "Transform" (:22) and the demo-box (SquareScene.vue:46, my read), same Instrument Serif, same demo-forced 400 — the "instrument-plate composition" has asymmetry of position with equality of rank, i.e. no typographic hierarchy, and the title's `opacity: 0.92` lowers its contrast without lowering its rank. Adjacent owner rulings point down-rung (VERDICT #24 sitewide size consistency; #5 same-genus). **NO-WAVE-OWNER (chrome commit; rides the D-2 sign-off).**

### INFO

- **C-9** *(ruled 10 — demoted from MINOR)* — four roots, no `inheritAttrs:false`/`$attrs`; fallthrough silently dropped; latent (sole consumer passes declared props; Vue dev-warns). **NO-WAVE-OWNER.**
- **C-12** *(+ reader-B's shape cell adopted, ruling 10)* — shadow **S-10**: the status pill vs glass-ui `Chip`, correctly *evaluate* (the idiom's measured AA guarantee — 7.58/6.33 settled, verified twice — is not statically provable of Chip's tone pipeline; SS-13 #7). Adopted: the idiom is shape-incomplete (owns tint/text/font, not padding/radius), so all three consumers re-type the identical four-class string — unguarded convention, zero drift today. **NO-WAVE-OWNER (census S-10 intake; the shape completion is a design-idioms.css edit).**
- **D-12** — physical insets + flow-relative `align-items: flex-end` in one composition; latent (no `dir` ships) ≡ the banked RTL-posture precedent. **KF.W9.**
- **D-15** *(rationale limb killed, K-10)* — surviving core: `0.15rem`/`0.45rem` are on no published step (grep → 0), in a file consuming the Tailwind step one line away; the row-gap half of `gap: 0.15rem 0.45rem` is INERT (four children, four explicit columns, one row — both readers). **NO-WAVE-OWNER (hygiene).**
- **D-19** — `text-display` (leading 1.05) vs `leading-none` (1) at equal specificity; winner = Tailwind v4 emission order; UNPROVEN-NEEDS-BUILD (SS-13 #4); ~2px stakes. **NO-WAVE-OWNER.**
- **D-20** *(stability caveat killed under K-5)* — glass-ui 7.0.0 DOES ship `--font-display-weight` (scale.css, verified) + `--type-weight-display`; the demo's :255-262 comment ("no token to swap it") is false and the eight-selector block could be token re-points (seat's note: `--type-tracking-display` also exists, covering the letter-spacing half — the whole block is two re-points). The BG-6 glass-ui ask should be re-derived against the tokenized producer (the ColorSpaceSelector L-10 stale-cell precedent, verbatim). **NO-WAVE-OWNER (prose/config sweep + the standing glass-ui BH relay note).**
- **D-21** — no forced-colors treatment; the stroke is in the forced set, the gradient stops are not — the two graphical layers degrade differently. Absence source-certain; rendering = SS-13 #5. **KF.W9.**
- **L-D9 (cascade half)** *(F-1 half killed, K-5)* — the census `b`/no-glass-ui label is true of the import graph and misleading at the render: every typography rung + four of six custom properties are producer-owned strings no compiler validates (C·S+4's surviving "purchased immunity" observation lives here). **KF.W0 fold (the F-1 identity) + NO-WAVE-OWNER residue (census column note).**
- **L-D10** — two adjacent spans, identical `v-if` + identical class string; one `<template v-if>` cure. **NO-WAVE-OWNER (hygiene).**
- **N-SQ-6** *(merged missed — BOTH readers independently, 2 derivations; reader-B's MINOR preserved as dissent)* — the square is the ONE consumer scene that never binds `--ball-tone`: `.readout-accent` resolves through the inherited token's bare fallback while all three siblings declare it at their stage root (EasingTarget.css:7, SequenceTarget.css:8, SpringTarget per-lane) — identical paint today (`--color-progress` either way), but the square alone cannot participate in the tone system the idiom exists for, and an ancestor declaration would silently re-tone it. **NO-WAVE-OWNER (one-line cure).**
- **N-SQ-7** *(NET-NEW, reader-B)* — `overflow: visible` on the tether (:154) is inert: the stage clips at the identical rect (`overflow: hidden`, SquareScene.css:4). Joins the file's dead-declaration family (vector-effect, preserveAspectRatio, the parent PRM rule, the row-gap). **NO-WAVE-OWNER (hygiene).**
- **N-SQ-8** *(NET-NEW, reader-B)* — the title literal `Transform` (:22) duplicates the engine animation's name (`anim.name = "Transform"`, SquareScene.vue:154 — verified) across a file boundary with no shared constant despite the colocated `squareKeys.ts` existing for exactly this; its evidence value for C-3 is adopted there. **NO-WAVE-OWNER (hygiene).**
- **N-SQ-9** *(NET-NEW, THIS SEAT)* — useSquareKeyboard.ts's header states "The legs are paced by the spring's own settle, not a fixed timer: each corner waits for the chase to arrive" (:20-22) while the implementation is literally `tourTimer = setTimeout(step, 520)` (:66) — the body comment even concedes it ("settles well under 520ms"). A load-bearing architectural comment asserting the opposite of the mechanism, in the scene whose prose already lies about red (D-16), double-click (D-17), and the SVG space (D-1). **NO-WAVE-OWNER (prose sweep, checklist entry).**

## Killed-claims register — 13 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | D-2's limb "the block has GROWN since the rejection; P.W6 added a fifth line" — killed by reader-A, **CONFIRMED by reader-B** | **DEAD (ruling 1)** — self-refuted (the challenge's own table lists PRESS C as owner-quoted row four); VERDICT.md:29 quotes it inside the rejected block (my read); git `-S` → `fd058fd2` pre-review; only the Play re-wording is post-verdict. Reader-B's cell = a killed-limb-confirmed-into-a-CONFIRMED-row inheritance event. |
| **K-2** | D-2 at BLOCKER (challenge + reader-A) | **DEMOTED MAJOR (ruling 2)** — square.json's own `pendingOwner`/`status` record the prune as scheduled-and-unexecuted behind T.B/OD-5 with a stated default; the unambiguous defects are the two false LANDED records + the shipped content. Reader-A's dissent preserved. |
| **K-3** | D-3's substrate limb: "three of four combinations fail", resting on `--muted-foreground = --neutral-5` and the falsifier "glass-ui's tier blocks do not touch it in 7.0.0" — **reader-B re-confirmed the challenge table** | **DEAD (ruling 3)** — ladder.css re-points `--muted-foreground: var(--on-glass-muted)` on `:where(.glass-card,.glass-resting,…)`, and the Card resolves `.glass-resting` (Surface defaults, verified). Corrected: 2 of 4 fail, both light, at both endpoints, under both candidate inks — the MAJOR and sole-cause survive; the dark half dies. Second reader-B inheritance event. |
| **K-4** | D-9's provenance: "commit `f9752c58` deleted the rule and left the class behind"; reader-B's variant "orphaned by f9752c58" | **DEAD** — my git: `--all -S ".square-live-caption {"` → empty (NO revision ever held a rule); single introduction `021f0eba`; `f9752c58` swapped the SIBLING utility (`text-mono-caption`→`text-caption`, verified at `5bf99ca7:41`). Born orphaned. The census's selector row was correct at its time; only its voice row is stale. The orphan-class core (C-7) stands. |
| **K-5** | The F-1 triad: L-D9's npm-ci half ("a lockfile containing zero glass-ui entries"), C·S+4's premise, D-20's stability caveat — **plus reader-B's missed #1 re-booking the baseline as a new MAJOR** | **FOLD ≡ census F-1 ≡ SCH-1, ruled FALSIFIED-at-HEAD and banked 5+ times (kf-AmigaScene et al.; the K-4 restatement law at kf-SequencePlayhead)** — my probe: origin/master declares + locks 7.0.0 exact; the dirty worktree deletes the declaration; local HEAD says 6.0.0 (a different, smaller defect). Sixth and seventh restatement events recorded. Surviving residue adopted: the cascade-coupling half (L-D9 INFO) and the purchased-immunity observation. |
| **K-6** | C-13: "the advertised tumble recruits into the known R1 surface; safe only by cascade order" — killed by reader-A's execution, **CONFIRMED MINOR by reader-B** | **DEAD BY EXECUTION, reproduced by this seat** — `parseCssColor("oklch(0.684 0.250 327.9)")` ok; mix/serialize ok; R1's true input is empty-args `oklch()` (throws in my run) which no token can produce (`getPropertyValue` → `""` → the `if (value)` guard). Safe regardless of cascade order. Residue: the `--rainbow-*` two-producer collision → the census flat-namespace datum (KF.W6); the unchecked reads → useSquareTumble's lane. Reader-B overruled. |
| **K-7** | L·S-5: the file's PRM rule is "the correct PRM posture (suppress the decorative transition, preserve the information)" | **SCOPED (reader-A's kill adopted in scoped form)** — what the rule "preserves" is the ungated motion (D-13, ruled MAJOR); the four lines are correct **for the one transition the file owns** and no more. Survives as S-8, scoped. |
| **K-8** | C-1's corroboration #1: "`stroke-width: 0.8` … at 1:1 px a sub-pixel ghost" as evidence the 0..100 space was intended | **VOID (reader-A)** — `non-scaling-stroke` (:165) pins painted width against any viewBox scale, so 0.8px results either way; challenge-L:57 states this itself. Neutral evidence; the BLOCKER and the other two corroborations unaffected. |
| **K-9** | C-5: the prop JSDoc "asserts an aria coupling this component does not hold" (reader-A confirmed MINOR) | **DEAD (reader-B's refutation adopted, ruling 5)** — the props bind the same `springReadout` members the parent's `aria-valuetext` reads; the parenthetical identifies WHICH numerals, a true provenance note. One-clause prose rider survives to the sweep; the charged version is L-D5. |
| **K-10** | D-15's rationale limb: "0.25rem between title and axes but 0.15rem between the axis rows … a 1.67× ratio" | **DEAD (both readers, independently)** — four children in four explicit columns = ONE row; the row-gap never applies; the vertical-rhythm comparison is vacuous. The off-grid core survives at INFO. |
| **K-11** | L §7(a)'s referral: `dispose()` omits `springSpin` — a possible teardown gap | **RESOLVED NEGATIVE (reader-B; verified by this seat)** — useSquareTumble.ts:48 `onScopeDispose(() => spin.dispose())` in the same setup scope. No row opens in the sibling lane. |
| **K-12** | Reader-B's self-generated hypothesis: `proof:square-honest` keeps `.square-live-caption` load-bearing, so the C-7/D-9 cure would RED a gate | **SELF-REFUTED, RECORDED so it is not re-run** — the script no longer exists; one comment mention repo-wide; no gate reads the square classes. Cures safe. |
| **K-13** | Micro-cells: "exactly three attributes" (challenge-C:58, repeated verbatim by BOTH readers while each listed four — the `<svg>` carries FOUR); reader-B's aria-hidden ":13" (true site :15); reader-A's single-file `registerShortcut` cell (3 consuming files); the challenge's `var(--border)` "×4" (×6, both readers already corrected) | **CORRECTED** — the attribute-count repetition is a cross-reader citation-inheritance signature (zero verdict consequence; recorded per the DISSENT-1 precedent: stale-cell repetition without re-derivation is itself evidence). |

## Superlatives (merged, provenance preserved)

1. **Zero custom-property declarations against a 98-property flat namespace** — every `--` occurrence in :100-212 is a `var()` read; all four own class names namespaced `.square-*`; shared idioms consumed from their single home. Both readers re-derived; strongest single fact about the file.
2. **Zero raw colour literals** — nine token reads, no hex/named/`rgb()`/`hsl()`; the discipline its sibling had to be repaired into (the `aquamarine` retirement, SquareScene.css:38-44, verified). Count corrected: `var(--border)` ×6.
3. **Structurally leak-proof, single-authority by shape** — one import, `defineProps`, one pure `computed`; nothing to tear down; the sole producer identified (useSquareDemo.ts:243-247). L-D3/N-SQ-1 are gaps in what the one feed COVERS, not second writers — compatible and both true. All axes + both readers.
4. **The layer cannot steal the gesture it measures** — `pointer-events: none` + `aria-hidden` on all four roots (:111/:151/:177/:200; :10/:15/:21/:35), complete not partial; the single thing keeping D-8 a legibility problem rather than a functional one.
5. **The degenerate-geometry guard at :92** — `Math.hypot(dx, dy) || 1` prevents NaN `d` output at rest, branchlessly, with the control point collapsing correctly; `bow` capped. Deliberate numerical care in a file whose larger geometry is wrong — and untested (no gate names these classes).
6. **Colocation cited as precedent by the tree** — one 212-line SFC, parent hands the surface over (SquareScene.css:8-10), CubeAxisLines.vue:9 names "the SquareInstrument colocation precedent"; the residue is one dead 4-line rule (D-14), not a failed migration.
7. **Mechanically provable T.D4 font-contract compliance** — every mono leaf carries `tabular-nums`; the UI-voice leaf handled at the idiom (`.status-badge`'s un-layered family bind out-cascades the utility's mono); zero hardcoded font-size/family; the badge recipe byte-identical at all three call sites (undrifted — with C-12's adopted counterpoint: unguarded).
8. **The file-local PRM guard is correct for what it owns** *(L·S-5/S+3, SCOPED per K-7)* — :207-211 kills exactly the decorative fade and says so; the scene-level gap (D-13) is the composables' charge, not this rule's.
9. **The corpus's arithmetic survives hostile re-derivation — and its substrate did not**: reader-B reproduced all 22 published contrast ratios to 2dp and reader-A independently reproduced the oklch→sRGB encodes (#7E5ACC/#BE95EC) — then reader-A found the ink token itself wrong (K-3). The method lesson, stated once: **reproducible arithmetic on an inherited substrate reproduces the substrate's error**; the D-lane's recorded self-correction (the omitted gamma encode, challenge-D:422) and the demo's own AA idiom surviving measurement (7.58/6.33) both stand.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The tether render** (D-1) — drag and hold at non-zero deflection; screenshot the stage's top-left quadrant (predicted: ≤38px violet arc anchored (50,50)px, over the telemetry, 0.8px at 0.45 alpha). The single highest-value probe.
2. **D-8's rects** — stage cell at 1440×900 and 390×844; focus the box, press `c`, capture legs 2 and 4 (derived thresholds ~1030×625 legend / ~790×630 telemetry).
3. **N-SQ-1's witness** — double-tap from rest; one frame mid-roll showing the badge "settled" and no tether. The born-RED material for the snapshot cure.
4. **D-19** — build the demo CSS and read the emitted order (`leading-none` vs `@utility text-display`).
5. **D-21** — forced-colors rendering of gradient field vs forced stroke (UA-dependent).
6. **Legend wrap at phone widths** — `.square-legend` has no max-width in a shrink-to-fit box inside `overflow: hidden`; row count and clipping below lg unknown.
7. **C-12/S-10** — whether Chip's `tone` pipeline reproduces the measured `.status-badge` AA guarantee (needs producer `chipVariants` + contrast evidence).
8. **L-D6's cost** — trace the per-frame re-render of a 4-root component against the paint budget.
9. **N-SQ-3's percept** — the asymmetric tick frame + the swallowed centre tick (stop arithmetic spec-certain; capture with #1).
10. **The resolved plate ink** — ladder's `@container`/`contrast-color` branches make the exact `--muted-foreground` on the resting plate live-only; D-3's verdict is ink-independent but the number wants a frame (both themes).

## Identity guards (fold-by-reference — never re-booked)

- **The F-1 limb ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1** → KF.W0 §B-12 — sixth (three axes) and seventh (reader-B's missed #1) restatement events recorded; reader-B's origin/master lock cells adopted into the fold.
- **The `--rainbow-*` two-producer collision ≡ the census flat-namespace row** (§6.3, "a collision surface worth a lane of its own") → KF.W6 — C-13's surviving residue lives there, with the execution result attached (real values parse; only empty-args throws).
- **N-SQ-2's idiom properties ≡ banked kf-SequenceAxis D-4** (multi-consumer status, bare-`--border` contrast, `--graph-major` tokenized-and-unconsumed) — different file, different mechanism (bypass vs mis-consumption); the shared-idiom cure serves both.
- **D-13, C-3/L-D3, N-SQ-1, D-5, N-SQ-9 are scene-level identities booked HERE** — the forthcoming kf-SquareScene / kf-useSquareDemo adjudications fold their PRM, feed-coverage, disclosure, and prose findings into these rows (anti-rename).
- **C-11/C-12 are census §5 additions S-9/S-10** — intake rows for the shadow census, not re-derivations of S-1..S-8.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold (§B-12). — **KF.W6**: the flat-namespace/`--rainbow-*` datum (with the execution attachment) · the D-20 producer-truth correction rides the standing glass-ui BH relay note (the BG-6 ask re-derived against the tokenized producer). — **KF.W9**: D-13's PRM posture · D-21 forced-colors · D-12 RTL posture · the SS-13 list.
- **NO-WAVE-OWNER — the square scene-repair packet (SS-1/SS-2 authoring must home it):** the **owner-decision gate D-2** (the OD-5/T.M2 sign-off decides which layers live; the false-closure addenda; D-18, D-10, N-SQ-5 ride it) · the **tether-truth commit D-1 + D-6 + D-16 + N-SQ-4** (N-SQ-4 ORDERS the cure: derive-from-travel or px-space, never `viewBox`+`none`) · the **instrument-truth commit C-3/L-D3 + N-SQ-1 + L-D5 + N-SQ-8** (one clock question: what does the strip report, in which mode, under whose name) · the **disclosure commit D-4 + D-5 + C-6** (registry entries + AT-reachable instructions + a keyboard-armed disclosure) · the **field commit D-7 + N-SQ-2 + N-SQ-3** (consume `.stage-field-*` + the graph tokens; stop diluting a hairline) · the **chrome commit D-8 + D-11 + N-SQ-5** (gutter vs envelope; sign column; rung hierarchy) · the **hygiene/prose sweep** D-9/C-7 · D-14 · D-15 · D-17 (×4 sites) · L-D8 (three values, one token) · L-D10 · N-SQ-6 · N-SQ-7 · N-SQ-9 · C-9 · C-5's rider · D-19 · L-D6 · the census-column note (L-D9) · the S-9/S-10 intake.

## Verdict

**ADJUDICATED.** The first full reading of this component ends 40-13-9: one BLOCKER that three axes reached by three roads and no reader could dent, ten MAJORs of which one is brand new (the third spring, missing from its own snapshot), and a killed-claims register in which the two readers are each other's correction — reader-A executed, git-checked, and producer-read its way through four challenge limbs that reader-B confirmed verbatim, while reader-B contributed the sharpest find in the corpus, the joint-unsatisfiability proof, and the severity calibration the manifest itself dictates. This seat re-derived every disputed cell at the bytes, reproduced the parser execution (and R1 itself, at its true degenerate input), demoted two grades, killed one kill's confirmation twice over, and added one find of its own: the tour that claims to be paced by the spring's own settle is a 520ms `setTimeout`. The component remains what the superlatives say — a disciplined, leak-proof, token-clean leaf — drawn in a coordinate system that does not exist, reporting a spring the engine is not driving, titled with an animation it does not observe, beneath an owner ruling it has not yet honoured. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life, N-SQ-1..N-SQ-9 for life.

— end of apotheosis r1. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/square/SquareInstrument.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SquareInstrument.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` appears in this record but NOT in the re-anchor sense** — it is a component-local id here, and the record-qualification rule (KF-W0 §Carry C-1.R row 6) keeps this sweep from booking a dead-token cluster as a re-anchor obligation. Eight records are in this class: kf-ChannelOptions · kf-CubeScene · kf-KeyframesStringControls · kf-OrbitalDrag · kf-SquareScene · kf-SquareInstrument · kf-StartingStyleTarget · kf-TimelineCaret.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

