# shell-dock-dockstatuslamp — adjudicated apotheosis · seat model: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited (M-23/L-18)

**Date**: 2026-08-08 · **HEAD**: `8d7e17fe` · branch `tranche-u`. The corpus was cut at `c654824e`/`4f78e57b`; the subject files are byte-stable across the advance — every line anchor cited below was re-resolved against the live tree by this seat (DockStatusLamp.vue, status-lamp.ts, availability.ts, client.ts, ApiOfflineChip.vue, useApiClient.ts, test/status-lamp.test.ts, o22-status-lamp.spec.ts, shell.css all read whole; every cited line matched).
**Corpus**: three r1 challenge axes (C-implementation · D-design · L-library, all Opus 5 seats) + two hostile r2 re-readers (reader-DU design-and-usage · reader-LC library-and-code). r2 governs; this seat challenged the re-readers back.
**Arbiter's own instruments (all static + probe-JSON reads; zero browser tooling — probe parsimony)**: full source reads listed above; probe/p1-lamp.json read whole (all four viewport blocks re-differenced by this seat); Dock.vue:14/:71/:129-132/:290-296; CurrentPaletteEditor.vue:116/:193; PalettesPane.vue:25/:41/:137; ProfileSection.vue:128-140; foundation.css forced-colors roster (:660-700); shell.css:19-45; harvest `registry/harvest/area-palettes.json` D-12 row read verbatim; sibling apotheoses `adjudicated/CurrentPaletteEditor.md` (K-8/K-12/L-21) + `adjudicated/ColorSpaceSelector.md` (L-14 identity) for identity guards; `registry/CARRY-CUT-LEDGER.md` X-W vocabulary; installed glass-ui 7.0.0: `dist/components/status-dot/StatusDot.vue.d.ts` (label JSDoc verbatim), `dist/styles/typography/scale.css` (`--type-micro: 0.6875rem` — exists), `dist/styles/typography/utilities.css` (`text-mono-caption` = uppercase), `dist/styles/tokens/light-dark.css` (`--destructive: light-dark(hsl(0 72% 50%), hsl(0 80% 60%))`), components.css `sr-only` (1 hit); censuses this seat: `--type-micro` demo consumers = 0; `LampVariant` external refs = 0; `@mkbabb/value.js/value` demo imports = 0; `dist/index.d.ts`/`dist/subpaths/{parsing,units}.d.ts` all MISSING at HEAD; `find demo/test -type d -empty` → `glass/__scratch__` + `palettes/api`; motion-quarantine.md grep for this component/chip → **no row** (exit 1 — nothing constrains or forbids any motion ruling here; no FALSE row exists to carry).
**Wave vocabulary** (per CARRY-CUT-LEDGER): X-W1 gates · X-W4 semantic controls · X-W5 one-scene/typed-action + transport-status scene ownership · X-W8 graph/config subtraction · X-W10 design/motion law. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave; re-resolve anchors before any cure.

## Scoreboard

**32 CONFIRMED (6 MAJOR · 13 MINOR · 13 INFO) · 18 KILLED · 11 missed findings adopted/promoted (folded into the roster) · 0 NO-WAVE-OWNER.**

The one-sentence truth: **DockStatusLamp is a dev-only fork of ApiOfflineChip whose loudest a11y guarantee is false in the shipped bytes, whose gates would stay green if its rendered contract were deleted, and whose dev-gate rests on a written justification the tree falsifies — while the resolver underneath it is the best-tested module in the cone.** The D-axis's three BLOCKERs die to the dev-gate ceiling (both readers, upheld); the structure rows are what survive at MAJOR.

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-8** (dev-gate asymmetry dilemma) | CONFIRMED MINOR (first horn: threading `isDev` is the contrivance) | KILLED (false dilemma) | **KILLED — LC wins** | Both horns fail on my reads: (a) the chip's misconfig face is prod-unreachable (availability.ts:114 loopback gate — `detectDevMisconfig` cannot fire on color.babb.dev), so ApiOfflineChip is not a prod bug; (b) the `isDev` gate is ONE whole-resolver gate (status-lamp.ts:48) required by the `unavailable` branch's deliberate prod-suppression policy (:21-23) — there is no per-branch gate to call contrived. What DU's confirm was reaching for is real but is a DIFFERENT row: the policy's justification is false (R-5). Residue kept: the 88-line scoped style block ships dead to gh-pages (Dock.vue:14 static import; scoped-CSS extraction cannot see `v-if`) — INFO, dies with R-2's cure; magnitude unmeasured (both readers correctly labelled it hypothesis-on-magnitude). |
| **L-6** (the DI seam "cannot seam") | CONFIRMED MINOR ("the SFC cannot be unit-mounted without an App-root provider") | KILLED | **KILLED — LC wins** | useApiClient.ts:33 `export const API_CLIENT_KEY: InjectionKey<ApiClient>` — exported; a `@vue/test-utils` mount supplies any client via `global.provide` (the devDependency exists at ^2.4.10, zero importers — R-3's fact). DU's "cannot be unit-mounted" is false; the seam is substitutable and merely unexercised. Residue INFO inside R-3's cure: `createApiClient()` takes no parameters (:36-44), one provider / three consumers, no test substitutes — a seam nobody has ever used as a seam. L-1's causal chain ("the false seam stranded the resolver, which forced the fork") falls with it — see K-4. |
| **C-2/D-9 grade** (small-caps command) | MAJOR | MINOR (escalation killed) | **MINOR — LC wins** | The defect is real and rendered (DU read the frame; source chain status-lamp.ts:54 → DockStatusLamp.vue:52 `font-variant: small-caps` over :19 verified). But the audience is exclusively the developer, and the byte-correct remedy fires in the same instant on a louder channel: availability.ts:163 `console.error(devMisconfigMessage(…))`, lowercase, copyable, asserted green at test/status-lamp.test.ts:139-152. The lamp is documented as a visible affordance OVER that load-bearing console signal (status-lamp.ts:12-18), not the sole carrier. "Fails at the last inch" is killed (K-17); the rendering defect books at MINOR and dies in R-2's cure with the cure-guard of R-8. |
| **D-1/D-2 grade** (contrast floor) | MAJOR (BLOCKER killed) | "MINOR/MAJOR-dev" (BLOCKER killed) | **MAJOR, dev-ceiling recorded** | The BLOCKER kill is unanimous and upheld (K-1): status-lamp.ts:48 + the 8-row matrix (test:74-78) prove the lamp ships dark in every production build. Within the dev audience I keep MAJOR: the `misconfigured` face is THE face every `dev:web-only` session renders, and 1.79:1/1.38:1 ink defeats the instrument's entire purpose. The mechanism is source-certain without a browser (DockStatusLamp.vue:60 45%-transparent film, :98-102 12% destructive film, no backdrop-filter, `.dock-band` paints no background — the substrate is the user-coloured aurora; **no contrast floor is computable**). DU's independent arithmetic re-derivation (luminances 0.16447/0.22562 from the light-dark token, chip fill = 0.12·destructive + 0.88·field to 1/255) is adopted as the numeric witness; D-2's "inert by construction" mechanism stays killed (K-11). D-10's unavailable-face numbers stay UNPROVEN-NEEDS-LIVE. |
| **C-1(a)** (unnamed alert at every width) | KILLED at MAJOR → INFO | CONFIRMED MINOR | **INFO — DU wins** | `alert`/`status` announce their CONTENTS; no WCAG SC requires an accessible name for either role; ≥1024 the announcement carries the child text (p1-lamp.json childIds:1, re-read by this seat). The consequences cited are rotor/tooling ergonomics. The MAJOR belongs entirely to the `childIds: 0` limb (R-1). The sr-only cure names the region at every width anyway — C-1(a) dies as a side effect, booked INFO so the id is not lost. |
| **C-3 grade** (v-if live region) | MAJOR (shape confirmed, impact PLAUSIBLE) | MINOR | **MINOR — LC wins, with an arbiter arm** | Shape source-certain (:13 `v-if="lamp"`; o22:40 proves the region never pre-exists, then :56-60 asserts it arrives content-loaded). But the lamp's only audience is a developer in dev; AT impact untested by any seat (residue). ARBITER ARM neither reader filed against this component's cure path: **the production-reachable instance of the same shape is the chip's own `v-else-if="offline"` `role="status"`** (ApiOfflineChip.vue:19-26 — inserted with content, prod-live on populated Palettes). The consolidation cure (permanent region, text-toggled) must land on the ONE primitive, or it cures the dev seat and leaves the prod seat broken. |
| **D-5 / C's N-3** (collision) | N-3 KILLED, D-5 upgraded MAJOR ("live latent overlap, 9.3px headroom") | D-5 numbers UNPROVEN, mechanism confirmed | **N-3's universal claim KILLED; D-5 CONFIRMED at MINOR; DU's affirmative demoted** | N-3's "no collision at any width or route" is over-claimed: p3 sampled 4 routes × {1024,1100,1280} with the dock in its DEFAULT layer only — the wide layers were never opened ≤1100 (kill stands, K-10). The threshold arithmetic is sound and mine agrees: p1 w1024 band {x:16,w:992}, lamp flush at 752.4 → overlap begins at dockW > 480.8; measured default dock 457.8 (11.5px clearance). But DU's "9.3px of headroom = live latent overlap" mixes viewports — the 471.5px dock was measured at w1440 (p1), and no probe shows that layer's width at ≤1100. The zero-reservation mechanism is structural and confirmed; the collision EVENT is UNPROVEN-NEEDS-LIVE. Grade MINOR, not MAJOR: dev-only face, `pointer-events: none` (:51) steals no hits — the failure is visual occlusion, unwitnessed. |
| **D-7 severity seat** (infinite pulse) | MINOR on the lamp, + missed-filing transfer to the chip | INFO on the lamp | **INFO on the lamp · MINOR on the chip (severity transferred)** | The lamp instance is dev-only, PRM-gated in the strongest form (superlative S-2), pulsing about a constant — INFO. The D-axis filed WCAG 2.2.2 against the harmless instance and left the live one unfiled: ApiOfflineChip.vue:81-90 runs the identical `2.4s … infinite` pulse with NO dev gate, prod-reachable (backend down + ≥1 saved colour) for the life of the tab with no pause/stop/hide. DU's missed-finding transfer adopted as R-20. Motion-quarantine carries no M-xx row for either file (my grep, exit 1) — nothing constrains the ruling and no FALSE row is carried. |
| **L-13 sub-claims** | CONFIRMED MAJOR (out-of-cone noted) | CONFIRMED MAJOR, `/value ×1` limb killed | **CONFIRMED MAJOR — LC's corrections adopted; identity-folded** | My own re-runs at HEAD `8d7e17fe`: `dist/index.d.ts`, `dist/subpaths/parsing.d.ts`, `dist/subpaths/units.d.ts` all MISSING; `dist/subpaths/` = color/css/easing/math/quantize/transform/value; demo imports of `@mkbabb/value.js/value` = **0** (LC's kill of the `/value` limb verified). IDENTITY GUARD: fact-identical to the ColorSpaceSelector apotheosis' **L-14** (exports↔tsconfig parity, W-HYGIENE fold with the born-RED generator-parity command) — ONE identity, cross-booked, never re-booked under a second name. |
| **L-1 mechanism** (placement forced the fork) | KILLED (chronology) | KILLED (chronology) | **KILLED — unanimous, verified** | File headers read by this seat: ApiOfflineChip.vue:2 "K-INV5 … (R.W3 residual)" + :8 misconfig face at S.W0; status-lamp.ts:2 "T.W6". R < S < T — the chip's inline derivation predates the resolver by two tranches; the LAMP is the fork. Duplication survives intact (R-2); the causal story does not. |

## Adjudicated defect roster

Six MAJOR first; every row carries its terminal disposition. Cross-repo identities are folded, never re-minted.

- **R-1 — C-1b/D-3/L-4 — MAJOR — the empty live region.** `.lamp-label{display:none}` below 1024 (DockStatusLamp.vue:67-69, lifted only at :70-74) removes the only text node from the a11y tree; the dot is `aria-hidden` (:18); no `aria-label` exists; p1-lamp.json w1023/w390 record `ax {role:'alert', name:'', childIds:0}`, `innerText:''` (re-read this seat). The comment at :64-66 ("the role + label stay in the accessibility tree") is **false in the shipped bytes**. The correct idiom ships in-tree: glass-ui `sr-only` (components.css, 1 hit; used at PalettesPane.vue:25 — verified). Killed limbs: the C-1 cure (K-7), the D-3 crescent characterization (K-13). **Terminal: X-W5** (dies inside R-2's consolidation; the sr-only/StatusDot-label decision is that wave's, with R-17's cure-guard).
- **R-2 — C-4/D-4/L-1 — MAJOR — one concept, two implementations, both mounted in dev.** Labels byte-identical (status-lamp.ts:54/:60 vs ApiOfflineChip.vue:17/:25); roles identical; twin keyframes under two names (`lamp-dot-pulse` :110-118 vs `offline-dot-pulse` :82-90, same body, same `2.4s var(--ease-standard) infinite`); the latch derived twice (tested resolver :49-64 vs untested inline :36-37); ~75% of the chip's declarations byte-identical in the lamp (conclusion confirmed; **no specific count may be quoted** — K-14). Double-alert MEASURED in dev by a sibling seat (harvest area-palettes.json D-12: `globalAlertRoles` = the same string twice, frame `frames-D/wk-hover-full.png`) — LC's promotion of L-1's hedge to measured fact adopted. BOUNDARY respected: in a shipped build the lamp is dev-dark, so max one alert — the sibling CurrentPaletteEditor apotheosis' **K-8 kill stands unbroken**; this row is a dev-session + structure defect, never a prod product defect. IDENTITY: the chip-side half is already booked at CurrentPaletteEditor **L-21 → X-W5** — ONE identity, this row is its lamp-side face. **Terminal: X-W5** (consolidation onto the pure resolver + one presentational seat; the T-9/O-10 glass-ui relay question rides the standing BH relay, and any producer-primitive adoption checks the CC-003 Glass-8 trigger first).
- **R-3 — C-9 — MAJOR — the gate stack is vacuous for the rendered contract.** test/status-lamp.test.ts imports only the resolver + transport (:27-41, verified) — never mounts the SFC; `@vue/test-utils` is a devDependency with zero importers; o22 asserts count/visibility/data-variant/role/landmark/mid-Y and **zero** text or accessible-name assertions (whole file re-read); the misconfigured face cannot fire under the harness (o22:21-27's own header; playwright.config VITE_API_URL). Mutations M1-M5 all stay green; M1 (delete :19) deletes the component's only accessible content at every width. **Terminal: X-W1** (CC-030 visual/golden suite + CC-031 CI cliff; the four-line accessible-name assertions land with the consolidation's suite move).
- **R-4 — L-13 — MAJOR — declared type surface ≠ published export map (out of this component's import cone).** Three declared paths targets MISSING (re-verified at HEAD this seat), `/css` (10 demo sites) has no paths entry, `node_modules/@mkbabb/value.js` is a real diverged directory that is what actually typechecks while Vite bundles live dist. `/value` limb killed (K-16). **Terminal: X-W8 via the existing fold** — identity = ColorSpaceSelector L-14 (W-HYGIENE exports↔tsconfig parity, born-RED generator command already booked there).
- **R-5 — ADJ-1 (reader-DU's missed find #1, adopted after independent re-derivation) — MAJOR — production has no backend-down affordance on most surfaces, and the dev-gate's written justification is false.** status-lamp.ts:21-23 justifies prod-darkness with "the `unavailable` face already has its designed PROD affordance (the per-surface ApiOfflineChip)". That affordance is doubly gated — my reads: ApiOfflineChip's ONE mount is CurrentPaletteEditor.vue:116 `v-if="savedColorStrings.length > 0"`, and CurrentPaletteEditor mounts only at PalettesPane.vue:41. Shipped build + backend down: every non-Palettes route shows nothing; Palettes with an empty draft shows nothing — while availability.ts:167-172 latches `unavailable` and saves degrade silently. The empty-draft half is measured (harvest D-12); the route-coverage half + the falsified rationale is this row. All three axes kept the chip as the prod half of their cures — any repair inheriting that inherits the hole. **Terminal: X-W5** (transport-status scene ownership decides the prod surface; the rationale comment is corrected by whichever wave touches the file).
- **R-6 — D-1/D-2 (+D-10 arm) — MAJOR (dev ceiling recorded) — no computable contrast floor.** Ruled above. Mechanism source-certain; measured numbers are chromium-cell (D-seat) + DU's independent arithmetic; D-10's unavailable-face 5.51/4.33 arithmetic-over-hypothetical stays UNPROVEN. The chip-transposition premise (opaque "specimen well" behind the chip) is UNPROVEN-NEEDS-LIVE — D-2's cure narrative may not assume it. **Terminal: X-W10** (the contrast-floor law; the substrate cure itself lands inside X-W5's consolidation).
- **R-7 — C-2/D-9 — MINOR (demoted, ruled above) — small-caps renders the remedy `NPM RUN DEV` + literal backticks.** Source chain verified; frame read by DU. Cure-guard (R-8): the casing cure must never be `text-mono-caption`. **Terminal: X-W5** (label/code-run split in the consolidation; the one-author string is availability.ts:119-130).
- **R-8 — C-5/D-12/L-2 (+ the cure-collision missed find, both readers convergent) — MINOR — phantom token + colliding cures.** `--type-mono-caption`: zero definitions, three consumers, two masks (verified by both readers; grep reproduced). The L-axis cure is REFUTED as a repair input: glass-ui `text-mono-caption` = `text-transform: uppercase` (utilities.css, read this seat) — adopting it would harden R-7 permanently; and the in-tree "precedent" is a REJECTION comment (ProfileSection.vue:130-134, read verbatim this seat — K-9). Meanwhile **`--type-micro: 0.6875rem` exists** (scale.css, read this seat) with **zero** demo consumers — the byte-exact rung for the current size was passed over. Dead fallbacks `--radius-pill, 9999px` / `--destructive, oklch(…)` confirmed (both tokens resolve). **Terminal: X-W10** (type-ladder law; the sites die in X-W5's consolidation).
- **R-9 — C-3/D-8 + arbiter arm — MINOR — v-if inserts region with content; the prod instance lives in the chip.** Ruled above. D-8's first-paint scoping is compatible (misconfigured latches at module eval, before mount); its absolute "never announced" claim stays unproven. **Terminal: X-W5** (permanent region on the ONE primitive).
- **R-10 — D-5 + N-3 kill — MINOR — zero-reservation absolute seat beside a content-width dock.** Ruled above; threshold arithmetic adopted, collision event UNPROVEN. **Terminal: X-W5** (the seat becomes owned layout — in-flow grid area or shell-owned rule — dissolving R-15 with it).
- **R-11 — D-6 — MINOR — compaction asks the viewport, not the band.** `@media (min-width:1024px)` (:70-74); `.dock-band` declares no `container-type` (shell.css:39-45, read this seat). DU's correction adopted and verified: **1024 is byte-identical to Dock.vue:71** `useMediaQuery("(min-width: 1024px)")` driving `:always-expanded="!isDesktop"` (:132) — the real defect is a duplicated magic literal across one JS and one CSS file with no shared token, and the lamp compacts exactly when the dock goes always-expanded. The 26→255.6px step is measured (p1). **Terminal: X-W10** (container-scaled law; O-10 already relayed the container-query question to glass).
- **R-12 — C-6 — MINOR — `default: return null` disables exhaustiveness over `ApiAvailability`.** Verified :49-64 vs the 4-member union (availability.ts:40-44). A fifth member compiles clean and goes dark silently — against the repo's structural-enforcement law. **Terminal: X-W5** (the resolver survives the consolidation; the `never` guard lands there).
- **R-13 — D-11 — MINOR — zero rendered witnesses; the shipping face cannot fire under its own gate.** Harness bound recorded in o22's own header (verified); absence from the 60 captures is corroborated-by-two, not re-proven here. **Terminal: X-W1** (a latch-forced capture arm in CC-029/CC-030's matrix).
- **R-14 — L-5 — MINOR — two paths to `@mkbabb/glass-ui/dock` on adjacent lines; self-referential barrel; arbitrary public surface.** Dock.vue:4 vs :5 verified; LC's cycle precision (index.ts → Dock.vue → index.ts, entered from App.vue) and 13-direct-importers count adopted. **Terminal: X-W8** (CC-007 barrel subtraction).
- **R-15 — L-10 core — MINOR — the positioning contract is non-local and unenforced.** `position:absolute` (:44-47) against `.dock-band{position:relative}` authored in shell.css:39-40 (comment names the dependency — superlative S-8), mounted from a third file. The overflow/disappearance mechanism is killed (K-5). **Terminal: X-W5** (dies with R-10's seat cure).
- **R-16 — L-7 — MINOR — two unit-suite homes; the demo matrix lives in the library tree.** test/status-lamp.test.ts imports demo modules; 10 such files vs demo/test's three; vitest includes both globs with no rule. **Terminal: X-W8** (suite-home subtraction; the matrix moves with the consolidation).
- **R-17 — L-3 — MINOR — three shipped glass-ui primitives hand-rolled twice.** `./status-dot`, `./chip`, `./pulse`, `./badge` all in the producer export map; StatusDot/Pulse/Badge demo consumers = 0. CURE-GUARD (kill K-8 upheld): StatusDot's JSDoc — "Accessible identity. **Omit when adjacent text already names the state**" (d.ts read verbatim this seat) — the L-axis's own prescribed render double-names; the adopting wave passes `label` XOR renders adjacent text, never both. Adoption checks CC-003's Glass-≥8 trigger before consuming producer primitives. **Terminal: X-W5.**
- **R-18 — reader-LC missed find #1, adopted after my own trace — MINOR — `VITE_API_URL=""` silently disarms the honesty contract.** client.ts:37 `??` passes `""` through (BASE_URL = ""); availability.ts:154 `Boolean("")` = false — the two disagree. `isCrossOrigin("", origin)` → same-origin → no misconfig; requests hit `""+path` relative → the dev server answers with an HTTP status → `markApiReachable()` → latch reads `available`. Silent 404s, green latch, no lamp, no loud console.error — the exact footgun class S.W0-1 abolishes, through the one env var the contract keys on. Traced end-to-end by this seat in source. **Terminal: X-W5** (transport honesty; one emptiness predicate shared by both files).
- **R-19 — reader-DU missed find #2, re-differenced this seat — MINOR — the lamp sits ~3px below the pill's optical axis at every viewport, against its own comment.** :38-40 claims "vertically centred on the pill's axis"; the code centres on the BAND (:46-47 vs shell.css:39-40). My differences from p1-lamp.json: lamp midY − dock midY = +3.5 (w1440) / +3.1 (w1024) / +3.1 (w1023) / +3.0 (w390). o22's 62px band tolerance cannot catch it. The producing declaration is producer-side and UNPROVEN-NEEDS-LIVE. **Terminal: X-W5** (seat cure).
- **R-20 — reader-DU missed find #3 (the D-7 severity transfer), verified in source — MINOR — the ungated production pulse.** ApiOfflineChip.vue:81-90: identical infinite pulse, no dev gate, prod-reachable, no pause/stop/hide. No M-xx row exists for either file (grep exit 1). **Terminal: X-W10** (motion law; the keyframe consolidates to one home in X-W5 — moved, never deleted, PRM guard preserved).
- **R-21 — C-1a — INFO** (demoted, ruled above) — unnamed live region; dies with R-1's cure. **Terminal: X-W5.**
- **R-22 — C-7 — INFO — only `misconfigured` is selector-addressed; the `unavailable` face is an implicit default.** Verified :76-86 vs :91-107. Consequence requires a third variant; the invariant is held by there being exactly two. **Terminal: X-W5** (explicit `[data-variant]` blocks fall out of the consolidation).
- **R-23 — D-7/C-10 (lamp instance) — INFO** — an infinite pulse signifying a latched constant, `2.4s` untokenized; dev-only, PRM-gated in the strong form (S-2). **Terminal: X-W10.**
- **R-24 — D-13 — INFO — raw literals on no ladder; the fork already renders at two densities** (padding 0.55rem vs 0.7rem verified in both files). **Terminal: X-W10.**
- **R-25 — D-14 — INFO — no forced-colors arm** — confirmed by construction this seat: no `@media (forced-colors)` in the file; foundation.css's tier-1 roster (:660-700, read) does not include the lamp; under WHCM + <1024 the only variant channel is a 6.4px ring-vs-fill shape, compounding R-1. Dev ceiling → INFO. **Terminal: X-W10.**
- **R-26 — L-8/L-9 — INFO — two dead barrel hops publish the chip to nobody; both S.W0-1 contract pointers are dead alias paths** (`@lib/…` at status-lamp.ts:16; `@components/…` at status/index.ts:5 — LC's :16 correction adopted). **Terminal: X-W8.**
- **R-27 — L-11 — INFO — demo `.fira-code` shadows the producer utility** (drops `font-feature-settings`; no-op today, silent shadow on divergence). **Terminal: X-W8.**
- **R-28 — L-12 — INFO — `import.meta.env.DEV` read raw in a leaf SFC** while the resolver takes it as a parameter (the parameterisation itself is superlative S-1). **Terminal: X-W5.**
- **R-29 — reader-LC missed finds #3/#4, verified this seat — INFO — comment-vs-code drift in the trusted module + suite isolation.** (a) availability.ts:148 claims "Idempotent"; the body (:151-164) has the browser guard and NO idempotence guard — same drift class as R-1's comment, in the module all axes declared "already correct". (b) test/status-lamp.test.ts:43-48's teardown comment claims public-transition resets while cases assign the ref directly (:122/:132/:143) and `misconfigDetail` (availability.ts:71) + `unavailableSince` (:61) are never reset — order-dependent by construction. **Terminal: X-W5** (the consolidation's suite move fixes both or corrects the comments).
- **R-30 — both readers' `@reference` missed find (one identity) — INFO — dead directive.** DockStatusLamp.vue:36 heads a block with zero `@apply`/`theme()` (whole file read); the chip does without it. LC's repo census (15 of 17 demo SFCs dead) adopted as LC-measured, not re-counted. **Terminal: X-W8.**
- **R-31 — reader-LC missed find #6, verified this seat — INFO — dead public surface + empty scaffolding.** `LampVariant` exported with zero external references (my grep: 2 hits, both in its own module); `demo/test/glass/__scratch__` + `demo/test/palettes/api` are empty dirs in the suite home R-16 migrates into (my `find`). **Terminal: X-W8.**
- **R-32 — reader-LC missed find #7 — INFO — mixed logical/physical axis in the certified seat.** :44-47 pairs `inset-inline-end` (logical) with `top`/`translate` (physical) — inert in horizontal-tb (why the RTL probe passed), accidental under `vertical-*`; sits inside the exact lines both r1 axes certified clean. **Terminal: X-W10.**

## Killed-claims register

- **K-1 — D-1/D-2/D-3 BLOCKER framing (unanimous, upheld).** status-lamp.ts:48 gates EVERY variant; `gh-pages` is a production build (DEV=false); test:74-78 asserts all four prod rows null. No end user can see this component. The D document never applies the ceiling in 507 lines while quoting the dev-gate in its own negative column.
- **K-2 — C-8's false dilemma (LC's kill upheld over DU's partial confirm — ruled above).** Residue (dead CSS bytes) rides R-2.
- **K-3 — L-6 "false seam / cannot be unit-mounted" (LC's kill upheld — ruled above).** `API_CLIENT_KEY` is exported; `global.provide` mounts it today. Residue INFO inside R-3.
- **K-4 — L-1's placement-forced mechanism (unanimous; chronology verified this seat: chip R.W3/S.W0 < resolver T.W6).** The lamp is the fork; the resolver's home cannot have forced a fork that predates it.
- **K-5 — L-10's `.dock-band{overflow:hidden}` disappearance mechanism (unanimous; verified this seat).** shell.css:26 is `.app-layout`'s overflow; `.dock-band` (:39-45) declares none; losing `position:relative` re-parents to `.app-layout` (itself relative) → visible mis-placement, not clipping.
- **K-6 — C-1(a) at MAJOR grade** (ruled above; INFO).
- **K-7 — C-1's prescribed cure** (`aria-label` on the region + `aria-hidden` on its only text) — strictly worse than today: it empties the announceable payload at EVERY width; live regions announce contents, not labels. The sound cure is sr-only (R-1).
- **K-8 — L-3's cure limb** — `<StatusDot :label>` beside a span of the identical string double-names the state against the producer's own JSDoc (read verbatim this seat).
- **K-9 — L-2's in-tree precedent** — ProfileSection.vue:130-134 is a comment REJECTING `text-mono-caption` ("shouted @MBABB as generic labelware"); the element uses `text-mono-small`. Precedent AGAINST the cure, and the utility would harden R-7 (uppercase is semantic there).
- **K-10 — C's N-3 "no collision at any width or route"** — over-claimed; wide dock layers never opened ≤1100 (ruled above).
- **K-11 — D-2's "dark-mode treatment inert by construction"** — falsified by the token itself: `--destructive` is `light-dark(…)` (read this seat) and DOES switch; the treatment is counter-productive (ink brightens, substrate doesn't follow → contrast falls), which is stronger than the sentence filed.
- **K-12 — D-9's "Fira Code has no smcp table" mechanism** — unproven by any probe and irrelevant: small-caps capitalizes lowercase whether real or synthesized. A disprovable decoration on a correct finding.
- **K-13 — D-3's "reads as a clipping artifact"** — an artifact of the axis's own band-box crop (358×72 = p1's bandBox at w390); `clippedRight: 0` at all four viewports. Must not enter a repair spec.
- **K-14 — all three duplication counts (26/34/17 vs 24/33/18 vs 13/18/11)** — mutually inconsistent for their stated scopes; the conclusion is confirmed on byte-level evidence, but no wave may quote any of these numbers as measured fact.
- **K-15 — D's Section-1 self-citation** ("absent from every capture (D-12)") — that claim is D-11; D-12 is the phantom token. Prose-precision defect in a document whose authority is citation.
- **K-16 — L-13's `/value ×1` limb** — zero `/value` imports in demo (LC's enumeration, reproduced by this seat's grep).
- **K-17 — C-2's "the lamp's entire purpose fails at the last inch"** — the byte-correct remedy fires simultaneously on the console channel, test-asserted (ruled above).
- **K-18 — two provenance pointers (LC's corrections adopted):** the "impossible by construction" sentence lives in shell.css:17-18, not Dock.vue:124; the `@lib` string is status-lamp.ts:16, not :14. Both underlying findings survive; the citation chains do not.

## Superlatives — provenance preserved

- **S-1 — `status-lamp.ts` is the best module in the cone** (all three axes + both readers independently): pure, total, `isDev` as a PARAMETER (:44-47) with the rationale written down (:23-25); the full (availability × dev) matrix closed-form asserted, all 8 cells + distinctness (test:51-94, read this seat).
- **S-2 — the strongest-form PRM idiom**: keyframes AND animation both inside `@media (prefers-reduced-motion: no-preference)` (:109-122) — `reduce` resolves to the static state with no counter-rule; the tranche later adopted this repo-wide FROM glass-ui (INBOX O-7 §C) and this T.W6 file already had it.
- **S-3 — the latch enforces `misconfigured ≠ unavailable` AT THE LATCH**: availability.ts:167-172 opens `if (misconfigured) return` — a transport failure can never relabel a dev-config error; proven at test:131-137.
- **S-4 — one message, one author, two channels**: `devMisconfigMessage` (:119-130) names origin, wrong target, CORS cause, exact remedy, alternative, and the explicit non-conflation — consumed by both console.error (:163) and DevMisconfigError (:141). The model the lamp's 38-character paraphrase should consume.
- **S-5 — the oracle publishes its own blind spot**: o22:21-27 records that the misconfigured face CANNOT fire under the harness and names where the gap is covered — the reason D-11/R-13 was findable at all.
- **S-6 — retirement done right, with a live resurrection guard**: o22:37's standing negative watch on `.dev-misconfig-banner` — no shim, no alias, a test that notices regression (the `feedback_no_backwards_compat` pattern executed).
- **S-7 — the e2e trips the latch with a REAL transport failure** (o22:48-50 `route.abort("connectionrefused")` → the actual fetch-rejection path end-to-end) and asserts arrival with no dock interaction.
- **S-8 — the coupling is written down at the site that creates it**: shell.css:37-38 "position: relative seats the band-chrome instruments (the W6-6 status lamp)" — unenforced (R-15), but the future editor is told in-line what breaks.
- **S-9 — zero runtime hazard surface by design**: the whole `<script setup>` is 8 lines, one computed; no listener/rAF/timer/observer/fetch/await anywhere (C's N-9 grep, shape confirmed by this seat's whole-file read) — in a repo whose memory records a PRM-RAF epidemic and a nesting-accumulation crash, an inert reactive leaf is the right shape.
- **S-10 — negative proofs recorded and shared**: C's N-1..N-9 (including N-5 killing its own 390px-occlusion hypothesis via `elementFromPoint`) and D's RTL frame + explicit no-finding — probe budget published so no seat re-spends it, even though N-3 itself fell (K-10).

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **The collision event** (R-10): a wide dock layer (slug-edit / action-bar) opened at ≤1100px vs the lamp's flush seat — measure the layer widths at w1024; the threshold is dockW > 480.8 at that viewport (arithmetic pinned above).
2. **D-10's unavailable-face contrast** (5.51:1 light / 4.33:1 dark) — arithmetic over a hypothetical composite; force the `unavailable` latch live and measure both schemes.
3. **Seed-dependence of every contrast number** (R-6): all ratios are functions of the user-coloured aurora; no pass/fail generalises from the default seed — the audit arm should sweep seed extremes at both schemes (this is also the strongest statement of the defect: no computable floor).
4. **AT behaviour** (R-9): v-if insert-with-content announcement across NVDA/JAWS/VoiceOver, and D-8's load-present-alert claim — untested by every seat; includes the chip's prod-reachable `role="status"` instance.
5. **The ~3px lamp/pill misalignment's producing declaration** (R-19): measured at all four viewports; no demo rule targets `.glass-dock` — producer-side, needs live style attribution.
6. **The chip's substrate premise** (R-6/D-2): whether the palette well is actually opaque behind the chip was never measured — no frame or computed-style probe exists; the transposition narrative may not assume it.
7. **gh-pages dead-bytes magnitude** (K-2 residue): mechanism certain (static import + static CSS extraction), magnitude measured by nobody.
8. **D-5/D-6's specific interval numbers** (426.2/230.6/56.2 gaps; the 26→247 step's 72.3% share) — single-session probe values, not re-run this seat; mechanisms confirmed from source.
9. **The 60-capture absence** (R-13): corroborated by two axes reading the PNGs; not re-read here; cause benign (healthy-backend harness).

## Closing verdict

**DEFECTIVE — ADJUDICATED.** 32 rows survive (6 MAJOR: the empty live region + false comment, the two-implementation fork, the vacuous gate stack, the type-surface drift (folded to its standing identity), the production affordance hole under a falsified rationale, and the floor-less contrast design), 18 claims killed with rationale — including both readers' one direct verdict conflict each (C-8, L-6, both resolved for reader-LC) and every unsound cure the corpus tried to hand a repair wave (aria-hidden labels, StatusDot double-naming, `text-mono-caption`). The D-axis's three BLOCKERs die to the dev-gate ceiling; nothing in this component can reach a production user — but the sibling it forked CAN, and the two production-reachable defects in this corpus (the ungated pulse, the v-if status region) both live in the chip, which is why every terminal cure routes through the ONE-primitive consolidation at **X-W5**, gated at **X-W1**, subtracted at **X-W8**, and lawed at **X-W10**. The resolver module and its matrix are the keep: the repair must be built ON `status-lamp.ts`, not around it. Dispositions are ADJUDICATED, not VERIFIED; the challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
