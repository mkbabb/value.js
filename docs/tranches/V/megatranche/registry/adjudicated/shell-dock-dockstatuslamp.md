# shell-dock-dockstatuslamp — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: adjudicator-F (incredulity seat over two hostile re-readers).
**Date**: 2026-08-08 · **HEAD at this seat's reads**: `027ca138` (branch `tranche-u`; the corpus was authored against `c654824e`/`4f78e57b` ancestry; last commit touching the SFC is `a61094e3` — my full reads confirm every cited line anchor still resolves; the one drifted anchor found is Dock.md G-Q's `status-lamp.ts:47` → now `:53`).
**Subject**: `demo/shell/dock/DockStatusLamp.vue` (123 L) + `demo/shell/dock/status-lamp.ts` (65 L) + the transport/twin cone.
**Corpus (r2 governs)**: `audit/components/shell-dock-dockstatuslamp/challenge-{C-implementation,D-design,L-library}.md` + `frames/` (7 PNG) + `probe/` (4 scripts + 2 JSON + 2 PNG). The directory holds exactly one challenge set with no r1/r2 suffixes — per the ApiOfflineChip.md precedent (its §Corpus r2 note), the extant set IS the governing text. Motion-quarantine bar: `grep -i 'lamp\|dockstatus' motion-quarantine.md` → zero M-xx rows name this component (M-10/M-26/M-34 name others); no FALSE row is carried anywhere in this record.
**Readers adjudicated**: reader-A (design-and-usage) · reader-B (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement (C-3/D-8, D-11, D-6 grade, D-7/C-10 grade, L-13, the phantom-token census, C-8's kill scope) was re-derived against the tree by this seat with its own instruments, and **both readers' L-13 confirmation cells are overturned below by this seat's own compiler run**.

**Arbiter's own instruments (all static + node/tsc against the working tree; ZERO browser tooling per law)**: full reads of DockStatusLamp.vue (whole, 123 L — :52 small-caps, :54 phantom var, :60 base wash, :67-74 fold pair, :91-107 misconfigured block, :109-122 PRM block all re-anchored), status-lamp.ts (whole), availability.ts (whole, 195 L), useApiClient.ts (whole, 62 L), ApiOfflineChip.vue (whole), test/status-lamp.test.ts (whole, 160 L; `grep -c "expect("` = **19**), o22-status-lamp.spec.ts (whole, 85 L), shell.css:1-60 (`.app-layout` :19-28 owns `overflow:hidden` at :26 + `position:relative` :25; `.dock-band` :39-45 has NO overflow — the L-10 decider; the "impossible by construction — grid rows do not overlap" sentence lives at :17-18, NOT Dock.vue — the D-5 citation decider), foundation.css:655-800 (forced-colors two-tier roster; `prefers-contrast: more` `--card-edge` 55% lift; `prefers-reduced-transparency` roster = `.glass-resting/.glass-floating/.glass-wash/.veil-surface/[data-surface="veil"]` — lamp on none), Dock.vue:1-16 (:4 barrel vs :5 direct) + :285-294 (mount), App.vue (nav :24 · main :47 · ErrorBoundary :50-140 wraps `<main>` only · `provideApiClient()` :219), CurrentPaletteEditor.vue:112-120/:193, all three barrels, utils.css:5-14, client.ts:40-44 (`initApiEnvironment(BASE_URL)` at module eval). **Producer bytes (mine)**: utilities.css — `@utility text-mono-caption { … font-size: var(--type-caption) … text-transform: uppercase }` AND `@utility text-mono-micro { … font-size: var(--type-micro); letter-spacing: 0.025em }` AND `@utility fira-code { … font-feature-settings: "liga","calt" }`; scale.css — `--type-micro: 0.6875rem` (byte-identical to the lamp's fallback), `--type-admin-label: 0.625rem`, `--type-caption: clamp(0.75rem, …)`; scheme-motion.css — `--type-tracking-caps: 0.1em`, duration ladder `0.1/0.12/0.2/0.3/0.45/0.55/1/1.5s` + shimmer `3/5/6s` — **no 2.4s rung**; feedback.d.ts — `PULSE_STATES = ["active","idle","success","warning"]` (**no `error`**), `STATUS_DOT_STATES = ["online","warning","error","unknown"]`; StatusDot.vue.d.ts — `label?: string` with the "Omit when adjacent text already names the state" JSDoc; exports incl. `./badge ./chip ./dock ./pulse ./status-dot`; `grep -c -- "--type-mono-caption" dist/glass-ui.css` = **1** (the producer's own minified consumer — the FOURTH site both readers missed). **Greps/censuses (mine)**: `--type-mono-caption:` declarations = 0 across demo/src/glass dist; demo consumers = 3 (two `0.6875rem` masks + one `var(--type-caption)` mask); `@vue/test-utils` importers = **0** (devDependency `^2.4.10` at package.json:96); `LampVariant` = 2 hits (declaration + internal use); `@mkbabb/value.js/value` + bare-root **import statements** = 0 across demo/e2e/scripts/test; outer `palettes/browser` barrel importers = 0; harvest `area-palettes.json:2125` — `globalAlertRoles = ["dev misconfigured — run \`npm run dev\`", …×2]`, both seats visible in `frames-D/wk-hover-full.png`. **Config (mine)**: `package.json#exports` = 7 keys, no `.`; `typecheck` = lib+demo projects only; tsconfig.demo `paths` — `test -f` → `dist/index.d.ts` MISSING, `dist/subpaths/parsing.d.ts` MISSING, `dist/subpaths/units.d.ts` MISSING, while `dist/subpaths/{css,value}.d.ts` EXIST unentried; tsconfig.demo `include = ["demo/","src/vite-env.d.ts"]`, tsconfig.lib `include` = src-only entry list → **`test/**` is compiled by NO TypeScript program** (reader-B's missed row, verified). **THE L-13 DECIDER (my own run)**: `npx tsc -p tsconfig.demo.json --noEmit --traceResolution` → `@mkbabb/value.js/css` **successfully resolved to `/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts` with Package ID `@4.0.0`** (×4 shown) — the LOCAL dist via package self-name resolution, never the Jul-17 node_modules copy. **My own eyes**: `frames/band-1440-light.png` (small-caps `● DEV MISCONFIGURED — RUN ˋNPM RUN DEVˋ` with literal graves; a red hairline pill on a salmon aurora; the orphan span to the wide slug-edit pill) and `frames/band-390.png` (the label-less crescent hugging the viewport edge). **Registry cross-reads (the identity map)**: ApiOfflineChip.md (AP-1 **BLOCKER, MT-F031 executor** — pins the lamp arm `status-lamp.ts:50-55` + `DockStatusLamp.vue:88-107` for death; AP-2/AP-3/AP-4/AP-9/AP-13/AP-14/AP-15/AP-18/AP-19/AP-21/AP-22/AP-23/AP-26; killed #5/#8; DISSENT #1 echo events, #3 transport NO-WAVE-OWNER flag), Dock.md (**MT-DOCK-LAYERS-1** owns `DockStatusLamp.vue` + `status-lamp.ts` BY NAME in §6.5 scope; D2-14 re-roles the lamp `alert`→`status` (INFO); D3-10 re-homes the mount to the band; DELTA-7/C-34/G-K(iii) announcement family; G-Q hygiene arms), X/waves/W5.md:274 (**C7 names `DockStatusLamp:70`** in the viewport-query subtraction, RED-measured), X/waves/W10.md:307 (DockStatusLamp.vue named in the **CONFORMING** no-preference motion baseline), CARRY-CUT-LEDGER X-map, ColorSpaceSelector.md L-14 + PaletteColorStrip.md K-2/PCS-30 (the config-parity identity + the prior stale-tarball kill).
**Environment disclosure**: no dev server touched; the readers' and challengers' live cells (chromium/CDP, PNG decoders) are adopted as corroboration only and quarantined into the UNPROVEN residue where load-bearing.

## Scoreboard

**31 rows CONFIRMED (8 MAJOR · 13 MINOR · 10 INFO) · 18 killed-register entries (16 corpus kills sustained, every one re-derived; 2 reader confirmation cells OVERTURNED by this seat's own instruments) · 11 reader-missed rows adopted (A×5, B×6, all re-verified) · 7 UNPROVEN-NEEDS-LIVE residue items → visual-audit inputs.**

**Headline — the corpus fights a battle the registry already won.** Neither hostile re-reader (nor any of the three axes) consulted the adjudicated registry: **AP-1 (ApiOfflineChip.md, BLOCKER) executes MT-F031 against this component's own misconfigured arm by file:line** (`status-lamp.ts:50-55` + `DockStatusLamp.vue:88-107` — "REMOVED, not restyled and not gated"), and **MT-DOCK-LAYERS-1 (Dock.md) owns both subject files by name** with D2-14 (re-role), D3-10 (re-home), DELTA-7/G-K (announcement), and G-Q (hygiene) already gating much of this corpus's territory. Consequence: the misconfigured-face rows the D axis graded BLOCKER — the 1.79:1 ink, the untypeable `NPM RUN DEV`, the 12% wash — are **moot-on-AP-1** (recorded so no restyle is ever attempted instead of the kill, the AP-6 idiom), and the surviving work routes through five standing owners, not a new wave. Second headline: **both readers confirmed L-13's stale-tarball mechanism as MAJOR, and it is false** — my own `--traceResolution` run (agreeing with PaletteColorStrip K-2 and Dock R-3, three independent arbiter instruments now) shows `/css` typechecking against this checkout's live dist via package self-name resolution. Third: the phantom-token consumer census is now wrong THREE times in a row (axes 3 → both readers 3 → truth **4**, the producer's own minified rule) — the exact echo species ApiOfflineChip DISSENT #1 already recorded.

## Ruled disagreements (the adjudication's actual work)

| id | reader-A | reader-B | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-3/D-8** (v-if live region; first-paint alert) | UNPROVEN-NEEDS-LIVE (AT behaviour not byte-decidable) | CONFIRMED MAJOR ("AT does not announce regions present at load") | **SPLIT — mechanism CONFIRMED MAJOR, magnitude to residue** | Structure is source-certain (my reads): `client.ts:43` runs `initApiEnvironment(BASE_URL)` at module eval → `availability.ts:161` latches `misconfigured` synchronously BEFORE mount, so the alert exists in the initial render; `o22:40`'s `toHaveCount(0)` proves the region never pre-exists for the `unavailable` face — insert-with-content for one face, present-at-load for the other: neither is the announcement shape live regions are specified for. Per-AT behaviour stays residue U-1. Disposition: FOLD → MT-DOCK-LAYERS-1 DELTA-7 (one dock-owned polite region) + D2-14; the boot-time face needs no region at all — its owner-sanctioned channel is `availability.ts:163`'s console.error, and post-AP-1 that face has no visual seat anyway. |
| **D-11** (zero rendered witnesses) | framing KILLED → INFO residue ("zero pixels is the component behaving") | CONFIRMED MAJOR (the meta-finding; cure is a prerequisite) | **MERGED into DSL-2 at MAJOR — A's framing kill sustained, B's prerequisite note adopted** | The 60-capture absence is correct behaviour (healthy backend; C's N-7 positive proof re-read by me), so "this is how D-1..D-9 survived four tranches" dies as rhetoric. What survives is not a framing but a fact: NO harness in the repo can force the latch, so the shipping face is unwitnessable by construction — one identity with C-9's vacuous gate, MAJOR, and its cure (a forced-latch capture arm) is the instrument for residue U-2. |
| **D-6 grade** (viewport wrong-question) | MAJOR | MINOR | **MINOR — B wins** | The mechanism is already owned BY NAME by a RED-measured gate: X-W5.md:274 C7 counts `DockStatusLamp:70` in the viewport-query subtraction (7 → 3). A dev-only component whose defective line is already scheduled to die under a numbered gate is not MAJOR. The `container-type` absence on `.dock-band` (my shell.css read) and the producer-side relay (INBOX O-10 §A, verified) are both confirmed facts riding that death. |
| **D-7/C-10 grade** (perpetual pulse) | INFO (per the C axis) | MINOR ("a pulse signifying a constant") | **INFO — and B's premise dies with AP-1 (this seat's sharpening)** | B's confirmation reasons from the `misconfigured` face ("the condition it pulses about cannot change" — true, `availability.ts:169` refuses the downgrade). But AP-1 kills that face's visual seat entirely; the SURVIVING `unavailable` face is recoverable (`markApiReachable`, :175-179), so the surviving pulse signifies a changeable state and the "constant" defect is moot-on-AP-1. The 2.4s literal residue ≡ **AP-21** (X-W10; no producer rung — my scheme-motion read makes "tokenize it" a producer mint, sustaining A's kill of the tokenization prescription). WCAG 2.2.2 ≡ AP residue U-5. W10:307 already counts this file's `no-preference` block CONFORMING — the PRM structure must survive any cure verbatim. |
| **L-13** (declared type surface ≠ export map) | CONFIRMED MAJOR (with a scope flag) | CONFIRMED MAJOR (minus the `/value` limb) | **BOTH CELLS OVERTURNED — RESCOPED to INFO, W-HYGIENE identity** | My own compiler run (pasted in instruments): `@mkbabb/value.js/css` resolves to **this checkout's** `dist/subpaths/css.d.ts@4.0.0` — package self-name resolution, NOT the stale node_modules copy. The headline mechanism both readers carried ("vue-tsc checks the stale copy while Vite bundles the live one") is refuted; type and runtime land on the same artifact by two different mechanisms. Third independent instrument to reach this: PaletteColorStrip K-2 (`--traceResolution`), Dock R-3 (`ts.resolveModuleName`), now this seat. Survivors, all verified by me: 3 dead `paths` targets, `/css`+`/value` unentried, no `.` key, the stale real-directory in node_modules (real but unconsulted) — **≡ ColorSpaceSelector L-14 ≡ PCS-30 ≡ Dock L-22, ONE identity at six confirmations, cure superseded to DELETE (Dock R-3)** → NO-WAVE-OWNER, identity preserved (X-W8.md declines it by name). B's K-7 (`/value` zero importers) is sustained by my grep. |
| **Phantom-token census** | 3 sites | 3 sites | **4 sites — both readers undercounted** | `grep -c -- "--type-mono-caption" node_modules/@mkbabb/glass-ui/dist/glass-ui.css` = 1 (the minified `.timeline-popover-state` rule). ≡ ApiOfflineChip disagreement #1, where the same undercount was already corrected once — this is the cell's THIRD uncorrected repetition in the program, recorded as an echo-integrity event. |
| **C-8 kill scope** | "unreachable in any non-loopback deployment … No defect on either side" | dilemma false via `detectDevMisconfig` | **KILL SUSTAINED, scope corrected per AP-18/AP killed-#8** | The dilemma is false (both horns fail; my availability.ts read). But A's "unreachable in production" conflates deployment with build: a production BUNDLE served on loopback (`vite preview`, no VITE_API_URL) latches `misconfigured` with the lamp dark — the AP-18 population (MINOR there, moot-on-AP-1). The kill survives on B's narrower ground. |
| **B's "safe today only by ordering accident"** (error-boundary miss) | — | filed as proof limb | **LIMB CORRECTED — this seat** | `provide()` in App.vue's setup (:219) precedes every child mount by Vue's provide/inject contract — template position of the `<nav>` (:24) is irrelevant. Not an accident. The row survives on its true ground: the dock band sits OUTSIDE the sole ErrorBoundary (:50-140 wraps `<main>` only), so any dock-band setup throw is an unprotected white screen against the App.vue:48-49 U-F58 law. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Ownership map: **AP-1/MT-F031** (misconfigured face dies, both seats, files pinned) · **MT-DOCK-LAYERS-1** (owns both subject files by name; D2-14/D3-10/DELTA-7/G-K/G-Q) · **X-W1** (re-gate: CC-030 visual goldens, CC-031 CI cliff; AP-4's guard repair) · **X-W5 C7** (the viewport query dies by name) · **X-W7 rider** (the AP-9 one-affordance rebuild on producer Chip/StatusDot) · **X-W8** (barrels/config/graph) · **X-W10** (type+motion canon; AP-14/AP-15/AP-19/AP-21/AP-28) · **GLASS-OWNED → BH relay** (producer-born phantom token; container-query defect already relayed in O-10 §A). `demo/platform/transport/**` and the `test/**` type-program remain claimed by NO wave — flagged loud below. Line anchors re-resolve before any cure (D-19).

### MAJOR

- **DSL-1 (C-4/D-4/L-1 — the fork, and the measured double alert).** Byte-identical labels (`status-lamp.ts:54,60` = `ApiOfflineChip.vue:17,25`), twin keyframes under two names at the same `2.4s var(--ease-standard) infinite`, near-identical chip CSS (my whole reads; divergences: padding 0.55 vs 0.7rem, radius-fallback spelling), the matrix derived twice (tested resolver vs untested inline :36-37), and the double `role="alert"` is MEASURED, not hypothesized — harvest `area-palettes.json:2125`, both seats visible in one frame. L-1's "structurally forced by placement" mechanism stays killed (chronology: the chip's inline derivation is R.W3, the resolver is T.W6). **Disposition: FOLD → AP-1 (BLOCKER lives THERE; the misconfigured halves die under MT-F031's born-RED grep gate) + AP-2 (the offline-register duplication → X-W7 rider, one affordance on producer primitives). Never re-booked; this docket adds the closed hedge (the collision L-1 called "not yet reproduced" is reproduced) and nothing else.**
- **DSL-2 (C-9 + D-11-as-rescoped — unwitnessable by construction).** All five mutations re-derived by me against the live files: unit suite never mounts the SFC (19 asserts, resolver-only); `@vue/test-utils` installed, zero importers; o22 asserts count/attr/geometry, never text or accessible name, and its own header records the misconfigured face CANNOT fire under the harness; no frame of any face exists in the 60-capture corpus (correctly — healthy backend). **Disposition: X-W1 rider** (AP-4 identity: the resurrection guard watches a dead selector; the mount-test + forced-latch capture arm land with CC-030/CC-031) — noting MT-DOCK-LAYERS-1's G-Q ("lamp `role:"status"`") and G-K(iii) retire the M2/M3 mutation classes when they land, and A's C-9-cure kill stands: `toHaveAccessibleName` would fail against a correctly-repaired region (name ≠ content for `status`/`alert`); the gate asserts rendered TEXT, or lands the author-name cure with it.
- **DSL-3 (C-1(b)/D-3/L-4 — the empty live region below 1024px).** Source-decidable and re-read by me: `.lamp-label { display:none }` (:67-69) removes the only text node; the dot is `aria-hidden` (:18); no author name; the comment at :64-66 asserts the opposite ("visually-hidden, not v-if'd" — false as written); the correct idiom ships at `PalettesPane.vue:25`. All three axes and both readers converge. **Disposition: FOLD → AP-13, CROSS-BOOKED → MT-DOCK-LAYERS-1** (DELTA-7/G-K announcement family; the false comment corrected by whichever wave touches the file first; the fold's `min-width:1024px` mechanism itself dies at X-W5 C7). C-1(a)'s unnamed-alert limb stays killed (nameFrom:author; content is what announces).
- **DSL-4 (C-2/D-9 — small-caps corrupts the register; the command copy).** Verified with my own eyes on `band-1440-light.png`: `● DEV MISCONFIGURED — RUN ˋNPM RUN DEVˋ`, graves painting as glyphs. Two limbs, two owners: the case-corrupted COMMAND copy is moot-on-AP-1 (≡ AP-22 — the string dies with the face; `devMisconfigMessage`'s correct composition already owns the channel); the register-wide SYNTHETIC small-caps (≡ AP-14: the shipped Fira Code subset has no `smcp` — the sibling seat's fontTools run) survives on the remaining face → **X-W10 + the standing BH note**.
- **DSL-5 (D-1/D-2/D-10 — the contrast family, BLOCKER inflation killed).** Mechanism confirmed from source by me: the base :60 wash is a 55% film with no opaque backing and no backdrop-filter; `[data-variant="misconfigured"]` :98-102 REPLACES it with a 12% destructive tint (so A's kill of D-2's ":60 transposition" thesis is sustained — the measured face never composites :60); the substrate is the user-seeded aurora, so no floor is computable; visually incontrovertible in both 1440 frames (my reads). Severity: MAJOR ceiling (dev-gated at `status-lamp.ts:48`; `import.meta.env.DEV` statically false in every build — both readers + the AP-6 precedent). **Disposition: misconfigured arms moot-on-AP-1, RECORDED so no restyle is attempted instead of the kill; the surviving `unavailable` face (D-10's computed 4.33:1 dark; the 55% wash on no reduced-transparency roster) → X-W7 rider (the AP-9 rebuild's producer surface is roster-member by construction). Exact ratios = seed-dependent samples → residue U-2.**
- **DSL-6 (C-3/D-8 — the announcement architecture).** Ruled above: mechanism CONFIRMED MAJOR, per-AT magnitude residue U-1. FOLD → MT-DOCK-LAYERS-1 DELTA-7 + D2-14. The temporal-disjointness fact (:169 refuses the downgrade — the two faces cannot co-occur in one session) is verified and carried as the cure's load-bearing premise.
- **DSL-7 (L-3 — three producer primitives hand-rolled).** Producer surface verified by me: `./chip` `./status-dot` `./pulse` shipped; `StatusDot.label` exists with the exact JSDoc; house precedent at `EasingSpecimenStrip.vue:14`; `StatusDot`/`Pulse` demo consumers = 0. Pulse limb stays killed (no `error` member — my feedback.d.ts read); the L-3 cure-snippet trap (label prop + adjacent text = double announcement) recorded against any executor. **Disposition: ≡ AP-9 → X-W7 rider** (one rebuilt affordance; the lamp's 88-line style block collapses to the band-seat declarations).
- **DSL-8 (reader-B missed #1, repo-scope — `test/**` is in NO TypeScript program).** Verified by me from both tsconfig includes + the typecheck script: vitest transpiles with esbuild, types never checked; the O-22 matrix's `Array<[ApiAvailability, boolean, LampState | null]>` annotation is decorative; `demo/test/**` IS inside tsconfig.demo — so L-7's two test homes differ in TYPE SAFETY, not just location (the consequence L-7 missed), and C-9's gate is type-vacuous as well as behaviourally vacuous. Booked at the scope where the count lives (19 test files): **NO-WAVE-OWNER — candidates X-W1 (CC-031 CI-cliff family) / X-W8; named gap: no X file mentions test-program membership.** L-7's move + B's missed state-leak row (DSL-26) ride the same decision.

### MINOR

- **DSL-9 (L-13-as-rescoped — dead config rows only).** Ruled above; INFO-grade facts at MINOR ledger weight per the six-confirmation identity: 3 dead `paths` targets + 2 unentried shipped subpaths + no `.` + the unconsulted stale node_modules directory. **≡ W-HYGIENE config parity (ColorSpaceSelector L-14; cure = DELETE per Dock R-3) → NO-WAVE-OWNER, identity preserved.**
- **DSL-10 (C-5/D-12/L-2 + A-missed #4 — the phantom token, census corrected to 4).** Zero definitions (my grep); FOUR consumers (3 demo + the producer's own minified rule); the lamp's `0.6875rem` fallback is byte-identical to `--type-micro`, so the corpus condemned the right line for the wrong reason; A's kill of L-2's cure is producer-verified by me (`text-mono-caption` UPPERCASES and resizes — it would manufacture C-2/D-9); the correct cure is `text-mono-micro`/`var(--type-micro)` — **independently re-derived by reader-A and byte-identical to AP killed-#5's ruling: convergent re-derivation, the anti-echo behaviour the program wants.** **Disposition: ≡ AP-15 (producer half GLASS-OWNED → BH relay) + AP-19 (demo half → X-W10).**
- **DSL-11 (L-9 — dead contract pointers).** Both verified (`@lib/palette/api/availability.ts` at status-lamp.ts:15; `@components/custom/dock/…` at status/index.ts:5; both aliases W43-dead per tsconfig). **≡ AP-26 → X-W8 (barrel death); the status-lamp.ts copy corrected by MT-DOCK-LAYERS-1 (owns the file).**
- **DSL-12 (L-5 — the barrel duality/cycle).** `Dock.vue:4` `from "./"` above `:5` direct, the barrel self-referential, the lamp not in it (my reads). **≡ Dock D3-09 — BUILD in MT-DOCK-LAYERS-1's G-Q (RED today).**
- **DSL-13 (L-8 — dead chip barrels).** Outer barrel zero importers (my grep); sole consumer imports relatively; the inner barrel's header admits it. **≡ AP-11 facts → X-W8.**
- **DSL-14 (D-13 — literals off every ladder).** `0.06em` vs `--type-tracking-caps: 0.1em`, `2.4s` vs a ladder with no such rung (both producer reads mine), `1.5px`, and the twins already at different densities (0.55 vs 0.7rem padding). **≡ AP-28 + AP-21 → X-W10.**
- **DSL-16 (C-6 — `default:` defeats exhaustiveness).** Verified; the standing law says structural. **→ MT-DOCK-LAYERS-1** (D2-14 edits the same switch; the `never` guard lands with the re-role; post-AP-1 the union shrinks anyway).
- **DSL-20 (D-5 main limb — the orphan interval).** Mechanism verified in source (pinned at inline-end vs a centred content-width pill: the gap is arithmetic residue) and in my frame read (the empty salmon span IS the composition at 1440). Citation kill and collision-as-defect kill sustained (the quoted law lives at shell.css:17-18; C's 12-row negative matrix). **→ design input to the AP-9/X-W7 rebuild + Dock D3-10's re-home; the zero-reservation fact rides DSL-31.**
- **DSL-24 (B missed #2, limb corrected — boundary posture).** The dock band sits outside the sole ErrorBoundary (my App.vue reads); any band setup throw (incl. `useApiClient()`'s, :54-62) is an unprotected white screen against the repo's own U-F58 law. The "ordering accident" limb is corrected (Vue contract, not luck). **→ NO-WAVE-OWNER — candidates MT-APP-1 / MT-DOCK-LAYERS-1.**
- **DSL-26 (B missed #5 — test-state leak).** Verified by my whole read: direct `apiAvailability.value` writes at :122/:132/:143 despite the afterEach comment claiming public-transitions-only; `initApiEnvironment(...)` at :146 permanently arms module-private `misconfigDetail` (no reset export); isolation is ordering-dependent, undeclared, in the ONE suite the corpus treats as trustworthy. **→ rides DSL-8's test-program decision.**
- **DSL-29 (A missed #1, demoted MAJOR→MINOR — modality opt-out).** Both arms verified in foundation.css by me; but the `prefers-contrast` limb's discard mechanism (the misconfigured border override) dies with AP-1 — the surviving base face's `--card-edge` border TAKES the 55% lift (the AP superlative-7 pattern). What survives: the 55% wash is on no reduced-transparency roster. **→ X-W7 rider (producer surface = roster membership by construction) + X-W10.**
- **DSL-30 (A missed #3 — the 26×18 capsule).** Arithmetic verified (8.8+6.4+8.8+2 = 26 around a 6.4px dot ≈ 68% padding); corroborated by my read of `band-390.png`. The label-era box was never re-proportioned for the label-less state. **→ rides X-W5 C7 (the fold dies) + the X-W7 rebuild.**
- **DSL-31 (A missed #5 — no degradation path).** Verified: no `max-width`/`overflow`/`text-overflow`/`min-width` anywhere in the 88-line block; `white-space: nowrap`; absolute seat reserves nothing; C's measured 11.5px clearance at the 1024 boundary is the entire safety margin. The source-decidable form of D-5's unproven collision. **→ X-W7 rider (producer Chip owns overflow behaviour); the live threshold stays residue U-4.**

### INFO

- **DSL-15 (D-14 + A missed #2 — the 3.4px channel).** A's border-box arithmetic adopted (ring interior = 6.4 − 2×1.5 = 3.4px; chip height 18px cross-checks it); the house forced-colors policy is genuinely two-tier with the lamp as unrostered chrome. **Structurally moot-on-AP-1 (≡ AP-23): one register remains, so there is no two-variant channel to carry.** The surviving dot's WHCM rendering → residue U-3.
- **DSL-17 (C-7 — one-variant selector).** Fact verified; the `[data-variant]` block dies with AP-1. Recorded.
- **DSL-18 (C-10/D-7-as-ruled — the pulse).** Ruled above: INFO; the "signifies a constant" premise dies with AP-1; the literal ≡ AP-21 → X-W10; the PRM structure is canon-CONFORMING (W10:307) and survives every cure verbatim.
- **DSL-19 (C-11/C-12 — unselectable remedy; alert inside the nav landmark).** Remedy-copy limb moot-on-AP-1; the landmark posture is resolved by D2-14 (re-role) + D3-10 (re-home makes the band the mount, its documented positioning context) → MT-DOCK-LAYERS-1.
- **DSL-21 (L-11 — `.fira-code` shadow).** Verified (utils.css:9-11 vs the producer utility's feature-settings). → X-W10.
- **DSL-22 (L-6 fact limb).** `createApiClient()` is parameterless and returns the singletons (my read) — a true observation; the causal chain and the seam-deletion cure stay killed (one-line `mount(…, { global: { provide } })` is available; the seam is the S.W2 W2-4 house contract). Recorded, no cure.
- **DSL-23 (L-7 — two test homes).** Fact verified (11 `test/` files reach `demo/`; `demo/test/` holds 3); subsumed as the location half of DSL-8 (the type-safety half is the finding).
- **DSL-25 (B missed #4 — `LampVariant` dead export).** Verified (2 hits, both internal). Post-AP-1 the union is single-member; dies in the MT-DOCK-LAYERS-1 edit that touches the type.
- **DSL-27 (B missed #6 — `@vue/test-utils` zero importers).** Verified; the enabler fact for DSL-2's cure, filed so the cure needs no new dependency decision.
- **DSL-28 (B missed #3 — specifier-extension duality in the transport cone).** Verified (`./client.js`/`./availability.js` with extensions inside; every inbound edge without). **→ NO-WAVE-OWNER (transport unclaimed — the ApiOfflineChip DISSENT-#3 flag, re-raised).**

## Killed-claims register (16 sustained + 2 overturned; every entry re-derived by this seat)

1. **L-6 causal chain** ("cannot be unit-mounted → forced extraction → caused L-1") — killed by both readers, sustained: `API_CLIENT_KEY` is exported, the one-line mount exists, `@vue/test-utils` is installed with zero importers (nothing was "stranded"), and the chip's inline derivation PREDATES the resolver by a tranche.
2. **L-1's "duplication is structurally forced" mechanism** — sustained on the same chronology; the palettes→shell boundary it calls illegal is one the tree already crosses (type-only edges verified by the corpus, unrefuted).
3. **C-1(a) — "unnamed alert" as a defect** — sustained: `alert`/`status` are nameFrom:author; a live region announces CONTENT; the residual harm is rotor-queryability on a non-operable dev annotation; the prescribed cure (aria-label + aria-hidden the text) severs visible from announced.
4. **C-8's false dilemma** — sustained with the AP-18 scope correction (deployment ≠ build; the `vite preview` population exists and is itself moot-on-AP-1). The wholesale `:48` gate is defence-in-depth, not per-branch contrivance.
5. **L-10's disappearance mechanism** — sustained by my own shell.css read: `overflow: hidden` belongs to `.app-layout` (:26), `.dock-band` (:39-45) has none; the failure mode is loud MISPLACEMENT (to `position:relative` `.app-layout`), and L-10's in-flow-grid cure would break the ratified T-57 centring law.
6. **D-2's ":60 transposition" thesis** — sustained by my source read: :98-102 fully overrides :60 for the measured face; :60 governs only `unavailable` (D-10's materially different numbers). The substance (translucent film, unbounded substrate) survives in DSL-5; the "one error, every consequence follows" rhetoric does not.
7. **D-3's amplifying limbs** ("color-only", "told nothing by any channel") — sustained: an open-ring vs filled-dot SHAPE channel exists (:84-86 vs :104-107, D-14 depends on it), and `availability.ts:163` is an unconditional loud channel carrying the full remedy.
8. **BLOCKER inflation (D-1/D-2/D-3)** — sustained: `status-lamp.ts:48` + Vite's static `import.meta.env.DEV` mean no production build renders any face; MAJOR ceiling (both readers; AP-6 precedent). The genuinely-shipping instance of the arithmetic was ApiOfflineChip's — graded THERE (AP-6/AP-7), and its loud register is ordered dead anyway.
9. **C-9's proposed `toHaveAccessibleName` gate** — sustained: passes only with an author name, which is a different cure the same axis proposes in a self-defeating form; the two must land together or the gate reverts.
10. **D-5's citation + collision-as-defect** — sustained: the quoted sentence lives at `shell.css:17-18`, not `Dock.vue:124` (my read) — a provenance failure under a file:line law; the collision has 12 measured negative rows and zero reproductions. The zero-reservation observation survives as DSL-31.
11. **D-7's MAJOR + "tokenize the 2.4s"** — sustained: triple-gated (component PRM block + two global neutralisers), dev-only, 0.42Hz; and the producer ladder has NO 2.4s rung (my read) — the prescription requires a producer mint (BH relay), not a local edit.
12. **D-11's four-tranche causal framing** — sustained (see ruled disagreements); the actionable residue lives in DSL-2 at MAJOR.
13. **C-7's third-variant scenario** — sustained: `LampVariant` is a closed 2-member union; the scenario requires editing the resolver, at which point the author is three lines from the styling contract.
14. **L-3's Pulse limb** — sustained by my feedback.d.ts read: `PULSE_STATES` has no `error`; the local keyframe is not a re-implementation of an available primitive. The Chip/StatusDot limbs stand (DSL-7); the cure-snippet's label+text double announcement stays flagged.
15. **L-2's cure** ("apply `text-mono-caption`"; "below the 12px floor at every viewport") — sustained and producer-verified by me: the utility UPPERCASES (manufacturing C-2/D-9) and resizes; `--type-micro` (11px) and `--type-admin-label` (10px) are real rungs below caption — there is no 12px floor. The correct cure is DSL-10's.
16. **L-13's `/value` limb (B's K-7)** — sustained by my grep: zero import statements anywhere; the count was 10, not 11.
17. **OVERTURNED BY THIS SEAT — L-13's stale-tarball mechanism, and BOTH readers' confirmation cells.** My `--traceResolution` run (pasted in instruments) shows package self-name resolution landing `/css` on this checkout's live dist. Two hostile re-readers, hired to attack the corpus, both repeated the challenger's mechanism without running the compiler — the citation-inheritance species the ColorSpaceSelector DISSENT-1 and ApiOfflineChip DISSENT-1 ledgers exist to catch, now demonstrated at the READER tier. Recorded as evidence about the fleet.
18. **CORRECTED BY THIS SEAT — B's "safe today only by ordering accident"** (error-boundary row): provide-in-setup precedes child mounts by Vue contract. The row survives on its true ground (DSL-24); the limb dies.

## Superlatives (provenance preserved)

1. **The PRM idiom — now quintuply attested.** `@keyframes` AND the `animation` declaration both inside `@media (prefers-reduced-motion: no-preference)` (:109-122): eliminates rather than clamps. Three axes, both readers, and X-W10:307's own CONFORMING baseline census name it. Whatever the repair (AP-1's kill, the X-W7 rebuild), this structure survives verbatim.
2. **`status-lamp.ts` is the model** (both readers, ApiOfflineChip superlative #5, re-read whole by me): pure, total, `isDev` threaded as an argument with the reason documented, the O-22 matrix asserted over it directly — two files from a twin that inlines the same decision with zero tests. AP-1 kills its misconfigured CASE, not its shape.
3. **`availability.ts` is the best module in the cone** (both readers, AP superlative #3, my whole read): the CORS-indistinguishability stated honestly, the pure triad detector over explicit inputs, the refusal-to-downgrade with its reason inline, the loud message naming origin/base/cause/remedy/non-conflation.
4. **Compositor-only, leak-free by construction**: opacity alone on a 6.4px element; the whole hazard-roster grep = 0 in an 8-line script — in a repo whose constellation audit recorded a ~40-site PRM-RAF epidemic.
5. **RTL by construction, verified twice**: `inset-inline-end` (:45); D rendered the RTL frame, C measured the flipped box — two seats spending budget to CLEAR a hypothesis.
6. **The oracle declares its own blind spot** (`o22:21-27`) and keeps a standing negative watch on the retired banner's class (:37) — a self-reported harness bound plus a resurrection guard, still working four tranches later; it is what made C-9's analysis possible.
7. **The C axis's negative-proof discipline** (N-1..N-9; N-5 kills its own strongest-looking hit via `elementFromPoint`; N-8 kills the unclearable-alert theory from the live process environment): nine recorded kills against twelve findings is the correct ratio for an assume-broken commission.
8. **Convergent independent re-derivation**: reader-A's missed-row cure (`--type-micro`/`text-mono-micro`) reproduces AP killed-#5's ruling byte-for-byte without having read it — the anti-echo event, recorded beside the three-deep census echo as the contrast the fleet should study.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **U-1 · AT announcement behaviour** for both region shapes (present-at-load `alert`; mid-session insert-with-content `status`) — ≡ ApiOfflineChip U-1; largely moot once AP-1 + DELTA-7 land, then verify the single surviving polite region announces the latch flip.
- **U-2 · Contrast across seed extremes** for the SURVIVING `unavailable` face (D-10's 4.33:1 dark is one seed's sample; "no floor" is an inference from one aurora) — instrument = DSL-2's forced-latch capture arm, both schemes, seed extremes, 1440/390.
- **U-3 · WHCM rendering of the open-ring dot** (`background: transparent` substitution behaviour) — ≡ AP U-3; the tracked forced-colors matrix contains no lamp frame.
- **U-4 · The D-5 collision threshold** (~480-500px dock layer at ≤1100px) — C's 12 rows all negative, widest observed 452.8px; undecided in both directions; DSL-31 is the source-side half.
- **U-5 · WCAG 2.2.2 on the surviving 2.4s pulse** — ≡ AP U-5; genuinely borderline, not decided from the armchair.
- **U-6 · The three mutually-inconsistent CSS-duplication counts** (26/34/17 vs 24/33/18 vs 13/18/11) and the divergent rendered widths (247.0 vs 255.6px) — no figure enters any repair ledger; mostly mooted by AP-1 + the AP-9 rebuild.
- **U-7 · The gh-pages CSS-bundle byte magnitude** of the dead-in-prod style block (mechanism certain: static CSS extraction cannot see the v-if; magnitude unmeasured — a build is outside this seat's scope).

## COORDINATION (cross-record notes so no future seat is misled)

1. **The corpus and BOTH reader returns are registry-blind.** Any session consuming `audit/components/shell-dock-dockstatuslamp/**` must read `ApiOfflineChip.md` (AP-1's file pins on THIS component) and `Dock.md` (MT-DOCK-LAYERS-1 §6.5 scope) FIRST. No restyle of the misconfigured register may ever be attempted instead of the kill — the AP-6 idiom, restated here because this corpus's D axis proposes exactly such restyles.
2. **The phantom-token census echo is now three deep** (axes → reader-A → reader-B, all "3 sites"); the corrected count is 4 and lives here and in ApiOfflineChip.md disagreement #1. The GLASS-OWNED relay (AP-15) already carries the producer half.
3. **L-13's stale-tarball mechanism is now refuted by THREE independent arbiter instruments** (PaletteColorStrip K-2, Dock R-3, this seat's trace). Any future record asserting "vue-tsc checks the stale node_modules copy" without a fresh trace is repeating a known-false cell.
4. **NO-WAVE-OWNER cluster, flagged loud** (echoing ApiOfflineChip DISSENT #3): `demo/platform/transport/**` rows (DSL-28 here; AP-17/24/29/30/31/33 there), the `test/**` type-program + test-home decision (DSL-8/DSL-26), the W-HYGIENE config-parity identity (DSL-9), and the band's error-boundary posture (DSL-24). If the next formation boundary does not mint owners, these strand.
5. **Sequencing note**: MT-DOCK-LAYERS-1's interim lamp corrections (D2-14 re-role, D3-10 re-home, G-Q arms) and the X-W7 one-affordance rebuild (AP-9) both touch this surface; the interim edits must not be counted as the rebuild, and the rebuild must not resurrect the misconfigured face MT-F031 kills.

## Verdict

**DEFECTIVE — adjudicated, and largely PRE-OWNED.** 31 rows confirmed (8 MAJOR · 13 MINOR · 10 INFO), 16 corpus kills sustained, 2 reader confirmation cells overturned by this seat's own compiler run, 11 reader-missed rows adopted. The component's loudest face is already owner-ordered dead by name (MT-F031/AP-1 — both seats, files pinned), its files are already owned by a standing born-RED wave (MT-DOCK-LAYERS-1), its defective media query is already counted by a RED gate (X-W5 C7), and its hand-rolled register already has a one-affordance successor booked on producer primitives (AP-9/X-W7). What this corpus genuinely adds: the measured double-alert closing L-1's hedge, the type-vacuous test program (DSL-8, repo-scope, unowned), the modality-roster and degradation-path gaps on the surviving face, and a third-tier demonstration that unexecuted mechanisms get inherited — twice by its own hostile readers. These dispositions are ADJUDICATED, not VERIFIED — X-W11 stamps, not this file.

— end of apotheosis. The three challenge records and both reader returns remain immutable beside this file per E-1/E-3.
