# wb-extract-imageeyedropper — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **HEAD**: `1994eb53` · branch `tranche-u` · **Corpus**: `demo/workbenches/extract/ImageEyedropper/` (ImageEyedropper.vue 299 · useImageSampler.ts 139 · useLoupeCanvas.ts 75 · useInertiaGesture.ts 379 · constants.ts 11). Challenge axes: **D-design r1/r2/r3** (r2 GOVERNS, 19 defects/4 BLOCKER; r3 = blind third round, 16 more), **C-implementation r1/r2/r3** (r2 governs; r3 = blind WebKit replication + 2 corrections + 3 new), **L-library** (4 passes, one file, 31 findings). Committed instruments: `evidence/probe-A-matrices.json`, `evidence/probe-B-behaviour.json` (spot-re-read by this seat: desktopDark `loupeEmpty {nonAlphaPx:0,totalPx:12100}`; rtl `viewport.x 767.5 / canvas.x 1211.5` = +444.0 exactly; rtl readout `"Tap to sample"`; `pinCycle.click2_atB` text unchanged / btns `["Close eyedropper"]`; `readoutTruncation {clippedPx:139, hasTitle:false}`).
**Re-readers adjudicated**: design-and-usage · library-and-code. Both were attacked before either was adopted; every kill re-derived against the tree, every promoted miss re-walked in source, every reader-vs-reader conflict ruled.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling)**: full reads of all five subject files + ImageDropZone.vue + ExtractWorkbench.vue mount/opener/readout regions; installed glass-ui 7.0.0: `dist/watercolor-dot.js` (**`inheritAttrs: !1`; setup reads `useAttrs()` and binds ONLY `attrs.class`/`attrs.style` onto the root span — no `v-bind="$attrs"`**; `props.animate default: !1`; `useWatercolorBlob`'s `!animate` branch registers `watch(colorRef, e => l.value = radiiToCSS(randomRadii(mulberry32(hashString(e + seed)))))`), `dock/DockControl.vue.d.ts` (props `shape|compact|active|type|disabled|as|asChild|class`; "Role rides the consumer"), `dock/styles/controls/touch-floor.css` (`@layer components { @media (pointer: coarse) { .dock-icon-button:not(--compact):not(:where(.glass-dock *)) { min 2.75rem } } }`), `icon-button.css` (`&:hover:not(:disabled){ background; color: var(--btn-hover-color,…); scale: var(--scale-hover-dock) }` + `.dock-icon-button > svg { width: var(--dock-icon-glyph, 1.25rem) }`, all `@layer components`), `dock/styles/shell.css` (**`--dock-separator-height` declared ONLY inside `.glass-dock`**) + `layer-group.css` (`.dock-separator { height: var(--dock-separator-height) }`, no fallback) + `dist/dock.js` DockSeparator (**renders `role="separator"` + `aria-orientation`**), `composables/motion/core/useReducedMotion.d.ts` (published via `motion-core.d.ts:6`), `composables/glass/canvas2d/useCanvas2D.d.ts:41` `dprPolicy` (exported via `./canvas`), exports map (`./dialog` real); `@vue/runtime-core/dist/runtime-core.cjs.js` `beforeEnter … if (!state.isMounted) { if (appear) {…} else return }` + `isUnmounting` set in `onBeforeUnmount`; `tsconfig.base.json:11` **`exactOptionalPropertyTypes: true`**; `vitest.config.ts:21` include + the aurora-suite comment; `demo/DESIGN.md` §Bespoke-literals `ImageEyedropper.vue:286 | swatch-pop 0.65s | KEEP`; `VISUAL-CONSTITUTION.md` :13-17/:19-20/:27-28/:82-85/:91 (P051/W17 eyedropper manifest)/:98-104/:126 (image-sampler axis row); `OPTICAL-BENCH-COMPOSITIONS.md` Extract golden row + retained-line `Extract | [] | none | none` + the any-additional-line-is-a-defect clause; `docs/tranches/V/ARCHITECTURE.md:107` ("specification notation only") + :216 ("`/css` never emits the library-only … There is no general library-string serializer") + the :50-54 lattice (**`feature → … / shared` and `shell → … / shared` both exist** — see ruled disagreements); `motion-quarantine.md` M-25 (UNPROVEN, ConsoleRail salience) / M-26 (FALSE, ConsoleRail PRM limb) rows re-read — **neither is carried by any row below**; `demo/styles/animations.css:83-98` (vj-enter real) + :184-193 (guard 1); `demo/styles/foundation.css:251` `--primary: var(--accent-view)` + `--dock-h` :root shadow comment (:400-405); `useViewAccents.ts:144` region + `useAtmosphereBoot.ts:103` `--ink-muted` stamp; `test/image-sampler-v4.test.ts` whole (31 lines, `canvasRef: ref(null)`, asserts `"HSV 0 · 1 · 1"`); censuses: `padStart(2` = exactly 4 homes; `--hover-color` = exactly 4 lines / 2 consumers; `zoom-200-desktop/` = 5 shots, **no extract**; `demo/@` gone + eslint.config.js:235-238/:275-276 dead globs; tsconfig.demo.json:42-49 (3 phantoms, `/css`+`/value` missing); `picker-color.ts:72-90` (`ICtCp/Jzazbz/Kelvin`) + :205-217 (`serializePickerColor` non-CSS→oklch; `pickerColorToHex` 8-digit alpha); dist/subpaths/color.d.ts:92 `rgb: ColorFactory<"rgb">`, `toRgba8` no inverse; `useMixingAnimation.ts:141` `devicePixelRatio` (under demo/workbenches/ — the false L-3 receipt); **X wave files W4/W5/W6/W7 read for routing**: ImageEyedropper.vue appears in exactly ONE wave's bounds — X-W4.md:166, inside the **trigger-gated X.W4.g** WatercolorDot migration, "**modify-carve — import + face only**"; ExtractWorkbench.vue is X-W7 modify-carve (W7.md:124) and W7 **creates `demo/color-session/format-color.ts`** (W7.md:58/:121); no wave's bounds hold the three composables, ImageDropZone.vue, or ExtractControls.vue.

## RULING PINS (this seat's own derivations)

1. **The interaction core has NO owning wave.** Every BLOCKER in this corpus lives in `ImageEyedropper.vue`'s handlers and its three composables. The only bounds hit in all of tranche X is X.W4.g's *trigger-gated, face-only* carve (closed unless X-W0.j's census PASSes). The wb-extract-pane apotheosis already booked the session cluster (XP-2/3/4/23/27) NO-WAVE-OWNER; this record extends that cluster with the **eyedropper interaction/render cluster** — together they are the extract feature's whole shipping-defect mass, and no X wave file touches them.
2. **A-17/CC-108 corrects the corpus's D-03 cure.** Reader design-and-usage confirmed D-03 with "the prescribed cure is canon-grounded: §5's axis composition over BI Slider names Extract as an adopter." Per the picker-componentsliders apotheosis R-15 ruling (**A-17: no chassis/axis arms exist at glass 8.0.0**), the §5 composition is a **producer-roadmap ask riding the BH relay, not a demo edit**; X-W5 declares its compositions bespoke by construction. The defect (VC:126's sampler-axis row wholly unimplemented) stands at full severity; the cure must be re-derived bespoke (focusable reticle + arrow-key `sampleAt`), never blocked on the producer.
3. **Round 3 exists and neither reader consumed it.** `challenge-D-design.md` (r3, 16 defects) and `challenge-C-implementation.md` (r3, WebKit replication) postdate r2 in this directory. They **independently corroborate the readers' two largest promoted misses** — the pulse latch (D3-02 BLOCKER; C-r3 §I-1 with pasted receipts) and the dead keyboard entry (D3-09) — and the loupe-centred-under-the-finger measurement (r3-D §2.2). Three r3-only rows the readers never carried are admitted below (EY-12, EY-20, EY-41) so nothing is lost.
4. **X-W5 supersedes D-10's numeric law.** Per the wb-extract-pane RULING PIN, X-W5 scope item 5 marks the exact golden proportions as fabricated tolerance to be replaced. D-10's direction (a sampler is a region, never a 100% occlusion of its own composition) survives; its `61.8033989%` arithmetic does not bind the cure.

## Scoreboard

**44 adjudicated defect rows: 4 BLOCKER · 20 MAJOR · 16 MINOR · 4 INFO** — 26 corpus-claim rows confirmed (several consolidated; six as identity cross-references to already-banked apotheoses), **15 reader-found admissions** (17 filed, two consolidated into siblings), **3 r3-only admissions**. **18 killed claims**: all 15 distinct reader kills UPHELD on re-derivation, plus 3 kills minted by this seat against reader cells. Two seat demotions ruled inside surviving rows. Headline: **the BLOCKER cluster — a magnifier that never paints, a tap the component eats, an instrument inert in RTL, and a feature unreachable without a pointer — sits in files no tranche-X wave owns.**

## Ruled disagreements and reader-challenges (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **D-06 "cannot be scrolled to"** | reader-1 KILLED the clause; reader-2's residue cell asserts "`touch-action: none` … makes them unscrollable is confirmed" | **reader-1's kill UPHELD; reader-2's cell corrected** | ImageEyedropper.vue:8 is `absolute` (in-flow, scrolls with its scroll container); `touch-action: none` is scoped to the inner viewport div (:66) only; the chrome bar and everything outside the overlay still scroll. The overflow *amounts* stay UNPROVEN-NEEDS-LIVE; the "unreachable" inference is dead. probe-A's `overlayExceedsViewport:false` flag contradicts its own rects — quote rects, never the flag (reader-1's corollary adopted). |
| **D-03's cure form** | reader-1: "canon-grounded" §5 axis composition | **cure KILLED (K-16), defect untouched** | RULING PIN 2. A-17/CC-108; R-15 precedent; X-W5 bespoke-by-construction. Neither reader read the X wave files — the identical failure the wb-extract-pane DISSENT §1 recorded. |
| **L-28 "no legal demo-side shared home"** | reader-2's confirm cell | **cell corrected (K-17); row survives** | ARCHITECTURE.md:50-54: `feature → … / shared` AND `shell → … / shared` — `demo/shared/` is reachable from both `--hover-color` consumers. The reason not to mint one is edict-5/KISS (the producer already owns hover: icon-button.css `--btn-hover-color`, `--scale-hover-dock`), not lattice impossibility. The BH-relay conclusion stands on the corrected ground. |
| **Pulse-latch severity** | readers: MAJOR · r3-D: BLOCKER ("the only feedback for the only two commit actions") | **MAJOR** | The emits still fire (:213-216, :219-223) — function survives, confirmation dies. BLOCKER is reserved for the instrument not doing its job (r2's own rubric). r3's dissent preserved below. |
| **Transition-inert severity** | reader-2: MAJOR | **DEMOTED to MINOR** | No canon law binds an overlay entrance animation; no capability is lost (the `:207` refit it gates is redundant with `:203`). The sharp limbs — a false in-file motion contract and C-5's dead cure receipt — are record-integrity, carried in the row. |
| **r3 D3-07 (53% stage empty at 200%)** | r3 files MAJOR | **KILLED as a standalone law row (K-18); folded** | Same aspect-fit letterbox arithmetic reader-1's D-9 kill retired (VC:28 governs the *secondary region* of a two-part scene, not fit letterboxing). The measured frame folds into EY-15's composition row, whose recomposition is the only cure that moves it. |
| **L-21 severity** | axis: BLOCKER · reader-2: MAJOR · wb-extract-pane XP-21: MINOR | **MINOR — identity wins** | Anti-rename identity guard: fact-identical to XP-21/ColorSpaceSelector-L-14/componentsliders-R-10 (this seat re-read tsconfig.demo.json:42-49 at HEAD: 3 phantoms, `/css`+`/value` missing, comment false by one). Nothing mis-resolves today; one identity, already booked NO-WAVE-OWNER (X-W8 declined it, W8.md:112). |
| **RTL severity** | reader-1/r2: BLOCKER · reader-2: MAJOR | **BLOCKER** | Reader-1's refinement decides it: probe-A rtl records `readout: "Tap to sample"` — in RTL the pointer maps out of bounds, `sampleAt` returns null, and the instrument is *inert*, not merely inaccurate. An instrument dead in an entire writing direction fails "does its job". |

## Adjudicated defect roster

Line anchors are HEAD `1994eb53`; re-resolve before executing any cure. Dispositions are **ADJUDICATED, not VERIFIED**. Route legend: a wave id means that wave's bounds/scope own the surface today; **NO-WAVE-OWNER** rows are booked for the next formation boundary. **EY-CLUSTER** = the eyedropper interaction/render cluster (this record's NO-WAVE-OWNER booking, sibling to wb-extract-pane's session cluster).

### BLOCKER

- **EY-1 — D-01 (= C-1, r1 D-3, r3 §3.1): the loupe paints nothing on first show and never on touch; every unpin re-arms the race.** `useLoupeCanvas.ts:55-60` sets `loupeVisible = true` then calls `drawLoupe` synchronously; the canvas is behind `v-if` (ImageEyedropper.vue:78-86) so `drawLoupe` early-returns at :30. Touch cannot recover: `onTap` pins (:161), `onHover` bails while pinned (:165), and the capture listener `hideLoupe()`s on the next pointerdown (:181) — unmounting the canvas re-arms the race for the whole session (r3's sharpening). Witness: probe-A desktopDark `loupeEmpty {0/12100}`; r3 ink counts on both paths. **Cure constraints ruled in**: (a) the fix is a post-flush watcher + permanent mount driven by opacity — and the ready-made seam already exists as *dead CSS*: `.loupe { transition: opacity … }` (:270) has never fired because nothing writes opacity (EY-31); (b) a paint fix alone does not restore the feature — EY-5 (occluded by the finger) and EY-6 (sub-1× power) still hold. → **EY-CLUSTER, NO-WAVE-OWNER.**
- **EY-2 — D-02 (= C-2, D3-08): `justUnpinned` is a one-exit latch — every sample after the first costs two clicks; a pan-while-pinned eats the next clean tap silently.** Capture pointerdown sets `pinned=false; justUnpinned=true` (:176-185); `onTap` is the only clearing site (:152-155) and fires only when `!hasMoved` (useInertiaGesture.ts:277-281), so a drag latches the flag. The discarded click also deletes Add/Apply (`v-if="pinned"`, :43-58) while `sampledColor` persists — the second-order lie reader-2 named. Witness: probe-B pinCycle (click2 text unchanged, buttons 3→1). → **EY-CLUSTER, NO-WAVE-OWNER.**
- **EY-3 — D-05: RTL seats the canvas +444 px outside its stage and the instrument goes inert.** The canvas is an in-flow block-level replaced element (:69-75, no `absolute`); under `direction: rtl` the over-constrained margin equation seats it at the inline start (right); `viewportToImage` reads model `panX` only (useImageSampler.ts:103-109). Displacement = stageWidth − canvasLayoutWidth (508−64 = +444 here; r1's 692 = the same formula at its frame — r2's §8 reconciliation adopted as law: *quote the number with its frame*). Reader-1's refinement ruled decisive: probe-A rtl `readout: "Tap to sample"` — samples map out of bounds, `sampleAt` returns null, the ghost dot never fills. → **EY-CLUSTER, NO-WAVE-OWNER.**
- **EY-4 — D-03 + reader-found entry limb (= r3 D3-09): no keyboard or numeric sampling path exists anywhere, and the only opener is dead to the keyboard in exactly the state that opens the eyedropper.** Inside: the sole key bound is Escape (:226-235, window-level :239); the canvas is `aria-hidden` (:74); no reticle, no x/y controls — VC:126's image-sampler axis row wholly unimplemented (§5 "every spatial action has a keyboard/numeric equivalent"). At the entrance: ImageDropZone.vue:18-22 — `role="button"`, `:tabindex="disableClick ? -1 : 0"`, Enter/Space gated on `!disableClick`, while ExtractWorkbench.vue:23-25 sets `disable-click` precisely when the preview exists and makes that same div the ONLY opener — AT is told a button named "Image preview area, tap to sample colors" exists that cannot be focused. WCAG 2.1.1/4.1.2 fail end-to-end; no cure inside ImageEyedropper.vue can reach it. **Cure form corrected (RULING PIN 2)**: bespoke keyboard path, not the A-17-foreclosed §5 composition. → **NO-WAVE-OWNER** (ImageDropZone.vue in no wave's bounds; X-W4 accname/semantic addendum candidate at the boundary).

### MAJOR

- **EY-5 — reader-2-found (r3-D §2.2 measured): the loupe is centred exactly on the pointer — on touch the finger fully occludes the magnifier.** `loupeStyle` = `left/top: rel − LOUPE_SIZE/2` (:191-196); `showLoupeAt` stores raw pointer coords (useLoupeCanvas.ts:55-60); no offset, no edge-flip. Every axis rested C-1's BLOCKER weight on "the loupe is the only way to see the occluded pixel" and none noticed the geometry defeats it. r3-D measured the tap point at the circle's exact centre. → **EY-CLUSTER.**
- **EY-6 — reader-1-found (+ r3 D3-06): loupe magnification is untethered from stage zoom — power = 9.64/zoom, sub-1× exactly when the user leans in.** Pure arithmetic: 11 texels (constants.ts:11) across a 106 CSS-px content box (110 border-box − 2×2px border, :262-266 under preflight border-box) = 9.64 px/texel fixed; the stage shows `zoom` px/texel with `fitToViewport` clamped to maxZoom 10 (useInertiaGesture.ts:162; :149). At the audit's fixtures: 16×16 fits at 10 → power 0.96×; 64×64 at 7.94 → 1.21×; ctrl-wheel to 10 → <1× on any image. L-2's "exact 10× magnification" is px-per-texel, not power. r3 adds: 110 fixed px = 46% of a 238px stage at 200% zoom. → **EY-CLUSTER.**
- **EY-7 — reader-promoted, r3-corroborated both axes (D3-02, C-r3 §I-1): the confirmation pulse fires once per MOUNT and is silent forever after — the `@animationend` reset is never attached.** Decisive mechanism verified by this seat in the shipped build: `watercolor-dot.js` `inheritAttrs: !1`; setup binds only `attrs.class`/`attrs.style` to the root span — the listener at :22 lands on nothing, no Vue warning (suppressed under inheritAttrs:false). `swatchPulse` latches true (:126, set at :215/:222, cleared nowhere reachable). **Load-bearing correction to D-16's cure**: after the latch the class list is permanently `swatch-pulse` — `w-7 h-7` is off, and the "redundant" `.swatch-pulse { width/height: 1.75rem }` (:285-289) is the ONLY thing sizing the dot; deleting it as D-16 prescribes collapses the swatch after the first Add. Severity MAJOR (ruled above; r3's BLOCKER dissent preserved). → **X.W4.g face carve** (trigger-gated — the WatercolorDot→WatercolorSwatch swap is exactly this wiring's surface); **if W4.g never fires, EY-CLUSTER**.
- **EY-8 — D-04 (= C-4, L-29): a hand-rolled non-modal modal — no role, no aria-modal, no name, no focus entry/return, no inerting, window-level Escape with no scope.** Bare `div.absolute.inset-0.z-popover.glass-floating` (:8); keydown on `window` (:239), swallowing Escape unconditionally (:226-235). Identity guard: fact-identical to **wb-extract-pane XP-9** (already routed **X-W4**, exceeds the declared face-only carve — addendum candidate) and the parked-pane keydown leak is **XP-1**'s limb (X-W5). glass-ui `./dialog` verified real. Focusable counts live-only (residue). ONE identity each — cross-booked, not re-booked.
- **EY-9 — reader-2-found: `pointercancel` is wired to `onPointerUp`, so a cancelled pointer COMMITS a tap — sample + readout overwrite + loupe + pin.** useInertiaGesture.ts:332; `onPointerUp` reaches the `!didMove → onTap` branch (:277-281) with no event-type distinction; palm rejection / edge-gesture takeover / orientation change all arrive with `hasMoved === false`. A palm brush silently commits a sample and reveals Add/Apply for a colour never chosen. → **EY-CLUSTER.**
- **EY-10 — C-3 (= L-4, D-15): undecodable image ⇒ floating-promise rejections, a dead overlay, no error register; the file-input path has no MIME guard while the drop path does.** `loadAndFit` async, called bare at :238/:247; `img.onerror = reject` rejects with a raw Event (useImageSampler.ts:72-75); ImageDropZone.vue:87-92 emits unconditionally vs :94-100's `type.startsWith("image/")`. The correct register exists next door (ExtractWorkbench.vue:80-85 `text-destructive`). Joins the **wb-extract-pane session-correctness cluster** (XP-2's eyedropper-side arm; XP-23 adjacency). → **NO-WAVE-OWNER (session cluster).**
- **EY-11 — C-5 (= L-12): the ResizeObserver is wired to an initialiser — any observed-box change discards the user's zoom and pan, unrestored.** :353-360 → `fitToViewport` writes zoom/panX/panY unconditionally (:162-166). Cure premises verified: `useResizeObserver` published at `@mkbabb/glass-ui/dom` (threshold + rafBatch); `clampPan` (:72-90) is the reflow primitive. **Receipt correction (reader-2's kill K-15 upheld)**: the cure's ":207 already calls fit" is a dead call site — `@after-enter` never fires (EY-31); only `:203` is live. r3's IV-1 non-repro is recorded as instrument error by its own account; C-5 stands. → **EY-CLUSTER.**
- **EY-12 — r3-only (C-r3 §II-1), ADMITTED: a breakpoint-crossing resize REMOUNTS the pane and destroys the whole extract session — overlay, sample, zoom, and the uploaded image.** Pasted receipts in the r3 record (1100×800 survives; 1000×640 → `previewStillLoaded:false`). Session state lives in ExtractWorkbench setup scope under a layout-switched slot tree — the altitude defect, exactly the class the sibling's XP-1/XP-2 cluster owns. Mechanism source-plausible, transcript r3's. → **NO-WAVE-OWNER (session cluster).**
- **EY-13 — D-07 (= L-7 limb): the readout truncates (139 px desktop, committed) with no `title`, in a workbench whose sibling documents the opposite rule.** :35-37 `truncate select-all`, no `:title`; ExtractWorkbench.vue:134-141 carries the rule verbatim ("never a lying readout") + `:title`. **"Unrecoverable" killed** (K-14): `select-all` makes the full value one click from the clipboard — legibility defect, not data loss. → **EY-CLUSTER rider** (the sibling idiom is the one-line cure).
- **EY-14 — D-08 token half (= C-r3 II-3, r1 D-14): the readout ink is the retired static `text-muted-foreground` — the exact token both siblings replaced with the certified `--ink-muted` rung.** :35 vs ImageDropZone.vue:103-111 / ExtractWorkbench.vue:284-292's E1-R1 comments; `--ink-muted` live-stamped at useAtmosphereBoot.ts:103. The 4.07:1 ratio itself is live-only (residue). Identity: the `.plate-ink` one-home fold is **wb-extract-pane XP-19 → X-W10**; the eyedropper's adoption is a rider on any wave touching the file. → **X-W10 (utility home) + EY-CLUSTER rider.**
- **EY-15 — D-08 material half + D-10 + r3 D3-01 (consolidated): one `glass-floating` surface wears two tier-roles and occludes 100% of the binding Extract composition, revealing a phantom duplicate of its own specimen.** :8 houses both the chrome bar (Instrument-veil role) and the stage (Specimen well — canon: opaque/quiet); mounted `absolute inset-0` as the workbench root's last child (ExtractWorkbench.vue:172-180) over the ImageDropZone preview of the same file. Canon VC:13-20 (one surface one tier; glass earns its blur), OPTICAL-BENCH Extract row ("image well is specimen, not Card"; ad-hoc topology forbidden). r3-D adds the measured bleed + saturate(1.6) mechanism and files BLOCKER — this seat rules MAJOR (the composition numbers are under X-W5 supersession, RULING PIN 4; the takeover is the defect, the golden arithmetic is not the law). → **X-W7 addendum candidate** (the mount surface is in W7's ExtractWorkbench carve) **+ X-W5** (bespoke scene recomposition precedent, XP-25 lineage).
- **EY-16 — D-09 + L-28 + reader-1-found glyph override (consolidated): three per-instance reaches into `DockControl`'s producer register on one root.** (a) `.eyedropper-action-btn:hover:not(:disabled) svg { color: var(--hover-color); transform: scale(1.2) }` (:279-282) — double-scales against the producer's own `scale: var(--scale-hover-dock)`, unguarded `:hover` (sticky-touch latch is live-only residue), ink unbounded (driven by the sampled colour, :10); (b) `--hover-color` census = exactly 4 lines / 2 consumers (ImageEyedropper + shell ActionButton), already diverged on painted property; (c) `w-4 h-4` on all three glyphs (:12/:49/:56) defeats the published `--dock-icon-glyph` seam by layer order (utilities > components) → 16px where the producer says 20px. Lattice cell corrected (K-17): `demo/shared` IS legal; the producer seam is still the right home. → **glass-ui BH relay** (hover-tint/active-color variant ask — the N-5 conclusion, reader-confirmed) **+ EY-CLUSTER rider** (delete the descendant override + utility sizes when the face carve lands).
- **EY-17 — D-11: commit actions are bound to the transient pin mode, not to "a sample exists" — a stray pointerdown deletes Add/Apply while the sample survives.** :43-58 vs :118-119 persisting; probe-B pinCycle 1→3→1→3 buttons across four pointer events. Canon VC:100 ("Persistent operation state stays with the entity/workspace"). Pairs EY-2; dies with it in the one-sample-model recomposition. → **EY-CLUSTER.**
- **EY-18 — D-12 (= L-2/L-3/L-24a): the loupe backing store carries no DPR term anywhere in the chain, and no reticle marks the sampled cell.** Literal `width="110" height="110"` (:85) into a **106** CSS-px content box (L-24a's "110 CSS box / ratio 1.0" arithmetic KILLED — reader-1's kill upheld; the substantive no-DPR conclusion untouched) at measured dpr 2; `drawLoupe` strokes no reticle. Cure target verified: `useCanvas2D` `dprPolicy` published at `./canvas`. → **EY-CLUSTER.**
- **EY-19 — D-13 (= L-24b, r2's corrected form; r3 ramp): three resampling laws over one bitmap — stage `image-rendering: auto` at up to 10×, loupe nearest-neighbour, sampler one texel — and nothing marks which.** Zero `image-rendering` declarations in demo (grep); useLoupeCanvas.ts:40; useImageSampler.ts:111-119. r1's "the sampler is wrong" headline stays KILLED (r2's own §8 + N-3 four-for-four exactness); r3-D's measured ramp (seven fabricated colours across one texel edge, the olive-aims-green transcript) converts the mechanism into a measured lie — display-side cure only. → **EY-CLUSTER.**
- **EY-20 — r3-only (D3-03), ADMITTED: after wheel/trackpad zoom while pinned, the loupe ring and readout stop corresponding to any pixel under them.** `onWheel` is not gated by `pinned` (useInertiaGesture.ts:299-321) so the transform moves the bitmap under a loupe whose `loupeRelX/Y` are frozen viewport coords and whose canvas is never redrawn; the pinned value is by design, the ring's *position claim* is not. Statically exact; r3's transcript corroborates. → **EY-CLUSTER.**
- **EY-21 — r1 D-10 (reader-1 confirmed; r3 D3-04/05 corroborate): the pinned state is colour-only on a moving user-derived token and collapses in forced colors.** Carriers: `border-color: var(--primary)` + box-shadow (:273-276) — both in the forced-colors neutralisation set; structural sharpening verified: `--primary: var(--accent-view)` (foundation.css:251), re-written per accent by useViewAccents — certified against the app plate, meaningless over the user's photograph. Canon VC:83-84. Contrast/byte-identity numbers live-only (residue). → **EY-CLUSTER.**
- **EY-22 — L-18 (= C-r1 D-3) + reader-2-found commit-seam limb: alpha is read and discarded — a transparent pixel reports and EMITS opaque `#000000` — and the value displayed is never the value committed.** useImageSampler.ts:116-118 consults `data[0..2]` only; the emits (:213/:221) always carry the 6-digit hex while the readout renders the selected-space string — `Apply` on an oklch session commits `#rrggbb`. Over transparency the stage shows the page behind while the readout says black (second displayed≠sampled mechanism). The alpha-correct writer exists in-repo: `pickerColorToHex` via `toRgba8` (picker-color.ts:212-217). → **EY-CLUSTER.**
- **EY-23 — L-5: one user file decoded three times, retained in three representations, uncapped; `previewDataUrl` never cleared; the visible canvas's native-resolution twin is never read by anyone.** Verified in this seat's own read of useImageSampler.ts:80-91 (both `sampleAt` and `drawLoupe` read the offscreen twin; the :86-92 visible paint is write-only) + the session sites the sibling record verified. → **NO-WAVE-OWNER (session cluster).**
- **EY-24 — L-6/L-17 + L-7 + L-20 (consolidated): a fourth private mint of "spell a colour for a human", drifting from the canonical names (`ICTCP` vs `ICtCp`), at unbounded precision beside a dead `_digits` knob, and a test that canonizes the drift.** formatLibraryColor (useImageSampler.ts:38-44) vs `PICKER_SPACE_NAMES` (verified `ICtCp/Jzazbz/Kelvin`); `serializePickerColor` renders hsv as oklch for the picker while the eyedropper prints `HSV 0 · 1 · 1` — same colour, same session; test/image-sampler-v4.test.ts:24 freezes it. **The program already owns the cure home**: X-W7 creates `demo/color-session/format-color.ts` (four registers over a per-channel decimal table, W7.md:58) — useImageSampler.ts is not in W7's bounds, so the consuming edit is an **addendum candidate on X-W7**. L-23's normative-violation framing stays killed (K-9). → **X-W7 (home) + addendum (consumer).**
- **EY-25 — L-9: the sampler round-trips raw bytes through a string and the CSS parser to build a colour the published surface constructs directly, and manufactures a throw inside the pointermove path.** bytes→`formatHex`→`parseCssColor`→convert (:116-118, :55-62); `rgb: ColorFactory<"rgb">` verified at dist/subpaths/color.d.ts:92; no `fromRgba8` inverse. Reader-2's `noUncheckedIndexedAccess` safety note verified logic. Cost figures inadmissible (three inconsistent L numbers — residue). → **EY-CLUSTER.**

### MINOR

- **EY-26 — N-1 (C-r2): off-image hover keeps a stale readout and a parked loupe — the one honest failure path (`return null`) is dropped by both callers** (:156-162, :166-171, no `else`). → **EY-CLUSTER.**
- **EY-27 — D-14: dead `tag="div"` on WatercolorDot — an enumerated, not-yet-executed canon migration row.** VC:91 (P051; W17's 25-site manifest names the eyedropper), DECISIONS B-25. Producer drops the attr (inheritAttrs verified). Identity: the repo-wide `tag=` sweep is ONE identity (ColorSpaceSelector L-8 residue); this site dies in **X.W4.g**. Typecheck-blind limb accepted as reported (residue).
- **EY-28 — D-16: two sources of truth for the loupe diameter (`LOUPE_SIZE` vs the `width="110"` literal) and the swatch size — WITH EY-7's correction: the `.swatch-pulse` size copy is currently load-bearing; fix the latch before deleting the duplicate.** → **EY-CLUSTER rider.**
- **EY-29 — D-17 + N-5 (= L-25): all three controls title-only, `aria-label` null; DockControl publishes no label seam — producer relay, not call-site patch.** Identity: **wb-extract-pane XP-8** (same three controls, already routed **X-W4** + BH relay). Cross-referenced, not re-booked; the capture-harness title-blindness (capture.mjs:102-105) verified by the sibling.
- **EY-30 — r1 D-15 + reader-1-found (consolidated): the `<DockSeparator/>` the canon forbids renders 1px × 0px — `--dock-separator-height` is declared only inside `.glass-dock` (shell.css, verified) so `height: var(…)` is invalid outside it — while still announcing `role="separator"` + `aria-orientation` (dist/dock.js, verified).** A producer component outside its required housing: invisible line, live AT noise, canon retained-line row `Extract | none`. Cure: delete the element (one line); the outside-dock rendering contract is a **BH-relay note**. → **EY-CLUSTER rider.**
- **EY-31 — reader-found both (consolidated, reader-2's MAJOR demoted — ruled above): the declared enter/exit motion family is structurally inert and the loupe's only transition targets a property nothing writes.** `<Transition name="vj-enter">` with no `appear` over an unconditional child: runtime-core skips enter when `!state.isMounted` and short-circuits leave when `isUnmounting` (both verified in the installed build) — the family (real, animations.css:83-98), the inline `--vj-enter-y: 0px`, and `@after-enter="onTransitionEnd"` (:206-208) have never executed. `.loupe`'s `transition: opacity` (:270) is dead under `v-if`; the two properties pin DOES change (:273-276) are untransitioned. Record-integrity limbs: C-5's cure receipt cites the dead :207; r1's D-3 cure asserts the opacity transition is live — the cure must ADD the binding. → **EY-CLUSTER.**
- **EY-32 — reader-1-found: the specimen dot's silhouette re-randomises on every sample — a writhing swatch during hover, a hard shape-cut per move under PRM.** Verified in the shipped build: `animate` defaults false; the `!animate` branch watches the color ref and re-seeds `borderRadius` per change; the eyedropper rewrites `sampledColor` per pointermove (:168). PRM limb rides guard-2's measured `transition-property` clamp (border-radius struck) — **cites the quarantine's own M-26 measurement in its own direction; no FALSE row carried** (M-25 UNPROVEN and M-26 FALSE are ConsoleRail rows, untouched). Producer-vs-consumer question moots at the X.W4.g face swap. → **X.W4.g / EY-CLUSTER.**
- **EY-33 — reader-1-found: "Tap to sample" is the sole instruction and it is wrong on fine pointers, where hover samples and the tap FREEZES** (:36 vs :151-172; the cursor tells a third story, :65). The prose face of EY-2's modal collision. → **EY-CLUSTER.**
- **EY-34 — reader-2-found: the loupe near an image edge composites up to ~75% translucent glass and page-behind under the magnified pixels — the same measurement NEG-1 used to certify the mapping clean.** `clearRect` + no ground fill + alpha backing (useLoupeCanvas.ts:35, :41-51); NEG-1's `opaquePx 2996/12100` proves the mapping AND the unfiled rendering defect at once. → **EY-CLUSTER.**
- **EY-35 — reader-2-found: the module documented as the sampling concern also owns painting the visible stage, exactly once, behind a silent null-guard, and nothing ever reads that canvas** (useImageSampler.ts:1-10 vs :86-92). The simpler mis-colocation beside L-14's transform triangle. → **EY-CLUSTER.**
- **EY-36 — reader-2-found: `watch(() => imageUrl, …)` (:247) resets no sample state — and is simultaneously unreachable in the shipped wiring** (the overlay covers both preview-producing entrances while open). Dead and wrong at once; a latent regression's standard shape. → **EY-CLUSTER.**
- **EY-37 — D-18: permanent `will-change: transform` on a native-resolution image canvas** (static class, :71; probe-A `canvasWillChange: "transform"` at rest in all matrices). Byte figures live-only. → **EY-CLUSTER rider.**
- **EY-38 — reader-2-found: `useReducedMotion` is published on glass-ui motion-core (verified: `composables/motion/core/useReducedMotion.d.ts`) and the file hand-rolls PRM through `useBreakpoint` instead** (useInertiaGesture.ts:37-39) — inside the very block every axis certifies as exemplary. Owner edict 4 at the smallest grain. → **EY-CLUSTER rider.**
- **EY-39 — N-3 + reader-2 INFO limb (consolidated): `loadImage` resurrects a disposed sampler (no generation token; :68-95 tail unconditional after the await) and `dispose()` leaves `imgWidth/imgHeight` live for the gesture's `contentSize`** (:97-101 vs :148). r3 IV-3's non-firing repro keeps it a code-certain, transcript-open row. → **EY-CLUSTER.**
- **EY-40 — L-22: hex serialization has four demo homes (`padStart(2` census = exactly 4, re-run by this seat) because the published surface has none.** → **X-W9** (published-surface decision; the 4.1 cut).
- **EY-41 — r3-only (C-r3 §II-2), ADMITTED: a plain wheel over a fitted image cancels the page default, moves nothing (clampPan re-centres to a no-op), and arms a ~70-frame coast with per-frame `getBoundingClientRect`.** Mechanism fully source-verified (:299-321, :72-90, :126); numbers r3's. → **EY-CLUSTER rider.**

### Identity cross-references (confirmed here; banked elsewhere — ONE identity each, anti-rename)

- **EY-42 — L-8: `DisplayColorSpace` re-declared 4× (this component exports the re-mint, :21; the test imports it).** ≡ **wb-extract-pane XP-14 → X-W5** (MINOR at the identity's severity; structurally identical today via `PickerSpace = SpaceId`).
- **EY-43 — L-21: tsconfig.demo.json paths drift (3 phantoms; `/css` — this component's own import — and `/value` unmapped; "CLOSED 8-key set" false by one).** ≡ **XP-21 / ColorSpaceSelector L-14 / componentsliders R-10 → NO-WAVE-OWNER** (X-W8 declined, W8.md:112). MINOR ruled above.
- **EY-44 — L-10: every demo import-boundary lint rule targets the deleted `demo/@` tree (eslint.config.js:235-238, :275-276 re-verified; `demo/@` gone).** ≡ **ColorSpaceSelector L-5(a)** lint-glob-liveness fold, with its born-RED `--print-config` gate. Not re-booked at component severity.
- **EY-45 — L-13: `useImageQuantize` carries a dead second camera implementation.** ≡ **wb-extract-pane XP-13 → X-W8 G-10 extension.**

### INFO

- **EY-46 — D-19 (= L-15, C-r2 Part IV): zero rendered coverage anywhere in the visual audit (extract absent from all six special-state matrices — re-verified `ls`), a 31-line vacuous test gate (whole file read: 3 tests, `canvasRef: ref(null)`), and C-r2's seven named mutations all keep it green.** → **X-W1** — extend XP-7's born-RED flow spec with eyedropper arms (open → loupe paints ≥1 opaque px; pan → tap samples in one click; RTL sample; Escape-scope; decode-error register).
- **EY-47 — L-30 + r1 D-21: four dead public names, and `useLoupeCanvas` couples to the shell's template by ref STRING (`useTemplateRef("loupeCanvasRef")` resolving against the caller's instance) while its returned ref is never bound** — rename the template ref and the loupe silently dies with no type error. → **EY-CLUSTER rider.**
- **EY-48 — L-27b: `img.crossOrigin = "anonymous"` guards an unreachable case (always a `data:` URL)** — and is set BEFORE `img.src`, the one ordering that would matter (superlative 2). → rider.
- **EY-49 — L-16 surviving half: `verbatimModuleSyntax` conformance clean across the chain** (record, not defect; the bullet's first half is killed, K-1).

**Not carried**: C-r2 NEG-3 (capture-ordering — order-independent, verified reasoning), NEG-4/IV-2 (22 µs hover cost — both seats agree not-a-defect; the anti-wave note is a superlative), NEG-5 (tainted canvas unreachable), N-6 (horizontal overflow clean), r2-D's dead `:not(:disabled)` selector observation (subsumed by EY-16's cure), L-31 (deliberate negative — superlative 7), the L-11 limb-1 registration-order note (no interaction, C-r2 NEG-3 concurs).

## Killed-claims register

Reader kills — **all 15 distinct kills UPHELD** on this seat's re-derivation:

- **K-1 — L-16 "`| undefined` redundant with `?`"**: FALSE — `tsconfig.base.json:11` `exactOptionalPropertyTypes: true` (read by this seat) makes the annotation load-bearing; deleting it is a behaviour-changing narrowing.
- **K-2 — L-3's pasted receipt "`grep devicePixelRatio demo/workbenches/` → no matches"**: FALSE output — `useMixingAnimation.ts:141` (read: `Math.min(window.devicePixelRatio || 1, 2)`); the same L file's pass 2 cites that exact line. Substance (no DPR in the eyedropper chain) survives as EY-18; the receipt must never be re-quoted.
- **K-3 — L-11 limb 2 (permanent double-bind on viewport-ref identity change)**: unreachable — the viewport div (:62-67) and the Transition child (:8) are both unconditional; `viewportRef` transitions null→el once per lifetime. (The gesture composable's own watch DOES clean up, :347-350 — read by this seat.)
- **K-4 — D-r2 §4 motion nit (swatch-pop 0.65s raw literal)**: the literal is a ratified KEEP row — demo/DESIGN.md §Bespoke-literals names `ImageEyedropper.vue:286` with its rationale (read verbatim). r2 regressed against its own r1 on a ratified row.
- **K-5 — r1 D-17 (40×40 sub-minimum tap targets)**: the producer applies the 2.75rem floor exactly where the floor is defined — `touch-floor.css` `@media (pointer: coarse)`, reaching these controls BECAUSE they are outside a dock (`:not(:where(.glass-dock *))`, read by this seat). 40×40 is a fine-pointer measurement against a 24px floor; r3-C IV-4 independently measured 44×44 on coarse.
- **K-6 — r1 D-9 (45.1% empty stage violates VC:28's ≤15% law)**: law misapplied — VC:28 governs the SECONDARY REGION of a two-part scene; aspect-preserving fit letterboxing is arithmetic, and the reading would condemn every correct image viewer, including the cure's own.
- **K-7 — r2 D-06's "cannot be scrolled to" clause**: ruled above (reader-1 kill upheld; reader-2's residue cell corrected). The `overlayExceedsViewport:false` flag vs its own rects: quote rects, never flags.
- **K-8 — C-r2 N-2's mechanism + NEG-2's clean bill (one identity, both readers)**: the 650ms-window framing and the "no bubbling hazard" negative proof both presume a listener that is never attached — `inheritAttrs: !1` + class/style-only spread verified in the shipped build by this seat. NEG-2 answered the wrong question and is the reason the real defect survived six prior passes. Promoted and carried as EY-7.
- **K-9 — L-23 (normative-spelling violation + `serializeLibraryColor` on `/css`)**: ARCHITECTURE.md:107 "specification notation only" + :216 "There is no general library-string serializer" (both read) — the framing over-reads spec notation into product law and the cure would build the exact serializer the pinned authority forbids. The demo-side drift survives inside EY-24.
- **K-10 — L-27a (vitest "convention" that demo suites must live under `demo/test/**`)**: the comment (read) explains why two relocated aurora suites were added to include; `test/**/*.ts` explicitly sanctions the sampler test. Colocation preference at most.
- **K-11 — r1 D-1's headline ("the color it reports is not the color on the screen")**: the sampler arithmetic is exact (r2 N-3 four-for-four; r2's own §8 correction) — display-side defect only, carried as EY-19.
- **K-12 — L-24(a)'s "backing ÷ CSS-box = 1.0 at 110/110"**: content box is 106 (border-box + 2px border, :262-266) — L-2's own pass-1 measurement had it right. Conclusion survives corrected in EY-18.
- **K-13 — L-11/C-r1 D-11 "capture listener never removed" as a leak**: subsumed by K-3 — element dies with the component.
- **K-14 — D-07's "unrecoverable truncation"**: `select-all` ships (:35) — recoverable, unreadable in place. Cure scoped to legibility (title + reservation), not data loss.
- **K-15 — C-5's cure receipt ":207 already calls fitToViewport"**: dead code — `@after-enter` on a no-`appear` Transition with an unconditional child never fires (runtime-core `!state.isMounted → return`, verified in the installed build). Only :203 is live.

Seat-minted kills:

- **K-16 — reader-1's D-03 cure endorsement ("§5 axis composition over BI Slider, canon-grounded")**: KILLED per A-17/CC-108 (RULING PIN 2) — the composition is a producer-roadmap ask riding the BH relay; the demo cure is bespoke. The defect stands untouched at BLOCKER.
- **K-17 — reader-2's L-28 cell "no legal demo-side shared home"**: the lattice grants `feature → shared` and `shell → shared` (ARCHITECTURE.md:50-54, read). The row survives on the corrected ground (producer seam + edict 5, not lattice impossibility).
- **K-18 — r3 D3-07 (53% stage empty at 200% as a standalone PR-10/§5.3 breach)**: same letterbox arithmetic K-6 retired; the measured frame folds into EY-15, whose recomposition is the only cure that moves it.

**Motion-law compliance**: no motion-quarantine FALSE row is carried. M-25 (UNPROVEN) and M-26 (FALSE) are ConsoleRail rows; EY-32 cites the quarantine's guard measurements in their own direction; D-r2's N-4 (PRM honoured) stands for motion and was correctly narrowed by r3 for state.

## Superlatives — provenance verified by this seat

1. **`useInertiaGesture`'s PRM ownership** (useInertiaGesture.ts:106-142): rides glass-ui `useRAFLoop` with `pauseWhenHidden: true`, deliberately sets `respectReducedMotion: false`, and OWNS the PRM decision in `startInertia` — snap to rest, zero the velocity so a resume inherits no stale momentum — with the reasoning written at the site. All three axes and both readers independently refused to manufacture a defect here, and they are right. (EY-38 is a one-line consumption nit inside it, not a contradiction.)
2. **`img.crossOrigin` set BEFORE `img.src`** (useImageSampler.ts:70-71): the one thing about crossOrigin that matters, correct even though inert on this path — and `getContext("2d", { willReadFrequently: true })` on the offscreen twin (:83) is the exactly-right hint, correctly NOT applied to the visible canvas, and is why C-r2 could measure 22 µs/move and stop the tranche coalescing a 0.26%-of-frame cost.
3. **The loupe's magnification geometry is integer-exact by construction** (useLoupeCanvas.ts:33-51): 11 texels into a 110 backing = 10 px/texel, `half=5` centres the sampled texel, edge clipping is proportional for free per the HTML spec — C-r2's NEG-1 proves it properly and is the best-argued paragraph in the corpus. The defects around it (power, DPR, centring, ground) are all OUTSIDE this arithmetic.
4. **The designed empty state**: the ghost `WatercolorDot` with stable `seed="eyedropper-empty"` (:24-33) — the producer's own ghost register, aria-hidden at the producer root, reading as "the shape of what this will give you" with zero AT noise.
5. **The tracked-canvas motion is canon and registered**: `transform var(--duration-fast) var(--ease-decelerate)` suspended during gestures via `gestureActive` (:71-72, :253-259), a KEEP row with rationale in demo/DESIGN.md — the one place the file writes down a motion decision and honours it.
6. **The corpus's negative-proof discipline**: D-r2 §7 + C-r2 §NEG + C-r3 §IV cleared seven suspects with evidence (object-URL, taint, interior mapping, edge clipping, hover cost — twice, capture ordering, PRM-motion) — three of four handed suspects innocent, said so, and stopped wave-spend. C-r2's N-4 ("an image is not a receipt here; screenshots may illustrate, they may not testify") plus reader-1's rects-not-flags corollary should be house law.
7. **L-31's negative finding** — "do not promote `useInertiaGesture` to glass-ui; one live consumer" — filed specifically to stop a future wave from doing the wrong thing. The hardest kind of finding to write, the most useful to inherit.
8. **D-r2 §8's frame-dependence reconciliation** of the RTL magnitude (692 vs 444 = the same formula at two geometries; re-derived independently by this seat and confirmed to the pixel in probe-A) — "the number must always be quoted with its frame" is adopted as law in EY-3.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **Contrast numbers**: D-08's 4.07:1 (r2) / 3.95:1-vs-5.00:1 same-glyph split (r3) / r1's 4.52:1/3.57:1 pair; r1 D-13's icon-ink 1.45–2.72:1; r1 D-10's 1.65:1 ring contrast + forced-colors byte-identity (WebKit forced-colors emulation partial per r2 §2.5). All token/mechanism limbs are statically CONFIRMED (EY-14/EY-16/EY-21); every ratio is a live composite over user photography.
2. **Geometry amounts**: D-06's fold-overflow rects (desktop committed in probe-B; the 200% arm's 2381.9/378px only in the same file's zoom200 block), the 96px pinned/unpinned readout reflow, the ~60px letterbox band. The "unscrollable" inference is DEAD (K-7); the amounts await the X-W1 matrix.
3. **Sticky-hover latch on touch** (D-r2 §2.2's tinted, 1.2×-enlarged `×`): mechanism proven (:279-282, no `(hover: hover)` guard; the guarded idiom exists two files over); whether :hover latches after THIS gesture on THIS element is a live observation.
4. **C-3's two escaping rejections at runtime**: floating-promise path proven; the console capture lives in gitignored `.playwright-mcp/` (C-r2's own N-4 rule applies to it).
5. **D-14's `vue-tsc` exit-0 gate claim**: accepted as reported; the runtime half (attr never reaches DOM) is independently proven from the shipped build.
6. **L-9's per-op cost**: three mutually inconsistent figures (1.26×/6.5×/1.19×) — no number quotable; the ownership argument stands without one (EY-25).
7. **r2 D-13(C) "wheel momentum double-counted"**: hypothesis only — each event OVERWRITES velocity (read); the intended-coast shape. Not promotable without a measurement; distinct from the measured EY-41.
8. **EY-12's remount transcript** (r3's probe9 receipts) and **EY-20's stale-pin transcript**: mechanisms source-confirmed; the numbers are r3's, whose probe scripts live in a session scratchpad, not the tree — the XP-7/EY-46 flow spec is designed to mint durable replacements.
9. **Focusable censuses** (21-of-22 / 19-behind): structure statically certain (EY-8); counts live-only.

## DISSENT / method notes — preserved, not ruled away

1. **The readers' round was excellent and its one systematic gap repeats the fleet's**: all 15 kills survived independent re-derivation — several sharpened (K-8's six-pass survival analysis; K-12's self-correcting L record) — and 15 of 17 promoted misses survived attack (two consolidated, none refuted). But neither reader read the tranche-X wave files (K-16 — a cure endorsed against a dead producer roadmap, the exact failure the wb-extract-pane DISSENT recorded), and neither opened the r3 files sitting in the same directory, which had already measured two of their promoted misses and carried three rows they never saw. *A re-reader's corpus is the directory, not the round it was pointed at.*
2. **r3-D's severity dissent preserved**: D3-01 (material tier) and D3-02 (pulse latch) filed BLOCKER; this seat ruled both MAJOR (EY-15, EY-7) under r2's own rubric. If the owner weighs "the instrument's only confirmation is silent for the session" as job-failure, EY-7 promotes without any new evidence.
3. **The corpus's self-corrections are its best feature and are retained as law**: r2 killed r1's headline (K-11) and its own round-1 ratified-literal regression (K-4) was caught by the reader against r1; C-r3 killed C-r2's NEG-2 with a receipt; L's pass structure preserved its own contradictory receipts honestly (K-2, L-9's three numbers). Under E-3 every round is addenda; nothing was rewritten.
4. **Unresolved severity instinct**: a case exists for EY-1 + EY-5 + EY-6 as one BLOCKER identity ("the magnifier does not magnify: blank, occluded, and under-powered") — kept at the readers' grain so each mechanism retains its own witness; a formation may merge them without loss.
5. **What this seat could not do**: no browser (commission), so no residue number moved to CONFIRMED here. Every disposition above is decidable from source + the shipped dists + the committed JSON evidence, and was.

## Closing verdict

**ADJUDICATED RED — the corpus holds, both readers hold, and the sharpest truth is jurisdictional.** 44 rows survive (4 BLOCKER · 20 MAJOR · 16 MINOR · 4 INFO): 26 corpus rows confirmed (six as identities already banked), 15 reader-found admissions, 3 r3-only admissions; 18 kills with written refutations (15 reader kills upheld, 3 seat-minted against reader cells); 2 seat demotions ruled. Terminal dispositions: **2 rows to X.W4.g** (trigger-gated face carve: EY-7, EY-27, with EY-32 riding), **2 identities at X-W4** (EY-8/EY-29 via XP-9/XP-8), **2 to X-W7** (EY-15 composition addendum; EY-24 format-color home), **1 to X-W5** (EY-42 identity), **1 to X-W8** (EY-45 identity) **+ 1 to X-W9** (EY-40), **1 to X-W10** (EY-14's utility home), **1 to X-W1** (EY-46's flow-spec arms — the highest-leverage artifact this record proposes), and **the entire BLOCKER set plus fourteen more rows to NO-WAVE-OWNER**: the eyedropper interaction/render cluster (EY-1..6, 9, 11, 13, 17-22, 25-26, 31, 33-39, 41, 47) joins wb-extract-pane's session cluster (EY-10, EY-12, EY-23) at the formation boundary. The component's magnifier never paints, its tap eats itself, it is inert in RTL, and it cannot be reached without a pointer — and no wave in tranche X owns a single line of the code that must change. That, not any row, is this apotheosis's headline.

— end of apotheosis. The challenger records (r1/r2/r3, all axes) and both re-reader returns remain immutable beside this file per E-1/E-3.
