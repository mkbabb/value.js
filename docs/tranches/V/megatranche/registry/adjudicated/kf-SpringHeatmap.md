Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SpringHeatmap; receipt read from this seat's own system prompt, not inherited).

# kf-SpringHeatmap — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c`, `origin/master` `81a56990`; `package.json`/`package-lock.json` dirty in the worktree (the F-1 substrate, see K-1); **SpringHeatmap.vue (338 L) read whole this session and byte-concordant with every reader anchor**. · **Registry tree** value.js `tranche-u` · **Corpus**: two fresh hostile re-reader returns over `audit/kf-components/SpringHeatmap/challenge-{D-design,L-library,C-consumption}.md` (all three challenge files re-read whole this session) — **reader-1** (design-and-usage: 24 confirmed · 8 kills · 7 misses · 7 unproven · 6 superlatives) and **reader-2** (library-and-code: 23 confirmed · 6 kills · 3 misses · 6 unproven · 7 superlatives).

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SpringHeatmap.vue (338 L) and SpringPhysicsFacet.vue (242 L); SpringScene.vue:8-13/:30-34/:65-67; demo/app/App.vue:60-64; SpringTarget.vue:270-280; useSpringDemo.ts:83-84/:320-345; springPresets.ts preset table (smooth 0.5/0.86 · snappy 0.35/0.65 · bouncy 0.5/0.45 · gentle 0.7/1.0); **the full arithmetic re-executed in node from the exact source expressions** — mix per row `0 0 0 0 0 0 0 0 0 2 4 8 12 17 23 29 35 43 50 59`, presets 2/15/33/0 %, pointer lattice `0.13 … 1.17`, arrow walk from the 0.50 seed `0.55 0.61 0.67 0.73 0.78 0.84` (IEEE: `0.5 + stepR = 0.5549999999999999` → 0.55), reversibility failures **84/111** response and **95/131** damping, damping walk `0.86 0.93 1.00 1.07 1.14 1.20 1.26`, worst click→param error **0.0325 / 0.0375** vs the published 0.0275/0.0325, paint ζ floor 0.2325; **the git baseline** — `git show origin/master:package.json` line 77 `"@mkbabb/glass-ui": "7.0.0"` (devDependencies) + lock lines 19/612/614 resolving 7.0.0 from the npm registry, worktree grep = 0 hits in both files, installed = 7.0.0 exact; **the installed producer bytes** — `dist/dark-z_P5QwqI.js` verbatim `n(e, (e) => { document.documentElement.style.colorScheme = e ? "dark" : "light" }, { immediate: !0 })` (a default/pre-flush watcher registered at singleton construction), `useGlobalDark.d.ts` `onFlipSettled` "the post-flip SETTLE hook", `dist/keyboard.js` skip predicate = INPUT/TEXTAREA/SELECT/isContentEditable/`.monaco-editor` only with **`defaultPrevented` = 0 occurrences**, `dist/labeled-field.js` **`valueCommit` = 0 occurrences**, package.json **73 exports incl. `./canvas`**, `resolveCanvasColor.d.ts` "… Canvas2D would reject raw … → NOT black", `tokens/property-regs.css` **22 `@property` registrations (1 `<angle>`, 1 `<color>` = `--glass-accent`, 3 `<length>`, 12 `<number>`, 5 `<percentage>`)**, `tokens/scheme-motion.css` `--duration-fast: 0.2s` / `--z-content: 10`, `theme/radius.css` `@theme { … --radius-pill: 9999px … }`; **the demo cascade** — style.css:130/:163 (`--accent-kf` light-dark violet → `--color-progress`), :181-190 (`.dark` = destructive-only; the comment says the motion tokens "flip with color-scheme"), body `touch-action: manipulation`, design-idioms.css:47/:60 (`--rail-width: clamp(25rem, 33svi, 32rem)`, `@property <length>`) and :73-79 (`.focus-ring:focus-visible` only), ControlsPaneWrapper.css:127 `width: var(--rail-width)`, demo `@property` census = **4** (`--lit`, `--axis-active`, `--ball-p`, `--rail-width`), `--ball-tone` declaration census = 3, all on `*-target` classes; **the collision chain** — useControlsKeyboardShortcuts.ts:53-58 (ArrowLeft/Right, Shift+arrows, Home/End → `scrubActive` with `preventDefault: true`), AnimationControlsGroup.vue:322 (unconditional registration), EditorShell.vue:75 (always-mounted); amiga/utils.ts:3-13; `ls bench/` = 19 artefacts, no heatmap; `grep -rln "eatmap" test/ e2e/ scripts/ bench/` = 0; spring barrel exports (no peak/settle helper) + progress.ts:104-106 (`ω₀ = 2π / response`, `ζ = dampingFraction`) + `solveDampedHarmonic` :47/:332; `role="application"` census — SpringHeatmap.vue is the demo's sole site, registry sweep 0 hits (unbanked). Registry instruments: kf-CopyButton.md:10/:24 (SCH-1/F-1 baseline law), kf-CSSCodeEditor.md KF-CE-28, kf-KeyframeCard.md KF-KC-28, kf-AmigaScene.md D-4 + MISSED-G, kf-CubeTarget.md #60, kf-AnimationControlsGroup.md M-1/L-2 + DISSENT #2, kf-KfPillTabs.md N-KPT-1, kf-SequencePlayhead.md D-14.

## Scoreboard

**35 roster rows — 0 BLOCKER · 17 MAJOR · 13 MINOR · 5 INFO.** All three axis BLOCKERs die as blockers (two demoted to MAJOR by both readers concordantly, one killed outright on the baseline). **Killed-claims register: 12 entries**, including one kill-of-a-confirmed-cell against each reader. **7 net-new missed finds admitted (N-SH-1..N-SH-7) + 1 rider**; of the 10 filed misses, one folds into another's limb, one is a kill (not a row), one becomes a rider. **8 superlatives** (one corpus-level). **8 UNPROVEN residues → SS-13.**

**Headline.** The corpus's arithmetic is flawless and its physics is right — and the instrument is wrong four ways at once: the legend labels the wrong axis, half the field is provably blank, the x axis carries zero information by its own docstring, and the published precision constant is false by 18%. The readers' real work survived this seat's re-derivation almost intact: every executed number reproduced to the digit. The adjudication's own yield is threefold. (1) **The theme-race direction is settled against axis D**: the installed dark chunk writes `documentElement.style.colorScheme` in a pre-flush singleton watcher registered before the component's own, so light→dark cannot paint near-white — the fatal direction is dark→light via the `.dark`-arm literal, exactly as L-B-2 stated; reader-2's kill sustained, reader-1's uncorrected adoption corrected. (2) **D's "airtight dead tone-repaint" dies in every branch of its own disjunction** — and this seat adds a third branch neither reader named: engines implementing css-values-5 arbitrary-substitution semantics substitute `light-dark()` in unregistered custom properties at computed-value time, so `getPropertyValue` itself returns the resolved arm and the repaint re-tints. The SS-13 probe must log the `getPropertyValue` return beside the `fillStyle` read-back to identify which of THREE branches ships. (3) **The keyboard collision inventory gains its second confirmed-live carrier** — a focused heatmap's ArrowLeft/Right steps the param, rebuilds the live SpringProgress, AND scrubs the playhead ±0.01 through the window shortcut registry (no `stopPropagation` here, no `defaultPrevented` check there), and both confirmed-live carriers now mount in the SAME facet, fifteen lines apart. Compounded by N-SH-1: the click that grants the field this capture paints no focus ring, so none of it is disclosed on screen.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census taxonomy (KF.W0/W6/W9) with **NO-WAVE-OWNER** for the spring physics-facet repair packet. Line anchors bind to the worktree at HEAD `8281638c`; re-resolve before any cure.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D-M2's failing direction** — reader-1 confirms "D-M2 = L-B-2" wholesale (inheriting D's "light→dark leaves the field near-white"); reader-2 kills D's direction paragraph and confirms the race on L-B-2's ground | **Reader-2 SUSTAINED (K-6); race CONFIRMED MAJOR; reader-1's cell corrected** | `dist/dark-z_P5QwqI.js` read this session: the colorScheme writer is a default-flush watcher with `immediate: !0`, registered when the `useGlobalDark()` singleton is constructed — at SpringHeatmap.vue:269, i.e. strictly before the component's :270 watcher (earlier still if any prior consumer constructed it); pre-queue jobs drain in creation order. So at paint time light→dark has `color-scheme: dark` already inline while `.dark` lags — the self-switching `light-dark()` tokens resolve to the INCOMING arm wherever they resolve at all. Dark→light is the fatal direction: `.dark` still on `<html>`, `dark-arm.css`'s theme-blind `--neutral-0: hsl(24 9% 4%)` wins, the field paints dark-on-light until a resize. L-B-2:95-114 states exactly this asymmetry. Mechanism family ≡ banked **KF-CE-28** (raw per-instance `watch(isDark)` where the producer documents `onFlipSettled`) — mechanism folded by reference, this site booked as the second instance with the stronger consequence (a correctness race, not a batching miss). |
| 2 | **D-M3's "airtight consequence"** ("the tone string is byte-identical … the repaint CANNOT re-tint the accent, ever") — reader-1 confirms it inside D-M3; reader-2 kills the airtight label as one horn of D's own UNPROVEN disjunction | **Reader-2 SUSTAINED and STRENGTHENED (K-7); the confirmed core stays MAJOR** | Three live branches, not two. (a) Classical `getPropertyValue` + a canvas parser that rejects `light-dark()`: every assignment is a silent no-op, the field is 400 opaque-black rects — no tone channel exists to re-tint. (b) Classical `getPropertyValue` + a parser that resolves `light-dark()` against the canvas's used `color-scheme`: the pre-flush colorScheme write (ruling 1) has already flipped, so the identical string resolves to DIFFERENT colors per theme and the :270 repaint is exactly what re-tints the tone. (c) **This seat's third branch**: css-values-5 reclassifies `light-dark()` as an arbitrary substitution function; engines shipping that semantics substitute it in unregistered custom properties at computed-value time, so `getPropertyValue` returns the RESOLVED arm outright — D's byte-identity premise itself dies, and the :111-113 comment ("dark mode re-tints for free") becomes accidentally TRUE. In no branch is "the repaint is dead weight for the tone" airtight. What is branch-independent and CONFIRMED: zero validation/read-back/fallback on a silently-ignorable assignment, the producer naming the exact hazard in a shipped docblock (`resolveCanvasColor.d.ts`, verified), the demo's own second statement of the law (amiga/utils.ts:3-13, verified), and the theme-blind surface operand under ruling 1's race. The SS-13 probe (residue #1) must log `getPropertyValue` AND `ctx.fillStyle` to pick the branch; branch (a) escalates the row to BLOCKER. |
| 3 | **Severity splits**: L-M-2 (reader-1 "MAJOR (behaviourally inert)" vs reader-2 MINOR) · L-M-5 (1: MINOR, 2: MAJOR) · L-M-6 (1: MINOR, 2: MAJOR) · L-m-3 (1: INFO, 2: MINOR) · D-m5's surviving no-fallback limb (1: INFO, 2: MINOR) | **MINOR · MINOR · MINOR · MINOR · INFO** | L-M-2: every consumer supplies the fallback, nothing mispaints today — a false load-bearing comment plus drift-silence is MINOR (reader-2 wins; reader-1's own parenthetical concedes inertness). L-M-5: zero live mismatch, latent drift + ghost comment — the SequencePlayhead D-10/L-2 duplication precedent is MINOR (reader-2's MAJOR preserved as dissent). L-M-6: the number is load-bearing prose whose evidence was deleted, but the design decision it defends is independently re-verified true (superlative 1) — evidence-hygiene MINOR, cure = re-land `Q.W-HEATMAP-EVID` or strike the number at all three sites (dissent preserved). L-m-3: a LIVE 1px offset / 2px size delta plus three co-existing definitions of one coordinate space in navigation code = real defect of low impact, MINOR (reader-2 wins). D-m5 residue: **this seat overrides reader-2** — under banked kf-SequencePlayhead D-14's token law, a bare read of a plain externally-owned token (`--z-content: 10`, scheme-motion.css, verified) is the CORRECT form, `:321`'s defensive form is warranted for the `@theme`-emitted `--radius-pill` (the same banked row cites this very line as the positive precedent), and the only true defect in the fallback family is `:328`'s 160ms lie (booked at L-m-4). The "inconsistency" is per-token correctness; INFO. |
| 4 | **Missed-find dedupe** — 10 filed across the two readers | **7 admitted (N-SH-1..7) + 1 rider + 1 kill + 1 fold** | Reader-1 M-3 (modifier swallowing) folds into N-SH-2 as its component-owned limb — same handler, same guard absence. Reader-1 M-4 ("true scale" is dimensionally void — seconds vs dimensionless ζ admit no shared unit) is admitted as a RIDER on D-M1: it survives even with the cap lifted, so the cure may NOT be "restore the ratio"; the annotation goes or the design decides a deliberate mapping. Reader-1 M-7 is a kill, not a row (K-8). All seven admissions re-verified at the bytes this session (see roster). |
| 5 | **The keyboard collision site (N-SH-2)** — reader-2 files it as a fold-by-reference site extension; no reader-1 counterpart | **ADMITTED MAJOR; chain verified end-to-end at this seat's own instruments; folds to the banked keyboard-scope spec row as its SECOND confirmed-live carrier** | My own reads close every link: SpringHeatmap.vue:235-263 switches on `e.key` alone, `preventDefault()` at :258, zero `stopPropagation` (whole read); useControlsKeyboardShortcuts.ts:53-58 registers ArrowLeft/Right (±0.01), Shift+arrows (±0.1), Home/End with `preventDefault: true`; AnimationControlsGroup.vue:322 registers unconditionally; EditorShell.vue:75 mounts it always; `dist/keyboard.js` skips only true form elements and contains zero `defaultPrevented` reads — a `role="application"` div is dispatched. One ArrowRight on the focused field = param step + SpringProgress teardown/rebuild (useSpringDemo.ts:341→:320-343, verified) + playhead scrub, disclosed nowhere. ArrowUp/Down don't collide (unregistered). Home/End over the focused field are NOT handled by the heatmap (D-m1's gap) so the registry alone fires them — registry-normal, not a heatmap defect. Identity: ≡ banked kf-AnimationControlsGroup M-1 (producer seam: "no defaultPrevented check") whose first confirmed-live carrier is kf-KfPillTabs N-KPT-1 — **and both live carriers mount in SpringPhysicsFacet (KeyframesEditor at :119, the heatmap at :58): one facet is now the demo's densest collision surface.** Site booked here; mechanism NOT re-booked; the glass-ui BH relay inventory gains member #2. |
| 6 | **D-B2 vs D-M8 identity** — reader-2 folds them into one row; reader-1 books separately | **TWO rows, one mechanism note** | One mechanism (tint = f(row) only, fillStyle assigned at :170 before the col loop at :171 — verified) but two distinct design failures: the y-axis legibility collapse (9 blank rows, 2.33:1 full range, the shipped vocabulary in the blank end) and the x-axis misrepresentation (a 20×1 signal wearing a 20×20 affordance, contradicted by :16/:32 and SpringPhysicsFacet.vue:50-52). A cure for one does not cure the other. |

**Corrections register (against this corpus's readers).** Reader-1: (a) adopted D-M2 without D's direction error corrected — ruling 1; (b) "property-regs registers 22 properties, **none a color token**" — false by one: `--glass-accent` is `@property syntax: "<color>"` (this seat's grep); the correctly-scoped conclusion (none of `--background`/`--neutral-0`/`--color-progress`/`--accent-kf` is registered) survives; (c) its confirmed D-M3 cell carries the "provable no-op" leg — demoted per ruling 2. Reader-2: (a) L-M-2 severity correction ADOPTED (and its catch of L's "once per row-loop entry (:152)" provenance misread — `resolveTone` runs once per paint, outside the loop — adopted); (b) filed no cells on D-m4/D-m6/D-m8..m10 — no consequence, reader-1 covered all five and this seat re-derived the decisive facts. Credits: reader-2's end-to-end collision chain and the three-branch-adjacent disjunction logic; reader-1's D-B3 sharpening (both children `aria-hidden` → an EMPTY application subtree — verified :37/:45), the D-m4/D-m6 kills, and the LabeledSlider `valueCommit`-absence fact both its M-2 miss and its D-m6 kill stand on (verified: 0 occurrences).

## Adjudicated defect roster — id · severity · claim · disposition

### MAJOR

- **D-B1** — the legend labels the field's HORIZONTAL extremes with the damping regimes the paint loop maps to the VERTICAL axis, contradicting the aria-label 18 lines above (:50-53 plain flex row `← underdamped (rings)` / `critical / overdamped →`; ζ = f(row) only at :163-164; :32 says "damping (vertical)"; no rotation/writing-mode in the scoped block — verified whole). D's BLOCKER declined by both readers (a two-span text error; the instrument still navigates) — **MAJOR**, blocker dissent preserved. New to the corpus (T lane 06-spring F5 quoted this legend without seeing the inversion). **NO-WAVE-OWNER (instrument-truth core of the facet packet).**
- **D-B2 / L-M-4 / C-M-4 (y-limb)** — 9 of 20 rows paint pixel-identical to the field's own `--background` (mix table re-executed: nine zeros; row 9 at 2% imperceptible); full-field range 2.33:1 light / 3.14:1 dark; worst adjacent step 1.026:1; **all four shipped presets land at 2/15/33/0 % and the seed (= the smooth preset, 0.5/0.86 — verified) sits in the 2% band**. Three independent derivations now concordant to three decimals. **MAJOR. NO-WAVE-OWNER** (the packet's central design decision, with D-M8). **KF.W9** witness rider.
- **D-M8 / L-M-4 (x-limb)** — a 20×1 ramp dressed as a 20×20 field: the x axis carries zero information by construction, stated by the file itself at :93-97, while :16, :32, and SpringPhysicsFacet.vue:50-52 all claim a 2-D parameter space; 400 fillRects paint 20 flat bands (380 redundant calls). The honest alternatives are already in the corpus: settle-time on x (reserved at P.W6:92) or a declared 1-D ramp — or C-M-4's 20-stop CSS `linear-gradient`, which also dissolves D-M3 entirely. **MAJOR. NO-WAVE-OWNER (one decision with D-B2).**
- **D-B3** — `role="application"` keyboard widget exposing no value and announcing no change: :28-36 is the complete attribute set (no `aria-valuenow/valuetext`, no live region); the readout :18-21 is an unassociated sibling; **both children are `aria-hidden` (:37 canvas, :45 marker), so the application subtree is EMPTY** (reader-1's sharpening, verified). Severity capped MAJOR (D's BLOCKER declined): the identical two params are fully operable and announced by the two LabeledSliders fifteen lines up (SpringPhysicsFacet.vue:27-46). Sole `role="application"` in the demo; unbanked (registry sweep 0). **KF.W9 + NO-WAVE-OWNER** (the packet decides: sliders-as-AT-path + honest widget semantics here, or a real 2-value slider pair). Cure sequencing note: any `aria-valuetext` must wait on the packet's lattice decision (L-M-1) — there is no honest cell to announce until the two input modes share one.
- **D-M1 / L-m-6 (+ rider N-R1: "true scale" is dimensionally void)** — `max-height: 16rem` beats `aspect-ratio: 11/13` at every shipped width: the cap binds above 216.6px and the host width is a TOKEN, not a live unknown — `--rail-width: clamp(25rem, 33svi, 32rem)` (registered `@property`) consumed at ControlsPaneWrapper.css:127, less CardContent `px-4` → ~340-470px field; anisotropy ≈1.5-2.1×. Statically decided; only the exact px is SS-13's (#6). **Rider (reader-1 M-4, admitted)**: response is seconds, ζ is dimensionless — no scale factor makes 1.1s and 1.3ζ commensurable, so "true scale" is void even uncapped; both original axes conceded the premise while attacking the cap. The cure may NOT be "restore the ratio": delete the annotation or author a deliberate mapping. **MAJOR. NO-WAVE-OWNER.**
- **D-M2 ≡ L-B-2** *(ruling 1; D's direction paragraph killed, K-6)* — the theme repaint races the class flip: :269-270's default pre-flush `watch(isDark, paint)` reads computed style before vueuse applies `.dark` at `flush:"post"`; **fatal direction = dark→light** (the `.dark`-arm literal `hsl(24 9% 4%)` is theme-blind; the rasterised canvas keeps it until a resize). The producer ships the cure on the already-imported subpath (`onFlipSettled`, verified in the installed d.ts). Mechanism ≡ banked **KF-CE-28** — folded by reference; this site booked (second instance, stronger consequence). Witness = SS-13 #2 (cured-by-resize-not-by-retoggle is the signature). **MAJOR. KF.W6** (the canvas/theme re-bake family kf-AmigaScene D-4 already routes "per the SpringHeatmap/CSSCodeEditor precedent" — this is that precedent's component-side booking).
- **D-M3 / L-M-3 / C-B-1** *(ruling 2; C's BLOCKER severity killed K-2, D's airtight leg killed K-7)* — an unresolved token stream is interpolated into `ctx.fillStyle` with zero validation, read-back, `CSS.supports` probe, or canvas-valid fallback path, staking the entire render on undeclared UA behavior with a silent failure mode whose outcome is one of THREE branches (solid-black field / classical-resolve / ASF-resolve — ruling 2); the producer names the failure in advance (`resolveCanvasColor.d.ts` "→ … NOT black", exported from `./canvas`, never imported) and the demo states the same law itself (amiga/utils.ts:3-13). The surface operand is additionally theme-blind under ruling 1's race. `:119`'s green and `:124`'s `#fff` last-resorts are unreachable AND wrong-identity (booked at L-m-4/D-m2). **MAJOR — escalates to BLOCKER iff SS-13 #1 lands branch (a). KF.W6** (canvas color discipline: `resolveCanvasColor`/`useCanvas2D`, or C-M-4's CSS-gradient dissolution — one decision with the packet).
- **D-M4** — the interactive field has no perceivable boundary: border vs own fill **1.223:1 light / 1.216:1 dark** (host-independent — both operands are the field's own tokens; re-derived by both readers to 3-4 digits from the installed token values, concordant), fill vs `--card` 1.039:1 light; combined with D-B2 the top half of a ~400×256 clickable control is a 1.04:1 patch fenced by a 1.22:1 hairline (WCAG 1.4.11 wants 3:1). **MAJOR. NO-WAVE-OWNER** (one paint decision with the D-B2/D-M8 ramp redesign); **KF.W9** witness.
- **D-M5** — `@pointerdown` navigates on the down edge with no `touch-action`, no pointer capture, no drag: a touch scroll beginning over the field rewrites both params and fires the spring rebuild. The scoped rule carries no `touch-action` (verified :294-304 complete); body-level `manipulation` permits panning; the declaring idiom exists in five sibling scenes. Secondary (L-m-7 folded here): no `pointermove`/capture/`pointerup` — the sweep gesture the crosshair + `select-none` advertise is unimplemented, though `navigateFromPointer` is already the pure function a drag needs. Touch outcome → SS-13 #3. **MAJOR. NO-WAVE-OWNER.**
- **D-M6 + D-M7** *(one row, two limbs — the T-SPR-6 still-open remainder, recorded as design-PENDING in the live tree at SpringPhysicsFacet.vue:13-16)* — colour is the sole information channel (paint() emits only `fillRect`; no grid/ticks/contours/axis labels/numeric scale; no `forced-colors`/`prefers-contrast` treatment; canvas bitmaps are exempt from forced-colors adjustment) AND there is no hover affordance or preview while clicks snap to an invisible lattice the component never surfaces (it computes the honest tolerance at :82-83 and shows it to no one — see N-SH-4 for the constant being wrong too). **MAJOR. NO-WAVE-OWNER** (the packet's instrument design) **+ KF.W9** (forced-colors/contrast witnesses).
- **L-M-1** *(= D-m1's substance; D's enumerated lattice killed K-5)* — arrow navigation is irreversible and walks a different lattice from the pointer path: steps 0.055/0.065 are half-hundredths re-quantised to the 0.01 grid by round-half-up — right-then-left fails for **84/111** response values, up-then-down for **95/131** damping values (re-executed this session, exact); after one arrow press the marker never again sits on a cell centre, falsifying :234 and :222-224. Every press is a lossy write firing a full SpringProgress teardown/rebuild (`watch([response, dampingFraction], rebuildLiveSpring)`, verified) — L-i-3's key-repeat cost (~30 rebuilds/s) rides this row. Cure = one shared snap-to-cell helper for both input paths. Missing Home/End/PageUp/PageDown noted (D-m1) — but see N-SH-2 before adding Home/End: today the registry owns them. **MAJOR. NO-WAVE-OWNER.**
- **L-M-7 / C-M-2** — the prop is the entire ~45-key scene context (`SpringDemoContext = ReturnType<typeof useSpringDemo>` over 499 L) for a component that reads exactly two refs (`grep demo\.` → response/dampingFraction only, re-verified); un-instantiable in isolation (constructing the context boots five SpringProgress instances and arms a rAF loop). The inject alternative is CORRECTLY declined by both axes and both readers — verified: `tabsContent = () => h(SpringPhysicsFacet, { demo })` (SpringScene.vue:67) is instantiated by the shell (App.vue:62), outside SpringScene's provide chain. Honest contract = two `Ref<number>`/`defineModel` props. **MAJOR. NO-WAVE-OWNER (one contract commit with C-M-3).**
- **C-M-1** — shadow of glass-ui's `./canvas` subpath (proposed census row **S-9**): `useCanvas2D` + `resolveCanvasColor` cover the DPR policy (byte-equivalent default, verified in the chunk), resize/mount lifecycle, PRM parking, offscreen suspend, dispose, and the color resolution this file reimplements wrongly (D-M3). Verified against the INSTALLED 7.0.0: 73 exports, `./canvas` present, d.ts surface as cited. Reader-1's provenance nit adopted: the falsifier bytes live in `dist/resolveCanvasColor-CBnFEM0G.js`; `dist/canvas.js` is a re-export shim. **MAJOR. KF.W6** (with D-M3 — one consumption decision; census S-9 extension to KF.W0's shadow inventory).
- **C-M-3** — four write sites mutate parent-owned refs through the prop object with no `defineEmits`/`defineModel`, while the two sliders on the SAME surface write the SAME refs through a declared `@update:model-value` contract fifteen lines up, and SpringPhysicsFacet.vue:55-57 asserts a symmetry the code does not implement. Demotion considered (the context object is a shared-write surface elsewhere in the scene) and declined: the divergence-in-one-file plus the false mount-site comment is the defect, and the cure is C-M-2's same `defineModel` commit. **MAJOR. NO-WAVE-OWNER.**
- **N-SH-1** *(NET-NEW — reader-1 M-1, verified)* — **one mouse click silently captures the page's arrow keys with no visual indication**: :229-232 calls `fieldEl.focus()` after navigating; the demo's focus contract is `:focus-visible`-only (design-idioms.css:74-79, verified) and a pointer-initiated programmatic focus on a non-text element matches it in no shipping engine — so the field holds DOM focus ringless; :235-263 then `preventDefault()`s all four arrows until focus leaves. Every stray arrow rewrites the live spring (and, per N-SH-2, scrubs the playhead). The praised "Safari cure" (L's S-5) is precisely what makes the capture invisible — superlative qualified, not killed (the focus() is still required for keyboard access; the missing half is disclosure). **MAJOR. NO-WAVE-OWNER (one gesture/focus spec row with N-SH-2; witness SS-13 #8).**
- **N-SH-2** *(NET-NEW SITE of a banked mechanism — reader-2 miss #1 + reader-1 M-3 folded as the modifier limb; ruling 5, chain verified end-to-end)* — ArrowLeft/Right on the focused field ALSO scrub the live playhead ±0.01 through glass-ui's window shortcut registry (`preventDefault` without `stopPropagation`; registry skips only form elements and never checks `defaultPrevented` — 0 occurrences, my grep); Shift+ArrowLeft/Right step the param AND fire the ±0.1 large-scrub; Alt/Meta/Ctrl+Arrow are swallowed (`switch (e.key)` consults no modifier — browser history-back and word-nav cancelled while focused). One press = three undisclosed effects. **Identity: mechanism ≡ banked kf-AnimationControlsGroup M-1 (producer seam) — NOT re-booked; this is confirmed-live carrier #2 after kf-KfPillTabs N-KPT-1, and both carriers mount in this one facet.** **MAJOR site. NO-WAVE-OWNER (the banked keyboard-scope spec row) + glass-ui BH relay rider (inventory member #2 — owner edict).**
- **N-SH-3** *(NET-NEW — reader-1 M-2, verified)* — the marker's stated premise is false fifteen lines from its counter-example: :39-41 justifies the reactive `:style` because the position "moves only on a param edit … no 60 Hz hot path", but glass-ui's LabeledSlider forwards reka's continuous `update:modelValue` with NO commit seam (`valueCommit` = 0 in the installed dist — verified) and SpringPhysicsFacet.vue:35/:45 write the refs on every emit — so a slider drag drives the marker at pointer rate through :328's `--duration-fast` (real value 0.2s) transition: the tracker trails the whole gesture, settles ~200ms after release, and never converges under arrow key-repeat. The transitionless idiom is IN THE SAME FILE (the painter-positioned preset balls, :152-161). This row is what satisfies D-m6's falsifier (K-10) and tempers superlative 3. Lag percept → SS-13 #7. **MAJOR. NO-WAVE-OWNER** (packet decides: kill the transition during drags, or add a commit seam — the latter is a **glass-ui BH relay datum**: LabeledSlider ships no `valueCommit`).

### MINOR

- **L-M-2 / C-m-2 / D-m3** *(ruling 3)* — `--ball-tone` is structurally out of scope at `.spring-heatmap` (3 declaration sites, all `*-target` classes — my grep; `.spring-target` is a sibling subtree via the render-factory mount — verified), so :117 returns `""` on every paint and the fallbacks always fire, while :291-293/:307/:109-113 claim the seam is ridden. Behaviourally inert; the load-bearing docs are false and a `.spring-target` re-tint silently strands this instrument. **MINOR. NO-WAVE-OWNER (prose-truth sweep + one-token cure: declare the seam on a real ancestor or delete the arm).**
- **L-M-5 / C-m-1 / D-m11** *(ruling 3; reader-2's MAJOR preserved as dissent)* — the coordinate system is four bare literals duplicated across two files (SpringHeatmap.vue:70-73 vs SpringPhysicsFacet.vue:32-34/:42-44), guarded by a comment naming the deleted `SpringSidebar.vue`; the 0.01 step is re-duplicated as `* 100 / 100` at four more sites; obvious home (`springPresets.ts`) sits beside both. **MINOR. NO-WAVE-OWNER.**
- **L-M-6** *(ruling 3; dissent preserved)* — the 507× bench cited at three sites is still backed by a deleted `/tmp` script; `Q.W-HEATMAP-EVID` never landed (`ls bench/` = 19 artefacts, no heatmap; grep = 0 — re-verified). L-M-4's rider stands: the number also benchmarks a strawman 20× the honest work (400 rects for 20 bands). **MINOR. NO-WAVE-OWNER (re-land the bench or strike the number; one edit, three sites).**
- **L-m-1 / C-m-4 / D-m7(watch)** — a registered watcher whose entire body is a comment, allocating a fresh tuple per param change, three lines below the comment that already documents the absence for free. Delete :281-286; behaviour-preserving. **MINOR. NO-WAVE-OWNER (hygiene).**
- **L-m-2 / C-m-5 / D-m7(expose)** — `defineExpose` of two module constants with zero consumers anywhere (grep = 3 hits, all this file; mount site carries no ref), suppressing the default instance surface for a speculative consumer — the KISS law in constant form. **MINOR. NO-WAVE-OWNER (delete with N-SH-4's correction).**
- **N-SH-4** *(NET-NEW — reader-2 miss #2, arithmetic re-executed)* — **the one number the component publishes as its contract is wrong**: :79-83 documents HALF_CELL_* as "the honest navigation precision ±half a cell" and :277 exposes them as the gate witness, but :225-226's re-quantisation to the 0.01 grid makes the true worst-case click→param error **0.0325 vs the published 0.0275 (+18%) response, 0.0375 vs 0.0325 (+15%) damping** — and on the arrow path (L-M-1) no half-cell relationship exists at all. All three axes charged the expose as dead; none audited its value. **MINOR. NO-WAVE-OWNER (correct or delete with the expose; a future gate calibrated by it would be born wrong).**
- **L-m-3** *(ruling 3)* — three box models for one coordinate space: paint sizes from `clientWidth` (content), the marker rides `cqw/cqh` (content), clicks map through `getBoundingClientRect` (border) against a declared 1px border — a live 1px/2px delta that decouples silently under any padding edit; the correct source is the canvas's own rect. **MINOR. NO-WAVE-OWNER.**
- **L-m-4(:328) / D-m2** *(the :321 row killed K-11)* — the dead-fallback family's two lies: `var(--duration-fast, 160ms)` misstates the real 0.2s by 25% (verified), and the tone/surface last-resorts are an ad-hoc GREEN `hsl(142 71% 45%)` + `"#fff"` inside the function whose header forswears ad-hoc hex, against the ruled violet authority (style.css:155-163, SpringPhysicsFacet.vue:200-208 — verified). Family note: ≡ kf-CubeTarget #60's duration-hygiene class, fallback-divergence variant. **MINOR. KF.W6 (token hygiene rider).**
- **L-m-5** — zero test/e2e coverage on the scene's only click→param write path, an arrow state machine, a silent-failure canvas paint, and a theme-repaint contract — all unit-testable today without a browser (pure `overshoot`, two-line reversibility property test, arithmetic click map). The packet's born-RED list starts here: the reversibility test and the N-SH-4 tolerance assertion are RED against today's bytes. **MINOR. NO-WAVE-OWNER (packet test obligation).**
- **C-m-6** — the 0.9rem marker is clipped in half at the field extremes (`overflow-hidden` + −0.45rem centring margins + clamps exactly to reachable endpoints); reader-1's cross-axis sharpening adopted: cqw/cqh resolve against the content box while overflow clips at the padding box, so the right/bottom extremes clip too. Magnitude → SS-13 #4. **MINOR. NO-WAVE-OWNER.**
- **D-m8 / D-m9 / D-m10** *(grouped, reader-1 confirmed; reader-2 no cell — no consequence)* — the 33-word aria-label announced whole on every focus (no `aria-describedby` split); RTL flips the legend but not the field (physical-left paint + translate; demo declares no `dir`, which caps this at MINOR); no empty/error/loading state — a failed paint is indistinguishable from a successful one by D-B2's own arithmetic. **MINOR. KF.W9 (first two) + NO-WAVE-OWNER (error posture, with D-M3's cure).**
- **N-SH-5** *(NET-NEW — reader-1 M-5, verified)* — the panel's own vocabulary is never plotted on the landscape it ships: the four preset Chips (verified :66-89, writing the same two refs at :169-172) sit twelve lines below a field that renders three of the four presets on visually blank cells (2/15/0 %; bouncy at 33% is the exception), with no pips/labels/active-cell mark — the "navigable landscape" (:50) never teaches the vocabulary beside it. Four absolutely-positioned pips reusing the marker geometry close it. **MINOR. NO-WAVE-OWNER (with D-M7).**
- **N-SH-6** *(FOLD-SITE, not a new mechanism — reader-2 miss #3)* — `onPointerDown` has no `e.button`/`e.isPrimary`/re-entrancy guard: a right-click or second finger silently rewrites both params. Mechanism banked house-wide at **kf-AmigaScene MISSED-G** ("zero such guards in any scene") — site recorded here only. **MINOR. NO-WAVE-OWNER (the shared guard idiom row).**

### INFO

- **D-m5 residue** *(wrong-rung limb KILLED K-8; this seat's demotion, ruling 3)* — `z-index: var(--z-content)` is the file's one bare token read; under banked kf-SequencePlayhead D-14's token law that is the CORRECT form for a plain externally-owned token, and `container-type: size`'s `contain: layout` traps the marker in the field's own stacking context anyway (its only sibling is the canvas), so the value is inert. Recorded, not charged. **NO-WAVE-OWNER (hygiene note only).**
- **D-m4 residue** *(text-mono-caption half KILLED K-9)* — `text-caption`'s italic register on the legend is a glass-ui utility-taxonomy question, not this component's choice; the readout pairing is the house idiom 16 lines over 15 files including this facet (:79). **KF.W6 (typography-register datum, producer-side).**
- **D-m6 residue** *(kill K-10)* — `will-change: transform` is EARNED by the pointer-rate slider path (N-SH-3) but never released outside a drag; a scoped promotion is the nicety. **NO-WAVE-OWNER.**
- **N-SH-7** *(NET-NEW — reader-1 M-6, verified)* — `overshoot()`'s `zeta <= 0` arm is statically unreachable from every writer (paint floor 0.2325, keyboard clamp 0.2, slider min 0.2 — verified); defensible as pure-function totality, recorded chiefly because all three axes cited the 5-line function as the component's best idea and none read the branch. **NO-WAVE-OWNER (note on the packet's unit test: assert the domain, then the guard is specified, not dead).**
- **L-i-2 carried** — `const demo = props.demo` is a one-time non-reactive read (same idiom in the facet); harmless at one context per scene lifetime, latent if a parent ever re-keys. *(L-i-3 rides L-M-1; L-i-4 rides SS-13 #5; L-i-1 ≡ D-B3.)* **NO-WAVE-OWNER.**

## Killed-claims register — 12 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **L-B-1 (BLOCKER) + challenge-C §0's F-1 framing** — "`@mkbabb/glass-ui` is a phantom dependency absent from BOTH manifests; `:60` is unbuildable; every finding below is unreachable until this lands" | **KILLED — worktree-only artifact, both readers concordant, re-verified at this seat's own git reads**: origin/master declares `"@mkbabb/glass-ui": "7.0.0"` (package.json:77) with lock entries at :19/:612/:614 resolving 7.0.0 from the registry — exactly the installed version; HEAD declares it too (optionalDependencies 6.0.0, locked). Only the uncommitted worktree deletes both. Identity ≡ census F-1/SCH-1, already banked (kf-CopyButton.md:10/:24: "the phantom is worktree-only; cured at the frontier … every 'blocked on F-1' gate is RE-SCOPED") — **FOLD → KF.W0, zero re-booking.** The sixth-plus one-baseline-short event; the axes' "nothing below is reproducible" preambles fall with it. |
| **K-2** | **C-B-1's BLOCKER severity** ("the field paints solid black … in both themes") | **SEVERITY KILLED, substance retained at MAJOR** — the document self-refutes in-section ("the render outcome is UNPROVEN-NEEDS-LIVE"); a top severity cannot rest on a branch its author declines to assert; both sibling axes graded the identical mechanism MAJOR-with-capped-render-half. Escalation path preserved (SS-13 #1 → branch (a) → BLOCKER). |
| **K-3** | **C-B-1's provenance leg** ("@property registrations in this tree: only `--rail-width`") | **KILLED** — four demo registrations (`--lit`, `--axis-active`, `--ball-p`, `--rail-width` — this seat's grep). The load-bearing conclusion survives (none of the sampled color tokens is registered anywhere — verified against the installed 22-reg sheet), but a false census cell inside the corpus's only BLOCKER is recorded. |
| **K-4** | **C-S+3's parity superlative** ("keyboard and pointer paths are quantised identically … no drift between modalities") | **KILLED — a false superlative filed on top of a confirmed defect, the worst L-18 failure.** Executed: pointer lattice {0.13 … 1.17} vs arrow walk {0.55, 0.61, 0.67, …} — disjoint after one press; C mistook a shared 0.01 ROUNDING GRID for a shared LATTICE, contradicted twice inside its own corpus (L-M-1 MAJOR, D-m1 MINOR). |
| **K-5** | **D-m1's enumerated arrow lattice** ("from the seed 0.50 the lattice is {0.56, 0.62, …}") | **KILLED on IEEE arithmetic** — `0.5 + 0.055 = 0.5549999999999999` → `Math.round(55.499…)` = 55 → **0.55**; the real walk is {0.55, 0.61, 0.67, 0.73, 0.78, 0.84} (this seat's execution ≡ both readers). The substantive divergence claim survives at L-M-1. |
| **K-6** | **D-M2's failing-direction paragraph** ("light → dark leaves the field near-white on a dark panel") | **KILLED (ruling 1)** — the pre-flush colorScheme singleton write (verified at the dist bytes) precedes the component's watcher, so light→dark resolves to the incoming arm wherever resolution happens at all; the fatal direction is dark→light via the `.dark`-arm literal. L-B-2 stated the asymmetry correctly; reader-1's uncorrected adoption corrected. |
| **K-7** | **D-M3's "airtight consequence" label** ("the repaint CANNOT re-tint the accent, ever") | **KILLED as airtight (ruling 2)** — false in branch (b) (canvas resolves `light-dark()` against the already-flipped color-scheme → the repaint IS the re-tint), false in branch (c) (ASF semantics resolve it in `getPropertyValue` itself — the byte-identity premise dies), vacuous in branch (a) (no tone exists). One horn of an undecidable disjunction was promoted to proof. |
| **K-8** | **D-m5's "wrong rung" limb** ("`--z-content` … a stage rung borrowed for a panel marker") | **KILLED by containment** (reader-1's miss M-7, concordant with reader-2's kill) — `container-type: size` ⇒ `contain: layout` ⇒ the field is its marker's stacking context; z:10 there is indistinguishable from z:1. Precedent ≡ banked kf-KeyframeCard KF-KC-28 ("paint consequence KILLED — the breach is scoped inside the contained band"). Residue demoted to INFO (ruling 3). |
| **K-9** | **D-m4's text-mono-caption half** ("a label register applied to a numeric value readout") | **KILLED — the pairing is the demo's house idiom, not this component's choice** (16 pairing lines across 15 files including SpringPhysicsFacet.vue:79, twenty-one lines from this mount — verified at :79). The system-level question routes to KF.W6; the component does not answer for a taxonomy it consumed correctly. The `text-caption`-italic leg survives at INFO. |
| **K-10** | **D-m6 as filed** ("permanent `will-change` on an element that moves only on a param edit") | **KILLED — its own falsifier is satisfied**: the slider path writes the refs at pointer rate (LabeledSlider has no commit seam — `valueCommit` 0, verified), so the marker moves at pointer frequency during the dominant interaction and the promotion is earned. Residue (never released) survives at INFO; the same fact founds N-SH-3. |
| **K-11** | **L-m-4's `:321` row** (`var(--radius-pill, 9999px)` filed among "dead or lying fallbacks") | **KILLED by banked precedent** — kf-SequencePlayhead D-14 rules this exact line the POSITIVE precedent: `--radius-pill` is emitted from a Tailwind-v4 `@theme` block (verified in the installed dist), the one token class where the defensive form is warranted house idiom. Re-filing a banked-as-correct line as a defect is the anti-rename law's severity twin. The `:328` row (160ms lie) stands separately. |
| **K-12** | **L-B-2's historical leg** ("the MutationObserver was crude but correct for this job; R.W6 traded a working mechanism for an idiomatic broken one") | **KILLED as over-claimed** (reader-1) — a mutation record does land after the class flip, curing the SURFACE timing; but under the classical branch the tone token computes theme-identically, so no observer timing ever re-tinted the accent either. The prior mechanism was also broken for half the paint; the race itself is confirmed (D-M2 row); only the R.W6 verdict is corrected. |

## Superlatives (provenance preserved)

1. **The analytic-overshoot substitution, with its honest negative result — and it is the engine's own kernel.** `exp(-ζπ/√(1-ζ²))` (:100-104) replaces 400 live instances; the docstring states the library does NOT export a peak/settle helper rather than pretending reuse — negative re-verified by this seat (barrel exports enumerated: samplers and solvers only), and reader-2's strengthening verified at the bytes: progress.ts:104-106 declares `ω₀ = 2π / response` / `ζ = dampingFraction` and imports `solveDampedHarmonic` (:47, used :332) — the closed form is parameter-for-parameter the peak of the very system the engine solves analytically, which makes "EXACT (under 1% vs the live 60 Hz peak)" honest (sampling error, not model error). All three axes + both readers, concordant.
2. **The `container-type: size` hazard is anticipated and ANSWERED correctly** (:300-303): the one failure mode size containment carries (contents-derived collapse) is named and correctly refuted (the box's size is `aspect-ratio` + definite inline-size). Survives D-M1 intact — and the same containment fact is what kills D-m5's limb (K-8).
3. **Reduced-motion honesty, exactly scoped** — one transition declared (:328), exactly that one killed (:333-337), by deletion not shortening: correct PRM semantics for a discrete position. Census-corroborated (lane-frontend §6.5). *Tempered by N-SH-3: the position is less discrete than the file believes.*
4. **One coordinate space, no shadow state** — reads and writes go through the same two refs the sliders drive, `clamp` on both the read (:194-195) and the write (:260-262); no mirror, no debounce, no reconcile. useSpringDemo's anti-shadow-authority rule correctly transposed to a view component.
5. **High-DPI handling is textbook and independently arrives at the producer's own number** — `Math.min(devicePixelRatio || 1, 2)`, device-pixel backing store, `setTransform` so drawing stays in CSS units, re-derived per paint (correct: `canvas.width =` resets state); the installed glass-ui canvas chunk's default `dprPolicy` is semantically identical (verified).
6. **Teardown is complete by DELETION** — two auto-disposing composable calls are the entire resource surface; no rAF, timer, raw observer, or manual listener (whole-file read). The R.W6 raw-observer excision genuinely landed; orthogonal to D-M2, which faults when :270 runs, not how long it lives.
7. **The narrowest possible value.js surface** — one pure function (`clamp`) by exact subpath on the repo's sole declared runtime dependency; `/css`/`/color` untouched, transitive reach verified absent — the R1 parser-crash class is structurally unreachable from this component.
8. **CORPUS superlative, worth banking**: both the L and C axes independently constructed the most tempting false defect available ("inject SPRING_DEMO_KEY like the siblings"), traced it to the render-factory mount, and KILLED it themselves — C in an explicit kill table beside its correct `--z-content` kill. This seat re-verified the ground (App.vue:62 instantiates the vnode in the shell subtree). Two axes reaching the same self-kill by independent routes is the discipline the kill-register law exists to produce — in the same corpus whose BLOCKER died on the baseline and whose parity superlative died on arithmetic, which is why kills must carry their frame and their numbers.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The three-branch fillStyle probe (the D-M3 decider — ruling 2).** After paint() in light mode, log BOTH `getComputedStyle(field).getPropertyValue("--color-progress")` AND `ctx.fillStyle` read-back. Raw `light-dark(…)` stream + non-round-tripping fillStyle → branch (a): the field is solid #000 in both themes, **D-M3 escalates to BLOCKER**. Raw stream + round-tripping violet-ward `rgb()/color()` → branch (b). Resolved `oklch()/rgb()` from getPropertyValue → branch (c) (ASF semantics; the byte-identity premise dead). Cheapest equivalent: one light-mode screenshot of the Physics facet.
2. **The dark→light race witness (D-M2).** Open the spring scene dark, toggle light, do NOT resize: field stays near-black on a light panel; a resize cures it. Cured-by-resize-not-by-retoggle is the signature. (Light→dark is NOT the tell — K-6.)
3. **D-M5's touch outcome** — a vertical scroll gesture beginning inside the field on a touch viewport: params jump → confirmed; unchanged → the practical half dies, the missing declaration stands.
4. **C-m-6's clipping magnitude** — drive to (0.10, 1.50) and each extreme; count clipped pixels of the 0.9rem dot at all four edges (content-vs-padding-box delta included).
5. **L-i-4's opaque plate** — does the ~45% blank band read as an opaque rectangle punched through the `tier="quiet"` glass (`background: var(--background)` at :298), rather than the "faint wash" :116-117 describes?
6. **D-M1's exact anisotropy** — `.spring-heatmap` clientWidth in the shipped rail and the mobile sheet (verdict already static; only the factor is live).
7. **N-SH-3's lag magnitude** — drag the response slider full-range; measure marker trail and post-release settle (~200ms predicted).
8. **N-SH-1/N-SH-2's compound witness** — click the field once, then press ArrowRight ×3 and Shift+ArrowRight ×1: photograph the ringless field, the moved marker, the rebuilt spring, AND the playhead displacement (+0.03 then +0.1 predicted alongside the +0.1/+0.055-quantised param walk); then Alt+ArrowLeft: history-back suppressed. The 30-second keyboard trace per the banked N-KPT-1 idiom.

## Identity guards (fold-by-reference — never re-booked)

- **K-1 ≡ census F-1 ≡ SCH-1 ≡ kf-App/kf-CopyButton** → KF.W0; sixth-plus baseline event recorded in the pattern ledger.
- **D-M2's mechanism ≡ kf-CSSCodeEditor KF-CE-28** (raw `watch(isDark)` vs documented `onFlipSettled`) — mechanism folded, this site booked as instance #2.
- **N-SH-2 ≡ kf-AnimationControlsGroup M-1** (the registry's missing `defaultPrevented`/composite-widget awareness — producer seam) **whose live-carrier inventory opened at kf-KfPillTabs N-KPT-1**; this site = carrier #2, both in one facet. One keyboard-scope spec row; BH relay rider per the group DISSENT #2's inventory framing.
- **N-SH-6 ≡ kf-AmigaScene MISSED-G** (house-wide pointer-guard absence) — site only.
- **K-8's precedent ≡ kf-KeyframeCard KF-KC-28** (containment kills the paint consequence); **K-11's precedent ≡ kf-SequencePlayhead D-14** (`:321` is the banked POSITIVE example — the token law also drives this record's D-m5 demotion).
- **D-M3/C-M-1's family ≡ kf-AmigaScene D-4's KF.W6 routing**, which already names "the SpringHeatmap/CSSCodeEditor precedent" — this record is that precedent's own component-side booking; one canvas-color-discipline row, not two.
- **The flat-namespace hazard** (runtime string lookup of unprefixed tokens — D's §6-fold sharpest-case note) ≡ census lane-frontend §6.3 — datum contributed to the census identity, not booked as a row here.
- **L-m-4's duration family ≡ kf-CubeTarget #60** (raw/wrong duration beside tokenised easing) — variant noted, not duplicated.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the K-1/F-1 fold; C-M-1's census extension (proposed shadow row **S-9**, `./canvas`).
- **KF.W6**: the canvas-color + theme-flip discipline packet — D-M3 (three-branch cure: `resolveCanvasColor`/`@property <color>` registration/or C-M-4's CSS-gradient dissolution) + D-M2's `onFlipSettled` site + C-M-1 adoption, one decision (the kf-AmigaScene D-4 family row) · L-m-4/D-m2 token-fallback hygiene · the D-m4 typography-register datum (producer-side).
- **KF.W9**: D-B3 · D-M6's forced-colors/contrast limbs · D-m8/m9 · witness list (SS-13 #1-#8).
- **glass-ui BH relay (owner edict)**: N-SH-2 = keyboard-registry inventory member #2 (with the banked M-1/N-KPT-1 carriers) · N-SH-3's datum: LabeledSlider ships no `valueCommit` commit seam.
- **NO-WAVE-OWNER — the spring physics-facet repair packet (SS-1/SS-2 authoring must home it):** the **instrument-truth core D-B1 · D-B2 · D-M8 · D-M1(+rider)** — one design decision (what the field encodes, on which axes, at what scale, with what legend), not four patches · the **input-integrity core L-M-1 · N-SH-1 · N-SH-2(site) · D-M5 · N-SH-6(site) · N-SH-4** — one lattice, one gesture/focus spec, one honest published tolerance · the **contract commit L-M-7/C-M-2 + C-M-3** (two `defineModel`s; the facet binds the heatmap exactly as its sliders) · N-SH-3's transition decision · D-M4 + D-M7 + N-SH-5 (boundary, hover, pips — with the ramp redesign) · the prose-truth sweep L-M-2 · L-M-5 · L-M-6 · the hygiene deletions L-m-1/L-m-2/L-m-3/C-m-6 + the INFO residues · L-m-5's born-RED test obligation (reversibility property test + N-SH-4 tolerance assertion + the D-B2 mix-table snapshot — all RED today with zero browser).

## Verdict

**ADJUDICATED.** Two hostile re-readers re-derived the corpus to the digit and the register cut both ways: one BLOCKER died on a baseline no axis named, one BLOCKER died on its own in-section self-refutation, a parity superlative died on executed arithmetic, D's race direction died at the installed producer's bytes, and D's one "airtight" promotion died in all three branches of a disjunction whose third branch neither reader saw — while the readers' ten missed-find filings yielded seven genuine admissions, including the corpus's sharpest compound fact: a ringless click that hands the page's arrow keys to a widget which then steps a parameter, rebuilds a live spring, and scrubs the playhead per press, undisclosed at every layer. The component underneath is the program in miniature: a genuinely excellent 5-line kernel, textbook DPR and teardown, honest PRM — wrapped in an instrument whose legend, lattice, published tolerance, aspect annotation, tone seam, bench citation, and provenance comment are all false at once, and whose entire render stakes itself on an unvalidated token-to-canvas gamble the design system it already imports both names and cures. The repair is a packet — one field design, one input lattice, one two-ref contract, one resolved color path — not a rewrite. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life; N-SH-1..N-SH-7 for life.

— end of apotheosis. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/spring/SpringHeatmap.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SpringHeatmap.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — **D-M3**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here. **Nothing above this line is
rewritten** — no edit, no re-grade, no renumber, no deletion, no new id (E-1 immutable bytes / E-3
addenda-not-patch). This addendum records a MEASUREMENT STATE, not a grade: **the banked severity is
unchanged and stays exactly where the record put it.***

- **armed trigger** — register row **3** of thirteen, `evidence/W9/ESCALATION-REGISTER.json` (id as banked there: `D-M3 branch (a)`); ceiling if it fires: **BLOCKER**; roster cell(s): `safari-app/desktop`.
- **measurement named by the trigger** — SS-13 residue **#1** — `getComputedStyle(field).getPropertyValue('--color-progress')` AND `ctx.fillStyle`, **logged together in one pass** after `paint()` in light mode.
- **measured value** — **NONE. The trigger did not run.**
- **resulting severity** — **PROVISIONAL (unrun)**. The banked grade stands; it is neither inherited as confirmed nor moved.
- **exact precondition** — the three-branch probe run as worded. **Logging ONE value cannot pick a branch**, and S-13 halts a third such pass. the `chromium` and `chromium/emulated-forced-colors` cells DID open at `.d` (8 rows, 2 + 2 captures), so this trigger is **unrun, not foreclosed** there: it needs a pass that mounts the subject. the `safari-app/desktop` cell was open and productive at `.d` (4 sessions, OP-4 taken, 3 shots) and **closed mid-seat**: session creation began returning *"You must enable 'Allow remote automation'…"*; `safaridriver --enable` → **`Password:Password is not valid`** (an interactive admin authorization no seat here can supply); the running Safari is the **owner's** (pid 23725, started 16:23:59, 6 windows) and was not quit. **Precondition: 'Allow Remote Automation' re-enabled by a hand that can answer an admin prompt, or the owner's Safari restarted.** `.d`'s AT pass photographed this component's `role="application"` subtree in the `chromium` cell (`at-chromium-spring-heatmap-application.png`) — **that shot is not this probe** and is not offered as one.
- **substrate of this wave** — keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD; frontier moved to `3e81f500`, the pin is its ancestor, **no re-pin on a seat's authority**). Served bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448`, built in the §0m.2 capture clone and **re-hashed at this seat, double-run identical**. Capture: **none for this row**; shot-sha256: **none**.
- **G-KFW9-13 reading** — this row closes the gate's *"unrun triggers reported PROVISIONAL at close"* arm, **not** its *"measured value written back"* arm. The gate stays **RED**.
