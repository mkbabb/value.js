# wb-gradient-easingauthoringstage — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.

**Seat**: fresh Fable adjudicator (M-23 §1 / L-18) · **Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u`.
**Subject**: `demo/workbenches/gradient/GradientVisualizer/easing/EasingAuthoringStage.vue` (117 lines) + its parent `GradientEasingEditor.vue` and dependency cone, against installed `@mkbabb/glass-ui@7.0.0`.
**Corpus**: six challenge files (`challenge-D-design{,-r2,-r3}.md` — **r2 governs**, all Opus 5 receipts; `challenge-C-implementation.md` incl. seats 3+4; `challenge-L-library{,-r3}.md`) + probes/frames/evidence.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both returns were attacked, not transcribed: every verdict-level disagreement, every kill, and every load-bearing confirmation was re-derived against the tree by this seat.
**Arbiter's own instruments (all static + node against dist; zero browser probes — probe parsimony + the task's no-browser law)**: full reads of EasingAuthoringStage.vue (whole), GradientEasingEditor.vue (whole), useGradientCSS.ts (whole), useGradientModel.ts (whole), useSpecimenRows.ts (whole), easingCatalogue.ts (whole), useGradientInterpolation.ts:25-45, GradientStopEditor.vue:74-180, GradientVisualizer.vue:60-90, gradientParse.ts (interval seeding), o17 spec :40-60 + locator greps, router/index.ts, foundation.css (:351-355 well recipe, :743-750 contrast arm, :822-828 print arm, forced-colors roster), animations.css:55-85 + :184-193, eslint.config.js:200-281, tsconfig.demo.json:38-52 + `package.json#exports` (7 keys, no `.` root), `src/color/operations.ts` guard greps (:65/:90), `src/subpaths/easing.ts` export census, `cubicBezierToString` repo grep; **producer bytes**: dist/easing.js role census (`role:"img"`→**0**; 1×group/1×slider/2×status), both `glass-card` class-string mints, steps viewBox literal `"-0.05 -0.1 1.1 1.2"`, `block-size: clamp(200px,…` + `aspect-ratio:"1"`, `aria-valuemin:0/valuemax:1/valuenow=x`, sr-only "y from -0.6 to 1.6", `r:"0.04"`, `initialPreset: l.preset` (once, unwatched), sole prop watcher `A(() => l.mode`, `--easing-curve-accent` consumed by curve/handle strokes only, slider chunk (`slider-DzqeQmMu.js`) containing **zero** `--easing-curve-accent` references; EasingPicker.vue.d.ts prop union (+ an undocumented **`footer` slot** — this seat's own find), EasingConfigurator.vue.d.ts, useEasingPicker.d.ts (`fn` REQUIRED; `viewBox: ComputedRef`), constants.d.ts (`DEFAULT_BEZIER_PRESET:"ease-out-back"`, VIEW_PAD 0.1, MAX_OVERSHOOT 0.6, HIT_RADIUS 0.1/0.15), useClipboard.d.ts (4-state + `onCopyError` + `CopyResult`), material.css `.glass-card::before` + hover/active arms (0.07/0.1 hairline-rest/hover), surfaces.css `.glass-card:has(:focus-visible)` token swap; **node runs against dist/subpaths/easing.js**: 30 presets, [0,1]-escape set = exactly the three `back` presets (ease-out-back f(0.5)=1.067553, ease-in-back f(0.5)=-0.063622, ease-in-out-back grid-32 escape -0.023983@t=0.03125), keypress arms (y2=1.2 escapes; y1=1.0 grid-SAFE / y1=1.1 escapes — the 2-press and 11-press arms are BOTH true on different handles), grid-accident boundary ((0,0,1,1.12) safe / (0,0,1,1.15) crashes); motion-quarantine.md §1 (both guards, sha-pinned; Guard 2 property-list rewrite); OPTICAL-BENCH-COMPOSITIONS.md §3 member table + §5 boundary table + "Closed member routes"; VISUAL-CONSTITUTION.md :7/:58/:75/:82-84/:125-128/:210-211; PROPORTION-AUDIT.md :50-53/:66-67; tranche-X wave files W1/W3/W5/W6/W9/W10 for disposition routing.
**Witness-chain disclosure**: `.github/workflows/` = ci.yml/deploy-pages.yml/release.yml, `grep playwright|e2e|smoke` → 0 files; e2e spec census **71** at this HEAD (reader-LC's 70→71 correction reconfirmed and advanced by me — counts drift, claims don't).

## Scoreboard

**46 CONFIRMED (2 BLOCKER · 20 MAJOR · 20 MINOR · 4 INFO) · 6 KILLED (+2 corrections sustained) · 12 corpus-missed findings admitted (3 MAJOR · 7 MINOR · 2 INFO) · 5 reader-coverage reinstatements (rows in the corpus BOTH readers dropped: D2-05, D2-14, L-6, L-12, L-14) · 13 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

Blocker-weight survivors: **EAS-1** (the seat's central law binds to zero elements in the installed producer — the entire Law-3 apparatus is dead code) and **EAS-2** (the instrument's own advertised codomain destroys its route through an uncaught throw in a computed — re-derived analytically by this seat, no browser). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

## Arbiter rulings — where the readers were challenged back

1. **D3-07's ".glass-card count = 1" — reader-LC's partial refutation SUSTAINED over reader-DU's carry.** The producer mints `.glass-card` on TWO elements (canvas cell + readout block — both class strings read by me from dist); the count of one is a runtime consequence of `:readout="false"`, not a subtree property. The chrome-column contrast finding survives unchanged; the blanket `:deep(.glass-card)` at :93 is confirmed written against a prop-dependent census (a producer minor adding a card silently re-skins unaudited surfaces).
2. **"Eleven keypresses" vs "two keypresses" — NO CONTRADICTION, both arms mine-verified.** Handle 2 (y=1.0 seed): two Shift+ArrowUp → y2=1.2 → grid-32 escape 1.00316@t=0.9375 → crash. Handle 1 (y=0 seed): ten presses grid-SAFE, eleven (y1=1.1) → escape 1.00062@t=0.875 → crash. r3's title understated (reader-DU's correction sustained); r1/r2/C measured the sharper arm.
3. **Reader-LC's MISSED-2 (interval migration) is CONFIRMED but NOT NOVEL to the program.** `X/waves/W6.md` unit .a (adjudicated 2026-07-28) already books the exact witness — "a `steps()` authored on 50→100 re-attaches to 25→50 after a 25% insert" — and its cure law (normalise-on-write + stop-owned easing land TOGETHER; the `intervals` parallel array, its length watcher and three throws DELETE). The miss belongs to the six challenge axes, not to tranche X. Same ruling for MISSED-4 (stops drag past neighbours): X.W6.a's `setStopPosition` map→stable-sort mechanism is the specified cure; "clamping the sole mutator is BANNED" (GRADSTOP-A §14).
4. **Reader-coverage gaps — five in-corpus rows NEITHER reader returned, reinstated by this seat** (the incredulity seat audits the auditors): **D2-05** (temporal-rail accent, MAJOR — mechanism now STATIC-confirmed by me: the slider chunk contains zero `--easing-curve-accent` references, so the specimen-ink door provably stops at the curve; canon VISUAL-CONSTITUTION.md:7 "Easing uses a neutral temporal rail carrying the selected specimen color" read verbatim); **D2-14** (no visible identity line, MINOR — PROPORTION-AUDIT.md:66-67 card law 2; `label` is consumed solely as the canvas `aria-label`, my read of :76-82); **L-6** (three cross-repo easing→SVG samplers, MAJOR, library-owned cure); **L-12** (`EasingConfigurator` exists and was forked past, MINOR — BOUNDED by my d.ts read: the configurator exposes no `readout`/`playback` doors either, so adopting it retires nothing today); **L-14** (easingCatalogue.ts gradient-agnostic, mis-homed, MINOR).
5. **Disclosure-count precision (against r2 §1.1 and reader-DU's missed-item prose).** `openInterval = ref<number|null>(0)` (GradientEasingEditor.vue:61) — row 0's interval region is OPEN at boot (o17's own comment agrees: "The bench mounts with row 0 open"). The stage sits behind ONE closed disclosure on row 0, two on every other row. The capture-blindness verdict is unaffected (`tuneOpen` defaults `{}` = closed), but "two disclosures deep" overstates the default row by one.
6. **This seat's own producer find**: EasingPicker.vue.d.ts declares a `footer` slot (`{ value: EasingPickerValue }`). No reader recorded it. It cannot express any of the three seat laws (no fit/surface/columns), so C-9/L-2's "no exit" verdict stands — but it is the ONE existing extension point and belongs in the BH relay ask as such.

## Adjudicated defect roster

Anchors are HEAD `9c779130`; re-resolve before any cure (D-19). Dispositions: **X-W1** re-gate · **X-W3** route closure · **X-W6** instruments (units .a gradient-model core / .d easing-instrument coherence — `EasingAuthoringStage.vue`, `GradientEasingEditor.vue`, `easingCatalogue.ts` are X.W6 `modify` files) · **X-W9** library apotheosis (4.1) · **X-W10** canonical design/motion · **GLASS-RELAY** = the standing glass-ui BH relay via X.W6.d's dated-ask mechanism (gate d2) · **FOLD(id)** = existing identity, anti-rename.

### BLOCKER

- **EAS-1 — Law 3 binds to zero elements** (D3-01/D2-01/D-1/C-2/L-2/N-1-geometry). Both the querySelector (:48-50) and the `:deep()` (:104) key on `svg[role="img"]`; the installed producer renders `role:"img"` **zero** times (my grep; roles = group/slider/status). `syncVbRatio` early-returns at :51 forever; `vbRatio` freezes at its `ref(1.2)` seed (:45); all five Law-3 declarations (:104-115), including the T-48 `transition: aspect-ratio` (:114), are inert. Drawn-plot constant statically derivable: block-size pinned at the `clamp(200px,38cqi,320px)` floor ÷ viewBox height 1.2 → a **166.67px** plot at every pane width; the steps regime diverges further (producer hardcodes viewBox `"-0.05 -0.1 1.1 1.2"`, true ratio 1.0909, vs the frozen 1.2). Had Law 3 bound: :105 `min(100%, 19rem)` → 304px. Subsumes D-10 (margin asymmetry), D-11 (inert container query — with Law 3 dead the producer's px/cqi clamp IS what pins the plot), C-4 (birth constant ≡ CSS fallback ≡ plausible value — silent failure by construction; the docblock's ":19-21 the drawn plot IS the element box" is false by ~243px). **Disposition: X-W6 (.d owns the file; the geometry cure rides the producer's already-computed `viewBox: ComputedRef` — see EAS-10's relay; L-2's cure premise CONFIRMED by my d.ts read).**
- **EAS-2 — the instrument's advertised codomain destroys its route** (D3-02/D2-02/D-2/C-1/L-1). Producer keyboard/sr-only advertises y ∈ [-0.6, 1.6]; consumer mix domain is [0,1]: `src/color/operations.ts:65/:90` return `err(color_progress_out_of_range)`; sinks convert Result→throw inside computeds — `useGradientInterpolation.ts:37`, `useGradientCSS.ts:199-208`, reached from `useSpecimenRows.ts:52-56` (`fn(0.5)` in a `computed`). My node runs against dist: crash set = exactly the three `back` presets (all three reachable from the seat's paired strip AND from this stage's own keyboard, both arms — ruling 2); **grid-accident sharpening CONFIRMED to the decimal** — (0,0,1,1.12) survives only because j/32 sampling steps over its overshoot, (0,0,1,1.15) dies; `stepsPerInterval` re-rolls with stop count (:180-183), so adding a stop re-rolls the catch set. Producer-side precedent for the cure: the handle's own machine-readable range is `aria-valuemin:0 / aria-valuemax:1` (my grep) — the keyboard handler violates the component's own published ARIA. INFO aggravator (L-1): `DEFAULT_BEZIER_PRESET = "ease-out-back"` — the producer's default is one of the three fatal curves. **Disposition: X-W6.a (owns the three throws + masking default deletion) with a GLASS-RELAY arm (the [0,1] keyboard clamp / honest sr-only) and an X-W9 rider (library totality band, W9.md:185 "no string reaches a public `./css`/`./easing` entry and produces a TypeError").**

### MAJOR

- **EAS-3 — the witness chain is dead end-to-end** (N-1/N-2/D3-10/C-5a,b,c). o17's `discloseAuthoring` fixture asserts visibility of `#easing-authoring-0 svg[role='img']` (:51-52, my read) — the SAME dead assumption as EAS-1 — so all three tests RED at the first shared assertion while :117/:189 click `[data-specimen='ease-out-back']`, the exact tile EAS-2 proves fatal: the oracle would have caught the BLOCKER it now masks. No workflow runs playwright (my grep: 0 files); vitest excludes e2e/ structurally; zero component-mount unit tests repo-wide (reader-LC's grep sustained). BLOCKER-masking severity noted; booked MAJOR as gate-truth. **Disposition: X-W1 (re-gate: CI, visual oracle, boot-smoke).**
- **EAS-4 — Law 2 does not flatten the material** (N-3/D2-06). The override enumerates four properties (:93-99) and cannot reach the producer's `.glass-card::before` specular layer (my read of material.css: `@layer components`, plus-lighter, rest-hairline 0.07 → hover 0.1 → active arms, inset hairline the seat's border ADDS to, two-stop mask — the D-06 red-card seam frame's mechanism). A non-interactive well retains an interactive glass response. **Disposition: X-W6.d, with the `surface="well"` door as GLASS-RELAY (see EAS-10); X-W10 material law cited.**
- **EAS-5 — the same override deletes the producer's focus channel** (N-4). `box-shadow: none` (:96, unlayered scoped author origin) beats the layered `.glass-card:has(:focus-visible)` token swap (my read of surfaces.css) on layer order AND specificity — card-level keyboard-focus signal erased at rest and under focus alike. The general failure mode: the producer signals state through a property the consumer zeroed. **Disposition: X-W6.d + X-W10 (focus law).**
- **EAS-6 — chrome outside the well; label out of type jurisdiction; 13px inset** (D3-07/D2-12, count corrected per ruling 1). Law 2 houses the canvas card and leaves the producer's chrome column (structurally a sibling of the card in the grid — static from dist) on the raw pane the parent itself floored with `bg-well` thirty lines above (:167-176 rationale comment read). "Preset" renders `text-mono-caption` (mono, uppercase — glass-ui's own CSS) against VISUAL-CONSTITUTION.md:75 (control labels = `text-small`, Jakarta). Ratios live-only (residue). **Disposition: X-W6.d; label register is GLASS-RELAY (producer's own mistake — see superlative 3).**
- **EAS-7 — two selection owners, one placeholder-locked, unrepairable consumer-side** (D3-05/D2-07/D-3/§S4-2). Static, from the bundle (my greps): `preset` consumed exactly once as `initialPreset: l.preset`; the ONLY prop watcher is `A(() => l.mode`; `setHandle` marks `CUSTOM_PRESET="custom"` ∉ `presetNames` → reka falls to the "Pick a curve" placeholder; the inbound model watcher never re-selects. Desync is structural. Against PR-06 (one selection owner). Reader-DU's correction carried: the combobox DOES hold a name when the pick originates in it — a lost round-trip, not an unimplemented control. **Disposition: X-W6.d consumer arm + GLASS-RELAY (preset round-trip is producer-only).**
- **EAS-8 — the mode trap** (D3-06). The seat passes no `mode` (:76-82, my read); the producer renders a hard chrome branch with no mode control in either regime; mode is writable only through the model = only from the sibling strip. Control inventory and keyboard model change identity underneath the user. **Disposition: X-W6.d + GLASS-RELAY (a mode door is producer surface).**
- **EAS-9 — 13.33px operable targets, Law 3 the cause** (D3-08/D-6/C-3). `r:"0.04"` × 166.67 = 13.33px visible/focusable target (SC 2.5.8 floor 24px); at the intended 304px → 24.32px, pass by 0.32px. C-3's caveat carried: pointer layer compensates (HIT_RADIUS 0.1/0.15 — my constants read) but the visible target and focus mark do not. **Disposition: X-W6 (falls out of EAS-1's cure) + GLASS-RELAY note on visible-target floor.**
- **EAS-10 — a per-instance override shim with no exit, declared temporary, shipped permanent** (D3-09/D-7/C-9/§V-6/L-2). Three `:deep()` into another package's private DOM (test id, class, dead role selector), three `!important`, retirement clause (":10-12/:102-103 the overrides retire at the adopt") — the adopt (glass 7.0.0) landed WITHOUT the seams. My d.ts read confirms the closed surface: `{mode?, preset?, steps?, term?, readout?, playback?, label?}` + `modelValue` — no `fit`/`surface`/`layout`; the composable ALREADY computes `viewBox: ComputedRef` (the number Law 3 scrapes from DOM), so the fit door is cheap producer work, and the `footer` slot (ruling 6) is the one existing extension point. Owner edicts 4/5 violated. **Disposition: X-W6.d's d2 ask mechanism = GLASS-RELAY, dated, carrying: `fit`/viewBox exposure, `surface="well"`, preset round-trip, mode door, [0,1] keyboard clamp + ARIA truth, label register, reference-frame/annotation marks, visible 24px handle, footer-slot census. Per the standing owner edict this relay is a FOND — blocking, not a nicety.**
- **EAS-11 — the canvas is the sole keyboard control and its ARIA is self-inconsistent** (D-5). Canon read verbatim (VISUAL-CONSTITUTION.md:128 named x/y fields; :125 Spectrum precedent "pointer canvas is not the sole keyboard control"). `aria-valuenow` = x only (Up/Down never move it) and `valuemax:1` contradicts the sr-only −0.6..1.6 (my grep); `aria-valuetext` does track both — wrong on one channel, not silent. **Disposition: GLASS-RELAY (producer ARIA + numeric fields) + X-W6.d seat arm.**
- **EAS-12 — the well recipe re-mint loses its contextual arms and its radius** (§S4-1/D2-11 — one identity + MISSED-5). `.console-well` is declared single-home (foundation.css:340-355, my read); the seat re-types background+border onto `.glass-card` and loses the `prefers-contrast: more` border bump (:743-750), the print arm (:822-828), AND `border-radius: var(--radius-panel)` — the stage's well rides the producer's `rounded-card` instead (MISSED-5, third arm, sustained). **Disposition: X-W6.d (X-W10 owns foundation.css's survivor recipe — cite, don't fork).**
- **EAS-13 — the temporal rail carries the wrong accent** (D2-05, REINSTATED per ruling 4). Steps-regime slider fill rides the global amber while the curve rides the specimen teal — two accents in one instrument, against VISUAL-CONSTITUTION.md:7 and PR-06. Mechanism static-certain (slider chunk consumes no `--easing-curve-accent`, my grep); the measured colours remain chromium-cell (residue). **Disposition: X-W6.d + GLASS-RELAY (thread the accent through the producer's slider at the root that already carries `--motion-accent`).**
- **EAS-14 — the copy-failure state is undesigned; the producer's failure door refused** (reader-DU missed #1, ADMITTED — every anchor mine-verified). `useClipboard` publishes 4-state `status` + `onCopyError` + discriminated `CopyResult` (my d.ts read); the parent consumes only the success arm (:94-103, :184-185), discards the returned result, renders no third arm. `no-api` reachable on any non-secure origin (LAN dev, `server.host: true`). PR-08 verbatim. **Disposition: X-W6.d (GradientEasingEditor.vue owned there).**
- **EAS-15 — authored curves silently migrate on stop insert/remove** (reader-LC MISSED-2, CONFIRMED; ruling 3). Tail-append/truncate reconciliation on `stops.length` alone (`useGradientModel.ts:88-100`, my read) vs positional `addStop` sort (:118); worked example holds; removal destroys authored intervals. **Disposition: X-W6.a — ALREADY BOOKED (gate a1's failing witness names this defect; adjudicated 2026-07-28). Nothing new owed; the row records the axes' miss.**
- **EAS-16 — dependency inverted: the persisted domain model is the widget payload** (L-3). `GradientInterval = EasingPickerValue` (:49) — required non-serialisable `fn`, no `linear()` arm, while value.js publishes `CssTimingFunction`. **Disposition: X-W6.a (`GradientStop` gains `easing`; the model re-shapes there) with the CssTimingFunction retyping as its direction.**
- **EAS-17 — four mints of the CSS timing literal; the library's own is an orphan** (L-4). `cubicBezierToString`: definition + subpath export + test-only references, zero shipping consumers (my grep). Byte-identity across a package boundary maintained by prose (easingCatalogue.ts:38-51) and load-bearing for identity (`:220` `t.css === interval.css`). **Disposition: X-W9 (the 4.1 export/consolidation cut) + X-W6.d consumer adoption.**
- **EAS-18 — the catalogue silently drops 6 of 30 presets** (L-5). My read: `buildFamilies` constructs tiles for ALL 30, then `FAMILY_ORDER` (:174, omits quart/quint) discards six at the final map — built then thrown away. 27 tiles. The 27-vs-30 strip question stays adjudicated to EasingSpecimenStrip's registry row (reader-DU's routing sustained); the whitelist defect is booked HERE. **Disposition: X-W6.d (easingCatalogue.ts owned there).**
- **EAS-19 — three cross-repo implementations of easing→SVG-path** (L-6, REINSTATED). demo `glyphPath` / glass-ui `bezierPathD`+`stepPathD` / keyframes `generateCurveSVGPath` — documented, deliberate, still three. Within-repo the single-painter law holds (superlative 9). **Disposition: X-W9 (`easingPathD` on `/easing` per W9's export band) + GLASS-RELAY adoption note.**
- **EAS-20 — `/css` and `/easing` disagree by type on a linear-easing stop; the bridge is orphaned AND unreachable** (N-6/L-11 + reader-LC MISSED-1, merged — one surface). `CssLinearStop` (input tuple) vs `LinearEasingStop` (input number); the 25-line inference bridge lives in a gradient composable — and is **statically dead**: `fn` is REQUIRED on the payload, every constructor supplies it (linearInterval :53-62, catalogue payloads :119-126/:145-153, updateInterval :134-140, gradientParse :295-297 — all mine-read), every call site passes a whole interval (:190, useSpecimenRows:52, GradientVisualizer:78), and `easingFnOf` short-circuits at :123. `resolvedEasingCache` never receives an entry. MISSED-1's cure caveat sustained: the transposition is a deletion plus a NEW library surface for an algorithm with zero live coverage. **Disposition: X-W9.**
- **EAS-21 — `/easing` exports no Result type; consumers hand-roll the unwrap** (N-5). My export census of `src/subpaths/easing.ts` vs `color.ts:9`/`css.ts` — three subpaths, three disclosure policies; duplicate `easingValue` adapters confirmed byte-similar (useGradientCSS.ts:71-77 / easingCatalogue.ts:98-104). **Disposition: X-W9.**
- **EAS-22 — the demo's import-boundary lint governs zero of its files** (L-10). My reads: `demo/@` does not exist; the :232-239 object's four `demo/@` globs dead; the :274+ composables object fully dead; the surviving `demo/color-picker/**` glob carries only a pattern aimed at a tree that no longer exists; `demo/workbenches/**` ungoverned; inv-K-1 (:204-218) is the one live, load-bearing rule. **Disposition: FOLD → the lint-glob-liveness identity minted at the ColorSpaceSelector adjudication (its L-5(a), born-RED `eslint --print-config` command) — ONE identity, anti-rename.**

### MINOR

- **EAS-23 — focus ink ≡ curve ink, supplied by the seat's own parent** (D3-11). `--easing-curve-accent ← --motion-accent` (:115) feeds both the curve stroke and the focus-visible handle stroke while `focus:outline-none` removes the UA ring; the parent's own `--focus-ring-shadow` register (:285-288) is not adopted. X-W10 focus law + GLASS-RELAY.
- **EAS-24 — the reference frame is invisible; annotations 8.33px, colliding with the resting handles** (D3-12/D2-04/D2-13/D-9, one identity). Producer marks read verbatim (`stroke-border`/40, `font-size:"0.05px"` × 166.67 = 8.33px; "1"@(0.95,0.12) and "0"@(0.05,0.95) = the linear handle rests). Ratios live-only. X-W6 geometry + GLASS-RELAY marks.
- **EAS-25 — no forced-colors design** (D-13). `.easing-authoring` absent from foundation.css's enumerated tier-1 roster (my awk: `.gradient-rail`/`.readout-rail` ARE rostered — the sibling 30 lines above). X-W10.
- **EAS-26 — ink certified on `resting`, painted on the well — now on THREE elements** (D2-10 + reader-DU missed #3's `.rail-btn--on`/`.rail-tick` arm). `useSafeAccentFn("resting")` (:40) feeds `--motion-accent` consumed inside the repainted well (:94) and on the `bg-well` rail (:176 vs :289-294). Latent, correctly labelled. X-W6.d.
- **EAS-27 — the tune disclosure's open-state is colour-only, in the mis-certified ink** (reader-DU missed #3, ADMITTED). `.rail-btn--on` colour delta is the entire visual channel (:190, :196 icon unchanged) while the sibling chevron rotates (:137-140); `aria-expanded` explicit, visual channel not. VISUAL-CONSTITUTION.md:83. X-W6.d.
- **EAS-28 — non-unique accessible name on the authoring trigger** (reader-DU missed #2, ADMITTED). `aria-label="Author a custom curve"` static inside the row `v-for` (:193) while every sibling is row-qualified (:156/:181/:210 — mine-read). Degrades with stop count. X-W6.d.
- **EAS-29 — rhythm inversion: producer `gap-4` (16px) governs the collapsed axis; parent gap 10px** (reader-DU missed #4, ADMITTED — Law 1 rewrites the track list and not the gap its collapse invalidated; parent :146 `gap-2.5`). X-W6.d.
- **EAS-30 — text-scaling immunity by construction** (reader-DU missed #5, ADMITTED). px/cqi/user-unit sizing only; the dead :105 `19rem` clamp was the sole font-relative quantity. Explains r3's "200% zoom: pass geometrically, fails proportionally" row. X-W6 + GLASS-RELAY.
- **EAS-31 — the literal-truth contract is inverted in code** (reader-LC MISSED-3, ADMITTED). ":66-67 the CSS literal is persisted truth; the callable only a live cache" — but :123 makes `fn` unconditionally authoritative for pixels while `css` remains authoritative for identity/readout/export; agreement rests on an unenforced 3-dp convention across a package boundary (my read of :48-51 + the producer's `+a.toFixed(3)`). X-W6.a.
- **EAS-32 — stops drag/nudge past neighbours; descending percentages serialize** (reader-LC MISSED-4, ADMITTED; ruling 3). Clamp-only `[0,100]` (:74-80, :172-180 mine-read), lone `.sort()` in `addStop`. The adjacent colorAtPosition-throw hypothesis stays REFUTED (endpoint guards :66-69). X-W6.a — ALREADY SPECIFIED (normalise-on-write).
- **EAS-33 — module-eval throw** (N-7). `SPECIMEN_FAMILIES` built at module scope (:198) through throwing adapters (:103/:137?) — reachability hypothesis, all 30 constructions ok today (my node run). X-W6.d.
- **EAS-34 — viewBox guard admits NaN/Infinity** (C-7) + **EAS-35 — `undefined` forwarded upward with a rAF scheduled for a discarded emission** (C-8/L-13; the undefined arm is a `useModel` type artifact, not live — seat-4's kill of reachability sustained) + **EAS-36 — two uncancelled rAF per authoring event** (C-6/D-14, honestly bounded, not a storm). All X-W6.d hygiene.
- **EAS-37 — `ref(1.2)` hardcodes producer arithmetic the producer does not export** (L-7). `VIEW_PAD` exists in constants.d.ts; no exports key reaches it. Dies with EAS-1's cure; the constants-export ask rides the GLASS-RELAY.
- **EAS-38 — tsconfig paths drift; "CLOSED 8-key set" false by one** (L-8). My reads: 8 hand entries / 7-key exports map / no `.` root; `/css` and `/value` omitted; latent (self-reference resolves). **FOLD → the W-HYGIENE config-parity identity minted at the ColorSpaceSelector adjudication (its L-14, born-RED generator-parity command) — ONE identity, anti-rename.**
- **EAS-39 — the standing visual-audit matrix has never photographed this component** (L-15/D2-§1.1/C-3 arm). Cure = one line of state-driving, not a probe change (seat-3's sharpening sustained; capture.mjs's selector already reaches `[role="slider"]`). Disclosure-count precision per ruling 5. **X-W1.**
- **EAS-40 — no visible identity line on the disclosed instrument** (D2-14, REINSTATED). Card law 2 (PROPORTION-AUDIT.md:66-67); `label` → aria only. X-W6.d.
- **EAS-41 — the seat forked past `EasingConfigurator`** (L-12, REINSTATED, BOUNDED per ruling 4 — adopting it retires nothing today; recorded for the relay/greenfield). NO-WAVE-OWNER (formation boundary) + GLASS-RELAY census. · **EAS-42 — easingCatalogue.ts mis-homed four levels inside the gradient visualizer** (L-14, REINSTATED). X-W6.d owns the file; the homing decision (glass gallery vs demo shared) is NO-WAVE-OWNER if .d declines.

### INFO

- **Signifier mismatch**: `SlidersHorizontal` promises the one control species the disclosed bezier surface lacks (reader-DU missed #6; slider exists only in the regime the user cannot reach from inside — EAS-8). X-W6.d.
- **Docblock written for an audit reader**: six unresolvable identifiers; the shim hazard stated as achievement; ":19-21" false in the present tense (reader-DU missed #7 + D3-03 residue: :112-113 credits the LOSING guard — animations.css — for a neutralisation glass-ui's a11y-overrides actually performs, more thoroughly than claimed). Any wave touching the file corrects the prose. X-W6.d.
- **DEFAULT_BEZIER_PRESET is a loaded gun** (L-1 aggravator, prospective) — rides EAS-2's relay arm.
- **ErrorBoundary blast-radius/legibility** (C-10) — cross-filed with the D2-15 residue to the ErrorBoundary seat; not this component's surface. NO-WAVE-OWNER here.

## Killed-claims register

1. **D3-03 (reduced-motion aspect-ratio relayout, MAJOR) — KILLED, both readers sustained by this seat's own quarantine read.** motion-quarantine.md §1 (sha-pinned, measured): Guard 2 (`*:not([data-allow-motion])`, glass-ui a11y-overrides) beats Guard 1 (`*`) and **rewrites the transition property list** to `opacity, color, background-color, border-color, box-shadow` — `aspect-ratio` is REMOVED from the set under `reduce`, not shortened. r3's probe measured only duration and was structurally blind to the rewrite. No quarantine FALSE row is carried (convention per M-01/M-17's property-rewrite precedent; no M-xx row licenses the claim). Residue: the INFO docblock-attribution row above.
2. **D3-13 "remount contradicts its own doc" — KILLED.** The `v-if` at :208 is a tautology for any rendered row (`useSpecimenRows.ts:47-51` `continue`s undefined intervals before emitting an index — my read). A dead nullability guard (KISS nit), not a violated aliveness law. r2's hypothesis-labelled handling of the same shape was the honest one.
3. **D2-03 (BLOCKER: "the ratified topology forbids this object by name") — KILLED as booked; residue RESCOPED.** My canon reads: the "no tiny nested Card" / "no local picker" clauses sit on the **Easing member's own row** (W27 close conditions for the `/easing` route body); the **Gradient row** one line above plans "stop/**easing**/code inspector 38.2%", mobile "…; easing/code/action" — an interval-scale easing surface inside /gradient is PLANNED, not forbidden. Arbiter's sharpening BOTH readers under-credited: §"Closed member routes" also says Easing "owns a separate route/chassis … rather than a nested P122 instrument", which gives r2 more textual support than reader-DU's kill acknowledged — but that sentence allocates the Easing MEMBER's chassis; it does not deletion-mark Gradient's planned inspector. Surviving residue, booked: (a) **the route-inventory gap** — `/easing` and `/about` ratified (VISUAL-CONSTITUTION.md:58) and absent from the 14-path router (my read) → **X-W3** (frontend route closure; W3.md:87-89 territory); (b) r2's transposition direction (authoring-at-scale lives at `/easing`; the gradient row hands context there) preserved as the terminal architecture for **X-W5/X-W6** — a direction, not a defect row.
4. **D3-04/D2-09/D-4 (protagonist 45% under the 19-22rem floor) — KILLED.** Same category error, and PR-09's own sentence is the disproof: inside /gradient the ratified protagonist is the meniscus at 61.8%; this canvas is REQUIRED to be the subordinate support. VISUAL-CONSTITUTION:210-211's band sits under "### Easing" (the unbuilt member). The real geometry defect is fully booked at EAS-1; enlarging a support to protagonist scale inside a 436px inspector would violate the governing row.
5. **D3-05's cure (`:preset="false"`) — KILLED.** `preset?: string` (my d.ts read) — a type error that, even as a valid string, is consumed once at setup, unwatched (my bundle greps). The defect stands (EAS-7); the prescription was never a consumer move. r1/r2's "glass-ui addendum that does not yet exist" framing was correct.
6. **D-8 (motion-family violation) + challenge-L's "T-48 morph DELETED, edict-6 violation" — KILLED twice on the cited bytes.** The three-family law governs Vue `<Transition>` class families (animations.css:56-60, my read; zero `<Transition>` in the subtree) and the roster is `vj-enter/vj-morph/vj-celebrate`, not "vj-move"; the preserve-animations edict bans REMOVING authored motion and the declaration is present — "declared but unmatched" is EAS-1's dead selector double-counted.
7. **Corrections sustained, not kills**: D3-07's `.glass-card` count (ruling 1); the 70→71 spec-census drift (recounted at 71 by me at this HEAD).

## Superlatives — provenance preserved

1. **The `--motion-accent` door** (both readers; mechanism re-read by me): consumer theming through a producer-published custom property, in the same file as the three wrong mechanisms — a discipline failure, not an ignorance failure.
2. **Subtractive accessibility as law**: `:readout="false"`/`:playback="false"` removes two `role="status" aria-live="polite"` nodes from a per-pointermove surface, reason written down (:23-27).
3. **The typography call the design system itself gets wrong**: :134-136 chooses `text-mono-small` because caption uppercases case-sensitive identifiers — while the producer's own "Preset" label makes exactly that mistake (EAS-6).
4. **Copy-confirmation correct under concurrency** (:96-98 gating a local index on the producer's live status; pending-set-synchronously) — its missing failure arm is EAS-14; the success path is genuinely well built.
5. **The head glyph is a truthful 16px specimen**: the interval's OWN curve from the live callable, `overflow: visible` so overshoot never lies, `vector-effect="non-scaling-stroke"` (:131-133, :255-266).
6. **Disclosure wiring textbook, twice, nested**: `aria-expanded`+`aria-controls` onto stable ids, `v-show` aliveness — three axes attacked the invariant and none broke it.
7. **`linearStops` (useGradientCSS.ts:80-104)**: the CSS Easing L2 missing-input-position algorithm, all four rules, correctly ordered — the best-written function in the cone, and per EAS-20 the most under-used: orphaned AND unreachable.
8. **`src/easing.ts` failure-explicit posture is real**: the BLOCKER exists precisely because a consumer threw the Results away.
9. **`glyphPath` — ONE painter, named provenance, honest sample-count rationale** (within-repo; the cross-repo triplication is EAS-19).
10. **`applyCSS` atomic swap** (useGradientModel.ts:158-168) — the counter-example to EAS-15 in the same file, with the killed failure named in the comment.
11. **Falsifiable docblocks**: the seat states three laws by number, mechanism and retirement condition — which is WHY three axes could kill Law 3 by reading. Prose this accountable survives the transposition (its present-tense falsehoods do not — INFO row).
12. **inv-K-1** (eslint.config.js:204-218): the one live, load-bearing boundary rule — the reason the library half of the topology is acyclic while the demo half (EAS-22) is ungoverned. Verified in the producer's own bundle imports (value.js → never the reverse).

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

1. **The tab-order transcript** (r1 `EASD-focus.mjs`): three stops from the trigger — handle 1, handle 2, then a textbox OUTSIDE the stage; the Preset combobox absent. If accurate, the stage's only labelled control is keyboard-unreachable — a **BLOCKER-class 2.1.1** sitting unremarked in r1's own evidence. Decidable in four Tab presses. **Highest-priority live probe.**
2. Every real-pixel contrast ratio (rounds disagree materially; r2's 40%-alpha grid figure internally implausible at three significant figures against its own 100%-alpha parent). Direction safe; no number carries into a repair spec without re-measurement.
3. Letterbox percentages (59.3/38.6/17.5/55.3) — mechanism and the 166.67px constant static-certain; percentages are functions of live pane width and `38cqi`.
4. D2-15 (+C-10): the ErrorBoundary statement that computes as painting but does not render — CONFIRMED-observation/UNRESOLVED-mechanism; filed to the ErrorBoundary seat.
5. D2-08: the +65.6px unanimated regime-flip lurch (single unreplicated measurement) — and the never-measured, larger disclosure-open lurch beside it.
6. §S4-2's user-visible "Pick a curve" persistence (mechanism static-certain).
7. §S4-3's two-SVG census (the chevron-lottery guard shape read directly; the census is corpus-measured).
8. C-6 rAF counts (361/s idle etc.); the double-schedule is read-confirmed.
9. D3-06's mode-trap transition table.
10. N-3/N-4 computed values (`::before` opacity/blend at rest-hover-active; AT-REST/AFTER-TAB token readings) — the producing rules byte-confirmed.
11. §S4-4 multi-instance cost figures.
12. The o17 3/3-RED playwright transcripts (locator/producer mismatch decisive statically; runs not repeated under the no-browser law).
13. EAS-13's two measured accent colours (mechanism static-certain per ruling 4).

## Closing verdict

**DEFECTIVE — ADJUDICATED.** A 117-line seat whose central law has never executed against the installed producer (EAS-1), wrapping an instrument whose advertised codomain kills its own route through a throw in a computed (EAS-2), behind a witness chain that is RED, unwired, and sharing the component's own dead assumption (EAS-3) — yet built by an author who demonstrably knows the right idiom (superlative 1) inside a producer that already computes the number the seat scrapes from its DOM (EAS-10). The terminal shape is subtraction, not repair: X-W6 owns the surface and already books the model core; the three seat laws become producer surface through the dated BH relay X.W6.d's d2 gate exists to force; X-W9 takes the library seams; X-W1 revives the witnesses; X-W3 closes the routes the canon ratified. 46 confirmed, 6 killed with written refutations, 12 axis-missed rows admitted, 5 reader-dropped rows reinstated, 13 live probes owed — item 1 of the residue may yet outrank everything but the two BLOCKERs. (46 = 2 BLOCKER + 20 MAJOR + 20 MINOR + 4 INFO.)

— end of apotheosis. The six challenge files, probes, frames and both re-reader returns remain immutable beside this record per E-1/E-3. Dispositions here are ADJUDICATED, not VERIFIED; no wave is stamped by this file.
