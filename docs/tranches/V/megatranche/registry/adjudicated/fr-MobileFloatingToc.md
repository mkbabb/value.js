claude-fable-5

# fr-MobileFloatingToc — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/paper/MobileFloatingToc.vue` (397 lines, read whole; every anchor below re-resolved against the working tree) + the PaperSearch family it mounts, its PaperView host, its PaperSidebar twin, and the glass-ui 4.0.0→7.0.0 disclosure chain.
**Substrate**: fourier HEAD `cd26c65` / branch `m/w1-bump-migration` (re-pinned by this seat); installed glass-ui **4.0.0**; producer glass-ui at `/Users/mkbabb/Programming/glass-ui` HEAD = **8.0.0**, tag `v7.0.0` read via `git show` for every uplift claim.
**Corpus**: 3 challenge files (D design 577 · L library 380 · C consumption 353) at `docs/tranches/V/megatranche/audit/fourier-components/MobileFloatingToc/` — ~67 accusation rows, 4 pairwise-overlapping BLOCKER claims, 16 superlative claims.
**Readers adjudicated**: reader-1 (design-and-usage hostile re-read) · reader-2 (library-and-code hostile re-read), returns via the orchestration channel; the challenge files sit immutable beside this record.
**Method (M-23/L-18)**: both readers were attacked before either was adopted. The decisive artifact for the corpus's biggest kill (the document tree) was **re-derived twice by this seat** — an awk pass over the tex AND a string-aware brace-walk over the shipped bundle — and it settles the readers' own three-way discrepancies (R-1). One reader superlative cell was caught republishing a non-reproducing receipt (R-4). One reader's UNPROVEN row was voided as-stated by the other reader's missed find (R-2). Every adopted kill was re-run or re-derived where it was load-bearing; adopted-not-rerun cells are enumerated in DISSENT-6.
**Arbiter's own instruments this session (all static + node against source, installed dists, git tags; zero browser probes — probe parsimony; product source read-only)**: full SFC read (397); tex census (`grep -c '^\\chapter{'` → **11**, `'^\\section{'` → **51**, `'^\\subsection'` → **35**, starred → **4**, math-bearing chapter/section titles → **0**, all four math titles at `\subsection` level — :1201/:1440/:1603/:2560); per-chapter section awk → **3/11/5/7/0/5/3/4/1/8/3**; subsections under Introduction → **0**; **bundle brace-walk over `web/dist/assets/PaperView-CS2UAWt8.js`** → 13 top-level entries in shipped order with `Bibliography` at **index 9** (Intro/Origin/LensI/Eigen/LensII/Interpreting/DFT/EigenApplied/Applications/**Bib**/ExtProofs/ImageRecon/WebCompanion), per-root level-1 descendants **0/3/11/5/7/0/5/3/4/0/1/8/3** = 50, sourceLevel census **12/51/35/0**, `number:""` ×4; `grep -c 'transition: all\|cubic-bezier' MobileFloatingToc.vue` → **2** (:245/:347 — comments); search-family `<style` census **1/0/0/0** (PaperSearch.vue only); PaperSearchModal root `<Teleport to="body">` :41-123 + `document.querySelector(".search-modal-results")` :32; PaperSearchDropdown **two** template roots (`<Transition>` :35 + backdrop `<div v-if="variant === 'floating' …">` :63-67); **23** `.search-modal-*` declarations inside PaperSearch.vue's sole scoped block; built CSS ships `.search-modal-overlay[data-v-a7d16b8c]` and `.paper-search-results[data-v-a7d16b8c]` (attribute-qualified — the death warrant); ladder.css read at :225-268 — `@supports (color: contrast-color(white))` :232, the `@container style(--glass-backdrop: light)` arm covering seven classes **including `.glass-resting`**, and the **unconditional** `:where(.glass-floating, .glass-overlay) { color/--foreground/--muted-foreground: contrast-color(var(--card)) }` :263-266; `grep -rn glass-backdrop web/src` → **0**; **independent WCAG recompute in node** (HSL & OKLCH→sRGB→relative luminance, my own math): full ink **5.209**, sub-70% mix **2.880**, search glyph 50% **2.041**, close glyph 60% **2.413**, collapse icon .45 **1.884**, divider **1.365**, ramp idx4 **4.439** / idx10 **4.404** / idx11 **3.643** (all FAIL 4.5), idx5 raw **3.552** → fourier override **4.709** PASS; tokens re-read at the pin (neutral-0/4/5, ramp :241-253, fourier `--section-color-5` overrides :121/:126, `--control-h-md` chain, coarse `--ui-scale` 1.5, `html{font-size:1.125rem}`); components.css **0 `@layer` / 0 `ui-glyph`** / `.whitespace-nowrap{white-space:nowrap}` emitted; button-BNDWhAZb.js extracted (base `btn-pill tap-squish focus-ring whitespace-nowrap …`, `size.default: "h-(--control-h-md) px-4 py-2 has-[>svg]:px-3"`, `size.icon: "h-(--control-h-md) w-(--control-h-md) p-0"`, ghost `bg-transparent …`, `as: {default:"button"}`, **no defineExpose**); surfaces.css `@layer components` :6 + `.btn-pill` ui-scale geometry + `border-radius: var(--radius-pill)`; **glass-ui `git show v7.0.0:src/components/button/styles.css`** → `min-block-size` **:7**, `justify-content: center` **:9**, `border-radius: var(--radius-pill)` **:13**, `text-align: center` **:19**; HEAD 8.0.0 `calc(var(--button-size)/2)` at **:69**; PaperView reads (`_scrollTo = navigateTo`; `usePaperSearch({sections, navigateTo})`; `mobileTocVisible = ref(true)` :146; IO threshold 0 → `mobileTocVisible = entry.isIntersecting`; sessionStorage restore inside onMounted/nextTick; `currentSection` resolves `treeIndex.get(activeRootId)` → **always a root**; `:322` passes `active-root-id` ONLY to MobileFloatingToc vs `:338-339` both ids to PaperSidebar; `.paper-progress-track` sticky `--z-overlay` pointer-events:none; `.teleport-overlay` fixed `--z-overlay`); scheme-motion z registry **10/20/30/35/50**; PaperSidebar (:74 double gesture; :104-116 third level; :132-148 `display:none` + ≥1024 unhide — CSS-only, no v-if); useScrollNavigation.ts:23-26 verbatim; usePaperSearch `:44-45` **`options.navigateTo(r.id); close();`** (navigate before close) + `:48-51` close body; searchHelpers per-segment escaping (:35/:49/:61/:64/:72 — my own grep); entry-sheet vs route-chunk stylesheet homes (`.floating-toc-item:focus-visible{…border-radius:inherit}` in `index-57FkGzlZ.css`; `.floating-toc-item[data-v-e92b0ff4]{…border-radius:.375rem}` in `PaperView-FJog9X2r.css`); dist staleness re-run (assets **Jun 12 18:13** vs installed glass-ui **Jun 17 21:48**; `control-h-md` **0** in the old build; `.rotate-180{rotate:180deg}` present); `@source "../*.js"` at glass dist/styles/index.css:222; registry identity sweep (FR-COB-24 · FR-EMT-5 in fr-EquationModeToggle · FR-AH-16 · FV-27 · fr-EquationPanel M-N14 · fr-AnimationControls K-18 · fr-AppHeader S-6 · fr-GalleryFeaturedCarousel FR-GFC-16 · fr-CollapsibleSection m-1/M-7 — all rows confirmed present).

## Scoreboard

**40 adjudicated register rows (1 BLOCKER · 17 MAJOR · 15 MINOR · 7 INFO) + 1 standalone FOLD BY REFERENCE and 5 in-row identity folds (never re-booked) · 14 kills in the killed register · 9 missed finds banked (2 convergent across both readers; every one re-proven or re-derived here) · 9 superlatives with provenance.**

Blocker-weight survivor: **B-1** — the PaperSearch family's stylesheet never reaches its own subtrees: Vue's scope hash stops at each child's root, two of the three children are multi-root or Teleport-rooted, none of the three has a `<style>` block of its own, so the mobile floating search renders with its layout stylesheet absent AND the expanded search modal — on **both** hosts — renders as an unstyled in-body block: 24 dead `.search-modal-*` rules, no overlay, no scrim, no clip, both transitions inert. Reader-2's modal extension is adopted over reader-1's narrower framing (R-2); the defect's home is the PaperSearch family, with this component as the sole floating-variant consumer.

The corpus's other three BLOCKERs all died or demoted on one fact the C axis alone got approximately right and both D and L got wrong: **the paper has 11 chapters and the ToC has 13 roots, not 51** — re-derived twice by this seat.

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **The true tree** (root order, childless set, per-root counts) | Counts exact (7 under LensII; sum 50; "Lens I has 11" ✓) but childless roots named without positions | Positions exact (Bibliography at si=9; childless si 0/5/9) but LensII cell **8** (true 7) and "the 51 `\section`s are the sub rows" (true **50** — Introduction is promoted, not a sub) | **Merged truth published here, from this seat's double derivation**: 13 roots in shipped order Intro/Origin/LensI/Eigen/LensII/Interpreting/DFT/EigenApplied/Applications/**Bib(9)**/ExtProofs(10)/ImageRecon(11)/WebCompanion(12); per-root subs 0/3/11/5/**7**/0/5/3/4/0/1/8/3 = **50**; childless = si 0/5/9; level-2 nodes **35**, none rendered | awk over the tex + brace-walk over `PaperView-CS2UAWt8.js` (instruments above). Decisive nuance BOTH readers used without flagging: **Bibliography sits mid-list at index 9, not last** — which is the only reason both readers' C-6 index names (idx10 = Extended Proofs, idx11 = Image Reconstruction) are correct. This seat's own initial document-order presumption (Bib last) was **wrong against the artifact** — recorded in DISSENT-2 as the method lesson: the shipped bundle, not source order, is the census. |
| R-2 | **The search-family styling** (is the teleported modal styled?) | Confirmed D-B2 for input/dropdown; left "which of the two overlays stacks on top" UNPROVEN — implicitly treating the modal as styled | Missed-find: the ENTIRE `.search-modal-*` family (24 rules, ~160 lines) is dead by the same scope mechanism; C-3's twice-composited-scrim leg killed | **Reader-2 wins — adopted at BLOCKER scope (B-1) after this seat's own re-derivation.** Reader-1's UNPROVEN item is **voided as stated**: there are no styled overlays to stack — both teleported subtrees render as unstyled in-flow blocks at the end of `<body>`; the surviving live question is document order for the `querySelector` cross-talk (SS-13 #5, reshaped) | My census: `<style` count 1/0/0/0 across the family; modal root = `<Teleport to="body">` (:41); 23 `.search-modal-*` declarations all inside PaperSearch.vue's single scoped block; built CSS carries them **attribute-qualified** (`.search-modal-overlay[data-v-a7d16b8c]`) — an attribute no element inside the un-scoped, teleported modal can carry. Class identity: fr-AnimationControls K-18 + fr-AppHeader S-6 (teleport-CSS split) — this is a new fourier instance of the banked class, and it decides C-3's scrim leg (K-12). |
| R-3 | **D-B4's grade** (sub-row 2.88:1 — BLOCKER?) | K-11: engine-conditional — the unconditional `:where(.glass-floating,.glass-overlay)` contrast-color re-point cures the ink on capable engines; MAJOR | Confirmed MAJOR with the challenge's own substrate caveat; never engaged the re-point mechanism | **Reader-1's K-11 adopted — MAJOR, engine-conditional (M-4).** | My own ladder.css :232-266 read: the `@supports (color: contrast-color(white))` block carries BOTH arms — the `@container style(--glass-backdrop: light)` arm (seven classes incl. `.glass-resting`) which is **dead in fourier** (`grep glass-backdrop web/src` → 0), and the **unconditional** `:where(.glass-floating, .glass-overlay)` arm which re-points `--muted-foreground` inside the dropdown. Consequence split verified: the dropdown inks flip on capable engines; the two **bar** glyphs sit on `.glass-resting` and are cured by **neither** arm — M-5 holds on every engine. Reader-2's cell repeated the challenge's caveat without the operative mechanism — inheritance-shaped, DISSENT-5. |
| R-4 | **The S-6 grep receipt** (`grep -c 'transition: all\|cubic-bezier'` → 0 or 2?) | Republished the corpus's **0** inside its superlative cell | Measured **2** (:245/:347 — the A.W3.d comments contain the string) and killed the published receipt | **Reader-2 wins — my own run returns 2.** The substantive claim survives (no `transition: all` *declaration*, no raw `cubic-bezier()`; :287's keyword `ease` is the recorded blemish) — superlative S-6 stands with a corrected receipt. Reader-1's cell is a **citation-inheritance event inside a superlative** — the fr-EasingPicker pattern, recorded in DISSENT-1 | `grep -c 'transition: all\|cubic-bezier' web/src/components/paper/MobileFloatingToc.vue` → **2**, both hits comment text whose own words are "no `transition: all`". A receipt that self-matches its prohibition's comment is exactly why confirmation cells need the same discipline as accusations. |
| R-5 | **L-13's fate** (the `triggerEl()` shim: defect or idiom?) | Confirmed MINOR (three ref idioms, dead `instanceof` branch, redundant cast) | Refuted-by-banked-record: fr-CollapsibleSection m-1 names `MobileFloatingToc:27-33` as *the in-tree conformant idiom* | **Demoted to INFO (i-1) — both halves kept.** The banked crown (verified in the registry by this seat) defeats defect-framing of the shim itself; the intra-file inconsistency (three ref idioms in 104 lines) and the type-honesty nit (the `instanceof` arm dead by construction — my dist read: no `defineExpose`, single reka `Primitive` root) survive as an observation, not a defect | fr-CollapsibleSection.md:58 read directly; button dist read directly. S-4 (the idiom superlative) stands with the fleet provenance. |
| R-6 | **L-7's grade** (two `useSidebarState` stores) | MAJOR (permanently divergent, both mounted at every viewport — premise riding L-9's unproven leg) | Not booked at all | **MINOR (m-4), with reader-1's MAJOR preserved as dissent — and a sharpening neither reader stated**: the mobile store is not "permanently divergent", it is **EPHEMERAL** — PaperView mounts/unmounts this component on every scroll past the inline nav (`v-if="!mobileTocVisible"`), so each remount constructs fresh Sets and the mobile expansion state resets per mount cycle, mostly masked by the active-root default-expansion rule. The live divergence window is one mount cycle + the breakpoint crossing; the architectural duplication (PaperView already owns `treeIndex` and drills it to the sidebar) is the real row | Producer allocates per call (dist/sidebar.js, Sets constructed inside the composable body — adopted, dual-derived); mount gating verified in my PaperView read. |
| R-7 | **Missed-find severities** | coverage-gap MINOR · search-bar-height MAJOR | coverage-gap MAJOR · scroll-lock MAJOR-latent | **Coverage gap = MAJOR (M-2)**: it is the true residual of the corpus's twin killed BLOCKERs and it makes their prescribed cure a no-op. **Search-bar height = MINOR (m-10)**: mechanism real (my derivation: ~+22.5px at the 18px root), but S-1's measuring contract self-corrects and `usePaperSearch:44-45` navigates BEFORE `close()`, so the offset is measured while the tall bar is still the truth — the residue is a post-swap ~22px gap and a visible mode jump. **Scroll-lock = MINOR (m-7)** with reader-2's MAJOR-latent dissent preserved: `grep -rn 'style.overflow' web/src` → 2 lines, no second writer today | usePaperSearch ordering read this session; bar arithmetic derived from the icon-arm extraction (`h/w-(--control-h-md)` = 67.5px square; scoped `.floating-toc-search-close` declares padding only, no height) + bar padding 0.625rem×2 = 22.5px. |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · terminal disposition → route**. Full proof cells live in the challenge files and reader returns, incorporated by reference; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **B-1 = D-B2 ∘ K-10 rescope ∘ reader-2 missed-2 — BLOCKER.** The PaperSearch family's one scoped stylesheet (PaperSearch.vue:41, the family's ONLY `<style>` — my census) never reaches: (a) PaperSearchInput's **children** (single root inherits the hash; `.paper-search-icon`/`-input`/`-action-btn` do not); (b) PaperSearchDropdown's **entire subtree** (two template roots — `<Transition>` :35 AND the floating backdrop `<div v-if>` :63-67, my read — a real Fragment, no single-root rescue); (c) the **entire teleported modal** (root = `<Teleport to="body">` :41; teleported children are never the subTree; PaperSearchModal has no `__scopeId` of its own). Net: the mobile floating search renders its results list, backdrop, and input layout unstyled — and the expanded modal renders as an unstyled in-body block **on both hosts**: no fixed overlay, no scrim, no `max-height`/`overflow` on `.search-modal-results` (so the modal's own `scrollIntoView` at :31-34 has no scrollable ancestor), and both enter/leave transitions inert. Built CSS confirms every rule shipped attribute-qualified. Scope correction ratified (K-10): the defect is family-wide — both the `sidebar` and `floating` variants are equally unstyled; only the two `--floating` override rules are variant-specific and they are dead for the same reason. This component's stake: sole consumer of `variant="floating"` (:111) and the surface where the loss is a primary mobile mode. Class identity: fr-AnimationControls K-18 + fr-AppHeader S-6 (teleport/scope CSS split) — new instance, decides K-12. **ADJUDICATED → home = the PaperSearch family row (forward pointer for the PaperSearch adjudication); route F.W3/W4** (the shadow-retirement decision: local `<style>` blocks per child = the KISS repair, vs folding the 832-LOC family onto glass 7.0.0's `./search` — which is also D §6 U-10's F.W1 decision input). Painted result → SS-13 #5.

### FOLDED BY REFERENCE (identity guard — banked rows, never re-booked)

- **F-1 = reader-2 missed-6 (the stale-dist evidence base) + D-i2's fresh-build caveat = fr-CollapsibleSection M-7 (banked: the stale `web/dist` artifact + THE EMISSION HINGE; → F.W1 pre-gate)** — one identity, re-verified by this seat's own re-run: dist assets `Jun 12 18:13` vs installed glass-ui `Jun 17 21:48`; `control-h-md` **0** in the old build vs shipped in the installed `components.css`; the L challenge's §0 claim that the dist "settles L-3, L-4, L-15" is therefore an unproven-as-published chain (the three conclusions survive because this seat re-derived each against the installed 4.0.0 directly). Riders this record contributes: (i) the `@source "../*.js"` directive verified at dist/styles/index.css:222 — reader-2's caveat that a **fresh** build might emit the `[&_svg]` legs D-i2 reports absent is the same hinge, one component over; (ii) the old build DOES already carry `.rotate-180{rotate:180deg}` and the post-rename `glass-resting` classes, so the cells that only needed those survive.

In-row identity folds, named where they land: **FR-COB-24** (M-8), **FR-EMT-5** (M-6), **FV-27** (M-16), **fr-EquationPanel M-N14** (i-7), **fr-CollapsibleSection m-1** (i-1/S-4).

### MAJOR

- **M-1 = D-B1 = L-2 = C-2 — MAJOR (demoted from 2× BLOCKER by K-1/K-2's corrected magnitude).** Root rows bind only `sidebarState.toggleSection(section.id)` (:155) and never navigate; the desktop twin does both on one gesture (PaperSidebar.vue:74 — my read). The producer's toggle is pure Set mutation and its `navigateTo` member is wired (:71) and never called. Corrected magnitude: **13 roots** are unreachable as targets (not 51), of which **3** (si 0/5/9 — Introduction, Interpreting The Results, Bibliography) render no chevron (:159) so the tap is a total no-op; the **50** level-1 sections ARE reachable via `selectSection` (:171). Still a one-line differential against an in-tree correct sibling, on mobile's only mid-document nav surface. → **F.W3/W4**.
- **M-2 = convergent missed find (both readers, re-proven here) — MAJOR (R-7).** The mobile ToC renders exactly two levels (:149 roots, :167 direct children) and stops: all **35** level-2 `\subsection` nodes — including the four math-bearing and four starred titles the corpus built its BLOCKERs on — have **no entry on any mobile navigation surface** (sidebar renders three levels, :104-116; the inline nav renders roots only). 35 of 99 heading nodes (35.4%) unreachable below `lg` except through search. This is the true form of the defect all three axes garbled into "it prints raw LaTeX" — and it makes the corpus's prescribed cure (`v-html="renderTitle(...)"`) a **no-op**, since no rendered title contains `$` (my grep: zero math at levels 0-1). Also the structural reason `activeId` was faked (:64-66). → **F.W3/W4** (design decision: third level, or explicit scoping with the depth-2 choice documented).
- **M-3 = D-B3 = L-5 = C-12 — MAJOR.** The search affordance is a bare `<span @click.stop title="Search paper">` (:123) nested inside the trigger `<button>` (glass Button `as: {default:"button"}` — my dist read): no role, no tabindex, no accessible name (lucide glyph is `aria-hidden`; `grep 'aria-\|role='` over the SFC → 0). Keyboard/AT users cannot reach mobile search from this surface. C-12's withdrawal mechanism corrected (ratified from both readers + my `ui-glyph` grep → 0): the `[&_svg]:pointer-events-none` leg is never emitted at all. Arbiter's addition: PaperView's global **Cmd/Ctrl+K** (`handleGlobalKeydown` — my read) opens the *shared* state but not the mobile UI (`searchActive` stays false), so the only keyboard path to search on mobile opens an invisible search — cross-feeds M-17's desync family. → **F.W3/W4** (promote the span to a sibling `<Button size="icon" aria-label>`; one element).
- **M-4 = D-B4 ∘ K-11 rescope — MAJOR (engine-conditional; BLOCKER grade killed).** `.floating-toc-sub` and `.floating-toc-top` at `color-mix(--muted-foreground 70%, transparent)` = **2.880:1** (my recompute) at 14.6px/13.5px — FAIL 4.5:1 — while the un-mixed token alone is 5.209:1 (the token's own comment says 5.21). BUT the rows live inside `.glass-floating`, and the **unconditional** `:where(.glass-floating,.glass-overlay)` arm re-points `--muted-foreground` to `contrast-color(var(--card))` on capable engines (my ladder read) — iOS Safari 26+, i.e. the component's primary device class. So: FAIL on non-supporting engines, cured on supporting ones. D's falsifier list never contained the operative falsifier. → **F.W3/W4** (drop the mixes; the cure is engine-independent). Engine-arm witness → SS-13 #2.
- **M-5 = D-M6 — MAJOR.** Non-text contrast, my recomputes: search glyph **2.041:1**, close glyph **2.413:1** against the 3:1 floor — these sit on `.glass-resting`, which is cured by **neither** contrast-color arm in fourier (the conditional arm needs `--glass-backdrop`, declared nowhere — my grep), so they hold on every engine. The collapse chevron at **1.884:1** — the only signal distinguishing a working row from M-1's no-op rows — sits inside the floating plate and shares M-4's engine conditionality. The control row reproduces: `.floating-toc-chevron` (opacity .6 on full `--foreground`) passes — the only glyph that skips the muted-mix idiom is the one that passes. → **F.W3/W4**.
- **M-6 = C-6 (arithmetic ratified) — MAJOR.** The section-colour ramp painted as text (:154, 18px semibold ⇒ 4.5:1 floor) fails light-mode AA at **three live indices** — my independent recompute: idx4 **4.439** (Lens II: Complex Analysis), idx10 **4.404** (Extended Proofs), idx11 **3.643** (Image Reconstruction Pipeline) — index names correct under the shipped order **because Bibliography sits at si=9** (R-1). Fourier's D.W4.d remediation rebased exactly one of thirteen stops (idx5: 3.552 → 4.709 PASS — my recompute). Fleet limb (the carry is calibrated against `--background`, mis-based for tinted plates) **folds to FR-EMT-5** (fr-EquationModeToggle); the twelve-unaudited-stops instance and this component's text consumption are booked here. Composited plate ratios → SS-13 #6. → **F.W3/W4** + the FR-EMT-5 carry.
- **M-7 = D-M1 = C-4 — MAJOR.** The 100%-wide sticky bar renders at `--radius-pill` (9999px → ~34px end radii at the 67.5px height); nothing local declares a radius; the square-cornered dropdown hangs beneath. `.btn-pill` geometry read verbatim at the pin. **K-8 rider**: the D §6 uplift cell citing 7.0.0's `calc(var(--button-size)/2)` is an 8.0.0 fact (my `git show`: v7 :13 is `var(--radius-pill)`, byte-same idea as 4.0.0; the calc form is HEAD :69) — the verdict "UNCHANGED at 7" survives, the citation does not. → **F.W3/W4**.
- **M-8 = D-M2 (mechanism) ∘ K-6 (arithmetic corrected) — MAJOR.** Every row height-locked to `--control-h-md` = **67.5px** on a coarse pointer (2.5rem × 1.5 × 18px root — full chain re-read at the pin; the utility unlayered in components.css, my `@layer` grep → 0) against an authored ~45px. Mechanism identity **folds to FR-COB-24** (fr-CanvasOverlayButton — the 18px-root × coarse-1.5 identity); this is an instance with a local repair. Corrected aggregate (this seat's own): collapsed = 14 rows ≈ **970px**, ~52% visible in a 60vh/844px window — not D's 3.4k/14.7%; worst case (Lens I active, 11 subs auto-expanded) = 25 rows ≈ 1.7k, ~30% visible. Uplift note verified at the tag: v7 `min-block-size` **:7** is a floor, so authored padding composes (U-3 IMPROVES — K-9's corrected anchor). → **F.W3/W4**.
- **M-9 = D-M3 — MAJOR.** Root rows and "Scroll to top" are centre-justified (`.btn-pill` `justify-center` in `@layer components`, unopposed — the flex re-declarations at :351-355/:315-322 name no `justify-content`) while sub rows are left-aligned via `display:block` — no reading edge in a scan-down list. K-9 rider: at v7 `text-align: center` is declared outright (**:19** at the tag, my read), so the local `text-align: left` loses on the block rows too — U-5's verdict survives on corrected anchors. → **F.W3/W4** (one repair unit with M-8: stop rendering list rows as `<Button>`).
- **M-10 = D-M5 = L-8 = C-15 (+ L-16 aria leg + L-14 latent guard as riders) — MAJOR.** Focus restored on Esc/backdrop only; `selectSection`, `handleScrollToTop`, and `closeMobileSearch` unmount the focused element bare → focus to `<body>`. C-15's precision adopted over the readers' broader phrasing: `:117` needs no restore, `openMobileSearch` correctly hands focus to the input (:90); three paths drop, not "every path". Zero `aria-*`/`role` in 397 lines (my grep); no `aria-expanded`/`aria-controls`; no landmark where the twin has `<nav aria-label>` (PaperSidebar:50); no containment. L-14's `v-else`-removed trigger is the latent same-class guard. → **F.W3/W4**.
- **M-11 = D-M8 — MAJOR.** The chevron never animates: `:287` transitions `transform` while Tailwind v4 rotates via the independent `rotate` property — the app's own shipped sheet carries `.rotate-180{rotate:180deg}` (my grep of the built CSS); `transform` never changes; `opacity` (0.6→0.8) changes and is absent from the transition list. Both legs snap. → **F.W3/W4** (rides the m-12 motion-token blemish).
- **M-12 = D-M9 — MAJOR.** Both adopted glass rungs partly destroyed by the unlayered-beats-layered inversion (components.css 0 `@layer` — my grep — vs the ladder/btn-pill in `@layer components`): the ghost arm's `.bg-transparent{background-color:#0000}` beats `.glass-resting`'s plate, the scoped `border: none` (:212) removes its edge — a blur with no plate; the scoped `box-shadow: 0 4px 16px rgba(0,0,0,.1)` (:300) replaces `.glass-floating`'s three-layer material stack including the rim. Rendered appearance → SS-13 #10. → **F.W3/W4**.
- **M-13 = D-M10 — MAJOR.** "Clear search" exits search mode entirely: `search.close()` (usePaperSearch:48-51, my read — clears `isOpen`/`isExpanded`/`query`) trips this component's `:99-103` watcher whose only guard is `searchActive` — so the input's clear-X and the bar's close-X, adjacent and `title`-labelled only, both exit the mode; they differ only in whether the query survives. Mechanism spans usePaperSearch + this watcher — cross-noted for the PaperSearch adjudication. → **F.W3/W4**.
- **M-14 = D-M11 — MAJOR.** The floating variant has no empty state: PaperSearchDropdown's whole surface gated on `results.length > 0` with no `v-else` (my read of the gate) while the modal has one; the Expand escape hatch (`canExpand = !!query && results.length > 0`, PaperSearch:28 — my read) is hidden by the same predicate that hides the list. A no-match query shows nothing. Home: the PaperSearch family (pointer), stake here as the sole floating consumer. → **F.W3/W4**.
- **M-15 = L-4 = C-5 — MAJOR.** Dropdown rows inherit `whitespace-nowrap` from the Button base (emitted in the installed components.css — my grep — and in the app's built sheet) with no `white-space`/`overflow`/`text-overflow` in the scoped rule, and `touch-action: pan-y` (:305) forbids the horizontal recovery pan. The 63-char exhibit (`Complex Analysis of Orthogonal Polynomials and Green's Functions`, tex:2187) survives the K-1 tree correction — it is a level-1 section under Lens II, i.e. a rendered sub row with a further 2.25rem indent. The bar label one element away has the full clipping idiom (:223-229). Visible clip → SS-13 #3. → **F.W3/W4**.
- **M-16 = L-6 = C-3 (duplication leg; scrim leg K-12; census leg folds to FV-27) — MAJOR.** Two PaperSearch hosts over one shared state (PaperSidebar mounted at ALL viewports — CSS-only hide, :132-148, my read; this component's mount IO-driven) teleport **two** PaperSearchModal subtrees into `<body>`; the modal resolves its scroll target with a **document-global** `querySelector(".search-modal-results")` (:32, my read) against the first match in document order, while the sibling dropdown correctly scopes to `resultsRef`. Reshaped by B-1: the duplicated subtrees are unstyled blocks, not stacked overlays — the cross-talk question is now "which unstyled block is first", still live for the keyboard-selection scroll. Teleport site-vs-instance census identity **folds to FV-27** (fr-FullscreenViewer: 2 sites tree-wide); the mounted-instance count (2 for one site) and L-6's R3-11 refinement (count per mounted host) are this record's riders to that row. → **F.W3/W4**; the counting-method rider → **NO-WAVE-OWNER** (SS-3/SS-4).
- **M-17 = C-8 — MAJOR.** Teardown asymmetry: unmount restores `overflow` only; the shared search state this component opened stays open. The remount watcher (:99-103) has **no `immediate`** (my file read), so a remount leaves `searchActive === false` against shared `isOpen === true` — mid-search scroll-up destroys the component (IO-driven `v-if`) and strands the state. Arbiter's addition: **Cmd/Ctrl+K on a mobile viewport is a third desync trigger** (opens shared state with no mobile UI — M-3's rider). → **F.W3/W4** (one state-ownership repair with M-13).

### MINOR

- **m-1 = D-M7 = L-1 = C-1 (dead-prop residue of K-4) — MINOR.** `renderTitle` is a required prop, passed (PaperView:326), never called — `grep -n renderTitle` → line 16 only — against three sibling call sites that all invoke it. The rendering-consequence legs of two BLOCKERs died (K-4: every rendered title is prose; the bar label is always a root); what survives is dead declared surface the parent must thread forever + a four-way divergence. → **F.W3/W4**.
- **m-2 = D-M4 — MINOR.** The dropdown always opens at scrollTop 0; no active-item follow; PaperView already imports and drives `useSidebarFollow` for the desktop arm (my read). Corrected magnitude: worst case (active root last) the active row sits ≈890px from the dropdown top ≈ 380px below a 506px fold — not 2 700px. → **F.W3/W4**.
- **m-3 = reader-2 missed-3 (re-proven) — MINOR.** PaperView never passes `activeId` to this component (`:322` `active-root-id` only vs `:338-339` both to the sidebar — my read), and `currentSection` resolves through `treeIndex.get(activeRootId)` — **always a root** (my read). The sticky bar labels the reader's chapter, never the section: 13 distinct readouts across 99 headings. Root cause of C-16's missing sub-tint and of the `:64-66` `activeId` stand-in that C-9/L-12 book as a symptom. → **F.W3/W4**.
- **m-4 = L-7 (R-6) — MINOR (reader-1's MAJOR preserved as dissent).** Two independent `useSidebarState` stores over one tree — and the mobile one is per-mount **ephemeral** (this seat's sharpening, R-6). PaperView already owns the tree index; the cure is one hoisted state as with `search`. → **F.W3/W4**.
- **m-5 = C-9 = L-12 — MINOR.** Over-consumption: six options in, two members out (`toggleSection`, `isExpanded` — my grep of `sidebarState.`); `navigateTo`/`scrollToTop` dead-wired (:71-72 vs :78/:83); `activeId` knowingly wrong making `isActive`/`isInActiveChain` wrong-by-construction (inert only because unused); a full tree walk built and discarded per mount (compounded by m-4's remount cycle). → **F.W3/W4**.
- **m-6 = L-9 — MINOR.** `useScrollNavigation.ts:23-26` (read verbatim) queries `document.querySelector(".floating-toc-bar")` — a class declared only in this component's scoped block; no contract, no data attribute, no rename guard. The 8px-vs-16px desktop consequence rides the unproven desktop-mount premise → SS-13 #1 (the corpus's cheapest probe). → **F.W3/W4**.
- **m-7 = C-7 = L-11 = D-m10 — MINOR (reader-2's MAJOR-latent dissent preserved).** The scroll lock writes a parent-owned node's inline style keyed on `floatingTocOpen` (not on the prop identity PaperView itself watches for — my read of :276's guard), restores by unconditional erase, and `onUnmounted` erases even when it never locked (and runs constantly under the IO mount cycle). Latent: `grep 'style.overflow' web/src` → these two lines only. The unlock-on-same-tick leg pairs with m-11. → **F.W3/W4**.
- **m-8 = L-15 — MINOR.** glass Button used as an unstyled `<button>` shim: twelve declarations re-lay it out from scoped CSS, opting out of the `--ui-scale` comfort axis (`.btn-pill`'s calc geometry read verbatim) — the `feedback_glass_ui_first_class`/`feedback_root_styling` line, and the same override pass that resets everything EXCEPT `white-space` (M-15). → **F.W3/W4** (resolved by M-8/M-9's stop-rendering-rows-as-Button repair).
- **m-9 = reader-1 missed-1 (re-proven by construction) — MINOR.** Sub rows keep `display:block` under the 67.5px height lock: content box 49.5px carrying one 27px line box at the TOP — 22.5px dead space under every sub label, while root rows re-declare flex centring. Two vertical rhythms in one list; the vertical composition of M-8+M-9, booked so it repairs with them. → **F.W3/W4**.
- **m-10 = reader-1 missed-2 (R-7 demotion) — MINOR.** Entering search mode changes the bar's height ≈ +22.5px (normal mode: the Button itself, locked 67.5px; search mode: content-driven around a 67.5px `size="icon"` square — arm extracted from dist — plus 0.625rem×2 padding), and both modes carry the class `useScrollNavigation` measures. Mechanism real; consequence transient (navigate-before-close ordering verified; S-1's contract self-corrects). New instance of the **FR-AH-16** class (bar literals vs pointer-conditional control heights) — noted, not re-booked. Exact rendered heights → SS-13 #8. → **F.W3/W4**.
- **m-11 = convergent missed-4 (both readers) — MINOR.** The dismissal backdrop is a sibling of the `<Transition>`, not inside it (:129-179 vs :182-186 — my read): on every dismissal it unmounts on the same tick while the dropdown fades 200ms — a window with no hit-test layer and (per m-7's leg) no scroll lock under a still-painted menu. This seat's sharpening: the backdrop is **invisible** (no background declared), so the loss is purely the hit-test layer — outside-taps pass to the article during the fade. One-element move. → **F.W3/W4**.
- **m-12 = D-m1/m2/m3/m4/m8/m9/m11/m12 (batch) — MINOR.** Off-ladder radii (6.75px/4.5px at the 18px root vs the px ladder); three left edges in one list; dead `-webkit-overflow-scrolling`; two unconverted motion sites beside two A.W3.d-annotated ones (:287 raw `ease`, :300 raw rgba); `title`-only labels on a touch surface; the divider at **1.365:1** (my recompute); no `type="button"` at any of five call sites (v7 defaults it — U-8 IMPROVES, anchor-corrected per K-9); the bar at `--z-controls` (20) while `--z-bar` (30) sits unused and two `--z-overlay` (50) siblings paint above it — z registry and both PaperView rules read by this seat; `pointer-events:none` keeps it cosmetic. → **F.W3/W4**.
- **m-13 = D-m6 + L-10's surviving leg — MINOR.** The bar's `{{ currentSection?.number }}.` emits an unguarded literal `.` while `currentSection` is null; the sessionStorage restore window (PaperView onMounted/nextTick — my read) is real; K-5 killed the starred-rows leg (all four `\subsection*` are level-2, never rendered; zero starred at rendered levels — my grep). Paint → SS-13 #7. → **F.W3/W4**.
- **m-14 = C-13 — MINOR.** Eight props / zero emits / three callback props / a raw DOM node / an unwrapped composable bag of refs (`.value` in templates across the family); no `defineExpose` so the parent cannot dismiss what it can unmount. The stateful child mutating shared state voids the presentational defence. → **F.W3/W4**.
- **m-15 = C-16 + C-17 + C-18 — MINOR.** Sub rows carry no active state/tint (root cause = m-3); expansion is unanimated where the sibling uses glass `Collapsible` (subpath present at 4.0.0 — adopted); the inline active colour outranks the hover rule so the active row alone never changes ink on press. → **F.W3/W4**.

### INFO

- **i-1 = L-13 = C-14 (R-5 demotion) — INFO.** Three template-ref idioms in one script; `triggerEl()`'s `instanceof` arm dead by construction (no `defineExpose`, single `Primitive` root — my dist read); the shim itself is the fleet's crowned conformant idiom (**fr-CollapsibleSection m-1**, fold). → **F.W3/W4** (uniformity rides any rewrite).
- **i-2 = L-17 = C-10 — INFO + GLASS-RELAY.** `useSidebarState` takes `sections` by value while its sibling options are `MaybeRefOrGetter` — a producer contract asymmetry (d.ts adopted, dual-derived); latent here (build-time-constant sections). Producer ask rides the standing glass-ui BH relay (owner edict): type `sections` as `MaybeRefOrGetter<T[]>` or document the snapshot. → **GLASS-RELAY**.
- **i-3 = D-i1 — INFO.** Zero test coverage: no e2e spec references the component (adopted grep, both readers), and a desktop-chromium project cannot mount it (`lg:hidden` + coarse-pointer sizing). → **F.W3/W4** (a section-nav e2e is the born-RED seed for M-1's witness).
- **i-4 = D-i2 — INFO + GLASS-RELAY rider.** Three legs of the 4.0.0 Button base are never emitted (`ui-glyph` 0 in components.css — my grep) — load-bearing for the ABSENCE of an icon-inflation finding here. The fresh-build emission question folds to **F-1** (the fr-CollapsibleSection M-7 hinge); the producer-side report rides the relay with i-2. → **F.W1 pre-gate rider (F-1)**.
- **i-5 = L-18 — INFO → NO-WAVE-OWNER (SS-3/SS-4).** `<template v-for>` (:149) is a third loop-host kind falling through both R5-7's component-callsite keying and R6-5's `NATIVE_TEMPLATE_LOOP` cure; the component-hosted flavour sits at :167 in the same file — the cleanest enumeration exhibit. R3-10's `MobileFloatingToc:157` dynamic-`:is` row re-verified exactly at :157-161 (my file read). Census methodology, not a wave row.
- **i-6 = reader-1 missed-5 — INFO.** On `contrast-color()` engines the lockstep lift re-points `--foreground` AND `--muted-foreground` to the same ink inside the plate (producer's deliberate scope-6 fix — my ladder read), collapsing this dropdown's three-tier muted hierarchy to alpha-only differences; on non-capable engines the authored ladder (and M-4's failure) holds. The colour design does not survive both engines identically. → SS-13 #9; design input to M-4's cure.
- **i-7 = reader-2 missed-5 — INFO.** Dead declared surface in the mounted family no gate can report (no `noUnusedLocals`, no lint): `defineEmits<{select}>` never emitted in PaperSearchDropdown and PaperSearchModal (my grep: `emit(` only in PaperSearchInput:52), `defineExpose({resultsRef})` with zero consumers. Class = **fr-EquationPanel M-N14** (fold); the dead search barrel folds by reference to the PaperSearchInput row reader-2 cites. Home: PaperSearch family (pointer). → **F.W3/W4** (rides B-1's family repair).

**F.W5–W8 gets nothing** — the component is API-inert, value.js-inert, keyframes-inert (C's S-2/S-3 closure greps, adopted and consistent with this seat's import-list read); the negative is recorded so no CRUD/provenance wave budgets this file. **F.W1's stake** = the glass leg only: U-1/U-2 variant/size breaks, the lucide rename (1 of the census's 35 sites), the K-8/K-9-corrected anchor set, B-1's U-10 decision input, and F-1's pre-gate.

## Killed-claims register

- **K-1 · The 51-root denominator (D §1/B1/M2/M4/m7; L-2's "zero `\chapter`"; L-3's 51-element loop) — KILLED, both readers, settled by this seat's double derivation.** 11 chapters (first at tex:118); 51 `\section`s are level-1 CHILDREN under `LEVEL_MAP {chapter:0, section:1}` (adopted producer read, dual-derived); the shipped bundle carries exactly **13** top-level entries (my brace-walk). Every magnitude dividing by 51 was built on a false denominator. Reader-discrepancy arbitration inside the kill (R-1): reader-1's counts exact; reader-2's LensII=8 and "51 sub rows" cells off by one.
- **K-2 · "41 of 51 rows inert" (L-2's headline; D-B1's severity line) — KILLED.** Childless roots = **3** (si 0/5/9 — bundle-verified `subsections` empty; corroborated by 0 sections under Interpreting The Results and 0 subsections under Introduction in the tex, my awk). The defect is real at 3 rows (booked in M-1); the headline was 13.7× too large.
- **K-3 · L-3's "38 of 51 active rows lose the tint" (MAJOR) + D-m7 — KILLED.** The loop has 13 iterations; the ramp has exactly 13 stops 0..12 (my token read + the readers' built-CSS greps). **Zero rows lose the tint today** — the ramp fits exactly (S-9). Survivor: C-11's latent row at **zero headroom** (no `% 13`, no accessor; the 14th root silently drops the highlight) — MINOR, folded into M-6's booking as the latent leg.
- **K-4 · L-1/C-1's raw-LaTeX BLOCKER (and D-M7's consequence leg) — KILLED.** All four math-bearing titles are `\subsection` = level-2 = never rendered by this two-level component (my grep: zero `$` in chapter/section titles); the bar label is always a root (my PaperView read). `renderTitle` is the identity function on every title this component renders — invoking it would change nothing shipped. Dead-prop leg survives (m-1). The truth the three axes were reaching for is M-2.
- **K-5 · L-10's starred-rows naked "." — KILLED.** All 4 `\subsection*` are level-2 (my grep; `number:""` ×4 in the bundle, all level-2); zero starred `\chapter`/`\section`. Only the bar's null transient survives (m-13).
- **K-6 · D's dropdown arithmetic (3 442–3 528px / 7.5 rows / 14.7%) — KILLED as arithmetic.** This seat's own recount: collapsed = 14 rows ≈ 970px, ~52% visible at 60vh/844; worst case (Lens I + 11 subs) ≈ 1.7k, ~30%. The 67.5px mechanism is confirmed separately (M-8); the drama was 2–3.6× overstated.
- **K-7 · D-m5 (focus ring squares the row via `border-radius: inherit`) — KILLED, both readers, mechanism ratified by this seat's stylesheet-home reads.** Both rules sit at (0,2,0) unlayered; the focus rule ships in the ENTRY sheet (`index-57FkGzlZ.css` — my grep), the scoped `border-radius:.375rem` in the route chunk appended to `<head>` at dynamic-import time — later wins; the row keeps its corners and the ring follows them. D's falsifier list checked `.glass-floating` and never checked source order.
- **K-8 · D-M1's uplift note / D §6 U-4 ("7.0.0 replaces the stadium with `calc(var(--button-size)/2)` at styles.css:24") — KILLED at the named baseline, by this seat's own `git show`.** v7.0.0:src/components/button/styles.css **:13** is `border-radius: var(--radius-pill)` — the stadium unchanged at the adopted target; the calc form exists only at HEAD (8.0.0, :69). An 8.0.0 fact wearing a 7.0.0 label — the K-12-class baseline breach. D's verdict cell ("UNCHANGED") survives more strongly than its own citation.
- **K-9 · D §6's line-anchor set into v7 button styles (:8/:12/:35/:50-53) — KILLED.** None resolves at the tag: my `git show` places them at **:7/:9/:19/:35-38**. The VERDICTS survive on this seat's re-read (U-3 IMPROVES, U-5 UNCHANGED-or-worse, U-7 NEUTRAL); a repair wave grepping D's anchors finds nothing — re-resolve before executing (the D-19 lesson).
- **K-10 · D-B2's localisation ("this component is the only consumer of the variant they were written for… one arm visually broken") — KILLED AS SCOPING, defect survives bigger.** The dead rules are dead for BOTH hosts (no `<style>` in any child — my census); only the two `--floating` overrides are variant-specific, dead for the same reason. Rescoped into B-1; homing corrected to the PaperSearch family.
- **K-11 · D-B4's BLOCKER universal ("every leaf target fails AA in the default theme") — KILLED (reader-1; mechanism re-read by this seat).** The unconditional `:where(.glass-floating,.glass-overlay)` contrast-color arm (ladder :263-266, read verbatim) re-points the very token the rows mix, on precisely the component's primary engine class. Engine-conditional MAJOR (M-4). D's three falsifiers never touched the ink re-point; the C axis footnoted the mechanism (C-6 note b) and D did not.
- **K-12 · C-3's twice-composited scrim ("the scrim and the blur composite twice") — KILLED (reader-2; ratified via B-1's own derivation).** The `.search-modal-overlay` declarations never apply to either teleported subtree — there is no scrim and no blur to composite. The duplication and the global-`querySelector` cross-talk survive (M-16).
- **K-13 · The S-6/L-S3/C-S6 grep receipt ("`grep -c 'transition: all\|cubic-bezier'` → 0") — KILLED (reader-2; settled by this seat's run → 2).** The hits are the two A.W3.d comments containing the banned phrase in prose. The substantive claim (no such *declaration*) is true and the superlative survives with a corrected receipt; **reader-1 republished the non-reproducing figure inside its own superlative cell** — the citation-inheritance event, DISSENT-1.
- **K-14 · The L challenge's evidentiary chain ("`web/dist/assets` settles L-3, L-4, L-15") — KILLED AS PUBLISHED (reader-2; staleness re-run by this seat: Jun 12 build vs Jun 17 installed producer; `control-h-md` 0 in the artifact).** All three conclusions survive because they re-derive against the installed 4.0.0 tree (this seat re-ran the decisive greps); the chain as written reasons about a package five days newer than its witness. Identity folds to fr-CollapsibleSection M-7 (F-1).

## Superlatives — with provenance (L-18 runs both ways)

1. **★★★ The scroll anchor MEASURES the bar; the sticky root costs zero layout** (D S-1 / L S-4; every limb re-read by this seat: useScrollNavigation:23-26, the height-0 sticky + relative anchor). Headings never hide under a bar whose 67.5px nobody authored, in both modes, degrading to 16px unmounted; a component that mounts/unmounts on every scroll direction change contributes zero CLS. m-10 rides this same contract — the mechanism is right and is also what makes the mode-swap delta propagate benignly.
2. **★★★ The section-colour ramp fits the live root count EXACTLY — 13 stops, 13 roots — and all three axes reported the opposite** (reader-2's honest inversion, ratified by this seat's double census). Every active root is tinted; none falls through. The fit is exact and unenforced — zero headroom — which is precisely why the latent leg survives inside M-6.
3. **★★ Reduced motion is correct by absence** (D S-2): the producer's PRM blanket rewrites `transition-property` to an opacity/colour list excluding `transform`, so the slide loses its spatial leg and keeps its fade with no local block needed. Adopted on the banked a11y-overrides read (fr-CollapsibleSection instruments) + both readers' concurrence.
4. **★★ The iOS scroll-containment discharge is complete on all five legs** (all three axes + both readers) — dropdown containment trio + ancestor lock + the unmount release most components forget, on an element scroller (the least destructive instrument). The lock's *manner* is m-7's defect; the SET is right. Convention identity: FR-GFC-16's six-site `overscroll-behavior` census, this file among them.
5. **★★ `triggerEl()` is the fleet's crowned conformant idiom** (fr-CollapsibleSection m-1 names these exact lines; my dist read ratifies the live branch: no `defineExpose`, single `Primitive` root, `$el` a real `<button>`). L-13's defect framing demoted (R-5); the residue is i-1.
6. **★★ `highlightFuzzy` escapes per-segment, the hard way** (reader-2; re-verified by this seat's grep of :35/:49/:61/:64/:72): both early returns escaped, `<mark>` wrapped around escaped segments, never markup spliced into an escaped whole — the one user-input `v-html` path in the closure, provably injection-safe.
7. **★ The glass 4.0.0 ladder rename is correct, complete, and correctly tiered** (C S-1; adopted dual-derived: the uncommitted diff is exactly 3 class swaps; `glass-medium` resolves to nothing at 4.0.0 — the born-RED-blank pattern this rename cures; resting for the bar, floating for the overlay is the right tier choice). Conditional on the branch landing, exactly as C flagged.
8. **★ Motion-token discipline holds at three of four sites — with the receipt corrected** (R-4/K-13): :246/:348/:384-385 name properties and use live tokens; :287's raw `ease` is the recorded blemish (M-11/m-12). A superlative that carries its own counter-evidence is worth more than one that does not — and this one now also carries the lesson that its original receipt did not reproduce.
9. **★ METHOD (corpus-level): the C axis read the tree nearly right and its hardest numbers reproduce.** C derived ~12 roots where D/L asserted 51 (true: 13 — C off by one, the others off 3.9×); C's thirteen-row contrast table reproduces to 2dp under this seat's independent pipeline; C alone footnoted the contrast-color re-point that killed D-B4's grade; C correctly scoped the component out of F.W2/F.W5 by closure grep. Where the three axes disagree on this component, C is the one to trust — and even C missed Bibliography.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **The desktop mount** — does `document.querySelector('.floating-toc-bar')` return non-null at ≥1024px? (IO initial record for a `display:none` target.) Decides m-6's 8px-vs-16px consequence and m-4's all-viewport premise. The corpus's cheapest probe.
2. **The engine arm** — computed `--muted-foreground` inside `.floating-toc-dropdown` on iOS Safari 26+ vs Chrome/Android: decides M-4 (2.88 vs ~cured) and M-5's collapse-icon leg. M-5's two bar glyphs hold on every engine — no probe needed there.
3. Visible horizontal clipping of the 63-char sub row at 360px (M-15) — cascade proven, paint not.
4. The rendered stadium + arcing hairline on the full-bleed bar (M-7).
5. The unstyled modal's actual paint on both hosts, and which teleported block sits first in document order (B-1/M-16's reshaped cross-talk polarity).
6. The composited `.glass-floating` plate value under M-6's ramp ratios and M-4's exact figures (direction proven; exact ratios not).
7. Whether the bare `.` transient paints during the sessionStorage restore (m-13).
8. The search-mode bar's measured height (derived ≈90px vs the locked 67.5px — m-10).
9. Whether the contrast-color lockstep visibly flattens the resting/active ink step (i-6; same probe as #2).
10. The "blur with no plate" rendered appearance of the ghosted `.glass-resting` bar (M-12).
11. The ResizeObserver recalculation cost when the scroll lock removes a classic-scrollbar gutter mid-interaction (L §5's booking; layout, not viz).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event inside reader-1's superlative cell (R-4/K-13).** Reader-1's S-6 republished the corpus's `grep → 0` receipt verbatim; the grep returns 2, and reader-2 caught it with the exact hit lines. Beside it, reader-1's original work on the same corpus was extensive and decisive (the K-8/K-9 baseline kills, K-11's operative falsifier, the tree arithmetic). Fleet lesson, third occurrence (fr-EasingPicker, fr-CollapsibleSection DISSENT-1): **a confirmation or superlative cell needs the same receipt discipline as an accusation cell.**
2. **This seat's own recorded near-miss.** My first tree derivation presumed Bibliography last by document order; the shipped bundle places it at index 9, which is what makes both readers' C-6 index names correct. The artifact outranks the source-order presumption — the same law (the census is the shipped tree) this record enforces on others, applied to itself. Reader-2's positions were right where my presumption was wrong; reader-1's counts were right where reader-2's LensII cell was wrong. Triangulation, not trust, produced R-1's table.
3. **Reader-1's MAJOR on the search-bar height shift (m-10) and reader-2's MAJOR-latent on the scroll lock (m-7) — both preserved.** If SS-13 #8 shows the mode swap visibly jarring, or a second `style.overflow` writer ever lands, the respective row re-opens at MAJOR.
4. **Reader-1's MAJOR on L-7 (m-4) preserved**, with the qualifier that this seat's ephemeral-store sharpening cuts both ways: per-mount resets make the divergence shorter-lived AND mean user expansion state is lost constantly — a live UX cost the MINOR grade partly discounts.
5. **Reader-2's D-B4 cell repeated the challenge's substrate caveat without engaging the operative contrast-color mechanism** — inheritance-shaped, though the same reader's C-6 cell carried the mechanism correctly. Recorded as a milder instance of DISSENT-1's class: an incomplete falsifier survey inside a confirmation.
6. **Adopted-not-rerun cells, enumerated**: the minified `useSidebarState` toggle/navigateTo bodies + d.ts shapes (dual-derived by both readers; consistent with my grep of consumption sites); the `setScopeId`/compiler-core recursion traces (dual-derived; my census + built-CSS attribute selectors carry the conclusion regardless); the a11y-overrides PRM property list (banked at fr-CollapsibleSection's arbiter instruments); the lucide `aria-hidden` spread; C-S1's 3-line `git diff`; the IO-initial-record spec behaviour (spec-derived, flagged unproven by all); dark-arm ramp values; the Tailwind `rotate` utility factory (corroborated by my `.rotate-180{rotate:180deg}` grep of the shipped sheet); `variant="floating"` sole-consumer grep (D's, low-risk); PaperSearchModal/Input internal line-cites beyond the lines I read. Every other load-bearing cell was re-derived with this seat's own instruments listed above.

## Closing verdict

**ADJUDICATED-DEFECTIVE — a 397-line component whose corpus arrived with four BLOCKERs and leaves with one, and it isn't the one anyone led with: the PaperSearch family it mounts ships a stylesheet that never reaches its own subtrees, so mobile search renders unstyled and the expanded modal is an unstyled in-body block on both hosts (B-1, home = PaperSearch, the F.W3/W4 shadow-retirement decision). Two of the four arrived-BLOCKERs died on a single false denominator — the paper has 13 ToC roots, not 51 (re-derived twice by this seat; the raw-LaTeX claim dies because every math-bearing title lives at a level this component never renders) — and the third demoted to engine-conditional because the producer's own contrast-color arm re-points the very token the failing rows mix. What the corpus garbled, the adjudication banks straight: the real navigation defect is a one-line differential against the in-tree twin (root rows toggle and never navigate, 3 of 13 taps total no-ops — M-1) plus a structural coverage gap no axis saw (35 of 99 headings unreachable on mobile — M-2, the killed BLOCKERs' true form). Seventeen MAJORs, dominated by one repair unit (stop rendering list rows as `<Button>`: kills the 67.5px lock, the stadium bar, the split alignment, and the ui-scale opt-out together) and one ownership unit (the shared search state this component opens, desyncs, and strands — M-13/M-16/M-17). Fifteen MINORs, seven INFOs, five identity folds riding banked rows (FR-COB-24, FR-EMT-5, FV-27, M-N14, the crowned triggerEl idiom), one standalone fold to the fr-CollapsibleSection M-7 stale-dist/emission hinge, fourteen kills — including three baseline-law kills against glass-ui v7 settled by this seat's own `git show`, and one grep receipt that a reader republished inside a superlative and the other reader shot down (settled: 2, not 0). Two producer asks ride the standing glass BH relay (sections-by-getter; the unemitted CVA legs). F.W1's stake is the glass leg + the corrected v7 anchor set; F.W3/W4 owns everything component-local; F.W5–W8 gets nothing and the negative is recorded; the loop-host taxonomy and teleport instance-counting rows go to SS-3/SS-4 with NO-WAVE-OWNER. Nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
