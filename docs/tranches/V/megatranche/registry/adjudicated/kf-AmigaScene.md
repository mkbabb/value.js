Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component AmigaScene)

# kf-AmigaScene — adjudicated apotheosis

**Date** 2026-08-07 · **Corpus** `audit/kf-components/AmigaScene/challenge-{D-design,L-library,C-consumption}.md` (all three `claude-opus-5[1m]`, dated 2026-08-04) · **Re-readers adjudicated**: design-and-usage (Fable) + library-and-code (Fable), both hostile, returns consumed whole.
**Substrate ruling (read first — it re-scopes one BLOCKER).** The entire corpus and BOTH re-readers measured `/Users/mkbabb/Programming/keyframes.js` = the SACRED stale checkout `8281638c`, 41 commits behind `origin/master` `81a56990` (census §0 substrate schism, published 2026-08-03 — the day *before* the challenge date; neither reader consulted it). This seat diffed the component's whole blast surface between the two trees: `demo/scenes/amiga/` drift is **comment-only** (AmigaScene.vue: 1 reworded comment line; useAmigaDemo.ts: 2 reworded comment blocks, net −2 lines — anchors below `:73` shift by −2 at HEAD; useAmigaThree.ts / useSphereSpin.ts / utils.ts / amigaKeys.ts byte-identical). **Every code-level finding survives at HEAD verbatim. C-1 does not** — `origin/master` package.json:77 carries `"@mkbabb/glass-ui": "7.0.0"` as an EXACT devDep (census SCH-1, AGG-P1), so the phantom-dependency headline is FALSIFIED at HEAD. All line anchors in this record bind to `8281638c` unless marked; KF.W0's re-anchor law applies before any cure executes.
**Method.** Every verdict-level disagreement between the two readers was re-derived by this seat against the tree with its own instruments, not vote-counted. Instruments this session (static + repo-local only, zero browser — probe parsimony): full source reads of AmigaScene.vue (271 L whole), useAmigaThree.ts (273 L whole), useSphereSpin.ts:90–246, useAmigaDemo.ts:14–42/:88–153, utils.ts (whole); `git diff 8281638c..origin/master -- demo/scenes/amiga/ package.json` (the substrate decider); node_modules/three SphereGeometry.js:55–125 (the pole-UV decider — my own signed-zero derivation, below); `src/animation/physics/spring/solver/reseat.ts:1–80` + `progress.ts:100–160/:255–290` (the cure-shape decider); scenePlaybackAdapters.ts:84–102; scene-facility/index.ts:100–110; AnimationControlsGroup/useAnimationGroupPlayback.ts:100–132 (scrub pause/resume); useSceneMachine.ts:190–240 (the only `suspend()` callers); EditorShell.vue:3/:66–76 (the `<main>` landmark + overflow-hidden chain); OrbitalDrag.vue:276–277 + useOrbitalPointer.ts:165–177 (the cube-keyboard kill); live-session.mjs:738–750 (the B3 oracle's 450 ms blindness); design-idioms.css:5–26 (the namespace policy); `ls -l checkerboard.jpg` (103 890 B); touch-action + isPrimary/button censuses; src/animation/index.ts:36–52 (barrel exports); demo/app/index.html:6 (viewport). Frustum arithmetic for D-11 re-derived by hand (below). Where BOTH readers independently verified the same third-party fact (OrbitControls `_EPS` return-false, `GridHelper extends LineSegments`, `Material.dispose()` 3-line body, the D-axis luminance table to 4 decimals), the double-agreement is adopted as corroborated without a third derivation.

## Scoreboard

**49 rows ADJUDICATED-CONFIRMED (2 BLOCKER · 14 MAJOR · 20 MINOR · 13 INFO) · 17 killed/rescoped claims registered · 13 net-new misses adopted into the roster · 12 UNPROVEN-NEEDS-LIVE residues for SS-13.**
Headline survivors: **D-1** (the paused stage is pinned to HOME — the transport scrubs and the subject does not move) and **L-B1** (the primary gesture paints nothing — `isDragging()` missing from the render gate). Headline kill: **C-1's BLOCKER is substrate-dead** — true of the stale checkout, falsified at HEAD.
Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; routings below are agglomeration input (M-25) for the SS-1/SS-2 spec authoring, keyed to the census wave sketch KF.W0–KF.W10.

## Ruled disagreements (this seat's actual work)

| dispute | reader A (design-and-usage) | reader B (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-1 substrate** | CONFIRMED BLOCKER, "re-run today" | CONFIRMED MAJOR, manifest re-probed | **BOTH OVERRULED — RESCOPED-BY-SUBSTRATE** | Both "re-ran" the manifest probe against the stale sacred checkout. My `git show origin/master:package.json` → `:77 "@mkbabb/glass-ui": "7.0.0"` exact devDep; census SCH-1 ruled this row FALSIFIED at HEAD five days before either reader wrote. The cure is KF.W0 §B-12 reconciliation, not a dependency wave. Surviving residue: the *token-dependent-import-free* invisibility observation (true at both trees — an import-graph query for glass-ui consumers excludes the single most token-dependent scene) and C-7's fallback asymmetry, both demoted to census-annotation + hygiene. This is a recorded citation-inheritance event: lane-frontend F-1's RED cell propagated through the challenge corpus into two independent "re-verifications" without either re-deriving the substrate. |
| **L-M4/C-2 cure shape** | CONFIRMED, "the unconsumed cure is on the same barrel two lines above" | defect CONFIRMED; prescribed cure KILLED (K-6): `reseatToSpring` is not a drop-in | **B wins** | My read of reseat.ts:1–80: `reseatToSpring(probe, newTarget)` seeds a *positional* spring at `(probe.curr.value, measured velocity)` — one scalar channel. The scene runs ONE 0→1 *progress* spring (AmigaScene.vue:116–121) and lerps three channels off it (:136–138) whose interruption velocities are three independent numbers (X linear 2.5 u/s constant · Y a cubic-bezier slam on a 5×-shorter period · spin a triangle wave). A shared progress velocity can match at most one channel. Adoption = three per-channel springs or `SpringVectorLanes` (`setTargets(Float64Array)`, progress.ts ~:114–125) — a design change, not a symbol swap. The defect stands on C-2's linear-X argument (no zero-velocity stop phase exists); the repair order must carry B's cure correction. |
| **L-M4 premise** | KILLED ("coprime-ish" false: 8000/1600 = exactly 5) | conclusion carried via C-2 | **A's kill CONFIRMED** | useAmigaDemo.ts:38–39 read by this seat: `X_PERIOD_MS = 8000`, `Y_PERIOD_MS = 1600` — ratio exactly 5, the composite arc exactly 8 s periodic. Cite C-2's linear-velocity argument, never the phase argument. The commensurate periods make D-3's worst case EXACT (t = 2000 ms is simultaneously the X wall and the Y floor). |
| **L-M2 limb 3 (GL context)** | CONFIRMED whole ("the GL context is never lost … sixteen round-trips") | limb 3 KILLED as overstated (K-4): GC reclaims on unmount | **B wins, rescoped** | `dispose()` (useAmigaThree.ts:223–237) indeed never calls `forceContextLoss` (exists in the installed WebGLRenderer), but unmount unroots the entire closure — renderer, canvas, textures — so the context is *held until GC, not leaked forever*. The 16-context-cap escalation does not follow (the browser force-loses the *oldest* context, which is the unreachable one). Limbs 1–2 (GridHelper geometry+material, both CanvasTextures incl. the 1024² ~4 MB checker) CONFIRMED exactly as written. Cure = one line: `renderer.forceContextLoss()` in dispose, plus the traverse fix. |
| **D-11 arithmetic** | CONFIRMED at 2.36:1 (while itself noting the ~6.2° pitch imprecision — flagged and shipped anyway) | KILLED the number (K-3): 1.72:1 | **B wins — my own derivation** | Camera (0, 1.5, 13.8), fov 50, target origin ⇒ pitch atan(1.5/13.8) = 6.204°. Frustum edges intersected with the ball plane z = 0: top y = 1.5 + 13.8·tan(25° − 6.204°) = **+6.20**; bottom y = 1.5 − 13.8·tan(25° + 6.204°) = **−6.86**. Headroom above apex+r (+3) = 3.20; footroom below floor−r (−5) = 1.86 → **1.72:1**, not 2.36:1. Direction (bottom-heavy, against the declared φ bias at layout.css:66–67) survives; the perpendicular-half-height shortcut does not. Reader A's "confirmed as arithmetic" verdict is itself corrected: noting an imprecision and shipping the uncorrected figure is the error class this program exists to catch. |
| **Pole-UV mechanism (missed rows)** | "Math.atan2(0,0)=0 → EVERY pole vertex u=0.5, smear at top AND bottom" | signed zeros → three constant u-blocks (1.0 / 0.5 / 0.0), north pole, 33 vertices | **B wins — my own derivation from the installed generator** | SphereGeometry.js:99–101: `x = −r·cosφ·sinθ`, `z = r·sinφ·sinθ`. North pole row (iy=0): θ=0 exactly ⇒ `sinθ = +0` ⇒ x, z are SIGNED zeros tracking −cosφ / +sinφ; `atan2(±0, ±0)` ∈ {+π, +0, −0, −π} ⇒ with widthSegments 32: u = 1.0 (ix 0–8), 0.5 (ix 9–24), 0.0 (ix 25–32) — three blocks against a smoothly-decreasing neighbor ring, and the deliberate `uOffset = 0.5/widthSegments` pole fan (:82–91) is discarded by the override. **South pole is NOT degenerate**: θ=π ⇒ `Math.sin(π) ≈ +1.22e-16 ≠ 0`, so atan2 receives tiny-but-signed reals and yields a smooth mapping. A's "both poles, all u=0.5" is wrong twice in detail; the defect (north-cap texture wedge + the radius-domain break, below) survives on B's mechanism. |
| **D-2 supporting legs** | killed 2 legs (cube keyboard claim; App.vue-grep landmark inference) | killed 1 leg (landmark; K-2), holds MAJOR | **Both kills CONFIRMED; MAJOR held** | My reads: OrbitalDrag.vue:276–277 + useOrbitalPointer.ts:169–177 — `updatePressedKeys` sets boolean MODIFIER flags (x/y/z/shift/ctrl/meta) consumed only to steer a *pointer* drag; no keyboard-only rotation path exists in cube. EditorShell.vue:74 renders a real `<main>` whose own comment (:66–73) records it was deliberately not `display:contents` to keep the implicit role. So TWO of three `stageMode:"subject"` scenes lack the contract (SquareScene is the sole positive) and a landmark exists but carries no NAME for the subject. The cure is a **shared subject-a11y idiom for cube AND amiga**, never an amiga-only patch. |
| **C-16 severity** | corpus MAJOR → killed to MINOR | MINOR | **MINOR, unanimous after A's kill** | The dead `else` arm is unreachable (shell `overflow-hidden h-dvh w-dvw`, mobile stage `position:fixed inset:0`, re-walked by both readers; my EditorShell read corroborates) and the live arm fires once at mount where both statements are redundant. Dead code + stale comment = MINOR by the corpus's own calibration (C-19/C-20 precedent). Also retires L-i9's live leg. |
| **C S+4 superlative** ("liveness tight in both directions") | not challenged | KILLED — the drag is a fifth mutator of `offset` the enumeration omits | **B's kill CONFIRMED** | useSphereSpin.ts:126–127 writes `offset` on every pointermove; AmigaScene.vue:171 has no `isDragging()` term; `controls.enabled=false` on sphere-hit and `renderDirty` cleared at :213. The superlative asserts tightness in exactly the direction the BLOCKER lives. A superlative and a BLOCKER cannot both hold on the same gate term. |
| **D S-D superlative** ("complete pointer discipline — nothing is missing") | shipped as superlative | refuted by missed M-2 (no `isPrimary`/`button`/re-entrancy guard) | **Demoted to qualified** | My grep: zero `isPrimary` / `e.button` guards in useSphereSpin.ts (or any scene). onPointerDown (:98–113) unconditionally overwrites `activePointer` — a second touch steals the drag; a right-button sphere press hijacks with `preventDefault/stopPropagation` while orbit is disabled. Capture/release/cancel/id-matching remain genuinely complete; multi-pointer arbitration is absent, house-wide. |
| **Missed M-3 trigger** ("group paused while loop down → same-frame construct+complete") | not raised | filed as the live breach | **RESCOPED — reachable, but via C-18's window, not B's stated trigger** | My read of the only `suspend()` callers (useSceneMachine.ts:199/:236 — scene-swap capture + the PAUSE event) shows no authority pauses the GROUP on tab-hide; B's "paused while the loop was down" variant is not shown reachable. The reachable path: user pauses (visible) → re-seat constructed → tab backgrounded within the ~400 ms window → visibility gate stops the loop → return minutes later → first frame computes `dt` = the whole suspend (:98–100, never re-armed) → `tickDt(dt)` evaluates the closed form at t≈minutes (progress.ts `_stepSeconds`) → settled in ONE frame → the exact `position.set`-class snap the T.A8 contract forbids. CONFIRMED-RESCOPED, folded into the D-3/C-18 teleport family. |

## Adjudicated defect roster

Identity-deduped: one row per defect, all axis ids attached. Anchors = stale checkout `8281638c` (comment-only drift to HEAD; useAmigaDemo.ts anchors below :73 shift −2). Routing targets = census wave sketch (KF.W0–W10); **NO-WAVE-OWNER** rows are the SS-1/SS-2 spec-authoring intake — they agglomerate naturally into one *amiga scene-repair wave* the forming specs must create (no census wave owns demo scene behavior today; census §1 "kf-side asks have no owning wave" is exactly this gap).

### BLOCKER

- **D-1 — CONFIRMED (BLOCKER).** The paused stage is pinned to HOME: `rendered` is written only under `playing` (AmigaScene.vue:126–131) or during the self-clearing re-seat (:133–145); `playing = started && playing()` is false when paused (group/lifecycle.ts:198–201). The scrub surface is live and reaches `pose` (`setChildTime(anim, t).render()`, scene-facility/index.ts:104–108 — read by this seat) but nothing reads `pose` outside the `playing` branch, and the gate is doubled (`onFrame` returns false at rest; `setProgress` never marks dirty). Reader A's sharpening ADOPTED and mechanically verified by this seat: `onScrubStart` pauses through the machine (useAnimationGroupPlayback.ts:115–123 → PAUSE → adapter.suspend() → `group.pause()`, both files read), so the actual user sequence is *grab the scrubber → the ball springs home → drag moves nothing → release → single-frame teleport to the scrubbed pose (D-3)*. The tranche's headline defect. **Disposition: ADJUDICATED → NO-WAVE-OWNER (amiga scene-repair wave; the cure couples the compose un-gating with a `markRenderDirty` from `setProgress`); the headline gesture sequence = SS-13 capture input.**
- **L-B1 — CONFIRMED (BLOCKER).** The primary gesture paints nothing: the render gate `renderDirty || controlsChanged || sceneLive` (useAmigaThree.ts:206–214, read whole by this seat) has no edge from the drag. `markRenderDirty`'s only caller is the IntersectionObserver (:216); OrbitControls returns false at rest AND is disabled for the drag's duration (useSphereSpin.ts:105 → :259–261); `isDragging()` exists (:~240, read) and is consulted nowhere. The compose writes the quaternion every frame and the frame is discarded. Reachable in the default state (no `autoPlays`); the B3 oracle is blind by construction (`page.mouse.up()` then `waitForTimeout(450)` — live-session.mjs:744–745, read by this seat — it measures the post-release glide, never the drag). Fix shape: one term. **Disposition: ADJUDICATED → NO-WAVE-OWNER (same scene-repair wave as D-1 — one gate, two missing edges); pixel = SS-13.**

### MAJOR

- **D-2 — CONFIRMED (MAJOR, two supporting legs killed).** The rendered DOM is one bare `<canvas>` (:13–16, read) — no role, name, tabindex, keydown, or fallback content; both interactions pointer-only. The in-repo counter-example (SquareScene role=group + sliders + focus-ring) and the sharpest form (the Suspense skeleton is more accessible than the scene it loads) stand. KILLED legs: cube has NO keyboard rotation path (modifier flags only) and a real `<main>` landmark DOES exist (EditorShell.vue:74) — so the defect is *no accessible name/role/operable path on the subject*, shared by cube, and the cure is a **shared subject-a11y idiom for cube AND amiga**. **Disposition: NO-WAVE-OWNER (scene-repair wave, shared-idiom scope); KF.W9's Safari audit witnesses.**
- **D-3 (+ C-18/L-m5 + missed M-3 rescoped — the teleport family) — CONFIRMED (MAJOR).** The T.A8 "never a teleport" contract is kept on stop, broken on resume (`rendered.px = pose.px` in one frame, :127–131; resume takes `group.resume()` from the stored clock — scenePlaybackAdapters.ts:96–99, read), worst case EXACT at t=2000 ms: jump √41 = 6.40 world units against a ~6.5 half-height. Same contract breached by the suspend window: `lastFrameAt` never re-armed across stop/start (:92,:98–100), so a re-seat live at backgrounding completes in one frame on return (M-3 as rescoped above). **Disposition: NO-WAVE-OWNER (one spec row: symmetric resume re-seat + clock re-arm on `start()`; cure shape per the L-M4 ruling — per-channel springs or vector lanes, NOT a bare `reseatToSpring` swap).**
- **D-4 — CONFIRMED (MAJOR, conformance framing killed).** The contact shadow — the load-bearing height cue after T.A10 — computes to 1.016:1 dark (both readers re-derived independently to 4 decimals; matched arithmetic adopted). The theme-blind `rgba(0,0,0,0.55)` literal (:69–71 — the CORRECT lines; C-3's :63–65 cite is the killed provenance nit), zero theme reads in the scene, and the 1.9× scale-up animating an invisible sprite. Filed as a *design* defect, not WCAG 1.4.11 (scenery — the kill is adopted). **Disposition: KF.W6 (token/theme discipline for canvas colors — `resolveColor` + `useGlobalDark` re-bake per the SpringHeatmap/CSSCodeEditor precedent); perceptual verdict = SS-13 under KF.W9.**
- **D-5 — CONFIRMED (MAJOR, same framing kill).** `#b9b9c6` = hsl(240 10% 75%) — hue 240 in a system whose every neutral is hue 24–40; 1.22:1 light / 2.15:1 dark, light 1.76× worse; no token edge (grep re-run by both readers, empty). The T.A10 comment assigns the grid the deleted box's load-bearing job, foreclosing the decorative escape. **Disposition: KF.W6; SS-13 witness under KF.W9.**
- **D-6 — CONFIRMED (MAJOR).** The ±5 sweep fits only when aspect ≥ 6/6.4728 = 0.927; the vertical-fov camera + aspect-only resize handler (:241–250, read — including the zero-size guard the mount path lacks, see INFO M-4) + no letterbox/aspect clamp anywhere; the T.A9 frustum-fit kill was correct and unreplaced. Device clip fractions (~50 % partial / ~10–12 % off-screen per loop at 390-wide) = SS-13. **Disposition: NO-WAVE-OWNER (aspect-aware camera framing term — scale the camera, never the arc); device numbers under KF.W9.**
- **D-7 / C-15 / L-m8 — CONFIRMED (MAJOR).** PRM posture inverted: the ~400 ms settle is the ONLY gated motion (:107–112); all three `CSSKeyframesAnimation` and the group omit `respectReducedMotion` (engine default false; `reducedMotionScale` numeric-intensity resolver exported on the LIGHT barrel — index.ts:36–52 read by this seat); the demo's own stored default `true` is never applied to a live animation; AmigaScene's SpringProgress is the only demo construction omitting the flag. Census FE §6.5 correction rides along: **four** PRM JS-query sites, **three** mechanisms (AmigaScene.vue:58 is the uncounted fourth). **Disposition: KF.W9 (PRM mechanism unification is that wave's named scope); the remedy is the *intensity* form (`respectReducedMotion: ~0.3`), not a binary snap, per the engine's own mechanism.**
- **D-8 — CONFIRMED (MAJOR).** No error posture: bare `new THREE.WebGLRenderer` in `setup()` under `onMounted` with no try/catch/fallback; `grep contextlost|contextrestored|onErrorCaptured` → zero across demo/ AND src/; the `cursor: grab` affordance outlives the object. **Disposition: NO-WAVE-OWNER (scene-repair wave; couples with C-9 limb 1).**
- **L-M1 / C-8 — CONFIRMED (MAJOR).** Stale flick velocity: `velX/velY` written only in `onPointerMove` (:130–131, read); `endDrag` never reads `lastMoveTime`; the 1 ms `dt` floor (:118) is a real up-to-1000× amplifier; REST_SPEED 1e-3 excludes nothing; the covering test is the near-miss (no intervening pointermove); the sibling `useOrbitalInertia` clamps exactly this. **Disposition: NO-WAVE-OWNER.**
- **L-M2 / C-14 — CONFIRMED (MAJOR, limb 3 rescoped).** `dispose()` leaks both GridHelpers (LineSegments, not Mesh — installed-three verified by both readers) and both CanvasTextures (`Material.dispose()` never walks `map`); limb 3 rescoped to *held-until-GC* per the ruling above. **Disposition: NO-WAVE-OWNER (traverse fix + texture dispose + one-line `forceContextLoss`).**
- **L-M3 / C-11 — CONFIRMED (MAJOR, provenance scope corrected).** `__kfAmigaProbe` is a production `window` global with ZERO executable consumers (both readers' exhaustive greps agree: exactly 2 in-repo hits, both in AmigaScene.vue; the "five files" cell was cross-repo conflation — the four prose docs live in value.js's audit tree, so the defect is *stronger* in-repo than written), while its comments name oracles (`proof:amiga-decay-visible`, T.A7/T.A8/T.A9 physics oracles) that do not exist as wired readers. Only `(window as unknown as …)` double-cast in demo/, beside the typed `__kfLoaf` idiom. **Disposition: NO-WAVE-OWNER (delete, or type + DEV-gate + correct the comment — the false-oracle prose must not survive either way).**
- **L-M4 / C-2 — CONFIRMED (MAJOR, premise and cure both corrected — see rulings).** The stop re-seat is velocity-discontinuous (SpringProgress seeds `initialVelocity: 0`; the linear X channel guarantees |v| ≡ 2.5 u/s at every instant, so no stop is kink-free). Cite C-2's argument, never the "coprime-ish" premise; the repair order must carry the per-channel/vector-lanes cure shape, never a bare `reseatToSpring` swap. **Disposition: NO-WAVE-OWNER (folds with the D-3 teleport family — one continuity spec row).**
- **L-M5 / C-4 — CONFIRMED (MAJOR).** `getSphere()` typed non-nullable over a genuinely-undefined closure field (`let sphereMesh: …` :89, assigned only in `setup()` :175, escaping at :256 — all read); JSDoc says "Undefined before mount"; both siblings honest; consumer guards a branch the types call unreachable; `useSphereSpin` independently re-widens. Three contracts, one value. **Disposition: NO-WAVE-OWNER (`| undefined` on the interface + field — a 2-token edit).**
- **MISSED-A (adopted from reader A) — CONFIRMED (MAJOR).** On touch there is NO affordance for the primary interaction: `cursor: grab` is the entire discoverability story (:249–255, read) and cursors do not exist on touch; the gesture legend was deleted at T.A10 (the template's own removal changelog says so); the sibling in the same register renders the literal "drag me" as its stated design principle. Verified adjacent: no keyboard route either (D-2) → zero discoverable interaction on a phone-filling canvas. All three axes audited the gesture's a11y, physics, and render gate; none audited findability. **Disposition: NO-WAVE-OWNER (scene-repair wave; SS-13 confirms on-device).**
- **MISSED-B (adopted from reader A) — CONFIRMED (MAJOR).** The scene's "themed paper-grid backdrop" is an opaque two-stop gradient (`--muted` → `--background`, :259–263, read — there is NO grid in it; the comment misnames its own CSS) painted by a positioned descendant ABOVE the shell's fixed `.grid-background` graph paper — the substrate the J.W7a rationale guarantees "PRESENT and legible behind the glass plate." Every register sibling uses a translucent `<Card surface="glass">` so the substrate shows through; amiga erases it across the stage (desktop) / viewport (mobile) and substitutes a wash that measures 1.064:1 in light theme (D-10). **Disposition: KF.W6 (folds with C-6/D-12 — one stage-surface decision, the S-9 evaluation); occlusion pixel = SS-13.**

### MINOR

- **C-16 — CONFIRMED (MINOR, severity ruled down).** The I.W3 S2 occlusion pause cannot fire (no scroller anywhere in the containment chain; mobile stage viewport-fixed); the `else` arm unreachable, the live arm once-and-redundant; the comment asserts a false runtime invariant. Also retires L-i9's live leg. **NO-WAVE-OWNER (delete or truth the comment; document the second-authority hazard against the honesty-contracted gate).**
- **C-9 limb 1 — CONFIRMED (MINOR).** The only LIVE resume path (tab-visibility — C-16 makes it sole) is the one missing `markRenderDirty()`; fatal exactly when composed with a context loss D-8 proves is unhandled. **NO-WAVE-OWNER (couple with D-8).** Context-loss outcome = SS-13.
- **C-6 / D-12 — CONFIRMED (MINOR).** Two shipped glass primitives (`Surface`, `PaperBackdrop`; `Card` per the sibling register) shadowed by 8 lines of bespoke CSS; AmigaScene is the only file in the tree hand-applying `rounded-card` to a raw element; the full-bleed mobile layer wears card chrome (the category error). Proposed shadow id **S-9** ADOPTED into the census shadow census. **KF.W6 (evaluate-not-swap; the :6–12 no-DOM-layer ruling may reduce the cure to a shared `.stage-plate` recipe); feasibility behind an alpha:true canvas = SS-13.**
- **C-7 / L-m7 — CONFIRMED-RESCOPED (MINOR → hygiene at HEAD).** The fallback asymmetry (two of three `var()`s fallbacked, `--border` bare; fallbacks achromatic-gray and light-only) is real and the only such idiom in the tree — but the clean-install trigger dies at HEAD (C-1 ruling: glass-ui is a declared devDep). Residue: dead-but-misleading off-palette fallbacks. **KF.W6 (token hygiene rider).**
- **C-17 — CONFIRMED (MINOR).** Two concurrent `RAFPlayback` loops hand `pose` across a frame boundary with no frame identity; the T.A7 apparatus specifies WHO writes, never WHEN. **NO-WAVE-OWNER (a one-line ordering contract in the T.A7 comment, or a frame stamp).** Lag count = SS-13.
- **C-18 / L-m5 — CONFIRMED (MINOR; sharpened by M-3's rescope into the D-3 family).** `lastFrameAt` never re-armed across stop/start; benign only because SpringProgress is analytic; the same folder's glide is pause-immune by design (wall-clock). **NO-WAVE-OWNER (rides the D-3 continuity row).**
- **C-19 / L-m1 — CONFIRMED (MINOR).** Dead `computed` import (:21, read — sole occurrence); no gate can see it (no eslint over demo/; tsconfig lacks `noUnusedLocals`). **KF.W4 (the K3 `noUnusedLocals` row — extend its scope explicitly to the shared tsconfig `include: ["src/","demo/"]` so the flag closes this class demo-wide).**
- **C-20 / L-i1 — CONFIRMED (MINOR).** `checkerboard.jpg` 103 890 B (my `ls`), zero code references (3 prose hits only); the board is procedural. **NO-WAVE-OWNER (delete).**
- **C-10 / L-m2 — CONFIRMED (MINOR).** `pz` write-never through four surfaces; `reseatFrom.pz` allocated, never assigned; the re-seat/PRM/lerp paths all drop it while the render path carries it. **NO-WAVE-OWNER (delete the channel or plumb it — the comment "Z motion DIES" says delete).**
- **C-5 / L-m4 — CONFIRMED (MINOR).** Three of five returned members dead; the typed per-channel surface advertised and structurally unreachable (facility `ChannelHandle.animation: KeyframesAnimation<any>`). **NO-WAVE-OWNER.**
- **D-9 — CONFIRMED (MINOR).** One color of seven tokenized; six literals (checker white — whiter than the system's own paper white — grid, hemisphere pair, spot, specular at utils.ts:70, shadow stops) invisible to any CSS token audit. **KF.W6 (with D-4/D-5; the specular constant may stay as lighting physics per D-9's own falsifier).**
- **D-10 — CONFIRMED (MINOR, figure corrected).** The "sky/ground wash" is **1.064:1** light (not 1.05 — kill registered) — visually a flat field — and polarity inverts between themes; one authored intent, two opposite renderings. **KF.W6 (the stage-surface decision, with MISSED-B).**
- **D-11 — CONFIRMED-CORRECTED (MINOR).** Bottom-heavy framing **1.72:1** (this seat's derivation; 2.36 killed) against the declared φ law; argument-from-consistency, correctly filed MINOR. **NO-WAVE-OWNER (rides the D-6 framing row).**
- **L-m3 — CONFIRMED (MINOR).** `CONTACT_FLOOR` + the `+0.01` epsilon duplicated across two files (byte-identical expressions over the same imports — both read); the invariant re-written per frame. **NO-WAVE-OWNER.**
- **L-m6 — CONFIRMED (MINOR).** `tickGlide()`'s documented consumer does not consume the return (:160–187 read; AmigaScene calls it bare, re-derives via `isGliding()`); L-M3's failure mode at smaller scale. **NO-WAVE-OWNER (fix the comment or consume the return in the :171 gate — which would also partially serve L-B1).**
- **MISSED-C (merged UV family; mechanism per the ruling) — CONFIRMED (MINOR).** utils.ts:75–88 (read whole) overwrites SphereGeometry's own equirectangular UVs with a hand mapping that is (a) redundant, (b) **north-pole degenerate** via IEEE signed zeros (three constant u-blocks across 33 coincident vertices; the generator's deliberate pole `uOffset` fan discarded — my derivation from the installed source), and (c) radius-unsound: `Math.asin(y)` over raw coordinates ⇒ NaN rows for any radius > 1, compressed band for radius < 1 — inert solely because `SPHERE_RADIUS = 1`, a constant the D-6 framing repair is a live reason to touch. All three axes read this file, praised it, and stopped at line 74. **NO-WAVE-OWNER (delete the override, or normalize y/r and keep three's pole fan); north-cap wedge visibility = SS-13.**
- **MISSED-D (adopted from reader A) — CONFIRMED (MINOR).** The J.W7a hairline — the chrome D-12 calls load-bearing — was never measured by the axis that measured everything else: 1.94:1 light / 2.82:1 dark, the same light-worse asymmetry as the grid. **KF.W6 (the same stage-surface decision).**
- **MISSED-E (adopted from reader A; verified by this seat) — CONFIRMED (MINOR).** The "gravity-flavoured bounce" is anti-gravity at the floor: ONE `cubic-bezier(0.36, 0, 0.66, 1)` across all five Y keyframes (useAmigaDemo.ts:127–140, read — zero slope at both control endpoints ⇒ v = 0 at 0/25/50/75/100 %), so the ball is SLOWEST at the slam and fastest mid-flight — the inverse of the physics the comments claim twice. Cure: per-segment easing (ease-in down, ease-out up), which the authored surface supports. The one thing a keyframe-engine demo showcases — the curve — is the one thing no axis audited. **NO-WAVE-OWNER.**
- **MISSED-F (adopted from reader A; census verified by this seat) — CONFIRMED (MINOR, house-wide).** `touch-action: none` on the viewport-filling mobile canvas suppresses pinch-zoom over most of the screen; the page-level escape is intact (viewport meta clean — my read); the idiom is house-wide (6 scene sites — my census), amiga's the largest surface. **NO-WAVE-OWNER (shared `touch-action: pinch-zoom` refinement across the drag scenes); KF.W9 mobile audit witnesses.**
- **MISSED-G (adopted from reader B; verified by this seat) — CONFIRMED (MINOR, house-wide).** No `isPrimary`/`button`/re-entrancy guard on the sphere pointerdown (:98–113, read): a second touch silently steals the active pointer; a secondary-button press hijacks pan while disabling orbit. Zero such guards in any scene (my grep). Demotes D's S-D superlative to qualified. **NO-WAVE-OWNER (shared guard idiom).**

### INFO

- **C-12 — CONFIRMED.** `Vars` generic declined; three silent-drop `typeof` guards where a narrowed instantiation would be compile errors; pairs with L-i2's recorded prior failure of this exact seam. **NO-WAVE-OWNER.**
- **C-13 — CONFIRMED.** Authored `rotation.y` vs rendered 0.28-rad tilted axis; one id, three names (`AMIGA_SCENE_ID`/`SCENE_ID`/`superKey`) in a keyspace built to have one. **NO-WAVE-OWNER.**
- **L-i2 — CONFIRMED.** Silent-drop error posture in the transform; the U-audit records this seam failing before with amiga the only affected scene. **NO-WAVE-OWNER (log-once on a non-number leaf, or narrow the generic per C-12).**
- **L-i3 — CONFIRMED.** A fresh `SpringProgress` (with its embedded never-started `RAFPlayback`) per stop transition, against the library's own "FEW and long-lived" allocation note. **NO-WAVE-OWNER.**
- **L-i5 — CONFIRMED.** Checker texture baked once at mount, theme-blind; inert today (`--amiga-red` → `--rainbow-red`, no `.dark` override — design-idioms.css read). **KF.W6 rider (goes live the day a dark red is authored).**
- **L-i6 — CONFIRMED.** The last glide delta is composed then discarded unrendered (~1.6e-5 rad — completeness record). **NO-WAVE-OWNER.**
- **L-i8 — CONFIRMED.** `superKey` single-use alias; `dispose` both auto-registered and exposed (double-call would double-dispose). **NO-WAVE-OWNER.**
- **D-13/D-14/D-15/L-i7 — CONFIRMED as the register complaint.** ~90 comment lines of unresolvable tranche archaeology, a removal changelog inside the rendered template, a shipped dev probe, zero forced-colors handling (canvas exempt; the CSS residue small). Two negative-decision comments genuinely earn their keep (content-visibility mechanism; non-DOM probe rationale). **NO-WAVE-OWNER; forced-colors treatment of the backdrop = SS-13.**
- **MISSED-H (adopted from reader A; policy text read by this seat) — CONFIRMED.** The demo DELIBERATELY shadows glass-ui's namespace: design-idioms.css:9–11 states the override policy in its own words; `--amiga-red`/`--rainbow-red` are unprefixed BY DESIGN. Nuances (does not refute) census FE §6.3: prefixing `--amiga-red` would break the stated intent and — because Canvas2D ignores invalid fillStyle — ship a solid-white ball with no error. **KF.W6 MUST fold this before touching the token layer (a guard row in that wave's spec).**
- **MISSED-I (adopted from reader A; arithmetic verified) — CONFIRMED.** The spin is decorative, not rolling: ±π authored vs 10 rad for a true roll of the 10-unit crossing (3.18× slip), while the comments frame it as physically locked. Plainly an aesthetic choice; recorded so the physical framing is not read as a derivation. **NO-WAVE-OWNER (comment truth only).**
- **MISSED (reader B, M-4) — CONFIRMED.** Zero-size guard asymmetry: mount path divides `clientWidth/clientHeight` unguarded (:114–119) while the sibling resize handler guards exactly that (:245) — the author demonstrably knew the window exists. **NO-WAVE-OWNER.**
- **MISSED (reader B, M-5) — CONFIRMED.** Render-on-demand gates the DRAW, not the FRAME: `return true` unconditionally (:215, read) — controls.update + the full compose run at 60 Hz forever while visible; "the WebGL context is idle at rest" is true of GL and false of the CPU; qualifies the T.A12 comment and D's S-A. **NO-WAVE-OWNER (return-false-at-rest with dirty re-arm, or record the cost as accepted).**
- **MISSED (reader B, M-6) — CONFIRMED.** `attach()` overwrites `stopHandles` without detaching (double-registration latent; single caller today) + the unchecked `as THREE.MeshBasicMaterial` hot-path cast beside a teardown that handles the array case. **NO-WAVE-OWNER.**

## Killed-claims register (refutations, quotable)

1. **C-1 as a BLOCKER — FALSIFIED AT HEAD.** `origin/master` `81a56990` package.json:77 = `"@mkbabb/glass-ui": "7.0.0"` exact devDep (this seat's `git show`; census SCH-1/AGG-P1). True only of the stale sacred checkout both readers "re-verified today." Cure = KF.W0 §B-12, never a dependency wave. Residue survives as census annotation (token-dependent-import-free) + C-7 hygiene. *The corpus-wide lesson: a challenge lane's "re-run today" is worthless without naming the tree; the census's re-anchor law now provably applies to the D/L/C component corpus, not just the formation lanes.*
2. **L-M4's "coprime-ish periods" premise.** 8000/1600 = exactly 5 (my read). The conclusion survives only via C-2's linear-velocity argument. The commensurate ratio makes D-3's worst case exact, not asymptotic.
3. **L-M4/C-2's prescribed cure ("`reseatToSpring` is available with zero restructuring").** The seam produces a positional scalar spring; the scene lerps three non-proportional channels off one progress scalar — no probe of `p` exists and no shared initial velocity can match three independent channel velocities. Adoption = per-channel springs or `SpringVectorLanes`: a design change. Both original axes should have said so; reader A repeated the barrel-adjacency framing uncorrected.
4. **D-2 leg: "cube drives its orbit from keydown."** `updatePressedKeys` sets modifier booleans consumed only by pointer-delta handlers (my read of both files). No keyboard rotation path exists in cube; two of three subject scenes lack the contract; the cure is shared, not amiga-only.
5. **D-2 leg: "no ancestor rescues it" (App.vue grep).** EditorShell.vue:74 renders a real `<main>` kept deliberately non-`contents` for the a11y tree (its own comment). Narrow to: no ancestor supplies an accessible NAME for the subject.
6. **D-4/D-5's WCAG 1.4.11 conformance framing.** Scenery, not UI components/required graphics; reporting as conformance invites dismissal of the real defect. Filed as design defects; the arithmetic itself is adopted (re-derived twice to 4 decimals).
7. **C-16 at MAJOR.** Runtime cost exactly zero; the latent hazard is latent; dead-code-with-stale-comment is MINOR by the corpus's own C-19/C-20 calibration.
8. **L-M2 limb 3 ("the GL context survives every scene exit"; 16-context escalation).** Unmount unroots the closure; the context is held until GC, not leaked forever; `forceContextLoss()` is the determinism cure, not the leak/no-leak difference. Limbs 1–2 stand verbatim.
9. **D-11's 2.36:1.** The perpendicular half-height applied to a non-perpendicular plane; correct figure 1.72:1 (this seat's frustum intersection). Reader A flagged the pitch and shipped the wrong figure anyway — recorded as a method lesson.
10. **C-3's provenance cell ":63–65."** The shadow stops live at :69–71 (my read). D-4 cites correctly; correct before quoting into any repair order.
11. **D-10's "1.05:1."** (0.9601+0.05)/(0.8992+0.05) = **1.0642**. Conclusion unaffected.
12. **L-M3's "five files" grep cell.** Cross-repo conflation: in-repo hits = exactly 2 (both AmigaScene.vue); the four prose documents live in value.js's audit tree. The defect is stronger than written; the sentence as written would fail re-verification in the repo it names.
13. **C-axis superlative S+4 ("liveness tight in both directions").** The drag is a fifth, uncovered mutator — this is L-B1. A superlative asserting tightness where the BLOCKER lives cannot stand; its own falsifier is satisfied.
14. **Reader A's pole-UV mechanism ("atan2(0,0)=0 ⇒ every pole vertex u=0.5, both poles smear").** Signed zeros differentiate: three constant u-blocks (1.0/0.5/0.0) at the NORTH pole only; the south pole (sin π ≈ +1.22e-16) maps smoothly. This seat's derivation from the installed generator; reader B's mechanism adopted.
15. **L-i9's live scenario** (group ticking while scrolled off-screen in a visible tab) — unreachable per C-16's containment walk (re-verified by both readers + this seat's shell read); the structural asymmetry stays INFO.
16. **D-axis superlative S-D's "Nothing is missing."** Multi-pointer/button arbitration is missing, house-wide (MISSED-G, my census). Demoted to qualified; the capture/release/cancel/id set remains genuinely complete.
17. **Reader B's M-3 trigger variant ("the group was paused while the loop was down").** No authority pauses the group on tab-hide (the only `suspend()` callers are scene-swap capture and the PAUSE event — my read). The breach is reachable via C-18's window instead; rescoped, not adopted as stated.

## Superlatives (provenance-pinned; L-18 runs both ways)

1. **The single-writer discipline (T.A7) — enforced by shape, locked by a test.** Exactly two per-frame mesh writes tree-wide (AmigaScene.vue:153–154) + one construction seat (useAmigaThree.ts:176); group writes `pose` only, gesture writes `offset` only; the unit test asserts `mesh.rotation.y === 0` AFTER a drag — a structural lock on "the gesture is not a second writer." Found independently by all three axes and both readers; grep re-confirmed by both.
2. **Two drag semantics on one surface, disjoint by construction, with the seam anticipated.** Capture-phase hit-test + `stopPropagation` before OrbitControls' bubbling listener (correct per DOM dispatch at the target), miss falls through, the orbit pivot IS the subject, and `mesh.updateMatrixWorld()` before every raycast defends specifically against render-on-demand having skipped frames (:90–95, read by this seat with its comment).
3. **The `decay()` consumption is expert, verified not vouched.** Absolute sampler bridged to an additive offset by per-frame differencing (:172–185, read); correct across re-grab (samplers dropped on down, accumulators re-zeroed on release); per-axis independent seeding. The difference between importing an engine and understanding it.
4. **Zero hand-rolled rAF across four frame consumers**; both loops ride the engine's `RAFPlayback`; `onFrame(): boolean` makes the scene declare liveness to the loop — the correct dependency direction. Structurally immune to the constellation's PRM-RAF epidemic class; also why C-15's remedy is cheap. (Qualified by M-5: the loop gates the draw, not the frame.)
5. **Negative decisions recorded WITH mechanism.** No `content-visibility: auto` over a live GL loop (ReadPixels stall named, :244–247) + `useSceneVisibilityPause`'s four-line honesty contract ("only resumes what IT paused"). The rare documentation that stops the next author un-fixing it — and the discipline C-16's dead second authority quietly threatens.
6. **The LIGHT/HEAVY firewall + structurally zero R1 exposure + the declared-and-locked value.js edge.** Five import lines, all LIGHT; HEAVY symbols only through `kfEngine()`; sole value.js edge `@mkbabb/value.js/math` (byte-probed by the corpus: 1 110 B, zero imports); the pin `4.0.0` locked. The exact inverse of C-1 *in the same file* — and at HEAD, with glass-ui now declared, the file becomes best-in-class on both edges once C-7's fallbacks are cleaned.
7. **utils.ts's perf rewrite + `resolveColor`'s platform-trap diagnosis** — the tile-loop isomorphism argument stated in reviewable form; the Canvas2D silent-`fillStyle` trap named precisely. QUALIFIED: all three axes praised this file and none read past line 74 — the UV override (MISSED-C) sits directly beneath the praised block. A superlative is a claim about a file, and the scope of the claim is part of the claim.
8. **S-F — playback cannot start without a gesture** (the machine coerces hydrated `playing → true` to false; the named regression was this scene's). The sole reason D-7 is not a second BLOCKER; credit to the shell, protection real.
9. **The D-axis arithmetic is trustworthy** — both readers independently re-derived the luminance table to 4 decimals from the installed tokens; of ~20 figures, exactly two fell (D-10's 1.05, D-11's 2.36) and both are corrected above. A design audit re-derivable by hand from token literals is what makes an owner ruling possible without a browser — the register this program should demand everywhere.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **L-B1 pixel**: slow sphere-drag, watch DURING the drag (not after release) — the ball must not rotate under the finger. Mechanism closed in source.
2. **D-1 headline sequence**: grab the transport scrubber → ball springs home ~400 ms → no stage response while dragging → single-frame teleport on release. One capture; it is the tranche's headline.
3. **D-6 device numbers**: real mobile stage aspect at 390×844 with dock bands; the 0.927 inequality is decided, the clipped fractions are not.
4. **D-4/D-5 legibility paints**: whether the 1.016:1 dark shadow and 1.22:1 light grid read as present at all.
5. **MISSED-B occlusion**: the amiga gradient erasing the shell's graph paper; whether the 1.064:1 light wash reads as anything.
6. **MISSED-C pole wedge**: screenshot of the ball's top cap under the signed-zero UV blocks.
7. **C-6/S-9 feasibility**: whether `Surface`/`PaperBackdrop`/`Card` can sit behind an alpha:true canvas without opaque paint or clipping.
8. **C-9 limb 1 outcome**: forced context loss while backgrounded → blank canvas with the loop spinning.
9. **C-17 frame lag**: 0 or 1 frame between the two `RAFPlayback` callbacks.
10. **L-i5 theme-toggle**: meaningful only after a dark `--rainbow-red` is authored.
11. **D-15 forced-colors**: treatment of `background-image` + inset box-shadow on a replaced `<canvas>` under `forced-colors: active`.
12. **C-15 under live PRM**: the full-amplitude bounce with `prefers-reduced-motion: reduce` active — the inversion witnessed, not just derived.

## Identity guards (fold-by-reference — never re-booked)

- **C-1 ≡ census F-1** — ONE identity, ruled FALSIFIED-at-HEAD by SCH-1; discharged by KF.W0 §B-12. This record adds only the token-dependent-import-free annotation.
- **C-6's S-9 ≡ a new census shadow-census row** — booked ONCE here, extends S-1..S-8, owned by KF.W6.
- **D-7/C-15/L-m8's census correction ≡ FE §6.5's PRM tally** — the row amends to 4 JS sites / 3 mechanisms; owned by KF.W9.
- **MISSED-H ≡ FE §6.3's 98-unprefixed-tokens row** — nuanced (documented policy, not accident) for the amiga instance; a guard clause in KF.W6, not a new defect.
- **L-i9 ≡ U-audit defect-amiga-suspend-resume §Defect B** — the prior record's live leg is retired by C-16; the structural note stands under its original id.
- **S+2/superlative 6 ≡ census R1 crash-surface row (lane-library §4.6)** — amiga is the *negative* exemplar (zero exposure); the R1 row itself stays owned by KF.W3.
- **C-19/L-m1 ≡ census K3 (`noUnusedLocals`)** — one flag, one wave (KF.W4); this record only extends the stated scope to demo/.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the C-1 discharge + the corpus-wide re-anchor clause (this component: comment-only drift, useAmigaDemo −2 below :73).
- **KF.W4**: C-19/L-m1 (K3's flag closes the class demo-wide).
- **KF.W6**: D-4 · D-5 · D-9 · D-10 · C-6/D-12(S-9) · C-7 · MISSED-B · MISSED-D · L-i5 · MISSED-H(guard) — one coherent stage-surface + canvas-theme-discipline packet.
- **KF.W9**: D-7/C-15/L-m8 (PRM unification, intensity form) + witnesses for D-2/D-4/D-5/D-6/MISSED-A/MISSED-B/MISSED-F + the SS-13 list above.
- **NO-WAVE-OWNER (the amiga scene-repair packet the SS-1/SS-2 authoring must home — no census wave owns demo scene behavior)**: D-1 · L-B1 · D-2(shared idiom) · D-3+C-18+M-3 · D-6/D-11 · D-8+C-9 · L-M1/C-8 · L-M2/C-14 · L-M3/C-11 · L-M4/C-2 · L-M5/C-4 · MISSED-A · MISSED-C · MISSED-E · MISSED-F · MISSED-G · C-16 · C-17 · C-20 · C-10 · C-5 · L-m3 · L-m6 · all INFO rows.

## Verdict

**ADJUDICATED.** The component is the program's sharpest specimen of *earned bespoke with one-line holes*: architecture superlative (single writer, disjoint gestures, firewall-exact, zero hand-rolled rAF), yet its two BLOCKERs are each ONE missing edge in an otherwise well-built gate (a `pose` read while paused; an `isDragging()` term), and its design layer is theme-blind precisely where its CSS layer is theme-fluent. The corpus survives hostile re-reading almost intact — 49 rows confirmed, and the 17 kills are dominated not by false defects but by wrong scopes, wrong numbers, and one substrate-stale BLOCKER that the census had already falsified before the challenge was written. The re-anchor law is not paperwork; it changed a BLOCKER's fate here. Nothing in this record stamps a wave: every disposition above is agglomeration input for the forming X·KF specs, original ids for life.

— end of apotheosis. The challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
