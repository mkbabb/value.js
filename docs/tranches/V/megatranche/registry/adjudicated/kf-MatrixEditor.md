Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component MatrixEditor; receipt read from this seat's own system prompt, not inherited).

# kf-MatrixEditor — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-10 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`, worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus (r2)**: two fresh hostile re-reader returns over `audit/kf-components/MatrixEditor/challenge-{D-design,L-library,C-consumption}.md` — **reader-D** (design-and-usage: 26 confirmation cells · 4 kills · 7 misses · 8 superlatives · 7 unproven) and **reader-L** (library-and-code: 38 confirmation cells · 6 kills · 8 misses · 8 unproven · 10 superlatives). Both returns consumed whole.

**Supersession (recorded, nothing lost).** This record supersedes the 2026-08-09 **r1** apotheosis at this same path, which adjudicated a *different* merged reader return (26 cells · 10 kills · 6 misses). The r1 roster identities **ME-1..ME-29** are preserved for life (anti-rename); every r1 disposition is carried where this corpus does not touch it, and r1 is **corrected in exactly four places**, each at the bytes (rulings 1–4 below): (a) ME-1's BLOCKER escalation **withdrawn** — its leak/wedge clause is dead (reader-L's kill, verified at `playback.ts` by this seat); the row reverts to the banked MAJOR; (b) ME-11 **re-graded MAJOR→MINOR** — its "exactly the perspective terms" attribution is killed by both readers and by this seat's greps; (c) ME-24's "re-widening the store's `SceneId` keyspace" clause **struck** — `SceneId` is a deliberate bare `string`; (d) r1's ME-28 cell confirming D-18 ("unreachable `min-h-[3.5rem]` at 91–119px cells") **overturned** — D-18 is KILLED by reader-D's `justify-center` inversion, and D-6's 91–119px arithmetic is struck with it. Where r1 content is restated it was re-verified this session, except where marked *(r1-carried)*.

**Substrate (re-run by this seat, decisive for three rulings).** `git log -1` → local HEAD `8281638c`; `git status --short -- demo/scenes/cube/matrix-editor/` → all three unit files ` M`; **`git diff origin/master --stat -- demo/scenes/cube/matrix-editor/` → EMPTY** — the dirty worktree is byte-identical to the landed frontier for the entire matrix-editor directory (r1's frontier inversion reproduces exactly). Manifest triangulation re-run **plus one new fact**: worktree `package.json` and `package-lock.json` are BOTH dirty, with **zero** glass-ui hits in either; `origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact (devDependencies) **and `origin/master:package-lock.json` carries glass-ui ×3** — the lockfile limb of the phantom-dep row, which no prior seat had probed, is substrate-dead too. Every line anchor below binds to worktree ≡ `origin/master` content.

**Method (incredulity).** Every load-bearing disagreement — between the two readers, and between either reader and the banked r1 record — was re-derived against the bytes before adoption. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session (all read-only): whole reads of `MatrixEditor.vue` (158 L), `useTransformState.ts` (227 L), `transformMath.ts` (181 L); engine deciders `playback.ts:98` (`running`), `:113-151` (`_run`: `:139` bare `const result = step(now)`, `:116-123` `reschedule`), `:242-247` (`_cleanup` — the sole `_rafId`-nuller and `_resolve`-caller) and `numeric.ts:89` (`respectReducedMotion ?? false`); `CubeScene.vue:50-204` (destructure `:71-78`, `tabsContent` `:170-181`, ribbon `:183-202`, the same-authority comment `:158-169`); `sceneMachine.ts:19-21` (`SceneId = string`, comment verbatim); `vitest.config.ts:42-54` (library `exclude: ["test/demo/**"]`; demo project unreferenced by any script); `package.json:37-46` (`check`/`check:lib`/`lint: depcruise src`/`test:lib`); `ci.yml:46` + `release.yml:47` (both `test:lib` only); eslint census (zero config files, 0 package.json hits); `grep perspective demo/scenes/cube/` (CubeTarget.css:15 container property, only); `quaternionEuler.ts:11/:46` (RAD2DEG — the rotate channel is degrees); `useTransformState` consumer census (CubeScene.vue:78 sole call + 2 prose mentions; `syncTransformations`/`updateTransformations` external consumers = 0); `isGroupPlaying` = declaration-only (`:22`); `matrixValueAt` module-private (`:91` bare `const`); glass-ui `class-names-Cpy5eaBk.js` catch-all `["text-color", /^text-/]` + enumerated `text-(left|center|right|justify|start|end)` group (confirmed at dist); keyframes' own `docs/tranches/C/audit/design-findings.txt:243-245` (the D-2 owner disposition, read verbatim); git substrate probes above. Registry reads: the r1 record at this path (whole), kf-SequencePlayhead (the dirty-manifest pattern law), kf-CubeScene roster via r1's identity table.

## Scoreboard

**41 roster rows, original ids for life — 0 BLOCKER · 21 MAJOR · 18 MINOR · 2 INFO** (r1's 29 identities carried, two re-graded, one clause struck, one subsumed; **12 net-new rows admitted from this corpus's 15 filed misses**, every one re-derived at the bytes; 2 misses PRE-BANKED and folded; 1 miss folded as a rider). **Killed-claims register: 21 entries** — r1's K1–K11 carried (three re-fired against this corpus's own readers) + **K12–K21 new**, including two kills that overturn r1 rulings and four corrections against the fresh readers' confirmation cells. **11 superlatives** (r1's 7 carried, two with standing corrections re-affirmed; 4 added). **11 UNPROVEN residues** → SS-13 (one narrowed to its dark arm by reader-D's theory-proof; one rerouted build-level, r1-carried).

**Headline.** After three challenge axes, one merged re-read, and a full r1 adjudication, **reader-L found the two worst library-consumption defects on this surface, and both verify at the bytes** (rulings 9–10): **ME-30** — editing any translate cell (12/13/14) makes the per-frame `syncTransformations` → deep-watcher → `updateTransformations` loop rebuild the whole matrix from the T·R·S slider triple, silently destroying every manually-edited non-TRS cell and injecting the drag rotation, against a stale in-file comment describing a call path with zero remaining callers; **ME-31** — the Reset path writes `Math.acos(scale)` **radians** into a channel every other consumer reads as **degrees**, and NaN for any diagonal outside [-1,1] (reachable from the slider's own `[0.4, 3]` preset), landing in an uncaught `TypeError` inside a rAF frame. Meanwhile the corpus's former BLOCKER **de-escalates**: reader-L killed the never-settling-promise *leak* clause r1's escalation stood on — the throw terminates the rAF chain and the whole graph is unreachable garbage, verified at `playback.ts:139/:242-247` — so ME-1 reverts to its banked MAJOR. The r2's other structural inversion: reader-D proved the three axes computed every cell dimension from a false premise — `justify-center` on CardContent (`:3`) means the grid never fills the rail, and the `min-h-[3.5rem]` all three axes wrote off as dead is very plausibly the **sole size authority** in the component.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave sketch KF.W0–W10.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **ME-1 severity — reader-D BLOCKER ("the play() promise never settles and RAFPlayback reports running forever") vs reader-L MAJOR + kill of the leak sub-claim vs r1's BLOCKER-at-scope escalation** | **Reader-L's kill SUSTAINED at the bytes; r1's escalation WITHDRAWN; ME-1 reverts to the banked MAJOR (kf-CubeScene L-5/C-6)** | `playback.ts:139` — `const result = step(now)` is bare; a synchronous throw escapes `frame` **before** `reschedule(:116-123)` is reached, so `requestAnimationFrame` is never re-armed: the chain **terminates**, it does not persist. `_cleanup()` (`:242-247`, the sole site nulling `_rafId` and calling `_resolve`) is unreached — the promise never settles — but `void new NumericAnimation(...).play(...)` (`useTransformState.ts:87`) retains no reference, the browser releases an invoked rAF callback, and the entire NumericAnimation→RAFPlayback→pending-promise graph is unreachable and collectable. "Wedged at `running === true`" (`:98`) is true of an object no reference can reach — unobservable, not a leak. What survives is exactly the banked row: an uncaught TypeError per poisoned keystroke (`""`/`"-"`/`"."` — and every negative entry transits `"-"`) and a silently dropped edit, recovering on the next keystroke. Both fresh graders' BLOCKER grounds (reader-D's and r1's) rested on the dead clause; with it gone, MAJOR is unanimous with the bank. |
| 2 | **D-18 — reader-D KILLS ("min-h can never bind" is unsound) vs reader-L CONFIRMS at INFO ("live and merely inert") vs r1's ME-28 confirmed-INFO cell** | **Reader-D's kill SUSTAINED; reader-L's cell and r1's ME-28 clause OVERTURNED; the inverse admitted as net-new ME-33** | `MatrixEditor.vue:3` — `<CardContent class="grid items-center justify-center gap-3 p-3">`, re-read this session. CSS Grid §12.8 stretch-auto-tracks is skipped when `justify-content` ≠ `normal`/`stretch`, so CardContent's implicit `auto` track is max-content-sized; `.matrix-grid`'s `w-full` (`:5`) is a percentage → contributes `auto` to intrinsic sizing; each cell (`:8-11`) has **zero in-flow children** (both `:18` and `:43` are `absolute`), so its only intrinsic contribution is `min-height: 3.5rem` transferred through `aspect-square`. Far from "inert dead weight", the utility is plausibly the sole thing giving any cell any size (≈4×56px + 3×4px ≈ 236px of grid in a 400–512px rail). D-18's premise (grid fills the rail) and D-6's 91–119px arithmetic fall together; D-6's circle conclusion is size-independent and survives (ME-12). Exact rendered dimension = browser-dependent aspect-ratio min-transfer → SS-13. |
| 3 | **D-11's grade — r1 held MAJOR with "exactly the perspective terms"; both fresh readers kill the perspective narrative and grade MINOR** | **Kill SUSTAINED; ME-11 re-graded MAJOR→MINOR, claim corrected** | `grep -rn perspective demo/scenes/cube/` → `CubeTarget.css:15 perspective: 1200px` (an ancestor **property**) + one comment; nothing else. `useTransformState.ts:129-175` composes strictly from `fromTranslation/fromX·Y·ZRotation/fromScaling` — the fourth row of every demo-reachable matrix is [0,0,0,1]; cells 3/7/11 are structurally 0 and the `step: 1` rotate preset can't produce 0.001 either. The only route to the misreading is a user hand-typing sub-0.005 values. The surviving defect is exactly what L-18/C-13 filed: a lossy sub-0.005 display + mixed 0dp/2dp widths down a tabular column. MINOR, both readers concordant. The K6 boundary *(r1-carried, re-affirmed)*: the pre-round is load-bearing for `-0` suppression — cure by per-cell digit policy, never by deleting `Math.round`. |
| 4 | **C-14 — reader-L KILLS ("SceneId is a bare string, deliberately") vs reader-D's tail confirmation ("superKey: string re-widening SceneId") vs r1's ME-24 text** | **Kill SUSTAINED; ME-24's widening clause STRUCK; reader-D's cell corrected** | `sceneMachine.ts:19-21`, read verbatim: `/** The SCENE axis … Kept a bare string so scenes.ts remains the single registry; the reducer never enumerates them. */ export type SceneId = string;` — the sole definition. `superKey: string` (`MatrixEditor.vue:105`) is *exactly* the declared type of the parameter it feeds; nothing is widened, and the tree defends the design in prose. ME-24's surviving substance = L-16 alone (read-once at `:113`, never tracked; construction-only contract undeclared; benign under the constant `SCENE_ID` at CubeScene.vue:60) — still FOLD ≡ kf-CubeScene L-22/C-14. |
| 5 | **C-15 — BOTH fresh readers confirm at MINOR ("module default installed by reference, mutated through the proxy") vs r1's K2 kill** | **r1's K2 RE-SUSTAINED against both readers; their cells REFUTED-as-booked** | `MatrixEditor.vue:115-118` sits inside `<script setup>` — per-instantiation construction, re-read this session. Each mount builds a fresh object; `??=` (`:120`) installs the first instance's copy as the store bucket, which is precisely what a seed is; `:38`'s write-through mutates the bucket, not a shared module singleton. The original hazard class (cross-instance contamination; the store's own `structuredClone` contrast) has no instance here and no observable path — reader-L's own cell concedes "no observable path exists today". A latent no-path non-defect confirmed twice is a registry-blindness event, not a row. |
| 6 | **The phantom-dependency confirmations — reader-L L-21/F-1 at MAJOR ("absent from package.json AND package-lock.json"), reader-D's INFO-tail F-1 clause** | **KILLED — SUBSTRATE-DEAD at the frontier, now in both files** | This seat's triangulation: worktree `package.json` AND `package-lock.json` both dirty (uncommitted deletions), 0 glass-ui hits in each; `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep; **`origin/master:package-lock.json` glass-ui ×3** (new fact — no prior seat probed the lock). ≡ census F-1/SCH-1, FALSIFIED-at-origin (kf-AmigaScene), re-killed at kf-CubeScene, r1 K8/K11, and promoted to *a priori* law at kf-SequencePlayhead K7: **a manifest claim in a kf corpus is inadmissible without a named tree**. Yet another dirty-manifest event for that pattern ledger; residue rides **KF.W0 §B-12** (the uncommitted deletions would revive F-1 if committed). |
| 7 | **Reader-L's ME-6 cell clause "The two gates differ in authority: the panel keys on `storedControls.selectedControl`, the ribbon on `slotProps.selectedControl`"** | **Clause RE-KILLED ≡ r1 K5; ME-6 itself unaffected** | `CubeScene.vue:158-169`, re-read: the in-file comment states the store read is "the in-scope mirror of ribbonContent's gate, both reading the same authority", and r1's K5 (backed by the banked ControlsPaneWrapper topology) already ruled the slot prop IS the same authority's value. Reader-L re-inherited a killed sub-mechanism without new evidence. The mount-ordering window r1 preserved inside the banked L-6/C-15 identity stays preserved (SS-13). |
| 8 | **Citation-inheritance pair: reader-D's S-1 clause "That test runs in CI — the `library` glob subsumes `test/demo/`" vs reader-L's kill; reader-L's superlative 8 "PRM-honest by construction — the strongest form of the property" vs reader-D's PRM miss** | **Both cells CORRECTED — each reader re-inherited a cell the other (and r1) killed** | (a) `vitest.config.ts:42-45`: library project `exclude: ["test/demo/**"]`; demo project (`:51-54`) is referenced by **no script** (`package.json:45-46`); ci.yml:46 + release.yml:47 run `test:lib` only. Reader-L's kill ≡ r1 K1, SUSTAINED; reader-D's S-1 CI clause is the challenge's false cell repeated. (b) `numeric.ts:89` `respectReducedMotion ?? false`; `useTransformState.ts:87-92` (300ms per keystroke) and `:105-107` (500ms `easeInBounce` reset) never set it — the component is the sole trigger of two ungated engine motions while `useCubeDemo` honours PRM one file over. Reader-D's miss is CONFIRMED in mechanism but **PRE-BANKED** ≡ r1 K10 ≡ kf-CubeScene D-8+M-7 (→ KF.W9, the two motions named verbatim in the banked row) — folded, not re-booked; reader-L's superlative 8 repeats the S-D2 cell r1 half-killed. The CSS half of the superlative remains true. |
| 9 | **Reader-L's miss #1 — the translate-edit feedback loop** (filed MAJOR) | **ADMITTED at MAJOR — net-new roster row ME-30, the r2 headline** | Chain re-derived whole at the bytes: `updateMatrixCell`'s per-frame callback runs `syncTransformations()` (`useTransformState.ts:91`) → `:63-65` write `translate.x/y/z = values[12]/[13]/[14]` every frame; value-identical writes are suppressed by Vue, but a translate-cell edit genuinely changes one → the `deep: true` watcher (`:197-215`) fires with `isGroupStarted` false (`:200`, the ordinary state) → rAF → `updateTransformations()` (`:205`) → `:132-169` composes strictly T·R·S from `transformSliderValues` → `:171-172` assign BOTH `matrix3dEnd` AND `matrix3dStart` — destroying every manually-edited cosine/shear (1/2/4/6/8/9), perspective (3/7/11) and w (15) cell, injecting whatever rotate/scale the drag/slider channels hold, and racing the in-flight 300ms animation whose next frame runs `withMatrixCell` against the rebuilt matrix. The excusing comment (`:193-195`, "matrix editor sliders still call updateTransformations() explicitly for their own path") is stale: `updateTransformations` is returned (`:225`) and `grep` finds **zero** external consumers — CubeScene destructures six of eight keys (`:71-78`), not it. No banked row covers this (checked against r1's identity table + kf-CubeScene roster): **first booking**. |
| 10 | **Reader-L's miss #2 — `syncTransformations(reset)` writes radians/NaN into the degrees channel** (filed MAJOR) | **ADMITTED at MAJOR — net-new roster row ME-31** | `useTransformState.ts:69-71`: `rotate.x/y/z = Math.acos(values[0]/[5]/[10])` — radians (`Math.acos` range [0,π]) into the channel `quaternionEuler.ts:11/:46` sources in DEGREES and `:145` itself converts `* (Math.PI/180)`. Beyond the unit error: `Math.acos` is NaN outside [-1,1], and the scale preset's own bounds are `[0.4, 3]` (`transformMath.ts:18`). Chain: set cell 0 = 2 → Reset (`CubeScene.vue:189` → `:177-182` → `animateUpdateMatrix(…, true)`) → frame 1 `syncTransformations(true)` with `values[0] ≈ 2` → `rotate.x = NaN` ≠ 0 → deep watcher → rAF → `updateTransformations` → `fromXRotation(NaN)` → `createMatrix` throws `TypeError` (`transformMath.ts:78-83`) inside the rAF frame — uncaught (playback has no try/catch; demo has zero `errorCaptured`). Even inside [-1,1] the intermediate frames write junk rotate/scale that the watcher consumes mid-reset. Conceptually `acos(scale-diagonal)` conflates scale with rotation — wrong math, not just wrong units. First booking. |
| 11 | **Reader-D's D-2 BLOCKER vs reader-L's MAJOR-with-ruled-row citation** | **MAJOR — reader-L and r1 sustained; reader-D's grade recorded as dissent against a ruled row** | `docs/tranches/C/audit/design-findings.txt:243-245`, read verbatim this session: "**[high] MatrixEditor raw `<Slider>` … no accessible name** — disposition: **SHIP (demo-owned)**", with the prescribed `:aria-label` cure from `matrixCellMeta`. A standing owner disposition bounds re-grading absent new facts; reader-D's "a disposition is not a measurement" is true and irrelevant — severity in this program prices dispositions in. The zero-names census (17 controls) re-confirmed by grep this session. |

**Adjudicator's corrections register (against this corpus's readers):** (1) reader-D's BLOCKER grades on ME-1/ME-2/D-2 — all three demoted (rulings 1, 11; ME-2 stays MAJOR with r1: a one-character class bug with a surviving focus-only cue is not a blocker); (2) reader-D's S-1 CI clause — ruling 8a; (3) reader-L's superlative-8 PRM clause — ruling 8b; (4) both readers' C-15 confirmations — ruling 5; (5) reader-L's phantom-dep MAJOR — ruling 6; (6) reader-L's two-authorities clause — ruling 7; (7) both readers' provenance-kill *consequence* clauses ("the entire audited surface is unbanked work" / "every finding is against code that has not landed") — SUSTAINED-AS-CORRECTED ≡ r1 K7: the kill of the L-doc's "CLEAN" sentence is right, the consequence is inverted at the frontier (this seat's empty `origin/master` diff). Third and fourth occurrences of the one-baseline-short pattern; it is now standing law here: **a dirty-worktree claim without a named comparison baseline is inadmissible.**

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0–W10** per the census wave sketch where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake, agglomerating into the cube scene-repair packet whose core is the matrix-editor family (per kf-CubeScene's booking). Anchors bind to worktree ≡ `origin/master`; re-anchor before any cure.

### MAJOR — carried from r1 (dispositions unchanged unless marked)

| id | merged ids | claim (compressed; r1 carries the full cell) | terminal disposition |
|---|---|---|---|
| **ME-1** | L-2 / C-1 | Unvalidated string emit (`""`/`"-"`/`"."`) → `parseFloat` NaN unguarded → `withMatrixCell` throws inside the engine's rAF frame; uncaught TypeError per poisoned keystroke, edit silently dropped. **r2 correction (ruling 1): the escalation rider is WITHDRAWN** — the throw terminates its own rAF chain before `reschedule`; the never-settling promise is unreachable garbage, not a leak. **Severity: MAJOR (banked grade restored)** | **FOLD ≡ kf-CubeScene L-5/C-6 (S-9)**. NO-WAVE-OWNER — family core; cure evaluates `/number-field` (number payload makes the class unreachable — ME-18) + a `Number.isFinite` gate at the seam either way |
| **ME-2** | L-3 / C-3 / D-1 | Missing trailing space fuses `text-ellipsisfont-bold`; `text-ellipsis` AND `font-bold` both die on the selected cell; only `focus:font-bold` survives, so bold means *focused*, not *selected*. Sibling literal `:46` carries the space. r2 riders (reader-D, adopted): the fused token classifies into `cn`'s `text-color` bucket with **no eviction** of `text-center`/`text-body` (three distinct groups — blast radius exactly as filed); had the space been present `font-bold` would have won on layer order. Reader-D's BLOCKER demoted (corrections register) | **NO-WAVE-OWNER** — one-character cure, one commit with ME-16 |
| **ME-3** | L-5 / C-2 / D-8 | `:start`/`:end`/`:step` not InputProps (**18 members exactly** — r1's correction of the banked "21" stands); fall through `inheritAttrs:false` onto a native `<input type="text">`; nothing clamps anywhere downstream (`:78-93` re-read) | **FOLD ≡ kf-CubeScene L-5/C-6** |
| **ME-4** | L-8 / C-10 | `Matrix3dCall.args: readonly CssValue[]` (`transformMath.ts:42`) discards the module's own `MatrixScalar`; `payload.value` inexpressible at both consumer reads; forces 16× four-way revalidation per read. Cure: narrow to `readonly MatrixScalar[]` — soundness argument adopted (sole constructor builds via `matrixScalar`) | **NO-WAVE-OWNER** — one-line narrowing; ME-41 and ME-27 ride the same commit |
| **ME-5** | C-9 / L-17 | No vue-tsc anywhere; `check` bare tsc; CI runs `check:lib` (src/ only); `*.vue` ambiently `any`. The enabling condition for ME-3/ME-4/ME-6 and the `:126` `: number` annotation lie | **FOLD ≡ KF-APP-4 (→ KF.W4)**; ME-32 extends the gate row to tests + lint |
| **ME-6** | L-6 / C-5 / D-10 | `selectedMatrixCell` absent from `matrixOptions?: { fixed: boolean }` (store `:26`); a leaf invents, defaults (`??=` `:120`) and persists a store field a sibling reads, violating the store's own written boundary (`:90-94`). **r2 (ruling 7): the "two authorities" sub-clause stays dead (r1 K5)** | **FOLD ≡ KF-APP-30 + kf-CubeScene M-8** — seed moves into the store's migration boundary; type row gains the field |
| **ME-7** | L-4 / C-4 | Every emission spawns a fresh un-cancelled 300ms NumericAnimation, private rAF chain, no handle/dedupe/disposal; Slider wires the continuous emit while `valueCommit` ships unused; engine ships `updateKeyframe` (retarget primitive) with zero demo consumers *(r1 rider)* | **FOLD ≡ kf-CubeScene C-7/L-13/L-14**; ME-30/ME-31 sharpen the same cure (retain-one-instance + retarget + stop-on-dispose) |
| **ME-8** | L-7 | Controlled `:model-value` derives from the very ref the 300ms animation rewrites per frame; Input is `passive:true` + `vModelDynamic` — DOM value re-synced against the user mid-entry. Reader-D's note adopted: arguably the worst *design* defect — typing is the primary interaction and it is structurally broken; the D axis missed it | **FOLD ≡ kf-CubeScene C-7/L-13/L-14**; caret witness → SS-13 |
| **ME-9** | D-2 / C-12 | Seventeen interactive controls, zero accessible names (grep = 0, re-run); `forwardedAttrs` would pass `aria-label` verbatim; `LabeledSlider` is the in-tree idiom one scene over. **Severity MAJOR per ruling 11 (owner disposition SHIP, design-findings.txt:243-245, verified)** | **NO-WAVE-OWNER** — rides the family cure; the ruled `:aria-label` prescription is the floor |
| **ME-10** | D-3 / C-16 | Slider preset domain-wrong for 10 of 16 cells — `"P"` and `""` both land on `rotate [-360,360] step 1`; one step = 50% of a cosine's legal range; negative (mirror) scale unreachable (`[0.4, 3]`) | **NO-WAVE-OWNER** — per-cell-class domains are the family spec table |
| **ME-12** | D-6 | Glass Input half-undressed: `bg-transparent p-0` win on layer order; 1.5px border, pill radius, rim shadow, backdrop blur survive — 16 circles in 16 square `rounded-lg` cells. **r2 correction (ruling 2): the 91–119px cell arithmetic is STRUCK with D-18's kill; the circle conclusion is size-independent and stands** | **NO-WAVE-OWNER** — presentation arm; `/number-field` supersedes if adopted; variants relay glass-ui BH |
| **ME-13** | D-4 | Sole per-cell identification at `opacity-20` light / `opacity-75` dark fails the 3:1 large-text bar. **r2 rider (reader-D, adopted): the light arm is theory-proof** — at α=0.2 the maximum achievable contrast is 1.606:1 (black/white) or 1.662:1 (white/black); no composited backdrop can rescue it. SS-13 narrows to the dark arm | **KF.W6** (theme-aware ink, not alpha) |
| **ME-14** | D-9 | `matrixOptions.fixed` write-only; ribbon Lock/Free toggles nothing but its own caption; evident consumer (`.toFixed(2)`) hard-coded | **FOLD ≡ kf-CubeScene C-16+D-10** |
| **ME-15** | D-5 | 10 of 16 labels duplicated or wrong: 3/7/11 all `P`+sub-`w`; six bare letters each twice; no description field exists to do better | **NO-WAVE-OWNER** — same spec table as ME-10/ME-11; ME-35 compounds |
| **ME-16** | L-10 / D-7 | Selection is `@click`-only (`:37-39` sole writer); Tab-focus cannot retarget the Slider; fused with ME-2 the sole surviving weight cue marks the FOCUSED cell while the Slider edits a different one | **NO-WAVE-OWNER** — one cure with ME-2/ME-9 |
| **ME-17** | L-1 / C-5 | Untrusted share-link JSON reaches the store wholesale (`isValidState` shape-only → `Object.assign`); `matrixCellMeta[…]` deref ×3 unguarded (`:75-89`); no shipped writer authors the poison (r1 K3/K4 sustained — both readers concordant this round) | **FOLD ≡ KF-APP-30**, share-link rider; localStorage survey + blast radius → SS-13 |
| **ME-18** | C-8 | Hand-rolled bounded number field while glass-ui 7.0.0 ships `/number-field` (number-payload emit), reached zero times demo-wide; anchor = `index.d.ts` + exports map (the `types.d.ts` citation stays killed — r1 K9 ≡ reader-L's kill, concordant) | **FOLD ≡ census S-9 → KF.W6** — evaluate, not mechanical swap (the cell doubles as the selection click-target) |

### MAJOR — net-new this corpus

| id | source | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **ME-30** | reader-L miss #1 | **The translate-edit feedback loop**: editing cell 12/13/14 → per-frame `syncTransformations` genuinely changes `translate.*` → deep watcher (gate `isGroupStarted` false) → rAF → `updateTransformations` rebuilds BOTH `matrix3dEnd`/`matrix3dStart` from the T·R·S triple — **destroying every manual non-TRS cell edit (1/2/3/4/6/7/8/9/11/15), injecting the drag rotation, and racing the in-flight 300ms animation**. The excusing comment `:193-195` is stale: zero external callers of `updateTransformations` (this seat's grep; CubeScene destructures 6 of 8 keys) | ruling 9; `useTransformState.ts:63-65/:91/:171-172/:193-215`; `CubeScene.vue:71-78` | **NO-WAVE-OWNER — family core, one spec with ME-7/ME-31**: the sync/watch topology is re-authored (one writer per channel; the watcher's rebuild must never consume a mid-animation mirror); born-RED gate: edit cell 1, then cell 12 — assert cell 1 survives |
| **ME-31** | reader-L miss #2 | **Reset writes `Math.acos(diagonal)` radians into the degrees rotate channel; NaN outside [-1,1]** — reachable from the slider's own scale preset (`[0.4, 3]`); NaN → watcher → `fromXRotation(NaN)` → `createMatrix` TypeError uncaught in rAF; NaN also flows out `v-model:transform` to OrbitalDrag's quaternion source-of-truth (reader-L's chain, spot-verified to the throw site) | ruling 10; `useTransformState.ts:69-75/:105-126/:177-182`; `quaternionEuler.ts:11/:46`; `transformMath.ts:78-83` | **NO-WAVE-OWNER — family core**: the reset arm's math is respecified (decompose properly or don't sync rotate/scale mid-reset); born-RED gate: cell 0 = 2 → Reset → no throw, cube lands identity. Visible end-state → SS-13 |
| **ME-32** | reader-L misses #3+#4; subsumes r1's ME-29 rider | **The demo's correctness surface is gate-free in all three dimensions**: types (ME-5, banked), tests (the `demo` vitest project is referenced by NO script and NO workflow — the only tests of transformMath never run on merge or release), and lint (**no ESLint at all**: zero configs, 0 package.json hits, `lint: depcruise src`) — so template classes only a linter sees (the `:11` keyless `v-for`, the only one in the scenes tree) are structurally invisible | ruling 8a; `vitest.config.ts:42-54`; `package.json:37-46`; ci.yml:46/release.yml:47; eslint census | **KF.W4** (one gate wave with ME-5/KF-APP-4): wire `test:demo` into CI, add eslint-plugin-vue essentials, `noUnusedLocals`; ME-29 (keyless v-for) is this row's exhibit now |
| **ME-33** | reader-D miss #1 (the D-18 inverse) | **The editor is intrinsically sized, not rail-filling**: `justify-center` on CardContent (`:3`) skips grid auto-track stretching; `w-full` contributes `auto`; cells have zero in-flow content — `min-h-[3.5rem]` is the sole size authority, giving ≈236px of matrix centred in a 400–512px rail. One-word cure (`justify-stretch` or drop the utility). All three axes and r1 computed cell sizes from the rail-filling premise | ruling 2; `MatrixEditor.vue:3/:5-6/:8-11/:18/:43` | **NO-WAVE-OWNER** — presentation arm with ME-12; rendered dimension → SS-13 (browser aspect-ratio min-transfer) |

### MINOR

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| **ME-11** | D-11 / L-18 / C-13 — **re-graded MAJOR→MINOR (ruling 3)** | Formatter reports sub-0.005 as `"0"` and mixes 0dp/2dp widths down a tabular column; the perspective attribution is DEAD (cells 3/7/11 structurally 0 in every demo-reachable state); the pre-round is load-bearing for `-0` (r1 K6) — cure by per-cell digit policy | **NO-WAVE-OWNER** — one formatter with ME-10's cell-class table |
| **ME-19** | L-13 *(r1-carried; re-grepped)* | Dead imports `MATRIX_AXES`/`transformSliderOptions` (`:9-10`, import-only) + zero-consumer type re-export (`:19`) | **FOLD ≡ kf-CubeScene L-10/L-11/L-12** |
| **ME-20** | L-9 / C-7 / D-14 *(r1-carried; re-grepped: `:110/:136/:137` only)* | `resetMatrix` emit chain dead in all three directions; live reset is the parent ribbon's direct call (`:189`) | **FOLD ≡ kf-CubeScene L-26/C-12b** |
| **ME-21** | L-15 *(r1-carried)* | Watcher's scheduled rAF (`:203`) never cancelled; unmount inside the window paints a detached element; no lifecycle import in the file (`:3`) | **FOLD ≡ kf-CubeScene C-7/L-13/L-14** |
| **ME-22** | L-12 / C-17 *(r1-carried)* | Per-frame allocation + invalidation storm inverting the engine's zero-allocation contract inside its own callback. r2 rider (reader-L): true per-frame cost = 48 four-way validations (+16 per emit at `:80`), not 32; `tabsContent` re-renders the 17-control subtree per frame | **FOLD ≡ kf-CubeScene L-9** |
| **ME-23** | L-14 / D-21 *(r1-carried)* | Split error posture: guarded RangeError at `:126-134` (duplicating transformMath's message, dropping its type guards) vs three unguarded reads at `:75-89`; a third read path at `:29` | **NO-WAVE-OWNER** — one guard policy; ME-41 names the root cause |
| **ME-24** | L-16 (C-14 clause STRUCK — ruling 4) | `superKey` read once at setup, never tracked; construction-only contract undeclared; benign under constant `SCENE_ID` | **FOLD ≡ kf-CubeScene L-22/C-14** |
| **ME-25** | C-11 *(r1-carried; + reader-L's cite fix: `Input` from `/forms`, not the root barrel)* | Two subpath policies in one import block; `./card`/`./slider` first-class and unused | **FOLD ≡ kf-CubeScene C-19a**; bundle cost rerouted build-level |
| **ME-26** | D-15 / D-16 / D-17 *(r1-carried)* | Latent trio: no RTL guard on a semantically-ordered grid (a matrix must opt OUT of RTL, undocumented); forced-colors can't remap `opacity` (the one dimming glass-ui's fallbacks can't repair); 2px+8px focus ring across a 4px gutter, no clip | **NO-WAVE-OWNER**; D-17 percept → SS-13 |
| **ME-27** | r1 miss #5 *(r1-carried; type re-verified: `MatrixCellMeta.sliderOptions` drops the `value` key runtime objects carry; same module object identity per class, fully mutable)* | Shared-singleton `sliderOptions` aliasing — the exact class the store `structuredClone`s against | **NO-WAVE-OWNER** — `readonly` + `satisfies` + freeze in the ME-4 commit |
| **ME-34** | reader-D miss #3 — NEW | **The `<sub>` is destroyed by the flex context**: the label div (`:43-45`) is `flex items-center`; the glyph text node and `<sub>` are two centre-aligned flex items; `vertical-align` does not apply to flex items (Flexbox §4), so only `font-size: smaller` survives — `T` and `x` render as adjacent letters, not `T_x`. Compounds ME-15; qualifies superlative S-9 | **NO-WAVE-OWNER** — family labeling cure (`align-items: baseline`, inline wrapper, or grid `place-content`) |
| **ME-35** | reader-D miss #4 — NEW | **Two contradictory conventions on one surface**: `matrix3d()` args are column-major, but `v-for` + `grid-cols-4` lays args 0–3 as the first visual ROW — the display is the transpose — while labels (`getAxisFromIx = i % 4`) name row-within-column. Translation triple lands in the bottom row (row-vector convention) over column-vector data. Zero reconciling prose (D-5's register noted the column-major fact; no axis drew the transpose consequence) | **NO-WAVE-OWNER** — cheapest fix in the component: a caption or `grid-flow-col`; one cure with ME-15 |
| **ME-36** | reader-D miss #5 — NEW | **~21.5% of every cell is a dead zone**: the Input is circle-clipped (ME-12), border-radius clips hit-testing, and `@click` — the only selection path — lives on the Input; the four corners carry the cell's own `rounded-lg shadow-sm` yet neither focus nor select. Worsens ME-16: the mouse path is lossy too | **NO-WAVE-OWNER** — dies with ME-12's cure; corner-click probe → SS-13 |
| **ME-37** | reader-D miss #6 — NEW | Three inert layout utilities in twelve lines: `justify-items-center` on a flex container (`:44-45`, does not apply); `grid` on a wrapper with zero in-flow children (`:9`, functionally block); `justify-items-stretch` restating the grid default (`:6`) | **NO-WAVE-OWNER** — hygiene, rides any touching wave (ME-28's class) |
| **ME-38** | reader-L miss #5 — NEW | `isGroupPlaying` is a required positional parameter the composable never reads (declaration `:22` is its sole occurrence; only `isGroupStarted` is used, once, `:200`); CubeScene dutifully feeds it (`:78`); undroppable without a coordinated edit; invisible per ME-32 | **NO-WAVE-OWNER** — dies in the ME-30 re-authoring |
| **ME-39** | reader-L miss #6 — NEW | `syncTransformations` + `updateTransformations` returned with **zero consumers tree-wide** (grep, this session) — dead public surface on the component's sole data source, and the mechanical reason ME-30's stale comment survived: the path it names has no caller left to break. Same shape as ME-20 one file over | **NO-WAVE-OWNER** — the ME-30 spec deletes or privatizes them; the `:193-195` comment correction is mandatory in that commit |
| **ME-40** | reader-L miss #7 — NEW | glass-ui `cn`'s final text rule is the catch-all `["text-color", /^text-/]` (confirmed at dist this session): `text-ellipsis` classifies as a text COLOR, so any consumer pairing it with a real `text-*` colour gets silent last-wins eviction — and unknown tokens are `cn`'s normal case, which is why ME-2's fused token produced zero diagnostics. Latent here (no colour utility passed); producer-class hazard | **GLASS-OWNED → BH relay** (owner edict: every glass-ui-level finding relays); recorded MINOR-latent; no local workaround booked |
| **ME-41** | reader-L miss #8 — NEW | `matrixValueAt` is module-private (`transformMath.ts:91`), so no guarded single-cell accessor is reachable: the demo pays whole-matrix revalidation per one-cell operation (one throwaway 16-cell validation per emit at `:80`; ×2 more per frame) and MatrixEditor hand-rolled its own unguarded reader (`:126-134`) BECAUSE the guarded one is unexported — the root cause ME-23 records the symptom of | **NO-WAVE-OWNER** — export it (or land ME-4's narrowing, which collapses the class); one commit with ME-4 |

### INFO

| id | claim | disposition |
|---|---|---|
| **ME-28** | The verified tail *(r1-carried, one cell struck)*: `toLocaleLowerCase` vs `toLowerCase` on the same datum (both no-ops); fixed 25.9px decoration over fluid editable value; hand-rolled mono rung (`text-mono-small` ships, 2 hits in the artifact); dead `matrix-grid` hook (0 hits / 571,142 B); dead one-hop `--color` (cargo-culted from CubeAxisLines where descendants DO read it — provenance visible in the shipped CSS). **The D-18 "unreachable min-h" cell is STRUCK (ruling 2 — D-18 is killed, the utility is load-bearing)** | **NO-WAVE-OWNER** — hygiene |
| **ME-29** | The `:11` keyless `v-for` *(r1-carried)* — now **subsumed as ME-32's exhibit** (the only keyless v-for in the scenes tree; `vue/require-v-for-key` is an essentials rule nothing can run) | rides ME-32 → KF.W4 |

## Killed-claims register — 21 entries

r1's K1–K11 are carried in force (compressed); K12–K21 are this corpus's adjudications.

| # | killed claim | adjudication |
|---|---|---|
| K1 *(r1)* | S★-1's CI half ("the library glob subsumes test/demo/") | SUSTAINED — re-verified this session (`vitest.config.ts:44` excludes; no script/workflow runs the demo project). **Re-fired against reader-D's S-1 cell (ruling 8a)** |
| K2 *(r1)* | C-15 "module singleton mutated through the proxy" | SUSTAINED — **re-fired against BOTH fresh readers (ruling 5)**: `<script setup>` scope, per-instance, no observable path |
| K3 *(r1)* | D-10's ordering mechanism (toggle authors the poison shape) | SUSTAINED — `CubeScene.vue:194` writes THROUGH `matrixOptions`; consumes, never produces. Reader-D's kill concordant |
| K4 *(r1)* | L-1 at BLOCKER | SUSTAINED → MAJOR (ME-17); both fresh readers concordant this round |
| K5 *(r1)* | L-6's "two authorities" sub-mechanism | SUSTAINED — **re-fired against reader-L's ME-6 cell (ruling 7)**; the mount-ordering window stays preserved inside the banked identity |
| K6 *(r1)* | "the Math.round is dead work" (three axes, one cure) | SUSTAINED — the pre-round is load-bearing for `-0`; ME-11 carries the boundary |
| K7 *(r1)* | challenge-L:6's tree basis ("MatrixEditor.vue itself CLEAN") | SUSTAINED-AS-CORRECTED — the sentence is false vs local HEAD; the consequence is INVERTED at the frontier (worktree ≡ `origin/master`, empty diff re-run this session). **Both fresh readers repeated the consequence error (corrections register #7)** |
| K8 *(r1)* | F-1 as consumed by the axes ("fails under npm ci") | SUSTAINED-AS-CORRECTED ≡ census F-1/SCH-1 → KF.W0 §B-12 — **extended by K17 below** |
| K9 *(r1)* | C-8's `types.d.ts` anchor + build-artifact count drift | SUSTAINED — reader-L's kill concordant (no `types.d.ts` in the installed package); artifact = 571,142 B |
| K10 *(r1)* | S-D2 "PRM-honest by construction — CANNOT lie about PRM" | HALF-KILL SUSTAINED — CSS half true; the component is the sole trigger of two ungated engine motions (`?? false` re-verified at `numeric.ts:89`). Substance ≡ kf-CubeScene D-8+M-7 → KF.W9. **Re-fired against reader-L's superlative 8 (ruling 8b)** |
| K11 *(r1)* | The 6.0.0-drift miss ("invalidates every glass-ui API verdict") | SUSTAINED — origin pins 7.0.0 exact = installed |
| **K12** | **D-18** ("min-h-[3.5rem] can never bind — inert dead weight"; the 91–119px cell figures) — reader-D's kill vs reader-L's INFO confirmation vs r1's ME-28 cell | **KILL SUSTAINED (ruling 2) — OVERTURNS r1's ME-28 cell and corrects reader-L**: `justify-center` skips track stretching; zero in-flow cell content; the min-h is plausibly the sole size authority. D-6's circle conclusion survives; its arithmetic does not. Inverse admitted as ME-33 |
| **K13** | **The leak sub-claim** ("one permanently-pending promise per keystroke; `running` true forever" — challenge-L:86-87, C:97, reader-D's ME-1 cell, r1's escalation rider) | **KILL SUSTAINED (ruling 1) — OVERTURNS r1's ruling 5**: the throw escapes before `reschedule` → chain terminates; graph unreachable and collectable; nothing accumulates, nothing observable. Verified at `playback.ts:139/:116-123/:242-247` by this seat. ME-1 reverts to banked MAJOR |
| **K14** | **D-11's headline** ("perspective(1000px) contributes −0.001 … user concludes there is no perspective while the cube is visibly projected") — both readers | **KILL SUSTAINED (ruling 3)**: perspective is a CSS ancestor property (`CubeTarget.css:15`); the composition chain never writes 3/7/11; ME-11 re-graded MINOR |
| **K15** | **C-14** ("superKey: string re-widens the SceneId union") — reader-L's kill vs reader-D's tail confirmation | **KILL SUSTAINED (ruling 4)**: `SceneId = string`, deliberate, comment verbatim; ME-24's clause struck; reader-D's cell corrected |
| **K16** | Both fresh readers' C-15 confirmations | **KILLED by this seat ≡ K2 (ruling 5)** — a latent no-path non-defect; registry-blindness event |
| **K17** | Reader-L's L-21/F-1 MAJOR + reader-D's INFO F-1 clause ("absent from package.json AND package-lock.json … unresolvable under npm ci") | **KILLED — SUBSTRATE-DEAD (ruling 6), with this seat's new fact**: origin lock carries glass-ui ×3; manifest devDep 7.0.0 exact. Another dirty-manifest event for the kf-SequencePlayhead *a priori* pattern; residue = KF.W0 §B-12 |
| **K18** | Reader-L's "two authorities" clause inside ME-6 | **KILLED ≡ K5 (ruling 7)** — re-inheritance of a killed sub-mechanism; the in-file comment (`CubeScene.vue:158-169`) states the same-authority fact |
| **K19** | Reader-D's D-2 BLOCKER grade | **SEVERITY KILL (ruling 11)** → MAJOR: the owner disposition (SHIP, demo-owned, prescribed cure) verified verbatim at `design-findings.txt:243-245` |
| **K20** | Reader-D's ME-1 BLOCKER cell + ME-2 BLOCKER grade | **SEVERITY KILLS** — ME-1 per K13; ME-2 stays MAJOR (a one-character class bug with a surviving focus cue; r1 grade, reader-L concordant) |
| **K21** | Reader-D's PRM miss as a *new* row; reader-D's 3×-mount-adjacent framings | **BOOKING KILLS** — mechanisms confirmed, identities PRE-BANKED (kf-CubeScene D-8+M-7 → KF.W9; the readers did not consult the registry). Fold, never re-book |

## Identity-guard folds (never re-booked)

- **ME-1/ME-3/ME-18 ≡ kf-CubeScene L-5/C-6 (S-9)** — one banked identity, three limbs; r2 contribution: the escalation rider is withdrawn (K13), restoring grade concordance with the bank.
- **ME-7/ME-8/ME-21 ≡ kf-CubeScene C-7/L-13/L-14** — the undisposed-animation identity; ME-30/ME-31 are its two new sharpest instances and share its cure spec.
- **ME-5 (+ME-32 extension) ≡ KF-APP-4 → KF.W4** · **ME-6 ≡ KF-APP-30 + kf-CubeScene M-8** · **ME-17 ≡ KF-APP-30** · **ME-14 ≡ kf-CubeScene C-16+D-10** · **ME-19 ≡ L-10/L-11/L-12** · **ME-20 ≡ L-26/C-12b** · **ME-22 ≡ L-9** · **ME-24 ≡ L-22/C-14** · **ME-25 ≡ C-19a** *(all r1-carried)*.
- **K10's substance ≡ kf-CubeScene D-8+M-7 → KF.W9**; **K17 ≡ census F-1/SCH-1 → KF.W0 §B-12**.
- **ME-30, ME-31, ME-33..ME-41: first bookings** — checked against r1's identity table and the banked kf-CubeScene roster; no prior row covers the sync-topology loop, the reset math, the intrinsic-sizing inversion, or the gate-free lint/test surface at this scope. Any later kf-CubeScene-family adjudication folds its findings on these mechanisms HERE (anti-rename).
- **ME-40 → glass-ui BH relay** (owner edict) — the `cn` catch-all is producer-owned; this registry keeps the latent record only.

## Superlatives — 11 (provenance: corpus → readers → this seat)

1. **`transformMath.ts` as a value module** *(r1 S1, both readers concordant)*: pure, total, immutable by construction, fail-explicit with named indices; the unit tests assert non-mutation and both rejection paths — **and still do not run in any gate** (K1; now ME-32's charge, so the module keeps its credit and the gate keeps the blame).
2. **The `isGroupStarted` gate + rAF debounce** *(r1 S2)*: the hazard named at the seam, cured with the correct clock. r2 qualification: its trailing sentence is now ME-30's stale comment — the mechanism superlative stands, its documentation does not.
3. **The `z-10` provenance comment** *(r1 S3)* — **sharpened by reader-L, adopted**: the comment is not merely accurate, it is **load-bearing** — both children are absolute and the decorative label FOLLOWS the Input in DOM order, so without `z-10` the label would paint over the field and swallow every click, including the only selection path. Three comment lines guarding the entire interaction.
4. **Engine-payload validation at `:109-117`** *(r1 S4)*: refuses to trust a dynamically-keyed record even from its own engine; the standing irony (meticulous on the engine's output, absent on its own input — ME-1) stands.
5. **Container-level glass vocabulary, landed** *(r1 S5, caveat inverted at the frontier — re-affirmed)*: `<Card cartoon tier="quiet">` is origin-current, API-correct against the exact 7.0.0 the frontier pins.
6. **Pure-view data flow** *(r1 S6)*: zero `ref(`/`watch(`/`computed(` in the SFC; the empty state structurally unreachable.
7. **The value.js edge is type-only and R1-clean** *(r1 S7)*: `import type` under `verbatimModuleSyntax`; no parser entered.
8. **NEW (reader-L) — `RAFPlayback`'s generation-guarded core is why the demo's worst defect stays bounded**: one scheduler behind three entries, `_gen` making stop-plus-restart unable to arm a second chain, the arrow-field bind rationale documented in place. Verified in this seat's `:113-151` read — and it is the very mechanism that decides K13: the library's discipline bounds the demo's carelessness.
9. **NEW (reader-D) — the `}}<sub>` line break at `:51-52`**: closing the interpolation flush against `<sub>` to suppress the whitespace text node — a detail most authors never notice. ME-34 shows the flex context then defeats the payoff — which makes the care more poignant, not less real (reader-D's words, adopted).
10. **NEW (both readers) — `--axis-w: var(--foreground)`**: the homogeneous column deliberately gets a neutral, not an invented fourth hue; the C axis's hypothesized missing-token bug was raised and the tree defended itself. A near-miss defended is worth as much as one found. Reader-D's INFO rider stands beside it: the token language's only consumer is a glyph at α=0.2 — the superlative and ME-13 describe the same four rules, recorded so the remediation fixes the opacity rather than congratulating the tokens.
11. **NEW (this seat) — the corpus's falsifier discipline held under two hostile rounds**: across ~59 filed defects, two full re-reads and two adjudications, the substantive kill count stands at single digits, every axis stated its falsifiers, and the readers' productive disagreements (BLOCKER-vs-MAJOR ×3, kill-vs-confirm ×2) were precisely where the truth needed a third instrument. The failure mode that recurs is never derivation — it is *not reading the registry* (K16, K21) and *not naming the baseline* (K7, K17).

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **ME-8 caret/pixel clobber** — mechanism source-closed; whether Vue's textual-equality skip hides the rewrite is live-only. Type `-12` into a translate cell mid-animation.
2. **ME-13 composited contrast — dark arm only** (r2 narrowing: the light arm is theory-proof at α=0.2, ceiling 1.662:1 — no probe needed). Measure the dark-arm glyph over the live quiet-tier glass.
3. **ME-17 localStorage survey** — any real `{fixed}`-only bucket (statically: only crafted links or pre-feature storage).
4. **ME-17/ME-31 blast radius** — does a render/rAF throw blank the tabpanel alone or an ancestor subtree (zero boundaries demo-wide).
5. **ME-26/D-17 focus-ring bleed percept** — geometry decided; annoyance is not.
6. **The 3×-mount DOM count** — rides the probe banked at kf-CubeScene L-6/C-15; one probe, two banked predictions.
7. **NEW — ME-33 rendered dimension**: `getBoundingClientRect()` of `.matrix-grid` and one cell at desktop width — ≈236px centred block, or a collapse; settles the browser's aspect-ratio min-transfer in one screenshot.
8. **NEW — ME-2/ME-16 selected-cell legibility**: is a focus-only bold cue perceptible at all across a 4×4 monospace grid (the class string is executed fact; the visual verdict is not).
9. **NEW — ME-31 visible end-state**: after cell 0 = 2 → Reset, does the cube blank, freeze, or snap (the throw is proven; the pixels are not).
10. **NEW — ME-36 corner dead-zone**: click a cell corner; assert neither focus nor selection fires.
11. **NEW — ME-30 witness capture**: edit cell 1, then cell 12; record cell 1's value being destroyed — the born-RED gate's live twin.

Rerouted, not SS-13 *(r1-carried)*: **ME-25 bundle-graph cost** → build-level analysis (kf-CubeScene C-19a precedent).

## Routing summary (M-25 agglomeration input)

- **NO-WAVE-OWNER — the cube scene-repair packet (SS-1/SS-2 intake)**: the family core ME-1·2·4·9·10·11·12·15·16·23·26·27·33·34·35·36·37·38·39·41 + **the sync-topology spec ME-30·ME-31 (one spec with ME-7's retain-and-retarget cure)** — born-RED gates named in the rows.
- **KF.W4**: ME-5 + **ME-32** (types + tests + lint become one gate row; ME-29 is the exhibit).
- **KF.W6**: ME-13 (theme-aware ink) · ME-18 (`/number-field` evaluation).
- **KF.W9**: K10's substance (PRM mechanism unification, ≡ kf-CubeScene D-8+M-7).
- **KF.W0 §B-12**: K17's manifest schism (worktree deletions vs origin's 7.0.0 pins, lock included).
- **glass-ui BH relay**: ME-40 (`cn` text-color catch-all), per the standing owner edict.
- **SS-13**: the 11 probes above.

## Closing verdict

**ADJUDICATED — r2 supersedes r1; nothing lost, two things corrected, two things found.** The two hostile re-readers survive the incredulity seat with **all ten of their kills sustained** — and two of those kills reach further than the readers knew, overturning r1 rulings at the bytes: the never-settling-promise *leak* that carried r1's BLOCKER escalation is unreachable garbage (`playback.ts:139/:242-247`), so the corpus's one BLOCKER quietly reverts to its banked MAJOR; and the `justify-center` inversion kills D-18 so thoroughly that the utility every axis called dead weight turns out to be the component's sole size authority. Against that, five of the readers' confirmation cells die on ground already ruled — C-15 (both readers, ≡ K2), the phantom dependency (both readers, substrate-dead with the lockfile now probed), the two-authorities clause (≡ K5), the CI clause in reader-D's superlative and the PRM clause in reader-L's (each reader re-inheriting a cell the other had killed) — the recurring failure is never derivation, always an unread registry or an unnamed baseline. The r2's yield is real: **reader-L's two net-new composable MAJORs both verify hop-by-hop** — the translate-edit feedback loop that silently destroys manual matrix edits behind a stale comment with zero remaining callers, and a Reset arm that writes `acos` radians (or NaN, reachable from the slider's own preset) into a degrees channel — the first genuinely new mechanisms found on this surface after two full audit generations, and they slot straight into the banked undisposed-animation identity's cure spec. Terminal shape: **0 BLOCKER · 21 MAJOR · 18 MINOR · 2 INFO across 41 rows, original ids for life; the family routes NO-WAVE-OWNER into the cube scene-repair packet; ME-5/ME-32 ride KF.W4; ME-13/ME-18 ride KF.W6; K10 rides KF.W9; K17 rides KF.W0 §B-12; ME-40 rides the glass-ui BH relay; eleven probes ride SS-13.** Nothing here stamps a wave; every disposition is agglomeration input for the forming X·KF specs, awaiting the owner's begin-word.

— end of apotheosis r2. The three challenge files and both fresh reader returns remain immutable beside this record per E-1/E-3; the superseded r1's identities and rulings are carried herein (supersession block). Dispositions are ADJUDICATED, not VERIFIED.
