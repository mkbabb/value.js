Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component MatrixEditor; receipt read from this seat's own system prompt, not inherited).

# kf-MatrixEditor — adjudicated apotheosis r3 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-11 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`, worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus (r3)**: two fresh hostile re-reader returns over `audit/kf-components/MatrixEditor/challenge-{D-design,L-library,C-consumption}.md` — **reader-DU** (design-and-usage: 23 confirmed rows · 6 kills · 7 misses M-1..M-7 · 8 superlatives · 8 unproven) and **reader-LC** (library-and-code: 26 confirmed rows · 7 kills incl. TREE-PROVENANCE · 8 misses · 8 superlatives · 7 unproven). Both returns consumed whole; all three underlying challenge files re-read whole by this seat.

**Supersession (recorded, nothing lost).** This record supersedes the 2026-08-10 **r2** apotheosis at this path, which adjudicated a *different* pair of re-reader returns (r2's readers found the `<sub>`-flex, transpose-convention, corner-dead-zone, `isGroupPlaying` and `matrixValueAt`-privacy misses; this corpus's readers found the CardHeader/marks/overflow/write-path misses instead — distinct fresh runs over the same challenge corpus). r2 itself superseded the 2026-08-09 r1. **All r1/r2 identities ME-1..ME-41 are preserved for life (anti-rename) and carried in full below**; r2's rulings 1–11 and kills K1–K21 are carried in force. r3 corrects r2 in **exactly two places**, both at the registry: (a) r2's "first booking" clauses on **ME-30/ME-31 are OVERTURNED** — `kf-CubeScene.md` was committed `209f47c3` **2026-08-07**, three days before r2, and its roster already banks both mechanisms verbatim (L-4+M-6 = the T·R·S rebuild destroying hand-edits AND collapsing the Matrix channel to zero-delta; L-3 = the `Math.acos` radians/NaN reset) — the rows keep their ME ids and grades but their disposition is repaired to **FOLD ≡ kf-CubeScene L-4+M-6 / L-3** (ruling 2); (b) **ME-25 re-graded MINOR→INFO** with its claim narrowed — reader-LC proved `Input` is unobtainable from the root barrel, so `/forms` is forced, not a policy choice (ruling 11). Where r1/r2 content is restated it was re-verified this session except where marked *(r2-carried)*.

**Substrate (re-run by this seat, decisive for three rulings).** `git log -1` → local HEAD `8281638c`; `git rev-parse origin/master` → `81a56990`; `git status --short -- demo/scenes/cube/` → MatrixEditor.vue / transformMath.ts / useTransformState.ts / useCubeDemo.ts all ` M`; **`git diff origin/master --stat -- demo/scenes/cube/matrix-editor/ demo/scenes/cube/useCubeDemo.ts demo/scenes/cube/orbital-drag/ demo/state/` → EMPTY** — the dirty worktree is byte-identical to the landed frontier for the entire audited surface (the r1/r2 frontier inversion reproduces a third time). Local-HEAD divergence verified at the bytes: `git show HEAD:…MatrixEditor.vue` carries `<Card surface="cartoon" tier="quiet">` and `v-for … in matrix3dEnd.values`, with no `matrixCellValue` — reader-LC's byte observations are accurate **against the stale local HEAD only**. Manifests: worktree `package.json`/`package-lock.json` dirty with **0** glass-ui hits; `origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact; `origin/master:package-lock.json` carries glass-ui ×3. Every line anchor below binds to worktree ≡ `origin/master`.

**Method (incredulity).** Every load-bearing disagreement — between the two readers, between either reader and the banked r1/r2 record, and between r2 and the wider registry — was re-derived against the bytes before adoption. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session: whole reads of `MatrixEditor.vue` (158 L) and `useTransformState.ts` (227 L); `transformMath.ts:1-28/:160-181` (bounds table, `getTransformFromIx`/`getSliderOptionsFromIx`); `playback.ts:95-151` (`running` `:98`, `_run` `:113-151`, `:139` bare `const result = step(now)`, `reschedule` `:116-123` unreached on throw); `numeric.ts:36-46/:86-92` (`respectReducedMotion ?? false`); `useCubeDemo.ts:45-174` (`compileMatrixAnimation` `:57-63`, the `watch([matrix3dStart, matrix3dEnd]) → adoptCompiled` `:69-71`, the house PRM contract `:159-169`); `CubeScene.vue:55-80/:168-205/:215-230` (destructure of 6-of-8 `:71-78`, `isStarted` `:66`, tabsContent `:170-181`, ribbon reset `:189` + toggle `:194-200`, unmount persist `:218-226` incl. `matrix: t.matrix` `:224`); `orbital-drag/index.ts` TransformState (`matrix: mat4` required, default `mat4.create()`); `demo/scenes/cube/cubeTransformStore.ts:1-21` (the reader's bare `cubeTransformStore.ts` citation resolves here, NOT `demo/state/`); `sceneMachine.ts:19-21` (`SceneId = string`, comment verbatim); `vitest.config.ts:38-56` (library `exclude: ["test/demo/**"]`; demo project referenced by NO script — `package.json:45-46`, `demo:correctness` `:52` = `node scripts/run-demo-roster.mjs`, not vitest); `package.json:37-46` + ci.yml run-step census (`check:lib`/`test:lib` only); glass-ui installed 7.0.0: `dist/index.d.ts` whole export list (NO `./components/input`; `./components/number-field` at line 14), `slider/types.d.ts:11` (`marks?: readonly number[]`), `Slider.vue.d.ts:4` (`valueCommit`), `dist/components/card/` listing (CardHeader/CardTitle/CardDescription ship); `grep -rln CardHeader demo/` → **0**; `grep -rn 'CardContent class="grid' demo/` → 1 (the target); `docs/frontend-design/demo/cube.md:123-131/:237-246/:337-342` (the design-authority diagnosis + prescription + schedule, read verbatim); git substrate probes above. Registry instruments: the r2 record at this path (whole), **kf-CubeScene.md** (rows L-3, L-4+M-6, L-9, L-19/C-13, D-8+M-7, MISS-6 + routing block, with `git log` dating it 2026-08-07), kf-CubeTarget.md:95 (the #53 identity note), kf-SequencePlayhead (the dirty-manifest law).

## Scoreboard

**45 roster rows, original ids for life — 0 BLOCKER · 21 MAJOR · 19 MINOR · 5 INFO** (r2's 41 identities carried; ME-30/ME-31 dispositions repaired to registry folds; ME-25 re-graded INFO; **4 net-new rows admitted from this corpus's 15 filed misses (ME-42..ME-45)**, each re-derived at the bytes; 7 of the remaining misses are PRE-BANKED and folded as independent confirmations; the rest are extension riders). **Killed-claims register: 30 entries** — r1/r2's K1–K21 carried (five re-fired against this corpus's readers) + **K22–K30 new**, including the one that overturns r2's own first-booking claim. **13 superlatives** (r2's 11 carried; 2 added from this corpus). **12 UNPROVEN residues** → SS-13.

**Headline.** The r3's structural yield is not a defect but a **registry repair**: both of reader-LC's headline "misses" — the translate-edit recompose loop and the `Math.acos` radians/NaN reset — verify hop-by-hop at the bytes *and were already banked at kf-CubeScene on 2026-08-07 as L-4+M-6 and L-3*, which r2's "first booking" check missed and this corpus's readers (who did not read the registry at all) re-derived a third time. Three independent derivations of the same two mechanisms is the strongest confirmation either row will ever get, and zero of them may book a new row (anti-rename). The corpus's one genuinely new mechanism survives at MINOR: **ME-42 — the display string IS the write path** (the 2-dp truncated `:model-value` is what any keystroke re-commits through `parseFloat`, so touching a cell that holds `0.7071067811865476` silently rewrites it to a value derived from `"0.71"` — no full-precision model exists behind the field). Meanwhile the recurring failure mode fires five more times: reader-DU asserts the CI clause reader-LC kills (K1), both readers re-confirm the killed C-15 and the struck SceneId-widening clause, both repeat the substrate-dead phantom-dep row, and both grade ME-1 BLOCKER on the leak clause K13 buried — **the failure is never derivation; it is an unread registry and an unnamed baseline.**

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave sketch KF.W0–W10.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **ME-1 severity — BOTH readers file BLOCKER carrying the leak clause ("play() promise never settles; RAFPlayback stuck reporting running") vs the banked MAJOR (K13)** | **K13 RE-FIRED against both readers (third round); ME-1 stays MAJOR** | `playback.ts:139` re-read: `const result = step(now)` is bare; a sync throw escapes `frame` before `reschedule` (`:116-123`) → the rAF chain **terminates**, `_cleanup` (sole `_resolve` caller) is unreached, and the whole NumericAnimation→RAFPlayback→pending-promise graph is unreachable garbage — unobservable, not a leak. Reader-LC's own proof cell states the terminating mechanism and then grades as if it persisted. What survives is the banked row: an uncaught TypeError per poisoned keystroke (`""`/`"-"`/`"."`), edit silently dropped, recovery on next keystroke. |
| 2 | **ME-30/ME-31 identity — r2 booked both as "first bookings"; reader-LC re-files both as new misses (#1/#2)** | **r2 OVERTURNED at the registry; both rows FOLD ≡ kf-CubeScene L-4+M-6 and L-3 (ids and MAJOR grades kept; third-derivation confirmations recorded)** | `git log -1 -- kf-CubeScene.md` → `209f47c3` **2026-08-07** — predates r2 (08-10). Its L-4+M-6 cell carries the full mechanism *including* reader-LC's (b) consequence ("writes the SAME matrix into BOTH endpoints … the Matrix channel's start-to-end delta collapses to zero — Play animates nothing on that channel") and its L-3 cell carries the acos row ("NaN whenever scale > 1 … radians into a degrees field, 57.3× dimensional error"). This seat re-verified the chain at the bytes anyway: `useTransformState.ts:60-76` (`syncTransformations` writes translate per frame; reset arm `:69-75` `Math.acos(values[0|5|10])`), `:171-172` (both endpoints assigned), `:196-215` (deep watcher gated only on `isGroupStarted`), `:193-195` (the stale comment — zero external `updateTransformations` callers; CubeScene destructures 6 of 8, `:71-78`), `useCubeDemo.ts:60-63` (`fromVars([{transform: matrix3dStart.value},{transform: matrix3dEnd.value}])` — identity→identity after any recompose). Dispositions harmonize with the bank: one spec row — never write both endpoints from one pose; `acos(m00)` is not a rotation read. |
| 3 | **Reader-LC misses #3 (per-frame full `CSSKeyframesAnimation` recompile in useCubeDemo) and #5 (`TransformState.matrix` dead required field)** | **BOOKING KILLS — both PRE-BANKED ≡ kf-CubeScene L-9 and L-19/C-13; mechanisms confirmed as independent derivations** | kf-CubeScene L-9 (MAJOR): "each replacement firing `adoptCompiled(compileMatrixAnimation())` — a fresh `CSSKeyframesAnimation` + full `fromVars` parse+compile per flush" — verified again this session at `useCubeDemo.ts:57-71` + `useTransformState.ts:90`. kf-CubeScene L-19/C-13 (MINOR) already carries the dead-field row **sharper** than the reader filed it (the persisted alias is the raw `Float32Array` buffer of the dying instance); re-verified: `orbital-drag/index.ts` requires `matrix: mat4`, `CubeScene.vue:224` persists `matrix: t.matrix` by reference, `grep '\.matrix\b'` over the scene → that one line; `cubeTransformStore.ts` lives at `demo/scenes/cube/` (reader's bare citation path-corrected), and its stale docblock is already kf-CubeScene MISS-6. Fold, never re-book. |
| 4 | **Reader-LC miss #8 — the lossy display round-trip** | **ADMITTED at MINOR — net-new ME-42, the corpus's one genuinely new mechanism** | `MatrixEditor.vue:28-32`: `:model-value` = `(Math.round(v*100)/100).toFixed(2).replace(/\.0*$/,'')` — the DOM field's text is the 2-dp truncation; `:33` emits the field text; `useTransformState.ts:79` `parseFloat`s and commits it. There is no hidden full-precision model: the displayed string is the editable state, so any edit to a cell holding >2 decimals (the ordinary content of the six cosine cells after any 45° composition) commits a value derived from the truncated text. ME-11 (the formatter's *display* inconsistency) and ME-42 (the formatter as *write path*) are distinct claims; no banked row states the second (kf-CubeScene grep: 0). One cure family with ME-11/ME-10's per-cell digit policy. |
| 5 | **Reader-DU misses M-1..M-7** | **M-1 BOOKING KILL (pre-banked); M-2 fold-confirm; M-3/M-5/M-6 ADMITTED (ME-43/ME-44/ME-45, one cell corrected); M-4 extension rider; M-7 adopted as rider + superlative** | M-1 (PRM): mechanism re-verified (`numeric.ts` `?? false`; `useTransformState.ts:87-92/:105-107` never pass it; `useCubeDemo.ts:159-169` the house contract) — but kf-CubeScene **D-8+M-7 → KF.W9** names both motions verbatim ("the 300 ms cell tween and the 500 ms `easeInBounce` reset"); fold ≡ K10/K21 class. Credit where due: reader-DU correctly killed S-D2 AND filed the miss — the axis's error, not the reader's. M-2 = ME-33's exact mechanism (justify-center skips stretch-auto-tracks) independently re-derived — confirmation recorded, MAJOR grade concordant. M-3 admitted as **ME-43**. M-5 admitted as **ME-44** with its cell corrected: `grep -rln CardHeader demo/` → **0 hits** — the "used elsewhere in the demo" clause is false; the true fact is that glass-ui ships the slots (`dist/components/card/` listing, this session) and the demo consumes them nowhere. M-6 admitted as **ME-45** (`marks?: readonly number[]` at `slider/types.d.ts:11`, read with its doc comment). M-4's six-utility census extends ME-37 (which held three) — rider adopted (`m-0 p-0 h-fit` on `.matrix-grid`, `text-center` redundancy). M-7 (the design authority already diagnosed ME-13 and prescribed cures for ME-2/ME-13) — verified verbatim at `cube.md:125-129/:239-244/:339-341`; adopted as the "found, ruled-in-tree, unlanded" rider on ME-2/ME-13 + superlative 12; the doc's own line refs are stale and need re-anchoring before execution. |
| 6 | **C-15 — reader-DU confirms (MINOR tail) vs reader-LC kills ("`<script setup>` — per-instantiation, no singleton")** | **Reader-LC's kill SUSTAINED ≡ K2/K16 (fourth firing); reader-DU's cell refuted-as-booked** | `MatrixEditor.vue:115-118` re-read: `defaultMatrixOptions` is declared inside `<script setup>` — constructed fresh per mount; `??=` installs that instance's copy as the store bucket, which is what a seed is; no cross-instance path exists. A latent no-path non-defect confirmed a third time is a registry-blindness event, not a row. |
| 7 | **C-14/SceneId — BOTH readers confirm the widening clause ("superKey: string re-widens the SceneId union")** | **K15 RE-FIRED against both; ME-24's surviving substance unchanged** | `sceneMachine.ts:19-21` verbatim: "Kept a bare string so scenes.ts remains the single registry" — `export type SceneId = string`. Nothing is widened; the tree defends the design in prose. ME-24 survives as L-16 alone (read-once at `:113`, construction-only contract undeclared, benign under constant `SCENE_ID`). |
| 8 | **The CI clause — reader-DU's S★-1 confirmation ("unit-tested … inside a suite CI actually runs") vs reader-LC's kill** | **Reader-LC's kill SUSTAINED ≡ K1 (third firing); reader-DU repeated the challenge's false cell** | `vitest.config.ts:38-56`: library project `exclude: ["test/demo/**"]`; the demo project is referenced by **no script** (`test:lib` = `--project library`; `demo:correctness` = `node scripts/run-demo-roster.mjs`, not vitest — probed this session); ci.yml runs `check:lib`/`test:lib` only. `test/demo/scenes/cube-scene.test.ts` never runs on merge or release. The module keeps its credit (superlative 1); the gate keeps the blame (ME-32). |
| 9 | **TREE-PROVENANCE — reader-LC kills all three axes' anchors ("every line number anchored to a tree that does not exist in git")** | **SUSTAINED-AS-CORRECTED ≡ K7 (fifth occurrence of the one-baseline-short pattern)** | The byte observations are accurate vs the stale local HEAD (verified: `surface="cartoon"`, `.values`, no `matrixCellValue` at `8281638c`) — but the tree the axes read **does exist in git**: worktree ≡ `origin/master` `81a56990`, empty diff over the whole audited surface (re-run this session). The consequence clause is inverted at the frontier; the reader's rider "at HEAD the eaten token is `text-body`" is a local-HEAD-only fact, moot. The same correction retires reader-LC's Card-superlative caveat ("working-tree property only — HEAD still carries `surface=cartoon`"): the boolean `cartoon` form is origin-current. Standing law re-affirmed: **a dirty-worktree claim without a named comparison baseline is inadmissible.** |
| 10 | **Phantom dependency — reader-DU's INFO-tail "PHANTOM DEP CONFIRMED THIS SESSION" + reader-LC's L-21/F-1 row** | **K17 RE-FIRED against both — SUBSTRATE-DEAD** | Re-probed: worktree manifests dirty, 0 glass-ui hits; `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact; origin lock ×3. ≡ census F-1/SCH-1, killed at kf-AmigaScene/kf-CubeScene/r1 K8/r2 K17, *a priori* law at kf-SequencePlayhead. Residue rides KF.W0 §B-12 (the uncommitted manifest deletions would revive F-1 if committed). |
| 11 | **C-11 — reader-DU confirms whole vs reader-LC's partial refutation ("`Input` is not obtainable from the root barrel")** | **Reader-LC WINS; ME-25 claim narrowed + re-graded MINOR→INFO** | `dist/index.d.ts` re-read whole: 28 component exports, **no `./components/input`** — `/forms` is the only route to `Input`, so `:98` is forced, not a policy choice. Residue: `Slider`/`Card`/`CardContent` ride the root barrel while dedicated `./slider`/`./card` subpaths exist — an INFO-tier inconsistency. Same read strengthens ME-18: `number-field` **is** on the root barrel (line 14) — the remedy needs no new import specifier. |
| 12 | **D-9 phrasing + D-18 grounds (minor concordances)** | **Recorded** | D-9: reader-LC's "changes nothing observable" repeats the phrasing r2 killed — `CubeScene.vue:195-200` re-read: the toggle flips its own Lock/LockOpen icon and Free/Fixed label; ME-14's narrower-and-still-damning form stands. D-18: both readers kill (concordant with K12); reader-DU's `justify-center` inversion is the ruling ground (premise proven false); reader-LC's padding-chain ground proves only "not proven" and is superseded. Reader-DU's kill of D-6's 91–119px arithmetic ≡ K12's rider; the circle conclusion stands (ME-12). Both readers' MAJOR on D-2 is concordant with ruling 11 (r2) — the owner disposition prices in. |

**Adjudicator's corrections register (against this corpus's readers):** (1) both readers' ME-1 BLOCKER grades — ruling 1; (2) reader-DU's C-15 + both readers' C-14 cells — rulings 6–7; (3) reader-DU's S★-1 CI clause — ruling 8; (4) both readers' phantom-dep cells — ruling 10; (5) reader-DU's M-5 "used elsewhere" cell — ruling 5; (6) reader-LC's provenance consequence + Card caveat — ruling 9; (7) reader-DU's C-11 cell — ruling 11; (8) reader-LC's D-9 phrasing — ruling 12; (9) both readers' re-filing of banked mechanisms as new (rulings 2–3, 5) — the readers did not consult the registry; neither did r2's first-booking check reach kf-CubeScene, which is now this record's own correction.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0–W10** per the census wave sketch where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake, agglomerating into the cube scene-repair packet whose core is the matrix-editor family (kf-CubeScene's routing block is the packet's spine). Anchors bind to worktree ≡ `origin/master`; re-anchor before any cure.

### MAJOR

| id | merged ids | claim (compressed; r1/r2 carry the full cells) | terminal disposition |
|---|---|---|---|
| **ME-1** | L-2 / C-1 | Unvalidated string emit (`""`/`"-"`/`"."` — every negative entry transits `"-"`) → `parseFloat` NaN unguarded (`useTransformState.ts:79`) → `withMatrixCell` throws inside the engine's rAF frame (`transformMath.ts:143-145`); uncaught TypeError per poisoned keystroke, edit silently dropped. Leak/wedge clause DEAD (K13, three firings): the throw terminates its own chain; the never-settled promise is unreachable garbage. **MAJOR (banked grade)** | **FOLD ≡ kf-CubeScene L-5/C-6 (S-9)**. NO-WAVE-OWNER — family core; cure evaluates `/number-field` (number-typed emit makes the class unreachable — ME-18) + a `Number.isFinite` gate at the seam either way |
| **ME-2** | L-3 / C-3 / D-1 | Missing trailing space fuses `text-ellipsisfont-bold` (`:18-26`); `text-ellipsis` AND `font-bold` both die on the selected cell; only `focus:font-bold` survives — bold means *focused*, not *selected*. Sibling literal `:46` carries the space. `cn` blast radius = exactly one dead token (the fused token lands alone in the `text-color` catch-all — see ME-40 + superlative 13). r3 rider (M-7): the in-tree authority *prescribes the cure* (`cube.md:239-244` — axis-color ring + inset on the active cell); found, ruled-in-tree, unlanded | **NO-WAVE-OWNER** — one-character cure, one commit with ME-16; the cube.md ring prescription is the design floor |
| **ME-3** | L-5 / C-2 / D-8 | `:start`/`:end`/`:step` not InputProps (18 members exactly); fall through `inheritAttrs:false` onto a native `<input type="text">`; nothing clamps anywhere downstream | **FOLD ≡ kf-CubeScene L-5/C-6** |
| **ME-4** | L-8 / C-10 | `Matrix3dCall.args: readonly CssValue[]` discards the module's own `MatrixScalar`; `payload.value` inexpressible at both consumer reads (`:29`, `:133`); forces 16× four-way revalidation per read. Cure: narrow to `readonly MatrixScalar[]` | **NO-WAVE-OWNER** — one-line narrowing; ME-41 and ME-27 ride the same commit |
| **ME-5** | C-9 / L-17 | No vue-tsc anywhere; `check` bare tsc; CI runs `check:lib` (src/ only); `*.vue` ambiently `DefineComponent<{},{},any>`. The enabling condition for ME-3/ME-4/ME-6 | **FOLD ≡ KF-APP-4 (→ KF.W4)**; ME-32 extends the gate row |
| **ME-6** | L-6 / C-5 / D-10 | `selectedMatrixCell` absent from `matrixOptions?: { fixed: boolean }` (store `:26`); a leaf invents, defaults (`??=` `:120`) and persists a store field a sibling reads, violating the store's own written boundary (`:90-94`). "Two authorities" sub-clause stays dead (K5/K18) | **FOLD ≡ KF-APP-30 + kf-CubeScene M-8** — seed moves into the store's migration boundary; type row gains the field |
| **ME-7** | L-4 / C-4 | Every emission spawns a fresh un-cancelled 300ms NumericAnimation, private rAF chain, no handle/dedupe/disposal; Slider wires the continuous emit while `valueCommit` ships unused (`Slider.vue.d.ts:4`, re-read) | **FOLD ≡ kf-CubeScene C-7/L-13/L-14**; ME-30/ME-31 share the cure spec (retain-one-instance + retarget + stop-on-dispose) |
| **ME-8** | L-7 | Controlled `:model-value` derives from the very ref the 300ms animation rewrites per frame; Input is `passive:true` + `vModelDynamic` — DOM value re-synced against the user mid-entry | **FOLD ≡ kf-CubeScene C-7/L-13/L-14**; caret witness → SS-13 |
| **ME-9** | D-2 / C-12 | Seventeen interactive controls, zero accessible names; `forwardedAttrs` would pass `aria-label` verbatim; the owner disposition (SHIP demo-owned, `design-findings.txt:243-245`) bounds the grade at MAJOR (r2 ruling 11; both r3 readers concordant) | **NO-WAVE-OWNER** — rides the family cure; the ruled `:aria-label` prescription is the floor |
| **ME-10** | D-3 / C-16 | Slider preset domain-wrong for 10 of 16 cells — `"P"` and `""` both land on `rotate [-360,360] step 1` (`transformMath.ts:6-22/:160-181`, re-read); one step = 50% of a cosine's legal range; negative (mirror) scale unreachable (`[0.4, 3]`) | **NO-WAVE-OWNER** — per-cell-class domains are the family spec table |
| **ME-12** | D-6 | Glass Input half-undressed: `bg-transparent p-0` win on layer order; 1.5px border, pill radius, rim shadow, backdrop blur survive — 16 circles in 16 square `rounded-lg` cells. 91–119px arithmetic STRUCK (K12); circle conclusion size-independent | **NO-WAVE-OWNER** — presentation arm; `/number-field` supersedes if adopted; variants relay glass-ui BH |
| **ME-13** | D-4 | Sole per-cell identification at `opacity-20` light / `opacity-75` dark fails the 3:1 large-text bar; light arm theory-proof (α=0.2 ceiling 1.662:1). Both r3 readers re-derived independently (1.201 for light-y; 1.326 for light-x) — table trustworthy. r3 rider (M-7): `cube.md:129` diagnoses this verbatim ("nearly invisible in light mode") and `:239-244` prescribes ~`opacity-45`; scheduled as work item 8 and unlanded | **KF.W6** (theme-aware ink, not alpha; the cube.md floor rides) |
| **ME-14** | D-9 | `matrixOptions.fixed` write-only beyond its own button: the ribbon Lock/Free toggle relabels itself and gates nothing; evident consumer (`.toFixed(2)`) hard-coded | **FOLD ≡ kf-CubeScene C-16+D-10** |
| **ME-15** | D-5 | 10 of 16 labels duplicated or wrong: 3/7/11 all `P`+sub-`w`; six bare letters each twice; no description field exists; zero prose/legend for the column-major convention | **NO-WAVE-OWNER** — same spec table as ME-10/ME-11; ME-35 compounds |
| **ME-16** | L-10 / D-7 | Selection is `@click`-only (`:37-39` sole writer); Tab-focus cannot retarget the Slider; fused with ME-2 the sole surviving weight cue marks the FOCUSED cell while the Slider edits a different one | **NO-WAVE-OWNER** — one cure with ME-2/ME-9 |
| **ME-17** | L-1 / C-5 | Untrusted share-link JSON reaches the store wholesale (`isValidState` shape-only → `Object.assign`); `matrixCellMeta[…]` deref ×3 unguarded (`:75-89`); no shipped writer authors the poison — BLOCKER grade killed (K4; both r3 readers concordant at MAJOR) | **FOLD ≡ KF-APP-30**, share-link rider; localStorage survey + blast radius → SS-13 |
| **ME-18** | C-8 | Hand-rolled bounded number field while glass-ui 7.0.0 ships `/number-field` (number-payload emit), reached zero times demo-wide. r3 strengthener (reader-LC, verified): NumberField rides the root barrel the file already imports (`index.d.ts:14`) | **FOLD ≡ census S-9 → KF.W6** — evaluate, not mechanical swap (the cell doubles as the selection click-target) |
| **ME-30** | kf-CubeScene **L-4+M-6** (r2 booking; reader-LC miss #1 = third derivation) | **The translate-edit/drag recompose loop**: any genuine change to `transformSliderValues` (translate-cell edit via per-frame `syncTransformations`; any OrbitalDrag frame) fires the deep watcher (gate `isGroupStarted` false = the ordinary state) → rAF → `updateTransformations` rebuilds from T·R·S only and assigns **BOTH** `matrix3dEnd` and `matrix3dStart` (`:171-172`) — destroying every manual non-TRS cell edit, injecting the drag rotation, racing the in-flight 300ms tween, **and collapsing the Matrix channel to zero-delta (identity→identity `fromVars`, `useCubeDemo.ts:60-63` — Play animates nothing on that channel)**. The excusing comment `:193-195` is stale: zero external `updateTransformations` callers | **r3 REPAIR: FOLD ≡ kf-CubeScene L-4+M-6** (banked 2026-08-07). NO-WAVE-OWNER — family core, one spec with ME-7/ME-31: one writer per channel; never write both endpoints from one pose; born-RED gate: edit cell 1, then cell 12 — assert cell 1 survives |
| **ME-31** | kf-CubeScene **L-3** (r2 booking; reader-LC miss #2 = third derivation) | **Reset writes `Math.acos(diagonal)` radians into the degrees rotate channel; NaN outside [-1,1]** — reachable from the slider's own scale preset `[0.4, 3]`; NaN → deep watcher → `fromXRotation(NaN)` → `createMatrix` TypeError uncaught in rAF; `rotate.x` stays NaN (nothing else writes it) — the free-transform path bricks until reload. Conceptually `acos(scale-diagonal)` conflates scale with rotation — wrong math, not just wrong units | **r3 REPAIR: FOLD ≡ kf-CubeScene L-3**. NO-WAVE-OWNER — family core: real polar/Euler extraction or don't sync rotate/scale mid-reset; born-RED gate: cell 0 = 2 → Reset → no throw, cube lands identity. Visible end-state → SS-13 |
| **ME-32** | reader-LC misses #6+#7 (this corpus) ≡ r2's row; subsumes ME-29 | **The demo's correctness surface is gate-free in all three dimensions**: types (ME-5), tests (the demo vitest project referenced by NO script and NO workflow — `demo:correctness` is a node roster script, probed this session), lint (no ESLint; `lint: depcruise src`) — template classes only a linter sees (the `:11` keyless `v-for`) are structurally invisible | **KF.W4** (one gate wave with ME-5/KF-APP-4): wire `test:demo` into CI, eslint-plugin-vue essentials, `noUnusedLocals`; ME-29 is the exhibit |
| **ME-33** | reader-DU miss M-2 (this corpus) ≡ r2's row — independent re-derivation, grades concordant | **The editor is intrinsically sized, not rail-filling**: `justify-center` on CardContent (`:3`) skips CSS-Grid §12.8 stretch-auto-tracks; `w-full` contributes `auto` under intrinsic sizing; cells have zero in-flow children (both `:18`/`:43` absolute) — `min-h-[3.5rem]` is plausibly the **sole size authority** (≈236px of grid centred in a 400–512px rail; the Slider only as wide as the grid). Sole instance of the idiom in the tree (grep, this session). One-word cure | **NO-WAVE-OWNER** — presentation arm with ME-12; rendered dimension → SS-13 |

### MINOR

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| **ME-11** | D-11 / L-18 / C-13 (MAJOR→MINOR per r2 ruling 3; both r3 readers concordant) | Formatter reports sub-0.005 as `"0"` and mixes 0dp/2dp widths down a tabular column; the perspective narrative DEAD (K14 — cells 3/7/11 structurally 0 in every demo-reachable state; perspective is a CSS ancestor property, `CubeTarget.css:15`); the pre-round is load-bearing for `-0` (K6) | **NO-WAVE-OWNER** — one formatter with ME-10's cell-class table + ME-42 |
| **ME-19** | L-13 | Dead imports `MATRIX_AXES`/`transformSliderOptions` (`useTransformState.ts:9-10`) + zero-consumer type re-export (`:19`) | **FOLD ≡ kf-CubeScene L-10/L-11/L-12** |
| **ME-20** | L-9 / C-7 / D-14 | `resetMatrix` emit chain dead in all three directions (`:110/:136/:137` only); live reset is the parent ribbon's direct call (`CubeScene.vue:189`) | **FOLD ≡ kf-CubeScene L-26/C-12b** |
| **ME-21** | L-15 | Watcher's scheduled rAF (`:203`) never cancelled; unmount inside the window paints a detached element; no lifecycle import (`:3`) | **FOLD ≡ kf-CubeScene C-7/L-13/L-14** |
| **ME-22** | L-12 / C-17 *(r2-carried)* | Per-frame allocation + invalidation storm inverting the engine's zero-allocation contract inside its own callback; `tabsContent` re-renders the 17-control subtree per frame. Reader-LC's useCubeDemo extension = the banked kf-CubeScene L-9 (ruling 3) | **FOLD ≡ kf-CubeScene L-9** |
| **ME-23** | L-14 / D-21 *(r2-carried)* | Split error posture: guarded RangeError at `:126-134` (duplicating transformMath's message, dropping its type guards) vs three unguarded reads at `:75-89`; a third read path at `:29` | **NO-WAVE-OWNER** — one guard policy; ME-41 names the root cause |
| **ME-24** | L-16 (SceneId clause STRUCK — K15, re-fired r3 against both readers) | `superKey` read once at setup, never tracked; construction-only contract undeclared; benign under constant `SCENE_ID` | **FOLD ≡ kf-CubeScene L-22/C-14** |
| **ME-26** | D-15 / D-16 / D-17 *(r2-carried)* | Latent trio: no RTL guard on a semantically-ordered grid; forced-colors can't remap `opacity` (the one dimming glass-ui's fallbacks can't repair); 2px+8px focus ring across a 4px gutter, no clip | **NO-WAVE-OWNER**; D-17 percept → SS-13 |
| **ME-27** | r1 miss #5 (re-verified: `transformSliderOptions` module const, same object identity per class, mutable) | Shared-singleton `sliderOptions` aliasing — the exact class the store `structuredClone`s against | **NO-WAVE-OWNER** — `readonly` + `satisfies` + freeze in the ME-4 commit |
| **ME-34** | r2 reader miss *(r2-carried)* | The `<sub>` is destroyed by the flex context: `vertical-align` does not apply to flex items — `T` and `x` render as adjacent letters, not `T_x`. Compounds ME-15 | **NO-WAVE-OWNER** — family labeling cure |
| **ME-35** | r2 reader miss *(r2-carried)* | Two contradictory conventions on one surface: column-major `matrix3d()` args laid out as visual ROWS (the display is the transpose) while labels name row-within-column; zero reconciling prose | **NO-WAVE-OWNER** — a caption or `grid-flow-col`; one cure with ME-15 |
| **ME-36** | r2 reader miss *(r2-carried)* | ~21.5% of every cell is a dead zone: circle-clipped hit-testing on the Input carrying the only selection path; the four corners neither focus nor select | **NO-WAVE-OWNER** — dies with ME-12's cure; corner-click probe → SS-13 |
| **ME-37** | r2 reader miss + **r3 extension rider (reader-DU M-4, verified)** | Inert layout utilities: `justify-items-center` on a flex container; `grid` on a wrapper with zero in-flow children; `justify-items-stretch` restating the default; **+ `m-0 p-0 h-fit` no-ops on `.matrix-grid` and a `text-center` already achieved by flex centering** — six inert or self-contradicting utilities in a 158-line file | **NO-WAVE-OWNER** — hygiene, rides any touching wave |
| **ME-38** | r2 reader miss *(r2-carried; re-verified `:22` declaration-only)* | `isGroupPlaying` is a required positional parameter the composable never reads; CubeScene dutifully feeds it (`:78`); undroppable without a coordinated edit; invisible per ME-32 | **NO-WAVE-OWNER** — dies in the ME-30 re-authoring |
| **ME-39** | r2 reader miss ≡ reader-LC miss #4 (this corpus — independent confirmation) | `syncTransformations` + `updateTransformations` returned with zero consumers tree-wide (CubeScene destructures 6 of 8, `:71-78`); the dead surface is the mechanical reason ME-30's stale comment survived | **NO-WAVE-OWNER** — the ME-30 spec deletes or privatizes them; the `:193-195` comment correction is mandatory in that commit |
| **ME-40** | r2 reader miss *(r2-carried)* | glass-ui `cn`'s final text rule is the catch-all `["text-color", /^text-/]`: `text-ellipsis` classifies as a text COLOR — silent last-wins eviction hazard for any consumer pairing it with a real `text-*` colour; latent here. See superlative 13 for the same mechanism's graceful half | **GLASS-OWNED → BH relay** (owner edict); MINOR-latent; no local workaround |
| **ME-41** | r2 reader miss *(r2-carried)* | `matrixValueAt` is module-private, so no guarded single-cell accessor is reachable: whole-matrix revalidation per one-cell operation, and MatrixEditor hand-rolled its own unguarded reader BECAUSE the guarded one is unexported — ME-23's root cause | **NO-WAVE-OWNER** — export it (or ME-4's narrowing collapses the class); one commit with ME-4 |
| **ME-42** | **NET-NEW r3** — reader-LC miss #8 (ruling 4) | **The display string is the write path**: the field renders the 2-dp truncated string and re-parses whatever text derives from it — any edit to a cell holding >2 decimals (e.g. `0.7071067811865476` → `"0.71"`) commits precision-destroyed values; no full-precision model exists behind the field. Distinct from ME-11 (display-only) | **NO-WAVE-OWNER** — the ME-10/ME-11 digit-policy spec must state the *edit* representation, not just the display one (or `/number-field`'s `formatOptions` separates the two); witness → SS-13 #12 |
| **ME-43** | **NET-NEW r3** — reader-DU miss M-3 (ruling 5) | **Values overflow the cell with no recovery affordance**: viewport-scaling `--type-body` (16–22px) monospace on a fixed ≈56px zero-padding cell; `"-1000"` is reachable (translate bounds `[-1000,1000]`, `transformMath.ts:8`) and ~5-7 chars ≈ 54-66px at 0.6em advance; no `title`, no tooltip, no `aria-describedby` (file read whole) — and on the SELECTED cell ME-2 destroys the ellipsis, so truncation is unsignposted. A numeric editor that can silently hide the number it exists to show | **NO-WAVE-OWNER** — one cure family with ME-12/ME-33 (right type rung + real cell sizing); exact threshold → SS-13 (depends on ME-33's geometry) |

### INFO

| id | claim | disposition |
|---|---|---|
| **ME-25** | C-11 — **r3 re-grade MINOR→INFO, claim narrowed (ruling 11)**: `Input` is `/forms`-forced (no root-barrel export — verified at `dist/index.d.ts`); the residue is only that `Slider`/`Card`/`CardContent` ride the root barrel while `./slider`/`./card` exist | **FOLD ≡ kf-CubeScene C-19a**; bundle cost rerouted build-level *(r2-carried)* |
| **ME-28** | The verified tail *(r2-carried)*: `toLocaleLowerCase` vs `toLowerCase` on the same datum (both no-ops); fixed 25.9px decoration over fluid editable value (type inversion, D-12); hand-rolled mono rung (`text-mono-small` ships); dead `matrix-grid` hook; dead one-hop `--color` (cargo-culted from CubeAxisLines where descendants DO read it). D-18 cell STRUCK (K12) | **NO-WAVE-OWNER** — hygiene |
| **ME-29** | The `:11` keyless `v-for` — subsumed as ME-32's exhibit ≡ reader-LC miss #6 (independent confirmation, `vue/require-v-for-key`, no gate can run it) | rides ME-32 → KF.W4 |
| **ME-44** | **NET-NEW r3** — reader-DU miss M-5, cell corrected (ruling 5): 17 controls inside a Card with no `role="group"`/`aria-label`, no heading, no legend for the column-major convention — while glass-ui ships CardHeader/CardTitle/CardDescription (dist listing verified) **and the demo consumes them zero times anywhere** (grep, this session — the reader's "used elsewhere" cell was false). The a11y complement to ME-9: named controls in an unlabeled group of 16 identical fields are still not navigable | **NO-WAVE-OWNER** — rides the ME-9/ME-15 labeling cure |
| **ME-45** | **NET-NEW r3** — reader-DU miss M-6: the Slider declines `marks?: readonly number[]` (`slider/types.d.ts:11`, "decorative checkpoints … never snap the value") — the shipped affordance for a control that silently retargets across three domains spanning `[0.4,3]`/`[-360,360]`/`[-1000,1000]` with no accessible name and no visible cell link; marks at 0/±1 are the one cheap reference point. `size`/`variant`/`motion` also unused | **NO-WAVE-OWNER** — one binding in the family cure |

## Killed-claims register — 30 entries

K1–K21 *(r1/r2, carried in force; compressed)*: **K1** S★-1's CI half (re-fired r3 → reader-DU, ruling 8) · **K2** C-15 module-singleton (re-fired r3 → reader-DU, ruling 6) · **K3** D-10's ordering mechanism (toggle authors the poison) · **K4** L-1 at BLOCKER → MAJOR (both r3 readers concordant) · **K5** L-6's "two authorities" · **K6** "the Math.round is dead work" (load-bearing for `-0`) · **K7** challenge-L:6's tree basis — sentence false vs local HEAD, consequence INVERTED at the frontier (re-fired r3, ruling 9) · **K8** F-1 "fails under npm ci" ≡ census F-1/SCH-1 → KF.W0 §B-12 · **K9** C-8's `types.d.ts` anchor · **K10** S-D2 "PRM-honest by construction" HALF-KILL (CSS half true; the component triggers two ungated engine motions) ≡ kf-CubeScene D-8+M-7 → KF.W9 · **K11** the 6.0.0-drift miss · **K12** D-18 + the 91–119px arithmetic (justify-center inversion; inverse = ME-33) · **K13** the leak sub-claim (throw terminates the chain; graph collectable) · **K14** D-11's perspective headline · **K15** C-14's SceneId-widening (re-fired r3 → BOTH readers, ruling 7) · **K16** r2 readers' C-15 confirmations · **K17** phantom-dep rows — SUBSTRATE-DEAD, origin lock ×3 (re-fired r3 → BOTH readers, ruling 10) · **K18** two-authorities re-inheritance · **K19** D-2 BLOCKER grade (owner disposition prices in) · **K20** r2 reader's ME-1/ME-2 BLOCKER grades · **K21** booking-kills for pre-banked mechanisms re-filed as new.

New this corpus:

| # | killed claim | adjudication |
|---|---|---|
| **K22** | Both r3 readers' ME-1 BLOCKER grades, each carrying the dead leak clause ("play() promise never settles … RAFPlayback stuck reporting running") | **SEVERITY KILLS ≡ K13 (ruling 1)** — reader-LC's own proof cell states the chain-terminating mechanism, then grades as if it persisted. ME-1 = MAJOR, grade-concordant with the bank |
| **K23** | **r2's own ME-30/ME-31 "first booking" clauses** ("no banked row covers this — checked against … the banked kf-CubeScene roster") | **OVERTURNED at the registry (ruling 2)** — kf-CubeScene (committed 2026-08-07, three days before r2) banks both as L-4+M-6 and L-3, including the zero-delta consequence r2's cell lacked. Identity repaired: FOLD, ids preserved. The registry's own anti-rename law applied to the registry itself |
| **K24** | Reader-LC's misses #1/#2 as *new* findings ("All three axes missed this"; "NOT covered anywhere in the corpus") | **BOOKING KILLS (ruling 2)** — true of the challenge corpus, false of the registry; third independent derivations of kf-CubeScene L-4+M-6/L-3, recorded as confirmations |
| **K25** | Reader-LC's misses #3/#5 as new findings (per-frame useCubeDemo recompile; dead `TransformState.matrix` field) | **BOOKING KILLS (ruling 3)** — pre-banked ≡ kf-CubeScene L-9 and L-19/C-13 (the latter already sharper: the Float32Array-buffer alias); mechanisms confirmed; the bare `cubeTransformStore.ts` citation path-corrected to `demo/scenes/cube/` |
| **K26** | Reader-DU's M-1 (PRM) as a new row | **BOOKING KILL (ruling 5)** ≡ kf-CubeScene D-8+M-7 → KF.W9, which names the 300ms tween + 500ms `easeInBounce` reset verbatim. Mechanism confirmed; reader-DU's paired S-D2 kill was correct and is subsumed by K10 |
| **K27** | Reader-DU's M-5 cell "**and is used elsewhere in the demo**" (CardHeader/CardTitle) | **CELL KILLED** — `grep -rln CardHeader demo/` → 0 hits (this session). The finding survives with the corrected cell (ME-44): the slots ship in glass-ui and the demo declines them *everywhere*, which is a sharper fact than the false one |
| **K28** | Reader-DU's C-15 + BOTH readers' C-14 confirmation cells; reader-DU's S★-1 CI clause; both readers' phantom-dep cells | **RE-INHERITANCE KILLS (rulings 6–8, 10)** ≡ K2/K16, K15, K1, K17 — four banked kills each re-fired; five separate events of a reader confirming a cell the registry had already killed. The recurring failure is an unread registry |
| **K29** | Reader-DU's C-11 confirmation ("three subpath policies … picks the coarse one") | **PARTIALLY KILLED (ruling 11)** — `Input` has no root-barrel route (28-line export list read); `/forms` is forced. ME-25 narrowed + re-graded INFO; the same read strengthens ME-18 |
| **K30** | Reader-LC's TREE-PROVENANCE consequence clauses ("anchored to a tree that does not exist in git"; "Any wave landing on 8281638c must re-anchor first"; the Card superlative's "working-tree property only") + reader-LC's D-9 "changes nothing observable" | **SUSTAINED-AS-CORRECTED / CORRECTED (rulings 9, 12)** — the tree exists in git at `origin/master` (empty diff, re-run); byte facts vs the stale local HEAD adopted; `<Card cartoon>` is origin-current. D-9's toggle flips its own icon/label — the narrower charge stands (ME-14) |

## Identity-guard folds (never re-booked)

- **ME-30 ≡ kf-CubeScene L-4+M-6 · ME-31 ≡ kf-CubeScene L-3** — the r3 repair (K23). One identity each, banked 2026-08-07; the ME ids persist as this component's cross-references; three independent derivations logged per row. Any later adjudication folds new findings on these mechanisms to the kf-CubeScene rows.
- **ME-1/ME-3/ME-18 ≡ kf-CubeScene L-5/C-6 (S-9)** — one banked identity, three limbs; grade concordance restored at r2 and held.
- **ME-7/ME-8/ME-21 ≡ kf-CubeScene C-7/L-13/L-14** — the undisposed-animation identity; ME-30/ME-31 share its cure spec.
- **ME-22 (+ reader-LC's useCubeDemo extension) ≡ kf-CubeScene L-9** · **reader-LC miss #5 ≡ kf-CubeScene L-19/C-13** · **ME-5 (+ME-32) ≡ KF-APP-4 → KF.W4** · **ME-6 ≡ KF-APP-30 + kf-CubeScene M-8** · **ME-17 ≡ KF-APP-30** · **ME-14 ≡ kf-CubeScene C-16+D-10** · **ME-19 ≡ L-10/L-11/L-12** · **ME-20 ≡ L-26/C-12b** · **ME-24 ≡ L-22/C-14** · **ME-25 ≡ C-19a**.
- **K10/K26's substance ≡ kf-CubeScene D-8+M-7 → KF.W9** · **K17 ≡ census F-1/SCH-1 → KF.W0 §B-12** · **ME-40 → glass-ui BH relay** (owner edict).
- **ME-42..ME-45: first bookings** — each checked against r1/r2's identity table, the kf-CubeScene and kf-CubeTarget rosters (incl. the #53 disjointness note), and the census S-1..S-9/F-1 rows; no prior row states the write-path truncation, the overflow-with-no-recovery consequence, the declined Card header slots, or the declined Slider marks.

## Superlatives — 13 (provenance: corpus → readers → this seat)

1. **`transformMath.ts` as a value module** *(r1; every reader round concordant)*: pure, total, immutable by construction, fail-explicit with named indices; the tests assert non-mutation and both rejection paths — **and still run in no gate** (K1 ×3; ME-32's charge). The module keeps its credit; the gate keeps the blame.
2. **The `isGroupStarted` gate + rAF debounce** *(r1)*: the hazard named at the seam, cured with the correct clock. Its trailing sentence is ME-30's stale comment — the mechanism superlative stands, its documentation does not.
3. **The `z-10` provenance comment** *(r1, sharpened r2)*: load-bearing, not merely accurate — without it the decorative label (later in DOM order) would paint over the field and swallow the only selection path.
4. **Engine-payload validation at `useTransformState.ts:109-117`** *(r1)*: refuses to trust a dynamically-keyed record even from its own engine; the standing irony (meticulous on the engine's output, absent on its own input — ME-1) stands.
5. **Container-level glass vocabulary, landed and origin-current** *(r1/r2; r3 re-affirmed against reader-LC's caveat, ruling 9)*: `<Card cartoon tier="quiet">` is API-correct against the exact 7.0.0 the frontier pins.
6. **Pure-view data flow** *(r1)*: zero `ref(`/`watch(`/`computed(` in the SFC; the empty state structurally unreachable.
7. **The value.js edge is type-only and R1-clean** *(r1)*: erased `import type` under `verbatimModuleSyntax`; no parser entered.
8. **`RAFPlayback`'s generation-guarded core** *(r2)*: `_gen` makes stop-plus-restart unable to arm a second chain — the very mechanism that decides K13: the library's discipline bounds the demo's carelessness.
9. **The `}}<sub>` whitespace-suppressing line break** *(r2)* — with ME-34's poignancy caveat.
10. **`--axis-w: var(--foreground)`** *(both r2 and r3 corpora; reader-DU re-affirmed)*: the homogeneous column deliberately gets a neutral; a near-miss the tree defended twice. The token language's only consumer is a glyph at α=0.2 (ME-13) — recorded so the remediation fixes the opacity, not congratulates the tokens.
11. **Falsifier discipline held under three hostile rounds** *(r2, extended r3)*: across ~59 filed defects, three re-reader seats and three adjudications, the substantive kill count stays single-digit and every genuinely new mechanism found post-corpus (ME-30/ME-31/ME-42) came from a reader stating falsifiers. The failure that recurs is never derivation — it is *not reading the registry* (K16, K21, K24–K26, K28 — and K23, r2's own) and *not naming the baseline* (K7, K17, K30).
12. **NEW (reader-DU, adopted) — `docs/frontend-design/demo/cube.md` is a functioning design authority, not decoration**: at `:125-129` it diagnoses its own component's two worst design defects in plain prose ("the active cell only goes font-bold"; "nearly invisible in light mode"), at `:239-244` it prescribes token-level cures, at `:339-341` it schedules them as work item 8. A repo whose design doc already found what a hostile three-axis audit found has a working design process — the gap is execution, not perception. (Its line refs are stale; re-anchor before executing — the D-19 lesson.) Rider attached to ME-2/ME-13: *found, ruled-in-tree, unlanded* is a materially stronger ledger row than *found*.
13. **NEW (reader-LC, adopted with the ME-40 tension) — glass-ui's `cn` is a well-built last-wins group resolver**: scope-aware (splits on the final `:` so `focus:font-bold` and `font-bold` occupy different buckets), `!`-tolerant, order-stable, degrading to pass-through for unknown tokens — which is exactly why ME-2's fused token is inert rather than destructive (it lands alone in the `/^text-/` catch-all and evicts nothing). The same catch-all is ME-40's latent eviction hazard: one mechanism, one graceful half, one sharp edge — both recorded, producer-owned.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **ME-8 caret/pixel clobber** — type `-12` into a translate cell mid-animation; whether Vue's textual-equality skip hides the rewrite.
2. **ME-13 composited contrast — dark arm only** (light arm theory-proof at α=0.2, ceiling 1.662:1).
3. **ME-17 localStorage survey** — any real `{fixed}`-only bucket in the wild.
4. **ME-17/ME-31 blast radius** — does a render/rAF throw blank the tabpanel alone or an ancestor subtree (zero boundaries demo-wide).
5. **ME-26/D-17 focus-ring bleed percept** — geometry decided; annoyance is not.
6. **The 3×-mount DOM count** — rides kf-CubeScene's banked probe (`document.querySelectorAll('.matrix-grid').length`, prediction 3).
7. **ME-33 rendered dimension** — `getBoundingClientRect()` of `.matrix-grid` and one cell at desktop width; settles the browser's aspect-ratio min-transfer and decides ME-12's rendered look, ME-43's threshold, and the component's whole proportion. Highest-value probe.
8. **ME-2/ME-16 selected-cell legibility** — is a focus-only bold cue perceptible across a 4×4 monospace grid.
9. **ME-31 visible end-state** — cell 0 = 2 → Reset: blank, freeze, or snap (the throw is proven; the pixels are not).
10. **ME-36 corner dead-zone** — click a cell corner; assert neither focus nor selection.
11. **ME-30 witness** — edit cell 1, then cell 12; record cell 1's destruction (the born-RED gate's live twin). Reader-LC's five-second drag variant: drag the cube, watch the grid snap to composed T·R·S.
12. **NEW — ME-42 precision-death witness**: compose a 45° rotation, click a cosine cell showing `0.71`, press one key; assert the underlying value is now derived from the truncated string.

Rerouted, not SS-13: **ME-25 bundle-graph cost** → build-level analysis *(r2-carried)*.

## Routing summary (M-25 agglomeration input)

- **NO-WAVE-OWNER — the cube scene-repair packet (SS-1/SS-2 intake; spine = kf-CubeScene's routing block)**: the family core ME-1·2·4·9·10·11·12·15·16·23·26·27·33·34·35·36·37·38·39·41·**42·43·44·45** + the sync-topology spec **ME-30·ME-31 (≡ kf-CubeScene L-4+M-6/L-3, one spec with ME-7's retain-and-retarget cure)** — born-RED gates named in the rows.
- **KF.W4**: ME-5 + ME-32 (types + tests + lint = one gate row; ME-29 the exhibit).
- **KF.W6**: ME-13 (theme-aware ink; the cube.md `opacity-45` floor rides) · ME-18 (`/number-field` evaluation).
- **KF.W9**: K10/K26's substance (PRM mechanism unification ≡ kf-CubeScene D-8+M-7).
- **KF.W0 §B-12**: K17's manifest schism (worktree deletions vs origin's 7.0.0 pins, lock included).
- **glass-ui BH relay**: ME-40 (`cn` text-color catch-all), per the standing owner edict.
- **SS-13**: the 12 probes above.

## Closing verdict

**ADJUDICATED — r3 supersedes r2; nothing lost, one registry breach repaired, one mechanism found.** Both hostile re-readers survive the incredulity seat with their kills substantially sustained — reader-LC's provenance kill and CI kill, reader-DU's D-18 inversion and S-D2 kill all hold, three of them re-firing banked kills against the *other* reader — but the corpus's most instructive event is that its two loudest "misses" were banked at kf-CubeScene four days before either reader ran, and **r2's own "first booking" claim missed the same rows**: the anti-rename law now applies to the registry's own seats, and ME-30/ME-31 fold home to L-4+M-6/L-3 carrying three independent derivations each. The demotions are wholesale: both readers' ME-1 BLOCKERs die on K13's terminated-chain mechanism, both re-confirm the killed C-15 and struck SceneId cells, both repeat the substrate-dead phantom dep, and reader-DU asserts the CI clause reader-LC kills — five re-inheritance events in one corpus, every one an unread registry or an unnamed baseline, never a derivation error. The yield that survives is real and correctly sized: **ME-42** (the truncated display string is the write path — a numeric editor with no full-precision model behind its field), **ME-43** (overflow with no recovery on the number the editor exists to show), **ME-44/ME-45** (the shipped Card-header and Slider-marks affordances declined, one reader cell corrected against the tree), plus the M-7 rider that upgrades ME-2/ME-13 to *found, ruled-in-tree, unlanded* on the authority of the repo's own design doc. Terminal shape: **0 BLOCKER · 21 MAJOR · 19 MINOR · 5 INFO across 45 rows, original ids for life; the family routes NO-WAVE-OWNER into the cube scene-repair packet on kf-CubeScene's spine; ME-5/ME-32 ride KF.W4; ME-13/ME-18 ride KF.W6; the PRM substance rides KF.W9; the manifest schism rides KF.W0 §B-12; ME-40 rides the glass-ui BH relay; twelve probes ride SS-13.** Nothing here stamps a wave; every disposition is agglomeration input for the forming X·KF specs, awaiting the owner's begin-word.

— end of apotheosis r3. The three challenge files and both fresh reader returns remain immutable beside this record per E-1/E-3; the superseded r1/r2 identities, rulings and kills are carried herein (supersession block). Dispositions are ADJUDICATED, not VERIFIED.
