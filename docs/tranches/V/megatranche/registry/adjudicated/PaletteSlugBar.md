# PaletteSlugBar — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt.

**Seat**: apotheosis adjudicator over two hostile re-readers (design-and-usage · library-and-code).
**Date**: 2026-08-06 · branch `tranche-u` · **Corpus**: 15 challenge runs (C/D/L × 5, r2 governs: `challenge-C-implementation.md` · `challenge-D-design.md` · `challenge-L-library.md`) against `demo/palettes/browser/slug/PaletteSlugBar.vue` and its dependency chain.
**Method**: both re-reader returns were attacked before adoption. Every verdict-level disagreement and every load-bearing new find was re-derived against the tree with this seat's own instruments. The largest single dispute — the dead-`variant` census (reader-1: 51 sites; reader-2: 16 sites) — was settled by this seat's own tag-aware multiline census, which killed one reader's number and confirmed the other's exactly. The corpus's headline new BLOCKER (the `setActiveTab("saved")` route-throw) was re-proven end-to-end from bytes: viewSchema's 14-member `ViewId`, router/index.ts's 14 registered names, the two casts at usePalettePorts.ts:87-88, useViewManager.ts:72-79's `router.push`, and vue-router 5.1.0's synchronous `throw createRouterError(1,…)` (dist/vue-router.js:670, reached from pushWithRedirect:1243's synchronous `resolve(to)`).
**Instruments this session (all static; zero browser probes — probe parsimony)**: full reads of PaletteSlugBar.vue (243 lines), useSlugMigration.ts (whole), SlugEditLayer.vue (whole), ProfileSection.vue (whole), viewSchema.ts:1-60, router/index.ts (whole), usePalettePorts.ts:70-145, useViewManager.ts:35-84, MigratePalettesDialog.vue:1-45, canonical.ts:45-72, vitest.config.ts (whole); node tag-aware census over `<Button…>` open tags in demo/**/*.vue (55 tags / 22 files / 51 with `variant=` / values outline×28 ghost×19 destructive×1 default×1 primary-audacious×2); greps: `<PaletteSlugBar` mounts (0), `slugBarRef` (6 hits, one file), `slug-pill` (10 code sites incl. AdminUsersPanel.vue:98/:167), `z-popover` (4, one on PopoverContent), `hasSavedPalettes`/`resetEditMode` (declaration-only), `emphasis=` (2 sites), `focus` in the subject file (7; `.focus()` → 1), importers of `demo/palettes/browser/index.ts` (0), `--select-font` in installed glass-ui (0 hits), `--dropdown-menu-font` (read at dist/components/_shared/menu.css), `font-display` on DropdownMenuContent (4 live sites); installed producer bytes: glass-ui 7.0.0 Button.vue.d.ts (emphasis/tone/size/iconOnly/loading — **no** `variant`), `grep -c variant button-Bu9F4uU6.js` → 0, `emphasis: { default: "secondary"` present, no `inheritAttrs:!` in the button chunk, search.js `inheritAttrs: !1` + `ref_key: "inputRef"` spread; @vue/test-utils 2.4.11 cjs :98-99 `transition: true, 'transition-group': true` + :7777 DEFAULT_STUBS; vue-router 5.1.0 dist as above; export tree (`demo/palettes/export/` = 12 modules, **no index.ts**; export.ts:9-11 naive slugify; utils.ts:3-12 NFKD slugify; zero `slugify` hits under export/); eslint.config.js:230-250 + `ls demo/@` → ENOENT + zero `"@components` refs; foundation.css :371-374 (the mono pins), :582-586 (the slug-pill recipe comment), :746 (forced-colors roster incl. `.slug-pill`); tranche-X wave map W0–W11 read for routing (W4 §a files, W7 §export rows :42-43/:92/:195-199/:347-349, W8 scope 2/3 + :310-315 + §d).
**Motion/PRM law**: no motion/PRM claim is carried in this roster. `motion-quarantine.md` FALSE rows M-01..M-33 name other components (ActionFeedback, AdminListSkeleton, PaletteRenameInput, picker-*, shell-dock-actiontoolbar, wb-gradient-pane) — none is PaletteSlugBar; D-30's forced-colors limb is a source-omission claim, not a PRM claim, and its receipt was re-verified here.

## Scoreboard

**24 CONFIRMED (2 BLOCKER · 13 MAJOR · 7 MINOR · 2 INFO) · 13 NEW FINDS ADMITTED (1 BLOCKER · 7 MAJOR · 4 MINOR · 1 INFO) · 9 KILLED (8 reader kills upheld + 1 kill by this seat of a reader's own census).**

Headline: the component itself is an orphan and dies by deletion — but the corpus's gravest defects are NOT the orphan's. The new BLOCKER (N-1) breaks **every live login success path today**; C-39's session destruction is live today via SlugEditLayer; the dead-`variant` axis is a 51-of-55-site repo condition; and the only login-error channel in the shipped product is a `?.` on a permanently-null ref beside a live twin that never renders its own error state. Deleting the file cures roughly a third of this roster; the rest ships.

## Ruled disagreements (this seat's actual work)

| dispute | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-26 census scope** | 51 Button sites in demo carry the dead prop (tag-aware) | 16 shipping `variant="` sites in 8 files | **Reader-1 EXACT; reader-2's census KILLED** | My node census over multiline `<Button…>` open tags: **55 tags / 22 files / 51 with `variant=`** vs exactly **2** `emphasis=` sites (PalettesPane.vue:113, AdminUsersPanel.vue:174). Reader-2's 16-list omits ProfileSection.vue entirely (:60/:112 `outline`, :136 `ghost` — 3 sites), MigratePalettesDialog.vue (3), CurrentPaletteEditor (3), GenerateControls (3), PaginationBar (2), VersionHistoryDrawer (2), PaletteCard, MiniColorPicker, UserSortMenu, MixConfigBar. The distribution: outline×28, ghost×19, destructive×1, default×1, primary-audacious×2 — the last two values do not exist in ANY Button vocabulary, past or present. |
| **D §2 state-model kill** | KILLED (the `@mbabb` co-render is a wordmark menu, not an identity state) | not addressed | **Kill UPHELD** | My ProfileSection.vue read: the identity cluster is the strict three-way `v-if="pm.userSlug.value"` (:52) → `v-else-if="pm.isAdminAuthenticated.value"` (:95) → `v-else` Login (:102); the `@mbabb` block (:126-179) is a sibling wordmark menu, named so by its own comment (:130-134). The orphan's three-way state model IS the product's. |
| **C-2 ranking** | carries C-2 as BLOCKER-confirmed | kills the r2 §5 ranking ("strongest implementation defect") | **Ranking kill UPHELD; mechanism CONFIRMED at MAJOR** | Mechanism proven from producer bytes (search.js `inheritAttrs: !1`, non-class attrs spread onto the inner `<input>`, never onto `j(e.tag)`). But it has zero live reach: no other `SearchBar tag="form"` + `@submit` exists, and the live login form (SlugEditLayer.vue:76-78) is a native `<form @submit.prevent>` with the handler on the form — correct. A zero-reach defect cannot outrank D-26/N-1, which ship today. |
| **D-33 receipt** | carries the finding | kills the published grep receipt (`grep -c focus` → 1) | **Receipt kill UPHELD; finding survives on corrected receipt** | My run: `grep -c "focus"` → **7** (six `focus-visible:` utility seats + :179); `grep -c "\.focus()"` → **1** (:179, entry only). The substantive claim (focus never arrives under `out-in`, never restored on any of the three exits :14/:36/:215) verifies statically. |
| **D-28's live-twin framing** | "SlugEditLayer fires it from a bare icon control with no confirmation whatsoever" | routes it through the composable | **RESCOPED — reader-1's "no confirmation whatsoever" is FALSE for palette-holders** | SlugEditLayer.vue:107 calls `pm.onRegenerateSlug()` → useSlugMigration.ts:91-104, the SAME gated path: palette-holders get the migrate dialog; only the zero-palette user regenerates unconfirmed. The finding's core (the gate asks a data question, not a danger question; zero-palette identity rotation is unconfirmed everywhere) survives; the layer-specific escalation does not. |
| **L3-3 severity** | MAJOR (as L5-1) | BLOCKER | **MAJOR** | The shipped path functions (naively); nothing crashes and no data is lost. The user-facing break it enables — N-6's hidden-dotfile downloads — is admitted separately at MAJOR. BLOCKER is reserved in this roster for shipped crashes, dead error channels, and unreachable-by-construction product surface. |
| **Migration silent-failure severity** | MINOR (as a family rider on L-3) | MAJOR | **MAJOR — reader-2's version adopted** | The `transfer` ordering is decisive (my read, useSlugMigration.ts:59-71): `userLogin` lands FIRST, then `publishAllLocal()` — so a partial/total failure strands the user on the NEW identity with an arbitrary subset of their library and a success-shaped UI. That is data-affecting, not cosmetic. |

## Adjudicated defect roster

Severities and dispositions are ADJUDICATED, not VERIFIED — nothing here stamps a wave. Line anchors are live-tree reads of 2026-08-06; re-resolve before executing any cure. Routing law: a row routes to a tranche-X wave only where that wave's written scope covers the surface; otherwise **NO-WAVE-OWNER** so the next formation boundary consumes it (nearest class home named in parentheses).

### BLOCKER

- **A-1 (C-1/L-1/D-1) — the component renders on zero routes; it is an orphan.** All three axes + both readers + this seat: `<PaletteSlugBar` appears in no template; the only code refs are an `import type` + `ref<InstanceType<…>>` (useSlugMigration.ts:6/:30) and two barrel lines (browser/slug/index.ts:3, browser/index.ts:44). The live twin is SlugEditLayer.vue, mounted at Dock.vue:149. **Disposition: NO-WAVE-OWNER (nearest: X-W8.d dead-surface subtraction — the unit's file bounds do not currently name this file; the X-W8.a census addendum must add PaletteSlugBar.vue + both barrel rows + the `slugBarRef`/`import type` plumbing in useSlugMigration.ts, or the deletion is lost).** Deletion alone discharges A-4, A-17, A-23, A-24, A-25, A-26, A-27, A-28, A-32, A-33, A-34 and the orphan-half of A-5/A-8/A-15/A-16.
- **A-2 (L-3) — the live login-failure channel is a `?.` against a permanent null; every login failure is silent.** Verified both halves: `slugBarRef` is created (useSlugMigration.ts:30), written through four `slugBarRef.value?.setError(…)` calls (:84-87), returned (:121), and bound by NOTHING (all 6 repo hits are inside the one file); the live twin declares `slugError` (SlugEditLayer.vue:13), writes it at :18/:43/:49/:59-62, and its template (:75-119) renders it nowhere. **Disposition: NO-WAVE-OWNER (the cure — a real reactive error sink on useSlugMigration consumed by SlugEditLayer's template — must also carry N-3's channel; no X wave owns the login flow's error surface).**
- **A-3 (N-1, reader-2 new find) — `setActiveTab("saved")` navigates to a route that does not exist; all three live login success paths throw out of vue-router.** RE-PROVEN END-TO-END BY THIS SEAT: `"saved"` is not one of viewSchema.ts:35-49's 14 `ViewId`s nor router/index.ts:22-35's 14 registered names; the chain useSlugMigration.ts:55/:69/:76 → deps.setActiveTab (ALWAYS supplied — usePalettePorts.ts:88) → `depsSwitchView("saved" as ViewId)` → useViewManager.ts:78 `router.push({ name: id, … })` → vue-router 5.1.0 pushWithRedirect:1243 calls `resolve(to)` **synchronously** → dist/vue-router.js:670 `if (!matcher) throw createRouterError(1, { location })`. The :73 same-view guard cannot fire (currentView is always a real ViewId). Three live consequences, each verified: (1) **admin login** — teardown + `adminLogin` land, then the throw escapes as an unhandled rejection because SlugEditLayer.vue:54 does not await `pm.onSlugSwitch(…)`, so the layer's own try/catch (:44-65) cannot see it; the view never switches. (2) **slug login, no local palettes** — `await deps.userLogin(value)` SUCCEEDS at :75, then :76 throws; the throw is caught by the LOGIN catch (:77), `e instanceof ApiProblem` is false, and the "error" is written to A-2's null ref — a successful login converted into a silently-swallowed router error. (3) **migration** — :69 throws inside `pendingMigrateAction`; onMigrateRespond (:112-114) degrades it to `console.warn("Migration action failed:")` — every palette migration reports failure after succeeding. Fifteen runs quoted the offending line verbatim while analysing C-39 and none checked whether the route exists. **Disposition: NO-WAVE-OWNER — flagged URGENT for the formation boundary (a one-line cure, `setActiveTab("palettes")` or a typed-deps fix per N-2, unbreaks the entire live login flow; X-W3 owns route closure and X-W5 owns the route↔scene law, but neither's written scope covers this call chain).**

### MAJOR

- **A-4 (C-2) — `@submit.prevent` is bound to SearchBar's inner `<input>`, never the `<form>`; the component's only submit path never fires.** Producer bytes verified (search.js: `inheritAttrs: !1`; `{class:e, …t}` spread onto the input vnode). Escape works for the mirror-image reason (keydown originates at the input) — the asymmetry is the proof. Zero live reach (ranking kill upheld above). **Disposition: dies with A-1's deletion. The producer-shape hazard (SearchBar's root-vs-input attr split) rides the standing glass-ui BH relay as an ergonomics note, not a defect.**
- **A-5 (L-2) — `looksLikeSlug` + `normalizeTokenInput` are byte-identical forks between the orphan (:184-196) and the live layer (SlugEditLayer.vue:25-37), both retaining the retired 409/404/429 substring taxonomy (:216-224 / :57-62) that useSlugMigration.ts:78-82 documents as never-fired.** Verified by parallel read. **Disposition: the orphan copy dies with A-1; the LIVE copy's extraction (the classification decision belongs in `demo/platform/auth/`, not a template layer) is NO-WAVE-OWNER — it is the same act as A-8's step-3 cure and must land with it.**
- **A-6 (L-4) — the eslint boundary that browser/index.ts:9-11 claims "enforces it standing" matches zero files.** Verified: `demo/@` is ENOENT (four dead globs at eslint.config.js:234-238), and the only pattern (`@components/custom/palette-browser/**/*.vue`, :246-248) matches nothing — zero `"@components` refs repo-wide (alias retired W43/RF-15). A green run is indistinguishable from a satisfied one. **Disposition: X-W8 (scope item 3 names the dead `demo/@/**` glob deletion + "the boundary ban that keeps the barrels dead" — this row's pattern-liveness half must ride the same unit).**
- **A-7 (L3-3 + L5-1) — the shipped export path is the naive twin of the tested one.** Verified: `demo/palettes/export/` = 12 modules with NO index.ts, so usePaletteExport.ts:9's `from "./export"` resolves to the FILE export.ts; export.ts:9-11's slugify has no NFKD/mark-strip vs utils.ts:3-12's correct one (`Café Noir` → `cafe-noir` vs `caf-noir` by construction); the tested directory (byte-exact.test.ts) is unreachable from the app. Reader-1's kill of the L5-1 cure limb UPHELD (see killed register). **Disposition: X-W7 (explicit — waves/W7.md:42-43/:92/:195-199 delete export.ts, route the composable at the certified serializers, keep ONE slugify = the NFKD one; gate at :347-349).**
- **A-8 (C-39 + N-7) — `ADMIN_TOKEN=""` (and sibling empty-token inputs) normalise to `""`, are emitted as an admin token, and the parent destroys the user session before the admin login fails — LIVE TODAY via SlugEditLayer.** Verified end-to-end: the `if (!raw) return` guard tests the pre-normalisation string in BOTH copies (orphan :200, layer :41); `!looksLikeSlug("")` → true; useSlugMigration.ts:52-56 runs `clearUserSlug()` (→ `_registrationCancelled = true` + `clearAuth()`) FIRST, then `adminLogin("")` persists an empty token while `isAuthenticated = !!""` stays false. Compounded by A-3: the live sequence today is teardown → empty-token persist → router throw → unhandled rejection, with no error rendered (A-2). **Disposition: NO-WAVE-OWNER — the cure is three-legged and all legs must land: (1) A-1's deletion, (2) guard the NORMALISED string, (3) move slug/token classification into `demo/platform/auth/` (with A-5). X-W4 modifies SlugEditLayer.vue but only for control targets/labels — its scope does not cover this.**
- **A-9 (C-38) — @vue/test-utils stubs `<Transition>` by default, so a naive jsdom gate asserts the OPPOSITE of the shipped focus behaviour (A-16's mechanism), and no instrument in the audit ever observed the defect it reported.** Verified from installed 2.4.11 bytes (cjs :98-99 config stubs; :7777 DEFAULT_STUBS; the in-source "even if we are using `mount`" comment family). Compounding: zero importers of @vue/test-utils anywhere (unused devDependency, package.json:96), zero `mount(`/`shallowMount` in test/ or demo/test/. **Disposition: NO-WAVE-OWNER (instrument law for the next formation: any component-mount gate must stub-strip Transition or run a real browser; nearest home X-W1's gate re-authoring, whose written scope does not cover vitest SFC capability).**
- **A-10 (C-18) — the shipped vitest project cannot import an SFC at all.** Verified: vitest.config.ts has NO plugins array (no @vitejs/plugin-vue); include = `test/**` + `demo/test/**`; no component-mount test exists. The axis's own repro config exists on disk precisely because of this. **Disposition: NO-WAVE-OWNER (same instrument-capability decision as A-9 — admit SFC mounting deliberately or record that component gates are e2e-only; must be a ruled decision, not drift).**
- **A-11 (D-26/C-9, census by this seat) — `variant` is not a glass-ui 7 Button prop; 51 of 55 Button open tags in demo carry it; the axis is `emphasis` (default `"secondary"`), and the unknown prop falls through $attrs to the DOM.** Producer verified (Button.vue.d.ts prop set; 0 `variant` in the chunk; `emphasis: { default: "secondary" }`; no inheritAttrs disable). Only 2 sites repo-wide use the real axis. Two of the 51 values (`primary-audacious` ×2) never existed in any Button vocabulary. **Disposition: X-W8 (its :310-315 unit already owns the variant→emphasis subtraction but its RED names only PaginationBar/UserSortMenu; per X-W8 scope item 1 the file bounds bind to the CENSUS, and this seat's 51-site/22-file census is that census's mandatory denominator). The HIERARCHY consequences (A-12, A-13) are separately owned — a mechanical variant-strip that leaves every button `secondary` closes this row and still ships those two.**
- **A-12 (N-4, reader-1 new find) — MigratePalettesDialog presents a three-way consequential choice (Publish / Transfer / DISCARD your library) as three visually identical filled capsules.** Verified: :14 `variant="default"`, :22 `variant="outline"`, :31 `variant="ghost"` — all dead on glass-ui 7, all rendering `emphasis="secondary"`; the only differentiation is `text-muted-foreground` on the discard row. Mounted live at App.vue:152, opened by the exact path this component's `switchSlug` emits into (useSlugMigration.ts:59-71). **Disposition: NO-WAVE-OWNER (the emphasis-ladder authoring — primary/secondary/quiet + a considered discard register — must be authored, not mechanically swept; note for the X-W8 executor: the variant-strip alone does NOT close this).**
- **A-13 (N-5, reader-1 new find) — FlagReportDialog's destructive confirm renders as a neutral twin of its own Cancel.** Verified: :38 `variant="outline"` Cancel, :41-42 `variant="destructive"` Report — `variant` dead, the real axis is `tone` (which AdminUsersPanel.vue:175 already uses correctly: `:tone="confirmDestructive ? 'destructive' : 'neutral'"`). The one irreversible action in the flag flow and its dismissal are a matched pair. **Disposition: NO-WAVE-OWNER (same authored-hierarchy family as A-12; the in-repo correct idiom is cited for the executor).**
- **A-14 (D-27) — the type matrix is violated at every seat: `font-display` beats the `text-small` control rung (utilities layer follows components — dist/styles/index.css `@layer theme, base, components, utilities`), `Login` is mono-bold, and `text-caption` is off the closed §4 matrix.** Static limbs verified (layer order; text-caption absent from VISUAL-CONSTITUTION §4; subject sites :54/:56/:73/:91/:98/:106/:113). Rendered values are dev-server probes — built-CSS confirmation is U-1. Live relays: ProfileSection.vue:70/:143, MobileMenuDropdown.vue:43, UserSortMenu.vue:16. The "least legible configuration" superlative is KILLED (see register). **Disposition: NO-WAVE-OWNER for the matrix-conformance sweep (X-W10 "Canonical Design and Motion" is the class home but its written scope does not name these rungs); the ROOT-CAUSE half is A-18.**
- **A-15 (D-28, rescoped) — an identity-destroying command is the quietest row in the menu, and confirmation is gated on data-migration, not danger: the zero-palette user rotates identity with no confirmation on EVERY live path.** Verified in the orphan (:112-118), the live menu (ProfileSection.vue:87-88, same `text-muted-foreground`, same `pm.onRegenerateSlug()`), and the live layer (SlugEditLayer.vue:104-110 — via the same gated composable; reader-1's "no confirmation whatsoever" corrected, see ruled disagreements). **Disposition: NO-WAVE-OWNER (a danger-register + unconditional confirm decision on `userRegenerate`; survives A-1's deletion in two live surfaces).**
- **A-16 (C-3/D-33, receipt corrected) — focus never reaches the input on entry (under `out-in` the SearchBar is unmounted at `nextTick`, so `searchBarRef.value` is null and the double `?.` swallows it) and is never restored on any of the three exits (:14/:36/:215; `.focus()` count = 1, entry only).** Verified statically. VISUAL-CONSTITUTION §5.1 requires the connected opener on close. **Disposition: dies with A-1 for this file; the PATTERN (focus into a `<Transition mode="out-in">` arm via a 50 ms timer) is the lesson A-9 makes untestable in jsdom — carried as the instrument-law example.**
- **A-17 (C-4) — `slugSwitching` round-trips inside one synchronous turn (async fn, zero awaits, `finally` in the same tick), so the spinner, the swapped aria-label, and the double-submit guard are unrenderable** — four hand-rolled parts of the `loading` prop the producer already ships. Verified from source (:198-225). **Disposition: dies with A-1. NOTE for the live twin: SlugEditLayer has the same shape (:39-66, no await on :54) — its `slugSwitching` is equally unrenderable; the fix is awaiting `pm.onSlugSwitch` (which also unswallows A-3's rejection into the layer's catch) — rides A-8's cure.**
- **A-18 (N-8, reader-1 new find) — the demo pins `--dropdown-menu-font` to mono at :root and then defeats it per-instance at 100% of live consumers.** Verified: foundation.css:371-374 pins it; the producer genuinely reads it (menu.css `font-family: var(--dropdown-menu-font, inherit)`); all four live DropdownMenuContent sites override with `font-display` (ProfileSection.vue:70/:143, MobileMenuDropdown.vue:43, UserSortMenu.vue:16) and win on layer order. The root token never paints. This is the exact inverse of the style-at-root edict, and demo/DESIGN.md:11 still documents the pin as live. **Disposition: NO-WAVE-OWNER (a one-token decision: either the pin is wrong — change the root and delete four overrides — or it is right — delete it and pin `font-display` at root; DESIGN.md:11/:37 must be corrected either way).**
- **A-19 (N-2, reader-2 new find) — two unsound casts at the port boundary defeat the ViewId type system and are what let `"saved"` compile.** Verified: usePalettePorts.ts:87 `currentView as Ref<string>` (widening a writable, invariant Ref), :88 `(tab: string) => depsSwitchView(tab as ViewId)` (unchecked narrowing). The correct guard (`isViewId`) ships and is used in the route→state direction (useViewManager.ts:43-46) but not state→route. Typing the deps `Ref<ViewId>` / `(id: ViewId) => void` makes A-3 a compile error in three places. **Disposition: NO-WAVE-OWNER — lands WITH A-3 as its structural cure (one edit, vue-tsc enforces it for free).**
- **A-20 (N-3, merged from both readers; reader-2's severity adopted) — the palette-migration flow reports failure only to the console, discards per-palette failures entirely, and on `transfer` runs AFTER the identity switch, stranding the user on the new account with a partial library and a success-shaped UI.** Verified: useSlugMigration.ts:36-45 bare `catch {}` per palette, :46-48 `console.warn` outer, :106-116 `console.warn` on the action, no reactive error state, no dialog error slot (App.vue:152-156 binds open/mode/respond only). Same root cause as A-2 — the composable has no error sink. **Disposition: NO-WAVE-OWNER — one cure with A-2 (the error sink must carry both channels + a per-palette failure count).**
- **A-21 (L-6/L3-6) — 19 single-line `demo/ui/*` re-export barrels (18 reaching the glass-ui root); this one file reaches the design system by three different paths (:132 subpath, :133-134 shim→root, :145 root direct).** Verified in-file; repo counts adopted from the corpus (consistent with the ColorSpaceSelector adjudication's L-4 at 48 files/90-92 statements). **Disposition: X-W8 (explicit scope item 2: delete the 19 barrels, re-point at the narrowest published export).**

### MINOR

- **A-22 (D-29/D-30 + N-10) — six hand-rolled focus seats at `ring-ring/40` with `outline-none`, a background-colour-only state vocabulary, zero forced-colors handling in the file — while a producer recipe that solves all of it (`focus-ring` + the a11y-overrides WHCM branch) ships installed and the demo documents its own composition rule (demo/styles/focus-ring.css).** Source omission verified (6 identical seats; the scoped block is a comment; `.slug-pill` IS in the forced-colors roster at foundation.css:746 — the buttons are not). Reader-1's rescope adopted: ONE repo-scale row (~22 `ring-ring/40` sites per the corpus grep) with a named, already-installed cure — not two per-file MINORs. Rendered WHCM result is U-2. **Disposition: NO-WAVE-OWNER (repo-scale focus-recipe adoption; the 6 in-file seats die with A-1).**
- **A-23 (C-10/D-6) — the account-menu trigger is a 22×22 px operable target** (`p-1` + `w-3.5`, arithmetic: 4+14+4). Dies with A-1. **The live-twin targets are X-W4's** (its §a explicitly lifts the three `DockControl compact` slug controls to ≥24/`--control-floor`). **Disposition: orphan half dies with A-1; live half X-W4.**
- **A-24 (C-12/D-12/L-9) — dead public surface: `hasSavedPalettes` required-but-never-read, `copy` declared-but-never-emitted (the menu calls the local `onCopySlug` directly — three commands delegate up, one does not, no rule), `resetEditMode` exposed with zero callers, pre-3.5 ref idiom beside a twin already on `useTemplateRef`.** All verified by grep. **Disposition: dies with A-1.**
- **A-25 (C-14) — no cancel path clears `slugError`; the stale error `<p>` (:124-126, outside the Transition) survives into default mode.** Verified. **Disposition: dies with A-1; the error-lifetime rule belongs to A-2's new sink.**
- **A-26 (D-31) — `z-popover` on a producer-portaled PopoverContent; the only such use in demo (my grep: 4 hits, the other three are non-producer surfaces).** **Disposition: dies with A-1.**
- **A-27 (C-11) — the 50 ms `setTimeout` mutates state with no unmount cleanup** (imports only `ref, nextTick`). **Disposition: dies with A-1.**
- **A-28 (C-8/D-8, framing corrected per reader-1's kill) — the substring-taxonomy `catch` is unreachable for the SHIPPED parent (async handler → rejection, not synchronous throw), so :216-224 is dead code — but not "by construction": a synchronous-throwing emit listener would reach it.** **Disposition: orphan copy dies with A-1; the live twin's identical dead taxonomy (SlugEditLayer.vue:57-62) is A-5's.**
- **A-29 (N-9, reader-1 new find) — `--select-font` is a dead token on glass-ui 7: zero hits in the installed package** (verified by this seat), while foundation.css:373 pins it and DESIGN.md:11/:37 document it as live — the documented mono-Select intent silently reverted at the W44 adoption. Same silent-major-version-drift family as A-11, token axis. **Disposition: NO-WAVE-OWNER (one-line deletion + doc correction, OR a glass-ui BH relay asking for the token back — must be a ruled choice; rides A-18's token decision).**
- **A-30 (N-11, reader-2 new find) — `demo/palettes/browser/index.ts`, the self-declared "TOP-LEVEL SEAM… stable public API", has ZERO importers** (verified: no bare-`browser` specifier anywhere; every consumer reaches a sub-barrel). Both halves of the U-F47 boundary — the barrel AND its gate (A-6) — are inert. **Disposition: NO-WAVE-OWNER (rule with A-6 at the boundary-ban authoring: either the seam is real — enforce it — or it is not — delete it; a 44-line dead file must not survive with a false comment).**
- **A-31 (N-12, reader-2 new find) — useSlugMigration's deps contract requires `userLogout` and `ensureUser` and calls neither, while hard-importing `useSession` and bypassing the injected session-ensure.** Verified from this seat's full read (the complete `deps.` read-set excludes both; :2/:19/:34 the hard import). The composable files the same phantom-contract defect the corpus filed against its own component. **Disposition: NO-WAVE-OWNER (two-line deletion + one DI decision; lands naturally with A-2/A-20's composable rework).**

### INFO

- **A-32 (D-34) — the `isAdmin` prop ("session is admin") is silently shadowed inside `onSlugSwitch` by a same-named local ("input does not look like a slug"); the emit sends the local.** Verified. Dies with A-1; the live twin repeats the local (SlugEditLayer.vue:46) without the prop collision.
- **A-33 (D-35) — `aria-haspopup="dialog"` announced on the trigger while the PopoverContent declines the producer's `ariaLabel`; an announced-but-unnamed dialog of four buttons.** Dies with A-1.
- **A-34 (N-13) — mixed ellipsis register (`...` at :12 vs `…` at :24), the U+2026 sitting in an aria-label C-4 proves unrenderable.** Dies with A-1.

## Killed-claims register

1. **D §2 "wrong-shape state-model" inference (challenge-D-design.md:119-124) — KILLED (reader-1; upheld by this seat's read).** `@mbabb` is the author WORDMARK menu (ProfileSection.vue:126-179, self-naming comment :130-134), a sibling of the identity cluster, which is the SAME strict three-way switch as the orphan (:52/:95/:102). `Login` and `@mbabb` co-render because they are different concepts. The "positive frame evidence" falls with it.
2. **L5-3 limb 2 "single-area recipe / two phantom consumers" — KILLED (both readers; upheld by this seat's grep).** AdminUsersPanel.vue:98 and :167 are live `slug-pill` consumers in a second feature tree; the foundation.css:582-584 comment has exactly ONE phantom (the dead slug bar). Post-deletion: 6 live sites, 3 files, 2 areas — the cross-feature warrant holds. Limb 1 (the per-instance `:style` contract codified in the comment) survives; note all live consumers now feed certified ink.
3. **L5-1's prescribed cure + risk mechanism — KILLED (reader-1; upheld by this seat).** Zero `slugify` exists under `demo/palettes/export/` (my grep), deliberately (canonical.ts:49-53: "no slugifier exists"); there is no serializer consumer for a shared function to bind to, and rewiring the import removes slugification from the export path rather than selecting between bodies. The divergence (A-7) stands; the cure and its smuggled-behaviour-change mechanism do not. X-W7's authored cure (one slugify in utils.ts) is the correct one.
4. **D-27's closing superlative ("the least legible configuration the design system can produce") — KILLED (reader-1; upheld).** `--type-micro` (11px) and `--type-admin-label` (10px) both ship as utilities, and ProfileSection.vue:150 renders `text-micro italic … font-display` LIVE — a smaller italic serif help line than the orphan's. The finding survives; the superlative was not only wrong but pointed away from the stronger live version of the same claim.
5. **D-8/C-16's "no throwing call, unreachable by construction" — KILLED as framed (reader-1; upheld).** `emit("switchSlug", …)` invokes listeners synchronously; a synchronously-throwing listener reaches the catch. Unreachable for the SHIPPED async parent — a property of the consumer, not the block. The dead-taxonomy conclusion survives (A-28).
6. **D-33's published receipt (`grep -c "focus"` → 1) — KILLED (reader-2; reproduced by this seat: 7).** Corrected receipt: `grep -c "\.focus()"` → 1. Finding survives (A-16). Recorded because the two receipts either side of it in the same §7 block verify exactly — a transcription failure, not a method failure.
7. **C-2's ranking ("the strongest implementation defect") — KILLED (reader-2; upheld).** Zero live reach; the combination exists nowhere else; the live login form binds its submit correctly. Ranking a zero-reach defect above D-26/N-1 inverts the repair priority the same document's disposition depends on.
8. **L2-3 (type/runtime split via tsconfig paths) — already withdrawn by run-5 §C-1; withdrawal INDEPENDENTLY RE-CONFIRMED (reader-2; concurred).** ESM package self-reference resolves the subpaths regardless of the paths block. Carry the standing hazard instead: vite.config.ts:24-26 + tsconfig.demo.json:37-41's comments misled five consecutive auditors; rewriting two comments to name self-reference is the cheapest fix in the directory (same family as the ColorSpaceSelector adjudication's L-14).
9. **Reader-2's own 16-site `variant` census — KILLED BY THIS SEAT.** The tag-aware count is 51 of 55 Button open tags in 22 files (values: outline×28, ghost×19, destructive×1, default×1, primary-audacious×2). The 16-list omits ProfileSection (×3), MigratePalettesDialog (×3), CurrentPaletteEditor (×3), GenerateControls (×3), PaginationBar (×2), VersionHistoryDrawer (×2), and four singles. Reader-1's census (which reproduced C run-3's figure) is adopted as A-11's denominator. Lesson (the ColorSpaceSelector L-9.1 law again): a census claim must paste the tag-aware command, not a single-line grep — `variant="` on a line is not `variant` on a Button.

## Superlatives — preserved with provenance

- **demo/color-session/ink.ts:145-175 + ProfileSection.vue:22-31** (reader-1): the certified-ink path — golden-step de-emphasis in oklch RE-CERTIFIED against a contrast floor, two hosts/two referents documented in situ. The orphan's raw `:style` pick (:49) is the fossil; every live `.slug-pill` consumer feeds certified ink.
- **demo/shell/viewSchema.ts + useViewManager.ts:51-70** (reader-2): the schema/runtime split with provenance in the header, an `isViewId` guard correctly applied at the route→state boundary, and the self-invalidating view-tagged paneOverride. The reason A-3 is a two-cast leak and not a systemic one: the type that catches it already exists and is already correct.
- **demo/palettes/useSlugMigration.ts:78-82** (both readers): first-order defect archaeology — names the retired bug, quotes the server titles that disprove the old test, shows the typed replacement below. Its counterexample still stands verbatim in both surviving copies of the dead taxonomy (A-5), which is exactly why the comment matters.
- **demo/palettes/export/canonical.ts:49-71** (reader-1): the correct answer to its sibling's defect, unreachable in the same directory — refuses to slugify, composes typed filename stems, byte-exact-tested. X-W7 makes it the shipped path.
- **demo/styles/focus-ring.css:1-35** (reader-1): a design-rationale header with a falsifiable mechanism, naming both deaths of U-F25, which half this file cures, why the affordance is dual-contrast, and the exact composition string consumers must write — the string A-22's ~22 hand-rolled seats should have been reading.
- **demo/styles/foundation.css:94-102** (reader-1): cascade archaeology naming the failed cure, its failure mechanism, and the working cure in three lines; verified real (`--font-stack-display` at :218, bridged by the producer).
- **challenge-L-library.md §Corrections C-1** (both readers): a seat reproducing a prior run's probe, retracting its own MAJOR, and extracting the transferable lesson (two comments misled five consecutive auditors). The best single piece of work in the 15-run corpus, and the model this adjudication's kill-register follows.
- **challenge-C-implementation.md §2 C-38** (reader-2): measuring the instrument before trusting it — proving no jsdom instrument in the audit had ever observed the defect it reported. Independently verified here from the installed VTU bytes.
- **docs/tranches/V/megatranche/audit/codex-provenance/motion-quarantine.md §1** (reader-1): the template for adjudicating a PRM claim by specificity-measurement rather than source order — and the reason this roster carries no motion defect.
- **demo/palettes/browser/index.ts:9-16** (reader-2): the PI-6 rationale is technically exact (SFC scoped styles ARE side-effecting imports; named re-exports ARE the tree-shake-honest form) — right reasoning governing a module with zero importers (A-30). Preserved as the register in which A-30's ruling should be written.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1** — whether `font-display` beats `text-small` in the BUILT gh-pages CSS (A-14/A-18). The dev-server probe (probes/psb-emphasis-type.out.txt: Fraunces 16.4px) and this seat's static layer-order derivation agree, but L5-2 records the production-bundle probe as invalid (its control string returned zero hits). Four live menu sites make this worth settling before any type cure lands.
- **U-2** — the RENDERED forced-colors result for the raw `<button>` seats (A-22), plus keyboard-focus, rtl, and zoom-200 frames: the source omission is CONFIRMED; no `palettes` frame exists under shots/forced-colors-desktop/, so the state-collapse is reasoned, not observed.
- **U-3** — whether `:close-delay="0"` on the help Popover (:45) strands its own two sentences of selectable prose for pointer users (the `data-grace-area-trigger` diagonal grace area may make it harmless; producer default 150 ms). Moot for keyboard/touch (D-5, run-4 D-20).
- **U-4** — the browser-observable surface of A-3's throw (which console messages, whether a Vue warning or global handler intercepts the unhandled rejection). The THROW is proven from bytes; only its rendering is unobserved. **This is the first probe the deep-audit frontend session should run.**
- **U-5** — C run-5's repro-suite green count (`23 passed`): all seven files exist with claimed shapes and both headline mechanisms are proven statically; the count itself is taken on report (probe parsimony — not executed here either).
- **U-6** — D-4/L2-4's contrast ratios, §5.1's 36.0 px SearchBar arm, D-3/D-22's error-box geometry: live-cascade readings, arithmetic sound, inputs unmeasured here.
- **NOT TRUTH-APT (recorded, not carried)** — D-32's "thirteen spacing literals cannot open a proportion row": the component renders nowhere, so it has no containing pane and no measurable C to be unrelated to; PROPORTION-AUDIT §5.8's own rule ("real rendered relation wins") cannot apply to a component with no rendered relation. Revives only if the file is ever mounted, which A-1 forecloses.

## Closing verdict

**ADJUDICATED — GUILTY AS AN ORPHAN, BUT THE ORPHAN IS NOT THE STORY.** 24 corpus rows CONFIRMED (2 BLOCKER, 13 MAJOR), 13 new finds ADMITTED (1 BLOCKER — the `setActiveTab("saved")` route-throw that breaks every live login success path today, re-proven here from vue-router's own bytes), 9 kills upheld or added (including this seat's kill of one reader's census). Deleting PaletteSlugBar.vue discharges eleven rows outright and halves four more; the remaining seventeen ship today through SlugEditLayer, ProfileSection, useSlugMigration, the export twin-path, and a 51-site dead prop axis. Routed: A-7→X-W7, A-6/A-11/A-21→X-W8, A-23's live half→X-W4; thirteen rows are NO-WAVE-OWNER and MUST cross the next formation boundary intact — A-3 (with its one-line cure and its typed-deps structural cure A-19) flagged URGENT. These dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this file.

— end of apotheosis. The challenger corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
