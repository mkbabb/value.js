`claude-fable-5` — served model receipt, line 1 per the M-23/L-18 commission; observed in this seat's own system prompt, not inherited.

# AboutPane — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code) of the full AboutPane challenge corpus (r2 governs).
**Date**: 2026-08-03 · **HEAD**: `c654824e` + working tree · branch `tranche-u` · **Corpus**: `docs/tranches/V/megatranche/audit/components/AboutPane/` — challenge-C-implementation.md, challenge-D-design.md, challenge-L-library.md, tracked `probe-D1.{mjs,json}`, `evidence/` ×2, `frames/` ×11.
**Law honored**: only write = this file; product source read-only; no browser tooling; motion-quarantine FALSE rows not carried (M-03, M-17, M-29, M-33 cited by id below); dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.
**Routing authority**: `registry/CARRY-CUT-LEDGER.md` (supersedes `V·MT*`/`V·C*` naming) + `docs/tranches/X/waves/W0–W11`. Where no X row owns a surface, the row is **NO-WAVE-OWNER**, flagged for the next formation boundary per the AdminListSkeleton precedent (its MT-AU1 carry-cut-gap finding).

**Method (L-18)**: both readers were attacked before either was adopted. Every verdict-level disagreement (D-6, C-1 severity, C-10, D-15, D-1/D-2/D-3 framing, D-9's evidence limbs, C-3's cardinality, MISS-1's weight) was re-derived against the tree or the tracked evidence by this seat. Two artifacts neither reader opened were opened here and were decisive: `frames/d9-rtl.png` (converts D-13's rendered limb from unproven to CONFIRMED) and a per-run dump of `probe-D1.json` (confirms the 11-vs-7 divider species split, the d8-zoom200 `present:false`, and the d3-jzazbz anomaly).

**Arbiter's own instruments this session (static + committed evidence only; zero live probes — probe parsimony)**: full reads of `AboutPane.vue` (99 lines), `ColorNutritionLabel.vue` (whole), `Markdown.vue:1-110`, `markdown/index.ts` (whole), `useMarkdownColors.ts` (whole), `useMarkdownHighlighting.ts:10-40`, `PaneHeader.vue` (whole), `PaneSlot.vue` (whole), `ErrorBoundary.vue` (whole), `App.vue:40-145`, `usePaneRouter.ts` (three regions), `shell.css:108-140`, `foundation.css` φ-ladder + U-F57 blocks; key censuses re-run: `colorSpaceInfo.ts` = **13** keys (`grep -cE '^ {4}"?[a-z0-9-]+"?: \{'`), `demo/ui` = **19** dirs, `pane-scroll-fade` = **9** hosts (4 `mx-auto` / 4 not / 1 divergent), `DocItem` = 1 declaration 0 consumers, `?source` = 0 consumers, `package.json#exports` = 7 keys no `.`, glass-ui dist value.js imports = subpath-only (`/color`×7 `/css`×4 `/easing`×2, zero bare), `hide-indicator` live at `ColorSpaceSelector.vue:64`, `specimenFor` rounds only the non-CSS branch (`:161-163`), router = 15 paths no `/about`, `viewSchema.ts:107` `right:"about"`; producer receipts: glass `Alert` compiled `announce:{default:"off"}`, `Separator` `decorative` computed, `--space-phi-5: 2.618rem`/`--space-phi-6: 4.236rem` in `dist/styles/tokens/sizing.css`; WCAG recompute of D-12 (dark trigger-vs-title **1.093:1 raw**, ≈1.19 composited; light 2.31 raw) — the reader's figures hold; probe-D1.json re-dumped per-run; `evidence/L3-prophoto-rgb-facts-and-empty-guide.png` and `frames/d9-rtl.png` read as images; motion-quarantine.md rows M-03/M-17/M-29/M-33 + §4 MQ-1 read verbatim; CARRY-CUT-LEDGER + X-W4/W5/W6/W8/W10 wave files read at the routing joints.

---

## Scoreboard

**42 roster rows: 40 CONFIRMED (1 BLOCKER · 16 MAJOR · 19 MINOR · 4 INFO) + 2 RECORDED-NOT-CONFIRMED (AB-41 hypothesis-as-labelled · AB-42 kept-decline) · 16 KILLED · 14 reader-misses ADOPTED into the roster · 5 UNPROVEN-NEEDS-LIVE residues (a sixth, D-13's rendered limb, converted to CONFIRMED by this seat from tracked evidence).**

Blocker-weight survivor: **the catalog lie** (D-9/L-1) — five selectable spaces render CIE-RGB-1931's dossier under their own names on a first-class surface; ONE identity with the already-adjudicated ColorSpaceSelector L-1 (cross-booked, never re-booked — anti-rename), owned and born-RED-gated at **X-W6.f (X:CSS-1, f1/f2/f3)**.

The gravest **unowned** result: the About recomposition (Card/Alert/dividers/tooltip-shells/66ch/two-selectors/hover-only-affordance) and the transport/config family (prop-drill, tsconfig parity) fold to owners that **do not exist in X** — the V-canon W18 row, MT-CSS-2, and W-HYGIENE were never cut into the X wave set. That carry-cut gap is itself a finding of this file (rider A-1).

---

## Ruled disagreements (the seat's actual work)

| id | reader 1 (design-and-usage) | reader 2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-6** | Mechanism limb KILLED (contain/backdrop-root inference backwards); consequence moved to unproven; BLOCKER dies | CONFIRMED MAJOR on token alpha + the tracked L3 frame | **CONFIRMED MAJOR — both partially right, merged.** Reader 1's TWO kills stand (the fade limb is the M-33 error class — no content mask exists, `.pane-scroll-fade` = `contain` + `scroll-timeline` only, PaneHeader.vue:54-57; and the `contain`-defeats-blur inference runs backwards). Reader 2's confirmation stands on what survives: `--glass-bg-resting` is a partial-alpha `color-mix` (α 0.65 L / 0.72 D) so the veil's `opacity: …→1` scrub (`:196-203`) can never occlude — alpha arithmetic, engine-independent — AND the phenomenon is *rendered in a tracked in-repo frame*: this seat read `evidence/L3-prophoto-rgb-facts-and-empty-guide.png` and can see `Created:`/`1931` and the `Red Green Blue / 0 to 1` row printing through the sticky band. Engine caveat recorded: the frame is a headless capture; `backdrop-filter` smear fidelity is a NEEDS-LIVE residue, so BLOCKER does not survive — MAJOR does. |
| **C-1 severity** | BLOCKER ("takes down the entire application") | MAJOR (boundary is announced, focus-managed, one-click recoverable) | **MAJOR — reader 2 wins.** This seat read `ErrorBoundary.vue` whole: `role="alert" aria-live="assertive" tabindex="-1"` (:19-21), `nextTick(() => alertRef.value?.focus())` (:64), real recovery `<Button>` + `reset()` (:30-33, :71-75), `return false` (:68). What dies is the two-pane GRID (`App.vue:50…:140` wraps both `PaneSlot`s), recoverably. The scope defect is real and MAJOR; the phrase is not what the source does. Reader 2's addendum adopted: on rejection `isLoading` sticks `true` forever (`Markdown.vue:59` set, `:61` never reached). |
| **C-10** | CONFIRMED (table rebuilt per mount, "re-created on every view switch") | KILLED (KeepAlive; About instantiated once per session) | **KILLED as filed — reader 2 wins, with an arbiter precision (rider A-2).** `PaneSlot.vue:120` `<KeepAlive :max="max">`; `App.vue:133` desktop-right `:max="4"` over exactly 4 distinct non-admin right panes — the desktop guarantee is absolute. BUT the mobile slot (`App.vue:88` `:max="9"`) cycles left+right panes of every view; enough admin traffic can push About off that LRU, so "instantiated ONCE per session" over-claims on mobile. The kill of the per-view-switch claim stands either way; the residual (a compile-time constant declared in instance scope) survives INFO. |
| **D-1/D-2/D-3 framing** | MAJOR each, with a self-applied framing correction | Facts confirmed MINOR/INFO; BLOCKER framing killed as circular (ratified-but-unexecuted W17–W30 rows) | **Facts CONFIRMED; framing correction ADOPTED; severities re-ranked per row (see roster).** `OPTICAL-BENCH-COMPOSITIONS.md:3` "Implementation: W17–W30"; `PROPORTION-AUDIT.md:6` "not a new gate"; PR-14 recites the exact facts; `VISUAL-CONSTITUTION.md:218` was written FROM this state — so "unchanged to the exact digit" is circular. But the X supersession changes the ledger: D-2/D-3/D-4 now have live X-W5 owners with born-RED gates (C8 measured 13 `right:"` rows; A5 measured h1=0), while **D-1's composition row has NO X owner at all** — the V-era W18 owner died un-cut (rider A-1). An owned scheduled row is not a fresh BLOCKER; an ORPHANED ratified row is worse than the readers' framing dispute. |
| **D-15** | KILLED (deliberate single-pane mobile grammar; WCAG 1.4.10 satisfied by the switcher) | KILLED (same; downgrade to INFO; "defect only after W17-W30 retire the control") | **KILL RATIFIED, jointly.** `App.vue:62-92` X6 single-mount (one WebGL context); `shell.css:122-127` shows the switcher exactly on that band; probe d8-zoom200 `present:false` is the grammar, not a disappearance; VC law 6 mandates the one-column mobile sequence. Residue: X-W5 deletes `PaneSegmentedControl.vue` — its C1 content-parity gate (every region present at 390) is the reachability successor and must land in the same wave as the deletion. No independent MAJOR. |
| **D-9's evidence limbs** | Carried all four rendered rows as proof of the lie | Killed two of four (ranges are PICKER_CHANNELS-true; Key Properties No/No/No true of display-p3) | **Reader 2's kill CONFIRMED from source.** `ColorNutritionLabel.vue:217-230` builds ranges from the compiler-total `PICKER_CHANNELS[resolveColorSpace(space)]` — the space's OWN true ranges; `:57` falls back only for NAMES, and Red/Green/Blue is correct for all five masked RGB-family spaces. The lie survives on `created`/`whitePoint`/`gamut`/`conversions`/`applications`/`industries` — the tracked L3 frame renders RGB's Usage rows under ProPhoto RGB. The defect is fully intact; two of its four cited bullets could never carry it. |
| **C-3 cardinality** | Killed (13 keys, 5 masked — "inflated the lie count by 40%") | Killed (same, with the grep-arithmetic diagnosis: 14 `industries` hits − 1 comment = 13) | **KILL RATIFIED, unanimously + this seat's own census** (`grep -cE` → 13; ictcp :261, jzazbz :287, hex :313 present with full dossiers). |
| **MISS-1 weight** | (not filed) | MAJOR miss — second render-path to the same boundary | **ADOPTED MAJOR.** This seat read `useMarkdownColors.ts` whole: three `throw`s (:33, :37, :41) inside a `computed` consumed at `Markdown.vue:15` `:style="mdColorVars"` — a render-time throw, caught by the same single App boundary. The self-inconsistency is exact: H `"none"` degrades (:52-53) while L/C `"none"` throws (:40-41), eight lines apart. Reachability of the `none` limb stays NEEDS-LIVE; the structure is source-certain. |

---

## Adjudicated defect roster

Every row: id · severity · claim · proof · **terminal disposition**. Line anchors are HEAD `c654824e`+tree; re-resolve before any cure (D-19). "X-W6.f" = X:CSS-1 (`docs/tranches/X/waves/W6.md §X.W6.f`, gates f1–f10); identities cross-booked to the standing `adjudicated/ColorSpaceSelector.md` register are marked ⇄CSS and are ONE identity, never re-booked (anti-rename).

### BLOCKER

- **AB-1 · D-9 / L-1 / C-3(5-space limb) ⇄CSS-L-1** — Five selectable spaces (srgb-linear, display-p3, a98-rgb, prophoto-rgb, rec2020) render RGB's dossier under their own names via the masking `?? colorSpaceInfo.rgb` fallback. PROOF: `ColorNutritionLabel.vue:210-215` read verbatim by this seat; `colorSpaceInfo.ts` = 13 keys (own census), domain = 18 (`DISPLAY_COLOR_SPACE_NAMES` over 17 SpaceIds + hex); `resolveColorSpace` maps only hex→rgb; tracked frame `evidence/L3-…png` renders "ProPhoto RGB … Created: 1931 … RGB→XYZ/Kelvin/HSL/Hex … Digital media, Entertainment, Gaming" (read as an image by this seat). Hybrid precision (adopted from reader 1 + the D-9 kill): ranges stay TRUE (`PICKER_CHANNELS`, `:217-230`) while identity facts lie — one label, two spaces' facts. **DISPOSITION: X-W6.f — f1 (catalog totality) / f2 (masking-fallback grep, 2 hits today incl. this file's `:214`) / f3 (o21 truth oracle). The 5 info rows are AUTHORED, not stubbed.**

### MAJOR

- **AB-2 · D-8 / C-2 / L-2 ⇄CSS-L-1** — 7 of 18 spaces render `<h2>Detailed Guide</h2>` over a void; the designed 'Oh snap…' empty state (`Markdown.vue:19-31`) is unreachable because its only consumer gates the mount (`AboutPane.vue:51` `v-if`) while the heading sits outside it (`:49`); the private 11-member `MarkdownSpace` union + `as MarkdownSpace` cast (`:79`, `:96-98`) is the only thing that compiles under `noUncheckedIndexedAccess`. PROOF: this seat's full read of both files; probe d3 arms (guideKids 0 / scrollH 1250 vs 8023); the L3 frame's terminal dead heading. **DISPOSITION: X-W6.f — f1/f3; `doc: null` becomes an authored explicit empty state, never a silent v-if.**
- **AB-3 · L-3 ⇄CSS-L-1(structure)** — One concept enumerated at four cardinalities (18/17/13/11); the two tables About owns are the two with no compiler totality; `colorSpaceInfo.ts:14-15`'s own header asserts the invariant it breaks ("Keys cover the `DisplayColorSpace` union" — covers 13 of 18, read verbatim by this seat). **DISPOSITION: X-W6.f — f1 + structure-over-gates (`satisfies Record<DisplayColorSpace, SpaceEntry>`); the cure mechanism already runs one module away (`picker-color.ts:70`).**
- **AB-4 · C-1 (severity re-ranked)** — One failed doc-chunk fetch replaces the entire two-pane grid: `Markdown.vue:58-62` unguarded `await module()` from async `onMounted`; nearest boundary `App.vue:50…:140` wraps both panes; plus the stuck `isLoading` (never cleared on rejection). The boundary itself is excellent (see superlatives) — the defect is its SCOPE plus the missing local catch that would route to the shipped `v-else` Alert. **DISPOSITION: NO-WAVE-OWNER** — no X row owns pane-granular error boundaries or this load path; books at the formation boundary as one row with AB-5 (the boundary-scope family).
- **AB-5 · MISS-1 (reader-2 miss, ADOPTED)** — `useMarkdownColors.ts` throws ×3 from a render-bound computed — a SECOND independent path to the same whole-grid boundary, with a self-inconsistent posture (H-none degrades :52-53; L/C-none throws :40-41) — and all three axes read this file without seeing it. PROOF: this seat's whole-file read; `Channel = number | "none"` + the valueDomain none-passes-through law make the limb structural, not fanciful. Live reachability NEEDS-LIVE (residue 3). **DISPOSITION: NO-WAVE-OWNER, same row as AB-4.**
- **AB-6 · C-4** — The test surface is vacuous: zero unit tests touch the subtree (grep, re-confirmed); all three e2e touchpoints verified by this seat (`o10-type-locks.spec.ts:66-67` type-only; `mobile/walk.spec.ts:103/:113` asserts exactly the heading that survives the AB-2 void; `o18:70` pins `?space=lab`). The reduce-map-to-`{lab}` green-keeping mutation holds. This is the structural explanation for how AB-1/AB-2 shipped. **DISPOSITION: X-W6.f (o21–o24 oracles are the cure) + the X-W5 walk/page-load carves; primary X-W6.f.**
- **AB-7 · D-6 (RESCOPED — see ruled disagreements)** — The sticky veil cannot occlude by construction (partial-alpha fill, opacity scrub animates the wrong quantity) and the transmission is rendered in tracked evidence; the fade limb and the contain-mechanism limb are DEAD (K-9, K-10). **DISPOSITION: X-W10.d** (owns `PaneHeader.vue` + the condense family) **with an arbiter rider**: G-5's re-derivation must state the occlusion quantity — either the veil endpoint occludes or the band condenses; the two cures interact. NEEDS-LIVE residue 1 for real-engine blur fidelity.
- **AB-8 · MQ-1 carrier (reader-1 miss, ADOPTED; missed by all three axes)** — About keeps its full spatial title scrub under `prefers-reduced-motion: reduce`: scroll-driven animations ignore the duration clamps both PRM guards apply; measured byte-identical at `motion-quarantine.md §4 MQ-1` (guard fired `1e-05s`, transform unchanged), About named in the nine-pane blast radius; `AboutPane.vue:4` carries the class (this seat's read). challenge-D:620-621 ADOPTED the producer's own exemption comment (`PaneHeader.vue:149-151`) as a finding — recorded as an axis failure. MQ-1 is the quarantine's own NEW defect (carryable; not a FALSE row). **DISPOSITION: X-W10.d (CC-099, gate G-4 — `animation-timeline` moves inside `@media (prefers-reduced-motion: no-preference)`).**
- **AB-9 · D-2 (framing corrected)** — About is Picker's companion by table: `viewSchema.ts:107` `right: "about"` (this seat's read). Not a discovery — an owned row. **DISPOSITION: X-W5.c — gate C8 (physical axis unrepresentable; 13 `right:"` rows born-RED) + `regions[]`.**
- **AB-10 · D-3** — No direct-load identity: 15 routes, no `/about` (this seat's read of `router/index.ts`); visibility = `mobilePaneIndex`/desktop slot. The "structurally unreachable after PaneSegmentedControl dies" rider is a forecast, not a shipped defect — but X-W5 DOES delete that control, so the successor reachability contract (C1 parity: every region present at 390) must land in the same wave. **DISPOSITION: X-W5 (regions[] + C1), with a carry-note: the V-canon first-class `/about` destination (OBC:31, ARCHITECTURE.md) appears in NO ledger row and no X wave by name — rider A-1 hands it to the boundary.**
- **AB-11 · D-4** — The default scene is 49.1/49.1, not any ratified posture: `shell.css:133-136` "Equal columns always (the kept 1fr↔1fr invariant)" read verbatim; probe rects 512/512 corroborate. **DISPOSITION: X-W5 — the dual grid and the companion posture die with regions[]; the exact-posture law is CC-052's (P-1, apply-never-requeue).**
- **AB-12 · D-10 / C-8** — Zero H1 on any route (`grep -rn '<h1' demo/` → 0, re-confirmed); an `h3` pane title (`PaneHeader.vue:21`, live anchor — C-8's `:26` is the comment, K-13) parents six `h2`s at three sizes; section headings speak Fraunces where OBC:104 assigns Plus Jakarta Sans. **DISPOSITION: X-W5.a — gate A5 (fourteen-ViewId H1 walk, born-RED at 0) for the H1/outline half; the family/register matrix half rides X-W10 (demo/DESIGN.md canon) as a rider.**
- **AB-13 · D-11** — The ratified 66ch About measure does not exist anywhere in the demo (`grep 66ch|max-inline-size` → 0, re-confirmed); `Markdown.vue:98-102` declares the opposite (`max-w-full`); current 49.4ch is an accident of the companion column and D-2's cure makes it worse — the compounding is why this stays MAJOR. **DISPOSITION: NO-WAVE-OWNER — PR-35/VC:78/:218 were never cut into X; books at the boundary inside the About-recomposition bundle (AB-17).**
- **AB-14 · D-12** — Dark-mode: the inline title control is optically identical to its sentence — **this seat's own recompute: 1.093:1 raw, ≈1.19:1 composited** (light 2.31) from probe-D1's computed inks; the source half is definitive (ghost trigger, `--space-title-ink` at 86% alpha, underline transparent until hover, caret-only rest affordance — reader 2's source read adopted). Hue-only affordance violates VC:82-83. **DISPOSITION: NO-WAVE-OWNER** — the trigger register is GLASS-OWNED at the standing CSS-L-10/BANK-1 relay (title/display SelectTrigger variant), but the color-only-affordance half is consumer-side and unowned; boundary row, BANK-1 adjacency noted. Live material-tier sample = residue 4.
- **AB-15 · hide-indicator / P051 (reader-1 miss, ADOPTED)** — `ColorSpaceSelector.vue:64` ships `hide-indicator` (this seat's read); selection is carried by dot idle-opacity alone (`:287-289` `opacity: 0.35`); `OPTICAL-BENCH-COMPOSITIONS.md:108` P051 names this component and this prop: "ColorSpaceSelector deletes `hide-indicator` and consumes the sole `SelectItem` indicator/gutter… nonzero monochrome/forced-colors selected delta." No axis and neither of the 15 CSS-corpus rows utters it. About is one of the two hosts; the seat is ColorSpaceSelector's. **DISPOSITION: X-W4.g — CC-113 (A-18 indicator paint), BLOCKED-ON the CC-003 four-condition glass-8/TR#84 census trigger; the P051 indicator-consumption clause rides that atomic cut.**
- **AB-16 · specimen float noise (reader-1 miss as TEXT; ⇄CSS-L-2 as identity)** — The 13 CSS-serializable rows print unrounded float noise truncated mid-token (`hsl(346.045357898758d…`) — rendered in this folder's own `evidence/L1-…png`, whose FILENAME asserts it while no axis text states it. Source re-confirmed by this seat: `specimenFor` rounds ONLY the non-CSS branch (`:161-163`), CSS branch passes `serializePickerColor` raw into a `truncate max-w-[16rem]` caption. ONE identity with CSS-L-2 (byte-pinned there) — cross-booked, not re-booked. **DISPOSITION: X-W6.f — f4 (caption fits box; 16/18 overflow born-RED) + f5 (one grammar per row).**
- **AB-17 · D-1 (facts; framing corrected)** — Card as page primitive (`AboutPane.vue:2-5`), static Alert as definition chip (`ColorNutritionLabel.vue:8-13`), 7 `<Separator/>` components (+4 markdown `<hr>` = 11 rendered rules on documented spaces — rider A-3), 4 Tooltip shells with LITERALLY EMPTY `<TooltipContent>` (`:139-140`) around a non-focusable `cursor-pointer` div. Every element the ratified About composition deletes (PR-14, VC:218). The a11y half was saved upstream (producer `announce:"off"` + `decorative` self-degrade — see superlatives); the composition half is fully live. **DISPOSITION: NO-WAVE-OWNER — the About recomposition (V-era owner W18) was never cut into X; THE flagship carry-cut gap (rider A-1). Bundle carries AB-13, AB-28, AB-33.**

### MINOR

- **AB-18 · D-7 (band limb only; hierarchy limb KILLED, K-8)** — The sticky band never contracts as a block: keyframes animate exactly opacity/transform (this seat's read of `:196-223`; the layout fork deliberately retired at `:153-157`); VC law 5 requires title+padding+band. **DISPOSITION: X-W10.d — CC-101, gate G-5 (its RED baseline already names these keyframes).**
- **AB-19 · C-6** — The `:key` remount (`AboutPane.vue:52`) is load-bearing: `Markdown.vue` has no `watch` on `module` (whole-file read); deleting the key freezes the guide silently; today every space change pays a full remount + TreeWalker + scroll-position loss in an 8023px scroller. **DISPOSITION: X-W6.f rider — the wave edits this exact call site (markdownModules → catalog docs); keep-`:key` vs add-watch must be DECIDED there, not drifted.**
- **AB-20 · C-7** — The async guide swap is never announced (zero `aria-live`/`role="status"` in the subtree, re-confirmed). X-W5's shell-owned polite region is a route-settlement node and **may not be cited as this cure** (AdminListSkeleton A-2 precedent, applied verbatim). **DISPOSITION: NO-WAVE-OWNER — joins the house-wide completion/`aria-busy` contract row at the boundary.**
- **AB-21 · C-5 / L-10 / D-17 ⇄CSS-L-12** — `(colorSpace: any)` whole-model spread against a `Partial<ColorModel>` patch setter (`AboutPane.vue:25` → `usePaneRouter.ts:149` → `useColorPipeline` `updateModel`, chain re-read); the emit is already typed `[value: string]`, so the `any` widens a typed surface. Reader 2's correction adopted: the sibling host's handler is equally `any`-typed (`ColorPicker.vue:43`) — only its write path is correct. The clobber limb stays an honestly-labelled hypothesis. **DISPOSITION: X-W6.f — f7 (boundary is the domain type; 3 `colorSpace: any` hits born-RED, lint inadmissible).**
- **AB-22 · C-11** — The pane scroll/overflow recipe is a 9-host per-instance duplicate that disagrees with itself (4 `mx-auto` / 4 not / ConfigSliderPane divergent — this seat's census), while `.pane-scroll-fade`'s real producer block sits unscoped in PaneHeader. **DISPOSITION: NO-WAVE-OWNER (hygiene; no X row owns the pane-chrome recipe).**
- **AB-23 · L-4 ⇄CSS-L-4 / CC-078** — `demo/ui/` = 19 pure-aliasing barrels (own census); AboutPane crosses twice (`:63-64`), its closure five more times. Component-scoped observation of a repo-scoped identity. **DISPOSITION: X-W8 (CC-078 BUILD; the 19-barrel deletion + 92-import repoint are its own gates).**
- **AB-24 · L-5 + MISS-7 ⇄CSS-L-14** — `tsconfig.demo.json:42-49` hand-declares 3 unpublished keys pointing at absent files, omits 2 real ones, and its comment block is false TWICE (the "CLOSED 8-key set" — 7; and the bare-specifier/glass-dist claim — glass dist imports subpaths only, and the exports map has no `.` so the "mirrored" alias cannot even be generated — both re-verified by this seat). **DISPOSITION: NO-WAVE-OWNER — X-W8:112 disclaims it to "L-22 ≡ W-HYGIENE, another owner," and W-HYGIENE exists in NO ledger row and no X wave (rider A-1). The stale-tarball risk model is K-16.**
- **AB-25 · L-6** — `Markdown.vue:37-38` runtime-imports the shell's global stylesheets into the lazy About chunk, duplicating the App-root pair, beside its own correct `@reference` (:79). **DISPOSITION: X-W8 rider — `Markdown.vue` is in W8's modify-carve bounds; the two lines die in the carve (flagged, no W8 gate names them).**
- **AB-26 · L-7 + C-9(structural) ⇄CSS-L-7/L-13** — About is the last prop-drilled pane; its two state routes carry different values (alpha-bearing prop `App.vue:340` vs alpha-stripped injection `:271` — re-read); and it rides the raw `cssColor` while `cssColorOpaqueFrame` exists. The perf half stays declined (its own author's A/B). **DISPOSITION: NO-WAVE-OWNER — the standing CSS apotheosis FOLDed this family to "MT-CSS-2," which exists in NO ledger row and no X wave (rider A-1); identity preserved under its original ids.**
- **AB-27 · L-9** — About's 11 guide SFCs live at repo-root `assets/docs/` and every one imports back into `demo/scenes/about/katex` (head of `lab.md` re-read) — content outside the app, cyclic by construction, `.md`-suffixed SFCs. **DISPOSITION: NO-WAVE-OWNER — X-W8's SCC row (CC-079) names the Markdown TYPE cycle only; `assets/` is outside W8's bounds. Boundary row; the X-W6.f `doc:` re-plumb is the natural landing and should claim it at wave-open.**
- **AB-28 · D-16** — Two ColorSpaceSelector instances own the same selection on one screen (both hosts re-read; visible in both tracked evidence frames). **DISPOSITION: NO-WAVE-OWNER — About-recomposition bundle (AB-17).**
- **AB-29 · D-5** — ~44.6px dual-edge misalignment; mechanism source-read (right wrapper `display:block` + `h-full` stretch vs left `flex-col justify-center`); probe Δtop 44.5/Δbottom 44.77 both schemes. Derived symptom, MINOR. **DISPOSITION: X-W6.g — CC-062, gate g2 (Picker↔About top edges ≤1px with Mix as the control), already born-RED with committed witness hashes.**
- **AB-30 · C-14 + the landmark/name miss (reader-1 miss, ADOPTED)** — A 10.4-viewport (14.5 at 390px) scroll document is an unnamed `div` with no role, no tabindex, inside a `<main>` that belongs to both panes; the only header-resident focusable is a Select trigger that owns the scroll keys by construction. Per-key measurements stay live-only (residue). C printed `tabindex/role/ariaLabel = null` and did not draw the finding — recorded. **DISPOSITION: NO-WAVE-OWNER — a11y bundle at the boundary; X-W5's regions[] (role-named wrappers) is adjacent but does not name the scroller contract.**
- **AB-31 · px-3 vs px-4 (reader-1 miss, ADOPTED)** — About is the only pane in the nine whose body inline padding (`px-3 sm:px-6`, `AboutPane.vue:42/:48`) disagrees with the shared header (`px-4 sm:px-6`, `PaneHeader.vue:11`) — a 4px ledger break down the whole document below `sm` (spot-verified against Gradient/Mix). **DISPOSITION: X-W6.g rider (About rhythm is .g's surface).**
- **AB-32 · φ-ladder shadow (reader-1 miss, ADOPTED)** — The demo maintains a private `--phi-0..4` ladder (`foundation.css:458-462`, re-read) shadowing the producer's shipped `--space-phi-5/6` (re-read in glass dist), and About's section interval `pt-phi-3` sits one rung below the ratified `--space-phi-5` (OBC PR-35). Token-level analogue of L-4's component aliasing. **DISPOSITION: X-W10.f (CC-102 producer-contract citation) with the CC-112 φ-canon bank adjacency; may not enact GOLDEN-GLASS naming (OP-3).**
- **AB-33 · hover-only affordance + MISS-4 (both readers' misses, MERGED)** — The pane's single dynamic affordance is pointer-hover-only (`:109-113` — no tabindex/focus/click/touch arm), the `cursor-pointer` promises nothing, `TooltipProvider` is instantiated per-item inside the `v-for` (defeating its own grouping contract), and the provided content is empty — dead code by inspection. **DISPOSITION: NO-WAVE-OWNER — About-recomposition bundle (AB-17); the empty `<TooltipContent>` pair is deletable at ANY touch of the file and should not wait for the bundle.**
- **AB-34 · MISS-2 (reader-2 miss, ADOPTED)** — The widest type hole is the barrel: `DocModule = () => Promise<{ default: any }>` (`markdown/index.ts:3`, re-read) erases the whole render path invisibly; honest closure `any`/cast census ≥7 (`:25`, `:97`, index.ts:3, CNL `:111/:123/:160/:213` — all re-read). **DISPOSITION: X-W8 (the DocModule/DocItem types-module carve at W8:174) with an X-W6.f rider — the catalog's `doc:` field must consume a retyped module, `{ default: Component }`, or the carve moves an `any`.**
- **AB-35 · MISS-3 (reader-2 miss, ADOPTED)** — The shared cure has a second consumer nobody costed: `ConsoleRail.vue:174` `(colorSpaceInfo as any)[space]` — which degrades HONESTLY (falls back to the raw key, re-read) and is the exemplar the About site should have copied. **DISPOSITION: X-W6.f — f2's grep counts this exact line (2 hits today); the cost is already in the wave.**
- **AB-36 · D-13 (CONVERTED from unproven by this seat)** — RTL: the identity line shatters ("About the color / Lab ⌄ ,spaces"), ranges garble ("to 125 125-", "to 100% 0%"), sentence periods lead their lines, the picker echoes ",%92.0" — ALL READ DIRECTLY off tracked `frames/d9-rtl.png` by this seat (neither reader opened it). Source half was already certain: a sentence-composed title with a control inside and zero `unicode-bidi`/`dir` isolation repo-wide. **DISPOSITION: NO-WAVE-OWNER — i18n/bidi isolation names no X owner; a11y bundle at the boundary.**

### INFO

- **AB-37 · L-8** — `sourceExportPlugin` + `@src` alias have zero consumers; kept alive by a comment about this component's assets (census re-run: only the plugin and the comment). **DISPOSITION: NO-WAVE-OWNER (hygiene; vite.config outside W8 bounds).**
- **AB-38 · MISS-5** — `DocItem` is a dead export, 1 declaration 0 consumers (own grep). **DISPOSITION: X-W8 — dies in the types-module carve.**
- **AB-39 · MISS-6** — Barrel↔component type cycle (`index.ts:1` exports Markdown; `Markdown.vue:40` imports `DocModule` from `"./"` — re-read). **DISPOSITION: X-W8 — CC-079's Markdown SCC row, explicitly owned (W8:289).**
- **AB-40 · C-10 residual + C-12/D-17 hygiene** — Compile-time module table in instance scope; discarded `defineProps` return against the subtree's own destructure idiom; mixed `:model-value`/`:cssColor` casing; inert `mx-auto`. **DISPOSITION: X-W6.f rider (the wave rewrites this script block anyway).**
- **AB-41 · C-13 (hypothesis, kept as labelled)** — `replaceChild` on Vue-owned text nodes; dormant — reader 2's dormancy derivation ADOPTED (the md component takes no props/slots, `shouldUpdateComponent` short-circuits, `markdownContent` changes once per instance). Correctly labelled by its author; no disposition, recorded.
- **AB-42 · C-9 perf half** — declined by its own author after an honest A/B (70ms vs 82ms); the decline is KEPT. Structural half lives in AB-26.

---

## Killed-claims register

FALSE rows may not be carried; motion-quarantine ids cited where the error class is pre-ruled.

- **K-1 · C-3's cardinality** ("10 keys… 7 silently render RGB's dossier", challenge-C:165-167) — killed by enumeration: **13** keys (ictcp :261, jzazbz :287, hex :313 have full dossiers; this seat's own grep census); masked set = **5**. The grep arithmetic error (14 `industries` hits − 1 header comment) diagnosed by reader 2, ratified.
- **K-2 · D-9's evidence limbs** — two of four cited bullets are TRUE of Display P3: ranges come from compiler-total `PICKER_CHANNELS` (`:217-230`), not the fallback; and No/No/No Key Properties are correct answers. The lie stands on identity facts only (see AB-1).
- **K-3 · C-1's "destroys the entire application"** — the boundary is announced (`role=alert`/assertive), focus-managed, and one-click recoverable (`ErrorBoundary.vue` whole-file read). The GRID dies, recoverably; the scope defect survives at MAJOR (AB-4).
- **K-4 · C-10 as filed** ("re-created on every view switch") — `PaneSlot.vue:120` KeepAlive; desktop-right `:max="4"` over 4 distinct panes = never evicted; `defineAsyncComponent` governs chunk load, not instance lifetime. Arbiter precision in rider A-2 (mobile 9-LRU CAN evict — the kill stands against the claim as filed, not as an absolute).
- **K-5 · D-14's PRM limb** ("`transition: color…` time-based and ungated") — the **M-03/M-17/M-29 error class**, pre-ruled FALSE three times: `animations.css:184-193` + glass `a11y-overrides.css` govern centrally; under `reduce` the measured cascade preserves colour legs BY DESIGN (non-vestibular). Cited line numbers also drift (live `:125`, `:110`). May not be carried.
- **K-6 · D-14's forced-colors limb** ("zero forced-colors blocks… 0.22-alpha separators untreated") — the grep is true, the conclusion false: `foundation.css:665+` U-F57 two-tier policy (re-read); chrome left at UA `auto` ON PURPOSE — the UA's system-color substitution is the cure, not the disease. Same scope-shopping class as M-03/M-29.
- **K-7 · D-15** ("About disappears entirely at 200% zoom", MAJOR) — the deliberate single-pane mobile grammar, canon-MANDATED (VC law 6); the switcher exists exactly there (`shell.css:122-127`); About shown at 390×844 in the same corpus's own runs. Residue: the reachability successor must co-land with X-W5's `PaneSegmentedControl` deletion (C1 parity gate).
- **K-8 · D-7's hierarchy-inversion limb** ("scrubbed title ~15px, smallest type on the pane") — killed by the file's own closed form: `--pane-title-shrink-ratio` = heading/display-1 = 0.618; stuck title = 41.888 × 0.618 = **25.888px** > 20.352 (section h2) > 16 (body). The "~15px" has no derivation and no support in the tracked evidence.
- **K-9 · D-6's fade limb** ("the mask fades content the header does not cover") — the **M-33 error class** verbatim: `.pane-scroll-fade` sets `contain` + `scroll-timeline` only; the only mask in reach feathers the VEIL pseudo's own 14px overhang (`:91-96`), not body ink. The wash visible in the L3 frame is veil TRANSMISSION (AB-7), not a content mask. May not be carried.
- **K-10 · D-6's contain-mechanism limb** ("paint containment makes the backdrop the contained subtree, defeating the blur") — the inference runs backwards: containment scopes the backdrop root to the pane whose content the header caps — that content is exactly what the filter samples. The alpha half survives into AB-7; this mechanism does not.
- **K-11 · D-1's divider arithmetic** ("measured 7… at its pre-remediation value to the exact digit") — this folder's own probe measures **11** dividers inside `.about-card` on every documented-space run (7 only on the empty-guide arms) — this seat re-dumped the JSON. Seven `<Separator/>` components + four markdown `<hr>` = the honest census (rider A-3); the "exact digit" rhetoric is a species-count quoted against a canon line written from this component's state — circular twice over.
- **K-12 · D-1/D-2/D-3 as BLOCKERs** ("ratified, costed, never applied" as discovery) — OBC's own header says Implementation W17–W30; PR-14 recites the facts; VC:218 was written FROM this state. Ratified-but-unexecuted owned rows are not fresh blockers (AdminListSkeleton precedent). What IS new: D-1's owner was never cut into X — see rider A-1.
- **K-13 · C-8's anchor** — `PaneHeader.vue:26` is inside the AB-2 comment; the `<h3>` is at `:21` (this seat's read). Finding correct (lives in AB-12); the anchor matters because the cure is a producer-level edit to that line.
- **K-14 · L's provenance drift** — the §1 import table is off by four lines against the live file (imports run `:62-71`, this seat's read; L cites `:66-75`), internally inconsistent with L's own correct `:79`/`:97` anchors; same drift in N-5 and L-10. Findings sound; provenance not re-checkable as written.
- **K-15 · C-9's re-entry cost mechanism** ("`onUpdated` re-enters `applyHighlighting`" as a per-drag cost) — `useMarkdownHighlighting.ts:15` short-circuits on `mark.cs-name` before regex or TreeWalker (this seat's read): after the first pass, each `onUpdated` costs one `querySelector`. The full walk runs once per `:key` remount — which is AB-19's finding, not C-9's.
- **K-16 · L-5's stale-tarball risk model** — glass-ui 7.0.0's dist imports ONLY subpaths (`/color`×7 `/css`×4 `/easing`×2 — this seat's grep), all covered by the exports-generated self-alias; the bare-specifier alias L assumes cannot be generated (`exports` has no `.` key). The staleness fact and the dead `paths` rows survive in AB-24; the served-stale-copy risk does not.

---

## Superlatives (provenance verified by this seat)

1. **PRODUCER SAVE, confirmed at the compiled artifact** — glass `Alert` ships `announce: { default: "off" }` (found verbatim in `dist/glass-ui.js`; `AlertAnnouncement = "off"|"polite"|"assertive"` in the d.ts) and `Separator` self-degrades to decorative — so About's two worst composition misuses are silent to AT instead of seven `role="separator"` nodes plus an assertive region firing per space-change. The visual misuse is AB-17; the a11y damage was designed out at the root. No axis noticed; reader 1 did.
2. **A PROBE THAT OVERTURNS ITS OWN REPORT** — tracked, re-runnable `probe-D1.mjs`: its JSON killed D-1's "7 dividers" (11), killed D-7's hierarchy limb (the `tokens` block carries the closed form's inputs), corroborated h1=0 ×10 runs, the 512/512 split, the 44.5px overhang, and the inks this seat recomputed to 1.093:1. The strongest artifact in the folder.
3. **NEGATIVE PROOFS WITH EVIDENCE** — challenge-L §N-2 (re-read): nearly filed a KaTeX overflow defect off a REPORT.json row, measured `.katex-mathml` at `clip-path: inset(50%)` + a live 316px scroll range, WITHDREW it, and filed an instrument fix against `capture.mjs`'s `vis()` instead. challenge-C `:274` "The clobber is a HYPOTHESIS", `:388` "NOT SUBSTANTIATED and I decline to make it", `:482` a fourteen-row absent-hazard table. The standard the corpus should be held to.
4. **THE CURE ALREADY RUNS ONE MODULE AWAY** — `picker-color.ts:52-70/:72-90`: `satisfies Record<SpaceId,…>` totality is why THAT table is complete while About's two tables drifted. X-W6.f's whole mechanism is a copy, not an invention — the cheapest structural fix in the register, and it makes AB-1/AB-2/AB-3 unrepresentable rather than merely fixed.
5. **FORENSIC COMMENTS THAT NAME THEIR OWN FALSIFICATION** — `Markdown.vue:291-306` (`:has(> .katex-display)` with the dead-selector post-mortem beside it); `useMarkdownHighlighting.ts:31-38` (`lastIndex = 0` on BOTH sides of a `/g/` predicate, the textbook cure for one of JS's most-missed bugs, with the finding id that produced it); `Katex.vue`'s scoped-`@import` post-mortem; `ColorNutritionLabel.vue:190-208`'s fg/bg double-duty split; `valueDomain.ts`'s encoded kelvin exception. This subtree documents its scars correctly.
6. **`ErrorBoundary.vue` itself** — role/aria-live/tabindex/focus-on-catch/real-Button/reset, WCAG numbers cited in the header. AB-4 is a scope defect against an excellent instrument.
7. **THE COMPONENT'S LOCAL HYGIENE** — 99 lines, no rAF loop, no listener, no observer, no timer, correct `import type` under `verbatimModuleSyntax`. Every surviving defect is composition, typing, transport, or canon conformance — challenge-D says so unprompted while filing a BLOCKER against the same file.

---

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs — task #4 feed)

1. **D-6's rendered double-exposure in real engines** (AB-7 rider): re-shoot `.about-card` at scrollTop 120/420/900, real Chrome AND real Safari, light+dark. The alpha arithmetic is proven; the tracked L3 frame renders transmission in a headless cell; whether `backdrop-filter` smear rescues legibility in real engines is the open pixel question. Commit frames into `evidence/` (a witness not committed does not exist).
2. **The d3-jzazbz deep-link anomaly** (this folder's own tracked probe): `?space=jzazbz` did NOT take effect (measured the OKLCh doc, guideKids 1, scrollH 8024 — re-dumped by this seat) while `display-p3`/`rec2020` applied cleanly. Either deep-linking the two non-CSS-serializable spaces silently falls back (a REAL unreported defect — `serializePickerColor` re-serializes them AS `oklch(…)` for the URL; note CC-081's ICtCp `cp`→`Ct` mis-resolve rides X-W9) or the evidence row is invalid. Re-run with a URL-echo assertion. AB-2 is unaffected (stands on source + two clean arms).
3. **MISS-1's `none`-limb reachability** (AB-5): drive a powerless/`none`-bearing color through a deep link and watch for the whole-grid boundary. The throw sites are source-certain either way.
4. **D-12's material-tier sample** (AB-14): this seat's recompute (raw 1.093 dark) is ink-vs-ink plus approximate compositing; VC:82 demands rendered contrast on the actual material tier — one live pixel pass, both schemes.
5. **Live-only measurements kept as labelled by their authors**: C-14's per-key scroll census; C-9's 285 style patches (perf half declined); D-14's cold-chunk blank frame (shape mismatch source-confirmed; the blank stays hypothesis).

*(D-13's rendered limb is NO LONGER residue — converted to AB-36 by this seat from tracked `frames/d9-rtl.png`.)*

---

## Arbiter riders (recorded here because this file is the only write)

- **A-1 · THE CARRY-CUT GAP, EXTENDED** — Three fold-targets that standing adjudications name as owners exist in NO CARRY-CUT-LEDGER row and no X wave: **(a)** the About recomposition's V-era owner (OBC W18 / PR-14 — AB-13/17/28/33 land NO-WAVE-OWNER because of it); **(b)** **MT-CSS-2** (the ColorSpaceSelector apotheosis FOLDed its L-7/L-13 there — AB-26); **(c)** **W-HYGIENE** (X-W8:112 disclaims tsconfig parity to it by name — AB-24). Same class as AdminListSkeleton's MT-AU1 finding, now three names wide. The next formation boundary consumes this list with the original ids.
- **A-2 · C-10 precision** — the desktop-right KeepAlive guarantee is absolute (4 distinct / max 4); the mobile slot is a 9-LRU over a larger pane population and CAN evict About under admin traffic. The kill stands against the claim as filed; "once per session" must not be quoted as absolute.
- **A-3 · Divider species** — 7 `<Separator/>` components (AboutPane ×2, ColorNutritionLabel ×5) + 4 markdown `<hr>` = 11 rendered rules on documented spaces (probe-corroborated). Any recomposition census must name which species it counts, or K-11 recurs.
- **A-4 · D-15's kill and AB-10's cure are one contract** — the single-column mobile grammar (VC law 6) is the reason zoom-200 "disappearance" is not a defect, and X-W5's C1 parity gate is the reason it stays not-a-defect after `PaneSegmentedControl` dies. Neither half may be re-litigated alone.
- **A-5 · Axis-failure record** — challenge-D adopted the producer's own PRM-exemption comment (`PaneHeader.vue:149-151`) as a finding and thereby cleared the one motion defect live on this surface (MQ-1); both readers carried motion-quarantine discipline correctly otherwise. The citation-inheritance lesson from the CSS apotheosis generalizes: a producer comment is a claim, not a measurement.

## DISSENT — preserved, not ruled away

1. **Severity ordering (unresolved, preserved)**: reader 1 ranks the D-6 family (veil legibility) above D-11 (measure); reader 2 ranks the catalog family strictly first and D-11 MINOR. This seat ranked D-11 MAJOR on the compounding argument (D-2's cure worsens it); if the About recomposition lands as one wave the ordering is moot.
2. **Reader 2's C-13 dormancy derivation** is adopted but is itself unfalsified-by-measurement (no forced re-patch was attempted — correctly, under the no-source-edit law). If any future wave adds props/slots to the rendered md component, AB-41 revives with a crash witness.
3. **Both readers' honest self-limits recorded**: reader 1 did not open `frames/d9-rtl.png` and said so; reader 2 declared every rect/contrast/keyboard figure NEEDS-LIVE rather than adopting probe numbers as its own. Both disclosures are the reason their returns could be adjudicated at this speed.

---

**CLOSING VERDICT — SOURCE-RED AFFIRMED, SEVERITY RE-RANKED, OWNERSHIP SPLIT THREE WAYS.** The catalog lie is the sole BLOCKER and is already owned born-RED at **X-W6.f** (one identity with CSS-L-1, cross-booked); the shell/canon family (companion topology, route identity, H1, proportion) is owned at **X-W5**; the header family (PRM scroll-scrub, non-condensing band, non-occluding veil) at **X-W10.d**; the alignment and rhythm rows at **X-W6.g**; the barrel/types family at **X-W8**; the indicator row banks at **X-W4.g/CC-113**. Both loudest BLOCKER framings die (C-1's blast radius, D-1's "exact digit" circularity), eight further limbs die with them (two on pre-ruled motion-quarantine classes M-03/M-17/M-29/M-33), and fourteen reader misses are adopted — the sharpest being MQ-1 (missed by all three axes, cleared by a producer comment) and the render-bound throws in `useMarkdownColors`. The gravest structural result is not in the component: the About recomposition, MT-CSS-2, and W-HYGIENE are **phantom owners** — adjudicated identities folded to waves that were never cut into X (rider A-1). Dispositions here are ADJUDICATED, not VERIFIED; nothing in this record stamps a wave. Original challenge records, probe, and frames remain immutable beside this file per E-1/E-3.
