# wb-mix-animationcanvas — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u` · **Corpus**: `demo/workbenches/mix/MixAnimationCanvas/` (MixAnimationCanvas.vue 35 · useMixingAnimation.ts 190 · mixStage.ts 282) plus the contract surfaces it rides (MixPane.vue · MixResultDisplay.vue · MixSourceSelector.vue · MixConfigBar.vue · composables/useMixingState.ts · color-session adapters · ErrorBoundary.vue · demo/palettes/mix.ts). Challenge axes on disk: **challenge-C-implementation.md** (EXISTS-ORIGINAL) · **challenge-D-design.md** (EXISTS-ORIGINAL, committed frames `mix-frozen-t715.png` / `canvas-only-t880.png`) · **challenge-L-library.md** (UNWITNESSED-DIRECT, HYDRATION-LEDGER.md:305; ledger definition :9-11 re-read — every L fact re-proven before carriage). The charge names the governing round "r2"; exactly one challenge round per axis exists in the directory — that round, plus the two current re-reader returns, is what this file adjudicates.
**Re-readers adjudicated**: TWO hostile returns (design-and-usage = "reader-DU" · library-and-code = "reader-LC"): reader-DU = 26 confirmed · 7 killed · 8 missed; reader-LC = 22 confirmed · 7 killed · 8 missed. Both were attacked before adoption; every kill re-derived; every promoted miss re-walked in source; one reader miss **killed by this seat** (K-13); the one direct reader-vs-reader contradiction (arrival seam) ruled with this seat's own arithmetic; one prior-round finding BOTH current readers dropped (the deactivation smear) re-verified and re-carried, escalating a row both readers under-graded.
**SUPERSESSION**: the 2026-08-10 file at this path adjudicated an EARLIER reader round (13/5/7 · 25/8/5) at HEAD `54a97489`. This file supersedes it for the CURRENT round at `9c779130`. Its receipts were not trusted: every carried row (MX-18/19/29/31/33/37, the X-wave bounds, P051, the K-register) was re-verified this session with this seat's own instruments; `git diff --stat c654824e HEAD -- demo/workbenches/mix/` is empty (re-run), so all subject anchors re-resolve unchanged. MX ids are kept stable; new admissions take MX-38..MX-45.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling)**: whole-file reads of mixStage.ts, useMixingAnimation.ts, MixAnimationCanvas.vue, useMixingState.ts, MixPane.vue, MixResultDisplay.vue, ErrorBoundary.vue (template + script) + MixSourceSelector.vue:120-270 + MixConfigBar.vue:150-175 + App.vue mount census (:47/:50/:58/:140); installed glass-ui 7.0.0 probed by node — `dist/watercolor-dot.js` (**`inheritAttrs:!1` true; `$attrs`-spread false; slot-render false; `aria-hidden`+`pointerEvents` hardcoded; no `tag` prop**), `dist/canvas.js` (**exports `useCanvas2D`/`useCanvasLifecycle`/`resolveCanvasColor`**) + full `useCanvas2D.d.ts` contract read, `dist/motion-core.js` (**`useReducedMotion` + `useIntersectionPause` + `readReducedMotion` shipped**), `dist/styles/tokens/scheme-motion.css` (**full ladder incl. `--duration-xl: 1s`, `--duration-xxl: 1.5s`; `--z-controls: 20`; `--motion-weight: 0.618`**); demo tokens: foundation.css `--z-ornament: 20` (~:393) + `seat.css:55` consumer + ColorPicker.vue:85 doc; forced-colors roster (foundation.css:653-685, bare `canvas,` heads tier-1); `.vj-morph-leave-active` all legs `--duration-fast` (animations.css:111-117); e2e/smoke/views/mix.spec.ts:39-52 + safari/mix-flow.spec.ts:29-40 (role-named add-slot + `[data-mix-target]` visible — both re-read); `.github/workflows/` = ci.yml + deploy-pages.yml + release.yml, `grep -rniE 'playwright|e2e'` = **0**; eslint.config.js re-read (**ts-block rules explicitly `off`; :232-239 globs live `demo/color-picker/**` but its ONLY pattern is the dead `@components/...palette-browser` alias shape; :274+ composables object globs only the deleted `demo/@`; `ls demo/@` → No such file**); tsconfig.demo.json:40-51 (8 hand-typed keys incl. dangling `parsing`/`units`; `/css`+`/value` omitted; "CLOSED 8-key set" comment) vs `package.json#exports` (7 keys, no `.`) vs `ls dist/` (**no `dist/value.js`, no `dist/index.d.ts`; subpaths lacks `parsing.d.ts`/`units.d.ts`**) + vite.config.ts:41-50 (aliases GENERATED from exports — no bare-root alias exists); demo/palettes/types.ts (`css: string` required); picker-color.ts:104-113 + color-utils.ts:11-20 throw sites; demo/palettes/mix.ts:110-135 (`resultLength = discard ? minLen : maxLen`); test/mix-v4.test.ts:3-4 (test/→demo inversion); usePaneRouter.ts:215-225 (dock Clear/Mix/Copy over MixPane's `defineExpose`); censuses re-run at HEAD: `mixStage|useMixingAnimation|MixAnimationCanvas` in test/+e2e/ = **0** · `aria-live|role="status"|role="alert"` in the mix tree = **0** · `onDeactivated` in demo/ = **0** · `useCanvas2D|useCanvasLifecycle|glass-ui/canvas` in demo/ = **0** (three `<canvas>` sites) · `data-mix-target` writers = MixResultDisplay.vue:69 alone; `git log -- demo/workbenches/mix/` → last touch = **`f2c8f565` (the glass-ui 7.0.0 adoption)**; lucide `defaultAttributes.mjs` (no `aria-hidden`, no `role`); canon verbatim: OPTICAL-BENCH-COMPOSITIONS.md:43 (P122) + :108 (**P051: "removes its button/tag branch in the clean major … Operable sites use named geometric seats; ornamental faces are aria-hidden"**), VISUAL-CONSTITUTION.md:38/:48/:139/:144/:145/:202; witness matrices listed by this seat: six state matrices (`reduced-motion|forced-colors|zoom-200|rtl-desktop|rtl-mobile|keyboard-focus`) each hold exactly the 5-route set, **no mix.png**; the four safari matrices hold mix.png at rest; **X wave bounds re-read from the wave files themselves**: X-W6.j files = MixPane + MixConfigBar + MixSourceSelector + MixAnimationCanvas.vue + useMixingAnimation.ts (W6.md:76-80, :315; gate j2 = canvas-lifetime, W6.md:321); X.W4.g **trigger-gated, CLOSED unless X-W0.j census PASS** (W4.md:7/:22), holds MixSourceSelector impostor seats `:168`/`:215` + MixResultDisplay "import + face only" (W4.md:161/:165); X-W1 restores `e2e-smoke`/`e2e-safari` HARD (W1.md:42-45, CC-031); X-W7 shares MixSourceSelector/MixConfigBar (W7.md:125-126); W8.md:112 **declines tsconfig parity ("L-22 ≡ W-HYGIENE, another owner")**; grep across all wave files: **no X wave holds `mixStage.ts`, `useMixingState.ts`, `MixResultDisplay.vue` beyond the face carve, `ErrorBoundary.vue`, `demo/palettes/mix.ts`, or the two RED mix specs.**

## RULING PINS (this seat's own derivations)

1. **The BLOCKER's mechanism, its phase machine, and its honest cure surface still live in files no X wave owns** — re-verified at HEAD `9c779130` against the wave files' own bounds tables. The masking fallback (mixStage.ts:121-124), the throwing ramp (:99-107), the one-exit phase machine (useMixingState.ts:104-106), and the anchor's Transition housing (MixResultDisplay.vue:60-76) sit outside every bounds table while every neighboring file is X-W6.j's. The **MX-CLUSTER** NO-WAVE-OWNER booking stands.
2. **P051 kills every producer-facing cure in the corpus** (canon re-read verbatim this session). The WatercolorDot attribute/interactivity drop is ratified canon — "removes its button/tag branch in the clean major"; operable sites migrate to named geometric seats. The BH relay may inform on blast radius; it may never ask to restore forwarding or mint an `as`/`tag` prop.
3. **The re-mix anchor absence is framework-certain (reader-LC M1 ≡ MX-2) and kills all three corpus cures — and reader-DU MISSED it this round.** MixResultDisplay.vue:60 `mode="out-in"` + differently-keyed `well`/`content` branches + `.vj-morph-leave-active` all-legs `--duration-fast` (~200 ms) defer the well's insertion past the `flush:"post"` measurement (useMixingAnimation.ts:171-183) on every `done → mixing` re-mix (the :83 guard blocks only `mixing`; `done → mixing` is legal). The :169-170 docstring is false for every mix after the first. Consequence: the corpus cure (`if (!targetEl) return null` → honest settle) converts every re-mix into an instant animation-free settle; L-1's anchor registry and D's prop/useTemplateRef anchor register on MOUNT and still miss. Constraint ruled: the anchor lives OUTSIDE the swapping Transition (the persistent `.mix-plate` root is one attribute away), or the measurement stops being a single post-flush instant.
4. **The strand family has one genuinely silent member, and even it is not reload-only.** The throw path (MX-4) lands in an ANNOUNCED, focus-managed, recoverable grid-wide boundary (ErrorBoundary template re-read: `role="alert"` + `aria-live="assertive"` + `tabindex="-1"` + a real `Try again` whose `reset()` remounts the slot with fresh `useMixingState`) — console-invisible, user-visible. The null-2D-context path (MX-5) strands silently with a live-looking UI — but `reset()` (useMixingState.ts:108-111) is UNGUARDED and the dock's Clear (usePaneRouter.ts:220) reaches `clearSelection() → reset()` from any phase, so "permanent / only reload recovers" is struck. **MAJOR, ruled against BOTH current readers' BLOCKER** (revival clause: a shipped-engine null-ctx witness revives BLOCKER weight, R-6). The visible Reset control is hidden in the plate's non-ghost branch during the strand — recovery exists and nothing points at it.
5. **The arrival "chord" is continuous per drop and discontinuous in composite — the round's direct contradiction, ruled with this seat's arithmetic.** At `elapsed → MIX_ARRIVE_MS⁻` each drop's head sits at ≈(tx,ty), radius → `lerp(r0,tr,1)·(1+0.22·sin π)` = `tr`, alpha 0.92 — reader-LC's per-drop continuity holds (mixStage.ts:237-240 vs :254-259). But N near-coincident 0.92-alpha source-over discs composite to `1−0.08^N` (0.9936 at N=2, ≈1 at 12), and the next frame paints ONE disc at 0.95 + a 0.171 halo — a composite alpha/edge step for every N ≥ 2, scaling with operand count. The `p >= 1` trail guard (:232) is dead code: `drawDrop` is only reached while `elapsed < MIX_ARRIVE_MS` (:277), where `p < 1` by its own denominator. Reader-DU MISS-8 CONFIRMED as mechanism (MX-34); reader-LC's "no visible pop" superlative RESCOPED to its per-drop form.
6. **The house duration ladder kills D-5's multiplier for good** (`--duration-xl: 1s` / `--duration-xxl: 1.5s`, this seat's token read): 1200 ms sits BETWEEN two shipped rungs. Reader-DU's kill is UPHELD; challenge-D's E-7 cell (three durations, "2.7× the slowest") is an evidence-collection failure, not a judgment call.
7. **The deactivation-smear escalation survives the round that forgot it.** Both current readers graded the KeepAlive row MINOR on the bounded-≤1.2 s-waste argument. The prior round's smear mechanism was re-derived from source this session: detached under `<KeepAlive>` (PaneSlot.vue:120), `clientWidth/clientHeight` = 0, so the per-frame clear (:97-99) AND the terminal clear (:110) degenerate to `clearRect(0,0,0,0)` no-ops while `drawStage` keeps compositing into the retained full-size backing store; the loop stops at 1.2 s leaving the accumulated near-opaque composite IN the bitmap; on reactivation the canvas (still `inset-0`, `style.height` still set) repaints it at z-20 until the next `arm()`'s `canvas.width` assignment or an explicit reset (`clearCanvas` clears ATTRIBUTE px, so it would work — but runs only on `idle`). Not bounded waste — a persistent visible artifact. **MAJOR (MX-31), escalated against both current readers**; on-screen appearance R-9.

## Scoreboard

**43 adjudicated defect rows: 1 BLOCKER · 13 MAJOR · 15 MINOR · 14 INFO.** Every confirmed row of both returns survives re-derivation (severity conflicts ruled below); **15 reader misses admitted** (8 of them re-findings of prior-round rows — identities preserved, not re-booked), **1 reader miss KILLED by this seat** (M5, K-13); **19 killed claims** with written refutations; **6 carried rows re-verified at HEAD**. Headline unchanged and sharpened: **a convergence whose target cannot exist in the shipped DOM — and is structurally absent at measure time on every RE-mix even if it could — lands ~194 px into empty acreage behind a masking fallback, on a route whose default (colors) arm is dead, gated by two structurally unsatisfiable specs that no CI job runs; the mechanism, the phase machine, and the honest cure surface have NO owning wave in tranche X.**

## Ruled disagreements (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **null-ctx severity (MX-5)** | reader-DU: BLOCKER; reader-LC: BLOCKER; D axis: BLOCKER; prior round: MAJOR | **MAJOR** | PIN 4 — `reset()` unguarded + dock Clear re-verified this session; "permanent"/"reload-only" struck; silence + hidden-Reset + inert-but-enabled Mix survive. Revival clause on R-6. |
| **arrival seam** | reader-DU MISS-8: "one-frame opacity drop, grows with N" vs reader-LC superlative-5: "no visible pop, deliberate matching" | **DU CONFIRMED (MX-34, MINOR); LC RESCOPED per-drop** | PIN 5 — the two readers computed different quantities (per-drop kinematics vs composite alpha) from the same lines and each stopped where its thesis was satisfied. |
| **KeepAlive row** | both readers: MINOR (bounded ≤1.2 s); prior round: MAJOR via smear | **MAJOR (MX-31)** | PIN 7 — the smear mechanism re-derived from :97-110 + PaneSlot.vue:120 this session; the current round LOST a prior finding, caught only by adjudication continuity. |
| **forced-colors (MX-17)** | reader-DU: INFO (with the pigment caveat); reader-LC: MINOR; prior round: MINOR | **INFO (demoted)** | DU's caveat is decisive and this seat adopts it: the mix canvas paints the LITERAL operand pigments — during its only visible window it IS a color-display surface, so D's "arbitrary pigment / does not qualify" is overstated. What survives is roster hygiene: a bare `canvas,` leaf rule cannot distinguish tiers and the roster comment (:661-663) enumerates three canvases, not four. LC/prior MINOR preserved as dissent. |
| **superseded composition (MX-28)** | reader-DU: MAJOR (D-4); reader-LC: MAJOR-with-hedge ("whether P122 binds an un-migrated pane is outside this lens") | **INFO — forward conformance, both readers overruled** | The recomposition IS X-W6.j's programmed work (W6.md:310-315, "Mix canary", re-read this session); booking programmed adoption as MAJOR double-counts it. All canon citations verified verbatim and carried. D's MAJOR preserved as dissent. |
| **colors-arm strand reachability** | reader-LC kill: colors mode CANNOT reach the strand; challenge-C/L presented it as the live route | **LC's kill UPHELD; the palettes route is the sole non-synthetic one** | `startMix` parses EVERY colors-mode operand at useMixingState.ts:86-88 BEFORE `:100` sets `mixing` — an unparseable colors operand detonates the boundary from `idle`, no strand. The route that stands: `discard` mode never parses indices ≥ minLen (mix.ts:124, re-read) while `collectStage` parses `slice(0,4)` of EVERY palette — a beyond-minLen unparseable entry within the first 4 passes the math and throws in the narration. MX-4 is non-synthetic on that route alone. |
| **chip-remove severity (MX-41)** | reader-DU: MAJOR | **MAJOR-latent, cross-attributed** | Verified :152-158 + lucide defaults (no `role`/`aria-hidden` → the `<X>` svg contributes no name → the button computes none; 16 px target; `opacity-0 group-hover` with no `focus-within` on the group — though the button's own `focus-visible:opacity-100` does reveal it to keyboard). LATENT: the colors add path is dead (MX-3), so no chip can exist today — the row goes live the day X.W4.g revives the add seats, which is exactly why the same wave must carry both. |
| **MISS-1's cure framing** | reader-DU: "converts nine findings into one adoption" | **fact ADMITTED (MX-38, MAJOR); cure RESCOPED** | The substrate is real and unconsumed (this seat's node probe + d.ts read + zero-adoption grep). But blind adoption re-mints the strand class: `respectReducedMotion` defaults TRUE (park under PRM) — fatal for a loop owning a phase machine's only forward edge — and the `off-screen-io` suspend defers `onSettled` exactly like the tab-hidden freeze (MX-45). Adoption must pass `respectReducedMotion:false` (or read `handle.reducedMotion` and settle), discharge the forward edge on every suspend, and note `useRAFLoop`'s paused-time-excluded `elapsed` is NOT provided (`render(ctx, now)` hands raw `performance.now()`) — the no-jump-cut property must be preserved by hand. Cure-adjudicated-separately law (ColorSpaceSelector addendum item 2). |
| **reader anchor errata** | — | corrected in place | L's line drift re-confirmed at HEAD (mixStage fallback = :121-124 not :117-121; clearCanvas = :160-167; onBeforeUnmount = :187; Card = MixPane.vue:62): corroborates UNWITNESSED-DIRECT (K-12). Reader-DU's D-1 anchor correction adopted; reader-LC's `:83`-guard and `:115`-ghost anchors verified exact. |

## Adjudicated defect roster

Line anchors are HEAD `9c779130`; re-resolve before executing any cure (the D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: a wave id means that wave's bounds own the surface today (re-read this session); **NO-WAVE-OWNER** rows are booked for the next formation boundary. **MX-CLUSTER** = mixStage.ts · useMixingState.ts · MixResultDisplay.vue-beyond-face · ErrorBoundary.vue · demo/palettes/mix.ts · the two RED mix specs — the NO-WAVE-OWNER booking this record re-establishes (PIN 1).

### BLOCKER

- **MX-1 — L-1 ≡ C-D2 ≡ D-D1 ≡ both readers' lead row: `[data-mix-target]` cannot render; every convergence lands on a fabricated target ~194 px from the well, behind a masking fallback.** Writer: MixResultDisplay.vue:69 (sole writer, repo grep). Producer: glass-ui 7.0.0 `watercolor-dot.js` `inheritAttrs:!1`, hand-forwards `class`/`style` only, no `<slot/>`, no `tag` prop, `aria-hidden` + `pointerEvents:"none"` hardcoded (this seat's node probe — now the fifth independent verification). Reader: mixStage.ts:121-124 falls to `{x: clientWidth/2, y: scrollHeight*0.7, r: 28}` — coordinates with no functional dependence on the well, vs the honest `null` at :119/:155. Fires on 100 % of mixes with stamped sources; both axes' live misses agree to 1 px (192.9/194 — R-1). Dating: **since glass-ui 7.0.0** (`f2c8f565`, last commit touching the tree; L's "NEVER existed" struck, K-5; P051's "removes its button/tag branch in the clean major" implies the branch existed pre-7). Cure road ruled (PINs 2+3): consumer-owned anchor OUTSIDE the out-in Transition + delete the fallback in favor of honest settle; NEVER a producer-forwarding ask. → mechanism + fallback: **MX-CLUSTER, NO-WAVE-OWNER**; face/seat migration: **X.W4.g** (trigger-gated; falls to MX-CLUSTER if never fired); canvas-side honest-settle: **X-W6.j**.

### MAJOR

- **MX-2 — reader-LC M1, ruled ADMITTED (PIN 3): on every `done → mixing` re-mix the anchor is structurally absent at measure time — independent of the attribute drop, fatal to all three corpus cures, and missed by reader-DU this round.** `mode="out-in"` (MixResultDisplay.vue:60) + `.vj-morph-leave-active` = `--duration-fast` defer the well ~200 ms past the `flush:"post"` measurement; the :169-170 docstring is false after mix one; C's negative proof #7 and D's "re-arms correctly" state row are both killed by it (K-3). `parent.scrollHeight` (:139) is also read mid-leave, so even the fabricated fallback drifts between first and later mixes (R-8). → anchor restructuring: **MX-CLUSTER, NO-WAVE-OWNER** (the X.W4.g carve is import+face only and cannot restructure the Transition).
- **MX-3 — C-D3 ≡ L-1.3 ≡ both readers: both e2e specs gating this component are structurally unsatisfiable, and NO CI job runs playwright at all.** mix.spec.ts:39-42 + mix-flow.spec.ts:29-32 role-query an add-slot the producer renders as a nameless `<span aria-hidden pointer-events:none>`; both then assert `[data-mix-target]` visible (:52/:40) — unmatchable per MX-1. `.github/workflows/` carries zero playwright/e2e references (this seat's grep, re-run at HEAD) — the RED gate is not a gate. Colors-arm reachability recorded with cross-attribution: BOTH colors-mode add paths are dead producer faces (:164-176 add-slot; :211-221 palette swatches) and colors is the DEFAULT mode (useMixingState.ts:41) — the pane lands on a dead arm; only the palettes arm (native `<button aria-pressed>`, :246-268) reaches this component; C's "workbench cannot be used at all" stays killed (K-2). Unit coverage zero (grep re-run). → spec repair: **MX-CLUSTER, NO-WAVE-OWNER**; CI e2e gating: **X-W1** (CC-031); the add-slot/swatch seats: **X.W4.g** impostor carve `:168`/`:215` + the **wb-mix-sourceselector** seat's roster.
- **MX-4 — C-D1 ≡ L-3 (mechanism): `arm()` is not total — four throwing sites inside `collectStage` bypass all four honest-settle branches.** `parseColorIn` ×2 (mixStage.ts:99-100 → picker-color.ts:109-113), the `!result.ok` throw (:106), `colorToRgb255` (:107 → color-utils.ts:16-20); `pigmentRamp` invoked at :177 inside `picked.map`, OUTSIDE the JSON try/catch (:139-153); called bare from the post-flush watcher (:147). Non-synthetic route = the beyond-minLen discard asymmetry ONLY (ruling table; colors-mode reachability killed). Blast corrected by K-1: an announced, recoverable, GRID-wide boundary with zero console output — both panes die, not just the plate. Cure = totality (`Stage | null` for every failure mode; the null branch exists at :148-154). → mixStage/adapters: **MX-CLUSTER, NO-WAVE-OWNER**; watcher seam: **X-W6.j**.
- **MX-5 — D-D2: `getContext("2d") === null` is the one abort path that never discharges the forward edge — the family's single genuinely silent strand.** :144-145 half-guard (`if (ctx)` guards only the transform) → `collectStage` + `loop.start()` regardless; frame 1 hits :92-95 `loop.stop()` with NO `onSettled()` — the only one of five abort paths that skips the discharge (compare :120-125, :129-133, :148-154). Severity + recovery at PIN 4 (**MAJOR against both readers' BLOCKER**; revival clause R-6). Cure: discharge on the null-ctx branch; the clock-inversion alternative stays KILLED as stated (K-7). → guard: **X-W6.j**.
- **MX-6 — L-4 ≡ D-D6: pigment travels source→canvas as JSON in DOM attributes while the typed truth sits one prop away; the operand budget is enforced three times in disagreement.** Writer caps 4 (MixSourceSelector.vue:253-255), reader caps 12 (mixStage.ts:29/:157), the math consumes `minLen`/`maxLen` by strategy (mix.ts:121-124 — L's "uses all" killed, K-15; DU's 2×8→8-of-16 verified; the 3×8 compounding example killed, K-16). `useMixingState` returns the typed state and MixPane already passes four props into this component (:67-73). → writer: **X-W6.j / X-W7**; reader + one-home budget: **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-7 — L-5 (boundary half) + the blast-radius sharpening: pane crashes are structurally console-invisible, and the one boundary is GRID-wide with per-pane copy.** ErrorBoundary.vue:59-69 `return false`, no `console.error`/`reportError`/telemetry — the audit's `consoleErrors: []` rows for `/#/mix` prove nothing. Mounted at App.vue:50 wrapping the whole `.pane-container` (:58) inside `<main>` (:47) while saying "This PANEL hit an unexpected error" — mis-describing its own blast radius. The boundary is invisible only to a console scraper, never to the user (K-1 cuts both ways). One-line observability cure before `return false`; scope cure = per-pane boundaries or honest copy. The admin-token reproduction half stays UNPROVEN (R-2). → **MX-CLUSTER, NO-WAVE-OWNER** (ErrorBoundary.vue in no X bounds).
- **MX-8 — C-D4: the settle event is silent to assistive technology.** Zero `aria-live`/`role="status"`/`role="alert"` in the mix tree (grep re-run); the premise "the animation IS the progress" (MixPane.vue:107-110) leaves NO non-visual channel for progress or completion; `onSettled` is the correct seam. The canvas itself is correctly out of the AT tree. → **X-W6.j**.
- **MX-18 (carried, re-verified): `convergeCss()` duplicates `wellColor` with divergent, non-interchangeable fallbacks.** useMixingAnimation.ts:75-82 (`null` → honest settle) vs MixResultDisplay.vue:36-40 (`var(--muted-foreground)`) — identical selection, opposite fallback poles; the display's fallback would detonate MX-4 if it ever crossed (`parseColorIn` cannot parse `var()`). Single home = the `MixResult` model. → **MX-CLUSTER, NO-WAVE-OWNER** + **X-W6.j** (convergeCss side).
- **MX-19 (carried, re-verified): the Mix verb is not gated on the narration window — on any strand path it is a permanently dead, permanently enabled primary action.** MixConfigBar's `Button :disabled="!canMix"` only (:162-170, re-read); the silent discard at useMixingState.ts:83; the dock mirror at usePaneRouter.ts:221. Healthy path ≤1.2 s inert-but-enabled; strand paths leave the page's ONE verb dead with no feedback and the only recovery (dock Clear, :220) unadvertised. → **X-W6.j / X-W7**.
- **MX-31 (carried, ESCALATED against both current readers — PIN 7): no deactivation gate + no-op clears = a persistent pigment smear on reactivation, not bounded waste.** PaneSlot.vue:120 `<KeepAlive>`; sole teardown `onBeforeUnmount` (:187); `onDeactivated` = 0 repo-wide; `pauseWhenHidden` binds document visibility only; `useIntersectionPause` ships unused. Detached: clears no-op at 0×0 while `drawStage` composites into the retained backing store; the terminal clear (:110) also no-ops; the composite survives reactivation at z-20. → **X-W6.j** (gate j2 is literally this contract).
- **MX-32 (≡ reader-DU MISS-2, prior-round identity, re-verified): the palette mix result — the only arm a real user can reach — has NO text alternative.** MixResultDisplay.vue:91-116: N producer-`aria-hidden` spans (`tag="div"` and `:title` both dropped per the producer bytes) + an explicitly `aria-hidden role="presentation"` gradient strip; contrast the color branch's visible css text (:85-87). A screen-reader/tooltip user gets the word "Result" and three dock controls; the css list exists (it feeds the clipboard, :43-46) and is never rendered. Compounds MX-8 into a complete non-visual dead end. Surface owner = the **wb-mix-resultdisplay** seat; the X.W4.g carve (import+face only) is insufficient alone. → **MX-CLUSTER / X.W4.g**.
- **MX-38 — reader-DU MISS-1, ADMITTED (cure rescoped, ruling table): glass-ui 7 ships the exact Canvas2D lifecycle substrate this component hand-rolls, and NO demo consumer uses it.** `@mkbabb/glass-ui/canvas` exports `useCanvas2D`/`useCanvasLifecycle`/`resolveCanvasColor` (this seat's dist read); the typed contract covers DPR-policy sizing (same `min(dpr,2)` ceiling), CSS-px pre-transformed ctx, substrate-owned resize, `off-screen-io` IntersectionObserver suspend, `tab-hidden` suspend, LIVE-monitored PRM, `dispose()` — the family MX-12/16/21/31 and half of MX-9/10 patch by hand. Adoption = zero across all three demo `<canvas>` sites (grep re-run). All three axes and the prior round missed it. Constraint ruled: adoption must preserve the forward-edge totality and the paused-time-excluded elapsed (ruling table) — the substrate parks; this loop must never park without settling. → **X-W6.j** (the adoption surface is the owned file; gate j2's teardown assertion is the natural home) + glass-ui BH relay note (informational).
- **MX-41 — reader-DU MISS-3, ADMITTED (MAJOR-latent, ruling table): the chip remove control is a nameless, 16 px, hover-revealed button.** MixSourceSelector.vue:152-158 — no `aria-label`/`title`/text; sole child a lucide `<X>` whose svg carries no `role`/`aria-hidden`/`<title>` (defaultAttributes read) → no computed name (WCAG 4.1.2); `w-4 h-4` = 16 px (SC 2.5.8 sub-minimum, spacing exception plausible); `opacity-0 group-hover:opacity-100` with no `focus-within` on the group → invisible-but-hittable on touch. Unwitnessable today: the add path is dead, so no capture ever contained a chip (the REPORT's `/#/mix` "namelessButtons 1" is a different control). Latent until X.W4.g revives the add seats — the same wave carries both. → **X-W6.j / X.W4.g** + the **wb-mix-sourceselector** seat's roster.

### MINOR

- **MX-9 — L-2 ≡ C-D6: PRM re-derived through `useBreakpoint` from a second glass-ui subpath while `motion-core` — imported on the next line — owns the concept as a refcounted singleton.** :41-42/:70-72; `useReducedMotion` unconsumed repo-wide; L's four-parallel-homes table verified by the readers, spot-checked here. → **X-W6.j** + repo-wide one-home rider: **NO-WAVE-OWNER**.
- **MX-10 — D-D9 ≡ L-2c: PRM sampled once at arm; reduce engaged mid-narration is ignored.** :120 sole read; `respectReducedMotion:false` makes the host's PRM ref a constant. The posture is right (superlative 3); the missing 3-line watcher (stop + settle on flip) is the correct expression. → **X-W6.j**.
- **MX-12 — L-6 ≡ C-D9: backing store sized to the pane's scroll extent, never released; the file's two clears disagree on coordinate space.** :137-143 sizing; `clearCanvas` (:160-167) clears ATTRIBUTE px through the still-armed dpr transform (:145) while the loop clears CSS px (:97-99); `style.height` reset but the attributes retained; runs only on explicit idle. Both corpus v-if cures stay killed (K-6); sound cure = zero the backing store at the loop's terminal frame. → **X-W6.j**.
- **MX-13 — L-9 ≡ D-D5a: the motion timeline is three private module constants outside the producer's duration/tempo register** (mixStage.ts:22-26; the CSS PRM neutralizers cannot reach JS constants — exactly why MX-10's hand gate must exist; `--motion-weight: 0.618` verified shipped). The "2.7×" multiplier stays dead (K-8, PIN 6). → constants: **MX-CLUSTER, NO-WAVE-OWNER**; arm-time token read rider: **X-W6.j**.
- **MX-14 — L-10 ≡ C-D3b: zero tests bind these 507 lines; the duplicate twin is covered through a test/→demo inversion** (test/mix-v4.test.ts:3-4, re-read; greps re-run = 0). The pure half is trivially Node-testable — the cheapest real repair in the corpus. → **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-15 — L-3b: "mix or throw" implemented twice with two error strings; the demo adapter erases the library's `Result` channel at the first boundary** (mix.ts:28-37 vs mixStage.ts:102-107; picker-color.ts:104-113 + color-utils.ts:16-20, all re-read). → **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-16 — D-D8: geometry sampled once at arm; resize/zoom/reflow mid-narration unhandled** (no ResizeObserver/resize listener — whole-file reads). MX-38 is the producer-shaped cure. → **X-W6.j**.
- **MX-20 (banked identity rider): tsconfig.demo.json ↔ exports drift — two dangling keys (`parsing`, `units`), two omissions (`/css`, `/value`), a bare-root mapping to a nonexistent `dist/index.d.ts`, and vue-tsc resolving `/css` from the stale tarball while Vite serves the checkout — inside this component's transitive closure (picker-color.ts imports `/css`).** All facts re-derived this session. Identity ≡ ColorSpaceSelector L-14 ≡ W8.md:112's declined row ("L-22 ≡ W-HYGIENE, another owner") — **banked NO-WAVE-OWNER, not re-booked**; reader-LC's typecheck-vs-runtime sharpening rides as the identity's note.
- **MX-33 (≡ reader-DU MISS-5, prior-round identity, re-verified): the canvas pins and inflates the very `scrollHeight` it measures.** :139-140 write-back; `clearCanvas` releases `style.height` only on idle; the abspos canvas (inset-0 in the `relative overflow-y-auto` Card) holds the scroll extent up after `done`, so shrinking content leaves a phantom scroll region and the next `arm()` re-reads a number the canvas itself sustains — monotone-nondecreasing across mixes. → **X-W6.j**.
- **MX-34 (≡ reader-DU MISS-8, ruled at PIN 5): the arrival hand-off steps discontinuously in composite alpha and edge profile, scaling with operand count; the `p >= 1` trail guard is dead code.** Per-drop kinematics genuinely continuous (rescoped superlative). Perceptual magnitude R-10; reader-DU's INFO grading preserved as dissent. → **MX-CLUSTER, NO-WAVE-OWNER** (drawStage's).
- **MX-35 (≡ reader-LC M3, banked identity + the sharper limb): the demo has effectively NO live import-boundary enforcement.** Re-verified this session: the `:274+` composables object globs only the deleted `demo/@`; the `:232-239` object's ONE pattern keys on the W43-killed `@components` alias shape, so even the live `demo/color-picker/**` glob bans a specifier no file can write; only `src/**` (:204) binds; the ts-block's substantive rules are explicitly `off`. Dead-glob half ≡ ColorSpaceSelector L-5(a), **banked**; the no-live-demo-enforcement limb: **NO-WAVE-OWNER**.
- **MX-39 — reader-LC M6, ADMITTED with one limb corrected: the `catch` in `collectStage` is mislabelled and swallows the one case that deserves a signal.** The documented case ("unstamped source") takes `?? "[]"` and parses successfully — it never enters the catch (:140/:151-153); the catch fires only on MALFORMED `data-mix-colors` and discards it with no diagnostic. Corrected limb: no partial pushes can be retained from the throwing element itself (JSON.parse and the array check throw before any push); cross-element retention is correct behavior. Combined with MX-4: the module swallows the recoverable failure and throws on the unrecoverable one — inverted. → **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-42 — reader-DU MISS-4, ADMITTED: the ghost plate is guard-then-alpha — a 0.55 post-hoc opacity over a subtree whose only text already rides the muted rung, for the full narration window, while the same workbench states the prohibition verbatim four files away.** `.mix-plate--ghost { opacity: 0.55 }` (:155-157) via `:class` at :56; the "Result" label is `text-muted-foreground` (:58); the law in-tree at MixSourceSelector.vue:186-189 ("'quieter' and 'illegible' must never collapse"). Whole-surface state opacity is a milder species than per-text alpha — noted — but the muted-text × 0.55 product during 900 ms is the same risk. Contrast ratio R-12. → **MX-CLUSTER, NO-WAVE-OWNER** (plate styling is beyond the face carve).
- **MX-43 — reader-DU MISS-6, ADMITTED: the decorative overlay is stamped on the controls z-tier while the demo owns a NAMED ornament tier for exactly this.** `z-controls` (MixAnimationCanvas.vue:32) vs `--z-ornament: 20` (foundation.css ~:393, consumed at seat.css:55, documented at ColorPicker.vue:85 "content-top, chrome above") — both resolve to 20 today (no visual delta), so all three axes' "utility is real" negative proofs answered the wrong question; the day the tiers separate, a decorative surface rides the controls band. → **X-W6.j** (one class swap in the owned file).
- **MX-45 — reader-LC M4, ADMITTED: a backgrounded tab freezes the reveal for an unbounded time.** `pauseWhenHidden` stops frame delivery and `elapsed` excludes hidden time, so `onSettled` — fired only inside a frame (:103-106) — waits for refocus; the plate holds ghost at 0.55 with the result already computed (useMixingState.ts:85-98). Self-heals on refocus (hence MINOR), but it is the same "decoration gates product state" class as MX-5, and both C's and D's state tables filed it as SOUND. → **X-W6.j**.

### INFO

- **MX-17 — D-D10, DEMOTED this round (ruling table): the mix canvas inherits the colour-DISPLAY forced-colors exemption via the bare `canvas,` leaf rule.** The pigment caveat is decisive (the canvas paints the true operand pigments); the surviving defect is roster granularity — the tier-1 roster should name a class, and its comment enumerates three canvases, not this fourth. LC/prior MINOR preserved as dissent. → roster cure: **NO-WAVE-OWNER**; witness: **X-W1**.
- **MX-21 — C-D7's surviving observation + the per-frame `getContext` re-acquisition (:91) for a context obtained at :144.** BOTH readers' kills of C-7's transposition cure UPHELD (K-14): `arm()` never sets `style.width`, so `clientWidth` is live layout truth — caching it entrenches MX-16. → **X-W6.j rider**.
- **MX-22 — D-D12: a fresh `CanvasGradient` per disc per frame** (mixStage.ts:199; ≤38/frame at cap). Not a present bottleneck. → rider.
- **MX-23 — L-8 ≡ C-D8: dead public surface** — the consumer-less constants re-export (:49; sole external echo is prose re-typing `900` at mix.spec.ts:15) and the discarded `{ stop }` return (:189 / MixAnimationCanvas.vue:20). → **X-W6.j rider**.
- **MX-24 — L-11: a pure, Vue-free model shelved inside `composables/`** (own docblock "No lifecycle, no clock"; non-`use*` census = 4, L's count a labelling slip per reader-DU). → **MX-CLUSTER rider**.
- **MX-25 — L-12: no import-boundary rule reaches this file** (`--print-config` → `[]`, both readers; config re-read here). Identity ≡ ColorSpaceSelector L-5(a) — cross-referenced, not re-booked (MX-35 carries the sharper limb).
- **MX-26 — L-13: published-surface facts — 7-key exports (no `.`, no `require`), `dist/value.js` + `dist/index.d.ts` absent while load-bearing comments assert them.** Facts re-verified; L's provenance citations killed (K-19). Identity overlaps MX-20; rides W-HYGIENE. → **NO-WAVE-OWNER (banked identity)**.
- **MX-27 — D-D13 + the sharpening: `/#/mix` is absent from every state matrix — six of them, not five** (keyboard-focus-desktop also holds only the 5-route set; this seat's own listing). Only the four safari at-rest cells hold mix.png. → **X-W1** (owns the witness matrices).
- **MX-28 — D-D4, ruled INFO (ruling table): built against the composition the ratified canon superseded** (P122 golden trough; "Card … never the default page primitive"; all citations verified verbatim). The recomposition is X-W6.j's programmed work; both readers' MAJOR preserved as dissent. → **X-W6.j**.
- **MX-29 (carried, re-verified): `arm()`'s PRM and no-canvas early returns leave the previous arm's `Stage` installed** (:120-133 never null module-scope `stage`; only the idle branch does). Held by accident (`loop.stop()` precedes); one line makes it structural. → **X-W6.j rider**.
- **MX-36 (≡ reader-LC M8, one limb demoted): `collectStage` re-parses the invariant pool pigment once per drop (≤12 redundant `parseColorIn` pairs) and derives the pool colour as operand 0's ramp endpoint instead of the parse it already has** (:99-100 via :177; :186). The order-dependence limb is DEMOTED to theoretical: `mixColors(a,b,1)` should equal `b` in-space with hue methods coinciding mod 360 — no demonstrated asymmetry exists; what stands is the redundancy and the indirection (hoisting also shrinks MX-4's throw surface ~24 → 13 calls). → **MX-CLUSTER rider**.
- **MX-37 (carried, re-verified, EXTENDED with reader-DU's confirmed D-11 observation): the `data-mix-*` contract exists ONLY as prose that is now false, and the component's declared props are not its real inputs.** useMixingAnimation.ts:5-13 + MixResultDisplay.vue:12-17 — no type, no prop, no test, no lint rule; both false since 7.0.0; the boundary declares `phase/result/space/hueMethod` while the real inputs are four data attributes on unowned subtrees plus `parentElement`'s identity. D-11's collapse-the-wrapper cure stays killed (K-9). → dies with MX-1/MX-2's cure: **MX-CLUSTER rider**.
- **MX-40 — reader-LC M7, ADMITTED: the composable's option types declare writable `Ref`s no caller can supply** (`useTemplateRef` → `Readonly<ShallowRef>`; `toRef(() => x)` → readonly getter-refs; TS ignores `readonly` in property assignability, so the mutability contract is a silent lie). Honest surface = `MaybeRefOrGetter` + `toValue`. → **X-W6.j rider**.
- **MX-44 — reader-DU MISS-7, ADMITTED: `layoutCenter`'s termination depends on an unasserted Tailwind utility in a different file.** The walk (:76-79) exits at `root` only because MixPane.vue:62's Card happens to carry `relative`; remove it and the walk runs to `null` past `<body>`, silently mis-placing every drop — no throw, no test. Same silent-coupling family as MX-1; all three axes filed the dependency as an EXONERATION (reader-LC's superlative verified it holds today via the Card→Surface slot-at-root chain — both true: it holds, and nothing binds it). One assertion or comment cures it. → **MX-CLUSTER rider**.

## Killed-claims register

Reader kills upheld on re-derivation, carried kills re-verified, and one reader claim killed by this seat.

- **K-1 — C-D1's blast narrative ("no result, no error, no Reset, dead forever, only reload recovers") — UPHELD (reader-DU).** The throw routes to ErrorBoundary's `role="alert"` fallback with focus management and a live `Try again` that remounts the grid with fresh state (template re-read this session). The console-silence limb is real (MX-7); the true blast is LARGER than C reported (grid-wide, both panes).
- **K-2 — C-D3's "the Mix workbench cannot be used at all / unreachable dead code" — UPHELD (reader-LC).** The palettes arm is a native `aria-pressed` button carrying the real `data-mix-*` stamps (:246-268, re-read); the dock Mix verb is real. The colors arm is fully dead — C's own sibling axis (D-7) states the truth C's generalisation contradicts.
- **K-3 — C's negative proof #7 + D's "re-arms correctly" state row — KILLED by MX-2 (reader-LC's M1).** Both axes certified clean the exact area their largest miss lives in; no probe could distinguish the re-mix null from the attribute-drop null while the masking fallback existed.
- **K-4 — L-1's `MixAnchorPort` registry cure (~40 lines) — UPHELD as killed, three grounds**: KISS-disproportionate (the plate's own wrapper div is one attribute-move away); structurally insufficient (mount-time registration still misses on every re-mix — MX-2); P051-blind (any cure keyed on a producer face is anchored to canon-retired semantics).
- **K-5 — L-1's "has NEVER existed in the shipped DOM" — UPHELD; "never" struck to "since glass-ui 7.0.0".** P051 re-read verbatim: the button/tag branch is removed "in the clean major" — it existed pre-7, as D dated it (W44/D58). `f2c8f565` is the last commit touching the tree (this seat's git log). A ≤6-dist witness remains R-7.
- **K-6 — L-6's `v-if="phase !== 'idle'"` cure — UPHELD, both directions (both readers)**: `done` satisfies the predicate for the whole KeepAlive-cached tail (releases nothing), and the tighter `phase === 'mixing'` amputates the 300 ms epilogue (`onSettled` fires at 900 ms; the loop draws to 1200 ms, :103-111). Sound cure: zero the backing store at the terminal frame.
- **K-7 — D's gestalt-cure step 2 ("invert the clock; delete `onSettled` as a state edge") — KILLED as stated (carried; neither current reader re-litigated).** A zero-width `mixing` deletes the ghost morph, the narration window and the announced destination — subtraction wearing an inversion's clothes. Surviving form: keep the edge, make it unconditional.
- **K-8 — D-D5's "1200 ms is 2.7× the slowest house duration" — KILLED (reader-DU; this seat's ladder read, PIN 6).** `--duration-xl: 1s` / `--duration-xxl: 1.5s` ship; 1200 ms sits between them. D's E-7 evidence cell was incomplete at collection time.
- **K-9 — D-11's "KISS-banned contrivance / collapse into MixPane" cure — UPHELD as killed (carried).** The 35 lines own the element, its a11y posture, the settled emit and the props→Ref adaptation. The props-vs-real-inputs OBSERVATION survives inside MX-37.
- **K-10 — the corpus's producer-relay cure limb ("restore forwarding, or an `as`/`tag` prop") — KILLED (carried; P051 re-read verbatim this session).** Ratified canon, not a regression; the BH relay informs, never asks to restore. Neither current reader challenged the framing — recorded again as a fleet observation.
- **K-11 — C-D1c's `css: undefined → "null" → .trim()` sub-route — UPHELD as killed** (`PaletteColor.css: string` required, types.ts re-read; the route presumes a type-violating payload). The route that stands is the beyond-minLen discard asymmetry (ruling table).
- **K-12 — L's citations as dated evidence — provenance kill SUSTAINED.** UNWITNESSED-DIRECT (HYDRATION-LEDGER.md:305, definition :9-11 re-read); systematic 2-5-line anchor drift on a tree unchanged since `c654824e` (diff re-run, empty). L's substance survives only where re-proven — as practiced throughout.
- **K-13 — reader-LC M5 ("the bitmap width derives from a different box than the CSS width; horizontally stretched on any classic-scrollbar platform") — KILLED BY THIS SEAT.** The premise contradicts the spec: CSS 2.1 §11.1.1 requires that "any space taken up by the scrollbars should be taken out of (subtracted from the dimensions of) the containing block formed by the element with the scrollbars" — so the abspos canvas's `w-full` resolves against the scrollbar-reduced padding box, i.e. the same quantity as `parent.clientWidth`, and the two widths agree. LC itself marked the visible consequence unproven; the factual assertion "where present, the two widths differ" is unestablished and spec-contrary. Revival clause: a live engine witness of the divergence (R-11) revives the row.
- **K-14 — C-D7's premise + transposition cure — UPHELD as killed (both readers; this seat's read).** `arm()` never writes `style.width`; `clientWidth` is live layout truth; hoisting it onto `Stage` freezes the clear rect against a box that CAN change, entrenching MX-16. The two-reads-per-frame observation survives as MX-21.
- **K-15 — L-4's "the mix math uses ALL of them" — KILLED (reader-DU; mix.ts:121-124 re-read).** Under the shipping default (`discard`, useMixingState.ts:46) the math consumes `minLen` per palette. The three-budget drift survives with the third number corrected.
- **K-16 — D-D6's "a 3×8 narrates 12 of 24 (both truncations compounding)" — KILLED (reader-DU; arithmetic).** Three palettes × `slice(0,4)` = 12 origins; `MAX_DROPS = 12` is exactly non-binding there. The caps compound only from four palettes up. The finding's core (two unconditional, unsurfaced truncations) stands in MX-6.
- **K-17 — D-D5's π-2 caption ("the entire payload of a 1.2-second narration is one ~56 px disc … 0.71 %") — KILLED as a caption (reader-DU; drawStage:277-281 re-read).** The either/or draws N travelling drops + trails for 700 of 1200 ms; t=880 is by construction the emptiest frame. The terminal-state design judgment survives; "the entire payload" does not.
- **K-18 — L-7's "two `onActivated`/`onDeactivated` hooks exist … that asymmetry is the finding" — KILLED as evidence (reader-LC; grep re-run: `onDeactivated` = 0 anywhere in demo).** Both HeroBlob hits are `onActivated`. The finding itself (no deactivation gate) is strengthened and lives at MX-31.
- **K-19 — L-13's provenance citations — KILLED (reader-LC; this seat's reads).** vite.config.ts:41-50 is the alias GENERATOR (no bare-root alias is ever minted — exports has no `.`); the quoted sentence and the "CLOSED 8-key set" comment live in tsconfig.demo.json (~:40-42). The substantive facts stand in MX-20/MX-26.

## Superlatives (provenance preserved)

Items 1-5 and 9 carry this seat's own re-verification this session; the rest are adopted from the axes + readers with their receipts.

1. **The pigment ramp is chromatically true, not decorative** — each drop rides a 16-stop perceptual ramp from its OWN colour to the true mixed result, sampled in the SAME space + hue method the mix math ran, projected to sRGB bytes only at the drawing boundary (mixStage.ts:93-109, re-read); the pool IS the last ramp stop (:186), so the landing colour cannot drift from the result. The narration narrates the actual computation.
2. **`layoutCenter`'s offsetParent walk** (:65-86) — transform-immune by frame choice, with the reason written down; canvas user-space and offset-space share one origin. All three axes tried to break it and could not. (Its unasserted termination dependency is MX-44 — a finding beside a superlative, both true.)
3. **The reduced-motion posture is the CURE for the repo's PRM-RAF epidemic, not an instance** — `respectReducedMotion: false` + complete-immediately is exactly right for a loop owning a state machine's only forward edge (docstring :33-36, re-read); a paused host loop would strand it. MX-10 is three missing lines, not a wrong posture — and MX-38's substrate must be held to the same discipline.
4. **Totality as an explicit design goal** — four named abort branches all fire the forward edge, the last commented "settle honestly rather than stall the phase machine" (:148-154). The two holes (MX-4, MX-5) are coverage failures inside a correctly-tracked invariant.
5. **The stagger bound is provably safe** — `min(60, 240/n)` caps the worst delay at 220 ms against a 700 ms arrival (:158, re-checked), so :216's denominator never degenerates. Algebra replacing a guard.
6. **The `useRAFLoop` adoption** — `pauseWhenHidden`'s `elapsed` excludes paused time (producer bytes, reader-LC), so resume never jump-cuts; the retired hand-rolled delta cap really was redundant. A consumer that read its dependency's implementation.
7. **Safari-true by construction, kept** — radial-gradient discs + source-over only; no `ctx.filter`, no engine branch, declared in both file headers (whole-file reads).
8. **The module split is real and load-bearing** — pure model / clock+lifecycle / 35-line mount, which is exactly why MX-14's zero-tests row stings: the author built the testable seam and no one used it.
9. **The drop→pool hand-off is C0/C1-continuous PER DROP — RESCOPED (PIN 5)**: position and radius hand over seamlessly at p=1 (reader-LC's arithmetic, verified); the composite step for N ≥ 2 is MX-34. The narrow superlative survives; "no seam" does not.
10. **The tree's docstrings state FALSIFIABLE invariants — and were caught lying because of it**: the flush-post rationale (MX-2), the chord promise (MX-34), the anchor contract (MX-37). Code that can be caught lying is better than code that says nothing. Challenge-C's probe log remains the corpus's most falsifiable artifact (reader-DU's superlative, adopted: where its findings died, severity framing died, never a measurement).

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — the 192.9/194 px landing-miss magnitudes + pool-centroid readings (mechanism statically certain; two axes agree to ~1 px; viewport/content-dependent).
- **R-2** — L-5's `palette-admin-token` crash reproduction (no `.replace` on that path statically; the admin surface has churned since the challenge base; the boundary-swallow half stands independently). PROCESS NOTE preserved: the L seat mutated shared dev-browser state and left it mutated.
- **R-3** — post-deactivation magnitudes (426 gradients / 100 frames) + the 1.33 MB backing-store figure (formula verified; instance numbers live-only).
- **R-4** — the pasted RED playwright run as a RUN (structurally entailed by MX-3 regardless).
- **R-5** — 0.71 % stage-area share, 510×683 pane dims, `armFlushMs 5.7`.
- **R-6** — the shipped-engine firing rate of `getContext("2d") === null` (MX-5's severity ceiling; both readers' BLOCKER revives on a witness).
- **R-7** — whether `[data-mix-target]` rendered under glass-ui ≤6 (canon implies yes; no seat inspected a ≤6 dist).
- **R-8** — the re-mix fallback drift (mid-leave `scrollHeight` delta, MX-2/MX-33 compounding).
- **R-9** — MX-31's on-screen residue: the retained composite's visibility on reactivation.
- **R-10** — MX-34's perceptual magnitude at N ∈ {2, 3, 8, 12}.
- **R-11** — K-13's revival witness: any engine where an abspos `w-full` child's containing block includes the scrollbar gutter.
- **R-12** — MX-42's resulting contrast ratio (`text-muted-foreground` × 0.55 against `bg-well`; boot-stamped, floor-clamped — one computed-style read grades MINOR vs MAJOR).
- **R-13** — the six state matrices' missing mix.png (MX-27): every reduced-motion / forced-colors / zoom / RTL / keyboard-focus claim about this route is reasoned, not seen; the only narration frames in the corpus are π-1/π-2.
- **R-14** — challenge-L's live numbers wholesale (UNWITNESSED-DIRECT): adopted nowhere in this record without independent re-proof.

## Motion-law compliance

No `motion-quarantine.md` FALSE row is carried. The quarantine's subject set is the 46 REPORT-AUTHORED rows (motion-quarantine.md:16, re-read this session); this component's three challenge files are EXISTS-ORIGINAL ×2 + UNWITNESSED-DIRECT ×1 (HYDRATION-LEDGER.md:240-241/:305, re-read), none in that set, and a grep of the quarantine for mix rows returns none. The nearest row, M-26, adjudicates a WatercolorDot CSS-transition claim in a different component's corpus; every PRM row here (MX-9/MX-10/MX-13/MX-45) is a JS-mechanism claim — the class the quarantine's own architecture finding says the central CSS guards cannot reach. The complete-immediately PRM-negative is adopted, not contradicted.

## DISSENT — preserved, not ruled away

1. **Both current readers hold BLOCKER on MX-5**; this seat ruled MAJOR on the re-verified recovery path (PIN 4). The revival clause (R-6) is the honest boundary of that ruling. Symmetrically, reader-DU's recorded severity split on the KeepAlive row (its own D-axis dissent) is resolved the OTHER way by PIN 7 — MAJOR, but for the smear, not for D's bounded-waste argument.
2. **The reader-vs-reader contradiction record**: reader-DU's MISS-8 and reader-LC's continuity superlative described the same frames and disagreed; both were partially right (PIN 5). Two hostile readers reaching opposite conclusions from the same arithmetic — each stopping at the quantity that suited its thesis — is evidence about single-quantity analysis, not about the tree. The same event occurred in the prior round; it is now a repeated fleet pattern.
3. **Round-over-round finding loss**: the current round DROPPED the prior round's smear mechanism (both readers graded the row MINOR without it) and reader-DU dropped the out-in re-mix finding reader-LC kept. Adjudication continuity — carrying re-verified rows across rounds — is what caught both. Recorded as evidence for keeping the supersession discipline exactly as practiced: carry nothing unverified, but verify before dropping.
4. **Citation-inheritance events**: challenge-D's E-7 stale ladder cell was killed in both rounds (K-8); neither current reader challenged the corpus's producer-relay framing (K-10 carried, again). Reader-DU's severity grading of MX-17 (INFO) is ADOPTED against reader-LC and the prior round — the reverse direction is possible too, and the pigment caveat is recorded so the next seat can re-derive rather than inherit.
5. **Reader-LC's M5 kill (K-13)** rests on spec text plus engine-behavior reasoning, not on a live witness; LC's underlying instinct (the sizing path reads a different box than the CSS resolves) is exactly the class of coupling this corpus keeps finding. If R-11 ever produces a witness, the row revives at LC's stated MINOR.
6. **Whether P122/the trough BINDS an un-migrated pane today** (reader-LC's hedge on D-4) remains a design/ownership ruling outside both reader lenses; this record books the conformance gap as INFO routed to the wave whose programmed work it is, and preserves both readers' MAJOR.

---

**ADJUDICATED RED — the corpus's direction confirmed, its consequence claims corrected, every cure it proposed cut down or rescoped, and one reader miss killed on spec grounds.** 43 rows survive (1 BLOCKER · 13 MAJOR · 15 MINOR · 14 INFO); 19 killed claims with written refutations; 15 reader misses admitted (8 as re-findings of prior-round identities), including the producer's own unconsumed Canvas2D substrate (MX-38) and the reachable arm's total absence of a text alternative (MX-32). Terminal dispositions: **~15 rows/limbs to X-W6.j** (the Mix canary owns the clock file and every wired neighbor) · **3 riding X.W4.g trigger-gated** (falling to MX-CLUSTER if the census never fires) · **3 to X-W1** (CI e2e gating; witness matrices; forced-colors witness) · **2 shared with X-W7** · **4 banked identities** (tsconfig ≡ L-14/L-22-W-HYGIENE · lint lattice ≡ L-5(a) · published surface ≡ W-HYGIENE) · **the MX-CLUSTER to NO-WAVE-OWNER** — mixStage.ts, useMixingState.ts, MixResultDisplay.vue beyond its face carve, ErrorBoundary.vue, demo/palettes/mix.ts, and both RED mix specs. That compound fact — the convergence's masking fallback, its one-exit phase machine, its Transition-housed anchor, and the silent grid-wide boundary that hides all their failures live in files no tranche-X wave owns, while every file around them is owned — remains this apotheosis's headline for the next formation boundary.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3. This file supersedes the 2026-08-10 prior-round adjudication at this path; nothing dated was rewritten.
