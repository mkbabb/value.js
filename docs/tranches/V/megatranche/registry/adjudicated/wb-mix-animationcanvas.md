# wb-mix-animationcanvas — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-10 · **HEAD**: `54a97489` · branch `tranche-u` · **Corpus**: `demo/workbenches/mix/MixAnimationCanvas/` (MixAnimationCanvas.vue 35 · useMixingAnimation.ts 190 · mixStage.ts 282) plus the contract surfaces it rides (MixPane.vue · MixResultDisplay.vue · MixSourceSelector.vue · MixConfigBar.vue · composables/useMixingState.ts · color-session adapters · ErrorBoundary.vue). Challenge axes on disk: **challenge-C-implementation.md** (EXISTS-ORIGINAL) · **challenge-D-design.md** (EXISTS-ORIGINAL, committed frames `mix-frozen-t715.png` / `canvas-only-t880.png`) · **challenge-L-library.md** (UNWITNESSED-DIRECT, HYDRATION-LEDGER.md:305 — sha intact, citations drift; every L fact re-proven before carriage). The charge names the governing round "r2"; exactly one round per axis exists in the directory — that round is adjudicated as found.
**Re-readers adjudicated**: TWO separate hostile returns (design-and-usage · library-and-code): return-1 = 13 confirmed · 5 killed · 7 missed; return-2 = 25 confirmed · 8 killed · 5 missed. Both attacked before adoption; every kill re-derived, every promoted miss re-walked in source, and the one direct reader-vs-reader contradiction (arrival-seam) ruled with this seat's own arithmetic.
**SUPERSESSION**: an uncommitted 2026-08-09 draft of this same seat sat at this path, adjudicating an EARLIER reader round. This file supersedes it. Its arbiter-minted receipts were NOT trusted — each was re-verified this session with this seat's own instruments before carriage (P051 canon verbatim; X-wave bounds re-read and EXTENDED through W10/W11, which the draft never opened; runtime-core out-in mechanism re-reasoned against `.vj-morph-leave-active`). Four of its reader-round rows (MX-18/19/20/29) survive re-verification and are carried with provenance marked; nothing else of it is inherited unexamined.
**Arbiter's own instruments (static only — probe parsimony; zero browser tooling)**: whole-file reads of mixStage.ts, useMixingAnimation.ts, MixAnimationCanvas.vue, useMixingState.ts, MixPane.vue, MixResultDisplay.vue + MixSourceSelector.vue:140-270 + MixConfigBar.vue:150-175 + ErrorBoundary.vue:55-80 + App.vue:45-52; installed glass-ui 7.0.0 probed by node — `dist/watercolor-dot.js` (**`inheritAttrs:!1` true; `$attrs`-spread regex false; slot-render regex false; `aria-hidden` + `pointerEvents` hardcoded; props open `{color:{}…}`, no `tag`**), `dist/motion-core.js` export tail (**`e as useReducedMotion` real; `useIntersectionPause` shipped**), `dist/styles/tokens/scheme-motion.css` (**full ladder: `--duration-instant .1s … --duration-slow .45s, --duration-panel .55s, --duration-xl 1s, --duration-xxl 1.5s`; `--motion-tempo` scoped to `:where(.glass-drawer,.liquid-stage)` only**); `.vj-morph-leave-active` = all-legs `var(--duration-fast)` (animations.css:111-117); `.github/workflows/` = ci.yml + deploy-pages.yml + release.yml, **zero playwright hits**, playwright.config.ts:33 cites the nonexistent `node.js.yml`; e2e/smoke/views/mix.spec.ts:39-52 + safari/mix-flow.spec.ts:30-40 (**both assert `[data-mix-target]` visible + the role-named add-slot**); foundation.css **bare `canvas,` at :680** opening the forced-colors tier-1 roster (:665-697); demo/palettes/mix.ts:110-145 (**`resultLength = discard ? minLen : maxLen`; loop `i < resultLength`** — the beyond-minLen asymmetry is real); test/mix-v4.test.ts:3-4 (test/→demo inversion); eslint.config.js (**every substantive rule `off` for .ts/.vue/.js; `no-restricted-imports` globs never select `demo/workbenches/**`; dead `demo/@` globs**); `grep -c computed demo/workbenches/mix/MixPane.vue` = **1** (the import — unused, lint-green); censuses re-run: `mixStage|useMixingAnimation|MixAnimationCanvas` in test/+e2e/ = **0**, `aria-live|role="status"|role="alert"` in the mix tree = **0**, `onDeactivated` in demo/ = **0**, `useIntersectionPause|useReducedMotion|readReducedMotion` in demo/ = **0**; **`git diff --stat c654824e HEAD -- demo/workbenches/mix/` empty; last commit touching the tree = `f2c8f565` (the glass-ui 7.0.0 adoption)**; usePaneRouter.ts:220-222 (**dock `Clear`/`Mix`/`Copy` handlers over MixPane's `defineExpose`**); `reset()` at useMixingState.ts:108-111 **unguarded** (a live `mixing → idle` edge); OPTICAL-BENCH-COMPOSITIONS.md:108 **P051 verbatim re-read** ("WatercolorDot owns only its noninteractive organic face/edge and removes its button/tag branch in the clean major … Operable sites use named geometric seats; ornamental faces are aria-hidden") + :43 P122 Mix trough; motion-quarantine.md subject set (:16 — the 46 REPORT-AUTHORED rows; this component's files are not in it) + M-26 (:117 — different component, CSS limb); **X wave bounds re-read W0/W1/W4/W6/W7/W8 AND W10/W11**: X-W6.j files = MixPane + MixConfigBar + MixSourceSelector + MixAnimationCanvas.vue + useMixingAnimation.ts (gate j2 names the `:91/:144/:162` getContext sites); X.W4.g trigger-gated (CLOSED unless X-W0.j census PASS) holds MixResultDisplay **"modify-carve — import + face only"**; X-W1 restores `e2e-smoke`/`e2e-safari` as HARD CI jobs (CC-031); W10/W11 hold **no mix surface** (W11:277 is a route enumeration); **no X wave's bounds hold `mixStage.ts`, `useMixingState.ts`, `MixResultDisplay.vue` beyond the face carve, `ErrorBoundary.vue`, `demo/palettes/mix.ts`, or the two RED mix e2e specs.**

## RULING PINS (this seat's own derivations)

1. **The BLOCKER's mechanism, its phase machine, and its only honest cure surface all live in files no X wave owns** — re-verified at HEAD through every wave file W0-W11. The masking fallback (mixStage.ts:121-124), the throwing ramp (:99-107), the one-exit-forward phase machine (useMixingState.ts:104-106), and the anchor's Transition housing (MixResultDisplay.vue:60-76, touchable only as the trigger-gated face carve) sit outside every bounds table, while every neighboring file is X-W6.j's. The **MX-CLUSTER** NO-WAVE-OWNER booking stands.
2. **P051 kills every producer-facing cure in the corpus** (carried kill, canon re-read this session). The WatercolorDot attribute/interactivity drop is ratified canon, not a regression to relay: operable sites migrate to named geometric seats; the BH relay may inform, never ask to restore forwarding or mint an `as`/`tag` prop.
3. **The re-mix anchor absence is framework-certain and kills all three corpus cures.** Both current readers independently found it (the joint largest miss of the axes): MixResultDisplay.vue:60 `mode="out-in"` + differently-keyed `well`/`content` branches means every `done → mixing` re-mix unmounts the outgoing content, waits ~0.2 s of `.vj-morph-leave-active`, and only then mounts the well — while useMixingAnimation.ts:171-183 measures at `flush:"post"` of the phase tick. First mix exempt (no outgoing element). The docstring at :169-170 is false for every mix after the first; attribute-forwarding fixes, L's anchor registry, and D's prop/useTemplateRef anchor all register on MOUNT and all still miss. Constraint ruled: the anchor lives OUTSIDE the swapping Transition (e.g. the persistent `.mix-plate` root) or the measurement stops being a single post-flush instant.
4. **The strand family has exactly one silent member, and even it is not reload-only.** The throw path (MX-4) is loud-but-console-invisible: Vue routes it to the grid-wide ErrorBoundary — announced, focus-managed, recoverable. The `getContext("2d") === null` path (MX-5) strands silently with a live-looking UI — but this seat's own reads cut the "permanent / reload-only" framing: `reset()` (useMixingState.ts:108-111) is UNGUARDED and the dock's Clear command (usePaneRouter.ts:220) reaches `clearSelection() → reset()` from any phase. The visible Reset control, however, is hidden during `mixing` (it lives in the plate's non-ghost branch, MixResultDisplay.vue:120-143), so recovery exists but nothing points at it. MAJOR, not BLOCKER.
5. **The arrival "chord" is continuous per drop and discontinuous in composite — both readers are half right.** This seat's arithmetic on mixStage.ts:216-259: at `elapsed → MIX_ARRIVE_MS⁻` each drop's head sits at ≈(tx,ty), radius ≈ tr, alpha 0.92 — position/radius hand into `drawPool`'s first frame seamlessly (return-2's superlative holds per drop). But N near-coincident 0.92-alpha soft discs composite to `1−0.08^N` (≈0.9995 at N=3) with an N-fold steepened edge profile, and the next frame paints ONE disc at 0.95 + halo 0.171. For N ≥ 2 — every reachable mix — the composite alpha and edge softness step discontinuously, scaling with operand count. Return-1's missed finding is CONFIRMED as mechanism (MINOR; perceptual magnitude live-only); return-2's superlative is RESCOPED to its per-drop form.
6. **The house duration ladder refutes return-1's carried multiplier.** scheme-motion.css ships `--duration-xl 1s` and `--duration-xxl 1.5s` (this seat's read): 1200 ms sits BETWEEN them, not "2.7× the slowest house duration". Return-2's kill is UPHELD; return-1 CONFIRMED its D-D5/L-9 row while repeating D's E-7 stale cell ("vs `--duration-slow .45s`") — a citation-inheritance event recorded against the fleet, in the exact class the ColorSpaceSelector DISSENT documented.

## Scoreboard

**35 adjudicated defect rows: 1 BLOCKER · 11 MAJOR · 13 MINOR · 10 INFO** — every confirmed row of both returns survives re-derivation (severity conflicts ruled below); **11 reader-found misses admitted** (all re-walked in source; one escalates a sibling row's severity, one is cross-attributed to the wb-mix-resultdisplay seat); **4 rows carried from the superseded draft's reader round**, each re-verified at HEAD. **12 killed claims** with written refutations. Headline unchanged and sharpened: **a convergence whose target cannot exist in the shipped DOM — and is structurally absent at measure time on every RE-mix even if it could — lands ~194 px into empty acreage behind a masking fallback, on a route whose colors arm is dead, gated by two structurally unsatisfiable specs that no CI job runs; the mechanism, the phase machine, and the honest cure surface have NO owning wave in tranche X.**

## Ruled disagreements (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **D-D2 severity** | return-1: BLOCKER ("permanently and SILENTLY"); return-2: MAJOR; D axis: BLOCKER | **MAJOR** | PIN 4: `reset()` unguarded + dock Clear (usePaneRouter.ts:220) = in-app recovery, so "permanent" is struck; silence + the hidden Reset + the inert-but-enabled Mix verb survive; firing rate unproven (R-6). Revival clause: a live witness of null-ctx in a shipped engine revives BLOCKER weight. |
| **arrival seam** | return-1 missed-5: "pops, grows with N"; return-2 superlative-1: "lands without a seam, C0/C1 by construction" | **return-1 CONFIRMED (MINOR); return-2 RESCOPED** | PIN 5 — this seat's compositing arithmetic; the two claims were about different quantities (per-drop kinematics vs composite alpha/edge). |
| **KeepAlive row severity** | return-1: MAJOR; return-2: MINOR; superseded draft: MINOR ("bounded ≤1.2 s waste") | **MAJOR (escalated)** | Return-1's missed smear mechanism, re-derived here: while deactivated, `clientWidth/clientHeight` = 0 so BOTH clears (:99, :110) degenerate to `clearRect(0,0,0,0)` no-ops while `drawStage` keeps compositing into the still-full-size backing store; the terminal clear is also a no-op; the accumulated near-opaque composite survives in the bitmap and is repainted at z-20 on reactivation until reset/next mix. Not bounded waste — a persistent visible artifact (visibility live-only, R-9). |
| **colors-arm death grading** | return-2: BLOCKER (C-D-3-addslot); return-1: MAJOR-fold + kill of C's "whole workbench dead" | **BLOCKER-weight at the wb-mix-sourceselector seat; recorded MAJOR here inside MX-3** | Both add paths in colors mode are producer faces (MixSourceSelector.vue:164-176 AND the palette-swatch adds :211-221 — both `WatercolorDot tag="button"`, this seat's read), and colors is the DEFAULT mode (useMixingState.ts:41) — the pane lands on a dead arm. The palettes arm is native and real (:246-268). Attribution follows the surface's owner. |
| **L-6 cure replacement** | return-1's kill prescribes `phase === 'mixing'` + teardown; return-2's kill shows that form breaks the 300 ms epilogue | **return-2's cure wins** | `onSettled` fires at MIX_CONVERGE_MS flipping phase to `done` while the loop draws the dissolve for MIX_EPILOGUE_MS more (:103-111) — a phase-keyed unmount amputates the epilogue. Sound cure: zero `canvas.width/height` at the loop's terminal frame. Return-1's replacement half-repeats the defect class it kills — the cure-adjudicated-separately law (ColorSpaceSelector addendum item 2) applies. |
| **D's clock-inversion cure** | return-1: KILLED (feature-deletion in disguise); return-2: silent; superseded draft: listed as a live cure candidate | **KILLED as stated — return-1 adopted; draft revised** | If `startMix` advances straight to `done`, `mixing` is zero-width: MixPane.vue:115's `:ghost` is never true, the ghost-well morph and the narration window become dead code, and restoring a window needs a clock the ONE-CLOCK law forbids the state machine from owning. The surviving narrow form: keep the edge, make it unconditional (a `finally`-shaped discharge / timeout-of-last-resort). |
| **C-D-1c undefined-css sub-route** | return-1: re-asserts it; superseded draft: killed it on `PaletteColor.css: string` | **kill UPHELD; return-1 corrected** | The limb presumes a type-violating payload without evidence one exists. The route that STANDS (return-1's real contribution, verified against mix.ts:117-131): `resultLength` never touches indices ≥ minLen under `discard`, while the narration parses `slice(0,4)` of EVERY palette — a beyond-minLen unparseable entry passes the math and throws in the narration. MX-4 is non-synthetic on that route alone. |
| **reader anchor errata** | — | corrected in place | Return-2's corrections adopted (`:ghost` at MixPane.vue:**115**; L's export re-export at :**49**); return-1's L-7 count correction adopted (ONE `onActivated` in HeroBlob — :27 import + :246 call; `onDeactivated` = 0 repo-wide, re-run). L's systematic 2-5-line drift on an unchanged tree corroborates its UNWITNESSED-DIRECT status (K-12). |

## Adjudicated defect roster

Line anchors are HEAD `54a97489`; re-resolve before executing any cure (the D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: a wave id means that wave's bounds own the surface today; **NO-WAVE-OWNER** rows are booked for the next formation boundary. **MX-CLUSTER** = mixStage.ts · useMixingState.ts · MixResultDisplay.vue-beyond-face · demo/palettes/mix.ts · the two RED mix specs — the NO-WAVE-OWNER booking this record establishes (PIN 1).

### BLOCKER

- **MX-1 — L-1 ≡ C-D2 ≡ D-D1: `[data-mix-target]` cannot render; every convergence lands on a fabricated target ~194 px from the well, behind a masking fallback.** Writer: MixResultDisplay.vue:69 stamps the bare attribute on a `<WatercolorDot>`; producer: glass-ui 7.0.0 `watercolor-dot.js` `inheritAttrs:!1`, forwards only `class`/`style` by hand, no `<slot/>`, no `tag` prop (this seat's node probe — fourth independent verification); reader: mixStage.ts:121-124 falls to `{x: clientWidth/2, y: scrollHeight*0.7, r: 28}` — the masking-fallback class the standing edicts ban. Fires on 100 % of mixes, proven statically end-to-end; both axes' live misses agree to 1 px (192.9/194 — magnitudes R-1). Scope: **since glass-ui 7.0.0** (`f2c8f565`, the last commit touching the tree; K-5). Cure road ruled (PINs 2+3): consumer-owned anchor OUTSIDE the out-in Transition + delete the fallback in favor of the honest-settle branch; NEVER a producer-forwarding ask. → mechanism + fallback: **MX-CLUSTER, NO-WAVE-OWNER**; face/seat migration: **X.W4.g** (trigger-gated; if never fired → MX-CLUSTER); canvas-side honest-settle: **X-W6.j**.

### MAJOR

- **MX-2 — both readers' shared missed finding, ruled ADMITTED (PIN 3): on every `done → mixing` re-mix the anchor is structurally absent at measure time — independently of the attribute drop, and fatal to all three corpus cures.** `mode="out-in"` (MixResultDisplay.vue:60) + `.vj-morph-leave-active` = `--duration-fast` (animations.css:111-117) defer the well mount ~0.2 s past the `flush:"post"` measurement (useMixingAnimation.ts:171-183); the :169-170 docstring is false for every mix after the first; C's negative proof #7 and D's state-table "re-arms correctly" are both wrong (K-3). `parent.scrollHeight` (:139) is also read mid-leave, so even the fabricated fallback drifts between first and later mixes (R-8). → anchor restructuring: **MX-CLUSTER, NO-WAVE-OWNER** (the X.W4.g carve is "import + face only" and cannot restructure the Transition).
- **MX-3 — C-D3 ≡ L-1.3 + return-2's missed CI finding: both e2e specs gating this component are structurally unsatisfiable, and NO CI job runs playwright at all.** mix.spec.ts:39-42 + mix-flow.spec.ts:30-33 role-query an add-slot the producer renders as `<span aria-hidden pointer-events:none>` with no name and no listener host; both then assert `[data-mix-target]` visible (:52/:40) — unmatchable per MX-1. `.github/workflows/` carries zero playwright invocations (this seat's grep; `test:e2e` is orphaned) and playwright.config.ts:33 cites a workflow file that does not exist — so C's "the only gate is RED" understates: it is not a gate at all. Colors-arm reachability recorded here with cross-attribution (ruling table): both colors-mode add paths are dead producer faces; the pane's DEFAULT mode lands dead; only the palettes arm (native buttons, :246-268) reaches this component. Unit coverage zero (grep re-run). → spec repair: **MX-CLUSTER, NO-WAVE-OWNER**; CI e2e gating: **X-W1** (CC-031 restores e2e-smoke/e2e-safari HARD); the add-slot/swatch seats: the **wb-mix-sourceselector** seat's roster + **X.W4.g**.
- **MX-4 — C-D1 ≡ L-3 (mechanism): `arm()` is not total — four throwing sites inside `collectStage` bypass all four honest-settle branches.** parseColorIn ×2 (mixStage.ts:99-100 → picker-color.ts:109-113), the `!result.ok` throw (:106), colorToRgb255 (:107 → color-utils.ts:16-20); `pigmentRamp` called at :177 inside `picked.map`, OUTSIDE the JSON-parse try/catch (:139-153); the watcher calls `collectStage` bare (:147), bypassing :120-125/:129-133/:148-154. Non-synthetic route CONFIRMED (the beyond-minLen asymmetry, ruling table; mix.ts:117-131 re-read); the undefined-css limb killed (K-11). Consequence corrected by the upheld kill of C's blast narrative (K-1): the pane dies to an ANNOUNCED, recoverable, grid-wide ErrorBoundary with zero console output. Cure = totality (`Stage | null` for every failure mode — the null branch already exists at :148-154). → mixStage/adapters: **MX-CLUSTER, NO-WAVE-OWNER**; watcher seam: **X-W6.j**.
- **MX-5 — D-D2: `getContext("2d") === null` is the one abort path that never discharges the forward edge — the family's single genuinely silent strand.** :144-145 half-guard (`if (ctx)` guards only the transform), then `collectStage` + `loop.start()`; frame 1 hits :92-95 `loop.stop()` with NO `onSettled()`; `settleMix` is the only forward exit and `startMix` is inert during `mixing` (:83). Nothing throws → no boundary. C's negative proof #7 declared this area clean and is wrong. Severity + recovery ruled at PIN 4 (dock Clear reaches `reset()`; the visible Reset is hidden in the ghost branch; the page's ONE verb sits enabled-but-dead). Firing rate unproven (R-6); D's BLOCKER dissent preserved with the revival clause. Cure: discharge on the null-ctx branch (one line beside :145); the clock-inversion alternative is KILLED as stated (K-7). → guard: **X-W6.j**.
- **MX-6 — L-4 ≡ D-D6: pigment travels source→canvas as JSON in DOM attributes while the typed truth sits one prop away, and the operand budget is enforced three times in disagreement.** Writer caps 4 (MixSourceSelector.vue:253-255 `slice(0,4)`), reader caps 12 (mixStage.ts:29/:157), the mix math caps at neither (mix.ts:121-143 iterates `resultLength`) — a 2×8 mix narrates 8 of 16 pigments, silently. `useMixingState` returns `selectedColors`/`selectedPalettes` and MixPane already passes four props into this very component (:67-73). → writer: **X-W6.j / X-W7**; reader + one-home budget: **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-7 — L-5 + return-1's missed blast-radius finding: pane crashes are structurally console-invisible, and the one boundary is GRID-wide.** ErrorBoundary.vue:59-69 `return false` with no `console.error`/`reportError`/telemetry — the audit corpus's `consoleErrors: []` for `/#/mix` cannot be read as health. Admitted sharpening (verified App.vue:47-50/:140): the boundary opens INSIDE `<main>` but ABOVE both PaneSlots — a `collectStage` throw replaces the ENTIRE pane grid, color picker included, while the copy says "This PANEL hit an unexpected error", mis-describing its own blast radius. One-line observability cure before `return false`; scope cure = per-pane boundaries or honest copy. → **NO-WAVE-OWNER** (ErrorBoundary.vue in no X bounds).
- **MX-8 — C-D4: the settle event is silent to assistive technology.** Zero `aria-live`/`role="status"`/`role="alert"` in the mix tree (grep re-run); during `mixing` the plate holds the literal "Result" + a double-`aria-hidden` ghost; the canvas is correctly out of the AT tree; MixPane.vue:107-110 states the premise ("the animation IS the progress") that leaves NO non-visual channel for progress or completion. `onSettled` is the seam. → **X-W6.j**.
- **MX-18 (carried, re-verified): `convergeCss()` duplicates `wellColor` with divergent, non-interchangeable fallbacks.** useMixingAnimation.ts:75-82 (`null` → honest settle) vs MixResultDisplay.vue:36-40 (`var(--muted-foreground)`) — identical selection logic, opposite sides of the DOM-bus boundary, and the display's fallback would detonate MX-4's throw path if it ever crossed (`parseColorIn` cannot parse a `var()`). Single home = the `MixResult` model. → **MX-CLUSTER, NO-WAVE-OWNER** + **X-W6.j** (convergeCss side).
- **MX-19 (carried, re-verified): the Mix verb is not gated on the narration window — on any strand path it is a permanently dead, permanently enabled primary action.** MixConfigBar's Button carries `:disabled="!canMix"` only (:162-169, this seat's read); the silent discard is useMixingState.ts:83; the dock mirror at usePaneRouter.ts:221. Healthy path ≤1.2 s of inert-but-enabled; strand paths (MX-5, armed-while-hidden) leave the page's ONE verb dead with no feedback, and the only recovery affordance (dock Clear, :220) is nothing the pane points at. → **X-W6.j / X-W7**.
- **MX-31 — return-1's missed smear finding, ADMITTED and ESCALATING the KeepAlive row (ruling table): no deactivation gate + no-op clears = a persistent pigment smear, not bounded waste.** PaneSlot.vue:120 `<KeepAlive>`; the only teardown is `onBeforeUnmount` (:187); `onDeactivated` = 0 repo-wide (grep re-run); the producer loop gates on document visibility only; `useIntersectionPause` ships unused. Detached mid-narration, `clientWidth/clientHeight` = 0 → :99 and :110 both `clearRect(0,0,0,0)` no-ops while `drawStage` composites into the retained full-size backing store; the accumulated composite survives reactivation at z-20 until reset or the next arm's `canvas.width` assignment. Mechanism is a pure code fact; on-screen appearance R-9. VC:145 ("Paused, parked and offscreen mean no animation work") is the standing law. → **X-W6.j** (gate j2 is literally this contract).
- **MX-32 — return-1's missed a11y finding, ADMITTED with cross-attribution: the palette mix result — the only arm a real user can reach — has NO text alternative.** MixResultDisplay.vue:91-116: N producer-`aria-hidden` spans (both `tag="div"` and `:title` dropped per the producer bytes) + an explicitly `aria-hidden` gradient strip; contrast the single-color branch's visible css text (:78-89). A screen-reader or tooltip user who mixes palettes gets the word "Result" and the Copy control. Surface owner = the **wb-mix-resultdisplay** seat; recorded here because it decides whether this component's narration has anything true to land on. → plate content: **MX-CLUSTER / X.W4.g face carve** (insufficient alone — the carve is import+face only).

### MINOR

- **MX-9 — L-2 ≡ C-D6: PRM re-derived through `useBreakpoint` from a second glass-ui subpath while `motion-core` — already imported on the next line — owns the concept.** :41-42 dual subpath; :70-72 hand-written query; `useReducedMotion`/`readReducedMotion` unconsumed repo-wide (grep re-run). The ref IS live (consumption gap, not staleness — return-1's precision adopted). → **X-W6.j** + repo-wide one-home rider: **NO-WAVE-OWNER**.
- **MX-10 — L-2c ≡ D-D9: PRM consumed once at arm; reduce engaged mid-narration is ignored.** :120 sole read; `respectReducedMotion: false` makes the host's PRM ref a constant. The posture is right (superlative 3); the missing 3-line watcher (stop + settle on PRM-flip) is the correct expression. Return-2 grades the limb INFO (1.2 s window) — dissent preserved. → **X-W6.j**.
- **MX-12 — L-6 ≡ C-D9: backing store sized to the pane's scroll extent and never released; the file's two clears disagree on coordinate space.** :137-143 sizing (dpr ≤ 2); `clearCanvas` (:160-167) clears `canvas.width/height` DEVICE px through the still-armed dpr transform (:145 never reset) while the loop clears CSS px (:97-99); `style.height` reset but the attributes retained; runs only on explicit idle. Cure ruled: terminal-frame zeroing (ruling table; both corpus v-if cures + return-1's half-cure corrected). → **X-W6.j**.
- **MX-13 — L-9 ≡ D-D5a: the motion timeline is three private constants outside the producer's duration/tempo register.** mixStage.ts:22-26; the CSS PRM neutralisers provably cannot reach JS constants — which is exactly why MX-10's hand gate must exist. The "2.7×" multiplier is DEAD (K-8, PIN 6); return-2's caveat verified: `--motion-tempo` is scoped to `:where(.glass-drawer,.liquid-stage)`, so a naive token read on this canvas resolves to nothing as the cascade stands. → constants: **MX-CLUSTER, NO-WAVE-OWNER**; arm-time token read rider: **X-W6.j**.
- **MX-14 — L-10 ≡ C-D3b: zero tests bind these 507 lines; the duplicate twin is covered through a test/→demo inversion.** Greps re-run = 0; test/mix-v4.test.ts:3-4 imports the application from the library suite; `npm test` = vitest only. → **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-15 — L-3b: "mix or throw" implemented twice with two error strings; the demo adapter erases the library's Result channel at the first boundary.** mix.ts:28-37 vs mixStage.ts:102-107; erasure at picker-color.ts:104-113 + color-utils.ts:16-20. → **MX-CLUSTER, NO-WAVE-OWNER**.
- **MX-16 — D-D8: geometry sampled once at arm; resize/zoom/reflow mid-narration unhandled.** No ResizeObserver/resize listener in the tree (whole-file reads + grep). → **X-W6.j**.
- **MX-17 — D-D10: the transient decorative narration inherits the colour-DISPLAY forced-colors exemption.** Bare `canvas,` opens the tier-1 `forced-color-adjust: none` roster (foundation.css:680) under a rationale scoped to "surfaces whose whole PURPOSE is to show a color"; in WHCM the overlay paints author pigment at up to α 0.95 across the pane at z-20. The roster needs a named class, not a blanket leaf rule. → roster cure: **NO-WAVE-OWNER** (the forced-colors block exceeds every carve); witness: **X-W1**.
- **MX-20 (carried identity rider): the tsconfig.demo.json path map drifts both directions; the demo typecheck is blind to checkout-only `/css` changes — the parser tranche's own surface.** Identity = ColorSpaceSelector L-14 ≡ XP-21 ≡ EC/EY L-21 — one row, already banked NO-WAVE-OWNER (X-W8 declined, W8.md:112); **not re-booked**; the vue-tsc-resolves-stale-tarball limb rides as a sharpening + **X-W9 coordination note**.
- **MX-33 — return-1's missed ratchet finding, ADMITTED: the canvas inflates the very `scrollHeight` it measures.** :137-140 reads `parent.scrollHeight` and writes it back as the canvas's own inline height; the absolutely-positioned canvas then holds the Card's scroll extent up, so if pane content shrinks, `scrollHeight` stays pinned at the stale value, which the next `arm()` re-reads and re-applies — monotonic, never shrinking, and the fabricated fallback's `scrollHeight * 0.7` (MX-1) drifts off a number the canvas itself sustains. `clearCanvas` releases `style.height` only on explicit reset. → **X-W6.j** (the sizing lives in the owned file).
- **MX-34 — return-1's missed compositing finding, ADMITTED as ruled (PIN 5): the arrival hand-off steps discontinuously in composite alpha and edge profile, scaling with operand count** — worst at the 12-drop cap nobody benched, against a docstring promising "a chord, not a queue … the merged pool swells" (:10-12). Per-drop kinematics genuinely continuous (rescoped superlative 9). Perceptual magnitude R-10. → **MX-CLUSTER, NO-WAVE-OWNER** (drawStage's).
- **MX-35 — return-2's missed lint finding, ADMITTED: the "hard lint" the corpus treats as a green gate enforces essentially nothing.** Every substantive rule `off` for .ts/.vue/.js (no recommended spread); the only live rule is `no-restricted-imports` on `src/**`; two of three restricted-import blocks glob the deleted `demo/@` tree; live in-tree witness: MixPane.vue:2's unused `computed` import passes `--max-warnings=0` (this seat's grep = 1 hit, the import). Identity overlap: the dead-glob half ≡ ColorSpaceSelector L-5(a), banked; the rules-off-everywhere half is NEW and larger. → lint-lattice fold: **X-W0/X-W1 banked identity** + the rules-posture limb **NO-WAVE-OWNER**.
- **MX-36 — return-2's missed redundancy finding, ADMITTED: `collectStage` re-parses the invariant pool pigment once per drop (≤12 redundant parses) and recovers the pool color as a ramp endpoint instead of the parse it already has** (:99-100 via :177; :186 `drops[0]!.ramp[RAMP_STOPS-1]`). Hoisting shrinks MX-4's unguarded throw surface from ~24 `parseColorIn` calls to 13. → **MX-CLUSTER rider**.

### INFO

- **MX-21 — C-D7 + return-2's missed getContext finding: per-frame forced layout reads of arm-invariant `clientWidth/clientHeight` (:97-98) AND a per-frame `getContext("2d")` re-acquisition (:91) for a context already obtained at :144.** All belong on the Stage or in an arm-captured closure. → **X-W6.j rider**.
- **MX-22 — D-D12: a fresh `CanvasGradient` per disc per frame** (mixStage.ts:199; ≤38/frame at cap). Not a present bottleneck. → rider.
- **MX-23 — L-8 ≡ C-D8: dead public surface — the consumer-less constants re-export (:49; sole external reference is a prose comment re-typing `900` at mix.spec.ts:15) and the discarded `{ stop }` return (:189 / MixAnimationCanvas.vue:20).** → **X-W6.j rider**.
- **MX-24 — L-11: a pure, Vue-free model shelved inside `composables/`** (its own docblock: "No lifecycle, no clock"; non-`use*` census = 4 repo-wide). → **MX-CLUSTER rider**.
- **MX-25 — L-12: no import-boundary rule reaches this file; dead `demo/@` globs.** Identity ≡ ColorSpaceSelector L-5(a) → the lint-glob-liveness fold — cross-referenced, not re-booked (MX-35 carries the new limb).
- **MX-26 — L-13: published-surface facts — 7-key exports (no `.`, no `require`), `dist/value.js` + `dist/index.d.ts` absent while load-bearing comments assert them.** Same disease as playwright.config.ts:33's phantom workflow (MX-3). Identity overlaps MX-20; rides W-HYGIENE. → **NO-WAVE-OWNER (banked identity)**.
- **MX-27 — D-D13 + return-2's sharpening: `/#/mix` is absent from every state matrix — and the gap is one matrix LARGER than D stated** (keyboard-focus-desktop also holds only the 5-route set; six matrices, no mix.png outside the four safari at-rest cells). → **X-W1** (owns the witness matrices).
- **MX-28 — D-D4: built against the composition the ratified canon superseded** (P122 golden trough; Card-as-page-primitive). INFO as a forward conformance gap — the recomposition IS X-W6.j's programmed work; booking a programmed adoption as MAJOR double-counts it (D's dissent preserved). → **X-W6.j** (+ X-W5 lineage).
- **MX-29 (carried, re-verified): `arm()`'s PRM and no-canvas early returns leave the previous arm's `Stage` installed** (:120-125/:129-133 never null module-scope `stage`; only the idle branch does). Invariant held by accident (`loop.stop()` precedes); one line makes it structural. → **X-W6.j rider**.
- **MX-37 — return-1's missed prose finding, ADMITTED: the `data-mix-*` contract exists ONLY as prose that is now false and reads as specification.** useMixingAnimation.ts:5-13 + MixResultDisplay.vue:12-17 — no type, no prop, no test, no lint rule; both false since glass-ui 7.0.0; the second authored by the component whose stamp is dropped; two audit seats quoted the docstring as thesis before testing it. (The DESIGN.md:302 `z-[var(--z-controls)]` vs `z-controls` half is drift, not a defect — both resolve to 20.) → dies with MX-1/MX-2's cure: **MX-CLUSTER rider**.

## Killed-claims register

Nine reader kills UPHELD on re-derivation; two kills carried from the superseded draft re-verified; one reader assertion killed by this seat.

- **K-1 — C-D1's blast narrative ("no result, no error, no Reset, dead forever, only reload recovers") — UPHELD (both readers).** A `flush:"post"` watcher throw routes via `handleError` to ErrorBoundary.vue:59-69: `role="alert"` fallback, focus-managed, with a live `Try again` whose reset remounts the grid with FRESH `useMixingState`. C's `plate: null` / `phaseAfter2500ms: "mixing"` readings are a boundary takeover read off a destroyed instance's captured setupState. The console-silence limb is real and is MX-7's; the grid-wide scope is the admitted sharpening.
- **K-2 — C-D3's "the Mix workbench cannot be used at all / MixAnimationCanvas is unreachable dead code" — UPHELD (both readers).** The palettes arm is a native `<button type="button" aria-pressed @click>` (MixSourceSelector.vue:246-268, re-read) with real `data-mix-*` stamps; the dock's Mix command is real. The COLORS arm is fully dead (both add paths, :164-176 + :211-221). C's own sibling axis (D-7) states the truth C's generalisation contradicts.
- **K-3 — C's negative proof #7 + D's "re-arms correctly" state row — KILLED by the admitted MX-2.** Both axes asserted the exact area their largest miss lives in was clean; neither probe could distinguish the re-mix null from the attribute-drop null. Framework mechanism at PIN 3.
- **K-4 — L-1's anchor-registry cure (MixAnchorPort, ~40 lines) — UPHELD as killed (return-2), on THREE grounds now**: disproportionate under the KISS edict (the plate's own wrapper div is consumer-owned, one attribute-move away); structurally insufficient (the wrapper sits INSIDE the out-in Transition — MX-2 defeats it on every re-mix); and under-specified against P051 (any cure keyed on a producer face is anchored to canon-retired semantics).
- **K-5 — L-1's "has NEVER existed in the shipped DOM" — UPHELD (both readers), with the canon receipt carried and re-read**: P051 removes the button/tag branch "in the clean major" — the branch existed pre-7, so the anchor plausibly rendered under glass ≤6, exactly as D dated it (W44/D58). "Never" is struck to "since glass-ui 7.0.0". A ≤6-dist witness remains R-7.
- **K-6 — L-6's `v-if="phase !== 'idle'"` cure — UPHELD (both readers), both directions**: `done` satisfies `phase !== 'idle'` for the whole KeepAlive-cached tail (releases nothing the reset path didn't already release), and the tighter `phase === 'mixing'` form amputates the 300 ms epilogue (ruling table). Sound cure: zero the backing store at the loop's terminal frame. Return-1's replacement half-cure corrected in the same ruling.
- **K-7 — D's gestalt cure step 2 ("invert the clock; delete `onSettled` as a state edge") — KILLED as stated (return-1's kill adopted; the superseded draft's live-candidate listing REVISED).** A zero-width `mixing` deletes the ghost morph, the narration window, and the announced destination — a subtraction argument wearing an inversion's clothes. Surviving form: the edge stays, made unconditional.
- **K-8 — D-D5's "1200 ms is 2.7× the slowest house duration" — KILLED (return-2; arbiter ladder re-read, PIN 6).** `--duration-xl 1s` / `--duration-xxl 1.5s` ship; 1200 ms sits between them. Return-1's repetition of the stale cell recorded as citation-inheritance.
- **K-9 — D-D11's "MixAnimationCanvas.vue is a KISS-banned contrivance" — UPHELD as killed (return-2).** The 35 lines own the element + its a11y posture, the `settled` emit (the ONE-CLOCK seam), and the props→Ref adaptation; collapsing it into MixPane is the anti-KISS direction; D's own negative-proof section praises the same split it indicts.
- **K-10 — the corpus's producer-relay cure limb ("restore `data-*`/`aria-*`/click forwarding, or a first-class `as`/`tag` prop") — KILLED (carried arbiter kill; P051 re-read verbatim this session).** The interactivity drop is ratified canon; the BH relay may carry an informational note on the break's blast radius, never a restore-forwarding ask. Neither current reader challenged this framing either — recorded again as a fleet observation.
- **K-11 — C-D1c's `css: undefined → JSON.stringify → "null" → .trim()` sub-route — UPHELD as killed (carried; types.ts `css: string` required), and return-1's re-assertion corrected.** The route that stands instead is the beyond-minLen unparseable-string asymmetry (ruling table), which return-1 proved and which makes MX-4 non-synthetic without any type violation.
- **K-12 — L's citations as dated evidence — provenance kill SUSTAINED (both readers).** UNWITNESSED-DIRECT (HYDRATION-LEDGER.md:305, re-read); sha intact; systematic 2-5-line drift against an unchanged tree (`git diff c654824e HEAD -- demo/workbenches/mix/` empty, re-run). L's substance survives only where re-proven — as practiced throughout this record.

## Superlatives (provenance preserved)

Items 1, 3, 5, 9, 10 carry this seat's own re-verification; the rest are adopted from the axes + both readers with their receipts.

1. **`layoutCenter`'s offsetParent walk** (mixStage.ts:65-86) — transform-immune AND scroll-immune by frame choice: canvas user-space and offset-space share one origin (absolute inset-0 in the relative, overflow-y-auto Card, sized to scrollHeight), dissolving two whole hazard classes with zero scroll arithmetic. All three axes independently tried to break it and could not.
2. **The `useRAFLoop` adoption**: `pauseWhenHidden`'s `elapsed` excludes paused time (producer bytes verified by return-2 — the cancel path nulls the previous timestamp, first resumed frame contributes delta 0); the retired hand-rolled delta cap really was redundant. A consumer that read its dependency's implementation, not its README.
3. **The reduced-motion posture is the CURE for the repo's PRM-RAF epidemic, not an instance of it** — `respectReducedMotion: false` + complete-immediately is exactly right for a loop owning a state machine's only forward edge; a paused host loop would strand it. Both axes, both readers, and this seat agree; MX-10 is three missing lines, not a wrong posture.
4. **The three-way split is real and load-bearing** — pure model (no vue import, own docblock says so) / clock + lifecycle / 35-line mount. The corollary is what makes MX-14 indefensible: `quadBezier`, `pigmentRamp`, `drawStage` are Node-testable with fake rects today.
5. **The stagger bound is provably safe for every operand count** — `min(60, 240/n)` caps the worst delay at 220 ms against a 700 ms arrival, so :216's denominator can never approach zero. Algebra replacing a guard (re-checked).
6. **Totality as an explicit design goal** — four named abort branches all fire the forward edge, the last commented "settle honestly rather than stall the phase machine." The two holes this record confirms (MX-4, MX-5) are coverage failures inside a correctly-tracked invariant, not failures of understanding.
7. **Safari-true by construction, kept** — radial-gradient discs + source-over only; `ctx.filter` appears exactly twice in the tree, both in the docstrings promising its absence (grep re-run).
8. **The decorative surface's own a11y posture** — `aria-hidden` + `pointer-events-none` + a real producer z-token; zero rows contributed to the route's nameless-control counts. The failure (MX-8) is the missing substitute channel, not a mis-marked canvas.
9. **The drop→pool hand-off is C0/C1-continuous per drop — RESCOPED (PIN 5)**: position and radius hand over seamlessly at p=1 (return-2's arithmetic, verified); the composite alpha/edge discontinuity for N ≥ 2 is MX-34. The narrow superlative survives; the "no seam" generalisation does not.
10. **The tree's docstrings state FALSIFIABLE invariants — and this adjudication is again the proof**: the flush-post rationale (MX-2), the chord promise (MX-34), and the anchor contract (MX-37) were all caught lying precisely because they said something checkable. Code that can be caught lying is better code than code that says nothing.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — the 192.9/194 px landing-miss magnitudes + pool-centroid pixel readings (mechanism statically certain; two axes agree to 1 px).
- **R-2** — L-5's `palette-admin-token` crash reproduction (boundary mechanism confirmed in source; the trigger was probe-planted; no `.replace` on that key exists statically).
- **R-3** — post-deactivation magnitudes: 426 gradient allocations (C) vs 100 frames (D); the 1.33/1.39 MB backing-store figures (arithmetic self-consistent).
- **R-4** — the pasted RED playwright run as a RUN (structurally entailed by MX-3 regardless).
- **R-5** — 0.71 % stage-area share; 510×683 pane dimensions; `armFlushMs 5.7`.
- **R-6** — the shipped-engine firing rate of `getContext("2d") === null` (MX-5's severity ceiling; D's BLOCKER revival clause keys on this).
- **R-7** — whether `[data-mix-target]` rendered under glass-ui ≤6 (canon implies the branch existed; no seat inspected a ≤6 dist).
- **R-8** — the re-mix fallback drift (MX-2's mid-leave `scrollHeight` delta between first and later mixes).
- **R-9** — MX-31's on-screen residue: the retained composite's visibility on reactivation (mechanism certain; rendered appearance, and whether any later paint incidentally clears it, needs a browser).
- **R-10** — MX-34's perceptual magnitude: whether the composite step reads as a pop at N ∈ {2, 3, 8, 12}.

## Motion-law compliance

No `motion-quarantine.md` FALSE row is carried. The quarantine's subject set is the 46 REPORT-AUTHORED files (:16, re-read); this component's three challenge files are EXISTS-ORIGINAL ×2 + UNWITNESSED-DIRECT ×1 (HYDRATION-LEDGER.md:240-241/:305), none in that set. The nearest row, **M-26** (:117), refutes a reduced-motion claim about `WatercolorDot` CSS transitions in a DIFFERENT component's corpus; every PRM row here (MX-9/MX-10/MX-13) is a JS-mechanism claim — precisely the class the quarantine's own architecture finding says the central CSS guards cannot reach. The complete-immediately PRM-negative is adopted, not contradicted.

## DISSENT — preserved, not ruled away

1. **D-axis severity dissents**: D-D2 at BLOCKER (reproduced by injection) and D-D4 at MAJOR — ruled MAJOR/INFO respectively; the MX-5 revival clause stands on R-6.
2. **Return-2's INFO grading of MX-10's mid-flight limb** (an OS-setting flip inside a 1.2 s window) vs the MINOR row — preserved; the row's weight rests on the missing watcher as structure, not on the trigger's frequency.
3. **The reader-vs-reader contradiction record**: return-1's missed compositing finding and return-2's continuity superlative described the same frames and disagreed; both were partially right (PIN 5). Two hostile readers reaching opposite conclusions from the same arithmetic — each stopping at the quantity that suited its thesis — is evidence about single-quantity analysis, not about the tree.
4. **Citation-inheritance events, both directions**: return-1 carried D's stale duration-ladder cell into a CONFIRMED row (K-8); the superseded draft carried D's clock-inversion cure as live (K-7); neither current reader challenged the corpus's producer-relay framing (K-10, carried). Recorded as evidence about the fleet.
5. **Supersession note**: the 2026-08-09 draft at this path adjudicated an earlier reader round whose misses (MX-18/19/29 + the tsconfig rider) do not appear in the current returns; they are carried here only because this seat re-verified each at HEAD. If that draft's reader round ever surfaces as a dated artifact, E-1 twice-audit applies — this file rewrites nothing dated.

---

**ADJUDICATED RED — the corpus's direction confirmed, its consequence claims corrected, every cure it proposed cut down or rescoped.** 35 rows survive (1 BLOCKER · 11 MAJOR · 13 MINOR · 10 INFO); 12 killed claims with written refutations; 11 reader-found misses admitted, one escalating the KeepAlive row to a persistent visible artifact and one exposing the reachable arm's total absence of a text alternative. Terminal dispositions: **13 rows/limbs to X-W6.j** (the Mix canary owns the clock file and every wired neighbor) · **2 to X.W4.g trigger-gated** (falling to MX-CLUSTER if the census never fires) · **3 to X-W1** (CI e2e gating; the witness matrices; the forced-colors witness) · **2 to X-W7 shared** · **4 to banked identities** (tsconfig ≡ L-14/XP-21 · lint lattice ≡ L-5(a) · published surface ≡ W-HYGIENE) · **the MX-CLUSTER to NO-WAVE-OWNER** — mixStage.ts, useMixingState.ts, MixResultDisplay.vue beyond its face carve, ErrorBoundary.vue, demo/palettes/mix.ts, and both RED mix specs. That compound fact — the convergence's masking fallback, its one-exit phase machine, its Transition-housed anchor, and the silent grid-wide boundary that hides all their failures live in files no tranche-X wave owns, while every file around them is owned — remains this apotheosis's headline for the next formation boundary.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
