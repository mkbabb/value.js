claude-fable-5

# kf-App — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject**: keyframes.js demo component **App** (`demo/app/App.vue` + its shell/dock/scene closure) · **Registry tree**: value.js `tranche-u`.
**Subject substrate — THREE layers, adjudicated as such (the single largest fact this seat adds).** The audited tree is `/Users/mkbabb/Programming/keyframes.js`, **252 dirty rows**. Its local HEAD is `8281638c` — the exact commit both challenges pin — and this seat established (git, this session) that `8281638c` is **divergent**: 1 commit ahead of merge-base `a59d3a22` and **41 commits BEHIND `origin/master` (`81a56990`)**. So the corpus audited a stale, locally-forked substrate: (a) **HEAD `8281638c`**: `package.json:71` `"@mkbabb/glass-ui": "6.0.0"` under `optionalDependencies` (:70), lock resolves it (3 entries); (b) **worktree** (what every reader actually read): `optionalDependencies` deleted, version 5.3.5→6.0.0, value.js ^3.1.0→4.0.0, lock glass-ui hits 0; (c) **`origin/master`**: `package.json:77` `"@mkbabb/glass-ui": "7.0.0"` **exact devDependency** — matching the installed producer (`node_modules/@mkbabb/glass-ui` = 7.0.0) that every token/PRM/primitive cell in this corpus was measured against. Layer (c) is the census's SCH-1 falsification of FE F-1, confirmed here by this seat's own git reads. Key-file dirty map (this seat): `App.vue`, `App.skeleton.vue`, `demo/styles/layout.css` **clean** (worktree == HEAD); `EditorShell.vue`, `ChromeDock.vue`, `MbabbMenu.vue` **dirty**. Frontier-durability was probed for every blocker (below) — an instrument **neither reader ran**.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the three challenge files (`audit/kf-components/App/challenge-D-design.md` 39,018 B · `challenge-L-library.md` 59,327 B · `challenge-C-consumption.md` 39,632 B, all dated Aug 3–4): **reader-α** (design-and-usage: 17 confirmations incl. L-01 and 4 superlative re-verifications · 5 kills · 6 misses M-A..M-F) and **reader-β** (library-and-code: 40 confirmation cells · 8 kills · 7 misses · 16 superlative confirmations). Both readers pinned "Tree HEAD 8281638c"; reader-β correctly established the worktree-vs-HEAD distinction; **neither tested the frontier**.
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, and every admitted missed row was re-derived against the bytes (or executed on this seat's own instruments) before adoption. One reader-vs-reader verdict conflict was found and ruled (S-B's a11y third, ruling 2). One kill was **extended** rather than merely sustained (L-02, ruling 1). One killed sub-claim was **sharpened past both readers** (K-5's lacquer, ruling 9). Product source READ-ONLY throughout; zero browser tooling; the only executions were `node` one-liners against no product code (a TypeError-message witness and a WCAG luminance recompute). Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing below is agglomeration input (M-25), not a wave stamp.
**This seat's own instruments (all read-only + node)**: keyframes git archaeology (`log`, `rev-list --left-right` = 1/41, `merge-base`, `show HEAD:package.json`, `show origin/master:{package.json, EditorShell.vue, MbabbMenu.vue, layout.css}`, per-file `status --short`, `diff HEAD origin/master -- demo/` = 60 files/+1379/−905, App.vue frontier diff = comments-only + the DP-02/CC-03 tooltip-provider annotation); `--dock-margin` definition grep at worktree AND `git grep -- '--dock-margin:' origin/master` (**both empty**; only `@property` registrations in demo are `--lit` + `--axis-active`); the K-1 recount (`--dock-*` tokens: glass dist/styles **36** · dist-wide **80** · demo **11**); `MbabbMenu.vue:95-102` worktree read (togglePpMode byte-intact) + `node` execution of the defect shape → **`TypeError: Cannot read properties of undefined (reading 'ppMode')`**; `controlOptionsStore.ts:66-96` (plain-bucket return + the `??=` migration write); `App.skeleton.vue:26-36` (role=status + `aria-busy="true"` + `:aria-label`; sole child `aria-hidden`); `ChromeDock.vue:238-244` (`aria-label="Scene"` over `<SelectValue />`) + `:213-216` (`top: var(--dock-top-anchor)`, no fallback); `App.vue:225-256` (unguarded `currentChannels` at :227 four lines under a comment asserting home yields `undefined`; the guarded `derivedSurfaces` sibling) + `:283-296` (`activeSceneComponent` / `activeSceneKey` — both return `"cube"`-family values for home) ; `useSceneSwap.ts:35-51` (the whole spring watch inside `if (!vtOwnsMotion)`, keyed on `activeSceneKey`); `scenes.ts:104-107` (loader registry) + `:126-134` (`homeScene` omits `component`); `useSceneMachineShellBinding.ts:136-147/:224-236/:254` (the stale `__home__`/`Cube` prose + the `"home"||"cube"` and `"cube"` literals); `EditorShell.vue:88-97` (the one slot without `v-bind`); `CubeScene.vue:259-264` (the dead pt-band comment; `pt-[var(--dock-top-band-reserve)]` grep in EditorStartScreen = 0) + `:283-285`; `EditorStartScreen.vue:88-94/:117-123` (0.45/0.52 seats with `0px`/`100dvh` fallbacks); `CubeTarget.css:108-126` (the lacquer stack verbatim); installed producer reads: `header-ribbon/types.d.ts` (props = placement/ariaLabel/class — **no `mode`**), compiled `header-ribbon.js` markers (`inert: !w.value || void 0`, `"aria-hidden": !w.value`, `pointerType !== "touch" && (S.value = !0)`, `role: "toolbar"`, `onClick: k`, `x.value = !x.value`), `.header-ribbon__anchor` css rule (grid/flex-none/place-items — **no min-block-size, no padding in the rule itself**), `--type-title: 2.058rem` + `--type-weight-title: 700` in `dist/styles/typography/scale.css`; the D-2 grep (`onErrorCaptured|errorComponent|errorHandler|ErrorBoundary|vite:preloadError` over demo/ = **0 hits**); `HOME_SCENE_ID = ` grep (**2 declarations**: `scenes.ts:126`, `sceneMachine.ts:31`); `setPPMode` grep (**exactly 2 lines**: `CubeScene.vue:85`, `:123`); the **WCAG recompute** (node, hsl→linear-RGB→relative-luminance, own code): face-3-over-light-bg L = **0.1011**, hint L = **0.1439**, contrast **1.284:1**; deck-at-.85-over-face-3 **2.337:1**; sheen-corner (0.22 white over the composited face) L = **0.1782** → hint:sheen **1.177:1**.

## Scoreboard

**59 adjudicated roster rows — 5 BLOCKER · 12 MAJOR · 32 MINOR · 10 INFO.** **13 killed-claims register entries, all SUSTAINED** — one extended by this seat with the three-substrate map (L-02), one sharpened past both readers (K-5). **13 missed rows ADMITTED** (reader-α's M-A..M-F; reader-β's seven, of which the ppMode-dead-feature composition merges into KF-APP-1 rather than booking twice). **2 adjudicator corrections against the readers** (ruling 2's S-B cell; ruling 5's M-A internal contradiction) plus one substrate-discipline finding against the whole corpus (ruling 1). **21 superlative cells sustained**, three census-linked (S-5/S-6/S-8).

Blocker-weight survivors of record: **KF-APP-1** (the ppMode toggle throws on every click AND the flag has zero working writers — verified live at HEAD, at the worktree, and **verbatim at `origin/master:MbabbMenu.vue:99-100`**: durable at the frontier), **KF-APP-2** (hero ink prints over the cube's facets at 1.28:1, re-derived to four decimals on this seat's own luminance code — and under the lacquer gradient the contrast **passes through 1.0:1**, a literal invisibility band on the facet), **KF-APP-5** (the header ribbon's three chrome controls are `inert` + `aria-hidden` at rest with no focusable affordance — keyboard/AT lockout source-decided from the installed producer; the frontier deleted the phantom `mode="persistent"` **without curing the lockout**), **KF-APP-3** (zero error paths demo-wide; 5 of 7 routes can hang forever on a shimmering `aria-busy` plate that M-C proves was never announced in the first place), **KF-APP-4** (no SFC typecheck anywhere — the enabling condition for KF-APP-1 and four other rows).

## The incredulity seat's own work — ruled disagreements and corrections

1. **The L-02 kill (phantom glass-ui dependency) — SUSTAINED and EXTENDED; the whole family folds to census SCH-1 (identity guard).** Reader-β killed L-02/C-§4's headline ("npm ci cannot build the app root" at HEAD) by producing `optionalDependencies: { "@mkbabb/glass-ui": "6.0.0" }` at `8281638c`. This seat re-ran every git read and adds the layer neither reader had: `origin/master` — 41 commits ahead — declares glass-ui **7.0.0 as an exact devDependency** (`package.json:77`), exactly matching the installed producer. Consequences ruled here: (i) the challenge headline is false at its stated commit (kill sustained); (ii) reader-β's sharpest inference — an `npm ci` at HEAD would silently **DOWNGRADE** the design system 7.0.0→6.0.0 under every measurement in this corpus — is confirmed and is the real HEAD-scoped defect; (iii) reader-β's "real defect class" (a root-of-graph import declared optional, skipped without error) is **HEAD-only** — cured at the frontier by the exact devDep; (iv) reader-α's standing provenance caveat ("npm ci cannot reconstruct the installed 7.0.0") is true at HEAD and at the worktree, **cured at origin/master**. This row is fact-identical to census **SCH-1**'s falsification of FE **F-1** — ONE identity, already census-owned. **FOLD by reference → KF.W0** (§B-12 substrate reconciliation). Never re-booked here. The corpus-level lesson is booked as a correction: both challenges AND both readers audited a divergent local fork and called it HEAD; the census's substrate-schism row (its own top risk #1) predicted precisely this.
2. **S-B (the loading skeleton) — the one head-on reader-vs-reader conflict. RULED for reader-α; reader-β's cell CORRECTED.** Reader-α killed the a11y third of superlative S-B (K-3) and filed M-C; reader-β confirmed "D S-B … role=status + aria-busy=true + a real aria-label … AT-announced today" whole. At the bytes (this seat, `App.skeleton.vue:26-36`): the region is `role="status" aria-busy="true" :aria-label`, and its **only child is `aria-hidden="true"`**. A `role="status"` live region announces from its **text content** on change; this region's subtree is hidden from the accessibility tree, so there is nothing to announce; ARIA further directs AT not to present live-region changes while `aria-busy` is true, and nothing ever flips it false (`App.vue:96-98` unmounts the fallback). Reader-β's "AT-announced today" clause is **false**; the theme/PRM thirds stand (both readers verified the bridged tokens and the `animation: none` + `background: none` PRM collapse independently). **S-B survives as a two-thirds superlative; M-C is ADMITTED (MINOR); K-3 SUSTAINED.**
3. **The L-01 thrown-message kill — SUSTAINED by execution.** Reader-β killed the L-challenge's observable ("Cannot set properties of undefined (setting 'ppMode')") on ES-semantics grounds (the RHS read of the same dead reference throws first). This seat executed the defect shape (`node`, no product code): **`TypeError: Cannot read properties of undefined (reading 'ppMode')`** — reader-β's string exactly. The BLOCKER stands; the debugger-searchable string in the corpus record is now the correct one.
4. **The K-2 kill (D-4's bounded interval) — SUSTAINED at all three substrates; M-B ADMITTED at MAJOR.** Reader-α killed its own axis's interval arithmetic: `--dock-margin` is not an unmeasured term but an **undefined custom property**. This seat re-verified the premise beyond the worktree: zero definitions in `demo/**`, zero in the installed glass-ui, zero `@property` registration (demo registers only `--lit` and `--axis-active`), and — the frontier check — **zero at `origin/master`** (`git grep -- '--dock-margin:'` empty). So the nine unfallbacked substitution sites (layout.css:80, :92, :117, :135, :161, :166, :204; TransportDock.vue:389, :395) are IACVT-poisoned **at the frontier too**: M-B is durable, D-4's hero seat resolves deterministically to the `0px`/`100dvh` fallbacks (52dvh below lg — re-read at `EditorStartScreen.vue:117-123`), and the challenge's interval prose at challenge-D-design.md:23/:115 stays uncitable.
5. **M-A (the inert header ribbon) — ADMITTED as BLOCKER, RECOMPOSED; one internal contradiction in reader-α's return corrected; producer half relayed.** This seat re-verified both sides: consumer `EditorShell.vue:16` `mode="persistent"` with only `#items` supplied (worktree, dirty file), producer `types.d.ts` = `{ placement?, ariaLabel?, class? }` (no `mode`), compiled gating markers `inert: !w.value || void 0`, `"aria-hidden": !w.value`, touch-skipping hover opener — all present. **Frontier probe (new)**: `origin/master:EditorShell.vue:16` is `<HeaderRibbon placement="right">` — the phantom prop is **already deleted** upstream, and still no `#anchor` is supplied; the phantom-prop evidence cell is substrate-dated (HEAD/worktree only) while the **rest-state lockout mechanism persists at the frontier**. Correction against reader-α: its proof cell asserts the touch target "is 0×0" as source-decided while its own UNPROVEN row says the pill has `min-block-size` + `padding` — this seat's extraction of the actual `.header-ribbon__anchor` rule (`display:grid; flex:none; place-items:center` — no size, no padding **in that rule**) shows the rest-state geometry is genuinely undecidable statically; the compiled `onClick: k` / `x.value = !x.value` pin-toggle means a nonzero-size anchor WOULD give mouse/touch a route. **Ruling**: keyboard + AT lockout is source-decided (inert strips the tab order; `aria-hidden` at rest; the anchor is not a focusable element) → BLOCKER sustained on WCAG 2.1.1 grounds for Share and the dark-mode toggle (the shortcuts modal keeps its independent `?` route, `EditorShell.vue:190`); the touch/mouse arm is SS-13 NEEDS-LIVE. The rest-state affordance question (should a collapsed ribbon with an empty anchor even be representable?) is **producer-owned** → glass-ui BH relay per the standing owner edict, riding with the L-24/D-17 stale "Glass BI 6.0.0" narrative correction.
6. **D-8 severity — RULED MINOR (reader-α); reader-β's MAJOR preserved as dissent.** Reader-α closed both falsifiers (GlassDock emits no nav role; HeaderRibbon's `role="toolbar"` is not a landmark) and argued the loss is orientation, not access (the Select is keyboard-reachable and labeled). This seat adopts MINOR **with the compound noted**: D-5 + D-7 + D-8 + M-C + M-D together leave a screen-reader user with almost no orientation surface anywhere in the app — the cluster routes as ONE spec surface (roster), so the per-row grade does not gate the cure.
7. **C-M8 severity kill — SUSTAINED, convergent.** Three axes, one finding, two MINOR grades and one MAJOR; reader-β's demotion argument (latency inconsistency; no broken state, no lost function) is adopted; the row books once at MINOR (KF-APP-21).
8. **K-1 (the dock-token census arithmetic) — SUSTAINED by this seat's own recount**: 36 (dist/styles) / 80 (dist-wide) / 11 (demo), intersection ∅ against both sets. Neither 65 nor any obvious method reproduces the challenge figure; the conclusion (no collision; the hazard is the inverse — phantom reads) survives and is now triply-derived.
9. **K-5 (the facet-luminance table) — SUSTAINED and SHARPENED past both readers.** This seat re-derived the compositing chain on its own luminance code: plain composited blue facet L = 0.1011, hint ink L = 0.1439, sheen-corner (0.22 white over the facet) L = 0.1782. Reader-α's point stands (the tabulated flat ratios are unciteable; the bright corner is 1.177:1, worse than 1.284:1). The sharpening: the hint's luminance lies **BETWEEN** the facet's plain value and its sheen-corner value, so somewhere along the 145° lacquer gradient the contrast passes through **1.0:1 exactly** — a band of total invisibility ON the facet. Any re-token remediation must be derived against the lacquer stack including this crossing, not the raw tokens (the challenge's :281 remediation is re-cut accordingly).
10. **The L-04-grep and D-17-density kills — SUSTAINED**: this seat's demo-wide error-path grep returns **0 hits of any kind** (reader-β's correction of "prose only" is exact), and reader-β's reproduction of L-24's line arithmetic (113/104/26; 2 declarations in 38 style lines) is adopted over D-17's unreproducible 28%.

**Adjudicator's corrections register (against this corpus's readers)**: (1) reader-β's D-S-B "AT-announced today" cell — corrected at the bytes (ruling 2); (2) reader-α's M-A touch-arm overclaim vs its own UNPROVEN row — reconciled, the arm demoted to NEEDS-LIVE (ruling 5); (3) both readers' substrate anchor — "HEAD 8281638c" is itself a 1-ahead/41-behind local fork, and frontier durability (which this seat ran for every blocker) belongs in any corpus that pins a commit (ruling 1). Per the ColorSpaceSelector DISSENT-1 precedent these are fleet evidence, not tree evidence: both readers' instruments were otherwise re-derived clean, and their one head-on conflict (ruling 2) was exactly where the truth needed a third instrument.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: the census lane taxonomy (CENSUS-2026-08-03 §(a)) — **KF.W0** substrate settle · **KF.W4** K-quartet hygiene/gates · **KF.W6** glass suffusion (demo) incl. the token audit re-cut as a **phantom-token audit** (the D-15 recommendation, ratified here) · **KF.W9** Safari/visual audit (owns ALL SS-13 residue) · **KF.W10** fold discharge. Where no census lane owns a demo-code repair, the row is **NO-WAVE-OWNER — SS-1/SS-2 spec authoring consumes it** (M-25 agglomeration input). Line anchors are worktree/HEAD `8281638c` anchors; **re-resolve at `origin/master` before any cure** (D-19 + the ruling-1 substrate law — the frontier moved 60 demo files).

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **KF-APP-1** | L-01 / C-B1 + β-kill (message) + β-miss 2 (dead feature) | `MbabbMenu.togglePpMode` dereferences `.value` on the plain bucket `controlOptionsStore` returns → **`TypeError: Cannot read properties of undefined (reading 'ppMode')`** on every click (message pinned by this seat's execution); AND the only correctly-formed writer (`CubeScene.vue:85-87 setPPMode`, grep = exactly 2 lines) lives inside the `headerLeft` render fn no host ever renders (KF-APP-17) — so `ppMode` has **zero working writers**: a shipped, persisted feature that is simultaneously crashing and dead | `MbabbMenu.vue:98-101` (byte-identical at worktree, HEAD, **and `origin/master:99-100`** — this seat's frontier probe); `controlOptionsStore.ts:86-96` plain-bucket return; sole mount + super-key supplier `App.vue:20-24` | **NO-WAVE-OWNER** (demo repair; SS-1/SS-2). Cure law from the composition: repairing :100 alone lands TWO writers for one persisted flag — the `setPPMode` twin must be disposed in the same motion as the KF-APP-17 headerLeft sweep. Click-witness → **KF.W9** SS-13 |
| **KF-APP-2** | D-1 + K-5-as-rescoped | The hero ink prints over the cube's saturated facets at **1.28:1** (hint vs blue back-face; deck 2.34:1; three of six facets under the large-text 3:1 floor — `--type-title` 32.9px confirmed), the shadow-stamp/scrim mitigations were deliberately deleted, and the overlap is attested by three live comments as designed; under the `.face-lacquer` gradient the contrast **passes through 1.0:1** (this seat: facet 0.1011 → sheen-corner 0.1782 straddles hint 0.1439) | tokens re-derived on this seat's own luminance code (1.284 / 2.337 / 1.177 — third independent derivation, agrees to 3 decimals); `style.css:148-153`; `EditorStartScreen.vue:12-13/:96-104/:116`; `EditorShell.vue:53-59`; `CubeTarget.css:108-126` verbatim | **NO-WAVE-OWNER** (design cure: re-derive ink treatment against the lacquer stack incl. the 1.0-crossing — never the raw tokens). Per-frame incidence → **KF.W9** SS-13 |
| **KF-APP-3** | D-2 / L-04 | No error state exists demo-wide — **0 grep hits of any kind** for every escape hatch (this seat re-ran); bare keyed `<Suspense>`, `defineAsyncComponent` with no errorComponent/onError/timeout, no `app.config.errorHandler`; `scenes.ts:118-123` swallows the warm rejection citing a `<Suspense>` receiver that does not exist. Reachable on 5 of 7 routes (cube/home resolve synchronously); the hang parks on the KF-APP-32 never-announced `aria-busy` plate forever | `App.vue:90-99`; `scenes.ts:104-107` (re-read); `main.ts` whole (both readers) | **NO-WAVE-OWNER**. Identity guard: value.js's `ErrorBoundary.md` apotheosis is a **different tree's component** — no fold, no re-book. Hang-witness (404 one chunk) → **KF.W9** SS-13 |
| **KF-APP-4** | L-03 / C-i1 | No SFC typecheck anywhere — `check`/`check:lib` are bare `tsc`, CI runs `check:lib` only, no vue-tsc in package.json/workflows/Makefile — so App.vue's script AND template are ungated. The proven enabling condition for KF-APP-1 (`.value` on a typed non-ref), KF-APP-12 (phantom slot member), KF-APP-30 (undeclared field ×7), KF-APP-34 (readonly→mutable), and β-miss KF-APP-45 (`Component | undefined` into `<component :is>`) | package.json:36-37; ci.yml:41-42; both readers' 0-hit greps, convergent | **KF.W4** — the gates/hygiene lane (K3 `noUnusedLocals` adjacency); the born-RED is trivial (`vue-tsc --noEmit` exits non-zero on 5 known rows today) |
| **KF-APP-5** | M-A (α-miss 1) | The header ribbon's three chrome controls (Share, shortcuts trigger, dark-mode toggle) are `inert` + `aria-hidden` + zero-width at rest with **no focusable affordance**: keyboard and AT lockout is source-decided from the installed producer (`inert: !w.value`, `"aria-hidden": !w.value`, hover-opener `pointerType !== "touch"`, anchor = non-focusable div); EditorShell supplies `#items` only, no `#anchor`. Frontier: the phantom `mode="persistent"` is **already deleted at origin/master** — the lockout is **not** (still no `#anchor`) | producer `types.d.ts` (no `mode`) + compiled markers + `.header-ribbon__anchor` rule — all this seat's own reads; consumer `EditorShell.vue:16-49`; `origin/master:16` probe | **KF.W6** (consumer half: supply `#anchor` content / adopt the producer's real API) **+ glass-ui BH relay** (producer half: a collapsed-at-rest toolbar whose empty-anchor state is unreachable-by-construction — standing owner edict). Rest-state render + tap affordance → **KF.W9** SS-13. Ruling 5's recomposition applies |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **KF-APP-6** | M-B (α-miss 2) + K-2 | `--dock-margin` is a **phantom token at all three substrates** (this seat: zero definitions at worktree, HEAD, `origin/master`, and the producer; zero `@property`): nine unfallbacked `var()` sites make the whole dock-anchor/work-area chain invalid-at-computed-value-time — `ChromeDock.vue:216`'s `top` computes to `auto` (the φ-capped anchor is inert), CubeScene's recede lower-guard computes to 0, TransportDock's safe-area padding computes to 0; fallback-carrying sites (the hero seat) resolve deterministically and mask it | layout.css:80/92/117/135/161/166/204 + TransportDock.vue:389/395 (re-verified); the frontier git grep (this seat) | **KF.W6** — the token audit re-cut as a **phantom-token audit** (D-15's recommendation, ratified; this is its largest instance, with KF-APP-25/-26). Rendered dock offset at 390×844 → **KF.W9** SS-13 |
| **KF-APP-7** | D-3 | `currentChannels` (App.vue:227) lacks the `isHome` guard its sibling (:248-255) and the shell binding (:74-79) both carry — and its own :225-226 comment asserts a premise ("`undefined` for home") that is false, since home mounts CubeScene whose facility exposes 3 channels synchronously → the bottom transport dock mounts on the home landing against VERDICT #6 ("home is COMPASS ONLY"). Both readers closed all suppression falsifiers (`hideControls`, `hasControlSurfaces`, TransportDock guards) | this seat re-read :225-256; chains: both readers, convergent, 9 files | **NO-WAVE-OWNER** (one-line guard + comment correction). Pill text with `selectedAnimation===""` → **KF.W9** SS-13 |
| **KF-APP-8** | D-4 + K-2-strengthened | Two live seat comments encode contradictory, both-false hero-vs-die geometry: `CubeScene.vue:259-264` cites a `pt-[var(--dock-top-band-reserve)]` seat that does not exist (grep = 0; this seat) and asserts a disjoint-band invariant that fails at the live 0.52 seat (52dvh deterministic per KF-APP-6's fallback collapse — INSIDE the die's ≥34dvh centering region); `EditorStartScreen.vue:116`'s "die keeps the upper ~45%" fails the same arithmetic | this seat re-read both comments + both seats + `--side-size` (156px = 18.5dvh) | **NO-WAVE-OWNER** (geometry re-derivation + both comments corrected in the same motion). Overlap capture 390×844 → **KF.W9** SS-13 |
| **KF-APP-9** | D-5 | The scene-swap focus target is an anonymous div (no role/label; `outline: none`), `useSceneTransition.ts:46-50` claims an announcement that cannot happen, one `aria-live` region exists tree-wide (CopyButton). The cure's ingredient (`currentLabel`, App.vue:192) already exists | both readers, convergent; part of the orientation cluster (ruling 6) | **NO-WAVE-OWNER** — the a11y-orientation cluster (with KF-APP-18/-19/-32/-33) as ONE spec surface |
| **KF-APP-10** | D-6 | The home outline is one sentence chopped into `<h1>` + two `<h2>`s; the file's own prose names them deck/hint and concedes the type rung is CSS-achieved | both readers; App.vue:50 is the sole hint supplier | **NO-WAVE-OWNER** (same cluster) |
| **KF-APP-11** | L-05 / C-B2 | Boot: `warmKfEngine().catch(() => undefined)` + `.finally(mount)` means a rejected engine load is memoised (`inflight ??=`, no retry seam) and App.vue:218's top-level `kfEngine()` read throws a message that misdiagnoses a network failure as a boot-order programmer error, naming a file that did await. With KF-APP-3: a blank page | reader-β's chain re-checked at the cited files | **NO-WAVE-OWNER**. Boot-block witness → **KF.W9** SS-13 |
| **KF-APP-12** | L-06 / C-M1 | The `#tabs-trigger` projection binds `sceneRef?.tabsTrigger` — a member no scene and no interface declares — through a 4-hop always-provided chain to a leaf that declares **no `tabs-trigger` slot at all** (reader-β closed the terminus): dead at both ends | sceneExposedApi.ts:16-34; ChannelControls :32/:180 | **NO-WAVE-OWNER** (delete the chain; KF-APP-4 is why it compiled) |
| **KF-APP-13** | L-07 / C-M4 | The "exactly ONE SURFACE-METADATA registry" exists twice — `controlSurfaces.ts` (states the invariant) and the 41-line `surfaceTabs.ts` fork (byte-identical SURFACE_META) — and App's render path reads **both copies in one frame** (machine tabs from copy A; dock tabs + cardinality from copy B) | both readers, convergent, incl. β's unexported-contract extension | **NO-WAVE-OWNER** (delete the fork at the root — feedback_no_god_modules/KISS lineage). Identity note: the same fork **shape** as β-miss KF-APP-44 (HOME_SCENE_ID) — one cure pattern, two rows |
| **KF-APP-14** | L-08 / C-M7 | cube is statically imported AND lazily registered: the descriptor's loader is dead, `warmScene("cube")` a no-op, the "all scenes load on demand" comment false, CubeScene rides the 481KB entry (artifact corroboration stale-disclosed by all parties; the source shape is current) | App.vue:155/:283-286; scenes.ts:143/:224-227 | **NO-WAVE-OWNER**. HEAD rebuild closes the artifact caveat → note to **KF.W9** |
| **KF-APP-15** | C-M6 | HeroAurora's static import puts glass-ui's 202KB aurora module + value.js's colour graph in the boot chunk on every route — the STATIC value.js exposure the kf-edge boundary proof cannot see (`v-if="isHome"` gates render, not the module edge) | reader-β's marker recount (`#version 300 es` ×3, `paperGrain` ×6, `interpolateHue` ×7, `css_syntax` ×0) | **KF.W6** (glass consumption weight: dynamic-import the hero layer). Scope-corrects, does not delete, superlative S-G |
| **KF-APP-16** | C-M3 | HeroAurora passes `initStrategy: "eager"` against its own docblock's "lazy WebGL arm past first paint", on the LCP route | HeroAurora.vue:6-8 vs :28; both readers | **KF.W6**. LCP/TBT magnitude → **KF.W9** SS-13 |
| **KF-APP-17** | L-09 / C-M2 | `headerLeft` is a dead contract member (producer + contract + slot exist; no host consumer) and the brand-mark consumer lists in two files are stale; the dead render fn hosts KF-APP-1's only correct writer | both readers, convergent | **NO-WAVE-OWNER** — swept in the SAME motion as KF-APP-1's cure (ruling above) |

### Minors (32)

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| KF-APP-18 | D-7 | Six of seven scenes ship zero `<h1>` (grep = 1 markup hit tree-wide, home-gated) | NO-WAVE-OWNER (orientation cluster) |
| KF-APP-19 | D-8 | Only navigation has no landmark; landmark nav reaches `<main>` alone. **Ruled MINOR** (ruling 6; reader-β's MAJOR = recorded dissent) | NO-WAVE-OWNER (orientation cluster) |
| KF-APP-20 | D-9 / L-13 + K-4 | Permanent `transform: scale(1)` on `.scene-host` on VT engines: dead binding, stacking context + containing-block for `absolute` AND `fixed` descendants (K-4's widening adopted), against the file's own de-layering essay. Latent (no fixed/abs victims in scenes today) | NO-WAVE-OWNER; CDP delta → KF.W9 SS-13 |
| KF-APP-21 | D-10 / L-21 / C-M8 | Three nested TooltipProviders, 700ms vs 100ms (7×) in one chrome; glass-ui forwards no defaults (reader-β closed the wrapper link); frontier keeps the root provider deliberately (DP-02/CC-03 — this seat's diff read) | **KF.W6** (one provider policy; the root provider is load-bearing at the frontier — cure is config unification, not deletion) |
| KF-APP-22 | D-11 | The only interaction hint is an in-joke name existing nowhere on screen + a ZWJ emoji carrying the verb, no gloss | NO-WAVE-OWNER; off-platform render → KF.W9 SS-13 |
| KF-APP-23 | D-12 | "from the list ☰ below" points up at a collapsed circle (strengthened by KF-APP-6: `top: auto`) | NO-WAVE-OWNER |
| KF-APP-24 | D-13 | `forced-colors`/`prefers-contrast` coverage = 0 demo-wide; glass-ui's block covers aria-state borders only | NO-WAVE-OWNER (demo half) + note to glass BH relay (producer half) |
| KF-APP-25 | D-14 | `--shadow-glass` phantom (producer ships only the five tiered siblings): hardcoded light elevation in both themes | **KF.W6** phantom-token audit |
| KF-APP-26 | D-15 + K-1 | `--dock-label-padding-inline` phantom (producer token is `--dock-trigger-padding-inline`); namespace-collision fear refuted ∅ (triply-derived, ruling 8); the audit re-cut ratified | **KF.W6** phantom-token audit (with KF-APP-6/-25) |
| KF-APP-27 | D-16 | Dead flex-centering contract wraps the hero (both offsets resolved → alignment inert); comment narrates the pre-T.D9 model | NO-WAVE-OWNER (with KF-APP-8's comment sweep) |
| KF-APP-28 | D-17 / L-24 | Style block 38 lines / 2 declarations; 18-line narrative for a deleted `contain: paint`; "queued to Glass BI 6.0.0" against installed 7.0.0 — stale on arrival | NO-WAVE-OWNER (comment surgery) + the BH-relay correction rides KF-APP-5's relay |
| KF-APP-29 | L-10 | `autoPlays` branch unsatisfiable (both producers `false`); docblock asserts the opposite | NO-WAVE-OWNER |
| KF-APP-30 | L-15 | `ribbonContent` reaches an unconditional deref of optional `matrixOptions` (:194 vs its own :197-198 chains); `selectedMatrixCell` stored at 7 undeclared sites | NO-WAVE-OWNER (KF-APP-4 gates the type half); collapsed-pane repro → KF.W9 SS-13 |
| KF-APP-31 | L-11 | `setActiveSurfaces` documents an idempotence `surfacesFor`'s fresh arrays make impossible | NO-WAVE-OWNER |
| KF-APP-32 | M-C (α-miss 3) + K-3 | The skeleton announces to nobody: `role="status"` region whose only content is `aria-hidden`, `aria-busy` never cleared (ruling 2) | NO-WAVE-OWNER (with KF-APP-3's cure: bind aria-busy + move the label into hidden text content) |
| KF-APP-33 | M-D (α-miss 4) | `aria-label="Scene"` overrides `<SelectValue />` per accname §4.3.1-2C — the last orientation surface announces a static word; same shape ×2 more (:291/:309). Verified at :241 this session | NO-WAVE-OWNER (orientation cluster); AT utterance → KF.W9 SS-13 |
| KF-APP-34 | L-19 / C-m1 | `readonly ControlSurface[]` bound to mutable `string[]` prop — advertises write-through of the machine's one mutation boundary | NO-WAVE-OWNER (KF-APP-4 gates it) |
| KF-APP-35 | L-12 | `storedControls` is a computed whose getter **writes** persisted global state (bucket-create + `??=` migration in localStorage) — re-verified at controlOptionsStore.ts:86-96 this session | NO-WAVE-OWNER |
| KF-APP-36 | L-14 / C-m9 | Ungated `data-last-vt-type` DOM stamp in production, four lines under its DEV-gated twin, zero readers | NO-WAVE-OWNER (one-line gate) |
| KF-APP-37 | L-20 | `SceneDescriptor.showStartScreen`/`gridBackground`: zero consumers — the drift class scenes.ts:218-222 congratulates itself for killing | NO-WAVE-OWNER |
| KF-APP-38 | L-22 + β-extension | `"cube"` hard-coded 3× in App + `"home"`/`"cube"` at shell-binding :236/:254 (this seat re-read both) = **five** literals against `cubeKeys.ts`'s own single-source law | NO-WAVE-OWNER — cure with KF-APP-44 (same keyspace law) |
| KF-APP-39 | L-23 / C-m3 | `AnimationGroup<any>` at App's widest type, propagated into four contracts; narrowing gated on KF-APP-4 | NO-WAVE-OWNER |
| KF-APP-40 | C-m4 | SceneSkeleton `label` prop dead API; glass Skeleton swap is root-barrel-only (73 subpaths, no `./skeleton`) | **≡ census S-6** (fold by reference — "keep stage geometry, delegate the plate to Skeleton") → **KF.W6** |
| KF-APP-41 | C-m5 | EditorHeader: 0 consumers, still barrel-exported on App's import path | NO-WAVE-OWNER (delete) |
| KF-APP-42 | C-m6 | `warmScene` reachable only from inside the already-open dropdown; worth nothing for cube (KF-APP-14) | NO-WAVE-OWNER |
| KF-APP-43 | C-m7 | App.vue:201-207's "BUILT-IN triad" comment is false (it is the FULL derived set); "correcting" App to match it would kill the easing/spring rail | NO-WAVE-OWNER (comment cure only — the code is right) |
| KF-APP-44 | β-miss 1 | `HOME_SCENE_ID` **declared twice** (scenes.ts:126 + sceneMachine.ts:31 — this seat's grep), App straddling both copies via its own two bindings, against scenes.ts:22-26's stated ownership law — the KF-APP-13 fork shape on "the ONE keyspace" | NO-WAVE-OWNER — one re-export, cure with KF-APP-38 |
| KF-APP-45 | β-miss 4 | `activeSceneComponent` is `Component | undefined` into unguarded `<component :is>`; the undefined arm unreachable only via the KF-APP-38 literals (`homeScene` omits `component` — re-read); one descriptor without `component` = silent empty scene under KF-APP-3 | NO-WAVE-OWNER (make `component` required or guard; KF-APP-4 gates it) |
| KF-APP-46 | β-miss 5 | The no-VT cross-dissolve never fires on home↔cube (`activeSceneKey` returns `"cube"` for both — re-read :287-290) while the VT path emits `types:["forward"]` for the same nav: the SpringProgress fallback is dead on VT engines everywhere (KF-APP-20) and dead on non-VT engines at the one nav every first-time user performs | NO-WAVE-OWNER |
| KF-APP-47 | β-miss 3 | `EditorShell.vue:93` is the single slot in the chain without `v-bind` (re-read :88-97 — both siblings forward); the leaf documents the compensating global-state read | NO-WAVE-OWNER (one-line `v-bind` + delete the store read) |
| KF-APP-48 | C-m8 + β-kill (count) | `PAPER_WASH_GROUND` spread AFTER the atoms silently overrides authored knobs (`medium` smooth→crayon; `saturation` hard-overwrite); "ONLY the opacity ceiling moved" false; the key arithmetic corrected to ≤7-of-12 moving (β's kill adopted on its evidence) | **KF.W6** (with KF-APP-15/-16 — one HeroAurora config motion) |
| KF-APP-49 | C-m10 | Hand-rolled pointer wiring beside the producer's `useCursorInteraction` on the same subpath — **UNSETTLED** by three seats (the stage-element/nuclei falsifier is credible) | **KF.W6** evaluate-not-mechanical-swap; settle at the bytes there |

### Info (10)

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| KF-APP-50 | L-16 | App is sole writer AND a reader of `machine.controlSurfaces` (altitude only; tick-lateness correctly killed) | NO-WAVE-OWNER (record) |
| KF-APP-51 | L-17 | Inline template mutation vs the sibling composable's written rule (and it evaluates KF-APP-35's writing getter twice per click) | NO-WAVE-OWNER |
| KF-APP-52 | L-18 | Deliberate TDZ-lazy `getRunSceneSwitch` closure — safe today, converts to boot ReferenceError if any of three watches gains `immediate` | NO-WAVE-OWNER (guard comment) |
| KF-APP-53 | L-25 | Two template-ref idioms six lines apart; the string-ref form is the mechanism KF-APP-12 survived by | NO-WAVE-OWNER |
| KF-APP-54 | L-26 | `CONTROLS_PANE_HOVER_KEY`'s two consumers define the absence-contract twice, differently (unreachable today — App always provides) | NO-WAVE-OWNER |
| KF-APP-55 | D-18 | RTL: honest no-claim, endorsed by both readers — recorded, not manufactured | terminal (no action) |
| KF-APP-56 | C-i2 / N-1 | value.js R1 parser-crash class NOT reachable from App's boot graph (`css_syntax` ×0 in the entry; colour module is the exposure = KF-APP-15) | **fold by reference → megatranche R1 / census risk-3** (KF.W2/W3 own the class; App books nothing) |
| KF-APP-57 | M-F (α-miss 6) | Copy/find-in-page duplication is the unpriced side of the S-C sr-only trade — correct trade, recorded | terminal (record beside census **S-5**) |
| KF-APP-58 | β-miss 6 | Shell-binding's load-bearing double-guard prose names retired superKeys (`__home__`/`Cube` — re-read verbatim :139/:224-225) against the live `home`/`cube` at its own :236 | NO-WAVE-OWNER (comment cure with KF-APP-8/-27/-28's sweep) |
| KF-APP-59 | β-miss 7 | glass-ui 7.0.0's peer set unsatisfiable in-tree (pencil-boil, embla) but **unreachable from App's closure** (root barrel imports neither) — a closed falsifier, not a defect | **KF.W0** substrate note + BH relay FYI; no App booking |

## Killed-claims register — 13 entries, all SUSTAINED

1. **L-02 / C-§4 (F-1 restated)** — "glass-ui phantom at HEAD; npm ci cannot build the app root". KILLED at the stated commit (optionalDep 6.0.0 + 3 lock entries at `8281638c`); phantom TRUE only of the uncommitted worktree; **extended by this seat**: `origin/master` = 7.0.0 exact devDep, so the reinstallability caveat AND the optional-dep defect class are both cured at the frontier, while an `npm ci` at HEAD silently downgrades 7.0.0→6.0.0 under every producer measurement in the corpus. Identity → census SCH-1 (ruling 1). Both lanes' evidence base survives because the installed 7.0.0 is what they read — and it is the frontier's declared producer.
2. **K-1 (D-15 sub-claim)** — "65 glass `--dock-*` tokens". Unreproducible; this seat's recount 36/80/11; conclusion (∅ intersection) survives triply-derived (ruling 8).
3. **K-2 (D-4 sub-claim)** — the bounded-interval derivation. `--dock-margin` is undefined, not unknown — at ALL three substrates (this seat). The hero seat is deterministic 52dvh; D-4 strengthened, M-B admitted (ruling 4).
4. **K-3 (S-B sub-claim)** — "AT-announced today". The role=status region's only content is aria-hidden and aria-busy never clears; announced in neither the happy path nor the hang path. Decides the one reader-vs-reader conflict (ruling 2); reader-β's cell corrected.
5. **K-4 (D-9/L-13 sub-claims)** — (a) `scale(1)` "hints compositing" — overreach, killed; (b) containing-block scoped to `fixed` only — under-scoped, killed and widened to `absolute`-or-`fixed`. Row survives at MINOR (KF-APP-20).
6. **K-5 (D-1's flat facet table)** — facets are not flat fills; the lacquer spans each facet's luminance. Sustained AND sharpened: the hint luminance is INSIDE the facet's lacquer range, so contrast crosses **1.0:1** on-face (ruling 9). D-1's verdict unharmed; the remediation must re-derive against the stack.
7. **L-01 thrown-message half** — wrong TypeError string; the RHS read throws first. Pinned by this seat's execution: `Cannot read properties of undefined (reading 'ppMode')` (ruling 3).
8. **L-01 blast-radius half** — "this toggle is its only writer". False and worse: a second, CORRECT, unreachable writer exists (`setPPMode`, grep = 2 lines, re-run this session) → zero working writers; the composition is folded into KF-APP-1's cure law.
9. **C-M8 severity** — MAJOR over-grade for a hover-latency inconsistency; two axes at MINOR; demoted (ruling 7).
10. **C-m8 measurement** — "11 of 30 keys move" irreproducible from the 12-key `PAPER_WASH_GROUND` literal (5 value-equal → ≤7 move); verdict survives on `medium` + `saturation` alone (adopted on reader-β's evidence).
11. **L-09 line count** — "33-line h() hover-card :118-150"; actual :118-144, 27 lines. Confirmed defect, corrected cell.
12. **D-17 comment-density figure** — "110 of 387 (28%)" irreproducible and UNDERSTATES (whole-file ≈37%); L-24's reproducible figures adopted.
13. **L-04 grep result** — "returns prose only"; actual **0 hits of any kind** (this seat re-ran). Defect marginally stronger than claimed; the reported evidence corrected.

## Superlatives — sustained, with provenance

- **S-A (PRM view-transition delegation)** — verified end-to-end **three times independently** (both readers + the census's "unverified statically" now closed): style.css:3 → glass index.css → view-transition.css opening with the universal `animation: none !important` PRM block. App.vue:353-357 describes it correctly. Byte-exact delegation, the rarest kind.
- **S-B (the skeleton) — two-thirds sustained**: genuinely theme-aware (bridged tokens re-verified) and PRM-honest (`animation: none` + `background: none`, the frozen-half-sweep detail almost everyone misses). The a11y third is refuted (K-3/M-C, ruling 2).
- **S-C (AnimatedText)** — the four-front a11y solve (sr-only mirror, aria-hidden glyph layer, margin-not-space word gaps with the regression recorded, em-relative lift) sustained by both readers; its one unpriced side is KF-APP-57. **Census S-5 linkage**: any `/typewriter` evaluation must preserve all four fronts — this record is the bar.
- **S-D (focus in `finished.finally`)** — sustained by both readers independently: the skipped/aborted/unsupported path still routes focus — precisely the PRM and mid-nav-interrupt users. What it routes TO is KF-APP-9; the mechanism is right.
- **S-E (exactly one `view-transition-name` + zero demo `::view-transition-*` rules)** — the ≤1-element mandatory satisfied structurally; single-sourcing is what makes S-A verifiable at all.
- **L-S-A (the Suspense shape encodes its own regression)**, **L-S-B (router reconcile: fixed point, discriminated errors, re-thrown non-benign)**, **L-S-C (useSceneSwap's reset/play/target ordering — the axis filed its own refuted suspicion)**, **L-S-E (brand.css scope reasoning)**, **L-S-F (useMonacoCancellationGuard: narrow, enumerates what it does NOT swallow)**, **L-S-G (the LIGHT/HEAVY kf edge — scope-corrected by KF-APP-15, not diminished)**, **L-S-H (identity-checked adapter release)** — all sustained on reader-β's re-derivations, spot-anchored by this seat where they intersected roster rows.
- **HeroAurora's decorative-layer contract** (aria-hidden + pointer-events-none + zero layout reads + scoped teardown + touch gated) — sustained; KF-APP-15/-16/-48 are configuration defects, not layer-hygiene defects.
- **TypingDots** — refuses its genre's two failure modes (rest opacity never 0; fixed cycle; PRM rests at the READABLE frame). **Census S-8 linkage**: the KEEP rationale (engine dogfood) is reinforced by this record.
- **The demo's transition CSS is inherited, PRM-aware, and undocumented at the consumption site** — sustained, with the documentation gap noted as part of KF-APP-28's comment surgery.
- **scenes.ts's loader registry** (reader-β's own find, re-read by this seat at :104-107): one import thunk feeds warm AND mount — the drift class made unrepresentable; cube subverts the outcome (KF-APP-14), the mechanism is why the other five scenes split cleanly.
- **D-18's honest no-claim on RTL** — endorsed; an honest absence outranks a manufactured row.

## UNPROVEN-NEEDS-LIVE residue — SS-13 inputs, routed whole to KF.W9

All live-only cells from both readers, merged, deduplicated; the mechanisms behind each are source-adjudicated above and need **no** re-derivation at probe time:
1. KF-APP-1 — the ppMode click witness (`TypeError … reading 'ppMode'` in console; string pinned).
2. KF-APP-2 — per-frame incidence of failing facets under hero glyphs across a full rotation (390×844 + desktop), and the screenshot-sampled per-facet luminance RANGE under lacquer+relit (the K-5 re-derivation base, including the 1.0:1 crossing band this seat derived).
3. KF-APP-3 — the 404-one-chunk hang witness (permanent shimmer, no retry).
4. KF-APP-5 — the ribbon's rendered rest state: is the empty pill visible at all; does any tap target exist (the anchor-geometry question ruling 5 left open); frontier build preferred.
5. KF-APP-6 — the top dock's actual offset with `top: auto` (delta vs the intended φ-cap) + the mobile recede/transport padding collapse, 390×844.
6. KF-APP-7 — the transport pill's rendered text on home with `selectedAnimation === ""`.
7. KF-APP-8 — the measured hero/die intersection in dvh at 390×844.
8. KF-APP-11 — the blocked-engine-chunk boot page state.
9. KF-APP-16 — LCP/TBT cost of eager Aurora on the home route (Q14-class measurement).
10. KF-APP-20 — CDP compositing delta of the permanent scale(1) (latent; measure only).
11. KF-APP-21 — the felt 700ms/100ms split (config proven; perception live).
12. KF-APP-22 — the ZWJ sequence's decomposition in target browsers.
13. KF-APP-30 — whether a collapsed controls pane renders `#ribbon-content` without MatrixEditor's panel (if yes: fresh-bucket throw at :194).
14. KF-APP-33 — NVDA/VoiceOver utterance for the dock combobox (accname precedence spec-decided; recovery-by-other-route live).
15. KF-APP-14/-15 — a HEAD (better: frontier) rebuild to close the stale-artifact caveat on the bundle cells.
16. **Standing provenance caveat, carried and extended (ruling 1)**: the corpus read a 252-dirty worktree on a 1-ahead/41-behind local fork; App.vue itself is clean and its frontier diff is comments-only (this seat's read), so every `App.vue:NNN` anchor is sound — but EditorShell/ChromeDock/MbabbMenu are dirty AND moved at the frontier (MbabbMenu −146 lines); **every cure re-resolves anchors at `origin/master` first**.

## Closing verdict

**ADJUDICATED — the corpus substantially survives hostile re-reading and adjudication: 59 roster rows (5 BLOCKER · 12 MAJOR · 32 MINOR · 10 INFO), 13 kills sustained (one extended, one sharpened), 13 misses admitted, 2 reader cells corrected, zero fabricated citations found anywhere in three challenge files or two returns.** The App is a genuinely well-crafted shell (21 sustained superlative cells — the PRM delegation, the focus discipline, the loader registry) wearing five blockers, of which three are one-line-to-one-file cures (KF-APP-1, -3's minimal arm, -4) and two are design decisions the tree itself documents as deliberate (KF-APP-2's ink-over-subject, KF-APP-5's collapsed chrome — the latter shared with the producer via BH relay). The deepest structural fact is KF-APP-6: the demo's entire layout authority chain runs through a token that does not exist at any substrate, masked by exactly the fallbacks that made the visible page look intended. Routings: 2 rows fold by reference (census SCH-1/F-1, S-6; plus the R1 reference), 9 rows to **KF.W6**, 1 to **KF.W4**, 2 to **KF.W0**, all SS-13 residue to **KF.W9**, remainder **NO-WAVE-OWNER** for SS-1/SS-2 spec agglomeration (M-25). Nothing here stamps a wave; X·KF executes on the owner's begin-word.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/app/App.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **App.vue=+8/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**CORRECTION (i) of the six named at G-0.9 — the `TABS_EXTERNALLY_MANAGED_KEY` provide site is `demo/app/App.vue:176`, not the banked stale-HEAD `:169`** (C-22 (i), inbound from `KF-W6-CARRY.md` §CrossEdges). Re-measured at the ref of record: ⟨`git grep -n 'TABS_EXTERNALLY_MANAGED_KEY' origin/master -- demo/app/App.vue`⟩ → `:140` the import, **`:176` `provide(TABS_EXTERNALLY_MANAGED_KEY, true);`**. **The path qualification is load-bearing**: ⟨`git show origin/master:demo/App.vue`⟩ → *fatal: path does not exist* — a bare leaf `App.vue:176` sends a seat to a path that is not in the tree.

**Scope note**: this file MOVED between the disqualified pin and the ref of record (+8/−1), so a `:169`-vs-`:176` discrepancy is a substrate artefact of exactly the class C-2 governs, not an authoring error at either end.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

