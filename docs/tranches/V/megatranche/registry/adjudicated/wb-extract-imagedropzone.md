Served model id: `claude-fable-5` (Fable 5) — observed in this seat's own system prompt, not inherited.

# wb-extract-imagedropzone — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Seat**: apotheosis adjudicator · **Date**: 2026-08-09 · **HEAD**: `1994eb53` · branch `tranche-u`.
**Corpus**: the r2 challenge set at `docs/tranches/V/megatranche/audit/components/wb-extract-imagedropzone/` (challenge-D-design.md · challenge-C-implementation.md · challenge-L-library.md, r2 governs; pass-1 archives carried where the r2 tables uphold them) against `demo/workbenches/extract/ImageDropZone.vue` (113 lines) and its consumer cone (ExtractWorkbench.vue, ExtractControls.vue, ExtractPane.vue, useExtractSession.ts, useImageQuantize.ts, ImageEyedropper/*).
**Re-readers adjudicated**: R1 (design-and-usage) · R2 (library-and-code). Both were challenged back — every verdict-level disagreement re-derived by this seat against the live tree; one of R2's two MAJOR missed-findings was killed by this seat's own instrument.
**Arbiter's own instruments (all static + one tsc trace; zero browser probes — probe parsimony)**: full source read of ImageDropZone.vue (whole), ExtractWorkbench.vue (whole), useExtractSession.ts (whole), useImageQuantize.ts (whole); ExtractControls.vue:38-46/:83-89/:117-123; ExtractPane.vue:1-15; ImageEyedropper.vue:8-14/:226-248; useImageSampler.ts:68-102; foundation.css:225-255 (`--primary` = `var(--accent-view)` = certified per-view accent) + :676-704 (forced-colors block + `:where(...):focus-visible` inside it); viewSchema.ts:135-141 (`extract: accentHueShift: 120`); focus-ring.css:1-10/:28-36 ("reused by … the gradient stop handles + remove chip today"); animations.css:56-80 (three-families law) + :184-193 (PRM guard 1); served bytes of glass-ui `dist/styles/utilities/a11y-overrides.css:1` (guard 2: `transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important` under `*:not([data-allow-motion])`); motion-quarantine.md §1; DESIGN.md:96-100 (rung-2 WELL deployment list) /:258-266 (bespoke-literal table) /:384-390; utils.css:88-106 (`.dashed-well`) /:170-174 (3-consumer rule); e2e/smoke/walk.spec.ts:74-84; pass-1 C1 probe table (G1..G4 traces); glass-ui 7.0.0 exports (`./chip` true, `./badge` true, 74 subpaths); repo censuses re-run: `bg-primary\b|border-primary\b` in demo = 8 rows (6 live + 1 comment + 1 `@apply`), `.plate-ink` declarations = exactly 5 files (none in demo/styles/), `transitionDuration:` in demo = exactly 2 (both this file), `aria-live|role="alert"|role="status"` under demo/workbenches/extract/ = 0, `cn(` call sites in demo .vue = 0, `--ink-muted:` CSS declarations in demo = **0**; @vueuse/core 14.3.0 installed with `useDropZone` + `useFileDialog` both present in dist; modality shot dirs enumerated (six dirs × the same five routes, `/#/extract` in none); tranche-X wave census (W0..W11 titles + extract-surface greps); **and the decider for R2's headline missed-finding: `tsc --traceResolution` against the real `useExtractSession.ts` under `tsconfig.demo.json`** → `@mkbabb/value.js/css` resolves to **`/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts`** (local dist, self-name resolution through the root exports map), not `node_modules`.
**Motion/PRM law**: no motion-quarantine FALSE row is carried here; HYDRATION-LEDGER marks the three motion files EXISTS-ORIGINAL (no M-xx row covers this component). The one PRM claim in this record cites the quarantine's own §1 measured cascade.

## Scoreboard

**33 CONFIRMED (2 BLOCKER · 9 MAJOR · 20 MINOR · 2 INFO) · 14 KILLED (incl. one reader-MAJOR killed by this seat) · 9 adjudicator-adopted missed findings · 7 UNPROVEN-NEEDS-LIVE residue rows.**

Blocker-weight survivors: **D-1/C-P2-2** (the stage lies about its own specimen — a portrait photo is guillotined to 9–15% visible while palette, dominance readout and eyedropper all read 100% of it) and **D-5/C-P2-3/L-13** (the sampling capability is pointer-only by construction; the node announces "tap to sample" while swallowing Enter/Space). ImageDropZone.vue itself has **no X·V wave owner** — the bulk of this roster is NO-WAVE-OWNER for the next formation boundary.

## Ruled disagreements (this seat's actual work)

| id | R1 (design-and-usage) | R2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| D-1/C-P2-2 severity | BLOCKER | MAJOR | **BLOCKER** | The instrument contradicts its own output on the default phone-photo orientation, on a shipped first-class route, silently — the ColorSpaceSelector L-1 class (product states falsehood). Arithmetic is exact on two engines (458×900/120=3435; 458×1400/300=2137.3 — the signature of `height:100%`→`auto` under an indefinite parent, CSS2.1 §10.5); both evidence shots show palette swatches absent from the visible frame. R2's own row concedes the three readings diverge structurally. |
| D-1 cure | endorses the C axis's leaf `max-h` | kills the C cure; proposes `min-h-0` on the `<img>` | **both prescribed cures KILLED; `min-h-0` adopted as the candidate** | R1's kill of the aspect-ratio cure holds (it promotes D-2's letterbox waste to an invariant). R2's kill of the leaf `max-h` holds (it hardcodes ONE consumer's clamp into the leaf while ExtractWorkbench:19 passes a different clamp on the `split` branch — the per-instance-override pathology). This seat re-derived R2's mechanism independently: the `<img>` is a direct flex item (Transition renders no wrapper); flexbox's automatic minimum size takes the **transferred size suggestion** (definite `w-full` × intrinsic ratio = full scaled height) for a replaced item, so the item is unshrinkable; `min-h-0` restores flex-shrink against the container's max-height-clamped used size, `object-contain` finally has a box to contain, zero literals, both layout branches preserved. Cure adjudicated separately from defect (ColorSpaceSelector addendum item 2); the executing wave must gate the breaking direction with a portrait-specimen delta witness. |
| D-5/C-P2-3/L-13 severity | BLOCKER | MAJOR ("confined to one feature") | **BLOCKER** | Total keyboard unreachability of a whole capability (one true-setter of `eyedropperActive`, ExtractWorkbench.vue:25, native click only; `tabindex=-1` the instant a preview exists; `div[role=button]` synthesizes no click from Enter/Space; the keydown handler `.prevent`s unconditionally while doing nothing). WCAG 2.1.1 Level A on a shipped route, aggravated by an aria-label instructing the impossible action. R2's demotion argument (feature-scoped) is preserved as dissent. |
| C-P2-1 severity | BLOCKER | MAJOR | **MAJOR** (R1 dissent preserved) | The chain is confirmed link-by-link (this seat re-read every hop: no type check at :87-92; preview committed at useExtractSession.ts:166 BEFORE decode; `quantizeFromFile` unawaited/uncaught at :155; `error.value` written only in worker callbacks at useImageQuantize.ts:62/:70, unreachable from a `createImageBitmap` rejection at :19) — but the trigger is a malformed input, unlike D-1 (any portrait photo) and D-5 (any keyboard user). The terminal-state lie it produces is booked at A-1, whose copy aggravator is the register's, not this component's. |
| C1 vs C2/D-12 identity | two findings (uncancelled `dragenter`; container `dragleave`) | one mechanism family (hit-testable descendants churn the drag target) | **ONE mechanism, two symptoms — merged row, MAJOR** | The pass-1 G1 trace shows both symptoms produced by the same target-change churn across `span.prompt`/`svg.icon`; the cure set is shared (cancel `dragenter` + depth-count or `pointer-events:none` on descendants). Both readers' kills of the absolutist framing ("100%", "cannot receive an image") stand on the probe's own G3/G4 = 1 delivered rows, which this seat re-read in the pass-1 file. |
| D-13 | CONFIRMED MAJOR | UNPROVEN-NEEDS-LIVE | **CONFIRMED MAJOR** (empty-state, chromium-emulated cell; populated state stays unproven) | The mechanism is spec-static: :12 vs :15 differ only in colour tokens + `scale-[1.01]`; forced-colors forces both border tokens to the same system ink and both `bg-primary` alphas onto Canvas (alpha preserved, 5% vs 10% over Canvas ≈ nothing); foundation.css:678's tier-1 `forced-color-adjust: none` list does not include this element (my read of :676-704). A forced-colors probe DID run (pass2-probe-D/probe3.mjs — R1 verified it toggles only the Vue `dragging` ref, legitimate for rendered-state) and measured scale as the sole survivor. What no capture covers is the populated state — residue row U-1. |
| D-14/C-P2-6 severity | MAJOR | MINOR | **MAJOR** | On touch, the chip is the ONLY possible visible signal that the preview became a sampling surface, and it is triply dead (hover-gated behind `@media (hover:hover)`, focus-arm structurally unreachable because `preview ⟹ tabindex=-1`, `aria-hidden`). A capability invisible to an entire device class is a discovery failure, not a polish gap; measured `opacity: "0"` on iPhone 14 Pro with a preview loaded. |
| D-9 severity | MAJOR | MINOR | **MINOR** (R1 dissent preserved) | Worst consequence re-derived: a drop mid-camera kills the viewfinder (ExtractWorkbench.vue:234-237 unconditional `stopCamera()`) — recoverable, no data loss, no lie; the mid-quantize second-drop arm is last-wins (its real hazard is A-6's correlation defect, booked separately). The missing-disabled-contract asymmetry against the gated rack (:70) is real and stays in the row. |
| D-6/C-P2-8c severity | MAJOR (while killing its own escalation) | MINOR | **MINOR** | R1's kill undercuts R1's label: with the dual-contrast recipe at ONE consuming rule in the tree (focus-ring.css:4-5 says so itself — my read), non-adoption is house-normal delegation, not an outlier. The core stands: zero designed focus affordance in the file; the only `[role="button"]:focus-visible` rule is quarantined inside the forced-colors block (my read of foundation.css:696-704). |
| L-11 | core MAJOR (parallel mint vs rung-2 WELL) | hue arm KILLED, material arm MINOR | **hue arm KILLED; material arm CONFIRMED MINOR** | Token graph verified by this seat: foundation.css:251 `--primary: var(--accent-view)`; `--accent-view` is resolved THROUGH the library (rotate → low-C floor → Ottosson gamut-map → L re-guard → WCAG ≥3:1) per :232-250; viewSchema.ts:140 `extract: { accentHueShift: 120 }` — the measured 120° is the designed per-view identity, and the challenge's prescribed cure (paint `--accent-live`) would undo S.W7·W7-4. What survives: an alpha-wash surface fill riding the accent where DESIGN.md:98's rung-2 WELL (`--well-bg`, `.dashed-well`, cartoon stamp, `--radius-card`) is the certified material — `bg-primary/<α>` as a surface fill is unique to this file (census re-run: the OTHER live alpha row, PaletteSlugBar.vue:73, is a border). |
| L-2 fallback arm | carried "the fallback arm is dead" | killed it | **R2 wins — the fallback is LIVE** | This seat's grep: `--ink-muted:` has **zero** CSS declarations anywhere in demo/ — the token exists only as a JS-stamped root property (useAtmosphereBoot.ts:100-106, `{ immediate: true }` watch). `var(--muted-foreground)` is the served value for every frame before that boot watch runs and the only value if boot fails — a LIVE masking fallback onto the exact static token the component's own :104-108 comment convicts. The Edict-2 charge stands in this corrected, sharper form; the cure is a CSS `:root` declaration for the token. |
| L-3+L-14 severity | MAJOR | MINOR | **MINOR** | Inverted ownership + the pre-3.5 ref idiom are architecture defects with no user harm of their own; their harm is C3, booked separately at MAJOR as the consequence. |
| R2 missed #1 (tsconfig binds published 4.0.0 for `/css`) | — (not seen) | MAJOR | **KILLED by this seat** | See the killed register, K-14 — the one reader-MAJOR this apotheosis kills outright, with a runnable witness. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `1994eb53`; re-resolve before executing any cure (the D-19 lesson). Routing: tranche-X wave census run by this seat — **no X·V wave (W0–W11) names ImageDropZone.vue**; X-W7 modify-carves ExtractWorkbench.vue and deletes its :163-166 caption; X-W4's extract row is ImageEyedropper "import + face only"; X-W1 owns e2e/visual + oracle re-gating. Everything else on this surface is NO-WAVE-OWNER for the next formation boundary.

### BLOCKER

- **R-1 · D-1 / C-P2-2 (+ D-2's mechanism) — the indefinite-box crop: the stage lies about the specimen.** `h-full` on the preview `<img>` (:41) resolves to `auto` against a parent that has only min/max heights (:9 `min-h-[140px]`; ExtractWorkbench.vue:20 `min-h-[180px] max-h-[min(320px,40dvh)]`), so `object-contain` is inert; `w-full` fixes width, intrinsic ratio sets height, `overflow-hidden` (:8) guillotines: 9.32%/14.8% visible on two engines, palette + dominance headline + eyedropper (ExtractWorkbench.vue:173-175 full `previewDataUrl`; useImageQuantize full file) all operate on 100%. Shots re-read by R1 (desktop-light-tall.png: green-over-blue stage, five-swatch palette with four swatches nowhere in frame). Unshrinkability mechanism (R2, re-derived by this seat): replaced-flex-item automatic minimum = transferred size. **Cure candidate `min-h-0` on the img (ruled above); both readers' prescribed cures killed. Disposition: NO-WAVE-OWNER.**
- **R-2 · D-5 / C-P2-3 / L-13 — sampling is pointer-only; the label instructs the impossible.** `:tabindex="disableClick ? -1 : 0"` (:19) with `disableClick` ≡ preview (ExtractWorkbench.vue:23); both keyboard branches dead once populated (:21-22, and both only ever call `openFilePicker`); the ONLY `eyedropperActive = true` setter is the parent's native-click fallthrough (ExtractWorkbench.vue:25); `div[role=button]` has no native activation; `@keydown.enter.space.prevent` swallows the keys it ignores; aria-label says "tap to sample colors" (:20). WCAG 2.1.1 Level A. **Disposition: NO-WAVE-OWNER** (theme fits X-W4 "semantic controls and action ownership", but W4's file table does not carry ImageDropZone.vue — booking behind a wave that doesn't own the file is the un-completable-wave failure).

### MAJOR

- **R-3 · D-2 — the landscape orphan.** 61px of image floating in a 180px block-axis floor (~66% of the stage reserved on an axis with no collision); confirmed in desktop-light-wide.png (gestalt only — see K-8 for the boundary sub-claim's evidence kill). Cure couples to R-1's sizing rework. **NO-WAVE-OWNER.**
- **R-4 · C-P2-1 / L-12 / D-7 / D-8 — one silent-failure mechanism, four filings.** Unguarded picker intake (:87-92) vs MIME-guarded drop with no else (:94-100, advisory `accept` at :30); preview committed before decode (useExtractSession.ts:166); `runQuantize` fire-and-forget (:153-157); `createImageBitmap` rejection lands nowhere (useImageQuantize.ts:19; `error.value` only at :62/:70 downstream of a worker message never posted); the pane's destructive register (ExtractWorkbench.vue:80-85) structurally unreachable from this failure; second unhandled rejection + never-sized 300×150 canvas in the eyedropper (ImageEyedropper.vue:237/:247 unawaited `loadAndFit`; useImageSampler.ts:72-75). Severity ruled MAJOR (R1's BLOCKER preserved as dissent); the entrapment sub-claim is killed (K-4). The cure is an emit-to-register + decode guard shared by both intakes — the destination already exists (S-7). **NO-WAVE-OWNER.**
- **R-5 · C1 + C2/D-12 (merged) — drag-target churn: refused drops + strobing highlight.** No `dragenter` handler (:23-25); container-level `dragleave` clears `dragging` on every descendant crossing (descendants :37/:46-51 are hit-testable); release on a target-change update delivers no `drop` (pass-1 A/B: G1 shipped 0 vs G2 cured 1, single injected line; G3/G4 = 1 — the failure is conditional, never "100%": K-3); highlight pulses over the measured 0.3s (:8 `transition-all` + :17). **NO-WAVE-OWNER.**
- **R-6 · C3 — the toolbar Upload button opens the eyedropper over the OS file picker.** Structurally decidable: hidden input (:27-33) is a descendant of the click-handling root (:6); `fileInputRef.value?.click()` (:82) bubbles back through both merged root listeners — the child's (:21, no-op when populated) and the parent's fallthrough (ExtractWorkbench.vue:25, fires). Path: ExtractControls.vue:40-45 → ExtractWorkbench.vue:74 → :230-232 → :82. Direct enabler: L-3's inverted ownership (R-16). **NO-WAVE-OWNER.**
- **R-7 · D-13 — forced-colors erases the drag state (empty-state cell).** Ruled above: colour-only + 1% scale encoding; tier-1 exemption list does not include this element; probe-measured, scale sole survivor. Cites VISUAL-CONSTITUTION §4.1 (never colour-only). Populated state = residue U-1. **NO-WAVE-OWNER.**
- **R-8 · D-14 / C-P2-6 — the `sample` chip is dead on touch, AT, and focus.** :57-61: renders only when `preview` (which forces `tabindex=-1`, so `group-focus-visible` is dead code by construction); `group-hover` gated behind `@media (hover:hover)`; `aria-hidden="true"`; measured `opacity: "0"` on iPhone with preview loaded; the aria-label says "tap" to the device class that never sees it. Cure may adopt glass-ui `./chip`/`./badge` (shipped — verified; consumer-side, no producer relay needed). **NO-WAVE-OWNER.**
- **R-9 · C-P2-4 — the only e2e assertion naming this component is vacuous.** walk.spec.ts:78-81 `getByRole("button", { name: /Upload image/i }).last()` resolves to ExtractControls' DockControl (accessible name from `title`; later in DOM — ExtractWorkbench.vue:15 vs :64), never to the zone; R2's executed mutation (component deleted → 1 match, still green) is the strongest evidence in the corpus; repo-wide `setInputFiles|dataTransfer|input[type=file]` in e2e/ = 0. **Route: X-W1** (oracle re-gate/oracle-slate successor — the re-gated slate must carry a real drop-zone assertion). 
- **R-10 · A-1 (adopted from R1's missed) — the result register contradicts the specimen for the whole decode window; on the broken-file path the contradiction is terminal and is the only feedback.** Verified by this seat: `isProcessing.value = true` at useImageQuantize.ts:87 sits AFTER `await imageFileToPixels(file)` (:106) — full-res decode uncovered; `palette` never cleared on a new file (:59 is the only write); so (a) first load renders the shadow branch's "· undeveloped plate — feed it an image ·" (ExtractWorkbench.vue:162-166) beneath the image just fed; (b) replace shows the PREVIOUS palette + dominance headline + k-gradient under the NEW specimen with no skeleton; (c) broken file = "feed it an image" forever. **Cross-note: X-W7 deletes the :163-166 caption under its own remit (arm (a)'s copy dies there; the wave must not re-home the lie); the temporal mechanism + palette-clear + error-channel cure: NO-WAVE-OWNER**, coupled to R-4's channel.
- **R-11 · A-2 (adopted from R1's missed) — the stage animates its own resize under the user's scroll on every phone.** `max-h-[min(320px,40dvh)]` + `transition-all` + 0.3s: `max-height` is on the transition list and `dvh` tracks the dynamic viewport (binds below ~800px — the D probe's own iPhone measurement, 264px = 40dvh); the host pane scrolls (ExtractPane.vue:5), which is the URL-bar-collapse gesture. Mechanism source-decidable; Safari perceptual amplitude = residue U-6. **NO-WAVE-OWNER.**

### MINOR

- **R-12 · D-9 — no disabled contract** (ruled MINOR above; camera-kill + mid-quantize intake, vs the gated sibling rack at ExtractWorkbench.vue:70). **NO-WAVE-OWNER.**
- **R-13 · D-6 / C-P2-8c — no designed focus state; UA ring delegated** (ruled MINOR above; the escalation and the pointer-click sub-claim are K-6/K-7). **NO-WAVE-OWNER.**
- **R-14 · L-11 (material arm only) — accent alpha-wash where the rung-2 WELL material is certified** (hue arm K-1): `bg-primary/5|10` + `--radius-panel` + no cartoon stamp vs `.dashed-well`'s `--well-bg`/`--card-edge`/`--shadow-cartoon-sm` (utils.css:90-105; DESIGN.md:98 lists `.dashed-well` first and not this file). Note the drag/hover accent tints are semantic drop-target states and may survive a WELL re-materialization. **NO-WAVE-OWNER.**
- **R-15 · D-17 / L-1 / L-2 (corrected) — `.plate-ink` ×5 byte-identical scoped twins with a LIVE masking fallback.** Census re-run by this seat: exactly 5 declarations (ImageDropZone.vue:109, ExtractWorkbench.vue:290, ExtractControls.vue:148, EmptyState.vue:102, ErrorBoundary.vue:84), none in demo/styles/; the repo's own 3-consumer globalization threshold (utils.css:172, DESIGN.md rule). Fallback arm corrected per the L-2 ruling: LIVE pre-boot, masking onto the convicted static token — cure = declare `--ink-muted` in CSS at `:root` + one global recipe. **NO-WAVE-OWNER.**
- **R-16 · L-3 + L-14 — inverted ownership of the file dialog + the pre-3.5 ref idiom** (defineExpose reach; `ref<InstanceType<…>>` at ExtractWorkbench.vue:222 beside `useTemplateRef` at :223; the capability has two triggers and lives in neither's parent; enabler of R-6). **NO-WAVE-OWNER.**
- **R-17 · L-4 — hand-rolled mechanics an installed dependency owns.** @vueuse/core 14.3.0 installed (verified) and ships `useDropZone` (with the enter/leave counter R-5 lacks) + `useFileDialog` (whose `open()` dissolves the R-16 reach) — both present in the installed dist (verified). Six existing demo consumers. **NO-WAVE-OWNER** (adoption is also the natural cure vehicle for R-5/R-16).
- **R-18 · D-3 / C-P2-8b / D-3b — the sizing contract split across two files, arbitration by stylesheet emission order** (`min-h-[140px]` vs `min-h-[180px]`, both (0,1,0), no `cn()`/twMerge anywhere in demo .vue — census 0, verified); the `layout="split"` arm is dead configuration (sole mount ExtractPane.vue:11-13 `layout="column"`, verified). **NO-WAVE-OWNER.**
- **R-19 · D-10 / L-5 — `disableClick` is a phantom degree of freedom**; sole consumer derives it from `preview` (ExtractWorkbench.vue:22-23), so the middle aria-label branch (:20), the `'replace'` chip word (:61) and the populated `cursor-pointer` (:10) are unrenderable. **NO-WAVE-OWNER.**
- **R-20 · D-11 — two simultaneous "add an image" affordances in one pane** (the dashed zone + the toolbar Upload control a few px below), compounded by R-6: in the populated state the toolbar control does something entirely different from what it says. **NO-WAVE-OWNER.**
- **R-21 · D-15 — physical `right-1.5` corner pin; RTL lands at inline-start**; previously filed (U w-a11y rtl census names this file:line pre-relocation) and survived both a relocation and a value change. **NO-WAVE-OWNER** (the rtl modality capture that would witness it is A-4's hole, routed X-W1).
- **R-22 · D-16 / L-6 — the demo's only two inline `transitionDuration` bindings, half no-op, half unbooked.** Census re-run: exactly 2, both this file (:17, :59); the timing-function half restates the root default (foundation.css:128-129); the 0.2s→0.3s deviation is absent from DESIGN.md:258-266's bespoke-literal book (verified — ImageEyedropper/ActionButton/PointerDebugOverlay/PaletteCard/useHeightTransition only). The PRM note travels in its corrected form only (K-9). **NO-WAVE-OWNER.**
- **R-23 · C-P2-5 — `alt="Uploaded image"` is unreachable** (WAI-ARIA children-presentational under `role=button`+aria-label); with zero live regions in the extract tree (census 0, verified), a healthy preview and a 0×0 broken one are AT-identical. Couples to R-4's cure. **NO-WAVE-OWNER.**
- **R-24 · C-P2-7 — base64 data URL where an object URL is the idiom**; the ~1.34× string is a ref, a `:src`, a `<Transition>`-compared `:key`, and a cross-component prop. **NO-WAVE-OWNER.**
- **R-25 · C-P2-8a / D-4 — `rounded-xl` raw rung on the img where `rounded-panel` is the token**; overshoots the parent's inner clip curve by 2px/corner (dead ink today; desyncs when `--radius-panel` moves). **NO-WAVE-OWNER.**
- **R-26 · L-9 — tsconfig.demo.json paths drift.** CONFIRMED as fact (dead `/parsing`+`/units` targets, omitted `/css`+`/value`, the "CLOSED 8-key set" comment false — my read of :31-52) — **DUPLICATE IDENTITY: this is ColorSpaceSelector L-14, already FOLDed to W-HYGIENE exports↔tsconfig parity with a born-RED generator-parity command. One identity, never re-booked (anti-rename). The consequence escalation R2 attached is K-14.** **Disposition: already FOLDED (W-HYGIENE).**
- **R-27 · A-3 (adopted) — the chip paints certified plate ink onto an uncertified ground**: `bg-background/85` + `.plate-ink` composited over arbitrary specimen pixels (:58), while the ink's floor-clamp referent is the page-ambient plate (useAtmosphereBoot derivation; the component's own :104-108 comment names the ground). The one text run in the file outside the contrast guarantee — in the file that documents deleting a `/50` post-hoc alpha for this exact class of reason (:44-45). **NO-WAVE-OWNER.**
- **R-28 · A-4 (adopted) — `/#/extract` is absent from ALL SIX modality matrices.** Enumerated by this seat: forced-colors/keyboard-focus/reduced-motion/rtl-desktop/rtl-mobile/zoom-200 each hold the same five routes (adminusers, blob, browse, gradient, picker); safari-* matrices do carry extract.png. One coverage defect, not four footnotes — the structural reason R-2/R-7/R-13/R-21 survived to be found here. **Route: X-W1** (the visual/oracle re-gate must run modality matrices against the route census, not a 5-route sample).
- **R-29 · A-6 (adopted) — single `pendingResolve/Reject` slot; no request correlation.** Verified: useImageQuantize.ts:51-52/:89-91 overwrite per call; first response resolves whatever is pending and clears `isProcessing` (:65) unconditionally. Overlap reachable (decode+quantize routinely > the 300ms slider debounce): orphaned promise, mis-correlated palette, skeleton dropping early over a stale palette. **NO-WAVE-OWNER.**
- **R-30 · A-7 (adopted) — copy-before-transfer contradicts the zero-copy contract.** `pixels.buffer.slice(0)` (:93) duplicates the full decoded image (~48MB @ 12MP) on the main thread; every caller's buffer is freshly created and discardable — direct transfer is safe; the file's own docblock (:5) and the worker's (:3) both claim Transferable zero-copy. **NO-WAVE-OWNER.**
- **R-31 · A-8 (adopted) — the sampler rejects with a raw DOM Event and every caller floats the promise.** useImageSampler.ts:74 `img.onerror = reject`; loadAndFit unawaited at mount and watch (ImageEyedropper.vue:237/:247); the eyedropper's only failure mode surfaces as "[object Event]". X-W4 touches ImageEyedropper "import + face only" — this is deeper than face. **NO-WAVE-OWNER.**
- **R-32 · A-9 (adopted) — `useBreakpoint` subscribes a matchMedia listener whose value can never be read** (ExtractWorkbench.vue:226; sole read at :148 short-circuits on the dead `split` branch). Dies with R-18's dead configuration. **NO-WAVE-OWNER.**

### INFO

- **R-33 · A-10 (adopted) — Reset doesn't reset what its gate implies**: enabled only because an image exists (ExtractControls.vue:83-89 `disabled || !hasImage`), yet clears neither image, preview, nor palette (useExtractSession.ts:180-184 restores k/chromaWeight and re-quantizes the same file). A naming/semantics decision for the owner, not a defect verdict. **NO-WAVE-OWNER.**
- **R-34 · L-10 — negative proof: glass-ui 7.0.0 ships no drop-zone/file primitive** (74 subpaths, none matches — verified), so demo-local residence is CORRECT; it does ship `./chip` + `./badge` (verified), which R-8's cure may adopt consumer-side. No producer relay required by this corpus.

## Killed-claims register

- **K-1 — L-11's hue/off-axis arm + its `--accent-live` cure (R2's kill, arbiter-verified).** `--primary` IS the certified per-view accent: foundation.css:251 `--primary: var(--accent-view)`, resolved through the library with a WCAG ≥3:1 guard (:232-250); viewSchema.ts:140 `accentHueShift: 120` is exactly the measured delta. The prescribed cure would undo S.W7·W7-4. 
- **K-2 — L-11's "species of one" pasted grep (both readers).** Re-run by this seat: 8 rows, not 4 — the pasted block was trimmed; PaletteSlugBar.vue:73 carries a live `border border-primary/30`. The narrow surface-fill claim survives (R-14); the pasted evidence was pruned to fit the sentence — in an enumeration axis, the one inadmissible move.
- **K-3 — C1's "fails 100% of the time" / "the zone cannot receive an image" (both readers).** Refuted by the establishing probe's own G3/G4 rows (`dropEventsDelivered: 1` against shipped source, re-read by this seat). The true form is conditional: refused when release lands on a target-change update.
- **K-4 — C-P2-1's "no route back" entrapment (R2's kill, arbiter-verified).** ImageEyedropper.vue:11 ships an explicit Close DockControl (a real focusable button) and :226-234 a window Escape handler (pin-unwind first, close second), removed on unmount. Both exits exist.
- **K-5 — L-2's "the fallback arm never fires" (R2's kill, arbiter-verified).** Zero `--ink-muted:` CSS declarations exist (my grep); the token is JS-stamped only — the fallback is the served value pre-boot and on boot failure. Measurement-at-steady-state was overstated into a structural claim. The masking charge survives in corrected form (R-15).
- **K-6 — D-6's escalation ("24 demo consumers make non-adoption an outlier") (R1's kill, arbiter-corroborated).** Line counts masquerading as consumer counts; the dual-contrast recipe has ONE consuming rule, and focus-ring.css:4-5 says so in prose (my read). A one-consumer recipe cannot make a non-adopter an outlier.
- **K-7 — D-6's "a mouse click draws the ring" sub-claim (R1's kill).** Never measured (both probes keyboard/programmatic focus); modern WebKit/Chromium gate the UA ring behind `:focus-visible` heuristics on `div[tabindex]`; the cited measurement (warm near-black outline) contradicts the prose ("bright blue-white halo"). Demoted to residue U-2.
- **K-8 — D-2's boundary-at-rest evidence (R1's kill).** The cited shot renders the SAMPLE chip, which requires `group-hover`, which simultaneously fires `hover:border-primary/50` — a hover shot is mutually exclusive with the at-rest state it was offered to evidence. The textual claim (`border-transparent` at :14) is true from source; the shot is inadmissible for it.
- **K-9 — D-16's PRM attribution ("the 0.01ms global guard covers it") (R1's kill, arbiter-verified against served bytes).** The measured cascade winner for `transition-*` is glass-ui's `a11y-overrides.css:1` (`*:not([data-allow-motion])` at (0,1,0) over `*` at (0,0,0)): served value under reduce = `transition-duration: 0.1s !important` with `transition-property` rewritten to exclude `transform` — so the `scale-[1.01]` drag beat does not animate under reduce at all (materially better than the killed claim). Cites motion-quarantine.md §1; no M-xx FALSE row touches this component (HYDRATION-LEDGER: EXISTS-ORIGINAL ×3).
- **K-10 — C-P2-2's landscape `visibleFraction: 2.9478` (R1's kill).** A visible fraction cannot exceed 1; the key computes zoneH/imgH, which is a visible fraction only under overflow — for the landscape control the true value is 1.0 and the printed number is the reciprocal plate-utilisation. Finding unaffected; the metric key means two quantities in adjacent rows, unflagged.
- **K-11 — D-1's prescribed cure (declared `aspect-ratio` stage) (R1's kill, upheld).** The cure's mechanism IS D-2's complaint promoted to an invariant: a 1600×200 specimen letterboxes into a near-square forever. §11 of the D axis cannot claim it kills both.
- **K-12 — C-P2-2's prescribed cure (leaf `max-h-[min(320px,40dvh)]`) (R2's kill, upheld).** Hardcodes one consumer's clamp into the leaf against the other branch the same consumer passes (`sm:max-h-[min(400px,50dvh)]`, split) — the per-instance-override pathology, plus a viewport literal duplicated across two files. `min-h-0` adopted as candidate (ruled above).
- **K-13 — challenge-C standing-edict row 4, "Glass-ui is the design system — PASS — nothing minted locally" (R2's kill, arbiter-verified).** False on the file's bytes: :56-61 hand-mints a chip while the producer ships `./chip` and `./badge` (verified in the installed exports map). The row launders a live finding into a pass.
- **K-14 — R2's missed-finding MAJOR: "the demo's typecheck binds the PUBLISHED npm 4.0.0 for `/css`/`/value`, so vue-tsc cannot see a breaking change to the library's `/css` surface" — KILLED BY THIS SEAT.** Runnable witness: `tsc --traceResolution` under `tsconfig.demo.json` against the real `demo/workbenches/extract/composables/useExtractSession.ts` prints `======== Module name '@mkbabb/value.js/css' was successfully resolved to '/Users/mkbabb/Programming/value.js/dist/subpaths/css.d.ts' with Package ID '@mkbabb/value.js/dist/subpaths/css.d.ts@4.0.0'. ========` — the LOCAL dist, via package self-name resolution through the root `exports` map (there is no demo/package.json to break the self-reference walk — verified). The published copy in node_modules (4.0.0, present via glass-ui's peer dep; css.d.ts diff non-empty, 41 lines — all internal `_2` alias plumbing, zero exported-surface divergence) is never consulted. This also EMPIRICALLY CONFIRMS the ColorSpaceSelector L-14 ruling's "bundler-mode self-name resolution covers it", which R2's finding contradicted without testing. The drift itself remains real and booked ONCE at that identity (R-26 → W-HYGIENE). The kill is of the mechanism and the MAJOR consequence, not of the config-parity row.

## Superlatives (provenance preserved)

- **S-1 — Motion-family conformance by construction** (both readers): :36 keys `vj-morph` for exactly its written definition ("ONE surface, NEW content" — animations.css:70-74, verified), zero local keyframes, in a demo whose law says a fourth family name is a defect.
- **S-2 — Correct PRM posture by absence** (R1): no local reduced-motion block, and that is the right answer — two global guards reach the file, and the measured cascade winner strikes `transform` from the transition list entirely (K-9's served bytes). Twelve of 46 Codex reports mistook this centralisation for absence; neither reader did.
- **S-3 — Certified ink with numeric receipts in the source** (both): :104-108 records WHY the previous token was retired; the sibling carries the convicting measurement in a comment (ExtractControls.vue:119-122 BORN-RED record, verified verbatim — light 1.88 / dark 1.85 vs the 3:1 floor).
- **S-4 — A comment that records a deletion** (R1): :44-45, dated and sourced, justifying what is NOT there — the harder and more useful record.
- **S-5 — Zero magic `[var(--…)]` reaches** (R1): every token arrives through a first-class utility; the only arbitrary values are geometry, which the rule permits (DESIGN.md:386, verified).
- **S-6 — The stated design law is right and the edge-affordance shape is right** (R1): "the specimen never lies — no veil over the field" (:3-5) plus a corner chip instead of a hover wash is the correct SHAPE for a colour instrument; what fails is the architecture around it (R-8). The cure must preserve this law.
- **S-7 — The state law exists at the seat that owns it** (R1): ExtractWorkbench.vue:79 "error ≠ empty: an explicit destructive line", with empty given its own voice — R-4's cure is an `emit`, not a design.
- **S-8 — The dominance readout's truncation handled as a truth problem** (R1): :134-141 `:title` + `select-all` actually present ("never a lying readout") — the exact instinct the preview stage failed to apply to itself.
- **S-9 — Failure-explicit presentation layer** (R2): useExtractSession.ts:25-73's `PalettePresentation` discriminated union folding serialization failure into the same visible channel as the worker error — the library's failure-explicit idiom carried into the demo, which makes R-4 an omission rather than a posture.
- **S-10 — Worker lifecycle + injectable factory** (R2): `workerFactory` for tests/Safari (S.W2 W2-4), both failure surfaces bound, terminate-on-unmount, and a worker that answers typed errors instead of dying (verified in useImageQuantize.ts/quantize-worker.ts).
- **S-11 — The file-input reset nobody credited** (R2): :91 `input.value = ""` — the canonical same-file-no-change bug handled correctly in a component all three axes rated defective.
- **S-12 — Powerless-`none` handling in the dominance tie-break** (R2): useExtractSession.ts:130-137 handles the CSS `"none"` channel on both sides of the comparison (verified); most consumer code coerces and mis-ranks.
- **S-13 — Generated self-alias as drift-proof configuration** (R2, now doubly vindicated): vite.config.ts derives the runtime aliases FROM `package.json#exports`; this seat's K-14 trace shows the TS side ALSO lands on the local dist via the same exports map — the pattern is correct twice over, and the hand-written tsconfig twin remains the only drifted copy (R-26).
- **S-14 — Eyedropper teardown and Escape unwind** (R2): pin-unwind before close, listener removed on unmount, offscreen canvas disposed (verified :226-248 + useImageSampler dispose) — a clean modal lifecycle in a component whose host has none.
- **S-15 — Audit-craft: the executed mutation test** (R2): challenge-C deleted the component from the DOM and re-ran the assertion rather than alleging vacuity — the single strongest evidence in the corpus (R-9).
- **S-16 — Audit-craft: the methodology retraction** (R2): challenge-C discarded its own first probe set on discovering synthetic `DragEvent` bypasses the drag controller, then re-derived every headline — self-invalidating evidence, published, before the findings that depend on it.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs, keyed for SS-8)

- **U-1** — Forced-colors POPULATED state: whether `border-transparent` (:14) is forced to visible ink in WHCM, inverting "no veil over the field" exactly where the specimen is (engines differ on preserving `transparent`). No capture exists — probe3.mjs and both forced-colors shots are empty-state. Pairs with R-7 (whose confirmed cell is empty-state only) and R-28's matrix hole.
- **U-2** — Whether a MOUSE CLICK on the empty zone draws the UA ring (i.e. `:focus` vs `:focus-visible`). Both readers' prior: FALSE on target engines. (K-7's demotion.)
- **U-3** — Pass-1 §D-1(b) blank preview ("0/625 image-coloured pixels"): did not reproduce at higher overflow in WebKit; engine/ratio-conditional; discharged by R-1's cure either way. Never quote as a universal.
- **U-4** — Focus destination after the picker returns: `tabindex` flips 0→-1 on the commit tick while DOM focus is plausibly on the zone — an AT user may be parked on a node announcing an operable button that no longer operates. Decidable only live (C-P2-3's measured `activeElementAfterOpen` is a different moment).
- **U-5** — H-1: `mode="out-in"` blank frame in the stage. Needs a TALL→WIDE swap (resolved heights differ); both prior specimens pinned the 320px ceiling. Upstream twin: PI-DRIFT-1 (t-deferred-census).
- **U-6** — R-11's perceptual amplitude on real Safari: the 0.3s max-height ride under URL-bar collapse (mechanism source-confirmed; amplitude and frame-pacing want the device).
- **U-7** — CARRIED-UNVERIFIED, not confirmed: pass-1 §D-6 rendered contrast (dashed border 1.21:1 light / 1.26:1 dark vs the 3:1 graphics floor; prompt ink 4.32/4.17 vs 4.5:1) and §D-7 (help copy in Fira Code at 16.4px). Neither re-measured in pass 2 nor by this seat (the plate composites a live-ambient ground — not statically derivable). Note the border figure measures `--primary` at 30% alpha; the base token's WCAG guard covers only its solid form.

## DISSENT — preserved, not ruled away

1. **R1's severity ordering** (C-P2-1 at BLOCKER; D-9, D-6, L-3+L-14, L-11 at MAJOR) is preserved against this seat's demotions. R1's strongest unadopted argument: the broken-file terminal state is a permanent lie whose copy actively instructs the user to repeat what they already did — if the owner weighs terminal lies above trigger frequency, R-4 promotes.
2. **R2's severity floor** (D-1 and D-5 at MAJOR, "not an app-level stop") is preserved against this seat's two BLOCKER rulings.
3. **R2's K-14 finding** contained real craft (the diff enumeration, the vite-twin contrast) — the kill is recorded with the note that the finding's SHAPE (test the resolution, don't assume it) is exactly what this seat then did to kill it; the traceResolution command belongs in the W-HYGIENE parity wave's born-RED kit alongside the generator-parity command.
4. **Reader agreement risk**: the two readers agreed on 20+ rows that both inherited from the same three challenge axes. This seat re-derived the load-bearing facts against the tree (every roster row above carries a "verified" mark where this seat's own instrument ran) but did not re-execute the browser probes; the probe-derived numbers (crop fractions, 0.3s measurements, chip opacity) are adopted as the challengers' committed evidence cells, labeled by engine where the source states it.

## Closing verdict

**ADJUDICATED — 33 confirmed (2 BLOCKER · 9 MAJOR · 20 MINOR · 2 INFO) · 14 killed (one of them a reader-MAJOR, killed by this seat's own traceResolution witness) · 9 missed findings adopted · 7 live-residue rows for SS-8.** The component is a well-intentioned instrument whose stated law is right and whose architecture betrays it three ways: the stage lies about the specimen (R-1), the sampling capability is pointer-only (R-2), and every failure is silent (R-4/R-10). ImageDropZone.vue has NO tranche-X wave owner — the roster above is the NO-WAVE-OWNER register's input for the next formation boundary, with exactly three rows routed today: R-9 + R-28 → X-W1, R-26 already FOLDED at the ColorSpaceSelector L-14 identity (W-HYGIENE). Nothing in this record stamps a wave; every anchor re-resolves at execution (D-19).

— end of apotheosis. The r2 challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
