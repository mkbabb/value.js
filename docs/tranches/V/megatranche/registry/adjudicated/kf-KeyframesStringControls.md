claude-fable-5

# kf-KeyframesStringControls — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Anchors**: value.js @ `c654824e` (branch `tranche-u`, the registry) · keyframes.js checkout `8281638c` (branch `master`) with a **DIRTY working tree = the audited tree** (SCH-1, per the banked kf-ChannelOptions record). Substrate facts re-measured by this seat: `KeyframesStringControls.vue` and `animationOptionsStore.ts` are **byte-equal to `origin/master`** (0-line diffs); the composable closure is near-master (10–19 line diffs); the emit chain (`backward.ts` 399 lines vs master / 143 vs `8281638c`; `backward-walk.ts` 154/114) **diverges from BOTH refs** — every emit-chain ruling below is a read of today's disk, and re-anchoring precedes any cure (KF.W0 law). Installed producer `@mkbabb/glass-ui` 7.0.0 (dist is the contract of record); `origin/master:package.json:77` carries `"@mkbabb/glass-ui": "7.0.0"` (my own `git show`).
**Corpus**: three axis challenges (D 26 defects/6 superlatives · L 22/4 · C 15/4, all `claude-opus-5[1m]`, dated 2026-08-05) at `docs/tranches/V/megatranche/audit/kf-components/KeyframesStringControls/`, attacked by two hostile re-readers — **RR-D** (design-and-usage) and **RR-L** (library-and-code). Both returns were themselves challenged by this seat before adoption.
**Method (L-18/M-23)**: every verdict-level disagreement between the re-readers (L-BL-3's grade, D-7's grade, L-M-5's grade, L-M-10's grade, D-3's grade, RR-D's C-5 sub-claim vs RR-L's kill of it) was re-derived against the tree with this seat's own instruments; every kill was re-executed at its decisive file; every net-new "missed" find was checked against the **banked registry** before being accepted as net-new — and two of them fell to the identity guard (they were already banked at kf-CSSCodeEditor). One corpus evidence cell fell to **tree motion** (the worktree moved between 2026-08-05 and today). No browser tooling (law); all rulings static + installed-dist + git reads; the irreducibly visual residue rides to SS-13/KF.W9.
**Arbiter's own instruments this session (all read-only; this file is the only write)**: `KeyframesStringControls.vue` whole (186 L); `useKeyframesState.ts` whole (62 L); `useKeyframeBrushApply.ts` whole (47 L); `useApplyCSS.ts` whole (70 L); `useHighlightCSS.ts:1-70`; `useKeyframesParsing.ts` whole (119 L); `useKeyframeOps.ts:1-110`; `CSSCodeEditor.vue` targeted (:38, :101-120, :144-152, :170-183 + the 3-arg `debounce` literal at :114-119); `helpers.ts:15-24` (two-parameter `debounce`, no third param); `refusal-probes.ts:1-30` (**union = 4 members**, verbatim); `backward.ts` :350-395 (walkList dispatch, name de-dup, `staggerDelays` opts-only, `chunks.length===0` return) + the refusal/null grep (**4 `refusals.push` sites, each immediately followed by `return null`** — :194-195, :208-217, :234-243, :266-274); `backward-walk.ts` (`cssIdent` :145 + applications :89/:110/:131); `format-options.ts:74` + `format.ts:339` (raw `${name}` interpolation); `css-text.ts:54` (the `TypeError("Value returned an unserializable CSS color.")` throw, verbatim); `catalog.ts:305-318` (`definePreset` today at :310-316 — tree moved); `ChannelOptions.vue` setter census (:36/:55/:83/:104/:128 — zero projector calls); `controlOptionsStore.ts` (`useStorage` :49; `selectedKeyframesControl` type :20 / default :39); spring chain (`SpringScene.vue:67/:198` `tabsContent = () => h(SpringPhysicsFacet…)`; `SpringPhysicsFacet.vue:119` `<KeyframesEditor :animation="demo.springEditAnim">`; `useSpringDemo.ts:406-409` Sweep = springEditAnim); whitespace names (`useSpringKeyframesEditor.ts:65`, `useAmigaDemo.ts:144/:146`, `SquareScene.vue:154` "Transform"); `scene-facility/index.ts:92` + `controlSurfaces.ts:104-107` (animation ⇒ triad); `ChannelControls.vue` :130/:136/:142/:252-253/:361; `RibbonBar.vue` :13/:19-53 (four `?.` handlers; apply state = class swap only; no `aria-pressed`); `KeyframesEditor.vue` :65/:90/:94/:180-205 (the sibling contrast: `role="toolbar"`, `:aria-pressed`, visible brush in a real button, `startDiagnosticId` + `toast.dismiss`); `useKeyframesPaneReveal.ts:123-136` (reveal-focus watch, verbatim); `useControlsKeyboardShortcuts.ts:60-70` (`Mod+S` → `copyCSS` with `preventDefault: true`); installed glass-ui `dist/keyboard.js:51` (`e.closest(".monaco-editor")` in the skip predicate, verbatim); `@vue/runtime-core/dist/runtime-core.d.ts:1554-1556` (**`defineAsyncComponent<T>` returns `T`** — the C-5 sub-claim decider); `keyframes/index.ts:1-12` (the barrel's lazy-boundary docblock); `useKeyframesEditor.ts:27-56` (20-member return); executed greps: `keyframes-style` → **2** (both useKeyframesState.ts) · `parseErrorShake` → 2 · `tabsListEl` (keyframes subtree) → declaration/read/return/barrel only, **no template binding** · `selectedKeyframesControl` → **exactly 3** (type/default/read — **no writer**) · `kfControls.keyframes` → **1** (the write at useKeyframeOps.ts:60 — **write-only**) · `clear` in the brush/apply consumers → **0 callers** · `templateFrameStrings` in the SFC → 0 · `setModelMarkers|aria-invalid` → 0 · `tap-floor` → 2 (both design-idioms.css — zero consumers) · `--panel-max-h` → **2 live consumers TODAY** (AnimationControlsGroup.vue:85, KeyboardShortcutsModal.vue:10 — the corpus's "zero consumers" cell is stale) · `cssIdent` in `public.ts`/`load-engine.ts`/`index.ts` → **0** (re-exported only at `compile/emit/index.ts:53`) · `reverseCSSTime|serializeTimingFunction|convertPixelsToCh|debounce` in the published surface → 0 · glass edges `CSSCodeEditor.vue:38` + `useHighlightCSS.ts:2` (`@mkbabb/glass-ui/dark`) · `updateAllStrings()` external callers → mutation sites only (KeyframesEditor.vue:280, useKeyframeOps.ts:181 — **ChannelOptions calls none**). Banked records leaned on (citable; axis assertions are not): kf-CSSCodeEditor (KF-CE-1/-2/-3/-6/-7/-8/-15/-16/-36/-42), kf-DemoGlobalChrome (D-1/L-1/C-1 + M-1 + D-2/S-9), kf-ChannelControls (§0/C-1, D-2/C-3, D-5, C-7, D-14, N-4, N-10, S-A), kf-ControlsPaneWrapper (M-4, MISS-3), kf-AnimationControlsGroup (M-8, L-4(a)), kf-App (KF-APP-4), kf-ChannelOptions (SCH-1 + the re-anchor law), census `lane-frontend.md` F-1 + `CENSUS-2026-08-03.md:177-206` (the KF.W0–W10 taxonomy).

## Scoreboard

**56 ADJUDICATED-CONFIRMED (4 BLOCKER · 22 MAJOR · 27 MINOR · 3 INFO) — 45 booked here, 11 FOLDED by reference to banked kf-* / census identities (identity guard, zero re-bookings) · 11 KILLED (with executed refutations, incl. one kill-of-a-confirm inside RR-D's return and two identity-guard kills of "net-new" status) · 15 reader-missed rows adjudicated (10 banked N-1..N-10, incl. one merged pair; 3 folded; 1 rescoped-latent; 1 riding an existing row) · 8 UNPROVEN-NEEDS-LIVE residue rows to SS-13/KF.W9 (3 prior residue rows RETIRED — settled at the banks).**

The net-new blocker of this corpus: **N-1 (RR-D)** — a Controls-tab option edit is never re-projected into the keyframes buffer, and the next keystroke in the editor **reverts the user's edits in both the live animation AND the persisted store**, under a celebratory "Keyframes parsed 🎉". Silent destruction of authored work sits on the BLOCKER rung by the kf-ChannelControls L-2/C-2 precedent. The two axis blockers survive whole: **L-BL-1** (Apply CSS adds a class no emitted rule can match — the affordance is inert on every animation and freezes the JS playback with no CSS replacement) and **L-BL-2** (whitespace animation names ship invalid CSS idents into the buffer from first paint and make `classList.add` throw on live scene channels). Their shared root cause is banked as **N-8**: one animation carries THREE different names inside one component, while the library owns the single normalizer (`cssIdent`), applies it on the export path only, and does not publish it.

Status verbs per M-25: dispositions are **ADJUDICATED**, not VERIFIED — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets from the census taxonomy (`CENSUS-2026-08-03.md:177-206`): **KF.W0** (substrate settle · F-1) · **KF.W3** (parser consumption / R1) · **KF.W4** (gates · vue-tsc) · **KF.W5** (library tri-fold · public surface) · **KF.W6** (glass suffusion) · **KF.W8** (structure/colocation) · **KF.W9** (Safari visual audit = SS-13). Editor-pipeline rows agglomerate into the banked **EDITOR-UNIT** (minted at kf-CSSCodeEditor); the apply/name/feedback rows that no census row owns are **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring (proposed agglomeration: **APPLY-UNIT** — the apply-CSS toggle, the name pipeline, the brush, the teardown).

## Ruled disagreements (the seat's actual work)

| id | RR-D (design-and-usage) | RR-L (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-BL-3/D-6/C-1 grade** | MAJOR (filed as D-6) | BLOCKER ("silent failure + permanent latch for the component's life") | **MAJOR — RR-D's grade** | No data loss, no wrong artifact: a prettier rejection silently latches `isFormatting` and mutes one toast channel (:83-88/:101, my read). The BLOCKER rung stays reserved for destroyed work (N-1) and dead affordances (L-BL-1/2). The heavier fact on the same button — even the SUCCESS path never persists (`formatCSS` emits nothing) — is **already banked at KF-CE-7** and may not be double-weighted here. |
| **D-7 grade** | MINOR (demoted; the human sentence IS shown, branch reachable only on refusal; union corrected to 4 members) | MAJOR (typography inverts the file's stated pedagogy) | **MINOR — RR-D wins** | My reads: `:154-158` puts the kebab slug in the bold title and `refusal.message` in the small description; `refusal-probes.ts:4-8` = FOUR members (RR-D's kill of the D axis's ten-member enumeration re-verified — the six `entry-*` slugs belong to a different union `compileToCSS` never reaches). RR-L offered no answer to the demotion rationale. And under the banked DemoGlobalChrome D-1 blocker, no toast paints at all today — every toast-content row here is latent-until-cure. |
| **L-M-5 grade** | MINOR | MAJOR ("its deadness is what makes the staleness silent") | **MINOR as mechanism — consequence weight booked ONCE at N-1** | My own derivation, sharper than either reader's: `watch(animation.templateFrames, …)` (:96-103) captures the setup-time plain array; plain-object elements register **zero reactive deps**, so the watcher effect has nothing that could ever re-trigger it — inert not merely by value-comparison but by dependency-graph emptiness; and `adoptCompiled` transplants the compiler out from under the held reference regardless. The 10-line comment (:86-95) asserts an ordering invariant not in force. Grading the dead watch MAJOR *and* the staleness BLOCKER would count one defect twice. |
| **L-M-10 grade** (+ N-3's grade, same logic) | MINOR | MAJOR (N+1 prettier formats per pause; per-keystroke localStorage serialize) | **MINOR — cost, not breakage** | Per the kf-ChannelControls precedent ("runs and does nothing" ≠ breakage), recurring waste on a debounced path books MINOR until SS-13 measures a felt INP cost; both rows carry an explicit escalation hook in the residue. RR-L's grades preserved in DISSENT. |
| **D-3 grade** | MAJOR | MINOR (glass-ui `keyboard.js` hard-excludes `.monaco-editor`, so a registry binding CANNOT serve this context — the bespoke listener is the only mechanism that works; only the literal is the defect) | **MINOR — RR-L wins** | My read of installed `keyboard.js:51`: `e.closest(".monaco-editor")` is in the skip predicate, verbatim. The cure is not "move it to the registry" (impossible in-context) but "spell the chord portably" — the portable form exists in the same folder (`KeyframesAddDialog.vue`, both readers). The `Ï` literal itself is prior art (U-audit lane-18 F6, disclosed by the D axis). |
| **RR-D's C-5 sub-claim** ("`InstanceType<typeof KeyframesStringControls>` at ChannelControls.vue:372 types the async wrapper, which lacks the expose surface") | CONFIRMED as part of C-5 | **KILLED**: `defineAsyncComponent<T>` returns `T`, so the hop is correctly typed; the `any` collapse is strictly downstream | **KILL UPHELD — a kill-of-a-confirm inside RR-D's return** | My own read of `runtime-core.d.ts:1554-1556`: `defineAsyncComponent<T extends Component>(source: …): T` — the wrapper IS the resolved SFC type; `InstanceType` carries the `defineExpose` surface. Independently corroborated by the banked kf-ChannelControls C-7 precision note ("typed InstanceType at :372, NOT any — the defect is nullability, not typing"). Repair-direction consequence: adding a named expose interface buys nothing until `AnimationControlsGroup.vue:191`'s `reactive<Record<string, any>>` stops erasing it — and that line is already banked (kf-ACG M-8/L-4(a)). |
| **RR-D's missed-M2 reachability** ("arrowing Controls → Keyframes lands the user inside the tabpanel; a keyboard user cannot get past Keyframes") | Filed MAJOR as live | (not raised) | **RESCOPED to LATENT — seat's own kill of the scenario** | The pill tablist **never renders in the shipped app**: the banked kf-ChannelControls §0 row (re-verified there at this HEAD: `TABS_EXTERNALLY_MANAGED_KEY` provided `true` unconditionally; ChromeDock draws a `<Select>`). No arrow traversal through `useKfPillTabs` exists to be defeated. What IS live: the reveal-focus watch (my read, `useKeyframesPaneReveal.ts:123-136`) moves focus to the unnamed panel on every activation — via Select, a recorded design choice (banked S-A credits it; banked N-10 bounds the N-host fan-out). The two-competing-`focus()` fact is real and latent; it books at N-9 and rides KF.W6 (the S-1 tabs decision must reconcile the two owners). |
| **D-11's "unused `--panel-max-h`" cell** | "two definition/prose hits and ZERO consumers" | "correctly left…" (inherited the zero) | **CELL STALE — tree motion, caught by this seat** | My grep TODAY: **2 live consumers** (`AnimationControlsGroup.vue:85` `max-h-[var(--panel-max-h)]`; `KeyboardShortcutsModal.vue:10`), matching the kf-KeyboardShortcutsModal seat's Aug-9 count. The worktree moved under the corpus between Aug 5 and Aug 9. The row SURVIVES STRENGTHENED: the token idiom is live and this callsite bypasses it. Recorded in DISSENT as the tree-motion law. |
| **"Net-new" status of RR-L's MISSED-2 and RR-D's missed-M7** | M7: "monochrome editor + wasted css.worker — missed by all three axes" | MISSED-2: "format never reaches the model — nobody noticed the happy path is broken" | **BOTH ALREADY BANKED — identity-guard kills of the net-new status (the facts are right)** | MISSED-2 ≡ **KF-CE-7** ("`formatCSS()` never reaches the model… the sibling's `isFormatting` latch is a compensator for an emit that does not exist" — the bank even names THIS component's latch). M7 ≡ **KF-CE-1 + KF-CE-15** (the `5fe2e4cb` regression: no CSS tokenizer, monochrome; the css.worker fetched and never instantiable). Folded with component-side riders; zero re-bookings. Method law recorded in DISSENT: a registry sweep precedes any "missed by all axes" claim. |

## Adjudicated defect roster

Ids keep their axis names for life; reader-missed finds bank as N-1..N-10. Line anchors are today's disk over checkout `8281638c` (SCH-1); **re-anchor at `origin/master` before executing any cure** — the emit chain diverges from both refs.

### BLOCKER

- **L-BL-1 — ADJUDICATED BLOCKER · "Apply CSS" adds a class no emitted rule can ever match; the affordance is inert on every animation AND freezes the JS playback with no CSS replacement.**
  Proof (this seat's own, statically closed): `useKeyframesState.ts:16` `keyframesStyleId = \`keyframes-style-${animationUUID}\`` and `:41-43` `getTmpAnimationName()` = the SAME id with the prefix STRIPPED and lowercased — the derivation exists precisely to remove the prefix the class keeps. `useKeyframeBrushApply.ts:33` `getClassName: () => options.styleId`; `useApplyCSS.ts:47-49` pushes that token into every target's classList after `:44-45` pauses the animation (`animation.paused = animation.started`); the injected sheet's selector is `.${getTmpAnimationName()}` (`useKeyframesParsing.ts:37-40` → `format-options.ts:74`, raw interpolation — my read). The two strings are constructed so as never to be equal. Falsifiers re-executed: `keyframes-style` grep → exactly 2 hits, both the derivation pair; no markup carries the tmp class. Net: press Apply → subject freezes, sheet binds nothing.
  Terminal disposition: **NO-WAVE-OWNER (APPLY-UNIT input for SS-1/SS-2)**. Cure shape constrained by N-8: `getClassName` and the emitted selector must share ONE `cssIdent`-derived name — not a second hand-rolled derivation.
- **L-BL-2 — ADJUDICATED BLOCKER · animation names containing whitespace emit invalid CSS idents into the buffer from first paint, and make `classList.add` throw mid-apply.**
  Proof: live whitespace names verified on disk (`useSpringKeyframesEditor.ts:65` "Spring Keyframes"; `useAmigaDemo.ts:144/:146` "Bouncing X"/"Bouncing Y"); those channels earn the keyframes surface by construction (`scene-facility/index.ts:92` maps every group animation to a channel; `controlSurfaces.ts:104-107` grants the triad to any channel with an `animation`; spring's Sweep IS `springEditAnim` — `useSpringDemo.ts:406-409`). The projection interpolates the name raw (`format-options.ts:74`, `format.ts:339` — my reads), so the buffer opens `.spring-spring keyframes { … }` / `@keyframes spring-spring keyframes`. The class token `keyframes-style-spring-Spring Keyframes` contains ASCII whitespace — DOM-Standard "validate token" throws `InvalidCharacterError`, landing after `setContent` (`useApplyCSS.ts:46`) and before `isApplied = true` (`:50`). The observed throw + the parser's verdict on the two-ident prelude are the residue's top two rows.
  Terminal disposition: **NO-WAVE-OWNER (APPLY-UNIT)**, cure via N-8. The escalation "the Export artifact is equally invalid" is REFUTED: `walkList` applies `cssIdent` (`backward-walk.ts:131`), so Export emits a valid ident for the same animation.
- **N-1 (RR-D net-new; banked) — ADJUDICATED BLOCKER · the keyframes pane goes silently STALE on any Controls-tab option edit, and the next keystroke reverts the user's edits in both the animation and the persisted store, under a success toast.**
  Proof (chain re-executed by this seat): `ChannelOptions.vue` setters (:36 `setDuration`, :55 `setDelay`, :83 `setIterationCount`, :104 `setDirection`, :128 `setFillMode`) mutate the LIVE animation and call **no projector** (my read of the file's setter census); `updateAllStrings` has exactly two external callers, both keyframes-side mutation sites (my grep) — no re-projection path exists from an option edit to `cssKeyframesString`. Both watchers in `useKeyframesParsing` are dead (L-M-5 by dependency-graph emptiness — my derivation; N-4 by writer absence — my 3-hit grep). The one-shot projection (`KeyframesStringControls.vue:123-125`) cannot re-run: the pane is force-mounted, never torn down (banked kf-ChannelControls). Then one keystroke → `onEditorChange` (:98-101) → `useKeyframeOps.ts:59-70` parses the STALE buffer and `adoptCompiled`s it over the animation → `:72-81` rewrites `stored.duration/delay/iterationCount/direction/fillMode` FROM the reverted animation into the `useStorage`-backed store (`controlOptionsStore.ts:49`). The user's edits are destroyed in both places; the only feedback is "Keyframes parsed 🎉" (:101). Data loss ⇒ BLOCKER (the kf-ChannelControls L-2/C-2 precedent).
  Terminal disposition: **NO-WAVE-OWNER**, and it MUST be settled with the EDITOR-UNIT's projection contract — any cure for the echo loop (KF-CE-8) that does not also define the model→editor re-projection path leaves this loss live. Live corroboration witness → SS-13 #4.
- **D-1 — BLOCKER · FOLD → banked kf-DemoGlobalChrome D-1/L-1/C-1 (the vue-sonner stylesheet is imported nowhere; the toaster is never `position:fixed`; every toast is structurally unreachable). Identity guard — NOT re-booked.**
  Component riders this record contributes: (i) all **eight** of this component's user-facing messages (:101, :105, :139, :144, :146, :155, :162, :175) are toasts — the component's ENTIRE feedback channel rides the unreachable surface, so today every failure here is fully silent-but-for-console; (ii) both re-readers graded D-1 MAJOR-for-this-component with the rendered landing "unproven" — the bank settles it statically (BLOCKER; after-the-fold geometry), and that residue row is RETIRED here; (iii) any local cure must respect the banked M-1 constraint (`layer(vendor)` import + the `z-toast` rung — never the bare import the axes prescribed) and the S-9 glass-toast swap owns the terminal state.

### MAJOR

- **N-8 (merged: RR-D missed-M3 ∪ RR-L MISSED-8; banked) — MAJOR · one animation, three names, inside one component — and the library owns the single normalizer, applies it on Export only, and does not publish it.**
  My reads: (1) class token `keyframes-style-${superKey}-${name}` (useKeyframesState:16); (2) editor ident `${superKey}-${name}`.toLowerCase() by string surgery (:41-43 → format-options:74/format.ts:339); (3) export ident `cssIdent(getAnimationId(anim))` (`backward-walk.ts:131`, `cssIdent` at :145). Worked example on the shipped square scene (`anim.name = "Transform"`, SquareScene.vue:154): class `keyframes-style-square-Transform`, buffer `@keyframes square-transform`, Export `@keyframes Transform` — paste the exported artifact back into the editor and it is a different animation, falsifying the file's own round-trip pedagogy (:127-132). Publication gap verified: `cssIdent` re-exported ONLY at `compile/emit/index.ts:53`; absent from `public.ts`, `load-engine.ts`, `index.ts` (my greps) — neither `kfEngine()` nor `loadAnimationEngine()` can reach it, which is WHY the demo hand-rolls `.replace().toLowerCase()`. Root cause of L-BL-1/L-BL-2's repair shape.
  Disposition: library half (publish `cssIdent`) → **KF.W5** (public-surface decision, alongside C-8); demo half (route all three derivations through it) → **NO-WAVE-OWNER (APPLY-UNIT)**, sequenced after the publication.
- **N-2 (RR-L net-new; banked) — MAJOR · three concurrent `useKeyframesEditor` instances run over ONE animation in the shipped spring scene: a shared `<style>` node with first-unmount-wins teardown, three independent apply state machines over one `animation.paused`, and divergent buffers with a silent edit-loss path.**
  Chain re-executed: `SpringScene.vue:67` `tabsContent = () => h(SpringPhysicsFacet, { demo })` (no surface gate) + `:198` exposes it; `SpringPhysicsFacet.vue:119` mounts `KeyframesEditor` on `demo.springEditAnim`; Sweep channel IS `springEditAnim` (useSpringDemo:406-409); the wrapper mounts EVERY channel under `v-show` and forwards the slot to all hosts ungated — that mechanism is **banked (kf-ControlsPaneWrapper M-4 + MISS-3; folded, not re-booked)**. Net: 2× KeyframesEditor (slot copies) + 1× KeyframesStringControls (Sweep's force-mounted pane) = three closures over one animation. Consequences verified here: all three compute the same `keyframesStyleId`; `useHighlightCSS.ts:31-40` REUSES the id-matched node, `:58-61` unconditionally `.remove()`s it on first unmount — survivors hold a detached reference and their `setContent` writes go nowhere; three private `prevPaused`/`isApplied` closures over one `animation.paused` restore stale pause state on interleaved toggles; cross-instance refresh depends on the dead structural watch (L-M-5), so a facet edit never reaches the Monaco buffer and the next keystroke there destroys it (N-1's mechanism, second trigger). Disposition: **NO-WAVE-OWNER (APPLY-UNIT + the banked mount-policy row)**; live ordering witness → SS-13 #3.
- **N-5 (RR-L net-new; banked) — MAJOR · `useApplyCSS.clear()` has ZERO callers, and the injected sheet is never refreshed while applied — unmount-while-applied leaves the animation pinned paused and the class on every target while the sheet is removed; editing while applied silently desynchronises the preview.**
  My reads: `setContent(getCSSString())` appears ONLY in `toggle()`'s apply arm (:46) — no watcher on the string; `useKeyframeBrushApply.ts:29` destructures `{ isApplied, toggle }` only, `:43`'s `onUnmounted` pauses the brush and nothing else; my grep for `clear` across the consumers → 0 callers. So: unmount while applied ⇒ `animation.paused` stays at the forced value from `:45`, the class stays on the targets, and `useHighlightCSS`'s `onUnmounted` removes the sheet — the subject stays frozen with no CSS replacement, permanently. This DEMOTES the corpus's S-6 superlative: the toggle's reversibility is real (my read of `:34-41` and the independent `clear` at `:54-66`) but reachable only through paths nobody wires. Disposition: **NO-WAVE-OWNER (APPLY-UNIT)**.
- **D-6 / L-BL-3 / C-1 — MAJOR (RR-L's BLOCKER killed per ruling) · `formatEditor` has no error posture: a prettier rejection latches `isFormatting` true for the life of the never-unmounted instance, permanently muting the success channel, with both callsites bare.**
  Verified: `:83-88` sets the flag, awaits `editorRef.value.formatCSS()`, resets only on the success path; `:101` gates the sole success toast on `!isFormatting.value`; `formatCSSContent` (CSSCodeEditor:178-183) has no try over `prettier.format(raw mid-edit text, { parser: "scss" })`; callsites `:93` (discarded promise in a sync keydown → true unhandled rejection) and `RibbonBar.vue:28`. The house idiom sits unused in the same closure (`withErrorToastAsync`, useKeyframeOps:25-40, toast + Retry — my read). Cure = one `finally` + the idiom. Disposition: **NO-WAVE-OWNER** (EDITOR-UNIT adjacency — its success-path twin is banked KF-CE-7).
- **C-2 — MAJOR (RR-L's demotion from BLOCKER adopted) · the pane's only content-production path is unguarded over a throwing, value.js-bearing serializer; a mount-time rejection boots Monaco empty, indistinguishable from "no keyframes", with no remount available.**
  Verified: `:123-125` is the sole mount writer, no try; the chain ends at `css-text.ts:54` `throw new TypeError("Value returned an unserializable CSS color.")` (verbatim, my read); every sibling op routes through `withErrorToastAsync` with Retry — only the mount path is bare. Reachability of the `!ok` arm is the open value.js **R1** question (banked; folded by reference — not re-booked). Disposition: absence-of-posture → **NO-WAVE-OWNER**; reachability → **KF.W3** (parser consumption / R1 surface).
- **D-4 / L-M-3 / C-3 — MAJOR · `presets.shake()` is constructed target-less and never given targets — the parse-error motion affordance animates zero elements, while its construction cost (a full `fromString` parse) is paid at every setup.**
  Verified: `parseErrorShake` grep → exactly 2 hits (:121 construction, :103 play); `definePreset` (catalog.ts:310-316 TODAY — the tree moved; both readers' cites off by one against today's disk) passes no targets; `setTargets` is the only populator and is never called on this instance. The folder's own idiom is the opposite (`KeyframesEditor.vue` sets targets on its presets). Disposition: **NO-WAVE-OWNER (APPLY-UNIT — the error-affordance family with D-19)**.
- **D-5 / L-M-4 / C-4 — MAJOR · the brush animation's sole target carries `class="hidden"`; the Apply-CSS motion feedback is invisible while an `iterationCount:"infinite"` loop runs indefinitely in a pane that never unmounts.**
  Verified: template `:15-18`; `useKeyframeBrushApply.ts:18-27` (700 ms infinite alternate), `:38` plays per apply-on, `:42` `setTargets(brush.value!)`; the composable's contract makes `templateRef` mandatory, forcing this consumer to fabricate a decoy — the locus is the contract. The visible brush is a different element (`RibbonBar.vue`), and the sibling proves the intended shape (`KeyframesEditor.vue:90-94`: the brush inside a real `aria-pressed` button). Disposition: **NO-WAVE-OWNER (APPLY-UNIT)**.
- **L-M-6 / C-15 — MAJOR · a fatal, unnecessary synchronous engine dependency: `kfEngine()` throws-if-unwarmed in the setup body (:44-45) AND at module scope in `useKeyframeBrushApply.ts:6` (the throw lands during the chunk's dynamic import); a failed warm renders the tabpanel blank with no signal.**
  Verified at both sites; `main.ts` swallows a failed warm and mounts anyway (both readers, consistent); the bare `defineAsyncComponent` wrappers with no `errorComponent` are **banked (kf-ChannelControls D-5 — folded)**. The coupling buys nothing load-bearing: `CSSKeyframesAnimation` is dead here (D-23), `presets.shake()` is inert (D-4), `compileToCSS` is used only inside an async handler where the sibling idiom (`await loadAnimationEngine()`) is non-fatal. Disposition: **NO-WAVE-OWNER**.
- **L-M-2 / C-11 — MAJOR · `tabsListEl` is never bound by any consumer, so the composable-side `getFormatWidth()` always returns `undefined` and all composable-side formatting falls back to `printWidth: 80` — colliding with CSSCodeEditor's own real container-width format on the same buffer.**
  Verified: my grep — declaration/read/return/barrel re-export only, no `ref="tabsListEl"` anywhere (the transport `tabsListElRef` is a different identifier); `el.offsetWidth == null` unreachable (:34); CSSCodeEditor's REAL width at :107-110. Two print widths, one buffer — the mechanical half of the echo loop's non-identity round trip (banked KF-CE-8). Disposition: **NO-WAVE-OWNER (EDITOR-UNIT adjacency: one width authority)**.
- **D-9 — MAJOR · the component's four actions live in a detached ribbon with no `role="toolbar"`, no accessible name, no roving tabindex, and no `aria-pressed` on the Apply toggle — state THIS component owns, rendered color-only.**
  Verified: `RibbonBar.vue:13` plain div gate; `:48-53` apply state = `rainbow-vivid` class swap; sibling contrast at `KeyframesEditor.vue:65` (`role="toolbar"` + label + keydown) and `:90` (`:aria-pressed="cssApplied"`). Pairs with the banked kf-ChannelControls C-7 (readiness/disabled) — one lever-binding spec. Disposition: **NO-WAVE-OWNER**.
- **D-11 — MAJOR · `height="450px"` is a raw viewport-bearing geometry literal at a callsite, against DESIGN.md §4 — beside a geometry token family that is LIVE on today's disk (the corpus's "unused token" cell is stale; 2 consumers now exist), which sharpens the bypass.**
  Verified: `:7`; my `--panel-max-h` grep (AnimationControlsGroup.vue:85, KeyboardShortcutsModal.vue:10). ≈67dvh at 667px inside the pane's `overflow-y-auto` flex body. Scroll capture → SS-13 #6. Disposition: **NO-WAVE-OWNER**.
- **D-13 / L-M-7 / C-6 (+ N-6 joined) — MAJOR · `copyCSS` silently no-ops on the empty buffer and silently fails on clipboard rejection — bound to `Mod+S` with `preventDefault: true`, a binding that is additionally DEAD exactly where the user works.**
  Verified: `:173-177` (guard, no else, no try; `cssKeyframesString` starts `""` and fills async, so the no-op path is live at first paint); registration at `useControlsKeyboardShortcuts.ts` (my read: `Mod+S → copyCSS`, `preventDefault: true`); and glass-ui `keyboard.js:51` skips any target inside `.monaco-editor` — so with the caret in the editor the shortcut never fires and the browser's Save dialog opens instead (N-6, RR-L). The asymmetry is in the same expose block: `exportCompiledCSS` wraps everything. Disposition: **NO-WAVE-OWNER**.
- **D-21 / C-5 — MAJOR · `defineExpose` is an inferred object literal (banned verbatim by DESIGN.md §9.2's "named interface, never an inferred object"), and the consumer chain erases it to `any` downstream — a rename degrades to a silent no-op button under a green `tsc`.**
  Verified: `:171-182`. Scope per ruling: the ChannelControls:372 hop is CORRECTLY typed (RR-D's sub-claim killed — my d.ts read); the erasure locus (`AnimationControlsGroup.vue:191` `reactive<Record<string, any>>`) and the nullability window are **banked (kf-ACG M-8/L-4(a); kf-ChannelControls C-7 — folded)**. Booked here: the component-owned named-interface violation, with the repair-order law that the interface and the registry typing must land together. Disposition: **NO-WAVE-OWNER** (L-BL-4's fold is why it compiles).
- **C-8 — MAJOR · the SFC's barrel-clean imports are cosmetic: its closure reaches four unpublished `@src/` deep paths, plus the fifth and load-bearing gap (`cssIdent`, N-8).**
  Verified by my greps: `reverseCSSTime` (useKeyframeOps:1), `serializeTimingFunction` (parseAnimationCSS:7), `debounce` (useKeyframeOps:4, useKeyframesParsing:1, CSSCodeEditor:42-area), `convertPixelsToCh` (useKeyframesState:1, CSSCodeEditor:40) — zero of these names in `index.ts`/`public.ts`/`load-engine.ts`. The CSSCodeEditor pair is **banked (KF-CE-42 → KF.W8; folded)**; the composable-side set + the publication decision book here. Disposition: **KF.W5** (surface decision) + **KF.W8** (colocation).
- **D-2 / L-M-8 — MAJOR · FOLD → banked KF-CE-36 (the success-toast-per-200ms-pause storm; EDITOR-UNIT). Identity guard — NOT re-booked.** Riders this record adds: no dedupe `id` on any toast in this file while the sibling's replaceable-diagnostic idiom sits one file away (`startDiagnosticId` + `toast.dismiss`, my read of KeyframesEditor:180-205); the eviction arithmetic (3-slot viewport, 4 s lifetime vs the 10 s error toast) is settled at the DemoGlobalChrome bank's constants; and the storm is latent today under the D-1 fold (no toast paints at all).
- **D-10 — MAJOR · FOLD → banked kf-ChannelControls D-5 (bare `defineAsyncComponent`, no loading/error component) + C-7 (the four ribbon buttons render live-and-inert through the load window). NOT re-booked.** Riders: DESIGN.md §9.1 names the exact cure (`loadingComponent` + a `.skeleton.vue`; only `App.skeleton.vue` exists in the tree — both readers, consistent); the duplicate lazy boundary (C-13) violates the barrel's own stated purpose.
- **D-12 (+ RR-D missed-M5) — MAJOR · FOLD, split at the identity seam: the unnamed tabpanel + absent tab↔panel ARIA (both sides — no `id`, no `aria-controls`, no `aria-labelledby`) → banked kf-ChannelControls D-2/C-3 (+N-4); the editor's own a11y surface (`accessibilitySupport: "off"`, the keyboard trap) → banked KF-CE-3. NOT re-booked.** Component rider booked: this file passes no label of any kind to `CSSCodeEditor` while the reveal gesture programmatically focuses the unnamed box (my read of the reveal watch); M5's sharpening rides the bank — the panel-side fix is impossible without minting tab-side ids first (a two-file cure, not one attribute).
- **L-M-1 / C-7 — MAJOR · FOLD → banked KF-CE-8 (the external-sync watch rewrites the buffer under the caret on every lossy round-trip) + KF-CE-6 (`setValue` destroys undo/redo). NOT re-booked.** Component rider: THIS component is the "other parent" the bank names — its round trip re-serializes through prettier at a different width (L-M-2) ~300 ms after every pause (200 ms emit + 100 ms `debouncedUpdateAllStrings`, my reads), and it holds `editorRef` yet uses neither imperative escape hatch the editor exposes for exactly this.
- **L-M-9 — MAJOR · FOLD → census F-1 ≡ SCH-1 → KF.W0. NOT re-booked.** Riders: the two transitive critical-path edges verified by this seat (`CSSCodeEditor.vue:38` — this component's entire render; `useHighlightCSS.ts:2` via `useApplyCSS.ts:4` — the Apply-CSS driver); and the substrate correction: `origin/master:package.json:77` already carries `"@mkbabb/glass-ui": "7.0.0"` (my `git show`) — the phantom is a checkout-lag artifact on the audited disk; KF.W0's reconciliation discharges it, and the "clean `npm ci` fails" probe is RETIRED from the residue.
- **L-BL-4 — MAJOR · FOLD → banked KF-APP-4 (no SFC typecheck anywhere; → KF.W4) ≡ KF-CE-16 (this corpus's own specimen — `debounce(fn, 200, false)` into a two-parameter function — is ALREADY the banked TS2554). NOT re-booked.** Verified again today (:114-119 vs helpers.ts:15-24) purely to pin the anchor; RR-L's demotion note (repo-wide gate gap, not a component defect) matches the bank's framing.
- **N-2's mount-mechanism half, D-10's window, D-12's panel naming, L-M-1's undo loss** — enumerated above as folds; listed once here so the fold count is auditable: **11 folds total** (D-1, D-2/L-M-8, D-10, D-12(+M5), D-16, L-M-1/C-7, L-M-9, L-BL-4, MISSED-2→KF-CE-7, M7→KF-CE-1/-15, and N-2's wrapper-mount mechanism→kf-ControlsPaneWrapper M-4).

### MINOR

- **D-3 / L-D-3 / C-9 — MINOR (per ruling) · the format shortcut tests `e.key === "Ï"` — macOS-composed, dead on Windows/Linux, absent from the shortcuts modal, `preventDefault()` on a printable key.** The registry CANNOT serve this context (keyboard.js:51 — my read), so the cure is a portable chord test in the bespoke listener (the form exists in `KeyframesAddDialog.vue`). Prior art: U-audit lane-18 F6. **NO-WAVE-OWNER.**
- **D-7 — MINOR (per ruling) · the total-refusal toast leads with the machine slug and demotes the library's pedagogic sentence to the small slot** (:154-158; `CompileRefusalReason` = 4 members, verified). Latent under the D-1 fold. **NO-WAVE-OWNER** (one-line swap; SUP-B protects the verbatim-message decision underneath).
- **L-M-5 — MINOR as mechanism (per ruling) · the "structural projector" watch is inert (zero reactive deps — this seat's own derivation) and the 10-line invariant comment above it is false.** Consequence weight booked at N-1. **NO-WAVE-OWNER** (delete the watch or make the source a getter over reactive state; correct the comment).
- **N-4 (RR-L; banked) — MINOR · the second dead watch: `selectedKeyframesControl` has NO writer anywhere (my grep: exactly 3 hits — type, default, this read), so `useKeyframesParsing` ships two watchers and zero live ones.** **NO-WAVE-OWNER** (dies with the N-1 projection-contract cure).
- **L-M-10 — MINOR (per ruling) · every typing pause runs N+1 prettier formats; N compute `templateFrameStrings`, which this component never renders** (my grep: 0 references in the SFC; the wide projector is driven via ops on every pause). Escalation hook → SS-13 #8. **NO-WAVE-OWNER.**
- **N-3 (RR-L; banked) — MINOR (per ruling) · `kfControls.keyframes` is a WRITE-ONLY localStorage-backed field, written on every keystroke pause BEFORE the parse — persisting even text that failed to parse, for zero readers** (my greps: 1 site total; `useStorage` at controlOptionsStore:49; the write at useKeyframeOps:60 precedes the parse at :64). **NO-WAVE-OWNER** (delete the field or read it; the S4 INP prose 30 lines below is contradicted by it).
- **L-M-11 — MINOR · `useHighlightCSS.ts` fuses two unrelated composables; this component pays the glass-ui edge for the half it does not use.** Verified by my whole-read: `useHighlightCSS` (:27-64) is a generic style-element lifecycle with zero highlighting — RR-L's misnomer rider adopted (it is `useStyleElement`), and the misnomer is what let three consumers share it unnoticed (N-2). **NO-WAVE-OWNER.**
- **D-14 — MINOR · emoji carry the affect into the live region; `🔧` labels two different failures, `🎉` celebrates per pause.** Latent under D-1; the announcement mechanics are settled at the DemoGlobalChrome bank. **NO-WAVE-OWNER** (dissolves under the S-9 tone axis).
- **D-15 — MINOR · register drift: marketing voice in a status toast; three modal registers for one event class in 55 lines; two names for one artifact** (:139/:144/:105/:146/:155/:175 — verified). Craft row, correctly filed. **NO-WAVE-OWNER.**
- **D-17 — MINOR · both root layout utilities are inert (`relative` contains nothing positioned; `min-w-0` on a block child of a block).** **NO-WAVE-OWNER** (hygiene).
- **D-18 — MINOR · both hand-rolled catch blocks reproduce `withErrorToastAsync`'s shape minus its Retry action, for idempotent-retryable failures** (:102-111/:161-167 vs useKeyframeOps:25-40 — verified). **NO-WAVE-OWNER.**
- **D-19 — MINOR · a parse failure has no durable representation: no `aria-invalid`, no Monaco marker, no inline region** (my grep: 0 hits; the editor exposes `editor()` so a caller COULD set markers). With D-2's eviction and D-4's dead shake, the failure state is unrepresented after ≤10 s — and today, under D-1, it is unrepresented from the start. **NO-WAVE-OWNER** (the honest cure is a marker, not a fourth toast).
- **D-20 (+ RR-D missed-M4 riding) — MINOR · the four ribbon buttons are authored `h-8` (32 px) beside the repo's own `.tap-floor` (44 px, WCAG 2.5.5) — and M4's sharpening is adopted: `.tap-floor` has ZERO consumers repo-wide (my grep), so the utility is policy-or-dead.** Rendered height under glass 7.0.0 `size="sm"` → SS-13 #7. Disposition: **KF.W6** (the size-scale/token decision belongs to glass suffusion; DESIGN.md §10 R7 owns the delete arm).
- **D-22 — MINOR · `keyframesUpdate` is the Noun+`Update` emit name the codex renames at the owning move, propagated verbatim across the chain** (re-emit verified at ChannelControls:142/:361). **NO-WAVE-OWNER.**
- **D-23 / L-D-1 / C-10 — MINOR · dead destructures (`CSSKeyframesAnimation` :44, `getTmpAnimationName` :65 — each exactly once, at its binding) + a header comment (:38-43) documenting a symbol this file does not import.** The dead engine binding is one third of L-M-6's fatal coupling. **NO-WAVE-OWNER.**
- **L-D-2 — MINOR · three of four props restate CSSCodeEditor's own defaults (:8-10); only the D-11 literal carries information.** **NO-WAVE-OWNER.**
- **L-D-4 — MINOR · the ten-line "partial compile" arm is unreachable at this callsite** — re-proved by this seat on today's disk (walkList → exactly one child; every `compileChild` null return is preceded by a `refusals.push`; hence for `[animation]`: css non-empty ⟺ refusals empty). The comment describes an impossible state. **NO-WAVE-OWNER** — entangled with C-14: if the cure passes the group, this arm goes live AND D-8's kill expires (constraint recorded on both rows).
- **L-D-5 / C-12 — MINOR · `useTemplateRef<HTMLElement>` over a lucide functional component's `<svg>` root — a false annotation that only survives because nothing typechecks it (L-BL-4 fold).** **NO-WAVE-OWNER.**
- **L-D-6 — MINOR · `useKeyframesEditor` is a 20-member monolith shaped for the other consumer (my count of the return literal: 20); this component uses four — and instantiating it also instantiates seven unused ops, both dead watches, and the L-M-10 waste.** **NO-WAVE-OWNER** (role-split input for SS-1/SS-2).
- **C-13 — MINOR · the component is async-registered twice; the barrel registration has zero importers while its own docblock claims to centralise the lazy boundary** (barrel read; ChannelControls:252). DESIGN.md :234 R2 violation. **NO-WAVE-OWNER** (one boundary; D-10's fold names the wrapper that stays).
- **C-14 — MINOR · the ribbon button's comment promises "the orchestration graph"; the code compiles `[animation]` — one child, `delay: 0`, no `staggerDelays` (opts-only, my read of backward.ts) — silently zeroing a group's stagger; the component's own :127-128 comment is the honest one.** **NO-WAVE-OWNER**, jointly with L-D-4; any cure that passes the group must first re-litigate D-8 (killed only for the one-child input).
- **N-6 (RR-L; banked) — MINOR · `Mod+S` is skipped exactly where the user is** — joined into D-13 above for the cure; banked as its own row so the registry-modality fact (keyboard.js:51) is findable by id. **NO-WAVE-OWNER.**
- **N-7 (RR-L; banked) — MINOR · `onEditorChange` is async and unserialized over an interleaving pipeline (three suspension points before `adoptCompiled` — my read of :62-70); two edits can land their transplants out of order, last-resolve-wins.** Window bounded by the 200 ms debounce; widens with keyframe count. **NO-WAVE-OWNER** (the EDITOR-UNIT's cancel/sequence seam — KF-CE-2's cure family — should absorb it).
- **N-9 (RR-D missed-M2, RESCOPED per ruling) — MINOR-LATENT · two composables own competing `focus()` calls on one activation (`useKfPillTabs`'s roving-focus restore vs `useKeyframesPaneReveal`'s panel focus), one of which is dead by construction — LATENT because the pill tablist never renders in the shipped app (banked §0).** Rides **KF.W6** (the S-1 tabs decision must pick ONE focus owner); ordering witness → SS-13 #9 only if the strip is restored.
- **N-10 (RR-D missed-M6; banked) — MINOR · `exportCompiledCSS`'s catch mislabels the one failure it handles: a clipboard rejection surfaces as "Export CSS failed 🔧" — attributing a clipboard fault to the compiler, on the one surface whose stated pedagogy is that the named refusal is the product value** (:161-167 — verified; the compile SUCCEEDED). **NO-WAVE-OWNER** (split the copy step out of the compile try).
- **D-16 — MINOR · FOLD → banked kf-DemoGlobalChrome D-2 (four toast types, one identical plate; the demo's own `--accent-red` error law reached by none). NOT re-booked.** Rider: this component's five failure toasts are all inside that blast radius.

### INFO

- **L-I-1 — INFO · `document.head.querySelector(\`#${styleId}\`)` interpolates an unvalidated id; under whitespace names it parses as a descendant selector and the reuse branch never matches** (:31 — verified; `getElementById` has no such failure mode). Rides the N-8 cure.
- **D-25 — INFO · neither animation sets `respectReducedMotion` (engine default false); vacuously harmless here because neither renders (D-4/D-5), but the same composable drives the VISIBLE brush at the sibling.** Recorded for the APPLY-UNIT spec; the axis's refusal to book motion that does not exist is the correct discipline.
- **D-26 — INFO · empty `<style scoped>` vestige (:185 — verified); the RTL/`[dir]` rules are part of the sheet the D-1 fold owns; no `forced-colors` anywhere in the chain.**

## Killed-claims register

1. **D-8 ("N refusal toasts, 3 visible") — KILLED (both re-readers; re-executed by this seat on today's disk).** `:135` passes `[animation]` → `walkList` → exactly one `CompileChild`; `compileChild` pushes at most one refusal and returns null when it does (all four null sites read). Max one toast. The kill is INPUT-SCOPED: it expires the moment C-14's cure passes the `AnimationGroup` — recorded as a constraint on that repair, not a defect of this tree.
2. **D-24 (the empty-`else` no-feedback state) — KILLED (both; re-executed).** For one child, `css === ""` ⟹ `refusals.length === 1` (every zero-chunk path pushed a refusal — my read of the four sites + the `chunks.length === 0` return). The state is expressible in the type and unproducible by this input. Struck, not carried.
3. **RR-D's C-5 sub-claim (the async wrapper's `InstanceType` lacks the expose surface) — KILLED (RR-L's kill upheld; my own `runtime-core.d.ts:1554-1556` read; corroborated by banked kf-ChannelControls C-7).** A kill-of-a-confirm inside a re-reader's return — the incredulity posture working in both directions. Repair misdirection neutralized: the erasure is at the banked ACG line, not ChannelControls.
4. **The D axis's ten-member `CompileRefusalReason` enumeration — KILLED (RR-D's correction re-verified: the union has FOUR members on today's disk; the six `entry-*` slugs belong to `EntryRefusalReason`, unreachable from `compileToCSS`).** A repair scoped to "ten reasons" would be scoped wrong.
5. **RR-L's BLOCKER grade on L-BL-3 — KILLED (ruling table).** No data loss; the affordance's heavier failure (success never persists) is banked at KF-CE-7 and may not be double-weighted.
6. **RR-L's MAJOR grade on D-7 — OVERRULED to MINOR (ruling table).** The demotion rationale (human sentence present; refusal-only reachability; D-1 latency) went unanswered.
7. **RR-D's missed-M2 as filed ("a keyboard user arrowing across the surfaces cannot get past Keyframes") — KILLED and REFILED LATENT (N-9).** The scenario traverses a tablist that never renders (banked §0). A reachability claim must be checked against the banked liveness rows, not the source alone.
8. **The corpus's "unused `--panel-max-h`" evidence cell (D-11; asserted by the axis and inherited by BOTH re-readers) — KILLED BY TREE MOTION (this seat's grep: 2 live consumers today, matching the Aug-9 KSM seat).** The ROW survives strengthened; the cell is stale. Both re-readers repeating a zero-count neither re-ran on the moving tree is a citation-inheritance event, recorded in DISSENT.
9. **RR-L's MISSED-2 as a "missed" find — IDENTITY-KILLED (≡ banked KF-CE-7, which even names this component's latch as the compensator).** The fact is right and folded with a component rider (copyCSS/apply operate on text that differs from what the user sees post-format); the net-new status is not.
10. **RR-D's missed-M7 as a "missed" find — IDENTITY-KILLED (≡ banked KF-CE-1 + KF-CE-15).** Folded with the callsite rider (the editor is this tabpanel's entire content; the file's own :43 calls it "the editor as a CSS IDE").
11. **The corpus's S-6 superlative as stated ("reversible on both exits") — DEMOTED by N-5 (RR-L).** The `prevPaused` mechanism is genuinely right (my read) — but the composable is reversible only through a `clear()` nobody calls, and the sheet it manages goes stale the moment the buffer is edited while applied. The craft credit survives; the safety claim does not.

## Superlatives (provenance preserved; the register runs both ways)

- **S-1 (all three axes + both re-readers; verified :73-81) · the `useTimeoutFn` formatting latch is the right primitive, stated by contract** (`{ immediate: false }`, restart-on-recall, scope-bound teardown; zero raw timers in the file). That the machine it drives is breakable (D-6) is a missing `finally`, not a flaw in the choice.
- **SUP-B (both re-readers; branch structure re-verified against today's `CompiledCSS`) · `exportCompiledCSS` is the file's best library consumption and the ONLY exposed method with a catch**: three honest branches, the typed `refusal.reason`/`message` surfaced VERBATIM at 10 s, the rationale stated in-file. D-7 attacks the typography, L-D-4 shows the partial arm is aspirational until the callsite passes the group, N-10 corrects the catch's label — the consumption decision underneath must survive every one of those repairs.
- **S-3 (D axis; durations re-verified) · errors get 2.5× the reading time, with perfect consistency** — all five failure toasts pass `duration: 10000`, no success toast passes any. A deliberate asymmetry in the right direction (moot today under the D-1 fold; correct at repair time).
- **SUP-C (RR-L; grep re-verified: `templateFrameStrings` 0 hits in the SFC) · the mount path calls the NARROW projector** — two consumers of one composable, two right choices; undercut in steady state by L-M-10's wide-projector echo, which is the defect, not the mount decision.
- **S-4 (D axis; RR-D re-verified) · zero bespoke chrome, zero `--kf-*` custom properties — the flat-namespace hazard this lane was briefed to hunt does not exist in this subtree**; the 186-line component renders three elements and forks no design-system primitive.
- **S-6-as-corrected (all axes; demoted per kill #11) · the `prevPaused` save/restore inside `useApplyCSS` is the correct mechanism** — both the toggle-off arm and the uncalled `clear()` restore the PRIOR pause state rather than blindly resuming. The APPLY-UNIT spec should preserve the mechanism and wire the teardown.
- **SUP-D (RR-L) · the comment discipline across this closure is what made a fully static adjudication possible** — contracts-with-reasons at CSSCodeEditor's boot, useKeyframeOps's yield ladder, ChannelControls's inert rationale — with the counterweight this record proves twice (L-M-5's false invariant; C-14's overpromising button comment): comments this good must be VERIFIED, not trusted, which is an argument for writing them and auditing them, never for writing fewer.
- **Cross-references, not re-credits**: the force-mounted pane's `inert` craft = banked kf-ChannelControls S-A; CSSCodeEditor's async boot hygiene = banked at kf-CSSCodeEditor (RR-L's SUP-A extension — the zero-size `useResizeObserver` deferral — noted there); `useKfPillTabs`'s APG prose = banked kf-ChannelControls SUP-4.

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs, ranked)

1. **L-BL-2 clause 3 observed** — open spring's Sweep (or amiga's Bouncing X), press Apply CSS, watch for `InvalidCharacterError` and its ordering vs `isApplied = true`. The cheapest decisive probe in this corpus; the invalid-token half is source-certain.
2. **L-BL-2 clause 2** — whether the parser emits a diagnostic for the two-ident `@keyframes spring-spring keyframes` prelude (i.e., whether every edit on those channels red-toasts). Statically undecidable here; NOT probed under this lane's static law.
3. **N-2(c) live edit-loss ordering on spring** — Physics facet edit → Keyframes surface → one keystroke → observe the facet edit revert.
4. **N-1 live corroboration** — Controls duration edit → Keyframes → one keystroke → observe both the animation and the stored options revert (the chain is statically closed; the witness is for the SS-13 record).
5. **Echo-loop felt severity** — caret displacement + undo-stack reset after the ~300 ms rewrite (API halves banked at KF-CE-6/-8; only the felt cost is open).
6. **D-11's scroll capture** — 450 px Monaco (own scroller) inside the pane's `overflow-y-auto` at 375×667.
7. **D-20's rendered height** — `h-8` under glass 7.0.0 `size="sm"` (the minified variant table is unreadable statically; the authored 32 px + declined floor are closed).
8. **L-M-10 / N-3 felt INP cost** — the N+1 prettier formats and the per-pause localStorage serialize; the MINOR grades carry an escalation hook if this measures material.
9. **N-9's focus ordering** — ONLY if the pill strip is ever restored (KF.W6).
   **RETIRED from prior residue** (settled at the banks, recorded so no seat re-spends the budget): the D-1 rendered-landing question (statically settled: structurally unreachable — DemoGlobalChrome bank); the Monaco escape-hatch/keyboard-trap probe (banked KF-CE-3); the `npm ci` clean-checkout probe (mooted by `origin/master:77` — KF.W0 owns the discharge); C-2's reachability (≡ value.js R1, banked, KF.W3-gated — a value.js-side question, not an SS-13 pixel).

## DISSENT — preserved, not ruled away

1. **RR-L's severity instinct on the cost rows (L-M-10, N-3, and its MAJOR on L-M-5).** The argument that recurring waste on the primary typing path of the flagship editor deserves MAJOR is close to the line, and S4's own INP prose is on RR-L's side. If SS-13 #8 measures material main-thread cost, the MINOR grades should be revisited by the owning spec — the escalation hook is in the residue.
2. **RR-D's MAJOR on D-3.** "The only working format affordance is invisible, mac-only, and absent from the modal that claims to list the shortcuts" is a real product-truth argument; it lost to the mechanism fact (the registry cannot serve the context), not to unimportance.
3. **The tree-motion law (this seat's).** This corpus was authored Aug 5 against a worktree that had moved by Aug 9 (`--panel-max-h` 0→2 consumers; `definePreset` :309→:310; ±1 drift across useKeyframeOps). Two re-readers inherited a stale count. Every anchor in this record is a TODAY-disk read; every cure re-resolves at `origin/master` first (SCH-1/KF.W0); and any future seat re-running a corpus grep on this repo must date its run.
4. **The registry-sweep law (this seat's).** Two of the fifteen "missed" rows were already banked at kf-CSSCodeEditor — found twice from two sides, which is evidence of their reality AND of a method gap: neither re-reader swept `registry/adjudicated/` before claiming novelty. The M-25 agglomeration depends on identities holding; a "missed" claim without a bank sweep is unadjudicable.
5. **The convergent-blocker note.** Both re-readers independently confirmed L-BL-1/L-BL-2 with disjoint falsifier sets (RR-D closed the markup-class falsifier; RR-L closed the square/spring name census), and this seat's re-derivation used a third instrument set (the greps + the raw-interpolation reads). Three-way convergence with disjoint instruments is the strongest evidence state in this record — noted because the same corpus produced two rows (D-8, D-24) where two-way convergence WITH SHARED instruments was wrong.
6. **HEAD-movement note**: the sacred checkout is unchanged (`8281638c`); the WORKING TREE is the moving object. The kf-ChannelOptions record's re-anchor law is hereby extended to worktree drift between audit dates.

## Closing verdict

**ADJUDICATED against the measured dirty worktree over checkout `8281638c` (component byte-equal to `origin/master`; emit chain WIP-divergent from both refs): 56 confirmed rows (4 BLOCKER · 22 MAJOR · 27 MINOR · 3 INFO), of which 11 fold by reference to banked kf-DemoGlobalChrome / kf-CSSCodeEditor / kf-ChannelControls / kf-ControlsPaneWrapper / kf-ACG / kf-App / census identities with zero re-bookings — the net-new blocker is N-1 (Controls-tab edits silently destroyed in animation AND store by the stale-buffer round trip), joining L-BL-1 (the inert Apply-CSS class) and L-BL-2 (whitespace names → invalid idents + mid-apply throw), all three converging on N-8's root cause (three names for one animation; the library's one normalizer unpublished — KF.W5) · 11 claims killed with executed refutations (two unreachable-state kills re-proved on today's disk, one kill-of-a-confirm, one tree-motion kill of a corpus cell both re-readers inherited, two identity-guard kills of "net-new" status) · 10 reader-missed rows banked (N-1..N-10, one merged pair), 3 folded, 1 rescoped latent · superlatives preserved with one demotion (S-6, by N-5's uncalled `clear`) · routings are M-25 agglomeration input for the forming X·KF specs — KF.W0 (F-1/substrate), KF.W3 (R1 reachability), KF.W4 (typecheck fold), KF.W5 (`cssIdent` + deep-path surface), KF.W6 (tap-floor, tabs/focus reconciliation), KF.W8 (colocation), KF.W9/SS-13 (8 residue rows, 3 retired), EDITOR-UNIT (echo/format/toast-storm folds), and the proposed APPLY-UNIT carrying everything the apply/name/feedback family owns as NO-WAVE-OWNER input for SS-1/SS-2.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/keyframes/KeyframesStringControls.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **KeyframesStringControls.vue=+1/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` appears in this record but NOT in the re-anchor sense** — it is a component-local id here, and the record-qualification rule (KF-W0 §Carry C-1.R row 6) keeps this sweep from booking a dead-token cluster as a re-anchor obligation. Eight records are in this class: kf-ChannelOptions · kf-CubeScene · kf-KeyframesStringControls · kf-OrbitalDrag · kf-SquareScene · kf-SquareInstrument · kf-StartingStyleTarget · kf-TimelineCaret.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**`L-M-9`'s TWO LIMBS ARE DISAMBIGUATED — MAJOR FOLD vs RETIRED PROBE** (C-1.R row 7, repair round 5 D-3). The banked row at `:86` carries BOTH: a **MAJOR fold** (`FOLD → census F-1 ≡ SCH-1 → KF.W0`, NOT re-booked) and a **RETIRED probe** (the clean-`npm ci`-fails probe). A seat greping the id now finds **exactly TWO dispositions, each naming its limb**, and re-books neither. **The fold is discharged by the §B-12 act, once, at C-1**; both transitive critical-path riders (`CSSCodeEditor.vue:38`; `useHighlightCSS.ts:2` via `useApplyCSS.ts:4`) are carried verbatim there.

**DISSENT 3's tree-motion law is now SATISFIED BY CONSTRUCTION**: *"every anchor in this record is a TODAY-disk read; every cure re-resolves at `origin/master` first; and any future seat re-running a corpus grep on this repo must date its run."* The today-disk of 2026-08-25 is gone; the ref of record is the checkout's own `HEAD`; this stamp is dated.

**`D-19` here is a COMPONENT-LOCAL id, not the re-anchor law** (C-1.R row 6's record-qualification rule); nothing is re-booked from it.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — **L-M-10/N-3**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here. **Nothing above this line is
rewritten** — no edit, no re-grade, no renumber, no deletion, no new id (E-1 immutable bytes / E-3
addenda-not-patch). This addendum records a MEASUREMENT STATE, not a grade: **the banked severity is
unchanged and stays exactly where the record put it.***

- **armed trigger** — register row **13** of thirteen, `evidence/W9/ESCALATION-REGISTER.json` (id as banked there: `L-M-10/N-3 + KF-TFP-19 (INP hooks)`); ceiling if it fires: **MAJOR**; roster cell(s): `—`.
- **measurement named by the trigger** — a **FELT INP cost** — not the existence of the waste, which is already banked. KSC: N+1 prettier formats per typing pause + per-pause `localStorage` serialize.
- **measured value** — **NONE. The trigger did not run.**
- **resulting severity** — **PROVISIONAL (unrun)**. The banked grade stands; it is neither inherited as confirmed nor moved.
- **exact precondition** — an INP figure on this path. **A CPU-time figure alone promotes neither row** — the discriminator is INP (the interaction's own latency), not total work, on the kf-ChannelControls precedent. This trigger binds **two records**; its other limb (**KF-TFP-19**) carries the same addendum at `kf-TimingFunctionPanel.md`, which is why the register counts **thirteen triggers over twelve records**. the `chromium` and `chromium/emulated-forced-colors` cells DID open at `.d` (8 rows, 2 + 2 captures), so this trigger is **unrun, not foreclosed** there: it needs a pass that mounts the subject. the `safari-app/desktop` cell was open and productive at `.d` (4 sessions, OP-4 taken, 3 shots) and **closed mid-seat**: session creation began returning *"You must enable 'Allow remote automation'…"*; `safaridriver --enable` → **`Password:Password is not valid`** (an interactive admin authorization no seat here can supply); the running Safari is the **owner's** (pid 23725, started 16:23:59, 6 windows) and was not quit. **Precondition: 'Allow Remote Automation' re-enabled by a hand that can answer an admin prompt, or the owner's Safari restarted.**
- **substrate of this wave** — keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD; frontier moved to `3e81f500`, the pin is its ancestor, **no re-pin on a seat's authority**). Served bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448`, built in the §0m.2 capture clone and **re-hashed at this seat, double-run identical**. Capture: **none for this row**; shot-sha256: **none**.
- **G-KFW9-13 reading** — this row closes the gate's *"unrun triggers reported PROVISIONAL at close"* arm, **not** its *"measured value written back"* arm. The gate stays **RED**.
