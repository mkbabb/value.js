`claude-fable-5` — model receipt (Fable 5), observed in this seat's own system prompt, not inherited (M-23/L-18 fresh-adjudicator seat; recorded against the tiering law).

# PreviewRamp — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Component**: `demo/color-session/color-chips/PreviewRamp.vue` (50 lines) + its closure: `sample.ts`, `index.ts`, `PreviewStrip.vue` (recipe twin), `MixConfigBar.vue` (sole host), `MixSourceSelector.vue` (operand door), `test/preview-chips.test.ts`, `e2e/smoke/oracles/o14-preview-truth.spec.ts`, `demo/styles/foundation.css` (roster/tokens).
**Date**: 2026-08-06 · **HEAD at adjudication**: `3487d73e` (branch `tranche-u`; challenge corpus pinned `c654824e`, measured at `f36f780c` — `git diff c654824e -- demo/color-session/color-chips/ demo/workbenches/mix/` re-verified EMPTY by the r2 readers; every chip/mix citation transfers).
**Corpus**: 3 r1 challenge seats (D-design · C-implementation · L-library, all `claude-opus-5[1m]`, receipts on file in each challenge header) → 2 hostile r2 re-readers (design-and-usage = **RR-DU** · library-and-code = **RR-LC**). **r2 governs**: the re-reader returns are the governing reading of the r1 corpus; this seat adjudicates the re-readers, kills their kills where the refutation is weak, and demotes their finds where unproven.
**Method (L-18 incredulity seat)**: every verdict-level disagreement between RR-DU and RR-LC was re-derived against the tree with this seat's own instruments; every r2 **missed** claim was independently re-verified before admission (none admitted on citation alone); both suspiciously-agreed BLOCKERs were re-derived, not spot-trusted.
**Arbiter's own instruments this session (all static + node against dist; ZERO browser probes — task law + probe parsimony)**: full reads of PreviewRamp.vue (whole), sample.ts (whole), index.ts (whole), PreviewStrip.vue (:1-30, :40-80), MixConfigBar.vue:50-155, MixSourceSelector.vue:30-45/:160-230/:270-285, foundation.css:130-160/:264-272/:450-464/:650-700/:815-842, o14-preview-truth.spec.ts:155-175/:330-416, ci.yml:30-73, VISUAL-CONSTITUTION.md:17/:23/:82-83/:91/:151-153/:194, grammar.ts:283-311, operations.ts:58-81/:100-115, anchors.ts:358-366, useMixingState.ts:40-46, mixStage.ts:26-30/:93-110, picker-color.ts:100-110/:204-212, preview-chips.test.ts:25-96, tsconfig.demo.json:37-50, aurora-harmony-stops.ts:20-45, index.html:1-12, GradientVisualizer.vue:175-212 (chip-absence), X-tranche wave bounds (W1/W2/W5/W6/W7/W8/W10/W11 §File Bounds). Node against dist (this seat's own runs): serializer dialect `oklch(62% 0.27 9.8deg)`; the o14 regex → **0** matches on the dialect, **1** on canonical; per-space distinct-ramp census for `#e11d48→#0ea5e9` → `oklch:2 oklab:1 lab:1 lch:2 hsl:2 hsv:2 hwb:2 rgb:1 xyz:1`. Greps (mine): `renderSlot|\$slots` in `glass-ui/dist/watercolor-dot.js` → **0** · `inheritAttrs: !1` present · `aria-describedby` → **0** across reka SelectItem + glass select chunks + all of demo/ · `var(--foreground) 12%, transparent` → 4 hits (mint + 2 chips + DESIGN.md prose) · `--radius-sm:` minted nowhere in demo/ · color-chips importers → exactly 3, all cross-tree · `data-color-surface` producers in demo/ → **0** · `docs/tranches/T/audit/w6-lane-d-record.md` → ENOENT · exports = 7 keys / tsconfig paths = 8 (3 phantom targets verified absent) · self-install `node_modules/@mkbabb/value.js` real dir, 4.0.0 = repo 4.0.0, `css.d.ts` 10910 B vs checkout 12490 B.
**Environment disclosure**: no dev server touched; every ruling in this record is decidable from source + shipped dist. The r1 seats' live cells (chromium, `:9000`) are adopted as corroboration only and are quarantined in §UNPROVEN where load-bearing.
**Motion/PRM**: PreviewRamp declares and delivers static paint (`PreviewRamp.vue:14` — "Static paint — no motion, PRM-neutral"; verified: no transition/animation property in either scoped block). **Zero MOTION-SOURCED rows in this corpus**; no `motion-quarantine.md` row (M-xx) is cited or carried, and no FALSE row could be — the quarantine record exists (tracked, W0 HG-11) and was checked.
**Status verbs**: every disposition below is **ADJUDICATED, not VERIFIED**. Nothing in this file stamps any wave; X-wave routings are ownership routings under the X CONFORMANCE bounds, and D-19 re-measure-at-open governs every line anchor.

## Scoreboard

**26 canonical tree rows adjudicated: 2 BLOCKER · 9 MAJOR · 12 MINOR · 3 INFO — plus 1 adjudication-layer collision row (ADJ-1, this seat's own find). 13 killed claims/sub-claims. 13 missed-then-admitted rows (12 from the r2 readers, independently re-verified; 1 minted by this seat).**

Blocker-weight survivors: **R-1** (the component renders zero instances — both operand doors are V-abrogated producer spans) and **R-2** (its only browser oracle is unreachable, textually unpassable in the stamped dialect, and CI runs no playwright at all). Both re-derived statically by this seat; neither depends on any live cell.

## Ruled disagreements (the seat's actual work)

| # | Question | RR-DU | RR-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| A | C-3 (engine fill between samples): kill or confirm, and at what severity | KILLED whole (module vocabulary: "discrete-stop" = built from discrete samples; residual already filed as D-9 MINOR) | CONFIRMED mechanism at MAJOR, BLOCKER killed | **CONFIRMED-RESCOPED at MINOR** — RR-DU's severity, RR-LC's mechanism | Both are half right. The mechanism is real and needs no pixels: `PreviewRamp.vue:24-26` emits positionless stops, so the UA interpolates every in-between pixel; `sample.ts:6-8` forbids only "CSS `in <space>` gradient interpolation" **by letter** (no `in` clause is present) but "no engine divergence" (:10) and "a chip that approximates the library output is FORBIDDEN" (:15-16) are violated in rationale. RR-DU's vocabulary defense holds against the BLOCKER (`PreviewRamp.vue:3` says CONTINUOUS in line one; `index.ts:7-8` mints STRIP=discrete/RAMP=continuous), and C-3's own P-6 numbers price the residue at max 8-15/255 vs 185-190/255 for the arm the sampler prevents — the apparatus buys ~92% of its claim. C-3's hard-band cure is REFUTED (it collapses the two chip species `index.ts:6-8` deliberately distinguishes — a design deletion sold as a bug fix). Surviving obligation: the prose must stop disagreeing with itself (`:6-7` "discrete-stop" vs `:3` "CONTINUOUS"), and the law must either name the residual engine fill with its measured band or the design must rule for hard stops — a **design ruling**, not an implementation default. |
| B | D-2 (quartet can never be four) severity | BLOCKER | MAJOR | **MAJOR** | My own dist run: distinct ramps `2/1/1/2/2/2/2/1/1` across the nine offered spaces — row-identical to both readers — and the bound is algebraic (`operations.ts:70-79`: `{shorter,longer}` is a sign-dependent relabelling of `{increasing,decreasing}`, so ≤2 distinct deltas ever; `anchors.ts:360-366` HUE_INDEX omits oklab/lab/rgb/xyz so the Select is a total no-op in 4 of 9 spaces including the shipped default `useMixingState.ts:44` `"oklab"`). BLOCKER is refuted by the corpus's own R-1: zero chips render today, so no user is currently lied to; and the chips themselves never lie — they truthfully paint identical ramps. The defect is the DESIGN's promise (an enabled four-way control whose four rows are at most two behaviors, `MixConfigBar.vue:122-139` carries no `:disabled`), which is first-class but not render-dead class. |
| C | D-10 (the `v-if="stops.length >= 2"` guard) | Confirmed MINOR with the unqualified claim killed | KILLED whole, downgrade INFO | **REFUTED-AS-FILED; INFO residual; cure RETIRED** | The corpus refutes itself: C-9 proves `k=NaN` → `perSegment=NaN` → the `:74` loop runs zero times → `[]` returned — truthy, passed through `MixConfigBar.vue:111`'s `v-if="spaceRamps.get(…)"`, and stopped ONLY by this guard. So "can never evaluate false" is false, and D-10's deletion cure would unguard the sole prop contract of a barrel-exported public component (`index.ts:25`) to cure a redundancy. C-9 and D-10 prescribe opposite edits to the same line and neither seat cross-read the other — recorded as addendum law 2. Residual (INFO): under both current hosts at default `k`, the guard is redundant; that is all. |
| D | D-6 (the 12% ring fails its own docstring) severity | MAJOR (with the WCAG 1.4.11 over-claim self-corrected) | Confirmed structurally; ratios quarantined UNPROVEN | **RESCOPED to MINOR** (merged with the missed inset-veil find) | The design-intent failure is arithmetic-certain: a single-tone 12%-alpha stroke (`PreviewRamp.vue:47-48`) cannot bound an arbitrary chip against a same-scheme translucent surface in both directions; the docstring (`:10-12`) claims it does. But 1.4.11 does not bind (RR-DU's own correction: `:33` is `aria-hidden`, and `color-space-meta.ts:26-43` duplicates every row's meaning in label + description text), every ratio is live-only, and the sharper half of the truth is the MISSED find both r1 axes and RR-LC walked past: the ring is painted `inset`, OVER the specimen, veiling ~12% of its block-size including exactly the two operand-identity endpoint stops — the ring IS the bleed its comment forbids. One MINOR row, one cure (outset ring or `outline`, in the one shared recipe of R-7). |
| E | L-3 (φ re-hardcode) and the cure collision | MAJOR; collision filed as a missed claim | MINOR | **MINOR; L-3's cure REFUTED; merged cure RULED** | The re-hardcode is real (my grep: `2.618rem` literal in both chips; the ladder's own comment at `foundation.css:455-461` forbids it; `--phi-4: 2.618rem` at `:462`, surfaced at `:156`). But L-3's cure `inline-size: var(--phi-4)` is a ROOT-anchored length and PRESERVES the two-anchor split D-3/C-5 file as MAJOR — the three seats' cures are mutually exclusive and no r1 seat noticed (RR-DU's missed find, verified). **Ruling**: the merged cure is `block-size: 1em; aspect-ratio: 2.618;` — reads no length token at all, φ² by construction at every font context, discharges D-3/C-5 AND L-3 simultaneously (the ladder owns lengths; a ratio is not a length, and 2.618-as-ratio is the design law itself, commented as such). Applied once, in R-7's single recipe. |
| F | The `[data-color-surface]` cure (C-4/D-5) | "C-4's one-attribute cure is the structurally correct one" | Adopted the roster-omission finding | **CONFIRMED defect; cure SEQUENCED — see ADJ-1** | Both readers endorsed the hook without cross-reading the sibling apotheosis: **GenerateControls (this registry, R-C + G8) rules `[data-color-surface]` a DEAD selector (zero producers — re-verified by my grep today) and orders it DELETED from foundation.css** with a grep-count-0 gate. The chip cure would mint the selector's FIRST producers. Two adjudicated records currently order opposite fates for the same selector; landed blind, the later one silently breaks the earlier one's gate or the chip's cure. Ruled in ADJ-1. |
| G | L-4 (dual sampler) identity sub-claim | Confirmed as filed ("identical modulo the final encoder") | Sub-claim KILLED: different grids (16 stops @ t=i/15 vs 17 @ t=j/16) | **RR-LC wins; row stays MAJOR, sharpened** | My reads: `mixStage.ts:28` `RAMP_STOPS = 16`, `:96-105` `index/(RAMP_STOPS-1)` — 16 stops on a 15-interval grid, throws on `!result.ok` (`:101`); `sample.ts:69` gives perSegment 17 for 2 operands — 17 stops on a 16-interval grid, returns null (`:81`). Same feature tree, same operands/options, different grid, different count, different error posture, one tested. The divergence is worse than L-4 stated, which strengthens the row and kills the sub-claim. |
| H | Where the shared recipe lives (L-2 → utils.css vs D-7 → module css vs C-6 → glass variant) | flagged the three-way home dispute | flagged it, plus DESIGN.md:387/:388 tension | **Module-local `chip.css` inside `color-chips/`, imported by both SFCs** | DESIGN.md:388's global-utility bar is 3 consumers; there are 2, and both are siblings in ONE module — "cross-feature" is not established, so utils.css is over-promotion. A glass variant exports a demo-private species to the producer with zero producer-side consumers — the KISS-banned contrivance. The `:387` "no parallel design-idioms.css" warning targets a second GLOBAL idiom sheet, not a colocated module stylesheet; a module-owned `chip.css` is the E-1 "ONE focused common module" text (`index.ts:2-4`) applied to its own styles. All five cures of this corpus (R-4 units, R-6 attribute, R-13 ring, R-26 radius hedge, `--card-edge` consume) land ONCE there. |

**Cure-level rulings inside agreed verdicts**: C-1's `v-bind="$attrs"`/`tag` restoration cure is KILLED (reverses the ratified VC:91/P051 abrogation — RR-LC's refutation adopted; concordant with the GenerateControls R-D precedent: consumer debt, named geometric seat cure). D-5's re-invented hook is retired in favor of the existing `[data-color-surface]` hook + ADJ-1 sequencing. L-1's MAJOR is demoted (dead code masking nothing that exists ≠ behavior defect) and its "doc-comment documents a mechanism that does not exist" sub-claim is killed (the comment describes the OUTPUT, which is true — `grammar.ts:287` emits alpha iff ≠ 1).

## Adjudicated defect roster

Line anchors at corpus pin `c654824e`/`f36f780c` (chip/mix surfaces re-verified unchanged; D-19: re-resolve before cutting). Routing law: a row routes to a tranche-X wave only where that wave's §File Bounds owns the edited surface; otherwise **NO-WAVE-OWNER** — a named formation-boundary carry, never a silent drop. `demo/color-session/color-chips/**`, `test/preview-chips.test.ts`, the foundation.css WHCM/print rosters, `aurora-harmony-stops.ts` and `mixStage.ts` are in NO X-wave's bounds today (verified against W1/W2/W5/W6/W7/W8/W10/W11 — W5/W8/W10 hold only carved foundation.css arms: layout, spectrum-shim bank, `.underline-tabs` survivor).

### BLOCKERs

- **R-1 — CONFIRMED (BLOCKER)** [D-1 / C-1] — **PreviewRamp renders zero instances in the shipped app.** Chain re-verified from source by this seat: `MixConfigBar.vue:111/:133` gate on `spaceRamps`/`hueRamps` → `sample.ts:58` null under 2 operands → `MixPane.vue:103` → `useMixingState.ts:42` `ref<SelectedColor[]>([])`; BOTH operand-add affordances are glass-ui 7.0.0 `WatercolorDot`s (`MixSourceSelector.vue:164-178` add-slot `@click="addCurrentColor"`; `:213-221` palette swatches `@click="emit('addColor', …)"`) and the producer (dist bytes, my greps) declares `inheritAttrs:!1`, six props with no `tag`, renders `<span aria-hidden="true" … pointerEvents:"none">` — `tag`/`@click`/`aria-label`/`:disabled` all silently drop. **Causation ruled: consumer debt, deliberately V-ruled** — VC:91/P051 abrogates the interactive host; the Mix sites are named, assigned work ("W19–W22/W25–W27 execute … Mix … sites"), not an undiscovered producer regression. Cure = a named enclosing geometric button/seat wrapping the dot face — ONE identity with the GenerateControls apotheosis B1 bank, which already names `MixSourceSelector:168,215` in its sibling census; MixSourceSelector is NOT §D-pinned, so the Mix sites need no v8 hold. **Route: X-W6** (owns `MixSourceSelector.vue`/`MixConfigBar.vue`; unit .j2's "one interaction owner" Mix canary is the natural home), identity cross-noted to the GenerateControls B1 census so neither books it twice (anti-rename).
- **R-2 — CONFIRMED (BLOCKER)** [C-2] — **The component's only browser oracle is unreachable, unpassable in the stamped dialect, and absent from CI.** All three halves re-derived by this seat without a browser: (a) unreachable — `o14-preview-truth.spec.ts:351-355/:410-416` await `getByRole('button', {name: 'Add current color to the mix'})`, an accessible node the producer's `<span aria-hidden>` can never expose (R-1); (b) unpassable — my dist run: the stamp dialect is `oklch(62% 0.27 9.8deg)` (`grammar.ts:309`: `format(a*100)%` + `angle(c)`→`deg`) and the spec's sole reader `parseOklchTriples` (`:162-168`) returns **0** matches on it vs **1** on canonical — its `[ ,]+` cannot cross `%` or `deg` — while `:376-392` asserts `painted.length === stamped.length`, defective on BOTH UA branches (17-vs-0 RED, or 0-vs-0 vacuous); (c) CI-dark — `ci.yml:32-37` runs lint/vue-tsc×2/build/vitest, zero playwright (my read; X-W1's B3 census concurs: 0 workflows). The one green chip leg (`:334-344`, honest-absence `count === 0`) is a permanent tautology under R-1. **Route: X-W1** (owns `e2e/smoke/**` fixtures, `test.fail()` rulings, `ci.yml`, the oracle slate — the dialect cure is: pass both sides through the app's own parser, one encoding, the GenerateControls G1 precedent); the reachability precondition rides R-1 at X-W6; the whole-suite green stamp is X-W11's. The stamped-dialect/oracle-regex split is the same defect as R-19 seen from the other end (RR-LC's sharpening, adopted).

### MAJORs

- **R-3 — CONFIRMED (MAJOR, severity ruled B)** [D-2] — **The "four-arc quartet" can never be four arcs: ≤2 distinct ramps ever, exactly 1 in four of nine spaces including the shipped default oklab.** Proven algebraically from `operations.ts:70-79` + `anchors.ts:360-366` and re-derived by this seat's own dist run (`2/1/1/2/2/2/2/1/1`; achromatic pairs collapse to 1 everywhere). The Hue Select ships enabled in all spaces (`MixConfigBar.vue:122-139`, no `:disabled`), defaulting to a space where it is a total no-op (`useMixingState.ts:44`). D-2's `VISUAL-CONSTITUTION.md:194` citation is killed (governs the Generate chassis, not mix rows) — the substance is unaffected. Cure is design-level: disable-or-annotate the hue row in non-polar spaces; collapse or annotate coinciding arcs per operand pair (the arcs remain distinct CSS Color 4 semantics — deleting options is NOT licensed). **Route: X-W6** (owns `MixConfigBar.vue` AND creates `test/interpolation-subset.test.ts`; the space-catalog work (f-gates) is the natural home for a `hue-capable` derivation).
- **R-4 — CONFIRMED (MAJOR)** [D-3 / C-5, absorbing L-3 at MINOR] — **The "golden plate (φ² × 1em)" holds at no shipping viewport: `inline-size: 2.618rem` (root-anchored) vs `block-size: 1em` (inheritance-anchored)** (`PreviewRamp.vue:43-44`, comment claiming the false relation; twin at `PreviewStrip.vue:59-60`). The chip is a SIBLING of the `text-micro` span (`MixConfigBar.vue:110-113`), inheriting the SelectItem rung — φ² only where inherited = root font-size. Cure ruled at E: `block-size: 1em; aspect-ratio: 2.618` in the one recipe (L-3's token cure refuted). Per-viewport magnitudes quarantined §UNPROVEN. **Route: NO-WAVE-OWNER** (color-chips files) — formation-boundary carry, cure text pinned here.
- **R-5 — CONFIRMED (MAJOR; structural half certain, magnitudes live)** [D-4] — **The chip does not join the description line it claims to join (`PreviewRamp.vue:9`), and as the tallest flex child the aria-hidden ornament SETS the lane's block size** (`MixConfigBar.vue:110` `flex items-center` peers: chip at the SelectItem rung vs `text-micro` line box). `PROPORTION-AUDIT.md:72` verbatim-verified by RR-DU. Pixel deltas (16.39 vs 13.75, +2.64px/lane, 384px scroller) → §UNPROVEN. **Route: split — X-W6** (host lane composition, MixConfigBar) **+ NO-WAVE-OWNER** (the chip's own sizing, cured by R-4's recipe).
- **R-6 — CONFIRMED (MAJOR)** [D-5 / C-4] — **`forced-colors: active` annihilates the chip whole**: 100% of its information is `background-image` + `box-shadow` — precisely what WHCM strips — it sits on no roster row (`foundation.css:678-700` re-read this session: `.preview-chip` absent, `[data-color-surface]` unset by `PreviewRamp.vue:30-36`), and `@media print` (`:815-840`) reuses the roster AND kills the ring (`* { box-shadow: none !important }`) — a blank rectangle in print too. `aria-hidden` (`:33`) means nothing survives in any channel. D-5's VC:23 citation killed (an identity-tally exclusion, not a forced-colors grant); the governing authority is the repo's own two-tier policy, which ships the exact opt-in hook. **Cure**: `data-color-surface` on both chip roots — **SEQUENCED BEHIND ADJ-1** (the hook is under a live deletion order from the GenerateControls wave). **Route: NO-WAVE-OWNER** (chip attribute + any roster edit; no X wave owns the WHCM roster arm), with the ADJ-1 sequencing rule binding.
- **R-7 — CONFIRMED (MAJOR)** [D-7 / C-6 / L-2, absorbing the `--card-edge` remint at MINOR] — **`.preview-chip` is one recipe minted twice in two disjoint scoped stylesheets, already drifted** (`PreviewRamp.vue:40-49` vs `PreviewStrip.vue:56-65`: SIX declarations, TWO divergences — `display` and Strip-only `overflow: hidden`, which is load-bearing; L-2's "four of five" enumeration killed). The module's own charter forbids it (`index.ts:2-4`). Sharpened by RR-LC's verified missed find, re-verified by my grep: the byte-identical ring VALUE is the demo's single-owner `--card-edge` mint (`foundation.css:266-270` "ONE `--card-edge` mint, owned here"; surfaced `:130-135`), and the two chips are the ENTIRE violator set (4 literal hits repo-wide: mint + 2 chips + DESIGN.md prose) while 13+ sites consume the token properly. **Cure**: ONE module-local `chip.css` (ruling H) carrying R-4's units, `var(--card-edge)` for the ring (redesigned per R-13), R-26's radius hedge, and the R-6 attribute — every cure in this corpus written once. **Route: NO-WAVE-OWNER**.
- **R-8 — CONFIRMED (MAJOR, sharpened)** [L-4 + the k-count contract miss] — **A second k=16 ramp sampler lives in the same feature tree, untested, ALREADY divergent**: `mixStage.ts:28/:96-105` (16 stops, t=i/15, throws) vs `sample.ts:69/:74-79` (17 stops for 2 operands, t=j/16, returns null) — the "identical modulo encoder" sub-claim killed (ruling G). Compounded by the verified missed find: **`k` is never honored** — emitted counts swing 17→23 across the supported 1..12 operand range (`total = segments·(perSegment−1)+1`; 2→17, 4→19, 8→22, 12→23 — arithmetic re-derived by this seat), never the documented "F6 sample count (≈16)" (`sample.ts:35`). One previews what the other animates on grids that share only their endpoints. **Route: NO-WAVE-OWNER** (neither `sample.ts` nor `mixStage.ts` is in any X wave's bounds; X-W6-adjacent — flagged to the formation boundary as a unification candidate).
- **R-9 — CONFIRMED (MAJOR) — IDENTITY-FOLD, never re-booked** [L-10 / L-11] — tsconfig.demo.json `paths` (8 keys, 3 phantom targets, `/css` + `/value` omitted while shipped and on this component's own chain, the "CLOSED 8-key set" comment false) vs package.json#exports (7 keys) — **re-derived whole by this seat** (`ls`: `dist/index.d.ts` ENOENT; `dist/subpaths/` = 7 pairs, no parsing/units). Fact-identical to **GenerateControls row 20 (TSCONFIG-PATHS-DRIFT, gated at its wave's G4)** and **ColorSpaceSelector L-14** — ONE identity under the anti-rename law; this record adds only the `/css`-on-this-chain sharpening. L-10 (real-dir registry self-install, 4.0.0 = 4.0.0, `css.d.ts` 10910 vs 12490 B — my own `wc -c`) is the packed-surface truth question. **Route: identity-fold to the existing GenerateControls G4 row; X-era home X-W1** (`verify-packed-surface.mjs` / the G-17 pack step is the packed-vs-checkout surface's owner).
- **R-10 — CONFIRMED (MAJOR) — GLASS-OWNED, BH relay** [r2 missed, RR-DU; re-verified by this seat] — **The `#description` lane the chip's `aria-hidden` defense leans on never reaches assistive technology.** My greps: reka's SelectItem names the option via `aria-labelledby: textId` (SelectItemText only — overrides content-based name computation); `aria-describedby` occurs **zero** times in reka SelectItem, the glass select chunks, and ALL of demo/. So `PreviewRamp.vue:13`'s "the a11y truth stays label + description" is half-false — an SR user picking a hue method hears four bare labels which (per R-3) denote at most two behaviors, with neither chip nor description available. Both r1 axes AND challenge-C's negative proof certified the opposite; the r2 reader caught it. Producer composition (glass SelectItem renders the description slot as a bare sibling with no aria wiring) → **GLASS-OWNED: standing BH-inbox relay per owner edict** (every glass-level defect relays); the consumer duty (do not justify `aria-hidden` by an unwired lane) rides R-1's X-W6 seat work.
- **R-11 — CONFIRMED (MAJOR)** [r2 missed, BOTH readers independently; re-verified by this seat] — **The add affordance is not merely inoperable (R-1), it is ILLEGIBLE: glass 7.0.0 WatercolorDot renders no slot at all** — my grep: `renderSlot|\$slots` → 0 in dist bytes; the render children are a fixed two-element array — so the `<Plus>` glyph (`MixSourceSelector.vue:~175`) is silently discarded, the `.add-slot-ghost` scoped rule (`:276-281`) centers a child that no longer exists, and the `disabled:*` classes can never match (dropped attr + meaningless on a span). The `.d.ts` declares an empty slots position, so vue-tsc is structurally silent. Independent double-discovery (both r2 readers) after all three r1 axes measured the element's death without noticing its content was gone. **Route: X-W6** with R-1 — the enclosing seat hosts the glyph; the dead CSS + dead classes delete in the same cut.

### MINORs

- **R-12 — CONFIRMED-RESCOPED (MINOR, ruling A)** [C-3 / D-9] — engine-interpolated fill between the 17 samples in a module whose stated rationale is "no engine divergence"; the docblock calls the paint "discrete-stop" (`:6-7`) two lines after calling the chip CONTINUOUS (`:3`). Obligation: reconcile prose + either amend the law with the measured band or rule for hard stops (design ruling; C-3's cure refuted). The e2e guard checks the stop LIST only, so the mechanism is present and unmeasured — the measurement rows are §UNPROVEN. **Route: NO-WAVE-OWNER** (chip file), with the design-ruling half flagged to **X-W10** (canonical design law).
- **R-13 — CONFIRMED-RESCOPED (MINOR, ruling D)** [D-6 + the inset-veil miss] — the 12% single-tone ring fails its docstring's bounding job in both schemes AND, painted `inset` over the specimen at 1em block-size, veils ~12% of the plate including both operand-identity endpoints — the ring is the bleed it forbids (`:46-48` vs `:10-12`). Normative floors do not bind (aria-hidden + text duplication). Cure: outset ring or `outline` in R-7's recipe. Ratios → §UNPROVEN. **Route: NO-WAVE-OWNER** (folds into R-7).
- **R-14 — CONFIRMED (MINOR)** [C-7 / L-1] — `serializeStop`'s `.replace(/ \/ 1\)$/, ")")` (`sample.ts:40`) is dead code: my read of `grammar.ts:287` — alpha 1 emits nothing, else a PERCENTAGE the literal-`1` regex cannot match either; and the vitest oracle is structurally blind (`preview-chips.test.ts:38-50` builds EXPECTED with the same `serializeStop` — the regex sits on both sides of `toEqual`). L-1's MAJOR demoted; doc-comment sub-claim killed. **Route: NO-WAVE-OWNER**.
- **R-15 — CONFIRMED (MINOR)** [C-9] — `k` has no domain guard: NaN → `[]` (truthy, violates `string[] | null`, caught only by the R-25 guard); Infinity → unbounded loop (arithmetic re-derived: `Math.max(2, ceil(∞/1)+1) = ∞`). Unreachable today (both hosts pass 3 args) but `sampleInterpolationRamp` is public barrel surface (`index.ts:29`). Cure: `Number.isFinite(k)` floor at entry. **Route: NO-WAVE-OWNER**.
- **R-16 — CONFIRMED (MINOR)** [C-10] — `:data-stops="stampStops(stops)"` is an unmemoised template call beside a computed sibling (`:24` vs `:34`; twin at `PreviewStrip.vue:44`); the host resolves each ramp Map twice per row (`MixConfigBar.vue:111/:133` — 26 lookups where 13 do). **Route: split — X-W6** (host halves) **+ NO-WAVE-OWNER** (chip halves).
- **R-17 — CONFIRMED (MINOR)** [L-6 + the dangling-labels miss] — the chip's composition lane is re-authored per call site and drifted (`MixConfigBar.vue:110/:132` lack the `min-w-0` that `GenerateControls.vue:244/:273` carry; AuroraPane wraps nothing; the chip's own `flex: none` (`:42`) exists because it owns no row); AND the host's three `section-label`s (`:98/:121/:145`) bind no control — no `for`, no wrapping — while the triggers carry duplicate `aria-label`s of which the third DIVERGES from its visible label ("Size mismatch" vs "Size mismatch strategy", `:145` vs `:147` — a 2.5.3 label-in-name hazard, and the drift has already begun). Verified in my MixConfigBar read. Overflow consequence → §UNPROVEN. **Route: X-W6** (MixConfigBar rows; the GenerateControls rows are §D-pinned — banked there, not here).
- **R-18 — CONFIRMED (MINOR)** [L-7] — the stops→gradient string is minted 7× in two axis idioms (`90deg` vs `to right`) across the demo (RR-LC's re-census adopted: both useGradientCSS sites, PreviewRamp, GenerateControls, useExtractSession, ComponentSliders, MixResultDisplay), and the O-14 stamp/paint pair spans two modules with the equality held only by the RED e2e leg (R-2). **Route: X-W6 partial** (owns `useGradientCSS.ts` + creates `gradient/model/sample.ts` — the gradient-side dedup lands there by design); residue **NO-WAVE-OWNER**.
- **R-19 — CONFIRMED (MINOR)** [L-8] — two incompatible OKLCh notations share the one `data-stops` contract: the library serializer's `oklch(62% 0.27 9.8deg)` (my dist run) vs `aurora-harmony-stops.ts:25-40`'s hand-rolled 4-dp unitless `oklch(0.7 0.15 220)`; the one oracle regex parses B and can NEVER parse A (my run: 0 vs 1) — this row and R-2(b) are one defect seen from both ends. **Route: X-W1** (oracle-reader dialect law) **+ NO-WAVE-OWNER** (one serialize home for producers).
- **R-20 — CONFIRMED (MINOR)** [L-5 / L-9 + the dead-barrel miss] — `color-chips/` is filed by authorship (zero `demo/color-session/` consumers; all 3 importers reach up-and-across — my census), a presentational leaf imports the color-engine module for a string join (`PreviewRamp.vue:17`; no shipped-bytes cost, `sideEffects:false`), and 3 of the barrel's 6 exports have zero external consumers while the only test bypasses the barrel via deep path (`preview-chips.test.ts:27-33` — my read). **Route: X-W8** (graph/barrel subtraction; feed X.W8.a's census — the six-file remainder census explicitly includes `demo/color-session` strays).
- **R-21 — CONFIRMED (MINOR)** [r2 missed, RR-LC; re-verified] — the sampler's honest-absence contract is half-throw: `sample.ts:82`'s `serializeStop` sits OUTSIDE the try/catch and rides `valueOrThrow` twice (`picker-color.ts:104-107/:204-211` — my read; `grammar.ts:290-297` err arms), so a serialization failure escapes a Vue computed as an exception while the documented posture is `null` (`:49-51`). Low reachability (mix outputs finite by construction); posture inconsistency certain. **Route: NO-WAVE-OWNER**.
- **R-22 — CONFIRMED (MINOR)** [r2 missed, RR-LC; re-verified] — the CONSUME MAP promises a GradientVisualizer consumer that has never existed (`index.ts:11-13` vs my census: 3 importers; `GradientVisualizer.vue:179-210` renders the SAME `INTERPOLATION_SPACES`/`HUE_INTERPOLATION_METHODS` vocabulary with text-only descriptions and no chip) — one of the two panes offering this exact choice previews it, the other silently doesn't. **Route: X-W6 decision** (owns GradientVisualizer.vue: wire the promised ramp or rule the restraint) **+ NO-WAVE-OWNER** (the index.ts map correction).
- **R-23 — CONFIRMED (MINOR)** [D-8 + the dangling-authority miss + the RTL register residual] — the chip is an unregistered second ornamental color-bearing species (VC:17 names Watercolor/data "the only" one; zero PreviewRamp/preview-chip/golden-plate hits across the V canon + proportion register — RR-DU's enumeration, spot-checked), its ONLY cited authority is a document that does not exist (`index.ts:13` → `docs/tranches/T/audit/w6-lane-d-record.md` — ENOENT, my `ls`), and it is likewise absent from the live RTL logical-property register its direction question belongs to. **Route: X-W10** (canonical design law owns species/registers; the F5-F8 laws must be re-homed or re-derived into the canon).

### INFO

- **R-24 — CONFIRMED (INFO, sizes corrected)** [D-12 / L-12 / C-11] — `data-stops` ships O-14 scaffolding to the production DOM (deliberate, `sample.ts:15-19`; T-era, no V ruling) — ceiling 9 simultaneous chips (one SelectContent mounts at a time), current shipped cost exactly 0 bytes (R-1), D-12's 13/6.5KB sizing killed; the dev-graph double-instance is no-consequence; the `90deg`/mask physical axis in two otherwise-logical files is fact, and the RTL premise-kills stand (`index.html:2-11` ships the deliberate `dir` seam; the U register rules gradient axes "DIRECTIONAL-by-intent"). **Route: NO-WAVE-OWNER** (notation only).
- **R-25 — REFUTED-AS-FILED (ruling C); INFO residual** [D-10] — the guard stays; the deletion cure is RETIRED with rationale.
- **R-26 — CONFIRMED (INFO)** [C-8 + the radius-hedge miss] — single-declaration totality: one unpaintable stop nulls the whole `background-image` while `data-stops` still claims 17 (no live trigger found — C-8's honest negative adopted); and `border-radius: var(--radius-sm)` resolves ONLY from the producer's Tailwind emit (no demo mint — my grep; `ColorComponentDisplay.vue` already hedges the same token). Both fold into R-7's recipe (`var(--radius-sm, 0.25rem)`). **Route: NO-WAVE-OWNER**.

### Adjudication-layer

- **ADJ-1 — CONFIRMED (MAJOR, process) — this seat's own find; neither r2 reader caught it.** **Two adjudicated records in this registry currently order opposite fates for `[data-color-surface]`.** GenerateControls (ruling R-C; wave G8; relay G-4 WITHDRAWN) rules it a dead selector — zero producers, re-verified by my grep today — and gates its DELETION from `foundation.css` (`grep -c … = 0`); this corpus's ruled cure (R-6, endorsed by RR-DU as "the structurally correct one") mints its first two producers. Landed blind in either order, one record breaks the other: the deletion strands the chip cure (chips re-blank in WHCM under a green G8), or the cure lands after deletion against a hook that no longer exists. **Sequencing rule (binding on both)**: whichever executes first re-reads the other's row — if the chip cure lands first, G8's grep gate is amended to exempt the now-live hook (the deletion premise "zero producers" is dated evidence, not law); if G8 lands first, the chip cure adds `.preview-chip` to the roster by class in the same commit it adds the attribute, or re-mints the hook WITH its first producer. **Route: formation boundary** (NO-WAVE-OWNER for the roster arm) + a dated note owed to the GenerateControls record's BANKS (E-3 addendum, never a rewrite).

## Killed-claims register

1. **C-3 as BLOCKER** — killed (ruling A). Mechanism survives at R-12 MINOR; the hard-band cure refuted (deletes the STRIP/RAMP species distinction; C's own P-6 prices the residue at ≤15/255 vs 185-190 prevented).
2. **C-1's chain enumeration** ("the only affordance … is the mix add-slot ghost") — killed: two `addColor` emitters verified (`:171` script, `:220` template); the conclusion survives only because both doors die by the same producer mechanism, which D-1 correctly enumerated and C-1 did not.
3. **C-1's cure** (restore `tag`/attrs passthrough in glass) — killed: reverses the ratified VC:91/P051 abrogation; the sites are assigned consumer work. D-1's enclosing-seat cure is the constitution-conformant one (GenerateControls R-D precedent).
4. **D-10 as filed** ("can never evaluate false"; delete the guard) — killed by the corpus's own C-9 (`k=NaN` → `[]` reaches the guard) and by the public-contract argument; opposite-edit contradiction with C-9 recorded (addendum law 2).
5. **D-11 / C-11's RTL premises** (both directions) — killed: `index.html:2-11` ships a ratified, commented `dir` seam (U-F58/BR-10) and the U register rules the gradient axis class "DIRECTIONAL-by-intent"; C-11's "nothing is broken" misreads a planted seam as absence of intent. Residual register-gap at R-23/R-24.
6. **D-2's `VISUAL-CONSTITUTION.md:194` citation** — killed: verbatim but off-target (governs the Generate chassis; the mix rows are not its object). Substance unaffected (my own dist run re-proves it).
7. **D-5's `VISUAL-CONSTITUTION.md:23` citation** — killed: an identity-TALLY exclusion, not a forced-colors grant; the governing authority is `foundation.css:653-700`'s own roster + hook, which D-5 re-invented.
8. **L-2's "four of five byte-identical, only `display` differs"** — killed: six declarations, two divergences (`overflow: hidden` Strip-only — load-bearing); the undercount understates the report's own case.
9. **L-3's cure** (`inline-size: var(--phi-4)`) — killed: preserves the exact two-anchor split two other seats file as MAJOR; mutually exclusive with C-5/D-3 and no r1 seat noticed. Merged cure ruled at E.
10. **L-1's MAJOR severity + its doc-comment sub-claim** — killed: unreachable dead code is not a behavior defect, and `sample.ts:38` truthfully describes the OUTPUT (`grammar.ts:287`), attributing nothing to the regex.
11. **L-4's "identical modulo the final encoder"** — killed: 16 stops @ t=i/15 (throws) vs 17 @ t=j/16 (nulls) — different grid, count, and error posture; the row is strengthened, the sub-claim is wrong.
12. **D-12's sizing** (13 chips / ~6.5 KB) — killed: one mounted SelectContent → ceiling 9; and per D-1's own BLOCKER the current cost is 0 bytes — an internal inconsistency in the same report.
13. **D-6's WCAG 1.4.11 invocation** — killed (RR-DU's own correction, ratified): the chip is aria-hidden and every row's meaning is text-duplicated; 1.4.11 does not bind. The finding survives on the design claim only, at R-13's rescoped severity.

## Superlatives (provenance preserved)

- **The sampler earns its keep, provably** (all three r1 axes + both r2 readers + this seat's read): `sample.ts:52-86` performs ZERO new color math — parse rides `parseColorIn`, sample rides `mixColors`, serialize rides `colorToCss`; the only arithmetic in the file is index math. The joint-dedupe (`:74`, `j = i === 0 ? 0 : 1`) has the closed form `total = segments·(perSegment−1)+1`, so every segment occupies exactly `1/segments` of the auto-distributed width — asserted precisely at `preview-chips.test.ts:65-76`. C's P-6 prices what it buys: the forbidden 2-stop arm deviates from library truth by mean ~116-119/255; the shipped chip by mean <1/255.
- **The library's hue-arc implementation is the best code on the cone** (both r2 readers; algebra re-read by this seat): `operations.ts:58-81` is total, validates finiteness and progress, and guards the zero-delta case in BOTH the `decreasing` and `longer` branches so an identical-hue pair never spins a full turn — the edge most implementations miss. It is also what makes R-3 provable from source alone.
- **Honest absence is designed, implemented, and tested end to end** (RR-DU, RR-LC): `sample.ts:58/:64/:81` null arms; hosts honor them; `MixPane.vue:93-103` extends the restraint to palettes mode with the reason in the comment; `preview-chips.test.ts:90-96` asserts all three arms.
- **The module names its own negative space** (RR-DU): `index.ts:16-23`'s RESTRAINT TABLE enumerates which dropdowns get NO preview, each with a reason — including "the picker's space catalog keeps its dot+specimen form … a ramp would be FALSE there." Refusing to preview because the preview would lie, written down before the code.
- **PreviewStrip's N-4 truncation is taste with its rejected alternative recorded** (RR-DU; my read): `PreviewStrip.vue:9-17` names the cap a taste knob, implements honest mask-fade truncation reading as "continues", and explicitly declines the width-step alternative.
- **The repo's forced-colors infrastructure is genuinely excellent — which is why R-6 stings** (both readers; my read of `:653-700`/`:815-842`): a first-principles two-tier WHCM policy, ONE named roster reused verbatim for print, a generic opt-in hook, and a real `outline` where shadow rings cannot survive.
- **The violated token discipline is near-universal, which is what makes the violations findable** (RR-LC): `--card-edge` has 13+ correct consumers and `--phi-4` a first-class Tailwind key with an anti-magic-value rationale; two chips are the entire violator set.
- **Seat discipline** (this seat's addition): all three r1 axes wrote substantial negative-proof sections and labeled their own unproven arms honestly (C-8's "NO LIVE TRIGGER FOUND", C-9's "direct call only", L-6's labeled hypothesis, D-11's labeled hypothesis); C and L both proved their citations transferred across the HEAD drift. That honesty is what let the r2 kills land fast — and let this seat kill the kills that were weak.

## UNPROVEN-NEEDS-LIVE residue (the visual-audit inputs)

Every row below is quarantined: entailed-or-corroborated by source but carrying live-only magnitudes. None is load-bearing for any verdict above. These are the component's visual-audit lane inputs; cells are chromium unless noted (I-20: chromium ≠ webkit ≠ safari-app, nothing inferred across cells).

1. **R-4/R-5 geometry**: lane 16.4px / description 11px/13.75px / chip 41.888×16.4 / aspect 2.5548 desktop · 2.9911 @390+320 · 2.869 @200% zoom; +2.64px per lane; ~23.8px extra in the 384px scroller; the 535.38/535.38 centring coincidence. Two seats agree independently (corroboration, not proof).
2. **R-13 ratios**: ring-over-white 1.281:1 / ring-over-black 1.231:1 / white-chip-vs-menu 1.150:1 / ring-vs-menu 1.114:1 — the direction is arithmetic-certain, the values depend on the live glass surface (`--foreground` is producer-owned; menu is composited translucent).
3. **R-2 painted side**: "17 painted / 0 stamped" — the 0 is my own offline derivation; the 17 requires the UA to canonicalize computed `background-image`. Either branch is defective, so the verdict is independent of it.
4. **R-1 live artifacts**: the CDP cascade dump (pointer-events from the inline style), real-click-adds-nothing, `<!--v-if-->` in all nine rows, the 2-failed/1-passed playwright tally, the Safari matrix — all ENTAILED by the producer bytes verified here, none re-derivable statically.
5. **R-12 measurement rows**: P-5's OKLab-default midpoint read ([133,134,134]) — consistent with CSS Images 4's non-legacy-stop default (oklch() stops force the OKLab default; a design-note for the ruling, not proof) — and the P-6/D-9 deviation tables that carry the severity argument.
6. **R-6 rendered result**: `background-image: none` / `box-shadow: none` / `forcedColorAdjust: auto` + `chip-forced-colors.png` (banked beside the corpus).
7. **R-17 overflow consequence**: whether the missing `min-w-0` produces a real flex-min-content overflow in a narrow SelectContent (L labeled it a hypothesis; the 60-shot matrix is chip-blind because closed SelectContent unmounts).
8. **R-24 RTL flip**: requires flipping `dir` at source (`index.html:11`), not at runtime — D's probe could not set the portaled listbox's direction post-hydration.
9. **R-15 Infinity wall-clock** (">170s hang") — non-termination proven by arithmetic; the duration is theirs.
10. **R-3 per-pair groupings**: D's probe scripts were written to `/tmp` and are NOT banked (unlike C's) — partially replaced by this seat's dist command, pasted in the header and re-runnable by any reader.
11. **The counterfactual**: whether the o14 chip legs pass once R-1's door and R-2's dialect are cured — unrunnable without editing the tree; belongs to X-W1's repaired oracle on X-W6's repaired door.

## Addendum clause (E-3 — appended beside the challenge corpus and the r2 returns, never rewriting them)

1. **A cure is adjudicated separately from its defect — ACROSS records, not only within one** (extends the GenerateControls addendum item 2, proven decisive twice here): the corpus prescribed three mutually exclusive cures for one declaration (ruling E) and two ADJUDICATED RECORDS prescribed opposite fates for one selector (ADJ-1). Every adopting wave must sweep the registry for the cure's OBJECT (selector, token, file) before landing, and every "dead — delete" ruling is dated evidence about producers-at-adjudication-time, not standing law.
2. **Opposite-edit detection**: C-9 and D-10 prescribe contradictory edits to `PreviewRamp.vue:31` and neither cites the other; RR-DU half-carried both. A corpus is not folded until every pair of rows touching the same line has been cross-read for edit-compatibility.
3. **Anti-rename identities**: this corpus's L-11 is the GenerateControls #20 / ColorSpaceSelector L-14 row (ONE identity, folded, G4-gated there); R-1's seat-transposition cure is ONE identity with the GenerateControls B1 census rows naming `MixSourceSelector:168,215`; R-19 and R-2(b) are ONE defect with two vantages. None may be re-booked under a new name.
4. **The a11y-defense audit rule** (from R-10): any `aria-hidden` justified by "the information is carried elsewhere" must name the WIRED channel — this corpus's justification cited a description lane that reaches no AT, and three axes plus one negative-proof section certified it without checking the wiring. Producer-side wiring defects relay to the glass BH inbox (standing owner edict), cited here.
5. **NO-WAVE-OWNER is a named carry**: `demo/color-session/color-chips/**` (both chips + sampler + barrel), `test/preview-chips.test.ts`, the foundation.css WHCM/print roster arm, `aurora-harmony-stops.ts`, and `mixStage.ts` are in NO X-wave's File Bounds (verified this session). The next formation boundary consumes rows R-4, R-6(+ADJ-1), R-7, R-8, R-12(file half), R-13, R-14, R-15, R-16(chip half), R-18(residue), R-19(producer half), R-21, R-22(map edit), R-24, R-25, R-26 whole — zero silent drops.
6. **Evidence portability**: the serializer dialect string, the regex 0/1 counts, and the distinct-ramp census in this record are byte-pinned to dist at HEAD `3487d73e` by this seat's own runs (commands pasted in the header); re-resolve all line anchors before executing any cure (D-19).

## DISSENT — preserved, not ruled away

1. **RR-DU vs this seat on C-3**: RR-DU killed C-3 outright where this seat preserved a MINOR mechanism row (R-12). RR-DU's position — that D-9 already files the residue and a kill keeps the register clean — is reasonable; this seat kept the row because the docblock's internal contradiction (`:3` vs `:6-7`) is C-3's find, not D-9's, and ids live forever.
2. **RR-DU's D-6 MAJOR**: preserved as filed for the owner — RR-DU holds that a boundary mechanism failing its stated purpose on every chip in both schemes is MAJOR regardless of normative floors. This seat rescoped to MINOR on the aria-hidden + text-duplication + live-only-ratios grounds (ruling D). If the visual audit lands the ratios at the predicted magnitudes AND the chips become reachable (R-1 cured), the severity question reopens with evidence.
3. **Both r1 BLOCKER severities on R-1/R-2 were adopted unanimously** across five seats and this one — no dissent exists on the headline; the only tension is scheduling (VC:91 names the Mix sites as assigned W19-W27 work; the X formation re-homes that execution at X-W6). A defect being scheduled does not de-BLOCKER it; a BLOCKER being ruled does not un-schedule it.
4. **Model-law note**: this seat ran as Fable by explicit spawn declaration (M-23/L-18); the standing "Fable sparingly" tension recorded by prior arbiters applies unchanged and is re-flagged for the same owner ruling.

## Closing verdict

**DEFECTIVE — ADJUDICATED (not verified): 2 BLOCKER · 9 MAJOR · 12 MINOR · 3 INFO confirmed on 26 canonical rows + 1 adjudication-layer collision (ADJ-1); 13 claims/sub-claims killed with written refutations; 13 missed rows admitted after independent re-verification. The component is a well-disciplined instrument (a genuinely lawful sampler, honest absence, named restraint) rendered dark by a V-ruled producer abrogation whose consumer half was never executed, guarded by an oracle that cannot run, cannot pass, and is run by no automation — with every repair route either owned (X-W6 · X-W1 · X-W8 · X-W10 · GLASS-relay · GenerateControls-identity) or named NO-WAVE-OWNER to the formation boundary. Nothing here stamps a wave.**

— end of apotheosis. The r1 challenge corpus, both r2 re-reader returns, and all banked probes/captures remain immutable beside this file per E-1/E-3.
