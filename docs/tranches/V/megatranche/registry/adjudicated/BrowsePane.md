# BrowsePane — adjudicated apotheosis · seat model receipt: `claude-fable-5` (Fable 5)

**Seat**: fresh-Fable adjudicator (M-23/L-18, SS-7 incredulity seat) · model id observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **HEAD**: `2e334231` · branch `tranche-u` · **Subject**: `demo/palettes/BrowsePane.vue` (360 lines) + its card/search/dialog/composable graph, route `#/browse`.
**Corpus**: 14 challenge passes across three axes (C ×3, D ×5, L ×6; r2 files govern — `challenge-C-implementation.md` pass 3, `challenge-D-design.md` pass 5, `challenge-L-library.md` pass 6), ~120 accusation rows, re-read by two hostile re-readers (design-and-usage · library-and-code) whose returns this seat then attacked.
**Method**: every verdict-level disagreement between the two re-readers was re-derived by this seat with its own instruments; every load-bearing kill was re-proven or overturned; the readers' MISSED rows were verified against live bytes before adoption. Dated evidence untouched; this file is new. Line anchors are HEAD `2e334231`; re-resolve before executing any cure (the D-19 lesson).

**Arbiter's own instruments this session (zero browser probes — probe parsimony; all rulings static + node):** full source reads of BrowsePane.vue (whole), EmptyState.vue (whole), PaletteCard.vue (:1-100, :130-254), PaletteCardMeta.vue (whole), PaletteColorStrip.vue (whole), PaletteCardGrid.vue (whole), PaletteCardSwatches.vue (:1-25), PaletteCardSkeleton.vue (:30-40), SearchFilterBar.vue (:1-60, :100-199), useBrowsePalettes.ts (whole), useFilteredList.ts, useVersionHistory.ts (:1-50, :90-112), useDialogBrowseActions.ts (:40-110), useTagEdit.ts (:35-50), usePaletteWiring.ts (:138-168), usePalettePorts.ts (shared-ref sites), api/palettes.ts (:20-60), PaneHeader.vue (:30-98 + animation grep), foundation.css (:650-700, :790-845), viewSchema.ts browse row, VISUAL-CONSTITUTION.md (:40-90), motion-quarantine.md (MQ-1, M-33), api crud-list.ts (`$text`, `colorMatcherFor`), api db.ts text index, browser/index.ts docstring; **the P-1 decider, run twice by this seat**: offline SFC compiles of EmptyState/PaletteCard/PaletteCardSkeleton/PaletteCardGrid via the repo's own `vue/compiler-sfc` 3.5.35 CJS bundle under `NODE_ENV=development` and `NODE_ENV=production` (dev → fragment root + `DEV_ROOT_FRAGMENT` on the first three, PaletteCardGrid single-root both; prod → single-root all); runtime-core.cjs.js reads at :1405-1425 (`afterLeave` installs, resets `isLeaving`, calls `instance.update()`), :6656-6693 (the `patchFlag & 2048` fragment branch recurses into transition-less children; `performRemove`'s `afterLeave` unreachable), :1762 (`isUnmount ? null : refValue` — the cardRefs mechanism); installed glass-ui 7.0.0 `dist/search.js` SearchBar setup read whole (`inheritAttrs:!1`; attrs-minus-class spread onto the `<input>`; no aria affordance; props exactly `{modelValue, placeholder, icon, tag, size, surface, variant}` per `SearchBar.vue.d.ts:4-12`); repo censuses: raw Tailwind palette literals (4 lines / 3 files), `showFeedback` consumers (BrowsePane ×4 + PalettesPane:207 — none in workbenches), `colorL` (3 hits: declare/serialise/comment), `coarse` (4 files, none the search field), `text-size-adjust` (0), `demo/palettes/export/index.ts` (absent), duplicate `./browser/dialog` specifier (BrowsePane.vue:191-194 + :199); tranche-X wave specs W0-W11 read for routing (W5.e posture strike, W7 §3/units/gates whole, W10 PRM unit, W1/W3/W8 greps).

**Routing authority**: `docs/tranches/X/COHESION.md` (X·V SPECIFIED; SS-7 = this seat; SS-8 = the visual-audit consumer of §UNPROVEN). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

## Scoreboard

**44 CONFIRMED (5 BLOCKER · 21 MAJOR · 14 MINOR · 4 INFO) · 15 KILLED with refutation · 14 reader-missed rows adopted · 1 binding-law RESCOPE (the §3.1 percentage band) · 14 UNPROVEN-NEEDS-LIVE residue rows → SS-8.**

Headline survivors: **D5-01** (the colour commons loses its colours under forced-colors and its ink in print — a one-attribute roster enrolment never made), **P-2** (typing the second character of any word empties the commons — two incompatible search predicates hand over at 2 characters, and **no tranche-X wave owns the seam**), and **P-1 as re-mechanised** (one press of Retry blanks the pane — but the true root cause is a leading template comment in EmptyState.vue that makes it a dev-compiler multi-root, the permanence is dev-gated, and the cure is one moved comment).

## Ruled disagreements (the incredulity seat's actual work)

| id | reader-DU (design-and-usage) | reader-LC (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **P-1 mechanism** (D-01/p1 · D-16/p2 · C·P-1) | CONFIRMED with the out-in microtask-strand mechanism ("leave-then-enter cannot sequence a branch that leaves and re-enters under the same key"), BLOCKER, "three-seat two-engine" | mechanism KILLED (afterLeave recovery path); replaced with the DEV_ROOT_FRAGMENT template-comment mechanism; severity MAJOR (dev-gated) | **LC wins on all three limbs** | (a) runtime-core.cjs.js:1413-1420: out-in installs `afterLeave = () => { state.isLeaving = false; … instance.update(); }` — the double-flip recovery DU's mechanism requires to be absent **exists**, unconditional but for job-flag 8; speed of failure is irrelevant. (b) My own dual compile of EmptyState.vue via the repo's compiler-sfc 3.5.35: `NODE_ENV=development` → fragment root, `DEV_ROOT_FRAGMENT`; `NODE_ENV=production` → single root. (c) runtime-core.cjs.js:6659-6666: the `patchFlag & 2048` branch recurses into children carrying no `transition`, so their `performRemove` (`:6676-6681`) never invokes `afterLeave` → `isLeaving` never resets → the permanent `<!---->` the probes photographed. The "three-seat two-engine" measurement is real and all three seats drove **dev servers** — the permanence claim never touched a shipped bundle. Decisive corroboration nobody cited: **PaletteCardGrid.vue:7-11's own comment documents this exact hazard** ("a leading comment node would make this a multi-root component… a fragment root resolves $el to the comment node") — the codebase knows the rule; EmptyState broke it. |
| **P-1 severity** | BLOCKER | MAJOR | **MAJOR** (dev-severity BLOCKER, recorded) | Prod compile is single-root by my own run; the shipped symptom degrades to a ~1-leave transient blank with the error plate re-mounting via `afterLeave`. One hinge stays open (§UNPROVEN row U14): no seat has observed a real `vite build` artifact of EmptyState. The cheapest correct cure — move the `:2-13` comment inside the root element (or single-wrap) — appears in **no** pass's cure list; LC's missed row supplies it and it also retires the latent hazard on PaletteCard + PaletteCardSkeleton (both compile dev-fragment by my run; both are today wrapped by transitions only at safe seats). |
| **R2.6** (the search input's accessible name) | RESTORED (pass 2's kill was a category error) | consistent (via N-L6 producer read) | **RESTORED — CONFIRMED MAJOR** | My read of `dist/search.js` SearchBar setup: `inheritAttrs:!1`; `o = computed(() => { let { class: e, ...t } = a; return t })`; `b("input", w({ref, type:"search"}, o.value, {value, placeholder, class:"input-bar-field", onInput}))` — no aria-label, no id, no `<label>`; prop union has no `ariaLabel` (unlike FuzzySearch, which ships one — the `:136` `e.ariaLabel` in the same dist file belongs to FuzzySearch, an attribution I checked because it could have killed the restoration). Pass 2 refuted an `<input>` claim with evidence about a `<button>` (SearchFilterBar.vue:5 `aria-label="Filters"` — verified present, on the trigger). And because the attrs spread lands on the input, `<SearchBar aria-label="…">` works **today** — pass 3's producer-ask cure (ASK-D dependency) is unnecessary, exactly as DU killed it. |
| **The §3.1 composition law** (D-01/D-05/D-06 pass 2 family) | CONFIRMED BLOCKER, citing VISUAL-CONSTITUTION:44's `64%…66.6666667%` band as binding | (not adjudicated on this axis) | **DEFECT CONFIRMED · CITED LAW RESCOPED** — this seat's own correction | `docs/tranches/X/waves/W5.md:49` + unit X.W5.e (:220-231): the owner program **strikes the `64%…66.6666667% / 33.3333333%…36%` tolerance as "fabricated"** and replaces it with one exact posture — `66.6666667% / 33.3333333%` when an inspector is selected, **100% protagonist when none is** — cited to CONSTELLATION.md P122, never restated as independent truth. The shipped surface fails **both** the struck band and the replacement posture: BrowsePane.vue:2 puts the Card shell on the field, PaletteCard.vue:5 is a hand-styled div (the exact inversion of :44's "every rendered bounded palette entity slip has exactly one Card shell, and the field/empty/inspector have none" — that clause survives the strike), viewSchema.ts:124-126 fills the companion slot with a **different route's pane** (`browse: { left: "browse", right: "palettes" }`), and no selected-palette inspector exists anywhere in the file. The defect is bigger than any resize; the adjudicated citation is the P-1 posture, not the dead band. VISUAL-CONSTITUTION:54's selection-seat law also survives the strike untouched. |
| **D5-02** (load-more focus/announcement) | not carried | two mechanism limbs KILLED, symptom preserved | **CONFIRMED-NARROWED MAJOR** | My template read settles both limbs LC killed: BrowsePane.vue:125-131 (`v-if="pm.loadingMore.value"` skeletons) and :132-144 (`v-else-if` trigger) are mutually exclusive, and useBrowsePalettes.ts:101 sets `loadingMore = true` **synchronously** before the await — so the trigger IS unmounted on the next flush ("trigger: 1 throughout" was a sampling artifact at ≥30 ms granularity, and a `:disabled` keep-mounted shape is a live candidate cure, not a proven dead end). What survives is the measured symptom: focus `BUTTON → BODY`, nothing announced, `statusRegions: 1` and it belongs to the other pane — arrivals are the genuinely silent state (see K7). |
| **D4-06 / PRM scroll-scrub** | CONFIRMED + pass 4's own narrowing overturned | consistent | **CONFIRMED MAJOR — pass 4's narrowing overruled** | PaneHeader.vue: no `prefers-reduced-motion` anywhere in the file (my grep); the three scroll-driven animations live only inside `@supports (animation-timeline: scroll())` (:177-191). motion-quarantine.md **MQ-1** (:131-169, a TRUE new-defect row — lawful to carry) measured `.pane-header-title` `transform: matrix(0.805831,…)` and height 43.98→35.44 px **byte-identically** at `no-preference` and `reduce` — the "pure transform whose from-state equals base state" defence fails because the user does not sit at scroll 0. X-W10 already specs the exact cure (W10.md:73-74, unit :199-209, gate G-4: the declarations move inside `@media (prefers-reduced-motion: no-preference)`). |
| **P-5** (the tag coercion) | not carried | reasoning KILLED, deletion recommendation kept | **KILL ADOPTED; deletion survives on N-7 grounds** | SearchFilterBar.vue:47 `v-if="availableTags.length > 0"` over a **required destructured prop** — on `undefined` the un-coerced path throws `undefined.length` in SearchFilterBar's render, so pass 3's "benign warning… the pane renders" comparison switched inputs mid-argument, exactly as LC ruled. And on the object input the coercion is the safer branch. The rows :215-220 still die as dead defensive code (live `/colors/tags` → `[]`, pass 2 N-7). |

**Cure-level rulings inside agreed verdicts.** P-1 cure 3 ("retry is a refresh") is REFUTED as a standalone cure (LC, verified: `loadRemotePalettes(true)` avoids the branch flip on failure but on **success** `error → wall` still unmounts EmptyState — blank-on-retry-success). P-4's "move the predicate server-side" cure limb is half-refuted (verified: `crud-list.ts` `colorMatcherFor` carries the identical `if (!oklabColors || oklabColors.length === 0) return false;` — the move fixes paging, not blindness). D3-03's structural limb gains a consumer-side cure no pass proposed, this seat's own: SearchBar accepts `tag` (string, default `"div"`) — `tag="label"` at the call site makes the whole bar an implicit label and every dead pixel operable, zero producer change.

## Adjudicated defect roster

Dispositions: **X-W5/X-W7/X-W10** = the named X·V wave owns the surface (unit/gate cited where the spec names one); **NO-WAVE-OWNER** = no X·V wave touches the surface — register for the next formation boundary; **GLASS-RELAY** = producer-owned, rides the SS-6 batched communique. Proof cells marked ⊙ were re-derived by this seat's own instruments; unmarked cells are adopted from a reader whose derivation this seat spot-audited.

### BLOCKER

- **B1 · D5-01 — forced-colors + print: the commons' specimen is the one colour surface not enrolled in the app's own colour-survival roster.** ⊙ foundation.css:678-696 (forced-colors) and :829-841 (print) share one named roster — `canvas, .spectrum-picker, …, .watercolor-swatch, …, [data-color-surface]`; PaletteColorStrip.vue:13-23 renders each palette member as `<div class="shrink-0">` with inline `backgroundColor` and its root (:3-11) is `overflow-hidden flex h-10 w-full` + `aria-hidden` — provably unmatched by every selector, in both blocks. The roster's stated purpose (:654-657 "the surfaces whose whole PURPOSE is to show a color… must survive WHCM's system-color substitution"; :793-799 "for a color tool the color IS the content") is inverted on the flagship surface while decoration (the watercolor dots, including the expanded card's duplicate swatches) keeps full colour. Pass 1's D-20 ("no treatment ships") is superseded — a 60-line treatment ships and omits exactly this. Cure: one `[data-color-surface]` on the strip root (`forced-color-adjust`/`print-color-adjust` inherit). Live readouts (white bands, `printColorAdjust: economy`) → U9. **→ X-W7.c** (the `PaletteSpecimen` successor MUST enrol; G12's purity gate is the natural carrier; foundation.css itself is X-W10's file — cross-noted so neither wave drops it).
- **B2 · P-2 (C axis) — two mutually incompatible search predicates hand over at 2 characters; typing the second character of a word empties the commons.** ⊙ useFilteredList.ts:9-11 lowercases and does NOT trim; useBrowsePalettes.ts:43-45 client substring over `name|slug`; :57 sends `q` server-side only at `trim().length >= 2`; api crud-list.ts `f.$text = { $search: q }` against db.ts's `{ name: "text" }` index — word/stem over `name` only vs substring over name-or-slug: not narrowings of each other in either direction. The untrimmed client predicate independently proves the leading-space arm. Live counts (`o`→3, `oc`→0, `ocean`→1 on the production commons) → U7. Compounded by B5: the zero renders as "the commons is empty." **→ NO-WAVE-OWNER** — no X·V wave touches this seam (W3 modifies crud-list.ts for policy/forkCount only; W7 never names the predicates). The single highest-priority unowned row in this apotheosis.
- **B3 · the Browse composition inversion (D-01/D-05/D-06 pass 2 · D4 concurrence) — the Card shell is on the field instead of the entity, no selected-palette inspector exists, and the mandated companion half is a different route's pane.** ⊙ BrowsePane.vue:2 `<Card tier="resting" …h-full>` (the field wears the Card; `h-full` is also why the empty invitation cannot content-hug); PaletteCard.vue:5-27 hand-styled div (the entity wears none); viewSchema.ts:124-126 `browse: { left: "browse", right: "palettes" }`; no third region in the template. **Binding law rescoped by this seat** (see the ruling table): the 64-66.7% band is owner-struck as fabricated; the adjudicated form is X.W5.e's exact posture (66.67/33.33 selected · 100% protagonist unselected) + the surviving one-Card-shell-per-entity clause. The 512/1024 measurement is corroboration only. **→ X-W5.e** (canon) **+ X-W7.d** (the selected-entity inspector build — W7.d moves rename/menu/feedback/versions/tags/export/publication/vote/delete there and deletes PaletteCard when every consumer has moved).
- **B4 · D-11/pass2 · D4-01 — the palette entity is a clickable `role="article"` with no selection seat, no focusable root, and no attribute recording the expanded state.** ⊙ PaletteCard.vue:5-27: no tabindex, no keydown, no aria-pressed/aria-expanded; `expanded` is a render prop only (:140 gates the swatches); BrowsePane.vue:97/:102 elect it. VISUAL-CONSTITUTION:54 (survives the W5.e strike) names the exact anti-pattern and the exact cure — a native `<button type="button" aria-pressed>` spanning the specimen/identity region. The component's own :2-4 comment argues the false dichotomy the law already answers. Two engines' tabIndex reads are corroboration; a div with no tabindex is unfocusable by construction. **→ X-W7.c/d** (props-only specimen + inspector seats; X-W4's seat law `W4.md:308,329` is the pattern authority).
- **B5 · D-06/pass1 · D3-07 · C-7 · P-2's plate — a wall emptied by ANY filter renders the true-empty invitation: a false statement about the commons plus an instruction that cannot change the result.** ⊙ BrowsePane.vue:80-91 passes `:empty="displayedBrowse.length === 0"` with three hard-coded strings, none reading `searchQuery`/`tierFilter`/`selectedTags`/`colorSearchParams`; `displayedBrowse` (:339-349) stacks four narrowing dimensions above it. EmptyState.vue:2-13 designs exactly two species; the third (filtered-to-zero) was never designed. Live reproduction ×3 seats → U7. Made the *dominant* outcome of typing by B2. **→ X-W7.d/g** (the state species is BrowsePane's; the strings die in W7.g's OM-15 execution — which also deletes the `eyebrow` prop and element, so cures must land against the post-W7.g plate).

### MAJOR

- **M1 · P-1 (D-01/p1 · D-16/p2 · C·P-1) — one press of Retry blanks the Browse wall; root cause: EmptyState.vue's leading template comment makes it a multi-root component under the dev compiler, which breaks the `<Transition mode="out-in">` leave and strands `isLeaving` forever.** ⊙ Mechanism fully re-derived (ruling table row 1; my own dual compiles + three runtime-core reads). Dev = permanent blank surviving search/clear/route round-trip/backend restore (KeepAlive holds it — one-seat matrix → U6); prod = transient. Severity MAJOR with a recorded dev-BLOCKER face. Cure (LC's missed row, adopted): move the comment inside the root element — strictly cheaper and more general than all three of pass 3's cures; also retires the same latent shape in PaletteCard.vue and PaletteCardSkeleton.vue (⊙ both compile dev-fragment). The born-RED gate the C axis asked for (load `#/browse` with `**/palettes*` aborted, press Retry, assert the plate is still there) rides **X-W1**'s browser suite. **→ X-W7.g** (EmptyState modify-carve) **+ X-W1** (the gate).
- **M2 · R2.6 (restored) — the route's primary control, the first tab stop in the pane, has no accessible name.** ⊙ Producer bytes + call site (ruling table row 3). BrowsePane.vue:10-14 passes only v-model/class/placeholder; a placeholder is not a name (VISUAL-CONSTITUTION §4.1). One attribute at the call site — lands on the input today via the attrs spread. **→ X-W7** (BrowsePane modify).
- **M3 · D-13/pass2 — the palette identity absorbs 100% of the width deficit: every decoration in its flex row is `shrink-0` and the identity is the only flexible item.** ⊙ PaletteCard.vue:44 row; identity span :53-59 no shrink-0 with `line-clamp-2 sm:line-clamp-1` (line-clamp sets `overflow:hidden` → flexbox resolves `min-width:auto` to 0 — the DU reader's stronger proof, checked, sound; it also explains D4-04's alternating mobile heights); Badges :64/:72 and every PaletteCardMeta chip shrink-0. Citation correction carried: PaletteCard.vue:82 (menu wrapper) carries no shrink-0 and is not a competitor. **→ X-W7** (scope 5/6: priority-collapsing chip row + N-fixture battery; G9/G10).
- **M4 · D4-06 + MQ-1 — scroll-driven header motion runs in full under `prefers-reduced-motion: reduce`; the app's two PRM guards are structurally blind to timeline-driven animation.** ⊙ + MQ-1 (TRUE row, cited by id; M-33 not carried — see K6). Pass 4's own narrowing OVERRULED (ruling table row 6). Blast radius nine panes; BrowsePane hosts via :2 `.pane-scroll-fade`. **→ X-W10** (unit at W10.md:199-209; gate G-4; probe `prm-scroll-timeline.mjs` re-run unmodified).
- **M5 · D-15/pass1 · D3-06 · C-6 · P-4 — "Find by Color" is a silent partial search: pane-local, page-scoped, an undocumented radius, and `return false` for any row without an index — structurally blind to half the live commons.** ⊙ BrowsePane.vue:336-355 (`radius = 0.15` :343; `return false` :346; the comment :353-354 declining the server predicate the client already serialises); the pager (:133) keys `pm.hasMore`, which the pane-local filter cannot see; a zero-match colour search lands in B5's false plate. Sharpened per DU (verified): 0.15 is the server's own default (`crud-list.ts` `query.colorRadius ?? 0.15`) — a duplicated protocol constant with two editable homes, not a bare literal. Live 5-of-10 empty `oklabColors` → U7. **→ NO-WAVE-OWNER** (the B2 seam's colour face; one design must own both).
- **M6 · P-3 — slug search is offered by the client predicate and dead in the product above one character.** ⊙ useBrowsePalettes.ts:44 `|| p.slug.includes(q)` vs the name-only `$text` index; the pane displays the identifier its own search cannot find (`show-slug`, :101). **→ NO-WAVE-OWNER** (B2 seam).
- **M7 · D-18/pass2 — sort and the text query are invisible in the filter register; the badge and every label are silent across sort changes.** ⊙ SearchFilterBar.vue:189-195 verbatim (`tier` + `selectedTags.length` + `colorSearchActive` — sort absent, text absent); the whole grammar behind one 32px trigger (:5), badge gated `activeFilterCount > 0` (:7-12). Against VISUAL-CONSTITUTION §4.1 / PROPORTION-AUDIT PR-07. **→ X-W7** (W7.a owns browser/search; the register cure is unspecified in the spec — flag at wave open).
- **M8 · D-23/pass2 structural · R2.2 — the specimen strip draws N adjacent colour cells with no separator, no gap, and no edge against the card body.** ⊙ PaletteColorStrip.vue:13-23 + parent :6-11 (no gap, no border, no outer rule). The two seats' numeric contrast methods disagree (WCAG vs OKLab ΔE) → U3; the structural gap needs no probe: a five-step neutral ramp renders as one undivided block. **→ X-W7.c** (the specimen rebuild; G12).
- **M9 · D5-03 — `contain: content` on the grid clips the entity's offset cartoon caster on its −X axis for every card and its +Y axis for the last card; one wall, two shadow treatments.** ⊙ PaletteCardGrid.vue:50-52 `contain: content` (= layout paint style; paint clips to padding box); :4 one column, no padding (gap-3 never pads after the last item) → zero slack by construction; PaletteCard.vue:19 adopts `cartoon-surface` (producer caster −3/−5/−7 X, +3/+5/+7 Y) while :16-18 explicitly declines a card-level clip to protect it. LC's missed limb folded in: BrowsePane.vue:2's `.pane-scroll-fade` (`contain: layout style paint`, PaneHeader.vue:54-57) is a SECOND paint-clip ancestor — any cure must audit both. Pixel samples → U11. **→ X-W7** (scope 7 "shadow follows the rounded silhouette… at the card root"; G11).
- **M10 · D5-04 — the true-empty invitation's `#emptyAction` slot is plumbed end-to-end and unused at the call site, while the error plate 15 lines above gets a real Button.** ⊙ PaletteCardGrid.vue:28-30 → EmptyState.vue:64; `grep -n emptyAction demo/palettes/BrowsePane.vue` → nothing; :68-77 fills `#action` with Retry. PaletteCardGrid.vue:19's own comment: "The CTA slot survives on the caption" — it survived in the primitive and died at the call site; the only affordance ships on the path the user cannot fix. **→ X-W7.g** (the plate is rebuilt there; the CTA decision must be made explicitly, not dropped again).
- **M11 · C·C-10 + C-2 — every entry to `#/browse` re-fetches unguarded, and `loadingMore` can strand permanently, replacing the load-more trigger with two skeletons forever.** ⊙ usePaletteWiring.ts:143-155 `{ immediate: true }` watch, browse branch unguarded while both `admin-*` branches are emptiness-guarded three lines below; useBrowsePalettes.ts:112 `if (gen === loadGeneration) loadingMore.value = false;` — any concurrent fresh load (search debounce :157-164, sort, tier, tags) bumps the generation and the release never runs; BrowsePane.vue:126-131 then pins the skeleton branch. A two-character fix on an otherwise exemplary protocol (see S8). **→ NO-WAVE-OWNER** (neither file is in any X·V wave's modify set).
- **M12 · C·C-1 — every typed transport failure is collapsed to the constant `"Failed to load palettes"`, which the plate renders as its own detail line — one sentence said twice; the DevMisconfig arm deletes a 400-character actionable diagnosis.** ⊙ useBrowsePalettes.ts:79 discards `e`; BrowsePane.vue:65-66. The discarded species are real: availability.ts's `ApiUnavailableError`/`DevMisconfigError`, client.ts's `ApiProblem`. Arm-narrowing carried from D pass 5 §5.6: with `VITE_API_URL` set the headline is accidentally true for the CORS case. **→ X-W7** (scope 3: the 31 `catch → console.warn` sites ruled once as a table; G7).
- **M13 · D5-02 (narrowed) — activating load-more by keyboard destroys focus (`BUTTON → BODY`) and three new cards arrive announced by nothing; the pane's only status regions are transient and the populated state has none.** Mechanism limbs killed (ruling table row 5; K15); symptom + K7's inversion survive. Focus transcript → U10. **→ X-W7** (BrowsePane owner; the cure — move focus to the first arrived card or keep the trigger node identity stable, plus one polite region — needs a design decision the spec does not yet name; flag at wave open).
- **M14 · L6-1 (core) — the `card` cluster is the demo's shared palette-presentation vocabulary (4 features consume it) homed three directories deep inside one of its own consumers; the `browser/` top-level seam has zero importers.** ⊙ Import sites verified (MixSourceSelector.vue:8, GenerateControls.vue:16, ExtractWorkbench.vue:196-200, seam-importer grep empty). Two sub-limbs KILLED (K13, K14) — the surviving identity is the home, and it is real. **→ NO-WAVE-OWNER** (W7 splits domains *within* palettes; no wave moves the cluster out; W8's graph subtraction never names it). Note: W7.c *creates* `PaletteSpecimen` inside `browser/card/` — the next formation should decide the home BEFORE that file multiplies the audience defect.
- **M15 · L6-2 — `UserSortMenu` is admin-only, homed in the `search` cluster, exported from the barrel `#/browse` imports; the public route fetches an admin component in dev.** ⊙ Four-hit grep verified (sole importer AdminPane.vue:86; barrel edge search/index.ts:4; SearchFilterBar's import block clean). No shipped-bytes claim (tree-shaken, per pass 3's build probe). Dev trace → U13. **→ NO-WAVE-OWNER** (W8 deletes its dead `variant` attr — a different defect).
- **M16 · L·L-1 — the export dual path: `export.ts` (132 lines, ships) vs the certified 12-module `demo/palettes/export/` (29 tests, no barrel, zero runtime consumers).** ⊙ `ls demo/palettes/export/index.ts` → absent; usePaletteExport.ts:2-9 imports `from "./export"` → resolves to the file. **→ X-W7** (scope 2; G4/G5/G6 — the certified serializers become the shipping path).
- **M17 · L·L5-1/L5-2 — the ports hand every surface the SAME `searchQuery` and `expandedId` refs: typing in Browse filters My Palettes and drives the admin user query.** ⊙ usePalettePorts.ts: one `ref("")` feeds useBrowsePalettes/useAdminUsers/useColorNameQueue/filteredSaved and is re-exported on all three ports; one `actions.expandedId` on all three. **→ X-W7.d** (usePalettePorts in the modify set; the split is not named — flag at wave open).
- **M18 · (DU missed, adopted) — in the wall's most-reached empty state (text-only filter) ALL FOUR recovery affordances are absent, and one predicate produces the absence: `activeFilterCount` omits the text query, and the SAME `v-if` gates both the badge AND the entire "Clear all filters" control.** ⊙ SearchFilterBar.vue:189-195 + :7-12 + :110-120; BrowsePane.vue:80-91 (no `#emptyAction`, hard-coded copy). So: no badge, no clear control anywhere in the product, no plate action, copy that says "go publish." Six D passes each held a fragment; none joined them. B2 makes this the dominant real-query outcome. **→ X-W7** (a + g).
- **M19 · (DU missed, adopted) — the vote control is a toggle whose only state register is a raw Tailwind palette colour: no `aria-pressed`, no state in the name, on the card's de-facto accessible representative.** ⊙ PaletteCardMeta.vue:43-54: `:aria-label="\`${voteCount} votes, click to vote\`"` (count + imperative, never voted-state); Heart `palette.voted ? 'fill-red-500 text-red-500' : 'text-muted-foreground'`. Exactly VISUAL-CONSTITUTION §4.1's forbidden construction. Census correction (⊙, this seat's own): raw palette literals = **4 lines / 3 files** in demo (Markdown.vue ×2 `@apply`, this line, ActionFeedback.vue:8) — not DU's nine sites; the substantive claim survives (the sibling EmptyState.vue:19 correctly uses `text-destructive/80`). **→ X-W7.d** (the vote seat moves to the inspector; W4 seat law is the pattern; W7.f/G17 owns the count formatting).
- **M20 · (DU missed, adopted; + D4-03's surviving first limb) — the provenance chip silently shows two different identifiers in identical chrome: `displaySlug = userSlug ?? slug`, so on the live commons half the wall is attributed to an author and half labelled with the palette's own slug — markup, accessible name and copy action all calling both "slug". And the whole block is `v-if="expanded"`-gated, so `show-slug` is inert on the collapsed wall.** ⊙ PaletteCard.vue:231, :143, :140; PaletteCardSwatches.vue:3-19 (`Copy slug ${displaySlug}` :15). VISUAL-CONSTITUTION §7: render nothing when there is no principal — never a different object's identifier. Live 5-of-10 `userSlug: null` → U7. **→ X-W7.c/d.**
- **M21 · (LC missed, adopted) — a failed remix is silently swallowed: `fork()` catches, warns, returns `undefined`; `onFork`'s `if (!forked) return;` exits BEFORE the catch, so the wired `onForkError → showFeedback` surface is unreachable for every HTTP/network failure of the fork itself — and two docblocks assert the opposite.** ⊙ useVersionHistory.ts:97-108; useDialogBrowseActions.ts:52-56 + the ":40-44 never a silent swallow on the pane path" docblock; BrowsePane.vue:262-264 wires the surface that cannot fire. **→ X-W7** (G7's swallow table + G13's one-visible-result).

### MINOR

- **m1 · C·P-6 — a colour search survives every subsequent sort/tier/tag/text change and silently intersects with it; it appears in no URL, no chip, no response.** ⊙ Two clear sites only (:357-359, :329-332); all filter handlers replace `remotePalettes` wholesale without touching it. **→ NO-WAVE-OWNER** (B2/M5 seam).
- **m2 · C·C-5 / L5-3 / D-16-p1 — `cardRefs` is write-only: the `el &&` guard swallows Vue's unmount `null` (⊙ runtime-core:1762), so detached component instances accumulate and operation verdicts can be delivered into unmounted cards, silently.** BrowsePane.vue:94, :209; consumers :228-230, :237-241, :249, :264. **→ X-W7.d** (dissolves when feedback leaves the card; PaletteCard deletes).
- **m3 · D3-04 (re-raising R2.8) + LC's missed unscoped-home limb — `.pane-scroll-fade` names a fade, implements none (⊙ two properties, PaneHeader.vue:54-57; corroborated by motion-quarantine **M-33**, which rules the fade's existence FALSE — the misnomer claim is the lawful carry), is declared unscoped inside a leaf component with nine external consumers, and BrowsePane.vue:2 stacks `w-full mx-auto` where `mx-auto` can never act.** **→ X-W10** (PaneHeader modify) **+ X-W7** (the BrowsePane class string).
- **m4 · D3-03 structural — the search bar's visible chrome exceeds its operable target: the root is a `tag`-rendered div, the input a child, no label pairing, no root click — the padding is dead to the pointer.** ⊙ dist bytes. The 5.7px/36px numbers → U-class (single-seat). Cure candidate (this seat's own): `tag="label"` at the call site. **→ X-W7.**
- **m5 · D5-05 — four computed type sizes in one `items-center` metadata row, one of them unauthored (the vote `<button>` carries no type class and inherits).** ⊙ Class census verified (identity `text-subheading`, chips `text-micro`, counts `text-mono-small`, button unclassed). Computed px → U12. **→ X-W7** (Meta rebuild; X-W10's canon is the authority).
- **m6 · D5-06 — one object, four nouns across seven authored strings; the header is the outlier and "the wall" is internal vocabulary leaked into copy at exactly one site.** ⊙ All seven strings byte-verified (:3, :13, :65, :84, :85, :86, :142). **→ X-W7.g** (OM-15 rows).
- **m7 · (DU missed, adopted) — BrowsePane's only two authored type decisions breach the closed type matrix: `font-display` (Fraunces) on the Retry Button (:72) and the load-more Button (:139), where VISUAL-CONSTITUTION:75 assigns controls `text-small`/Plus Jakarta Sans non-bold and :78 closes the matrix.** ⊙ Verified. Pass 2's D-25 found the same law broken in EmptyState and nobody checked the file under audit. **→ X-W7.**
- **m8 · (DU missed, adopted) — three of the card's four metadata marks carry their entire meaning in `title` (hover-only, dead on touch, unreliable in AT); the fork *indicator* is a naked icon span with no accessible name at all, and the SAME GitFork glyph means two different things 30px apart.** ⊙ PaletteCardMeta.vue:7-13, :16-23, :26-33. **→ X-W7** (scope 5's chip-row redesign).
- **m9 · (LC missed, adopted) — the tag catalog loads exactly once (`onMounted`, BrowsePane.vue:222-224), useTagEdit.ts:40-48 swallows failure with a bare `catch {}` and no retry path exists; the pane is KeepAlive'd, so one transient failure hides the Tags filter (`SearchFilterBar.vue:47`) for the session.** ⊙ Verified. **→ X-W7.d** (useTagEdit in the modify set; G8 "Tag truth").
- **m10 · (LC missed, adopted) — `onSave` reports "Saved!" unconditionally for a purely local, verdict-less operation (`onSaveRemote` = `addPublishedPalette`, synchronous), on the same feedback rail where its siblings report awaited server verdicts.** ⊙ BrowsePane.vue:226-232; useBrowsePalettes.ts:122-124 vs :144-154/:186-217. **→ X-W7.d** (G13).
- **m11 · P-5 as rescoped — the tag coercion (:215-220) is dead defensive code (live payload is `[]`), differently-unsafe rather than strictly-worse (K11); delete and bind directly.** **→ X-W7** (G3's `strictTemplates` closes the class).
- **m12 · L6-3 — `demo/palettes/mix.ts` has zero consumers in its own feature; both importers are the mix workbench.** Adopted (LC verified the two sites). **→ NO-WAVE-OWNER** (with M14 — one home ruling).
- **m13 · L·L5-4 — `request<T>` is a type assertion, not a decoder (`return res.json()` unvalidated), the root cause under the `getTags` type lie and m11's coercion.** Adopted. **→ NO-WAVE-OWNER** (demo/platform/transport is in no wave's modify set).
- **m14 · (LC missed, adopted) — the colour-search server plumbing is dead client code: `colorL/colorA/colorB/colorRadius` declared (api/palettes.ts:27-30) and serialised (:50-53) with zero callers (⊙ 3-hit grep).** State as unexercised plumbing, not an available seam. **→ NO-WAVE-OWNER** (B2/M5 seam).

### INFO

- **i1 · L6-4 — `SKELETON_COUNT = 4` named and reasoned (:204-207) vs the hard-coded `v-for="i in 2"` (:130) in the same file.** ⊙ **→ X-W7.**
- **i2 · (LC missed) — duplicate module specifier: `"./browser/dialog"` imported at :191-194 and again at :199.** ⊙ **→ X-W7.**
- **i3 · (LC missed) — both casts in `displayedBrowse` (:344-345) disable checking for nothing: `types.ts:32` already declares `oklabColors` in the identical shape; removal type-checks unchanged.** ⊙ **→ X-W7.**
- **i4 · L·N-L6 — glass-ui 7.0.0 ships no `seated` rung (`SearchVariant = inline|bare|floating`; no `well` surface), so the booked `.search-seated` swap (three panes) is producer-blocked: ASK-D must be re-relayed against 7.0.0, never repaired demo-side.** ⊙ d.ts verified. **→ GLASS-RELAY** (SS-6 communique; carries the measurement).

## Killed-claims register

- **K1 — P-1's out-in microtask-strand mechanism** (pass 3 C, adopted by reader-DU): KILLED. runtime-core.cjs.js:1413-1420's `afterLeave` recovery re-renders the current branch when any leave completes; "the faster the failure, the more certain the blank" is false — speed is irrelevant. The real mechanism is EmptyState's dev-only fragment root (M1). ⊙
- **K2 — P-1's shipped-product permanence / BLOCKER framing**: KILLED. The permanence is gated on the dev compiler preserving comments; ⊙ my `NODE_ENV=production` compile of the same SFC is single-root, so prod takes the element path and `afterLeave` fires. All probe evidence in the corpus drove dev servers. Residual hinge → U14.
- **K3 — P-1 cure 3 ("Retry is a refresh, not a first load")**: KILLED as standalone — on retry-success `error → wall` still flips and the same strand occurs; it converts blank-on-failure into blank-on-success. ⊙
- **K4 — pass 2's kill of R2.6**: KILLED (the kill, not the finding). Category error: an `<input>` claim refuted with a `<button>`'s aria-label (SearchFilterBar.vue:5) and a metric (`namelessButtons`) that counts only buttons. R2.6 restored → M2. ⊙
- **K5 — D3-01's authored-mobile-rung mechanism + its producer-ask cure**: KILLED. ⊙ No `coarse` rung touches the search field (4-file census), no `text-size-adjust` anywhere — the 1.5× inflation is engine text-autosizing under the emulated device, not an authored rung; and the producer ask is unnecessary because SearchBar's attrs spread lands `aria-label` on the input today. Surviving carries: `.input-bar-field` never sets `text-overflow`, and the placeholder is load-bearing only because the field is nameless (M2). Real-device symptom → U2.
- **K6 — pass 2's kill of R2.8 ("the fade exists")**: KILLED. PaneHeader.vue:54-57 declares exactly `contain` + `scroll-timeline`; the ::before veil is a TOP-edge material on a different element. motion-quarantine **M-33** independently rules the bottom fade's existence FALSE — that FALSE row is honoured by *not* carrying any fade claim; the misnomer finding (m3) is its lawful complement. ⊙
- **K7 — "filtering to zero announces nothing" (D-20/p2 · R2.5/p1)**: KILLED as stated. EmptyState.vue:28 gives the empty branch `role="status"` — the filtered-to-zero plate announces (falsely, per B5). The `liveRegions: 0` figure counted `[aria-live]` attributes, which `role=status` never carries. The correct narrower statement — Browse has no PERSISTENT status region and the populated state's arrivals are the genuinely silent events — survives inside M13. ⊙
- **K8 — "the loading states are inaudible" (D-10/p1)**: KILLED and inverted. ⊙ PaletteCardSkeleton.vue:35-36: every skeleton root is `role="status" aria-label="Loading palette"` — 4 on entry + 2 per page: OVER-announced, not silent. The wrapper-div prohibited-name half stands as a fragment.
- **K9 — D4-03's second limb ("the slug is rendered as a clipboard action, not provenance text")**: KILLED. ⊙ PaletteCardSwatches.vue:8-11 renders the slug as text on exactly the `text-mono-small` provenance rung; the Copy control is its labelled sibling. First limb survives inside M20.
- **K10 — D4-06's own narrowing ("the transform arm is defensible; TIGHTEN the opacity arm only")**: KILLED. MQ-1's measurement: the transform scrub is byte-identical under `reduce` at scroll 200. The full gate (X-W10's media-query move) is the cure, not a tighten.
- **K11 — P-5's "strictly less safe on a strictly wider input set"**: KILLED. ⊙ The comparison switches inputs mid-argument; on `undefined` the un-coerced path also throws (`SearchFilterBar.vue:47` on a required destructured prop), and on the object input the coercion is safer. Deletion recommendation survives (m11).
- **K12 — P-4's cure limb ("the server at least… " implying the server move cures the blindness)**: KILLED in half. ⊙ `colorMatcherFor` carries the identical `return false` on empty `oklabColors`. The paging limb is true. Folded into M5's cure constraints.
- **K13 — L6-1's audience sub-limb ("the seam's docstring asserts the opposite of the graph")**: KILLED. ⊙ browser/index.ts:5-6 sanctions sub-barrel reach in the parenthetical the finding dropped, and :9-16 documents the side path as intended. The seam is over-declared, not lying; the home defect survives (M14).
- **K14 — L6-1's defineExpose consequence ("part of a contract three workbenches also consume")**: KILLED by enumeration. ⊙ `showFeedback` has exactly two consumers, both in demo/palettes; the workbenches consume props/events only.
- **K15 — D5-02's two mechanism limbs ("the button never disappears"; "the loading skeleton never rendered once")**: KILLED. ⊙ `v-if`/`v-else-if` are mutually exclusive and `loadingMore` is set synchronously — the trigger IS unmounted; 30 ms sampling cannot see a sub-30 ms branch. Symptom survives (M13).

**Law-rescope (recorded beside the kills, distinct in kind): the VISUAL-CONSTITUTION:44 percentage band is owner-struck as fabricated (X.W5.e) — no adjudication row may restate it as independent truth; the binding form is the exact P-1 posture. The one-Card-shell clause and the :54 selection-seat law survive the strike.** ⊙

## Superlatives — preserved with provenance

1. **EmptyState.vue** (D pass 5 / DU): species separated at the ROLE level (`role="alert"` vs `role="status"`), reading measures, threaded action slot, a caption rung that records its own contrast measurement (:49-54, :95-101). Eight consumers inherit every cure. The pane's failure is under-use (M10) — and this seat adds the ironic coda: the best-designed atom in the graph is also the P-1 hazard carrier, in a codebase whose sibling file (PaletteCardGrid.vue:7-11) wrote the hazard rule down.
2. **PaletteColorStrip's weighted-segment design** (:38-71): population-proportional specimen with an 8% legibility floor, renormalisation, three-level fallback and a zero-width guard — the reason B1 is a BLOCKER and not a shrug. ⊙
3. **foundation.css's one named colour-surface roster** (:650-696, :793-841), declared once, reused by forced-colors AND print, with `[data-color-surface]` as the extension point and the policy's purpose stated in source. The architecture makes B1's cure one attribute. ⊙
4. **PaletteCardSkeleton's motion/announcement discipline**: two temporal registers with stated semantics; announcement split from motion in one sentence; producer seams consumed rather than forked.
5. **The app-wide motion architecture and BrowsePane's conformance**: zero local keyframes, sanctioned `vj-morph` family, tokens only; five D passes attacked the motion axis and — MQ-1's scroll-timeline blind spot aside, which is PaneHeader's — all five failed to break the Vue-family half.
6. **PaletteCard.vue:228-230** — `useSafeAccentFn("well")`: ink certified on the actual material tier, verbatim what §4.1 demands. Where this codebase reached its own contrast law it applied it correctly.
7. **CHALLENGE-C pass 3's `probe-c3-live-proxy.mjs`** — the 20-line Node re-issuing proxy that defeated both the localhost latch and the CORS wall and turned the empty-plate lie into a measured two-character cliff. Every subsequent live seat on a palette surface should reuse it (SS-8 note).
8. **useBrowsePalettes' keyset protocol**: one shared filter builder for fresh load and continuation, a monotonic `loadGeneration` guard applied on success AND error paths. Its one defect (M11's finally-scope) is a two-character fix on an otherwise exemplary composable. ⊙
9. **availability.ts** — the transport that refuses to let a dev-config error masquerade as "backend offline", with a pure, testable detector and an actionable 400-character diagnosis. That M12 throws this away one layer up is what makes M12 sharp.
10. **vite.config.ts:37-50** — self-aliases generated from `package.json#exports` with anchored per-entry regexes; the alias table cannot drift from the export map by construction.

## UNPROVEN-NEEDS-LIVE residue → SS-8 visual-audit inputs

Each row keys this file's ids; every one is a bounded probe, and rows U1-U13 name the only seats that ever measured them.

- **U1** (D3-02): the filter trigger at 32×40, radius 9999px at aspect 0.8, overhanging the 36px bar — geometry AND attribution unlocatable statically (glass-ui 7.0.0 ships no `.button` min-height; the only `min-height: var(--control-h-md)` is on `.tags-input`). Single-seat.
- **U2** (K5 residue): whether real-iPhone text-autosizing reproduces the severed placeholder the emulated captures show.
- **U3** (M8): the numeric dark-scheme strip contrast (WCAG 1.41:1 vs OKLab ΔE 0.0878/0.0998 — two seats, two metrics).
- **U4** (D-08/p1): the failed-sort stale wall under the new sort's label — branch shape ⊙-verified (sortMode set before await; catch never clears `remotePalettes`; the error plate is suppressed on a populated wall by :62's `length === 0` arm); the runtime arm needs a reachable-then-failing API.
- **U5** (D-21/p2): the 471.7px layout collapse on route re-entry — mechanism sound (M11's unguarded watcher + `SKELETON_COUNT` 4 vs row count), measurement single-seat single-engine.
- **U6** (M1): the four-step unrecoverability matrix + the live retry timelines (one seat; dev-only per K2).
- **U7** (B2/B5/M5/M6/M20): every live production-commons count — 3/0/0/0/1 cards, 5-of-10 empty `oklabColors`, 5-of-10 `userSlug: null`, the leading-space arm.
- **U8** (P-7): the single-mount correction of pass 2's `[30,30]` cardRefs pair.
- **U9** (B1): the measured `stripDistinctBg: ["rgb(255,255,255)"]`, `watercolorCount: 11`, and the `printColorAdjust` economy/exact readouts. The roster-membership half needs no probe.
- **U10** (M13): the BUTTON→BODY focus transcript and `statusRegions: 1`.
- **U11** (M9): the two-sample pixel luminance measurement (left Δ0.0/12px vs gutter Δ96.8/7px).
- **U12** (m5): the computed 20.352/18.608/16.4/11 px sizes.
- **U13** (M15): the dev-server network trace fetching `UserSortMenu.vue` on `#/browse`.
- **U14** (K2's hinge): one real `vite build` artifact observation confirming EmptyState compiles single-root in this repo's exact plugin order — strongly indicated (⊙ prod-bundle parser default + my NODE_ENV compile), never observed on a built artifact.

## Closing verdict

**DEFECTIVE — ADJUDICATED.** 44 rows survive two hostile re-readings and this seat's counter-attack (5 BLOCKER · 21 MAJOR · 14 MINOR · 4 INFO); 15 claims died with written refutations, two of them killing a prior pass's kill; the corpus's most-litigated mechanism (P-1) was wrong in every seat until the library reader's compile — re-proven here twice — reduced it to one moved comment; and the two defect families a real user hits first — the search cliff and the colour-search blindness — plus the card cluster's home, the refetch/strand pair, and the transport decoder have **NO owning wave in tranche X** and are hereby registered for the next formation boundary. Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3; nothing here stamps a wave.
