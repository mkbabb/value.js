claude-fable-5 (served model id)

# fr-NotationPills — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter seat)

**Seat**: apotheosis arbiter · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/equation/NotationPills.vue` (47 lines) + `web/src/lib/equation/notation.ts` (48), at fourier HEAD `cd26c6533adc32dfe1453d74117d3cb73b89ea16`. Subject files CLEAN in the WT at this seat's own `git status` (the WT carries 28 dirty rows — the in-flight `^4.0.0` bump both re-readers described; the subject and its data module are not among them). Installed producer: `@mkbabb/glass-ui@4.0.0` (this seat's node read).
**Corpus**: three challenge axes (D·design 19 defects / L·library 20 findings / C·consumption 13 defects — all three `claude-opus-5[1m]`), challenged by two hostile re-readers (design-and-usage · library-and-code), adjudicated here. Both re-reader returns were themselves attacked; four re-reader cells died against this seat's own instruments (see DISSENT).

**This seat's own instruments (all static + node, read-only; zero browser probes — probe parsimony)**: NotationPills.vue WHOLE (47 — anchors exact for L and C; D's systematic −1 template / −6 style offsets confirmed); notation.ts WHOLE (docblock :3-8, literals :15-17, TIER_INFO :20-42 colors :26/:32/:38, energyColor :44-48 re-declaring them verbatim); BasisSelector.vue:240-293 (the `:249-257` retint-law comment · `.basis-toggle` resting legs :259-262 · the deliberate hover pair :264-268/:274-277 · `[aria-pressed="true"]` body :269-273 with NO static fallback · the 639px compaction block) and `@reference "tailwindcss"` at :211; SliderControl.vue:95 `@reference`; NotationPills.vue + FunctionInput.vue carry NO `@reference` (my grep); EasingPicker.vue:5-30 (the `role="group"`/`role="menuitemradio"`+`aria-checked` doctrine verbatim, incl. "`aria-pressed` would mislabel a radio as a toggle"); **dist cascade decider**: `node_modules/@mkbabb/glass-ui/dist/styles/components.css` → `grep -c "@layer"` = **0**, containing verbatim `.hover\:bg-accent:hover`, `.active\:bg-accent\/80:active`, `.aria-pressed\:bg-accent[aria-pressed=true]`, `.active\:scale-\(--scale-press-btn\):active`, imported unconditionally at `dist/styles/index.css:201`, and `package.json#exports["./styles"] = "./dist/styles/index.css"`; **built artifacts**: `web/dist/assets/NotationPills-CjQ8uEBB.css` emits `.notation-active[data-v-fe3be5c9]{background:color-mix(…12%…);border-color:color-mix(…40%…);color:var(--pill-color)}` BARE, while `VisualizationView-Cx8Xulfm.css` emits `.basis-toggle[aria-pressed=true][data-v-489339ff]{background:var(--pill-color)}` + the `@supports (color:color-mix(in lab,red,red))` upgrade — a SYNTHESIZED static fallback + guard from a source with no static declaration (the C·D-5 mechanism decider); **R6 substrate**: my own node run over `~/Documents/Codex/2026-08-02/fourier-mobile-safari-instance-source-plan-r6/outputs/DERIVED-REGISTRIES.json` — the NotationPills loops row byte-identical (`cardinality: 18`, `FINITE_ARRAY_LITERAL_SOURCE_DERIVED`), loops n=16, callsites n=512 {RESOLVED 110 · LOCAL_OR_GLOBAL_OPEN 396 · DYNAMIC_TARGET_OPEN 6}, Button 89/89 open; **contrast (third instrument)**: my own WCAG 2.x node recompute from the cited literals + token chain — labels on own 12% tint: light **3.841/5.370/4.667**, dark **3.849/2.779/3.192**; 40% borders vs page: light 1.840/1.891/1.837, dark 1.535/1.381/1.453 — reproducing C·D-3 and both re-readers to 2dp (three independent instruments now agree); **export map**: 80 keys at the installed 4.0.0 — `./tabs`, `./toggle-group`, `./toggle-chip` present, `./chip` ABSENT; **identity-guard re-reads at the cited rows**: fr-BasisSelector (B-3 · M-4 · M-7 · M-12 · M-13 · m-3 · m-22 · i-3 · i-4 · K-7 · K-9 · R-2 · R-5), fr-FunctionInput (D-4.2+C-6 · L-m5 · C §0 row 7), fr-EquationView (D·D-M12 incl. "NotationPills rides the same edit" + the R2-K1/BC-1 amendment precedent · RD-4/C·D-04 ruled MINOR on the fr-ConvergencePlot RD-3 threat model · K-16), fr-EquationPanel (M-N13 — the unhidden-glyph accessible-name row, BANKED · M-N1 six duplicate literals · D-L3 · D-L10+C-10), fr-EquationModeToggle (FR-EMT-15(b)/(c)/(d) · K-5 whose kill-note confirms the hand-rolled pair's re-emit), fr-CanvasOverlayButton (FR-COB-2 · FR-COB-8 · FR-COB-12 with the R-7 challenge-inherited ruling · FR-COB-26 · FR-COB-28 relay packet), fr-AdminAuditLog (AA-2 · AA-22), fr-AppHeader (FR-AH-2), fr-BasisCanvas (D-1 by reference), fr-InfoCard (FR-IC-1/F8-REACH-01), fr-AdminUserList (FR-AUL-16), fr-GalleryFeaturedCarousel (FR-GFC-15).

## Scoreboard

**31 adjudicated rows — 2 BLOCKER · 7 MAJOR · 17 MINOR · 5 INFO — with 13 identities FOLDED BY REFERENCE into the banked registry (never re-booked). Killed-claims register: 17, including four re-reader cells corrected by this seat's own instruments and two identity-guard demotions of grades both re-readers agreed on. Six missed finds adopted after re-proof; a seventh (the unhidden glyph) caught as a re-book of banked fr-EquationPanel M-N13. Superlatives: 7.**

Blocker-weight survivors: **FR-NP-1** (the active pill's label and border fail WCAG contrast in both themes, painted by three frozen mode-invariant module literals — the fr-BasisSelector B-3 mechanism on a second set of literals, four-of-six label cells and six-of-six border cells failing at three independent instruments) and **FR-NP-2** (the R6 derivation registry ships a fabricated 6× loop denominator under a proof label asserting source-exactness — the lying-label substrate defect, reproduced byte-exact by this seat).

**The single most consequential ruling**: the corpus's unanimous top-priority cure — bind `aria-pressed` and copy `.basis-toggle` — is the WRONG ARIA semantics for this control and would ship the banked fr-BasisSelector M-4 defect into a second file. The amendment is relayed to the banked cure cells (see FR-NP-3).

## Ruled disagreements (the seat's actual work)

| # | question | re-reader 1 (design-and-usage) | re-reader 2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **Cascade model: are glass-ui's competing utilities layered?** (decides L·L-8/D·M-2's mechanism and D·M-8's status) | Unlayered in dist components.css → specificity TIE vs the scoped rule → build-contingent (kill of the mechanism) | "@layer utilities" model; "statically decidable and decided" (confirmation) | **Re-reader 1 wins, on my own grep.** | `grep -c "@layer" dist/styles/components.css` → **0**; the hover/active/aria-pressed utilities extracted verbatim from that sheet; imported unconditionally (index.css:201) and `./styles` resolves to it. `.notation-active[data-v]` (0,2,0) vs `.hover\:bg-accent:hover` (0,2,0) is a tie decided by emitted order — the fr-App R-2 UNPROVEN cell, the FR-COB-8 banked producer class, and the fr-CanvasOverlayButton R-7 ruling (the layered mechanism is challenge-inherited) all align. Re-reader 2 built two confirmations on a model the registry had already corrected — recorded in DISSENT. |
| 2 | **D·M-8 (PRM squish) severity** | MINOR, folds to banked FR-COB-12 with the R-7 mechanism correction + bounded harm | MAJOR, "statically decided" via source-layer arithmetic | **MINOR — fold to FR-COB-12 (identity guard).** | FR-COB-12 is banked MINOR with the mechanism ruled: the UNLAYERED shipped `active:scale-(--scale-press-btn)` (verbatim in my 0-@layer grep) defeats the `@layer components` reset version-independently; the PRM transition allow-list omits `scale` so the survivor is an instantaneous snap. Producer class; NO-WAVE-OWNER + BH relay (already in the FR-COB-28 packet). Re-graded here would be a re-book. |
| 3 | **C·D-5's mechanism (why the fallback guard was not generated)** | Ratified C's "static declaration first produces the guarded pair" as "exactly right" | Killed it: BasisSelector's byte-identical no-static-declaration source got the guarded pair; the discriminator is the missing `@reference "tailwindcss"` | **Re-reader 2 wins, byte-level, on my own extraction.** | The built `VisualizationView-Cx8Xulfm.css` emits `.basis-toggle[aria-pressed=true]{background:var(--pill-color)}` + the `@supports` upgrade — a SYNTHESIZED fallback from a source (my :269-273 read) declaring only `color-mix`. So "nothing to fall back to" does not discriminate; pipeline membership does. `@reference` present at BasisSelector.vue:211 + SliderControl.vue:95, absent from NotationPills.vue and FunctionInput.vue (my grep); the bare vs guarded emissions match exactly. Re-reader 1's ratification is corrected (DISSENT). New mechanism row minted: FR-NP-8. |
| 4 | **The no-ARIA row's grade** | BLOCKER (compound 1.4.1+1.4.3+4.1.2) | MAJOR (folds to fr-FunctionInput D-4.2+C-6) | **MAJOR — fold (identity guard).** | The NotationPills ARIA limb is banked TWICE: fr-EquationView D·D-M12 ("NotationPills rides the same edit") and fr-FunctionInput D-4.2+C-6 (the twelve-control family, MAJOR, citing notation.ts:5-7's doc-comment by name). The compound's other leg (contrast) is separately booked as FR-NP-1; grading the compound twice double-counts. Fold at the banked MAJOR — with the cure AMENDED (ruling 6). |
| 5 | **L·D-1 (cardinality 18) grade** | MAJOR ("honestly scoped as substrate") | BLOCKER ("blocking for any F.W4 pass that reads cardinality") | **BLOCKER (substrate-scoped) — L's own two-tier logic preserved.** | The axis's D-1/D-2 severity split is principled and correct: D-2's `LOCAL_OR_GLOBAL_OPEN` is an honest open leaf (MAJOR); D-1 wraps a fabricated number in `..._SOURCE_DERIVED` — a label whose whole content is a claim of source-exactness — and a gate went GREEN over it. A lying label outranks an honest hole. Re-reader 2's mitigating find stands and bounds the blast: the callsite row's `disposition: "CARDINALITY_SOURCE_EVALUATION_REQUIRED"` is honest; only the loops table launders. Scoped to the audit substrate, never to the component. |
| 6 | **Is `aria-pressed` the cure?** (all three axes prescribe it; L §8 makes it repair #1) | NO — mutually-exclusive single-select; banked M-4 + EasingPicker doctrine say radio semantics (the headline missed find) | Treats `BasisSelector.vue:144`'s `aria-pressed` as "sibling correct" | **Re-reader 1 wins — against the bank and the tree.** | fr-BasisSelector M-4 (banked MAJOR) books BasisSelector's own `aria-pressed` AS the defect on a non-binary control; EasingPicker.vue:10-16 (my verbatim read) is the in-tree doctrine: mutually-exclusive options carry `aria-checked` under a radio-family role — "`aria-pressed` would mislabel a radio as a toggle". NotationPills is single-select over 3. The corpus's #1 repair would propagate an adjudicated defect. Amendment relayed to the banked D·D-M12/D-4.2 cure cells (the R2-K1/BC-1 precedent shows banked-cell amendment is the practice). Correct targets, all at the pinned 4.0.0: `role="radiogroup"` + `role="radio"`/`aria-checked` by hand, or `ToggleGroup type="single"` (`./toggle-group` in the 80-key map, my run), or `SegmentedTabs` (FR-NP-6). |
| 7 | **The rehydration limb's grade (C·D-13 sub-finding)** | MAJOR | MAJOR ("folds to fr-EquationView C·D-04/RD-4") | **MINOR (latent) — both re-readers over-graded against the bank they cited.** | fr-EquationView's ruled disagreement table grades RD-4/C·D-04 **MINOR** on the fr-ConvergencePlot RD-3 threat model — same file, sibling loader: no shipped writer produces a shapeless record; the trigger is stale/tampered sessionStorage or a future `NotationMode` member rename. The notation limb's genuine increment (it reaches a RENDERED control: no-pill-active unrepresentable state, invisible to AT, 422 on every compute) books as the row's content, not as a second severity — the fr-InfoCard D-7/L-3 precedent exactly. |

## Adjudicated defect roster

Dispositions are ADJUDICATED, not VERIFIED — X·F specs are forming; routings are M-25 agglomeration input. Line anchors are HEAD `cd26c653`; re-resolve before any cure.

### BLOCKER

- **FR-NP-1 · D·B-2 ∘ C·D-3 ∘ L·L-4 (contrast) — BLOCKER — F.W3/W4.** The active pill's label and border fail WCAG in both themes, painted by three frozen mode-invariant `hsl()` literals (notation.ts:15-17 → `--pill-color` at :24 → :43-45). Three independent instruments (C's, both re-readers', now this seat's) agree to 2dp: label on its own 12% self-tint — light **3.84 / 5.37 / 4.67**, dark **3.85 / 2.78 / 3.19** against the 4.5:1 bar (`--control-text` = small register; four of six cells FAIL, incl. Trig in LIGHT — the theme D's table cleared); 40% border 1.38–1.89:1 everywhere against 1.4.11's 3:1 (six of six FAIL; only C booked the border row). Dark is reachable pre-paint (web/index.html:22-29). **Identity**: the mechanism family is banked at **fr-BasisSelector B-3 (BLOCKER)** — frozen module literals → `--pill-color` → 12%/40%, six failing ratios both themes; this is a SECOND instance on different literals, cross-booked to the family, no third arithmetic minted. **Cure clause (bound by K-8)**: the sound cure is banked at fr-BasisSelector M-12 — bind `var(--viz-*)` (or a `:root`/`.dark` pair per style.css:113-127) STRAIGHT THROUGH in CSS; `color-mix(in srgb, var(--viz-fourier) 12%, transparent)` accepts any `<color>` — the idiom already sitting in the HOST file at FunctionInput.vue:244-249, fifteen lines from the corpus's broken prescription. NEVER route through `VIZ_COLORS`/`cssVarToHex` (K-8). One modelling caveat carried from re-reader 2: 1.4.11 for a state indicator is properly also measured against the unselected state's border; direction unaffected. `/visualize` composited figure is SS-13; the `/equation` light-mode Trig failure at 3.84 stands unconditionally (opaque host).
- **FR-NP-2 · L·D-1 — BLOCKER (audit substrate, NOT the component) — F.W0.** R6's `DERIVED-REGISTRIES.json.loops` ships `cardinality: 18` for the 3-element `NOTATION_OPTIONS` under `FINITE_ARRAY_LITERAL_SOURCE_DERIVED` — reproduced byte-exact by this seat's own node run. L's generator reconstruction (a string-unaware comma counter: 3 × (3 intra-object + 2 inside `"hsl(6, 72%, 49%)"` + 1 trailing) = 18; `tabs` 5×3=15; `PRESETS` 8→7 on the multi-line branch) predicts all three array rows exactly; both object-literal rows are correct — 3-of-3 array literals wrong. The R5-7 class uncured at the round that closed R5-7's letter, under a GREEN gate (`GREEN_BROAD_DERIVATION_RELEASED`). Bounding fact (re-reader 2's find, adopted): the callsite row's `disposition` is honest (`CARDINALITY_SOURCE_EVALUATION_REQUIRED`) — only the loops table launders. **Rider**: any F.W4 consumer must treat `loops[].cardinality` as fabricated for array literals; the L·C-1 "6×" census cell is its plausible downstream victim (18÷3). Severity split vs FR-NP-4 preserved deliberately (ruling 5).

### MAJOR

- **FR-NP-3 · D·B-1 ∘ L·L-1/L-2 ∘ C·D-2 (no programmatic selection) — MAJOR, FOLDS BY REFERENCE — F.W3/W4.** No `aria-pressed`/`aria-checked`/`role`/group name anywhere in the 47 lines (my grep; sole "aria" hit is none — `variant="outline"` at :20 is the only substring match); colour is the sole carrier; the producer's `aria-pressed:` CVA arms are dead at this callsite; the docblock (notation.ts:3-8) claims the pattern it does not implement. **Folds**: the NotationPills limb → banked **fr-EquationView D·D-M12** ("NotationPills rides the same edit") ∘ **fr-FunctionInput D-4.2+C-6** (the twelve-control family, MAJOR) ∘ fleet law **fr-DarkModeToggle N-2**. Corpus BLOCKER grades demoted (ruling 4). **CURE AMENDMENT (ruling 6, relayed to the banked cells)**: the edit NotationPills rides must NOT be `aria-pressed` — it is a mutually-exclusive single-select; per banked fr-BasisSelector M-4 and the EasingPicker.vue:10-16 doctrine the correct semantics are radio-family (`role="radiogroup"` + `role="radio"`/`aria-checked`), or `ToggleGroup type="single"`/`SegmentedTabs` at the pin. L·L-2's docblock row survives inside this fold with its intent-claim struck (K-9) and one sharpening: the docblock is not merely counterfactual — it is prescriptively wrong.
- **FR-NP-4 · L·D-2 — MAJOR (audit substrate) — F.W0.** Every glass-ui callsite in R6's registry is an unresolved leaf: 89/89 Button, 396/512 overall (`LOCAL_OR_GLOBAL_OPEN`), both NotationPills HOST callsites RESOLVED — my run byte-exact to both re-readers. No instance denominator over the glass surface — the principal F.W1 budget surface — can be derived from it. MAJOR not BLOCKER: the label is honest (ruling 5's other half).
- **FR-NP-5 · C·D-1 (variant deleted at 7.0.0, typecheck-SILENT) — MAJOR, FOLDS — F.W1.** At tag v7.0.0 Button declares `emphasis|tone|size|iconOnly|loading|type|disabled|class`; zero `variant`; no `vueCompilerOptions`/`strictTemplates` in fourier's single tsconfig → `variant="outline"` falls through as a junk DOM attribute, the pill silently re-tiers to `emphasis: "secondary"`, and `vue-tsc -b && vite build` stays GREEN. `size="sm"` survives (`ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">`) — this file has ZERO hard breaks and one silent degradation, the worse kind. **Folds**: fleet identity → banked **fr-BasisSelector M-7** (F.W1 SCOPE AMENDMENT: prop-VALUE-axis diff) ∘ **fr-AdminAuditLog AA-2** (census-extension BLOCKER, 37-file blast radius) ∘ **FR-COB-2** (the silent/loud asymmetry limb). D·M-3's TS2322 mechanism is K-1. The census-gap leg (lane-frontend §5 break table omits Button; :508 budget derived from an incomplete table) is the SAME banked identity + i-4 — never re-booked. The `.btn-pill`-deletion rider (NotationPills.vue:38/:40 become load-bearing after the hop) books at FR-NP-13.
- **FR-NP-6 · C·D-6 (SegmentedTabs is the seat that survives the hop) — MAJOR, extends banked fr-BasisSelector M-13 — F.W3 (shadow retirement), F.W1 sequencing rider.** My node enumeration of the installed 4.0.0 exports: **80 keys; `./tabs` + `./toggle-group` + `./toggle-chip` present; NO `./chip`**. `SegmentedTabOption {label; value; icon?; disabled?; tooltip?}` + `modelValue: string` + `#option` slot `{option, active}` + `responsive` Select collapse — structurally matching `NOTATION_OPTIONS` (the `color` field rides the slot — C's honest limit stands); EquationView.vue:13 already imports it. The producer-guidance INVERSION verified by both re-readers at the tag: ToggleChip's 4.0.0 docblock steers toward hand-wiring; v7.0.0 `chip/README.md` steers exclusive selection to ToggleGroup/SegmentedTabs — so lane-frontend.md:441's `./toggle-chip → ./chip` arrow points at the seat the 7.0.0 producer abandons. Census half already banked (**fr-FunctionInput L-m5**: CENSUS:100's unqualified `./chip` at a pin where it does not exist). **Adopted missed half (re-reader 1)**: the tri-package RESOLUTION DEADLOCK (CENSUS:107-110) does NOT gate the a11y repair — `ToggleGroup type="single"` and `SegmentedTabs` exist at the pin and survive the hop, so the group semantics, roving focus and single-selection model are repairable TODAY; L·C-1's "every repair must be authored against the installed 4.0.0 **Button**" is too strong by exactly this much.
- **FR-NP-7 · L·L-6 (two hosts, two unsynchronised notation states; /visualize silently non-durable) — MAJOR — F.W4.** `/equation` is cache-seeded and durable (EquationView.vue:30 → :204 → FunctionInput.vue:30 defineModel → :223); `/visualize` is a bare component-local ref (EquationPanel.vue:18) behind `v-if="showEquation && store.epicycleData && !isEditing"` (VisualizationView.vue:231) — close/reopen re-initialises to `"trig"` and the `watchDebounced(…, {debounce: 300, immediate: true})` at :61-65 immediately re-POSTs, repainting a DIFFERENT equation than the one the user was reading. Unbanked at fr-EquationPanel (my grep — its D-L3 no-unmount-abort row is adjacent, not this); **FR-EMT-15(b)** (eqMode re-inits `"sigma"` every mount) is the banked sibling class member on the other route — this is the class's sharpest member (state loss + silent semantic repaint). L's scope honesty preserved: the defect is the hosts'; the leaf's contribution is having no persistence seam for a host that forgets.
- **FR-NP-8 · re-reader-2 M1, ratified (the missing `@reference "tailwindcss"`) — MAJOR (new mechanism row) — F.W4.** NotationPills.vue's scoped block carries no Tailwind at-rule, so it never enters the `@tailwindcss/postcss` → Lightning pass that synthesises `color-mix` fallbacks + `@supports` guards; its byte-identical twins that DO carry `@reference` (BasisSelector.vue:211, SliderControl.vue:95) got the guarded pairs in the same 2026-06-12 build — this seat extracted both emissions and the discriminating trio verbatim (see instruments; the mechanism kill of C·D-5's "static colour first" is K-3). Re-reader 2's repo-wide census adopted after spot-verification: 53 scoped-style SFCs / 35 with `@reference` / 18 without, **7 of the 18 using `color-mix` — 19 declarations shipping unguarded** (NotationPills 2, FunctionInput 6, EquationModeToggle 1, ImageUpload 4, ContourEditorCanvas 2, FullscreenViewer 2, CanvasControlsDock 2). Registry-swept: the only banked `@reference` row is the INVERSE defect (**FR-GFC-15**, a dead directive in a feature-free block — same 53/35 denominators, corroborating the census); this mechanism is unbanked. Cure: one line per file; do NOT execute C·D-5's "declare a static colour first" (K-3). Fresh-build reproduction is the one UNPROVEN-NEEDS-BUILD residue (prediction: reproduces — the mechanism turns on pipeline membership, not toolchain version).
- **FR-NP-9 · C·D-4 ∘ L·L-4 (structural leg) — the palette sits outside the app's theme-reactive colour arm AND outside value.js — MAJOR, extends banked fr-BasisSelector M-12 — F.W3/W4 (cure), F.W2 tie.** App.vue's MutationObserver + `resolveVizColors()` re-resolve every `--viz-*` surface on theme flip; `NOTATION_OPTIONS` is a module-eval const of literals — frozen. The producer ships the analogous tokens as light-dark pairs with materially LIGHTER dark arms (light-dark.css:145-147) — the mechanical cause of FR-NP-1's dark column. C's three-way-bypass framing (colour arm · value.js · the hand-rolled colors.ts the census already books as F.W2 scope) is the banked M-12 shape — value.js is a declared dependency consumed zero here while the palette is its textbook consumer. **The prescribed cure is K-8** (it lands on two banked defects); the cure clause lives in FR-NP-1.

### MINOR

- **FR-NP-10 · D·M-2 ∘ L·L-8 (no authored hover/press arm for the active state) — MINOR (mechanism corrected) — F.W3/W4.** The design gap is static and stands: the archetype authors BOTH legs deliberately (:264-268 neutralised inactive hover; :274-277 the 12%→16% active step — my read); this file authors neither. The MECHANISM both the corpus and re-reader 2 asserted is killed (K-6): the competition is unlayered-vs-unlayered → a source-order tie, build-contingent (fr-App R-2 cell; FR-COB-8 class). Either outcome is a defect — active pill inert under the pointer, or flashing `bg-accent` and losing its tint — but WHICH paints is UNPROVEN-NEEDS-BUILD. Composed with FR-NP-11, a PRM user may get only the signal they opted out of.
- **FR-NP-11 · D·M-8 (press squish under PRM) — MINOR, FOLDS to banked FR-COB-12 — NO-WAVE-OWNER (producer) + BH relay (already in the FR-COB-28 packet).** Facts confirmed; mechanism per the R-7 ruling (unlayered dist utility, version-independent — my 0-@layer grep is the third independent observation); harm bounded (transition allow-list omits `scale` → instantaneous snap). Cured for free at 7.0.0. Re-reader 2's MAJOR + source-layer mechanism declined (ruling 2).
- **FR-NP-12 · D·M-7 (`.cm-serif` phantom) — MINOR, FOLDS — F.W1 token parity + F.W4.** `--font-serif-math` consumed at one site, declared nowhere (banked; re-confirmed at fr-EquationPanel's instruments: repo grep → 0). **Folds**: fr-BasisSelector m-3 ("define or retire") ∘ **fr-AppHeader FR-AH-2** (the blocker-weight font-truth cluster: 26 sites/17 files → OS serif; the label meanwhile renders in an unloaded-PJS-fallback sans — two unrelated fallback families in one pill). Component contribution: `text-[1.3em]`/`min-w-[1.2em]` are optical constants tuned for Computer Modern, applied to generic serif. D's MAJOR takes the banked MINOR here (severity follows the fold); the cluster's weight lives at FR-AH-2.
- **FR-NP-13 · L·L-7 ∘ D·m-2 (two inert declarations; one token opt-out) — MINOR — F.W1 rider.** `:38 border-radius: 9999px` and `:40 justify-content: center` are inert at the pin (`.btn-pill` supplies both — surfaces.css:120/:134 + `rounded-pill`); both become load-bearing when 7.0.0 deletes `.btn-pill`. L's sharpest cell adopted: :38 hardcodes 9999px PAST `--radius-pill` — a radius retheme skips these pills. C·D-7's carve-out is K-5.
- **FR-NP-14 · L·L-3 (no union→table exhaustiveness bond) — MINOR — F.W4.** Plain `T[]` annotation; line 11 constrains members, not the set; no `as const satisfies`, no `Record<NotationMode,…>`; a 4th mode compiles clean and renders an all-inactive row. Sharpens banked **FR-EMT-15(d)**, which cites `NOTATION_OPTIONS` as "the table idiom next door" — the exemplar is itself unbound (amendment relayed). Re-reader 2's MAJOR declined: the reachable-today path is FR-NP-15's row; grading it twice double-counts.
- **FR-NP-15 · C·D-13 sub-finding (unvalidated rehydration reaches the prop) — MINOR (latent), FOLDS to banked fr-EquationView RD-4/C·D-04 — F.W4.** `loadCachedInputState` (useEquationCache.ts:25-30; the file is 49 lines) returns bare `JSON.parse` typed `CachedInputState | null`; `?? "trig"` guards nullity only; an out-of-union string renders zero active pills (no `v-else`, no narrowing at :23-24), invisible to AT (FR-NP-3), and 422s on every compute (`pattern=r"^(trig|exponential|polar)$"`, equations.py:22/:41). Banked precedent grades the threat model MINOR (ruling 7 — both re-readers corrected down). The notation limb is the rider: it is the loader's one limb that reaches a rendered control, and a future `NotationMode` member rename under the un-bumped `-v2` key converts it live (bank-trigger: any wave renaming a member must bump the key or add the validator).
- **FR-NP-16 · L·L-5 ∘ C·D-11 (the 12%/40% recipe triplicated, geometry drifted, one copy theme-reactive) — MINOR — F.W3/W4.** Three instances + two drifted near-copies; C·D-11's framing adopted over L's "verbatim" (K-11): `/equation` renders a theme-reactive selected-pill register (`.preset-pill.is-active` off `var(--viz-fourier)`) and a frozen one inside one panel column — the drift IS the finding. `.preset-pill` copy banked (fr-FunctionInput L-m5); dies inside the FR-NP-6 migration + FR-NP-1 cure.
- **FR-NP-17 · D·M-6 (leaf hardcodes its container's alignment; wrong in one host) — MINOR — F.W4.** Byte-identical labels over two stacked chip rows on two alignment axes (FunctionInput.vue:155-156 left vs :222-223 centred via the baked `justify-center` at :16 — re-measured by re-reader 1; D's cites −1). Layout belongs to the host.
- **FR-NP-18 · D·M-5 (zero state coverage: no disabled/loading/error association) — MINOR — F.W3/W4 rider.** One prop declared; the host owns loading/error and the pills reflect none of it; 7.0.0 `loading`/`aria-busy` is the unconsumed seat. Cf. banked fr-BasisSelector m-22(i) — same disposition, dies inside the migration.
- **FR-NP-19 · L·L-10 (the group cannot be named) — MINOR (mechanism corrected) — F.W3/W4, rides FR-NP-3.** Substance confirmed: both hosts fail to name the set; FunctionInput.vue:222's `<label>` has no `for` and wraps nothing. Mechanism corrected per re-reader 2 (K-10): no `inheritAttrs: false` + single root → host attrs already fall through; what defeats naming is the missing ROLE (name-from-author needs one). Cure is one attribute on :16 — `role="radiogroup"` per ruling 6, not a pass-through API.
- **FR-NP-20 · L·L-11/L-12/L-13/L-14 ∘ D·D-8 ∘ re-reader-2 M2 (the module band) — MINOR — F.W4.** notation.ts is three unrelated concerns; `TIER_INFO: Record<string,…>` widened past the exported `EquationTier` with `noUncheckedIndexedAccess` absent (flag identity banked: fr-BasisSelector i-3; the lying `?? TIER_INFO.spline` fallback banked at fr-InfoCard's ruling); energyColor's literals re-declare TIER_INFO's in-file (banked: fr-EquationPanel M-N1 — fold, not re-book); `NOTATION_OPTIONS` mutable (plain `T[]`); the leaf hand-rolls v-model while its host uses `defineModel` six times (cf. FR-EMT-15(c), which cites exactly this pair). **Adopted correction (re-reader 2 M2)**: the cohesion census counts DEAD `InfoCard.vue` as a live consumer — zero inbound (banked: FR-IC-1/F8-REACH-01 ∘ FR-COB zero-inbound sweep); live counts: TIER_INFO 1, energyColor 2 — which strengthens the split argument while invalidating L's denominator.
- **FR-NP-21 · D·m-4 ∘ C·D-9 (cryptic glyphs, no tooltip/description) — MINOR — F.W3 (rides intake R3-7a).** `grep Tooltip|title=|aria-describedby` → 0; every sibling pill family wraps (BasisSelector.vue:139; every preset chip); the same module holds the description register (TIER_INFO's sentences). R3-7a's F.W3 budget reads 35/9 → +1 site here; `SegmentedTabOption.tooltip?` is the unused 4.0.0 seat.
- **FR-NP-22 · D·m-1 ∘ L·L-9 (min-width 4.5rem undocumented; five divergences from the named pattern) — MINOR — F.W3/W4.** The archetype documents WHY 5.5rem (:249-257, my read: "keeps all three pills uniform-width"); this file's 4.5rem floor plausibly never binds (D's width arithmetic; computed widths → SS-13). Resting border-weight/color/text-color legs absent (which is why selection LOWERS contrast here — D·M-1's inversion, booked inside FR-NP-1's family) and the compaction block absent (FR-NP-24).
- **FR-NP-23 · re-reader-1 missed, adopted (the active pill alone loses its opaque plate) — MINOR (new) — F.W3/W4; SS-13 for the pixel.** Statically decided — unlike the hover tie, this is a specificity WIN: `.bg-background` (0,1,0, unlayered) loses to `.notation-active` (0,2,0) whose `background:` shorthand replaces the opaque plate with an 88%-transparent fill. Inside `/visualize`'s `.eq-panel glass-wash` over the per-frame epicycle canvas, the two REJECTED options render as solid chips and the CHOSEN one becomes a translucent window onto moving content — material hierarchy inverted; also the physical reason FR-NP-1's `/visualize` figure is composited-undecidable. Mechanism family adjacent to banked fr-BasisSelector M-1 (outline's opaque rest inside the glass panel); the within-row split is in no banked row and in none of the three axes.
- **FR-NP-24 · D·M-4 + re-reader-1 missed, merged (no coarse-pointer compaction; the one unscaled geometry is the gutter) — MINOR — F.W3/W4; SS-13 for the wrap point.** The archetype needed and authored a 639px compaction for the identical three-pill row (my read of :279-293); this file has none. Adopted sharpening: everything inside the pill rides `--ui-scale` (height/font/padding/intra-gap) but the wrapper's `gap-1.5` is fixed 6px — at coarse the row is ≥54px pills with 6px gutters, a 9:1 mass-to-gutter ratio that tightens the wrap arithmetic and reads as one welded bar.
- **FR-NP-25 · D·D-10 ∘ C·D-10 (`--pill-color` unnamespaced, unregistered, shared) — MINOR — F.W3/W4 rider.** Two writers in two unrelated components (my grep via re-readers, spot-confirmed), zero `@property` anywhere, every read bare. Route-disjoint today; the naming is the defect. Dies inside the FR-NP-6 migration.
- **FR-NP-26 · C·D-12 (clsx + cva are LIVE runtime externals of this exact chunk) — MINOR, census correction — NO-WAVE-OWNER (SS-3/SS-4 consumes).** `dist/button-BNDWhAZb.js` imports `cva` and (via `cn-DJXf4yaB.js`) `clsx` as externals; both are non-optional glass-ui peers (peerDependenciesMeta marks 7 optional — not these); `tailwind-merge` alone is genuinely dead (bespoke 66-rule resolver, no `twMerge`). lane-frontend §1 / DESIGN.md:32's "dead devDeps" row is 1-of-3 correct; acting on it is installer-dependent (no `.npmrc` anywhere). Both re-readers verified independently; adopted.

### INFO

- **FR-NP-27 · re-reader-1 missed #5 (glyph not `aria-hidden` → two-token accessible names) — FOLD, already banked at fr-EquationPanel M-N13 (MINOR → F.W4, "one attribute per site, two sites", BasisSelector:148 the second member).** The re-reader filed it as unbanked; the identity guard caught the re-book. Recorded here so the FR-NP-3 radio cure and the M-N13 attribute land in ONE edit.
- **FR-NP-28 · L·C-1 ∘ D·I-3 ∘ C·D-6.1 ("currently 6× Button") — FOLD, already banked at fr-FunctionInput C §0 row 7 (INFO, NO-WAVE-OWNER).** Correct row: 1 callsite × 3 options × 2 mutually-exclusive mount routes (max 3 live). Live instance of intake X-9 (scope-law); FR-NP-2's fabricated 18÷3 is the plausible provenance. All three axes filed it fresh — K-17.
- **FR-NP-29 · D·m-6 ∘ L·L-15 (implicit `type="submit"`) — INFO (latent), cf. banked fr-BasisSelector m-22(k).** No `<form>` in the tree; cured at the tag (`type: nativeButton ? (props.type ?? "button") : undefined`). Both axes stated the negative result — the right posture.
- **FR-NP-30 · C·D-13 head ∘ L·L-16 ∘ D·I-2 (residual band) — INFO.** (a) Zero behavioural witness anywhere (no spec clicks a pill; the axe harness's keystones: `test.fixme` at :110/:133/:192/:212, only :150 live — the banked fr-BasisSelector R-2 count; folds to FR-AUL-16's coverage-debt family, NO-WAVE-OWNER → SS-3/SS-4). (b) The `notation` enum enumerated 4× with the silent Python `.get(…, render_trig)` fallback — a drift renders trig with a 200; ties intake R6-8 (CARRY → F.W5); the client-side `string` laundering is banked (fr-EquationPanel D-L10+C-10); conformance dir count corrected to 16 specs (re-reader 2), none for equations. (c) The `sm` recipe's fixed `px-3` vs scaled height at coarse — producer-inherited proportion break, rides the banked M-7/i-4 family at F.W1.
- **FR-NP-31 · D·I-1 (unconditional re-emit on the already-active pill) — INFO — rides FR-NP-6.** Confirmed BY THE BANK: fr-EquationModeToggle K-5's kill-note states the framework guard does not cover "the sibling's hand-rolled pair, which is FR-EMT-15(c)'s point" — this file is that sibling. `ToggleGroup type="single"` gives selected-vs-reaffirmed for free.

## Killed-claims register

1. **K-1 · D·M-3's mechanism ("vue-tsc TS2322, the ToastVariant class of hard break") — REFUTED (both re-readers; ratified).** No `vueCompilerOptions`/`strictTemplates` anywhere; an undeclared attribute is a silent fallthrough; banked twice (fr-AdminFlaggedPanel FR-AFP-2, FR-COB-2) with in-tree proof (`title=` attrs building green). C·D-1's silence mechanism is the truth, and it prices the wave WORSE: green CI over 29 `variant="outline"` sites.
2. **K-2 · D·B-2's light-mode column ("Trig 4.57 bare pass ✓") — KILLED AS ARITHMETIC, verdict sustained and worsened (my own instrument: 3.841 FAIL).** The label sits on its own 12% self-tint, not the bare page — the fr-BasisSelector K-7 shape exactly (input selection, not method). D also booked no border row; C's 6-of-6 border failures stand.
3. **K-3 · C·D-5's mechanism ("Lightning guarded SliderControl because a static colour is declared first; nothing to fall back to here") — REFUTED byte-level (re-reader 2, ratified by my own extraction).** The built `.basis-toggle[aria-pressed=true]` pair — from a source declaring ONLY `color-mix` (my :269-273 read) — got a SYNTHESIZED `background:var(--pill-color)` fallback + `@supports` guard. The discriminator is pipeline membership via `@reference` (FR-NP-8). **Re-reader 1's ratification of C's mechanism ("exactly right") is corrected — see DISSENT.**
4. **K-4 · C·D-5's consequence ("visually indistinguishable… undetectable by any means") — REFUTED (re-reader 1, ratified).** Only the two `color-mix` declarations are invalid-at-parse; `color: var(--pill-color)` survives every engine in the claimed set — degraded to colour-only, not gone. FR-COB-26's banked note: every current baseline engine supports lab color-mix. The fact survives at FR-NP-8's mechanism row.
5. **K-5 · C·D-7's justify-content carve-out ("keep it; UNPROVEN-NEEDS-BUILD") — REFUTED at the adopted pin (both re-readers).** The stale-dist trap: C reasoned from the 3.1.0-era artifact; the installed 4.0.0 `surfaces.css:120` `@apply … justify-center` routes through the consumer's Tailwind. Both declarations inert (FR-NP-13).
6. **K-6 · D·M-2/L·L-8's cascade mechanism ("unlayered beats @layer utilities" / "its competition is entirely layered") — REFUTED as the decider (my own grep: dist components.css has 0 `@layer` and carries the competing rules verbatim).** Unlayered-vs-unlayered → specificity tie → emitted document order → build-contingent: the fr-App R-2 UNPROVEN cell, the FR-COB-8 banked class, the R-7 challenge-inherited precedent. The design gap survives at FR-NP-10; the "which rule wins" pixel is UNPROVEN-NEEDS-BUILD. **Re-reader 2's "statically decidable and decided" confirmation dies with it — see DISSENT.**
7. **K-7 · re-reader 2's D·M-8 escalation (MAJOR; "the layer arithmetic settles it statically") — DECLINED.** The identity is banked at FR-COB-12 (MINOR) with the mechanism ruled at R-7 to be the unlayered-dist one, and the harm bounded (no animated squish — instantaneous snap). Identity guard: fold at the banked grade (FR-NP-11).
8. **K-8 · L §8 repair 4 + C·D-4's framing ("route the three colours through VIZ_COLORS / --viz-*; basis-display.ts is the pattern") — the CURE KILLED, two independent banked legs (both re-readers agree).** (a) `basisDisplay` copies `VIZ_COLORS` at module eval — permanently the reactive initialiser, theme-invariant (banked: fr-BasisCanvas D-2 family / fr-BasisSelector B-3's painter); (b) `cssVarToHex` has no `oklch` arm → `#888888` (banked BLOCKER: fr-BasisCanvas D-1/BC-1/C-1). The prescribed cure would paint all three pills grey. Sound cure banked at fr-BasisSelector M-12 and present in the HOST at FunctionInput.vue:244-249 — nobody in the corpus noticed. (The cure-adjudicated-separately law — ColorSpaceSelector addendum item 2 — instanced again.)
9. **K-9 · L·L-2's intent claim ("counterfactual at the moment it was written") — STRUCK as unproven psychology (re-reader 1, ratified).** One commit covers both files; nothing distinguishes born-false from drifted. The docblock defect survives inside FR-NP-3 — sharpened to prescriptively wrong (ruling 6).
10. **K-10 · L·L-10's mechanism ("exposes no aria-label/id pass-through") — CORRECTED (re-reader 2, ratified).** Default fallthrough already delivers host attrs to the single root; the missing ROLE is what defeats naming. Repair size: one attribute, not an API (FR-NP-19).
11. **K-11 · L·L-5's "three VERBATIM instances" — CORRECTED (re-reader 2, ratified).** `.preset-pill.is-active` mixes from `var(--viz-fourier)` — live token, distinct arms — while the other two mix from frozen `--pill-color`. The colour SOURCE differs, and that difference is the L-4 defect itself; "verbatim" erases the one cell that matters (FR-NP-16 carries C·D-11's framing).
12. **K-12 · D's subject-file provenance — the receipts die, the claims survive.** Systematic −1 (template) / −6 (style block) / −1 (notation.ts) offsets plus `lane-frontend.md:138` for a :441 string (re-reader 2's full table; my whole-file read confirms the real anchors). D's citations into glass-ui/BasisSelector/style.css are exact — a subject-file transcription fault, the K-9/DISSENT-1 companion-drift shape at higher amplitude. D's file is unusable as a patch index without re-anchoring.
13. **K-13 · D·M-3's IMPROVE-row citation (`chipVariants.ts:3-7` for the tone/tint mechanism) — half-invented (re-reader 1, ratified).** At the tag those lines are class-string constants; the claim is true only at `Chip.vue:65-66`. The IMPROVE row survives on the surviving citation.
14. **K-14 · re-reader 2's "sibling correct at BasisSelector.vue:144 (`aria-pressed`)" — KILLED against the bank.** fr-BasisSelector M-4 (MAJOR, banked) books that very `aria-pressed` as the defect on a non-binary control, with the EasingPicker doctrine as the in-tree law. The cell would have laundered an adjudicated defect into a repair exemplar — the exact failure ruling 6 exists to prevent.
15. **K-15 · the corpus's BLOCKER grades on the ARIA row AND both re-readers' MAJOR on the rehydration limb — DEMOTED by identity guard.** The ARIA family is banked MAJOR (fr-FunctionInput D-4.2+C-6 ∘ fr-EquationView D·D-M12); the rehydration threat model is banked MINOR (fr-EquationView RD-4 per fr-ConvergencePlot RD-3). Folding takes the adjudicated grade — both directions.
16. **K-16 · D·B-1's "keystones 1 and 2 are additionally test.fixme" and L·L-1's "the axe gate passes" — both UNDERSTATE, in the tree's favour.** The banked count (fr-BasisSelector R-2, re-verified by re-reader 1's grep): fixme at :110/:133/:192/:212, only :150 live — four-of-five off. "Zero a11y coverage" survives at full strength for a stronger reason (FR-NP-30a).
17. **K-17 · L·C-1 / D·I-3 / C·D-6.1 filed as fresh census corrections — RE-BOOKS.** Banked at fr-FunctionInput C §0 row 7 (INFO, NO-WAVE-OWNER). All three axes and neither re-reader's severity tier survives; the fold is FR-NP-28.

## Missed finds — adopted after re-proof (6) + one re-book caught

1. **The radio-doctrine cure amendment** (re-reader 1) — adopted as ruling 6, MAJOR-weight, riding FR-NP-3; relayed as an amendment to the banked D·D-M12 / D-4.2+C-6 cure cells (per the fr-EquationView R2-K1/BC-1 banked-cell-amendment precedent).
2. **The `@reference` mechanism + 19-declaration census** (re-reader 2) — adopted as FR-NP-8 (MAJOR, new).
3. **The VIZ_COLORS-cure kill + the sound in-host cure** (both re-readers, convergent) — adopted as K-8 + FR-NP-1's cure clause.
4. **The active-pill material split** (re-reader 1) — adopted as FR-NP-23 after this seat confirmed the specificity win is static (unlike the hover tie).
5. **The unscaled `gap-1.5` gutter** (re-reader 1) — adopted, merged into FR-NP-24.
6. **The deadlock does not gate the a11y repair** (re-reader 1) — adopted, merged into FR-NP-6.
- *Caught as re-book*: the unhidden-glyph accessible-name find (re-reader 1) — already banked at fr-EquationPanel M-N13; folded at FR-NP-27, never re-booked.

## UNPROVEN residue (SS-13 visual-audit inputs, plus two non-browser probes)

1. **Composited contrast on `/visualize` only** — the pills sit inside `.eq-panel glass-wash` (backdrop-filtered, over the epicycle canvas); direction robust both themes, third digit not. The `/equation` host is opaque: light-mode Trig 3.84 FAILS unconditionally, no probe needed (C's per-host scoping adopted — saves SS-13 a probe).
2. **Which rule wins the source-order tie** on the active pill's hover/press (FR-NP-10) — **UNPROVEN-NEEDS-BUILD**: fresh `vite build` on the ^4.0.0 install + rule-order readback; the 2026-06-12 artifact was built against 3.1.0 and cannot decide it.
3. **Fresh-build reproduction of the bare `.notation-active` emission** (FR-NP-8) — **UNPROVEN-NEEDS-BUILD**; prediction YES (pipeline-membership mechanism, toolchain-independent). F.W1 runs a build anyway.
4. **The wrap point at 390×844 under `pointer: coarse`** + the three computed pill widths (does the 4.5rem floor ever bind?) — FR-NP-22/24's live halves; the structural halves are static and stand.
5. **The rendered face of `.cm-serif`** and U+2071 coverage in the resolved generic serif (`"eⁱ"` tofu/synthesised-superscript question) — the token gap is static and total (banked); only the codepoint's coverage is live.
6. **Active-pill legibility over the running canvas** (FR-NP-23) — mechanism source-certain; perception live-only.
7. **Compositor cost of the 12%/40% repaint inside a backdrop-filtered layer over a per-frame-invalidated canvas** — stacking static; cost needs a trace (toggle notation with the epicycle clock running, `/visualize`, panel open).
8. **vue-tsc diagnostic count for this file at glass-ui 7.0.0** — **UNPROVEN-NEEDS-INSTALL**; prediction ZERO (`size="sm"` survives, `variant` silent) — the sharpest form of FR-NP-5's silence claim; parent question = fr-AdminAuditLog open probe 9.
9. **An un-fixme'd axe run at either mount** — both mounts sit outside the sole live keystone; absent state ARIA is not an axe rule regardless — the gate's silence is the finding (L's argument, ratified).

## Superlatives (L-18 runs both ways; provenance per row)

1. **L·D-1's forensics** (the comma-counter reconstruction predicting 18/15/7 across all three array rows, with the object-literal control group correct, honestly scoped to the substrate) — the corpus's finest work; reproduced in full by both re-readers and byte-exact by this seat's own node run. fr-BasisSelector-superlative-#1 quality.
2. **C·D-3's contrast instrument** — the corpus's most accurate arithmetic BECAUSE it chose the right compositing model (label over its own 12% self-tint), precisely where D went wrong; the only axis to book the 1.4.11 border row. Three independent recomputes now agree to 2dp.
3. **C·D-5's chunk-mate control probe** — proving a generator's decision from the repo's own build output with an in-chunk control — a genuinely original probe under the no-browser law; its mechanism died (K-3) but the probe design is the keeper, and it is what made the K-3 counterexample findable.
4. **L §7's refuted-hypotheses register** — seven hypotheses killed with the arithmetic shown, including the 0.2px icon-overflow computation declared "not claimable" and the honest rider that the SIBLING's 1.5em icon would overflow. Recording null results at full rigour (cf. banked fr-BasisSelector superlative #5).
5. **The component itself: zero-teardown by construction** (47 lines; no ref/computed/watch/lifecycle/listener/timer — my whole read) — the LIBRARY axis's leak leg is vacuous, in a family whose head carries a banked watcher BLOCKER; **and it beats its own archetype on v-model correctness** (no local mirror; BasisSelector's `selected` re-sync watch is banked fr-BasisSelector m-1). The "degraded copy" is the better citizen on exactly one axis, found by the incredulity posture working.
6. **The token-consumption lesson in one file** (D·S-2, sharpened by the bank): `size="sm"` inherits the WCAG 2.5.5 coarse clamp with zero height declarations — while the sibling that hand-pins `h-7` mechanically deletes the same clamp (banked fr-AdminAuditLog AA-22). The one dimension this file did NOT hand-roll is the one carrying the a11y requirement; the hand-rolled `min-width` is the one that fails. Preserve through every cure.
7. **C·S-2's end-to-end `notation` leaf walk** (four independent enumerations agree exactly; each mode reaches a distinct renderer pair; then the same leaf booked as unfixtured at D-13) — the "correct today, unprotected tomorrow" split the F.W5 seam work needs, crossing the client↔operation seam into Python to prove liveness.

## DISSENT / corrections register (against this corpus's re-readers — recorded as evidence about the fleet)

1. **Re-reader 2, two cascade cells** (L·L-8 "statically decidable and decided"; D·M-8 "the layer arithmetic settles it statically… should be booked DECIDED"): both built on the source-tree layer model after the registry had corrected it to the unlayered-dist model (FR-COB-8; R-7 ruled the layered mechanism challenge-inherited). This seat's grep of the dist sheet (0 `@layer`; competing rules present verbatim) is the third independent observation. A citation-inheritance-shaped event — from the CHALLENGE this time, not the corpus.
2. **Re-reader 1, one mechanism ratification** (C·D-5's "static declaration first… exactly right"): refuted by the same build the reader read — the guarded basis-toggle pair from a no-static-declaration source. The reader verified the emission and inherited the explanation.
3. **Re-reader 2, one exemplar cell** ("sibling correct at BasisSelector.vue:144"): contradicts banked fr-BasisSelector M-4. The registry, not the tree, is the exemplar authority once a row is adjudicated.
4. **Both re-readers, one grade** (the rehydration limb at MAJOR): both cited the banked identity; neither took its banked grade (MINOR per the RD-4 ruling). Folding takes the adjudicated severity — the identity guard is not advisory.
5. **Preserved, unresolved**: re-reader disagreement on FR-NP-2's tier (MAJOR vs BLOCKER) — ruled BLOCKER-substrate here on L's own lying-label principle; the dissenting demotion argument ("honest callsite disposition bounds harm") is recorded and is why the row is substrate-scoped rather than fleet-wide.
6. **Method note**: minor count drift observed and not chased — re-reader 1 says 3 `@supports` blocks in the NotationPills chunk, my grep counts 1; the load-bearing fact (`.notation-active` bare) is unaffected. C's "19 specs" in conformance/ corrected to 16 by re-reader 2; substantive claim (none for equations) unaffected.

## Closing verdict

**ADJUDICATED — 31 rows (2 BLOCKER · 7 MAJOR · 17 MINOR · 5 INFO), 13 identities folded by reference, 17 kills, 6 missed finds adopted, 7 superlatives.** The contrast BLOCKER joins the banked fr-BasisSelector B-3 family with its sound cure named in-host and its unsound corpus cure killed on two banked legs; the substrate BLOCKER (fabricated cardinality under a source-derived label) routes to F.W0 with a fabrication rider on every `loops[].cardinality` consumer; the ARIA row folds to its two banked homes carrying the radio-semantics cure amendment that stops an adjudicated defect from propagating into a second file; the new `@reference` mechanism row hands F.W4 a one-line cure covering 19 unguarded declarations across 7 SFCs. Routings: **F.W0** (FR-NP-2, FR-NP-4) · **F.W1** (FR-NP-5 fold, FR-NP-13 rider, FR-NP-12 token parity) · **F.W3/W4** (FR-NP-1, FR-NP-3, FR-NP-6, FR-NP-7, FR-NP-8, FR-NP-9, and the MINOR mass) · **F.W5** tie (FR-NP-30b via R6-8) · **NO-WAVE-OWNER** (FR-NP-11 producer/BH-relay via FR-COB-28; FR-NP-26 + FR-NP-28 + FR-NP-30a census/coverage → SS-3/SS-4). Four re-reader cells corrected by this seat's own instruments; every load-bearing ruling in this file rests on evidence this seat ran itself. Nothing here stamps a wave.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
