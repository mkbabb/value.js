claude-fable-5 (served model id)

# fr-NotationPills — adjudicated apotheosis, SECOND PASS (M-23/L-18, fresh-Fable arbiter seat)

**Seat**: apotheosis arbiter, pass 2 · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt.
**Date**: 2026-08-25 · **Subject**: `fourier-analysis/web/src/components/equation/NotationPills.vue` (47 lines) + `web/src/lib/equation/notation.ts` (48), at fourier HEAD `cd26c6533adc32dfe1453d74117d3cb73b89ea16` (re-verified this seat; subject files CLEAN in the WT at my own `git status`). Installed producer `@mkbabb/glass-ui@4.0.0`, pin `^4.0.0` (my node reads); producer working tree at 8.0.0.
**Corpus**: three challenge axes (D·design / L·library / C·consumption, all `claude-opus-5[1m]`) → first-pass apotheosis (2026-08-24, this path's git history — carried forward below, never re-derived where unchallenged) → **two hostile re-readers** (design-and-usage · library-and-code) who attacked the corpus AND the bank. Both re-reader returns were themselves attacked; this pass rules their five disagreements, adopts all five filed missed finds (two with corrections), and adds four kills.

**This seat's own instruments (all static + node, read-only; zero browser probes — probe parsimony)**: NotationPills.vue WHOLE (47, anchors re-confirmed: :16 wrapper, :20 variant, :24 `--pill-color`, :37-41/:42-46 style rules); notation.ts:1-20 (docblock :3-8, literals :15-17); **the FR-NP-32 decider, run by this seat**: `postcss([@tailwindcss/postcss])` — the app's own plugin at its own version — over `@import "tailwindcss"; @import "@mkbabb/glass-ui/styles";` with `from: web/src/style.css` → **`CssSyntaxError: Unterminated string: 's own'`** thrown from `@tailwindcss/node` `loadStylesheet`; byte-level root cause read directly: installed `src/styles/index.css:195` is one continuous comment ("re-imported this `@source` line, Tailwind scanned…") while the shipped `dist/styles/index.css:195-196` has the AN.W1 fold-block INJECTED at the comment's prose mention of `@source` — the injected `*/` terminates the enclosing comment early, leaving :203-221 un-commented prose (apostrophes in "glass-ui's own" open the unterminated string) before a stray `*/` at :221; comment delimiters **17 `/*` / 8 `*/` in dist vs 15/6 in src** (my counts); `web/src/style.css:3` carries the live import; **FR-NP-10 decider sub-facts**: main.ts:6 `import "./style.css"` (entry module), router/index.ts :61/:73 (VisualizationView) and :94 (EquationView) both `() => import(…)` lazy, `web/dist/index.html` head carries exactly ONE app stylesheet `<link>` (`index-57FkGzlZ.css`) and NO link for `NotationPills-CjQ8uEBB.css` (a separately emitted asset, my `ls`); the 3.1.0-era ENTRY artifact contains **0** `hover\:bg-accent` (my grep — the artifact is admissible for emission SHAPE only, reader 1's split, now sharpened); **M2 decider**: EasingPicker.vue:10-16 read verbatim (`role="group"` + `aria-label="Easing"` wrapper at :16; children `role="menuitemradio"` + `aria-checked`; "satisfies `aria-required-children`; `aria-pressed` would mislabel a radio as a toggle"); **M4 fold targets re-read**: fr-CanvasOverlayButton.md:25 (R-2 `border-none` chain, "zero pixels, fleet-wide") + :56 (FR-COB-7 producer row), fr-VisualizationView.md:22 + :118 (D-10(a)/K2 carrying the `--tw-border-style` custom-property chain verbatim — utilities-layer border-STYLE resolves to the same element's `none`); `dist/styles/glass/surfaces.css:119-120` `.btn-pill { @apply … border-none }`, gap :133 / `border-radius: var(--radius-pill)` :134 / padding :135 (my grep — decides the K-12 increments); **byte-identity instrument (the pass's own closure)**: value.js's shipped `dist/subpaths/{css,color}.js` — the very dependency FR-NP-9 books as declared-and-bypassed — round-trips producer tokens vs consumer literals: `oklch(0.579 0.201 30.4)`→(215,53,35) ≡ `hsl(6,72%,49%)`; `oklch(0.484 0.163 265.5)`→(49,86,185) ≡ `hsl(224,58%,46%)`; `oklch(0.532 0.180 317.5)`→(149,65,175) ≡ `hsl(286,46%,47%)` — **identical in all nine channels** (tokens read at dist tokens/color-radius.css:263-265); **route-disjoint decider**: VisualizationView.vue :231 `<EquationPanel v-if="showEquation && store.epicycleData && !isEditing">` and :262-267 `viz-panel-left` (the v-else = NOT-editing branch) mounting `<BasisSelector v-if="hasData">` — co-render on one route; EquationPanel.vue (live at `components/visualization/`) imports NotationPills :9, mounts :96; useViewState.ts:21 `showEquation` **default false** (my read); `--pill-c` verified at gallery/GallerySearchBar.vue:121/:214-221 + GalleryCard.vue:121/:271-273 (anchors EXACT) + GalleryCardModal (4 occurrences), live path `components/visualization/gallery/`; counts: `api/tests/conformance/test_*.py` = **17** (my ls); useEquationCache.ts = **49** lines (my wc); lane-frontend.md (formation/fourier/) :138 = the inventory row with NO count, "currently 6× `Button`" at **:441** (my sed), challenge-D-design.md:460 = D·I-3 citing :138 (my sed); registry sweep for the FR-NP-32 class → **unbanked** (my grep over registry/ + audit/ + formation/; the three loose hits are value.js/keyframes contexts, none books a parse failure of `exports["./styles"]`); producer local `dist/styles/index.css` = 1,514-byte stub, producer HEAD 8.0.0 (my reads — reader 1's unproven #6 stands).

## Scoreboard

**32 adjudicated rows — 3 BLOCKER · 7 MAJOR · 17 MINOR · 5 INFO — 14 identities folded by reference. Killed-claims register: 21 (K-1..K-17 carried; K-18..K-21 new, including one kill of the bank's own cure cell and one closure of the bank's own residue). All 5 filed missed finds adopted (two corrected in adoption). Superlatives: 10.**

Blocker-weight survivors: **FR-NP-32** (NEW — the adopted 4.0.0 dist stylesheet is a CSS parse error; `vite dev`/`vite build` RED at the pin, reproduced by this seat with the app's own toolchain), **FR-NP-1** (contrast, label cells — border cells VOID per M4), **FR-NP-2** (fabricated R6 cardinality under a source-derived label).

**The pass's most consequential rulings**: (1) the bank's own FR-NP-19 cure ("one attribute — `role="radiogroup"`") is killed — it would ship an axe-critical `aria-required-children` violation and owes the APG keyboard contract; the naming cure is `role="group"` + `aria-label`, radio semantics ride FR-NP-6 (K-18). (2) FR-NP-10's "which rule wins" is DECIDED statically by module-graph emission order — the bank's residue #2 closes (K-20). (3) The three colour literals are a BYTE-EXACT transcription of the producer tokens' light arms — proven here with value.js itself — collapsing FR-NP-1's cure to a zero-design-decision substitution.

## Ruled disagreements (the pass's actual work)

| # | question | reader 1 (design-and-usage) | reader 2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **Is FR-NP-10's source-order tie statically decidable?** | YES — entry-vs-async-chunk emission order (M3) | NO — "UNPROVEN-NEEDS-BUILD" (= the bank's residue #2) | **Reader 1 wins; FR-NP-10 → DECIDED; residue #2 CLOSED.** | All four sub-facts re-read by this seat: glass-ui's unlayered components.css reaches the page via style.css:3, imported by the ENTRY module (main.ts:6) → the statically-linked entry stylesheet; `.notation-active` lands in a route-split chunk stylesheet (both hosts lazy at router :61/:94; `NotationPills-CjQ8uEBB.css` a separate asset with NO html `<link>`), which Vite APPENDS to `document.head` at chunk load — strictly later document order ⇒ the (0,2,0) tie goes to `.notation-active` ⇒ `hover:bg-accent`/`active:bg-accent/80` are OVERRIDDEN on the active pill. The decision is a property of the app's module graph, not the glass version — which is why the 3.1.0 artifact is admissible for the SHAPE (and my grep sharpens reader 1's split: the old entry sheet holds 0 `hover\:bg-accent`; the competitor itself only arrives at 4.0.0). Stated contingency: DECIDED-for-the-default-module-graph — hoisting either host into the entry would reopen it; the first post-FR-NP-32 build carries a one-line rule-order readback as confirmation, not as the decider. This is NOT the prior re-reader's dead "statically decided" cell (DISSENT-1, first pass): that one decided the tie with the wrong LAYER model; this one accepts the tie (K-6 intact) and decides the ORDER. |
| 2 | **Does the banked FR-NP-19 cure survive?** ("one attribute on :16 — `role="radiogroup"`") | NO (M2): `radiogroup` requires `radio` children — glass Buttons render `role=button` (dist binds no role) → axe-critical `aria-required-children`; plus the APG one-tabstop/arrow-key contract — "by hand" is an API with new state, not one attribute | Silently re-affirms the banked target list ("`role="radiogroup"` + `role="radio"`/`aria-checked` by hand" first) | **Reader 1 wins — K-18. FR-NP-19 cure re-sized; ruling 6's target order re-ranked.** | EasingPicker.vue:10-16, my verbatim read, is the tree's own doctrine AND the correct split embodied: the WRAPPER carries `role="group"` + `aria-label` (:16); the radio-family role goes on the OPTIONS with `aria-checked` (:24/:27). Naming cure (FR-NP-19's actual subject) = `role="group"` + `aria-label="Notation"` — one attribute pair, no required children, gives FunctionInput.vue:222's dangling `<label>` an anchor. Radio SEMANTICS belong to the FR-NP-6 seat (`ToggleGroup type="single"`/`SegmentedTabs` — roving focus from reka-ui, both at the pin). **One clause of M2 corrected in adoption**: the defective cure would NOT "newly RED the very gate at visualization-ux.spec.ts:27-43" — neither pill mount sits inside the sole live keystone (ExportModal), 4 of 5 keystones are `test.fixme`, and reader 2's own mount fact (showEquation default FALSE, my read of useViewState.ts:21) keeps `/visualize`'s pills unmounted by default. The violation would ship SILENTLY — which makes the mis-sized cure worse, not better. |
| 3 | **Does the pill's border paint at all?** | NO (M4): `.btn-pill`'s `@apply border-none` sets `--tw-border-style: none` + `border-style: none` on the same element; the `outline` variant's `border border-input` AND `.notation-active`'s :44 border-color are colour/width over a zero-style box — reader 1 COMPILED the app's sheet and read the emission | Confirmed FR-NP-22 still pricing "the variant's 1px" | **Reader 1 wins — identity carry, not a new row.** FR-NP-1's six 1.4.11 border cells and FR-NP-22's border legs are VOID at the pin. | The chain is ALREADY BANKED and was re-read by this seat: fr-CanvasOverlayButton.md:25/:56 (FR-COB-7(b), "zero pixels, fleet-wide") re-ruled at fr-VisualizationView.md:22/:118 — including the exact `--tw-border-style` custom-property resolution that defeats the utilities-layer `border-style: var(--tw-border-style)` (`@property` initial `solid` yields only when the cascade leaves it unset; `.btn-pill` sets it). `border-none` verified in the @apply at surfaces.css:119-120 (my read). Direction of harm SHARPENS FR-NP-3's 1.4.1 leg: the active pill has exactly TWO differentiators, background tint (:43) and label colour (:45) — colour twice over; the shape channel is structurally unavailable at this pin. BasisSelector.vue:249-257's "2px vs the variant's 1px" design comment is counterfactual for the same banked reason. FOLD → FR-COB-7(b); never re-booked. |
| 4 | **FR-NP-9's relation: "analogous tokens" or identity?** | (not held) | BYTE-EXACT transcription of the tokens' light arms (his missed find, node round-trip both directions) | **Reader 2 wins — proven here with a fourth, independent instrument: value.js's own dist.** FR-NP-9 AMENDED; FR-NP-1's cure clause COLLAPSED. | My run against `dist/subpaths/{css,color}.js`: all three `oklch()` tokens and all three `hsl()` literals land on identical rounded sRGB triplets — (215,53,35)/(49,86,185)/(149,65,175) — nine of nine channels. The pills are pinned to the light arm BY CONSTRUCTION (the whole of FR-NP-1's dark column, no residual colour question); the duplication is a cross-package copy-by-value with NO witness of any kind (the producer has already re-tuned this cohort once — the local `--viz-amber` darken at style.css:113-127); and the cure choice collapses: substitute `var(--viz-fourier)`/`var(--viz-chebyshev)`/`var(--viz-legendre)` straight into the `color-mix` at :43-45 — byte-identical in light mode, ZERO visual delta to review, dark arms free. The instrument is its own superlative: the defect (value.js declared, bypassed) proven with the bypassed dependency. |
| 5 | **FR-NP-25's "route-disjoint today" mitigation** | (not held) | KILLED: the two `--pill-color` writers CO-RENDER on `/visualize` (C had the routes in its own §1 and still concluded "different") | **Reader 2's kill sustained — K-19. FR-NP-25 AMENDED.** | My reads: VisualizationView.vue:231 mounts EquationPanel (→ NotationPills at its :96) and :262-267's NOT-editing branch mounts BasisSelector — both under `/visualize` (+ `/w/:imageSlug?`), simultaneously satisfiable. Honest bound preserved: each writer sets `--pill-color` inline on its own element and the rows are DOM siblings, not nested — no live mis-tint; what dies is the MITIGATION (risk class: "one layout refactor away", not "impossible"). Rider adopted after my own grep: the same 12%/30-40% idiom runs under a SECOND property name `--pill-c` in three gallery files (SearchBar/Card/CardModal, anchors verified; live path `components/visualization/gallery/` — reader 2's path cite drifted) — the recipe family is six-wide under two names. |

## Adjudicated defect roster

Dispositions are ADJUDICATED, not VERIFIED — X·F specs are forming; routings are M-25 agglomeration input. Line anchors re-resolved at HEAD `cd26c653` this pass. Rows unchanged from the first pass are carried CONDENSED (full first-pass text in this path's git history, 2026-08-24); ★ marks second-pass amendments.

### BLOCKER

- **FR-NP-32 ★NEW · reader-1 M1, reproduced by this seat — BLOCKER — NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE.** The ADOPTED `@mkbabb/glass-ui@4.0.0` dist stylesheet is a CSS syntax error: the dist emitter injected the AN.W1 fold-block (`@import "../glass-ui.css"` + `@import "./components.css"`) at the first literal `@source` occurrence in the file — a PROSE MENTION inside a comment, not the directive — so dist/styles/index.css :195-203 terminates the comment early and leaves :203-221 as un-commented prose whose apostrophes open an unterminated string (delimiters 17/8 dist vs 15/6 src, my counts; src is intact). **My own repro with the app's own toolchain**: `@import "@mkbabb/glass-ui/styles"` under `@tailwindcss/postcss` → `CssSyntaxError: Unterminated string: 's own'`; style.css:3 is the live import ⇒ **`vite dev` and `vite build` are RED at the `^4.0.0` pin**. Reader 1's necessary-and-sufficient patch test (repair ONLY the comment → 437,303-byte OK output) adopted with my byte-level corroboration. Why no seat hit it before: web/dist artifacts are dated 2026-06-12, built at 3.1.0 — every prior "what the installed 4.0.0 emits" argument reasoned about a build that cannot currently run. **Consequences booked**: the bank's residues on fresh-build reproduction (FR-NP-8) and vue-tsc-at-7.0.0 are BLOCKED, not merely unrun; the bank's "F.W1 runs a build anyway" clause is struck — **F.W1 opens on a red build**. Relay ask: the emitter fix AND a producer gate that parses its own published `exports["./styles"]` — NOT a version bump (no v8 dist exists to read: local dist is a 1,514-byte stub, dist untracked at every tag — my reads). Registry swept: unbanked (my grep).
- **FR-NP-1 · D·B-2 ∘ C·D-3 ∘ L·L-4 — BLOCKER — F.W3/W4.** ★AMENDED twice. The active pill's LABEL fails WCAG in both themes, painted by three frozen mode-invariant literals (notation.ts:15-17 → :24 → :43-45): label on its own 12% self-tint — light 3.84/5.37/4.67, dark 3.85/2.78/3.19 vs 4.5:1 (four of six FAIL, incl. Trig in LIGHT); instrument spread across four independent recomputes ≤0.02, all FAIL. **★(M4)** The six BORDER cells are VOID — the 40% border paints zero pixels at the pin (ruling 3, FOLD → banked FR-COB-7(b)); the 1.4.11 row is re-scoped from "six of six fail" to "the indicator's only non-colour channel does not exist", which is WORSE. **★(ruling 4)** Cure clause COLLAPSED by byte-identity: substitute `var(--viz-*)` straight into the `color-mix` at :43-45 (the FunctionInput.vue:244-249 idiom, fifteen lines away) — zero light-mode delta, dark arms free; NEVER through `VIZ_COLORS`/`cssVarToHex` (K-8). Identity: banked fr-BasisSelector B-3 family, cross-booked. `/equation` light Trig 3.84 FAILS unconditionally — now on a FOURTH leg: reader 1's recorded null proves `.eq-grid`/`.eq-panel-left*` declare no background (EquationView.vue:340-376), so the pills sit on `--background` = `--neutral-0`. `/visualize` composited figure stays SS-13.
- **FR-NP-2 · L·D-1 — BLOCKER (audit substrate, NOT the component) — F.W0.** Carried. R6's `loops` row ships `cardinality: 18` for the 3-element array under `FINITE_ARRAY_LITERAL_SOURCE_DERIVED`; the comma-counter reconstruction predicts 18/15/7 across all three array rows; the callsite row's honest disposition bounds the blast. Rider: every F.W4 consumer treats array-literal `loops[].cardinality` as fabricated.

### MAJOR

- **FR-NP-3 · D·B-1 ∘ L·L-1/L-2 ∘ C·D-2 — MAJOR, FOLDS — F.W3/W4.** Carried: no aria/role/name in 47 lines; colour sole carrier; producer's `aria-pressed:` CVA arms dead at this callsite; docblock (notation.ts:3-8) prescribes the pattern it doesn't implement — prescriptively wrong (K-9 sharpening). Folds: fr-EquationView D·D-M12 ∘ fr-FunctionInput D-4.2+C-6 ∘ fr-DarkModeToggle N-2. Cure amendment (ruling 6, first pass) stands with ruling 2's re-rank: `ToggleGroup type="single"`/`SegmentedTabs` FIRST (roving focus free), hand-rolled radiogroup LAST (it owes the full APG contract). ★Sharpened by M4: the shape channel is structurally unavailable at this pin — colour is the sole carrier twice over.
- **FR-NP-4 · L·D-2 — MAJOR (audit substrate) — F.W0.** Carried. 89/89 Button, 396/512 unresolved leaves; honest label bounds it at MAJOR.
- **FR-NP-5 · C·D-1 — MAJOR, FOLDS — F.W1.** Carried: `variant` deleted at v7.0.0 (git-show-proven at the tag by both readers; still absent at v8.0.0 — reader 1's diff), typecheck-SILENT (no strictTemplates), pill silently re-tiers. Folds: fr-BasisSelector M-7 ∘ fr-AdminAuditLog AA-2 ∘ FR-COB-2. ★Baseline-law note: producer HEAD is now 8.0.0 (bumped 2026-08-09); the challenges' "7.0.0" was correct at authoring (2026-08-06).
- **FR-NP-6 · C·D-6 — MAJOR, extends banked fr-BasisSelector M-13 — F.W3 + F.W1 sequencing rider.** Carried, ★enriched with reader 1's new receipt: `./tabs` present at ALL THREE points (installed 4.0.0's 80-key map · v7.0.0's 74 · v8.0.0's 69), and `git show v7.0.0:…/SegmentedTabs.vue` RETAINS `variant?: SegmentedTabsVariant` (:98, default `"pill"`, `"underline"` branch :149) across the very hop that deletes Button's — EquationView.vue:188's `variant="underline"` survives. The deadlock does NOT gate the a11y repair (first-pass adoption stands).
- **FR-NP-7 · L·L-6 — MAJOR — F.W4.** Carried: `/equation` durable via cache→defineModel chain; `/visualize` a bare local ref behind the :231 v-if — reopen re-inits `"trig"` and the `{debounce:300, immediate:true}` watcher re-POSTs a DIFFERENT equation. ★Enriched: `showEquation` defaults FALSE (useViewState.ts:21, my read) — the non-durable host is also the never-mounted-by-default host, deepening both this row and FR-NP-30a.
- **FR-NP-8 · the missing `@reference "tailwindcss"` — MAJOR — F.W4.** Carried: pipeline-membership mechanism (K-3's survivor), 19 unguarded `color-mix` declarations across 7 SFCs; one line per file. Residue: fresh-build reproduction — ★now BLOCKED behind FR-NP-32.
- **FR-NP-9 · C·D-4 ∘ L·L-4 (structural leg) — MAJOR, extends banked fr-BasisSelector M-12 — F.W3/W4 cure, F.W2 tie.** ★AMENDED (ruling 4): the relation is not "analogous" — the three literals are a BYTE-EXACT hand transcription of the producer tokens' LIGHT arms (nine of nine channels, proven with value.js's own dist — the very declared-and-bypassed dependency this row books). New rider: cross-package duplicate-by-value with no typecheck/lint/visual witness; the producer has re-tuned this cohort once already (`--viz-amber`), and any `--viz-*` re-tune silently diverges the notation triad from the basis triad it was copied from. This is the complete mechanical account of FR-NP-1's dark column. Cure = FR-NP-1's collapsed clause.

### MINOR

- **FR-NP-10 · D·M-2 ∘ L·L-8 — MINOR — F.W3/W4.** ★AMENDED (ruling 1): upgraded from UNPROVEN-NEEDS-BUILD to **DECIDED-against-the-active-pill** — the chunk stylesheet is appended after the entry `<link>`, later document order wins the (0,2,0) tie, so `hover:bg-accent`/`active:bg-accent/80` are overridden: the active pill gives NO hover/press surface change (the corpus's CONCLUSION vindicated; its layer MECHANISM stays killed, K-6). Module-graph-contingent (hoisting either host reopens it); post-FR-NP-32 build carries a confirmation readback. FR-NP-23's `.bg-background` (0,1,0) loss holds a fortiori. The design gap (no AUTHORED active-hover arm vs the archetype's deliberate pair) is unchanged. Composed with FR-NP-11: a PRM user may get only the signal they opted out of.
- **FR-NP-11 · D·M-8 (PRM squish) — MINOR, FOLDS to banked FR-COB-12 — NO-WAVE-OWNER (producer) + BH relay.** Carried; unlayered-dist mechanism (R-7), harm bounded, cured at 7.0.0.
- **FR-NP-12 · D·M-7 (`.cm-serif` phantom) — MINOR, FOLDS — F.W1 token parity + F.W4.** Carried: `--font-serif-math` consumed at one site, declared nowhere; folds fr-BasisSelector m-3 ∘ fr-AppHeader FR-AH-2. ★Reader 1's tightening adopted: with the property undeclared everywhere, `var(--font-serif-math, serif)` resolves to generic `serif` DETERMINISTICALLY — no live probe needed for the face, only for U+2071 coverage.
- **FR-NP-13 · L·L-7 ∘ D·m-2 — MINOR — F.W1 rider.** Carried: :38/:40 inert at the pin (`.btn-pill` supplies both — surfaces.css:120 @apply, :134 radius, my re-grep), load-bearing after 7.0.0 deletes `.btn-pill`; :38 hardcodes past `--radius-pill`.
- **FR-NP-14 · L·L-3 (no exhaustiveness bond) — MINOR — F.W4.** Carried.
- **FR-NP-15 · C·D-13 sub-finding (unvalidated rehydration) — MINOR (latent), FOLDS to fr-EquationView RD-4/C·D-04 — F.W4.** Carried at the banked grade (ruling 7). ★Count note: useEquationCache.ts is 49 lines (my wc; the bank was right, reader 2's "48" and C's "47" both off — DISSENT 4).
- **FR-NP-16 · L·L-5 ∘ C·D-11 (recipe triplicated, one copy theme-reactive) — MINOR — F.W3/W4.** Carried; the drift IS the finding (K-11).
- **FR-NP-17 · D·M-6 (leaf hardcodes container alignment) — MINOR — F.W4.** Carried: byte-identical labels over two chip rows on two alignment axes (FunctionInput :155-156 left vs :222-223 centred via :16).
- **FR-NP-18 · D·M-5 (zero state coverage) — MINOR — F.W3/W4 rider.** Carried.
- **FR-NP-19 · L·L-10 (the group cannot be named) — MINOR — F.W3/W4, rides FR-NP-3.** ★CURE RE-SIZED (ruling 2 / K-18): the banked "one attribute — `role="radiogroup"`" cure is killed — it ships axe-critical `aria-required-children` (glass Buttons render `role=button`; dist binds no role) and, delivered minimally, announces a role whose APG keyboard contract (one tabstop, arrow move-and-select) the 13-line script block does not implement — a strictly worse a11y outcome than the honest three-button set. Correct naming cure: **`role="group"` + `aria-label="Notation"`** on :16 — EasingPicker's own wrapper choice (:16, my read), no required children, and it anchors FunctionInput.vue:222's dangling label. Radio semantics ride FR-NP-6 (which the deadlock does not gate).
- **FR-NP-20 · the module band — MINOR — F.W4.** Carried (three concerns; TIER_INFO widened; energyColor re-declares; dead-consumer census corrected).
- **FR-NP-21 · D·m-4 ∘ C·D-9 (cryptic glyphs, no tooltip) — MINOR — F.W3 (rides R3-7a).** Carried.
- **FR-NP-22 · D·m-1 ∘ L·L-9 (min-width undocumented; five divergences) — MINOR — F.W3/W4.** Carried, ★with the border legs VOID per ruling 3 (they priced an unpainted object — the "resting border-weight/color legs absent → variant's 1px" clause is struck; what is absent is any painted boundary at all, banked producer reason). The 4.5rem floor's bind question stays SS-13 (reader 1's arithmetic: ~363px of pills+gutters vs ~354px of `/visualize` panel content at coarse — right at the threshold).
- **FR-NP-23 · the active pill loses its opaque plate — MINOR — F.W3/W4; SS-13 for the pixel.** Carried; ★now a fortiori under ruling 1 (the specificity WIN was already static; the tie-decided rows join it).
- **FR-NP-24 · no coarse-pointer compaction; fixed gutter — MINOR — F.W3/W4; SS-13 for the wrap point.** Carried.
- **FR-NP-25 · D·D-10 ∘ C·D-10 (`--pill-color` unnamespaced, unregistered, shared) — MINOR — F.W3/W4 rider.** ★AMENDED (ruling 5 / K-19): the "route-disjoint today" mitigation is DEAD — both writers co-render under `/visualize`'s NOT-editing arm (my reads at :231/:262-267); no live mis-tint (inline per-element writes, sibling rows), but the risk class moves to "one layout refactor away". ★Rider: the idiom runs under a SECOND name `--pill-c` in three gallery files (my grep) — the unregistered-property family is six files wide under two names.
- **FR-NP-26 · C·D-12 (clsx + cva live runtime externals) — MINOR, census correction — NO-WAVE-OWNER (SS-3/SS-4).** Carried; ★reader 2's peer enumeration adopted (7 optional in peerDependenciesMeta — cva/clsx/reka-ui not among them).

### INFO

- **FR-NP-27 · glyph not `aria-hidden` — FOLD, banked fr-EquationPanel M-N13.** Carried (lands in ONE edit with the FR-NP-19 naming cure).
- **FR-NP-28 · "currently 6× Button" — FOLD, banked fr-FunctionInput C §0 row 7.** Carried; ★K-12 increment (a) confirmed by my own sed: the string lives at lane-frontend.md:441; :138 is the count-free inventory row D·I-3 mis-cites (challenge-D:460, my read).
- **FR-NP-29 · implicit `type="submit"` — INFO (latent).** Carried; cured at the tag.
- **FR-NP-30 · residual band — INFO.** Carried: (a) zero behavioural/a11y witness — ★enriched: `showEquation` default-false means the `/visualize` mount is additionally never-mounted-by-default (reader 2, my re-read); (b) the `notation` enum 4× enumerated, silent Python `.get(…, render_trig)` fallback — ties R6-8 → F.W5; ★conformance dir count corrected AGAIN: **17** specs (my ls; C said 19, the bank's correction said 16, reader 2 said 17 — reader 2 right), none for equations; (c) fixed `px-3` vs scaled height — rides M-7/i-4 at F.W1.
- **FR-NP-31 · unconditional re-emit on the active pill — INFO — rides FR-NP-6.** Carried.

## Killed-claims register

K-1..K-17 carried verbatim from the first pass (this path's git history) — headline stubs: **K-1** D·M-3's TS2322 mechanism (silence is the truth) · **K-2** D·B-2's light-mode pass (3.84 FAIL; wrong substrate) · **K-3** C·D-5's "static colour first" mechanism (the synthesized `.basis-toggle` fallback refutes; `@reference` discriminates) · **K-4** C·D-5's "undetectable by any means" (label colour survives) · **K-5** C·D-7's carve-out (stale-dist trap) · **K-6** the `@layer utilities` cascade model (dist sheet has 0 `@layer`; tie, not layer win) · **K-7** re-reader-2(first-pass) D·M-8 escalation (fold at banked grade) · **K-8** the VIZ_COLORS cure (would paint all three pills `#888888`; two banked legs) · **K-9** L·L-2's intent psychology · **K-10** L·L-10's pass-through mechanism · **K-11** L·L-5's "verbatim" · **K-12** D's subject-file provenance (−1/−6 offsets) ★AMENDED: (a) D·I-3's `lane-frontend.md:138` cite is wrong — the string is at :441 (my sed); (b) K-12's own scope clause "D's citations into glass-ui/BasisSelector/style.css are exact" was too generous — D·m-2 puts the `border-radius` at surfaces.css:120 (the @apply line; live :134, my grep) and D·M-4/m-3 put the gap at :131 (live :133); L·L-7's split was the correct one · **K-13** D·M-3's chipVariants cite (half-invented) · **K-14** re-reader-2(first-pass)'s "sibling correct at :144" (banked M-4 is the defect) · **K-15** the BLOCKER/MAJOR over-grades (identity guard takes the banked grade) · **K-16** the fixme undercounts (4 of 5, tree's favour) · **K-17** the "6×" filed fresh (re-book).

**New this pass:**

18. **K-18 · the BANK's own FR-NP-19 cure cell ("Cure is one attribute on :16 — `role="radiogroup"`") — KILLED (reader 1 M2, ratified with my own reads).** Limb 1: `radiogroup` requires `radio` owned elements; the three children render `role=button` (dist binds only data-slot/variant/size/type/disabled) → `aria-required-children`, an axe serious/critical rule — the failure the tree's OWN doctrine names at EasingPicker.vue:13-14 ("satisfies `aria-required-children`"). Limb 2: the APG radiogroup keyboard contract (one tabstop, arrows move-and-select) — NotationPills.vue:1-13 has zero refs/handlers/tabindex management, so "one attribute" ships a role whose behaviour it does not implement. Ruling 6's target LIST survives; its ordering and FR-NP-19's sizing do not. Corrected cure in FR-NP-19. **One M2 clause corrected in adoption** (ruling 2): the violation would ship silently — the gate cannot see either mount — not "newly RED the gate".
19. **K-19 · C·D-10's mitigation AND the bank's inherited "Route-disjoint today" clause (FR-NP-25) — KILLED (reader 2, sustained on my own reads).** Both `--pill-color` writers co-render under `/visualize`'s NOT-editing arm (VisualizationView.vue:231 + :262-267; EquationPanel→NotationPills :96). C's own §1 listed the routes and still concluded disjoint. Harm honestly bounded: sibling inline writers, no live mis-tint; the mitigation dies, the naming defect stands.
20. **K-20 · reader 2's "the cascade is NOT statically decided — UNPROVEN-NEEDS-BUILD" (= the bank's residue #2) — SUPERSEDED (ruling 1).** The tie is real (K-6 intact); the ORDER is a module-graph property decided without a build: entry `<link>` before runtime-appended chunk stylesheet. The bank's residue #2 closes; a rule-order readback rides the first post-FR-NP-32 build as confirmation only. Recorded precisely so the distinction survives: the PRIOR re-reader died deciding the tie with the wrong layer model (DISSENT-1, pass 1); reader 1 decided the ORDER with the right one.
21. **K-21 · C's two corpus line anchors — CORRECTED (reader 1, spot-ratified).** (a) the `--viz-amber` carry is at web/src/style.css:113-127, not :119-131 (the bank already cited the correct span); (b) `.preset-pill.is-active` at FunctionInput.vue:244-248, not :247-252. Anchors only — both substantive claims survive. With K-12's amendments this is now a corpus-wide anchor-drift habit, not a D-only fault; every corpus cite consumed by a forming spec must be re-resolved (the D-19 lesson, third instance).

## Missed finds — all 5 filed adopted (2 corrected in adoption)

1. **The dist-stylesheet parse BLOCKER** (reader 1 M1) → FR-NP-32, reproduced by this seat's own toolchain run. The pass's decisive find: every prior "installed 4.0.0" argument reasoned about a build that cannot run.
2. **The radiogroup cure defect** (reader 1 M2) → K-18 + FR-NP-19 re-sized; gate-consequence clause corrected (it ships silent, which is worse).
3. **The static order decision** (reader 1 M3) → FR-NP-10 DECIDED + K-20 + bank residue #2 closed; module-graph contingency stated.
4. **The zero-pixel border** (reader 1 M4) → ruling 3: identity carry to banked FR-COB-7(b); FR-NP-1/22 border cells void; FR-NP-3 sharpened. Filed by the reader as a fold, honoured as one.
5. **The byte-exact palette transcription** (reader 2) → FR-NP-9 amended + FR-NP-1's cure collapsed; proven independently with value.js's own color math (nine of nine channels).

## UNPROVEN residue (SS-13 visual-audit inputs, plus non-browser probes)

1. **Composited contrast on `/visualize` only** — `.eq-panel glass-wash` over the per-frame canvas; direction robust, third digit not. `/equation` needs NO probe — now four legs deep (reader 1's recorded null: EquationView.vue:340-376 declares no background; pills sit on `--background`; light Trig 3.84 FAILS unconditionally). Reader 1's null is RECORDED so no later wave re-files the card-substrate hypothesis.
2. ~~Which rule wins the source-order tie~~ — **CLOSED this pass (ruling 1/K-20)**; one-line rule-order readback rides the first post-FR-NP-32 build as confirmation.
3. **Fresh-build reproduction of the bare `.notation-active` emission** (FR-NP-8) — NEEDS-BUILD, **BLOCKED behind FR-NP-32** (no build can run at the pin). Prediction unchanged: reproduces.
4. **The wrap point at 390×844 under `pointer: coarse`** + whether the 4.5rem floor ever binds — reader 1's threshold arithmetic (~363 vs ~354px) noted; three computed widths + one screenshot close both.
5. **U+2071 coverage in the resolved generic serif** (`"eⁱ"` tofu) — the face itself is now deterministic (generic `serif`, no probe needed); only the codepoint's coverage is live.
6. **Active-pill legibility over the running canvas** (FR-NP-23) — mechanism static; perception live.
7. **Compositor cost of the 12%/40% repaint** in the backdrop-filtered layer — needs a trace.
8. **vue-tsc diagnostic count at glass-ui 7.0.0** — NEEDS-INSTALL, **BLOCKED behind FR-NP-32**; prediction ZERO (the sharpest form of FR-NP-5).
9. **An un-fixme'd axe run at either mount** — the gate's silence is the finding; deepened by showEquation-default-false.
10. **★Whether producer v8.0.0's dist reproduces the FR-NP-32 emitter fault** — UNKNOWABLE HERE (local producer dist a 1,514-byte stub; dist untracked at every tag — my reads). The ADOPTED pin decides, and 4.0.0 decides RED. The relay asks for the emitter fix + a published-stylesheet parse gate, never a bump.

## Superlatives (L-18 runs both ways; provenance per row)

1-7 carried from the first pass (L·D-1's comma-counter forensics · C·D-3's compositing model · C·D-5's chunk-mate control probe · L §7's refuted-hypotheses register · the component's zero-teardown construction beating its archetype · the token-consumption lesson (`size="sm"` inherits the 2.5.5 clamp; the one hand-rolled number is the one that fails) · C·S-2's four-enumeration leaf walk into Python).

8. **★Reader 1's M1 repro discipline** — a four-step necessary-and-sufficient proof (baseline OK → import FAIL → real style.css FAIL → one-comment patch OK) run with the app's own plugin at the app's own version, plus the pin-discipline check and the honest account of WHY three axes, two readers and one arbiter all missed it (they reasoned about a 3.1.0-era artifact). Reproduced by this seat in one command. The finest single find in either pass.
9. **★Reader 2's byte-identity round-trip** — both directions, nine channels, with the cure-collapse consequence drawn correctly. This seat's ratification instrument is its own closure: the identity was proven with value.js's shipped color math — the dependency FR-NP-9 books as declared-and-bypassed, proving its own bypass.
10. **★EasingPicker.vue:10-16 as living doctrine** — one comment that names the role chosen, the constraint satisfied (`aria-required-children`), the wrong answer rejected (`aria-pressed`), and the parent-context reason — and whose WRAPPER line (:16, `role="group"` + `aria-label`) turned out to also be the correct cure the bank's own ruling under-sized. Doctrine that survived three audits, corrected its adjudicator, and then supplied the repair.

## DISSENT / corrections register (about the re-readers and the bank — evidence about the fleet)

1. **Reader 2, one carried-forward pricing error**: confirmed FR-NP-22 still valuing "the variant's 1px" border legs the banked FR-COB-7(b) chain (and reader 1's M4) prove unpainted — the identity-guard-failure shape the first pass's DISSENT-4 already recorded, now in the border direction.
2. **Reader 1, internal arithmetic drift**: the L §7 re-derivation subtracts "2×1px border" from the content box (→18px) while its own M4 proves used border-width 0 (→20px). The icon-fit null holds a fortiori; recorded as method, not chased.
3. **Reader 2 right, the bank wrong, on one count**: conformance specs = **17** (my ls) — C's 19 was wrong AND the bank's correction to "16" was off by one. A correction is a claim like any other.
4. **Reader 2 wrong, the bank right, on one count**: useEquationCache.ts = **49** lines (my wc), not reader 2's 48. Symmetric with (3); neither affects a verdict.
5. **Contrast instrument spread**: reader 1 3.83 / bank 3.841 / C 3.84 / reader 2 3.852 for light Trig — ≤0.02 rounding spread across four instruments, all FAIL; the banked 3.84 figure kept.
6. **Reader 2's path cites drift**: gallery files live at `components/visualization/gallery/` (my find), and EquationPanel at `components/visualization/` — line anchors exact, directory prose off. The K-21 habit, minor key.
7. **The bank's own two corrected cells, owned**: the FR-NP-19 cure sizing (K-18) and residue #2's needs-build status (K-20) were first-pass errors of THIS seat's predecessor, caught only because the re-readers were pointed at the bank as well as the corpus. The M-23 both-directions posture is the mechanism working — same lesson as ColorSpaceSelector's self-overruling grep, one level up.
8. **Preserved, unresolved**: FR-NP-2's tier (BLOCKER-substrate vs MAJOR) stands as first-pass ruled; no new evidence moved it.

## Closing verdict

**ADJUDICATED, SECOND PASS — 32 rows (3 BLOCKER · 7 MAJOR · 17 MINOR · 5 INFO), 14 identities folded by reference, 21 kills, all 5 filed missed finds adopted, 10 superlatives.** The new BLOCKER is upstream of everything: the adopted 4.0.0 dist stylesheet does not parse, so the pin's build is RED (reproduced by this seat), two banked residues are BLOCKED not unrun, and F.W1 opens on a red build — producer-owned, relayed at the top of the FR-COB-28 packet with a parse-gate ask. The bank's own cure cell for the naming row is killed and re-sized to EasingPicker's wrapper idiom; the hover-tie residue closes by module-graph order; the contrast BLOCKER's border cells void into the banked zero-pixel chain while its cure collapses — by byte-identity proven with value.js itself — to a zero-decision token substitution. Routings: **F.W0** (FR-NP-2, FR-NP-4) · **F.W1** (FR-NP-5, FR-NP-13, FR-NP-12 parity; SEQUENCING GATED on FR-NP-32) · **F.W3/W4** (FR-NP-1, FR-NP-3, FR-NP-6, FR-NP-7, FR-NP-8, FR-NP-9, the MINOR mass) · **F.W5** (FR-NP-30b via R6-8) · **NO-WAVE-OWNER** (FR-NP-32 + FR-NP-11 producer/BH relay via FR-COB-28; FR-NP-26/28/30a census-coverage → SS-3/SS-4). Every load-bearing second-pass ruling rests on evidence this seat ran itself. Nothing here stamps a wave.

— end of apotheosis, pass 2. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3; the first-pass text is preserved in this path's git history (2026-08-24).
