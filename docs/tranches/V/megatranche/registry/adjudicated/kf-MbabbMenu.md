Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component MbabbMenu; receipt read from this seat's own system prompt, not inherited).

# kf-MbabbMenu — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-12 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`, worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus**: the three challenge files at `audit/kf-components/MbabbMenu/` (`challenge-{D-design,L-library,C-consumption}.md`, all authored `claude-opus-5[1m]`, read whole by this seat) plus two fresh hostile re-reader returns — **reader-DU** (design-and-usage: 30 confirmed rows · 9 kills · 8 misses · 11 superlative cells · 10 unproven) and **reader-LC** (library-and-code: 28 confirmed rows · 6 kills · 7 misses · 10 superlative cells · 11 unproven). Both returns consumed whole.

**Substrate ruling (re-run by this seat; decisive for one BLOCKER).** `git log -1` → `8281638c`; `git rev-parse origin/master` → `81a56990`. `git diff origin/master --stat -- demo/app/dock/MbabbMenu.vue demo/state/ demo/components/instrument/shell/SharePopover.vue demo/components/instrument/transport/ demo/styles/` → drift confined to `TimingFunctionPanel.vue`/`useTimingFunctionEditor.ts` (outside this blast surface) — **every anchor below is worktree ≡ origin-current**, including the L-14 prose (present verbatim at `origin/master:App.vue:351-352`). The manifests are the exception: `package.json`/`package-lock.json` are ` M` dirty with **0** glass-ui hits, while **`origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact and the origin lock carries glass-ui ×3** (this seat's own `git show`). That is the banked manifest triangulation (kf-App ruling 1 · kf-CubeScene substrate · kf-SequencePlayhead's *a priori* law) reproducing again — and **both readers re-offended on it** (ruling 2).

**Method (incredulity).** Every load-bearing disagreement — between the two readers, between either reader and the banked registry, and between the readers and the challenge corpus — was re-derived against the bytes before adoption. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session: whole read of `MbabbMenu.vue` (121 L); git substrate probes above; `demo/styles/style.css` :14-40 (the z-contract verbatim) / :114-119 (the `--accent-red` DESTRUCTIVE-ONLY law naming Clear-all) / :58-59 (`--color-accent-red` bridge) / :168-192 (`--accent-kf` light-dark, `--ppmycota-primary`, the single-arm `--filter-brand-color`, the `.dark` block re-declaring only 3 tokens) / :228-240 (the inverted layer comment verbatim); `demo/styles/font-roles.json` :66-84 (`monoAllowedSelectors` + `_monoContract` naming the @mbabb handle + "reds the census"); `grep -rln font-roles scripts/ test/` → **0** (the census clause is prose-only); `demo/state/index.ts:100-118` (`resetAllStores` — two ref-resets + key removal only) + `demo/state/useSceneMachine.ts:45/:76` (the persisted `useStorage` ref never reset; reader-LC's citation path CORRECT at `demo/state/`, not `demo/app/scene/`); `demo/components/instrument/shell/index.ts` (4-export barrel) + `SharePopover.vue:45-62` (optional `onSceneRestore?`, setup-time capture into `useShareState`) + `EditorHeader.vue:23,45` / `EditorShell.vue:20,117` (both sibling consumers import the SFC directly and pass NO handler) + `EditorShell.vue:131` (`import "@styles/style.css"` bare side-effect); `useAnimationGroupActions.ts:63-74` + `AnimationControlsGroup.vue:106` (the unguarded `clear()` verbatim, still wired); `grep -rln "DropdownMenuItem|DropdownMenuCheckboxItem" demo/` → **exactly one file** (MbabbMenu.vue). Installed-producer instruments: **glass-ui.css brace-matched by this seat's own python** — single `@layer components{` 2531→18826, `.dropdown-menu__content` at 29178 and `.dropdown-menu__item` at 29522 both OUTSIDE (D-1's core re-derived third-hand); `components/_shared/menu.css` brace-matched — `@layer` closes at byte 675, the PRM block (685) and `.dropdown-menu-content` rule (781) both UNLAYERED; `styles/tokens/offsets.css` (`--overlay-min-width: 8rem`, `--panel-padding: 0.375rem`); `styles/theme/bridges.css` (`--color-destructive: var(--destructive)`); `styles/typography/semantic.css` + `utilities.css` + `scale.css` (all five utilities read verbatim: `text-admin-label` binds `var(--font-mono)` + uppercase + `--type-tracking-caps`; `text-mono-caption` same tracking/transform; `text-micro` neither; `--type-admin-label: 0.625rem`); `styles/utilities/a11y-overrides.css` read WHOLE (the `[data-allow-motion]` 0.01ms clamp vs the negated arm's 0.1s — the inversion verbatim); `components/dark-mode-toggle/dark-mode-toggle.css` read WHOLE (base 2.25rem both axes in `@layer components`; `[data-size="sm"]` 1.75rem; the dock arm resolving `var(--dock-control-size, var(--size-icon-btn))`; its own trailing PRM `transition:none`); `components/dock/styles/density.css` (**`--dock-control-size` declared ONLY inside `.glass-dock[data-size]`/`[data-preset]` scopes**) + `styles/tokens/sizing.css` (`--size-icon-btn: 2.5rem`); **`dropdown-menu-0gkd7rMF.js` :13-16 (`defaultOpen`/`open`/`modal` all bare `{ type: Boolean }`, NO defaults) + :25-30 (`modal: n.modal`, `open: n.open` passed explicitly to reka) + :47/:49 (`sideOffset` default 4, `align` default "start") + :98/:163/:170/:194 (content + item class stamps; `DropdownMenuCheckboxItem` defined)**; reka `DropdownMenu/DropdownMenuRoot.js` (`modal: { default: true }`; `useVModel(props,"open",emit,{ passive: props.open === void 0 })`); reka `Menu/MenuContent.js:118-124` (the modal/non-modal branch) + `MenuRootContentNonModal.js:111-113` (`trap-focus:false, disable-outside-pointer-events:false, disable-outside-scroll:false`); reka `Menu/MenuContentImpl.js:190-207` (the Tab `preventDefault` as a SEPARATE statement from the typeahead call); reka `shared/useTypeahead.js` (`textValue ?? textContent.trim()`, `startsWith` prefix match); glass-ui `package.json` exports (73 keys; `./dropdown-menu` TRUE, `./dialog` TRUE, `./avatar` FALSE); **producer `/Users/mkbabb/Programming/glass-ui/package.json` → version `8.0.0`** (the provenance decider). Registry instruments: kf-App.md (KF-APP-1 whole row + KF-APP-4 + KF-APP-17 + ruling 1's three-substrate map), kf-CubeScene.md (substrate ruling; C-1 ≡ KF-APP-1 fold + the C-14 bucket-split rider; M-8), kf-AnimationControlsGroup.md (D-11/C-8 — the wired-dead `clear()` row), kf-ChromeDock.md (M-4 — the `itemsPopupOpen` round-trip row + the C/S-2 inject kill), kf-SequencePlayhead.md (the dirty-manifest law), kf-MatrixEditor.md (the booking-kill idiom), CENSUS-2026-08-03 (the KF.W0–W10 wave sketch), lane-frontend.md (F-1, §3.2, §5 S-1..S-8, §6.3).

## Scoreboard

**44 roster rows — 0 net-new BLOCKER · 13 MAJOR-band (incl. the KF-APP-1 fold carrying that bank's BLOCKER grade, and 2 further folds) · 20 MINOR · 11 INFO.** **Killed-claims register: 15 entries** — including both corpus BLOCKERs as filed: the `.value` throw is a **fourth derivation of banked KF-APP-1**, and the phantom-dep BLOCKER is **substrate-dead** against origin's exact 7.0.0 pin, with both readers re-offending against the banked dirty-manifest law. **14 net-new rows admitted from the readers' 15 filed misses** (one merged, one folded as a rider), each re-derived at the bytes. **13 superlative cells sustained** (two with corrected mechanisms). **12 UNPROVEN residues → SS-13** (one of the readers' probes MOOTED by the non-modal ruling).

**Headline.** The corpus's sharpest genuinely-new yield is reader-DU's **Boolean-cast pair**: glass-ui declares `modal`/`open` as bare `{ type: Boolean }`, so Vue's boolean casting turns every absent prop into an explicit `false` — the menu is **non-modal by accident** (reka's documented `modal: true` default is unreachable for every glass-ui consumer), and the primitive is **permanently controlled** (`passive: props.open === void 0` can never be true), which makes MbabbMenu's `v-model:open` load-bearing while the file's own comments call it dock-plumbing — a rider that MUST travel with the banked kf-ChromeDock M-4 cure, whose worded deletion list would otherwise ship an unopenable menu. The same fact kills reader-LC's modal-composition proof cells and moots one of its probes. Meanwhile the recurring registry failure fires again: both readers filed as headline BLOCKERs two rows the registry had already banked or killed — **the failure is never derivation; it is an unread registry and an unnamed baseline.**

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave sketch KF.W0–W10.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **L-1/B-1 — both readers file the `.value` throw as the headline BLOCKER, first-booked** | **BOOKING KILL — FOLD ≡ KF-APP-1** (banked at kf-App as a BLOCKER of record, frontier-verified there by node execution + `origin/master:99-100` probe; ≡ kf-CubeScene C-1 fold). Mechanism CONFIRMED — these are the third and fourth independent derivations | kf-App.md:41 carries the row whole, incl. the zero-working-writers composition (`setPPMode` dead inside the unmounted `headerLeft`, KF-APP-17) and the cure law ("repairing :100 alone lands TWO writers"). This corpus **adds** two riders the bank lacks: (a) the extraction provenance — `git show 440e5c30^:App.vue:345-346` held `storedControls.value.ppMode` where `storedControls` WAS a `computed` (still live at App.vue:229); the S.D1 extraction kept the `.value` hop; (b) reader-LC's refinement that the throw fires AFTER `getStoredAnimationGroupControlOptions` has seeded and persisted a bucket (controlOptionsStore.ts:76-84) — each failing click still writes localStorage |
| 2 | **L-2/i-15/F-1 — both readers file the phantom dep as BLOCKER from the worktree manifests** | **SUBSTRATE-DEAD — both readers re-offended against the banked dirty-manifest law** (kf-SequencePlayhead *a priori*; kf-App ruling 1; kf-CubeScene; kf-MatrixEditor K17). Residue → **KF.W0 §B-12** | This seat's git probe: worktree `package.json`/`package-lock.json` both ` M` dirty, 0 glass-ui hits; `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` **exact**, origin lock ×3. The "npm ci resolves nothing" scenario is a property of an uncommitted local deletion, not of any committed tree. Neither reader named a baseline. Reader-LC's correction survives as a datum on the KF.W0 row: the transitive count through `@components/instrument/shell` is **8 glass-ui specifiers across 5 modules**, not the axis's "2 more" |
| 3 | **The modal question — reader-DU's K2 ("the menu is NON-MODAL: Boolean-cast defeats reka's default") vs reader-LC's L-10 proof cell ("MenuRootContentModal … `document.body.style.pointerEvents='none'` while the layer is up") and reader-LC's unproven #10 (useHideOthers sweep)** | **Reader-DU WINS — the menu is NON-MODAL. Reader-LC's L-10 composition cell KILLED; its unproven #10 MOOTED; its "NEW" nested-layer superlative re-scoped** | Re-derived whole by this seat: `dropdown-menu-0gkd7rMF.js:16` `modal: { type: Boolean }` (no default) → Vue boolean casting resolves the absent prop to `false` → :25/:30 pass `modal: n.modal` explicitly into reka's root, defeating `DropdownMenuRoot.js`'s `default: true`; `MenuContent.js:118-124` branches on `rootContext.modal.value` → `MenuRootContentNonModal`, which passes `"trap-focus": false, "disable-outside-pointer-events": false, "disable-outside-scroll": false` (:111-113). No FocusScope trap, no body pointer-events write, no `useHideOthers` (modal-branch-only). Reader-DU's missed #1 ADMITTED (MM-27, GLASS-OWNED): non-modal is *defensible* for a settings menu but undeclared, unintended, and upstream it makes reka's documented default unreachable for every consumer |
| 4 | **Reader-DU missed #2 — `v-model:open` is load-bearing (permanently-controlled primitive), contradicting the file's own comments** | **ADMITTED (MM-28, MINOR) and RAISED to a MUST-CARRY rider on banked kf-ChromeDock M-4** | `dropdown-menu-0gkd7rMF.js:14` `open: { type: Boolean }` → Boolean-cast means `props.open` is `false` when unbound, never `undefined` → reka's `passive: props.open === void 0` is false for EVERY glass-ui consumer → the root only emits `update:open` and never toggles itself. MbabbMenu.vue:96's `defineModel` supplies the round-trip; :2-3 and :95 describe it as existing "only to hold ChromeDock expanded" — false. **Interaction with the bank**: kf-ChromeDock M-4's cure list names "`v-model:open` in MbabbMenu.vue:96" among the round-trip cost items to delete (MbabbMenu self-holds via `useOptionalDockContext()`); executed as worded, the menu becomes permanently unopenable. The rider: the local open binding (or a glass-ui `default: undefined` fix) must survive that motion. Upstream half → BH relay |
| 5 | **Keyboard severity — B-2 (C axis) and the merged core filed BLOCKER by reader-DU; reader-LC files MAJOR** | **MAJOR — reader-LC's calibration adopted** (BLOCKER on this program = shipping crash or install failure). Reader-DU's K1 (Tab escape-hatch kill) RATIFIED; reader-DU's K8 (4-of-5 count) RATIFIED; reader-LC's precision note adopted | My MenuContentImpl.js:204-206 read: `if (isKeyDownInside) { if (event.key === "Tab") event.preventDefault(); if (…isCharacterKey…) handleTypeaheadSearch(…) }` — Tab prevention is a separate statement; there is NO second interaction model (the D axis's :177 paragraph dies; both readers concordant). The pointer path works for all five rows' actuators, and reka's FocusScope accepts a pointer-focused descendant (reader-LC's nuance: "keyboard-unREACHABLE", not "never focusable"). Row 4 has no interactive descendant — the ARIA-violation census is 4 of 5 |
| 6 | **D-2 severity — the D axis's BLOCKER ("a stateful control that renders no state is not a control")** | **MAJOR — two independent demotion grounds, both adopted** | Reader-DU's ground: the BLOCKER rung double-counts a crash — :100 throws on every activation (MM-1), so the "fire it again and watch the scene" recovery narrative is impossible; the a11y defect is strictly downstream of the repair. Reader-LC's ground: D-2's own declared falsifier — `CubeTarget.vue:53` `<template v-if="!ppMode">` makes the cube face a visible full-scene indicator on 2 of 7 scenes incl. home. Both demote independently; MAJOR is over-determined |
| 7 | **The size cure — the L axis (:208) and reader-LC's L-5 cell both prescribe `size="dock"` ("literally the token minted for this call site"); reader-DU's K4 refutes** | **Reader-DU's K4 SUSTAINED by this seat's own reads — the defect stands (MM-7), the `dock` cure is a second regression; the sound variant is `size="sm"`** | dark-mode-toggle.css read whole: the dock arm resolves `--dark-mode-toggle-size: var(--dock-control-size, var(--size-icon-btn))`; density.css declares `--dock-control-size` ONLY inside `.glass-dock[data-size]`/`[data-preset]` scopes; the menu content is body-portalled OUT of `.glass-dock` (reka Portal, both readers concordant) → fallback `--size-icon-btn: 2.5rem` = **40px**, double the authored 20px and too tall for the 44px row. `[data-size="sm"]` = 1.75rem = 28px — clears the 24×24 floor and matches the row's other 28px glyphs |
| 8 | **m-12 — reader-DU confirms MINOR; reader-LC kills the scenario** | **Reader-LC's kill SUSTAINED; m-12 re-scoped to INFO (MM-40)** | My useTypeahead.js read: `textValue: item.value?.textValue ?? item.ref.textContent?.trim() ?? ""` matched by `value.toLowerCase().startsWith(search)` — every row's label is its first text node, so the derived strings are label-PREFIXED and `s`/`d`/`c` match exactly as intended. Surviving residues: the @mbabb row keys on `@` (unreachable by its visible name), and multi-character search breaks at the condensed label/description seam |
| 9 | **TREE-PROVENANCE — reader-LC kills the D axis's "producer 7.0.0 byte-identical" sentence** | **SUSTAINED** — the provenance sentence dies; no finding collapses | This seat's read: `/Users/mkbabb/Programming/glass-ui/package.json` → `"version": "8.0.0"`. Every D-axis producer-`src/` line citation is unreproducible as written TODAY; every load-bearing fact in this record is grounded in the INSTALLED dist (7.0.0), which is what origin/master pins |
| 10 | **S-A's PRM mechanism — the D axis's "`data-allow-motion` is the vendor's deliberate opt-out … the icon IS the state"** | **Mechanism KILLED (both readers concordant; reader-LC's fuller form adopted); the OUTCOME survives stronger than claimed; the upstream inversion ADMITTED (MM-43, GLASS-OWNED)** | a11y-overrides.css read whole by this seat: under PRM, `*:not([data-allow-motion])` gets `transition-duration: 0.1s !important` (property-allow-listed) while `[data-allow-motion]` gets `0.01ms !important` everything — the attribute named "allow motion" receives the HARSHER clamp. dark-mode-toggle.css's own trailing PRM block zeroes its transitions again. Net: zero residual motion — S-A's conclusion holds by two belts and an inversion. Also confirmed: menu.css's PRM block sits OUTSIDE its `@layer` close (byte 685 > 675) — unlayered, wins outright (reader-DU's detail) |
| 11 | **Reader-LC missed #2 — five mono leaves violate the demo's own ruled T.D4 mono contract** | **ADMITTED (MM-9, MAJOR) — the manifest is explicit and the gate is prose-only** | font-roles.json:66-84 re-read: `monoAllowedSelectors` closed list; `_monoContract` sanctions `data-register="code"` chips (naming the @mbabb handle) and specific vendor voices — `text-admin-label` is in neither clause; MbabbMenu applies it at :12/:38/:50/:65/:66 (its two `data-register` marks are on OTHER leaves). The demo's own `.status-badge` idiom (design-idioms.css) exists precisely to un-mono this utility, and MbabbMenu doesn't use it. `grep -rln font-roles scripts/ test/` → 0 — "reds the census" enforces nothing. Distinct claim from D-7 (rung misuse) — one cure family, two laws violated |
| 12 | **L-8 — both readers file the transport `clear()` duplicate at MAJOR** | **GRADE HARMONIZED to the banked MINOR — FOLD ≡ kf-AnimationControlsGroup D-11/C-8** | The bank already holds the row whole ("a destructive `resetAllStores()` + `window.location.reload()` stays wired to a branch nothing can take … NO-WAVE-OWNER: delete the arm + narrow the emit type"). This seat re-verified the bytes (:63-74 unguarded, :106 wired, sole emitter `false`). The "loaded gun" framing does not lift an unreachable branch above the bank's rung |
| 13 | **Reader-LC missed #1 — rows 1/2 are pointer-dead across ~90% of their advertised hit area** | **ADMITTED (MM-3, MAJOR) — the mouse-user twin of the keyboard row, filed by no axis** | Composition of facts each independently verified: no `@click` on items :8/:18; actuators are the 24×24 Share2 button and the 20×36 toggle (MM-7); the vendor stamps `interactive-item glass-menu-row` on the full ~272×44 row (dropdown chunk :163; `min-block-size: max(2rem, 2.75rem)` in the layered half of menu.css) with hover tint + lift + row-level focus. The comment at :16-17 documents the no-row-actuation decision without noticing the vendor row class contradicts it. Exact ratio → SS-13 |

**Adjudicator's corrections register (against this corpus's readers):** (1) both readers' L-1 and F-1 BLOCKER headline framings — rulings 1–2; (2) reader-LC's L-10 modal-composition cell + unproven #10 + the modal premise of its "NEW" superlative — ruling 3; (3) reader-LC's L-5 `size="dock"` cell — ruling 7; (4) reader-DU's m-12 MINOR confirmation — ruling 8; (5) both readers' L-8 MAJOR grades — ruling 12; (6) reader-DU's D-6 MAJOR carried a dead modality clause (ruling 3) — the DS-bypass half alone earns the rung (MM-13).

## Adjudicated defect roster — id · severity · claim · terminal disposition

Routing law: **KF.W0–W10** per CENSUS-2026-08-03's wave sketch where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (this component's family = the dock-menu actuation/typography/brand packet). Anchors bind to worktree ≡ `origin/master`; re-anchor before any cure (D-19).

### Folds + MAJOR

| id | merged ids | claim (compressed; the challenge files carry the full cells) | terminal disposition |
|---|---|---|---|
| **MM-1** | L-1 / B-1 (both readers) | `stored.value.ppMode` — `.value` deref on the plain bucket → `TypeError` on every ppmycota click; sole outlier among ~10 call sites; enabling condition = no SFC typecheck anywhere | **FOLD ≡ KF-APP-1** (banked BLOCKER of record; ≡ kf-CubeScene C-1). Riders added by this corpus: the `440e5c30^` extraction provenance (the `.value` was correct against the pre-extraction `computed`); the seeded-bucket-per-failing-click write. Cure law from the bank travels whole: never land two writers (`setPPMode` disposal with the KF-APP-17 sweep) + the kf-CubeScene C-14 bucket-split resolution in the same motion. NO-WAVE-OWNER |
| **MM-2** | B-2 / L-3 / L-4 / D-3-core | Rows 1 (Share), 2 (Dark mode), 5 (@mbabb) are keyboard-dead: actuation lives in focusable descendants of `role=menuitem`; reka swallows Tab (MenuContentImpl:205) and synthesizes Enter/Space clicks on the ITEM only; 4 of 5 rows carry ARIA-illegal interactive descendants (row 4 is clean — K-9); rows 3/4 work because `@click` sits on the item. **MAJOR** (BLOCKER demoted, ruling 5) | **NO-WAVE-OWNER** — the family actuation spec: hoist `@click` to the item for rows 1/2 (child made presentational), row 3's anchor is the one `as-child` candidate, row 5 wants `DropdownMenuLabel` (MM-26; the `as-child` cure is structurally unavailable there — K-10). Exposure half → SS-13 |
| **MM-3** | **NET-NEW** — reader-LC miss #1 (ruling 13) | Rows 1/2 are POINTER-dead across ~90% of the advertised hit area: full-row hover tint + lift + focus from the vendor row class, actuation only on a 24×24 / 20×36 nested button | **NO-WAVE-OWNER** — dies in MM-2's hoist; measured ratio → SS-13 |
| **MM-4** | D-1 (+ D-18 as evidence) | glass-ui's dropdown CSS is UNLAYERED (this seat's own brace-match: `.dropdown-menu__item` at byte 29522, outside the single layer block 2531→18826) and outranks every `@layer utilities` class on the same element — `cursor-pointer` (:29,:46) and `text-destructive` (:46) never paint; the vendor's own layered `.glass-menu-row:hover` color loses to its own unlayered `color:inherit`; no demo override exists. **MAJOR** (the D axis's BLOCKER demoted per calibration — the confirmed-guarded command still functions; the loss is affordance). The `min-w` table row is mechanism-confirmed / effect-unproven (K-14) | **GLASS-OWNED core → BH relay** (wrap `dropdown-menu/styles.css` + the `_shared/menu.css` tail in `layer(components)` — `styles/index.css` already does it for `components.css`, so the omission is not a build limitation) **+ demo-side repair → KF.W6** (interim unlayered override or post-relay adoption; MM-15's z correction and MM-30's token correction land in the same motion). Computed-style witness + min-w binding → SS-13 |
| **MM-5** | D-2 / M-3 | A persisted boolean rendered as a plain `DropdownMenuItem`: no `menuitemcheckbox`, no `aria-checked`, no visible mark, while `DropdownMenuCheckboxItem` ships on the same barrel (chunk :170; indicator seat styled); `@select.prevent` keeps the menu open so dismissal isn't even the feedback. **MAJOR** (BLOCKER demoted twice over — ruling 6) | **NO-WAVE-OWNER** — ~3-line cure in the same motion as MM-1's repair; severity hinge (scene-as-indicator magnitude) → SS-13, doubly deferred on the KF-APP-1 cure + the C-14 bucket resolution |
| **MM-6** | L-7 / M-4 | `ppMode` written to `props.superKey`'s bucket; the only reader hard-keys `"cube"` — inert on 6 of 7 scenes, worst on home where CubeScene IS the backdrop; reader census closed (8 lines); the getter seeds+persists a bucket for any key | **FOLD ≡ kf-CubeScene C-14 bucket-split rider** (banked: "a repaired MbabbMenu writes whichever bucket is CURRENT, which on home is not the bucket CubeScene reads"). One motion with MM-1's cure law. NO-WAVE-OWNER |
| **MM-7** | L-5 / m-6 (DarkModeToggle half) | `class="aspect-square w-5"` bypasses the `size` API and cannot work: the primitive sets BOTH axes in `@layer components` → `w-5` (utilities) wins width only → a 20×36 button, ~8px letterboxed glyph, sub-24px hit width | **NO-WAVE-OWNER** — cure is **`size="sm"`** (1.75rem; K-5/ruling 7: the corpus's `size="dock"` prescription resolves to 40px outside `.glass-dock` and is a second regression). Rendered box → SS-13 |
| **MM-8** | D-7 | `text-admin-label` (10px fixed, uppercase, mono, 0.1em tracking, `line-height:1`) applied to four sentence-case prose strings (:12,:50,:65,:66; :38's URL is the one defensible use); `text-micro` sits unused one rung up; the demo's own `.status-badge` convention takes the utility for SIZE only | **NO-WAVE-OWNER** — one typography cure family with MM-9/MM-29/MM-31 |
| **MM-9** | **NET-NEW** — reader-LC miss #2 (ruling 11) | The same five leaves violate the ruled T.D4 mono contract: `text-admin-label` binds `var(--font-mono)` and none of the five matches any `monoAllowedSelectors` entry; the manifest's "reds the census" clause has no gate (grep scripts/+test/ → 0); the file honours the same contract perfectly at its two `data-register="code"` leaves | **NO-WAVE-OWNER** — same cure family as MM-8; the gate-absence datum rides the KF.W4 gates row |
| **MM-10** | D-4 | Row 3's title+subtitle collapses to one line: `<span>`+`<a>` both inline in a block wrapper; Vue's default `condense` deletes the separating newline text node (no `compilerOptions.whitespace` set); rows 1/4/5 stack via a block `<p>`. One-word cure (`block` on :38) | **NO-WAVE-OWNER**; rendered height → SS-13 |
| **MM-11** | D-5 | The leading-glyph column is authored-ragged 24/20/28/20/28px (`icon-lg`=24 via design-idioms.css; `w-5`=20; `w-7`=28; Trash 20; Avatar 28) — the label column's left edge oscillates ~8px with no fixed slot; not a cascade casualty | **NO-WAVE-OWNER** — one slot width (28px reads best against the 44px row); geometry → SS-13 |
| **MM-12** | D-8 | `--filter-brand-color` is a static single-arm filter chain while `--ppmycota-primary` = `--accent-kf` is `light-dark()`; `.dark` re-declares only `--accent-red`/`--accent-red-foreground`/`--primary` (this seat's read) — mark and label diverge in dark mode by construction | **NO-WAVE-OWNER** — cure = mask/inline SVG on `currentColor`, or a `.dark` filter arm; perceptual magnitude → SS-13 (both themes) |
| **MM-13** | D-6 / L-10 / M-5(b) | `window.confirm()` guards the sole destructive command in a DS that ships `Dialog*` (exports `./dialog`) with four in-tree precedents; unthemeable, and Playwright auto-dismisses it so the confirmed branch is harness-untestable. The modality-hazard sub-claim is DEAD (ruling 3: non-modal — no trapped FocusScope, no body pointer-events lock) | **NO-WAVE-OWNER** — Dialog cure per the KeyframesAddDialog precedent; the untestability rider joins the KF.W4 gate row; focus-return-on-cancel (narrowed) → SS-13 |

### MINOR

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| **MM-14** | L-8 | The T.C2 relocation left a byte-equivalent UNGUARDED duplicate of the destructive path in the transport (`useAnimationGroupActions.ts:63-74`), still wired at `AnimationControlsGroup.vue:106`, reachable by no emitter | **FOLD ≡ kf-AnimationControlsGroup D-11/C-8** (banked MINOR — ruling 12: delete the arm + narrow the emit type); MbabbMenu.vue:103-105's "relocation complete" comment corrected in that motion |
| **MM-15** | D-9 | `z-modal` (:6) is inert today (unlayered `z-index: var(--z-popover)` wins) and violates the demo's written z-contract ("--z-modal: 140 modal dialogs"); the durable MM-4 fix would ARM it and occlude the Share popover (both portalled siblings at 130, DOM-order-decided today) | rides **MM-4's motion** — the `z-modal`→`z-popover` correction lands in the same change as any layer fix |
| **MM-16** | D-10 / L-12 / m-7 | Remote `AvatarImage` with no `AvatarFallback` (ships, styled), no `referrerPolicy`/`crossOrigin` (declared props) — the menu's only third-party request; failure renders a deliberate-looking empty plate; full referrer leaked to GitHub per open | **NO-WAVE-OWNER** |
| **MM-17** | D-11 | "Github" (product is GitHub); decorative 🎉 announced inside the link's accessible name (no `aria-hidden` wrapper); contentless CTA — inside a 10px uppercase mono run | **NO-WAVE-OWNER** — 2 edits |
| **MM-18** | D-12 | `scale-on-hover` sits on the 28px logo child while `@click` covers the ~272×44 row — the affordance points at the wrong object; the destructive row carries none | **NO-WAVE-OWNER** |
| **MM-19** | D-13 / m-10 / L-6-as-corrected | `title="Toggle dark mode"` survives attrs-merge onto the button beside the primitive's dynamic `aria-label` + the visible sibling label — three competing name sources, a UA tooltip inside an open menu. The L axis's WCAG 2.5.3 framing is DEAD (K-6): accname "Switch to dark mode" contains the visible "Dark mode" | **NO-WAVE-OWNER** — drop the `title` |
| **MM-20** | D-14 / L-9 / M-5(a) | The SSR guard is inverted: no-`window` skips the confirm and runs the destructive path into an unguarded `window.location.reload()`; latent (client-only SPA, hash router, no ssr build arm) — K-7 holds the rung at MINOR | **NO-WAVE-OWNER** — collapses to one early return in MM-13's cure |
| **MM-21** | D-15 / L-11 / m-9 | The five-line inline-style rationale (:32-36) is mechanically false — `--ppmycota-primary` is a `:root` global (its own comment says so) that inherits into the portal, and :6 reads another `:root` token via a utility on the same portalled node. Reader-LC verified the prescribed cure is sound (Tailwind v4's `text-[var(…)]` default arm emits `color:`). Real cost: highest-priority inline style; the word can never be re-tinted | **NO-WAVE-OWNER** — comment + binding corrected together |
| **MM-22** | D-16 / L-13 | The row layout string hand-repeated 5× over classes the primitive already stamps (`display:flex; align-items:center` unlayered; `.interactive-item` radius layered); :57 alone silently omits `rounded-lg` — zero rendered consequence, which is what makes the drift corrosive | **NO-WAVE-OWNER** — hygiene, rides any touching wave |
| **MM-23** | L-14 | The mount-site prose (App.vue) describes a "combined open state" and a "hover→press window" this component does not have — **frontier-durable** (this seat: verbatim at `origin/master:App.vue:351-352`) | rides the **banked kf-ChromeDock M-4 motion** (the round-trip deletion rewrites that prose block) — with MM-28's MUST-CARRY rider |
| **MM-24** | m-8 | One import block, two subpath disciplines: barrel for four `DropdownMenu*` symbols while `./dropdown-menu` exists; `Avatar` correctly barrel-only because **`./avatar` is absent from the 73-key exports map** (this seat's node read) | **NO-WAVE-OWNER** (consistency); the export-map gap → **BH relay** |
| **MM-25** | m-11 + reader-LC strengthener + reader-LC miss #6 (rider) | Callback-prop with optional→required polarity flip on a value this component only relays; `App.vue:23`'s `:on-scene-restore` reads as event binding; sibling ChromeDock uses `defineEmits`. Strengthened: MbabbMenu is the ONLY one of three SharePopover consumers passing the handler (EditorHeader:23 / EditorShell:20 omit it → shared-URL restore silently skips the scene switch there); and SharePopover captures the prop ONCE at setup into `useShareState` (reactivity-loss idiom, stable today) | **NO-WAVE-OWNER** — one contract decision across the three consumers |
| **MM-26** | m-13 | The @mbabb row is a menuitem with no handler whose only actionable content is two inline anchors — full-row interactive chrome advertising an action the row lacks; `DropdownMenuLabel` ships unused | **NO-WAVE-OWNER** — one cure with MM-2's row-5 arm |
| **MM-27** | **NET-NEW** — reader-DU miss #1 (ruling 3) | The menu is **non-modal by accident**: `modal: { type: Boolean }` + Vue boolean casting → explicit `false` defeats reka's documented `default: true` for every glass-ui consumer; no aria-hidden isolation, no trap, no scroll lock — defensible for a settings menu, undeclared and upstream-unintended | **GLASS-OWNED → BH relay** (declare `default: undefined` or document the flip); demo-side: none needed once declared |
| **MM-28** | **NET-NEW** — reader-DU miss #2 (ruling 4) | The primitive is **permanently controlled** (`open: { type: Boolean }` → `passive` never true) — `v-model:open` at :96 is the only reason the menu opens, while :2-3/:95 call it dock-plumbing; a future trim ships an unopenable menu | **GLASS-OWNED → BH relay** (the same Boolean-cast fix) + **MUST-CARRY rider on kf-ChromeDock M-4**: the round-trip deletion must preserve a local open binding |
| **MM-29** | **NET-NEW** — reader-DU miss #4 | 0.1em CAPS tracking survives on lowercase text twice: `text-mono-caption normal-case` (:5, :64) cancels only `text-transform`; `letter-spacing: var(--type-tracking-caps)` rides onto the lowercase @mbabb handle — the mono FAMILY is manifest-sanctioned there; the caps tracking is not | **NO-WAVE-OWNER** — `tracking-normal` beside `normal-case`, or a dedicated lowercase mono rung; cure family MM-8 |
| **MM-30** | **NET-NEW** — reader-DU miss #5 | The destructive row reaches for the WRONG red: `text-destructive` = the vendor's `--destructive`, while the demo's own law (style.css:114-119, naming Clear-all) assigns `--accent-red`, which has a bespoke `.dark` arm (:183-184) and a Tailwind bridge (`--color-accent-red`, :58) — repairing MM-4's layer alone would paint the vendor's red, not the demo's | **NO-WAVE-OWNER** — `text-accent-red` lands with MM-4's demo-side repair |
| **MM-31** | **NET-NEW** — reader-DU miss #6 | Row 5's title is simultaneously the smallest (`text-mono-caption` ≈ caption rung) and the only bold string in the file — the person the menu is named after reads below "Share" and "Clear all & reload"; a structural typographic inversion no axis measured | **NO-WAVE-OWNER** — cure family MM-8 |
| **MM-32** | **NET-NEW** — reader-DU miss #7 | The title-over-subtitle module holds for only 3 of 5 rows (row 2 has no subtitle — named by no axis; row 3 collapses; row 5 runs 3 lines) while all five receive identical full-width actionable hover chrome — one visual grammar over four row contracts; the design statement unifying MM-10/MM-11/MM-26 | **NO-WAVE-OWNER** — the family spec's design floor; row-height rhythm → SS-13 |
| **MM-33** | **NET-NEW** — reader-LC miss #3 | MbabbMenu is the only SharePopover consumer importing through the `@components/instrument/shell` BARREL (siblings import the SFC), dragging EditorShell's whole graph — incl. the non-prunable `import "@styles/style.css"` side-effect (EditorShell.vue:131, this seat's read) and 8 glass-ui specifiers — into dock-chrome's module graph | **NO-WAVE-OWNER** — import the SFC like the siblings; the datum also corrects L-2's transitive count (ruling 2) |

### INFO

| id | claim | disposition |
|---|---|---|
| **MM-34** | D-17 — `p-1.5` is a symptomless MM-4 casualty: defeated by the unlayered `.dropdown-menu-content{padding:var(--panel-padding)}` (both 0.375rem — this seat verified both numbers and the rule's position outside the layer close) | rides MM-4 |
| **MM-35** | D-18 — the inverted mental model is written down: style.css:232-236 "fold into `@layer utilities` so they sit **below** component CSS"; Tailwind v4's order is theme→base→components→utilities, all below unlayered | evidence row; the comment is corrected in MM-4's motion |
| **MM-36** | L-15 — zero tests + no SFC typecheck + (MM-13) a harness that cannot cross `window.confirm`: the file has no gate of any kind | **FOLD ≡ KF-APP-4 → KF.W4** (the banked enabling-condition row; MM-9's gateless census clause joins it) |
| **MM-37** | i-14 — the value.js R1 `parseCssColor` class is in the bundle graph via `/dock` but on no reachable path: dock.js imports only the numeric guards (`n`,`t`), never the `i` wrapper; both readers re-traced byte-for-byte, concordant | INFO — exposure-not-reachability, ≡ the census R1 line; dead-weight note rides the bundle row |
| **MM-38** | i-16 — no shadow component; the one bespoke visual is the brand mark (no DS analogue); census S-1..S-8 does not reach this file | INFO — census-clean |
| **MM-39** | m-6 (Avatar half) as K3-corrected — `w-7 h-7` is a NECESSARY escape hatch (every DS avatar size ≥40px exceeds the row's 32px content band; no `sm` rule ships), not a `feedback_root_styling` violation; only the missing call-site rationale survives | **NO-WAVE-OWNER** — one comment |
| **MM-40** | m-12 as re-scoped (ruling 8) — typeahead residues only: the @mbabb row keys on `@`; multi-char search breaks at the condensed seam | INFO — `textValue` bindings ride the family cure |
| **MM-41** | Both readers' missed (merged) — `text-body` (:6) is a further MM-4 casualty the D table omits: font-family resolves through the unlayered `inherit`, font-size is overridden per-row by unlayered `--dropdown-text` (a rung BELOW body), weight restated by every leaf; only `text-wrap: pretty` survives | rides MM-4's repair; reader-LC's four-limb decomposition adopted |
| **MM-42** | reader-LC miss #5 — `resetAllStores()` asymmetry: two stores ref-reset AND key-removed; the scene machine only key-removed, its live `useStorage` ref (useSceneMachine.ts:76) un-reset — correct today only because both callers reload immediately; captured-bucket components hold detached proxies post-reset | INFO — filed against `demo/state`, reload-masked; a caveat comment rides MM-13's cure motion |
| **MM-43** | reader-LC miss #7 — UPSTREAM: the `data-allow-motion` PRM opt-out is semantically INVERTED in glass-ui 7.0.0 (the named attribute gets the harsher 0.01ms clamp; this seat read the file whole) — every consumer reaching for the escape hatch gets the opposite of the name's promise | **GLASS-OWNED → BH relay** (with ruling 10's mechanism correction to S-A) |
| **MM-44** | reader-DU miss #8 — census datum: MbabbMenu is the demo's ONLY `DropdownMenu*` consumer (this seat's grep: one file) — scopes MM-4's demo-side blast radius to one component, kills every "precedent exists in-tree" argument for this primitive (K-10), and makes a demo-local `.dropdown-menu__*` override regression-free | INFO — recorded for the MM-4/KF.W6 repair |

## Killed-claims register — 15 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | Both readers' L-2 / i-15 / F-1 at BLOCKER ("npm ci resolves nothing") | **SUBSTRATE-DEAD (ruling 2)** — the worktree manifests are uncommitted deletions; `origin/master` pins glass-ui `7.0.0` exact, lock ×3 (this seat's git probe). ≡ census F-1/SCH-1, killed at kf-App/kf-CubeScene/kf-MatrixEditor K17, *a priori* law at kf-SequencePlayhead. Both readers re-offended — the unnamed-baseline failure's nth firing. Residue → KF.W0 §B-12 |
| **K-2** | Both readers' L-1/B-1 headline-BLOCKER first-booking framing | **BOOKING KILL (ruling 1)** ≡ banked KF-APP-1 (+ kf-CubeScene C-1 fold). Mechanism confirmed; third and fourth derivations recorded; the corpus's two new riders adopted onto the banked row |
| **K-3** | challenge-D-design.md:177 — Tab "is not swallowed by the typeahead", yielding an operable-by-accident second interaction model | **KILLED** (both readers concordant; ratified by this seat's MenuContentImpl:204-206 read — the Tab `preventDefault` is a separate statement inside the same `isKeyDownInside` block). MM-2's headline survives on the L/C axes' correct citations |
| **K-4** | The modality cluster: D-6's "synchronous confirm inside a reka FocusScope trapped layer with `modal:true` … `document.body` pointer-events-disabled"; D-3's "FocusScope trapped" wrap; reader-LC's L-10 proof cell repeating the MenuRootContentModal/DismissableLayer chain; reader-LC's unproven #10 (useHideOthers sweep over the portalled popover) | **KILLED AT THE ROOT (ruling 3)** — the menu is NON-MODAL by Boolean-cast; the non-modal branch passes `trap-focus:false`/`disable-outside-pointer-events:false`; `useHideOthers` never runs. Reader-DU's K2 ratified and extended against reader-LC. MM-13 (DS bypass) and MM-2 (keyboard) survive on other grounds |
| **K-5** | The `size="dock"` cure (challenge-L:208; challenge-C:233-235; **reader-LC's L-5 cell** "the dock variant is the one minted for this call site") | **KILLED (ruling 7)** — `--dock-control-size` is scoped to `.glass-dock` density blocks; the portalled row falls to `--size-icon-btn` = 40px. Reader-DU's K4 ratified by this seat's own css reads. The defect (MM-7) is untouched; the sound variant is `size="sm"` |
| **K-6** | L-6's WCAG 2.5.3 "Label in Name mismatch" framing | **KILLED** (both readers concordant) — `title` is not a visible label; the accname "Switch to dark mode" contains the visible "Dark mode". The D axis's own D-13 verdict is the surviving form (MM-19) |
| **K-7** | C·M-5's MAJOR via the fail-open guard | **SEVERITY KILL** (both readers concordant) — latent by the axis's own concession (client SPA, hash router, no ssr arm); the live half is the DS bypass, already MM-13. MM-20 holds MINOR |
| **K-8** | The BLOCKER grades on C·B-2 (keyboard) and D-2 (stateless toggle) | **CALIBRATION KILLS (rulings 5–6)** — BLOCKER on this program = shipping crash or install failure; the pointer path works (B-2), and D-2 is doubly demoted (downstream-of-crash + the cube-face indicator, D-2's own falsifier) |
| **K-9** | challenge-D:179 — "Five of five rows violate the authoring rule" | **COUNT KILL** (reader-DU's K8 ratified) — row 4 has no focusable descendant and its handler is correctly on the item; the census is 4 of 5, the number a repair spec sizes against |
| **K-10** | challenge-L:163 — the `as-child` cure "already used elsewhere in the demo's glass surface" for the link rows | **KILLED** (reader-DU's K9 ratified by this seat's grep — MbabbMenu is the demo's sole `DropdownMenu*` consumer, zero `as-child` uses on the primitive tree-wide) and row 5 cannot satisfy `as-child`'s single-root-child constraint — it wants `DropdownMenuLabel` (MM-26). Row 3's anchor is the one candidate |
| **K-11** | challenge-C:383-384 — typing `s` "matches on whichever of those sorts first, not on the row's label" | **SCENARIO KILL (ruling 8)** — reka's typeahead is prefix-match over label-prefixed derived strings; `s` matches Share exactly as intended. Replaced by MM-40's two real residues |
| **K-12** | challenge-D §8 — "cross-read against the producer tree … 7.0.0, byte-identical version to the install" | **KILLED (ruling 9)** — the producer is 8.0.0 today (this seat's read). Every producer-`src/` citation in the D axis is unreproducible as written; no finding collapses (installed-dist grounded) |
| **K-13** | challenge-D:366 — `data-allow-motion` as "the vendor's deliberate opt-out … the icon IS the state" | **MECHANISM KILL (ruling 10)** — the attribute receives the HARSHER clamp (0.01ms); the outcome (zero residual motion under PRM) survives stronger than claimed. The upstream inversion is MM-43 → BH relay |
| **K-14** | D-1's `min-w` table row's stated delta ("panel floor is 8rem, not 17rem") | **HALF-KILL** (both readers concordant) — cascade mechanism confirmed (this seat re-verified `--overlay-min-width: 8rem`); the floor probably never binds against the menu's intrinsic max-content width. Mechanism-CONFIRMED / effect-UNPROVEN → SS-13 |
| **K-15** | Both readers' MAJOR on L-8 (the transport `clear()` duplicate) | **GRADE KILL (ruling 12)** — ≡ banked kf-AnimationControlsGroup D-11/C-8 at MINOR ("a branch nothing can take"); this seat re-verified the sole emitter passes `false`. FOLD as MM-14 |

## Identity-guard folds (never re-booked)

- **MM-1 ≡ KF-APP-1** (kf-App, BLOCKER of record; ≡ kf-CubeScene C-1) — fourth derivation logged; two new riders adopted onto the banked row; the cure law travels whole (two-writers + KF-APP-17 sweep + C-14 bucket resolution).
- **MM-6 ≡ kf-CubeScene C-14 bucket-split rider** — one identity; this corpus contributes the closed 8-line reader census and the seeded-bucket write amplification.
- **MM-14 ≡ kf-AnimationControlsGroup D-11/C-8** — grade harmonized MINOR.
- **MM-36 ≡ KF-APP-4 → KF.W4** — the gate row; MM-9's prose-only census clause and MM-13's Playwright-auto-dismiss untestability join it as riders.
- **K-1's residue ≡ census F-1/SCH-1 → KF.W0 §B-12** — the uncommitted manifest deletions would revive F-1 if committed.
- **MM-23 + MM-28 ride banked kf-ChromeDock M-4** — the round-trip deletion motion; MM-28 is a MUST-CARRY correctness rider on that cure's wording.
- **The C axis's S★-3 ChromeDock note** (":74-78 documents the opposite") ≡ kf-ChromeDock M-4, already banked — not re-filed here; the H1 harness result is concordant with that seat's own kill of the opposite claim.
- **MM-37 ≡ the census R1-exposure line** · **MM-38 ≡ census S-1..S-8 (clean)**.
- **Net-new first bookings** (checked against kf-App/kf-CubeScene/kf-ChromeDock/kf-AnimationControlsGroup/kf-MatrixEditor/kf-SequencePlayhead rosters + census S/F rows): MM-3, MM-9, MM-27, MM-28, MM-29, MM-30, MM-31, MM-32, MM-33, MM-41, MM-42, MM-43, MM-44 — no prior row states any of these mechanisms.

## Superlatives — 13 (provenance: corpus → readers → this seat)

1. **`@click.stop` at :38** *(L axis S-1; both readers tried to break it and could not)* — the enclosing row toggles ppMode and the anchor is `target="_blank"`; one modifier closes the most commonly shipped row-with-link bug. Reader-LC's depth adopted: pointerdown still bubbles, so reka's pointer-up bookkeeping stays correct while only the click is stopped. The honest asymmetry stands: row 5's anchors carry no `.stop` and need none (no row handler).
2. **`data-register="code"` at :5 and :64** *(C axis S★-1)* — the governing manifest names the @mbabb handle as a reviewed exception, and the file applies the hook to exactly the two sanctioned leaves. Tempered hard by MM-9 (five UNSANCTIONED mono leaves in the same 60 lines) and MM-29 (the caps tracking riding along) — the sharpest L-18-both-ways instance in the component.
3. **The destructive reset is single-sourced** *(L axis S-3)* — zero storage knowledge in this file; `@state` owns the key list. Tempered by MM-14 (the duplicate lives in the transport) and MM-42 (the sweep's asymmetry lives in `@state`) — this file did the right thing both times.
4. **`--dock-panel-width` discipline** *(L axis S-4)* — one `17rem` in the whole demo, at the token's declaration; both consumers route through it. The MM-4 irony recorded: the MbabbMenu consumption is inert, so the token has one *effective* consumer — the discipline is exact and the outcome is zero.
5. **The trigger is consumed whole and 2.5.3-correct** *(D S-D / L S-5)* — no fork, accname contains the visible label as a prefix, and `triggers.css` is layered, making :5 the one place in the component where the authored utilities win outright. The counter-exemplar to lane-frontend S-1.
6. **`Avatar decorative`** *(L S-2 / C S★-2)* — the correct arm of a three-way `never`-guarded union; the compiled primitive honours it; zero double-naming (the name lives on :64).
7. **`@select.prevent` on all five items** *(C S★-4)* — verified against the primitive's cancelable ITEM_SELECT event; uniformly applied, the conservative choice for a settings menu.
8. **`align="end"` / `:side-offset="8"`** *(C S★-5)* — genuine deviations from the shipped defaults (start/4 — re-read by this seat at chunk :47/:49), both correct for a right-anchored dock chip; `surface` correctly left at default.
9. **RTL-clean by construction** *(D S-C)* — zero physical-direction properties in 121 lines; the vendor's own dropdown rules are logical throughout (verified in the unlayered block this seat re-read). Residual bidi-isolation note on the LTR strings carried.
10. **Motion delegated whole, and PRM holds — by two belts and an inversion** *(D S-A, mechanism corrected per ruling 10)* — the file has no `@keyframes`, no `<style>`, no `matchMedia`; every delegated path is clamped under PRM, including via the unlayered menu.css PRM block and the toggle's own `transition:none`; the axis's stated `data-allow-motion` rationale is dead but the conclusion is over-secured.
11. **The dock-portal ownership seam resolves** *(C S★-3, harness H1)* — concordant with the banked kf-ChromeDock ruling that killed the opposite claim; the menu's portalled content is correctly dock-attributed by documented Vue provide/inject semantics.
12. **Reader-LC's nested-layer safety proof, re-scoped** — the hypothesis (opening SharePopover from inside the menu must dismiss the menu) was killed by `isLayerExist`'s document-order rule; under this seat's non-modal ruling the focus half is trivial (no trap to pause) and the dismiss-ordering half remains the load-bearing reason Share works from a pointer at all. Filed with corrected premises — the conclusion survives by a simpler route.
13. **The corpus's own discipline** — the L axis banked three investigated-and-dropped claims (§4) so no one re-walks them (this seat spot-confirmed the `@select.prevent` drop); the D axis recorded a falsifier catching its own near-miss (D-5's `icon-lg`); every livable-only magnitude was marked UNPROVEN rather than guessed. Almost every kill in this record lands on sub-clauses, prescriptions, and grades — the load-bearing mechanisms survived two hostile re-reads and an incredulous third.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (→ KF.W9)

1. **MM-4 computed-style witness** — `getComputedStyle` on a `[data-slot="dropdown-menu-item"]` in the built demo: if `cursor` resolves `pointer` on the Clear row or `color` resolves the destructive token, the unlayered-wins derivation is wrong and MM-4/MM-15/MM-34 die together (this seat's byte-offset brace-match says they will not).
2. **MM-4's min-w row** — rendered content-box width vs 272px; decides whether the 8rem floor is a symptomless casualty (K-14).
3. **MM-7 rendered box** — the predicted 20×36 button and ~8px glyph; every cascade step is source-proven.
4. **MM-10 row-3 height** vs row 1 (equal ≈ collapse confirmed).
5. **MM-11 geometry** — `getBoundingClientRect().left` of the five text wrappers.
6. **MM-12 percept** — sample the filtered mark vs the label's computed colour in BOTH themes.
7. **S-B composited contrast** — the light-arm `--muted-foreground` pair (~11% headroom over 4.5:1) under real glass, carrying all four 10px runs.
8. **MM-5's severity hinge** — the ppMode scene-change magnitude; doubly deferred on the KF-APP-1 cure + the C-14 bucket resolution.
9. **MM-2's exposure half** — a11y-tree dump in Chrome and WebKit: is the nested `aria-pressed` button exposed or suppressed under children-presentational.
10. **MM-3's ratio** — measured actuating area over hover-advertised area for rows 1/2.
11. **MM-32's rhythm** — the five row heights over the 44px floor (prediction: rows 1–4 floored equal, row 5 alone ~56px).
12. **MM-13 focus-return** — after cancelling `confirm()`, narrowed by ruling 3: no trap, no body lock; the only open question is where focus lands.

**MOOTED** (not carried): reader-LC's aria-hidden/`useHideOthers` probe — the non-modal branch never calls it. **Source-undecidable, irrelevant** (both readers concordant): D-7's `leading-tight` vs `line-height:1` intra-layer order — a 10px uppercase mono prose line is the defect at either leading.

## Routing summary (M-25 agglomeration input)

- **NO-WAVE-OWNER — the dock-menu actuation/typography/brand family (SS-1/SS-2 intake)**: MM-2·3·5·7·8·9·10·11·12·13·16·17·18·19·20·21·22·24(consistency)·25·26·29·30·31·32·39·40·42 + MM-1/MM-6's cure law (≡ the banked KF-APP-1 motion: repair `:100`, dispose `setPPMode` with the KF-APP-17 sweep, resolve the C-14 bucket split, land the MM-5 CheckboxItem in the same edit).
- **KF.W0 §B-12**: K-1's residue — the manifest schism (worktree deletions vs origin's 7.0.0 pins, lock included).
- **KF.W4**: MM-36 (≡ KF-APP-4: vue-tsc over demo, test wiring) + MM-13's confirm-untestability rider + MM-9's gateless-census datum.
- **KF.W6**: MM-4's demo-side repair (interim unlayered override or post-relay adoption; MM-15's z correction and MM-30's `text-accent-red` land in the same motion; MM-44 scopes the blast radius to one component).
- **KF.W9**: the 12 SS-13 probes above.
- **glass-ui BH relay** (one communiqué, per the standing owner edict): (1) `dropdown-menu/styles.css` + the `_shared/menu.css` tail are UNLAYERED — the `layer(components)` wrap, sequenced with the consumer's `z-modal`→`z-popover` correction (MM-4/MM-15); (2) the Boolean-cast pair — `modal` and `open` declared `{ type: Boolean }` without `default: undefined`, making the documented reka modal default unreachable and the primitive permanently controlled for every consumer (MM-27/MM-28); (3) the `./avatar` exports-map gap (MM-24); (4) the `data-allow-motion` PRM inversion (MM-43, with ruling 10's mechanism correction).
- **kf-ChromeDock M-4 riders**: MM-28 (MUST-CARRY: preserve a local open binding through the round-trip deletion) + MM-23 (the stale App.vue prose dies in the same motion).

## Closing verdict

**ADJUDICATED — both corpus BLOCKERs die at the registry, and the sharpest new mechanism is upstream.** The `.value` throw that both readers led with is real, quadruply derived, and **already banked as KF-APP-1** — it folds home carrying two new riders (the `440e5c30^` extraction provenance; the seeded-bucket write per failing click). The phantom-dep BLOCKER is **substrate-dead** against origin/master's exact `7.0.0` pin — both readers re-offended against the banked dirty-manifest law, the corpus's only systemic failure, and once again it is an unnamed baseline, not a derivation error. The decisive incredulity work of this seat was the **modal question**: glass-ui's bare `{ type: Boolean }` declarations make the menu non-modal by accident and the primitive permanently controlled — reader-DU's pair of misses is confirmed at the bytes, kills reader-LC's modal-composition cells and moots one of its probes, and mints a MUST-CARRY rider on the banked kf-ChromeDock M-4 cure, which as worded would ship an unopenable menu. The surviving yield is dense and correctly sized: a keyboard/pointer actuation family (MM-2/MM-3, with the Tab escape-hatch and 5-of-5 count killed), a cascade family whose core is **GLASS-OWNED** (MM-4's unlayered dropdown sheet, independently brace-matched by this seat, with nine consumer casualties incl. the destructive row's colour — which must be repaired to the demo's OWN red, MM-30, not the vendor's), a typography family where the same file is both the manifest's named exception and its five-fold violator (MM-8/MM-9/MM-29/MM-31), and a brand row whose mark and label part ways in the dark (MM-12). Grade discipline held throughout: every BLOCKER filed by the axes or readers demoted or folded — the component ships no crash of its own that isn't already banked, and nothing here installs. Terminal shape: **44 rows — 0 net-new BLOCKER · 13 MAJOR-band (3 folds) · 20 MINOR · 11 INFO; 15 kills; 14 misses admitted; the family routes NO-WAVE-OWNER into the dock-menu packet; KF.W0 §B-12 takes the manifest schism; KF.W4 the gates; KF.W6 the demo-side cascade repair; KF.W9 the twelve probes; four items ride one glass-ui BH communiqué; two riders travel with banked kf-ChromeDock M-4.** Nothing here stamps a wave; every disposition is M-25 agglomeration input for the forming X·KF specs, awaiting the owner's begin-word.

— end of apotheosis. The three challenge files and both fresh reader returns remain immutable beside this record per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/app/dock/MbabbMenu.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **MbabbMenu.vue=+13/-133**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**THE SUBJECT OF C-20's TWO-BANK CONTRADICTION, MEASURED.** ⟨`git show 8281638c:demo/app/dock/MbabbMenu.vue | wc -l`⟩ → **241** · ⟨`… 81a56990 …`⟩ → **121** · ⟨`… 6d280ee7 …`⟩ (the pre-settle worktree) → **121**. ⟨`git diff --numstat 6d280ee7 81a56990 -- …`⟩ → **EMPTY**; ⟨`git diff --numstat 8281638c 81a56990 -- …`⟩ → **`13  133`**. So kf-CubeScene's *"byte-identical between the worktree and origin/master"* is TRUE and kf-ChromeDock's *"MOVED at the frontier"* is TRUE — of different coordinate pairs. **`−146` reproduces at no coordinate this wave can name; the measured motion is +13/−133, net −120.** The load-bearing shape survives: the `@select.prevent` menuitem wrappers are present verbatim at the ref of record, **5 sites** (`:8` `:18` `:29` `:46` `:57`).

**K-2's banked-row-refiled-as-fresh-BLOCKER is a C-19 sibling-class firing**, and the law it grounds is booked verbatim at C-19: *"the failure is never derivation; it is an unread registry and an unnamed baseline."*

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

