Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SpringTarget; receipt read from this seat's own system prompt, not inherited).

# kf-SpringTarget — adjudicated apotheosis r1 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — **worktree ≡ `origin/master` `81a56990` on every load-bearing surface** (my `git diff origin/master --stat` over `demo/scenes/spring`, `useDragScrub.ts`, `useDoubleTap.ts`, `demo/styles`: only `package.json`/`package-lock.json` differ — the uncommitted glass-ui devDep deletion, banked at KF.W0 §B-12); local HEAD `8281638c` divergent (its package.json pins glass-ui 6.0.0; the frontier pins **7.0.0 exact**, locked at lock:612; installed = 7.0.0 — my own `git show` receipts). · **Registry tree** value.js `tranche-u` · **Corpus** three challenge axes over `audit/kf-components/SpringTarget/` — **D** (design, 16 defects · 2 BLOCKER) · **L** (library, 18 defects · 1 BLOCKER) · **C** (consumption, 10 defects · 1 BLOCKER) — plus **two hostile re-reader returns** (reader-1 design-and-usage: 25 confirmed-groups / 5 kills / 5 misses; reader-2 library-and-code: 20 confirmed-groups / 8 kills / 5 misses), both consumed whole; the three challenge files re-read whole by this seat.

**Method (incredulity).** Every reader-vs-reader disagreement, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived at the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session — whole reads: SpringTarget.vue (470 L), useSpringDerby.ts (115 L), SpringTrace.vue (129 L, M-3 hand-traced against the emitter shape → 2/98, both guards dead), useSpringLinearStops.ts (34 L), useCompiledEntry.ts (84 L), usePainterRegistry.ts (20 L). Targeted: useSpringDemo.ts :1-30/:80-130/:190-244/:285-339/:470-499 (the frame gate :197-206 with `repaintSprings()` past it; `reseat` :294-300; derby wiring :310-318 — `settle = () => reseat(0)` verified; `play/pause/togglePlay` exported :483-485, unconsumed); useSweepScene.ts:77-92; SpringScene.vue:5-12 (the OUTER `px-6 lg:px-8` wrapper — decisive for the geometry kill) /:180-199; useSpringKeyframesEditor.ts:35-60 (`easing.fn(t)` at :50 — decisive against C-2's exculpation); useDragScrub.ts:108-150; useDoubleTap.ts:55-84; engine linear-stops.ts:38-71 + timing-function.ts:35-104; design-idioms.css:14-31/:73-80/:158-237; style.css:61-66/:126-165/:205-222/:236-242; springPresets.ts (ζ 0.86/0.65/0.45/1.0); SpringPhysicsFacet.vue:39-47/:168-198; SpringHeatmap.vue:72 (`DAMPING_MIN = 0.2`); EasingTarget.vue:12-28; font-roles.json:82. Installed glass-ui 7.0.0: typography/utilities.css (`text-mono-caption` verbatim — `letter-spacing: var(--type-tracking-caps); text-transform: uppercase`; `text-mono-small` transform-free), typography/scale.css (`--type-display-audacious: clamp(8.728rem, 5rem + 16vw, 22rem)`; `--type-display-2`; `--type-caption`), tokens/scheme-motion.css (`--duration-fast: 0.2s`; `--type-tracking-snug: -0.01em`; `--type-tracking-tight: -0.025em`; `--type-tracking-caps: 0.1em`; `--z-content: 10`), theme/bridges.css (`--ease-standard`), utilities/a11y-overrides.css whole head (the PRM universal rule constrains `animation-duration`/`iteration-count` AND forces `transition-property: opacity,… 0.1s` — this seat's PRM-irony cell below; forced-colors names `.focus-ring:focus-visible` explicitly), components/metric/index.d.ts (exports exactly `Metric/MetricCell/MetricRow/MetricStack`; `grep -rl MetricBadge dist` → 0), card/styles.css (`container-type: inline-size` on the un-rendered card-header only). Git: `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"`; lock:612 block present; worktree grep → 0; SpringSidebar.vue deletion at `277c01ec`; PLAY-dispatch census (exactly 2 sites); PRM census (3 CSS blocks, 0 JS gates in the spring scene; EasingTarget.vue:234 `useMediaQuery`); `useSpringLinearStops` consumers (SpringTrace.vue:35 + StartingStyleTarget.vue:86 — neither the one the prose names). My own arithmetic: the Mp table (ζ 0.86/0.65/0.45/0.20 → 0.0050/0.0681/0.2054/0.5266 — matches both readers), and the **full D-4 compositing chain** (raw #26D944 vs #fdf5ec ≈ 1.75 → 90/10 mix with the near-black foreground **raises** it to ≈ 2.11 → ×0.85 opacity over the plate → **1.92**) — decisive for ruling 8. Registry instruments: kf-SequencePlayhead r4 (whole, the register exemplar), ColorSpaceSelector (whole), targeted reads of kf-EasingTarget (KF-ET-8/KF-ET-12/P-2), kf-SequenceAxis (D-4/L-2/D-12 relay), kf-AnimationVisualizer (KF-AV-10/KF-AV-29), kf-AmigaScene (MISSED-F), kf-CubeScene (M-1 substrate / the PRM-universal-reset ruling), kf-CopyButton (KF-CB-13), kf-EasingSidebar (KF-ES-8), kf-MbabbMenu (MM-29), kf-OrbitalDrag (ruling 5, which cites SpringTarget.vue:65-70 as the house ARIA exemplar).

## Scoreboard

**45 roster rows — 0 BLOCKER · 5 MAJOR · 27 MINOR · 13 INFO.** All three axis BLOCKERs die or demote: **B-1 is DEAD WHOLE by its own falsifier at the frontier** (both readers concordant, my own git receipts — the sixth one-baseline-short event in this program's ledger, folding to census F-1/SCH-1 ≡ kf-CubeScene M-1's residue); **C-1 and D-1 demote to MAJOR** with their mechanisms fully confirmed; **D-2 demotes to MAJOR** (ruled up from reader-2's MINOR). **Killed-claims register: 16 entries**, including one reader arithmetic cell killed by this seat's own derivation (K-15) and three severity kills. **8 net-new misses admitted (N-1..N-8)** — one of them filed independently by BOTH readers (the derby's one-way exit), the strongest convergence event in this corpus. **10 superlatives.** **10 UNPROVEN residues → SS-13.**

**Headline.** The corpus's sharpest find survives everything thrown at it: on the scene's documented entry state the rail's primary gesture moves the ghost marker and the ARIA value while the solver — and therefore the protagonist ball — never ticks (C-1), and three prose surfaces assert the behaviour the gate forbids. The same session establishes that the fix corpus is largely one packet: the unclamped protagonist (M-2), the settle pulse pinned to the wrong end of a user-mutable rail (D-2), the derby's droppable hide-timer (M-5), and the parser that re-implements the engine's own public numeric curve (M-4, whose exculpation in C died at `useSpringKeyframesEditor.ts:50` — the same directory already consumes `.fn`). Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census taxonomy (KF.W0/W6/W7/W9) with **NO-WAVE-OWNER** for the spring scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **C-1 grade: BLOCKER (C + reader-1) vs MAJOR (reader-2)** | **MAJOR — reader-2's demotion adopted; reader-1's aggravator cells adopted into the row** | Chain re-walked at the bytes: `reseat` :294-300 writes targets + `startLoop()`, dispatches nothing; `startLoop` (useSweepScene:82-87) only arms; `frame()` :197-206 self-terminates before `liveSpring.tickDt` :214 and `repaintSprings()` :232 whenever the machine is not `playing`; SpringScene.vue:194 `autoPlays: false`; PLAY census = 2 sites, neither on the rail path; the cure (`demo.play`) exported :483-485 and unconsumed. But "every gesture is inert" is falsified by C's own trace: the ghost marker :87-90, `aria-valuenow` :67, and the target ref all respond — what is inert is the SOLVER; the transport Play sits in the ribbon; a returning `playing:true` snapshot works. The response-without-motion state is arguably worse than dead (it looks broken, not idle) — that and the three false prose surfaces (SpringTarget.vue:126-130; SpringScene.vue:192-193 "reseat re-arms the loop directly"; the :91-93 painter comment) are the aggravators, and they keep this row at the TOP of MAJOR. The repair is a design decision (gesture dispatches play-intent, or `reseat` becomes self-arming), not a bug patch — reader-2's framing, adopted. |
| 2 | **D-2 grade: BLOCKER (D) vs MAJOR (reader-1) vs MINOR (reader-2)** | **MAJOR — reader-2's demotion REVERSED; the BLOCKER stays killed** | Reader-2's mitigation ("unreachable while target === 1, the default") is inverted as a mitigation: `settle = () => reseat(0)` (useSpringDemo.ts:316, my read) means **every derby ends at target 0** — and the settle confirmation then fires on `.spring-target-line` at `right: 0` (:362-370), the full rail away from where the field settled, on the scene's headline feature, every time. Add the drag/arrow/Home paths (:241, :254-268) and the false prose invariant :75-79 ("the rail's right edge = the spring's target"). An instrument that signals state at the wrong coordinate on its core surface is the banked N-1 class (instrument-truth), not cosmetic nit-class. No state corruption + the un-pulsing true marker keep it out of BLOCKER. Percept witness → SS-13 #10. |
| 3 | **M-3/C-2 grade: MAJOR (corpus + reader-1) vs MINOR (reader-2)** | **MINOR, riding M-4's cure — reader-2 adopted** | My own hand-trace against linear-stops.ts:62-70's exact shape (26 stops, first/last implicit, interiors 4.000-96.000%) reproduces 2/98 and proves both `??` guards at SpringTrace.vue:77-78 unreachable. Rendered consequence: a ~2% inset each end of a `preserveAspectRatio="none"` stretch (~15 px on the max-w-3xl column) against reference lines spanning 0-100 — legible, wrong, and small. The durable content is the inverted algorithm + two dead guards + the violated written oracle (:48-49 vs the emitter's own :40). M-4 (MAJOR, both readers concordant) owns the cure: swapping the body onto `springTimingFunction(...).fn` deletes the parser, M-3, and both guards in one edit. |
| 4 | **The six D MAJORs (D-3..D-8): reader-1 sustains all six at MAJOR; reader-2 carries them at MINOR** | **ALL SIX → MINOR; reader-1's cells adopted, grades not** | Calibration is the banked registry, not axis rhetoric: kf-SequenceAxis D-4 carries the same-idiom gridline contrast at MINOR (and already names SpringTarget :63/:141 as co-consumer — that row FOLDS, both readers concordant); kf-EasingTarget KF-ET-8 carries the rail-tint row (FOLDS); KF-ET-12 (MAJOR there) is the *component's own central graphic* clipped — here the clip rows' protagonist half is already MAJOR at M-2, and the derby half (D-7/C-8) is a transient egg overlay; D-3's substance is one declined engine flag (`respectReducedMotion`, default false, none of the three `new SpringProgress` sites passes it — my read) whose ambush case is *removed by the scene's own `autoPlays: false`* (reader-1's own superlative concedes the non-escalation); D-4's 1.92:1 is a certain AA fail but on a ~2 s aria-hidden overlay (the banked D-8/C-§5 and contrast precedents run MINOR/INFO); D-5's pointer-only egg is a deliberate-retirement design decision (:173) with the `aria-hidden` half the real charge; D-6/D-14 are structural a11y gaps of the class the register books MINOR. Reader-1's MAJOR grades are PRESERVED AS DISSENT on all six. |
| 5 | **m-9: MINOR (L + reader-1) vs INFO-with-correction (reader-2)** | **INFO — reader-2's correction adopted** | The clamp the comment describes DOES exist (:210) and DOES apply to a ball whose readout shows >1 (the sampler; `demo.sampled` at :139 is unclamped) — so L's "describes a clamp that neither exists where it sits nor applies to the element it names" is half-wrong. What survives: 27-line orphaning + the word "marker" colliding with `.spring-target-marker`. Corroborating evidence for M-2 either way. |
| 6 | **i-13/C-3: MAJOR (C) vs MINOR (reader-1) vs cure-kill + fold (reader-2)** | **MINOR, riding banked kf-AmigaScene MISSED-F; C's cure KILLED** | The census fact is real (my style.css:210-219 read confirms the exposure analysis; the declaring-site census is concordant across both readers) and it sits on the scene's primary gesture surface — MINOR, not INFO. But the prescribed cure (`touch-action: none`) would manufacture a SEVENTH instance of a CONFIRMED banked defect (MISSED-F: the house-wide `none` idiom suppresses pinch-zoom; banked cure = shared `pinch-zoom` refinement, NO-WAVE-OWNER, KF.W9 witnesses). The edit is owned by the banked row, never by this component. Symptom → SS-13 (iOS). |
| 7 | **C-7: cure-killed-to-a-nit (reader-1) vs "CONFIRMED, tokens exist" (reader-2)** | **Both true; row NARROWED to INFO** | My reads settle it: `--type-display-audacious` = 139.6-352 px — adopting the named utility inside a ~279 px content box is structurally unsound, and `--type-tracking-tight` = −0.025em would change the design 2.5×, while the file's literal `-0.01em` IS `--type-tracking-snug` to the digit. The rung the file actually approximates is `--type-display-2` (D-9's cell, correct). Surviving row: one line — `-0.01em` → `var(--type-tracking-snug)` — folded into D-9's cure. C's prescription dies (K-10). |
| 8 | **Reader-2's D-4 arithmetic clause: "≈1.75 BEFORE the 90/10 mix and the 0.85 composite, both of which move it further toward the plate"** | **CLAUSE KILLED (K-15) — this seat's own derivation; reader-1's chain is the correct one** | Mixing 90% #26D944 with 10% of the near-black `--foreground` DARKENS the colour and **raises** contrast against the light plate: my chain gives raw ≈ 1.75 → post-mix ≈ **2.11** → ×0.85 over the plate → **1.92**, matching reader-1's composited (0.272, 0.803, 0.351)/L 0.457 row and D's published 1.92 to the second decimal. The row's conclusion (certain AA fail; a saturated green cannot reach 4.5:1 on any light-family plate) survives untouched — which is exactly why the direction cell had to die: robust conclusions must not carry wrong mechanisms into the bank. |
| 9 | **The convergent miss: reader-1 #1 and reader-2 #1 independently file the derby's one-way exit** | **ADMITTED ONCE (N-1, MINOR) — with this seat's PRM-irony cell added** | Verified whole at :379-386/:404-416: the `transition: opacity` lives INSIDE `.spring-rail--derby`, so class-add fades (at the real 0.2s, not the written 160ms) and class-remove snaps (after-change style has no opacity transition — the marker's base :331 transitions `border-color` only); the overlay's `v-if` :105 has no `<Transition>` and `.derby-lanes` declares only `derby-fade-in`, so the unmount is instant against THREE false prose cells (:402-403 "fades in/out"; useSpringDerby.ts:100-101 "drain back"; C:51 inherited it — that kill, K-12, is the seed of this find). The file itself models the correct pattern twice (:331, :372-377). **This seat's addition, verified at the installed a11y-overrides.css: under PRM the universal `transition-property: opacity,… 0.1s !important` gives reduced-motion users a smoother derby exit than default users get** — the scene's only two-way fade is the accessibility override's accident. |
| 10 | **N-2 (uppercase corruption) novelty vs the banked family** | **ADMITTED (MINOR) as the spring-scene member of a three-times-banked family — with this seat's MM-29 rider** | `text-mono-caption` verbatim at the installed pin: `text-transform: uppercase` + `letter-spacing: var(--type-tracking-caps)` (0.1em); `text-mono-small` has neither (the asymmetry is real). Net-new: SpringTarget.vue:48 renders the velocity label "v" as "V"; SpringTrace.vue:12-13 renders **ζ (U+03B6) as Ζ (U+0396)** — a different symbol, reading as Latin Z, in the one place the plot names its own parameter; and the file patches exactly ONE of its sites (`.derby-lane-tag { text-transform: none }` :454) — a known-and-unpropagated fix. **Rider (this seat's own, the banked MM-29 mechanism at a new site): the :454 patch resets `text-transform` but NOT `letter-spacing` — 0.1em caps tracking rides on the lowercase lane names.** Family folds: KF-ES-8 (mechanism MAJOR at its home), kf-SequenceAxis D-12 relay, MM-29. |
| 11 | **B-1 and its cascade: both readers kill; the corpus filed it as the component's only BLOCKER** | **KILL SUSTAINED WHOLE, my own receipts; identity folded, not re-booked** | `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact; lock:612 block with integrity + `dev: true`; installed 7.0.0; worktree = the uncommitted deletion (banked: kf-CubeScene M-1 "would revive F-1 if committed" → KF.W0 §B-12); local HEAD = 6.0.0 (the three-way churn kf-AnimatedText recorded). L's falsifier ("if both exist, B-1 dies whole") fires. Collaterals die with it: i-14's "unpinned, unlocked" aggravator; C-6's F-1 paragraph; C-4's lockfile hedge; S★5's caveat. Reader-1's THIRD independent limb verified by this seat: L's symbol-definer table is wrong in 3 of 8 rows against the installed dist (`--z-content` and `--duration-fast` live in tokens/scheme-motion.css, `--ease-standard` in theme/bridges.css) **and `--duration-fast` = 0.2s**, so the file's three `160ms` fallbacks encode a stale figure (→ N-7). Every load-bearing spring file is frontier-identical (my diff) — only the dependency declaration was ever read from the dirt. |
| 12 | **C-2's exculpation ("keyframes.js publishes no numeric spring sampler; the parser is forced") + its §5.3 library prescription** | **KILLED at the bytes — reader-2's decisive cell verified** | `springTimingFunction(opts): Easing` is barrel-exported; timing-function.ts:91-100 builds `.fn` from the SAME `sampleNormalizedSpring` with the same `response * 4` default, `fn(0)=0`/`fn(1)=1` pinned (:83-89). DECISIVE and unseen by all three axes: **the demo already consumes `.fn` as a numeric sampler in this very directory** — useSpringKeyframesEditor.ts:44-52 (`const easing = springTimingFunction({...}); const v = easing.fn(t)`), imported by useSpringDemo.ts:15. The parser contradicts the directory's own idiom, which STRENGTHENS M-4; publishing `sampleNormalizedSpring` (C §5.3) is an unnecessary library change. C-2's endpoint arithmetic survives inside M-3. |
| 13 | **L's S-1 superlative exclusivity ("I have not seen it elsewhere in either repo's demo trees")** | **EXCLUSIVITY KILLED; engineering praise survives as superlative 1** | The `translateX(<cqw>)` idiom is a named house law ("T.G4", cited five times in this file alone) with verified sibling sites (SpringPhysicsFacet.vue:185-198 `.preset-track`/`.preset-ball`, my read; SequenceScrubber/SequencePlayhead/SpringHeatmap per the banked kf-AnimationVisualizer and kf-SequencePlayhead records). D's SL-1 form — completeness-checked, no exclusivity claim — is adopted. |
| 14 | **The 327 px narrow-geometry cells (D-1's clip thresholds, D-7's clip, C-8's 7.3% budget)** | **CELLS KILLED; the finding survives SHARPER (both readers concordant)** | SpringScene.vue:8 wraps the Card in its own `px-6 lg:px-8` (my read) — at 375 px the Card is 327 and its content box **279**, which is L's frame. Corrected consequence: ζ = 0.45 puts the 36 px ball's box at [342, 378] against a 327 px clip edge — fully off-plate at peak (reader-2's arithmetic, re-checked); snappy ζ = 0.65 ≈ 36% clipped; the derby ball is invisible above v ≈ 1.11 of its sanctioned 1.18. Rendered paint → SS-13. |
| 15 | **m-12 (derby extraction) — reader-2 confirms; reader-1 files no cell** | **CONFIRMED (MINOR); no consequence to the silence** | My counts: derby markup :96-124 (29 L) + style :398-460/:466-468 (~68 L of a 200 L scoped block); the file's own precedent argued at :148-151. A `SpringDerbyLanes.vue` would take m-6, m-7, and a third of m-8 with it. |

**Adjudicator's corrections register (against this corpus's readers):** (1) reader-2's D-4 mix-direction clause — K-15, this seat's own derivation; (2) reader-2's D-2 MINOR — reversed, ruling 2; (3) reader-1's six MAJOR sustains — declined, ruling 4; (4) reader-1's C-1 BLOCKER — declined, ruling 1; (5) reader-1's M-3 MAJOR — declined, ruling 3; (6) reader-1's m-9/i-13 grades — split, rulings 5-6. Credits: reader-1's full-chain contrast arithmetic (the only correct D-4 chain in the corpus after D's own), the `text-mono-caption` Greek-substitution find, and the `--duration-fast` 0.2s catch; reader-2's `useSpringKeyframesEditor.ts` decisive cell, the SpringScene wrapper-padding correction, the m-9 correction, and the banked-cure kill on C-3; both readers' independent convergence on N-1.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the spring scene-repair packet). Anchors bind to worktree ≡ `origin/master` `81a56990`; re-resolve before any cure.

### MAJOR

- **C-1** *(demoted from BLOCKER, ruling 1)* — **the rail's primary gesture never moves the protagonist in the scene's documented entry state**: `reseat()`/`derby()` arm a loop whose first frame self-terminates while the machine is not `playing` (useSpringDemo.ts:197-206, gate ahead of `tickDt` :214 and `repaintSprings` :232); entry is `paused` (`autoPlays: false`, SpringScene.vue:194; PLAY census = 2 sites, neither on the rail path); the marker/ARIA respond while the ball stays parked — response-without-motion. The injected cure (`demo.play` :483-485) is unconsumed. Three prose surfaces assert the forbidden behaviour (SpringTarget.vue:126-130; SpringScene.vue:192-193; :91-93). The repair is a **contract decision** — gesture dispatches play-intent, or the facility makes `reseat`/`derby` self-arming — and it must preserve the rest-on-entry posture that superlative 5 protects. **NO-WAVE-OWNER (packet head).**
- **M-2 / D-1 / m-9-as-evidence** *(D's BLOCKER killed, K-3)* — **the protagonist ball is painted unclamped** (`translateX(${live.value * 100}cqw)` :206-208) while both siblings in the same closure clamp (:210, :222 with the "can't leave the lane" rationale); engine documents the overshoot (peak ≈ 1.205 at ζ 0.45, timing-function.ts:54-59); ζ user-writable to 0.2 (facet slider, heatmap) and 0.45 by one preset click; at 375 px the ball's box [342, 378] exits the Card's 327 px `overflow-hidden` edge entirely (279 px rail frame — ruling 14); a 1→0 re-seat clips left symmetrically. The orphaned :179-180 comment documents the lost guarantee. Cure: one clamp mirroring the derby's own bound (an explicit overshoot ALLOWANCE, not `[0,1]` — the overshoot is the thesis), plus the comment relocation. Clip-mechanism family template: banked kf-EasingTarget KF-ET-12. Wide-desktop regime → SS-13 #1. **NO-WAVE-OWNER.**
- **M-4 (+ C-2's mechanism half)** — **SpringTrace round-trips the engine through CSS text** (`springLinearStops` string → ~40-line regex reparse, SpringTrace.vue:50-86) when the engine's public `springTimingFunction(...).fn` hands it the same curve numerically (same sampler, same `response*4` default, endpoints pinned exactly) — **and the same directory already consumes `.fn`** (useSpringKeyframesEditor.ts:44-52, ruling 12). The swap deletes M-3 and both dead guards; `.css` still supplies the stop-count label. C-2's "the parser is forced" exculpation and §5.3 library prescription are killed. **NO-WAVE-OWNER.**
- **M-5** — **derby re-entrancy drops a live timer**: the only guard is `derbyRunning` (:74), cleared at ~1340 ms (:99) while `derbyActive` — the flag the overlay renders on — clears at ~2040 ms (:102-106); a second double-tap in that 700 ms window passes the guard and `derbyTimers.length = 0` (:77) orphans the pending hide-timer without `clearTimeout`, which then unmounts the lanes ~540 ms into race #2; the same truncation empties the array `onScopeDispose` (:112) iterates, leaving a timer pending against a disposed scope on a scene swap. Found by L alone; verified by both readers and this seat. Cure: `clearTimeout` before truncation + gate on `derbyActive`. **NO-WAVE-OWNER.**
- **D-2** *(ruled up, ruling 2)* — **the settle confirmation fires at a position the spring may never have visited**: the pulse binds to `.spring-target-line` pinned `right: 0` (:80-84, :362-370) while the true, user-mutable target is the separate un-pulsing marker (:87-90); `reseat` accepts any ratio and **every derby ends at target 0** (`settle = () => reseat(0)`), so the headline egg's ending is confirmed with a flash at the opposite end of the rail, every time; the prose invariant :75-79 is false. Cure: re-home the pulse on the marker (or demote the line to an explicitly-labeled scale reference); SpringTrace's fixed y=1 line is correct and untouched. Witness → SS-13 #10. **NO-WAVE-OWNER.**

### MINOR

- **M-3 / C-2** *(demoted, ruling 3)* — the implicit-stop fill runs before the first/last anchoring; both `??` guards dead; trace spans x∈[2,98] against its own written oracle and the emitter's. Dies with M-4's swap. **NO-WAVE-OWNER (inside M-4).**
- **D-3** *(demoted, ruling 4; limb (a) narrowed per K-6)* — the PRM block reaches only the two decorations (redundant-in-effect against the universal a11y-overrides rule — which constrains duration/iteration-count, NOT `animation-name`, so the local `animation: none` is not "dead" and deleting it on D's reasoning would leave 0.01ms animations still firing events); the 60 Hz solver path has no PRM gate anywhere, and the engine ships the opt-in (`respectReducedMotion`, default false; none of the three construction sites passes it) while the sibling scene proves the JS route (EasingTarget.vue:234). Escalation is capped by `autoPlays: false` (motion is user-initiated; transport Pause exists). Mechanism family banked at kf-CubeScene (universal-reset + engine-motion-not-CSS-governable). **NO-WAVE-OWNER (pass the flag at the three sites) + KF.W9 (PRM datum).**
- **D-4** *(demoted, ruling 4; arithmetic settled by ruling 8)* — all four derby lane tags fail AA in light theme (snappy composited ≈ **1.92:1**, the corpus's worst figure; gentle ≈ 3.9) at 12-16 px/400 against a 4.5:1 floor, while the repo's AA-solved `.status-badge` idiom (design-idioms.css:213-229, "AA-CONTRAST (load-bearing)") is consumed correctly 400 lines above by the same file. A regression, not a hard problem. Exact in-situ figures over the glass plate → SS-13 #4. Reader-1's MAJOR preserved as dissent. **NO-WAVE-OWNER (tag re-derivation through the badge mix) + KF.W9.**
- **D-5** *(demoted, ruling 4)* — the derby is reachable by exactly one pointer gesture (useDoubleTap binds pointer events only; no Enter/Space/registry shortcut — census verified) and the whole overlay is `aria-hidden` (:107); the discovery affordance was deliberately retired (:173). Keyboard-only cannot trigger; AT cannot trigger or perceive. Owner decision flagged: if eggs are ruled pointer-only by design, the row reduces to the `aria-hidden` half. **NO-WAVE-OWNER (gesture spec) + KF.W9.**
- **D-6** — no heading, no landmark, no accessible name anywhere in the scene (grep zero across seven files); the sibling at the identical rung is semantic (EasingTarget.vue:22-28 `<header>` + `<h2>`, same utilities). Intra-repo divergence, not house style. **NO-WAVE-OWNER + KF.W9.**
- **D-7 / C-8** *(one row: the derby's payload is occluded twice; narrow-geometry cells corrected per ruling 14)* — the sanctioned 18% overshoot clips at the Card edge above v ≈ 1.11 (279 px frame), and `.derby-lane-tag` (`right: 0; top: -0.65rem`, painted last) sits over exactly the region the winning ball occupies — reader-1's ~6.8-of-12.8 px ink overlap is plausible and line-height-dependent (its own hedge), so the overprint magnitude is SS-13's. D's tag-vs-tag kill (withdrawn candidate) stays dead — lane-pitch re-derivation concordant. **NO-WAVE-OWNER; witnesses SS-13 #2.**
- **D-8** *(demoted, ruling 4; two rows FOLD by identity)* — the rail (8% tint ≈ 1.1:1, backdrop-independent), the target line (35%), the lane rails (22%), and the ghost marker (50% ≈ 2.0:1) sit below the 1.4.11 3:1 floor; the derby state multiplies rail/ball/marker by 0.35 (≈ 1.04:1). The sharpest cell is this seat's compounding note: **in the C-1 paused entry state the sub-2:1 ghost marker is the ONLY visual response to the primary gesture.** FOLDS, not re-booked: the `.stage-field-x` gridline row ≡ banked kf-SequenceAxis D-4 (which names SpringTarget :63/:141); the 8%-tint idiom row ≡ banked kf-EasingTarget KF-ET-8; D's own :250 inherited-not-invented framing adopted. Near-threshold rows → SS-13 #4. **NO-WAVE-OWNER + KF.W9.**
- **D-9 (+ C-7's surviving nit, ruling 7)** — `6cqi` has no query container on any ancestor (my census: the file's own two `container-type` sites are non-ancestors; the demo utility is applied only in AnimationVisualizer.vue:13; glass-ui's only card-side container is the un-rendered `.card-header`) → resolves as `6svw`, a viewport-keyed two-value step duplicating `--type-display-2` to ~3 px at both ends — the opposite of the author's intent. Cure: containerize the header column or adopt `--type-display-2`; fold in the one-liner `-0.01em` → `var(--type-tracking-snug)`. **NO-WAVE-OWNER.**
- **D-10** — two structurally identical label rows 20 px apart at `mb-2` vs `mb-1` with nothing explaining the difference; the plot's `overflow: visible` argues for MORE clearance. **NO-WAVE-OWNER (hygiene).**
- **D-11** — the Card has inline padding only, `overflow-hidden`, and no overflow strategy (no `overflow-y-auto`, no FadingScroll — which the repo owns and the sibling imports); `justify-center` clips symmetrically, taking the readout and plot first. Short-stage threshold → SS-13 #7. **NO-WAVE-OWNER.**
- **D-12** *(family member — folds to the banked kf-SequencePlayhead D-4 prose-truth family; NOT a new identity)* — five colour claims false against the violet the tokens resolve to (`--color-progress` = `--accent-kf`, style.css:163/:130, under the "red exits the chrome entirely" authority): SpringTarget.vue:272-276 (the load-bearing "progress green" rationale justifying the `--ball-tone` seam), :102-103, :357-360, SpringTrace.vue:100-101, useSpringDerby.ts:9. Five spring sites ADDED to the banked family's sweep checklist. **NO-WAVE-OWNER (the banked prose sweep).**
- **D-13** — the declared "ONE colour consumer" seam is bypassed by three rules in the same file (:368, :387-396 ×3) and every rule in the child (SpringTrace.vue:109/:122/:127 — a DOM descendant the variable cascades into); setting `--ball-tone` would half-recolour the instrument. The inverted-fallback twin (six dead `var(--ball-tone, var(--color-progress))` fallbacks under the unconditional :277-279 declaration) ≡ the banked kf-SequencePlayhead D-14/N-9 class — folded, not re-booked (with the note that design-idioms.css:161-165 documents the fallback form as the deliberate multi-scene idiom; the deadness is scoped to this file's own rules). **NO-WAVE-OWNER.**
- **D-14** — the one discrete, high-salience state change (settled↔tracking, :44-47) has no `role="status"`/`aria-live`; the surrounding 6 Hz restraint is correct and preserved; `aria-valuenow` tracks the commanded value and cannot convey settle. **NO-WAVE-OWNER + KF.W9 (one binding, with N-4).**
- **C-4** *(both readers concordant at MINOR; S-9 nomination stands)* — the header's load-bearing rationale claims adoption of `MetricBadge`, which exists in no resolvable glass-ui (my grep: 0 files; the metric barrel exports exactly four names); the markup is hand-rolled; the credibility comes from the real `MetricSize "xl"`. Reader-2's enrichment verified: **font-roles.json:82 encodes "the MetricBadge telemetry label idiom" as house law** — the cure is a repo-wide prose sweep + the S-9 evaluate (adopt `/metric` or delete the sentence; the `.status-badge` AA mix is load-bearing and Metric has no badge affordance). B-1's death makes the claim STRONGER (7.0.0 pinned exact). **KF.W7 (S-9 evaluate, sequenced with banked KF-AV-10) + the prose sweep.**
- **C-5 (+ N-8 rider)** — the ghost marker animates `left` on the every-pointermove drag path against the file's own T.G4 law three lines below; true-for-tap, false-for-drag defence recorded. Magnitude → SS-13 #6. **NO-WAVE-OWNER.**
- **i-13 / C-3** *(ruling 6)* — the only pointer-drag surface in the demo without a `touch-action` declaration, on the primary gesture; rides the banked kf-AmigaScene MISSED-F house cure (`pinch-zoom` family — NEVER `none`); symptom → SS-13 #5. **Rides the banked row.**
- **m-6** — `spring-lane-${lane.name}` is a phantom class (no matching selector in either tree; the tone is delivered by the inline `--ball-tone` binding) that reads as the tone mechanism. **NO-WAVE-OWNER (deletion).**
- **m-7** — `display: flex; align-items: center` on `.spring-rail`/`.sampler-track` is inert (every child absolute with both axes resolved — enumerated); only `container-type` is load-bearing. **NO-WAVE-OWNER (deletion).**
- **m-8** *(rides banked KF-AV-10 — the seventh-authoring family row, which already cites SpringTarget :113; NOT a new identity)* — the T.G4 anchor triplet re-authored four times (`.spring-ball`/`.sampler-ball`/`.derby-lane-ball`/`.preset-ball` — my reads) against the idiom file's own "promoted ONCE (was authored four ways)" boast; `.derby-lane-ball` also re-declares the idiom's `margin-top`. **Cure cautioned by banked kf-EasingTarget P-2** (the idiom leaves `transform` unclaimed BY DESIGN; a careless consolidation drops 28 balls out of their rails). **KF.W7 (with KF-AV-10).**
- **m-10** — `el as HTMLElement` converts the file's only cast from known-narrow to unchecked; a future component-ref edit throws inside the 60 Hz painter; `instanceof` narrowing is the honest form. **NO-WAVE-OWNER.**
- **m-11** — the rail stays fully live during the derby and `reseat` overwrites all four staggered targets while the stagger timers keep firing behind it — self-inflicted, recovers at settle. One gesture-spec decision with D-5/i-18. **NO-WAVE-OWNER.**
- **m-12** *(ruling 15)* — the derby overlay is the one sub-concern not extracted against the file's own SpringTrace precedent; a `SpringDerbyLanes.vue` takes m-6/m-7 and a third of m-8 with it. **NO-WAVE-OWNER.**
- **N-1** *(NET-NEW — BOTH readers independently; ruling 9)* — **the derby's exit is an instant snap in both halves and three prose cells assert a fade that does not exist**: the overlay unmounts with no `<Transition>`; the dimmed rail/ball/marker jump 0.35→1 because the opacity transition lives only inside the state class (the classic modifier-only asymmetry, on the scene's headline delight; the file uses the correct base-rule pattern twice). PRM-irony cell recorded (the a11y override accidentally gives PRM users the only two-way fade). Cure: move the transition to the three base rules + a leave treatment on the overlay. **NO-WAVE-OWNER.**
- **N-2** *(NET-NEW site of a banked family; ruling 10)* — `text-mono-caption`'s `uppercase` corrupts the scene's three-symbol vocabulary on two of three surfaces ("v"→"V" :48; **ζ→Ζ** SpringTrace:12-13) while the one patched site (:454) proves the fix was known and unpropagated — and the patch itself is MM-29-incomplete (caps tracking survives). **NO-WAVE-OWNER (one cure family with the banked rows).**
- **N-3** *(NET-NEW — reader-1 #3 + reader-2 #5 merged)* — the component's only interactive element has **no hover, no active, no drag-state styling whatsoever** (`dragging` ref returned by useDragScrub:149 and never destructured), and its one hover-adjacent declaration is dead CSS: `.spring-target-marker`'s `transition: border-color` (:331) has no writer — no rule, state class, or binding ever changes that border colour (theme swap aside). Family-rides banked KF-AV-29/KF-CB-13; the kf-PlaybackRibbon K-4 anti-pattern noted (no producer hover exists to inherit — the rail is a bespoke div). Compounds D-8. **NO-WAVE-OWNER.**
- **N-4** *(NET-NEW — reader-1 #4)* — the slider's accessible name is an instruction naming a modality ("Drag to re-seat the spring target") and its `aria-valuenow` announces the COMMANDED target while the hero numeral 20 px away shows the live displacement — two scalars, one unlabelled 0-100 announcement, no `aria-valuetext`. The valuetext half is concordant with the banked kf-SequenceAxis D-12 relay class; the instruction-as-name and commanded-vs-live halves are net-new. Unlike the banked N-14 case, no domain decision blocks the cure (announce the commanded target honestly: noun-phrase name + valuetext). C's presence-only killed-candidate #5 tested presence, not correctness. **NO-WAVE-OWNER + KF.W9 (with D-14).**
- **N-6** *(NET-NEW — reader-2 #4; scene-scope, outside this component's blast surface — filed to the packet, not to the file)* — useCompiledEntry.ts:65-81's `recompile` is async with no debounce, no in-flight token, no generation guard, no dispose cancellation, watched per slider input on a self-described HEAVY compile — two in-flight compiles resolving out of order pin the copy-pasteable artifact to a stale curve (last-to-RESOLVE wins). The sync `setTimingFunction` half is race-free; only `css.value` is exposed. **NO-WAVE-OWNER (spring packet).**

### INFO

- **m-9** *(demoted, ruling 5)* — the `// clamp the *marker*` comment is 27 lines orphaned and name-collides with `.spring-target-marker`; the clamp it describes exists (:210, the sampler) — corroborating evidence for M-2. — **i-15** — `derbyBallEls` never shrinks; four null-checks per frame forever after the first derby. — **i-16** — two permanent `will-change` layers against the scene's "zero cost at rest" posture. — **i-17/C-6 (idiom half only; weight half killed as the axis predicted)** — root-barrel `Card` import; `./card` published (217 B) and consumed by nobody; house idiom per the lane census — INFO, not a defect. — **i-18** — the egg is not gesture-disjoint from the primary action (each launching tap first re-seats the target via `onScrub` on pointerdown :125); one gesture-spec decision with m-11. — **D-15** — three decimals at a display rung refreshed 6×/s (`PROGRESS_READOUT_HZ = 6`). — **D-16** — the quarter-tick prose says three lines, the gradient paints four (0/25/50/75); the shared-idiom cure is owned by banked kf-SequenceAxis L-2 (`--tick-count`, whose fallback keeps SpringTarget at 4). — **C-7-narrowed** *(ruling 7)* — the `-0.01em` literal IS `--type-tracking-snug`; write the name. — **C-9** — `inject(SPRING_DEMO_KEY)!` is the five-of-five house idiom; lane-owned, correctly not charged here. — **C-10** — the parent/child data-flow asymmetry is undeclared; the child's prop contract is the better idiom (both readers concordant). — **N-5** *(NET-NEW — reader-2 #2)* — useSpringLinearStops.ts:8-14, the scene's ONE documented engine-seam contract, cites `SpringSidebar.vue:130` as a consumer — deleted at `277c01ec` (my git log); the live consumers are SpringTrace.vue:35 + StartingStyleTarget.vue:86, neither named. Same class as banked kf-SequencePlayhead N-19 (the one written contract statement is wrong); distinct site. — **N-7** *(NET-NEW — reader-1 #5)* — `var(--duration-fast, 160ms)` ×3 (:331, :373, :385) encodes a stale figure 25% off the real token (0.2s, my read); the fallback never applies; documentation drift on exactly the lines N-1's cure touches. — **N-8** *(NET-NEW — reader-2 #3; rides C-5)* — the drag projector runs `getBoundingClientRect()` per pointermove (:236-239) after the marker's `left` write dirties layout — a read-after-write reflow pair per pointer sample, in the file whose painter law is "NO per-frame width read"; ordering static, magnitude → SS-13 #6.

## Killed-claims register — 16 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **B-1 (L, BLOCKER)** — "@mkbabb/glass-ui appears in neither package.json nor package-lock.json … hard build failure" + cascade | **DEAD WHOLE by its own falsifier** (ruling 11, my own git receipts: origin/master pins 7.0.0 exact + locked; the absence is uncommitted worktree dirt). Identity FOLDS to census F-1/SCH-1 ≡ kf-CubeScene M-1 residue (KF.W0 §B-12) — the program's sixth one-baseline-short event. Collaterals dead: i-14's aggravator, C-6's F-1 paragraph, C-4's lockfile hedge, S★5's caveat, the legacy-peer-deps horn (exact pin + integrity). L's definer table corrected in 3 of 8 rows; `--duration-fast` = 0.2s (→ N-7) |
| **K-2** | C-1's BLOCKER grade ("every gesture … inert; nothing lower fits") | **SEVERITY KILL** (ruling 1) — the gesture surface responds (marker, ARIA, target); the solver is what's inert; transport Play adjacent. MAJOR, mechanism intact, aggravators adopted |
| **K-3** | D-1's BLOCKER grade | **SEVERITY KILL** — the shipped default ζ 0.86 (Mp ≈ 0.005, my table) does not clip; reaching the clip needs a preset click or slider move; demo scene, no data at risk. MAJOR (M-2) |
| **K-4** | D-2's BLOCKER grade | **SEVERITY KILL** (ruling 2) — no state corruption, redundant true marker present; but reader-2's MINOR is also killed (the derby ALWAYS exhibits it). MAJOR |
| **K-5** | The 327 px rail-width cells (D-1 geometry, D-7 clip, C-8's 7.3% budget) | **CELLS KILLED, finding sharper** (ruling 14) — SpringScene.vue:8's outer `px-6` makes the content box ~279 px; L's frame is the citable one; consequences worse than D/C stated |
| **K-6** | D-3(a) — "both local `animation: none` declarations were already dead" | **NARROWED** — the universal PRM rule constrains duration/iteration-count, not `animation-name`; the local declarations win for the property they set: redundant-in-effect, not dead. The distinction changes the repair (deleting them leaves 0.01ms animations firing events) |
| **K-7** | C-2's aggravating clause ("keyframes.js publishes no numeric spring sampler … the parser is forced") + §5.3's publish-the-sampler prescription | **KILLED at the bytes** (ruling 12) — `springTimingFunction(...).fn` is the sanctioned public route and useSpringKeyframesEditor.ts:44-52 already consumes it, three files from the parser |
| **K-8** | C-3's MAJOR grade + prescribed cure ("one declaration … `touch-action: none`, matching the six siblings") | **CURE KILLED by the banked registry** (ruling 6) — kf-AmigaScene MISSED-F rules the `none` idiom itself a CONFIRMED defect; the correct edit is the banked `pinch-zoom` family, owned there. Census fact survives at MINOR; symptom UNPROVEN |
| **K-9** | C:51 — "the four derby lanes fade in, race nothing, and **fade out** ~2.3 s later" | **KILLED** — no fade-out exists (`derby-fade-in` only; bare `v-if`); C inherited the file's own false :402-403 comment. The kill seeded N-1 |
| **K-10** | C-7's prescribed cure (adopt `text-display-audacious` / `--type-tracking-tight`) | **KILLED at the installed pin** (ruling 7) — audacious = 139.6-352 px inside a ~279 px box; tight = −0.025em vs the file's −0.01em which IS the published snug value. Row narrowed to the one-line token-name nit |
| **K-11** | L's S-1 exclusivity ("I have not seen it elsewhere in either repo's demo trees") | **KILLED** (ruling 13) — T.G4 is a named house law with verified sibling sites; the engineering praise survives as superlative 1 |
| **K-12** | C-6's weight half (root-barrel = bundle cost) | **KILLED as the axis itself predicted** — `sideEffects: ["*.css"]`, pure re-export barrel, tree-shakes to parity; idiom half survives at INFO |
| **K-13** | m-9's MINOR grade ("describes a clamp that neither exists where it sits nor applies to the element it names") | **DEMOTED to INFO** (ruling 5) — the clamp exists (:210) and applies to the sampler ball whose readout shows >1; what survives is orphaning + name collision |
| **K-14** | M-3's MAJOR grade (L + C + reader-1) | **SEVERITY KILL** (ruling 3) — a ~2% inset each end; the durable half is dead guards + violated oracle; the cure belongs whole to M-4 |
| **K-15** | **Reader-2's D-4 arithmetic clause** — "≈1.75 BEFORE the 90/10 mix and the 0.85 composite, **both of which move it further toward the plate**" | **CELL KILLED by this seat's own derivation** (ruling 8) — the mix with the near-black foreground RAISES contrast (1.75 → 2.11); only the opacity composite lowers it (→ 1.92, = reader-1's chain = D's figure). The conclusion survives; the mechanism cell dies. Recorded per the K-30 law: robust conclusions must not carry wrong arithmetic into the bank |
| **K-16** | D's killed-candidate #3 (tag-vs-tag collision), C's killed candidates 1-6, L's killed candidates 1-6 | **ALL KILL-SECTIONS SUSTAINED under hostile re-reading** — both readers and this seat re-derived the lane-pitch/ink-box, `--ball-tone` IACVT, Card class-forwarding, function-ref unmount, ARIA-presence, and fill-mode cells; no kill was falsified (the register's healthiest outcome; contrast K-29's false kill in the sequence corpus) |

## Superlatives (provenance preserved; all re-verified at this seat except where noted)

1. **The `translateX(<cqw>)` value axis, COMPLETE** — every direct positioning ancestor of all six balls declares `container-type: inline-size` (verified line by line; one missing container would silently re-key the axis to the viewport — exactly what D-9 documents happening one selector away for `cqi`). Compositor-only, rail-relative, zero per-frame width reads, reasoning written down at :200-205. Exclusivity claim killed (K-11); engineering intact. (D SL-1 · L S-1 · both readers.)
2. **The painter seam is complete in the one place everyone forgets** — usePainterRegistry.ts:10 paints at registration (my whole read), and registration happens in `onMounted` with refs populated, so every ball is seated at its true value before frame 1; `onScopeDispose` (not `onUnmounted`) binds teardown to the scope the `v-if` remount cycle actually exercises; `Set.delete` closure makes double-dispose harmless; no leak constructible. (All three axes + both readers.)
3. **One writer drives six moving marks** — a single registered painter off a single non-reactive snapshot written by a single loop; the 6 Hz/60 Hz channel split is argued from a measured born-RED witness with the rejected cures named (useSpringHotPath.ts). (Reader-2's cell, adopted.)
4. **Two gesture recognizers on one element, neither able to launder into the other, both cancel-safe** — the 12 px slop zeroes the pending tap; `pointercancel` routes through the same end-path as `pointerup` so the global select-suppression token cannot strand; the iOS `setPointerCapture` throw is swallowed with the rationale written down. Reader-1's unnamed third property adopted with a spec-behaviour caveat: pointer capture on the shared element retargets moves to the rail, so the slop guard sees off-rail drags too. (My reads of both composables.)
5. **`autoPlays: false` is the honest answer to a measured regression — and it is exactly the mechanism C-1 convicts.** The receipt is in the file (VERDICT #19, ~33% of a core at idle); the gate makes the scene cost zero at rest and keeps PRM users un-ambushed (the reason D-3 stays MINOR). Both readings are true at once, which is what makes C-1's repair a design decision. (Reader-1's framing, adopted as the packet's head-note.)
6. **The engine's `springTimingFunction` docblock is a model library contract** — the typed pair, the exactness guarantee, the overshoot with a worked figure, and its precise relation to the sibling emitter — it is why M-4's cure is statically decidable and M-2's physics checkable without running anything. (Reader-1; my read of :40-104.)
7. **`.focus-ring` consumed rather than rolled — and that choice alone is why the focus indicator survives forced-colors**: the installed a11y-overrides names `.focus-ring:focus-visible` explicitly under `@media (forced-colors: active)`, with no bare-element fallback that would rescue a hand-rolled ring (my read of the whole block). (D SL-5 · both readers.)
8. **The `role="slider"` posture is the house exemplar with banked provenance** — kf-OrbitalDrag's ruling 5 cites SpringTarget.vue:65-70 FIRST in its house-law census; C's own killed-candidate #5 killed the missing-ARIA charge correctly. N-4 shows presence ≠ correctness — the posture is exemplary, the *name and value semantics* still owe one edit.
9. **SpringTrace is the right seam even carrying two findings** — a pure two-prop child; the seam is why M-3/M-4 are small and locally fixable, and the cure is a one-file edit. (L S-4 · both readers.)
10. **The `.status-badge` AA idiom is present, correct, documented as load-bearing, and consumed by this file** — which is precisely what convicts D-4 as a regression rather than a hard problem. (Reader-1's closing superlative, adopted.)

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The live-ball clip photo set** — ζ 0.86 (expect no clip) / 0.65 (expect ~36% clipped) / 0.45 (expect fully off-plate at peak) at 375 w, and the ≥1210 px full-bleed falsifier regime with the controls rail closed.
2. **One mid-race derby screenshot** — the lane-ball clip threshold (~1.11 of the 1.18 cap at 375 w) AND the tag-over-ball ink overlap (reader-1's ~6.8 px figure is preflight-line-height-dependent) in one capture.
3. **The derby overlay's vertical stack** — ~82.4 px centred on the 48 px rail with lane 1's tag ink ~20 px above the rail top vs the gap-8/gap-6 clearances (~12 / ~6.8 px computed): collision is a paint question.
4. **Contrast reads over the live glass plate** — the four derby tags (composited chain says 1.92-3.9 light); `.spring-target-marker` (50%) and `.sampler-ball` (65%), the two rows near the 3:1 floor. The 8%/22% tint rows are backdrop-independent (no probe needed); `.stage-field-x` is banked at kf-SequenceAxis D-4 — do not re-measure.
5. **i-13/C-3's symptom** — iOS Safari: does a real rail drag ever receive `pointercancel` given the `html,body` posture; if not, only the census fact survives.
6. **C-5/N-8 magnitude** — a performance trace during a rail drag (the per-sample `left` write + forced `getBoundingClientRect`); ordering is static, cost is not.
7. **D-11's short-stage clip** — landscape phone: does the ~400-430 px stack exceed the cell and clip the readout/plot symmetrically.
8. **D-7's line-height dependency** — the tag ink-box assumes Tailwind preflight's 1.5; the overprint itself is guaranteed by `right: 0` regardless.
9. **C-6's build byte-diff** — expected zero (recorded only so the claim is not reopened).
10. **D-2's percept witness** — run one derby to completion and photograph the settle pulse firing at the rail's right edge while the field rests at 0 (the born-RED material for the pulse re-home).

## Identity guards (fold-by-reference — never re-booked)

- **B-1 ≡ census F-1 ≡ SCH-1 ≡ kf-CubeScene M-1 residue** → KF.W0 §B-12 (the uncommitted devDep deletion still needs the banked owner ruling; committing it as-is revives F-1).
- **D-8's gridline row ≡ kf-SequenceAxis D-4** (which names SpringTarget :63/:141 and owns the shared-idiom cure); **D-8's rail-tint row ≡ kf-EasingTarget KF-ET-8**; **D-16's cure ≡ kf-SequenceAxis L-2** (`--tick-count`).
- **The clip-mechanism family template ≡ kf-EasingTarget KF-ET-12** (the sparkline-vs-rail asymmetry is the cure template for M-2/D-7).
- **m-8 ≡ banked KF-AV-10** (the seventh-authoring family, SpringTarget :113 already cited there) with **kf-EasingTarget P-2** as the standing cure caution. → KF.W7.
- **D-12 ≡ the banked kf-SequencePlayhead D-4 colour-prose family** (green→red→violet; five spring sites added as riders). **D-13's inverted-fallback twin ≡ kf-SequencePlayhead D-14/N-9 class.**
- **N-2 ≡ the KF-ES-8 / kf-SequenceAxis D-12-relay / MM-29 uppercase family** (net-new site + one-site-patched evidence + the Greek substitution + this seat's MM-29 rider). **N-3 family ≡ KF-AV-29 / KF-CB-13.** **N-5's class ≡ kf-SequencePlayhead N-19** (distinct site, filed here).
- **i-13/C-3's cure ≡ kf-AmigaScene MISSED-F** (the `pinch-zoom` family owns the edit).
- **D-3's mechanism family ≡ kf-CubeScene's PRM rulings** (universal-reset exists; engine motion not CSS-governable); the net-new limb here is the declined `respectReducedMotion` flag — booked HERE.
- **C-9 ≡ the lane-owned inject idiom** (five-of-five census; L's killed-candidate 1 and C-9 concordant — no component charge).

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the B-1/F-1 fold + the checkout-schism/devDep-deletion owner ruling (§B-12).
- **KF.W6**: no axis-owned material-register rows (the derby-tag cure is component-local through the badge mix).
- **KF.W7**: C-4's S-9 metric evaluate (sequenced with banked KF-AV-10) · m-8 (the ball-idiom consolidation, P-2-cautioned).
- **KF.W9**: the contrast reads (D-4/D-8 SS-13 rows) · the PRM datum (D-3) · the a11y data (D-5's aria-hidden half, D-6, D-14, N-4) · touch-action witnesses (with the MISSED-F family) · the SS-13 list.
- **NO-WAVE-OWNER — the spring scene-repair packet (SS-1/SS-2 authoring must home it):** the **contract head C-1** (one decision: who dispatches play-intent — with the rest-on-entry posture preserved) · the **honest-instrument core M-2 (clamp-with-allowance) + D-2 (pulse re-home) + D-7/C-8 (overshoot room or clip allowance) + the prose corrections** (:126-130, SpringScene:192-193, :75-79, :179-180, :402-403, useSpringDerby:100-101) · **M-4→M-3** (swap SpringTrace onto `.fn`; the parser, both dead guards, and the regex die together) · **M-5** (clearTimeout + gate on `derbyActive`) · **N-1** (base-rule transitions + a leave treatment) · the **gesture spec** (D-5 owner decision · m-11 race-time rail policy · i-18 · N-3's affordance vocabulary — one spec, not four patches) · the **a11y one-edit family** (D-6 heading · D-14 status region · N-4 name+valuetext) · **D-9 + C-7-nit** (containerize or adopt `--type-display-2`; write `--type-tracking-snug`) · **D-3's three-site flag pass** · **N-2's register cure** (with the banked uppercase family) · **N-6's generation guard** · hygiene (m-6 · m-7 · m-9 · m-10 · m-12 · D-10 · D-11 · D-12-riders · D-13 · i-15 · i-16 · N-5 · N-7 · N-8) · the packet's test obligation, born-RED today: (a) paused-entry rail-tap moves the ball (C-1's gate), (b) the trace's first/last plotted x = 0/100 (M-3's gate), (c) a double-tap at t≈1500 ms leaves the overlay mounted through race #2 (M-5's gate), (d) the settle pulse paints at `target`, not at 1 (D-2's gate).

## Verdict

**ADJUDICATED.** The corpus's three BLOCKERs resolve exactly as an incredulity seat should resolve them: one dead whole at the frontier by its own falsifier (B-1, the sixth one-baseline-short event — the readers' concordant kill sustained on this seat's own git receipts), two demoted with their mechanisms fully intact and their prose aggravators adopted (C-1, D-1/M-2). Between the two hostile re-readers the seat cut both ways: reader-2's D-2 demotion reversed on the derby's own `settle = reseat(0)`, its D-4 mix-direction cell killed by this seat's arithmetic, reader-1's six MAJOR sustains declined against the banked calibration, and its BLOCKER declined against C's own trace — while both readers' independent convergence on the derby's one-way exit was admitted once, enriched with a PRM irony neither found. The component underneath is the register's recurring shape at its most vivid: a 470-line leaf whose hot path, gesture seams, and focus posture are house exemplars — three of them cited as exemplars by other banked adjudications — wrapped in prose that lies about its colour, its fade, its clamp, its consumers, and its own primary gesture, and whose repair is one packet: one play-intent contract, one honest instrument (clamp, pulse, overshoot room), one numeric seam, one gesture spec, one register cure. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C/m/i for life, N-1..N-8 for life.

— end of apotheosis r1. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/spring/SpringTarget.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SpringTarget.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · `.d` measured, `.e` landed) — **D-14 · N-4 · D-6**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here; **measured by seat `.d`**, which
was forbidden to land it. **Nothing above this line is rewritten** — no edit, no re-grade, no renumber, no
deletion, no new id (E-1 / E-3). Every bullet is a dated MEASUREMENT; none re-books a row or moves a severity.*

- **cell** — `chromium` · `safari-app/desktop` (both read in the same pass where the cell was open)
- **capture** — none for these rows — live DOM/computed-style reads, banked unedited at `evidence/W9/W9d-raw-chromium-cells.json`
- **stamp** — substrate keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD) · bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448` (built in the §0m.2 capture clone, **measured and double-run** at `.d` and **re-hashed identical at `.e`**)

- **D-14 + N-4 CONFIRM as ONE BINDING**: on `/#/spring`, **16 of 16** sliders read `aria-valuetext: null`; names are **positional** (`"Value 1 of 5"`).
- **NET-NEW: `aria-valuenow` runs `0…1` against a declared `max` of 110.** A value space and a declared range that do not describe each other — recorded as a dated measurement, **not** as a re-grade of either id, and not booked as a new defect: no id is minted here.
- **D-6 CONFIRMS, and the intra-repo divergence now rests on a side-by-side reading rather than on two dates**: `/#/spring` headings **`[]`** and `/#/easing` headings **`["H2:ease"]`**, measured **in the same pass**.
- **D-5's `aria-hidden` half stays UNMEASURED** with its precondition named, and **i-13/C-3's iOS symptom (residue #5) is UNREACHABLE-IN-CELL** — `xcrun devicectl list devices` → `No devices found.`, `safaridriver --help | grep -ci simulator` → **0**, ruled at COHESION §0m.2.
