claude-fable-5

# kf-CSSCodeEditor — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt — recorded against L-11/L-14.
**Date**: 2026-08-07 · **Substrate**: keyframes.js `master` HEAD `8281638c` (re-confirmed by this seat; same HEAD as axis C) · **Corpus**: 3 challenge documents (D-1..D-18 · L-B1..L-i4 · C-1..C-19, all opus-5[1m]) against `demo/components/instrument/keyframes/CSSCodeEditor.vue` (229 L), plus two hostile re-reader returns (reader-A, reader-B) over the full corpus.
**Method (L-14)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (D-9, C-3's mechanism, L-m5/C-17's rationale, D-3's provenance, D-2's severity, the D-5/L-M5 `index.ts:11` sub-citation) was re-derived against the tree with this seat's own instruments; every solo-reader BLOCKER (reader-A's keyboard trap, reader-B's TS2307) was independently reproduced, not spot-trusted; two suspiciously-agreed cells (the 98/100 theme-rule count, the Ï/RibbonBar pair both readers found) were re-measured.
**Arbiter's own instruments (all static + node; zero browser probes — probe parsimony, per the read-only law)**: full read of CSSCodeEditor.vue (229 L); a transitive ESM import-graph walk from `editor.api.js` (677 modules in closure — suggest/toggleTabFocusMode/comment/bracketMatching/find/contextmenu/hover/folding all ABSENT, `browser/coreCommands.js` + `contrib/format/browser/format.js` REACHED); `ts.resolveModuleName` under the repo's own tsconfig (typescript 6.0.3, moduleResolution bundler): `monaco-editor` → `editor.main.d.ts`, `monaco-editor/esm/vs/editor/editor.api` → **UNRESOLVED (TS2307)**, `…/editor.api.js` → `editor.api.d.ts`; both theme JSONs measured by node (Dracula 46 rules / 1 empty-token / 8 colors incl. `editor.selectionBackground` = `editor.lineHighlightBackground` = `#44475a` byte-identical; GitHub 54 / 1 / 6, clean); monaco `package.json#exports` (`.` + bare `"./*"` wildcard, no types condition); `codeEditorWidget.js:1583/1599/1608` + `editorOptions.js:3431` + `editorContextKeys.js:44-45` + `tabFocus.js` + `coreCommands.js:1593-1601` (the tab-focus wiring); `_.contribution.js:1-55` (the full contrib import block); `find -ipath '*accessib*'` (3 accessible-view files, no help widget); `editor.api.d.ts` `accessibilitySupport` doc + `Environment.getWorker?(…): Promise<Worker> | Worker` verbatim; glass-ui 7.0.0 `package.json#exports` (73 subpaths, **no `./skeleton`**, `./surface` present), `grep -c Skeleton dist/index.d.ts` → 0; `glass-atom.css` (the 14% mix belongs to `.badge-atom--outline`; `.glass-atom` border-color appears only under `prefers-contrast: more` at 24%), `ladder.css` (quiet rung = `1px solid var(--glass-border-accent)` over `--glass-border-rung: var(--glass-border-quiet)`), `tokens/glass.css` (`--glass-border-quiet: color-mix(in srgb, var(--foreground) 4%, transparent)`); sibling reads: KeyframesStringControls.vue (Ï-only handler, `isFormatting` latch, per-emit `toast.success("Keyframes parsed 🎉")`, `defineExpose.formatCSS`), RibbonBar.vue:28 (`@click="activeKeyframesRef?.formatCSS?.()"` un-awaited), useKeyframeOps.ts:25-40 + :87-106 (withErrorToastAsync; the 1000 ms guarded op whose comment claims "run on every Monaco edit"), useKeyframesParsing.ts:66 (its sole caller), KeyframesAddDialog.vue:138-141 (the correct chord), KeyframesEditor.vue:218-221 (the second Ï literal — swallow-only), helpers.ts:15-24 (two params, closure-private timer, no cancel), keyframes/index.ts (barrel `defineAsyncComponent`), KeyframeTimeline.vue:85/:96-97/:123-127 (no `:key`)/:173/:239-265, `grep tabsListEl demo/` (zero template bindings), formatEditorCSS.ts (default `printWidth = 80` at **line 4** — C-15's `:3` off-by-one confirmed).

## Scoreboard

**49 adjudicated rows: 3 BLOCKER · 14 MAJOR · 23 MINOR · 9 INFO. 13 killed-claim register entries (incl. 2 kills of this corpus's own readers by this seat). 14 corpus-missed defects adjudicated IN. 1 row absorbed (L-m10 → L-M3's cure).**

Blocker-weight survivors: **(1)** the `5fe2e4cb` regression — the demo's flagship CSS editor ships with no CSS tokenizer, no language service, and no editor contributions, monochrome behind a green pipeline; **(2)** the stale 200 ms debounced emit that full-replaces the WRONG keyframe's vars after a selection change — silent data loss on the instrument's primary interaction loop; **(3)** the same regression's a11y half, found by neither original axis and only one re-reader: a WCAG 2.1.2 keyboard trap — Tab inserts indentation, the toggle command is unregistered, and `accessibilitySupport: "off"` removes the AT-detected alternative.

## Ruled disagreements (this seat's actual work)

| id | reader-A | reader-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-9** (clipped suggest widget) | KILLED — no suggest controller in the api graph | CONFIRMED MINOR — "wordBasedSuggestions defaults on … widgets do open there" | **KILLED — A wins** | This seat's transitive walk of the `editor.api.js` closure (677 modules): `contrib/suggest/browser/suggestController.js` is ABSENT. `wordBasedSuggestions: true` is a config with no controller to consume it; no suggest widget can render, so the clipping premise is dead. B's confirmation quotes only two d.ts defaults and `view.js`'s append site — none establishes a widget exists. Letter-correction to A: the suggestController importer set is wider than "exactly two places" (the bundled `basic-languages/*.js` and `language/*` files also carry the path, plus `edcore.main.js`) — immaterial, since none is in the closure. Re-priced as a NOTE pinned to the KF-CE-1 fix: whichever cure restores contributions must set `fixedOverflowWidgets: true` or supply `overflowWidgetsDomNode`, or D-9 revives at that moment in the 250px instance. |
| **C-3 mechanism** (type/runtime realm split) | CONFIRMED as stated ("the seven-member delta is exact") | KILLED mechanism, conclusion survives: the specifier is TS2307-unresolvable, `m` is `any` | **B wins — my own `ts.resolveModuleName` run is the decider** | Under the repo tsconfig (ts 6.0.3): `monaco-editor/esm/vs/editor/editor.api` → UNRESOLVED — monaco's `exports` wildcard `"./*": "./*"` carries no types condition and no extension, so TS never reaches `editor.api.d.ts`; the root `monaco-editor` → `editor.main.d.ts`; the `.js`-suffixed specifier → `editor.api.d.ts` (the cheapest cure, verified). So the boot's `m` is `any`, the ENTIRE boot body is unchecked, and A's "the seven-member delta is exact" is true of the two export lists but NOT of what the compiler sees. C-3's conclusion (`(await bootMonaco()).css` compiles clean and throws) holds under both mechanisms because `:31`'s root `import type` types the DECLARED return as the wide editor.main surface either way. Booked as one MAJOR row (KF-CE-11) fusing C-3-as-rescoped + B's missed TS2307. |
| **L-m5 / C-17 rationale** (`as any` on defineTheme) | CONFIRMED — "suppresses the one check…" | Rationale KILLED — dead ceremony; `m` is `any`, the cast suppresses nothing today | **B wins, consequence of the C-3 ruling** | With `m: any`, `m.editor.defineTheme` is untyped and the casts are inert ornament. The row survives RESCOPED (MINOR): the casts become load-bearing only after the specifier fix, at which point the narrow `IStandaloneThemeData` cast is the cure — a **dependent** fix, sequenced after KF-CE-11, not the independent cheap minor both original axes ordered it as. |
| **D-3 provenance** (the intended hairline) | 14% mis-attributed; real comparator = 4% via ladder/tokens | Confirmed D-3 repeating the "14%-foreground hairline" phrase; ratios left unproven | **A wins on provenance; the defect stands for both** | My reads: the `14%` `color-mix` in `glass-atom.css` sits inside `.badge-atom--outline` — a badge rule; `.glass-atom` itself takes a border-COLOR only under `@media (prefers-contrast: more)` (24%). A `<Card cartoon tier="quiet">` borders via `ladder.css` `border: 1px solid var(--glass-border-accent)` over `--glass-border-rung: var(--glass-border-quiet)`, and `tokens/glass.css` pins `--glass-border-quiet: color-mix(in srgb, var(--foreground) 4%, transparent)`. Intended tint ≈ 4%, not 14% — D's appendix-A ratio table dies, in the direction that UNDERSTATES the defect. The structural half (preflight `border: 0 solid` leaves `currentColor`; the utility sets width+shadow only; sole raw-utility site in demo/) is CONFIRMED MAJOR. B's repetition of the 14% phrase in a confirmed cell is a citation-inheritance event, recorded in DISSENT. |
| **D-2 / C-14 severity** | BLOCKER | MAJOR | **MAJOR — B's rating, cluster-headed by the trap** | With the keyboard trap standing as its own BLOCKER (KF-CE-3), `accessibilitySupport: "off"` is the same exclusion's aggravator, not a second independent BLOCKER: it removes the AT-detected escape (screen-reader mode announces the tab-behavior state) from a surface that is already a one-way door. One a11y-regression cluster, one BLOCKER head, this row MAJOR beneath it. A's correction to D-2's letter is adopted: monaco 0.55 ships NO accessibility-help widget at all (my `find`: only `accessibleDiffViewer` + two accessible-view files), so "drops the accessibility-help affordance" claimed a loss of something that does not exist. |
| **Keyboard trap** (reader-A solo BLOCKER) | CONFIRMED BLOCKER | — (not found) | **CONFIRMED BLOCKER, mechanism corrected, cure cheapened** | Independently reproduced by this seat: `toggleTabFocusMode` ABSENT from the 677-module closure; `coreCommands.js:1593-1601` registers Tab under `EditorContextKeys.tabDoesNotMoveFocus`; `editorContextKeys.js:45` derives it as `tabMovesFocus.toNegated()`; default false. **Correction to A's mechanism**: the toggle contrib is NOT "the ONLY code path that can flip tab-focus mode" — `tabFocusMode` is a registered create-time editor option (`editorOptions.js:3431`, EditorOption 164, default false), and `codeEditorWidget.js:1608` seeds the context key from `options.get(164) || TabFocus.getTabFocusMode()`. So a contrib-free, one-line consumer cure exists (`tabFocusMode: true` at `editor.create`, correct for a short-snippet editor) — the defect is exactly as A states in the SHIPPED config, and the cure is cheaper than A implies. |
| **D-5 / L-M5 sub-citation** (`keyframes/index.ts:11` governs the mount) | cited as part of the confirmation | sub-citation KILLED — both consumers import the SFC statically | **B wins** | My reads: KeyframesStringControls.vue:47 and KeyframeTimeline.vue:173 both `import CSSCodeEditor from "…/CSSCodeEditor.vue"` — direct, static. The barrel's `defineAsyncComponent` is evaluated and never used (B's missed #6, confirmed as its own MINOR row). Both parent defects (no loading state; no error surface) survive untouched; only the repair SITE named by the sub-citation dies. |

## Adjudicated defect roster — 49 rows

Ids are the corpus's own, for life; `KF-CE-n` is this record's roster index. Dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input (M-25). `EDITOR-UNIT` = **NO-WAVE-OWNER**: a proposed single wave-surface ("the Monaco seam made honest" — boot, language, a11y, v-model, error posture, themes) for SS-1/SS-2 spec authoring to consume; no census KF.W0–W10 row names this repair, and scattering it across ten waves would orphan its one root cause. It must sequence AFTER the KF.W4 type-gate lands (nothing in it is verifiable until a `.vue` file can fail a build).

### BLOCKER
- **KF-CE-1 · D-1 = L-B1 = C-1 — the `editor.api` regression: no CSS tokenizer, no language service, monochrome editor; both vendored themes ~96% inert.** Proof: my closure walk + both readers' convergent chains; `editor.api2.js` imports six modules, registers zero languages; `git show 5fe2e4cb` = the one-line diff whose commit message measured the deletion as a saving. Count corrected: **98 of 100** rules inert (each theme's one `{token:""}` rule DOES match; measured by this seat), and 8/6 `colors` keys still paint — which is what let the corpus miss KF-CE-33/34. → **EDITOR-UNIT** (NO-WAVE-OWNER). The cure decision is entangled with KF-CE-3/4 — see the killed register #3: there is no cheap highlighting-only import; the honest options are (a) restore contributions (bytes return, a11y contribs return) or (b) hand-register a Monarch tokenizer + `setLanguageConfiguration` + `tabFocusMode: true` (bytes stay saved, the trap is cured by option, comment-toggle/find/context-menu stay absent BY DECISION). Re-measure `vendor-monaco` either way; U.D5's 4.18→2.53 MB is invalid as like-for-like.
- **KF-CE-2 · L-B2 (= C-9 as its weaker form) — a stale in-flight 200 ms debounced emit lands on the WRONG keyframe after selection change; `kf.vars` is full-replaced with the previous keyframe's text. Silent data loss.** Proof: helpers.ts:15-24 (no cancel handle, closure-private timer — my read); the guarded `setValue` fires `onDidChangeModelContent` synchronously (both readers' textModel.js chain) so the armed timer is neither cancelled nor re-armed; KeyframeTimeline binds no `:key` (:123-127, my read) so no remount on A→B; `:246-249` resolves the target from the LIVE ref at emit time; `:263` assigns, not merges. One identity: C-9 (parent-write-clobbered) is the same defect's milder trigger — folded, not double-booked. → child half (cancel-on-external-write + cancel-on-unmount, one `replaceContent()` seam) **EDITOR-UNIT**; parent half (emit-time target resolution; a `:key`d remount or arm-time id capture) → **KF.W7** (Timeline Evaluate).
- **KF-CE-3 · corpus-missed (reader-A) — WCAG 2.1.2 keyboard trap: Tab cannot leave the editor; the toggle command is unregistered; `accessibilitySupport: "off"` removes the AT-detected alternative; no Escape handler exists.** Proof: this seat's closure walk + tab-focus wiring reads (header block). Mechanism corrected and cure cheapened per the ruled table: `tabFocusMode: true` at create is a contrib-free fix. The a11y half of `5fe2e4cb`, outranking the highlighting half. → **EDITOR-UNIT**, first in its repair order.

### MAJOR
- **KF-CE-4 · corpus-missed (reader-A) — the regression stripped the ~110-module editor contribution set, not just the CSS language**: no find widget, no context menu, no comment toggle, no folding, no bracket matching, no multicursor, no hover, no clipboard actions, no unicode highlighter. Proof: my walk — every `contrib/*` except `format/browser/format.js` absent; basic editing survives only via `coreCommands` (reached). Kills D-9's premise; re-prices both original repair plans. All three axes framed the loss as "language" — the framing itself was the miss. → **EDITOR-UNIT** (same decision as KF-CE-1).
- **KF-CE-5 · D-2 = C-14 — `accessibilitySupport: "off"`, undocumented, un-propped, against monaco's own "It is best to leave this to 'auto'"** (doc verbatim, my read at editor.api.d.ts ~3617-3621). Severity MAJOR per the ruled table; the accessibility-help clause of D-2 is dead (killed register #12). → **EDITOR-UNIT**.
- **KF-CE-6 · C-4 = L-M2 — all three `setValue` sites destroy undo/redo and every decoration** (`textModel.js:303-317`, both readers; the normal cycle guarantees the write because both parents normalize). C's BLOCKER rating stays inflated — history loss, not data loss; MAJOR. Cure: `executeEdits`/`pushEditOperations` in the one `replaceContent()` seam. → **EDITOR-UNIT**.
- **KF-CE-7 · L-M1 — `formatCSS()` never reaches the model**: the formatted text is written under `isSettingValue`, no `modelValue.value = formatted` exists, the toast asserts a persistence that did not happen, the next sync reverts it; the sibling's `isFormatting` latch is a compensator for an emit that does not exist. Found by L alone; D-6/C-5 read the same ten lines and saw only the missing catch. → **EDITOR-UNIT**.
- **KF-CE-8 · L-M3 — the external-sync watch rewrites the buffer under the user's caret on every lossy round-trip**; no focus guard, no echo suppression; both parents' transforms provably non-identity (KeyframeTimeline drops blanks/comments and re-serializes — my read of :251-261/:241-243; the other parent round-trips through prettier). L-m10 (the optionless watch shape) ABSORBED into this row's cure. → **EDITOR-UNIT**.
- **KF-CE-9 · D-6 = C-5 — `formatCSSContent` has no error path**; prettier's scss parser rejects on the ordinary mid-edit buffer state; the rejection floats; the sibling's `isFormatting` latch sticks true for the session, permanently suppressing its success feedback. The declined idiom sits one file over (`withErrorToastAsync`, my read of useKeyframeOps.ts:25-40). The toastGuard adjacency clause is dead (killed #7). See also KF-CE-37 (the RibbonBar path both original axes under-scoped). → **EDITOR-UNIT**.
- **KF-CE-10 · C-6 = L-M5 — a rejected `bootMonaco()` is cached forever by `??=`; `initEditor` fire-and-forget at both sites; outcome = a permanently blank sized box, no message, no retry.** The `index.ts:11` sub-citation is dead (killed #11); the cancellation guard correctly declines to swallow it and nothing else surfaces it. Cure: reset-on-reject + rendered failure state + retry. → **EDITOR-UNIT**.
- **KF-CE-11 · C-3-as-rescoped + corpus-missed (reader-B) — the boot's own import specifier is TS2307-unresolvable under the repo tsconfig, so `m` is `any` and the single most consequential block in the file is the one block no type ever touched**; the declared `Promise<typeof Monaco>` (= editor.main surface, via `:31`) still promises seven members the runtime lacks, so `(await bootMonaco()).css` compiles clean and throws. Proof: this seat's own `resolveModuleName` run (header block) — the decider. Cheapest cure verified: the `.js`-suffixed specifier (maps to `editor.api.d.ts`), then correct `:52`'s return type; settles C-3 at the root. → **EDITOR-UNIT**, sequenced immediately after the KF.W4 gate (it is the first diagnostic vue-tsc will surface). L §5 dropped-item #1's mechanism corrected en route (killed #10): the `MonacoEnvironment` global rides `:31`'s ROOT type import, which is therefore NOT deletable.
- **KF-CE-12 · C-7 (+ L-i2) — the `@src/animation/resolve/browser` deep import drags two value.js subpaths + a permanent module-scope `window` resize listener onto the editor chunk, around the `loadAnimationEngine()` firewall, for two lines of arithmetic; falsifies `sideEffects: false`; `depcruise src` cannot see it; `useKeyframesState.ts:1` commits the identical breach.** C-7's R1 paragraph is adopted as written: no defect HERE, but `:116` is the demo's largest untrusted-CSS ingress and the parser waves should treat it as the fuzz entry of record — that note folds by reference to the megatranche R1 identity, never re-booked. → publish-or-relocate `convertPixelsToCh` + the deep-import extirpation → **KF.W8** (Structure & Colocation Settle); the depcruise-over-demo gate arm → **KF.W4**; the R1 ingress note → **KF.W2/KF.W3**.
- **KF-CE-13 · D-8 — explicit `theme:` at create + `setTheme` on every dark flip defeat `autoDetectHighContrast` while monaco unconditionally emits `forced-color-adjust: none` for its subtree**; zero `forced-colors` sites demo-wide against 13+ PRM sites. Code path closed on source (both readers re-verified the service lines); rendered result UNPROVEN-NEEDS-LIVE (SS-13). → **EDITOR-UNIT**; the demo-wide categorical `forced-colors` gap extends census §6.5 and routes with **KF.W9**'s visual-audit horizon.
- **KF-CE-14 · D-5 — no loading state for the demo's largest module**: an empty hard-bordered 450px/250px box, no aria-busy, no fallback at either call-site. The Skeleton cure is DEAD (killed #2 — unreachable from any consumer; producer-side gap); the defect stands whole. → **EDITOR-UNIT** for the loading state (App.skeleton.vue's shimmer plate is the in-tree precedent); the Skeleton-export producer gap → **glass-ui BH relay** (owner edict, standing formation invariant) + census S-6 correction: "unreached" must read "UNREACHABLE".
- **KF-CE-15 · D-4 = C-2 — the `css.worker?worker` chunk is fetched and awaited on the boot critical path and can never be instantiated; the `label === "css"` arm is dead code.** Statically closed; byte cost UNPROVEN-NEEDS-LIVE. Decide jointly with KF-CE-1's cure: service restored → keep; Monarch-only → delete both the import and the arm. See KF-CE-39 for the sibling insight neither original axis had. → **EDITOR-UNIT**.
- **KF-CE-16 · D-12 = L-M4 = C-8 — `debounce(fn, 200, false)` passes a third argument to a two-parameter function (helpers.ts:15-24, my read), surviving because zero `.vue` files are typechecked by any script in the repo** (`vue-tsc` absent; CI runs `check:lib` only; reader-B measured the TS2554 with an in-memory program). The call-site fix rides the **EDITOR-UNIT**; the gate hole — the finding's real body — routes to **KF.W4** (K-Quartet Hygiene, beside K3): `vue-tsc --noEmit` into `check` + CI. Every other row in this roster is unverifiable until it lands; it is the sequencing head of the whole record.
- **KF-CE-17 · D-3 — `cartoon-surface` on a bare `<div>` paints its 2px border in `currentColor`; the only raw-utility site in the demo; the intended glass hairline is a ~4% foreground tint** (provenance corrected per the ruled table; the 14% table is dead, killed #6). Cure: glass-ui `Surface` (published `./surface`, verified) or `<Card cartoon>`. → **KF.W6** (Glass Suffusion).

### MINOR
- **KF-CE-18 · D-13 = L-M6** — the debounce has no cancel and `onUnmounted` doesn't flush it; a pending emit fires from a torn-down instance (benign today only by the parent's null-check luck). Same seam as KF-CE-2's cure. → **EDITOR-UNIT**.
- **KF-CE-19 · D-11 = L-m7 = C-12** — `fontSize`/`lineNumbers`/`padding` read once at create, never re-applied; no `updateOptions` in the file; latent (both call-sites pass constants). → **EDITOR-UNIT**.
- **KF-CE-20 · D-7 = C-13** — `fontFamily: "Fira Code"` hardcoded, no generic fallback, bypassing `--font-mono`; latent (style.css:9 loads the payload), so C's MINOR beats D's MAJOR. → **EDITOR-UNIT**; token-read cure coordinates with **KF.W6**.
- **KF-CE-21 · D-10** — no accessible name on the host, no `ariaLabel` option; two same-named editors alive at once. Rides the KF-CE-3/5 a11y cluster. → **EDITOR-UNIT**.
- **KF-CE-22 · D-14** — all geometry is JS-prop pixels; zero CSS surface; `--panel-max-h: 60dvh` unused; sharpens census §6.3 (zero `--kf-*` tokens — the problem here is ABSENCE of a token surface, not collision). → **KF.W6** (the `--kf-*` token-namespace audit is named in its scope).
- **KF-CE-23 · D-15** — `rounded-lg` is the demo's only cartoon-stamped surface off the `--radius-card` ladder, nested inside a `rounded-card` Card. → **KF.W6**.
- **KF-CE-24 · L-m2 = C-15 (duplication half)** — `getFormatWidth` duplicated in-feature; the composable twin is permanently inert (`tabsListEl` has NO template binding anywhere — my grep; useTabStripScroll's `tabsListElRef` is a different, separately-bound ref), so the three parse-path formats silently take prettier's default 80 (`formatEditorCSS.ts:4` — C-15's `:3` off-by-one confirmed) while the component formats at a measured width. → **EDITOR-UNIT** (one width fn) + the inert-twin delete.
- **KF-CE-25 · L-m3 = C-15 (measurement half)** — the width is measured on the wrong font (container inherits body font; monaco renders Fira Code at props.fontSize), through a `ch ≈ 0.5em` approximation, on a box that includes gutter+padding. Magnitude UNPROVEN-NEEDS-LIVE. → **EDITOR-UNIT**.
- **KF-CE-26 · L-m1** — the `ch != null` guard is statically unreachable (`=> number`; the `|| 16` divisor floor). Pairs with KF-CE-49. → **EDITOR-UNIT**.
- **KF-CE-27 · L-m4 = C-11** — 3 of 4 exposed members dead; `setValue` is a model-desync hatch by construction; `getValue` returns `""` pre-boot; `editor: () => editor` leaks the raw instance. Correction adopted (reader-A): `formatCSS` has TWO consumers — the hotkey AND, transitively, RibbonBar.vue:28. → **EDITOR-UNIT**.
- **KF-CE-28 · L-m6 = C-10** — the dark flip uses a raw per-instance `watch` where glass-ui documents `onFlipSettled` for exactly this batching; currently masked by KF-CE-1 (nothing to re-tokenize), bites when it's fixed. → **EDITOR-UNIT**.
- **KF-CE-29 · L-m5 = C-17-as-rescoped** — the `as any` casts are dead ceremony today (`m` is `any`); post-KF-CE-11 they become the one suppressed check; cure = the narrow `IStandaloneThemeData` cast, DEPENDENT on KF-CE-11. → **EDITOR-UNIT**.
- **KF-CE-30 · L-m8** — `monaco-themes` is a dead devDependency (themes vendored; only prose references remain). → **KF.W4** (manifest hygiene).
- **KF-CE-31 · L-m9** — the save-position/`setValue`/restore ritual written three times, already divergent (the third omits the dance); the single `replaceContent()` seam is where KF-CE-2's cancel, KF-CE-6's `executeEdits`, and KF-CE-8's guards all live. → **EDITOR-UNIT**.
- **KF-CE-32 · L-i4 = C-16 — FOLD, not booked here**: census **F-1** (phantom `@mkbabb/glass-ui` dep) lands on line 38; identity owned by lane-frontend §10 step 1 (F-1 first; `npm ci` broken until it lands). One identity, never re-booked. → **FOLD → census F-1** (KF.W6-adjacent sequencing).
- **KF-CE-33 · corpus-missed (reader-A) — LIVE visual defect unmasked by the very count C-1 got wrong**: Dracula (the default dark theme) paints `editor.selectionBackground` and `editor.lineHighlightBackground` the SAME `#44475a` (byte-identical — measured by this seat), so selecting text on the caret's own line shows nothing; `editor.selectionHighlightBorder: #222218` is darker than the plate. These are `colors` keys — they paint TODAY, untouched by the missing tokenizer. GitHub is clean. → **EDITOR-UNIT** (theme repair), delta witness to **SS-13/KF.W9**.
- **KF-CE-34 · corpus-missed (reader-A) — the line-number gutter fails WCAG 1.4.3 NOW, not latently**: neither theme defines `editorLineNumber.foreground` (measured — 8/6 keys, absent both), so both inherit the registry's `#858585` dark default — a workbench colour, not a token rule; D-17 mis-filed this one row under "masked by D-1". → **EDITOR-UNIT**; the rest of D-17 stays INFO (KF-CE-47).
- **KF-CE-35 · corpus-missed (both readers) — the format accelerator is a macOS dead-key literal**: `e.key === "Ï"` (KeyframesStringControls.vue:90-96, my read) matches nothing on Windows/Linux, and `preventDefault` makes `Ï` untypeable in the buffer; the correct chord idiom exists in-feature (KeyframesAddDialog.vue:138-141, my read). Sharpening (this seat): the second literal at KeyframesEditor.vue:218-221 is SWALLOW-ONLY — it preventDefaults and returns without formatting, so at that level the key does nothing at all. → **EDITOR-UNIT** (feature-level keymap normalization).
- **KF-CE-36 · corpus-missed (reader-A) — a success toast fires on every pause in typing**: each 200 ms debounced emit that parses lands `toast.success("Keyframes parsed 🎉")` (my read of onEditorChange) — no dedupe, no id, no rate limit; continuous authoring = a toast storm on the flagship surface. All three axes quoted the line as latch evidence; none read it as rate. → **EDITOR-UNIT**.
- **KF-CE-37 · corpus-missed (both readers) — RibbonBar.vue:28 is a second, PRIMARY unguarded `formatCSS` rejection path** (the visible Format button, un-awaited — my read); D-6/C-5 traced only the hotkey, so a repair applied where they point leaves the main path broken. → **EDITOR-UNIT**, same boundary as KF-CE-9.
- **KF-CE-38 · corpus-missed (reader-B) — the editor's edit path bypasses the op built for it**: `updateAnimationFromKeyframesString` (1000 ms, withErrorToastAsync-wrapped, comment claiming "run on every Monaco edit" — my read of useKeyframeOps.ts:87-106) is reachable ONLY from useKeyframesParsing.ts:66 (my grep); `onEditorChange` calls raw `updateFromString`, so the heaviest op runs at 5× the intended rate without the Retry posture and the comment is false about the tree. → **EDITOR-UNIT** (wire the edit path through the guarded op, or fix the comment and the rate deliberately).
- **KF-CE-39 · corpus-missed (reader-B) — both `?worker` chunks are AWAITED in the boot's `Promise.all`, blocking first-mount on fetches monaco doesn't need at create**: `Environment.getWorker` may return `Promise<Worker>` (contract verbatim, my read), so both imports can move inside `getWorker`, off the critical path, still dynamic (the S-A property preserved). Neither original axis noticed the EDITOR worker is also serialized ahead of `create`. → **EDITOR-UNIT**.
- **KF-CE-40 · corpus-missed (reader-B) — the barrel's lazy `CSSCodeEditor` export is dead code** under a comment describing protection the tree doesn't get from it; both consumers import statically (my reads, :47/:173); the real graph protection is the three sibling lazy re-exports. → **EDITOR-UNIT** (delete or adopt — if adopted, it is where loading/error components belong, resolving KF-CE-14's site question).

### INFO
- **KF-CE-41 · L-i1 = C-18 + corpus-missed (reader-A)** — the 16-line header narrates replaced code; and `:45-48` carries a THIRD false sentence, asserting the boot configures "the `css` language" — the exact capability the BLOCKER denies — plus a second stale "4 MB". Any KF-CE-1 cure must rewrite the header (E-3: the prose is part of the defect surface). → **EDITOR-UNIT**.
- **KF-CE-42 · L-i2** — `convertPixelsToCh` + `debounce` deep-imported past the published surface, one from a directory named `internal`; the L.W8 ED-3 dogfood-inversion tension. Body folded into KF-CE-12's routing. → **KF.W8**.
- **KF-CE-43 · L-i3** — `formatEditorCSS` parses plain CSS with prettier's `scss` parser (line-exact, my read). → **EDITOR-UNIT** (one-token change riding the format-boundary work).
- **KF-CE-44 · C-19** — zero test coverage: nothing under `test/` names the component or monaco — which is why `5fe2e4cb` shipped behind a green pipeline. Reader-B's allowlist note adopted (two observe-scripts name it in console-regex only; not coverage). → **EDITOR-UNIT** born-RED gates; enabled by KF-CE-48.
- **KF-CE-45 · D-16** — the `border` prop toggles border+3-layer cartoon stamp+`position: relative`; the name describes a third of it, and the `position: relative` is what contains monaco's overlays. → **EDITOR-UNIT** (rename rides the KF-CE-17 re-framing).
- **KF-CE-46 · D-18** — `.cartoon-surface:has(:focus-visible)` fires on pointer focus here (monaco focuses a hidden textarea; text-entry matches `:focus-visible` when focused), contradicting design-idioms.css's own written "pointer focus stays quiet" contract; sole raw-class consumer. Contradiction closed on source; pixel half → SS-13. → **KF.W6** (the contract text or the rule must move).
- **KF-CE-47 · D-17 (minus the line-number row, promoted to KF-CE-34)** — latent token-contrast debt in both themes (comments 3.03/2.73:1, numerics 3.30:1), due the moment KF-CE-1 lands; plus the Δhue ≈ 207° two-plates taste question (declined honestly by D; ruled UNPROVEN-NEEDS-LIVE here). → **EDITOR-UNIT** theme repair, witnesses to **SS-13/KF.W9**.
- **KF-CE-48 · corpus-missed (reader-B) — the boot singleton is un-importable, un-testable, un-mockable at `<script setup>` module scope**; C-19's zero coverage is structural, not lazy; extracting `monacoBoot.ts` (precedent: formatEditorCSS.ts, iosTextEntry.ts) is the prerequisite that makes every KF-CE-1/10/11 gate authorable. → **EDITOR-UNIT**, early.
- **KF-CE-49 · corpus-missed (reader-B) — `getFormatWidth`'s third guard clause is redundant** (`!el.offsetWidth` subsumes `=== 0`); paired with KF-CE-26 it reads as uncertainty about a two-line helper's contract — the real cost signal of the KF-CE-12 deep import. → **EDITOR-UNIT**.

## Killed-claims register — 13 entries, refutations preserved

1. **D-9 (whole row)** — "a suggest widget DOES appear on typing even under D-1, and is clipped." DEAD PREMISE: no `SuggestController` in the `editor.api` closure (this seat's 677-module walk); `wordBasedSuggestions` is inert without a controller; no overflow-widget producer of any kind survives the import. Reader-B's re-confirmation is OVERRULED — it verified option defaults and the DOM append site, neither of which conjures a controller. Re-priced as a note pinned to KF-CE-1's cure (revives the day contributions return). Letter-correction to reader-A's "exactly two importers" recorded (bundled basic-languages/* files also carry it; immaterial).
2. **D-5's prescribed cure (glass-ui `Skeleton`)** — UNREACHABLE, not unreached: no `./skeleton` among the 73 export subpaths, zero `Skeleton` matches in `dist/index.d.ts` (both my own probes); the dist files exist behind no export condition. Census S-6's phrasing corrected; producer-side gap → BH relay. The defect (no loading state) survives whole.
3. **L §7 step 1 / C §9 step 1's cheap import cure** ("import `basic-languages/css/css.contribution` alongside `editor.api` to keep most of the saving") — THE SAVING DOES NOT SURVIVE: `_.contribution.js:1-55` (my read) imports the entire contrib set (coreCommands, codeEditorWidget, diffEditor, bracketMatching:5, comment:12, contextmenu:13, find:18, folding:19, multicursor:40, …), so that one specifier drags most of the 1.65 MB back. **Sharpened by this seat**: it would ALSO restore `toggleTabFocusMode` and the other a11y contribs — the byte question and the trap question are ONE decision, and the Monarch-only route cures highlighting but NOT KF-CE-3 (which then needs `tabFocusMode: true`). Both original byte budgets are wrong.
4. **C-1's "100 of the 100 token rules are inert"** — 98 of 100: each theme carries exactly one `{token: ""}` rule that DOES match the null token (measured by this seat). L-B1's 45/53 count was right all along. The companion "only four colors survive" undercount is what hid KF-CE-33/34.
5. **L-B1 §5's mechanism** ("no Ctrl+/, no bracket matching — all carried by the language configuration") — right conclusion, wrong cause: comment-toggle and bracket-matching are editor CONTRIBUTIONS (`_.contribution.js:12`, `:5` — my read), absent regardless of any `setLanguageConfiguration` call. The mis-attribution is the sentence that stopped all three axes at "the language is missing" instead of walking the contrib graph.
6. **D-3's ratio table and its cited source** — the 14% mix in glass-atom.css belongs to `.badge-atom--outline`; the true quiet-Card comparator is `--glass-border-quiet` = 4% foreground via ladder.css (all my own reads). Every "intended"/"overshoot" figure dies — in the direction that understates the confirmed structural defect.
7. **C-5's toastGuard adjacency** — false locator, false relevance: `utils/toastGuard.ts` lives one directory up and exports a pointer hit-test (`isInsideToaster`), not an error posture. The one real declined idiom (`withErrorToastAsync`) carries the row alone.
8. **C-3's mechanism** ("resolves through exports to editor.api's own d.ts; the seven-member delta typechecks") — the specifier is TS2307-unresolvable (this seat's `resolveModuleName` run, the decider); `m` is `any`; the conclusion survives on `:31`'s wide declared type; the larger defect it walked past is KF-CE-11.
9. **L-m5/C-17's rationale** ("required only because resolveJsonModule widens `base`… suppresses the one check") — under TS2307 the casts suppress nothing; dead ceremony until KF-CE-11 lands; both original repair orders had it mis-sequenced as independent.
10. **L §5 dropped-item #1's mechanism** ("the `:54` dynamic import pulls editor.api.d.ts into the program, typing `self.MonacoEnvironment`") — the `:54` import resolves to NOTHING; the global rides `:31`'s root type import → `editor.main.d.ts` → `./editor.api.js` types (corroborated by my resolution runs). Recorded because the wrong mechanism makes `:31` look deletable, and deleting it breaks the global.
11. **D-5 / L-M5's `keyframes/index.ts:11` sub-citation** — inert for every real mount: both consumers import the SFC statically (my reads at :47 and :173). The core defects survive; the repair site named was wrong. The wrapper's true status is KF-CE-40.
12. **D-2's "drops the accessibility-help affordance"** — monaco 0.55 ships no accessibility-help module anywhere in `esm/vs/editor` (my `find`: accessibleDiffViewer + two accessible-views only); nothing existed to drop. The row's core survives as KF-CE-5.
13. **Reader-A's keyboard-trap mechanism sentence** ("contrib/toggleTabFocusMode is the ONLY code path in monaco that can flip tab-focus mode") — KILLED BY THIS SEAT: `tabFocusMode` is a registered create-time option (EditorOption 164) and `codeEditorWidget.js:1608` seeds the context key from `options.get(164) || TabFocus.getTabFocusMode()`. The trap (KF-CE-3) stands in the shipped config; the cure is one option, no contribs.

## Superlatives — provenance preserved (L-18 runs both ways)

- **THE COMPONENT · the `?worker` re-eagerization insight, structurally enforced** (D-S1 = L-S2 = C-S-A, all three axes + both readers, ends verified): a static `?worker` import re-eagerizes `vendor-monaco`; the fix is dynamic-in-boot; `scripts/observe/demo/smoke.mjs` fails the build if the entry statically imports it. Insight + fix + gate.
- **THE COMPONENT · the three-point unmount race guard** (`disposed` + pre-await + post-await re-check of both flags) — the classic lazy-editor leak, closed on both legs, hazard named in prose.
- **THE COMPONENT · the module-scoped idempotent boot** — right shape for a 2.5 MB namespace + one env install + two theme registrations; instance state correctly NOT hoisted; missing only its `.catch` (KF-CE-10).
- **THE COMPONENT · `clampIOSNoZoomFontSize`** at the exact input Safari reads, shared not inlined, with the iPadOS desktop-UA-spoof fallback.
- **THE COMPONENT · the one-shot deferred init** — handles the zero-size mount ChannelControls' fallback actually produces; `stop()` before init; `entries[0]` guarded; scope-disposal verified by reader-B against installed Vue.
- **THE COMPONENT · L-S5, the pre/post-boot content seam** — `value: modelValue.value` read after the await; a model change during the chunk flight is not lost.
- **THE COMPONENT · the vendored-theme rationale (S-F)** — the monaco-themes exports-map diagnosis reproduced from the installed package by reader-B; vendor-two-small-files was the right call (the only miss is the dead dep, KF-CE-30).
- **THE COMPONENT · the `isSettingValue` guard is itself correct** (reader-B re-verified the synchronous-emit dependency end-to-end); every defect around it (KF-CE-2/7/8) is of what it does NOT do, never of the guard. And **no model leak**: standalone editors own implicit models and dispose them — reader-B opened it expecting a leak and cleared it, recorded so no future seat re-spends the budget.
- **THE CORPUS · axis D's documented PRM NON-claim** — verified (caret blink is the residual; not WCAG 2.2.2 content); the most trustworthy paragraph in a corpus structurally rewarded for finding things.
- **THE CORPUS · axis L's §5 "investigated and DROPPED" ledger** — all three drops re-verified true (one mechanism corrected, killed #10); publishing refuted candidates with refutations saved every downstream seat three walks.
- **THE CORPUS · the git-provenance line** — "the 1.65 MB that shrank IS the language contributions; the measurement and the defect are the same delta read with opposite signs" (L-B1). Reader-A's keyboard trap and this seat's contrib-set walk are that same delta read a third and fourth time; the sentence is the record's center of gravity.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs (route with KF.W9)

1. KF-CE-1's visual half: a DOM probe counting distinct `color` values among `.monaco-editor .view-line .mtk*` spans, or one screenshot. Source half closed.
2. KF-CE-3's live witness: a Tab-walk into and (not) out of the 450px editor, keyboard-only. Statically closed; the live capture is the WCAG record.
3. KF-CE-33's delta witness: select text on the caret's line under Dracula — no visible change (byte-identity closed; the pixel is the exhibit).
4. KF-CE-13: rendered result under `forced-colors: active` emulation.
5. KF-CE-15 + S-1: the css.worker byte cost on the boot path; per-scene chunk-graph eagerness (the smoke gate asserts the ENTRY only).
6. KF-CE-46: the shadow jump on mouse click into the buffer.
7. KF-CE-25's magnitude: the `ch ≈ 0.5em` error against Fira Code's true advance.
8. Reader-A's two new probes, adopted: whether the cartoon stamp's LEFT lobe is clipped at the pane edge (`overflow-y: auto` forcing `overflow-x` computed `auto`); whether the two vendored plates read as a deliberate second medium (Δhue ≈ 207°, contrast 1.021:1) — a taste ruling only pixels can settle.
9. Post-KF-CE-11 diagnostic (derived, unproven): once the specifier resolves, the narrower namespace should fail assignment to the wide declared return (forcing the `:52` correction) — confirm under the first vue-tsc run.

## Identity guards (anti-rename) & folds

- **KF-CE-32 = census F-1** — folded by reference; owned by lane-frontend §10 step 1. Never re-booked.
- **C-7's R1 paragraph = megatranche R1** (parser crash surface) — the `:116` fuzz-entry note folds to the R1 identity in the parser lanes (KF.W2/KF.W3); no new row.
- **Census S-6** corrected in place by killed #2 (Skeleton UNREACHABLE — producer gap → BH relay), not re-booked as a component defect.
- **Census §6.3 / §6.5 / §3.1 / lane-library §3.3 §4.6** — sharpened/extended exactly as the axes recorded; no contradiction of the census survives adjudication except S-6's verb.
- **C-9 folds into L-B2** (one stale-emit identity, two triggers); **L-m10 absorbed by L-M3's cure**; **L-i2 rides KF-CE-12's routing**; **D-17's line-number row promoted out (KF-CE-34), the remainder stays latent-INFO**.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet)**: reader-B's confirmed D-3 cell repeats the challenger's "14%-foreground hairline" while honestly listing the ratios as unproven; reader-A re-derived the source and found the badge-rule misattribution. This seat's own token reads settle it for A. Half-inherited cells inside otherwise-verified rows remain detectable; the epoch rule stands.
2. **Reader-B's D-9 re-confirmation** is the return's one verdict this seat overruled outright — it re-verified true facts (option defaults, the append site) that do not compose into the claimed behavior. Preserved as a method lesson: defaults are not capabilities; a widget needs its controller.
3. **Severity ordering, unresolved and preserved**: reader-A ranks the keyboard trap above the monochrome regression ("the a11y half outranks the highlighting half"); the EDITOR-UNIT cures both in one decision, so no ruling is required — but the spec author should read the trap first.

## Closing verdict

**ADJUDICATED — RED.** 49 rows stand (3 BLOCKER · 14 MAJOR · 23 MINOR · 9 INFO); 13 claims killed with refutations; 14 corpus-missed defects adjudicated in, two of them verified here to BLOCKER/MAJOR weight. One root cause — the one-line `5fe2e4cb` specifier swap, measured only in the direction that flattered it — accounts for both remaining capability BLOCKERs, the a11y trap, the dead worker, the unresolvable type specifier, and the inert themes; one structural absence — no `.vue` file typechecked anywhere — is why every one of them shipped green. Routings: the component's repair coheres as ONE proposed wave-surface (**EDITOR-UNIT, NO-WAVE-OWNER** — SS-1/SS-2 spec authoring to consume, sequenced after the KF.W4 vue-tsc gate), with discrete rows to **KF.W4** (type/manifest/depcruise gates), **KF.W6** (Surface, radius, `--kf-*` tokens, focus-contract), **KF.W7** (the timeline parent's emit-time targeting), **KF.W8** (deep-import/publish-surface settle), **KF.W2/W3** (the R1 ingress note, by fold), and **KF.W9/SS-13** (nine live witnesses); one producer relay (glass-ui Skeleton export) rides the standing BH channel. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.

— end of apotheosis. The three challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.
