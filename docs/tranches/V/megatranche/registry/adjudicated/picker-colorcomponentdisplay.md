# picker-colorcomponentdisplay — adjudicated apotheosis · seat model receipt: `claude-fable-5` (Fable 5, observed in this seat's own system prompt — M-23/L-18)

**Seat**: fresh Fable adjudicator (the incredulity seat) · **Date**: 2026-08-07 · branch `tranche-u`.
**Corpus**: 17 accusation rows (C-1..C-6, D-1..D-6, L-1..L-5) against `demo/picker/display/ColorComponentDisplay/ColorComponentDisplay.vue` (214 lines, SHA-256 `4e14149252…b24466` — re-hashed by this seat, byte-identical to all three challenge receipts) + its reservation module `readoutReservation.ts`.
**Re-readers adjudicated**: design-and-usage · library-and-code (both hostile re-reads of the full corpus).
**Method**: both returns were attacked before either was adopted. Every verdict-level disagreement (C-6, D-4, D-6, L-2/L-3 severity, D-1 severity, L-4 severity) was re-derived against the tree by this seat. The BLOCKER and every missed-MAJOR was re-traced from source, not trusted: this seat independently read `ColorComponentDisplay.vue` (whole) + `readoutReservation.ts` (whole), `useSliderGradients.ts:58-92`, `useColorPipeline.ts:55-122,180-209`, `picker-color.ts:40-217`, `valueDomain.ts:1-50`, `color-model.ts:25-79`, `ColorPicker.vue:30-73,215-274,340-386`, `ComponentSliders.vue:70-105,160-180`, `demo/shared/utils.ts:18-45`, `demo/styles/foundation.css:690-725`, `demo/styles/focus-ring.css:25-35`, `demo/DESIGN.md:30-85`, `o27-focus-affordance.spec.ts:206-228`, `o18-contrast-census.spec.ts:870-895`, installed glass-ui 7.0.0 (`dist/components/number-field/` roster + `NumberField.vue.d.ts` prop grep; `card-*.js` `__name:"CardTitle" … as:{default:"h3"}`), the glass-ui `scale.css` `--type-display-4` token, and repo greps (readoutReservation consumers = exactly 2 SFCs + valueDomain's comment; `ComponentFormat` importers = 0; e2e coverage of the module's exports = 0, only `--readout-lines` reads at o10:300 / readout-seam:67; no `html{font-size}`/`text-size-adjust` lock anywhere in `demo/styles/`). All packing arithmetic in this record (oklch 12.85ch / oklab 12.15 / lab 16.15 / ictcp 10.15 / jzazbz 10.15 / rgb-family 10.5 / srgb-linear-family 8.85 / kelvin 6.45 / hex 9 against the 11.7 budget; fits 0.9105 / 0.9630 / 0.7245 against the 0.97 floor) is this seat's own re-computation from `PICKER_CHANNELS` + `chOf`, not either reader's.
**Motion law**: challenge-D's Target-gestalt reduced-motion tail is quarantined **FALSE (M-23)** and is NOT carried; M-21 (digit-boundary movement = deliberate trade) and M-22 (zero motion declarations; "no mode transition" true in the non-motion sense) are the TRUE rows cited below. No FALSE motion row rides in this record.
**Status discipline**: every disposition below is ADJUDICATED, not VERIFIED — nothing here stamps a wave.

## Scoreboard

**11 CONFIRMED (7 MAJOR · 4 MINOR, one of them rescoped) · 3 KILLED (C-5, D-4, D-5; plus L-1's path half and C-6-as-stated folded) · 16 MISSED rows adjudicated in (1 BLOCKER · 5 MAJOR · 6 MINOR · 4 INFO).**

Blocker-weight survivor — and the corpus's indictment: **all three axes discussed this component's edit contract at length and none traced a single value through it.** Both hostile re-readers found the same BLOCKER independently, with the same trace, and it survived this seat's own re-derivation: **committing the readout's own displayed number in the DEFAULT space snaps the color to white** (M-1 below).

## Ruled disagreements (the adjudication's actual work)

| row | reader A (design-and-usage) | reader B (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C-6** | KILLED as stated (framing misleads; clause ii duplicates C-1); coarseness promoted to missed-MAJOR | CONFIRMED MAJOR (the focused-DOM overwrite, guaranteed via the clamp) | **FOLDED — neither label survives.** Clause (i) ("precision itself is bounded" as mitigation) is refuted as framing: the bound is the defect (→ M-4). Clause (ii) is real and proven — but it is C-1's commit-contract identity wearing a second id; under the anti-rename identity guard it books ONCE, inside C-1. B's mechanism (the 500ms clamp-divergent rewrite is guaranteed, not possible) is adopted INTO C-1's proof. | `challenge-C-implementation.md:14` is one sentence carrying two claims joined by "however" — the axis authored two rows as one. `useColorPipeline.ts:209` + `useSliderGradients.ts:63-80` prove the rewrite; `readoutReservation.ts:62-64` proves the bound. One defect identity each, no third. |
| **D-4** | KILLED (both stated mechanisms wrong) | CONFIRMED MINOR (pivoted rationale: unit exclusion load-bearing; unit span un-hidden) | **KILLED as stated — A wins.** Source order at `ColorComponentDisplay.vue:36-42` is number-then-unit = the conceptual order; excluding the unit from the editable host is CORRECT (including it would force every commit through unit-stripping). B's "confirmation" concedes both stated mechanisms and substitutes new ones — that is a rescope mislabeled as a confirmation, and the substituted content (unit absent from the accessible value; announced as loose text) already books in M-6. | My read of `:39-42`: `.fig-unit` is the immediate NEXT sibling of the textbox; nothing is transposed. `.fig-comma` carries `aria-hidden` (`:45`), `.fig-unit` does not — M-6's cell, not D-4's. |
| **D-6** | CONFIRMED MINOR (default-space arithmetic: 2-line lock, 1 painted line) | KILLED ("short tuples" refuted by the per-space derivation; oklch's 2-line lock honest) | **RESCOPED → CONFIRMED (MINOR), mechanism replaced.** Both are right about different sentences. The claim's stated mechanism ("short tuples") is wrong — `readoutLineCount` derives from the actual components, short tuples derive 1 (B's kill holds on the letter). But the reserved-air fact is real by width, not by tuple length: the lock is WORST-CASE derived while typical values paint narrower. | My arithmetic: oklch (the DEFAULT space, `color-model.ts:44`) worst-packs 4.45+0.75+2.45+0.75+4.45 = **12.85ch** > 11.7 → fit 0.9105 < 0.97 → lever 2 declines → **2-line lock**; a typical tuple (`62.3 0.2 45.1`) packs **10.85ch** → paints ONE line. A full display-scale reserved line sits above the numbers on first paint, permanently. It is deliberately routed as title air (`align-content: flex-end`, `:152-166`, owner lineage T-33b) — hence MINOR, taste residual to the visual audit. |
| **L-2/L-3 severity** | MAJOR (merged) | MINOR (split ×2) | **MAJOR, merged — one identity.** Ruled on program precedent: the ColorSpaceSelector apotheosis confirmed its typed-boundary erasure (`colorSpace: any`, L-12) MAJOR; this is the same class with a sharper silent-failure cell. B's fact that the tuple's second element is never read (template destructures `([component], ix)` at `:18`, so the `any` buys nothing) is adopted. | `ColorComponentDisplay.vue:67-72` (`[string, any][]`, `Record<string, …>`, `space: string`); `readoutReservation.ts:42` imports `PickerSpace` then widens every export back to `string`; `readoutCh:117-120` resolves an unknown space to `HEX_CH` 9 **silently** — pricing every cell at the widest single-cell format, manufacturing a spurious reserved line with zero diagnostic, in the module whose whole purpose is a static guarantee. `PICKER_CHANNELS` is already `satisfies Record<SpaceId,…>` — the typed contract costs nothing. |
| **D-1 severity** | MAJOR | MINOR (UA I-beam exists) | **MAJOR — A wins.** The readout is the picker's typographic hero (47–86px Fraunces, the page's most decorative type) and its ONLY edit affordance is the UA I-beam — invisible until hover, non-existent on touch. Editing is a primary capability of the instrument; its discoverability is zero by construction. B's caveat demotes only the "no caret treatment" sub-clause (UA default caret exists), adopted as a correction to the axis prose, not to the severity. | The sole state-bearing rule in the scoped block is `.readout-fig:focus-visible` (`:178-183`); no `:hover`, no border, no cursor declaration, no instruction, no mode. M-22 (TRUE) independently confirms zero transition/animation declarations. |
| **L-4 severity** | MAJOR (producer-primitive bypass; glass-ui-first law) | MINOR (cure caveat: NumberField renders `<input>`, cannot host the fig spans) | **MAJOR, with B's caveat adopted as a CURE ruling.** The defect is the bypass, and the replacement ships today: this seat's read of installed glass-ui 7.0.0 confirms the full `number-field/` roster (`NumberField/-Input/-Increment/-Decrement/-Content` + `styles.css`) declaring `min/max/step/formatOptions/disabled/readonly/invalid`. Per the MT-CSS-A law ("the cure is adjudicated separately from the defect"): a naive swap would destroy the int/frac instrument — the cure is only sound in the ColorReadout/ColorComponentEditor split form. Bonus: a producer `<input>` matches the WHCM roster's `input` selector, so M-2 dies structurally with this cure. | `dist/components/number-field/` ls + d.ts grep, this seat's own. |
| **L-1 path half** | KILLED (one-hop barrel) | "Deep forwarding path" overstated (same finding, kept inside CONFIRMED) | **KILLED — unanimous in substance.** `demo/ui/card/index.ts` is ONE line re-exporting glass-ui; the axis read the importer's own nesting depth (`../../../ui/card`) as forwarding depth. The heading half is confirmed separately below. | My read of the barrel file (verbatim single `export … from "@mkbabb/glass-ui"`). |

## Adjudicated defect roster — confirmed corpus rows

Terminal dispositions route to the tranche-X waves per `registry/CARRY-CUT-LEDGER.md` (X-W4 = semantic controls, the CC-003 glass-independent partition; X-W8 = graph subtraction; X-W10 = canonical design law; X-W1 = re-gate).

- **C-1 (+ C-6 clause ii, folded) — CONFIRMED (MAJOR) → X-W4.** Browser-mutated contenteditable DOM and Vue-owned nested text interpolations have no commit/reconciliation contract. Both directions proven static: (fwd) the 500ms debounce boundary lands the model, `currentColorComponentsFormatted` recomputes, Vue re-runs `setElementText` inside the focused editing host — GUARANTEED divergent because the committed value clamps (see M-1), so the caret's ground is rewritten mid-edit; (conv) input that produces no model change (NaN drop at `ColorPicker.vue:233`, or emptying a cell) leaves the DOM permanently disagreeing with state — no blur/reset/`:key` bump exists (`:key="component"` is invariant while editing). Crash arm (v-if anchor detachment → insertBefore DOMException) UNPROVEN-NEEDS-LIVE.
- **C-2 — CONFIRMED (MAJOR) → X-W4.** The sole handler reads `innerText.trim()` and emits per input event (`:30-35`); zero `keydown/beforeinput/composition*/paste/blur` bindings in 214 lines. Enter inserts a `<div>`/`<br>` (plain `contenteditable="true"`, not `plaintext-only`), so `"12\n.5"` parseFloats to 12 and the fraction silently drops. The Enter/Escape handler at `ColorPicker.vue:249-261` is gated on the PALETTE edit-target machine (`isEditing`), null during readout editing — verified.
- **C-3 — CONFIRMED (MINOR) → X-W4.** `:22-25` carries only `contenteditable`/`role`/`:aria-label`/`:class`/`@input`. Missing: `inputmode` (mobile QWERTY on a decimal instrument), `spellcheck="false"`, `aria-describedby` (the range prose already exists unused — `currentColorRanges`, `useSliderGradients.ts:82-88`). Adopted from A: the `aria-invalid` sub-claim is a consequence, not an independent defect (no validation state exists to expose). Adopted from B: `role="textbox"` without `aria-multiline` asserts single-line while the element accepts newlines — the ARIA contract falsified by the element's own behavior; the labels interpolate RAW channel keys ("ct component value").
- **C-4 (= L-5, one identity) — CONFIRMED (MINOR) → X-W4.** `defineEmits` declares `update: [value: number, component: string]` (`:92-95`); the only `emit(` call is `input` (`:33`). Worse than either axis: the consumer BINDS the dead event — `ColorPicker.vue:51` `@update="(v,c) => updateColorComponentDebounced(v,c)"` is permanently cold wiring one line above the live `@input`. Corroborated independently at `audit/codex-provenance/axes-quality.md:150-151`.
- **D-1 — CONFIRMED (MAJOR, ruled over B's MINOR) → X-W4.** No edit affordance on the page's least-input-like element (ruling above; M-22 TRUE cited).
- **D-2 — CONFIRMED (MAJOR) → X-W4.** Four silent outcome paths, zero painted state: PARSED (`ColorPicker.vue:232`), REJECTED (NaN drop `:233`; hex regex drop `:227`), CLAMPED (`useColorPipeline.ts:67` → `clampPickerColor`, `picker-color.ts:195-204`), COMMITTED (500ms later). Plus `useColorPipeline.ts:195`'s non-finite drop — a fifth silent site.
- **D-3 — CONFIRMED (MINOR) → X-W4.** No `inputmode`, no mobile editor arm anywhere in the scoped block; the phone band is explicitly in the component's design scope (`readoutReservation.ts:142-147` derives a dedicated 390 arm). The 200%-zoom half is settled statically and MORE severely by M-5; the residual keyboard ergonomics ride UNPROVEN.
- **D-6 — RESCOPED → CONFIRMED (MINOR); taste residual → visual audit + X-W10.** Ruling above: mechanism-as-stated (short tuples) killed; the default-space worst-case-vs-typical reserved line stands by this seat's arithmetic; deliberately routed as designed air.
- **L-1 (heading half) — CONFIRMED (MAJOR) → X-W4.** `CardTitle` defaults `as: "h3"` (verified in the installed producer bundle); no `:as` passed → the readout renders an `<h3>` containing three `role="textbox"` contenteditable descendants. Grep across `ColorPicker.vue`/`ComponentSliders.vue`/`ConsoleRail.vue`: this is the picker card's ONLY heading. Adopted from A: the minimal cure is `:as="'div'"` + a real heading elsewhere, not the axis's component split (cure adjudicated separately). Path half KILLED (ruling above).
- **L-2 + L-3 (merged, one identity) — CONFIRMED (MAJOR, precedent-ruled) → X-W4.** Untyped contract + the silent `HEX_CH` fallback (ruling above).
- **L-4 — CONFIRMED (MAJOR) → X-W4, cure in split form only; note the M-2 structural discharge.** Producer-primitive bypass; NumberField ships in installed 7.0.0 (ruling above).

## Missed rows — adjudicated in (the corpus's failure surface)

- **M-1 — BLOCKER — the display scale and the edit scale disagree by 100×: committing the readout's OWN displayed number in the DEFAULT space snaps the color to white → X-W4 (glass-independent; must not wait on any Glass-8 trigger).** Found independently by BOTH re-readers with the same trace; re-derived whole by this seat. Display: `useSliderGradients.ts:73` `meta.unit === "%" && meta.max <= 1 ? value * 100 : value` — oklch l=0.623 inks `62.3`. Commit: `ColorPicker.vue:232-233` `parseFloat(text)` → `updateColorComponentDebounced(v, component)` → `updateColorComponent(value, component, normalized=false)` (`useColorPipeline.ts:190-200`) → `withChannel` writes RAW (`picker-color.ts:165-176`) → the write gate clamps: `Math.min(meta.max=1, 62.3)` = 1 (`picker-color.ts:200` via `updateModel:67`). Touching the L cell and re-committing what it already shows drives L to 100%. The asymmetry inside the very same function proves oversight, not convention: the alpha branch DOES divide by 100 when unnormalized (`:197`), and the slider path passes `normalized=true` (`ComponentSliders.vue:78`) — the readout is the only unnormalized writer and the broken one. Affected: every `unit==="%" && max<=1` channel — hsl.s/.l, hsv.s/.v, hwb.w/.b, oklab.l, oklch.l, xyz.x/.y/.z = **11 channels across 6 spaces, including the landing state** (`DEFAULT_COLOR_SPACE = "oklch"`). lab.l/lch.l safe only because declared max=100. Corroborating irony: the reservation module itself carries the ×100 convention correctly (`readoutReservation.ts:98` `scale = meta.unit === "%" && meta.max <= 1 ? 100 : 1`) — the display, the reservation, and the ranges all agree; only the commit path forgot.
- **M-2 — MAJOR — zero focus affordance in forced-colors/WHCM on the editable cells → X-W4 (dies structurally with L-4's producer `<input>`, which the roster's `input` selector matches; interim = one roster line) + X-W1 (o27 extension: the forced-colors oracle reaches only the gradient rail handle — verified at `o27-focus-affordance.spec.ts:206-228`).** `:178-183` sets `outline: none` + `box-shadow: var(--focus-ring-shadow)`; `focus-ring.css:31` states the law ("box-shadow is stripped in WHCM"); the restoration roster (`foundation.css:698-720`, re-read whole by this seat) contains neither `[role="textbox"]` nor `[contenteditable]`, and the cells carry no `tabindex` so no arm matches. Even on a match, the roster is deliberately `:where()` specificity-0 while `.readout-fig:focus-visible` is (0,2,0) and wins with `outline: none`. WCAG 2.4.7, statically decidable — the axes' "unproven" hedge (D-5) was avoidable and is itself a finding about the corpus. Residual UA-default-outline masking per engine rides UNPROVEN.
- **M-3 — MAJOR — uncaught `PickerColorError` from a bare timer on a space change inside the 500ms debounce window → X-W4.** `debounce` is a bare trailing-edge `setTimeout`, no try/catch (`demo/shared/utils.ts:28-34`); `updateColorComponent` never validates `component` against the CURRENT space; `withChannel` throws on unknown key (`picker-color.ts:171-172`). Sequence: type in rgb's `r` cell → switch space within 500ms (Cmd+K is bound, `ColorPicker.vue:263-266`; the pre-flush watcher `:348-354` converts the color) → timer fires with stale `"r"` → `findIndex` −1 → throw, outside Vue's errorHandler, edit silently lost. `.cancel()` is called in `onUnmounted` only (`:384`), never in the space watcher — the one place it matters.
- **M-4 — MAJOR — the per-space least count makes the readout near-useless as an instrument for the normalized spaces, and `readoutDecimals` structurally cannot fix it: it discards the `_component` argument its own doctrine demands → X-W10 (the format law; ripples the reservation table).** `readoutReservation.ts:62-64` accepts and throws away the component; the module's own principle (`:32-34`: "a meter's least count is per-QUANTITY") is per-channel, the implementation per-space. Measured cost at d=1, this seat's own derivation from `PICKER_CHANNELS` (NB: these channels are `unit("")`, so NO ×100 display rescue applies): srgb-linear/display-p3/a98-rgb/prophoto-rgb/rec2020 r,g,b over 0..1 → **11 distinguishable readout values per channel**; oklab a,b (±0.4) → 9; oklch c (0..0.5) → 6; ictcp ct,cp (±0.5) → 11; jzazbz jz (0..0.222) → **3** (`0.0/0.1/0.2`). Compounds the edit path: the cell shows `0.4` for a true 0.4392, so re-committing the displayed number silently quantizes the color even where M-1 doesn't bite. Fix is free: ranges already in `PICKER_CHANNELS`; `readoutCh` already takes the component.
- **M-5 — MAJOR — the 11.7ch capacity constant is valid only while font ∝ container width; the `2.618rem` floor breaks the proportionality at raised root font-size, the reservation under-reserves, and the NORMATIVE card-lock law is breached → X-W10 (+ X-W1 zoom gate).** Reader A's derivation, independently re-verified by this seat: no `html{font-size}`/`text-size-adjust` lock exists in `demo/styles/` (my grep); `--type-display-4: clamp(3.33rem, 2.5rem + 4vw, 5.382rem)` (my read of the installed scale.css). At 24px root / 400px pane: `2.618rem`=62.8px beats `11.65cqi`=46.6px, ch = 0.67em = 42.1px, capacity = (400−36)/42.1 ≈ **8.65ch** — rgb's 10.5ch tuple paints TWO lines inside a ONE-line reservation; the `h-fit` CardTitle grows past min-height and the card rect moves on digit crossings — precisely the failure the module exists to prevent, and its own derivation comment names the proportionality as the premise (`:136-141`). At default 16px root the derivation holds exactly as documented (11.79ch band / 11.73 phone) — the breach is the raised-root band edge. WCAG 1.4.4 + self-declared NORMATIVE law breach.
- **M-6 — MAJOR — the card's only heading takes its accessible name from labelled descendants, announcing label boilerplate instead of the color; the unit is excluded from every textbox's accessible value → X-W4 (with L-1).** Per accname step 2F the `<h3>`'s name traverses each descendant's own computed name, so the labelled textboxes contribute "l component value / c component value / h component value". Labels are raw channel keys (lab's "b" vs rgb's "b" — same announcement, different quantities), no space name, no unit, no range — while `PICKER_SPACE_NAMES` and `currentColorRanges` both exist unpassed. The `%`/`deg`/`K` sibling sits OUTSIDE the labelled textbox, so the control's value announces unitless. The `aria-hidden` commas (`:45`) are correct and stay. Exact AT interleavings ride UNPROVEN.
- **M-7 — MINOR — Q11b lever 2 is dead code: `readoutFit` cannot return < 1 for any space in the catalog → X-W10 (revives or deletes with M-4's re-derivation).** This seat's own full-table evaluation: every space packs ≤ 11.7 (returns 1 at `:180`) except oklch 12.85 (fit 0.9105), oklab 12.15 (0.9630), lab 16.15 (0.7245) — each below the 0.97 floor, returning 1 at `:182`. So `--readout-fit` is permanently 1, the `:15` binding, the `:85-90` computed, the `:138-139` multiply, and `readoutLineCount`'s `:194` early-return are all unreachable. The docstrings' named beneficiary is falsified: ictcp packs 10.15 with 1.55ch slack ("the ictcp-class overhang", `:83-84`, `:156-158`, `:176-177` — three stale citations). Nearest miss oklab at 3.70% over vs the 3% floor. Zero oracle coverage (my grep: no `readout-fit`/`readoutFit` in e2e/ or test/).
- **M-8 — MINOR — `kelvin` missing from `INTEGER_LEAST_COUNT`: colour temperature inks `6500.0 K` → X-W10.** `:50-57` lists rgb/hsl/hsv/hwb/lch/xyz; kelvin absent → d=1. The channel is 1000..40000 K stored physical (`valueDomain.ts` kelvin exception, encoded); integer-native by the module's own lever-1 rationale, and `lab` is the only documented deliberate omission (`:48`). Lock unaffected (6.45ch) — cosmetic doctrine violation.
- **M-9 — MINOR — the NORMATIVE card-lock law in `demo/DESIGN.md` mandates the retired mechanism, prohibits the shipped one, and mis-cites the module three ways; AND the module's own header carries the same stale provenance → X-W10.** Verified by this seat: DESIGN.md's clause 2 mandates per-cell `min-width` ch reservation (retired at R4; zero `min-width` in the SFC) and ends "No `flex-wrap` on a locked readout row" while `:14` ships `flex-wrap` LOAD-BEARING (multi-line lock + `align-content: flex-end` applies only to multi-line containers); "a 2-line block lock" superseded by per-space counts; the cited path `color-picker/display/…` is dead (live: `picker/display/…`; `demo/color-picker/` has no `display/` — my ls); "derived from `COLOR_SPACE_RANGES`" is stale (live import: `PICKER_CHANNELS`, `:42`); DESIGN.md §fonts assigns the readout to Fira Code while the live readout is `font-display` Fraunces except hex. **This seat's own seventh drift**: `readoutReservation.ts:20-21` — the module's OWN header — still claims derivation "from the library's own `COLOR_SPACE_RANGES` + `COLOR_SPACE_DENORM_UNITS`" while line 42 imports `PICKER_CHANNELS`. A normative doc contradicting the shipped component in seven places is a governance defect.
- **M-10 — MINOR — `readoutReservation.ts` is a cross-boundary dependency colocated in one consumer's private folder, with zero unit coverage for 209 lines of hand-derived arithmetic → X-W8 (module home) + X-W1 (unit oracle; the sibling `valueDomain.ts` names its own oracle, so the house standard exists and this module sits outside it — which is exactly why M-7 survived).** `ComponentSliders.vue:100` reaches `../../display/ColorComponentDisplay/readoutReservation`; my grep: no test/e2e hit on any export.
- **M-11 — MINOR — `figParts`' format core is duplicated verbatim in ComponentSliders, and the two copies feed `readoutDecimals` DIFFERENT space identities → X-W8 (the natural third export of the module).** `ColorComponentDisplay.vue:108-111` vs `ComponentSliders.vue:169-172` (line-for-line: toFixed(d) + the negative-zero strip); the display passes its `space` prop = `model.selectedColorSpace` (a `DisplayColorSpace`, possibly `"hex"`), the sliders pass `currentColorSpace.value` (resolved `PickerSpace`). They agree today only because hex short-circuits as a string — an untyped coincidence the `space: string` prop (L-2/L-3) makes invisible.
- **M-12 — MINOR — hex mode swaps the cell to Fira Code mid-derivation: the ch capacity is minted for Fraunces tabular and nothing records that `.fira-code` invalidates it → X-W10 (document the exception or derive per-face).** `:26-29` applies `fira-code` for `monospace` formats (hex only, `useSliderGradients.ts:67`); `READOUT_LINE_CAPACITY_CH` and the 0.67em cell are Fraunces-minted (`:136-141`). Safe today only by margin (HEX_CH 9 vs 11.7, and Fira Code's advance is narrower). **This seat's correction to reader A**: the gap-mismatch clause is MOOT — hex mode has exactly one cell (`[["hex", 0]]`), so no inter-cell gap ever paints; the surviving defect is the undocumented cross-face ch-budget exception only.
- **M-13 — INFO — render-path churn: `figParts()` invoked three times per cell per render (`:36,:37,:39`; toFixed + parseFloat + regex + allocation each, no memo); the inline arrow in `v-for` defeats handler caching → rider on X-W4's rebuild.**
- **M-14 — INFO — three ungrouped natively-focusable tab stops in the card header duplicating the sliders below, no `role="group"`/`fieldset`/`aria-roledescription` — traversal cost, distinct from L-1's semantics → rider on X-W4.**
- **M-15 — INFO — dead exports and dead data → X-W8.** `READOUT_CH`/`readoutCh` have no external importer (my grep; `readoutCh` internal-only via `packWidth`); the synthesized `alpha` row (`:95-96`) is unreachable — `colorComponents` never contains "alpha", and the one caller that passes "alpha" (`ComponentSliders` meterText) reaches only `readoutDecimals`, which discards the argument; `ComponentFormat` exported, zero importers; `readoutDecimals`' docstring advertises per-(space,component) while implementing per-space (the M-4 signature cell).
- **M-16 — INFO — hygiene outliers → X-W8.** `(e.target as any).innerText` (`:32`) — one of exactly two `as any` in the picker subtree; `(e.currentTarget as HTMLElement)` is both typed and the guaranteed-correct binding. `focus-visible:outline-none` on the CardTitle root (`:14`) — an `<h3>` with no tabindex, can never match; the live rule is `:178`.

## Killed-claims register

- **C-5 — KILLED (both arms).** Duplicates: `colorComponents` derives solely from the `Object.freeze`d `PICKER_CHANNELS` literal (pairwise-distinct keys, `satisfies Record<SpaceId,…>` — my read `picker-color.ts:52-70`) or the singleton `[["hex",0]]`; no user/network path constructs it. Missing entries: the reconciling watcher (`ColorPicker.vue:348-354`) is a default `flush:'pre'` watcher in the PARENT, running `updateToColorSpace` synchronously before the child renders — no torn frame observable. Retained as INFO: the guard is flush ordering, not construction; the external-write seam (`patchModelExternal`) rides UNPROVEN.
- **C-6 as stated — FOLDED, not separately booked** (ruling table). Clause i refuted as framing → M-4; clause ii = C-1's identity, adopted into its proof.
- **D-4 — KILLED** (ruling table). Reading order correct; unit exclusion correct; residue → M-6.
- **D-5 — KILLED (both clauses), with the forced-colors sub-clause PROMOTED, not parked.** "Color alone demotes": false on the file's face — `.fig-int` 600 vs `.fig-frac` 300 (weight carries the demotion beside color); `.fig-unit` adds small-caps + tracking + 0.55em + italic; the file's own law at `:185-194` ("weight is rhythm, not contrast; alpha dies"). "Contrast unproven": refuted by a SHIPPED oracle — `o18-contrast-census.spec.ts:880-895` censuses `.fig-frac/.fig-unit/.fig-comma` ≥ 4.5:1 against the live surface, both schemes (my read). "Forced-colors unproven": the verdict is not unproven, it is FAILS, determinately — M-2. An axis that parks a statically-decidable WCAG failure as "unproven" lets it through three challenge files; recorded as a corpus-method defect.
- **L-1 path half — KILLED** (ruling table). One-hop barrel, repo-standard.

## Superlatives — with provenance (both readers concur; this seat's re-reads confirm)

- **The paint/derivation identity**: `column-gap: 0.75ch` in CSS (`:146`) and `READOUT_GAP_CH = 0.75` in TS (`:161`) declared as the same quantity on purpose, reason written down (`:142-152`) — structurally incapable of the drift most reservation schemes suffer. *This seat's rider*: the identity carries two unstated exceptions now on record — hex mode's face swap (M-12) and the rem-floor band edge (M-5). A superlative with a documented boundary is still a superlative.
- **Derivation over measurement**: the entire worst-case ch table built at module scope from `PICKER_CHANNELS` bounds — no ResizeObserver, no getBoundingClientRect, no nudged px — with the packing ratios taken from the SHIPPED FONT ARTIFACT's own measured advances (`DOT_CH 0.45`, `SIGN_CH 0.65`, provenance recorded `:66-76`, including the retired 1ch approximation and what it over-reserved). Instrument engineering, not styling.
- **The write-gate clamp is a "true by construction" claim that actually is**: one gate (`useColorPipeline.ts:59-71`), hue WRAPS rather than clamps (`picker-color.ts:199` — clamping would repaint), the kelvin physical-units exception ENCODED not sniffed (`valueDomain.ts`), oracle-tested (`test/value-domain-clamp.test.ts`). *The bitter rider, this seat's*: the same silent clamp is what converts M-1's scale miss into snap-to-white — a guard that swallows its inputs amplifies an upstream bug invisibly. The clamp is correct; its silence is D-2's.
- **`align-content: flex-end` (`:166`)**: one property converting the reserved-minus-painted delta from a dead band below into designed air above, killing a measured 61px gap while min-height stays bit-for-bit — the comment states the defect killed, the invariant held, and what is left to taste (`:152-165`). M-21 (TRUE) confirms the digit-boundary movement beside it is a recorded deliberate trade.
- **The guard-then-alpha cure (`:185-201`)**: demotion moved off uncertifiable opacity-over-live-tint onto the floor-clamped `--ink-muted`, weight kept as rhythm — and ASSERTED by o18's census, which is what killed D-5.
- **Negative zero never inks, in the general form**: `Number.parseFloat(s) === 0` after `toFixed` catches `-0.0` as well as `-0` (`:111`).
- **AOM hygiene unasked**: the commas are `aria-hidden` (`:43-46`).
- **The per-space lock replaced the blanket 2 and recorded why** (`:148-151`; `readoutReservation.ts:122-127`) — the harder choice, with the cured pathology named.
- **Whitespace-sensitive template authoring** (`:36-46`): `</span><span` with no gap — deliberately ugly, load-bearing twice (contiguous paint; no stray spaces in the `innerText` read), and commented so no one tidies it into a break.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. Whether CSS `ch` resolves against the tabular '0' advance (0.67em mint) or the proportional default per engine — the 11.7 derivation's premise (`:136-141`); one `getComputedStyle` probe on Safari. Failure direction is safe (over-reserve) but the paint/derivation identity would no longer hold exactly.
2. Exact AT announcements of the `<h3>` name and the unit-sibling interleaving (NVDA/JAWS/VoiceOver) — M-6's structural half is spec-determined; the surfaced strings are not.
3. The C-1 crash arm: whether select-all-then-replace detaches the `.fig-int`/`.fig-frac` elements (permanent orphan writes / insertBefore NotFoundError on an integer→decimal space transition that preserves `:key`, e.g. rgb→srgb-linear keeping r/g/b while d goes 0→1). Severity-determining for C-1.
4. Caret position after Vue's textContent patch of the focused host, per engine (the rewrite itself is proven).
5. IME/composition: no handlers is proven; per-keystroke partial emits are engine-dependent.
6. Focus-ring contrast in the NORMAL register: the 30%-alpha `--focus-ring-shadow` ring over the live-tinted translucent plate vs the 3:1 non-text floor — a measurement, not a read. (Forced-colors is NOT here — it fails determinately, M-2.)
7. Whether any engine's UA default outline on contenteditable masks M-2 in practice.
8. Mobile keyboard ergonomics of a display-rung textbox (the missing `inputmode` is proven; the felt cost is not). The 200%-text half is now settled statically by M-5's arithmetic.
9. Torn `colorComponents`/`formatted` key sets via the external-write seam (`patchModelExternal` / `useColorUrl`) — static analysis says unreachable; the guard is flush ordering, not construction.
10. The D-6 taste residual: how the default-space reserved line reads as air at 32rem and 390 (the o10 gate viewports).

## DISSENT — preserved, not ruled away

1. **The corpus's method failure is the headline finding**: three hostile axes wrote 17 rows about this component's edit surface and none traced one value through the commit path; both re-readers found the BLOCKER in their missed sections. An audit that discusses a contract at length without executing it once is measuring prose, not behavior. (Same class as the D-5 "unproven" hedge on a statically-decidable WCAG failure.)
2. **Reader B mislabeled two rescopes as confirmations** (C-6, D-4): in each, B conceded the stated mechanism and substituted a better one under the same id. The substituted content was real (adopted into C-1 and M-6) — but a verdict label that contradicts its own proof cell is exactly how citation-inheritance propagates. Axis rows containing an embedded "however" are two claims and must be split at challenge time.
3. **Reader A's sole unshared missed-MAJOR (M-5, the rem-floor breach) survived this seat's re-derivation intact**, including the token value and the absence of a root font lock — recorded as evidence that the single-reader rows were independently derived, not padded.
4. **Severity orderings ruled, positions preserved**: B held D-1 and L-4 at MINOR (UA affordances exist; cure is hard) — ruled MAJOR here on capability-discoverability and the glass-ui-first law respectively. A held L-2/L-3 at MAJOR — sustained on the ColorSpaceSelector L-12 precedent over B's split MINORs.
5. **This seat's own corrections to the readers**: A's hex gap-mismatch clause is moot (single cell — M-12); A's "aria-invalid missing" demotion is adopted over the C-axis's flat listing; the module's own stale `COLOR_SPACE_RANGES` header (M-9's seventh drift) was caught by neither reader.

## Closing verdict

**SOURCE-RED, blocker-weight — adjudicated, not verified.** The reservation/typography half of this component is genuinely first-rate instrument engineering with written-down invariants; the editing half is a facade — un-affordanced, un-validated, un-reconciled, and in the default space actively destructive (M-1: committing the displayed number whitens the color). Terminal routing: the edit-contract cluster (M-1, M-2, M-3, C-1, C-2, C-3, C-4, D-1, D-2, D-3, L-1, L-2/L-3, L-4, M-6, M-13, M-14) → **X-W4**; the format/derivation-law cluster (M-4, M-5, M-7, M-8, M-9, M-12, D-6 residual) → **X-W10**; the graph/hygiene cluster (M-10, M-11, M-15, M-16) → **X-W8**; the oracle extensions (o27 readout arm, readoutReservation unit oracle, zoom gate) → **X-W1**. No row lacks an owner; nothing is marked NO-WAVE-OWNER. The ten UNPROVEN rows above are this component's visual-audit input set.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3; dated evidence was not rewritten.
