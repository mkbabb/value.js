claude-fable-5 (served model id, observed in this seat's own system prompt — M-23/L-18 receipt)

# fr-EquationPanel — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Date**: 2026-08-11 · **Subject**: `fourier-analysis/web/src/components/visualization/EquationPanel.vue` (134 lines, read whole by this seat) — the KaTeX partial-sum equation overlay on `/visualize`.
**Substrate**: fourier HEAD `cd26c65` (re-verified this session, `git log -1`; byte-identity with the corpus's anchors confirmed at every line this seat checked) · installed `@mkbabb/glass-ui@4.0.0` (version re-read from the installed package.json this session) · producer coordinate where cited: reader-1's `79d9ca2f` @ 8.0.0 (one commit past r1's `074a3d0e`; adopted only where it re-confirms an r1 kill, never load-bearing alone).
**Corpus adjudicated (r2)**: two fresh hostile re-reader returns over the FULL challenge corpus — **reader-1** (design-and-usage: 21 confirmation cells · 11 kills · 6 misses · 8 superlative cells) and **reader-2** (library-and-code: 12 confirmation cells · 4 kills · 5 misses · 5 superlative cells + 1 not-a-defect record). The three axis challenge files were spot-read verbatim by this seat at every disputed cell (C §S-2 :388-396 · D :160-172 · L :686-695).
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-10 r1 apotheosis at this same path (in git history at `tranche-u`), which adjudicated the *prior* re-reader pair. The r1 roster — 44 rows, ids D-*/C-*/D-L*/M-N1..M-N7 — is **preserved for life** (anti-rename); r1 rulings are CARRIED where this corpus does not touch them and corrected in exactly **four** cells (amendments register below). r1's killed[1..17] all remain SUSTAINED; one (killed[15]) is NARROWED in place. Where r1 content is restated it was re-verified, not copied, except where marked *(r1-carried)*.
**Method (incredulity)**: every verdict-level disagreement between the two readers, every kill of a banked cell, and every NEW missed row was re-derived against the tree with this seat's own instruments before adoption. One banked r1 superlative was tested and **OVERTURNED by this seat's own greps** (ruling 1) — the second superlative overturn in this file's corpus (D·S-6 was r1's). Zero browser probes (probe parsimony); product source READ-ONLY; **the sole write is this file**.
**This seat's own instruments (all static + read-only)**: EquationPanel.vue whole (134 — every cited anchor checked: :21/:26/:38/:41-42/:52-57/:61-65/:70-72/:78/:81/:86/:90/:96/:98-102/:106-111/:118-133); SliderControl.vue whole (150 — clamp :40-42, onInput :44-49, array-setter :53-56, one-way `:value` :74, `--slider-scrub-*` :143-148, muted `.slider-label` :108, v1.8.x-era docstring); NotationPills.vue whole (47 — glyph span :27-30 with **no aria-hidden**, modelValue pair :6-12, `.notation-active` :42-46); notation.ts whole (48 — icons :15-17 incl. `eⁱ` = e+U+2071; duplicate literals :26≡:45/:32≡:46/:38≡:47); lib/equation/api.ts whole (57 — `notation: string` :36, `as` :49, unsliced map :38-44); lib/api.ts:150-180 (**`const signal = … ?? abortable(abortKey)` hoisted OUTSIDE `while (true)` at :161-164** — the reader-2 extension decider); style.css:50-77 (H.W0/γ signed comment; `.katex` 1.02em :60-62; `.katex-display > .katex` 1.1em :71-73); BasisSelector.vue:144-150 (unhidden `basis-icon cm-serif` span :148); BasisCanvas.vue grep census (:47 `maxCircles = ref(80)`, :135/:321 `nVis`, :196/:239 `level = ceil(anim.easedT * components.length)`, :303 `nTerms = min(level, len)`); python — simplification.py:15-31 (amplitude sort + `(abs(n), -n)` display re-sort), latex_rendering.py:30-56 (`max_terms = 4`, `\cdots` break) + :270-283 (`render_latex` takes `budget`, body never forwards it), rate_limiter.py:188-198 (`/api/equations/simplify` → compute zone); installed dist — **`grep -rl "slider-scrub" node_modules/@mkbabb/glass-ui/` → 0 files** and the consumed-token census (`--slider-range-bg` ×1 read, `var(--primary)` fallback; `--slider-track-height`/`--slider-thumb-*` — NO scrub family), material.css:60-100/:190-225 (specular family: rest `--glass-specular-intensity-rest, 0`; hover 0.1 / **active 0.16** rungs enumerated for `.glass-wash::before`; `--specular-x: var(--mouse-x, 50%)` centred fallback; z-index 1; `plus-lighter` ×3; the AX.W52 D19 bounded edge-gleam comment), ladder.css:190-219 (`:where(.glass-floating,.glass-overlay)` unconditional `--muted-foreground: var(--foreground)` lift; the wash cohort at :216-219 = tint floor with the verbatim "NO unconditional --muted-foreground lift here"), MetricBadge-BpC0R_Ec.js extraction (amount span `style: u(g.value.isEmpty ? void 0 : { color: n.color })` ×2 arms; unit span `metric-badge__unit font-mono text-muted-foreground` hard-coded; label spans `text-muted-foreground/80`; `labelPosition && (abbreviation || label)` gate), button chunk (`ghost: … text-foreground/70 …`); web/src greps — `energyColor` → **3 consumers** (EquationPanel :6/:38 · EquationView :7/:70 · InfoCard :5/:14), `v-html` → **10 sites / 8 files**, importers of the `lib/equation` barrel → **0** (the 4-line `export *` index.ts read whole), `--font-serif-math` → **0**, `term_count` → exactly types.ts:45; registry re-reads at the cited lines — fr-BasisSelector B-2/M-8/R-7/m-3, fr-EqCoefficientsPanel FR-EQC-1, fr-EquationModeToggle FR-EMT-19/K-9, fr-AnimationControls C-15/D-5, fr-CanvasControlsDock D-1/M-9, fr-ConvergenceLegend D-1+C-3 (whose seat proved `grep glass-backdrop web/src → ∅` — the bright-bucket decider this corpus needed).

## Scoreboard

**53 adjudicated roster rows — 0 BLOCKER · 17 MAJOR · 28 MINOR · 8 INFO** (r1's 44 identities carried; **9 net-new rows admitted** from this corpus's misses: 2 MAJOR · 7 MINOR). **Killed-claims register grows 17 → 20** (all r1 kills sustained; one narrowed in place). **One banked superlative OVERTURNED** (C S-2 / prior-reader S-5, r1-ratified — killed at this seat's own greps), **three new superlatives admitted**, one sharpened. **Four r1 cells amended** (D-3 blast radius · L·§3c denominator · killed[15] scope · the D-1/D-13 kills re-confirmed at a newer producer coordinate). **Two banked-row amendments relayed beyond this file** (fr-BasisSelector M-8's cure clause; fr-BasisSelector B-2 gains a ratified-consumer-site member). Each reader failed exactly once in a load-bearing place — reader-2 by re-ratifying a colour path into a dead token family, reader-1 by filing the specular geometry one producer-generation stale — and this file's verdicts rest on this seat's own instruments at both points.

## Ruled disagreements (this seat's actual work)

| # | contest | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **C S-2 / S-5 — "the right colour path" (banked r1 superlative)** | KILLED — `--slider-scrub-*` definition-absent at the pin; the Terms slider has never painted the Fourier hue; live member of banked fr-BasisSelector B-2 | RATIFIED "with producer receipt" (immune to the cssVarToHex `#888888` BLOCKER) | **KILLED — reader-1 wins; the r1 superlative is OVERTURNED** | My own runs: `grep -rl "slider-scrub" node_modules/@mkbabb/glass-ui/` → **0 files**; the installed slider consumes `var(--slider-range-bg, var(--primary))` and a token census shows NO scrub family anywhere in the dist. SliderControl.vue:143-148 writes five `--slider-scrub-*` declarations under a comment naming a "glass-scrubber variant token" API — its own docstring pins authorship at "v1.8.x `<Slider>`"; the family died before 4.0.0. `color="var(--viz-fourier)"` (EquationPanel.vue:101) → `--track-color` (:89) → five dead tokens: declaratively perfect, **paints nothing** — the slider renders the stock `--primary` capsule. Banked **fr-BasisSelector B-2 (BLOCKER, fleet-scoped)** already counts these exact 5 declarations in its 23/7-file census; three axes, two prior readers and the r1 arbiter ratified a colour path B-2 had already pronounced dead. Reader-2's surviving half is real and kept: the file never touches lib/colors.ts, so it is NOT exposed to the resolver BLOCKER (fr-BasisCanvas D-1/BC-1) — an immunity, not a feature. Reader-2's failure mode, recorded as fleet evidence: it verified the **declaration** side at the bytes and inherited the **consumption** side — the exact one-hop-short failure class the killed superlative itself embodies. |
| 2 | **SliderControl's commit model — defect or superlative?** | Missed-find M1: untypeable field, NaN→min refill fires a POST, out-of-range silent desync — MAJOR; banked fr-BasisSelector M-8's cure prescription UNSOUND | Minted superlative: dual clamp — cannot emit an invalid budget on either path | **BOTH ADMITTED — new row M-N8 (MAJOR) + superlative QUALIFIED** | My whole read of SliderControl.vue: `:44-49` emits `clamp(parseFloat(el.value))` on EVERY `input` event against a strictly one-way `:value="displayValue"` (:74 ← String(modelValue) :58-60); `:40-42` `Number.isFinite(v) ? … : lo`. All three legs re-derived: (a) from 6, typing "15" → '1' clamps to 2, Vue patches the DOM, '5' appends "25" → 20 — the target is unreachable by typing; (b) clearing → `parseFloat("") = NaN` → lo = 2, field refills, a real POST fires after the 300ms settle; (c) typing >20 at budget 20 emits 20 ≡ modelValue — no dep changes, no re-render, the DOM keeps the out-of-range text (native `max` is validation-only) — banked M-8(b) verbatim, on the blessed chassis. And reader-2's fact holds simultaneously: the emit can never leave [min,max], so no 422 is spellable. Both are true because they are about different halves: the server seam is guarded; the human seam is broken. **Amendment relayed to fr-BasisSelector M-8** (recorded here, the banked file never rewritten): its cure clause "the correct posture is in-tree 150 LOC away (SliderControl:40-49) and unused" migrates legs (a)+(c) instead of curing them — the true root stays M-16/m-14 (NumberField). |
| 3 | **r1 killed[15] — the render-cost kill** | (silent) | OVERTURN, half: "the computed is gated; the subtree is not" | **NARROWED, reader-2 wins the half it claimed** | My template read: :107-111 is a `v-if`/`v-else-if`/`v-else` chain over three sibling divs, and `loading` flips true (:42) → false (:57) on EVERY debounce-triggered cycle — so the `v-html` div is unmounted and re-created per cycle, and its `innerHTML` is re-parsed into the full KaTeX span tree even when `latex` is byte-identical (the Object.is gate spares only `renderedHtml`/`katex.renderToString`, exactly as r1 ruled). killed[15] survives for the computed; it is wrong for the DOM. New row M-N16 (MINOR); magnitude rides UNPROVEN #6. Cure interlocks with D-8 (keep the equation mounted; overlay the loading state) — one repair unit. |
| 4 | **D-3's falsifier enumeration** | (carried the banked framing) | KILLED: three consumers, not one | **KILL SUSTAINED at my own grep** | `energyColor` → EquationPanel.vue:6/:38 **+ EquationView.vue:7/:70 + InfoCard.vue:5/:14**. The D axis's falsifier cell ("consumed EquationPanel.vue:6,38 and nowhere else") is false at HEAD, and r1 carried it unexamined. The defect is HARDENED (unconditioned literals unchanged), but the cure's blast radius is three components on two routes — a fourier-local darkened arm must be regression-checked on the two `/equation` surfaces no D-axis probe ever opened. Same error class as D·S-6's grep (r1 killed[13]): the axis's `grep -n energyColor` was wrong in the same direction twice. |
| 5 | **L·§3c's denominator** | (silent) | KILLED: 10 sites / **8** files, not 6 | **KILL SUSTAINED at my own grep** — and the challenge cell self-contradicts: its own enumeration in the same paragraph names all eight files (PaperView · PaperSidebar · PaperSearchDropdown · PaperSearchModal · EquationPanel · ConvergencePlot · EquationResult · EquationView) under a "6 files" headline. r1's L·§3c row carried the 6; amended to 8. The fourth-blindness-class finding itself survives intact (≥2 independent members — the R5-7 bar). |
| 6 | **Reader-1's M2 (specular self-lighting) — admission + geometry correction** | Filed: gleam "sits at the panel's dead centre… paints above the equation"; survives every prescribed cure | (silent) | **ADMITTED as M-N10 (MINOR), one cell corrected** | My material.css read confirms every mechanism leg: `.glass-wash::before` at z-index 1 / `plus-lighter` / centred `--mouse-x, 50%` fallback (nothing in web/src writes it — banked FR-EMT-19's seat verified the same tree-wide), hover 0.1 / **active 0.16**, and CSS activation-chain propagation means pressing a pill, dragging the Terms thumb, or holding the close X lights the whole plate's 0.16 rung for the gesture, painting above the static children. CORRECTION: the "full-plate central disc" framing is one producer-generation stale — the installed dist's AX.W52 D19 comment declares a **bounded, explicit-radius edge gleam** (36% of plate, falloff inside 70%), and rest intensity is 0, so the defect is interaction-gated, not permanent. Cure-survival leg VERIFIED: `.glass-quiet`/`.glass-resting`/`.glass-floating`/`.glass-overlay` are ALL in the same hover/active cohort (my read of the rung lists), so neither D-4's cure nor the banked glass-floating ruling removes it. Second member of banked **fr-EquationModeToggle FR-EMT-19**; new here: the member site, the ancestor-activation leg, the cure interaction. |
| 7 | **Reader-1's M4 (three muted inks) — admission with the bucket settled** | Filed: "the wash rung is the one rung the design system deliberately gives no muted lift" | (silent) | **ADMITTED as M-N12 (MINOR), framing sharpened** | My ladder.css read: the unconditional lift belongs to `:where(.glass-floating,.glass-overlay)` only; the wash cohort carries the verbatim "NO unconditional --muted-foreground lift here". The one qualifier reader-1 skipped — a CONDITIONAL lift exists in the `@container style(--glass-backdrop: light)` bucket — is already settled by the banked fr-ConvergenceLegend seat: `grep glass-backdrop web/src → ∅`, the bucket never fires in this tree. Three muted inks confirmed on the unlifted plate: the close-X glyph (`text-muted-foreground` :86 wins the ghost variant's `text-foreground/70` via the cn text- last-wins bucket — variant ink verified in the button chunk; the lucide svg strokes currentColor, an SC 1.4.11 graphical object), the MetricBadge `%` unit (hard-coded muted in the chunk, my extraction), and SliderControl's `.slider-label` (:108, carries "Terms"). All three discharge free under the banked glass-floating rung change (r1 M-N5 amendment) that already binds this file. Composited ratios → SS-13. |

**Adjudicator's corrections register (against this corpus's readers)**: (1) reader-2's C S-2 re-ratification — corrected, ruling 1 (the load-bearing one); (2) reader-1's M2 geometry cell — corrected, ruling 6, verdict intact; (3) reader-1's M4 absolute framing — sharpened via the banked bright-bucket proof, ruling 7, verdict intact; (4) reader-2's census legs (7 metric-badge files / 35 lucide files) — adopted at fold altitude only, not re-counted here (they ride the banked census identity, INFO). Reader-1's eleven kills: nine re-verify r1 kills (sustained — D-1-as-BLOCKER, D-13's second-order arm at the newer producer `79d9ca2f`, C-13's arithmetic, C-12's premise, C-20's rename framing, D-16, D-4's cure, D-15's ratio half, D-2's sub-claim, D·S-6); one is the decisive new kill (ruling 1); one (D-2's sub-claim) it annotated rather than reversed — see M-N13.

## Roster delta — net-new rows admitted this corpus (M-N8..M-N16)

Dispositions are **ADJUDICATED, not VERIFIED** — routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave. Line anchors are HEAD `cd26c65`; re-resolve before any cure (D-19).

- **M-N8 (reader-1 M1, all three legs re-derived by this seat) — MAJOR — F.W3/W4** (one repair unit with fr-BasisSelector M-8 + m-14/M-16 NumberField root; the M-8 cure-clause amendment relayed). The "Terms" field is effectively untypeable (intermediate values unreachable — clamp-per-keystroke against a one-way `:value`), a cleared field silently refills with the minimum AND fires a real POST against the 5/60s compute limiter, and an out-of-range entry silently desyncs DOM from model (equal-emit no-op). The blessed chassis every banked cure points at carries the defect. Live witness is cheap → UNPROVEN #9.
- **M-N9 (reader-2 missed-1, spine re-derived by this seat) — MAJOR — F.W3/W4, dispositions WITH banked fr-EqCoefficientsPanel FR-EQC-1** (second member of the identity; never re-booked as a new class). The overlay's equation and the instrument under it describe two different partial sums selected by two different laws with no wire between them: the canvas draws a **clock-driven index-prefix** (`level = ceil(anim.easedT × N)` at BasisCanvas.vue:196/:239 → `nTerms` :303; plus a third hard cap `maxCircles = 80` :47 for the drawn circles) while the panel ships the full array and the server keeps the **top-budget by amplitude** (simplification.py:24, re-sorted `(abs(n), -n)` for display :30) then displays ≤4 of those (latex_rendering.py `max_terms = 4`) — and EquationPanel imports no animation state (whole import block read). Paused mid-sweep: a ~N·easedT-term drawing beside a `f(t) ≈ …` over ≤4 amplitude-ranked terms and a green `100.0 %`, 40px apart. New vs FR-EQC-1: the second surface is the CANVAS and the divergence axis is clock-index vs amplitude-rank. Adjacent-not-identical to fr-BasisCanvas D-9/BC-8. Cure shared with FR-EQC-1 (name the selection criterion in copy, or one level authority). Duty cycle → SS-13 (UNPROVEN #10).
- **M-N10 (reader-1 M2, corrected per ruling 6) — MINOR — F.W3/W4 + GLASS-RELAY rider** (a static informational surface has no way to opt out of the interaction-light state machine; producer question, rides the standing BH channel). The wash plate's specular gleam is lit by the panel's OWN controls via :active propagation and composites `plus-lighter` above the equation at the centred fallback; survives every rung-change cure in the corpus (whole cohort shares the rungs). Second member of banked FR-EMT-19. Perceptibility during a Terms drag → SS-13 (UNPROVEN #4).
- **M-N11 (reader-1 M3 + reader-2 missed-5, one mechanism family — merged) — MINOR — F.W4, binds the D-7/D-3/D-9 cures into ONE edit.** The installed badge applies `:color` to the amount span ONLY (`style: (isEmpty ? undefined : { color })`, my extraction): the `%` unit is hard-coded muted ink the prop cannot reach (two-tone readout by construction — unit leg is a member of banked fr-AnimationControls C-15), and the D-7 label cure would land `text-muted-foreground/80` micro-caps — a NEW sub-AA-suspect ink — on the plate D-3 is failing on. So D-7 (labelPosition + label), D-3 (literal repair ×6 per M-N1), M-N12's rung change and this row must land as one edit or each partially regresses another. Cure-cheapness receipt for D-9 carried: the chunk already suppresses colour and drops to `text-muted-foreground/40` on the empty path — `placeholder` alone yields a correct grey unknown state.
- **M-N12 (reader-1 M4, sharpened per ruling 7) — MINOR — F.W4; discharged by the banked glass-floating rung ruling (fr-ConvergenceLegend D-1+C-3 via r1 M-N5) the moment it lands.** Three muted inks (close-X glyph · `%` unit · "Terms" caption) sit on the one rung with no unconditional muted lift, over a live canvas, in the tree where the conditional bright bucket provably never fires. The glyph is an SC 1.4.11 graphical object (3:1 floor). Composited ratios → SS-13 with the sampling precision of UNPROVEN #3.
- **M-N13 (reader-1 M5, mechanism verified at my NotationPills/BasisSelector reads) — MINOR — F.W4 (one attribute per site, two sites).** The pills' accessible names carry the decorative math glyph — `{{ opt.icon }}` ("sin", "eⁱ" with U+2071, "Ae") in a span with **no `aria-hidden`** (:27-30), so names compute as "sin Trig" / "e[i] Exp" / "Ae Polar"; the house rule is the inverse (every lucide glyph auto-hidden — banked fr-CanvasControlsDock D-1 cites Icon.js:41), and BasisSelector.vue:148 is the identical second member. r1's killed[12] (names ARE distinct/computed — the "three identical unlabelled buttons" kill) stands unreversed; it stopped one question short of whether the computed name is CORRECT. Distinct from fr-BasisSelector m-22(d) and FR-EMT-10 (different mechanisms; registry-swept by the reader, spot-confirmed by this seat).
- **M-N14 (reader-2 missed-3, importer census re-run by this seat) — MINOR — F.W3/W4 (deletion) + F.W0 rider** (no `noUnusedLocals`/lint step can ever report it — substrate gap, rides D-L17's booking). `web/src/lib/equation/index.ts` is a wholly dead 4-line barrel (`export *` ×4, zero importers tree-wide — every consumer takes the deep path), and not inert: `./api` drags the 672-LOC fetch core into any future entrant's graph. New fourier member of the fleet-banked dead-barrel class (ApiOfflineChip AP-11 · kf-EditorHeader EH-6 · kf-KeyframeCard KF-KC-45 · PaperSearchInput C-9 — cited per reader-2, class identity not re-derived); challenge-witnessed once at the sibling and written up as praise, not filed.
- **M-N15 (reader-2 missed-4, cascade re-derived by this seat) — MINOR — F.W4 (comment or token the divergence; objective half only).** The panel's `:deep(.katex) { font-size: 0.9em }` (:131-133, compiled to (0,3,0)) silently overrides the app entry's SIGNED display-math decision (`.katex-display > .katex { font-size: 1.1em }` at (0,2,0), style.css:71-73, beside the H.W0/γ provenance comment) — display math in this panel paints at 0.82× every other math surface, uncommented, on the one surface whose equation is server-capped at 4 terms and least needs the shrink. Coexists with r1's minted cascade superlative (the mechanism is correct and deliberate; the divergence is undocumented) — both carried.
- **M-N16 (reader-2 missed-2 = the killed[15] narrow) — MINOR — F.W4, ONE repair unit with D-8** (keep the equation subtree mounted; overlay spinner/error — also cures the collapse-and-destroy leg). Per-cycle `innerHTML` re-parse of the unchanged KaTeX tree via the `v-if` branch swap (ruling 3). Frame cost → SS-13 (UNPROVEN #6, merged).

## Carried roster (r1, 44 rows — identities and routings unchanged except where marked AMENDED)

Full proof cells live in the r1 record (git history, this path, 2026-08-10) and are incorporated by reference; the four amendments are stated in full below.

**MAJOR (15)**: D-3+C-5 (**AMENDED**: cure blast radius = 3 consumers / 2 routes — EquationView.vue:7/:70 + InfoCard.vue:5/:14 join the repair's regression surface; 6-literal budget per M-N1 unchanged) — F.W4 + F.W2 rider · D-2+C-11+M-N4 (colour-only selection; dead pressed arm root) — F.W3/W4 · D-4+C-6+D-L16 (unlayered shadow replaces the wash's rim+shadow pair — reader-1's scope precision adopted: backdrop-filter/border/tint/specular/grain survive, "the two declarations that make it glass" was axis inflation) — F.W4, glass-floating cure per the banked rung ruling · D-5+D-L14+C-11 (unreachable Escape; focus dropped) — F.W4 · D-6 (no name/role/heading) — F.W4 · D-7+C-15 (unlabelled figure; TWO-attribute cure, now bound into M-N11's one-edit law) — F.W4 · D-8 (destructive loading; PRM frozen arc; now one repair unit with M-N16) — F.W4 + F.W1 rider · D-9+D-L4+C-5-leg (false green 100.0% per open; `placeholder` receipt in M-N11) — F.W4 · D-10+D-L12+C-22 (Terms names a quantity it stops governing; `budget` never forwarded — re-verified at latex_rendering.py:281 this session) — F.W5-W8 · D-11+C-14 (error surface; local darkened-arm cure) — F.W4 · D-14+D-L18 (Metric drops `color` + the pill plate at 8.0.0) — F.W1 · D-L1+C-23 (loading not request-scoped — reader-2's added falsifier ADOPTED as hardening: the 429 retry loop reuses ONE controller because the signal is hoisted outside `while(true)`, lib/api.ts:161-164, my read; a naive request-scoped rewrite would break that property) — F.W4 · D-L5+C-1 (full-payload re-upload vs the 5/60s compute limiter; two-slot hardening) — F.W5-W8 · D-1-coarse-arm (occlusion sign proven, magnitude SS-13; magic-constant leg MINOR) — F.W4 · M-N2 (no height clamp inside `overflow: hidden`) — F.W4.

**MINOR (21)**: D-12+D-L13 · D-15 (objective half) · D-17+C-12-leg · D-18+M-N5a · D-19+C-13 (inert glyph class; outlier on corrected numbers) · D-20 (with M-N3) · D-21+D-L8/9+C-3/4 (`trust: true` unnecessary; dead raw-interpolating catch) · D-L2+C-17 (armed timer survives unmount) · D-L3 (no unmount abort) · D-L6+C-9 · D-L7+C-19 · D-L10+C-10 (`notation` laundered through `string` — anchors re-verified whole this session) · D-L15 · D-L17 (no unit runner — scripts census matches reader-2's re-run) · C-7 (cn width/height bucket defeat; coarse floor holds) · C-8 (zero-prop singleton contract) · C-20 (devDep misclassification; live two-icon divergence) · M-N1 (six duplicate literals, two ramps) · M-N3 (rounding vs colour-step disagreement) · M-N6 (docstring's four false "matching" axes) · M-N7 (@types/katex shadowing) — routings as r1.

**INFO (8)**: C-16 (dispositions with D-10) · C-18 (fourth colour island) · C-21 · C-24 (modelValue pair — NotationPills.vue:6-12 re-verified) · D-L11 · D-L19 · **L·§3c (AMENDED: 10 sites / 8 files — my grep; the SS-3/SS-4 spec authoring must not inherit the 6)** — NO-WAVE-OWNER · D-16 residue — routings as r1.

**FOLDED identities (r1's 8, all carried)**: D-13 → fr-ConvergenceTimeline D·D-2 (second-order arm stays KILLED — reader-1 re-verified the unconditional `data-control-target` floor at producer `79d9ca2f`, one commit past r1's read) · C-2 → census §1 pins + G33 (with the ^4.0.0 peer rider) · MetricBadge cursor-pointer → fr-AnimationControls C-28 · ApiProblem detail-discard → FR-AUL-50/FR-AFP-22 · scrollbar-width → fr-ConvergenceLegend SC-4 · outline-opaque-rest → fr-BasisSelector M-1 · lucide rename framing → fr-AnimationControls C-1 rider / fr-AppHeader:69 · M-N5 wash-as-overlay fold-AND-AMEND → fr-ConvergenceLegend D-1+C-3.

**FOLDED identities — r2 additions (by reference, never re-booked)**: **the slider retint consumer site** (EquationPanel.vue:101 → SliderControl:143-148) → **fr-BasisSelector B-2** (BLOCKER, fleet-scoped; the 23-declaration census already contains SliderControl's 5 — this fold adds a ratified consumer BINDING, not a declaration; F.W1 token-family gate + F.W3/W4 execution, cure per B-2's corrected rename-range/delete-thumb clause) · **cm-serif inert at NotationPills.vue:27** → **fr-BasisSelector m-3** (reader-1 M6; reproduced by my `--font-serif-math` → 0 grep; F.W1 token-parity member count +1; the panel's pills fall back to generic serif two divs above a KaTeX block rendering real CM faces — recorded, not re-booked) · **the `%`-unit muted-ink mechanism** → **fr-AnimationControls C-15** (new member site inside M-N11).

## Killed-claims register (20 — r1's 17 sustained, #15 narrowed; three added)

r1 entries 1-14, 16, 17 carried verbatim by reference (git history). Changes and additions:

15. *(NARROWED this corpus)* **L·§4b's frequency premise** — r1's kill survives for the computed (`Object.is` gate on the string ref spares `katex.renderToString`); it is WRONG for the DOM: the `loading` branch swap re-creates the `v-html` div and re-parses its `innerHTML` per debounce cycle (my :42/:57/:107-111 reads). The killed register reads henceforth: "the computed is gated; the subtree is not." Survivor booked as M-N16.
18. **C S-2 / prior-reader S-5 / r1 superlative "the right colour path" — OVERTURNED.** The `--slider-scrub-*` family is definition-absent at the installed pin (my 0-file grep; the installed slider reads `--slider-range-bg` with a `--primary` fallback); the Terms slider has never rendered in the Fourier hue. The challenge's own cell (C :390-392) verified `color-mix(in srgb, var(--track-color) 25%, transparent)` as the composition site and never asked whether the composed token has a reader. Only the narrow immunity half survives (no lib/colors.ts exposure). Folds to fr-BasisSelector B-2.
19. **D-3's falsifier enumeration** ("consumed EquationPanel.vue:6,38 and nowhere else") — false at HEAD: three consumers, two routes (my grep). The defect survives hardened; the cure's regression surface triples. The D axis's greps are now wrong twice in the same direction (with D·S-6, r1 killed[13]).
20. **L·§3c's "6 files" denominator** — 10 sites / **8** files (my grep), and the challenge's own same-paragraph enumeration names all eight — an internal contradiction neither axis nor r1 caught. The finding's substance is untouched.

## Superlatives (adjudicated, provenance preserved)

- **RATIFIED + EXTENDED (D S-5 / L S-L1 / C S-1; reader-2's falsifier adopted, verified by this seat)** — the abort seam: `isAbortError` discriminated before any error write, riding the canonical per-key registry; AND the 429 retry loop cannot self-abort because the signal is hoisted outside `while (true)` (lib/api.ts:161-164, my read) — a re-entrancy subtlety any C-1 hardening rewrite must preserve.
- **RATIFIED (D S-1)** — opacity-only tokened mount transition, exactly the PRM-preserved property set *(r1-carried)*.
- **RATIFIED, QUALIFIED TWICE (D S-3 / C S-4 + reader-2's dual-clamp mint)** — both slider controls named from one string, and the numeric seam is structurally incapable of emitting a 422 (both paths clamp — my :40-56 read). Qualified by M-N8: the correctly-named, correctly-clamped input cannot be typed into; and by ruling 1: the correctly-passed colour cannot paint.
- **RATIFIED + SHARPENED (D S-2, reader-1's extension)** — `aria-label="Close equation panel"` is the only named control in the entire equation round trip: the dock trigger that opens the panel carries no name at all (banked fr-CanvasControlsDock D-1). The panel names its exit and not its entrance.
- **RATIFIED (r1, prior-reader-minted)** — zero ambient registrations in 66 script lines; re-verified by both r2 readers and this seat's whole-file read.
- **RATIFIED (reader-2 S-4 / C S-5)** — the notation axis is type-exhaustive and single-tabled, with the standing qualifiers (duplicate literals; the `string` laundering four lines away) *(r1-carried)*.
- **RATIFIED (reader-1, minted r2; coexists with M-N15)** — the two `:deep()` overrides are the file's one correct cascade argument: (0,3,0) beats the entry's (0,1,0)/(0,2,0), so the authored compaction actually lands. The same author's unlayered box-shadow (D-4) fails the identical reasoning nine lines earlier — the competence is real and the failure is local. M-N15 books the undocumented app-authority divergence the correct mechanism produces.
- **RATIFIED (reader-2, minted r2)** — `energyColor` is total over ℝ and cannot throw on the render path (three-arm step, unconditional final return — my whole read): the D-3 repair is a pure value edit with zero control-flow risk.
- **RATIFIED (r1, prior-reader-minted)** — the consumed store demonstrates the exact teardown discipline the component lacks *(r1-carried)*.
- **OVERTURNED — D·S-6** *(r1, killed[13])*: "one colour authority" — six duplicate literals.
- **OVERTURNED — C S-2 / S-5** *(this corpus, killed[18])*: "the right colour path" — a declaratively perfect binding into a dead token family. Two superlative overturns in one file's corpus, both by a seat's own grep at the exact line the ratifiers cited: the fleet lesson is that a superlative's falsifier must be RUN, not admired.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **D-1 geometry, the decisive probe** *(r1-carried)*: dock vs panel `getBoundingClientRect`, coarse phone + fine sub-1024 — settles the fine arm's sign among the three static derivations.
2. **M-N2 stage clipping** *(r1-carried)*: clip-vs-occlusion order at landscape-phone / portrait-phone / 1024-1280.
3. **All contrast decimals** (D-2/D-3/D-11/M-N12) — **sampling precision added this corpus (reader-2, chunk-verified by this seat): target `.metric-badge__amount` specifically, never the badge root and never the `%` unit (muted, a different ratio)**; sample under the badge, the trig pill, the error row, AND the three muted inks (close-X glyph / `%` / "Terms" caption), both themes, animation running. The light-mode FAIL columns survive any plausible sampling; the muted-ink trio does not.
4. **Specular perceptibility (M-N10)**: is the 0.16 plus-lighter active rung legible over the KaTeX glyphs for the length of a Terms drag (the bounded-gleam geometry makes this narrower than filed — measure, don't assume either way).
5. **D-L5/C-1 wall-clock** *(r1-carried)*: 429 spinner duration; compressed payload bytes; per-frame backdrop-filter cost.
6. **Per-cycle innerHTML re-parse cost (M-N16, merged with r1's KaTeX-cardinality probe)**: `performance.mark` around a debounce cycle + `getElementsByTagName('*').length` at budget 20, all three notations.
7. **Energy duty cycle** *(r1-carried)*: how often real contours sit below 0.99/0.95.
8. **D-18 double-scrollbar materialisation** *(r1-carried, low priority)* — **plus reader-1's new leg, adopted: `.katex-display` keeps `text-align: center` from katex.min.css, so whether the LEADING terms of an over-wide equation are reachable by scroll from a centred origin is engine-dependent — decide live.**
9. **M-N8 witness (cheap, wiring fully static)**: focus the Terms field at budget 20, type "99" — assert input text / thumb / model disagree; the banked M-8(b) desync on the chassis that row prescribes as the cure.
10. **M-N9 duty cycle**: read `anim.easedT` and the derived level beside the panel's budget, playing and paused — decides whether the canvas/panel contradiction is permanent or transient, and therefore whether the cure is copy or a shared authority. Dispositions WITH FR-EQC-1.
11. **C-13/D-19 painted glyph sizes** *(r1-carried)*.
12. **NOT A DEFECT, recorded so it is not re-hunted (reader-2)**: `import katex` costs `/visualize` nothing incremental — vite welds value.js+katex into one `vendor-math` chunk already modulepreloaded via the banked fr-AppHeader FR-AH-7 easings edge; code-splitting this panel measures as a no-op until FR-AH-7's repair lands first. Sequencing constraint for F.W3/W4.

## Closing verdict

**ADJUDICATED (r2)**: 53 rows survive (0 BLOCKER · 17 MAJOR · 28 MINOR · 8 INFO) — r1's 44 carried with four cells amended, 9 net-new rows admitted (2 MAJOR · 7 MINOR); the killed register stands at 20 with every r1 kill sustained and one narrowed; **one banked superlative overturned at this seat's own instruments** (the colour path into the dead `--slider-scrub-*` family — the r1 ratification, two prior readers and three axes all stopped one grep short of fr-BasisSelector B-2's already-banked proof), and two banked-row amendments relayed (fr-BasisSelector M-8's cure clause is unsound as written; B-2 gains a ratified consumer-binding member). The r1 portrait sharpens rather than changes: a well-seamed consumer of an excellent transport substrate wearing its defect mass in the a11y/state core — now with the two instruments it sits between (the Terms field it renders and the canvas it annotates) each proven to disagree with it. Each r2 reader was load-bearing-wrong exactly once (reader-2 on the slider superlative; reader-1 on the specular geometry generation), and every such point in this file rests on this seat's own reads. Routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of apotheosis r2. The three challenge files, both r2 reader returns, and the r1 record (git history at this path) remain immutable beside this file per E-1/E-3.

---

## Addendum r2-a — re-adjudication receipt (2026-08-12, E-3: appended beside the record, nothing above rewritten)

**Seat**: fresh Fable adjudicator, re-spawned M-23/L-18 · served model id `claude-fable-5` (observed in this seat's own system prompt, not inherited).
**Occasion**: the r2 seat's structured return was severed at the org monthly-spend wall; the record above was salvaged and committed (`9c779130`, value.js `tranche-u`) without its orchestrator receipt. This seat did NOT trust the salvage: it re-adjudicated the same two-reader corpus from scratch — every ruled disagreement, every kill of a banked cell, every net-new row — before re-issuing this file.
**Substrate re-pinned this session**: fourier HEAD `cd26c65` (unchanged, `git log -1`); installed `@mkbabb/glass-ui@4.0.0` (version re-read from the installed package.json).

**Result: ZERO corrections. All seven rulings, the 20-row killed register, the 9 net-new rows (M-N8..M-N16), the 4 r1 amendments, and every superlative disposition SUSTAINED at this seat's own instruments.** The decisive re-runs, each executed fresh:

1. **Ruling 1 (the overturn)**: `grep -rl "slider-scrub" node_modules/@mkbabb/glass-ui/` → **0 files**; the dist slider reads `slider-range-bg,var(--primary` (my own extraction from `dist/glass-ui.css`); SliderControl.vue:143-148 read whole — five `--slider-scrub-*` declarations under the v1.8.x-era "glass-scrubber variant tokens" comment. The C S-2 cell re-read verbatim (challenge-C:387-396): it verifies the `color-mix` composition site and never asks whether the composed token family has a reader. OVERTURN STANDS.
2. **Ruling 2 (M-N8 + dual-clamp superlative)**: SliderControl.vue:40-56/:74 — all three M-N8 legs re-derived (clamp-per-keystroke against the one-way `:value`; `parseFloat("")→NaN→lo` refill; equal-emit no-op desync) AND both emit paths provably clamped. Both admissions stand; the fr-BasisSelector M-8 cure-clause amendment stands.
3. **Rulings 3/4/5 (the kills)**: `v-if`/`v-else-if`/`v-else` chain re-read at :107-111 with `loading` flipping :42/:57 per cycle (killed[15] narrow stands); `energyColor` → **3 consumers** (EquationPanel :6/:38 · EquationView :7/:70 · InfoCard :5/:14 — my grep; killed[19] stands, D-3 falsifier cell re-read verbatim at challenge-D:164-172); `v-html` → **10 sites / 8 files** (my grep; killed[20] stands, the challenge's same-paragraph self-contradiction re-read at challenge-L:689-695).
4. **Ruling 6 (M-N10 geometry correction)**: installed material.css — specular rest `var(--glass-specular-intensity-rest, 0)`, bounded `circle var(--glass-specular-size, 36%)` explicit-radius gleam (the AX.W52 D19 comment), `plus-lighter`, centred `var(--mouse-x, 50%)` fallback, and `.glass-wash:hover::before`/`.glass-wash:active::before` rungs live at :151-157/:194-196. The bounded-gleam correction and the whole-cohort cure-survival leg both stand.
5. **Ruling 7 (M-N12 bucket)**: the wash-including `--muted-foreground: var(--foreground)` block sits inside `@container style(--glass-backdrop: light)` (ladder.css :135-150, my read); **`grep -rn "glass-backdrop" web/src/` → 0 — this seat's OWN run**, no longer resting on the fr-ConvergenceLegend citation; the unconditional lift belongs to `:where(.glass-floating,.glass-overlay)` only (:200-203) with the verbatim "NO unconditional --muted-foreground lift" disclaimer over the wash cohort (:213-216). Framing stands.
6. **Roster spot-re-derivations, all green**: notation.ts read whole (six duplicate literals :26≡:45/:32≡:46/:38≡:47; :15 = the trig `hsl(6, 72%, 49%)` — D·S-6 kill's basis re-read verbatim at challenge-D:655-663); NotationPills.vue read whole (no `aria-pressed` anywhere in `src/components/equation/` — my grep → 0; the unhidden glyph span :27-30; the modelValue pair :6-12); the dead barrel (4-line `export *` ×4, **0 importers** — my grep); `--font-serif-math` → 0; `term_count` → types.ts:45 only; `eq-simplify` → one occurrence; lib/api.ts `const signal = options?.signal ?? abortable(abortKey)` hoisted OUTSIDE `while (true)` (my :161-164 read — the retry-reuses-one-controller hardening stands); lib/equation/api.ts:36/:49 `string`+`as` laundering beside the :4-11 "retires structurally" header; style.css :60-73 (1.02em/1.1em + the H.W0/γ signed comment — M-N15 stands); VisualizationView :230-231 (Transition + single `@close`), `.viz-panel-right { overflow: hidden; position: relative }`, opacity-only `.fade` with `var(--ease-standard)`, and the ≥1024px `right: 0.5rem` dock pin (D-1 desktop-arm kill stands); BasisCanvas :47/:135/:196/:239/:303 (M-N9 spine; the panel's sole "anim" grep hit is `animate-spin` :108 — no animation import); MetricBadge chunk (color on the amount span ONLY, gated `isEmpty ? void 0 : { color }`; `labelPosition && (abbreviation || label)` gate; unit hard-coded `text-muted-foreground`; labels `/80`); button chunk (ghost = `text-foreground/70…aria-pressed:bg-foreground/10` — the pressed arms EXIST and are dead here, D-2's root; `[&_svg:not([class*=size-])]:size-(--ui-glyph)` — D-19/C-13 inertness; `data-size` reflected); cn buckets `["width", /^w-/]` / `["text-color", /^text-/]`; a11y-overrides both PRM arms (`*:not([data-allow-motion])` + the identical `[data-allow-motion]` re-kill) and the coarse `[data-size="icon"]` `min-block/inline-size: var(--touch-target, 2.75rem)` floor; export map `./dialog`/`./pulse`/`./fading-scroll`/`./metric-badge` live, `./metric`/`./chip` absent; peers `@mkbabb/value.js ^0.10.0 || ^0.11.0` vs web's `^0.13.0` and `@lucide/vue ^1.16.0` already present at the pin (C-20 kill stands); `lucide-vue-next ^1.0.0` in devDependencies against the :10 runtime import.
7. **Python legs at the true package path** (a citation precision, not a correction): the simplify chain lives at `src/fourier_analysis/symbolic/{simplification,latex_rendering}.py`, imported dynamically inside the route closure at `api/routers/equations.py:139` — all cited anchors byte-match (amplitude sort desc + `(abs(n), -n)` display re-sort; `max_terms = 4` + `\cdots` break; `render_latex(… budget: int = 10 …)` whose body calls `renderer(terms, variable, compact=compact)` — budget never forwarded); `rate_limiter.py:188-198` routes `/api/equations/simplify` to the compute zone; `config.py:23 compute_rate_limit: int = 5`; the whole handler is `submit_compute_job("simplify", _run)` over a pure closure (:134-162).

**Standing statement**: the record above is hereby this seat's own — re-derived, not adopted. 53 rows (0 BLOCKER · 17 MAJOR · 28 MINOR · 8 INFO) · killed register 20 · superlatives as adjudicated · UNPROVEN #1-#12 unchanged as the SS-13 inputs. Dispositions remain **ADJUDICATED, not VERIFIED**; routings remain agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of addendum r2-a.
