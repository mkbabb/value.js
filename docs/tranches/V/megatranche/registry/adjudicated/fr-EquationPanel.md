claude-fable-5 (served model id, observed in this seat's own system prompt — M-23/L-18 receipt)

# fr-EquationPanel — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Date**: 2026-08-10 · **Subject**: `fourier-analysis/web/src/components/visualization/EquationPanel.vue` (134 lines, read whole by this seat) — the KaTeX partial-sum equation overlay on `/visualize`.
**Substrate**: fourier HEAD `cd26c65` (intake R4-9 byte-identity) · installed `@mkbabb/glass-ui@4.0.0` (this seat re-read the package.json: peer set already includes `@lucide/vue ^1.16.0`) · producer `/Users/mkbabb/Programming/glass-ui` HEAD `074a3d0e` @ **8.0.0** (this seat's own read — one major past the D axis's 7.0.0 snapshot; both re-readers caught the drift independently).
**Corpus**: three hostile challenges (D 21 rows · L 19 rows+classes · C 24 rows, all axes `claude-opus-5[1m]`) → two hostile re-reads (design-and-usage; library-and-code). This seat adjudicated the re-readers, not just the axes.
**Method**: every verdict-level disagreement between the two re-readers was re-derived against the tree with this seat's own instruments; suspiciously-agreed severity demotions were spot-checked; one re-reader superlative-ratification (D·S-6) was tested and **overturned by this seat's own file read**.
**This seat's own instruments (static + read-only; zero browser — probe parsimony; the sole write is this file)**: EquationPanel.vue whole; NotationPills.vue whole (47); lib/equation/notation.ts whole (48); VisualizationView.vue :185-240 (mount, dock anchor, controls-overlay) + :370-480 (stage CSS, media queries, transitions); CanvasControlsDock.vue:41 (`fit-content :start-collapsed="true"`); BasisSelector.vue:144/:253-254/:262-270; style.css:1-5/:38-45/:62-70; installed dist — dock/overflow.css:200-285 (the ENTIRE coarse block), dock/morph.css:108-190 (height-pin contract + B4 collapsed-circle rule), dock/density.css:70-100 (comfortable rung), dock/layers.css:126/:211-223 (min-height floor, no display:none), dock/shell.css height census, offsets-sizing.css:243-320 token greps, dock.js:701 (`"data-density": K(d)` — always reflected, so the coarse `[data-density]` block ALWAYS applies); producer HEAD — package.json 8.0.0, src/components/button/Button.vue:169-171 (`:data-size` + **unconditional** `data-control-target=""`), src/styles/utilities/responsive.css:1-8 (the coarse `[data-control-target]` 2.75rem floor), `git show v7.0.0:src/components/button/Button.vue`:92 (the conditional predecessor); web/package.json (`@types/katex ^0.16.8` devDep, `lucide-vue-next ^1.0.0` devDep, `vue ^3.5.38`, scripts = dev/build/preview/test:e2e ×2 — no unit runner); node_modules/katex/package.json (0.17.0, `types: types/katex.d.ts` present); greps — `eq-simplify` (exactly 1: lib/equation/api.ts:51), `term_count` (exactly 1: types.ts:45), `renderToString` (exactly 4 files), `aria-pressed` in components/equation (0).

## Scoreboard

**44 defect rows ADJUDICATED-CONFIRMED (0 BLOCKER · 15 MAJOR · 21 MINOR · 8 INFO) · 17 claims KILLED · 5 net-new finds banked · 8 identities FOLDED by reference (never re-booked).** Both filed BLOCKERs die as BLOCKERs: D-1 demotes to a coarse-arm MAJOR + a MINOR magic constant; C-1 demotes to MAJOR (with the shared-limiter hardening adopted); C-2 folds to the banked census identity at INFO altitude. Heaviest surviving mass: the a11y/state-machine core (D-2/D-3/D-5/D-6/D-7/D-8/D-9/D-11 + M1) — nine MAJORs on one 134-line file, all F.W4.

## Ruled disagreements (this seat's actual work)

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-1** | KILLED as BLOCKER (3 premise errors; coarse residue ~0.36rem UNPROVEN; MINOR constant survives) | CONFIRMED BLOCKER (coarse arm; 1.875rem overlap; collapsed coarse 1.31rem) | **BLOCKER KILLED · coarse arm MAJOR · fine arm UNPROVEN both directions** | My dist read settles the scale: `dock/overflow.css:209-237` `@media (pointer:coarse) { .glass-dock[data-density] { --dock-local-scale: var(--dock-mobile-scale, var(--dock-coarse-scale, 0.78)); --dock-scale: calc(var(--ui-scale) * var(--dock-local-scale,1)); } }` with the producer's own comment — *"the coarse dock painted ~22% too big … an ~80px collapsed pill"* — naming reader-2's 1.5× figure as the FIXED regression; `dock.js:701` reflects `data-density` unconditionally so the block always fires (reader-2's `grep density CanvasControlsDock → empty` falsifier tested the wrong element). Coarse `--dock-scale` ≈ 1.17, killing the 1.875rem figure. **BUT reader-1's collapsed arithmetic is also unsafe**: it assumes collapsed content = the 0.85× summary, while `morph.css:108-137` pins padding-block *"so the outer block-size is constant → 0px container-height delta"* and attributes the pre-fix collapse delta to the PAD alone — implying content height stays at `--dock-layer-height` (`layers.css:223` `min-height` on every layer; `:126` layers keep geometry, no display:none). Under that reading: fine collapsed bottom = 0.5+2.5+0.75 = **3.75rem → 0.25rem overlap**; coarse collapsed = 0.5+2.925+0.8775 = **4.30rem → ~0.80rem (≈14px @ 1.125rem root) overlap**. Three static derivations now exist; ALL agree the coarse arm overlaps (0.36–0.94rem), they disagree on fine. Coarse arm books MAJOR (sign proven thrice, magnitude SS-13); fine arm UNPROVEN-NEEDS-LIVE; ≥1024px desktop is horizontally disjoint (VisualizationView.vue:467-473, my read) so *"covers the toggle that spawned it"* is unestablished as filed. |
| **D-13 second-order arm** (coarse touch floor dies with `size="icon"`) | KILLED (producer re-homed the floor) | CONFIRMED — *"the real payload"* | **KILLED — reader-1 wins, by this seat's own producer read** | Button.vue:171 at 8.0.0 HEAD emits `data-control-target=""` **unconditionally on every Button** (and still reflects `:data-size` at :169); `src/styles/utilities/responsive.css:3-7` floors `[data-control-target]` at `var(--touch-target, 2.75rem)` on coarse. At `git show v7.0.0:` the attribute was conditional (`iconOnly ? '' : undefined`, :92) — so the floor survives the uplift and is WIDER at 8.0.0. Reader-2 re-asserted the a11y-overrides `[data-size="icon"]` mechanism it had itself used to kill C-7, without checking for a successor. The F.W1 budget must NOT carry this risk row. D-13 itself stays a FOLD to banked **fr-ConvergenceTimeline D·D-2** (prop-surface breaks behind retained subpaths, F.W1, 38× `size="icon"` + 14× `variant="standard"`); EquationPanel.vue:84-85 + NotationPills.vue:20-21 are member sites. |
| **D·S-6 superlative** ("feature colour has exactly one authority") | RATIFIED | KILLED (six duplicate literals; the cited grep is wrong) | **KILLED — reader-2 wins, by this seat's own file read** | notation.ts read whole: the green `hsl(142, 71%, 45%)` lives at **:26** (TIER_INFO.symbolic) AND **:45** (energyColor); amber at :32/:46; red at :38/:47 — six literals, two semantically independent ramps (derivation tier vs energy capture) pinned together by copy, no shared constant. And **:15 — the line reader-1's ratifying grep cites as the green's home — is `hsl(6, 72%, 49%)`, the trig PILL colour**, a third unrelated ramp. One module owns feature colour; one authority does not. Reader-2's replacement find (M-N1 below) is banked; the D-3 cure budget becomes 6 literals in 2 tables. |
| **D-19** | CONFIRMED at MINOR with corrected numerator (painted glyph 16px, ratio 0.67 vs dock 0.45 — larger margin than filed) | KILLED entirely ("0.67 and ~0.55, straddling rather than outlying") | **CONFIRMED MINOR — reader-1 wins** | Both painted ratios (fine 16/24 = 0.67; coarse 24/44 ≈ 0.55) sit ABOVE the dock band (0.45–0.50) — "straddling" is arithmetically wrong. The filed 14px numerator IS inert (C-13's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` mechanism, both readers agree, specificity (0,2,1) > (0,1,0) in the same layer), so D-19 and C-13 adjudicate as ONE row: the authored class is dead code and the true ratio is the outlier by a wider margin. MINOR, F.W4. |
| **C-16** | CONFIRMED MINOR (under-exposure 20 vs 50) | KILLED as a defect; re-book INFO contract drift | **INFO — reader-2 wins** | Widening the slider to the server's `le=50` would add thirty stops that cannot change the display (latex_rendering.py caps at 4 and `render_latex`:281 never forwards `budget`) while each costs a full-payload POST against the 5/60s limiter — the "cure" strictly worsens D-10 and C-1. The ceiling divergence is real contract drift; it must be dispositioned WITH D-10/D-L12 at the server seam, never by widening the client. |
| **D-L2 severity** | MINOR (demoted) | MAJOR (house-pattern deviation) | **MINOR** | Blast radius = one stray ~53kB POST on a destroyed instance + one burned compute slot; no state corruption (refs are garbage after unmount). Reader-2's hardening is adopted as the cure shape (stores/workspace.ts:74-76 `onScopeDispose` clears its own debounce timer — the discipline is one import away), but a bounded single-shot waste is not MAJOR. |
| **C-1/D-L5 severity** | MAJOR (demoted from C's BLOCKER) | BLOCKER sustained | **MAJOR** | The demotion holds: retry sleep bounded ≤2×30s, panel read-only w.r.t. the store, no data loss, and the panel is an optional overlay. Reader-2's hardening is ADOPTED into the row: the same `compute_limiter` governs `/compute/…` + `/extract-contour` (rate_limiter.py:189-195), and `epicycleData` in the watch source means one recompute spends TWO slots. Severe, cross-repo, F.W5-W8 — but a ship-stopper it is not. |
| **D-L6 / D-L7 severity** | MINOR / MINOR | MAJOR / MAJOR | **MINOR both** | Missing memo and a 4-site render clone are quality defects with house cures in-tree (EquationView.vue:132-133; useEquationCache.ts imported once); their operational cost is already carried by the C-1/D-L5 MAJOR — double-counting the same wasted requests at MAJOR twice inflates the file. |

## Adjudicated defect roster

Severity order; merged ids keep every original id for life. Dispositions are ADJUDICATED, not VERIFIED — routings are agglomeration input for the forming X·F specs (M-25).

### MAJOR (15)

- **D-3 + C-5 (+D-L4 colour leg) — MAJOR — F.W4 (contrast repair) + F.W2 rider (colour island).** The energy badge — the panel's only quantitative claim — fails WCAG AA in light mode in all three states on the plate the tree actually paints (reader-1's node run on the composited `glass-bg-quiet`-over-`glass-wash`-over-tint-floor plate: green 2.09 / amber 1.94 / red 3.44 vs the 4.5 floor for `--type-micro` 11px text; reader-2 reproduces the raw-background column ±0.04). Literals unconditioned, no dark arm (notation.ts:44-47). Cure budget is **6 literals in 2 tables** (see M-N1), on the style.css:113-127 `--viz-amber` D.W4.d precedent. Exact on-plate decimals → SS-13.
- **D-2 + C-11(partial) + M-N4 — MAJOR — F.W3/W4.** Selection on the notation pills is colour-only: no `aria-pressed`/role, `.notation-active` class+tint only (NotationPills.vue:17-25/:42-46; `aria-pressed` grep → 0, this seat re-ran via file read), while the module's own docstring names the missing attribute (notation.ts:3-8) and the sibling ships it (BasisSelector.vue:144/:269, my read). **Causal root adopted from reader-1's M2**: the installed `outline` variant SHIPS a pressed arm (`aria-pressed:bg-accent …`, button-BNDWhAZb.js:57) that is dead code here, while its hover arm stays live on unselected pills — a 200ms pointer state paints louder than the persistent selection (BasisSelector neutralises exactly this at :264-267, my read). Sub-claim *"three identical unlabelled buttons"* KILLED (names compute from content — icon+label spans, NotationPills.vue:27-31). Light-mode table killed in the worsening direction (see killed register). Opaque `bg-background` rest inside a glass overlay FOLDS to banked **fr-BasisSelector M-1** (second member; aggravation: floating overlay, not in-flow panel).
- **D-4 + C-6 + D-L16 — MAJOR — F.W4, cure per the banked rung ruling.** The scoped `box-shadow: 0 4px 16px rgba(0,0,0,0.08)` (:124) is unlayered and unconditionally replaces `.glass-wash`'s `var(--glass-material-rim), var(--glass-shadow-wash)` (ladder.css:41 in `@layer components`) — deleting the rim + inset catch-light and substituting a theme-blind copy of `--glass-under-shadow-default` on the one rung documented never to compose an under-shadow (ladder.css:53-54). **Cure conflict RESOLVED**: D-4's `.glass-quiet` prescription is overruled; the banked **fr-ConvergenceLegend D-1+C-3** ruling (`glass-floating`, re-measure post-F.W1 per U-3/SR-1) now binds BOTH members of the wash-as-overlay identity (see the M-N5 amendment).
- **D-5 + D-L14 + C-11 — MAJOR — F.W4.** The Escape handler is unreachable (`tabindex="-1"` + `@keydown.esc` with nothing ever focusing the element; opener focus stays on the dock trigger; `focus(` grep over the subtree → hits only in FullscreenViewer.vue:53-77, reader-2's sharper in-directory precedent, which also ships the tab-cycle trap) and closing drops focus on the floor. House patterns exist three ways (FullscreenViewer, MobileFloatingToc, GalleryCardModal's `<Dialog>`); `./dialog` is live in the installed 4.0.0 export map.
- **D-6 — MAJOR — F.W4.** No accessible name, no role, no heading: bare `<div class="eq-panel glass-wash" tabindex="-1">` (:69-73); the title is a styled `<span>` (:75); exactly one aria attribute in the file (:87, the close button).
- **D-7 + C-15 — MAJOR — F.W4.** The energy figure is unlabelled; the installed MetricBadge ships `label`/`abbreviation` unused. **Cure corrected per reader-1's kill**: the label slot is gated on `labelPosition` (MetricBadge.vue.d.ts, verbatim: without it the slot does not render even when `label`/`abbreviation` carry values) — the fix is TWO attributes, and a one-attribute edit would silently not land. Post-uplift the successor `Metric` keeps `label` (types.ts:17-42 at 8.0.0).
- **D-8 — MAJOR — F.W4 (+F.W1 rider: `Metric` ships `loading`/`aria-busy`).** `v-if="loading"` destroys the equation every debounce cycle; no `min-h` so the panel collapses; no `role="status"`/`aria-live`/`aria-busy`; and under PRM the producer's universal kill (a11y-overrides.css:6-10, escape re-killed at :26-30) freezes the hand-rolled `animate-spin` into a static arc — the one loading affordance with no reduced-motion fallback. (C-12's inverted framing killed below.)
- **D-9 + D-L4 + C-5(leg) — MAJOR — F.W4.** `energy = ref(1)` + `loading = ref(false)` + the debounced-even-when-`immediate` first call (vueuse filter chain verified by both readers in the shipped bundle) ⇒ every open shows ~300ms+RTT of void, then a confident green `100.0 %` over an empty equation — a false quantitative claim indistinguishable from the server's own vacuous-completeness sentinel (simplification.py:62), re-manufactured per toggle (`v-if` parent) and per reload (persisted `showEquation`, useViewState.ts:21). `MetricBadgeProps.placeholder` exists unused.
- **D-10 (headline) + D-L12 + C-22 — MAJOR — F.W5-W8 (client↔server contract seam), label leg ridable at F.W4.** The "Terms" slider spans 2-20 while every renderer hard-caps the DISPLAY at 4 (latex_rendering.py:32/:53-55/:104/:144) and `render_latex`:280-281 accepts `budget` and never forwards it — the control's name describes a quantity it stops governing at ~20% of travel; what it governs is the energy budget. `term_count` is computed server-side and discarded (types.ts:45 declared-and-never-read; this seat's grep). Byte-identity sub-claim ("16 of 19 positions identical") KILLED — see register. **C-16 (INFO) dispositions here**: the 20-vs-50 ceiling drift is resolved at this seam, never by widening the client.
- **D-11 + C-14 — MAJOR — F.W4.** The error state: raw `text-red-400 fira-code` monospace (2.42:1 on the true plate, light), `"Failed"` as the non-Error fallback, no retry, sticky until an input moves. `text-destructive` unused tree-wide. **Cure corrected per reader-1's kill**: `--destructive` measures 4.27:1 light / 3.75:1 dark ON THE PLATE and would regress dark (incumbent red-400 = 5.16:1 there) — the cure is a fourier-local darkened arm on the `--viz-amber` precedent, not a token swap. The ApiProblem `detail`/`status`/`type` discard at :54 FOLDS to banked **fr-AdminUserList FR-AUL-50 / fr-AdminFlaggedPanel FR-AFP-22** (new call-site rider only; under C-1 the likeliest failure is a 429 whose actionable content is entirely in the dropped fields).
- **D-14 + D-L18 — MAJOR — F.W1 (uplift budget).** `MetricBadge → Metric` at producer 8.0.0 drops the `color` prop with no successor (`grep color` over metric/types.ts → 0, both readers at HEAD) and drops the glass pill plate the header composition depends on (`.metric-badge` bordered/blurred pill vs `.metric` inline-grid, no surface) — a design decision, not a rename; `./metric-badge` absent from the HEAD export map. The installed badge's `cursor-pointer` false affordance FOLDS to banked **fr-AnimationControls C-28** (GLASS-RELAY). Provenance drift recorded: the D axis quoted 7.0.0 shapes (`MetricTextProps`/`orientation`); 8.0.0 uses `posture`.
- **D-L1 + C-23 — MAJOR — F.W4.** `loading` is not request-scoped: `abortable("eq-simplify")` cancels the predecessor strictly AFTER the successor's synchronous `loading = true`, so A's `finally` clears B's spinner while B is on the wire — stale equation with no in-flight affordance. The debounce spaces starts, not durations. House cure = workspace.ts revision counters. (One corpus filed this identical mechanism at MAJOR and INFO — L's severity is correct.)
- **D-L5 + C-1 — MAJOR (C's BLOCKER demoted; reader-2's hardening adopted) — F.W5-W8; extends banked intake R6-8.** 401-1001 coefficient rows re-uploaded whole per debounce tick to move one integer, against `compute_rate_limit = 5`/60s, behind a fetch core that sleeps ≤2×30s inside the same `loading` the spinner reads; the operation is pure (no clock/RNG/DB), so the client is coupled to the server for zero server-owned knowledge — the identity-addressed sibling (`POST /api/contours/{hash}/compute/epicycles`) is the house shape. **Hardening**: the SAME limiter governs the epicycle compute routes, and `epicycleData` in the watch source means one recompute costs two slots.
- **D-1 (coarse arm, as re-derived) — MAJOR — F.W4; magnitude → SS-13.** On coarse pointers the panel's `top: 3.5rem` sits inside the dock's rendered extent under every surviving derivation (0.36rem by the summary-content model; ~0.80rem by the stable-plate model; this seat's ruling above), with total horizontal overlap below 1024px (full-bleed panel vs right-anchored/centred dock) and the panel winning the z-tie by DOM order. The BLOCKER is dead; what survives is coarse-only partial occlusion of the dock's bottom edge plus the root cause: **`3.5rem` is a magic constant privately duplicating a `--dock-scale`-responsive token chain from another package with nothing linking them** (that constant leg books MINOR and is the cure surface — derive the offset or anchor the panel below the dock's box).
- **M-N2 (reader-1 missed M1, ratified by this seat's reads) — MAJOR — F.W4; live magnitude → SS-13.** The panel clamps three axes and not the one that clips its subject: `.eq-panel` declares `top`/`left`/`max-width` and **no `max-height`, no `bottom`, no overflow of its own** (:118-125, whole style block read), inside `.viz-panel-right { overflow: hidden }` (VisualizationView.vue:374-386, my read) — so on a short stage the KaTeX box is silently cut with no scroll affordance, and the space it grows into is occupied by `.controls-overlay` at the same `--z-controls` declared later (:412-417/:235). The D axis PRAISED the width clamp (S-4) as "the piece that does not break" while the height has no relationship at all; all three axes read the same nine lines. Registry-swept by reader-1 (not fr-ConvergenceLegend D-L3 / fr-CoefficientsPanel FR-CP-8 / fr-BasisCanvas); net-new.

### MINOR (21)

- **D-12 + D-L13 — MINOR (latent) — F.W4.** The `:41` early-return preserves stale latex/energy/error; no empty state. Unreachable from the live API (epicycles.py:81-82 always appends DC); reachable only via hand-edited/corrupt draft (markRaw restore, no validation). The L demotion overrules D's MAJOR.
- **D-15 — MINOR — F.W4.** `rounded-xl` + literal rgba shadow where `--radius-*`/`--glass-*` are in scope (:119/:124). Objective half only.
- **D-17 + C-12(cure leg) — MINOR — F.W4; scope FOLDS to banked fr-ConvergenceLegend SC-4.** Inline `scrollbar-width: thin` (:106) — the one declaration no theme/token/fallback can reach; `./fading-scroll` + `./pulse` live in the installed export map.
- **D-18 + M-N5a (reader-1 missed M5, spec-verified) — MINOR — F.W4.** Nested scrollers, and TWICE what D filed: per CSS Overflow L3, `.katex-display { overflow-x: auto; overflow-y: visible }` (style.css:64-69, my read) computes the `visible` axis to `auto` — a two-axis scroll box inside the wrapper's two-axis `max-h-32` box; the app entry's stated intent (let tall glyphs escape) is silently defeated at all four display-math sites.
- **D-19 + C-13 — MINOR (one row, ruled above) — F.W4.** `h-3.5 w-3.5` is inert (the `size-*` opt-out mechanism); painted glyph = `--ui-glyph` 16px fine; true icon-to-box ratio 0.67 vs the dock's 0.45 — outlier confirmed with a larger margin than filed, off dead-code inputs. Post-uplift re-check rides F.W1.
- **D-20 — MINOR — F.W4.** `(energy * 100).toFixed(1)` ⇒ permanent spurious decimal `100.0 %` in the most common state, costing real badge width below the 8rem cap. Adjudicate WITH M-N3 (the threshold disagreement).
- **D-21 + D-L8 + D-L9 + C-3 + C-4 — MINOR (C-4's MAJOR overruled) — F.W4.** `trust: true` is provably unnecessary at this call site (the only trust-gated commands live in the sigma renderers this endpoint never reaches — both readers closed the chain) and widens KaTeX's URL-command surface into a `v-html` sink; the `catch` fallback beneath `throwOnError: false` is effectively dead code that interpolates raw. One-line cure; the shared-helper cure (D-L7) makes the trust policy a one-site decision.
- **D-L2 + C-17 — MINOR (ruled above) — F.W4.** The armed vueuse debounce timer survives unmount and fires a real POST (no `tryOnScopeDispose` on that bundle path; zero unmount hooks in the file); the consumed store demonstrates the exact cure at workspace.ts:74-76.
- **D-L3 — MINOR — F.W4.** No unmount abort; `eq-simplify` appears in no `abortInflight` list (this seat's grep: 1 occurrence tree-wide); burns a bounded compute-pool slot.
- **D-L6 + C-9 — MINOR (ruled above) — F.W4.** No same-input dedup; the sibling caller of the same client function has the guard AND the tree ships an unused session-storage memo (useEquationCache.ts, imported only by EquationView).
- **D-L7 + C-19 — MINOR (ruled above) — F.W4.** 4 KaTeX render sites, 3 sharing the identical option triple, 0 shared helper; the colocation target (lib/equation/) is already imported by this file.
- **D-L10 + C-10 — MINOR — F.W5-W8.** `notation` widened to `string` and re-narrowed by unchecked `as` in a file whose header claims the class was closed structurally; both callers already hold `NotationMode`; failure surfaces only as a raw-rendered 422.
- **D-L15 — MINOR — F.W4.** Component-owned notation/budget destroyed by the parent's triple-condition `v-if` per toggle; `immediate: true` re-fires the full payload; the asymmetry is sharp — `showEquation` IS persisted, the panel's own controls are not.
- **D-L17 — MINOR — F.W0 (substrate).** Zero automated coverage and no unit runner exists in `web/` to add any (scripts census re-run by this seat); `EquationPanel|eq-panel` in e2e → 0. Fair causal claim: the state-machine MAJORs are exactly what a mount test catches.
- **C-7 — MINOR (a11y consequence killed, both readers + this seat concur) — F.W4, F.W1 rider.** `h-6 w-6` defeats the Button size variant via the cn conflict table (mechanism confirmed); the coarse floor holds regardless via the reflected-attribute rule today and via `data-control-target` after the uplift (this seat's producer read). `size="icon-sm"` is the unused rung. Same shape as banked fr-ConvergenceTimeline D·D-5.
- **C-8 — MINOR — F.W4.** Zero props: the input contract is a global singleton asserted twice in two shapes (parent truthiness vs child `?.components.length`); the honest contract is the one prop `:46` already passes.
- **C-20 — MINOR — F.W1; identity FOLDS to banked fr-AnimationControls C-1 rider / fr-AppHeader:69.** Runtime `lucide-vue-next` import resolved from devDependencies (my package.json read), in a tree where the installed 4.0.0 ALREADY peers `@lucide/vue` — a live two-icon-package divergence at the current pin; the "7.0.0 rename" framing is stale (killed register). The devDep-misclassification leg stands here.
- **M-N1 (reader-2 missed, ratified by this seat's notation.ts read) — MINOR — F.W2/F.W4, binds the D-3 cure.** The three energy-ramp literals are byte-duplicates of the three TIER_INFO literals in the same 48-line file (:26≡:45, :32≡:46, :38≡:47) — two semantically independent ramps pinned together by copy; a D-3 contrast repair applied once silently diverges the other table. This is the concrete defect behind the D·S-6 kill.
- **M-N3 (reader-2 missed, arithmetic re-verified by this seat) — MINOR — F.W4, adjudicate with D-20.** Displayed rounding and the colour step function disagree at both thresholds: `toFixed(1)` maps [0.98950, 0.99050) to `"99.0"` while `energyColor` splits that band at 0.99 (and identically at 0.95) — two byte-identical readouts paint two different colours on a band ordinary slider travel crosses. One-line cure (threshold the rounded value). No axis joined D-20, D-3 and D-L4.
- **M-N6 (reader-1 missed M3, ratified) — MINOR — F.W4.** The notation.ts:3-8 docstring's `.basis-toggle`-match claim is false on FOUR axes, not the one the corpus caught: no `aria-pressed` (D-2); `min-width 4.5rem` vs the sibling's deliberate 5.5rem; no 2px border weight (named deliberate at BasisSelector.vue:254); no muted rest ink (:262, my read) — two "matching" pill families at different weights, widths and border masses, in a docstring the corpus reads as a contract.
- **M-N7 (reader-2 missed, verified by this seat) — MINOR — F.W0/F.W1 (dependency hygiene); cure is a deletion.** `@types/katex@^0.16.8` shadows katex 0.17.0's own bundled `types/katex.d.ts` (both read by this seat) — dead weight at best, silent type-skew at worst on exactly the `trust`/`throwOnError` option surface this corpus litigates. Registry-swept net-new.

### INFO (8)

- **C-16 — INFO (ruled above)** — contract drift 20-vs-50, dispositions with D-10 at F.W5-W8.
- **C-18 — INFO — F.W2; folds to the banked census identity (lane-frontend.md:480).** `energyColor` is a fourth independent colour island; this component consumes value.js zero times.
- **C-21 — INFO — F.W5-W8.** Half-guarded chain rests entirely on the DTO contract; `(await res.json()) as T`, no envelope validation.
- **C-24 — INFO — F.W4.** Legacy `modelValue` pair where `defineModel()` is pinned-supported.
- **D-L11 — INFO — F.W4.** Fresh-tuple watch source: change-detection is accidental, not semantic; house habit (EquationView same shape).
- **D-L19 — INFO — F.W4.** The two consumers of one client function have opposite error postures and the seam documents neither; the panel's is the better half.
- **L·§3c — INFO — NO-WAVE-OWNER (SS-3/SS-4 spec-authoring input).** Fourth derivation-blindness class: runtime `v-html` subtrees (10 sites / 6 files, re-measured) invisible to both R5's callsite model and R6's NATIVE_TEMPLATE_LOOP cure; ≥2 independent members — the R5-7 bar. Method row, not a component defect.
- **D-16 (residue only) — INFO — F.W4, note on the D-2 cure.** Both mechanisms killed (register); what survives is bare register-mixing, and the genuinely fluid axis in the subtree is the PILLS (`--control-text` clamp), not the badge.

### FOLDED identities (by reference — never re-booked)

**D-13** → fr-ConvergenceTimeline D·D-2 (F.W1; member sites recorded; second-order arm KILLED here). **C-2** → census §1 pins + G33 / fr-AnimationControls C-1 / fr-BasisCanvas C-8 (INFO at component altitude; **rider relayed to the banked row**: producer-HEAD peer is `@mkbabb/value.js ^4.0.0` — the F.W1/F.W2 gap is two majors wider than the corpus records). **MetricBadge cursor-pointer** → fr-AnimationControls C-28 (GLASS-RELAY). **ApiProblem detail-discard** → FR-AUL-50/FR-AFP-22. **scrollbar-width census** → fr-ConvergenceLegend SC-4. **outline-opaque-rest** → fr-BasisSelector M-1 (second member). **lucide rename framing** → fr-AnimationControls C-1 rider / fr-AppHeader:69. **M-N5 (reader-1 missed M4) — fold-AND-AMEND**: EquationPanel.vue:70 is a SECOND `glass-wash`-as-overlay (`position: absolute`, z-tokened, over the live instrument), falsifying fr-ConvergenceLegend's *"the legend is the sole wash-as-overlay"* cell — the banked `glass-floating` rung ruling (with its U-3/SR-1 re-measure rider) now binds two files, and the two members must not land different cures.

## Killed-claims register (17)

1. **D-1 as BLOCKER (both arms as filed).** The 1.875rem coarse figure re-derives the regression the producer FIXED (`--dock-coarse-scale 0.78`, overflow.css comment verbatim — this seat's dist read); ≥1024px is horizontally disjoint; "covers the toggle that spawned it" unestablished. Survives as coarse-arm MAJOR + magic-constant MINOR. **Both re-readers' fine-collapsed arithmetics are additionally impeached by this seat** (the stable-plate morph contract, ruled above) — the fine arm is UNPROVEN in BOTH directions, not killed-with-clearance as reader-1 filed.
2. **D-13 second-order touch-floor arm** ("the single highest-risk line in the migration") — killed by this seat's own producer read: `data-control-target=""` unconditional at 8.0.0 + the responsive.css coarse floor. Reader-2's CONFIRMED-AND-HARDENED verdict on this arm is overruled.
3. **D-16 (both mechanisms).** `text-sm` and `--type-micro` are both rem-relative; the 0.875/0.6875 ratio is viewport-invariant; nothing inverts. The falsifier misread relative-to-root as viewport-relative; residue INFO.
4. **C-7's a11y consequence** ("24px on every pointer type") — the coarse floor keys on the reflected `data-size` attribute, `min-block-size` outranks `height`; 44px today. Mechanism survives at MINOR.
5. **D-2's light-mode contrast table + falsifier arithmetic** — killed in the direction that makes the defect WORSE: the 12% self-hue tint over the wash plate moves ratios by 1.06-1.26 (not "well under 0.2"); trig (the default) and polar cross below 4.5 in light. Finding hardened, table dead.
6. **D-11's `--destructive` one-word cure** — 4.27:1 light on the true plate (still failing) and a 1.4-point dark-mode REGRESSION vs the incumbent. The defect stands; that cure must not ship.
7. **D-7's one-attribute cure** — the label slot is gated on `labelPosition`; `abbreviation` alone renders nothing.
8. **C-12's premise** ("animate-spin ignores reduced motion") — inverted: the producer's universal PRM kill reaches it through the entry import; the real defect is D-8's frozen-arc, which is booked.
9. **C-2 as a component BLOCKER** — 4th independent reproduction of a banked repo-level identity; folds, with the ^4.0.0 peer rider relayed.
10. **D-19's arithmetic** (14px numerator; "0.58 outlier") — the authored class is inert; conclusion survives at MINOR on corrected numbers. Reader-2's total kill overruled (ruled above).
11. **D-10's byte-identity sub-claim** — `truncate_by_budget` re-sorts by `(abs(n), -n)` after amplitude selection, so raising the budget can change the visible prefix; holds only for amplitude-monotone spectra. Headline unaffected.
12. **D-2's "three identical unlabelled buttons" sub-claim** — the pills compute names from content; the row is unconveyed STATE (4.1.2), not unnamed controls.
13. **D·S-6 superlative as stated** — killed by this seat's own read (six duplicate literals; the cited grep names the wrong line). Reader-1's ratification overruled; downgraded to "colocation right, single-authority false" and replaced by M-N1.
14. **C-16 as an under-exposure defect** — direction inverted; widening strictly worsens C-1/D-10. INFO drift.
15. **L·§4b's frequency premise** — a bit-identical `latex` reassignment triggers zero re-render (Object.is gate on the string ref); the wasted round-trip is D-L6's, the render cost is not. The structural same-thread claim survives inside D-L5/C-1's routing.
16. **C-20/D-L18's "glass-ui 7 renames to @lucide/vue" framing** — stale: the installed 4.0.0 already peers `@lucide/vue` (this seat re-read the installed package.json); a live divergence at HEAD, banked as the fr-AppHeader reconciliation rider.
17. **D-1's fine-pointer "4px inside even on desktop"** — ≥1024px the two are horizontally disjoint by the media-query pin (my read of :467-473); no desktop collision exists on any stage wider than ~30rem. (Sub-1024 fine-expanded remains UNPROVEN, folded into the D-1 residue.)

## Superlatives (adjudicated, provenance preserved)

- **RATIFIED (D S-5 / L S-L1 / C S-1, three independent finds)** — the abort seam and its consumption: `isAbortError` discriminated before ANY error write, riding the canonical per-key `abortable()` registry; with a 300ms debounce guaranteeing cancellation per drag, omitting that guard would paint the red error every drag. The file gets the error half right and the flag half (D-L1) wrong — a precise, separable distinction.
- **RATIFIED (D S-1 + reader-2's extension)** — the mount transition is opacity-only with tokened easing, exactly the property set the producer's PRM policy preserves; four transform-bearing siblings in the same stylesheet were correctly not used. Deliberate, and drawn correctly once while the spinner (D-8) misses the same distinction once.
- **RATIFIED (D S-3 / C S-4)** — both slider controls named from one string (implicit label + `:aria-label`), correct explicit `:model-value` pair for a scalar-adapting wrapper — the exact pattern the pills failed to reach, two elements away.
- **RATIFIED (C S-2 / reader-2 S-5)** — `color="var(--viz-fourier)"` passes the custom property itself into a `color-mix()` composition, dodging the `cssVarToHex` resolver's `#888888` fallthrough entirely; the correct consumption is the one that stays declarative. (The resolver-overwrite inference stays UNPROVEN; the superlative doesn't depend on it.)
- **RATIFIED (D S-2)** — `aria-label="Close equation panel"`, named SPECIFICALLY, keeping the R3-7a tooltip-migration budget at 35 not 36.
- **RATIFIED (reader-2, minted)** — the consumed store demonstrates the exact teardown discipline the component lacks (onScopeDispose timer clear, revision counters, `abortInflight` composition) — what makes D-L1/D-L2/D-L3 deviations from a standard, not absences of one.
- **RATIFIED (reader-1, minted)** — zero ambient registrations in 66 script lines (no rAF/observers/listeners/canvas/timers beyond the one debounce), on a route-constellation with a banked ~40-site ungated-rAF epidemic — the F.W4 frame-budget work knows where NOT to look.
- **RATIFIED (reader-2 S-4)** — the notation axis is type-exhaustive and matches the server 3-for-3 with one data table as the only edit surface — the modelling to preserve through F.W5, in the same seam that launders the type through `string` four lines away.
- **OVERTURNED — D·S-6** (see killed[13]).

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **D-1 geometry, the decisive probe**: `getBoundingClientRect` on `.controls-dock-anchor > .glass-dock` (collapsed AND expanded) vs `.eq-panel`, at a coarse phone viewport and a fine sub-1024 window — decides between the three static derivations (clearance / 0.36rem / 0.80rem) and settles the fine arm's sign.
2. **M-N2 stage clipping**: does the panel overrun `.viz-panel-right` at landscape-phone / portrait-phone / 1024-1280 stages, and does `.controls-overlay` occlude the KaTeX box before the clip does.
3. **All contrast decimals** (D-2/D-3/D-11) are modelled-plate figures; sample composited pixels under the badge, the trig pill, and the error row, both themes, animation running. The light-mode pass/fail COLUMNS survive any plausible sampling (2.09-4.27 vs 4.5); the third decimal does not.
4. **C-13/D-19**: `getComputedStyle` on the close button's svg, fine and coarse (cascade-derived, unmeasured).
5. **D-L5/C-1 wall-clock**: spinner duration on a 429; on-wire compressed byte count of the 401-row payload; per-frame `backdrop-filter` cost of the wash panel over the free-running canvas (coupling static-proven, cost not).
6. **KaTeX `v-html` node cardinality** at budget 20, all three notations (sets L·§3c's magnitude).
7. **Energy duty cycle**: how often real contours sit below the 0.99/0.95 stops — decides whether the D-3 cure priority is the green literal alone, and how reachable M-N3's disagreement bands are.
8. **D-18 double scrollbar materialisation** (low priority; D-10's cap makes >28rem equations unlikely).

## Closing verdict

**ADJUDICATED**: 44 rows survive (0 BLOCKER · 15 MAJOR · 21 MINOR · 8 INFO), 17 claims killed, 5 net-new finds banked, 8 identities folded with 2 banked-row amendments relayed (fr-ConvergenceLegend "sole wash-as-overlay" cell falsified; census C-2 peer-gap widened to ^4.0.0). The component is a well-seamed consumer of an excellent abort/transport substrate wearing nine MAJOR a11y-and-state defects on one 134-line surface — every one cheap, most one-to-six lines, and all F.W4-shaped except the three contract-seam rows (F.W5-W8), the uplift budget (F.W1), and the substrate gap (F.W0). Both re-readers were themselves defective exactly once each in a load-bearing place — reader-2 on the D-1 scale and the D-13 floor, reader-1 on the D·S-6 grep — and this file's verdicts rest on this seat's own instruments at every such point. Routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
