claude-fable-5

# kf-PlaybackRibbon — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **Registry tree**: value.js `tranche-u` · **Subject tree**: keyframes.js HEAD `8281638c` (2026-07-28), worktree DIRTY in 252 paths — and **all five audited files are byte-identical to `origin/master` `81a56990`** (per-file diff run by this seat: PlaybackRibbon.vue, AnimationVisualizer.vue, playback-idiom.css, design-idioms.css, style.css → 0 diff lines), so every anchor below is **frontier-durable by identity**. The worktree's glass-7-adoption character (HEAD's dead `variant="timeline"`, the `IconTooltip`→`Tooltip` swap) is banked at kf-AnimationVisualizer's substrate disclosure and carried by reference.
**Corpus (r1)**: two fresh hostile re-reader returns over `audit/kf-components/PlaybackRibbon/challenge-{D-design,L-library,C-consumption}.md` (375/617/595 L, all opus-5[1m]) — **reader-DU** (design-and-usage: 32 confirmed cells · 7 kills · 8 misses M-1..M-8 · 9 superlatives · 9 unproven) and **reader-LC** (library-and-code: 37 confirmed cells · 7 kills · 6 misses · 9 superlatives · 9 unproven). Both returns consumed whole; all three challenge axes read whole by this seat.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every load-bearing verdict was re-derived at the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of PlaybackRibbon.vue (245 L — every cited anchor checked), playback-idiom.css (87 L), AnimationVisualizer.vue :1-256 (the gate at :230-235, the guard at :241-250, the comment at :238-239, coast :172-226, dispose :255); design-idioms.css :70-129 (:76-79 focus-ring copy, the `@utility` census incl. icon-md/lg); style.css :40-260 (`@theme` opens :42, `:root` opens :91 — `--accent-kf` family + `--accent-kf-foreground` + `--primary` repoint + `--color-progress`/`--color-slider-track` all inside `:root`; the `--glass-tint-strength-aa: 0%` block ~:195-208; `.is-disabled` in `@layer utilities`); **installed glass-ui 7.0.0 dist, decisive extractions**: styles/index.css import order (glass.css = #4 → ladder.css; components/button/styles.css = #24; accessibility.css = #37, no `layer()` clause, itself importing utilities/a11y-overrides.css), glass/ladder.css (`@layer components`, `.glass-wash { border: 1px solid var(--glass-border-accent) }`), components/button/styles.css (`@layer components`, `.button { border: 0; font-size: var(--control-text); min-block-size: var(--button-size); --scale-press: var(--scale-press-btn) }` + `.button > svg:not([class*="size-"]) { inline-size: var(--ui-glyph) }` + tone-gated border), button-B7c944jy.js (props `emphasis:{default:"secondary"}, tone:{default:"neutral"}`; class `"button tap-squish focus-ring" + glass-wash glass-capsule`; `"data-press-armed": armed ? "" : void 0`; `style: pressStyle`), useLiquidPress-D0PFjKuk.js (`g = ref(false); onMounted(() => g.value = true)`; pressStyle emits inline `scale: "${(1−e·0.03)·n} ${(1−e·0.03)/n}"` past the 0.001 threshold), utilities/base.css (`.tap-squish:not([data-press-armed]):active { scale: var(--scale-press) }` AND **`.focus-ring:focus-visible { outline: none; border-radius: var(--radius-pill); box-shadow: var(--focus-ring-shadow) }` — the K-5-extending find**), utilities/a11y-overrides.css whole (forced-colors list verbatim, no slider selector; PRM allowlist without `transform`), accessibility.css (`[aria-pressed="true"]` forced-colors Highlight + prefers-contrast currentColor blocks), theme/bridges.css (`--transition-duration-fast: var(--duration-fast)` inside `@theme inline`; tokens/scheme-motion.css `--duration-fast: 0.2s`), slider-DDia69Fy.js (thumb forwards exactly `aria-label/-labelledby/-describedby/-errormessage/-invalid` — **no `aria-valuetext`**; `inheritAttrs` grep → **-1/absent**; `variant:{default:"standard"}`; `step: {}` bare; internal useTouchGate wiring + `data-touch-active`), useTouchGate-B4mzQcHJ.js whole head (module-level `Set` + shared document `touchstart` sweep; `l = "ontouchstart" in window` sole device gate; `S()` false-on-first-press; activate sets `touchAction="none"`, deactivate clears to `""`), glass-ui.css 41-slider-rule extraction (rules [0] touch-action / [12] thumb width:0 / [13] non-spectrum hover / [14] focus-within box-shadow-only / [19] data-disabled / [26] spectrum thumb-bg — all reproduced), tokens (sizing.css `--control-h-md`/`--control-text`/`--ui-glyph`; light-dark.css coarse `--ui-scale: 1.5` + `--control-floor`; scale-paper.css `--scale-press-btn = --scale-press-sm = 0.97`; liquid-fill backdrop-filter chain + `--liquid-fill-strength, 88%`; dark-arm `--glass-blur-quiet … brightness(1.16)` vs light `brightness(1.02)`); **reka-ui dist**: SliderThumbImpl (role/tabindex/aria-valuenow-min-max, no valuetext; `$attrs["aria-label"] || label.value`), utils.js `getLabel` (void 0 for one thumb); **engine/mounts**: option-setters.ts applyDuration (in-place `anim.options.duration = d`), animation.ts :95-96 (raw `set reversed`, no shift) + :270-273, play-lifecycle.ts :95-102 (`reverse()` shifts startTime) + :448-450 (`effectiveT`), useEasingDemo.ts (:289 markRaw, :317 setDuration watch, :328 raw `previewAnim.t =`), EasingScene.vue :81/:84-91/:100/:102, useSpringDemo.ts :240/:288, SpringScene.vue :119-122, useAnimationSync.ts :33/:44 (effectiveT — the channel pin), ChannelOptions.vue :36/:391/:495; **seat censuses**: `btn-interactive` = 8 lines/6 files (matches the banked figure), `touch-gate` = 4 demo call-sites/0 rules, comment-only lines in PlaybackRibbon.vue = **88 of 245 ≈ 36%** (rule: full-line comments incl. JSDoc, template and style blocks; per-block: 15 template + 48 script + 25 style); **registry cross-reads (the identity-guard sweep)**: kf-AnimationVisualizer.md whole (KF-AV-1/-2/-3/-7/-8/-9/-11/-14/-15/-16/-18/-32/-38 + SUP-1/SUP-2 + the relay obligations + the substrate disclosure), kf-ChannelOptions.md KF-CO-15 + its §B permanence correction, kf-EasingScene.md (KF-ES-2's cure observation, KF-ES-4/-5/-6/-22), kf-ControlsPaneWrapper.md D-M11/D-m12, kf-CubeScene.md D-11, kf-CSSCodeEditor KF-CE-13, fr-AnimationControls.md L-1/L-3/K-4/K-5 (the constellation valueCommit/keyboard-session pattern), fr-App.md MG-ζ (the forced-colors tempering that does NOT apply here), CENSUS-2026-08-03.md wave taxonomy (KF.W0 §B-12 · W4 · W6 · W7 · W8 · W9).

## Scoreboard

**38 adjudicated roster rows — 4 BLOCKER · 9 MAJOR · 16 MINOR · 9 INFO — plus 8 folds-by-identity into banked records (zero re-bookings).** **13 killed-claims register entries** — the readers' 7 kills all SUSTAINED (two extended by this seat), 4 seat-minted kills against reader cells or corpus prescriptions, 1 grouped BOOKING-KILL entry covering **thirteen** identities the readers filed without reading the bank, 1 axis false-negative recorded. **12 of the readers' 14 filed misses ADMITTED** (5 become N-rows, 4 fold as cures/riders into existing identities, 3 fold to banked rows); 2 booking-killed. **10 superlatives sustained** (one reader's "NEW" superlative was itself banked — SUP-1 at kf-AnimationVisualizer). **10 UNPROVEN residues → SS-13.** Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input keyed to the census taxonomy, with **NO-WAVE-OWNER** marking the transport-packet rows for SS-1/SS-2 authoring.

**Headline.** Three results. (1) **The border question is settled against the D axis and reader-DU**: the transport buttons have **no border at all** — `.glass-wash { border: 1px solid … }` (ladder.css, `@layer components`, via import #4) loses to `.button { border: 0 }` (button/styles.css, same layer, import #24) on source order, `tone` defaults `"neutral"` so the tone border never engages — reader-LC's missed row is confirmed at the bytes, five `border-color` declarations plus one Tailwind utility are dead, and reader-DU's D-8 cell repeating the axis's ".glass-wash's 1px hairline" is this corpus's premise-inheritance event. (2) **The registry's recurring failure mode fired thirteen times in one corpus**: both readers "discovered" the frozen-duration BLOCKER (banked 5 days earlier as KF-CO-15, extended at KF-ES-4), the fling-drops-dragEnd MAJOR (≡ KF-AV-2), the frozen-twin MAJOR (≡ KF-AV-1), the phantom touch-gate classes (≡ KF-AV-7), the false "ONE motion-color identity" (≡ KF-AV-11), the 24px non-twin geometry (≡ KF-AV-14), the `bumpLayoutEpoch` boundary breach (≡ KF-AV-8/-9/-18), the 8-site `btn-interactive` phantom (≡ KF-CPW D-M11 / KF-CS D-11), the mutual-deactivation gate census (≡ KF-AV-32), the false "Always poll" comment (inside KF-AV-1's own proof), the mirrored easing mount (≡ KF-ES-5), the prose-only either-or contract (≡ KF-ES-22) — and reader-DU's flagship "NEW (mine)" superlative, the rail-geometry identity, is kf-AnimationVisualizer's banked SUP-1. All are recorded as independent confirmations; none books a row. (3) **The bank kills a corpus prescription**: L's disposition "drop the unwired `source` branch" dies against banked KF-ES-2's cure observation, which *consumes* the `source` seam as the facility-honesty repair — the branch's defects (type drift, the un-inverted reversal limb reader-LC found, C-4's step degeneracy, KF-ES-22's untyped either-or) are hereby re-filed as **preconditions of that cure**, not deletions.

## Ruled disagreements and seat rulings (the incredulity seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **The border premise: reader-DU's D-8 cell ("the only width is `.glass-wash`'s hairline") vs reader-LC's missed row ("the buttons have NO border")** | **LC WINS — no border exists; DU's cell KILLED (K-8) as premise-inheritance; N-1 admitted at MINOR (LC's MAJOR as dissent)** | My import-order extraction: styles/index.css lists `./glass.css` at position 4 (→ glass/ladder.css, `@layer components`, `.glass-wash{…border:1px solid var(--glass-border-accent)…}`) and `../components/button/styles.css` at position 24 (`@layer components`, `.button{…border:0…}`). Same layer, both (0,1,0) → source order → `border: 0` wins and resets border-style. `tone:{default:"neutral"}` (button-B7c944jy.js props, my read) keeps `.button:not([data-tone="neutral"])`'s 1px unreachable; `.glass-capsule` declares no border. Dead as a result: playback-idiom.css :31/:46/:67/:86 `border-color` + PlaybackRibbon.vue:56 `aria-pressed:border-primary/40`. Severity: the pressed-state harm is already priced in D-8 (whose contrast figures were computed background-only and therefore **do not move**); N-1's own content is a dead-declaration family plus false hover narration ("stronger border") → MINOR, LC's MAJOR preserved as dissent. DU's cell quoted the fact that refutes it (`.button { border: 0 }`) in the same sentence as the inherited conclusion — the fr-DarkModeToggle SC-3 class of event. |
| 2 | **D-3 scope + severity: DU BLOCKER (+ its missed M-1 demo-wide BLOCKER) vs LC MAJOR** | **ONE identity, booked at the demo-wide scope (M-1 is the home, D-3 the component witness) — MAJOR, blocker-in-mode; DU's BLOCKER preserved as dissent. K-5 SUSTAINED AND EXTENDED: BOTH demo copies are wholly redundant — the cure is two deletions, zero replacements** | Both readers' facts verified (unlayered (0,2,0) ties, resolved source order style.css:3 → :14 → design-idioms:6/:76). My extension: glass-ui's own utilities/base.css ships `.focus-ring:focus-visible { outline: none; border-radius: var(--radius-pill); box-shadow: var(--focus-ring-shadow) }` (import ~#22) and its a11y-overrides forced-colors arm arrives later via accessibility.css (import #37) — **the producer pair is self-consistent**; the demo's design-idioms.css:76-79 copy re-asserts `outline:none` after the remedy and playback-idiom.css:72-75 is a third copy. Delete both; the producer already paints the normal-mode ring on every `.focus-ring` element demo-wide AND restores the outline under forced-colors. Scope law (ColorSpaceSelector addendum 1): the count lives demo-wide → booked there. Severity: total loss of focus indication, but only within forced-colors mode → MAJOR with blocker-in-mode noted. Cross-refs: KF-CE-13 (the demo-wide *categorical* forced-colors gap — sibling class, distinct mechanism: this row is an authored defeat); fr-App MG-ζ's tempering ("forced-colors coerces outline-color regardless") does NOT apply — that case substituted an outline, this one removes it. |
| 3 | **Thirteen registry-blind filings (both readers)** | **BOOKING KILLS (K-12), grouped — every one re-verified at the bytes and recorded as an independent confirmation of its banked row** | Enumerated in the headline and the folds section. The sharpest: L-B1/C-1 was filed BLOCKER by both readers with full falsifier batteries — and KF-CO-15 (2026-08-07, kf-ChannelOptions) already names "PlaybackRibbon.vue:119-121's computed over markRaw options consumed at :19" as the root, KF-ES-4 already extends it with the hotter easing writer, and the kf-EasingScene §B correction already fixed the permanence claim ("self-healing on a tab bounce; permanent within an uninterrupted Controls session" — both readers' "never until remount" is the uncorrected form). What THIS corpus adds to that identity is real and is carried below: the **signed divergence** (stale `:max` × fresh reversal pivot → negative `rawT` seeks) and L-m9's two-freshness mechanism. |
| 4 | **C-2's home and grade (both readers BLOCKER) + the L axis's opposite cell** | **BLOCKER, booked HERE as the ribbon-owned contract root; the easing manifestation folds ≡ KF-ES-5 (MAJOR there, scene-scoped); the spring manifestation is this record's witness (no kf-SpringScene bank exists); the L axis's §1 cell "the raw/effective mismatch … is absent" is recorded as an axis FALSE NEGATIVE (K-13)** | Every link re-verified: play-lifecycle :448-450; animation.ts :95-96 raw setter (no clock shift) vs `reverse()`'s shift (:95-102); useAnimationSync :33/:44 effective (the channel pin — and kf-EasingScene's corrections register already crowns the teleported ChannelOptions instance "the reference transport… the convention pin"); EasingScene :100 + useEasingDemo :328 raw; SpringScene :119-120 + useSpringDemo :240/:288 raw; AnimationVisualizer :246 reads `effectiveT` directly → mirrored slider/ball under Reverse at 2 of 3 mounts, mirror-jump on release. The ribbon owns the defect by the majority rule (three authors, one bare `number`, two wrong) and by asymmetry of knowledge: only `scrubTo` knows it inverts (:183-185) and the prop doc gives `source` eleven lines while `currentT` gets zero (:111). |
| 5 | **The `source`-branch disposition: the corpus's deletion prescription vs the bank** | **Deletion KILLED (K-11); D-23/L-M2/C-12 RESCOPED to MINOR: dead-today scaffold whose defects are PRECONDITIONS of the banked KF-ES-2 cure** | kf-EasingScene KF-ES-2 (BLOCKER, unanimous) carries the cure observation: "the in-tree shape already exists — `surfacesFor` honors a light channel's declared `surfaces`, and PlaybackRibbon accepts a `source {progress, setProgress, duration}` — the channel already carries `progress`/`setProgress`." A light-channel consumer is therefore *planned*, not hypothetical. Surviving defects, all verified: the inline type drifts from transportSource.ts:24-33's `TransportChannel` (adds `duration?`, drops `name`, demands a `progress()` the file never calls); **reader-LC's missed reversal-skip is admitted as the branch's second latent defect** (PlaybackRibbon :195-199 seats `setProgress(effectiveT/dur)` with NO reversal inversion while the `animation` limb inverts — same parameter, same rail, same declared contract); C-4(b)'s min=0/max=1/step-default-1 degeneracy; the either-or invariant ≡ banked KF-ES-22 (fold). The readers' INFO/MINOR split resolves MINOR. |
| 6 | **D-25's comment count: DU 65/245 (26.5%, "L-i3 overcounts by ~25") vs LC ~85/245 (~35%)** | **LC WINS — seat count 88/245 ≈ 36%; DU's cell KILLED (K-9)** | Counted by this seat with a stated rule (full-line comments incl. JSDoc, excluding blanks): template 38-45 + 46-52 = 15; script 85-87, 99-103, 108, 117-118, 126-127, 136-147, 153-166, 171-172, 187-190, 196-197, 202 = 48; style 208-229 + 235-237 = 25. Total **88**. The L axis's "~90" was nearly exact; the D axis's 63 and DU's 65 understate by a third. Direction unchanged (comment mass dominates and the biggest blocks are false); the number is now pinned. |
| 7 | **Grade splits: D-4 (DU MAJOR / LC MINOR) · D-12 (DU MAJOR / LC MINOR) · D-19 (MINOR/INFO) · D-21 (MINOR/INFO) · D-23 (INFO/MINOR)** | **D-4 → fold ≡ KF-AV-11 (MAJOR at its banked home; nothing re-graded here) · D-12 → MINOR at this record, GLASS-OWNED (DU's MAJOR as dissent) · D-19 → INFO · D-21 → INFO · D-23 → MINOR (ruling 5)** | D-12: same failure class as row 2 but producer-owned and single-control; the verdict class follows the cure's owner (ColorSpaceSelector L-10 precedent) — BH relay, with the demo-side `@media (forced-colors: active)` outline on `.timeline-green :deep(.slider-track)` as the named stopgap. D-19: one file, zero external consumers of the literal (`grep` = this file only), naming-only. D-21: facts exact, remedy a design judgment, no token authority violated. |
| 8 | **Reader-DU's M-4 (press-affordance doubling) vs the jointly-killed D-18** | **K-1 SUSTAINED (both readers' identical specificity arithmetic re-verified at the file); M-4 ADMITTED as N-3 (MINOR) — every link byte-verified by this seat** | playback-idiom.css :44 `.btn-playback-accent:hover` = (0,2,0), TIES :76 `.btn-playback:active` (0,2,0), `:active` later → wins on Play/Pause; :65's `:not()` hover = (0,3,0) shadows it on Reverse — D-18's "never lands on a mouse press" is half-wrong exactly as both readers said. The mechanism DU adds is real: useLiquidPress arms on mount (`onMounted(() => g.value = true)`), Button always stamps `data-press-armed` → `.tap-squish:not([data-press-armed]):active` is permanently disabled; the live press is the spring's INLINE `scale` (pressStyle emits `scale: "${(1−e·0.03)·n} …"` — my dist read), which composes multiplicatively with the demo's `transform: scale(var(--scale-press))` (`--scale-press-btn` = 0.97, my token read) → ≈0.941 on Play/Pause, while Reverse gets lift + the producer's ~0.97 only. Two cells, two press physics. Cure: delete playback-idiom.css:76-78. |
| 9 | **Reader-LC's third-gate prescription ("the deletion L-M1 prescribes must cover the visualizer too")** | **Census fact folds ≡ KF-AV-32 (booking-kill); the prescription is KILLED (K-10)** | The visualizer's instance is the composable's documented per-control use ("Per-control tap-to-activate guard"), it manages `touch-action` correctly (`:11` dynamic `pan-y` — the C-5 control case), and its actual defects are already banked: KF-AV-3 (no pointerType branch — re-derived independently by this seat at :230-235 before the bank check caught it: a third derivation) and KF-AV-32 (mutual deactivation via the module-level Set — which this corpus upgrades from two instances to **three**, glass-7's Slider-internal gate included, all registered in one shared document sweep). L-M1's cure deletes exactly one gate: the ribbon's wrapper. |
| 10 | **Reader-LC's D-2 dark-arm correction (≈1.73:1, not 1.30:1)** | **ADMITTED as an evidence rider on D-2 — the BLOCKER is unmoved; no dark figure from the axis may enter a repair spec or acceptance test** | Token bytes verified: `.slider-range` carries `.glass-liquid-fill` (slider chunk), whose backdrop-filter resolves `--glass-blur-quiet`; dark arm declares `… brightness(1.16)` vs light `brightness(1.02)` (my reads of tokens/dark-arm-glass.css and tokens/glass.css) — the axis's plain alpha-over model omits it. The exact 1.73:1 is analytic (compositor behaviour not statically exact) → bracketed, SS-13. Light figures stand: both readers independently reproduced 1.23:1 byte-for-byte from the token graph. |

**Adjudicator's corrections register (against this corpus's readers):** (1) DU's D-8 border cell — K-8, premise-inheritance; (2) DU's D-25 count + its "L-i3 overcounts" clause — K-9; (3) LC's visualizer-deletion prescription — K-10; (4) both readers' registry-blind filings ×13 — K-12; (5) LC's L-M1 cell "proper `onUnmounted` removal" — it is `onBeforeUnmount` (LC self-noted; recorded); (6) DU's M-2 cell "the worst of the three outcomes" (valuetext lands on the role-less root) — confirmed: the slider chunk declares no `inheritAttrs` (grep = absent) and imports `useAttrs`, so fallthrough to the single root holds; (7) DU's D-9 phrasing "the arrowheads swap ROWS" and LC's "opposite diagonal" describe the same C2-symmetry fact — harmonized, no conflict; (8) the D-10 contrast third-decimal (DU "EXACTLY 3.58" vs LC 3.567) — immaterial, two independent recomputations agree to 0.02 over verified token bytes.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W4/W6/W7/W8/W9** per CENSUS-2026-08-03 (§B-12 substrate · gates · Glass Suffusion · Timeline Evaluate · Structure & Colocation · Safari Visual Audit); behavioral repairs with no census owner = **NO-WAVE-OWNER (SS-1/SS-2 spec input — the transport/ribbon packet)**. Standing rider: **KF.W7's timeline-evaluate verdict may supersede any NO-WAVE-OWNER cure below** (the KF-AV-28 rider); note the C axis's own §7 caution rides with it — this ribbon is the NON-bespoke case (it consumes the real `Slider`) and carries BLOCKERs anyway, so "swap onto the primitive" is never sufficient as a cure. Anchors bind to worktree ≡ `origin/master` `81a56990`; re-resolve before any cure (D-19).

### BLOCKER

- **D-1 / L-i1 — the sole AT-exposed scrub control has no accessible name; glass-ui's three purpose-built naming seams (`aria-label/-labelledby/-describedby` forwarded to the thumb) are all unused.** Verified: PlaybackRibbon.vue:15-22 passes no `aria-*`; the thumb-forward list read at the dist; reka's `getLabel` returns `void 0` for a single thumb; all three mounts pass nothing. Announces "slider, 4820" — no name, and (N-2) no meaningful value either. Deliberately the ONLY AT surface (the visualizer's earned `aria-hidden`), which concentrates the failure. Cure: one `aria-label` + the tooltip re-homed as `aria-describedby` (D-5). → **NO-WAVE-OWNER** (transport packet); the a11y datum also rides **KF.W9**.
- **D-2 — there is no visible playhead: the standard variant's thumb is `width:0; opacity:0`, and the only remaining cue — the range/track boundary — is ~1.23:1 (light, both readers' independent byte-exact reproductions) against SC 1.4.11's 3:1 floor.** Rules [12]/[26] extraction re-run by this seat; every visible-thumb rule spectrum-gated; the rim/undershadow hairlines (5%/4%) cannot carry it. **Rider (ruling 10): the dark-arm figures are corrected to ≈1.73:1 via `brightness(1.16)` — still 1.7× short; no axis dark numeral is portable.** Under `.is-disabled` ≈1.10:1. The producer half (standard thumb invisible-by-design) is **already on the recorded kf-AnimationVisualizer relay, item (b)** — not re-relayed. Demo-side cure space: `variant`/`size`/a visible marker, adjudicated by the packet. → **NO-WAVE-OWNER** + SS-13 witness.
- **C-2 — `currentT` declares no time-space; `scrubTo` assumes EFFECTIVE time and inverts (:183-185, the exact involution of play-lifecycle:448-450), and 2 of 3 mounts feed RAW `t` — under Reverse the ribbon's own slider and its own ball paint mirrored positions and every scrub reflects across the midpoint.** Ribbon-owned contract root (ruling 4); the easing manifestation **folds ≡ KF-ES-5** (with KF-ES-6's dual-instance context); the spring manifestation is this record's witness pending any kf-SpringScene adjudication (which folds here); the channel mount (useAnimationSync, effective) is the convention pin. The L axis's opposite cell is K-13. Cure: publish the space (branded type or the one doc line) AND repair the two raw mounts against the pin; the C-15 engine-flag/user-flag dual authority is this row's INFO satellite. → **NO-WAVE-OWNER** (the packet's contract obligation).
- **L-B1 / C-1 ≡ banked KF-CO-15 (kf-ChannelOptions, 2026-08-07) + KF-ES-4 (kf-EasingScene) — FOLD-EXTENDED, not re-booked: `effectiveDuration` computes over a `markRaw` instance's untracked `options.duration`, so the rail's `:max` freezes while the engine mutates in place.** Both readers filed it BLOCKER with full falsifier batteries; neither read the bank (K-12). Carried grade: BLOCKER-weight at this corpus's framing; the banked §B permanence correction is adopted (self-heals on a Controls tab bounce; permanent within an uninterrupted session). **This corpus's genuine extensions, carried into the identity**: (a) the **signed divergence** — `scrubTo` reads the duration FRESH one function below (:183-185), so a stale `:max` 5000 with fresh pivot 2000 under `reversed` emits `rawT = −3000` into the group seam: staleness becomes signed corruption; (b) **L-m9's mechanism** — one field, two access paths, two freshnesses, with the zero-guard sitting on the dead branch while the live branch is bare (the sibling guards it, AnimationVisualizer:114). Cure home: the reactive-`:max` repair in this component, as KF-CO-15 already routes (**OPTIONS-UNIT** + the shared-component fix; one duration read, one freshness). → fold; riders to the packet.

### MAJOR

- **D-3 + DU-M-1 (one identity, demo-wide home) — `.focus-ring:focus-visible { outline: none }`, authored twice in the demo (design-idioms.css:76-79 the surviving authority, playback-idiom.css:72-75 the local copy), defeats glass-ui's forced-colors focus remedy for EVERY glass interactive primitive in the demo.** MAJOR, blocker-in-mode (DU's BLOCKER dissent recorded). **Seat extension (ruling 2): both demo copies are wholly redundant against producer base.css's identical rule + the self-consistent a11y-overrides arm — the complete cure is two deletions.** K-5 sustained (deleting only the playback copy fixes nothing). Cross-refs: KF-CE-13 (categorical sibling), fr-App MG-ζ (distinguished). → **KF.W9** (with the demo-wide sweep); the two-line deletion may land with any packet commit.
- **C-3 — the scrub-gesture seam is pointer-only; glass-ui's declared `valueCommit` (keyboard-inclusive) is forwarded and ignored, so a keyboard scrub during playback is never paired with pause/resume and is overwritten within a frame.** All links verified (reka stepKeyDown → `updateValues({commit:true})` → `valueCommit`; the d.ts; the three pausing mounts; the ribbon's own :126-128 docstring proving the keyboard path was known). Constellation echo, pattern not identity: fr-AnimationControls L-3 (fourier's keyboard scrub never opens a session) — the third repo where the commit-vs-gesture seam split bites. → **NO-WAVE-OWNER** (packet: adopt `valueCommit` alongside or instead of the pointer seam; carry C-L-2's gesture/value split reasoning).
- **C-4 — no `:step`: reka defaults 1, `:max` is milliseconds → the sole AT scrub control moves 1ms per arrow press (5000 presses per traverse); the documented normalized `source` rail would degenerate to a two-position toggle.** `step: {}` bare in the dist (my read). Limb (b) rides ruling 5's precondition list. → **NO-WAVE-OWNER** (packet: a duration-relative step + Page multiplier).
- **C-5 — the touch gate's "defer to page scroll" branch is structurally void: glass-ui declares `touch-action: none` on `.glass-slider` itself (rule [0]), and the composable only ever sets `none`/clears to `""`.** The sibling's dynamic `pan-y` (AnimationVisualizer:11) is the in-tree control. → **GLASS-OWNED — BH relay** (a `scrollGate`/`touch-action` seam on Slider), recorded below; demo keeps no wrapper patch. UNPROVEN live behaviour → SS-13.
- **D-5 (+ DU-M-6 rider) — the one instructional string is bound to a non-focusable `<div>`: hover-only, sighted-only, its `aria-describedby` attached to the wrapper instead of the `role="slider"` node.** Rider (M-6, verified): `.is-disabled`'s `pointer-events: none` lands on the same TooltipTrigger div (:5-14 ← :9), so the explanation is unreachable exactly in the state that needs explaining (ChannelOptions mount only — the scenes hardcode `isAnimStarted: true`). Cure: `aria-describedby` through the producer's forward seam; tooltip on a focusable element. → **NO-WAVE-OWNER**.
- **D-6 / L-m8 (+ LC's composition rider) — `.is-disabled` is a costume: `opacity:.5; pointer-events:none` (style.css, `@layer utilities`) leaves the reka thumb tab-focusable and arrow-operable with no `aria-disabled`, while the producer's real `disabled` prop (tabindex removal + `[data-disabled]` styling, rule [19]) sits unused.** Rider (LC missed, verified): while "disabled", the keyboard path still emits `sliderUpdate` **with no `scrubStart`/`scrubEnd` lifecycle at all** — pointer-events killed the only seam that produces them — so the strongest side effect ships with the weakest lifecycle in the state the UI advertises as inert. One-prop cure: `:disabled="!isAnimStarted"`. → **NO-WAVE-OWNER**.
- **D-10 (+ DU-M-3 as root-cause and cure, folded — one identity) — the Play/Pause label is accent ink on its own 20% accent wash: 3.58/3.70/4.02 vs the 4.5:1 floor (large-text exemption verified inapplicable), worsening on hover (32% wash, same ink); and the demo already owns the exact cure pair — `--accent-kf-foreground` is declared "text/glyph ON the accent" (style.css :130-141, my read) and consumed by this component's own ball (`bg-accent-kf text-accent-kf-foreground`, AnimationVisualizer:21) 64px below the failing CTA.** One-token cure: solid `--accent-kf` plate + `--accent-kf-foreground` ink. The producer half (no brand-accent `tone` rung; TONES fixed to status hues) → relay note. Composited-plate exactness → SS-13. → **NO-WAVE-OWNER**.
- **N-2 (DU-M-2, admitted) — the producer forwards five aria attributes to the thumb and `aria-valuetext` is not one of them; with no `inheritAttrs: false`, a consumer-passed valuetext lands on the role-less root — silently misplaced.** Verified at the dist (forward list; `inheritAttrs` absent; reka binds valuenow/min/max only). Consequence: the sole AT slider announces a bare millisecond integer with no seam to fix it. → **GLASS-OWNED — BH relay** (`aria-valuetext` passthrough or a `formatValue` prop), + the demo obligation to bind it the day the seam ships; actual SR rendering flagged to the AT-verification lane, not SS-13.
- **L-M1 — the touch gate is implemented twice: glass-ui 7.0.0's Slider owns its own `useTouchGate` bound to the slider root (verified in the chunk: same composable, same first-tap contract, same reject shape, `data-touch-active` painted), and the in-file comment (:165-166) asserts the opposite as fact.** Discharges keyframes' own 27-ledger-sweep:421 conditional; the census S-9 registration L proposes is endorsed. Gate count completes at **three** per ribbon ≡ **KF-AV-32** (fold; the mutual-deactivation Set is the banked mechanism; K-10 bounds the cure to the wrapper gate only). Cure: delete the wrapper gate + its dead classes (≡ KF-AV-7) + the false comment — **carrying S-3's pointerType reasoning into whatever remains** (it is the cure KF-AV-3 still needs at the ball). → **NO-WAVE-OWNER**.

**MAJOR-weight folds by identity (banked homes; this corpus = independent confirmation, zero re-booking):** **D-4 / L-M3 / C-8(a)** ≡ **KF-AV-11** (the false "ONE motion-color identity"; the ribbon's :208-229 prose block and both dead `--slider-thumb-bg` declarations are named there; L-M3's F-1/"4.0.0" citation-rot arm rides the same row; → KF.W6) · **D-11 / C-6 / L-m3** ≡ **KF-AV-7** (phantom `touch-gate-*` classes, 4 sites incl. this file's two; → NO-WAVE-OWNER + the recorded relay (a)) · **L-M4** ≡ **KF-AV-2** (fling re-grab drops the deferred `dragEnd`, strands the pause latch; BLOCKER at its home; the seam's non-reentrancy half ≡ KF-AV-15) · **L-M5** ≡ **KF-AV-1** (the guard `isPlaying || isDragging` freezes the twin through every slider scrub — LC's "broader than stated" idle-scrub note is already the banked row's own scope; the false "Always poll" comment DU filed as M-7 is inside KF-AV-1's proof cell) · **D-22** ≡ **KF-AV-14** (the geometric non-twin; K-6's corrected symmetric-24px arithmetic concords with the banked frame) · **C-13** ≡ **KF-AV-8/-9/-18** (the `@src` boundary breach → KF-CE-12/KF.W8; the global eviction wire; the value.js misattribution fossil — with the banked substrate disclosure explaining its origin) · **D-14 / C-10 / L-m1** ≡ **KF-CPW D-M11 + KF-CS D-11** (`btn-interactive` 8 lines/6 files, seat census matches the banked figure; K-7 stands against the D/C six-count) · **C-14** ≡ **census F-1** (undeclared glass-ui; 0/0 re-verified) → **KF.W0 §B-12**.

### MINOR

- **D-7 (rescoped by K-3) — the `:deep(.glass-slider[data-variant="standard"])` track-height reach reimplements the declared `size` prop and undershoots it (`lg` = 1.75rem vs the hand-set 1.5rem); the thumb-proportion table half is killed (no thumb renders to proportion).** L-M3's verification that the reach's cascade DOES land — (0,4,0) over (0,3,0) — is adopted; the standard it fails is in this very file's superlative 8 (the tint-strength repoint names its authority; the `:deep` names none). → packet (swap to `size`, or keep with a recorded reason).
- **D-8 (premise corrected per ruling 1) — the Reverse pressed state is a 1.21-1.31:1 tint (both readers' independent reproductions agree), with NO border channel at all (N-1) and a semantically-null glyph flip (D-9) as its only companions.** Composited-plate exactness bracketed (the `--glass-tint-strength-aa: 0%` neutralization keeps the residual small) → SS-13. → packet (pressed-state redesign; S-2's forced-colors survival is the floor to preserve).
- **N-1 (LC missed, admitted; ruling 1) — the transport buttons have no border: five `border-color` declarations + one utility are dead ink, and the hover "stronger border" narration is false.** LC's MAJOR as dissent. → packet (dead-declaration sweep; if a border is wanted, it must be authored on `.btn-playback` directly, not assumed from `.glass-wash`).
- **N-3 (DU-M-4, admitted; ruling 8) — the press affordance is doubled on Play/Pause (inline spring `scale` × demo `transform: scale` ≈ 0.941) and asymmetric across the pair (Reverse: lift + 0.97 only).** Cure: delete playback-idiom.css:76-78; the producer's single spring press remains. Percept → SS-13. → packet.
- **D-17 + N-4 (DU-M-5, admitted — one row) — icon sizes disagree in one two-cell row (20px vs 24px), and the mechanism is the demo's `icon-*` utilities silently defeating the producer's `--ui-glyph` rung (`@layer utilities` beats `@layer components`; the svg class contains no `size-` substring so the producer rule matches and loses): on coarse pointers the buttons grow 40→60px while Play's glyph stays pinned at 20px.** All selectors and tokens byte-verified. → packet (one icon rung, ideally the producer's).
- **D-9 — `scale-x-[-1]` on a C2-symmetric glyph is a weak state signal (the mirrored form ≡ vertical flip; a bidirectional-swap glyph cannot encode "now reversed"), and the two transport cells use incompatible toggle idioms.** Both readers' geometry concordant; percept → SS-13. → packet.
- **D-13 — `.btn-playback` sets the prose rung (`--type-body`) on a control; the control rung is `--control-text`; the gap widens with viewport; the comment argues "CONTROLS" then reaches past the control token.** Verified at tokens. → packet.
- **D-15 — dual, disagreeing pressed-state authority (template utilities 10/40 dead-by-layering under the skin's 15/30), eight lines below the K.W2 comment claiming the band carries one voice — and the border halves of BOTH authorities are dead anyway (N-1).** → packet (one authority; the utilities die).
- **D-16 — both height declarations are dead (`h-10` layered-out by `height: 2rem`, which is itself exceeded by `.button`'s `min-block-size` = 40/60px) and the eight-line comment defends the deader one with a mechanism that does not exist (wrong property, wrong owner).** Token chain verified. → packet (delete both + the comment).
- **D-20 / L-m4 / L-m7 — the prose quartet, all four verified: "red range fill" vs the neutral mix in the same block that cites red's retirement; "the thumb keeps its variant size" as virtue; "Non-scoped global rules" above a sole `<style scoped>`; playback-idiom.css:10-11's "imported by PlaybackRibbon.vue" false in both halves.** With :219's "glass-ui 4.0.0" (≡ KF-AV-11's rot arm) and :165-166 (L-M1) this file carries five false load-bearing comment blocks against one true one (S-3). → packet prose sweep (D-25's cure: fewer, checkable comments).
- **D-23 / L-M2 / C-12 (rescoped, ruling 5) — the `source` branch is dead today (0 of 3 mounts), its type has drifted from `TransportChannel`, its `progress()` is required-and-never-called, its empty state is unreachable, and (LC's admitted rider) its seat path SKIPS the reversal inversion the `animation` limb performs.** Type-invariant limb ≡ **KF-ES-22** (fold). Disposition: **preconditions of the banked KF-ES-2 cure** — align the type with `TransportChannel`, add the inversion, fix C-4(b), make the either-or a type — never deletion. → packet, sequenced with the KF-ES-2 owner.
- **L-m5 / C-7 — `(val: any)` erases the declared `number[] | undefined` payload; `val[0]` on undefined is the TypeError the compiler was told to permit.** → packet (one guard).
- **L-m6 — `KeyframesAnimation<any>` twice on the public surface (prop + `sliderUpdate` payload); ≡ the house idiom KF-AV-38 logs (fold-adjacent, logged per-site per that precedent).** → packet.
- **L-m9 — the duration is read at two freshnesses with asymmetric guards (the stricter zero-guard on the dead branch).** Mechanism carried into the L-B1 fold; standalone MINOR for the guard asymmetry. → packet (one read, one guard).
- **C-9 — `ref="sliderRef"` is a dead template ref (1 occurrence; no `useTemplateRef`, no `const`).** → packet.
- **C-11 — one import block, two subpath conventions two lines apart (root barrel :92 vs `/tooltip` :94) while `./button`, `./slider`, `./dom` all exist; the per-file instance of lane-frontend §3.1.** → packet hygiene.

### INFO

- **D-19** — `.timeline-green` names a colour retired at T.D7; paints violet; zero external consumers; rename-at-will (`.scrub-rail`). *(demoted per ruling 7)* → packet.
- **D-21** — flat `gap-2` at both grouping levels; proximity encodes nothing; facts exact, remedy a design judgment. *(demoted)* → packet.
- **D-24** — zero authored PRM/RTL/loading/error; PRM is rescued only by the producer's allowlist (verified: `transform` absent — precisely what neutralizes the icon flip and press transforms); the two physical-direction affordances (`pl-px`, `scale-x-[-1]`) would need RTL mirroring while the producer Slider handles `:dir(rtl)` itself. → **KF.W9** (with KF-AV-37's RTL datum).
- **D-25 (count corrected, ruling 6)** — **88 of 245 lines ≈ 36%** comment mass, and the four largest blocks are wrong about the tree (D-4/D-16/D-20/L-M1); the user-facing prose, by contrast, is the best-maintained text in the file (superlative 7). → packet prose sweep.
- **L-i2** — `scrubbed` documented as firing "on EVERY scrub" for loop-rearming, bound by 1 of 3 consumers; over-specified or under-wired (and the wake it feeds goes up, never down — ≡ KF-AV-1's seam). → packet.
- **C-15** — `isAnimStarted` hardcoded `true` at 2 of 3 mounts (making D-6 ChannelOptions-only); `userReversed` (visual) vs `animation.reversed` (math) is a documented dual authority that disagrees on alternate-direction iterations — the satellite of C-2, and the reason L-S2's flag choice is right. → packet doc obligation.
- **N-5 (DU-M-8, admitted)** — `--color-progress`/`--color-slider-track` sit in Tailwind v4's reserved `--color-*` namespace but are declared in `:root` (verified: `@theme` opens :42, `:root` :91, the tokens ~:163), so `bg-progress`-class utilities generate nothing; zero live damage today (grep 0); the loaded-gun class this demo has paid for before (design-idioms :92-95's own icon-* confession). → packet hygiene note.
- **N-6 (LC missed, admitted)** — `emphasis="secondary"` is the Button's own default passed explicitly on both cells; width is declared three times (`w-full` utility, skin `width:100%`, the grid track). → packet dead-declaration sweep.
- **C-negative (upheld)** — the value.js R1 parser-crash class is NOT reachable from this component's scrub path (no value.js import; the child's one import is `/math` `clamp`; the seat path is numeric end-to-end) — concord with KF-AV SUP-11's precise phrasing; bundle co-residency only. Negative recorded so it is never re-litigated.

## Killed-claims register — 13 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **D-18** — "the press squish never lands on a mouse press; both hover rules are (0,3,0)" | SUSTAINED (both readers, convergent; seat re-verified at the file): `.btn-playback-accent:hover` is (0,2,0) and LOSES to the later `:active` — the squish lands on Play/Pause and is shadowed on Reverse only. The axis's `.tap-squish` mechanism was also wrong (permanently disarmed via `data-press-armed`); the real second affordance is the inline spring scale → admitted as N-3 |
| **K-2** | **L-m2** — "`duration-fast` emits nothing under Tailwind 4.3" | SUSTAINED (both readers): the axis's non-recursive `dist/*.css` glob missed `dist/styles/theme/bridges.css`, whose `@theme inline` declares `--transition-duration-fast: var(--duration-fast)` (= 0.2s) — seat-verified in the import graph. The utility emits; the collateral claims at 3 other files fall with it |
| **K-3** | **D-7(b)** — "`size='lg'` scales the thumb with the groove; the hack lands the worst thumb/track ratio" | SUSTAINED (both readers): under `standard` no thumb renders and `--slider-thumb-size` is read only by spectrum rules — the entire proportion table measures a quantity that cannot render. Concordant with the banked kill class (kf-AV K-1; fr-AnimationControls K-5). D-7's reach-vs-prop core survives at MINOR |
| **K-4** | **C-8(b)** — "the rail has no hover affordance at all" | SUSTAINED (reader-DU; seat verified rule [13]): `.glass-slider:not([data-variant=spectrum]):hover .slider-range` ships a producer hover (rim + undershadow + 1px `--surface-tint-8` ring) and rule [22] adds a 2px held ring. The demo's hover intent is unexpressed; the rail is not affordance-less |
| **K-5** | **D-3's prescribed cure** — "deleting playback-idiom.css:72-75 fixes the blocker and loses nothing" | SUSTAINED AND EXTENDED (ruling 2): the surviving authority is design-idioms.css:76-79; and BOTH copies are wholly redundant against producer base.css's identical rule + its later forced-colors arm — the complete cure is deleting both demo copies |
| **K-6** | **D-22's endpoint arithmetic** — "24px at t=0, 8px at t=1" | SUSTAINED (both readers): the ball's centre travel is symmetric — 24px divergence at BOTH ends, opposite signs, zero at midpoint; concordant with banked KF-AV-14's frame. No axis magnitude enters a repair spec |
| **K-7** | **D-14/C-10's "six call-sites"** | SUSTAINED: the count is EIGHT (seat census re-run; RibbonBar's `RIBBON_BUTTON_CLASS` constant + SpringPhysicsFacet:105 missed) — which is also the banked figure at KF-CPW D-M11, filed before this corpus existed |
| **K-8** | **Reader-DU's D-8 border cell** — "the only width is `.glass-wash`'s hairline… its colour delta is the same sub-3:1 mix" | **SEAT KILL (ruling 1)**: `.button { border: 0 }` beats `.glass-wash`'s border on same-layer source order (imports #24 vs #4); tone defaults neutral. The cell quotes the refuting rule and repeats the axis's conclusion — this corpus's premise-inheritance event (fr-DarkModeToggle SC-3 class). D-8's contrast figures are unaffected (background-only model) |
| **K-9** | **Reader-DU's D-25 cells** — "65 comment-ish lines (26.5%)… L-i3's ~90 overcounts by ~25" | **SEAT KILL (ruling 6)**: seat count with a stated rule = **88/245 ≈ 36%**; L-i3's ~90 was nearly exact; DU and the D axis understated by a third. The row's direction survives; the numbers are re-pinned |
| **K-10** | **Reader-LC's rider** — "the deletion L-M1 prescribes must cover the visualizer too, or the duplication survives the repair" | **SEAT KILL (ruling 9)**: the visualizer's gate is the composable's documented per-control use, manages `touch-action` correctly, and its real defects are banked (KF-AV-3, KF-AV-32). L-M1's cure deletes exactly one gate: the ribbon wrapper's |
| **K-11** | **The corpus's `source`-branch deletion** (L §4 item 3; both readers' dead-code framing) | **SEAT KILL (ruling 5)**: banked KF-ES-2's cure observation consumes the `source` seam as the facility-honesty repair. The branch's defects are re-filed as that cure's preconditions — the L-15/ColorSpaceSelector law again: the cure is adjudicated separately from the defect, and here the BANK adjudicated first |
| **K-12** | **Thirteen registry-blind filings** (both readers; enumerated in the headline) | **BOOKING KILLS**: every identity re-verified at the bytes and recorded as an independent confirmation of its banked row — L-B1/C-1 ≡ KF-CO-15/KF-ES-4 · L-M4 ≡ KF-AV-2 · L-M5 ≡ KF-AV-1 · D-11 ≡ KF-AV-7 · D-4 ≡ KF-AV-11 · D-22 ≡ KF-AV-14 · C-13 ≡ KF-AV-8/-9/-18 · D-14 ≡ KF-CPW D-M11/KF-CS D-11 · LC-miss-3 ≡ KF-AV-32 · DU-M-7 ≡ KF-AV-1's comment cell · DU's "NEW" rail-geometry superlative ≡ KF-AV SUP-1 · C-2's easing arm ≡ KF-ES-5 · the either-or limb ≡ KF-ES-22. The readers re-derive brilliantly and do not read the bank — the kf-SequencePlayhead lesson, at scale |
| **K-13** | **The L axis's §1 cell** — "I checked for the obvious raw/effective mismatch and it is absent" | **AXIS FALSE NEGATIVE, recorded**: refuted by C-2's proof chain (seat-verified at all six files); both readers implicitly killed it by confirming C-2. The L return must not be cited for mount-space facts |

## Superlatives — sustained, with provenance

1. **S-3 / L-S1 / C-L1 · the `pointerType === "touch"` gate bypass** (:153-179) — premise verified at the dist (`l = "ontouchstart" in window` is the sole device gate; first-press-false); the one long comment in the file still true of its subject; the recorded measurement KF-AV-3 cites as the sibling's cure. **Must be carried into whatever replaces the wrapper (L-M1) and ported to the ball.** (All axes, both readers, this seat.)
2. **L-S2 · the reversal inverse reads the RIGHT flag** — `:183-185` is the exact involution of play-lifecycle:448-450, and it reads `animation.reversed` (engine truth, flipped per-iteration under `alternate`) over the adjacent `userReversed` (intent) — the wrong choice would invert every odd pass on the easing scene's own `alternate infinite` preview. Tension carried: C-2 shows the right formula still mirrors at 2 of 3 mounts because the contract was never published.
3. **L-S3 · teardown correctness by having nothing to tear down** — no onUnmounted/onScopeDispose/addEventListener/timer/observer in the file, and none needed; every listener is composable-owned. Caveat carried from the bank: the seam it trusts has its own non-reentrancy row (KF-AV-15).
4. **S-1 · the `aria-hidden` disposition on the visual twin ≡ banked KF-AV SUP-2** — concord; the premise-repairs (KF-AV-1/-14/-11) do not touch the technique.
5. **S-2 · `aria-pressed` over a class buys forced-colors + high-contrast state for free** — both producer blocks verified verbatim (Highlight / currentColor, 2px solid). **Sharpened by ruling 1: this is now the ONLY border the Reverse toggle ever paints in any mode** — the one state signal that survives is the one expressed semantically. The lesson generalizes.
6. **S-5 · `pl-px` on the Play triangle only** — optical centring applied to exactly the asymmetric glyph, withheld from the symmetric one.
7. **S-6 · the shipping copy** — four user-facing strings, zero filler; the tooltip a verb phrase naming action and object; against 36% comment mass, the prose a user reads is the best-maintained text in the file.
8. **DU-new (adopted, seat-verified) · the `--glass-tint-strength-aa: 0%` consume-seam discipline** (style.css ~:195-208) — one producer token re-pointed, scoped to out-specify the producer's `:where()` self-engage, citing the producer's own documented opt-out and the ceiling it defends, self-declared "never a node_modules patch". The standard this file's OTHER producer reach (D-7's `:deep`) fails — the contrast is instructive.
9. **C-L-2 · the gesture/value split composes correctly with the primitive** — "the seam owns the GESTURE, the component owns the VALUE," honored (no onMove re-derives the value), and the double-capture question closed: reka's bubble-phase capture on a descendant retargets without silencing the wrapper's up/cancel listeners.
10. **L-S4 / C-L3 · the visualizer's engine dogfooding ≡ the banked kf-AV superlative family** (SUP-4/-5/-7 there) — concord, counterweights already banked (KF-AV-8/-9); and DU's independently-derived rail-geometry identity ≡ banked SUP-1 (K-12) — praise confirmed twice, booked once.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **D-2's composited range/track boundary** on the live `Card cartoon tier="quiet"` plate, both themes — light ≈1.23 analytic (twice-reproduced), dark ≈1.73 analytic (seat-admitted brightness-1.16 correction); eyedropper settles both. The BLOCKER is robust to any plausible error.
2. **D-10/M-3's composited wash** under `.glass-wash` + backdrop-filter — direction certain; three of four rows would need +0.5-0.9 to clear.
3. **D-8's composited pressed-vs-resting delta** at both tints, with NO border channel (ruling 1).
4. **D-9's percept** — a screenshot pair of both Reverse states at 24px/150ms.
5. **N-3's percept** — the two cells' press physics side by side (0.941 vs lift+0.97).
6. **C-5's rendered dead-zone** on a real touch device (rail neither scrubs nor scrolls on first touch).
7. **L-M1/KF-AV-32's gate desync** — three gates, three timers, one document sweep, on device.
8. **C-4's traverse severity** — 1ms/press lived, not computed.
9. **The KF-CO-15 extension's signed seek** — reproduce `rawT < 0` live (duration edit + reverse + rail-end drag) to pin the corruption witness for the packet's born-RED gate.
10. **M-2/N-2's actual announcement** — routed to the AT-verification lane (out of SS-13's visual scope, flagged so it is not lost).

## Identity guards (fold-by-reference — never re-booked)

- **L-B1/C-1 ≡ KF-CO-15 (kf-ChannelOptions) + KF-ES-4 (kf-EasingScene)** — the signed-divergence + two-freshness riders live HERE; the §B permanence correction is adopted; the cure home is this component per the banked routing.
- **L-M4 ≡ KF-AV-2 · L-M5 ≡ KF-AV-1 · D-11/C-6/L-m3 ≡ KF-AV-7 · D-4/L-M3/C-8(a) ≡ KF-AV-11 · D-22 ≡ KF-AV-14 · C-13 ≡ KF-AV-8/-9/-18 · LC-miss-3 ≡ KF-AV-32 · DU-M-7 ≡ KF-AV-1(proof cell) · DU's rail-geometry superlative ≡ KF-AV SUP-1** — kf-AnimationVisualizer is the child's home; this record adds independent confirmations and the gate-count upgrade (three instances) to KF-AV-32.
- **C-2's easing manifestation ≡ KF-ES-5** (+ KF-ES-6 context); any future kf-SpringScene adjudication folds its raw-feed row to C-2 here.
- **The either-or contract limb ≡ KF-ES-22**; the `source` branch's future ≡ KF-ES-2's cure observation (K-11).
- **D-14/C-10/L-m1 ≡ KF-CPW D-M11 (NO-WAVE-OWNER deletion incl. `RIBBON_BUTTON_CLASS`) + KF-CS D-11 (KF.W6 phantom-utility audit)**; the value.js-side `btn-interactive` family stays cross-referenced, never merged (the banked law).
- **C-14 ≡ census F-1 → KF.W0 §B-12**; every dist-negative in this record is install-specific until F-1 lands.
- **D-3+M-1 cross-refs KF-CE-13** (categorical demo-wide forced-colors gap) — sibling, not the same identity: this row is an authored defeat with a two-deletion cure.
- **C-3's pattern echo ≡ fr-AnimationControls L-1/L-3** (fourier) and value.js's ComponentSliders precedent — constellation pattern recorded, never merged (per the fr-AnimationControls precedent).
- **L-m6 rides KF-AV-38's house-idiom identity**, logged per-site per that precedent.

## Relay obligations (recorded here; no inbox write from this seat — sole-write law)

The next session with mail duty should relay to the glass-ui BH inbox, beside the two items kf-AnimationVisualizer already recorded (gate styling contract + group facility; standard-thumb invisibility): (a) **the Slider offers no scroll-gate seam** — `touch-action: none` is hard-declared on `.glass-slider` with no prop to soften it, so no consumer can implement tap-to-activate above it (C-5; the demo's wrapper is structurally void and will be deleted); (b) **the thumb's aria forward list omits `aria-valuetext`** and the component declares no `inheritAttrs: false`, so a consumer-passed valuetext lands on the role-less root — an `aria-valuetext`/`formatValue` seam is the producer-shaped cure (N-2); (c) **the slider's focus affordance is box-shadow-only and its selectors are absent from the forced-colors list** (D-12) — one selector addition; (d) *(optional, with D-10)* the `tone` axis has no brand-accent rung, so an accent CTA cannot ride the paired `--button-tone`/`--button-tone-ink` recipe.

## Routing summary (M-25 agglomeration input)

- **KF.W0 §B-12**: the F-1 fold (C-14).
- **KF.W6**: D-4 ≡ KF-AV-11 (banked) · D-14's phantom-utility audit arm ≡ KF-CS D-11 (banked).
- **KF.W8**: C-13 ≡ KF-AV-8/-9 (banked).
- **KF.W9**: **D-3+M-1** (the two-deletion forced-colors cure + the demo-wide sweep; x-ref KF-CE-13) · D-12's demo-side stopgap · D-24 (PRM/RTL data, with KF-AV-37) · the SS-13 list above.
- **GLASS-OWNED (BH relay)**: C-5 · N-2 · D-12 · the D-10 tone-rung note.
- **NO-WAVE-OWNER — the transport/ribbon repair packet (SS-1/SS-2 authoring must home it, under the KF.W7 rider)**: the **instrument-truth core** — D-1 (+N-2's demo half) · D-2's demo arm · **C-2 (the time-space contract; the channel mount is the pin; cure jointly with the KF-CO-15 extension so scale and space land together)** · the KF-CO-15 riders (reactive `:max`, one duration read, one guard — L-m9) · C-3 (`valueCommit`) · C-4 (`:step`) · the **one-prop/one-token cures** — D-5 (+M-6) · D-6 (`disabled`) · D-10 (accent pair) · L-M1 (delete the wrapper gate, carry S-3) — · the **hygiene sweep** — D-7 (`size`) · D-8/N-1/D-15 (one pressed authority, borders authored or deleted) · N-3 (delete :76-78) · D-17/N-4 (one icon rung) · D-13 (`--control-text`) · D-16 (delete both heights + the comment) · D-19 (rename) · D-20/D-25 (the five-false-comments prose sweep) · D-23-as-preconditions (with KF-ES-2's owner) · L-m5/L-m6/L-m9/C-9/C-11/N-5/N-6 · L-i2/C-15 (doc obligations). Born-RED candidates the packet inherits: the signed-seek witness (residue 9), a keyboard-scrub pause/resume assertion (C-3), an axe/name assertion on the thumb (D-1), and a forced-colors focus screenshot (D-3).

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c` ≡ `origin/master` `81a56990` for every audited file (frontier-durable by identity): 38 roster rows — 4 BLOCKER (the anonymous sole-AT scrub control; the invisible playhead over a 1.23:1 boundary; the unpublished effective-time contract that mirrors two of three mounts; the banked frozen-duration identity extended with a signed-seek rider) · 9 MAJOR · 16 MINOR · 9 INFO — plus 8 folds into banked records and ZERO re-bookings. The readers' seven kills all sustained, two extended at the bytes (the forced-colors cure is two deletions because the producer already ships the whole answer; the press-squish kill hands its truth to a new admitted row); four seat kills against reader cells, including this corpus's premise-inheritance event (a hairline border that does not exist, quoted beside the rule that deletes it) and one prescription killed by the bank itself (the `source` branch is the planned consumer of a banked cure, not dead weight). The dominant result is the registry lesson at its largest yet: thirteen booking-kills in one corpus — two readers re-deriving, brilliantly and independently, defects the bank had held for up to five days, one of them re-deriving a banked superlative as its own discovery. The component's shape survives every ruling: a correctly-seamed, correctly-worded, token-disciplined transport whose five false comment blocks, two dead style authorities, and one unpublished contract are exactly where all four blockers and most of the majors live. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C and the readers' M-rows for life; N-1..N-6 minted here.**

— end of apotheosis r1. The three challenge records and both reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/playback/PlaybackRibbon.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **PlaybackRibbon.vue=+30/-23**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · `.d` measured, `.e` landed) — **D-3 · DU-M-1 · D-12 (demo-side stopgap)**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here; **measured by seat `.d`**, which
was forbidden to land it. **Nothing above this line is rewritten** — no edit, no re-grade, no renumber, no
deletion, no new id (E-1 / E-3). Every bullet is a dated MEASUREMENT; none re-books a row or moves a severity.*

- **cell** — `chromium/emulated-forced-colors` (the BEFORE witness + its falsifier) · `safari-app/desktop` (the normal-state antecedent)
- **capture** — `…/audit/visual/safari-real/hcm-chromium-emulated-forced-colors-easing-ribbon.png` · shot-sha256 `bb2434b5c7bb9e17f29736ef8246b422e013ca7f3222af2b9dfd20bdcc1bdd88`
- **stamp** — substrate keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD) · bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448` (built in the §0m.2 capture clone, **measured and double-run** at `.d` and **re-hashed identical at `.e`**)

- **THE REDUNDANCY GROUND IS FALSE AS WORDED — measured, and handed to KF.W13 before its act lands.** The standing ground for deleting `playback-idiom.css:72-75` is *"both demo copies are wholly redundant against producer `base.css`'s identical rule."* Measured per copy at glass **7.0.0**'s shipped dist: `design-idioms.css:76-79` `.focus-ring:focus-visible` — the producer ships the **identical selector** with the same two declarations → **TRUE AS STATED**; `playback-idiom.css:72-75` `.btn-playback:focus-visible` — ⟨`find …/dist -name '*.css' | xargs grep -l 'btn-playback'`⟩ → **exit 1, no output**, widened to all file types → **no output** → **FALSE AS STATED: no producer rule at any coordinate.**
- **The row survives by a different path, and the path is the finding.** Measured live, the ribbon's own class lists carry **both** `btn-playback` **and** `focus-ring` (`"button tap-squish focus-ring glass-wash glass-capsule glass-capsule-hover btn-playback btn-playback-accent"`), and ⟨`querySelectorAll('.focus-ring').length`⟩ → **44**. The producer reaches these buttons once *both* demo copies are gone — **via the co-present `.focus-ring` class, not via a producer `.btn-playback` rule.**
- **This makes K-5 MEASURED rather than argued.** Delete only `design-idioms` → `.btn-playback:focus-visible { outline: none }` still matches **the same buttons**; delete only `playback-idiom` → `.focus-ring:focus-visible { outline: none }` still matches **the same buttons**. **ONE ACT, NEVER SPLIT — proved by the buttons' own class list, not by inference.**
- **The BEFORE witness exists and is falsifiable.** Under `forced-colors: active` genuinely matching, `.btn-playback` reads `:focus-visible true` · `outline-style: none` · `box-shadow: none`; the FALSIFIER (same engine, same bundle, `forcedColors: "none"`) paints an indicator. **The AFTER witness reads UNMEASURED** — the two-deletion act is **KF.W13's**, this wave spent no cure and deleted no byte (S-9).
- **The obligation this hands KF.W13**: the ground must be **re-worded to redundancy via the co-present `.focus-ring` class** before the deletion lands. **The act itself is unchanged and is still ONE act.** No severity moves here; the row's cure home is untouched.
