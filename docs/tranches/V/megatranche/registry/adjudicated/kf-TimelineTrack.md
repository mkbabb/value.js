claude-fable-5

# kf-TimelineTrack — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Anchors**: keyframes.js worktree at HEAD `8281638c`, 41 commits behind `origin/master` (both re-measured by this seat this session); **`TimelineTrack.vue` and `useZoomPan.ts` are byte-identical at HEAD, origin/master, and the worktree** (my own `git diff origin/master --stat` — empty for both), so every target-file anchor in this record is baseline-invariant; seven timeline SIBLINGS (`KeyframeTimeline.vue`, `useTimelineOps.ts`, `useTimelineBuild.ts`, `timelineTypes.ts`, `timelineEngine.ts`, `snapshotCapture.ts`, `flattenVars.ts`) are worktree-modified, so sibling anchors resolve in the worktree only. Registry branch `tranche-u` (value.js). Installed producers: `@mkbabb/glass-ui` 7.0.0 (= origin/master's exact devDependency pin — my probe), `@mkbabb/value.js` 4.0.0.
**Corpus**: three axis challenges (D: 27 defects/5 SUP · L: 29/5 · C: 15/4, all `claude-opus-5[1m]`, 2026-08-06, 1269 lines) at `docs/tranches/V/megatranche/audit/kf-components/TimelineTrack/`, re-read by two hostile re-readers — **RR-A** (design-and-usage) and **RR-B** (library-and-code) — both returns adjudicated here, attacked before either was adopted.
**Method (L-18/M-23)**: every verdict-level disagreement was re-derived against the tree with this seat's own instruments — the one direct reader-vs-reader CONTRADICTION (RR-A's kill of m-5's magnitude vs RR-B's confirmation of L-m-5 with the same magnitude) was settled by this seat's own read of `useZoomPan.ts`; the killed twin BLOCKERs (B-1/C-1) were re-killed with this seat's own three-baseline git probes, never adopted on trust; both of RR-B's net-new MAJORs (the grab-offset teleport, the per-move rebuild storm) were re-walked at the bytes end to end before booking. **No browser tooling** (law); product source READ-ONLY; the irreducibly visual residue rides to SS-13.
**Arbiter's own instruments (all static + git; zero writes to product source)**: read WHOLE — `TimelineTrack.vue` (246 L), `useZoomPan.ts` (110 L), `TimelineCaret.vue` (70 L), `useTimelineOps.ts` (88 L), `useTimelineBuild.ts` :20-120; read targeted — `KeyframeTimeline.vue` :75-90/:190-235 (the bindings, the 15-member destructure with `scrub` ABSENT, `previewCache`, the KEEP-catch), `timelineEngine.ts` :20-57 (the "constructor is HEAVY" docblock + the same-percent merge + `new CSSKeyframesAnimation(...).fromKeyframes(...)`); **git probes**: `git show origin/master:package.json` → `:77 "@mkbabb/glass-ui": "7.0.0"` exact devDependency; `git show origin/master:package-lock.json | grep -c glass-ui` → 3; `git show HEAD:package.json` → `optionalDependencies { "@mkbabb/glass-ui": "6.0.0" }` with 3 lockfile rows (`npm ci` installs optionalDependencies by default); worktree `package.json`/`package-lock.json` → **0** glass-ui rows (the uncommitted in-flight edit both BLOCKER filings mistook for the committed state); installed version 7.0.0 re-read via `node`; `git rev-list --count HEAD..origin/master` → 41; `git status --short` → 7 modified timeline siblings, target file clean; `grep -n scrub` over `KeyframeTimeline.vue` → destructure `:197 scrubT` / `:202 scrubAndCapture`, no `scrub`, `:83` bare ref write.
**Witness-staleness caveat (inherited from the banked kf-KeyframeTimeline header)**: every built-bundle byte in this corpus is provisional until `build:gh-pages` re-runs; the token/cascade cells both re-readers re-derived against the INSTALLED 7.0.0 dist are exempt.

## Scoreboard

**35 ADJUDICATED-CONFIRMED (0 BLOCKER · 7 MAJOR · 19 MINOR · 9 INFO) booked here · 20 whole-row FOLDS + 4 partial-arm folds by reference to banked kf-KeyframeTimeline / kf-CSSCodeEditor / census / megatranche identities (identity guard, zero re-bookings) · 9 KILLED with re-executed refutations (both axis BLOCKERs die on this seat's own three-baseline probes; one reader-solo kill adopted against the other reader's confirmation on this seat's own read) · 11 net-new missed rows adjudicated IN (5 RR-A · 5 RR-B · 1 this seat's own) · 12 UNPROVEN-NEEDS-LIVE residue rows to SS-13.**

Zero BLOCKERs survive. Both axes' BLOCKERs (B-1 ≡ C-1-fresh, the phantom glass-ui dependency) are **dirty-worktree artifacts killed at every committed baseline** — the corpus's sixth one-baseline-short event, and the sharpest process fact in this record: two independent axes escalated an uncommitted `package.json` edit to a shipping-build BLOCKER while each listed, as its own first falsifier, the single `git show` that kills it. The heaviest surviving local truth is the **drag seam**: the marker drag teleports the keyframe to the pointer (no grab offset) and then rebuilds the entire engine — construction plus full CSS parse — on every pointermove, unthrottled and without a dirty check; both halves are net-new re-reader finds no axis filed, and both land on KF.W7's desk beside the banked scrub-seam cluster (kf-KeyframeTimeline C-1/C-6/L-3/L-5/N-10).

Status verbs per M-25: dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input. Wave targets from `CENSUS-2026-08-03.md:179-206`. **No row here is NO-WAVE-OWNER**: this component is inside the census-named subject of **KF.W7 · Timeline Evaluate**, with discrete rows to KF.W6 (Glass Suffusion), KF.W8 (Structure, by fold), KF.W4 (type-gate, by fold), KF.W2/W3 (parser ingress, by fold), KF.W9/SS-13 (visual horizon).

## Ruled disagreements (the seat's actual work)

| id | RR-A (design-and-usage) | RR-B (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **m-5 / L-m-5 magnitude** | KILLED the ~102-tick figure (window filter ⇒ ~15) | CONFIRMED at INFO, repeating "can hold ~102 entries" | **KILL ADOPTED — RR-A wins, on this seat's own read** | `useZoomPan.ts:34-38`: the loop PUSHES only `t >= visibleStart - step && t <= visibleEnd + step`. At z=8 the window is 100/8 = 12.5%, step 1 ⇒ ≤ ~15 ticks (5 at z=1); the ~102 figure requires the filter not to exist. RR-B cited `:23-40` as provenance and still repeated the axis's false number — a citation-inheritance event recorded in DISSENT. The churn observation (3 `percentToPosition` calls per tick, re-rendered at pointermove rate) survives at INFO. |
| **M-3/C-4 harm + rung** | MAJOR, harm bounded by banked C-1 (playhead div jumps; engine does not re-render) | Killed axis-L's harm sentence outright ("`scrubT` drives `scrub()`" is false by its own corpus), mechanism survives | **CONFIRMED MAJOR with the corrected harm; axis-L's harm sentence KILLED** | My reads: `:168-172`/`:174-186` test no `isPrimary`/`pointerType`/pointer-count; a touch contact reports `buttons === 1` so `:182` passes; and `scrub` is absent from the parent's 15-member destructure (my grep) so the axis's "animation target visibly re-renders" is impossible today. Today's harm: a jumping playhead + a corrupted `scrubT` that IS consumed as `snapshot()`'s default capture percent (`useTimelineOps.ts:28`). Rung stays MAJOR because the row is coupled to the banked C-1 cure: the moment KF.W7 wires the scrub seam, an unguarded pinch drives the engine — booking this MINOR would let the W7 spec wire the seam without the guard. |
| **M-7 rung (shift-wheel reads `deltaY` only)** | MAJOR | MINOR | **MINOR — RR-B wins; escalation trigger recorded** | My `useZoomPan.ts` read: `:57-61` reads only `event.deltaY`; no `deltaX` anywhere in the file. The certain half is a wrong-axis read; the disabling half (Chromium delivers shift-wheel as `deltaX`, zeroing the branch) is UNPROVEN-NEEDS-LIVE, and in engines that keep `deltaY` the pan works. MINOR now; **jointly escalates with RR-B's missed-4 to MAJOR (pan unreachable) if the SS-13 probe logs `deltaY: 0`**. RR-A's MAJOR preserved in DISSENT. |
| **D-4 rung (tick-ladder pitch)** | MAJOR | MINOR | **MINOR — RR-B wins** | The arithmetic is proven and re-derived (pitch = step·z: 74.8% at z=2.99 → 30% at z=3; 39.9% at z=7.99 → 8% at z=8 — a 9.4× swing, a 5× cliff on a 0.01 increment). But the ticks are the secondary layer (labels carry the information), and the MAJOR-grade consequence — label collision at the 32px pitch — needs a measured advance width. MINOR with the collision arm to SS-13; escalates if the collision is photographed. RR-A's MAJOR in DISSENT. |
| **D-12 rung (2.5.8 spacing exception)** | MAJOR | routed the hit-collision magnitude to SS-13 | **MINOR, merged with C-12/i-2** | Statically certain: the arrow step is 1% (`:204`), `moveKeyframe` clamps only (`useTimelineOps.ts:59` — my read), pads overlap below ~6% separation at z=1 and the later DOM sibling wins paint and hit at equal `z-controls`. But zoom is a real escape hatch and the unhittable-marker claim needs a live hit test (folded with banked D-17/M1's SS-13 rows). The 24-vs-44 arm FOLDS to banked **S-1 BOUNDED**. Citation correction adopted from RR-A: L's "800px track" and C's "~600px rail" are fictions — `--rail-width` is 400-512px; only axis D used the real figure. |
| **m-3 rung (hand-rolled affine remap)** | MINOR | INFO ("the two-line hand-roll is arguably more legible than a five-argument `scale()` call") | **INFO — RR-B wins** | Algebra re-checked: `scale(pct, pan, pan+100/z, 0, 100) = (pct−pan)·z` — identical. Under-consumption evidence for the KF.W7 record, not user harm; the KISS law cuts against a mechanical swap. |
| **B-1 / C-1-fresh (phantom dependency BLOCKERs)** | KILLED (three-baseline probe) | KILLED (same probe, independently) | **KILL RATIFIED on this seat's OWN probes — never adopted on trust** | Reader convergence is cheap where both inherit the same corpus, so this seat re-ran everything: `origin/master` declares `"@mkbabb/glass-ui": "7.0.0"` exact (package.json:77) with 3 lockfile rows; HEAD `8281638c` declares `6.0.0` under `optionalDependencies` with 3 lockfile rows (and `npm ci` installs optionals); ONLY the uncommitted worktree has 0 rows; the installed tree is 7.0.0 = origin/master's pin exactly. Full consequence at the killed-claims register. |

**Merged/mutually-corroborating pairs**: RR-A's grab-severity note ≡ RR-B's L-M-3 kill (same corrected harm, found independently); both readers independently promoted axis-L's S-3 (`:key="kf.id"` drag-survival) to a net-new superlative and both used it as the instrument that kills m-13's latch escalation.

## Adjudicated defect roster — 35 rows

Ids are the corpus's own, for life; net-new rows keep their finder's designation. Target-file anchors are baseline-invariant (header); sibling anchors are worktree. Every routing is M-25 agglomeration input for the forming X·KF specs.

### MAJOR (7)

- **D-1 — scrubbing has no keyboard path anywhere in the tree** (SC 2.1.1): the track is a bare `<div>` with six pointer/touch listeners plus `@wheel` (`:21-35` — my read); no role/tabindex/keydown; every `scrubT` writer is pointer-driven (both readers' exhaustive greps agree); `defineExpose` publishes 10 members, `scrubT`/`scrub` absent, so no ancestor can drive it; the SpringTarget mirror the comment claims (`:199`) landed on the markers, not the rail. **BLOCKER rung killed** (ruled table precedent: banked C-1 establishes the seam drives nothing in ANY modality; program BLOCKER law). The one modality-specific residual harm, RR-B's isolation adopted: `useTimelineOps.ts:28` makes the playhead position the sole selector of `snapshot()`'s default capture percent and `RibbonBar` invokes `snapshot()` argless — a keyboard user's every snapshot lands at 0%. The caret's click-only-div limb FOLDS to banked **D-3**. **→ KF.W7** (design jointly with the banked scrub-seam cluster).
- **M-3/C-4 — pointer and touch pipelines both live on one element, no multi-touch guard — a pinch also scrubs** (ruled table: mechanism confirmed, axis-L's harm sentence killed, MAJOR for coupling to the C-1 cure). NOT banked — the bank's gesture rows are M5 (wheel), M2 (caret pointerdown), D-11 (zoom discoverability). **→ KF.W7, explicitly sequenced WITH the C-1 rewiring** (an `isPrimary`/pointer-count guard is a precondition of wiring the seam, not a follow-up).
- **RR-B missed-1 — a marker drag TELEPORTS the keyframe to the pointer, discarding the grab offset** — re-walked by this seat: `onMarkerPointerDown` (`:192-196`) records no pointer-to-marker offset; `onTrackPointerMove` (`:174-178`) emits `moveKeyframe(id, getPercentFromPointer(event))` — the ABSOLUTE pointer position; the grabbable region is the 24px pad, so a legitimate grab starts up to ~12px off-centre ≈ 3% of the 400-512px rail at z=1 (3× the component's own finest keyboard increment), shrinking as 1/z. Not cosmetic: `useTimelineOps.ts:56-62` commits percent AND re-derives `selector`, and the parent's `useRefHistory(clone: true)` banks the jump as an undo step. Registry-swept (RR-B's sweep re-trusted after spot-check): zero kf rows; the value.js GradientStopEditor C11 row is the severity precedent, not the identity. **→ KF.W7**; rendered jump extent → SS-13.
- **RR-B missed-2 — every pointermove of a marker drag runs a full engine construction and CSS parse** — chain re-walked by this seat end to end: `:177` emits per pointermove (~60-120/s) → `KeyframeTimeline.vue:84` binds `moveKeyframe` directly → `useTimelineOps.ts:56-62` calls `rebuild()` unconditionally (no early-out even when the clamped percent is unchanged — a drag held at a rail end rebuilds at pointer rate for zero delta) → `useTimelineBuild.ts:34-51` → `buildAnimationFromTimeline`, whose own docblock reads "ASYNC because the engine constructor is HEAVY" → `new CSSKeyframesAnimation(options, ...targets).fromKeyframes(...)` (my `timelineEngine.ts` read). No throttle, no rAF coalescing, no dirty check; held arrow-key auto-repeat does the same. Scope bound verified by RR-B and accepted: CPU/garbage churn, not a leak, and `rebuild`'s <2-keyframes early-return means the storm needs ≥2 frames — the normal authoring case. TimelineTrack authored the emit granularity, so the rate books HERE; the banked L-11 (async-as-void) and N-6 (same-percent merge) rows are adjacent, not identical. **→ KF.W7**; frame-cost profile → SS-13.
- **D-5 — the zoom viewport indicator fails SC 1.4.11 in both arms** (`bg-primary/40` fill ≈1.72:1 light — RR-A re-derived from installed tokens to within 2%; rail outline ≈1.19:1) and the mini-bar is the ONLY rendering of pan position. Net (banked D-7 is the rail's own boundary; this is the zoom bar). **→ KF.W6**; exact composited ratios ride banked D-7's SS-13 residue row — do not re-measure separately.
- **D-8 — selection exposes no `aria-selected`/`aria-current`/`data-state`**: AT cannot perceive which keyframe the inline editor below operates on, and one attribute would buy glass-ui's free `prefers-contrast: more` + forced-colors treatment (both keyed on exactly those attributes — verified verbatim in the installed `accessibility.css` by RR-A). The scale-125-survives-HCM mitigation is honest and keeps this MAJOR-not-BLOCKER. The forced-colors-coverage ARM folds to banked **D-21** (→ KF.W9); the AT-state mechanism books here as the sharper rider. **→ KF.W7** (the marker's state contract).
- **D-10 — `expanded` grows the track 48→128px (2.67×) and adds no content**: whole-template walk (RR-A's, re-confirmed against my own full read) — every child stretches (`top-0 h-full`), pins above (`-top-5`), or centres (`top-1/2`; carets at `calc(50% + var(--caret-offset))`); no `v-if="expanded"` branch, no slot, no lanes; ~62% of the expanded box is a plate that D-6's banked fold says is ~1.03:1 against its own background. RR-A's net consequence adopted: the graduations stay pinned at `-top-5`, so expanded mode strands the scale ~84px from the marks it measures. Not banked (banked D-17 is the diamond/caret occlusion only). **→ KF.W7** (expanded mode earns lanes/curves or dies).

### MINOR (19)

- **D-3 — both numeric readouts break the Mono-as-data law**: `:17` (`{{ zoomLevel.toFixed(1) }}x`) and `:45-48` (`{{ tick }}%`) ride `text-small` — the proportional stack — with neither `font-mono` nor `tabular-nums`, while every sibling readout obeys (caret, hover preview, header — my reads). Family register is the net half; the SIZE-register half folds to banked **M10**. Micro-note (this seat's): `toFixed(1)` renders "1.0x" for zoom 1.0-1.049 beside a mini-bar whose `v-if="zoomLevel > 1"` says otherwise. **→ KF.W6**.
- **D-4 — the tick ladder's pitch swings 9.4× and drops 5× on a 0.01 zoom increment at z=8** (ruled table; MINOR; collision arm + `#timeline-expanded-target` width → SS-13; the `1`-step rung is simply wrong — `2`/`5` gives 16%/40% pitch). **→ KF.W7**.
- **M-7 — shift-wheel pan reads only `deltaY`** (ruled table; MINOR; `event.deltaX || event.deltaY` is the one-token cure; compounds with banked **M5**: the gesture is cancelled AND inert). **→ KF.W7**; Chromium delta log → SS-13 (escalation trigger).
- **RR-B missed-4 — pan is display-only, and at zoom > 1 may have no working input at all**: the mini range bar carries no handler, no role, no tabindex (`:4-18` — my read); the complete `panOffset` writer set is the zoom recentre side-effect, the shift-wheel branch (M-7's), and `clampPan` (my `useZoomPan.ts` read confirms exactly these three); no drag-to-pan, no click-to-jump, no keyboard route (banked **D-11** owns zoom's identical gap). The composition — the only pan readout is inert while the only pan gesture reads the wrong axis — is what makes M-7 more than a typo, and no axis assembled it. **→ KF.W7**, jointly with M-7.
- **RR-B missed-3 — the scrub path has no drag state**: `:182`'s `event.buttons > 0` is a proxy the comment above it miscalls "pointer is captured"; no `isScrubbing` flag exists (the marker path has one, `:137/:194`), so any button-held pointer ENTERING the 48/128px full-width band scrubs — a text-selection drag begun elsewhere in the pane included. The design system's own rail gates on an explicit `dragging` flag (axis C quoted it and filed only the contrast). **→ KF.W7**.
- **D-12/C-12/i-2 merged — SC 2.5.8's spacing exception is unmet and near-coincident pads make the earlier marker unhittable** (ruled table; MINOR; 24-vs-44 arm folds to banked **S-1 BOUNDED**; hit test → SS-13 folded with banked D-17/M1). **→ KF.W7**.
- **D-13 net arm — the tooltip payload sits behind a 96px scroll with `truncate` on every row** against a 17-property default capture set — the tooltip's stated purpose defeated for most `transform`/`box-shadow` values; the double-reflow arm folds to banked **M9**; the axis's own falsifier honesty stands (glass forwards `disableHoverableContent`, default off, so the pointer-unreachable-scroll claim was withdrawn). **→ KF.W7**.
- **D-14/i-1 — `margin-top: 1.25rem` (scoped, `:218-221`) and `-top-5` (`:45`) are coupled magic numbers 174 lines apart** against DESIGN.md R4; nothing states the coupling; the file's OTHER geometry constant (`--caret-offset`) was homed correctly, which is the provenance that makes this an oversight. Not banked (the banked hygiene group does not include it). **→ KF.W7**.
- **m-17 — `class="p-2 max-w-56"` on TooltipContent flattens glass-ui's designed 1.272 block/inline optical padding ratio to 1.0** — verified by RR-A against the installed `tooltip-*.js` (caller class merged LAST through the package's tailwind-merge). Not banked. **→ KF.W6**.
- **C-10/C-11/D-m6/m-1/D-m5 merged — the contract-hygiene group**: dead prop `keyframeId` (sole occurrence is its own declaration — RR-B's grep, spot-confirmed in my whole `TimelineCaret.vue` read); redundant derived `position` beside `percent` (the zoom transform lives in the parent, neither provided nor passed); the public prop type drags `KeyframeSelector` from value.js for fields the subtree never reads; `const props` bound and never read in two of three cluster SFCs (my reads confirm both); hand-written `update:*` emits + call-signature overloads against the demo's stated `defineModel`/named-tuple law (D-m5, adopted at this seat's read of `:129-134` — neither reader contested the emit facts). All unenforceable by construction per the banked gate hole. **→ KF.W7** (cure), **KF.W4** (enforcement, by fold).
- **m-9/m-11/m-12 merged — the pointer-handler hygiene group**: in-band sentinel `0` on a null ref (`:160-161` — a failure scrubs to the start); the dead `!draggingKeyframeId.value` re-test at `:182` (the `:178` return guarantees it); no `event.button`/`isPrimary` guard so right/middle-press scrubs and takes capture before the context menu opens (`:168-172`). All three re-read at the bytes by this seat. **→ KF.W7**.
- **L-m-10 — `useZoomPan(trackEl: Ref<HTMLElement | null>)` admits null then asserts `trackEl.value!`** (`:5` vs `:46` — my read); the sole caller supplies a genuinely-nullable `useTemplateRef`, so the signature cannot be narrowed and the `!` is a real unguarded dereference invisible to the (absent) type gate. **→ KF.W7**.
- **L-m-14 — `draggingKeyframeId` is never reconciled against `sortedKeyframes`**: mid-drag delete/undo dangles the id; the sink degrades silently (`useTimelineOps.ts:57` `find` → no-op, no signal — my read). **→ KF.W7**.
- **m-7/m-8 merged — `getGhostStyle` hardcodes 4 properties against the 17-property capture set** (an all-`filter` keyframe renders no ghost) **and composes `scale(0.3) ${transform}` unvalidated** (`transform: none` → the whole declaration dropped; reachable — the inline editor commits untyped text, my `:151-158` read + the banked ingress). Cross-ref banked **L-6/C-4** (the hand-rolled declaration parser) and **C-7** (the untrusted-CSS ingress). **→ KF.W7**.
- **RR-A missed-1 — the rail exposes no accessible container and its graduations are not hidden from AT**: no `role="group"`/`aria-label` on the track (`:21-35` — my read), so the N `role="slider"` markers are orphan sliders in a generic div; the tick wrappers/labels carry no `aria-hidden`, so ~5-15 bare percent strings interleave with the sliders in the tree. Registry-swept by the reader (banked a11y rows are D-3/D-20/D-21/M3/M8/D-10 — none touches container semantics); no axis filed it. Two-attribute cure. **→ KF.W7**; browse-mode lived cost → SS-13.
- **RR-A missed-2 — selecting a keyframe deletes its hover affordance**: both hover signals (`hover:border-primary` and glass's `scale-on-hover`) live only on the UNSELECTED branch (`:70-73` — confirmed in my read); the affordance gradient is inverted — the focus of work is the one marker that ignores the pointer — while the kept bespoke `scale-125` does not animate (banked D-12). **→ KF.W7**.
- **RR-A missed-3 — one gesture, two motion languages on expand**: `transition-all duration-fast` sits on the track (`:24`) whose scoped rule declares only margins (my read — nothing narrows the shorthand, unlike the marker's), so the 80px `h-12↔h-32` height delta EASES — a layout-animating property the repo's own rail contract rejects — while the markers' `w-4→w-6`, the carets, and the ticks all snap. **→ KF.W7**; Teleport-interplay magnitude → SS-13.
- **RR-A missed-4 — every marker's accessible DESCRIPTION is the tooltip's uppercased declaration dump**: reka sets `aria-describedby` to the open tooltip's content and opens on focus of the `tabindex="0"` marker; the content is the full `Object.entries(kf.vars)` list under `text-admin-label`'s `text-transform: uppercase` (the visual-uppercase arm is banked **M7**; the description-payload arm is net — tabbing ten markers reads ten uppercased stylesheets). **→ KF.W7**.
- **D-m2 — the tick-label edge-clamp threshold (2%/98%) is under-sized for the actual 400-512px rail** (half a "100%" label ≈ 5% of the rail floor; the threshold suffices only from ≈1025px). Adjudicated as banked **S-8's bounding clause** — the three-way handling is deliberate and the file's only edge-aware layer; the constant is simply mis-tuned. **→ KF.W7**.

### INFO (9)

- **C-14 — the keyframes editor's own track imports zero keyframes.js symbols** — and correctly so: routing marker chrome through the engine would be contrivance (owner KISS law); the element that wants engine drive is the playhead, which is banked **C-1**. Cross-ref banked **S-6** (the LIGHT/HEAVY firewall). Record.
- **m-3 — the affine remap hand-rolled one file from the imported module that exports it** (ruled table; INFO; under-consumption record for KF.W7).
- **L-m-4 — two clamp idioms in one 110-line composable that imports `clamp` on line 3** (`:19/:51` vs `:86-89` — my read). **→ KF.W7** hygiene.
- **L-m-5 — `percentToPosition(tick)` evaluated three times per tick per render at pointermove rate** — magnitude KILLED (ruled table: ≤ ~15 ticks, ~45 calls, not ~306); the churn shape survives. **→ KF.W7** note.
- **RR-A missed-5 — the instrument's geometry ignores `pointer: coarse` while every glass control beside it grows 1.5×**: `h-12/h-32`, `w-4/w-6`, the literal 24px pad, `--caret-offset: 14px` all frozen; no `[data-control-target]`, no `.tap-floor`. The pad's hard-px arm folds to banked **S-1 BOUNDED**; net is the one-card-two-scaling-regimes composition. **→ KF.W6**.
- **RR-B missed-5 — the pointer→percent map and the percent→pixel map use different boxes**: `getPercentFromPointer` measures the BORDER box (`getBoundingClientRect` on a `border border-border` element — my read) while every `left: N%` child resolves against the PADDING box — a fixed ~1px origin / 2px span skew that grows with any border change (exactly the cure banked D-20 prescribes). **→ KF.W7**.
- **m-6 — markers and carets are the same datum rendered in two sibling `v-for`s with independent geometry calls** (`:62`, `:98`) — neither reader adopted it; adjudicated IN at INFO as structure input for the W7 rail redesign (the Tooltip wrapper forces SOME split; the duplicated geometry does not follow from it). **→ KF.W7** spec input.
- **i-3 — the indentation break at `useZoomPan.ts:51`** (`const newZoom` at 8 spaces inside a 12-space block — confirmed in my read): the fingerprint of an unformatted hand-patch; joins the banked hygiene family (no formatter reaches the demo). **→ KF.W7** hygiene.
- **ARB-1 (this seat's own missed row) — the blind drag zone**: at zoom > 1 a captured marker drag continues past the window edge (`getPercentFromPointer` is unbounded in pointer space — `:160-166`, the clamp is domain 0-100, not window), while `percentToPosition` puts the marker outside 0-100 position-space where `overflow-x-clip` hides it — the user drags a keyframe they can no longer see, and no auto-pan follows (the writer census above proves none exists). Arrow keys/Home/End produce the same off-window disappearance. Derived and bounds-checked by this seat (an earlier stronger claim — "the drag is clamped to the window" — was formed and killed: pointer overshoot past the rail extends the reachable domain). **→ KF.W7**; percept → SS-13.

## FOLDS — identity guard (anti-rename; zero re-bookings)

Whole rows discharged by reference; each contributes this corpus's re-verification as evidence, never a second booking.

- **C-2 (fresh BLOCKER filing "the scrub emit is severed") = banked kf-KeyframeTimeline C-1** (MAJOR, cluster head, → KF.W7). Both readers caught the re-booking + rung regression; my own grep re-confirms (`scrub` absent from the 15-member destructure, `:83` bare ref write). The axis's honest counterweight rides the bank: `useTimelineOps.ts:28` keeps the playhead an authoring input.
- **D-11/M-1/C-3 (`@wheel.prevent` trap + `touch-none` escape removal) = banked M5 ≡ RR-B-3.**
- **M-2/D-m1-headline (dead marker motion: scoped `transition: transform` vs Tailwind-4 independent properties; `scale-on-hover`'s shorthand clobbered) = banked D-12**, with this corpus's compiler-probe sharpening riding as evidence.
- **M-4 (write-once `previewCache`) = banked D-4/L-4/C-5** (incl. the merged KEEP-catch correction).
- **M-5/C-5/L-M-5 (nothing typechecks `.vue`; CI runs `check:lib` only) = banked fold of kf-CSSCodeEditor KF-CE-16** (→ KF.W4, the sequencing head). RR-B's baseline nuance adopted: verified at origin/master, not the dirty worktree.
- **M-6/m-13/C-9 (caret-label pointerdown scrubs; capture on transient `event.target`) = banked M2 ≡ RR-B-7** — the generic mechanism only; m-13's latch escalation is KILLED (register, K-8).
- **m-16/C-8 (capture on `@mouseenter` only; tooltip opens on focus) = banked D-10.**
- **C-6 (`style.left` on every moving element vs the producer's "NEVER style.left" docblock) = banked C-15's cost clause** (→ SS-13); this corpus contributes the four-site enumeration (`:41/:54/:80` + caret `:4` — all in my reads).
- **C-15 (the census S-3 correction: `ScrubberTimeline` is the true counterpart, reachable but slotless) = banked C-15**, already adopted as "the corpus's best row" — corroboration recorded (RR-B re-verified the single `export { me as GlassTimeline }`, the `./fonts/*`-only wildcard, and the zero-`renderSlot` scrubber region; endorsed by both).
- **D-6 (hover delta ~1.01:1; rest plate ~1.03:1) = banked D-7** (the rail with no perceivable boundary), hover-delta arm contributed.
- **D-7 (no `.focus-ring` on the only focusable nodes; forced-colors focus forfeited) = banked D-20**; RR-A's net mitigation rides the bank: `design-idioms.css:127`'s `.cartoon-surface:has(:focus-visible)` whole-card shadow lift signals "something inside is focused" without locating it.
- **D-9 (the playhead is paint-by-background and vanishes in forced-colors) = banked D-21** (which names the playhead verbatim; → KF.W9).
- **D-15 (no empty state; six undiscoverable gestures) = banked D-15.**
- **D-2 (tick labels 1.4-1.76× the data readout) = banked M10.**
- **D-m4 (the user's label never reaches the accessible name) = banked N-2's consumer arm** (the label is write-only dead state; wiring it is the one cure for both).
- **D-m10 (the caret's unnamed 40×20px number input) = banked D-3 + M8.**
- **D-m11/C-13 (the zoom bar `v-if`-mounts mid-gesture; identity arithmetic noted) = banked D-11**; the identity-arithmetic clause (`(panOffset/100)*100`) = banked hygiene D-26.
- **D-m8/m-18 (the same identity arithmetic filed as its own row) = banked hygiene D-26.**
- **m-15 (TimelineCaret colocated above its sole consumer) = banked N-11** (→ KF.W8).
- **D-m12 (all-physical geometry; RTL latent) = banked SS-13 residue #10 (D-24).**
- **Partial arms**: D-8's forced-colors arm → banked **D-21**; D-12's 24-vs-44 arm → banked **S-1 BOUNDED**; D-13's reflow arm → banked **M9**; D-3's size-register arm → banked **M10**.
- **Census F-1** — see the killed register: this record's consequence is a RE-RULE request, not a fold-as-confirmed.

## Killed-claims register — 9 rows, refutations re-executed

1. **K-1 · B-1 (axis L, BLOCKER) ≡ C-1-fresh (axis C, BLOCKER) — "`@mkbabb/glass-ui` is a phantom dependency; `npm ci` + `npm run gh-pages` cannot resolve `:113`"** — REFUTED AT EVERY COMMITTED BASELINE, re-executed by this seat (header instruments): origin/master declares `7.0.0` EXACT with 3 lockfile rows; HEAD `8281638c` — the anchor both filings name — declares `6.0.0` under `optionalDependencies` with 3 lockfile rows (`npm ci` installs optionals); only the UNCOMMITTED worktree lacks the rows; the installed 7.0.0 equals origin/master's pin, the positive proof that manifest and disk agree at the CI baseline. Both axes' own first-listed falsifiers fire verbatim. The claim survives only as "the worktree is mid-edit". **Corpus consequence**: census **F-1** and every banked "phantom dependency" corroboration line inherit a one-baseline-short caveat and should be **RE-RULED per-baseline, not re-booked** — note the surviving true residue is different and smaller: at HEAD the installed 7.0.0 vs the declared 6.0.0 pin is a version-drift fact, not an absence. Axis D is exempt and credited: it filed F-1 as a reproducibility caveat only.
2. **K-2 · axis-L's provenance statement ("every line reference resolves against the working tree … census HEAD 8281638c")** — REFUTED: the worktree is not HEAD (41 commits behind origin/master; 7 timeline siblings modified — my own probes). Mitigating and load-bearing for this record: `TimelineTrack.vue` and `useZoomPan.ts` are byte-clean at all three baselines (my diff), so the TARGET's citations are baseline-invariant; the defect is confined to sibling anchors and the manifest.
3. **K-3 · m-2/C-7/D-m7's bundle-graph half ("the root-barrel specifier enlarges the lazy timeline chunk")** — REFUTED independently of the banked kill: the same async chunk's own parent imports `{ Button, Card, CardContent, Separator }` from the root barrel, and ~31 demo sites do — the barrel is in the graph whatever TimelineTrack does. FOLDS to banked **K-10**; only the consistency clause survives (rides banked L-18/C-11, INFO). Filing the cost three times, once per axis, is the re-filing the registry-first law forbids.
4. **K-4 · axis-L's §6.5 corpus-reconciliation row ("TimelineTrack's motion does not reach glass-ui's PRM block because the scoped shorthand overrides it")** — REFUTED at the bytes: the installed PRM bracket is `*:not([data-allow-motion])` with `!important` — universally selected AND important, outranking the unlayered scoped shorthand it allegedly cannot reach. Whether the component clobbers `scale-on-hover` (it does — banked D-12) is orthogonal to whether PRM applies (it does). Axis D's SUP-5 and the banked record both got this right; the CSS is dispositive.
5. **K-5 · D-m1's consequence sub-clause ("`transform` eases while `background-color` snaps")** — REFUTED and inverted: Tailwind 4.3.0 emits `scale`, not `transform`; nothing on the marker ever sets `transform`; the selection scale SNAPS exactly like the fill and only `border-color` transitions. Recorded because D-m1's version would send a fixer to widen the shorthand and leave the scale still dead. The correct row is banked **D-12**.
6. **K-6 · m-5's magnitude ("~102 ticks ⇒ ~306 calls at z ≥ 8")** — REFUTED by the composable's own `:34-38` window filter, re-derived by THIS SEAT: the rendered set is ≤ ~15 ticks at any zoom ≥ 3 (5 at z=1); the number that made the row worth filing is ~7× wrong. The churn observation survives at INFO (roster). RR-B's contrary confirmation is a citation-inheritance event (DISSENT).
7. **K-7 · axis-L M-3's harm sentence ("`scrubT` drives `scrub()` on the live engine, so the target re-renders through the pinch")** — REFUTED BY ITS OWN CORPUS (banked C-1; my destructure grep; the cited `useTimeline.ts:41-46` anchors show no such drive). The mechanism survives and is booked (roster M-3/C-4) with the corrected harm.
8. **K-8 · m-13's latch escalation ("capture on a transient node strands `draggingKeyframeId` latched; the next button-less move drags with no button held")** — REFUTED: the file's two capture sites have disjoint failure modes — the latch is set ONLY at `:192-196`, whose `event.target` is the marker div itself (pseudo-element hits attribute to the originating element), a node keyed `kf.id` that Vue MOVES rather than remounts mid-drag (the same fact both readers promote as a superlative); the transient nodes m-13 names (value-keyed ticks, `v-if`-swapped caret label) are only ever targets of `:168-172`, which sets NO latch. The generic `event.target`-vs-`currentTarget` concern is real and BANKED as **M2**; the escalation is unreachable and must not enter a spec.
9. **K-9 · D-1's BLOCKER rung (and axis C's fresh BLOCKER rung on the scrub emit)** — REFUTED under the program's BLOCKER law (data loss / trap / flagship-capability absence): the verb graded as the instrument's primary one is inert in EVERY modality today (banked C-1), so the keyboard gap cannot be a blocker over a function that produces no effect; precedent banked K-3. The facts survive at MAJOR (roster D-1) with RR-B's pointer-only-snapshot harm isolated.

**Citation corrections (conclusions unaffected; these documents present as provenance-exact)**: axis-L i-2's "800px track" and axis-C C-12's "~600px rail" — the rail is `clamp(25rem, 33svi, 32rem)` = 400-512px (only axis D used the real figure); axis-L's `useTimeline.ts:41-46` anchors do not show what its M-3 claims; RR-A's registry line for the banked scrub row cites it as "kf-KeyframeTimeline C-1" correctly but at `registry:41` vs RR-B's `:96` for C-15 — both resolve in the banked file (my read).

## Superlatives — provenance preserved (L-18 runs both ways)

- **S-1 · the counter-rotated 24px hit pad** — FOLDS to banked **S-1 (BOUNDED)**; this corpus adds TWO sharpenings, both verified against the installed producer: (a) glass's `@utility touch-hit-area` sets `pointer-events: none` on its `::before`, so the published pad cannot extend a hit region while the hand-roll can (axis D); (b) the published pad is gated `@media (pointer: coarse)`, so it would do nothing for mouse/stylus users, while the hand-roll is unconditional (RR-B). The bank's bounds stand (hard-px, M1's edge clipping, the scale-125 pad oscillation).
- **S-2 · the full ARIA slider keyboard template with clamping delegated, not duplicated** — FOLDS to banked **S-2** (bounded by banked M3); `preventDefault` AFTER the unhandled-key early return re-confirmed in my own `:203-214` read.
- **S-3 · `overflow-x-clip overflow-y-visible`** — the one mixed pair CSS Overflow 3 refuses to coerce, and exactly what the geometry needs; all three axes derived it from spec independently. FOLDS to banked **S-3**, bounded here by D-m2 (the edge-clamp constant is the one mis-tuned piece of the same edge-awareness).
- **NET-NEW · `:key="kf.id"` over a re-sorting computed is what makes drag-past-neighbour work at all** (axis-L S-3; adopted by BOTH readers independently; not in the banked superlative set): `sortedKeyframes` re-sorts mid-gesture on every percent write, and the id key makes Vue MOVE the captured node (`insertBefore`) instead of remounting it, so the in-flight `setPointerCapture` survives the crossing; an index or percent key would strand the drag at the crossing point; the caret loop keeps its keyspace disjoint (`'caret-' + kf.id`). Load-bearing twice over: it is also the fact that kills m-13's escalation (K-8). Source-derived, no witness dependency.
- **NET-NEW · the three-way touch posture** (axis-L S-4; not banked): `touch-action: none` → no `preventDefault` needed → `.passive` listeners — each link the precondition of the next (none of the three touch handlers cancels — my `useZoomPan.ts:75-96` read). Honestly mirrored by its own author against M-1/banked-M5: the same file blanket-cancels wheel.
- **value.js consumption is minimal, granular, and R1-contained** (axis-C SUP-3): `clamp` from the `/math` SUBPATH beside the root-barrel glass import; `import type` erased under `verbatimModuleSyntax`; the only path from a TimelineTrack emit into the value.js parser is wrapped with the `await` inside the `try` (`useTimelineBuild.ts:40-50` — my read). The ingress/posture identities are banked at **C-7** and megatranche **R1**; recorded as a negative result so parser waves skip this file. Note the containment is also why RR-B missed-2's storm is churn, not crash.
- **Corpus method, both ways** — the killed-before-filing registers are the best work in these documents: axis C's reka `.stop`-vs-`mergeProps` kill, axis L's TooltipPortal and truthy-`"0"` kills, axis D's refusal to file the retired `proof:*` gates (owner law) and its AFFIRMATIVE close of lane-frontend.md:489's PRM question — the very discipline whose absence on B-1/C-1 produced the corpus's two false BLOCKERs. Both re-readers' three-baseline kills are themselves exemplary; this seat's independent re-execution of every probe is the required response to reader convergence, and every probe held.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (route with KF.W9)

1. **M-7 / missed-4's behaviour arm** — log `{deltaX, deltaY, shiftKey}` on shift-wheel over the track in Chromium; `deltaY: 0` proves pan unreachable and ESCALATES both rows jointly to MAJOR.
2. **D-4's label-collision arm** — rendered advance of a `100%` label at `text-small` against the 32px pitch at z ≥ 8 on the 368-480px usable rail, plus the unmeasured `#timeline-expanded-target` width; collision proof escalates D-4.
3. **D-5/D-6/D-m3 composited ratios** over the live `Card cartoon tier="quiet"` plate and the expanded transparent plate — ride banked D-7's residue row #2; do not re-measure separately.
4. **RR-B missed-1's rendered grab-jump extent** at z=1 and z=10 (the MAJOR/MINOR decider for the felt severity; the mechanism is settled).
5. **RR-B missed-2's frame cost** — wall-clock of one `buildAnimationFromTimeline` + `parse` per pointermove at a realistic keyframe count; drop-frame proof feeds the W7 throttle design.
6. **Marker-vs-marker hit collision** at ≤ 2-3% separation — folded with banked D-17/M1's photographs.
7. **RR-A missed-3's height-transition vs the simultaneous Teleport** (`:disabled` toggled on the same tick) — whether the 200ms ease ever actually paints.
8. **D-13's hoverable-content reachability** across the trigger→content gap (pointer half probably reachable; keyboard half stands regardless).
9. **RR-A missed-1 + missed-4's lived AT cost** — a browse-mode pass over ~15 unhidden percent strings interleaved with N orphan sliders, each described by an uppercased stylesheet.
10. **ARB-1's blind-drag percept** — a captured drag past the window edge with the marker clipped invisible.
11. **Witness refresh** — re-run `build:gh-pages` before trusting any chunk-byte receipt (inherited caveat); the C-7 chunk question is a build-witness item, not SS-13 proper.
12. **Checked-and-negative, recorded so it is not re-probed**: the flat `--kf-*` namespace hazard does not apply (zero `--kf-` custom properties in demo/styles; the accent family is `--accent-kf*`; the cluster defines no custom properties — RR-A's grep, consistent with banked D-28).

## DISSENT — preserved, not ruled away

1. **RR-A's MAJOR rungs on D-4, M-7, D-12, and D-13's net arm** — preserved with their reopening conditions: residue rows 1, 2, and 6 are each a single live probe away from restoring the higher rung; the demotions here are burden-of-proof rulings, not mechanism refutations. Axis D's BLOCKER on D-1 is preserved at the banked C-1 dissent's terms (if the W7 spec rules scene-scrubbing the instrument's core promise, the whole seam inherits blocker weight as one cluster).
2. **RR-B's L-m-5 confirmation repeated the axis's ~102-tick figure while citing the very lines that refute it** (`useZoomPan.ts:23-40`) — a citation-inheritance event recorded for the fleet register, joining the ColorSpaceSelector event, kf-CSSPasteDialog K-9, and the banked kf-KeyframeTimeline DISSENT-4. The same reader's OTHER cells were verified sharp (its three-baseline B-1 kill, its two net-new MAJORs) — inheritance is per-cell, not per-reader.
3. **The M-3/C-4 rung** — RR-B would presumably book the mechanism lower given its kill of the harm; this seat ruled MAJOR on cure-coupling (the C-1 rewiring unmasks it). Reopening condition inverted: if the W7 spec permanently rules the scrub seam display-only, the rung drops to MINOR.
4. **Reader convergence as a risk surface** — the two readers agreed on nearly every kill and fold. Per the suspicious-agreement law this seat re-executed the four most load-bearing agreed cells with its own instruments (the three-baseline git probes; the `scrub` destructure grep; the tick-filter read; the rebuild-chain walk) — all held, and one reader-vs-reader CONTRADICTION (m-5) was found and settled where convergence broke.
5. **F-1 re-rule** — this record formally requests the census F-1 identity and the banked kf-KeyframeTimeline FOLDS line ("every glass claim resolves against an installed tree no manifest declares") be re-derived per-baseline: at origin/master the manifest and the installed tree AGREE (7.0.0 exact); at HEAD the true residue is a 6.0.0-declared vs 7.0.0-installed drift; only the uncommitted worktree is phantom. Nothing is re-booked here; the request is agglomeration input.
6. **HEAD movement** — the worktree sits 41 commits behind origin/master with 7 timeline siblings dirty; every sibling anchor in this record is worktree-resolved and must be re-resolved before any cure (D-19). The target file's anchors are exempt (byte-clean at all three baselines — my diff).

## Closing verdict

**ADJUDICATED — RED at keyframes worktree/HEAD `8281638c` (target file baseline-invariant): 35 rows stand (0 BLOCKER · 7 MAJOR · 19 MINOR · 9 INFO) with 20 whole-row folds + 4 partial arms discharged by reference to banked kf-KeyframeTimeline (C-1, M2, M5, M9, M10, D-4/L-4/C-5, D-7, D-10, D-11, D-12, D-15, D-20, D-21, C-15, N-2, N-11, S-1, S-8, hygiene D-26, the KF-CE-16 gate-hole fold) and census identities — zero re-bookings; 9 claims killed with re-executed refutations, headed by both axes' phantom-dependency BLOCKERs dying on this seat's own three-baseline git probes (the corpus's sixth one-baseline-short event, now with a formal F-1 re-rule request) and one reader-vs-reader contradiction (m-5's tick count) settled at the bytes against the confirming reader; 11 net-new missed rows booked, the two heaviest — the grab-offset teleport and the per-pointermove engine rebuild — re-walked end to end by this seat and routed to KF.W7 beside the banked scrub-seam cluster they aggravate. The component's centre of gravity: a drag pipeline that teleports data, rebuilds the world per pointermove, guards neither multi-touch nor foreign drags nor non-primary buttons, and pans by the wrong axis into a display-only readout — one gesture system, adjudicated as ONE design problem for the KF.W7 spec, exactly as the census's "evaluate, not mechanical swap" predicted. Routings: the bulk to KF.W7; token/contrast/register/coarse rows to KF.W6; structure by fold to KF.W8; the type gate to KF.W4 by fold; parser ingress negative-result recorded for KF.W2/W3; 12 residue rows to KF.W9/SS-13 with two explicit severity-escalation triggers armed. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.
