claude-fable-5

# kf-SpringPhysicsFacet — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator seat)

**Seat**: fresh Fable adjudicator · model receipt `claude-fable-5` (Fable 5), observed in this seat's own system prompt (line 1 above), not inherited — L-11/L-14.
**Date**: 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), worktree **≡ origin/master for the entire audited surface** (this seat's `git diff origin/master -- demo/scenes/spring/SpringPhysicsFacet.vue` → EMPTY; every line anchor binds to both). `origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact (my `git show`); the worktree manifests are dirty local edits with 0 glass-ui hits — the K17/SCH-1 substrate law (kf-RibbonBar.md:5) governs every phantom-dep cell below. **Registry tree** value.js `tranche-u`.
**Corpus**: three axis challenges (`challenge-D-design.md` 18 rows · `challenge-L-library.md` 13 rows · `challenge-C-consumption.md` 14 rows, all `claude-opus-5[1m]`) + two hostile re-reader returns (**RR-1** design-and-usage, slug `SpringPhysicsFacet` · **RR-2** library-and-code, slug `kf-SpringPhysicsFacet`).
**Method (M-23/L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement between them (the Chip class-merger direction, C-B1's life-or-death, the m-5 focus outcome, the container-type grade, the label-class severity, the M-5 residual, one registry citation) was re-derived against the tree with this seat's own instruments. The two convergent missed-finds (the transition shorthand; the fromString doubling) were re-executed, not adopted on agreement.

**This seat's own instruments (all read-only; one node execution against the shipped dist; zero browser probes — probe parsimony)**: full byte read of SpringPhysicsFacet.vue (242 L, worktree ≡ origin/master); `git show origin/master:package.json` + lock grep + `git status` (the C-B1 decider); **`dist/class-names-Cpy5eaBk.js` read whole** — export map `export { t as n, e as t }` (local `t` = plain join → exported `n`; local `e = a(t(...))` = the last-wins-per-`scope|group` merger → exported `t`) — **the merger-direction decider**; `chip-6ysLmScu.js:1` `import { n as e, t }` + `:65` `O = r(() => t(x({…}), g.class))` + the full variant strings (base/`_`interactive/size-md `"gap-1.5 px-3.5 py-1.5 text-small"`/cell `"glass-chip--cell flex-col gap-1.5 px-2 py-2.5 text-micro"`) + both fallthrough-attr filters (selectable: `style` + `/^(role|aria-pressed|data-state)$/i`; non-selectable adds `tabindex` + `on*`) + the `tone` branch (`--glass-fill-tint`/`--glass-fill-strength`); `dist/styles/glass.css` (18 `@import`s, glass-chip absent) + package-wide `glass-chip` refs → 0 + `--glass-fill-tinted` consumers → glass-atom.css/glass-chip.css only, **glass-atom also referenced by nothing**; `dist/styles/glass/glass-capsule.css` whole (`@layer components`; `.glass-capsule{…border-radius:var(--radius-pill)}`; `.glass-capsule-hover{transition:scale …punch, --specular-intensity …}` + hover 1.015 / active 0.97 / PRM `scale:1` arm); `dist/styles/index.css` tail `@import "./components.css" layer(components)`; shipped `dist/gh-pages/assets/index-CL_QYCiO.css` (571,110 B): `glass-chip` **0**, `btn-interactive` **0**, layer opens properties@9251/theme@12254/base@18022/components@21885/utilities@182187, utilities-layer offsets `.gap-1{`@198114 < `.gap-1\.5{`@198150, `.py-2\.5{`@209144 < `.pt-1\.5{`@209637 < `.pb-2{`@210344 (earlier copies @158679/@164423 sit in glass's components layer), `.focus-ring:focus-visible{` @109930 (components) + @551819 (unlayered demo tail), `.glass-capsule-hover{`@42830; shipped `SpringScene-JgaghR1J.css` verbatim (all three `.preset-cell[data-v-8b10d55e]` rules UNLAYERED, base rule carries the transition shorthand + `outline:1px dashed #0000`); **executed probe** `node --input-type=module` against `dist/engine/index.js`: `fromString` 3 frames → second `fromString`+`parse` → **6**, selectors `0,0,0.5,0.5,1,1` (the KF-KE-10 re-execution); css-animation.ts:169-250 (no frame reset; the addFrame loop ends in `this.parse()`); own OKLCH→sRGB + WCAG battery: `oklch(0.56 0.17 295)` → rgb(126,90,204); rest **5.197** / hover **4.688** / active **4.464**; SpringScene.vue:186-194 (`autoPlays: false` + VERDICT #19 verbatim) + :67 (`tabsContent`); App.vue:61-79 (`#tabs-content` sibling of `#target`; NO `<KeepAlive>`, reason written); SpringTarget.vue:200-228 (the three clamp policies + "the overshoot is the point" verbatim); springPresets.ts:17-42; useSpringDemo.ts:188-210/:218-228/:318-345/:370-380/:164-176 (frame gate, straight `t.spring.value` copy, `rebuildLiveSpring` = dispose + new SpringProgress + new NumericAnimation per `[response, dampingFraction]` tick); playback.ts:74 ("FEW and long-lived") + numeric.ts:80 (`_playback = new RAFPlayback()`); useSpringHotPath.ts:23-33/:87-95; useSpringKeyframesEditor.ts:55-77; usePainterRegistry.ts whole (eager `paint(...currentArgs())`, Set-delete unregister); KeyframesEditor.vue:1-40 (`<div class="contents">`, both `framed` branches, the `p-4 pt-4 m-4` footer, "SpringSidebar's quiet parent Card" at :5-6); SpringHeatmap.vue:14-32/:68-74/:275-295/:300-337 (labels, `role="application"`, RESPONSE/DAMPING bounds ≡ slider bounds, dead expose + empty watch verbatim, `container-type: size`, PRM arm); design-idioms.css:76-85 (`.focus-ring`/`.tap-floor`)/:160-190 (ball idiom, `var(--ball-tone, var(--color-progress))`)/:255-273 (subgrid + `.labeled-field-error` 1/-1); `labeled-field.js` `tooltip` grep 0, `labelClass|label-class` package grep 0, `labeled-field-copy` contains the description node (the KF-LP-4 geometry check); lucide `defaultAttributes.mjs` (no aria-hidden/role/focusable); repo greps: `btn-interactive` **8 lines / 6 files** + 0 CSS, `!important` **exactly 4 lines** (this file ×2 + design-idioms :295-296), `--ball-tone:` setters exactly 3 (all stage-subject), `forced-colors` in demo → 0, `cq*|@container` under `components/instrument/keyframes/` → 0, `.spring-pane|.preset-grid|.preset-name-row` rules → 0, `--ball-size|--ball-glow|--rail-tint` in glass-ui dist → 0, proof scripts exactly 2 + `scripts/gates/ = surface, visual`, ToggleGroup consumed at EasingTarget.vue:139, useSpringDemo return = 33 members; registry re-reads: kf-ChannelOptions (KF-CO-2/-7/-31/-33/-47; "L·B-3 ≡ F-1 FALSIFIED AT HEAD"), kf-EasingSidebar (KF-ES-10 names this site 2+2; KF-ES-34), kf-CubeScene D-11, kf-ControlsPaneWrapper D-M11 (8/6 pinned), kf-KeyframesEditor (KF-KE-10 probe + KF-KE-41 containment lean, `.keyframes-editor-scroll` named), kf-LayerConfigPanel LP-4 (the rename-geometry rider), kf-KeyframesAddDialog KAD-24/K-9, kf-AmigaScene L-i3, kf-SequencePlayhead L-11/C-10, kf-CubeTarget #34, kf-EasingTarget KF-ET-4/-14/-16/-31, kf-EditorHeader EH-16, kf-AnimationVisualizer KF-AV-17/K-12, kf-RibbonBar:5/:14, KF-CE-13 home (kf-CSSCodeEditor).

## Scoreboard

**33 roster rows stand — 5 MAJOR-weight (2 at their banks) · 17 MINOR · 11 INFO; 12 are FOLDS to banked identities (zero re-bookings) · 8 net-new rows admitted from the re-readers' missed-find registers, each re-derived or re-executed by this seat.**
**18 killed-claims register entries — including one axis BLOCKER dead on baseline, one axis MAJOR (D-M6) killed whole by a two-character export map, an entire six-cell cascade analysis dead on a stylesheet nothing loads, and THREE kills of re-reader cells (RR-2's merger reading, RR-2's C-B1 confirmation, RR-2's KF-ES-25 citation).**
**8 superlatives · 8 UNPROVEN-NEEDS-LIVE residues → SS-13.**

Blocker-weight survivors: **SPF-20** (the re-sample button doubles the keyframe stop set on every press — BLOCKER at its bank KF-KE-10, re-executed by this seat: 3 → 6, selectors `0,0,0.5,0.5,1,1`) and **SPF-3** (the preset-ball painter clamps away the overshoot that is the only thing the four presets differ by — the component's one physics job, discarded at `:159`).

## Ruled disagreements (the tri-fold's actual work)

| question | RR-1 | RR-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **The Chip class-merger direction** (decides D-M6, D-L9, the shape-cell miss, the h-auto/text-micro riders) | Chip's `t` IS the merger; consumer wins every collision; `gap-1.5`/`px-2`/`items-center` dropped from the DOM | Chip's `t` is "the PLAIN joiner"; both sides of every pair reach the DOM; axis-D's gap row *survives* | **RR-1 RIGHT — the export map inverts the local names.** | `class-names-Cpy5eaBk.js` ends `export { t as n, e as t }`; chip imports `{ n as e, t }`, so the `t` at `O = r(() => t(x({…}), g.class))` is the deduping last-wins merger, run LAST with the consumer class LAST. Group walk: `gap-1.5`(md) → `gap-1.5`(cell) → `gap-1` = **gap-1 alone reaches the DOM**; `px-3.5`→`px-2`→`px-3` = px-3; `items-center`→`items-start` = items-start; `py-2.5` survives (no consumer `py-`) and loses to `pt-1.5`/`pb-2` on utilities-layer order (@209144 < @209637 < @210344, my offsets = RR-2's own). RR-2 read the source's local names, not the dist aliasing — and its "M-6 row 1 survives by emission order" dies with it: `gap-1.5` never reaches the element. |
| **C-B1 (glass-ui phantom dependency)** | KILLED — dead on baseline | CONFIRMED, "correctly folded to census F-1" | **RR-1 RIGHT — killed.** | My runs: `git show origin/master:package.json` → `77: "@mkbabb/glass-ui": "7.0.0"`; lock 3 hits; `git status` → ` M` on both manifests; installed 7.0.0 = the pin exact. All three axes and RR-2 probed the DIRTY WORKTREE manifests. Registry law already twice on record: kf-ChannelOptions.md:55 ("≡ census F-1 — FALSIFIED AT HEAD"), kf-RibbonBar.md:5/:14 (the substrate-dead phantom-dep probe re-fired against both its readers). This corpus makes it the third and fourth firings. |
| **m-5 outcome (does the dashed ring survive keyboard focus?)** | mechanism killed; outcome UNPROVEN-NEEDS-LIVE | mechanism killed; outcome = ring SURVIVES (source order) | **Both kill the mechanism; RR-2's static prediction ADOPTED, one live read retained.** | Shipped selectors: base `.preset-cell[data-v-8b10d55e]` = (0,2,0) carrying `outline:1px dashed #0000`, on-state `[data-state=on]` = (0,3,0); `.focus-ring:focus-visible` = (0,2,0), unlayered at @551819 in the ENTRY sheet; the SpringScene chunk's `<link>` is injected after the entry sheet, so the base rule wins the tie and `outline-style: dashed` survives, colored by the (0,3,0) rule. Axis D's arithmetic omitted the scope attribute twice (rule and falsifier). Residue: one `:focus-visible` computed-style read (SS-13 #1) because link-injection order is an artifact of the loader, not the cascade. |
| **`container-type` on `.keyframes-editor-scroll`: dead or load-bearing?** | dead (MINOR); adopt L's `contain: inline-size` cure | mis-declared, NOT dead — the registry leans on it | **RR-2 RIGHT.** | kf-KeyframesEditor.md:86 (KF-KE-41): this exact declaration is one of the two containment traps that neutralize KeyframeCard's `z-modal` escape. My grep confirms zero `cq*`/`@container` consumers — so the line is wrong about itself (says query container, means containment) but not inert. Any cure (L's `contain` rewrite with a written rationale) must preserve the layout-containment trap or KF-KE-41's demotion re-opens. |
| **label-class severity** | MINOR (at bank, color-only) | MAJOR (family) | **RR-1's site grade RULED: MINOR.** | KF-CO-31 (kf-ChannelOptions.md:86) settled this statically: `.glass-label` already renders `--type-small`/500; only `--foreground` vs `--muted-foreground` differs, and the vendor default is the higher-contrast side. The family's MAJOR lives at KF-CO-2's bank; re-grading the site MAJOR would double-count the family. |
| **M-5 residual after the autoplay kill** | MINOR survives (PRM-ungated decoration on a picker) | killed whole (user-initiated; essential-content concession) | **Ruled INFO (between the two, with rationale).** | The motion exists only after the user presses Play (`autoPlays: false` + the self-terminating frame gate, both verbatim), and the four balls demonstrate the four ζ side-by-side — motion-on-request, arguably essential. What survives is one line: the spring scene remains the lane-§6.5 outlier (heatmap's 160 ms gated, the sweep not; engine `honorReducedMotion` unconsumed) — an observation for the PRM-mechanism census, not a defect grade. SPF-33. |
| **RR-2's "KF-ES-25 struck the identical band/flood arithmetic"** | (not cited) | cited as precedent | **Mis-citation.** | KF-ES-25 (kf-EasingSidebar.md:65) is the duration-rebase row. The actual precedent that struck orphan-sheet band/flood arithmetic is **KF-ET-4** (kf-EasingTarget.md:43). Substance unaffected; the cite is corrected here so the registry's cross-references stay walkable. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED**, not verified; X·KF specs are forming, and these routings are agglomeration input (M-25). Line anchors bind worktree ≡ origin/master.

**MAJOR-weight**

- **SPF-1 · MAJOR (family, at bank) — `tooltip=` ×2 is a phantom prop; the only copy explaining `response`/`damping (ζ)` renders nowhere** (:31, :41; `labeled-field.js` tooltip grep 0, my run). **FOLD ≡ KF-CO-2**, extended by KF-ES-10 which names this exact site ("SpringPhysicsFacet 2+2"). All three axes AND both filed it as new; severity is the bank's. Cure law rides the bank: KF-CO-47 (decide hover-vs-persistent per field, never rename blindly) + the KF-LP-4 geometry rider (see K-12). → **OPTIONS-UNIT** (sequenced after the KF.W4 gate).
- **SPF-2 · MINOR (color-only at bank) — `label-class=` ×2 is inert; a dead DOM attribute on `div.labeled-field`** (:30, :40; package grep 0). **FOLD ≡ KF-CO-2 + KF-CO-31** (divergence is COLOR-ONLY, wrong-direction; vendor default is higher-contrast). → **OPTIONS-UNIT** (cure = delete ×2, pick one register).
- **SPF-3 · MAJOR — the preset-ball painter clamps to [0,1], discarding 100 % of the overshoot that is the ONLY thing distinguishing the four presets — while the sibling painting from the SAME array relaxes to 1.18 under written rationale.** `:159` `clamp(values[i] ?? 0, 0, 1)` vs SpringTarget.vue:207 (no clamp) / :210 (0..1 sampler) / :222 (`0, 1.18` under ":the overshoot is the point"). Source unclamped (`progress.ts` `get value()`; `trackValues[i] = t.spring.value` straight copy, my read). ζ 0.86/0.65/0.45/1.0 → analytic peaks 1.005/1.068/1.205/1.000; `bouncy`'s blurb "pronounced overshoot, playful ring" prints inside the very cell whose ball cannot express it. Three clamp policies over one array in one scene; only the strictest is undocumented. **NET-NEW — books here** (no banked identity; overshoot rows in kf-* hit other components). Cure register: L's cure #2 KILLED (K-10); sound forms = remap `[-0.25,1.25]→[0,1]` in the painter, or a stated-geometry headroom cap. Cure value → SS-13 #4. → **NO-WAVE-OWNER** (SS-1/SS-2 spring-unit spec consume; pairs with SPF-15).
- **SPF-4 · MAJOR (BLOCKER grade killed) — the preset grid is the panel's only unlabeled section, and four mutually-exclusive presets are modeled as four independent `aria-pressed` toggles** (:66 bare div; file has ZERO `role`/`aria-*`, my grep; both consumed siblings label their grouped surfaces — SpringHeatmap :28-32 `role="application"` + label, KeyframesEditor toolbar). Un-deselectable by construction (:72 controlled `:model-value`, :169-172 discards the emitted boolean). Sharpened by this seat at the bytes: the installed Chip's selectable-branch filter strips `role|aria-pressed|data-state` (+`style`) from fallthrough — the item half of `role="radiogroup"` CANNOT be added by attribute; a ToggleGroup port (`@mkbabb/glass-ui/toggle-group`, consumed at EasingTarget.vue:139) or a producer change is required; a per-chip `aria-label` partial cure IS available (K-11). Port feasibility → SS-13 #8. → **NO-WAVE-OWNER** (+ the producer constraint rides the standing glass-ui BH relay as a rider on KF-ET-4's letter).
- **SPF-5 · MAJOR — the muted mono readout on the ACTIVE preset cell computes below the 4.5:1 AA floor in light mode.** `:79` `text-mono-caption text-muted-foreground` over `:221` `color-mix(in srgb, var(--color-progress) 12%, var(--background))`. This seat's OWN battery (token chain re-derived from style.css:130/:163 + glass tokens): rest **5.197** PASS · hover **4.688** PASS · active **4.464** FAIL — landing between RR-1's 4.44 and RR-2's 4.465; the direction (5.20 → 4.46 on selection) is arithmetic. The "plus-lighter flood lowers it further" rider is DEAD (K-3: no flood exists in the loaded cascade). Delivered pixels → SS-13 #3. → **NO-WAVE-OWNER** (+ KF.W9 witness adjacency, the KF-ET-14 pattern).

**MINOR**

- **SPF-6 · MINOR (at bank) — `btn-interactive` ×2 resolves to no rule in any loaded stylesheet** (:74, :105; my runs: glass-ui package 0, demo CSS 0, shipped 571 KB sheet 0; census 8 lines / 6 files = the pinned bank figure). **FOLD ≡ kf-CubeScene D-11 / kf-ControlsPaneWrapper D-M11 → KF.W6** (8-site sweep). Net-new rider that survives the fold: `:105` is the ONE of the 8 sites the bank's downgrade ("Button applies `.focus-ring` itself") does not cover — a bare `<button>`, carried by SPF-8.
- **SPF-7 · MINOR — the two `background:…!important` declarations are half the demo's entire `!important` budget** (my census: exactly 4 lines — :217/:221 + design-idioms :295-296), **and both are INERT**: the scoped rules ship UNLAYERED (shipped chunk verbatim) and already beat `@layer utilities`/`@layer components` without them (K-5). Strike-riders: the G-tranche exemplary rows `a-styling.md:355` ("zero !important") and `:357` ("btn-interactive … correct usage") are falsified by this file — carried unverified at the doc coordinates (SS-13 #6 note) but the local facts are mine. → **NO-WAVE-OWNER** (delete both `!important`s free).
- **SPF-8 · MINOR — `.reseed-btn` is the only interactive control in the panel outside the demo's single-sourced focus AND touch contracts** (:103-111 raw `<button>`: no `.focus-ring` (design-idioms:76-79), no `.tap-floor` (:82-85), not glass `Button` though it is consumed twice in this scene; only `hover:text-foreground` + the phantom class). Because `.focus-ring:focus-visible{outline:none}` applies only to carriers, this button uniquely keeps the raw UA outline every sibling normalized away — an *inconsistent* focus indicator inside one card (RR-1's breadth, adopted; the inverse mechanism of banked RB-1). Static height ~21 px vs the repo's declared 44 px floor; the coarse `--ui-scale` arm → SS-13 #5. D-m-3's second floor killed (K-6). → **NO-WAVE-OWNER**.
- **SPF-9 · MINOR — NET-NEW (convergent miss, both re-readers; verified by this seat at both shipped ends): the scoped `transition` SHORTHAND at :212-215 deletes the design system's own hover/press motion on all four preset cells.** The unlayered chunk rule (`transition: outline-color …, background-color …` verbatim in `SpringScene-JgaghR1J.css`) replaces LOADED `.glass-capsule-hover{transition: scale var(--duration-fast) var(--ease-cartoon-punch), --specular-intensity …}` (@42830, `@layer components`; hover `scale:1.015`, press `0.97`, PRM `scale:1` arm — all my reads). The lift and squish still fire but SNAP; the specular ramp never runs. The one piece of chip motion that survives the orphaned glass-chip.css is destroyed by a rule written to animate an outline. Kin but distinct from banked EH-16 (producer-side PRM row); registry sweep confirms no banked consumer-side transition-clobber row. Cure: longhands, or append `scale` (and `--specular-intensity`) to the list. Magnitude → SS-13 #2. → **NO-WAVE-OWNER**.
- **SPF-10 · MINOR — NET-NEW (RR-1's miss, re-derived by this seat from the export map): `shape="cell"` is a no-op by erosion.** The merger drops `gap-1.5` ×2, `px-2`, `px-3.5`, `py-1.5`, `text-small`, `items-center` from the emitted class; `py-2.5` survives to the DOM and loses on utilities-layer order; the variant's one distinguishing hook (`glass-chip--cell`) has its only rule in the never-loaded sheet. Surviving live contribution: **`flex-col` + `text-micro`** — and `text-micro` sizes no glyph (both child spans set their own; RR-2's rider adopted), while `h-auto` collides with nothing at all three removes (RR-2's rider adopted; both are INERT-not-defeated, deletable). `rounded-pill` is redundant with loaded `.glass-capsule` `--radius-pill`, not contested. The component asks for a design-system geometry variant and receives a flex-direction — the real glass-ui-conformance defect at this site. → **KF.W6** (the conformance sweep's correct target).
- **SPF-11 · MINOR — `container-type: inline-size` on `.keyframes-editor-scroll` is mis-declared, NOT dead** (:237; zero `cq*`/`@container` consumers, my grep — but banked KF-KE-41 leans on this exact line as one of the two traps containing KeyframeCard's `z-modal` escape). The comment says query container; the job is containment. Cure = `contain`-form with the rationale written down, **preserving layout containment** (the KF-KE-41 coupling is the rider). → **KFED-UNIT** (KF-KE-41's home).
- **SPF-12 · MINOR — `tabular-nums` on four ragged unformatted floats that cannot form a column** (:79; presets stringify with 1/2/2/0 fractional digits; `gentle`'s ζ renders as bare `1`). The correct idiom is 60 lines up in the same Card (SpringHeatmap :19-20 `.toFixed(2)`, my read). → **NO-WAVE-OWNER**.
- **SPF-13 · MINOR — the four best pieces of copy reach roughly one input modality** (:73 `:title="t.preset.blurb"`; the attribute DOES survive the Chip's filter — my bytes read — but `title` is not surfaced on keyboard focus and is unreliable on touch). The decision (tooltip idiom vs slotted copy) is the SAME decision KF-CO-47 owns for SPF-1 — ride it together. → **NO-WAVE-OWNER** (decision shared with OPTIONS-UNIT).
- **SPF-14 · MINOR — `(el as HTMLElement) ?? null` is a raw assertion over `Element | ComponentPublicInstance | null` with an unreachable `?? null` arm** (:146-148; would throw at 60 Hz if the `:ref` moved onto a component; `el instanceof HTMLElement ? el : null` is sound). NR-6's dead-guard reduction of `values[i] ?? 0` folds in. → **NO-WAVE-OWNER**.
- **SPF-15 · MINOR — the ball-el collector + painter loop is a verbatim duplicate of SpringTarget's, and that duplication IS the mechanism by which the clamp policies diverged** (:145-162 vs SpringTarget :186-188/:198-227, side-by-side read; only the clamp bound differs = SPF-3). The extraction home exists (`demo/composables/scene-runtime/`, four siblings). → **NO-WAVE-OWNER** (with SPF-3; a `useBallPainter(els, getValues, { cap })` forces one policy).
- **SPF-16 · MINOR — `<RefreshCw>` carries no `aria-hidden`, against the mounted sibling's own convention** (:109; lucide `defaultAttributes.mjs` read by this seat: no aria-hidden/role/focusable; KeyframesEditor does it right). Name-safe (the "re-sample" text names the button) — noise, not a name defect. → **NO-WAVE-OWNER**.
- **SPF-17 · MINOR — the child this facet mounts carries a live watcher with an empty callback and a `defineExpose` of two constants unreachable by construction** (SpringHeatmap :277 + :281-287 verbatim, my read; the facet mounts it with no ref at :58 and is the sole mount site). Attribution is SpringHeatmap's; recorded here because this is its only consumer. → **NO-WAVE-OWNER**.
- **SPF-18 · MINOR — god-prop: the facet takes the whole anonymous `ReturnType<typeof useSpringDemo>` (33 members, my count), reads 7, declares zero emits, and holds write access to `play/pause/reset/derby/scrubTo` with nothing narrowing it** (:141 + springKeys.ts:4). A named `Pick<>` costs one line. → **NO-WAVE-OWNER**.
- **SPF-19 · MINOR — `demo.seedKeyframes()` is the one parse call in the facet's reach with no error boundary, and its trailing `.parse()` is a redundant second parse** (:107 → useSpringKeyframesEditor :71-77; settled by this seat: the `fromString` body ends in `this.parse()` — K-14; contrast `useKeyframeOps`'s `withErrorToastAsync` posture). Folds into SPF-20's cure. → **KFED-UNIT**.
- **SPF-20 · BLOCKER at its bank — the "re-sample" button DOUBLES the keyframe stop set on every press. FOLD ≡ KF-KE-10, RE-EXECUTED by this seat**: node against `dist/engine/index.js` — 3 frames → second `fromString`+`parse` → **6**, selectors `0,0,0.5,0.5,1,1`; `fromString` only ever `addFrame`s (no reset anywhere; the diagnostics field gets a fresh array per call — the author considered re-parse staleness and missed the frames). The facet's 5 seeded stops go 5 → 10 → 15 with duplicated selectors and degenerate zero-length segments; at the live tree the cured length-watch renders the doubling as visible duplicate cards. All three axes missed it; C-m5 stood one inch away and declined to chase. Disposition already routed at the bank: → **KFED-UNIT** (clear-then-ingest) + **KF.W5** (library letter: `fromString` append-on-repeat, replace-or-document).
- **SPF-21 · MINOR — every 0.01 slider step tears down and rebuilds a full `SpringProgress` + a fresh `NumericAnimation` (each embedding its own `RAFPlayback`), against the library's own written "FEW and long-lived" allocation note** (useSpringDemo :341 watch → :321-339 dispose/new/new + `startLoop()`; playback.ts:74 + numeric.ts:80, all my reads). Pure churn, not a leak (no global registration; old instance dereferenced). **FOLD-EXTENDS kf-AmigaScene L-i3** (same disease, hotter site — a drag across the declared range ≈ 110 pairs). → **NO-WAVE-OWNER** (L-i3's disposition).
- **SPF-22 · MINOR (at bank) — the direct `KeyframesEditor.vue` import makes both lazy-loading barrels and their documented invariant dead code. FOLD ≡ KAD-24** (:135; sole mount site per K-9; verified at the bank). → **KF.W8** (structure/colocation).

**INFO**

- **SPF-23 · INFO — the mounted editor's sticky footer sits 2 rem from the card edge against the panel's 1 rem** (:22 `px-4` + :119 unframed inside an unpadded scroll band; KeyframesEditor :35 `p-4 pt-4 m-4` verbatim). The seam is here; the defect is upstream. → **KFED-UNIT** rider.
- **SPF-24 · INFO — the documented `--ball-tone` inheritance seam cannot occur in this subtree; every consumer resolves via the fallback** (setters exactly 3, all stage-subject — my grep; the facet renders in the shell's `#tabs-content`, a sibling slot of `#target` — App.vue :61-79 read). SpringHeatmap :291-293 documents an inheritance that never happens (and its "SpringSidebar sliders" comment at :68 names the dissolved component — feeds SPF-27). → **NO-WAVE-OWNER** (comment correction rider).
- **SPF-25 · INFO — `will-change: transform` on 4 preset balls, set at author time, never released, in a scene explicitly engineered to rest** (:197; SpringScene :186-194 verbatim). **FOLD to the banked recurring class** (kf-SequencePlayhead L-11/C-10 · KF-ET-31 · kf-CubeTarget #34 — all NO-WAVE-OWNER). Cost → SS-13. → **NO-WAVE-OWNER**.
- **SPF-26 · INFO (at bank) — nothing typechecks a template; the generator behind the phantom-prop family. FOLD ≡ KF-APP-4 = KF-CO-7 = KF-CE-16 = KF-CB-8 → KF.W4.** Booked once for the program; no re-booking.
- **SPF-27 · INFO — prose drift bundle: 41 % of the template is tranche changelog naming dissolved files; the dead `SpringSidebar` name is still load-bearing in the consumed primitive's own `framed` doc (KeyframesEditor :5-6, my verbatim read); the `SpringPreset`/`SpringTrack` re-export hop's rationale names "the sidebar" (D-L6); `:118` cites `proof:easing-sidebar-normalized` with no runnable gate (exactly 2 proof scripts + `gates/ = surface, visual`, my run).** FOLD to the prose-rot class (≡ kf-ChannelControls L-19 / kin KF-CO-33/-37) → **KF.W4-PROSE** (census S-2).
- **SPF-28 · INFO — zero forced-colors coverage anywhere in the demo; this component's rest/hover/selected distinction rides `background` + `outline-color` alone** (my grep: 0). **FOLD ≡ KF-CE-13 → KF.W9**; the component question (forced `outline-color: transparent`) → SS-13 #7.
- **SPF-29 · INFO — a 26 rem cap imposed on a sibling from outside, and `overflow-y:auto` coerces `overflow-x` to auto** (:233-238; the editor declares `animation`+`framed` only; NR-9's sticky-footer counter-check holds — `<div class="contents">` root verified). → **NO-WAVE-OWNER**.
- **SPF-30 · INFO — `const demo = props.demo` snapshots the prop non-reactively; inert at the sole call site** (:141-142; SpringScene :67 closes over a per-mount const; keyed `<Suspense>`, no KeepAlive). → **NO-WAVE-OWNER**.
- **SPF-31 · INFO — three dead class hooks read as styling seams and are not**: `.spring-pane` (:21), `.preset-grid` (:66), `.preset-name-row` (:77) have zero rules anywhere (my grep over demo + glass dist). The file's other named hooks are all live, so this is drift, not convention. Kin KF-CO-33's hygiene class. → **NO-WAVE-OWNER**.
- **SPF-32 · INFO — the flat-namespace hazard is LATENT at this site, not live** (RR-1's census sharpening, re-verified: `--ball-size|--ball-glow|--rail-tint` shipped by glass-ui nowhere — my grep 0; the remaining reads are intentional token reads). Recorded so **KF.W6** prices this file correctly (no rename spend here).
- **SPF-33 · INFO — the ruled M-5 residual**: the spring sweep, once user-started, has no PRM story in any mechanism while the same Card gates a 160 ms transition (SpringHeatmap :333-337, my read), and the engine's `honorReducedMotion` goes unconsumed. An observation for the lane-§6.5 mechanism census, not a defect (see ruled disagreements). → **NO-WAVE-OWNER**.

## Killed-claims register

- **K-1 · C-B1 (BLOCKER) + the F-1 substrate caveat carried by ALL THREE axes and CONFIRMED by RR-2 — DEAD ON BASELINE.** `origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact; lock 3 hits; the probed manifests are dirty worktree edits (` M` both); installed = the pin. My own runs. The registry had already killed this cell twice (kf-ChannelOptions:55, kf-RibbonBar:14); this corpus is the third and fourth firing — and RR-2, the seat whose axis correctly folded rather than re-derived, inherited the corpse. The audited component surface is worktree ≡ origin/master (my empty diff), so no other cell in this record is touched.
- **K-2 · D-M6 (MAJOR) in toto — "three authored cell-geometry utilities are silently defeated; the rendered cell is not the authored cell."** FALSE on all three rows, twice inverted: (a) the merger (see ruled disagreements) makes the consumer win every collision by construction — `gap-1.5` never reaches the DOM (rendered gap = the authored 0.25 rem); (b) "glass-ui is unlayered" fails on `@import "./components.css" layer(components)` (my read) — the utilities-layer copies decide, and `pt-1.5`/`pb-2` are LATER (my offsets); (c) `rounded-pill`'s cited competitor lives in the never-loaded glass-chip.css and would sit in a losing layer anyway. Zero of five authored utilities are defeated. D's closing "the design system wins, which is the right outcome" is exactly backwards — the author won, and did not know.
- **K-3 · D-M3(b)+(c) / D-L3 legs 1–4 / C-m1's premise / D-M4's flood rider / m-3's second floor — the entire glass-chip cascade analysis: EVERY RULE CITED IS IN AN ORPHANED STYLESHEET THAT NEVER LOADS.** My runs: `glass.css` = 18 imports, chip absent; package-wide `glass-chip` refs outside the file = 0; shipped 571 KB sheet = 0. There is no `background-image` fill to null, no `[data-state=on]` band, no `--chip-flood-t`, no `::after` plus-lighter flood, no "two selected treatments in two hues," nothing for `border-none` to discard. ≡ **KF-ET-4** (GLASS-OWNED, BH relay; the fix is one producer `@import`). Three independent axes each read the real installed file off disk and none ran the one grep that decides it — the STALE-DIST law in its subtlest costume: *a dist file being real on disk is not evidence it is in the cascade*.
- **K-4 · the prescribed cure `tone="var(--color-progress)"` (D-L3, C-m1) — STRUCTURALLY VOID against the installed build.** The `tone` branch writes `--tone` + `--glass-fill-tint`/`--glass-fill-strength` (chip chunk, my read); `accent-tone.css` derives channels whose only consumer is the orphaned glass-chip.css; `--glass-fill-tinted`'s consumers are glass-atom.css + glass-chip.css — **and glass-atom is also referenced by nothing** (my greps). `tone` on a Chip in glass-ui 7.0.0 paints nothing. A repair wave following this prescription would delete two working declarations and ship a no-op. Producer row rides KF-ET-4's BH letter.
- **K-5 · "the `!important` was needed / is the tell the class route lost" (D-M3 framing, C-m1) — the `!important` is INERT.** Vue scoped styles ship unlayered (shipped chunk verbatim, my read); an unlayered normal declaration outranks every `@layer`, so `.preset-cell:hover` already beats `.bg-background` (utilities) and `.glass-capsule` (components). Axis L's own falsifier (b) reached this; D and C built MAJORs on the inverse premise.
- **K-6 · D-m-3's mechanism — "`.glass-chip--interactive`'s min-block-size is why the preset cells DO meet 44 px."** Doubly false: the rule is in the orphan sheet, and even there it sits inside `@media (pointer: coarse)` (KF-ET-4's row records the lost coarse floor). Nothing gives the cells a 44 px floor on a fine pointer. The reseed-btn finding survives re-scoped (SPF-8); the cells' awarded conformance does not.
- **K-7 · D-m-5 — "the active cell's dashed ring disappears under keyboard focus."** The specificity arithmetic omits the scope attribute in both the claim AND its falsifier; shipped selectors are (0,2,0) base / (0,3,0) on-state vs (0,2,0) for `.focus-ring:focus-visible`, and source order (entry sheet @551819 vs the later-injected scene chunk) favors the ring SURVIVING. Mechanism killed; predicted outcome inverted; one live read retained (SS-13 #1).
- **K-8 · D-M5's premise — "autoplaying, indefinitely looping 60 Hz ball motion."** SpringScene :194 `autoPlays: false` under the verbatim VERDICT #19 comment; `frame()` self-terminates on its first tick while the machine is not playing (my reads). The motion is user-initiated; D's own axis-L sibling cites the same verdict forty lines from the claim. Residual demoted to SPF-33 (INFO).
- **K-9 · D-M1's "split typographic register" framing — over-claimed and pre-adjudicated.** ≡ KF-CO-31: color-only, wrong-direction, size and weight already match; the UNPROVEN tag D re-attaches was explicitly discharged there. Axis C stated it correctly.
- **K-10 · D-L1's cure #2 ("overflow: hidden on `.preset-track`, cap 1.25") — unsound at the bytes.** The track is `h-2` = 8 px (:81); the ball is `--ball-size: 0.85rem` = 13.6 px (:193). The cure would clip ~2.8 px off the ball top and bottom on all four cells, permanently, in every state. Recorded so no repair wave adopts the middle option verbatim.
- **K-11 · C-M5's falsifier parenthetical ("Chip filters role|aria-pressed|data-state|name|required, so the consumer cannot add them by attribute either").** The installed selectable-branch filter is `style` + `/^(role|aria-pressed|data-state)$/i` — three names; `name`/`required` are declared PROPS (my bytes read). `aria-label(ledby)`, `aria-describedby`, and `title` all pass — which is why `:title` works, and why the cheapest partial cure (per-chip `aria-label` naming the numerals) is NOT foreclosed. `tabindex` is filtered only on the NON-selectable branch (RR-2's nuance, verified).
- **K-12 · the "one edit, 13 sites: `tooltip=` → `description=`" whole-fix prescription (all three axes) — ≡ the banked KF-LP-4 cure-rider.** The compiled LabeledField renders `description` inside `.labeled-field-copy` — grid item #1, the shared `auto` label track of the §LABEL-subgrid idiom (verified at both ends: `labeled-field.js` + design-idioms :255-273). This facet's two 42/47-char strings would pour into the uniform label column of the very `.labeled-field-grid` all three axes award a superlative. Sound cures live at the bank (explicit Tooltip idiom, or a demo `.labeled-field-description { grid-column: 1 / -1 }` mirroring the existing error rule).
- **K-13 · C-m4 ("two seams for one context") and its falsifier — the falsifier is factually wrong and decides the row.** `tabsContent` is RENDERED by App.vue's `#tabs-content` slot (:61-62), a sibling of `#target` where SpringScene mounts — SpringScene is not an instance-tree ancestor of the facet, so its `provide` is unreachable. The prop is FORCED by the shell's slot architecture (axis L's NR-1, exactly right; slot geometry corroborated at kf-EasingSidebar:9).
- **K-14 · C-m5's open "secondary asymmetry" — SETTLED against the explicit call.** `fromString`'s addFrame loop ends in `this.parse()` (my source read at the loop tail), so the constructor's bare `.fromString(...)` is correct and `seedKeyframes`' trailing `.parse()` is a redundant second parse — and under SPF-20's append semantics, the redundancy is the least of what those two lines do.
- **K-15 · D-M2's mechanism half ("glass-chip--interactive supplies hover/press/focus material").** The class string is quoted correctly and attributed to a rule with no loaded declaration. What actually saves the cells is `.glass-capsule-hover` (loaded, `@layer components`, scale 1.015/0.97 + PRM arm — my read) + `.focus-ring` + `cursor-pointer`. The correction matters twice: the surviving material is exactly what SPF-9's shorthand un-eases, and the conclusion (cells degrade gracefully, reseed-btn does not) stands on the corrected mechanism.
- **K-16 · RR-2's D-L9 confirmation ("Chip's `t` is the PLAIN joiner … both sides of every colliding pair survive into the class attribute") and its dependent "M-6 row 1 survives by emission order" — KILLED by the export map** (ruled disagreements). D-L9 is RESCOPED, not confirmed: the only collision that reaches the DOM is `py-2.5` vs `pt-1.5`/`pb-2`, and it resolves to the authored side; the variant-re-authoring observation survives as SPF-10's erosion form.
- **K-17 · RR-2's C-B1 cell ("confirmed and correctly folded … BLOCKER at the census") — killed with K-1.** The fold discipline was right; the fact folded was a corpse. Recorded as the corpus's second citation-inheritance event (the first is the three axes inheriting each other's worktree probes).
- **K-18 · RR-2's "KF-ES-25" precedent citation — mis-cite.** KF-ES-25 is the duration-rebase row; the orphan-sheet arithmetic strike precedent is **KF-ET-4** (kf-EasingTarget.md:43). Corrected so cross-references stay walkable.

## Superlatives (L-18 runs both ways; provenance per row)

- **★ S-1 — the 60 Hz painter** (:151-162 + :187-198; all three axes + both re-readers, sharpened at this seat): `transform` never `left`; `translateX(<n>cqw)` against `container-type: inline-size` declared on the smallest element that can own it — **zero geometry reads**, no cached width to desynchronize; centring in the cascade (`left: 0` + half-size negative margin) so the per-frame string is one interpolation. The correction the axes missed (RR-2, sustained by banked K-12/KF-AV-17): the demo's OTHER cqw idiom maps progress over the FULL container width and overshoots at p=1 — this facet's half-size-margin form is the one correct at BOTH ends, and the pattern the census S-3 replacement wave should adopt. The tragedy of the component: the clamp bound inside this otherwise-exemplary line is SPF-3.
- **★ S-2 — state in the one box decoration that never participates in layout** (:209-215; RR-1's own find): `outline: 1px dashed transparent` declared at REST so the transition has something to interpolate and selection cannot shift geometry; `outline-offset: -1px` pulls the dash inside the pill; only `outline-color` changes across three states. Strictly better than the border-swap the repo reaches for elsewhere; no axis named it.
- **★ S-3 — teardown exact, total, and verified against the real mount shape** (:150-163; axis L's S-L3/S-L4, re-verified): one acquire, one release, null-guarded; the file owns NO other effect; the registry unregister is a Set-delete so remounts cannot accumulate; and the `onMounted` placement is load-bearing — `usePainterRegistry` paints eagerly at registration (my read), so registering after the `:ref` callbacks fill seats all four balls without a blink; `setup()` would no-op the eager paint, `onUpdated` would storm. `onScopeDispose` is safe precisely because App.vue records NO `<KeepAlive>` with the reason written (my read).
- **★ S-4 — zero reactive writes on the 60 Hz path** (axis L's S-L1): every reactive read in the file is a cold-path user parameter; `springLive` appears once, inside the painter, as a non-reactive snapshot (useSpringHotPath :87-95). The discipline useSpringHotPath :23-33 was written to enforce, honored without one leak back.
- **★ S-5 — domain coherence between the two control surfaces** (RR-1's find, re-verified at the bytes): SpringHeatmap's RESPONSE/DAMPING bounds (0.1/1.2 · 0.2/1.5) are byte-identical to the two sliders' `:min`/`:max` — every navigable field point is slider-reachable and vice versa; in a panel where three mechanisms write the same two refs, an invariant held by construction. (The heatmap's own comment crediting "the SpringSidebar sliders" feeds SPF-27.)
- **★ S-6 — `:framed="false"` is the producer seam used, not a CSS suppression** (:119; three-axis convergence, structure confirmed): two REAL template branches in the editor, a `contents` root, the prop declared and documented, default serving the standalone pane. The sharpest available contrast with the same file's `!important` pair fifty lines later — the correct instinct and its abandonment in one 242-line file.
- **★ S-7 — the value.js subpath is the minimal correct one, and the R1 crash class does not reach this subtree** (axis C's S★1/S★2): `@mkbabb/value.js/math` = nine pure functions, zero imports; every user-driven parse below the facet runs through `withErrorToastAsync` with Retry; the single exception is machine-fed (SPF-19/20).
- **★ S-8 — method superlative: axis L's §4 "verified NON-defects" table** (RR-2's note, endorsed): nine tested-and-refused hypotheses, one of which (NR-1) is the decider that kills C-m4. Writing down what was checked and NOT found is what this fourth-reader seat needed most, and it is the artifact the rest of the corpus should imitate.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. The `:focus-visible` computed style on the SELECTED preset cell — this record's static prediction is the dashed ring SURVIVES (source order); one read seals it.
2. The composed visual magnitude of SPF-9 — how legible the loss of `--ease-cartoon-punch` easing is on the cells' hover 1.015 / press 0.97 scale and the `--specular-intensity` bloom, now that all three snap.
3. The delivered active-cell contrast against this seat's 4.464:1 (browser oklch gamut mapping + `color-mix` rounding own the last digit; the direction is arithmetic).
4. Whether a relaxed clamp's ball visibly crosses into the neighbouring cell — decides SPF-3's CURE VALUE (1.18 vs 1.25 vs remap), not the finding; nothing in the loaded cascade clips it (`.glass-capsule` sets no overflow; the Card is `overflow-visible`).
5. `.reseed-btn`'s computed height against the 44 px `.tap-floor` under the live `--type-caption` clamp and the coarse-pointer `--ui-scale` arm.
6. Whether the 14 %-tinted rail still reads as a groove on the 12 %-washed ACTIVE cell (the delta collapses on exactly the cell the user attends to). Also outstanding at doc coordinates: the two G-tranche exemplary rows SPF-7 proposes to strike (`a-styling.md:355/:357`) — carried unverified.
7. The forced-colors rendering of rest/hover/selected (folds to KF-CE-13 → KF.W9); the component-specific question is `outline-color: transparent` under a forced palette.
8. Whether a ToggleGroup/ToggleGroupItem port can host the two-row slotted cell content (name row + live track ball) near-pixel-identical — SPF-4's cure feasibility; the Chip's attr filter makes the port more nearly mandatory than any axis said.

## DISSENT — preserved, not ruled away

1. **The merger-direction event is the corpus's sharpest method lesson**: RR-2 quoted the correct file, the correct line, and the correct final expression — and read the SOURCE's local names instead of the DIST's export aliasing, producing a confident inversion that would have re-litigated a correct kill and banked a false confirmation. A two-character export map (`export { t as n, e as t }`) decided an axis MAJOR, one re-reader row, and two missed-find riders. The falsifier discipline that catches it: when two readers assert opposite mechanics of the same bytes, the bytes are shorter than the argument — read them.
2. **RR-1's caution on the m-5 outcome is preserved against this seat's adoption of RR-2's prediction**: link-injection order for async chunk CSS is loader behaviour, not cascade law; if a build change inlines or reorders the scene chunk's sheet, the tie flips silently. The SS-13 read is one selector away and should be taken.
3. **RR-2's genuinely independent contributions are recorded beside its two killed cells**: the KF-KE-10 headline miss (the single most consequential row in this corpus), the KF-KE-41 containment lean, the allocation-churn find, and the KAD-24 fold are all RR-2's — the seat that mis-read the merger also out-read everyone else on the engine seam. Neither fact discounts the other.
4. **Grade dissent, unresolved and harmless**: RR-2 graded the label-class family MAJOR at the family, RR-1 MINOR at the site; this record books the site MINOR under the bank's own KF-CO-31 ruling. If the OPTIONS-UNIT ever re-opens the family grade, both positions are here.

---

**Closing verdict**: SpringPhysicsFacet is ADJUDICATED as the spring scene's best engine consumer wearing its worst glass-ui boundary — a compositor-exemplary painter whose one clamp bound erases the physics it exists to show (SPF-3), above a re-sample button whose every press doubles the stop set at its banked BLOCKER home (SPF-20 ≡ KF-KE-10, re-executed 3→6 by this seat), inside a chip surface whose phantom props, phantom class, and phantom variant all fold to banked identities — while the corpus's loudest cascade indictments died together on one orphaned stylesheet, one dirty manifest, and one inverted export map. 33 rows stand (12 folds, 8 net-new), 18 claims killed, 8 superlatives, 8 live residues to SS-13; routings are agglomeration input for the forming X·KF specs (M-25), nothing here stamps a wave.

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3; this file is this seat's only write.
