# wb-mix-configbar — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5`

**Seat**: fresh-Fable adjudicator (M-23/L-18) · model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **HEAD**: `4ddf11d1` · branch `tranche-u` · **Corpus**: `demo/workbenches/mix/MixConfigBar.vue` (172 lines) plus the contract surfaces it rides (useMixingState.ts · MixPane.vue · MixSourceSelector.vue · color-session/color-chips/sample.ts · color-session/color-space-meta.ts · picker-color.ts · demo/palettes/mix.ts · installed glass-ui 7.0.0 Button/Select/WatercolorDot · vite/tsconfig/vitest/package manifests). Challenge axes on disk: challenge-C-implementation.md · challenge-D-design.md · challenge-L-library.md, **all EXISTS-ORIGINAL** (HYDRATION-LEDGER.md:242-245), with five dated prior rounds per axis beside them. The charge names the governing round "r2"; the governing corpus is the current un-suffixed round (the readers' "r6"), which subsumes the priors by carriage — that round plus the two current re-reader returns is what this file adjudicates. `git diff c654824e HEAD -- demo/workbenches/mix demo/color-session` is EMPTY (re-run this session): subject bytes identical to the challenge baseline; the readers' `08232cd3` anchors re-resolve unchanged at `4ddf11d1`.
**Re-readers adjudicated**: TWO hostile returns (design-and-usage = "reader-DU" · library-and-code = "reader-LC"): reader-DU = 12 confirmed · 6 killed · 4 missed; reader-LC = 17 confirmed · 5 killed · 4 missed. Both were attacked before adoption; every kill re-derived; every promoted miss re-walked in source; **one reader-DU miss KILLED by this seat as already-banked** (K-10); **one reader-DU confirmation RESCOPED by this seat on producer bytes** (K-11 → MC-6); one banked-registry contradiction (wb-generate-pane K-10 vs the installed pin) ruled with this seat's own probes.
**Arbiter's own instruments (static + node against installed dist — probe parsimony; zero browser tooling)**: whole-file reads of MixConfigBar.vue, useMixingState.ts, sample.ts, demo/palettes/mix.ts, color-space-meta.ts, MixPane.vue + picker-color.ts:30-119 + MixSourceSelector.vue:140-283 + color-chips/index.ts:16-33; installed glass-ui 7.0.0 probed by node — `watercolor-dot.js` (**`inheritAttrs:!1` true; `renderSlot|$slots` = 0; `aria-hidden` + `pointerEvents:"none"` hardcoded**), `components/button/styles.css` (**`.button { min-block-size: var(--control-h-md); gap: calc(0.375rem*var(--ui-scale)); border: 0; font-weight: 500; … }`; box-shadow set ONLY on `:not([data-tone="neutral"])` (`--shadow-sm`) and `[data-emphasis="quiet"|"text"]` (`none`) — the base neutral rule sets none, so `.glass-wash`/`.glass-capsule` shadows apply**), `styles/tokens/sizing.css` (**`--control-h-md: max(calc(2.5rem*var(--ui-scale)), var(--control-floor))`; `--control-text: calc(var(--type-small)*var(--ui-scale))`; `--ui-glyph`**), `styles/tokens/light-dark.css` (**exactly one `@media (pointer: coarse)` → `--ui-scale: 1.5; --control-floor: 2.75rem`**), `select-BcBAyLXA.js` (**trigger classes `h-(--control-h-sm)/(--control-h-md)`, NO min-block-size; `isSelectionValue` throw "non-scalar" present**), `components/select/index.d.ts` + `dist/index.d.ts` (**`SelectionValue` 0 hits in both; declared ONLY in `_shared/selection.d.ts`; `SelectProps.modelValue` / `SelectItemProps.value` / `SelectEmits["update:modelValue"]` all annotated `SelectionValue`**), `styles/index.css` **import order: `./glass.css` (ladder/capsule/material) precedes `../components/button/styles.css`, both `@layer components`** — the D·R6-7 cascade decider; `styles/glass/{ladder,glass-capsule,material}.css` + `styles/tokens/glass-fx.css` (**`--glass-shadow-wash: var(--shadow-sm), var(--glass-highlight)`; `--glass-rim-top/bottom` ride their own white/foreground inks, NOT `--glass-highlight`**), `styles/glass/a11y-fallback.css` (**reduced-transparency zeroes `--glass-level`/`--glass-grain-opacity`/`--glass-highlight` — and nothing else**), typography `utilities.css` (`.section-label { @apply text-mono-caption }`) + `scale.css` (**`--type-caption: clamp(0.75rem, 0.71rem + 0.21vw, 1rem)` — viewport-fluid, `--ui-scale`-blind; 14.384px @1440 / 12.179px @390, reader-DU's three-decimal arithmetic reproduced**); demo/styles/foundation.css:728-790 (both a11y media blocks read verbatim); animations.css vj-morph + vj-celebrate incl. the `--vj-celebrate-collapse/expanded` max-height pair; vite.config.ts:80-138 (mechanism-C prose) + :139 `siblingFsAllowTransient` + :285-288 `fs.allow`; package.json (**`pretypecheck` yes / `pretest` ABSENT; deps = glass-ui ^7.0.0 + keyframes ^6.0.0, NO self-dependency; reka-ui ^2.9 devDep**); package-lock (**glass-ui 7.0.0 registry tarball; `node_modules/@mkbabb/value.js` = 4.0.0 registry**); `lstat` glass-ui symlink = **false**; `ls test/dist` = **ENOENT** vs vitest.config.ts:22-27's comment; css.d.ts surface diff (**383 vs 351 lines; `^export` = 53 BOTH; `_2` refs 58 local vs 0 installed**); greps at HEAD: `strictTemplates|vueCompilerOptions` = **0** across tsconfigs+vite · `noUnusedLocals` = 0 · eslint `no-unused-vars: "off"` (:71) · router/App/shell `beforeEach|afterEach` = **0** · `section-subtitle` .vue consumers = **0** (utils.css:18 + DESIGN.md:58 live) · `weights` outside mix.ts = PaletteColorStrip's unrelated prop + test/mix-v4.test.ts only · `computed` in MixPane.vue = 1 hit (the import) · usePaneRouter.ts:219-222 mix actions carry NO `disabled`; probes/r6-out4.json re-opened: **`light.info.opacity = dark.info.opacity = "0.5"` on both "verb-enabled" captures; coarse taps 158×36 / 324×60; `fit.widths` includes `Linear sRGB` (a string the tree spells `sRGB Linear`, picker-color.ts:83) and `Discard extras`** — the seat's own evidence file convicts and acquits exactly as reader-DU said; registry sweeps: PreviewRamp.md R-1/R-2/R-3/R-8/R-9/R-11/R-16 re-read · GenerateControls.md rows 1+5 · wb-generate-pane.md:104 (K-10) · `mixColorSequence|centroid` in registry = 3 files, all unrelated mechanisms · `surface-reduced-opaque` in registry = **0** · wb-mix-animationcanvas.md MX-3/MX-19/MX-39; X wave bounds re-read from the wave files: **X-W6.j owns MixConfigBar.vue + MixPane + MixSourceSelector (W6.md:77/:315, gate j2); X-W7 shares MixConfigBar (W7.md:126, W6∥W7 at :152); X-W1 owns CI e2e (CC-031, B3 census 0 workflows); foundation.css: X-W5 carve = layout rows :471/:479 only, W8 DO-NOT-TOUCH, W10 = `.underline-tabs:554` survivor only — the :728-790 a11y media blocks are in NO wave's bounds**; motion-quarantine.md grep `MixConfigBar` = 0.

## RULING PINS (this seat's own derivations)

1. **The preview-apparatus BLOCKER is banked; this record folds, never re-mints.** The full chain (WatercolorDot drops `@click` → `operandColors` never reaches 2 → sample.ts:58 `null` → every `PreviewRamp v-if` false at :111/:133) is PreviewRamp.md **R-1** verbatim, re-confirmed at HEAD by this seat's own producer probe (the fifth-plus independent verification of `inheritAttrs:!1`). Both readers' BLOCKER rows are the same identity. Anti-rename law: ONE row, routed where R-1 routed it (X-W6, GenerateControls B1 census cross-noted).
2. **The verb is NOT permanently disabled — the palettes arm is native and alive.** useMixingState.ts:50-53 is mode-branched; MixSourceSelector.vue:246-268 is a real `<button type="button" aria-pressed @click="togglePalette">`; `addPalette` (:65-68) dedupes on slug and fires. C·R6-1's derived clause (":139-141 false forever") is over-broad by exactly one mode; the corpus's two unexplained enabled-verb sightings (C:131-138 "provenance unknown"; L's §7.2) have the ordinary in-product explanation. Both readers found this independently — UPHELD, and it re-grounds the residue: the *intended-state* contrast measurements can be taken in the shipped app with two saved palettes, no surgery.
3. **The strongest unbanked row in the corpus is reader-LC's L-R4-1: the chip and the verb compute different mathematical objects.** Re-derived whole by this seat: sample.ts:68-84 walks `segments = N−1` and emits a **piecewise path** through the operands; mix.ts:57-66 folds `mixed = mix(mixed, cᵢ, wᵢ/total)` — a **running weighted centroid**. They coincide only at N=2 (equal weights put the centroid at the path midpoint); at N≥3 the produced color is generally not ON the previewed path at all — the path *ends at the last operand*, the result does not. The file's own prose asserts the opposite (MixConfigBar.vue:38-43 "the preview ramps' truth inputs"; sample.ts:15-16 "a chip that approximates the library output is FORBIDDEN"). Registry sweep: `mixColorSequence|centroid` hits three unrelated records — **UNBANKED**. This also corrects reader-DU's missed-2 parenthetical ("any single column is an N-operand interpolation the existing `sampleInterpolationRamp` already handles"): a column mix is the centroid-fold object, not the piecewise object — the existing sampler previews the WRONG thing for columns too; the palettes-preview design question is open, not solved.
4. **D·R6-7's boundary-erasure limb dies on producer bytes; the background identity survives.** Source-certain: under `prefers-reduced-transparency: reduce` the verb (`.glass-wash`) and its card (`.glass-resting`) both take `background-color: var(--surface-reduced-opaque, var(--card)) !important` (foundation.css:763-790), and `.button { border: 0 }` BEATS `.glass-wash { border: 1px solid … }` (button/styles.css imported after glass.css, same layer, same specificity — this seat's import-order read). But the neutral `.button` base sets NO box-shadow, so the verb keeps `.glass-capsule`/`.glass-wash`'s `box-shadow` — `--glass-rim-top/bottom` inset hairlines plus a `--shadow-sm/xl`-class drop shadow — and the ONLY leg either reduced-transparency block zeroes is `--glass-highlight`. "The verb has no boundary at all" is therefore FALSE at the source level; what ships is a same-fill control distinguishable only by rim/shadow. Reader-DU's "UPGRADED: provable from source, no live cell needed" is itself the round's overreach — RESCOPED to MINOR (MC-6) with a live-perceptibility revival clause (R-10). Reader-LC's live-only framing was the more honest one.
5. **The r6-out4.json receipts settle three D rows at once.** Both "verb-enabled" captures carry `opacity: 0.5` — `.button:disabled { opacity: var(--opacity-disabled); pointer-events: none }` still matched at measurement time, because `removeAttribute('disabled')` cannot change a Vue prop and the producer's hover/press classes are reactive-prop-gated (`glass-capsule-hover` computed from `!disabled`; `useLiquidPress` `v = () => { _() || m.press() }`). The C axis's OWN dump of a genuinely enabled verb lists `glass-capsule-hover` verbatim. So D·R6-4 is refuted (the file withholds nothing), D·R6-5's enabled rows are unmeasured, and the coarse `fit.widths` list measures strings from the PICKER's 17-member vocabulary (`Linear sRGB` — misquoting `sRGB Linear`) against this component's nine ≤5-glyph labels (color-space-meta.ts:26-36) — D·R6-3's headline describes nothing this component can render. All three kills UPHELD with the seat's own file reads.
6. **The `SelectionValue` contradiction is ruled against the banked row.** wb-generate-pane.md K-10 asserts "the producer exports `SelectProps`/`SelectEmits`/`SelectionValue` at component barrel + root (my reads)". This seat's probe of the installed pin: `SelectionValue` = 0 hits in `dist/index.d.ts` AND in the select barrel; it is declared only in `_shared/selection.d.ts`, which no export key reaches. **K-10's evidence cell is FALSE at glass-ui 7.0.0** (recorded here as a registry correction — the dated file is not rewritten). Both consequences follow reader-LC: (a) C·R6-5's prescribed cure (`import type { SelectionValue } from "@mkbabb/glass-ui"`) does not compile; (b) NO BH relay is required — the type is derivable today from three published surfaces (`SelectProps["modelValue"]`, `SelectItemProps["value"]`, `SelectEmits["update:modelValue"][0]`, all annotated `SelectionValue` in the shipped d.ts). The underlying reka-reach defect survives as MINOR (MC-21).
7. **The coarse-pointer register kills the file's hard heights from both ends, by token arithmetic alone.** `--control-h-md = max(2.5rem·--ui-scale, --control-floor)`: at `--ui-scale:1` it is exactly 40px (so `h-10` is a no-op on desktop) and on coarse it is max(60px, 44px) = 60px carried by `.button`'s `min-block-size` (so `h-10` is defeated on touch — dead at both ends, reader-DU's sharpening adopted). The SelectTrigger applies its size class with NO min-block-size, so the consumer's `h-9` WINS the merge → 36px against a 60px register / 44px floor. And the caption law splits: `.section-label` → `--type-caption` (viewport-fluid, scale-blind) vs the trigger's `--control-text` (scale-riding) — the caption:value ratio inverts between pointer classes by token construction. All producer-byte-certain; only the rendered magnitudes remain live.

## Scoreboard

**28 adjudicated defect rows: 1 BLOCKER (banked fold) · 12 MAJOR · 10 MINOR · 5 INFO.** 11 killed claims with written refutations (2 issued by this seat, incl. one kill-of-a-miss); **7 reader misses admitted** (1 further miss killed as already-banked); one banked-registry evidence cell corrected (PIN 6). Headline: **the file's every internal declaration is either dead against the producer's register or actively defeating it, its preview apparatus is a banked-BLOCKER's unreachable limb — and the one row nobody had banked is that the preview, were it ever reachable, previews a different mathematical object than the verb computes, in both modes, with the truth-law prose asserting otherwise.**

## Ruled disagreements (the seat's actual work)

| dispute | positions | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|
| **C·R6-1 booking** | reader-DU: confirm as new BLOCKER (colors limb); reader-LC: FOLD to PreviewRamp.md R-1 | **FOLD (MC-1)** | PIN 1 — identity verbatim with the banked R-1 chain; anti-rename. Both readers' verb-limb kill UPHELD (PIN 2). |
| **D·R6-7 severity + decidability** | reader-DU: MAJOR, "provable from source, no live cell needed"; reader-LC: UNPROVEN-NEEDS-LIVE | **RESCOPED MINOR (MC-6), against BOTH** | PIN 4 — background identity + border-0 ARE source-certain (DU right, LC too conservative); the no-boundary totality is source-FALSE (rim + drop shadow survive; only `--glass-highlight` zeroed). Revival R-10. |
| **L-L-8 severity** | reader-LC: MAJOR (ownership hole + live symptom) | **MINOR pending witness (MC-15)** | The zero-guards fact is mine too (grep 0); the severity carrier (body `pointer-events: none` surviving a hash route change) is undecidable statically and was not re-taken. Revival R-6. |
| **D·R6-2 booking shape** | reader-DU: three MAJORs (a/b/e); reader-LC: one mechanism MAJOR + a banked-MINOR species (h-9≡size="sm") | **two rows (MC-3 heights · MC-4 type-law) + fold** | PIN 7 — (a)+(b) share one register mechanism; (e) is a different law (typography vs control tokens) with a producer-utility ownership caveat; the h-9-as-magic-number species folds to GV-19. |
| **`SelectionValue` cure road** | C axis: named import; reader-DU: BH relay first or local literal; reader-LC: consumer-derivable, no relay | **reader-LC wins (PIN 6)** | This seat's d.ts probes: unexported at the pin (banked K-10's cell false) AND derivable from three exported annotated surfaces. The mandatory-relay framing dies with the cure; an INFORMATIONAL note may ride the standing relay (naming gap real). |
| **reader-DU missed-4 (slot-drop limb)** | DU: "a limb the banked parent row does not carry" | **KILLED BY THIS SEAT (K-10)** | PreviewRamp.md R-11 carries exactly this limb — the discarded `<Plus>` glyph, the `.add-slot-ghost` rule centring nothing, the dead `disabled:*` classes. DU swept GenerateControls but not PreviewRamp. Already banked; re-booking refused. |
| **D·R6-3's surviving limb** | DU kill: headline dead, `Decreasing` 111.5/110 survives | **kill UPHELD; limb → R-3 residue** | PIN 5 — my own read of the probe JSON + color-space-meta labels; anchor erratum noted (DU's "SPACE_LABELS" is spelled `PICKER_SPACE_NAMES`, picker-color.ts:72-90; substance unaffected). |
| **L-R6-4 drift sub-claim** | LC kill: exported surface symbol-identical | **kill UPHELD, counts corrected** | My own measurement: 53 `^export` lines BOTH; the 32-line delta is `_2` private fork declarations (58 occurrences local, 0 installed — LC's "25" was a line-count, mine an occurrence-count; same direction). |

## Adjudicated defect roster

Line anchors are HEAD `4ddf11d1` (≡ baseline `c654824e` for all subject files); re-resolve before executing any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Route legend: X-W6.j / X-W7 / X-W1 / X-W10 own the named surfaces today (bounds re-read this session); **NO-WAVE-OWNER** rows are booked for the next formation boundary. sample.ts, mix.ts, useMixingState.ts and foundation.css's a11y blocks sit in NO X wave's bounds (PreviewRamp R-8 precedent + this seat's wave-file greps).

### BLOCKER

- **MC-1 — C·R6-1 (ramp limb) ≡ reader-LC's fold ≡ PreviewRamp.md R-1 — IDENTITY-FOLD, confirmed at HEAD, never re-booked.** The whole T-17 apparatus (~60 of 172 lines) is unreachable: both operand writers are glass-7 WatercolorDots whose `@click`/`tag`/`aria-label` silently drop (`inheritAttrs:!1`, class/style-only readback — this seat's node probe); `operandColors` cannot reach 2; sample.ts:58 nulls; every `<PreviewRamp v-if>` at :111/:133 is false. The **verb limb is STRUCK** (PIN 2, K-1). → **fold to R-1: X-W6(.j)**, GenerateControls B1 census cross-noted; oracle limb rides MC-9.

### MAJOR

- **MC-2 — reader-LC L-R4-1, ADMITTED as the corpus's strongest unbanked row (PIN 3): the preview chip (piecewise path, N→k) and the Mix verb (running weighted centroid, N→1) compute different mathematical objects; they coincide only at N=2; the file's own truth-law prose asserts identity.** sample.ts:68-84 vs mix.ts:57-66, both read whole by this seat; both reached through the SAME two v-models (:57-74 chips; :166 `emit('mix')` → useMixingState.ts:89). Cure surface spans the chip sampler, the mix fold, and the prose. → **NO-WAVE-OWNER** (sample.ts + mix.ts); the MixConfigBar.vue:38-43 prose correction rides **X-W6.j** as a rider.
- **MC-3 — D·R6-2(a)+(b) ≡ reader-LC's mechanism limb: four hard heights collide with the coarse-pointer register — `h-10` dead at both ends, `h-9` ×3 defeats the 60px register / 44px floor.** PIN 7; consumer sites :100/:123/:147/:165; probe taps corroborate (158×36, 324×60). The h-9≡`size="sm"`-as-magic-number species **folds to wb-gradient-visualizer GV-19** (X-W6.b), the producer `size` prop unused repo-wide. Rendered magnitudes R-5. → **X-W6.j / shared X-W7**.
- **MC-4 — D·R6-2(e): the caption:value type ratio inverts ~34% between pointer classes because `.section-label` (→ `--type-caption`, viewport-fluid, `--ui-scale`-blind) and the trigger text (→ `--control-text`, scale-riding) obey different laws.** Token arithmetic reproduced to three decimals (14.384/12.179) with no browser. OWNERSHIP CAVEAT (reader-DU's, adopted): `.section-label` is a producer utility — an inherited token interaction; the cure is either a consumer move to the control register or a producer typography decision. → **X-W6.j** (consumer side) + **informational note on the standing glass-ui BH relay** (owner-edict compliant; no producer ask is mandated by this row).
- **MC-5 — D·R6-6.4: the demo re-mints the producer's elevated-contrast register at `:root` instead of expressing deltas over it.** foundation.css:728-733 (`--glass-level: 0.1`, `--glass-tint-strength: 0%`, no `--glass-tint-source`) vs installed a11y-fallback.css (`0.3`, `var(--glass-tint-strength-aa)`, `var(--glass-tint-ink)`) — both sides read verbatim by this seat; two shared tokens diverge, one omitted. Measured regression figures (3.277→3.258) R-2. → **NO-WAVE-OWNER** (foundation.css a11y blocks: W5's carve is layout-only, W8 says do-not-touch, W10 owns one survivor recipe).
- **MC-7 — D·R6-8: `MixConfigBar.vue:144` is an unmeasured F034-b site — the labelled Size-mismatch section enters/leaves in one frame with no `<Transition>` while the sibling result plate IS wrapped (MixPane.vue:111), and the prescribed cure family (`vj-celebrate` + its `--vj-celebrate-collapse/expanded` max-height pair) exists verbatim in animations.css.** Mechanism source-certain; the 207.7px displacement magnitude R-11. → **X-W6.j** (one additive line in the owned file).
- **MC-8 — C·R6-4 ≡ PreviewRamp.md R-3 — IDENTITY-FOLD: the four-arc hue quartet collapses to ≤2 distinct ramps everywhere and exactly 1 in the shipped default (oklab, useMixingState.ts:44), while the Hue Select ships enabled and fully described; the exact fixing predicate (`PICKER_CHANNELS[space].some(m => m.hue)`) already exists at picker-color.ts:52-70.** Confirmed by construction (my reads: {oklab, lab, rgb, xyz} carry no hue channel). The C axis's BLOCKER framing stays refuted per R-3 ruling B — with zero chips rendering, no one is currently lied to. → **fold to R-3: X-W6**.
- **MC-9 — C·R6-2 ≡ PreviewRamp.md R-2 — IDENTITY-FOLD: the oracle's only GREEN leg is green because of the blocker (`[data-stops]` count 0, a tautology), the add-slot role query is structurally unsatisfiable, no CI job runs playwright, AND the fixture adds the same color twice — `addColor` (useMixingState.ts:55-57) has no dedupe (contrast `addPalette`'s slug dedupe :65-68), so distinctness is untestable by construction.** → **fold to R-2: X-W1** (CC-031); reachability rides MC-1.
- **MC-10 — C·R6-3 ≡ GenerateControls §3 row 5 BUTTON-VARIANT-INERT ≡ PaletteSlugBar A-11 — IDENTITY-FOLD: `variant="primary-audacious"` (:163) is not a glass-7 prop; the verb renders `data-emphasis="secondary"` over `glass-wash` and the string lands in the DOM as junk.** 51-site census banked; lint wall G5 in V·MEGA-W-GEN-1. The "gates structurally cannot see this class" headline is KILLED (K-7). → **fold**.
- **MC-11 — L-R6-1: three config files justify their contents by a mechanism-C `file:`-symlink posture this checkout does not use** — lstat symlink false, lock resolves the registry tarball (this seat's probes); vite.config.ts:86-117 + tsconfig.demo.json prose now false-at-install. → **NO-WAVE-OWNER (W-HYGIENE config-parity identity**, ≡ wb-extract-workbench XW-29 class).
- **MC-12 — L-R6-2: `server.fs.allow` grants the repository's PARENT (vite.config.ts:139 → :285-288) on a font-walk premise the installed producer has retired** (fonts ship as data: URIs; both readers' greps agree; the config sites read by this seat). The "server currently running" half not re-taken (R-9). → **NO-WAVE-OWNER (W-HYGIENE)**.
- **MC-13 — L-R6-3 (defect limbs): `npm test` has no build hook where `typecheck` has `pretypecheck` — the O-14 vitest oracle binds to an untracked `dist/` nothing refreshes; and vitest.config.ts:22-27 justifies its exclude filter by a `test/dist/` that does not exist** (my `ls`: ENOENT). → **folds: pretest half ≡ wb-gradient-visualizer GV-15 → X-W1; dead `test/dist` machinery ≡ ErrorBoundary EB-33**. The src/-realias cure stays KILLED (K-9).
- **MC-14 — L-R6-4 (install limb): `@mkbabb/value.js@4.0.0` installs from the public registry into its own repo's node_modules (glass-ui's `^4.0.0` peer; no self-dependency declared — my package.json + lock reads).** → **fold ≡ PreviewRamp R-9 / GenerateControls G4 → X-W1's packed-surface owner**. The drift sub-claim is KILLED (K-8).

### MINOR

- **MC-6 — D·R6-7 RESCOPED (PIN 4, ruled against reader-DU's MAJOR): under reduced-transparency the verb and its card share one background token (`--surface-reduced-opaque` `!important`, source-certain) and the verb's border is zeroed by `.button` (cascade-order-certain) — but the rim insets and drop shadow survive (only `--glass-highlight` is zeroed), so the boundary degrades rather than vanishes.** Fleet-scope note survives in weakened form: every neutral glass Button on a `.glass-resting` card shares the fill under this media state; `surface-reduced-opaque` remains registry-unbanked (my sweep: 0). Revival to MAJOR on a live perceptibility witness (R-10). → **NO-WAVE-OWNER** (foundation.css a11y blocks, with MC-5).
- **MC-15 — L-L-8 RESCOPED (ruled against reader-LC's MAJOR): no module owns "dismiss portalled overlays on route change" — zero router guards (my grep), three portalled Selects mounted by this file alone.** The ownership hole is a code fact; the severity-carrying symptom (body `pointer-events: none` surviving gradient→mix) is R-6. → **NO-WAVE-OWNER** (shell-owned; no X wave holds the router).
- **MC-16 — D·R6-9: two left rails 13px apart — the caption sits on the field's border-box rail, the value on the content-box rail (`px-3` + 1px `.control-surface` border, arithmetic-exact from producer bytes).** → **X-W6.j**.
- **MC-17 — reader-DU miss ADMITTED: `gap-2` (:165) pins the ONE interval that overrides a `--ui-scale`-aware producer declaration** (`.button { gap: calc(0.375rem*var(--ui-scale)) }` — 6px fine / 9px coarse, pinned at 8px; utilities layer beats `@layer components`). Sharper than the six rounds' layout-wrapper gap rows. The `<Blend class="w-4 h-4">` companion **folds to PaginationBar M6** (`--ui-glyph` / `size-*` escape), not re-filed. → **X-W6.j**.
- **MC-18 — L-L2-4: `PickerSpace` is a bare `SpaceId` alias (picker-color.ts:37), so `colorSpace` admits 8 states this component cannot render (17-member alias vs 9 offered rows).** Latent — the sole writer is this component's own emit (reader-LC's downgrade adopted). → **X-W6.j rider**.
- **MC-19 — L-L3-4: `.section-subtitle` is dead CSS (0 .vue consumers — my grep) whose recipe comment AND DESIGN.md:58 both still claim the control bars consume it; MixConfigBar.vue:95-96 records the deletion that orphaned it.** → **X-W10** (owns demo/DESIGN.md) + the recipe dies in any utils.css pass.
- **MC-20 — L-R5-4 ≡ wb-mix-animationcanvas MX-19 / wb-gradient-pane C-2 — IDENTITY-FOLD: the Mix verb has two homes with divergent affordances — this file gates `:disabled="!canMix"` (:164) while the dock action (usePaneRouter.ts:221) is permanently enabled and silently swallowed at useMixingState.ts:80; `DockAction.disabled?` exists and is unplumbed here.** → **fold: X-W6.j / X-W7**.
- **MC-21 — C·R6-5 / L-L-6 (reach limb): all three Select handlers typed with reka-ui's `AcceptableValue` (:15, consumed :99/:122/:146 with unchecked `as`) — four union members wider than the vocabulary glass-ui throws on (`isSelectionValue` TypeError, producer bytes).** Declared devDep — not an undeclared-dependency defect (reader-LC's check adopted). CURES RULED at PIN 6: named import does not compile; no relay required; derive from `SelectEmits["update:modelValue"][0]`. → **fold ≡ AuroraPane AP-20 → X.W6.j** (+ wb-generate-pane GEN-22; K-10 cell corrected).
- **MC-22 — reader-LC miss ADMITTED: `weights` is dead public API on the mix domain module and carries its ONLY input-validation surface — the three guards (mix.ts:47-55) can never fire from the app** (the only caller passes no weights, and the default all-1s vector passes every guard — my read + repo grep), while the live failure surface (`parseColorIn`/`mixedOrThrow`) is unguarded. Exercised solely by test/mix-v4.test.ts (the banked test/→demo inversion). → **NO-WAVE-OWNER** (mix.ts).
- **MC-23 — reader-DU miss ADMITTED (corrected per PIN 3): the two halves the corpus never joined — colors mode has previews but cannot arm; palettes mode arms but is deliberately preview-blind (MixPane.vue:103 `[]`) — so in the ONLY shipped-reachable configuration the Space/Hue choices are made blind, in the mode with least user intuition.** The "honest restraint" is a documented choice (MixPane.vue:92-96) — but its justification ("no single ramp to preview") is exactly right per MC-2, and the DU parenthetical claiming the existing sampler could preview columns is STRUCK: it previews the wrong object. The open design question routes with MC-2. → **NO-WAVE-OWNER**.

### INFO

- **MC-24 — reader-DU miss ADMITTED: `font-medium` (:165) restates the producer's own `font-weight: 500` — a seventh dead declaration, provable from producer bytes; `font-display` on the same line is NOT redundant (`font: inherit` leaves family to the consumer).** Registry-checked distinct from the AL-M6/B-3/A-10 mechanisms. → **X-W6.j rider** (dies free).
- **MC-25 — reader-LC miss ADMITTED: one of the thirteen ramps is computed twice per invalidation — at `s.value === colorSpace` and `m.value === hueMethod` the two computeds (:57-74) issue byte-identical sampler calls.** By construction; distinct from banked R-16 (double `Map.get`) and R-8 (grid divergence), collapsing under R-16's one-array cure. → **X-W6.j rider**, cross-noted to R-16.
- **MC-26 — reader-LC miss ADMITTED: the color-chips barrel import (:23) drags the unused `PreviewStrip` SFC + scoped stylesheet into the mix route's graph** (barrel re-read; CSS is a module side effect). Chunk magnitude R-7. → **X-W6.j rider**.
- **MC-27 — reader-LC miss ADMITTED as IDENTITY-FOLD: MixPane.vue:2 imports `computed` and never uses it; no gate can see it** (eslint unused-vars off :71, `noUnusedLocals` absent — my greps). ≡ PalettesPane PP-25 / fr-App MG-θ class — dies free in whatever wave opens MixPane.vue, never minted as a row.
- **MC-28 — L-L-9, confirmed-INFO: the "costs nothing at rest" comment (:19-23) is TRUE** — lazy computeds whose only readers sit inside reka-unmounted `<SelectContent>` subtrees (my read). Becomes ~13×17 `mixColors` per invalidation the day operands exist. Recorded so the axis's restraint is not mistaken for a live defect.

## Killed-claims register

- **K-1 — C·R6-1's verb limb ("`canMix` false forever ⟹ the ONE verb permanently disabled") — UPHELD (both readers; PIN 2).** The :50-53 mode branch + the native palettes buttons (:246-268) + live `addPalette` arm the verb in-product. The registry already drew this line (wb-mix-animationcanvas MX-3: "only the palettes arm reaches this component").
- **K-2 — D·R6-4 ("the page's ONE verb has no hover and no press; the file withholds the affordance") — UPHELD (reader-DU; corroborated by this seat).** The producer computes `glass-capsule-hover` for every enabled default Button; the C axis's own dump lists it; the D seat's `removeAttribute('disabled')` never changed the Vue prop (`opacity: 0.5` receipts in its own probe JSON; `:disabled` ⟹ `pointer-events: none` — no hover could engage); the press gate rides the reactive disabled getter, not `data-press-armed`. Measurement artifact, not a design decision.
- **K-3 — D·R6-5's ENABLED rows (1.089:1 / 1.305:1 "the intended state fails") — UPHELD as killed-as-to-state (reader-DU).** Both "enabled" captures composited the disabled register at half alpha (the probe's own `opacity: 0.5`). The DISABLED figures (1.035/1.096) and the `border-width: 0` fact stand — the border fact now producer-explained (`border: 0` on neutral tone; `:not([data-tone="neutral"])` gets the border), which also makes the real-emphasis/tone cure structurally correct. Proper-state measurement R-2 — and PIN 2 makes it takeable without surgery.
- **K-4 — D·R6-3's headline ("two of nine color-space labels unrenderable at phone width") — UPHELD (reader-DU).** The measured strings are the PICKER's vocabulary (one misquoted: `Linear sRGB` vs the tree's `sRGB Linear`), not this component's nine ≤5-glyph labels; `Discard extras` renders in the full-width third field. Zero of nine offered labels overflow. Surviving limb (`Decreasing` 111.5 vs 110) → R-3.
- **K-5 — D·R6-1b ("the grid sacrifices the one control with no other route") — UPHELD (reader-DU).** `useMixingState` is instantiated locally in MixPane (:18-36); `colorSpace` is provided to nothing; BOTH grid halves are equally single-routed; r3 R3-3 was a duplication finding, misattributed.
- **K-6 — C·R6-5's cure (`import type { SelectionValue } from "@mkbabb/glass-ui"`) — UPHELD (both readers; PIN 6).** Unexported at the pin — this seat's grep of every export surface. The C axis prescribed an import its sibling L axis had already proved unavailable — the corpus's one internal contradiction.
- **K-7 — C·R6-3's headline ("the class of defect this repo's gates structurally cannot see") — UPHELD (reader-LC).** The gate exists and is off: `strictTemplates` absent from every config (my grep 0); the banked Markdown.md instrument ran the counterfactual (271 errors, 263 × TS2353 — precisely this class, at scale). A one-line omission with a known blast radius, not an impossibility.
- **K-8 — L-R6-4's drift sub-claim ("the two copies have already drifted … two artifacts, one immutable name@version" as contract divergence) — UPHELD (reader-LC; counts re-taken by this seat).** 53 exported symbols BOTH; the whole delta is private `_2` fork declarations (58 vs 0). What survives is the narrower non-reproducibility hygiene fact, already banked (R-9).
- **K-9 — L-R6-3's preferred cure ("point the test program at `src/`") — UPHELD (reader-LC).** It would abrogate the T.W1 demo-dogfood keystone (tsconfig.demo.json:5-8 records `@src/*` deliberately RETIRED) to fix a freshness problem `"pretest": "npm run build"` closes. Cure-adjudicated-separately law.
- **K-10 — reader-DU's missed-4 ("the discarded default slot + dead centring CSS are a limb the banked row does not carry") — KILLED BY THIS SEAT.** PreviewRamp.md **R-11** carries exactly this: "the `<Plus>` glyph … silently discarded, the `.add-slot-ghost` scoped rule centers a child that no longer exists, the `disabled:*` classes can never match". The facts are true (my dist probe: `renderSlot|$slots` = 0) and already banked; the miss dies as a re-booking under the anti-rename law. Method note: DU's registry sweep hit GenerateControls SWATCH-INERT and stopped one file short.
- **K-11 — reader-DU's D·R6-7 UPGRADE ("no boundary at all — provable from source, no live cell needed") — PARTIAL KILL BY THIS SEAT (PIN 4).** Background identity and border-0: source-certain, adopted. Boundary-erasure totality: source-FALSE — `.glass-capsule`/`.glass-wash` box-shadow (rim insets + drop shadow) survives both reduced-transparency blocks, which zero only `--glass-highlight`. Row rescoped to MC-6.

## Superlatives (provenance preserved)

Items 1-5 carry this seat's own re-verification; the rest are adopted with their receipts.

1. **The color-chips seam is the best-drawn boundary in this cone** — `sample.ts` states an honest-absence law and enforces it in one line (:58); `stampStops` (:89-91) is ONE serialization shared by paint and oracle; `PreviewRamp` takes pre-sampled stops and is forbidden to compute color. (Both readers; boundary re-read whole by this seat. MC-2 is a finding *about what the sampler samples*, not about the seam's cleanliness — a defect beside a superlative, both true.)
2. **`useMixingState.ts` earns its docblock** — zero timers, one forward edge (`settleMix` :104-106, phase-guarded), re-entry guard (:83), slug-deduped `addPalette`, and the mode-branched `canMix` that refuted three of the corpus's derived claims. The cleanest module in the mix tree, again.
3. **A comment that is a checkable claim, and checks out** — ":19-23 costs nothing at rest" verified structurally by this seat and by both readers independently (MC-28). Two hostile rounds and an adjudicator checked a performance comment and it was true.
4. **The restraint table** (color-chips/index.ts:16-25) — an explicit, auditable negative-space specification naming every dropdown that gets NO chip, with a reason each, closing "menus preview, triggers name". The reason the strategy Select's chip-less rows are a choice, not an omission — re-read verbatim this session.
5. **The producer refuses rather than degrades** — `isSelectionValue` throw ("non-scalar", verified in the shipped chunk) over a published `string | number` contract. It is what keeps MC-21 a type-hygiene MINOR instead of a runtime hazard.
6. **MixConfigBar.vue has no internal defects** — no state, no timers, no lifecycle, no DOM access, no `any`; correct `import type` on all four type-only edges under `verbatimModuleSyntax`. Eighteen rounds of hostile reading produced only BOUNDARY defects: producer props, barrels, manifests, lockfiles, sibling modules, gate configs (reader-LC's negative proof, upheld by this record's roster: every row's cure surface is at or beyond the file's edge — except the seven dead declarations, which are boundary defects *written locally*).
7. **The F16 rehoming discipline** (color-space-meta.ts:1-9) — the vocabulary moved to a neutral home with the cohesion reasoning recorded verbatim, the consumer note matching (:16-18). Adopted from reader-LC with this seat's read.
8. **The axes' evidence hygiene convicted and acquitted from the same files** — the D seat's probe JSON carried the `opacity: 0.5` that killed two of its own MAJORs and the tap sizes that confirmed a third; the L axis preserved its own near-misses (§7.1/§7.2) as evidence, which is what let the readers trace the corpus's one internal contradiction. Where findings died here, severity framing or scope died — never a measurement.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **R-1** — D·R6-1: the safari-app cell's pane pinned at `translateX(110%) rotate(2deg)` — two candidate mechanisms with two different cures (CSS settle vs PaneSlot.vue:80-92's rAF-deferred swap never committing under starvation); only the real-browser cell can decide.
- **R-2** — the enabled-state contrast battery (D·R6-5 plate↔card, D·R6-6 glyph↔plate, the elevated-contrast 3.277→3.258 regression): take with two saved palettes (PIN 2 — no surgery needed), in the genuinely enabled register.
- **R-3** — `Decreasing` at 111.5px vs 110 available — inside font-fallback noise ("Plus Jakarta Sans Fallback" in the probe's own font string); canvas `measureText`, not the rendered span.
- **R-4** — D·R6-10's measured 0.1s/0.15s transitions under reduce — mechanism already ruled by motion-quarantine.md §1 Guard 2 (producer a11y-overrides beats the demo's `*` neutraliser); carried as INFO-consistent, NOT as a defect row.
- **R-5** — MC-3/MC-4 rendered magnitudes on a coarse device (36/60px, 2.25px surviving padding, ratio 0.877→0.580).
- **R-6** — MC-15's live symptom: body `pointer-events: none; overflow: hidden` surviving a gradient→mix hash route change with a Select open. Revives MAJOR.
- **R-7** — MC-26's chunk bytes (production build + analysis).
- **R-8** — the silent-null vs uncaught-throw divergence firing rate on persisted palette css (banked from the other end: MX-39 / PS-17 — folded, not re-filed).
- **R-9** — L-R6-2's session-dated half (a live :9000 serving the parent tree to the LAN) — config confirmed, occupancy not re-verifiable.
- **R-10** — MC-6's surviving-boundary perceptibility under reduced-transparency (rim insets + drop shadow against the shared opaque fill; one computed-style + contrast read). Revives MAJOR.
- **R-11** — MC-7's one-frame displacement magnitude (the axis's 207.7px).

## Motion-law compliance

No `motion-quarantine.md` FALSE row is carried. This component's three axis files are EXISTS-ORIGINAL (HYDRATION-LEDGER.md:242-245) — none in the quarantine's REPORT-AUTHORED subject set; a grep of the quarantine for this component returns nothing. The one motion-adjacent claim in the corpus (D·R6-10) is carried only as concurrence with the quarantine's own §1 Guard-2 architecture ruling (M-xx FALSE rows untouched), per both readers.

## DISSENT — preserved, not ruled away

1. **Reader-DU holds D·R6-7 at MAJOR, source-decided**; this seat rescoped to MINOR on the surviving shadow/rim bytes (PIN 4, K-11). R-10 is the honest boundary: if the rim at `--glass-specular-rest-hairline`-class opacities measures imperceptible over the shared fill, DU's severity revives whole.
2. **Reader-LC holds L-L-8 at MAJOR**; this seat demoted pending the live witness (R-6). LC's instinct — an ownership hole is a defect independent of its first symptom — is recorded; the severity simply cannot ride an unmeasured lock.
3. **The reader asymmetry event**: reader-DU never engaged L-R4-1 (this record's strongest unbanked row, MC-2) and its own missed-2 parenthetical contradicts it — the same single-quantity pattern the animationcanvas adjudication logged as a repeated fleet behavior. Recorded again: two hostile readers, one mathematical object each, neither computing the other's.
4. **The banked-registry correction (PIN 6)** is evidence about the fleet's citation hygiene: wb-generate-pane K-10's "my reads" cell asserts an export that does not exist at the pin. The verdict class it supported survives by a different road (derivability); the cell did not. Next seat: re-derive export surfaces, never inherit them.
5. **Reader-DU's missed-4 kill (K-10)** is a registry-sweep depth lesson, not a facts lesson — every byte of the claim is true and every byte was already banked. The L-18 REGISTRY-FIRST sweep must cover the component's own banked NEIGHBORS (PreviewRamp is this component's chip), not only same-named rows.

---

**ADJUDICATED RED — the corpus's direction confirmed, its two strongest severity claims corrected in opposite directions, one cure family cut down, one banked evidence cell corrected, and one reader miss killed as already-banked.** 28 rows survive (1 BLOCKER banked-fold · 12 MAJOR · 10 MINOR · 5 INFO); 11 killed claims with written refutations; 7 reader misses admitted. Terminal dispositions: **~11 rows/riders to X-W6.j** (which owns MixConfigBar.vue outright, shared with X-W7) · **2 to X-W1** (oracle + pretest identities) · **1 to X-W10** (DESIGN.md's false line) · **6 identity-folds into banked rows** (R-1 · R-2 · R-3 · BUTTON-VARIANT-INERT · GV-19/GV-15/EB-33 · R-9/G4 · MX-19 · AP-20 · PP-25 · M6, original ids preserved) · **7 rows/limbs NO-WAVE-OWNER** — the preview-vs-verb object divergence (MC-2), the blind-reachable-mode join (MC-23), the dead `weights` validation surface (MC-22), both foundation.css a11y-block rows (MC-5, MC-6), the overlay-dismissal ownership hole (MC-15), and the W-HYGIENE config cluster (MC-11/MC-12). That compound fact — the one unbanked mathematical-truth defect and the whole a11y-media surface live in files no tranche-X wave owns, while every file around them is owned twice over — is this apotheosis's headline for the next formation boundary.

— end of apotheosis. The three challenge records, five prior rounds per axis, and both re-reader returns remain immutable beside this file per E-1/E-3. This file is new at this path; nothing dated was rewritten.

---

## ADDENDUM — independent concurrence sweep (E-3: appended; nothing above rewritten)

**Seat**: fresh-Fable adjudicator (M-23/L-18), SECOND seating · served model: `claude-fable-5` (receipt observed in this seat's own system prompt, not inherited). **Date**: 2026-08-24 · **HEAD**: `053f7d59` — the very commit that banked the record above ("SS-7 batch 11 PARTIAL 2/4 banked at the monthly wall").

**Occasion.** A duplicate M-23 spawn handed this seat the byte-identical pair of re-reader returns already adjudicated above (reader-DU 12 confirmed / 6 killed / 4 missed · reader-LC 17/5/4 — row-for-row the same claims, proofs, and kill texts). Under the never-rewrite law, the correct move was not to re-mint the apotheosis but to ATTACK the banked one with this seat's own instruments and record the outcome. The record survived.

**This seat's own instruments (static + node against installed dist; zero browser tooling; all run 2026-08-24 at `053f7d59`).** Subject-byte identity: `git diff 08232cd3 HEAD` over demo/workbenches/mix · demo/color-session · demo/palettes · demo/styles · vite.config.ts · package.json · package-lock.json · vitest.config.ts · tsconfig.demo.json · e2e = **EMPTY** — every line anchor above re-resolves unchanged. Re-derived independently, not inherited: `watercolor-dot.js` `inheritAttrs: !1` / `renderSlot|$slots` = 0 / hardcoded `aria-hidden` + `pointerEvents:"none"` (PIN 1, MC-1, K-10's premise); useMixingState.ts:50-53 mode branch + MixSourceSelector.vue:246-268 native palette buttons + slug-deduped `addPalette` (PIN 2, K-1); sample.ts:52-85 piecewise `segments = N−1` chain vs mix.ts:39-66 running `wᵢ/total` centroid fold, plus mix.ts:25 `weights` and its three app-unreachable guards (PIN 3, MC-2, MC-22); foundation.css:728-790 both a11y media blocks verbatim vs installed `a11y-fallback.css` (MC-5; and PIN 4's token halves — the producer block zeroes ONLY `--glass-highlight`, the demo block touches only backdrop-filter/background — re-taken; the glass.css-before-button-styles import-order read and `--glass-shadow-wash` byte are ADOPTED from the first seating, the one cell this seating did not re-take); probes/r6-out4.json re-opened — `light.info.opacity = dark.info.opacity = 0.5` on both "verb-enabled" captures, coarse taps 158×36 / 324×60, `fit.widths` carrying `Linear sRGB`/`Display P3`/`Discard extras` (PIN 5; K-2/K-3/K-4) — against color-space-meta.ts:26-41's nine ≤5-glyph labels and picker-color.ts:83-84's `sRGB Linear` spelling; button dist bytes — no `variant` prop (0 hits), `glass-capsule-hover` reactively gated, `.button:disabled { opacity: var(--opacity-disabled); pointer-events: none }`, base `font-weight: 500`, `gap: calc(0.375rem * var(--ui-scale))`, `border: 0` neutral vs `:not([data-tone="neutral"])` border, `svg:not([class*="size-"])` → `--ui-glyph` (MC-3, MC-10, MC-17, MC-24, the M6 fold); `SelectionValue` = 0 hits in `dist/index.d.ts` AND the select barrel, declared only in `_shared/selection.d.ts`, while `SelectProps.modelValue` / `SelectItemProps.value` / `SelectEmits["update:modelValue"]` are all annotated with it, and the "non-scalar value." throw string ships in `select-BcBAyLXA.js` (PIN 6, K-6, MC-21 — **wb-generate-pane K-10's corrected cell re-confirmed false at the pin by a third independent probe**); `select-BcBAyLXA.js` `min-block-size` = **0** with both `h-(--control-h-sm|md)` classes present, `sizing.css` `--control-h-md: max(calc(2.5rem * var(--ui-scale)), var(--control-floor))`, exactly one `@media (pointer: coarse)` → `1.5`/`2.75rem`, `--type-caption: clamp(0.75rem, 0.71rem + 0.21vw, 1rem)` `--ui-scale`-blind (PIN 7, MC-3, MC-4 — now fully re-derived including the trigger cell the first seating took on three concurring reads); o14-preview-truth.spec.ts:334-355 tautology-leg + unsatisfiable role query + double-click-same-color fixture (MC-9); config limbs — `pretest` absent / `pretypecheck` present, `test/dist` ENOENT vs vitest.config.ts:22-27's comment, `siblingFsAllowTransient` consumed at `server.fs.allow`, fonts.css 0 `../fonts` + 4 `data:font/woff2`, glass-ui REAL-DIR + both registry tarballs in the lock, no self-dependency, reka-ui `^2.9` devDep (MC-11..MC-14, MC-21); `strictTemplates|vueCompilerOptions` = 0 (K-7), router/shell guards = 0 (MC-15), `section-subtitle` .vue consumers = 0 with utils.css + DESIGN.md:58 still claiming it (MC-19), usePaneRouter.ts:221 mix action with no `disabled` (MC-20), MixPane.vue:2 dead `computed` under eslint unused-vars-off + zero `noUnusedLocals` (MC-27), PICKER_CHANNELS hue() on exactly {hsl, hsv, hwb, lch, oklch} with oklab the default (MC-8); registry cross-rows re-read at their cited lines — PreviewRamp R-1/R-2/R-3/R-8/R-9/R-11/R-16, GenerateControls §3 rows 1+5, PaletteSlugBar A-11, wb-generate-pane K-10, PalettesPane PP-25, PaginationBar M6, wb-mix-animationcanvas MX-19/MX-39; motion-quarantine.md grep for this component = 0 (motion-law compliance re-affirmed, no FALSE row carried).

**Result: CONCURRENCE 28/28 rows · 11/11 kills · all terminal dispositions · both DISSENT severity holds left standing.** Zero new rows minted; zero severities moved. One registry-state delta recorded for future sweeps: `surface-reduced-opaque` in registry/adjudicated/ now greps **1** — this file itself; MC-6's "registry-unbanked (my sweep: 0)" cell was true when written and remains true of every record OTHER than this one. One citation note: K-1's `MX-3` pointer and reader-LC's `wb-mix-animationcanvas.md:52` anchor no longer land on the palettes-arm sentence at today's line numbering — the underlying mode-branch fact is source-verified directly above and does not ride the anchor. The first seating's closing instruction ("re-derive export surfaces, never inherit them") was followed to the letter and is hereby proven cheap: every load-bearing cell of this record re-derived in one session from the tree and the installed pin.

— end of addendum. The apotheosis above is the adjudication of record; this addendum is its independent re-verification, not its replacement.
