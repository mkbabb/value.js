claude-fable-5

# kf-TypingDots — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js`, re-measured today by this seat: HEAD `8281638c`, `origin/master` `81a56990`, repo **1 ahead / 41 behind** origin/master, working tree still churning (workflows, bench, dock all dirty). **The decisive substrate fact, this seat's own diff**: `demo/components/instrument/shell/TypingDots.vue` (125 L) in the working tree is **byte-identical to `origin/master`** (`git diff origin/master -- TypingDots.vue` → empty) and differs from HEAD by exactly the two-hunk `steppedEase`-callable → `"steps(4, jump-none)"`-string flip (plus template whitespace). See ruling 1 — the registry's substrate hazard for this file runs the OTHER way from what the bank recorded. **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two hostile re-reader returns (reader-1, design-and-usage · reader-2, library-and-code) over the component's three challenge files (`audit/kf-components/TypingDots/challenge-D-design.md` 264 · `challenge-L-library.md` 312 · `challenge-C-consumption.md` 275 — all three read whole by this seat). The two returns were attacked before either was adopted.
**Method (incredulity)**: every verdict-level disagreement between the readers (six: the M1 booking shape, KF-EST-5 severity, KF-EST-18 severity, the `.delays` bank question, L-8/C-2 severity, C-4/L-10 severity) was re-derived against the bytes with this seat's own instruments; both readers' shared kills were spot-third-derived rather than trusted; three corpus rows BOTH readers dropped (D-10, L·D-16, L·D-15's surviving sharpening) were recovered and adjudicated here. Zero browser probes (probe parsimony; every dynamic claim routes to SS-13). Dispositions are **ADJUDICATED, not VERIFIED**; the X·KF specs are still forming — every routing below is agglomeration input (M-25), not an act.
**This seat's own instruments (all read-only + git)**: full read of TypingDots.vue (whole, 125, frontier bytes); `git diff origin/master|HEAD -- TypingDots.vue`; `grep -rn snapToReducedMotion src/` → exactly 2 hits (definition play-lifecycle.ts:342, sole call :219 inside `playFrame`); play-lifecycle.ts:195-225 (`playFrame` = "One frame of the standalone rAF play path"), :285-300 (`playViaWAAPI`/`cancelWAAPI`), :330-395 (the `snapToReducedMotion` docblock's false WAAPI clause verbatim; `play()`'s up-front `withReducedMotion` gate → `playReducedMotion` | `playViaWAAPI` | `playRAF`); frontier split-module check (`origin/master:…/play-lifecycle/frame.ts` — sleep :38, `startTime === undefined` :82, `playFrame` :121, sole `snapToReducedMotion` call :137; `strategies.ts:72-73` the same false docblock); delegation.ts:40-100 (`shadowTick` calls `advanceTo`+`reconcile`, never `withReducedMotion`; `return !animation.done`; the AbortError-swallowing catch + `finally` teardown); onStart/onEnd/advanceTo (the per-iteration `delay` re-sleep, no first-iteration guard, both layouts); stagger.ts:15-23 docblock example + :151-152 `defaultTotal` + :173 `fn.delays(total = defaultTotal)`; group/types.ts:27-29 (`AnimationGroupInput` — **no `options` member**); group/waapi.ts:31-33 (`!group.singleTarget` refusal) + group/group.ts:159; package.json at THREE baselines (HEAD: glass-ui `6.0.0` optionalDep, lock 3 · origin/master: `7.0.0` EXACT devDep, lock 3 · worktree: ABSENT, lock 0 · installed: 7.0.0) and proof scripts at both (origin/master carries `proof:structure` + a 3-stage `check`; worktree only `proof:publish`/`proof:owner-golden`; **no `proof:typing-dots` anywhere**); gates census at both baselines (origin/master `ls-tree` → **9** files incl. `structure/index.mjs` + `surface/agent-surface.mjs`; worktree `find` → 7); `vue-tsc` = 0 at both; origin/master ci.yml:40-56 (merge job = `check:lib`/`build:lib`/`test:lib`/`proof:publish`; `demo-correctness` nightly/dispatch-gated, its own comment saying "a scheduled red … does not block library merges"); installed Vue runtime (`runtime-core.esm-bundler.js:3189-3197` — the `v-for` number-source guard is `__DEV__`-gated, prod `else` = `new Array(source)`; `runtime-core.cjs.js:205-213` — `callWithAsyncErrorHandling` `.catch`es a returned promise); node executions: `new Array(2.5|-1|NaN)` → RangeError ×3; WCAG contrast recompute (white-on-black @α=0.2 → **1.662**, black-on-white → **1.606**); `grep errorHandler|onErrorCaptured demo/app/main.ts EditorShell.vue` → 0; `grep -rn TypingDots test/` → 0; glass-ui tokens.d.ts:29-47 (`motionStagger {tight:40, default:80, relaxed:120}` + the JS-cascade docblock); scripts/ `typing-dot` strings (published-surface.mjs:175-176 past-tense, demo-driver.mjs:77, live-session.mjs:1143/1162/1171 the surviving probe); App.vue:138-143 (the barrel-membership comment, K10's decider, verbatim); **registry cross-reads (the identity-guard bank)**: kf-EditorStartScreen.md whole (KF-EST-1/-3/-5/-6/-11/-17/-18/-20/-21/-22, rulings 4/5/7/8b, superlatives 1/7/8, probes P-8/P-9), kf-App.md (KF-APP-4 :44, KF-APP-24 :74, the :141 PRM superlative, rulings :20/:118), kf-AnimatedText.md (KF-AT-12 :52, KF-AT-14 :54, KF-AT-15 :55, KF-AT-16 :56, KF-AT-17 :57, KF-AT-28 :73), kf-CSSCodeEditor.md:47 (KF-CE-13), CENSUS-2026-08-03.md:32 (SCH-1 verbatim).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**24 adjudicated roster rows — 15 folds to banked identities (two carrying MAJOR weight at their banks: KF-EST-1, KF-EST-5) · 9 fresh bookings (1 MAJOR · 5 MINOR · 3 INFO).** **10 killed-claims register entries** — all ten sustained under third derivation, two of them corrections OF THE BANK (K2 fixes KF-EST-17's phrasing; ruling 1 inverts KF-EST-21's and EST-ruling-7's scoping for this file). **5 admitted misses across the two readers, all adjudicated real** — one a new library MAJOR both readers found independently and one axis had inverted into a superlative. **9 superlatives sustained** (one re-scoped). **3 corpus rows both readers dropped, recovered here** (D-10 folded, L·D-16 and L·D-15's sharpening attached as riders — nothing lost).

MAJOR-weight survivor of record: **KF-TD-1** — on the lane this component actually ships (WAAPI delegation, proven by the frontier's own string easing), a mid-session flip to `prefers-reduced-motion: reduce` is **never observed**: `snapToReducedMotion` has exactly one caller and it lives on the rAF lane, while the library docblock asserts the opposite in as many words. The banked release-direction asymmetry (KF-EST-11) now has its engagement-direction twin, and it is library-side.

## The incredulity seat's own work — ruled disagreements and corrections

1. **The substrate inversion (reader-2's frontier probe) — CONFIRMED by this seat's own diff, and it corrects the bank.** `TypingDots.vue` worktree ≡ `origin/master` byte-for-byte; HEAD `8281638c` is the outlier (the repo is 1 ahead / 41 behind). Three banked scopings flip for this file: (a) **KF-EST-21** (kf-EditorStartScreen.md:88) books the orphaned `steppedEase` comment as something "the dirty tree deleted" — the frontier's own bytes carry it (:25-26 names a symbol imported nowhere; the only `steppedEase` import exists at stale HEAD), so the comment-truth cure is REQUIRED, not merely re-anchorable. (b) **EST ruling 7 / K5** reads the string-easing flip as worktree divergence pending re-anchor; the frontier ships the string, so the **WAAPI lane is the shipping truth** — C·C-6/D·D-3's shadow-tick reading and KF-TD-1 are live, while the rAF-lane readings (where L·D-4's drift is visible) describe only the stale HEAD fork. (c) **KF-AT-28**'s re-anchor obligation is DISCHARGED for this file specifically — every line anchor in all three challenge docs is frontier-durable by identity. Booked as **KF-TD-2**; the general provenance-discipline kill (all three axes declared HEAD READ-ONLY while reading other bytes) is sustained at K6 — the failure is naming the wrong baseline, not citing wrong bytes.
2. **M1's booking shape — RULED a NEW library row (reader-2's form), not reader-1's fold-as-rider.** Both readers converged on the mechanism; this seat re-derived it whole: the sole `snapToReducedMotion` caller is `playFrame` (:219), `playFrame` is the rAF lane's frame body, `playWAAPI`'s driver is `shadowTick` (delegation.ts:53-64) which never consults `withReducedMotion`, and the docblock at play-lifecycle.ts:337-341 (frontier: strategies.ts:66-75) claims "a live flip on a WAAPI animation cancels the compositor handles before settling" — prose for a path with no caller (`cancelWAAPI` fires only inside the uncalled snap). Banked KF-EST-11 is a DEMO row about the RELEASE direction (reduce→no-reduce, frozen at 0.2); this is a LIBRARY row about the ENGAGEMENT direction (no-reduce→reduce, never snaps) plus a false docblock — different mechanism, different repo surface, different cure owner. Booked **KF-TD-1 MAJOR → KF.W5**, rider-linked to KF-EST-11 so the two directions land as one PRM-unification story; probe rides banked P-9's pass. Consequences ruled with it: C's S★-4 second bullet stays killed (K5); **L's S-3 superlative survives SCOPED** — its up-front-gate trace (play() → withReducedMotion → fillForwards → the value.js clamp at `easing.js:239` → exactly 0.2) is exact and re-verified, but "the PRM delegation is verified end-to-end / closes lane-frontend §6.5" is overbroad by precisely this row: it verified the mount-time arm only.
3. **KF-EST-5 severity (three perpetual rAF drivers) — the BANK's MAJOR governs; reader-2's MINOR demotion OVERRULED.** Reader-2's stated ground — "D-3's MAJOR rests on a refuted remedy" — violates the cure-separation law (ColorSpaceSelector addendum 2: a defect's severity inherits nothing from its cure's fate) and reader-2's own bank-governs principle, applied one row earlier at D-1. The bank booked the row MAJOR **with the cure already ruled severed** (kf-EditorStartScreen ruling 5), so the demotion re-litigates a settled ruling on inadmissible ground. Count re-verified this seat (`return !animation.done` / the `else` branch forever); magnitude stays UNPROVEN → SS-13. FOLD at the bank's MAJOR.
4. **KF-EST-18 severity (`count` half-reactive) — the bank's MINOR governs; reader-2's MAJOR recorded as dissent.** Latent (no consumer passes `count` — banked KF-EST-3), which is the bank's own calibration. What this corpus genuinely ADDS is attached as riders at the bank's id: the leak-on-decrement mechanism (a live WAAPI effect + shadow loop against a detached node — stop() is unmount-only), the `delays`-tail collapse (`delays[i] ?? 0` silently zeroes new dots), L·D-16's silent partial-ref posture (no `els.length === count` reconciliation — a row BOTH readers dropped, recovered here), and reader-2's missed snapshot refinement (`els` captured at :72 BEFORE the await, consumed at :78 after it — the one shape that binds animations to a pre-change node array; the banked superlative 1 blesses the flag and is silent on the snapshot; both readings are true at once).
5. **The `.delays(count)` bank dispute — reader-1 right, reader-2 corrected.** kf-EditorStartScreen.md:87 (KF-EST-20, the seam/no-op INFO cluster) books "`stagger(count,…).delays(count)` twice-redundant" by name; reader-2's "Unbanked; INFO" missed it. FOLD, never re-book. (The API facts re-verified this seat: `defaultTotal` :151-152, `delays(total = defaultTotal)` :173.)
6. **The C-4 kill (unhandled rejection) — SUSTAINED at the installed runtime, and it corrects the bank's phrasing.** `callWithAsyncErrorHandling` (runtime-core.cjs.js:205-213) `.catch`es the promise every lifecycle hook returns — Vue holds it, a rejected `async onMounted` routes to `handleError`, never to `unhandledrejection`. Banked **KF-EST-17** inherited C's wrong words ("an unhandled rejection on the LCP node"); its two limbs stand (the `??=`-memoized rejected promise; the floating `anim.play()` at :98) with the phrasing corrected — the visible surface of a failed engine chunk is a **handled console error plus static 0.2 dots**, not an unhandled rejection. Severity stays the bank's MINOR; reader-2's INFO preference noted with its genuinely sharper reachability work adopted as a rider (delegation.ts's catch swallows `stop()`'s AbortError; both `play()` throw sites unreachable here).
7. **Two severity contests ruled small.** (a) L·D-8/C-2 (two PRM authorities in one `<h1>`): reader-2's INFO re-scope adopted — each substrate uses the mechanism correct FOR that substrate; the identity is the substrate split, banked at KF-AT-16/census §6.5 — with the KF-TD-1 direction rider (for live engagement the CSS half is the ONLY half that responds at all; the corpus's framing of the CSS mirror as the inferior twin is backwards) and C-2's genuinely distinct arm attached: the props surface has **no seam** to phase-continue the sibling's ripple (no `initialDelayMs`/`each`/`cycleMs`/`from` — the four knobs :46/:50/:61 hardcode); perceptual half → SS-13. (b) D-7/L·D-7 (`display: inline-block` dead on a flex item): INFO, not reader-2's MINOR — a zero-effect declaration with a false-signal cost only, the same calibration the bank's KF-EST-20 cluster applies to its inert declarations.
8. **Rows both readers dropped, recovered (nothing lost).** D·D-10 (named "typing indicator", realized as hero ellipsis; `:count="5"` renders `.....`) — FOLDED, split across banked KF-EST-3 (the dead-props arm: count/glyph never passed) and KF-EST-7 (the idiom arm: the typing-indicator reading and its AT silence are already the bank's copy-motion content). L·D-16 → rider on KF-EST-18 (ruling 4). L·D-15's surviving sharpening — an F-1-class breakage is invisible on every push/PR and surfaces only on a nightly whose red "does not block library merges" (ci.yml's own comment, re-read at origin/master this seat) — attached as a rider on the KF-APP-4 fold; the phantom-dependency headline it decorated stays dead (K1).

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0..W10** per CENSUS-2026-08-03 §(a) (W0 substrate settle · W4 gate/hygiene law · W5 D/L/C library tri-fold · W6 glass suffusion/demo · W8 structure & colocation · W9 Safari visual audit = the SS-13 surface · W10 fold discharge); rows the forming specs do not yet own are **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring. Line anchors for this file are **frontier-durable** (ruling 1); library anchors are the 2026-08-25 working tree with frontier twins named where the module split moved them.

### Folds to banked identities (fold-by-reference, anti-rename — never re-booked)

| corpus ids | banked identity | what this corpus adds to the bank | adjudicated severity | route |
|---|---|---|---|---|
| D:D-1 (+C:S★-4 residue) | **KF-EST-11** (PRM rest at 0.2 — a GATED choice the file documents; EST ruling 8b) | Nothing mechanical; D's MAJOR recorded as dissent against the bank's MINOR. The kf-App:141 "rests at the READABLE frame" superlative tension is already ruled — the later, more specific EST row governs. KF-TD-1 is this bank's engagement-direction twin (ruling 2) | MINOR (bank) | KF.W9 PRM unification (banked) |
| D:D-4 / L:D-1 / C:C-1 | **KF-EST-18** (`count` reactive in template, mount-once in engine wiring) + KF-EST-3 (the dead-prop half) | Riders (ruling 4): the leak-on-decrement mechanism; the `delays[i] ?? 0` tail-collapse; L:D-16's silent partial-ref posture; the pre-await `els` snapshot refinement (reader-2's miss) | MINOR (bank; reader-2's MAJOR = dissent) | KF.W6 — ONE contract motion with KF-TD-3 (delete, validate, or react — never two of the three half-done) |
| D:D-3 / L:D-5 / C:C-6 | **KF-EST-5** (N perpetual per-instance rAF drivers, substrate-split; cure SEVERED at the bank) | The frontier confirmation: the WAAPI+shadow-tick arm is the SHIPPING arm (ruling 1); count re-verified at delegation.ts:50/:54/:64 | MAJOR (bank; reader-2's demotion overruled, ruling 3) | KF.W5 library arm (incl. the stagger docblock that does not typecheck — already IN the bank row) · KF.W6 component arm · magnitude → KF.W9 |
| L:D-3 / C:C-5 | **KF-EST-1** (eight dead `proof:*` citations incl. TypingDots :45/:51/:59) | C's surviving-oracle rider: live-session.mjs:1130-1177 guards ≈clause (a) + the PRM snap only — (b) and (d) are unguarded even by the successor; gates census corrected at K8 (name the baseline) | MAJOR (bank) | KF.W4 provenance-gate law |
| L:D-3 zero-test arm | **KF-EST-6** (rescoped at the bank: the engine seam exercised by no gate anywhere) | `grep -rn TypingDots test/` → 0, re-run this seat | MINOR (bank rescope) | KF.W4 |
| L:D-2 | **KF-APP-4** ≡ KF-AT-12 (no SFC typecheck; already folded once at the EST bank) | The merge-path scoping verified at origin/master this seat (ci.yml merge job = 4 lib steps; demo-correctness nightly-gated, red non-blocking by its own comment); L:D-15's nightly-only-visibility sharpening attached (ruling 8) | bank's (the enabling-condition row) | KF.W4 |
| D:D-6 | **KF-EST-21** (the orphaned `steppedEase` comment) | **Scoping INVERTED (ruling 1)**: the frontier ships the orphan — comment-truth cure REQUIRED, not a worktree artifact | MINOR | KF.W6 |
| D:D-9 / L:D-11 / C:C-8 | **KF-EST-20** (the seam/no-op cluster names this row verbatim) | Reader-2's "unbanked" corrected (ruling 5) | INFO (bank) | KF.W6 hygiene pass |
| L:D-12 | **KF-EST-22** (v-for ref-order + unverified `HTMLElement[]` generic) | Nothing — the bank's paired form is stronger | INFO (bank) | KF.W6 |
| L:D-10 / C:C-4's surviving arm | **KF-EST-17** (memoized rejection + floating `anim.play()`) | K2's phrasing correction OF THE BANK (ruling 6) + reader-2's reachability riders (AbortError swallowed at delegation.ts; both throw sites unreachable) | MINOR (bank) | component arm KF.W6 · memoized-rejection design → KF.W5 |
| D:D-8 | **KF-APP-24** ≡ KF-CE-13 (forced-colors = 0 demo-wide) | Fourth re-file of one identity; the alpha-dimmed-ink exposure note (the one construction forced-colors cannot repair) rides the bank | INFO here | NO-WAVE-OWNER (bank) + KF.W9 horizon |
| C:C-3 | **KF-AT-16** (motion-token suffusion; the bank already scopes "TypingDots already routed engine-side") | The rider: `./tokens` JS subpath has ZERO demo consumers while TypingDots is the demo's only JS-driven per-element stagger — the exact published use case (tokens.d.ts:30-42 re-read this seat); `STEP_MS=160` = 2× canon default, on no rung | INFO | KF.W6 suffusion |
| L:D-8 / C:C-2 | **KF-AT-16** + census §6.5 via KF-EST-11 | Ruling 7a: INFO re-scope; the KF-TD-1 direction correction (the CSS half is the only live-responsive half); C-2's missing-seam arm (no phase-continuation knob; the ripple restarts at the dots) attached, perceptual half → SS-13 | INFO | KF.W6 |
| L:D-14 | **KF-AT-14** (the `.hero-dots` nowrap is prose, not a declaration; soft wrap opportunity between adjacent atomic inlines) | L's "harmless" verdict OVERTURNED by the bank; the redundancy observation folds into the bank's stronger total-no-op form | MINOR (bank) | KF.W6; trigger widths → SS-13 P-8 (banked) |
| D:D-10 (dropped by both readers; recovered) | **KF-EST-3** (props arm) + **KF-EST-7** (idiom arm) | The `:count="5"` → `.....` generality-the-form-cannot-serve observation rides KF-EST-3's delete-or-keep decision | INFO | KF.W6 / NO-WAVE-OWNER (with the banks) |

### Booked rows — KF-TD-1..9

| id | merged ids | severity | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|---|
| **KF-TD-1** | both readers' missed-1; kills C:S★-4's second bullet; scopes L:S-3 | **MAJOR** | On the shipping lane (WAAPI delegation — the frontier's own string easing earns the CSS twin), a live flip to `prefers-reduced-motion: reduce` is NEVER observed: `snapToReducedMotion`'s sole caller is `playFrame` (rAF lane); `shadowTick` never consults `withReducedMotion`; `respectReducedMotion: true` buys only the mount-time gate. **The library docblock asserts the opposite verbatim** ("a live flip on a WAAPI animation cancels the compositor handles before settling" — play-lifecycle.ts:337-341; frontier strategies.ts:66-75) — unreachable-path prose; `cancelWAAPI` cannot fire for a delegated animation. A user enabling Reduce Motion with the start screen open gets three dots pulsing on the LCP `<h1>` forever, while the sibling's CSS `@media` one span away responds instantly. Substrate note: at stale HEAD (rAF lane) live PRM DID work — the frontier flip silently removed it | this seat: grep = 2 hits; playFrame :199-222; delegation :53-64; play() :375-392; frontier twins named | **KF.W5** (the live-flip design decision + the docblock comment-truth cure), rider-linked to banked KF-EST-11 as one two-direction PRM story; live witness rides banked P-9's pass → KF.W9 |
| **KF-TD-2** | reader-2 missed-2; ruling 1 | MINOR | The substrate inversion: worktree `TypingDots.vue` ≡ `origin/master`; HEAD is the outlier. Corrects KF-EST-21's scoping, EST ruling 7's direction for this file, and discharges KF-AT-28's re-anchor obligation for it; the WAAPI-lane readings are the shipping truth, the rAF-lane readings describe stale HEAD | this seat's own diffs (empty vs origin/master; two-hunk vs HEAD) | **KF.W0** substrate settle; the registry corrections attach at the banks' ids |
| **KF-TD-3** | reader-1 missed-2; kills D:L-5's totality (K4); class-linked to banked KF-AT-15 (kf-AnimatedText.md:55) | MINOR (latent) | `count` is an unvalidated public numeric prop whose ONLY guard is Vue's `__DEV__`-gated `renderList` branch: in the production bundle a fractional/negative/NaN/Infinity `count` throws `RangeError: Invalid array length` inside the LCP `<h1>`'s render, with no `app.config.errorHandler` and no `onErrorCaptured` ancestor. Same defect CLASS as KF-AT-15 (sibling's unvalidated `cycleMs`), distinct mechanism (render-throw vs silent IACVT) — a new witness in the banked class, not a new identity class | runtime-core.esm-bundler.js:3189-3197 (re-read); node RangeError ×3 (re-run); grep errorHandler → 0 (re-run) | **KF.W6** — the same ONE contract motion as KF-EST-18 (delete or validate; `stagger`'s own fail-explicit posture is the house standard) |
| **KF-TD-4** | D:D-2 (its 1.52 slip killed at K7) | MINOR | The legibility floor is denominated in ALPHA (the retired gate's ≥0.15) rather than CONTRAST: no 20%-alpha ink composite can exceed **1.662:1** against its own backdrop (recomputed this seat; backdrop-independent), unreachable-by-≈1.8× against the 3:1 large-text floor — a green gate coexisting with a ghost. Correctly scoped by the axis itself as design-legibility, not conformance (`aria-hidden`); the animated ladder's bottom rung holds 300 of every 1200 ms | this seat's WCAG recompute (1.662/1.606); ladder re-derived by both readers digit-exact | **KF.W6** — one design decision WITH banked KF-EST-11's polarity question (rest-at-1 cures both); rendered-contrast + glyph-ink confirmatory probes → KF.W9 |
| **KF-TD-5** | L:D-4 / C:C-7 | MINOR (latent on the shipping lane) | `delay` is not a one-time phase offset: `onEnd` clears `startTime` every iteration, `advanceTo` re-enters `onStart`, which re-sleeps the full delay — JS-side period = `duration + delay`, drifting monotonically from the compositor's exact clock (dot 3: 27%/cycle, unbounded). No first-iteration guard at either substrate (both layouts read this seat). Latent because the pixels are compositor-owned and nothing consumes `animationiteration`; live on rAF/jsdom lanes, and it corrupts `iteration` bookkeeping today. C's own falsifier correctly answered: the compositor is never paused (reconcile chains after the sleep) | play-lifecycle onStart :118-124 / onEnd :135 / advanceTo :164-167; frontier frame.ts :38/:82 | **KF.W5** — a library RULING (is `delay` per-play or per-iteration?), then the fix follows the ruling |
| **KF-TD-6** | L:D-6 / D:D-5 | MINOR | `REST_OPACITY = 0.2` (script, :53→:93/:95) and `opacity: 0.2` (scoped CSS, :123) are two unbound authorities for one load-bearing number; a desync is invisible (both paths still render dots). The INTENT is the banked superlative (EST #7 — the resting paint as a designed frame); the unbound duplication is the defect; both true at once | file read whole this seat; zero `--` custom properties in file | **KF.W6** — bind (`:style` or one custom property); the cure may NEVER delete the CSS floor (the superlative constrains it) |
| **KF-TD-7** | D:D-7 / L:D-7 | INFO (ruling 7b) | `.typing-dot { display: inline-block }` is dead — the dots are flex items of the `inline-flex` container (the file's only markup) and flex items are blockified; scoped block, no out-of-container render. False-signal cost only | :111-118 re-read | **KF.W6** hygiene pass |
| **KF-TD-8** | L:D-9 | INFO (structural confirmed; magnitude unmeasured) | `steps()` is the pathological densify input: chord error across a discontinuity never falls below `WAAPI_CHORD_TOLERANCE`, the early exit is unreachable, the full 16-stop budget is spent per segment per dot (~900 sampler calls + Map allocations) inside `play()` inside `onMounted`, in the hero's first-paint window — against T's standing Q14 LCP history. L's own honesty adopted: possibly sub-millisecond | densify.ts:45/:57 (L's :44 corrected at K9); emission.ts:35-36 | one `performance.measure` → **KF.W9**/SS-13 decides retire-or-act; the densify-cost design question rides **KF.W5** |
| **KF-TD-9** | reader-1 missed-3 | INFO | The two ornaments in the LCP `<h1>` are phase-locked 3:1 by unguarded coincidence (`cycleMs` 3600 default / `CYCLE_MS` 1200), documented in neither file; any edit to either literal, or any consumer binding `:cycle-ms`, silently unlocks the beat. A latent cross-file motion coupling beside KF-AT-16/KF-EST-11; perceptibility explicitly NOT claimed | AnimatedText.vue:65; TypingDots.vue:46 | **KF.W6** — record the relationship in the motion-clock/token decision (one comment or one derived constant); perceptual half free-rides SS-13 |

## Killed-claims register — 10 entries

| # | killed claim | adjudication |
|---|---|---|
| K1 | **L:D-15 + C:C-3's rider + D's "Bounded by F-1"** — glass-ui is a phantom dependency; lockfile-faithful `npm ci` cannot build the demo; any `/tokens` wave must land F-1 first | **SUSTAINED (both readers; third-derived this seat at all three baselines).** HEAD: `6.0.0` optionalDep + lock 3; origin/master: `7.0.0` EXACT devDep + lock 3, matching the installed 7.0.0; the phantom is TRUE only of the uncommitted worktree strip. Identity = census SCH-1 (**FALSIFIED at HEAD**, banked; re-derived at kf-App ruling 1). D's contrast figures are NOT F-1-bounded — the installed 7.0.0 IS the frontier's declared producer. Surviving half re-homed at ruling 8: the nightly-only CI visibility rider on KF-APP-4 |
| K2 | **C:C-4's central mechanism** — a rejected `async onMounted` is an *unhandled promise rejection*; "errorHandler does not receive" it | **SUSTAINED (reader-2; this seat's runtime read).** `callWithAsyncErrorHandling` catches the returned promise (cjs:205-213) → `handleError`, never `unhandledrejection`. Both sibling axes had it right. **Corrects banked KF-EST-17's inherited phrasing** (ruling 6); the bank's two limbs stand |
| K3 | **D:D-3's / L:D-5's prescribed cure** — "the one API that would collapse three drivers to one is the one the primitive is documented against" (`AnimationGroup`) | **SUSTAINED (both readers; already banked as severed at KF-EST-5; re-derived this seat).** `isGroupWAAPIEligible` refuses multi-target (group/waapi.ts:31-33; `singleTarget` group.ts:159) → the rewrite trades 3 compositor animations for 1 main-thread loop; a managed child's `play()` throws; and the docblock example passes an `options` member `AnimationGroupInput` does not have (types.ts:27-29) — the artifact needing repair is stagger.ts:15-23 (in the KF-EST-5 bank row, → KF.W5). Per ruling 3 the kill demotes the CURE only, never the defect |
| K4 | **D:L-5's totality superlative** — "no value of `count` throws or produces NaN delays" | **SUSTAINED (reader-1; node + runtime re-verified this seat).** The Vue guard is `__DEV__`-only; the shipped bundle takes `new Array(source)` → RangeError for fractional/negative/NaN/Infinity, inside the LCP `<h1>`'s render. The `count = 0`/`count = 1` arms hold (stagger.ts's `total <= 1 → 0`). The surviving half is KF-TD-3 |
| K5 | **C:S★-4's second bullet** — "`playFrame` re-consults `withReducedMotion` every tick, so an OS toggle mid-session snaps a running infinite animation … `respectReducedMotion: true` is exactly the two words that buy it" | **SUSTAINED (both readers; grep + delegation read this seat).** The credited behaviour lives on the rAF lane this component never takes; C proved WAAPI eligibility itself in S★-2 and then credited the other lane's behaviour as this component's superlative — the axis's two halves contradict each other. The residue is KF-TD-1, where the credit inverts into the corpus's one new MAJOR |
| K6 | **All three axes' substrate headers** — "substrate keyframes.js `master` / `8281638c`, READ-ONLY" | **SUSTAINED-AND-INVERTED (reader-2 + ruling 1).** The axes read worktree bytes while naming HEAD — a real provenance-discipline failure (name your baseline). But for THIS file the worktree ≡ origin/master, so every anchor is frontier-durable and the practical damage is nil; HEAD, not the worktree, is the divergent layer — the opposite of the bank's standing model for this repo's churn |
| K7 | **D:D-2's extreme-pair figure** — "black ink on white → 1.52:1" | **SUSTAINED (both readers; recomputed this seat: 1.606).** 1.52 is the light-token figure from the doc's own table, reused as the extremum. The 1.662 ceiling and the backdrop-independence argument survive unchanged; the number is corrected before any wave quotes it |
| K8 | **L:D-3's gates census** — "`find scripts/gates` → 7 files … nothing typing-dots-shaped" | **SUSTAINED (reader-1; both baselines re-run this seat).** True of the dirty worktree only; origin/master carries **9** (incl. `structure/index.mjs` + `agent-surface.mjs`) and a `proof:structure` script the worktree deleted. The CONCLUSION (no `proof:typing-dots` anywhere) holds at both — the census supporting it did not; any re-run names its baseline |
| K9 | **Citation drift in L's strongest cells** — easing.js:238, interpolate.ts:249, densify.ts:44, emission.ts:33-35 | **SUSTAINED (reader-2).** True lines: easing.js:**239**, interpolate.ts:**253-254**, densify.ts:**45**, emission.ts:**35-36**; and this corpus adds reader-1's own drift (KF-AT-15 cited at kf-AnimatedText.md:57; it lives at :55). Substance survives everywhere; provenance folded to the KF-AT-28 law |
| K10 | **L:D-13** — the `shell/` barrel's membership rule "is unstated" | **SUSTAINED (reader-2; App.vue:138-143 re-read this seat).** The rule is stated between the two imports L quoted around ("imported directly, not via the barrel — a single-consumer leaf, the P-HERO import shape") and TypingDots satisfies it exactly; C's own K-2 had already killed the claim via the colocation edict. L read :138 and :142 and not :139-141 |

## Superlatives — 9 sustained (L-18 runs both ways; provenance per row)

1. **The three-part late-resolve/early-unmount guard** (:69/:76/:103-107) — banked (kf-EditorStartScreen #1), re-walked at the frontier bytes this seat, still survives every attack — now carrying the one honest qualifier this corpus adds: the blessing covers the flag, not the pre-await `els` snapshot (ruling 4's rider). Both readings are true at once.
2. **The resting paint exists in CSS as a designed frame** (banked EST #7) — chunk failure, `play()` rejection, unmount race, PRM snap, and null-`els` ALL land on visible static dots; the component cannot render a blank ellipsis, which is the H-era bug its own header describes. Simultaneously the substrate of D-1/D-2's polarity complaint (KF-TD-4); the corpus is right to record both.
3. **The LIGHT/HEAVY boundary consumed exactly as designed** (banked EST #8; L:S-1, C:S★-1) — type-only heavy import, two LIGHT value imports, engine only through the loader; the barrel's `export type` makes it the only shape that compiles; strengthened at the frontier, where the last static value.js edge (HEAD's `/easing` import) is gone and the LCP component adds zero eager value.js weight.
4. **D's L-3 stagger arithmetic** — the 6-rung ladder {0.2, 0.4667, 0.7333, 1.0, …} on the 150 ms grid, dwells 300/150/150/300/150/150, and BOTH empty triple intersections, reproduced digit-for-digit by both readers; `STEP_MS = 160` sits 7% off the step quantum — the setup for a synchronous blink — and survives it. The corpus's best single piece of design arithmetic.
5. **C's S★-2** — form the obvious criticism, then kill it twice at the tree (the group demotes to main thread; the canonical example does not typecheck) — both legs re-verified this seat. It converts a would-be component MAJOR into a real library-documentation defect and is why two sibling-axis findings die. Lift into the KF.W5 wave record.
6. **C's S★-3 executed rather than reasoned** — the one string crossing the package boundary checked against the installed `@mkbabb/value.js@4.0.0` dist; the CSS-twin bit it observed is precisely the WAAPI-eligibility bit the substrate rulings turn on. The right spend of a read-only budget while the parser lane's R1 is a live shipping crash.
7. **`aria-hidden="true"` is the right call and not the obvious one** — the tempting `role="status"` reading of the name would announce a decorative ornament where nothing is loading; the `<h1>`'s accessible name resolves cleanly to the sibling's single sr-only span; the one cost (AT hears no ellipsis) is already banked at KF-EST-7.
8. **The published kill-tables as instrument** — L's ten rows (nine hold; the F-1 row inherited a census-falsified premise, the gates census was one-baseline-short) and C's four (three hold clean; K-4's F3 coupling is a genuine cross-record flag) are what made the bad entries findable, on both axes. The discipline is the reason this corpus's false-positive rate stayed low.
9. **L's S-3 up-front PRM trace — sustained SCOPED (ruling 2)** — the mount-time chain, including the value.js clamp near-miss nobody asked it to find (unclamped, `steppedEase` at t=1 overshoots to 4/3 and the PRM rest would blank the dots), is exact at `easing.js:239`. Only the headline ("verified end-to-end; closes §6.5") is trimmed by KF-TD-1: it verified one of the two directions.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (→ KF.W9)

One pass, one pinned commit, the substrate NAMED in every capture (ruling 1: frontier vs HEAD flips the shipping lane).

- **U-1 · KF-TD-1's engagement witness** (run WITH banked P-9, same pass): start screen mounted, toggle no-preference → reduce. Prediction from source: at worktree/origin-master the dots KEEP pulsing (WAAPI lane, no re-consult); at HEAD they stop (rAF lane). The pair of directions (this + banked P-9's release arm) decides the KF.W9 PRM-unification shape.
- **U-2 · The shadow-tick magnitude** (KF-EST-5 residue, banked): `advanceTo` + playState reconcile ×3 dots ×60 Hz for the life of the start screen, on the LCP subtree — count source-final, cost unmeasured; T's Q14 LCP history (5141/5988) is the reason to actually measure.
- **U-3 · The densify cost** (KF-TD-8): one `performance.measure` around `play()` ×3 dots retires it to INFO-forever or promotes it.
- **U-4 · KF-TD-4's confirmatory pair**: rendered contrast against the true local backdrop under `.hero-dots` (aurora/cube/graph-paper — overlaps banked P-7) and the period-glyph ink diameter of Instrument Serif at the mega rung (86-177 px). Confirmatory only — the 1.662 ceiling is backdrop-independent and source-final.
- **U-5 · KF-TD-5's visual scatter** — reachable only where `Element.animate` is absent or `useWAAPI: false`; the code path is source-final, the rendered drift is not.
- **U-6 · KF-EST-22's permutation probe** (banked) — whether Vue 3.5's `setRef` can ever permute a static keyed `v-for` ref array; documentary risk, decidable mounted.
- **U-7 · C-2's perceptual half** — whether a reader sees the ripple break where the dots restart the ramp at zero (the missing-seam arm's only live term).
- **U-8 · Flagged for the next seat** — this corpus diffed TypingDots.vue, package.json, package-lock.json, ci.yml, stagger.ts, play-lifecycle/*, delegation.ts against the frontier; `EditorStartScreen.vue`, `AnimatedText.vue`, `shell/index.ts` anchors remain worktree anchors under KF-AT-28.

## Identity guards (anti-rename; fold-by-reference, never re-book)

- **kf-EditorStartScreen KF-EST-1 / -3 / -5 / -6 / -7 / -11 / -17 / -18 / -20 / -21 / -22** — eleven fold destinations above; every rider this corpus adds attaches at the bank's id. Two bank corrections carried OUT of this record: KF-EST-17's phrasing (K2) and KF-EST-21's scoping (KF-TD-2) — addenda at the bank, never rewrites.
- **kf-App KF-APP-4 / -24** · **kf-AnimatedText KF-AT-12 / -14 / -15-class / -16 / -28** · **kf-CSSCodeEditor KF-CE-13** — cross-file banks folded by reference; the forced-colors identity is now on its FOURTH re-file — sweep the bank before filing, per D's own scoping honesty.
- **Census SCH-1 / F-1** — the phantom-dependency family stays dead at its bank (K1); **census §6.5** PRM inconsistency folds via KF-EST-11 + KF-TD-1's two-direction story; **census S-8** (JUSTIFIED BESPOKE, keep) survives this corpus unanimously — every axis and both readers reinforce it.
- Successor kf-* records fold against **KF-TD-1..9** and the banks named here.

## Closing verdict

Both hostile re-reader returns survive with corrections in both directions, and the seat's work concentrated where they disagreed: the corpus's one genuinely new MAJOR is **library-side and directional** — the engine's live reduced-motion snap is unreachable on the very lane this component ships, under a docblock that asserts the opposite (KF-TD-1, the engagement twin of banked KF-EST-11's release asymmetry; one axis had inverted it into a superlative) — while the substrate hazard the registry has been carrying for this file runs **backwards**: the dirty worktree IS the frontier, HEAD is the outlier, so the orphaned comment is a shipping defect, the WAAPI-lane mechanics are the shipping truth, and every challenge anchor is frontier-durable (KF-TD-2). Two banked rows were corrected in passing (KF-EST-17's phrasing, KF-EST-21's scoping), one reader's severity demotion was overruled on the cure-separation law, one reader's "unbanked" was corrected against the bank's own text, and three corpus rows both readers dropped were recovered — nothing lost: head, tail, or interval. The component's adjudicated shape is unchanged from its bank: a small, carefully-reasoned machine whose teardown, resting-frame, and boundary discipline are genuinely exemplary, and whose contract (a half-reactive, unvalidated public prop), prose (three constants sworn to a dead gate; a comment naming an import that isn't there), and polarity (every non-animating path lands on the dimmest frame a ≤1.662:1 ceiling can render) are where the defects live. Routings: **KF.W0** the substrate inversion · **KF.W4** the gate/provenance folds · **KF.W5** the library rows (KF-TD-1, KF-TD-5, the banked stagger-docblock and memoized-rejection rows, KF-TD-8's cost question) · **KF.W6** the component contract/design motions under the named constraints (one `count` motion; the CSS floor never deleted; the comment-truth cures) · **KF.W9** the eight-probe residue with U-1 paired to banked P-9 · **NO-WAVE-OWNER** the copy/idiom folds riding their banks.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; the X·KF specs are forming and these routings are their agglomeration input (M-25); execution awaits the owner's begin-word.
