# ColorSpaceSelector — adjudicated apotheosis (M-12 tri-fold, arbiter-F)

**Seat**: arbiter-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-07-27 · **HEAD**: `c654824e` · branch `tranche-u` · **Corpus**: 15 accusations (L-1..L-15) against `demo/color-session/ColorSpaceSelector.vue` and its catalog/formatter dependency chain.
**Workers adjudicated**: worker-F (Fable 5, `colorspaceselector-worker-f-adjudication`) · worker-O (Opus 5 [1m], `colorspaceselector-adjudication-worker-O`).
**Method (L-14)**: both workers were attacked before either was adopted. All seven verdict-level disagreements (L-3, L-4, L-5, L-8, L-10, L-14, L-15) were re-derived against the tree by this seat with its own instruments. Four suspiciously-agreed rows (L-1's five counts, L-2's byte-identity, L-7's 38-key census, L-12's `any` census) were independently re-measured, not spot-trusted. One predicted citation-inheritance event (F's dissent item 3 → O's L-10 evidence cell) was tested and **confirmed** — see DISSENT.
**Arbiter's own instruments this session (all static + node against dist; zero browser probes needed — probe parsimony)**: full source read of ColorSpaceSelector.vue (whole, 311 lines); picker-color.ts:70-95; color-model.ts:55-80; colorSpaceInfo.ts key census (`grep -nE '^ {4}"?[a-z0-9-]+"?: \{'` → 13); AboutPane.vue:20-100; color-space-meta.ts (whole head); MixConfigBar.vue:95-120; GradientVisualizer.vue:178-196; ColorNutritionLabel.vue:208-216; ConsoleRail.vue:172-176; ColorPicker.vue:40-44; App.vue (demo/color-picker/) cssColor census incl. :271/:340; usePaneRouter.ts:98/:150/:175-182; useColorPipeline.ts key count (38, my own grep over :265-332); useSliderGradients.ts:12-16; src/css/grammar.ts:283-286; eslint.config.js demo-glob census; tsconfig.demo.json:38-52 + package.json#exports + `ls dist`; ARCHITECTURE.md:35-55; `node --input-type=module` against `dist/subpaths/{css,color}.js` (five-space serialization + parse + lab round-trip — the L-3 decider); installed glass-ui 7.0.0: `dist/styles/typography/semantic.css` text-title utility, `dist/styles/typography/scale.css` `--type-weight-title: 700`, WatercolorDot.vue.d.ts prop union + hashString doc, SelectTrigger.vue.d.ts prop union, package.json exports (`./select`, `./watercolor-dot` first-class); repo-scope counts: `../ui/` imports (48 files / 90 statements / 1 in color-session), `<WatercolorDot` (23 instances / 21 with `tag=`), `inject(SAFE_ACCENT_KEY)!` (6 consumers), `updateToColorSpace` (exactly 4 lines), `colorSpace: any` (exactly 3), `CSS_NATIVE_SPACES` (definition-only), `toCSSColorString` (3 one-arg call sites + 1 dead import), `ls demo/@` (does not exist).
**Environment disclosure (L-12)**: the dev server :9000 is UP and API-LESS; no gate in this corpus is data-backed, so the environment could witness all of them — but this seat needed none: every ruling was decidable from source + the shipped dist. The workers' live cells (chromium 1600x1000) are adopted as corroboration only, never as load-bearing where a static derivation exists. playwright-webkit and safari-app are separate evidence cells (INBOX I-20); every pixel claim in this record is chromium-cell and labeled so.

## Scoreboard

**8 CONFIRMED · 5 RESCOPED (L-3, L-4, L-5, L-8, L-14) · 1 REFUTED (L-15, replacement rider booked) · 1 GLASS-OWNED (L-10) · 0 UNVERIFIABLE-HERE.**

Blocker-weight survivor: **L-1** — the product states false color-science facts (the CIE-RGB-1931 nutrition card) for 5 of 18 offered spaces and renders a bare "Detailed Guide" heading for 7, on a first-class route; re-derived statically by this seat from the 18/13/11 set differences and the two masking fallbacks read in source.

## Ruled disagreements (the tri-fold's actual work)

| id | worker-F | worker-O | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-3** | CONFIRMED (garnish rescoped in place; wave leaves projection open as an option) | RESCOPED (mechanism refuted; mark-never-project + anti-projection lock) | **RESCOPED — O wins** | This seat's node run against `dist/subpaths/{css,color}.js`: all five out-of-gamut specimens (`rgb(385.302…)`, `hsl(… -1295.15% 103.66%)`, `hwb(… -51.10%)`, `color(display-p3 1.402…)`, `color(srgb-linear 2.580…)`) return `ok:true` from `parseCssColor` AND `convertColor(·,'lab')` recovers `lab(92, 88.8, 20 / 0.827)` to ≤1e-10 on every one. The challenger's "would not parse" is false in both witnesses, and the un-projected specimens round-trip **losslessly** — so folding `mapColorToGamut` into the formatter (left open by F's wave item 2) would replace an exact statement with a lie and break a property GREEN today. O's G6(ii) round-trip lock is adopted verbatim. F's surviving observations fold in: the callsite made no explicit gamut-policy decision, and the hex row projects (`toRgba8 gamut:'clip'`) while the CSS rows do not — resolved by the mark: the hex row's clip is forced by its 8-bit sRGB encoding and carries `data-out-of-gamut` when the source leaves sRGB. |
| **L-4** | CONFIRMED (component's own :109 edge; deletion arm already W-HYGIENE's) | RESCOPED (repo-wide 90 edges / 48 files; component owns 1 of 90) | **RESCOPED — O's identity ruling; no substantive conflict** | My repo-scope grep: 48 files / 90 statements / exactly 1 in `demo/color-session/` (ColorSpaceSelector.vue:109). ARCHITECTURE.md:39 bans the layer by name; the lattice (~:53) grants color-session `platform / shared / published packages` only; glass-ui 7.0.0 ships `./select` first-class (my read of the installed exports map). Both workers agree the component edge dies here under a gate and the 19-dir/90-site extirpation keeps its W-HYGIENE identity (booked at the ColorPicker adjudication's L-6). The verdict label follows where the count lives: repo-wide → RESCOPED, with the 1 color-session edge CONFIRMED inside it and gated in this wave (G10). |
| **L-5** | RESCOPED (dead globs + "incoherent injection contract" kept as co-core) | RESCOPED (dead globs confirmed; inverted-edge arm REFUTED; hard-assert = house contract) | **RESCOPED — O's version wins** | (a) Dead guards: my eslint.config.js read — 4 dead `demo/@/{components,lib}` globs inside the :232-239 object (which also carries live `demo/color-picker/**` globs, F's precision adopted) + 2 in the fully-dead :275-276 composables object = 6 dead globs; `ls demo/@` → No such file or directory; no object covers `demo/color-session/**`. Airtight, repo-wide, FOLDed to a lint-glob-liveness wave under id L-5(a) with F's born-RED `eslint --print-config` command. (b) Inverted edge: REFUTED — SAFE_ACCENT_KEY is declared in color-session's own keys.ts; provide/inject is ambient, no import edge exists; and my grep finds `inject(SAFE_ACCENT_KEY)!` at **6** consumers (shell/dock ×3, palettes ×2, this component) under a provider App.vue installs for every route — the non-null assertion is the house contract, not this component's deviation. F's "hard-assert 8 lines from a null-tolerant inject" is demoted to a non-defect observation: the two keys carry deliberately different criticality (accent styling always provided at boot vs. an optional pipeline), and the file's own :136-142 comment explains the asymmetry. |
| **L-8** | CONFIRMED (component-scoped dead prop) | RESCOPED (21 of 23 WatercolorDot instances repo-wide carry `tag=`) | **RESCOPED — O wins on identity** | My multiline count: 23 `<WatercolorDot` instances in demo across 11 files, 21 with `tag=`. The producer d.ts (read by this seat) declares `{ color, variant, animate, cycleDuration, range, seed }` — no `tag`. The component's own :82 attribute dies free in this wave (G8); the row's identity is a 21-site W44 glass-7 adoption residue and FOLDs to a residue sweep so the other 20 are not lost. |
| **L-10** | CONFIRMED (with producer-state correction: text-title TOKENIZED in 7.0.0) | GLASS-OWNED (but repeats the stale "semantic.css:132 hardcoded 700" cell) | **GLASS-OWNED — O's verdict class + F's evidence correction, both adopted** | Verdict class: the named cure (a SelectTrigger title/display variant) is a producer change — per the ColorPicker-adjudication D-10 precedent, the label follows the cure's owner; rides the standing glass-ui BH relay (owner edict), cites INBOX I-20; no local `:deep` expansion permitted meanwhile. Evidence: decisive for F — the installed 7.0.0 has **no** `dist/semantic.css`; `dist/styles/typography/semantic.css`'s `text-title` utility rides `font-weight: var(--type-weight-title)` (my read), default `700` in `dist/styles/typography/scale.css` (my read). The component's :291-299 comment ("a UTILITY hardcode, not a token") and O's repetition of the same cell are both stale against today's producer. F predicted that a verbatim repetition of the 700-hardcode claim would evidence citation-inheritance; O repeated it; the event is recorded in DISSENT. The relay request and the P10 BOOKED SWAPs (:199, :212-213, :235-238) must be re-derived against the tokenized producer; a narrower consumer cure now exists (pin `--type-weight-title` in scope) pending the variant. O's counts adopted where sharper: scoped block = 143 raw lines (~56 non-comment); `:deep(` occurrences = 4 (:239, :249, :261, :269), a fifth of demo's 20. |
| **L-14** | CONFIRMED (MINOR stale-config; + the "CLOSED 8-key set" comment vs 7 exports drift) | RESCOPED (program-wide config row, no component locus, no live break) | **RESCOPED — O wins on booking; facts unanimous, F's bonus drift adopted** | My read: tsconfig.demo.json:42-49 = 8 hand-written entries; `dist/index.d.ts`, `dist/subpaths/parsing.d.ts`, `dist/subpaths/units.d.ts` all missing (my `ls`); `/css` and `/value` omitted while shipped (`dist/subpaths/` holds both) and consumed by this component's chain (picker-color.ts:34); package.json#exports = exactly 7 keys, no `.` root; the block's own comment claims "a CLOSED 8-key set" — false by one. vite.config.ts generates its aliases from the exports map; TS is hand-typed. No live break (both workers' vue-tsc exit 0; bundler-mode self-name resolution covers it). FOLD to W-HYGIENE config parity with F's born-RED generator-parity node command. |
| **L-15** | CONFIRMED (contingent on the catalog's `interpolatable` flag; fold sequenced) | REFUTED (two concepts sharing a noun; consolidation = KISS-banned contrivance) | **REFUTED — O wins; a replacement rider is booked in its place** | My source reads: INTERPOLATION_SPACES (color-space-meta.ts:26-36) is a NINE-member `{value,label,description}` vocabulary explicitly authored at S.W5-6/F16 ("spaces you may interpolate THROUGH"); the two workbench blocks iterate it with typed `v as PickerSpace` casts and materially different row anatomy (description + PreviewRamp) from the selector's 18-member specimen catalog. One shared component would offer hex/kelvin as interpolation spaces or amputate the catalog — the exact shared-wrapper contrivance the KISS edict bans; and the "duplicates" are the better-typed instances (the `any` is L-12's, already booked). F substantially conceded ("'one concept' holds only at the choose-a-color-space altitude"). **Replacement rider (this seat's own derivation, per the C-7 precedent)**: the 9 INTERPOLATION_SPACES labels verbatim-duplicate PICKER_SPACE_NAMES strings and membership is hand-maintained — once MT-CSS-1 lands `interpolatable`, membership + labels derive from the catalog (`CATALOG.filter(e => e.interpolatable)`), descriptions stay local. Born-RED today: `npx vitest run test/interpolation-subset.test.ts` asserting derivation — RED because no catalog exists. Sequenced strictly after MT-CSS-1; the workbench Selects are NOT consolidated into this component. |

**Cure-level rulings inside agreed verdicts**: L-9 is CONFIRMED by both; F's garnish ("paint the space's own projected color per dot") is REFUTED by the same L-3 anti-projection lock — the cure is `:seed="space"` alone. L-6 is CONFIRMED by both; O's re-dating adopted: the picker-free break is unreachable TODAY (VIEW_MAP pins About as the picker view's right pane; KeepAlive holds the watcher) but ARCHITECTURE.md:41-43 makes `/about` a first-class trailing destination ("About is a quiet trailing destination, not Picker's permanent right-hand companion" — my read), at which moment the sole conversion watcher loses its guaranteed host. The defect is structural and must be cured before that route lands.

## Verdict register — all 15 rows

Every CONFIRMED row carries a reproduction a fresh session can run. Line anchors are HEAD `c654824e`; re-resolve before executing any cure (the D-19 lesson).

- **L-1 — CONFIRMED (BLOCKER).** Set differences re-computed by this seat: PICKER_SPACE_NAMES (picker-color.ts:72-90) = 17; DISPLAY_COLOR_SPACE_NAMES (color-model.ts:75-78) = +hex = 18, iterated at ColorSpaceSelector.vue:150; colorSpaceInfo.ts = 13 keys (rgb hsl hsv hwb lab lch oklab oklch xyz kelvin ictcp jzazbz hex) → missingInfo(5) = srgb-linear, display-p3, a98-rgb, prophoto-rgb, rec2020; AboutPane.vue:80-95 MarkdownSpace = 11 → missingDoc(7) = those five + ictcp + jzazbz. Masking fallbacks read in source: ColorNutritionLabel.vue:210-215 `space in colorSpaceInfo ? … : colorSpaceInfo.rgb`; ConsoleRail.vue:174 `(colorSpaceInfo as any)[space]`; AboutPane.vue:51 `v-if="activeMarkdownModule"`. Repro (static, fresh session): select `display-p3` → `resolveColorSpace` yields a key absent from the 13 → the nutrition card renders `colorSpaceInfo.rgb` ("… Created: 1931") and the guide v-if hides. O's live chromium witness (verbatim CIE-1931 card under the "Display P3" title; guide h2 childCount 1) corroborates. Booking: the render sites are About-owned; the catalog home is color-session's — ONE identity, cross-booked, not two defects.
- **L-2 — CONFIRMED (MAJOR).** Byte-identity reproduced by this seat from dist: the shipped default `lab(92% 88.8 20 / 82.70%)` → rgb branch = `rgb(385.302835934518 143.376536829596 199.64311881105 / 82.7%)` — exact match to both workers. Five precision homes verified: src/css/grammar.ts:283-285 `Number(value.toFixed(12))`; color-model.ts:67-72 `toCSSColorString(color, _digits = 2)` dead param (my grep: all 3 call sites one-arg — ColorPicker.vue:293, useColorPipeline.ts:214/:216); useColorPipeline.ts:28 `DIGITS = 2` returned at :331 with zero readers; useSliderGradients.ts:14 second copy; ColorSpaceSelector.vue:162-165 `toFixed(4)` in the adjacent branch. O's sixth fact verified: useColorUrl.ts:6 imports `toCSSColorString` and never calls it. Truncation: O's 16-of-18 overflow census (worst 758px in a 234px box) adopted as the chromium-cell witness; the string lengths re-derive from my dist run. ARCHITECTURE.md:245's "no library-wide string serializer exists" cited by both.
- **L-3 — RESCOPED** (ruled above). Surviving row: (a) five CSS rows print out-of-gamut coordinates with no mark, and the browser silently clamps on paint (O's measured witness: the HSL row browser-resolves to `rgba(255,255,255,0.827)` — pure white — beside a dot painting the true color); (b) four rows (hsv, kelvin, ictcp, jzazbz) emit a `space · a · b · c` pseudo-syntax no parser accepts. Cure: per-row gamut MARK + a declared non-CSS presentation form — **never projection** (this seat's round-trip run is the lock). The unused clampPickerColor/mapPickerOklabToSrgb helpers are correctly absent from this path.
- **L-4 — RESCOPED** (ruled above). The component's :109 edge is CONFIRMED and dies here (G10, scoped); the 89 remaining edges keep their W-HYGIENE extirpation identity.
- **L-5 — RESCOPED** (ruled above) → L-5(a) lint-glob liveness, repo-wide FOLD. Born-RED today: `npx eslint --print-config demo/color-session/useColorPipeline.ts | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s).rules['no-restricted-imports'];process.exit(r&&JSON.stringify(r).includes('color-picker')?0:1)})"` — exits 1 (no boundary rule reaches color-session). The inverted-edge arm is REFUTED and retired with rationale.
- **L-6 — CONFIRMED (MAJOR, symptom re-dated).** My grep: `updateToColorSpace` = exactly 4 lines repo-wide (useColorPipeline.ts:170 definition, :324 return, ColorPicker.vue:214 destructure, :352 sole call inside the :348-354 watcher) — zero callers in the domain that owns the command. Two divergent host write mechanics verified: ColorPicker.vue:43 `updateModel({selectedColorSpace})` vs AboutPane.vue:25 bare model spread. The conversion is a side effect of one peer component's watcher. Unreachable today (VIEW_MAP + KeepAlive), structurally due the day ARCHITECTURE.md:41's first-class `/about` lands. Repro: the grep above, plus G9's born-RED pair.
- **L-7 — CONFIRMED (MAJOR).** My own count of the useColorPipeline.ts:265-332 return literal = 38 keys; keys.ts:7 types COLOR_MODEL_KEY as the whole surface; the component reads `colorModel.model.value.color` at :156/:158 — 1 of 38. DIGITS exported with zero consumers (my grep). Disposition: FOLD to the COLOR_MODEL_KEY role-split wave (MT-CSS-2); this wave's obligation is negative — add no new consumer of the bag. F's transport caveat verified by this seat: CSS_COLOR_KEY is provided as `cssColorOpaque` (App.vue:271) — alpha-stripped, NOT a drop-in for the alpha-bearing cssColor; the successor reads the pipeline's own `cssColor` computed.
- **L-8 — RESCOPED** (ruled above). Component fact certain (producer d.ts has no `tag`; the attribute doesn't survive to the DOM per both workers' reads); dies here under G8; identity = 21-site glass-7 residue sweep (20 remaining sites FOLDed).
- **L-9 — CONFIRMED (statically certain).** All 18 rows bind the identical `:color="cssColor"` with no `seed` (:81-86, my read); the producer doc (WatercolorDot.vue.d.ts:14-16, my read): "The filter `seed` is per-instance off `hashString(color + seed)` so each dot's wet edge is uniquely displaced (no twelve-clones)" — equal inputs, equal hash, 18 identical silhouettes by construction. O's corrected live measurement (distinctVisualSignatures = 1 after dereferencing the per-mount filter urls; identical borderRadius/feTurbulence seed 240 across all rows) corroborates; O's recorded first-pass false refutation (signature included the per-mount filter url) is preserved in DISSENT as a method lesson. House census: 16 of 23 instances pass `seed`; this omission site is the only one rendering 18 clones in one visible list. Cure: `:seed="space"`, one binding — the projected-color garnish is REFUTED (anti-projection lock).
- **L-10 — GLASS-OWNED** (ruled above; cites INBOX I-20). Producer prop union read by this seat: `{ disabled, class, variant: "default"|"ghost", size: "sm"|"default" }` — no title/display register. Consumer residue booked here as FOLD (4 `:deep()` reaches + 3 BOOKED SWAPs + the stale :291-299 comment, which any wave touching the file must correct to the tokenized-producer truth), discharging the day the producer variant ships (BANK-1). Relay carries F's producer-state correction.
- **L-11 — CONFIRMED (MINOR).** My greps: `CSS_NATIVE_SPACES` (color-model.ts:58) — definition is the only hit across demo/src/api/test; `colorToHexString` (:61-65) pure delegate, 5 callers; `toCSSColorString` (:67-72) dead `_digits`, 3 one-arg call sites + useColorUrl.ts:6 dead import. The component imports the rename at :119 five lines after importing from picker-color (the real home). IDENTITY GUARD: fact-identical to the ColorPicker adjudication's L-10 — one identity, already FOLDed to W-HYGIENE, never re-booked under a second name (anti-rename). Within this wave: CSS_NATIVE_SPACES + toCSSColorString die with the new formatter; colorToHexString (5 callers) rides the key-split wave.
- **L-12 — CONFIRMED (MAJOR).** My grep: `colorSpace: any` = exactly 3 (ColorSpaceSelector.vue:11, ColorPicker.vue:43, AboutPane.vue:25); the fourth hole :91 `space as DisplayColorSpace` forced by :150 `Object.entries` erasing the key union; :125 `modelValue: string` wider than the 18-member domain type; the pre-3.4 emit pair (:146-148) eleven lines from the 3.4+ `defineModel<boolean>("open")` (:144). Both workers independently ran eslint (exit 0) and vue-tsc (exit 0) — nothing guards it; the cure must be structural. Repro: G7's greps + the G3-style type witness (RED today by inversion — the deliberately-wrong binding compiles).
- **L-13 — CONFIRMED (MINOR; FOLD not BUILD).** Both transport chains re-walked by this seat: picker origin — ColorPicker injects the pipeline, destructures cssColor, passes it back DOWN at :42 to a child that injects the same pipeline; About origin — App.vue:340 `cssColor: () => cssColor.value` → usePaneRouter.ts:98 (dep type) / :150 (about rightProps) → AboutPane.vue:76 prop → :23 into the selector: four hops for an ambient datum. The incoherence holds: :137-142 promises null-tolerant degradation while :126's required prop demands the same datum by another road. FOLD to MT-CSS-2 with L-7 (deleting the prop today would deepen the 38-key coupling); the cure reads the pipeline's alpha-bearing `cssColor`, never CSS_COLOR_KEY (= cssColorOpaque, verified App.vue:271).
- **L-14 — RESCOPED** (ruled above) → W-HYGIENE config parity. Born-RED today (F's command, logic re-verified against my reads): generate tsconfig.demo.json `paths` from package.json#exports as vite.config.ts already does; exits 1 now (3 dead targets, 2 omissions, and the "CLOSED 8-key set" comment false by one).
- **L-15 — REFUTED** (ruled above), replacement derivation-rider booked with F's born-RED vitest, sequenced strictly after MT-CSS-1.

## The wave — V·MT-W-CSS1 (re-authored: O's chassis, F's corrections, this seat's rulings)

**Title**: The space catalog becomes one total type; the specimen line becomes one legible, honest sentence.
**bornRed**: true. **Repo**: `/Users/mkbabb/Programming/value.js`, branch `tranche-u`.

**Scope.**
NEW `demo/color-session/space-catalog.ts`: one `SPACE_CATALOG` typed `satisfies Record<DisplayColorSpace, SpaceEntry>`, `SpaceEntry = { id, label, channels, info: SpaceInfo, doc: DocModule | null, interpolatable: boolean }`, exported as a key-preserving `readonly SpaceEntry[]` so `Object.entries` never erases the union — folding DISPLAY_COLOR_SPACE_NAMES, colorSpaceInfo, AboutPane's markdownModules and INTERPOLATION_SPACES *membership* into one total home. The wave AUTHORS the 5 missing info rows (srgb-linear, display-p3, a98-rgb, prophoto-rgb, rec2020) and decides the 7 missing docs explicitly (`doc: null` is an authored decision rendering an explicit empty state, never a silent v-if) — totality by content: a catalog that compiles because five entries were stubbed is the masking fallback wearing a type's clothes.
NEW `demo/color-session/specimen-format.ts`: one `formatSpecimen(color, space): { text, form: 'css' | 'channels', outOfGamut: boolean }` — total over all 18 members, ONE digit policy sized to the 234px caption box, gamut MEASURED and MARKED, **never projected** (the L-3 ruling; the hex row's forced 8-bit clip carries the mark when the source leaves sRGB). Replaces the dual formatter; `specimenFor` becomes one call.
EDITED `ColorSpaceSelector.vue`: `defineModel<DisplayColorSpace>({ required: true })` replaces the :125-127 wide prop + :146-148 emit + the :11 `any`; typed catalog iteration kills the :91 cast; rows render `data-specimen-form` + `data-out-of-gamut`; `tag="div"` deleted, `:seed="space"` added; the :109 `../ui/select` edge repointed to `@mkbabb/glass-ui/select`; when the pipeline is present the selection routes through `colorModel.updateToColorSpace(space)` (one command home — G9), the typed v-model remaining the pipeline-less contract; the stale :291-299 producer comment corrected to the tokenized-7.0.0 truth.
EDITED hosts: ColorPicker.vue:43 + AboutPane.vue:25 handlers become typed v-model bindings (their `any`s die); ColorPicker.vue:348-354 watcher DELETED (G9). ColorNutritionLabel.vue:214 fallback and ConsoleRail.vue:174 cast DELETED — both read the catalog (unreachable code once the catalog is total). AboutPane.vue markdownModules → catalog docs; the :51 v-if becomes an explicit authored empty state. color-model.ts: CSS_NATIVE_SPACES + toCSSColorString deleted; call sites route through specimen-format.
OUT OF SCOPE, named so nothing is lost: the 89 remaining `demo/ui` edges; the 6 dead eslint globs; the COLOR_MODEL_KEY split + cssColor transport (MT-CSS-2); the exports↔tsconfig parity; the 20 other dead `tag=` sites; the glass-ui SelectTrigger title variant (relay + BANK-1); the INTERPOLATION_SPACES derivation rider (post-MT-CSS-1).

**Structure (L-8 law — three classes made unrepresentable; gates only for what types cannot carry).** (1) `satisfies Record<DisplayColorSpace, SpaceEntry>` makes a space-with-no-info a compile error — the masking fallbacks are not banned, they become meaningless and delete. (2) `defineModel<DisplayColorSpace>` + key-preserving catalog array makes all four `any`/cast holes unspellable at the boundary. (3) The discriminated `{ form: 'css' | 'channels' }` return makes two-grammars-in-one-branch unrepresentable — the form is data, not a branch. Gates remain for rendering truth (pixels, live gamut, dot identity, About facts) and mechanical config/edge invariants.

**Gates (all RED today; each failing input pasted).**
- **G1 — catalog totality**: `node docs/tranches/V/megatranche/gates/gate-catalog-totality.mjs` — RED: offered=18 info=13 docs=11; failing input the string `display-p3` (in DISPLAY_COLOR_SPACE_NAMES, absent from colorSpaceInfo.ts and markdownModules).
- **G2 — no masking fallback survives**: `test "$(grep -rn 'colorSpaceInfo\.rgb\|(colorSpaceInfo as any)' demo | wc -l | tr -d ' ')" = 0` — RED: 2 hits (ColorNutritionLabel.vue:214, ConsoleRail.vue:174). The structural cure's receipt: if the catalog is total, both lines are unreachable.
- **G3 — About states true facts, never a bare heading**: `npx playwright test e2e/smoke/oracles/o21-space-catalog-truth.spec.ts` (dev :9000, hash route, chromium 1600x1000) — RED: selecting "Display P3" renders the CIE-1931 rgb card and a guide h2 with childCount 1 (O's live witness; re-derived statically by this seat). Asserts per space: nutrition ≠ the rgb card unless space IS rgb; guide renders a body or `data-guide-state="absent"`.
- **G4 — every specimen caption fits its box**: `npx playwright test e2e/smoke/oracles/o22-specimen-legibility.spec.ts` — RED: 16 of 18 captions overflow (worst 758px/234px); failing input = the boot color's RGB row `rgb(385.302835934518 143.376536829596 199.64311881105 / 82.7%)` (byte-pinned by this seat from dist).
- **G5 — one grammar per row; CSS rows round-trip the shipped parser**: `node docs/tranches/V/megatranche/gates/gate-specimen-grammar.mjs` — RED: `data-specimen-form` absent (grep -c → 0) and the four `·`-rows return ok:false from `parseCssColor` while sitting in the same slot as real CSS; failing input the `hsv` row of the boot color.
- **G6 — out-of-gamut is MARKED, never projected**: `npx playwright test e2e/smoke/oracles/o23-specimen-gamut-honesty.spec.ts` — RED: five rows out-of-gamut unmarked (`data-out-of-gamut` grep → 0); sharpest input the HSL row, which browser-resolves to `rgba(255,255,255,0.827)` — white — beside a dot painting the true color. Clause (ii), the anti-projection lock: printed CSS coordinates must still satisfy `convertColor(parseCssColor(caption).value,'lab') ≈ model color` to 1e-6 — GREEN today by this seat's own run (≤1e-10), and it goes RED the moment anyone folds mapColorToGamut into the formatter.
- **G7 — the boundary is the domain type**: `test "$(grep -rn 'colorSpace: any' demo | wc -l | tr -d ' ')" = 0 && test "$(grep -c 'as DisplayColorSpace' demo/color-session/ColorSpaceSelector.vue)" = 0 && npx vue-tsc --noEmit -p tsconfig.demo.json` — with F's type witness in the demo glob: `// @ts-expect-error — a bare string must not be assignable to the selector model` over a `modelValue="not-a-space"` binding. RED today twice over: 3+1 grep hits, AND the witness's expectation is reported UNUSED (the wide prop compiles the wrong binding) so vue-tsc exits non-zero. Lint is inadmissible here (eslint exits 0 today — both workers, independently).
- **G8 — eighteen dots, eighteen silhouettes, zero dead props**: `npx playwright test e2e/smoke/oracles/o24-specimen-dot-identity.spec.ts && test "$(grep -c 'tag=' demo/color-session/ColorSpaceSelector.vue)" = 0` — RED: distinctVisualSignatures = 1 (identical borderRadius / feTurbulence seed 240 across all 18 — O's corrected measurement; statically certain from equal hash inputs) and the :82 `tag="div"` grep hits. The oracle keys on (borderRadius | background | feTurbulence@seed) — explicitly NOT the per-mount filter url (O's recorded false-GREEN trap). Must NOT be satisfied by repainting dots with projected colors (would fail G6 ii).
- **G9 — one command, one home**: `test "$(grep -c 'updateToColorSpace' demo/color-session/ColorSpaceSelector.vue)" -ge 1 && test "$(grep -A3 -n 'watch(' demo/picker/ColorPicker.vue | grep -c 'selectedColorSpace')" = 0` — RED: the component has zero references; the sole caller is the :348-354 picker watcher. Recorded so ARCHITECTURE.md:41's first-class `/about` cannot land on a live defect; today's unreachability is dated, not denied.
- **G10 — color-session draws no edge into the banned layer**: `test "$(grep -rn 'from \"\.\./ui/' demo/color-session/ | wc -l | tr -d ' ')" = 0` — RED: exactly 1 hit (:109). Deliberately scoped to color-session (repo-wide = 90 statements = the W-HYGIENE wave; booking 90 behind a component gate is the un-completable-wave failure).

**piObligations.** Matrix: chromium 1600x1000 (primary, the workers' cell) + 1440x900; safari-app via safaridriver :4599 is a SEPARATE cell (I-20) and this wave's pixel claims are chromium-cell claims labeled so; playwright-webkit is a third cell, never a stand-in. Route: `http://localhost:9000/#/` (hash only — MT-F006). ENV: dev is API-LESS; every arm of this wave is API-independent, so this environment can witness all ten gates; built-bundle witnesses ride MT-PICKER-1's standing :8091 harness, not duplicated here. Selectors pinned: `.space-trigger`, `[role=option] .specimen-name/.specimen-caption/.specimen-dot`, `.about-card`, `h2:has-text('Detailed Guide')`. Committed captures force-added past `.gitignore` `*.png`: `docs/tranches/V/megatranche/audit/components/ColorSpaceSelector/witness/{before,after}-catalog-open.png`, `{before,after}-about-display-p3.png`, `specimen-dots-{before,after}.png`; the challenger's existing probe-catalog-open.png + probe-display-p3-about.png adopted as BEFORE frames and force-added in this wave's commit. A witness not committed does not exist.

**deltaObligations.** (1) about-display-p3: BEFORE = "Created: 1931 / Components Red, Green, Blue" under the title "Display P3" + guide childCount 1; AFTER = Display-P3's authored facts + explicit guide body or absent-state. (2) catalog legibility: BEFORE = 16/18 overflow (worst 758/234); AFTER = 0/18, same color/viewport/fonts. (3) gamut-mark: BEFORE = the HSL row unmarked; AFTER = the SAME coordinate values, digit-budgeted, carrying `data-out-of-gamut="true"` — a delta in which the numbers moved is a projection and fails G6(ii). (4) dot identity: BEFORE distinctVisualSignatures = 1; AFTER = 18. (5) boundary: `(colorSpace: any)` 3 → 0, witnessed by G7's type run, never by grep alone.

**Banks (runnable re-triggers, swept at wave opens).** BANK-1 — producer title/display variant ships: `node -e "const s=require('fs').readFileSync('node_modules/@mkbabb/glass-ui/dist/components/select/SelectTrigger.vue.d.ts','utf8');process.exit(/variant\?:[^;]*title|size\?:[^;]*display/.test(s)?1:0)"` — exits 1 the day it lands, discharging the 4 `:deep()` reaches + 3 BOOKED SWAPs in one edit. BANK-2 — Glass 8.0.0 adoption: `node -e "process.exit(require('./node_modules/@mkbabb/glass-ui/package.json').version.split('.')[0]>=8?1:0)"` — fires re-derivation of the whole scoped block; also the spectrum-slider blur-over-ramp prefix seam stays GLASS-OWNED (cured in 8.0.0) and no row of this corpus re-books it or patches it locally.

**Carries — every row leaves with exactly one disposition, original ids for life.** BUILD here: L-1 (G1/G2/G3), L-2 (G4), L-3-as-rescoped (G5/G6; the projection cure RETIRED with rationale), L-6 (G9), L-9 (G8, seed-only cure), L-12 (G7), L-4's color-session edge (G10), L-8's own site (G8), L-11-in-part (CSS_NATIVE_SPACES + toCSSColorString die with the formatter). FOLD, identity preserved: L-4 → W-HYGIENE demo/ui extirpation (89 edges; identity from the ColorPicker adjudication); L-5(a) → lint-glob liveness wave (born-RED `eslint --print-config` above); L-7 + L-13 + L-11's colorToHexString → **MT-CSS-2** "the operation and the datum get one home" (COLOR_MODEL_KEY splits into role-scoped keys; the cssColor prop deletes, reading the pipeline's alpha-bearing `cssColor`, never CSS_COLOR_KEY = cssColorOpaque; born-RED restated at that wave's open against the post-CSS1 seam); L-8 residue → glass-7 `tag=` sweep (20 sites, list in this record); L-10 → glass-ui BH relay WITH the producer-state correction (text-title is tokenized; re-derive the request against `var(--type-weight-title)`) + BANK-1; L-14 → W-HYGIENE exports↔tsconfig parity (born-RED generator-parity command). REFUTED/RETIRED with written rationale: L-5(b) inverted edge (no import edge; house contract at 6 consumers); L-15 as booked (two concepts sharing a noun; consolidation KISS-banned) — its replacement derivation-rider rides post-MT-CSS-1 with `npx vitest run test/interpolation-subset.test.ts` born-RED.

**completableAlone**: YES. If this is the only wave that ever executes: the product stops stating false color science on a first-class route (the BLOCKER dies), every specimen line becomes a true, bounded, legible statement in a declared grammar, the catalog gap becomes a compile error, the boundary is the domain type, the space-switch command has one home before `/about` lands, and the component's banned lattice edge is gone — zero capability lost, zero dependency on any successor, ten gates each RED today with pasted failing inputs (two runnable this minute via node/vue-tsc, the rest pinned by this seat's static re-derivations + the workers' committed captures).

## Addendum clause (E-3 — appended beside the challenger record, never rewriting it)

**MT-CSS-A, merged from both workers and ratified by this seat's re-derivations.**
1. **L-9.1 — a "sole offender" is a claim about a scope, and the scope is part of the claim** (O's law, premise re-verified by this seat: 1-vs-90 `../ui` edges, 1-vs-21 `tag=` sites, 1-vs-6 hard-injects, a program-wide tsconfig row). Any accusation containing *sole / only / unique to / the one place* must paste the SAME command re-run at repository scope beside the scoped one, and must book the row at the scope where the count lives. Corollary: a wave inherits the scope, not the accusation — a repo-wide condition behind a component-sized gate is an arc wearing a wave's name.
2. **The cure is adjudicated separately from the defect** (O's second corollary, proven decisive here by this seat's own round-trip run): L-3's true fact carried a cure that would have shipped a regression under a green gate. A `proposedCure` inherits no evidential weight from its `defect`'s evidence; every adopting wave states the property the cure could break and gates it in the breaking direction (G6 ii is the template).
3. **Producer-state correction** (F's, verified by this seat): the "semantic.css:132 text-title font-weight:700 UTILITY hardcode" cell is STALE against installed glass-ui 7.0.0 — `dist/styles/typography/semantic.css`'s text-title rides `font-weight: var(--type-weight-title)` (default 700, scale.css). ColorSpaceSelector.vue:291-299's comment asserts the old producer and must be corrected by any wave touching the file; the P10 BOOKED SWAPs and the BH-relay variant request are re-derived against the tokenized producer first. Stale-cell repetition without re-derivation is detectable (see DISSENT) and is itself evidence of citation-inheritance.
4. **Glass boundary**: the SelectTrigger variant cure and any WatercolorDot API question ride the standing glass-ui BH relay (owner edict; INBOX I-20); no local `:deep` expansion, no masking fallback; the spectrum-slider prefix seam remains GLASS-OWNED (8.0.0) and unbooked here.
5. **Identity guards (anti-rename)**: this corpus's L-11 is fact-identical to the ColorPicker adjudication's L-10 (color-model.ts shims); this corpus's L-4 deletion arm is W-HYGIENE's demo/ui row — ONE identity each, folded, never re-booked.
6. **Sequencing**: the L-15 replacement rider (INTERPOLATION_SPACES derivation) may not open before MT-CSS-1 lands `interpolatable`; MT-CSS-2's born-RED must be restated against the post-CSS1 seam (the AboutPane bare-spread mechanic this wave deletes is not the seam MT-CSS-2 will test).
7. **Evidence portability**: specimen strings in this record are byte-pinned to dist at HEAD `c654824e` (re-reproduced by this seat); re-resolve all line anchors before executing any cure (D-19).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (recorded as evidence about the fleet, not about the tree).** Worker-F's dissent item 3 predicted: "if worker-O repeats the 700-hardcode claim verbatim, that is evidence of citation-inheritance, not independent derivation." Worker-O's L-10 note repeats it: "the utility re-pin at :300 fighting glass-ui semantic.css:132's hardcoded `text-title { font-weight: 700 }`". This seat's read of the installed producer settles it: no `dist/semantic.css` exists; the utility is tokenized. O's L-10 evidence cell was inherited from the challenger (and from the component's own stale comment), not re-derived — while O's OTHER corrections on the same row (143-line block census, 4 `:deep(` occurrences) were genuinely its own. The verdict class (GLASS-OWNED) survives on its own merits; the cell did not.
2. **Severity ordering (worker-O, unresolved)**: "I would rank L-2 above L-3-as-rescoped: a caption showing 653px of text in a 234px box defeats the row's entire stated purpose for 16 of 18 rows on every open, whereas the gamut-honesty defect requires a user to act on a number they cannot currently finish reading." Preserved; the wave cures both, so no ruling is required.
3. **Worker-O's recorded self-errors (method lessons, preserved verbatim in substance)**: (a) the L-9 first-pass false refutation — a dot signature including the per-mount namespaced filter url gave distinctDotSignatures=18; dereferencing the ids gave 1 ("my probe read a signature without reading what the signature meant — the MT-F019 signature-before-output failure in a new costume"); G8's oracle explicitly excludes the filter url because of it. (b) The product-level paste round-trip remains unwitnessed (O found only the dock slug input); the round-trip claims in this record are library-level (dist) and are labeled so.
4. **Worker-F's suspicious-agreement disclosure (preserved)**: F confirmed 15/15 of the challenger's rows in some form and disclosed it under the L-14 corollary, offering its two independent corrections (L-5's mislabeled edge, L-10's stale producer cell) as proof of non-inheritance. This seat's ruling record partially vindicates and partially corrects F: both corrections were real and adopted, but three of F's CONFIRMED labels (L-4, L-8, L-14) sat at the wrong scope and one (L-15) did not survive re-derivation — the scope law (addendum item 1) exists precisely because high agreement with a challenger's component-scoped greps is cheap.
5. **Residual disagreement on L-5's intra-file contrast (ruled, but F's position preserved)**: F holds that `inject(SAFE_ACCENT_KEY)!` eight lines from a documented null-tolerant `inject(COLOR_MODEL_KEY, null)` is an incoherent contract worth a row. This seat ruled it a deliberate two-key policy difference (house contract at 6 consumers, provider at every route, the asymmetry explained by the file's own comment) — an observation, not a defect. If a seventh consumer ever mounts outside App.vue's provider, F's row revives with a crash witness.

— end of apotheosis. Original challenger record and both worker returns remain immutable beside this file per E-1/E-3.
