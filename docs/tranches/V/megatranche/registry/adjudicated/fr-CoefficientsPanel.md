# fr-CoefficientsPanel — adjudicated apotheosis (M-23/L-18 fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-08 · **Registry HEAD**: value.js `c654824e` (branch `tranche-u`) · **Subject tree**: fourier-analysis branch `m/w1-bump-migration`, subtree files git-clean per both readers (corroborated by this seat's whole-file reads matching every L/C-axis line cite).
**Corpus**: `docs/tranches/V/megatranche/audit/fourier-components/CoefficientsPanel/` — challenge-D-design.md (392) · challenge-L-library.md (454) · challenge-C-consumption.md (574) — over `web/src/components/visualization/CoefficientsPanel.vue` (26), `web/src/components/shared/CoefficientsSpectrum.vue` (168), `web/src/components/equation/FrequencyGraph.vue` (247) and their chassis/store/e2e seams.
**Readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every reader-vs-reader conflict was re-derived by this seat with its own instruments.
**This seat's own instruments (all static, zero browser probes — probe parsimony; product source READ-ONLY)**: whole-file reads of CoefficientsPanel.vue / CoefficientsSpectrum.vue / FrequencyGraph.vue and `ui/CollapsibleSection.vue`; VisualizationView.vue:255-280 (mount block) and :310-375 (the geometry deciders — `grid-template-columns` tiers at :317-331, the `.viz-panel-left-wrap` release at :361, `.viz-panel-left` gap/padding); installed fourier `node_modules`: `@mkbabb/glass-ui/dist/styles/utilities/a11y-overrides.css:6-16` (the universal PRM rule) + the full import chain `web/src/style.css:3` → `dist/styles/index.css:165` → `dist/styles/utilities.css:19`; `@vue/reactivity` `traverse` (:1979-2009, read verbatim — the deep-watch decider); `reka-ui/dist/Tooltip/TooltipContent.js` (`disableHoverableContent ? Impl : Hoverable` dynamic resolve) + TooltipProvider prop; App.vue:4/:23 (the single provider, no hoverable flag); glass 4.0.0 `dist/TooltipProvider-B3MkB_8P.js` header (thin re-export over reka primitives); **producer glass-ui 7.0.0 by node run**: `package.json` version 7.0.0, **66 export keys, `./animated-digit` ABSENT, `./motion`/`./motion-core`/`./metric` present**; `grep -rn AnimatedDigit src` → **empty**; `src/components/metric/` = Metric/MetricRow/MetricStack only; `src/composables/motion/index.ts:56` exports `useAnimatedNumber`; **repo-scope blast radius**: `grep -rl "animated-digit|AnimatedDigit" web/src` → **exactly one file, CoefficientsSpectrum.vue**; challenge-D read at the disputed anchors (:19-45 D-1, :104-135 D-7/8/9, :170-210 S-5/§5/§6) — this seat's own reads confirm the D-axis line drift the readers charge (spectrumColor call cited :96, live :84; `.scrollbar-thin` cited :230-232, live :236-239) and the D-axis §6 "73 keys / all four survive" cell that this seat's producer run falsifies; banked-identity greps across `registry/adjudicated/` (fr-BasisCanvas M-β1/C-8/L§R5-7, fr-BasisSelector B-3/R-2/m-10/m-22, fr-App MG-β, fr-AnimationControls C-29, fr-AdminFlaggedPanel R3-7a/R5-7 routing law).

## Scoreboard

**46 adjudicated rows: 0 BLOCKER (two demotions ruled) · 15 MAJOR booked + 2 MAJOR folds · 23 MINOR booked + 1 MINOR fold · 3 INFO booked + 2 INFO folds. 16 kills banked (10 DU upheld · 3 LC upheld · 3 minted by this seat against the readers). 15 missed finds adopted after re-proof (8 DU · 7 LC), one with its blast-radius arm amputated.**

Heaviest survivors: **FR-CP-1** (the sole output panel models neither `computing` nor `error` — a failed recompute silently re-presents the previous run as current), **FR-CP-14** (`./animated-digit` does not exist at glass-ui 7.0.0 — a silent F.W1 resolution break the D axis affirmatively certified as SURVIVE), **FR-CP-3** (the coeff-list TransitionGroup is wholly inert by Fragment-root construction).

## Ruled disagreements (the seat's actual work)

| # | question | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **PRM coverage** (D-9) | KILLED (K1): glass ships an app-wide PRM rule | CONFIRMED "no PRM guard anywhere in the subtree" | **DU wins — the PRM arm is DEAD.** | `dist/styles/utilities/a11y-overrides.css:6-16` read verbatim in fourier's installed 4.0.0: `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { … transition-property: opacity, color, background-color, border-color, box-shadow !important } }` — the universal selector, `!important`, reached end-to-end via `style.css:3` → `index.css:165` → `utilities.css:19` (all three hops read). Under PRM the bar's `width` transition, the rows' `translateX(±8px)` and `.coeff-list-move`'s `transform` are all struck from `transition-property`. LC inherited the D-axis falsifier's blind spot (grepping only `web/src/style.css`) — a citation-inheritance event, recorded in DISSENT. The precept-violation arm (`transition-all`, off-token clocks) is untouched and books at FR-CP-25. |
| R-2 | **`deep: true` mechanism** (L-D-8 vs C-13) | K5: `markRaw` stamps only the top object; the walk over ~401 components is real | "Vue's `traverse` short-circuits on `ReactiveFlags.SKIP`… there is no walk" | **DU wins.** | `@vue/reactivity` traverse :1979-2009 read verbatim: the `__v_skip` test applies to **the value handed in**; the watcher source is a fresh array literal whose member `props.components` (= `result.components`) carries no skip flag (`markRaw` stamped `result`, the EpicycleData object, only), and `isArray` → per-element recursion follows. The walk over ~401 plain objects × their fields happens on every trigger; zero deps are collected either way. LC's mechanism cell is KILLED (K-14); LC's conclusion (drop `deep`) survives on DU's mechanism. |
| R-3 | **Aside geometry** (D-24's arithmetic) | CONFIRMED with "no app override → 360px cap → 320px content, 18 of 40 visible" (killed only the `:351-359` rider, K3) | KILLED the whole §9 geometry premise: the app overrides `grid-template-columns` directly, tiered 320/360 · 360/400 · 400/440 | **LC wins; DU's own proof cell is corrected (K-16).** | VisualizationView.vue:317-331 read by this seat: three `@media` tiers override `.viz-configurator`'s columns with the authored comment "aside band tracks the prior 360/400/440px left-panel widths"; :361 releases `.viz-panel-left-wrap`'s 480px cap at ≥1024. DU's grep hunted `--configurator-aside-*` tokens — the wrong artifact. The core overflow SURVIVES at every arm (691px canvas vs ≤ ~400px content) and books at FR-CP-10 with LC's figures; DU's lg-only "18 visible" may not be quoted into a spec. |
| R-4 | **D-1 severity** (rank-ramp incoherence) | MAJOR (K10: both panes label the signed harmonic index — the exclusivity premise is false) | BLOCKER | **DU wins — MAJOR.** | This seat's own reads: FrequencyGraph.vue:106-111 `ctx.fillText(String(comp.index), …)` under every bar (α 0.5, 9px Fira Code); CoefficientsSpectrum.vue:86-88 the same signed index as each row's first cell; both tooltips repeat `n = {index}`. A reader correlating the two panes has an exact shared textual key; hue is a redundant channel there. The epicycle-canvas arm (no textual key) is real but is **the banked identity**: fr-BasisCanvas **M-β1** already adjudicated the four-fork divergence co-visible with the canvas at MAJOR. One identity, one grade. LC's BLOCKER preserved as dissent. |
| R-5 | **D-2 severity** (state coverage) | BLOCKER | MAJOR | **LC wins — MAJOR.** | The lie (stale coefficients presented as current, error swallowed by `Promise.allSettled`, the sole `store.error` surface gated `&& !store.imageSlug`) fires only on a **failure path**, not in the shipped default state — unlike this program's BLOCKER precedents (fr-BasisSelector B-1/B-3, fr-BasisCanvas grey-canvas), which fail at rest. Note the retention itself (`epicycleData` never nulled in the catch) is a defensible keep-last-good policy; the defect is the missing signal. DU's BLOCKER preserved as dissent. |
| R-6 | **glass-subpath survival at 7.0.0** (D-axis S-5/§6) | K2: `./animated-digit` is GONE; the "73 keys, all four survive" cell is false | (not reached) | **DU wins, verified by this seat's own producer run.** | node against `/Users/mkbabb/Programming/glass-ui/package.json`: version 7.0.0, **66** keys, no `./animated-digit`; `grep -rn AnimatedDigit src` → empty; `metric/` holds Metric/MetricRow/MetricStack only. Books at FR-CP-14. **Correction to DU**: the "re-opens the ledger for every other animated-digit consumer in fourier" arm is EMPTY — this seat's repo grep finds exactly one consumer file (CoefficientsSpectrum.vue). Blast radius = 1 file, 1 import, 1 callsite. |
| R-7 | **Wire-or-delete** (the L §5 step 4 vs D/D-5 cure contradiction, flagged unresolved by LC) | — | — | **RULED, two arms split.** | (a) The selection contract (`toggle-harmonic`/`hover-harmonic` emits, `activeIndices`, `cursor-pointer`, `@click`) **deletes**: zero consumers repo-wide (both readers' greps, consistent with this seat's callsite read), a wired version would be new feature scope, and the house law (KISS / no-contrivance / feedback_no_backwards_compat) forbids shipping an affordance with no consumer. `cursor-pointer` dies with it. (b) The `logScale` arm is different in kind: an authored, annotated, already-implemented capability (`:164-171` names both transforms; `:49`'s 0.008 floor is the linear arm's own admission) — whether to wire a control or default it is a **product decision the forming F.W4 spec owns**; whichever arm ships, the LC-missed touch constraint binds (the hit-test must move into the click/pointer handler before ANY interaction survives — `onClick` reads state only `mousemove` writes, silently dead on the touch arm the route supports). |
| R-8 | **Severity ladder demotions** (this seat vs reader-DU) | MAJOR ×2 (collapse-control-in-scroller; side-bottom occlusion) | (not booked) | **MINOR ×2.** | Per the fr-BasisSelector R-6 precedent (no capability loss ⇒ MINOR, live magnitude pending): the buried collapse control is reachable by scroll (ergonomic asymmetry, not loss); the side-bottom occlusion's extent hinges on reka's grace-area polygon, expressly UNPROVEN-NEEDS-LIVE. Both re-open on SS-13 evidence; DU's grades preserved as dissent. Same ladder: LC's MAJOR on the hover-redraw multiplier and on C-6 → MINOR (magnitude unmeasured; latent, low blast). |

## Adjudicated defect roster

Routing law: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5-W8** CRUD/provenance union · **F.W9/W10** · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are forming; these routings are M-25 agglomeration input; nothing here stamps a wave. Line anchors are the subject tree at adjudication time; re-resolve before any cure (the D-19 lesson — and doubly so here, given K9). Identity guards: rows extending banked identities (**fr-BasisCanvas M-β1 · C-8 · L§R5-7; fr-BasisSelector B-3 · R-2 · m-10 · m-22(a); fr-App MG-β; fr-AnimationControls C-29; census/intake R3-10 · R3-7a · R5-7 · R6-6**) **fold by reference — never re-booked**.

### MAJOR band

- **FR-CP-1 = D-2(+A-1) / L-D-3 / C-8 — MAJOR (R-5).** The panel reads 1 of 4 relevant store fields (`CoefficientsPanel.vue:8-10`, this seat's whole read). `runComputeEpicycles`'s catch sets `error` and never nulls `epicycleData` (workspace.ts:301-305, both readers line-by-line); the rejection is swallowed by `Promise.allSettled` (ContourSettings.vue:128-131); the only `store.error` render is gated `&& !store.imageSlug` (VisualizationView.vue:162). Walks: failed FIRST compute → the panel unmounts (guard `:273`); failed RE-compute → stale coefficients under a changed slider with zero staleness signal; the only reachable rendering of the empty copy fires **during** the compute it tells you to run. `n_components` never read. **→ F.W3/W4** (consume `computing`/`error` at the panel + un-gate or re-home the error surface; the store's revision discipline is a credited superlative, S-4 — the omission is entirely the consumer's).
- **FR-CP-2 = D-1 / C-1 / L(add)-A-1 — MAJOR · FOLD → fr-BasisCanvas M-β1** (four `spectrumColor` forks, one ramp home, F.W3/W4 — one identity, anti-rename). What books HERE, as this panel's own composition-seam riders: (a) the **denominator divergence is authored in the 26 lines under review** — `:max-bars="40"` (CoefficientsPanel.vue:20) against the child's collapsed 12 (CoefficientsSpectrum.vue:35-38), so the identical byte-identical function paints the same harmonic two hues 8px apart, and expanding re-colors every row (colour is a property of how many rows are on screen); (b) severity RULED MAJOR (R-4; LC's BLOCKER dissent preserved); (c) cure corrections adopted: DU's K4 (VIZ_COLORS is a five-entry categorical palette, not a ramp — "derive from VIZ_COLORS" is structurally impossible) and LC's kill of L§A1's "one import edge" (importing `visualization/lib`'s gamma fork into `shared/` or `equation/` IS the colocation inversion L-D-5 charges, and drags the equation route onto the visualization module graph). The honest cure is a NEW route-neutral ramp home — FR-CP-15's value.js `sampleColorRamp` or a token-backed ramp — which also carries D-8's surviving arms (theme-invariant `hsl(h,85%,55%)`, non-monotonic luminance under an ordered quantity, no CVD-safe redundancy, the light-mode 55°–110° thin band; final ratios SS-13). **→ F.W3/W4**, cross-ref F.W1 (inv-33 substrate).
- **FR-CP-3 = D-22 / L-D-1 / C-5 — MAJOR.** The `coeff-list` TransitionGroup is wholly inert: the `v-for` child's terminal root is reka `PopperRoot`'s `renderSlot` Fragment; `isElementRoot` fails; hooks land on a vnode `processFragment` never animates; FLIP separately excluded by the `child.el instanceof Element` gate. Five dead CSS rules (`:147-163`) + one dev warning per row. Chain verified by both readers link-for-link with matching Vue line cites; LC adds the nuance that component roots propagate silently so the warn fires only at the PopperRoot boundary. **Anti-cure lock adopted (DU K6)**: `renderSlot` mints the Fragment **unconditionally** — collapsing the shim to one child does NOT fix it; the only cures are keying the `v-for` on the `.coeff-row` element itself (Tooltip inside) or an element-root primitive. L/A-4's `MetricStack as=TransitionGroup` re-seat is a live candidate, with LC's overclaim note (the subgrid contract is layout convention, not construction — a re-wrapped Fragment child breaks it silently). **→ F.W3/W4** (FR-CP-30's key fix rides the same cure).
- **FR-CP-4 = D-3 — MAJOR.** The docblock's a11y-discharge claim (`:11-15` "discharging the L5 §5 A8 LOW a11y gap") is false: the tooltip trigger chain contributes **no tabindex anywhere** (reka's trigger injects listeners + `aria-describedby` only — both readers' independent greps; glass 4.0.0's trigger is a bare pass-through, K8), and the `as-child` child is a plain `<div class="coeff-row">` with `cursor: default`. Four of six data columns (4-dp amplitude, phase, relative %, Re/Im) are keyboard/AT-unreachable; the canvas half is a bare `<canvas>` with no role, name, tabindex or fallback (this seat's whole read, :177-183). **Mechanism correction is load-bearing (K8)**: any repair written against the D-axis' cited `as="button"` + conditional-`type` glass trigger would target a shape 4.0.0 does not ship. **→ F.W3/W4** (focusable rows or a visible detail surface + canvas role/name; pairs with FR-CP-9's dead gates).
- **FR-CP-5 = D-4 — MAJOR.** At the ^4.0.0 pin the disclosure chevron snaps: glass's scoped `transition: transform` cannot cover the `rotate` **longhand** Tailwind v4 emits for `rotate-180` (compiled chevron class + scoped rule + the Tailwind compiler's `rotate` property emission — all three artifacts read by both readers; LC read the compiler at its installed 4.3.1 offsets). Cured at 7.0.0 by `@utility transition-disclosure` over `rotate` (producer btn.css:82-84, applied ConfiguratorLayer.vue:147 — DU's clock/line cites corrected: `--spring-present-duration`, `:147` not `:139`). **→ F.W1** (discharged by the uplift; consumer no-op; verify at bump).
- **FR-CP-6 = D-5 / L-D-2 / C-3 (+ LC-missed touch-dead click) — MAJOR.** The sole callsite binds 2 of 6 inputs and 0 of 2 emits (CoefficientsPanel.vue:17-22, this seat's read); `isActive` unconditionally true makes both dim arms unreachable; `logScale:false` strands the log arm, the axis label's log branch, the tooltip's log row, and makes `:49`'s 0.008 floor the linear arm's confession; `cursor-pointer` + hover glow promise a selection with no listener anywhere in the repo. LC-missed adopted after this seat's read: `onClick` never hit-tests — it reads `hoveredBar`, which only `mousemove` writes, so even a wired emit is silently dead on touch (the route ships a mobile arm). **Disposition per R-7**: selection contract deletes; `logScale` wiring is the forming spec's decision under the touch constraint. **→ F.W3/W4**.
- **FR-CP-7 = D-6 / L-D-11 — MAJOR.** "Show more (401 total)" delivers 40; the slice caps at 40 even expanded (`:37-39`); the readout then reads 40/401 with no further affordance; the 40 is authored twice across a component boundary with no shared constant (`:max-bars="40"` / the slice literal). Default ~401 components (n_harmonics 200, UI-raisable to 500 → ~1001). **→ F.W3/W4** (one constant, one honest affordance — pagination/virtualization or an honest cap).
- **FR-CP-8 = D-7 / L-D-12 (+ LC-missed CAUSE: the half-applied B.W2.c lift) — MAJOR.** The canvas tooltip is an absolutely-positioned in-flow child of the `overflow-x-auto` port that is also its containing block (`.scrollbar-thin { position: relative }`, :236-239); near x=0 its left half lands at negative content coordinates LTR overflow cannot expose — and the clipped rows are the LARGEST harmonics. z-index cannot escape an overflow clip. **The adopted cause**: FrequencyGraph:193-209 is a hand-rolled twin of the tooltip CoefficientsSpectrum:105-120 already lifted to the glass primitive at B.W2.c (same swatch, heading, grid, 4-dp cells; even `formatPhase` duplicated inline) — one of the two got the portal + collision padding for free, the other did not. **The repair is a deletion, not a positioning fix.** Trailing-edge scrollbar jitter + exact clipped-bar count → SS-13. **→ F.W3/W4**.
- **FR-CP-9 = D-10 / L(add)-A-2 / C-4 — FOLD → fr-BasisSelector B-3 + R-2** (three axe keystones `test.fixme` on the stale `^2.0.0`/"omits inert" premise, doubly false against installed 4.0.0's `inert` emission + trigger-outside-region; plus visualization-crud:630, only :150 live) **and fr-AnimationControls C-29**. This corpus adds only color: this panel is the largest named collapsed sibling the dead keystones would cover (12–40 tooltip roots + a canvas + the one real Button). No re-book; un-fixme rides B-3's cure. What an un-fixme'd run would hit instead (FR-CP-27) → residue.
- **FR-CP-10 = D-24-as-corrected — MAJOR (R-3).** `:max-bars="40"` derives a 691px canvas (40×17+3+8, constants :25-26/:31-34, this seat's read) inside an aside granting ~320–400px of content across the 360/400/440 tiers (LC's corrected geometry, verified by this seat at VisualizationView.vue:317-331 minus padding) — 18–22 of 40 bars visible behind a 4px scrollbar as the only affordance, while glass ships `./fading-scroll` at both pins and the enclosing Configurator uses it. F.W1 rider: 7.0.0's body padding widens 12→20px, worsening the arithmetic (D-axis §6, credited). **→ F.W3/W4** (+F.W1 sequencing note).
- **FR-CP-11 = D-25 — MAJOR · producer-owned → glass-ui BH relay** (standing owner edict). `sub` renders `text-micro font-mono text-muted-foreground/70` — 11px at α=0.70, ≈2.85:1 light / ≈3.46:1 dark against the card plate (DU re-derived the light arm independently to 3 s.f.; the alpha is the primitive's, consumer-unreachable; unchanged at 7.0.0). Backdrop composite (glass plate vs flat `--card`) → SS-13. Consumer lever = don't pass `sub` — which FR-CP-12 orders anyway. **Relay row; no local patch.**
- **FR-CP-12 = D-26 / D-29 / D-30 — MAJOR, scoped.** The charge is NOT the closed default (the D axis' own S-3 credits the disclosure as right for a 401-term spectrum in an aside): it is that the column's only OUTPUT panel hides behind a header that reports **nothing** — a `sub` restating the label ("Coefficients"/"Fourier spectrum") where both siblings' subs name contents; and an empty copy naming an action the panel cannot offer, in vocabulary diverging from its own twin (which uses the child's own default string). Cure shape: a header that carries a datum (count/summary) — the header-actions producer ask is **banked at fr-BasisSelector m-10** (fold by reference; BH relay already carries it). **→ F.W3/W4**.
- **FR-CP-13 = D-23 — MAJOR (F.W1 break-risk, static facts confirmed).** 7.0.0's adjacent-sibling fusing pair (`configurator/styles.css:126-135`, read verbatim by LC) assumes touching layers; `.viz-panel-left` gaps them 12px — the uplift as-is yields flat-ended rectangles floating in air, invisible to keyset diffs and typecheck. One gap decision must be made once, jointly with FR-CP-24's hairline row. Visual outcome → SS-13 (post-uplift). **→ F.W1** (the uplift wave removes the gap or opts out of fusing in the same commit).
- **FR-CP-14 = DU-missed-1 — MAJOR, verified by this seat's own producer run (R-6).** `import { AnimatedDigit } from "@mkbabb/glass-ui/animated-digit"` (CoefficientsSpectrum.vue:19, used :99-103) is a **hard resolution failure at the F.W1 uplift**: 7.0.0 has 66 export keys, no `./animated-digit`, no AnimatedDigit source, and the removal is CHANGELOG-silent (the consolidation table books only the metric-* rows). Blast radius corrected: **exactly one consumer file repo-wide** (this seat's grep). Cure is one import: `useAnimatedNumber` survives at `./motion` (producer index.ts:56, verified) + a `tabular-nums` span, or adopt `./metric`/MetricStack (which also serves FR-CP-3). Kills the D-axis S-5/§6 SURVIVE certification (K2). Same break class as **fr-App MG-β** (silent 7.0.0 removals) — cross-booked into that F.W1 census gate, distinct row. **→ F.W1**.
- **FR-CP-15 = C-2 — MAJOR.** `@mkbabb/value.js@0.13.0` is installed and declared; the subtree consumes zero of it while hand-rolling the N-stop hue arc four times in raw sRGB; `sampleColorRamp` (OKLCH, gamut-mapped, hue-method-aware) ships in the installed dist. **Identity checked**: distinct from fr-BasisCanvas C-8 (the glass↔value.js peer-range row); this is the consumption gap. **→ F.W3/W4** as FR-CP-2's cure substrate, cross-ref the F.W1 tri-package charter.
- **FR-CP-16 = C-17 / L/A-3 (+ LC-missed enforcement funnel) — MAJOR.** Every number in the panel divides by `components[0].amplitude`; the descending-amplitude invariant is enforced only in Python (`epicycles.py:54`, sort cite corrected from L's :56), expressed nowhere in TypeScript, and the whole 45-operation client surface lands as one unchecked `(await res.json()) as T` (api.ts:192); `n_components` is declared and never read; IndexedDB draft rehydration (`workspace.ts:171`) replays older payloads through the same zero checking. A violation surfaces as `NaN%` bar widths, not an error. **→ F.W5-W8** (the enforcement point — validate-at-boundary for the epicycle payload at minimum) + **F.W4** (the ordering annotation/brand + read `n_components`).
- **FR-CP-17 = L-D-4 / C-10 — MAJOR.** `:default-open="false"` defers nothing: ConfiguratorLayer's reveal is CSS-only (`0fr ↔ 1fr`) over an unconditionally rendered slot, so 12–40 rows, per-row spring animators and the 40-bar canvas draw all run behind a zero-height inert region on every recompute; `onMounted(() => draw())` and the props watcher have no visibility predicate while the house discipline (BasisCanvas's IntersectionObserver → animation-store gate) sits one directory away. **Inventory correction adopted (DU K7)**: reka's `TooltipContent` is `<Presence>`-gated — no content/floating-ui/portal cost until hover; the eager per-row cost is Root→Popper→Anchor→Primitive + an empty Portal/Presence. Magnitude → SS-13. **→ F.W3/W4**.

### MINOR band

- **FR-CP-18 = D-13 / C-9 — MINOR.** The `n / N` readout sits outside the emptiness guard: the reachable empty state renders `0 / 0` right-aligned above the centred empty sentence; no accessible name in any state. → **F.W3/W4**.
- **FR-CP-19 = D-15 — MINOR.** 20px row pitch (16px `text-xs` line box + 4px `space-y-1`) for tooltip-bearing targets — under WCAG 2.2 SC 2.5.8's 24px, spacing exception also failed. → **F.W3/W4** (rides FR-CP-4's row rework).
- **FR-CP-20 = D-16 — MINOR.** A bare `pt-1` whose sibling `space-y-2` never applies + two `mb-2`s — a 4px orphan on an 8px grid, 12-above/8-below asymmetry in a symmetric section. Sequence AFTER the F.W1 pad change (12→20px re-tunes the whole rhythm). → **F.W3/W4**.
- **FR-CP-21 = D-18 + DU-missed-7 — MINOR.** Both tooltip data grids set the panel's most precise numerals in `text-admin-label` — and the utility imposes the FULL admin register (uppercase, 0.1em caps tracking, `line-height:1`, mono, 10px), on the only numerals in the panel WITHOUT `tabular-nums`; precision and size inverted vs the 12px row cells. **Cross-booked → fr-App MG-β**: the utility is GONE at 7.0.0, so the repair must target a surviving rung (`--type-micro`/`--type-caption` + `tabular-nums`), reconciled inside MG-β's F.W1 census gate. → **F.W3/W4** riding **F.W1**.
- **FR-CP-22 = D-19 / C-12 / L-D-10 — MINOR.** The `v-if` sits on the Button inside an ungated `<Tooltip>` — a popper tree mounts around a comment node whenever total ≤ 12; and the copy promises "Show top 40" for totals 13..39 (`Math.min(40, total)` is the honest string). C-12's "outside reka's contract" mechanism half KILLED (LC; the D axis self-killed the same hypothesis — `Slot.js` handles the all-comment case cleanly); pure waste survives. → **F.W3/W4**.
- **FR-CP-23 = D-20 — MINOR.** `font-family: "EB Garamond", …` + a comment asserting a family the repo does not ship (7 `@font-face` rules: Computer Modern Serif / Fira Code / Fraunces; Garamond greps to this one file). The comment is factually false; the fallback is correct by accident. → **F.W3/W4**.
- **FR-CP-24 = D-28 — MINOR.** `last:border-b-0` is `:last-child`; this panel's conditional mount toggles ContourSettings' bottom hairline as a function of whether a compute has run (Transition renders no element; comment placeholders don't satisfy `:last-child` — mount block verified by this seat at :262-275); compounded by `gap: 0.75rem` terminating every `border-b` in 12px of air. One gap decision jointly with FR-CP-13. → **F.W3/W4**.
- **FR-CP-25 = D-17 / L-D-17 — MINOR (PRM arm DEAD, R-1).** The amplitude bar's `transition-all duration-500 ease-out` (animating `width` AND the D-1 hue crossfade) and `.coeff-list-move`'s raw `0.3s ease` sit ten lines from the scoped block's own precept comment ("named properties + canonical tokens, no `transition: all`") whose other rules obey it with a resolving `var(--ease-standard)`. The violation is legible only because the rule was written down — which is the point of writing it down. → **F.W3/W4**.
- **FR-CP-26 = D-12 — FOLD → fr-BasisSelector m-22(a)** (prose in the documented token-reference `sub` slot, systemic 3/3 convention, producer ask rider already banked). No re-book; the local instance dies with FR-CP-12's sub replacement.
- **FR-CP-27 = D-14 — MINOR · producer-owned → glass-ui BH relay.** `role="region"` with no `aria-labelledby`/`aria-label` (compiled dynamic-prop array is exactly `["id","aria-hidden","inert","data-state"]`), bare `<button>` trigger outside any heading — at BOTH pins. Not consumer-fixable; a live candidate for what un-fixme'd keystones hit next (residue). **Relay row.**
- **FR-CP-28 = L-D-5 / C-14 — MINOR (DU's MAJOR-library preserved as dissent).** Colocation inversion: the 247-line canvas lives under `components/equation/` with its only consumer in `components/visualization/`; `components/shared/` holds one file; the census (lane-frontend:133) mis-attributes the render path. → **F.W3/W4** (re-file with the FR-CP-8/FR-CP-6 edits) + census correction noted for SS-3/SS-4.
- **FR-CP-29 = L-D-8 / C-13 — MINOR (mechanism per R-2).** `{ deep: true }` registers zero deps on this route while genuinely walking ~401 plain objects per trigger (traverse read verbatim by this seat). Drop `deep`. → **F.W3/W4**.
- **FR-CP-30 = L-D-9 — MINOR.** `:key="`${comp.index}-${i}`"` fuses stable identity with array position — a harmonic that moves is destroyed and recreated, the exact case a TransitionGroup exists to animate. `comp.index` alone is unique. Rides FR-CP-3's cure. → **F.W3/W4**.
- **FR-CP-31 = L-D-13 — MINOR.** Both children divide by a maximum never proven non-zero (`NaN%` width / `Infinity` barH on an all-zero spectrum) while the same file half-guards it in `formatPercent`. Thin reachability, stated honestly by the corpus. → **F.W3/W4**.
- **FR-CP-32 = L-D-14 — MINOR.** Unused `onUnmounted` import, a guard-only local, an unreachable `|| "#888"` — invisible because `noUnusedLocals` is off and there is no unit-test runner (tsconfig cells banked at fr-BasisCanvas/fr-BasisSelector instruments; `e2e/**` outside `include` is LC's addendum). → **F.W3/W4** locals + **F.W0** substrate (toolchain flags).
- **FR-CP-33 = C-6 / D-27 / L(add)-S-6 rider — MINOR (LC's MAJOR preserved as dissent).** The twin's chassis (`CollapsibleSection`, read whole by this seat): root-barrel glass import defeating the ~60-subpath split (:2), an uncleared 250ms `setTimeout` in a watcher that can fire against a detached element (:17-30, no `clearTimeout` in file), composing the exact `Collapsible`+`CollapsibleContent` pair ConfiguratorLayer's docblock records fleeing (the F-ε-3 recursion history). One shared body, two disclosure chassis. → **F.W3/W4** (converge the twins on one chassis; the import hygiene arm rides **F.W1**).
- **FR-CP-34 = C-7 / L-D-15 — MINOR (DU's MAJOR preserved as dissent).** Zero props, zero emits, store-coupled — while the twin takes `components` as a prop and the parent already reads the same store field at the mount guard. One-prop fix; blocks isolated mounting, not the shipped route. → **F.W3/W4**.
- **FR-CP-35 = D-31 / L-D-7 / C-11 + LC-missed hover-multiplier — MINOR (LC's MAJOR preserved; magnitude → SS-13).** The repo's only canvas with no clock, no observer, no theme subscription: DPR + theme colour sampled at draw; `getComputedStyle` per bar INSIDE the loop (:75-112, :108); `draw()` unconditionally resets the backing store (:58-61) and runs synchronously in `mousemove` on every bar/gutter transition (~a full repaint + 40 forced style reads per ~8.5px of pointer travel — this seat re-derived the trigger structure from :132-141); the house alternative (`useCanvasSetup`: sizing/drawing split + ResizeObserver) is colocated and unapplied; ConvergencePlot's rAF coalescing is the in-repo pattern. INFO rider (LC): `getContext("2d")!` with no context-loss posture, family-wide idiom. → **F.W3/W4** (hoist colour, split size/draw, rAF-coalesce hover).
- **FR-CP-36 = DU-missed-2 — MINOR (demoted per R-8).** The collapse affordance is buried inside the 300px scroller its own expansion fills: collapsed content ≈276px (fits), expanded ≈836px puts "Show less" ~530px below the fold of a box the user must scroll to the bottom of; the count readout was correctly placed outside, the control was not. Geometry re-derived by this seat from :78-136 (the Button is inside the `overflow-y-auto` div; closing tag :136). One-line fix: move the control beside the readout. → **F.W3/W4**.
- **FR-CP-37 = DU-missed-3 — MINOR (demoted per R-8; extent → SS-13).** `side="bottom"` + reka's default HOVERABLE content (this seat verified the `disableHoverableContent ? Impl : Hoverable` resolve, the provider default, and that App.vue:23 + the shim set no flag): each open tooltip (~85-95px) covers ~4 subsequent 20px rows and receives their pointermove — downward scanning parks on the tooltip instead of advancing. `side="right"` or `disable-hoverable-content` is the shape; grace-area behavior decides the re-grade. → **F.W3/W4**.
- **FR-CP-38 = DU-missed-4 + DU-missed-5 — MINOR.** Canvas hover ergonomics, both verified in this seat's whole read: (a) `hitTest` returns null in the 3px gutters, so a horizontal sweep tears down and rebuilds the tooltip ~39 times (17px cycle) with two extra full redraws per bar — nearest-bar clamp is the same line count; (b) `tooltipPos` is assigned only on bar ENTRY (:134-139), i.e. at a bar EDGE, so the panel centres a full `BAR_W` apart depending on sweep direction and never on the bar it describes — anchor to the bar centre. (DU's discarded vertical-clip hypothesis recorded: the −56 offset is correctly tuned.) → **F.W3/W4**.
- **FR-CP-39 = DU-missed-6 — MINOR.** The expand control is a disclosure with no `aria-expanded`/`aria-controls`/target `id`, its state carried by an `aria-hidden` glyph swap — while the house pattern (ConfiguratorLayer's compiled `["aria-expanded","aria-controls","data-state"]` trigger) sits directly above it in the DOM. The one focusable element in the panel body is the one that does not announce what it does. → **F.W3/W4**.
- **FR-CP-40 = LC-missed-3 — MINOR.** `hoveredBar` is a raw ordinal that survives a recompute: the props watcher redraws but never resets it, and the server re-sorts every run — a stationary cursor's tooltip and glow silently describe a DIFFERENT harmonic after recompute (shrink case guarded, so no crash and no gate sees it). One line in the watcher. → **F.W3/W4**.
- **FR-CP-41 = LC-missed-4 — MINOR.** `expanded` is never reconciled with the data: expand at 401, lower `n_harmonics` to ≤13 → the toggle unmounts with `expanded` stranded true and no path back to the 12-row default; raise it again and the panel silently returns 40 rows. Compounds FR-CP-2's denominator flip. → **F.W3/W4**.
- **FR-CP-42 = D-axis §6 lucide row — FOLD → census/intake R3-10** (`lucide-vue-next` → `@lucide/vue`, ChevronDown/Up at :20/:132, one of the 35 booked rename sites; dynamic-`:is` family). **F.W1**, no re-book.

### INFO band

- **FR-CP-43 = L-D-16 — INFO · FOLD → R5-7/R6-6 + fr-BasisCanvas L§R5-7**, which explicitly demanded FrequencyGraph be checked before publishing any denominator — this row completes it: the subtree's one `v-for` is component-hosted while the canvas renders and hit-tests 40 repeated units in two imperative loops with no template node to key. Any instance/loop census reads 1 where there are 3. **NO-WAVE-OWNER** (SS-3/SS-4 spec-authoring input).
- **FR-CP-44 = L/A-4 — INFO.** lane-frontend:443 seats the `./metric-stack → ./metric` shadow candidacy on the two 26/17-line hosts, neither of which renders a metric; the tree says it belongs on CoefficientsSpectrum.vue (whose row anatomy matches the MetricStack docblock almost line for line). Census correction; the re-seat is FR-CP-3's candidate cure, with the "by construction" overclaim already noted. **NO-WAVE-OWNER** (census correction) + F.W3 candidate.
- **FR-CP-45 = DU-missed-8 / K9 — INFO (corpus-quality, spec-authoring law).** The D axis is the only one of the three whose line citations do not hold against the clean tree (systematic ≈−10/−2 drift in FrequencyGraph plus the K2/K8 assertion-level inversions); the L and C axes cite accurately at every line this seat checked (and its own whole-file reads concur). **Law for SS-3/SS-4: take D's CLAIMS and L/C's LINES; re-resolve every anchor before quoting into a wave spec.** **NO-WAVE-OWNER**.

## Killed-claims register

DU's ten kills (K1–K10) are **all UPHELD**, four with this seat's own re-derivation: **K1** (PRM — a11y-overrides.css + full import chain, R-1), **K2** (animated-digit — producer node run, R-6), **K3** (the `:361` release — read), **K10** (the D-1 exclusivity premise — fillText + index cells read; upheld as a severity kill only, the incoherence books at FR-CP-2). K4 (VIZ_COLORS not a ramp), K5 (traverse, R-2), K6 (renderSlot unconditional Fragment — the anti-cure lock), K7 (Presence-gated content), K8 (4.0.0 trigger bare pass-through — corroborated by this seat's chunk-header read: the chunk thin-wraps reka primitives directly), K9 (D-axis drift — corroborated against this seat's own reads) adopted as recorded. LC's four kills: **K11** = the §9 geometry premise (UPHELD, this seat's read of :317-331 — and extended, see K-16), **K12** = C-12's "outside reka's contract" mechanism (UPHELD; the D axis self-killed the same hypothesis), **K13** = L§A1's "one import edge" cure (UPHELD — the cure would commit the colocation inversion its own axis charges; the cost delta is what decides whether the ramp row discharges in one wave).

**This seat's three fresh kills, minted against the readers:**

- **K-14 — reader-LC's PRM confirmation arm** (the "no blanket kill / not in the 12-file PRM list" cell inside its D-17/D-9 row): REFUTED by the installed `a11y-overrides.css:6-16` universal rule reached via the verified three-hop import chain. LC repeated the D-axis falsifier's file-scoped grep instead of walking the imported cascade — while its neighbouring cells on the same row (the `respectReducedMotion` chunk extraction) were genuinely its own. The row survives K-14 as FR-CP-25 (precept violation only).
- **K-15 — reader-LC's deep-watch mechanism cell** ("traverse short-circuits on ReactiveFlags.SKIP… no in-place mutation deep exists to catch"): REFUTED by the traverse source read (R-2). The skip flag lives on the EpicycleData object, not on `.components`; the ~401-object walk is real. The conclusion (drop `deep`) survives on DU's mechanism; the cell does not — and it contradicted DU's K5 on the page facing it, which is why arbitration read the source.
- **K-16 — reader-DU's D-24 geometry proof cell** ("the compiled aside track is `minmax(280px,360px)` with no app override → 320px content → 18 of 40 visible"): REFUTED by VisualizationView.vue:317-331 — the app overrides `grid-template-columns` directly, tiered to 440px at 2xl. DU killed the `:351-359` rider via `:361` (K3) yet confirmed its own arithmetic without reading 30 lines further up — the partial-read hazard in miniature. The finding survives as FR-CP-10 with LC's figures; DU's arithmetic may not be quoted.

**Severity kills (rulings, not fact kills)**: LC's BLOCKER on D-1 (R-4) · DU's BLOCKER on D-2 (R-5) · DU's MAJOR ×2 on missed-2/missed-3 (R-8) · LC's MAJOR on the hover-multiplier and C-6 (R-8 ladder). All preserved as dissent; each re-opens on SS-13 evidence.

## Superlatives (with provenance)

- **S-1 — the `#graph` slot is the right joint** (both readers independently, falsifiers applied; this seat's whole read concurs: no route sniff, no boolean, the equation route never carries the 247-line canvas). The extraction's docblock names both consumers and quantifies the prior duplication.
- **S-2 — the collapsed region is genuinely `inert`, not merely clipped** (DU; same evidence banked at fr-BasisSelector B-3's premise-falsification): zero phantom tab stops for up to 40 rows + a Button + a canvas — the same fact that charges FR-CP-9's dead gates earns the primitive its credit.
- **S-3 — the reduced-motion division of labour is correct by construction** (DU's K1 corollary, upheld by this seat's own cascade walk): glass's universal PRM rule strips what CSS can reach; the rAF-driven numeric damp — which CSS cannot see — is exactly the surface routed through `useAnimatedNumber`, whose `respectReducedMotion` default this callsite cannot even disable (`AnimatedDigitProps` does not expose the flag).
- **S-4 — revision + abort discipline on the one data seam the panel stands on** (both): `rev` taken before the await, re-checked before the write; sibling path bumps counters and aborts in-flight; the split counters carry their own authored bug rationale. A stale RESPONSE cannot repaint this panel — which is precisely why FR-CP-1 is a consumer omission, not a store race.
- **S-5 — zero teardown obligations across the 441-line subtree, by enumeration** (both): every listener template-bound; AnimatedDigit's springs self-dispose under scope; the store's timer cleared under `onScopeDispose` — sharpened by the file importing `onUnmounted` and not needing it, and contrasted by the twin chassis's uncleared timer (FR-CP-33).
- **S-6 — tabular + fixed-width numerics, paired deliberately** (both): damped numerals walk many intermediate strings; proportional figures would jitter 40 right-aligned columns per frame; the fixed cells are safe at the reachable range.
- **S-7 — the axis annotation is scholarly and honest in both arms** (both): the transform is named in the UI, the `+1` convention explained, and the label matches `barFraction` exactly — degenerate only because the log arm is unwired (FR-CP-6), a wiring fault, not an annotation fault.
- **S-8 — one app-level `TooltipProvider` makes 12–40 per-row roots viable** (LC's find, verified by this seat at App.vue:4/:23): a single delay group means row-to-row movement uses the 200ms skip-delay instead of forty independent 400ms stopwatches. Got right once, at the root, for free.
- **S-9 — the canvas sizes from DATA, not a measured container** (LC, verified: `draw()` reads no DOM geometry) — the only reason mounting inside a `0fr` region does not produce the classic blank canvas; the DPR ceremony (backing store scale, CSS pin, `setTransform`, logical-unit clear) is correct in a file that gets several other things wrong.
- **S-10 — the precept comments are load-bearing** (DU): the motion rule written at :146 is what makes :91 and :161-163 legible as violations. Writing the rule down beside the code is the point.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. FR-CP-8: exact clipped-bar count at the leading edge + trailing-edge scrollbar-extent jitter (tooltip width content-dependent).
2. FR-CP-2/FR-CP-11: the effective backdrop behind the ConfiguratorLayer body (flat `--card` vs translucent glass plate) — sets the final ramp-swatch and `sub`-slot contrast ratios (DU re-derived both arithmetics soundly; the compositing target is the open question).
3. D-11 (uncharged pending): wheel-trap behavior of the inner `max-h-[300px]` box vs the Configurator's own scroller — with LC's correction that the corpus's "~360px" content figure understates (~836px expanded per FR-CP-36's geometry), which strengthens whatever the observation shows.
4. D-21/A-2: reachability of |amplitude| ≥ 1e5 with real contour coordinates (the `w-16` cell's overflow regime).
5. FR-CP-3: dev-console warning count per open (≥12 collapsed / ≥40 expanded) + visual absence of enter/leave/move — mechanism settled statically, observation not.
6. FR-CP-17 + FR-CP-35: measured frame cost — the invisible subtree per recompute, and the hover-path repaint multiplier (decides LC's MAJOR dissent).
7. FR-CP-9 residue: whether an un-fixme'd axe run is clean or trips FR-CP-27's unnamed `role="region"` (changes the remedy, not the finding).
8. FR-CP-35 rider: whether `getComputedStyle(canvas).getPropertyValue("color")` under Tailwind v4 `oklch` tokens serialises to a `fillStyle`-parseable string — if not, index labels silently inherit the previous bar's spectrum colour (a worse defect than the staleness charged).
9. FR-CP-35: perceptibility of the stale raster after DPR/theme flip.
10. FR-CP-37: occlusion extent + reka grace-area polygon behavior (decides the R-8 re-grade).
11. FR-CP-13: the fused-card visual outcome at the F.W1 uplift (CSS facts confirmed; pixels post-uplift).
12. LC's honesty notes carried: the third-column hue-delta figures are conditional on `nVis = 40` (a runtime parameter — SS-13 should read the actual value); the ~401-component denominator is inferred from 2N+1, not counted from a response (±1–2 moves no finding).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance events (evidence about the fleet, not the tree).** (a) Reader-LC's PRM confirmation (K-14) repeated the D-axis falsifier's file-scoped grep — the same failure shape as fr-BasisSelector DISSENT-2 (extending a true neighbouring fact past what was read); LC's adjacent cells on the same row were original and correct. (b) Reader-LC's traverse cell (K-15) restated C-13's mechanism while its own conclusion sat on L-D-8's contrary one. (c) Reader-DU confirmed D-24 on a premise its co-reader refuted from the same file DU had partially read (K-16). The pattern across three events: the kill discipline was excellent at the readers' seats and still needed arbitration wherever a falsifier was inherited rather than re-run.
2. **Severity dissents preserved**: LC's BLOCKER on the rank-ramp (R-4); DU's BLOCKER on state coverage (R-5); DU's MAJOR on FR-CP-36/37 and LC's MAJOR on the hover-multiplier and FR-CP-33 (R-8). Each re-opens on the named SS-13 evidence.
3. **DU's amputated arm**: "re-opens the F.W1 ledger for every other `animated-digit` consumer in fourier" — the set is empty (this seat's grep: one consumer file). The break stands (FR-CP-14); the fleet-wide arm dies.
4. **The R-7 ruling's minority position**: the D axis holds that the log arm is the CORRECT default (the linear arm hides the O(1/n) tail as a sliver band, the 0.008 floor being the admission) — preserved so the F.W4 spec inherits the argument, not just the disposition.
5. **Uncharged observation carried from the D axis' own §5**: the panel heading resolves to Plus Jakarta Sans in a Computer-Modern-Serif treatise (an app-wide token-override decision; the D axis names it the single highest-leverage F.W1 one-liner). Not a row; belongs in the F.W1 spec's notes.

## Closing verdict

**ADJUDICATED — 0 BLOCKER after two ruled demotions (R-4 via the verified textual-key falsifier + the banked M-β1 grade; R-5 as failure-path-conditional); 15 MAJOR + 23 MINOR + 3 INFO booked, 5 identities folded by reference (fr-BasisCanvas M-β1 · fr-BasisSelector B-3/m-22(a) · fr-App MG-β cross-book · census R3-10 · R5-7/R6-6), never re-booked; 16 kills banked of which 3 minted by this seat against the readers (K-14 PRM · K-15 traverse · K-16 geometry — each an inherited-falsifier event); 15 missed finds adopted after re-proof, the sharpest being the silent `./animated-digit` F.W1 resolution break this seat re-proved against the producer with a corrected one-file blast radius; routings are M-25 agglomeration input for the forming X·F specs — F.W1 owns the uplift-break cluster (FR-CP-5/13/14/42 + the MG-β and fusing riders), F.W3/W4 owns the frontend truth/a11y/motion cluster, F.W5-W8 owns the wire-contract enforcement row, the glass BH relay carries the two producer rows (FR-CP-11/27), NO-WAVE-OWNER carries the census corrections; nothing here stamps a wave.**

— end of apotheosis. The three challenge axes and both reader returns remain immutable beside this file per E-1/E-3.
