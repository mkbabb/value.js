# CurrentPaletteEditor — adjudicated apotheosis · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Seat**: fresh Fable adjudicator (M-23/L-18 apotheosis seat). **Date**: 2026-08-04 · **HEAD**: `2e334231` · branch `tranche-u`.
**Corpus**: `audit/components/CurrentPaletteEditor/` — three canonical challenge files (C 22 rows across 4 passes · D 4+27+?+? rows across 4 passes · L 25 rows), 30+ probe artifacts including an **orphaned pass 5** (probe-D14…D18-pass5, probe-P5-1…P5-4, frames-D14, all 2026-07-29, no report ever written from them). Registry: all three canonical reports EXISTS-ORIGINAL in `registry/HYDRATION-LEDGER.md:66-68` (area-palettes.json, 4 payloads each) — nothing here is REPORT-AUTHORED.
**Workers adjudicated**: hostile re-reader design-and-usage (`currentpaletteeditor-l18-hostile-reread-design-usage`) · hostile re-reader library-and-code (`L-18-hostile-reread-CurrentPaletteEditor-library-code`).
**Method**: both returns were attacked before either was adopted. The one true verdict-level conflict between them — **D4-01 arm (a)**, which reader-1 kept standing and reader-2 killed — was re-derived by this seat from source and **ruled for reader-2** (see K-2). Every kill was re-proven or re-scoped with this seat's own instruments; three reader claims were corrected or narrowed by this seat (K-12, the M1 phrasing, the L-14 precedent).
**Arbiter's own instruments (all static + probe-JSON reads; zero browser tooling — probe parsimony + task law)**: full source reads of CurrentPaletteEditor.vue (whole, 312 lines), SwatchHoverMenu.vue (whole), useSwatchActions.ts (whole), usePaletteActions.ts (whole), usePaletteStore.ts:40-114, useColorPipeline.ts:95-174 + :200-244, PalettesPane.vue:36-100, availability.ts:105-172, status-lamp.ts:40-50, browser/index.ts:1-20; shipped producer: `dist/watercolor-dot.js` (`inheritAttrs: !1`, renderSlot count **0**), `Button.vue.d.ts` (zero `variant` occurrences), `glass-ui.css` `.watercolor-swatch[data-v-292b9032]{…display:inline-block;position:relative}` (the L-3 decider), `semantic.css` `@utility text-caption { … font-style: italic; … }` (the ADJ-m6 decider), `ring-ring` count in glass-ui.css **0**, `WatercolorDotProps` export grep **empty** (the K-11 decider); repo censuses re-run: `--color-ring` → exactly ONE hit and it is the tombstone comment `demo/styles/focus-ring.css:11` ("resolved EMPTY"); `ApiOfflineChip` mount census → exactly ONE (`CurrentPaletteEditor.vue:116`); `writeClipboard` import statements → **12** (root barrel), 0 from `/dom` (the K-9 decider); Button `variant=` census via my own python parse → **51 sites / 22 files** (third independent reproduction); `npx eslint --print-config` on the subject file → `no-unused-vars` severity **0**, `@typescript-eslint/no-unused-vars` `[0]`, `no-restricted-imports` **undefined** (the ADJ-m7 + L-6 decider); md5 `challenge-D-design.md` ≡ `challenge-D-design.2026-07-28-pass4.md` (`829e1e25…`, corpus-hygiene claim verified); motion-quarantine grep for this component → **no row** (clearance verified); probe payloads read whole: probe-P5-2.json, probe-P5-3.json, probe-D11-pass4.json, probe-D16/D17/D18-pass5.json.

## Scoreboard

**41 rows survive: 4 BLOCKER composites · 21 MAJOR · 16 MINOR**, plus 1 INFO and 1 identity-fold (not re-counted). **12 claims KILLED** (11 upheld from the re-readers + 1 this seat's own). **9 missed findings PROMOTED** (6 from reader-1, 4 from reader-2, 1 convergent duplicate — the convergence itself is evidence: both hostile seats independently derived ADJ-B3c).

Blocker-weight sentence: **the component's primary CTA and every swatch action surface are inert or unplaceable against shipped Glass 7 in every modality that was actually measured; its duplicate-name latch silently destroys the user's draft against a deleted target; and the gate stack that should have caught all of it is vacuous by construction.**

## Ruled disagreements and arbiter corrections (the seat's actual work)

| id | reader-1 | reader-2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D4-01 arm (a)** | stands ("emit, blank, emit, no status") | KILLED — `expandedId.value = palette.id` IS the durable result relocation | **reader-2 wins; D4-01 RESCOPED BLOCKER→MAJOR** | My read of usePaletteActions.ts:65-77: `onCurrentPaletteSaved` ends `if (palette.id != null) expandedId.value = palette.id;` and PalettesPane.vue:87 binds `:expanded="pm.expandedId.value === palette.id"` — in the unfiltered case the new palette lands at the front (store unshift, usePaletteStore.ts:93) **expanded**: that is exactly the "durable operation result" D4-01's own VISUAL-CONSTITUTION cite demands. Reader-2's quote of the handler omitted the same line in `onCurrentPaletteUpdated` (:81 also sets `expandedId`) — corrected here. What survives is the filter-blind case (probe-D11-pass4 P2: `after.cardCount: 0`, `headerBadge "(2 saved)"`, `searchStillActive "qqqzzz"`) + arm (c) focus destruction (`activeElement: "BODY:"` measured; the commit button unmounts itself while focused, v-if at :118) + total AT silence (see K-1). MAJOR, one row. |
| **D2-15 focus leg + ADJ-M1 phrasing** | killed the inversion; filed `ring-ring` phantom as missed | (not examined) | **kill upheld; M1 adopted with one precision** | `--color-ring` sole hit = the focus-ring.css:11 tombstone (my grep); glass-ui ships 0 `ring-ring` rules (my count). One precision on reader-1's own wording: `ring-2` without a minted color key still paints — `var(--tw-ring-color, currentcolor)` full-strength — so :46/:49/:52 are not focus-invisible; they carry an **accidental** full-strength currentcolor ring where the design specified a 40%-alpha token ring, atop a deleted UA outline. The defect is the phantom THEME KEY (3rd of the family, 9 sites repo-wide per reader-1's census), not invisibility. |
| **L-14** | (not examined) | CONFIRMED MINOR incl. "the two injects disagree on contract" | **arbiter kill of the contract-asymmetry leg (K-12); residue MINOR** | Precedent: the ColorSpaceSelector apotheosis ruled this exact pattern (L-5(b)) — `inject(SAFE_ACCENT_KEY)!` at 6 consumers under a provider App.vue installs on every route is **the house contract**, and the SAFE-vs-optional-key asymmetry is a deliberate two-key criticality policy, not incoherence. One identity; the ruling carries (anti-rename). Residue kept: `safeAccent` powers exactly one per-instance inline style (:76) whose value is `var(--primary)` by construction (foundation.css:250-251 chain) — a taste MINOR. |
| **B-2 cure constraint** | C axis proposed building a phantom-class walk (`scripts/proof-phantom-classes.mjs`) | echoed the Q-chron-4 invariant-33 walk | **cure REJECTED as shaped; defect stands** | Owner law: the proof:* / gate-farm idiom is owner-deleted and its rebuild is banned (memory `feedback-proof-idiom-retired`; CARRY-CUT-LEDGER CC-019/DR-19: "no `scripts/**/proof-*.mjs`; invariants live in types/tsc/eslint/tests"). The phantom-name family must die structurally (delete the phantoms, mint or repoint the tokens) with any standing check living in eslint/build config — never a proof script. Filed so no X wave inherits a banned cure from this corpus. |
| **D2-01 pointer leg** | KILLED (absolutist; never non-force measured) | (same substance in confirmed L-1/L-12/C-4 legs) | **kill upheld; substance carried in A-1/A-2** | probe-D16-pass5.mjs uses `click({force:true})`; probe-P5-2 measures the panel at `position: "static"`, rect `{x:0, y:900, w:1440, h:40}` — ~550 px below the swatch, full-width, held open only by a 250 ms `useLeaveTimer`. "Practically unreachable" is proven geometry; "unreachable by any means" was never measured in either direction → UNPROVEN-NEEDS-LIVE (U-8). |

All other kills (K-1, K-5..K-11) were re-derived and upheld without conflict — evidence in the register below.

## Adjudicated defect roster

Line anchors are HEAD `2e334231`; re-resolve before executing any cure (D-19 lesson). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Wave ids per `registry/CARRY-CUT-LEDGER.md` (letter X ratified 2026-08-03).

### BLOCKER composites

- **A-1 — L-1 / L-12 / C-1 / C-2 / D2-01(core) — CONFIRMED (BLOCKER).** Every `WatercolorDot` mount in this component and its child is written against an API Glass 7 deleted. My own reads of the shipped bundle: `dist/components/watercolor-dot/WatercolorDot.vue.d.ts` `__VLS_Props` = {color, variant, animate, cycleDuration, range, seed} — no `tag`; `dist/watercolor-dot.js` `inheritAttrs: !1`, only `class`/`style` re-applied from attrs, **zero** `renderSlot`, root `o("span", {"aria-hidden":"true", …, pointerEvents:"none" applied after attrs.style})`. Consumers: CurrentPaletteEditor.vue:95-105 (tag="button", :aria-label, @click, `<Plus>` default slot — all four discarded → the "Add current color" primary CTA is a decorative span); SwatchHoverMenu.vue:13-21/:29-36 (`<PopoverTrigger as-child>` merged attrs die by the same clause → the touch/AT action path cannot open; probe corroboration aria-expanded/data-state null). The e2e spec targets the dropped accessible name (palette-save.spec.ts:35) — cannot match by construction. **Terminal: X-W4** (glass-independent action-ownership inversion: a real `<button>` hosts, the dot becomes the paint it already declares itself to be); the long-run swatch-control ownership rides **CC-003 (BLOCKED-ON Glass ≥8 / TR#84 consumer-painted indicator)**; the producer-API question rides the standing **glass-ui BH relay** (owner edict). Note the K-11 asymmetry: `WatercolorDotProps` is NOT exported (my grep), so no consumer-side type-guard cure exists for this row — unlike L-20, whose `ButtonProps` IS exported.
- **A-2 — C-4 (BLOCKER core) / L-5 · C-5 (MAJOR leg) / D2-15-as-rescoped / ADJ-M1 (MAJOR leg, promoted missed) — CONFIRMED (BLOCKER composite): the phantom-name family, three names in one file.** (i) `.floating-panel` — zero rules anywhere in the served cascade (readers' independent sweeps + my probe-P5-2 read: `ruleCount: 0`, `position: "static"`, panel rect y=900 full-width — the inline top/left are inert, the hover panel lays out in normal body flow ~550 px from its swatch). With A-1 killing the touch path, the swatch Edit/Copy/Remove actions have **no working surface on any pointer type**. (ii) `.btn-interactive` at :75/:78/:100 — zero rules (probe `btnInteractiveRuleCount: 0`); the comment at :68-74 narrates hover/press legs "retired onto the producer atom" that were in fact deleted; LESSONS-LEARNED Q-chron-4 records the producer retirement under a false zero-site verdict. (iii) `ring-ring/40` at :46/:49/:52 + 8 more demo sites — `--color-ring` minted nowhere; sole repo hit is the tombstone comment focus-ring.css:11 recording this exact token's death (U-F25); the designed ring is replaced by an accidental full-strength currentcolor ring (see ruling table). Control proving the sweeps sound: `.glass-floating` (:58) and `--z-content` (:305) are real. **Terminal: X-W4** (the surfaces) + **X-W10** (token law: phantom names made structurally impossible — via types/eslint/build, NEVER a proof script, per the DR-19 ruling above).
- **A-3 — C-3 / C-10 / ADJ-B3c (promoted missed, convergent across both readers) — CONFIRMED (BLOCKER): the duplicate-name latch destroys user work three ways.** My source reads: `duplicateTarget` (:241) latched at :257, gate `v-if="duplicateTarget"` alone (:145), nothing watches name or buffer → stale-target Update (C-3). `colorsFromStrings` (:243-245) emits {css, position} only → Update wholesale-replaces the target's colors (usePaletteStore.ts:106), destroying per-colour name/weight metadata (C-10; measured: probe-P5-3 chain2 `meta` → all-null after Update). **The dangling-target arm (ADJ-B3c)**: `confirmUpdatePalette` (:267-276) guards only `id == null` — a deleted palette's object still holds its id — then `emit("updated")` → `updatePalette` → `findIndex === -1` → **silent no-op** (usePaletteStore.ts:99-100, my read), then **unconditionally** `emit("clearCurrent")` → PalettesPane.vue:53 `colorTarget.emitApply([])` → the buffer is wiped. Measured end-to-end in the orphaned pass-5 probe (probe-P5-3 chain1, read whole by this seat): t2 store `[]` + banner alive + `updateBtnStillThere: 1`; t3 store `[]`, row wiped, `anyErrorFeedback: false`. Three colours gone, nothing written, nothing said, no undo. Also confirmed inside this identity: `confirmUpdatePalette` lacks the empty-buffer guard `saveCurrentPalette` has (:248 vs :267) → a latched banner + emptied buffer writes `colors: []`. **Terminal: X-W7** (CRUD ownership; the collision rule belongs in the store, the latch must be invalidated by store change, and a no-op write must never clear the draft).
- **A-4 — C-16 / L-2 — CONFIRMED (BLOCKER): the gate stack is vacuous.** Re-measured by both readers and consistent with this seat's instrument runs: zero unit tests touch the component or its five composables; `.github/workflows/` = ci.yml + deploy-pages.yml + release.yml with zero playwright/e2e hits; vue-tsc green **by construction** at this seam (unknown props are legal Vue fallthrough attrs — L-20's 51 sites prove the property is structural); the one e2e spec written for this flow targets the accessible name Glass 7 dropped and narrates the nameless-button defect in prose instead of failing on it (palette-save.spec.ts:35-41). **Terminal: X-W1** (CI verification cliff DR-09 + visual-regression DR-08; includes C4-5's cure — a seeded populated-state `/#/palettes` variant in the capture matrix, without which this component's entire defective surface is invisible to the 60-capture matrix).

### MAJOR

- **L-20 — CONFIRMED.** `Button variant=` is a dead producer axis (Glass 7 = emphasis+tone; `Button.vue.d.ts` has zero `variant` occurrences — my grep). Census **51 sites / 22 files** reproduced independently three times (both readers + my python parse). Three sites here (:135, :152, :160); DOM corroboration probe-P5-2 B_buttons (`variant: "outline"` attr leaking beside `data-emphasis: "secondary"`). Cure IS executable consumer-side (`ButtonProps` exported). **Terminal: X-W4** (repo-wide codemod, sized from the 51/22 census).
- **C4-1 — CONFIRMED.** The seed props break the file's own thrice-asserted "one shape source" invariant (:60-61, :92-94, :280-283): ghost previews seeded `add-current-slot`/`edit-from-i`/`edit-to-i` vs the landing swatch's default seed `""` → four different silhouettes + filter seeds (reader-2 re-derived the PRNG offline, digit-for-digit). **Terminal: X-W7** (specimen identity).
- **C4-2 — CONFIRMED (mechanism); cost UNPROVEN-NEEDS-LIVE (U-6).** The always-mounted add slot binds the RAW `cssColorOpaque` (App.vue provides the uncoalesced signal; the coalesced `cssColorOpaqueFrame` exists at useColorPipeline.ts:279 and is not used here) → every colour change re-randomises silhouette + feTurbulence seed, against the producer's rasterize-once contract. **Terminal: X-W7**, witness rides **X-W6** temporal scenes.
- **C4-3 — CONFIRMED, GLASS-OWNED.** Producer `hashString(color+seed) % 256` (djb2; 33⁸ ≡ 1 mod 256) collides across channel-permuted colours — R/G/B all → feTurbulence seed 173 (both readers re-executed the shipped PRNG independently, matching outputs). Defeats the producer's own "no twelve-clones" doc promise. **Terminal: glass-ui BH relay** (owner edict); consumer carries nothing.
- **C-6 — CONFIRMED.** The hover panel is `aria-hidden="true"` with 3 focusable buttons inside (SwatchHoverMenu.vue:40-50 + :44-55 slot content — my read); its declared accessible alternative is the touch path A-1 proves dead → the swatch actions are reachable by **neither** AT route. **Terminal: X-W4** (dies inside the A-1/A-2 cure; id preserved).
- **C-7 / D2-14 / L-19-as-rescoped — CONFIRMED.** The component's only genuine `<button>` (:134-142) is its only nameless one (probe-P5-2 `accName: ""`); the Input's only name is its placeholder (`hasLabel: false`); the e2e spec routes around it in prose. The route-level corroboration leg is KILLED (K-5) — the visual matrix's nameless count belongs to a cold-boot control (C4-5 measured it), and the canonical L file carries the wrong side of a live intra-corpus contradiction. **Terminal: X-W4** (label + name); the matrix-seeding cure → **X-W1**.
- **C-9 — CONFIRMED, upgraded evidence.** `swatchKeys` embeds index in identity (`${color}::${i}`, useSwatchActions.ts:45-59 — my read; also a side-effecting computed) → every removal re-mints every survivor's key. Now MEASURED, not just derived: probe-P5-3 chain3 `stampsAfter: ["UNSTAMPED","UNSTAMPED"]` — full teardown/remount where a move was intended. **Terminal: X-W7**.
- **L-16 / C4-4 — CONFIRMED.** Three implementations of "is this colour in the palette?" with three semantics and two insertion orders on one gesture; `onPaletteAddColor` guards the MODEL colour but inserts the ARGUMENT (useColorPipeline.ts:212-224 — my read); alpha divergence :104 vs :166-168 (my read). Live disagreement currently unobservable (A-1 makes the control unclickable) — noted, not exculpatory. **Terminal: X-W7** (one membership predicate, one home).
- **L-4-as-rescoped — CONFIRMED with corrected census (K-9).** demo/ui/ = 19-module pure-alias layer, 18/19 through the root barrel; live dual path inside demo/palettes (dialog reached two ways). `writeClipboard`: **12** import sites (my count), all root-barrel, 0 from the real `./dom` export — reader-2's halving of the L file's 26 adopted. Graph-reach tables remain UNPROVEN (U-10). **Terminal: X-W8** (barrel subtraction; identity shared with the ColorSpaceSelector apotheosis' L-4/W-HYGIENE row — one extirpation, never two).
- **L-6 — CONFIRMED.** The G-DEMO boundary is dead config twice over: `no-restricted-imports` **undefined** for the subject file (my `--print-config` run); the globs target `demo/@/**` which does not exist. The one live violating edge: :193 raw `../status/ApiOfflineChip.vue`, self-excepted at status/index.ts:2-3. **Terminal: X-W8** (config liveness; identity shared with ColorSpaceSelector L-5(a)).
- **L-7 / L-22 — CONFIRMED.** 4 props + 8 emits re-declare state already on the injection ports; PalettesPane.vue:41-54 is fourteen lines of pure relay (my read); the cure's named blocker is real (usePalettePorts.ts value-imports 16 composables; the correct 27-line `import type` idiom exists at color-session/keys.ts and this component already consumes it). **Terminal: X-W5** (typed action contract).
- **L-10 — CONFIRMED.** Palette export has two live implementations; the byte-exact suite tests the 914-LoC tree the app never runs; the shipped path is the legacy-labelled file. **Terminal: X-W7**.
- **L-13 — CONFIRMED.** The edit-commit control pair exists twice (this file :56-84 and Dock.vue:136-144, same inline `:style="{color: safeAccent}"` copied verbatim) and the commitEdit command has two wirings that already drifted (App.vue:41 adds a pane side-effect usePaneRouter.ts:156 lacks). **Terminal: X-W5** (one command home).
- **L-15 — CONFIRMED.** The library's generic colour model is private (SPACE_SCHEMA/SPACE_IDS not re-exported; `withAlpha` grep = 0 in src/) so the demo forged a second one (PICKER_CHANNELS et al.). **Terminal: X-W9** (4.1 surface).
- **L-21-as-rescoped / C-13 / ADJ-M3 (promoted missed) — CONFIRMED, one identity: the availability affordance is wrongly homed and wrongly gated.** Surviving from L-21: the chip forks the pure, tested `resolveLampState`, 19 duplicated declarations + two identically-bodied keyframes, drifted padding/radius, test asymmetry (killed legs: K-7 production leak — structurally impossible, my read of `detectDevMisconfig`'s loopback clause; and the role-divergence leg — both seats map identically). From C-13: the `savedColorStrings.length > 0` gate makes an assertive `role="alert"` re-announce on every 0↔1 buffer crossing. From ADJ-M3 (reader-2's find, verified by my mount census): that same gate means the app's **sole production affordance for a down backend** (`status-lamp.ts:45 if (!isDev) return null` — my read; the lamp's own header assigns the prod job to the chip) renders nowhere unless the user is on /#/palettes with a staged colour — the corpus filed the over-announcement and never the coverage hole. **Terminal: X-W5** (scene/chrome ownership of transport status) + the chip/lamp consolidation onto the producer `./chip` atom.
- **D4-01-as-rescoped — MAJOR (was BLOCKER; both arms (a) and (b) killed as filed — see ruling table, K-1, K-2).** What survives, all measured: success invisible under an active filter (card filtered out while the badge increments), zero AT announcement in that frame (the status region pre-mounted with byte-identical text — probe-D11-pass4 P2, `filtered.emptyText` ≡ `after.emptyText`), and the commit button unmounting itself while focused (`activeElement: "BODY:"`). **Terminal: X-W7**.
- **D4-02 — CONFIRMED.** The contractual 1–50 cardinality (PALETTE-CONTRACT.md §3; enforced at reload.ts:191) has zero representation in the editor (unconditional add slot, bare count, disabled-only-at-zero); the house guard pattern sits 60 lines away in MixSourceSelector. **Terminal: X-W7** ("arbitrary-N truth" is named in that wave's own gist).
- **D4-03 — CONFIRMED.** Draft well and saved Card share fill/radius/edge tokens and the cartoon caster ladder (my token-chain read via reader-1's foundation.css cites; utils.css:103-105's own comment denies the box-shadow two lines above declaring it); Card-in-Card-in-Card confirmed by probe (`wellIsCardDescendant: true`). **Terminal: X-W10**.
- **D3-01 — CONFIRMED.** Two irreconcilable specimen species on one screen: 48 px seeded organic dots in the draft vs a 40 px flat hard-edged strip in the card (probe-D11-pass4 P3: `stripSegRadius: "0px"`, `stripSegFilter: "none"`). **Terminal: X-W7** (the specimen/domain split is that wave's title).
- **D2-03 — CONFIRMED (mechanism); exact ratios UNPROVEN (U-2).** Unguarded raw user colour on the ghost stroke + 24% fill against the fixed warm `--well-bg` stage, no contrast floor on the path — while the repo ships a gamut-guarded ≥4.5:1 floor for the title ramp. **Terminal: X-W10**.
- **ADJ-M2 (promoted missed, reader-1) — CONFIRMED.** The edit overlay occludes the palette it exists to compare against: statically derivable from :295-311 (absolute, top/left 0, -0.375rem offsets on a 48 px anchor with a 141×106 panel over a 58 px pitch) and measured in the orphaned probe-D17-pass5 (read whole by this seat): swatch coverage **100 / 100 / 39.8 %**, the name Input **31.4 %**, under `blur(11px) saturate(1.6)`. Pass 4's negative-evidence note ("a future seat must not extend that claim to the edit overlay") was misread as a clean bill for the panel — recorded as a corpus method lesson. **Terminal: X-W7**.

### MINOR

- **D3-02 — CONFIRMED.** `:disabled="savedColorStrings.length === 0"` (:138) inside `v-if="savedColorStrings.length > 0"` (:118) — complementary predicates; the disabled state is unrenderable; "nothing to save" is expressed by removal, not rest. → **X-W7** (dies in the D4-01 cure).
- **D2-21 — CONFIRMED.** Three per-instance overrides on producer roots half-apply (h-8 loses to the producer minimum → 32×40 stadium; border colour on a 0-width border) — compounding with L-20's dead `variant="outline"`. → **X-W4** (dies with L-20).
- **D2-24 — CONFIRMED.** Palette name unbounded at origin (`maxLength: -1`), written into every PaletteCard identity line. → **X-W7**.
- **D4-04 — CONFIRMED.** One empty state, two meanings: predicate is filtered length, copy is owned inventory. Cure is a third arm on EmptyState's existing variant union. → **X-W7**.
- **L-8-residue (from K-6) — RESCOPED to MINOR.** The name-identity rule lives in a leaf editor (costing a 23-field `Palette[]` prop for one `.find()`), and because the component blocks any name collision first, `createPalette`'s own dedupe branch (usePaletteStore.ts:67-81 — my read) is unreachable from this path. The "three semantic divergences" claim is dead (see K-6). → **X-W7**.
- **L-11 — CONFIRMED, IDENTITY-GUARDED.** tsconfig.demo.json paths drift — fact-identical to the ColorSpaceSelector apotheosis' L-14 row, already FOLDed to config parity. ONE identity; cross-referenced, not re-booked. → **X-W8** (via the existing fold).
- **L-14-as-rescoped — MINOR (arbiter kill K-12 of the asymmetry leg).** Residue: one inline style whose value is `var(--primary)` by construction. → **X-W5** (dies with L-13's consolidation).
- **L-23 — CONFIRMED.** TooltipProvider per call-site with three different house delays (4 mounts, 100/200/200/300, zero at root — reader-2's census). → **X-W5**.
- **L-24 — CONFIRMED.** demo/palettes ↔ demo/shell mutual dependency (type-import up, five value-imports down). → **X-W8** (SCC subtraction).
- **L-25 — CONFIRMED.** `EditTarget.paletteId` flattens two disjoint kinds behind the `"__current__"` sentinel. → **X-W7**.
- **C-12 — CONFIRMED.** useLeaveTimer holds a pending setTimeout across unmount, no disposal (17 lines, read whole). Hygiene; reproduction NONE. → **X-W7** (dies with the panel cure).
- **ADJ-m4 (promoted missed, reader-1) — CONFIRMED.** The edit overlay is physically-anchored (top/left/margin-left, `transform-origin: top left` — my read of :295-311) so RTL never flips it: measured escape of 74 px past the well's edge with `insetInline` identical in both directions (probe-D18-pass5, read whole); and the literal `&rarr;` (:63) is Bidi_Mirrored=No while the row itself mirrors → the FROM→TO diff reads backwards in RTL. Missed because both RTL passes ran believing the edit state unreachable. → **X-W10**.
- **ADJ-m5 (promoted missed, reader-1) — CONFIRMED.** Two identical ✓ glyphs on screen simultaneously during a desktop edit, 379.5 px apart, committing two unrelated irreversible things — and the smaller one is the nameless button (probe-D17-pass5 `checkGlyphs`, read whole). Strictly worse than the filed D2-07 "✓ and Update". → **X-W7**.
- **ADJ-m6 (promoted missed, reader-1) — CONFIRMED.** `text-caption` is producer-defined **italic** prose (`font-style: italic` in shipped semantic.css — my read) and the file spends it on the Update/Cancel action labels (:154, :162) and the FROM→TO arrow (:63): destructive-collision controls in an italic prose face, a skewed diagram glyph. → **X-W10**.
- **ADJ-m7 (promoted missed, reader-2) — CONFIRMED.** The dead-parameter class has no gate at all: `no-unused-vars` severity 0 + `@typescript-eslint/no-unused-vars` `[0]` (my `--print-config` run), no `noUnused*` in either tsconfig — the structural reason L-17-class instances keep being rediscovered one at a time. → **X-W8**.
- **ADJ-m8 (promoted missed, reader-2) — CONFIRMED.** Every mutating gesture round-trips the ENTIRE saved set through serialize→parse where the failure mode is a silent per-element drop (useColorPipeline.ts:227-238, empty catch + filter — my read): removing one swatch is structurally capable of deleting others with no diagnostic. Narrow practical trigger; data-mutating path nonetheless. → **X-W7**.

### INFO + identity-fold

- **D2-27 — INFO, CONFIRMED.** The tranche's visual evidence has never seen this component work: /#/palettes is the cold-boot empty pane in all four Safari matrices (bodyText 237/237/169/169 — my read of REPORT.md's per-capture table) and absent from all five special matrices. Feeds A-4's X-W1 disposition (seeded capture variant).
- **L-17 — CONFIRMED, IDENTITY-FOLD (not re-counted).** `toCSSColorString`/`colorToHexString` pass-through aliases + dead `_digits` — fact-identical to the row already banked at the ColorSpaceSelector apotheosis (its L-11, itself identity-guarded to ColorPicker L-10). One identity for life; the alias is load-bearing context for L-16 and is cited there.

## Killed-claims register

- **K-1 — D4-01(b) "the commit announces its opposite" (reader-1's kill, UPHELD).** The role="status" region was pre-mounted with byte-identical text before the commit (probe-D11-pass4 P2, `filtered.emptyText` ≡ `after.emptyText`, cardCount 0→0 — re-read by this seat); a live region that neither mounts nor changes fires no announcement. The defect is total silence on success — a different, differently-cured defect. Cite drift noted (EmptyState.vue:28, not :33).
- **K-2 — D4-01(a) "the success has no representation at all / nothing persists" (reader-2's kill, UPHELD; ruled over reader-1).** `onCurrentPaletteSaved` ends by expanding the new palette (`expandedId.value = palette.id`, usePaletteActions.ts:76 — my read; rendered via PalettesPane.vue:87). The durable-result relocation D4-01's cure prescribes already exists; the defect is that a filter can hide it (survives inside the rescoped row).
- **K-3 — D2-01's absolutist "unreachable by pointer, touch, keyboard and AT … by any means" (reader-1's kill, UPHELD).** The action buttons are live `<button>`s with handlers; the barrier is phantom-class geometry + a dwell timer. The corpus only ever clicked with `force:true`, so pointer reachability is formally unmeasured in both directions (→ U-8). Precise surviving claim: hidden from AT, dead on touch, pointer-hostile by measured geometry.
- **K-4 — D2-15's "no focus register at all, unlike their siblings" inversion (reader-1's kill, UPHELD).** No universal outline reset exists in the served cascade (every glass-ui `outline: none` is class-scoped); the bare :75/:78 buttons RETAIN the UA ring, while the :46/:49/:52 "siblings with a register" are the ones suppressing the working ring in favour of a phantom token (→ ADJ-M1, with this seat's currentcolor precision).
- **K-5 — L-19's route-level corroboration (reader-1's kill, UPHELD).** The nameless Save button is not in the DOM at cold boot (v-if :117-118); the matrix's `namelessButtons: 1` (REPORT.md:97 — my read) belongs to a cold-boot control; C4-5 measured exactly this. The canonical L file carries the wrong side of a live intra-corpus contradiction.
- **K-6 — L-8's "three semantic divergences" (reader-2's kill, UPHELD).** `savedPalettes` is pre-filtered by a type predicate (`p.isLocal && p.id != null`, usePaletteStore.ts:51-55 — my read) and that is what the parent passes (:45) — the isLocal leg is vacuously satisfied; the colorsMatch leg is two different questions by design. Residue banked at MINOR (L-8-residue).
- **K-7 — L-21's "the chip leaks the dev string to production users" (reader-2's kill, UPHELD).** `detectDevMisconfig` requires a loopback page hostname (availability.ts:112-115 — my read: "never fires in prod"); `markApiUnreachable` refuses to overwrite misconfigured. The role-divergence companion leg also false (both seats map misconfigured→alert, unavailable→status).
- **K-8 — L-18 "46 lines of unreachable re-export = rot" (reader-2's kill, UPHELD).** The zero-importer seam is documented, deliberate design (PI-6 named re-exports; sub-barrel reaches prescribed in the module's own header — my read of index.ts:1-16). The false clause ("eslint enforces it standing") is L-6's row; L-18 adds no defect.
- **K-9 — L-4's "writeClipboard at 26 demo sites" (reader-2's kill, UPHELD).** 12 import statements, not 26 (my grep; the rest are call sites). The substance (all 12 root-barrel, 0 from `./dom`) holds exactly; the codemod-sizing figure was wrong.
- **K-10 — L-3 "nine lines of scoped CSS laying out a node never rendered" (reader-2's kill, UPHELD).** Three declarations across five lines; `display: inline-flex` is a LIVE override of the producer's `display:inline-block` (verified by this seat in shipped glass-ui.css) — only the align/justify legs are inert. The underlying law is fully carried by A-1.
- **K-11 — L-2's consumer-side type-guard cure (reader-2's kill, UPHELD).** `WatercolorDotProps` is not exported from the shipped package (my grep: zero hits in dist/) — the cure is not executable without a producer change. The actionable asymmetry: `ButtonProps` IS exported, so the identical cure IS executable for L-20's 51 sites.
- **K-12 — L-14's "the two injects disagree on contract" leg (ARBITER'S OWN kill).** Ruled non-defect by precedent at the ColorSpaceSelector apotheosis (L-5(b)): the hard-assert is the house contract at 6 consumers under an always-installed provider; the SAFE-vs-EDIT key asymmetry is deliberate two-key criticality policy. One identity; a prior apotheosis' ruling is not re-litigated by a later corpus restating the same fact (anti-rename). Residue banked at MINOR.

## Superlatives (provenance: R1 = design-usage reader, R2 = library-code reader, A = re-verified by this seat)

1. **The producer contract is the mirror of the consumer's failure** (R1, A): WatercolorDot documents why it is not a GPU context, states its Safari rasterize-once invariant, and hard-codes `aria-hidden` + `pointerEvents:none` — it declares itself paint. Every BLOCKER in A-1 is a consumer ignoring a contract that could not have been clearer; the strongest argument for the containment inversion.
2. **save-P0 local-first inversion** (R1+R2, A — read whole): usePaletteActions.ts:65-77 names the prior contract inversion it cures and runs `createPalette` unconditionally so a save with the backend down loses zero data. Probe-D11's `storedNames` is this decision paying out.
3. **The one designed string** (R1): the S.W5-7 singular-form comment + ternary at :17-21 — "1 color", never "1 colors".
4. **PANEL_LAYOUT anti-drift constant** (R1+R2, A): SwatchHoverMenu.vue:64-66 — the one seam of the two-path fork that COULD silently diverge made structurally incapable of diverging, at a cost of two lines. Must survive whatever cures A-2.
5. **The Vue 3.5 bridge** (R1, A): reactive props destructure + `toRef(() => …)` across the composable boundary (:196-202, :234-235) — exactly the MEMORY-recorded cure for the stale-read caveat; attacked by four seats, unbroken.
6. **EmptyState's two-species split** (R1): error vs true-empty with recorded Q6 ruling — D4-04's cure is cheap because this file did the hard part.
7. **A real token among three phantoms** (R1, A): `z-index: var(--z-content)` resolves to the producer's genuine stacking token while `.btn-interactive`/`.floating-panel`/`ring-ring` sit phantom in the same file.
8. **The motion architecture** (R1, A — quarantine grep clean): centrally-owned two-guard PRM contract; the absence of a local PRM branch is the correct architecture (M-03/M-17/M-29 reasoning), recorded as clean under the standing quarantine ruling; no FALSE row names this component.
9. **The K-PALID pair** (R1, A): :267-271 + usePaletteActions.ts:88-96 refuse to coerce the optional `id` and say why — the instinct whose one missing clause is ADJ-B3c, a superlative and a lesson in six lines.
10. **keys.ts as the contract exemplar** (R2, A): 27 lines, five InjectionKeys, every import `import type` — what makes L-22's charge legible.
11. **availability.ts's error-state modelling** (R2, A): a pure three-clause predicate that provably cannot fire in prod; the refusal to overwrite a designed misconfig — the file that decided K-7 from source alone.
12. **status-lamp's resolveLampState** (R2, A): pure, total, isDev-as-argument, 6 unit tests + an e2e oracle — the one module in this cone with a real proof; its only defect is its address (and the coverage hole its dev-gate delegates to a mis-gated chip, ADJ-M3).
13. **savedPalettes' type predicate** (R2, A): optionality modelled instead of asserted — and it is what killed L-8.
14. **useAtmosphereFrameCoalesce** (R2): a coalescer with documented perf provenance, a stated terminal-colour invariant, and an explicit statement of which consumers must read which signal — C4-2 exists only because this file drew the line clearly enough to be crossed.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1**: D4-05's 57.6 % zoom-arm viewport share at 200 % (arithmetic internally consistent; companion `docOverflowX: 0` corroborated four ways).
- **U-2**: D2-03's exact contrast ratios (1.00:1 / 2.89:1) — mechanism CONFIRMED static; numbers live-only.
- **U-3**: D2-04's DPR hairline rendering (authored 1.5px vs rendered 1px) — UA-specific.
- **U-4**: D2-05's per-frame implosion coordinates (mechanism CONFIRMED: absolutely-positioned leavers with no positioned ancestor).
- **U-5**: D2-11's rendered mobile name-field font-size vs the iOS 16 px focus-zoom threshold.
- **U-6**: C4-2's churn cadence (34 style / 17 seed mutations per 60-move drag; 267 fleet-wide) and any perceptibility claim.
- **U-7**: L-21's simultaneous double `role="alert"` frame (dev-only preconditions statically CONFIRMED).
- **U-8**: D2-01 pointer reachability in BOTH directions — the corpus never non-forcibly clicked the teleported panel; whether a real mouse can traverse ~550 px against a 250 ms leave timer decides how the A-1/A-2 severity prose reads, not whether they are defects.
- **U-9**: the RED status of palette-save.spec.ts (CONFIRMED BY CONSTRUCTION; never executed here; not CI-wired either way).
- **U-10**: L-4's dev-graph reach table and L-22's cone measurements (direction structurally certain; figures unreproduced).
- **U-11 (matrix input, from C4-5/D2-27)**: a seeded populated-state `/#/palettes` capture variant — without it every one of this component's a11y defects is invisible to the visual matrix; this is the first probe the next visual audit should add.

## Corpus-hygiene and law notes

1. `challenge-D-design.md` ≡ `challenge-D-design.2026-07-28-pass4.md` byte-identical (md5 `829e1e25…`, my run) — no r2 file exists on any axis; AXIS-PROMOTIONS does not apply. A pass 5 ran (probe-D14…D18, P5-1…4, frames-D14, all 2026-07-29) and left no report; four of the nine promoted missed findings and the decisive A-3 measurement come from those orphaned probes. All three canonical reports are EXISTS-ORIGINAL in the hydration ledger — the thin-Codex caveat does not apply to this component.
2. Motion/PRM: no M-xx quarantine row names CurrentPaletteEditor (my grep); the general two-guard ruling was applied, not violated; no FALSE row carried.
3. DR-19 (owner law): no cure emerging from this corpus may take the shape of a `proof-*.mjs` script; the phantom-name family dies structurally.
4. Identity guards honoured: L-11→ColorSpaceSelector-L-14, L-17→ColorSpaceSelector-L-11→ColorPicker-L-10, L-4→the W-HYGIENE demo/ui extirpation, K-12→ColorSpaceSelector-L-5(b). One identity each, for life.
5. Dispositions here are **ADJUDICATED, not VERIFIED**; X-wave routing follows CARRY-CUT-LEDGER vocabulary (X-W1 gates · X-W4 semantic controls/action ownership · X-W5 scene/typed-action contract · X-W7 palette specimen/domain/CRUD · X-W8 graph/config subtraction · X-W9 library surface · X-W10 design/motion law), with the producer questions on the glass-ui BH relay and CC-003's BLOCKED-ON conditions untouched. No row lands NO-WAVE-OWNER.

## Closing verdict

**ADJUDICATED — GRAVE.** 41 rows survive hostile re-reading (4 BLOCKER composites · 21 MAJOR · 16 MINOR) against 12 killed claims and 9 promoted misses: the primary CTA and the entire swatch-action surface are inert or unplaceable in every measured modality (A-1/A-2), the duplicate-name latch can silently destroy the user's draft against a deleted target (A-3), and the vacuous gate stack (A-4) is why none of it was ever caught — while the save path's local-first core, the producer's self-documenting paint contract, and the component's own anti-drift devices are among the best-evidenced craft in the audit. Every surviving row carries exactly one X-wave owner; the live-only residue (U-1..U-11) is the next visual audit's input list, headed by the seeded populated-state capture without which this component cannot be seen at all.

— end of apotheosis. The challenger corpus and both hostile re-reader returns remain immutable beside this file per E-1/E-3; dated evidence was not rewritten.
