# CurrentPaletteEditor — adjudicated apotheosis (M-23/L-18) · served model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited)

**Seat**: fresh Fable adjudicator (M-23/L-18 apotheosis seat) — the incredulity seat, not a transcriber. **Date**: 2026-08-04 · **HEAD**: `2f6cf70e` · branch `tranche-u`.
**Supersession note**: this path carried an r1 adjudication (committed `e5c85303`, HEAD `2e334231`) that adjudicated a *different* worker pair — its kill attributions do not match this corpus's two returns. This file is the r2 adjudication of the two hostile re-reader returns named below; where an r1 ruling was independently correct (the `expandedId` relocation, the availability loopback triad, the `cartoon-surface` rest rung) this seat re-derived it from the tree with its own instruments before adopting anything. Git history preserves r1; dated evidence was not rewritten.
**Corpus**: `audit/components/CurrentPaletteEditor/` — three canonical challenge files (all EXISTS-ORIGINAL in `registry/HYDRATION-LEDGER.md:66-68`, area-palettes.json, depth 4 — re-read by this seat) + 30+ probe artifacts including an **orphaned pass-5 instrument run** (probe-D14…D18-pass5, probe-P5-1…4, frames-D14, 2026-07-29, no report ever written). **No r2 challenge files exist on any axis** (`ls *.r2*` empty; canonical D ≡ pass4, md5 `829e1e25…` re-run by this seat) — the canonical files govern.
**Workers adjudicated**: hostile re-reader **R1** (design-and-usage: 14 confirmed · 7 kills · 9 missed · 5 unproven) · hostile re-reader **R2** (library-and-code: 27 confirmed · 5 kills · 8 missed · 7 unproven).
**Method**: both returns were attacked before either was adopted. Four verdict-level conflicts between them (D4-01 arm (a) severity, D4-03, the L-21 production-leak leg, the Dock mobile-edit routing) were re-derived against the tree and ruled; two claims both readers shared were killed by this seat's own instruments; one reader-confirmed row (L-18) was rescoped to a kill; one defect arm neither reader filed was promoted from the corpus's own orphaned evidence.
**Arbiter's own instruments (all static + node + probe-JSON reads; zero browser tooling — probe parsimony + task law)**: full source reads of CurrentPaletteEditor.vue (whole, 312 lines), SwatchHoverMenu.vue (whole), useSwatchActions.ts (whole), useHoverPopover.ts (whole); usePaletteStore.ts:40-115, usePaletteActions.ts:55-110 (+ `expandedId` census :24/:28/:76/:81), PalettesPane.vue:36-120, availability.ts:95-175, status-lamp.ts:38-68, DockStatusLamp.vue:25-40, ApiOfflineChip.vue (whole), Dock.vue:55-150 (the :111 decider), useColorPipeline.ts:100-110/:160-170/:208-240, usePaletteWiring.ts:82-100, color-model.ts:17-72, browser/index.ts:1-16, focus-ring.css:1-20, utils.css:84-150, foundation.css:140-146, MixSourceSelector.vue guards + ring census, reload.ts:188-194, palette-save.spec.ts:30-45; **shipped producer**: WatercolorDot.vue.d.ts (6-prop union, no `tag`), `dist/watercolor-dot.js` (`inheritAttrs: !1`, `renderSlot` count **0**, `aria-hidden:"true"` span root, `pointerEvents:"none"`), Button.vue.d.ts (`variant` count **0**; emphasis/tone/size/iconOnly present), `dist/components/card/styles.css` `@utility cartoon-surface { … box-shadow: var(--shadow-cartoon-md) }` (the D4-03 decider), `dist/styles/tokens/shadow.css` sm=−2/−3/−4 vs md=−3/−5/−7 ladders; **node PRNG re-execution** (third independent reproduction): `""`→173 · `add-current-slot`→85 · `edit-from-0`→177 · `edit-to-0`→128; R/G/B→173/173/173; permuted trios→185; hex pair→20; **censuses**: btn-interactive = 5 class attrs + 3 comments; floating-panel = 0 rules anywhere; `--color-ring` sole hit = the U-F25 tombstone; TooltipProvider = 4 mounts (100/300/200/200), 0 at root; swatchExtraClass = 3 hits all internal; `emitApply([])` = 1 site; `tag="div"` = 18 lines; zero test/e2e/workflow references to the component cluster; **probe payloads read whole**: probe-D10-pass4.json, probe-D11-pass4.json, probe-D16/D17/D18-pass5.json, probe-P5-2.json, probe-P5-3.json; **docs**: VISUAL-CONSTITUTION.md §3 laws 2/8 + §Palette-library, PALETTE-CONTRACT.md §3, CARRY-CUT-LEDGER wave vocabulary (CC-003/CC-006/CC-019/CC-030/CC-031/CC-041/CC-044 + §X-W rows), motion-quarantine grep (**no row** names this component or SwatchHoverMenu).

## Scoreboard

**42 rows survive: 3 BLOCKER composites · 23 MAJOR · 14 MINOR · 2 INFO** (2 of the MINORs are identity-folds to prior apotheoses, cross-referenced not re-booked). **14 claims KILLED** (11 reader kills upheld after re-derivation · 3 arbiter's own — one of them against a leg BOTH readers asserted). **17 missed findings adjudicated** (9 from R1 · 8 from R2: 14 promoted to rows or row-arms, 2 identity-folded, 1 folded as a cure note) **+ 1 arbiter-promoted arm** from the corpus's own orphaned pass-5 evidence.

Blocker-weight sentence: **the primary CTA and every swatch control are decorative spans against shipped Glass 7 (A-1); the duplicate-name latch destroys user work three measured ways (A-2); and the gate stack that should have caught both is vacuous by construction (A-3).**

## Ruled disagreements and arbiter corrections (the seat's actual work)

| id | R1 (design-usage) | R2 (library-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D4-01 arm (a)** | CONFIRMED BLOCKER ("no result state, no operation status, no focus move") | UNPROVEN-NEEDS-LIVE (confirmed only arm (c) from source) | **ARBITER KILL of arm (a) as filed; row RESCOPED BLOCKER→MAJOR** | Neither reader read the handler's tail: `usePaletteActions.ts:76` `if (palette.id != null) expandedId.value = palette.id;` (and `:81` for Update), rendered via `PalettesPane.vue:87` `:expanded="pm.expandedId.value === palette.id"` — the saved palette lands at the store front (`unshift`, usePaletteStore.ts) **expanded**: a designed, durable result relocation exists. What survives, all measured (probe-D11-pass4 P2, re-read whole): the filter-blind case (card filtered out, `cardCount 0→0`, badge `(1 saved)→(2 saved)`, `searchStillActive "qqqzzz"`), total AT silence (see K-1), and arm (c) focus destruction (`activeElement: "BODY:"`; the commit row's `v-if` at :117-118 unmounts the focused button — source-certain). |
| **D4-03** | KILLED ("byte-for-byte / equal shadow" false; sm vs md rungs) | UNPROVEN-NEEDS-LIVE | **R1's kill wins — sealed statically by this seat** | My producer reads decide what R2 thought needed a browser: `dist/components/card/styles.css` `@utility cartoon-surface { … box-shadow: var(--shadow-cartoon-md) }` is the saved card's rest rung (PaletteCard.vue:19 rides the atom); `demo/styles/utils.css:107` gives the well `var(--shadow-cartoon-sm)`; the token ladders differ (−2/−3/−4 vs −3/−5/−7, shadow.css read raw). "Equal shadow, measured" is false; VISUAL-CONSTITUTION §3 law 8 names size and shadow, not fill. R1's truncated-quotation catch also verified: utils.css:103-105 reads in one sentence "The dashed edge ALONE carries the in-progress semantics; **the chip cartoon rung stays**" — the comment retains the stamp it was accused of contradicting. Residue observation only (shared `--well-bg` fill is real and lawful; §Palette-library canon retires cartoon on entity cards *prospectively* — that is X-W10's business, not this row's). |
| **L-21 production-leak leg** | asserted in its own kill's residue ("a production user can be told `dev misconfigured`") | CONFIRMED as the row's sharpest leg ("NO dev gate") | **ARBITER KILL of BOTH readers' leg** | availability.ts:112-115 (my read): `detectDevMisconfig` returns false unless `isLoopbackHost(i.pageHostname)` — comment: "never fires in prod" — and the state is resolved ONCE at client init (`initApiEnvironment`), with `markApiUnreachable` refusing to overwrite `misconfigured`. The chip has no gate, but the STATE it renders on is structurally unreachable in production. Surviving L-21 substance: the chip forks the pure, 6-tested `resolveLampState` matrix inline (roles/labels hard-coded in template, duplicated `offline-dot-pulse` keyframes, zero tests — my reads of both files). The dev-frame double-alert is a dev-server artifact (R1's kill upheld: DockStatusLamp gates on `import.meta.env.DEV`). |
| **Dock mobile-edit routing** | UNPROVEN-NEEDS-LIVE ("only real if something sets `activeLayer` on startEdit"; two ✓ glyphs during edit read as the layer NOT active) | superlative ("routing is exact") | **R2 wins — decidable statically** | Dock.vue:111 (my read): `if (mobileEditActive.value) activeLayer.value = "mobile-edit"` inside the layer-priority watch, with `mobileEditActive = !isDesktop && !!editTarget` at :72. The wiring exists; R1's two-glyph observation was taken at 1440px where `isDesktop` is true — the layer's absence in that frame is correct behaviour, not a hole. R1's residual (a live small-viewport witness) demoted to the U-list. |
| **L-18** | (killed the sibling L-19 framing only) | CONFIRMED MINOR ("declared seam has zero importers … invariant with neither enforcement nor users") | **ARBITER RESCOPE-TO-KILL** | browser/index.ts:1-16 (my read): the header itself prescribes the sub-barrel reach — "the seam's presence is the public contract; the sub-barrel is the tree-shake-honest reach" (PI-6 named re-exports, SFC style side-effects). Zero root-seam importers is the *documented design*, not rot. The one false clause ("the G-DEMO-3b boundary (eslint.config.js) enforces it standing") is L-6's row and is carried there. L-18 adds no independent defect. |
| C-5 count · C-9 scope · L-8 isLocal · L-13 fork · L-19 | — / kills | kills / — | **all five kills UPHELD** | Each re-derived: btn-interactive = 5 class attrs + 3 comments (my grep); `swatchKeyCounter`/`swatchKeyMap` are closure state inside `useSwatchActions()` at :43-44 (two coexisting mounts per the e2e header would otherwise collide — they don't); `savedPalettes` is pre-narrowed `(p): p is Palette & {id: string} => p.isLocal && p.id != null` (usePaletteStore.ts:51-56) and PalettesPane:45 passes exactly that; Dock.vue:58/:71-72 + `hidden lg:flex` are exact complements at 1024px; the component holds FIVE native labelled `<button>`s (:46/:49/:52/:75/:78) + three glass Buttons — "only real button" is false in both limbs. |

## Adjudicated defect roster

Line anchors are HEAD `2f6cf70e`; re-resolve before any cure. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Wave vocabulary per `registry/CARRY-CUT-LEDGER.md` (X-W1 gates · X-W4 semantic controls/action ownership · X-W5 scene/typed-action contract · X-W6 temporal scenes · X-W7 palette specimen/domain/CRUD · X-W8 graph/config subtraction · X-W9 library 4.1 surface · X-W10 design/motion law).

### BLOCKER composites

- **A-1 — C-1/L-1/L-12 + C-2/D2-01(core), converged across both readers — CONFIRMED (BLOCKER).** Every WatercolorDot mount in this component and its child is written against an API Glass 7 deleted. My own producer reads: d.ts props exactly `{color, variant, animate, cycleDuration, range, seed}` (no `tag`), slots `{}`; bundle `inheritAttrs: !1`, only `class`/`style` re-applied, **zero** `renderSlot`, root span `aria-hidden="true"` with `pointerEvents:"none"`. Consumers: :95-105 (the "Add current color" primary CTA — `tag="button"`, `:aria-label`, `@click`, `<Plus>` child, all four discarded → a decorative, glyph-less span); SwatchHoverMenu.vue:13-21 (touch `PopoverTrigger as-child` — reka-ui's merged contract dies on the same clause) and :29-36 (hover swatch click). Precision carried from the kills: the swatch *controls* are inert in all modalities; the action panel still opens on wrapper-div hover but lays out in body flow (see A-2's phantom leg) — probe-P5-2: panel `position: "static"`, rect `{x:0, y:900, w:1440, h:40}`. L-12's census exact: 6 `tag="button"` mounts / 4 components / + 18 `tag="div"` lines (identity-folds to the standing 21-site glass-7 `tag=` residue sweep banked at the ColorSpaceSelector apotheosis — one identity). **Terminal: CC-044** ("six WatercolorDot impostors", one atomic cut, **BLOCKED-ON CC-003's Glass ≥8 trigger**; the ledger forbids a pre-8 wrapper — the severity of waiting is X-W0 owner-sitting business); the producer-API question rides the standing **glass-ui BH relay** (owner edict). The dead e2e guard leg feeds A-3.
- **A-2 — C-3 + C-10 + R2-missed(1) + ARBITER arm from orphaned evidence — CONFIRMED (BLOCKER composite): the duplicate-name latch destroys user work three ways.** (i) *Stale latch* (C-3): `duplicateTarget` (:241) set at :257, banner gated on it alone (:144-145), **nothing watches `currentPaletteName`** (sole watch in the subtree is useSwatchActions.ts:54 on savedColorStrings — my read); Update then wholesale-overwrites via the lossy `:243-245` `{css, position}` projection, destroying per-colour `name`/`weight` (types + PaletteColorStrip weight-sizing verified; store write unconditional at usePaletteStore.ts `updatePalette`). (ii) *Empty-buffer overwrite* (R2's missed find, guard asymmetry source-certain): the banner sits OUTSIDE the `v-if="savedColorStrings.length > 0"` gate (:117 vs :144); `saveCurrentPalette` re-guards (:248), `confirmUpdatePalette` guards only `id == null` (:270-271) → a latched banner + emptied buffer writes `colors: []` — a zero-colour palette violating PALETTE-CONTRACT §3's own 1–50 bound (enforced at reload.ts:191 but not here). End-to-end reachability inherits the panel-geometry caveat (U-7). (iii) *Dangling target* (ARBITER-PROMOTED from orphaned probe-P5-3 chain1, read whole; neither reader filed it): Update against a deleted palette → `updatePalette` `findIndex === -1` → **silent no-op** — then `emit("clearCurrent")` fires unconditionally (:275) → `PalettesPane.vue:53` `emitApply([])` wipes the draft. Measured: t2 store `[]` + banner alive + `updateBtnStillThere: 1`; t3 store `[]`, row wiped (`solidDots: 0`), `anyErrorFeedback: false`. Reachable through delete-all + Update — ordinary controls. Companion asymmetry (R1's missed find #5, source-certain): `@keydown.enter` (:132) re-derives name + target on every call and does the right thing from the same visible state the banner gets wrong — the cheap disproof that A-2 needs a rewrite; `confirmUpdatePalette` need only re-derive as `saveCurrentPalette` already does. **Terminal: X-W7** (CC-006 CRUD ownership: collision rule belongs in the store; latch invalidated by store change; a no-op write never clears the draft).
- **A-3 — C-16/L-2 — CONFIRMED (BLOCKER): the gate stack is vacuous.** My own re-measurement: zero unit-test files reference the component or its composables (grep over test/ + demo/test/); zero playwright/e2e hits in `.github/workflows/*.yml`; vue-tsc green *by construction* at this seam (unknown props are legal fallthrough attrs — L-20's live sites prove the property structural); the one e2e spec written for this exact flow targets the accessible name Glass 7 drops (`palette-save.spec.ts:35` `getByRole("button", {name: /Add current color/})` — cannot match a `span[aria-hidden]`) and **narrates the nameless Save button in prose while routing around it** (:40-43, verified verbatim). **Terminal: X-W1** (CC-030 visual-regression + CC-031 CI cliff), with the seeded populated-`/#/palettes` capture variant as that wave's matrix input (U-9).

### MAJOR

- **C-4 / L-5 / C-5-as-rescoped — the phantom-name family — CONFIRMED.** `.floating-panel`: zero rules in demo CSS or shipped glass-ui (my greps; sole hits = usage :42, two prose comments); probe-P5-2 corroborates (`ruleCount: 0`, `position: "static"`, inline top/left inert, panel full-width at y=900, ~550px from its swatch). `btn-interactive`: zero rules anywhere; **5** class attrs (:75/:78/:100 + ColorInput.vue:69/:78 — R2's corrected count, my third reproduction), with the :68-74 comment narrating a retirement onto a producer atom that does not exist. Corollary (my read of useHoverPopover.ts:20-24): `positionPanel` centre-anchors with no `translateX(-50%)` anywhere and no scroll/resize re-derivation — even a restored `position: fixed` detaches on first scroll (U-8). The repo has cured this exact failure mode before and says so (utils.css:86: `.dashed-well` was "a never-defined phantom (inv-N-7)"). **Terminal: X-W4** (surfaces die in the action-ownership cure) + **X-W10** (token law). Cure constraint: DR-19/CC-019 — the phantom family dies structurally (types/eslint/build), NEVER a proof script.
- **C4-1 — CONFIRMED.** The seed props break the file's own thrice-asserted "one shape source" invariant (:60-61, :92-94, :280-283): `add-current-slot` / `edit-from-i` / `edit-to-i` vs the landing swatch's default `""` → four silhouettes + four feTurbulence seeds for one colour. My node re-execution matches both readers digit-for-digit (`""`→173, `add-current-slot`→85, `edit-from-0`→177, `edit-to-0`→128); probe-D16-pass5 `silhouettesIdentical: false`, `addSlotMatchesAnyFace: false`. **Terminal: X-W7** (specimen identity).
- **C4-3 — CONFIRMED, GLASS-OWNED.** `hashString(color+seed) % 256` (djb2, 33⁸ ≡ 1 mod 256) collides across channel-permuted colours: R/G/B all → 173, the three permuted trios → 185, `#ff0000`/`#00ff00` → 20 (my third independent PRNG run against the shipped bundle) — defeating the producer's documented "no twelve-clones" promise (d.ts:14-16, my read). **Terminal: glass-ui BH relay** (owner edict); consumer carries nothing.
- **C4-4 / L-16 — CONFIRMED.** Three membership predicates, two insertion orders, one gesture — all re-read by this seat: useSwatchActions.ts:62-74 (indexOf on opaque strings; hit → push END; single-hit → silent no-op), usePaletteWiring.ts:84-98 (serialize-compare; miss → `unshift` FRONT), useColorPipeline.ts:212-220 (**guards the MODEL colour, inserts the ARGUMENT** — verified verbatim), with the alpha divergence :104 (`withAlpha(…,1)`) vs :166-168 (alpha-preserving) live. **Terminal: X-W7** (one membership predicate, one home). L-17's alias pair is load-bearing context here (identity-fold below).
- **C-6 (+ R2-missed(7) cure note) — CONFIRMED.** SwatchHoverMenu.vue:40-50: the teleported panel carries `aria-hidden="true"` (:45) over `<slot name="actions"/>` filled with three real, focusable, labelled `<button>`s (:44-55) — canonical `aria-hidden-focus`; probe-P5-2 `focusableInside: 3`. R2's promoted addendum adopted: `inert` is the primitive that delivers the comment's stated intent (:38-39), and the comment's claimed mitigation ("the reka-ui Popover (touch path) is the accessible route") is the exact path A-1 proves dead — a false-mitigation link neither axis had connected. **Terminal: X-W4** (dies inside the A-1 cure; id preserved).
- **C-7 / D2-14 / L-19(body) — CONFIRMED.** The palette-commit Button (:134-142) — the component's only always-reachable control — has no accessible name (no aria-label/title/text; `<Check>` unlabelled), while every sibling icon control carries one (:46/:49/:52/:75/:78/:101). Corroborated three ways: probe-D17-pass5 `ownerAriaLabel: null` (`ownerBox: "32×40"`), probe-D10-pass4 `buttons: [{name:"<<NONAME>>"}]`, probe-P5-2 `accName: ""`. The Input's only name is its vanishing placeholder (:127-129). The e2e spec knows and routes around it (A-3). **Terminal: X-W4** (CC-041 kin).
- **C-9-as-rescoped — CONFIRMED (scope leg killed, K-9).** `${color}::${i}` puts index inside identity (useSwatchActions.ts:45-53, closure-scoped per-instance — NOT module state); any removal/reorder re-mints every survivor's key under `TransitionGroup name="vj-enter"` (:24-31), so a move can never be observed — full teardown/remount + fresh per-mount filter mint each time. **Terminal: X-W7**.
- **D4-01-as-rescoped — MAJOR (was BLOCKER; arm (a) killed K-2, arm (b) killed K-1).** Survives, all measured (probe-D11-pass4 P2): success invisible under an active filter (new palette front-inserted + expanded but filtered out; badge increments against an "empty" grid), zero AT announcement in that frame (the status region pre-mounted with byte-identical text), and the commit control unmounting itself while focused (`activeElement: "BODY:"`; v-if :117-118 + `clearCurrent` :264 → `emitApply([])`). **Terminal: X-W7**.
- **D4-02 — CONFIRMED (evidence table killed, K-4; core stands on corrected numbers).** The contractual 1–50 cardinality (PALETTE-CONTRACT.md §3, verified; enforced at reload.ts:191, verified) has zero representation: bare count (:18-22), uncapped add slot (:95-105), `:disabled` only at zero (:138), store `updatePalette` validates nothing. True measured geometry (my probe re-read): n50 wellH **650.8** (= n51), n80 **910.4** vs vh 900 with `saveBtnDisabled: false`, `anyOverLimitCopy: false`. The house pattern sits one file away (MixSourceSelector.vue:37-40 MIN/MAX + `canAddColor`, verified). Reachability of >50 through shipped affordances stays open (U-4). **Terminal: X-W7** ("arbitrary-N truth" is CC-006's own gist).
- **L-4 — CONFIRMED.** demo/ui/ = 19 one-line pure aliases, 18/19 through the root barrel (button alias read verbatim); live dual path in the direct parent (PalettesPane `../ui/*` beside `@mkbabb/glass-ui/dialog`); the subject mixes three reaches in one file (:174-181 aliases, :191 subpath, useSwatchActions.ts:5 root). **Terminal: X-W8** (identity shared with the standing W-HYGIENE demo/ui extirpation — one identity).
- **L-6 — CONFIRMED.** The G-DEMO boundary is dead config: globs target `demo/@/**` (does not exist), banned specifiers hit nothing, `no-restricted-imports` resolves undefined for the subject (R2's `--print-config` run; consistent with the ColorSpaceSelector apotheosis' L-5(a) — one identity, FOLDed there). The self-excepted raw edge :193 → status/index.ts:2-3 concession verified. L-18's false "enforces it standing" clause carried HERE, not as its own row. **Terminal: X-W8** (config liveness, via the existing fold).
- **L-7 / L-22 — CONFIRMED.** 4 props + 8 emits re-declare injected state; PalettesPane.vue:41-54 is pure relay (six 1:1 pass-throughs, verified); both props filled from one port read; the correct 27-line `import type` key idiom exists at color-session/keys.ts and this component already consumes it (:173), while usePalettePorts.ts colocates all five KEYs (:271-275) with a 275-line value-importing provider (my wc + grep). **Terminal: X-W5** (typed action contract).
- **L-8-as-rescoped — CONFIRMED (isLocal leg killed, K-6).** The collision rule has two disagreeing homes: component name-only (:253-255) vs store name+`colorsMatch` with move-to-front reuse (usePaletteStore.ts:66-82, verified) — a collision here raises a destructive-overwrite banner for a palette the store would have created fresh; and a 23-field `Palette[]` is drilled into a leaf editor for one `.find()`. **Terminal: X-W7** (the rule moves into the store with A-2's cure).
- **L-10 — CONFIRMED.** `demo/palettes/export.ts` (file) shadows `demo/palettes/export/` (12 modules, **no index.ts** — my ls); usePaletteExport.ts:9 `from "./export"` resolves to the legacy file; the byte-exact suite is the sole consumer of the tree the app never runs (one external hit, verified). Slugify divergence real; collision bounded by the UUID8 suffix (R1's precision adopted). **Terminal: X-W7**.
- **L-13-as-rescoped — CONFIRMED (fork implication killed, K-10).** The edit-commit pair is implemented twice with verbatim-copied `:style="{color: safeAccent}"` (this file :75-80 vs Dock.vue:141-144, both read), and `commitEdit` has two drifted wirings (App.vue:41 adds a pane side-effect usePaneRouter.ts:156 lacks — cited consistently by both readers). The responsive split itself is exact and correct (ruling table). **Terminal: X-W5** (one command home).
- **L-15 — CONFIRMED.** The library's generic colour model is private (SPACE_SCHEMA/SPACE_IDS/isAnyColor/makeColor unexported; `withAlpha` = 0 hits in src/; no bare-root export) so the demo forged a second one — the direct cause of the string-typed `savedColorStrings` + `indexOf` comparisons behind C4-4. **Terminal: X-W9** (4.1 surface).
- **L-20 — CONFIRMED.** `Button variant=` addresses a deleted axis (d.ts: zero `variant`; emphasis/tone/size live — my grep) at :135/:152/:160, leaking to the DOM as a literal attribute (probe-P5-2 B_buttons: `variant: "outline"` beside `data-emphasis: "secondary"`); the same repo already speaks the new axis (PalettesPane.vue:113 `emphasis="text"`, :116 `tone="destructive"`) while :63 still writes `variant="ghost"`. `ButtonProps` IS exported → consumer-side codemod executable. **Terminal: X-W4** (repo-wide codemod).
- **L-21-as-rescoped — CONFIRMED (production-leak leg killed K-12; dev double-alert killed K-8).** ApiOfflineChip re-derives the tested `resolveLampState` matrix inline (roles + labels hard-coded :11-26; fork verified against status-lamp.ts:44-65), duplicates the `offline-dot-pulse` keyframes, and has zero tests — an untested fork of the one module in this cone with a real proof. **Terminal: X-W5** (consolidation onto the pure resolver; transport-status scene ownership).
- **ADJ-M1 (R1 missed #1, promoted) — CONFIRMED.** The edit overlay occludes the data it exists to compare against: probe-D17-pass5 (read whole) — neighbour swatches covered **100 / 100 / 39.8 %**, the name field **31.4 %**, under `oklab(… / 0.808)` + `blur(11px) saturate(1.6)`; source cause :295-311 (absolute top/left 0, −0.375rem offsets, ~141px growth over a 58px pitch). `grep -in occlu` across all ten corpus files = zero — genuinely unfiled; pass-4's §D negative inspected this element's containing block and never measured what it covers. **Terminal: X-W7**.
- **ADJ-M2 (R1 missed #2, promoted with one arbiter correction) — CONFIRMED.** The overlay is physically anchored (`transform-origin: top left`, `left: 0`, `margin-left: -0.375rem` — :299-311): probe-D18-pass5 (read whole) — RTL overlay right edge 760.1 vs well right edge 686 → **74px escape**, `insetInline` identical both directions. **Correction to R1**: the internal FROM→TO row DOES mirror (rowItems: ghost x=705.1 > solid x=626) — what fails is the literal `&rarr;` (:63, Bidi_Mirrored=No), so the arrow points backwards against the mirrored row; R1's "stays LTR-ordered" is the wrong mechanism for the right defect. **Terminal: X-W10** (logical properties + direction law).
- **ADJ-M3 (R1 missed #3, promoted) — CONFIRMED.** Update (destructive overwrite) and Cancel (dismiss) render as identical twins: byte-identical except the dead `variant` prop (:151-166, source-read), so both collapse to default `emphasis="secondary" tone="neutral"` — while `tone="destructive"` is used correctly one file away (PalettesPane.vue:116). The corpus measured the collapse without concluding it (pass-3: Update 61.8×36 vs Cancel 59.2×36). **Terminal: X-W4** (dies with L-20's codemod + an explicit destructive tone).
- **ADJ-M4 (R1 missed #4, promoted) — CONFIRMED.** The draft tray has no list/region semantics: root `div.dashed-well` unnamed (:2-4), title a `<span>` (:8-16), swatch row a bare flex div (:24-28), per-swatch labels carry no ordinal/setsize — while PaletteCardGrid does exactly the right thing (`role="list"` + article children, verified by both readers). Orthogonal to A-1: survives any button cure. **Terminal: X-W4**.
- **ADJ-M5 (R1 missed #5) — CONFIRMED, folded as A-2's asymmetry evidence** (see A-2 arm (iii) companion; recorded here so the id is not lost). **Terminal: X-W7** (inside A-2's cure).

### MINOR

- **C-12 — CONFIRMED.** useLeaveTimer: 17 lines, no `onScopeDispose` (read whole); the 250ms close fires against a disposed scope. R2's honesty preserved ("Hypothesis by inspection", no repro). → **X-W7** (dies with the panel cure).
- **C-14 — CONFIRMED.** `savedPaletteCount` + `savedPalettes` filled from one port read in the same expression pair (PalettesPane.vue:44-45, verified). → **X-W5**.
- **D4-04 — CONFIRMED.** One empty state, two meanings: predicate is `filteredSaved.length === 0`, copy is "No saved palettes yet." (PalettesPane.vue:77-80 byte-verified); measured over a non-empty library (probe-D11 P2 `headerBadge "(1 saved)"`). EmptyState's existing variant union makes the cure a third arm, not a new species. → **X-W7**.
- **L-11 — CONFIRMED, IDENTITY-FOLD.** tsconfig paths drift — fact-identical to the ColorSpaceSelector apotheosis' L-14, already FOLDed to config parity. One identity; cross-referenced, not re-booked. → **X-W8** (via the fold).
- **L-17 — CONFIRMED, IDENTITY-FOLD (+ R2 missed #4 folded in).** `toCSSColorString`/`colorToHexString` pure aliases + dead `_digits` (color-model.ts:61-71 verified) — fact-identical to ColorSpaceSelector-L-11 → ColorPicker-L-10; R2's `CSS_NATIVE_SPACES` dead alias (:58, sole hit = declaration, my grep) belongs to the SAME banked identity (that apotheosis already sentences it). One identity for life; the alias pair is load-bearing context for C4-4.
- **L-23 — CONFIRMED.** TooltipProvider per call-site: exactly 4 mounts, delays 100/300/200/200, zero at root (my census, correcting nobody — both readers' 4-site count exact; the delay-set is this seat's addition). Here it wraps A-1's inert span. → **X-W5**.
- **L-24 — CONFIRMED.** palettes ↔ shell mutual dependency (type-import up at usePalettePorts.ts:19, five SESSION_PORT_KEY value-imports down — verified); lattice cycle, not load-time (R2's own limitation correct). → **X-W8** (SCC subtraction).
- **L-25 (+ R2 missed #5 merged) — CONFIRMED.** `EditTarget.paletteId` flattens two kinds behind a sentinel sharing the temp-id namespace (constants + utils verified) — and the shape is declared THREE times: color-model.ts:19-23 (canonical), useSwatchActions.ts:15 (structural copy), **:207 inline in this component's own defineEmits** while its own composable imports the real type (useSwatchActions.ts:3). → **X-W7** (types).
- **ADJ-m1 (R1 missed #6) — CONFIRMED.** Two identical ✓ glyphs on screen during an edit, 379.5px apart, bound to different commands — and the larger, accent-tinted, NAMED one is the minor command while the smaller nameless one commits the palette (probe-D17-pass5 checkGlyphs, read whole; sources :76 vs :141; third instance Dock.vue:143). PR-05 one-mark-one-meaning. → **X-W10**.
- **ADJ-m2 (R1 missed #7) — CONFIRMED.** No draft-discard exists: `emitApply([])` has exactly one site (PalettesPane.vue:53, my grep) reached only via the two commit handlers; single-colour removal is C-2-dead or panel-hostile. Abandoning a draft is not an available action; wholesale silent replacement is. → **X-W7**.
- **ADJ-m3 (R1 missed #8, RESCOPED by this seat) — CONFIRMED as rescoped.** :46/:49/:52 suppress the UA outline and substitute `ring-ring/40` — but the deeper truth R1 missed: `--color-ring` is minted NOWHERE; the repo's own U-F25 tombstone (focus-ring.css:11) records that exact token "resolved EMPTY — … an un-clobbered ring painted nothing", and the house cure is the dual-contrast `--focus-ring-shadow` recipe these sites don't consume (the intra-repo `/40` vs full split at MixSourceSelector:153/:170/:257 verified but both ride the same dead token). The painted result is U-3; the authored state — a per-instance focus register on a tombstoned token under `outline-none` — is static-certain. → **X-W4** (register) + **X-W10** (token law).
- **ADJ-m4 (R2 missed #2) — CONFIRMED.** The prop type erases the store's proven `& { id: string }` refinement (:201 vs usePaletteStore.ts:51-56), forcing the dead re-guard at :270-271 whose failure posture is a silent no-op on the user's primary decision. Cure is a type, not a guard. → **X-W7**.
- **ADJ-m5 (R2 missed #3) — CONFIRMED.** `swatchExtraClass`: declared + bound twice, zero consumers anywhere (my grep: 3 hits, all internal). Dead public API on the child this component is built from. → **X-W7** (dies in the cure).
- **ADJ-m6 (R2 missed #8) — CONFIRMED.** usePalettePorts.ts:88 launders `string` into the closed `ViewId` union with a bare `as` — the exact posture the same feature refuses two files away (the store's type predicate; usePaletteActions' early returns). → **X-W5** (typed contract).

### INFO

- **D4-05 (measurement only) — CONFIRMED as a number, not a defect.** 259/450 = 57.6% at the 200% zoom arm, `docOverflowX: 0` (probe re-read). No cited law binds a *populated* tray (K-5); banked as a visual-audit input (U-1).
- **ADJ-i1 (R1 missed #9) — CONFIRMED: the corpus has orphaned evidence.** A full pass-5 instrument run (probe-D14…D18-pass5, P5-1…4, frames-D14, 2026-07-29) has no report; two of this roster's MAJORs and A-2's measured third arm live only in those JSONs; any completeness validator keyed to the three hashed reports reads GREEN over them. **Terminal: NO-WAVE-OWNER** — a registry/COMPLETENESS-LEDGER row for the next formation boundary, not a product wave.

## Killed-claims register

- **K-1 — D4-01(b) "the commit announces its opposite" (R1's kill, UPHELD).** The `role="status"` region was pre-mounted with byte-identical text before the commit (probe-D11 P2 `filtered.emptyText` ≡ `after.emptyText`, cardCount 0→0 — re-read by this seat); an unchanged live region fires nothing. The defect is total silence — differently cured. R1's cite drift adopted (EmptyState.vue:28, not :33).
- **K-2 — D4-01(a) "no result state / no designed outcome" (ARBITER'S OWN kill, against R1's confirmation).** `usePaletteActions.ts:76`/`:81` end both handlers with `expandedId.value = …`; PalettesPane.vue:87 renders it; the store unshifts to front. The durable result relocation D4-01's own canon cite demands *exists* — the defect is that a filter can hide it and AT never hears it (survives inside the rescoped row).
- **K-3 — D4-03 "byte-for-byte … equal shadow, measured" (R1's kill, UPHELD and SEALED statically).** Producer `cartoon-surface` rest rung = `--shadow-cartoon-md`; the well = `--shadow-cartoon-sm`; the token ladders differ (my reads). Law 8 names shadow and size, not fill. The utils.css:103-105 comment explicitly retains the chip rung — the "contradiction" was a truncated quotation.
- **K-4 — D4-02's geometry table "12 → 274.6 / 50 → 456.6" (R1's kill, UPHELD).** Neither number exists in probe-D10-pass4 (real: n12 261.4, n50 = n51 = 650.8, n80 910.4 — my re-read). The finding survives on the true numbers (worse than the table claimed); the table must not be carried as measured fact.
- **K-5 — D4-05's §3-law-2 severity hook (R1's kill, UPHELD).** Law 2 governs "**Empty** secondary content" (verified verbatim); the 57.6% frame was a 12-colour populated tray. Real number, no binding law → INFO.
- **K-6 — L-8's isLocal leg (R2's kill, UPHELD).** `savedPalettes` is pre-narrowed by the type predicate (usePaletteStore.ts:51-56, my read) and PalettesPane:45 passes exactly that computed — the divergence cannot fire. The colorsMatch + resolution legs survive (roster).
- **K-7 — C-5's "eight `.vue` sites" (R2's kill, UPHELD).** 5 class attributes + 3 prose comments (my grep) — the report counted its own documentation; the fix wave carries five.
- **K-8 — L-21's dev double-alert as a product defect (R1's kill, UPHELD).** DockStatusLamp gates on `import.meta.env.DEV` (:31-32) and `resolveLampState` opens `if (!isDev) return null` (my reads) — one `role="alert"` maximum in a shipped build.
- **K-9 — C-9's "module-scoped counter" (R2's kill, UPHELD).** `let swatchKeyCounter` / `swatchKeyMap` are closure state inside `useSwatchActions()` (:43-44 after the :19 open — my read); the editor's two coexisting mounts would otherwise collide and do not. Primary finding untouched.
- **K-10 — L-13's accidental-fork implication (R2's kill, UPHELD and extended).** `hidden lg:flex` (1024px) and `!useMediaQuery("(min-width: 1024px)")` are exact complements; **and** Dock.vue:111 wires `mobileEditActive` → `activeLayer.value = "mobile-edit"` — the routing R1 doubted exists, statically. The duplicated implementation + drifted `commitEdit` wiring survive.
- **K-11 — L-19's "only real `<button>`" title (both readers' kills, merged, UPHELD).** Five native labelled buttons + three glass Buttons in the template (my read); the scoped DOM query measured teleport/v-if geometry, not the source. The nameless-Save substance is C-7 verbatim — one identity, no second row.
- **K-12 — L-21's production leak of the dev string (ARBITER'S OWN kill, against BOTH readers).** `detectDevMisconfig` requires a loopback page hostname ("never fires in prod" — availability.ts, my read), resolved once at init; `markApiUnreachable` refuses to overwrite it. The chip's missing gate is real; the state it would leak is unreachable in production.
- **K-13 — C-13's attribution limb (R1's kill, UPHELD).** The :68-74 comment retires exactly "transition-all + hover:scale-110/active:scale-95" for the :75/:78 buttons; `focus-visible:outline-none` appears in no retired list and the :46/:49/:52 buttons are outside the comment's scope (my read). The real narrower defect is ADJ-m3.
- **K-14 — L-18 as an independent defect (ARBITER'S OWN rescope-to-kill).** The seam's header prescribes the sub-barrel reach as the tree-shake-honest contract (browser/index.ts:9-16, my read); zero root importers is design, not rot. The false eslint clause rides L-6. No row.

## Superlatives (provenance: R1 · R2 · A = re-verified by this seat)

1. **The producer contract is the mirror of the consumer's failure** (R2, A): WatercolorDot.vue.d.ts explains why it is paint — the CSS/SVG primitive framing, the rasterize-once invariant, the seeded one-shape-source ghost contract. Every fact needed to predict A-1 was published in the type the consumer imports — the sharpest restatement of A-3.
2. **save-P0 local-first inversion** (A, from source): usePaletteActions.ts:65-77 names the contract inversion it cures and runs `createPalette` unconditionally so a save with the backend down loses zero data — and quietly finishes with the `expandedId` relocation that decided K-2.
3. **`PANEL_LAYOUT`** (R1+R2, A): one string, two mutually-exclusive render paths, drift made structurally impossible (SwatchHoverMenu.vue:64-66). Must survive whatever cures C-4.
4. **The `.edit-overlay` var-override block** (R1, A): :295-311 knows `.swatch-row` sets `--vj-enter-*` custom properties that inherit, re-declares them explicitly, and documents why at the point of the fix — the most careful CSS in the file (and still physically anchored, ADJ-M2: craft and defect in the same nine lines).
5. **savedPalettes' type predicate** (R2, A): `(p): p is Palette & { id: string }` — optionality modelled, not asserted; it is what killed L-8's third leg, and precisely the invariant the subject's prop declaration throws away (ADJ-m4).
6. **The K-PALID pair** (R1+R2, A): :267-271 and usePaletteActions.ts:95 refuse to coerce the honest optional `id` and say why — the discipline whose one missing clause is A-2's arm (iii).
7. **resolveLampState** (R2, A): pure, total, `isDev` as an argument, six tests — the one module in this cone with a real proof; its only defect is that its consumer forked it (L-21) instead of calling it.
8. **ApiOfflineChip's reduced-motion gating** (R1, A): both the `@keyframes` and the `animation:` live inside `@media (prefers-reduced-motion: no-preference)` — under `reduce` there is nothing to clamp. Of thirteen reduced-motion raisers in the provenance census, this file simply does it right; its two-register misconfig/offline design is likewise deliberate.
9. **Dock ↔ overlay responsive complement** (R2, A — sealed by this seat's :111 read): `hidden lg:flex` and `!isDesktop` meet exactly at 1024px, the layer auto-activates off `editTarget`, with correct Vue 3.5 destructured-prop reactivity. No gap, no double-render.
10. **The offline-derivation evidence standard** (R1, A — third reproduction): C4-1/C4-3 were derived from the producer's own exported PRNG and matched live DOM digit-for-digit; this seat re-ran the derivation from a clean shell and reproduced every value. Evidence a hostile reader can re-run in one command is the corpus's high-water mark.
11. **Pass-4 §D's negative evidence** (R1, A): a section spent verifying the overlay's containing block IS correct and instructing future seats not to over-extend the claim — adversarial discipline rarer than a true positive (and, per ADJ-M1, still not a substitute for measuring what the panel covers).
12. **The one designed string** (R1+R2, A): the S.W5-7 singular-form ternary + provenance comment (:17-21) — "1 color", never "1 colors".
13. **browser/card/index.ts's named-re-export rationale** (R2, A): the SFC-style-side-effect tree-shake argument, articulated where it is load-bearing — the reasoning that also decided K-14.

## UNPROVEN-NEEDS-LIVE residue (the next visual audit's input list)

- **U-1**: D4-05's 57.6% populated-tray share at the 200% zoom arm — real number, no binding law; input to any ratified populated-secondary proportion rule.
- **U-2**: forced-colors / reduced-transparency behaviour of the edit overlay over its measured 100%-occluded neighbours (`frames-D/five-forced-colors.png` was captured without an active edit — no frame settles it).
- **U-3**: the PAINTED focus state of :46/:49/:52 — whether Tailwind emitted anything for `ring-ring/40` over the tombstoned `--color-ring`, and its 1.4.11 contrast against `--well-bg` in both schemes (authored state static-certain, ADJ-m3).
- **U-4**: reachability of a >50-colour draft through shipped add paths (D4-02's 51/80 arms were localStorage-seeded).
- **U-5**: the hover panel's real-pointer reachability in BOTH directions — the corpus only ever clicked `force:true`; ~550px of body-flow travel against a 250ms leave timer is proven geometry, "unreachable by any means" is not.
- **U-6**: C4-2's churn cadence (the always-mounted add slot binds the uncoalesced `cssColorOpaque` — mechanism verified against useColorPipeline.ts:104 and the producer's re-derivation; the 34/17-mutation counts are live-only) → witness rides X-W6 temporal scenes.
- **U-7**: A-2 arm (ii)'s end-to-end trigger via the hover panel's Remove button (the guard hole is source-certain; the gesture route inherits U-5).
- **U-8**: useHoverPopover's centre-anchor with no `translateX(-50%)` — whether the positioning contract was ever complete is unknowable from the tree (do not restore `position: fixed` assuming the maths is sound).
- **U-9 (matrix input)**: a seeded populated-state `/#/palettes` capture variant — without it this component's entire defective surface is invisible to the visual matrix; first probe the next visual audit adds (feeds A-3's X-W1 disposition).
- **U-10**: onPaletteApply's silent per-element drop on whole-buffer rewrites (useColorPipeline.ts:227-238, verified) — no reachable loss constructed; recorded as an error-posture hazard, not a defect.
- **U-11**: a small-viewport live witness of the mobile-edit dock layer (routing now statically proven, K-10; the rendered frame remains unwitnessed).

## Corpus-hygiene and law notes

1. **r2 governance**: no r2 challenge files exist on any axis; the canonical files govern (D canonical ≡ pass4, md5 re-run). The orphaned pass-5 evidence is sound, re-runnable, and now has an owner-row request (ADJ-i1, NO-WAVE-OWNER → registry).
2. **Motion/PRM**: no motion-quarantine row names CurrentPaletteEditor or SwatchHoverMenu (my grep); no M-xx FALSE row is carried; no verdict above rests on a motion assertion.
3. **DR-19 / CC-019 (owner law)**: no cure emerging from this corpus may take the shape of a `proof-*.mjs` script — neither reader proposed one; the phantom-name family dies structurally.
4. **Identity guards (anti-rename)**: L-11 → ColorSpaceSelector-L-14; L-17 (+CSS_NATIVE_SPACES) → ColorSpaceSelector-L-11 → ColorPicker-L-10; the `tag="div"` census → the ColorSpaceSelector apotheosis' 21-site glass-7 `tag=` residue sweep; L-4 → the standing W-HYGIENE demo/ui extirpation. One identity each, for life.
5. **Dispositions are ADJUDICATED, not VERIFIED.** X-wave routing follows CARRY-CUT-LEDGER vocabulary; A-1 sits in the CC-044/CC-003 blocked bank by that ledger's own atomic-cut ruling (no pre-8 wrapper) with the producer questions on the glass-ui BH relay; exactly one row lands NO-WAVE-OWNER (ADJ-i1, a registry row by nature).

## Closing verdict

**ADJUDICATED — GRAVE.** 42 rows survive two hostile re-readings and this seat's counter-attack (3 BLOCKER composites · 23 MAJOR · 14 MINOR · 2 INFO) against 14 killed claims — three of them this seat's own, including one leg both readers asserted and one confirmation neither reader had read deep enough to doubt: the component's primary CTA and entire swatch-control surface are decorative spans against shipped Glass 7 (A-1), its duplicate-name latch destroys user work by stale overwrite, empty-buffer overwrite, and measured silent-no-op-plus-wipe (A-2), and the vacuous gate stack (A-3) is why a born-dead flow shipped with a green build — while the local-first save core, the store's type-predicate boundary, and the producer's self-documenting paint contract remain the best-evidenced craft in the cone. Every surviving row carries exactly one terminal disposition; the U-list is the next visual audit's opening slate, headed by the seeded populated-state capture without which this component cannot be seen at all.

— end of apotheosis. The challenger corpus and both hostile re-reader returns remain immutable beside this file per E-1/E-3; the r1 adjudication at this path is preserved in git history (`e5c85303`); dated evidence was not rewritten.
