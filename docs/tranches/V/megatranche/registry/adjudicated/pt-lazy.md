claude-fable-5

# pt-lazy — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-05 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ `ef10d5b` (read-only, main checkout), `typescript/src/parse/lazy.ts` (43 lines, 3 exports) + its import closure · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: TWO independent reader returns over `audit/parse-that-modules/lazy/challenge-L-library.md` (Opus, 3 passes, 23 defects · 7 superlatives) and `challenge-C-consumption.md` (Opus, 2 passes, 16 defects · 4 superlatives). Reader-A: 12 confirmed · 13 killed · 7 missed. Reader-B: 20 confirmed · 9 killed · 7 missed. The two readers **disagree at verdict level on A.5, D-C2's rationale, D-C6, D-C8, D-C15, and L-M3's exclusivity clause** — every disagreement was re-derived against the bytes by this seat before ruling.
**Method (incredulity)**: both challenge files read whole by this seat (1,214 + 704 lines). Every reader verdict that kills, demotes, reframes, or adds was re-checked at the live source with this seat's own instruments. Nothing was executed against parse-that (the PT-03 latch is one-way); every ns/depth figure in either challenge is carried as **MEASURE-AT-OPEN**. Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (all read-only: git/grep/sed + Read)**: `lazy.ts` whole (43); `parser.ts:14-79` (parseState/parseStateInner — `this.state` written at :66/:71 only after `this.parser(state)` at :53; the try/finally at :44-47 wraps `parseStateInner`, so a finally-clear would wipe after EVERY parse; `parseState` **returns the live state** at :74), `:120-190` (`chain` invokes `fn(state.value).parser(state)` at :137 **during** the parse; `map` :151; `mapState`'s `Object.create(state)` view at :177-180), `:437-517` (`call()` flags fast-path :438-440; `flaggedParser` built :492, flagged :496, discarded — `trim()` returns the `whitespaceTrim` parser), `:688-711` (`debug`/`toString`/`Parser.lazy`/`_initWhitespace()` at :711); `debug.ts:1-8` (value imports incl. `getLazyParser`), `:240-350` whole (`PARSER_STRINGS` :245, 8 occurrences, **no clear/reset/delete**; the `case "lazy"` arm :316-327 — `if (!id)` :320 and `if (id) PARSER_STRINGS.set` :339-341 are the printer's only terminators, both disabled by a falsy id 0; `parserString` resolved from `context.parser` at :261-265 **before** the switch — the A.7-cure trap); `state.ts:1-6` (**value** import of `statePrint` from `./debug.js` at :2), `:141-152` (`"lazy"` at :149; `"mergeMemo"` :151), `:178-190` (`createParserContext`); `leaf.ts:4` + context census (`eof` :23, `any` :77, `dispatch` :149, `all` :162, `string` :311, `regex` :367 — **six** `createParserContext(name, undefined, …)` sites) + `:393-399` (`export let whitespace` / `_initWhitespace`, the sole module-scope Parser construction → **`whitespace` consumes id 0**); `core.ts:1-16` (docblock verbatim + the lazy trio at :15); `index.ts:1-14` (trio at :7, Span-excision record :10-12); try/catch census: exactly two `try {` (`parser.ts:44`, `packrat.ts:390`), **zero `catch`** on the parse path; `.call(` census: one live site (`parser.ts:493`) + `state.ts:91-92`; `unsafeCall` callers: **zero**; rust `lazy.rs:40-65` (cache :42-45, `pub fn lazy` :49, the **flag-bypass invariant comment** :58-60 verbatim); parse-that `CLAUDE.md:100-106` ("no decorators" + the stale Span-variants line, this seat reads them at :103/:105 — both challenges cite :104/:106, a ±1 drift that changes nothing); `docs/api.md:170-190` (decorator documented **with the cured signature** `TypedPropertyDescriptor<() => Parser<T>>`; `## Span Variants (span.ts)` still present); `git log -- typescript/src/parse/lazy.ts` → exactly `37f958f`/`e47c241`/`7ec4b31` (byte-frozen 2026-03-30); **value.js incumbent** (the A.5 decider): `src/css/grammar.ts:338-403` (`parseValueInternal` self-recurses at :344/:354/:364/**:383**), `src/css/stylesheet.ts` (`parseStyleBody` :559 ↔ `parseItems` :624 mutually recursive via :573/:736, direct recursion :693/:725, wrapper :743-744; `collect`'s `visit` recurses :756); `parsethat-surface-gaps.mjs` whole (**no 7,761 anywhere**; DEBT-3 rows assert failure-class + arity only, the depth appears only inside a label template); `cand-o/idiom.test.ts:110-170` (walker children = `context.parser instanceof Parser` + `args` filtered `instanceof Parser`; `expect(graph.byName.get("lazy")).toBe(1)`); `cand-o/grammar.ts` `Parser.lazy` census → exactly :283; `X/parse-that/waves/W2.md:360-368` (AC-3(d) steady-state gloss), `:470-478` (K-6/K-7/K-8 verbatim), `:806-835` (G-10/G-11 + the pasted born-RED 7,761), `W1.md:540-575` (G-9 measured-with-margin + the pasted script output + "a stable property"); `registry/harvest/wf_076062ab-dc8.json:327` ("deepest 7,787 … (**7,761 / 7,762 at `--stack-size=984`**)"); value.js `git cat-file -t 2636c238` → `commit` ("docs(X): M-26 marked + COHESION.md authored") — the N-7 decider.
**STOP/law compliance**: `/Users/mkbabb/Programming/parse-that-css-totality-p2` not entered, not created (both challenges verified it ABSENT four times; this seat performed no probe requiring it). No worktree, frozen root, or Codex path. parse-that touched read-only. **Sole write = this file.**

## Scoreboard

**22 confirmed rows (0 BLOCKER · 6 MAJOR · 11 MINOR · 5 INFO)** — all four filed BLOCKERs demoted with written rationale · **18 killed rows** (the union of both readers' registers, every one re-derived; one reader-confirmation REVERSED) · **11 misses admitted** (3 MAJOR-class; two found independently by both readers — convergence is itself evidence) · **7 superlatives sustained** · **6 MEASURE-AT-OPEN rows**.

The module verdict both readers reached and this seat sustains: **`createLazyCached` (lazy.ts:18-24) is six lines of correct, minimal, load-bearing code** — load-bearing for packrat `(id,offset)` soundness, not merely perf — and the corpus proves it from six directions including the Rust lowering's independent convergence on the same cache and the same dispatch bypass. The defects that are genuinely *lazy's own* at full strength are exactly **two**: the dual cache (LZ-1) and the missing depth parameter (LZ-5). Everything else is a dead 14-line decorator counted ten ways, a neighbor-module row honestly declared, or a measurement whose protocol the challenge itself later invalidates.

## The incredulity seat's own work — reader disagreements ruled, one reversal, three clause-strikes

1. **A.5 — reader-A's confirmation REVERSED; reader-B's PRIMARY KILL adopted, re-derived at the bytes.** Challenge-C A.5 claims value.js's incumbent CSS parser "handles **every** nesting axis with an iterative integer counter and **no ceiling at all** … none of them can throw a `RangeError`". Reader-A verified the six `let depth` counter sites and confirmed; reader-B killed it; this seat read the incumbent whole where it matters. **The four entry points A.5 names are precisely the recursive ones**: `parseValueInternal` self-recurses at `grammar.ts:344` (comma), `:354` (slash), `:364` (space), and `:383` (the function-call arm — `calc(calc(…))`, `var(--a, var(--b, …))`); `parseCssValue`/`parseCssValues` (`:393-403`) are wrappers over it. `parseStyleBody` (`stylesheet.ts:559`) and `parseItems` (`:624`) are mutually recursive (`:573`, `:736`) with direct `parseItems` recursion at `:693`/`:725`; `parseStylesheet` (`:743-744`) wraps it. `collect`'s `visit` recurses at `:756` under every `collect*` export. The six counters are the tokenizer's **splitters**, not the tree builder. So the incumbent carries a stack ceiling of exactly the same kind — unmeasured and undeclared — and the "ceiling-free incumbent vs. throwing challenger" framing collapses. **What survives, and the roster carries it**: the incumbent's *malformed-input* failures are `Result`-typed (`stylesheet.ts:464` returns `failure(…"closing brace")`) where `Parser.lazy` throws; on **depth** the two are on the same footing. Reader-A's error is instructive: it verified that the cited counters exist (true) instead of the load-bearing universal ("none can throw") — the L-9.1 scope law in the ColorSpaceSelector apotheosis, in library costume.
2. **D-C2's severity disagreement RULED: MAJOR.** Reader-A's severity kill ("D-C1/D-C2 … MAJOR and MINOR respectively") would put D-C2 at MINOR; reader-B confirmed MAJOR. This seat rules **MAJOR**: the walker blindness is *measured* (challenge-C A.4: the `opt`-inside-`all` behind a lazy edge is invisible), it sits under the ruled band's decisive instrument (`parser-band.md:17`, `W2.md:808-815` G-10), and the admitted miss M2 (the count is a lower bound underwritten by a grep) compounds it. But D-C2's **rationale is corrected**: reader-B's proof cell carries P3-3's killed clause ("every other combinator passes `this` … the graph's only child-link hole"). This seat's leaf.ts census: `eof`/`any`/`dispatch`/`all`/`string`/`regex` all pass `createParserContext(name, undefined, …)` (leaf.ts:23/:77/:149/:162/:311/:367) — `context.parser === undefined` is the *normal* leaf/n-ary shape, and `any`/`all` children are walkable because they sit in `args` as Parsers. The one true distinguishing fact is challenge-C A.4's own: **lazy's `args[0]` is a `Function`, so the `arg instanceof Parser` filter drops it.** Verdict intact, mechanism corrected.
3. **L-M3's exclusivity clause STRUCK inside a confirmed row.** Reader-B's confirmed L-M3 carries "`createLazyCached` is the only construct in the algebra that makes unbounded recursion expressible" — reader-A killed the twin clause in N-1. This seat re-read `chain` (parser.ts:124-144): `fn(state.value).parser(state)` at `:137` invokes the user continuation **during** the parse and may construct anything (including a `memoize()` — the N-1 seam) and expresses a legal back-edge without lazy (`const p: Parser<any> = string("a").chain(() => p)`). `map` (:151) and `mapState` (:180) run user callbacks inside the same `packratEnter` bracket. L-M3 and N-1 both survive **with the clause struck**; any §9b-step-6 cure scoped to lazy alone cures one door of three.
4. **The A.7 cure trap — both readers found it independently; this seat re-derived it as the tie-breaker.** Challenge-C A.7 prices "populate `context.parser` on a lazy node after first build" as a safe **minor** curing D-C1+D-C2 together. Mechanism, re-walked at debug.ts: `parserString` is resolved from `context.parser` at `:261-265` **before** the switch (`:267`); every memo write happens on the way **up** (`:322` after the recursive call returns; `:339-341` gated `if (id)`; `:346` top-level); the `case "lazy"` arm's `id` latch (`:320-326`) is the printer's only cycle terminator and is reachable today only because `context.parser` is `undefined` for lazy nodes. Populate it and the top-level `print(parser)` (id `undefined`) descends the body eagerly with nothing memoized → unbounded recursion on every recursive grammar (`test/print.test.ts:21`'s `mijn` is the suite's own counterexample). **Two independent readers converging on a defect in the challenge's own priced cure, confirmed mechanically by a third seat, is the strongest single result of this adjudication**: the sound cure is an accessor (a new slot or function the printer opts into) — never the canonical child link — with the memo write moved above the recursion.
5. **This seat's own addition — the corpus was internally inconsistent on the depth pin before either challenge measured anything.** `wf_076062ab-dc8.json:327` records "deepest … **7,787** … (**7,761 / 7,762 at `--stack-size=984`**)" — i.e. the corpus filed the pinned pair as a *reduced-stack* reading — while `W1.md:547-549` pastes `deepest OK = 7761` from the same script run **without** any `--stack-size` flag. 7,787 and 7,761 cannot both be the default-stack reading of one stable property. The "a stable property" prose (`W1.md` ~:555, `W2.md:831-832`) was refutable from inside the corpus by reading alone; challenge-C's 8,277 (+6.6%, identical instrument) merely made it undeniable. Every number stays MEASURE-AT-OPEN.
6. **Cite-drift ledger (none verdict-bearing, recorded per D-19)**: challenge-L cites `lazy.ts:36` for the bind (live: **:35**) and `:32-43` for the signature (live: :30-43); both readers cite `CLAUDE.md:104/:106` (this seat reads :103/:105); challenge-C cites `proof-no-dead-combinator.mjs:30-33`/`:11-12` (reader-A re-anchored :29-32/:10-11); challenge-C D-C9 describes `parsethat-surface-gaps.mjs:36-37` as if both rows pin — `:37` (arity) is sound, `:36` asserts the failure class only. Re-resolve all anchors before any cure lands.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law (pt-packrat precedent): **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec · **NO-WAVE-OWNER** where no X·P wave owns it (incumbent parse-that repair; evidence rides the standing O-15 letter channel, owner-held). X·P executes nothing until the owner's begin-word; every routing is a booking, not an act.

| id | sev | claim (adjudicated form) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **LZ-1** (L-M4/D-C1) | **MAJOR** | Two unreconciled caches over one thunk: printing a lazy parser re-runs the body, mints a shadow subtree with fresh ids (memoized into `PARSER_STRINGS` under ids that can never appear in packrat's `(id,offset)` `MEMO`), retained for process life — the WeakMap's weakness is defeated by `context.args` holding `fn`, and no `clearLazyCache()` exists while both siblings ship resets. Demoted from BLOCKER: no wrong answer, trigger is explicit stringification only (`${p}`/`String(p)`/`.toString()`; NOT `console.log`, NOT default `.debug()` — see K-14) | `lazy.ts:5-15` vs `:19-23`; `debug.ts:316-322`; `parser.ts:705`; API-TEST receipts (calls 1→2; ids 42≠47; id-burn doubling) | **W2** — the successor algebra's spec: ONE cache, printer folded onto the execution cache via an **accessor** (M1's constraint: never `context.parser`; memo-write above the recursion). Incumbent repair + `resetLazyCache()` symmetry: **NO-WAVE-OWNER**, O-15 channel. W1 rider: a harness cell must never `toString()` a grammar it also identity-measures |
| **LZ-2** (D-C2 + A.4) | **MAJOR** (ruled, §2) | The realized back-edge parser is structurally unreachable (`cached` has no reader; `args[0]` is a `Function` the `instanceof Parser` filter drops), so the band's graph-walk instrument terminates at every back-edge; G-10/G-11's walks inherit the blind region; the only lookalike accessor (`getLazyParser`) returns LZ-1's phantom | `lazy.ts:19`; `cand-o/idiom.test.ts:135-139`; measured blindness (A.4); leaf.ts census correcting the mechanism (§2) | **REFINEMENT FOLD → W2 G-10/G-11 spec**: the harness descends a lazy edge via `args[0]` exactly once, caches at the node, never via `getLazyParser`; operational rule ratified — *across a lazy edge assert presence, never identity, allocation, or total count* (M2). The accessor itself: W2 candidate-API + NO-WAVE-OWNER incumbent |
| **LZ-3** (L-B2/D-C5) | **MAJOR** | The `lazy` decorator does not tie the recursion knot: a fresh `Parser` + fresh empty cache **per method call** — its docstring's "then caches" is false; a decorator-built grammar mints ~O(depth) parsers forever, `memoize()` can never hit, ids march toward `MEMO_MAX_ID`'s `RangeError`. Demoted from BLOCKER: the consumer it requires cannot exist (LZ-4) | `lazy.ts:26-29` vs `:37-42`; contrast `parser.ts:702-707`; API-TEST receipts (ids 37/38/39; 3 invocations for 3 parses) | **W2** — the KEEP/WRAP/RETIRE table's **RETIRE** verdict owns the capability ruling; the package-side excision (major at 1.x; de-barrel suffices — in-tree callers import `./lazy.js` directly) is the owner's act, booked: **NO-WAVE-OWNER**, O-15 channel |
| **LZ-4** (L-B1/D-C3, mechanism corrected) | **MAJOR** | The decorator is unusable rather than merely broken: under the repo's own tsconfig (no `experimentalDecorators`, TS≥5 standard semantics) a TS consumer writing `@lazy` gets a **compile-time type error** (`ClassMethodDecoratorContext` ⊄ `string`); the runtime `TypeError` at `lazy.ts:35` fires for JS/babel/esbuild consumers or anyone shipping past the diagnostic — both probes hand-built a call shape `tsc` rejects. Zero `@lazy` anywhere in either tree. Demoted from BLOCKER: a type-error-guarded, zero-consumer export | `lazy.ts:30-43` (`descriptor.value!` at :35); tsconfig read whole; both challenges' P1/M probes reinterpreted | **W2 RETIRE** (with LZ-3); doc row rides LZ-12. Ten ledger rows across the two challenges (L-B1·L-B2·L-m3·L-m4·D-C3·D-C4·D-C5·D-C12·D-C14·P3-1) collapse to this one disposition: **delete 14 dead lines** (after the LZ-17 sequencing: make `_initWhitespace` explicit first) |
| **LZ-5** (L-M3/D-C9-surface) | **MAJOR** | No depth parameter, no ceiling, no `catch` anywhere on the parse path (exactly two `try/finally`, zero `catch` — re-verified): the failure mode is an uncaught host `RangeError` escaping `parse()`, unreachable by `recover()`'s documented promise (branches on `state.isError` only). Exclusivity clause STRUCK (§3): `chain` is a second unbounded-recursion spelling and a second construction-during-parse door | `parser.ts:702` arity 1; `:653-688`; try/catch census; `parser.ts:137` | **W2 G-11** owns the class (`ok:false` from a constructed bound, never `RangeError`) with the **W3 rider** (the breach becomes a `ParseIssue` value, K-8/R-LAW-3 class, per the pt-packrat C-8 precedent). A.6 stands ADJUDICATED: the seam (`Parser` + `createParserContext` + `createLazyCached`, all public) suffices to build the bounded back-edge **above** the library, gate-indistinguishable — no 1.1.0 ask required; a library-side arity-2 remains the better cure and is the shape `surface-gaps.mjs:37` reds on |
| **LZ-6** (D-C8, corrected) | **MAJOR** | A **zero-barrel-consumer** trio frozen onto both public entry points at 1.0.0, past the repo's own dead-export precept and excision idiom — and `proof-no-dead-combinator.mjs` enumerates nothing (two hardcoded names), so it greens over `lazy.ts` by construction. Corrections bound in: `getLazyParser` has one workspace consumer (`debug.ts:2,318`) — the shared property is zero *barrel* consumers; and §0's "no parse-that dependency at all" is falsified by value.js's own band workspace (`prototypes/css-parser/package.json` declares `@mkbabb/parse-that: 1.0.0`; the RULED winner consumes `Parser.lazy` at `cand-o/grammar.ts:283`) — zero *shipped* consumers, one ruled prospective consumer | `index.ts:7`; `core.ts:15`; `proof-no-dead-combinator.mjs:10-12,29-32`; package-receipt.json (34 exports, sha-pinned) | **W2** — the RETIRE verdict + G-2's registries-not-counts discipline own the class; the gate's enumerate-the-barrel repair and the de-barrel act: **NO-WAVE-OWNER**, O-15 channel |
| **LZ-7** (L-M2, rescoped) | MINOR | After a thrown `RangeError`, `Parser.state` still holds the previous parse's success (`this.state` assigned only after `this.parser(state)` returns; the :44-47 finally restores only the packrat epoch). Demoted from MAJOR: the falsifier-rejection was refuted — `parseState` **returns** the live diagnostics-bearing state (:74) and `getCollectedDiagnostics()` is a second exit, so `.state` is not the only door; and the challenge's second cure ("in the existing finally") is WRONG — it would wipe `.state` after every parse | `parser.ts:51-75`; API-TEST receipt (stale `offset=2`) | **NO-WAVE-OWNER** — one-line incumbent cure (`this.state = undefined` as `parseStateInner`'s first statement — the first cure only), O-15 channel |
| **LZ-8** (N-1, descoped) | MINOR | `lazy.ts:21` can run `makeMemoized` mid-parse, arming `PACKRAT_ARMED` inside a parse `packratEnter` already declined to epoch; the cells become permanent module-global residue (never read, never freed; `resetPackrat` clears without disarming). No wrong-answer window (both hazard classes traced closed). Exclusivity STRUCK — `chain`/`map`/`mapState` callbacks are the same seam | `packrat.ts:216-217,243-244,280-290`; `lazy.ts:21`; ANALYTIC (unrun by law) | **REFINEMENT FOLD → W2 O-8/K-6**: the anti-latch construction rule must scope ALL user-callback seams, not lazy alone; **W1 rider ratified**: the exit-side `PACKRAT_ARMED` assertion is load-bearing (a cell can arm itself mid-parse). Identity: rides pt-packrat C-2/C-15 — one latch, never re-booked. Falsifier → MEASURE-AT-OPEN #2 |
| **LZ-9** (L-m5/D-C11 + M4 + M8) | MINOR | The flag machinery is dead (sole write `parser.ts:496` on the discarded `flaggedParser`; `FLAG_EOF` never written; `flags === 0` universally, `call()` below :440 unreachable) — and the `.parser()`-not-`.call()` bypass at `lazy.ts:22` is **not an accident**: the Rust lowering documents it as a designed invariant (`lazy.rs:58-60` — "the cached inner parser never has flags set (flags live on the outer wrapper)") that the TS side states nowhere. `.call()` and `unsafeCall` are moreover **unreachable in-tree** (one constructor-internal site; zero `unsafeCall` callers) — one function more dead than either challenge filed | `.flags`/`.call(`/`unsafeCall` censuses (this seat's); `lazy.rs:42-46,58-60` | **REFINEMENT FOLD → W2** enumeration discipline (dead flag machinery = the counter-example class; identity: pt-packrat M3, one row). Incumbent disposition ADJUDICATED: **keep the bypass, fix `trim()`, write the invariant down in TS** — routing lazy through `.call()` would violate the cross-lowering contract, not repair a latency. O-15 channel |
| **LZ-10** (D-C7) | MINOR | `getLazyParser` is a public unconstrained generic memoizer: value-presence guard (an `undefined`-returning thunk re-runs forever), `WeakMap<Function, unknown>` + `cached as T` unchecked cast, published loose at `dist/lazy.d.ts:3` | `lazy.ts:5-15`; API-TEST receipt (3/3 re-runs) | **W2 RETIRE table** (demote to LZ-1's accessor or retire outright); incumbent: NO-WAVE-OWNER |
| **LZ-11** (D-C10/N-5 + M9) | MINOR | `core.ts:3-5`'s "never pulls the diagnostics accumulator, the packrat tier" is false at the bytes (40,576 B chunk, 30.4×) **and structurally at the source** — `parser.ts:7` value-calls `packratEnter/Exit` on the default parse path and `state.ts:2` value-imports `statePrint` → `debug.ts:6` → `isDiagnosticsEnabled` — reachable from the two exports a `/core` consumer cannot decline, so no chunker and no N-4 import surgery can make the sentence true. The json/csv third IS honoured | `core.ts:3-15`; `parser.ts:7,43,47`; `state.ts:2`; `debug.ts:2,6`; `dist/core.js:1` | **REFINEMENT FOLD → W4** subpath-gate spec (payload isolation asserted, never file-existence+`typeof`; identity: pt-packrat C-16/C-9, one row). Incumbent: delete the sentence or split the tier — NO-WAVE-OWNER |
| **LZ-12** (D-C16 + M6 + M7) | MINOR | No non-stale prose contract exists anywhere: `api.md:173-182` documents the crashing decorator and neither live export, `:184` still documents the `*Span` family excised at 1.0.0 — and BOTH strengtheners hold: `api.md` publishes the **cured** decorator signature (`TypedPropertyDescriptor<() => Parser<T>>`, verified this seat) that the shipped `.d.ts` (`() => any`) does not deliver, and `CLAUDE.md` carries the identical Span staleness two lines under the very line D-C16 used as its authority | `docs/api.md:173-190` (this seat's read); `CLAUDE.md:103-105`; `dist/lazy.d.ts:9` | **NO-WAVE-OWNER** — incumbent doc repair, O-15 channel (with pt-packrat C-4/C-5, one letter) |
| **LZ-13** (N-9/P3-6) | MINOR | Zero direct tests for the module; the suite owns **no invocation-count mechanism at all** (`toHaveBeenCalled`/`callCount` → 0 hits), so LZ-1/LZ-3/LZ-14 are unobservable to it; no depth test anywhere — why the 7,761 pin survived four months | coverage greps (both readers, agreeing); P3-6's 3-line red test | **REFINEMENT FOLD → W1** instrument law (the invocation-count red test + the depth-class test are W1 harness obligations); package-side tests owner-held |
| **LZ-14** (L-m1/L-m2) | MINOR | Both cache guards are truthiness/`undefined` tests: a malformed body is retried every invocation (no negative latch) and fails as a nameless raw `TypeError` the naming machinery cannot improve (`context.parser === undefined` → "unknown"; `debug.ts:321`'s `p.id` throws a second `TypeError`) — PT-07's posture at another door | `lazy.ts:8-11,21-22`; API-TEST receipts | **NO-WAVE-OWNER** — discharges with LZ-1's resolution sentinel; W1 §3.9 rider via LZ-20 |
| **LZ-15** (D-C14, demoted) | MINOR | `lazy` is the name parse-that's own Rust lowering gives the **combinator** (`lazy.rs:49`, committed, clean) while TS binds it to the broken decorator — an AC-1 signature-leak class row. Demoted from MAJOR: `CLAUDE.md` names the TS spelling explicitly; fully subsumed by the RETIRE — adds a reason, not a failure | `lazy.rs:49`; `parser.ts:702` | **W2 RETIRE table rider** (naming: post-retire, the free name is unbound and the collision dissolves) |
| **LZ-16** (N-3/N-4/P3-4) | MINOR | The `lazy.ts:17` IC rationale is stale since `7ec4b31` (the mutating strawman it contrasts was deleted 2026-03-30); the parser↔lazy value cycle was engineered around at `37f958f` (`_setParserClass` forward-ref) and re-introduced at `7ec4b31` to serve only the dead decorator; the module is byte-frozen since 03-30, so O-15 measured these exact bytes (staleness doubt removed, methodology doubt intact) | `git log` (3 commits, this seat); `git show` cites (readers') | **NO-WAVE-OWNER** — discharges with the RETIRE (both imports become type-only under `verbatimModuleSyntax`); comment fix rides the same edit |
| **LZ-17** (L-m6 + M5-anchor) | MINOR | `sideEffects:false` vs the load-bearing `_initWhitespace()` module-eval mutation (`leaf.ts:395-399`): drop `parser.js`'s evaluation and `whitespace` is `undefined` at `trim()`'s default param. Anchored by this seat: **`whitespace` is the process's Parser id 0** (sole module-scope construction), which is also what keeps the LZ-M4 id-0 printer latch unreachable — one design carrying two latent rows | `leaf.ts:393-399`; `package.json:6`; module-scope census (this seat) | **NO-WAVE-OWNER** — sequencing law for the RETIRE (explicit init FIRST, then delete); **W4** release-condition rider (artifact truth) |
| **LZ-18** (D-C12/P3-1/P3-2) | INFO | Phantom `T` published at `dist/lazy.d.ts:9`; the file's only `any` un-annotated; `createLazyCached`'s precise-looking `ParserState<T>` signature erased on contact with `ParserFunction<any>` | readers' cites, spot-confirmed | **W2 RETIRE** discharges the first two; the variance row is the algebra-wide typing posture — REFINEMENT FOLD note on W2's candidate typing |
| **LZ-19** (D-C13, trimmed) | INFO | Four names for one concept; one `"lazy"` tag (`state.ts:149`) cannot distinguish three producers (thunk vs bound-method `args[0]`). Alphabetical-first clause KILLED (K-16); the tag itself is what makes the structural walk possible (S-5) | `state.ts:141-152`; `debug.ts:316-318` | **REFINEMENT FOLD → W2** registry discipline: one tag per producer (or typed `args[0]`) for any registry-comparing gate |
| **LZ-20** (N-2) | INFO | PT-07's raw-`TypeError` 5/5 boundary posture recurs at a `./core` public export (`getLazyParser(non-function)`) — the JS-boundary invariant asserted only over `parse`/`parseState` leaves a `./core`-wide hole | `lazy.ts:12`; API-TEST receipt | **REFINEMENT FOLD → W1 §3.9**: the boundary invariant enumerates every value-receiving public export |
| **LZ-21** (D-C6, demoted) | INFO | Dual-package fork of the cache + `Parser` identity across declared `import`/`require` conditions. Demoted from MAJOR: every module global forks identically (`PARSER_ID`, the latch, `MEMO`, `PARSER_STRINGS`…) and `LAZY_PARSER_CACHE` is the least consequential; zero `instanceof Parser` branding in-tree; the only branding consumer is the band walker, ESM-only | readers' structural proof; branding grep | Identity: **rides pt-packrat C-7's REFINEMENT FOLD** (one-format-per-cell; no dual-instance state in the shipped artifact) — never re-booked here |
| **LZ-22** (N-8-actionable + O-15 corroborations) | INFO | Every O-15 byte cite (PT-01 guard/effect split; PT-03 `:678/:682/:714/:722`, two assignments, no disarm; PT-07) reproduces exactly; pass 1's narrowing stands (the diagnostics error path calls `statePrint`, never `parserPrint`). N-8's warrant is corrected (K-12) but its rule survives: pin any gate-read `dist` by sha256 — already discharged for the published tarball (`package-receipt.json`, sha-pinned, 34 exports) | readers' convergent byte cites; receipt | **REFINEMENT FOLD → W1** evidence law (sha-pin before any `dist:line` gate); O-15 corroboration filed to the letter channel |

## Misses admitted into the roster — 11 rows (convergences marked ◆ = found independently by both readers)

| id | sev | claim | disposition |
|---|---|---|---|
| **M1 ◆** | MAJOR (against the proposed cure) | Challenge-C A.7's priced cure ("populate `context.parser`… a minor") breaks `parserPrint` on every recursive grammar — eager pre-switch descent + up-only memo writes + the `id`-latch bypassed (§4, re-derived by this seat) | REFINEMENT FOLD → W2: the accessor spec must be a NEW slot/function; memo-write moves above the recursion; A.7's semver row is re-priced from minor to regression |
| **M2** | MAJOR | `expect(byName.get("lazy")).toBe(1)` is a **lower bound**: the walker counts visited nodes only and terminates at back-edges, so N nested lazies behind the first are invisible; it is correct for cand-O only because `grep` finds exactly one `Parser.lazy` (`grammar.ts:283`) — the gate is underwritten by the very grep the corpus says the graph replaces. Both challenge docs exonerated it in opposite ways | REFINEMENT FOLD → W2 G-10: presence-not-total across lazy edges; the census pairs graph-walk + call-site grep and both are printed |
| **M3** | MAJOR (spec wording) | K-6 as written (`W2.md:473-474`, "parse N distinguishable from parse 1") kills every candidate with a lazy back-edge including the ruled winner — parse #1 pays construction by design; the saving steady-state gloss lives only inside AC-3(d)'s predicted-failure list (`:366-368`), not in the kill rule | REFINEMENT FOLD → W2: K-6 gains "in steady state (parse #N vs parse #2)" — one clause, or `createLazyCached` is out of the algebra §6 keeps |
| **M4 ◆** | MAJOR (reframe of LZ-9) | The thunk-must-not-return-a-flagged-parser precondition is a **designed cross-lowering invariant stated only in the Rust comment** (`lazy.rs:58-60`); TS ships it undocumented and unenforced — `Parser.lazy(() => p.trim())` is correct today only because `trim()` discards the flagged parser | folded into LZ-9's disposition (document in TS; keep bypass; fix `trim()`) |
| **M5** | MINOR (latent) | debug.ts's only cycle guards are truthiness tests on `Parser.id` (`if (!id)` :320, `if (id)` :339) — id 0 is legitimate (`PARSER_ID = 0`), and a lazy inner parser holding id 0 recurses the printer to `RangeError`; unreachable today only because `whitespace` consumes id 0 at module eval — the very call LZ-17 shows a bundler may drop | NO-WAVE-OWNER — rider on LZ-1's printer cure (`id === undefined` sentinel, not truthiness) |
| **M6** | MINOR | `api.md:181` publishes the **cured** decorator signature (`() => Parser<T>` binding `T`) while the tree ships `() => any` — the only prose contract promises the exact repair D-C12 asks for and the bytes don't deliver | folded into LZ-12 |
| **M7 ◆** | INFO | `CLAUDE.md` carries the same 1.0.0 Span-excision staleness as `api.md`, two lines below the line D-C16 cited as its contradicting authority — the witness is a defendant | folded into LZ-12 |
| **M8** | MINOR | `Parser.call()` + `ParserState.unsafeCall()` are unreachable in-tree (censuses §instruments) — one function more dead than filed; the sole live choke-point idiom is `unsafeCallRaw`, the path lazy takes | folded into LZ-9 |
| **M9 ◆** | MINOR | The second module cycle `state → debug → lazy → state` (value imports, verified) is the edge that drags the printer/diagnostics into `/core` — N-4's "sever lazy.ts:1 and the cycle is severed" is half true | folded into LZ-11 |
| **M10** | INFO | `mapState`'s `Object.create(state)` view (parser.ts:177-180) is the tree's one second `ParserState` shape and can be returned onto the threaded path by a user callback — the single counterexample to S-3's one-map census | MEASURE-AT-OPEN #5 (magnitude); shape fact recorded |
| **M11** (this seat) | INFO | The corpus internally carries 7,787 (default stack, harvest :327) beside 7,761 (W1's pasted default-stack run) with the parenthetical mapping 7,761→`--stack-size=984` — mutually inconsistent before either challenge ran; the "stable property" clause was refutable by reading | REFINEMENT FOLD → W1: depth baselines carry `(shape, stack-size, JIT state, prior-overflow)` qualifiers; strike "stable property" from W1/W2 prose |

## Killed-claims register — 18 rows, each re-derived by this seat

| # | killed claim | adjudication |
|---|---|---|
| K-1 | **A.5** — "the incumbent handles every nesting axis iteratively … no ceiling at all … none can throw a `RangeError`" | **KILL SUSTAINED, this seat's own re-derivation is the decider** (§1). Reader-A's confirmation REVERSED. Survivor: the incumbent's malformed-input signalling is `Result`-typed; on depth the two implementations are the same class |
| K-2 | "Three places pin 7,761: O-15, `parsethat-surface-gaps.mjs`, W2.md:831" | KILL SUSTAINED — the script pins **nothing** (read whole: depth appears only in a label template; `:36` asserts the failure class, `:37` the arity — the script already implements the challenge's own recommendation). Two places paste the number |
| K-3 | "a gate asserting `deepest === 7761` is flaky by construction / W1:549/573 + W2:831 will flake as gates" | KILL SUSTAINED — no such gate exists: G-11's GREEN condition is the class; G-9 asserts measured-at-gate-time with declared margin; the pasted numbers are dated baselines. Survivor: the "a stable property" prose over-claim (struck via M11) |
| K-4 | L-M1's headline — "the cold figure is **2.74× lower** than the pinned 7,761" | KILL SUSTAINED — shape and JIT state changed at once (pass 1's grammar ≠ the pin's; N-6 itself proves shape alone moves 3.6× cold-to-cold); the frame arithmetic is wrong (delta 3, not 4). Survivors: the intra-process 2.89× and the overflow hysteresis, single-shape → MEASURE-AT-OPEN |
| K-5 | N-6 item 2 — "the entire achievable win from deleting lazy is ~25% of stack depth" | KILL SUSTAINED — a shape-free ratio, the exact error N-6 convicts PT-04 of; on L-M1's own shape the figure is +50%. The conclusion (depth must be an algebra parameter) survives on other legs |
| K-6 | N-6 item 3 — "PT-04's probe source is not published" | KILL SUSTAINED for the corpus rows (W1.md names the exact command; the grammar is in-tree at `surface-gaps.mjs`); survives only against O-15's letter itself |
| K-7 | L-B1's headline+severity — BLOCKER, "the decorator cannot run under this package's own compiler configuration" | KILL SUSTAINED — the definition compiles; a TS consumer is stopped by a compile-time type error, not a silent throw; both probes hand-built a shape `tsc` rejects; zero consumers exist. Ruled MAJOR (LZ-4) |
| K-8 | BLOCKER severities on D-C1/D-C5 (and D-C2 as filed) | KILL SUSTAINED — none produces a wrong answer on the parse path; D-C1's blast radius further narrowed by K-14. D-C1→MAJOR, D-C5→MAJOR, D-C2→**MAJOR** (reader-A's MINOR overruled, §2). Four filed BLOCKERs → zero |
| K-9 | N-1 + L-M3's lazy-exclusivity clauses ("the one mechanism… the only construct") | KILL SUSTAINED — `chain` (parser.ts:137) constructs during the parse and expresses a back-edge without lazy; `map`/`mapState` callbacks run inside the bracket. Both parent rows survive with clauses struck |
| K-10 | P3-3 — "`context.parser = undefined` is the graph's **only** child-link hole; every other combinator passes `this`" | KILL SUSTAINED at leaf.ts (six `undefined` passes; n-ary children ride `args`); the true distinguisher is A.4's function-not-Parser. P3-3's operational rule survives with the corrected reason (LZ-2) |
| K-11 | L-M2's falsifier-rejection ("`.state` is the only accessor…") + its second cure ("or in the existing finally") | KILL SUSTAINED — `parseState` returns the live state (:74) carrying furthest/expected/suggestions; `getCollectedDiagnostics()` is a second exit; the finally-cure would wipe `.state` after every parse. LZ-7 at MINOR, first cure only |
| K-12 | N-7 — "W1's pinned substrate `2636c238` is not reachable" | KILL SUSTAINED, re-verified by this seat: it is a **value.js** commit (`docs(X): M-26 marked + COHESION.md authored`) — category error. The surviving observation (typescript/ has zero commits since 07-20) is P3-4's |
| K-13 | N-8's warrant — "the chunk hashes happen to match… the sole warrant" | KILL SUSTAINED — O-15 cites a glob, names no hash; the byte cites hold because the source is frozen (zero commits since 07-20 → a 07-29 rebuild is byte-equivalent). The sha-pin rule survives (LZ-22) |
| K-14 | "fires on `console.log` of a parser" (D-C1/D-C15) · "forced by `.debug()`" (L-M4) | KILL SUSTAINED — `console.log` routes through `util.inspect`, never `Parser.prototype.toString`; `.debug()` defaults `recursivePrint = false` and calls `parserPrint` only when true (`debug.ts:363-365`). Trigger set = `${p}` / `String(p)` / explicit `.toString()` |
| K-15 | D-C6 at MAJOR · D-C15 at MAJOR | KILLS SUSTAINED — D-C6 is generic to every module global and least consequential on this one (→ LZ-21 INFO, packrat-identity); D-C15 is contradicted twelve sections later by A.6 (the assembly is composable from shipped exports); its generic residue (default-context parsers are unprintable) recorded at INFO inside LZ-1's cure scope |
| K-16 | D-C13's "a consumer scanning the barrel finds the decorator **first — alphabetically**" | KILL SUSTAINED — `createLazyCached` < `getLazyParser` < `lazy`; barrel order identical. Only the exact-token-match half survives |
| K-17 | §1's call-site census ("14 in-tree call sites") and P3-6's "eight fixtures" | KILL SUSTAINED — the listed sites sum 16; the sweep misses `json.test.ts:17,27` + every `scripts/` site; live count 20 (+4 benches). No verdict turned on it |
| K-18 | D-C8's "getLazyParser has zero workspace consumers" + §0's "does not depend on parse-that at all" | KILLS SUSTAINED AS CORRECTIONS — one consumer (`debug.ts:2,318`); zero **barrel** consumers is the honest property; and the band workspace declares `@mkbabb/parse-that: 1.0.0` with the ruled winner on `Parser.lazy` — zero shipped consumers, one ruled prospective consumer. Both fold into LZ-6's semver calculus |

## Superlatives — 7 sustained (L-18 runs both ways)

1. **S-1/S-6 (merged)** — `createLazyCached`'s closure cache is load-bearing for packrat **soundness**: it fixes `Parser.id` across parses against the `(id,offset)` memo key, and it is keyed on nothing (input-independent by construction) — the two properties that jointly make `(id,offset)` a sound key, and why lazy appears in neither PT-B1 nor PT-Q1. The `lazy.ts:17` comment sells an IC nicety; the mechanism is doing soundness work.
2. **S-2/S-3** — the hot arrow is allocation-free and monomorphic, and `Parser`/`ParserState` are hidden-class transition-free at the emitted bytes (`state;`/`expected;` initialiser-free, so error-path writes add no property) — verified at the dist by three seats.
3. **S-5** — deferring construction is what makes the back-edge **structurally visible** (`"lazy"` a first-class `parserNames` member; `fn` retained at `args[0]`): the only reason `parser-band.md`'s decisive graph-walk instrument is constructible at all. The superlative and LZ-1 are one design choice read from two sides — which is why the accessor cure (M1-constrained) preserves it whole.
4. **Deferral is to first PARSE, not first construction** (`lazy.ts:21` sits inside the returned arrow) — what lets `json.ts:25,28` write mutually-recursive module-level `const`s without a TDZ throw. The easy wrong choice was avoided.
5. **Cross-lowering convergence** (both readers' missed-superlative, sustained): `lazy.rs:42-46` and `lazy.ts:19-21` independently reached the same one-shot cache, and the Rust side reached AND DOCUMENTED the same dispatch bypass TS takes silently — the strongest evidence in the corpus that these six lines are right, sitting nine lines past where D-C14 stopped reading.
6. **A.6/S-4** — the public surface is *exactly sufficient* to discharge DEBT-3 above the library (seam exported, assembly not): measured, gate-indistinguishable, and it is what demotes the depth cure from a 1.1.0 ask to a consumer act.
7. **Evidence-hygiene superlatives, both directions**: the published package is sha-pinned better than N-8 asked (package-receipt.json: tarball sha256 + installed-tree sha256 + full export list); the parse path's two `try/finally`-zero-`catch` posture never converts a host error into a silent `ok:false`; and challenge-C's D-C9 was the only depth measurement in either document that re-ran the corpus's own instrument and reported the +6.6% disagreement without inflating it. The shared failure mode was severity: 4 BLOCKERs, none a wrong answer, ten rows on one dead 14-line function.

## MEASURE-AT-OPEN residue (W1 harness cells; every number in this corpus is carried, none re-derived here)

1. **Depth-ceiling family** — reconcile 2,833 (cold, shape A) / 6,203 (cold, shape B) / 10,336×3 (cold, wrapper-nest) / 7,761-7,762 (`--stack-size=984` per harvest; also W1's pasted default run — the M11 contradiction) / 7,787 (default, harvest) / 8,277 (default, challenge-C, same instrument) under W1 cell law: one shape per cell, stack-size declared, cold/warm separated, prior-overflow state pinned. Gate predicates stay class-only (`RangeError thrown` → must become `ok:false`; arity).
2. **LZ-8 falsifier** (N-1): `Parser.lazy(() => memoize(inner))`, parse once, assert the next `packratEnter` snapshot's `memo.size > 0` — fresh disposable process, latch dies with it. If 0, LZ-8 is withdrawn.
3. **LZ-1 quantification**: id-burn doubling on `toString()` (receipt exists: 6+6) + retained-heap witness, under a sha-pinned dist.
4. **Hysteresis + JIT spread** (K-4 survivors): single-shape, intra-process 2.89× and the post-overflow collapse (3,000 fails where 1,000 passes) — decides whether W1's bench cells must be one-parse-per-process near depth work.
5. **M10**: `mapState` foreign-shape magnitude on the threaded path (`--trace-maps` class instrument).
6. **S-4's IC probe** (`--trace-ic` over ≥16 lazy nodes) — INFO-grade, only if W2's candidate multiplies back-edges.

## Closing verdict

**KEEP THE ENGINE, RETIRE THE COSTUME, BOUND THE DEPTH, AND FIX THE SPECS THE FINDINGS SHARPENED.** `createLazyCached` survives two hostile challenge documents, two hostile readers, and this seat's incredulity with its six lines not merely intact but *proven load-bearing* — the correct terminal statement about the module's core. The adjudicated defect mass lives in (i) the dead decorator (one RETIRE discharges ten filed rows), (ii) the dual cache and its unreachable realized parser (one accessor + one fold discharges both MAJORs — under M1's constraint, because the challenge's own priced cure would crash the printer), (iii) the missing depth parameter (curable above the library today per A.6; properly an arity-2 algebra parameter per W2 G-11/W3), and (iv) prose/spec drift on both sides of the fence (the package's two stale documents; the corpus's "stable property" over-claim and K-6's missing steady-state clause). The A.5 reversal matters beyond this module: the incumbent and the challenger are the **same class** on depth — the honest comparison is `Result`-typed malformed-input signalling vs thrown ceilings in both, which is exactly the W3 totality frame, not a lazy-specific indictment. Nothing here blocks X·P; every routing above is a booking awaiting the owner's begin-word.

— end of apotheosis. The two challenge files and both reader returns remain immutable beside this record per E-1/E-3.
