claude-fable-5

# fr-GalleryInfiniteGrid — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-24 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/gallery/GalleryInfiniteGrid.vue` (54 lines) + its consumer chain (GalleryView / stores/gallery / lib/api / lib/scheduler) and the installed `@mkbabb/glass-ui@4.0.0` `infinite-scroll` primitive it adapts.
**Substrate**: fourier HEAD `cd26c65` (2026-07-03), re-pinned by this seat (`git log -1`); 28 dirty rows repo-wide, **the subject file is CLEAN at HEAD** (`git status --porcelain` on the path → 0). Producer glass-ui working tree is 8.0.0; **every producer claim below was discharged at `git show v7.0.0:`/`v8.0.0:` per BASELINE LAW**, and the installed pin was read at the bytes (`node_modules/@mkbabb/glass-ui`, version 4.0.0 node-verified).
**Corpus**: 44 accusation rows across three axes — challenge-D (design, 14 defects + 5 superlatives, opus-5[1m]) · challenge-L (library, 12 defects + 4 superlatives, opus-5[1m]) · challenge-C (consumption, 18 defects + 5 superlatives, opus-5[1m]) — at `docs/tranches/V/megatranche/audit/fourier-components/GalleryInfiniteGrid/`.
**Readers adjudicated**: reader-A (design-and-usage hostile re-read) · reader-B (library-and-code hostile re-read). Both returns arrived through the orchestration channel; every load-bearing or disputed cell was **re-derived by this seat against the trees with its own instruments** before adoption. Registry-currency note that decides several rows: **reader-A's registry sweep predates the same-day banking of `fr-GalleryCard.md`** (its return asserts "no fr-GalleryCard registry row exists"; the row exists today, dated 2026-08-24) — reader-B's fold targets are real, and four of reader-A's "novel" rows re-disposition as folds (K-16).
**Arbiter's own instruments this session (all static + node + git-show, zero browser probes — probe parsimony; product source READ-ONLY)**: whole-file reads of GalleryInfiniteGrid.vue (54) and the installed `dist/infinite-scroll.js` (73 — `check()` at :20-22, the isLoading re-arm watch at :25-26, `root: e.scrollContainer?.value ?? null` at :13, `ref_key:"scrollContainer"` on the component's own root div at :57-59, the sentinel child at :62-67, both slot wrappers + the `h-5 w-5 animate-spin rounded-pill` default at :35-40/:68, `threshold` default 200 at :4/:46); stores/gallery.ts :40-105 + :130-200 (guard :61 · `limit: 20` :65 · `processInChunks {chunkSize:24}` :74 · `hasMore = result.has_more` :76 · catch/finally :77-81 · `resetAndFetch` clears sync :85, re-asserts `hasMore` :87, sends `{limit,sort,owner}` :90-94 · setTier→`resetAndFetch()` :143 · deleteEntry→ :155 · softDelete splice :170-171 · restore unshift :182 · `like()` = `const liked = true` local bump :189-199); GalleryView.vue :64-70 (the featured/non-featured partition), :95-98/:111-114 (the two refetch watchers), :220 (`overflow-y-auto h-full` — the C-2 kill decider), :266/:270 (the marquee double guard), :290-297 (mount guard, `nonFeaturedEntries`, fused `loading`, `@load-more`); lib/api.ts :397-407 (`listVisualizations` serialises exactly limit/sort/cursor/owner); lib/scheduler.ts :44-54 (default `chunkSize ?? 32`, yield predicate :50) + repo grep (`processInChunks`/`yieldToMain` = 1 call site each); lib/colors.ts :20-96 (all four `cssVarToHex` arms, both `#888888` returns, `resolveVizColors` writes all five); basis-display.ts whole (7 lines, plain reads); router/index.ts :81-90 (`/gallery` lazy); web/src/style.css :1-5 (glass styles import), :36-50 (18px root below 768px), :113-126 (**fourier overrides `--viz-amber` + `--section-color-5` ONLY — no `--section-color-4`/`--viz-green` override, the R-3 decider**), :133-143 (`.gallery-card:focus-visible` pinned by class to the card ROOT — the FR-GIG-13 decider); GalleryCard.vue :65-110 (root role/tabindex/`aria-label="Open ${image_slug}"`/keydown + `alt="entry.image_slug"` + `loading="lazy"`), :148-152 (the surface-less tier box, Crown at :150), :199-206 (`--deferred-section-size: 17rem`), :296-308 (like-bounce + the vacuous PRM block); installed glass tokens: color-radius.css :243-268 (**`--viz-fourier/chebyshev/legendre` bare `oklch()`; `--viz-amber: var(--section-color-5)`; `--viz-green: var(--section-color-4)`; `--section-color-4: oklch(0.551 0.088 171.1)` at :245**), dark-arm.css :113-115, **light-dark.css :145-147 (`--viz-fourier: light-dark(oklch(…), oklch(…))` — VERBATIM the C axis's quoted block, the R-4 decider)**, `@property` grep over tokens/ → **0** `--viz-*` registrations; a11y-overrides.css :7-17 (the universal `*:not([data-allow-motion])` PRM blanket, `animation-duration: 0.01ms !important`) + utilities.css:19 import; producer at the tags: `git show v7.0.0:src/styles/theme/bridges.css` → **`grep -c initial` = 0**, :35-36 = `--text-display-3/-4` (the D-1 decider), `git show v8.0.0:…bridges.css:25-26` = `--text-sm: initial; --text-xs: initial`, `git merge-base --is-ancestor 35a30fbb` → **NOT v7 / IS v8**; `v7.0.0:…useInfiniteScroll.ts` (:16-18 `shouldLoad` · :20-28 `handleIntersect` fires on `isIntersecting && shouldLoad()` with `unobserve` first · **:33 `root: options.scrollContainer?.value ?? null` UNCHANGED** · :30-37 `setupObserver` = teardown + fresh observer + observe · :46-48 `check()` = `setupObserver(sentinelRef.value)` · :57-66 the nextTick re-arm on every isLoading true→false edge · :68 `onScopeDispose(teardown)`); `v7.0.0:…InfiniteScroll.vue` = **60 lines**, `<div ref="scrollContainer">` at :39 (no overflow), the same `animate-spin rounded-pill` default at :49-51, `v-else-if` end wrapper with `text-sm` at :56, **no DotRing (`git ls-tree v7.0.0 | grep -ci dot-ring` = 0), no sr-only, no `role="status"` anywhere**; `v8.0.0:…InfiniteScroll.vue` :38-39/:64-70/:76-77/:84-86 = the Transition + DotRing + `text-small` + live-region cells the challenges attributed to 7.0.0, line-for-line; `v8.0.0:…dot-ring.css:239-247` (PRM `animation: none` **with the authored "static unequal ring still says in-flight" park** — the FR-GIG-12 footnote corrector); export maps by node: **4.0.0 = 80 keys / v7.0.0 = 74 keys; `./infinite-scroll` PRESENT in both; `./skeleton` ABSENT in both; `./checkbox` ABSENT in both (arrives 8.0.0)**; **installed-pin Skeleton: `dist/index.d.ts:28` `export * from "./components/ui/skeleton"`, `dist/components/ui/skeleton/` present, `dist/glass-ui.js` exports `Skeleton` — importable from the barrel TODAY**; `--radius-pill: 9999px` at theme/radius.css:**25**; **own `@vue/compiler-sfc` `compileTemplate` run on the live SFC** reproducing both readers' output token-for-token (`onClick: $event => (_ctx.emit('card-click', entry))` UNCACHED · `onLike/onSetTier/onDelete/onToggleSelect` = `_cache[0..3]` · dynamic-props `["entry","admin-mode","liked-hashes","selected","onClick"]` · the `#loading` subtree static-hoisted at `_cache[5]`); api truth: `grep -c tier api/models/visualization.py` = **0**, admin `$set` writers at admin.py:183/:432/:438, visualizations.py `@router.` census = **13**, gallery.py:3 "carved out" header; types.ts:96 (`GalleryTier` export) / :211 (`content_hash … never identity`) / :232 (`tier?`); e2e/gallery.spec.ts = 139 lines, paging-vocabulary grep = **0**; web/package.json (`private: true`; lucide-vue-next/reka-ui/cva/clsx/tailwind-merge AND vite/typescript/vue-tsc all devDependencies); registry sweeps: `InfiniteScroll|infinite-scroll` over adjudicated/ → passing mentions only (fr-GalleryCardModal GCM-24, fr-PathPreview) — **no banked row adjudicates the primitive's behaviour**; fold-target reads: fr-GalleryCard.md (L·M-2 :49 · G-LC1 :53 · G-LC3 :73 · K-4 :94 · K-9 :99 · K-13 :103 · D-7 :52 · the D-12..23 alt/aria-FK cluster :72 · calibration note :130), fr-GalleryFeaturedCarousel.md (FR-GFC-1/-2/-3/-4/-8/-9/-17/-18/-19/-21/-22/-24/-25/-27/-29 · R-3/K4 · K5 · cursors credit :103 · baseline-drift note :126), fr-GalleryCardModal.md (GCM-20/-21/-24 · K-9 "default entry: both sites agree grey"), fr-App.md B-1 (**"the sole survivor `--viz-amber`" — the banked concurrence for R-3**), fr-BasisCanvas.md D-1/BC-1/C-1, fr-AdminUserList.md FR-AUL-42; corpus quotes re-read line-exact: lane-frontend.md:113/:568/:636, CENSUS-2026-08-03.md:254-258.

## Scoreboard

**34 rows ADJUDICATED-CONFIRMED (0 BLOCKER · 9 MAJOR · 19 MINOR · 6 INFO), of which 14 FOLD BY REFERENCE to banked registry rows · 17 kills in the killed register (4 minted or extended by this seat, including one kill-of-a-kill) · 9 missed finds adopted after re-proof (2 as folds onto banked rows) · 8 superlatives with provenance.**

Blocker-weight survivor: **FR-GIG-1** — at the installed pin the grid drains the entire collection on mount (`check()` fires without consulting intersection; every isLoading true→false edge re-arms it; independently, the observer root is the primitive's own non-scrolling wrapper so the sentinel intersects from first observation). Ruled MAJOR-with-blocker-weight on the registry's demotion discipline; **three BLOCKER filings preserved in dissent** (L axis, C axis, reader-B).

Headline cure-integrity ruling: **the census's [P0] tri-package bump does NOT cure the drain** (FR-GIG-5). The v7.0.0 `check()` fix is real and is defeated by the observer-root geometry the same file never changed — booking the drain against F.W1 would close the wave green on a live runaway. The cure is a **producer change** (glass-ui BH relay, standing owner edict).

Status law: dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25); nothing here stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-A | reader-B | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **Drain severity** (L-1/C-1 ⊕ C-2) | MAJOR (blocker-weight), demoted per registry discipline | BLOCKER | **MAJOR (blocker-weight); three BLOCKER filings in dissent.** | Family precedent governs (fr-GalleryFeaturedCarousel R-5: "grades grade the populated surface"; fr-GalleryCard's sole BLOCKER removes a whole capability from a user class). The drain destroys no data, crashes nothing, and is masked on the young corpus by exactly TWO live maskers (`loading="lazy"` GalleryCard:103; `.deferred-section` 17rem :71/:206) — the third claimed masker is killed (K-15: the chunker never yields at page ≤ 20). Re-opener: if SS-13 measures the frame-cadence retry storm or a ⌈N/20⌉ burst at production scale, this grade re-opens. |
| R-2 | **Does the F.W1 bump cure the drain?** (L's carry row vs both readers' missed find) | KILLED the carry; filed M1 | KILLED the carry; filed MISS-1 | **Carry KILLED; the missed find is re-proven by this seat at the tag and booked FR-GIG-5 (MAJOR).** | My `git show v7.0.0`: `useInfiniteScroll.ts:46-48` `check()` = `setupObserver(...)` (teardown + **fresh** observer + observe), `:33` `root: options.scrollContainer?.value ?? null` byte-unchanged, `InfiniteScroll.vue:39` still binds its own no-overflow `<div ref="scrollContainer">`, `:57-66` re-arms on every isLoading false edge. A fresh IntersectionObserver always delivers an initial record; with a non-clipped element root the root rectangle is the element's own box (ancestors above the root never clip), the sentinel is its normal-flow child ⇒ `isIntersecting: true` ⇒ `handleIntersect` (:20-28) fires `onLoadMore`. The v7 cure fixes leg A and leaves leg B whole; the loop survives 4.0.0 → 7.0.0 → 8.0.0. The D axis sensed this ("survives the uplift") without the mechanism; the C axis's repair-order item 2 named the correct cure; the L axis's carry is the one that is wrong. |
| R-3 | **C-6 survivor cell** — which `--viz-*` tokens survive `resolveVizColors()`? | Confirmed C's "only `--viz-amber`/`--viz-green` survive" | Killed the cell: `--viz-green` greys too; four of five | **reader-B wins — reader-A's adopted cell KILLED (K-8), with banked concurrence.** | My bytes: color-radius.css:267 `--viz-green: var(--section-color-4)`; fourier's style.css overrides `--viz-amber` AND `--section-color-5` (:120-126) but **never `--section-color-4`**, which glass-ui declares `oklch(0.551 0.088 171.1)` (:245). Custom-property computed values substitute `var()` — `cssVarToHex` receives an `oklch()` stream, matches none of its four arms, returns `#888888` (:54). Four of five grey; sole survivor `--viz-amber`. **Banked fr-App B-1 already says "the sole survivor `--viz-amber`"** — reader-A's adoption contradicted both the bytes and the registry. |
| R-4 | **C-6's `light-dark()` quotation** | KILLED it ("not in the tree at any line") | Genuine — sourced from light-dark.css | **reader-B wins — reader-A's kill is itself KILLED (kill-of-a-kill, K-8b).** | My read: `dist/styles/tokens/light-dark.css:145-147` carries `--viz-fourier: light-dark(oklch(0.579 0.201 30.4), oklch(0.693 0.151 28.1));` (+chebyshev/legendre) — **verbatim** the C axis's quoted block. reader-A read color-radius.css + dark-arm.css (both real, both bare `oklch()`) and declared the third arm nonexistent. The mechanism is unaffected in every arm (no form parses; 0 `@property` registrations), but an axis's true quotation must not die to an incomplete grep. L-18 runs against re-readers too. |
| R-5 | **M5 (thumbnail alt) novelty** | Net-new missed find ("no fr-GalleryCard row exists to have banked it") | — (not filed) | **FOLD BY REFERENCE — the registry sweep was stale (K-16).** | fr-GalleryCard.md exists (banked 2026-08-24) and its D-7 (MAJOR) + D-12..23 cluster explicitly bank "the card's label, `alt` and `aria-label` all spend the machine FK". My read confirms the facts (GalleryCard:74 `aria-label="Open ${entry.image_slug}"`, :100-101 `:alt="entry.image_slug"`). The one net-new grain — the alt DUPLICATES the enclosing button's accessible name, so the correct shape is `alt=""` decorative — rides the banked row as a cure note, not a new booking. |
| R-6 | **Whole-Set `likedHashes` + uncached `onClick`** — novel-but-narrowed vs fold | Narrowed-novel (cited FR-GFC-19 + FR-GFC-9) | FOLD to fr-GalleryCard L·M-2 + FR-GFC-19; joint booked as MISS-2 | **reader-B's disposition wins: both halves FOLD; the composition books (FR-GIG-14).** | Banked fr-GalleryCard L·M-2 (MAJOR) already cites "GalleryInfiniteGrid:35 vs :36" as its evidence contrast — the grid site is inside that identity. FR-GFC-19 owns the onClick cache escape ("fixes the grid's identical wart in the same edit"). The unbanked grain is the INTERACTION: my own `compileTemplate` run shows the uncached `onClick` in the dynamic-props array, so every card patches on every parent render regardless of `likedHashes` — the banked one-line `:liked` hoist delivers zero measured win unless the `click: [entry]` payload cure lands in the same edit. One repair unit, booked FR-GIG-14. |
| R-7 | **D-13/D-D-13 (filters never serialised)** | Folded into banked FR-GFC-1/GCM-24 | Re-graded standalone MAJOR | **FOLD sustained — identity guard beats re-grade.** | The dead-controls arm is banked inside FR-GFC-1 (MISS-1 blast-radius arm: search-pause/filter-change fire `resetAndFetch()`; zero filter readers; `{limit,sort,owner}` only — all re-verified by my reads of GalleryView:95-98/:111-114 + api.ts:397-407) and the param hole is FR-GFC-4 (F.W5-W8). reader-B's severity input is preserved for the agglomeration; the genuinely new increment books as a rider: under the drain, one tier click costs ⌈N/20⌉ round trips, not one. |
| R-8 | **D-8's Skeleton-availability cell** | KILLED at the installed bytes (barrel-importable at 4.0.0) | Verified barrel-only at the v7 tag; did not check the pin | **reader-A wins; ratified by this seat's own node reads.** | `dist/index.d.ts:28` re-exports `./components/ui/skeleton`; the compiled `Skeleton` is in `dist/glass-ui.js`; no `./skeleton` subpath at 4.0.0 (80 keys) or v7.0.0 (74 keys). D-8's cure is uplift-free TODAY, and a hand-rolled local placeholder would have been a glass-ui-first violation manufactured by a false negative. |
| R-9 | **Grid-track row severity** (D-9/L-11) | MINOR | INFO | **MINOR; reader-B's INFO preserved as dissent.** | The only inline style in the file, the only `auto-fill` in the tree, rem-coupled to the 768px root fork (my style.css read) — a real untokenized coupling with a benign direction. Both arithmetic corrections adopted (K-10). |
| R-10 | **C-14 (devDependencies)** | Confirmed MINOR, folded FR-GFC-21 | Killed the failure mode as vacuous, folded the residue | **reader-B's sharper form adopted; fold unchanged.** | My package.json read: `private: true`; vite/typescript/vue-tsc/tailwindcss are ALL devDependencies — `npm ci --omit=dev` cannot build the app at all, and a bundled private SPA has no production-install path where the split bites. The classification-hygiene residue folds to banked FR-GFC-21 (which already killed the uplift-delta half at K9). |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · disposition → route.** Full proof cells live in the immutable challenge files and reader returns; anchors re-verified by this seat unless marked (adopted). Line anchors are fourier HEAD `cd26c65` and the installed 4.0.0 dist; re-resolve before executing any cure.

### MAJOR

- **FR-GIG-1 = L-1 / C-1 ⊕ C-2 — MAJOR (blocker-weight; three BLOCKER filings in dissent) — the drain, both legs.** Leg A (trigger): `dist/infinite-scroll.js:20-22` `check()` fires `onLoadMore` on `sentinel && hasMore && !isLoading` — never consulting any intersection record — and `:25-26` re-arms it via rAF on **every** isLoading true→false edge; the consumer completes the circuit (GalleryInfiniteGrid:28 → GalleryView:292 fused `loading`, :297 `fetchNextPage()` → gallery.ts:61 re-entrancy-only guard, :80 the `finally` edge). Leg B (geometry, survives every pin): `:13` `root:` the primitive's OWN root `<div>` (`ref_key:"scrollContainer"` :57-59), no overflow at any consumer (grid :26; the real scroll port is GalleryView:220, an ANCESTOR of the root and therefore non-clipping) — the sentinel is a child of the root, intersecting from first observation. Loop terminates only on `has_more:false`: ⌈N/20⌉ automatic round trips, every non-featured row mounted, zero scrolling. Two live maskers explain survival (lazy thumbnails; `content-visibility` 17rem); the claimed third is dead (K-15). `hasMore = ref(true)` from mount (FR-GIG-18) is the ignition condition. Registry-swept: unbanked (no adjudicated row covers the primitive). **ADJUDICATED → producer cure via the standing glass-ui BH relay** (root must default `null`, or a real scroll port must be required; `check()` must consult intersection) **+ F.W3/W4 consumer brake rider** (the adapter is the layer that must bound its primitive: page cap / in-view assertion / error gate). Magnitude → SS-13.
- **FR-GIG-2 = D-4 / L-2 — MAJOR.** No error posture anywhere on the load path: six props, six emits, no `error`, no `#error`, no retry, no `aria-busy` (grid :6-22, :44-51); the store toasts and leaves `hasMore` true, clearing `loadingMore` in `finally` (gallery.ts:77-81) — which IS the re-arm edge, so a failing `GET /api/visualizations` re-requests at frame cadence with one toast per iteration. The dist has no error concept (props :43-47 = hasMore/isLoading/threshold). State coverage of the component's own vocabulary: loading ✓, exhausted ✓, empty ✗, first-load ✗, error ✗. Unbanked (fr-GalleryCard D-6 is image posture; FR-GFC-1 is self-destruction — different organs). **ADJUDICATED → F.W3/W4** (store sets `hasMore=false` on catch + an in-flow error/retry affordance). Retry rate → SS-13.
- **FR-GIG-3 = D-6 / C-12 — MAJOR.** The app's primary list has no list semantics and no live region: `:29` is a bare `<div class="grid">` of `role="button"` children (GalleryCard:72-73), `:27` a static `<p>` with no `aria-live`, the spinner (:46) no `role="status"`/`aria-label`; the installed primitive supplies only the `aria-hidden` sentinel. **At the census's F.W1 target the surface stays TOTALLY silent** — my tag read: v7.0.0's InfiniteScroll.vue (60 lines) has no live region; the sr-only `role="status"` span is an 8.0.0 fact (K-3) — so the defect is larger after the uplift than the D axis stated, not smaller. The codebase knows the idiom three screens away (GalleryView:309-311 toolbar; the card's documented ARIA provenance comment :65-69). **ADJUDICATED → F.W3/W4**, one cure unit with FR-GIG-11/FR-GIG-13 (the PRM-frozen indicator and the focus-ring cure-guard). SR observation → SS-13.
- **FR-GIG-4 = D-8 ⊕ C-15(fusion half) — MAJOR.** No first-load state: GalleryView:290 mounts the grid on `entries.length || loading` and :292 feeds the FUSED `gallery.loading || gallery.loadingMore` while `entries === []` — the primary route's first paint is "0 loaded" over an empty rectangle with a bottom-anchored spinner beneath nothing, and the fused flag is the reason the component cannot distinguish first-load from append (C-15's sharper half, unbanked). Geometry for the cure exists in-tree (`aspect-[4/3]`, `--deferred-section-size: 17rem`), and the Skeleton cure is **available TODAY at the installed barrel** (R-8). Cure-guard (ColorSpaceSelector addendum §2; K5 precedent): banked FR-GFC-1/MISS-1 proves `resetAndFetch()` fires on every search-pause/filter-change — a skeleton keyed on the raw prop would flash on each; gate on `loading && !entries.length`. **ADJUDICATED → F.W3/W4** (one unit with FR-GIG-7's caption/noun repair).
- **FR-GIG-5 = M1/MISS-1 (both readers; re-proven by this seat at the tag — see R-2) — MAJOR, cure-integrity class.** The [P0] tri-package bump (lane-frontend.md:636, re-read verbatim) does not cure FR-GIG-1: v7.0.0 fixes `check()`'s trigger and leaves the observer-root geometry byte-identical (`:33`; HEAD 8.0.0 identical), so the re-observation loop reproduces the drain through a different door. The L axis's carry row ("cured at 7.0.0 … rides the P0 bump") is the corpus's only routing for its heaviest defect and it is wrong. **ADJUDICATED → glass-ui BH relay (producer fix) + an explicit F.W1 spec footnote: the uplift transaction must NOT be credited with curing the drain.**
- **C-6 — MAJOR here · FOLDS BY REFERENCE to banked fr-BasisCanvas D-1/BC-1/C-1 via fr-App B-1 + GCM-20 (→ F.W1). Do NOT re-book.** `cssVarToHex` (four arms, `#888888` fallthrough — my read) cannot parse any arm of the `--viz-*` forms (bare `oklch()` in color-radius/dark-arm; `light-dark(oklch…)` in light-dark.css; 0 `@property` registrations). This record's contributions to the banked identity: the survivor-cell record corrected at the bytes (R-3 — **four of five grey; sole survivor `--viz-amber`**, concurring with fr-App B-1) and the quotation-provenance correction (R-4 — the C axis's `light-dark()` block is genuine, at light-dark.css:145-147). Rides banked FR-GFC-8's F.W2 wave-lock.
- **C-5 — MAJOR here · FOLDS BY REFERENCE to banked FR-GFC-8 (→ F.W2) + GCM-21. Do NOT re-book.** `basis-display.ts:1-7` (read whole) snapshots the reactive palette at module eval; `--pill-c` (GalleryCard:121) can never track the MutationObserver re-resolve. The absolutization ("forever", "two defects cancelling") is killed AGAIN by banked K4/R-3 + GCM K-9 (K-7): path-dependent capture — deep-link `/gallery` = fallback brand hexes; in-app/default entry = post-poison `#888888`; theme toggle stranded on every path. Reader-A's M4 ("in-app = grey", inverting C-6's masking claim on the common path) **folds onto GCM-21/K-9, which already banks it** ("on the default entry both sites agree grey") — adopted as an amendment note on FR-GFC-8's "capture-time resolved values" phrasing, not a row. Every witness pins the entry route.
- **C-3 — MAJOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-3 (→ F.W5-W8). Do NOT re-book.** The `like` emit terminates in nothing: 13 routes on visualizations.py (my census), zero like verbs anywhere, gallery.py:3's header contradiction, `const liked = true` (gallery.ts:195), `aria-pressed` over it (GalleryCard:140). C's BLOCKER grade over the banked MAJOR is preserved as dissent. C-3's genuinely new grain — the **orphan-client class** (a published emit contract with no reachable operation, the R6-8 degenerate case) — is carried **NO-WAVE-OWNER → SS-3/SS-4** as census methodology.
- **C-4 / D-13 — MAJOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-1 (incl. its MISS-1 dead-controls arm) + GCM-24 (→ F.W3/W4) and FR-GFC-4 (→ F.W5-W8). Do NOT re-book** (R-7). `set-tier`/`delete` end in `resetAndFetch()` (gallery.ts:143/:155, my reads; the in-place patch idiom sits unused at :197/:208); searchQuery/tierFilter/basisFilter never reach the wire while both watchers fire refetches. New rider carried on the banked rows: **coupled to FR-GIG-1, one tier click costs ⌈N/20⌉ round trips and a full N-card unmount/remount.**

### MINOR

- **FR-GIG-6 = D-2 / L-5 / C-8 — MINOR (D and C filed MAJOR — dissents preserved).** Both slot overrides re-implement the producer's wrappers INSIDE them: `:45` duplicates `flex justify-center py-4` exactly; `:50` restates the end wrapper's classes and demotes `text-sm`→`text-xs`; net 2rem+2rem chrome bands against a 0.75rem gutter; the spinner fork trades `rounded-pill` (`--radius-pill: 9999px`, radius.css:25 — cited :110 by the challenge, corrected) for stock `rounded-full` to buy 0.25rem. Doubling survives the F.W1 target (v7:46/:56 both wrap — my tag read); the "v7 Transition/`text-small`" falsifier cells are v8 facts (K-3). Runtime cost nil (the `#loading` subtree is static-hoisted `_cache[5]` — my compile). Deletion-only cure. **→ F.W3/W4.**
- **FR-GIG-7 = D-5 / L-6 / C-15 — MINOR (D filed MAJOR — dissent preserved).** The caption counts the wrong set in the wrong register with the wrong noun: `{{ entries.length }} loaded` over `nonFeaturedEntries` (GalleryView:291 ← :68-70) while the excluded featured rows render directly above; no denominator, no noun, no plural branch; three nouns for one object across adjacent surfaces (visualizations/entries/entr(ies)); graded on the banked FR-GFC-26 count-prose precedent. **→ F.W3/W4** (one unit with FR-GIG-4).
- **FR-GIG-8 = D-7 / C-16 — MINOR.** All-featured page ⇒ parent empty state (keyed on ALL entries, :266) suppressed ⇒ grid mounts empty ⇒ "0 loaded", a zero-height grid, and the producer's unconditional `#end` wrapper as a 2rem empty band (`:50`'s `v-if` guards the copy, not the box — dist:68 unconditional, my read). Reachability honestly bounded (young-gallery curation; NOT reachable via the tier filter — api.ts:397-407). **→ F.W3/W4.**
- **FR-GIG-9 = D-9 / L-11 — MINOR (reader-B's INFO in dissent, R-9).** The grid track is an untokenized inline magic number — the only inline style in the file and the only `auto-fill` in web/src — rem-coupled to the 768px root-font fork (252px mobile / 224px desktop cell, decided by nobody). Arithmetic corrected (K-10): at 320px, `px-4` = 36px (18px rem), 284 > 252 — one column, no overflow; benign direction, real coupling. **→ F.W3/W4** (one `--gallery-track` token, shared with the card's intrinsic-size estimate).
- **FR-GIG-10 = D-11 — MINOR.** Gutter (`gap-3`) equals card inner padding (`px-3`): three ladder positions, two distinct values; frame `px-4` correctly greater; separation legitimately carried on value (border/shadow/fill). Correctly graded by the axis. **→ F.W3/W4.**
- **FR-GIG-11 = D-12 — MINOR.** The end-cap is a dead end ("No more entries" — negation, machine register, no next action) while the sibling empty state was designed (icon + line + CTA). **→ F.W3/W4.**
- **FR-GIG-12 = M2 (reader-A missed find; re-proven, with this seat's own footnote correction) — MINOR.** Under the house PRM blanket (a11y-overrides.css:7-11, `animation-duration: 0.01ms !important` on `*:not([data-allow-motion])` — chain closed via style.css:3 → utilities.css:19) the loading spinner is FROZEN, and it has no static or textual fallback: reduced-motion users get an inert circle-minus-a-quadrant as the app's only in-flight signal; AT users get nothing (composes with FR-GIG-3). All three axes missed it in opposite directions (D-3 asserted unbounded spin — killed K-2; C-12 caught the ARIA but presupposed motion). Footnote CORRECTED by this seat: 8.0.0's DotRing PRM carve is `animation: none` **by design** — "a static unequal ring still says in-flight" (dot-ring.css:239-243, my tag read) — i.e. the future producer default parks as an AUTHORED legible state while this local fork keeps opting out; that sharpens the row rather than weakening it. Cure = `role="status"` + text fallback, needed at every pin. Registry-swept: the banked PRM rows all concern the blanket killing UNWANTED motion; none covers the blanket destroying the only affordance. **→ F.W3/W4** (one unit with FR-GIG-3). Perception → SS-13.
- **FR-GIG-13 = M3 (reader-A missed find; re-proven) — MINOR, cure-guard (ColorSpaceSelector addendum §2 law).** D-6's prescribed `role="list"`/`listitem` cure — "requires moving GalleryCard's `role="button"` off its root" — silently destroys the card's focus ring: style.css:136-143 pins `.gallery-card:focus-visible` by CLASS to the root, and GalleryCard:70-79 carries class + role + tabindex on the SAME element; move the focus target inward and the selector stops matching — a WCAG 2.4.7 regression reverting the D.W4.d repair authored for exactly this card, invisible to vue-tsc and the 139-line e2e spec. Sound shape: a `role="listitem"` WRAPPER as the grid item, root untouched. **→ rides FR-GIG-3's cure.**
- **FR-GIG-14 = MISS-2 (reader-B; re-proven by this seat's own compile — see R-6) — MINOR (reader-B's MAJOR in dissent), composition rider on two banked rows.** The uncached `onClick` (`$event => emit('card-click', entry)`, outside `_cache`, in the dynamic-props array — my `compileTemplate` run) forces all N cards to patch on every parent render, so banked fr-GalleryCard L·M-2's one-line `:liked` hoist delivers zero measured win alone, and banked FR-GFC-19's `click: [entry]` payload cure must land in the same edit. One repair unit; neither banked record states the interaction. **→ F.W3/W4** (sequencing note on the L·M-2/FR-GFC-19 cure).
- **FR-GIG-15 = C-11 ⊕ MISS-3 (reader-B; re-proven) — MINOR.** The `scheduler.yield` floor never fires on the gallery path — `chunkSize: 24` (and the module default 32) against pages capped at 20 makes the predicate (`scheduler.ts:50`) unsatisfiable; `processInChunks`/`yieldToMain` have exactly one call site each (my grep) — the three-rung INP floor is unreachable in the shipped tree. CONTRADICTS lane-frontend.md:568's "good hygiene, correctly scoped" (re-read verbatim), and **kills the L axis's masker cell** (K-15): under the drain, the remedy authored FOR the runaway can never fire DURING it. **→ F.W3/W4.**
- **L-4 — MINOR (banked) · FOLDS BY REFERENCE to banked fr-GalleryCard L·D-1/C·C-5-as-rescoped, ⊕ reader-B's MISS-4 grid-limb (adopted after re-proof). Do NOT re-book.** `tier` is a phantom on every never-curated row (0 `tier` tokens in the Pydantic model under `extra="forbid"`; admin `$set` writers only — my greps) and `!== 'normal'` inverts for `undefined`; the visible-box claim is governed by banked K-4 (nothing paints). Grid-limb (MISS-4, verified): the grid is bound to `nonFeaturedEntries`, so `entry.tier === 'featured'` is FALSE BY CONSTRUCTION here — the Crown branch and the featured glow are statically unreachable at this site; the correct guard at the grid is simply `tier === 'saved'`. **→ rides the banked row (F.W3/W4).**
- **L-3 / C-9 / C-10 — MINOR (banked) · FOLD BY REFERENCE to banked fr-GalleryCard L·M-2 (Set half) + banked FR-GFC-19 (onClick half). Do NOT re-book** (R-6). L's "only one child re-render" contrast sentence is killed (K-13); the fan-out censuses are over-counted (K-14: GalleryMarquee unreachable per banked FR-GFC-9/G-LC1 — live fan-out 2 hosts).
- **C-7 — MINOR (banked; C's MAJOR in dissent) · FOLDS BY REFERENCE to banked FR-GFC-17.** The whole props/emits vocabulary says `hash` and carries `slug` (grid :11/:12/:18/:20/:21 vs :32/:36; types.ts:211 "never identity" re-read). Type-only cure at the leaf/relay layer.
- **L-7 / C-13 — MINOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-24 ≡ fr-GalleryCard G-LC3 — ONE identity, registry reconciliation noted.** The tier union is hand-copied past the exported `GalleryTier` (types.ts:96); both banked censuses name grid `:19`. Both axes undercount (3 and 4 vs the banked 9-lines/8-files). **Anti-rename note for the forming specs**: FR-GFC-24 (6+2 census) and G-LC3 (9/8 census) were banked the same day for the same identity — they must agglomerate as one row, and this record's grid site belongs to it.
- **L-8 — MINOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-22 + fr-GalleryCard L·D-3.** Two dead imports in GalleryCard (:9-10), no gate can see them; the PathPreview bundle-cost cell stays KILLED per banked K-13 (`.path-preview` ships in no built sheet). F.W0 rider (`noUnusedLocals`) rides the banked rows.
- **L-9 — MINOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-18.** `adminMode`/`likedHashes` required here, optional on the card; the axis's precision credit (`selected?` genuinely optional) is earned and carried.
- **C-14 — MINOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-21, with reader-B's vacuity correction adopted (R-10).** Classification hygiene only; the stated failure mode (`npm ci --omit=dev`) is vacuous for a `private: true` SPA whose build toolchain is itself in devDependencies (my package.json read).
- **D-1-residue — MINOR (banked class) · FOLDS BY REFERENCE to banked FR-AUL-42 (+ the fr-CoefficientsSpectrum m-9 class) → F.W4.** After D-1's kill (K-1), what survives is real TODAY: raw Tailwind `text-xs`/`text-sm` rungs over the house `--type-*` ladder (the caption stays 12px while glass-native surfaces ride the `--type-caption` clamp). The readers' grep census (40×`text-xs`/16 files · 77×`text-sm`/31 files · union 36/117) is adopted as twice-derived; the census-omission observation is preserved at INFO as a pre-8.0.0 forward hazard.
- **M5 — MINOR (banked) · FOLDS BY REFERENCE to banked fr-GalleryCard D-7 (render arm) + the D-12..23 alt/aria-FK cluster** (R-5). The thumbnail alt names the identifier and duplicates the button's accessible name; the `alt=""`-decorative cure note rides the banked rows. The honest ceiling stands: `Visualization` has no title field — the defect is the wrong KIND of alt, not the absence of a better string.

### INFO

- **FR-GIG-16 = C-17 — INFO.** `threshold` never tuned (default 200 → `rootMargin: 0px 0px 200px 0px`, dist:14/:46 — my read); premise corrected (14rem is track WIDTH, K-9-cell): the runway is under one row via the card's own `--deferred-section-size: 17rem` ≈ 272px. Moot while FR-GIG-1 stands. **→ F.W3/W4 rider.**
- **FR-GIG-17 = C-18 — INFO.** value.js ^0.13.0 and keyframes ^4.3.0 declared and wholly unconsumed in this subtree (the repo's 5 value.js imports + 1 keyframes import all live outside the closure — adopted, spot-consistent with the F.W2 census surface); the component has a colour problem value.js already solves and does not reach for it. **→ F.W2 rider.**
- **FR-GIG-18 = D-14 — INFO.** `hasMore` optimistic from mount (gallery.ts:45; re-asserted :87) — correctly filed as state-matrix completeness, and it is FR-GIG-1's ignition condition. **→ rides FR-GIG-1.**
- **FR-GIG-19 = L-10 — INFO.** No dedup on append (push :74 / unshift :182, never cross-checked); no live duplicate constructible (splice-first :170-171; deleted_at filters); the axis's refusal to manufacture a defect is ratified, and the registry already credits the server's compound keyset predicate (fr-GalleryFeaturedCarousel:103). No action.
- **L-12 / C-12-coverage — INFO (banked) · FOLDS BY REFERENCE to banked FR-GFC-27 (→ F.W4 test rider) + F.W0 rider.** Every finding here is invisible to CI: 139-line e2e spec with zero paging-vocabulary hits (my grep), no vitest, no lint — corroborating CENSUS:254-258 ([P2], re-read).
- **FR-GIG-20 = MISS-5 (reader-B; anchors re-derived via banked rows) — INFO · NO-WAVE-OWNER (SS-3/SS-4 repair-unit sizing input).** The 8-line GalleryCard binding block is instantiated at 4 sites across 3 components, and banked FR-GFC-9/G-LC1 proves the two GalleryMarquee copies are unreachable — the live duplication is 2 sites. Any repair or census touching the block must not count 4 live consumers.

## Killed-claims register

- **K-1 · D-1's BLOCKER ("7.0.0 bridges.css:35-36 declares `--text-sm: initial; --text-xs: initial`" ⇒ F.W1 ships a 36-file typography regression) — KILLED** (both readers; ratified by this seat's own tag reads). `git show v7.0.0:src/styles/theme/bridges.css` contains **zero** `initial`; :35-36 are `--text-display-3/-4`; the clear is v8.0.0:25-26; the landing commit `35a30fbb` is NOT an ancestor of v7.0.0 and IS of v8.0.0 (my merge-base runs). The reader-of-record read producer HEAD (8.0.0) and labelled it 7.0.0 — the exact class banked at fr-GalleryFeaturedCarousel dissent-5. At the census's named F.W1 target both captions keep rendering. Salvage: the grep census is exact (adopted twice-derived) and the raw-rung residue folds to FR-AUL-42 (see roster); the BLOCKER, the "~7× under-scoped census" charge, and the F.W1 carry all die.
- **K-2 · D-3's PRM leg ("a reduced-motion user gets an unbounded 360°/s rotation"; "every PRM block in the 4.0.0 cascade is target-scoped") — KILLED** (both readers; ratified at my bytes). a11y-overrides.css:7-11 is a universal `!important` blanket reaching `.animate-spin` through the closed import chain; the axis's nine-block enumeration omitted the one blanket file. At least the **fourth** banked instance of this citation-inheritance event on this exact producer file (fr-AdminFlaggedPanel ruling 1 · fr-CoefficientsPanel R-1 · fr-GalleryFeaturedCarousel R-1/K1 · fr-GalleryCard's D-9 context) — the carousel record's dissent-1 ("a11y-overrides.css should be on every fourier reader's mandatory-read list") predicted it again. What survives became FR-GIG-12 — the OPPOSITE defect. Anchor corrections carried: the card's PRM block is :304-308 (not :281-285); `--radius-pill` is radius.css:25 (not :110).
- **K-3 · The v8-labelled-v7 uplift cells across D-2/D-3/D-6 and L-5** ("7.0.0 replaces the default with DotRing … `InfiniteScroll.vue:66-70`"; "7.0.0's `:84-86` adds the sr-only `role=status` live region"; the `text-small`/`v-if` end-wrapper quotes; "the loading row wrapped in `<Transition name=glass-dot-ring>`") — **KILLED** (both readers; ratified at both tags by this seat). v7.0.0's file is 60 lines: no DotRing (ls-tree grep 0), no line 66+, no sr-only, `v-else-if` + `text-sm` at :56; every quoted byte sits at v8.0.0 exactly where cited. Consequences flow the OTHER way: at the F.W1 target the paging surface is still totally silent to AT (FR-GIG-3 is bigger post-uplift, not smaller) and the doubling conclusion survives on v7's own wrappers (:46/:56).
- **K-4 · L-1's carry/remediation cell ("the producer already cured exactly this at 7.0.0; the fix rides the [P0] tri-package bump") — KILLED** (both readers; re-proven at the tag by this seat, R-2). The v7 `check()` cure is defeated by the unchanged observer-root geometry; the drain survives the bump. Replaced by FR-GIG-5. The D axis's instinct ("this survives the uplift") and the C axis's repair-order item 2 (root must be `null` or a real scroll port) are the correct cells; L's carry — the corpus's only routing for its heaviest defect — is the wrong one.
- **K-5 · D-8's Skeleton-availability cell ("glass-ui 4.0.0 ships no Skeleton … pre-uplift the honest cure is a local placeholder") — KILLED** (reader-A; ratified by this seat's node reads, R-8). The subpath half is true at both maps; the component ships and is barrel-importable at the installed pin (`dist/index.d.ts:28`; `Skeleton` exported from `dist/glass-ui.js`). It cuts in the defect's favour: FR-GIG-4's cure is uplift-free, and the "local placeholder" prescription would have been a glass-ui-first violation born of a false negative.
- **K-6 · C-2's "the page scrolls, not the container" clause — KILLED** (reader-A; ratified). GalleryView:220 is `overflow-y-auto h-full` — a bounded scroll port. The conclusion is unaffected (the observer root is the primitive's own unclipped wrapper; ancestors above the root never clip), but the cure text inherits a load-bearing caveat: `root: null` is correct precisely BECAUSE the viewport-root algorithm applies every intermediate ancestor clip including :220's.
- **K-7 · C-5's absolutization ("painted with the hardcoded literals … forever"; "two defects cancelling") — KILLED** (reader-A per banked K4/R-3; reader-B per banked GCM-21/K-9; ratified). Path-dependent capture; on the default entry both sites agree grey. The notable failure: this is the second corpus to re-file the identical absolutization after it was adjudicated — fleet-calibration signal, recorded in DISSENT.
- **K-8 · C-6's survivor cell ("only `--viz-amber`/`--viz-green` survive") AND reader-A's adoption of it — KILLED (R-3, this seat's bytes + banked fr-App B-1 concurrence).** `--viz-green: var(--section-color-4)` and fourier never overrides `--section-color-4`: four of five tokens grey; the finding is WORSE than the axis stated. **K-8b (kill-of-a-kill, seat-minted): reader-A's kill of C-6's `light-dark()` quotation ("not in the tree at any line") — KILLED (R-4).** The quote is verbatim at light-dark.css:145-147. An incomplete grep must not execute a true quotation.
- **K-9 · C-17's premise cell ("cells ≈ 224px TALL") — KILLED as stated** (both readers; ratified). 14rem is the track's minimum COLUMN WIDTH; the conclusion survives on the tree's own `--deferred-section-size: 17rem`. Row stands as INFO with corrected premise.
- **K-10 · D-9's arithmetic ("320 − 32 = 288") — KILLED as a cell** (both readers; ratified). The finding's own thesis is an 18px rem below 768px: `px-4` = 36px; 284 > 252. Conclusion survives; recorded because the row's whole force is metric precision.
- **K-11 · C-14's failure-mode cell ("`npm ci --omit=dev` cannot resolve them") — KILLED as vacuous** (reader-B; ratified at my package.json read, R-10). The build toolchain itself is devDependencies and the package is `private: true` — no production-install path exists where the split has effect. Residue folds to banked FR-GFC-21.
- **K-12 · The subpath-count cells (D·S-5's "7.0.0 = 68 subpaths"; C·S-2's "~40-entry export map") — KILLED** (both readers; ratified by my node runs: 80 at 4.0.0, 74 at v7.0.0 — matching banked fr-GalleryCard dissent-4's "tag measures govern"). The load-bearing claims survive: `./infinite-scroll` present at both; `./checkbox` absent at both, arriving 8.0.0 — so the S-2 BH-relay ask must name 8.0.0 as the Checkbox cure pin, past the F.W1 target.
- **K-13 · L-3's contrast sentence ("the `selected` path costs … only one child re-render") and the sufficiency of the one-line `:liked` hoist — KILLED** (reader-B; ratified by this seat's own `compileTemplate` run). The uncached `onClick` makes `shouldUpdateComponent` true for every card on every parent render; the hoist alone buys nothing measurable. Became FR-GIG-14.
- **K-14 · The fan-out censuses (L-3's "three hosts"; MISS-5's four-live-sites frame; D·S-5's file-boundary "off the 35-site lucide rename") — NARROWED** (both readers; ratified via banked FR-GFC-9/G-LC1 + FR-GFC-29). GalleryMarquee's two sites are unreachable code (live fan-out 2); the grid's RENDER closure contains five lucide rename sites and the forced barrel Checkbox (banked).
- **K-15 · L-1's third masker cell (`processInChunks(...{chunkSize:24})` "masks the symptom") — KILLED** (minted by reader-B via MISS-3; ratified by this seat's scheduler + grep reads). The chunker never yields at page ≤ 20 (and the module default 32 fails too); it is not a mitigation, so the drain's main-thread exposure is worse than the L axis's own model. Two maskers, not three.
- **K-16 · reader-A's registry-currency cells ("no fr-GalleryCard/fr-GalleryView/fr-GalleryMarquee row exists"; M5/L-4/L-3-Set-half filed as novel) — KILLED as dispositions (seat-minted; evidence about the RETURN, not the tree).** fr-GalleryCard.md and fr-GalleryMarquee.md are banked (same-day); the affected rows re-disposition as folds (R-5, R-6, roster). reader-A's underlying FACTS all re-verify — only the novelty claims die. A registry sweep is dated evidence; adjudication re-runs it.
- **K-17 · The C axis's systematic anchor drift — RECORDED as a provenance defect** (reader-B's enumeration; spot-ratified by this seat: watchers at GalleryView:95-98/:111-114 not :118/:128; `like()` at gallery.ts:189-199 not :180-190; `cssVarToHex` at colors.ts:22-54; setTier/delete at :143/:155 not :138/:150). Every cited fact exists at the corrected line; the same class is banked at fr-GalleryCard:130 ("killed cells clustered in the C axis") and fr-GalleryFeaturedCarousel K10. Repair waves take anchors from THIS record, not from challenge-C. The L axis is near-line-exact throughout; the D axis is exact on fourier source and systematically v8-as-v7 on the producer.

## Missed finds — adopted after re-proof

1. **M1/MISS-1 (BOTH readers, independently)** — the P0 bump does not cure the drain. Re-proven at the tag by this seat (R-2). **Booked FR-GIG-5 (MAJOR)** + glass-ui BH relay.
2. **MISS-2 (reader-B)** — the `onClick`×`likedHashes` one-repair-unit. Re-proven by this seat's own compile. **Booked FR-GIG-14 (MINOR; reader-B's MAJOR in dissent).**
3. **M2 (reader-A)** — the PRM-frozen spinner with no fallback. Re-proven at the bytes; footnote corrected by this seat (DotRing's park is authored). **Booked FR-GIG-12 (MINOR).**
4. **M3 (reader-A)** — the list-semantics cure destroys the focus ring. Re-proven (style.css:136-143 + card root). **Booked FR-GIG-13 (MINOR, cure-guard).**
5. **MISS-3 (reader-B)** — scheduler dead code / inert yield floor. Re-proven (sole call sites; default 32). **Merged into FR-GIG-15 (MINOR)**; kills K-15.
6. **MISS-4 (reader-B)** — the featured branch is statically unreachable at the grid site. Re-proven (nonFeaturedEntries partition). **Banked as the grid-limb on fr-GalleryCard L·D-1's fold.**
7. **MISS-5 (reader-B)** — 4 binding sites, 2 dead. **Booked FR-GIG-20 (INFO, NO-WAVE-OWNER → SS-3/SS-4).**
8. **M4 (reader-A)** — "in-app = grey", the FR-GFC-8 cell amendment. **Adopted as a fold onto banked GCM-21/K-9** (which already carries it); rides FR-GFC-8's probe #8 with the entry-route pin law.
9. **M5 (reader-A)** — the thumbnail alt. **Adopted as a fold onto banked fr-GalleryCard D-7 + the alt/aria-FK cluster**, with the `alt=""`-decorative cure note (R-5).

## Superlatives — with provenance (L-18 runs both ways)

1. **A genuinely pure presentational shell, and the seam is in the right place** — all three axes + both readers; my whole-file read: `<script setup>` is a props decl and an emits decl and nothing else; zero store/api/ref/computed/lifecycle/listener. This is why ~30 defects land at its seams and almost none in its body, and why every repair is cheap. The census's one-word row (lane-frontend.md:113, re-read verbatim) is the whole truth about it — rare.
2. **Line 36 is the reference implementation for the family** — `:selected="selectedHashes?.has(entry.slug) ?? false"` narrows a Set to a boolean AT the prop boundary; banked FR-GFC-2 prescribes `GalleryInfiniteGrid.vue:36/:41` BY LINE as the shape the carousel must mirror. The grid is the only gallery host that wired the multi-select seam correctly. (Its measured win is nullified today by FR-GIG-14 — a fact about the repair, not the judgement.)
3. **`:key="entry.slug"`, never `content_hash`** (:32) — the one right answer under the §1 single-slug contract (types.ts:211 re-read); it matters MORE than it looks: under the drain and FR-GFC-1's wholesale replacement, an index key would have been catastrophic. Banked corroboration at fr-GalleryFeaturedCarousel:102.
4. **Perfect colour-token discipline in 54 lines** — every colour semantic, zero hex/raw-fn/arbitrary classes (D·S-2's greps adopted; spot-consistent with my reads). The axis looked for a contrast defect and reported honestly that there is none.
5. **`auto-fill` + `minmax(_, 1fr)` is the right density primitive** — one declaration, continuous degradation, no breakpoints, no JS; the sole `auto-fill` in the tree. The D axis's separation of the primitive (right) from its delivery (inline, untokenized — FR-GIG-9) is the harder, more useful call.
6. **The compiled-render instrument** — C-9 shipped a runnable one-liner; both readers AND this seat re-executed it to token-identical output (three independent reproductions of a source-invisible defect). The registry's "best-evidenced row" standard (FR-GFC-19), met again — and it is the instrument that decided R-6/K-13.
7. **Bounded negatives, stated because the axis asked** — L §6 (zero canvas/WebGL contact; the only pixel surface is a server raster) and §7 (R5-7's blind spot does NOT apply: both `v-for`s sit on registered components — a clean control case), plus the dual it named (one callsite ≈ 18 native elements — an SS-3/SS-4 weighting input). Refusing to manufacture a finding from an available frame is the rarer discipline.
8. **Convergent correct restraint** — all three axes independently found and correctly bounded the slot-doubling and the empty-state reachability; and the file's own craft verifies at the bytes: the proximity ladder correct in direction (gap-2 < gap-3 < px-4, with the parent's gap-4 outside all three), fully typed tuple emits including both two-argument relays (the payload-preserving arrow form the carousel record banked as house style), and the granular `./infinite-scroll` subpath import present at BOTH maps (80-key and 74-key, my node runs) — the import surface genuinely survives the named uplift.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **The drain's magnitude** (FR-GIG-1): request count + `.gallery-card` count after one `goto('/gallery')` with zero scrolling. Predicted: ⌈N/20⌉ back-to-back `GET /api/visualizations`, every non-featured row mounted. The code path is static-certain at every pin; only the count is unobserved. One page load.
2. **The retry-storm cadence** (FR-GIG-2): the same page with the list endpoint forced to 500. Predicted: request → toast → rAF → request at frame cadence, `hasMore` still true. Re-opens R-1's severity if measured hot.
3. **The PRM-frozen ring's perception** (FR-GIG-12): whether it reads as an indicator or a border artefact; whether any UA suppresses the 0.01ms iteration. Cascade + missing ARIA are static fact; perception is not.
4. **Whether any screen reader announces anything during a page fetch at the installed pin** (FR-GIG-3) — the attribute absences are static fact.
5. **Rendered magnitude of the doubled 4rem bands** (FR-GIG-6) at 390px/1440px, and whether the 2rem empty `#end` band (FR-GIG-8) reads as a layout break.
6. **First-reveal correction from `--deferred-section-size: 17rem`** against a `1fr`-grown ~19rem cell — rides banked FR-GFC-25's existing SS-13 item.
7. **Basis-pill colour per navigation path** (deep link `#bf4040/#3d72b8/#9545b8` vs default entry `#888888`) + the theme-toggle no-op on both — rides banked FR-GFC-8's probe #8; **every witness pins the entry route** (GCM K-9 law), and the expected in-app reading is grey for FOUR of five tokens (R-3).
8. **Long-task attribution during the drain**, run knowing FR-GIG-15: there is no yield floor on the path; plus whether ConvergencePlot's ungated rAF is co-resident after in-app navigation (census §3a).

## DISSENT — preserved, not ruled away

1. **Severity dissents**: three BLOCKER filings on the drain (L axis, C axis, reader-B) against this seat's MAJOR-blocker-weight (R-1, with its stated re-opener); D's MAJOR on the caption row and the slot-doubling; C's MAJOR on the doubling and on the `hash`/`slug` vocabulary (over banked FR-GFC-17's MINOR); reader-B's MAJOR on FR-GIG-14 and its INFO on FR-GIG-9; reader-B's standalone-MAJOR re-grade of the inert filters (R-7) preserved as agglomeration input.
2. **Reader-vs-reader inversion recorded (evidence about the fleet).** reader-A over-killed a true axis quotation (R-4) and adopted a false survivor cell the registry already contradicted (R-3); reader-B ran both to the bytes and won both — yet reader-A alone caught the installed-pin Skeleton (R-8) and minted the two a11y missed finds (M2/M3) reader-B never saw. Skepticism is not conserved across rows; the incredulity seat exists because neither reader is systematically the skeptic (the fr-GalleryCard dissent-1 lesson, re-confirmed).
3. **Registry currency is part of the claim.** reader-A's sweep was true when run and false when adjudicated — four dispositions moved (K-16) without a single underlying fact changing. Corollary for the fleet: novelty claims must carry the sweep's timestamp, and same-day sibling apotheoses (FR-GFC-24 ≡ G-LC3) can double-book an identity — this record's reconciliation note is the anti-rename hook for the forming specs.
4. **The citation-inheritance tally on a11y-overrides.css keeps growing** (K-2: at least the fourth banked instance) and the C-5 absolutization was re-filed verbatim after adjudication (K-7, second corpus). A corpus that inherits killed cells is measurable; both classes are now twice-predicted, twice-confirmed.
5. **This seat's own adoption disclosure**: the text-xs/text-sm grep census (twice-derived by the axes/readers; not run a third time); C-18's five-import value.js census (spot-consistent, not re-enumerated); the reka/lucide internals cited inside banked fr-GalleryCard rows (trusted at the banked record); e2e paging-grep and package.json reads are my own. Each adopted cell is labeled (adopted) at its row.

## Closing verdict

**ADJUDICATED — DEFECTIVE at the seams, exemplary in the body: the corpus's heaviest defect is a whole-collection pagination drain at the installed pin (FR-GIG-1, MAJOR blocker-weight, three BLOCKER dissents) whose only proposed routing was wrong — the F.W1 tri-package bump does not cure it (FR-GIG-5), and the true cure is a producer change riding the standing glass-ui BH relay; 34 rows confirmed (0 BLOCKER · 9 MAJOR · 19 MINOR · 6 INFO) with 14 folded by reference (fr-BasisCanvas D-1 via fr-App B-1/GCM-20 · FR-GFC-8/GCM-21 · FR-GFC-3 · FR-GFC-1/GCM-24/FR-GFC-4 · fr-GalleryCard L·D-1/L·M-2/D-7/L·D-3 · FR-GFC-17/-18/-19/-21/-22/-24≡G-LC3/-27 · FR-AUL-42) and never re-booked; 17 kills banked including this seat's own four (the survivor-cell kill with banked concurrence, the kill-of-a-kill on a true quotation, the stale-registry-sweep kill, and the masker-cell ratification); 9 missed finds adopted after re-proof, led by the shared misrouted-cure find; routings are M-25 agglomeration input for the forming X·F specs — the glass-ui BH relay + F.W1 footnote own the drain's producer cure and non-cure, F.W3/W4 owns the error/first-load/semantics/PRM/chrome cluster, F.W2 owns the colour seam locks, F.W5-W8 owns the like-verb and list-contract folds, and the orphan-client class + repair-unit sizing notes ride NO-WAVE-OWNER to SS-3/SS-4; nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
