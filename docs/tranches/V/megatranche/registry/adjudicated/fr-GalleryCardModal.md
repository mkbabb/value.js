claude-fable-5

# fr-GalleryCardModal — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/gallery/GalleryCardModal.vue` (261 lines, read whole this session) + its consumer chain (`GalleryView.vue`, `stores/gallery.ts`, `stores/workspace.ts`, `useWorkspaceLoader.ts`, `router/index.ts`), the sibling `GalleryCard.vue`, three glass-ui subpath dependencies, and the FastAPI arm.
**Substrate disclosure**: fourier HEAD `cd26c65`; the subject file is **DIRTY by exactly one uncommitted hunk** — `:72` `variant="opaque"` → `surface="opaque"` (my `git diff`); every reader anchor resolves against the **working tree**, which is what all three axes and both readers read. ~30 sibling files are also dirty; every line anchor in this record was re-resolved at the working tree this session.
**Corpus adjudicated**: three challenge axes (D 34 defects/7 superlatives · L 16+2 · C 17/4, all authored `claude-opus-5[1m]`) already challenged by **two hostile re-readers** — **reader-DU** (design-and-usage: 36 confirmation cells · 11 kill rows · 5 missed finds · 9 superlatives · 9 unproven) and **reader-LC** (library-and-code: 30 confirmation cells · 10 kills incl. 1 self-kill · 4 missed finds · 7 superlatives · 6 unproven). Both returns arrived through the orchestration channel; the three immutable challenge files sit beside this record.
**Producer states**: installed `@mkbabb/glass-ui` **4.0.0** (my `node -e` read); producer repo at **8.0.0**, HEAD `350f7a90` (2026-08-24), tags `v7.0.0` and `v8.0.0` both present. CENSUS-2026-08-03 declares the F.W1 atomic transaction as **glass 4→7**; every version-contingent verdict below is proved at `git show v7.0.0:…` by this seat, and the 7-vs-8 target question is escalated (see UNPROVEN #8).
**Method (incredulity)**: every reader kill was re-derived before sustaining (a weak kill gets un-killed — none needed un-killing, but one reader proof cell died under this seat's greps and two reader residues were narrowed by banked instruments the readers' sweeps missed); every missed find was re-proven at the bytes before admission; every reader-vs-reader severity conflict ruled with fresh instruments. Zero browser probes (probe parsimony); product source read-only; **sole write = this file**.
**This seat's own instruments (all static + node, this session)**: GalleryCardModal.vue whole (261); GalleryView.vue :40-50/:112-145/:385-415 (incl. the `viewedHashes` guard both readers' scenario cells omit); stores/gallery.ts :20-35/:55-100/:130-160/:185-215; stores/workspace.ts grep (`loadVisualization` = definition :197 + alias :238 + exports :447-448, **zero callers**); useWorkspaceLoader.ts :1-55 (imageSlug-only reads, warm-state `router.replace`); router/index.ts :38-72; VisualizationView.vue greps (`visualizationSlug` → 0; `useWorkspaceLoader` at :9/:50); lib/api.ts :290-300/:500-516 (`overlayUrl` resize=1024; `setVisualizationTier` = **PUT**, typed `Promise<Visualization>`); lib/types.ts :92-98/:205-262; lib/colors.ts :20-55/:74-97/:99-110; basis-display.ts whole (7 lines); main.ts :1-12 (`router.isReady().then(mount)`); PaperArticleWindow.vue :108-122 (bare `<router-link class="callout-btn">`); style.css :1-50/:110-145 (`@layer base` closes; the D.W4.d focus rule is **unlayered**, carries `border-radius: inherit`); GalleryCard.vue :96-106/:128-133/:145-180/:282-287; **byte-identity diff re-run, zero output** (`:41-66` ≡ GalleryCard `:36-61`); `timeAgo` census (5 definitions), `DialogTitle` census (**4 consumers ship one; this file alone does not**), `GalleryTier` census (types.ts:96 + **8 inline re-declarations / 7 files**; importers = api.ts:8 + gallery.ts:3 + AdminFlaggedPanel — **3, not 1**); api: admin.py :160-190 (`@admin_router.put`, returns `_json(_public_doc(updated))` + `set_etag_header`), images.py :168-209 (no session dep, no visibility check, `Cache-Control: public, max-age=86400`), visualizations.py :76-80/:330-336 (`_public_doc` strips `_id`/`liked_ips` only; raw-doc `json.dumps` list path), models/visualization.py (`tier` grep → 0; `likes: int = 0`; `extra="forbid"`); `grep -in like lib/api.ts` → 1 prose hit; e2e/gallery.spec.ts (6 `test(`; console guard `error`-only + `404` filter; `modal|dialog|card|Open Visualizer` → **0**); web/package.json (cva/clsx/lucide/reka-ui/tailwind-merge ALL in devDependencies; TS ^6.0.3); installed glass-ui 4.0.0 dist: cn-DJXf4yaB.js **whole** (first-match classifier, `if (l === null) continue`, `padding-x`/`padding-y` rows BEFORE `padding`, closed `rounded` enum), DialogContent-DDE6pQBU.js (base `"fixed left-1/2 top-1/2 z-modal grid w-full max-w-lg gap-4 p-6"`, `showClose {default:!0}`, slot-then-✕, `opacity-70` ✕ class verbatim, `rounded-dialog` compose), button-BNDWhAZb.js (`ghost`/`outline` variant strings incl. `aria-pressed:bg-accent…`; CVA `[&_svg:not([class*=size-])]:size-(--ui-glyph)`), useSurfaceAxis (opaque → `glass-floating glass-opaque`), glass/ladder.css :83-113 (`.glass-floating` **three-leg** `box-shadow: var(--glass-material-rim), var(--glass-under-shadow-vivid), var(--glass-shadow-floating)`), a11y-overrides.css :108-126 (coarse floor keys `[data-size="icon"]` + 2 trigger classes only), tokens (color-radius :40/:57/:262-272 · offsets-sizing :177 · light-dark :15-22), property-regs.css (**registrations = progress-crescendo/phase-tint-amount/specular-x·y·intensity/glass-level/ui-scale** — the K-8 decider), typography/scale.css :17-46 (**"Plus Jakarta Sans Fallback" IS declared** — four `local()` metric faces); reka-ui 2.9.10: DialogContentImpl.js (titleId/descriptionId minted; `aria-labelledby`/`aria-describedby` bound **unconditionally**; dev-only `useWarning`), Dialog/utils.js **whole** (TITLE_MESSAGE + the **second** DESCRIPTION_MESSAGE warn — both fire here), DialogRoot.js (`passive: props.open === void 0` → fully controlled), FocusScope.js :88-106 (`focusFirst(getTabbableCandidates)` unless mount-event prevented); **producer at the tag**: `git ls-tree v7.0.0 -- src/components/dialog/` (13 files, **no styles.css**), `git show v7.0.0:…/DialogContent.vue` (baseClasses = `…gap-4 [--overlay-pad-inline:--spacing(6)] [--overlay-pad-block:…] px-(--overlay-pad-inline) py-(--overlay-pad-block)` — **no `:where()` clamp**; `showClose` survives, default true; ✕ = `<X class="w-4 h-4">` at `opacity-70`, rendered **after** the slot; `centerScrollClasses` = `max-h-[calc(100dvh-2rem)] overflow-y-auto`; `cn(resolveSurfaceClass("floating"), "rounded-dialog")`), `_shared/class-names.ts` at the tag (:98 `padding-x` / :99 `padding-y` / :106 `padding`; :141 the SAME closed `rounded` enum), Button.vue at the tag (`ButtonEmphasis`, `ButtonSize = xs|sm|md|lg`, `loading`, **no `variant`**, default `emphasis:"secondary"`), badge/index.ts at the tag (BASE has **no `border`**; `outline: "badge-atom--outline text-foreground"`), MIGRATION.md :73/:350; **own WCAG instrument (node, Ottosson OKLCH→sRGB→WCAG)**: `--tier-featured` → rgb(255,191,0) **1.587:1** vs `--background`, `--tier-saved` → rgb(52,153,255) **2.824:1**; frozen pill literals over the `bg-muted/30`-on-background plate with 12% self-tint: `#bf4040` **4.165** · `#3d72b8` **3.968** · `#9545b8` **4.398** · `#888888` vs plate **3.346** — reproducing reader-DU's figures to the third decimal; registry re-reads: fr-AppHeader :48-51 (FR-AH-2/-3/-5), fr-BasisSelector :9/:17/:29/:41/:46/:59/:88 (B-3, M-10, m-22(f), K-7), fr-App :17/:25/:30/:42/:47/:56-57 (B-1/R-1/R-6/m-8), fr-BasisCanvas :43-44 (D-2/BC-5/C-3), fr-AdminUserList :54-57 (FR-AUL-16/-17/-19), fr-AdminAuditLog :36/:87 (AA-2/AA-44), fr-AdminFlaggedPanel :59/:71 (FR-AFP-11/-59), fr-ExportModal (whole; M-δ/M-γ/K-16), fr-DarkModeToggle :129; **registry novelty sweeps re-run wider than the readers'**: `overlay-pad|p-0` over ALL adjudicated (not just fr-*) → **TagEditPopover TEP-12 (MAJOR) + SwatchHoverMenu instrument line** — the p/px/py bucket-split AND tailwind's emission order (`p`@106031 < `px`@106047 < `py`@106071 at tailwindcss 4.3.0, byte-read there) are ALREADY BANKED; `border-radius: inherit|callout-btn` → 0; `resetAndFetch` → FR-AFP-11 only (the complement); `GalleryTier` → FR-AFP-59 only (the widening, not the census).

## Scoreboard

**55 adjudicated roster rows — 3 BLOCKER · 22 MAJOR · 22 MINOR · 8 INFO**, of which **12 are identity-folds to banked rows** (never re-booked) and **9 are new rows admitted from the readers' missed finds** (all re-proven at the bytes by this seat). **16 killed-claims register entries** (14 reader kills sustained after re-derivation, 1 composed two-reader kill, 1 recorded reader self-kill) **+ 3 seat corrections against the readers' own cells**. **10 superlatives.** Reader coverage gap: corpus rows **D-17, D-18, D-33** were adjudicated by neither reader — spot-checked by this seat and admitted at corpus grade (DISSENT-3).

The component's centre of gravity: **the data contract is broken at every seam the modal touches** — its only CTA routes to the arm that discards the entity (GCM-1), its like affordance has no operation anywhere in the 45 (GCM-2), and its prop is an orphaned snapshot that makes the admin tier toggle a one-shot latch (GCM-3) — while the **presentation layer sits on one banked token/palette defect family** (FR-AH-2/FR-AH-5/fr-BasisCanvas) **plus an a11y composite of blocker weight** (no name · no headings · alt=FK · a 16px unplated ✕ that scrolls away).

Status law: dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing is M-25 agglomeration input; nothing here stamps a wave.

## Ruled disagreements and seat corrections (this seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **D-01/L-7/C-6 severity** (unnamed dialog) | BLOCKER | MAJOR | **MAJOR; DU's BLOCKER preserved as dissent; the COMPOSITE named blocker-weight.** | The dialog is operable and dismissable (Escape/scrim live — reka verified), so the naming failure alone is not capability-absent; house precedent grades naming failures MAJOR. But D-01∘D-13∘D-14∘D-03/D-05 together put an SR/touch user in an unnamed, heading-less trap whose only visible dismissal scrolls away — the cluster, not the row, carries blocker weight. LC's sharpening banked: Dialog/utils.js fires **TWO** warns per open (TITLE + DESCRIPTION — `aria-describedby` is bound unconditionally, so the second guard passes; my whole-file read). |
| R-2 | **D-02 severity + framing** (tier chip 1.59:1) | BLOCKER | MAJOR, fold → FR-AH-5 | **MAJOR as second carrier of the banked FR-AH-5 token identity; fold sustained; site increment booked.** | My own recompute: 1.587/2.824 (= FR-AH-5's banked 1.587 for the same token). The banked row demoted BLOCKER→MAJOR for a 16px glyph under the 3:1 non-text floor; THIS carrier renders the tier **word** (`:91`, text-sm/600 = 14-15.75px) so the 4.5:1 text floor applies — a larger shortfall on the same token, booked inside the fold as the increment. K-4's ceiling-kill sustained by my own derivation (ratio non-monotonic in plate luminance; passes below L≈0.09). |
| R-3 | **C-8/D-21 severity** (radius double-declare) | MINOR | MAJOR | **MINOR at this site; the producer mechanism carries MAJOR weight in the GLASS-RELAY; LC's grade preserved as dissent.** | The visual stake here is a 12-vs-16px corner. The mechanism (glass-ui's `cn` blind to its own semantic radius aliases — closed enum verified at BOTH the installed 4.0.0 and `v7.0.0:_shared/class-names.ts:141`) is producer-owned with a 10-alias blast radius → standing glass-ui BH relay per the owner edict. DU's tag extension ratified: the conflict **persists at 7.0.0** — the axis's U-4 "cure" does not exist there. |
| R-4 | **D-08/L-11 severity** (hero image posture) | MAJOR | MINOR | **MAJOR.** | The `<img>` is the modal's dominant element, a live server-side PIL render (images.py :168-209, my read) with 404/timeout as reachable states, and the file ships zero posture. fr-ExportModal's error-posture precedent (L-3, MAJOR) governs. LC dissent noted. |
| R-5 | **DU's M-α residue** ("UNPROVEN-NEEDS-BUILD: whether `px-(…)` beats `p-0` in emission order") | NEEDS-BUILD | — | **RESIDUE NARROWED BY BANKED INSTRUMENT — seat's own registry find.** | DU's novelty sweep covered `registry/adjudicated/fr-*.md` only. The wider sweep hits **TagEditPopover TEP-12 (MAJOR)** — `p-0` measured LOSING to the producer's overlay-pad longhands (`p0Applied: true`, 20.35px/16px pad) — and the **SwatchHoverMenu** arbiter's byte-read of tailwindcss 4.3.0's registration order (`p` < `px` < `py` ⇒ longhands emit later and win the (0,1,0) tie). Same producer recipe, same merge mechanism, tailwind pinned ^4.3.1 here. M-α's cascade outcome is therefore **settled-by-banked-instrument**; only a one-line fourier-build spot-check remains (SS-13 #5). |
| R-6 | **LC missed-find #3's census cell** ("exactly one file imports `GalleryTier`") | — | as stated | **CORRECTED — 3 importers, not 1.** | My grep: `lib/api.ts:8` and `stores/gallery.ts:3` also import the named type (and gallery.ts imports it while STILL hand-widening the filter union at :33 — sharpening the finding). The census's substance (8 inline re-declarations / 7 files; zero **component** importers besides AdminFlaggedPanel) survives; the row is admitted with the corrected cell. |
| R-7 | **Both readers' C-2 scenario cells** ("openModal assigns the object THEN fires recordView") | as stated | as stated | **PRECISION CORRECTION, verdict untouched.** | GalleryView :117-121 guards with `viewedHashes` — `recordView` fires only on the FIRST open per slug per session. The freeze mechanism and all three failure legs stand exactly as filed; any repair test must not assert a view-increment on re-open. |
| R-8 | **D-15 replacement figures** (DU 4.17/3.97/4.40 vs banked B-3 4.24/4.04/4.48) | own recompute | fold to B-3 | **BOTH RIGHT — ground-dependent; fold to B-3 with the modal's ground noted.** | My independent instrument reproduces DU exactly over THIS component's `bg-muted/30`-on-background plate; B-3's figures are over bare `--neutral-0`. All six values under 4.5 on either ground — the verdict is ground-invariant. |
| R-9 | **The two versions of the big uplift kill** (DU's K-12-REPEATED vs LC's K-9 BASELINE-LAW) | facts at the tag | provenance of the axis's oracle | **COMPOSED — one kill, two independent grounds, both re-derived by this seat at `git show v7.0.0`.** | See kill K-1. My tag reads confirm every survival/death DU enumerated: U-1/D-16 and U-5/D-34 and I-4 SURVIVE at v7.0.0; U-2 survives (`ButtonSize` has no `"icon"`); U-3/U-4/I-1/I-2/I-3/I-6/I-7 DIE (no styles.css, no clamp, ✕ still 16px/opacity-70/slot-last, no `dismiss` axis). |
| R-10 | **Reader coverage of the corpus** | D-17/D-18/D-33 absent | same | **Seat spot-checked all three; admitted at corpus grade (MINOR·MINOR·INFO).** | D-18's four cells verified at both files (`gap-[0.3rem]`/`gap: 0.3rem` vs `gap-1`/`gap: 0.25rem`); D-33's dual-role Crown/Bookmark verified (:89-90 read-out vs :166/:176 command); D-17's census not re-counted rung-by-rung — admitted as corpus-derived with that marking. |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

### BLOCKER

| id | claim (adjudicated) | proof | disposition |
|---|---|---|---|
| **GCM-1 · L-1** | The modal's only CTA routes to the **pre-save workspace**, discarding the saved visualization: `:185` emits `image_slug` → GalleryView :396 `router.push(\`/w/${slug}\`)`; `/w/` loads the CALLER's IndexedDB draft, hard-resets to defaults when absent, and **writes a draft row keyed on the other user's image_slug** into the viewer's DB; the correct loader `loadVisualization` (workspace.ts :197-232, restores settings + captures slug/ETag) has **zero call sites**; any subsequent save POSTs a NEW entity with no `fork_of` despite the fork substrate existing. Both readers BLOCKER; every link re-walked by this seat. | router :53-71 (the §1 rule verbatim); useWorkspaceLoader :24-47 (imageSlug-only); my greps (`loadVisualization` 4 hits, no caller; `/v/` pushes = 0) | **→ F.W4** (emit `entry.slug`, route `/v/`, point the loader at `loadVisualization`) + **F.W5-W8 rider** (the no-provenance save is the CRUD/fork seam's fact) |
| **GCM-2 · C-1** | The Like control has **no operation behind it anywhere in the 45-op API**: api.ts has no like wrapper (my grep: 1 prose hit); gallery.ts :189-199 hard-codes `liked = true` + local bump; `liked_ips` exists only as 7 projection exclusions, never written; `likes` is a live server sort key with a compound index — the gallery ranks by a column no client can increment; the un-like arm at GalleryView :128 is dead code. | my greps over api.ts + `api/` routers; gallery.ts read; C-1's scenario CELL killed separately (K-11) | **→ F.W5-W8** (ship the 46th operation or delete the affordance — the decision is the CRUD union's) + **F.W3/W4** frontend arm |
| **GCM-3 · C-2/L-2** | The `entry` prop is an **orphaned object reference**: every store mutation spread-replaces the element (`like` :197, `recordView` :207-208) or rebuilds the array (`resetAndFetch` :84-97 ← `setTier`/`deleteEntry`), and GalleryView never re-points `selectedEntry` (:44 capture + three `= null`s). Views/likes/tier freeze in the modal; the two admin tier buttons compute the next value from the frozen snapshot — **the Featured button can never emit `normal`**; the second click re-sends the same request. Sibling GalleryCard is immune (v-for over the live array). | all store sites + GalleryView writes re-read this session; R-7's precision correction applies to the views leg | **→ F.W3/W4** (take the identity, derive the row: `selectedSlug` + computed lookup — three lines; lands before every other data fix per C's own dependency order) |

### MAJOR

| id | claim | proof | disposition |
|---|---|---|---|
| **GCM-4 · D-01/L-7/C-6** | No `DialogTitle`/`DialogDescription`/`aria-label` anywhere in :69-195 — reka binds `aria-labelledby`/`aria-describedby` to ids that exist nowhere and fires **two** `console.warn`s per open in dev; **sole offender among the tree's five DialogContent consumers** (my census: ExportModal :51, GalleryView :404, AdminFlaggedPanel :267, AdminUserList :463 all ship one). R-1 ruling; C-6's fold premise killed (K-6). | DialogContentImpl + utils.js whole reads; my DialogTitle grep | **→ F.W3/W4** — `<DialogTitle>` (sr-only acceptable) naming the entity, not the FK; one edit with GCM-17 and MF-3 |
| **GCM-5 · D-02** | Light-arm tier chip: `--tier-featured` **1.587:1**, `--tier-saved` **2.824:1** against the page ground; the 16px Crown/Bookmark also under the 3:1 non-text floor; dark arm passes. **Second carrier of the banked FR-AH-5 token identity** — increment here: the chip renders the tier WORD (4.5:1 text floor). "Ceiling" framing killed (K-4). | my own OKLCH→WCAG instrument; tokens re-read at the installed pin; fourier overrides neither token (style.css grep) | **FOLD → FR-AH-5 (F.W4 + GLASS-RELAY)** — the `--viz-amber` local-darken template; site cure = darken locally, hold dark arm |
| **GCM-6 · D-03** | The library ✕ is a bare **16×16** target (no padding, no min-size; verified in the 4.0.0 dist render fn) and the coarse floor keys `[data-size="icon"]` only — reka's DialogClose carries none. **NOT cured at v7.0.0** (my tag read: same 16px `w-4 h-4` at `opacity-70`) — the axis's I-2 "free at uplift" is an 8.0.0 fact (K-1). | dist + tag reads; a11y-overrides :113-120 | **→ F.W3/W4**, contingency-noted on the F.W1 target (free only if the target becomes 8.0.0) |
| **GCM-7 · D-04/C-11(1)** | `p-0` displaces the base `p-6` (same `padding` bucket at 4.0.0 — my cn read), making a full-bleed user image the first child; the ✕ is a SIBLING of the slot positioned against DialogContent — inside the image frame, at `opacity-70`, no backdrop plate, ten lines from the component's own plated tier chip (:86). Measured worst-case ratio → SS-13. | cn-DJXf4yaB.js + DialogContent dist reads; the file | **→ F.W3/W4** (plate the ✕ or re-scope the scroll/bleed; composes with GCM-6/GCM-8) |
| **GCM-8 · D-05/C-11(2)** | `overflow-y-auto` on the same element that hosts the abspos ✕ → the ✕ **scrolls out of view**; height arithmetic (246px frame + ≈260px meta vs 90vh=324px on a 360px-tall viewport) makes it reachable on real phones. Escape/scrim survive (degradation, not entrapment). | the file :73; CSS containment reasoning; DU's arithmetic re-checked | **→ F.W3/W4** (pin the dismissal outside the scroller) |
| **GCM-9 · D-06/L-8** | `open` is a constant-`true` getter (:36-39) — reka runs fully controlled (`passive: false`, my DialogRoot read) with the parent `v-if` tearing the subtree down synchronously, so `data-state` never becomes `closed`: the exit half of `popover-animate` is **unreachable by construction** while the closing comment (:258-260) credits it; and `@close` is a mandatory-but-unenforced contract (any v-show consumer gets an undismissable trap). | reka reads; GalleryView :390/:394; animate utility data-state-keyed (DU-verified, mechanism-consistent with my dist read) | **→ F.W3/W4** (local `ref(true)` flipped before emit — correct standalone AND restores the exit animation) |
| **GCM-10 · D-07/L-5/C-3** | An entry with no `tier` — the **normal** server state (model has no field, `extra="forbid"`; only admin.py :183/:432/:438 ever writes it; `_public_doc` passes the raw doc) — renders a full-strength Bookmark chip with an **empty label**, asserting "saved" about an untiered entry (`v-if !== 'normal'` passes `undefined`; unguarded `v-else`; `:data-tier` omitted → no colour rule matches). Sibling card uses `v-else-if` and never asserts. | api model/routers greps this session; the file :84-92 | **→ F.W3/W4** (guard) + **F.W5-W8** (a `tier` field with a default on the model — the serialization-bypass family, with MF-9) |
| **GCM-11 · D-08/L-11** | Zero posture on the hero: no `@error`, no `@load`, no skeleton, no `decoding`/`fetchpriority`, on a live server-side PIL render (AVIF/WEBP encode per request). R-4 ruling; sibling at least sets `loading="lazy"`. | images.py + api.ts + the file :78-82 | **→ F.W3/W4** |
| **GCM-12 · D-09** | The "Decomposition" plate renders unconditionally — a titled `bg-muted/30` box with an empty flex row whenever `basisLabels` is empty (`active_bases` unconstrained `string[]`, server default `[]`; the `:46` null-guard is the admission). | the file :41-56/:124-141; types.ts :214; model default | **→ F.W3/W4** (empty state or `v-if`) |
| **GCM-13 · D-10** | No in-flight state on `like`/`set-tier`: no `:disabled`, no pending ref, no re-entry suppression anywhere in the chain (component, view handlers, store) — N clicks = N mutations. glass 7.0.0 ships `loading` first-class (my tag read of Button.vue); 4.0.0 has no equivalent. | the file; GalleryView :124-135; gallery.ts | **→ F.W3/W4**, sequenced WITH F.W1 (the `loading` prop is the cure's seat) |
| **GCM-14 · D-11(a)** | At 393px the plate is **flush to both viewport edges** (`w-full` survives the cn merge; fixed → viewport containing block) with `border-2` + `rounded-xl` corners against the screen. **Survives the 7.0.0 uplift unchanged** — my tag read: no `:where()` inline-size clamp exists at v7.0.0 (the clamp is 8.0.0's styles.css). One of exactly TWO hand-rolling consumers (K-14: ExportModal's `export-dialog` is the other). | cn + dist + tag reads | **→ F.W3/W4** (retire `w-full`, adopt the producer geometry), sequenced with F.W1 |
| **GCM-15 · D-12** | All four authored icon sizes inside Buttons are dead under the CVA's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` descendant rule ((0,2,1) beats lucide's presentational attrs and `.h-4`), and on coarse pointers those glyphs render 24px while the two un-Buttoned glyphs (:89 Crown, :105 Eye) stay 16px — **a 1.5× inversion invisible in source**. Mechanism FOLD → fr-BasisSelector **m-22(f)** (banked); the coarse INVERSION against siblings is this component's increment. | dist CVA string + tokens read this session; fr-BasisSelector :88 re-read | **→ F.W3/W4** (inside the m-22 repair family) |
| **GCM-16 · D-13** | Zero headings — both section titles are `<span>`s (:126, :146); no `role="heading"` anywhere (my grep). Compounds GCM-4. | the file | **→ F.W3/W4** |
| **GCM-17 · D-14/L-16/D-22** | The `alt` is an asset FK duplicated verbatim as visible text (:80/:98), while the entity's real identity — `title`, `description`, `owner_slug`, `tags` — is never rendered anywhere in the modal; the detail view shows the card's payload plus a bigger image. Write path exists (Create/Patch accept all three). | types.ts :207-238/:248-262; the file | **→ F.W3/W4** (one edit with GCM-4: title the dialog with the entity, `alt` from prose or empty) |
| **GCM-18 · D-16/U-1** | `Button variant` does not exist at 7.0.0 (my tag read: `ButtonProps` = emphasis/tone/size/iconOnly/loading, default secondary) — 4 callsites here go dead and collapse to the secondary glass capsule under three scoped recipes written against ghost/outline chassis. **Identity FOLD → fr-AdminAuditLog AA-2** (banked BLOCKER census-extension, 37 files/38/124); the census recommendation killed as re-book (K-10). | `git show v7.0.0:…/Button.vue` + MIGRATION.md :73/:350 (my reads) | **FOLD → AA-2 (F.W1)**; the three scoped recipes re-derive against the new chassis in the same edit |
| **GCM-19 · C-7** | Both ℱ glyphs resolve through fonts the app never loads, via two disjoint stacks (`cm-serif` → undeclared `--font-serif-math` → generic serif; `fourier-f` → `--font-display` → unloaded Plus Jakarta). **FOLD → FR-AH-2** (banked five-way MISS-FONT identity, blocker-weight cluster). Increment banked inside the fold: 4 carrier sites in one card, and DU's refinement verified by this seat — "Plus Jakarta Sans Fallback" IS a declared `local()` metric-face family (scale.css :17-46), so the codepoint falls **per-character** past four payload-less faces, not to a missing family. | my scale.css read; utilities/bridges chain reader-verified, consistent with FR-AH-2's banked chain | **FOLD → FR-AH-2 (F.W4 + GLASS-RELAY)** |
| **GCM-20 · L-3/C-4** | `VIZ_COLORS.fourier` at :150 renders `#888888` — `cssVarToHex` has no `oklch()` arm and every producer-sourced viz token is oklch. **FOLD → fr-BasisCanvas D-1/BC-1/C-1 via fr-App B-1** (banked, → F.W1, with the R-6 remedy facts: `colorUnit2 ∘ parseCSSColor`, never `color2`). | colors.ts :20-55 re-read; C-4's falsifier CELL killed (K-8) | **FOLD → banked B-1 (F.W1)** |
| **GCM-21 · L-4/C-5** | `basisDisplay` snapshots the reactive palette at module eval — the pills never track theme and diverge from the :150 readout. **FOLD → fr-BasisCanvas D-2/BC-5/C-3** (banked → F.W1) **with the fr-App R-1 entry-route condition** (K-9: on the default `/paper`-first path both sites agree grey; the red/grey split-brain needs a deep-link/saved-tab `/gallery` entry). The "NEW — CONTRADICTS the corpus" banner killed (K-7). | basis-display.ts + main.ts + router reads this session | **FOLD → banked D-2 (F.W1)**; every witness pins the entry route |
| **GCM-22 · M-α (DU missed find, admitted)** | **The F.W1 uplift silently breaks `p-0`**: at v7.0.0 the dialog pad moves to `px-(--overlay-pad-inline)`/`py-(--overlay-pad-block)` longhands, which land in DIFFERENT cn buckets (`padding-x`/`padding-y` vs `padding` — verified at the tag, first-match classifier), so the consumer's neutraliser stops displacing anything, both ship, and tailwind emits the longhands later — **the full-bleed hero regains ≈24px/30.5px insets**. Mechanism + emission order FOLD → banked **TEP-12 + the SwatchHoverMenu byte-read instrument** (R-5); the fourier instance is the increment. The D axis's §6 concluded the opposite ("an override of nothing"). | my tag reads of DialogContent.vue + class-names.ts; R-5's banked instruments | **→ F.W1 ledger rider** (retire `p-0` for authored bleed geometry in the same transaction); build spot-check → SS-13 #5 |
| **GCM-23 (DU missed find, admitted)** | The primary CTA **squares off on keyboard focus**: style.css's UNLAYERED `.callout-btn:focus-visible { …; border-radius: inherit }` (:136-143, after `@layer base` closes — my read) beats `@layer components`' `.btn-pill { border-radius: var(--radius-pill) }`, and the CTA's parent (:95 flex column) has no radius → `inherit` = 0. The element's own border-radius changes on the keyboard path only — the stadium visibly deforms on the one control the modal exists to offer. `border-radius: inherit` is the wrong instrument (modern outline follows the element's radius); all four rule members carry it. D-24 read this exact rule and saw only a redundant ring. Registry-novel (my sweep → 0). | style.css + dist `btn-pill` layer verified this session | **→ F.W3/W4** (drop the `border-radius: inherit` declaration rule-wide; pairs with the D-24-as-rescoped cure) |
| **GCM-24 (LC missed find, admitted)** | `gallery.setTier` **discards the fresh entity the operation already returns** (admin.py re-reads and returns `_public_doc(updated)` + ETag — my read; api.ts types `Promise<Visualization>`) and pays `resetAndFetch()` for it — destroying accumulated infinite-scroll state on every tier toggle; `deleteEntry` repeats the shape. `entries.value[idx] = updated` was on the wire, unclaimed — with GCM-3's fix it makes the modal correct at zero network cost. Registry-novel: FR-AFP-11 banks the **inverse** (dupe minus resetAndFetch) — complements, one shared-invalidation cure. | admin.py :178-190, api.ts :506-516, gallery.ts :138-158 — all my reads | **→ F.W3/W4** (with GCM-3; cross-cite FR-AFP-11's "delegate to the store or a shared invalidation channel") |
| **GCM-25 (LC missed find, admitted)** | `/v/:visualizationSlug` is not merely UI-unreachable: **a deep link/bookmark to it is non-functional** — VisualizationView reads no `visualizationSlug` (my grep → 0), `useWorkspaceLoader` reads only `imageSlug` (cold visit → empty upload state under a `/v/` URL; warm visit → `router.replace` **away** from the shared entity). Corrects the banked **FR-AH-3**'s narrowing rationale ("deep links/bookmarks only" — that surviving path is itself broken). Rides GCM-1's identity, distinct fact. | useWorkspaceLoader :24-47 + VisualizationView greps, my reads | **→ F.W4** (same repair unit as GCM-1; FR-AH-3 rider recorded) |

### MINOR

| id | claim | disposition |
|---|---|---|
| **GCM-26 · D-11(b)** | `max-h-[90vh]` uses the large viewport where the repo's own entry uses `dvh` (style.css :21); the 7.0.0 oracle is correct at the tag (`centerScrollClasses` = `calc(100dvh-2rem)`, my read). Pixel clip → SS-13. | **→ F.W3/W4** (adopt the producer `scroll` posture at F.W1) |
| **GCM-27 · D-17** | Twelve spacing rungs, two off-scale arbitraries, no repeated interval for a repeated relationship (corpus-derived; reader-unchallenged; seat spot-checks passed — R-10). | **→ F.W3/W4** |
| **GCM-28 · D-18** | The modal drifted from GalleryCard's identical stat-row recipe by non-round deltas (`gap-[0.3rem]`/`0.3rem` vs `gap-1`/`0.25rem` — all four cells verified by this seat). | **→ F.W3/W4** (dies inside GCM-30's dedup) |
| **GCM-29 · D-19/L-6/C-12** | `basisLabels` + `timeAgo` byte-identical to GalleryCard (my zero-output diff re-run); `timeAgo` ×5 in one directory in two dialects. **FOLD → fr-BasisSelector M-10** (basisLabels clone banked, repair deletes two whole functions) **+ FR-AUL-17** (timeAgo family banked) **+ intake R3-12** (denominator). | **FOLD (F.W3/W4 · F.W3)** |
| **GCM-30 · D-20** | Nested equal 12px radii inside a 2px border (:73/:75) — concentric requires inner = 10px. Sliver → SS-13. | **→ F.W3/W4** (dies with GCM-52's wrapper deletion) |
| **GCM-31 · D-21/C-8** | `rounded-dialog` + `rounded-xl` both ship (cn's closed rounded enum, verified at both pins); winner is emission-order-decided. R-3 ruling. | **→ GLASS-RELAY** (producer: teach `cn` its own aliases) + site cure F.W3/W4; NEEDS-BUILD probe |
| **GCM-32 · D-23** | `.tier-btn[aria-pressed="true"]` silently replaces the primitive's shipped pressed treatment (`aria-pressed:bg-accent…` verified in the outline variant string). | **→ F.W3/W4** (re-derive with GCM-18) |
| **GCM-33 · D-24-as-rescoped** | The CTA composites two differently-shaped focus indicators (`focus-ring` box-shadow + the unlayered global outline). The axis's CURE is killed (K-5: deleting the selector strips PaperArticleWindow's bare router-link of its only indicator — my read); cure must be site-scoped. | **→ F.W3/W4** (with GCM-23 — same rule) |
| **GCM-34 · D-25/L-10** | `timeAgo` truncates at days forever and samples `Date.now()` during render, no ticker. **FOLD → FR-AUL-17.** | **FOLD (F.W3)** |
| **GCM-35 · D-26** | The ornamental `&Fscr;` sits inside the CTA's accessible name with no `aria-hidden` (lucide neighbours auto-hide — verified by DU at Icon.js:41, adopted); the "glass-7 dialog glyph" neighbour cite was an 8.0.0 fact (K-1). | **→ F.W3/W4** |
| **GCM-36 · D-27** | The like button renames itself on activation (count inside the accessible name + `aria-pressed` flip in one beat) — live only once GCM-3 unfreezes the count. | **→ F.W3/W4** |
| **GCM-37 · D-28/C-17** | Dead `@reference "tailwindcss"` (:198; no `@apply`/`theme()` in :200-256 — read whole); 7 of 12 gallery SFCs share the tic. | **→ F.W3/W4** hygiene |
| **GCM-38 · D-29** | The detail view drops the card's graph-paper motif and flips three image axes at once (4/3→16/10, cover→contain, 85→100%), none stated. | **→ F.W3/W4** |
| **GCM-39 · D-30/L-9/C-9** | Both emit parameters are named `hash`, every callsite passes a slug (types.ts §1 rule verbatim); the vocabulary propagated through the whole parent. C-9's verb cell killed (K-12: it is a **PUT** — my reads of admin.py + api.ts). | **→ F.W3/W4** (rename at source) |
| **GCM-40 · L-12/C-13** | `.filter(Boolean) as {…}[]` — unchecked assertion where a predicate is free; LC's executed TS 6.0.3 probe adopted (still no narrowing). | **→ F.W3/W4** (dies inside GCM-29's dedup) |
| **GCM-41 · L-13** | `:key="b.label"` collides — LC's sharpening ratified by my read of :44-52: ANY two distinct `fourier*` tokens (e.g. `fourier` + `fourier-modes`) fold to duplicate "Fourier" keys; no duplicate array entry needed. | **→ F.W3/W4** (key on the source token) |
| **GCM-42 · L-14** | Zero tests at every level: no vitest anywhere in web/ (my package.json read), 8 e2e specs, gallery.spec.ts never opens a card and its console guard filters `error`-only + `404` — blind to GCM-4's warnings three ways. **FOLD → AA-44 / FR-AUL-16 class.** | **→ F.W9/W10** (AA-44's route) + FR-AUL-16's NO-WAVE-OWNER coverage input cross-cited |
| **GCM-43 · C-10** | `:class="{ active: … }"` on both tier buttons is a dead binding — no reachable `.active` rule (my grep of the file + style.css); the correct idiom used ten lines earlier (`.like-btn.liked`). | **→ F.W3/W4** |
| **GCM-44 (DU missed find, admitted)** | Initial focus lands on the **Like button** — a mutating control whose mutation is fictional (GCM-2) and whose count is frozen (GCM-3): FocusScope focuses the first tabbable in DOM order (my reka read); everything before the like Button is div/img/span; the ✕ renders last at BOTH pins (my dist + tag reads). Same pattern class as banked fr-ExportModal **M-δ** (different component — new instance, cross-cited, not a fold). | **→ F.W3/W4** (`@open-auto-focus.prevent` onto the GCM-4 title — one edit, two rows) |
| **GCM-45 (DU missed find, admitted)** | `.like-btn { height: auto; padding: 0 }` strips the Button chassis's whole target box — ~17-20px tall, under SC 2.5.8's 24px with no exception available, and the coarse floor cannot reach it (`data-size="sm"`, not `"icon"` — my a11y-overrides read). The same defect D-03 filed against the library's ✕, at a control the component itself authored; all three axes missed it. Fold-adjacent to FR-AUL-19 (distinct: those keep the coarse floor). | **→ F.W3/W4** |
| **GCM-46 (DU missed find, admitted)** | `.modal-card { box-shadow: var(--shadow-modal) }` silently replaces the **glass material**, not just the shadow: `surface="opaque"` puts the plate on `.glass-floating`, whose three-leg `box-shadow` carries `--glass-material-rim` (my ladder.css read) — a single-value unlayered override strips rim + under-shadow together, documented only as `/* Card shadow */`. Also the concrete falsifier of superlative D-S5 (K-15). | **→ F.W3/W4** |
| **GCM-47 (LC missed find, admitted, R-6-corrected)** | The closed `GalleryTier` union is re-declared inline at **8 sites / 7 files** (verbatim ×6, hand-widened ×2) while the named export sits one identifier from an existing import in this very file; importers = **3** (api.ts, gallery.ts, AdminFlaggedPanel — seat-corrected from LC's "exactly one"). The banked FR-AFP-59 books the widening direction; this census is the re-declaration direction. | **→ F.W3/W4** (import the type; with GCM-39's rename) |

### INFO

| id | claim | disposition |
|---|---|---|
| **GCM-48 · D-31/D-32/L-15** | `:75`'s wrapper div is retired-Teleport residue (already-positioned/clipping/rounded parent) and the :76-192 indentation never re-flowed; LC's correction adopted (close sits at 16 spaces, not D-32's 17 — my read). | **→ F.W3/W4** (delete; kills GCM-30) |
| **GCM-49 · D-33** | Crown/Bookmark serve as tier read-out (:89-90) AND tier command (:166/:176) with no differentiation (seat-verified; reader-unchallenged — R-10). | **→ F.W3/W4** |
| **GCM-50 · D-34/U-5** | Badge outline rim changes shape at 7.0.0 (base drops `border`; `badge-atom--outline` rim — my tag read of badge/index.ts); `.basis-tint`'s bare `border-color` re-tints a hairline it did not author. | **→ F.W1 ledger** |
| **GCM-51 · C-14** | value.js `color2`-over-hand-constructed-`OKLCHColor` silently mis-converts while `colorUnit2 ∘ parseCSSValue` is exact — **CONFIRMED-AND-BANKED** at fr-App R-6 (which adds the sharper `color2(parsed.value)` trap + the `light-dark()` used-value caveat). | **FOLD → fr-App R-6 remedy facts (F.W1)** |
| **GCM-52 · C-15** | The modal's only API call is unauthenticated, visibility-unchecked, and publicly cacheable for a day (my images.py read) — a draft/unlisted viz's source image is world-readable by slug. R3-7b instantiated at this leaf. | **→ F.W5-W8** |
| **GCM-53 · C-16** | Five runtime-imported packages declared as devDependencies (my package.json read: cva, clsx, lucide-vue-next, reka-ui, tailwind-merge); the census pin row audits `dependencies` only. | **→ F.W1** (census correction, per the fr-ExportModal precedent) |
| **GCM-54 · L-17/L-18** | Both required-coverage negatives are CORRECT (my own exhaustive grep over all 261 lines: the only `v-for` hosts a component; zero listeners/timers/observers/watches — the teardown class is uninstantiable). L-17's generalisation (canonical-form collapse over duplicated derivations = a second loop-denominator leak, co-extensive with GCM-29) carries. | **→ F.W4** (denominator method input) |
| **GCM-55 (LC missed find, admitted)** | The store's `likes ?? 0` guard and the template's bare `{{ entry.likes }}` assert opposite nullabilities of a field the wire serves raw (`json.dumps` bypasses the Pydantic model — my visualizations.py read); one of them is wrong by construction. Same serialization-bypass family as GCM-10. | **→ F.W5-W8** (with GCM-10's model fix) |

## Killed-claims register — 16 entries (every kill re-derived before sustaining)

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **The D axis's entire §6 7.0.0-oracle ledger where it cites `dialog/styles.css`** — U-3 (`:where()` gutter clamp defeated by `w-full`), U-4 (room radius cure), I-1 (✕ authored first), I-2 (44×44 ✕), I-3 (`opacity-70` retired), I-6 (`dismiss` axis), I-7 (`:has()` header geometry), the D-11a docblock quote, D-26's second neighbour cite | **SUSTAINED-COMPOSED (DU's K-12-REPEATED ground + LC's K-9 baseline-law ground), re-derived at the tag by this seat**: `git ls-tree v7.0.0 -- src/components/dialog/` has **no styles.css** (added `b155ca4c`, present at v8.0.0); at the tag `showClose` survives, the ✕ is 16px/`opacity-70`/slot-LAST, baseClasses is a plain utility string with **no clamp**. The axis's oracle (working tree at `51cfdfaf`, "7.0.0" version field) carried landed 8.0.0 work. **Survives at v7.0.0**: U-1/D-16, U-2 (no `"icon"` in `ButtonSize` — my read), U-5/D-34, I-4 (`100dvh` scroll). Net: the axis's "three of fourteen MAJORs die at the uplift for free" is FALSE at 7.0.0; GCM-6/GCM-7/GCM-14 must be authored either way at that target. |
| **K-2** | D-15's contrast rows 1 and 3 (fourier pill 3.77:1; N= 4.48:1) and its "passing" column (chebyshev 5.27, legendre 4.59, "entire dark arm passes") | **SUSTAINED (DU's re-ink + LC's K-5 provenance ground), settled by this seat's own instrument**: the pill ink is the FROZEN `basisDisplay` copy, never the live token (the axis's own D-19 proves it); recomputed at the values that paint: **4.165 / 3.968 / 4.398** (my node run — DU's figures to the third decimal), N= at `#888888` **3.346**. One third of the exculpatory column inverts (chebyshev + legendre FAIL). **FOLD → fr-BasisSelector B-3** (banked, both-themes-fail, F.W3/W4); R-8 records the ground-dependence of the two figure sets. A repair keyed to darkening `--viz-fourier` would not move this pill at all. |
| **K-3** | D-S3's superlative clause: "the dark arm consequently passes **every** leg I could compute … the light arm is the whole of D-02/D-15" | **SUSTAINED (DU)** — the frozen light-seed literals reach the dark arm unchanged; DU's dark-ground recompute (3.65/3.90/3.47, all under 4.5) adopted with this seat's one-value sanity check (~3.6 for `#bf4040` over the near-black ground — consistent). The pills fail in BOTH arms; B-3's banked line ("the theme the corpus cleared is the theme that fails hardest") reproduced one component later. |
| **K-4** | D-02's "1.59 is the CEILING, not the figure" framing | **SUSTAINED (LC's K-6), re-derived**: contrast vs the composited plate is **non-monotonic** in plate luminance (falls toward 1.0 near L≈0.586, rises again, passes 4.5 below L≈0.09) — 1.587 is a point value over the light page ground; over a dark contour render the chip can PASS. The defect stands (GCM-5); the "worse than worst case" rhetoric does not. |
| **K-5** | D-24's prescribed cure ("remove the `.callout-btn` selector from style.css — the ring survives via `focus-ring`") + its scoping claim ("the other three selectors are needed; only this one is not") | **SUSTAINED (DU), verified**: `.callout-btn` has TWO carriers — this modal's glass Button AND **PaperArticleWindow.vue:112-119's bare `<router-link class="callout-btn">`** (my read), which has no `focus-ring` and whose scoped rules declare no focus treatment. The deletion strips the paper callout's only focus indicator. Cure re-authored site-scoped (GCM-33); the ColorSpaceSelector addendum-2 law ("the cure is adjudicated separately from the defect") fires again. |
| **K-6** | C-6's fold premise: "ExportModal/challenge-C:186 already names ExportModal and GalleryCardModal as the TWO title-less outliers" | **SUSTAINED (DU + LC's K-1), verified**: `ExportModal.vue:51` ships `<DialogTitle>Export Frame</DialogTitle>` (my grep census). GalleryCardModal is the SOLE offender among five callsites. The defect survives (GCM-4); C's provenance chain does not, and the fold must never spread the finding to ExportModal. |
| **K-7** | C-5's "[NEW — CONTRADICTS the corpus]" banner (basis-display freeze as a novel find) | **SUSTAINED (DU)** — REGISTRY-FIRST violation: the mechanism was banked before the challenge was written (fr-BasisCanvas D-2/BC-5/C-3, re-read this session at :43) and re-cited at fr-App m-8 and fr-BasisSelector C-4/L-M6. TRUE finding, re-booked "NEW" claim. |
| **K-8** | C-4's second falsifier cell: "`@property` registrations only for `--dock-morph-t`, `--fade-start`, `--fade-end`" | **SUSTAINED (LC's K-2), verified at the installed pin**: property-regs.css registers progress-crescendo / phase-tint-amount / specular-x·y·intensity / glass-level / ui-scale — none of C's three names exists (my grep). The CONCLUSION (no `<color>` registration) survives; the evidence cell must not be copied into F.W1. |
| **K-9** | L-4's and C-5's UNCONDITIONAL red-pill/grey-readout divergence ("six template lines apart", flat fact) | **SUSTAINED (both readers, = the banked fr-App R-1 correction), re-verified end-to-end**: `main.ts:11` mounts after `router.isReady()`; `/` redirects to `/paper` by default; basis-display is imported only under `components/visualization/**` — on the default entry it snapshots post-poison `#888888` and **both sites agree grey**. The split-brain needs a deep-link/saved-tab `/gallery` entry. Every SS-13 witness and repair test pins the entry route. |
| **K-10** | D-16's closing recommendation ("amend the census break surface with this row before F.W1 budgets") | **SUSTAINED (DU)** — already executed and banked as fr-AdminAuditLog **AA-2** (BLOCKER census-extension, 37/38/124 — re-read this session). The defect instance confirms (GCM-18); the recommendation is a re-book. |
| **K-11** | C-1's failure-scenario cell ("the count goes 12 → 13, the heart fills…" in the modal) | **SUSTAINED (LC's K-4)** — contradicted by C's own C-2: the modal's `{{ entry.likes }}` binds the orphan and never shows the bump; the 12→13 happens on the grid card behind the scrim. Two BLOCKERs in one document asserted contradictory renderings of one line; C-2's is correct. GCM-2's verdict unaffected. |
| **K-12** | C-9's (and C §0's) claim that `set-tier` feeds `PATCH /api/admin/visualizations/{slug}/tier` | **SUSTAINED (LC's K-7), verified**: `@admin_router.put` (admin.py :160-162) and `method: "PUT"` (api.ts :513) — my reads. Self-undermining in a row about contract-vocabulary integrity; the `If-Match: "*"` bypass is deliberate and documented (not a finding). |
| **K-13** | L-18's parenthetical "~2.3× linear over-fetch" for `resize=1024` into a 28rem frame | **SUSTAINED (LC's K-8)** — assumes DPR 1; at DPR 2 the 448-CSS-px frame is 896 device px → ~1.14×. The untuned-parameter observation survives; the magnitude would mis-budget a perf fix. |
| **K-14** | D-11(a)/D-01's sibling enumeration ("the other four DialogContent sites pass only `class=\"max-w-sm\"`" — listing three) | **SUSTAINED (DU)** — the fourth is `ExportModal.vue:49` `class="export-dialog"`, a bespoke geometry class (whose `min-width: 300px` fr-ExportModal M-γ books as inverting at the uplift). The sentence that made this component look like the lone hand-roller; it is one of two. |
| **K-15** | D-S5's superlative ("the scoped block documents each override against the primitive it overrides — falsifier: find an undocumented one") | **SUSTAINED (DU), falsifier met and verified by this seat**: `.modal-card` (:200-203) is prefaced only by `/* Card shadow */` and silently replaces `.glass-floating`'s three-leg material stack (rim included — my ladder.css read). Four of five scoped rules meet the standard; the fifth, the only one that strips producer material, does not. Books as GCM-46. |
| **K-16** | The C axis's per-claim `file:line` provenance guarantee, for its `<style scoped>` anchors | **SUSTAINED (DU), spot-verified**: the D.W4.c motion comment is at **:258-260** (C cites :217-219, 40 off); `.tier-btn` family at **:235/:240/:241-244** (C: :238/:244/:245-248); `.basis-tint` at **:208-212** (C: :207-211). Outside the style block C's anchors check out; the D axis's anchors were exact at every site this seat re-checked. Not a defect kill — a provenance-grade demotion: repair waves take style-block anchors from THIS record or the live file, never from the C challenge. |

**Recorded, not a kill**: reader-LC's **self-kill K-10** (the hypothesis that glass-ui 4.0.0's undefaulted `modal: {type: Boolean}` Vue-casts to `false` and silently de-modalizes reka) — refuted by reka's `useForwardProps` defined-key filter, the identical mechanism the banked fr-ExportModal S-1 verified; preserved so no future seat re-runs it. **Seat corrections against the readers**: R-5 (DU's M-α residue narrowed by banked instruments), R-6 (LC's importer count), R-7 (both readers' recordView scenario cells).

## Superlatives — 10 (provenance marked)

1. **The D.W4.c re-point is genuine, complete, and outran the roadmap** (all three axes + both readers; this seat's own exhaustive grep over 261 lines: zero listeners/timers/observers/watches/lifecycle — the leak class is uninstantiable, and `web/DESIGN.md:31` still lists the migration as open).
2. **The `open-visualizer` payload is RIGHT and the naive accusation refuted before it is made** (L-S3, both readers; my router + loader reads confirm the emit type honestly names `imageSlug` and matches its consumer exactly — the defect is the destination, not the payload; the distinction is what made GCM-1 a BLOCKER rather than a rename).
3. **`--pill-c` is the correct one-variable-three-channels idiom** (C-S3/L-S4, both readers) — one inline custom property, three `color-mix` derivations crossing the scoped boundary; the tint PATH needs no migration, only its SOURCE (the banked snapshot) is defective.
4. **All three toggles carry `aria-pressed`, and the like state encodes as SHAPE** (`:fill` current/none — survives colour-blind and forced-colors readings); the sibling card renders the same actions with none. Fixing GCM-2/GCM-3 makes this load-bearing rather than decorative.
5. **Motion is PRM-safe by inheritance and the component adds nothing that escapes it** (zero `@keyframes`/`transition` authored; it did NOT copy the card's `like-bounce`, the one motion that needed a local guard).
6. **Uniform deep-subpath imports, 3/3** — all three subpaths survive at v7.0.0 (DU's export-map enumeration adopted; consistent with my tag reads); the neighbour pulls the barrel for one Checkbox.
7. **The image box is reserved before the bytes arrive** (`aspect-[16/10]` — real CLS discipline; the missing affordance INSIDE the box is GCM-11, the reservation itself is right).
8. **The D axis's line provenance is exact** (DU re-checked ~40 anchors, this seat re-checked a dozen more — every one landed): its contrast METHOD failed (K-2) but its arithmetic reproduced to two decimals on every correctly-inked row. The standard C's style-block cells fell short of (K-16).
9. **The L axis's §4 required-coverage negatives are exemplary audit hygiene** (both readers; my re-read): a checked-and-inapplicable class recorded with proof, plus a genuinely new denominator mechanism (canonical-form collapse) carried to F.W4.
10. **Reader discipline this round** (seat's own): both readers held the identity guard across 12+ banked fold targets with zero re-books; DU's kill arithmetic reproduced exactly under my independent implementation; LC's missed find #1 (GCM-24) found the one-line cure the whole corpus missed sitting on the wire.

## UNPROVEN-NEEDS-LIVE / NEEDS-BUILD — SS-13 visual-audit inputs

1. **D-04** — measured worst-case contrast of the unplated ✕ over a real contour render (structure source-proved). → SS-13.
2. **D-11(b)** — the iOS-Safari 90vh clip in pixels, URL bar shown (unit divergence source-proved). → SS-13.
3. **D-20** — the rendered corner sliver from equal nested radii (arithmetic deterministic). → SS-13.
4. **D-21/C-8 + GCM-31** — which of `.rounded-dialog`/`.rounded-xl` Tailwind v4 emits last. → NEEDS-BUILD (one emitted-CSS byte-order probe; the p/px/py half of the same probe is already settled by the banked SwatchHoverMenu byte-read — R-5).
5. **GCM-22 (M-α)** — one-line spot-check of the padding pair at fourier's OWN build at the F.W1 target (mechanism + order settled by banked instruments; this is confirmation, not proof). → NEEDS-BUILD, cheap.
6. **C-7** — which concrete glyphs render for the two U+2131 sites per platform (stack resolution proved and folded to FR-AH-2; the four `local()` PJS-Fallback faces verified by this seat carry no U+2131, so the codepoint falls per-character). → SS-13.
7. **C-17** — Tailwind-v4 build cost of the 7 inert `@reference`s. → NEEDS-BUILD, low priority.
8. **THE F.W1 TARGET TAG — FORMATION DECISION, not a fact this seat can settle**: producer is at 8.0.0 (v8.0.0 tagged; HEAD `350f7a90`); the census pins glass 4→7; fr-ExportModal r2 already adjudicates 8.0.0-aware rows. If F.W1 re-points at 8.0.0: kill K-1's seven dead rows revive (the ✕/gutter/dismiss cures land, GCM-6 becomes free), GCM-22 is superseded by the `:where()` clamp (which `w-full` then genuinely outranks — the axis's U-3 logic, correct at the wrong version), and GCM-14's cure inverts from "adopt producer geometry" to "retire `w-full` so the clamp can act". **F.W1 must state its tag before this component's uplift ledger is executable.**
9. **Any witness of GCM-21's split-brain pins the entry route** (K-9): deep-link/saved-tab `/gallery` → `#bf4040` pills; default `/paper`-first → both grey. Unpinned recipes are unfalsifiable.
10. **Dark-arm pill figures** (K-3): DU's 3.65/3.90/3.47 adopted with a one-value seat sanity check, not a full recompute — a repair asserting exact dark-arm ratios re-runs the instrument at the composited dark ground.

## DISSENT — preserved, not ruled away

1. **Reader scorecard.** reader-DU: the strongest single contribution is the two-ground kill architecture on the §6 uplift ledger plus five missed finds that all survived re-proof (M-α's cn-bucket derivation is the round's sharpest increment); debits: the M-α residue was already settled in the registry its sweep under-scanned (R-5), and its D-15/D-S3 dark figures are adopted-with-sanity-check rather than fully re-derived. reader-LC: four-for-four on missed finds including the round's best cure-shaped find (GCM-24) and the /v/ deep-link correction that reaches a banked row (FR-AH-3); debits: one census cell wrong by two (R-6), and its C-2 scenario cell (like DU's) missed the `viewedHashes` guard (R-7). Both readers: zero identity-guard violations across the banked fold set.
2. **Severity dissents carried**: DU's BLOCKER on GCM-4 (unnamed dialog) and GCM-5 (tier chip); LC's MAJOR on GCM-31 (radius conflict) and MINOR on GCM-11 (image posture). A future seat re-grading GCM-4 upward should weigh that the SR composite (GCM-4∘16∘17∘6/8) was named blocker-weight here without a BLOCKER row to carry it.
3. **Reader coverage gap (fleet evidence)**: corpus rows D-17, D-18, D-33 appear in neither return — neither confirmed nor killed. Two hostile re-readers over a 34-row axis both silently dropped the same three low-severity rows; full-roster reconciliation (this record's roster table) is the cheap guard.
4. **Substrate note**: the subject file is dirty by one hunk (`variant`→`surface`, :72) atop `cd26c65` alongside ~30 dirty siblings. All anchors here are working-tree anchors; a committed HEAD that differs will shift them. The one-hunk delta is itself evidence the D.W4.c work is mid-flight — any F wave touching this file re-resolves anchors first (the D-19 lesson).
5. **This seat's scope notes**: lucide `Icon.js:41` aria-hidden, the animate.css `popover-animate` body, and the glass 4.0.0 export-map enumeration were adopted from reader verifications consistent with my dist reads, not independently re-extracted; every verdict-deciding cell was re-derived this session.

## Closing verdict

**ADJUDICATED — the corpus survives its second, adversarial reading with its three BLOCKERs intact and its version story rewritten: 55 roster rows (3 BLOCKER · 22 MAJOR · 22 MINOR · 8 INFO; 12 identity-folds honoured, zero re-books), 16 kills sustained after independent re-derivation (the D axis's entire 7.0.0-styles.css oracle dead at the tag; the D-15 contrast table re-inked and folded to B-3; three cure-integrity kills), 9 missed finds admitted after re-proof at the bytes — the sharpest being the F.W1 `p-0` padding regression (settled by banked cross-repo instruments the readers' sweeps missed), the keyboard-path CTA squaring, and the on-the-wire `setTier` entity discard. The component's data seams (route, operation, prop) are the BLOCKER family and belong to F.W4 + F.W5-W8 + F.W3/W4 respectively; the palette/font/token families fold to their banked identities (F.W1 · F.W4+GLASS-RELAY); the dialog-anatomy and a11y cluster is F.W3/W4's, sequenced against an F.W1 whose target tag (7.0.0 vs the shipped 8.0.0) the formation must now state — seven of this record's dispositions flip polarity on that single decision. Routings are M-25 agglomeration input; nothing here stamps a wave.**

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED.
