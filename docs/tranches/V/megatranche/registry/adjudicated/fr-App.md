claude-fable-5

# fr-App — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-07 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/App.vue` (32 lines, read whole by this seat) + its shell closure: `AppHeader.vue` (354) · `SvgFilters.vue` (178) · `lib/colors.ts` (117) · `DarkModeToggle.vue` · `basis-display.ts` (7) · `useToast.ts` · `main.ts` (11) · `router/index.ts` · `style.css` · `index.html` (38).
**Substrate**: fourier HEAD `cd26c65` / branch `m/w1-bump-migration` — re-pinned by this seat (`git log -1`), identical to the coordinate all three challenges and the banked fr-BasisCanvas pin. **Dirty-tree disclosure**: 28 dirty rows in the fourier working tree at adjudication time; **every file in the subject closure is CLEAN at HEAD** (pathspec `git status` over all eleven → ∅). Installed glass-ui **4.0.0** / producer **7.0.0** (`/Users/mkbabb/Programming/glass-ui`, package.json re-read); installed value.js **0.13.0**; @vueuse/core **14.3.0**; pencil-boil 0.4.1.
**Corpus**: 44 accusation rows + 8 superlatives across three axes — challenge-D (design, 20 rows, opus-5[1m]), challenge-L (library, 11 rows, opus-5[1m]), challenge-C (consumption, 13 rows, opus-5[1m]) — at `docs/tranches/V/megatranche/audit/fourier-components/App/`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Returns arrived through the orchestration channel; every load-bearing, disputed, or newly-minted cell was **re-derived by this seat against the tree with its own instruments** before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. Every kill was re-derived (a weak kill gets un-killed — none needed un-killing, but two reader proof cells died under this seat's reads, R-1 and R-6); every missed find was re-proven before banking (all 11 banked, one narrowed); every reader-vs-reader conflict was ruled with a fresh read. Two reader errors were detected that neither reader caught in itself — reader-LC's L-2 proof cell repeats the exact universality claim its sibling killed, and reader-DU's admin-label count is off by one — see DISSENT.
**Arbiter's own instruments this session (all static + node against installed dists; zero browser probes — probe parsimony; product source read-only)**: full read of App.vue (32); router/index.ts:28-125 (getSavedTab default `/paper`, 9-entry routes array, `/visualize` = alias of `/w/:imageSlug?`); AppHeader.vue anchors :49/:54/:108-125/:137-139/:155-160/:170-222/:232-258/:276-302/:323-348; SvgFilters.vue:5-27 + `wc -l` = 178; DarkModeToggle.vue:29-31; useCoeffHover.ts:60-65; main.ts (whole); style.css:17-26/:136-143; useToast.ts:1-32; index.html:18-34 (the pre-paint predicate, verbatim); GalleryCard.vue `VIZ_COLORS` grep → 1 hit (the import); `basis-display` importer census → 7 files, **all** under `components/visualization/`; repo greps `scrollBehavior|savedPosition|scrollRestoration` → ∅, `aria-live` → 3 hits, `aria-current` → ∅, `text-admin-label` → **7 sites in 4 files**; installed glass-ui dist: `tokens.css` @import order (:27 color-radius → :32 light-dark → **:33 dark-arm** → :34 property-regs — the R-5 cascade decider), Button CVA (`base = btn-pill tap-squish focus-ring … active:scale-(--scale-press-btn)`, `ghost = bg-transparent text-foreground/70 hover:bg-foreground/8 hover:text-foreground active:bg-foreground/12 …`, `size.default = h-(--control-h-md) px-4 py-2 …`), `glass/surfaces.css:118-158` (btn-pill six-leg transition + the CAPITALISED clobber warning), `utilities/base.css:172-178` (focus-ring) + `:255-275` (tap-squish full set + PRM carve), `utilities/a11y-overrides.css:60-95` (forced-colors Highlight rung incl. `.focus-ring`/`.btn-pill` at (0,2,0)), `tokens/light-dark.css:19-20` (coarse-pointer `--ui-scale: 1.5` / `--control-floor: 2.75rem`), `typography/semantic.css:213` `@utility text-admin-label` + `scale.css:86`; `dist/dark.js` (whole — `installDarkModeSync`, `darkModeSyncScript` handling `"auto"` + setting `colorScheme`, storage key `vueuse-color-scheme`) + `useGlobalDark-C28t0VWJ.js` head (initialValue undefined → vueuse default); @vueuse/core dist: `useColorMode` `initialValue = "auto"` / `storageKey = "vueuse-color-scheme"` (:2027), `useStorage` `writeDefaults = true` (:1908), `useDark` setter `if (system.value === modeVal) mode.value = "auto"` (:2345); pencil-boil `src/vue.ts:96-140` (`start()` gates `prefersReducedMotion()` **before** `subscribers.add` — the K-1 decider, read whole); **producer 7.0.0**: `@utility text-*` roster = 25 names, **no `text-admin-label`** (only the `class-names.ts:84` regex allowlist), `@utility paper*` roster = `paper-underpaint` + `paper-grain-overlay`, `.paper-texture` rule over src/+dist/ → **∅**; **node executions against installed value.js 0.13.0**: `parseCSSColor("oklch(0.579 0.201 30.4)")` OK, `parseCSSColor("light-dark(…)")` OK (round-trips verbatim), `colorUnit2(parsed,"rgb")` → `rgb(0.8433 0.2084 0.1372)` = **`#d73523`** at 255-scale, `color2(parsed,"rgb")` **THROWS** `Unknown source color space: "undefined"`, `color2(parsed.value,"rgb")` → **silently WRONG** `{r:0.3256, g:0.5389, b:0.3242}` (greenish for a red input — this seat's own new evidence); hsl arithmetic: `hsl(35 76% 35%)` = **`#9d6515`**, `hsl(37 73% 67%)` = **`#e8b96d`**; stale dist artifact `web/dist/assets/index-57FkGzlZ.css` → `--viz-fourier:#d73523` / `light-dark(#d73523,#eb7366)` / `#eb7366` (verbatim, and `#d73523` = this seat's own colorUnit2 output — double-keyed corroboration); registry identity sweep over `adjudicated/*.md` for `text-admin-label` / `paper-texture` / `vueuse-color-scheme` / `scrollBehavior` / `installDarkModeSync` / `SvgFilters` (the AA-15 collision found — see MG-β).

## Scoreboard

**45 rows ADJUDICATED-CONFIRMED (1 BLOCKER · 15 MAJOR · 19 MINOR · 10 INFO) · 14 KILLS in the killed register (incl. 2 reader proof cells corrected by this seat) · 11 missed finds banked (6 reader-DU · 6 reader-LC · 1 convergent) · 8 superlatives with provenance.**

Blocker-weight survivor: **B-1/L-1/C-1** — the `--viz-*` palette collapse to `#888888` — **FOLDED BY REFERENCE to the banked fr-BasisCanvas D-1/BC-1/C-1 identity** (App.vue:11 is that row's trigger locus; this record adds the trigger-side facts, the corrected arithmetic, the dist-artifact provenance, and the sharpened chrome↔canvas framing — it does not re-book the defect).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **basisDisplay evaluation order** (L-2/C-3/m-8) | KILLED the universality: entry-route dependent; on the DEFAULT path the snapshot is post-poison grey, not the authored defaults | CONFIRMED L-2 asserting "the route chunk (and basis-display.ts) evaluates **strictly before** App's onMounted" — the exact claim its sibling killed | **DU wins; LC's proof cell CORRECTED.** | My importer census: `basis-display` is imported by **7 files, all under `components/visualization/`** — never by the `/paper` chunk. `router/index.ts:32-34`: `getSavedTab()` defaults to `"/paper"`; `:40-41` redirects `/` there. A first-time visitor mounts on /paper; `basis-display.ts` evaluates only on the LATER navigation — **after** App's onMounted — snapshotting the poisoned `#888888` (frozen and live sides AGREE, both grey; no split-brain). The split-brain (red legend / grey curve) exists only on a deep link or a saved-tab `/visualize`·`/gallery` entry. The defect (module-scope read of a `reactive()`) is real either way; its determinism, the "in one frame" staging as a universal, and the unconditional SS-13 screenshot recipe are all entry-route-conditional. |
| R-2 | **m-3 hover feedback** | KILLED "no perceptible feedback" — the ghost CVA supplies `hover:bg-foreground/8` + an unconditional press squish | CONFIRMED as filed ("the channel reaches almost nothing") | **DU wins on the kill; row NARROWED, not dead.** | My dist reads: `.nav-trigger` sits on `<Button variant="ghost">` (AppHeader.vue:112-114); CVA base = `btn-pill tap-squish focus-ring … active:scale-(--scale-press-btn)`; ghost = `hover:bg-foreground/8 hover:text-foreground active:bg-foreground/12`. The press squish is unconditional (the scoped block declares no `scale`). What SURVIVES: `.nav-trigger:hover` at (0,3,0) beats `hover:text-foreground` and **dims** ink to 70% — inverted direction — and reaches only the 50%-opacity chevron (icon self-colours `--viz-amber` :193; label `display:none`). The `hover:bg` vs scoped `background: none` (0,2,0)-tie rides bundle source order — UNPROVEN (needs a rebuild readback; DU's byte measurement was against a stale Jun-12 bundle). |
| R-3 | **m-7 touch targets** | KILLED the `.nav-trigger` 32px leg (`h-(--control-h-md)` applies: 40px fine / 60px coarse) | CONFIRMED the row whole | **DU wins on the leg; row NARROWED.** | My reads: CVA `size.default = h-(--control-h-md)`; the scoped block sets padding (0,2,0 beats `px-4 py-2`) but **no height**, so the height utility applies; `tokens/light-dark.css:19-20` lifts `--ui-scale` to 1.5 and `--control-floor` to 2.75rem under `@media (pointer: coarse)` → the nav trigger clears the 44px floor exactly where the floor exists. What SURVIVES (both readers agree): `.dark-mode-toggle` 2.5rem base → 2.75rem at ≥640px (my read of :241-257 — inverted: touch gets 40px, pointer gets 44px, on a bare `<button>` sized wholly by `--toggle-size`) and `.admin-badge` fixed 1.75rem. |
| R-4 | **B-2 severity** (`.paper-texture` absent at 7.0.0) | MAJOR (regrade: silent loss of one background pair; no capability loss) | BLOCKER | **MAJOR.** | Fact CONFIRMED at both ends by this seat (installed `dist/styles/cards.css` rule present; producer 7.0.0 `.paper-texture` rule over src/+dist/ → ∅; `@utility paper*` roster = underpaint + grain-overlay only). The failure fires only at the F.W1 uplift and costs one `background-image`+`blend-mode` pair on the shell; no function, a11y affordance, or data path is lost. The finding's real weight is the **break-class**: a CSS-class break the census's import-shaped surface does not model. That weight is carried as a P0 census-gate obligation on F.W1, not as a blocker grade on the class. LC's dissent preserved. |
| R-5 | **C-1's cascade characterisation** (`light-dark()` "wins in every engine") | (did not contest) | KILLED for the dark arm | **LC's kill CONFIRMED by this seat's own read.** | `dist/styles/tokens.css` imports `:32 light-dark.css` → **`:33 dark-arm.css`**. `dark-arm.css`'s `.dark` block (0,1,0) ties `:root` (0,1,0) and is later in source order, so with `.dark` applied the computed string is a **bare `oklch(…)`**, not `light-dark(…)`. Light mode (supporting engines): `light-dark(oklch(),oklch())`. The BLOCKER is untouched — both shapes fall through `colors.ts:53` — but every SS-13 recipe must expect **`oklch(` in dark, `light-dark(` in light**. The L axis alone hedged this correctly. |
| R-6 | **Remedy soundness at value.js 0.13.0** (C-2) | Ran `colorUnit2(parsed,"rgb")` → works; graded C-2 BLOCKER | Ran `color2(parsed,"rgb")` → throws; minted the "remedy does not satisfy the contract" missed row; graded C-2 MAJOR | **BOTH right — different entry points; this seat ran both plus a third probe and found the sharpest trap.** C-2 ruled **MAJOR**. | My node runs against the installed 0.13.0: `parseCSSColor` handles both failing forms (the R1 hedge is DISCHARGED — the crash does not reproduce at 0.13.0); **`colorUnit2(parsed,"rgb")` is the sound conversion leg** — its output at 255-scale is `#d73523`, byte-identical to the stale dist artifact's historical hex (double-keyed proof of correctness); `color2(parsed,…)` rejects the wrapper (`Unknown source color space: "undefined"`); and — **this seat's own evidence** — `color2(parsed.value,…)` does NOT throw and returns **wrong channels** (`{r:0.33,g:0.54,b:0.32}`, green for a red input). The naive "unwrap" repair is worse than the reject. The severity: C-2 is the repair-path row for B-1, not an independent product break — the blocker grade belongs to B-1's banked identity. DU's BLOCKER dissent preserved. |
| R-7 | **m-2/L-3/C-7 severity** (observer teardown + producer duplication) | MINOR | MAJOR | **MINOR.** | Production blast radius is bounded (the root never unmounts; `attributeFilter` is set); the live legs are HMR accumulation, the missing test-mount hygiene, 5 forced style reads per firing, and the producer-facility duplication — and the duplication leg's cure (adopt `@mkbabb/glass-ui/dark`) is the SAME edit as MG-γ's FOUC cure, where the MAJOR weight of this family actually lives. LC's MAJOR dissent preserved; the family lands in one F.W3/W4 item either way. |
| R-8 | **L-3's `colorScheme`-blindness clause** | NOT ESTABLISHED (fourier never writes `element.style.colorScheme` — no second mutation exists for the class filter to miss) | Verified as a live leg of L-3 | **DU wins TODAY; a coupling rider is minted.** | My reads: fourier's pre-paint script toggles only the class; `useGlobalDark` (installed chunk) toggles only the class. No `colorScheme` write exists → no blind spot today. **Rider**: the day MG-γ's cure adopts `darkModeSyncScript` (which sets `e.style.colorScheme`), the class-filtered observer's blindness becomes live in the same commit — so the two must be cured together (`installDarkModeSync` keys off `isDark`, not the attribute, and closes both). |

Cross-cutting arithmetic corrections (this seat's own, against all three axes): the surviving `--viz-amber` resolves to **`#9d6515`** light / **`#e8b96d`** dark (my hsl computation; L said `#a3702f`, C said `#9d6415`, reader-LC's confirmed cell reproduced C's wrong hex — see DISSENT). Route denominator: the routes array is **9 entries / 7 lazy `component` registrations / 6 distinct view modules** (`/v` and `/w`-alias-`/visualize` share `VisualizationView.vue`) — corrects D's "8 routes" and L's ":46-146". `text-admin-label`: **7 sites in 4 files** (reader-DU said 8; my grep and fr-AdminAuditLog's independent census both say 7).

## Verdict register — adjudicated defect roster

Format: **id · severity · claim (one line) · decisive anchor · terminal disposition → route**. Full proof cells live in the immutable challenge files beside this record; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **B-1 / L-1 / C-1 — BLOCKER · FOLD BY REFERENCE (identity guard).** `resolveVizColors()` — triggered solely by `App.vue:11` (sole caller, re-verified) and re-fired by the `:13-17` observer on every theme flip — writes `#888888` into fourier/chebyshev/legendre/green: every `--viz-*` token at installed glass-ui 4.0.0 is `oklch()` (dark arm, per R-5) or `light-dark(oklch(),oklch())` (light arm), and `cssVarToHex` (`colors.ts:22-54`) has no matching arm; no `@property` color registration exists. **This is the banked fr-BasisCanvas D-1/BC-1/C-1 identity — ONE defect, already routed → F.W1; never re-booked.** App-side additions this record contributes to that identity: (a) the trigger locus and the observer's re-poisoning loop live HERE; (b) the sole survivor `--viz-amber` = `#9d6515`/`#e8b96d` (corrected arithmetic); (c) **the sharpened frame neither axis stated** (DU's K-10): the poisoning is confined to the JS/Canvas path — 20+ live `var(--viz-*)` CSS declarations across the chrome resolve natively — so the true symptom is a **CHROME↔CANVAS divergence**: the header, docks and controls paint correct brand hues while the instrument they frame paints grey; (d) provenance (MG-κ): the committed stale build `dist/assets/index-57FkGzlZ.css` emits `--viz-fourier:#d73523|light-dark(#d73523,#eb7366)|#eb7366` — the prior pin authored HEX (making `colors.ts:29` the then-primary path), the light-mode `light-dark()` arm was ALREADY falling through before the 4.0.0 bump, and `#d73523` is byte-identical to this seat's own `colorUnit2` output for the oklch token. **→ F.W1** (with the R-6 remedy facts: `parseCSSColor`+`colorUnit2` at 0.13.0 is a working leg TODAY; `color2` is a trap in both spellings; `light-dark()` arm-selection requires the used-value probe-element read, never string parsing).

### MAJOR

- **B-2 — MAJOR (R-4 ruling; corpus BLOCKER regraded).** `.paper-texture` (`App.vue:24`) is a live rule at installed 4.0.0 (`dist/styles/cards.css`, `@layer components`, `:where(.dark)` arm) and **definition-absent at producer 7.0.0** (this seat's roster: `@utility paper*` = `paper-underpaint`+`paper-grain-overlay`; `.paper-texture` rule over src/+dist/ → ∅; the only survivors are the `--paper-texture-size` token and stale prose). Neither surviving recipe is a drop-in for an `h-dvh` flex shell. A CSS-class break the census's import-shaped break surface does not model; `.cartoon-card` (style.css:107) is the in-tree precedent. **→ F.W1** with a **P0 gate obligation: the CSS-class census** (diff every glass-ui-owned class fourier applies against the 7.0.0 `@layer components` + `@utility` roster BEFORE the bump) — MG-β below is that census's third instance, found by running it.
- **C-2 — MAJOR (R-6 ruling; DU's BLOCKER dissent preserved).** The cure for B-1 is already installed and unimported: value.js 0.13.0 (root export only) ships `parseCSSColor` handling both failing forms — **executed by this seat, no crash; the value.js memory ledger's R1 is a tranche-U-head artifact, not a 0.13.0 one** (cross-repo note for V·π). The census's F.W1-before-F.W2 sequencing is wrong for this leg: the `colors.ts` cure is landable at 0.13.0 today, alone; only the bare-root-specifier leg is genuinely gated on the uplift. Remedy constraints from R-6: route conversion through `colorUnit2`; `color2` rejects the wrapper and silently mis-converts the unwrapped value; `light-dark()` needs the probe-element used-value read. **→ F.W1** (remedy input to the same wave item as B-1).
- **M-1 — MAJOR.** `.nav-trigger-label` is `display:none` at every viewport (AppHeader.vue:118, :201-204; grep → exactly 2 hits, nothing restores it); the ≥640px block `:218-222` is byte-identical to the base padding — a no-op media query sitting exactly where the restore belongs, two rules above the correctly-restored `.logo-text` pattern; the `:115` aria-label proves the section name was understood to be load-bearing. The header never shows a sighted user which of five sections they are in. **→ F.W3/W4.**
- **M-2 — MAJOR.** No skip link; `<main>` (App.vue:26, verbatim re-read) has no `id`/`tabindex`/name — WCAG 2.4.1 fails on every route; glass-ui ships `.sr-only`, so the cure is three lines in this file. **→ F.W3/W4.**
- **M-3 — MAJOR (evidence corrected per K-2).** No focus move and no route announcement on navigation: `afterEach` (router/index.ts:166-189) does meta + view-transition release + tab persistence only. The axis's "no `aria-live` anywhere" grep is FALSE (3 hits, all admin panes — my grep) — and those two `role="status" aria-live="polite"` regions are the exact idiom the cure needs, already in-tree two directories away. Compounds i-2 (three `<h1>` in the whole tree). **→ F.W3/W4** (one seat with M-2: App.vue owns `<main>`).
- **M-4 / L-4 / C-6 — MAJOR (denominator corrected per K-12).** Zero error and zero loading posture for the lazy route surface (9 entries / 7 lazy registrations / 6 chunks): `grep onError|errorHandler|onErrorCaptured|Suspense` → ∅ whole-tree; `main.ts:11` parks the mount behind `router.isReady()` with no `.catch` (an initial-chunk 404 = mount never called, empty `#app`); an in-app chunk 404 = silent blank `<main>` under a header that still asserts the previous section. The aggravator: `<Toaster/>` (App.vue:31) is a fully-wired channel the error paths never reach. **→ F.W3/W4.**
- **M-5 — MAJOR.** The `role="button"` logo div binds `@keydown.enter` only — no Space (APG violation); and the app's only two outbound attribution links (`grep github.com` → exactly AppHeader.vue:88,:99) live solely inside a HoverCard whose trigger's `@click.stop` navigates away on tap (reka `HoverCardTrigger` default `as:"a"` overridden by `as-child` → bare div; `excludeTouch` guard present). Touch behaviour → SS-13. **→ F.W3/W4.**
- **M-6 — MAJOR.** `env(safe-area-inset-bottom)` is padded onto `<body>` (style.css:24-26, re-read) — outside the `h-dvh overflow-hidden` shell — so it never protects the sole scroller (`<main>`, App.vue:26) and adds an inset-sized document overflow under `viewport-fit=cover`. Mechanism caveat retained (DU): under border-box preflight the overflow comes from the `h-dvh` child + body padding, not `min-height + padding`. Magnitude → SS-13 (notched device). **→ F.W3/W4.**
- **M-7 — MAJOR.** `<Toaster/>` inherits `position="bottom-right"`, whose entire anchor set is `sm:`-prefixed (installed `Toaster-Bm_HQSpc.js`, verbatim) — below 640px the viewport degrades to `fixed top-0 w-full p-4` at `--z-toast:160` over the header's `--z-overlay:50`: full-bleed over the entire chrome, and the anchor flips screen-sides across one breakpoint. The exact inverse of S-2 one line above it. Cite **App.vue:31** (D's `:29` is wrong — my read). **→ F.W3/W4.**
- **L-5 / C-5 (dead-reference half) — MAJOR.** `SvgFilters.vue` is **178 lines** of dead DOM mounted unconditionally at `App.vue:22`: all four filter ids (`title-boil`/`wobble-celestial`/`paper-grain`/`canvas-grain`) have zero consumers repo-wide (both readers' exhaustive greps, incl. `url(#` → ∅ and the quoting-agnostic id sweep → 6 self-hits), while two `useLineBoil` clocks mutate `baseFrequency` on unreferenced `feTurbulence` nodes (~13 setAttribute+querySelector/sec for the tab's life on every route). The `paper-texture` near-miss trap is correctly defused (a CSS utility, never an SVG filter). Census correction carried: §3a's "12 SVG surfaces" → 8 live. The PRM sub-clause is KILLED (K-1) — the non-PRM waste is what survives. **→ F.W3/W4** (dead-code scrub; L-8 dissolves with it).
- **C-4 — MAJOR.** Resolution is hooked at the root's `onMounted` — the one placement that guarantees the child-first race — and the tree documents the race in prose instead of fixing it (`useCoeffHover.ts:60-65`, re-read verbatim: "the canonical fallback used when resolveVizColors has not yet run" → `VIZ_COLORS.amber || VIZ_COLORS.golden`). The routine is a pure global bootstrap; the correct seat is `main.ts` before `app.mount()`. Consumer denominator corrected per K-13: **11** true readers (GalleryCard's import is dead). **→ F.W1** (ships with the B-1 cure — same lines).
- **m-8 / L-2 / C-3 — MAJOR · FOLD BY REFERENCE (identity guard) + R-1 correction.** `basisDisplay` (basis-display.ts:1-7) reads three `reactive()` properties into a plain object literal at module scope — **the banked fr-BasisCanvas D-2/BC-5/C-3 identity, already routed → F.W1; not re-booked.** This record's contribution is the R-1 ordering correction that the banked row's own cell ("reached statically … before App.vue's onMounted") inherits: that staging holds only when the ENTRY route's chunk imports basis-display (deep link / saved-tab `/visualize`·`/gallery`); on the clean default `/paper` path the module evaluates post-poison and freezes **grey**, agreeing with the live side — the red-legend/grey-curve split-brain is entry-conditional. Any SS-13 witness and any repair test must pin the entry route. **→ F.W1** (rider on the banked identity).
- **MG-α — MAJOR (missed find, reader-DU; re-proven by this seat).** Scroll position is never restored and structurally cannot be: App.vue:24-26 makes `<main>` the sole scroller (document scroll range = 0) while the router declares no `scrollBehavior` (my grep: `scrollBehavior|savedPosition|scrollRestoration` → ∅) — vue-router's hook is absent AND would target `window`, and native `history.scrollRestoration` has nothing to restore. Every back-navigation into the book-length `/paper` treatise lands at section 1. The single UX cost of the shell's (good) scroller decision, and all three axes walked past it while praising the decision. **→ F.W3/W4.**
- **MG-β — MAJOR (missed find, reader-DU; re-proven + count corrected + collision found by this seat).** `text-admin-label` is the **third live instance of B-2's break class, found by running the census B-2 prescribes**: a real `@utility` at installed 4.0.0 (`semantic.css:213`, over `--type-admin-label: 0.625rem`), applied at **7 sites in 4 files** (AdminFlaggedPanel :176/:180/:189 · AdminUserList :376/:379 · FrequencyGraph :200 · CoefficientsSpectrum :110 — my grep; DU's "8" corrected), and GONE from producer 7.0.0's 25-name `@utility text-*` roster (my roster read) — surviving only as a `class-names.ts:84` regex-allowlist entry with no rule behind it, which is worse than absence (a validator still accepts it). `vue-tsc` and Playwright are both blind; the 10px admin micro-type silently reverts at F.W1. **IDENTITY COLLISION (this seat's own find)**: the banked **fr-AdminAuditLog AA-15** books `text-admin-label` as the CURE for `text-[0.65rem]` ("the one-line cure, no uplift") — that cure and this break are on a collision course at F.W1; the census gate must reconcile them (either the producer restores the utility via the BH relay, or AA-15's cure re-targets a surviving rung). **→ F.W1** (census gate; cross-booked against AA-15, one identity per anti-rename law).
- **MG-γ — MAJOR (missed find, reader-LC; fully re-proven by this seat).** `index.html:24-31`'s pre-paint dark-mode script mishandles the `"auto"` storage value: `saved === "dark" || (!saved && matchMedia(…))` — the literal string `"auto"` is truthy and not `"dark"`, so **both arms fail** and no class is applied. `"auto"` is the common stored value by two verified paths (@vueuse 14.3.0 dist, my reads): `useColorMode` persists its `initialValue = "auto"` on first visit (`writeDefaults = true`), and `useDark`'s setter writes `"auto"` whenever an explicit toggle matches the OS. Result: a dark-OS user gets a guaranteed light-theme flash for the ENTIRE initial lazy-chunk fetch (the mount is parked behind `router.isReady()`). The producer ships the exact correct generator — `darkModeSyncScript` handles `"auto"` AND sets `colorScheme` (my read of `dist/dark.js`) — and fourier hand-rolled it instead: the same defect class as m-2/L-3, at a second site, and functionally wrong rather than merely untorn-down. Couple the cure with the observer per R-8's rider. **→ F.W3/W4.**

### MINOR

- **m-1 (primary clause) — MINOR.** `SvgFilters.vue:7-9`'s `reducedMotion` is a one-shot `const` in a never-unmounting component — a mid-session OS preference change is never honoured; the repo's own live idiom (`router/index.ts:15-17`, a function per navigation) and `useMediaQuery` are both in-tree. The second-order "clocks keep ticking" clause is KILLED (K-1). Dissolves if L-5 deletes the file. **→ F.W3/W4.**
- **m-2 / L-3 / C-7 — MINOR (R-7 ruling; LC's MAJOR dissent preserved).** The observer is never disconnected (no `onUnmounted` in the 32-line file); each firing costs 5 separate `getComputedStyle` reads; the producer ships `installDarkModeSync` at the installed pin and the dependency edge already exists (`DarkModeToggle.vue:18`). The `colorScheme` clause is not-established today but coupled to MG-γ's cure (R-8 rider). **→ F.W3/W4** (one item with MG-γ).
- **m-3 — MINOR (R-2 narrowed).** `.nav-trigger:hover` dims ink to 70% (inverted direction) and the channel reaches only the half-opacity chevron; the "no perceptible feedback" framing is killed (press squish + probable bg tint from the ghost CVA); the bg-tint tie-break → SS-13/rebuild. **→ F.W3/W4.**
- **m-4 — MINOR.** 24 lines of `<Transition>` name-classes (`.share-pop-*`, `.fade-*`, AppHeader.vue:278-301, re-read verbatim) in a template with zero `<Transition>`, under a tranche-provenance comment that makes dead code read as maintained. Both blocks scoped — no external consumer possible. **→ F.W3/W4.**
- **m-5 — MINOR.** No `prefers-reduced-motion` carve in AppHeader (my grep-equivalent read of both style blocks) against an explicit 8-block + 3-gate repo policy; motion present at :180 and :206-216. **→ F.W3/W4.**
- **m-6 — MINOR.** Token-bypass triple re-read verbatim: `:157` `padding: 0.5rem 0.625rem` (10px off the 4px rhythm), `:175` `border-radius: 0.4375rem` (7px matches no `--radius-*` rung), `:234` `width: 1.5px` hairline without the dock-plate's blur cover. **→ F.W3/W4.**
- **m-7 — MINOR (R-3 narrowed).** Touch-target inversion survives on exactly two off-CVA surfaces: `.dark-mode-toggle` (`--toggle-size` 2.5rem base → 2.75rem ≥640px — backwards; my read :241-257) and `.admin-badge` (fixed 1.75rem). The nav-trigger leg is killed. **→ F.W3/W4.**
- **m-9 / C-9 — MINOR.** `MOON_COLOR` `#c084fc` "matches VIZ_COLORS.legendre" (DarkModeToggle.vue:29-31, re-read) — false at both ends: legendre's default is `#9545b8`; `#c084fc` is `STATIC.rainbow[4]`; post-B-1 legendre is `#888888`. `SUN_COLOR` has no palette anchor at all. **→ F.W1** (rides the palette-cohesion cure).
- **L-6 — MINOR.** Theme-sync POLICY in the root SFC, MECHANISM in `lib/colors.ts`; no teardown surface, untestable without mounting the app; scheduling contract is a docstring. A `useVizColorSync()` returning a stop handle costs no new edge. **→ F.W3/W4** (folds into the m-2/MG-γ cure).
- **L-7 — MINOR.** `VIZ_COLORS.green` is written (`colors.ts:95`) and read by nobody (no `.green` read, no dynamic access, no destructure — both readers' enumerations, spot-consistent with my GalleryCard grep); one of the five forced style reads per boot/flip computes a dead value. **→ F.W1** (dies in the resolver cure).
- **L-8 — MINOR.** `SvgFilters` re-implements the PRM gate the producer owns at `pencil-boil vue.ts:129` (making the local `:24`/`:36` guards unreachable — my read) and re-runs `querySelector` every frame with the mount hook sitting right there. Dissolves with L-5. **→ F.W3/W4.**
- **L-9 — MINOR (enriched).** The hex fast-path (`colors.ts:29`) forwards unvalidated hex into fixed-slice parsers (`#f0b` → `[11, NaN, NaN]` → invalid strokeStyle silently ignored) — and MG-κ's artifact shows this path is **newly-dead, not born-dead**: it was the PRIMARY path at the prior hex-token pin, so a hex-token reversion repair walks straight into it. **→ F.W1** (constraint on the B-1 repair).
- **C-8 — MINOR.** `AppHeader.vue:49` constructs `useWorkspaceStore()` and never references it again (my read: sole hit) — forcing a 471-line store (router dep, deep watcher, scope-dispose) into existence on every route's boot path. **→ F.W3/W4.**
- **MG-δ — MINOR (missed find, reader-DU; re-proven by this seat).** `.nav-trigger { transition: color 0.15s ease }` (:180, (0,2,0), unlayered) clobbers the producer's deliberately-coherent six-leg button transition on both `btn-pill` (surfaces.css:150-157) and `tap-squish` (base.css:262-268) — reducing the set to `color` alone, so the CVA's press `scale` still fires but SNAPS instead of settling on `--spring-smooth`. The producer's own comment at surfaces.css:141-149 names this exact hazard IN CAPITALS as its RED-witness-5 cascade bug; fourier is the third declarant it did not anticipate, on the app's primary nav control. Zero of three axes opened the Button CVA. **→ F.W3/W4.**
- **MG-ε — MINOR (missed find, BOTH readers independently — banked once).** AppHeader's `sticky top-0` + `backdrop-blur-md` glass (:54, re-read) are structurally inert: the header is a flex SIBLING of `<main>` (the sole scroller), its own scrollport is the `overflow-hidden` shell whose scrollTop is pinned at 0 (children sum to exactly `h-dvh`) — `sticky` never engages and nothing ever paints behind the blur; the glass band samples only the shell's own paper-texture, at the cost of a permanent compositing layer + a containing block for fixed descendants (which the z-index half still legitimately mints). Repair is a design choice: move the header inside the scroller (making both real) or retire the treatment. Aesthetic consequence → SS-13. **→ F.W3/W4.**
- **MG-ζ — MINOR (missed find, reader-DU; re-proven by this seat).** The hand-rolled focus rings out-specific the producer's forced-colors rung: `.nav-trigger:focus-visible` (0,3,0) replaces `focus-ring`'s canonical glass ring (base.css:174-178, my read) AND beats `a11y-overrides.css:79-93`'s deliberate `@media (forced-colors: active) { .focus-ring:focus-visible, … .btn-pill:focus-visible { outline: 2px solid Highlight } }` at (0,2,0) — my read of the block and its AX.W36 rationale. Practical impact tempered (forced-colors coerces outline-color regardless): a colour-semantics regression against an explicitly-authored a11y rung, repeated at style.css:136-143 (four classes) and DarkModeToggle. **→ F.W3/W4.**
- **MG-η — MINOR (missed find, reader-DU; re-proven by this seat).** `aria-current` appears nowhere in the tree (my grep → ∅); the nav dropdown marks the active section by decoration only (`:class="{ 'is-active': … }"` → colour/tint/glow at :340-347, re-read); a SR user hears five undifferentiated items. Two-line cure in the same edit as M-1. **→ F.W3/W4.**
- **MG-θ — MINOR (missed find, reader-LC; adopted with spot-verification).** The dead-code family has no gate that could ever catch it: no `noUnusedLocals`/`noUnusedParameters` in web/tsconfig.json, no lint config, no `lint` script (LC's reads; consistent with this seat's fr-BasisCanvas-session tsconfig read). This is the O(1) fix behind GalleryCard's dead import (K-13), the unread `observer` binding, `workspaceStore` (C-8), the 24 dead transition lines (m-4), the dead green write (L-7), and SvgFilters whole (L-5) — three axes reported instances; none named the gate. **→ F.W0** (substrate).
- **MG-ι — MINOR (missed find, reader-LC; SHARPENED by this seat's own third probe).** C-2's "hand the string to value.js" prescription is not a drop-in: `color2(parseCSSColor(x), …)` throws (LC's run, reproduced), and — this seat's addition — `color2(parseCSSColor(x).value, …)` **silently returns wrong channels**, so the obvious unwrap repair ships a wrong-colour bug with no error signal. The sound path is `colorUnit2` (my run, output cross-keyed to the dist artifact); the `light-dark()` half still requires the used-value probe read. **Fourier-side: → F.W1** (remedy constraint, with C-2). **value.js-side (the `color2` dispatch rejecting/mis-reading its own sibling parser's output): NO-WAVE-OWNER in the F-taxonomy — cross-repo input to the value.js V·π/parser-seam program**, flagged for the owner's mail sweep.

### INFO

- **i-2 — INFO.** Five of the route surfaces mint no `<h1>` (`grep "<h1"` → exactly PaperView:353, FourierMorphDemo:5, FourierShapeExtractor:3 — my grep-equivalent, adopted); compounds M-3. Route-owned: **carry to the per-route component challenges**, NO-WAVE-OWNER here.
- **i-3 — INFO.** `.admin-badge` (AppHeader.vue:137-139, re-read verbatim) — a `title` on a non-focusable div; the Shield contributes no name; a privileged mode with no accessible announcement. **→ F.W3/W4** (rides the header item).
- **C-10 — INFO (conclusion survives K-7's evidence kill).** `<Toaster/>` sits outside `<TooltipProvider>` (App.vue:23/:30/:31, my read): any future tooltip inside toast content resolves no provider — provide/inject follows the component tree; the (real) ToastPortal Teleport is irrelevant to injection. Latent edge, correctly INFO. **→ F.W3/W4** (note on the M-7 edit).
- **C-11 — INFO.** The App subtree's sole keyframes edge: AppHeader → DarkModeToggle → `useFourierMorph` → `@mkbabb/keyframes.js` — inside the atomic tri-package transaction; the persistent header cannot be uplifted independently. **→ F.W1** (budgeting fact).
- **C-12 — INFO (guard).** App is API-inert: no network call, no client import; nearest edges are user-gesture-gated; no store issues a boot request (re-confirmed by reader-LC). **R6-8 and R3-7c have NO leaf in this component** — a per-component sweep must not manufacture one. NO-WAVE-OWNER (negative finding, recorded).
- **C-13 — INFO.** App's published contract is four untyped ambient global side effects (provider provide, documentElement observer, reactive-singleton mutation, four global filter ids) with no vitest floor to assert any of them. **→ F.W9/W10** (test-floor decision input) with MG-θ's F.W0 substrate row.
- **L-10 — INFO (method constraint; denominator corrected).** App.vue is the apex of the derivation blindness cone: zero `v-for`, and `<RouterView/>` is a dynamic-component boundary over the lazy chunks — a source-keyed deriver terminates at depth ~4; seed denominators from the route table, which is **9 entries / 7 lazy / 6 modules** (K-12), not "8". **→ F.W3/W4** (method note for the audit denominator).
- **L-11 — INFO.** Three-root fragment is benign here (no `#app >` selectors, no root attrs); its "portaled therefore safe" justification is corrected by K-7 (portaling is irrelevant to injection; the placement is safe because no tooltip descendants exist in toast content today). Recorded so it is not re-litigated.
- **MG-κ — INFO (missed find, reader-LC; re-verified verbatim + double-keyed by this seat).** The committed stale build `web/dist/assets/index-57FkGzlZ.css` emits `--viz-fourier:#d73523` / `light-dark(#d73523,#eb7366)` / `#eb7366` — dating the regression precisely: prior pin = hex tokens (dark arm worked; the light `light-dark()` arm was ALREADY broken), 4.0.0's oklch move extended the failure to both arms; and `#d73523` equals this seat's `colorUnit2(oklch-token)` output, corroborating both the artifact and the remedy in one stroke. Evidence of the PAST cascade only. **→ F.W1** (evidence cell on the B-1 identity).
- **MG-λ — INFO (missed find, reader-LC; re-verified by this seat's read of useToast.ts).** `addToast`'s `options.duration` is accepted, typed, and silently discarded (body reads `options?.slug` only — my read :21-29) — dead API surface on the app's only error channel, precisely the knob a chunk-failure toast (M-4's cure) would want. The same file's `type ToastVariant` import is an already-censused F.W1 break site (fold — no re-book). **→ F.W3/W4** (rides the M-4/M-7 toast item).

## Killed register — accusations and reader cells that did not survive

- **K-1 · D/m-1 (second-order clause) + C-5 (PRM sub-defect) — KILLED, re-derived by this seat.** "The boil clocks keep ticking under reduce; only the DOM write is skipped." FALSE at the producer: `pencil-boil/src/vue.ts` `start()` gates `if (prefersReducedMotion()) return;` **before** `subscribers.add(sub)`/`ensureScheduler()` (my read of :96-140, incl. the producer's own comment stating the contract). Under PRM the subscriber never enrols, no timer exists, the watches never fire. C-5's own falsifier ("dies if useLineBoil self-gates on PRM") is met; its escape hatch (the redundant local const) is a non-sequitur. The challenge-L axis alone got this right (L-8 cites vue.ts:129 exactly). C's corpus-table claim of "a THIRD ungated clock pair extending lane-frontend §8" dies with it.
- **K-2 · D-M-3's evidence line — KILLED.** "`grep aria-live src/` → empty" is FALSE: 3 hits (AdminFlaggedPanel:151, AdminUserList:277,:445 — my grep). The defect survives (none is a route announcer); the published falsifier does not, and the tree's a11y posture is less barren than the row asserts — the prescribed idiom already exists in it.
- **K-3 · D/m-3 "no perceptible feedback" — KILLED (R-2).** The axis never opened the component it styled: the ghost CVA supplies `hover:bg-foreground/8` + `active:bg-foreground/12`, and the base supplies an unconditional press squish (`active:scale-(--scale-press-btn)`, `tap-squish:active`) — my dist reads. Only the inverted hover-dim leg (reaching the chevron alone) survives, as m-3-narrowed.
- **K-4 · D/m-7's `.nav-trigger` leg — KILLED (R-3).** "2rem (32px) at every viewport" ignores the applied `h-(--control-h-md)` size utility: 40px fine-pointer, 60px coarse (`--ui-scale` 1.5 / `--control-floor` 2.75rem under `pointer: coarse` — my token reads). The row survives only on the two off-CVA surfaces.
- **K-5 · L-2/C-3's universality + reader-LC's confirming cell — KILLED/CORRECTED (R-1).** "The snapshot is GUARANTEED to hold the authored defaults / evaluates strictly before App's onMounted" is entry-route dependent and INVERTS on the clean default path (`/paper` chunk does not import basis-display; the later evaluation snapshots grey). The unconditional red-legend/grey-curve SS-13 recipe will not reproduce on a first visit. Defect real; determinism dead.
- **K-6 · C-1's cascade characterisation (and D's "even more hostile light-dark" framing for dark mode) — KILLED (R-5).** `dark-arm.css` imports AFTER `light-dark.css` (tokens.css:32→:33, my read): the dark-mode computed value is a bare `oklch(…)`. Blocker untouched; the characterisation and derived probe expectations corrected.
- **K-7 · C-10's evidence + D/S-2's rationale + L-11's justification — KILLED (conclusions survive).** The installed Toaster's viewport IS portaled (reka `ToastPortal` = Teleport — both readers' dist reads, adopted); C's "not a Teleport" grep chased the wrong literal. D/S-2's "AppHeader is exactly such an ancestor" is a non-sequitur — AppHeader is a sibling of `<main>`, on no plausible ancestor path (my App.vue read). L-11's "portaled, therefore the placement is safe for injection" is backwards. C-10's conclusion stands on the component-tree argument alone.
- **K-8 · D/B-2's BLOCKER grade — KILLED as severity (R-4).** Fact confirmed at both ends by this seat; the failure is the silent loss of one background pair at a future uplift. MAJOR, with the census carry as the P0 payload. (LC's contrary grade preserved as dissent.)
- **K-9 · C/S-2 ("paper-texture consumed, the correct posture") — KILLED as a superlative.** The falsifier was under-run: checked only the installed pin, never the producer, where the class has no rule at all (this seat's roster grep → ∅). The same line is D's B-2 from the other end; only D ran the check. C/S-2 carries the F.W1 debit.
- **K-10 · D/B-1's scope framing — KILLED (adopted from reader-DU, spot-consistent with the tree).** "Four of the five semantic colours are neutral grey" overstates: the CSS consumption path (20+ `var(--viz-*)` declarations across the chrome) resolves natively and is healthy; the poisoning is JS/Canvas-path only. The sharper, more diagnosable symptom — chrome↔canvas divergence — is what the SS-13 probe should capture, and none of the three axes stated it.
- **K-11 · C-2's UNPROVEN-NEEDS-LIVE caveat — DISCHARGED by execution.** `parseCSSColor` on both failing forms returns cleanly at the installed 0.13.0 (both readers' runs AND this seat's own). The value.js ledger's R1 crash is a tranche-U-head artifact, not a 0.13.0 one; the "V·π is the real prerequisite" branch is dead. (Replaced by the sharper MG-ι remedy constraint.)
- **K-12 · L-4/L-10's route denominator ("8 routes … :46-146") and D-M-4's "eight lazily-loaded routes" — KILLED.** My read of router/index.ts:38-120: NINE entries, SEVEN lazy `component` registrations, SIX distinct view modules (`/v` and `/w`+alias-`/visualize` share VisualizationView), TWO redirect-only entries. Finding unaffected; every downstream wave-budget denominator corrected.
- **K-13 · C-4's twelve-consumer enumeration — KILLED by one.** `GalleryCard.vue:9` imports `VIZ_COLORS` and reads nothing from it (my grep: 1 hit = the import; its colours come from `basisDisplay`). Eleven true consumers; the dead import itself banks under MG-θ's gate row.
- **K-14 · Provenance-drift bundle — KILLED as citable cells (substance unaffected).** D: `colors.ts:68-72` → actual `:90-96`/`:91-95` (the cited lines are hslToHex/rgbToHex); `App.vue:29` → `:31` (my read); SvgFilters "168 LOC" → **178** (my `wc -l`); index.html "39" → 38. L: colors.ts branch cites off by one (`:33-38`/`:41-44` for `:32-37`/`:40-43`). All three axes' amber hexes (`#a3702f` D/L · `#9d6415` C) → **`#9d6515`** light / **`#e8b96d`** dark (my arithmetic). Reader-DU's `text-admin-label` "8 sites" → **7**. Reader-LC's `DarkModeToggle.vue:19` → `:18`. Also adopted: reader-LC's kill of L-2's legendre-swatch attribution (HarmonicLevelGrid binds chebyshev at both sites; legendre has NO live-side sink anywhere — its frozen label has nothing to contradict; split-brain applies to fourier and chebyshev only).

## Superlatives — with provenance (preserve through every cure)

- **S-1 (D/S-1; both readers verified exhaustively; adopted).** Exactly one `<main>` across 66 SFCs and every route surface — App.vue:26 is the single hit, no `role="main"` anywhere. The commonest SPA landmark defect is absent by construction: the shell claimed the landmark and every independently-authored view accepted the constraint.
- **S-2 (D/S-4 = L/S-3 = C/S-3; producer defaults re-verified at 7.0.0 by reader-LC).** `TooltipProvider` is the one producer default in the file that was interrogated rather than inherited: 400/200 against 700/300, both downward, correct for a measured 35-callsite instrument surface; one provider, one skip-delay group, at the true root. The named debit rides with it: `<Toaster/>` one line below inherits its default whole (M-7).
- **S-3 (D/S-3; verified).** Zero third-party origins honoured to the last subresource — same-origin CM preloads, self-hosted fonts.css, bundler-imported KaTeX CSS, the avatar served from `${baseUrl}assets/` with width/height/`decoding="async"` and a 2× source; the only external URLs are two `noopener noreferrer` navigations.
- **S-4 (D/S-5; verified at both pins).** Brand utilities consumed, not forked, on the most fork-prone surface: `cm-serif`/`fourier-f`/`fira-code` are producer `@utility` recipes surviving to 7.0.0; the single local hit is a descendant selector styling the utility in context. (Contrast the same stylesheet's two forks: `cartoon-card`, `--viz-amber`.)
- **S-5 (L/S-2 = C/S-1; producer re-verified).** App.vue's own glass-ui import surface is uplift-clean across the 4.0.0→7.0.0 break — `./tooltip`/`TooltipProvider` and `./toast`/`Toaster` both survive; 0-for-2 broken in a consumer surface with 11 broken sites — while sitting exactly one hop from AppHeader's `./hover-card` + lucide breaks. Honest at the edge.
- **S-6 (L/S-1, with the debit ledger extended per reader-LC).** Goldilocks root: 32 lines, no misplaced concern found by any of five examiners. A root is only correctly small if nothing that belongs there is missing; **two** things are (the error posture, M-4; the focus/announce seat, M-3) — both hostable only by the component that owns `<main>`. Recorded as a superlative with two named debits.
- **S-7 (both readers; re-read by this seat at style.css:52-58's vicinity).** The stylesheet documents a DELETED defect in full — why the `local()`-only `@font-face` overrides were a phantom that silently degraded the regular weight — a post-mortem written into the cascade so the next reader does not re-introduce the bug. The rarer discipline.
- **S-8 (producer + in-tree credit, load-bearing for K-1; this seat's own read).** `router/index.ts:15-17`'s `prefersReducedMotion()` is a live function re-evaluated per navigation, gating `startViewTransition` alongside capability detection — the correct idiom, in-tree, one import away from every gap this corpus fingers; and `pencil-boil`'s `start()`-seated PRM gate (vue.ts:129, with teardown and reactive re-enrolment) is what makes two of the corpus's PRM accusations false. Reading the producer, not the consumer's comment about the producer, is the transferable lesson — the challenge-L axis was the only axis document that did.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **B-1's painted divergence (the sharpened probe).** On `/visualize`: `getComputedStyle(document.documentElement).getPropertyValue('--viz-fourier')` → expect **`oklch(` in dark mode, `light-dark(` in light mode** (the R-5-corrected cascade); `VIZ_COLORS.fourier` → `"#888888"`; and in the same frame confirm the CHROME is correctly coloured (the amber nav icon; `var(--viz-fourier)` borders in FunctionInput). **The divergence, not the greyness, is the diagnostic.**
2. **The split-brain frame — entry-route-conditioned (R-1).** Deep-link or saved-tab entry to `/visualize` only: expect the fourier/chebyshev legend labels in authored hues against grey curves. On a clean `/paper`-first visit the recipe MUST NOT be expected to reproduce (frozen and live sides agree grey). Legendre never split-brains (no live sink).
3. **MG-γ's FOUC.** OS dark ON + `localStorage.setItem('vueuse-color-scheme','auto')` + hard-reload with network throttling: expect light paint for the full initial chunk fetch. The predicate failure is source-decided; only the visible duration is live.
4. **M-6's phantom scroll** — notched iOS device under `viewport-fit=cover`; box arithmetic source-decided, magnitude device-dependent.
5. **D/F-1's light-arm dropdown contrast** — 4.52:1 passes by ~0.4% against an opaque `--popover`, but the panel is glass; probe with the nav menu open over the dark `/visualize` canvas.
6. **M-5's touch half** — whether the attribution hover-card is reachable at all on iOS Safari / Chrome Android against the trigger's competing `@click.stop` navigation.
7. **m-3's residual tie** — rebuild at the current pin, then one hover readback: does `hover:bg-foreground/8` win its (0,2,0) source-order tie against the scoped `background: none`? (DU's byte measurement predates the 4.0.0 bump.)
8. **MG-ε's aesthetic consequence** — one at-rest screenshot: does the inert 60%-opaque blur band read as a register or a washed-out strip? Decides restore-vs-retire.
9. **B-2's perceptual weight** — one before/after of the shell with `.paper-texture` removed; decides whether the F.W1 cure is a texture restore or a class deletion.

## Routing summary (M-25 agglomeration input)

- **→ F.W1 (tri-package uplift / palette seam)**: B-1 fold (+ MG-κ evidence, K-10 framing, corrected amber arithmetic) · C-2 (+ R-6/MG-ι remedy constraints: `colorUnit2` yes, `color2` never, probe-element for `light-dark()`; sequencing correction — the colors.ts leg is landable at 0.13.0 pre-uplift) · C-4 (resolver moves to main.ts) · m-8 fold (basisDisplay, + the R-1 rider) · m-9 · L-7 · L-9 · B-2 **+ the P0 CSS-class census gate** · MG-β (census instance 3, **AA-15 collision — reconcile via the glass-ui BH relay**) · C-11 (budget fact).
- **→ F.W3/W4 (shell a11y/UX + shadow retirement + dead code)**: M-1 · M-2 · M-3 · M-4 (+ MG-λ) · M-5 · M-6 · M-7 (+ C-10 note) · MG-α (scroll restoration) · MG-γ + m-2/L-3 + L-6 (one dark-sync item, R-8 coupling) · L-5 + L-8 + m-1 (SvgFilters scrub; census §3a 12→8 correction) · m-3 · m-4 · m-5 · m-6 · m-7 · C-8 · MG-δ · MG-ε · MG-ζ · MG-η · i-3 · L-10 (method note, corrected denominator).
- **→ F.W0 (substrate)**: MG-θ (unused-code gates: `noUnusedLocals`/`noUnusedParameters` + a lint floor).
- **→ F.W9/W10**: C-13 (the four ambient contracts as vitest-floor input).
- **NO-WAVE-OWNER**: i-2 (route-owned; carries to per-route challenges) · C-12 (negative guard: App is API-inert — R6-8/R3-7c must not grow a false leaf here) · MG-ι's value.js-side `color2` dispatch hazard (cross-repo: input to the value.js V·π/parser-seam program, owner-mail flag).
- **Nothing routes → F.W5–W8**: App's coupling budget is spent entirely on the CSS-token↔parser seam and the mount seam; the CRUD/provenance union has no edge in this component (C-12).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance events (evidence about the fleet).** Reader-LC's confirmed L-2 cell asserts verbatim the "evaluates strictly before App's onMounted" universality that its sibling had killed — inherited from C-3's prose, not re-derived (the importer census that decides it takes one grep, which this seat ran; reader-LC did not). Reader-LC's merged-blocker cell likewise reproduces C's wrong amber hex `#9d6415` unre-derived. Both corrected here (R-1, K-14). Reader-DU committed the inverse sin at smaller scale: its `text-admin-label` count (8) inherited its own first enumeration rather than a recount (actual 7). Verdict classes all survive; the cells did not — the ColorSpaceSelector DISSENT §1 pattern, reproduced twice more.
2. **Severity dissents preserved**: reader-LC holds B-2 at BLOCKER (the shell's whole ground register) and m-2/L-3 at MAJOR (three legs, structural leak); reader-DU holds C-2 at BLOCKER (the cure being installed-and-unimported is the consumption-axis definition of one). Ruled MAJOR/MINOR/MAJOR respectively (R-4, R-7, R-6); if the F.W1 census gate is NOT adopted, B-2's regrade should be revisited — the demotion is priced on the gate existing.
3. **The m-3 residual**: whether the ghost hover-tint actually paints rides a source-order tie this seat declined to decide from a stale bundle (item 7, SS-13). If it loses, m-3 regains its "reaches almost nothing" clause (though never "no perceptible feedback" — the press squish is unconditional).
4. **Method distribution (both readers, concurred by this seat)**: the corpus's error mass concentrated in secondary enumerations, comparanda, and evidence-line greps — not primary mechanisms. Every one of the four axis-level blockers survived; nearly every killed cell was a proof-hygiene failure around a true claim. The one systematic blind spot all three axes shared: **not opening the producer component under the consumer's scoped CSS** (Button CVA — K-3, K-4, MG-δ all fall out of one dist file nobody read).
5. **This seat's own limits**: S-1/S-3/S-4's exhaustive greps were adopted from two independent readers rather than re-run (convergent, low-risk); K-14's legendre-swatch kill was adopted from reader-LC without opening HarmonicLevelGrid (both cited lines named by the killer, consistent with the importer census). Every load-bearing or disputed cell was re-derived; these adopted cells are labeled.

## Closing verdict

**ADJUDICATED — DEFECTIVE, with the blocker folded to the banked palette identity rather than re-booked.** App.vue is a 32-line chassis whose structural metrics are the best in the fourier tree — placement, provider topology, import hygiene, origin discipline, all superlative-grade — wrapped around one imperative act that greys out the instrument the application is named for (B-1, fr-BasisCanvas identity, → F.W1 with the corrected remedy facts: R1 discharged at 0.13.0, `colorUnit2` sound, `color2` a two-mode trap, `light-dark()` needs a used-value probe), one standing act that re-commits it on every theme flip, and an omission surface (error posture · focus/announce · skip target · scroll restoration · FOUC handling) that only this file can host. 45 rows adjudicated-confirmed (1 BLOCKER · 15 MAJOR · 19 MINOR · 10 INFO), 14 kills, 11 missed finds banked — two of them (MG-β's `text-admin-label` census instance colliding with the banked AA-15 cure, and MG-γ's `"auto"` FOUC) discovered or fully proven only at this adjudication. The mass routes to F.W1 (the palette/uplift seam + the P0 CSS-class census gate) and F.W3/W4 (the shell's a11y/UX debt); one substrate row to F.W0; nothing to F.W5–W8 — App is API-inert, and that negative is itself a guarded finding. The file's five best lines must survive its forty worst.

— end of apotheosis. The three challenge axes and both reader returns remain immutable beside this file per E-1/E-3; this record is new, dated evidence rewritten nowhere.
