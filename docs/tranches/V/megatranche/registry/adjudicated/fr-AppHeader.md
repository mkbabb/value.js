claude-fable-5 (served model id)

# fr-AppHeader — adjudicated apotheosis (M-23/L-18 fresh-adjudicator seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject**: `fourier-analysis/web/src/components/layout/AppHeader.vue` (354 lines) + its subtree (`DarkModeToggle.vue`, `useFourierMorph.ts`, `lib/svg-fourier.ts`, `lib/easings.ts`) · fourier `m/w1-bump-migration @ cd26c653` + the frozen M.W1a dirty WT (re-confirmed by this seat: 27 modified rows; **zero of them touch this component's closure** — the dirt is gallery-admin/equation SFCs + `web/package.json`/lock, whose diff IS the M.W1a pin bump 3.1.0→4.0.0 / 2.2.0→4.3.0 / 0.10→0.13; installed node_modules matches the dirty manifest) · installed glass-ui 4.0.0 / keyframes.js 4.3.0 / value.js 0.13.0 / reka-ui 2.x; producer glass-ui at 7.0.0.
**Corpus adjudicated**: three axis challenges (`audit/fourier-components/AppHeader/challenge-{D-design,L-library,C-consumption}.md`, all read whole by this seat — 537/391/422 lines, all three `claude-opus-5[1m]`-authored) × two hostile re-reader returns (design-and-usage · library-and-code, received in-brief).
**Method (M-23)**: every verdict-level disagreement between the two re-readers was re-derived against the tree with this seat's own instruments before ruling. One load-bearing kill was **overturned** (the L-6 engine-quote "fabrication" — see the disagreement table: both quotes exist, in two different classes, and the challenge quoted the right one). One re-reader "missed find" was **demoted to an identity fold** (the value.js peer-range violation is already banked as fr-AnimationControls C-1). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings below are agglomeration input (M-25), never wave stamps.
**This seat's own instruments (all static + node, read-only, zero browser — probe parsimony)**: full reads of AppHeader.vue (354), DarkModeToggle.vue (109), useFourierMorph.ts (230); ranged reads svg-fourier.ts (interface, `pointsToSvgPath`, `interpolateAtHarmonicLevel` incl. the clamp at the levels-bounds), router/index.ts (PRM helper, `/v/` record, `/s/` redirect at :116-119, `/demo/shape-extractor` :111-115, beforeResolve flush, tab-persist :177-188), easings.ts (module-scope `timingFunctions` indexing), App.vue:1-30 (MutationObserver :13-17, shell :24-28), style.css (`@theme --font-sans` :14, `@layer base` font-serif :17-22, root-font steps :40-50, `--viz-amber` carry :113-127, the rotted line-pin :129-131), index.html:8-35 (three CM preloads **without `crossorigin`**), main.ts import order, colors.ts (four regex arms — `#`/hsl/bare-triplet/rgb, **zero `oklch`**; `rainbow[4]="#c084fc"` :15; `legendre:"#9545b8"` **:80**), gallery.ts/auth.ts/workspace.ts (adminMode writes :109/:119 only; `isAdminAuthenticated` exactly 2 hits, zero consumers; workspace 471 lines, `useRouter()` :34, deep watch :108), tsconfig.json (**13** compilerOptions keys; no `noUnusedLocals`). Producer reads at the installed pin: **keyframes engine `engine-BKm1GcJT.js` — BOTH `stop()` bodies extracted with line numbers (`AnimationGroup` :327-329 vs `Animation` :937-939) + the export map (`$ as Animation`)**; glass-ui `button-BNDWhAZb.js` (CVA base incl. `text-[length:var(--control-text)]` + `active:scale-(--scale-press-btn)`, ghost arm verbatim, `size.default: "h-(--control-h-md) px-4 py-2 has-[>svg]:px-3"`, `defaultVariants {variant:"default", size:"default"}`, `data-size` bound to the raw prop); `dist/styles/components.css` (**zero `@layer` statements**; `height:var(--control-h-md)` emitted; TWO `.hover\:bg-foreground\/8:hover` rules); tokens `offsets-sizing.css` :136/:142/:148/:151/:170, `light-dark.css` coarse block, `scheme-motion.css` :337-341 z-ladder + :43-44 font stacks, `bridges.css` :67-68/:80/:238/:241, `color-radius.css` :40/:57-58/:72-74/:102/:245-246/:258/:263-267/:270, `dark-arm.css` :111/:141, `radius.css` ladder, `menu.css` whole trap prose + `.glass-menu-row` transition/lift + the 44px `min-block-size` clamp, `a11y-overrides.css` PRM blanket :6-16 + `[data-allow-motion]` carve + `[data-size="icon"]` floor :116 + `@utility touch-hit-area` :151, `styles/index.css` (**no fonts.css import**; components.css at :201; the `@source "../*.js"` backstop); `menuItemVariants`/`HoverCardContent`/`dropdown-menu` base classes; hover-card d.ts (bare `HoverCardRootProps` pass-through); glass-ui package.json (version 4.0.0, **peers `@mkbabb/value.js ^0.10.0 || ^0.11.0`**, exports `./hover-card`/`./popover`/`./controls`/`./styles/fonts` all present, `./controls` d.ts exports `DarkModeToggle`); reka `HoverCardTrigger.js` (exactly 4 handlers) + `utils.js` `excludeTouch`. Measurements run by this seat: per-key JSON census via node (sun: original 18,859 / decomposition 12,896 / partial_sums 188,150 / eval_points 5,633 / levels 26 — **dead fields 37,388 + 37,822 = 75,210 B**; levels 1+2+3 = 112,388 B exactly); entry-chunk scan (491,312 B / 491,280 chars; 23,336 long-float hits / **405,139 chars = 82.5%**; both payload markers; `eval_points` ×2; static imports of vendor-math AND vendor-paper; all five vendor chunks modulepreloaded; engine chunk NOT); CM woffs 123,340+120,936+158,668 = **402,944 B**; five contrast recomputes (OKLCH→OKLab→LMS³→sRGB + WCAG): tier-featured glyph **rgb(255,191,0)**, vs 10% plate **1.509:1**, vs page 1.587:1; divider **1.455:1**; menu label **4.502:1**; local amber darken **4.709:1**; upstream `--section-color-5` 3.552:1 — agreeing with both re-readers to 3 decimals on every disputed ratio; repo-scope greps: `nav-trigger-label` = 2 hits (this file), `cm-serif` = **26 hits / 17 files**, `--font-serif-math` = exactly 1 occurrence tree-wide (the consumption), `font-sans` application sites = 0, `isAdminAuthenticated` consumers = 0, `getIdlePoints`/`currentPoints` external consumers = 0, `--ease-apple-spring` definitions = 0 (4 consumer files), `--ui-scale|--touch-target|--control-h-|--control-floor` overrides in fourier = 0, `"/v/` navigations = comments + router record only, skip link = none, `--z-overlay` uses = exactly 4, fourier `tier-featured` overrides = 0, no nested value.js under glass-ui (flat 0.13.0 serves the producer's runtime imports).
**Environment disclosure**: `web/dist/` is gitignored and stale (2026-06-12, built against the pre-bump pins — this seat probed its CSS: no `control-h-md`, no `glass-menu-row`). Every dist-derived claim in this record is therefore **corroboration of the source-forced outcome**, never primary; and the one cascade tie that only a fresh build can witness is left in SS-13 rather than settled from stale bytes.

## Scoreboard

**58 corpus rows ADJUDICATED-CONFIRMED (1 BLOCKER · 15 MAJOR · 22 MINOR · 18 INFO · 2 identity folds) · 16 killed-register entries (1 of them a kill-of-a-kill: a re-reader kill overturned by this seat) · 11 missed rows booked · 3 of the corpus's 6 BLOCKERs survive demotion arithmetic as booked severities, 1 as a BLOCKER.**

The sole surviving BLOCKER is **L-1/C-1** (450,631 B of Fourier-coefficient JSON on the eager boot path of every route — the value.js-Q14 shape). Two blocker-weight clusters are named so severity arithmetic does not hide them: **font-truth** (MISS-1's no-webface-loads-at-all root cause + D-B2's three orphaned bindings + 402,944 B of structurally-unusable preloads — the product whose visual thesis is the typeset paper ships zero real webfaces) and **admin-truth** (D-B3's 1.51:1 glyph × C-6's never-rehydrated predicate — the privilege indicator is faint when true and absent when it should be true).

## Ruled disagreements (the adjudication's actual work)

| seat | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L-6 engine quote** | KILLED the challenge's `stop()` quote as "fabricated in its details" — offering `stop() { return this.playback.stop(), this.reset(), this._resolvePlay(), this; }` as the real body | CONFIRMED with the challenge's verbatim `stop(){this._cancelWAAPI(),this.playback.stop(),this.settle(),this._resolvePlay()}` | **KILL OVERTURNED — the challenge and reader-2 are right; reader-1 quoted the wrong class** | This seat extracted both bodies with line numbers from `engine-BKm1GcJT.js`: line 94 opens `z = class` → exported as **AnimationGroup**, whose `stop()` at :327-329 is reader-1's quote; line 599 opens `$ = class` → exported as **`Animation`** (`export { $ as Animation … }`), whose `stop()` at :937-939 matches the challenge byte-for-byte. `useFourierMorph` consumes `engine.Animation`. Reader-1's kill is itself the citation error it alleged — it grabbed the file's FIRST `stop()` match. The load-bearing mechanism (`stop()` calls `_resolvePlay()`, resolving the awaited play promise) is true in BOTH classes, which is why L-6's verdict never wavered; but the "fabricated quotation" accusation must not be copied forward, and the killed-register entry reader-1 filed is stricken. |
| **C-1 module-eval timing** | confirmed cell: "`prepareFourierShape` runs at module-eval scope (DarkModeToggle.vue:26-27), not inside `setup()`" | KILLED: the statements sit inside `<script setup>` (opens :16) — the SFC compiler puts them in `setup()`, per instance | **reader-2 wins; reader-1's cell is an inherited error** | This seat's whole-file read: :16 `<script setup lang="ts">`, :26-27 top-level statements inside it. Vue compiles the entire `<script setup>` body into the component's `setup()`. Only the JSON `import`s (:23-24) are module-eval. The conversion runs once per DarkModeToggle mount — once per session from the never-unmounting header, once more per `/morph` visit — NOT "on every route" and not before the module graph settles. The parse+convert **cost** claim survives in weakened form (synchronous, in the shell's first mount, on the critical path); the module-eval **mechanism** dies. Reader-1 reproduced the challenge's own error verbatim inside an otherwise-verified row — a citation-inheritance event on the confirming side, recorded in DISSENT. |
| **D-B3 severity** | BLOCKER→MAJOR (28px non-interactive glyph; SC 1.4.11 applicability arguable) | BLOCKER sustained ("a privilege indicator invisible in the default theme manufactures false confidence") | **MAJOR** | Arithmetic certain (this seat's own conversion: glyph rgb(255,191,0), 1.509:1 vs plate, 1.587:1 vs page — all three seats agree within 1%; dark arm passes; fourier overrides `--tier-featured` nowhere). But the harm bounds it: light-arm-only, a 28px status chip with a `title`, not a gate (admin ops authorize via `getAdminToken()` regardless — C-6), and the sibling `--viz-amber` precedent proves the one-token cure. A signal faint-by-paint (here) and wrong-by-state (C-6) is two MAJORs, not a BLOCKER. Reader-2's position preserved below. |
| **D-B2 / the font cluster** | BLOCKER sustained on D-B2 with leg 2 (unicode-range excludes U+2131) intact in its own §2 | leg 2 KILLED — PJS is never range-gated because it is **never loaded**: `styles/fonts.css` is a separate export subpath no fourier file imports; MISS-1 books the strictly-stronger root cause | **reader-2's root cause wins; leg-2 kill RATIFIED; the merged cluster carries blocker weight but books MAJOR per row** | This seat: `dist/styles/index.css` @import list carries NO fonts.css (its own header documents the split at :8/:24-25); `grep "glass-ui/styles"` in fourier → exactly `style.css:3` (the cascade), never `/styles/fonts`; `--font-serif-math` = 1 occurrence tree-wide (the consumption at utilities.css:66); `font-sans` application sites = 0 (the `@theme` binding at style.css:14 is read only by preflight, and `@layer base html,body { @apply font-serif }` at :17-22 — same layer, later — wins); 26 `.cm-serif` sites / 17 files all fall to OS generic serif; the three CM preloads total 402,944 B. Every glyph the brand register paints is a system face while 403 KB of Computer Modern is fetched and discarded. The unicode-range enumeration was real work refuting a mechanism that never engages — the exact "cure adjudicated separately from defect" lesson. |
| **L-4/C-4/D-m5 severity** (dead `workspaceStore`) | MINOR (Pinia singleton — one-time boot cost) | MAJOR ("eagerly instantiates a 471-line store … on every route") | **MINOR** | The store is a Pinia singleton: `useWorkspaceStore()` runs its 471-line setup ONCE per app boot, not per route; the genuine consumers instantiate it anyway on workspace routes; the residual harm on non-workspace boots is a deep watcher over untouched state + `workspace.ts`/`draftStorage` joining the eager graph. Dead binding, invisible to gates (13 compilerOptions keys, no `noUnusedLocals` — this seat's count; C-4's "15" is killed below). One-line deletion. Reader-2's MAJOR preserved. |
| **D-M2/L-10 severity** (z-overlay vs z-header) | MAJOR | MINOR | **MINOR** | The tie is real (this seat: exactly 4 `--z-overlay` uses; the three siblings all render after the header, winning by DOM order; `--z-header: 35` at scheme-motion.css:338, bridged at bridges.css:238) — but no visible break exists today, teleported overlays escape at 120+, and D-M1 shows the header's z-claim is doing no flow work anyway. A convention defect at 2 sites (VisualizationView repeats it), one-token cure. |
| **D-M5/L-15/C-11 severity** (Enter-not-Space) | MAJOR | MINOR | **MINOR** | Enter works, click works, the destination is duplicated by the nav dropdown, and there is no scroller for Space to scroll (D-M1). A real ARIA-button contract breach with a trivial structural cure (`<RouterLink>`), booked in the same repair family as D-M4 (which stays MAJOR — the empty label/name intersection on the masthead control is a WCAG 2.5.3 failure both readers sustain). |
| **MISS-PRM severity** (reader-1's M-4: the JS morph ignores `prefers-reduced-motion`) | MAJOR | (not booked; its own §4 noted the C axis declined it) | **MINOR — this seat demotes** | Verified whole-chain by this seat: no PRM gate in DarkModeToggle/useFourierMorph/FourierMorphSvg; the local PRM block (:104-108) covers only the CSS hover transform; `router/index.ts` :15-17 + the beforeResolve gate prove the in-tree pattern; and glass-ui's `[data-allow-motion]` carve comment names its OWN toggle's snap-under-PRM contract — the fork skipped it. But the surface is a 350 ms user-initiated decorative morph, not continuous motion. The row's real weight is that it **falsifies the corpus's S-5/PRM superlative as stated** ("the component gets the nuanced PRM behaviour for free" — only its CSS does), which is recorded on the superlative itself. |
| **C-13 decidability** | KILLED the "UNPROVEN-NEEDS-LIVE" framing: fully static — scoped `.nav-trigger[data-v]` (0-2-0) beats unlayered utilities (0-1-0) for every declared property; `cn()` never arbitrates a non-Tailwind class | agreed in substance ("the one case that mattered is settled statically") | **PARTIALLY RATIFIED — the base state is static, the hover plate is not** | Right: tailwind-merge is out of play, and every `.nav-trigger` base declaration beats the CVA's 0-1-0 utilities; `height`/`font-size`/press-scale survive to the CVA (undeclared locally). Wrong by one cell: `.hover\:bg-foreground\/8:hover` is 0-2-0 — EQUAL to the scoped base `background: none` — both unlayered, so the hover-state background falls to emitted-stylesheet order, which is build-graph-dependent (main.ts imports App.vue at :3 before style.css at :6) and **cannot be witnessed from the stale dist** (this seat probed it: the June-12 CSS predates the 4.0.0 layout entirely — no `hover\:bg-foreground\/8`, no `control-h-md`). D-M9's secondary leg stays in SS-13. |
| **reader-2's missed row #3** (value.js 0.13.0 outside glass-ui 4.0.0's peer range `^0.10.0 \|\| ^0.11.0`) | (not raised) | booked as a NEW missed MINOR ("a live contract violation today … the corpus treats as a future F.W1 transaction") | **DEMOTED TO IDENTITY FOLD — this seat's identity-guard catch** | The fact is true (this seat re-verified: peers `^0.10.0 \|\| ^0.11.0`, flat 0.13.0, no nested copy, glass-ui dist imports value.js at runtime) — and it is **already banked** as **fr-AnimationControls C-1** (the `npm ls` ELSPROBLEMS row, census §1 pins identity, "never re-book per-component", G33 gate). One identity, folded. The genuinely new sharpening rides as a rider on that identity: glass-ui 4.0.0 ALSO peers `@lucide/vue ^1.16.0` and `@lucide/vue@1.20.0` is installed **alongside** the app's `lucide-vue-next@1.0.0` — two icon packages in one tree, which reframes the F.W1 lucide rename as reconciling an already-installed peer, not adopting a new one. |
| **D-M6 kill** (nav trigger misses the 44px touch floor) | KILLED (with the CVA default-size mechanism) | KILLED (same mechanism, independently derived) | **KILL RATIFIED — unanimous + this seat's own token walk** | `defaultVariants.size:"default"` → `h-(--control-h-md)` applies with no `size` prop (only the `data-size` ATTRIBUTE is absent); `.nav-trigger` declares no height; `--control-h-md = max(calc(2.5rem × var(--ui-scale)), var(--control-floor))` (offsets-sizing.css:151) with the coarse block lifting scale to 1.5 and floor to 2.75rem (light-dark.css); fourier overrides none of the five tokens (this seat's grep: 0). Coarse-pointer height = 3.75rem = 60px at 16px root / 67.5px at 18px — 136-153% of the floor. The floor is CLEARED by a mechanism the axis did not look for; what that mechanism costs instead is MISS-2 (the row's exact inverse). |

## Adjudicated defect roster

Line anchors are fourier WT at `cd26c653`+M.W1a-dirty (component closure commit-clean); re-resolve before any cure. Dispositions: **F.W0** substrate · **F.W1** tri-package uplift · **F.W3** shadow retirement · **F.W4** frontend audit · **F.W5-W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** for SS-3/SS-4 spec authoring.

### BLOCKER

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **FR-AH-1 · L-1/C-1** | AppHeader statically drags **450,631 B** of Fourier-coefficient JSON into the eager entry chunk (App.vue:25 → AppHeader.vue:6 → DarkModeToggle.vue:23-24, every edge a bare static import) to draw a 40px icon; the entry chunk is 82.5% long-float literals (this seat's scan: 23,336 hits / 405,139 chars of 491,280) with both payload markers byte-present; **75,210 B** (16.7%) is fields no reachable code reads (`original`+`decomposition`+`eval_points`; `prepareFourierShape` reads `levels`+`partial_sums` only, and retains the WHOLE parsed graph via `FourierShape.data`). Adjudicated corrections carried: conversion is per-instance-at-mount (not module-eval); the "112,388 B unreachable levels" arm is dead (level 3 is read on every morph via the 3→5 bracket; levels 1-2 are user-reachable on `/morph` via `setLevel`); the idle icon needs ~20 KB. Same shape as value.js's own Q14 escalation — any fourier LCP measurement without this fix is measuring `sun.json`. | DarkModeToggle.vue:23-27; svg-fourier.ts (bracket walk re-derived); this seat's chunk scan + per-key census | **F.W4** (async boundary + payload prune: `defineAsyncComponent` or shallow-import seam + generator-side field/level prune — the census's "characterful, keep" survives at ~3% of the cost) + **F.W3 rider** (glass-ui 4.0.0 ALREADY exports `DarkModeToggle` at `./controls` — the shadow-reconcile seat is live today, not a 7.0.0 wait) + SS-13 (the LCP/TBT trace) |

### MAJOR

| id | claim (adjudicated form) | proof anchor | disposition |
|---|---|---|---|
| **FR-AH-2 · MISS-FONT (r2-M1 ⊕ r1-M-1) / D-B2 legs 1+3** — *blocker-weight cluster, font-truth* | The app never imports glass-ui's font corpus (`./styles/fonts` is a deliberate separate subpath; style.css:3 pulls only `./styles`), so **every** brand-register face falls to `system-ui`/generic-serif: `--font-serif-math` is declared nowhere (26 `.cm-serif` sites / 17 files → OS serif), `--font-display`/`--font-serif` resolve to an unloaded Plus Jakarta stack, and the masthead's ℱ + "ourier analysis" render in two unrelated fallbacks. Meanwhile 402,944 B of vendored Computer Modern is **preloaded on every route** for a family with zero application sites (`--font-sans` bound at style.css:14, consumed by preflight only, beaten by the same-layer later `font-serif` apply at :17-22). The D-B2 unicode-range leg is killed (PJS never loads at all — see the table); the fix is ~2 import/declaration lines plus a decision about CM's actual role. | this seat's grep set + styles/index.css read; index.html:12-14; public/fonts.css:14-40; style.css:13-22 | **F.W4** (wire `@import "@mkbabb/glass-ui/styles/fonts"` OR commit to the CM identity: declare `--font-serif-math`, re-point the masthead, drop or fix the preloads — one decision, stated) + **GLASS-RELAY** (producer ask: `@utility cm-serif` reads `var(--font-serif-math, serif)` against a variable the library never declares — silently no-ops for every consumer) |
| **FR-AH-3 · D-B4/L-2/C-3** | `activeTab` (:34-41) misses `/v/:visualizationSlug` (the canonical saved-visualization share target, router :57-59) and tests a `/s/` prefix that can never settle (redirect-only record :116-119) — the header reports "Paper" (icon :117, aria-label :115, `is-active` row :127) on the share route. The B.W4 rename fingerprint is in-tree (router :53-56); the corroborating twin (`fourier_active_tab` persist, :177-188, same five paths + `/w/`, same `/v/` gap) re-verified by this seat. Blast radius honestly narrow (C-3's own narrowing ratified: zero in-app `/v/` navigations, no share-link generator — deep links/bookmarks only). L-20 folds here: route knowledge is triplicated (tabs[].value · VALID_TABS · the afterEach chain) and the repair should collapse it. | AppHeader.vue:34-41; router/index.ts (this seat's ranged read) | **F.W4** — one shared route→tab source; the C-3 line-pin kill (below) means a repair wave must take anchors from THIS record, not the C challenge |
| **FR-AH-4 · D-B1/L-3 (+D-m4 folded)** | `.nav-trigger-label` is `display:none` at every viewport (:201-204; grep = 2 hits, both this file) — the app's only nav control never shows its section name; the 640px slot where the reveal belonged holds a rule **byte-identical to the base** (:218-222 restates :174's `padding: 0.25rem`), and the sibling `.logo-text` proves the intended reveal pattern (:229-231 → :249-251). A `color` on a permanently-hidden span is the lost-rule fingerprint. Severity MAJOR not BLOCKER: the computed `aria-label` (:115) keeps assistive tech served — the loss is visual (and, per FR-AH-3, the one surviving channel lies on `/v/`). | AppHeader.vue:118, :174, :201-204, :218-222, :229-251 (whole-file read) | **F.W4** — one `display: inline` in the existing media block + delete the no-op rule |
| **FR-AH-5 · D-B3** | Admin badge glyph at **1.509:1** against its own plate in light mode (this seat's independent OKLCH→sRGB recompute: glyph rgb(255,191,0); 3:1 SC 1.4.11 floor; dark arm passes ~11:1) and no accessible name (`title` on a non-interactive div; lucide emits a bare svg). The team ran EXACTLY this analysis for the sibling token (`--viz-amber` darkened to a re-verified 4.709:1, style.css:113-127) and `--tier-featured` got no treatment; fourier overrides it nowhere. Demoted from reader-2's BLOCKER per the table. | AppHeader.vue:137-139, :261-271; color-radius.css:270; this seat's recompute | **F.W4** (local light-arm darken now, `role="img"` + `aria-label`) + **GLASS-RELAY** (light `--tier-featured` rebaseline ask — the `--viz-amber` precedent template) |
| **FR-AH-6 · C-6** | The badge reads `galleryStore.adminMode` — a plain ref written at exactly 2 sites (:109/:119), never rehydrated — while `auth.ts` restores `adminToken` from localStorage (:16) and exposes `isAdminAuthenticated` (:22, exported :129) with **zero consumers** (this seat's grep). After reload the app is still admin-capable (`getAdminToken()` authorizes) and the badge is gone. Two sources of truth for one predicate; the header consumes the volatile one. (Corroborates — and is corroborated by — fr-AdminAuditLog K-5's "adminMode non-persisted" fact, used there to kill a null-token blocker; the DEFECT booked here is the header's state lie, not re-booking that record.) | AppHeader.vue:137; gallery.ts:39/:109/:119; auth.ts:16/:22/:129 | **F.W4** (one predicate, one home) + **F.W5-W8 rider** (the admin-auth seam owns which predicate is canonical) |
| **FR-AH-7 · C-2** | The header alone forces `vendor-math` (value.js + katex, **348,707 B**) eager, refuting the comment at useFourierMorph.ts:33-36 verbatim: App.vue → AppHeader → DarkModeToggle:20 → useFourierMorph:21-27 → easings.ts:9,16 → value.js, with a module-scope value use (easings.ts `Object.entries(EASING_LABELS).map` indexing `timingFunctions` — this seat's read). Sole-cause verified: the other value.js/katex importers are all `/equation`-lazy; vendor-ui carries no value.js (this seat's chunk probe); the entry statically imports vendor-math and dist/index.html modulepreloads it. The engine chunk IS genuinely deferred (not preloaded) — the lazy boundary defers the engine, not value.js. | this seat's chunk + import-chain walk | **F.W4** (break the eager easings edge with FR-AH-1's boundary; correct the falsified comment) + **F.W1 rider** (the fork itself dies at `./easing` — FR-AH-24) |
| **FR-AH-8 · L-5** | One failed dynamic import is memoised forever: `enginePromise` is written before resolution, never cleared on rejection (:38-44, no `.catch`); the caller flips the theme BEFORE the await (DarkModeToggle:69→:71) inside a promise-discarding `@click` with no try/catch; rejection at :149 precedes the first phase write at :169, so `phase` stays `"idle"` and the guard keeps admitting clicks. Rendered result: the old glyph stroked in the NEW theme's idle colour, permanently, plus one unhandled rejection per click. | useFourierMorph.ts:38-44, :145-149, :169; DarkModeToggle.vue:62-72 (whole reads) | **F.W4** — clear the memo in a `.catch`; caller falls back to `setShape(target)` |
| **FR-AH-9 · L-6** | `onUnmounted(() => stopAnim())` is an anti-teardown: `Animation.stop()` (the `$` class, :937-939 — quote REINSTATED by this seat, see the table) calls `_resolvePlay()`, resolving the awaited play promise, so unmount mid-phase ADVANCES `morphTo` into its next phase, constructing and playing a fresh Animation on a destroyed scope with no owner (three awaited phases :170-179/:186-193/:199-208, no cancellation token). Latent from AppHeader (never unmounts); **live** via the shared composable on `/morph` (FourierMorphDemo :108/:124/:172/:177 + user-configurable durations via `syncWith` — this seat's read). The engine's resolve-on-stop is documented and correct; the composable treats stop as abort. | engine-BKm1GcJT.js :937-939 (this seat's extraction); useFourierMorph.ts:115-120, :215 | **F.W4** — cancellation token (epoch counter checked after each await) in the composable; L-7/L-8 die in the same edit |
| **FR-AH-10 · D-M1** | `sticky top-0`, the z-claim, and `backdrop-blur-md` are all inert: the header's scrollport is the `h-dvh … overflow-hidden` shell div (App.vue:24) — exactly viewport height, never scrolls; `<main>` (the scroller) is the header's SIBLING (:26). Nothing ever passes beneath; the per-frame backdrop-filter composites a static paper texture. Four coupled declarations encoding a scroll contract the layout cannot honour, one charging GPU rent. Sole mount site verified. | AppHeader.vue:54; App.vue:24-28 (this seat's read) | **F.W4** — decide: real sticky (make `<main>`'s scroll the header's) or drop the treatment + take `z-header` (with FR-AH-17) |
| **FR-AH-11 · D-M3 (+M-3 mechanism)** | The unscoped `.nav-dropdown-item` block (:317-338) re-introduces, verbatim, the cascade trap glass-ui's menu.css:7-19 documents in prose ("NEVER re-add an unlayered flat-accent utility") — unlayered beats every `@layer`, replacing the glass hover plate, density/indicator/typography rungs wholesale; and its `font-family: var(--font-serif)` (:326) resolves to Plus Jakarta Sans — **a sans** (bridges.css:68 → scheme-motion.css:43), unloaded at that (FR-AH-2). The missed M-3 leg is verified and folds here: the override's `transition: background .12s, color .12s` (:330) truncates the producer's `transition: translate var(--duration-fast) var(--spring-smooth)` while declaring no `translate`, so the `@layer` hover rule's `-1px` lift still fires — **instantly**. Every menu row keeps the lift and loses the spring: reads as jitter, not as a bug. | AppHeader.vue:305-338; menu.css (this seat's whole read incl. :53-59, :65-72) | **F.W4** — one cascade decision for the whole override family (see FR-AH-30's caution: the D-axis's own rank-5 remedy is unsound as first stated) |
| **FR-AH-12 · D-M4** | WCAG 2.5.3 Label in Name: `aria-label="Go to paper"` overrides visible content "ℱourier analysis" — zero shared words; a speech-input user saying what they see cannot activate the masthead control. Degrades below 640px to a single glyph. | AppHeader.vue:59-69, :229-231 | **F.W4** — `<RouterLink to="/paper">` whose text IS the name (also cures FR-AH-18; does NOT alone cure FR-AH-26's hit-box) |
| **FR-AH-13 · D-M7** | The attribution card and both external links are touch-unreachable: reka wraps both pointer handlers in `excludeTouch` (utils.js verbatim, re-read), glass-ui's HoverCard is a bare `HoverCardRootProps` pass-through (d.ts re-read), the links have no other entry point in this component, and the same tap fires `@click.stop="router.push('/paper')"` — touch users get navigation, never attribution. Keyboard access genuinely works (raw `onFocus`), which is why it survives desktop review. | AppHeader.vue:57-106; reka HoverCardTrigger.js (4 handlers, this seat's census) | **F.W1** — the one row the uplift IMPROVES: the 7.0.0 `<Popover trigger="hover">` swap adapts to coarse pointers, but MUST pass `trigger="hover"` and resolve the :64 click collision (the D-axis U-1 design decision, adopted) |
| **FR-AH-14 · D-M8** | No `<nav>` landmark anywhere in :53-144 (in-tree precedent: PaperSidebar.vue:50, re-read); no `aria-current` (active row = colour-only, :340-343); no skip link (`grep skip` → none; App.vue:26 already provides the `<main>` target). | AppHeader.vue:53-144; this seat's greps | **F.W4** — `<nav aria-label>` + `aria-current="page"` + skip link, ~6 lines |
| **FR-AH-15 · D-M9 (primary leg)** | The nav trigger's hover affordance is one 16px chevron moving from effective alpha 0.50 to 0.35: the hover rule changes only `color` (:184-186) while both coloured children pin `--viz-amber` (:197, :203 — the latter `display:none` anyway) and the ghost variant's plate is contested by the unlayered `background: none` (:177). Secondary leg (which rule wins the hover-plate tie) stays SS-13 — see the C-13 ruling. | AppHeader.vue:170-212 | **F.W4** — restore a hover surface (lifting the `background:none` override or an explicit plate) |
| **FR-AH-16 · MISS-2 (r1)** | On every coarse-pointer viewport ≥640px the nav trigger is TALLER than the bar containing it: `.header-inner { height: 3.5rem }` (:161-167) = 63px/56px vs the Button's `--control-h-md` = 3.75rem = 67.5px/60px (this seat's full token walk; zero fourier overrides), inside an `overflow-hidden` ancestor; below 640px the header computes ≈85.5px (~18% of a 480px viewport) and the declared `min-height: 2.75rem` never binds; the sibling toggle sits 16px shorter because `--toggle-size` literals (:242/:256) don't ride `--ui-scale` — the two controls diverge only on touch. The exact inverse of the killed D-M6; the root cause is hard-coded bar/toggle literals against pointer-conditional control heights. | AppHeader.vue:157-167, :241-257; offsets-sizing/light-dark (this seat) | **F.W4** (size the bar and toggle from the same tokens as the control) + **SS-13** (the single highest-value screenshot: 768×1024, `pointer: coarse`) |

### Identity folds (confirmed here, banked elsewhere — never re-booked)

| id | identity | this record's contribution |
|---|---|---|
| **C-19** (`cssVarToHex` has no `oklch` arm → four of five `--viz-*` resolve `#888888`) | **fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER, → F.W1)** — the resolver/token seam is already adjudicated there with the full blast radius | Re-verified independently (zero `oklch` in colors.ts; the four arms incl. the `#` passthrough at :27-28; `--viz-*` oklch-only at :263-267). AppHeader's additions ride as riders on that identity: (a) the header's toggle is the **sole in-app trigger edge** of `resolveVizColors` re-runs; (b) `--viz-amber` survives the JS path only via the style.css:119 hsl() contrast carry — this seat hand-confirmed the regex match; (c) FR-AH-23's false comment points at this broken referent. |
| **value.js 0.13.0 outside glass-ui 4.0.0's peer range** | **fr-AnimationControls C-1 (census §1 pins identity, → F.W1, G33 `npm ls` gate)** | Fourth independent confirmation (peers `^0.10.0 \|\| ^0.11.0`, flat 0.13.0, no nested copy) + the NEW rider: `@lucide/vue@1.20.0` installed beside `lucide-vue-next@1.0.0` — glass-ui's peer icon package is already in the tree; the F.W1 rename is a reconciliation, not an adoption. |

### MINOR

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **FR-AH-17 · D-M2/L-10** | `z-[var(--z-overlay)]` (50) claims the scrim rung when `--z-header` (35) exists and is bridged/mintable; all three tied siblings win by DOM order. Demoted per the table; two-site convention (VisualizationView:146). | **F.W4** (with FR-AH-10) |
| **FR-AH-18 · D-M5/L-15/C-11** | `role="button"` div, Enter-only, no `.prevent`, no Space — in a file importing glass-ui `Button`. Demoted per the table. | **F.W4** (the FR-AH-12 RouterLink) |
| **FR-AH-19 · L-4/C-4/D-m5** | Dead `const workspaceStore = useWorkspaceStore()` (:49, sole occurrence) — one-time-at-boot instantiation of the 471-line store (deep watcher :108, `useRouter()` :34), invisible to gates. Demoted per the table. | **F.W4** — delete the line; the gates gap (no `noUnusedLocals`) is a lane carry, not a component charge |
| **FR-AH-20 · L-7** | `morphTo` restores `"idle"` only on the success path behind a one-way guard — any post-:169 throw bricks the toggle for the session. Reachable throw UNPROVEN (getEasingFn soft-falls; fixed frame spec) → the posture books, the reachability rides SS-13. | **F.W4** (dies in FR-AH-9's edit: `try/finally`) |
| **FR-AH-21 · L-8** | `setShape`/`setLevel` race the in-flight chain (stop resolves the awaited phase; the continuation clobbers the reset; both write `currentPoints`). Latent here, live on `/morph` (:172/:177 — this seat's read). | **F.W4** (same edit) |
| **FR-AH-22 · L-9/C-5** | `./hover-card` present at 4.0.0 (export map re-read), removed at 5.0.0; `^4.0.0` caret holds it — MINOR today, MAJOR the day the range moves. AppHeader is 1 of 2 sites. | **F.W1 fold** — identity = census lane-frontend:473 (`BI.W-OVERLAY-UNION`); the swap design decision is FR-AH-13's |
| **FR-AH-23 · L-12/C-7** | `SUN_COLOR`/`MOON_COLOR` are frozen RGB literals; the `:31` provenance comment ("matches VIZ_COLORS.legendre") is false three ways (declared `#9545b8` at colors.ts **:80** — this seat's pin; runtime = oklch tokens; the literal is actually `rainbow[4]`); the component imports neither `@/lib/colors` nor its `hexToRgb`; the lerp bypasses both first-party colour systems and cannot track a theme flip. | **F.W4** (route through the resolved palette — after fr-BasisCanvas D-1's resolver cure lands, else it lerps `#888888`) |
| **FR-AH-24 · L-14** | The header's boot path transitively imports the forbidden easings fork (census §4 HARD SHADOW), and useFourierMorph:29 barrel-re-exports `EASING_PRESETS`/`EASING_PRESET_NAMES` + two types (**not** `getEasingFn` — reader-2's wording correction ratified against the live file). | **F.W1 fold** (identity = census §4; `./easing` ships at 7.0.0) — the colocation re-export can die in F.W4 independently |
| **FR-AH-25 · L-23/C-10** | `as any` ×2 erases the 8-field `FourierPathData` contract to silence tuple widening (`resolveJsonModule`); structural drift then soft-degrades through the S-degrade path to an invisible toggle with no error. `as unknown as FourierPathData` suffices (structure verified exact by both readers against the data; this seat re-confirmed keys/levels/n_eval). | **F.W4** |
| **FR-AH-26 · MISS-6 (r1)** | The logo's hit box is ~36px and shrinks with the viewport — the one header control no glass-ui floor can reach (bare div; `.logo-trigger` declares only `cursor:pointer`; below 640px the box collapses to the ℱ glyph); `@utility touch-hit-area` (a11y-overrides:151, re-read) exists precisely for this and is unused in fourier. Survives the RouterLink cure. | **F.W4** |
| **FR-AH-27 · D-m1/L-11/C-8/C-9** | 24 lines of dead transition CSS (`.share-pop-*`/`.fade-*`, zero `<Transition>` in the template — whole-file read; the A.W3.d comment names a share button that left) — the matching orphan to FR-AH-19. The `--ease-apple-spring` token half **FOLDS to fr-BasisCanvas M-α6** (F.W1 token parity; 5-file scope incl. AppHeader:280 — that record's dissent noted AppHeader was not read whole; THIS record supplies the whole-file read). | **F.W4** (delete) + fold |
| **FR-AH-28 · D-m2** | Two off-ladder radii (0.4375rem :175, 0.5rem :322 — ladder re-read: 4/6/10/12px rungs) and broken concentricity (12px panel − 8px pad wants 4px inner, not 8). | **F.W4** |
| **FR-AH-29 · D-m3 (breakpoint leg)** | The header steps at 640px, the root font at 768px (style.css:40-50) → a 12.5% height discontinuity (63→56px) at a breakpoint the header never declares; mobile-taller-than-desktop holds on BOTH pointer types (fine: 63 vs 56; coarse: 85.5 vs 60 — MISS-2's numbers). The killed absolute arithmetic is in the register. | **F.W4** (with FR-AH-16) |
| **FR-AH-30 · MISS-5 (r1)** | The D-axis's own rank-5 remedy ("wrap both style blocks in `@layer components`, 2 lines") REVERSES the outcome as first stated: glass-ui's competing rules are already in `@layer components` at higher specificity (`.glass-menu-row:hover:not([data-disabled])` 0-3-0 vs 0-2-0), so the consumer block would silently LOSE every override — a visual regression on the ledger's fastest row. Only the parenthetical (`@layer glass-overrides` appended after `utilities`) works, and it must be stated. Adjudication of a cure, not of the tree. | **NO-WAVE-OWNER** — spec-input for the SS-3/SS-4 authoring that consumes FR-AH-11 (the "cure adjudicated separately from the defect" law, ColorSpaceSelector addendum §2) |
| **FR-AH-31 · MISS-M-4 (r1, demoted)** | The header's signature motion (the 350ms JS morph) is the one thing PRM cannot reach — a11y-overrides' `!important` blanket strips CSS transitions but not a JS-written path attribute; no gate anywhere in the chain (this seat's whole-chain read); the router's own `prefersReducedMotion()` (:15-17) is the in-tree pattern; glass-ui's `[data-allow-motion]` comment documents its OWN toggle's snap-under-PRM contract. Demoted MAJOR→MINOR per the table; falsifies the S-5 superlative's scope as stated. | **F.W4** — gate `morphTo` on PRM → `setShape(to)` |
| **FR-AH-32 · C-12/D-m8** | Four generic unscoped global classes as the portal seam (permanent — the header never unmounts), plus two width overrides that had utilities (`min-width: 17rem` = `w-68` over the `w-64` base; `12rem` over `min-w-32` — bases re-read from dist). Mechanism justified (reka portals), naming and widths not. Collision-free today. | **F.W4** |
| **FR-AH-33 · C-14** | `lucide-vue-next` (7 runtime icons) and `reka-ui` sit in devDependencies while vite.config's production `vendor-ui` chunk names lucide — the manifest misdescribes the graph the build config asserts. No install path breaks (private SPA). | **F.W0** (manifest hygiene; the rename itself is F.W1) |
| **FR-AH-34 · D-m6** | Inert `relative shrink-0`'s `relative` (no positioned descendant, no slot) + `cursor: pointer` declared twice for the same box (:67, :274-276). | **F.W4** (deletion batch) |
| **FR-AH-35 · D-m7** | Hand-rolled 1.5px divider at **1.455:1** (this seat's recompute) where the library ships `Separator` + `twin-line-divider` + the `--border` token. Design finding (decorative exemption noted by all seats). | **F.W4** |
| **FR-AH-36 · MISS-crossorigin (r2-M2)** | The three CM preloads lack `crossorigin` — font fetches from `@font-face` are ALWAYS CORS-mode-anonymous, so a bare font preload occupies a different cache key and can never be matched (the :10-11 comment's same-origin reasoning is wrong on the platform rule). Today doubly moot because nothing requests CM at all (FR-AH-2) — the preload is 402,944 B fetched at highest priority and discarded regardless. | **F.W4** (with FR-AH-2 — the preloads either gain `crossorigin` and a consumer, or die) |
| **FR-AH-37 · MISS-dead-exports (r2)** | `getIdlePoints` and `currentPoints` are exported from useFourierMorph's return surface with ZERO consumers (this seat's grep) — same gate-invisibility mechanism as FR-AH-19. | **F.W4** (deletion batch) |
| **FR-AH-38 · L-13** | Nav chrome coloured from `--viz-amber` — a chart-legibility token under an open upstream coordination ask (style.css:113-127; both arms recomputed by this seat: local 4.709:1, upstream 3.552:1, matching the file's own annotations) — an upstream rebaseline silently re-skins the navigation. | **GLASS-RELAY fold** (identity = the census's standing `--viz-amber` carry; this record adds AppHeader to the consumer list) |

### INFO

| id | claim | disposition |
|---|---|---|
| **FR-AH-39 · D-i1** | Register clash: 🎉 in the academic masthead's attribution card (sibling: UserSlugBar's 🐌). | **F.W4** (judgement call, batched) |
| **FR-AH-40 · D-i2** | Three identities for one person in 40px (`alt="mkbabb"` / `@mbabb` / `github.com/mkbabb`); the avatar should be `alt=""` (name adjacent). | **F.W4** |
| **FR-AH-41 · D-i3** | `font-semibold` (600) synthesises — CM ships 400/700 only (fonts.css re-read); `tracking-tight` fights a serif at display size. | **F.W4** (with FR-AH-2) |
| **FR-AH-42 · D-i4** | Bare `v-if` admin badge beside an unused `.fade-*` pair 157 lines below; header-region loading/error states are toast-only. | **F.W4** |
| **FR-AH-43 · D-i5** | Resting menu label at **4.502:1** (this seat's recompute) — 0.4% over AA on a translucent `glass-floating` surface whose oklab tint darkens over light. | **SS-13** (composited readback), then F.W4 if it crosses |
| **FR-AH-44 · C-15** | `?? tabs[0]` unreachable (six returns ⊆ five values — enumerated) and it is what converts the FR-AH-3 gap into a silent "Paper". | **F.W4** (with FR-AH-3: surface, don't mask) |
| **FR-AH-45 · C-16** | Zero props/emits/slots — no contract, no isolated test surface; web/ has no unit runner and no e2e touches the header's nav/logo/toggle. | **NO-WAVE-OWNER** — SS-3/SS-4 spec-input (what contract should a shell header state?) |
| **FR-AH-46 · C-17** | `view-box="0 0 200 200"` passed byte-identical to the default. The `--accent-red`-undefined sub-claim is KILLED (defined at color-radius.css:258 — re-read). | **F.W4** (deletion batch) |
| **FR-AH-47 · L-16/C-18** | The two dynamic `:is` families (:117, :130) — line-exact at HEAD. **R3-10 fold** (intake identity); the L-16 sharpening carries: F.W4's per-component denominators must resolve `:is` candidate sets or inherit an R5-7-shaped blind spot on a second axis. | **R3-10 fold** + F.W4 denominator rider |
| **FR-AH-48 · L-17** | ~10,240 tuple arrays allocated synchronously per mount; data retained twice (`data` + `pointsByLevel`). Placement corrected to per-instance (the table). | **F.W4** (with FR-AH-1) |
| **FR-AH-49 · L-18** | Per-tick path-string rebuild (~512 segments × ~21 frames / 350ms). Bounded; booked for completeness. | **F.W4** (no action likely) |
| **FR-AH-50 · L-19** | Explicit negative: R5-7's native-loop blind spot does NOT bite (sole v-for is a component callsite; zero native list elements). | recorded — no phantom in the F.W4 roll-up |
| **FR-AH-51 · L-21** | `router.push` results discarded ×3 — the NavigationFailure channel is unread. | **F.W4** |
| **FR-AH-52 · L-22** | style.css:129-131's line-pin into this file rotted by 14 lines (ring now :188-191 — re-verified); cite the selector instead. | **F.W4** (one line) |
| **FR-AH-53 · MISS-vendor-paper (r2)** | The same header edge forces `vendor-paper` (3,111 B) eager via svg-fourier.ts:11's static `@mkbabb/pencil-boil` import, falsifying vite.config's "only loaded on /paper routes" comment — the second cadence-premise failure on one decorative edge (this seat re-verified both the import and the entry's static vendor-paper edge + modulepreload). | **F.W4** (comment + boundary ride FR-AH-1's fix) |
| **FR-AH-54 · MISS-observer (r2)** | `resolveVizColors()` re-runs on ANY root class mutation (attributeFilter is the attribute, not the value), five synchronous `getComputedStyle` reads per fire, observer never disconnected — firing on exactly the interaction this header owns, concurrent with the 350ms morph. Multiplier per toggle UNPROVEN. | **F.W4** (owner App.vue; cross-ref fr-BasisCanvas D-1) |
| **FR-AH-55 · MISS-7 (r1)** | `-webkit-tap-highlight-color: transparent` (:181) kills the last touch feedback channel on a control that suppresses its ghost plate and pins its children's colours — what survives is a sub-perceptual press scale. | **F.W4** (with FR-AH-15) |
| **FR-AH-56 · C-3-narrowing** | The C axis's own severity narrowing of the route row is the honest reading (no share-link generator exists; the D axis's "destination of every share link" overstates) — adopted into FR-AH-3's severity. | folded into FR-AH-3 |

## Killed-claims register (refutations re-verified or issued by this seat)

1. **D-M6 headline** ("nav trigger at 82% of the 44px floor") — REFUTED, ratified unanimously + this seat's own token walk: the CVA default size arm applies without the prop; coarse-pointer height = 60-67.5px = 136-153% of the floor. The kill's own inversion is booked as FR-AH-16.
2. **D-M6 second falsifier** ("the header opts OUT of the comfort axis") — HALF-REFUTED, ratified: box/font/gap all ride `--ui-scale`; only the two icon glyphs are pinned — a proportion break inside a conforming control, not an opt-out.
3. **D-m3 mobile arithmetic** (63px table; "13% of a 480px viewport") — REFUTED AS STATED, ratified: the tallest coarse-pointer child is the Button at 67.5px → ≈85.5px header ≈18%. Direction survives and worsens (FR-AH-16/29).
4. **C-3 line pins** (`router:112` for `/s/`, `:106` for the demo route, `:186-196` for the persist block) — REFUTED, ratified against this seat's ranged read (`/demo` :111-115 · `/s/` :116-119 · persist :177-188). D-B4 and L-2 cite correctly; repair waves must not open C-3's lines.
5. **C-1 module-eval sub-claim** — REFUTED (reader-2), ratified by this seat's whole-file read (`<script setup>` opens :16). Consequence corrected: once per mount, not per route.
6. **C-1 "112,388 B unreachable levels"** — REFUTED three ways (reader-2), all three re-verified: the clamp is to the levels bounds [1,50], not the config; level 3 is fetched on every default morph (the 3→5 bracket — this seat re-walked the loop); levels 1-2 are user-reachable on `/morph` (`setLevel` via HarmonicLevelGrid's full-levels v-for — re-read). The 112,388 B arithmetic is exact (this seat: 56,266+56,122) and measures nothing.
7. **C-1 long-float measurement** ("428,474 chars = 87.2%") — PARTIALLY REFUTED, reproduced exactly by this seat: 23,336 hits (matches) / **405,139 chars = 82.5%** (does not). The BLOCKER is untouched; F-wave sizing must use these numbers.
8. **L-1 per-key census** (partial_sums 198,419 / "≈79 KB (17.6%) dead") — REFUTED (reader-2: internally impossible, sums to 105.5%), and this seat's own census reproduces reader-2's corrected table digit-for-digit: dead = **75,210 B = 16.7%** (C-1's figure). Also L-1's "491,278 B" → 491,312 B on disk / 491,280 chars.
9. **D-B2 leg 2** (PJS unicode-range excludes U+2131) — REFUTED (reader-2), ratified: PJS is never loaded at all (`./styles/fonts` unimported — this seat's grep + index.css read). Replaced by the strictly-stronger FR-AH-2.
10. **C-17 sub-claim** (`--accent-red` "not defined at 4.0.0") — REFUTED: color-radius.css:258 light / dark-arm.css:111 dark, both re-read.
11. **D §5 U-3 reconcile note** (`--color-ring` vs `--ring`: "one is on the wrong side of the bridge") — REFUTED: bridges.css:80 declares `--color-ring: var(--ring)`; both resolve; a naming normalisation, not an uplift risk.
12. **C-13's "UNPROVEN-NEEDS-LIVE" framing** — PARTIALLY KILLED (see the ruled table): base-state arbitration is static (scoped 0-2-0 beats unlayered 0-1-0; `cn()` inert on non-Tailwind classes); the hover-plate background is an equal-specificity unlayered ORDER tie that the stale dist cannot witness — that one cell stays in SS-13.
13. **Provenance-pin cluster** (C-4 "15 compilerOptions keys" → **13**; C-6 `auth.ts:19` → **:16**; C-7 `colors.ts:81` / L-12 `:79` → **:80**; L-14 "re-exports getEasingFn" → it does not) — all re-pinned by this seat against the live tree.
14. **★ reader-1's kill of the L-6 engine quote** ("REFUTED AS A QUOTE … fabricated in its details … must not be copied forward") — **KILLED BY THIS SEAT.** Both quotes exist; reader-1 extracted `AnimationGroup.stop()` (:327), the challenge and reader-2 quoted `Animation.stop()` (:937) — the class the consumer awaits. The challenge's quotation is REINSTATED; the accusation of fabrication is stricken; the mechanism was never in doubt (both bodies call `_resolvePlay()`).
15. **reader-1's L-1/C-1 cell** ("`prepareFourierShape` runs at module-eval scope … not inside `setup()`") — KILLED BY THIS SEAT (inherited from C-1's killed sub-claim; contradicted by reader-2's L-17-consistent reading and this seat's file read).
16. **reader-2's missed row #3 as a NEW find** — KILLED AS NEW by this seat's identity guard: the peer-range violation is fr-AnimationControls C-1 (banked 2026-08-07, same registry). The fact stands; the booking folds; the `@lucide/vue` dual-tree rider is the only new material.

## Missed rows booked (11)

MISS-FONT (=FR-AH-2 root cause, r2-M1 ⊕ r1-M-1) · MISS-2 header-height inversion (FR-AH-16) · MISS-3 de-sprung menu lift (folded into FR-AH-11) · MISS-4 PRM-vs-JS-morph (FR-AH-31, demoted) · MISS-5 unsound rank-5 remedy (FR-AH-30) · MISS-6 logo hit-box (FR-AH-26) · MISS-7 tap-highlight (FR-AH-55) · MISS-crossorigin (FR-AH-36) · MISS-dead-exports (FR-AH-37) · MISS-vendor-paper (FR-AH-53) · MISS-observer (FR-AH-54).

## Superlatives (provenance: which seat, endorsed by whom; L-18 runs both ways)

- **S-1 (all three axes + both readers + this seat)** — the self-hosted avatar block (:74-85): policy, convention, and density arithmetic committed beside the code; intrinsic 80×80 against the h-10 box (zero CLS); `decoding="async"`; base-URL-correct. Asset ships (8,825 B; `web/public/assets` is a symlink `find` misses — see S-7).
- **S-2 (D/L/C, ratified)** — `@click.stop` on both hover-card anchors is load-bearing, not reflexive: Vue's synthetic propagation follows the component tree through the portal, and the enclosing logo binds its own click at :64. Two characters exactly where the composition needs them.
- **S-3 (D, ratified)** — chevron rotation driven by the primitive's own `data-state` (:214-216): no duplicated `isOpen`, no possible disagreement with the menu.
- **S-4 (D-S5, endorsed WITH ITS BLIND SPOT NAMED)** — PRM inherited, not re-implemented: the axis hypothesised a defect, killed it against a11y-overrides' `!important` blanket (re-verified by this seat, incl. the `[data-allow-motion]` carve), and published the kill. Best-argued row in the corpus — and its scope claim is falsified one layer down by FR-AH-31: the blanket reaches CSS, not the JS-written path attribute. The posture is superlative; the generalisation was not.
- **S-5 (L-S6, ratified)** — the computed accessible name (:115) is the single reason FR-AH-4 is visual-only; its value exactly bounded by FR-AH-3 (wrong on `/v/`). Superlative and defect fitted to each other.
- **S-6 (D/L, ratified)** — the teleport-CSS split is EXACT: precisely the four portaled classes escape scoping, each annotated with the primitive it serves; the other 22 rules stay scoped. The mechanism is right; its contents are FR-AH-11/32.
- **S-7 (corpus superlative, re-verified)** — challenge-L §5's killed-candidates table: three candidate defects killed by their own falsifiers and PUBLISHED, including one caused by the author's own tooling (the symlink) and one "correct as written — no finding" (`tabs[]` un-reactive). The falsifier gate demonstrably real.
- **S-8 (corpus superlative, re-verified)** — challenge-C §4's 18-row typed reconciliation (AGREE/EXTEND/CONTRADICT/ADOPT), including the argued CONTRADICT of lane-frontend:46's load-cadence premise (independently re-derived: the shell header's static reach breaks it) and the negatives that stop F.W1 over-budgeting this component.
- **S-9 (reader-2's own, ratified)** — `loadAnimationEngine()` is a genuinely correct lazy boundary (engine chunk absent from the entry and the preload list — this seat's probe), whose consumer one hop up ships 450 KB eagerly. The discipline exists in this codebase; it was not applied to the largest asset.
- **S-10 (C-S1/C-S2, ratified)** — dark-mode state fully delegated to `useGlobalDark` (no rival writer; the pre-paint bootstrap is the documented hand-off), and UserSlugBar's complete `useClipboard` migration with rationale committed.
- **S-11 (reader-2's own, ratified)** — the router this header mirrors so badly is itself carefully built: the `/s/` redirect FORWARDS its param as a function; beforeResolve flushes parked resolvers so the view-transition queue "can never deadlock" (comment re-read). The header's staleness is the anomaly in its neighbourhood.
- **S-12 (this seat's own)** — reader-2's decision to test the strongest falsifier nobody asked for (is glass-ui an independent eager path to value.js? — vendor-ui carries none of it) is the difference between asserting sole-cause and proving it; this seat reproduced the probe.

## UNPROVEN-NEEDS-LIVE residue (SS-13 inputs)

1. **The FR-AH-16 screenshot** — 768×1024 with `pointer: coarse` emulated: does the 60px control clip, overlap `<main>`, or grow the bar? Highest-value single shot for this component. (Second frame: <640px, ≈85.5px header.)
2. **The hover-plate order tie** (FR-AH-15 secondary / C-13 residue) — computed `background-color` of `.nav-trigger:hover` on a FRESH build (the stale dist cannot witness the current pin).
3. **D-i5 composited surface** — whether `glass-floating`'s oklab tint drops the menu surface below `--popover`'s luminance; nominal margin is 0.4% (4.502:1, this seat's recompute).
4. **iOS-Safari focus-on-tap** for `div[tabindex="0"]` (FR-AH-13's second leg) — which failure mode touch users get (navigation vs card-flash-then-navigation).
5. **L-7's reachable throw** — fault-inject `Animation#parse`; confirm the toggle bricks for the session (the posture defect books regardless).
6. **The U+2131 fallback face** — macOS Apple Symbols vs Windows/Android tofu, once the (now fully-proven) chain terminates at `system-ui`.
7. **The boot-payload trace** — real LCP/TBT attributable to FR-AH-1 (450,631 B) + FR-AH-7 (348,707 B) + FR-AH-2 (402,944 B of unusable preloads competing for the same bandwidth). Three statically-proven byte findings converge on one first paint; the Q14 precedent says the trace is worth taking.
8. **The UserSlugBar pill geometry** under the coarse-pointer 44px lift on its two `size="icon"` buttons (the surviving D-M6 leg; the lift is proven, its visual cost is not).
9. **`resolveVizColors` fire-multiplier per theme toggle** (FR-AH-54) — whether glass-ui's flip machinery mutates the root class more than once.
10. **`getComputedStyle` serialisation of `--viz-*`** (fr-BasisCanvas D-1's residue, restated here only because the header is the trigger edge).

## DISSENT — preserved, not ruled away

1. **The double citation-inheritance event.** The corpus produced BOTH directions of the pathology in one component: reader-1 inherited C-1's module-eval error into a confirmed cell (killed here), and reader-1's own kill of the L-6 quote was a misread that ACCUSED the challenge of fabrication while committing the citation error itself (overturned here). Both readers were simultaneously right about the mechanism and wrong about a quotation. The fleet lesson: a "verbatim" cell is only as good as the CLASS it was read from; extraction must carry line numbers (this record's engine cells do).
2. **Reader-2's D-B3 BLOCKER** — preserved: "a privilege indicator invisible in the default theme manufactures false confidence." This seat's MAJOR rests on the badge not being a gate; if admin affordances ever key on the badge's visibility rather than the token, the row revives at BLOCKER.
3. **Reader-2's L-4 MAJOR** — preserved: the store-instantiation cost argument, demoted on Pinia-singleton grounds.
4. **Reader-1's M-4 MAJOR (PRM)** — preserved: "the one route-independent, always-mounted animated surface ignores the user's stated preference." Demoted on surface size; the superlative-falsification is retained at full weight.
5. **Severity ordering across the payload cluster** — neither reader ranked FR-AH-2 (fonts) against FR-AH-1 (JSON); this seat notes they are the SAME first paint and the F-wave should budget them as one boot transaction, whatever the per-row severities say.
6. **What this seat could not witness**: no browser, no fresh build. Every cascade/paint claim above is either pin-static or explicitly parked in SS-13; the stale dist was used only where its staleness is immaterial (byte sizes of its own artifacts) and never to arbitrate the current pin's cascade.

## Verdict

**ADJUDICATED-DEFECTIVE, well-authored in its instincts** (the corpus's phrase, earned): 1 BLOCKER (the 450 KB decorative boot payload), 15 MAJORs concentrated on the three things a masthead is for (typography-truth, location-truth, privilege-truth) plus a morph composable whose error/teardown postures are both inverted, 22 MINORs, 18 INFOs, 2 identity folds honoured (fr-BasisCanvas D-1, fr-AnimationControls C-1), 16 register kills of which one is a kill-of-a-kill in the challenge's favour, 11 missed rows banked — routings are M-25 agglomeration input for the forming X·F specs, dominated by **F.W4** (the repair surface), with **F.W1** holding the uplift folds (hover-card, easings fork, peer graph, Popover-touch improvement), **F.W3** the DarkModeToggle shadow-reconcile rider, **F.W0** the manifest row, **GLASS-RELAY** two producer asks (cm-serif's undeclared variable; light `--tier-featured`), and **NO-WAVE-OWNER** two spec-inputs (the layering remedy caution; the no-contract test surface).

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
