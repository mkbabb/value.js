claude-fable-5

# fr-BasisSelector — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-08 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/BasisSelector.vue` (324 lines) + its display/palette/loader/contract chain.
**Substrate**: fourier HEAD `cd26c653` — re-pinned by this seat (`git log -1`). **Dirty-tree disclosure**: the subject file is DRIFTED by exactly the 3-hunk `variant="glass-scrubber"→"standard"` rename (comment `:25`, `variant=` `:170`, `:196`) — this seat re-ran `git diff -- …BasisSelector.vue` and reproduced the three hunks byte-for-byte; every line anchor in this record is against the working tree (the state that ships) and is stable across the drift.
**Corpus**: 72 accusation rows across three axes — challenge-D (design, 30 rows, opus-5[1m]), challenge-L (library, 19 rows, opus-5[1m]), challenge-C (consumption, 23 rows + §6 addendum, opus-5[1m]) — at `docs/tranches/V/megatranche/audit/fourier-components/BasisSelector/`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Their returns arrived through the orchestration channel; the immutable challenge files sit beside this record; every load-bearing or disputed cell was **re-derived by this seat against the trees with its own instruments** before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. Every kill was re-derived (a weak kill gets un-killed — none needed un-killing, but three reader cells died under this seat's reads); every missed find was re-proven before banking; all seven reader-vs-reader conflicts were ruled with fresh reads. Two counting errors were found INSIDE the readers' own counting-correction kills — recorded in DISSENT as the fleet lesson.
**Arbiter's own instruments this session (all static + node, zero browser probes — probe parsimony; product source read-only)**: full read of BasisSelector.vue (324, whole); basis-display.ts (whole, 7 lines); colors.ts:77-96 + cssVarToHex arm census; defaults.ts:7-8; useWorkspaceLoader.ts:18-19/:51-56/:67/:84/:106-116; ContourSettings.vue:104-150 (runCompute + debounce + `lastComputedKey` guard) + `CONTOUR_DEFAULTS` import :6/:33-34; VisualizationView.vue:258-270 (both binding sites); stores/workspace.ts:294/:319-324/:349-355; stores/gallery.ts:252; EasingPicker doctrine (found at `src/components/visualization/EasingPicker.vue:12/:24` — both readers cited a wrong path); EditorControlsDock.vue:106/:205/:208; useCoeffHover.ts:58-66; api: shared.py:12, computation.py:44, equations.py:20, visualization.py `n_harmonics`×5 + `active_bases`×5 + AnimationData:65-130, images.py:215-228 (cache-hit branch), contours.py:21-25 (`source="editor"`), image_storage.py:248-267 (**`extraction_cache_key` field list — `n_harmonics` omitted, `n_points` present**); e2e: `grep -n fixme` visualization-ux.spec.ts (**:110/:133/:192 + :212**; sole live keystone :150) + visualization-crud.spec.ts:630 + settings-persistence.spec.ts:72-73/:93 ("default is 50" prose) + `grep -rn "Reset to defaults" e2e` → 0; tsconfig.json (whole — **13** compilerOptions; no `noUnusedLocals`, no `noUncheckedIndexedAccess`); package.json (11 deps, no lucide; lucide in devDeps); **byte-identity diffs, all four zero-output**: `.inline-number` (BasisSelector:212-233 ≡ SliderControl:117-138), reset template (:121-136 ≡ ContourSettings:191-206), reset CSS (:295-313 ≡ ContourSettings:387-405), gallery `basisLabels` (GalleryCard:36-50 ≡ GalleryCardModal:41-55); repo greps: `--slider-scrub` = **23 declarations / 7 files**, `variant="standard"` = **14 / the same 7 files**, `startsWith("fourier")` = **7 files** (incl. useWorkspaceLoader), `basisDisplay` = **8 files**, `fourier-f` = **5 live sites / 4 files**, `<SliderControl` in ContourSettings = **5**, `":where("` in subject = **1** (in-comment, :255); installed glass-ui 4.0.0 dist: `grep -rl slider-scrub` → **EMPTY**; `.slider-thumb[data-v-534634a7]` base rule (width:0/opacity:0/background:0 0) vs the **`[data-variant=spectrum]`-only** `--slider-thumb-bg` reader; `.slider-range` reading `var(--slider-range-bg,var(--primary))` unqualified; `.glass-slider[data-size=*]` setting `--slider-track-height` at (0,3,0) on the root; `@layer` count **0**; ConfiguratorRow.vue.d.ts `canReset` + "No a11y for/id wiring"; Slider.vue.d.ts `number[] | undefined` payload; Button.vue.d.ts `disabled?`; compiled chunks: `"aria-label": n.$attrs["aria-label"] ?? void 0` (slider), `inert: !i.value || void 0` (configurator), the 13-variant cva `outline:` string; **producer glass-ui 7.0.0** (`package.json` 7.0.0): `slider/types.ts:10` **`SliderVariant = "scrubber" | "spectrum"`**, slider token census (**6 names, NO `--slider-thumb-bg`**), `Slider.vue` = 367 lines with **0** `--slider-` tokens (they live in styles.css), Button.vue:39-60 (`emphasis/tone/size/iconOnly/loading` — no `variant`, no `"icon"`), export map: `./icon-tooltip` **NO** / `./tooltip` YES / `./toggle-chip` **NO** / `./chip` YES / `./slider` `./button` `./configurator` `./number-field` `./toggle-group` YES; **contrast recompute (own node instrument, premultiplied `color-mix` per the declared method)**: `#bf4040/#3d72b8/#9545b8` labels over their 12% self-tint on `--neutral-0`: **light 4.24 / 4.04 / 4.48 · dark 3.50 / 3.70 / 3.33 — all six FAIL AA 4.5**.

## Scoreboard

**44 adjudicated register rows (3 BLOCKER · 14 MAJOR · 22 MINOR · 5 INFO), covering 68-of-72 corpus rows confirmed in some form · 2 identities FOLDED BY REFERENCE to fr-BasisCanvas (never re-booked) · 13 kills in the killed register · 15 missed finds banked (5 folded into composite rows) · 9 superlatives with provenance.**

Blocker-weight survivors: **B-1** (the private default 50 contradicts the canonical 200 — "Reset to defaults" is destructive and its dim-state is inverted at the pristine state, with the wrong constant already absorbed into test prose), **B-2** (the entire `--slider-scrub-*` retint contract is definition-absent — a declared feature that has never painted, 23 dead declarations fleet-wide), **B-3** (every basis pill's pressed label fails WCAG AA in BOTH themes — painted by frozen module-eval literals, not the tokens the corpus measured — while three of the four axe keystones over the panel are `test.fixme` on a justification that expired two majors ago).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **C-5's migration target** (`./icon-tooltip` as the text-only cure) | CONFIRMED C-5 incl. "the amendment stands: target `./icon-tooltip`" | KILLED the sequencing — the subpath is deleted at 7.0.0 | **LC wins; DU overruled on sequencing.** | My producer export-map read: `./icon-tooltip` **NO** at 7.0.0, `./tooltip` YES. With the F.W1 tri-package uplift co-resident (CENSUS one-atomic-transaction), the prescribed one-word swap migrates 35 callsites onto a deleted subpath. The shim defect survives at MINOR; the target must be re-derived against 7.0.0. Same hazard class C-18 correctly flagged for `./toggle-chip`→`./chip` three sections later in the same document. |
| R-2 | **How much of the a11y net is off** (corpus: 2 keystones fixme; LC confirmed 2) | Missed-find: THREE of four + a fourth booked fixme in crud | confirmed the two-fixme leg as filed | **DU wins.** | My grep: `test.fixme` at **:110** (workspace default), **:133** (ContourSettings open), **:192** (AnimationControls dropdown), plus :212 (Invariant-19 red baseline) and visualization-crud.spec.ts:630. Only keystone 3 (`:150`, ExportModal) runs. The keystone-1 comment at :108 ("keystones 2–4 below still enforce…") is self-falsifying against :133/:192 in the same file. Un-fixme is a four-test operation. LC's confirmation repeated the corpus's undercount — recorded in DISSENT. |
| R-3 | **`fourier-f` consumption count** (inside the shared kill of D-M8's "unused" leg) | five live sites | three live sites | **DU wins.** | My grep: AppHeader.vue:68, PaperView.vue:356, PaperArticleWindow.vue:116 + :208 (styling hook), GalleryCardModal.vue:187 — five sites, four files. The kill stands either way (the utility is an established in-tree idiom; BasisSelector is the outlier hand-tuning six constants); LC's own kill undercounted — DISSENT. |
| R-4 | **Aliasing severity** (L-M1/C-12: `selected` aliases the parent's reactive array) | MAJOR | MINOR ("C's framing is the honest severity") | **MINOR.** | Facts unanimous and verified (`:37` no copy vs `:39` `[...v]` — the inconsistency is the tell; mutation at :108-113 before the emit at :115). Both readers agree no symptom is reachable at HEAD: VV:267 installs a fresh array same-tick, BasisCanvas watches by identity. A contract violation with a one-character cure and no reachable break follows the fr-BasisCanvas D-23/BC-16 severity precedent. DU's dissent preserved. |
| R-5 | **The shape of the contrast blocker** (D-B3) | BLOCKER, verdict-only, evidence corrected (4.23:1 at the frozen literal) | MAJOR as filed + a separate missed BLOCKER (all six ratios, both themes) | **ONE BLOCKER row (B-3), composed** — no double-booking. | The corpus's table resolved `--viz-*` tokens that never reach the element (`--pill-color` binds `basisDisplay[k].color` = the module-eval snapshot of the reactive initialiser — my read of basis-display.ts + colors.ts:77-87). My own recompute at the values that actually paint: light **4.24/4.04/4.48**, dark **3.50/3.70/3.33** — all six fail; the theme the corpus cleared (dark, reported 5.92+) is the theme that fails hardest. LC's arithmetic exact (DU's 4.23 = rounding). The table dies (K-7); the verdict survives worse than filed. |
| R-6 | **The DU-MAJOR / LC-MINOR ladder** (D-M6 reflow · D-M8 cm-serif · D-M9 mobile axis · D-M11 three bands) | MAJOR ×4 | MINOR ×4 | **MINOR ×4.** | D-M6: direction proven by construction (min-width is a floor over three unequal labels under `justify-center` — my read of :47-51/:138/:259), magnitude live-only, no capability loss. D-M8: an inert utility + typography drift; the repo-wide `--font-serif-math` absence books at F.W1 token parity. D-M9: the producer's coarse floor (`--control-floor`, a11y-overrides — the D-§6.2 self-kill both readers verified) caps the harm; wrap threshold live-only. D-M11: density/hierarchy, no capability loss — the fr-BasisCanvas R-5 precedent (D-12/D-13 MINOR). All four DU dissents preserved; each re-opens on SS-13 evidence. |
| R-7 | **The thumb half of the retint cure** (both readers killed the corpus cure; DU also filed it as a standalone missed MAJOR) | standalone missed row | inside the K-1 kill | **Folded into B-2's cure clause + K-1 — not a second row.** | One identity: the corpus's "rename to `--slider-range-bg`/`--slider-thumb-bg`, identical pre/post-uplift, cheapest blocker" is killed three ways by my own reads — (a) at 4.0.0 `--slider-thumb-bg` has a receiver ONLY under `[data-variant=spectrum]` while the `standard` thumb is width:0/opacity:0; (b) at 7.0.0 `--slider-thumb-bg` does not exist (6-token census); (c) the cited `Slider.vue:337-338, 522` is out of range — the file is 367 lines and contains zero `--slider-` tokens. The correct disposition lives in B-2: rename the range pair, DELETE the thumb pair (8 of 23 fleet declarations have no receiver under any rename, either version). |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim (one line) · decisive anchor · terminal disposition → route**. Full proof cells live in the challenge files, incorporated by reference; anchors re-verified by this seat unless marked (adopted).

### BLOCKER

- **B-1 = D-B1 / L-B2 / C-3 — BLOCKER.** The private `DEFAULTS.nHarmonics = 50` (`:73-77`) contradicts the canonical 200 at every other site this seat read (defaults.ts:7; loader seeds :18/:67/:84 all `CONTOUR_DEFAULTS.n_harmonics`; shared.py:12; computation.py:44), and the single call site binds the loader's ref with no transform (VV:266). Consequences, all mechanical: `isDefault` (`:79-84`, verified — also requires `selected` = exactly `["fourier-epicycles"]`) is FALSE at the pristine state, so the "nothing to reset" dimming (`:307-310`) engages only AFTER the destructive act; `resetDefaults` (`:86-91`) emits 50 — a 4× fidelity cut that arms ContourSettings' debounced `runCompute` (both scalars in the watch key, :140-149, my read). The wrong constant has propagated into test prose (settings-persistence.spec.ts:72-73 "default is 50", :93) and **no test covers the reset path** (`grep "Reset to defaults" e2e` → 0). **Trimmed per K-5**: the recompute is epicycles + bases + one redundant extract round-trip that CACHE-HITS — `extraction_cache_key` deliberately omits `n_harmonics` (image_storage.py:248-266, my read) — not a CV re-extraction; the destruction claim survives at full strength, the cost claim at reduced. **ADJUDICATED → F.W3/W4** (import `CONTOUR_DEFAULTS`, delete the private object, make the props required per C-14 — one repair unit; a reset e2e is the born-RED).
- **B-2 = D-B2 / C-1 — BLOCKER (fleet-scoped per the L-9.1 scope law).** The `--slider-scrub-*` token family is DEFINITION-ABSENT in installed glass-ui 4.0.0 (`grep -rl slider-scrub node_modules/@mkbabb/glass-ui` → EMPTY, my run) — the declared per-basis retint (`:315-323`) has NEVER painted; both sliders render the stock `--primary` capsule (`.slider-range` reads `var(--slider-range-bg,var(--primary))` unqualified, my dist extraction). Blast radius at the scope where the count lives: **23 dead declarations across 7 files** (BasisSelector 4 · EditorControlsDock 4 · SliderControl 5 · HarmonicLevelGrid 4 · MorphPhaseConfig 4 · GlassTimeline 1 · ConvergenceTimeline 1 — my grep; the corpus's "20" is wrong). Root cause is a MECHANISM, not a residue: the uncommitted rename sweep updated 3-of-4 occurrences of the literal `glass-scrubber` and 0-of-4 token declarations — this seat re-ran the `git diff` and reproduced the exact three hunks. **Cure corrected per R-7/K-1**: rename ONLY the range pair to `--slider-range-bg`; **DELETE** the thumb pair — no receiver at `variant="standard"` at 4.0.0 (spectrum-only rule, zero-width thumb) and no `--slider-thumb-bg` at 7.0.0 at all. A third independent unsoundness banked at M-21: `--slider-track-height` cannot be overridden from consumer scope either (library sets it at (0,3,0) on the same element; 0 `@layer`). **ADJUDICATED → F.W1** (the acceptance gate must be a token-family sweep — `grep -rn -- "--slider-scrub" web/src | wc -l` → 0 — never a variant-string sweep; execution rides F.W3/W4).
- **B-3 = D-B3-as-corrected ∘ the C-4 consequence — BLOCKER.** Every pressed basis pill fails WCAG 1.4.3 AA in BOTH themes: `--pill-color` (`:145`) binds `basisDisplay[k].color`, the module-eval snapshot of the reactive initialiser (`#bf4040/#3d72b8/#9545b8` — basis-display.ts + colors.ts:77-87, both read whole), theme-invariant; composited per `:269-273` over `--neutral-0`, **this seat's own instrument: light 4.24 / 4.04 / 4.48 · dark 3.50 / 3.70 / 3.33 — all six under 4.5**, the failing state is the component's resting default (`:37`/`:74`), and the large-text exemption is excluded (control-text = small-register clamp × `--ui-scale`, `font-medium` at :259). The corpus's token-resolved table is killed (K-7): D never folded C-4, and the dark column it cleared is the worst axis. The net that should catch it is three-quarters off: **three of four axe keystones `test.fixme`** (:110/:133/:192; only :150 runs — R-2, my grep) on a stale-by-supersession justification (the booked `^2.0.0` "no `inert`" baseline vs the installed 4.0.0 emitting `inert: !i.value || void 0`, verified in the compiled chunk), with self-falsifying keystone comments and a fourth booked fixme at crud:630. **ADJUDICATED → F.W3/W4** (the painter fix is C-4's reactivity cure or C-7's `var(--viz-*)` binding — the snapshot defect itself is fr-BasisCanvas's row, see FOLDS; un-fixme all four tests; only THEN does a producer token-darkening relay make sense — the glass BH ask D-B3 proposed is premature while a frozen literal is the painter).

### FOLDED BY REFERENCE (identity guard — banked at fr-BasisCanvas, never re-booked)

- **C-2 / L-B1** (`cssVarToHex` has no `oklch()`/`light-dark()` arm; `VIZ_COLORS.fourier/chebyshev/legendre` → `#888888`) **= fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER → F.W1)** — one identity, already banked with the canvas blast radius (trail/glow/connector/labels). At THIS component the local consequence is **REFUTED (K-4)**: the projection targets the dead `--slider-scrub-*` vocabulary, so `#888888` never reaches a pixel here — the sliders paint `--primary` (B-2), and the surviving local content is C-7's superfluous round-trip.
- **C-4 / L-M6** (the `basisDisplay` module-eval snapshot) **= fr-BasisCanvas D-2/BC-5/C-3 (MAJOR → F.W1)** — one identity, banked there with the frozen-vs-live split. What is NEW here — the pill-contrast consequence — books as B-3, citing the snapshot by reference. Scope correction carried: `basisDisplay` has **8** files, not "declaration + one consumer" (K-10; six consumers are frozen at the light literals).

### MAJOR

- **M-1 = D-M1 — MAJOR.** `variant="outline"` rests at opaque `bg-background` inside the glass panel and the scoped hover REMOVES it (`:264-268` `background: transparent` at (0,3,0) beats `.hover\:bg-accent:hover`; at rest nothing scoped touches background) — the inverse of every other surface; `outline` is the one non-glass register in the pinned 13-variant set and **has no successor at 7.0.0** (M-7). → **F.W3/W4** (variant re-selection rides F.W1).
- **M-2 = D-M2 — MAJOR.** Every pill-row boundary is under the 1.4.11 3:1 floor (resting 12%-mix ≈1.3:1, hover 25% ≈1.7-1.8:1, pressed 40% ≈1.7-1.9:1 — the two readers' recomputes agree in conclusion; small drift from D's input error noted), and the inactive pill compounds with M-1 into a page-coloured rectangle bounded at 1.3:1. → **F.W3/W4**.
- **M-3 = D-M3 — MAJOR.** `outline: none` on both numeric inputs (`:221`) with focus feedback = the hover hairline in ONE shared selector list (`:225-228`, verified — focus and hover are indistinguishable, sub-2:1); the global `:focus-visible` rings cover four other classes only. → **F.W3/W4**.
- **M-4 = D-M4 / C-23 — MAJOR.** A tri-state cycler (epicycles→series→off, `:94-106`) wears a binary `aria-pressed` (`:144`; `isBasisActive` true for both fourier modes, `:53-55`), no `role="group"`, no group name — while the repo's own doctrine sits in-tree verbatim: `role="menuitemradio"` + `aria-checked`, `src/components/visualization/EasingPicker.vue:12/:24` (my read; both readers mis-pathed it, doctrine confirmed). → **F.W3/W4** (rides M-13's ToggleGroup cure).
- **M-5 = D-M5 — MAJOR.** Basis-identity hues on basis-agnostic parameters, both directions: `n_harmonics` drives ALL THREE bases (`max_degree: contourSettings.n_harmonics` at workspace.ts:319, my read) yet is tinted fourier-red (`:176`); `n_points` has no basis affiliation (:320/:324) yet is tinted chebyshev-blue (`:203`); the sibling's convention is one non-identity amber accent ×5. Moot today under B-2; ships the day the retint is cured — adjudicated at the severity it ships at. → **F.W3/W4**.
- **M-6 = D-M7 / L-M3 / C-19 — MAJOR.** The repo's own labelled-slider chassis (`SliderControl.vue`, census-blessed "correct posture — keep") is bypassed and hand-copied twice: `.inline-number` **byte-identical** (my zero-output diff), retint tokens drifted (30/45 vs 25/35; the 16px track-height absent here), the clamp idiom regressed (`|| min` vs `Number.isFinite`), and the copy lost the `<label>` wrapping (bare `aria-label`-named inputs). ContourSettings consumes the adapter **5** times (my count; C-19's "six" and L's "four" both wrong). Present-tense visible divergence KILLED (K-11 — all 23 declarations dead, all sliders render identically today). → **F.W3/W4**.
- **M-7 = D-M10 + DU-missed-1 — MAJOR (F.W1 SCOPE AMENDMENT).** The 7.0.0 break surface is prop-VALUE-axis, and the census model misses it: Button deletes `variant` and `size:"icon"` entirely (`ButtonProps` = emphasis/tone/size/iconOnly/loading — my producer read; `outline` has no successor; breaks `:126/:127/:141/:142`); **Slider `variant="standard"` DOES NOT EXIST at 7.0.0** — `SliderVariant = "scrubber" | "spectrum"` (types.ts:10, my read), the SECOND rename of the same axis (glass-scrubber→standard→scrubber), **14 occurrences across the same 7 files** already carrying B-2's dead tokens (my grep). This file: zero subpath breaks, SIX prop-value breaks + the lucide specifier (DU-missed-8, banked INFO). F.W1 must re-derive the break surface as a prop-level diff over every surviving subpath. Improve side: 7.0.0 adds `loading` (cures D-m12) and defaults `type` (cures D-m14, at Button.vue:95 — corpus cite :55 is line drift). → **F.W1**.
- **M-8 = L-M2 / C-15 + LC-missed-2 — MAJOR.** The number inputs' commit model is wrong in both directions: (a) per-keystroke emit + `parseInt(…) || min` coerces the cleared field to the minimum and refills under the caret (`:157-166`/`:184-193`, one-way `:value`; `"1e3"` → 1); (b) the CONVERSE, missed by all three axes and banked from reader-LC after mechanism re-derivation: when the clamp result EQUALS the current prop (type "999" at prop 500 → emits 500), no reactive dep changes, no re-render is scheduled, and the DOM retains the out-of-range text — a silent display/model desync that nothing corrects (`max`/`min` on number inputs are validation-only). The correct posture is in-tree 150 LOC away (SliderControl:40-49) and unused; the primitive root is M-16 (NumberField). Every keystroke also re-arms the 1s compute debounce. → **F.W3/W4**.
- **M-9 = L-M4 / C-20 — MAJOR.** The deliberately-minted "off" state (`[]`, `:103-104` "canvas handles it gracefully") is unrepresentable end-to-end and silently laundered on every path — server forbids it (`min_length=1` at visualization.py:186/:280, my read; corpus cites :184/:279, drift), the store substitutes (`workspace.ts:351-353`, `gallery.ts:252` — my reads), the loader refuses to restore it (`if (as?.active_bases?.length)`, :53, my read) and force-prepends on first data (:109-114). The third click is honoured on screen and destroyed on save with zero diagnostic. Needs an owner ruling: admit `[]` in the contract, or stop minting it — the silent rewrite must not survive either way. → **F.W5–W8** (the R6-8 seam, cardinality axis).
- **M-10 = L-M5 / C-21 — MAJOR.** The basis-key domain is duplicated across the tree while its 7-LOC owner holds none of it: the `startsWith("fourier")` normaliser at **7 sites** (my grep — including `useWorkspaceLoader.ts:113`, missed by both original axes), the mode-label map re-authored ×4, and the gallery pair's entire 15-line `basisLabels` computed is a **verbatim clone** (my zero-output diff — LC-missed-6, folded here: the repair deletes two whole functions, not two expressions). `basisDisplay: Record<string,…>` keys 3 names against an emitted vocabulary of 4, so `basisDisplay["fourier-epicycles"]` type-checks and returns undefined — the `if (!cfg) continue` guards downstream are load-bearing workarounds. One repair unit: `BasisKey` union + `normalizeBasisKey()` + `basisModeLabel()` + `satisfies`, landing WITH the snapshot cure (cross-ref fr-BasisCanvas C-15 — same repair unit, one identity). → **F.W3/W4**.
- **M-11 = L-M7 / C-10 — MAJOR.** `pointer-events: none` as the disabled mechanism (`:307-310`): the reset stays focusable, keyboard-activatable, and announced ENABLED at 25% opacity, and the mechanism also suppresses the Tooltip that is the affordance's only explanation; the pinned Button ships `disabled` (d.ts verified) unused (`grep "disabled\|tabindex"` → 0 hits). **Composed severity KILLED (K-6)**: in precisely the is-default state, harmonics is already 50 — activation re-emits 50, the prop doesn't change, and `lastComputedKey` short-circuits (ContourSettings:143-146, my read). The a11y defect survives whole; the "one Tab+Enter from a 200→50 recompute" does not. → **F.W3/W4**.
- **M-12 = C-7 — MAJOR.** value.js is a declared dependency this component consumes ZERO of, while pulling `VIZ_COLORS` from the 117-line hand-rolled parser — and the whole JS round-trip is superfluous before it is wrong: both consumers (`:176`/`:203`) feed `color-mix(in srgb, var(--track-color) …)`, a context accepting any `<color>`; the correct idiom is in the sibling (`EditorControlsDock.vue:205/:208` — `var(--viz-fourier)` straight through, my read), immune to both the rename and the missing arm. LC's Receipt R-A (0.13.0 parses the exact strings `cssVarToHex` fails on) corroborates the F.W1 substrate with the parser-proof R1 caveat carried. → **F.W3/W4** (the one-line `var()` cure here; the colors.ts retirement identity rides fr-BasisCanvas D-1 at F.W1).
- **M-13 = C-18 — MAJOR.** The pill row hand-rolls a toggle glass-ui 4.0.0 ships (`./toggle-chip` + `./toggle-group`, export map + dist artefacts verified; 0 imports repo-wide): ~46 lines of manual `aria-pressed` + `[aria-pressed="true"]` retint + mobile-compact. Migration rider CONFIRMED by my producer read: `./toggle-chip` is GONE at 7.0.0, `./chip` present — target `./chip` under co-resident uplift. `ToggleGroup type="single"` also natively expresses the M-4 state model. → **F.W3/W4** with the F.W1 sequencing rider.
- **M-14 = LC-missed-3 — MAJOR (mechanism source-certain; end-to-end UNPROVEN-NEEDS-LIVE).** A Harmonics/Sample-Points nudge from this panel — the sole editor of both scalars (VV:265-267; ContourSettings binds but renders no editor for them) — silently discards a manually edited contour: the debounced watcher fires `runCompute()` → `store.extractContour()` POSTs the full settings; the server's cache lookup keys on `extraction_cache_key` (images.py:220-227, my read), which an editor-saved contour NEVER carries (`store_contour_asset(..., source="editor")` with no cache key — contours.py:24 + image_storage.py, my reads) — so the original extraction wins and replaces the edit, no toast, no guard. Attribution: defect body in ContourSettings/workspace/api provenance; the live trigger is this component's two editors. → **F.W5–W8** (contour provenance/CRUD union).

### MINOR

- **m-1 = L-M1 / C-12 — MINOR** (R-4). `selected` aliases the parent's reactive array (`:37`, no copy — vs `:39`'s `[...v]`); splice/push mutate parent state before the emit (`:108-115`). No reachable symptom at HEAD; VV's `{deep:true}` watcher does observe it. One-character cure. → **F.W3/W4**.
- **m-2 = D-M6 — MINOR** (R-6). Clicking Fourier reflows both neighbours: the label cycles 7/9/6 glyphs, `min-w-[5.5rem]` is a floor, `justify-center` re-centres — the comment's "uniform-width" is structurally impossible. Magnitude SS-13. → **F.W3/W4**.
- **m-3 = D-M8 — MINOR** (R-6; "unused" leg KILLED, K-8/R-3). `.cm-serif` is inert — `--font-serif-math` defined NOWHERE in either tree (utility body verified 4.0.0 + 7.0.0) — so the ℱ/Tₙ/Pₙ glyphs resolve to generic `serif` while six hand-tuned optical constants (`:235-248`/`:285-292`) substitute for the shipped `fourier-f` recipe five siblings already use. Note the register difference: `fourier-f` binds `--font-display`, not the serif-math face — adoption is a design decision, not a swap. → **F.W1** (token parity: define or retire `--font-serif-math`) + F.W3/W4 local.
- **m-4 = D-M9 — MINOR** (R-6). The mobile-compact block keys on viewport width (`:280`) while the producer keys comfort on pointer coarseness (`--ui-scale`/`--control-floor`, verified in the D-§6 self-kill); fourier retunes neither (grep clean). Orthogonal predicates fight; harm capped by the producer's coarse floor. → **F.W3/W4**.
- **m-5 = D-M11 — MINOR** (R-6). Three stacked bands per scalar where two suffice — the value floats in its own band under a header row whose right cell (`canReset`'s seat) is unconditionally empty; the chassis being bypassed (M-6) puts label and value on one baseline. Geometry magnitudes SS-13. → **F.W3/W4**.
- **m-6 = C-5 — MINOR** (R-1). The local 38-line Tooltip shim shadows a shipped primitive; both callsites pass `text` only (R3-7a's "BasisSelector 2" re-confirmed). Target CORRECTED: `./icon-tooltip` is a strict drop-in at the pin and DELETED at 7.0.0 — F.W3's 35-callsite split survives, its cheap arm re-targets against the 7.0.0 surface. → **F.W3/W4** with F.W1 sequencing rider.
- **m-7 = C-6-as-rescoped — MINOR** (422 scenario KILLED, K-3). The client's numeric window `[1,500]` is invented at the leaf, restated ~6× per control, and matches NONE of the server's **six** divergent windows (256 / 4096×3 / 200-unbounded / 200-le — my model reads; LC's six-count confirmed over C-6's five). The straddle-a-422 scenario is refuted from source (AnimationData is a server-computed embedded document; the client-reachable bound is 4096 ⊇ [1,500]). Constructive cure: generate client bounds from the operation models (the R6-8 dual). → **F.W5–W8**.
- **m-8 = C-8 / L-m1 — MINOR.** Dead `fourierModes` const (`:11`, 1 grep hit) — which is also the union the file should be typed against; no configured gate can see it (**13** compilerOptions — my count; C-8's "14" wrong — no `noUnusedLocals`, no eslint). → **F.W3/W4**.
- **m-9 = C-9 — MINOR.** `lucide-vue-next` runtime-imported (`:9`), declared ONLY in devDependencies (11 deps verified, no lucide) — and one of the 35 `@lucide/vue` migration sites. → **F.W1**.
- **m-10 = C-11 / D-m4 / L-m5 — MINOR.** `ConfiguratorRow.canReset`/`@reset` shipped and unused; the layer-level workaround cancels the Button recipe and is duplicated **byte-for-byte** across two consumers (both diffs zero-output, my runs) — meeting the producer's own ≥2-consumer threshold; the in-file justification (":121-122 no header-actions slot") is TRUE at the pin AND at 7.0.0. → **F.W3/W4** + **glass BH relay** (header-actions slot producer ask — standing relay law).
- **m-11 = C-13 — MINOR.** Motion literals (`0.15s`/`0.2s`, no easing) bypass the shipped `--duration-*`/`--ease-*` rungs consumed by four siblings; `0.15s` is not a rung. → **F.W3/W4**.
- **m-12 = C-14 / L-m3 — MINOR.** All three props optional though the sole mount binds them; the optionality manufactures the six fallback literals, four carrying B-1's wrong constant; the sibling declares the same scalars required. → **F.W3/W4** (lands inside B-1's repair).
- **m-13 = C-16 / L-m6 — MINOR.** The Slider payload is typed `number[] | undefined` (d.ts verified); both setters guard the element, never the array (`arr[0] ?? …`). Typed hazard, not a live crash (reka doesn't emit undefined on the drag path); shared idiom with SliderControl:55. → **F.W3/W4**.
- **m-14 = C-22 — MINOR.** Two raw `<input type="number">` shadow the pin's `NumberField` (export + dist verified; 0 imports) — the primitive-side root of M-8's both defects and the six lines of spinner de-chroming S-4 credits. Rendered chrome SS-13. → **F.W3/W4**.
- **m-15 = L-m4 / D-m6 / C-15B — MINOR.** The points input clamps to `[128,4096]` but never snaps to the slider's declared `step="128"` lattice; the next drag silently rewrites the typed value; server corrects nothing (bare ints). → **F.W3/W4** (dies with m-14).
- **m-16 = L-m7 / C-17 — MINOR.** Five redundant `key as string` casts over `Record<string,…>` — inert now, lossy the day M-10's key union lands. → **F.W3/W4**.
- **m-17 = L-m8 / D-m9 — MINOR.** `getBasisTooltip` leaks the raw machine key for any unmapped basis (`?? key`, `:70`); `basisTooltips` is a third parallel registry with no type linkage. → **F.W3/W4**.
- **m-18 = M-doc (merged comment-hazard row: D-i1 / L-m2 + DU-missed-7 + C-17a) — MINOR.** The subject's central provenance comment (`:249-257`) is false in three of four substantive mechanisms: (a) "uniform-width" — impossible (m-2); (b) `:where()` — does not exist as a selector (my grep: 1 hit, the comment itself; the override wins by RAISING specificity via the scope attribute, the opposite of the stated mechanism); (c) "h-9 px-3 defaults" — the variant's height is `h-(--control-h-sm)` and `px-3` is `sm`-only. TRUE claim (d): the 2px-over-1px border delta (cva verified). Plus `:315`'s stale "glass-scrubber" prose and `:103`'s "canvas handles it gracefully" scoping an end-to-end risk (M-9) to one consumer. Ruling the C-S3 superlative demanded: the comments made the audit cheaper AND mis-predict the cascade — **bounded credit, booked hazard**; any wave touching the file corrects all three. → **F.W3/W4**.
- **m-19 = DU-missed-5 — MINOR (adopted; d.ts + compiled-markup derivation).** The visible field labels are bare `<label>`s with no `for` and no wrapped control (ConfiguratorRow's OWN documented divergence: "No a11y for/id wiring"), so clicking "Harmonics"/"Sample Points" focuses nothing — the precise unstated cost of bypassing SliderControl's `<label>` wrapper (M-6). Accessible names are fine (S-2); the pointer-target association is absent. → **F.W3/W4**.
- **m-20 = DU-missed-6 — MINOR (re-proven).** An inactive pill's hover feedback is near-nil by construction: `color: var(--muted-foreground)` re-declared IDENTICALLY on rest and hover (`:262`/`:267`, my read), the border shifts 1.3→1.7:1 (both under the 3:1 floor, M-2), and the background goes AWAY (M-1). Nothing perceptible crossfades. → **F.W3/W4**.
- **m-21 = LC-missed-4 — MINOR (re-proven from my dist extraction).** `--slider-track-height` cannot be overridden from any Slider host: the library sets it via `.glass-slider[data-size=*][data-v-534634a7]` at (0,3,0) on the SliderRoot itself, a consumer's scoped class is (0,2,0), and no cascade-layer escape exists (`@layer` count 0). SliderControl's 16px (and GlassTimeline's 24 / ConvergenceTimeline's 20) stay inert even after B-2's rename — the third independent reason the corpus cure was unsound. → **F.W1** (producer seam: relay whether track-height is meant to be consumer-tunable; glass BH).
- **m-22 = D-m band (all re-verified in the subject read or adopted from double-confirmed compiled-markup cells) — MINOR.** (a) D-m1 `sub="basis & resolution"` — prose in a documented token-reference slot, systemic with the sibling → producer ask rider. (b) D-m2 `name="N"` on one row, none on the other. (c) D-m3 `description` unused — the 1–500 / 128–4096 ranges are undiscoverable. (d) D-m5 + LC-missed-5: duplicate accessible names per row (input + Slider both "Harmonics"/"Sample Points"), the reset's tooltip text ≡ its aria-label, AND each Slider emits its name twice (root fallthrough + explicit thumb forward — the chunk's own `$attrs` line). (e) D-m7 redundant `<div class="w-full">` ×2. (f) D-m8 `h-3.5 w-3.5` icon silently overridden by the `[&_svg:not([class*=size-])]:size-(--ui-glyph)` descendant rule — misses the `size-` escape hatch. (g) D-m10 tooltip register drift (one imperative, two noun phrases). (h) D-m11 `@click.stop` cargo (sibling trigger). (i) D-m12 no busy affordance while `hasData` includes `store.computing` — 7.0.0 `loading` is the seat. (j) D-m13 the off-state has no panel-side affordance (pairs with M-9 and fr-BasisCanvas M-β7). (k) D-m14 `type` undefined → native submit default, latent (no ancestor form), cured at 7.0.0 Button.vue:95. (l) D-m15 the range/default constants restated across ~6 sites — B-1 is what that drift looks like. → **F.W3/W4** (each dies inside the M-6/M-8/M-13/B-1 repairs; none needs its own wave item).

### INFO

- **i-1 = L-INFO-2 — INFO (census correction UPHELD).** The sliders are GLOBAL contour parameters (VV binds the same two refs to BasisSelector and ContourSettings — my read of :260/:265-267/:270); lane-frontend.md:87's "per-basis term sliders" is wrong. → **NO-WAVE-OWNER** (SS-3/SS-4 census input).
- **i-2 = L-INFO-1 — INFO.** The R5-7/R6-5 census family (`NATIVE_TEMPLATE_LOOP`) is loop-scoped and blind to native NON-LOOP controls: this file's two primary editors are native `<input>`s outside any loop — a 2-of-4 undercount of the panel's editable surface, one level down from where R6 cured the class. Component-side facts confirmed; the deriver itself is out-of-tree (UNPROVEN cell honest in both readers). Cross-ref fr-BasisCanvas L§R5-7 (same family, different mechanism). → **NO-WAVE-OWNER** (SS-3/SS-4 census methodology).
- **i-3 = LC-missed-7 — INFO (re-proven).** `noUncheckedIndexedAccess` is absent from the 13 compilerOptions (my read) — THAT flag, not the loose `Record` alone, is what lets `basisDisplay["fourier-epicycles"]` type-check non-optional and keeps M-10's two-domain key space invisible to `vue-tsc`. → **F.W0** (substrate: gate-flag decision) rider on M-10.
- **i-4 = DU-missed-8 — INFO.** This file is the cleanest single-file proof that the census break-surface model measures the wrong dimension: all three imported subpaths SURVIVE 7.0.0, yet the file breaks on six prop values + the lucide specifier — zero subpath breaks, six prop breaks. Rides M-7. → **F.W1**.
- **i-5 = D-§6 — INFO (epistemic record; all three self-kills verified by both readers, spot-confirmed by this seat).** (1) PRM: the producer's blanket a11y-overrides gate covers the scoped transitions — the component correctly inherits. (2) Tap targets: coarse-pointer floors cover the icon button and pills. (3) The optional-prop "loading fabrication" is a dead path behind the parent's `v-if="hasData"`. Null results recorded at full rigor — retained so no future audit re-litigates them.

## Killed-claims register

- **K-1 · The B-2 rename cure ("cheapest blocker in the set; identical pre- and post-uplift; rename to `--slider-range-bg`/`--slider-thumb-bg`") — KILLED** (both readers; ratified + extended by this seat's own reads). (a) 4.0.0: `--slider-thumb-bg` is read by exactly one rule, `[data-variant=spectrum] .slider-thumb…`; the `standard` thumb is `width:0; opacity:0; background:0 0` — my dist extraction. (b) 7.0.0: the token DOES NOT EXIST (my 6-name census of `src/components/slider/`). (c) The cited `slider/Slider.vue:337-338, 522` is fabricated against a 367-line file containing zero `--slider-` tokens (they live in styles.css). Only 1 of the subject's 4 declarations survives a rename; 8 of the fleet's 23 have no receiver under any rename at either version. The corrected cure lives in B-2.
- **K-2 · C-1's "Audited state" leg ("at HEAD the un-renamed variant would fall through to reka's unstyled default — a different break") — KILLED** (both readers, unanimous cva extraction: both variant arms map to the empty string; all paint is unconditional base + descendant classes; HEAD and worktree render identically but for the `data-variant` literal). Adopted as twice-independently-extracted.
- **K-3 · C-6's failure scenario (client-permitted `n_harmonics ∈ (256,500]` → Pydantic 422 via `AnimationData`) — KILLED** (reader-DU as refuted-not-unproven, reader-LC concurring; ratified by my model reads: `AnimationData` is referenced once, as the server-computed `Visualization.animation_data`; `VisualizationCreate` is `extra="forbid"` with no such member; the client-reachable bound is `le=4096` ⊇ [1,500]; the web tree's sole `animation_data` hit is a comment). The six-window enumeration survives as m-7.
- **K-4 · L-B1's component-local consequence ("both tracks tint from `#888888`… visually identical greys") and L-SUP-3's "the projection itself is correct and will simply start working the day the parser does" — KILLED** (reader-LC's own kill against the L axis; ratified). The projection targets the dead `--slider-scrub-*` vocabulary at BOTH versions; nothing reads it; the tracks paint `--primary`; `#888888` never reaches a pixel in this component. The L axis — the LIBRARY lens — missed the dead token family both sibling axes proved, then built a superlative on the unsound premise. Canvas half folds to fr-BasisCanvas (see FOLDS).
- **K-5 · L-B2's recompute-cost leg ("a complete re-extraction of the contour") — PARTIALLY KILLED** (reader-LC; ratified by my read of image_storage.py:248-266 — `extraction_cache_key` hashes nine fields and deliberately omits `n_harmonics`, so images.py:220-227 takes the cache-hit branch). A harmonics-only reset costs one redundant round-trip + a touch write + the epicycles/bases recompute — not a CV re-extraction. (An `n_points` change genuinely re-extracts — `n_points` IS in the key.) B-1's destruction claim unaffected.
- **K-6 · L-M7/C-10's composed severity ("the 'already at defaults' state is one Tab + one Enter from a 200→50 recompute") — KILLED** (reader-DU; ratified by my reads of `:79-84` and ContourSettings' `lastComputedKey` guard). `.is-default` requires `nHarmonics === 50`; activation re-emits 50; no prop change; the guard short-circuits. The a11y defect survives whole (M-11); the destructive composition does not — the dangerous pristine-200 state has the button fully live and honestly enabled, which is B-1's row, not this one's.
- **K-7 · D-B3's contrast table (token-resolved inputs; dark column all-passing) — KILLED as arithmetic, verdict sustained and worsened** (both readers; settled by this seat's own instrument). `--pill-color` receives the frozen literals, never the tokens; recomputed at the values that paint: light 4.24/4.04/4.48, dark 3.50/3.70/3.33 — the corpus reported one failure in one theme; the truth is six failures in two, worst where the corpus reported the all-clear. The instrument's method was sound (the corpus's own 3.84 reproduces from its stated inputs); the input selection was not.
- **K-8 · D-M8's "`@utility fourier-f` … unused" leg — KILLED** (both readers; count settled by this seat per R-3: **five** live sites across four files). The utility is an established idiom; the sharpened finding (BasisSelector as the sole hand-tuning outlier) survives in m-3.
- **K-9 · D-i1's printed receipt (`grep -c ":where(" → 0`) — KILLED** (reader-DU; ratified: my grep returns **1** — line 255, inside the comment). The finding is correct (no `:where()` SELECTOR exists); the receipt as printed is false, and the L axis had already stated it correctly. Two axes, one command, two different reported outputs — a receipt-integrity lesson.
- **K-10 · C-4's falsifier cell ("`grep -rn basisDisplay` → the declaration plus the single v-for consumer") — KILLED** (reader-DU; ratified: my grep returns **8 files**). The core claim survives BROADER than filed — six consumers are frozen at the light literals. NOTE: DU's kill-cell says "SEVEN files" while listing eight — see DISSENT.
- **K-11 · L-M3's present-tense consequence ("different track heights and different fill opacities" visible in the panel today) — KILLED** (reader-LC; ratified via B-2: every one of the 23 declarations is dead, including SliderControl's five, so all seven aside sliders render the identical stock capsule). The source-level drift is real and books inside M-6.
- **K-12 · C-5's migration sequencing (the `./icon-tooltip` one-word swap as the F.W3 cheap arm) — KILLED** (reader-LC; ratified by my 7.0.0 export-map read: the subpath is deleted; `./tooltip` survives). Reader-DU's confirmation of the amendment is overruled on this leg (R-1). The split-the-35-on-the-text-only-predicate idea survives with a re-derived target.
- **K-13 · Arithmetic/provenance band — KILLED as stated, none load-bearing** (readers + this seat). "20 dead declarations" → **23**; ContourSettings' SliderControl count "six"/"four" → **5**; the ToggleChip docblock quotation is a composite of two files' docblocks; "14 compilerOptions" → **13**; C-20's `:184/:279` → **:186/:280**; D-m14's 7.0.0 cite `:55` → **:95**; the normaliser census "six sites"/"five downstream" → **7 files** (both original axes missed useWorkspaceLoader).

## Superlatives — with provenance (L-18 runs both ways)

1. **PROVENANCE OF THE YEAR — C-1's git-anchored mechanism reconstruction** (challenge-C §1). It predicted the uncommitted sweep's exact reach — 3-of-4 literal-string occurrences updated, 0-of-4 token declarations — and this seat reproduced the diff byte-for-byte at HEAD `cd26c653`. It converts a residue into a mechanism ("the sweep was a string substitution over the variant name; the token contract was never in the search") and yields the reusable F.W1 acceptance gate (token-family sweeps, not variant-string sweeps). Both readers independently crowned it; ratified.
2. **The `aria-label` discipline is load-bearing, not decorative** (D-S2/L-SUP-2/C-S1, all three axes + both readers). Every interactive control carries an explicit name although the row chassis documents "No a11y for/id wiring", and the Slider's thumb-forward (`$attrs["aria-label"]`) is right about an undocumented contract; e2e settings-persistence reaches the field by `getByRole("spinbutton", { name: "Harmonics" })`. **Preserve verbatim through any F.W3/W4 rework.**
3. **Emit-boundary clamping in front of a server that validates nothing** (C-S8/L-SUP-2 fold; server side verified by this seat: shared.py/computation.py declare bare ints, no `Field`, no bounds). Four exits, four clamps, four NaN fallbacks; the slider path and keyboard path trust each other not at all. This component is genuinely the only guard between a hand-typed `-9e9` and the FFT call. The window is wrong (m-7); the enforcement shape is right and must survive the generated-bounds cure.
4. **Three genuinely different readings of one 19-line block** (the reset recipe): D files it as duplication owed to a producer gap, L as an a11y mechanism defect, C as the producer's own ≥2-consumer threshold met — all three true, none a restatement, the duplication byte-verified by this seat's zero-output diffs. Three-axis audits usually triplicate; this one triangulated.
5. **Null results recorded at full rigor** (D §6's three self-kills + the corpus's honest UNPROVEN tags). Killing a defensible finding costs more than filing it; this corpus did it three times unprompted, and both readers verified all three against the pinned dist.
6. **Zero teardown surface where it concretely matters** (L-SUP-1): no imperative resource in 324 lines; the one `watch` is setup-scoped; the component mounts/unmounts on every compute cycle behind `<Transition>` + `v-if="hasData"`. Contrast App.vue's never-disconnected MutationObserver.
7. **Authored, not inherited, information hierarchy** (C-S6): `:default-open="true"` here vs the sibling's explicit `false` — the shipped default is `true`, so the divergence required authorship.
8. **The scalar↔array bridge as the file's own counter-example** (C-S2): paired writable computeds clamping at the single write point, three lines above the mirror-plus-watcher idiom that produced m-1. The right answer and the wrong answer are adjacent — the repair has an in-file template.
9. **Reader-LC's Receipt R-A** (installed value.js 0.13.0 parses `oklch()`/`light-dark(oklch,oklch)`/`hsl()`/hex — the exact strings `cssVarToHex` fails on), with the honestly-bounded conversion-leg caveat citing the parser-proof gate's R1. The F.W1 substrate decision inherits a measured input, not an argument.
   **Rescission ruling (the one DU demanded)**: C-S3 ("exemplary provenance discipline… auditing this file was faster because of those comments") is **RESCINDED-IN-PART** — bounded credit stands (the `:315-317` comment is literally what made B-2 findable; the honest-intent half is real), but a comment block that is false in three of four mechanisms (m-18) is a hazard wearing an asset's costume, and the falsifier C-S3 itself attached ("dies if the comments are wrong") fires on this seat's own reads.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. `getComputedStyle(document.documentElement).getPropertyValue('--viz-fourier')` returning the unresolved token stream (spec-derived; no `@property` registration for `--viz-*` — strengthened by reader-LC's registration census). Immaterial to this component's verdicts (the local consequence is dead-token-gated); it is the 30-second confirmation for the fr-BasisCanvas-folded chain.
2. The true backdrop behind the pills: every ratio in this record (including this seat's) composites over `--background`; the pills sit on a glass surface. Direction robust in both themes; the third digit is not.
3. Whether an un-fixme'd axe run REPORTS the pill contrast (axe returns `incomplete` on unresolvable translucent backdrops) — the number is token-decidable; the gate's verdict is not.
4. m-2's reflow magnitude in device px; m-4's wrap threshold — both directions proven by construction, magnitudes live-only.
5. m-3's glyph-coverage half: whether generic `serif` covers U+2131 / U+2099 and shifts the metrics the six constants were tuned against.
6. m-14: whether `NumberFieldContent`+`NumberFieldInput` compose chrome-free into the 2.75rem right-aligned register at 4.0.0.
7. M-7's Slider migration depth: whether `standard`→`scrubber` is a one-word swap or a re-tune (7.0.0 styles.css restructures the size axis's track heights) — needs the F.W1 uplift branch, not a browser.
8. M-14's end-to-end execution (edit contour → nudge Harmonics → edit replaced): every link source-certain, the sequence unexecuted.
9. M-8(b)'s felt behaviour (the DOM retaining "999" against model 500): mechanism certain from Vue's render-gated patching; the retention unwitnessed.
10. m-5's band geometry (~88px/scalar) and m-22(a)'s `sub` truncation at the 320px aside — compiled-markup-derived, unmeasured.

## DISSENT — preserved, not ruled away

1. **Corrections inherit the original's failure mode — twice, once per reader.** Reader-DU's K-10 kill-cell corrects C-4's file count and says "SEVEN files" while listing eight (my grep: 8). Reader-LC's K-8 kill corrects D-M8's "unused" with "three live sites" (my grep: 5). Both kills stand on their merits; both counting-corrections miscounted. Fleet lesson: a correction cell needs the same receipt discipline as an accusation cell — the incredulity seat exists because arithmetic does not become true by being adversarial.
2. **Reader-LC's two-fixme confirmation** (R-2): it confirmed D-B3's stale-fixme leg as filed, citing the justification block, without re-counting the fixmes in the same file (three, plus the crud fourth — reader-DU's re-count was right). Same failure shape as the fr-BasisCanvas R-2/K-12 events: extending a true neighbouring fact past what was read. LC's cells beside it (the contrast recompute, the cache-key derivation, the track-height specificity find) were genuinely excellent original work.
3. **Reader-DU's severity dissents preserved** (R-4/R-6): aliasing, reflow, cm-serif, mobile-axis, three-bands at MAJOR on the argument that a flagship-panel control that moves under the pointer, an invisible hover, and a mis-scaled mobile register are user-visible incoherences on the product's primary control surface. If SS-13's live pass shows any prominently visible in ordinary use, its severity re-opens.
4. **Both readers' EasingPicker citation drift**: the doctrine file is `src/components/visualization/EasingPicker.vue` (comment :12, role :24), not `equation/EasingPicker.vue`. Doctrine confirmed verbatim; anchors corrected here.
5. **This seat's own scope notes**: the cva `outline:` string, the configurator `inert` emission, the slider `aria-label` thumb-forward, and the `canReset` d.ts cells were verified by targeted grep against dist, not by whole-chunk re-extraction; the compiled ConfiguratorRow band geometry (m-5, m-22) and the `@click.stop` sibling-trigger claim (m-22h) are adopted from the readers' twice-agreed extraction. `workspace.ts`/`gallery.ts` substitution sites were read at the cited lines, not whole-file. K-2's cva empty-string extraction is adopted as twice-independent, not this seat's own run.

## Closing verdict

**ADJUDICATED — the wrong-default / dead-retint / frozen-palette-contrast BLOCKER triple stands (B-1 · B-2 · B-3), with the contrast row corrected to six failing ratios across both themes by this seat's own instrument; 44 register rows confirmed (3 BLOCKER · 14 MAJOR · 22 MINOR · 5 INFO) covering 68-of-72 corpus rows; 13 kills banked — including the corpus's own prescribed cure for its cheapest blocker, killed three independent ways; 15 missed finds adopted after re-proof; 2 identities FOLDED BY REFERENCE to fr-BasisCanvas (the `#888888` resolver and the `basisDisplay` snapshot — one booking each, forever); routings are M-25 agglomeration input for the forming X·F specs — F.W1 owns the prop-VALUE break-surface amendment + the token-family sweep gate + typography/package parity, F.W3/W4 owns the defaults/a11y/contrast/chassis/shadow cluster, F.W5–W8 owns the off-state contract + generated numeric bounds + contour-provenance loss, and the census-methodology rows go to SS-3/SS-4 with NO-WAVE-OWNER; nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
