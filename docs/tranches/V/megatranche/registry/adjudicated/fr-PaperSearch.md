claude-fable-5

# fr-PaperSearch — adjudicated apotheosis r3 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ `cd26c65`, branch `m/w1-bump-migration`; **the audited pin is the WORKING TREE**, which now carries a wider uncommitted cohort than r2 recorded (search-family delta vs HEAD, this seat's own `git diff`: `PaperSearchDropdown.vue:39` `glass-elevated → glass-floating` + 6L `MobileFloatingToc.vue` + 2L `PaperView.vue`; `PaperSearch.vue` itself CLEAN; installed glass-ui **4.0.0** re-verified from `web/node_modules`). Every reader cite this seat checked matches the working tree. · **Registry tree**: value.js `tranche-u`.
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-24 **r2** apotheosis at this same path, which adjudicated a different reader corpus (r2's reader-α/reader-β). The r2 roster identities (D-* · L-* · C-* merges, **FR-PS-NAV**, **FR-PS-FTS**, **MISS-A1..A7**, **MISS-B3..B7**, kills **K1..K16**) are preserved for life (anti-rename); r2 rulings are carried where this corpus does not touch them and are **corrected in exactly two places**, both by fresh attack on the producer sheet r2 reasoned past: (i) the **D-26 roster row is RETIRED by kill** (K18 — the pill-ring repair hazard does not exist; cascade layers), and (ii) the **D-21/D-22 row loses its PRM limb** (K17 — r2's own cell "the gap is precisely the component's own rules" is false against the universal `*:not([data-allow-motion])` reset; D-22 survives alone). Where r2 content is restated it was re-verified, not copied, except where marked *(r2-carried)*.
**Corpus adjudicated (r3)**: two hostile re-reader returns over the component's three challenge files — reader-γ (design-and-usage seat, slug `PaperSearch`: 13 confirmation cells · 9 kills · 3 misses · 6 superlatives · 4 unproven) and reader-δ (library-and-code seat, slug `fr-PaperSearch`: 26 confirmation cells · 10 kills · 4 misses · 6 superlatives · 5 unproven). Challenge files spot-read by this seat at every ruled row (D-21 :354-369 · D-26 :443-453 · D-23 :399-417 · C-B2 :94-116 · L-20 :425-436) — the readers' representations of the corpus are accurate everywhere I checked.
**Method (incredulity)**: neither reader was adopted before being attacked. The one head-on verdict conflict (D-26: γ kills, δ confirms, r2 banked it) and the one banked-row conflict (D-21: γ kills against r2's own surviving cell, δ's batch re-confirms it) were both re-derived at the installed producer bytes with this seat's own instruments; every NEW missed row was re-verified operand-by-operand at source before admission; one row was sharpened by this seat's own git archaeology neither reader ran. Kills that merely fold to banked K1/K4/K5/K7/K9/K10/K13/K16 were spot-confirmed, not re-litigated.
**This seat's own instruments (all read-only, zero browser probes — probe parsimony)**: node brace-match over installed `dist/styles/utilities/base.css` (**one** `@layer` block spanning 43→541 of 542; `.focus-ring:focus-visible` at :174-178 INSIDE it — the D-26 decider; `.interactive-item` :190-200 + `:focus-visible` :205-207 read verbatim); `dist/styles/utilities/a11y-overrides.css:1-31` whole (the universal PRM reset — the D-21 decider) + the import chain re-walked (`dist/styles/index.css:165` → `utilities.css:19`; exports `./styles` → `./dist/styles/index.css`; fourier `style.css:3`); `dist/styles/glass/ladder.css` `.glass-resting` (`position:relative` + `backdrop-filter: var(--glass-blur-resting)`) and `.glass-floating` (`position:relative`, backdrop-filter, no offset) read verbatim; `tokens/glass.css:45,:76` (`--glass-blur-resting: blur(calc(10px * var(--glass-level))) saturate(1.05)` — non-`none` for every level incl. 0); `tokens/scheme-motion.css:333-348` z ladder verbatim (`--z-controls: 20 · --z-bar: 30 · --z-header: 35 · --z-dock: 40 · --z-panel: 45 · --z-overlay: 50 · --z-popover: 130 · --z-modal: 140`); `dist/components/custom/search/FuzzySearch.vue.d.ts` whole (props `{ state; variant?: "sidebar"|"floating"; placeholder?; typeLabel? }` + exposed `focus()` — the D-23-limb decider) + `find dist -iname '*searchVariants*'` → empty; consumer whole/section reads: `usePaperSearch.ts` (whole, 109), `PaperSearchDropdown.vue` (whole, 70 — the backdrop v-if `variant==='floating' && isOpen && !isExpanded && length>0` at :66, DOM order plate:35 → backdrop:65, `@click="search.close()"` :68), `PaperSearch.vue` :20-60/:90-132/:186-210 (canExpand :28 = `!!query && length>0`; `.paper-search-results` z-bar :108; `.paper-search-result` radius `calc(var(--radius) - 4px)`; `.paper-search-action-btn` radius 3px; `.paper-search-backdrop { position:fixed; inset:0; z-index:var(--z-bar) }` :192-196), `MobileFloatingToc.vue` :88-115/:190-222/:258-265 (`closeMobileSearch()` routes through `search.close()`; the `glass-resting` host bar behind `v-if="searchActive"`; `.floating-toc { sticky; z-index:var(--z-controls); height:0 }`; `.floating-toc-bar { relative; z-index:2 }`; the `> :first-child { flex:1; min-width:0 }` reach), `PaperSidebar.vue` :48-54/:130-168 (PaperSearch first child of `.sidebar-nav`; `overflow-y:auto` + both overscroll containments + the foreground-token border/shadow idiom), `searchHelpers.ts:1-30` (TYPE_LABELS 13 entries verbatim; `resultLabel` unbounded-label arm), `paperSearchIndex.ts:196-241` (cache identity return :210-211, trimmed keys :203, prefix probe :221-227, `_cache.set` of the returned array :239-241), `App.vue:24`, `style.css:108-143` (the viz-amber arithmetic block + the D.W4.d scope-hash hoist — both superlative deciders); **git archaeology**: `ffba307^:PaperSearch.vue` grep — pre-split DOM order input-wrap :126 → results :162 → backdrop :189 → Teleport :194, backdrop rule :429 (the FR-PS-BDT provenance decider); banked-registry identity sweeps: `grep -rn 'containing block'` over fr-PaperSearch/fr-PaperSearchModal/fr-PaperSidebar/fr-MobileFloatingToc → **0** (only fr-App.md MG-ε :78 banks the class); fr-PaperSearchModal.md K-1/R-1 (the banked PRM kill, same bytes + the SHIPPED-artifact extraction), PSM-17, PSM-24/R-3, PSM-32, K-5, K-7, SS-13 #7; fr-MobileFloatingToc.md M-12 (the unlayered-beats-layered inversion live in THIS consumer) + m-12 (`--z-controls` row).
**Environment disclosure**: every ruling below was decidable from source + installed producers + node + git; no browser was opened. The readers' WCAG recomputations (D-14 1.8838, D-15 four cells, D-19 ratios) reproduce r2's banked cells to 2 d.p. and were adopted as corroboration, not re-run a fourth time.

## Scoreboard

**54 adjudicated roster rows — 4 BLOCKER · 22 MAJOR · 20 MINOR · 8 INFO** (r2's 49 identities preserved; one row RETIRED by kill; one row's limb killed; **6 new rows admitted** from this corpus's misses). **20 killed-claims register entries** — K1..K16 carried, **K17..K20 new**, including two kills that **overturn banked r2 rows** (K17, K18) and one that **overturns reader-δ's re-confirmation of a banked row** (K18 — the registry-inheritance event, recorded). **6 missed rows ADMITTED** (γ 3 · δ 4; one row is a two-limb merge — the readers found DIFFERENT limbs of the same armed cure; otherwise the missed sets are again disjoint). **11 superlatives** (r2's 8 sustained + 3 new). **5 adjudicator corrections.**

The r3's headline is not a new defect in the component — it is that **the component's prescribed cures are armed**: both variants' repair paths fail structurally (FR-PS-BDT: the revived mobile backdrop becomes a one-tap trap that dismisses on every tap including result selection; FR-PS-CLIP: the revived sidebar dropdown stays clipped inside the ToC scroller), and the repair-hazard bank simultaneously LOSES its most-cited member (D-26 — the pill-ring hazard never existed; cascade layers). Two banked r2 cells fell under fresh attack, both because an earlier seat reasoned about a producer sheet instead of opening it.

## Ruled disagreements and corrections (the incredulity seat's actual work)

| # | row | reader-γ | reader-δ | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **D-26** (focus ring forces pill geometry on repaired rows) | KILLED (cascade layers + `.interactive-item` opt-out) | CONFIRMED MINOR-LATENT (restating banked K6) | **KILLED — γ sustained; δ AND the banked r2 row overturned (K18)** | My brace-match of the installed, actually-imported sheet: `dist/styles/utilities/base.css` opens exactly ONE `@layer` block spanning lines 43→541, and `.focus-ring:focus-visible { outline:none; border-radius: var(--radius-pill); box-shadow: var(--focus-ring-shadow) }` sits at :174-178 — INSIDE it. Vue SFC `<style scoped>` output is UNLAYERED (`@reference "tailwindcss"` at `PaperSearch.vue:42` emits nothing). Per Cascade 5, unlayered author normal declarations beat ALL layered ones regardless of specificity or `:focus-visible` state — so revived `.paper-search-result[data-v-…] { border-radius: calc(var(--radius) - 4px) }` and `.paper-search-action-btn { border-radius: 3px }` WIN while focused, and the ring's box-shadow follows the element's 6px/3px curvature. The pill never appears. Independent second limb: `.interactive-item:focus-visible` (:205-207) ships `outline:none; box-shadow: var(--focus-ring-shadow)` with NO radius override — the "no opt-out" cell is false too. Mechanism folds to banked fr-MobileFloatingToc **M-12** (the same inversion live in this consumer) and is CONSISTENT with its K-7 (row keeps corners, ring follows them, via source order in the shipped build). δ's confirmation inherited the banked K6 register cell without attacking the layering — **citation-inheritance from the registry itself**, a new costume for the ColorSpaceSelector DISSENT-1 event. r2's K6 was right as far as it went (LIVE→LATENT) and under-attacked. **Residue = a BANK re-trigger, not a row**: if a future glass major un-layers `base.css`, the hazard revives — `node -e "…"` grep the `@layer` span at every glass uplift. |
| 2 | **D-21** (PRM guards missing on the component's six transitions) | KILLED at the producer sheet (universal reset; transform struck) | CONFIRMED inside the hygiene batch ("six transitions, zero `prefers-reduced-motion` guards") | **KILLED — γ sustained; δ's batch cell AND r2's cell overturned (K17)** | `a11y-overrides.css:6-17`, read whole by this seat: `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { animation-duration:.01ms !important; transition-duration:.1s !important; transition-property: opacity,color,background-color,border-color,box-shadow !important } }` — `transform` is globally struck from the transitioned set, and even `[data-allow-motion]` snaps (:24-31, "accessibility is absolute"). Reachability re-walked: `style.css:3` → `./styles` → `index.css:165` → `utilities.css:19`. Both scale/translate modal legs and the dropdown's `translateY(-4px)` can never tween under PRM; the ONE live transition (`:55` border-color) is in the preserved list by design. FOLDS to banked fr-PaperSearchModal **K-1/R-1** (same bytes, same chain, plus the shipped-artifact byte-extraction) with residue **PSM-32** (the one-frame enter-from offset snap — stays banked in the Modal registry). **r2's :71 cell** ("glass supplies 47 upstream guards for INHERITED motion — the gap is precisely the component's own rules") **is corrected**: the `*` selector reaches the component's own rules; there is no gap. δ's batch cell repeated the challenge row against a kill banked one registry file away. The row survives as **D-22 alone** (MINOR): nine raw durations beside two "migration done" annotations — a vocabulary defect, not an a11y one. |
| 3 | **C-N3 first limb** ("the expand affordance appears while `isOpen` is false — a control for a panel that is not showing") | (not addressed) | KILLED (unreachable state) | **KILL ADMITTED (K20) — a limb r2 left unruled** | Verified whole-file by this seat: `close()` (`usePaperSearch.ts:48-53`) is the tree's ONLY writer of `isOpen=false` and clears `query` in the same block; Escape-while-expanded lowers only `isExpanded` (:85-86); `open()`/`@focus` only raise; `closeMobileSearch()` routes through the same `close()` (`MobileFloatingToc.vue:93-96`, re-read). `canExpand` (`PaperSearch.vue:28`) requires `!!search.query.value` — so `query !== "" ∧ isOpen === false` is unreachable and the "control for a hidden panel" state cannot exist. The r2 row **D-29/C-N3 narrows to its phase-skew limb alone** (undebounced `query` conjoined with debounced `results`), which is confirmed and unaffected. |
| 4 | **D-23 "does not exist upstream" limb** ("the wanted list-row variant does not exist upstream and was re-implemented locally") | KILLED (the falsifier is answerable) | (adjacent kill of L-20's gating limb, concordant) | **KILL ADMITTED (K19) — extends banked K16/MISS-B3 with a sharper truth** | Three answers to the challenge's own falsifier, all verified at the installed 4.0.0: (1) `.interactive-item` (`base.css:190-207`) IS the full-width left-aligned non-pill row recipe — `border-radius: var(--radius-lg)`, tokenised five-leg transition, accent-mix hover, radius-preserving focus ring; (2) `./command` ships `CommandItem`; (3) decisively, `./search` ships **`FuzzySearch.vue`** — the SAME `variant: "sidebar"|"floating"` union, the SAME `focus()` expose, the same 120 ms/30 defaults: the 1:1 twin of `PaperSearch.vue` that **no axis and neither r2 reader ever named**. Provenance corrections adopted: `searchVariants.ts` does NOT exist at 4.0.0 (my find → empty). The D-23 row's surviving statement sharpens to: **"glass ships the primitive; the component picked a pill Button instead"** — a different defect with a named cure. MISS-B3's decision row gains the FuzzySearch cell. |
| 5 | **The armed backdrop cure** (γ-miss-1: containing block; δ-miss-1: containing block + tap trap) | MISSED row (MAJOR) | MISSED row (MAJOR, superset) | **ADMITTED as ONE row, FR-PS-BDT — δ's superset adopted, plus this seat's archaeology** | Every operand verified at the bytes (see instruments): the backdrop renders only while the plate shows, as a LATER sibling, both at `--z-bar`; the floating host bar carries `glass-resting` whose `backdrop-filter` (non-`none` at every `--glass-level` incl. 0) makes it the containing block for fixed descendants (Filter Effects L2). This seat's own dig: at `ffba307^` the pre-split monolith ALREADY ordered plate :162 → backdrop :189 with both rungs at `--z-bar` — **the front/back authoring error predates the split**; the naive D-1 repair would REVIVE an original defect, not mint a new one. Mechanism classes fold to banked fr-App **MG-ε** (backdrop-filter containing block) and kf-SharePopover **D·B-2(c)** / kf-EditorShell **S-2** (equal-z DOM-order); the instance is unbanked in this family (grep → 0). |
| 6 | **D-24 cross-registry tension** (both readers kill, folding to banked K1 — δ adds the countervailing `@source "../*.js"` fact) | folds to K1 | folds to K1, empirical caveat carried | **K1 SUSTAINED as carried-empirical; a registry-coherence rider BOOKED** | The kill rests on the emission fact (rule pre-compiled nowhere; r2 reader-α's fresh build saw 0 emissions) — but δ is right that glass's own `dist/styles/index.css:222` `@source "../*.js"` places the CVA string inside any consumer's Tailwind scan path, so the decider is the extractor's tokenisation of one nested-bracket candidate: **build-empirical, not structural**. And the fleet's registries currently DISAGREE: fr-PaperSearchModal **PSM-24/R-3** books the same guard LATENT-on-next-build ("becomes live on the first 4.0.0 build") while this registry's K1 kills it on emission. Both cannot survive the first real 4.0.0 build. **F.W0 rider**: when MISS-A7's build unblock lands, run the build, grep the emitted CSS for `class*=size-`, and correct whichever banked cell lost — booked here so the contradiction cannot outlive the evidence. |

**Adjudicator corrections register (5).** (1) δ's D-26 re-confirmation — overturned; it inherited the banked K6 cell instead of attacking it (ruling 1): **banked rows are citable, not load-bearing, in a hostile re-read**. (2) δ's batch D-21 cell — overturned against a kill already banked in the sibling registry it cites elsewhere (ruling 2). (3) **r2's own registry fell in two places** (D-26 row; D-21 cell) — both times because a seat reasoned about producer CSS without opening/brace-matching the sheet; recorded as fleet evidence beside fr-PaperSearchModal K-1's identical lesson. (4) γ under-escalated its own D-21 find — it filed the r2:71 inconsistency as a fleet "rider" instead of demanding the registry correction; the incredulity seat exists to make that correction. (5) The readers' missed sets are again near-disjoint (3+4 with one two-limb overlap) — third consecutive corpus confirming the r2 correction-4 lesson that single hostile readers under-cover.

## Roster delta r2→r3

Dispositions are **ADJUDICATED, not VERIFIED** (M-25); routes name the forming X·F wave surface. Line anchors are working-tree; re-resolve before any cure.

### New rows admitted (6)

- **FR-PS-BDT — MAJOR, LATENT-ARMED (repair hazard; merged γ-miss-1 + δ-miss-1, superset adopted; ruling 5).** The D-6/L-1¶3 cure is armed to fail AND to trap. Reviving `.paper-search-backdrop { position: fixed; inset: 0; z-index: var(--z-bar) }` (`PaperSearch.vue:192-196`) on the floating variant: (a) the host bar (`MobileFloatingToc.vue:110`, `glass-resting`) has a non-`none` `backdrop-filter`, so it is the containing block for fixed descendants — `inset:0` resolves against the BAR's padding box, never the viewport → outside-tap-to-dismiss stays broken after the repair; (b) inside the bar the backdrop covers everything: later DOM sibling at z-30 over the input-wrap (z auto) AND over the plate (z-30, earlier sibling — equal z, tree order) → every tap on the input, Clear, Expand, or ANY RESULT ROW lands on `@click="search.close()"` — result selection becomes impossible and every bar tap is a destructive dismiss. This seat's archaeology: the equal-z/backdrop-last order exists at `ffba307^` (:126/:162/:189) — the authored design carried the trap before the split killed it. Cure: backdrop teleports to body (or the bar's filter policy changes) AND the front/back pair gets distinct rungs. **Route F.W3/W4 as a GATE on the D-1 repair** — lands in the repair-hazard bank beside D-8/D-14/D-16/MISS-A5.
- **FR-PS-CLIP — MAJOR, LATENT-ARMED (repair hazard; δ-miss-2, operands verified).** The D-3 cure is armed on the sidebar variant: revived `.paper-search-results { position:absolute; … z-index: var(--z-bar); max-height:50vh; overflow-y:auto }` cannot escape `.sidebar-nav`'s clip — the containing block (`.paper-search`, `position:relative`) sits INSIDE the scroller (`PaperSidebar.vue:51` first child; `:155` `overflow-y:auto`, which forces computed `overflow-x:auto`, clipping both axes); an absolutely positioned box in a scroll container is clipped by it and CONTRIBUTES to its scrollable overflow, so the "repaired" 50vh dropdown lengthens the ToC's scroll instead of overlaying it — the exact outcome D-3 promises the repair fixes ("scrolls with the ToC") survives the repair verbatim; `z-index` lifts nothing out of an overflow clip. Cure: teleport/anchor positioning, or relax the panel clip. Neither banked D-3 nor MISS-A3 rules the POST-repair geometry (both describe the current dead state; grep verified). **Route F.W3/W4 gate.**
- **FR-PS-Z — MINOR, LATENT (γ-miss-2; ladder + context chain verified).** Z-token altitude error, three ways: the dropdown is filed at the DOCK/BAR rung (`--z-bar` 30, `:108`) when `--z-popover` (130) exists for exactly this surface; the backdrop is given the IDENTICAL token as the plate it must sit behind (`:195` — the FR-PS-BDT ingredient, an authoring error in its own right); and on the floating consumer both are clamped inside `.floating-toc { sticky; z-index: var(--z-controls) }` (20) → `.floating-toc-bar { relative; z-index: 2 }` → `glass-resting`'s filter context — the declared 30 is unreachable as a page altitude; effective altitude 20, beneath the `--z-overlay` (50) chrome. Goes live with the D-1 repair. Equal-z mechanism folds to kf-SharePopover D·B-2(c)/kf-EditorShell S-2; the token-altitude instance is new. **F.W3/W4** (rung assignment is spec input).
- **FR-PS-HL — MINOR (δ-miss-3; operands verified).** Two query sources in one render: rows come from `debouncedQuery` (`usePaperSearch.ts:35-37`, 120 ms) but their `<mark>`s are computed from the raw `query` (`PaperSearchDropdown.vue:58`, `PaperSearchModal.vue:100`) — any keystroke that breaks a subsequence blanks ALL highlights for 120 ms on rows still presented as matches (`highlightFuzzy` returns unmarked escaped text when `matchSet.size === 0`), then re-marks. A visible flicker and a second source of truth. Cure is free: highlight from `debouncedQuery`. Distinct from banked D-29 (canExpand skew) and L-9 (re-derivation cost); grep for `debounced` across the banked family rows → no row on this seam. **F.W3/W4.**
- **FR-PS-LBL — MINOR (γ-miss-3; TYPE_LABELS read verbatim).** The badge's TEXT channel is as incoherent as its colour channel: 13 labels use four abbreviation strategies — truncation (Sec/Thm/Def/…), identity (Code), non-conventional vowel-dropping (`proof`→"Prf"; AMS sets "Proof"), and outright RENAME (`aside`→"Note" — the one thing a type badge exists to encode, made unrecoverable). Combined with banked D-15/D-18 (13 types → 4 unstable colour buckets, no legend), the badge fails as a legend on BOTH channels; the authored 0.6rem/700/uppercase flattening is itself dead, so the authored form was never even seen. Distinct from banked PSM-17 (typing/exhaustiveness) and D-27 (prose register) — neither examines the vocabulary as a visual code. **F.W3/W4** spec input (the label table is re-authored with the badge system).
- **FR-PS-CACHE-ID — INFO (δ-miss-4; verified at :210-211/:239-241).** `searchIndex` hands out its cache entry BY IDENTITY (returns the stored array; stores the returned array) — two mounted trees iterate a live, shared, module-owned reference, so any consumer-side mutation (a future sort toggle/pin) silently rewrites the cache for the session; the third falsification of the file's ":3 Pure functions, no Vue reactivity" docblock, distinct from L-4's residency and key limbs. Adjacent to K11, which it does not disturb (identity semantics is exactly what makes the cursor-preservation correct). The producer's copy re-slices on cache hit (verified in the fr-PaperSearchModal instrument record), so **MISS-B3 adoption cures it for free. F.W3/W4.**

### Rows corrected or retired (4)

- **D-26 — RETIRED (K18, ruling 1).** Removed from the roster and from the repair-hazard bank. Replacement residue: the glass-uplift BANK re-trigger (re-grep the `@layer` span of `base.css` on any glass major bump; the kill is pinned to the layered 4.0.0 sheet).
- **D-21/D-22 → D-22 — MINOR (K17, ruling 2).** Surviving claim: half-migrated motion vocabulary — nine raw durations (`0.1s`…`0.25s`) beside two "migration done" annotations (`:224`, `:363`); zero of the tokenised `--duration-*`/`--ease-*` registers used. The PRM limb is dead at the producer sheet; the snap-frame residue stays banked as fr-PaperSearchModal PSM-32. **F.W3/W4.**
- **D-29/C-N3 — MINOR, cell narrowed (K20, ruling 3).** Survives as the phase-skew limb only: `can-expand` conjoins undebounced `query` with debounced `results`, so the Expand button phases one debounce out of step with the panel it opens. The "control shown while the panel is not showing" limb is unreachable. **F.W3/W4.**
- **MISS-B3 — MAJOR decision row, cell extended (K19, ruling 4).** Adds: `FuzzySearch.vue` is the producer's 1:1 component twin (same variant union, same focus expose) — adoption is component-for-component, not merely composable-for-composable; `.interactive-item`/`CommandItem` are the row primitives the hand-rolled skin shadows; `searchVariants.ts` does not exist at 4.0.0. Honest costs and the BH-relay obligation carry unchanged from r2. **F.W3/W4 + glass-ui BH relay.**

### Carried rows (r2-carried; re-attacked only where this corpus touched them)

**Blockers (4):** **D-1/L-1/C-B1** (43-of-48 rule blocks dead; now FIVE concordant independent counts — both r3 readers re-enumerated; `style.css:129-143` proves the team knows the mechanism) · **D-2/L-1¶1** (unstyled body-teleported modal after a 100dvh `overflow-hidden` shell — `App.vue:24` re-read) · **D-3/L-1¶2** (in-flow 30-row dropdown; now carries the FR-PS-CLIP post-repair gate) · **FR-PS-NAV** (selection navigates nowhere for labelled theorem/figure/equation ids — untouched by this corpus). All **F.W3/W4**, one atomic repair cluster: D-1 ∧ L-3 ∧ FR-PS-NAV ∧ producer adoption, now gated by FR-PS-BDT + FR-PS-CLIP.
**Majors (r2's 20, all carried):** L-2/C-M4 (within-token cache narrowing; γ+δ re-verified the trim mechanics — K13 stands) · FR-PS-FTS · D-4 (both readers re-derived the pill geometry from the installed CVA bytes) · D-5 (labels unbounded — `resultLabel` re-read this session) · D-6 (now subsumed as the FR-PS-BDT trigger; the dead-state fact stands) · D-7/L-7 · D-8 · D-9/D-10/D-11/C-M7/C-B3/C-M1 (zero AT surface; `./dialog` present at the pin) · D-12 · D-14 (γ recomputed the placeholder cell independently: 1.8838:1 — three concordant instruments; the `--viz-amber` block at `style.css:113-122` is the house's own measured floor, making this a self-indictment) · D-15/D-18 (γ recomputed all four theme cells to 2 d.p.) · D-16 (the ≈1.71:1 focus-substitute inversion — the bank's sharpest surviving member now that D-26 is retired) · D-17 (the correct idiom verified at `PaperSidebar.vue:165-166` this session) · L-3/C-B2 (as corrected in r2 ruling 6; K10/K14 stand — γ's kill of C-B2's consequence-1 folds to K10) · L-4/C-M5 · L-5 (both readers re-verified the `display:none` chain) · MISS-A1 · MISS-A2 · MISS-A3 · MISS-B3 (as extended above). All **F.W3/W4** except D-14/D-15/D-17's token limbs (**F.W1** re-ink) and MISS-B3's producer limb (**BH relay**).
**Minors (17 carried):** L-6 · D-19/D-20/D-31 (K7 stands) · D-22 (as corrected) · D-23/D-25/C-I3+L-20 (as sharpened by K19) · D-27 · D-29 (as narrowed) · L-8/C-N6 · L-9/C-M6 · L-10/L-13/L-17 · C-N2 · C-M3 · L-11/L-18/C-N7/C-N8 · MISS-A4 · MISS-A5 · MISS-A6 (FOLDS to the fleet non-text-contrast class; F.W1) · MISS-B4 · MISS-B5. All **F.W3/W4** unless noted.
**Infos (7 carried):** D-28/L-12/C-N4/C-N5 (δ re-verified the dead emits/expose/props/type-import at source) · C-Z1/C-Z2 (load-bearing zeros re-run by δ; lucide = 2 files/7 symbols, K5 re-confirmed by both) · L-14 residue · L-19+C-N1 (**NO-WAVE-OWNER** bank) · MISS-B6 · MISS-B7 · MISS-A7 (**F.W0**; now carries the ruling-6 registry-coherence rider).

## Killed-claims register — 20 entries

**K1..K16 carried from r2** (all sustained; this corpus touched five): K1 (D-24 — sustained as **carried-empirical** per ruling 6; both r3 readers folded to it, δ adding the `@source` caveat; the F.W0 rider now owns the decider) · K5 (lucide 2/7 — re-confirmed by both readers independently) · K7 (four off-grid, not five — γ re-derived) · K9 (arithmetic cells; δ re-confirmed both, adding that `resultLabel`'s unbounded arm makes D-5 an understatement — verified) · K10 (C-B2 scope — γ's kill of consequence-1 folds here; the challenge self-contradicts four lines later) · K13 (L-2 flagship example — δ re-verified the trim mechanics; this seat re-read :203/:222/:240) · K16 (L-20 gating — both readers re-verified `./search` at 4.0.0). K2, K3, K4, K6†, K8, K11, K12, K14, K15 untouched. († K6 is completed, not contradicted, by K18: its LIVE→LATENT correction was right; the surviving "repair hazard" register is what dies.)

- **K17 · D-21 as filed AND r2's own D-21 cell — RULED KILLED (overturns reader-δ's batch cell and corrects this registry).** Ruling 2. The universal PRM reset (`a11y-overrides.css:6-17`, `!important`, unlayered-outranking irrelevant since it wins on importance) strikes `transform` from every transitioned set including the component's own rules; reachability verified end-to-end; folds to banked fr-PaperSearchModal K-1/R-1 + fleet precedent fr-FullscreenViewer K-2. Residue: PSM-32 (banked, Modal registry) + D-22 (vocabulary, survives).
- **K18 · D-26 — RULED KILLED (overturns reader-δ's confirmation AND the banked r2 row).** Ruling 1. One `@layer` block 43→541; the pill rule layered; scoped output unlayered; unlayered wins → revived radii hold and the ring follows them. Second limb: `.interactive-item:focus-visible` is the shipped radius-preserving opt-out, so "under the OLD PIN there is no opt-out" is independently false. Mechanism folds to banked M-12/FR-FG-5/R2-N1. BANK re-trigger on glass major bump replaces the row.
- **K19 · D-23 "does not exist upstream" limb — SUSTAINED (reader-γ).** Ruling 4. The falsifier is answerable three ways at the installed pin (`.interactive-item`, `CommandItem`, `FuzzySearch.vue` — the never-before-named 1:1 twin). The surviving substance ("hand-rolled skin over the design system") sharpens; MISS-B3's cell extended.
- **K20 · C-N3 first limb — SUSTAINED (reader-δ).** Ruling 3. `close()` is the sole `isOpen=false` writer and clears `query` atomically; `query!=="" ∧ isOpen===false` is unreachable. D-29/C-N3 narrows to the phase-skew limb.

## Superlatives — 11 (r2's 8 sustained; 3 new)

r2's 1-8 carried intact (the `v-html` sink — now upheld under a SEVENTH independent attack, δ came at it from the attacker's side again and could not break it; `_lc` normalisation; safe `/g` hoists; the never-applying `min-height:0`/`overscroll` craft; the total-cursor watcher — reinforced by K11's identity semantics and now by FR-PS-CACHE-ID's adjacency note; the partitioned scroll watchers; the typed focus chain; the corpus's measurement discipline).

9. **`web/src/style.css:126-143` + `:113-122` (reader-γ, verified by this seat's own read)** — the D.W4.d hoist writes the Vue scope-hash mechanism out in prose and then ACTS on it for four classes, and the `--viz-amber` block carries its own contrast arithmetic (3.54:1 → 4.6:1) with the upstream carry filed as a coordination ask instead of a silent fork. The house KNOWS the mechanism that killed this component's stylesheet and ENFORCES the floor its `color-mix` sites fail — which is precisely why D-1 adjudicates as a lapse and D-14 as a self-indictment. A repair wave extends this hoist list; it does not re-derive the rule.
10. **`MobileFloatingToc.vue:261-263` (both readers, converging from opposite sides; δ's mechanism explanation adopted)** — `.floating-toc-bar--search > :first-child { flex:1; min-width:0 }`: the one place a parent's scoped CSS reaches into this family and WORKS, because `.paper-search` IS the child's single subTree root and receives the parent's scope id — the identical mechanism that kills 43 rules one file away, applied correctly; and it is the host supplying the flex-min-content defusal the child authored for itself and lost. Preserve verbatim through the repair as the in-tree proof of the correct scoped-CSS model.
11. **Installed glass-ui `a11y-overrides.css:6-30` (reader-γ, read whole by this seat)** — the nuanced PRM reset: preserves the five non-spatial legs at 0.1s, strips `transform`, then overrides its OWN `[data-allow-motion]` carve with the written rationale "a user who asks for reduced motion outranks any per-element motion-allow." Producer craft at the correct altitude — and the structural reason consumer-level PRM findings in this fleet are refutable rather than merely unmeasured. (Two banked rows fell to this one file; the fleet lesson is to open it FIRST.)

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

r2's 1-11 carried unchanged (D-2's opposite live predictions — δ's unproven restatement folds into #1; the two-modal witness; the dark-`MarkText` photograph; frame costs; iOS units; FR-PS-NAV's before/after film). Added by this corpus:

12. **FR-PS-BDT footprint** — film AFTER a D-1-only repair on a throwaway branch (never ship it): the bar-box overlap of the confined backdrop, which controls it occludes, and the tap-trap witness (tap a result row → `close()` fires, no navigation). The containing block and paint order are decided statically; the pixels are not.
13. **FR-PS-CLIP geometry** — `getComputedStyle($('.sidebar-nav')).overflowX` (expected `auto`) and the repaired dropdown's boundingRect vs the nav's, at the live `--paper-scroll-viewport-height`.
14. **FR-PS-Z occlusion** — which page chrome (teleport-overlay, progress track at `--z-overlay`) actually paints over the clamped dropdown once revived.
15. **`.glass-resting` computed blur on the search bar** — no verdict depends on it (the containing block holds for any non-`none` value including `blur(0px)`), but capture it beside the banked PSM SS-13 #7 blur-cost probe.
16. **Coarse-pointer field proportion** — figures already banked (D-4 / PSM K-5: ~67.5px at fourier's mobile root); recorded as measurement target only, not a find.

(The D-24 emission decider is **F.W0**, not SS-13 — it needs the build, not a browser; ruling 6's rider owns it.)

## Closing verdict

**DEFECTIVE — and its prescribed cures were defective too, until this round.** The component's three layers of failure stand from r2 (a 43-of-48-dead stylesheet; a dead central affordance — FR-PS-NAV/FTS; a producer that already ships the corrected twin this family shadows — now named: `FuzzySearch.vue`). What r3 adds is about the CURES and the REGISTRY: both variants' naive repairs fail structurally (FR-PS-BDT — the revived mobile backdrop is a one-tap dismiss trap over its own input and results, an ORIGINAL defect the split had accidentally disarmed; FR-PS-CLIP — the revived sidebar dropdown stays inside the ToC's clip), while the bank's most-cited hazard (D-26) and the PRM gap (D-21) are KILLED at the producer sheet — two banked r2 cells corrected because earlier seats reasoned past a stylesheet instead of brace-matching it. Roster: **4 BLOCKER · 22 MAJOR · 20 MINOR · 8 INFO**; kills: **20**, two overturning this registry's own prior rows; routes: overwhelmingly **F.W3/W4** (one atomic cluster: D-1 ∧ L-3 ∧ FR-PS-NAV ∧ MISS-B3 producer adoption, gated by the five-row repair-hazard bank D-8 · D-14 · D-16 · FR-PS-BDT · FR-PS-CLIP), **F.W1** token re-ink + 2-file lucide rename, **F.W0** build unblock + the K1/PSM-24 coherence rider, **F.W5-W8 empty** (measured zeros), one **NO-WAVE-OWNER** bank (L-19), three **glass-ui BH relay** obligations carried, one glass-uplift BANK re-trigger minted (the D-26 layer pin).

— end of apotheosis r3. The three challenge files, both r3 reader returns, and the superseded r2 rulings' identities remain immutable per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED (M-25); the routings are agglomeration input to the forming X·F specs, and execution awaits the owner's begin-word.
