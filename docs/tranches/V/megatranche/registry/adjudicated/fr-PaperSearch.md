claude-fable-5

# fr-PaperSearch — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ `cd26c65`, **working tree carries the uncommitted tri-package uplift** (`git diff web/package.json`: glass-ui `^3.1.0→^4.0.0`, keyframes `^2.2→^4.3`, vite `→^8.0.16` — the audited pin is the WORKING TREE, resolved glass-ui **4.0.0** in `web/node_modules`) · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the component's three challenge files — reader-α (slug `fr-PaperSearch`: 24 confirmation cells · 10 kills · 7 misses · 9 superlatives) and reader-β (slug `PaperSearch`: 33 confirmation cells · 7 kills · 7 misses · 7 superlatives). Challenge files (`audit/fourier-components/PaperSearch/challenge-D-design.md` 53K, `challenge-L-library.md` 45K, `challenge-C-consumption.md` 37K) spot-read by this seat at every disputed row (D-16, D-24, D-25, L-2, L-14, L-15, L-16, L-19, L-20, C-M3, C-M4, C-B2) — the readers' representations of the corpus are accurate everywhere I checked.
**Method**: neither reader was adopted before being attacked. Every verdict-level disagreement between the two returns (D-24, D-16's arithmetic cell, L-2's grade, L-3's blast-radius limb, L-6's grade, L-15, L-16) was re-derived by this seat with its own instruments, and both load-bearing NEW rows (reader-β's navigation BLOCKER, reader-β's full-text-dead MAJOR) were re-verified end-to-end before admission — a miss this large does not enter the registry on one reader's word.
**This seat's own instruments (all read-only, zero browser probes — probe parsimony)**: full read of `paperSearchIndex.ts` (whole, 402), `usePaperSearch.ts` (whole, 109), `PaperSearchModal.vue:14-68`, `PaperView.vue:1-120`, `useScrollNavigation.ts:28-200`, `MobileFloatingToc.vue:85-112`; installed `@mkbabb/latex-paper@0.2.1` `dist/vue.js` — `ensureTargetWindow` (`itemIndex.get(id) == null → return`), `getOffsetFor` (= `findSectionOffset(layout, id)`), `flattenPaperSections` (sections/subsections ONLY) read verbatim; shipped `web/dist/assets` id-shape census (`label:"thm:` ×40 · `label:"fig:` ×48 · `id:"eq-` ×63 vs section slugs `id:"introduction"` etc.); a node run of `fuzzyMatch` transcribed verbatim from `:70-127` (the FTS decider); a node WCAG computation (the D-16 decider: composite `#8c8a88`, L 0.2553 vs border L 0.4715 → **1.708:1**; resting border vs bg → **1.930:1**); installed glass-ui 4.0.0 — exports map (80 keys; `./search` `./dialog` `./command` `./confirm-dialog` present), `dist/button-BNDWhAZb.js` CVA base string (contains `[&_svg:not([class*=size-])]:size-(--ui-glyph)` — 1 occurrence), `dist/styles/**` grep for the compiled rule (**absent**; `class*=size-` exists ONLY in a comment at `tokens/offsets-sizing.css:175`; `--ui-glyph` declared, never consumed by any shipped rule), the emission-policy comment at `dist/styles/index.css:210-222` read whole; shipped consumer CSS greps (8 files: `class*=size-` **0** everywhere, `.btn-pill`/`.h-3`/`.w-3`/`.h-3\.5`/`whitespace-nowrap`/`.btn-pill>*{flex-shrink:0}`/`.focus-ring:focus-visible{border-radius:var(--radius-pill)…}`/`glass-floating` all present; 26 distinct `[data-v-a7d16b8c]` compounds in `PaperView-FJog9X2r.css`); `PaperView.vue:562` (`220px minmax(0,48rem)`); lucide-vue-next 1.0.0 `defaultAttributes` (`width:24, height:24`); repo greps: barrel importers = **0**, `SearchEntry.sectionId`/`.depth` readers outside the module = **0** (the two hits are different objects), `vite.config.ts:47-58` manualChunks OBJECT.
**Environment disclosure**: every ruling below was decidable from source + installed producers + node; the readers' fresh-build cells are adopted as corroboration where this seat's static derivation agrees, and one shipped-dist evidence class is CORRECTED (adjudicator correction 5).

## Scoreboard

**49 adjudicated roster rows — 4 BLOCKER · 20 MAJOR · 18 MINOR · 7 INFO.** **16 killed-claims register entries** — including one ruled kill that **overturns reader-β's re-confirmation** (D-24), two ruled kills that **overturn reader-α's confirmations** (L-15, L-16), and one **kill of a killer's cell** (reader-α's D-16 arithmetic "correction" is itself wrong; the original challenge cell stands). **14 missed rows ADMITTED** (7 from each reader — each reader missed the other's entire missed set), two of them the severest facts in the corpus. **8 superlatives sustained.** **5 adjudicator corrections against the readers.**

Blocker-weight survivors of record: **D-1/L-1/C-B1** (43-of-48 rule blocks orphaned by the `ffba307` split — three independent counts now agree, plus this seat's 26-compound shipped-CSS census) and the newly admitted **FR-PS-NAV** (reader-β's find, re-derived six-step by this seat): *selecting any labelled theorem, figure, or numbered equation navigates nowhere* — the component's most-used affordance is dead for 151 of its highest-ranked id shapes, and `close()` then wipes the user's query. The corpus's three axes and reader-α all missed it. The component is DEFECTIVE at both layers the audit cares about: nothing it styles paints, and the half of its results users most want cannot navigate.

## Ruled disagreements and corrections (the incredulity seat's actual work)

| # | row | reader-α | reader-β | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **D-24** (svg sizing overridden by Button base) | KILLED (fresh-build: rule never emitted) | CONFIRMED MINOR (CVA string + specificity + `@source` reach) | **KILLED — α sustained, β overturned** | The rule exists NOWHERE a consumer can get it: the candidate string is real (my read of `dist/button-BNDWhAZb.js`) but glass-ui pre-compiles **no** `[&_svg:not([class*=size-])]` rule into `dist/styles/**` (my grep: only a comment at `offsets-sizing.css:175` describing the intended rule), and glass's own emission-policy comment (`dist/styles/index.css:210-222`, read whole) states fully-arbitrary bracket utilities "are rejected by that path's `classish` token filter AND are not reliably reachable by a JIT scan, so every STRUCTURAL bracket utility … is pre-compiled into shipped CSS instead". This one was NOT pre-compiled — the policy's own gap. α's fresh build (0 emissions while sibling utilities from the same string emit) is the empirical witness; β argued from `@source` reach without checking emission. Consequence: `.h-3`/`.w-3`/`.h-3.5` apply; in-Button icons keep their authored 12/14 px. **Producer relay rider**: the `--ui-glyph` un-sized-svg register is dead fleet-wide at 4.0.0 — a glass-ui BH letter, not a local defect. |
| 2 | **D-16 arithmetic cell** | "1.71 is wrong; it is **1.56**" | 1.713 (matches D's 1.71) | **α's correction REJECTED — the challenge's own cell stands** | My node computation from installed tokens: focused border = `#1c1917` @50% over `#fbfaf8` → composite `#8c8a88`, L 0.2553; unfocused `#c6b49f` L 0.4715 → **1.708:1**. Three independent instruments (D, β, this seat) land 1.708–1.713; α's 1.56 rests on a mis-computed composite luminance (0.2849). α's OTHER D-16 correction is ADOPTED: in the WCAG 2.2 Recommendation the cited SC is 2.4.13 *Focus Appearance* (AAA), not 2.4.11; the binding AA criterion is **1.4.11 Non-text Contrast**. Verdict unchanged either way (≈1.7 ≪ 3:1), and the inversion (compliant UA ring live today, failing substitute after repair) remains the corpus's sharpest repair hazard. |
| 3 | **L-2 grade** (truncated-cache prefix narrowing) | BLOCKER | MAJOR (flagship scenario killed) | **MAJOR — β sustained** | Source read: `:203` caches TRIMMED keys; `:222` `q.slice(0,-1)` of `"fourier t"` is `"fourier "` — never a key → cache miss → `candidates = index`. **The narrowing chain resets at every space**, so multi-token queries re-scan the full index at each token boundary; the degradation is real but confined to within-token chains. β's kill of the "fourier transform returns junk" worked example is correct; the defect (top-30-for-`"f"` seeding single-word queries, sticky across backspace) survives at MAJOR. |
| 4 | **L-15** (selectedIndex reset misses cache-hit identity) | CONFIRMED MINOR ("benign in effect") | KILLED | **KILLED — β sustained, α overturned** | Self-defeating premise, verified in source: `_cache` maps one normalised `q` to one array (`:240`); every non-cached path allocates fresh (`:205,:217,:239`). Identical instance ⇔ identical normalised query ⇔ literally the same result set — preserving the cursor across `"Foo"→"foo"` is CORRECT behaviour. α's own concession ("no index can dangle") is the kill; a confirmed-with-no-reachable-harm is not a defect. D·S-2's superlative reading of the watcher is the right one. |
| 5 | **L-16** (field attribution by string value) | CONFIRMED (in the ranker group) | KILLED | **KILLED — β sustained** | `:185` guards on STRING EQUALITY: `text === entry._lc.label` can only be true when the strings are character-identical, and indices into one are then valid into the other by definition. Misattribution requires distinct strings, which the guard excludes. Residue = a value-vs-identity style nit on a field with zero readers — folds into L-9's dead-`matches` row. |
| 6 | **L-3 blast-radius limb** ("drives the hidden one") | Sustained ("holds, for a reason neither axis states": scheduler-uid insertion order) | Limb killed (Teleport escapes `display:none`) | **BOTH partially adopted — corrected statement booked** | Verified: `PaperSearchModal.vue:41` teleports to `<body>`; the sidebar's overlay is a body child, NOT a descendant of `.paper-sidebar`, so `display:none` never reaches it — β is right that no modal is "hidden". α is right about WHICH node `document.querySelector(".search-modal-results")` (`:32`, verified) returns: the sidebar instance mounts at page load, its overlay is inserted first on the shared `isExpanded` flip (uid-ordered flush), and IS the query's answer. Corrected claim: **the scroll driver steers the first-teleported DUPLICATE, so the modal the user is typing in never scrolls its selection into view.** MAJOR unchanged; two-modal condition requires mobile `searchActive` (C-B2 scope cell killed by both readers, sustained). |
| 7 | **L-6 grade** (index-suffixed `:key`) | MAJOR | MINOR | **MINOR** | Mechanism certain (keys minted `${id}-${type}-${i}`, re-sort every query, keyed diff must unmount/mount); magnitude unmeasured (SS-13). A certain mechanism with an unmeasured cost and a trivial cure (build-time unique entry id — the `code`/`proof` `id: sectionId` collisions at `:335`/`:350` are the motive) books at MINOR with a measurement rider, per the corpus's own severity discipline. |
| 8 | **C-M3** (inferred `PaperSearchState` as six components' prop contract) | Flagged as a genuine axis conflict, punted to F.W1 | (not adjudicated) | **RULED HERE — MINOR, dual-register** | Both properties are real properties of `usePaperSearch.ts:108` (verified): the derived type cannot drift (D·S-2/L·S-2's superlative is true) AND it publishes a composable's private shape with no reviewable declaration site, which is what makes the children's write-through-props pattern (C-M2, `Input:45`, `Dropdown:48`) typecheck. The forming spec should replace it with an explicit narrow interface per child — the drift-proofness survives in the implementation, the contract becomes reviewable. Booked once, MINOR, routed F.W3/W4; neither register is lost. |

**Adjudicator corrections against the readers (5).** (1) β re-confirmed D-24 from specificity reasoning without an emission check — the exact failure α's build caught; overturned above. (2) α's D-16 arithmetic correction was itself wrong (ruling 2) — killers' cells get re-derived too. (3) α confirmed L-15/L-16 while conceding their harmlessness in the same cell; overturned. (4) **Each reader missed the other's entire missed set** — 7+7 disjoint rows, including a BLOCKER (α) and the two severest library facts (β missed α's live-geometry rows; α missed β's navigation and FTS rows). The r2 idiom exists because single hostile readers still under-cover; recorded as fleet evidence. (5) **Shipped-dist staleness**: `web/dist/assets` predates the uncommitted uplift — the 4.0.0 CVA string (`class*=size-`, `h-(--control-h-md)`) appears NOWHERE in the shipped JS (my grep), so the shipped bundle was built against pre-4.0.0 glass. β's shipped-chunk cells ("it reaches production": `emits:["select"]`, the 5 `__scopeId` bindings, the paper-content id shapes) remain true of what shipped AND of current source (independently re-verified), but shipped-dist is evidence of the PRE-uplift build and must not be cited as the audited pin. Where a shipped-dist cell was load-bearing, this seat re-derived it from source + installed producers.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Dispositions are **ADJUDICATED, not VERIFIED** (M-25); routes name the forming X·F wave surface that will own the row. Line anchors are working-tree; re-resolve before any cure.

### Blockers (4)

- **D-1 / L-1 / C-B1 — BLOCKER.** The 355-line `<style scoped>` block is 43-of-48 rule blocks dead: `ffba307` moved the markup into three child SFCs with no `<style>` of their own; Vue 3.5.38's `setScopeId` reaches only a single-root child's root element (Input's wrap survives; Dropdown is a two-root fragment; Modal roots on Teleport). Three independent counts agree 5-alive/43-dead; this seat adds the shipped-CSS census (26 distinct `[data-v-a7d16b8c]` compounds, every one unreachable). `style.css:129-143` proves the team knows the mechanism and this family is absent from the hoist list. Line-count cells: α's 329/355 over D's 330/356 — cell noted, headline unaffected. **Route F.W3/W4** — relocate style into the children (α's runtime-core verification: a child's own `__scopeId` stamps teleported vnodes; the `:deep()` alternative is killed, see K3), carrying the repair-hazard group (D-8, D-16, D-26, MISS-A5, D-14's ghost inversion) as gate conditions, not afterthoughts.
- **D-2 / L-1¶1 — BLOCKER.** The expand modal teleports to `<body>` with every `.search-modal*` rule dead: an unstyled static block after a `100dvh` `overflow-hidden` shell (`App.vue:24`, `style.css:19-22`); `@click.self` bound to a zero-tint in-flow div; auto-focus scroll-anchors the document. Mechanism CONFIRMED-STATIC; pixels SS-13. **Route F.W3/W4** (producer `./dialog` is the named cure — see the a11y row).
- **D-3 / L-1¶2 — BLOCKER.** The inline dropdown is an unpositioned in-flow block carrying up to 30 rows inside the sidebar's own scroll panel (`PaperSidebar.vue:51` first child of `.sidebar-nav`, `:151-165`), with only the global `glass-floating` material biting — material without geometry. **Route F.W3/W4.**
- **FR-PS-NAV — BLOCKER (NEW, reader-β's find; re-derived six-step by this seat).** Selecting any labelled theorem/definition/lemma/etc., any labelled figure, or any numbered equation performs NO navigation. Chain, every link read live by this seat: search ids are raw label keys / anchor ids (`paperSearchIndex.ts:301,:314,:325`; shipped content: `label:"thm:parseval"`, `id:"eq-f1"` — ×40/×48/×63) → `selectResult` → `navigateTo(r.id)` (`usePaperSearch.ts:43-46`) → `performScroll` → `estimateAbsoluteTop` → `getOffsetFor` = `findSectionOffset` over `flattenPaperSections` (sections/subsections ONLY, installed latex-paper dist read verbatim) → null → `ensureTargetWindow(id)` no-ops (`itemIndex.get(id) == null → return`) → `performScroll` returns with no scroll and no DOM fallback (`useScrollNavigation.ts:174-179`; the `getElementById` path is gated behind a non-null layout offset). Then `close()` wipes the query. The cruelest cell: `_lc.number` carries the ×18 top weight, so `"2.129"` ranks its equation FIRST — and clicking it does nothing. The repo's own cross-ref path (`PaperView.vue:92-94` via `labelMap` → `anchorId ?? elementId ?? sectionId`) is the missing step the search bypasses. `section`/`code`/`proof` entries (id = section slug) work — which is exactly why this survived casual use. **Route F.W3/W4** — resolve result ids through `labelMap`/anchor path at selection, or index resolvable ids at build; must land WITH the D-1 repair so the fixed modal doesn't demo a dead click.

### Majors (20)

- **L-2 / C-M4 — MAJOR (re-graded from BLOCKER, ruling 3).** `searchIndex` narrows from a cache entry already truncated to the prior query's top-30 (`:223-226` vs `:239-240`); resets only at token boundaries (trim, this seat's read) and empty query; sticky across backspace. The installed producer's own `dist/search.js` caches the UNTRUNCATED scored list and slices at return (β's decisive find) — divergence from an already-correct upstream. **Route F.W3/W4** (cured mechanically by producer adoption — see MISS-B3).
- **FR-PS-FTS — MAJOR (NEW, reader-β's find; re-run by this seat).** Full-text search is structurally dead: the unbounded `−0.1`/excess-char penalty (`:124`) exceeds any achievable greedy score on a long field, and `best <= 0` (`:189`) HARD-DROPS the entry. My transcribed-verbatim run: `"fourier"` mid-field in a 500-char body → **−37.3** (×3 weight → dropped); even an ideal contiguous word-start run mid-field → **−5.3**; only a field that STARTS with the query survives (+41.9). Break-even for a contiguous run: L ≥ 8, which greedy-leftmost won't find unless leftmost. `plainText` (500/300-char slices, weight 3) and long `rawTex` (weight 6) contribute nothing, ever; a body-only match returns NOTHING. The corpus's axes praised the field-weight table without testing reachability. **Present verbatim in the producer's copy → glass-ui BH relay obligation; local cure route F.W3/W4** (bounded penalty or soft-rank floor).
- **D-4 — MAJOR.** Result rows render as glass Button defaults — inline-flex pills, `--radius-pill`, hard 40 px `--control-h-md` height (worse on coarse pointers), `whitespace-nowrap` → one-overflowing-pill-per-line (α's shape correction adopted). **F.W3/W4.**
- **D-5 — MAJOR (understatement corrected).** Labels cannot truncate: authored `min-width:0`/`text-overflow` dead while `.btn-pill > * { flex-shrink: 0 }` (verified in shipped CSS) and base `whitespace-nowrap` are live — and `resultLabel()` returns `r.label` UNBOUNDED (`searchHelpers.ts:24-28`; the 120-slice hits only fallbacks). **F.W3/W4.**
- **D-6 — MAJOR.** The floating dismiss backdrop is a zero-height in-flow div (`position:fixed; inset:0` dead); tap-outside-to-dismiss does not exist. **F.W3/W4.**
- **D-7 / L-7 — MAJOR.** No working outside-dismissal in either variant; sidebar has none BY DESIGN (backdrop gated `variant === 'floating'`); `isOpen` raised on `@focus`, lowered only by `close()`/Escape/selection; zero `addEventListener` in the family (grep re-run by both readers). **F.W3/W4.**
- **D-8 — MAJOR.** `<mark>` falls to UA `Mark`/`MarkText` system colours (`.paper-search-label :deep(mark)` compiles to a dead scoped selector — found verbatim in built CSS); the `color: inherit` that would suppress forced black is itself dead. Repair-hazard group member. **F.W3/W4.**
- **D-9 / D-10 / D-11 / C-M7 / C-B3 / C-M1 — MAJOR.** Zero AT surface: no combobox/listbox/option, no dialog role/trap/restore, no live region, `aria-|role=|tabindex` grep = 0 across the family; keyboard handling exists only on the two `<input>`s, so Escape/arrows die when focus reaches a result Button; selection is an invisible-AND-inaudible CSS class. `@mkbabb/glass-ui/dialog` is present at the installed pin and consumed at five sibling sites (both readers re-verified all five). D's calibration adopted over C's BLOCKER: today the modal is unreachable/unpainted (D-2), so missing dialog semantics are subsumed, additive after repair. **F.W3/W4** (adopt `./dialog`; combobox semantics in the spec).
- **D-12 — MAJOR.** No empty state on the only surface users meet (dropdown gates on `length > 0`); the modal's "No results" is styled by a dead rule and unreachable from the primary surface. **F.W3/W4.**
- **D-14 — MAJOR (LATENT).** Eight `color-mix(…, transparent)` sites dilute an at-minimum token below AA — α re-derived two cells to 2 d.p. (placeholder 1.884, badge 2.75). The inversion is real: glass `ghost` supplies `text-foreground/70` today, so reviving `.paper-search-action-btn` REGRESSES contrast. **F.W1 token re-ink** (substrate-wide pair) **+ F.W3/W4** repair-hazard gate.
- **D-15 / D-18 — MAJOR (LATENT).** Hardcoded badge hues, no dark arm, 3-of-4 theme cells fail AA (four cells concordant across α and D to 2 d.p.); token-derived badges collapse into `--foreground` in light (byte-identical `--primary`, verified) and invert to magenta in dark — 13 semantic types → 4 unstable colour buckets. **F.W1 + F.W3/W4.**
- **D-16 — MAJOR (LATENT; cells ruled, ruling 2).** `outline:none` dead / weak `:focus-within` substitute LIVE; delta ≈ **1.71:1** (this seat 1.708) against the 3:1 floor; binding criterion 1.4.11 (not the AAA 2.4.13 the challenge draft-numbered). The sharpest inversion in the corpus: a naive repair trades a compliant UA ring for a failing substitute. **F.W3/W4** repair-hazard gate.
- **D-17 — MAJOR (LATENT).** Literal `rgba(0,0,0,…)` shadows invisible on the `#0b0a09` dark ground; the theme-reactive idiom sits one file away (`PaperSidebar.vue:165`). **F.W1 idiom + F.W3/W4.**
- **L-3 / C-B2 — MAJOR (as corrected, ruling 6).** One `usePaperSearch` state shared by two `PaperSearch` trees; on desktop the mobile bar IS mounted (IntersectionObserver on a `display:none` target never intersects — spec-clear, live value SS-13); with mobile `searchActive`, expanding mounts two body-teleported modals and `PaperSearchModal.vue:32`'s document-global selector steers the first-teleported duplicate — the user's own modal never scrolls its selection. The sibling Dropdown does the same job correctly through its own `resultsRef`. **F.W3/W4** (single mount or scoped refs; must land WITH D-1's cure — two live `backdrop-filter` overlays is a compositor regression, see SS-13).
- **L-4 / C-M5 — MAJOR (leak limb; consequence limb killed, K4).** Module-singleton `Map` cache, no teardown path, index-agnostic key (`:210-211` returns without consulting `index`); retains 200×30 entry spreads at ceiling; falsifies the file's own "no Vue reactivity" docblock. Producer's copy already cures it (WeakMap-per-index + `clearSearchCache`). **F.W3/W4** via producer adoption.
- **L-5 — MAJOR.** ⌘/Ctrl-K is a no-op below 1024 px — `open()`'s sole consumer focuses an input inside a `display:none` subtree — while still `preventDefault`-ing the browser's own shortcut; the mobile path's local `searchActive` is unreachable from `open()`. **F.W3/W4.**
- **MISS-A1 — MAJOR (NEW, reader-α; premises verified by this seat).** The two bare lucide icons render 24×24 (installed `defaultAttributes` read; both authored sizing rules dead; no substitute rule in the emitted cascade) against authored 12.8/16 px — the most visible LIVE artefact of the scope-kill, and the exact inverse of D-24: icons INSIDE Buttons keep authored sizes, icons OUTSIDE blow out. **F.W3/W4.**
- **MISS-A2 — MAJOR (NEW, reader-α; operands verified: 220 px column at `PaperView.vue:562`, live 40 px icon Buttons, dead `min-width:0`).** The sidebar search field's ~295 px content demand in a ~184 px box — worst-case exactly while typing with results showing. Geometry SS-13. **F.W3/W4.**
- **MISS-A3 — MAJOR (NEW, reader-α; container chain verified).** Arrow keys scroll the TABLE OF CONTENTS: `scrollIntoView` has no owned scroll container (`.paper-search-results` geometry dead) and walks to `.sidebar-nav` — which also contains the search field, so arrowing carries the focused input out of view. A live consequence no axis joined together. Amplitude SS-13. **F.W3/W4.**
- **MISS-B3 — MAJOR disposition row (NEW, reader-β; spot-verified).** `@mkbabb/glass-ui/search` at the INSTALLED 4.0.0 is the same module upstreamed — identical algorithm and sentinels — with L-2 and L-4 ALREADY FIXED (untruncated cache; WeakMap key; `clearSearchCache`), so adoption cures two majors for free. Honest costs booked: the producer weights only 3 fields (no `number`×18/`rawTex`×6 — this paper's most precise query path must be preserved as a local scorer delta), and the producer's `FuzzySearch.vue` carries the L-3-class global-selector defect fourier's Dropdown already solved. L-20's "gated behind the tri-package deadlock" claim is KILLED (K16): no §5 break row touches this family except the lucide rename. **F.W3/W4 decision row + glass-ui BH relay** (the shared −0.1/hard-drop defect goes upstream, never patched only locally).

### Minors (18)

- **L-6 — MINOR (ruling 7).** Index-suffixed `:key`s defeat keyed reconciliation; up to 30 Button teardown+mount cycles per re-sort; motive is the real `code`/`proof` id collision; cure = build-time unique entry id. Frame cost SS-13. **F.W3/W4.**
- **D-19 / D-20 / D-31 — MINOR (cells corrected per K7/K9's discipline).** Five genuine raw font sizes with no ratio (α's fold-out of the SVG box dims adopted); FOUR off-grid spacing values {0.1, 0.15, 0.3, 0.35} (0.625 is Tailwind's 2.5 step — α's kill of the fifth adopted); modal/dropdown proportion as six magic numbers. **F.W3/W4.**
- **D-21 / D-22 — MINOR.** Six unguarded transitions, zero `prefers-reduced-motion` in-file (one, `:55`, LIVE today); glass supplies 47 upstream guards for inherited motion — the gap is precisely the component's own rules. **F.W3/W4.**
- **D-23 / D-25 / C-I3 (+ L-20 as ruled) — MINOR.** Hand-rolled button skin re-declaring `.btn-pill`+`ghost`'s surface with two direct contradictions; two unrelated surface languages inside one feature; the 832-LOC family shadows an installed, unimported producer subpath. **F.W3/W4** shadow retirement.
- **D-26 — MINOR, LATENT (register corrected, K6).** `.focus-ring:focus-visible` pill geometry (verified in shipped CSS) matches today's live pill rows exactly; the mismatch appears only after authored radii revive. Repair-hazard bank. **F.W3/W4.**
- **D-27 — MINOR.** Prose: three ASCII periods duplicated across two files in a Computer-Modern product; two-word empty state; register-drifting key hints advertised only in the modal. **F.W3/W4.**
- **D-29 / C-N3 — MINOR.** `can-expand` conjoins the undebounced `query` with debounced `results` and omits `isOpen` — the Expand button phases one debounce out of step with the panel it opens. **F.W3/W4.**
- **L-8 / C-N6 — MINOR.** Debounce timer with no disposal; zero `onScopeDispose`/`onUnmounted` in the family; same omission leaves `_cache` resident. **F.W3/W4.**
- **L-9 / C-M6 — MINOR.** `SearchResult.matches` computed/allocated per result, zero consumers; the docblock's fallback has no branch; the highlighter re-runs the matcher for all 30 rows inline in `v-html` on every re-render including `@mouseenter`. Root cause (lowercase-index vs display-text) means the field never could have worked as stored. L-16's residue folds here. **F.W3/W4.**
- **L-10 / L-13 / L-17 — MINOR.** Ranker hygiene: camelCase +6 unreachable (β's structural strengthening ADOPTED — a matched char can never be uppercase when every in-tree pattern is lowercased, which holds even under un-lowercased text); `flattenNestedBlocks` declares figure/code arms it never handles, silently dropping captions/code inside theorem/proof bodies (reachable per installed latex-paper types); separator docblock drift. **F.W3/W4.**
- **C-N2 — MINOR.** "Clear search" performs a full close; Escape wipes the text rather than dismissing the popup; `title` the only accessible name. **F.W3/W4.**
- **C-M3 — MINOR (ruled here, ruling 8).** Dual-register: drift-proof derived type AND unreviewable public contract enabling write-through-props. **F.W3/W4** spec (explicit narrow per-child interfaces).
- **L-11 / L-18 / C-N7 / C-N8 — MINOR (L-18's test-gap cell INFO within).** Colocation inversion (20 lines of logic hosting 355 lines of three other files' CSS — the mechanism by which D-1 outlived a motion pass that EDITED two dead blocks); `variant` union respelled ×3 with a 4-line barrel that exports no type; the 20-line result row and the scroll watcher duplicated across Dropdown/Modal; zero unit tests over pure, hot logic with `noUnusedLocals` off. **F.W3/W4.**
- **MISS-A4 — MINOR (NEW, reader-α; bindings verified).** `@mouseenter` writes the same `selectedIndex` the arrows drive and the scroll watcher observes — the canonical pointer/keyboard combobox conflict, plus a bounded hover→scroll→hover loop. **F.W3/W4.**
- **MISS-A5 — MINOR, LATENT (NEW, reader-α).** The dropdown wears two plates: live `glass-floating` already owns edge/elevation; the dead authored block adds its own 1.5 px border + literal-black shadow on the same element — a conflict the D-1 repair will surface. Repair-hazard bank. **F.W3/W4.**
- **MISS-A6 — MINOR, LIVE (NEW, reader-α; recomputed by this seat: 1.930:1).** The component's only live styled affordance fails SC 1.4.11 today — resting `--border` vs own `--background`. **FOLDS BY REFERENCE** into the banked non-text-contrast class (fr-EasingPicker MISSED-B · kf-SquareInstrument D-7 · kf-SpringHeatmap D-M4 · shell-dock ABT-11) — the token pair is substrate-wide; **cure rides F.W1 token re-ink**, never a leaf patch.
- **MISS-B4 — MINOR (NEW, reader-β; verified from my whole-file read: `multiTokenFuzzy` is field-local AND).** Composite queries the UI itself teaches ("eq 2.129", "theorem parseval") can never match — no field contains both tokens, and the cross-field fields are dead per FR-PS-FTS. **F.W3/W4.**
- **MISS-B5 — MINOR (NEW, reader-β; greps re-run by this seat: zero readers).** `SearchEntry.sectionId` and `.depth` are write-only tree-wide, and `sectionId` carries two contradictory meanings (root-section vs immediate-section). The 4-line dead barrel (zero importers, re-verified) **folds by reference** into the fleet-banked dead-barrel class (fr-EquationPanel row: ApiOfflineChip AP-11 · kf-EditorHeader EH-6 · kf-KeyframeCard KF-KC-45 · PaperSearchInput C-9). **F.W3/W4.**

### Info (7)

- **D-28 / L-12 / C-M2 / C-N4 / C-N5 — INFO.** Dead declared API ships (two `select` emits, a `defineExpose`, an unused `props` binding, an unused type import); the real data flow runs backwards through nested Refs in props, invisible to Vue's readonly warning; `noUnusedLocals` absent. **F.W3/W4.**
- **C-Z1 / C-Z2 — INFO (load-bearing zeros).** value.js/keyframes/api consumption in this leaf = 0 (re-run by both readers); the leaf contributes nothing to the F.W2 break budget; sole uplift cost = the lucide rename at **2 files / 7 symbols** (α's kill K5 of D's 3/8 cell adopted). **Recorded for F.W1/F.W2 sizing; F.W5-W8 = no rows.**
- **L-14 residue — INFO (scenario killed, K15).** `thm.type as SearchEntry["type"]` is unvalidated locally but unreachable-wrong at the adopted pin (installed latex-paper gates emission to the lowercase set). Boundary-hygiene note. **F.W3/W4.**
- **L-19 (+ C-N1 folded, K2) — INFO, LATENT.** Code-unit vs code-point mismatch in `highlightFuzzy`; 0 non-BMP code points in the shipped corpus (both readers re-measured; β checked the compiled chunks too). **NO-WAVE-OWNER** — banked against a future corpus; re-measure on any paper swap.
- **MISS-B6 — INFO (NEW, reader-β; demonstrated by this seat's run: greedy 12.6 vs optimal 13.6 on the witness pair).** "VSCode-style" self-description vs greedy-leftmost implementation — the ranking-quality mechanism that makes the length penalty fatal. Identical upstream → rides the **BH relay**; local cell **F.W3/W4**.
- **MISS-B7 — INFO (NEW, reader-β; regex read).** `stripHtml`'s naive `/<[^>]*>/g` can excise prose between an unrelated `<` and the next `>` in math-bearing paragraphs; INFO only because the field it corrupts is unreachable (FR-PS-FTS) — goes live the moment the scorer is fixed. **F.W3/W4** (sequenced with FTS cure).
- **MISS-A7 — INFO, substrate (NEW, reader-α; re-verified: vite `^8.0.16` in the working tree, `manualChunks` as OBJECT at `vite.config.ts:47-58`).** `npm run build` cannot run at the audited pin (rolldown requires a function) — silently blocking the corpus's own build-based falsifiers; plus this seat's shipped-dist staleness finding (correction 5). The glass-ui malformed `dist/styles/index.css` block comment (α chased and correctly demoted: real under bare postcss, harmless under Vite) rides the **BH relay** as producer hygiene. **Route F.W0.**

## Killed-claims register — 16 entries

- **K1 · D-24 — RULED KILLED (overturns reader-β).** See ruling 1. The rule is emitted nowhere: not JIT-extracted (α's build), not pre-compiled (this seat's glass-dist grep), and glass's own emission policy predicts exactly this gap. β's confirmation reasoned specificity over emission. Rider: producer relay for the dead `--ui-glyph` register.
- **K2 · C-N1 (severity + routing) — SUSTAINED (both readers).** 0 non-BMP code points in the corpus; math survives as `$…$`; folds into L-19 LATENT/INFO. L-19's honest self-demotion is the model.
- **K3 · L-1 cure limb (`:deep()` alternative) — SUSTAINED (reader-α).** `:deep()` compiles to a descendant combinator; the Teleported subtree is not a descendant of anything bearing the attribute — all 22 `.search-modal-*` rules would stay dead. Relocation into the children works (child `__scopeId` stamps teleported vnodes).
- **K4 · C-M5 live-consequence limb — SUSTAINED (both).** The index is built from a build-time module constant; no cache hit can hand a foreign id to `navigateTo`. Demoted to latent, merged with L-4b. (The ids it hands over are dead for OTHER reasons — FR-PS-NAV — which no axis saw.)
- **K5 · D §4 lucide row cardinality — SUSTAINED (reader-α).** 2 files / 7 symbols, not 3/8; the row's conclusion survives, the F.W1 sizing figure is corrected.
- **K6 · D-26 "LIVE" register cell — SUSTAINED (reader-α).** LATENT by the finding's own body text; today's pills match the pill ring exactly.
- **K7 · D-20 "five off-grid" — SUSTAINED (reader-α).** Four; 0.625rem is the standard 2.5 step, utility emitted.
- **K8 · D-16 SC-citation cell — SUSTAINED (reader-α), arithmetic cell REJECTED (ruling 2).** 2.4.13/AAA vs the binding 1.4.11 adopted; the 1.71 figure STANDS against α's 1.56 — a killer's cell killed.
- **K9 · D-5/D-1 arithmetic cells — SUSTAINED (reader-α), both in the understating direction.** Labels unbounded (not 120-capped); 329/355 (not 330/356).
- **K10 · C-B2 scope cell ("both mounted at every viewport") — SUSTAINED (both, convergent).** Dual mount is conditional below `lg` (post-scroll) and requires `searchActive` for the two-modal case; the desktop mount is real.
- **K11 · L-15 — RULED KILLED (overturns reader-α).** Ruling 4.
- **K12 · L-16 — RULED KILLED (overturns reader-α).** Ruling 5.
- **K13 · L-2 worked example — SUSTAINED (reader-β; source-verified by this seat).** Trimmed keys make `"fourier "` unreachable; the chain resets at every space. Severity re-booked MAJOR.
- **K14 · L-3 "hidden modal" limb + C-B2 cardinality/order sub-claims — SUSTAINED (reader-β) with reader-α's insertion-order derivation ADOPTED.** Ruling 6's corrected statement replaces both originals.
- **K15 · L-14 failure scenario — SUSTAINED (reader-β).** Installed latex-paper's `validTypes` gate + `inferTheoremLikeType` switch make the uppercase-type scenario unreachable at the pin. Residue INFO.
- **K16 · L-20 gating claim — SUSTAINED (reader-β).** `./search` ships at the installed 4.0.0 (this seat re-read the exports map — 80 keys); no tri-package transaction is required; the disposition consequence is MISS-B3.

## Superlatives — 8 sustained (L-18 runs both ways)

1. **`searchHelpers.ts:34-74` — the `v-html` sink, upheld under FIVE independent attacks** (three axes + both readers, each from the attacker's side): all four exits escape, `&`-first order, query never interpolated, element-content sink makes the `"`/`'` omission correct. The strongest convergent correctness result in the fourier corpus so far.
2. **`paperSearchIndex.ts:38-45/:271-283` — index-time `_lc` normalisation at exactly the right layer**, now externally corroborated: the producer's upstreamed module builds the identical bundle. (The tragedy adjudicated above: the fields it normalises so well are largely unreachable — FR-PS-FTS.)
3. **`:246-255` — `/g` regexes hoisted safely** (used only through `.replace`, which resets `lastIndex`) in a module whose OTHER module state does bite — deliberate, not lucky.
4. **`PaperSearch.vue:296-302/:111` — `min-height:0` beside `flex:1`, `overscroll-behavior: contain` on BOTH scroll containers.** Whoever wrote this knew the failure modes; none of it has ever applied. Two of the repo's six `overscroll-behavior` sites live here (FR-GFC-16 cross-ref), benefiting nothing.
5. **`usePaperSearch.ts:39-41` — the results watcher makes `results[selectedIndex]` total** — and the L-15 kill confirms D·S-2's reading over the challenge's: the cursor can never dangle, including on cache hits.
6. **The partitioned scroll watchers** (`Dropdown:19-28`/`Modal:27-37`) — the one place the `ffba307` split improved the design, with the caveat both readers converged on: the Dropdown half scrolls a container it no longer owns (MISS-A3), and the Dropdown's ref-based query is BETTER than the producer's own regressed global selector.
7. **The three-level typed focus chain** (`Input defineExpose → PaperSearch re-expose → MobileFloatingToc`) — the family's only imperative API, minimal, typed, and the reason mobile focus works despite the shared-state hazards.
8. **Corpus measurement discipline, verified not assumed**: D's WCAG cells reproduce to 2 d.p. on every recomputed cell (including against this seat's independent 1.708); L's P-3/P-4/P-8 probes reproduce exactly; three axes + two readers + this seat all land 43-of-48. Where the corpus failed it failed on cells and on TWO mechanisms (D-24's emission, the navigation id vocabulary) — the second being the reminder that a corpus can be arithmetically immaculate and still miss the dead click at the centre of the feature.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. D-2's pixel end-state: where the unstyled teleported overlay lands relative to the 100dvh shell; whether `modalInputRef.focus()` scroll-anchors the document. 2. D-3/D-4/D-5 rendered geometry: ToC displacement; whether 30 nowrap pills wrap or each overflows its own line; unbounded-label overflow. 3. MISS-A2's overflow resolution (~295 px demand / ~184 px box): clip vs overflow vs input auto-minimum shrink. 4. MISS-A3's scroll amplitude per ArrowDown (container resolution is structural). 5. L-3's two-modal witness: `document.querySelectorAll('.search-modal').length === 2` under mobile `searchActive`, and the stacked-`backdrop-filter` compositor cost once D-1 is repaired — **the D-1 and L-3 cures must land together or the fix is a compositor regression**. 6. `mobileTocVisible`'s live desktop value (spec-derived false; observed value pending). 7. D-8's dark-mode `MarkText`-on-`#0b0a09` severity — photograph, don't reason. 8. L-6/C-M6 frame cost: up to 30-60 Button teardown+mount cycles + full fuzzy re-scans per keystroke/mouseenter. 9. MISS-A4's cursor-drift perceptibility under `block:'nearest'`. 10. D-32's iOS `vh`/`dvw` divergence (mixed-unit fact proven; needs a real Safari session). 11. FR-PS-NAV needs NO live probe — it is decided statically — but the SS-13 pass should film the click-does-nothing for the repair's before/after delta.

## Closing verdict

**DEFECTIVE — twice over, in two different layers, found by two different generations of the audit.** The styling layer: a 43-of-48-dead scoped stylesheet (D-1) whose live remnant fails the one contrast check it can still take (MISS-A6), with four repair hazards pre-booked so the cure cannot ship a regression under a green gate. The behaviour layer: the component's central affordance — click a result, go there — is dead for every labelled theorem, figure, and numbered equation in the paper (FR-PS-NAV), while its highest-weighted field ranks exactly those results first; full-text search never worked (FR-PS-FTS); and the producer already ships a corrected core this family shadows (MISS-B3). Roster: **4 BLOCKER · 20 MAJOR · 18 MINOR · 7 INFO**; kills: **16 sustained**, three of them overturning a reader; routes: overwhelmingly **F.W3/W4** (shadow retirement + frontend audit, one atomic repair cluster: D-1 ∧ L-3 ∧ FR-PS-NAV ∧ producer adoption), with **F.W1** owning the token/contrast re-ink and the 2-file lucide rename, **F.W0** owning the build-unrunnable substrate row, **F.W5-W8 empty** (measured zeros), one **NO-WAVE-OWNER** bank (L-19), and three **glass-ui BH relay** obligations (the dead `--ui-glyph` register; the shared scorer defect; the producer's regressed global selector — plus the malformed-dist hygiene note).

— end of apotheosis r2. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED (M-25); the routings are agglomeration input to the forming X·F specs, and execution awaits the owner's begin-word.
