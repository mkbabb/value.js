claude-fable-5

# pt-packrat — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ `ef10d5b` (re-verified this session), `typescript/src/parse/packrat.ts` (488 lines) + its import closure and shipped dist · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: the reader's C-axis return (19 confirmed · 7 killed · 5 missed · 7 superlatives) over `audit/parse-that-modules/packrat/challenge-C-consumption.md` (Opus, 18 findings C-1..C-18) with its one cross-axis kill against `challenge-L-library.md` §8.3 (Opus r2, 24 findings — the sibling corpus, identity-mapped below, not re-adjudicated row-by-row here).
**Method (incredulity)**: every reader verdict that kills, contests, refines, or adds was re-derived against the bytes by this seat with its own instruments before adoption. Nothing was executed against parse-that — the PT-03 latch is one-way and arming poisons every later measurement seat; every dynamic claim below is a hand-executed static trace or an UNRUN-BY-LAW falsifier routed to §MEASURE-AT-OPEN. Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (all read-only: git/grep/sed/cat + one dependency-free `node -e` against value.js's own node_modules)**: full read of `packrat.ts` (whole, 488); `parser.ts:14-79` (entry surfaces), `:425-520` (`call`/`trim` whole), `:700-710` (`lazy`); `state.ts:43-57,92-99,148-153`; `lazy.ts:14-30`; `debug.ts:363-367`; `index.ts` + `packrat-entry.ts` + `core.ts` whole heads; `test/reentrancy.test.ts` (whole, 215) and `test/memoize.test.ts` (grep census + `:85-134` whole); `scripts/proof-packrat-cross-input.mjs:1-40`, `proof-packrat-armed.mjs:10-115`, `proof-no-dead-combinator.mjs:8-14,60-70`; `docs/api.md:95-110` (repo root — NOT `typescript/docs`); `README.md:303-312` + `git diff README.md` (dirty at one line, ~:347, outside every cited region); `CHANGELOG.md:14-20,37-43,100-122,136-141`; dist: `packrat.js` whole (3 names), `packrat.d.ts` whole (**6** export-declared functions + bare `export {}`), `packrat.d.ts.map` sources = `../src/parse/packrat.ts`, `packrat-entry.d.ts` (one line, 3 names), first lines of all 10 entry files ESM+CJS, `PACKRAT_ARMED` grep in both chunks (`:678/:682/:714/:722` ESM, `:679/:683/:715/:723` CJS), chunk internals (`getCijKey` :664, `mergeMemos` :847, `PARSER_ID` :850, byte size 40,576), chunk line 1 imports the diagnostics chunk; `package.json` (exports `./packrat` at :23-27 with types→`packrat-entry.d.ts` at :24, `sideEffects` :6, `files: ["./dist"]`); corpus: `X/parse-that/waves/W2.md` at `:253-258/:281-283/:335-337/:473-478/:784-788/:810/:962-964`, `W1.md:92-96/:430-460/:440-446`, `W3.md:285-295`; value.js: `parsethat-surface-gaps.mjs:10-60` (9-row census re-counted), `git show 164343c1 -- package.json` (deletion at hunk line 145), `src/css/index.ts` whole export census (**33 types + 19 runtime = 52**, ParseIssue/ParseResult present), `src/css/types.ts:8-30`, `src/subpaths/math.ts:2` + `transform.ts:4` (the absence-asserting prose), packed `@mkbabb/value.js@4.0.0` `dependencies: undefined`, `node_modules/@mkbabb/` = {glass-ui, keyframes.js, value.js}; bbnf-lang: root ls (Cargo workspace, `src/` absent), 3-line package.json stub, `package-lock.json:670-691` (parse-that@0.8.2 transitive), playground/extension first-party sweep (zero hits; extension hits are node_modules lodash noise).
**STOP/law compliance**: `/Users/mkbabb/Programming/parse-that-css-totality-p2` → `No such file or directory`; not created. No `.worktrees`, frozen root, or Codex path entered. parse-that touched read-only. **Sole write = this file.**

## Scoreboard

**19/19 confirmed rows SUSTAINED** (C-1, C-2 BLOCKER-of-record; C-3/C-4/C-5/C-6/C-8/C-14 MAJOR; C-7 and C-15 sustained at the reader's contested-down MINOR; 7 MINOR; C-13/C-§1 INFO) · **7/7 kills SUSTAINED** · **5/5 misses ADMITTED** (M2 at MAJOR) · **7 superlatives SUSTAINED** (S-1 carried with the L-file's range-only scoping) · **3 adjudicator corrections to the reader** (§CORRECTIONS — none verdict-changing).

Blocker-weight survivors: **C-1** (epoch soundness bound to the `parseState` boundary while `p.parser(state)`/`p.call(state)` are public and the library's own suite drives its soundness assertions through them — PT-B1 revived by proof-by-construction) and **C-2** (a one-way, process-global, construction-time arming with no public opt-out, conceded twice by the tree itself). Both are BLOCKER-of-record on a published 1.0.0; realized in-constellation impact is zero (C-§1: the consume edge is empty at every hop, and the dependency was *deleted* at `164343c1`).

## The incredulity seat's own work — reader challenged, three corrections, everything else survived

1. **C-3/K2 phrasing CORRECTED (verdict intact): "ALL seven in-repo call sites exercise only the no-op arm" is imprecise.** This seat re-read both test files whole. `memoize.test.ts:97` (`p.parser(st)`, memoized `P` inside a composite) and `:131` (`seeded.parser(st)`) are raw-entry calls that write cells into the **module-root** MEMO — no epoch opens, nothing drops them. The *next* `resetPackrat()` (the `:14` `beforeEach`, and any later in-test call) therefore clears **non-empty** root tables: real clearing of C-1's raw-entry residue, not a no-op. What survives, re-derived: (a) **no call site can reach the crash arm** — none runs with a parse on the stack; (b) **none is load-bearing** — every residue cell lives under a fresh test-local `PARSER_ID`, so no later lookup can alias it, and the reset-tax probe's two assertions cannot distinguish a hit from a cold re-parse regardless (L-M7's vacuity, mechanism re-derived by this seat at `:218-230/:244-249`). Corrected statement of record: *the suite's `resetPackrat()` calls include non-vacuous clears of raw-entry residue; none is load-bearing; the crash arm has zero coverage anywhere.* This is also the in-tree realization of the L-file's L-M1 Arm C — the export's only meaningful work is janitoring the unsound path — which neither the reader nor the C challenge stated exactly.
2. **M1 count CORRECTED: `dist/packrat.d.ts` declares SIX exports, not "five".** Re-read whole: `getCijKey` (:2), `packratEnter` (~:46), `packratExit` (~:57), `resetPackrat`, `memoize`, `mergeMemos`, then bare `export {}`. The reader's proof cell says "five" and then lists all six names — a prose slip, not an evidence error. The finding itself is sustained and is real: the `.d.ts`/`.js` basename pair under `dist/packrat.*` is emitted from **different source modules** (`.d.ts.map` sources = `src/parse/packrat.ts`; the `.js` is the vite build of `packrat-entry.ts`), both ship under `files: ["./dist"]`, and a node10-resolution deep import typechecks against 6 names and throws at runtime against 3. The L-file's §8.3 kill ("types and runtime **agree**") is right at the exports-map level and wrong at the artifact-pair level — the reader's refutation of that kill is SUSTAINED, with the count fixed.
3. **Provenance census CORRECTED: 14 dirty tracked rows, 12 under `rust/`** (11 M + 1 D) plus `.cargo/config.toml` and `README.md` — the reader said "13 dirty `rust/` paths". Substance intact: **zero under `typescript/`**, every cited file byte-identical to HEAD, and README's sole diff is one reference line (~:347) outside the cited `:303-312` region.
4. **C-1's evidence base EXTENDED (verdict unchanged)**: the reader's cell cites only `reentrancy.test.ts:203-211` as the suite's raw entry on a memoized parser. `memoize.test.ts:97` and `:131` are two more — the (id,offset)-soundness assertion and the reset-tax assertion are *both* measured on the un-epoched path (the L-file's §2.2 table had this; the reader's C-1 cell did not). The PT-B1 revival trace itself was **re-walked step for step by this seat** against the full module read: parse 1 writes the root MEMO (`:437→:450→:463`, recall return at `:335`, `HEADS.get` at `:308`), parse 2 hits the stale cell → `applyAnswer` → `"hello"`. Sound.
5. **Sustained without residue** (each re-derived, not spot-trusted): K1/K2 — `outer`/`a`/`b` at `reentrancy.test.ts:42/:62-63` are non-memoized, every memoized parse before `:197` rides `.parse()`, `letter` is minted at `:203` with a virgin id and the two probes use distinct offsets (0 vs 2²⁰); K3 — `:266` fires only when **unarmed**, the C challenge's "no-op in exactly the state where disarming would be needed" was inverted; K4 — bbnf-lang is a Cargo workspace with no `src/` (the challenge's grep was vacuous), the stub package.json trivially clean, the lockfile carrying parse-that@0.8.2 only transitively, first-party JS clean — conclusion stronger on the corrected evidence; K5 — `W2.md:335-337` is AC-3 prose ("…deposed combinator baseline… tempts a `hostFn` node…"), the scar row is at `:962-964` verbatim; H-ESM-DUAL — all ESM entries import the single `packrat-entry-CS1td-8B.js` chunk (parse/core/packrat directly, utils at line 2), CJS mirrors on `46NYx4_U`, so C-7's split is exactly and only ESM-vs-CJS; M3 — `.flags` written exactly once repo-wide (`parser.ts:496`) on `flaggedParser`, which `.trim()` builds and never returns (returns the `whitespaceTrim` parser at `:514-517`), so `Parser.flags === 0` universally, `call()`'s `:438` fast path makes `p.call ≡ p.parser`, C-1's four-row table is three code paths, and each `.trim()` mints two ids and burns one against C-8's ceiling; M2 — `parseState(val: string)` / `parse(val)` both construct a fresh state at offset 0 (`parser.ts:52`), no positioned overload exists, `packratEnter` is exported from neither entry, so a positioned parse **has no epoched path** — the library's own >1MB soundness test needs `st.offset = span` by hand. (Honesty rider this seat adds: `p.parse(src.slice(k))` exists as a workaround, but it re-keys every cell to offset-0 — it cannot express the distinct-offsets-one-keyspace property the test exercises, and it forfeits span fidelity. M2 stands, at MAJOR.)

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec rather than landing in a unit · **NO-WAVE-OWNER** where no X·P wave owns it (incumbent-repo repair; evidence rides the standing O-15 letter channel, owner-held). X·P executes nothing until the owner's begin-word; every routing below is a booking, not an act.

| id | sev | claim (compressed) | proof anchor (adjudicated) | terminal disposition |
|---|---|---|---|---|
| **C-1** | **BLOCKER** | Epoch soundness bound to the `parseState` boundary; PT-B1 reachable via public `p.parser`/`p.call`; the suite itself uses the raw entry (3 sites, §5.4) | `parser.ts:30,43,47,437`; `state.ts:47-53,92-99`; static PT-B1 trace re-walked; `reentrancy.test.ts:203-211`; `memoize.test.ts:97,131`; gate blind: `proof-packrat-cross-input.mjs` uses `.parse()` only | **W2** — O-8/K-6 own the class: memoization becomes a parse parameter, no boundary to bypass. W1 rider: a stray raw entry in a harness accumulates root MEMO (one-process law, `W1.md:94-96`). Falsifier → MEASURE-AT-OPEN #1 |
| **C-2** | **BLOCKER** | One-way, process-global, construction-time arming; no public opt-out; conceded at `proof-packrat-armed.mjs:12-21` + `CHANGELOG.md:106-111` | `packrat.ts:156,:290,:217,:266` — the `true` write is the first statement of `makeMemoized`, before the parser is even bound; both bundles 4 lines each, byte-cited | **W2** — G-8's pasted born-RED baseline (`W2.md:784-788`), O-8's named counter-example (`:253-258`), scar row 4 (`:962-964`). W1 rider: G-4 + one-fresh-process-per-cell. Ratio re-measure → MEASURE-AT-OPEN #3 |
| **C-3** | MAJOR | Public `resetPackrat()` can crash a live grow (three `!` on a clearable Map with user code on the stack) and is non-load-bearing everywhere in-tree; header still instructs the call | `packrat.ts:15,262-272,396,401,455`; `.map`/`.chain`/`.mapState`/`recover` under `evalParser :295-300`; §CORRECTIONS 1 phrasing of record | **W3** — the disarm-and-refuse-mid-grow ruling already written (`W3.md:289-292`, "must actually disarm rather than only clearing"). Crash-arm falsifier → MEASURE-AT-OPEN #2 |
| **C-4** | MAJOR | The only consumer docs show `.memoize()`/`.mergeMemos()` as `Parser` methods; the class has neither (25 prototype properties, free functions only) | `README.md:305-310`; `docs/api.md:101` (repo root; sited between `recover` :95 and `chain` :106); `packrat.ts:482` | **NO-WAVE-OWNER** — incumbent doc repair, owner-held; evidence rides the O-15 letter channel |
| **C-5** | MAJOR | The one api.md semantics sentence is false three ways: LR-seed hits (initialised `isError:true`), synthetic never-cached ε, evict-and-recompute on lookup | `docs/api.md:103-104` vs `packrat.ts:443-448,470-472` / `:315,:329` / `:349-359`; `mergeMemos`/`resetPackrat`/`./packrat` documented nowhere | **NO-WAVE-OWNER** — same channel as C-4 |
| **C-6** | MAJOR | Zero-consumer tier frozen into a permanent 1.x subpath against the tree's own dead-code precept; the stated retirement condition (bbnf-lang) is already met — and met *harder* on the corrected evidence (K4) | `proof-no-dead-combinator.mjs:10-12,63-68`; `package.json:23-27`; `CHANGELOG.md:106-111`; bbnf-lang census §5 (K4); M4: the freeze binds the ROOT barrel too (`index.ts:8`) | **W2** — the KEEP/WRAP/RETIRE table's RETIRE verdict owns the capability decision for the CSS lane; the package-side retirement act is the owner's (major bump on TWO surfaces per M4), booked, not performed |
| **C-7** | MINOR (contested down, **sustained**) | Dual-package hazard: two latches, two `PARSER_ID` counters, memo keys with no copy discriminator; no gate can fail for it | ESM `:678/:850` vs CJS `:679/:851`; all four proof gates load `dist/parse.js` only; reachable arm = the split latch (mixed require/import); id-collision arm contrived — `getCijKey` keys on the INNER parser (`:292,:437`), re-verified | **REFINEMENT FOLD** — W1 cell law gains "one module format per cell"; W4's release condition gains "no dual-instance state in the shipped artifact". Witness → MEASURE-AT-OPEN #4 |
| **C-8** | MAJOR | In-parse `RangeError` off an unresettable, unobservable process-global counter (K-8 class); burn rate doubled by `.trim()`'s discarded parser (M3) | `packrat.ts:90-98`; `parser.ts:18,25`; no export/reset on any entry; `parser.ts:492-517` | **W3** — totality unit: the budget breach becomes a `ParseIssue` value (K-8 `W2.md:477-478`, R-LAW-3 `:281-283`); the landing type already ships frozen in the successor's own `/css` surface (S-7) |
| **C-14** | MAJOR | `mergeMemos` ≡ `memoize`; `name` reaches only the debug printer and one `"whitespace"` compare; served plural vs recorded singular `"mergeMemo"` | `packrat.ts:479,482-488`; `debug.ts:365`; `parser.ts:488`; `state.ts:151`; sole use `memoize.test.ts:58` asserts nothing distinguishing | **W2** — the closed-enumeration discipline (`W2.md:139-142` class): an operator set cannot carry two names for one operation. Identity probe rides MEASURE-AT-OPEN #10 |
| **C-15** | MINOR (contested down, **sustained**) | The lazy-armed first parse permanently retains its memo cells and whole source string in the module root (epoch snapshots it by reference forever after) | `lazy.ts:20-22`; `parser.ts:43,702-707`; `packrat.ts:217,219,225-229,244-245,433-435`; retention, never a wrong answer; zero consumers | **REFINEMENT FOLD** — sharpens O-8's phrasing to the L-fold's "no global whose value is a function of construction history"; the mid-parse-construction window is the sharpest witness. Heap witness → MEASURE-AT-OPEN #5 |
| **C-9** | MINOR | `./packrat` is a name zone, not a payload zone: 40,576 B chunk for 3 functions (packrat region 5,533 B ≈ 13.6%, `:664→:847`); `./core` structurally cannot shed it (`parser.ts:7`) | chunk measured; `dist/core.js:1`; `sideEffects:false` at `package.json:6` | **W4** — payload-zone truth as a release condition on the successor's artifact (nominal-vs-physical zoning gated, not asserted) |
| **C-10** | MINOR | `getCijKey` public in an unreachable `.d.ts`; `PackratEpoch` unnameable (bare `export {}`); sole consumer deep-imports source | `packrat.ts:79,196`; `dist/packrat.d.ts` (6 exports, §CORRECTIONS 2); `memoize.test.ts:3`; no `./dist/*` wildcard | **W4** — types/runtime parity gate, jointly with M1 |
| **C-11** | MINOR | Module header ships pre-PT-Q1 guidance ("resetting the caches per parse via resetPackrat()") that C-3 makes hazardous | `packrat.ts:13-15` vs `:173-181` + `parser.ts:43-48` | **NO-WAVE-OWNER** — incumbent prose; O-15 channel |
| **C-12** | MINOR | No parse- or parser-scoped primitive on any published surface; vocabulary is arm/cache/clear **globally** | `index.ts:8`; `packrat-entry.ts:5`; `packrat.ts:216,243` unexported; M2 is the same gap from the consumer side | **W2** — the parse-parameter design IS the scoping primitive; subsumed by C-1's cure |
| **C-16** | MINOR | `core.ts` promises isolation from packrat and diagnostics; the build falsifies both (core→packrat chunk→diagnostics chunk); the gate checks `typeof` only | `core.ts:3-6`; `dist/core.js:1`; chunk line 1; `subpath-gate.mjs:26-56` re-read | **REFINEMENT FOLD** — W4's subpath gate spec: assert payload isolation, never file-existence + `typeof` |
| **C-17** | MINOR | value.js's own RED-7 instrument carries `row(!ARM ? false : false, …)` — unconditionally `ok`, can never fire (L-19 violated in the instrument); 9-row census re-counted by this seat | `parsethat-surface-gaps.mjs` ARM block (rows 6-7 of 9) | **NO-WAVE-OWNER** in X·P — value.js-side one-line instrument cure, booked to megatranche instrument hygiene; the file is otherwise frozen evidence |
| **C-18** | MINOR | Semver notes spend the "Type ripple" on an unreachable surface; the served behaviour change (resetPackrat no-ops on the documented entry) is filed under "Performance" | `CHANGELOG.md:18,39-43`; C-10 | **NO-WAVE-OWNER** — incumbent CHANGELOG; O-15 channel |
| **C-13** | INFO | The RED-7 probe is unrunnable in this workspace (`@mkbabb/parse-that` unresolvable by construction); its packrat rows are inherited O-15 receipts | imports `:11-13`; `grep -c parse-that package.json` → 0; `node_modules/@mkbabb/` = 3 dirs | **NO-WAVE-OWNER** — recorded; consistent with `W2.md:193` scope |
| **C-§1** | INFO | Consumption ground truth: the consume edge is empty at every hop; the dependency was **deleted** (`164343c1`, hunk line 145), the packed release carries zero packrat bytes, the 52-export `/css` surface is parse-that-free, and future consumption is ruled to zero | all re-verified by this seat (§instruments) | **W2** — the evidence floor under G-10 (`memoize = 0`, `W2.md:810`) and the RETIRE verdict |

**Misses admitted into the roster** (reader's, all sustained): **M2** (MAJOR → REFINEMENT FOLD: W2's candidate API must either publish an epoched positioned entry or rule the use case out in writing — the incumbent's documented surface is *incapable* of it, which re-prices C-1's cure from doc-line to surface); **M1** (MINOR → REFINEMENT FOLD: W4 artifact-parity clause; incumbent cure `@internal`, owner-held); **M3** (MINOR → REFINEMENT FOLD: W2's enumeration discipline — dead flag machinery is the counter-example class; riders cashed into C-1 and C-8); **M4** (MINOR → REFINEMENT FOLD: G-10's sweep must bind the ROOT barrel, not only `./packrat` — the default surface arms the latch); **M5** (INFO → NO-WAVE-OWNER: `proof-packrat-cross-input.mjs:7-8` justifies the gate by two consumers that provably do not exist — the O-15 channel carries it).

**Sibling L-axis corpus, identity-mapped so nothing is lost or double-booked**: L-B2≡C-1 · L-M1≡C-3 · L-M6≡C-2 · L-m9≡C-14 · L-m6≡C-10/M1 (its §8.3 kill overturned as stated, reader sustained) · L-m4≡C-8 · L-m8 rides C-2's evidence (the "mid-teens %" comment vs the measured 1.47×). L-only rows — **L-B1** (the ε-substitution admits strings outside the grammar; hand-traced twice in the L file, this seat re-read the guard `:315-331` and confirms the positional-only predicate cannot see the requesting rule), L-M2 (NaN/negative offsets bypass the key guard — pure arithmetic, reproducible anywhere), L-M3/L-M4 (PLAUSIBLE, instrumentation-gated), L-M5 (diagnostics loss), L-M7 (vacuous named gate — mechanism re-derived by this seat under §CORRECTIONS 1) — **retain their challenge-file standing**; their dynamic falsifiers are routed to §MEASURE-AT-OPEN, and L-B1 + L-M2 sharpen the W2/W3 spec the same way C-1/C-8 do (a candidate with a structural involved-set guard and an `Number.isSafeInteger` boundary is the cure class).

## Killed-claims register — 7 sustained, with this seat's re-derivations

| id | killed claim | adjudication |
|---|---|---|
| C-1(sub) | "the >1MB test is correct only because of the manual `resetPackrat()` at :197" | **KILL SUSTAINED** — `letter` minted at `:203` with a virgin id; distinct offsets; root MEMO empty at `:197` (all prior raw entries non-memoized, all memoized parses epoched). Re-derived whole-file. |
| C-3(sub) | "`reentrancy.test.ts:197` is load-bearing" | **KILL SUSTAINED** — same evidence; and the strengthened universal is now itself corrected (§CORRECTIONS 1): non-load-bearing everywhere, but not "no-op" everywhere. |
| C-2(sub) | "`:266` early-return = no-op exactly where disarming is needed" | **KILL SUSTAINED** — logic inverted in the challenge; armed state falls through and clears; the one-wayness stands on `:290` + both bundles. |
| C-6(sub) | bbnf-lang cleared by a grep over a nonexistent `src/` and a dependency-free stub manifest | **KILL SUSTAINED (evidence), finding strengthened** — Cargo workspace; lockfile carries parse-that@0.8.2 transitively; first-party JS clean; the Rust port omits packrat entirely (`packrat.ts:12`). |
| C-2(cite) | scar row at `W2.md:335-337` | **KILL SUSTAINED** — `:335-337` is AC-3 prose; the row is at `:962-964`, byte-checked. |
| L-8.3 | "types and runtime **agree**" (L-file kill of the API-leak claim) | **REFUTATION SUSTAINED as rescoped** — agreement holds at the exports-map level (S-5) and fails at the artifact-pair level (`dist/packrat.d.ts` = 6 declarations from `packrat.ts`; `dist/packrat.js` = 3 exports from `packrat-entry.ts`; both ship). Count corrected 5→6. |
| H-ESM-DUAL | reader's own hypothesis: an ESM-internal dual instance | **KILL SUSTAINED** — all five ESM entries share one chunk; recorded to the reader's credit for the honest tally. |

**Adjudicator's corrections register** (this seat's, against the reader — none overturns a verdict): (1) the C-3 "only the no-op arm" universal, corrected to *non-load-bearing, crash-arm-uncovered, with non-vacuous residue clears*; (2) "five exports" → **six** in `dist/packrat.d.ts`; (3) dirty census 13 → 14 rows / 12 under `rust/`. A fleet-evidence note per the ColorSpaceSelector precedent: all three are prose-precision faults, not citation-inheritance — each of the reader's underlying evidence cells re-derived clean.

## Superlatives — sustained, with scoping

**S-1** float64-safe memo key, derivation written into the source, fail-loud at the ceiling (`:52-98`) — carried with the L-file's scoping: exactly tight on the integer **range**, not total over NaN/negative/fractional offsets (L-M2). **S-2** the five-field epoch is provably set-equal to the five module globals (`:196-202` vs `:133-135,:186,:193` — re-checked), snapshot/restore under `try/finally` at the one call site, `growLR` mirroring internally. **S-3** arming at construction is a theorem, not a race (`:290` before `p` binds); its one hole is C-15, filed and contested down honestly. **S-4** `proof:packrat-armed` proves its own non-vacuity (Map subclassed before dist import; poison child in a separate process; the flake-trap refusal written down) — and is the tree's own confession of C-2. **S-5** the `./packrat` **type routing** is deliberate and correct: `package.json:24` → the one-line 3-name `packrat-entry.d.ts`, so the nameable surface equals the callable surface — this is what holds M1 at MINOR. **S-6** the zero-consumer state is provable by manifest, packed artifact, and *absence asserted in the source it is absent from* (`math.ts:2`, `transform.ts:4`) — re-verified. **S-7** the X·P "wrap the throw into a value" verdict has an already-shipped, frozen landing type: `ParseIssue`/`ParseResult` on value.js's 52-export `/css` surface (`types.ts:10,:25-27` re-read) — the W3 conversion is re-plumbing, not invention.

## MEASURE-AT-OPEN residue — bench-dependent rows for the lane's W1 harness

Law inherited whole: **one fresh process per cell** (`W1.md:94-96`), G-4's never-armed clause re-specified as the retained-heap form (the L-file's §7 correction — no arm-state accessor exists on any published surface, so "read from the dist" can only be the `proof-packrat-armed` Map-count mechanism), and **every probe below is a poison cell**: it arms the latch or dirties root tables, runs in a disposable PID, and its PID never reappears in a timing cell. None was run in this session (UNRUN-BY-LAW; the latch is one-way).

1. **PT-B1 revival (C-1/L-B2)** — fresh process: `const w=memoize(regex(/[a-z]+/)); const a=new ParserState("hello"); w.parser(a); const b=new ParserState("world"); w.parser(b);` — prediction `b.value === "hello"` (this seat's static trace says so); `"world"` demotes C-1 to doc-gap MINOR.
2. **Mid-grow `resetPackrat()` crash (C-3)** — a `.map` calling `resetPackrat()` inside an LR grow; prediction: `TypeError` off the `:396` non-null assert; no throw refutes the crash arm.
3. **The latch ratio (C-2)** — re-measure UNARMED vs ARMED vs after-reset on the W1 rig; the **ratio and one-wayness** are the claim of record (O-15's 93.9/138.2/139.3 vs W1's 55.6 unarmed — absolute ns non-portable, `W2.md:772-774`).
4. **Dual-copy witness (C-7)** — one process, `import` dist/parse.js + `require` dist/parse.cjs: overlapping `new Parser().id` ranges confirm the collision precondition; then the split-latch arm (copy-A parseState, copy-B memoizer) in a second disposable process.
5. **Lazy-armed retention (C-15)** — heap snapshot around `Parser.lazy(() => memoize(inner))`'s first `.parse()` of a large source; prediction: the source string stays reachable from the module root for process lifetime.
6. **L-M3 seed identity** — instrument `:396` to assert the fetched cell is the installed/last-stored one; run `mSL`. Identity everywhere ⇒ MINOR (defensive); failure ⇒ CONFIRMED and the `:48-50` termination comment comes down.
7. **L-M4 nested head** — instrument `:381` with a `HEADS.has(pos)` throw; full suite. Silence ⇒ MINOR + `prevGrowing` dead; throw ⇒ BLOCKER-class.
8. **L-M7 counter-probe** — `memoize(string("ab").map(v=>{n++;return v}))`, parse, unrelated parse, raw-entry re-probe; prediction: `n` increments (the "cache hit" is a cold re-parse; the named gate is vacuous).
9. **L-i1 memoized depth ceiling** — deepest-OK for `memoize(Parser.lazy(...))` vs the 7,761 baseline; prediction: materially lower.
10. **C-14 identity rider** — `memoize(p)` vs `mergeMemos(p)` over the math corpus in the same poison cell; byte-identical results predicted.
11. **L-B1 ε counterexample** — the four-line `H = H R | 'x'; R = H 'z'` grammar over `"xz"`; prediction per the L-file's twice-walked trace: spurious accept `["x",[undefined,"z"]]` consuming the whole input. `"x"`@1 or an error refutes the sibling BLOCKER in full.

## Closing verdict

The reader's return **survives the incredulity seat intact**: 19/19 confirmations sustained (both severity contests — C-7, C-15 — sustained at MINOR with the reasoning adopted), 7/7 kills sustained on re-derivation, 5/5 misses admitted (M2 the consequential one: the documented surface cannot express a positioned parse, so C-1's "disciplined consumer" escape does not exist for that consumer class), 7 superlatives sustained. Three prose-precision corrections were needed and none moved a verdict. The module's shape is unchanged from both challenges' concord: **an above-median implementation of a capability with zero consumers, whose entire served surface reduces to one real operator behind a one-way process-global latch, published and frozen at 1.0.0 the moment its sole declared downstream deleted the dependency and ruled `memoize = 0`.** X·P's algebra keeps two ideas (the key arithmetic, the epoch shape — S-1/S-2/S-8) and retires the surface; the tree's own written retirement condition is met (C-6, strengthened by the corrected bbnf-lang evidence); every consumer-visible defect routes to W2 (laws/enumeration/RETIRE), W3 (disarm + totality), W4 (artifact parity, payload truth), the REFINEMENT FOLD (six spec-sharpeners: C-7, C-15, C-16, M1–M4), or NO-WAVE-OWNER via the owner-held O-15 letter channel (the incumbent's prose: C-4, C-5, C-11, C-18, M5, plus C-13/C-17 records). Eleven dynamic rows are banked to MEASURE-AT-OPEN under W1's poison-cell law. Nothing lost: head, tail, or interval.

— end of apotheosis. Both challenge files and the reader's return remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
