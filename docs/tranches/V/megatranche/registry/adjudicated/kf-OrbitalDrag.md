Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component OrbitalDrag; receipt read from this seat's own system prompt, not inherited).

# kf-OrbitalDrag — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-12 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale), `origin/master` `81a56990`, worktree DIRTY · **Registry tree** value.js `tranche-u` · **Corpus**: two fresh hostile re-reader returns over `audit/kf-components/OrbitalDrag/challenge-{D-design (380 L), L-library (293 L), C-consumption (449 L)}.md` (all three `claude-opus-5[1m]`) — **reader-DU** (design-and-usage: 21 confirmed rows · 3 kills · 7 misses · 8 superlatives · 6 unproven) and **reader-LC** (library-and-code: 33 confirmed rows · 4 kills · 10 misses MISS-1..10 · 7 superlatives · 6 unproven). Both returns consumed whole; all three challenge files re-read whole by this seat.

**Substrate (re-run by this seat, decisive for four rulings).** `git log -1` → `8281638c`; `git rev-parse origin/master` → `81a56990`; **`git diff origin/master --stat -- demo/scenes/cube/ demo/composables/ demo/state/` → EMPTY** — the entire audited surface (orbital-drag/ whole, CubeTarget.vue/.css, CubeScene.vue, matrix-editor/useTransformState.ts, useDoubleTap.ts) is byte-identical worktree ↔ `origin/master`; every anchor below binds to both. The only worktree drift near the blast surface is uncommitted deletions in `demo/utils/{formatEditorCSS,helpers,keyframeSelector}.ts` (−75 L, none cited). Manifest at `origin/master` (my `git show`): `dependencies: { "@mkbabb/value.js": "4.0.0" }` (:70), `devDependencies: { "@mkbabb/glass-ui": "7.0.0" }` (:77), `gl-matrix ^3.4.4` devDep (:87) — the worktree's glass-ui deletion is the banked F-1 residue (KF.W0 §B-12), and one reader cell dies on it (K15).

**Method (incredulity).** Every reader-vs-reader disagreement, every kill of a load-bearing cell, and every filed miss was re-derived at the bytes before adoption; every claimed-new row was checked against the banked registry FIRST. Zero browser tooling; product source read-only; **sole write = this file**. Instruments this session: whole reads of OrbitalDrag.vue (352 L), useOrbitalPointer.ts (249 L), useOrbitalPinch.ts (201 L), useOrbitalInertia.ts (144 L), index.ts (117 L), quaternionEuler.ts (64 L), useDoubleTap.ts (84 L), CubeTarget.vue (239 L); CubeScene.vue:55-80/:240-256; matrix-editor/useTransformState.ts:190-220; useSceneMachineShellBinding.ts:190-300 (`onStartStateChange` :261-266); useAnimationGroupPlayback.ts:40-80/:115-130 (`syncPlayState` :67-72); useAnimationGroupActions.ts:50-75 (`reset()` → `getGroup().stop(); syncPlayState(false)` :55-61); src/animation/group/lifecycle.ts:155-200 (`settle` :171 `group.started = false`; `reset` paints children to frame 0; `stop` :190-194); CubeTarget.css:14-85 (the `.cube--rolling` comment :18-23, the T.G3 verdict, the G5 law :50-60); CubeAxisLines.vue:37-75; useCubeDemo.ts:155-170 (the house PRM contract); useControlsKeyboardShortcuts.ts:28-72 (the one-registry law verbatim; Escape/R :51-52; Mod+Z/Mod+Shift+Z :70-71); useTypedTrigger.ts:5-23 (the editable-target law + guard); useDragCapture.ts:14-27/:45-65 (the twin deferred registration + false comment); gestureSelectSuppression.ts:1-6 + design-idioms.css:73-79 (`.focus-ring` = "the SINGLE keyboard-focus affordance") + :286-300; glass-ui installed 7.0.0: `dist/keyboard.js` editable predicate (`isContentEditable || e.closest(".monaco-editor")`) **and** the matcher's `e.code.toLowerCase() === a` layout fallback, `dist/composables/dom/useDragVelocity.d.ts:26` (`respectReducedMotion?: boolean` — "Default true"); cursor-idiom census grep (6 sites verified: SquareScene.css:63/:84, AmigaScene.vue:255/:269 + rationale :251, SequenceTarget.css:133/:169, TimelineTrack.vue:68/:244, AnimationVisualizer.vue:22 live `isDragging` binding); sibling ARIA-posture grep (SpringTarget.vue:65-70, SquareScene.vue:49-56 + `sr-only-slider`, SequenceScrubber.vue:22-27, SequenceTarget.vue:99-104); `registerShortcut` census (23 hits / 3 files); test/demo/scenes/orbital-inertia-parity.test.ts:1-6/:29 and orbital-rotate3d.test.ts:150-169 (both gates read verbatim); `isPlaying`-writer grep (none for CubeScene's ref). Registry instruments: **kf-CubeTarget.md r2 whole** (rows #1, #6, #34, #52, #53, #54, #55, register #18-25), **kf-CubeScene.md whole** (L-2/C-5, L-4+M-6, L-19/C-13, D-8+M-7, AmigaScene-MISSED-F fold, kill 1), **kf-CubeAxisLines.md r2** (KF-AX-1, KF-AX-2, KF-AX-10/α-3), kf-MatrixEditor.md r3 (the K22-K30 precedent laws), ColorSpaceSelector.md (format + the scope law).

## Scoreboard

**44 adjudicated roster rows — 0 BLOCKER · 14 MAJOR · 23 MINOR · 7 INFO** (ids OD-1..OD-44, original ids for life; **10 net-new rows/limbs admitted from the readers' 17 filed misses**, 3 more admitted as riders, 4 booking-killed or folded to banked identities). **Killed-claims register: 15 entries.** **11 superlatives** (one partially killed by a banked row). **11 UNPROVEN-NEEDS-LIVE residues** → SS-13.

**Headline.** The corpus's genuinely new yield is the **gesture-physics family**: the inertia hand-off **discards the gesture's direction** on the Z-roll and pinch paths (OD-12 — release a leftward Ctrl-drag and the cube glides back the other way), the axis-locked rotation takes its **magnitude from both axes and its sign from one** (OD-13 — full-drag-magnitude rotation whose sign is set by cross-axis jitter), and the v-model **never emits `update:modelValue`** (OD-14 — the contract works for exactly one consumer shape). All three passed this seat's independent re-derivation. The record's biggest *event*, though, is the registry striking four reader cells in one corpus: reader-LC's loudest miss (the "pre-Play Euler painter") is **killed by the banked kf-CubeTarget #53** — the alternate painter has never painted (K1, the kill-inheritance law's third firing); reader-DU's loudest miss (the double-tap Roll dead under pointer capture) was **banked five days earlier as kf-CubeTarget #1** (K8); reader-LC's MISS-6 (`isPlaying` has no writer) is **banked verbatim as kf-CubeScene L-2/C-5** (K7); and reader-LC's phantom-glass-ui superlative cell re-read a dirty worktree manifest — the **fourth** library-side unnamed-baseline event across four corpora (K15). The failure is never derivation; it is an unread registry and an unnamed tree.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for the SS-1/SS-2 spec authoring, keyed to the census wave sketch KF.W0–W10, on the cube scene-repair packet whose spine is kf-CubeScene's routing block.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **Reader-LC MISS-4 — "pre-Play, the drag renders through useTransformState's LOSSY Euler painter onto `.cube`; the quaternion render is the started-state render only"** | **KILLED AS FILED ≡ kf-CubeTarget #53 (banked BLOCKER, 2026-08-07); the superlative-scope correction survives, sharper** | The cited painter never paints: `transformTargetsStyle` is handed a `Matrix3dCall` **object** the renderer skips (`compile/value/compile.ts:280` at origin — #53's decided chain, re-checked against my read of useTransformState.ts:197-215, whose call site is real and whose write is dead). Pre-Play, the drag renders **nothing** — no pose paint at all; only `faceLit` re-shades. Reader-LC verified the call site, not the write — the exact failure kf-CubeTarget register #18 minted as law ("a kill is only as dead as its mechanism — verify the write, not the call site"), now fired against a *miss*. What survives is stronger than the reader filed: SUP-1/SUP-2's "the render path" is the **started-state render only**; in the default state the celebrated quaternion path paints nothing because its gate is closed and the alternate painter is a no-op. Fold ≡ #53; no new row. |
| 2 | **Reader-DU missed #2 — "the manual orbit is discarded on transport Stop and snaps back on the next pointer move"** | **DISCARD CONFIRMED (this seat re-walked the full chain); the restore-on-move clause KILLED by the same #53; fold ≡ kf-CubeTarget #6** | Chain verified end-to-end at the bytes: ribbon/Escape/R → `reset()` → `getGroup().stop()` (lifecycle.ts:190-194 → `settle` :171 `started = false`; `reset` paints started children to frame 0) → `syncPlayState(false)` → `emit("startStateChange", false)` (useAnimationGroupPlayback.ts:67-72) → App.vue:37 → `onStartStateChange` → `sceneRef.value.isStarted = false` (useSceneMachineShellBinding.ts:261-266) → CubeTarget.vue:14 → `containerStyle` returns `{}` (OrbitalDrag.vue:64) → Vue's style patch **clears the transform**. But the "snaps back on the next pointer move" clause cites the #53 no-op painter — post-Stop, pointer moves repaint **nothing**; the orbit reappears only at the next Play, as a snap. The corrected row is the sharpest concrete symptom of the banked **kf-CubeTarget #6** ("the container… switched by `isPlaying \|\| isStarted` with **no hand-off or clear**") — folded there as a sharpening rider, with reader-DU's design credit kept (the two gates are exact complements; the handoff is missing a repaint, not a guard). |
| 3 | **L·M-4 (stale pinch baseline ⇒ scale jump) — reader-DU CONFIRMED MAJOR vs reader-LC KILLED** | **Reader-LC's kill SUSTAINED; reader-DU's confirmation REFUTED** | My own read: `startTouchPinch` (useOrbitalPinch.ts:84-91, registered at OrbitalDrag.vue:283) re-seeds ALL THREE baselines on **every** `touchstart` with `touches.length >= 2` — a normal next pinch always re-seeds before any `touchmove`; and `handleTouchPinch`'s first guard is `isTouching`, which after a `pointercancel` reset (useOrbitalPointer.ts:203-210) is false until a path that re-seeds runs. Reader-DU's cell ("the next pinch's first frame computes against a baseline from a different gesture") never engages `startTouchPinch` at all. The axis's premises (no `touchcancel`; the cancel branch skips `onStopDrag`) are true and consequence-free on the stated path. Residual (reader-LC's, adopted): a second finger landing **outside** the container gives the container `touchmove` with two touches but no two-finger `touchstart` — INFO-latent, geometry-dependent → SS-13 #6. |
| 4 | **L·B-1 severity + case set (reader-DU BLOCKER vs reader-LC MAJOR; two case kills in opposite directions)** | **MAJOR (reader-LC's grade); BOTH case kills SUSTAINED — they kill different overreaches** | Gate re-verified: the freeze is reachable only while `applyTransformToContainer` is true (started state — CubeTarget.vue:14, and per ruling 1 the pre-start state has no pose render at all). Algebra re-derived from quaternionEuler.ts:29-46: pure-Y and pure-Z give `r12 = yz − wx = 0` ⇒ `ex = atan2(-0, r22) = -0` every frame (one +0→−0 invalidation, then never again); **pure-X gives `r12 = −wx ≠ 0`** ⇒ `ex` advances ⇒ reader-LC's kill of the axis's "axis-locked drag" universal is right (X lock re-renders). Reader-DU's kill of case 4 is also right: general pinch writes tracked translate/scale keys every `touchmove` (useOrbitalPinch.ts:109-113), so only the degenerate pure-rotation limit freezes. Surviving inputs: Ctrl/⌘ Z-roll, Y lock, Z lock, canonical-axis glides, and a pure-Y-accumulated horizontal free drag. Severity: the registry's BLOCKER bar (kf-CubeTarget #1/#2/#53 = entire paint paths dead) is not met by a mode-subset freeze on a reachable-but-secondary surface; reader-DU's BLOCKER dissent recorded. **Composition (this seat's own): B-1 + #53 ⇒ the Ctrl/⌘ Z-roll has NO state in which it visibly turns the die past frame 1 — and its on-screen signature in both states is identical (motionless pose, faces re-shading via `faceLit`, which reads the full rotate triple reactively while the computed tracks only `rotate.x`). One SS-13 probe covers both rows.** |
| 5 | **L·B-2/C-1 and D-1 BLOCKER grades (reader-DU) vs MAJOR (reader-LC)** | **Both MAJOR** | B-2/C-1: proven-fatal, held shut by accident (every barrel consumer `import type`; zero runtime consumers of the `OrbitalDrag` value export) — the latent-fatal class grades MAJOR per the registry bar (kf-MatrixEditor K4/ME-17 precedent); both readers' compiled-SFC proofs concordant and adopted. D-1: the structural facts are exactly as filed (zero keyboard transform path — my trace concurs; MatrixEditor gated to one channel surface), and reader-DU's five-sibling house-law census is REAL (re-verified: SpringTarget.vue:65-70, SquareScene.vue:49-56, SequenceScrubber.vue:22-27, SequenceTarget.vue:99-104, `.focus-ring` = design-idioms.css:76-79) — but the owner-disposition bar that held ME-9 and killed K19's BLOCKER applies to a demo scene's direct-manipulation surface, and the 2.1.1 shape is already registered at **kf-CubeAxisLines KF-AX-10 (α-3)** with "cure upstream with KF-AX-1's registry adoption" — i.e. upstream = HERE. MAJOR, house-law sharpening adopted, cross-booked. |
| 6 | **The latch family vs the registry (neither reader consulted it)** | **D-3/C-4 ≡ kf-CubeAxisLines KF-AX-1 (banked MAJOR — "window-latch bypasses the demo's ONE-registry law; cure upstream in orbital-drag/"); this record IS the upstream owner's full mechanism; both readers' filings recorded as independent re-derivations, not new rows** | KF-AX-1 re-read; the one-registry law re-verified verbatim (useControlsKeyboardShortcuts.ts:30-32); the glass-ui registry's editable predicate AND `e.code` layout fallback verified in `dist/keyboard.js`. The two admitted riders extend the banked row: **(a)** reader-DU missed #3 — the app's own `Mod+Z`/`Mod+Shift+Z` registrations (:70-71) carry `event.key === "z"`, so every Undo/Redo keystroke latches `pressedKeys.z` and lights the Z axis line (chain source-proven: OrbitalDrag.vue:276 → useOrbitalPointer.ts:169-175 → deep watch :317-321 → CubeTarget.vue:159-163 → `--axis-active`); reachability class upgraded from "editor co-mounted" to "press ⌘Z", no macOS-swallow assumption needed for the flash; **(b)** reader-DU missed #4 — `event.key` with no `event.code` fallback makes the spatial X/Y/Z binding unreachable on non-Latin layouts, while all 18 registry bindings are layout-robust. One cure (registry adoption) discharges guard + discoverability + layout + the ⌘Z collision. |
| 7 | **D-10 (resident `will-change`) — reader-DU killed the axis premise and re-filed MAJOR; reader-LC RAISED to MAJOR** | **FOLD ≡ kf-CubeTarget #34 (banked MINOR, sustained r2); both MAJOR re-grades die (K6); reader-DU's lifetime correction ADOPTED against the banked cell** | #34 stands on the same G5 law both readers cite (CubeTarget.css:50-60, re-read). But #34's carried cell "the gate latches for the session" is now CORRECTED: this seat verified the full Stop chain (ruling 2) — `isStarted` collapses on every transport Stop/Escape/R, so the hint's lifetime is the started window, not the session. Reader-DU's kill of the axis's "isStarted never returns to false" premise is thereby sustained WITH its own re-filing demoted to the banked grade. The strengthening both readers add (kf-CubeScene L-2/C-5: `isPlaying` never written ⇒ `.idle-hover.playing .cube` is dead ⇒ the resident hint is the only live promotion) is already in #34's r2 sharpening. |
| 8 | **D-7 (wheel/zoom trap) — reader-DU mis-attribution kill vs reader-LC full kill** | **Killed as filed against this component (concordant); stage half ≡ AmigaScene MISSED-F (banked house-wide, cube included); component residue booked MINOR (OD-35)** | Reader-LC's ground is decisive: `@wheel.prevent` sits on BOTH full-bleed ancestors (CubeTarget.vue:5, CubeScene.vue) — gating the component's `preventDefault` restores nothing. Reader-DU's footprint correction (the OrbitalDrag root is shrink-to-fit; the die footprint, not the stage) concordant. What the component owns: the unconditional `preventDefault` first statement (useOrbitalPointer.ts:138) + the `ctrlKey` branch re-purposing browser zoom to cube scale (:160-163). MISS-10 (touch-action declared 3×, `user-select` 2×) corroborates "no single owner" and rides as the row's evidence cell. |
| 9 | **MISS-6, reader-DU missed #1, and L·M-5 — three pre-banked filings** | **BOOKING/SEVERITY KILLS: MISS-6 ≡ kf-CubeScene L-2/C-5 (third derivation); missed #1 ≡ kf-CubeTarget #1 (the Roll trigger-dead BLOCKER — reader-DU's pointer-capture retargeting derivation logged as an additional independent proof, with its useDoubleTap three-consumer disjointness census adopted as new evidence on the banked row); L·M-5 ≡ kf-CubeTarget #54 (banked MINOR by ruling 7 there: the leaked doc handlers self-heal at the next pointer release — BOTH readers' MAJOR grades die on the banked demotion)** | All three checked at the banked rows this session. Reader-DU's "all three axes read both files and missed it" is true of the corpus and false of the registry — the K24 pattern's second component. The L·M-5 rider ADMITTED: `useDragCapture.ts` carries the identical deferred registration (:57-64) under the identical false `tryOnScopeDispose` comment (:19-21, my read) — so "adopt the house seam" does NOT fix the class; `useDragScrub` remains the honest form. Extends #54. |
| 10 | **C-2 severity (reader-DU MAJOR vs reader-LC MINOR); the m-4/C-8 cure; MISS-2's breadth** | **C-2 MINOR (dead-API is the finding; ~7 small literals/frame is not MAJOR cost); the annotation cure KILLED (reader-LC's TS7053 derivation sustained — `Object.entries` string keys still cannot index the tuple-keyed type under `strict`+`noUncheckedIndexedAccess`; the true cure iterates the `axes` tuple, collapsing the neighboring casts too); MISS-2 NARROWED (the shaking manifests on drags whose dominant component is not the sign-source component — the aligned gesture is stable; and under Y/Z locks B-1 hides it from the container render)** | Each re-derived at the bytes; the diagnosis halves all survive. |

**Adjudicator's corrections register (against this corpus's readers):** (1) reader-LC's MISS-4 as filed + reader-DU's missed-#2 restore clause — rulings 1-2 (kill-inheritance, third firing); (2) reader-DU's L·M-4 confirmation — ruling 3; (3) reader-DU's three BLOCKER grades — rulings 4-5; (4) both readers' L·M-5 and D-10 grades — rulings 7, 9; (5) reader-LC's SUP-B glass-ui-phantom cell — K15 (dirty manifest, fourth event); (6) reader-DU's SUP-2 generalization ("the contrast maths check out") — partially killed by banked kf-CubeTarget #58 (the LOCKED Y axis line fails 1.4.11's 3:1 in the light arm ≈2.7:1; reader-DU re-derived X only); (7) both readers' failure to consult the registry before filing "new" rows — rulings 1, 9.

## Adjudicated defect roster — id · severity · claim · terminal disposition

Routing law: **KF.W0–W10** per the census wave sketch where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake, agglomerating into the **cube scene-repair packet** (spine = kf-CubeScene's routing block). Anchors bind to worktree ≡ `origin/master`; re-anchor before any cure.

### MAJOR

| id | merged ids | claim (compressed; the reader returns carry the full cells) | terminal disposition |
|---|---|---|---|
| **OD-1** | L·B-1 | `containerStyle`'s only rotate dep is `void model.value.rotate.x` (:68); `currentQuaternion` is non-reactive; canonical-axis rotations write `rotate.x = -0` every frame ⇒ the container transform freezes after frame 1. Surviving inputs (ruling 4): Ctrl/⌘ Z-roll, Y lock, Z lock, canonical-axis glides, pure-Y-accumulated horizontal drag; X lock and general pinch KILLED. Started-state only; composed with #53 the Z-roll is visible in NO state | **NO-WAVE-OWNER** — gesture-math family; cure = track all three keys or version-bump a `shallowRef`; born-RED gate: mount started, Ctrl-drag, assert the `rotate3d` angle advances past frame 1 (the text gate OD-29 cannot see this) |
| **OD-2** | L·B-2 / C-1 | `index.ts:3` re-exports the SFC before `:63` declares `defaultTransformState`; `defineModel({ default })` compiles to a module-scope read ⇒ TDZ `ReferenceError` on any barrel-first entry (native ESM), silent `default: undefined` under rolldown/Vite-SSR. Held shut by accident: every barrel consumer is `import type`. Both readers compiled the real SFC independently — concordant | **NO-WAVE-OWNER** — factory default (`() => structuredClone(defaultTransformState)`) kills OD-2 + OD-8 in one edit; OD-16 completes the composables' cycle exit; the dead guard :47-49 deletes |
| **OD-3** | D-1 (+ KF-AX-10/α-3 cross-book) | A transform manipulator with zero keyboard operability, no name/role/state (bare `<div>`; grep aria-/role=/tabindex in the module → 0), keyboard alternative gated to one channel surface (CubeScene.vue:170-181) — while FIVE sibling scene stages carry full slider/group ARIA posture + `.focus-ring` (re-verified census, ruling 5). WCAG 2.1.1 shape, owner-disposition-bounded | **NO-WAVE-OWNER** — one cure family with OD-4/OD-6 (the registry adoption gives arrow-key nudge bindings a home); ≡ KF-AX-10's α-3 limb, owned here as the upstream component |
| **OD-4** | D-3 / C-4 / L·M-3a **≡ kf-CubeAxisLines KF-AX-1** | Window-bound keydown/keyup with no editable-target guard, no `event.repeat`/`code` handling, against the in-repo one-registry law (:30-32) and the shipped guard (glass-ui `keyboard.js` predicate; useTypedTrigger.ts:16-23). Riders (r-new, ruling 6): **the app's own ⌘Z/⌘⇧Z registrations latch the Z axis and light the axis line on every Undo/Redo**; `event.key`-only matching makes the spatial binding unreachable on non-Latin layouts (the registry matcher accepts `e.code`) | **FOLD ≡ KF-AX-1 (banked MAJOR; "cure upstream in orbital-drag/")** — this record is the upstream mechanism roster; cross kf-CubeTarget #13/#18 (the consumer-side latch rows). One cure: route the latch through `registerShortcut` (guard + modal + layout + ⌘Z collision discharged together) |
| **OD-5** | D-2 / C-5 / L·M-3b | The x/y/z latch has no reset but a matching keyup: window blur/app-switch strands a silent single-axis lock (the latch is the FIRST branch of both `drag` :117 and `handleWheel` :155); `syncModifiers` (:62-66, called first in both) self-heals only shift/ctrl/meta; no `defineExpose`, so the parent has no escape (contrast CubeTarget.vue:129). macOS ⌘-keyup swallow stays a severity input, not a premise (SS-13 #1) | **NO-WAVE-OWNER** — same registry cure (KF-AX-1 ships keyup handling) + a blur/visibilitychange latch clear; cross kf-CubeTarget #13/#18 |
| **OD-6** | D-4 | The ten-mode gesture vocabulary (table re-verified line-by-line) registers nothing in the house registry and is structurally absent from the derived shortcuts modal; the sole reveal (CubeAxisLines) fires only after the key is held — confirms, never teaches. Provenance corrected: 23 `registerShortcut` hits across THREE files (one a prose mention) | **NO-WAVE-OWNER** — one motion with OD-4; cross KF-AX-2 (the reveal's false semantics — one reveal-truth family with OD-31) |
| **OD-7** | L·M-1 | `updateAxisRotation` writes only `angularVelocitySpeed` (:157), never `angularVelocityAxis` (writers: :136-137, pointer :128, pinch :124 — nowhere else); and :157 arms unconditionally even when `Math.sign(0) = 0` made `applyRotation` a zero-angle no-op (:113) ⇒ release glides about a stale orthogonal axis after a gesture that visibly did nothing | **NO-WAVE-OWNER** — one hand-off spec with OD-12/OD-13: the inertia hand-off carries the SIGNED angle and the true axis, or arms nothing |
| **OD-8** | L·M-2 / C-6 **≡ kf-CubeTarget #52** | `defineModel({ default: defaultTransformState })` hands out the shared mutable module singleton (`type: null`, non-function default — compiled-props proof, both readers; `matrix` is a live `mat4.create()` Float32Array); the guard beneath is shallow AND dead; `velocity` ten lines later is correctly `structuredClone`d — the tell | **FOLD ≡ #52** (banked at the consumer's seat as INFO-latent). **This record grades the component-side row MAJOR-latent** — new coupling the #52 seat did not have: the same default literal is OD-2's TDZ read, so one factory-default edit cures both, and the latent-fatal API-trap class (C-1/C-3) grades MAJOR-latent at the owning surface. Tension recorded, not hidden; the SS-1/SS-2 authoring re-grades at family level |
| **OD-9** | L·M-6 / C-3 | `inertiaFactor` is a bare `number?` fed unvalidated into `decay()` at setup: `1` (the obvious "spin forever") and `>1` throw synchronously (`decay.ts:66-68`, documented `@throws`) and take the scene down; `<0` gives NaN past the `k <= 0` guard and silently poisons the transform string. The `(0,1)` precondition lives only in inertiaDecay.ts:31 | **NO-WAVE-OWNER** — clamp/validate at the prop seam + document the range; the parity gate never exercises the boundary (OD-29) |
| **OD-10** | D-5 | `cursor: move` violates the six-site house grab/grabbing idiom (census re-verified, incl. AnimationVisualizer.vue:22's live `isDragging` binding — the exact pattern free here), names the wrong verb (unmodified drag ROTATES; translate is the Shift path), and binds no pressed state though `isDragging` is live and thrice-consumed | **NO-WAVE-OWNER** — presentation arm of the family cure |
| **OD-11** | D-6 | The post-release inertial glide (per-frame JS via `useRafFn`; no CSS for a cascade PRM rule to catch) honours no `prefers-reduced-motion` in the scene that names and enforces "the house reduced-motion contract" (useCubeDemo.ts:159-169, sole PRM line in the cube tree); the installed design system's own drag primitive ships `respectReducedMotion` **default true** (useDragVelocity.d.ts:26, verified); `inertiaFactor`'s limit degrades to an instant stop — the knob exists | **KF.W9** — PRM mechanism unification; extends the banked kf-CubeScene D-8+M-7 tally: the cube's ungated motions are 4 engine + **this demo-side rAF glide = 5** |
| **OD-12** | **NET-NEW** — reader-LC MISS-1 | **The Z-roll and pinch-rotate inertia hand-offs discard the gesture's direction**: the applied angle is correctly signed (:126 `* Math.sign(deltaX)`; pinch `deltaAngle`), then the hand-off stores the fixed +Z axis and `Math.abs(angle)` (:128-129; pinch :124-125), and `applyInertia` (:98) applies a **positive** rotation about +Z forever ⇒ releasing a leftward Ctrl-drag or clockwise twist makes the cube reverse. The free-orbit path is correct precisely because it puts direction IN the axis (:124, :136-138). Re-derived at the bytes; masked in part by OD-1's freeze (the reversal shows in `faceLit` shading and post-Play state) | **NO-WAVE-OWNER** — one hand-off spec with OD-7/OD-13; born-RED gate: Ctrl-drag left, release, assert `rotate.z` decreases |
| **OD-13** | **NET-NEW** — reader-LC MISS-2 (narrowed, ruling 10) | **Axis-locked rotation takes magnitude from BOTH deltas and sign from one, quantized to ±1** (:142 `sqrt(dx²+dy²)`, :154 `Math.sign(...)`): a misaligned locked drag produces full-drag-magnitude rotation whose sign flips with cross-axis jitter — visible shaking on any drag whose dominant component is not the sign source (the mode has zero teaching surface — OD-6 — so misaligned drags are the ordinary case). OD-7's zero-sign corner is this row's limit case; the sibling translate/scale branches already use the signed dominant delta (:195) — the rotation branch is the odd one out | **NO-WAVE-OWNER** — same spec: derive the angle from the signed relevant component |
| **OD-14** | **NET-NEW** — reader-LC MISS-3 | **The component never emits `update:modelValue`** — every model write is an in-place property mutation (:91-93, :172, :178; no `model.value =` anywhere) while the compiled emits list advertises the event: the explicit `:model-value`/`@update:model-value` form never fires; a plain non-reactive object gets silent mutation and no re-render; only a deep-reactive parent-owned object (the one shipped shape) works. Distinct from OD-8 (the default) — this is the write path | **NO-WAVE-OWNER** — model-contract family with OD-2/OD-8: either assign through `model.value` (triggering the emit) or document the mutate-in-place contract and drop the pretense |

### MINOR

| id | merged ids | claim (compressed) | disposition |
|---|---|---|---|
| **OD-15** | D-10 **≡ kf-CubeTarget #34** | Resident `will-change: transform` (:73) on the preserve-3d ancestor whenever the container renders — against the in-repo G5 law 40 lines from the consumer's binding (CubeTarget.css:50-60) and the recorded T.G3 verdict. Banked-cell CORRECTION adopted (ruling 7): the lifetime is the **started window** (Stop clears it), not the session; and (≡ kf-CubeScene L-2/C-5) it is the only promotion that ever fires | **FOLD ≡ #34 (MINOR per bank; both readers' MAJOR dissents recorded)** — drop the hint at `useOrbitalInertia`'s :125-128 all-stopped moment |
| **OD-16** | reader-LC MISS-5 (**net-new**) | `axes` is a runtime value import used only in type position in all four files (verified: OrbitalDrag.vue:18→:141/:168/:185/:189; pointer :5→:16-18; pinch :5→:14-15; inertia :5→:31/:110); under `verbatimModuleSyntax` each composable carries a real runtime edge into the cycling barrel for a type `import type` provides | **NO-WAVE-OWNER** — `import type { axes }` in the three composables deletes their OD-2 cycle participation; cheaper and more complete than the factory default alone |
| **OD-17** | L·m-1 / C-7 / D-R-2 | Five of six props snapshotted at setup into consts (:53-58), permanently non-reactive; `applyTransformToContainer` alone is live (:64); nothing marks the split. Evidence correction rides: half of the consumer's binding expression is a compile-time constant (≡ kf-CubeScene L-2/C-5) | **NO-WAVE-OWNER** — document init-only or thread reactively |
| **OD-18** | L·m-2 | `bounds.rotate` declared, typed, defaulted (index.ts:28-32/:83-87), never applied — clamping covers only translate/scale (:170-179) | **NO-WAVE-OWNER** hygiene |
| **OD-19** | L·m-3 | `VelocityState.rotate` dead — every consumer loop is `["translate","scale"] as const`; rotational velocity migrated to axis+speed and the vestige stayed | **NO-WAVE-OWNER** hygiene |
| **OD-20** | L·m-4a / C-8a (cure corrected, ruling 10) | `defaultTransformBounds` (index.ts:82) is the one unannotated default ⇒ members infer `number[]` not tuples ⇒ the `as unknown as` double-cast (:175; five casts in 17 lines). The annotation-only cure is KILLED (TS7053); the real cure iterates the `axes` tuple | **NO-WAVE-OWNER** — annotate AND iterate `axes`; one commit with OD-13's rewrite |
| **OD-21** | L·m-5 | `handleAxisSpecificInput` repeats one 5-line block 3× with `axes` already imported; `updateAxisRotation`'s array parameter is dead generality (three single-element call sites; :157 would be wrong if the loop ever ran twice) | **NO-WAVE-OWNER** — dies in the OD-13 rewrite |
| **OD-22** | L·m-6 + reader-LC MISS-8 (**net-new limb**) | One seam, two symptoms: grabbing a coasting cube leaves the rAF loop ticking a no-op for the whole drag (:82-83 early-return; sole `pause()` downstream of it; the watcher only resumes) — inverting the scene's own no-idle-cost posture — and the early-return skips the timestamp update, so release computes `dt = min(now − stale, 100)` = 100 ms ⇒ factor 0.735 instead of 0.950, compounding the deliberate ×0.8 into a ~41% momentum cliff. Both readers' narrowing concordant: unreachable on a cold first drag | **NO-WAVE-OWNER** — pause on gesture-start or reset `lastInertiaTime` in the early-return |
| **OD-23** | L·m-7 | `useOrbitalPinch` requires `model: Ref<TransformState>` (:9), omits it from the destructure, never reads it; OrbitalDrag.vue:235 passes it anyway — puncturing the ownership rule stated at :160-164 | **NO-WAVE-OWNER** hygiene |
| **OD-24** | L·m-8 | Composable return surfaces ~half unconsumed (pointer 12→7; pinch `previousPinchDistance` unread; inertia `resume` external-unconsumed) — counted by reader-LC, spot-checked | **NO-WAVE-OWNER** hygiene |
| **OD-25** | L·m-9 | `event.pointerType !== "touch" || size === 0` (:198): a mouse/pen up while a finger is down removes ALL three shared doc listeners; the surviving touch is unheard; `isDragging`/`isTouching` stick true; inertia early-returns forever. Hybrid-device-narrow — MINOR is right | **NO-WAVE-OWNER** — latch/pointer seam row with kf-CubeTarget #54/#55 |
| **OD-26** | L·m-10 / C-9 | Comment-as-contract failures around allocation: `decay.ts:53-54` promises "allocation-free per call" and returns an object literal both branches (~60 discarded `DecaySample`/s through the glide); useOrbitalInertia.ts:69 repeats it verbatim; the seat reduces provably to `Math.exp`; `decayRest` exported and unconsumed; in-unit "zero-alloc" beside `quat.create()` per pointermove | **NO-WAVE-OWNER** — library-side doc correction (kf src) + the consumer comment; the trajectory math is right (no behavior change) |
| **OD-27** | reader-LC MISS-7 (**net-new**) | The echo-guard watcher (:305-312) re-derives a full Euler triple (`quaternionToEulerDegrees` — one allocation, asin + 2×atan2 + ~20 multiplies) on every one of the component's OWN rotation writes purely to discover it is an echo; `isInteracting` (:87) already is the we-are-the-writer flag | **NO-WAVE-OWNER** — flag-guard the watcher; rides the OD-1 cure commit |
| **OD-28** | L·m-11 | Emit suppression asymmetric: `isInteracting` gates only `emit("rotate")` (:95-97); translate/scale emit unconditionally on every frame (:181-182) — if the iOS cascade rationale is real, the higher-frequency offenders are ungated | **NO-WAVE-OWNER** — one emit policy with C-2 (OD-30's evidence) |
| **OD-29** | C-14 / L·m-12 + L·I-5 / C-14b | The parity gate pins `INERTIA_FACTOR = 0.92; // the orbital-drag default` vs the shipped 0.95 (:56, no consumer override) and imports the deep source path, not the barrel the component uses; the rotate3d gate's clause (c) is `readFileSync` + regex over the SFC source (verbatim re-read) — green under text-preserving regressions, red under behavior-preserving refactors: exactly the hole OD-1 lives in | **KF.W4 rider** — demo gate truth (with kf-MatrixEditor ME-32's gate row); recalibrate + barrel-import + a mounted reactivity assertion |
| **OD-30** | C-2 (MINOR per ruling 10) | Three of four emits have zero consumers repo-wide and fire ≤7 dispatches + 7 spreads per rAF frame through the glide (the `isInteracting` gate is OPEN while coasting — verified); the data already flows through v-model | **NO-WAVE-OWNER** — delete or gate; the dead-API half is the finding |
| **OD-31** | D-8 | Shift→translate and Ctrl/⌘→roll/scale have no feedback in any state: all six keys emitted, consumer wires x/y/z only, no cursor/class/data-attribute | **NO-WAVE-OWNER** — reveal-truth family with KF-AX-2 |
| **OD-32** | D-9 | Scoped bare `div` selector compiles to `(0,1,1)` and out-ranks consumer utilities — a caller's `cursor-grab` (the OD-10 cure) loses without `!important`; fragile to any second element; contrary to the standing root-styling posture | **NO-WAVE-OWNER** — class-based root selector; rides OD-10 |
| **OD-33** | D-11 | The interaction's entire feel is twelve untokenized magic numbers across three files (every row re-verified in this seat's whole-file reads); the module's own inertiaDecay.ts shows the correct derive-and-name pattern on exactly one of thirteen | **NO-WAVE-OWNER** — constants module with derivations; feeds the OD-7/12/13 spec |
| **OD-34** | D-12 | 46 comment lines / 53 blank in the 334-line script (~14%); the P.W5.S3 egg narrative told 3×, the reverse-path rationale 2×; tranche-ledger register (`P.W5.S3`, `inv ζ`, `F.W10.S1`, `L.W8 ED-3`) in the library's shop window | **NO-WAVE-OWNER** — prune to load-bearing rationale (census S-2 class) |
| **OD-35** | D-7 residue (ruling 8) | Component-owned half only: unconditional `preventDefault` as `handleWheel`'s first statement + the `ctrlKey` branch re-purposing browser page-zoom/trackpad-pinch to cube scale | **NO-WAVE-OWNER**; the stage-wide scroll/zoom trap ≡ **AmigaScene MISSED-F** (banked house-wide, rides KF.W9's audit) |
| **OD-36** | reader-DU missed #5 (**net-new**) | `handleTouchPinch` is the only input path with NO deadzone (drag 0.5px :115; wheel 1e-4 :148; Safari gesture composite 1e-4 :168-174): a resting two-finger touch continuously jitters scale + translate + z-roll simultaneously, with `atan2` angular noise scaling as 1/finger-separation, and each frame arms the jitter into inertia (:125); no gesture-dominance arbitration. Small extra (this seat's): the `previousPinchAngle.value !== 0` sentinel conflates "unset" with a legitimate 0 rad angle | **NO-WAVE-OWNER** — composite deadzone + dominance policy; one spec with OD-12's pinch limb |
| **OD-37** | reader-DU missed #6 (**net-new**, honest scope kept) | The demo has an UNDOCUMENTED second drag tier: the 3D manipulators (OrbitalDrag's hand-rolled capture lifecycle :181-233; amiga's useSphereSpin) sit outside the one named gesture-in-flight authority (gestureSelectSuppression.ts:1-5 + useDragScrub/useDragCapture + the single CSS rule) — the non-participation is where kf-CubeTarget #54's bug class lives. Practical select-leak small here (`preventDefault` + `user-select: none`) | **NO-WAVE-OWNER** — either document the tier or adopt the token; NOT "OrbitalDrag defected alone" |

### INFO

| id | claim | disposition |
|---|---|---|
| **OD-38** | C-11 — the v-model contract requires `matrix: mat4` the component never touches, dragging the devDep `gl-matrix` into the public type surface (devDep status verified at `origin/master:package.json:87`) | **FOLD ≡ kf-CubeScene L-19/C-13** (the dead matrix field, banked sharper — the Float32Array alias); this record adds the devDep-type-surface rider |
| **OD-39** | L·I-1 / C-12 — nine listeners registered inside `onMounted`, scope-covered only by Vue ≥3.4's `setCurrentInstance` grace (verified in the installed runtime by both readers, concordant); `onUnmounted(() => inertia.pause())` redundant (`useRafFn` self-disposes) | **NO-WAVE-OWNER** — record the guarantee at the call sites or hoist |
| **OD-40** | C-13 — "consumes the published surface" is true at the module graph (the vite/tsconfig alias unify realms) and false at the artifact (the demo never loads dist/); the comment overstates by one level | **NO-WAVE-OWNER** — comment correction |
| **OD-41** | Uncontested corpus tail, spot-verified: L·I-2 (`slot in pressedKeys.value` walks the prototype chain — currently unreachable, `Object.hasOwn` closes it; :173 verified) · L·I-4 (`containerStyle` mutates `renderAxis` from inside the computed — impure but single-evaluation-consistent; :70 verified) · L·I-3's portable-graph half (the unit's own imports are glass-free) — **its npm-ci consequence is SUBSTRATE-DEAD** (F-1 falsified at origin; kf-CubeScene kill 1) | **NO-WAVE-OWNER** hygiene; I-3's residue ≡ census F-1 → KF.W0 §B-12 |
| **OD-42** | reader-LC MISS-9 (**net-new**) — type drift inside the four-file unit: inertia declares the injected `applyRotation`/`angularVelocityAxis` as `Float32Array` (:27-28) where pointer/pinch declare `vec3`; both compile; one contract, two spellings, three files | **NO-WAVE-OWNER** hygiene |
| **OD-43** | reader-LC MISS-10 (**net-new**) — `user-select: none` declared twice (component :349 + consumer's `select-none` on the same element) and `touch-action: none` three times over nested boxes (:350, CubeTarget.vue:4, CubeScene inline) — the no-single-owner evidence for OD-35 | **NO-WAVE-OWNER** hygiene; rides OD-35 |
| **OD-44** | reader-DU missed #7 + D-R-4 — the comment-asserts-a-mechanism-that-does-not-exist family, third and fourth instances in one scene: `.cube--rolling { pointer-events: none }` does not "stand the pointer down" (the ancestor OrbitalDrag root still receives the pointerdown and drives the quaternion through the whole roll arc; the only thing it suppresses — the double-tap — is already dead ≡ kf-CubeTarget #1); the CubeAxisLines "PRM-respecting via the wrapper below" comment ≡ kf-CubeTarget #50 (banked); the `tryOnScopeDispose` comment ≡ #54 (banked) | **FOLD** — the rolling-flag observation rides kf-CubeTarget's roll-stack repair as a comment-truth rider; nothing re-booked |

## Killed-claims register — 15 entries

| # | killed claim | adjudication |
|---|---|---|
| **K1** | Reader-LC MISS-4 — "while the group is not started, the drag is painted through the LOSSY Euler triple and its pole branch" | **KILLED ≡ kf-CubeTarget #53** (ruling 1): the cited painter has never painted (object-skip, both trees, banked BLOCKER). The reader verified the call site, not the write — the #18 kill-inheritance law's third firing, this time against a miss. The superlative-scope correction survives sharper: pre-start, NOTHING renders the pose |
| **K2** | Reader-DU missed #2's restore clause — "the orbit… snaps back on the next pointer move" | **KILLED on the same #53 ground** (ruling 2): post-Stop pointer moves repaint nothing; the true restore edge is the next Play. The discard core is CONFIRMED by this seat's full-chain walk and folds ≡ kf-CubeTarget #6 |
| **K3** | Reader-DU's L·M-4 confirmation ("the next pinch's first frame computes against a stale baseline… scale jump", MAJOR) + the axis's L·M-4 as filed | **KILLED** (ruling 3): `startTouchPinch` re-seeds all three baselines on every 2-finger touchstart; the confirmation never engaged it. Residual outside-finger topology → SS-13 #6, INFO-latent |
| **K4** | Reader-DU's three BLOCKER grades (D-1, L·B-1, L·B-2) | **DEMOTED to MAJOR** (rulings 4-5): owner-disposition bar (ME-9/K19 precedent), latent-fatal bar (K4/ME-17 precedent), registry BLOCKER bar (whole-paint-path-dead) |
| **K5** | Both readers' MAJOR grades on L·M-5 (doc listeners out of scope) | **SEVERITY KILL ≡ kf-CubeTarget #54's ruling 7** (banked MINOR: the leaked handlers self-heal at the next pointer release). The useDragCapture twin-bug rider ADMITTED as new (ruling 9) |
| **K6** | Both readers' MAJOR (re-)grades on D-10 (resident will-change) | **SEVERITY KILL ≡ kf-CubeTarget #34** (banked MINOR, sustained r2 with the same G5 sharpening); reader-DU's lifetime correction adopted INTO the banked cell (Stop clears `isStarted` — this seat's chain walk) |
| **K7** | Reader-LC MISS-6 as a new finding (`isPlaying` has no writer; dead CSS/class/param) | **BOOKING KILL ≡ kf-CubeScene L-2/C-5** (banked MAJOR, verbatim including all three dead consumers); third independent derivation recorded |
| **K8** | Reader-DU missed #1 as a new finding ("the Roll is STRUCTURALLY DEAD… all three axes missed it") | **BOOKING KILL ≡ kf-CubeTarget #1** (banked BLOCKER, survived two hostile re-reads). True of the corpus, false of the registry. The capture-retargeting derivation + the three-consumer useDoubleTap disjointness census logged as additional independent evidence on the banked row |
| **K9** | D-7 as filed against OrbitalDrag (the stage scroll/zoom trap) | **MIS-ATTRIBUTION KILL, concordant** (ruling 8): the trap is owned twice over the component's head (`@wheel.prevent` on both full-bleed ancestors; the root is shrink-to-fit) and the stage half ≡ AmigaScene MISSED-F (banked). Residue = OD-35 |
| **K10** | The axis's L·B-1 case 3 as universal ("axis-locked drag" freezes) | **Reader-LC's algebra kill SUSTAINED, re-derived** (`r12 = −wx ≠ 0` for pure-X ⇒ the X lock advances every frame); only Y and Z locks freeze |
| **K11** | The axis's L·B-1 case 4 (two-finger pinch freezes) | **Reader-DU's kill SUSTAINED**: pinch writes tracked translate/scale keys every touchmove; only the degenerate pure-rotation limit freezes |
| **K12** | The m-4/C-8 prescribed cure ("annotate `index.ts:82` and the double cast collapses") | **Reader-LC's cure kill SUSTAINED**: `Object.entries` string keys cannot index the tuple-keyed type under `strict` + `noUncheckedIndexedAccess` (TS7053); the true cure iterates the `axes` tuple. Diagnosis kept (OD-20) |
| **K13** | Reader-LC MISS-2's breadth ("visible shaking on every axis-locked drag") | **NARROWED** (ruling 10): aligned drags take sign from the dominant component; the shaking manifests on misaligned drags — which the zero-teaching-surface finding (OD-6) makes the ordinary case; and under Y/Z locks OD-1 hides it from the container |
| **K14** | Reader-DU's C-2 MAJOR grade | **DEMOTED MINOR** (reader-LC's ground adopted): ~7 three-key literals per frame is not MAJOR cost; the dead-API half is the finding |
| **K15** | Reader-LC's SUP-B cell — "the phantom glass-ui, which I re-verified absent from BOTH dependencies and devDependencies" | **SUBSTRATE-DEAD**: the absence is the uncommitted worktree deletion; `origin/master:package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` exact (my `git show`). The FOURTH library-side dirty-manifest event across four corpora (kf-CubeScene reader-B, kf-CubeTarget r1 reader-B + r2 reader-β, now this reader) — the banked law re-fired: *name the tree before quoting a manifest*. SUP-B's value.js half survives (4.0.0 in `dependencies` at origin, verified) |

## Identity-guard folds (never re-booked)

- **OD-4 ≡ kf-CubeAxisLines KF-AX-1** (banked MAJOR; "cure upstream in orbital-drag/") — this record is the upstream owner's mechanism roster; both readers' filings = independent re-derivations. Cross: kf-CubeTarget #13/#18 (consumer-side latch rows).
- **OD-3's 2.1.1 limb ≡ KF-AX-10 (α-3)** — one identity, owned here as the upstream component.
- **OD-8 ≡ kf-CubeTarget #52** (grade tension recorded transparently) · **OD-15 ≡ #34** (with the lifetime-cell correction) · **L·M-5 ≡ #54** (+ the useDragCapture rider) · **reader-DU missed #1 ≡ #1** · **missed #2's core ≡ #6** · **MISS-4's residue ≡ #53** · **missed #7's PRM/rolling comments ≡ #50 + the roll stack**.
- **MISS-6 ≡ kf-CubeScene L-2/C-5** · **OD-38 ≡ kf-CubeScene L-19/C-13** · **OD-35's stage half ≡ AmigaScene MISSED-F** · **OD-41's I-3 residue ≡ census F-1/SCH-1 → KF.W0 §B-12**.
- **OD-11 extends kf-CubeScene D-8+M-7** (the PRM tally: 4 engine motions + the glide = 5) — extension, not re-book; rides the same KF.W9 row.
- **OD-1/2/7/9/12/13/14/16/22/27/36/42/43: first bookings** — each checked against kf-CubeScene (49 rows), kf-CubeTarget (#1–#60), kf-CubeAxisLines (KF-AX-1..32), kf-MatrixEditor (ME-1..45), and census S-1..S-8/F-1; no prior row states the dep-registration freeze, the TDZ cycle, the direction-discarding hand-off, the magnitude/sign split, the missing model emit, or the setup-throw prop.

## Superlatives — 11 (provenance: corpus → readers → this seat)

1. **The started-state render path is genuinely SOTA** (D SUP-1 / L SUP-2, both readers concordant, re-verified): ONE `rotate3d()` off the quaternion's native axis-angle into a hoisted out-param, delta-multiply + normalize accumulation, the Euler pole degeneracy confined to the v-model surface — **with the #53 scope correction: it is the started-state render ONLY; the default state renders no pose at all**. The credit and OD-1 are the two halves of one mechanism: the hand-registered dep is elegant, and it is one third of what it needs to be (reader-DU's both-halves reading, adopted).
2. **`inertiaDecay.ts` — a seam carved so its own falsifier could reach it** (C S★4 / both readers): Vue-free by stated design, the k-mapping derived in prose, the parity gate importing exactly it; the one entry into the unit that is load-order-safe by construction. Survives OD-29's calibration dent untouched.
3. **The value.js `/math` subpath choice** (C S★1 / SUP-B's surviving half): a 1,110-byte zero-import leaf making the R1 crash class unreachable by module topology while a sibling scene took the exposed edge; `@mkbabb/value.js: 4.0.0` is a real pinned dependency at origin (this seat's `git show`).
4. **`syncModifiers` is structurally self-healing** (D SUP-3 / L SUP-C): re-read from the live event as the FIRST statement of both consumers — the worse failure (a stuck meta silently converting every drag) is impossible by construction, and it is exactly why OD-5 is confined to the three keys with no event mirror. Axis D raised it as a candidate BLOCKER and killed it against the tree — the posture working.
5. **The touch-transition hardening is real-device scar tissue** (L SUP-5 / SUP-D) — and it is ALSO what kills L·M-4 (ruling 3): `startTouchPinch`'s triple re-seed makes the cancel story more complete than the axis that faulted it realized.
6. **`quaternionEuler.ts`** (L SUP-3 / SUP-E): 64 pure lines, both directions documented with their convention, numerically replayed by reader-LC and algebraically re-checked by this seat (the signed-zero it emits is the CONSUMER's dep bug, not this module's).
7. **`decay` off the LIGHT barrel** (C S★2 / SUP-F, with the twist): ED-3 honored where cheating would have been invisible — while the gate that certifies the seam imports the deep path (OD-29). The consumer is more disciplined than its own test.
8. **`@property --axis-active` registration** (reader-DU's credit): correct current-spec Houdini registration in service of an accessibility affordance, `inherits: false` (the choice `--lit` missed) — cross-banked at kf-CubeScene superlative 14 / the kf-CubeAxisLines record.
9. **SUP-2's non-colour second tell (dashed→solid) — adopted WITH a banked partial kill**: the mechanism credit stands, and reader-DU's X-line arithmetic reproduces (≈4.49:1) — but "the contrast maths check out" generalized one axis to three: **kf-CubeTarget #58 (banked) proves the LOCKED Y line fails 1.4.11's 3:1 in the light arm (≈2.7:1)**. The credit is now correctly scoped: X passes, Y fails, Z is projectively degenerate (#57).
10. **The census correction** (D SUP-5 / L SUP-G, concordant, both re-measured): there is no `--kf-*` namespace anywhere; the §6.3 hazard is the ABSENCE of a prefix; OrbitalDrag declares and reads zero custom properties — neither contributor nor victim. Any remediation reads §6.3 as "adopt a prefix".
11. **Method meta**: both readers carried explicit falsifiers, killed hypotheses, and each other's overreaches (K10/K11 — opposite-direction case kills, both right). The failure mode that recurs — four times in this one corpus — is never derivation: it is the unread registry (K1, K7, K8) and the unnamed baseline (K15). The registry's own laws (kill-inheritance #18; name-the-tree) each re-fired here and each held.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **macOS ⌘-keyup swallow** (OD-5's second trigger) — severity input only; the blur trigger is source-certain and the ⌘Z flash (OD-4 rider) needs no swallow.
2. **Co-visibility of the cube while typing** (OD-4) — the state chain is source-proven; only the pane layout and flash magnitude need the live surface.
3. **OD-1 + #53 composition signature** — one probe, two banked/booked rows: Ctrl-drag the die pre-Play AND during started state; expected in BOTH: motionless pose, six faces re-shading with the pointer. Then a free diagonal drag started-state as the control (should turn).
4. **The Stop-discard percept** (OD-2's fold ≡ #6, corrected): Play → drag to an off-axis orbit → Escape → jiggle the pointer 1 px (prediction: NOTHING repaints — the K2 correction's live twin) → press Play (prediction: snap to the accumulated orbit).
5. **OD-12 felt reversal** — pinch-twist clockwise, release: shading direction reverses; and `rotate.z` in the matrix editor readout decreases after a leftward Ctrl-drag release.
6. **L·M-4 residual topology** (K3) — second finger landing outside the die's footprint while the first stays inside: does the container receive 2-touch `touchmove` without a 2-touch `touchstart` on a real device.
7. **OD-13 shaking** — X-lock + horizontal drag (the misaligned case): full-magnitude sign-flipping rotation, container-visible since the X lock re-renders.
8. **OD-1's free-drag stutter** (input #1's magnitude) — `deltaY === 0` runs present as intermittent stutter, the symptom a user would report.
9. **OD-15/#34 layer cost** — DevTools layers read on the preserve-3d subtree during the started window (banked residue, carried).
10. **OD-36 resting-pinch jitter** — two stationary fingers close together: scale/pan/roll drift magnitude and the armed glide on release.
11. **#58's dark-arm and locked-line composites** (banked residue, rides with superlative 9's scope correction).

## Routing summary (M-25 agglomeration input)

- **NO-WAVE-OWNER — the cube scene-repair packet** (SS-1/SS-2 intake; spine = kf-CubeScene's routing block): the **gesture-math + hand-off family** OD-1·7·12·13·21·22·27·33·36 (one spec: signed hand-offs, per-component angles, tracked deps, derived constants; born-RED gates named in-row) · the **latch family** OD-3·4·5·6·25·31 (one cure = KF-AX-1's registry adoption + blur clear + expose reset; riders ⌘Z + `event.code`) · the **model-contract family** OD-2·8·14·16·18·19·20·23·24 (factory default + emit-or-document + type-tuple iteration) · the **presentation arm** OD-10·32·35·43 · hygiene/INFO OD-17·26·28·30·34·37·39·40·41·42·44.
- **KF.W4**: OD-29 (gate truth: recalibrate the parity gate, barrel import, replace the source-regex clause with a mounted reactivity assertion — with ME-32's gate row).
- **KF.W9**: OD-11 (PRM unification — the cube's tally corrects to 5 ungated motions) + the SS-13 roster.
- **KF.W0 §B-12**: K15's manifest schism (fourth firing) + the demo/utils uncommitted deletions noted in the substrate block.
- **Banked rows extended, owned elsewhere**: kf-CubeTarget #1 (new derivation + census), #6 (the Stop-edge symptom + corrected restore), #34 (lifetime correction), #52 (compiled-props proof + the OD-2 coupling), #53 (the miss-kill), #54 (the useDragCapture twin), #58 (superlative 9's scope); kf-CubeScene L-2/C-5 (third derivation), L-19/C-13 (devDep rider), D-8+M-7 (the fifth motion).

## Closing verdict

**ADJUDICATED — 0 BLOCKER · 14 MAJOR · 23 MINOR · 7 INFO across 44 rows, original ids OD-1..OD-44 for life; 15 kills; 10 net-new misses admitted as rows, 3 as riders, 4 booking-killed into the bank.** The component is the best-engineered gesture unit in the demo — five of its superlatives survived two hostile axes and this seat's incredulity intact, and its library consumption is close to exemplary — wearing three families of genuine defects: a **reactivity contract one-third registered** (OD-1, whose freeze composes with the banked #53 into a Z-roll that visibly works in no state), a **latch that answers to no law** (OD-4/5/6, already named at KF-AX-1 with the cure pointed at this very directory, down to the app's own Undo shortcut driving the cube's axis lock), and — this corpus's real contribution — **gesture physics that lose information at every hand-off** (direction discarded, magnitude mis-sourced, the model emit never fired). The adjudication events matter as much as the rows: four reader cells died on the registry and the substrate in one corpus, including a filed miss killed by a banked BLOCKER and a banked BLOCKER re-derived as a "miss" — the program's recurring lesson (*read the registry; name the tree; verify the write, not the call site*) is now three-for-three across the cube scene's seats. Nothing here stamps a wave; every disposition is agglomeration input for the forming X·KF specs, awaiting the owner's begin-word.

— end of apotheosis. The three challenge files and both fresh reader returns remain immutable beside this record per E-1/E-3. Dispositions are ADJUDICATED, not VERIFIED.
