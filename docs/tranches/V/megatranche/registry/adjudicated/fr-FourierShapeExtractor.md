claude-fable-5 (served model id)

# fr-FourierShapeExtractor — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator over two hostile re-readers · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-12 · **Subject**: `fourier-analysis/web/src/components/morph/FourierShapeExtractor.vue` (191 lines; read whole by this seat) — the `/demo/shape-extractor` internal tool: two hand-authored 200×200 SVG subjects (16 shape elements, all `red`), one glass Button, a `<pre>` dump, and `extractContours(·, 128)` → `window.__fourierShapeData` — **the sole tracked half of the build-time generator for the site-wide dark-mode toggle's geometry** (`sun.json`/`moon.json`, 450 KB, every route).
**Substrate**: fourier `cd26c65` + dirty WT (re-pinned by this seat: `git log -1` + porcelain, 28 dirty rows; the subject and `lib/svg-contours.ts` both CLEAN) · installed glass-ui **4.0.0** (peers value.js `^0.10.0 || ^0.11.0`, pencil-boil `^0.4.1` — seat-read) · installed pencil-boil **0.4.1** (raw-TS exports — seat-read) · pencil-boil producer **0.12.0** · glass-ui producer **8.0.0** at HEAD **`558c3fa3` (2026-08-12)** — the producer has moved AGAIN since reader-2 pinned `4a86570b`, which had itself moved past the `51cfdfaf`/7.0.0 both axes audited. Third drift event in this component family's record (SR-1 lineage, fr-ConvergenceLegend.md:7).
**Corpus**: three axis challenges (`audit/fourier-components/FourierShapeExtractor/challenge-{D-design 16+5, L-library 20+5, C-consumption 18+4}.md`, all read whole by this seat) × two hostile re-reader returns (reader-1 design-and-usage · reader-2 library-and-code), delivered as two separable JSON returns — this record carries the reader-vs-reader ruled-disagreements table.
**Method (M-23/L-18)**: every reader-vs-reader severity split (nine of them) was ruled after this seat re-derived the deciding facts with its own instruments; the load-bearing kills (C-3-discriminator, D-14-radii, L-m6, C-2-tracked-scripts, the L-B1 point-set test) were re-executed, not spot-trusted; three seat corrections were minted against the readers (SC-1..SC-3, including one against BOTH). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; every routing below is agglomeration input (M-25) and stamps nothing.
**This seat's own instruments (static + node + PIL; zero browser — probe parsimony; product source READ-ONLY; this file is the sole write)**: whole-file reads of the subject (all line anchors below re-resolved) and `lib/svg-contours.ts` (58 lines — `:16` selector no `polyline`, `:25` magic 50, `:40` `totalLen < 1`, `:47-49` bare catch, `:52` cardinality floor, circle branch no extent bail); App.vue:24-28 (`overflow-hidden h-dvh` shell + `<main … overflow-y-auto>`); style.css:1-50 (`@theme --font-sans` remap :13-15, `@layer base` `font-serif` :17-22, root fork :40-49); router/index.ts:95-168 (`:112-115` the meta-less record; `:149-164` DEFAULT_* + applyRouteMeta); installed pencil-boil `random.ts`+`celestial.ts` read whole — **the innerPoly RNG interleave verified by read: iteration *i* draws 6 outer-feeding then 6 innerPoly-only calls (`:75,:76,:78,:81,:85,:88`) before iteration *i+1*'s first outer draw**; **seat node run `scratchpad/rederive.mjs`** — transcribed mulberry32+generateSunRays(42): outerPoly n=20, radii **49.27…95.93**, bbox x[10.8,175.2] y[10.9,177.2], **max abs Δ vs `raw-contours.json` sun[0] = 6.10e-6**, exactly ONE vertex in the disc stroke band [46.5,49.5] (at 49.27); innerPoly radii 53.10…90.57; **all-16 endpoint-gap table** (sun 44.08/2.45/44.97/11.04/10.21/10.76/0.78/0.98 · moon 3.75/91.54/10.09/6.48/7.95/0.78/0.98/0.59); **shipped-asset re-measure**: moon.json original n=512 bbox [30.06,140.96]×[36.55,174.73], sun.json [11.15,175.10]×[10.90,177.77], raw crescent-alone [28.03,153.93]×[30,177.11], raw unions moon [28.03,187.5]×[7,177.11] / sun [10.8,175.2]×[10.9,178.4]; **point-set proximity (the L-B1 decider, re-run)**: moon frac≤1.0 = **0.037**, median 2.93, p90 37.57, max 44.83 · sun 0.326/3.44/13.95 — reader-2's figures reproduced to the digit; both assets carry `n_harmonics: 50`, 10 levels, `n_samples: 512`; **seat PIL run over `before/shape-extractor-375x667.png`**: border columns >100px tall = **{36, 235, 272}**, no right moon border, max red x = **374** (paint reaches the last column) — the D-1/L-m6/SC-2 decider; git archaeology — `9e5ba74` body carries the verbatim "remove stars from moon SVG contour, cap sun/moon at 50 harmonics"; `git ls-files scripts/` = **16 tracked paths incl. `export_pidinet.py` + `precompute_nav_icons.py`** (docstring seat-read: outputs the 5 nav assets in-process, `N_HARMONICS = 80`, no browser step); `git check-ignore -v` → `.gitignore:53 scripts/*` for the generator AND `raw-contours.json`; `precompute_svg_fourier.py:5/:40/:81` `order_contours` vs `shortest_tour.py` defs (`ContourTour :31`, `build_contour_tour :49`, NO `order_contours`) + README.md:126/:135 doc rot; `contours/geometry.py:114-115` `interp1d(…, kind="cubic")` — the monotonicity-kill mechanism; glass-ui producer at TODAY's HEAD — Button.vue props (`emphasis/tone: Extract<Tone,"neutral"|"destructive">/size: xs|sm|md|lg`, no `variant`, `withDefaults size:"md"`), **the DEV `watchEffect` detector present and verbatim ("THE SILENT BREAK GETS A VOICE" … `size="default"` is `size="md"`)**, `styles.css` data-size rules **xs/sm/lg only — no `[data-size="md"]` rule** (the SC-against-D mechanism ratified), `exports["./button"]` `{types,import}` unchanged, 70 keys, index.ts exports 5 names; seat greps — `@mkbabb/pencil-boil` in web/src = **3 lines/3 files** (loose `pencil-boil` = 4, the 4th a comment at SvgFilters.vue:19), `__fourierShapeData` with the readers' stated exclusions = **2 hits** (source :180 + the stale `web/dist` chunk — SC-1), `ls web/tsconfig*` = `tsconfig.json` only, AxeBuilder = exactly {visualization-ux, visualization-crud}.spec.ts, e2e goto census = /visualize ×13 · /gallery ×5 · /paper ×1, `as any` at FourierMorphDemo.vue:99-100, `pointsToSvgPath(points, closed = true)` at svg-fourier.ts:47-50; visual-baseline.spec.ts:36 (the route row) + the `documentElement.scrollWidth - clientWidth ≤ 2` gate; DELTA.md:9-11 ("**occlusion gate: 21/21 GREEN**") + the shape-extractor "stable" row; web/package.json:14-19 (value.js at :18); fold anchors read at their cited lines — fr-AdminAuditLog.md:36 (AA-2) · fr-ExportModal.md:75 (L-12/C-D-15) · fr-ConvergenceLegend.md:7/:59 (SR-1, M-R2) · fr-CoefficientsSpectrum.md:58 (M-6) · fr-AppHeader.md:10/:42 (stale-dist, FR-AH-1) · CENSUS-2026-08-03.md:102-106 (no Button row) · lane-frontend.md:481 ("4 sites"/3 enumerated) · intake `lane-fourier-r3-r6.md` located at `audit/codex-provenance/intakes/`.

## Scoreboard

**47 identities ADJUDICATED-CONFIRMED (0 component-local BLOCKER · 1 BLOCKER-weight identity FOLDED to banked AA-2 · 17 MAJOR · 21 MINOR · 8 INFO — counting the 12 reader-missed rows adopted/folded) · 16 killed-claims register entries ratified or entered · 3 seat corrections minted against the readers (one against BOTH) · 9 superlatives ratified.**

Nine reader-vs-reader severity splits ruled; every axis BLOCKER dies as a local BLOCKER: D-1 demotes (internal route, content reachable by undiscoverable scroll — but its harness half books separately), D-2/C-1 folds to the banked ADJUDICATED BLOCKER AA-2, L-B1/L-B2/C-2 demote to MAJOR on latency (the pipeline is already dead; nothing regressed a user today), L-B3 demotes to MINOR on this seat's own diff-closed exposure. The deepest fact in the corpus is provenance, now tri-derived: **the shipped `moon.json` is not derivable from this component's Moon SVG by any tracked or untracked recipe in the tree** (seat point-set test: 3.7% of shipped points within 1.0 unit of any extracted point, p90 37.6, against a 1.23-unit resample spacing) — the site-wide dark-mode toggle renders geometry whose source no longer exists, while the only tool claiming to regenerate it would reinstate six deliberately-removed decorations and re-parameterize 50→100 harmonics. **DO-NOT-REGENERATE rider stands: no regeneration on `master` until F.W5-W8 owns the pipeline.**

## The incredulity seat's own work — ruled reader-vs-reader disagreements

| id | reader-1 (design-and-usage) | reader-2 (library-and-code) | RULING | Deciding evidence (seat's own) |
|---|---|---|---|---|
| **D-1** | MAJOR ("unnavigable internal route") | BLOCKER | **MAJOR (component) + the harness half booked separately (M-4)** | Seat PIL run: border columns {36,235,272}, red paint to x=374 — the Moon box (272..472) loses 97px, 51.5% visible; reader-1's corrected geometry exact. Route reachable only by typed URL (absent from VALID_TABS, no nav link); content reachable by an undiscoverable horizontal drag inside `<main>`. Fleet precedent (fr-ConvergenceLegend scoreboard ¶2): no crash/leak/data-loss ⇒ sub-BLOCKER. The *generalising* half — the gate that certifies this green — is a distinct harness-level identity and books at MAJOR as M-4. |
| **L-B1** | BLOCKER | MAJOR (+ FR-AH-1 fold rider) | **MAJOR** | Latent: the divergence harms no user today and the pipeline that would surface it is already broken at import (L-B2). What it blocks is *work* — any regeneration, any pencil-boil-bump re-baseline, any F.W4 "keep and reconcile" of DarkModeToggle. Seat re-ran the decisive instrument (proximity 0.037/2.93/37.57/44.83 — digit-exact vs reader-2) and re-read `9e5ba74`'s body. MAJOR with the DO-NOT-REGENERATE rider; reader-1's BLOCKER preserved in DISSENT. |
| **L-B2 + C-2** | two separate BLOCKERs | one merged MAJOR | **ONE identity, MAJOR — reader-2's merge wins** | Same defect: the regeneration seam (artifact + transformer) is gitignored (`.gitignore:53`, seat-verified) AND broken at import (`order_contours` absent from `shortest_tour.py` — seat def-census) AND parameter-diverged (100/12 vs shipped 50/10 — seat asset read). Booking it twice inflates the count without adding a cure surface. Severity: latent (assets tracked and shipped; loss is reproducibility), and the repair is SMALLER than either axis believed — see M-2: a tracked, working, browser-free sibling generator already produces 5 of the 7 assets in this family. |
| **L-B3** | MAJOR | MINOR | **MINOR — reader-2 wins** | The concrete exposure (0.4.1→0.12.0 re-rolls the canon) is CLOSED by source diff — tri-derived (both axes, both readers; this seat independently reproduced the generator against `raw-contours.json` to 6.10e-6, which is the same closure by different means). What remains is the golden-file absence — structural, but latent-with-no-reachable-symptom, which grades MINOR by the fr-ConvergenceTimeline L·D-12 precedent. It rides the L-B2/C-2 cure (tracking `raw-contours.json` IS the golden file). C-18.2 also blocks the bump premise: glass-ui 8.0.0 still peers `^0.11.2`, which excludes pencil-boil 0.12.0. |
| **L-M2 (CTM-naive)** | MINOR | MAJOR | **MINOR — reader-1 wins, on precedent** | Statically certain (`grep getCTM` → 0; `polyline` absent from `:16` — seat read) and **latent**: both `<g>` wrappers are bare (`:17`, `:80`). Latent type/space hole with no reachable symptom = MINOR (fr-ConvergenceTimeline L·D-12, the same precedent reader-1 cited for D-L4 there). Magnitude → SS-13. |
| **L-M4 / C-8 (timer)** | MINOR | MAJOR | **MINOR — reader-1 wins** | Harm bound re-derived: the uncancelled one-shot fires once into a document whose `#output` is gone (`getElementById` miss → both writes skipped, `if (el)` wraps them — seat read of `:176-181`) or writes one 24 KB object to a dead global; closure freed after fire. No loop, no listener, no retained subscription. The 200 ms-wager arm (short contours on a throttled load) is UNPROVEN and stays SS-13; a MAJOR cannot rest on it. |
| **D-9 / L-i2 / C-17 (route meta)** | MINOR | MAJOR | **MINOR — reader-1 wins** | Governance/brand-surface fact on a route no nav reaches; the only vector is a crawler on a URL nothing links. Real (seat-read `:112-115` vs the sibling meta blocks, DEFAULT_DESCRIPTION at `:150-151`), cheap to cure, not MAJOR. |
| **D-2 / C-1 severity label** | MAJOR (local) | BLOCKER (local) | **DISSOLVED IN FOLD** | The identity is the banked **AA-2 — ADJUDICATED BLOCKER, census-extension row → F.W1** (fr-AdminAuditLog.md:36, seat-read). Neither reader may re-grade a banked row from a leaf; this file contributes its instance (`:125`, 1 of the 4 `variant="default"` / 6 `size="default"` sites) and two corpus corrections (the census gap restated; the producer-drift re-pin). |
| **The 44.08 ray-polygon closure miss** | MAJOR (standalone missed row) | MINOR (standalone missed row) | **FOLDED INTO L-M1 as its sharpest arm — the severity split dissolves** | Seat-computed: sun[0] endpoint gap **44.08** — the largest single dropped edge in either shape, on the sun's dominant silhouette, produced by the same `:32-35` no-wrap mechanism L-M1 books. One mechanism, one row; the arm makes L-M1's MAJOR unarguable and kills C-3's discriminator (44.08 closed vs 44.97 open — bands overlap, seat-verified). |
| **L-i1 (prod chunk)** | INFO confirmed with stale-dist caveat | evidence KILLED (untracked dist, mtime Jun 12) | **Evidence killed, verdict survives at INFO — both halves adopted** | The chunk is corroboration only (fr-AppHeader.md:10 stale-dist class, seat-read anchor); the conclusion rests on `router/index.ts:114`'s ungated dynamic import (seat-read). |

**Seat corrections minted against the readers:**

- **SC-1 (against BOTH readers).** Both state the `__fourierShapeData` census as "exactly ONE hit" under exclusions of `node_modules/.git/docs`. The grep **as stated returns 2 lines** — the source at `:180` AND the stale untracked `web/dist/assets/FourierShapeExtractor-BntwCyyr.js` chunk (seat-run). The conclusion (zero live consumers) is unharmed — the second hit is a build artifact of the first — but a census sentence that says "exactly one" must count what its own command returns. The corrected statement: 1 tracked-source hit; 0 readers.
- **SC-2 (against reader-2 — a measurement artifact).** Reader-2's D-1 cell reads "the `#ccc` border bbox spans x 13→374". This seat's PIL run: the raw `#ccc`-match bbox does span 13→374, but the extremes are sparse anti-aliased strays — the actual **border columns** (>100 matched px tall) are **{36, 235, 272}**: sun box 36..235, moon left border 272, right border off-canvas. Reader-1's geometry ("sun box 36..236, moon starts 272") is the correct reading; a bbox over a loose color threshold is not border geometry. Conclusion (Moon amputated) unaffected.
- **SC-3 (route sharpening — the drift is still running).** The glass-ui producer HEAD moved a THIRD time across this component family's adjudication records: `e73ec80c` (fr-ConvergenceLegend) → `4a86570b` (reader-2, 2026-08-09-ish) → **`558c3fa3` (this seat, 2026-08-12)**. Version still 8.0.0; this seat re-verified at today's HEAD that the Button contract (no `variant`; `ButtonSize` xs|sm|md|lg; `ButtonTone` narrowed; 5 index exports) and the DEV migration detector both survive, and that `styles.css` still carries **no `[data-size="md"]` rule** (xs/sm/lg only — the D-2-size-md-mechanism kill's mechanism, ratified). Standing consequence: **every "at 7.0.0"/"at 8.0.0" disposition in this folder is a dated snapshot; F.W1 re-pins at wave open** (third occurrence of the fr-ConvergenceTimeline K-8 lesson).

## Adjudicated defect roster

Line anchors are HEAD `cd26c65` (subject clean); re-resolve before any cure (D-19). Routes: **F.W1** tri-package uplift · **F.W3/W4** shadow retirement + frontend audit · **F.W5-W8** CRUD/provenance union · **GLASS-RELAY** rides the standing glass-ui BH inbox (owner edict) · **NO-WAVE-OWNER** → SS-3/SS-4. Identity guards fold by reference, never re-book.

### BLOCKER (0 local · 1 folded)

- **AA-2 fold — `<Button id="extract-btn" variant="default" size="default">` (`:125`) · D-2/C-1.** The glass-ui 4→7/8 Button prop-model break, invisible to a subpath-diff census because `./button`'s export shape is byte-identical at every pin (seat-read at 8.0.0 HEAD today). **FOLD BY REFERENCE to `fr-AdminAuditLog.md:36` AA-2 — ADJUDICATED BLOCKER, census-extension row, → F.W1** (with FR-AFP-2; fifth+ confirmation). This file's contributions, adjudicated here: (i) the instance census re-verified by the readers (4 `variant="default"` / 6 `size="default"` / 38 `size="icon*"` / 35 importing files / 99 callsites) with the CENSUS-2026-08-03.md:102-106 Button-row gap restated; (ii) the runtime-silence claim KILLED at producer HEAD (the DEV detector — see killed register); (iii) the size="md" fallthrough mechanism corrected (withDefaults does not substitute for a *defined* invalid prop; the md-shaped outcome comes from the base `.button` box because no `[data-size="md"]` selector exists — seat-verified today); (iv) the `data-variant`/`data-size` materialisation folds to fr-ExportModal L-12/C-D-15. Both readers filed the census gap without the registry sweep that would have found AA-2 banked — noted in DISSENT.

### MAJOR (17)

| id | claim (adjudicated form) | proof anchor (seat-verified) | disposition |
|---|---|---|---|
| **L-B1** | The generator has diverged from the artifact it generates: shipped `moon.json` is **not derivable from this component's Moon SVG** (proximity 0.037/median 2.93/p90 37.57/max 44.83 vs a 1.23-unit spacing; crescent-alone bbox strictly contains the shipped bbox by 12.97 x-max / 6.55 y-min — both seat-re-run), the sun IS consistent (0.326/3.44; bbox agreement ≤0.63), and `9e5ba74` (asset-only commit, verbatim "remove stars … cap at 50") is the divergence event the generator never received. The component still renders the six removed decorations (`:98-119`); shipped params 50/10 vs the script's 100/12. Re-running the tool regresses the site-wide toggle. | seat node runs; git show 9e5ba74; subject :98-119 | **F.W5-W8** (provenance union: decide the moon's true source or re-author it; the eager-payload arm is banked FR-AH-1 → F.W4). **DO-NOT-REGENERATE on `master`.** What moon.json WAS generated from → SS-13/undetermined. |
| **L-B2 / C-2 (one identity)** | The only consumer of this component's output is **untracked AND broken at import AND parameter-diverged**: `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json` (seat check-ignore); `:40` imports `order_contours` which no module defines (seat def-census of `shortest_tour.py`; README.md:126/:135 = tracked doc rot); `:139-142` hard-codes 100/12 vs shipped 50/10. A clone holds the producer and 450 KB of product with nothing between. **Repair-shaping correction bound in (M-2, reader-1's find, seat-verified)**: `git ls-files scripts/` = 16 paths including **`precompute_nav_icons.py`** — a tracked, working, in-process generator producing 5 of the 7 `fourier-paths/*.json` assets with no browser step and no gitignored artifact. The correct frame is "a tracked path exists for 5 of 7; only sun/moon route through an untracked browser detour" — both axes' falsifier sweeps asserted otherwise on a false enumeration (see killed register). | seat ls-files/check-ignore/def-census; nav-icons docstring | **F.W5-W8** — track the seam (negation lines), fix the import (`build_contour_tour(...).path`), reconcile 50/10, and prefer the nav-icons in-process idiom for retiring the browser round-trip. L-B3's golden-file gap rides this cure. |
| **L-M1 (+ the 44.08 arm, both readers' missed find)** | Closed shapes are emitted open — but the asymmetry is ONE convention, not a path-vs-polygon split (reader-2's sharpening ratified): every branch is endpoint-exclusive, so closed shapes lose exactly one step (crescent 3.75 = its 478/128 step; disc 2.45) and polygons lose a whole authored edge (diamonds 23.6-27.1%, stars 8.6-11.6%). **The largest instance was in no axis table: sun[0], the 20-vertex ray polygon — a 44.08-unit dropped closing flank on the page's dominant silhouette** (seat-computed over all 16 contours; the same number kills C-3's discriminator). Nothing downstream closes a contour. | svg-contours.ts:26-35,:42-43; seat gap table | **F.W5-W8** — closure flag + wrap at extraction (one cure with C-3); lands with the pipeline repair. |
| **C-3** | The contour type `[number,number][][]` cannot express open-vs-closed; two open strokes (spiral gap 44.97, moon inner stroke **91.54** — seat-recomputed exact) feed a closed-only downstream (`pointsToSvgPath(…, closed = true)`, all callers 1-arg — seat grep). The "unambiguous discriminator" sub-claim is KILLED (44.08 closed vs 44.97 open overlap — no post-hoc heuristic can recover closure), which *strengthens* the type complaint: the flag must be carried, not inferred. | svg-contours.ts:10-13; svg-fourier.ts:47-50; seat gap table | **F.W5-W8** (one change with L-M1). |
| **C-4** | `samplesPerPath` is honoured by one of three branches: paths get 128, circles get `max(16, round(128·r/50))` (magic 50, no constant), polygons ignore it — measured output `[20,123,128,4,4,4,16,16]` / `[128,128,10,10,10,16,16,16]` (seat-verified from raw-contours). A parameter named "samples per path" produced a 4-point contour. | svg-contours.ts:25,:32-35,:41 | **F.W5-W8** — contract doc or uniform post-pass; C-14's downstream resample bounds the geometric cost. |
| **L-M3 / C-6** | Three silent-drop gates with zero diagnostics on the path that defines shipped geometry (`:40` sub-unit bail, `:47-49` bare catch, `:52` floor; `grep console|throw|warn` → 0), caller records no counts, `:166` is a bare return — while the server twin ships `extract_contours_result` with diagnostics. This is the mechanism that makes L-B1 structurally unobservable at the human seam. | svg-contours.ts (whole, seat-read); subject :166,:168-174 | **F.W5-W8** — diagnosed result type mirroring the server twin. |
| **L-M5 / C-5** | The machine handoff is dead, untyped, and hostage to a presentational node: `if (el)` wraps BOTH writes, `(window as any)` with no `declare global` anywhere, zero readers (SC-1's corrected census: 1 tracked hit, 0 consumers), the Playwright comment false at HEAD — and the file owns the correct ref idiom twice (`:147-148`). L's corpus correction stands: SE-05's "just delete it" is wrong on the tree — `precompute_svg_fourier.py:4-5` names this component as its input; the seam needs a real harvester or a real deletion *with* the pipeline decision, not either alone. | subject :176-181; seat greps | **F.W5-W8** — named exported interface + ref emission, decided WITH the pipeline repair. |
| **L-M6** | Hand-unrolled indexed repetition (6 indexed bindings over 3-element arrays; 16 rendered shape elements; `v-for` count 0 — seat-verified) defeats both component-callsite and NATIVE_TEMPLATE_LOOP extractors, and is a live array/template desync hazard (`noUncheckedIndexedAccess` absent from the real `tsconfig.json`). Extends intake **R5-7** exactly as claimed. | subject :44-61,:98-115,:153-163 | **F.W4** (the R5-7 carry target). |
| **C-7** | Zero consumption of the 39-function API surface; the leaf reimplements stage 0 of the server's contour pipeline with no test runner in `web/` at all — the **R6-8** echo. C's honesty adopted: the decomposition is NOT duplicated; the offline detour is elective (`saveContour` accepts ingested contours). | api.ts export census (readers, cross-verified); subject :142-145 | **F.W5-W8** — the pipeline decision names which of the two stage-0s survives. |
| **D-1** | At 375px the Moon is amputated mid-crescent: 36+200+36+200+36 = **508px** (root fork live in the capture — seat PIL: sun border at x=36) vs 375; no wrap, no shrink, no affordance; **97px of the Moon box off-canvas, 51.5% visible** (figure corrected from D's 133px/67% — see killed register). | seat PIL run; style.css:40-49; subject :2,:5,:13-14,:76-77 | **F.W4** — wrap/shrink cure; re-witness at all three declared viewports. |
| **M-4 (missed, reader-1 — the harness half of D-1)** | The repo's occlusion gate is structurally vacuous repo-wide AND its record certifies the vacuity in writing: App.vue:24/:26 guarantee `documentElement.scrollWidth - clientWidth` = 0 for every route (overflow consumed inside `<main>`, CSS Overflow 3 §3.1), so `visual-baseline.spec.ts`'s gate passes by construction — and **DELTA.md:9-11 books "occlusion gate: 21/21 GREEN (zero horizontal overflow on every page × viewport)" for the very run whose 375px capture shows the amputation** (both halves seat-read/measured). 21/21 GREEN is 21/21 UNMEASURED; DELTA.md:21's "stable" row compounds it. | App.vue:24-28; visual-baseline.spec.ts gate; DELTA.md:9-21; seat PIL | **F.W4** — re-point the gate at `<main>` (or per-element intersection), correct the DELTA record's standing mis-signal. Harness-level, repo-wide. |
| **D-3** | The page's five type properties come from an UNLAYERED glass-ui `body` rule the app's `@layer base` cannot outrank (semantic.css:13-18, 0 `@layer`; import chain unlayered end-to-end), and the brand fork remaps `--font-sans` while the shell applies `font-serif` (seat-read style.css:13-22) — the remap never reaches the element carrying the text. The component is the maximal instrument: 191 lines, zero classes, zero tokens. | readers' chain (spot-verified at style.css); subject whole | **F.W4** (the page authors a register) + **GLASS-RELAY** rider (unlayered dist styles are producer-owned). Served face → SS-13. |
| **D-4** | h1/h2 render at body size AND weight — Tailwind preflight flattens both (`font-size: inherit; font-weight: inherit`), nothing restores them; the prior corpus's SE-06 ("browser defaults") and SE-14 ("no serif family") are BOTH refuted diagnoses. | preflight.css:73-80 (readers, image-corroborated); subject :3,:8,:71 | **F.W4** — type scale; the corpus correction rides with it. |
| **D-5** | The page's only CTA rendered as a solid `--primary` pill with **zero label glyph pixels** in the repo's only visual record, across three viewports, for two months, all gates green — pixel-measured independently by both readers (pill bbox 178×40, 0 light-glyph pixels, corners = background bleed). The 4.0.0 cure is structural (cva `default` = `glass-wash … text-foreground`); present tense → SS-13. The surviving MAJOR is governance: the route is design-observed and assertion-free. | readers' independent PIL runs (methods differ, figures agree); dist cva | **F.W4** — re-witness; DELTA.md row re-marked either way. |
| **D-6 / D-7** | Empty/loading/error are unrepresentable (the `<pre>` has no bound state; `:166` silently returns; `svg-contours` drops silently) and the CTA writes byte-identical DOM announcing nothing (all inputs literal, extraction pure, no aria-live/status/focus move) — the human-seam half of L-M3's observability hole, and the mechanism by which a 15-of-16-contour regression ships unnoticed. | subject :129-137,:151-163,:166,:176-190 | **F.W4** — reactive output + announced states; one cure. |
| **D-8** | Both SVG subjects are anonymous to AT (no role/label/title/hidden; the `<h2>`s unassociated), the output region unannounced, and axe never visits the route (AxeBuilder in exactly 2 specs, neither reaching `/demo/shape-extractor` — seat grep + goto census). | subject :8-16,:71-79,:129-137; seat e2e census | **F.W4** — name the subjects, announce the region, add the route to an axe pass; sequence with M-3 (its rule fires first). |
| **M-3 (missed, reader-1 — instance of a banked identity)** | `<pre id="output">` is an axe `scrollable-region-focusable` violation whose overflow is **UNCONDITIONAL and horizontal** (one-line 24,048-byte payload, `white-space: pre`, no tabindex/role/name; the same magic 300px port; glass-ui ships `./fading-scroll` at both pins, zero imports). **FOLD BY REFERENCE to fr-ConvergenceLegend M-R2 + fr-CoefficientsSpectrum M-6 — this books the instance only**, noting the banked rows' "conditional on overflow" caveat does NOT apply here. | subject :129-137,:178; banked anchors seat-read | **F.W3/W4** — with D-8's axe pass. |

### MINOR (21)

- **L-B3 — MINOR** (ruled above). Canonical geometry = pure function of an unpinned third-party PRNG with zero drift detection; concrete 0.4.1→0.12.0 exposure CLOSED (tri-derived diff/re-derivation; seat Δ 6.10e-6); the golden-file gap rides L-B2/C-2's cure (tracking `raw-contours.json` is the golden file). → **F.W5-W8**.
- **L-M2 — MINOR** (ruled above). CTM-naive extraction + `polyline` absent from the selector; latent (bare `<g>`s). Magnitude → SS-13. → **F.W5-W8**.
- **L-M4 / C-8 — MINOR** (ruled above). Uncancelled 200 ms `setTimeout`, no teardown, `nextTick` unimported, the repo's own `useTimeoutFn`/scheduler floor unconsumed; the wager arm → SS-13. → **F.W4**.
- **D-9 / L-i2 / C-17 — MINOR** (ruled above). The only meta-less route of nine stamps DEFAULT_DESCRIPTION (the product's marketing sentence) on a debug harness; no noindex, no DEV gate; prior art (DA-A4, 70+ days undischarged) folded. → **F.W4**.
- **D-10 — MINOR.** `border: 1px solid #ccc` weight-inverts across modes (1.54:1 light / 12.3:1 dark — both readers recomputed independently; adopted); raw `red` ×16 vs the house `FourierMorphSvg` wrapper — the bypass identity is banked fr-FourierMorphSvg FM-8 (fold); D's S-3 correction stands: red PASSES 1.4.11 in both modes — token-discipline, not contrast. → **F.W4**.
- **D-11 / L-i4 — MINOR.** `max-height: 300px; overflow: auto` over a one-line `JSON.stringify` (no space arg): the vertical log can never engage; the element is a horizontal scroller ~1 line tall (capture-verified by readers); `useMorphConfig().copyToClipboard()` exists next door unconsumed. → **F.W4** (one cure with M-3/D-6).
- **D-12 — MINOR.** Every dimension is rem and the root fork inverts: the smallest viewport gets the largest chrome (36px vs 32px paddings; 13.5px vs 12px pre text) — the proximate multiplier of D-1's 508px. → **F.W4**.
- **D-13 — MINOR.** At the installed pin the Button press-scale survives `prefers-reduced-motion` (unlayered emitted utility beats the `@layer components` reset — four links byte-verified by readers; reader-1's refinement adopted: a11y-overrides strips `scale` from transition-property, so the leak is an INSTANTANEOUS scale). Cured at 7/8.0.0 (`useLiquidPress`, hostClass carries no inline scale — seat-read today). → **F.W1** (credit row) + SS-13 readback.
- **D-14 — MINOR, figures corrected.** Four subject collisions confirmed on THIS seat's numbers: sparkle-on-dot exact co-location (55,170); second sparkle/dot overlap; **exactly one ray vertex in the disc stroke band — at radius 49.27, not 46.98** (seat re-derivation; the killed-figures register carries the original); spiral terminus 0.06px from the disc's inner stroke edge. Two shapes that read as one line are emitted as two contours. → **F.W4**.
- **L-m1 / C-11 — MINOR.** Three `computed()` over zero reactive dependencies — module constants in reactivity costume (all arguments literal — seat read). → **F.W4**.
- **L-m3 / C-10 — MINOR.** The single provenance comment is arithmetically false (42·100+42 = 4242) and cites a frame-seed formula existing in neither repo (`useLineBoil` takes no seed) — the only prose explaining why the canonical geometry is what it is, wrong twice. → **F.W5-W8** (with the seeds row). |
- **L-m4 / C-12 — MINOR.** Document-global unnamespaced ids (`output`, `extract-btn`), `getElementById` for an element the file owns while owning the ref idiom twice; `id` rides as Button fallthrough at every pin. → **F.W4**.
- **L-m5 — MINOR.** The canonical seed set (42; 1,2,3; 10,20,30) — the artifact's only free parameters — scattered inline with no single knob. → **F.W5-W8**.
- **M-5 (missed, reader-1) — MINOR.** Three nouns name one surface (URL "demo", h1 "internal tool", route name "shape-extractor") and `/demo/` is a one-member namespace with no policy; the h1 never states what the output is for — per L-B1/L-B2 the most load-bearing missing sentence in the file. → **F.W4**.
- **M-7 (missed, reader-2) — MINOR.** `:40`'s `if (totalLen < 1) continue;` is NaN-permissive (`NaN < 1` is false): a NaN `getTotalLength()` emits 128 `[NaN,NaN]` samples that pass every gate and serialize as `null` pairs into the handoff. Correct guard: `!(totalLen >= 1)`. Seat-read; registry-swept clean. → **F.W5-W8**.
- **M-8 (missed, reader-2) — MINOR.** The sampler is endpoint-exclusive, so the two OPEN paths silently lose their terminal point (spiral last sample 1.53 short of its authored endpoint; inner stroke 0.76 short) — the mirror image of L-M1, one convention producing both wrongs in opposite directions. → **F.W5-W8** (same cure).
- **M-9 (missed, reader-2) — MINOR.** The `points.length >= 3` floor guards cardinality, not extent: an `r = 0` circle yields 16 coincident points that pass and reach the decomposition — the one branch with no degenerate bail (seat read of `:21-29`). Bounds L·S-4's superlative. → **F.W5-W8**.
- **M-11 (missed, reader-2) — MINOR.** `extractContours` has no paint-visibility filter (`<defs>`/`<mask>`/`<clipPath>`/`display:none` subtrees would contribute canonical geometry); the repo already mounts a global `<defs>` surface app-wide (SvgFilters), so the hazard idiom is live in-tree. Distinct from L-M2's space-blindness. → **F.W5-W8**.
- **C-13 — MINOR.** pencil-boil 0.4.1 ships raw TS (`exports: {".": "./src/index.ts"}` — seat-read), so 5 dependency sources enter the app's strict typecheck; `skipLibCheck` covers `.d.ts` only. Resolved by the 0.12.0 bump (real dist). `--listFiles` → SS-13. → **F.W1**.
- **C-14 — MINOR.** The polygon branch's exact-vertex care is undone by `resample_arc_length(stitched, 512)` downstream and neither file documents the precedence. → **F.W5-W8**.
- **C-15 — MINOR.** The producer's output type is unenforced end-to-end: anonymous literal → untyped JSON → independently-declared `FourierPathData` → `as any` at both consumer leaves (seat-verified :99-100), with `resolveJsonModule` already providing the structural type being discarded. Three boundaries, typed at none. → **F.W5-W8** (with C-5's named interface).

### INFO (8)

- **D-15** — single-value 2rem rhythm, no measure; the composition occupies ~10% of the 1440 field with the `<pre>` shooting full-width. → **F.W4**.
- **D-16 / C-9 / L-m2-arm (+ M-10 rider, reader-2's find — REPAIR-CRITICAL)** — dead seams and stale prose (the three unread hooks; the false comment; `innerPoly` computed and discarded). **M-10 bound in, seat-verified by read of the RNG interleave: `innerPoly`'s six per-iteration draws are entangled with `outerPoly`'s stream — any "cure" that skips the innerPoly work re-rolls the canonical sun.** All three axes booked the discard as waste; the naive fix is a trap. → **F.W4** for the seams, the M-10 rider travels with EVERY L-B1/L-B2 cure. |
- **L-i1** — the internal tool is production-bundled (verdict on `router/index.ts:114`'s ungated dynamic import; the dist-chunk "proof" is killed — stale, untracked); D's S-5 cost framing adopted (lazy chunk, not payload). → **F.W4** (rides D-9's gate decision).
- **L-i3 / C-18.1** — census correction: pencil-boil = **3 import sites / 3 files** (seat grep; the "4" was a loose-grep comment artifact at SvgFilters.vue:19). → **F.W1** (correct lane-frontend.md:481).
- **C-16** — value.js 0.13.0 sits outside installed glass-ui 4.0.0's peer range (`^0.10.0 || ^0.11.0`, optional — seat-read): the peer graph is inconsistent TODAY. **FOLD to the banked fr-AnimationControls C-1 peer-graph identity** (per the fr-DarkModeToggle ruling); this file holds the pin, doesn't exercise it. → **F.W1**.
- **C-18.2** — glass-ui (7.0.0 AND 8.0.0 — seat-confirmed the range survives at today's HEAD's package) peers pencil-boil `^0.11.2`, which **excludes** producer 0.12.0 (`^0.x` = `>=0.11.2 <0.12.0`). The sharpest un-banked coordination item in the C axis; it blocks L-B3's bump premise. → **F.W1**.
- **M-6 (missed, reader-1)** — border-box puts the 1px frame INSIDE the 200px attribute box: each 200-unit viewBox renders at 198×198 (0.99×), the frame is painted on the subject rather than a container — the mechanical reason the h2 sits flush; the structural repair is a container primitive (`./card` ships at both pins). No effect on extracted geometry. → **F.W4**.
- **M-12 (missed, reader-2)** — `window.__fourierShapeData` is written and never cleared: ~2,900 point tuples retained for the life of the tab, past unmount and navigation — the retention half L-M4 doesn't book. → **F.W4** (dies with C-5's cure).

## Killed-claims register (refutations ratified or entered by this seat)

1. **L-m6** ("the route provably cannot satisfy the occlusion gate") — KILLED TWICE, both readers, seat-ratified: (a) 496px assumes a 16px root; the mobile root is 18px (seat PIL: sun border at x=36 confirms the fork live in the capture) → 508px; (b) the gate CANNOT fail — App.vue's shell consumes the overflow before `documentElement` (seat-read :24-28). L-m6 asserts the opposite of the truth; the gate is green *because* it is blind (= M-4). The L axis never opened App.vue.
2. **L-M7-falsifier** ("delete both props; byte-identical class list ⇒ they pin nothing") — KILLED: the 4.0.0 chunk reflects `data-variant`/`data-size` onto the DOM, so the props are not inert. **FOLD to banked fr-ExportModal L-12/C-D-15** (seat-read at :75). L-M7's historical half (the `default` variant silently changed meaning 3.1→4.0 under an explicit spelling) survives inside AA-2's story.
3. **C-3-discriminator** ("closed land at 2-4 units, open at 45/92 — unambiguous") — KILLED by the full 16-gap table (seat-recomputed): closed includes **44.08** (sun[0], a semantically closed `<polygon>`) vs open 44.97 — the bands overlap on this very dataset. The defect (no closure flag) survives *stronger*; the offered proof does not.
4. **D-14-radii** ("radii 46.98…96.66; the offender at 46.98") — FIGURES KILLED, verdict survives: seat re-derivation (matching raw sun[0] to 6.10e-6, hence authoritative) gives **49.27…95.93**; exactly one vertex in [46.5,49.5], at 49.27. A finding whose method is "measured, not eyeballed" must carry the measured numbers.
5. **D-S-2 figures** ("max 96.66, x∈[20.1,187.5]") — KILLED: actual outerPoly bbox x[10.8,175.2] y[10.9,177.2], max radius 95.93 (seat run); 187.5 is the MOON dot's x-max transposed. Clearance conclusion survives by larger margins than stated.
6. **L-tsconfig-citation** — `web/tsconfig.app.json` DOES NOT EXIST (seat ls: `tsconfig.json` + tsbuildinfo only). Substantive facts survive against the real file; the citations were fabricated. Twice, both L-axis.
7. **D-2-runtime-silence** ("a suppressed vue-tsc gate ships a silently different button") — TRUE at the axes' 7.0.0 target, **FALSE at producer HEAD**: the DEV `watchEffect` detector ("THE SILENT BREAK GETS A VOICE", naming `variant`/`size="icon"`/`size="default"` with the exact migration) — **re-verified by this seat at TODAY's HEAD `558c3fa3`**, landed `70dc0f06` 2026-08-08, after both challenges. Folds by reference (fr-CoefficientsSpectrum reads it verbatim; drift = SR-1).
8. **D-2-size-md-mechanism** ("withDefaults substitutes md") — MECHANISM KILLED, outcome coincidentally survives: `size="default"` is *defined*, so `props.size` stays the invalid string; the md-shaped box comes from the base `.button` class because **no `[data-size="md"]` rule exists** (seat grep today: xs/sm/lg only). The difference matters to the F.W1 mapping table's author.
9. **D-1-moon-clip-figure** ("133px of the Moon off-screen, ~67% visible") — FIGURE KILLED (finding CONFIRMED): 133px is the *document* overflow including right padding; the Moon box (272..472) loses **97px → 51.5% visible**; the sentence was also internally inconsistent (133/200 would be 33.5%). Seat PIL border-column run ratifies.
10. **C-2-tracked-scripts** ("the tracked scripts set = 10 shell files + conformance, all deploy/e2e/DNS") — THE SEARCH STATEMENT IS FALSE: `git ls-files scripts/` = **16 paths incl. two tracked Python generators**, one of which (`precompute_nav_icons.py`) is the repair-shaping precedent (M-2). L-B2's stronger form ("ZERO tracked paths from the `<pre>` to `fourier-paths/*.json`") errs identically. C-2's verdict survives — nav-icons does not produce sun/moon — but its falsifier was declared unmet on a false premise.
11. **L-i3-grep** ("grep → 4 lines, of which SvgFilters.vue:19 is a comment") — KILLED as evidence, verdict adopted: the scoped grep returns **3** lines (seat-run); :19 lacks the `@mkbabb/` scope and cannot match the stated pattern. C-18.1 stated it correctly.
12. **L-B1-monotonicity** ("resample output bbox ⊆ input bbox") — KILLED: `geometry.py:114-115` uses `interp1d(kind="cubic")` (seat-read), which overshoots (~0.2-0.4 units measured by reader-2). Two orders below the 13-unit shrink, so L-B1's conclusion stands — on the point-set test (seat-re-run), not on the monotonicity argument.
13. **C-1/D-2 version label** ("at 7.0.0", HEAD `51cfdfaf`) — STALE, twice over: producer 8.0.0, and HEAD has moved again to `558c3fa3` since reader-2's own correction (SC-3). Substance unaffected at every observed pin; every disposition re-reads as "at the F.W1 re-pin".
14. **L-i1-proof** ("build-artifact proof — the chunk exists on disk") — KILLED as evidence: `web/dist` untracked, mtime 2026-06-12, pre-bump pins (the banked stale-dist class, fr-AppHeader.md:10). Verdict survives on the ungated router import.
15. **C-16-citation** ("web/package.json:16") — off by two: value.js is **:18** (seat-read; :16 is latex-paper).
16. **L-M1-asymmetry** ("`<path Z>` handled correctly, polygon/circle not") — MISDIAGNOSIS KILLED (reader-2, seat-ratified from the gap table): one endpoint-exclusive convention everywhere; the crescent's 3.75 gap IS its one lost step. The cure survives; the framing "path right, others wrong" does not.

## Superlatives (provenance preserved)

1. **L·S-1 / D·S-1 / C·S-1 — the seed-42 canon is bit-reproducible and survives the pending bump — now QUAD-derived.** Two axes (by diff and by re-derivation), two readers (independent re-derivations), and this seat (transcription → **max abs Δ 6.10e-6** vs `raw-contours.json`, float32 ULP at magnitude 10²; contour counts element-exact). The strongest positive result in the folder; it is what demotes L-B3 and lets F.W1 cite instead of re-baseline.
2. **L-B1's evidence construction** — a product decision quoted from a commit BODY, a bbox comparison, and a control (the sun agreeing to ≤0.63) that converts "the moon is different" into "the two canonical assets have different provenance and only one is reproducible" — with reader-2's point-set test (seat-reproduced to the digit) supplying the proof the original's monotonicity argument could not, and reader-1's five-commit byte-identity excluding the post-generation-edit alternative.
3. **D-5's control experiment** (the same-frame gallery Button rendering its label normally) — the instrument that converts a blank pill from capture artifact to variant-specific failure; both readers reproduced the pixel measurement by different methods and agreed to the pixel class.
4. **D-13's four-link cascade derivation** (cva base → layered PRM reset → unlayered emitted utility → cascade order) — byte-verified at every link by both readers; reader-1's a11y-overrides refinement (instantaneous, not animated, scale) is the kind of sharpening kills exist to fund.
5. **C-3's operationalisation** of an abstract type complaint into four exact figures — every number reproduced on this seat's own computation; its discriminator died to a fifth number nobody computed, which is a sampling lesson, not a method one.
6. **The three axes' honesty registers** — D's five dead hypotheses (incl. S-3's disarming of a false contrast framing), C-7's declined overclaim, C-4's self-demotion — all load-bearing in this adjudication's demotions.
7. **D·S-4 — the focus ring for free**: the one act of design-system adoption in a zero-token file is the one thing it got right; verified at both pins (8.0.0 hostClass still `cn("button tap-squish focus-ring", …)` — seat-read today).
8. **glass-ui's producer — "THE SILENT BREAK GETS A VOICE"**: the DEV migration detector, landed three days after these challenges were written, converts the corpus's headline silent break into a loud one at adopt time; seat-verified surviving at today's `558c3fa3`.
9. **The convergent missed find** — both hostile readers, working separately, surfaced the SAME un-tabled 44.08-unit ray-polygon closure loss from the same raw data; independent convergence on an omission is the strongest evidence a review layer can produce that the omission is real.

## UNPROVEN-NEEDS-LIVE (SS-13 residue)

1. **D-3** — computed `font-family`/`font-size` on `h1` at `/demo/shape-extractor` under the 4.0.0 pin. Predicted: Plus Jakarta Sans at `clamp(1rem, 0.92rem + 0.27vw, 1.375rem)`.
2. **D-5** — the Button's rendered `textContent` + computed colors today (the 3.1.0 blank pill is closed history; the cure is inferred from the cva). DELTA.md:21 re-marked either way.
3. **D-13** — computed `scale` on `#extract-btn:active` under emulated PRM. Predicted `var(--scale-press-btn)` ≠ 1, applied instantly.
4. **D-8 + M-3** — one axe pass over the route (never run in repo history). Predicted: `svg-img-alt`/`image-alt` on both subjects AND `scrollable-region-focusable` on `#output` (unconditional here).
5. **L-M4 / C-8** — whether the orphaned timer's post-navigation fire ever writes, and whether the 200 ms wager loses on a throttled load (compare emitted lengths against `[20,123,128,4,4,4,16,16]` / `[128,128,10,10,10,16,16,16]`).
6. **C-13** — the `tsc --listFiles` measurement (mechanism source-certain; the file list unobserved).
7. **L-M2** — error magnitude under a real transform (currently zero; both `<g>`s bare).
8. **L-B1 residual** — what `moon.json` WAS generated from is undetermined by the tracked tree; no candidate source exists. **Never regenerate on `master`.**
9. **F.W1 PIN QUESTION** — the census budgets 7.0.0; the producer is 8.0.0 and its HEAD has moved at every observation (`e73ec80c` → `4a86570b` → `558c3fa3`). The Button break is invariant across all observed pins; the detector and `ButtonTone` narrowing are 8.0.0 facts. Folds to SR-1; F.W1 re-pins at wave open.

## DISSENT — preserved, not ruled away

1. **Reader-1's BLOCKER positions on L-B1 and L-B2/C-2** ("the tool's output is a regression and nothing in the tree says so"; "promoting the M-audit's open question on gitignore evidence is sound"). This seat demoted both to MAJOR on latency and on M-2's repair-shrink; if any wave attempts regeneration before F.W5-W8 lands the pipeline, both rows revive at BLOCKER with the DO-NOT-REGENERATE rider as the tripwire.
2. **Reader-2's BLOCKER position on D-1** ("one of the two things the page exists to show is amputated with every gate green"). Preserved; the seat's split booking (component MAJOR + harness MAJOR) is intended to carry the full weight without inflating the leaf.
3. **Reader-2's MAJOR positions on L-M2, L-M4/C-8, D-9** — each demoted on the latency precedent; each revives on its named trigger (a transform on a `<g>`; a reproduced cross-route write; the route ever entering a nav surface).
4. **Both readers filed the AA-2 census gap as a novel contradiction without a registry sweep** — the identity guard caught it here, as it caught it at fr-AdminFlaggedPanel. Recorded as evidence about the fleet's sweep discipline, not about the tree.
5. **Reader-2's severity for the 44.08 arm (MINOR)** — subsumed by the fold into L-M1 (MAJOR); the standalone grading question is moot but the instinct ("an arm of one mechanism, not a new defect") is the one this seat adopted.

---

**VERDICT: 47 identities adjudicated — 0 component-local BLOCKERs (the one BLOCKER-weight break folds to banked AA-2 → F.W1); 17 MAJOR · 21 MINOR · 8 INFO; 16 register kills; 3 seat corrections; 9 superlatives. The file is a 191-line page wearing a build-step's responsibilities: its geometry pipeline routes to F.W5-W8 (provenance union, with the DO-NOT-REGENERATE rider and the M-10 innerPoly trap attached), its page-surface debts to F.W3/W4, its uplift exposure to F.W1 under a producer that has drifted at every observation. Original challenges and both reader returns remain immutable beside this file per E-1/E-3.**
