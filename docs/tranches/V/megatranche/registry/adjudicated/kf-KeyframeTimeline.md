claude-fable-5

# kf-KeyframeTimeline — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-09 · **Anchors**: keyframes.js `master` HEAD `8281638c` (re-confirmed by this seat, working tree = the audited tree) · registry branch `tranche-u` (value.js) · installed producers: `@mkbabb/glass-ui` 7.0.0 dist (contract of record) · `@mkbabb/value.js` 4.0.0 (version re-read from the installed package.json by this seat).
**Corpus**: three axis challenges (D-1..D-28 · L-1..L-21 · C-1..C-18, all `claude-opus-5[1m]`, 2026-08-05, 1500 lines total) at `docs/tranches/V/megatranche/audit/kf-components/KeyframeTimeline/`, re-read by two hostile re-readers — **RR-A** (design-and-usage) and **RR-B** (library-and-code) — both returns adjudicated here, attacked before either was adopted.
**Method (L-18/M-23)**: every verdict-level disagreement (the three BLOCKER↔MAJOR contentions on D-1/C-1/D-2, the D-13/D-16 rungs, RR-B's two solo kills L-15/L-10, RR-A's solo kill-scope on K3) was re-derived against the tree with this seat's own instruments; the one falsifier NEITHER reader ran (M7's `data-register` probe) was run by this seat; the decisive execution neither axis performed (C-7's parser throw) was performed by this seat; four suspiciously-agreed cells (the fade-slide zero-count, the min-block-size cascade, the watch census, the expose-consumer census) were re-measured, not spot-trusted. **No browser tooling** (law); product source READ-ONLY; the irreducibly visual residue rides to SS-13.
**Arbiter's own instruments (all static + node against installed dists; zero writes to product source)**: read WHOLE — KeyframeTimeline.vue (313 L), TimelineTrack.vue (247 L), TimelineCaret.vue (71 L), TimelineHoverPreview.vue (39 L), useTimeline.ts (129 L), useTimelineBuild.ts (200 L), useTimelineOps.ts (88 L), useZoomPan.ts (110 L), timelineTypes.ts (42 L), timeline/index.ts (8 L), utils/snapshotCapture.ts (66 L), utils/timelineEngine.ts (104 L), utils/flattenVars.ts; **executed** `node` against installed `@mkbabb/value.js/dist/subpaths/css.js`: `parseCssValues("oklch()")` / `rgb()` / `hsl()` / `color()` **all THROW `TypeError: Cannot read properties of undefined (reading 'replace')`** — the R1 crash shape, live at 4.0.0 (the C-7 decider); installed glass-ui 7.0.0: `components/button/styles.css` (base `.button` carries `min-block-size: var(--button-size)` — the K-5 decider; `[data-icon-only]` inline/block/min-block trio; `.button:disabled{opacity:var(--opacity-disabled);cursor:not-allowed}` + `.button:disabled{pointer-events:none}` — the K-2 decider; `[data-size="sm"]{--button-size:var(--control-h-sm)}`; `[data-emphasis="quiet"]{color:var(--muted-foreground)}`; `[data-tone="destructive"]` present), `tokens/sizing.css` (`--control-h-sm: max(calc(2.25rem * var(--ui-scale)), var(--control-floor))`, `--touch-target: 2.75rem`, `--icon-sm: 0.875rem` — the glass TOKEN, vs the demo's 16px `.icon-sm` CLASS), `tokens/light-dark.css` (`@media (pointer:coarse){--ui-scale:1.5; --control-floor:2.75rem}`; the `--neutral-5`/`--card` hsl values reconcile exactly with the corpus's hex figures), `tokens/scale-paper.css` (`--opacity-disabled: 0.5`), `typography/semantic.css` (`@utility text-admin-label{…text-transform:uppercase…}`), `typography/scale.css` (`--type-admin-label: 0.625rem`, fixed; `--type-small` clamp 14→20px), `_shared/field-control.css` (`block-size` only, NO min-block-size; `padding-inline: calc(1rem * var(--ui-scale))` — D-9's coarse-padding sharpening), `glass-ui.css` (`.field-control[data-kind=input]{block-size:var(--field-control-height)}`), `Input-*.js` (single element, caller class merged); whole-package `fade-slide` grep → **0**; producer SOURCE tombstone `/Users/mkbabb/Programming/glass-ui/src/styles/transitions.css:22` — "`fade-slide` RETIRED (census-dead: 0 src/ consumers…" verbatim; `reka-ui/dist/Tooltip/TooltipTrigger.js` (`focus: handleFocus` at :39 → `onOpen` — the D-10 premise); `@vue/reactivity/dist/reactivity.d.ts:346/:353` (`ComputedRef extends BaseComputedRef extends Ref` — the K-9 decider); shipped chunk `dist/gh-pages/assets/KeyframeTimeline-C1xKzyhf.css` read (unlayered `.keyframe-marker[data-v-39fe8774]{transition:transform …}` + the counter-rotated 24px pad, verbatim); **greps**: `data-register` CSS rules across demo/styles + glass dist → **0** (a census contract in font-roles.json only — the M7 falsifier), `label` reads in the cluster → write site + type decl ONLY, watch/lifecycle census → exactly 1 (`CSSPasteDialog.vue:65`), expose consumers of `canUndo|canRedo|selectedKeyframeId` outside the cluster → **0**, `loadPreset` callers → 0, `caret-offset` → decl (`demo/styles/layout.css:139`) + 1 consumer, `activeTimelineRef` (RibbonBar :76/:84/:92/:100 + shortcuts :65/:70/:71; `any` at RibbonBar:140 / ControlsPaneWrapper:196 / useControlsKeyboardShortcuts:22), `RIBBON_BUTTON_CLASS = "h-8 …"` (:135) beside `size="sm"`, `formatComboParts` (shell/KeyboardShortcutsModal.vue:26/:50 — path corrected: `shell/`, not `transport/`), `demo/env.d.ts:3-7` (`*.vue` → `DefineComponent<{}, {}, any>`), `package.json` `"check": "tsc --noEmit …"` (no vue-tsc), ChannelControls.vue `:targets="animation.targets"` / `:animation-options="animation.options"` / `animation: KeyframesAnimation<any>` / local `defineAsyncComponent` bypass / `InstanceType<typeof KeyframeTimeline>`, TimingFunctionPanel.vue:145 (`props.animation.options.timingFunction = …` in place — the K-4 premise).
**Witness-staleness caveat (inherited from kf-CSSPasteDialog residue #8)**: `dist/gh-pages/assets/index-CL_QYCiO.css` is dated Jul-16, possibly pre-glass-7; every built-bundle byte-offset in the corpus is provisional. This record therefore re-derived every load-bearing token/cascade cell against the **installed** 7.0.0 dist instead; rows that still lean on the built bundle (D-12's layer roster, C-11's chunk graph, D-6/D-7's exact ratios) carry the caveat inline.

## Scoreboard

**55 ADJUDICATED-CONFIRMED (0 BLOCKER · 26 MAJOR · 21 MINOR · 8 INFO) booked here · 8 whole-row FOLDS + 3 partial-arm folds by reference to banked kf-CSSPasteDialog / kf-CSSCodeEditor / census / megatranche-R1 identities (identity guard, zero re-bookings) · 10 KILLED with re-executed refutations (three BLOCKER demotions, two kills-of-a-reader-solo adopted on this seat's own instruments, one kill that PROTECTS live code) · 15 net-new missed rows adjudicated IN (booked; 4 more missed candidates folded to banked identities rather than booked) · 13 UNPROVEN-NEEDS-LIVE residue rows to SS-13.**

Zero BLOCKERs are booked HERE — but two banked BLOCKERs land their damage on THIS component's data and are sequencing constraints on its wave: **kf-CSSCodeEditor KF-CE-2** (the stale 200 ms debounced emit full-replacing the wrong keyframe's vars — parent half already routed KF.W7) and **kf-ChannelControls L-2/C-2** (the `:key` remount destroying all authored keyframes + the undo trail on surface switch while expanded). The heaviest local truth is the **scrub-seam cluster** (C-1 + C-6 + L-3 + L-5): the instrument's primary affordance is inert, its only live engine write is the restore-side-effect of a hover, a SECOND engine stomps the scene's own DOM, and the corpus's own prescribed cures are mutually exclusive — individually MAJOR, jointly the reason census KF.W7 is "evaluate, not mechanical swap."

Status verbs per M-25: dispositions are **ADJUDICATED**, never VERIFIED — X·KF specs are forming; routings are agglomeration input. Wave targets from `CENSUS-2026-08-03.md:179-206` (KF.W0–W10). **No row here is NO-WAVE-OWNER**: this cluster is the census-named subject of **KF.W7 · Timeline Evaluate**, with discrete rows to KF.W6 (Glass Suffusion), KF.W8 (Structure & Colocation), KF.W4 (type-gate, by fold), KF.W2/W3 (parser ingress, by fold), KF.W9/SS-13 (visual horizon).

## Ruled disagreements (the seat's actual work)

| id | RR-A (design-and-usage) | RR-B (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-1/L-1/C-3 severity** | BLOCKER | MAJOR ("the functional consequence is only a missing enter/leave transition; the durable harm is the false, line-precise cross-repo citation") | **MAJOR — RR-B wins** | Facts unanimous and re-run by this seat: whole-package `fade-slide` grep → 0; the producer SOURCE tombstone read verbatim ("RETIRED, census-dead, clean break, no alias"); demo grep → only the 3 self-references (:90/:96/:311). A `<Transition>` with an unmatched name renders instant mount/unmount — no capability lost, no data lost. BLOCKER is reserved in this program for data loss, traps, and flagship capability absence (kf-ChannelControls killed-register #1's law). What endures is two comment blocks asserting a false provenance with a line-range (`transitions.css:23-37`) that cannot even resolve in the installed single-line artifact — a documentation defect of the KF-CE-41 class, cured in the same commit as the transition swap. RR-A's rung preserved in DISSENT. |
| **C-1 severity** | BLOCKER (a complete scrubber affordance — cursor, 7 handlers, playhead, 10× zoom — whose only effect is repositioning a div) | MAJOR (+ the sharpest fact in either return: C-1's prescribed cure would convert C-6's transient stomp into a continuous one) | **MAJOR, cluster-headed — RR-B wins the rung; RR-A's affordance framing is adopted into the row** | My reads: `useTimeline.ts:117` returns `scrub`; the :194-210 destructure takes 15 members, `scrub` absent; :83 is a bare ref write; my watch census = exactly 1 (a dialog-open flag) so no bridge exists; the ONLY live `scrub()` callers are `scrubAndCapture`'s :99/:114 — so the engine reaches the playhead position only as the RESTORE side-effect of hovering a marker. That is an inert primary affordance, not an absent instrument: capture/edit/import/export/undo all function. MAJOR under the program's BLOCKER law, with the cluster note above; RR-B's cure-conflict (missed row N-10) is booked so no wave can "fix" C-1 by wiring the stomp. |
| **D-16 rung** | MAJOR (kept the axis grade; killed only the height column) | MINOR | **MINOR — RR-B wins, on a decider neither reader stated** | The dead-end reading fails on co-visibility: RibbonBar's timeline block (:69, my read of :67-78) gates on `storedControls.selectedControl === 'timeline'` — the SAME condition that shows the timeline pane, so whenever the card is on screen, Snapshot/Import/Export/Add CSS are on screen too. What survives is a real two-visual-languages split (quiet 28px icons vs secondary text pills) and the card owning destroy-not-create — coherence, not a functional dead end. K-5 already killed the table's 28-vs-32 height delta (both rows floor at 36px — my dist read). RR-A/axis-D's MAJOR preserved in DISSENT with the co-visibility fact as the reopening condition (if the ribbon ever detaches from the pane, the rung reverts). |
| **D-13 rung** | MAJOR (axis grade) | MINOR | **MINOR — RR-B wins** | One binding, two renderings — my reads: `:25` literal `Undo (Mod+Z)` / `:41` literal `Mod+Shift+Z` vs `shell/KeyboardShortcutsModal.vue:26` rendering the SAME registry rows through glass-ui `formatComboParts` (import verified :50). A register-format inconsistency with a one-line, already-imported cure is MINOR. Path correction recorded: the modal lives under `shell/`, not the path both readers implied. |
| **C-7 proof status** | Confirmed the demo-side half by inspection; left the parser throw UNPROVEN ("I did not execute the parser") | Confirmed BY EXECUTION | **CONFIRMED — settled by this seat's OWN execution** | `node` against the installed `dist/subpaths/css.js`: `parseCssValues("oklch()")` → `TypeError: Cannot read properties of undefined (reading 'replace')`; same for `rgb()`/`hsl()`/`color()`; installed version re-read = exactly 4.0.0. The demo chain is complete in my reads: :252-260 admits `oklch()` as a value verbatim → `kf.vars` → un-awaited `rebuild()` (:264) → `buildAnimationFromTimeline` → engine parse → THROW → swallowed at useTimelineBuild.ts:47-50 (`console.error` + `animation.value = null` — the file's ONLY non-toasting failure; compare :121/:133/:137/:148/:155/:157). This is a genuine scope-correction on GATE-VERDICT §F-2's "no known consumer feeds the crash shape": the census covered kf's `src/` seams, not this live typing surface. |
| **L-15 kill** (RR-B solo) | — (not addressed) | KILLED: the ":248-249 guard cannot fire" claim is false — the guard is load-bearing against the stale 200 ms timer | **KILL ADOPTED — corroborated by a banked record** | The axis (challenge-L :300) argues the guard duplicates `selectedKeyframe.value` behind a `v-if` and "cannot fire." But the handler runs behind CSSCodeEditor's 200 ms debounce with **no cancel handle** — the exact mechanism banked at **kf-CSSCodeEditor KF-CE-2/KF-CE-18**, whose own text calls this parent null-check "the parent's null-check luck." A deselection (✕, Delete, undo, clear) inside the window unmounts the editor while the timer still fires; the `find` then legitimately misses. The axis's implied cleanup (drop the guard, use `selectedKeyframe.value`) would ship a crash. A kill that PROTECTS code — the rarest verdict class. The duplicated-lookup observation survives as INFO-grade redundancy only. |
| **L-10/C-10 kill** (RR-B solo) | — (not addressed) | KILLED the rationale: `ComputedRef<T>` IS assignable to `Ref<T>`; both casts are noise; the prescribed widening is unnecessary work | **KILL ADOPTED — re-derived on this seat's own instrument** | `@vue/reactivity/dist/reactivity.d.ts:346` `interface BaseComputedRef<T, S = T> extends Ref<T, S>`; `:353` `interface ComputedRef<T> extends BaseComputedRef<T>` — a declared subtype; `const r: Ref<HTMLElement[]> = computed(…)` compiles bare. The `:189-192` `as unknown as` double-casts launder nothing and should simply be deleted; the axis's remedy (widen useTimeline's parameters) is dead. Root cause of nobody noticing = the KF.W4 gate hole (fold below). |
| **M7's unrun falsifier** (RR-A's net-new MAJOR) | Filed MAJOR — but its quote of TimelineHoverPreview.vue:20 OMITS the `data-register="code"` attribute | — (not filed) | **CONFIRMED MAJOR — the falsifier run by THIS SEAT, and it held** | If any stylesheet reset `text-transform` under `[data-register]`, the row dies. My grep across demo/styles and the whole glass dist: **zero CSS rules** target the attribute — it exists only as the T.D4 font-census SELECTOR CONTRACT in `font-roles.json`. So `text-admin-label`'s `text-transform: uppercase` (installed semantic.css, verbatim) applies to the tooltip's declaration dump and `transform: translateX(10px)` renders `TRANSFORM: TRANSLATEX(10PX)` — case-lossy over case-sensitive value grammar (`var(--myVar)`, `url()`, custom idents) on the instrument's only textual ground truth. The omission of the attribute from RR-A's quote is recorded in DISSENT as citation-hygiene evidence: the reader quoted around the one token that could have killed its row. |

**Merged net-new pairs (both readers found the same defect independently — mutual corroboration, one row each)**: RR-A M2 ≡ RR-B missed-7 (pointer capture on transient `event.target` + caret pointerdown scrubs the playhead); RR-A M5 ≡ RR-B missed-3 (`@wheel.prevent` scroll trap); RR-A's D-4 mechanism correction ≡ RR-B missed-4 (the `// KEEP:` catch documents conditions the inner layer already swallowed — only `canvas.toDataURL`'s SecurityError can reach it).

## Adjudicated defect roster — 55 rows

Ids are the corpus's own, for life; net-new rows keep their finder's designation. Line anchors are keyframes `8281638c`; re-resolve before any cure (D-19). Every routing is M-25 agglomeration input for the forming X·KF specs.

### MAJOR (26)

- **D-1/L-1/C-3 — `<Transition name="fade-slide">` binds a class set glass-ui 7.0.0 does not ship, and two comment blocks assert a false, line-precise provenance** (`transitions.css:23-37` — a range that cannot resolve in the installed single-line artifact): whole-package grep 0, producer source tombstone "RETIRED, census-dead, clean break, no alias", demo grep = the 3 self-references (all my own probes). MAJOR per the ruled table (RR-A's BLOCKER demoted). **→ KF.W6** (adopt a shipped transition set or author one WITH the PRM bracket the comment falsely claims); the prose correction rides the same commit (the KF-CE-41 law: the comment is part of the defect surface); the phantom-dependency corroboration folds to census F-1.
- **C-1 — the scrub seam is not wired**: `scrub` returned (useTimeline.ts:117), never destructured (:194-210); `@update:scrub-t` is a bare ref write (:83); the complete scrubber affordance (TimelineTrack :24 cursor, :27-34 pointer+wheel+3 touch handlers, :52-55 playhead, 10× zoom) drives nothing; the engine reaches the playhead position only via `finally { scrub(prevT) }` after a marker hover (useTimelineBuild :112-115). Cluster head. **→ KF.W7** (the seam is W7's named subject; the cure must be designed jointly with C-6 — see N-10).
- **C-6 — a SECOND engine instance over the scene's own DOM**: ChannelControls :194 passes `animation.targets` by reference; timelineEngine.ts:51 spreads the same `HTMLElement`s into an independent `CSSKeyframesAnimation`; `scrub` → `interpFrames(t, true)` writes inline styles onto the scene's elements; the hover restore returns them to the TIMELINE's `t`, not the scene's. Cross-ref banked kf-ChannelControls L-16 (the engine-fields-as-props reactive gap — same boundary, milder trigger). Visible displacement → SS-13. **→ KF.W7** (own detached/cloned preview target, or drive the scene's single engine through AnimationControlsGroup).
- **L-3 — no reactive edge re-derives the animation**: my census — exactly ONE `watch` in the 1500+-line cluster (CSSPasteDialog.vue:65, a dialog flag); `animOptions`/`targets` read only inside `rebuild()`/`exportCSS`; every `rebuild` call imperative (all 9 sites enumerated by both readers, re-verified). The write path is live and non-reactive: `animation: KeyframesAnimation<any>` raw class prop, `props.animation.options.timingFunction` mutated in place (TimingFunctionPanel:145, my read). The finding is STRONGER than the axis stated — the source is non-reactive, so the failure is ownership, not a missing watcher (K-4 kills the axis's cure). **→ KF.W7**, joint with the banked L-16 identity.
- **L-5 — concurrent `scrubAndCapture` calls cross their scrub/restore pairs**: guard keyed per `kf.id` (:221), `prevT` read per call (:96), no lock/queue/generation counter, bare undebounced `@mouseenter` per diamond (TimelineTrack :83). Self-indicting: `nextFrame()` (:63-87) was hardened for EXACTLY this concurrent caller and stopped one layer short of the shared `prevT`. Stranded-playhead visibility → SS-13. **→ KF.W7**.
- **D-4/L-4/C-5 (+ the merged KEEP-catch correction) — `previewCache` is never invalidated or evicted; the tooltip pairs a LIVE percent (TimelineHoverPreview :3, :8 alt) with a pre-edit capture; ids are stable across every mutation (in-place `kf.vars`/`kf.percent`; `createKeyframeId` only on create/import) so every edit makes every cached thumbnail a lie; failed captures retry forever with no feedback; removed/cleared/imported-over frames orphan base64 PNGs (monotonic ids, no `delete` anywhere).** Mechanism correction merged (RR-A + RR-B convergent): `scrubAndCapture` swallows its own failures (`return null` at :92-93, :110-111), so the `// KEEP:` catch (:228-230) documents conditions that can no longer reach it — the ONE throw it can catch is `canvas.toDataURL`'s cross-origin SecurityError (:226), precisely the failure the comment does not name. Contrast: the sibling history is bounded (`capacity: 50`). **→ KF.W7**.
- **L-6/C-4 — a hand-rolled CSS declaration parser+emitter inside the SFC of a CSS engine's demo**: `:251-261` (`split("\n")` + `indexOf(":")` + comment-prefix skip) then `:263` `kf.vars = newVars` — a WHOLE replacement, so anything the scanner misses is DELETED, not mis-parsed; the emitter `:239-244` is the mirror. The sibling path in the same cluster honours the delegation doctrine (timelineEngine.ts:13 `parseAnimationCSS`, :90 `serializeCssValue` — my read), which makes this a bypass, not an omission; `@mkbabb/value.js/css` exports the collectors (RR-B verified). **→ KF.W7** (delegate), cross-ref **KF.W2** (the parse façade this bypass would have used).
- **C-7 — the inline editor is a live untrusted-CSS ingress to the R1 crash shape, and its containment is the file's only silent failure** — CONFIRMED BY THIS SEAT'S EXECUTION (header). The crash identity FOLDS to the banked megatranche R1 row (per the KF-CE-12 precedent) — never re-booked; the census correction to GATE-VERDICT §F-2 is recorded; the fuzz-entry note rides **KF.W2/KF.W3**; the silent `console.error` posture (vs the file's five toasting failures) books HERE **→ KF.W7** (one failure posture; cross-ref KF.W2's posture registry).
- **D-3 — the percent readout is a click-only `<div>`** (TimelineCaret :6-13: no role/tabindex/keydown/aria; `cursor-pointer` is the entire affordance) **and its successor `<input>` is 40×20 px at 10 px type** (:14-27) — **SC 4.1.2 + SC 2.5.8; the 2.1.1/BLOCKER arm is killed** (K-3; the true 2.1.1 residue is M3). **→ KF.W7** (the caret redesign's a11y arm).
- **D-5 (+ K-2 strengthening) — disabled Undo/Redo are pixel-identical to enabled**: `opacity-50` ≡ `--opacity-disabled: 0.5` (my token read — cascade-order-independent because equal); and NOTHING visual survives — `pointer-events:none` (my dist read) means the `cursor:not-allowed` cue never paints and `:hover` never engages. The file's own :7-10 comment calls these "the discoverable affordance… bounded by canUndo/canRedo" — boundedness real in the DOM, invisible on screen. **→ KF.W6**.
- **D-6 — quiet-emphasis pane icons composite to ~2.0:1 light / ~2.46:1 dark against SC 1.4.11's 3:1** — token fine (`--muted-foreground` at full opacity ≈5:1), `opacity-50` breaks it; Clear and Expand are never disabled so no exemption applies. Both readers reproduced the arithmetic independently to 2 d.p.; painted plate over glass = SS-13. **→ KF.W6**.
- **D-7 — the scrub rail has no perceivable boundary in either theme**: fill@50% vs card ≈1.01/1.11:1, border ≈1.86/2.00:1, border-vs-fill ≈1.84/2.22:1 (independent recomputations agree; token values reconciled against installed dist by this seat); the rail is the PRIMARY interactive surface, and expanded mode strips the Card plate entirely (:3 `border-0 shadow-none bg-transparent`). **→ KF.W6**; exact painted figures SS-13.
- **D-8 — `h-7 w-7`/`h-6 w-6` override height but not `min-block-size`** → 28×36 px (fine) / 28×54 px (coarse) pill-radius buttons; ALL machinery re-verified by this seat in the INSTALLED dist (immune to the witness-staleness caveat): `[data-icon-only]` trio, `--control-h-sm` `max()`, the coarse media flip, Tailwind emitting `height` only. The Input half hardened: `.field-control` carries NO min-block-size, Input is one element with caller classes merged (my `Input-*.js` read), so the `h-6` label Input genuinely renders 24 px — 33% under the smallest published control — even on coarse. **→ KF.W6**.
- **D-9 — `text-admin-label` on the user's own label input**: force-uppercases typed content, fixed 10 px at every viewport (`--type-admin-label: .625rem`, no clamp — my scale.css read), trips iOS focus-zoom while the repo's own cure (`clampIOSNoZoomFontSize`, applied to Monaco at CSSCodeEditor:137) sits unused, and on coarse pointers `padding-inline: calc(1rem × 1.5)` eats 48 px of the 128 px box (my field-control read). Letter-correction recorded: "the ONE type token with no clamp()" is false (micro/subheading/heading/title are also fixed) — the substance ("stays 10 px at every viewport") is untouched. Compounds with N-2: the control this typography wrongs is ALSO inert. **→ KF.W6**.
- **D-10 — capture on `@mouseenter` only; the tooltip opens on FOCUS** (reka `TooltipTrigger` `focus: handleFocus` → `onOpen` — my dist read) — keyboard users get the tooltip with `previewSrc` permanently undefined: the 4-property ghost forever. **→ KF.W7**.
- **D-11 — the zoom bar `v-if="zoomLevel > 1"` mounts mid-gesture** (~32 px row + gap ABOVE the track, no reserved height, no transition; `zoomLevel` continuous through 1.0 in both directions — my useZoomPan read) **and zoom has no discoverable affordance and no keyboard route** (wheel+pinch only, my read of the full return surface). **→ KF.W7**; magnitude SS-13.
- **D-12 — the diamond's selection/hover scale is un-transitioned**: the scoped rule transitions `transform` (TimelineTrack :224-227, and verbatim UNLAYERED in the shipped chunk — my read), Tailwind 4 writes individual `rotate`/`scale`/`translate`; `transition-all` on the marker (:69) is dead code that reads as live. The axis's honest PRM non-claim (the substrate blanket does cover this element) stands. Layer-roster cell carries the witness caveat; the mechanism is source-derived and stable. **→ KF.W7**.
- **D-14 — Clear-all and Remove ship undifferentiated**: same `size/emphasis/geometry/opacity` as Undo/Redo/Expand, no `tone="destructive"` (published and styled — my dist read), no confirmation; `clear()` empties the array and nulls the engine (:185-188). Undo mitigates the rung to MAJOR, not the row. K-6's glyph-size slip recorded. **→ KF.W6**.
- **D-15 — empty / single-frame / rebuild-failure states are unexpressed** (no empty branch anywhere — my whole-template read; `<2` contract surfaces only as an export-time toast; the one failure reachable BY TYPING is the one that doesn't toast). The whitespace-submit arm FOLDS → banked kf-CSSPasteDialog **R-17**; the closes-before-parse arm FOLDS → banked **R-4**. Glass-conformance tail stands: `/forms` publishes `invalid` + `useUserInvalidAria` on the subpath this file already imports. **→ KF.W7**.
- **D-17 — the expanded diamond occludes and hit-steals its own percent caret**: half-diagonals 11.31/14.14/16.97/21.21 px vs `--caret-offset: 14px` (decl+sole-consumer verified by my grep); marker carries `z-controls`, caret nothing, so the marker wins paint and hit-test regardless of DOM order. Photograph → SS-13. **→ KF.W7**.
- **M1 (RR-A net-new) — `overflow-x: clip` halves the celebrated 24 px hit pad and cuts the caret in half at 0% and 100%** — the two positions a keyframe timeline almost always occupies. The asymmetry is the proof of oversight: the TICK labels got explicit three-way edge handling in the same file (:44-47, my read); markers (unconditional `-translate-x-1/2`) and carets (unconditional `translateX(-50%)`) did not. Bounds S-1/S-2/S-3 below. Pixel counts → SS-13. **→ KF.W7**.
- **M2 ≡ RR-B-7 (both readers, merged) — a pointerdown on the percent caret scrubs the playhead and captures the pointer on a transient node**: the caret display div carries `@click.stop` ONLY (:12) while its own sibling input carries `@click.stop` + `@pointerdown.stop` (:25-26) and the marker carries `@pointerdown.stop` (:81) — so the caret's pointerdown bubbles to `onTrackPointerDown` (:168-171), which scrubs AND `setPointerCapture`s on `event.target`: a node that `startEdit` immediately unmounts (`v-if` swap) and that tick re-keying can unmount mid-drag — an in-flight drag dies with no pointercancel path. Reaching for "type an exact percent" silently discards the user's scrub position. **→ KF.W7**.
- **M3 (RR-A net-new) — a keyboard user cannot select a keyframe without mutating it**: `onMarkerKeydown` (:203-214, my read) computes `next` only for arrows/Home/End, discards Enter/Space (`if (next === null) return` — and a bare div synthesizes no click), and emits `select` ONLY paired with `moveKeyframe`; each inspection keystroke lands in the 100 ms-debounced undo history. Pointer users get non-destructive selection two ways (:192-196; caret `startEdit`). **This is the correctly-targeted residue of D-3's killed 2.1.1 arm.** **→ KF.W7**.
- **M5 ≡ RR-B-3 (both readers, merged) — `@wheel.prevent` cancels page/pane scroll BEFORE the handler runs, and a plain wheel matches neither branch** (ctrl/meta zoom; shift+zoomed pan — my useZoomPan read): a 48-128 px full-width band inside stacked `overflow-y-auto` containers where scrolling silently dies; `touch-none` (:24) removes the touch escape. The affordance D-11 says is undiscoverable is ALSO a scroll trap. Cure shape: prevent only on consumed events. **→ KF.W7**.
- **M7 (RR-A net-new; falsifier run by this seat) — the hover tooltip's declaration dump is force-uppercased** (ruled table). In a demo whose subject is CSS, the tooltip lies about the text of the CSS. **→ KF.W7** (register misuse; cross-ref KF.W6's type-register discipline).
- **N-2 (RR-B net-new; my grep) — the keyframe Label input is write-only dead state**: `v-model="selectedKeyframe.label"` (:106) + `label?: string` (timelineTypes :11) are the ONLY two occurrences — rendered nowhere, exported never (`buildAnimationFromTimeline` reads only selector+vars — my read), not on the expose surface; typed text is stored, deep-cloned into all 50 undo snapshots, and read by nothing. D-9 spent a MAJOR on this input's typography without noticing the control is inert. **→ KF.W7** (wire it — the hover preview and export are the obvious readers — or delete it).
- **(fold-cluster header, counted once above)** — see FOLDS for the five MAJOR-weight identities landing elsewhere: D-2/L-2/C-2 → R-3 · L-7 → KF-CE-8 · M4 → KF-CE-5 · M6 → R-1/R-5/R-6 · type-gate → KF-CE-16.

### MINOR (21)

- **L-8/C-9 — two divergent removals nine lines apart**: the ✕ (:117, with a `!` assertion) leaves `selectedKeyframeId` dangling; `removeSelectedKeyframe` (:289-294) clears it. Both live (✕ in-panel; Delete shortcut — my grep). Real consequence = undo-resurrection re-opens the editor on the restored id; BOTH axes' dramatized harms are dead (K-1). One-token cure (`@click="removeSelectedKeyframe()"`, deleting the `!`). **→ KF.W7**.
- **L-11 — `rebuild` is `async` typed/called as `() => void`** (useTimelineOps :19 — my read); `snapshot` toasts success before the rebuild can fail (:31-34); failures reach only `console.error`. **→ KF.W7**.
- **L-12/C-8 — 3 of 10 exposed members dead** (`canUndo`/`canRedo`/`selectedKeyframeId` — my repo grep: 0 consumers) **and the 7-verb command contract crosses three seams as `any`** (RibbonBar:140, ControlsPaneWrapper:196, shortcuts:22 — my greps), erased at hop ONE by the `*.vue` shim (env.d.ts:3-7). Typing is unverifiable until the KF.W4 gate lands. **→ KF.W7**.
- **L-13 — 22 returned members, 7 never taken; `loadPreset` is dead** (my grep: 0 callers) **and is the sole consumer of all 33 lines of `flattenVars.ts`**. **→ KF.W7**.
- **L-14/C-16 — `captureNonDefaultSnapshot` (the more careful strategy) unexported, uncalled** (snapshotCapture :34 — my read); invisible to `npm run check` (noUnusedLocals rides KF.W4/K3). **→ KF.W7** (adopt or delete).
- **L-16/L-17 — four copy-pasted Tooltip/Button blocks (~62 of 154 template lines) + two CSSPasteDialog mounts differing in four strings** (:11-72, :135-152 — my reads). Cure bound by `feedback_kiss_no_contrivance`: an in-file `v-for` over a descriptor, NO new component. **→ KF.W7**.
- **L-19/C-14 — the lazy barrel is dead**: index.ts exports async `KeyframeTimeline` under a chunk-rationale comment; the sole consumer writes its own `defineAsyncComponent` (ChannelControls :253 — my read). RR-B's sharpening adopted on its evidence: the instrument umbrella barrel above it is import-dead too. **→ KF.W8**.
- **C-12 — the cluster's closure reaches around the two-entry exports map into library-private `src/`** (timelineEngine :4 `@src/animation/internal/helpers`, :19 `@src/animation/compile/emit/css-text` — my reads; plus the keyframeSelector/parseAnimationCSS chain): the demo's round-trip is therefore not a consumer proof. Same breach class as banked KF-CE-12. **→ KF.W8** (publish-or-relocate) + **KF.W4** (depcruise-over-demo arm).
- **C-15 — the glass `/timeline` shadow is real for the 0..1 playhead ONLY**; `ContinuousRail`/`ScrubberTimeline` etc. are not importable; `geometry.d.ts` is weighted-segment math with no percent↔position map and no zoom/pan — so census lane-frontend **S-3 is corrected to a split verdict** (playhead = mechanical swap candidate; N-marker zoom/pan rail = no counterpart). Adopted on RR-B's installed-dist verification (endorsed by both readers; the corpus's best row). `left`-vs-`transform` cost → SS-13. **→ KF.W7** (it IS the W7 evaluation input).
- **N-6 (RR-B net-new) — two keyframes at the same percent silently MERGE in the built animation and the exported CSS while the track renders two diamonds** (timelineEngine :40-48, the comment at :42 admitting it — my read; arrow-keys step whole percents so collisions are the common case; no dedupe, no coalesced rendering, no surfacing). **→ KF.W7**.
- **N-8 (RR-B net-new) — `captureSnapshot` drops computed `none`/`auto`** (:16-21 — my read) over a property set including transform/filter/box-shadow: snapshots at rest yield asymmetric property sets and "animate to none" is unauthorable. **→ KF.W7**.
- **N-9 (RR-B net-new) — dead surfaces + a round-trip fidelity gap**: `isPlaying` is dead STATE (declared :31, returned :109, never written — my read); `clampPan` returned, never destructured (my TimelineTrack read); `TimelineKeyframe.easing?` never written/read and `importCSSToTimeline` drops per-stop `animation-timing-function` (my reads). The `CSSPasteDialog.textEl` arm FOLDS → banked **R-19**. **→ KF.W7**.
- **M8 (RR-A net-new) — neither text input has an accessible name**: the label Input is placeholder-only (`Label...`, uppercased by D-9); the caret `<input type="number">` has NO name of any kind; glass publishes `./label` and `./labeled-field`. **→ KF.W6**.
- **M9 (RR-A net-new) — the tooltip resizes under the pointer when the capture lands**: unsized 64×64 ghost swapped for a `w-36 h-auto` image of unknown aspect, mid-hover, no reserved box (my reads). On a motion-quality instrument, the one async surface jumps. **→ KF.W7**.
- **M10 (RR-A net-new) — typographic hierarchy inversion on the rail**: decorative ticks at fluid `text-small` (14-20 px) vs the EDITABLE percent at fixed 10 px, 14 px apart — D-9's register error at the composition level. **→ KF.W6**.
- **D-13 — literal `Mod+Z` tooltips vs the modal's `formatComboParts` chips** (ruled MINOR). **→ KF.W7**.
- **D-16 — create verbs on RibbonBar, destroy verbs on the card; two visual languages** (ruled MINOR on co-visibility; K-5 killed the height column). **→ KF.W7**.
- **D-18 — `font-mono` stacked redundantly on `text-admin-label` at 4 sites** (3 axis + TimelineHoverPreview :20; the utility already sets the family — my semantic.css read). **→ KF.W6**.
- **D-20 — the bespoke `role="slider"` markers get the UA focus ring**, not `.focus-ring` (class absent from the marker list — my read; no global focus-visible rule ships — RR-A's dist probe adopted), beside glass Buttons that carry it. **→ KF.W6**.
- **D-21 — zero `forced-colors` coverage; the timeline's entire state is background-color-encoded** (playhead/selected/unselected/rail — my reads; demo-wide grep 0 by both readers). Extends the census §6.5 categorical gap exactly as banked KF-CE-13 did. **→ KF.W9** (visual-audit horizon).
- **D-22 — three verbs for one operation (Clear/Remove/Delete) + filler placeholders (`Label...`, `Capturing...` with no live region)** — my reads. **→ KF.W7**.

### INFO (8)

- **L-18/C-11 — parent and child import the same three Tooltip symbols from two specifiers** (`@mkbabb/glass-ui/tooltip` :172 vs root barrel TimelineTrack :113 — my reads); the interesting duplicate-module/bundle hypotheses are dead (K-10). **→ KF.W7** hygiene.
- **L-20/L-21 — no unmount guard on an in-flight capture** (zero lifecycle hooks — my census; `finally` scrubs a detached target, writes into a disposed reactive; `useRafFn`'s scope-teardown half is clean) **+ the keyframe sort computed in two places** (useTimeline :33-35 / timelineEngine :37 — my reads). Contrast-cite: the banked kf-CSSCodeEditor superlative (the component it MOUNTS already meets the discipline). **→ KF.W7**.
- **C-13 — the core datum (`TimelineKeyframe.selector`) is typed by transitive value.js, and `clamp` is drawn from `value.js/math` at five sites** (all five seen in my reads). Legal; recorded as blast-radius input for the D-23 mirror-primary swap. **→ KF.W7** record.
- **C-17 — `targets` is plural, the preview reads `targets.value[0]` only, unlabeled as a sample** (:92 — my read). **→ KF.W7**.
- **Hygiene group (D-25/D-26/D-28)** — broken wrapper indentation (:2-4/:131-153); identity arithmetic `(panOffset/100)*100`, `(100/zoomLevel/100)*100` (:12-13); `--caret-offset` as an unprefixed `:root` singleton with one consumer (cluster defines ZERO custom properties — the honest counterweight stands). **→ KF.W7**.
- **N-10 (RR-B net-new, process) — C-1's prescribed cure and C-6's defect are mutually exclusive**: wiring `scrub` into the drag converts the transient hover stomp into a continuous one. The corpus prescribed a fix that worsens a confirmed defect; the remedy is upstream of both. Booked so the W7 spec author reads the scrub seam as ONE design problem. **→ KF.W7** spec input.
- **N-11 (RR-B net-new) — colocation split without a reason**: TimelineCaret.vue at the cluster root, its two sibling leaves under `components/`, the child importing back up (`../TimelineCaret.vue` — my read). **→ KF.W8**.
- **N-12 (RR-B net-new) — `markRaw` on a `shallowRef` payload is redundant** (:31/:46 — my read): the superlative credited two mechanisms; there is one. Not a defect; recorded so no future wave preserves `markRaw` as load-bearing while changing the ref kind. **→ KF.W7** note (corrects S-7 below).

## FOLDS — identity guard (anti-rename; zero re-bookings)

- **D-2/L-2/C-2 (the "merge"-labeled whole-array replace + success toast) = banked kf-CSSPasteDialog R-3** (MAJOR, → KF.W7). Both re-readers re-confirmed it as if fresh; the identity guard catches it. This record contributes the consumer-side re-verification (`:148` description, `:267-279` twin handlers, `useTimelineBuild:152` bare assignment — all my reads) and both readers' independent BLOCKER→MAJOR demotions, which AGREE with the bank's undo-recoverability ruling.
- **L-7 (the 200 ms canonicalisation echo rewriting the buffer under the caret) = banked kf-CSSCodeEditor KF-CE-8** (the external-sync watch; the bank already names THIS parent's lossy transform at :251-261/:241-243). Fold contributes the parent-side evidence: the insertion-order re-serializer (:239-244) + the `:model-value`/`@update:model-value` loop (:124-126). Percept residue stays at the bank's SS-13 row.
- **M4 (`accessibilitySupport: "off"`) = banked kf-CSSCodeEditor KF-CE-5** (MAJOR, aggravator of the KF-CE-3 trap BLOCKER). RR-A re-derived it independently — evidence of its reality, not a second defect (the kf-ChannelControls DISSENT-2 law).
- **M6 (the contenteditable `<pre>` dialog vs shipped Textarea) = banked kf-CSSPasteDialog R-1/R-5/R-6** (the S-9 swap spine, → KF.W6). The twin mounts (:135/:145) verified here.
- **C-18 (dialog closes before the async parse settles; toast over a dismissed dialog) = banked R-4**; **D-15's whitespace-submit arm = banked R-17**; **N-9's `textEl` expose arm = banked R-19**.
- **RR-B missed-1 (no `.vue` file typechecked anywhere) + missed-5 (`debounce(fn, 200, false)` TS2554) = banked kf-CSSCodeEditor KF-CE-16** (the gate hole → **KF.W4**, "the sequencing head of the whole record"). This cluster's corroboration rides as evidence: the env.d.ts shim + `InstanceType` erasure at hop one (my reads).
- **C-7's crash identity = the banked megatranche R1 row** (parser lanes, KF.W2/W3) — the ingress and the posture book here; the crash books once, there.
- **F-1 (phantom glass-ui dependency)** — every glass claim in this record resolves against an installed tree no manifest declares; census F-1 owns it (lane-frontend §10 step 1). Reproducibility caveat inherited.
- **Cross-refs, not folds**: kf-ChannelControls **L-2/C-2** (the `:key` remount destroying THIS component's state — must be settled before/within KF.W7) and **L-16** (stale `targets` across the markRaw boundary — joint identity family with L-3 here).

## Killed-claims register — 10 rows, refutations re-executed

1. **K-1 · C-9's "the user must press Delete twice, the first press doing nothing" (+ L-8's "a frame the user has no memory of selecting")** — REFUTED at my :289-294/:48-54 reads: after the ✕, the first Delete no-ops AND nulls the id; the second fails the guard entirely. No state exists where two presses do what one wouldn't — nothing is selected. The user necessarily selected the frame to reach the ✕. The dangling-id defect is real (L-8/C-9 row); both dramatized harms are manufactured.
2. **K-2 · D-5's "what survives is `cursor: not-allowed`"** — REFUTED by the rule the axis itself quotes: `.button:disabled{pointer-events:none}` (my installed-dist read) removes the element from hit-testing, so its cursor declaration never paints. Nothing visual survives — the headline is STRENGTHENED.
3. **K-3 · D-3's WCAG 2.1.1 citation and BLOCKER rung** — REFUTED as stated: 2.1.1 is written over FUNCTIONALITY; retiming is fully keyboard-operable on the sibling marker (:203-214 — my read), which D-3's own body cites before ruling the SC failed anyway (challenge-D :161 — an internal contradiction). Survives at MAJOR under 4.1.2 + 2.5.8. The residual, correctly-targeted 2.1.1 argument is **M3** — about the MARKER's destructive-only keyboard selection, not the caret.
4. **K-4 · L-3's prescribed cure (`watch([animOptions, targets], rebuild, { deep: true })`)** — REFUTED as structurally sound: the bound source is `animation.options` on a RAW `KeyframesAnimation` class instance (`KeyframesAnimation<any>` prop — my read), mutated in place (TimingFunctionPanel:145 — my read); a deep traversal of a non-reactive object registers no dependency; the watcher can never fire. The cure would compile, typecheck, and change nothing. The finding survives STRONGER (non-reactive source ⇒ ownership cure).
5. **K-5 · D-16's height column ("card 28×36 vs RibbonBar `h-8` = 32 px")** — REFUTED with my own installed-dist read: the BASE `.button` rule carries `min-block-size: var(--button-size)`, and the ribbon's `size="sm"` (my RibbonBar read) puts the same unopposed 36 px floor under `h-8` as under `h-7`. Both rows render 36 px (54 coarse). The split finding survives on emphasis/shape/labelling alone.
6. **K-6 · D-14's "14 px trash glyph"** — REFUTED: :53 is `<Trash class="icon-sm">` and the demo's `.icon-sm` CLASS is 16 px (`icon-xs` 14 px sits only on the ✕ at :119). Trivial; recorded because the figure is load-bearing in D-14's narrative — and because the demo class collides with glass's `--icon-sm: 0.875rem` TOKEN (my sizing.css read), the R-16 collision family at another site.
7. **K-7 · L-9's central mechanism ("the fallback requires html2canvas to throw")** — REFUTED at my useTimelineBuild :92-93/:103-111 + KeyframeTimeline :225 reads: `scrubAndCapture` returns `null` on every ordinary failure, `if (canvas)` leaves the cache unset, and the ghost renders exactly as documented. The fallback is the DEFAULT outcome of failure, not dead code. What survives is only the successful-but-blank WebGL capture — marked UNPROVEN by the axis itself. The MAJOR does not survive; the sharper true statement is D-4's retry-forever arm.
8. **K-8 · L-15 ("the :248-249 guard cannot fire")** — REFUTED (ruled table): the stale 200 ms timer (banked KF-CE-2/KF-CE-18 mechanism, no cancel handle) makes the guard load-bearing; the axis's implied cleanup would ship a crash. **A kill that protects live code.**
9. **K-9 · L-10/C-10's cast rationale ("`ComputedRef` is not assignable to `Ref`")** — REFUTED on my own reactivity.d.ts read (:346/:353 — declared subtype). The `:189-192` double-casts are pure noise; delete them, widen nothing. The reason nobody noticed is the KF.W4 gate hole (KF-CE-16 fold).
10. **K-10 · C-11's bundle-graph tail ("the child drags the glass barrel into the timeline chunk")** — REFUTED by RR-B's artifact read (the chunk imports from the SHARED `glass-ui-*.js` module), adopted with the witness-staleness caveat noted; the conclusion is robust (dozens of SFCs import the barrel — it is a shared chunk by construction). Only the consistency clause survives (L-18/C-11, INFO).

**Citation corrections (conclusions unaffected, recorded because these documents present as provenance-exact)**: D-9's "the one type token with no clamp()" — false in the letter (my scale.css read); L-6's CSSCodeEditor anchors `:114`→`:140`, `:184`→`:178-187` (RR-B); KeyboardShortcutsModal lives under `shell/` (both readers' paths silently wrong); RR-A's D-3 target estimate 20×10 vs the axis's 22×10 (immaterial); RR-A's M7 quote omitted the `data-register` attribute (DISSENT-4).

## Superlatives — provenance preserved (L-18 runs both ways)

- **S-1 · the counter-rotated 24 px hit pad** (all three axes + both readers; verified by this seat in source :229-241 AND verbatim in the shipped chunk): `translate(-50%,-50%) rotate(-45deg)` makes the pad an axis-aligned square — the thing WCAG 2.5.8 actually measures — without moving a pixel of the diamond, with the reasoning in the comment. **BOUNDED**: hard-px ignores `--ui-scale` (24 is the floor, not glass's 44 px `--touch-target`); M1 clips it to ~12×24 at 0%/100%; `scale-125` scales the pad too — an undesigned 24→30 px target oscillation on selection.
- **S-2 · the full ARIA slider keyboard contract, hand-built** (:74-79 + :203-214, my reads): correct role/name/value, arrows both axes, Shift 10×, Home/End, `preventDefault` AFTER the unhandled-key early return, one clamped mutation shared with the pointer path. Most hand-rolled timelines ship drag-only. **BOUNDED by M3**: no non-destructive keyboard selection exists.
- **S-3 · `overflow-x-clip overflow-y-visible`** (:24) — the one mixed pair CSS Overflow 3 does not coerce, chosen correctly, uncommented. **BOUND**: it is also M1's mechanism, and the file's own :44-47 proves the author knew — for the decorative layer only.
- **S-4 · the `useRefHistory` bind + the undo/redo rebuild wrappers** (useTimeline :56-103, read whole): three non-obvious options each argued against the failure it prevents; the wrappers `rebuild()` because undoing back through a `clear()` must re-materialize the engine — the exact case a naive bind ships broken. The clone-totality falsifier (readonly discriminated-union selectors) was formed and killed by both readers independently.
- **S-5 · the `nextFrame()` re-entrancy repair** (:63-87): resolver queue replacing the single slot, cured defect named in prose, lifecycle delegated to `useRafFn`. Textbook — and simultaneously the self-indictment beside L-5 (same author, same concurrent caller, one layer short of `prevT`).
- **S-6 · the LIGHT/HEAVY firewall + the container/presentational boundary**: engine reached only via `loadAnimationEngine()` (timelineEngine :33/:67 — my reads), `defineAsyncComponent` at both wrapper sites, Monaco's own dynamic boot; TimelineTrack takes six props, emits four events, holds no engine reference, does no I/O.
- **S-7 · `markRaw` + `shallowRef` on the engine object — CORRECTED**: the protection is real but SINGLE-mechanism (`shallowRef` never proxies); `markRaw` is redundant (N-12). The instinct is right; the credit is halved.
- **S-8 · the tick-label three-way edge handling** (:44-47, RR-A's find, my read): clip-aware placement, uncommented, correct — and the provenance that makes M1 a defect rather than an unknown.
- **S-9 · corpus method** — C-15 is exemplary audit work: it contradicted a standing census row (lane-frontend S-3) on two checkable points, checked both against the installed artifact, and replaced it with a sharper split verdict (re-verified by RR-B, endorsed here). Likewise axis-D's :433 refusal to stack a false PRM defect on a true one. Cross-cite (not re-booked): CSSCodeEditor's unmount-race discipline is banked at kf-CSSCodeEditor's superlatives — the contrast that convicts L-20 as a regression against an in-tree standard.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs (route with KF.W9)

1. **L-9's surviving half / D-4's WebGL arm** — whether html2canvas RESOLVES blank (rather than throws) on the `cube`/`amiga` scenes, caching a truthy-but-blank thumbnail forever. The single sharpest probe for this component.
2. **D-6/D-7 composited ratios** over the live `Card cartoon tier="quiet"` plate (and the expanded-mode transparent plate). Token math reconciled to the installed dist by this seat; direction structural; magnitude live-only.
3. **D-17's rendered overlap** — arithmetic and paint order settled statically; whether it reads as defect at shipped scale needs a photograph.
4. **M1's residual target widths** at 0%/100% (nominally 12×24) and the legibility of a half-cut "100%" caret.
5. **L-5's visible severity** — stranded playhead, crossed capture.
6. **C-6's visible displacement** — running scene (next rAF overwrites) vs paused/settled (displacement persists).
7. **D-11's mount shift** (~32 px estimated).
8. **C-15's `left`-vs-`transform` compositing cost** (three surfaces).
9. **Tick-label overlap with the rail at large viewports** (fluid `--type-small` upper clamp vs the fixed `-top-5`).
10. **D-24 · RTL** — all geometry physical; mirrored toolbar over an unmirrored axis is the predicted accident.
11. **L-3's blast radius** — whether any other surface force-rebuilds after an options edit (the one-hop failure is proven).
12. **KF-CE-8's caret-jump percept** — carried at the bank's residue, not double-booked.
13. **Witness refresh** — re-run `build:gh-pages` before trusting ANY byte-offset receipt in this corpus (the Jul-16 bundle may predate glass-7); this record's token/cascade cells were re-derived against the installed dist and are exempt.

## DISSENT — preserved, not ruled away

1. **RR-A's BLOCKER rungs on D-1 and C-1** — preserved. For C-1: if the W7 spec rules that scene-scrubbing IS the instrument's core promise (the affordance says so; the wiring says otherwise), the cluster head inherits blocker weight as one seam. The demotions here are program-law rulings (BLOCKER = data loss / trap / flagship capability absence), not mechanism refutations.
2. **RR-A's MAJOR on D-16 and D-13** — preserved; D-16's rung reopens if the ribbon ever stops being co-visible with the card (the decider recorded in the ruled table).
3. **Reader convergence as a risk surface** — the two hostile re-readers agreed on nearly every severity correction and every kill. Per the suspicious-agreement law, this seat re-measured the four most load-bearing agreed cells with its own instruments (fade-slide 0-count; the min-block-size cascade; the watch census; the C-7 execution) — all held. Recorded because high reader agreement is cheap exactly where both inherit the same corpus.
4. **RR-A's M7 quote omitted `data-register="code"`** from its citation of the very line it convicts — the one attribute that could have hosted the killing rule. The row SURVIVED this seat's probe (zero CSS rules), but a quote that elides its own strongest falsifier is citation-hygiene evidence for the fleet, joining the ColorSpaceSelector citation-inheritance event and kf-CSSPasteDialog K-9. Spec authors: require the verbatim element, not the class list.
5. **RR-B's L-9 kill vs RR-A's K7** — same kill reached independently with the same mechanism; no conflict, recorded as the corpus's one genuinely duplicated verdict (evidence of the kill's reality).
6. **HEAD movement** — zero line drift observed at every site this seat re-opened against `8281638c`; anchors remain re-resolve-before-cure (D-19).

## Closing verdict

**ADJUDICATED — RED at keyframes HEAD `8281638c`: 55 rows stand (0 BLOCKER · 26 MAJOR · 21 MINOR · 8 INFO) with 8 whole-row folds + 3 partial arms discharged by reference to banked kf-CSSPasteDialog (R-3/R-1/R-4/R-17/R-19), kf-CSSCodeEditor (KF-CE-2/KF-CE-5/KF-CE-8/KF-CE-16), census F-1, and the megatranche R1 identity — zero re-bookings; 10 claims killed with re-executed refutations (three BLOCKER demotions, one kill that protects live code, two reader-solo kills adopted on this seat's own instruments); 15 net-new missed rows booked, one of them (C-7) settled by this seat's own execution of the R1 crash shape against installed value.js 4.0.0 — a live scope-correction on GATE-VERDICT §F-2. The component's center of gravity is the scrub seam read as ONE design problem (C-1 + C-6 + L-3 + L-5 + N-10: an inert primary affordance, a second engine over the scene's DOM, a non-reactive options source, an unguarded shared restore, and mutually-exclusive prescribed cures) — the concrete reason census KF.W7 is "evaluate, not mechanical swap." Routings: the bulk to KF.W7 (the census names this cluster); glass/token/contrast/name rows to KF.W6; structure rows to KF.W8; the type-gate and depcruise arms to KF.W4 by fold; the parser ingress to KF.W2/W3 by fold; 13 residue rows to KF.W9/SS-13. Sequencing constraints: the KF.W4 vue-tsc gate precedes verification of every typed cure here, and the two banked BLOCKERs that destroy this component's data (KF-CE-2's stale emit; kf-ChannelControls' `:key` remount) must be settled by or before the W7 spec that owns this roster. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis. The three axis challenges and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/timeline/KeyframeTimeline.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **KeyframeTimeline.vue=+66/-49**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**CORRECTION (vi) of the six named at G-0.9 — THE HEADER'S SUBSTRATE EQUATION IS FALSE AND IS CORRECTED HERE BY APPENDED NOTE** (C-1.R row 4; the attached correction of `kf-TimelineCaret`'s MISS-α5 ≡ RR-β K-BASE). The header at `:6` reads *"HEAD `8281638c` (re-confirmed by this seat, **working tree = the audited tree**)"*. **That equation is false at every coordinate this wave measured**: at the audit's own date the checkout was **41 behind and 1 ahead** of `origin/master`, neither ref an ancestor of the other (merge-base `a59d3a22`), with **252** tracked modifications and **124** untracked files — two of the untracked ones cited BY LINE in landed records while absent from `8281638c`'s tree. A record whose header asserts the equation cannot state which ref it cures against, which is the whole content of this stamp.

**The witness-staleness caveat at `:10` is DISCHARGED**: the Jul-16 `index-CL_QYCiO.css` is superseded by a fresh, dated, hashed build (`index-CBB2Hr7m.css`, 2026-09-17 13:37). Its findings re-derive GREEN; only the filename moved.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

