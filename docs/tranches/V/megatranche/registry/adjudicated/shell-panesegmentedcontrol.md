# shell-panesegmentedcontrol — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · served model: `claude-fable-5` (Fable 5)

**Seat**: incredulity adjudicator over two hostile re-readers — reader-1 (design-and-usage) · reader-2 (library-and-code) — who each re-read the full challenge corpus. Model receipt observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-08 · **HEAD**: `f99a55c9` · branch `tranche-u` · **Subject**: `demo/shell/PaneSegmentedControl.vue` (52 lines, read whole by this seat). The corpus was authored at `c654824e` (C, L) / `7775473b` (D); the subject file has exactly ONE commit in its history (`git log` → `6dc12aad`, the R100 rename) and is byte-identical across all three HEADs — every line anchor below was re-verified at `f99a55c9`, not inherited.
**Corpus (r2 governs)**: `audit/components/shell-panesegmentedcontrol/{challenge-C-implementation,challenge-D-design,challenge-L-library}.md` — all three read whole by this seat (458/629/524 lines). Axes: C = Opus 5 [1m], D = Opus 5 [1m], L = Opus 5 [1m], receipts declared in each file.
**Method (L-18)**: both reader returns were attacked before either was adopted. Every verdict-level disagreement (L-1 severity MINOR-vs-MAJOR · C-2/L-5/D-6 severity MAJOR-vs-MINOR · L-4/C-5/D-9 severity MINOR-vs-MAJOR) was re-derived against the tree with this seat's own instruments; every kill was re-proven before being carried (none re-opened, one evidence cell inside a kill corrected); two reader evidence cells are corrected by this seat (reader-1's seven-assertion count; the D-8 STATES.json cell both readers flagged); one seat-original sharpening is added to the ink row (the `@container` and `data-backdrop-mode` arms).
**Arbiter's own instruments (all static — no browser tooling this seat, per the work order)**: full reads of PaneSegmentedControl.vue (52), useViewManager.ts (100), the Dock.vue slices :60-80/:125-135/:160-205, viewSchema.ts type + full 14-row VIEW_MAP census (`easing` grep → 0), usePaneRouter.ts:59-62/:178-182, walk.spec.ts:10-45 + the full `aria-pressed` grep (9 assertion lines), shell.css:95-130, App.vue:1-3/:74-92/:305-315, index.html:1-12, foundation.css:678-722/:723-730, focus-ring.css:25-40, VISUAL-CONSTITUTION.md:30-34/:57-59/:74-90 (all seven anchors line-exact), CARRY-LEDGER.md:22 verbatim (incl. the D53.iv rider); installed glass-ui 7.0.0 whole-file reads: `dist/components/dock/styles/adaptive-legibility.css`, `dist/components/tabs/styles/segmented.css`, `dist/components/tabs/styles/drag.css`, `dist/useMotionAxis-D-rtIhca.js` (whole, 20 lines); targeted greps over `dist/tabs.js` (`variant:{default:"pill"}` · `activation:{default:"automatic"}` · `motion: {}` · `W.value ? "tablist" : "group"` · `semantics === "tabs" || !p.semantics && M.value` · `"aria-label": p.ariaLabel` · the pressed/selected/controls spreads) and `dist/useTabRovingFocus-YnWh-Ytr.js` (`rtl` ×2 + `direction` ×1, the belt-and-braces arm verbatim, `getBoundingClientRect()` deltas, `ResizeObserver` ×1, `tabWidth|contentBoxLeft` → 0); censuses: `<SegmentedTabs` (3 sites), `font-display` at all three (2-of-3 on the strip; AdminNamesPanel also on two Buttons), one-level `../` edges out of `dock/` (6 rows, Dock.vue:15 the unique SFC), PaneSlot (SFC = 1 consumer App.vue:177; `export interface PaneSlot` usePaneRouter.ts:61), the L-6 shim importers (exactly 3 rows total), `focus-visible` = 0 in all three producer tabs files, `ls scripts/ci/` + repo-wide `css-emission-probe` find → absent, STATES.json keyboard-focus-desktop `#/` focused cell = `span[l component value]`; tranche-X ownership reads: W5.md:43/:100/:195-201/:270-274/:332, W10.md:117/:142/:235-237/:354, CONFORMANCE-2026-08-03.md:869.
**Motion/PRM law**: `motion-quarantine.md` grep (`panesegmented|PaneSegmented|segmented`, case-insensitive) → **zero rows**; no M-xx FALSE row constrains this record and none is cited. Every motion claim herein is structural (useMotionAxis + drag.css read at source); the PRM transition measurements stay in UNPROVEN.
**Status verbs**: every disposition below is **ADJUDICATED**, not VERIFIED. Nothing here stamps a wave; X-W5's own gates do that when it executes.

## Scoreboard

**24 adjudicated rows: 10 MAJOR · 12 MINOR · 2 INFO · 0 BLOCKER.** Both axis-claimed BLOCKERs (D-1, L-1's severity) died under both readers and stay dead under this seat's re-derivation. **12 killed claims** in the register. **12 missed findings** ratified (11 reader-net-new after de-duplicating the converged ink row, + the ink row's seat sharpening). **Verdict: DEFECTIVE — DELETE, DON'T REPAIR.** The canon retired this component twice in writing (VISUAL-CONSTITUTION.md:32, :89), the retirement wave exists and is specified (**X-W5**, which holds `delete` on this file at W5.md:100, kills `mobilePaneIndex` at gate C3, and counts this SFC's own media query at gate C7), and 17 of 24 rows discharge by that deletion rather than by any patch. No interim repair is booked; two producer defects and one producer robustness gap ride the glass-ui BJ relay; one instrument defect and one orphan a11y arm carry NO-WAVE-OWNER.

Strongest surviving finding: **PSC-1** (C-3) — three producer defaults adopted by omission whose combination describes no real control: `toggle` semantics + `automatic` activation + zero panel linkage, so either arrow key commits a destructive pane remount inside an unnamed `role="group"`, on the only route to the second pane on mobile.

## Ruled disagreements + corrections (the seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L-1 severity** (mis-homing) | MINOR | MAJOR (down from L's BLOCKER) | **MINOR** | Reader-2's own rationale — "a directory placement with zero runtime consequence" — is the MINOR argument. The cure is deletion (X-W5 gate C4 `test ! -e demo/shell/PaneSegmentedControl.vue`, W5.md:271), at which point the homing is moot; the structural weight of "this file should not exist" is already carried at MAJOR by PSC-4 (C-1). A severity that survives only until its file is deleted by an already-specified wave is not MAJOR in a corpus whose MAJORs reach users or breach canon. Both readers' kills of L-1's supporting generalizations are confirmed (see the register). |
| **C-2/L-5/D-6 severity** (unnamed `role="group"`) | MAJOR | MINOR | **MINOR** | My reads: the BUTTONS are named (walk.spec drives them by role+name; the corpus's REPORT.json cell `namelessButtons: 0` is quoted identically by both readers) — the anonymous node is the container only, and `aria-label` on it is context, not operability. The graver failure on the same surface — focus-commits-selection destroying the main region (PSC-1) — already carries the MAJOR. Aggravator recorded: with the group unnamed, the M4 label collision ("Palettes" as route AND as pane, six views) is unmitigated for AT — booked at PSC-20, and the aggregate is why this MINOR is not INFO. |
| **L-4/C-5/D-9 severity** (four stacked guards + masking `?? ''`) | MINOR | MAJOR | **MINOR** | All four guards are dead TODAY by my own 14-row enumeration (`right === null` iff `rightLabel === null`; sole null row `atmosphere` viewSchema.ts:170-178, all five admin rows `right:"palettes"`/`rightLabel:"Palettes"`); the silent-blank path needs a future row that violates the schema invariant, and X-W5.c replaces the two-field encoding itself (`regions[]` replaces `left`/`right`/`defaultPaneIndex`, W5.md:43). A latent masking path whose enabling encoding is already scheduled for structural dissolution is MINOR — with the binding accretion that the `regions[]` type must make the disagreement unrepresentable (else the finding revives at the new seam). |
| **D-2 scope** (ink) | universal-collapse form (its missed M2) | universal-collapse form (its missed M1 + killed D-2-scope) | **CONVERGED — adopted, plus a seat sharpening** | My whole-file read of `adaptive-legibility.css`: the base `@layer components` block sets `--muted-foreground: var(--foreground)` on `:where(.glass-dock)` UNCONDITIONALLY, outside the `@supports (color: contrast-color(white))` arm. Seat addition: the `@container style(--glass-backdrop: light)` arm and the `[data-backdrop-mode="static"]` arm touch tint/`--dock-fg-on-aurora` only — NEITHER restores a muted/normal delta, so the collapse is not backdrop-conditional either. The inversion D-2 measured is the `contrast-color()`-engine specialization of a universal zero-delta. |
| **reader-1's M1 assertion count** | "seven hard assertions … :95, :96, :108, :109, :117, :118 and :146" | (not carried) | **CORRECTED to NINE** | My grep at `f99a55c9`: `toHaveAttribute("aria-pressed", …)` at walk.spec.ts:95, :96, :108, :109, :118, :119, :146, **:156, :157** — reader-1 missed the two in the Mix re-route block and drifted two line numbers. The correction STRENGTHENS the finding (any `semantics="tabs"` interim patch turns nine assertions RED, not seven). |
| **D-8 evidence cell** | flags the STATES.json misquote | flags the same | **CONFIRMED, both readers right** | My read: `keyboard-focus-desktop` `#/` `focused` = `span[l component value]`, not the `body[→lab(92%…)Hom]` string the D axis printed. The coverage conclusion (no pane labels in the three desktop-only matrices) is unaffected — I checked the gradient/browse rows too and they carry route labels, not pane labels. |

Kills were re-derived, not inherited: D-1's four-part refutation re-proven at source (the `rtl` ×2/`direction` ×1 census in `useTabRovingFocus-YnWh-Ytr.js`, the measured-rect engine with zero index arithmetic, `motion`-independent `ResizeObserver`-only invalidation, `index.html:11` `dir="ltr"` + the U-F58 scope comment); D-7-forcedcolors re-proven (foundation.css:678 opens the block, the `:where(...)` roster at :698-720 includes `button` and `[tabindex]:not([tabindex="-1"])` → `outline: 2px solid Highlight`); C-8(b) re-proven (`dataMotion: computed(() => o.value === "full" ? void 0 : o.value)` — absence IS the encoding); L-6-counts re-proven (exactly 3 importer rows total: `ViewId` ×2, `PaneConfig` ×1).

## Adjudicated defect roster

Line anchors are HEAD `f99a55c9`; re-resolve before executing any cure. **Wave-owner key**: X-W5 = `docs/tranches/X/waves/W5.md` (holds `delete` on the subject at :100; unit c "One mount, one column" — `regions[]` replaces `left`/`right`/`defaultPaneIndex`, every `useBreakpoint`/`isDesktop`/`isMobile`/`mobilePaneIndex` layout fork dies, walk.spec.ts + page-load-mobile.spec.ts modify-carve; gates C3/C4/C7 measured RED in the spec itself) · X-W8 = MT-DOCK-LAYERS-1 (the Dock-adj DELTA-5/G-L breakpoint-predicate family) · X-W10 = the closed type matrix (CC-098; also holds this file's Ad-18 marker row at W10.md:117, discharged by X-W5's delete as the survivor's terminal event) · GLASS-BJ = the standing glass-ui BH/BJ inbox relay (owner edict) · NO-WAVE-OWNER = flagged for the next formation boundary.

### MAJOR

- **PSC-1 (C-3, with D-6's semantics arm) — MAJOR — toggle semantics + automatic activation + zero panel linkage.** Three producer defaults adopted by omission: no `semantics` ⇒ pill→`toggle` ⇒ `role="group"` + per-button `aria-pressed` (tabs.js `W.value ? "tablist" : "group"`, `W = semantics === "tabs" || (!p.semantics && M.value)` — my greps); `activation:{default:"automatic"}` ⇒ focus move COMMITS selection (`useTabRovingFocus` automatic branch, both readers' quote re-confirmed) with 2-option wrap so either arrow flips the pane every press; `SegmentedTabOption.controls` (d.ts:17) never populated and the governed panel — App.vue:77's `.pane-slot-mobile` div — carries no `id`, no `role="tabpanel"`, no `aria-live` (my read of :74-92: class list + style attr only). A keyboard user arrowing through an announced button group silently destroys and rebuilds the page's main content per keypress (remount cost through PaneSlot's rAF-deferred commit — corpus-verified). **Disposition: X-W5.c — the deletion + `regions[]` IS the cure of record. Binding accretion (reader-1's M1, ratified + corrected)**: the `semantics="tabs"` interim patch prescribed by two axes is REFUSED — it silently disarms the producer's drag path (drag.css keys its only hit-test rule on `[aria-pressed="true"]`, which `tabs` semantics stops emitting — my whole-file read) and turns NINE walk.spec assertions RED, both consequences uncosted in the axes. No interim patch lands; the wave's walk.spec modify-carve ("amputation assertions become presence assertions", W5.md) carries the spec side.
- **PSC-2 (D-2 ≡ reader-1 M2 ≡ reader-2 M1, seat-sharpened) — MAJOR — the selection ink is DEAD in every engine, inverted in `contrast-color()` engines. GLASS-OWNED mechanism.** The producer's entire state signal is `color: var(--muted-foreground)` → `var(--foreground)` when pressed (segmented.css, my read). Inside `.glass-dock`, `adaptive-legibility.css`'s BASE block sets `--muted-foreground: var(--foreground)` unconditionally — selected/unselected delta exactly 1.00:1 everywhere — and the `@supports (color: contrast-color(white))` arm then sets `--muted-foreground: contrast-color(var(--card))` with `--foreground` untouched, producing the measured inversion in WebKit. Seat sharpening: the `@container style(--glass-backdrop: light)` and `[data-backdrop-mode="static"]` arms restore nothing. `@media (prefers-contrast: more)` (foundation.css:728) raises the token at `:root, .dark` only — the dock's own descendant declaration wins by inheritance proximity, so elevated contrast does not rescue it. Selection is single-channel (the indicator fill) by construction of the container — VISUAL-CONSTITUTION.md:83 breached in all engines, for different reasons. The e2e engine detail (mobile suite = Pixel-7 **Chromium**, walk.spec:25-28 my read) means the one automated gate exercising this control runs where the corpus asserts the zero-delta form; the Chromium `contrast-color()` support cell itself sits in UNPROVEN. **Disposition: GLASS-BJ relay — the dock's aurora-legibility rule collapses the muted/normal pair that state controls build on; the cure must move the state channel (weight / non-color mark / a dock-local selected-ink token), NEVER a clamp (see killed D-2-cure). App side: discharged by X-W5.c — after the delete, zero `SegmentedTabs` remain inside `.glass-dock` (the three preserved P092 sites — owner-state, AdminNames, Mix — are pane content, not dock chrome).**
- **PSC-3 (D-4 + D-4b + the census) — MAJOR — the canon retired this component and its state twice in writing; both still ship; the old gate counted files, not state.** VISUAL-CONSTITUTION.md:32 ("no global pane selector, left/right split state … survives") and :89 ("V retires the global Dock `PaneSegmentedControl` and left/right view state") — both line-exact at my read. Live: the SFC ships, Dock.vue:15/:198 renders it, `mobilePaneIndex` ships (useViewManager.ts:23/:60-70). D-4b confirmed: CARRY-LEDGER.md:22's `1→0` is a file count, satisfiable by inlining while the banned state survives. **Disposition: X-W5.c — and the D-4b under-specification is ALREADY CURED there**: gate C3 greps `useBreakpoint|isDesktop|isMobile|mobilePaneIndex` to zero at layout sites (RED today: 38 occurrences/13 files, measured in the spec), C4 tests file non-existence, and unit c replaces the schema fields themselves. The W47 gate's parenthetical dies with the ledger row; render-site census = 1 re-confirmed by this seat (Dock.vue:15/:198 the only code hits). **Binding note to the wave author (from the killed D-4-cure)**: the migration domain is the 14-member `ViewId` union — five admin views carry `(left, "palettes")` pairs OUTSIDE the 11-route inventory at VC:58, and `/easing` is IN the inventory with NO `VIEW_MAP` row (my grep → 0). A route split scoped to the inventory hits five undefined cases and one phantom.
- **PSC-4 (C-1, absorbing L-7's "the adapter is the entire component") — MAJOR — a pure passthrough wrapper at 1 of 3 producer call sites.** `variant="pill"` is the producer default (tabs.js, my grep) — a no-op prop; net contribution = the `0|1`↔`"0"|"1"` coercion, a dead `v-if`, and the `:deep()` padding delta. Both sibling consumers inline the producer (MixSourceSelector.vue:105, AdminNamesPanel.vue:14 — my reads). Owner-edict KISS/no-contrivance violation. **Disposition: X-W5.c (C4).**
- **PSC-5 (D-5(a)(c)(d) ≡ C-4 ≡ L-3, absorbing L-2) — MAJOR — a `:deep()` density forge on a producer-private class + three disagreeing responsive authorities.** The full published prop surface (d.ts) has no `size`/`density`/`compact` — L-3's framing governs; the demo forged the variant from `.segmented-tab` internals with raw lengths (`0.25rem 0.375rem`) in a width-only fork (`max-width: 639px`, PaneSegmentedControl.vue:46-51), the exact complement of the producer's own `min-width: 640px` rung (segmented.css, my read). Authorities: (1) the aspect-aware stamp App.vue:310-311 → shell.css:123; (2) the component's width-only media query; (3) Dock.vue:71's width-only `isDesktop`. At 1024×1366 portrait: stamp says mobile (control SHOWN), the media query doesn't fire (producer desktop rung renders), `isDesktop` says desktop — three answers, structurally certain from the three predicates, no probe needed. shell.css:98-110 forbids exactly this in prose. VC:33 (breakpoint pile) breached. **Disposition: X-W5.c — gate C7 already counts `PaneSegmentedControl:46` among the six width-based queries it retires (W5.md:274); the fork census dies at C3. The glass-ui `density` ask (L's cure d) is MOOT after deletion — neither surviving SegmentedTabs site compacts — so no relay is filed; D-5(b)'s `responsive` misread is killed in the register.** Cross-cite: the width-only-predicate family law is Dock-adj DELTA-5/G-L at X-W8; this row is its PaneSegmentedControl instance.
- **PSC-6 (D-7, RESCOPED to the normal register) — MAJOR — the focused state was never designed where users actually run.** Producer: `focus-visible` = 0 across tabs.js/segmented.css/drag.css (my counts). App: the only `:focus-visible` rule in the demo sits INSIDE `@media (forced-colors: active)` (foundation.css:678/:698-720), and focus-ring.css ships opt-in tokens with no selector (:33-36, consumers enumerated as "the gradient stop handles + remove chip"). So the normal light/dark register falls to the UA `outline: auto`, whose radius/colour/containment disagree with the capsule (geometry source-supported: `.segmented-tab{border-radius:0.3125rem}` inside a `--radius-pill` indicator). VC:84 breached in the normal register; the forced-colors arm IS designed and reaches this control (the kill). **Disposition: GLASS-BJ relay — a tokenised `:focus-visible` for `.segmented-tab` is producer-owned and survives at the three preserved P092 sites after this instance dies at X-W5.c. The rendered-ring appearance cells stay UNPROVEN.**
- **PSC-7 (D-8) — MAJOR — three of six state matrices are structurally blind to the component.** `forced-colors` / `keyboard-focus` / `reduced-motion` captured desktop-only (1440×900, states.mjs) while shell.css:123 hides the control on desktop — zero captures of the mobile-exclusive control in exactly the states most likely to break a glass indicator; PSC-6 is the defect that hole concealed. Evidence cell corrected (both readers + my read: the kb-focus `#/` focused string is `span[l component value]`). **Disposition: NO-WAVE-OWNER (audit instrument, not product) — the state matrices need a mobile arm; this row is a direct input to the visual-audit lane that consumes this file's UNPROVEN residue. Flagged for the formation boundary; no product wave can discharge it.**
- **PSC-8 (C-9) — MAJOR — the SFC's one real contribution is the one thing no gate observes.** walk.spec.ts:86-157 asserts only `aria-pressed`/visibility/count — zero geometry, padding, font-size or tap-floor assertions anywhere in e2e/ or test/ for this control (corpus mutation table; #3/#4 statically re-proven — #4 by the producer's `variant:{default:"pill"}`). Deleting `<style scoped>` keeps the suite green. **Disposition: discharged by X-W5.c's delete — the untestable CSS ceases to exist; the wave's walk.spec modify-carve must PRESERVE the content-landmark discipline (the "Detailed Guide" visibility proof — see superlatives) as the presence assertions it converts to. C-9's mutation results #1/#2/#5 stay UNPROVEN (no test run this seat).**
- **PSC-9 (reader-2 missed-M2, ratified) — MAJOR — Dock.vue's width-only `isDesktop` fans out to four dock behaviours; at portrait ≥1024 the sole mobile route to the second pane sits inside a dock that auto-collapses after 5s.** My reads: Dock.vue:71 `useMediaQuery("(min-width: 1024px)")`; :132 `<GlassDock :collapse-delay="5000" … :always-expanded="!isDesktop">` — at 1024×1366 (stamp: mobile; control: visible; both panes reachable ONLY through it) `isDesktop` is true ⇒ `always-expanded` false ⇒ the 5s collapse arms, making the pane switcher a two-step interaction on exactly the band shell.css:118-122 names as the rule's reason for existing. Also fans to `:is-desktop` on DockViewSelect (:172) and ActionBarToggle (:186) and the `mobileEditActive` predicate (:72). **Disposition: X-W5.c — gate C3's fork-kill covers Dock.vue's `isDesktop`; cross-cite Dock-adj DELTA-5/G-L (X-W8) for the family law. The "does the collapse visibly maroon" live cell stays UNPROVEN; the arming is source-certain.**
- **PSC-10 (D-3 ≡ C-7) — MAJOR — type jurisdiction violated on both axes: Fraunces (display family) at a rung BELOW `text-small`, on a control.** `class="font-display"` (:10) reaches the tabs through the producer's designed `font: inherit` hook; `font-size: var(--type-caption)` (:49) sits one rung below the mandated `text-small` (token arithmetic corpus-verified against glass scale.css). VC:75 (control/label row) + :78 (closed matrix, P019 sole exception) — line-exact at my read. Refinements carried: the FAMILY violation is unconditional, the RUNG violation sub-640 only; the sibling inconsistency (AdminNamesPanel:17 yes / MixSourceSelector:105 no — my reads; AdminNamesPanel also puts `font-display` on two Buttons :37/:87, seat observation) makes it a register-family question, not a two-line patch. **Disposition: this instance dies at X-W5.c; the family row (the `font-display`-on-controls voice) → X-W10 (CC-098 closed type matrix — the DVS-7 precedent already carries a 17-site census of this voice).**

### MINOR

- **PSC-11 (C-2 ≡ D-6-name ≡ L-5) — MINOR (ruled) — the producer's `ariaLabel` exists and no consumer passes it; the strip ships as an unnamed `role="group"`.** d.ts:50 + tabs.js `"aria-label": p.ariaLabel` (undefined ⇒ omitted) — my greps; all three call sites omit it (my reads of :6-12, MixSourceSelector:105-110, AdminNamesPanel:14-21). L-5's precision governs (`controls` correctly omitted under `toggle` — documented as ignored). VC:83's explicit-name clause breached at the container. **Disposition: this instance dies at X-W5.c. Sibling arms: MixSourceSelector.vue is IN X-W5's file table (modify-carve) — rider there to pass `aria-label` when touched; AdminNamesPanel.vue is in no named wave — NO-WAVE-OWNER, one line, flagged for the formation boundary.**
- **PSC-12 (L-4 ≡ C-5 ≡ D-9) — MINOR (ruled) — four stacked guards defending an invariant the schema already enforces; the `?? ''` + falsy-`''` pair is a masking path.** All four sites re-verified (Dock.vue:197/:200-201, PaneSegmentedControl.vue:3/:31-32); the type widening (`string | null` at :22 vs `viewSchema.ts:73` `string`) confirmed; the 14-row enumeration is THIS SEAT'S OWN (both D counts are killed in the register). **Disposition: X-W5.c — binding accretion: the `regions[]` type must make pane-without-label unrepresentable (a `{ pane, label }` pair or equivalent), else the masking seam re-forms.**
- **PSC-13 (C-6 ≡ L-7 ≡ D-12) — MINOR — `Number(v) as 0 | 1` launders the producer's `string` emit (d.ts:103) through an unchecked assertion into route-derived state; the deeper fault is a POSITION model, not a pane identity.** Failure chain source-verified in the corpus (NaN ⇒ `"NaN"` model ⇒ indicator opacity-0 branch ⇒ usePaneRouter.ts:180 silently resolves left — anchor re-confirmed mine). Latent only because `tabOptions` hardcodes `"0"`/`"1"`. **Disposition: X-W5.c — the string↔number seam and the positional model both die with `regions[]`.**
- **PSC-14 (L-1, severity ruled) — MINOR — homed one directory above its only consumer; Dock.vue:15 is the unique one-level upward SFC edge out of `dock/`; origin one R100 rename (`6dc12aad`, routed by lexical name).** My one-level census: 6 rows, :15 the only SFC. Both supporting generalizations stay killed. **Disposition: X-W5.c (C4) — discharged by deletion.**
- **PSC-15 (L-6 + reader-2 missed-M6) — MINOR — the supplying module's boundary is dirty on two arms.** (a) The dead back-compat re-export (useViewManager.ts:14-18, naming the killed `@composables` alias — vite.config.ts's W43 note): `LeftPane`/`RightPane` have ZERO external importers and `ViewId` ×2 + `PaneConfig` ×1 route through the shim (my census — exactly 3 rows TOTAL; reader-1's "three each" kill confirmed). Partly pre-booked as CARRY-LEDGER:22's D53.iv rider (`VIEW_MANAGER_KEY ×2 · ViewId type` — verbatim at my read). (b) `useViewManager.ts:91` `currentView: currentView as unknown as Ref<ViewId>` advertises a read-only computed as writable — any of its consumers can type-check a write Vue will drop (dev-warn only). **Disposition: X-W5.c rider — useViewManager.ts is in the wave's file table (`modify`); the re-typing against `regions[]` deletes the shim, repoints the 3 importers at viewSchema, and types `currentView` as `ComputedRef`/`Readonly` honestly.**
- **PSC-16 (D-10) — MINOR — tap target clears WCAG 2.2 AA 24px by ~2.25px, fails AAA/HIG 44px, and the demo's own override contributes the trim** (producer sub-640 inline padding 10px vs the override's 6px — 8px/tab, my read of both stylesheets; the harness threshold `< 24` explains its absence from all 60 REPORT.json rows). The two axes' differing tab widths (49.09 vs 57.51) are label-set-dependent (`grid-auto-columns: 1fr` — my read) and neither is load-bearing. **Disposition: X-W5.c — dies with the control; the replacement (no pane pill at all) moots the floor.**
- **PSC-17 (D-11 + reader-1 M7 ≡ reader-2 missed-M7) — MINOR — the mobile spec's contract prose is stale twice over**: `BouncyTabs` (walk.spec.ts:16, :31 — does not exist in glass 7.0.0) AND `lg:flex` as the hiding mechanism (:19-20 — the exact width-only mechanism shell.css:98-110 records as deliberately retired; the spec documents the abolished law in the header of the only spec exercising the control). **Disposition: X-W5.c — walk.spec.ts is modify-carve'd there; the re-point must correct BOTH lines, not just the rename.**
- **PSC-18 (C-8 + reader-1 M3) — MINOR (elevated from C-8's INFO by the preference-conditional arm) — the drag-morph indicator is armed by omission, and an OS accessibility preference silently selects between two interaction models.** `motion: {}` (no default, my grep) ⇒ `useMotionAxis` resolves `full` ⇒ armed ⇒ drag.css makes the ACTIVE half of the ~121px control pointer-dead with `touch-action: none`; under `prefers-reduced-motion: reduce` the same half becomes an ordinary button (`i()` forces `reduced`, `armed` false — my whole-file read). The consumer never chose. Nothing is lost either way (the active tab is already selected). C-8(b)'s `data-motion` sub-claim stays killed. **Disposition: X-W5.c — dies with the control. No producer relay: the tiers are documented; the omission was the consumer's.**
- **PSC-19 (reader-2 missed-M3 — the real defect in D-1's ashes) — MINOR — GLASS-OWNED: `useSelectionIndicator` has no invalidation path for a relayout that repositions the buttons without resizing the container.** Re-measure triggers = model/options/vertical watchers + a container `ResizeObserver` (my read); a runtime `dir` flip reorders the grid at constant box ⇒ none fire ⇒ the stale translate persists — which exactly explains the harness artifact (states.mjs applies `dir=rtl` POST-load) and D-1's own self-heal-on-click. Unreachable in product (`index.html:11` `dir="ltr"`, no runtime switcher — grep 0). **Disposition: GLASS-BJ relay with the CORRECT ask — observe resolved `direction`/`[dir]` (or the buttons) as an invalidation source. The relay MUST NOT carry D-1's text (its root cause, proof and cure are all killed — a false bug report). Pre-relay obligation: the natively-RTL-first-paint probe (UNPROVEN #2) decides whether ANY real RTL defect exists.**
- **PSC-20 (reader-1 M4) — MINOR — label-namespace collision: on six of fourteen views the DockViewSelect and the pane pill render the SAME word simultaneously with two meanings (route vs pane), inside one aperture, unmitigated for AT because the group is unnamed (PSC-11).** walk.spec.ts:34-38 documents the ambiguity as a selector hazard in its own prose (my read) — a harness that must invent landmark scoping to disambiguate is documentary evidence the label design is ambiguous to users. **Disposition: X-W5.c — dissolves with the pill (the view-select becomes the sole label surface).**
- **PSC-21 (reader-2 missed-M4) — MINOR — the CI canary two stylesheets cite as the guarantor of this component's visibility rules does not exist.** shell.css MOB-1 (:106-107) and foundation.css:29 both name `scripts/ci/css-emission-probe.mjs`; live: `scripts/ci/` holds only `verify-packed-surface.mjs`, repo-wide find → nothing, no workflow step, no package script (my checks). The sole mechanism hiding the control on desktop (shell.css:123) is documented as gated and is not. **Disposition: X-W5.c rider — both stylesheets are in the wave's file table (modify-carve); the comments must be corrected to the truth (or the probe restored) in the same edit that touches the rules.**
- **PSC-22 (reader-2 missed-M5) — MINOR — `PaneSlot` names two unrelated concepts in one directory** (the SFC, one consumer App.vue:177; `export interface PaneSlot` usePaneRouter.ts:61 — my census), which is what corrupted the L axis's consumer census (killed L-1-evidence). **Disposition: X-W5.c rider — both files are in the wave's table (`modify`); rename the interface (e.g. `PaneSlotResolution`) when the re-type lands.**

### INFO

- **PSC-23 (census, all three axes + both readers) — INFO — the retirement gate is LIVE, not vacuous: render-site count is exactly 1** (Dock.vue:15 import + :198 render; remaining hits prose). Re-run by this seat. Recorded so X-W5's C4 executes against a known-true baseline.
- **PSC-24 (reader-2 missed-M8 + the comment mislabels) — INFO — 18 of the file's 52 lines are a changelog for absent code guarding four lines of CSS**, whose "AT THE ROOT (never per-instance)" claim launders the per-instance override (D-5(a), confirmed). The `:45` Ad-18 marker is X-W10's tracked marker (W10.md:117/:354): **discharged by X-W5's deletion, recorded as the survivor's terminal event, spending the W10 row** — the cross-wave mechanics are already written; nothing to add.

## Killed-claims register — refutations re-derived, none inherited

1. **D-1 (the RTL BLOCKER) — KILLED, all four limbs, both readers concurring and this seat re-proving.** (i) The proof measured the wrong file: `grep tabs.js` → 0, but the engine lives in `useTabRovingFocus-YnWh-Ytr.js`, which contains `rtl` ×2 + `direction` ×1 and the belt-and-braces arm (`getComputedStyle(u).direction` + `closest("[dir]")`) — my counts and my verbatim extraction. "RTL-unaware by construction" is measurably false. (ii) The root cause does not exist: the indicator positions from measured `getBoundingClientRect()` deltas; `tabWidth|contentBoxLeft` grep → 0 — there is no index arithmetic to indict. (iii) The cure is a no-op: it prescribes exactly what the producer already does; filing it would send glass-ui a false bug report. (iv) Severity unreachable: `index.html:11` hardcodes `dir="ltr"` with the U-F58 scope comment; the only RTL producers are the harnesses flipping `dir` POST-load (states.mjs:50; a11y-web-modality.spec.ts) — the mechanism that DOES explain the observation is the invalidation gap, booked at PSC-19 at MINOR. The observation itself (the shot) is real; the pixel geometry stays UNPROVEN. **This kill is the corpus's cautionary exhibit: a measured symptom, a fabricated mechanism, a no-op cure, and a BLOCKER severity — every layer above the pixels wrong.**
2. **D-5(b) ("re-implements the producer's `responsive` facility") — KILLED.** `responsive` is the strip→`<Select>` collapse breakpoint, default OFF (`default: !1` — corpus; d.ts wording re-read by this seat). Enabling it below 640px would replace the pill with a dropdown at the only viewport where the pill is load-bearing. The demo overrides the producer's existing sub-640 rung; nothing is re-implemented. D-5's other three limbs stand (PSC-5).
3. **D-2-cure ("clamp `--muted-foreground` toward the foreground floor, never past it") — KILLED.** My whole-file read: the base rule ALREADY IS the clamp's fixed point (`--muted-foreground: var(--foreground)`), and it carries zero state information. The cure converges on the defect's other face. The relay ask must move the state channel, not adjust the clamp (PSC-2).
4. **D-2-scope ("WebKit-specific inversion") — KILLED as scoped; RESCOPED into PSC-2.** The failure is universal (base rule); the inversion is the `contrast-color()` specialization. The corrected form is worse than the original claim, not narrower.
5. **C-8(b) (`data-motion: null` while armed = "producer inconsistency worth a relay") — KILLED.** `dataMotion: computed(() => o.value === "full" ? void 0 : o.value)` — my read of the shipped chunk. `full` ⇒ armed AND no attribute, by design. No relay line is owed.
6. **L-1's BLOCKER severity + "every other `../` edge … never a component" — KILLED (severity ruled MINOR above).** The one-level claim is true (my 6-row census) but the rhetorical generalization fails: dock SFCs import components from outside `dock/` routinely at two levels (`../../ui/...` — reader-1's rows, consistent with L's own grep tail). A 52-line file's directory, cured by deletion, is not a blocker.
7. **L-1-evidence ("PaneSlot.vue … multiple consumers") — KILLED.** The SFC has exactly ONE consumer (App.vue:177 — my census); the other `PaneSlot` is an unrelated interface (usePaneRouter.ts:61). The stated criterion would condemn a file L certifies clean — the collision itself is PSC-22.
8. **D-9-enumeration ("all nine `VIEW_MAP` entries") + D-4's "15 entries" — KILLED, both counts.** The table has FOURTEEN rows (my census: 9 unquoted + 5 quoted admin at :188-224); D-9 dropped a third of the domain, D-4 overcounted by one, in the same document. The conclusion survives on THIS SEAT'S enumeration; C (14) and L (14) counted correctly and govern.
9. **D-4-cure step 1 as written — KILLED.** Five admin `(left, "palettes")` pairs have no destination in the 11-route inventory it says to match, and `/easing` is in the inventory with no `VIEW_MAP` row (my grep → 0) — the two authorities disagree in both directions. Cured in the booking: PSC-3's binding note scopes X-W5.c's migration to the 14-member union.
10. **D-7-forcedcolors ("cannot be asserted … across forced colors") — KILLED.** foundation.css:678 + :698-720: a real `outline: 2px solid Highlight` on a roster including `button` and `[tabindex]:not([tabindex="-1"])`; `.segmented-tab` is a native button. The undesigned register is the NORMAL one — PSC-6 is stated that way.
11. **L-6-counts ("three live consumers each") — KILLED.** Three importer rows TOTAL: `ViewId` ×2, `PaneConfig` ×1 (my grep, all three rows printed). The finding survives at the corrected multiplier (PSC-15).
12. **Citation-drift register (reader-2's, spot-checked and carried).** Stale-by-offset anchors across all three axes (viewSchema/VIEW_MAP ranges, d.ts prop-surface range, AdminNamesPanel:16→17, MixSourceSelector:103→104, VC:38→:33, VC:82→:83). Every spot-check this seat ran landed at reader-2's corrected line. Not defect-killing; recorded because a wave will navigate by these. Corollary carried from the exemplar: re-resolve all anchors at execution HEAD.

## Superlatives — provenance preserved

1. **`demo/shell/useViewManager.ts:51-70` — the `paneOverride` design** (both readers, ratified by my whole-file read): the mobile pane modeled as a VIEW-TAGGED override behind a `WritableComputedRef` falling back to the schema default — "a toggle on one view cannot leak into another" true BY CONSTRUCTION across every navigation mechanism, with the comment naming the three mechanisms it retired. The same codebase demonstrating the right and the wrong way (PSC-12's guard stack) to encode a fact, thirty lines apart. X-W5.c deletes it with the state it manages; the DESIGN PATTERN (tagged override + schema fallback) is worth carrying into the `regions[]` successor and is named here so the deletion does not silently discard the lesson.
2. **`demo/shell/viewSchema.ts` — a genuinely clean data module** (reader-2, ratified): `Record<ViewId, PaneConfig>` over a closed 14-member union makes the table total by construction; the header's purity contract ("No reactivity. No vue-router.") is kept; it records the 4-copy enumeration it was extracted to kill.
3. **`demo/styles/shell.css:98-110` (MOB-1)** (both readers): names the retired mechanism, the exact pathology, the new single truth, and forbids the regression in advance — the prose two independent axes proved live defects FROM (PSC-5, PSC-9). Its one blemish is its dead canary citation (PSC-21), which is why comments citing gates must be gated themselves.
4. **`demo/color-picker/index.html:1-11` — scope honesty that changes an audit's outcome** (reader-1): the `dir` seam shipped, the not-done half named, the residual booked — the paragraph that let D-1's reachability be bounded to zero without a browser.
5. **`e2e/smoke/mobile/walk.spec.ts:39-42 + :105-126` — the content-landmark proof discipline** (reader-1, ratified): the toggle proven by a landmark unique to the toggled-in pane, both directions plus return — the exact shape X-W5.c's presence-assertion re-point must preserve (PSC-8's binding note).
6. **`challenge-C-implementation.md:400-435` — the evidence discipline that made C the highest-survival axis** (both readers): eleven checked-and-clean negative proofs each carrying its clearing artifact, and self-graded hypothesis labels (C-3's journey, C-6) that all held. Contrast: D's two BLOCKERs both rested on unstated inference and both died. The fleet lesson stands re-confirmed at this seat: **severity tracks evidence discipline, not axis assignment.**
7. **`node_modules/@mkbabb/glass-ui/dist/useTabRovingFocus-YnWh-Ytr.js` — the engine the design axis certified "RTL-unaware" makes two independently correct direction decisions** (reader-1, ratified by my own extraction): the belt-and-braces `[dir]`+computed-style arm, and a measured-rect indicator that is direction-correct for free. Its one honest gap is PSC-19's relay.

## UNPROVEN-NEEDS-LIVE residue — the visual-audit inputs

No browser tooling at this seat; each row is a bounded probe, most decidable in one capture. These feed the mobile-arm captures PSC-7 demands.

1. **The decisive RTL probe**: a NATIVELY-RTL first paint (dir set before mount, not states.mjs:50's post-load flip) — PSC-19's mechanism predicts the indicator lands correctly, at which point NO real RTL defect exists and the BJ relay is a robustness note only. Run BEFORE relaying anything (both readers concur; this seat makes it a relay precondition).
2. D-1's raw RTL geometry (indicator 134.94/49.36 vs pressed tab 184.00/49.09 at 390×844) — plausible via the invalidation-gap mechanism, never re-measured; unreachable from product.
3. D-2's resolved colours (`rgb(28,25,23)`/`rgb(0,0,0)` light; dark pair) and the 1.20/1.24:1 ratios; **plus the Chromium `contrast-color()` support cell** that decides whether the e2e engine sees the zero-delta or the inverted form (either way PSC-2 stands — the split is which face the gate exercises).
4. The 639/641px discontinuity table and L-2's 1024×1366 probe block — direction and token arithmetic derived and confirmed by this seat (producer ≥640 rung = 5px 12px / 14px at 16px root); rendered boxes unmeasured. Treat as confirmed-by-derivation.
5. C-3's end-to-end keyboard journey (arrow → commit → rAF remount through KeepAlive+Transition) — every link source-verified; the journey never executed. C correctly self-labelled it.
6. D-7's rendered UA ring (colour/radius/containment) — the ABSENCE of any authored ring in the normal register is statically proven (the load-bearing half); the paint is not.
7. C-9's mutation results #1/#2/#5 (marked GREEN in the corpus; no test run at any seat in this chain).
8. Whether PSC-9's 5s auto-collapse visibly maroons at 1024×1366 (arming source-certain; the felt cost live-only), and whether the dock's `fit-content` row visibly jolts on `/#/atmosphere` (the sole `right: null` view unmounting `.dock-mobile-panes`) — the one residue row no axis raised, carried from reader-1.
9. PRM transition measurements (the `reducedMotion: "reduce"` cells) — structural side fully verified (useMotionAxis + drag.css); the rendered transitions not re-run. Motion-quarantine: zero rows for this component; no M-xx constraint.

## Closing verdict

**DEFECTIVE — DELETE, DON'T REPAIR.** No blocker survives adjudication; both axis BLOCKERs were evidence failures, not defects. 10 MAJOR · 12 MINOR · 2 INFO adjudicated: 17 rows discharge at **X-W5** (unit c owns the file's `delete`, the state's dissolution into `regions[]`, the fork-kill gates C3/C4/C7, and the spec re-point — with this record's binding accretions: the `semantics="tabs"` interim patch REFUSED, the 14-member migration domain, the unrepresentable pane-label pair, the canary-comment correction, the shim+cast+`PaneSlot`-rename riders, the content-landmark preservation); 3 ride the **glass-ui BJ relay** (the dock ink-collapse with the move-the-channel ask, the `.segmented-tab` normal-register focus ring, the indicator invalidation gap with its relay-precondition probe — and NOT D-1's text); 1 goes to **X-W10** (the `font-display` register family; the Ad-18 marker mechanics already written at W10.md:117); 2 carry **NO-WAVE-OWNER** (the state-matrix mobile arm — an instrument, and AdminNamesPanel's one-line `aria-label`). These dispositions are ADJUDICATED, not VERIFIED; X-W5's gates stamp them when it runs. Original challenge records and both reader returns remain immutable beside this file per E-1/E-3.
