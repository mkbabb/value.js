claude-fable-5

# kf-AnimationVisualizer — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/components/playback/AnimationVisualizer.vue` (256 L) — reader-1 ("design-and-usage": 27 confirmation cells · 5 kills · 11 misses · 9 superlatives) and reader-2 ("library-and-code": 36 confirmation cells · 8 kills · 7 misses · 8 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/AnimationVisualizer/challenge-{D-design,L-library,C-consumption}.md`, 297/361/389 L, all opus-5[1m]).
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption. The head-on contradictions (D-7 thumb, D-6 causal story, the drag-seam-cleanup consequence) were decided by fresh extraction, not by vote. **Zero browser tooling** (law); all rulings static + installed-dist reads; the irreducibly visual residue rides to SS-13. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only)**: full reads of AnimationVisualizer.vue (whole, 256 — every cited anchor checked: `:25` sole handler, `:45` deep import, `:79` wire, `:136` estimator, `:183` boundary target, `:200/:108` grabOffset algebra, `:210-212` unconditional down-press applyProgress, `:230-235` gate-before-button, `:250` guard vs `:237-239` comment, `:255` dispose), PlaybackRibbon.vue (whole, 244 — `:167-179` pointerType cure, `:208-216` "ONE motion-color identity" prose, `:230-243` scoped slider vars, `:72-80` mount), useDragCapture.ts (whole — **no re-entrancy guard at `:52`**, element-targeted `useEventListener` from inside the DOM handler, `stops` array replaced, release-once at `:41-49`), gestureSelectSuppression.ts (whole — **ref-counted, no timeout, no visibility escape**), useDemoTicker.ts (whole — per-subscriber `enabled()` gate `:20`), useRafLoop.ts (whole), useAnimationSync.ts:70-95 (`wake` returns the pass-through `isPlaying`), useAnimationGroupPlayback.ts:110-150 (plain-assignment latch; `sliderUpdate` → `setChildTime().render()` + `machine.dispatch({type:"SCRUB"})`), ChannelOptions.vue:370-400 (`<Teleport v-if="active">`; wake routed UP only), SpringScene.vue:95-125 (raw-scene latch), sceneMachine.ts:160-250 (`scrub()` **always allocates a fresh context** — the echo-guard cannot fire), useSceneMachine.ts:70-186 (`useStorage(SCENE_MACHINE_PERSIST_KEY, {…})` with **no options** → deep + synchronous serialize per dispatch; `coerceColdBootPaused`), SequenceScrubber.vue:18-40 (`role="slider"` + full a11y + rail-wide `@pointerdown`; ball transform `translateX(calc(p*100cqw))` — **full-width**, the K-8 decider), useDragScrub.ts:1-25, SequenceTarget.vue:80/:161 (`ROW_TONES` per-row `--ball-tone`) + SpringTarget.vue:113 (`lane.tone`), design-idioms.css:161-195 (the promoted rail/ball pair) + :289-300 (`user-select:none !important` under `body.is-dragging`), style.css (z-contract :25-40; `color-scheme` :91 + `.dark` :180-186; `--accent-kf` :130; **`--color-progress: var(--accent-kf)` beside `--color-slider-track: color-mix(in srgb, var(--foreground) 35%, var(--border))` with the "rails are substrate, not signal" prose**; `.container-inline-size`/`.is-disabled` :236-252), layout.css:1-30 (`--visualizer-track-gutter: 3rem` + "1.5rem each side"), kf-engine.ts:1-26 (the ED-3 law verbatim), smooth.ts:30-60/:120-150 (**snap unconditional, before the clamp block**; `snapThreshold: 0.001` default; `readonly _playback = new RAFPlayback()`), spring/progress.ts:131/:220-243/:348-368 + spring/types.ts (both settle thresholds 1e-3; `checkSettled` measures distance **to `scaledTarget` only**), playback.ts:62-78 (arrow class-fields) /:200-250 (`drive` idempotent, `dt = now − _lastFrameT`, returns `!settled`; `stop()` = `_gen++` + cancel + `_cleanup()`, **no terminal step**), decay.ts (whole head — **pure sampler `(t)=>DecaySample`, zero `Tickable`/`tickDt` occurrences**), reseat.ts:38-62 (`probeVelocity` units/second; `reseatToSpring` targets a **supplied** `newTarget`), browser.ts (whole head — total `new WeakMap()` flush; module-eval `window.resize` listener; two value.js imports) + :210-232 (the forced-style-recalc probe path), interp-slot.ts:276-288 (the sole epoch consumer), resize-tracks.test.ts:1-16/:125-141 (the `fs.readFileSync` + two-regex pin; the "value.js's `bumpLayoutEpoch`" prose beside its own `../../../src/animation/resolve/browser` import), installed glass-ui 7.0.0: **python re-extraction of every `.slider-thumb` rule setting width/opacity in dist/glass-ui.css** (base = `width:0; opacity:0`; the ONLY un-hiding selectors are `[data-variant=spectrum]`-scoped, incl. the vertical arm), `slider-DDia69Fy.js` `variant: { default: "standard" }`, `dist/components/slider/types.d.ts:4` **`SliderVariant = "standard" | "spectrum"`** (no "timeline"), useTouchGate dist chunk (whole head — module-level `Set` + one shared document `touchstart` handler that `deactivate()`s every other active instance; `l = "ontouchstart" in window` captured once; the 150 ms arm + 3 s inactivity timers), @vueuse/core 14 `useEventListener` (bare `watchImmediate`, **no `tryOnScopeDispose` call**) + @vueuse/shared `tryOnScopeDispose` (scope-gated no-op), timeline dist listing (ScrubberTimeline ships; demo imports **0**); repo censuses re-run: `touch-gate` = 4 demo/src hits + 2 glass-ui prose hits · `bumpLayoutEpoch` in demo = 2 (both this file) · `getLayoutEpoch` in demo = 0 · `bumpLayoutEpoch` in dist/keyframes.js + dist/engine/index.js = 0/0 · exports map = exactly 2 entries, `sideEffects: false` bare · `@src/` in demo = 11 · `export.*clamp` in src/animation/index.ts = 0, `decay` exported at :136 · forced-colors/prefers-contrast in demo+src = 0 · dir/rtl = 0 · glass-ui in package.json/package-lock.json = 0/0 (F-1) · `useRafLoop` files = 4, **consumers = 2** (useSquareDemo's hit is a comment — instrument caution, reader-1's cell stands) · progress-rail/ball consumers = 7 component files + the sheet + DESIGN.md · DESIGN.md §5/§6/§7 clauses verbatim at :119/:140/:149-156.
**Substrate disclosure (net-new, this seat's own)**: keyframes.js `master` HEAD `8281638c` with a **dirty working tree**, and the audited bytes are the WORKTREE. `git diff HEAD` on the target: **1 line** — HEAD reads `import { bumpLayoutEpoch } from "@mkbabb/value.js"`, a named import of a symbol value.js has **never** exported (my grep: zero `layoutEpoch` anywhere in the installed value.js), i.e. HEAD is born-broken on this file; the uncommitted Glass-7 adoption diff repoints it to `@src/animation/resolve/browser` and left the four-fold "value.js" comment as a **fossil of the broken import** — the comment and the import were born consistent and wrong together. PlaybackRibbon's diff (53 L) is the same adoption pass: `IconTooltip`→`Tooltip`, `variant="outline"`→`emphasis="secondary"`, and **`variant="timeline"` deleted from the Slider** — "timeline" is not in 7.0.0's variant union, so at HEAD both the variant AND the `:deep([data-variant=timeline])` track-height rule were dead; the worktree re-scopes to `"standard"`. Every challenge anchor matches the worktree bytes exactly, so both readers and both challenges audited this same dirty tree. All line anchors below are worktree-at-`8281638c`; re-resolve before any cure (D-19).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**42 adjudicated roster rows — 3 BLOCKER · 12 MAJOR · 16 MINOR · 11 INFO** (id KF-AV-31 is reserved-folded into KF-AV-12, anti-rename). **12 killed-claims register entries, all SUSTAINED** — including one that overturns a reader's confirmation cell at the bytes (D-7). **18 missed rows ADMITTED** (11 from reader-1, 7 from reader-2; 10 become roster rows, 8 fold as facets into existing identities). **11 superlatives sustained.** **6 adjudicator corrections against the readers.** **8 UNPROVEN-NEEDS-LIVE rows to SS-13.** Identity guards held: the `@src` deep-import class folds to **KF-CE-12** (kf-CSSCodeEditor), the demo-wide forced-colors gap folds to **KF-CE-13**, the facility-SCRUB branch cross-refs **kf-AnimationControlsGroup L-1**, F-1/S-4/R1 fold by reference — zero re-bookings.

Blocker-weight survivors: **KF-AV-1** (the twin freezes under the primary control, is never seated at mount — and, the amplification no axis saw, **silently overwrites the playhead it exists to mirror on first touch**), **KF-AV-2** (the non-reentrant emit contract strands playback paused), **KF-AV-3** (mouse/pen routed through the touch-only gate; the sibling 100 lines away carries the recorded measurement and the three-word cure).

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-7 (thumb proportion row)** | KILLED — standard-variant thumb not rendered | CONFIRMED MINOR — quotes `--slider-thumb-size: 1rem` as the operative 16px thumb | **KILLED — reader-1 wins; reader-2's cell CORRECTED** | My python extraction of every `.slider-thumb` rule in installed dist/glass-ui.css that sets width/opacity: the base rule is `width:0; height:var(--slider-track-height); opacity:0`, and the **only** un-hiding selectors are `.glass-slider[data-variant=spectrum] …` (horizontal + vertical arms). The chunk declares `variant: { default: "standard" }`; the ribbon passes no variant (worktree) and 7.0.0's union is `"standard" | "spectrum"` — no state renders a thumb. `--slider-thumb-size` under `[data-size=md]` is a dead var for this slider. Reader-2 re-inherited D's table without checking variant scoping — a citation-inheritance-shaped event (corrections register #1). D-7 is **RESCOPED**: the thumb row is fabricated; the track-ratio half (24px lifted track vs 4px rail) and the proportion-inversion macro survive inside KF-AV-14/15's identities. |
| **D-6 causal story** | KILLED the "they agree today" reasoning; raised M-gamma (the split is live) | CONFIRMED D-6 incl. "the two paths agree only by the current alias" | **Reader-1 wins; reader-2's cell CORRECTED** | style.css defines `--color-slider-track` as a **neutral** `color-mix(in srgb, var(--foreground) 35%, var(--border))` with the authored rationale "rails are substrate, not signal"; PlaybackRibbon :231-232 derives both visible slider surfaces from it; the only `--color-progress` consumers (`--slider-thumb-bg` :233, hover twin :242) land on the `width:0; opacity:0` thumb. The slider is neutral grey and the twin is violet **today, at every token value** — not a latent repoint hazard. D-6's defect (the ball bypasses the authority) is confirmed; its mechanism is inverted; the ribbon prose at :208-216 asserting "ONE motion-color identity" is false in the shipped tree. Booked as KF-AV-11. |
| **Drag-seam unmount consequence** | M-delta MAJOR: suppression counter stuck → document-wide `user-select:none` forever | missed-M4 MINOR: "listeners element-scoped … the practical consequence is benign" | **Reader-1 wins; reader-2's "benign" clause CORRECTED** | gestureSelectSuppression.ts read whole: a module `let activeGestureCount` with **no timeout, no visibility reset, no other decrementer** than `releaseSelectSuppression()`, reachable only from `onPointerUp` (:47). Mid-drag unmount: the three `useEventListener`s were registered from inside a DOM event task (no active scope — @vueuse 14's `useEventListener` is a bare `watchImmediate`, and `tryOnScopeDispose` is scope-gated), the listeners ride the **detached ball**, pointer capture dies with the node, `onPointerUp` never fires, the counter stays ≥1, and design-idioms.css :292-297 applies `user-select:none !important` to `html`, `body`, `body *` for the session. Second-pointer trigger equally real: `onPointerDown` (:52) never tests `isDragging` → acquire ×2, `stops` replaced, release ×1. Reader-2's clause priced only the listener memory; the counter is the damage. Booked MAJOR as KF-AV-15. |
| **D-1 severity** | BLOCKER ("the component's sole purpose") | MAJOR (axes L/C also MAJOR) | **MAJOR — dissent recorded** | The fling-to-boundary mechanism is proven (`:183`; `checkSettled` can latch only at `scaledTarget`; both thresholds 1e-3; `dampingFraction: 1`). But precision placement survives at the instrument level (the reka Slider is the operative control and is correctly synced), and a slow release **with a recent slow sample** does position (`:174-176`). What makes the gesture unreliable is the composition with KF-AV-5/6 — booked there. The intent half ("flick to end" as affordance) is an owner ruling (residue #5); the prose claims physics either way and must change. Reader-1's and axis-D's BLOCKER grade preserved as dissent. |
| **D-14 severity** (value.js misattribution) | MAJOR | MINOR | **MINOR** | Prose-only, but pinned into the regression net (resize-tracks.test.ts repeats it while importing from keyframes' own tree). The substrate discovery reframes it: the comment is the fossil of HEAD's broken `@mkbabb/value.js` import — the cure is one sweep with the import decision (KF-AV-8), not an independent major. Reader-1's MAJOR recorded as dissent. |
| **D-10 severity** (zero-duration dead ball) | MINOR | INFO (reachability unproven) | **INFO** | Both guards are real (`:114` early return; `:243` re-check) and the affordance mismatch is real, but no route delivers `duration === 0` to a mounted instance (PlaybackRibbon :119-121 defaults, `:73` gates). A posture inconsistency, not a demonstrated failure. |
| **C-8/L·m-3 grade** (unmount swallows `dragEnd`) | MAJOR | MAJOR (contra axis-L's MINOR) | **MAJOR** | The unmount is routine (`<Teleport v-if="active">` channel switch; `v-if="animation"`), the latch has no other clearer, and PlaybackRibbon (read whole) has no dispose that could re-emit. |
| **The decay-cure framing** (D-1/M-3/C-6 prescribe re-seating onto `decay()`) | Confirmed cell cites "the correct primitive… the sibling already migrated" | K-1 kills the one-line-swap framing | **Reader-2's kill SUSTAINED; reader-1's cure cell corrected** | decay.ts read whole: `decay()` returns a **pure sampler** `(t) => DecaySample` — zero `Tickable`/`tickDt` occurrences — so it cannot be handed to `RAFPlayback.drive` (requires `tickDt` + `settled`); adopting it re-derives the hand-rolled loop the file retired. And useOrbitalInertia.ts:60-80 uses `decay({velocity: 1, friction})` only as a per-frame **velocity-factor source**, keeping its own integration — the precedent establishes "closed form for the factor", not "decay owns the endpoint". The defect stands whole; the cure needs W-side design, not a swap. (Bonus, adopted: useOrbitalInertia's own comment names the ED-3 published-barrel law the deep import breaches — the sibling obeys it.) |
| **will-change / C-11 / C-15 grade splits** | INFO / MINOR / INFO | MINOR / INFO / INFO | **INFO all three** | will-change: the ball transforms every frame of playback and is one 48px layer (L's merits argument; measured half to SS-13). C-11: four statements (`:47-50`), no lint requires the split. C-15: containment proven — `container-inline-size` ⇒ `contain: layout style inline-size` ⇒ stacking context + abspos containing block, so the rung is unobservable outside the visualizer (reader-1's M-lambda and reader-2's K-7 reached this independently — convergent, sustained). |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (CENSUS-2026-08-03 §: W4 K-Quartet Hygiene/gates · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**. A standing sequencing rider on every NO-WAVE-OWNER row below: **KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure here** — if the evaluate verdict is "swap", the bespoke rows discharge with the component. Routings are bookings, not acts.

### BLOCKER

- **KF-AV-1 · D-2 = L·B-2 = C-7, amplified by reader-2 miss #3 — the sync guard excludes the paused-slider scrub and there is no initial paint; the stale twin then SILENTLY OVERWRITES the playhead on first touch.** Proof: `:250` `guard: computed(() => props.isPlaying || isDragging.value)` against its own `:237-239` "Always poll" comment; useDemoTicker `:20` gates per subscriber, so the shared loop's liveness is no rescue; `setBallProgress` has exactly two callers (`:116`, `:248`), no onMounted/watch/defineExpose; useAnimationSync names the exact hazard and cures it for the parent only (`wake`, routed UP at ChannelOptions :385-396, never DOWN); the scene latch pauses on scrubStart so `props.isPlaying` is false for the whole gesture; `coerceColdBootPaused` makes the un-painted mount the DEFAULT cold-boot state. The amplification (no axis saw it; reader-2 found it): `onStart` (:210-212) calls `applyProgress(progressFromPointerX(e.clientX))` **unconditionally on pointer-down**, and the grabOffset algebra (SUP-8) makes that expression the ball's own **rendered** progress — so under staleness, merely touching the ball emits `scrub` at the wrong position and `setChildTime().render()` seeks the animation there; at cold boot (transform unset, restored `t` non-zero) the first touch **destroys the restored playhead by seeking to 0**. Data-destructive, not cosmetic. → **NO-WAVE-OWNER** (SS-1/SS-2). Spec obligations: (i) the guard admits scrub (wake routed down, or the guard drops), (ii) an initial seat at mount, (iii) the down-press seek stays correct-by-algebra once staleness is cured (no change to the grab math — SUP-8).
- **KF-AV-2 · L·B-1 — the emit contract is non-reentrant: a re-grab mid-coast destroys the pending `dragEnd` and re-latches `wasPlayingBeforeScrub` from an already-paused state; playback stays paused forever.** Proof: `:206` `coastPlayback.stop()` then `:208` `emit("dragStart")` in one `onStart`; `dragEnd` reachable only from `:175` and `:189` (inside the coast's settle frame); playback.ts `stop()` = `_gen++` + cancel + `_cleanup()`, no terminal step, so the `:189` emit is destroyed; useAnimationGroupPlayback `:119` is a plain assignment, `:125-131` resumes only if true; `coastPlayback.running` is read at exactly one site (`:243`) — no re-entrancy guard exists; the raw-scene shape (SpringScene :98-106) latches identically. The exclusivity clause is killed (register K4): the shared seam can double-start too, so the cure is seam-level, with KF-AV-15. Trigger is the natural gesture — D-1's fling races the ball to a boundary, which is precisely when a user grabs it back. → **NO-WAVE-OWNER** (SS-1/SS-2). Spec obligations: a terminal-emit invariant (every `dragStart` pairs with exactly one `dragEnd`, incl. stop/dispose paths) + a counted or state-checked consumer latch.
- **KF-AV-3 · C-1 = the C-corpus BLOCKER — mouse/pen pointers are routed through the touch-only gate; the first press on the ball is swallowed on any `'ontouchstart' in window` runtime, and re-swallowed after every 3 s idle; the sibling 100 lines away carries the recorded measurement and the three-word cure.** Proof: `:230-235` calls `gate.handleTouchStart(ball, e.clientY)` with no `e.pointerType` test; installed dist: `S(e,t)` returns FALSE when touch-capable and not yet active, the 150 ms arm timer + the 3 s inactivity timer restarted by `:221`'s `suppressDeactivate(false)`; PlaybackRibbon :167-179 gates the identical composable behind `if (e.pointerType === "touch")` under the 14-line comment recording the measured Chromium failure ("TRUE in Chromium (desktop + Playwright) even for a MOUSE… proof:drag-gesture clause (a) RED on this one surface"). The runtime predicate itself is UNPROVEN-NEEDS-LIVE this session (residue #1); the structural asymmetry is unconditional. → **NO-WAVE-OWNER** (SS-1/SS-2): the pointerType branch, verbatim from the sibling.

### MAJOR

- **KF-AV-4 · D-1 = L·M-3 = C-6(b) — the "inertia" is not inertia: every above-threshold release is retargeted to a rail boundary; velocity governs transit time only, never destination.** Proof: `:182-183`; spring `checkSettled` measures distance to `scaledTarget` only (both thresholds 1e-3), `dampingFraction: 1` forbids the ringing that could rest elsewhere; `FLING_THRESHOLD = 2e-5` progress/ms = 2%/s. Severity MAJOR per the ruled table (BLOCKER dissent recorded: axis-D + reader-1). Cure framing corrected per K-6/K-7 (register): `decay` is a pure sampler, not a `Tickable`; `reseatToSpring` is shape-identical to the current two lines; `probeVelocity` replaces the estimator but carries no staleness test either. The prose (`:139-141`, `:166-171`) claims momentum physics and must change under ANY ruling on intent (residue #5). → **NO-WAVE-OWNER** (SS-1/SS-2): a destination law (velocity-proportional endpoint, boundary-clamped) or an owner-ratified paging affordance with honest prose.
- **KF-AV-5 · L·M-1 = C-5 — the "exponentially-smoothed" velocity estimator is a raw passthrough across the entire tap-vs-fling decision band; the docblock's "no settle snap during sampling" is false.** Proof: `:136` leaves `snapThreshold` at the 0.001 default; smooth.ts runs the snap **unconditionally** inside `tickDt`, before and independent of the `clamp` block; factor = 1−e^−0.4 ≈ 0.33, so the snap fires for residuals under ~1.5e-3 — containing the whole band [2e-5, 1.5e-3]. Reader-2's sharpening adopted: after the first sample the residual is the **consecutive-sample delta**, so the estimator snap-locks to the raw last sample for essentially every realistic drag. Fix shape `snapThreshold: 0` is sound (nothing reads `.settled`; `_playback` never armed). → **NO-WAVE-OWNER** (rides KF-AV-4's spec).
- **KF-AV-6 · L·M-2 = C-6(a) — velocity is never invalidated by a sample gap: press → flick → hold still → release still flings at the pre-hold speed.** Proof: `:155` refuses to COMPUTE across a gap but never zeroes the estimator; `lastMoveTime` never read in the release path; a stationary pointer emits no `pointermove`; `onPointerUp` calls `onEnd` with no terminal move sample. The precision-placement gesture ("drag, stop, release") is the one this breaks — the composition with KF-AV-4/5 is the real severity of the fling cluster. → **NO-WAVE-OWNER** (rides KF-AV-4's spec: a staleness bound at release).
- **KF-AV-7 · D-4 = L·M-4 = C-2 — `touch-gate-target`/`touch-gate-active` are phantom classes: 4 call sites, 0 rule definitions in the reachable cascade; the mobile arm-state has zero visual feedback.** Proof: census re-run this session (4 hits demo+src, both components; 2 glass-ui hits, both prose); the gate mutates `el.style.touchAction` directly and never touches a class; the SFC has no `<style>` block; neither name is a Tailwind pattern. F-1 caveat folds by reference (glass-ui in neither manifest — re-verified 0/0): every negative proved against the installed dist is install-specific. → **NO-WAVE-OWNER** for the demo-side affordance decision (author the two rules or drop the dead pair and style the armed state); **glass-ui BH relay note** recorded below (a behavior contract shipped with no styling contract is a producer-side gap).
- **KF-AV-8 · L·M-5 = C-3 (+ C-16) — the lone deep `@src` import breaches the demo's own written law (kf-engine.ts:4-11) and reaches a symbol absent from both published bundles; the target module is value.js-bearing, parser-touching, and side-effectful at module eval.** Proof re-run: exports map = 2 entries; `bumpLayoutEpoch` = 0 hits in both dist bundles; browser.ts imports `@mkbabb/value.js/{value,css}` and installs the module-eval `window.resize` listener; the same file imports its other four kf symbols correctly from the barrel. **Substrate sharpening (this seat's own)**: at HEAD the import is `from "@mkbabb/value.js"` — a build-breaking named import of a symbol value.js never shipped; the deep import is the **uncommitted repair**, which chose the boundary-breaching path because **no compliant path exists** — the epoch surface is unpublished. So the cure is forced to be a publication/relocation decision, not an import edit. → **fold by reference to KF-CE-12** (kf-CSSCodeEditor): the 3-site `@src/animation/resolve/browser` extirpation + publish-or-relocate decision → **KF.W8**; the depcruise-over-demo gate arm → **KF.W4**. Bundle-weight half UNPROVEN (residue #4). C-16's listener observation rides the same identity with the K-10 read-down (the `sideEffects: false` "contradiction" framing is dropped — see the killed register; KF-CE-12's "falsifies sideEffects" phrasing should be read down likewise; recorded as addendum, the dated record itself untouched).
- **KF-AV-9 · L·M-6 + reader-2 miss #1 — the app-global epoch wire is wrong in BOTH directions: hosted by a conditionally-mounted decorative twin that consumes the epoch for nothing (under-eviction — `source` mode and ribbon-less routes lose container-resize eviction entirely), AND un-debounced (over-eviction — `bumpLayoutEpoch` is a TOTAL `new WeakMap()` flush of the app-global cache, fired once per mount by `useResizeObserver`'s initial observation and once per frame of any width transition, double-bumping with the library's own window listener; each flush forces the probe path's synchronous style recalc per animated computed endpoint app-wide).** Proof: `:79` is the only wire in demo/ (census 2 hits, both this file); `getLayoutEpoch` in demo = 0; the ball is a direct JS transform, the dashed twin a static container-query class — nothing here is engine-resolved (C-14 confirmed); browser.ts:17-20 + the probe path :210-232 read this session; interp-slot :280-284 is the sole consumer. Bounding note (this seat's): the eviction API is global-by-construction (library-side — no scoped bump exists) and the wire mirrors the library's own undebounced window-resize policy, so half of this row is a library-surface design question. → **KF.W8** with KF-AV-8 (the epoch surface decision owns eviction scoping/debounce); the wire's false LOCAL rationale + its source-regex pin → KF-AV-18/KF.W4.
- **KF-AV-10 · D-5 — the flagship rail/ball hand-rolls the demo's own promoted `.progress-rail`/`.progress-ball` idiom — its seventh, divergent authoring, against DESIGN.md §5's "remain central rather than being copied into SFCs".** Proof: design-idioms.css :161-187 (2px rail, `--rail-tint` 8%, `--ball-size` 36px, `--ball-glow` 35%) vs `h-1`/`bg-accent-kf/20`/`h-12 w-12`/`shadow-md`; 7 consuming component files (reader-2's count adopted — incl. SpringPhysicsFacet.vue); no `<style>` block to reconcile. **Both hedge-kills sustained and verified**: the retint seam is LIVE today (SequenceTarget `:80` `ROW_TONES[row.index]`; SpringTarget `:113` `lane.tone`) — the divergence is stronger than the axis claimed: a live five-way retint idiom the flagship cannot participate in. → **KF.W7** (sequenced with the S-9 evaluate: bespoke-survives ⇒ adopt the idiom; swap ⇒ moot).
- **KF-AV-11 · D-6-as-rescoped + reader-1 miss M-gamma — the "ONE motion-color identity" is false in the shipped tree: the operative Slider paints no violet at all (neutral track + range; both `--color-progress` declarations land on the invisible standard thumb), the twin is violet throughout, and the ribbon prose asserts the opposite.** Proof: the D-6/D-7 ruled rows above; PlaybackRibbon :208-216 verbatim; playback-idiom.css consumes `--color-progress` (the indirection is intended live); the ball additionally bypasses the authority (`bg-accent-kf` at `:15/:21/:31/:35`). A live shipping split, worse than the latent hazard D-6 argued. → **KF.W6** (Glass Suffusion owns the slider-var wiring + token truth): either route the violet into the slider's visible surfaces (or retint the twin neutral) and correct the prose — SS-13 judges the result (residue #6).
- **KF-AV-12 · C-8 = L·m-3 (+ reader-1 miss M-theta as sibling trigger) — scope dispose (and any mid-coast unmount) stops the coast without emitting the terminator, stranding the consumer's pause latch; the hidden-tab variant strands the whole gesture and lands with a one-frame boundary jump on return.** Proof: `:255`; no terminal step in `stop()`; `dragEnd` only at `:175`/`:189`; `<Teleport v-if="active">` + `v-if="animation"` make the unmount routine; PlaybackRibbon has no dispose. M-theta verified mechanically: the coast is a bare `RAFPlayback` with no visibility term (unlike useDemoTicker's reconcile); rAF stops in a hidden tab with `coastSpring.settled === false`; on return `dt = now − _lastFrameT` spans the hidden interval and the closed form lands at the boundary in one frame. → **NO-WAVE-OWNER** (rides KF-AV-2's terminal-emit invariant; the dispose emits, and the coast either rides the visible ticker or bounds its dt).
- **KF-AV-14 · reader-1 miss M-beta (+ D-7's surviving macro) — the "visual twin" is geometrically not a twin: the ball's centre travels [24px, W−24px] while the Slider's visible indicator (the range fill edge — the standard thumb being `width:0`) travels [0, W]; they coincide only at 50%, disagreeing ~12px at the quartiles and 24px at each end, in opposite directions.** Proof: both rows in one `grid gap-2`, both inner boxes `p-2`; `getMaxX()` = width−48; the rail's 3rem gutter (SUP-1's internally-beautiful geometry is exactly what breaks the twin externally); `.slider-range` is a percent-width fill from `--slider-range-origin: left center`. The proportion-inversion macro (48px decorative ball + 4px rail beside a 24px operative track) folds here as the surviving half of D-7. Perceived magnitude → SS-13 (residue #6, judged in the same frame as the KF-AV-11 hue split). → **NO-WAVE-OWNER** (SS-1/SS-2; cross-ref KF.W7).
- **KF-AV-15 · reader-1 miss M-delta = reader-2 miss #4, merged — `useDragCapture` is non-reentrant and has no scope-bound cleanup (its own docblock's `tryOnScopeDispose` guarantee is false against installed @vueuse 14); both holes terminate in a permanently stuck document-wide `user-select: none`.** Proof: the ruled table (this seat's whole-file reads of useDragCapture + gestureSelectSuppression + the vueuse dist). Two static triggers: (1) second pointer — no `isDragging` test at `:52`, acquire ×2 / release ×1, `stops` replaced (first handles orphaned), second `dragStart` with no `dragEnd` (KF-AV-2's corruption with no coast required — and PlaybackRibbon's slider seam shares it); (2) mid-drag unmount — no active scope inside the DOM handler, listeners die with the detached node, `releaseSelectSuppression` unreachable. Both leave `activeGestureCount ≥ 1` forever → `user-select:none !important` on `html/body/*` for the session. The 48px ball is the seam's most exposed consumer; the seam is shared. → **NO-WAVE-OWNER** (SS-1/SS-2, seam-level): re-entrancy guard + scope-owned registration (or explicit unmount teardown that releases) + the docblock corrected; the consumer latch hardening rides KF-AV-2.
- **KF-AV-16 · reader-2 miss #2 — the coast writes to localStorage once per animation frame: `applyProgress` emits `scrub` from inside the free-running coast rAF with no user input, and the chain terminates in a scene-machine dispatch that rebuilds the persisted context and synchronously serialises it.** Proof (chain re-walked at the key nodes this session): `:185-190` → `:119` emit per frame → PlaybackRibbon `:77`/`:181-194` → ChannelOptions `:393-396` → useAnimationGroupPlayback `sliderUpdate` → `setChildTime().render()` + `machine.dispatch({type:"SCRUB", t})` → sceneMachine `scrub()` **always allocates a fresh context** (fresh `animations` record + two spreads — the dispatch echo-guard cannot fire) → `persisted.value = {…}` → `useStorage` with no options = deep watch + synchronous `JSON.stringify` + `setItem` per flush. ~30-60 blocking writes per flick. Scoping (this seat's): the per-SCRUB-dispatch persist is a chain property that pointer scrubs pay too; the coast makes it **autonomous** (rAF-driven, no input). Cross-ref kf-AnimationControlsGroup **L-1**: the same branch carries the inverse defect for facility channels (no dispatch at all) — one branch, two defects, one spec seam. → **NO-WAVE-OWNER** (SS-1/SS-2): persistence debounced/throttled at the machine seam or SCRUB-persist decoupled from per-frame emission; spec'd jointly with the ACG L-1 obligation.

### MINOR

- **KF-AV-13 · reader-1 miss M-alpha — no press-to-seek on the track: only the 48px ball is a pointer target, and the geometry written for a track press is dead by placement.** Proof (this seat's re-derivation): `:25` is the sole handler; rail + both markers `pointer-events-none`; `trackEl`/`containerEl` carry nothing; substituting `:200` into `:108` cancels `clientX` and `ballW/2` — the on-grab projection is an exact identity, so `progressFromPointerX`'s generality only does work for a press that lands off the ball, which the handler placement forbids. The sibling binds the whole rail (SequenceScrubber `:28`), as does the Slider. Graded MINOR (reader-1's MAJOR as dissent): the operative Slider sits directly above and owns seek; this is twin-completeness + dead code, not broken behavior. → **NO-WAVE-OWNER** (SS-1/SS-2; decide press-to-seek with the KF.W7 evaluation).
- **KF-AV-17 · D-3 (demoted by reader-1, concurred) — a container resize leaves the ball at a stale absolute px; the resize hook cures a cache the ball never consults.** Self-heals within ~16ms while playing; the persistence window IS KF-AV-1's window — a symptom, not a second defect. Cure correction per K-12 (sustained at the bytes): the sibling's `translateX(calc(p*100cqw))` is NOT portable (full-width mapping — at p=1 the ball overshoots the rail); the resize-immune form is `translateX(calc(var(--p) * (100cqw − 100%)))`, the dashed marker's own proven expression. → folds into KF-AV-1's cure.
- **KF-AV-18 · D-14 = L·m-5 = C-4 — the load-bearing 9-line comment attributes the layout-epoch cache to value.js four times; every named symbol is keyframes.js's own; the error is replicated into the regression net (resize-tracks.test.ts prose vs its own import).** Substrate reframe (this seat's): the comment is the fossil of HEAD's broken `@mkbabb/value.js` import — born consistent and wrong together; the uncommitted diff fixed the import and left the prose. MINOR (reader-1's MAJOR as dissent). → prose sweep rides KF-AV-8's KF.W8 decision; the **source-regex gate pin** (`fs.readFileSync` + two `toMatch`) is the KF.W4 gate-authoring-law class (a gate may not pin a mechanism the component cannot exercise) → **KF.W4**.
- **KF-AV-19 · D-8 — non-text contrast: the rail and both end markers fall below WCAG 1.4.11's 3:1 in both theme arms; the ball is comfortably clear.** Direction robust (no 15-40% overlay of a mid-luminance accent reaches 3:1 against either plate); **no numeral in this corpus is quotable** — the axis, reader-1, and reader-2 produced three mutually inconsistent recomputes (corrections register #5). The dashed end marker is the load-bearing failure. → **KF.W9** (SS-13 re-derives on the live composited `Card cartoon tier="quiet"` plate; residue #2).
- **KF-AV-20 · D-9 — no forced-colors handling; under WHC the rail, ball and markers collapse into one strip (only the dashed border survives as form).** Census re-run: 0 hits demo+src; all four paints are Tailwind background-colors on bare divs, unreachable by glass-ui's accessibility.css. → **fold by reference to KF-CE-13's demo-wide forced-colors row → KF.W9**; this component's paints are that row's second witness. Rendering outcome residue #3.
- **KF-AV-21 · D-11 — the manipulable ball carries no label, tooltip, or legend; discoverability rests on the cursor-only hint DESIGN.md §7 names insufficient.** The Slider's tooltip excludes the visualizer (`:72-80` sits outside `<Tooltip>`). §7's "scene" scope makes MINOR the honest grade. → **NO-WAVE-OWNER** (SS-1/SS-2, with KF-AV-29).
- **KF-AV-22 · D-13 = L·m-2 + reader-2 miss #6 — four inert utilities (`text-accent-kf-foreground` on a permanently empty div; `h-full` on the root; `relative` on trackEl; `relative` on containerEl — redundant under `contain: layout`), two width oracles (`clientWidth` int vs `getBoundingClientRect().width` fractional, on two different elements), and a hard-coded `48` duplicating `w-12`.** K-9 mechanism correction adopted: the root's `h-full` is inert via the grid item's default `align-self: stretch`, not via percentage-against-auto (the corpus's block-flow reasoning would mis-analyse any grid-hosted component). → **NO-WAVE-OWNER** hygiene (rides whichever wave touches the file).
- **KF-AV-23 · D-15 + reader-1 miss M-iota — comment mass mints rationale DESIGN.md §6 reserves to itself, in a promotional register that conceals KF-AV-4; the coast docblock's "nearest boundary" is vacuous (exactly one boundary exists per direction) and actively imports the wrong magnetic-snap model — from 0.05 a rightward flick targets the FARTHEST boundary.** → **NO-WAVE-OWNER** prose sweep, bound to KF-AV-4's cure (a currency sweep fixing only the flagged sentence would leave the misdirection intact).
- **KF-AV-24 · D-16 — `progressFromPointerX` is a fourth hand-rolled rect-ratio projector against useDragScrub's "the ONE pointer-drag scrub seam" charter; the demo carries two rail/ball scrubbers on two drag seams with two opposite a11y postures** (SequenceScrubber: `role=slider` + keyboard; this file: `aria-hidden`, no keyboard — both deliberate, both verified). → **KF.W8** (seam consolidation decision), cross-ref KF.W7.
- **KF-AV-25 · L·m-1 = C-10 — contradictory null posture: dead `!anim` guard at `:114` against the non-optional prop, unguarded dereference at `:243` inside the shared ticker loop (one throw kills every demo ticker — useDemoTicker iterates all subscribers in one loop).** Currently unreachable (parent gates the mount). → **NO-WAVE-OWNER** hygiene.
- **KF-AV-26 · L·m-4 — six concerns in one SFC; the DOM-free `:122-191` inertia block is an un-extracted composable in a tree with 71 colocated `use*.ts`; the extraction is why KF-AV-4/5/6 have no unit test (the only test touching the component is the source-regex).** → **KF.W8** (Structure & Colocation Settle).
- **KF-AV-27 · C-12 — `useRafLoop` is a six-line pass-through whose docblock describes the engine's `RAFPlayback.loop` driver while the real contract is useDemoTicker's shared module singleton + per-subscriber gating + visibility reconcile; the hiding is what let KF-AV-1's guard look sufficient.** Census closed by this seat: 2 consumers, both on the singleton (the third filename hit is a comment — instrument caution recorded). Extends F-5's shim class → **KF.W6** (the F-5 shim-delete arm): inline or re-document.
- **KF-AV-28 · D-18 = C-13 — census extension S-9: the shadow census skipped the larger of the two rail/ball shadows. AnimationVisualizer re-derives glass-ui ScrubberTimeline's shape (256 L vs the censused 162-L SequenceScrubber), on a `/timeline` subpath the demo never imports (verified: ships in dist; demo imports 0).** Both axes and both readers converge on **evaluate, not swap** (the aria-hidden charter and fling semantics may exceed the primitive; `role=slider` would conflict with the deliberate disposition). → **KF.W7** (Timeline Evaluate) — this row is the wave's intake, and its verdict gates every NO-WAVE-OWNER row above (the standing rider).
- **KF-AV-29 · reader-1 miss M-zeta — the ball has no hover/active/armed affordance whatsoever: the cursor is the component's only interaction feedback, three interaction states share one pixel of difference; the idiom it bypasses ships the vocabulary (`--ball-glow`) and the Slider it mirrors has hover + `[data-held]` states.** Compounds KF-AV-7 (the armed state is likewise invisible). → **NO-WAVE-OWNER** (SS-1/SS-2, with KF-AV-7's affordance decision).
- **KF-AV-30 · reader-1 miss M-eta — a non-primary button press on the ball arms the touch gate without starting any drag: the gate is consulted before `useDragCapture`'s `e.button !== 0` check — the same ordering mistake as KF-AV-3 at a second site in the same five-line function.** Verified: `:233` then `:234` → `:52`'s button test. → **NO-WAVE-OWNER** (rides KF-AV-3's cure).
- **KF-AV-31** — reserved under KF-AV-12 (M-theta folded there; no separate booking — anti-rename).
- **KF-AV-32 · reader-2 miss #5 — two independent `useTouchGate()` instances arm and DISARM each other inside one ribbon: the installed gate keeps a module-level Set + one shared document `touchstart` handler that deactivates every active instance whose controlEl does not contain the touch target — so arming the ball's gate disarms the slider's and vice versa; a touch user re-taps-to-arm on every switch between the two halves of one instrument.** Verified at the dist chunk this session (the Set, the handler, the per-instance 3 s timers). → **NO-WAVE-OWNER** (one gate per ribbon, or a producer-side group facility — noted in the BH relay).

### INFO

- **KF-AV-33 · C-11** — FOUR import statements resolve `@mkbabb/keyframes.js` (`:47-50`; both readers converged on 4 after the axis's 3); no lint requires the split. NO-WAVE-OWNER hygiene.
- **KF-AV-34 · C-15 + reader-1 M-lambda = reader-2 K-7 (convergent)** — `z-bar` is the wrong semantic rung under the demo's own z-contract (`z-controls` is the named rung and still clears both markers), and the paint risk is provably nil: `container-inline-size` ⇒ `contain: layout` ⇒ stacking context — the rung never leaves the visualizer. Priced as hygiene, not a paint bug. NO-WAVE-OWNER.
- **KF-AV-35 · C-16-as-rescoped** — the deep import installs a permanent, teardown-less global `resize` listener at module eval. The `sideEffects: false` "contradiction" framing is dropped per K-10 (the declaration is self-consistent: dropping the module drops the cache the listener serves). Folds into KF-AV-8/KF-CE-12 → KF.W8.
- **KF-AV-36 · C-17** — `--visualizer-track-gutter: 3rem` has exactly one consumer, and its comment encodes an invariant the sibling literal `left-6` must satisfy by hand. NO-WAVE-OWNER hygiene (couple or inline at the KF-AV-10/14 cure).
- **KF-AV-37 · D-17** — RTL: physical-axis geometry throughout; latent (dir census 0); the glass slider carries `:dir(rtl)` handling, so the operative control would flip while the twin would not. Recorded for the first RTL pass; no route.
- **KF-AV-38 · L·I-1 = C-9** — `KeyframesAnimation<any>` at the props boundary; a house idiom (ribbon/ChannelOptions/useAnimationSync), logged at this site; `unknown` may be the honest ceiling for a target-agnostic ribbon. Seam observation, no route.
- **KF-AV-39 · L·I-2** — `useTemplateRef<HTMLElement>('ball')`: the script's lone single-quoted string and the only ref whose variable diverges from its template key. NO-WAVE-OWNER hygiene.
- **KF-AV-40 · D-12 = L·I-3, ruled** — permanent `will-change-transform`: INFO on the merits (the ball transforms every playback frame; one 48px layer); the measured-budget half rides SS-13 (residue #7).
- **KF-AV-41 · D-10, ruled** — zero-duration state: dead ball wearing a live grab cursor and acquiring real pointer capture + the suppression token; INFO — no route delivers `duration === 0` to a mounted instance (reader-1's MINOR as dissent). NO-WAVE-OWNER observation.
- **KF-AV-42 · reader-1 miss M-kappa** — the unprefixed-token collision set is measured EMPTY today (`--ball-*`/`--rail-*` = 0 hits in installed glass-ui); the hazard both D-6 and C-17 raise is a future glass-ui minor, not the current tree — and under F-1 (unpinned) the negative is install-specific. Recorded so the next reader dates the drift instead of re-asserting the risk.
- **KF-AV-43 · reader-2 miss #7** — each instance allocates three `RAFPlayback`s (coast + the two steppers' never-armed `_playback`s), each with four per-instance arrow-field closures — by documented design (few, long-lived instances), but worth pricing before any wave instantiates steppers per row/lane. Library note; no route.

## Killed-claims register — all SUSTAINED, refutations re-executed by this seat

1. **K-1 (reader-1) · D-7's thumb row** ("operative thumb 16px vs ball 48px — 3×") — the visible-thumb rules are exclusively `[data-variant=spectrum]`-scoped (my python extraction, full selectors); default = "standard"; 7.0.0's union has no "timeline" (and HEAD's `variant="timeline"` was therefore dead too). There is no on-screen thumb; the ratio is fabricated. The macro survives, rebooked as KF-AV-14.
2. **K-2 (both readers, convergent) · D-5's "latent, not live" hedge** — refuted by the tree the axis grepped: SequenceTarget `:80` (`ROW_TONES`, five distinct hues) and SpringTarget `:113` (`lane.tone`) retint `--ball-tone` per row/lane TODAY. Strengthens D-5.
3. **K-3 (reader-1) · D-6's "they agree only because :163 aliases" reasoning** — the twin is ALREADY split: neutral `--color-slider-track` drives both visible slider surfaces; the two `--color-progress` declarations land on the invisible thumb. Rebooked stronger as KF-AV-11.
4. **K-4 (reader-1) · L·B-1's exclusivity clause** ("AnimationVisualizer is the ONLY emitter that can double-`dragStart`") — `useDragCapture.onPointerDown` has no re-entrancy guard (my whole-file read), so the ribbon's slider seam double-starts under a second pointer too. The defect stands; the blame locus is the shared seam (KF-AV-15).
5. **K-5 (reader-1 + reader-2's miss #4) · C-8's "vueuse cleans the listeners on scope dispose" premise and useDragCapture's own docblock** — installed @vueuse 14's `useEventListener` is a bare `watchImmediate` (no `tryOnScopeDispose` call — my dist read), and it is invoked from inside a DOM event task where no scope is active; nothing is auto-cleaned. C-8's substantive claim survives; its premise was backwards and the consequence worse (KF-AV-15). Reader-2's "benign" pricing corrected (see corrections #3).
6. **K-6 (reader-2) · the decay()-swap cure framing in D-1/M-3/C-6** — `decay()` is a pure sampler implementing no `Tickable` (grep 0); `RAFPlayback.drive` requires `tickDt`+`settled`; the cited precedent (useOrbitalInertia) keeps its own integration and uses decay only as a velocity-factor source. Defect intact; one-line-swap framing dead.
7. **K-7 (reader-2) · C-6's `reseatToSpring` sub-claim** — reseat targets a **supplied** `newTarget` (my read of reseat.ts): functionally identical to the current two lines; its consumption could not cure the boundary target. `probeVelocity` is the better estimator but carries no staleness test, so KF-AV-6 survives that swap too.
8. **K-8 (reader-2) · C's superlative S-f overshoot condition** ("exceeds the target for v₀ > 0") — re-derived by this seat: for ζ=1 the zero-crossing needs `v₀ > ω·|x₀|` (ω ≈ 13.96 rad/s at response 0.45), so a slow mid-rail release cannot overshoot. The conclusion (the `:186` clamp is genuinely required for near-rail flings) survives; the stated justification must not be re-quoted.
9. **K-9 (reader-2) · D-13's `h-full` mechanism** — grid items resolve percentage heights against the grid area, not a block parent's auto height; the utility is inert via default `align-self: stretch`. Conclusion survives; the block-flow reasoning would mis-analyse any grid-hosted component.
10. **K-10 (reader-2) · C-16's `sideEffects: false` contradiction** — the declaration licenses dropping the module only when no export is used, and cache + reader + listener cohabit one module; self-consistent. The surviving charge is the narrower one (unremovable unowned global listener, no teardown export) — carried in KF-AV-35. Corollary recorded: KF-CE-12's "falsifies `sideEffects: false`" phrasing reads down the same way (addendum beside the dated record, never rewriting it).
11. **K-11 (reader-2 K-7 = reader-1 M-lambda, independently convergent) · C-15's containment speculation** — the stacking context is one level away and unconditional (`container-inline-size` ⇒ `contain: layout style inline-size`); the rung is unobservable outside the visualizer. Carried in KF-AV-34.
12. **K-12 (reader-2) · D-3's cited cure portability** — SequenceScrubber's `translateX(calc(p*100cqw))` maps progress over the FULL container width (verified at `:34`); verbatim adoption overshoots the rail at p=1 and breaks SUP-1's three-way coherence. The resize-immune form is the dashed marker's own `calc(… * (100cqw − 100%))`. Carried in KF-AV-17.

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-2's D-7 confirmation cell** — quoted `--slider-thumb-size: 1rem` as the operative thumb without checking variant scoping; the var is dead for this slider. A citation-inheritance-shaped event (the challenge's table re-endorsed unverified), per the ColorSpaceSelector DISSENT-1 precedent.
2. **Reader-2's D-6 cell** ("the two paths agree only by the current alias") — they do not agree; the split is live. Same root miss as #1 (the invisible thumb).
3. **Reader-2's missed-M4 "practical consequence is benign" clause** — the suppression counter is escape-free (my whole-file read: no timeout, no visibility reset) and both triggers are routine; the consequence is a session-permanent document-wide `user-select: none`. The clause priced the wrong half.
4. **Reader-1's B-1-kill mechanism letter** — cited `tryOnScopeDispose` as `useEventListener`'s internal guard; installed v14 never calls it (cleanup rides the watcher). Reader-1's conclusion ("ownership depends on the active scope") was nonetheless correct; letter-correction only.
5. **The D-8 numerals** — the axis (rail 1.30, ball 4.81), reader-1 (rail ~1.20), and reader-2 (rail ~1.18, ball ~4.4) are mutually inconsistent; register law: state the compositing assumptions beside the number or the number is not portable. No numeral from this corpus is quotable; SS-13 re-derives on the live plate.
6. **Reader-1's C-12 census instrument** — "grep -rn useRafLoop demo/ returns exactly two consumers" is true of consumers but a filename-level grep returns 4 files (one comment-only, one the definition); state the counting unit beside the claim (the pt-leaf ruling-6 law, second occurrence).

## Superlatives — sustained, with provenance

1. **SUP-1 · the gutter geometry** (axis D SUP-1; both readers verified independently; this seat re-checked the token + template): three independent expressions of `containerWidth − 48` — JS measurement, CSS calc inset with the 3rem token, container-query transform — agree by construction and scale together; the falsifier (padding/border on containerEl) is negative. The best thing in the file — and, per KF-AV-14, exactly what breaks the twin externally.
2. **SUP-2 · the aria-hidden disposition** (all three axes + both readers + this seat): states the rule it applies; structurally guaranteed — no tabindex/role/native control in the subtree, one mount site, the deferred-to Slider in the same subtree. KF-AV-1/14/11 attack the PREMISE (redundancy); the technique needs no change when they are cured.
3. **SUP-3 · the `touchAction` computation** (`:11`): `isTouchDevice` read without `.value` (a plain boolean — verified in the dist), `isActive.value` with; the posture preserves page scroll on touch until the gate arms — better mobile posture than glass-ui's own Slider (static `touch-action: none`).
4. **SUP-4 · the teardown reasons rather than asserts** (`:252-255`): distinguishes the two playbacks by ownership, names the bounded micro-leak; `stop` is an idempotent arrow class-field (`:236` guard; the bind-proof block at playback.ts:62-78 read this session). The gap is the unemitted event (KF-AV-12), not the stop.
5. **SUP-5 · the terminal emit is exactly-once by the driver's contract** — `drive` returns `!tickable.settled`, so the settle frame is provably the last; no flag, no de-dup; the re-arm is idempotent. (The overshoot-justification half is corrected per K-8; the `:186` clamp remains genuinely required.)
6. **SUP-6 · the clock frame is single-authority** — `effectiveT` out, `effectiveT` back; the reversed→raw conversion lives entirely in the ribbon (`:183-185`); no `reversed` in the component's 256 lines. The classic twin-drift bug, conspicuously absent.
7. **SUP-7 · the value.js edge is textbook** (`:46` `clamp` from `/math`): parser-free subpath (no R1 crash class in this call graph), correct arity at both sites, necessary (the kf barrel exports no clamp — grep 0), and the one `@mkbabb` dep that is declared AND locked. One line above the boundary breach, four ways to get it wrong, none taken.
8. **SUP-8 · the grabOffset algebra is exactly right** — the on-grab projection is an exact identity (this seat re-derived the cancellation), so a grab never jumps; `grabOffset` resets on release. Diagnostically load-bearing twice: it is what exposes KF-AV-13 (the dead generality) and what makes KF-AV-1's overwrite purely a staleness consequence — fix the staleness and the grab math is already correct.
9. **SUP-9 · theme honesty is airtight AND the silent failure is closed** — zero color literals in the SFC (grep 0); every paint rides `--accent-kf`'s single `light-dark()` pair; and the coupling that makes it work is present twice and documented: `color-scheme: light` at root + `.dark { color-scheme: dark }` (style.css :91/:181), reinforced by glass-ui's dark-arm.
10. **SUP-10 · the two-driver interlock** (`:243`): the sync loop yields the transform to whichever driver owns it — drag, coast, or playback — one writer per instant, including the hard both-loops-live case. Only the guard's arming condition (KF-AV-1) is wrong; the interlock survives the fix unchanged.
11. **SUP-11 · no parser surface is reachable from this component's call graph** (C's S-e, re-affirmed): only numbers and the animation handle cross its boundary; KF-AV-8 drags parser CODE into the graph, never a CALL into the path — stated precisely so the imprecise version does not become a false defect later.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **C-1's runtime predicate** — whether `'ontouchstart' in window` is TRUE in the SS-13 cell. The structural defect is unconditional; the swallow is conditional; the only current evidence is the repo's own recorded measurement (PlaybackRibbon :158-162). One first-press drag settles it.
2. **D-8's composited contrast** on the live `Card cartoon tier="quiet"` plate — direction robust, no corpus numeral quotable (corrections #5).
3. **D-9's forced-colors rendering** — the absence of handling is proven; what the four surfaces collapse to is not static.
4. **The bundle-weight half of KF-AV-8** — reader-2's stale-dist finding adopted: the on-disk gh-pages assets predate the seam (and the eager value.js css-chunk observation there is non-attributable); only a fresh `KF_ANALYZE=1 vite build --mode gh-pages` settles it. The encapsulation half stands unconditionally.
5. **KF-AV-4's intent** — owner ruling: defect or "flick to end" affordance. The mechanism is proven; the prose claims physics and is wrong under either ruling.
6. **KF-AV-14's perceived magnitude** at live ribbon widths — judge in the same frame as KF-AV-11's hue split (they compound).
7. **KF-AV-40's compositor-layer budget** — no measured figure exists for this ribbon.
8. **KF-AV-41's reachability** — no static route to `duration === 0` on a mounted instance; a live counterexample would promote it.

## Relay obligations (recorded here; no inbox write from this seat — sole-write law)

Per the standing glass-ui BH relay edict, the next value.js/keyframes session with mail duty should relay: (a) **useTouchGate ships a behavior contract with no styling contract** — consumers bind `isActive` to classes glass-ui neither defines nor documents (KF-AV-7), and multiple instances on one surface mutually deactivate via the module-level Set (KF-AV-32) — a per-group or per-scope gate facility is the producer-shaped cure; (b) **the standard-variant Slider renders no thumb** (`width:0; opacity:0`; only `spectrum` un-hides) — consumers routing accent tokens into `--slider-thumb-bg` paint nothing and the docs should say so (the KF-AV-11 discovery).

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c` (dirty; diff characterized in the substrate disclosure): 42 roster rows — 3 BLOCKER (the stale twin that silently overwrites the playhead it mirrors; the non-reentrant emit contract that strands playback paused; the touch-only gate swallowing every first mouse press) · 12 MAJOR · 16 MINOR · 11 INFO; 12 kills sustained with re-executed refutations, including the D-7 thumb-row kill decided at the bytes against a reader's confirmation; 18 missed rows admitted (the playhead-overwrite, the drag-seam user-select latch-up, the per-frame localStorage coast, the geometric non-twin, and the false "ONE motion-color identity" chief among them); one net-new substrate finding by this seat (HEAD's build-breaking `@mkbabb/value.js` import — the deep import is its uncommitted repair and the misattributed comment its fossil); identity guards held (KF-CE-12, KF-CE-13, ACG L-1, F-1, S-4, R1 folded by reference — zero re-bookings); routings are M-25 agglomeration input — behavioral rows NO-WAVE-OWNER for SS-1/SS-2 under the standing KF.W7-may-supersede rider, structural folds to KF.W4/W6/W7/W8, visual residue (8 rows) to KF.W9/SS-13. The component's taste is high and its eleven superlatives are real; its two seams — the gate it consumes without the sibling's cure, and the twin premise its own guard falsifies — are where every blocker lives.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/playback/AnimationVisualizer.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **AnimationVisualizer.vue=+1/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**CORRECTION (iv) of the six named at G-0.9 — the census cell at `:9` is corrected to WORKTREE-SCOPE** (C-9 / C-1.R row 3; the kf-SequenceScrubber K-1 kill). Every anchor in that instruments line is a read of the **pre-settle working tree**, not of `origin/master`, and not of `8281638c` either: the worktree was 1-ahead-and-41-behind and is now preserved only at snapshot `6d280ee7bec7793846b2e2e1d250e1ea0a21859a`. The cell is not rewritten; its SCOPE is stated here.

**KF-AV-42's collision set (measured EMPTY) is what keeps kf-SpringTrace's `D-15` a dated hazard note and not a cure** — see the guard appended at `kf-SpringTrace.md` (C-1.R row 5, NO RE-BOOK).

**KF-AV-28, the STANDING SUPERSESSION RIDER, travels with its packets and not with this wave**: banked here at `:35`, and at `kf-PlaybackRibbon.md:36` (transport/ribbon) and `kf-SequenceScrubber.md:36` (drag-seam). Both limbs — the supersession sentence AND the C-axis §7 caution with its NON-bespoke clause — ride the packets.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — **KF-AV-41**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here. **Nothing above this line is
rewritten** — no edit, no re-grade, no renumber, no deletion, no new id (E-1 immutable bytes / E-3
addenda-not-patch). This addendum records a MEASUREMENT STATE, not a grade: **the banked severity is
unchanged and stays exactly where the record put it.***

- **armed trigger** — register row **9** of thirteen, `evidence/W9/ESCALATION-REGISTER.json` (id as banked there: `KF-AV-41`); ceiling if it fires: **promote**; roster cell(s): `—`.
- **measurement named by the trigger** — **a live counterexample instance, captured** — not an argument that one could exist.
- **measured value** — **NONE. The trigger did not run.**
- **resulting severity** — **PROVISIONAL (unrun)**. The banked grade stands; it is neither inherited as confirmed nor moved.
- **exact precondition** — a captured band wide enough for a counterexample to appear in. **This is NOT a reading of absence**: the falsifier (*"absence across the captured band leaves the grade where it stands"*) requires a captured band, and this wave produced **7 shots over 3 cells**, none on this component's surface. Absence across an unrun band is not absence, and is not recorded as one.
- **substrate of this wave** — keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD; frontier moved to `3e81f500`, the pin is its ancestor, **no re-pin on a seat's authority**). Served bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448`, built in the §0m.2 capture clone and **re-hashed at this seat, double-run identical**. Capture: **none for this row**; shot-sha256: **none**.
- **G-KFW9-13 reading** — this row closes the gate's *"unrun triggers reported PROVISIONAL at close"* arm, **not** its *"measured value written back"* arm. The gate stays **RED**.
