claude-fable-5

# fr-SliderControl — adjudicated apotheosis (M-23/L-18 r2, fresh-adjudicator seat)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **fourier HEAD**: `cd26c653` (working tree, 28 dirty paths — the audited subject per R4-9) · **value.js HEAD**: `5b8aed88`, branch `tranche-u`.
**Corpus adjudicated**: three axis challenges over `web/src/components/ui/SliderControl.vue` (150 LOC, 8 callsites) — challenge-D-design (19 defects), challenge-L-library (16), challenge-C-consumption (15), each header-receipted `claude-opus-5[1m]` — plus two hostile re-reader returns: **reader-DU** (design-and-usage) and **reader-LC** (library-and-code), both re-run against the tree.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. The one head-on verdict conflict (D-8: DU KILLED / LC CONFIRMED MAJOR) was re-derived by this seat with its own instruments and RULED. Every load-bearing kill (D-2, the three uplift clauses, D-1's provenance and specificity, C-1's count, D §6.3, D-9's spin-buttons, C-5's third row, C-7's cap, C-13's consequence) was re-run at the bytes, not spot-trusted. All six reader missed-finds were re-derived; two seat-level arithmetic corrections to the READERS themselves are recorded (both immaterial to any verdict).
**This seat's own instruments (all static + git + node; product source READ-ONLY; zero browser — probe parsimony; this file the sole write)**: SliderControl.vue read WHOLE (150 — every cited anchor checked: `:3/:16/:140` the three surviving `glass-scrubber` strings · `:18` v1.8.x pin · `:25-38` 8 props/1 emit · `:40-42` clamp (guard tests `v` only) · `:44-49` per-keystroke emit · `:53-56` bridge · `:61/:72/:75-77` type-flip trio · `:83` `variant="standard"` · `:87` aria-label · `:89` `--track-color` · `:113` `0.75em` · `:126` `outline: none` · `:128` bare `0.15s` · `:130-133` the ONE shared hover/focus selector · `:134-138` spin-button suppression · `:143-149` the five scrub declarations); fleet census `grep -rn -- "--slider-scrub" web/src` → **23 declarations / 7 files** (SliderControl 5 · BasisSelector 4 · EditorControlsDock 4 · HarmonicLevelGrid 4 · MorphPhaseConfig 4 · GlassTimeline 1 · ConvergenceTimeline 1) — L's table exact, C's 22/6 dead; `grep -o slider-scrub dist/glass-ui.css` → 0; **the D-8 decider**: `dist/styles/utilities/a11y-overrides.css` read — blanket `@media (prefers-reduced-motion: reduce)` reset `*:not([data-allow-motion]){animation-duration:.01ms!important;…}` + `{transition-duration:.1s!important; transition-property: opacity,color,background-color,border-color,box-shadow!important}` + the `[data-allow-motion]` override arm ("accessibility is absolute"), with `@media (forced-colors: active)` at `:78` and `@media (pointer: coarse)` at `:115/:154` in the SAME file, chain traced style.css:3 → `@mkbabb/glass-ui/styles` → dist/styles/index.css cascade item 11 → utilities.css:19 → a11y-overrides.css; glass-ui producer git: `v7.0.0:src/components/slider/types.ts:5` = `"standard" | "spectrum"` verbatim, `v8.0.0:…:10` = `"scrubber" | "spectrum"` with `valueText` at `:35`, `git ls-tree v7.0.0 -- src/components/slider/` → Slider.vue/index.ts/types.ts ONLY (no styles.css), HEAD package version **8.0.0**, tags v7.0.0 2026-07-17 / v8.0.0 2026-08-09; `v3.1.0:src/components/ui/slider/Slider.vue` reads **13 distinct `--slider-scrub-*` names including ALL FIVE this file writes**, deleted by `99a11083` (2026-06-06, v3.2.0, 13 scrub lines removed from src/); installed-pin checks: export map carries `./labeled-field` and `./styles/fonts`; `dist/components/custom/labeled-field/LabeledSlider.vue.d.ts` declares REQUIRED `tooltip: string` and NO description/formatValue/color; `fira-code` utility = exactly two declarations, no `tnum` (typography/utilities.css:69-72); token literals — light `--neutral-5: hsl(30 22% 40%)` (:45, "5.21:1" annotation) / dark `hsl(34 14% 62%)` (:47, "7.64:1") / `--muted-foreground: var(--neutral-5)` (:85) / `--card` `hsl(36 48% 97%)`|`hsl(24 8% 16%)` (:72/:64) / `--text-sm .875rem`+`--text-xs .75rem` (components.css:42/:44); fourier root fork style.css:40-49 (html 1.125rem mobile-first, 768px→1rem — the inversion); `cartoon-card` utility carries `background: var(--card)` (the DU backdrop correction, verified); reka-ui 2.9.10 `dist/Slider/` per-component layout EXISTS, `SliderRoot.js:91` `emits:["update:modelValue","valueCommit"]`, `:118/:128` commit guarded by `hasChanged`, `:131` `modelValue.value = nextValues` (always non-empty), `utils.js:78-84` `linearScale` degenerate short-circuit BEFORE division; consumers — ContourSettings five callsites read (`:229-238` ML toFixed(2) · `:242-251` Blur toFixed(1) · `:268-277` **"Min Area %" min=0/max=20/step=.5** toFixed(1) · `:281-290` Max Contours `'All'` · `:294-302` Smoothing toFixed(2) — formatValue census = **4 fractional + 1 integer-or-word**), FunctionInput `:179-187` (Harmonics min 1/max 100) + `:213-220` (Display terms `:min="2" :max="Math.max(2, vizHarmonics ?? nHarmonics)"`), EquationView `:161-172` **plain watch** → saveCachedInputState vs `:177-181` watchDebounced 200ms on the doSimplify path (the C-5 decider), EquationPanel (components/visualization/) `:61-65` watchDebounced 300ms auto-fires `fetchSimplified` + `:97-103` Terms min 2/max 20; api/models/equations.py — `n_harmonics: int (ge=1,le=200)` `:20`, `budget: int (ge=2,le=50)` at **both** models, `grep -c "model_config\|ConfigDict"` → **0** (the N-1 decider); `git diff SliderControl.vue` → **3 changed lines in 2 hunks** (`:7`,`:12` merge under default context; both readers' "three hunks" corrected, immaterial); `inline-number` count 6+7=**13** (LC's correction exact); registry sweeps over 42 `fr-*.md` — `slider-subtitle|opacity: 0.7` → 0 (D-4 genuinely novel), banked rows re-read at the cited anchors: fr-BasisSelector B-2/M-3/M-6/M-8/m-13/m-21/K-13/R-7, fr-ContourSettings B-1/B-6/M-2/m-11/m-13/i-4, fr-EquationPanel M-N8 (+ its qualified superlative), fr-EquationView B-2/L·M-3, fr-FunctionInput D-9=L-M2, fr-ConvergenceTimeline K-2/L·D-4, fr-AdminFlaggedPanel K6, fr-ExportModal LC-3, fr-FourierMorphDemo FMD-14, fr-AppHeader FR-AH-2, fr-AnimationControls R-11/C-5/M-7.

## Scoreboard

**28 rows CONFIRMED-OR-FOLDED (3 BLOCKER — all banked folds · 8 MAJOR · 12 MINOR · 5 INFO) · 13 KILLED · 6 reader missed-finds ADMITTED (4 as rows, 1 folded into R-21, 1 as method addendum) · 9 superlatives · 10 UNPROVEN-NEEDS-LIVE.**

Net-novel content of this entire 3×34KB corpus after the registry-first sweep: **D-4** (subtitle contrast, with DU's `--card` backdrop correction), **D-6's defect half** (no `aria-valuetext` path at any adoptable pin ≤7.0.0), **the wrapper-narrowing mechanism** behind banked M-2, **L-5's slider-cadence extension**, **D-11**, **D-15(b)**, **D-16**, **C-9's instance**, and the four admitted missed rows (N-1..N-4). Everything else folds by reference. No banked row is re-booked.

## Ruled disagreements (the seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-8** (PRM ungated "on both sides of the seam") | **KILLED** — blanket PRM reset at a11y-overrides.css on the critical path; `border-color` on the deliberately-preserved list | **CONFIRMED MAJOR** — zero PRM rules in dist/glass-ui.css scope `data-v-534634a7` | **KILLED — DU sustained; LC's confirmation REFUTED by this seat** | My own read of `dist/styles/utilities/a11y-overrides.css:6-31` reproduces DU's quote byte-for-byte: under PRM every element loses `transform` from the permitted `transition-property` list (the press-squish SNAPS — no animated spring) and `border-color` transitions are kept-but-clamped to 0.1s, so `:128` is NOT ungated. Chain verified end to end: style.css:3 → dist/styles/index.css (item 11) → utilities.css:19 → a11y-overrides.css. LC verified only the compiled SFC-scoped sheet — true as far as it goes, one file short of the cascade. This is the **fr-AdminFlaggedPanel K6 dispute re-run verbatim** (α killed on the same file, β confirmed against the wrong sheet, seat ruled for α; corroborated by the fr-AdminUserList/fr-AdminAuditLog/fr-EquationView instrument lines, all citing a11y-overrides.css:6-31) — LC's cell is recorded as a **citation-inheritance event**: it repeats challenge-D's falsified premise against fleet precedent already in the registry LC itself swept. Package scope also refutes D's garnishes: forced-colors block at `:78`, coarse-pointer floor at `:115` — 4.0.0 is not bare. What SURVIVES of the row: nothing needing a booking — the substrate's blanket-with-a-carve design handles this component correctly (see superlative S-9). |
| **D §6.3** ("delete SliderControl for LabeledSlider — a 7.0.0 gain discharging D-4/5/6/7/8/16") | KILLED as framing (ships at pin; not drop-in; discharge list unsound) | Kill of the uplift clauses notes "§6.3 stands at the named baseline" (labeled-field exists at v7.0.0) | **KILLED AS FRAMED — DU's two defeats ratified; LC's survival note folded as a re-scheduled rider** | My node read of the installed 4.0.0: export map carries `./labeled-field` and `LabeledSlider.vue.d.ts` exists — **the cure ships AT THE PIN** (already banked at fr-FourierMorphDemo FMD-14), so presenting it as an F.W1 gain mis-schedules the repair. And it is NOT drop-in: `tooltip: string` is REQUIRED, and the d.ts declares no `description`, no `formatValue`, no `color`, no inline numeric field — the fr-ExportModal **LC-3** precedent (a prescribed producer cure that redesigns the surface is a spec-integrity defect, adjudicated separately from the defect it cures; ColorSpaceSelector addendum-2). The discharge list is unsound on its face: D-8 is dead (above) and D-4's `subtitle` has no seat in any LabeledSlider contract at 4.0.0 or 7.0.0. SURVIVES: an **evaluate-retirement rider at the pin** (F.W3/W4, below) with honest anatomy. |
| **D-1 §0 provenance** ("No glass-ui version has ever shipped a `--slider-scrub-*` token") | Not addressed (folded D-1 to B-2 without the history) | **KILLED AND INVERTED** — v3.1.0 read them; `99a11083` (v3.2.0) deleted them; the uncommitted `^3.1.0→^4.0.0` bump crossed it | **KILLED — LC's inversion verified and adopted as the canonical account** | My run: `git show v3.1.0:src/components/ui/slider/Slider.vue` reads 13 distinct scrub names **including all five this file writes**; `git show 99a11083 -- src/` removes 13 scrub lines (2026-06-06, package 3.2.0). So the retint block was **correct at fourier HEAD's lockfile pin (3.1.0)** and was killed by the uncommitted bump — C-1 §1's account, not D's. Consequence: the defect row is unchanged (dead AT the audited tree), but the **cause taxonomy** changes from "consumer implemented a prose promise the producer never kept" to "an unlanded major bump silently severed a once-live contract" — which is what makes the F.W1 gate (a token-FAMILY sweep, not a variant-string sweep) the right acceptance shape. **Amendment relayed (E-3, banked files never rewritten)**: fr-BasisSelector B-2's "a declared feature that has NEVER painted" and fr-ConvergenceTimeline's "`--slider-scrub-track-height` never existed in glass-ui" are true at the installed pin and FALSE as history — any wave narrating this cure should carry LC's provenance. |
| Severity postures (L-§2 BLOCKER→? · D-5 MAJOR→? · C-13 INFO→?) | MAJOR / BLOCKER / MAJOR (as banked) | MAJOR / BLOCKER / — | **The registry decides**: M-N8 MAJOR · B-6 BLOCKER · fr-FunctionInput D-9 MAJOR | Identity guard both readers applied and this seat ratifies: a banked severity is neither under-booked (D-5 at MAJOR, C-13 at INFO) nor over-booked (L-§2 at BLOCKER) by a later corpus without new facts. No new facts were tendered. |

## Adjudicated defect roster

Dispositions are ADJUDICATED, not VERIFIED — X·F specs are forming; routings are agglomeration input (M-25). Line anchors are the fourier working tree at HEAD `cd26c653`; re-anchor before any cure.

- **R-1 = D-1/L-§1/C-1 — BLOCKER (as banked) — FOLD → fr-BasisSelector B-2 → F.W1** (gate = token-family sweep `grep -rn -- "--slider-scrub" web/src | wc -l` → 0; execution rides F.W3/W4). The five `--slider-scrub-*` declarations (`:143-149`) name a namespace nothing installed reads; the REQUIRED `color` prop (`:32`) is inert at all 8 callsites; every range paints `var(--primary)`. Census 23/7 (my run, = B-2's banked count). Cure per the banked R-7/K-1 correction: rename ONLY the range pair, DELETE the thumb pair, height via `size`/inline. **Provenance carried = LC's** (live ≤3.1.x, deleted at 3.2.0, the bump crossed it). Rider adjudicated here because BOTH readers silently dropped it: **D-14** (`--track-color` has no `var()` fallback; an empty `color` makes the `color-mix()` invalid) — TRUE at the bytes, unobservable while R-1 stands, booked as an **INFO cure-rider inside the B-2 repair** (the renamed declarations must land with fallbacks), not a row.
- **R-2 = D-3/C-2/C-4/L-§2 — MAJOR (as banked) — FOLD → fr-EquationPanel M-N8 (∪ fr-BasisSelector M-8 family; root = NumberField M-16/m-14) → F.W3/W4.** Clamp-per-keystroke against a one-way `:value` (`:40-49/:74/:78`, no `@change`/`@blur` — my grep 0): values unreachable by typing, `parseFloat("")→NaN→lo` refill, equal-emit no-op desync. Both worked traces re-verified at the real callsites (EquationPanel `:97-103` 20-not-12; ContourSettings toFixed rewrites). Kills inside: L-§2(a)'s second exhibit (K-9), D-3's blast radius (K-10).
- **R-3 = D-4 — MAJOR — NOVEL (registry sweep 0) → F.W3/W4** (+ **F.W0 rider**: extend the axe keystone set to the equations route — the only route where `subtitle` renders; gap verified by both readers). `.slider-subtitle` (`:111-115`: `0.75em` + `opacity: 0.7` over inherited `--muted-foreground`) fails WCAG 1.4.3 in BOTH arms. **DU's backdrop correction ADOPTED**: the two live sites (FunctionInput `:182/:215`) render inside `cartoon-card`, whose utility carries `background: var(--card)` (my read) — composites ≈2.85:1 light / **3.56:1 dark** (dark materially worse than D filed); LC independently reproduced D's vs-background 2.880:1 to three digits. Size 10.5px desktop / 11.8px mobile under the root fork (verified); no large-text exemption; the `:where(.glass-floating,.glass-overlay)` ladder lift does not reach `cartoon-card` (DU's refutation, adopted). Cure: drop `opacity`, express de-emphasis via token at full alpha. Sub-rider: **N-4** below.
- **R-4 = D-5 — BLOCKER (as banked; D's MAJOR under-book corrected) — FOLD → fr-ContourSettings B-6 → F.W3/W4.** Five tooltip-wrapped sliders (`:229/:242/:268/:281/:294`) merge `TooltipTrigger as-child` onto the non-focusable root `<div>` (`:65`); B-6 is strictly stronger (the coarse-pointer arm: ALL SIX tooltips unopenable on touch). Do not port `as-child`-on-a-div into any cure (C-24 warning, ratified there).
- **R-5 = D-6 (defect half) — MAJOR — NOVEL at this seam → GLASS-RELAY + F.W1 REBASELINE BANK.** `formatValue` (`:33/:58-60`) is visual-only; the announced value is reka's raw `aria-valuenow`; the installed 4.0.0 d.ts has no `valueText` — **and neither does v7.0.0** (my git grep → 0; the prop is v8.0.0, types.ts:35). So the humane-readout gap SURVIVES the named F.W1 target: the consumer has no clean hook at any adoptable pin ≤7.0.0. Live consequence real (ContourSettings `:282-290`: sighted "All", announced "0"). Disposition: producer ask via the standing glass-ui BH relay (owner edict — every glass-level change relays) requesting `valueText` at the adopted major, + a bank firing if F.W1's target moves to 8.0.0 (where `formatValue` maps 1:1). The uplift clause as filed is K-3.
- **R-6 = D-7/C-5/L-4 — MAJOR (wrapper-narrowing mechanism = the unbanked half; consumer consequence FOLDS → fr-ContourSettings M-2) → F.W3/W4.** 8 props/1 emit (`:25-38`) vs the producer's `SliderRootProps & {…}` + two emits: `disabled`/`size`/`orientation`/`keepDockOpen` unreachable, `valueCommit` destroyed (reka fires it per gesture — SliderRoot.js:118/:128, my read). The narrowing is precisely why M-2 is uncurable at the callsite. L-4's counterweight adopted: `class` fallthrough is WANTED (`FunctionInput:180 class="flex-1"`) — the cure is explicit passthrough + a re-emitted `valueCommit`, never blanket `inheritAttrs:false`. **F.W1 benefit rider survives at the named baseline**: `invalid?: boolean` IS declared at v7.0.0 (LC verified; D's :32 cite is the 8.0.0 line — anchor drift only).
- **R-7 = L-5 — MAJOR (extension; the storage write itself FOLDS → fr-EquationView L·M-3 = C·C-29) → F.W3/W4** (same repair unit as R-6 — the settle event is the cure). Per-accepted-step emit with `valueCommit` swallowed ⇒ three coalescing dialects (1000ms / 300ms / **none**); the live cost is EquationView `:161-172`'s PLAIN watch → synchronous `sessionStorage.setItem` per slider step off a min-1/max-100 track (my reads). The unbanked contribution = the per-step cadence attributed to the adapter. C-5's third table row is K-11.
- **R-8 = L-7 — MAJOR (as banked) — FOLD → fr-BasisSelector M-6 → F.W3/W4.** `:117-138` byte-identical to BasisSelector `:212-233` (both readers' zero-output diffs); chassis used by 2 of 8 slider-bearing components. LC's count correction adopted: `inline-number` = **13** hits (6+7, my grep), not L's 12.
- **R-9 = D-9/L-6/C-8 · D-10 — MINOR (as banked) — FOLD → fr-ContourSettings m-13 → F.W3/W4** (rides the R-2 NumberField unit). `:type` mutates with the value (`:61/:72`), `min`/`max`/`step` thrash (`:75-77`), the field cannot be cleared. D-9's spin-button clause is K-8 (statically suppressed in BOTH arms by `:127/:134-138` — the subject file's own CSS).
- **R-10 = D-11 — MINOR — NOVEL at this site (nearest banked = FMD-4, different file) → F.W3/W4.** No shrink discipline on the label span (`:67`, flex `min-width:auto`) against the fixed 2.75rem input (`:118`) inside ContourSettings' hard `1fr 1fr` grid (`:377-382`, sole `@media` = PRM). Threshold → UNPROVEN #5.
- **R-11 = D-12/L-8/C-11 — MINOR (as banked) — FOLD → fr-BasisSelector m-21 + fr-AnimationControls R-11/C-5 → F.W1 (budget) / F.W3 (sweep).** 16px is off the sm/md/lg ladder; `size` not forwarded (R-6); a bare rename loses at (0,3,0) vs (0,2,0), 0 `@layer` (K-2-ratified arithmetic). L-8's three-obstruction analysis ADOPTED into the B-2 cure clause (rename 1 of 5; thumb pair doubly dead; no `-hover` token exists — hover is a `:deep()` rule, not a token).
- **R-12 = D-15(a) — MINOR (as banked) — FOLD → fr-AppHeader FR-AH-2 (font-truth cluster) → F.W4 there.** `.fira-code` paints the metric-calibrated fallback: `./styles/fonts` is a separate subpath fourier never imports (export map + style.css:1-3 + main.ts, verified).
- **R-13 = D-15(b) — MINOR — NOVEL (nearest banked rows are font-FAMILY drift, not the feature register) → F.W3/W4.** The `fira-code` utility body is exactly `font-family` + `"liga","calt"` (my read) — no `tnum`/`font-variant-numeric` on a changing numeric readout whose house idiom is `.animated-digit`. Consumer-side cure (apply the tabular register at the readout); optional producer note rides the R-5 relay.
- **R-14 = D-16 — MINOR — NOVEL at this site → F.W3/W4** (+ F.W1 benefit rider: `controlLabelable`/`describedBy` at v7.0.0 labeled-field, DU-verified). `:87` binds `label` alone while the `<label>` at `:66-80` names the input with label+subtitle — two controls for one value under different accessible names, and the dropped subtitle is the discriminator at the two near-synonym FunctionInput sites.
- **R-15 = D-17/C-12 — INFO (as banked) — FOLD → fr-ConvergenceTimeline L·D-4.** `aria-label` lands on both the thumb and the role-less SliderRoot span (statically certain from the compiled chunk + reka's `inheritAttrs:false` + `mergeProps` — LC's derivation adopted). Announcement → UNPROVEN #4.
- **R-16 = C-3 — BLOCKER (as banked) — FOLD → fr-ContourSettings B-1 → F.W3/W4**, with the R6-8 extension (operation records must carry field DOMAINS) already banked there as the **F.W5–W8 rider**. "Min Area %" drives a [0,1] fraction from a 0–20/step-0.5 axis; anchors re-read whole (`:268-277`; min=0 — which is also what kills L-§2(a), K-9).
- **R-17 = C-9 — MINOR (new instance of the banked fr-AnimationControls M-7 family) → F.W3/W4.** `:128` raw `0.15s` + UA-default easing where the consumed system ships `--duration-fast`/`--ease-standard` (92 defs, both readers' counts agree). Note from the D-8 ruling: under PRM the substrate clamps this to 0.1s — the token cure is consistency, not accessibility.
- **R-18 = C-10 ∪ D-13/C-6 — MINOR → F.W1 rider (the value.js leg of the tri-package uplift) + F.W3/W4.** value.js consumed 0% at the app's only per-instance color seam (5 import lines / 4 files, all easing — reproduced by LC, = lane-frontend §9.5); the `color` prop carries two incompatible provenance arms (5× hex snapshot / 3× live `var()` — LC's enumeration; D's anchors off-by-one, noted). The hex-resolution arm (`cssVarToHex` no-`oklch` → `#888888`) FOLDS → fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER there, banked). Local content = the undocumented two-contract prop, visible the day R-1 is cured.
- **R-19 = C-13 — MAJOR (as banked; C's INFO under-book corrected) — FOLD → fr-FunctionInput D-9 = L-M2 → F.W3/W4.** `min === max === 2` reachable (`FunctionInput:216-217`, my read); the wrapper forwards the degenerate range verbatim. The NaN consequence is K-13 (reka's `linearScale` short-circuits — my read of utils.js:78-84); what remains is the banked zero-travel + model<min anatomy.
- **R-20 = C-14 — INFO → F.W0 (test substrate: the vitest seat + honest e2e).** The only e2e touching the component asserts `canvas.toBeVisible()` and its `input[type="range"]` fallback can never match (reka renders spans; the `VisuallyHiddenInput` is gated on `name`, never passed — LC's derivation). DU's rider adopted: Playwright `fill()` dispatches ONE input event, so even an assertive spec cannot reproduce R-2 — the gate must be a component test.
- **R-21 = C-15/L-10 ∪ DU-missed-2 — MINOR → F.W3/W4** (the docblock is corrected in the SAME edit as the R-1 cure — the ColorSpaceSelector addendum-3 stale-comment law). Provenance drift, now fully enumerated: `:18` pins v1.8.x against installed 4.0.0; `:140-142` asserts a projection that does not occur; and the working-tree rename sweep updated `:7/:12/:83` while leaving **three** `glass-scrubber` strings alive at `:3`, `:16`, `:140` — `:3`/`:16` named by NO axis (DU's missed-find, admitted; DU's "four stale variant references" corrected to three, and both readers' "exactly three hunks" corrected to 3 changed lines in 2 hunks — immaterial). The sharpest witness that lane-frontend §5's "pure rename sweep" was mechanically incomplete INSIDE one docblock.
- **R-22 = L-11/C-7 — MINOR (as banked) — FOLD → fr-BasisSelector m-13.** Typed `number[] | undefined`, setter derefs `arr[0]`; runtime-unreachable (PROVEN — K-12 discharged C-7's cap: the per-component reka layout exists and `nextValues` is always non-empty, my reads).
- **R-23 = the residual band (L-12 · L-13 · L-14 · L-15 → FOLD fr-ContourSettings m-11 · L-16 · L-§R · D-18 · D-19/L-9) — INFO → F.W3/W4 riders.** All re-verified by both readers; L-12 (no step snap on typing) is the enabling condition of N-1; L-§R's clean null result (0 `v-for`) + its scope-law extension (the producer's compiled `renderList` is invisible to a `web/src`-scoped census) carries to **F.W4** as census law.
- **N-1 (LC missed-find 1) — MINOR — NOVEL (registry sweep clean; distinct from fr-EquationView B-2's RANGE arm) → F.W3/W4** (one token in the R-2 edit: snap in `clamp` per reka's own `updateValues` idiom) **+ E-3 amendment relay to fr-EquationPanel**: the TYPE arm of the seam. `parseFloat` with no step snap (`:44-49`, L-12) lets a pasted `5.5` through `clamp(5.5,2,20)` into `budget`; EquationPanel's 300ms `watchDebounced` (`:61-65`, my read) auto-fires `POST /simplify`; `budget: int` has **no `mode="before"` coercer** (equations.py `:23/:40`, `model_config` grep 0 — my reads) ⇒ pydantic-v2 rejects the fractional float ⇒ 422 surfaced as a bare `e.message`. Same route for `n_harmonics` on the next compute. The banked M-N8 superlative rationale ("structurally incapable of emitting a 422") is FALSE on this arm — amendment recorded here, the banked file never rewritten. Live 422 witness → UNPROVEN #9.
- **N-2 (LC missed-find 2) — INFO — NOVEL → F.W3/W4 rider** (one-line hardening in the same edit). The finite guard is total over the parsed VALUE only: `clamp(v, lo, NaN) → NaN` (`:40-42` tests `v` alone — my read), and the one computed-bounds callsite (`FunctionInput:217`, `??` guards null, not NaN) is the aperture. Both feeders are ints today — INFO, no live claim. **Superlative S-2 re-worded accordingly** (below); any cure carrying the barrier as "total" inherits the hole.
- **N-3 (DU missed-find 1) — MAJOR — scope-extension of banked fr-BasisSelector M-3 (the banked row covers the CLONE at 2 callsites; the chassis carries 8) → F.W3/W4** (one repair with M-3). `:126` `outline: none`; the ENTIRE focus feedback is `:130-133`, one selector list shared with `:hover` — UA indicator deleted, `:focus` not `:focus-visible`, focus indistinguishable from hover, hairline ≈1.93:1 (DU's token arithmetic, consistent with M-3's banked "sub-2:1" and fr-ContourSettings M-3's 2.50:1 grading precedent) — on the component's only precise-entry affordance. Painted value → UNPROVEN #10.
- **N-4 (DU missed-find 3) — MINOR — NOVEL clause (the root-fork inversion itself is banked repo-wide) → F.W3/W4** (rides R-3's cure). `0.75em` of `text-sm` mints 0.65625rem — 12.5% BELOW the system's smallest rung (`--text-xs: 0.75rem`, my read) — and the root fork inverts it (10.5px desktop < 11.8px mobile). This is also why R-3 is unrecoverable by size or weight alone.

## Killed-claims register

| id | claim | refutation (this seat's re-derivation) |
|---|---|---|
| **K-1 · D-2 (BLOCKER)** | `variant="standard"` definition-absent at 7.0.0; hard vue-tsc break ×9; census §5 must add the row | KILLED at the named baseline — `git show v7.0.0:src/components/slider/types.ts:5` = `"standard" \| "spectrum"`; the `"scrubber"` rename is **v8.0.0** (types.ts:10, the exact anchor D cites); `slider/styles.css` does not exist at v7.0.0 (`git ls-tree` → 3 files); producer HEAD = 8.0.0 — D read the working tree and labelled it 7.0.0. At the census's named F.W1 target the break DOES NOT EXIST; D's §6.2 prescription would inject a false row into a ratified table. CORRECTED FORM, banked as a **rebaseline bank**: real against 8.0.0/HEAD at 9 live sites / 8 files (D's 14/9/7 counts are exact and survive); re-book if and only if F.W1's target moves. |
| **K-2 · D-8 (MAJOR)** | PRM ungated on both sides of the seam | KILLED — ruled above (blanket reset on the critical path; `border-color` preserved-and-clamped; transform snapped). LC's confirmation REFUTED; recorded as a citation-inheritance event per the K6 precedent. |
| **K-3 · D-6/D-8/D-12 uplift clauses** | "7.0.0 adds `valueText`… three PRM blocks, forced-colors, coarse floor in slider/styles.css" | KILLED as 7.0.0 facts — all v8.0.0 (my git runs: `valueText` 0 hits at v7.0.0; v7.0.0 Slider.vue has 2 PRM blocks, 0 forced-colors, 0 coarse; styles.css born 2026-08-08). Booking these as F.W1 gains would over-credit the uplift by a major. SURVIVES at v7.0.0, verified: `invalid` (R-6 rider), labeled-field module (K-7's rider). |
| **K-4 · D-1 §0 provenance** | "No glass-ui version has ever shipped a `--slider-scrub-*` token" | KILLED AND INVERTED — ruled above (v3.1.0 read all five; deleted at `99a11083`/3.2.0; the uncommitted bump crossed it; D's own falsifier is satisfied by every release ≤3.1.x). C-1's provenance is the canonical account. |
| **K-5 · D-1 specificity sub-claim** | "(0,2,0) exactly tied … bundler emission order decides" | KILLED — `.glass-slider[data-size=md][data-v-534634a7]` = **(0,3,0)**; producer wins deterministically; 0 `@layer`. Banked twice (fr-BasisSelector m-21, fr-ConvergenceTimeline K-2). Second-order (LC, adopted): D's "must not go into the scoped block" is over-broad — `--slider-range-bg` has NO competing producer declaration and renames fine at (0,2,0); L-8's three-way cure is the sound one. |
| **K-6 · C-1's blast-radius count** | "22 declarations / 6 files" | KILLED by enumeration — 23/7 (my census, matching L's table and the banked B-2/K-13 corrections). Third fleet correction of the same undercount class. |
| **K-7 · D §6.3 framing** | "LabeledSlider is a 7.0.0 gain; single adoption discharges D-4/5/6/7/8/16" | KILLED AS FRAMED — ruled above (ships at pin per FMD-14; REQUIRED `tooltip`, no description/formatValue/color per my d.ts read — the LC-3 not-drop-in precedent; D-8 dead; D-4's subtitle seatless). SURVIVES: an at-pin evaluate-retirement rider (F.W3/W4). |
| **K-8 · D-9 spin-button clause (+ C-8's matching UNPROVEN)** | "the spin buttons vanish at the type flip" | KILLED statically by the subject's own CSS — `:134-138` suppresses webkit spin buttons and `:127` the moz arm in BOTH modes; nothing can vanish, and no probe should be spent (SS-13 work order annotated). Surviving clauses carried under R-9. |
| **K-9 · L-§2(a) second exhibit** | "ContourSettings.vue:269 (Display terms, min=2) has it too" | KILLED — `:269-277` is "Min Area %", `min=0` (my read); the min-clamp route cannot fire there. The class survives at its first exhibit (EquationPanel `:97-103`) and at FunctionInput `:213`. |
| **K-10 · D-3 blast radius** | "6 of the 8 call sites pass a fractional formatValue … total within ContourSettings" | KILLED — six asserted, five listed, **four** fractional (my census: toFixed(2)/toFixed(1)/toFixed(1)/toFixed(2); the fifth is the 'All' formatter, D's own D-9 exhibit). Integer sites fail by the K-9-surviving leading-digit route, so "total within ContourSettings" is also wrong. |
| **K-11 · C-5's third debounce row** | "EquationView.vue:180 (200 ms)" coalesces the slider path | KILLED — the 200ms `watchDebounced` watches `[notation, budget] → doSimplify` (render-refresh); the watcher the slider emits actually drive is the PLAIN watch at `:161-172` → synchronous storage write (my read). C's table would let a cure wave close the file believing all three consumers coalesced; L-5's row is the one carried (R-7). |
| **K-12 · C-7's UNPROVEN cap** | "reka's emit declaration not reachable in the installed layout" | KILLED — reka-ui 2.9.10 ships `dist/Slider/SliderRoot.js` per-component (my ls); the only model write is `nextValues`, always non-empty, `passive: false` here. Pure type-contract defect; L-11's MINOR resolution ratified (R-22). |
| **K-13 · C-13's consequence** | reka's position math "divides by max − min" → possible NaN | KILLED — `utils.js:78-84` `linearScale` short-circuits the degenerate case BEFORE division (my read); `updateValues` divides by `step`, not the span. Reachability survives into R-19 at the banked severity. |

Seat corrections to the READERS (recorded, immaterial): both repeated "exactly three hunks" (actual: 3 changed lines / 2 hunks — `:7`/`:12` merge under default context); DU's "FOUR stale variant references" (actual: three — `:3/:16/:140`); L's `inline-number` 12 (actual: 13, LC caught it).

## Superlatives (provenance = axis mints ∪ reader re-derivations ∪ this seat's checks)

- **S-1 · The scalar↔array bridge** (`:51-56`) — writable computed, no lag frame, no echo loop, clamps inbound; genuine controlled-mode assumption verified against reka's `useVModel` passive:false. Minted by all three axes, re-derived by both readers, already a fleet superlative (fr-EquationView S-13). Its in-tree counter-example (`parseInt(...) || 1`, 0→1) is banked beside it (fr-BasisSelector M-8).
- **S-2 · The finite guard before the bound** (`:40-42`) — **RE-WORDED per N-2**: total over the parsed value, *conditional on finite bounds*; `""`/`"Al"`/`"Infinity"` all collapse to `lo` instead of poisoning the Canvas2D consumers (CENSUS §3a). The barrier's PLACEMENT is R-2's defect and its TYPE blindness is N-1's — the repair keeps the barrier, moves the commit, snaps the step, and guards the bounds.
- **S-3 · Zero teardown surface** — 0 listeners/timers/observers/lifecycle hooks (both readers' greps); notable against a child that carries a real event surface (`useDockHold`) and tears it down correctly. The adapter adds no risk to a correct primitive.
- **S-4 · Contract-v2-clean import posture** (`:23`) — bare published subpath, no dist path, no alias, 0 direct `reka-ui` imports tree-wide; lane-frontend §1/§3's "cleanest consumer posture" survives the hostile pass.
- **S-5 · The A.W3.b retirement record** (`:10-16`) — names the retired prop, disposition letter, ledger, verification command, and discharged flag; re-run by both readers (3 importers / 8 callsites / 0 `variant`). **L-18 counterweight, sharpened by R-21**: the same block's `:16` still names the retired `glass-scrubber`, `:18` pins v1.8.x, `:140-142` asserts a dead mechanism — the file is simultaneously the best argument for provenance comments and the exact mechanism by which a whole-namespace defect survived a major hop and a census sweep.
- **S-6 · The dock-token retirement is real in the shipped artifact** (`:18-20`) — `useDockHold` inlined in the 4.0.0 bundle drives `keepOpen()/release()`; 0 live string-key injects (the 2 grep hits are this comment and its GlassTimeline twin — DU's correction to C's S-1, adopted). CR-2's regression class actually closed.
- **S-7 · Number-only attributes gated off in text mode** (`:75-77`) — hygiene the sibling hand-roll skips (BasisSelector hardcodes them ungated); correct hygiene downstream of R-9's bad premise — preserve it verbatim when the type-flip is cured.
- **S-8 · Correct Tailwind v4 scoped-`@apply` idiom** (`:95/:106`), house-consistent ×35 files; C's caveat carried (bare `"tailwindcss"` reference blinds `@apply` to the project `@theme` fork — a trap for any future `@apply font-sans`).
- **S-9 · (DU's mint, ratified by this seat's own chain-trace — and it is exactly what D-8 mistook for a defect)** The one motion this component authors (`:128` border-color) lands precisely on the substrate's deliberately-preserved PRM list, while the spatial press-spring beneath it is snapped by the same block (`a11y-overrides.css:6-31`, "accessibility is absolute"). The wrapper inherits correct reduced-motion behaviour for free; the credit belongs to the producer's blanket-with-a-carve design, and the fleet's K6 precedent now has a positive-space twin.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. Pixel proof two SliderControls with different `color` paint identically — confirmation only; the static derivation is triply banked (R-1).
2. Caret/selection behaviour after Vue rewrites `el.value` mid-keystroke (R-2); the value rewrite is source-derived and IS the defect.
3. UA behaviour on a mid-edit `type="number" ⇄ "text"` mutation (R-9) — **minus the spin-button half, statically refuted (K-8): do not spend a probe on it.**
4. Screen-reader announcement of the doubled `aria-label` (R-15); the double application is byte-certain.
5. R-10's clip threshold — the viewport width at which the label row overflows the half-width grid cell.
6. R-7's magnitude — a performance trace sizing the per-drag synchronous storage train (existence static and banked).
7. R-12's painted face — "Fira Code" vs the metric-calibrated fallback (cause statically proven).
8. R-19's felt UX at the zero-travel track (reka's math now proven safe — low priority).
9. N-1's live 422 — paste `5.5` into Terms, observe the bare `e.message` surface after the 300ms settle (the full chain is statically derived; the witness is cheap).
10. N-3/R-3's painted composites over the live glass — the token arithmetic (1.93:1 hairline; 2.85/3.56 subtitle) is decidable; the rendered pixels are SS-13's.

## Addendum (E-3 — appended, nothing rewritten)

1. **Amendment relays** (recorded here; banked files immutable): (a) → fr-BasisSelector B-2 + fr-ConvergenceTimeline L·D-6-cell: the scrub namespace was LIVE at every release ≤3.1.x and at fourier HEAD's lockfile pin — "never painted/never existed" holds only at the installed 4.0.0 (K-4's inversion; carry LC's provenance in any cure narrative). (b) → fr-EquationPanel M-N8's qualified superlative: "structurally incapable of emitting a 422" is false on the TYPE arm (N-1); the guarded-server-seam half survives only for RANGE.
2. **The baseline law, re-proven twice in one corpus** (K-1, K-3): a version claim cited from a working tree is not a claim about the tag; every uplift gain/break row must carry a `git show <tag>:` receipt. D's challenge committed the error in both directions (break-side D-2, improve-side D-6/D-8/D-12) — over- and under-crediting the same uplift by one major.
3. **Citation-inheritance, third recorded event** (K-2): LC confirmed D-8 against the scoped sheet after the fleet had killed the identical claim in writing (fr-AdminFlaggedPanel K6, with the ColorSpaceSelector DISSENT-1 and pt-leaf events prior). A reader cell that repeats a challenged claim without opening the token/utility cascade is itself evidence — the registry sweep must include the KILL registers, not only the confirmed rosters.
4. **Corpus-method note (DU missed-find 4, admitted as method)**: no axis swept the adjudicated registry; ~13 of ~50 corpus rows re-book banked facts, three at a LOWER severity than ratified, two re-committing corrected errors. Net-novel yield of 3×34KB ≈ six rows. The SS-3/SS-4 spec authors should require a REGISTRY-FIRST sweep declaration in every future challenge header.
5. **Scope-law carry (L-§R → F.W4)**: a `web/src`-scoped instance census reports zero interactive slider nodes for a surface that has 14+ — derive component-boundary loops from first-party `@mkbabb/*` d.ts + compiled render functions (closed, versioned denominator).

## Closing verdict

**ADJUDICATED: KEEP-AND-REPAIR — the adapter shape is right and its editing, theming, and announcement layers are each broken in ways the registry had already mostly banked.** 28 rows confirmed-or-folded (3 BLOCKERs, every one a banked fold — B-2 → F.W1, B-6 + B-1 → F.W3/W4); the genuinely novel surviving content is R-3 (subtitle contrast, `--card`-corrected), R-5 (no `aria-valuetext` at ANY pin ≤7.0.0 — GLASS-RELAY), R-6's narrowing mechanism, R-7's cadence extension, N-1 (the fractional-422 TYPE arm) and N-3 (the focus-indicator scope extension); 13 corpus claims KILLED, including one whole BLOCKER (D-2) and one MAJOR (D-8) that would have mis-directed the F.W1 census in opposite directions; one reader-verdict conflict RULED with the fleet's own K6 precedent; zero rows re-booked; every routing is agglomeration input for the forming X·F specs (M-25), not a stamped wave.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3.
