# fr-GalleryView — adjudicated apotheosis (M-23/L-18 fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-25 · **Registry**: value.js branch `tranche-u` · **Subject tree**: fourier-analysis `HEAD cd26c653` (verified by this seat's own `git rev-parse`; matches the fr-GalleryFeaturedCarousel subject attestation).
**Corpus**: `docs/tranches/V/megatranche/audit/fourier-components/GalleryView/` — challenge-D-design.md (612) · challenge-L-library.md (451) · challenge-C-consumption.md (235) — over `web/src/components/visualization/GalleryView.vue` (442 lines) and its store/API/server seams.
**Readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted. The two returns contain a **direct mechanism contradiction** (D-23 / the `finally` guarantee) that only a source read can settle — this seat settled it (R-1/R-2). Suspicious agreements were spot-re-measured, not trusted.
**This seat's own instruments (all static, zero browser probes — probe parsimony; product source READ-ONLY)**: whole-file read of GalleryView.vue (442 lines — every load-bearing anchor line-exact: the `performBatchGallery` control flow :171-200, the complete `selectedHashes` writer set :150/:154-163/:192/:204-205, the `onMounted` serialisation :83-91, the three non-immediate watchers :95-98/:101-108/:111-114, the two exhaustive content guards :266/:290, the v-else-less DialogTitle/Description/affirmative-Button chains :404-437, the Cancel at :429); whole-file read of `stores/gallery.ts` (294 lines — `loading` init `false` :31, `resetAndFetch` :84-103 with the `isAbortError`-guarded catch and the `finally` flag-clear, the bare `if (!token) return` pair :139-140/:151-152, `like()` :189-199 `const liked = true`, `recordView` :201-212, `publishDraft` :237-263 with the :241 slug rebind, `visibilityFilter` :36/:55/:270); `lib/api.ts` :50-70 (`abortable` aborts the prior controller per key) / :120-200 (`coreFetch` auth+idempotency+ApiProblem) / :395-413 (`listVisualizations` — exactly four `URLSearchParams.set` calls, hardcoded key `"listVisualizations"`); `GallerySearchBar.vue` :40-153 (the bare `<input>` :48-54; both `<SelectTrigger>`s contain only `<SelectValue />` :86-88/:101-103; `grep 'aria-|label'` → `aria-pressed` :70/:120 only); `api/routers/visualizations.py` :250-275 (**`find_one` precedes the `$inc`; `_public_doc(doc)` serialises the PRE-increment document** — the MISS-LC-1 decider); `api/routers/admin.py` :405-450 (batch `delete` → `soft_delete` per slug; `unfeature` → unconditional `{"$set": {"tier": "normal"}}` `update_many`; **every branch falls to the single `_json({"ok": True, "affected": affected})`** — no `errors` emission); `web/index.html` :21-31 (the pre-paint theme IIFE — the K5 decider); `router/index.ts` :50-95 (`/gallery` lazy, no children/props/query contract; `/v/:visualizationSlug` declared); repo greps re-run fresh: `visibilityFilter` → **exactly 3 hits, all inside stores/gallery.ts** (zero writers), `gallery.softDelete|gallery.restore` → **0 callers**; original challenge cells re-read at the bytes: challenge-D-design.md:480 (D-23 as filed) and challenge-L-library.md:420 (S-1 as filed); banked-registry reads: fr-GalleryCard R-4/K-6/K-8/K-9/K-13/G-LC2, fr-GalleryFeaturedCarousel R-5/K2/K4/FR-GFC-1, fr-GalleryInfiniteGrid FR-GIG-2, fr-GalleryAdminBanner GAB-20, fr-PaperArticleWindow PAW-8, fr-AdminFlaggedPanel FR-AFP-9/FR-AFP-35, fr-AdminUserList FR-AUL-15. Contrast figures are adopted on **two independent full reproductions** (both readers, separate instruments, gamma-space compositing discriminator) — not re-run a third time by this seat.

## Scoreboard

**37 booked rows: 1 BLOCKER (at banked severity) · 13 MAJOR · 20 MINOR · 3 INFO — plus 26 folds-by-reference (identity guard; never re-booked). 12 kill-register rows (10 sustained from the readers after re-derivation · 1 reader-confirmation killed by this seat at the bytes · 1 hedge-kill cluster). 9 missed finds adopted after re-proof (4 reader-DU · 5 reader-LC). One grade demoted against both readers (L-5, R-5).**

Heaviest survivors: **FR-GV-1** (publish is non-idempotent against a dead-by-construction drafts filter — duplicate public rows, banked BLOCKER B-2), the **false-empty pair FR-GV-10/FR-GV-11** (the primary public route's FIRST PAINT is the cold-empty "No visualizations yet." CTA for every visitor, and the shared-abort-key flag orphan re-paints the same false statement MID-FETCH), **FR-GV-12** (the view counter rides an unsafe GET that cannot observe its own side effect and silently inflates on every ETag cache-miss), and **FR-GV-13** (the entire `?owner=me` capability is producerless — a documented, server-supported contract with no control).

The corpus-level event of this adjudication: **reader-LC confirmed D-23 and re-asserted the L-axis's `finally` guarantee against control flow that refutes both** — the token-missing `return` at :178 precedes the `try` at :180, so the `finally` at :197-199 cannot run on that path. Reader-DU re-derived; reader-LC inherited the challenge's false cell. Ruled at R-1/R-2; recorded in DISSENT as the fleet's next citation-inheritance instance.

## Ruled disagreements (the seat's actual work)

| # | question | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **D-23's mechanism** (dialog exits blank on the token-missing path) | KILLED — the `return` at :178 precedes the `try`; the `finally` never runs on that path | CONFIRMED — "the token-missing early return at :178 still runs the finally at :197-199, so pendingBatch nulls synchronously" | **DU wins — D-23 as filed is DEAD (K1).** | This seat's read of :171-200 in order: `batchDialogOpen=false` (:173) → `if (!pending) return` (:174) → `if (!token) { toast; return; }` (:176-179) → `try {` (:180) … `finally { pendingBatch.value = null }` (:197-199). A `finally` binds to its `try`; a function that returns before entering the block cannot trigger it — LC's confirmation asserts false JavaScript semantics, and the original filing (challenge-D-design.md:480, re-read by this seat) carries the same false cell ("the finally runs *synchronously* in the same tick"). `pendingBatch` stays POPULATED on that path; the dialog exits with title and description intact. The residue (catch-path timing, :195-199) is real and books as MISS-DU-3 (FR-GV-30). |
| R-2 | **The `finally`-guarantee property** (L·S-1 property 5; C·S-2; LC superlative 2 — "an error cannot strand the dialog state") | KILLED — token-missing error strands `pendingBatch`; only the next `askBatchGallery` (:167) clears it | Re-asserted verbatim as a superlative ("`:198`'s finally cannot strand pendingBatch") | **DU wins (K2). The superlative survives at 3-of-5, corrected.** | Same control flow as R-1. Of S-1's five claimed properties: the :172 pre-await snapshot, the :173 close-before-I/O, and the :175-179 explicit token toast HOLD; the :189-191 per-item error fan-out is DEAD CODE (admin.py:412-450 read whole by this seat — every branch returns the single `_json({"ok": True, "affected"})`; LC's own C·m-7 proves it against itself); and the `finally` guarantee is FALSE (the token-missing error strands `pendingBatch`). L·S-1's own falsifier ("find a path where `pendingBatch` survives") fires on the path its author graded as a correct property three lines up. |
| R-3 | **M-8 / C·M-8 first clause** ("no featured entry can ever enter `selectedHashes`") + the intra-registry conflict LC surfaced | KILLED via banked FR-GFC K2 | KILLED via banked FR-GFC K2, flagging that fr-GalleryCard **G-LC2** states the same killed absolute | **Kill SUSTAINED (K3); FR-GFC K2 GOVERNS the registry.** | This seat's own writer-set re-derivation from the whole-file read: `selectedHashes` writers = :150 (init), :154-158 (toggle), :161-163 (clear); `clearGallerySelection` called at :192/:204/:205 only. `handleSetTier` (:132-135) never prunes — tick a normal card, click its Crown, the now-featured slug remains selected and Unfeature acts on a genuinely featured entry. FR-GFC K2's explicit derivation governs; **fr-GalleryCard G-LC2's absolute is hereby ruled non-citable for this claim** (dated evidence stands unedited; this record is the correction rider). Surviving limb booked FR-GV-9. |
| R-4 | **D-2 / D-4 / L-1 severities** | MAJOR each (BLOCKER filings preserved as dissent) | BLOCKER each | **MAJOR each; all BLOCKER filings preserved as dissent.** | Family precedent governs: unnamed-but-operable controls ruled MAJOR at fr-GalleryFeaturedCarousel R-5 and fr-GalleryCard K-6 (D-2 is now the FOURTH axis to inflate this class); the load-path error posture is banked MAJOR at FR-GIG-2 (D-4 is its view-organ sibling); L-1's identity is banked at FR-GFC-1/FR-GFC-4 (MAJOR, blocker-weight flagged) and **the banked severity governs at a fold** (fr-GalleryCard R-4's law). L-2 stays BLOCKER because its banked identity (fr-GalleryDraftsSection B-2) is BLOCKER — the same law in the other direction. |
| R-5 | **L-5's grade** (un-cleared search timer) | MAJOR | MAJOR — while simultaneously KILLING the only user-visible consequence (K4: every mount re-runs `resetAndFetch`; the shared abort key retires any orphan) | **DEMOTED to MINOR by this seat, against both readers.** | LC's K4 is correct and this seat re-verified it (:83-91 unconditional refetch on every mount; api.ts abortable + gallery.ts:99 swallow) — there is no reachable state in which a user observes the orphan's output. A MAJOR resting on a consequence the same return kills is a grade inconsistency, not a grade. The mechanism (raw `setTimeout`, zero teardown imports, wasted GET into a singleton) is real, is the banked FR-AUL-15 idiom family, and rides that row's F.W4 cure — at MINOR. Both readers' MAJOR preserved in DISSENT; note the banked sibling carries an aggravator this site lacks (FR-AUL-15's orphan runs `getAdminToken()!` on null). |
| R-6 | **D-7 / D-15 / D-16 grades** | MINOR residue · MINOR · listed UNPROVEN | MAJOR · MAJOR · MAJOR | **MINOR · MINOR · MINOR-provisional.** | D-7: three of its five sites are already banked at MAJOR (GCM-16, FR-GFC-7, GAB-28) — the unbanked increment (the two empty states, the batch toolbar, the whole-route outline) is the residue and grades MINOR; re-grading banked mass is double-counting. D-15: a gutter-authoring census plus one omission — the adjacent banked identity (fr-GalleryDraftsSection m-4) is MINOR. D-16: the z-vocabulary trisection (raw `z-20` vs `--z-bar: 30` vs `z-5`) and the shim dependence are static-certain, but the load-bearing exposure question (does `@utility` registration cross the `@import` boundary in a consumer build at the F.W1 tag?) is NEEDS-BUILD — a MAJOR cannot rest on an unproven emission; LC's own favourable correction (cartoon-surface survives the uplift) cuts the same direction. LC's three MAJORs preserved as dissent. |
| R-7 | **The Cancel-button "opposite directions" cell** (LC's D-21/22/23 residue: "Cancel closes WITHOUT nulling pendingBatch — the two halves of one state machine fail in opposite directions") | not filed | filed as additive residue | **RULED A NON-DEFECT — the framing is inverted.** | Given R-1, leaving `pendingBatch` populated through close is the EXIT-SAFE behaviour: the dialog animates out with its content intact, and the next `askBatchGallery` (:167) overwrites before any reuse. The hazard in this state machine is the confirm path's EAGER nulling (:198) inside the animation window — Cancel is the half that gets it right. The correct one-line cure (a `v-else` fallback, or null-after-exit) is owned by FR-GV-30; a wave "fixing" Cancel to null eagerly would manufacture the blank-exit state on the common path. Preserved as observation. |

## Adjudicated roster

Dispositions are **ADJUDICATED, not VERIFIED** — routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave. Line anchors are fourier `HEAD cd26c653`; re-resolve before executing any cure. F.W* targets follow the census lane taxonomy (F.W0 substrate · F.W1 tri-package uplift · F.W3/W4 shadow retirement + frontend audit · F.W5-W8 CRUD/provenance union · F.W9/W10 gates).

### BLOCKER

- **FR-GV-1 = L-2 / C·C-3 — BLOCKER at the banked severity · FOLDS BY REFERENCE to fr-GalleryDraftsSection B-2 (split F.W3/W4 contract + F.W5-W8 server dedupe).** `unpublishedDrafts` is dead by construction (`savedSnapshots: []` written once, workspace.ts:102; the :78 predicate invariantly true), the draft never leaves the tab, and publish is non-idempotent (api.ts:372-380 sends no `Idempotency-Key` though coreFetch supports it — this seat's :138-140 read; PLAIN `content_hash` index vs flags' deliberate `unique=True`). **K12 strengthens**: both axes' UNPROVEN-NEEDS-LIVE hedges on the duplicate-row outcome are DEAD — B-2 closes it statically (idempotency.py no-header passthrough + the plain index); SS-13 spends no probe here. Do not re-book.

### MAJOR

- **FR-GV-2 = D-2 — MAJOR (two BLOCKER filings in dissent) → F.W3/W4.** The search field has no programmatic name — GallerySearchBar.vue:48-54 is a bare `<input type="text">`; this seat's own grep over the 223-line file: the only ARIA anywhere is `aria-pressed` at :70/:120. Its only visual label is the α=0.50 placeholder at **2.04:1** (twice independently reproduced from token bytes; gamma-space compositing is the discriminator both readers honoured). One repair unit with FR-GV-33 (the same file's two unnamed Selects).
- **FR-GV-3 = D-4 — MAJOR (BLOCKER filing in dissent) → F.W3/W4.** A failed gallery fetch renders as "No visualizations yet." — the store has no error ref (this seat's read of the :265-292 return object), the :266/:290 guards are exhaustive and mutually exclusive, so every terminal zero-entry state funnels into the cold-empty CTA whose only action navigates AWAY (:282); an aborted list request produces the empty state with **no toast at all** (:99 `isAbortError` swallow). Sibling organ of banked FR-GIG-2 (grid error posture); one repair unit with FR-GV-10/FR-GV-11 (a third render branch / `hasMore=false`-on-catch / error affordance).
- **FR-GV-4 = D-9 / L-6 — MAJOR → F.W3/W4.** One un-migrated native `confirm()` at :137-141 (the higher-frequency single-entry delete) in a file that ships the fully-styled destructive `<Dialog>` at :400-440 and a directory whose both async siblings carry the explicit retirement comment. **Cure law (both readers, ratified)**: `@mkbabb/glass-ui/confirm-dialog` is TRUE at the installed 4.0.0 and ABSENT at v7.0.0/v8.0.0 — the cure is the in-file Dialog pattern, and the F.W1 spec must restate its target tag before this ledger executes (DISSENT 4).
- **FR-GV-5 = L-4 / C·M-4 — MAJOR (BLOCKER filing in dissent) → F.W3/W4.** Leaving admin mode while on an admin tab renders the tab strip and nothing else: the :204 `adminMode` watcher clears the selection but not the tab, while the sibling :101-108 `isLoggedIn` watcher DOES reset (:104) — the author fixed one of two instances of the same bug. All three admin bodies are double-gated (:375/:380/:385). Composes with FR-GV-32 (no URL recovery path). One-line cure mirrors :104.
- **FR-GV-6 = L-9 — MAJOR → F.W3/W4.** `setTier`/`deleteEntry` are bare `if (!token) return;` (gallery.ts:139-140/:151-152, this seat's read) — no toast, no throw — after the user has already confirmed a destructive action; `handleDelete` (:137-141) then nulls `selectedEntry`, closing the modal over an undeleted row. The correct posture is 35 lines away in the same component (:175-179). Reachable via the M-9 cross-tab desync (in-memory `adminMode` vs localStorage token).
- **FR-GV-7 = M-9 / C·M-9 — MAJOR → F.W3/W4 + F.W5-W8 rider (token-transport contract).** An admin bearer token is accepted from the URL query string (:83-88), persisted to localStorage with no expiry, and sent as `Authorization: Bearer`; `adminMode` never rehydrates from `isAdminAuthenticated` (zero readers in this file — both readers' grep, spot-trusted against this seat's whole-file read), while `performBatchGallery` (:175) authorises directly off the stored token — credential live, UI says inactive. `router.replace({query:{}})` scrubs the bar after the fact and unwinds nothing else. Composes with FR-GV-22.
- **FR-GV-8 = M-6 / L-12 (operation-count arm) — MAJOR → F.W5-W8.** Seven of thirteen `/api/visualizations` operations (remix/publish/unpublish/forks/provenance/diff/versions) have zero client function; three independent prior counts corroborate. Carries the banked K-1 caveat (a `@router.` grep is blind to prefixed routers — gallery.py's `@gallery_router.get("/cursor")` must be read against 13/44, not zero). The identity/provenance display arms FOLD to banked fr-GalleryCard D-7, GCM-25 (`/v/` non-functional — sharper than C·M-5 filed), and GCM-3 (`selectedEntry` snapshot) — do not re-book.
- **FR-GV-9 = C·M-8 (second clause) — MAJOR → F.W5-W8.** Batch Unfeature sets `{"$set": {"tier": "normal"}}` unconditionally via `update_many` (admin.py:435-440, this seat's read) — unfeaturing a SAVED-tier entry silently erases the saved tier while the :423-425 dialog copy is honest about the mechanism and the :326-333 button label is not. First clause killed at R-3/K3. Rides banked FR-GFC-20's stale-slug row.
- **FR-GV-10 = MISS-DU-1 (adopted after re-proof) — MAJOR → F.W3/W4.** The `/gallery` route's FIRST PAINT is the cold-empty CTA — for every user, on every cold load, gated on an IndexedDB read. Re-proven by this seat's own four reads: `loading` initialises **false** (gallery.ts:31); `onMounted` awaits `workspace.refreshDrafts()` (an IndexedDB `getAll`) BEFORE `resetAndFetch()` (:89-90); none of the three watchers is `immediate` (:95/:101/:111); the :266/:290 guards then select the empty state until :90 executes. **Amends banked FR-GIG-4** ("the first paint is the grid" — true only post-:90). Cure is one line (`Promise.all`) or the FR-GIG-4 skeleton gated on `loading && !entries.length`. Duration → SS-13.
- **FR-GV-11 = MISS-DU-2 (adopted after re-proof) — MAJOR → F.W3/W4.** The shared abort key plus an orphaned `finally` makes `gallery.loading` read FALSE while a list fetch is in flight — the cold CTA paints MID-FETCH over a live request, with no toast. Re-proven at all three hops by this seat: `listVisualizations` hardcodes `"listVisualizations"` for both callers (api.ts:409-412); `abortable(key)` aborts the PRIOR controller (:54-59); aborted call A's catch is silenced (:99) and A's `finally` (:101) clears the flag call B owns while B holds `entries=[]` (:85). Reachable from the un-debounced :111-114 filter watcher (two clicks in one round trip) or a `setTier`/`deleteEntry` refetch landing on the 300ms search debounce. The banked GAB-20 mechanism class, unbanked on the list path; FR-GV-26 books the mechanism census. Cure: generation token — one repair unit with FR-GV-3/FR-GV-10.
- **FR-GV-12 = MISS-LC-1 (adopted after re-proof) — MAJOR → F.W5-W8.** `GET /api/visualizations/{slug}` is an unsafe read that cannot observe its own side effect: this seat's read of visualizations.py:255-272 — `find_one` FIRST (:256), `$inc {"views": 1}` after (:268-270), `_public_doc(doc)` serialises the **pre-increment** document (:272). So (a) `recordView`'s write-back (gallery.ts:207-208) restores a value the list already held — the counter can never show the user's own open; (b) the same verb is the ETag-capture path (gallery.ts:167, :223 — `publish` is live at VisualizationView), so every ETag cache-miss on publish/soft-delete adds a phantom view to the row being mutated; (c) no `viewed_ips` dedup exists while `liked_ips` does — and a mutating GET is unsafe against any proxy/prefetch (RFC 9110 §9.2.1). Registry-swept clean (this seat trusts LC's sweep, corroborated by DU's non-collision). FR-GV-24 (session-scoped `viewedHashes`) composes.
- **FR-GV-13 = MISS-LC-2 (adopted after re-proof) — MAJOR → F.W5-W8 (+ F.W3/W4 control).** `visibilityFilter` is a PRODUCERLESS store ref — this seat's own repo grep: **exactly 3 hits, all inside stores/gallery.ts** (:36 declaration, :55 sole reader, :270 export), zero writers anywhere. `ownerParam()` therefore returns `undefined` unconditionally and the server's whole `owner == "me"` branch (visualizations.py:305-309, with its own session-invalid error path) is dead from this client — a shipped capability with a documented contract (`gallery.ts:34-36`), server support, a client parameter, and no control. The worst sibling of the banked FR-GFC-4 filter hole: the other three filters at least have producers.
- **FR-GV-14 = D-11 — MAJOR → F.W3/W4.** `opacity-70` over `text-muted-foreground` on the drafts empty state's only instructional sentence (:357-364) = **2.88:1 light / 4.24:1 dark** — fails AA in BOTH arms at normal text size (15.75px mobile / 14px desktop). Twice independently reproduced from token bytes; the figures reproduce only under gamma-space compositing (the browser's actual behaviour) — the discriminator both readers' instruments honoured.

### MINOR

- **FR-GV-15 = L-5 / C·m-2 — MINOR (DEMOTED, R-5; both readers' MAJOR in dissent) → F.W3/W4.** The 300ms `searchTimer` (:94-98) is never cleared — sole lifecycle import is `onMounted` (:2); Vue stops the watcher, not the timeout. The consequence clause is KILLED (K4: every mount refetches; the shared key retires orphans) — the row's justification is the leak and the wasted GET, not a symptom. Rides banked FR-AUL-15's `onScopeDispose` cure identity.
- **FR-GV-16 = D-15 — MINOR (LC MAJOR in dissent) → F.W3/W4.** The 1rem gutter is authored nine times across three mechanisms (`px-4`/`mx-4`/raw `padding: 0 1rem`) and the gallery empty state (:265-286, `py-6` only, ancestor chain supplies nothing — :242 is a bare `<template>`, :220 root has no horizontal padding) authors it zero times. Adjacent banked identity: fr-GalleryDraftsSection m-4 (the DRAFTS empty state's split — different pair).
- **FR-GV-17 = D-7 (residue) — MINOR (LC MAJOR in dissent) → F.W3/W4.** Zero headings in the whole route (`grep '<h[1-6]'` → empty; the visual heading recipe is a `<span class="cm-serif …">` ×5). Three sites FOLD to banked MAJORs (GCM-16 modal · FR-GFC-7 carousel · GAB-28 banner); the booked increment is GalleryView's OWN surfaces (two empty states, batch toolbar) and the whole-route outline.
- **FR-GV-18 = D-16 / D-S5 — MINOR-provisional (LC MAJOR in dissent) → F.W1.** The batch toolbar's entire surface rides the fourier-local `cartoon-card` shim (:308-313; style.css:98-111 `@apply cartoon-surface`), and one stack uses three z vocabularies (raw `z-20` vs producer `--z-bar: 30` vs overlay `z-5`). The favourable correction (cartoon-surface RELOCATED, not deleted, at v7.0.0 — verified at the tag by LC, method sound) de-scopes corpus carry #8 **provisionally**: whether Tailwind `@utility` registration crosses the `@import` boundary in a consumer build is NEEDS-BUILD (SS-13 item 4). The safe-area/sticky half is a device measurement (SS-13).
- **FR-GV-19 = D-18 — MINOR → F.W3/W4 copy pass.** Legal-register "shall" ×3 in the confirm dialog (:417/:421/:424) against the file's own plain voice. Diction rides banked FR-AFP-35's register row; the TRUTH of the delete sentence is owned by FR-GV-33.
- **FR-GV-20 = D-21 — MINOR → F.W3/W4.** `role="toolbar"` (:308-316) over four independently-tabbable Buttons with no roving tabindex, and the selection count (:314-316) with no `aria-live` — appearance and every count change silent. Class precedent banked cross-repo (shell-dock AT-15, kf-EditorShell D-15); this is the fourier instance.
- **FR-GV-21 = D-24 (increment) — MINOR → F.W3/W4.** The drafts empty state states the wrong cause for the still-loading (:89 awaits before first render truth) and logged-out (:101-104 clears the array) situations. The error-swallow arm FOLDS to banked fr-GalleryDraftsSection m-14, which names this exact parent copy — do not re-book that arm.
- **FR-GV-22 = L-15 / C·m-6 — MINOR → F.W3/W4.** `route.query.admin as string | undefined` lies about the repeated-key array case (an array would stringify into `Bearer a,b` at api.ts:130), and `router.replace({ query: {} })` (:87) discards every unrelated param. Composes with FR-GV-7.
- **FR-GV-23 = L-16 — MINOR → F.W3/W4.** `catch (e: any)` at :195/:211 discards the typed `ApiProblem` (RFC 7807 type/status/detail/instance — this seat's api.ts :180-199 read); zero `instanceof ApiProblem` narrowing in the subtree; typed URNs unreachable to every handler.
- **FR-GV-24 = L-19 / C·m-9 — MINOR → F.W5-W8 (rides FR-GV-12).** `viewedHashes` is component-local and resets on every mount (:46, :117-121) — ordinary navigation re-increments the server counter. Carries the banked GCM-3 R-7 precision: the guard DOES work within a session; the defect is scope, not absence — repair tests must not assert a re-open increment.
- **FR-GV-25 = L-22 / C·m-10 — MINOR → F.W3/W4 (one unit with FR-GV-10).** `onMounted` serialises three independent round-trips (:83-91); the only real edge is activateAdmin-before-list, which `Promise.all` after it preserves.
- **FR-GV-26 = m-1 / C·m-1 — MINOR (mechanism row; consequence = FR-GV-11) → F.W3/W4.** `fetchNextPage` and `resetAndFetch` share the hardcoded abort key so either cancels the other; `fetchNextPage`'s guard (gallery.ts:61) checks `hasMore`/`loadingMore` and not `loading`; both catch arms swallow via `isAbortError`, leaving `entries=[]` with `hasMore` true.
- **FR-GV-27 = L-23 — MINOR → F.W5-W8 (rides FR-GV-8).** `publish` PATCHes `{visibility}` behind an extra GET (gallery.ts:223 — which, per FR-GV-12, adds a phantom view) while dedicated `/publish`/`/unpublish` routes ship with no client wrapper.
- **FR-GV-28 = L-25 — MINOR → F.W0.** `Visualization` imported from two paths in one subtree (GalleryView :10 `@/lib/types` vs gallery.ts :4 `@/lib/api` passthrough).
- **FR-GV-29 = L-17 / C·m-4 — MINOR → F.W3/W4 rider.** Three Set-valued refs, two mutation idioms (:119 in-place vs :127-129/:155-158 copy-on-write). Correctly graded by its own axis: consistency, not correctness.
- **FR-GV-30 = MISS-DU-3 (adopted after re-proof) — MINOR → F.W3/W4 (one line with the D-23 residue).** The batch dialog's AFFIRMATIVE BUTTON (:430-437) is the same v-else-less `v-if` chain as the title/description D-23 booked — on the null-`pendingBatch` path it renders with no text (no accessible name, WCAG 4.1.2) and its `:variant` ternary falls to `default` (non-destructive emphasis on a destructive dialog's affirmative). Reachability is the CATCH path only (:195-199 — R-1 killed the token-missing path); the structural absence is static, the visibility window is SS-13 item 3. Cure: one `v-else` set, or null-after-exit — never Cancel-side eager nulling (R-7).
- **FR-GV-31 = MISS-DU-4 (adopted; member-site rider on banked PAW-8, never a new row) — MINOR → F.W3/W4.** The route's only CTA is a `<button>` with `@click="router.push('/visualize')"` (:282-284, this seat's read) — not middle-clickable, absent from the link graph, announced as a button. Third member site of PAW-8's identity (glass Button `as-child` over `<router-link>`), and the sharpest: per FR-GV-3 it is also the only affordance in the failure state and the FR-GV-10 first-paint window.
- **FR-GV-32 = MISS-LC-3 (adopted after re-proof) — MINOR → F.W3/W4.** `activeTab` has no URL representation — five panes behind one `/gallery` URL (router/index.ts: no children/props/query contract — this seat's read); no deep link, no Back traversal, refresh lands on Gallery; and it removes the only cheap recovery from FR-GV-5's blank-tab state. The component demonstrably owns the query string (:84/:87) and simply never uses it for this.
- **FR-GV-33 = MISS-LC-4 (adopted after re-proof) — MINOR → F.W3/W4 (one unit with FR-GV-2).** The two `<Select>`s in GallerySearchBar (:82-95/:97-109 — this seat's read: each trigger contains only `<SelectValue />`) have no accessible name — the computed name is the current VALUE; a value is not a label. Same class as FR-GV-2, two more controls, one file; no fr-GallerySearchBar record exists (this seat's registry ls).
- **FR-GV-34 = MISS-LC-5 (adopted; member-site rider on banked FR-AFP-9) — MINOR → F.W5-W8 (copy F.W3/W4).** The batch-delete dialog asserts irrevocability (:417-419) over a SOFT delete (admin.py:425-428 → `soft_delete`, this seat's read) for which the client ships an unreachable restore verb (`gallery.restore` implemented :178-187, **zero callers** — this seat's own grep). False in the direction that suppresses a safe action. Second call site of FR-AFP-9's mechanism; D-18 booked this sentence's register and never tested its truth.

### INFO

- **FR-GV-35 = i-2 — INFO → F.W3/W4 rider.** Three bare `defineAsyncComponent` loaders (:31-33), no `errorComponent`/`timeout`; a chunk-load failure renders the same blank pane as FR-GV-5, indistinguishable. Folds to banked AA-38 / FR-AUL-57.
- **FR-GV-36 = L-24 / GAB-10 / GCM-42 — INFO at the banked severity → F.W9/W10.** Every defect here is structurally invisible to the gates that exist (no vitest, no ESLint, no `noUnusedLocals`, no `vueCompilerOptions`; the console e2e passes against a dead API). Folds to banked FR-GFC-27 + AA-44's route + FR-AUL-16's coverage input; L's three-conditionally-vacuous-tests census is the honest increment and rides those rows.
- **FR-GV-37 = L-20 — INFO · NO-WAVE-OWNER (SS-3/SS-4 repair-unit sizing input).** 442 lines, four responsibilities; the ~140-line batch feature's only coupling to the rest is `adminMode`+`activeTab` — an extraction seam for the spec authors, not a wave's defect.

### Folds-by-reference (identity guard — adjudicated AT the banked severity; never re-booked)

Each row was verified live by at least one reader and spot-checked by this seat where load-bearing; the banked row owns severity and cure. **D-1 / L·filter-drawer** → fr-BasisCanvas M-α6 via fr-GalleryCard R-4 (MINOR; the dead-enter/live-leave drawer asymmetry is the additive site note; THIRD BLOCKER inflation killed, K11). **D-3** → fr-GalleryCard D-5 (MAJOR) + GAB-1 (banner arm); §8's default-arm premise killed (K5). **D-5 / L-7 / C·M-2** → FR-GFC-9 ≡ G-LC1 ≡ GM-F1 (MAJOR, delete-or-revive; triple independent set-theoretic derivation, `0 ≥ 4` false for all inputs — re-reproduced by this seat from :64-66/:266/:270). **D-6** → fr-EquationView M-TL (MINOR; `aria-controls` framing killed, K11). **D-8** → fr-GalleryCard G-DU1/G-DU2. **D-10** → FR-GFC-6 (MAJOR; the cure precedent is the marquee's PADDING, never the mask — K10). **D-12** → GAB-18 + FR-GFC-11 (MINOR). **D-13 (surviving limbs)** → GAB-2 (MAJOR; `orientation` prescription killed per GAB K-5 — v8.0.0 renames to `posture`; DOM-order limb killed at the installed bytes, K11). **D-17** → FR-GIG-7 (member sites :188/:315/:406/:409/:412). **L-1 / C·C-2** → FR-GFC-1 (blast-radius arm) + FR-GFC-4 (MAJOR; BLOCKER filings in dissent, R-4). **L-3 / C·M-3** → FR-GFC-3 + fr-GalleryCard C-1-as-corrected (MAJOR). **L-8(b)** → fr-GalleryDraftsSection M-10 (logout clears the array, not the store; no owner field). **L-10 / C·M-10** → fr-GalleryDraftsSection m-13 (the :241 slug rebind — re-read live by this seat). **L-11** → FR-GIG-15 (MINOR; chunkSize 24 > limit 20 re-confirmed at gallery.ts:65/:74). **L-12-identity / C·M-5 / C·M-7** → fr-GalleryCard D-7, GCM-25, GCM-3 (MAJOR each). **L-14 / D-30** → FR-AUL-17 + FR-AFP-32 (timeAgo) + fr-BasisSelector M-10 (basisLabels) + GCM-40 (`.filter(Boolean) as`); the R3-12 attribution stripped (K8). **L-18 / D-14 / C·m-8** → FR-GIG-7/FR-GIG-8. **L-21 / C·m-14** → GM-F2 as LATENT (live arm killed, K9). **C·C-1** → fr-BasisCanvas D-1/BC-1/C-1 (BLOCKER → F.W1; blast-radius absolute killed, K6; the value.js seam gating on parser-proof R1 is the sentence to carry). **C·M-1** → fr-BasisCanvas D-2/BC-5 + FR-GFC-8 (path-dependence per its R-3/K4). **C·m-5** → fr-GalleryDraftsSection M-3. **C·m-7** → the FR-AUL-13 class (server never emits `errors` — re-read by this seat at admin.py:448; the :189-191 loop is dead, which also demotes S-1's property 4, R-2). **D-25/D-26/D-27/D-28/D-29** → FR-GFC-25/GM-F11 (INFO) · GM-7 · FR-GFC-29 (which proves `./checkbox` absent at both pins). **C·m-11/m-12/i-1** → banked uplift-exposure and value.js-consumption rows (metric-badge 7-site census; `ToastVariant` absent at both tags — BASELINE LAW discharged by LC's own tag greps).

## Killed-claims register

1. **K1 · D-23 as filed + reader-LC's re-confirmation — KILLED (this seat, R-1).** The cited mechanism is unreachable: the token-missing branch returns at :178, lexically before the `try` at :180; the `finally` (:197-199) binds to that `try` and cannot run on that path. `pendingBatch` stays populated; the dialog exits with content intact. The original filing's "the `finally` runs synchronously in the same tick" (challenge-D-design.md:480) and LC's "the early return at :178 still runs the finally" are the same false cell. Residue rebooked at FR-GV-30 via the catch path.
2. **K2 · L·S-1 property 5 / C·S-2 / LC superlative 2 ("an error cannot strand the dialog state") — KILLED (R-2).** The token-missing error strands `pendingBatch`; only :167 ever clears it on that path. Of five claimed properties, three hold; the fan-out (:189-191) is dead code (admin.py single return).
3. **K3 · M-8 / C·M-8 first clause ("no featured entry can ever enter selectedHashes") — KILL SUSTAINED (banked FR-GFC K2 governs; writer set re-derived by this seat).** Intra-registry conflict RULED: fr-GalleryCard G-LC2's identical absolute is non-citable for this claim; K2's derivation is the registry's authority. Surviving limb booked FR-GV-9.
4. **K4 · L-5's consequence clause ("returning to the gallery shows whatever the orphan left") — KILL SUSTAINED** (:83-91 unconditional refetch every mount; shared abort key retires the orphan; catch swallows). Drove the R-5 demotion.
5. **K5 · D-3 §8's default-arm premise ("with neither stored, the light arm paints") — KILL SUSTAINED, re-verified at the bytes by this seat** (web/index.html:21-31: absent a stored preference, `matchMedia("(prefers-color-scheme: dark)")` decides — the OS, not the light arm). The contrast findings survive on the weaker, sufficient reachability ground; the prescribed clean-profile probe would measure the tester's OS.
6. **K6 · C·C-1's blast-radius absolute ("every colour … #888888 … forever") — KILL SUSTAINED** (deep-link path captures the hardcoded hexes before mount — C's own M-1; banked FR-GFC K4 killed the identical absolutization). The C-3/C-4 one-repair interlock is the sentence to carry, not the absolute.
7. **K7 · L-13's bundle-cost limb — KILL SUSTAINED** (banked fr-GalleryCard K-13: `.path-preview` in no built sheet; Rollup shook the SFC, CSS included). Dead-import finding intact (FR-GFC-22 + L·D-3, F.W0 `noUnusedLocals` rider); citation drifts corrected (PathPreview 69 lines, GalleryMarquee 134).
8. **K8 · L-14/D-30's R3-12 attribution ("the mechanism behind the intake over-count is the byte-clone") — KILL SUSTAINED** (banked fr-GalleryCard K-8: R3-12 is a deriver-side canonicalization collapse keyed on component callsites). The byte-clone folds to fr-BasisSelector M-10 on its own legs.
9. **K9 · L-21's live arm (aria-hidden-focus as a live violation) — KILL SUSTAINED** (banked fr-GalleryCard K-9: the guards :266∧:270 are jointly unsatisfiable; re-booked LATENT at GM-F2 as a rider on delete-or-revive).
10. **K10 · Two superlatives awarded to dead or defective bytes — KILLS SUSTAINED.** (a) D-S3's marquee mask-geometry craft: the mask is Y-invariant and `mask-repeat` resets to `repeat` — rendering-inert (banked GM-9); the working clipper is `overflow: clip` + the :83 padding, which is what FR-GFC-6 prescribes for D-10's cure. (b) L·S-3's modal open-bridge "minimal correct adapter": its own falsifier is the banked defect (GCM-9 — constant-`true` getter + parent `v-if` teardown makes the exit half of `popover-animate` unreachable; the sibling batch Dialog 100 lines below animates correctly).
11. **K11 · The severity/prescription cell cluster — KILLS SUSTAINED against the bank.** D-1 BLOCKER (fourth-axis inflation of a banked MINOR — calibration signal), L-3 BLOCKER (banked MAJOR), D-3 BLOCKER (banked MAJOR; GAB-1's BLOCKER is the banner's own arm), D-6's `aria-controls`-load-bearing MAJOR (banked MINOR per the ratified kf-KfPillTabs kill — supported, not required), D-12 MAJOR (banked MINOR ×2), D-13's `labelPosition → orientation` prescription (killed per GAB K-5 — v8.0.0's `posture`; budget from 8.0.0, never the 7.0.0 snapshot) and its DOM-order-flip limb (killed at the installed dist bytes: label precedes amount at BOTH pins; the F.W1 visual-diff budget is one regression, not two).
12. **K12 · The live-only hedge cluster — HEDGES KILLED, findings STRENGTHENED.** L-2/C·C-3(c)'s duplicate-row hedge closed statically by banked B-2 (idempotency passthrough + plain index); L-18's sentinel hedge closed by banked FR-GIG-1 (dist read: `check()` never consults an intersection record; rAF re-arm); D-4/L-24's retry question closed by banked FR-GIG-2 (toast-per-iteration storm). Three SS-13 items struck — probes are for what statics cannot reach.

## Missed finds — adopted after re-proof

1. **MISS-DU-1** — first paint = cold-empty CTA. Re-proven (four reads, above). **Booked FR-GV-10 (MAJOR).**
2. **MISS-DU-2** — abort-orphaned `finally` paints the false-empty mid-fetch. Re-proven at all three hops. **Booked FR-GV-11 (MAJOR).**
3. **MISS-DU-3** — the v-else-less affirmative button. Re-proven at :430-437; reachability corrected to catch-path-only by R-1. **Booked FR-GV-30 (MINOR).**
4. **MISS-DU-4** — the CTA is a button, not a link. Re-proven at :282-284. **Booked FR-GV-31 (MINOR, PAW-8 member-site rider).**
5. **MISS-LC-1** — the unsafe GET's pre-increment read-back + ETag phantom views. Re-proven at visualizations.py:255-272 by this seat's own read of the statement order. **Booked FR-GV-12 (MAJOR).**
6. **MISS-LC-2** — `visibilityFilter` producerless. Re-proven by this seat's own grep (3 hits, zero writers). **Booked FR-GV-13 (MAJOR).**
7. **MISS-LC-3** — `activeTab` has no URL. Re-proven (router read + component query ownership). **Booked FR-GV-32 (MINOR).**
8. **MISS-LC-4** — the two unnamed Selects. Re-proven at :82-109. **Booked FR-GV-33 (MINOR).**
9. **MISS-LC-5** — irrevocable copy over a soft delete with a shipped, unwired restore. Re-proven (admin.py:425-428; zero-caller grep). **Booked FR-GV-34 (MINOR, FR-AFP-9 member-site rider).**

## Superlatives (provenance preserved)

1. **The D axis's contrast instrument** — reproduced independently by BOTH readers from raw token bytes, all figures to 2-3 decimals, with the gamma-vs-linear compositing subtlety as the discriminator (DU's first linearised run got it wrong and said so). Third and fourth independent reproductions in the fleet; §7's published-inputs format is the reason re-derivation is cheap. Adopted on that double reproduction; not re-run a fifth time (parsimony).
2. **D-9's pre-empted cure failure** — read the export maps at both pins before prescribing, found `./confirm-dialog` dies at the uplift, and told the wave not to migrate onto it. The cure-adjudicated-separately law run unprompted — the failure class the fleet has killed after the fact five times.
3. **L-3's honesty on the like row** — read and credited the store's own stopgap comment (gallery.ts:189-192, re-read live by this seat) and located the defect at the VIEW. Same file, same lines where the sibling C axis was killed (fr-GalleryCard K-1) for excising that comment. Same bytes, opposite integrity.
4. **`performBatchGallery` — the file's best function, CORRECTED to 3-of-5** (R-2): the pre-await snapshot, the close-before-I/O, and the explicit token toast are real, individually verified properties; the error fan-out is dead code and the `finally` guarantee is false. A superlative that survives an audit at 60% is still the honest ceiling of this file.
5. **C·C-1's value.js seam discipline** — traced `lib/colors.ts` to the installed `@mkbabb/value.js` surface and refused to book the cure blind, gating it on the parser-proof R1 oklch crash. The single most action-relevant sentence in the corpus; inverts the census's "cheapest leg" framing, and the bank already adopted the wave-lock it implies (FR-GFC-8's F.W2 rider).
6. **`lib/scheduler.ts`** (LC) — the three-rung `scheduler.yield()` ladder with a typed shim and a written negative decision (why the rAF render loop is not a consumer); only its single caller's constant is wrong (banked FR-GIG-15), which is why that repair is one integer.
7. **Triple-independent unreachability proof** — L-7/D-5/C·M-2 each separately reduced the marquee mount to `0 ≥ 4` with self-named falsifiers; this seat reproduced it in one read. The banked identity (FR-GFC-9/G-LC1/GM-F1) agrees.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **FR-GV-10's perceived duration** — frames the cold CTA holds before `resetAndFetch` raises `loading`, cold-IndexedDB vs warm. One `goto('/gallery')` on a clean profile with video. Pairs with FR-GIG-4's item and FR-GIG-1's drain count.
2. **FR-GV-11's observed sequence** — two filter clicks inside one round trip (or a throttled list endpoint) → assert the empty CTA paints mid-flight. Same probe closes FR-GV-26.
3. **FR-GV-30's visibility window** — whether the catch-path `finally` can null `pendingBatch` inside `popover-animate`'s exit on a real rejection. One forced 500 on `POST /api/admin/visualizations/batch` with a recording. (The token-missing path is NOT a probe target — R-1.)
4. **FR-GV-18's emission hinge (NEEDS-BUILD, not browser)** — build fourier at the F.W1 target tag, grep the emitted CSS for `.cartoon-surface`; alongside fr-GalleryDraftsSection UNPROVEN #1. Plus the `sticky bottom-2` vs `env(safe-area-inset-bottom)` device check (the inset is on `body`, outside the scroller — static half confirmed).
5. **D-10's clipped pixel extent** at the featured rail (banked FR-GFC-6 item; cascade closed statically).
6. **C·C-1's rendered grey** — with K6's probe constraint: the deep-link path is a different colour language; any probe must record its entry route or it measures one arm and reports both.
7. **The tab strip's mobile geometry** — five tabs at an 18px root in a 375px viewport, `responsive` defaulting false; if it overflows, the opt-in cure also requires the `ariaLabel` GalleryView does not pass.
8. **vue-tsc behaviour on unknown props under this exact config** (NEEDS-BUILD) — the static premise (no `vueCompilerOptions`, strictTemplates off) is established.
9. **Deployment-side view idempotency** — whether any proxy/edge layer dedups the mutating GET; changes FR-GV-12's blast radius in either direction; not readable from the tree.
10. **THE F.W1 TARGET TAG** — a formation decision this seat cannot settle; governs FR-GV-4's cure, the D-13 fold's budget, and FR-GV-18's build probe. The challenges' "producer latest 7.0.0" was true on 2026-08-06; v8.0.0 tagged 2026-08-09; banked GAB K-5/FMD-11 already budget from 8.0.0. Same open item as fr-GalleryCardModal UNPROVEN #8 and fr-GalleryFeaturedCarousel DISSENT 5.

**Struck from SS-13** (K12): the duplicate-row outcome (closed by B-2), the sentinel/threshold behaviour (closed by FR-GIG-1), the retry storm's existence (closed by FR-GIG-2 — its CADENCE remains a live measurement under FR-GIG-2's own banked item, not re-listed here).

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet, not the tree)**: reader-LC confirmed D-23 and re-asserted L·S-1's `finally` guarantee by inheriting the challenge corpus's false control-flow cell, where reader-DU re-derived the flow and killed both. This is the fleet's known failure class (three banked instances on a11y-overrides.css alone) firing on a NINE-LINE function body — the mandatory-read lesson generalises: a `finally` cell is never portable without the `return` statements above it. LC's independent corrections elsewhere in the same return (K2-DOM-order at the dist bytes, K3's index.html read, K5, K6) were genuinely its own and are adopted; the seat notes the same reader was right at the bytes everywhere it went to the bytes, and wrong exactly where it quoted.
2. **Severity dissents preserved**: D-2/D-4/L-1 carried BLOCKER filings (axes + reader-LC) — ruled MAJOR on family precedent (R-4); both readers' MAJOR on L-5 — ruled MINOR (R-5); reader-LC's MAJOR on D-7/D-15/D-16 — ruled MINOR (R-6). If the forming F.W3/W4 spec grades the false-empty pair (FR-GV-3/10/11) as one composed surface — a primary public route that states a falsehood about the database on first paint, mid-fetch, and on failure — the blocker-weight hook is there.
3. **Intra-registry conflict, ruled (R-3)**: banked fr-GalleryCard G-LC2 asserts the absolute that banked FR-GFC K2 kills. K2 governs; G-LC2 stands as dated evidence with this record as its correction rider. Repair waves cite K2/FR-GFC-2, never G-LC2's absolute.
4. **Baseline drift, dated**: producer claims in this record are discharged at tags 4.0.0/v7.0.0/v8.0.0 per BASELINE LAW; the F.W1 spec must state its target tag before this component's uplift ledger (FR-GV-4, FR-GV-18, the D-13 fold) is executable.
5. **Reader-LC's Cancel-asymmetry cell** preserved as filed (R-7 rules it a non-defect with the framing inverted); if a future wave makes `pendingBatch` nulling exit-aware, both halves converge and the observation discharges.

## Verdict

**A VIEW THAT LIES ABOUT ITS OWN DATA, WIRED OVER CAPABILITIES THAT DO NOT EXIST** — 37 booked rows (1 BLOCKER · 13 MAJOR · 20 MINOR · 3 INFO) + 26 folds-by-reference, 12 kill-register rows, 9 missed finds adopted: the primary public route's first paint, mid-fetch state, and failure state are all the same false "No visualizations yet." CTA whose only affordance can't even open in a new tab; search, three of four filters, likes, and the whole `owner=me` axis are producerless or inert end-to-end; the view counter rides a mutating GET that cannot see itself; the admin surface accepts a bearer token from the URL, strands its tabs, and no-ops its confirmed destructive actions — while the best-written function in the file needed its two most-quoted correctness properties struck by a source read both hostile re-readers should have made and only one did. Dispositions are ADJUDICATED, not VERIFIED; routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
