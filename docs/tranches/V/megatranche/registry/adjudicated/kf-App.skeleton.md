claude-fable-5

# kf-App.skeleton — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ local HEAD `8281638c` — **41 commits behind `origin/master` `81a56990`** (this seat's own `git rev-list --count`; the SCH-1 substrate-schism class, and it BITES this corpus — see ruling 1). `demo/app/App.skeleton.vue` (101 lines) is **byte-identical at both trees** (absent from the 474-file HEAD diff); `App.vue` anchors shift +5 template / +2 script at origin/master (a root `TooltipProvider` comment block), the `<Suspense>`/fallback structure unchanged. Line anchors below are local-checkout; re-resolve before any cure (D-19). · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two fresh hostile re-reader returns (reader-1 = design-and-usage axis · reader-2 = library-and-code axis) over the component's three challenge files — `challenge-D-design.md` (296 lines, Opus 5, 17 defects incl. the corpus's sole BLOCKER) · `challenge-L-library.md` (294 lines, Opus 5, 10 defects + 1 self-retraction) · `challenge-C-consumption.md` (273 lines, Opus 5, 9 defects). All three challenge files were re-read **whole** by this seat.
**Method (incredulity)**: every cross-reader disagreement, every kill of the corpus's load-bearing rows, and every NEW missed row was re-derived against the bytes by this seat before adoption. Suspiciously-agreed cells were re-measured, not spot-trusted — and the two most-agreed cells in the whole corpus **both failed** (ruling 1: the F-1 re-affirmation both readers ran; ruling 2: the copy-provenance cell reader-1 confirmed). Zero browser tooling (probe parsimony; lane law); every adjudication is static (read + grep + git archaeology across keyframes.js AND glass-ui). Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is agglomeration input (M-25), never a wave stamp.
**This seat's own instruments (all read-only)**: full read of `App.skeleton.vue` (whole, 101); `App.vue:84-99` (scene-host + bare `<Suspense>` + `<SceneSkeleton />`), `:283-292` (home/cube → static `CubeScene`, the C·D-4 denominator decider), `:155` (static import); `grep -c "aria-\|role=" demo/app/App.vue` → **0**; `useSceneTransition.ts:90` (`sceneHost.value?.focus()`); `main.ts:50-54` (**warm catch-swallowed** — the L-6 falsifier decider, verbatim); `scenes.ts` stageMode census (subject `:132/:141/:149/:157` = 4 · editor `:165` · storyboard `:173/:185` = **4-of-7**); `SquareScene.vue:3-13` (`<Card :shadow="false">` — the subject-with-card counter-example); EditorShell.vue `gridBackground` v-if + default **true** + App.vue passes none (the D-1-kill decider); `layout.css:33-35` (`--graph-opacity: 3%` / `--graph-major-opacity: 11%`) vs EditorShell `:234` prose "12%" and `:241/:246` fallbacks 5%/12% (M7's three numbers); `AnimationControlsGroup.css:52` (`[rail] var(--rail-track) [stage] minmax(0,1fr)` — **two columns, ONE rail**, the D-5-mechanism decider), `:12-23` (the cqi container-promotion comment, M4's law), `:130-140` (mobile `.stage-cell{position:fixed;inset:0}`, M3); `layout.css:49-51` (work-area clamps); token greps: `--shadow-glass` = 1 hit repo-wide (the consumption site) and **zero declarations** in the shipped bundle (`grep -o -- "--shadow-glass[a-z-]*:"` over `dist/gh-pages/assets/index-CL_QYCiO.css` → empty) while the **backing family `--glass-shadow-{wash,quiet,resting,floating,overlay}` IS in the bundle** (7/3/4/4/5 occurrences — this seat's cure-repricing find); bundle layer order by byte offset: properties 9251 < theme 12254 (`--radius-lg:var(--radius)`@14283, `--radius:.625rem`@15104) < base 18022 < components 21885 (`--radius:.25rem`@152859, `--radius-lg:.5rem`@152875) < utilities 182187 — **M1 re-derived independently**; `components.css:1` unlayered `:root{--radius: 0.25rem; --radius-lg: 0.5rem; --radius-sm: 0.25rem…}` + `index.css` tail `@import "./components.css" layer(components)`; `theme/radius.css` (`--radius-2xl: 1rem`, `--radius-card: var(--radius-2xl)`, `--radius-input: var(--radius)`); installed `.skeleton[data-v-cd03d0b0]{…border-radius:var(--radius-input);background:var(--muted)…}` + its forced-colors arm verbatim; `rounded-card` live in 4 scene files (square/amiga/spring×2); `scheme-motion.css` (`--duration-shimmer: 5s`, `--duration-shimmer-fast: 3s`, `.motion-calm`, PRM `--motion-weight: 0` — same file); bridges emitted in bundle (`--color-muted:var(--muted)` + border + foreground, all three); counts: `SceneSkeleton` = 3 lines · `42rem|24rem` = the two component lines + unrelated `layout.css:24` · `forced-colors|prefers-reduced-transparency` in demo = **0** · `--color-{muted,border,foreground}` var-reads = **1 file** (this one) · stage-mode selectors `stage-{subject,editor,storyboard}` = **0** in demo and **0** in the bundle (`controls-layout--stage` grep -c → 0), emitter live at `AnimationControlsGroup.vue:6`; `DESIGN.md:191-192` (`defineAsyncComponent({ loadingComponent, delay: 150 })` verbatim) + `:202-206` §9.2 (`withDefaults` = "legacy form", verbatim); `CopyButton.vue:15` (`<span class="sr-only" role="status" aria-live="polite">{{ liveStatus }}</span>` verbatim); `TimelineHoverPreview.vue:17-18` (`v-if="loading"` … `Capturing...` — the corrected anchor); **git archaeology, both repos**: keyframes.js `git log --follow` → `61cf7e7d` 2026-07-06 15:05 (creation as SceneSkeleton, "T.F8: skeletons tier") + `969990f6` 2026-07-12 (re-home) — TWO commits; glass-ui: Skeleton born `e8380d73` 2026-03-25, shimmer variant `625a1729` 2026-04-22, `git log -S"105deg"` and `-S"in oklab, var(--foreground)"` over skeleton files → **first hit `490cc46e` 2026-07-16** (Glass 7); `git show 20b09bc7:…Skeleton.vue` (2026-07-06 12:09, ~3h before the demo file): gradient = `color-mix(in srgb, var(--muted-foreground) 30%, transparent)`, `transform: translateX(-100%)`, `animation: skeleton-shimmer-slide 1.5s linear infinite`, `will-change: transform`, PRM = `reduce`-override, **and the K.WP P1-4 comment verbatim** ("18 simultaneous shimmer skeletons on /aurora pushed TBT to 120ms vs 10ms baseline"); manifest: local `package.json` — zero glass entries, lock 0 hits; **`origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep** (ruling 1's decider); census `CENSUS-2026-08-03.md` SCH-1 (F-1 **FALSIFIED at HEAD**), S-6 row `:131`, KF.W0–W10 wave roster `:179-206`.
**Law compliance**: sole write = this file. Product source read-only; no browser tooling; no crons.

## Scoreboard

**22 adjudicated roster rows — 0 BLOCKER · 9 MAJOR · 9 MINOR · 4 INFO.** The corpus's sole BLOCKER (D-1) is **killed and stays killed** — twice over, by two independent mechanisms, both re-verified by this seat. **11 killed-claims register entries, all SUSTAINED or sharpened** — including one NEW kill this seat scored against BOTH readers (K11: the F-1 re-affirmation, stale-tree measurement). **11 admitted misses** across the two readers, all re-derived real; the two largest (M1 radius-layer defeat; M2 wrong-plate echo) **re-price every cure the three challenge axes prescribed**. **11 superlatives sustained**, one of them the corpus's own inversion corrected (the demo shipped the house shimmer color language ten days before the design system did). **2 corrections against the readers** beyond their own cross-corrections.

Blocker-weight survivor: **none**. Major-weight survivors of record: **KF-SKEL-1** (the a11y contract is inert four independent ways AND is designated the house skeleton spec — a defect with fan-out) and **KF-SKEL-4** (glass-ui's own `components.css` defeats its own `@theme` radius scale through cascade layers, app-wide — GLASS-OWNED, relay-owed). The component's realized user harm today is small; its adjudicated significance is that **every one of its four docblock promises is false, every token it reaches for is wrong or dead, and the cure all three axes converged on would make two of the defects worse** (M1/M2).

## Ruled disagreements and corrections (the incredulity seat's actual work)

1. **F-1 — BOTH readers corrected; the corpus's sequencing premise dies at HEAD.** Both readers re-ran the F-1 phantom-dependency greps fresh and re-affirmed RED ("dependencies = {\"@mkbabb/value.js\":\"4.0.0\"}; zero glass entries; lock 0 hits"). Both measured **the stale checkout**: this seat's own runs — local HEAD `8281638c` is **41 commits behind** `origin/master` `81a56990`, and at origin/master **`package.json:77` declares `"@mkbabb/glass-ui": "7.0.0"` as an exact devDep**. The census already ruled this (SCH-1: "FALSIFIED at HEAD. True only of the stale checkout; the cure is DOCS §B-12 reconciliation, not a dependency wave") — neither reader checked. Consequences: (a) the D-axis's sequencing constraint ("Declare the dependency first, then delegate") and C's "any S-6 remediation inherits F-1 as a hard prerequisite" are **discharged at HEAD**; (b) the *methodological* half of L-3.3 — an import-line census cannot see cascade-level coupling, witnessed by this zero-import, 4/5-token-dependent file — **survives intact**, because it is about the instrument, not the manifest; (c) `App.skeleton.vue` is byte-identical at origin/master, so every component-local row survives the re-anchor. Recorded as the corpus's second repeated citation-inheritance event — the census's top-listed defect (substrate schism) reproduced inside the audit of a single component.
2. **The copy-provenance contradiction — RULED for reader-2; reader-1's confirmation cell CORRECTED.** Reader-1 confirmed D-6/L-2/C·D-5 as "verbatim-shaped duplication … I RESOLVE the ordering L-2 left open: the primitive predates the copy by ~2.5 months; **convergence is refuted**." Reader-2 **killed** the copy claim on the fingerprint timeline. This seat's own git runs settle it: the primitive's *existence* long predates the demo file (Skeleton `e8380d73` 2026-03-25; shimmer `625a1729` 2026-04-22 — reader-1's dates are right), but the alleged **fingerprint** — `105deg` + `color-mix(in oklab, var(--foreground) …)` — first appears in glass-ui's skeleton at `490cc46e` **2026-07-16**, TEN DAYS after the demo file (`61cf7e7d` 2026-07-06 15:05). At `20b09bc7` (2026-07-06 12:09, three hours before the demo commit) glass-ui's shimmer was `in srgb, var(--muted-foreground) 30%` + `translateX` + `1.5s linear`. Reader-1 answered a different question than the one L-2 asked: primitive-existence proves the **reuse defect** (a shimmer Skeleton existed 2.5 months and was reimplemented); it cannot prove the **copy** (the color language flowed demo→glass, or converged at Glass 7). The roster row survives as reuse-plus-divergence (KF-SKEL-3); the "retyped primitive" framing and reader-1's "convergence is refuted" cell are struck; reader-2's superlative (the demo shipped the idiom first) is adopted as S-10.
3. **D-9's duration half — reader-1 confirmed, reader-2 killed; RESCOPED, both cells corrected.** Reader-2's era forensics are exact (verified at `20b09bc7`: the house clock at authoring was a hardcoded `1.5s linear` — the demo's `1.6s` was 7% *slower*, equally untokenised; `var(--duration-shimmer)` arrived with Glass 7). But the corpus adjudicates the tree **today**, against installed 7.0.0: `--duration-shimmer: 5s` exists (`scheme-motion.css`, verified), the primitive reads it, this file hardcodes `1.6s` ≈ 3.1× the house clock, and the same file's `.motion-calm` register and PRM `--motion-weight: 0` cannot reach a hardcoded literal. **CONFIRMED at MINOR, present-tense** (KF-SKEL-11), with the era annotation mandatory in the row: the defect is *drift left uncured by the producer's evolution*, not authoring-day deviance. Reader-2's kill is retained as the era-correction inside the row, not as a kill.
4. **L-5 (PRM polarity) — reader-2's kill SUSTAINED; reader-1's silent drop noted.** At authoring, glass-ui's own arm was the identical `reduce`-override polarity (this seat's read of `20b09bc7`); L-5's sole stated filing reason ("duplicates a primitive that chose the safer polarity") is false for the era, and L-5 itself concedes the form is correct in every supporting browser. The present-day divergence from installed 7.0.0's `no-preference` gate survives **only** as one arm in KF-SKEL-3's table — not a standalone row. Reader-1 listed L-5 in neither confirmed nor killed; a dropped row is a lost row — recorded as a process nit.
5. **The sweep-cost severity split (D-8/L-4/C·D-2: reader-1 MINOR, reader-2 MAJOR) — RULED MINOR with a named escalator.** Reader-2's new receipt is real and this seat verified it at the bytes (`20b09bc7` Skeleton docblock: 18 simultaneous shimmer skeletons = 120ms TBT vs 10ms baseline — the design system *measured and rejected* `background-position` animation). But the receipt's magnitude is an 18-instance figure; this component mounts **one** instance, ≤672×384 CSS px, for the lifetime of one chunk fetch. MINOR against this component today; the escalator is **U-24.2's per-module skeleton roster** — the moment the `<Name>.skeleton.vue` fan-out lands on this template, the 18-instance receipt becomes the governing figure and the grade flips. Magnitude to SS-13 (residue 2). The mechanism (paint-only property; `will-change: background-position` promotes nothing and survives the PRM arm forever) is deterministic and CONFIRMED.
6. **The error-boundary severity split (D-7: reader-1 MINOR-here/fix-upstream, reader-2 MAJOR) — RULED MAJOR, locus disclosed.** The defect is real four ways (zero `onErrorCaptured`/`errorHandler` repo-wide; bare `<Suspense>` no timeout; `defineAsyncComponent(loader)` bare at `scenes.ts:104`; `scenes.ts:113` documents the opposite), and reader-2's L-6 falsifier-fire **compounds it** (this seat re-read `main.ts:50-54`: the engine warm is catch-swallowed, so the offline/stale-deploy case that pins the shimmer forever can ALSO have a dead engine behind it). A component whose only job is "the loading state" renders the terminal state of an unhandled failure class — the severity belongs to the seam it is the face of. MAJOR, cure owned by `App.vue`/`scenes.ts`, routed NO-WAVE-OWNER (no forming KF wave owns app-shell async correctness).
7. **Anchor/citation corrections adopted from the readers' cross-fire, all re-verified where load-bearing**: L-9's `:21` → `TimelineHoverPreview.vue:17-18` (this seat re-read; the `:21` line is the code-register `v-for`); C·D-3's cure precedent `AnimatedText.vue` → **`CopyButton.vue:15`** (re-read verbatim — a real `aria-live="polite"` region with text; AnimatedText's mirror is static); L-2's "only commit touching this path" → two commits (re-run); D-10's "22 .vue sites" → 16 files / 45 occurrences (adopted from convergent reader agreement); D-1/D-4's contrast numerals drift from their own token sources (border 1.676 not 1.63 light / 2.204 not 2.46 dark; sheen-on-plate 1.172 not 1.08 — adopted as INFO row KF-SKEL-22's law: thresholds unaffected, but numbers must be re-derived, never quoted).

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** from the census wave taxonomy (CENSUS-2026-08-03 §a) where a forming X·KF wave owns the surface — here chiefly **KF.W6 · Glass Suffusion** (owns the S-6 partial: "keep stage geometry, delegate the plate" + the token-namespace audit), **KF.W5 · Tri-Fold Library Audit** (adjacent-component folds), **KF.W9 · Safari Visual Audit / PRM mechanism unification** (live witnesses), **KF.W0 · Substrate Settle** (re-anchor law) · **GLASS-OWNED** where the cure is the producer's (rides the standing BH/BI relay, owner edict) · **NO-WAVE-OWNER** where no forming wave owns it (SS-1/SS-2 spec authoring consumes). Identity guards: census **F-1** folds by reference to SCH-1 (never re-booked — ruling 1); census **S-6** is sharpened in place by KF-SKEL-3/-6, one identity; the inv-ζ INFO holds the `recap-deferred.md:270` BOOK precedent. No banked kf-* apotheosis precedes this record; it is the first.

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **KF-SKEL-1** | D-2 / L-1 / C·D-3 + r1-miss M5/M6 | The announced a11y contract is inert **four independent ways** — `aria-busy="true"` suppresses and never clears (static literal; node destroyed on resolve); the `role="status"` region's only content is `aria-hidden` + an empty span; the region is **born inside the suspension and dies with it** (M6 — a transient live region presents no announcement window, so the corpus's own "smallest cure" of putting text in the region is insufficient); and the swap's documented focus destination `.scene-host` is a nameless bare `div` (M5 — `tabindex="-1"`, zero `aria-`/`role` in all of App.vue, focus routed at `useSceneTransition.ts:90`). The defect has **fan-out**: `U.G.md:253-254` + `lane-24:77-79` canonize these exact attributes as "the house skeleton spec" / "already-correct" | `App.skeleton.vue:30-35`, `:16`; `App.vue:84-99` (all re-read); the in-tree correct idiom is `CopyButton.vue:15` (sr-only `aria-live="polite"` region that **outlives** its updates — re-read verbatim) | **KF.W6** — the S-6 partial keeps the wrapper; the wrapper's cure is a persistent announcer at/above `.scene-host` (CopyButton precedent) + a name/role on the focus target + the house-spec citations corrected wherever quoted. SR speech capture = SS-13 residue 1; the structure is source/spec-proven |
| **KF-SKEL-2** | D-3 / L-3.1 / C·D-1 | `--shadow-glass` is a dead token — zero declarations in installed glass-ui dist, demo styles, and the shipped bundle; the "glass plate" always paints the hardcoded 4%-black literal, invisible on the dark arm. **Cure re-priced by this seat's own find**: the suffixed bridge family `--shadow-glass-*` is ALSO absent from the shipped bundle (the `@theme inline` bridge emits only on utility reference — a scoped `var()` read does not trigger it), while the **backing family `--glass-shadow-{wash,quiet,resting,…}` IS emitted** — so "fix the name to the bridge spelling" would STILL paint the fallback; the only resolvable spellings today are the `--glass-shadow-*` backing tokens, or delegation to a surface that owns its elevation | `App.skeleton.vue:64`; this seat's `grep -o` runs over `index-CL_QYCiO.css` (bridge family: empty; backing family: 7/3/4/4/5); `glass-fx.css`/`bridges.css` provenance per C·D-1 (correct file) | **KF.W6** — dies in the plate delegation; if any interim local fix ships, it reads `--glass-shadow-quiet`/`-resting`, never the bridge spelling. The bridge-emission trap itself is a token-namespace-audit input (KF.W6's `--kf-*` audit arm) |
| **KF-SKEL-3** | D-6 / L-2 / C·D-5 *(as rescoped by ruling 2)* + census S-6 | Reimplementation of an installed primitive **minus five arms** (compositable transform sweep · forced-colors · reduced-transparency · motion-opt-in polarity · `--duration-shimmer` token), while the primitive's compiled CSS ships in the demo bundle as dead payload (`.skeleton[data-v-cd03d0b0]` + `skeleton-scan` present — verified). The **copy framing is REFUTED** (ruling 2): the color language is demo-first; the defect is *failure to reuse plus failure to track*, not plagiarism. Packaging facts (C) re-confirmed: 73 subpaths, no `./skeleton`, runtime rides the data-table chunk, tree-shake unmeasured | §0 table of challenge-L (re-verified against installed dist); glass-ui git per ruling 2 | **KF.W6** — the S-6 partial, sharpened by KF-SKEL-6: delegate to the **stage-card register**, not the bare primitive (see next row); the five arms arrive with the delegation. Chunk-cost measurement = SS-13 residue 5 |
| **KF-SKEL-4** | r1-miss M1 ≡ r2-miss 2 (independently convergent) | glass-ui's own packaged `components.css` **defeats its own `@theme` radius scale through cascade layers**: `index.css` ends `@import "./components.css" layer(components)`, and that file's unlayered `:root` re-declares Tailwind defaults — `@layer components` sorts after `@layer theme`, so app-wide `--radius` = **0.25rem** (not 0.625rem) and `--radius-lg` = **0.5rem**. Re-derived by this seat from the shipped bundle by byte offset (theme 12254: `--radius:.625rem`@15104; components 21885: `--radius:.25rem`@152859) AND from the installed source files. Resolves the D-13/C·D-6 hedges **exactly** (the plate paints 0.5rem; the authored 0.75rem fallback never applies); detunes every derived token (`--radius-input` → 4px — glass-ui's own Skeleton corner) | `components.css:1` + `index.css` tail + bundle offsets (this seat's runs) | **GLASS-OWNED** — a producer packaging defect; rides the standing glass-ui BH/BI relay (owner edict, a fond). Downstream blast-radius enumeration belongs to the relay, not this file (residue 7). Consumer side: KF.W6 must **not** cure this component against the detuned values — the spec pins intended-vs-shipped radius explicitly |
| **KF-SKEL-5** | D-7 *(ruled MAJOR, ruling 6)* + L-6-falsifier-fire | No error or timeout arm anywhere: zero `onErrorCaptured`/`errorHandler` repo-wide, bare `<Suspense>`, bare `defineAsyncComponent(loader)`; a rejected chunk shimmers forever with `aria-busy` pinned, and `scenes.ts:113` documents the opposite ("the real mount surfaces the error via `<Suspense>`" — false without an ancestor boundary). Compounded: the engine warm is **catch-swallowed** (`main.ts:50-54`, re-read), so the same network failure class can leave the app mounted with `kfEngine()` throwing behind the shimmer | greps re-run by this seat; `scenes.ts:104/:113`; `App.vue:90` | **NO-WAVE-OWNER** — app-shell async correctness; no forming KF wave owns it; SS-1/SS-2 spec authoring consumes. The docblock's "THE shared loading placeholder" scope claim is corrected by KF-SKEL-14 regardless |
| **KF-SKEL-6** | r1-miss M2 | The skeleton echoes the **wrong plate** — the demo owns an explicit stage-card register (`<Card :shadow="false">` + `rounded-card` = `--radius-card` = **1rem**, live in square/amiga/spring×2 scenes — re-verified) which IS "the scene's stage panel" the docblock claims to echo; the skeleton paints 0.5rem (post-M1) flat veil, and the bare glass `Skeleton` all three axes prescribed paints `--radius-input` = **0.25rem** with no border/shadow — **every prescribed cure increases the swap-time silhouette discontinuity it is meant to remove**. The structurally sound cure: the demo's own Card carrying a skeleton sheen inside it | `SquareScene.vue:3-13` (re-read; "the SAME plate easing/spring/sequence stand on"); radius chain re-verified (`--radius-card: var(--radius-2xl)` = 1rem; installed `.skeleton` reads `--radius-input`) | **KF.W6** — this row IS the S-6 partial's spec-shaping law: "delegate the plate" means *delegate to the stage-card register*, primitive-as-sheen only. One identity with census S-6; sharpened, not re-booked |
| **KF-SKEL-7** | L-3.3 *(as rescoped by ruling 1)* | The census's import-line coupling instrument **undercounts cascade coupling** — witness: this file has zero imports and zero `@import`, sits among lane-frontend's "21 .vue with no glass-ui import", yet 4 of its 5 custom-property reads resolve **only** through glass-ui's cascade (nothing in `demo/styles/**` defines them). The phantom-dependency premise is dead at HEAD (ruling 1; SCH-1); the instrument critique stands on its own | this seat's greps + ruling 1 | **KF.W0** (re-anchor/re-count law: the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census) + **KF.W6** (the token-namespace audit is the natural home of the probe). F-1 itself: fold by reference to SCH-1, **never re-booked** |
| **KF-SKEL-8** | D-5 / L-8.2 + r1-miss M3 *(mechanism corrected per K4)* | `42rem × 24rem` are magic numbers appearing nowhere else in the tree (re-verified: the two component lines + one unrelated dropdown cap); "matching the stage geometry" is false, and the plate **under-fills** the stage on both layout forks — desktop ≈54% of the stage cell at 1920px; mobile is the severe case (`.stage-cell{position:fixed;inset:0}` full-bleeds the viewport while the 24rem cap leaves the plate ≈ half the stage height — chain re-verified end-to-end). The challenge's own mechanism ("rail·stage·rail; the cap collapses") is killed (K4): ONE rail, the cap **binds** | `App.skeleton.vue:53-54`; `AnimationControlsGroup.css:52` + `:130-140`; `layout.css:49-51` (all re-read) | **KF.W6** — geometry derives from the stage box (cq units / the Card register), never viewport literals; joins KF-SKEL-6's cure |
| **KF-SKEL-9** | r2-miss 1 | Nine dead class names are emitted onto the live DOM: `controls-layout--stage-${stageMode}` (`AnimationControlsGroup.vue:6`, emitter re-verified) + the `controls-drawer--stage-*`/`controls-pane--stage-*` family, with **zero matching selectors** in demo source or the shipped bundle (both greps re-run: 0). C·D-4 built a MAJOR on the assumption these hooks are live; `usePaneRegister.ts:14` documents them as "the class driver" | this seat's greps; locus = AnimationControlsGroup / ControlsPaneWrapper, **not this component** | **KF.W5** — folds to those components' own tri-fold rosters (cross-component locus disclosed; booked here because this corpus surfaced it and its falsity decided K3) |

### Minors and info

| id | merged ids | claim (adjudicated, compressed) | disposition |
|---|---|---|---|
| **KF-SKEL-10** | D-8 / L-4 / C·D-2 *(ruling 5)* | Paint-only `background-position` sweep; `will-change` promotes nothing and **survives the PRM arm forever** (the arm resets only `animation`+`background`); the producer measured and rejected exactly this (K.WP P1-4 receipt, verified at the bytes). MINOR with the **U-24.2 fan-out escalator** named | KF.W6 (transform sweep arrives with delegation) · magnitude = SS-13 residue 2 |
| **KF-SKEL-11** | D-9 *(ruling 3)* | `1.6s` hardcoded ≈3.1× the installed house clock (`--duration-shimmer: 5s`), unreachable by `.motion-calm`/PRM `--motion-weight`; third local `background-position` shimmer `@keyframes` in one cascade. Era annotation mandatory (at authoring: 7% slower than the then-hardcoded 1.5s) | KF.W6 (tokenize or delegate) + **KF.W9** (PRM-mechanism-unification owns the motion-register reachability arm) |
| **KF-SKEL-12** | D-11 / D-12 / L-10 | No `forced-colors` and no `prefers-reduced-transparency` arm anywhere in the demo (repo-wide grep = 0); the installed primitive ships both; glass-ui's `accessibility.css` forced-colors block targets state selectors only and cannot reach a generic translucent plate (reader-1's falsifier check, adopted) | KF.W6 (both arms arrive with delegation) · render witness = SS-13 residue 3 |
| **KF-SKEL-13** | D-14 / L-7 / C·D-4-prop-half + r2-miss 3 | The `label` prop is dead API (sole call site `<SceneSkeleton />`, 3 references repo-wide — re-verified) and could not be heard if passed (KF-SKEL-1); AND the grammar is the banned legacy form — `DESIGN.md:202-206` §9.2 rules `withDefaults` legacy (verified verbatim); L-7's "the macro form is fine" cell is corrected: fine as Vue, wrong as house grammar (7 sites repo-wide per reader-2) | KF.W6 (any wave touching the file migrates to reactive-destructure defaults; the prop lives or dies with KF-SKEL-1's cure) |
| **KF-SKEL-14** | D-15 / L-8.1 / L-8.3 / C·D-7 | The docblock is false on four counts: "THE shared" (one consumer), "the skeletons tier" (deleted by `969990f6`), "a static dimmed plate" (**nothing dims** — the PRM arm touches only the sheen; plate background/border/shadow untouched), "matching the stage geometry" (KF-SKEL-8); five tranche ids unresolvable from inside keyframes.js | KF.W6 (docblock rewritten to the tree's truth by whichever wave touches the file first) |
| **KF-SKEL-15** | C·D-8 + r2 sharpening | No `delay`/minimum-display guard — a **twice-stated house contract** (`DESIGN.md:191-192` verbatim `delay: 150`; `U.G.md:250-252`) violated at `scenes.ts:104`; whether a warmed swap paints zero fallback frames is the live falsifier | NO-WAVE-OWNER (same app-shell seam as KF-SKEL-5) · warm-swap frame count = SS-13 residue 6 |
| **KF-SKEL-16** | L-9 / C·D-7 residue | The bare-text loading idiom the docblock claims retired is live at `TimelineHoverPreview.vue:17-18` (corrected anchor, re-read) + three `defineAsyncComponent` boundaries with no `loadingComponent` | KF.W5 (folds to those components' rosters; the U-24.2 skeleton roster is the eventual cure surface) |
| **KF-SKEL-17** | D-10 | The `--color-*` triplet is a 1-of-1 deviation (re-verified: exactly one file) against 45 raw-form occurrences in 16 files; not a live bug (all three bridges emitted — re-verified) but a live tripwire on `@theme inline` emission | KF.W6 token-namespace audit |
| **KF-SKEL-18** | r1-miss M4 | `clamp(1rem, 4vw, 3rem)` reads the **viewport** inside the layout this repo deliberately promoted to a container (`container-type: inline-size`, the cqi idiom documented in-file with two named precedents — re-read at `AnimationControlsGroup.css:12-23`); D·S-5's praise of the same line is inverted against the repo's own law | KF.W6 (cq units with the geometry cure, KF-SKEL-8) |
| **KF-SKEL-19** | D-16 / L-6 / C·D-9 (INFO) | inv-ζ: hand-rolled CSS motion in the engine's own demo — held at BOOK/INFO by standing precedent (`recap-deferred.md:270`, both readers + both challenge axes concordant). L-6's absolutist premise ("engine in hand every time") is **narrowed** by the catch-swallowed warm (K9) — which cuts both ways: it also strengthens the "fallback must not need the engine" carve-out | NO-WAVE-OWNER · recorded; the carve-out survives as the reason the sheen stays CSS |
| **KF-SKEL-20** | r1-miss M7 (INFO) | Token/prose drift in the D-1-deciding backdrop: three different numbers for two graph-opacity tokens (prose 12% · fallbacks 5%/12% · declared 3%/11% — all three re-verified) in a comment that calls itself a hard-gate legibility assertion | KF.W5 (EditorShell's roster; same defect class as KF-SKEL-14, one component up) |
| **KF-SKEL-21** | r2-miss 6 (INFO) | `@keyframes` collision safety is clean — Vue's SFC compiler hashes the name (`scene-skeleton-sweep-b3f6999c` in the bundle); four `background-position` shimmer keyframes coexist in one cascade without fighting. A checked non-defect, recorded | none needed |
| **KF-SKEL-22** | r2-miss 5 (INFO) | The challenge's contrast numerals drift from their own token sources (border 1.676/2.204 vs stated 1.63/2.46; sheen-peak 1.172 vs 1.08); thresholds and directions unaffected. Law: contrast figures are re-derived at use, never quoted | rides every KF.W6/W9 witness that touches contrast |

## Killed-claims register — 11 entries

| # | killed claim | adjudication |
|---|---|---|
| K1 | **D-1, the corpus's sole BLOCKER** — "the PRM branch erases the only perceptible element … a blank viewport indistinguishable from a hung app" | **KILL SUSTAINED, twice over, both mechanisms re-verified by this seat.** (a) D-1 grepped the wrong subtree: the painted substrate is one level up — `EditorShell.vue` renders the graph-paper `grid-background` (v-if defaulted **true**, App.vue passes no override — both re-read), so the 70%-plate *veils a visible texture* and its 1px border (~1.68:1 light / ~2.2:1 dark) is the strongest edge in BOTH states; a legible rectangle, not a blank viewport. (b) `background: none` is not even load-bearing: with `animation: none` alone, `background-position` reverts to initial `0% 0%`, and at `background-size: 220% 100%` the 50%-stop highlight core sits at 1.1W — **off the plate's right edge**; there is nothing frozen on-plate to delete. Residue survives inside KF-SKEL-14 (nothing dims; no progress cue) + SS-13 residue 4 (does the veiled rectangle *read* as loading?) |
| K2 | D-4 — "the plate does not read as a surface … and adopting the primitive discharges most of it" | **KILL SUSTAINED.** The ~1.05:1 fill is a property of the `--muted`/`--background` token ramp (the primitive's own opaque plate computes 1.06/1.17 — below the same 3:1 bar, with NO border and NO shadow); the 70% thinning costs Δ0.02; the prescribed cure would *delete* the component's only >1.5:1 element. Survives only as a design-system-ramp observation, booked nowhere against this file |
| K3 | C·D-4 — "`stageMode` is missing and the single silhouette is false for the MAJORITY (5 of 8)" | **KILL SUSTAINED on all limbs, re-verified**: 7 descriptors not 8 (subject = 4-of-7); home+cube resolve to a **statically imported** CubeScene (`App.vue:283-286` re-read) so only 2-of-5 suspendable destinations are `subject`; the stage-mode class hooks are DEAD (KF-SKEL-9 — 0 selectors in source and bundle, this seat's greps); and `square` is a subject scene WITH the stage card (`SquareScene.vue`, re-read). C's own falsifier fired on its own roster |
| K4 | D-5's mechanism clause — "rail·stage·rail … the 42rem cap collapses to 100%" | **KILL SUSTAINED at the bytes**: `grid-template-columns: [rail] var(--rail-track) [stage] minmax(0, 1fr)` — two columns, ONE rail (re-read); at the 72rem floor the stage cell ≈52rem − padding, so the cap **binds**. D-5's headline (unmoored magic numbers) survives as KF-SKEL-8 with the opposite-sign geometry (under-fill) |
| K5 | C·S★2 — "the reduced-motion arm is complete … avoids freezing a bright highlight mid-sweep" | **KILL SUSTAINED** — same geometry as K1(b): at initial position the highlight core is off-plate; `background: none` removes a faint sub-6%-ink edge ramp, not a frozen band. The arm is mechanically sound, cosmetically redundant, and leaves `will-change` standing (KF-SKEL-10). The corpus graded the same six lines a superlative AND a BLOCKER; neither reading survives |
| K6 | The copy-provenance framing (L §0/L-2 "the primitive, retyped" · C·D-5a "`105deg` is a fingerprint" · D-6's table framing) **and reader-1's "convergence is refuted" confirmation cell** | **KILL SUSTAINED AND EXTENDED (ruling 2, this seat's own git runs in both repos)** — the fingerprint postdates the demo file by ten days; at authoring-day glass-ui the gradient was srgb/muted-foreground/translateX. The reuse defect survives (KF-SKEL-3); the copy dies; the direction of flow is recorded as S-10 |
| K7 | L-5 — "PRM polarity is a regression relative to its own source" | **KILL SUSTAINED (ruling 4)** — the source's polarity at authoring was identical (`20b09bc7`, re-read). Present-day divergence folds into KF-SKEL-3's arms table |
| K8 | D-9's duration framing — "runs ~3× faster than the house skeleton clock" *as an authoring-time accusation* | **RESCOPED (ruling 3)** — true against installed 7.0.0 today (1.6s vs 5s), false for the era of authorship (7% slower than the then-hardcoded 1.5s). Row survives present-tense as KF-SKEL-11 with the era annotation |
| K9 | L-6's premise — "`CSSKeyframesAnimation` is in hand every time this component renders" | **NARROWING SUSTAINED** — L-6's own falsifier fires: `main.ts:50-54` catch-swallows the engine warm (re-read verbatim), so the fallback can paint with no engine. INFO verdict (KF-SKEL-19) unaffected; the absolutist cell is struck |
| K10 | Citation/provenance drift bundle: L-9's `:21`; C·D-3's AnimatedText precedent; L-2's "only commit"; D-10's "22 .vue sites"; D-1/D-4's numerals | **ALL SUSTAINED** (ruling 7; load-bearing ones re-derived by this seat: THP `:17-18`, CopyButton `:15`, the two-commit history). No substantive verdict moved by any of them |
| K11 | **NEW, this seat, against BOTH readers**: the fresh F-1 re-affirmation cells ("package.json … zero glass entries; lock 0 hits" presented as present-tense fact) and every remediation sequenced on the phantom-dependency premise | **KILLED at origin/master (ruling 1)** — `package.json:77` declares glass-ui 7.0.0 exactly; the local checkout is 41 commits stale; the census's SCH-1 had already ruled it and its top defect (substrate schism) predicted exactly this error. The instrument-critique half of L-3.3 survives (KF-SKEL-7); the F-1-gate on S-6 remediation is discharged |

## Superlatives — 11 sustained (L-18 runs both ways)

1. **S-1 — the sweep geometry is arithmetically exact** (D·S-3): `offset = P × (W − 2.2W)` puts the highlight core at −0.58W → +1.58W — full traverse, clean entry/exit. Both readers re-ran it; concordant. The same identity is what killed D-1(b) and C·S★2 — one piece of shown arithmetic decided three rows.
2. **S-2 — `overflow: hidden` at `:55` is load-bearing** (D·S-4, contradicting a prior pass): the sheen is a separate element; a parent's `border-radius` does not clip a descendant's paint without it. Survives a transform rewrite.
3. **S-3 — three axes, published self-kills** (D §5 KILLED-1..3 · C's inline killed claim · L §R): the D-axis expected a theme-blindness BLOCKER, killed it against the built artifact, and wrote down why. This seat re-verified the KILLED-1 decider (all three bridges emitted). The discipline held under two hostile re-reads: zero bad retractions in the corpus.
4. **S-4 — teardown-free by construction** (L·P-1): 25 script lines, 17 docblock; no lifecycle surface at all for a node that mounts/unmounts every keyed swap. The exact counterweight (TypingDots' three-guard engine path) is in the same tree — the inv-ζ cure would import a hazard this file structurally cannot have.
5. **S-5 — token-relative oklab color, zero dark-mode rules** (L·P-4): every color is `color-mix(in oklab, <token> N%, transparent)`; the chain to `light-dark()` is source-verified end to end.
6. **S-6 — every `var()` carries a fallback** (C·S★1): the file renders under a broken install. The F-1 premise that made this glamorous is dead at HEAD (K11), but the resilience property is real and must not be silently traded away by the delegation cure.
7. **S-7 — zero authored custom properties**: consumes tokens, contributes none to the flat `--kf-*` namespace. Correct citizenship even where the specific tokens are wrong.
8. **S-8 — the T.F8 structural contract holds exactly** (D·S-2): a real component in the `#fallback` slot, no `<Transition>`/`<KeepAlive>` wrapper — respecting the B.W3 blank-viewport lesson documented beside it. Structure right, design wrong; the corpus was right to keep them separate.
9. **S-9 — `aria-hidden` on the decorative subtree is the correct instinct** (L·P-3), stated with its sharp edge: it is also the mechanism that empties the live region; the fix is additive.
10. **S-10 — the demo shipped the house shimmer color language ten days before the design system did** (reader-2's find, verified by this seat in both repos — ruling 2). All three challenge axes inverted this into an accusation. Whatever else is wrong in the file, `105deg` + oklab-foreground was upstream of Glass 7, not downstream.
11. **S-11 — the colocation recommendation LANDED and the history proves it** (L·P-2): two commits, tier dissolved, file beside its sole consumer under the U-24.2 naming clause. Four tranche recommendations closed by one move.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

Law: every witness runs against **origin/master or later** (the 41-commit stale checkout is disqualified as a witness substrate — ruling 1); chromium and safari are separate evidence cells; contrast numerals re-derived at capture (KF-SKEL-22).

1. **SR speech capture** (KF-SKEL-1): NVDA/VoiceOver on a cold scene swap — does anything announce? The four silence mechanisms are source/spec-proven; only the audible outcome is open. Also witnesses the cure (persistent announcer above `.scene-host`).
2. **Raster attribution + magnitude** (KF-SKEL-10): DevTools Layers/Animations over one cold chunk fetch — settles the reader-1/reader-2 severity split against the K.WP 18-instance receipt. Re-run at fan-out if U-24.2's roster lands.
3. **Forced-colors + reduced-transparency render** (KF-SKEL-12): does the translucent silhouette survive Windows HCM? Absence of the arms is proven; the collapse is not.
4. **PRM-state legibility** (K1 residue): does the graph-veiling bordered rectangle *read as loading* rather than an empty card? The whole surviving question of the killed BLOCKER; perceptual, undecidable from source.
5. **Skeleton chunk cost** (KF-SKEL-3): whether Rollup shakes `Skeleton` free of the 20,037-byte data-table chunk under `sideEffects: ["*.css"]` — no build was run under lane law by any seat.
6. **Warmed-swap fallback frames** (KF-SKEL-15): C·D-8's own plausible falsifier — a pre-warmed swap may paint zero fallback frames, which would rescope the delay-guard row to cold loads only.
7. **M1 blast radius** (KF-SKEL-4): enumeration of which glass-ui components visibly change corner at `--radius` 0.25rem — belongs to the **BH/BI relay**, not to this component's file; recorded here so the relay letter carries the bundle-offset proof.

## Closing verdict

Both reader returns survive the incredulity seat **with their verdicts largely intact and their two deepest instincts vindicated** — the corpus's sole BLOCKER stays dead (two independent refutation mechanisms, both re-verified), and the misses they surfaced (the radius-layer defeat, the wrong-plate echo, the transient-live-region mechanism, the dead stage-mode classes) are the rows that re-price every cure the three challenge axes prescribed. But the seat was needed at exactly the two places the readers agreed most confidently: **both re-affirmed F-1 against a 41-commit-stale checkout** that the census had already ruled falsified at HEAD (K11 — the substrate-schism class reproduced inside a single component's audit), and **reader-1 confirmed the copy-provenance framing that the bytes refute** — the alleged fingerprint postdates the demo file by ten days, in the other repo's own history (K6). One reader silently dropped a row instead of ruling it (L-5); the other's era-kill of D-9 was half right and is carried as an annotation, not a kill.

The component's adjudicated shape: **a structurally correct, teardown-free, token-citizen fallback whose every design promise is false** — it announces nothing, dims nothing, matches no geometry, reads one dead token and three detuned ones, and reimplements-without-tracking a primitive whose own packaging defeats its own radius scale. Zero blockers; the harm today is bounded by one mount site and one chunk-fetch lifetime; the significance is the **fan-out** (a broken contract canonized as the house skeleton spec) and the **cure inversion** (delegating to the bare primitive makes the silhouette and the PRM state worse — the right target is the demo's own stage-card register). Routings: **KF.W6** owns the component's cure surface whole (delegation-via-Card KF-SKEL-3/-6/-8/-18, tokens KF-SKEL-2/-11/-17, a11y wrapper KF-SKEL-1, grammar+docblock KF-SKEL-13/-14, plus the cascade-coupling probe KF-SKEL-7); **KF.W5** takes the adjacent-component folds (KF-SKEL-9/-16/-20); **KF.W9** takes the PRM/motion-register unification arm and every live witness; **KF.W0** takes the re-anchor law; **GLASS-OWNED/BH-relay** takes KF-SKEL-4 with the bundle-offset proof attached; **NO-WAVE-OWNER** carries the app-shell seam (KF-SKEL-5/-15) for SS-1/SS-2 to consume. Nothing lost: head, tail, or interval.

— end of apotheosis. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; X·KF execution awaits the owner's begin-word.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/app/App.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **App.vue=+8/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**CORRECTION (iii) of the six named at G-0.9 — KF-SKEL-7's cascade-level coupling arm is RECORDED BESIDE THE IMPORT CENSUS** (C-22 (iii) / C-1.F row F-13). Banked ⟨`kf-App.skeleton.md:42`⟩: *"the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census."* The arm is C-9's legend rider and lands in the census re-anchor at `lane-frontend.md` (KF.W0.d's motion, same wave); **its token-namespace probe half stays KF.W6's** and is not booked here.

**KF-SKEL-2 re-derived in the FRESH build**: ⟨`grep -c -- '--shadow-glass:' dist/gh-pages/assets/index-CBB2Hr7m.css`⟩ → **0**. The dead-token finding HOLDS at a dated, hashed artifact instead of a Jul-16 one.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

