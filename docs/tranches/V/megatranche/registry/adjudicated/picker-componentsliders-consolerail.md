# picker-componentsliders-consolerail — adjudicated apotheosis · seat model receipt: `claude-fable-5` (Fable 5, observed in this seat's own system prompt — M-23/L-18)

**Seat**: fresh Fable adjudicator (the incredulity seat) · **Date**: 2026-08-07 · branch `tranche-u` · HEAD at adjudication `ee9474e4`.
**Corpus**: 26 accusation rows (C-1..C-7, D2-01..D2-15, L-1..L-5; r2 governs — `challenge-D-design-r2.md` is the D authority, r1 D is Codex report-authored and re-adjudicated inside it) against `demo/picker/controls/ComponentSliders/ConsoleRail.vue` (329 lines, SHA-256 `a37d644b…b6e279` — **re-hashed by this seat, byte-identical to all three challenge receipts and to the r2 receipt**, so every line anchor below remains valid at today's HEAD) + the parent `ComponentSliders.vue` (395 lines, `a61b5ed3…82ef327`, re-hashed — cited by the corpus, audited by none of it).
**Re-readers adjudicated**: design-and-usage · library-and-code (both hostile re-reads of the full corpus).
**Method**: both returns were attacked before either was adopted. Every verdict-level disagreement (D2-07's loudness limb, C-2 severity, C-5 severity, L-2, L-4, the D2-02 cure, the D2-11 kill grounds, reader-1's prefers-contrast "sole consumer" claim, reader-2's "identical expression" and "every other press" cells) was re-derived against the tree by this seat with its own instruments. Both BLOCKERs and every missed-MAJOR were re-traced from source, not trusted.
**This seat's own instruments (zero browser probes — probe parsimony; every ruling decidable from source + the committed receipts, which were re-read RAW, not via either reader's quotation)**: full reads of `ConsoleRail.vue` (whole) + `ComponentSliders.vue` (whole) + `composables/useSliderTouchGates.ts` (whole); `useSliderGradients.ts:70-94`; `colorSpaceInfo.ts` key census (13: rgb hsl hsv hwb lab lch oklab oklch xyz kelvin ictcp jzazbz hex) + the kelvin/ictcp/jzazbz `components[]` literals; `picker-color.ts:38-72` (all 17 `PICKER_CHANNELS` entries walked); `useColorPipeline.ts:110-126`; `readoutReservation.ts:45-64`; `useContrastSafeColor.ts:60-80`; `color-model.ts:25-40`; `demo/styles/foundation.css:585-760` (the `@layer base` boundary :590-635, the forced-colors block :678-721 whole, the prefers-contrast block :728-750 whole); `demo/styles/focus-ring.css` (whole); `demo/styles/animations.css:25-54,175-200`; receipts read raw: `probe-11-register-loudness.json`, `probe-06-dot-focus-glyphs.json`, `probe-12-touch-register.json`, `probe-09-spaces-verified.json` (structure + four label-gated arms); `motion-quarantine.md` rows M-24..M-27 + §6; `ROOT-FINDINGS.md:1665-1680` (MT-F042 verbatim); `DEFECT-LEDGER.md` rows at :1047/:1083 (the mask booking), :8846 (four-homes census), :10024 (the roving-tabindex house-reference), :38531 (D-14 rail-label row), :41293 (epoch census); `CARRY-CUT-LEDGER.md` §1 whole (the X-wave authority); `layout-gestalt-worker-o.md:537` (G-4); `om-14-formatting/FORMAT-AUDIT.md:165` (D-f); installed glass-ui 7.0.0 dist: `styles/tokens/sizing.css` (`--dock-touch-target: 2.75rem`, `--dock-control-press-bg: color-mix(in oklab, var(--glass-bg-resting), var(--foreground) 7%)` — both verbatim), `styles/tokens/scheme-spring.css` (`--spring-press-settle: 0.12s` → `--spring-press-duration: calc(0.12s * var(--motion-tempo))`), `components/dock/DockLayerGroup.vue.d.ts` + `dock/styles/layer-group.css` (the producer dock **emits `role="tab"`** — new fact, corrects one missed row); repo greps: `role="tab"` markup census (ConsoleRail.vue:29 is the sole demo-authored site; foundation.css hits are selectors), `aria-controls` in the SFC = 0, `role="tabpanel"` in demo = 0, `forced-colors` in the ComponentSliders folder = 0, `scrollIntoView` = 3 sites (ComponentSliders.vue:213, useHeightTransition.ts:48, and EasingSpecimenStrip.vue:40 which *documents the house ban*), `TabsTrigger|<Tabs` in demo = 0.
**Motion law**: no FALSE motion-quarantine row rides in this record. **M-24 (TRUE)** is cited under D2-08/C-6. **M-26/M-27 are FALSE** and bar only r1 D's :21b reduced-motion limb and its Kronecker cell — D2-03 asserts the *inverse* of that claim (the press transform renders ONLY under reduce) and is independently corroborated by M-26's own evidence item (3); the high-contrast/forced-colors limbs are explicitly left surviving by motion-quarantine.md:264, so D2-02's escalation is lawful. **M-25 (UNPROVEN)** is not carried; D2-07's loudness table is a new r2 claim, adjudicated on its own receipts below.
**Status discipline**: every disposition below is ADJUDICATED, not VERIFIED — nothing here stamps a wave. Line anchors are valid at the re-hashed bytes; re-resolve before executing any cure (D-19).

## Scoreboard

**21 CONFIRMED (2 BLOCKER · 9 MAJOR · 6 MINOR · 4 INFO; D2-07 and D2-15 rescoped, C-6 folded into D2-08, L-3/L-5 FOLDED onto already-booked identities) · 8 KILLED (C-1, C-3, C-7, L-4, D2-11 whole; plus three sub-claim kills: the §2 press-register table, D2-07's "focused-but-unselected unreachable", and the §8 D2-02 cure) · 13 MISSED rows adjudicated in (3 MAJOR + 1 corpus-method MAJOR · 8 MINOR · 1 INFO).**

Blocker-weight survivors: **D2-01** — the tablist ships zero selected tabs at boot and after every space change, the sole indicator absent, **owner-marked (MT-F042 arm 1) and still open**; and **D2-02** — zero focus indicator under forced-colors while `:focus-visible` matches, whose TRUE mechanism (a missed-MAJOR, verified whole by this seat) is that the repo's own WHCM register already names `[role="tab"]` and the rail **defeats it** with `outline: none` at higher specificity — which kills the r2's own proposed cure.

## Ruled disagreements (the adjudication's actual work)

| row | reader A (design-and-usage) | reader B (library-and-code) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D2-07 loudness limb** | CONFIRMED with "hover 2.22× / focus 1.97× louder than selection" | CONTESTED: the doc's own receipt carries an opposing intensity metric; "loudness" never defined | **RESCOPED — B wins the metric, A keeps the row.** CONFIRMED MAJOR on the two byte-provable limbs (one silhouette for cursor and state; the selected item's hover response dead by inline-style precedence, ConsoleRail.vue:34 over :264). The "2.22×" framing is DEMOTED: it is true of changed-AREA only. | `probe-11-register-loudness.json` read raw by this seat: dot `{changedPx 7037, pct 11.11, maxChannelDist 147.2}` vs hover `{15611, 24.64, 103.9}` vs focus `{13840, 21.84, 109}` — area says hover 2.22× louder, per-channel intensity says the DOT is the strongest change (147.2 > 103.9). Two metrics, opposite orderings, neither privileged by the doc. The design defect that survives needs no metric: hover pill and selected dot share box, radius (`--radius-pill`, :251-252), and scale; the inline `:style` outranks `:hover { color }` unconditionally. |
| **§2 press-register table (r2's own)** | KILLED — press is the LOUDEST measured register | (not separately ruled) | **KILLED — A's kill adopted and re-verified.** The r2 §2 table records press as "0 — transform never leaves identity / DEAD / DEAD" and concludes a phone vocabulary of "rest and dot". Its own receipt refutes the table: only the TRANSFORM leg is dead (that is D2-03, which stands); the background leg is the largest measured delta in the file. Phone vocabulary = rest + press-fill + dot. | `probe-11` `R_press_beyond_hover: {changedPx 27814, pctOfRail 43.9}` with the author's own isolation note, read raw; `--dock-control-press-bg` verified a real producer token (glass-ui `sizing.css`, verbatim `color-mix(in oklab, var(--glass-bg-resting), var(--foreground) 7%)`); `cursor: pointer` at ConsoleRail.vue:252 (so `:active` fires on iOS). |
| **D2-02 cure (r2 §8)** | KILLED — "one forced-colors `outline` fallback in the shared register closes all five" is wrong; the register exists and is defeated | CONFIRMED the finding; did not rule the cure | **KILLED — A wins; verified whole by this seat.** `foundation.css:678-721` ships, UNLAYERED (`@layer base` closes at :635): `@media (forced-colors: active) { :where(…button, [role="tab"], [role="slider"]…):focus-visible { outline: 2px solid Highlight; outline-offset: 2px } }` (:702-719), with its own comment (:699-701) stating the `:where()` specificity-0 is deliberate. `.channel-rail-item:focus-visible { outline: none }` (ConsoleRail.vue:271-273) scores (0,2,0) over the register's (0,1,0) — inside forced-colors too (WHCM forces colors, not `outline-style`). Adding another fallback to that register changes nothing. The sound cure is MISS-3's: stop emitting unconditional `outline: none` at the five sites, or gate it out of forced-colors — the working in-repo model is GradientStopEditor.vue's own `@media (forced-colors: active)` block. Same MT-CSS-A law as the ColorSpaceSelector L-3 ruling: **the cure is adjudicated separately from the defect** — the r2's finding survives while its routing would have shipped a no-op under a green-looking edit. | My own read of foundation.css whole; specificity arithmetic from bytes. |
| **D2-11** | KILLED (alpha-vs-opaque category error; witness contradicts) | KILLED (same, plus: the probe's LIGHT-mode composited pixels contradict the "near-equal in light" premise too) | **KILLED — unanimous; B's sharper ground adopted.** Comparing an un-composited 0.6-alpha ring L (0.958) against an opaque ink L (0.878) proves nothing about rendered weight; no dark-mode composited pixel read exists in evidence-r2/. The structural residue (flat `color-mix` ring, ConsoleRail.vue:233, vs live-probed per-scheme letters :134-143) is real and re-files as UNPROVEN-NEEDS-LIVE at INFO weight. | The cited values are a computed-style read of `color-mix(in oklab, var(--accent-view) 60%, transparent)` — 60% alpha by construction (my read of :233). |
| **C-2 severity** | INFO (inert retention; remount re-registers every current key) | MINOR (~20 detached buttons retained for the session) | **INFO — A wins.** The retention is real (`:ref` callback :27 guards `if (el)`, swallowing Vue's null unmount call) and bounded (~20 keys); but `onRailKeydown` reads only `next ∈ components` (:216-219) and `:key="animationKey"` (:20) remounts fresh buttons per space, so no stale element is ever focusable. No observable consequence in the rail. The SAME pattern in the parent (MISS-5) DOES have teeth and takes the MINOR. One cure closes both: delete-on-null in both ref callbacks. | My trace of both ref callbacks + the keydown reader. |
| **C-5 severity** | MAJOR | MINOR | **MINOR as the code row, with the user-facing totality carried at MAJOR by MISS-1.** The bare render (`{{ currentColorRanges[component] }}`, :75) with a provably absent `alpha` key (`useSliderGradients.ts:82-88` reduces `PICKER_CHANNELS` only; alpha is appended separately at ComponentSliders.vue:123) is one code defect; the MAJOR weight belongs to the whole degenerate explanation surface (MISS-1), where this is one cell of many. Booked once each, cross-referenced. | My read of both files; `PICKER_CHANNELS` carries no `alpha` in any of 17 spaces. |
| **L-2** | INFO (house convention; parent identical) | MINOR (unnamed TypeError on missing provider; silent 0.5 fallback = second visual path) | **RETIRED-AS-OBSERVATION (INFO), precedent-ruled.** The ColorSpaceSelector apotheosis ruled the identical two-key posture (hard-assert `COLOR_MODEL_KEY` beside null-tolerant ambient keys) "a deliberate two-key policy difference … an observation, not a defect" — the provider is installed at every route and the parent uses the same idiom (ComponentSliders.vue:107-117). B's facts are accurate and preserved; the revival trigger is unchanged: a consumer mounting outside the provider revives the row with a crash witness. | Precedent (adjudicated/ColorSpaceSelector.md L-5(b)) + my read of both inject sites. |
| **L-4** | KILLED (taste prescription; no defect; contradicts the booked P5 seam) | Parked UNPROVEN (prescriptive, not truth-apt; its target conflicts with L-1's own facts) | **KILLED — A's label, B's reconciliation adopted as a rider.** A challenge that prescribes a refactor without naming a failure has not established a defect; the ~35-line roving-tabindex machine it wants split out is the repo's OWN reference implementation (`DEFECT-LEDGER.md:10024`: "That machine already exists in this repo at ConsoleRail.vue:183"). The rider survives on CC-113: the file's booked P5 producer-primitive swap (:2-4) and L-1's inject-heavy dependency shape are in genuine tension (a producer primitive cannot inject demo keys) — reconcile at the TR#84 ask, not as a defect row. | My reads of :2-4, :183-220, the ledger row. |
| **Missed prefers-contrast row's scope** | "the rail is its only target … the four rail buttons are the rule's sole consumers in the served DOM" | (row not filed by B) | **RESCOPED — this seat's own correction to A.** The rail half is CONFIRMED: `foundation.css:747-749` bumps `[role="tab"] { border-width: 2px }` under `prefers-contrast: more`, and the rail buttons declare `border: 0` (ConsoleRail.vue:247) — the shorthand zeroes `border-style`, so a width bump can never paint there. The "sole consumer" half is FALSE: A's grep covered demo-authored markup only, but installed glass-ui's **DockLayerGroup emits `role="tab"`** ("the rail is a plain `role="tablist"` of `role="tab"` buttons" — its own d.ts), and the demo mounts dock layers. The elevated-contrast rule is dead ON THE RAIL; whether it paints on the dock tabs depends on producer border styles, undetermined here. | `DockLayerGroup.vue.d.ts:14` + `dock/styles/layer-group.css` in installed dist (my grep); `demo/shell/dock/` usage. |

## Adjudicated defect roster — confirmed corpus rows

Terminal dispositions route to the tranche-X waves per `registry/CARRY-CUT-LEDGER.md` (X-W4 = semantic controls, the CC-003 glass-independent partition; X-W6 = instruments, incl. CC-068 = the folded MT-CSS-1 catalog wave; X-W8 = graph subtraction; X-W9 = library apotheosis; X-W10 = canonical design/motion law; X-W1 = re-gate; CC-113 = the MT-F042/TR#84 glass-blocked bank). No row lacks an owner.

- **D2-01 — CONFIRMED (BLOCKER) → X-W6** (glass-independent; must not wait on CC-113). Boot and every space change render a `role="tablist"` with zero `aria-selected="true"` and zero `.rail-dot-seat` — the component's declared sole indicator absent in the state every user first sees. Mechanism byte-certain: `ComponentSliders.vue:135` `ref<string | null>(null)` + `:138` `watch(currentColorSpace, () => { activeComponent.value = null })`; the seat gates on `v-if="active === component"` (ConsoleRail.vue:53); `activeComponent` has exactly two non-null writers (:79 slider drag, :211 rail select), never a seed. 14 label-gated arms all `anySelected:false` (probe-09, read raw). **Owner-marked**: MT-F042 arm 1, ROOT-FINDINGS.md:1665-1675 verbatim ("should default to the first selected component to have a water color dot"). Cure shape (seed to `componentEntries[0][0]` at mount + in the watcher) is structurally sound — `componentEntries` is declared before `activeComponent` and the pre-flush watcher sees the new space's list — and it discharges MISS-2 in the same edit. Rider: MT-F042 arms 2-3 (loupe grab/grow/bounce) stay **CC-113 BLOCKED-ON TR#84**; whether "channel 0" is the right default semantics for the loupe is an owner call (UNPROVEN §8).
- **D2-02 (+ MISS-3, one cure; + D2-15 residue) — CONFIRMED (BLOCKER) → X-W4** (the five-site `outline: none` extirpation; oracle extension → X-W1: the o27 forced-colors arm reaches only the gradient rail today). Under `forced-colors: active`, `:focus-visible` matches and nothing paints: `outline: none; box-shadow: var(--focus-ring-shadow)` (ConsoleRail.vue:271-275) removes the property WHCM preserves and keeps the one it strips — against the repo's own written law (focus-ring.css:31). The TRUE mechanism is MISS-3 (below): the shared WHCM register already exists and names `[role="tab"]`; the rail defeats it at (0,2,0). Receipts: probe-06 `forcedColorsFocus {focusVisible:true, boxShadow:"none", outline:"3px none"}`, read raw. WCAG 2.4.7 fails determinately. The compounding "the dot escapes forced-color adjustment" limb rides UNPROVEN (computed-style is not a paint oracle in WHCM). Escalation lawful: motion-quarantine.md:264 leaves the forced-colors limbs unadjudicated.
- **D2-03 — CONFIRMED (MAJOR) → X-W10** (a demo-wide motion-law row, CC-099-adjacent — the class-level `.stagger-children` fill contract, not a rail patch). The tokenized `:active` press scale (ConsoleRail.vue:267-269) never renders in the default motion mode: `.rail-letters` carries `stagger-children` (:21) whose direct children are the four buttons (reka Tooltip wrappers are renderless; `as-child` at :25); `animations.css:43-46` sets `animation: stagger-child-in … both` inside `no-preference`, and a FINISHED fill-both animation keeps `to { transform: translateY(0) }` (:36) applied from the animation origin, which outranks author `:active` declarations — permanently, re-armed each space change by `:key="animationKey"` (:20). Measured cross-engine (probe-02/probe-03: transform pinned at identity in webkit/chromium/forced-colors/dark; `matrix(0.96,…)` under `reduce` alone). So the press scale is a reduced-motion-exclusive feature — an inversion, NOT a PRM violation in either direction (M-26's three guards all verified real). Secondary confirmed from producer bytes: the comment's "0.16s" (:259) is false — `--spring-press-settle: 0.12s` in glass-ui scheme-spring.css. Blast-radius census UNPROVEN (§6): the named sibling `.channel-rows` has transform-less children — the demo-wide claim in r2 §8 was not run.
- **D2-04 — CONFIRMED (MAJOR) → X-W6** (the rail joins the parent's own T-59 one-law rhythm; canon echo at X-W10). Rail pitch 39.80 px vs rows 43.50 px, monotonic drift +4.97…−5.59 over four rows against a 24 px glyph; worst in ratio at Kelvin (36.11 vs 43.50 = 17.0%). Both distribution laws in live bytes (rows: ComponentSliders.vue:328-333 clamp regime; rail: ConsoleRail.vue:21 `justify-around`, no gap, box shrunk 6 px by :233-235). The arithmetic closes independently: rows 166.32/4 + 7.68/4 = 43.50 ✓; rail (166.33−6)/4 = 40.08 vs 39.80 measured (glyph-centre vs box-centre). The parent DECLARES the one-law regime at :318-327; the rail — the element whose job is to name the rows — is its only non-consumer.
- **D2-05 — CONFIRMED (MAJOR) → X-W6.** The "persistent chassis" (stated twice: ConsoleRail.vue:10-11, ComponentSliders.vue:19-24) takes 8 distinct widths across 14 verified spaces, 30.53→41.23 px = +35.0%, because the rail is an intrinsically-sized flex sibling (ComponentSliders.vue:32) with no width reservation anywhere in :232-236 — every space change translates the whole slider column. Cure shape: reserve the widest-glyph inline size (ch-based) once; D2-12(b)'s Jzazbz outlier is the largest single contributor.
- **D2-06 — CONFIRMED (MAJOR) → X-W6** (rest-state clearance) **+ CC-113** (the budget re-derivation is a CONSTRAINT on the TR#84 ask — the relay letter must carry it, or the producer inherits an impossible geometry). The dot paints over and slightly outside the enclosure: probe-06 (read raw, arithmetic reconciled by this seat: contentBox 24.53 = 30.53 − 2×1 − 2×2 ✓; seatBox 26.53 = content + `inset:-1px` ✓) gives painted span 235.03–265.70 against a border box ending at 265.00; clearance −0.51/−0.76 px. Three causes in bytes: `.rail-dot-seat { inset: -1px }` (:290-296), the horizontal die-rim's `padding: 2px` reused portrait (:233-235), and filter displacement. MT-F042's "grows slightly larger on grab/select" has negative headroom at rest. The filter-bleed attribution and the arm-2/3 impossibility claim ride UNPROVEN (§4, §5) — the negative clearance itself does not.
- **D2-07 — RESCOPED → CONFIRMED (MAJOR) → X-W6** (register hierarchy; canon echo X-W10). Ruling above. Surviving limbs, byte-proven: hover and selection share one silhouette at one scale (only hue differs — witness shot-P), and the selected item cannot respond to hover (inline `:style` :34 > `:hover` :264; the hover fill paints behind an opaque dot covering the padding box +1px). Demoted: the "2.22×/1.97× louder" framing (area-only). KILLED sub-claim: "focused-but-unselected is structurally unreachable" — see the register.
- **D2-08 (+ C-6, one identity — the C row is the same defect from the implementation side) — CONFIRMED (MAJOR) → X-W4.** `role="tablist"`/`role="tab"`/`aria-selected` with zero `aria-controls`, zero `tabpanel` anywhere in demo-authored markup (my greps: 0 and 0), and no panel consequence: probe-10 measures 0 changed px in `.channel-rows`, no scroll (`scrollIntoView({block:"nearest"})` is a definitional no-op with all rows in view — ComponentSliders.vue:210-214), focus does not reach a slider. **M-24 (TRUE)** cited. Framing tempered per reader B: selection DOES move the dot and set `activeComponent` — the rail's stated job — so the load-bearing defect is the ARIA pattern promise, decidable from source: the honest pattern is toolbar/radiogroup, or real panel semantics. Cure adjudicated separately from the defect: if D2-01's seed lands first, a `role` change must not orphan the seeded-selection semantics — one wave, one decision.
- **D2-09 — CONFIRMED (MINOR) → X-W6** (rides MISS-1's explanation-surface redesign). On touch (probe-12, raw): tap → `tooltips: []` (selection only); long-press alone opens the anatomy tooltip; `anyHover:false` at 390×844 — and the file itself designates this tooltip "the static range's retirement home #1" (:69-72). The retirement home's door is undiscoverable on the input class that most needs it. Compounds MISS-1: on the α row what the long-press reveals is the word "alpha" over a blank line.
- **D2-10 — CONFIRMED (MINOR) → X-W4** (CC-040's target-size class; one identity with layout-gestalt **G-4**, which already books ConsoleRail.vue:323 as "the 7th viewport media rule" and seats the coarse-pointer control rung producer-side). Arithmetic exact from bytes: `min-width: calc(var(--dock-touch-target, 2.75rem) * 0.72)` (:326) with the producer token verified 2.75rem → 31.68 px against the comment's "≥44px hits" (:321-322); measured 31.67×44 (probe-12 itemBox, raw). Doctrine split verified: `@media (max-width: 1023px)` (:323) vs the sibling's `@media (pointer: coarse)` whose comment (:342-345) bans width-keyed gating by name. Clears SC 2.5.8 (24×24); fails its own comment.
- **D2-12 — CONFIRMED (MINOR) → X-W6** (glyph registry; the face question rides the recorded C-4-face bracket + relay). (a) the α item is 1.1 px taller (probe-01: 24.52 vs 25.62) — the deterministic cost of the otherwise-correct S-3 mono-upright cure (:316-319) under `line-height: 1` (:249). (b) `ᴢ` (U+1D22 ×3, :162) renders full-size where `ᴛ`/`ᴘ` (U+1D1B/U+1D18, :161) render small — the width consequence is measured (Jzazbz railW 41.23, the widest, feeding D2-05); the font-coverage mechanism is UNPROVEN (needs a cmap dump). The deeper notation defect is MISS-11.
- **D2-13 — CONFIRMED (MINOR) → X-W10** (format/legibility law; one identity with FORMAT-AUDIT row D-f at :165, verified — same line, now two findings). ConsoleRail.vue:75 `opacity-60` on the range caption vs the sibling's own law forty lines away (ComponentSliders.vue:305-308 verbatim: "never italic, never opacity — the t-2000-41 legibility class"). The doctrine-inconsistency half is source-proven; the measured-contrast half rides UNPROVEN (§2).
- **D2-14 — CONFIRMED (INFO) → X-W6** (correct the record in the wave that touches the file; feeds CC-113's ask authoring — a reader implementing MT-F042 against the current header inherits a dead voice). The four-voice model (:8-9, "ring says WHICH") names a ring T-28's ABROGATE law removed (:286-289 records the removal; `.rail-dot` :297-300 carries only width/height). Three voices ship, or the prose is stale — either way the header must match the build before the loupe is authored.
- **D2-15 — RESCOPED → CONFIRMED (INFO) → X-W4** (one focus doctrine; rides D2-02's cure). TRUE that two recipes coexist (`--focus-ring-inner/outer` with exactly one consumer vs `--focus-ring-shadow` with five); but reader B's correction is adopted: focus-ring.css:4-6 scopes itself to its two consumers in the same sentence the r2 elides ("the gradient stop handles + remove chip today") — it does not claim universal adoption. The operative fact is MISS-3's: the shared WHCM register is foundation.css's, and the five `--focus-ring-shadow` consumers all defeat it the same way.
- **C-4 — CONFIRMED (MAJOR) — one identity with CC-081's K-10 ICtCp row (ledger-routed → X-W9) ∩ CC-068's catalog totality (→ X-W6); CROSS-BOOKED, not duplicated — the cure lands wherever the catalog does, and both waves must cite this row so neither drops it.** `componentDescription` (ConsoleRail.vue:176-179) prefix-matches after the `any` cast: for `cp`, upper = "C" and `"Ct (tritan)"` is found first (colorSpaceInfo.ts ictcp `components` verbatim, re-read by this seat) — the Cᴘ tooltip describes the tritan axis; `ct` is right only by array order. This seat re-walked the whole function domain and confirms reader B's amplification: `jz` → nothing starts with "J" or "jz" (`["Lightness (Jz)", …]`) → the literal "jz"; `kelvin` → `["Temperature (K)"]` starts with "T" → the literal "kelvin"; `alpha` → "A" matches nothing in any of the 13 entries → the literal "alpha", in all 18 spaces.
- **C-5 — CONFIRMED (MINOR, ruled above; the totality carried by MISS-1) → X-W6 (CC-068).** `currentColorRanges[component]` rendered bare (:75); the map provably never contains `alpha` (useSliderGradients.ts:82-88 reduces `PICKER_CHANNELS` only, verified — no alpha key in any of 17 entries; the parent fabricates alpha separately at ComponentSliders.vue:123). The α range line is empty in every space, always.
- **L-3 — CONFIRMED (MAJOR) — FOLD, identity preserved: this is the exact cast the ColorSpaceSelector apotheosis already books for deletion (its G2 names `ConsoleRail.vue:174` verbatim; DEFECT-LEDGER :1047/:1083 book it as a "mask") → X-W6 (CC-068).** Reader B's amplification ADOPTED as a cure ruling: the `any` is LOAD-BEARING — `colorSpaceInfo` has 13 keys against `DisplayColorSpace`'s 18, so deleting the cast alone does not typecheck; challenge-C's closure clause ("Source closure also requires removing `any`") is not a sound cure until the catalog is total. The interim-correct pattern already ships in the sibling consumer: ColorNutritionLabel.vue's `space in colorSpaceInfo ? … : colorSpaceInfo.rgb`. The adjacent `as DisplayColorSpace` (:173) is inert (this seat verified `currentColorSpace` is already `PickerSpace` via `resolveColorSpace`, color-model.ts:32-34).
- **L-5 — CONFIRMED (MAJOR) — FOLD onto the four-homes identity (DEFECT-LEDGER:8846, verified: SPACE_SCHEMA · PICKER_CHANNELS · colorSpaceInfo `components[]` · ConsoleRail `SPACE_GLYPHS` :156-164) → X-W6 (CC-068).** The drift is realized, not hypothetical: 5 selectable spaces missing from the description registry, `jz`/`kelvin`/`alpha` unmatchable, `cp` mis-matched, alpha rangeless — three registries, three coverage sets, one 18-member selectable domain.
- **L-1 — CONFIRMED (MINOR) → rider on CC-113 + X-W6.** Three props (:103-110) against five ambient arrivals (`inject(COLOR_MODEL_KEY)!` :114-115, `INK_AMBIENT_KEY` :128, `useGlobalDark` :127, `useBreakpoint` :118, module-scope `colorSpaceInfo` :99) plus the `bumpProbeEpochOnMount()` side effect. A known seam, not an accident (:2-4 books the P5 swap) — but reader B's sharpening stands: this dependency shape FORECLOSES the booked producer swap (a producer primitive cannot inject demo keys). Reconcile at the TR#84 ask (with L-4's rider).
- **L-2 — RETIRED-AS-OBSERVATION (INFO), precedent-ruled** (table above). Revival trigger preserved.
- **C-2 — CONFIRMED (INFO, ruled above) → X-W8** (one hygiene cure with MISS-5/MISS-13: delete-on-null in both ref callbacks).
- **Identity-guard note (no new row)**: ConsoleRail.vue:58 `tag="div"` on the WatercolorDot is one of the 20 dead-`tag=` sites already FOLDED by the ColorSpaceSelector apotheosis's L-8 residue sweep — recorded here so the sweep list demonstrably includes this file; neither reader named it.

## Missed rows — adjudicated in

- **MISS-1 — MAJOR — the rail's SOLE explanation surface is degenerate or actively WRONG in 8 of 18 selectable spaces, plus the α cell in all 18 → X-W6 (CC-068 — the catalog totality wave must count the TOOLTIP as a render site and must author alpha as a first-class catalog row, or this surface stays degenerate after the About card is cured).** Both readers found it independently; this seat re-derived the whole cell table from source: (1) `srgb-linear`/`display-p3`/`a98-rgb`/`prophoto-rgb`/`rec2020` — `info` undefined → the tooltip title is the raw channel key ("r"); (2) `jzazbz.jz` → "jz"; (3) `kelvin.kelvin` → "kelvin"; (4) `ictcp.cp` → the WRONG description (C-4); (5) `alpha` → the word "alpha" over an EMPTY range line (C-5), in every space — for the one glyph the file spends fourteen comment lines giving its own typographic identity, and the one channel whose range is not inferable from its symbol. Evidence-selection fact, preserved as method data: the r2 seat's only two tooltip captures both sampled Lab's `a` — a working cell — while its own arms drove Display P3 and Rec. 2020 without opening the tooltip there.
- **MISS-2 — MAJOR — `aria-selected` and the roving `tabindex` use two definitions of "current": in the shipped boot state the rail exposes a tablist with ZERO selected tabs and `tabindex=0` on a tab whose `aria-selected` is false → X-W6 (dies with D2-01's seed — an argument for the seed cure over a bare role swap; cross-cited at D2-08's X-W4 row so the two cures are decided together).** ConsoleRail.vue:30 has no fallback; :187 (`active ?? components[0]`) has one. When `active === null` — every boot, every space change — the two attributes disagree on the same element. WAI-ARIA requires exactly one selected tab. The evidence sat in the r2's own probe-00 dump (`selected:"false", tabindex:"0"`); the C axis filed the unreachable non-null branch instead (C-1).
- **MISS-3 — MAJOR — the repo's WHCM focus register already exists, already names `[role="tab"]`, and the rail (plus four siblings) DEFEATS it with unconditional `outline: none` at higher specificity → X-W4 (this is D2-02's real mechanism and cure; + X-W1 oracle).** Verified whole by this seat (ruled-disagreements table). The same defeat holds at ColorSpaceSelector.vue, ColorComponentDisplay.vue, GradientEasingEditor.vue (×2); GradientStopEditor is the sole law-conformant control. Consequence adjudicated: the cure is at the five sites (or a specificity raise on the shared block), never another line in the register.
- **MISS-4 — MAJOR (corpus-method) — the component is named `picker-componentsliders-consolerail` and all three axes + the r2 audited ONLY ConsoleRail.vue.** All three receipts pin the same single subject and SHA (verified); `ComponentSliders.vue` (395 lines, the owner of the slider contract, touch gates, meter, announcements — SHA re-hashed by this seat) has zero corpus rows and appears only as cure destination or contrast. MISS-5..MISS-10 live there; the still-unexamined remainder (useSliderAnnouncements + sliderAnnouncement.ts, the meter's aria-live posture) is a visual/challenge-audit input (§7). Recorded in DISSENT as the corpus's failure surface.
- **MISS-5 — MINOR — `sliderWrapperEls` is never pruned, and `attachSliderListeners` re-binds five listeners to every DETACHED wrapper from every space ever visited → X-W8 (one cure with C-2/MISS-13).** Verified whole by this seat: ComponentSliders.vue:56 `if (el)` guard; `.channel-rows` re-keys per space (:47) detaching old wrappers; useSliderTouchGates.ts:53 iterates the FULL historical map, attaching `pointerdown`(capture)/`touchmove`/`touchend`/`pointercancel`/`lostpointercapture` (:88-92) per entry on every space change (:106-108). `listenerCleanups` (:94-100) bounds the leak; the retained subtrees and the dead re-bind work are real. One-line cure (else-delete) closes C-2 too.
- **MISS-6 — MINOR — touch-gate state bleeds across colour spaces: one gate per BARE channel key shared by every space using that key (`b` = rgb-blue/hwb-blackness/lab-b*/oklab-b*), and the space-change watcher re-attaches listeners without resetting gates → X-W6.** Verified: useSliderTouchGates.ts:31-40 builds gates over the flattened key union; :106-108 does nothing else; the sibling `activeComponent` watcher DOES reset (:138) — the asymmetry is the tell. Rider (this seat's): the gate's own inactivity timer may bound the bleed window — the live width of the window is UNPROVEN (§6), the state-sharing is not.
- **MISS-7 — MINOR — `componentEntries` re-derives a computed the injected model already exposes, its tuple payload is dead in every consumer, and the two copies disagree → X-W8.** Verified with one correction to reader B: the expressions are NOT identical — the pipeline's `colorComponents` (useColorPipeline.ts:117-122) omits the alpha append and short-circuits hex to `[["hex", 0]]`, while the local copy (ComponentSliders.vue:120-125) fabricates an untyped alpha literal and, in hex mode, yields rgb+alpha (via `resolveColorSpace("hex") → "rgb"`, verified color-model.ts:32-34). Two answers to "what channels does this space have", both consumers reading only index 0 (:34, :51 — verified).
- **MISS-8 — MINOR — `scrollIntoView({ behavior: "smooth" })` bypasses BOTH reduced-motion guards: the CSSOM-View `behavior` option overrides the computed `scroll-behavior` the guards clamp (animations.css:191) → X-W10 (rides D2-03's motion-law row).** Latent at the measured arms (D2-08: no-op with all rows in view); fires the moment the console scrolls. Census by this seat: exactly two live sites (ComponentSliders.vue:213, useHeightTransition.ts:48) — and EasingSpecimenStrip.vue:40 already documents the house ban ("Never `scrollIntoView`"), so the law exists and two callers ignore it. No M-row covers it; carryable.
- **MISS-9 — MINOR — the accessible names are raw internal channel keys, derived by TWO different formulas for the same datum → X-W4.** ConsoleRail.vue:32 `` `${component} channel` `` ("ct channel", "jz channel", "kelvin channel") vs ComponentSliders.vue:66 `` `${component.toUpperCase()} channel` `` ("CT channel") — two names, one channel, neither the human string sitting in `colorSpaceInfo.components[]` the rail already imports (:99). Because `aria-label` overrides content, the human name is unreachable to AT on the rail. Pre-booked in part from the slider side (DEFECT-LEDGER:38531, D-14) — cross-cited, one identity. This also punctures the r2's §6 rebuttal of r1-claim-2, which cited `aria-label="L channel"` as adequacy evidence — true only where the raw key happens to be the notation.
- **MISS-10 — MINOR — the slider keyboard step is sub-least-count for the six integer-format spaces → X-W10 (one identity with the display adjudication's M-4 least-count law re-derivation; the step is its input-side half).** Verified from bytes: `:step="0.001"` normalized for every channel (ComponentSliders.vue:68-70); `INTEGER_LEAST_COUNT` renders rgb/hsl/hsv/hwb/lch/xyz at 0 decimals (readoutReservation.ts:50-64); the meter formats through it (:169-171). Correction to reader B (understated): rgb moves 0.001×255 = 0.255/press, so the rendered integer changes on roughly every FOURTH press, not "every other" — the control visibly ignores ~3 of 4 keystrokes; kelvin's uniform step is 39 K/press.
- **MISS-11 — MINOR — small capitals used as a subscript surrogate: `Cᴛ Cᴘ Jᴢ aᴢ bᴢ` are notationally wrong for every reader, not merely mis-rendered in one font → X-W6 (with D2-12; the copy/search consequence is spec-certain: U+1D22 does not case-fold to Z, so a copied "Jᴢ" matches no search for "Jz").** Codepoints verified in source (:161-162) and in probe-09's cps arrays. The correct forms are subscripts (C_T, J_z — `font-variant-position: sub`/`vertical-align`), which also dissolves D2-12(b)'s width outlier.
- **MISS-12 — MINOR (RESCOPED by this seat, ruled-disagreements table) — the `prefers-contrast: more` tab affordance (foundation.css:747-749) is dead ON THE RAIL: `border: 0` (:247) zeroes border-style, so the width bump can never paint on the only demo-authored `[role="tab"]` elements → X-W4 (with the D2-02/MISS-3 modality cluster).** The "sole consumer in the served DOM" half is corrected: glass-ui's DockLayerGroup emits `role="tab"` at runtime.
- **MISS-13 — INFO — both template ref callbacks are typed `any` (ConsoleRail.vue:27, ComponentSliders.vue:56), erasing exactly the null branch whose loss built both never-pruned maps → X-W8 (with C-2/MISS-5).**

## Killed-claims register

- **C-1 — KILLED (unanimous, re-verified).** "Unknown non-null `active` → every tab gets −1": the state has no producer — both writers assign members of `componentEntries` (ComponentSliders.vue:79/:211, drawn from frozen `PICKER_CHANNELS` + the literal "alpha"), the space watcher nulls pre-flush (:138), and `components[0]` is always defined (every space has ≥1 channel + alpha, n ∈ {2,4} — walked by this seat). The REACHABLE branch is the null default, where the fallback FIRES — producing MISS-2, the defect the axis inverted. Keyboard entry measured fine both engines (probe-03).
- **C-3 — KILLED (unanimous, re-verified by enumeration).** All 17 `PICKER_CHANNELS` arrays have pairwise-distinct keys (this seat walked every literal, picker-color.ts:52-70); no space carries a channel keyed `alpha`, so the unconditional append cannot collide. No input to this component produces a duplicate `:key` or a registry overwrite.
- **C-7 — KILLED (unanimous, re-verified).** Inverted accusation: the rail is one of only two of four folding consumers that HONOUR the epoch-bump contract stated in law-voice at useContrastSafeColor.ts:69-77 (which names "the ConsoleRail idiom" BY NAME in its own comment — read by this seat) — `bumpProbeEpochOnMount()` at ConsoleRail.vue:133 with the nine-line why (:129-132); the tint cache self-heals on scheme flips (dark-class-keyed entries); DEFECT-LEDGER:41293's census: ConsoleRail + useViewAccents compliant, useMarkdownColors + HeroBlob ABSENT. The axis penalized the compliant consumer.
- **L-4 — KILLED (ruled above).** A refactor prescription with no named failure mode, against the repo's own reference implementation (DEFECT-LEDGER:10024) and in tension with the file's booked P5 seam. Reconciliation rider preserved on CC-113.
- **D2-11 — KILLED (unanimous; B's sharper ground).** A 0.6-alpha un-composited L compared against an opaque L is a category error; the same run's light-mode composited pixels contradict the row's own premise; no dark composited read exists. Residue re-filed UNPROVEN (§1).
- **D2-03-press-register (r2 §2 table + touch corollary) — KILLED.** The doc's own receipt records press as the LOUDEST register (`R_press_beyond_hover` 43.9% — read raw); the press-fill token is real and live; `cursor: pointer` makes `:active` fire on touch. Only the transform leg is dead — which is what D2-03's body says. The phone vocabulary is rest + press-fill + dot, three registers not two.
- **D2-07-sub ("focused-but-unselected is structurally unreachable") — KILLED.** False in the state the same document calls the default: at `active === null`, `railTabIndex` hands tabindex 0 to `components[0]` while `aria-selected` is false everywhere (:187 vs :30) — the r2's own probe-00 dump shows exactly that. Tabbing into the rail at boot lands focused-and-unselected every time. The interaction-time half (select-before-focus, :218-219) stands inside D2-07.
- **D2-02-cure (r2 §8: "one forced-colors `outline` fallback in the shared register closes all five") — KILLED (ruled above, verified whole).** The register exists, names the role, and is defeated by the sites' own `outline: none` at higher specificity. The MT-CSS-A law restated: a cure inherits no evidential weight from its defect's evidence.

## Superlatives — with provenance (both readers concur; this seat's re-reads confirm)

- **S-1 · The active glyph's ink is DERIVED from the fill it sits on** (`contrastInkFor(cssColorOpaque) ?? var(--foreground)`, :147-149, reasoning at :144-146) — pixel-sampled on the rendered composite at **10.02:1** (probe-06, read raw). The correct architecture for ink-over-live-color, and rare.
- **S-2 · The live-probed rest ink + the epoch contract, honored and explained.** :127-143 registers the mount bump BECAUSE the rail folds the referent into its own computed — the exact contract useContrastSafeColor states in law-voice, with the rail named as the reference idiom in the producer file's own comment. Measured 5.65:1 against the 5.75 target. This is the mechanism that killed C-7; the nine-line comment is why the next reader won't delete it.
- **S-3 · The glyph registry is correct by ENUMERATION, not luck**: 14 label-gated arms return 14 correct notations (probe-08/09); this seat's walk of all unlisted spaces confirms every uppercase-initial fallback lands on canonical notation, including the four rgb-family aliases. (MISS-11's subscript objection refines the encoding, not the design.)
- **S-4 · The α rung is a real diagnosis with a landed cure** (:306-319): Fraunces carries no Greek; U+03B1 fell back to a near-twin of the italic `a*`; the cure encodes a SEMANTIC distinction (alpha is not a color axis → mono, upright) rather than nudging a size. Verified in every arm; the 1.1 px cost is booked honestly at D2-12(a). Beside it, the italic-axes/upright-α split is the correct ISO variable-notation convention — a designer knowing the notation, unasked.
- **S-5 · The seat cure overrides ZERO producer properties** (:38-51, :277-296): the cascade post-mortem names the exact failing weights (0-2-0 vs 0-1-0), the exact dead render (11.6×0px), and cures with a demo-owned positioned box the producer dot fills — glass-ui-first law honored under pressure, and written down. What a landed-fix comment should look like.
- **S-6 · useSliderTouchGates' teardown is complete where almost nothing's is**: five per-element listeners removed with matching capture flags + the document-level net dropped at unmount (verified whole). Which sharpens, rather than softens, MISS-5: the map it iterates is the one thing never pruned.
- **S-7 · The interim-correct registry pattern already ships in the sibling** (ColorNutritionLabel's typed `in`-narrowing + explicit fallback, zero `any`) — the cure for L-3 exists in-repo, unadopted at :174.
- **S-8 (audit provenance) · challenge-D-design-r2.md is internally reconcilable and self-disqualifying where blind** — its geometry re-derives (this seat closed the D2-04 and D2-06 arithmetic independently from its receipts), and its §0 hygiene note voluntarily strikes three of its own artefacts. Its §8 cure error (D2-02) and §2 table error (press register) were both catchable from its OWN receipts — which is exactly what makes the corpus adjudicable.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **D2-11 residual (INFO)**: whether the flat, per-scheme-uncalibrated enclosure ring produces a perceptible weight inversion in dark — needs a composited-luminance read of ring vs letters in BOTH schemes; no probe took one.
2. **D2-13 contrast half**: whether `opacity-60` on `text-mono-caption` drops the tooltip range caption below 4.5:1 on the glass tooltip ground — no probe sampled caption pixels (probe-10's captionBox is null).
3. **D2-02 dot limb**: whether the WatercolorDot actually paints an author colour in WHCM — needs a pixel sample; `getComputedStyle` under forced-colors is not a paint oracle.
4. **D2-06 arms 2-3**: the loupe-impossibility claim is an inference from rest clearance (−0.51/−0.76 px, proven); no grown state was measured. Also the ~2px/side filter-bleed attribution (the excess is measured; its cause is asserted).
5. **D2-12(b) mechanism**: Fraunces-italic cmap coverage of U+1D22 vs U+1D1B/U+1D18 — one fonttools dump.
6. **D2-03 blast radius**: the demo-wide census of `.stagger-children` consumers carrying `:active`/state transforms was never run (the one named sibling is transform-less — verified); the class-level risk is real, the extent unmeasured. Related: MISS-6's live bleed window (the gate's own inactivity timer may bound it).
7. **The unexamined ComponentSliders remainder** (MISS-4): useSliderAnnouncements/sliderAnnouncement.ts (137 lines) and the meter's `aria-live="off"` posture have zero corpus rows — a challenge-audit input, not a cleared surface.
8. **D2-01 seed semantics**: whether MT-F042's loupe wants "channel 0" as the default selection is an OWNER ruling at the mark, not a measurement; `activeComponent`'s only other reader is the gate-class binding (verified), so the seed is mechanically safe either way.

## DISSENT — preserved, not ruled away

1. **The corpus's method failure is the scope gap** (MISS-4): three axes and a 46-witness r2 round audited a two-SFC component's child file only; both hostile re-readers found the parent's four defects in their missed sections; and the r2's only two tooltip captures sampled the one cell class that works while its own arms stood in five broken ones. Evidence selection is part of the audit; a receipt corpus can be internally honest and still aimed at the wrong wall.
2. **The loudness dispute is preserved, not synthesized away**: reader A holds that changed-area is the right salience proxy for a glanceable rail; reader B holds that an undefined metric with an opposing twin in the same JSON proves nothing. This seat ruled for B on the number and for A on the row — if a future wave wants the register hierarchy quantified, it must first DEFINE loudness and gate on the definition.
3. **The r2's two self-refuting cells** (the §2 press table against its own probe-11; the §8 cure against the register its author never read) are recorded as the same fleet lesson the ColorSpaceSelector DISSENT recorded: routing prose inherits less scrutiny than finding prose, and cure cells are where citation-inheritance hides. Both errors were catchable from the r2's own committed receipts.
4. **This seat's own corrections to the readers, on the record**: A's "the rail is [the prefers-contrast rule's] only target" — refuted by the producer dock's `role="tab"` emission (the rail-dead half stands); B's "already computes the identical expression" — the pipeline copy omits alpha and forks on hex (the duplication stands); B's "roughly every other press" — the rgb meter ignores ~3 of 4 keystrokes, worse than claimed.
5. **Severity splits ruled, positions preserved**: C-2 (A:INFO over B:MINOR — no observable consequence in the rail; the parent's twin takes the MINOR), C-5 (B:MINOR as the code row, with A's MAJOR weight carried by MISS-1), L-2 (retired-as-observation on the ColorSpaceSelector precedent over B's MINOR).
6. **Cross-booking risk recorded**: C-4's identity exists in two ledgers (CC-081 → X-W9; CC-068 → X-W6). This record cites both and instructs the cross-reference; if the X-wave authoring collapses them, the collapse must be written, not silent.

## Closing verdict

**DESIGN-RED, double-blocker — adjudicated, not verified.** The rail's craft rows are real and unusually well-written-down — derived active ink, live-probed rest ink with the epoch contract honored, an enumerated glyph registry, a producer-respecting cascade cure — and the same file boots with its sole state indicator absent (owner-marked, still open), erases its own focus indicator in WHCM by defeating the repo's own register, suppresses its only tokenized press response for every default-motion user, and explains its channels through a tooltip surface that is wrong or empty in nearly half its reachable cells. Terminal routing: the instrument cluster (D2-01+MISS-2, D2-04, D2-05, D2-06 rest-state, D2-07, D2-09, D2-12+MISS-11, D2-14, MISS-1/C-4/C-5/L-3/L-5 via CC-068, L-1 rider, MISS-6) → **X-W6**; the semantic/modality cluster (D2-02+MISS-3+D2-15, D2-08/C-6, D2-10, MISS-9, MISS-12) → **X-W4**; the motion/format law cluster (D2-03, D2-13, MISS-8, MISS-10) → **X-W10**; the hygiene cluster (C-2, MISS-5, MISS-7, MISS-13) → **X-W8**; the o27 forced-colors oracle extension → **X-W1**; the loupe constraints (D2-06 budget, D2-14 record, L-1/L-4 reconciliation) → **CC-113, BLOCKED-ON TR#84**; C-4 cross-booked at **X-W9 (CC-081)**. No row is marked NO-WAVE-OWNER. The eight UNPROVEN rows above are this component's visual-audit input set.

— end of apotheosis. The three challenge files, the r2 record, its evidence-r2/ receipts, and both re-reader returns remain immutable beside this record per E-1/E-3; dated evidence was not rewritten.
