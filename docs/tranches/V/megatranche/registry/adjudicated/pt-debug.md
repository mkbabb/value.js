`claude-fable-5`

# pt-debug — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: apotheosis arbiter · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **value.js HEAD**: `c654824e` (branch `tranche-u`) · **parse-that tree**: main checkout `master @ ef10d5b`, READ-ONLY throughout — pre-existing uncommitted rows in `rust/**`, `.cargo/config.toml`, `README.md` untouched (README evidence below is pinned to the working tree as read).
**Corpus**: the two challenge files `audit/parse-that-modules/debug/challenge-L-library.md` (Opus 5 [1m], 32 defects / 5 superlatives / 5 refutations) and `challenge-C-consumption.md` (Opus 5 [1m], 16 defects / 5 superlatives / 2 refutations), plus the L-18 hostile-reread return (`L-18-hostile-reread-parse-that-debug-library-correctness`) that confirmed, killed, and extended them.
**Law compliance**: `ls /Users/mkbabb/Programming/parse-that-css-totality-p2` → `No such file or directory`, re-verified this session — the fresh root was never created; **no STOP**. Zero writes to parse-that; this file is this seat's only write. Diagnostics never armed, no memoizer constructed in any probe process (the PT-03 latch stayed cold; `packrat.ts:290` is the only `= true` write and fires only at memoizer construction). Two Node probes total, both parse-that-free (`console.log`-vs-`toString` semantics; dist-text pattern counts via python) — probe parsimony held.
**Method (the incredulity seat's own instruments, all static + two probes)**: `debug.ts` read WHOLE (383 lines); `state.ts` WHOLE (189); `lazy.ts` WHOLE (43); `ansi.ts` WHOLE (17); `utils.ts:1-62/80-129`; `parser.ts` at the `id`/flags, `parseStateInner`, `then`/`or`, `map`, `not`/`minus`, `trim`, `many`/`sepBy`, `eof`, `recover`, `debug`/`toString`/`lazy`/`_initWhitespace` seams; `leaf.ts:70-80/145-165/360-399`; `test/debug.test.ts` + `test/print.test.ts` assertion regions; `test/subpath-gate.mjs:20-56`; `diagnostics.ts` + `index.ts` barrels; `package.json#exports`; `docs/api.md:78-296`; `README.md:180-240`; `docs/tranches/A/waves/A.W3.md:78-84`; dist: entry-file import lines (all five), chunk export block `:1417-1437`, chunk `:28-40/:174/:262-264/:678-722/:881-882/:1406`, byte-regions 28-281 and 38-277 re-measured (`8,444` / `7,652` B of a `40,576` B chunk; CJS twin `41,041` B), `debug.d.ts` (8 declares, no `dist/debug.js`); value.js census re-run (`src/css/` = 7 files / 1,948 lines; deps = glass-ui + keyframes only; the two parse-that-freedom comments); band + prototype anchors (`parser-band.md` §"WHAT CAND-O OWES CAND-F" clause 1; `parsethat-surface-gaps.mjs:19-20`).
**Dispositions are ADJUDICATED, not VERIFIED** (R-A: VERIFIED is stamped only at X.P.W4's sub-tranche release close). Routing targets: **X.P.W1** (harness/bench — MEASURE-AT-OPEN), **X.P.W2** (the dual-target algebra — KEEP/WRAP/RETIRE owns this module's successor surface), **X.P.W3** (totality/recovery — the throw-class and diagnostics-to-spec cures), **X.P.W4** (release condition — published-surface parity), **X·P REFINEMENT FOLD** (SCOPE M-27 ¶2 — findings that sharpen a spec before its L-20 stamp), **NO-WAVE-OWNER** (banked outside X·P).

## Scoreboard

**46 adjudicated defects — 3 BLOCKER · 16 MAJOR · 18 MINOR · 9 INFO. 14 killed claims. 10 reader-recovered misses (adjudicated into the 46). 6 superlatives.** Every reader kill and every reader miss that this seat spot-checked survived; two precision corrections of the reader's own cells are recorded in §INCREDULITY.

The module's shape, adjudicated: a renderer whose two BLOCKER-grade coordinate defects sit exactly on the library's stated domain (one-line minified CSS; leading-newline sources), a grammar printer whose output is a function of process call history rather than of its argument, and a finished, README-documented API that no published entry point can reach — welded into 4 of 5 subpaths by two `toString` conveniences. X·P's ruled posture (challenge-C §6, adopted): **KEEP `formatExpected` behind a real door · WRAP the renderer as a value · RETIRE `parserPrint`/`parserDebug`/the `toString` welds.**

## Ruled disagreements and severity rulings (the adjudication's actual work)

| id | L grade | C grade | RULING | Deciding rationale (this seat's own) |
|---|---|---|---|---|
| **M-1 = C-1 + C-2** (unreachable mandated API + phantom `.d.ts` + blind gate) | MAJOR | BLOCKER ×2 | **MAJOR** | BLOCKER in this corpus means *wrong output on a reachable, common input, no workaround*. Total unreachability is a spec/packaging break with a vendor/copy workaround, and X·P re-doors the surface regardless. C's three-level severity argument (cannot be reached / trusted / avoided) is adopted as the module verdict, not as this row's grade. C's BLOCKER reading recorded, not ruled away. |
| **M-8 = C-3** (`toString()` throws on any public-constructor Parser, incl. the band's adjudicated `reject()`) | MAJOR | BLOCKER | **MAJOR, with C's bearing made binding on W2/W3** | C's total-function-contract argument is the stronger reading and its cand-F limb is real (my read: `parsethat-surface-gaps.mjs:20` is `new Parser((s) => {...})` verbatim; `parser-band.md` §owes clause 1 makes labelled zero-width failures binding on the wave — so the adjudicated grammar hosts nodes whose `toString` throws). It stays MAJOR because the throw needs a stringification site; but the CROSS-1 kill (below) removes the most likely reproduction path, so the row's severity must not lean on `console.log`. |
| **M-7** (renderer throws raw TypeError on non-string `src`) | MAJOR | — | **MINOR** (reader's downgrade adopted) | Two independent gates verified at the bytes: every shipped leaf reads `state.src` and throws in the *parse* before `parseStateInner` can render (`parser.ts:53` precedes `:67-69`), and the render is gated on `isDiagnosticsEnabled()` (default `false`, `utils.ts:6`). The bare fact (no boundary check) stands at MINOR. |
| **B-3 remediation** (L doc: the one-line unconditional memo write "widens the defect") | asserted | — | **KILLED — the reader's kill CONFIRMED by this seat's own trace** | I traced both scenarios: the degraded `"lazy"` placeholder is produced only in the `id`-truthy arm (`debug.ts:324-326`), where writes already happen; dropping the `:339` guard adds cache entries only for the id-`undefined` top-level walk, and those hold exactly what a fresh recomputation would produce (a top-level walk that reads a poisoned nested id at `:257` yields the same degraded string cached or not — `:346` already freezes it under the root's id today). Cycle termination is owned by the `:320` flag, not the memo; self- and mutually-recursive cases terminate either way. The doc's two-part fix is better design, but not because the one-liner regresses correctness. |
| **m-12 threshold** | `src.length + lines.length − 1` | — | **KILLED and corrected**: drop iff `span.offset > src.length` | My worked example at the bytes: `lineEnd` accumulates `Σ(lenᵢ+1) = src.length+1`; src `"ab\ncd"` (len 5): offsets 0–5 match, 6 drops; the doc's formula predicts 6 matches. The true threshold is lower and line-count-independent — the defect is *more* reachable than filed. |

## Adjudicated defect roster

Format: **id · severity · claim** — proof (compressed; the full chains live in the two challenge files and the reader return; `✓A` = re-derived by this seat at the bytes or by probe) — **DISPOSITION**.

### BLOCKER

- **B-1 = C-5 · BLOCKER · The caret pad is computed in original-source columns while the line it annotates is re-origined by `summarizeLine`** — any line >74 chars puts caret and text in different coordinate systems; displacement `columnNum − start − 3`, unbounded. `debug.ts:83` (`pad = lnWidth+4+columnNum`) vs `:72`→`:19-30` (window `start = min(max(mid−37,0),len)`) ✓A; L's 200-char case re-derives to caret index 145 vs 85-col rendered line ✓A; C's 240-char case re-derives to caret index **155** (not 158 — see killed C-5-label) vs 85. No test pins a caret position (`test/debug.test.ts:39-50/:197-214` ✓A). Minified CSS = the library's stated domain (`packrat.ts:9-11`) — **X.P.W2** (the renderer WRAP's `{row, col}`-relative caret contract kills this class by construction; the wrap spec must gate the caret in rendered coordinates).
- **B-2 · BLOCKER · An error at byte 0 of a source beginning with a newline is reported on line 2 at column −1** — `state.ts:128` `lastIndexOf("\n", offset−1)`: at offset 0 the fromIndex −1 clamps to 0 per ECMA-262, so a leading `"\n"` is FOUND ✓A; `"\nabc\ndef"` → `{line: 2, column: −1}` where truth is `{1, 0}` ✓A (closed-form; the −1 flows into `:83`'s repeat and `summarizeLine`'s mid without throwing only because `lnWidth+4 > 1`). For every offset ≥1 the algorithm is correct ✓A — **X.P.W3** (diagnostics-to-spec: the line/column law belongs to the typed-recovery gates; see MISS-1 for the disagreement that exposes it).
- **B-3 · BLOCKER · `parserPrint` is not a function of its argument — printing an enclosing grammar permanently caches the string `"lazy"` under a nested lazy's own id** — `PARSER_STRINGS` (`debug.ts:245`) written at `:322/:340/:346`; `id` is a mode flag (`:345` calls `print(parser)` one-arg; `:321` re-enters truthy); inside an expansion `case "lazy"` returns `name` at `:325` and `:339-340` writes `"lazy"` under the nested node's id; `:346` heals only the top-level argument ✓A whole-arm trace. `String(sameObject)` returns `"b"` or `"lazy"` by process call history, no clear API. Trigger is *nesting*, not recursion ✓A — **X.P.W2** (RETIRE `parserPrint` in current form; under AC-2 CLOSED-IR the successor is a fold over the IR, not a closure walk — challenge-C §6 adopted; m-8/C-8 is the permanence mechanism and MISS-10 the second no-recovery direction).

### MAJOR

- **M-1 = C-1 + C-2 · MAJOR (ruled) · `formatDiagnostic`/`formatAllDiagnostics`/`parserDebug`/`parserPrint` are mandated for `./diagnostics` by parse-that's own ratified A.W3, documented in README, and importable from no published entry; the gate never looks; `dist/debug.d.ts` still declares all 8** — A.W3.md:81 `./diagnostics` row lists all four ✓A; `diagnostics.ts` re-exports 6 runtime names from `./utils.js` only, never imports `./debug.js` (`grep 'from "./debug'` → exactly `state.ts:2` + `parser.ts:3` ✓A); exports map = 5 doors, no wildcard, no `dist/debug.js` ✓A; `subpath-gate.mjs` imports only `./core` + `./packrat`, asserts 3 symbols ✓A; `debug.d.ts` = 8 `export declare function` ✓A; README instructs `formatAllDiagnostics(...)` and claims TS/Rust API parity ✓A — **X.P.W4** (release-condition parity: published-`.d.ts` ↔ exports-map is exactly the direction `dist-surface.test.ts` leaves uncovered — SUP-6; the A.W3 spec-regression *record* itself is banked NO-WAVE-OWNER, parse-that program history).
- **M-2 · MAJOR · `parserPrint` is exponential in `.then`/`.or` spine depth** — `parser.ts:101/:120` pass `this` as BOTH `context.parser` and `args[0]` ✓A; `debug.ts:261-265` prints `context.parser` unconditionally before the switch, `:294-307` re-print the same left child and discard the first result ✓A; memo writes gated `if (id)`, id undefined for the whole top-level walk → reads never hit ✓A. Falsifier holds: `leaf.ts:77/:149/:162` pass `parser: undefined`, so shipped grammars never trip it ✓A — **X.P.W2** (RETIRE) + **MEASURE-AT-OPEN** (timing curve, W1) + the MISS-9 rider (the observable failure at depth ≈30 is a hang, not a throw — remediation needs a call budget, not only a depth guard).
- **M-3 · MAJOR · `a.not(b)` prints as `!"a"` — the excluded parser is dropped and the rendering asserts the negation of what the parser accepts; `a.not()` prints identically** — one context name for two languages (`parser.ts:299-302` ✓A: `negate` zero-width vs `not` accepts-`a`-when-`b`-fails-after); printer arm `debug.ts:280-281` never reads `args` ✓A — **X.P.W2** (RETIRE; the successor printer's spec must render exclusion explicitly — REFINEMENT FOLD rider on the W2 printer contract).
- **M-4 = C-6 · MAJOR · `debugDepth` leaks permanently on any throw between `++` and `--`** — `debug.ts:12/:358/:361/:379`, no try/finally in `:357-381` ✓A; module-private, no reset; real throw sources: the packrat key-budget RangeError (`packrat.ts:90-97` ✓A) and PT-04's lazy-depth RangeError. The codebase's own contrast: `parser.ts:38-48` wraps the packrat epoch in try/finally and says so — **X.P.W2** (RETIRE `parserDebug`; W2 §3 clause 3's no-latch contract makes this a K-6 candidate-killer class; any successor trace facility lives in the harness, not on the shipped surface).
- **M-5 = C-9 · MAJOR · Printing a lazy parser invokes the user's thunk and mints a second, independent grammar that never runs; the shadow graph is retained because the WeakMap key is held strongly one field away** — `debug.ts:316-318` → `lazy.ts:7-15` (calls `fn()`, caches in `LAZY_PARSER_CACHE` keyed on the thunk) vs the executing graph's separate closure-local cache (`lazy.ts:18-24`) ✓A; `parser.ts:705` stores the thunk in `context.args` → strong ref to the WeakMap key ✓A; id budget: `PARSER_ID` process-global, `getCijKey` throws above ≈2,097,151 (`packrat.ts:77-97` ✓A) — **X.P.W2** (RETIRE; under AC-2 grammar-as-data removes the thunk-invocation affordance; under W1 §3.5 a stringification perturbs a measured process — harness ban rides W1).
- **M-6 · MAJOR · `statePrint` is Θ(|src|) allocation per call and `parserDebug` calls it once per parser invocation — O(n) trace becomes O(n²)** — `debug.ts:58` full split (≤9 lines used), `:59`→`state.ts:131` full prefix copy + second split for one integer, `:100` third split, `:373-376` split/map/join of the rendered block per trace line ✓A; the cheap `lastIndexOf` window exists in-tree (`state.ts:113-116`). BOUND (reader's, ✓A): the single-line/minified case pays an array per step, not a source copy — the Θ(|src|) copy is the multi-line case — **X.P.W2** (WRAP: the value-renderer computes line/col by `lastIndexOf` window) + **MEASURE-AT-OPEN** (quantify if any of this code survives into the wrap).
- **M-8 = C-3 · MAJOR (ruled) · `toString()` throws a raw Error on any Parser built through the public constructor — including the band's adjudicated `reject()` node** — `parser.ts:31` `context = {}` → `debug.ts:261` destructure undefined → switch default `undefined` → `result` undefined → `:336-338` throw ✓A; reached via `parser.ts:698-700` ✓A; `createParserContext` public, constructor never calls it (`state.ts:179-189` ✓A). Implicit-invocation list corrected by CROSS-1: template literal, `+`, `String()`, `join`, error-message assembly — **not** `console.log` (my live probe) — **X.P.W3** (the R1 throw class: a total `toString` for every constructible parser is a W3 §6 cure; the reject-node hosting obligation binds via parser-band clause 1).
- **M-9 = C-12 · MAJOR · The primary caret block and the secondary-span block in one document compute gutter width independently and disagree for every source under 100 lines** — `debug.ts:65` `lineNumberWidth(endIdx)` (1/2/3) vs `:110` hardcoded floor 3 ✓A; both pads literal over their own `lnWidth` (`:83`/`:116`) ✓A; compounding: `:114` prints `lines[i]` RAW while `:72` truncates the same line ✓A; the spans tests populate but never render (`test/debug.test.ts:152-161` ✓A). Executable UNARMED via `formatDiagnostic`'s ungated call (`:216-217` — the GATE-REACH kill) — **X.P.W2** (WRAP: one gutter computation in the value renderer).
- **M-10 · MAJOR · The printer is information-free on 11 of the 29 declared context names, each erasing its whole subtree** — 29 `parserNames` counted (`state.ts:141-171` ✓A); switch handles 18 ✓A; the 11 (chain, dispatch, eof, lookAhead, mapState, memoize, mergeMemo, minus, peek, recover, whitespace) fall to `default → undefined` → bare name; `dispatch`'s args ARE the parsers (`leaf.ts:149` ✓A), directly consumable by the existing any/or arm; all 11 are public surface ✓A — **X.P.W2** (RETIRE/successor spec: totality over the context-name union is a REFINEMENT FOLD clause — a printer arm per declared name or a closed IR that cannot omit one).
- **C-4 · MAJOR · The documented `debug()` API is a 2-parameter stderr signature; the shipped method is 3 parameters with a stdout default — a consumer following the docs binds their logger to `recursivePrint` and silently gets `console.log` plus the forced recursive print** — `docs/api.md:81/:83` ✓A vs `parser.ts:690-696` ✓A; third limb: `api.md:111` documents `toString(indent)`, shipped `toString()` takes none ✓A — surface limb **X.P.W2** (RETIRE `parserDebug`); doc limb **NO-WAVE-OWNER** (parse-that repo doc hygiene, banked for the M-27 parity program — X·P waves do not own parse-that main's docs; the pause forbids touching them).
- **C-7 = m-13 · MAJOR · `/core`'s minimality docstring is false: four of five entries import the 40,576 B chunk carrying the whole renderer + ANSI palette, welded by two `toString` conveniences; `./diagnostics` — the tier A.W3 says should carry it — is the only entry that does not** — my full re-read of every entry's import lines ✓A: parse.js → packrat-entry + diagnostics-chunk + core.js **+ utils.js** (FOUR import lines — a precision correction to both the C doc and the reader, strengthening the pinning: the root barrel also pulls the json/csv domain graph); core.js/utils.js/packrat.js → packrat-entry; diagnostics.js → diagnostics-chunk ONLY ✓A. Byte-regions reproduce exactly: C's 28-281 = 8,444 B (20.8%), L's 38-277 = 7,652 B (18.9%), the delta = the ANSI palette + parserDebug tail ✓A; CJS twin 41,041 B ✓A; weld: `state.ts:2` ⇄ `debug.ts:1`, `parser.ts:3` ⇄ `debug.ts:3`, methods on live exported classes ✓A — **X.P.W2** (RETIRE the welds — cutting them un-pins the bytes and makes `/core`'s docstring true) + **X.P.W4** (the release condition should gate per-subpath payload honesty).
- **C-10 · MAJOR · Every recursion point renders as the bare token `lazy`, and the one recursive-fixture test asserts only `toBeTruthy()`** — `debug.ts:324-326` else-arm returns the context name, always `"lazy"` ✓A; the suite's own fixture `mijn` (`print.test.ts:21` ✓A) renders `… | "b" | lazy`; gates at `:26`/`:31` are `toBeTruthy()` ✓A — satisfied by the string `"lazy"` alone — **X.P.W2** (successor spec: recursion renders a rule name or back-reference; the L-19 no-vacuous-gate law rider → REFINEMENT FOLD).
- **C-11 = PT-01 · MAJOR · Arming diagnostics couples an unconditional `console.error` whose payload IS `statePrint`, with no sink parameter or suppression on that path** — `parser.ts:67-69` ✓A → `state.ts:136-138` → `debug.ts:141`; dist chunk `:881-882` ✓A; no logger parameter anywhere in `parseStateInner`; `statePrint` reads the global directly (`:174` ✓A) and the function that would render without arming has no door (M-1) — **X.P.W2** (R-LAW-3 "diagnostics are values, never effects" owns the cure architecturally) + **X.P.W3** ("labelled diagnostics without arming" is a named W3 §6 condition — the two waves split architecture/cure).
- **MISS-1 · MAJOR · One rendered diagnostic contradicts itself: `formatDiagnostic`'s header prints line:column from the snapshot (computed CORRECTLY at offset 0 by `collectDiagnostic`) while the caret block underneath recomputes through the buggy `getLineAndColumn` — header `1:0`, caret drawn on line 2** — ✓A at the bytes, the decisive check of this adjudication: `utils.ts:107-110` slices FIRST (`before = src.slice(0, furthest)`; at 0, `"".lastIndexOf("\n")` = −1 → `{1,0}` correct) — it does NOT share `state.ts:128`'s negative-fromIndex clamp; the two provably agree for every offset ≥1 and disagree at exactly the offset that matters ✓A. `debug.ts:203` (snapshot) vs `:207-208` (fresh ParserState → `addCursor` → `:59`). This INVERTS the L doc's falsifier-b (killed below). Reach: any `recover()` diagnostic at offset 0 of a newline-leading source — **X.P.W3** (with B-2; the diagnostics-to-spec gate must pin one line/column law and one implementation).
- **MISS-2 · MAJOR · `formatAllDiagnostics` is O(N × |src|): each diagnostic reconstructs a full `ParserState` and re-splits the entire source (twice with secondary spans) — the many-errors function degrades exactly in the many-errors case** — `debug.ts:238` map → per-d `:207` + `:208`→`:58` split + `:217`→`:100` second split ✓A; `src` invariant across the map, never hoisted ✓A — **X.P.W2** (WRAP: split once, render N) + **MEASURE-AT-OPEN** (quantify on W1's corpus).
- **MISS-3 · MAJOR · The `(Diagnostic, src)` API cannot enforce that `src` is the string the offsets came from — and the package's own worked example gets it wrong: collects from `cssWithErrors`, renders against `css`** — README Error-Recovery block ✓A (`stylesheet.parse(cssWithErrors);` … `formatAllDiagnostics(diagnostics, css)` — two variables, one line apart); `Diagnostic` (`utils.ts:84-93` ✓A) carries no source identity though `collectDiagnostic` had `state.src` in hand (`utils.ts:103` ✓A); the clamped renderer degrades to a confidently-wrong caret, never a throw — **X.P.W2** (the Diagnostic-as-value spec carries its source identity — REFINEMENT FOLD clause on the W2/W3 diagnostic type) + doc limb NO-WAVE-OWNER.

### MINOR

- **M-7 (re-graded) · MINOR · No boundary check on `src` in the renderer: `debug.ts:58/:100`, `state.ts:113/:128` assume string; a non-string `src` throws a raw TypeError from the render path** — facts ✓A; reach refuted (see ruled table): every shipped leaf throws in the parse first, and the render is diagnostics-gated — **X.P.W3** (the same PT-07 boundary law W3 §6 already carries).
- **m-1 · MINOR · `sepBy` bounds dropped from context — every `sepBy` prints identically** — `parser.ts:634` records only `sep`; `many` keeps `(min,max)` (`:561`) ✓A — **X.P.W2** (successor context/IR totality).
- **m-2 · MINOR · `map(fn, mapError=true)` prints identically to the unmapped parser though it accepts strictly more** — `parser.ts:150` (`!state.isError || mapError`) ✓A; context records neither (`:158` ✓A) — **X.P.W2** (same clause).
- **m-3 · MINOR · The rendered active line overruns the file's own 80-column comment** — `debug.ts:10` claims `80 − 6`; actual prefix `lnWidth+4` (5-7) and both ellipses outside the budget → 85-87 rendered ✓A (85 re-derived for the 1-digit gutter) — **X.P.W2** (dies in the WRAP; width becomes data).
- **m-4 · MINOR · Non-active lines get 40 of the 74-column budget** — `:72` passes 0 → window `[0,37]` + `"..."` ✓A; `:19` treats 74 as a whole-line budget, refuting the half-width reading ✓A — **X.P.W2** (same).
- **m-5 · MINOR · The `isDiagnosticsEnabled()` gate in `statePrint` is redundant on every ordering but enable→populate→disable→print, where it silently discards real user data** — every field inside is already write-gated on the same flag (`utils.ts:33/:52/:58` ✓A) and length-guarded (`:176/:181/:186` ✓A); fields public and mutable (`state.ts:43-45` ✓A); the `:172-173` comment promises the opposite posture — **X.P.W2** (the value renderer takes data, never reads the global).
- **m-6 · MINOR · `formatDiagnostic`'s "same visual style as statePrint" docstring is false three measurable ways** (header shape; extras gating `:174` vs ungated `:212-226`; `found` rendered only in one) — ✓A at the bytes — **X.P.W2** (one renderer, one style, by construction).
- **m-7 · MINOR · The context label `"trim"` is dead — no site constructs it** — `grep createParserContext("trim"` → 0 ✓A; `trim()` produces `trimWhitespace`/`wrap`/`all` ✓A; precision (reader's, adopted): the *label* is dead, not the arm — `case "wrap": case "trim":` share one live body — **X.P.W2** (dies with the surface).
- **m-8 = C-8 · MINOR · `PARSER_STRINGS` is an unbounded module global keyed on a monotone process-global id, never cleared, no exported clear — the mechanism that makes B-3 permanent, in a package that ships `resetPackrat` for its other global** — `debug.ts:245`, writes `:322/:340/:346` ✓A; `resetPackrat` exported (`index.ts:8` ✓A); holds no object refs (bounded by string length — the honest reading, adopted) — **X.P.W2** (W2 §3 clause 3 no-latch contract; K-6 killer class).
- **m-9 · MINOR · `parserPrint` recurses with no depth guard and no try/catch — a deep grammar makes `toString()` throw RangeError; the print ceiling is lower and less stable than the parse path's** — structural ✓A (`:252-343` carries no counter/visited/try); folds with PT-04 and W1 §G-9's declare-your-depth law — **X.P.W2** (RETIRE) + **MEASURE-AT-OPEN** (the non-monotonic 5000/6500/7000 ceiling is inherited, never re-run) + the MISS-9 rider.
- **m-10 = C-13 · MINOR · The exported function and the method disagree on sink (stderr vs stdout), and ANSI is decided once at module load from `process.stderr` regardless of the actual sink — consumer loggers receive escapes** — `debug.ts:355` `console.error` vs `parser.ts:693` `console.log` ✓A; `ansi.ts:3-6` module-load const, no setter/flag/option ✓A; stderr is the stream W1 §G-5 requires stay empty — **X.P.W2** (ANSI becomes an explicit adapter at the consumer boundary — C §6 WRAP, adopted).
- **m-11 · MINOR · The two functions carrying the BLOCKER defects are the two with vacuous assertions** — truncation asserts `< 200` + `toContain("...")`; addCursor asserts `toContain("^")`/`toContain("hello world")`; statePrint's `toContain("1")/("2")` satisfied by the source text ✓A; the gutter sub-limb is killed (the `"|"` assertion at `:193` is real ✓A) — **REFINEMENT FOLD** (the L-19 gate-authoring law: no wave gate may be satisfiable by its failure mode; cite this row as the exhibit).
- **C-14 + C-16 (limbs 1-2) · MINOR · Zero tests for the diagnostic formatters; the suite imports source deep paths no consumer can resolve; README's colour claim is inverted (name yellow, parser green, no blue exported) and the shared-TS/Rust-vector claim is unenforced (nothing reads `grammar/tests/debug/`)** — greps ✓A (formatter grep empty; `debug.test.ts:13-18`/`print.test.ts:3` deep imports; ansi.ts has 10 formatters, no blue ✓A); limb 3 killed (below) — tests limb **X.P.W4** (surface-parity gates must import through the doors); docs limb **NO-WAVE-OWNER**.
- **m-12 (corrected) · MINOR · `formatSecondarySpans` silently drops spans with `offset > src.length` — no marker, no note — and an all-dropped list leaves a bare blank line after `statePrint:181-182`'s committed `"\n"`** — loop arithmetic re-derived ✓A (threshold corrected per the ruled table); executable UNARMED through `formatDiagnostic:216-217` ✓A — **X.P.W2** (WRAP: out-of-range spans become typed notes, never silence).
- **MISS-4 · MINOR · `summarizeLine`'s `trimEnd()` desynchronizes the caret on SHORT lines too — the lines both challenges certify as correct**: rendered content is trailing-whitespace-trimmed (`:15`, and the ≤74 fast path returns `trimmed`, `:19` ✓A), the pad is not (`:83`) — every CRLF source mis-points by ≥1 at end-of-line; overshoot bound = trailing-whitespace width, unbounded. Worked case `"a  \nb"` @3 → content ends at rendered 5, caret at 8 ✓A — **X.P.W2** (the WRAP's coordinate contract must be gated on CRLF + trailing-whitespace fixtures — REFINEMENT FOLD rider naming the fixture classes).
- **MISS-5 · MINOR · The two renderers disagree about carriage returns: the primary block is CR-safe by accident (`trimEnd` eats `\r`), the secondary block emits `lines[i]` raw — on CRLF a bare `\r` returns the terminal cursor to column 0 and the marker line overwrites the source line** — `:100` splits on `"\n"` only; `:114` verbatim ✓A; column arithmetic unaffected (`:107` counts the `\r` ✓A) — **X.P.W2** (same fixture rider).
- **MISS-6 · MINOR · `formatSecondarySpans` throws a raw RangeError from `" ".repeat` on a negative span offset ≤ −8** — first-iteration match (`span.offset < lineEnd` trivially true), `col` negative, `lnWidth` floored 3 → `repeat(7+col)` ✓A; `SecondarySpan.offset` a bare number on public mutable arrays (`state.ts:31-34/:45`, `utils.ts:91` ✓A), no check at `addSecondarySpan` (`utils.ts:57-61` ✓A); latent for library-built spans — **X.P.W3** (the same throw-class/boundary law as M-7/MISS-6's PT-07 family).
- **MISS-7 · MINOR · The grammar printer emits its own metacharacters unescaped — `string('"')` prints `"""`; terminals containing `| [ ] { } ! ? << >>` render as printer syntax, so output is ambiguous and not round-trippable** — `debug.ts:269-270` raw interpolation ✓A against the printer's own operator vocabulary (`:281/:283/:286/:290/:299/:306/:312` ✓A); CSS delimiter parsers are routine in the band's grammars — **X.P.W2** (successor printer spec: escaping or a quoted-terminal IR — REFINEMENT FOLD clause).

### INFO

- **i-1 · INFO · `parserDebug` bypasses `call()` — attaching `.debug()` could change what its subject accepts; saved today only by a second dead mechanism** — `:361` invokes `.parser` not `.call` ✓A; the flag machinery is dead: sole `.flags =` write at `parser.ts:496` onto a constructed-then-discarded Parser ✓A; `FLAG_EOF` has declaration `:22` + read `:466` and **no write site** ✓A — **REFINEMENT FOLD** (whoever revives `flags` inherits the Heisenbug; the W2 spec must state that observers go through the same invocation path as execution).
- **i-2 = C-15 · INFO · The falsy-id hazard is latent, held off by one line of module init** — `:320`/`:339` boolean-read a numeric id; `PARSER_ID` starts 0; `_initWhitespace();` at **`parser.ts:711`** ✓A (C's `:709` cite wrong — killed below) permanently owns id 0; the first-fresh-id-38 figure is root-barrel-specific (`index.ts:14` re-exports the module-scope json/csv graph ✓A — my parse.js 4th-import-line find corroborates) — **REFINEMENT FOLD** (`id != null`, and the same id-as-flag conflation is B-3's live sibling) + **MEASURE-AT-OPEN** (per-entry first-fresh-id).
- **i-3 · INFO · `Diagnostic.offset` is carried and never rendered** — `utils.ts:85` set from `recover`'s checkpoint (`parser.ts:666`); `formatDiagnostic` consumes everything else ✓A — **REFINEMENT FOLD** (the W2/W3 Diagnostic value spec decides: render it or drop the field; MISS-3's source-identity clause is authored in the same breath).
- **i-4 · INFO · The line/column surface has three implementations, two dead, and they disagree** — `getColumnNumber`/`getLineNumber` have zero call sites ✓A; for `"abc\ndef"` @5 they yield 1 / 1 vs `getLineAndColumn`'s 2 ✓A; `getLineAndColumn` has exactly ONE call site, `debug.ts:59` ✓A — **X.P.W3** (one law, one implementation — with B-2/MISS-1).
- **i-5 · INFO · `debug.ts` sits on both legs of a two-cycle import knot created solely to give two classes a `toString`, and the knot is the mechanism behind the single-chunk fan-in** — all four edges ✓A (`state.ts:2`⇄`debug.ts:1`, `parser.ts:3`⇄`debug.ts:3`); the codebase's own scar comment (`leaf.ts:393-395` + `parser.ts:711`) names the class ✓A — **X.P.W2** (RETIRE the welds; one change discharges this and C-7).
- **i-6 · INFO · `parserDebug` retains the user's logger in `context.args` where nothing reads it** — `:382` stores; `case "debug"` never reads args ✓A; the same args-retains-a-closure shape that defeats the WeakMap in M-5 — **X.P.W2** (dies with RETIRE).
- **MISS-8 · INFO · The unimportable documented surface is twice as wide as filed, and api.md documents a `ParserState.addCursor(cursor)` METHOD that has never existed** — `api.md:288` (`formatExpected`) + `:292` (`addCursor(state, cursor?, error?)`) under the diagnostics heading ✓A — four documented unreachable names, not two; `api.md:132` documents the phantom method; `ParserState` has no such member (read whole ✓A) — **NO-WAVE-OWNER** (parse-that doc hygiene, banked; strengthens M-1's W4 gate shape: the parity gate should also diff docs-documented names against resolvable names).
- **MISS-9 · INFO · For `.then`/`.or` spines the exponential DOMINATES the recursion ceiling by orders of magnitude — the observable failure at depth ≈30 is an unbounded hang, never the RangeError m-9 predicts** — call count 2^depth at frame depth = depth ✓A by construction from `:261-265/:294-307`; materially changes the remediation shape for M-2 and m-9 (call budget, not only depth guard) — **REFINEMENT FOLD** (sharpens W1 §G-9's declare-your-depth into declare-your-work for any print path W2 keeps).
- **MISS-10 · INFO · `PARSER_STRINGS` caches on `parser.id` while `Parser.context` is public-mutable and the library itself rewrites it post-construction — a cached rendering can never be invalidated** — `parser.ts:640` reassigns `p.context` in `eof()` ✓A; `leaf.ts:398` `whitespace.context.name = "whitespace"` post-init ✓A; the printer keys on id alone (`:248/:257`) ✓A; both in-tree mutations precede any print — latent, the second no-recovery direction after B-3 — **REFINEMENT FOLD** (any memoized successor printer carries a context fingerprint, not just a cycle guard).

## Killed-claims register

All 14 reader kills ADOPTED; the 9 marked ✓A were re-executed by this seat's own instruments, the rest verified at the cited bytes.

1. **L·B-2-falsifier-b** ✓A — the L doc's "both paths carry the same clamp, wrong identically" is FALSE and inverts: `utils.ts:107-110` slices before searching and is CORRECT at offset 0. Inverted into MISS-1 (MAJOR). The doc's stated reason nothing catches B-2 is not the reason; the real reason is M-1 + C-14 (no door, no test).
2. **L·B-3-remediation** ✓A — "the one-line unconditional memo write widens the defect" is unestablished; my own trace: the delta entries hold exactly what a fresh recomputation produces, cycle termination is the `:320` flag's, and the guarded version already freezes the degraded root string at `:346`. The two-part fix is better design on its own merits only.
3. **L·M-7-reach** — the renderer-crash-buries-the-failure narrative refuted on two gates (parse throws first; render diagnostics-gated). Fact carried at MINOR.
4. **L·m-12-threshold** ✓A — corrected to `span.offset > src.length`, line-count-independent, *more* reachable than filed.
5. **L·GATE-REACH** ✓A — `formatSecondarySpans` has an ungated second call site, `formatDiagnostic:216-217`; M-9 and m-12 were always executable unarmed via the source deep path. The arming hedge was unnecessary.
6. **CROSS-1** ✓A (live probe this session) — `console.log(parserInstance)` routes through `util.inspect` and does NOT invoke `toString` (`A {}` printed, no throw); the template-literal limb fires. The single most likely reproduction path for B-3/M-8/C-3 will not reproduce; all other limbs stand.
7. **C·C-16-limb3** ✓A — api.md's diagnostics section DOES carry debug.ts entries (`:288`, `:292`); the "empty heading" claim is refuted and inverts into MISS-8.
8. **L·B-2-callsites** ✓A — `getLineAndColumn` has ONE call site (`debug.ts:59`), not two; `state.ts:137`'s toString calls `statePrint`, not the accessor. Non-material, strengthens the doc's own conclusion.
9. **L·m-11-gutter** ✓A — `test/debug.test.ts:193` asserts `toContain("|")`; deleting the gutter reds it. The load-bearing half (no position pinned) stands.
10. **L·HEADER-COUNT** ✓A — 8 exported functions, not 9 (grep + `debug.d.ts` + the doc's own §M-1 sentence).
11. **C·C-13-count** ✓A — ansi.ts exports TEN formatters, not 9; "no control" half correct.
12. **C·C-1-count** ✓A — the chunk export block binds NINETEEN names (`:1417-1437`), not 20; substantive claim (none a debug formatter) confirmed.
13. **C·C-5-label** ✓A — caret *index* 155 (= `1+4+150`), overshoot 70; "158" is the caret line's length (155 + `"^^^"`); the doc's falsifier repeats the conflation. Defect real, arithmetic otherwise reproducible.
14. **C·C-15-cite** ✓A — `_initWhitespace();` is `parser.ts:711` (`:709` blank, `:710` comment); no "leaf singletons" exist at leaf.ts module scope (`whitespace` is an unassigned `export let`); the low ids burn in the root barrel's json/csv graph, so id-38 is root-barrel-specific.

## §INCREDULITY — the reader challenged back

The hostile-reread return was itself audited, not inherited. Nine of its fourteen kills were re-executed by this seat (register above); all ten of its recovered misses were re-derived at the bytes and all ten survive (MISS-1 through MISS-10, adjudicated into the roster); its severity calibrations were adopted after independent re-derivation (M-7's downgrade, B-3-remediation's kill-of-a-kill). Two corrections OF the reader from this seat's own instruments:

1. **C-7 evidence cell, precision**: the reader's proof lists parse.js's imports as "packrat-entry + diagnostics chunk + core.js". parse.js has a **fourth** import line — `utils.js` (`csvParser, escapedString, jsonParser, numberParser, quotedString`) ✓A. Non-material to the 4-of-5 pinning (it strengthens it: a root-barrel import also pulls the json/csv domain graph, which is precisely the i-2/C-15 low-id-burn mechanism). Recorded because an adjudication that inherits an import census without re-listing the lines is how citation-inheritance starts.
2. **This seat's own instrument error, recorded as method**: my first probe for SUP-2's `dim`-absence counted the raw ESC byte (`0x1b`) in the chunk and returned 0 for *every* formatter — including nine that provably ship. The chunk encodes escapes textually (`\x1B[2m` as five characters). The corrected textual probe: nine formatters present at chunk `:29-37`, `dim` alone absent (pattern count 0 vs 1 for each of the other nine) ✓A — the reader's per-symbol tree-shaking strengthening is CONFIRMED, and the lesson (verify the artifact's encoding before trusting a zero) joins the exemplar's signature-before-output family.

## Superlatives (L-18 runs both ways — all six adopted, each re-checked)

1. **SUP-1 · `formatExpected`** (`debug.ts:33-47`) — the module's one total function, the only one pinned by real tests (four exact-equality assertions ✓A), `readonly` parameter, dedupe refutation upstream at `utils.ts:40` ✓A. C's consumption reading stands: it maps exactly onto value.js's `ParseIssue.expected: readonly string[]` (`src/css/types.ts:22` ✓A) — the X·P KEEP, promoted to a real door.
2. **SUP-2 · `ansi.ts`** — 17 lines, zero deps, decides once at module scope, `typeof process` browser guard, `NO_COLOR` honored ✓A; per-symbol tree-shakeable, proven at the artifact by the corrected `dim` probe (§INCREDULITY 2).
3. **SUP-3 · `addCursor`'s window arithmetic is total under adversarial input** — both bounds clamped (`:62-63` ✓A); B-2's out-of-range line 2 degrades to a wrong answer, never a crash; `lineNum > lines.length` returns `""`.
4. **SUP-4 · The diagnostic substate is per-parse, not global, and this module honors it** — `:172-188` reads off the state instance ✓A; the one global read (`:174`) affects whether, never whose; simultaneously filed as m-5, which is the honest reading.
5. **SUP-5 · The type posture is stricter than the tier below** — all four array parameters `readonly`, none violated (bodies read ✓A); one eslint-disabled `any` in 383 lines; `utils.ts` must defensively spread what this module got from the type system free.
6. **SUP-6 · `dist-surface.test.ts` states its own provenance** (the 0.8.2 incident) and thereby makes its own blind spot diagnosable — source-barrel→dist cured, published-`.d.ts`→exports-map open, which is exactly where C-2 lives. The W4 parity gate is authored FROM this header.

## MEASURE-AT-OPEN residue (bench-dependent rows for X.P.W1's harness)

None of these numbers is a bar (W1: the bar is OWNER-GATED). Each is an inherited or N=1 figure to be re-measured under W1 §3.5's one-fresh-process-per-cell discipline before any gate cites it:

| row | inherited figure | instrument at open |
|---|---|---|
| M-2 exponential curve | depth 12→18: 1.21→31.70 ms (N=1); prior pass depth 22 → 10.7 s | fresh-process `.then`-spine sweep; assert class (≈2× per +1), never constants |
| m-9 print ceiling | non-monotonic 5000 OK / 6500 RangeError / 7000 OK (inherited, not re-run) | record as an interval with the V8-frame-size invalidation, per the L doc's own discipline |
| PT-04 parse ceiling | lazy deepest OK 7,761 / RangeError 7,762 (O-15, reproduced by C's session) | W1 §3.7 corpus depth bound restated against the re-measured ceiling |
| M-6 / MISS-2 allocation | ~200 kB/step (multi-line); ~20 MB per 100 diagnostics on 100 kB — derivational bounds | heap-delta per `statePrint`/`formatAllDiagnostics` call on the W1 corpus, IF any of this code survives the W2 WRAP |
| i-2 / C-15 first fresh id | 38 via root barrel (probe-order drift ±1 observed between passes) | per-entry measurement (`.`, `/core`, `/utils`) — the figure is entry-specific |
| C-13 escape leakage | 20 ESC sequences into a plain-array logger under a pty | cell-dependent (TTY state); re-measure in the W1 cell that owns stderr-empty §G-5 |
| unarmed baseline | 60.9 ns/parse median (`parsethat-surface-gaps.mjs`, unarmed) | W1's bench instrument; the surface-gaps runner is rescued onto tracked disk by W1's own gates |

## Closing verdict

**DEFECTIVE — RETIRE-AND-REDOOR.** Adjudicated: the module fails on three axes at once — it cannot be *reached* (M-1/C-1/C-2: four spec-mandated, README-documented functions behind no door, a phantom typed surface, a blind gate), it cannot be *trusted* where its own methods make it reachable (B-1/B-2/B-3: two coordinate systems in one caret block, a wrong line at the most common failure offset, a printer whose output depends on process history; M-8's throwing `toString` on the band's own adjudicated node), and it cannot be *avoided* (C-7/i-5: two `toString` welds pin 7.6-8.4 kB of renderer into four of five subpaths, including the one whose docstring promises the opposite). The severest single fact this adjudication adds to the record is MISS-1: the package computes the correct line/column at offset 0 in one function and the wrong one in another, and `formatDiagnostic` prints both in the same document.

Terminal routing, in one line each: the **W2 algebra** owns the successor surface (KEEP `formatExpected` · WRAP the renderer as a value with rendered-coordinate carets, one gutter, CRLF-safe fixtures, source-identified Diagnostics · RETIRE `parserPrint`/`parserDebug`/the welds); **W3** owns the throw-class and line/column-law cures and the labelled-diagnostics-without-arming condition; **W4** owns published-surface parity (exports-map ↔ `.d.ts` ↔ docs); **W1** holds the MEASURE-AT-OPEN residue; the **REFINEMENT FOLD** carries the eight spec-sharpening riders (gate-vacuity law, call-budget law, context-fingerprint, observer-path identity, printer escaping/totality, Diagnostic field decisions, CRLF fixture classes, falsy-id hygiene); **NO-WAVE-OWNER** banks the parse-that doc-hygiene rows (README colour/vector claims, api.md's phantom signatures) for the M-27 parity program — none of them may silently die with this file.

— end of apotheosis. The two challenger records and the hostile-reread return remain immutable beside this file per E-1/E-3; original ids for life.
