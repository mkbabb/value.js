claude-fable-5 (served model id)

# fr-MorphShapePreview — adjudicated apotheosis (M-23/L-18, incredulity seat over two hostile re-readers)

**Seat**: fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code) · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `fourier-analysis/web/src/components/morph/MorphShapePreview.vue` (175 lines, read whole by this seat) — the `/morph` preview stage: the route's only interactive control (a native `<button>` hosting `FourierMorphSvg`) plus the four-chip readout, duplicated desktop/mobile. **Corpus**: `challenge-D-design.md` (26 rows + 6 superlatives, opus-5[1m]) · `challenge-L-library.md` (18 rows + 4 superlatives + 5 self-withdrawals) · `challenge-C-consumption.md` (14 rows + 3 non-findings + 3 superlatives) at fourier HEAD `cd26c65`; subject + closure files HEAD-clean (git-porcelain empty; mtimes Mar 30/Jun 2 — months before the Aug 6 challenge).
**Standing context**: this component's closure was already adjudicated twice — **fr-FourierMorphSvg.md r2** (2026-08-12; FM-1..FM-24, K-1..K-20) and **fr-FourierMorphDemo.md** (2026-08-12; FMD-1..FMD-N6, K-1..K-13). Most of this corpus therefore FOLDS BY REFERENCE; the adjudication's work is the identity discipline, the reader-kill challenges, the two genuinely new bookings, and three reader-missed rows.
**Method (incredulity)**: both readers' verdict-level work was challenged back before adoption. Every kill was re-executed at the bytes (all eight held). All three missed finds were independently re-derived; one is RESCOPED against a bank row that landed *after* the readers ran (SC-1). Two reader evidence cells are corrected (SC-2, SC-3) and two upstream bank cells corrected in passing (SC-4, plus the FMD-12 mirror-error rider). Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings are M-25 agglomeration input and stamp nothing.
**This seat's own instruments (static + node/git only; zero browser probes — probe parsimony; product source READ-ONLY; this file the sole write)**: MorphShapePreview.vue whole (all reader anchors byte-exact: `:4` button, `:97` transition shorthand, `:108-112` hover, `:114-116` active, `:118-120` disabled-block-is-cursor-wait, `:124-145` display-none swap, `:147-156` chip base, `:152` the file's ONLY border-radius (info-chip), `:165-174` phase tints, media queries exactly `:77/:100/:135/:158` all 640px); useFourierMorph.ts whole (morphTo `:145`, stopAnim `:146` guarded `:116` over `currentAnim` init-null `:87`, `await getAnimationCtor()` `:149`, FIRST phase write `:169`, handle assignment `:171`, raw `harmonicLevel` writes `:174/:203` vs geometry clamps `:175/:204`, phase writes `:169/:182/:196/:210`, `onUnmounted` `:215`, `currentPoints` deep `ref([])` `:80` + `currentPath` computed `:81`, return surface `:217-229`); FourierMorphDemo.vue anchors (`:15` Math.round, `:18` `:disabled`, `:113` isAnimating, `:128` guard, `:132` isMoon-flip BEFORE `:134` await, `:115-120` nearestActiveLevel); MorphPhaseConfig bounds (`min=50 max=800` input+Slider, clamp `:94` → 2400 ms max); useMorphConfig `:30` candidate literal, no shape argument; pointsToSvgPath body (svg-fourier.ts:47-73: 4 modular index reads + ~13 tuple-element reads per iteration, all fresh-array producers); **node executions** — installed value.js back-easing extrema `ease-out-back max 1.086857 @0.6335 · ease-in-back min −0.096882 @0.3478 · ease-in-out-back ∓0.092713` (byte-matching the reader to six decimals — L-10's declared load-bearing assumption CLOSED), vue 3.5.38 deep-vs-shallow ref proxy witness (`deepRef.value[0] !== raw[0]` TRUE / shallow FALSE), both assets `levels=[1..50] · n_harmonics=50 · 512 pts`; **my own oklch→OKLab→sRGB→WCAG pipeline** reproducing D-2 to the hundredth (`#db2424`/`#d7428c`, plates `#f8e1df`/`#f7e4eb`, **3.91/3.43 light · 4.94/5.71 dark**) with token anchors line-exact (color-radius.css `:40/:44/:57/:72/:84-85/:256/:258`, dark-arm.css `:42-43/:46/:109/:111`); **the C-6/D-8 kill chain traced whole** — style.css:3 `@import "@mkbabb/glass-ui/styles"` → exports `./styles` = dist/styles/index.css → utilities.css:19 → `utilities/a11y-overrides.css:6-16`, PRM blanket read verbatim (`*:not([data-allow-motion]) { transition-property: opacity, color, background-color, border-color, box-shadow !important }` — **transform/translate ABSENT**); cards.css `@utility cartoon-surface` at `:33` read whole; fourier style.css shim `:98-111` + ring allow-list `:136-143` (four classes, no `.morph-button`) + root-font step `:41-49` (1.125rem → 1rem @768); censuses — `type="button"` exactly **1** (ImageUpload.vue:94) vs **7 live native `<button>` sites** + one doc-comment (Tooltip.vue:8); `grep -rn morph e2e/` → **one** line (visual-baseline.spec.ts:35), its only expect the overflow≤2 gate `:65-68` under `reducedMotion:"reduce"` + `animations:"disabled"`; checkA11y sites `visualization-ux:114/146/163/201` + crud `×3`, all `/visualize`; `currentPoints` external consumers → **0** (dead export confirmed); DarkModeToggle own `useFourierMorph()` instance `:37`, `aria-label` `:5`, ring `:98-101`, PRM block `:103-107`; **cartoon-card live census: 21 occurrences / 14 files** (SC-3); stale-dist structural witness (`.cartoon-card{…transition:translate…}` inlined + `.morph-button[data-v-c69c00c8]` emitted UNLAYERED — cited for structure only, `--ease-apple-spring` proves the build stale, the reader's declared caveat honoured); MetricBadge.vue.d.ts `:5-38` (abbreviation dual-sibling doc verbatim) + components.css `:56` hover 1.02 / `:60` press 0.96 / `:63` focus-visible / `:182-183` `--full + --abbr` toggle; **glass-ui producer at the tags** (repo HEAD **407de2d3**, 8.0.0 — moved past the readers' pin; re-verified at today's HEAD): `git grep abbreviation v7.0.0|v8.0.0 -- src/` → **zero**, HEAD src/ → zero; `v7.0.0:src/components/metric/types.ts` = value/unit/placeholder/loading/label/context/class/size/orientation — **no `color`, no `tone`, no `abbreviation`**; `v7.0.0:src/components/chip/types.ts:11` `tone?: string`; `v7.0.0:…/Button.vue` `type: nativeButton ? (props.type ?? "button")` at **:55 exact**; v7 exports `./badge ./chip ./metric`, `./metric-badge` ABSENT; fourier `docs/tranches/A/audit/W3-button-ledger.md:65` (MorphShapePreview retire-with-rationale) + the adjacent DarkModeToggle row ("the focus-visible outline survives via the local `<style>` block") + the `:147`-area folder tally (`morph | 4 | 1`); value.js `docs/tranches/A/audit/` = HARDEN-1..6 + W0/W1 series, **no button ledger** (the C-1 attribution kill's decider); registry identity sweeps — `abbreviation` in adjudicated/ → GAB-28 · fr-AnimationControls M-5 · **fr-InfoCard FR-IC-10 (banked TODAY 12:14 — the no-successor fact, "class banked at GAB-2/K-5")** · fr-GalleryDraftsSection m-3 · fr-EquationPanel instruments; `currentPoints` → fr-AppHeader + fr-DarkModeToggle only (no reactive-depth booking); morph-button radius → **zero** banked rows; `muted-foreground` → no morph booking; FR-COB-13 verbatim; fr-EquationView D·D-M10 verbatim; fr-BasisSelector M-7 verbatim; fr-ContourPreview row 35 / fr-ContourEditorCanvas L-5 / kf-CubeScene L-9 (the deep-reactive class precedents, all read at the cited lines).

## Scoreboard

**26 adjudicated roster rows — 2 BLOCKER-tier folds · 18 folds-by-reference total (identities preserved, zero re-books) · 2 NEW component-owned bookings (L-13, C-12) · 3 reader-missed rows adjudicated (1 ADMITTED-with-corrected-arithmetic, 1 RESCOPED-to-fold, 1 ADMITTED-INFO) · 3 INFO records.** **8 killed-claims register entries — all eight reader kills CHALLENGED and SUSTAINED at the bytes; none overturned.** **4 seat corrections minted (SC-1..SC-4) + 1 bank-correction rider** (the FMD-12 "target `./chip`" mirror error). **9 superlatives ratified with provenance.** **7 UNPROVEN-NEEDS-LIVE residues → SS-13.**

The headline is negative space: after two prior closure adjudications, this corpus's 58 rows collapse to **two genuinely new component-owned defects** (the radius-less primary control, the `--muted`/`--foreground` pairing break) **plus one reader-found composable amplifier** (the deep-`ref` reactive-proxy tax on the rAF hot path) — everything else was already banked, and the readers' fold discipline held under hostile re-check at every single row. The corrections headline is **SC-1**: the bank moved under the readers mid-flight — `fr-InfoCard FR-IC-10` (banked today) already owns the `abbreviation` no-successor fact their missed #2 booked as new, so that row folds instead of booking; its riders survive.

## The incredulity seat's own work — rulings and corrections

1. **The C-6 ⊕ D-8 kill (PRM legs) — CHALLENGED, SUSTAINED, chain re-traced whole.** This is the corpus's only kill that fells a MAJOR, so I walked it myself: fourier style.css:3 imports the producer stylesheet; the export map routes `./styles` → `dist/styles/index.css`; utilities.css:19 imports `utilities/a11y-overrides.css`; its `:6-16` PRM blanket (`*:not([data-allow-motion])`, `!important`) rewrites `transition-property` to `opacity, color, background-color, border-color, box-shadow` — **transform and translate absent**, so under PRM the hover scale, press scale, and `--lift-sm` lift lose their transitions entirely (instant state change, not animation). C-6's own falsifier ("a global kill-switch reaching `.morph-button`") FIRED — its census was scoped to `web/src` and never followed the `:3` import. D-8's enumerated unguarded set is refuted; its headline survives **only** on the JS morph clock, which is banked (FR-AH-31 + FM-11, with FMD-22 already carrying this exact narrowing). Fourth reader-seat concordance on the family's MAJOR dissent recorded below.
2. **The D-14 mechanism kill — SUSTAINED at the bytes; FOURTH minting of a thrice-banked kill.** My read: on the cold-import path both calls park at `:149` before any `currentAnim` assignment (`:171`); the initialiser is null (`:87`) and `stopAnim` is guarded (`:116`) — call B's `stopAnim()` is a no-op; the real failure is B **overwriting** the single handle at `:171`, orphaning A's Animation beyond `stopAnim`/`onUnmounted:215`/Reset. Banked as fr-DarkModeToggle K-2 · fr-FourierMorphSvg K-2 · fr-FourierMorphDemo K-1; this register's entry exists so the wrong causality cannot travel into a repair ticket a fourth time. D-14's *conclusion* (no vocabulary between "idle" and "settle-out"; the guard reopens) folds → FMD-6.
3. **SC-1 — the readers' missed #2 RESCOPED: the bank moved under them.** Their registry sweep ("`abbreviation` → 4 hits, all prescriptive, none a no-successor booking") was true at their epoch and is FALSE today: **fr-InfoCard FR-IC-10** (mtime 2026-08-24 12:14) books "`abbreviation` is absent from `MetricProps` at both tags (class banked at GAB-2/K-5)", and **fr-GalleryAdminBanner GAB-2** (rewritten today) books the metric-family prop deletions with K-5's double-rename kill. My own tag runs re-confirm the substance at today's producer HEAD `407de2d3`: `abbreviation` zero at v7.0.0 src, v8.0.0 src, AND HEAD src. So: **FOLD, not book** — with the readers' three genuine increments preserved as riders (roster row below). Not a reader error; an epoch event, recorded so the F.W1 census knows the fact has ONE home.
4. **SC-2 — missed #1's tracking arithmetic corrected UPWARD.** The reader priced ~6 100 `track()` calls/tick; my count from the live loop body (svg-fourier.ts:47-73, n=512): 512×4 = 2 048 tracked index reads **plus ~13 tuple-element reads per iteration** (p0×2 · p1×4 · p2×5 · p3×2) ≈ 6 650 — total **≈ 8 700 tracked reads + ~512 fresh tuple proxies per tick** (fresh arrays every write defeat the `reactiveMap` cache across ticks; within a tick each tuple wraps once). The correction favours the finding. The `shallowRef` cure's safety re-verified: 5 wholesale assignments (`:93/:102/:175/:189/:204`), 1 declaration, 1 computed read, 1 export, zero in-place mutations — and the export has **zero external consumers** (my grep), so the change is invisible outside the file.
5. **SC-3 — the cartoon-card site count, third value in the corpus, all three wrong.** D-25 (and the reader's confirmed cell repeating it) says "25 sites" per [FE §3]; the shim's own comment (style.css:101-102) says "14 sites (13 files)"; C-14 says "21+". My census at HEAD: **21 occurrences / 14 files** (`grep -rn cartoon-card src --include='*.vue'`). The scope-law from the ColorSpaceSelector addendum applies: the count is part of the claim. The glass BH relay packet must carry 21/14, and any wave touching the shim should correct its stale comment.
6. **SC-4 — a bank cell corrected in passing.** fr-BasisSelector **M-7**'s parenthetical "(cures D-m14, at Button.vue:95 — corpus cite :55 is line drift)" is itself wrong **at the v7.0.0 tag**: my `git show v7.0.0:src/components/button/Button.vue` finds the `type ?? "button"` pinning at **:55 exactly**, concordant with FR-COB-13's cell ("Cured upstream at v7 Button.vue :55"). M-7's seat evidently read a producer-HEAD file; the tag is immutable and :55 is the true anchor. Recorded so the F.W1 spec cites the tag line.
7. **The FMD-12 cure-phrasing mirror error — reader's bank-correction rider ADOPTED.** Banked FMD-12/FM-14 says "`./metric-badge` is GONE at 8.0.0 — target `./chip`"; the readers' kill of D-11/C-4's cure table proves the symmetric truth at the tag: v7 `Metric` has **no `color`/`tone`** (my types.ts read; the D·D-M10 identity), so the one TINTED chip needs `Chip.tone` — but the other three (`n=` value+label, `ms` value+unit, bare name) are `Metric`'s exact shape. The correct F.W1/F.W3 mapping is **3×`Metric` + 1×`Chip`**, not either single target. Rider minted on FM-14/FMD-12; no re-book.
8. **Seat narrowing on missed #2 (beyond both readers)**: even at the installed 4.0.0, `abbreviation` was the WRONG cure for THIS file's duplication — the two chip arms are **byte-identical** (no label variation to swap); what differs is *placement* (inside `.stage-row` vs sibling) and flex direction, which a label-toggle channel cannot express. The axes reached for a responsive-*label* affordance to cure a responsive-*layout* duplication. This strengthens the readers' sequencing rider: the 8-for-4 duplication cures locally (one `.demo-info`, container query or flex switch) independent of, and before, any primitive adoption.

## Adjudicated defect roster

Line anchors are fourier HEAD `cd26c65`; re-resolve before executing any cure (the D-19 lesson). Grades are the banked homes' — a fold carries the bank's identity and severity.

### BLOCKER-tier (both folds)

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-1 ⊕ L-01 ⊕ C-1** | The `/morph` route's only interactive control computes an EMPTY accessible name; the component's public surface offers no cure route (fallthrough targets the `generic` `.demo-stage` div — L-01's remedy table re-verified; no label prop, no slot, no `$attrs` forward). Gate census EXACT at my re-run: one `morph` line in e2e (a screenshot harness), seven `checkA11y` sites all `/visualize`. The name is already in hand (`shapeName`); the precedent is one directory away (DarkModeToggle.vue:5). | **FOLD → banked fr-FourierMorphSvg FM-1 (BLOCKER) + fr-FourierMorphDemo FMD-2** (leaf half → fr-DarkModeToggle N-4). **F.W4**; axe run on `/morph` → SS-13. Not a new booking — third corpus to re-derive it, zero drift across all three. |
| **L-11 ⊕ C-3** | The chip prints `n=75`/`n=100` over a level-50 reconstruction; three surfaces offer levels the shipped data does not contain (candidates literal useMorphConfig:30 with no shape argument; grid `max="100"` ×3; `setLevel` assigns raw at useFourierMorph:101). Assets re-measured by this seat: both `levels` top at 50, 512 pts. The route's two highlight systems disagree about one click. | **FOLD → banked FMD-1 (BLOCKER) / FM-20 (MAJOR)** — the phantom-level family's third derivation, concordant to the byte. **F.W4** (derive ladder + High bound from `shape.data.levels`, naming the source-of-truth shape per FM-20's mid-transit rider). |

### MAJOR-tier folds

| id | claim | disposition |
|---|---|---|
| **D-4 ⊕ L-02 ⊕ C-7(a)** | `:disabled` during the morph blurs the focused control with no restore (350 ms default, 2 400 ms max — clamp bounds re-verified at MorphPhaseConfig:94), duplicating the parent's `:128` JS guard; zero `focus()`/`tabindex` in the subtree. | **FOLD → the banked focus-theft row (fr-FourierMorphSvg FM-4..FM-16 batch)** · **F.W4** (`aria-disabled` + keep the guard). |
| **D-14 (conclusion only)** | No pending state across the async engine boundary: during the cold dynamic import the UI reads `idle`, stays enabled, and a second click re-enters `morphTo`. Reading order verified: first phase write `:169` sits after the `:149` await; `isAnimating` is solely `phase !== "idle"`. Mechanism KILLED (register entry 2). | **FOLD → FMD-6 / fr-DarkModeToggle M-3·L-4 (the FR-AH-8/-9/-20/-21 epoch family)** · **F.W4**. |
| **L-09** | `shapeName` flips to the DESTINATION on the click tick (`:132` before `:134`) while phase 1 renders the SOURCE up to 800 ms; the same flip swaps all 12 grid tiles. | **FOLD → FR-AH-8 / FMD-9** (flag-before-await) · **F.W4**. |
| **L-16 ⊕ C-1(aggravator 3)** | Nothing gates any of it: vitest absent (my package.json read), `/morph`'s sole e2e is a capture harness whose only expect is overflow≤2 under forced reduced-motion, axe never visits the route. | **FOLD → FMD-31 + FM-10's e2e row** · **F.W4**; feeds SS-13's probe list. |

### MINOR-tier folds

| id | claim | disposition |
|---|---|---|
| **D-2 (arithmetic)** | Both phase-tint chips fail WCAG AA light (**3.91 / 3.43**; dark 4.94/5.71) — reproduced independently by this seat's own pipeline to the hundredth, token anchors line-exact. Grade ruled at register entry 3. | **FOLD → FM-4** (phase-transience demotion, MINOR, MAJOR dissent preserved) **+ FMD-19** (the contrast suite, MAJOR — same figures). Homes own the grades. **F.W4** token edit. |
| **D-3 ⊕ C-7(b)** | No `:focus-visible` on the one interactive element; the house ring's allow-list (style.css:136-143, four classes, my read) excludes it; no producer outline reset, so the UA ring survives — non-conformance, not invisibility (the corpus's own honest hedge, re-verified). | **FOLD → the banked ring-sweep row (FM-4..FM-16 batch)** · **F.W4**; legibility → SS-13. |
| **D-5** | The disabled state's entire treatment is `cursor: wait` (`:118-120`); cursors do not exist on touch, and mobile is first-class in this file. | **FOLD → FMD-6** (whose ruled cure is the busy affordance) · **F.W4** — **rider**: the affordance must span the WHOLE animating window (350–2 400 ms), not only the cold-cache import, and must be non-cursor (touch). |
| **D-6 ⊕ L-12** | The `:97` scoped `transition` shorthand resets `transition-property` wholesale (unlayered scoped beats `@layer utilities`), dropping the producer's `translate` — the cartoon lift SNAPS while `scale(1.02)` eases. Producer bytes re-read (cards.css:33-48 verbatim); the stale-dist structural witness admitted with the reader's declared caveat (`@apply` inlining + unlayered SFC emission proven; token names NOT cited from it). | **FOLD → FMD-27, riding FR-AH-11's cascade decision at the banked INFO magnitude** · **F.W4**. |
| **D-7** | Hover REPLACES the offset-stamp shadow with a flat 3 px halo — the card flattens exactly when it lifts (box-shadow is one property; `--lift-sm` still fires, so depth and position cues disagree). | **FOLD → FMD-N3 (MAJOR, elevated-weight rider on FR-AH-11)** · **F.W4**; glass BH relay carry rides the cartoon-card packet (standing owner edict), with SC-3's corrected 21/14 census. |
| **D-9 ⊕ D-23** | The desktop chip column stretches to its longest member (4→10→5→9 chars per toggle — `MorphPhase` writes re-verified `:169/:182/:196/:210`); `.stage-row`'s mobile centring declarations are inert (one child below 640). | **FOLD → FMD-30** · **F.W4**. |
| **D-10 ⊕ L-04 ⊕ C-5** | Eight chips render four facts — `:13-26` ≡ `:30-43` modulo wrapper class, toggled by `display:none`; zero `v-for`, so iteration-keyed censuses are blind (R5-7's predicted face). Both L §D-1 self-withdrawals sound (AX-tree pruning; text fast path). | **FOLD → FM-16 via FMD-34** · **F.W4** — **with the new sequencing rider (SC-1/ruling 8): cure the duplication LOCALLY (one `.demo-info` + container query/flex switch) BEFORE and independent of primitive adoption; the `abbreviation` channel is dead at the destination AND was inapposite here even at the pin.** |
| **D-11 ⊕ L-14 ⊕ C-4** | A bespoke chip where the CURRENT pin ships the primitive, re-typing the producer's magic numbers (`:111 scale(1.02)` ≡ components.css:56; `:115 0.98` diverges from the shipped 0.96; no focus rule vs `:63`). BASELINE LAW satisfied by my own tag runs (`./metric-badge` absent at v7; `MetricBadge` zero files in v7 src). Cure table ruled at register entry 4. | **FOLD → FM-14 / FMD-12** · **F.W1/F.W3** — with the adopted mapping rider: **3×`Metric` + 1×`Chip.tone`** (ruling 7). |
| **D-12 ⊕ C-13** | Non-monotonic type ladder 15.75 → 18.00 → 16.00 px: the component's 640 breakpoint (×4, my read) vs the root-font 768 step (style.css:41-49, my read) — the banked three-armed-ladder mechanism at this component's own chips. | **FOLD → FM-21 + FMD-29** · **F.W4** (one seam decision). |
| **D-13** | The route's primary affordance wears a decoration-only CARD recipe via the locally resurrected shim, while the secondary Export/Reset are genuine glass `Button`s — the design language inverts with importance; the only control site of `cartoon-card` in `components/morph/`. | **FOLD → FMD-12 (hand-rolled-where-the-seat-ships) + FR-AH-11 (cascade decision)** · **F.W3** (re-basing, not break-cure). |
| **D-17** | Fixed user-space `stroke-width=4.5` under a changing render scale → 2.10/3.15/3.24 px strokes, 1.54× spread; content boxes 93.5/140/144 re-derived, byte-identical to FM-21's CORRECTED ladder. | **FOLD → FM-6 + FM-21** · **F.W4**. |
| **D-19 ⊕ L-03 ⊕ C-2** | `phase: string` discards the exported `MorphPhase` union that is simultaneously display text AND a CSS class selector; `.info-chip.idle` has no rule, so a typo is indistinguishable from idle; with vitest absent, the widening disarms the only gate. | **FOLD → FMD-33** · **F.W4**. |
| **L-05** | The native `<button>` declares no `type` → defaults `submit`; latent (no `<form>` ancestor on `/morph`). Census re-run by this seat: 1 `type="button"` vs 7 live native sites. | **FOLD → fr-CanvasOverlayButton FR-COB-13 (latent-MINOR class)** · **F.W1** — with missed #3's scope correction (below). |
| **L-10** | The `n=` readout escapes `[lowLevel, highLevel]` — n=1, n=54, and n=−8 reachable — because display is unclamped (`:174/:203`) while render is clamped (`:175/:204`). The row's single declared unproven premise (value.js might clamp) now CLOSED by two independent executions (reader's + this seat's, matching to six decimals: no clamp; extrema 1.086857/−0.096882/∓0.092713). | **FOLD → FMD-N4 (MINOR as banked; the axis's MAJOR over-graded against the home)** · **F.W4** (clamp at the write, FM-12's cure idiom). |

### NEW bookings (component-owned, registry-swept clean)

| id | claim | proof | disposition |
|---|---|---|---|
| **FR-MSP-1 · L-13 (reader-confirmed, seat-ratified) — MINOR** | NO `border-radius` reaches `.morph-button` from any of its three style sources — the 120/180 px primary control is the one square-cornered surface on a route where `.grid-cell` (0.5rem), `.level-input`/`.num-input` (0.375rem), `.btn-export`/`.btn-reset` (0.5rem), and this file's own `.info-chip` (0.375rem) are all rounded. Corrects the M-critique corpus row (`raw-findings.json:2343` "radius inherited only from cartoon-card" — false: neither the shim nor `cartoon-surface` declares one). | My reads: `grep border-radius MorphShapePreview.vue` → exactly 1 (`:152`, info-chip); shim style.css:107-111 = border-color + background only; cards.css:33-48 = border-width/box-shadow/translate/transition/hover only; no bare `button {}` rule in fourier style.css or glass dist. Registry-swept: zero banked rows book a radius at this site. | **ADJUDICATED → F.W4** — a one-declaration design decision; NOTE it may discharge for free under FMD-12's F.W3 re-basing (glass Button/Card carry radii), so the F.W4 ticket should check the F.W3 outcome first. |
| **FR-MSP-2 · C-12 (reader-confirmed, seat-ratified) — MINOR** | `--muted` consumed with `--foreground` (`:153-154`), breaking the producer's declared `--muted`/`--muted-foreground` pairing (color-radius.css:84-85, my read) — four secondary readouts compete typographically with primary text. Explicitly NOT a contrast defect (the mispairing is HIGHER contrast — 16.21:1; the corpus's own honest scoping, sustained). | Token anchors line-exact (my sed); no banked morph row books the pairing (my sweep). | **ADJUDICATED → F.W4** — rides FMD-19's token-suite edit as one pass; semantic/hierarchy, not a11y. |

### Reader-missed rows — adjudicated

| id | source | ruling |
|---|---|---|
| **FR-MSP-3 · missed #1 — REACTIVITY-DEPTH AMPLIFIER — ADMITTED, MINOR, arithmetic corrected (SC-2)** | reader | `currentPoints` at useFourierMorph:80 is a DEEP `ref` over a 512-tuple array replaced wholesale every rAF tick; Vue 3.5.38 proxies every tuple (my node witness), and the hot path (`currentPath` computed → pointsToSvgPath) pays **≈ 8 700 tracked reads + ~512 fresh tuple proxies per tick** (seat count from the live loop body — the reader's ~6 100 was an undercount) for a value nothing reads element-wise reactively. Fresh arrays at all 5 write sites defeat the proxy cache across ticks. Cure provably safe: `ref` → `shallowRef`, one word — zero in-place mutations, zero external consumers of the export (my greps). Paid by both instances on `/morph` (the page's + DarkModeToggle's `:37`). Identity-guarded: the CLASS is banked (fr-ContourPreview row 35 · fr-ContourEditorCanvas L-5 · kf-CubeScene L-9, all read at the lines); `currentPoints` appears in the bank only as FR-AH-37's dead-export member and the M-3/FR-AH-21 write-race — the reactive-proxy layer is an unbooked site, and N-12/FM-3 price adjacent allocations without naming it. **→ F.W4** — a one-word rider on the banked N-12/FM-3 edit (the FR-AH-24 cure-completeness precedent: land together or the memo alone leaves the tax). Frame-time impact → SS-13. |
| **FR-MSP-4 · missed #2 — the `abbreviation` no-successor — RESCOPED TO FOLD (SC-1), riders ADMITTED** | reader | Substance re-verified by this seat at today's producer HEAD `407de2d3` AND both tags: `abbreviation` zero everywhere; v7 `Metric` = nine props, three spans, no dual-label channel; the 4.0.0 affordance real (d.ts:13-19 + components.css:182-183, my reads). **FOLD → fr-InfoCard FR-IC-10 (the no-successor booking, banked 2026-08-24) under the GAB-2/K-5 class and fr-EquationView D·D-M10 / fr-BasisSelector M-7's prop-VALUE-axis law.** Surviving increments, booked as riders: (a) **F.W1** — this corpus is now the fourth consumer surface whose written cure names the dead channel (C-4/C-5/L-14/D-11); the F.W1 spec must treat every `abbreviation`-citing prescription as doomed-by-default; (b) riders on **GAB-28** ("the `abbreviation` channel unused… the documented affordance for exactly this cluster") and **fr-AnimationControls M-5** ("sanctioned slots") — both now prescribe a dead prop and need re-derivation at the 8.0.0 truth; (c) the **FM-16/FMD-34 sequencing rider** (roster above) — local cure first; plus ruling 8's narrowing: the channel could never have expressed this file's placement-swap anyway. |
| **FR-MSP-5 · missed #3 — the FR-COB-13 hop-benefit scope correction — ADMITTED, INFO** | reader | Banked FR-COB-13 credits F.W1 with the missing-`type` cure because v7 `Button` pins `type ?? "button"` (my tag read, **:55 exact** — and SC-4 corrects M-7's contrary drift claim). But the credit pays only at `<Button>` call sites: my census finds **7 live native `<button>` sites**, and the two morph-family sites (`MorphShapePreview.vue:4`, `DarkModeToggle.vue:2`) were **deliberately retired-with-rationale from the A.W3 Button migration** (fourier W3-button-ledger.md:65 + the folder tally) — exactly the sites a `<Button>`-keyed sweep will not touch. Six of seven remain type-less after F.W1. **→ F.W1** — scope the census credit to `<Button>` call sites; the native-site residue keeps FR-COB-13's latent-MINOR identity. |

### INFO records

- **C-1 (aggravator 1) — ADJUDICATED-INFO.** The A.W3 ledger retired both native-button sites together; the SAME table records DarkModeToggle carrying its obligations forward ("the focus-visible outline survives via the local `<style>` block") while MorphShapePreview's rationale is purely visual — the unpaid balance is documented in the retirement record itself (seat re-read at the source). Provenance record for the F.W4 ticket; repo attribution corrected in the killed register (entry 6).
- **D-25 — ADJUDICATED-INFO, GLASS-RELAY.** The resting `--border` boundary at **1.94:1 light / 2.82:1 dark** (my own recomputation; anchors color-radius.css:44/:95 + dark-arm.css:46) is an upstream token shared by **21 occurrences / 14 files** (SC-3's corrected census — not "25"). Correctly NOT charged to this file; routes to the glass BH inbox with the existing `cartoon-card` + `--viz-amber` carries per the standing relay edict. Perceivability → SS-13.
- **C §3 negatives — RATIFIED.** R6-8 (operation↔client coupling) and R3-7b structurally UNREACHABLE here: static build-time JSON, zero `lib/api` edges (reader re-ran; concordant with my closure reads). A declared negative, preserved so no F.W5-W8 CRUD/provenance row manufactures an overlap.

## Killed-claims register — 8 entries, all reader kills challenged and SUSTAINED

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **C-6 ⊕ D-8 (CSS-motion legs)** — "no `prefers-reduced-motion` guard reaches `.morph-button`; the scale/lift motions are UNGUARDED" | **SUSTAINED — the falsifier fired; chain re-traced whole by this seat (ruling 1).** The producer's a11y blanket (a11y-overrides.css:6-16, reached via style.css:3 → index.css → utilities.css:19) strips transform/translate transitions under PRM with universal-selector `!important`. C-6's census stopped at `web/src`. Only the JS morph clock survives — already banked (FR-AH-31 + FM-11; FMD-22 carries the identical narrowing). |
| **K-2** | **D-14 (mechanism)** — "a second click's `stopAnim()` kills the first run's animation mid-await" | **SUSTAINED — refuted at `:87/:116/:149/:171` (ruling 2). FOURTH minting** of the thrice-banked kill (fr-DarkModeToggle K-2 · fr-FourierMorphSvg K-2 · fr-FourierMorphDemo K-1). The true failure — handle OVERWRITE orphaning run A — is strictly worse and already owned. |
| **K-3** | **D-2 (severity BLOCKER)** | **SUSTAINED as a grade kill; arithmetic fully CONFIRMED** (this seat's independent pipeline: 3.91/3.43/4.94/5.71 exact). The tints paint only during the 350 ms-default transient (`.info-chip.idle` has no rule — my read); the banked homes own the grade (FM-4 MINOR-with-dissent; FMD-19's suite MAJOR). A third-site BLOCKER re-rate is the anti-rename failure. |
| **K-4** | **D-11/C-4 (the prescribed cure)** — "adopt `Metric` at F.W1/F.W3; phase chip → `value` + `color`" | **SUSTAINED at the tag by this seat's own reads**: v7 `Metric` has no `color`/`tone` (types.ts verbatim); the tinted chip needs `Chip.tone` (`chip/types.ts:11`). The table is true at 4.0.0 and false at its own named destination — the "cure adjudicated separately from the defect" law. Symmetric bank rider minted on FMD-12/FM-14's "target `./chip`" (ruling 7: 3×Metric + 1×Chip). |
| **K-5** | **D axis · citation provenance (all closure files)** | **SUSTAINED with this seat's own drift table**: D quotes morphTo at `:141/:142/:145/:154` — live `:145/:146/:149/:169`; isAnimating `:107`→`:113`; guard `:130-131`→`:128`; flip `:136`→`:132`; phase writes `:154/:168/:180/:195`→`:169/:182/:196/:210`. Decisive controls verified: files HEAD-clean at `cd26c65` with pre-challenge mtimes; the L and C axes cite the SAME files line-exact (`:15/:18/:113/:128/:132/:134` all my reads); every D anchor into MorphShapePreview.vue ITSELF is exact. A D-axis closure-read provenance defect: substance survives, anchors must not travel into tickets. |
| **K-6** | **C-1 (repo attribution)** — "`value.js docs/tranches/A/audit/W3-button-ledger.md:65`" | **SUSTAINED**: the ledger lives in **fourier-analysis** (my read at the path, row verbatim); value.js's `A/audit/` is the HARDEN-1..6 + W0/W1 series (my ls). L-01 cites it un-prefixed and correctly. |
| **K-7** | **C-11 (operand census)** — "four declarations, five token references" | **SUSTAINED**: the four `color-mix()` calls carry exactly four `var()` operands (red/muted/red/pink — my read); the fifth counted reference is `:109`'s bare `border-color`, not a mix site. The substantive halves (verbose alpha idiom; premultiplied space inert; N-2's in-gamut self-kill) survive. |
| **K-8** | **L-04 (residual cost clause)** — "phase changes every tick" | **SUSTAINED**: `phase` writes 3× per morph + the `:210` idle return (my read); the per-tick writers are `currentPoints`/`currentPath` and `harmonicLevel`. An internal contradiction the row's own §D-1 had already stated correctly two pages later. FM-16 unaffected. |

## Superlatives — 9, ratified with provenance

1. **The strongest in the file (D·S-5 ⊕ L·S-1/S-2 ⊕ C·S-1)** — a leak-free, one-way presentational leaf: the entire `<script setup>` is `:47-62` — one import, six typed props, one payload-free emit; zero hooks/refs/listeners/timers/DOM/inject/store/expose/`any`/`as`/`!` (seat whole-read). Nothing to tear down — which matters because the composable one layer up has the real teardown defect (FR-AH-9); the ownership placement is why that fix is one file and this leaf needs none.
2. **The neutral chip is the in-file disproof of the failing pair (D·S-4 ⊕ S-2)** — `--foreground` on `--muted`@60% ≈ 16:1 both themes (bank-reproduced twice; direction confirmed by my pipeline), eight lines above the 3.43:1 rule — plus zero hard-coded colour in 175 lines, stricter than DarkModeToggle's own `#E88845`/`#c084fc` concessions.
3. **C·S-2 — dodging `lib/colors.ts`'s broken oklch path by staying in CSS** — the file consumes the oklch tokens where they still mean something; the fix belongs in colors.ts (banked fr-BasisCanvas D-1 / fr-App B-1), never in dragging this component onto the JS colour path. Preserve verbatim through F.W2.
4. **D·S-1 — the tap target**: 120/180 px with `flex-shrink: 0` — ~7.4× the WCAG 2.5.8 area minimum on the smallest rung; contrast banked FMD-4's arithmetic-starved sibling slider.
5. **C·S-3 — `FourierMorphSvg` correctly factored AND correctly consumed**: two consumers, two correct and DIFFERENT choices (token flow-through here; colour-animating override there), one primitive — the extraction working. The subject's stroke clears 1.4.11 in all four theme×surface cells (bank + reader concordant).
6. **L·S-3 ⊕ D·S-6 — one breakpoint, mobile-first, applied whole** (four queries, all 640, no max-width, parent concordant ×6) — why D-10's cure is a two-line container change; the honest blemish (literal ×4, no `--breakpoint-sm`) recorded, and FM-21/FMD-29 fault the SEAM choice, not the ratio.
7. **METHOD, corpus-level — the L axis's §D**: five claims published WITH the falsifiers that killed them, including its own opening headline and §D-2's supersession of a prior audit's headline for this exact file — then filing the two residues the CURE introduced (L-12, L-13). Retiring your own corpus's headline and booking the repair's side-effects is the §D discipline at its best.
8. **METHOD — the C axis's §4 N-3**: hypothesised Gibbs-overshoot escape, MEASURED per-level bounding boxes across all ten levels of both assets (worst ≈185.3 < 200), killed its own row, and preserved the honest residue as an inherited constraint on future shapes.
9. **GENUINE AND NOW DOUBLY CLOSED — L-10's declared load-bearing assumption**: the row named its one unexecuted premise precisely enough that two later seats each closed it in a single node run (reader + this seat, byte-concordant to six decimals). Naming your soft premise so the next seat can kill it in one command is the highest-leverage thing an axis document can do.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **D-3 residue** — whether the surviving UA focus ring is LEGIBLE against the 2 px cartoon border on the 120/180 px card (absence of an authored rule statically proven).
2. **D-6/D-7 residue** — whether the 1 px translate SNAP and the hover offset-stamp DELETION read as the inverted affordance (cascade + emitted rules statically settled; percept open).
3. **D-17 residue** — exact device-pixel strokes per band (the 1.54× ratio is scale-invariant and closed; absolutes are not; banked FM-21 already retired the corpus's older absolute-px cells).
4. **D-18 residue** — perceptibility of the one-tick `<path d="">` first paint (onMounted normally beats paint; suspended-mount exception banked I-4).
5. **L-10 magnitude** — whether an n≈1 / n≈54 / n=−8 excursion is NOTICED inside a 150 ms phase (arithmetic closed twice over; concordant with FMD-N4's residue).
6. **FR-MSP-3 frame-time** — the ≈8 700-tracked-reads/tick figure is derived and static; the profiled impact rides the banked N-12/FM-3 jank witness.
7. **D-25 residue** — whether the 1.94/2.82:1 boundary is perceivable given plate + stamp + scale; ratios closed, 1.4.11 verdict arguable; routes with the glass BH relay packet (21/14 census).

## DISSENT — preserved, not ruled away

1. **Severity ledger, cumulative**: the accessible-name family's BLOCKER is unanimous (third corpus); the PRM family's MAJOR dissent gains its **fourth and fifth** reader-seats (C-6 and D-8 both graded it MAJOR before the kill) — the banked MINOR-with-rationale (FR-AH-31/FMD-22) stands, the drumbeat is recorded. L-10's axis MAJOR preserved against FMD-N4's MINOR. FM-4 (chip pair, MINOR) vs FMD-19 (suite, MAJOR) is a live grade tension between two banked homes for one arithmetic — flagged for the F.W4 spec to resolve at the suite level, not per-site.
2. **FR-MSP-1's booking locus** — arguably the radius belongs to the shim/system (every cartoon-card site is radius-less), but the *defect* is contextual: this is the only 120/180 px CONTROL wearing the recipe on a fully-rounded route. Booked here; the F.W3 re-basing may moot it.
3. **The D axis's substance survives its provenance** — every D conclusion this record folds was independently true at the correct lines; K-5 exists to stop the anchors, not the findings.
4. **SC-1's epoch lesson** (recorded as evidence about the fleet): a hostile re-reader's registry sweep is a snapshot; two bank files rewrote themselves between the readers' run and this adjudication (fr-GalleryAdminBanner 11:40, fr-InfoCard 12:14, same day). Adjudicators must re-sweep at their own clock — the reader's "unbanked" claim was true when made and wrong when adjudicated.

## Closing verdict

**ADJUDICATED — LEAF RATIFIED, SEAMS ALREADY OWNED, TWO NEW BOOKINGS AND ONE AMPLIFIER ADMITTED (M-25 agglomeration input — stamps nothing):** 26 roster rows — 18 folds-by-reference into the banked FM-*/FMD-*/FR-AH-* families with zero re-books and zero identity drift; 2 new component-owned MINORs (the radius-less primary control FR-MSP-1; the `--muted`/`--foreground` pairing break FR-MSP-2); 3 reader-missed rows adjudicated (the deep-`ref` reactive-proxy amplifier ADMITTED at corrected-upward arithmetic; the `abbreviation` no-successor RESCOPED to today's fr-InfoCard FR-IC-10 fold with its three riders preserved; the FR-COB-13 census-scope correction ADMITTED); all 8 reader kills challenged and SUSTAINED at the bytes — including the producer-blanket PRM kill this seat re-traced import-by-import and the fourth minting of the stop-races kill; 4 seat corrections minted (the bank-epoch rescope; the ≈8 700-reads arithmetic; the 21/14 cartoon-card census against three wrong counts; the M-7 tag-line correction) plus the FMD-12 3×Metric+1×Chip mapping rider. The component itself remains what three corpora have now found it to be: a scrupulously typed, leak-free, one-way leaf whose every defect is either a surface choice (curable in one edit) or its producers' — and whose four chips assert, as fact, a shape not yet on screen, a level the renderer clamped away, and a fidelity the shipped data cannot reach, all of which the bank already owns under FMD-1/FM-20, FMD-9, and FMD-N4. Routings: F.W1 (uplift truths + census scopes), F.W3 (re-basing), F.W4 (the bulk), GLASS-RELAY (D-25 + the cartoon-card packet at the corrected census), SS-13 (7 residues). Original challenge files and both reader returns remain immutable beside this record per E-1/E-3.
