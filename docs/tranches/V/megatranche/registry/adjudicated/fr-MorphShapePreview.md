claude-fable-5 (served model id)

# fr-MorphShapePreview — adjudicated apotheosis r2 (M-23/L-18, incredulity seat over the SECOND hostile re-read)

**Seat**: fresh Fable adjudicator over two hostile re-readers (design-and-usage · library-and-code), second pass · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject**: `fourier-analysis/web/src/components/morph/MorphShapePreview.vue` (175 lines, read whole by this seat; mtime Mar 30, git-clean at fourier HEAD `cd26c65` — the only dirty morph files are HarmonicLevelGrid.vue and MorphPhaseConfig.vue, whose working-tree dirt is Slider `variant` swaps only, not the cited style rules — seat `git diff`).
**Corpus**: the immutable challenge files (`challenge-D-design.md` · `challenge-L-library.md` · `challenge-C-consumption.md`, untouched per E-1/E-3) + **two r2 re-reader returns** that hostilely re-read the full corpus AND the banked r1 record + **the r1 apotheosis (2026-08-24)**, which this file SUPERSEDES at the same path per the registry's living-file practice (r1's own SC-1 documented bank files rewriting themselves). **r1 is preserved verbatim in git history; every r1 identity — the roster rows, FR-MSP-1..5, K-1..K-8, SC-1..SC-4, superlatives 1..9, residues 1..7 — is carried forward here un-renumbered.** Dated evidence is not rewritten: the challenge files and both reader returns stand beside this record.
**Standing context**: this component's closure is now adjudicated across FOUR records — fr-FourierMorphSvg r2 (FM-1..FM-24, K-1..K-20), fr-FourierMorphDemo (FMD-1..FMD-N6), fr-MorphShapePreview r1, and this r2. The r2 readers' central discipline held: they folded to the bank at every banked identity and brought (a) a completed tail (rows r1 never rostered), (b) nine missed finds — one of which kills a banked-adjacent cure — and (c) five new kills. Dispositions are **ADJUDICATED, not VERIFIED** — X·F specs are still forming; routings are M-25 agglomeration input and stamp nothing.
**Method (incredulity)**: both readers were challenged back before adoption. Every NEW arithmetic cell was re-computed by this seat's own hsl/oklch→sRGB→WCAG pipeline and reproduced **byte-exact, including cells only one reader computed** (tint plates 1.205/1.169/1.117/1.140; solve-percentages 67/79/67/61; counterfactual fallback 4.43/3.76/4.74/5.58; neutral plates 1.038/1.084; D-2's 3.91/3.43/4.94/5.71; D-25's 1.94/2.82; the neutral chip 16.21). Every new kill was re-executed at the bytes (one sustained with a corrected mechanism — SC-6). The r2 readers' two verdict-level disagreements were ruled (rulings 2, 9). Three reader cells corrected (SC-5..SC-7) + one estimate corrected in-row (SC-8).
**This seat's own instruments (static + node/git only; zero browser probes — probe parsimony; product source READ-ONLY; this file the sole write)**: MorphShapePreview.vue whole (every reader anchor byte-exact: `:4/:65/:97/:108-112/:114-116/:118-120/:124-145/:147-156/:151/:152/:153-154/:165-174`, media queries exactly `:77/:100/:135/:158` all 640px, **zero hex literals** — `grep -cE '#[0-9a-fA-F]{3,8}'` → 0); useFourierMorph.ts whole (`:31/:60-64/:80-87/:101-102/:116/:145-149/:169-179/:182-204/:210-215/:217-229` — all exact); FourierMorphDemo.vue:1-200 (`:15/:16/:17/:18` bindings, `:27/:37/:47` titles, **`:31/:41/:51` slider-colors — the tint-concordance decider**, `:99-100` module-scope `as any`, `:103/:104/:113/:115-120/:123-125/:128/:132-134/:137-172`, and `.demo-page` `:183-193` **declares no background** — the FR-MSP-7 backdrop decider, with body `@apply bg-background` at style.css:20); useMorphConfig.ts (`:30` candidates literal no-shape-argument, `:47-49` totalMs sum); fourier style.css whole (`:3` producer import, `:40-50` root-font step, `:92-96` its own PRM block, `:98-111` shim + stale "14 sites" comment, `:113-127` viz-amber, `:136-143` four-class ring allow-list); HarmonicLevelGrid.vue at the working tree (`:36/:44/:115` the 100s, `:203/:257` `#60a5fa`, **`:242-245` hover = 50%-mix + scale(1.04) vs `:251-254` `.active` = `--accent-red` + `0 0 0 2px` 15%-mix — the FR-MSP-11 decider**, dirt = variant swaps only); FourierShapeExtractor.vue `:15/:78` `#ccc` (S-12); installed glass-ui **4.0.0** (`node -p` version): color-radius.css neutrals/aliases/accents + dark-arm.css read at the lines (light `--neutral-0 hsl(40 30% 98%)` / `--neutral-1 hsl(38 26% 95%)` / `--neutral-4 hsl(32 26% 70%)` / `--muted: var(--neutral-1)` / `--muted-foreground: var(--neutral-5)` / `--accent-red oklch(0.574 0.216 27.5)` / `--accent-pink oklch(0.613 0.197 353.8)`; dark `hsl(24 9% 4%)` / `hsl(28 12% 11%)` / `hsl(30 16% 34%)` / `oklch(0.644 0.165 22.9)` / `oklch(0.683 0.131 354.7)`), utilities/a11y-overrides.css `:6-16` PRM blanket verbatim (**transform/translate ABSENT from the `!important` allow-list** — K-1's chain re-held); **the built artifact** `web/dist/assets/FourierMorphDemo-CQRQVWTU.css` (mtime Jun 12 vs subject mtime Mar 30 → admissible for THIS file), extracted at byte positions: base `.info-chip[data-v-c69c00c8]{…background:var(--muted);border-radius:.375rem;padding:.1875rem .5rem;font-weight:500}` (the SYNTHESIZED LEGIBLE base fallback) at 1327; `@supports (color:color-mix(in lab,red,red))` guards at 1578/2061/2412; **`.info-chip.settle-out…{background:var(--accent-red)}` at 1955 — OUTSIDE any `@supports`** — then the guarded 12% mix at 2105, then `{color:var(--accent-red)}` at 2247; identical shape for `.morph`/`--accent-pink`; `.morph-button:hover` fallback `box-shadow:0 0 0 3px var(--accent-red)` (solid ring) — the FR-MSP-12 deciders; **seat WCAG pipeline (node)** — all cells listed under Method; **censuses** — `prefers-reduced-motion` **15 occurrences / 12 files** (the 12th = `src/router/index.ts`, absent from reader-1's 11-file list — SC-5); `cartoon-card` **21 occurrences / 14 .vue files** (SC-3 re-held); `v-for` in subject → 0; `type="button"` → exactly 1 (ImageUpload.vue:94); `<button` → 8 lines / 6 files, one inside Tooltip.vue's JSDoc usage example (seat-read `:1-12`) → **7 live** (K-10 decider); `grep -rn morph e2e/` → one line (visual-baseline.spec.ts:35; PAGES list + `reducedMotion:"reduce"` + `animations:"disabled"` + overflow≤2 sole expect — seat-read); web/package.json scripts = dev/build/preview/test:e2e/test:e2e:ui, **no unit runner in devDependencies** (seat-read); **registry sweeps** — `grep -rn "4\.1\.3" fr-*.md` → **0 hits across the whole fr- registry** (FR-MSP-9 decider); `color-mix(in lab` → fr-CanvasOverlayButton + fr-NotationPills only; **FR-COB-26 read verbatim** ("machine-generated non-color-mix fallback… foreground-on-foreground, 1:1 … NO-WAVE-OWNER (producer) — BH relay member") + **FR-COB-28's seven-item relay packet** (FR-MSP-12 joins it); **FR-NP-8 read whole** (the missing-`@reference` mechanism; 7 files / 19 unguarded declarations — the coin-flip tension); FMD-1/FMD-6/FMD-9/FMD-12/FMD-19/FMD-30/FMD-34/FMD-N3/FMD-N4 + HLG-9 + fr-BasisSelector m-2 + fr-MorphPhaseConfig MPC-26 + fr-PaperSearch K7/D-20 all read at the rows (every fold target real; **FMD-19 books text ratios only — no plate-vs-page figure anywhere in the bank**; FMD-30 books the desktop arm + "two no-op declarations"); **glass-ui producer at the tag** (`git show v7.0.0:`) — `MetricProps` = value/unit/placeholder/loading/label/context/class/size/orientation, **no `color`/`tone`/`abbreviation`**; exports `./badge ./chip ./metric`, `./metric-badge` ABSENT; `chip/types.ts` `tone?: string` (K-4 re-held at the tag); **challenge-file re-reads at every load-bearing cell** — D `:124-133` (the void cure verbatim: "carry state in the plate only (the neutral chip already proves 16.21:1)"), D `:274` ("15 hits"), D `:412-430` (the killed stop-races mechanism + the drifted `:141/:142/:145/:154` anchors), D `:444-451` (D-15, parent titles cited `:26/:35/:44` — drifted; live `:27/:37/:47`), D `:558-567` ("all **25** sites"), D `:298-302` (D-9's desktop-only cure), C `:31-41` (C-1 + the aggravator-2 aria-label prescription at `:38`), C `:45-51` (the injection clause at `:49`), C `:110-129` (C-6's completeness sentence at `:119` + C-7), C `:94-106` (C-5 + the correct per-tick attribution at `:102`), C `:159-173` (C-11's five-vs-four self-contradiction; **C-12's "sitting on `--card`" at `:171` + its declared "I did not measure computed contrast"**), L `:60-70` (L-01's un-prefixed correct ledger cite), L `:148-155` + `:576-592` (L-04's per-tick clause vs its own §D-1 correction), L `:171-182` (L-05's "8 native sites" denominator), L `:318-328` (L-10's declared premise), L `:610-619` (§D-5's own scoped-hash withdrawal — the SC-6 concordance). **Not re-run**: the value.js back-easing extrema — already closed by THREE byte-concordant executions (r1 seat + both r2 readers, six decimals); a fourth adds nothing.

## Scoreboard

**51 adjudicated roster rows** — 20 carried r1 folds (all re-ratified under the second hostile re-read; zero identity drift) · **14 tail rows rostered at r2** (the corpus rows r1 left un-rostered — reader-1's D-15 flag, generalized by this seat's own r1 diff) · 2 carried component-owned bookings (FR-MSP-1/2) · 3 carried r1 missed-row adjudications (FR-MSP-3/4/5) · **7 NEW bookings from the r2 readers' missed finds (FR-MSP-6..12** — one MAJOR: the void-cure lock**)** · 5 INFO records. **13 killed-claims register entries** — K-1..K-8 carried and RE-RATIFIED (both r2 readers independently re-executed several; all fold to the existing ids, no re-mintings) + **K-9..K-13 new**, one sustained-with-corrected-mechanism (K-11/SC-6). **4 new seat corrections (SC-5..SC-8)**, continuing r1's SC-1..SC-4. **12 superlatives** (r1's 9 sustained, one updated; 3 new). **11 UNPROVEN-NEEDS-LIVE residues → SS-13.**

The r2 headline is **FR-MSP-6 ⊕ K-13**: the D-2 challenge row's own prescribed cure — "keep the tint plate, set `color: var(--foreground)`, and carry state in the plate only" — is **structurally VOID and a net regression if executed**: the 12% tint plate it would promote to sole state channel measures **1.117–1.205:1** against its own backdrop in all four theme×accent cells (seat-reproduced byte-exact), the 1.4.11 bar is 3:1, and the mix percentage that would clear it (**61–79%**) turns the plate into a filled badge on which `--foreground` is no longer a legal text colour. The "16.21:1 proof" the cure cites is a fact about TEXT, not the plate. A two-channel lock is minted so no F.W4 ticket can ship the regression under a green-looking edit. The secondary headline is **FR-MSP-12**: the build pipeline's synthesized `color-mix` fallback paints the phase chips **accent-on-accent (1.00:1)** on any non-`color-mix` engine — a new site of the banked FR-COB-26 class with a counterfactual this site uniquely proves (the generator's own base-rule fallback is legible and would win absent the tint fallback: the generator *manufactures* the failure).

## The incredulity seat's own work — rulings and corrections

1. **FR-MSP-6 admitted at MAJOR; K-13 minted (the D-2 cure kill) — the r2 headline, fully re-derived.** Reader-1's plate arithmetic reproduced by this seat's own pipeline to the third decimal in all four cells (red 12% over `--background`: 1.205 light / 1.117 dark; pink: 1.169 / 1.140) and the solve-for-3:1 percentages reproduced exactly (67/79/67/61%). The corpus cell killed is challenge-D-design.md:128-132, read verbatim by this seat. Registry-swept before admission: FMD-19's suite and FM-4 book **text** ratios only; FR-MSP-2 explicitly disclaims the contrast axis; **no banked row anywhere carries a plate-vs-page figure** — the channel's deadness was unmeasured until this pass. The "cure adjudicated separately from the defect" law (ColorSpaceSelector addendum item 2) applies exactly: D-2's defect folds unchanged (FM-4/FMD-19 own it); its cure dies; the lock replaces it. Grade MAJOR as filed — the row guards a MAJOR home's repair from shipping a regression under a green gate, the precedent being the ColorSpaceSelector L-3 anti-projection lock.
2. **FR-MSP-7's booking locus — the readers disagreed; RULED: new booking, not a rider.** Reader-1 filed the invisible neutral plate (1.038:1 light / 1.084:1 dark vs page) as a distinct missed find; reader-2 filed the identical measurement as "an evidentiary rider" closing C-12's declared unproven half. Both computed **byte-identical hexes and ratios** (`#f8f6f3`/`#fbfaf8`, `#171513`/`#0b0a09`) — expected from shared tokens, and reproduced exactly by this seat. Ruling: the mechanism (background-vs-backdrop) and the cure (drop the 60% alpha or move a neutral rung) are DISJOINT from FR-MSP-2's (foreground pairing; re-pair the text), and FR-MSP-2's banked text explicitly scoped contrast OUT — a rider would bury a defect inside a row that disclaims its axis. Booked as **FR-MSP-7**, with FR-MSP-2 cross-referenced (one edit site `:153-154`, two defects) and C-12's wrong backdrop cell killed (K-12).
3. **FR-MSP-12 (reader-2's missed find) — re-extracted at the bytes and ADMITTED as a fold.** This seat pulled the emitted rules at byte positions from the built artifact: the tint fallback `background:var(--accent-red)` sits **outside** any `@supports` (position 1955) and, by specificity, overrides the base rule's own synthesized `background:var(--muted)` (position 1327) on engines failing the guard — solid accent under accent text, 1.00:1, both pairs. The counterfactual is provable exactly as claimed: strip the synthesized tint fallback and the legacy path lands on solid `--muted`, which this seat measures at **4.43/3.76 light · 4.74/5.58 dark** — legible. Admissibility: dist mtime Jun 12, subject mtime Mar 30 and git-clean — the artifact is faithful for this file. Identity: **FOLD → FR-COB-26** (the banked machine-generated-fallback class, NO-WAVE-OWNER producer, BH relay member) as a **new site** — the first on `/morph`, the first where the failing pair is a route's live readout, and the first with a provable manufactures-the-failure counterfactual. The site joins **FR-COB-28's consolidated relay letter**. The FR-NP-8 tension is recorded verbatim: read together, the bank now says the synthesis is a coin-flip — absent where it would help (7 files, 19 unguarded declarations), harmful where it fires (here).
4. **SC-5 — reader-1's replacement census corrected: 15 occurrences across 12 files, not 11.** The kill of C-6's completeness sentence ("the three sites above are the complete set" — challenge-C:119) is SUSTAINED by count either way, but reader-1's own list omits `src/router/index.ts` (seat `grep -rln`). D-8's parallel "15 hits" occurrence-count cell (challenge-D:274) is EXACT, as reader-1 said. The corrected census travels with K-9.
5. **SC-6 — reader-2's C-2 kill sustained, mechanism corrected.** The kill's stated ground — "an injected class can only match a rule that also carries this file's scope hash" — is true of the SFC's own scoped rules and **false for global unscoped classes** (Tailwind/glass-ui utilities are emitted unscoped; a hostile `phase` string could in principle name one). What actually fells the claim is the **supply side**: the sole caller (seat grep) passes `morph.phase.value`, a `Ref<MorphPhase>` from the typed composable — no untrusted input reaches the prop, so "uncontrolled… by construction" is false at the producer, concordant with the L axis's own §D-5 withdrawal and its L-03 residue (which folds, confirmed, at D-19). Kill SUSTAINED on corrected ground (K-11).
6. **SC-7 — reader-1's missed #8 rescoped downward.** "0.1875rem is not a step" is wrong under Tailwind v4's dynamic spacing scale (any 0.25-unit multiple is representable; 0.1875rem = `0.75` units), so the MPC-26/PaperSearch-D-20 "off-grid" class does NOT cleanly apply. What survives is the half the row itself computed: **fractional device pixels under the 1.125rem root** (3.375px — and the ≥640 rung's on-grid 0.25rem yields 4.5px, equally fractional), which is the banked root-seam mechanism. Rescoped to an INFO rider on **FM-21/FMD-29**, not a standalone row.
7. **SC-8 — reader-1's missed #3 spare-width estimate corrected in-row.** Their "~59px to spare at 375px" against wrap: this seat's own arithmetic (4 chips ≈ 24ch × ~9.45px + 8×9px padding + 3×6.75px gaps ≈ 319px inside a 375 − 27px-padding container) lands the margin nearer **~29px**. The no-wrap conclusion at 375px stands; 320px moves from "marginal" to "probably wraps" — the SS-13 probe must check both. The ±28px translation estimate itself is sound (6-char phase swing × ~9.45px/ch ≈ 57px, centred box).
8. **The tail roster completed.** Reader-1 flagged D-15 as "NOT rostered in the banked adjudication — surfaced here so it is not lost"; this seat diffed the whole r1 roster against the corpus and found **16 corpus identities un-rostered** (D-15/16/18/20/21/22/24/26, L-06/07/15/17/18, C-9/10/11-core/14 — merging pairs, 14 roster rows). All are rostered below with dispositions, so the M-25 agglomeration consumes a COMPLETE roster. Not an r1 error of substance — r1's scope was the identity discipline over the readers' folds — but a completeness gap this pass closes.
9. **D-22's grade — the readers disagreed (reader-1 MINOR, reader-2 INFO); RULED INFO, MINOR dissent preserved.** The shape never carrying the phase colour is an unused affordance and a coherence observation — no capability lost, no falsehood asserted; `morphProgress` is exported (`:84/:223`, seat-read) and the sibling consumes it, so the F.W4 spec inherits an OPTION, not a defect. Reader-1's "colour applied to the metadata and withheld from the object it describes" is preserved as dissent — it is the better sentence.
10. **K-1..K-8 re-ratification.** Both r2 readers independently re-executed the r1 kills they touched — the PRM producer-blanket chain (both re-traced the import chain themselves), the D-14 stop-races mechanism (both re-derived the orphaned-handle truth; the corpus now carries a FIFTH minting of the thrice-banked kill), the D-2 grade, the D-11/C-4 cure at the v7 tag, the D-axis drift table (reader-2 built its own against a git-clean HEAD with controls), C-1's repo attribution, C-11's operand count, L-04's per-tick clause. All fold to K-1..K-8 unchanged; this seat spot-re-executed the chain ends (a11y-overrides bytes; v7 tag types/exports; the drift anchors `:141/:142/:145/:154` vs live `:145/:146/:149/:169`; the ledger location) and everything held. **Zero r1 kills overturned by the second re-read** — the register is stable under hostility, which is what a register is for.

## Adjudicated defect roster

Line anchors are fourier HEAD `cd26c65` (subject and closure files clean; HarmonicLevelGrid anchors are working-tree, dirt verified cosmetic). Grades on folds are the banked homes' — a fold carries the bank's identity and severity. Re-resolve all anchors before executing any cure (the D-19 lesson).

### A · Carried r1 folds — all re-ratified under the second hostile re-read

| id | claim (adjudicated form) | disposition |
|---|---|---|
| **D-1 ⊕ L-01 ⊕ C-1** | The `/morph` route's only interactive control computes an EMPTY accessible name; no cure route on the public surface (fallthrough → the `generic` `.demo-stage` div; no label prop/slot/forward). Gate census exact at three seats' runs: one `morph` line in e2e (a screenshot harness), all checkA11y sites `/visualize`. BLOCKER, third corpus, zero drift. | **FOLD → FM-1 (BLOCKER) + FMD-2** · **F.W4** · axe on `/morph` → SS-13. **r2 rider: FR-MSP-10** — the corpus's prescribed label is poisoned; the name must derive from transaction-stable state (below). |
| **L-11 ⊕ C-3** | `n=75`/`n=100` printed over a level-50 reconstruction; three surfaces offer levels the shipped data lacks; the route's two highlight systems disagree about one click. Assets re-measured (levels top at 50). | **FOLD → FMD-1 (BLOCKER) / FM-20 (MAJOR)** · **F.W4** (derive ladder + High bound from `shape.data.levels`, naming the source shape per FM-20's mid-transit rider). |
| **D-4 ⊕ L-02 ⊕ C-7(a)** | `:disabled` during the morph blurs the focused control with no restore (350 ms default / 2 400 ms max — bounds re-verified), duplicating the parent's `:128` JS guard; zero `focus()`/`tabindex` in the subtree. | **FOLD → the banked focus-theft row (FM-4..FM-16 batch)** · **F.W4** (`aria-disabled` + keep the guard). |
| **D-14 (conclusion only)** | No pending state across the async engine boundary: during the cold import the UI reads `idle`, stays enabled, and a second activation re-enters `morphTo` (first phase write `:169` after the `:149` await; `isAnimating` solely `phase !== "idle"`). Mechanism KILLED (K-2 — fifth minting recorded). | **FOLD → FMD-6 / fr-DarkModeToggle M-3·L-4 (the FR-AH-8/-9/-20/-21 epoch family)** · **F.W4**. |
| **L-09** | `shapeName` flips to the DESTINATION on the click tick (`:132` before `:134`) while phase 1 renders the SOURCE up to 800 ms; the same flip swaps all 12 grid tiles. | **FOLD → FR-AH-8 / FMD-9** · **F.W4**. Feeds FR-MSP-10. |
| **D-7** | Hover REPLACES the offset-stamp shadow with a flat 3 px halo — the card flattens exactly when it lifts (`box-shadow` is one property; `--lift-sm` still fires, so depth and position cues disagree). Built-artifact witness re-extracted at r2 (unlayered scoped emission; single non-composite shadow). | **FOLD → FMD-N3 (MAJOR, riding FR-AH-11)** · **F.W4** · glass BH relay rides the cartoon-card packet at the corrected 21/14 census (SC-3). |
| **L-16 ⊕ C-1(agg. 3)** | Nothing gates any of it: vitest absent (seat package.json read), `/morph`'s sole e2e is a capture harness whose only expect is overflow≤2 under forced reduced-motion, axe never visits the route. | **FOLD → FMD-31 + FM-10** · **F.W4** · feeds SS-13's probe list. |
| **D-2 (arithmetic)** | Both phase-tint chips fail WCAG AA light (**3.91 / 3.43**; dark 4.94/5.71) — seat-reproduced to the hundredth a second time at r2. Grade owned by the homes (K-3); **the corpus row's own cure is now KILLED (K-13) and replaced by the FR-MSP-6 lock.** | **FOLD → FM-4 (MINOR, MAJOR dissent preserved) + FMD-19 (suite, MAJOR)** · **F.W4 token edit — under the FR-MSP-6 two-channel lock.** |
| **D-3 ⊕ C-7(b)** | No `:focus-visible` on the one interactive element; the house ring's four-class allow-list excludes it; no producer outline reset reaches a bare `<button>`, so the UA ring survives — non-conformance, not invisibility. | **FOLD → the banked ring-sweep row (FM-4..FM-16 batch)** · **F.W4** · legibility → SS-13. |
| **D-5** | The disabled state's entire treatment is `cursor: wait` (`:118-120`); cursors do not exist on touch; mobile is first-class in this file. Confirmed at the built artifact too (sole emitted `:disabled` rule). | **FOLD → FMD-6** · **F.W4** — rider: the affordance must span the WHOLE animating window and be non-cursor. D-21's `progress`-keyword point rides this cure (below). |
| **D-6 ⊕ L-12** | The `:97` scoped `transition` shorthand resets `transition-property` wholesale (unlayered scoped beats `@layer utilities`), dropping the producer's `translate` — the cartoon lift SNAPS while `scale(1.02)` eases; `--duration-normal`/`--spring-smooth` discarded. | **FOLD → FMD-27, riding FR-AH-11** · **F.W4**. |
| **D-9 ⊕ D-23** | The desktop chip column stretches to its longest member (4→10→5→9 chars per toggle); `.stage-row`'s centring declarations are inert at EVERY breakpoint (reader-2's sharpening adopted: fit-content in both arms). | **FOLD → FMD-30** · **F.W4** — **r2 rider: the cure as written (`align-items: flex-start` on `.desktop-info`) does not touch the mobile arm — see FR-MSP-8.** |
| **D-10 ⊕ L-04 ⊕ C-5** | Eight chips render four facts — `:13-26` ≡ `:30-43` modulo wrapper class, `display:none`-toggled; zero `v-for` (iteration-keyed censuses blind — R5-7's face). L §D-1's self-withdrawals sound. | **FOLD → FM-16 via FMD-34** · **F.W4** — sequencing rider carried: cure the duplication LOCALLY before any primitive adoption; `abbreviation` is dead at the destination AND was inapposite (placement, not label, differs). |
| **D-11 ⊕ L-14 ⊕ C-4** | Bespoke chip where the CURRENT pin ships the primitive, re-typing producer magic numbers (`:111` 1.02 ≡ components.css:56; `:115` 0.98 vs shipped 0.96; no focus rule vs `:63`). Cure table killed at the tag (K-4, re-held by this seat's own v7 reads). | **FOLD → FM-14 / FMD-12** · **F.W1/F.W3** — mapping rider: **3×`Metric` + 1×`Chip.tone`**. |
| **D-12 ⊕ C-13** | Non-monotonic type ladder 15.75 → 18.00 → 16.00 px — component 640 breakpoint (×4) vs root-font 768 step; padding rides the same fault. | **FOLD → FM-21 + FMD-29** · **F.W4** (one seam decision). SC-7's fractional-pixel rider lands here. |
| **D-13** | The route's primary affordance wears a decoration-only CARD recipe via the resurrected shim while secondary Export/Reset are genuine glass `Button`s — the design language inverts with importance; sole control site of `cartoon-card` in the folder. | **FOLD → FMD-12 + FR-AH-11** · **F.W3** (re-basing, not break-cure). |
| **D-17** | Fixed user-space `stroke-width=4.5` under changing render scale → ≈2.10/3.15/3.24 px, 1.54× spread (ratio scale-invariant; absolutes → SS-13). | **FOLD → FM-6 + FM-21** · **F.W4**. |
| **D-19 ⊕ L-03 ⊕ C-2 (core)** | `phase: string` discards the exported `MorphPhase` union that is simultaneously display text AND a CSS class selector; `.info-chip.idle` has no rule, so a typo is indistinguishable from idle; vitest absent → the widening disarms the only gate. C-2's injection clause killed separately (K-11). | **FOLD → FMD-33** · **F.W4**. |
| **L-05** | Native `<button>` with no `type` → defaults `submit`; latent (no `<form>` on `/morph`). Denominator corrected at r2: 1 `type="button"` vs **7 live** native sites (K-10). | **FOLD → FR-COB-13 (latent-MINOR class)** · **F.W1** — with FR-MSP-5's census-scope correction. |
| **L-10** | The `n=` readout escapes `[lowLevel, highLevel]` — n=1, n=54, n=−8 reachable — display unclamped (`:174/:203`) while render clamped (`:175/:204`). The declared premise closed by THREE byte-concordant executions. | **FOLD → FMD-N4 (MINOR as banked; axis MAJOR dissent preserved)** · **F.W4** (clamp at the write, FM-12's idiom). |

### B · Tail rows — rostered at r2 (the corpus identities r1 left un-rostered; both readers confirmed each)

| id | claim | disposition |
|---|---|---|
| **D-15 ⊕ L-08 ⊕ C-8** | The chips print the raw kebab-case state-machine enum (`{{ phase }}` at `:15/:32`) as user-facing prose while the SAME view labels the same three phases in title case (`FourierMorphDemo.vue:27/:37/:47` — the C axis's anchors exact, the D axis's `:26/:35/:44` drifted, both seat-read). No label map anywhere in the chain. | **MINOR · F.W4** — a label map beside the `MorphPhase` union; **coupled to FR-MSP-8**: title-case labels are LONGER, so the map and the chip `min-width` must land together or the translation worsens. |
| **D-16** | Three of four chips render bare unlabeled values (`{{ shapeName }}`, `{{ totalMs }}ms`; only `n=` carries a key) — ambiguous as a readout, unannounceable once named. | **MINOR · FOLD → the FM-4..FM-16 batch** (which owns the labelling/live-region family) · **F.W4**. |
| **D-18** | No empty state: first paint renders `<path d="">` with confident metadata beside it (`currentPoints` init `[]`; `pointsToSvgPath` → `""` under 2 points; shape set only in the parent's `onMounted`); module-scope `prepareFourierShape(sunData as any)` with a suppressed type and zero error boundary in the chain. | **MINOR · F.W4** — leaf half (v-if/skeleton) component-owned; the module-scope `as any` + boundary half is demo-owned and rides the FMD family. Perceptibility → SS-13 (residue 4, banked I-4 exception noted). |
| **D-20** | Hand-rolled `font-family: var(--font-mono)` (`:148`) where the producer ships `@utility fira-code` (family + liga/calt feature settings, utilities.css:69-72 verbatim) and BOTH siblings use it (HarmonicLevelGrid `:15/:38`, MorphPhaseConfig `:17/:19`) — the chips get the family but not the treatment, in the same view as siblings that do. | **MINOR · F.W1/F.W4** — one-class swap. |
| **D-21** | `cursor: wait` overstates the busy scope (`wait` = the application is busy; the page's three config cards, the grid, Export and Reset all stay live during a morph — seat-read `:26-79`); `progress` is the correct keyword. | **INFO · rider on FMD-6/D-5's busy-affordance cure** · **F.W4**. |
| **D-22** | The shape never carries the phase colour — no `strokeColor` passed, so the default `var(--accent-red)` holds through all four phases while the chips speak red/pink state; `morphProgress` is exported for exactly this and DarkModeToggle consumes it. | **INFO (ruled — ruling 9; reader-1's MINOR preserved as dissent)** · **F.W4 option note** on the phase-colour work. |
| **D-24 ⊕ C-10 (view-box half)** | `view-box="0 0 200 200"` restates the callee's own default byte-for-byte (third restatement at DarkModeToggle); by contrast `:stroke-width="4.5"` genuinely overrides (default 3) and is correct to pass. | **INFO · F.W4 hygiene** — delete the redundant prop, keep the override. |
| **D-26** | Two spellings of the ring token in one repo (`var(--ring)` at style.css:140; `var(--color-ring)` at DarkModeToggle:99, the Tailwind bridge alias) — a component adopting the house ring has two equally-precedented choices. Both resolve (bridges verified at r1). | **INFO · F.W1** — pick one spelling in the ring sweep. |
| **L-06** | Six required scalar props where two would do; `disabled` is a pure function of `phase` (the sole caller derives it `:113` → `:18`); four of six exist only to be printed into markup-identical chips. | **MINOR · F.W3** — the API re-shape belongs to the re-basing decision (KISS: not a standalone edit). |
| **L-07** | Presentation logic split across the boundary in both directions: the parent rounds (`:15 Math.round`), the child appends the unit (`{{ totalMs }}ms`), and the child's `number` type accepts the unrounded float the composable actually holds. | **MINOR · F.W4** — one side owns rounding + unit. |
| **L-15 ⊕ L-18** | Measures: 64% of the file is CSS serving two concerns; instance arithmetic exact (1+1 consumers, 8 unrolled chips, 12 grid instances). | **INFO** — context records for F.W3. |
| **L-17 ⊕ C-9** | Config echo and live state share one undifferentiated chip row: `totalMs` is a computed config SUM (useMorphConfig:47-49) beside per-tick live state, identical chrome, three epistemic statuses; durations are captured ONCE per morph (destructure after the await) while the sliders stay live — the `ms` chip updates mid-morph, the running animation keeps captured values. | **INFO · F.W4 design note** — separate the registers (the `MorphPhaseConfig :disabled` question is FMD-owned). |
| **C-11 (core)** | Four `color-mix(in srgb, var(--token) N%, transparent)` declarations spell alpha verbosely; the premultiplied-against-`transparent` space is inert; N-2's in-gamut self-kill preserved (all four accents in-gamut). Census cell killed (K-7). | **INFO · F.W4** — spelling choice rides the token edit. **Note: the fallback SYNTHESIS over these declarations is FR-MSP-12's defect — cure interactions there.** |
| **C-14** | `cartoon-card` is ALIVE via the local shim (style.css:98-111 + `cartoon-surface` at the installed pin) — the prior D-audit's dead-class headline for this exact file is CURED, recorded in L §D-2's supersession. Site-count cells all wrong (SC-3: 21/14; the shim's own comment stale at "14 sites/13 files"). | **INFO truth record** — the shim's comment corrected by whichever wave touches it; census travels with the BH relay packet. |

### C · Component-owned bookings (carried from r1)

| id | claim | disposition |
|---|---|---|
| **FR-MSP-1 · L-13 — MINOR** | NO `border-radius` reaches `.morph-button` from any of its three style sources — the 120/180 px primary control is the one square-cornered surface on a fully-rounded route (file's only radius is `:152`, info-chip; shim = border-color+background only; `cartoon-surface` declares none; confirmed in the built artifact). | **ADJUDICATED → F.W4** — may discharge free under FMD-12's F.W3 re-basing; the ticket checks F.W3's outcome first. |
| **FR-MSP-2 · C-12 (pairing half) — MINOR** | `--muted` consumed with `--foreground` (`:153-154`), breaking the producer's `--muted`/`--muted-foreground` pairing — four secondary readouts compete typographically with primary text. The pairing break is HIGHER contrast, so it is semantic/hierarchy, not a11y — that scoping stands **for the text**. | **ADJUDICATED → F.W4**, rides FMD-19's token edit. **r2 increments: the row's backdrop cell is corrected (K-12 — the chips sit on `--background`, not `--card`) and the PLATE's own invisibility is now a separate booked defect (FR-MSP-7) at the same edit site.** |

### D · r1 missed-row adjudications (carried)

| id | ruling |
|---|---|
| **FR-MSP-3 — REACTIVITY-DEPTH AMPLIFIER — MINOR (carried)** | `currentPoints` deep-`ref` over a 512-tuple array replaced wholesale per rAF tick ≈ 8 700 tracked reads + ~512 fresh tuple proxies per tick (r1's SC-2 corrected-upward arithmetic); cure `ref`→`shallowRef`, provably safe (zero in-place mutations, zero external consumers). **F.W4** one-word rider on the N-12/FM-3 edit. Frame-time → SS-13 (residue 6). |
| **FR-MSP-4 — the `abbreviation` no-successor — FOLD (carried; SC-1's epoch event)** | Owned by fr-InfoCard FR-IC-10 under the GAB-2/K-5 class; riders preserved (fourth consumer surface prescribing the dead channel; GAB-28/fr-AnimationControls-M-5 re-derivation; the FM-16/FMD-34 sequencing rider). **F.W1.** |
| **FR-MSP-5 — FR-COB-13 hop-benefit scope correction — INFO (carried)** | The v7 `type ?? "button"` credit pays only at `<Button>` call sites; the two morph-family native sites were retired-with-rationale from the A.W3 migration and a `<Button>`-keyed sweep will not reach them; six of seven live native sites remain type-less after F.W1 (denominator corrected at r2 — K-10). **F.W1** census-scope note. |

### E · NEW r2 bookings — the second re-read's missed finds, adjudicated

| id | claim + proof | disposition |
|---|---|---|
| **FR-MSP-6 · r2-missed #1 (design reader) — MAJOR — THE VOID-CURE LOCK** | The phase-tint plate is a dead state channel: `color-mix(in srgb, var(--accent-red) 12%, transparent)` over `--background` measures **1.205:1 light / 1.117:1 dark** plate-vs-page; `--accent-pink` 12%: **1.169 / 1.140** (seat pipeline, byte-concordant with the reader). The 1.4.11 bar for state-identifying visual information is 3:1; the mix that clears it is **67/79/67/61%** per cell — a filled badge on which `--foreground` is no longer legal text. Today state IS conveyed (at 3.91/3.43) by exactly the text-colour swap D-2 faults; D-2's prescribed cure ("carry state in the plate only… the neutral chip already proves 16.21:1" — challenge-D:128-132, seat-read) would remove the only working channel and leave the inert one, citing a TEXT fact as a PLATE proof. Registry-swept: no banked row carries a plate-vs-page figure. | **ADJUDICATED → F.W4 — a LOCK on the FM-4/FMD-19 contrast edit** (K-13 is the companion kill): any cure must EITHER (deepen the plate to ≥3:1 vs backdrop AND re-pair the text to ≥4.5:1 on the deepened plate) OR add a non-colour channel (hairline border, icon, or the phase text itself once D-15's label map lands) — never a single-channel edit. Percept of the current plates → SS-13. |
| **FR-MSP-7 · r2-missed #2 (both readers, ruled at ruling 2) — MINOR** | The `.info-chip` pill affordance does not render: `--muted`@60% over the actual backdrop (`--background` — `.demo-page` sets none, body carries `bg-background`; seat-read) = **1.038:1 light (`#f8f6f3` on `#fbfaf8`) / 1.084:1 dark (`#171513` on `#0b0a09`)** — below every perceptibility threshold. Mechanism: `--muted` = `--neutral-1`, ONE ~3-point lightness rung from `--neutral-0` (= `--background`), and the 60% alpha discards 40% of an already-minimal delta. Twelve lines of plate/radius/padding CSS produce four bare mono words — the state three of four chips are ALWAYS in, and the fourth at rest. Quantifies the M-critique's "nearly invisible at rest" that C-11/C-12 could only cite. | **ADJUDICATED → F.W4** — drop the 60% mix or move a real rung (e.g. `--neutral-2`); one edit site with FR-MSP-2 (`:153-154`), two defects, cure together. Percept → SS-13. |
| **FR-MSP-8 · r2-missed #3 (design reader) — MINOR** | The MOBILE chip run translates horizontally ≈ ±28 px at each end, four times per toggle: below 640 `.mobile-info` is the live arm, a shrink-wrapped flex item centred by `.demo-stage`'s `align-items: center` (`:72`, seat-read), its width driven by the phase enum's 4→10→5→9 character swing + the `n=` digit count, `white-space: nowrap` on every chip, 15.75 px mono at the mobile rung (≈57 px box swing, centred → ±28 px per end), directly beneath the shape the user is watching. D-9's cure (`align-items: flex-start` on `.desktop-info` — challenge-D:298-302, seat-read) and banked FMD-30 (desktop-arm mechanism + "two no-op declarations", seat-read at the row) leave this arm untouched. Second-order increment: `.mobile-info`'s own `justify-content: center` (`:132`) is a THIRD inert declaration at this site (live only if the run wraps). Class precedent at a different component only (fr-BasisSelector m-2, seat-read). | **ADJUDICATED → F.W4 — rider on FMD-30**: `min-width` (ch-sized to the longest label) on the phase chip; **coupled to D-15** (title-case lengthens the strings — land together). Wrap behaviour at 320/375 px → SS-13 (SC-8's corrected margin: ~29 px at 375). |
| **FR-MSP-9 · r2-missed #4 (design reader) — MINOR** | WCAG 4.1.3 Status Messages: the phase readout is precisely a progress status message ("the waiting state of an application, the progress of a process") with no `role="status"`, no `aria-live`, no programmatic association to the control — and the control is simultaneously `disabled`, stripping even the incidental focus-based announcement path. An AT user who triggers the morph receives NO signal for 350–2 400 ms. Registry sweep by this seat: **zero `4.1.3` rows across the entire fr- registry**; the banked a11y family (FM-1 name, the ring/focus batch, HLG-9's ARIA absence) covers name/focus/state-on-controls, never status messages. Design constraint that makes this spec-work, not a bolt-on: `harmonicLevel` mutates per rAF tick (`:174/:203`), so the live region must scope to the PHASE chip alone — a naive `aria-live` on `.demo-info` emits ~60 announcements/second. | **ADJUDICATED → F.W4** — rides the FM-4..FM-16 batch + FMD-6's busy-state cure as ONE announcement design (name, `aria-disabled`, busy affordance, status region — four coupled edits, one spec). |
| **FR-MSP-10 · r2-missed #5 (design reader) — MINOR — CURE-INTERACTION RIDER on the BLOCKER fold** | C-1's prescribed `aria-label` (`` `Morph to ${shapeName === 'Sun' ? 'Moon' : 'Sun'}` `` — challenge-C:38, seat-read) is poisoned by the banked flip: `shapeName` derives from `isMoon`, which flips SYNCHRONOUSLY before the await (`:132`/`:134` — L-09/FMD-9/FR-AH-8), so the prescribed name INVERTS at the instant of activation and is wrong for the whole settle-out window; under D-4's own cure (`aria-disabled`, control stays focusable) a screen reader can and will read it. The r1 record held both halves without the join (endorsing "the name is already in hand" at the D-1 fold while booking the flip at L-09). | **ADJUDICATED → F.W4 — a lock on the FM-1/FMD-2 name cure**: derive the label from transaction-stable state (capture before the flip, or an explicit `nextShapeName` computed after the await, or a static verb). |
| **FR-MSP-11 · r2-missed #6 (design reader) — MINOR** | Cross-component state-vocabulary collision in one view: the primary control's HOVER (`:108-112` — `--accent-red` border + `0 0 0 3px` 15%-mix flat ring) is the same recipe as a grid cell's SELECTED state (HarmonicLevelGrid `:251-254` — `--accent-red` border + `0 0 0 2px` 15%-mix ring; seat-read at the working tree, dirt cosmetic), differing only 3 px vs 2 px — "the pointer is here" and "this is the current level" wear one costume, on screen simultaneously. Within the grid the two states ARE distinguished (`:242-245`: 50% border-mix + `scale(1.04)`), proving per-component authorship never reconciled across components. Registry: HLG-9 books the grid's literals and ARIA absence, not the collision. | **ADJUDICATED → F.W3** — one ring vocabulary chosen at the FMD-12 re-basing. |
| **FR-MSP-12 · r2-missed (library reader) — MINOR — FOLD → FR-COB-26 (new site)** | The machine-synthesized non-`color-mix` fallback paints ACCENT-ON-ACCENT: the built artifact emits `.info-chip.settle-out/.settle-in{background:var(--accent-red)}` OUTSIDE any `@supports` (byte position 1955, seat-extracted), overriding by specificity the base rule's own synthesized legible fallback (`background:var(--muted)` solid, position 1327), then `{color:var(--accent-red)}` — **1.00:1** on any engine failing `@supports (color:color-mix(in lab,red,red))`; identical shape for `.morph`/pink. **The counterfactual is provable at this site**: absent the tint fallback, the legacy path lands on solid `--muted` at **4.43/3.76 light · 4.74/5.58 dark** (seat-measured) — the generator manufactures the failure. Cosmetic sibling: hover's `box-shadow: 0 0 0 3px var(--accent-red)` solid ring. Admissible: dist Jun 12, subject Mar 30 + git-clean. FR-NP-8 tension recorded (the synthesis is a coin-flip: absent where it would help, harmful where it fires). | **ADJUDICATED FOLD → FR-COB-26** (banked class, NO-WAVE-OWNER — producer/pipeline) — this site joins **FR-COB-28's consolidated BH relay letter** as item 8, carrying the counterfactual (the strongest evidence the class has). Engine REACH → SS-13 / the F.W1 browser-support decision (`color-mix` is Baseline-2023; the failing band is legacy-only and its size separates MINOR from INFO). |

### F · INFO records

- **C-1 (aggravator 1) — carried.** The A.W3 ledger retired both native-button sites together; the same table documents DarkModeToggle paying its obligations forward while MorphShapePreview's rationale is purely visual — the unpaid balance is recorded inside the retirement record. Provenance for the F.W4 ticket; attribution corrected at K-6.
- **D-25 — carried, GLASS-RELAY.** Resting `--border` at **1.94:1 light / 2.82:1 dark** (seat-recomputed a second time, exact) — upstream token, shared by **21 occurrences / 14 files** (not "25" — K-8-adjacent census, SC-3), correctly not charged here; rides the BH relay with the cartoon-card packet. Perceivability → SS-13.
- **C §3 negatives — carried, RATIFIED.** R6-8 and R3-7b structurally unreachable (static build-time JSON, zero `lib/api` edges) — preserved so no F.W5-W8 row manufactures overlap.
- **PRM residue (r2-missed #7, design reader) — ADMITTED-INFO, rider on K-1.** The producer blanket does not GUARD this component's motion — it makes it differently broken: under PRM the `!important` rewrite strips the TRANSITIONS but not the transforms, so hover becomes a simultaneous instantaneous 1 px translate + 2% scale with the offset stamp still deleted (D-7) — a one-frame spatial jump arguably harsher than the 150 ms eased version, against the blanket's own stated intent ("preserve opacity, remove spatial motion", a11y-overrides.css header, seat-read). 2.3.3 satisfied by the letter; filed so the F.W4 spec does not read K-1 as "nothing to do here", and relayed to the glass BH inbox as an observation on the blanket's transform omission.
- **Off-grid spacing (r2-missed #8, design reader) — ADMITTED-INFO as rescoped (SC-7), rider on FM-21/FMD-29.** `padding: 0.1875rem 0.5rem` (`:151`) computes to **3.375 px** at the 18 px root and the ≥640 rung's 0.25rem to **4.5 px** — both fractional, on a plate whose own background is 1.04:1 (FR-MSP-7), leaving the sub-pixel edge the only thing defining it. The "not a Tailwind step" cell is corrected (v4's dynamic scale represents 0.75 units); the defect is the ROOT-SEAM's fractional pixels, already owned by FM-21/FMD-29 — this row is its chip-padding instance.

## Killed-claims register — 13 entries

K-1..K-8 are r1's, re-ratified under the second hostile re-read (both r2 readers independently re-executed the chains they touched; nothing moved). K-9..K-13 are new at r2.

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | C-6 ⊕ D-8 (CSS-motion legs) — "no PRM guard reaches `.morph-button`" | **SUSTAINED (carried)** — the producer blanket (a11y-overrides.css:6-16, re-read at r2) strips transform/translate transitions under PRM with universal `!important`; both r2 readers re-traced the import chain themselves. Only the JS morph clock survives — banked (FR-AH-31 + FM-11; FMD-22's narrowing). **r2 rider: the kill has a RESIDUE (the snap percept) — see the PRM INFO record.** |
| **K-2** | D-14 (mechanism) — "a second click's `stopAnim()` kills the first run mid-await" | **SUSTAINED (carried)** — `stopAnim` is null-guarded before any handle assignment on the cold path; the real failure is handle OVERWRITE orphaning run A (strictly worse, already owned). The r2 corpus minted this kill a FIFTH time (fr-DarkModeToggle K-2 · fr-FourierMorphSvg K-2 · fr-FourierMorphDemo K-1 · r1 K-2 · both r2 readers) — the register entry exists so the wrong causality never travels into a ticket. |
| **K-3** | D-2 (severity BLOCKER) | **SUSTAINED (carried)** — arithmetic confirmed a THIRD time by this seat (3.91/3.43/4.94/5.71 exact); `.info-chip.idle` has no rule, so the failing paint exists only in the ≤2 400 ms transient; FM-4 (MINOR-with-dissent) + FMD-19 (suite MAJOR) own the grade. Both r2 readers deferred to the homes. |
| **K-4** | D-11/C-4/L-14 (the prescribed `Metric` cure incl. `abbreviation`) | **SUSTAINED (carried)** — re-held at the tag by this seat's own `git show v7.0.0` reads: `MetricProps` has no `color`/`tone`/`abbreviation`; `./metric-badge` absent; `Chip.tone` is the tint channel. Mapping rider: 3×`Metric` + 1×`Chip.tone`. And even at the pin, `abbreviation` could not express this file's placement-swap (r1 ruling 8). |
| **K-5** | D axis — closure-file citation provenance | **SUSTAINED (carried)** — drift table re-spot-checked (`:141/:142/:145/:154` vs live `:145/:146/:149/:169`; titles `:26/:35/:44` vs `:27/:37/:47`); reader-2 rebuilt the table against a git-clean HEAD with decisive controls (subject anchors byte-exact; L and C cite the same files line-exact). Substance survives; anchors must not travel. |
| **K-6** | C-1 (repo attribution of the W3 button ledger) | **SUSTAINED (carried)** — the ledger lives in fourier-analysis; L-01 cites it un-prefixed and correctly (seat re-read of challenge-L:66-67). |
| **K-7** | C-11 (operand census "four declarations, five token references") | **SUSTAINED (carried)** — four `color-mix()` calls, four `var()` operands; the fifth is `:109`'s bare `border-color`; the heading ("five sites") contradicts its own parenthetical one line later (seat re-read of challenge-C:159-165). Substantive halves survive. |
| **K-8** | L-04 (residual cost clause "phase changes every tick") | **SUSTAINED (carried)** — `phase` writes 4× per morph (`:169/:182/:196/:210`); per-tick writers are `currentPoints`/`currentPath` and `harmonicLevel`; the row's own §D-1 states it correctly two pages later (internal contradiction, seat re-read of both cells). C states it correctly (challenge-C:102). |
| **K-9** | **C-6 (census completeness cell)** — "the three sites above are the complete set of `prefers-reduced-motion` occurrences in `web/src`" (challenge-C:119, seat-read verbatim) | **KILLED (new, reader-1's kill sustained with SC-5)** — seat census: **15 occurrences / 12 files** (style.css, PaperView, ContourSettings, VisualizationView, AnimationControls, GalleryCard, GalleryMarquee, CollapsibleSection, ConvergencePlot, SvgFilters, DarkModeToggle, **router/index.ts** — the file reader-1's 11-file list omits). A second, independent defect in C-6 beyond the K-1 producer-blanket kill: the exact sentence carrying the row's falsifier is false by twelve. D-8's parallel "15 hits" occurrence cell is EXACT. |
| **K-10** | **L-05 (denominator)** — "1 hit, against 8 native `<button>` sites" (challenge-L:176-177, seat-read) | **KILLED (new, reader-2's kill seat-verified)** — 8 grep lines, but ui/tooltip/Tooltip.vue:8 is inside the component's JSDoc usage example (seat-read `:1-12`), not a rendered element. Live count **7** (DarkModeToggle 1, MorphShapePreview 1, AnimationControls 2, ImageUpload 1, AdminAuditLog 2). Finding unaffected; the denominator travels into FR-MSP-5's census note. |
| **K-11** | **C-2 (class-injection clause)** — "a stringly-typed value flowing into `:class` is also an uncontrolled class-injection seam by construction" (challenge-C:49, seat-read) | **KILLED (new) — SUSTAINED on CORRECTED ground (SC-6).** Reader-2's scope-hash argument covers only this SFC's scoped rules; global unscoped utilities remain matchable by ANY class string. The decisive ground is supply: the sole caller passes `Ref<MorphPhase>` from the typed composable — no untrusted input reaches the prop, so "by construction" is false at the producer. Concordant with the L axis's own §D-5 withdrawal; the surviving residue is exactly L-03/D-19 (confirmed in roster A). |
| **K-12** | **C-12 (backdrop cell)** — the chips "sitting on `--card`, i.e. near-identical to its own backdrop" (challenge-C:171, seat-read) | **KILLED (new)** — the chips sit in `.demo-info` on `.demo-page`, which declares NO background (FourierMorphDemo:183-193, seat-read); the backdrop is body's `--background` (style.css:20). The conclusion ("near-identical to its backdrop") survives — indeed strengthened: vs `--background` the plate is 1.038/1.084 (FR-MSP-7's measurement, which uses the CORRECT backdrop). The row's pairing half is untouched (FR-MSP-2). |
| **K-13** | **D-2 (the prescribed cure)** — "keep the tint plate, set `color: var(--foreground)`, and carry state in the plate only (the neutral chip already proves 16.21:1)" (challenge-D:128-132, seat-read verbatim) | **KILLED (new) — the r2 headline.** The plate channel the cure relies on measures 1.117–1.205:1 in all four cells (seat-reproduced); 3:1 requires 61–79% mix, at which point `--foreground` fails on the plate; the 16.21:1 citation is a TEXT fact offered as a PLATE proof. Executing this cure at F.W4 removes the only working (if sub-AA) state channel and leaves the inert one — a net regression under a green-looking edit. Replaced by the FR-MSP-6 two-channel lock. The "cure adjudicated separately from the defect" law's cleanest instance yet in this registry. |

## Superlatives — 12, with provenance

1. **The strongest in the file (D·S-5 ⊕ L·S-1/S-2 ⊕ C·S-1 ⊕ both r2 readers) — carried.** A leak-free, one-way presentational leaf: the entire `<script setup>` is `:47-62` — one import, six typed props, one payload-free emit; zero hooks/refs/listeners/timers/DOM/inject/expose/`any`/`as`/`!` (three seats' whole-reads). The ownership placement is why the composable's real defects are one-file fixes and this leaf needs none.
2. **The neutral chip is the in-file disproof of the failing pair — carried, now double-edged.** `--foreground` on the neutral plate = 16.21:1 (seat-reproduced), eight lines above the 3.43:1 rule — **and r2 shows the same chip's PLATE is invisible (FR-MSP-7) and its 16.21 was mis-spent as a plate proof (K-13)**. The superlative survives about the text; its citation history is the cautionary tale.
3. **C·S-2 — dodging `lib/colors.ts`'s broken oklch path by staying in CSS — carried.** Preserve verbatim through F.W2.
4. **D·S-1 — the tap target — carried.** 120/180 px with `flex-shrink: 0` — ~7.4× the WCAG 2.5.8 area minimum on the smallest rung.
5. **C·S-3 — `FourierMorphSvg` correctly factored AND correctly consumed — carried.** Two consumers, two correct different choices, one primitive; the stroke clears 1.4.11 in all four theme×surface cells (re-ratified by an r2 reader's independent recomputation, byte-exact).
6. **L·S-3 ⊕ D·S-6 — one breakpoint, mobile-first, applied whole — carried.** Four queries, all 640, parent concordant ×6; the honest blemish (literal ×4) recorded; FM-21/FMD-29 fault the SEAM, not the discipline.
7. **METHOD — the L axis's §D — carried.** Five claims published WITH their falsifiers, including its own headline and the supersession of a prior audit's headline for this exact file.
8. **METHOD — the C axis's §4 N-3/N-2 — carried.** Gibbs-overshoot hypothesised, MEASURED across all ten levels of both assets, self-killed, residue preserved; the oklch-gamut reading self-killed by measurement.
9. **L-10's declared load-bearing premise — carried, now CLOSED A THIRD TIME.** r1's seat + both r2 readers each closed it in one node run, byte-concordant to six decimals (`ease-out-back` max 1.086857 · `ease-in-back` min −0.096882 · `∓0.092713`). Naming your soft premise so the next seat can kill it in one command has now paid three times — the highest-leverage thing an axis document can do.
10. **NEW (both r2 readers independently; seat-verified) — the chip tint language is an exact cross-component match to the parent's slider colours.** `.settle-out/.settle-in` → `--accent-red` ≡ FourierMorphDemo:31/:51 (`slider-color="var(--accent-red)"` on Settle Out/Settle In); `.morph` → `--accent-pink` ≡ :41. Three phases, two components, one colour vocabulary, zero shared code enforcing it — the reason the chip row reads as an index into the config cards. Partly answers D-22 on its own terms. Falsifier (checked by all three seats): any mismatch — none. Unrecorded by all three axes and by r1.
11. **NEW (library reader; seat-verified) — the file is on the correct side of a banked repo-wide MAJOR.** `@reference "tailwindcss"` (`:65`) puts this SFC inside the pipeline whose ABSENCE is FR-NP-8 (7 siblings, 19 unguarded `color-mix` declarations); all four of this file's mixes emerge with `@supports` guards, and the directive is load-bearing twice (two `@apply` rungs + guard synthesis) — distinguishing it from the banked inert-`@reference` cohort. Honest tension recorded: the arm it earns is itself defective at the two tinted chips (FR-MSP-12).
12. **NEW (library reader; seat-verified) — zero hard-coded colour, measured against its own folder.** HarmonicLevelGrid hard-codes `#60a5fa` ×2 (`:203/:257`); FourierShapeExtractor inlines `#ccc` ×2 (`:15/:78`); MorphShapePreview has **zero** literals in 175 lines (seat grep) — the only fully-tokenised file in `components/morph/`. Sharper than r1's DarkModeToggle comparison.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. **D-3 residue (carried)** — legibility of the surviving UA focus ring against the 2 px cartoon border at 120/180 px.
2. **D-6/D-7 residue (carried)** — whether the 1 px translate SNAP + hover stamp DELETION read as the inverted affordance.
3. **D-17 absolutes (carried)** — per-band device-pixel strokes (the 1.54× ratio closed).
4. **D-18 residue (carried)** — perceptibility of the one-tick `<path d="">` first paint (banked I-4 exception).
5. **L-10 magnitude (carried)** — whether the n≈1/n≈54/n=−8 excursion is noticed inside a 150 ms phase (arithmetic closed thrice).
6. **FR-MSP-3 frame-time (carried)** — profiled impact rides the banked N-12/FM-3 jank witness.
7. **D-25 residue (carried)** — perceivability of the 1.94/2.82:1 boundary; travels with the BH relay packet (21/14 census).
8. **NEW — plate percepts**: whether the 1.038/1.084 neutral plates and 1.117–1.205 tint plates are perceivable on real displays at real ambient — the felt severity of FR-MSP-6/7. Ratios closed by computation; the percept is the SS-13 question.
9. **NEW — the mobile chip-run**: felt magnitude of the ≈±28 px translation at 375 px, and whether the run wraps at any shipped viewport (SC-8: ~29 px margin at 375 says no; 320 px probably wraps — check both).
10. **NEW — FR-MSP-12 REACH**: which engines in this deployment's real audience fail `@supports (color:color-mix(in lab,red,red))`. Baseline-2023 says legacy-only; the band's size separates MINOR from INFO and is the F.W1 browser-support decision's input.
11. **NEW — L-01's remedy-table cell**: whether a consumer `aria-label` on the `generic` `.demo-stage` div is DISCARDED vs merely misplaced (ARIA 1.2 prohibits naming `role=generic`; AX-tree exposure engine-specific). The load-bearing half (no label ever reaches the button) is static and closed.

## DISSENT — preserved, not ruled away

1. **Complementary blindness as fleet evidence.** The two r2 readers' missed-find sets are DISJOINT — eight design-side finds (plates, translation, 4.1.3, name-poisoning, vocabulary collision, PRM residue, spacing) vs one library-side find (the build-artifact fallback), and neither found the other's. Same corpus, same subject, same week. Hostile re-readers inherit their axis's instruments; a registry needs both axes to converge, and an adjudicator must never treat one reader's empty `missed` list as evidence of exhaustion.
2. **Severity ledger, cumulative (carried + r2).** The accessible-name BLOCKER is unanimous across four records. The PRM family's MAJOR dissent stands at five reader-seats against the banked MINOR-with-rationale — the drumbeat is recorded, the bank holds. L-10's axis MAJOR preserved against FMD-N4's MINOR. FM-4 (MINOR) vs FMD-19 (MAJOR) remains a live grade tension for one arithmetic — now sharpened by FR-MSP-6: whichever grade wins, the EDIT is now under a lock. D-22: reader-1's MINOR preserved against this seat's INFO ruling.
3. **FR-MSP-6's grade** — arguably the lock is process, not product, and "MAJOR" grades the hazard to the repair rather than the shipped pixels. Booked MAJOR on the ColorSpaceSelector L-3 precedent (a cure that ships a regression under a green gate is the costliest class of defect a registry can fail to record); the dissent is noted for the F.W4 spec author.
4. **Reader concordance on the plate hexes** (byte-identical `#f8f6f3`/`#171513` from both) is expected from shared tokens, not evidence of citation-inheritance — unlike the ColorSpaceSelector event, no stale cell was repeated here; both readers' pipelines also reproduced cells the other did NOT compute, which is the true independence witness. Recorded so a future seat doesn't mistake convergence for copying.
5. **The r1 supersession** — this file replaces r1 at its path per the registry's living-file practice; a reader who needs r1's exact prose (its instruments block, its ruling order) reads it in git history at the pre-overwrite blob. Nothing in r1 was overturned: r2 completes (tail roster), extends (FR-MSP-6..12, K-9..K-13, SC-5..SC-8, S-10..S-12), and re-ratifies everything else.

## Closing verdict

**ADJUDICATED — REGISTER STABLE UNDER SECOND HOSTILITY; ROSTER COMPLETED; ONE BANKED-ADJACENT CURE KILLED AND REPLACED WITH A LOCK (M-25 agglomeration input — stamps nothing):** 51 roster rows — 20 r1 folds re-ratified with zero identity drift and zero kills overturned; 14 tail rows rostered so the M-25 census consumes a complete corpus; FR-MSP-1..5 carried with r2 increments; **7 new bookings admitted from the second re-read's missed finds** — the void-cure lock FR-MSP-6 (MAJOR: D-2's "carry state in the plate only" would promote a 1.117–1.205:1 dead channel to sole state carrier; two-channel lock minted, K-13), the invisible pill plate FR-MSP-7 (1.038/1.084, backdrop corrected via K-12), the mobile chip-run FR-MSP-8 (FMD-30's uncovered arm), the registry's first WCAG 4.1.3 row FR-MSP-9, the name-stability lock FR-MSP-10 (C-1's cure × L-09's flip, the join the bank held unconnected), the hover≡selected vocabulary collision FR-MSP-11, and the manufactured accent-on-accent fallback FR-MSP-12 (FOLD → FR-COB-26 as a new site with the class's first provable counterfactual, joining the FR-COB-28 relay letter); 5 new kills (K-9..K-13) with one sustained on corrected ground (SC-6) and four seat corrections minted (SC-5..SC-8). Every new arithmetic cell in both returns reproduced byte-exact by this seat's own pipeline — including the cells only one reader computed. The component remains what four records have found it to be: a scrupulously typed, leak-free, one-way leaf whose defects are surface choices or its producers' — and the r2 lesson is about the REGISTRY, not the leaf: a banked record can hold both halves of a poisoned cure without the join, a challenge row's disposition can be the most dangerous sentence in it, and the plate under the text nobody measured was the difference between a cure and a regression. Routings: F.W1 (uplift truths + census scopes + browser-support decision), F.W3 (re-basing: L-06, FR-MSP-11, D-13), F.W4 (the bulk, under two locks), NO-WAVE-OWNER (FR-MSP-12 → producer relay; D-25 + cartoon-card packet at 21/14), SS-13 (11 residues). Original challenge files, both r2 reader returns, and r1 (in git history) remain immutable beside this record per E-1/E-3.
