Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SpringTrace; receipt read from this seat's own system prompt, not inherited).

# kf-SpringTrace — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c`, `origin/master` `81a56990`; worktree package.json/lock DIRTY (the L-2 decider, ruled below) but **`SpringTrace.vue` byte-identical across worktree, HEAD, and origin/master** (my `git diff` both ways → 0 lines), so no component verdict is baseline-contaminated. · **Registry tree** value.js `tranche-u` · **Corpus**: three challenge axes (`audit/kf-components/SpringTrace/challenge-{D-design,L-library,C-consumption}.md` — D 16 defects/5 superlatives, L 14/5, C 10/5) + two hostile re-reader returns (**reader-A**, design-and-usage: 18 confirmed identities · 8 kills · 6 misses · superlatives; **reader-B**, library-and-code: 26 confirmed · 5 kills · 6 misses). Both returns consumed whole; every inter-reader contradiction and every load-bearing verdict re-derived by this seat before adoption.

**Method (incredulity).** Zero product writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SpringTrace.vue (129 L), linear-stops.ts (71 L), sample.ts (66 L), timingCurveUtils.ts (90 L — the corpus decider), easing-registry.ts:50-99, timing-function.ts:60-116; SpringTarget.vue:1-40/:53-62/:104-108/:124-140/:448-455; targeted greps: `text-transform` (spring scene + styles → exactly 1 hit, SpringTarget.vue:454), `forced-colors` demo-wide (0), `sr-only` census (3 genuine sites), `--ball-tone:` (3 scene-root declarations), vendor-token `var(…, fallback)` census (exactly 1 demo-wide, AmigaScene.vue:262), `useSpringLinearStops` roster (2 consumers), `SpringSidebar` find (0), `generateCurveSVGPath|getCurvePath|timingCurveUtils` demo roster (4 consumer files) AND across all three challenge files (2 hits, both naming `animationDescriptions.ts` — a different file); the installed `@utility text-mono-caption` extracted verbatim (uppercase confirmed); **three-baseline dependency probe run fresh** (worktree grep glass-ui → 0/0 with both manifests `M`-dirty; `git show HEAD:package.json:71` → 6.0.0; `git show origin/master:package.json:77` → `"@mkbabb/glass-ui": "7.0.0"` exact, lockfile hits 3; installed 7.0.0); **node against the SHIPPED dist** (`dist/keyframes.js`, stronger than either reader's transcription): the component's `:50-86` parse/fill replayed verbatim on the real emitter → `n=26 · first pct=2 · last pct=98 · guards fired false/false · first5 2,4,8,12,16`; response-invariance grid → **byte-identical string at ζ=0.2 across response 0.1↔1.2**, max |Δv| 3.7e-4 elsewhere (0.016 px @72px); drawn max 1.51685 @ζ=0.2, exactly 1 @ζ=1.0; `grep -c "sampleNormalizedSpring\|resolveLinearStops" dist/keyframes.d.ts` → 0; **my own OKLCh→OKLab→linear-sRGB→WCAG chain, both theme arms** — light 4.61/1.85/1.34, dark accent rgb(190,149,236) → **5.78 PASS / 2.32 FAIL / 1.41 FAIL**; design-idioms.css:244-248 `.code-token` + font-roles.json `".code-token"` + `_monoContract` verbatim; EasingTarget.css:127-131/:173-175 (neutral-by-default sparkline) + EasingTarget.vue:97-103 (the duplicated SVG idiom); registry novelty greps (`stroke-dasharray`, `code-token`, response-inert → 0 hits each). Banked precedents re-read at their bytes: KF-ET-36 (kf-EasingTarget.md:81, INFO), KF-CE-13 (kf-CSSCodeEditor.md:47), KF-ES-8/KF-ES-17 (MAJOR uppercase-register class), kf-KfPillTabs.md:43 (KF.W4-PROSE phantom-consumer rider, SpringSidebar named verbatim), kf-AmigaScene.md:19 (the substrate overrule + citation-inheritance event), kf-SequenceAxis.md:45/:105 (the tabular-nums trap).

## Scoreboard

**32 roster rows, original ids for life — 0 BLOCKER · 8 MAJOR · 15 MINOR · 9 INFO.** Both filed BLOCKERs die to MAJOR (banked bar + banked class). **5 net-new rows admitted (N-1..N-5)** from the readers' 12 filed misses; the other 7 fold as cells/riders into existing identities (anti-double-booking). **Killed-claims register: 12 entries** — headed by L-2 (a MAJOR killed twice over) and the first killed census-contribution in this component's ledger (S-9 "justified bespoke"). **12 superlatives** (one RESCOPED). **10 UNPROVEN residues** → SS-13.

**Headline.** The incredulity seat's yield ran in both directions. Between the readers: they contradict each other exactly once at full weight — reader-A endorsed the axes' census contribution *S-9 · JUSTIFIED BESPOKE, no counterpart exists* ("two seats, two enumerations, one conclusion") while reader-B proved the enumerations were scoped to the wrong tree: **`demo/utils/reference-data/timingCurveUtils.ts:48` exports `generateCurveSVGPath(fn, n)`, cached, consumed by four files, rendering through the identical viewBox/`preserveAspectRatio="none"`/non-scaling-stroke idiom one scene over** — verified whole by this seat, along with the fact that all three challenge documents mention that directory only via a different file. Reader-B wins; reader-A's endorsement is recorded as an adoption-without-re-derivation event (it re-verified glass-ui's 68/73 subpaths and never ran one grep over the demo). Against the readers: reader-A's "every child is `shrink-0`" cell (its M-4) is false at the bytes — the `:55` rail wrapper carries no `shrink-0` — the finding survives with a corrected mechanism; reader-B's "two live consumers" undercounts the util's reach (2 direct + 2 via `getCurvePath`) and its cache cell misses that the memo is name-keyed and could never serve a parameterized spring. Against the corpus: both filed BLOCKERs demoted on banked calibration, D-13's clearance arithmetic corrected 0.25→0.67 px, L-14's margin corrected 1.9→2.5 %, and the L-2 phantom-dep MAJOR killed by this seat's own fresh three-baseline probe — the same substrate event the registry has now recorded at kf-AmigaScene, kf-AnimatedText, and kf-AnimationControlsGroup, re-fired here. The strongest new fact is convergent, not contested: **the plot is provably invariant to its `response` prop** — both readers derived it independently, and this seat reproduced it against the shipped dist artifact itself (byte-identical emission at ζ=0.2), which no prior seat had done.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0 / KF.W4-PROSE / KF.W7 / KF.W9) with **NO-WAVE-OWNER** for the spring-plot repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **Reader-A endorses census row S-9 "JUSTIFIED BESPOKE — no counterpart exists" (D-§S-9 = C-S-B) and wants it landed in lane-frontend §5; reader-B kills it via `generateCurveSVGPath`** | **B WINS — the census row is KILLED AS STATED (K-6); the superlative RESCOPED; N-2 admitted** | `timingCurveUtils.ts` read whole: `:48` `export function generateCurveSVGPath(fn: EasingFunction, n = 32): string`, body `:50-55` the identical polyline algorithm (`1 - v` at `:53` — the honest 0..1-range caveat, kept); name-keyed memo `:69-90`; consumers `useEasingDemo.ts:113`, `useTimingFunctionEditor.ts:74` (direct) + `EasingTarget.vue:195`, `useTimingFunctionEditor.ts:80` (via `getCurvePath`); `EasingTarget.vue:97-103` renders through SpringTrace's own SVG idiom. My grep across all three challenge files for the util/its file → 2 hits, both `animationDescriptions.ts`. The glass-ui half of both axes' enumeration stands (73 subpaths, no plot primitive); the "no counterpart" clause is false and the row must not be written. Corrections against B recorded in N-2. |
| 2 | **L-2 (MAJOR, "the phantom-dep bite") — killed by both readers on baseline law** | **KILL SUSTAINED (K-1), twice over** | My fresh probe: worktree manifests both `M`-dirty, glass-ui grep 0/0; HEAD:71 `6.0.0`; **origin/master:77 `"@mkbabb/glass-ui": "7.0.0"` EXACT + 3 lockfile hits; installed = 7.0.0** — `npm ci` from the shipping baseline is whole; both "silent" failure modes cannot fire. Second, independent kill: the identity is banked and thrice-overruled (kf-AmigaScene.md:19 "RESCOPED-BY-SUBSTRATE … a recorded citation-inheritance event"; census F-1/SCH-1 → KF.W0 §B-12, never re-booked). L-9 dies with it (ruling 3). Census F-1 must be re-measured against origin/master before routing any wave. Surviving residue: an INFO-weight note that the local worktree manifest is dirty — not a row. |
| 3 | **L-9 (`var()` fallbacks) — reader-A KILLED it outright; reader-B admitted it at INFO** | **KILLED AS FILED (K-2) — A wins; B's INFO preserved as dissent** | The cited "house idiom" is a misread: `--ball-tone` is an override SLOT, defined at three scene roots (my grep — EasingTarget.css:7, SpringTarget.vue:278, SequenceTarget.css:8) and overridden per-instance; `var(--ball-tone, var(--color-progress))` is parameterization-with-default — `.derby-lane-tag:452` itself uses exactly that form. The defensive form on vendor tokens exists at **exactly one site demo-wide** (AmigaScene.vue:262, my census) — one site is not a house style. And L-9's stated stakes ("what turns L-2 from degraded into invisible") died with L-2. The banked counter-cell (kf-SequencePlayhead D-14: `--radius-pill`'s defensive read where the token lives only in a Tailwind `@theme`) covers a different token class and does not revive this row. |
| 4 | **N-1 admission — the `response` prop is visually inert (reader-A M-1 ≡ reader-B M-1, both MAJOR)** | **ADMITTED MAJOR — and upgraded from transcription to artifact proof** | Algebra verified at the bytes: `omega = 2π/response` (progress.ts:157), `dt = 4·response/25` (linear-stops.ts:49/:59) → ω·tᵢ = 8πi/25, response cancels; stepper closed-form (`O(machine epsilon), not O(dt²)`, progress.ts:270-272); abscissae `(i/25)·100` response-free (:64). **This seat ran the SHIPPED `dist/keyframes.js` itself** — `springLinearStops` at response 0.1 vs 1.2: byte-identical at ζ=0.2; max |Δv| ≤ 3.7e-4 (= 0.016 px at 72 px) at ζ ∈ {0.45, 0.86, 1.0, 1.5}, all from the absolute settle-pin threshold. Neither reader executed the real emitter; both transcribed. C's S-C derived the cancellation and drove past its meaning — credited in the roster. |
| 5 | **D-5 grade — reader-A MAJOR ("the graphic is the component's entire reason to exist"), reader-B MINOR ("the ζ readout IS announced")** | **MINOR — B wins; A's MAJOR preserved as dissent** | Banked calibration is exposure-graded: mirrored → superlative (kf-SequencePlayhead #3), thin-but-present → INFO (its N-14), domain-exposed-elsewhere → non-defect (kf-SequenceAxis D-12). Absent-entirely (here — my sr-only census: 3 genuine sites, none in the spring scene; no overshoot numeral in the solver branch) is the live case, one-to-two rungs up → MINOR. The corpus's MAJOR a11y rows (KF-ES-9) attach to unannounced state-change messages on interactive surfaces; this SVG is non-interactive and its driving readouts announce. The cure is one computed sentence — and **D-5's prescribed cure is KILLED separately (K-5)**: `Math.max(...linearPlot.map(p => p.v))` evaluates to NaN — `:85` maps to `{x, y}` and discards `v` (my read). Correct forms: over the pre-map `pts`, or invert `(Y_ZERO − y)/36`. |
| 6 | **D-2 grade — D filed BLOCKER, C filed MINOR; both readers MAJOR** | **MAJOR (banked class); the class-swap cure adopted over the copied override** | The uppercase-register class is banked at MAJOR twice (KF-ES-8, KF-ES-17 — both re-read this session; the vendor utility's `text-transform: uppercase` re-extracted verbatim from the installed 7.0.0). Cascade proven: exactly one `text-transform` in scene+styles (SpringTarget.vue:454, written for the same glyph). Cure ruling (N-4): the demo owns a documented case-preserving mono register — `.code-token` (design-idioms.css:244-248, "camelCase, case-preserving mono"; census-legal by name in font-roles.json; LIVE at SpringTarget.vue:128-129, 24 lines above the mount) — a class swap beats a third local `text-transform: none` escape hatch. **Trap restated (≡ banked kf-SequenceAxis D-7): `tabular-nums` at `:12` must survive any cure** — it is the `_monoContract` clause-(b) license for this call-site today. Reader-A's M-6 rider adopted: the sibling renders `ζ0.86` (no space, :121), this file `ζ 0.86` (:13) — one glyph, two cases, two spacings; normalize in one motion. Reader-A's co-visibility framing NARROWED (K-9): the sibling tag renders only during the derby egg (`v-if="demo.derbyActive.value"`, :105) and inside `aria-hidden` (:107) — and A's own citation (":112-114") is off by seven lines. |
| 7 | **D-8 — reader-A folds to banked KF-ET-36 at INFO; reader-B kept MINOR without opening the bank** | **FOLD → INFO (identity + grade-stability); B's cell corrected** | KF-ET-36 re-read at kf-EasingTarget.md:81: the same mechanism (`preserveAspectRatio="none"` anisotropy varying across the responsive range, "the fragility inside the P-7 praise"), adjudicated **INFO / NO-WAVE-OWNER**. One mechanism, two components; the banked grade governs (the kf-SequencePlayhead K-31 law, applied here). |
| 8 | **C-§4 kill-row 4 ("`.fn` is not a drop-in — different discretisation") — both readers kill the kill** | **THE KILL IS KILLED (K-4), concordant + this seat's third fact** | `.fn` is a callable over t∈[0,1] (timing-function.ts:91-100), pinned `fn(0)=0`/`fn(1)=1` (:92-93, :89), samplable at exactly the 26 stop abscissae; the stepper is closed-form (analytic `solveDampedHarmonic`, not an integrator), so both discretisations sample ONE trajectory — sample.ts:44 says so verbatim. This seat adds the decisive convenience neither axis cited and both readers did: **`springTimingFunction` RETURNS the string too** — `:115` `return { fn, css: springLinearStops(stopOpts) }` — `.css` is byte-identical to what the component already consumes, so the "26 stops plotted" label survives any `.fn`-based redraw. C-3 remains the correct structural answer; the `.fn` route is viable-with-caveat. Do not re-raise the kill. |
| 9 | **Reader-A M-4 (vertical clipping) — "every child is `shrink-0`"** | **ADMITTED MINOR AS N-5, mechanism CORRECTED (K-12)** | My read: Card `:12` `flex flex-col … justify-center gap-8 … overflow-hidden`; children `:33` shrink-0, **`:55` rail wrapper NO shrink-0**, `:134` shrink-0, SpringTrace root `:9` shrink-0. A's own enumeration lists the rail stage without the class, then claims "every child" — the column CAN compress, on that one member, down to its min-content. The terminal hazard survives intact: below the column's min-content height, `justify-center` + `overflow-hidden` (not `auto`) clip both ends symmetrically with no scroll affordance and no recovery. Threshold pushed further out than A claimed; bite point UNPROVEN → SS-13 #10 (the cheapest probe in this record). |
| 10 | **D-13 / L-14 / S-E arithmetic — analytic peak vs drawn peak** | **CORRECTED against the SAMPLED maximum (K-7, K-8), findings survive** | My dist run: drawn max at ζ=0.2 = **1.51685** (readers: 1.5169 — concordant, now artifact-grade). D-13's clearance: peak y = 56 − 36·1.51685 = 1.393 units = 1.672 px from the top; after the 1 px non-scaling half-stroke → **0.67 px, not 0.25 px** (2.7× D's figure; the 3 px blur still exceeds it — MINOR stands). L-14/S-E margin: 2.5 % in drawn value, not 1.9 % analytic; and L-14's citation is `:42` (`:min="0.2"`), not `:43` (`:max`) — my grep. `gentle` ζ=1.0 draws max exactly 1 → D-4's occlusion is by construction (my run). |
| 11 | **D-3 — the falsifier's `surface="glass"` premise (both readers kill the citation)** | **CITATION KILLED (K-3); the arithmetic CONFIRMED at ±0.02 both arms, dark arm now two-seat concordant** | SpringTarget.vue:10-13 read verbatim: the Card binds `:shadow="false"` + `class` ONLY; `surface`/`tier` live in the authoring COMMENT at :3-4. The composite backdrop stays UNPROVEN → SS-13 #1. My own conversion chain, run fresh: light 4.61 (trace, PASS) / 1.85 (target, FAIL) / 1.34 (baseline, FAIL); **dark — this seat's independent derivation matching reader-A's to ±0.01: accent rgb(190,149,236), trace 5.78 PASS / target 2.32 FAIL / baseline 1.41 FAIL**. The α=0.45 structural ceiling holds in both arms; "maybe it passes in dark" is closed as a falsifier. |

**Adjudicator's corrections register (against this corpus's readers):** (1) A's shrink-0 census — ruling 9/K-12; (2) A's derby line-cite drift (:112-114 → :105-108) — ruling 6/K-9; (3) A's S-9 endorsement — ruling 1/K-6, the adoption-without-re-derivation event; (4) B's consumer undercount + name-keyed-cache miss on the util — folded into N-2; (5) B's D-8 booking-miss against KF-ET-36 — ruling 7; (6) B's L-9 INFO admission — ruling 3 (the row dies, not demotes). Credits: A's dark-arm computation (independently reproduced by this seat, ±0.01); A's `.code-token` find (N-4) and dash-affordance find (N-3); B's `generateCurveSVGPath` find (N-2) and 2×-slope sharpening; both readers' independent N-1 derivations and their concordant kills of D-1/D-2's BLOCKER grades, the `.fn` over-kill, and the L-2 substrate event.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0 / KF.W4-PROSE / KF.W7 / KF.W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the spring-plot repair packet). Anchors bind to a file byte-identical across worktree/HEAD/origin-master; re-resolve before any cure.

### MAJOR

- **D-1 = L-1 = C-1** *(one identity, three axes + two readers + this seat's dist run — six concordant derivations)* — **the implicit-position fill clobbers the CSS `linear()` anchors: the trace draws over x ∈ [2, 98] against a full-bleed [0, 100] graticule; the `?? 0` / `?? 100` guards at :77-78 are dead code.** Proven on the SHIPPED emitter this session: n=26, first=2, last=98, guards never fire. The repo's own resolver does it right two directories away (easing-registry.ts:69-70 anchors BEFORE the :81-91 distribution), and the producer's docblock states the contract the parse destroys (linear-stops.ts:40 "passes through (0%, 0) and (100%, 1) exactly"). ≈15.4 px missing at each end at the 768 px measure. **Cells adopted**: reader-B's sharpening — the leading/trailing gaps are HALVED (2 units vs 4), so the onset and the settle-landing render at exactly 2× their true slope (my replay: 2,4,8…96,98) — and reader-B's second-divergence cell — the fill also lacks the resolver's monotonic clamp (easing-registry.ts:72-79, verified), latent under today's monotone emitter; the divergence from the canonical resolver is two rules wide. BLOCKER grade killed (K-11; banked bar — nothing crashes, no artifact exports wrong); D's dissent preserved. Cure fork: two-line anchor hoist, or the N-2/C-2 route. **MAJOR. NO-WAVE-OWNER (instrument-truth core, one commit with N-1/N-2/C-2).**
- **N-1** *(NET-NEW — reader-A M-1 ≡ reader-B M-1; ruling 4)* — **the `response` prop is visually inert: dragging the first slider a user reaches (SpringPhysicsFacet.vue:29-33, response ∈ [0.1, 1.2]) redraws a pixel-identical plot**, while the component pays a full solver construct/24-step/dispose + string build + 26-token regex parse + fill + path rebuild + `drop-shadow`-filtered repaint per pointer frame to do it. ω·tᵢ = 8πi/25 — response cancels exactly (bytes verified; dist-executed: byte-identical at ζ=0.2, ≤0.016 px elsewhere, the residue entirely the absolute settle-pin). The x-axis choice (percent-of-`maxDuration`, linear-stops.ts:64) is the mechanism: it normalizes away the one quantity `response` controls. Half the declared prop surface (:37) is a no-op on the output, unlabeled (D-6 compounds — nothing tells the viewer x is normalized), in an instrument whose thesis is "the math made beautifully visible". Cures are cheap and mutually exclusive, a spec decision: drop the prop (a ζ-instrument), or label x in milliseconds (`pct/100 × response × 4`) — the second also discharges D-6's x-axis charge. C's S-C derived the cancellation and spent it on the viewBox; C-10 sharpens from "cheap work" to "provably wasted work on one of two sliders". **MAJOR. NO-WAVE-OWNER (packet core).**
- **D-2 = C-5** *(+ M-6 spacing rider; ruling 6)* — **`&zeta;` inside `text-mono-caption` renders Ζ (U+0396): the installed utility sets `text-transform: uppercase`** (extracted verbatim); the only counter-declaration in the scene is the sibling's own cure for the same glyph (SpringTarget.vue:454), not carried here; the driving control says "damping (ζ)" (SpringPhysicsFacet.vue:39). Banked class precedent grades it MAJOR (KF-ES-8, KF-ES-17); D's BLOCKER and C's MINOR both die. Cure = the N-4 class swap, `tabular-nums` preserved (the license trap). Rendered glyph → SS-13 #2. **MAJOR. NO-WAVE-OWNER (type/register packet, with N-4/D-10/D-9).**
- **D-3** *(ruling 11)* — **both reference lines fail SC 1.4.11 in BOTH theme arms** (light 1.85/1.34; dark 2.32/1.41 — two concordant independent derivations, reader-A + this seat), while the trace itself passes both (4.61/5.78); and at α=0.45 over either card no chromatic mid-tone can reach 3:1 — a token choice foreclosing conformance, not a tuning miss. Load-bearing status is the component's own claim (:22, :3-5). The falsifier's `surface="glass"` basis is dead (K-3); exact composite → SS-13 #1. **MAJOR. NO-WAVE-OWNER (mark-design packet, one motion with D-4).**
- **D-4** — **the reference geometry wears the data hue** (target line = `--color-progress` @45 %, trace = same hue @100 %, baseline = neutral `--border`): hue does double duty, and at ζ ≥ 1 the settled trace is collinear with and occludes the reference — proven by construction this session (drawn max exactly 1 at `gentle`). The sibling precedent is STRONGER than D claimed: EasingTarget's sparklines are neutral-by-default (`--foreground` 22 %, EasingTarget.css:127-131) with accent reserved for the selected state (:173-175) — verified. Also causal for D-3 (the 45 % dilution exists to keep the accent-on-accent legible). **MAJOR. NO-WAVE-OWNER.**
- **D-6** — **the graticule is unlabeled**: the SVG contains three elements and zero `<text>`; nothing rendered says 1, 0, or names the x-axis; the stated thesis ("the overshoot is y > 1") is unverifiable by the viewer; 20 viewBox units of headroom sit empty for the labels. Load-bearing for N-1's second cure arm. **MAJOR. NO-WAVE-OWNER.**
- **D-7** — **33 % of the plot height is reserved for overshoot headroom the shipped states barely exercise**: boot default ζ=0.86 fills 0.9 % of the band (0.22 px); `gentle` fills 0 px; the signal region (rise, knee, settle) is compressed into 43.2 px. Both readers re-derived every figure; drawn-vs-analytic distinction honored (drawn peaks sit under the analytic bounds). Allocation judgement, honestly labeled; legibility of 0.22 px → SS-13 #3. **MAJOR. NO-WAVE-OWNER.**
- **C-2 = L-3** *(+ N-2 completes it; ruling 8)* — **serialize→regex-reparse round trip: the component holds the numbers, emits them to a string, and spends 37 lines regexing them back** — the lane-library §4.3 Tier-C shape, one layer out, invisible to §4.6's value.js-scoped roster (C's census correction adopted). The counterweight stands as C wrote it (the tokenizer half is replaceable today via `parseTimingFunction`, a live demo import; the resolve half needs C-3) — but C's kill of the `.fn` route is itself killed (K-4): `.fn` sampled at the 26 abscissae reproduces the stops, and `springTimingFunction` returns `{fn, css}` with `.css` byte-identical to today's input. **MAJOR. NO-WAVE-OWNER (one commit with D-1/N-1/N-2; the library-side ask is C-3 → KF.W7).**

### MINOR

- **D-5 = C-9 = L-12** *(ruling 5 — MINOR; reader-A's MAJOR preserved as dissent)* — `aria-hidden="true"` on the sole informational artifact, no `role="img"`/`<title>`/`<desc>`/sr-only mirror, and nothing in the solver branch carries the curve's quantity (my census: 3 genuine sr-only sites demo-wide, none here; D's "five places" narrowed — two were `sr-only-slider`, a different class, K-10). The banked exposure-graded calibration puts absent-equivalent on a non-interactive supplementary plot at MINOR. **The prescribed cure is killed (K-5** — NaN against the component's own shape); the correct one-sentence cure computes over pre-map `pts`. SR pass → SS-13 #8. **NO-WAVE-OWNER; a11y datum also rides KF.W9.**
- **D-9** *(+ reader-A's extension, verified)* — `mb-1` here vs `mb-2` on the structurally identical sibling row (SpringTarget.vue:135) — and it is THREE gaps, not two: StartingStyleTarget.vue:57 runs the same grammar at `mb-1.5` (my grep). One label-row idiom, three spacings, zero shared primitive. **NO-WAVE-OWNER.**
- **D-10** *(reader-B confirmed; reader-A filed no cell — no consequence)* — the live ζ readout wears `text-muted-foreground` while the static label wears `text-foreground`, inverting the scene's own documented rule (SpringTarget.vue:137-139, read verbatim: "the live sampled value wears the scene accent; the label stays muted"). D's own falsifier concession (parameter-vs-readout) is what holds it at MINOR. **NO-WAVE-OWNER (type/register packet).**
- **D-11** — `{{ linearPlot.length }} stops plotted` is invariantly 26 in every reachable state (chain verified end-to-end; my dist run n=26 always) — a constant dressed as a live value in the primary label slot. **NO-WAVE-OWNER.**
- **D-12 = L-6 = C-4** *(+ reader-B's `filter(Boolean)` cell, verified at :53)* — silent-swallow error posture: an unparseable stop becomes `{v: 0}` — indistinguishable from a legitimate baseline datum — while the header still counts it as plotted; and one line earlier, `filter(Boolean)` silently RENUMBERS the axis (the count is the denominator for every implicit position, so a dropped token rescales the whole curve, self-consistently mis-reported). Latent, not live (the emitter is total over the regex today); violates the engine's own fail-explicit law (easing.ts:13-16 — C's framing, the sharpest). **NO-WAVE-OWNER (one fail-explicit cure for both mechanisms).**
- **D-13** *(arithmetic corrected — K-7)* — `overflow: visible` + 3 px `drop-shadow` at the reachable ζ=0.2 floor: the DRAWN peak sits 1.67 px from the top edge, **0.67 px** of clearance after the non-scaling half-stroke (not D's 0.25 px) — the 3 px blur exceeds it either way and paints into the 4 px `mb-1` gap. Spill magnitude inherits L-8's filter-space question → SS-13 #6. **NO-WAVE-OWNER.**
- **L-4 = C-8** *(L's MAJOR demoted by both readers, concordant)* — the header's reason-for-existing ("the curve drawn BESIDE its string", claimed three times) is architecturally impossible: SpringScene.vue:10-11 forks the trace (solver branch) and the string (StartingStyleTarget.vue:61) into mutually exclusive `v-if`/`v-else`; the named sidebar does not exist (find → 0). Instance of the banked phantom-consumer prose class (kf-KfPillTabs.md:43 names SpringSidebar verbatim). **MINOR → KF.W4-PROSE.**
- **L-5** — plot geometry has two sources of truth: `Y_TARGET`/`Y_ZERO` as script constants (:82-83) AND as literal SVG attributes (:23/:25), nothing binding them; the file's own comments name them as the same quantities, closing the independent-graticule defence. **NO-WAVE-OWNER.**
- **L-7 = C-6** — the style header claims "the scene's red identity"; `--color-progress` → `--accent-kf` = oklch hue 295/305 (violet), and the retiring ruling ("red exits the chrome entirely") sits in the same token block (style.css:130/:163, verified). **MINOR → KF.W4-PROSE.**
- **L-8** — the glow and the stroke reason about scale in opposite directions: `:126` pins stroking to device space while `:127`'s `drop-shadow(0 0 3px …)` blur length is spec-ambiguous between local user space (≈6:1 anisotropic glow) and device px. The adjacent-declarations-disagree fact is source-true (my read); the rendered magnitude is the whole severity → SS-13 #5. **MINOR-as-filed, contingent. NO-WAVE-OWNER.**
- **C-3** — the library exports the emitter but neither `resolveLinearStops` nor `sampleNormalizedSpring` (my dist grep → 0); every plotting consumer must re-derive the CSS rule by hand. The sharpest reusable row in the corpus: exporting either makes D-1 structurally unwritable downstream, and the same export cures reader-B's monotonic-clamp divergence. **MINOR → KF.W7 (export-surface wave; kf-KeyframeTimeline C-15 precedent).**
- **N-2** *(NET-NEW — reader-B M-2; ruling 1; admitted MINOR, B's MAJOR preserved as dissent)* — **the demo already owns an exported, cached, four-consumer curve-path builder taking exactly the callable `springTimingFunction().fn` returns** — `generateCurveSVGPath` (timingCurveUtils.ts:48) — and SpringTrace hand-rolls another copy behind a hand-rolled parser (L-10's "third tokenizer" becomes the FOURTH copy of the curve-plot idiom). Unbundled by this seat: the census-kill lives at K-6; the cure enrichment lives on C-2/L-3; what remains against the component — a duplicated ~7-line builder + no memo — is MINOR. Corrections against B: consumers are 2 direct + 2 via `getCurvePath`; the memo is name-keyed and could not serve a parameterized spring anyway. Honest caveats kept: the util hardcodes a 0..1 y-range (`1 - v`, :53) — an overshoot plot needs a range parameter (five lines against 45); and **the cure spends a superlative cell** — routing through timingCurveUtils imports runtime `@mkbabb/value.js/easing` symbols (:1-7, my read) into a closure C-S-A certified value.js-free; R1 (`parseCssColor`, the `/css` subpath) stays unreachable, but the "zero value.js imports" cell dies. Per the ColorSpaceSelector addendum law: the adopting spec states the property the cure could break and gates it. **NO-WAVE-OWNER (packet, with D-1/C-2).**
- **N-3** *(NET-NEW — reader-A M-2)* — **`vector-effect: non-scaling-stroke` also pins `stroke-dasharray` to device space** (stroking, dash pattern included, is performed in the non-scaled space): `stroke-dasharray: 3 2` on the same rule (:111-112) becomes a 5-CSS-px period repeating ≈154× across the 768 px measure on a 1 px 45 %-alpha hairline — the target line's three distinguishing channels (hue = D-4, luminance = D-3, now pattern) are all spent at once. A distinct SC 1.4.1 use-of-color exposure beside D-3's 1.4.11; it also inverts D-14's forced-colors premise (the dash discriminator is already near-spent in normal mode). Registry novelty verified (zero dash rows). Percept (dashed vs continuous-faint) → SS-13 #4. **MINOR. NO-WAVE-OWNER (with D-3/D-4).**
- **N-4** *(NET-NEW — reader-A M-3, verified whole)* — **the demo owns a NAMED case-preserving mono register two files from the mount and unused here**: `.code-token` (design-idioms.css:244-248 — same face, same `--type-caption` size, no uppercase, no caps-tracking; census-legal by name in font-roles.json; live at SpringTarget.vue:128-129). All three axes prescribed the copied `text-transform: none` override because none found it; the right cure for D-2 is a class swap. Rider: `linear()` at :11 is "an in-prose API name" rendering in the prose face — a scene-wide idiom-adoption note (SpringTarget.vue:136 equally bare), not a component indictment. **MINOR. NO-WAVE-OWNER (the D-2 cure home).**
- **N-5** *(NET-NEW — reader-A M-4, mechanism corrected; ruling 9)* — **the plot is the last block of a `justify-center overflow-hidden` flex column that clips symmetrically with NO scroll affordance below its min-content height** — corrected census: three of the four sibling blocks are `shrink-0` (:33, :134, SpringTrace :9); the `:55` rail wrapper is not, so the column first compresses that member to min-content, THEN clips both ends unreachably. SpringTrace is the marginal ~124 px (72 px plot + `gap-8` + header row) that moves the bite point. Threshold and whether any shipped breakpoint reaches it → SS-13 #10. **MINOR. NO-WAVE-OWNER.**

### INFO

- **D-8** *(ruling 7)* — `preserveAspectRatio="none"` + fixed 72 px height + fluid width: anisotropy 2.73:1 → 6.40:1 across the responsive range; the drawn shape is viewport-dependent. **FOLD by identity → banked KF-ET-36 (INFO / NO-WAVE-OWNER); severity aligned to the bank.** Percept → SS-13 #9.
- **D-14** — zero `forced-colors` demo-wide (my grep, 0 hits) — correctly declined as a component charge; **FOLD → banked KF-CE-13 → KF.W9.** The component-specific hazard note survives with N-3's correction: in forced colors the dash would be the only trace/target discriminator, and it is already near-spent in normal mode.
- **D-15** — dual-owner token consumption (`--color-progress` demo-owned, `--border` vendor-owned) with no call-site qualification; corroborates lane-frontend §6.3; the banked measurement (kf-AnimationVisualizer KF-AV-42: collision set EMPTY today) keeps it a dated hazard note. **KF.W0 fold-adjacent; no re-book.**
- **D-16** — empty/loading/RTL clean by construction (the `:90` guard dead-but-harmless — verified reachability logic); negative-space bookkeeping so a later sweep manufactures no work. **No route.**
- **L-10** *(sharpened by N-2)* — 37 lines of pure, testable string arithmetic sealed in an SFC computed, zero coverage (my earlier greps concur with both readers); now the FOURTH copy of the curve-plot idiom. Extraction makes D-1 a one-line unit test. **NO-WAVE-OWNER (packet test obligation).**
- **L-11** — the two-position `<linear-stop>` form is unparseable; latent, not live (the emitter never emits it); the axis's own scoping honored. **NO-WAVE-OWNER (dies with the packet's parser cure).**
- **L-13 = C-7** — the consumed composable's docblock (useSpringLinearStops.ts:9/:12, verified) cites the deleted `SpringSidebar.vue` and a false present-tense census; the "ONE surface" invariant itself HOLDS (roster verified: 2 consumers). Instance of the banked KF.W4-PROSE class — **INFO, fold-by-class (kf-KfPillTabs rider precedent); reader-A's MINOR noted.**
- **L-14** *(corrected — K-8)* — undeclared geometry contract: nothing binds `Y_TARGET`/`Y_ZERO` (ceiling 1.5556) to the ζ = 0.2 slider floor one file away (`SpringPhysicsFacet.vue:42`, duplicated at SpringHeatmap.vue:72); true margin 2.5 % in drawn value (my dist run). One `:min` edit from painting outside the box, enforced by nothing. **NO-WAVE-OWNER (one pinned comment or a clamp, with S-E's coupling documented).**
- **C-10** *(sharpened by N-1)* — re-enters the Vue render graph on the exact axis the parent moved off it (SpringTarget.vue:72-76's stated law), at pointer rate — and half that axis is now PROVABLY wasted work. Frame-cost magnitude deliberately unclaimed → SS-13. **NO-WAVE-OWNER (with N-1).**

## Killed-claims register — 12 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **L-2 (MAJOR)** — "phantom-dep bite … `npm ci` reconstructs from a lockfile with ZERO glass-ui entries" | **KILLED — baseline law, sustained by this seat's fresh probe** (origin/master:77 ships `7.0.0` exact + 3 lockfile entries, matching the installed package; the worktree diff DELETES the line). Fourth re-booking of a banked, thrice-overruled identity (kf-AmigaScene.md:19 et al.); census F-1 must re-measure against origin/master. Residue: worktree-manifest-dirty, INFO-weight note only. L-9 collapses with it |
| **K-2** | **L-9 (MINOR)** — "no `var()` fallbacks, against the demo's own sibling idiom" | **KILLED AS FILED (ruling 3)** — the cited idiom is an override SLOT (three scene-root declarations, per-instance override; `.derby-lane-tag:452` uses the same form as parameterization); the defensive form exists at exactly ONE demo site (my census); the stakes died with K-1. Reader-B's INFO admission preserved as dissent, overruled |
| **K-3** | **D-3 falsifier (a)** — "the Card is `surface=\"glass\"` (SpringTarget.vue:10-13)" | **KILLED — false citation** (both readers concordant; my read): :10-13 binds `:shadow="false"` + `class` only; `surface`/`tier` are the COMMENT at :3-4. The composite stays UNPROVEN; the FAIL verdicts are robust to it (both arms, two seats) |
| **K-4** | **C-§4 kill-row 4** — "`springTimingFunction().fn` is not a drop-in … plotting `.fn` would not be 'the 26 stops plotted'" | **THE KILL IS KILLED (ruling 8)** — `.fn` is a pinned callable samplable at the 26 abscissae over ONE closed-form trajectory; and `springTimingFunction` returns `{fn, css}` (:115) with `.css` byte-identical to today's input, so even the label objection dies. Do not re-raise |
| **K-5** | **D-5's prescribed cure** — "`Math.max(...linearPlot.map(p => p.v))` is one line from data it already computes" | **CURE KILLED (≡ reader-B M-6)** — `:85` maps to `{x, y}` and DROPS `v`; the expression is `Math.max(...[undefined × 26])` = NaN, shipped into the accessible name. Correct: compute over pre-map `pts`, or invert `(Y_ZERO − y)/36`. The FINDING stands (MINOR, ruling 5) |
| **K-6** | **D-§S-9 = C-S-B census row** — "S-9 · SpringTrace — JUSTIFIED BESPOKE, no counterpart exists" | **KILLED AS STATED (ruling 1)** — `generateCurveSVGPath` (timingCurveUtils.ts:48; 4 consumer files; the same SVG idiom at EasingTarget.vue:97-103) was one `ls` away from a directory both axes cited. The glass-ui half of the enumeration stands; the row must be re-derived (0..1-range caveat included) before any census landing. Reader-A's endorsement = adoption-without-re-derivation, recorded |
| **K-7** | **D-13's clearance arithmetic** — "1.25 px from the top edge … 0.25 px of clearance" | **WRONG NUMBER** — analytic peak substituted for the SAMPLED polyline that actually paints; drawn max 1.51685 (my dist run) → 1.67 px / **0.67 px**. Finding survives; SS-13 measures against the corrected figure |
| **K-8** | **L-14/S-E cells** — ":min at SpringPhysicsFacet.vue:43"; "1.9 % margin" | **CORRECTED** — `:min="0.2"` is :42 (:43 is `:max`); margin is 2.5 % against the drawn maximum. Immaterial to either finding; citation hygiene |
| **K-9** | **D-2's co-visibility framing** — "two components, one scene, one symbol, one fix applied to one of them … standing side by side" | **NARROWED** — the sibling renders only during the derby egg (`v-if`, :105) inside `aria-hidden` (:107); the precedent-and-cure claim survives, the standing-co-visibility rhetoric does not. Reader-A's own cite (":112-114") drifted seven lines |
| **K-10** | **D-5's sr-only census** — "the sr-only mirror this demo already uses in five places … SquareScene.vue:56,66" | **NARROWED** — those two are `sr-only-slider`, a visually-hidden REAL control; the three genuine sites (AnimatedText, CopyButton, ControlsPaneWrapper — my grep) fully carry the argument |
| **K-11** | **The two BLOCKER grades** (D-1, D-2) | **SEVERITY KILLS** — both readers concordant, banked calibration (the corpus BLOCKER bar; the KF-ES uppercase class at MAJOR). D's dissents preserved on both rows |
| **K-12** | **Reader-A M-4's mechanism cell** — "every child is `shrink-0`: the column cannot compress" | **CELL KILLED, finding admitted (N-5)** — the `:55` rail wrapper carries no `shrink-0` (my read); A's own enumeration listed it classless and the summary sentence overreached. The no-scroll symmetric-clip terminal state survives at min-content |

L's own §4 killed-hypotheses (K-1 `--color-progress` demo-owned; K-2 `text-mono-caption` is a real `@utility`) and C's §4 rows 1-3/5-6: **sustained** — re-confirmed incidentally by this seat's token and utility reads; row 4 is K-4 above.

## Superlatives (provenance preserved)

1. **S-A (D) — the polyline is the CORRECT mark, and the label names it.** CSS `linear()` IS a polyline; a spline would depict a spring the engine never runs; the drawn apex understating the analytic peak is the truth about the approximation (my dist run: drawn 1.51685 vs analytic 1.5266 at ζ=0.2). Provenance deepened by reader-A: the producer's docblock (linear-stops.ts:40) is the same sentence D-1's parse destroys — the superlative and the headline defect share one line of contract.
2. **S-B (D) / S-1 (L) / S-C-part (C) — `non-scaling-stroke` on all three marks, none forgotten** — the classic partial-application failure did not happen; round caps/joins compound correctly. **QUALIFIED by N-3**: the same property spends the target line's dash affordance in device space. A real superlative with a bill.
3. **S-C (D) — no motion, no PRM block, and that is the honest answer** — zero `@keyframes`/`transition`/`animation` in the scoped block; both props input-driven only. Census refinement kept: SpringTrace is "no guard needed", not a gap — a symmetry sweep must not add a vacuous `@media` (the three siblings all carry real PRM blocks).
4. **S-D (D) — total typography-register conformance** — zero raw px, zero local font declarations; both header spans mirror the sibling's exact register pairing. Stands DESPITE D-2 (the hazard is the chosen utility's transform, not a scale violation).
5. **S-E (D) / S-C (C) — the headroom exactly brackets the reachable maximum** (ceiling 1.5556 vs drawn max 1.51685 — 2.5 % margin, corrected), with `overflow: visible` as deliberate belt-and-braces; the undocumented `:min` coupling is L-14's row.
6. **S-3 (L) — zero teardown surface, correct by construction** — two computeds + one composable, nothing to dispose; the hot painter path stayed in the parent that needs it. The extraction is also what makes D-1 cheap to fix.
7. **S-4 (L) — `useSpringLinearStops` is a textbook Vue 3.5 composable contract** — `MaybeRefOrGetter` + `toValue`, explicit `ComputedRef<string>` annotation, and BOTH call sites pass getters (the freeze-at-mount trap avoided twice). Its docblock is stale (L-13); its code is exemplary; the facts are independent.
8. **S-A (C) — the light/heavy boundary is honoured and the R1 crash class is provably unreachable** — the closure is value.js-free by construction (sample.ts:16), proven by enumeration. **Cure note (this seat's)**: the N-2 route imports runtime `@mkbabb/value.js/easing` and spends the "zero value.js imports" cell — R1 (`/css`) stays unreachable either way; the adopting spec must state this trade.
9. **S-B (C) / §1 (D) — RESCOPED (K-6).** The glass-ui enumerations were real and correct (68 dirs / 73 subpaths, no plot primitive; `/axes` and `/easing` seriously disposed); the "no counterpart exists" conclusion was scoped to the wrong tree. The bespoke verdict may yet survive on the 0..1-range caveat — as a re-derivation, not an inheritance.
10. **S-5 (L) — goldilocks size, exact concern seam, correct colocation** — 129 lines, one concern, beside its parent/composable/keys; resisted absorption and over-fragmentation both.
11. **ENGINE (reader-B, uncredited by all three axes)** — `sample.ts` + the closed-form stepper: "the two serializers describe ONE curve" is made literally true by an analytic evaluator with O(machine-epsilon) per-step error — the fact that makes K-4 and N-1 provable at all; and `resolveLinearStops` (easing-registry.ts:57-94) is the CSS rule done correctly in the right phase order, 38 dependency-free lines the demo needed and could not import (C-3).
12. **METHOD** — all three axes published falsifiers and two published killed-hypothesis tables that survived hostile re-reading (5 of C's 6 rows held at the bytes); both readers independently re-derived the D-1 arithmetic to the same digits this seat then reproduced on the shipped artifact — six concordant derivations, one answer, zero inherited cells on the headline row.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The exact composite backdrop** under the Card (vendor-default surface; K-3's corrected basis). Sensitivity bounded (~2 % luminance between candidates); the four FAIL verdicts (1.85/1.34 light, 2.32/1.41 dark) are robust; the third decimal is not.
2. **The rendered ζ glyph** — that Ζ actually paints and reads as Latin Z in Fira Code. One glance; bundle with the banked uppercase-register class (KF-ES-8/KF-ES-17 horizon).
3. **Whether 0.22 px of overshoot is legible** at the boot default against a 1 px dashed 1.85:1 line (D-7's perceptual half; the arithmetic carries the severity regardless).
4. **The dash period percept** — 3-on/2-off ≈154 cycles at 768 px: dashed reference or continuous faint line? One screenshot settles N-3 AND D-14's forced-colors premise.
5. **The `drop-shadow` coordinate space** (L-8) — anisotropic ≈6:1 in user space vs radially symmetric in device px; UA-dependent.
6. **The glow spill** into the 4 px label gap at ζ=0.2 — measure against **0.67 px** clearance (K-7's corrected figure), never 0.25.
7. **Forced-colors rendering** of the three marks — folds to banked KF-CE-13's horizon.
8. **The SR pass** (D-5) — whether anything co-rendered supplies a spoken equivalent; the one input that could move ruling 5's grade.
9. **Shape-percept across the anisotropy range** (D-8 ≡ KF-ET-36's optional glance): same ζ at 2.73:1 vs 6.40:1.
10. **N-5's bite point** — one short-viewport capture of the `justify-center overflow-hidden` column; the cheapest probe in this record, and it decides N-5's MINOR-vs-MAJOR.

## Identity guards (fold-by-reference — never re-booked)

- **L-2 ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1** → KF.W0 §B-12; this corpus adds the FOURTH re-booking event and the SIXTH one-baseline-short measurement to the pattern ledger. F-1 re-measures against origin/master before routing anything.
- **D-8 ≡ banked KF-ET-36** (one anisotropy mechanism, two components) — INFO, folded, severity aligned.
- **D-14 ≡ banked KF-CE-13** (demo-wide forced-colors) → KF.W9; the component hazard note rides as a cell, corrected by N-3.
- **D-2 is an INSTANCE of the banked uppercase-register class** (KF-ES-8 / KF-ES-17 / kf-SequenceAxis D-7) — graded with the class (MAJOR), cured by N-4's swap, `tabular-nums` trap restated.
- **L-4/C-8 and L-13/C-7 are INSTANCES of the banked KF.W4-PROSE phantom-consumer class** (kf-KfPillTabs.md:43 names SpringSidebar verbatim) — booked citing it, at class weight.
- **D-15's dual-owner hazard ≡ the banked KF-AV-42 measurement** (collision set empty) — dated note, no re-book.
- **Reader-2's M-3/M-4/M-5 are CELLS of D-1 and D-12, not rows** (their own filings say so); reader-1's M-5/M-6 are cells of D-3/D-2 — the anti-double-booking law applied at intake, five times.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the L-2/F-1 substrate fold (§B-12; re-measure obligation). — **KF.W4-PROSE**: L-4/C-8 (the header premise — retire or re-home the string), L-7/C-6 (the red-identity comment), L-13/C-7 (the composable docblock), D-1's `:48-49` comment truth (rides the fix). — **KF.W7**: C-3 (export `resolveLinearStops` or a `springLinearStopsPoints()`; covers the monotonic-clamp divergence too; kf-KeyframeTimeline C-15 precedent). — **KF.W9**: the D-5 a11y datum · the D-14/KF-CE-13 fold · the SS-13 list.
- **NO-WAVE-OWNER — the spring-plot repair packet (SS-1/SS-2 authoring must home it):** the **instrument-truth core D-1 + N-1 + N-2 + C-2/L-3** as ONE commit family — the spec decides the N-1 fork first (drop the prop, or label x in ms and discharge D-6), then the draw route (anchor hoist minimum; the shared-builder route with a range parameter preferred, its value.js/easing trade stated), with the parser posture (D-12/L-6/C-4 + the filter-cell), L-5's bindings, D-11's label, and L-10's extraction/test obligation landing inside it · the **mark-design rows D-3 + D-4 + N-3** (neutral reference vocabulary per the EasingTarget precedent; one motion) + D-6's labels + D-7's allocation + D-13's containment · the **type/register rows D-2 + N-4 (+ the M-6 spacing rider) + D-10 + D-9** (one register pass over the header) · **N-5** (the column's overflow posture) · **L-8** (pending SS-13 #5) · **L-14** (document or clamp the coupling) · D-16 recorded as clean-by-construction so no sweep manufactures work.

## Verdict

**ADJUDICATED.** Three axes wrote a corpus good enough to be wrong in interesting places, two hostile readers caught most of it, and the incredulity seat caught the rest of them: one census contribution killed before it could land (the counterpart was one grep away, in a directory both axes had cited), one MAJOR killed twice over on a substrate the registry had already ruled three times, one reader's mechanism cell false at the bytes inside an otherwise-admitted finding, two BLOCKERs re-graded to the banked bar, and two arithmetic cells corrected against the sampled curve that actually paints. What survives is a clean shape: a 129-line leaf that is genuinely well-made — correct mark, correct stroke discipline, correct lifecycle, correct composable contract — wrapped around one two-line fill bug six seats have now derived identically, one prop that provably does nothing (proven this session on the shipped artifact itself), reference geometry that fails contrast in both themes while wearing the data's own hue, and a header whose only Greek letter renders as the wrong alphabet. The repair is a packet, not a rewrite — one domain decision, one draw route, one register pass — and every row of it is agglomeration input for the forming X·KF specs. Nothing here stamps a wave. Original ids — D/L/C for life, N-1..N-5 for life.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/spring/SpringTrace.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SpringTrace.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**`D-15` — NO RE-BOOK, and the token is RECORD-QUALIFIED** (C-1.R row 5, repair round 3 E-6). Banked at `:72`: *"dual-owner token consumption (`--color-progress` demo-owned, `--border` vendor-owned) with no call-site qualification … **KF.W0 fold-adjacent; no re-book**."* Booked here as a ONE-LINE GUARD and nothing else: the collision set is measured EMPTY at `kf-AnimationVisualizer` KF-AV-42, so it stays a dated hazard note. **A seat greping `D-15` finds TWO record-qualified rows and re-books neither** — this one, and kf-EditorHeader's `D-15`/`D-17`, which ride the header-ribbon-migration decision at §Excluded (NO-WAVE-OWNER, SS-1/SS-2's).

**F-1's re-measure obligation is DISCHARGED**: *"F-1 must be re-measured against origin/master before routing any wave."* It was, dated, at C-1 and again at this wave's execution; the ref of record IS `origin/master`.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — **N-5**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here. **Nothing above this line is
rewritten** — no edit, no re-grade, no renumber, no deletion, no new id (E-1 immutable bytes / E-3
addenda-not-patch). This addendum records a MEASUREMENT STATE, not a grade: **the banked severity is
unchanged and stays exactly where the record put it.***

- **armed trigger** — register row **12** of thirteen, `evidence/W9/ESCALATION-REGISTER.json` (id as banked there: `N-5`); ceiling if it fires: **MAJOR**; roster cell(s): `—`.
- **measurement named by the trigger** — the **BITE POINT** — the viewport height at which clipping begins (residue #10, the cheapest probe in that record).
- **measured value** — **NONE. The trigger did not run.**
- **resulting severity** — **PROVISIONAL (unrun)**. The banked grade stands; it is neither inherited as confirmed nor moved.
- **exact precondition** — a viewport-height sweep against the served bundle. **A reading that only confirms "it clips eventually" does not separate MINOR from MAJOR** — the bite point itself is the measurement. the `chromium` and `chromium/emulated-forced-colors` cells DID open at `.d` (8 rows, 2 + 2 captures), so this trigger is **unrun, not foreclosed** there: it needs a pass that mounts the subject. the `safari-app/desktop` cell was open and productive at `.d` (4 sessions, OP-4 taken, 3 shots) and **closed mid-seat**: session creation began returning *"You must enable 'Allow remote automation'…"*; `safaridriver --enable` → **`Password:Password is not valid`** (an interactive admin authorization no seat here can supply); the running Safari is the **owner's** (pid 23725, started 16:23:59, 6 windows) and was not quit. **Precondition: 'Allow Remote Automation' re-enabled by a hand that can answer an admin prompt, or the owner's Safari restarted.**
- **substrate of this wave** — keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD; frontier moved to `3e81f500`, the pin is its ancestor, **no re-pin on a seat's authority**). Served bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448`, built in the §0m.2 capture clone and **re-hashed at this seat, double-run identical**. Capture: **none for this row**; shot-sha256: **none**.
- **G-KFW9-13 reading** — this row closes the gate's *"unrun triggers reported PROVISIONAL at close"* arm, **not** its *"measured value written back"* arm. The gate stays **RED**.
