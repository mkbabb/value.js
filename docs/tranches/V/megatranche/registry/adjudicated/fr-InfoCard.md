claude-fable-5 (served model id)

# fr-InfoCard — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh Fable adjudicator · model receipt `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-25 · **Subject repo**: fourier-analysis HEAD `cd26c65` (re-verified this seat: `git log -1` = 2026-07-03, the value.js-2.0.0 peer-floor coordination letter; WT carries `:value=` at InfoCard.vue:32 per the r1-recorded dirty hunk) · **Subject**: `web/src/components/equation/InfoCard.vue` (43 lines, read WHOLE this seat — every anchor re-resolved).
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full challenge corpus — **reader-A** (design-and-usage: 9 confirmation cells · 6 kills · 3 misses · 4 superlatives · 4 unproven) and **reader-B** (library-and-code: 11 confirmation cells · 8 kills · 4 misses · 8 superlatives · 6 unproven). The three challenge axes (`challenge-D-design.md` · `challenge-L-library.md` · `challenge-C-consumption.md`, 2026-08-06) were spot-read by this seat at every disputed anchor (D:127-169, D:188-199, D:285-295, D:324, D:518-526, C:74-80, L:283-286).
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-24 r1 apotheosis at this same path, which adjudicated a *different* reader corpus (reader-DU / reader-LC). The r1 roster identities **FR-IC-1..22**, its kill register **K-1..K-15**, and its 8 superlatives are preserved for life (anti-rename); r1 rulings are carried where this corpus does not touch them and are **corrected in exactly three places**: (1) r1's K-6 mobile arithmetic is OVERTURNED at the bytes (ruling 1 — the axis's original figure is restored); (2) r1's FR-IC-16 D-14 prose ("the twin's HoverCard announces on open") is FALSE and inverts (ruling 3); (3) r1's SS-13 probe items 1 and 3 carried a wrong selector chain and a wrong predicted pair — both re-issued (rulings 1, 4). Where r1 content is restated it was re-verified, not copied, except where marked *(r1-carried)*.
**Producer pins**: installed glass-ui **4.0.0** (package.json read this seat); the 4→8 re-key law (FMD-11, fr-EditorControlsDock M-10) stands from r1 untouched.
**Status verbs**: every disposition is **ADJUDICATED**, not VERIFIED — the X·F specs are still forming; routings are agglomeration input (M-25). Line anchors are the dirty worktree at `cd26c65`; re-resolve before executing any cure (the D-19 lesson).

**This seat's own instruments (static + git/grep/node arithmetic only; zero browser probes — probe parsimony; product source READ-ONLY; this file the sole write)**: InfoCard.vue WHOLE (43) · notation.ts WHOLE (48: colours **:26/:32/:38** ≡ energyColor **:45/:46/:47**; thresholds 0.99/0.95 at :45-46) · EquationView.vue:8-16 + :270-304 (`Info` import **:11**; the bare hand-rolled `<svg>` trigger **:276-280** — `circle cx12 cy12 r10 + M12 16v-4 + M12 8h.01`, element-identical to `node_modules/lucide-vue-next/dist/esm/icons/info.js:10-14`, read side-by-side this seat; pill `border-[1.5px]` :285; `unit="% energy"` :294; lucide `<Info>` rendered :300) · installed glass dist reads: utilities/components.css **:7-40** (`max-width` 8rem :15 · `min-height` 1.5rem :16 · padding .125/.5rem · `justify-content:center` :9 + `text-align:center` :10 · `overflow:hidden` · root `line-height:1`) + **:41-48** (`.metric-badge > span { line-height:1; overflow-wrap:anywhere }` under the "Scoped to the value span" comment) + :50-66 (hover scale 1.02 / active .96 / focus-visible ring); the compiled Tailwind theme in dist/styles/components.css — **`--spacing: 0.25rem` :33** · **`--text-sm: 0.875rem` :42 + `--text-sm--line-height: calc(1.25/0.875)` :43** (the ruling-1 deciders); typography/scale.css:87 `--type-micro: 0.6875rem` (fixed); typography/semantic.css ~:207-210 `text-micro` = **size+line-height 1.25 ONLY, no font-family**; typography/utilities.css:50-55 `text-mono-micro` = mono + 11px + lh 1.25 + **letter-spacing 0.025em**; theme/bridges.css:66-70 (`--font-serif → --font-stack-text`; `--font-mono → --font-stack-mono`); tokens/scheme-motion.css:43/:46 (PJS text / Fira Code mono); tokens/color-radius.css:45 (`--neutral-5: hsl(30 22% 40%)` **with its own in-file AA receipt**) + `--card: hsl(36 48% 97%)` + `--popover: var(--card)` + `--muted-foreground: var(--neutral-5)`; tokens/dark-arm.css:47 (`--neutral-5` dark) + :64-65 (`--card`/`--popover: hsl(24 8% 16%)`) · dist/MetricBadge-BpC0R_Ec.js: size ladder (sm ⇒ amount `text-mono-micro` / unit `text-micro` / labels `text-mono-micro`); label guard `!!(labelPosition && (abbreviation || label))`; root `"metric-badge cursor-pointer"`; amount span classes + `style: (isEmpty ? void 0 : { color: n.color })` in BOTH arms; unit span `"metric-badge__unit font-mono text-muted-foreground shrink-0"` — **no colour hook**; label spans emitted BEFORE the amount · fourier web/src/style.css: `@theme --font-sans` remap :13-15; body `@apply … font-serif` :20; mobile root `html{font-size:1.125rem}` ~:40 with the ≥768px 1rem reset ~:44-49; `cartoon-card` shim :107-111 (`@apply cartoon-surface` + opaque `background: var(--card)`); the `--viz-amber` darken block ~:112-127 with its published ≈3.54/≈4.6 · **CI gates: ci.yml:95 + deploy-pages.yml:114 both `npx vue-tsc -b --force`** (grep -n exact); .gitignore:41-44 (node_modules/ · web/dist/ · tsconfig.tsbuildinfo · web/tsconfig.tsbuildinfo) · useEquationCache.ts:7-8 (`eq-tab-state-v2`/`eq-tab-result-v2`) + :36-41 (try-guarded, shape-unvalidated `JSON.parse`) · repo greps re-run: InfoCard importers **0** · `forced-colors|forced-color-adjust` in web/src **0** · `metric-badge-` overrides **0** · `lucide-vue-next` **35 hits / 35 files** · `<svg` **17 occurrences / 12 .vue files** · `h-3\.5` **15 files** · `size-3\.5` **2 files** · `border-\[1\.5px\]` **10 lines / 7 files** · glass dist `forced-colors` **14 occurrences / 4 files** (a11y-overrides 6 · a11y-fallback 6 · ladder 1 · components 1; content read = focus/silhouette restoration, no semantic tone) · the untracked `web/dist` static close re-run: `"energy captured"` **0 files** / `"% energy"` **1 file** · value.js tag reads: `git show v2.0.0:package.json` → exports **includes `.` root**; `v4.0.0` → root **ABSENT**, 7 subpaths · CENSUS-2026-08-03.md ~:109 ("value 0.13→4.0 is ONE atomic transaction; none lands alone") + :184-186 (F.W1 = glass 4→7 ∧ keyframes 4.3→6 ∧ **value 0.13→4.0**) · **contrast recomputed from scratch (own node run, sRGB-linearized WCAG)**: light tinted **1.919 / 1.815 / 2.950**, light plain **2.171 / 2.020 / 3.574**, dark tinted **4.906 / 5.119 / 3.300**, dark plain 6.346 / 6.821 / **3.854**, `--neutral-5` vs light `--card` **5.116** — converging with both readers and the corpus to three decimals (the sixth independent derivation) · **pixel arithmetic re-derived**: desktop content box 110px vs 17-ch unit 112.2px; mobile 124 vs 126.2; twin fits 94.4 / 106.2; pill **desktop 27.0 (20+4+3) / mobile 30.0 (22.5+4.5+3)** vs badge 27 / 24-per-arm inverse (min-height 1.5rem = 24 desktop / 27 mobile); badge content 19.75px < 24 min-height.

## Scoreboard

**28 adjudicated roster identities — the r1 22 carried (2 amended in place, 1 with a prose correction) + 6 admitted from this corpus's misses (FR-IC-23..28: 2 MINOR · 4 INFO, three of them folds-by-reference). r2 kill register: 5 new entries (K-16..K-20), including ONE that overturns a banked r1 kill's arithmetic (K-16) — cumulative register 20, with K-6 now standing in half. All 7 reader-missed rows adopted. Both readers survive the incredulity seat; each needed one correction (reader-A's forced-colors counting unit; reader-B's C-i2 narrowing over-reaches on the target of record). 0 new BLOCKERs — the F8-REACH-01 disposition demand is untouched and re-sharpened.**

The r1 verdict is sustained and sealed against a second adversarial corpus: born-dead (the static module-graph close re-run by this seat), well-made, disposed-in-demand at **F8-REACH-01 / F.W0**. What this corpus adds is not a new defect class but **three corrections to the bank itself** and the cure-sequencing law that binds FR-IC-2 to FR-IC-3's amount limb as one edit.

## Ruled disagreements and corrections (the seat's actual work)

| # | dispute | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|
| **R2-1** | **Banked K-6's mobile column ("29.5 vs 27, a 2.5px delta") vs BOTH readers ("30 vs 27, 3px — the axis was right")** | **THE READERS WIN; banked K-6 OVERTURNED IN PART (K-16). The axis's original figure at challenge-D:290 is restored.** | Own arithmetic at the installed bytes: `--spacing: 0.25rem` (dist components.css:33 — **a rem, not a px**) ⇒ `py-0.5` = 0.125rem/side = **2.25px** at the 18px mobile root (style.css ~:40) = 4.5px total; `text-sm` line box = 0.875rem × calc(1.25/0.875) = **1.25rem = 22.5px** at any root (:42-43); `border-[1.5px]` = 3px fixed. Pill = 22.5+4.5+3 = **30.0px**; badge `min-height:1.5rem` = **27px** ⇒ **3px delta, identical to desktop's 27 vs 24** — which is also why the bank's own "symmetric mismatch" half of K-6 is right and STANDS. r1 scaled the line-height with the root and left the padding at 4px (22.5+4+3 = 29.5 — the exact arithmetic that produced the banked number). Consequence: SS-13 item 3's predicted pair is re-issued (below). |
| **R2-2** | **C-i2 / FR-IC-20's value.js bare-root break: reader-A ("fires — the later census governs the target") vs reader-B ("NARROWED — under fourier's recorded ^2.0.0 it does not fire at all; the row must name its target")** | **Reader-A's date argument governs the FACT; reader-B's discipline is adopted as LAW (procedural rider).** | Own tag reads: v2.0.0 exports **include the `.` root**; v4.0.0 exports = 7 subpaths, **root ABSENT**. Own git read: cd26c65 (the ^0.13.0→^2.0.0 peer-floor note) is dated **2026-07-03**; CENSUS-2026-08-03.md ~:109 and :184-186 — a month later, the planning record of record — fix the F.W1 target as **value 0.13→4.0, one atomic transaction**. The five bare-root sites break at the adopted target; reader-B's "does not fire at all" leans on a superseded interim coordination letter. Surviving law, bound to SS-3/SS-4: **every version-conditional row names its target version and the record that fixes it.** FR-IC-20 amended accordingly. |
| **R2-3** | **Reader-A's kill of D-14's parenthetical (the twin "is announced on open — an always-visible InfoCard has no such excuse", challenge-D:324) — reader-B silent** | **KILL SUSTAINED at this seat's own read — and it CORRECTS BANKED r1 PROSE (FR-IC-16's "the twin's HoverCard announces on open").** | EquationView.vue:276-280 read this seat: the HoverCard trigger is `<Button variant="glass" size="icon">` wrapping a bare hand-rolled `<svg>` — **no aria-label/title/sr-only/labelledby** — in the same file that imports lucide `Info` at :11 and renders it at :300; the inline paths are element-identical to lucide's info.js:10-14 (both read side-by-side). With banked fr-EquationView **D·D-B2** (nameless trigger) and **D·D-M3** (pointer-only disclosure — unreachable on iOS Safari touch), the twin is the WORSE fork on this row, not the excused one. D-14's core (no live region on a changing value) survives untouched; only the comparative framing dies — mounting InfoCard would not be an a11y *regression* relative to the status quo. |
| **R2-4** | **Reader-A's kill of D-2's typographic mechanism ("size ladder's monospace micro rung" on the unit) — the corpus at D:145-146 reads `text-micro … in --font-stack-mono` as one rung** | **KILL SUSTAINED as a mechanism correction; the headline and all width arithmetic unaffected; SS-13 item 1 re-based.** | Own reads: `text-micro` (semantic.css ~:207-210) carries **no font-family** — the unit's mono arrives via the separate `font-mono` utility in its hard-coded class list (chunk: `"metric-badge__unit font-mono text-muted-foreground shrink-0"`, resolved via bridges.css:70); it is the AMOUNT that gets `text-mono-micro` (chunk sm-arm), which additionally carries **letter-spacing: 0.025em** (utilities.css:53) that the D-2/D-4 value-width arithmetic silently ignores (immaterial to the verdict: the value is floored at 0 regardless; the unit's 0.6em × 11px advance is family-identical either way). The bilateral-clip correction (banked K-4) is re-confirmed by this seat's own read of `justify-content:center` + `text-align:center` at components.css:9-10. |
| **R2-5** | **The two readers' new rows overlap: reader-A's mutual-exclusion (FR-IC-2 ⟂ FR-IC-3's amount limb) and reader-B's colour-carrier row (the `color` prop tints the amount ONLY)** | **MERGED — they are two halves of one mechanism, admitted as FR-IC-23 (MINOR) + FR-IC-27 (INFO fold).** | Chunk read: the sole colour carrier is `style: { color: n.color }` on `.metric-badge__amount`, both arms; the unit span has no hook. Under FR-IC-2's zero-width floor the ENTIRE colour channel is unpainted at this call site — so D-4's 2.02/2.17/3.57 ratios bite only at the twin (own arithmetic: `"% energy"` twin fits, 94.4px < 110 desktop / 106.2 < 124 mobile), and the obvious FR-IC-2 cure is precisely the edit that promotes a sub-2.2:1 numeral from unpainted to painted. Reader-B's negative close adopted: the unit ink (`--muted-foreground` → `--neutral-5`) measures **5.116:1** vs light `--card` (own run) — CLEARS AA; no new defect opens there. |
| **R2-6** | **Counting-unit cautions (pt-leaf K7's law), against both readers** | **Recorded; substance unaffected.** | Reader-B's "5.11:1" and reader-A's "5.12:1" are the same number — **5.116** (own run); state the precision beside the figure. Reader-A's "14 rules across 4 dist files" for glass's forced-colors posture: this seat counts **14 grep occurrences / 4 files** (6+6+1+1) — occurrences, not rules; the characterization (focus/silhouette restoration, no semantic tone preserved) verified by content read of a11y-overrides.css:57-81. |

## Adjudicated defect roster

r1 identities carried whole (anti-rename); amendments and admissions marked. Routing per the census lane taxonomy (F.W0 substrate · F.W1 tri-package uplift · F.W3/W4 shadow retirement + frontend audit · F.W5–W8 CRUD/provenance union · F.W9/W10), else NO-WAVE-OWNER for SS-3/SS-4.

### Carried from r1 — re-corroborated by this corpus, unchanged in identity

- **FR-IC-1 · MAJOR (fact), FOLDS → F8-REACH-01 ∘ FR-COB-10 ∘ D·D-M5/RD-7 ∘ intake R3-7 — → F.W0** *(amended: evidence upgraded by FR-IC-25)*. Born-dead orphan, now re-closed by this seat a seventh way: importer grep 0; the untracked `web/dist` carries the twin's `"% energy"` in 1 file and InfoCard's unique `"energy captured"` in **0** (re-run). Both readers again filed the corpus's BLOCKER framings as the R-6 failure repeated; both correctly folded. The enforcing gate is **CI, not the package script** — see FR-IC-25.
- **FR-IC-2 · D-2 — MAJOR (latent; branch-conditional) — → F.W0 decision input; cure at F.W3/W4 under the one-edit law** *(amended: headline extended)*. The 17-ch `unit` at `size="sm"` floors the value at zero width (unit alone 112.2px > 110px box desktop; 126.2 > 124 mobile — arithmetic re-derived this seat from the installed bytes). AMENDED per FR-IC-27: under the clip the badge's entire visible content is the **muted annotation** — the semantic colour channel disappears completely, not just the number. Mechanism corrections now two deep: bilateral clip (r1 K-4), and the utility chain (this record's K-18). The `--metric-badge-max-width` knob ships unused (grep 0, re-run).
- **FR-IC-3 · MAJOR, FOLDS → fr-EquationView D·D-B3 + fr-EquationPanel D-3+C-5 — → F.W4 + GLASS-RELAY** *(amended: bound to FR-IC-2 as ONE edit per FR-IC-23)*. All six light-arm ratios re-derived by this seat from scratch (1.919/1.815/2.950 tinted · 2.171/2.020/3.574 plain) — sixth-derivation convergence. The aggravator re-verified in-file: the `--viz-amber` token-layer cure (~style.css:112-127, published ≈3.54/≈4.6, this seat reproduces the class of figures) cannot reach three TS literals.
- **FR-IC-4 · MAJOR — correction to banked D·D-B3 ("Dark arm passes" false for spline: 3.300 tinted / 3.854 plain) — → F.W4** *(re-verified: this seat's own dark-arm run reproduces 3.300/3.854 exactly; both readers converge; the both-arm AA requirement stands)*.
- **FR-IC-5 · MAJOR (live at the twin, latent here) — → F.W4** *(amended by FR-IC-24: the hue-alone/forced-colors limb)*. Two channels, one three-colour alphabet, no legend; duplication half FOLDS → M-N1 (anchors :26/:32/:38 ≡ :45/:46/:47 — re-read whole this seat).
- **FR-IC-6 · MINOR (latent), FOLDS → L·m-7 — → F.W4 + F.W5–W8** *(sharpened: reader-B's narrowing adopted — the sessionStorage keys are schema-versioned `eq-tab-*-v2` (useEquationCache.ts:7-8, read this seat), blunting the stale-schema limb; the backend-adds-a-tier limb — the one that matters — survives whole; the parse is throw-guarded but shape-unvalidated :36-41)*.
- **FR-IC-7 · MAJOR, RE-KEYED 4→8 — → F.W1** *(r1-carried; reader-B's independent v8 tag read corroborates: 70 export keys, metric family `./metric` only)*.
- **FR-IC-8 · MAJOR — the FR-COB-10/GAB-2 budget correction (7 importing files = 6 live + 1 unmountable) — → F.W1 + F.W0** *(r1-carried; sharpened by FR-IC-25: the TS2307 at the hop fails CI AND the Pages deploy, not merely a local build)*.
- **FR-IC-9 · MINOR member site, FOLDS → C-28 GLASS-RELAY ∘ GAB-5 — cured free by the uplift** *(r1-carried; byte-corroborated by both readers: root `"metric-badge cursor-pointer"`, hover 1.02 / active .96 / focus ring, no tabindex/role/emits — re-read this seat)*.
- **FR-IC-10 · MINOR, cure killed twice (child order; `orientation`→`posture`, `abbreviation` absent) — → F.W4 (M-N11 one-edit law) + F.W1** *(r1-carried; the compiled guard `!!(labelPosition && (abbreviation || label))` and label-before-amount order re-read this seat)*.
- **FR-IC-11 · MINOR (latent)** · **FR-IC-12 · MINOR (three red registers)** · **FR-IC-13 · MINOR (the `.glass-floating` `--muted-foreground` lift — the fourth twin divergence)** · **FR-IC-14 · MINOR (glass-plate nullity on cartoon grounds — the shim's opaque `var(--card)` re-read this seat at style.css:107-111)** · **FR-IC-15 · MINOR (`tier.ts` split)** — all r1-carried, untouched by this corpus, routings stand (F.W3/W4 / NO-WAVE-OWNER + C-28 rider as banked).
- **FR-IC-16 · the craft tail — MINOR — → F.W3/W4** *(amended three ways)*: (a) **D-11's mobile column RESTORED to the axis's 30 vs 27** (ruling R2-1; K-16) — desktop 27 vs 24 and the symmetric-mismatch kill stand; (b) **the D-14 prose corrected** (ruling R2-3): the twin's HoverCard does NOT announce on open — nameless pointer-only trigger (D·D-B2/D·D-M3); D-14's no-live-region core is branch-conditional as before; (c) **D-18 ADMITTED into the tail** (absent from the r1 roster): two spellings of one size utility — `h-3.5` **15 files** vs `size-3.5` **2 files** (this seat's own grep; the axis figure exact) — subject :39 vs twin :300; repo-level sweep item. D-10/D-12/D-15 unchanged (D-12's corrected 10 lines / 7 files re-grepped this seat).
- **FR-IC-17 · INFO, DISCHARGED-BY-UPLIFT** · **FR-IC-18 · MAJOR → GLASS-RELAY + F.W4** · **FR-IC-19 · MINOR, census of record 21 sites / 14 files → F.W1-sequenced F.W3** — r1-carried, untouched.
- **FR-IC-20 · INFO (consumption posture) — AMENDED by ruling R2-2**: the bare-root break row now carries its target: **fires at the census-adopted `value 0.13→4.0`** (CENSUS-2026-08-03 ~:109/:184-186, the later record governing over cd26c65's interim ^2.0.0 note — both read this seat; v2 root present / v4 root absent at the tags — own reads). Reader-B's registry context adopted: the W.L5 library-band row (timingFunctions deleted; 8/22 curves drift) and `safeAccentColor` surviving at v4 `./color` ride their banked homes. Anti-superlative framing (immunity by omission) sustained.
- **FR-IC-21 · MINOR (procedural — sweep the F8 delta table whole)** · **FR-IC-22 · INFO, GLASS-RELAY rider — AMENDED: the four-line producer rule now carries a THIRD defect** (see FR-IC-26-rider below) — r1-carried otherwise.

### Admitted this corpus (r2)

- **FR-IC-23 · reader-A miss 1 — MINOR (cure-sequencing; decision-bearing for F.W3/W4).** **FR-IC-2 and FR-IC-3's badge-amount limb (D-4) are mutually exclusive at this call site — and curing the first is what makes the second visible.** At InfoCard the amount is floored at 0 (unit alone exceeds the box at `shrink-0`), so the 2.02/2.17/3.57 numeral ratios paint nothing HERE; at the twin (`unit="% energy"`, fits at 94.4/106.2px — own arithmetic both roots) the amount paints and D-4 bites. Each row is live at exactly one fork; neither the corpus nor the r1 bank says so (registry re-swept: fr-InfoCard routes the two rows to different gates with no interaction note). CONSEQUENCE: the obvious FR-IC-2 cures (shorten the unit; set the shipped `--metric-badge-max-width` knob) promote a sub-AA numeral from unpainted to painted — **FR-IC-2 ↔ FR-IC-3 must land as ONE edit**, extending fr-EquationPanel M-N11's one-edit law (which FR-IC-10 already binds for this file). The tier-pill limb of FR-IC-3 is live at InfoCard regardless — the exclusivity is scoped to the amount limb only. **→ F.W0 (decision note) + F.W3/W4 (the one-edit binding).**
- **FR-IC-24 · reader-A miss 2 — MINOR (amendment to FR-IC-5, not a free-standing row).** **The energy band is encoded in hue alone — WCAG 1.4.1 (Level A) — and `forced-colors: active` erases it outright.** Sole DOM carrier = the inline `color` on `.metric-badge__amount` (chunk re-read this seat: two spans, no icon/shape/word/state hook); the ramp IS the green↔red confusion pair with amber between; the UA overrides inline `color` under forced colors; fourier ships **zero** forced-colors rules (grep 0, re-run) and glass's 14 occurrences restore focus/silhouette only (content read). The tier pill degrades acceptably (its label word survives); the energy badge degrades to nothing. Fleet class CITED not re-booked: fr-ConvergenceLegend M-R3 ∘ fr-EquationView D·D-M12 ∘ fr-EquationPanel D-2+C-11+M-N4. Graded MINOR on reader-A's own honest counter (the numeral remains legible; what is lost is the judgment band — whose thresholds are also undocumented, FR-IC-5's own point). Falsifier: any textual/iconic band indicator or a `forced-color-adjust` — neither exists. **→ F.W4** (a redundant-encoding requirement riding the D·D-B3 ramp spec).
- **FR-IC-25 · reader-B miss 2 — INFO (evidence upgrade to FR-IC-1/FR-IC-8).** **The enforcing type gate is CI, and it runs cold by construction**: ci.yml:95 and deploy-pages.yml:114 both `npx vue-tsc -b --force` (grep -n this seat); `--force` defeats the `-b` incremental skip, and the tsbuildinfo is gitignored twice (.gitignore:43-44) so CI always starts cold anyway. At the tri-package hop the TS2307 on `@mkbabb/glass-ui/metric-badge` (InfoCard.vue:4) fails **CI and the Pages deploy** — the sharp form of FR-IC-8's budget stake. Both original axes had rested the gate claim on the package script; registry-swept (no fr-* row cites either workflow line). **→ F.W0 substrate note + F.W1.**
- **FR-IC-26 · reader-B miss 3 — INFO (census/budget row).** **The `lucide-vue-next → @lucide/vue` migration budget (35 sites — re-grepped this seat) is import-keyed and blind to 17 hand-inlined `<svg>`s across 12 .vue files (own grep)** — one of which is an element-identical copy of lucide's `Info` inside InfoCard's extraction target (EquationView.vue:277-279 vs info.js:10-14, both read side-by-side this seat), 12 lines below the file's own `import { Info }` and 23 above its `<Info>` render. Identity note: fr-EquationView records the byte-identity as a D·D-B2 instrument — the icon-duplication fact folds there; the **census-blindness increment** (any KEEP branch extracts two spellings of one glyph into one 43-line component; DELETE still leaves 17 unmigrated hand glyphs) is this row's content. **→ F.W1** (budget rider).
- **FR-IC-27 · reader-B miss 1 — INFO, FOLDS BY REFERENCE → fr-EquationPanel M-N11 ∘ fr-AnimationControls C-15.** The `color` prop reaches the amount span ONLY (chunk: `style: { color: n.color }` both arms; unit hard-coded `text-muted-foreground`, no hook) — absent from all three axes and all 22 r1 rows as a stated fact. Books here only as the FR-IC-2 headline amendment (above) plus the negative close: the unit ink measures **5.116:1** vs light `--card` (own run) — CLEARS AA, matching the producer's own in-file 5.21-vs-page annotation. Not a new book.
- **FR-IC-28 · reader-B miss 4 — INFO, FOLDS BY REFERENCE → fr-EquationPanel M-N3.** The 1-decimal rounding and the energyColor thresholds disagree at the boundaries: e=0.9499 renders **"95.0" in red** (0.9499 < 0.95); e=0.98999 renders **"99.0" in amber** — the printed number contradicts its own colour. Verified at the bytes this seat: `toFixed(1)` at InfoCard.vue:32, thresholds at notation.ts:45-46; reachable verbatim at both live sites (EquationView.vue:293/:296; EquationPanel per the banked row). Not a new book; recorded because it applies at this file and no InfoCard axis or r1 row carries it.
- **FR-IC-22-rider (reader-A miss 3) — INFO, GLASS-RELAY rider on FR-IC-22.** **A third defect in the same four-line producer rule**: `.metric-badge > span { line-height: 1 }` (components.css:41-48, `@layer components`) is dead for every span MetricBadge renders — each carries a size `@utility` (`text-micro`/`text-mono-micro`, both re-declaring `line-height: 1.25` — own reads) and Tailwind v4 places utilities above components unconditionally. The rule now carries three defects at once: the comment's false scope (banked FR-IC-22), the `overflow-wrap` that cannot engage on the `shrink-0` unit (D-2's own note), and an unreachable `line-height`. Verdict impact: none — `min-height: 1.5rem` dominates (content 19.75px < 24px, own arithmetic — which is why no prior seat noticed). Falsifier: a `.metric-badge > span` rendered without a size utility (the compiled render emits none), or an `!important`. **→ the standing glass-ui BH relay beside FR-IC-22; NOT consumer work.**

## Killed-claims register — r2 entries (K-16..K-20, continuing r1's K-1..K-15)

| # | killed claim | adjudication |
|---|---|---|
| **K-16** | **Banked r1 K-6's mobile arithmetic** — "`text-sm` is Tailwind's own scale … mobile is **29.5 vs 27**, a 2.5px delta" — and its propagation into FR-IC-16 and SS-13 item 3 | **OVERTURNED at the bytes by this seat (ruling R2-1); both readers concordant.** `--spacing` is a rem (components.css:33): `py-0.5` = 2.25px/side at the 18px root; pill = 22.5+4.5+3 = **30.0px** vs badge 27 — **3px, identical to desktop**, exactly what challenge-D:290 filed. The banked correction scaled the line-height and not the padding. K-6's other half — the "asymmetric optical drift" prose kill (symmetric mismatch) — **STANDS**. The Tailwind-own-scale premise also stands; only the arithmetic falls. SS-13 item 3 re-issued below. |
| **K-17** | **D-14's parenthetical** (challenge-D:324: the twin's HoverCard content "*is* announced on open. An always-visible InfoCard has no such excuse") — and the banked FR-IC-16 prose that repeated it | **KILL SUSTAINED (ruling R2-3, this seat's own read of :276-280).** The twin's trigger is a nameless hand-rolled `<svg>` in a glass Button (D·D-B2, BLOCKER→MAJOR banked) and the disclosure is pointer-only (D·D-M3) — the twin is the WORSE fork. D-14's underlying finding survives; the comparative framing and the banked prose die. |
| **K-18** | **D-2's typographic mechanism** — the unit typeset via "the size ladder's monospace micro rung" (challenge-D:145-146's `text-micro … in --font-stack-mono` conflation) | **KILL SUSTAINED (ruling R2-4).** `text-micro` carries no family; the unit's mono is the separate `font-mono` utility; the AMOUNT gets `text-mono-micro` + **0.025em tracking** that the D-2/D-4 arithmetic ignores. Headline, sign-of-deficit, and both K-4-corrected clip facts unaffected; SS-13 item 1's selector chain re-based. |
| **K-19** | **Reader-B's C-i2 narrowing in its strong form** — "under fourier's own recorded target the bare-root break does not fire at all" | **REJECTED AS STATED; the discipline survives as law (ruling R2-2).** The recorded ^2.0.0 floor (cd26c65, 2026-07-03) is an interim coordination note; the census of record (2026-08-03, ~:109/:184-186) fixes `value 0.13→4.0` atomic — the break fires at the adopted target. Adopted rider: version-conditional rows name their target and the record that fixes it. |
| **K-20** | **Precision/counting-unit slips, both readers** — "5.11:1" vs "5.12:1" for one number; "14 rules" for 14 grep occurrences | **RECORDED (ruling R2-6); substance unaffected.** The number is 5.116 (own run); the forced-colors posture is 14 occurrences / 4 files, content = focus/silhouette only. pt-leaf K7's law re-affirmed: state the unit and precision beside every census figure. |

**Convergent re-derivations (not new kills — recorded as corroboration):** both readers independently reproduced r1's K-2 (10 lines / 7 files — re-grepped this seat), K-4 (bilateral clip — re-read this seat at components.css:9-10), K-7 (12 tags / 7 files total; live 11/6), K-12 (the CM-Serif dead wiring — re-read this seat: remap :13-15, body `font-serif` :20, bridges :68 → PJS), and K-13 (the anchor-drift cluster: colours :26/:32/:38; api.ts:192; Dockerfile:11). Five independent reproductions of five banked kills, zero reversals — the r1 kill register is sealed except where K-16 corrects it.

## Superlatives (provenance preserved; r1's eight sustained by carriage)

1. **The producer's neutral ramp ships its own measured contrast beside the value** (reader-A, adopted): color-radius.css:45 carries `--neutral-5 … WCAG AA: 5.21:1 vs page / 4.90:1 vs muted` in-file, and this seat's own measurement on this component's actual surface (vs `--card`) = **5.116:1 — clears AA**. The one part of InfoCard's colour never in doubt is the part that came from a token; the three TS literals ship with no receipt in a repo that had already darkened `--viz-amber` for exactly this. The cleanest possible statement of FR-IC-3's aggravator — and no axis or prior seat computed it.
2. **Correct micro-typography, automatic and invisible** (reader-A, adopted): `text-mono-micro`'s **0.025em positive tracking** on 11px monospace numerals reaches `.metric-badge__amount` through the size ladder with no consumer action (own reads). Both challenge arithmetic passes silently assumed zero tracking; the SS-13 pixel spec must account for it.
3. **The icon gutter survived the fork 3-for-3** (reader-A, sustaining r1 superlative 6 with a survival-rate instrument): when the twin was forked, the unit string, the icon-size spelling, and the rhythm all changed — `items-start`, `shrink-0`, `gap-1.5` were kept verbatim (both files read this seat). Bugs-by-default avoided twice; the strongest available evidence the construction is right.
4. **Calibrate-before-measure, now at six independent derivations** (both readers + this seat): challenge-C:79's method validation (reproducing the repo's own published `--viz-amber` figures before asserting new ratios) was re-run blind by both readers and by this seat — all landing on 1.815 / 3.300 to three decimals. The standard the megatranche should require of every colour row; challenge-L FC-4 (killing your own finding by measurement) is the same virtue negated, and D-12's stale 24-site figure remains the counter-example.
5. **Reader-B's negative close discipline**: filing its own new row (FR-IC-27) WITH the measurement that caps it (the unit ink clears AA at 5.116:1) — so the row cannot spawn a phantom defect downstream. A finding filed with its own bound is worth two filed without.
6. **The component itself, unchanged from r1** (four counts re-verified in passing: zero style/teardown surface; props read inside computeds; 43 LOC; the contract-v2-honouring bare-subpath import) — the extraction was done well and abandoned at the last step; the failure is in the graph, not the unit. F8-REACH-01 remains genuinely three-way.

## UNPROVEN-NEEDS-LIVE (SS-13 inputs — re-issued where r1's specs carried errors)

1. **FR-IC-2 pixel witness — only under an ISOLATED_HARNESS/KEEP ruling at F8-REACH-01** (under DELETE it retires unmeasured, correctly). Read `.metric-badge__amount` `getBoundingClientRect().width` (predicted **0**), host `scrollWidth` vs `clientWidth`, and `getComputedStyle(host).justifyContent`, at 375px and 1280px — the clip is **bilateral** (~2.1px/edge desktop). **RE-BASED (K-18)**: assert the unit span's computed `font-family` via its `font-mono` utility and the amount's `letter-spacing: 0.275px` (0.025em @ 11px) — the r1 spec's selector chain named the wrong utility. Fold into the same mount: FR-IC-14's plate probe (badge background vs parent, rest + hover — predicted indistinguishable, hover delta 0) and the FR-IC-24 forced-colors limb (DevTools forced-colors emulation: the band is static-certain lost; live-only is the residual ink's legibility and whether `glass/a11y-fallback.css` changes the badge plate). Never run standalone — the component paints on no route today.
2. **SS-13 chip heights — RE-ISSUED (K-16)**: one `getBoundingClientRect().height` pair at the live twin, <768px viewport. Predicted **27 vs 24 desktop, 30 vs 27 mobile** — the axis's original figures, NOT the r1 bank's 29.5. A 29.5 measurement would vindicate the bank; 30.0 confirms this record.
3. **The twin's composited backdrop** *(r1-carried)*: fourier's `.info-hovercard` override × ladder.css's `--muted-foreground` re-point under `.glass-floating` (FR-IC-13). One probe serves both; the dark-arm arithmetic is CLOSED (FR-IC-4), not live. The axe gate never visits `/equation` today.
4. **Whether the two-tone badge** (semantic numeral vs permanently muted annotation — FR-IC-27) **reads as one readout or two at 11px** — eyes, not greps; KEEP branch only.
5. **The FR-IC-23 knob question**: curing FR-IC-2 via `--metric-badge-max-width` (shipped, unused — grep 0) rather than shortening the unit paints the numeral at the same sub-AA ratios — the one-edit constraint holds either way, but the wrap/geometry outcome at 375px is not statically decidable. Rides mount 1.
6. **S-1's rendered `aria-hidden`** *(r1-carried)*: re-assert after the `@lucide/vue` migration — 35 sites depend on the default; FR-IC-26 adds that the 17 hand-inlined SVGs are outside that default entirely.
7. **RETIRED** *(r1-carried, re-confirmed)*: D-1's build-graph probe (static close re-run this seat: 0/1); D-3's dark limb (closed arithmetic, re-run this seat: 3.300/3.854); D-17's optical nudge (premise dead — K-12, re-verified).

## Banked-row corrections leaving this record (for the forming specs)

1. **fr-InfoCard r1 K-6 / FR-IC-16 / SS-13 item 3**: the mobile chip pair is **30 vs 27** (3px, symmetric with desktop) — the r1 correction to 29.5 is itself the error; the axis was right (K-16; own arithmetic at the bytes).
2. **fr-InfoCard r1 FR-IC-16 (D-14 prose)**: "the twin's HoverCard announces on open" is FALSE — nameless pointer-only trigger (D·D-B2/D·D-M3, re-verified at :276-280). The comparative framing inverts; the twin is the worse fork.
3. **fr-InfoCard r1 SS-13 item 1**: probe spec re-based — unit = `text-micro` + `font-mono` (the size rung carries no family); amount = `text-mono-micro` with 0.025em tracking (K-18).
4. **FR-IC-2's headline extended**: under the clip the entire semantic colour channel is unpainted (FR-IC-27) — the loss is worse than "the number disappears."
5. **FR-IC-2 ↔ FR-IC-3 (amount limb) are mutually exclusive per fork and must land as ONE edit** (FR-IC-23) — no F.W3/W4 spec may cure one without pricing the other.
6. **FR-IC-20 names its target**: the bare-root break fires at the census-adopted `value 0.13→4.0` (the 2026-08-03 record governs over cd26c65's interim ^2.0.0 note); procedural law — version-conditional rows name their target and its fixing record (K-19).
7. Procedural, binding SS-3/SS-4 *(r1-carried, extended)*: sweep the F8 delta table whole; re-resolve every anchor; state the counting unit AND precision beside every figure (K-20).

## Closing verdict

**DEFECTIVE-AND-DEAD — SUSTAINED AT r2; disposition = execute the standing F8-REACH-01 demand at F.W0, in one breath with F8-REACH-02 (ADJUDICATED, routed, not stamped).** A second adversarial corpus moved no verdict and re-derived every banked kill it touched — but it caught the bank itself three times: r1's K-6 arithmetic falls at the bytes (the axis's 30-vs-27 restored), r1's D-14 prose inverted the fork comparison the registry had already refuted, and r1's probe specs would have measured the wrong selector chain against the wrong predicted pair. Its best new work is sequencing, not discovery: FR-IC-2 and FR-IC-3's amount limb are live at opposite forks and must be cured as one edit — the sharpest statement yet that KEEP-as-is is not a branch. Both readers survive with one correction each (a counting unit; an over-reached narrowing), and their convergent independent arithmetic — now six derivations deep on the colour rows — is the method bar the SS-3/SS-4 authors inherit. Nothing lost: head (the orphan still routed to the wave that owns its ruling), tail (the unit's craft sustained through whatever branch F.W0 rules), or interval (every r1 identity carried, every kill retired or overturned with its refutation on the record).

— end of apotheosis r2. The three challenge files, both r2 reader returns, and the r1 record's evidence remain immutable beside this file per E-1/E-3; the r1 roster is superseded-by-carriage, not erased — every surviving row is present above under its original id; dispositions are ADJUDICATED, not VERIFIED.
