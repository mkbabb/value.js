claude-fable-5 (served model id)

# fr-InfoCard — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Seat**: fresh Fable adjudicator · model receipt `claude-fable-5` (Fable 5), observed in this seat's own system prompt — recorded against L-11/L-14.
**Date**: 2026-08-24 · **Subject repo**: fourier-analysis HEAD `cd26c65`, dirty WT (the M.W1a pin-bump family; the subject's own diff is exactly one hunk, `:amount=`→`:value=`, 1+/1− against index blob `fe451b4` — re-taken this seat) · **Subject**: `web/src/components/equation/InfoCard.vue` (43 lines, read whole).
**Corpus**: 3 challenge axes (`challenge-D-design.md` 21 defects/5 superlatives · `challenge-L-library.md` 8/3 · `challenge-C-consumption.md` 12/3, all authored 2026-08-06) re-read by two hostile re-readers — **reader-DU** (design-and-usage) and **reader-LC** (library-and-code) — whose returns this seat adjudicated, attacking both before adopting either.
**Producer pins**: installed glass-ui **4.0.0** · producer repo carries tags **v7.0.0 (2026-07-17)** and **v8.0.0 (2026-08-09)** — the challenges were current at authoring and stale at adjudication; per banked FMD-11 + fr-EditorControlsDock M-10, every budget below is keyed to the **4→8 hop**.
**Status verbs**: every disposition is **ADJUDICATED**, not VERIFIED — the X·F specs are still forming; routings are agglomeration input (M-25). Line anchors are the dirty worktree at `cd26c65`; re-resolve before executing any cure (the D-19 lesson).

**This seat's own instruments (static + git/grep only; zero browser probes — probe parsimony; product source READ-ONLY; this file the sole write)**: InfoCard.vue read WHOLE (43) · notation.ts read WHOLE (48: trig red `hsl(6, 72%, 49%)` at **:15**; TIER_INFO colours **:26/:32/:38**; energyColor **:45-47** — the L-4 anchor decider) · EquationView.vue:274-302 read (v-if="tierInfo" :274; `HoverCardContent class="info-hovercard"` ; `unit="% energy"`; `size-3.5`; `mt-2`) · `git diff -- InfoCard.vue` → exactly `- :amount=` / `+ :value=` (the C-S-1 decider) · metric importer census `grep -rln "glass-ui/metric" web/src` → **7 files** (EquationView, EquationPanel, AnimationControls, EditorControlsDock, GalleryAdminBanner, GalleryDraftsSection, InfoCard) — the FR-COB-10-correction decider · `<MetricBadge` tag census → **12 real tags / 7 files** (GalleryAdminBanner :45/:52/:60/:68/:75/:82 = 6; its :96 hit verified in context = a CSS comment inside `<style scoped>`) · font census: `font-sans` in web/src = declaration style.css:14 + its own comment :6, **zero application sites**; body `@apply … font-serif` at :20 (the D-13/D-17 decider) · NotationPills reachability: FunctionInput.vue:10/:223 → EquationView.vue:14/:198 (the D-5-kill decider) · web/Dockerfile `RUN npm ci` at **:11** · tsconfig include glob at :19; package.json `build: vue-tsc -b && vite build` at :8 · installed glass-ui dist reads: ladder.css `:where(.glass-floating, .glass-overlay) { --glass-tint-source; --glass-tint-strength: var(--glass-tint-strength-aa); --muted-foreground: var(--foreground) }` **verbatim** + the OVERLAY-band comment above it (the MISS-1 decider); glass.css `--glass-opacity-quiet: 0.50` / `--glass-opacity-resting: 0.65` (:23-24) and the `--glass-bg-quiet/resting = color-mix(in srgb, var(--card) …%, transparent)` recipes (:138-141) (the MISS-3 decider); components.css:41-48 `.metric-badge > span { … overflow-wrap: anywhere }` under a comment claiming "Scoped to the value span" (the MISS-4 decider); `HoverCardContent-*.js` chunk carries the literal `glass-floating` class · producer tag reads (never HEAD): `git show v7.0.0:src/components/card/Card.vue` → `cartoon?: boolean` + `surface` default `"glass"` + `cartoon: false` (the C-M2-kill decider); v7/v8 exports via json parse → metric keys = `['./metric']` at BOTH tags, **74 keys at v7.0.0 / 70 at v8.0.0**; tag dates v7 2026-07-17, v8 2026-08-09 · registry re-reads at every cited fold: fr-CanvasOverlayButton (F8-REACH-01 verbatim, R-6, FR-COB-10, K-12) · fr-EquationView (RD-7 · D·D-B3 **incl. the verbatim "Dark arm passes."** · D·D-M10 · L·m-7 · D·D-M5) · fr-EquationPanel (M-N1 · M-N11 · D-3+C-5 AMENDED "3 consumers / 2 routes … InfoCard.vue:5/:14" · D-9+D-L4 · C-20 · D-14+D-L18) · fr-GalleryAdminBanner (GAB-2 incl. the labelPosition→orientation→posture double rename + K-5 · GAB-5 "6 of the repo's 12 instances" + K-6 · GAB-8 · GAB-13/K-9) · fr-ContourPreview (row 4 + KILL-6) · fr-EqCoefficientsPanel (FR-EQC-3 "21 sites / 14 files … not 14, not 25" · FR-EQC-5 · FR-EQC-13 · K-7) · fr-FourierMorphSvg (ruling 4: cartoon-surface DE-SPRUNG at 8.0.0, "STATIC", card/styles.css:256) · fr-FourierMorphDemo (FMD-11 8.0.0 baseline law) · fr-EditorControlsDock (M-10/L-7 4→8 re-key law · D-15/D-16/C-18) · fr-AnimationControls (C-28) · fr-AppHeader (FR-AH-2 font-truth · FR-AH-11) · fr-AdminAuditLog (AA-4 · AA-16 · K-13) · fr-BasisSelector (M-7).

## Scoreboard

**22 adjudicated roster rows — 8 MAJOR · 11 MINOR · 3 INFO. 15 killed claims (2 of them against the re-readers themselves). 8 reader-missed rows adopted (4 per reader — including TWO corrections to banked registry rows). 8 superlatives sustained. 0 BLOCKERs booked — the corpus's three BLOCKER filings all fold to the standing F8-REACH-01 demand.**

The component is born-dead (five-way proven, now with a **static** module-graph close), well-made (eight sustained superlatives), and disposed-in-demand: **F8-REACH-01** (`N.C11`, `equation/InfoCard.vue`, sha `b2c2e718…`/1,516 B, "exact KEEP_WITH_MOUNT, ISOLATED_HARNESS, or DELETE; HOLD is RED") already governs it, one row above the F8-REACH-02 row the sibling corpus also missed (fr-CanvasOverlayButton R-6 — the identical one-row-of-a-two-row-table failure, now committed by a second full corpus and both of its axes' BLOCKER filings).

## Ruled disagreements (the seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **D-2 severity** | MAJOR — "BLOCKER on any mount; presently latent; the ONE genuinely novel finding" | MINOR — "orphan-only latent; BLOCKER is the double-count RD-7 banned" | **MAJOR (latent, branch-conditional) — DU's grade with LC's conditionality as the routing** | The defect is real, novel (both readers' registry sweeps agree: unbanked), and total — the measured quantity renders zero pixels. RD-7's ban forbids re-grading the *orphan amplifier*, not a novel authored-surface defect; but LC is right that the row's life is branch-dependent. Ruled: MAJOR-latent, explicitly conditional on the F8-REACH-01 branch — under DELETE it retires unmeasured (correctly); under any KEEP branch it is the **first mount-gate**. It is decision INPUT to F.W0, not a free-standing blocker. |
| **D-13/D-17 — the typographic register** | CONFIRMED with the register described as "serif-14-semibold → mono-11-muted → serif-14-muted" (CM Serif premise) | KILLED the premise: `--font-sans` is dead wiring; body runs `font-serif` → Plus Jakarta (a sans) → unloaded → system faces (FR-AH-2) | **LC wins — this seat's own greps decide it** | `font-sans` application sites in web/src = **0** (declaration :14 + its own comment :6 only); body `@apply font-serif` at :20; no fourier override of `--font-serif`/`--font-stack-text`; the resolution chain and the unloaded-PJS fact are banked verbatim at FR-AH-2/FR-AH-11. D-13's collision arm survives **RE-BASED** (prose-in-mono-slot at 11px beside 14px semibold is still a register collision; the flanking face is a system fallback, not CM Serif); D-17's CM rationale dies with the premise. |
| **C §3 S-1 — the `:amount→:value` superlative** | Kept (praised the silent-break analysis) | KILLED the framing: the credited edit is uncommitted working-tree state; GAB-13/K-9 + FR-COB-10 route the identical edit as accrued COST | **LC wins on framing; DU's substrate preserved as method-praise** | My `git diff`: exactly 1+/1− against blob `fe451b4` — `git stash` erases the "superlative". The precedent is exact and banked twice (GAB-13: "challenge-C §6 S-3's superlative credits the uncommitted edit itself"; FR-COB-10 books InfoCard's identical dirty edit at F.W0 as cost on a frozen tree). The silent-break MECHANISM (no `amount` key in the compiled props table → `$attrs` fall-through → muted em-dash with the colour binding stripped, no warning, no type error) is correct, valuable, and kept as superlative S-2 below — about the method, never the edit. |
| **C-M2 — the cartoon-card shim** | CONFIRMED-MINOR, folded to "fr-AdminAuditLog AA-4/K-13" | KILLED the prescription: `<Card surface="cartoon">` is UNSPELLABLE at v7 (`cartoon?: boolean`), struck at v8; the shim is producer-endorsed (fr-ContourPreview row 4/KILL-6) | **Merged: LC's kill governs the cure; DU's fold governs the fact; BOTH fold anchors corrected** | My v7 tag read: `cartoon?: boolean` at the prop surface, `surface` defaulting `"glass"` — C-M2's "dischargeable NOW at 4.0.0" migration would be reverted one major later, exactly as KILL-6 ruled for the sibling. And DU's anchor is the wrong identity: AA-4 is the shim-**adoption** cure row; the shim-scope identity is **FR-EQC-3** (21 sites/14 files, F.W1-sequenced F.W3) + ContourPreview row 4. Counting-unit correction (pt-leaf K7 law): DU's re-confirmed "25 occurrences / 15 files" is the raw-occurrence instrument FR-EQC-3 already explicitly rejected — the adjudicated census is **21 application sites / 14 files**. |
| **D-7/L-3/C-M4 severity — the lying fallback** | MAJOR (semantics half) | MINOR (folded to L·m-7's banked grade; trigger latent) | **MINOR (latent) — LC wins; DU's semantics increment ADOPTED as the row's content** | Reachability is honest: api/routers/equations.py assigns exactly three tiers; the live route is the unvalidated sessionStorage parse, which requires a stale/tampered payload. The banked L·m-7 row already carries the type-dead/runtime-live mechanism at MINOR; DU's genuine increment — the fallback *fabricates provenance* in a provenance-disclosure component — books as the CURE clause (explicit unknown branch, no mechanism sentence), not as a second severity. Same latency logic as RD-7. |
| **The dark-arm contrast fact** | MISS-2: banked D·D-B3's "Dark arm passes" is FALSE for spline (3.30 tinted / 3.85 plain) | Confirmed the same figures in its own D-3/D-4 row, but its UNPROVEN cell claims "no axis asserted" the dark failure | **DU wins whole; LC's cell corrected (K-14)** | challenge-C:77's table carries `spline … dark 3.30:1 ❌` — the corpus HAD the fact; the bank does not. Both readers independently recomputed and converged (4.91/5.12/**3.30** tinted; 6.35/6.82/**3.85** plain), with the C axis a third derivation and the method validated against the repo's own published D.W4.d numbers. Kills and unproven cells must quote the corpus whole (fr-AdminAuditLog K-11's law). |
| **D-16 disposition** | Cure KILLED — pre-empted by the 8.0.0 de-sprung `cartoon-surface`; record discharged-by-uplift | Confirmed in the craft tail without the uplift note | **DU wins** | Banked twice: fr-FourierMorphSvg ruling 4 (utility moved to card/styles.css:256, body three static declarations, docblock "STATIC") + FR-EQC-5/K-7. A consumer-side `--static` carve authored now is dead code at F.W1; the converse risk (interactive cartoon sites silently losing lift) is already banked at FR-EQC-13. |

## Adjudicated defect roster

Merged identities; original axis ids for life. Routing targets are the census lane taxonomy (F.W0 substrate · F.W1 tri-package uplift · F.W3/W4 shadow retirement + frontend audit · F.W5–W8 CRUD/provenance union · F.W9/W10), else NO-WAVE-OWNER for SS-3/SS-4 spec authoring.

- **FR-IC-1 · D-1 / L-1 / C-B1 — MAJOR (fact), FOLDS BY REFERENCE — never bookable as a fresh BLOCKER.** Born-dead orphan: zero importers in tree and history (grep/pickaxe/blob-level/registration-surface — all re-verified by the readers, importer census re-run by this seat); one of exactly 2 zero-inbound SFCs of 66 (fifth independent derivation); gates the only frontend type gate (`tsconfig.json:19` glob + `vue-tsc -b`) while Rollup never emits a byte; falsifies the A-ledger (W3-adoption-ledger:36/:80; PROGRESS:385). Identity: **F8-REACH-01** (standing RED-if-held disposition demand) ∘ **FR-COB-10** ∘ **fr-EquationView D·D-M5/RD-7** ∘ intake **R3-7** (ADOPT-AS-FACT). All three axes filed it fresh at BLOCKER and none cited F8-REACH-01 — the R-6 failure repeated by a second corpus (see FR-IC-21). NEW evidence this corpus adds, adopted: reader-LC's **static module-graph close** — the untracked local `web/dist` build (mtime 2026-06-12, post-dating InfoCard's last commit with no intervening change to the file) contains the twin's `"% energy"` and **zero** occurrences of InfoCard's unique `"energy captured"` — retiring the corpus's last open reachability probe without a browser or a build. **→ F.W0** — rule F8-REACH-01 and F8-REACH-02 in one breath (FR-COB-1's routing), with the uncommitted `:amount→:value` edit landed-or-minuted per GAB-13.
- **FR-IC-2 · D-2 — MAJOR (latent; branch-conditional; the corpus's one genuinely novel find).** `unit="% energy captured"` (17 ch) at `size="sm"` overruns MetricBadge's 8rem cap: unit 17×0.6×11 = 112.2px alone exceeds the 110px desktop content box (124 vs 126.2 mobile); `shrink-0` on the unit + `truncate` on the value (automatic-minimum-size 0 — the mechanism the fleet ratified at fr-AdminAuditLog's truncate rows) floor the value at **zero width**. The number — the badge's reason to exist — vanishes. Every CSS byte re-verified by both readers at the installed pin; Fira Code IS self-hosted (index.html:19 → public/fonts.css); the live twin's `"% energy"` (9 ch) fits — in-tree proof the failure was felt and fixed on the other fork only. Rendering prediction CORRECTED (K-4): `justify-content: center` makes the clip **bilateral**, not right-hand. No `--metric-badge-*` override exists in fourier (grep → 0). **→ F.W0** (decision input to F8-REACH-01: KEEP-as-is is not a branch); under any KEEP/ISOLATED_HARNESS branch the cure rides **F.W3/W4** bound into M-N11's one-edit law. Retires unmeasured under DELETE — correctly.
- **FR-IC-3 · D-3 / D-4 / C-B2 — MAJOR, FOLDS → fr-EquationView D·D-B3 + fr-EquationPanel D-3+C-5 AMENDED (which already names InfoCard.vue:5/:14 in the repair's regression surface).** All three tier literals fail WCAG AA light-arm on the tinted pill (identified **1.82** · symbolic **1.92** · spline **2.95**) and on the plain card (2.02/2.17/3.57) at 14px/600 — quadruple-converged arithmetic (two axes + two readers, method validated against the repo's own published 3.54/4.6 figures). The aggravator is banked: literals in a TS module are unreachable by the token-layer cure that fixed `--viz-amber`. **→ F.W4 + GLASS-RELAY** per the banked row — with the FR-IC-4 amendment below.
- **FR-IC-4 · reader-DU MISS-2 — MAJOR — a CORRECTION TO A BANKED ROW.** fr-EquationView **D·D-B3's "Dark arm passes." is false for the spline/"Approximate" rung**: dark `--card`/`--popover` = `hsl(24 8% 16%)` (both, dark-arm.css:64/:65), spline `hsl(0, 84%, 60%)` → **3.30:1 tinted / 3.85:1 plain** — below the 4.5:1 floor in BOTH arms, on the highest-frequency tier (the numeric fallback path). The other two tiers clear dark (4.91/5.12 tinted), which is presumably how the false summary formed. Triple-derived (challenge-C:77's own table + both readers' independent recomputations, converged to two decimals). Consequence: D·D-B3's routed cure is under-specified — the semantic-status ramp must clear AA in **both** arms for the red rung; fourier's own `.dark` re-basing block (style.css:123-127) is the in-repo precedent. **→ F.W4** (amendment riding D·D-B3; the forming spec states both-arm AA).
- **FR-IC-5 · D-6 / L-4 (collision half) — MAJOR (live at the twin, latent here).** Two independently-derived colour channels — `info.color` (tier/provenance) and `eColor` (energy/fidelity) — share one three-colour alphabet in one 8px `items-center` row with no legend. Independence verified: `budget` is a free request parameter (`ge=2, le=50`); nothing couples tier to `energy_captured`; green-"Exact"-beside-red-90.0 and red-"Approximate"-beside-green-99.5 both reachable. Thresholds 0.99/0.95 are unnamed and reconcile with nothing (`compute_effective_n(threshold=0.9999)` — L's :69 cite corrected to **:70**). The DUPLICATION half (six literals, :26≡:45/:32≡:46/:38≡:47 — anchors this seat's own whole-file read; L's :25/:30/:36 killed, K-13) FOLDS → fr-EquationPanel **M-N1**. **→ F.W4** — the one-alphabet/legend decision is design input to the D·D-B3 ramp spec.
- **FR-IC-6 · D-7 / L-3 / C-M4 / C-M5 — MINOR (latent; ruled down from DU's MAJOR — see ruled table); typing half FOLDS → fr-EquationView L·m-7.** The tier contract is unguarded end-to-end (bare `str` + comment at equations.py:28 — four lines below the same file's `pattern=` proof of competence; `(await res.json()) as T` at api.ts:**192** — both L and C cited :191/:228, off by one, K-13; `Record<string,…>`; `strict` without `noUncheckedIndexedAccess` = type-dead/runtime-live `??`), and the fallback renders an unknown tier as the specific false sentence "Coefficients computed numerically via cubic-spline integration…" in red. Live route = the unvalidated `JSON.parse(sessionStorage…)` at useEquationCache.ts:36-41. **→ F.W4** (L·m-7's retype, its cure clause AMENDED: explicit unknown branch — neutral chip, NO mechanism sentence — never a silent spline default) **+ F.W5–W8** (the server-side `Literal` half — the wire-contract family per GAB-12/R6-8).
- **FR-IC-7 · D-20 — MAJOR, FOLDS → fr-EquationView D·D-M10 ∘ fr-GalleryAdminBanner GAB-2 ∘ class fr-BasisSelector M-7 — RE-KEYED to 8.0.0.** The break at `:4` is three stacked breaks, none a specifier rewrite: `./metric-badge` definition-absent at BOTH tags (this seat's json parse: metric keys = `['./metric']`, 74 keys v7 / 70 keys v8); `MetricBadge` the identifier gone; `color` deleted with no carrier at either tag — the `energyColor()` ladder has no destination. Gains survive at v8 (`loading`, `context`, `compact`); geometry renamed TWICE (`labelPosition`→`orientation`→`posture`, GAB-2/K-5); v8's `.metric` is structurally immune to FR-IC-2 (inline-grid, `min-inline-size: 3ch`). D-20's CHANGELOG line cites and 4→7 baseline killed (K-3). **→ F.W1** per the banked rows, budget from 8.0.0 (FMD-11).
- **FR-IC-8 · reader-LC MISS-1 (corroborated by reader-DU's kill-register note) — MAJOR — a CORRECTION TO TWO BANKED ROWS.** **FR-COB-10's `./metric-badge` budget "5 live files + 1 unmountable" is one live file short**, and GAB-2 propagates the undercount. This seat's own census: **7 importing files = 6 live + 1 unmountable** (EquationView, EquationPanel, AnimationControls, EditorControlsDock, GalleryAdminBanner, GalleryDraftsSection + InfoCard) — matching CENSUS C-4 and all three axes. An F.W1 sweep budgeted off the banked row edits 5 files and leaves one live consumer importing a subpath that exists at neither tag — a hard TS2307 under the repo's only gate. **→ F.W1** (budget amendment) **+ F.W0** (substrate note). The registry is wrong and the corpus is right on this row.
- **FR-IC-9 · D-9 / L-8 / C-m1 — MINOR member site, FOLDS → fr-AnimationControls C-28 (GLASS-RELAY, already open) ∘ GAB-5 (member-site precedent).** `cursor-pointer` + focus ring + hover-lift + press-scale unconditional on a read-only readout with no handler/tabindex/role — byte-verified in the chunk by both readers. All three axes filed fresh and proposed a relay the fleet already opened; PRM escalation pre-killed (GAB-5's K-6). Cured free by the uplift (no cursor/hover/press in the v7/v8 metric family — tag reads). D-9's MAJOR is above the banked altitude — ruled to the member-site grade.
- **FR-IC-10 · D-13 / L-6 / C-M3 — MINOR, FOLDS → fr-EquationView C·C-33 → fr-EditorControlsDock D-15/D-16/C-18 — WITH THE CORPUS'S OWN CURE KILLED TWICE (the "cure adjudicated separately from the defect" law).** Prose in the `unit` slot (producer contract: "Unit suffix … e.g. Mbps, ms"); the correct `label` prop silently no-ops without `labelPosition` (compiled guard `!!(labelPosition && (abbreviation || label))` — verified by both readers). Register collision survives RE-BASED per the font ruling (system-face 14px semibold → mono 11px muted → system 14px muted). The prescribed cure (challenge-C:135 `label-position="inline"` + `abbreviation`) is DOOMED twice: (a) reader-DU MISS-4 — the compiled child order emits the label fragment FIRST, so `labelPosition="inline"` **reorders the readout** (annotation-leading, left-aligned, uppercase-tracked) — a typographic redesign sold as a two-attribute rename; (b) reader-LC MISS-3 — `label-position` is a dead prop name at v7 (`orientation`) and again at v8 (`posture`), and `abbreviation` is absent from `MetricProps` at both tags (class banked at GAB-2/K-5; the increment: the corpus's own written cure is one of the doomed writes). Cure constrained by fr-EquationPanel **M-N11** (the label ink is a NEW sub-AA-suspect; must land as ONE edit with the literal repair and rung change). **→ F.W4** (M-N11's one-edit law) **+ F.W1** (prop names from 8.0.0 only).
- **FR-IC-11 · D-8 — MINOR (latent), cure receipt FOLDS → fr-EquationPanel D-9+D-L4 / M-N11.** Pre-formatting to a string guarantees `coalesceMetric` never fires (its empty path is the GOOD path: muted em-dash + colour binding stripped); `null → "0.0"` limb live-in-kind; no loading/error branch (contrast the twin's `v-if="tierInfo"` + the view's computing/error cards). NaN limb dead at the producer (`simplification.py:62` guard) — both axes' own concession, sustained.
- **FR-IC-12 · D-5-as-refiled — MINOR (DU's kill + refile ADOPTED; was MAJOR).** D-5's premise ("red = the error register, only EquationView:243") is killed by its own falsifier: the trig notation pill paints `hsl(6, 72%, 49%)` — red at hue 6°, informational, on the DEFAULT notation, in the same view (notation.ts:15; FunctionInput.vue:223 mounted at EquationView.vue:198 — this seat's own reachability walk). The surviving, sharper row: **one route runs red in THREE unrelated registers** (notation-mode Trig · tier Approximate · computation failure). Cure unchanged (reserve red for `--destructive`; tier ladder as tone-of-confidence) with three collidents. **→ F.W4** (rides the D·D-B3 ramp design).
- **FR-IC-13 · reader-DU MISS-1 — MINOR (new; the FOURTH twin divergence, invisible to a template diff).** The live twin sits under `HoverCardContent`'s `glass-floating`, where ladder.css **unconditionally re-points `--muted-foreground: var(--foreground)`** (read verbatim this seat); InfoCard's host is the decoration-only cartoon shim — no `.glass-*` class, no lift. Same markup, two different muted inks: the twin's unit/description render at `--foreground` (`hsl(24 10% 10%)`), InfoCard's at `--neutral-5` (`hsl(30 22% 40%)`). Load-bearing for BOTH prescribed dispositions: swapping `<InfoCard>` into the hovercard drops an opaque `--card` plate into a glass panel; extracting the twin into this file silently drops the description register from foreground to muted. Not booked anywhere (D·D-M5 enumerates three cosmetics + the computed pair). **→ F.W3** (fourth drift fact riding D·D-M5's F8-REACH-01 execution).
- **FR-IC-14 · reader-DU MISS-3 — MINOR (new).** MetricBadge's glass plate is **chromatically null on every cartoon-card host**: `--glass-bg-quiet/resting` resolve to `var(--card)` at an alpha (recipes read this seat), composited over the shim's opaque `var(--card)` = card exactly. Three unstated consequences: the badge is delineated by border+shadow only (compounding the D-11 height mismatch); the hover fill-step is a guaranteed zero-delta (halving the FR-IC-9 mechanism on cartoon grounds — the false affordance is carried by scale/border/shadow alone); a backdrop-filter pass + stacking context is minted per instance for ~1% brightness over a uniform fill, with the `--metric-badge-blur` off-knob shipped and unused. Also VALIDATES D-4's contrast ground as exact. Registry-swept by the reader (GAB-6 = override fight; COB = cascade order; neither = nullity). **→ NO-WAVE-OWNER** (SS-3/SS-4 spec input) + a factual rider on the standing C-28 GLASS-RELAY.
- **FR-IC-15 · L-5 — MINOR.** `TIER_INFO` + `energyColor` squat in a module whose docblock (:3-8, this seat's read) covers only `NOTATION_OPTIONS`; one real notation consumer vs three tier/energy consumers incl. the false `visualization/` edge (EquationPanel.vue:6). The `tier.ts` split gives M-N1's palette a home. **→ F.W4** (with the M-N1/D·D-B3 repair).
- **FR-IC-16 · the craft tail — D-10 · D-11 · D-12 · D-14 · D-15 — MINOR (each verified; per-row status noted).** D-10 frame==gutter (8px py-2 vs 8px space-y-2 vs 12px px-3; sibling `p-4` contradicts) — orphan-local, branch-conditional. D-11 chip heights 27 vs 24 desktop CONFIRMED, live at the twin; mobile column CORRECTED to **29.5 vs 27** (`text-sm` is Tailwind's own scale, not a glass bridge — LC's kill) and the "asymmetric drift" prose killed (symmetric mismatch — DU's kill). D-12 three border weights, one token; supporting count corrected to **10 utility lines / 7 files** (the cited 24 was a mixed raw-CSS+utility census — K-2); live at the twin (:285). D-14 no live region on a changing value — specific to the always-visible form (the twin's HoverCard announces on open); branch-conditional. D-15 prose defects — `identified`'s dangling antecedent; `spline`'s unquantified truncation while `effective_n`/`term_count` ship unused — live in notation.ts via the twin. **→ F.W3/W4** (the live rows with the twin's repair family; the conditional rows only under a KEEP branch).
- **FR-IC-17 · D-16 — INFO, DISCHARGED-BY-UPLIFT (ruled table).** The shim's inherited hover-lift on a read-only panel ceases to exist at F.W1 (cartoon-surface STATIC at 8.0.0 — fr-FourierMorphSvg M-2, FR-EQC-5/K-7); the prescribed consumer-side carve is dead code and must NOT be scheduled (K-8); the converse silent-loss risk is banked (FR-EQC-13). Recorded, not routed.
- **FR-IC-18 · D-21 — MAJOR, cure FOLDS → D·D-B3's GLASS-RELAY.** The ~57-site tonal-accent re-derivation (M-deep-audit findings-index:352, named sites incl. InfoCard); the same-route sibling already disagrees on proportions (NotationPills 12%/40% vs InfoCard 15%/30% — sharpening the case); untokenised ⇒ unreachable by the token-layer contrast cure — the exact aggravator D·D-B3 books. Falsifier discharged at both pins (no free-tone AA-safe primitive at 4.0.0 or v7/v8). **→ GLASS-RELAY** (the tokenized-ramp ask, already routed) + **F.W4** membership.
- **FR-IC-19 · C-M2-as-rewritten — MINOR; the prescribed cure KILLED (K-10); fact FOLDS → fr-EqCoefficientsPanel FR-EQC-3 ∘ fr-ContourPreview row 4/KILL-6.** The shim is real, self-documenting, and **producer-endorsed as the version-stable form** (v8 docblock strikes the `cartoon` prop in favour of the consumer-written class the shim `@apply`s). Census of record: **21 sites / 14 files** (FR-EQC-3 — not the shim comment's stale 14, not the corpus's raw-occurrence 25). Nothing to migrate to `<Card>`; the shim's stale comment corrects at the FR-EQC-3-sequenced wave. **→ F.W1-sequenced F.W3** per FR-EQC-3.
- **FR-IC-20 · L-7 / C-i1 / C-i2 / C-m3 / C-M1 — INFO (consumption posture; folds and census).** lucide-vue-next runtime-imported from devDependencies, inert for this Dockerfile (`RUN npm ci` at **:11** — L's :10 corrected; folds → fr-EquationPanel C-20); value.js colour half 0-adopted while the installed 0.13.0 ships the contrast arm (C-M1's narrowed claims sustained — the `sampleColorRamp` non-claim is the right discipline; `energy` prop named after the cache, not the wire); keyframes 2 sites repo-wide (house posture; the AnimatedDigit retire-rationale re-verified); bare-root value.js immunity by omission (anti-superlative, sustained); `metric-badge` = 1 of 7 importer files, `lucide-vue-next` = 1 of 35 sites. Census context rides the banked F.W1/F.W2 rows.
- **FR-IC-21 · reader-LC MISS-2 — MINOR (procedural).** All three axes prescribe an InfoCard disposition without citing **F8-REACH-01** and route it to F.W1/F.W4 — while FR-COB-1 routes the structurally identical twin ruling to **F.W0** ("rule F8-REACH-01 and F8-REACH-02 in one breath"). The routing conflict is live and would split one atomic decision across two waves. **→ F.W0** (this record so routes it — FR-IC-1) + a procedural note binding SS-3/SS-4: a disposition claim must sweep the F8 delta table whole.
- **FR-IC-22 · reader-LC MISS-4 — INFO, GLASS-RELAY rider.** Producer comment/implementation drift at 4.0.0: components.css:41-48's rule is `.metric-badge > span` (matches amount, unit, and both label spans) while its own comment asserts "Scoped to the value span" — read verbatim this seat; the stated intent is doubly defeated (`truncate`'s nowrap suppresses wrapping on the value; `shrink-0` exempts the unit). GAB-8 books the consequence for its file, not the selector/comment drift. **→ rider on the standing glass-ui BH relay** (fix the comment or narrow to `.metric-badge__amount`).

## Killed-claims register

| id | Killed claim | Refutation |
|---|---|---|
| **K-1** | **D-5 as filed** — "red for Approximate collides with the app's error register … I find none (EquationView.vue:243 is the only red)" | The falsifier FIRES: the trig notation pill (`hsl(6, 72%, 49%)`, notation.ts:15) renders informational red on the default mode in the same view (FunctionInput.vue:223 ← EquationView.vue:198 — this seat's walk). Refiled at three collidents (FR-IC-12). |
| **K-2** | **D-12's "24 `border-[1.5px]` sites"** (raw-findings.json:1835) | A MIXED census of raw `border: 1.5px` CSS and the Tailwind utility, quoted 3 months stale without re-measurement. Utility-only at HEAD: **10 lines / 7 files** (both readers concur; matches L's own FC-4). The `:22` anchor holds; the "systemic, 24 sites" framing dies. |
| **K-3** | **D-20's producer cites + baseline** — CHANGELOG ":25"/":70-71"; the whole budget keyed 4→7 | At the tag the rows sit at :12 and :57-59 — the axis read producer HEAD's unreleased 8.0.0 CHANGELOG (the STALE-DIST trap). And **v8.0.0 is a real tag** (2026-08-09, this seat's read): the hop is 4→8 (FMD-11; ECD M-10). D-20's conclusions survive re-keyed (FR-IC-7); its arithmetic and planning target do not. |
| **K-4** | **D-2's predicted render** — "the badge renders the unit alone — % energy capture‹clip›" | `justify-content: center` distributes the negative free space equally: the clip is **bilateral** (~2.1px off each side desktop; the leading `%` loses its edge too; no ellipsis anywhere). The headline survives; the picture and the probe spec are corrected (see SS-13 item 1). |
| **K-5** | **D-3's dark-arm deferral** — "not asserted here and is UNPROVEN-NEEDS-LIVE" | Closed arithmetic from the same tokens the light arm used — and challenge-C:77 had already closed it (spline 3.30 ❌). An inconsistent evidentiary standard inside one document that would have cost a probe slot; the dark limb of the probe retires (FR-IC-4 books the fact). |
| **K-6** | **D-11's mechanism prose + mobile column** — "1.5px of asymmetric optical drift"; "on mobile … 30px vs 27px" | The 24px badge centres in the 27px line box — a SYMMETRIC size mismatch; "asymmetric … on both edges" is self-contradictory. And `text-sm` is Tailwind's own scale (glass bridges `--text-small`/`--text-micro`, never `--text-sm`): mobile is **29.5 vs 27**, a 2.5px delta. Desktop 27 vs 24 and the cure stand. |
| **K-7** | **The corpus's corrected adoption figure** — all three axes: "the true live figure is 12 across 7 files" | All three subtracted from the stale ledger instead of counting. This seat's census: 12 real tags / 7 files TOTAL (GalleryAdminBanner:96 is a CSS comment — verified in context); **live = 11 instances / 6 files**. The A-ledger's 13/8 is doubly stale (lists a deleted `EditorToolsPanel.vue`). GAB-5's "6 of the repo's 12" corroborates. |
| **K-8** | **D-16's prescribed cure** — a consumer-side `:not(.cartoon-card--static)` carve | Pre-empted by the uplift: `cartoon-surface` is DE-SPRUNG and STATIC at 8.0.0 (fr-FourierMorphSvg M-2; FR-EQC-5/K-7). The carve becomes dead code at F.W1; the real risk (interactive sites silently losing lift) is already banked (FR-EQC-13). Recorded discharged-by-uplift (FR-IC-17). |
| **K-9** | **The open-decision framing + the proposed new carry** — D §0b's "module-minus-callsite difference pass" to F.W4; L §4's registry join; all three axes presenting deletion as this corpus's call | Already demanded and already banked: **F8-REACH-01** demands the exact three-way ruling; FR-COB-10 §4 banks the census-vs-instance join (routed F.W4); intake R3-7 (ADOPT-AS-FACT) banks the "Unmounted 2" integer, now five-times derived. Nothing here is a new carry; L §4's mirror-class essay is the corpus's best writing and is still a re-derivation. |
| **K-10** | **C-M2's prescribed migration** ("dischargeable NOW at 4.0.0 → `<Card surface="cartoon">`") and D-20's break-table cartoon-card cell ("7.0.0's sanctioned form is `<Card surface='cartoon'>`") | Unspellable at the uplift target: v7's Card carries `cartoon?: boolean` with `surface` defaulting `"glass"` (this seat's tag read); v8 strikes the prop entirely and its docblock endorses the consumer-written `cartoon-surface` — the shim IS the version-stable form. Banked inversion: fr-ContourPreview row 4/KILL-6. The D cell also cites a 4.0.0 artifact as testimony about 7.0.0. |
| **K-11** | **challenge-C §3 S-1's superlative framing** ("the one migration row this file got right") — and reader-DU's retention of it | The credited edit is UNCOMMITTED (this seat's git diff: 1+/1− vs blob `fe451b4`); a stash erases it. Banked precedent exact: GAB-13/K-9 (superlative-credits-the-uncommitted-edit, ruled a substrate finding) + FR-COB-10 (the identical InfoCard edit routed F.W0 as COST). The silent-break mechanism survives as superlative S-2 — method, never merit. |
| **K-12** | **D-13's register description + D-17's CM-Serif rationale** — "fourier remaps the body face to Computer Modern Serif … serif-14-semibold → mono-11 → serif-14" — and reader-DU's confirmation of it | The `--font-sans` remap is dead wiring: **zero application sites** (this seat's grep — declaration :14 + its own comment); body runs `font-serif` → glass bridges → Plus Jakarta Sans, **a sans, and unloaded** (banked FR-AH-2/FR-AH-11: `./styles/fonts` never imported; every brand glyph is a system face while 403KB of CM is fetched and discarded). D-13's collision arm survives re-based; D-17's rationale dies and its probe is re-framed (SS-13 item 5). |
| **K-13** | **The citation-drift cluster** — L-4's palette anchors (:25/:30/:36 = label lines); L's simplification.py:69; L-7's Dockerfile:10; C-M1's vite.config.ts:44; C/L's api.ts:191; D-1's "2 commits" framing beside a dirty hunk | Corrected by whole-file reads this seat and the readers: colours at **:26/:32/:38** (≡ :45/:46/:47), threshold at **:70**, `npm ci` at **:11**, vendor-math at **:57**, the cast at **:192**; the file's third state is the uncommitted hunk (GAB-13 class). True findings, wrong pointers — binds SS-3/SS-4 to re-resolve every anchor before acting (ContourPreview row 31's class; the D-19 lesson). |
| **K-14** *(this seat's, against reader-LC)* | LC's UNPROVEN cell: "the spline tier fails at 3.30 in BOTH arms, **which no axis asserted**" | challenge-C:77's table carries `spline … dark 3.30:1 ❌` — the corpus asserted it; the BANK lacks it (that is FR-IC-4's whole point, and reader-DU's MISS-2 states it correctly). Kills and unproven cells must quote the corpus whole (fr-AdminAuditLog K-11's law). |
| **K-15** *(this seat's, against reader-DU)* | DU's C-M2 fold anchor ("folds to fr-AdminAuditLog AA-4/K-13") and its re-confirmed "25 occurrences / 15 files" shim census | AA-4 is the shim-**adoption** cure row (it prescribes `cartoon-card` as AdminAuditLog's chassis), not the shim-scope identity — that is **FR-EQC-3**, which also already adjudicated the census at **21 application sites / 14 files, "not 14, not 25"**: DU re-published the raw-occurrence instrument the bank explicitly rejected. State the counting unit beside the number (pt-leaf K7's law). Fold re-anchored in FR-IC-19. |

## Superlatives (provenance preserved)

1. **challenge-D S-3 / challenge-L FC-1 — the hypothesis raised and killed against the dependency source, converted into a migration hazard.** `lucide-vue-next@1.0.0 Icon.js:41` auto-emits `aria-hidden="true"` absent slots/a11y-props; InfoCard passes only `class`, so the SVG ships correctly hidden. The rider is the value: **35 call sites silently depend on this default** — the `@lucide/vue` migration must preserve it or ship 35 newly-exposed decorative SVGs. Independently derived by both axes; re-verified by both readers.
2. **challenge-C S-1's silent-break analysis — method, not merit (framing killed at K-11).** Tracing `:amount`→`:value` through the compiled props table rather than the CHANGELOG: no `amount` key → `$attrs` fall-through → `coalesceMetric(undefined)` → muted em-dash with the colour binding stripped, no warning, no type error. Identifying which breaks are loud and which are silent is the core skill of an uplift audit; the fleet keeps the method and refuses the uncommitted edit as its trophy.
3. **challenge-L §2 L-1's negative proof — four probes at three layers, each with its falsifier, plus the dirty-tree caveat discharged** — and now closed a SIXTH way by reader-LC's static dist-build read (the untracked build carries `"% energy"` and zero `"energy captured"`), retiring the corpus's last open reachability probe without a browser. The method praise stands even though the demand it re-derives was already on the record at F8-REACH-01.
4. **challenge-C B-2's method validation** — reproducing the repo's OWN published contrast figures (3.49/4.62 vs the stated ≈3.54/≈4.6) before asserting new ones. Four seats have now converged on the same six ratios to two decimals; instrument calibration before measurement is the standard the megatranche should adopt for every colour row.
5. **challenge-L FC-4 — killing your own finding by measurement** (per-class file counts; CSS delta ≈ 0; JS delta = 0). The same discipline D-12 failed to apply to its 24-site figure — the contrast between the two rows is itself the lesson.
6. **challenge-D S-5 — the icon-gutter construction is correct** (`items-start` + `shrink-0` + `gap-1.5`): both default-mode bugs avoided, and the twin's fork kept all three verbatim while changing everything cosmetic around them — the strongest available evidence they are right.
7. **The component itself, on four counts, all re-verified** (both readers + this seat's whole read): zero `<style>`/`!important`/teardown surface; props read inside computed getters (correct under every Vue version, no 3.5 sugar dependency); 43 LOC against a ~101 directory median; a clean bare-subpath import honouring the contract-v2 precept quoted in-file at vite.config.ts:24-32 — the precept whose violation cost value.js a reversion wave. **The extraction was done well and abandoned at the last step; the failure is in the graph, not the unit** — which is why F8-REACH-01 is genuinely three-way, not a foregone `git rm`.
8. **Both hostile readers' token-layer finds** — reader-DU's MISS-1 (the `--muted-foreground` lift divergence) and MISS-3 (the glass-plate nullity) are closed token arithmetic invisible to template diffs and grep censuses alike; reader-LC's tag-read discipline (every producer claim at `git show v7.0.0:`/`v8.0.0:`, never HEAD) is K-12/BASELINE law working as designed. Recorded so the SS-3/SS-4 authors know where the method bar now sits.

## UNPROVEN-NEEDS-LIVE (SS-13 inputs)

1. **FR-IC-2 pixel witness — only under an ISOLATED_HARNESS/KEEP ruling** (the component never mounts; under DELETE the row retires unmeasured, correctly). Read `.metric-badge__amount` `getBoundingClientRect().width` (predicted **0**) AND the host's `scrollWidth` vs `clientWidth` AND `getComputedStyle(host).justifyContent`, at 375px and 1280px — the clip is bilateral (K-4). Fold FR-IC-14's plate probe into the same mount: computed `.metric-badge` background vs its parent, at rest and on hover (predicted: indistinguishable; hover delta = 0).
2. **The twin's composited backdrop** — the one genuinely live contrast residue: fourier's global `.info-hovercard` override (banked fr-EquationView D·D-M4) × ladder.css's tint/`--muted-foreground` re-point under `.glass-floating` (FR-IC-13). One probe serves both. Light/dark token values coincide (`--popover: var(--card)` light; both `hsl(24 8% 16%)` dark) — the dark-arm arithmetic itself is CLOSED (FR-IC-4), not live. Note the axe gate never visits `/equation` today (2 of 8 specs; the route appears once, visual-baseline.spec.ts:34).
3. **FR-IC-16 chip heights** — one `getBoundingClientRect().height` pair at the twin: predicted 27 vs 24 desktop, **29.5 vs 27** mobile (corrected column).
4. **D-16 residual** — confirmatory only (GAB-5 K-6 already ruled the class; the lift dies at F.W1 regardless). Lowest priority.
5. **D-17 residual optical nudge — re-framed**: the face is a system fallback (K-12), so the empirical question is against `system-ui`/generic serif per FR-AH-2 — and it cannot be answered on a route that does not paint the component. Runs only under a KEEP branch, after the FR-AH-2 font decision lands.
6. **S-1's rendered `aria-hidden`** — one assertion on the emitted `<svg>`, re-run AFTER the `@lucide/vue` migration (35 sites depend on the default).
7. **RETIRED**: D-1's production build-graph probe (closed statically by reader-LC's dist read — FR-IC-1); D-3's dark-arm limb (closed arithmetic — K-5/FR-IC-4).

## Banked-row corrections leaving this record (for the forming specs)

1. **fr-EquationView D·D-B3**: "Dark arm passes" → FALSE for the spline rung (3.30 tinted / 3.85 plain dark). The ramp cure must clear AA in BOTH arms (FR-IC-4).
2. **fr-CanvasOverlayButton FR-COB-10** (propagated by fr-GalleryAdminBanner GAB-2): `./metric-badge` budget "5 live files + 1 unmountable" → **6 live + 1 unmountable = 7 importing files** (FR-IC-8).
3. **fr-EquationView D·D-M5's drift enumeration** gains a fourth, non-cosmetic divergence: the `.glass-floating` `--muted-foreground` lift (FR-IC-13).
4. Procedural, binding SS-3/SS-4: disposition claims must sweep the F8 delta table WHOLE (FR-IC-21); re-resolve every corpus anchor before acting (K-13); state the counting unit beside every census number (K-15).

## Closing verdict

**DEFECTIVE-AND-DEAD; disposition = execute the standing F8-REACH-01 demand at F.W0, ruled in one breath with F8-REACH-02 (ADJUDICATED, routed, not stamped).** The corpus's three BLOCKER filings are one already-banked demand wearing three axis ids; its one genuinely novel defect (FR-IC-2) is the sharpest evidence that KEEP-as-is is not a branch; its best work is negative proof and token arithmetic; and its two banked-row corrections (D·D-B3's dark arm; FR-COB-10's metric budget) are worth more to F.W1/F.W4 than any of its re-filings. Both hostile readers survive the incredulity seat with corrections that sharpen rather than reverse — reader-LC wins the three cure-level rulings (fonts, the uncommitted superlative, the Card migration), reader-DU wins the two fact-level ones (D-2's grade, the dark-arm bank correction), and each needed one kill of its own (K-14, K-15). Nothing lost: head (the orphan finally routed to the wave that owns its ruling), tail (the unit's craft preserved through whatever branch F.W0 rules), or interval (every killed claim retired with its refutation on the record).

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
