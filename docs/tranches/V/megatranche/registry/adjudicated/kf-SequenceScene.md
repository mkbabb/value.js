Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component SequenceScene; receipt read from this seat's own system prompt, not inherited).

# kf-SequenceScene — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c`, `origin/master` `81a56990`; **`git diff --stat origin/master -- demo/scenes/sequence/` → EMPTY this session** (every scene citation binds to origin/master content; the worktree drift lives elsewhere and includes the uncommitted `package.json` glass-ui deletion that killed three axis rows below) · **Registry tree** value.js `tranche-u` · **Corpus**: three challenge axes (`audit/kf-components/SequenceScene/challenge-{D-design,L-library,C-consumption}.md` — D: 26 defects/2 BLOCKER/7 superlatives · L: 13 defects/1 BLOCKER/6 superlatives · C: 11 defects/1 BLOCKER/4 superlatives) re-read whole by this seat, plus two fresh hostile re-reader returns consumed whole: **reader-A** (design-and-usage: 30 confirmed · 6 kills · 2 misses · 12 superlatives · 6 unproven) and **reader-B** (library-and-code: 30 confirmed · 12 kills · 6 misses · 8 superlatives · 9 unproven).

**Scope note (identity).** The 43-line SFC is a provider shell; both the axes and the readers correctly treat the **mounted subtree** (SequenceTarget + SequenceScrubber + useSequenceDemo + the closure) as the unit. The banked kf-SequenceAxis record relays two findings "to the forthcoming kf-SequenceTarget corpus" — **that relay consummates HERE** (D10, and the valuetext family's Target limb); no separate kf-SequenceTarget record will exist.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every load-bearing cell was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of SequenceScene.vue (43 L), useSequenceDemo.ts (483 L), SequenceTarget.vue (253 L), SequenceTarget.css (259 L), SequenceScrubber.vue (162 L), the three challenge files, both re-reader returns; targeted reads of useSweepScene.ts:40-125 (the gate binding at :119, `SweepSceneOptions` seam), useSceneMachineShellBinding.ts (:85-95 placeholder group, :155-170/:210-220 `facility.identity` reads + the `scenePlayback` comments, :258-268 the isStarted write, :280-290 TAB status-only), controlOptionsStore.ts:40-100 (the mint-on-read getter), controlSurfaces.ts (:1-12 inversion record, :85-125 `surfacesFor` incl. the line-88 JSDoc prose, :165-185 `SCENE_DEFAULT_CONTROL` — no sequence entry), scenePlaybackAdapters.ts:205-225 (`isPlaying = getPlaying() && isLoopRunning()`), lifecycle.ts:50-120 (play seeds `startTime=at/started/managed`; pause retains; resume re-seeds nothing), transport.ts:260-280 (`settleSequence` releases ownership, stops no child loop), play-lifecycle.ts:362-370 (the managed throw) + :460-468 (standalone settle clears `startTime`), useControlsKeyboardShortcuts.ts:51-52 + AnimationControlsGroup.vue:302-303 (Escape/R → `useAnimationGroupActions({getGroup})`), usePlaybackToggle.ts:28; **my own probes**: `git show origin/master:package.json` → `:77 "@mkbabb/glass-ui": "7.0.0"` exact devDep, lock → 3 entries, worktree 0/0 (the L-1 decider); the `demo.<member>` census over the exhaustive injector set (`grep SEQUENCE_DEMO_KEY` → provide :19 + exactly two injects) → **18 distinct consumed members**, none of `reverse/setTimeScale/timeScale/reset/play/pause/togglePlay/resume/delays/scenePlayback`; `isReversed` writers → :266/:290 only, both inside uncalled verbs (the D19(b)/SC-2 decider); the return-literal count → **28 members** (the "24" both readers repeat is the challenger's, and reader-B's own 18+10 sums to 28); a node WCAG run (my arithmetic): on-glass-muted light opaque **6.292**, α=0.8 composite **3.983** bare / **3.816** over the 5% stage tint, dark **10.234**/**6.784** (the D2 decider, concordant with reader-B and banked kf-SequenceAxis ruling 1 to 3 d.p.); installed glass-ui 7.0.0 — button/styles.css read whole (base `border: 0` + `min-block-size: var(--button-size)`; the tone-gated `.button:not([data-tone="neutral"]) { border: 1px solid … }`; the `[data-icon-only]` trio), Button.vue.d.ts (EIGHT props incl. `iconOnly`), tokens/sizing.css (`--control-h-md: max(2.5rem·--ui-scale, --control-floor)` = 40px fine) + tokens/light-dark.css (coarse → 60px), glass/ladder.css (`:where(.glass-card,.glass-resting,.glass-quiet,.glass-wash){--muted-foreground: var(--on-glass-muted)}` + the `@container style(--glass-backdrop: light)` and `contrast-color()` arms), on-glass-fg.css/dark-arm.css token values verbatim, typography semantic.css (`text-caption` italic Jakarta; `text-admin-label` mono/upper/0.1em/500) + utilities.css (`text-mono-caption` uppercase) + scale.css (display-1/display-3 clamps → **exactly φ** at 1440, both saturated), metric/styles.css (`[data-size="xl"] → --type-display-3`; `min-inline-size: 3ch` + tabular — the K1 kill reproduced); `wc -l design-idioms.css` → **300**, `--ball-glow` read at **:185** (the SC-4 decider); registry sweeps — `playReel|isReeling|Clapperboard|reel-active` → 0, `isControlsPanelOpen|isLoopRunning` → 0, `useSceneVisibilityPause` → kf-AmigaScene (superlative only) + kf-EasingScene (KF-ES-1, the onArm phase inversion — a DIFFERENT mechanism), and the banked ids KF-ES-16/KF-ES-18/KF-APP-4/kf-CubeScene D-11/kf-ControlsPaneWrapper D-M11·D-m12/kf-KeyframeTimeline D-8 all verified present.

## Scoreboard

**37 roster rows booked here — 6 MAJOR · 22 MINOR · 9 INFO — plus 10 fold-groups by identity to banked homes (no row). Killed-claims register: 14 entries, including two kills of reader cells that the OTHER reader got right. 8 net-new rows admitted from the readers' 8 filed misses (SC-1..SC-8), every one re-derived at the bytes. 10 superlatives. 12 UNPROVEN residues → SS-13 (one banked SS-13 witness re-framed).**

**Headline.** Three results. (1) **The strongest live finding is fresh and structural: the tab-visibility gate protects the read-out mirror, not the motion loop (L-3)** — `useSweepScene` hard-wires `useSceneVisibilityPause` to its own RAFPlayback because `SweepSceneOptions` has no seam for "the sweep is a mirror, the motion is elsewhere"; the same missing parameter yields the discarded second adapter (L-10) and a `ScenePlayback.isPlaying()` that reads the wrong loop (SC-5). Hide the tab >~2 s mid-play and the storyboard silently runs to the end. (2) **The incredulity seat cut against BOTH readers**: reader-A re-derived D2's contrast on the `--neutral-5` chain the bank had explicitly warned against two records earlier (the FOURTH citation-inheritance event of that exact chain in this scene's cluster — killed, my own arithmetic siding with reader-B at 3.98/3.82), and confirmed D19(b)'s reachability that a verb census refutes; reader-B confirmed D22's "28×28" geometry that the installed producer's `min-block-size` floors at 28×40/28×60 (reader-A's kill wins), and both readers repeated the challenger's "24-member bag" against a return literal that counts 28 — reader-B's own 18-consumed + 10-unconsumed arithmetic contradicting the cell it sits beside. (3) **The F-1 restatement law fired its sixth, seventh and eighth times in one corpus**: all three axes "independently re-verified" the glass-ui phantom dep against a dirty worktree with no baseline named; `origin/master` pins `7.0.0` exact with a full lock entry. Both readers killed it concordantly; my own probes re-confirm. The best net-new work is reader-B's verb-reachability census (SC-2, MAJOR): three of the six advertised transport verbs — `reverse`, `setTimeScale`, `reset` — have **zero call sites**, so the composable's own F.W9 contract prose is false for two of them, the headline drag gesture has **no undo**, and banked K-24's Reset mitigation at kf-SequencePlayhead rests on an uncallable function.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census taxonomy (KF.W0/W6/W7/W9) with **NO-WAVE-OWNER** for the sequence scene-repair packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D2's arithmetic: reader-A confirms at 3.47:1 via `--muted-foreground = --neutral-5`; reader-B kills that chain (`--on-glass-muted` inside the glass Card) and re-derives 3.98/3.82 light · 6.78 dark** | **B WINS — A's every published cell (5.21/3.47/3.37/5.23) is KILLED; the substance survives at MAJOR** | My own run against the installed 7.0.0: ladder.css `:where(.glass-card,.glass-resting,…)` rebinds `--muted-foreground → --on-glass-muted` (hsl(30 26% 35%) light / hsl(34 16% 72%) dark); opaque **6.292/10.234** (reproducing banked kf-SequenceAxis ruling 1 to 3 d.p.), α=0.8 composite **3.983** bare / **3.816** over the 5% stage tint (both < 4.5, light FAILS) and **6.784** dark (passes). Banked kf-SequenceAxis SS-13 #3 states verbatim: "any label re-measure must use the corrected chain … never `--neutral-5`" — reader-A re-ran the arithmetic on the banned chain anyway: **citation-inheritance event #4 of this exact class in the sequence cluster** (after D's S-D, reader-DU, and the axis corpus's LC cell). Grade: MAJOR, not BLOCKER — one sub-label, one arm, one-line cure (`delete opacity: 0.8`), and this cluster has killed BLOCKER re-grades three times (K-6, K-31, and ruling 4 below). |
| 2 | **D19(b) reverse-while-paused: reader-A confirms it reachable ("`reverse()` at :271 only dispatches PLAY when `isMidPlay()`"); reader-B kills it as unreachable dead code** | **B WINS — the limb is KILLED; D19(a) stands MINOR; the real charge is SC-2** | My census over the exhaustive injector set (provide :19; injects Target:150 + Scrubber:47 only): 18 distinct `demo.*` members consumed, `demo.reverse` not among them. `isReversed` is written ONLY at useSequenceDemo.ts:266 and :290 — both inside verbs (`reverse`, `reset`) that nothing calls. The channel path cannot reach it either: usePlaybackToggle.ts:28 calls `getAnimation().reverse()` on a channel `animation` this scene's lone channel does not carry (:424-433). `isReversed` is permanently `false`; the `reverse-badge` ternary arm (Target:40-41) is dead code. Reader-A's falsifier cell — D's own offered falsifier, "a state matrix showing reverse-while-paused is unreachable" — is satisfied and A did not run it. |
| 3 | **D22: reader-A kills the 28×28 geometry (producer `min-block-size` floors it); reader-B confirms "h-7 w-7 p-0 = 28×28" at MINOR** | **A WINS — B's cell is KILLED; the row is RESCOPED (width limb + conformance limb survive, MINOR)** | My read of installed button/styles.css: base `.button` carries `min-block-size: var(--button-size)`, `--button-size: var(--control-h-md)` = `max(2.5rem·--ui-scale, --control-floor)` = **40px** fine / **60px** coarse (`--ui-scale: 1.5`, `--control-floor: 44px` under `pointer: coarse`); Tailwind's `h-7` sets `height`, a different property — no cascade contest. The element renders **28×40 / 28×60**, never 28×28. The MECHANISM folds ≡ banked kf-KeyframeTimeline **D-8** + kf-ControlsPaneWrapper **D-m12** (both verified present). Survives here: the WIDTH axis (28px against the repo's own `.tap-floor` 44px, design-idioms.css:81-85) + the conformance limb (the scene hand-sizes a design-system primitive with utilities where the shipped `iconOnly` prop — verified in Button.vue.d.ts, with the `[data-icon-only]` sizing trio — exists for exactly this). D22's own falsifier was half-satisfied: a min-height surviving `p-0` DOES exist; it floors at 40, not ≥44. |
| 4 | **The three BLOCKER claims (L-1 phantom dep · C-1 stale duration · D1 origin split) and the D4 MAJOR re-grade** | **ALL KILLED — two by baseline law, two by booking/grade stability** | L-1/C-11/D§6: the 0/0 greps read the DIRTY WORKTREE; `git show origin/master:package.json:77` → `"@mkbabb/glass-ui": "7.0.0"` exact, lock 3 entries (my probes) — folds ≡ census **SCH-1** / kf-App ruling 1; kf-AmigaScene C-1 is the recorded precedent and this corpus is re-inheritance events six-through-eight. C-1: the mechanism is TRUE and banked as **N-2** (kf-SequencePlayhead:44, MAJOR, five prior derivations incl. the identical `End`-keypress witness) — the BLOCKER arrives with zero new evidence and dies under the same law that killed it at K-6/K-31. D1: ≡ banked **D-1/L-1/C-1** (MAJOR, ruling-1 cure `gap: 0.5rem var(--col-gap)`); the BLOCKER grade is the third-generation re-contest, killed. D4: banked twice (kf-CubeScene **D-11** MINOR, kf-ControlsPaneWrapper **D-M11**); the 8-site count matches the banked census exactly — nothing net-new to grade upward. |
| 5 | **L-2's visual cell ("the ball rests at 37.8% of its rail, forever") vs reader-B's kill** | **KILL SUSTAINED — and banked SS-13 #11's photograph brief is RE-FRAMED** | Re-derived at the bytes: `.seq-ball`'s transform is `(--row-start + --ball-p·(1 − --row-start)) · (100cqw − --ball-size)` (css:194-199) with `--row-start = clamp(at/1600, 0, 1)` (Target:81). At the very `at = 1600` the claim uses, `--row-start = 1` collapses the bracket to 1 for every `--ball-p` — the ball is PINNED at the rail's right end from t=0. And 340/900 is a local-TIME fraction fed through the spring-eased 0/0.7/1 keyframe fan, not a rail fraction. The stranding is real (N-2 stands); it manifests in opacity/scale-pop/bloom, not horizontal position. **Consequence**: SS-13 #11's banked witness ("photograph the lane frozen ≈38% mid-glide") will not show what it expects at `at = 1600` — re-shoot at a MID-range `at` (e.g. 1200, `--row-start = 0.75`). |
| 6 | **The 8 filed misses (A: 2 · B: 6)** | **ALL 8 ADMITTED (SC-1..SC-8) — one at MAJOR, with cell refinements both ways** | Every one re-derived: SC-1 (mint-on-read getter — controlOptionsStore.ts:76-84 writes a full persisted bucket on the READ at SequenceScene.vue:30, so the three axes' delete-line-31 cure is structurally incomplete; `defaultControlSurfaceFor("sequence")` → `"controls"` fallback verified, no `SCENE_DEFAULT_CONTROL` entry); SC-2 (the verb census — ruling 2's instrument, plus Escape/R binding to `useAnimationGroupActions({getGroup})` over the shell's EMPTY placeholder group for facility-only scenes, useSceneMachineShellBinding.ts:92-94 — the labelled Reset resets nothing while its play-state emission still pauses); SC-3 (the reel's one-sided guard: `playReel` blocks only a second reel; a machine-routed PLAY in the 360ms window re-seeds every child `managed = true` (lifecycle.ts:57-61) while pending timeouts strip `managed` and start standalone loops — the engine's own throw (play-lifecycle.ts:364-368) is defeated because the reel clears the flag first, so the double-writer collision is silent); SC-4 (the phantom cite: Scrubber:145 names `design-idioms.css:584`; the file is **300 lines**, `--ball-glow` reads at **:185** — my wc/grep); SC-5 (`isLoopRunning: () => mirror.running` at :410 under a docblock claiming "the single suspend path — no orphan rAF", while the one orphanable loop, `sequence.playback`, is invisible to the adapter's `isPlaying()` conjunction — scenePlaybackAdapters.ts:216 read); SC-6 (three byte-similar `setProgress` bodies at :199-202/:402-405/:427-431, `getPlaying` twice, one object published under three names — the ambiguity L-6/C-5's drift trips over); SC-7 (`.reel-active`'s `border-color` paints NOTHING: installed Button base `border: 0`, the only border rule tone-gated `:not([data-tone="neutral"])`, and the scene passes no tone — the designed active ring is silently absent, a `color` shift on a 14px glyph is all that remains); SC-8 (the 44px handle host with `margin-left: -22px` crosses the column boundary at `at = 0` — the crossing is gutter-branch-INDEPENDENT since 22px exceeds the 12px maximum gutter, though the overlap depth (10 vs 22px) rides SS-13 #1; the transparent host hit-tests above the earlier static label, so the row's own `@0MS` caption becomes an unmarked drag surface; refines A's cell, which stated the 22px figure without the branch caveat). |
| 7 | **Severity echoes: reader-B's confirmed fields carry the challenger's grades (D3/D5/D9 MAJOR, D2 BLOCKER, D4 MAJOR…) where reader-A re-graded** | **Reader-A's re-grades ADOPTED wholesale — restraint with reasons beats echo** | D3 → MINOR (the ~18.5px overflow is absorbed by the stage's 16px padding and crosses the 1px border by ~2.5px @1440 — A's derivation); D5 → MINOR (a one-class swap on a 15-character eyebrow; the weight contest → SS-13); D9 → MINOR (discoverability-only, no WCAG failure, D's own concession); D21 → INFO (both centring utilities no-ops, `gap-4` dead against a single child); D24 → INFO (function refs on a static `v-for` resolve during patch, before `onMounted` — the null branch is practically unreachable; the surviving charge is the absent degraded-state posture, and `Metric.loading` exists unused, verified in the dist d.ts); L-9 → INFO (the demo binds no child animation event — my grep concurs; promotes if a consumer ever binds `animationend`; B's MINOR preserved as dissent). D6 stays MAJOR on the fresh φ-rung limb (my scale.css derivation: both clamps saturate at 1440 → 67.776/41.888 = **1.618 exactly**); the duplicate-exposure limb folds ≡ banked N-14's rider. |

**Adjudicator's corrections register.** Against reader-A: the D2 chain (ruling 1, killed); the D19(b) reachability cell (ruling 2, killed); credits — the D22 kill (ruling 3, decisive), the Button d.ts eight-prop correction, both misses verified (SC-7/SC-8, with SC-8's branch caveat added by this seat). Against reader-B: the D22 confirmed cell (ruling 3, killed — inherited geometry while its neighbor checked the producer); the "24-member bag" repetition its own arithmetic contradicts (the count is **28**; the same cell also passed through reader-A's L-12/C-8 row); severity echoes (ruling 7); credits — the D2-CHAIN kill (my arithmetic concordant to 3 d.p.), the L-2 visual-cell kill and its SS-13 re-frame, the D19(b)/D23-mechanism/`controlSurfaces.ts:88` kills, and all six misses verified incl. the MAJOR SC-2. Against the corpus: the "24-member" cell originates in challenge-C §C-8 and survived two hostile readers — countable cells are the cheapest inheritance vector this ledger keeps proving.

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the sequence scene-repair packet). Anchors bind to `origin/master` `81a56990`; re-resolve before any cure.

### MAJOR

- **L-3** *(FRESH — the corpus's strongest live finding)* — **the tab-visibility gate protects the mirror, not the motion**: useSweepScene.ts:119 binds `useSceneVisibilityPause` to its OWN RAFPlayback (the read-out mirror); useSequenceDemo.ts:188-192 destructures only the mirror verbs, so `sequence.playback` reaches no gate; the machine deliberately declines (TAB_HIDDEN/TAB_SHOWN status-only — shell binding :280-284 verbatim, sceneMachine parks `suspended`); RAFPlayback passes raw rAF timestamps with no dt clamp; `driveSequenceFrame` settles on `rawMaster ≥ 1940` — so the first visible frame after a >~2s hide finishes the run, resolves the play promise, and the `finally` (:231-239) dispatches PAUSE. Root cause: `SweepSceneOptions` (:43-64) has no seam for supplying the loop verbs — the same missing parameter under L-10 and SC-5. Registry-swept fresh (the banked `useSceneVisibilityPause` rows are kf-EasingScene KF-ES-1's onArm phase inversion and kf-AmigaScene's honesty-contract superlative — different mechanisms). Witness → SS-13. **NO-WAVE-OWNER (one seam parameter; cures L-3 + L-10 + SC-5 together).**
- **SC-2** *(NET-NEW — reader-B M-2, ruling 6)* — **three of the six advertised transport verbs are unreachable from any affordance**: `reverse`, `setTimeScale`, `reset` have zero call sites (my census: 18 of the 28 returned members consumed; the module docblock's F.W9 claim "play / pause / … / reverse / timeScale / progress-scrub" is false for two verbs); the headline row-drag gesture has **NO undo** (`reset()` is the only path restoring DEFAULT_DELAYS); banked **K-24**'s mitigation ("Reset restores the frozen 1940 coincidentally") and SS-13 #11's "then Reset" probe rest on an uncallable function; the app-wide Escape/R "Reset animation" shortcuts bind to an EMPTY placeholder AnimationGroup for this facility-only scene (shell :92-94) — resetting nothing while still pausing. Consequences already adjudicated under it: D19(b) dead branch (ruling 2). **NO-WAVE-OWNER (transport-surface decision: expose the verbs or delete them + their state; the engine-side rider joins banked N-2's).**
- **D2** *(arithmetic corrected — ruling 1)* — `.seq-row-at`'s `opacity: 0.8` drops the per-row `@…ms` sub-label to **≈3.82-3.98:1 in the light arm** (corrected `--on-glass-muted` chain; dark passes 6.78) — WCAG 1.4.3 AA failure on the storyboard's only per-row datum, at 14.38px/400 (no large-text exemption). One-line cure: delete `opacity: 0.8` (the weight-400/600 delta vs `.seq-row-name` already carries the subordination, as the file's own :95-99 comment argues). ADJACENCY: kf-SequenceAxis **SA-1** charges the same declaration as a HIERARCHY datum; the contrast limb is this record's; both cures point the same way. **NO-WAVE-OWNER (one commit with SA-1).**
- **D6** *(rung limb fresh; duplicate limb ≡ banked N-14 rider)* — the header `Metric size="xl"` renders at **exactly φ** the scene title (67.776/41.888 @1440, both clamps saturated — my derivation) — the φ ladder respected, the hierarchy inverted: the least durable datum (a re-derivable %) is the largest thing on the card, forcing an ~88px header bar, while duplicating the scrubber timecode (`(p·100).toFixed(0)%` vs `p.toFixed(3)` — the THIRD normalized exposure, banked N-14 rider). D's K1 kill (no digit jitter — `min-inline-size: 3ch` + tabular, reproduced by this seat) stands. **NO-WAVE-OWNER (header hierarchy decision).**
- **D7** *(FRESH)* — **PRM is honoured for the 700ms boot only; the reel's under-damped overshoot cascade has no gate, 30 lines from the working example**: useSequenceInstrument.ts:29-32 is the correct JS guard; `playReel` (:364-392, `dampingFraction: 0.34` ×5 at 90ms) has no branch; the CSS arm reaches only the boot (css:238-243); the engine will not rescue it (`respectReducedMotion` opt-in, default false); reachable from the visible Button AND the typed trigger (whose editable-target skip does not block a focused `role="slider"` div). The essential motion is correctly NOT suppressible (credited at superlative 9); the charge is the file's own decorative/essential boundary, drawn once and not re-applied. Registry-swept fresh. **NO-WAVE-OWNER (3-line cure mirroring the instrument guard).**
- **D8** *(mechanism proven; threshold → SS-13)* — every Card child is `shrink-0` inside `h-fit max-h-full overflow-hidden` with no `overflow-y` anywhere in the chain: a short cell silently clips the LAST child — the master scrubber, the card's only in-content transport surface (the derived DFA set is empty) — with no scrollbar, no cue, no fallback. The `controlSurfaces.ts:88` citation cell is corrected (killed-claims #7): line 88 is JSDoc prose inside `surfacesFor`, not a table row; the `[]` arrives via `?? []` over a channel that declares no `surfaces` at all (L-11's reading). **NO-WAVE-OWNER (overflow posture decision).**

### MINOR

- **D3** *(grade ruled 7)* — `--label-col: 3.25rem` cannot contain its own default content: `@1040MS` ≈ 70.5px vs the 52px track (0.6em Fira advance + 0.1em caps tracking at 14.38px, 7 glyphs), overflowing the START edge past `align-items: flex-end`; absorbed by the stage's 16px padding to a ~2.5px border-crossing @1440 (~7px @1920). Row 5 is `@1040ms` at first paint. Exact overflow → SS-13. **NO-WAVE-OWNER (one token with the packet's geometry commit).**
- **D5** *(grade ruled 7)* — the scrubber eyebrow renders **italic Plus Jakarta Sans** (installed `text-caption`: `font-style: italic`, `--font-text`) under a comment claiming Fira Code, with `letter-spacing: 0.18em` = 1.8× the system's `--type-tracking-caps`; `text-admin-label` — the shipped idiom for exactly this — is in use two files away (Target:39). One-class swap. The `font-medium`-vs-utility weight contest is genuinely unresolved from source → SS-13. **NO-WAVE-OWNER (with the S-4 fold).**
- **D9** *(grade ruled 7; falsifier cell corrected)* — the reel button is icon-only with no visible/hover name — defeating its stated purpose as "the discoverable twin"; no `title`, no `<Tooltip>` (the demo's 9-file idiom), no caption. NOT a WCAG charge (aria-label present). Corrected cell: Button.vue.d.ts exposes EIGHT props (incl. `iconOnly`, `loading`) — not one; the conclusion survives, the enumeration did not. **D22 (rescoped, ruling 3) rides here**: 28px WIDE against the repo's own `.tap-floor` 44px (height floored at 40/60 by the producer — mechanism ≡ banked kf-KeyframeTimeline D-8/kf-ControlsPaneWrapper D-m12, folded), and the `iconOnly` prop unused where it is the designed affordance. **NO-WAVE-OWNER (one button, three limbs: name, width, prop conformance — plus SC-7's dead ring).**
- **D10** *(the kf-SequenceAxis D-12 relay, consummated here)* — `text-mono-caption`'s `text-transform: uppercase` renders the SI unit as `@1040MS` (`.seq-row-at` resets weight/opacity, never the transform); distinct from banked kf-SequenceAxis D-7 where the same utility is INERT on pure numerals — here it corrupts a unit. **NO-WAVE-OWNER (one-line: `text-transform: none` on `.seq-row-at`, or move `ms` out of the span).**
- **D11** *(scoping limb fresh; census limb banked)* — `@property --ball-p` inside a `<style scoped>` block registers a **document-global**, inheriting, un-namespaced token (Vue's scoped transform rewrites selectors, not at-rules). The flat-namespace census folds ≡ kf-SequenceAxis D-10/L-11 (16 authored properties, 1 prefixed) + kf-SequencePlayhead D-11/L-6/C-7. **NO-WAVE-OWNER (property-hygiene family).**
- **D15** *(sharpened, with a cell this seat adds)* — two sliders in one card, two focus affordances: `.seq-handle:focus-visible` RE-AUTHORS the `.focus-ring` contract inline; `.seq-scrub` has no focus rule at all. D's K2 downgrade (UA ring still paints; all 13 `outline: none` are class-scoped) sound. **This seat's addition**: the re-author is DOCUMENTED (css:160-162 — the `class="seq-handle"` source contract is read by a `proof:sequence-rows-draggable` static clause), so the cure must respect or retire that clause; the scrubber's omission has no such excuse. **NO-WAVE-OWNER.**
- **D16** — the row-handle focus ring outlines the 44×track-height transparent host, not the 6.4×24 grip it names (ring on the host :163-167; grip a `pointer-events: none` ::after). WCAG 2.4.11 satisfied; the indicator misrepresents the target. SC-8 is the interaction consequence of the same host. **NO-WAVE-OWNER.**
- **D19(a)** *(limb (b) killed — ruling 2)* — badge salience inverted for a transport axis: the violet accent (`settled-badge → --color-progress`) lands on idle "ready", the neutral grey (`tracking-badge`) on live "playing" — the pair reused from arrival-semantics scenes it was authored for. **NO-WAVE-OWNER (with the SC-2 transport decision — the dead `reverse` arm dies with it).**
- **D20** — the status badge (the card's only textual transport-state surface) carries no `role="status"`/`aria-live`; play/pause/natural-end all change it silently. Correctly scoped: `progress` itself must NOT be live-regioned. Family-adjacent to banked N-14 (distinct surface — the badge, not the sliders). **KF.W9 datum + NO-WAVE-OWNER (one attribute).**
- **D23 / L-4 / C-3** *(+ SC-1; the per-mount-write cell killed — #8)* — `storedControls.isControlsPanelOpen = false` is the last survivor of the extirpated poke-set (ChromeDock names it superseded by id; Easing/Spring carry tombstones), and it is inert (`--closed` and `--railless` share the identical `--rail-track: 0px`; ControlsPaneWrapper never mounts). The write is identity-suppressed after first entry (L-4's cell, correct). **The complete cure is BOTH lines** — see SC-1. **NO-WAVE-OWNER.**
- **SC-1** *(NET-NEW — reader-B M-1)* — the GETTER on line 30 mints and persists a whole control-options bucket on the READ (`controlOptionsStore.ts:76-84`, `useStorage`-backed, seeding `selectedControl: "controls"` via the no-entry fallback) for a scene whose panel can never render. All three axes prescribed deleting line 31 only. Registry-swept: 0 banked rows on the mint-on-read side effect. **NO-WAVE-OWNER (delete both lines).**
- **SC-3** *(NET-NEW — reader-B M-3)* — the reel's re-entrancy guard is one-sided: a machine-routed PLAY inside the ~1.26s reel window steals children out of Sequence ownership mid-claim, double-writes the same elements from two loops, and swaps the master's shared easing for the under-damped `reelOvershoot` until the `finally` restores it — silently, because the reel clears `managed` before the engine's own throw can fire (the very protocol superlative S+6 credits). Self-healing; percept → SS-13. **NO-WAVE-OWNER (guard the transport during `isReeling`).**
- **SC-4** *(NET-NEW — reader-B M-4)* — a phantom line citation inside the one comment the design axis elevated to a superlative: Scrubber:145 cites `design-idioms.css:584` in a **300-line** file (`--ball-glow` lives at :185). Joins the banked prose-truth family (kf-SequencePlayhead D-4/D-5/L-7/N-19 — the checklist grows again); the direct support under D's S4 superlative, which is demoted accordingly (killed-claims #13). **NO-WAVE-OWNER (prose sweep).**
- **SC-7** *(NET-NEW — reader-A M1)* — `.reel-active`'s `border-color: var(--color-progress)` is inert: installed glass Button ships `border: 0` base and grants a border only via the tone gate `:not([data-tone="neutral"])`; the scene passes `emphasis="secondary"` and no tone. Half the designed active cue does not exist; what remains is a `color` shift on a 14px stroke glyph for ~1.26s. Registry-swept clean (the banked glass-Button families are the phantom class and the height floor — different mechanisms). **NO-WAVE-OWNER (with the D9 button packet; pass a tone or paint the ring locally).**
- **SC-8** *(NET-NEW — reader-A M2, branch-qualified by this seat)* — the 44px handle host (`margin-left: -22px`) crosses the column boundary at `at = 0`: crossing certain under every subgrid-gutter branch (22px > the 12px maximum gutter), depth 10-22px branch-dependent (→ SS-13 #1); the transparent host paints in the positioned phase above the static label, so pressing the row's own `@0MS` caption begins `reseatRow` and the caption cannot be text-selected. Every row reaches `at = 0` via Home or a full-left drag. The one axis the file's otherwise-excellent 44px reasoning (S6) did not consider. **NO-WAVE-OWNER (with the interaction spec ≡ banked N-12's packet).**
- **L-6 / C-5** — the `defineExpose` docblock documents a `scenePlayback` member the expose object does not contain (`{facility, superKey, isStarted}`); the shell's own comments (:160, :216) repeat the phantom while its code reads `facility.identity` — bilateral drift, no surviving correct account of the readiness seam. Compounded by SC-6's three-name publication. **NO-WAVE-OWNER (prose sweep + the SC-6 alias decision).**
- **L-7 / C-6** *(class ≡ banked KF-ES-18 + the KF-ES-16 rider)* — `isStarted: ref(true)` is a write-only sink CONSTRUCTED INLINE in the expose literal (no local handle — structurally unreadable from inside the scene), typed `Ref<boolean>` against the contract's `boolean`. The inline degeneracy is this file's own; the fleet class and the sceneExposedApi.ts:33 mismatch are banked. **NO-WAVE-OWNER.**
- **L-8** *(playReel limb fresh; instrument limb ≡ banked SA-5)* — five unretained `setTimeout`s + up to five detached child RAFPlayback loops outlive unmount: teardown is `stopMirror(); sequence.stop()`, and `settleSequence` releases ownership without stopping child loops (transport.ts:268-279, my read). Bounded ~1.26s, self-terminating. **NO-WAVE-OWNER (retain + clear handles; with SC-3).**
- **L-10** — `useSweepScene` already builds a fully-wired `ScenePlayback`; the demo discards it and hand-builds a second (`createRafAdapter` ×2 per mount). Shares L-3's root: no seam in `SweepSceneOptions` for external loop verbs. **NO-WAVE-OWNER (dies with the L-3 cure).**
- **L-11 / C-4** *(class banked; both axes under-scope it)* — the docblock cites `CONTROL_SURFACES.sequence = []` as "the AUTHORITY"; the table was deleted at T.B2 (controlSurfaces.ts:1-12 records it). `grep CONTROL_SURFACES demo/` → 4 hits, ALL prose — **EasingScene.vue:24 and SpringScene.vue:35 carry the same dead cite**: a THREE-instance fleet prose sweep, not a scene defect. Sub-note verified: the channel does not "declare []" — the `[]` arrives via `?? []` (controlSurfaces.ts:109). **NO-WAVE-OWNER (fleet prose sweep, all three instances in one commit).**
- **C-9** *(census extension + residue)* — the subtree hand-rolls TWO slider families, six runtime nodes (five row handles + the scrubber) against a `Slider` primitive the repo already imports; census S-4 enumerates one. C-9's applied falsifier (neither is a11y-broken) correct. **Residue recorded**: the two keyboard models disagree (40ms = 2.5% vs 0.05 = 5% steps) and EVERY key on either family silently pauses the transport (`scrub`:280, `reseatRow`:327), flipping the badge with no announcement (D20's surface). **NO-WAVE-OWNER (rides the S-4 fold decision).**

### INFO

- **D21** *(grade ruled 7)* — the scene wrapper contributes nothing but a node: both centring utilities are no-ops against an `h-full w-full` child; `gap-4` on the target root is dead against its single Card child. **NO-WAVE-OWNER (hygiene).**
- **D24** *(grade ruled 7, mechanism qualified)* — the null-ref guard's dead-lane branch is practically unreachable (function refs resolve during patch); the surviving charge is the absent degraded-state posture — no error boundary anywhere in demo/ (grep 0), `Metric.loading` shipped and unused. **NO-WAVE-OWNER.**
- **D25 / L-5 / C-7** — `computed` imported, never used (the 43-line file); the byte is this file's own; the detector hole (no ESLint, tsc never parses SFCs, no `noUnusedLocals`, CI = `check:lib` over src/) folds ≡ banked **KF-APP-4** ≡ **KF-ES-16**. C-7's `prettier-plugin-organize-imports` corroboration survives. **NO-WAVE-OWNER (one line; the gate is banked).**
- **L-9** *(grade ruled 7; B's MINOR as dissent)* — the reel returns children with `startTime` cleared while the master is merely paused; `resume()` never re-seeds (lifecycle.ts:107-116, my read); pixels survive via `applySequenceAt`'s `at`-derived clock; damage confined to event-emission bookkeeping the demo binds nowhere. Promotes to MAJOR the day a consumer binds `animationend`. **NO-WAVE-OWNER (one line in the reel's finally: restore `startTime = at`).**
- **L-12 / C-8** *(count corrected)* — the provide contract publishes the whole composable — **28 members** (not the "24" all parties repeat), raw `sequence`/`childAnims` included — behind bare `inject(...)!` with no guard, no `readonly`, no facade. C-8's applied falsifier (no live desync; Target:192's mirror-to-engine copy is order-argued correctly) honest and verified. **NO-WAVE-OWNER (with the SC-2 surface decision — 10 of the 28 have no consumer).**
- **L-13** — `CSSKeyframesAnimationT<any>[]` + the `reset()` cast; three consecutive imports from one specifier. L-13's own non-charge of the `SCENE_ID` rename (fleet-consistent) is right. **NO-WAVE-OWNER (hygiene).**
- **C-10** *(correctly self-demoted)* — synchronous `kfEngine()` at setup on a boot path that swallows the warm's rejection; the failure mode is App-wide (App.vue calls it first) and belongs to the shell's docket. **Fold-noted to kf-App's surface; no local cure.**
- **SC-5** *(NET-NEW — reader-B M-5)* — the registered `ScenePlayback` reports the wrong loop: `isLoopRunning` names the mirror, so the one loop that can actually be orphaned (`sequence.playback`) is invisible to the contract whose docblock promises "no orphan rAF"; during L-3's window `isPlaying()` reads false over a live engine loop. **NO-WAVE-OWNER (dies with the L-3 seam).**
- **SC-6** *(NET-NEW — reader-B M-6)* — the seam duplication is wider than L-10 states: three byte-similar `setProgress` bodies, `getPlaying` twice, one adapter published under three names (`facility.identity`/`facility.playback`/`scenePlayback`) — the precise ambiguity the L-6/C-5 prose drift trips over. **NO-WAVE-OWNER (one name, one body).**

### Folds-by-identity (≡ banked rows — no row here, confirmations recorded)

- **D1 / (this corpus's) L-geometry / C-geometry** ≡ banked **D-1/L-1/C-1** at kf-SequencePlayhead (MAJOR; ruling-1 cure `gap: 0.5rem var(--col-gap)`); the BLOCKER re-grade killed a THIRD time (ruling 4; K-6/K-31 lineage).
- **L-2 / C-1 / C-2** (reseatRow bypasses `_duration`) ≡ banked **N-2** (MAJOR; five prior derivations + this corpus's three axis derivations and two reader re-derivations recorded as confirmations; C-1's BLOCKER killed, ruling 4; L-2's rail-fraction cell killed, ruling 5). SC-2 attaches the no-undo rider and re-arms K-24.
- **D4** (`btn-interactive` phantom, 8 sites) ≡ **kf-CubeScene D-11** / **kf-ControlsPaneWrapper D-M11**; count matches the banked census; MAJOR killed. → KF.W6.
- **D12** (`--stagger-max` dead write) ≡ **N-5**. **D13** (playhead top hardcode + mobile break) ≡ **D-2/L-4/C-5**. **D26** (`--seq-glow: 0` redeclared) ≡ **N-15**. **D14** (band confinement + 4-vs-5 rules) ≡ **kf-SequenceAxis D-2** + **N-16** (cure = the `--tick-count` edit at kf-SequenceAxis L-2).
- **D17 + D18(unit limb)** ≡ **N-14** + **kf-SequenceAxis D-8a/L-9/C-5** — with N-14's SEQUENCING RULING re-armed: the valuetext-in-ms cure stays BLOCKED on banked N-1's time-domain decision (no honest millisecond exists until the scene picks a clock).
- **D18 (aria-hidden limb)** ≡ **kf-SequenceAxis D-12** — adjudicated NON-DEFECT; the re-file dies (killed-claims #9).
- **D22's height mechanism** ≡ **kf-KeyframeTimeline D-8** / **kf-ControlsPaneWrapper D-m12** (ruling 3; the width/conformance residue booked under D9).
- **The F-1 family (L-1 · C-11 · D §6)** ≡ **census SCH-1** / kf-App ruling 1 — killed at the frontier (ruling 4); the pattern ledger gains re-inheritance events six-through-eight, a triple in one corpus.
- **L-8's instrument limb** (the 760ms untracked timer) ≡ **kf-SequenceAxis SA-5**.
- **D25's detector limb** ≡ **KF-APP-4** ≡ **KF-ES-16**; **L-7's fleet class** ≡ **KF-ES-18** + the KF-ES-16 rider.

## Killed-claims register — 14 entries

| # | killed claim | adjudication |
|---|---|---|
| 1 | **L-1 (BLOCKER) · C-11 · D §6** — "glass-ui absent from package.json/lock (0/0); `npm ci` cannot mount the scene; all token findings unlocked" | **BASELINE-LAW TRIPLE KILL** (both readers concordant; my own probes): the 0/0 read the dirty worktree; `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep, lock 3 entries resolving to the registry tarball; `git diff origin/master -- demo/scenes/sequence/` EMPTY so every other citation binds clean. D §6's "re-verify after F-1 lands" rider is VOID — the measurements were taken against the exact version origin/master pins. Folds ≡ SCH-1/kf-App ruling 1; sixth-through-eighth re-inheritance events |
| 2 | **C-1 BLOCKER** ("duration permanently stale" as this component's headline) | **BOOKING + GRADE KILL** ≡ banked N-2 (MAJOR, the identical `End` witness already a banked cell); zero new evidence |
| 3 | **D1 BLOCKER** ("a ruler that lies falsifies the thesis") | **GRADE KILL, third generation** — K-6 killed it with both r4 readers concordant; K-31 killed the re-contest; nothing new here |
| 4 | **L-2's visual cell** ("the ball rests at 37.8% of its rail, forever") | **KILLED (ruling 5)** — `--row-start = 1` pins the ball at the rail's right end for every `--ball-p`; and 340/900 is a local-time fraction through a spring-eased keyframe fan, not a rail fraction. C-1's "37.8% of its glide" phrasing survives; the banked SS-13 #11 photograph brief is re-framed (shoot at mid-range `at`) |
| 5 | **Reader-A's D2 arithmetic** (5.21 opaque / 3.47 / ≈3.37 / dark 5.23 via `--neutral-5`) | **CHAIN KILL (ruling 1)** — the ladder rebinds `--muted-foreground` inside the Card; my run: 6.292/3.983/3.816/10.234/6.784. Citation-inheritance event #4 of this chain class, committed AFTER the bank's explicit warning (kf-SequenceAxis SS-13 #3). The conclusion survives slightly softened |
| 6 | **D4 MAJOR** ("silent design regression at 8 review sites") | **BOOKING + GRADE KILL** — banked twice at MINOR with the cure ruled; the 8-count matches the banked census exactly |
| 7 | **D8/D23/S7's `controlSurfaces.ts:88` citation** ("the DFA row … 'sequence → []'") | **CELL CORRECTED** — line 88 is JSDoc prose inside `surfacesFor`; no row, no table, no export (grep: 4 hits, all comments). Substance unaffected via `?? []` (:109); D's own D4 states it correctly in the same document |
| 8 | **D23's mechanism cell** ("a localStorage write on every mount") | **KILLED** — Vue's reactive setter identity-checks; the bucket default `true` flips once per fresh profile and every later mount assigns false-over-false. The REAL persistence is the line-30 getter — booked as SC-1 |
| 9 | **D18's aria-hidden limb** ("an AT user gets nothing at all") | **KILLED BY THE BANK** — kf-SequenceAxis D-12 rules `aria-hidden` on the ruler defensible (no focusable descendants; domain exposed by the five sliders' triple), recorded as a non-defect and a superlative. The unit limb folds separately |
| 10 | **D19(b) + reader-A's reachability cell** ("a paused, reversed transport reads 'reverse' … it is reachable") | **KILLED (ruling 2)** — `demo.reverse` has zero call sites (my census); `isReversed` writers live only inside uncalled verbs; the channel path carries no `animation`. The arm is dead code; the correct charge is SC-2. D's own offered falsifier satisfied |
| 11 | **D22-as-measured + reader-B's confirming cell** ("h-7 w-7 p-0 = 28×28 px") | **KILLED (ruling 3)** — installed Button base `min-block-size` floors the element at 28×40 fine / 28×60 coarse; `height` vs `min-block-size` is no cascade contest. Mechanism ≡ banked kf-KeyframeTimeline D-8 / kf-ControlsPaneWrapper D-m12. Width + `iconOnly` conformance limbs survive under D9 |
| 12 | **D9's supporting d.ts cell** ("Button.vue.d.ts exposes `emphasis` and no tooltip prop") | **CELL CORRECTED** (reader-A, verified by this seat) — eight props including `iconOnly` ('Square geometry for an accessibly named icon command'), `loading`, `tone`, `size`. The conclusion (no title/tooltip mirroring) survives; the enumeration was load-bearing for D22 and wrong |
| 13 | **D's S4 superlative** ("ownership boundary stated … design-idioms.css:584") | **DEMOTED** — the consumption is genuinely right; the stated boundary is a PHANTOM CITE (300-line file, `--ball-glow` at :185 — my wc/grep). "Correct consumption, unfollowable provenance"; the cite booked as SC-4 in the banked prose-truth family |
| 14 | **The "24-member bag" count** (challenge C-8, repeated by BOTH readers; reader-B's own M-2 arithmetic 18+10 contradicts it) | **CELL CORRECTED** — the return literal at useSequenceDemo.ts:452-479 counts **28** members (my enumeration). Recorded because a countable cell surviving two hostile readers is this ledger's smallest and most reliable inheritance signature |

## Superlatives (provenance preserved)

1. **inv-ζ by exhaustion, three independent runs** — `grep requestAnimationFrame|setInterval demo/scenes/sequence/` → 0; every pixel of the signature motion is `CSSKeyframesAnimation` under `Sequence`'s own RAFPlayback; the only Vue-side loop copies one scalar. The demo's thesis holds at the bytes. *(L S+1; reader-B re-ran; the axes' claim survives every hostile pass.)*
2. **`markRaw` discipline on the whole engine graph** — every child, the Sequence, and a plain (never `ref`) `childAnims` array: no engine object proxied, no per-frame reactivity tax. *(L S+4; verified by whole-file read.)*
3. **The `stagger` → `at:` consumption is genuine construction-time dogfood** — the distribution generator's own delays feed `Sequence.add` unmodified, and `DEFAULT_DELAYS` is captured from the same generator. Honestly self-qualified: true at t=0, false thereafter under banked N-2 — and now doubly qualified by SC-2 (the Reset that would restore the distribution is unreachable). *(C S★-4.)*
4. **The value.js edge is the narrowest possible leaf and R1 is structurally unreachable** — four imports, all `clamp` from `/math`; the 4.0.0 exports map has no root entry; no colour string ever enters the engine. Package-enforced, not discipline-enforced. *(C S★-2 ≡ banked kf-SequencePlayhead superlative 7.)*
5. **The LIGHT/HEAVY boundary honoured exactly** — `import type` from the barrel, runtime constructor from `kfEngine()`; value.js stays off the static light graph by construction. *(C S★-1; verified at the import statements.)*
6. **The D12 shadow-authority kill, executed and documented** — the private `isPlaying` ref deleted for a read-only machine projection; the dock shows no affordance because the DERIVED surface set is empty, not because a panel is hidden. The correct cure for exactly the class L-3 is a residue of. *(D S7 · L S+3.)*
7. **The engine target moved from track to ball, structurally** — a motion parameter (`scale: 0.7`) was producing an intermittent target-size failure mid-glide; cured by relocating the target, not clamping the scale, with the rationale recorded at both the template and the stylesheet. *(D S5 · L S+5 · C concordant — the sharpest engineering judgement in the file.)*
8. **`--row-start` makes the stagger legible in the still frame** — the scene's subject IS a delay distribution, and it reads as a diagonal at t=0. Qualified by banked N-4 (the three p→x maps disagree; ±12.8px gate offsets) — a calibration bug inside an idea that is right. *(D S2; the container-query transform choreography S1 rides with it, qualified by the 8th promoted layer ≡ banked L-11/C-10.)*
9. **`playReel` performs the exact `managed` handshake the engine documents** — clear before standalone play, restore after, rather than eating the throw. Qualified by SC-3: the same handshake is what makes the transport race silent. *(L S+6 — the superlative and its limit now in one place.)*
10. **METHOD, both axes' kill hygiene** — D's §4 published kill table (K1's `min-inline-size: 3ch` jitter kill reproduced at the dist by reader-B AND this seat; the K2 class-scoped `outline: none` census; K3's variable-font check; the P1 subgrid reservation that a banked K-29 proves was the rarer skill) and C-11's withdrawn barrel/subpath claim with its census recorded. Publishing falsified candidates with evidence is what let two hostile readers and an adjudicator work at the margin instead of re-deriving the corpus. *(Both readers concordant; sustained.)*

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The subgrid gutter** *(≡ banked SS-13 #1; K-29 stands as the warning against ruling it statically)* — `.seq-track`/`.seq-axis`/`.seq-playhead-track` rect lefts at 1440; now ALSO decides SC-8's overlap depth (10 vs 22px — the crossing itself is branch-independent).
2. **L-3's user-visible sequence** — play, hide >2s, return: storyboard settled, machine parked `paused`; record the mirror's re-arm snap on TAB_SHOWN. The chain is proven statically end-to-end; the paint is not.
3. **N-2's witness, RE-FRAMED (supersedes banked SS-13 #11's brief)** — at `at = 1600` the traveller is PINNED right from t=0 (ruling 5); shoot at a mid-range `at` (e.g. 1200, `--row-start = 0.75`) for the visible horizontal shortfall; the `--ball-p` stranding shows in opacity/settle-pop/bloom. K-24's Reset re-truthing is NOT probeable from the UI (SC-2) — the probe needs `demo.reset()` via console or the cure landed.
4. **D2's exact painted ratio** — my 3.816 assumes the 5% gradient stop; the row band sits between 5% and 2%, so truth ∈ ~3.82-3.98; any live re-measure MUST use the corrected chain (`--on-glass-muted`), and should record whether ladder.css's `@container style(--glass-backdrop: light)` or `contrast-color()` arms fire *(carried ≡ banked kf-SequenceAxis UNPROVEN #3 — note `.glass-floating`/`.glass-overlay` set `--glass-backdrop: light` on themselves, so a floating ancestor is the live risk)*.
5. **D3's exact overflow** — `.seq-row-at` vs `.seq-row-label` rects at 1440/1920 with row 5 at 1040ms; whether glyphs cross the stage's 1px border.
6. **D8's clipping threshold viewport** — mechanism decided from source; landscape phone (667×375) first candidate.
7. **D5's eyebrow rendered weight** — Tailwind `font-medium` (500) vs `text-caption`'s own 400, equal-specificity utilities-layer contest decided by Tailwind's internal ordering; probe with the rest of D5.
8. **SC-3's race percept** — start the reel, PLAY within 360ms: do the un-woken lanes desync, does the master's glide briefly wear the overshoot curve.
9. **D7 under live PRM** — confirm the five-lane bounce fires from both triggers with `prefers-reduced-motion: reduce` active.
10. **D9/D22 percept** — does a sighted pointer user ever find a 28px-wide unlabelled clapperboard; does it read as a control at touch sizes.
11. **Interpolation-space split (recorded, not charged)** — the `--ball-tone` family is mixed in `oklab` at Target:165 and `srgb` at seven other sites; probably a house-wide class — fleet sweep, not a per-component row.
12. **Keyboard-step semantics residue (C-9)** — the silent-pause badge flip on every arrow key, both families; pairs with D20's announcement gap.

## Identity guards (fold-by-reference — never re-booked)

- **The scene-cluster home stays kf-SequencePlayhead** (N-1/N-2/N-4/D-1 + the ruling-1 cure); this record contributes confirmations, the SC-2 no-undo rider on N-2/K-24, and the SS-13 #11 re-frame. **kf-SequenceAxis's two relays consummate here** (D10; the valuetext family's Target limb inside the N-14 fold).
- **The F-1 limb ≡ census SCH-1 ≡ kf-App ruling 1** → KF.W0 §B-12; the pattern ledger gains three re-inheritance events (one per axis) plus the "24-member" countable-cell signature (killed-claims #14) — the class kf-App.md predicted by name.
- **D4 ≡ kf-CubeScene D-11 / kf-ControlsPaneWrapper D-M11** → KF.W6. **D22's height mechanism ≡ kf-KeyframeTimeline D-8 / kf-ControlsPaneWrapper D-m12** → the residue rides D9 here.
- **L-7's class ≡ KF-ES-18 (+ KF-ES-16 rider); D25's detector ≡ KF-APP-4 ≡ KF-ES-16; L-8's instrument limb ≡ kf-SequenceAxis SA-5; D18's aria-hidden limb ≡ kf-SequenceAxis D-12 (non-defect).**
- **L-3 is FRESH and books HERE** — the banked `useSceneVisibilityPause` rows (KF-ES-1's onArm inversion; kf-AmigaScene's honesty-contract superlative) are different mechanisms; any later scene adjudication finding the mirror-vs-motion binding folds to THIS row (anti-rename).
- **SC-1..SC-8 minted here for life**; SC-1 extends D23/L-4/C-3's cure, SC-5/SC-6 die with L-3's seam cure, SC-7/SC-8 ride the D9/N-12 packets.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 fold (§B-12) + the countable-cell inheritance datum. — **KF.W6**: the D4 fold (phantom-utility audit, 8 sites) · the D22 height-floor fold. — **KF.W7**: none (no export-surface rows in this corpus). — **KF.W9**: D20's announcement gap · the D17/D18-unit fold (SEQUENCED after banked N-1) · the SS-13 list.
- **NO-WAVE-OWNER — the sequence scene-repair packet (SS-1/SS-2 authoring must home it):** the **loop-seam commit L-3 + L-10 + SC-5 + SC-6** (one `SweepSceneOptions` parameter: supply the loop verbs; one adapter, one name) · the **transport-surface decision SC-2 (+ D19(a), the dead reverse arm, L-12/C-8's 10 unconsumed members)** — expose the verbs or delete them, and give the drag gesture its undo; engine-side rider joins banked N-2's · the **reel packet D7 + SC-3 + L-8 + L-9 + SC-7 + D9/D22-residue** (PRM guard, transport lock during `isReeling`, retained handles, `startTime` restore, a real active cue, a name and a width) · the **row-surface packet D2 (+ SA-1) · D3 · D10 · SC-8 (≡ N-12's interaction spec) · D16 · D15** · the **store hygiene D23/L-4/C-3 + SC-1 (both lines)** · the **prose-truth sweep L-6/C-5 · L-11/C-4 (THREE instances fleet-wide) · SC-4 · the F.W9 docblock (SC-2's limb)** — the banked checklist grows by four · **D6 + D8** (header hierarchy and overflow posture — design decisions, not patches) · hygiene **D21 · D24 · D25 · L-13 · D11's registration relocation** · C-9's census extension riding the S-4 fold.

## Verdict

**ADJUDICATED.** The provider shell is close to what every axis found — 43 lines, one responsibility, honest engine discipline (inv-ζ by exhaustion, markRaw, the managed handshake, the narrowest value.js edge) — and the closure around it bleeds in exactly the ways an incredulity pass exposes: the one loop that moves the pixels is the one loop no gate protects; three of the six advertised transport verbs are wired to nothing, so the headline gesture has no undo and a banked mitigation rests on an uncallable function; the reel is guarded against itself but not against the transport; and the scene's prose misdescribes its expose surface, its cited authority, its eyebrow's font, and a line number 284 lines past the end of the file it names. Both hostile readers were themselves caught once each — one re-deriving a contrast on a chain the bank had banned by name, one confirming a geometry the installed producer floors — and both repeated a member count their own arithmetic refutes. Every BLOCKER in the corpus died: two on baseline law against a frontier that pins glass-ui 7.0.0 exactly, two on grade stability against banked homes. What remains is a packet, not a rewrite: one seam parameter, one transport decision, one reel guard, one deleted opacity, two deleted lines in a provider shell. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life; SC-1..SC-8 minted here for life.

— end of apotheosis. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3; the banked kf-SequencePlayhead and kf-SequenceAxis rows referenced here are untouched by this record except where their own SS-13 briefs are re-framed above, which is recorded here and never edited there.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/sequence/SequenceScene.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SequenceScene.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

