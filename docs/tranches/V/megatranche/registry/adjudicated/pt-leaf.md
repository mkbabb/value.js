claude-fable-5

# pt-leaf — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-05 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ `ef10d5b` (re-verified this session; `git status` shows **zero** dirty rows under `typescript/`), `typescript/src/parse/leaf.ts` (399 lines, 9 exports) + its import closure and shipped dist · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two hostile re-reader returns over the module's two challenge files — the **L-axis re-reader** (`leaf-L18-hostile-reread-library`: 18 confirmed · 15 killed · 5 missed · 11 superlatives, over `audit/parse-that-modules/leaf/challenge-L-library.md`, Opus ×2 passes, 32 defects) and the **C-axis re-reader** (`leaf-C-consumption-L18-rereader`: 18 confirmed · 9 killed · 6 missed · 5 superlatives, over `audit/parse-that-modules/leaf/challenge-C-consumption.md`, Opus ×3 seats, 29 defects). Both challenge files were re-read whole by this seat.
**Method (incredulity)**: every cross-reader **disagreement**, every kill of a challenge blocker's supporting prose, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption; suspiciously-agreed cells (the dead-FLAG grep triple, the consumer receipts, the dist chunk edges) were independently re-measured, not spot-trusted. Nothing in parse-that was armed — no `enableDiagnostics`, no `memoize` — and no parse-that code was executed by this seat at all; the single dynamic instrument was a dependency-free V8 elements-kind probe over array literals (below). Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (read-only: git/grep/sed/cat/ls + one `node --allow-natives-syntax -e` importing nothing)**: full read of `leaf.ts` (whole, 399 — every cited anchor checked); `parser.ts:1-140` (imports, ParserFunction, parseState/parse, then/or/chain), `:185-245` (skip, opt), `:380-431` (wrap), `:433-521` (call/trim whole), `:523-545` (many), `:595-615` (sepBy), `:700-711` (`_initWhitespace()` top-level call); `state.ts:40-104` (constructor `furthest=-1` at `:52`, `ok +=` at `:55-60`, unsafeCall `:92-94`, unsafeCallRaw `:97-99`), `:150-180` (`"trimWhitespace"` :157, `"whitespace"` :158, mutable `ParserContext.name`); `utils.ts:1-60` (mergeErrorState whole); `core.ts` whole (leaf block = `:16-25`, names `:17-24`, split `:26`); `index.ts` whole (14 lines; diagnostics on `:5`, leaf on `:9`); `dist/leaf.d.ts` whole (all 10 declaration cites exact, PT-Q5 note `:5-23`); `dist/core.js:1` → packrat-entry chunk; `dist/packrat-entry-CS1td-8B.js:1` → diagnostics chunk, `PACKRAT_ARMED :678/:682/:714/:722`, `:881-882` guard+`console.error`; `dist/diagnostics-DDazRHgl.js:14-16` (guard covers only `:14`); `package.json:1-55` (version 1.0.0 `:3`, `sideEffects: false` `:6`, exports `:7-33` = five specifiers, **no** `main`/top-level `types`/`"./package.json"`); `test/math.test.ts:1-105` whole head; `ls test/` (**13** `.test.ts` specs + setup/utils + 2 `.mjs` gates + benchmarks/); `test/benchmarks/pt-b3-fusion.bench.ts:1-30/:35-60/:138-145` + `ls -la` (6,791 B, mtime 2026-07-03); `scripts/proof-perf.mjs` (14,205 B) + `scripts/perf-baseline.json` (522 B) existence; `scripts/proof-no-dead-combinator.mjs:8-12/:28-33/:49-58`; `test/subpath-gate.mjs:20-56`; repo greps: `.flags` writes = **1** (`parser.ts:496`), `.call(` callers = **2** (`parser.ts:493` + `state.ts:93`), `unsafeCall` callers = **0**, `.trim(x, false)` / `.wrap(a,b,false)` sites in src+test = **0**; value.js archive: `git show 164343c1^:src/parsing/index.ts:435-470` (the C-M4 decider), `stylesheet/stylesheet.ts:275-282` and `math.ts:142-152` (post-mortem receipts, verbatim); HEAD `src/css/grammar.ts` (483 lines; `splitTopLevel` at `:63`, zero Int8Array/dispatch/charCode LUT, `lower === "rgb"` present — the C-§7 decider); corpus: `parser-band.md:70-80` (HONESTY LAW section) + `:105-145` (idiom law, debts 1-5, G7/G8); `X/parse-that/waves/W0..W4` headers (the routing roster).
**The one dynamic instrument (engine receipt: node v26.0.0, this box)**: `node --allow-natives-syntax` over pure array literals — `%HasHoleyElements(new Array(5) fully written)` → **true**; `%HasHoleyElements(new Array(5).fill(undefined))` → **false**; `[]` written contiguously → packed; `[u,u]` after `out.length=1` → stays packed. This decides one killed-claim dispute (below) and seeds MEASURE-AT-OPEN #1.
**STOP/law compliance**: `/Users/mkbabb/Programming/parse-that-css-totality-p2` → `No such file or directory` (re-verified; not created). parse-that touched read-only, main checkout only. **Sole write = this file.**

## Scoreboard

**48 adjudicated roster rows — 7 BLOCKER · 18 MAJOR · 20 MINOR · 3 INFO** (identities merged across the two axes; original ids preserved for life). **16 killed-claims register entries: 15 kills SUSTAINED on re-derivation, 1 kill OVERTURNED by this seat's own measurement** (the L-M3-cure kill — the readers' "self-contradiction" was a false dichotomy). **10 missed rows ADMITTED** (2 of them found **independently by both readers** — the strongest evidence class in this corpus). **11 superlatives sustained** (1 narrowed). **6 adjudicator corrections to the readers** — two against the L-re-reader's over-credit, one overturned kill, one citation-inheritance-shaped slip inside a correct C-re-reader kill, two severity rulings.

Blocker-weight survivors of record: **PT-LEAF-2** (`all()` drops `undefined` and compacts — live in the library's own green suite, paid for by three consumer post-mortems and an entire band idiom law) and **PT-LEAF-7** (three mutually inconsistent whitespace alphabets, none CSS's, with `.trim()` silently substituting a third for the parser the caller passed — COMP-1-unsatisfiable by construction, live at 41 of the sole consumer's own sites). Realized in-constellation impact today is **zero at every hop**: the consume edge was severed at `164343c1` (C-i1, re-verified by both readers), so every blocker is a defect of the **published 1.0.0 contract** and of the **X·P adoption premise**, not a shipping outage.

## The incredulity seat's own work — ruled disagreements and corrections

The two readers disagreed, or one over-credited, at six load-bearing points. Each was re-derived by this seat before ruling.

1. **The math.test.ts "verbatim" dispute — RULED for the C-re-reader.** Live source `test/math.test.ts:18-22` is a *block-bodied* arrow with an explicit `return`, and the binding carries a trailing `.opt()` on `:22` that the L-challenge's quote omits. The L-re-reader's cell "verbatim as quoted" is **wrong**; the C-re-reader's kill is exact, and its sharpening is adopted: the trailing `.opt()` is load-bearing (the outer `all(numberPart, exponent)` at `:32` drops the absent exponent too, and only the `?? ""` at `:35` catches that one). The **substance** both readers agree on is re-derived clean by this seat: fixture `"123.456e2"` (`:87`) → `number.parse` (`:97`) → the map at `:19-21` receives `["e","2"]`, binds `exponentSign="2"`, `exponent=undefined`, emits `"e2undefined"`, and `:100`'s `toBe(parseFloat(num))` stays green only because `parseFloat` truncates at `'u'`. A named fixture in a shipped green suite, one line from the assertion — PT-LEAF-2's sharpest receipt.
2. **The `.trim()` routing tangle — BOTH kills sustained; they are complementary, not conflicting.** This seat's read of `parser.ts:480-521` plus three greps settles the whole knot. (a) `discard` defaults **`true`** (`:482`) — the L-challenge's B6 sentence ("documented default … `discard = false`") is false as written; the C-re-reader's kill stands, and the exercise bound is re-measured: **zero** `.trim(x, false)` / `.wrap(a,b,false)` call sites in parse-that src+test (this seat's grep) and zero at `164343c1^` (both readers, agreeing). (b) The FLAG mechanism cited by **both challenges** (`FLAG_TRIM_WS` via `parser.ts:488-496` / `:443-465`) is **dead-code attribution** — the L-re-reader's kill stands at the bytes: `:492-496` builds `flaggedParser` and writes the repo's **only** `.flags` assignment onto it (`:496`), then `trim()` returns a *different* Parser built from the `whitespaceTrim` closure (`:498-512`, returned `:514-517`); `flaggedParser` is never referenced again. `Parser.call()`'s only callers are `:493` (inside the discarded object) and `state.ts:93` (inside `unsafeCall`, which itself has **zero** callers). So `Parser.call()`, `FLAG_TRIM_WS`, `FLAG_EOF` (`~37` lines, `:441-477`) and `state.ts:92-94` are unreachable in-realm, and `flags === 0` on every shipped parser. (c) One blemish inside a correct kill: the C-re-reader's own kill-cell prose says the default call "takes the FLAG_TRIM_WS branch (the doc's own B7)" — repeating the challenges' dead-mechanism attribution *inside the kill that requires it to be dead-adjacent*. Verdict unaffected (the conclusion — default `.trim()` runs `trimStateWhitespace`, never the passed `/\s*/` parser — is what both readers and this seat all derive), but recorded as a citation-inheritance-shaped slip per the ColorSpaceSelector DISSENT-1 precedent.
3. **The L-M3-cure kill — OVERTURNED by measurement.** The L-re-reader killed the challenge's cure (`new Array(n).fill(undefined)` restores PACKED) as "self-contradictory with the row's own falsifier" (N4: a fully written `new Array(5)` stays holey), reasoning "if writing every slot cannot restore PACKED, neither can fill", and routed it MEASURE-AT-OPEN under the static-only law. This seat ran the probe (node v26.0.0, zero imports): **N3 and N4 are both true simultaneously** — `new Array(5)` written store-by-store stays HOLEY (per-slot stores never re-generalize the map), while `new Array(5).fill(undefined)` measures **PACKED** (the `fill` builtin rebuilds the backing store wholesale). The "one of N3/N4 must be wrong" inference is a **false dichotomy**; the challenge's own probe record was internally consistent. The cure is verified-on-this-box; because the property is engine-version-dependent V8 internals, the *predicate* (not the folklore) is pinned as MEASURE-AT-OPEN #1 for the lane's W1 harness. The L-re-reader's alternative cure (`const out: unknown[] = []` written contiguously — the shape the arity-2/3 arms already use) also measures packed and is adopted as the *preferred* spelling since it needs no engine assumption.
4. **The C-M4 second receipt — RULED for the C-re-reader; the L-re-reader over-credited.** This seat read `164343c1^:src/parsing/index.ts:435-470` directly: the buckets (`const bucketT = any(fnTransform, fnGeneric);` …) carry **no** type annotations; the widening enters through exactly two annotated *arms* (`fnGeneric: Parser<any>` at `~:437`, `fnDashed: Parser<any>` at `~:444`); and `:466`'s `const Function_: Parser<any> = dispatch({` is an **annotation**, not an `as` cast. The C-challenge's "every bucket declared `Parser<any>` (`:455-464`)" is false at the bytes, and the L-re-reader's "BOTH consumer receipts verified verbatim" verified the quoted *line* while inheriting the false *support*. C-M4 survives — on **one** receipt: `color/color.ts:673`'s `as Parser<ParsedColorUnit>` with its 10-line justification, which is exactly the G-10 seam.
5. **Severity contests — three rulings.** (a) **C-B9/L-M7 (whitespace family): BLOCKER sustained** (C's grade; the L-re-reader's MAJOR recorded as the reachability dissent). Grounds: it is the one blocker with **no** current-exercise bound — live at 41 of the sole consumer's own `.trim(whitespace)` sites, needing nothing more exotic than an NBSP — and the COMP-1 failure is *unsatisfiable by construction* (no complement channel exists in any of the three mechanisms), which is spec-fatal for the lane per `W2.md:237/:241`. The L-re-reader's bound is preserved inside the row: the divergence is invisible to any ASCII corpus (all three classes agree on {09,0A,0C,0D,20}), which is a *corpus-design* obligation (MEASURE-AT-OPEN #7), not a severity discount. (b) **C-B7 (digit-key hoisting): MAJOR sustained** (the L-re-reader's grade; C's BLOCKER recorded as dissent). Both live tables dodge it with no single-digit override, and the inversion is a published-contract defect with a construction-time-throw cure; it sits at the top of MAJOR because the JSDoc names `"0-9"` as its own example and the sole consumer wrote the override contract down as "verified against parse-that's dispatch" — true for letters, silently inverted for digits, *in either insertion order* (total inversion, the C-re-reader's added row). (c) **C-B3/C-B4 lumped at BLOCKER by the C-re-reader — SPLIT ruled** (the L-re-reader's split adopted): the null-arm negative delta (L-M14/C-B3) needs only a caller who honours the declared `RegExpMatchArray | null` signature → **BLOCKER**; the re-entrancy desync (L-M5/C-B4) needs a re-entrant `matchFunction` → **MAJOR**. Shared bound, verified by both readers: the sole historical `matchFunction` site (`color/color.ts:129`) returns `null` and dodges both; zero two-argument `regex(` anywhere in parse-that src/test.
6. **Independent convergence noted for the record**: the wrap/trim `discard=false` composition (PT-LEAF-21) was found by **both** readers independently, from opposite axes, with compatible traces (`["a"," "]` on `"a "`; hard-fail on `"x"` via the EOF guard). Under the fleet's own epistemics this is the highest-confidence class in the corpus and it is a row **neither challenge stated** — the strongest single argument that the challenge pair, at ~60 rows, had not yet exhausted this 399-line file.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/corpus/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec rather than landing in a unit · **NO-WAVE-OWNER** where no X·P wave owns it (incumbent parse-that repair; evidence rides the standing O-15 letter channel, owner-held). X·P executes nothing until the owner's begin-word; every routing below is a booking, not an act. Line anchors are `ef10d5b`; re-resolve before any cure (D-19).

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-LEAF-1** | L-B1 / C-B1 + L-miss-M1 | `all()` at arity 1 returns the child's bare value against a published mapped-tuple `.d.ts` — **and** (the admitted miss) silently loses the offset-restore-on-error discipline all three `fuseAll` arms carry | `leaf.ts:161` raw `parsers[0].parser` vs restores at `:195/:226/:263`; `dist/leaf.d.ts:25`; the phantom-cast laundering at `:163` | **W2** — the algebra's `sequence` admits no arity short-circuit; arity 1 is a spec case with a stated shape AND a stated rollback law (EQ-5). W1 rider: arity-0/1/2/3/N equivalence oracle (MEASURE-AT-OPEN #6) |
| **PT-LEAF-2** | L-B2 / C-B2 | `all()` drops every `undefined`-succeeding child and compacts left: declared arity fixed, runtime arity input-dependent; **live in the library's own green suite** (`"e2undefined"` masked by `parseFloat`); paid for by the consumer in three prose post-mortems incl. a shipped crash | drops `leaf.ts:199/207/230/238/246/267`, truncations `:209/:248/:270`; producers `parser.ts:241` (opt), `leaf.ts:342/:355`; `test/math.test.ts:18-22/:87/:97/:100` (this seat's trace, ruling 1); receipts verbatim at `164343c1^` `index.ts:208-216`, `stylesheet.ts:275-282`, `math.ts:144-150`; `parser-band.md:108` idiom law + `:134` G8 gate exist *because of these six lines* | **W2** — this is the decision the algebra actually turns on (L-challenge cure 8): ONE sequence operator, absence represented explicitly, `.opt()`-under-sequence legal again, G8's routing-around gate retired. PT-LEAF-22 is its missing premise |
| **PT-LEAF-3** | L-B3 / C-B5 + L-M6 | `regex()` rejects unconditionally at EOF **without merging**, so every nullable pattern — including exported `whitespace` (89 consumer refs) — fails at EOF with `furthest=-1`, and the rendered error points at the backtracked offset (the truncated-input case, the commonest authoring error, displays offset 0) | `leaf.ts:327-330` (no merge; contrast `:360/:291/:303/:16`); `leaf.ts:397`; `state.ts:52`; `parser.ts:60`; the `test()` path `:344-357` already handles EOF correctly, so the guard is a semantics-changing fast exit | **W2** — the terminal tier's totality law (COMP-1) + the RETIRE-regex ruling (C-§7, sustained); the furthest-merge asymmetry becomes a stated diagnostic-conservation law. W1 rider: EOF-inclusive corpus row (`""` and every trailing-ws-free input). Incumbent one-line deletion rides O-15 |
| **PT-LEAF-4** | L-M14 / C-B3 | The documented `null` arm of `matchFunction` makes the offset delta **negative** (failed sticky exec resets `lastIndex` to 0), yielding a *successful* parse whose cursor rewound to absolute 0 — input consumed **twice**; every downstream monotonicity guard is one-sided and blind to it | `leaf.ts:319/:337-340`; `state.ts:55-57`; guards `parser.ts:538/:605`, `packrat.ts:398`; both readers' static traces agree; zero call sites (bound) | **W2** — an explicit **offset-monotonicity law** on every operator (success advances or holds, never regresses), with the W1 monotonicity oracle (MEASURE-AT-OPEN #2). Incumbent two-line cure rides O-15 |
| **PT-LEAF-5** | C-B8 | Three postures toward one non-string input class, and the most-consumed export **silently succeeds** (`whitespace.parseState(42)` → `isError:false` via `0 >= undefined` → ToString coercion → empty-match arm) — falsifying the corpus premise (PT-07 "5/5 throw") the debt-4 boundary guard is specified from | `leaf.ts:327` / `:344` / `:354-357` (this seat's static trace); contrast `:285/:297/:136` (raw TypeError); `parser-band.md:119` debt 4 | **REFINEMENT FOLD** — W2's JS-boundary guard is re-specified as a **positional `typeof` check at entry**, never throw-inference; W1's boundary corpus gains the eight-probe row |
| **PT-LEAF-6** | C-B6 / L-M2 | Any 3-char dispatch key with middle `-` is silently a range with no escape and no precedence note: `"+-."` — the CSS number-start set, verbatim — admits `','` (a separator) as a value head; reversed ranges map nothing silently; the in-tree bench itself writes a range key beside a bare `-` key | `leaf.ts:116-119`, JSDoc `:86-88`; charCodes 43..46 ⊇ 44; `pt-b3-fusion.bench.ts:141-142` (this seat re-read) | **W2** — dispatch-wrapper requirement (i): explicit range constructor, throw on reversed/ambiguous keys; the LUT is re-authored, never imported (C-§7 sustained) |
| **PT-LEAF-7** | C-B9 / L-M7 + L-m7 | **Three mutually inconsistent whitespace alphabets ship in one package, none of them CSS's** ({09-0D,20} / JS `\s` incl. NBSP-BOM-LS / `<=32` incl. NUL+C0), and `.trim()` silently substitutes the charCode scanner for the parser the caller passed, dispatching on a **mutable public string tag** — a forged `context.name = "whitespace"` on any parser silently discards that parser. No mechanism has a complement channel, so COMP-1 is unsatisfiable **by construction**. Severity ruled BLOCKER (ruling 5a; L-axis MAJOR preserved as the ASCII-invisibility bound) | `leaf.ts:383/:397-398`; `utils.ts:162`; `parser.ts:481/:488/:498-512`; `state.ts:158` public union + mutable `name` field; 41 consumer `.trim(whitespace` sites | **W2** — the span-emitting `trivia` operator, class fixed by css-syntax-3 §4.2 **citation** (never harvested from any of the three in-tree classes); all three mechanisms retire together; tag-dispatch is banned (identity, not string, selects behavior). W1 rider: the non-ASCII code-point sweep (MEASURE-AT-OPEN #7) |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-LEAF-8** | C-B7 | `Object.entries` integer-key hoisting makes the JSDoc's own example range `"0-9"` unoverridable by a digit key **in either insertion order**, while letters obey last-wins — silently inverting the override contract the consumer verified and wrote down. Ruled MAJOR (ruling 5b; C-axis BLOCKER dissent recorded) | `leaf.ts:113/:87`; ECMA-262 OrdinaryOwnPropertyKeys; receipt `164343c1^:index.ts:463-465` verbatim (this seat re-read); both live tables dodge it | **W2** — wrapper requirement (ii): tables are ordered `Array<[key, parser]>` or `Map`, never a plain object |
| **PT-LEAF-9** | L-M5 / C-B4 | `regex()` reads the closure-singleton `sticky.lastIndex` **after** invoking arbitrary user code — a re-entrant `matchFunction` desynchronizes value from offset (offset past `src.length`). MAJOR per ruling 5c | `leaf.ts:322/:333/:337-340`; the packrat tier got a full epoch save/restore for exactly this class (`packrat.ts:158-186`) and the leaf got nothing; `parser.ts:35-41`'s "SOUND" is packrat-scoped | **W2** — same monotonicity/re-entrancy law as PT-LEAF-4; regex RETIREs from the terminal tier (host-object, no zero-import lowering — AC-1/AC-2). Probe offsets un-re-measured → MEASURE-AT-OPEN #2 |
| **PT-LEAF-10** | L-M1 / C-m3 | `dispatch()`'s Int8Array stores an unbounded intern index: 128-255 fail **closed** (mapped char silently rejected; 255 aliases the -1 sentinel exactly), ≥256 fail **open** (wrong parser dispatched — 256→`parsers[0]`) | `leaf.ts:101/:104-111/:114/:119/:122/:137-141`; Int8 wrap arithmetic; C-re-reader's corrected boundary table adopted | **W2** — wrapper requirement (iv): bound the intern index or widen (`Int16Array`, 256 B, once per construction) |
| **PT-LEAF-11** | L-M9 / C-M3 / C-M10 | Three spellings of "try this alternative", three post-failure offset disciplines: `any()` arity≥2 restores, `any()` arity 1 leaks, `dispatch()`'s hit path never captures `savedOffset` at all — committed choice sold by its own JSDoc as the drop-in for backtracking `any()`, with no label on arm failure | `leaf.ts:76/:134-145` (no savedOffset — this seat re-read the whole closure) vs `:46/:51-52/:66-67`; leaking arm = two primitives via `chain` (`parser.ts:131-138`, no restore); JSDoc `:82-85`; both consumer tables survived only inside re-restoring wrappers | **W2** — *backtracking choice* and *committed choice* become two **named** operator families, each with a stateable EQ-5; the C-re-reader's §7-correction stands: no table-side adapter can fix a leak that is in the loop |
| **PT-LEAF-12** | C-M5 | Non-ASCII dispatch keys are silently dropped (out-of-bounds TypedArray writes are no-ops) while the construction-time label still advertises them — CSS idents admit non-ASCII from U+0080 | `leaf.ts:101/:122` (no range check) /:128-132 (label from all keys) /:137 | **W2** — wrapper requirement (iii): non-ASCII keys rejected loudly at construction |
| **PT-LEAF-13** | C-M4 | `dispatch`'s homogeneous `Record<string, Parser<T>>` cannot express the heterogeneous table its own advertised `any`→`dispatch` migration needs — surviving after ruling 4 on **one** receipt: the sole consumer paid an `as` cast at the exact boundary G-10 makes normative | `dist/leaf.d.ts:24` vs `:4`; `164343c1^:color/color.ts:673` + `:663-672` (verbatim); the index.ts receipt restated as inferred widening (ruling 4) | **W2** — wrapper requirement (v): heterogeneous inference (`T[keyof T] extends Parser<infer V> ? V : never`) so V stays assignable under the G-10 excess-property check |
| **PT-LEAF-14** | L-M10 / C-M9 | The `parser ↔ leaf` cycle: `export let whitespace` (non-optional, never initialized in-module) + a cross-module top-level `_initWhitespace()` under `"sideEffects": false` — package correctness hangs on barrel statement order; **strengthened by the L-re-reader and sustained**: an ESM entry importing `src/parse/leaf.js` first evaluates parser.js's body inside the cycle and assigns a still-TDZ `let` → throws **today** at src level. Bundle-scope safe (single chunk, verified) | `leaf.ts:395-399`; `parser.ts:6` (**five** imports — both readers' kill of the 3-symbol cure confirmed by this seat) + `:711`; `package.json:6`; `dist/packrat-entry:656/:1416` per readers | **REFINEMENT FOLD** — W0's fresh root consumes src: the formation inherits a load-order proof obligation (or consumes dist only, stated); W2's module topology law bans in-cycle mutable exports. Incumbent cure (relocate `whitespace` or lazy-resolve trim's default) rides O-15 |
| **PT-LEAF-15** | L-M11 | `ParserFunction<T>` is phantom (`T` unused): all **9** `as ParserFunction<X>` casts assert nothing and **16** `as ParserState<unknown>` casts launder a variance hole — 25 assertions in 399 lines; the mechanism by which PT-LEAF-1/-2 pass `tsc --strict` silently | `parser.ts:14-16`; counts re-verified by the C-re-reader against its own naive-count trap; the 9 sites enumerated | **W2** — type-soundness law: the algebra's function type carries `T` on both sides; casts are a counted, gated budget |
| **PT-LEAF-16** | L-M13 / C-M7 | `.parse()` on failure returns whatever `state.value` holds — frequently a plausible partial of a *different shape* (`"b"` where success yields a 3-array; `[]` from `many(2)`) — so an `undefined`-check boundary invariant **passes failed parses**; and the printed error view is not the returned one | `parser.ts:77-79/:61/:66/:74`; fuseAll error arms never reset `value`; both readers' traces concur | **REFINEMENT FOLD** — W1's JS-boundary invariant is *upgraded from convention to law*: entry via `parseState` + `isError` only; `.parse()` truthiness is a banned idiom with this row as the written reason (the band already practices it — `parser-band.md:108`) |
| **PT-LEAF-17** | C-M1 + C-M11 | `./core` ships leaf's three label constructions but withholds `enableDiagnostics`/`mergeErrorState` (labels unobservable on the tier) — while the shipped `dist/core.js` **already pulls the packrat chunk and the diagnostics chunk in two hops**, so the tier-isolation promise (`core.ts:3-5`) is false and the consumer pays for the capability it is denied; `proof:subpath` checks existence + `typeof`, never a module edge — structurally vacuous for its stated purpose | `core.ts:16-25` (this seat re-counted; drifted cite corrected); `utils.ts:33`; `dist/core.js:1` → `packrat-entry-CS1td-8B.js:1` → `diagnostics-DDazRHgl.js` (this seat re-read line 1 of both); `test/subpath-gate.mjs:24-55` (this seat re-read) | **W2** — EQ-4 owns the class: labels are first-class values, never mode-gated (leaf's *production* already complies — superlative 8). **W4 rider**: the release condition gains "subpath isolation asserted over import graphs, not file existence" |
| **PT-LEAF-18** | C-M6 | `trimStateWhitespace` is dead public surface on both barrels (**0** consumers ever, anywhere), not even a `Parser`; the project's dead-export gate is hardcoded to two past deletions and structurally cannot catch the next instance | `leaf.ts:372-391`; `index.ts:9`/`core.ts:23`; `proof-no-dead-combinator.mjs:10-11` precept, `:29-32` banned list, `:49-58` vacuity guard (this seat re-read all three) | **NO-WAVE-OWNER** — incumbent deletion, owner-held via O-15. **W4 rider**: release gates enforce standing properties, not enumerated corpses |
| **PT-LEAF-19** | C-M8 + C-miss (stale anchors) | 19 lines of internal tranche archaeology ship in the published `.d.ts` directly above `dispatch` — and its version anchors ("shipped in 0.12.0 … RETRACTED in 0.13.0") are **below** the 1.0.0 artifact carrying them, so a consumer cannot tell from the hover whether the retraction applies | `dist/leaf.d.ts:5-23` (this seat re-read whole) vs `package.json:3` | **NO-WAVE-OWNER** — incumbent doc repair (CHANGELOG relocation + anchor refresh), O-15 channel |
| **PT-LEAF-20** | NEW (L-miss-M2) | The entire `FLAG_*`/`Parser.call()` machinery is **dead code** (~40 lines incl. the only FLAG_EOF consumer), `.trim()` allocates and discards a Parser + context per call for it, and the dead design is also *wrong* (the flag sits on a wrapper whose `.parser` delegates to `inner.call`, `inner.flags` 0 — combinators calling `.parser()` would trim nothing had it been returned); invisible to the dead-export gate — the second instance of PT-LEAF-18's structural point; **also falsifies the routing prose of both challenges' B6/B7 cells** | this seat's greps (ruling 2): sole `.flags` write `:496`, sole `.call()` caller `:493`, `unsafeCall` callers 0; dead surface `parser.ts:20-22/:27/:441-477`, `state.ts:92-94` | **NO-WAVE-OWNER** — incumbent deletion, O-15. **W4 rider** shared with PT-LEAF-18 |
| **PT-LEAF-21** | NEW (L-miss-M3 ≡ C-miss-1, **independent convergence**) | The two `discard:false` public APIs are built on `all()` and inherit the compaction whole: `wrap(start,end,false)` is *inferred* `Parser<[unknown,unknown,unknown]>` with no cast, `.trim(p,false)` is double-asserted to `Parser<T[]>` — and with the default whitespace both return arrays of length 1-3 by input (the caller's own value migrating between indices 0 and 1) or **hard-fail at EOF** via PT-LEAF-3. First-party proof of PT-LEAF-2 inside the library's own API. Bound: zero `discard=false` call sites anywhere (this seat's grep) — a published-contract defect | `parser.ts:392-395` (uncast) /:484-486 (double assert); trace via `leaf.ts:355` drop + `:327` EOF guard; found independently by both readers (ruling 6) | **W2** — the sequence-algebra decision (PT-LEAF-2) inherits both first-party compositions as its acceptance witnesses; W1 oracle row for the 4-outcomes-1-type case |
| **PT-LEAF-22** | NEW (C-miss-2) | `Parser.then` **keeps** `undefined` (unconditional 2-tuple, `parser.ts:90`) while `all()` **drops** it — two sequencing operators with two value algebras in one package, and leaf's own fusion docblock (`:174-176`) advertises the then→all migration without stating it is not shape-preserving. This is the undocumented mechanism the band idiom law (`parser-band.md:108`) precisely routes around, and the missing premise of PT-LEAF-2's cure decision | `parser.ts:90` vs the six drop sites; `leaf.ts:174-176`; also narrows superlative S-5 (below) | **W2** — ONE sequence operator with ONE stated `undefined` algebra; the migration-equivalence claim becomes a property test (MEASURE-AT-OPEN #6) |
| **PT-LEAF-23** | L-M8 | PT-01 confirmed at the bytes and **extended**: `mergeErrorState`'s two array assignments are outside the diagnostics guard — two fresh arrays per furthest-advance on the shipping default path, driven by leaf's seven merge sites; plus the armed tier's `console.error` coupling is uninjectable (`parserDebug` takes a logger; `parseStateInner` does not) | `utils.ts:33-35` (this seat re-read; guard covers only the `expected` line — `dist/diagnostics-DDazRHgl.js:14-16` re-read); merge sites `:16/54/69/142/291/303/360` (7, re-counted); `parser.ts:67-68` | **W2** — R-LAW-3's delivery side: diagnostics are values, allocation-free when disarmed, printed by no one. Incumbent one-line guard rides O-15. Alloc-count table re-measured at W1 (MEASURE-AT-OPEN #3) |
| **PT-LEAF-24** | L-M4 | `fuseAll` allocates the result array **before** the first child runs — every failed `all()` allocates garbage on exactly the reject-path leg `parser-band.md:117` makes a binding bench debt; the cure is a pure hoist (`:199` is the first write) | `leaf.ts:191→193, :222→224, :257→260` (this seat re-read all three arms) | **REFINEMENT FOLD** — W1's reject-path bench leg gains an alloc-count oracle (MEASURE-AT-OPEN #4); W2 implementation law: the deliverable is allocated after the first commit point |
| **PT-LEAF-25** | L-M3 | One API, two element kinds: arity 2-3 return PACKED literals, arity ≥4 permanently HOLEY `new Array(n)` — the IC pollution the module's own comments claim to avoid; arity-4 consumer in-tree (`print.test.ts`). Mechanism confirmed by this seat's probe (N4); cure verified-on-this-box (ruling 3) | `leaf.ts:191/:222` vs `:257`; this seat's node v26.0.0 probe | **REFINEMENT FOLD** — W2 implementation law: one construction shape (`[]` contiguous preferred); W1 pins the elements-kind predicate on the lane's node (MEASURE-AT-OPEN #1) |

### Minors (20) and info (3)

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| L-m1 | `Span` imported dead at `leaf.ts:3` (this seat's read of line 3 confirms; no second occurrence) | NO-WAVE-OWNER (O-15) |
| L-m2 / C-m1 | `eof<T>()` phantom generic; published `Parser<unknown>` (`dist/leaf.d.ts:3`) | W2 — eof RETIREs into a total-consumption judgment (C-§7 sustained: 0 real call sites; its `undefined` value is invisible to `all()`) |
| L-m3 | `any()` arity 1 builds the general closure then discards it (`:58` bare else, `:76`) — one dead closure per construction | folds into PT-LEAF-1/-11's short-circuit deletion |
| L-m4 | `makeParser` dead indirection; generic cast away at **3 of 6** sites (`:24/:78/:163` — count corrected from the challenge's 4, this seat re-derived) | NO-WAVE-OWNER |
| L-m5 / C-m7 | `matchFunction === ""` is an undocumented sentinel: zero-width success, `undefined` value, the regex's consumed length **discarded** (`:341-343`) | W2 — the regex-RETIRE absorbs it |
| L-m6 | "zero-alloc default path" header vs `substring` allocation on every non-empty match (`:315-316` vs `:350`) | NO-WAVE-OWNER (comment truth) |
| L-m8 | `dispatch` conflates non-ASCII with unmapped at the same error label (`:137/:142`) | folds into PT-LEAF-12 |
| L-m9 / C-m4 | `internParser` is `indexOf` inside the entries loop — O(n²) construction | folds into PT-LEAF-10's wrapper |
| L-m10 | All three construction labels are dead weight unless armed (PT-01 corollary; dispatch's is spread+map+2 joins) | W2 — EQ-4 (PT-LEAF-17) |
| L-m11 | **No `test/leaf.test.ts`** — 8 public exports, the package's densest hot path, zero direct spec; the direct cause of PT-LEAF-1/-2/-25 shipping (13 specs exist, none targets leaf — count corrected 12→13, this seat's `ls`) | W1 — the lane's oracle suite is the successor; incumbent gap rides O-15 |
| L-m12 | No declared frame budget; each level costs a frame against PT-04's measured 7,761 ceiling; G-9 "DEPTH IS DECLARED" (`W1.md:544`) | REFINEMENT FOLD — W1 publishes k; superlative 9 is the counterweight (fuseAll *raises* the ceiling) |
| L-m13 | Redundant `state.isError = true` dead stores in fuseAll error arms — **6** sites `:196/204/227/235/243/264` (count corrected from the challenge's prose "4", this seat re-derived) | NO-WAVE-OWNER |
| C-m2 | `regex`/`whitespace` typed `Parser<string>` while the empty-match arm sets `undefined` (`:355`; `dist/leaf.d.ts:27/:29`) | W2 — the trivia/scan redesign absorbs it |
| C-m5 | `string("")` zero-width unconditional success which `all()` **keeps** (unlike `undefined`) — the value-vs-absence asymmetry in miniature | W2 — `succeed` becomes a named operator (band precedent) |
| C-m6 | `_initWhitespace` declared in the published `.d.ts` though no specifier reaches it | NO-WAVE-OWNER (surface noise; the *sealing* is superlative 12) |
| L-M12′ | The re-graded survivor of the overturned L-M12 (killed-claims K1): arity-2/3-vs-general **equivalence** is unpinned by any test, and the unroll comments' figures ("59 all() sites", "dominant 2-way") remain uncited | REFINEMENT FOLD — W1 equivalence oracle (MEASURE-AT-OPEN #6) |
| NEW C-miss-3 | A labelless `mergeErrorState(state)` at `offset > furthest` **erases** an accumulated expected-set (`state.expected = … : undefined`) and reallocates both arrays — reachable exactly when PT-LEAF-3's unmerged EOF failure leaves `furthest=-1`; `any()`'s only merges are labelless (93 consumer refs) | W2 — diagnostic-conservation law: merge may never destroy D |
| NEW L-miss-M4 | `regex()`'s two success paths write offset under two disciplines — absolute (`:349`, immune by construction) vs relative (`:340`, the root of PT-LEAF-4/-9); naming the mismatch yields a single cure | folds into PT-LEAF-4/-9's monotonicity law (cure-shaping) |
| NEW L-miss-M5 | `Parser.state` is per-Parser mutable state a nested parse clobbers before the outer parse renders its error; the packrat half of this hazard got PT-Q1 epochs, this half got nothing | W2 — no operator-owned mutable parse state; NO-WAVE-OWNER for the incumbent |
| NEW C-miss-4 | `string(str: string): Parser<string>` erases the literal type; `string<S extends string>(str: S): Parser<S>` is a one-token, backward-compatible carry-through for the frozen surface's 13+ literal-union types under G-10 | W2 — terminal-tier signature law |
| NEW C-miss-5 | The manifest publishes **no** `main`, no top-level `types`, no `"./package.json"` specifier — exports-only resolution is a silent adoption wall for node10/legacy tooling (`package.json:7-33`, this seat re-read) | NO-WAVE-OWNER (non-breaking manifest add), O-15; W4 rider: the release condition checks resolution matrices |
| C-i1 (INFO) | The consume edge is severed at HEAD; the last-real-consumption census is exact (any 93 · ws 89 · all 61/60 · string 46 · regex 19 · dispatch 6 · eof 1-in-comment · tSW 0; `.trim(ws` 41 / `.skip(ws` 10 / `ws.next(` 7) — re-counted independently by both readers with the POSIX-class trap documented | frame of record for every bound above |
| C-i2 (INFO) | `parsethat-surface-gaps.mjs:4`'s comment is wrong (workspace has no parse-that) but the prototype-local install is shasum-identical 4/4 — RED-7 numbers remain comparable (OP-7) | recorded so no later reader voids the corpus on a false alarm |
| NEW (INFO) | PT-Q5 note's version anchors (0.12.0/0.13.0) are stale below the 1.0.0 artifact | folds into PT-LEAF-19 |

## Killed-claims register — 15 sustained, 1 overturned

| # | killed claim | adjudication |
|---|---|---|
| K1 | L-M12's core: "no benchmark file, no test/benchmarks/ entry, no ledger row" backs the unroll comments | **KILL SUSTAINED, re-verified at the bytes by this seat**: `test/benchmarks/pt-b3-fusion.bench.ts` exists (6,791 B, mtime 2026-07-03 = the 1.0.0 cut), self-declares as the `proof:perf` measurement substrate, benches exactly the two claims (fused all(a,b,c) vs then-chain; first-char dispatch vs any over the real CSS bucket), wired to `scripts/proof-perf.mjs` (--expose-gc, checked in) + `scripts/perf-baseline.json`. Survivor re-graded MINOR (L-M12′) |
| K2 | L-M10/L-i1's cure: "all, eof, trimStateWhitespace are the only three symbols parser.ts needs" | **KILL SUSTAINED** — `parser.ts:6` imports **five**; the omitted two (`whitespace`, `_initWhitespace`) are load-bearing (`:481`, `:711`). Defect stands; cure incomplete (both readers agree; this seat re-read the line) |
| K3 | L-challenge B6: ".trim() with the documented default `discard = false`" reaches the all() branch | **KILL SUSTAINED** — `discard` defaults **true** (`:482`); B6 needs an explicit `false` and **zero** such call sites exist (this seat's grep over parse-that src+test; readers' grep over `164343c1^`) |
| K4 | Both challenges' FLAG_TRIM_WS routing mechanism (L's B7, C-B5's ".trim routes through parser.ts:443-465") | **KILL SUSTAINED** — dead-code attribution (ruling 2): the flag arms are unreachable in-realm; the live path is the `whitespaceTrim` closure. Both rows' *conclusions* survive |
| K5 | C-M2's "the merge is provably dead / the branch is impossible" | **NARROWING SUSTAINED** (both readers, convergent) — the `offset > furthest` branch fires exactly when no arm merged (PT-LEAF-3 supplies it) and is then *destructive*, not inert (erases `expected`, reallocates). C-M2's surviving point — no label slot on the busiest export — is confirmed |
| K6 | L-challenge superlative 4: "any() and dispatch() allocate nothing per invocation" | **KILL SUSTAINED** — false on the reject path (`utils.ts:34-35` unguarded, re-read by this seat); refuted by L-M8 inside the same document. Accepted-path half survives |
| K7 | C-challenge §8's PT-03/O-8 rows: "leaf holds NO process-global mutable state; every export is a pure closure factory" | **KILL SUSTAINED** — `export let whitespace` is module-global mutable state mutated cross-module and in place (`leaf.ts:395/:398`, `parser.ts:711`); the closure-singleton sticky carries mutable `lastIndex` across the user-code boundary. The honest O-8 statement carries both exceptions |
| K8 | C-B9's printed probe block (`" a "` → isError true etc.) | **KILL SUSTAINED** — as ASCII-rendered the receipt is false (space=32 is in `trimStateWhitespace`'s class; this seat re-read `:383`); the rows hold only with invisible U+00A0/U+000B, which markdown renders as space/nothing. Substance survives (PT-LEAF-7); the evidence block must be re-authored with escapes |
| K9 | C-M4's second receipt ("every bucket declared Parser<any> … a cast") | **KILL SUSTAINED, this seat's own re-derivation (ruling 4)** — buckets unannotated; widening via two annotated arms; `:466` is an annotation, not an `as`. The L-re-reader's "both receipts verified verbatim" is corrected. C-M4 survives on one receipt |
| K10 | C-challenge §7's closing: dispatch is "the one thing value.js … could not have hand-rolled cheaply" | **KILL SUSTAINED, spot-verified** — HEAD `src/css/grammar.ts` (483 lines) is the shipping hand-rolled replacement: pre-split (`splitTopLevel :63`) + lowered-name equality, zero LUT/typed-array/charCode dispatch. The megamorphic-any problem is an artifact of the combinator design and does not arise. The AC-3 LUT argument stands on its own merits |
| K11 | "parser-band.md:74-78 states … 'No speed claims outside the printed table'" (cited 3×) | **KILL SUSTAINED** — the sentence is in the G7 gate row (~`:133`); `:74-78` is the HONESTY LAW section with three different bullets (this seat re-read both regions). Substance unaffected; the law is real, in a *gate* row — which strengthens the findings leaning on it |
| K12 | L-B2's fenced block presented as `test/math.test.ts:18-21` verbatim | **KILL SUSTAINED, this seat's own read (ruling 1)** — block-bodied arrow + load-bearing trailing `.opt()` on `:22` omitted. The L-re-reader's "verbatim as quoted" cell is corrected. Row confirmed regardless |
| K13 | L-B1's "that idiom is type-unsound at arity 1 and **neither candidate's gate catches it**" | **KILL SUSTAINED as rescoped** — G8's structural graph-walk already enumerates `all()` children; no evidence either gate lacks an arity check. "Unexercised", not "uncaught". Defect unaffected |
| K14 | Cite/count drift bundle: L-m4 "4 of 6" → **3**; L-m13 "4 sites" → **6**; "12 .ts specs" → **13**; `core.ts:19-26` → `:16-25`; proof-script `:9-11/:30-33` → `:10-11/:29-32`; `W2.md:239` → `:237`; `state.ts:157` → `:158` | **ALL SUSTAINED** — each re-derived by this seat where load-bearing (L-m4, L-m13, spec count, core block) and adopted from convergent reader agreement otherwise. No substantive claim moved |
| K15 | **The L-re-reader's kill of the L-M3 cure** ("self-contradictory with N4; one of N3/N4 must be wrong") | **KILL OVERTURNED by this seat's measurement (ruling 3)** — N3 (`fill` → packed) and N4 (store-by-store → holey) are both true on node v26.0.0: per-slot stores never re-generalize, the `fill` builtin rebuilds the store. The challenge's cure stands verified-on-this-box; the engine-dependence is pinned as MEASURE-AT-OPEN #1; the reader's alternative `[]`-contiguous spelling adopted as preferred |
| K16 | C-challenge S-5's verification method ("preserves the EXACT drop-undefined … semantics of the original", read against `then`) | **NARROWED (new, this seat, from C-miss-2)** — `then` has no undefined filter to compare against and the pre-fusion `all()` is not in the tree at HEAD; what S-5 actually verified (unrolled arms faithful to the general arm) survives and is true |

**Adjudicator's corrections register (against the readers — two moved evidence cells, one overturned a kill, none moved a defect verdict)**: (1) L-re-reader's math.test verbatim cell — corrected (K12/ruling 1); (2) L-re-reader's C-M4 both-receipts cell — corrected (K9/ruling 4); (3) L-re-reader's L-M3-cure kill — overturned (K15/ruling 3); (4) C-re-reader's FLAG-branch prose inside its own correct kill — flagged as citation-inheritance-shaped, verdict intact (ruling 2c); (5) C-B9 ruled BLOCKER over the L-re-reader's MAJOR, C-B7 ruled MAJOR over the C-axis BLOCKER, C-B3/C-B4 split (ruling 5). Per the ColorSpaceSelector precedent these are recorded as fleet evidence: both readers' underlying instruments were otherwise re-derived clean, and the two readers' *disagreements* were exactly where the truth needed a third instrument.

## Superlatives — sustained, with scoping (L-18 runs both ways)

1. **`string()`'s single-char fast path** (`leaf.ts:282-294`): one charCode compare, the **shared `str` reference** stored on success — zero allocation on the hottest leaf in any grammar.
2. **`regex()`'s default path picks `test()` over `exec()` AND writes the offset absolutely** (`:344-353`): the second half is the unclaimed better half — the fast path is *structurally immune* to both offset-corruption classes that kill the custom-match arm (the readers' root-cause reframe, adopted).
3. **`trimStateWhitespace` as code** (`:372-391`): zero-alloc, no regex engine, fast-exit at `:378`, explicit code-point comment. Its only sin is being one of three alphabets.
4. **`dispatch`'s one-expression guard** (`:136-137`): `charCodeAt` past EOF → NaN → `NaN < 128` false → labelled error — EOF guard, non-ASCII guard, and unmapped-key path in one branchless expression.
5. **`Int8Array(128).fill(-1)` is the right structure** — the width is the bug, the design is not; the one artifact with a clean zero-import Wasm lowering (AC-3's literal shape).
6. **The PT-Q5 RETRACT note's honesty discipline** — a shipped seam removed for having zero consumers, re-introduction condition named precisely. Its location (PT-LEAF-19) and stale anchors are the defects; the *posture* is the house standard.
7. **The bench discipline exists — contra the L-challenge**: `pt-b3-fusion.bench.ts` + `proof:perf` + a checked-in baseline measure exactly the two claims the unroll comments make (K1, re-verified by this seat).
8. **Construction-time-constant labels** (`:127-132/:278/:324`): zero error-path allocation, nothing printed, no global touched — leaf's diagnostic *production* satisfies R-LAW-3 in full; the whole PT-01/EQ-4 problem is delivery-side.
9. **`fuseAll` raises the recursion ceiling**: n children threaded in ONE frame vs then-chains' n−1 live nested frames — every fused sequence buys back PT-04 budget; a "simplification" back to then-chaining would *lower* the ceiling. (Structural; the depth falsifier rides MEASURE-AT-OPEN #5.)
10. **Every leaf success path writes `state.value` explicitly** (9 sites) — the drop-filter can never read a stale value: PT-LEAF-2 is a *policy* defect atop an airtight state discipline.
11. **The arity-0 identities are algebraically correct with no arity-0 arm written**: `all()` → `ok([])` (unit of sequencing), `any()` → labelled failure (unit of choice) — correct by construction. And **`_initWhitespace` is correctly sealed** (absent from both barrels, no wildcard specifier — the house rule the rest of the surface should be held to).

## MEASURE-AT-OPEN residue — bench-dependent rows for the lane's W1 harness

Inherited law: one fresh process per cell (`W1.md:94-96`); nothing armed; every probe below is disposable-PID.

1. **Elements-kind predicate (PT-LEAF-25 / K15)** — re-run this seat's five-predicate `--allow-natives-syntax` probe on the lane's **pinned** node; this box (v26.0.0): fill→packed, store-by-store→holey, literal→packed, `out.length=w`→stays packed. The W2 implementation law pins ONE construction shape against the measured predicate, not folklore.
2. **Offset-monotonicity oracle (PT-LEAF-4/-9)** — the readers' static traces are adjudicated sound but the specific probe offsets ("offset 8" / "offset 6") were never re-measured under the static law; W1's oracle asserts `result.offset ∈ [entry, src.length]` over every operator including a hostile `matchFunction` pair (null-arm synth; re-entrant).
3. **Diagnostic realloc counts (PT-LEAF-23)** — the L-challenge's table (2 arrays/furthest-advance; 40 arrays/88-char corpus) is adopted as mechanism, its magnitudes re-measured on the W1 rig's reject-path leg.
4. **Reject-path alloc hoist (PT-LEAF-24)** — band debt 2's own leg; before/after alloc-count under `--expose-gc` via the existing `proof:perf` mechanism.
5. **Fused-vs-chained depth ceiling (superlative 9's falsifier)** — depth-to-RangeError for `Parser.lazy` bodies fused vs then-chained, against PT-04's 7,761 baseline.
6. **Arity-equivalence property test (PT-LEAF-1/-22/L-M12′)** — arity 2/3 unrolls vs the general arm, and then-vs-sequence shape equivalence, as a generated property suite (the missing `leaf.test.ts`'s successor).
7. **The non-ASCII whitespace sweep (PT-LEAF-7)** — the readers' probe-design note is adopted as law: ASCII corpora cannot separate the three classes; W1's totality oracle carries the NUL/C0/NBSP/BOM/U+2028/VT code-point row, with escapes, never raw glyphs (K8's lesson).
8. **Module-edge assertion (PT-LEAF-17's W4 rider)** — the release gate imports the shipped `./core` in a bare process and asserts the loaded-module set, not `existsSync`.
9. **Dispatch probe-design law** — arms must be total on any first character (`regex(/./).map(tag)`) so a mis-dispatch is *observable*; both challenge seats independently hit the masking trap (C-m3, C-B7) and the lane inherits the lesson as harness law.

## Closing verdict

Both reader returns **survive the incredulity seat with corrections that sharpen rather than reverse**: of 36 confirmation cells across the two returns, 36 are sustained (three severity relabels under rulings 5a-5c); of 24 kill cells, 23 are sustained and **one is overturned by this seat's own measurement** (K15 — the only place either reader manufactured a contradiction that wasn't there); all 10 admitted misses are adjudicated real, two of them found independently by both readers from opposite axes (PT-LEAF-21 — the strongest evidence class in the corpus, and a row neither original challenge stated).

The module's adjudicated shape is unchanged from the challenges' concord and now carries the adjudicator's seal: **five genuinely excellent terminals and one right data structure, wrapped around three n-ary combinators whose declared types do not describe their runtime behavior, one dead 40-line control-flow machine, and a whitespace story that is wrong three different ways at once.** The terminals' discipline (superlatives 1-4, 8, 10) is the best allocation-and-state hygiene in the package; the combinators shipped a type lie the sole consumer paid for in a production crash, three defensive comments, a band-wide idiom law, and a structural gate built specifically to route around six lines of `leaf.ts`. Every consumer-visible defect routes: **W2** owns the algebra-class rows (the sequence/undefined decision PT-LEAF-2/-21/-22, the choice-family split PT-LEAF-11, the terminal totality + trivia + monotonicity laws PT-LEAF-3/-4/-7/-9, the dispatch wrapper requirements PT-LEAF-6/-8/-10/-12/-13, the type-soundness law PT-LEAF-15, EQ-4 delivery PT-LEAF-17/-23); **REFINEMENT FOLD** sharpens W0 (src-consumption load-order, PT-LEAF-14), W1 (the `.parse()` ban PT-LEAF-16, the boundary-guard respecification PT-LEAF-5, the bench legs PT-LEAF-24/-25), and W4 (edge-asserting release gates); **NO-WAVE-OWNER** carries the incumbent repairs (dead surface PT-LEAF-18/-20, doc truth PT-LEAF-19, manifest fallback) on the owner-held O-15 channel. Nine dynamic rows are banked to MEASURE-AT-OPEN under W1's law. Nothing lost: head, tail, or interval.

— end of apotheosis. Both challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
