claude-fable-5

# pt-leaf — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ master `ef10d5b` (re-verified this session; `git status --short` dirty rows are `rust/**`, `README.md`, `.cargo/config.toml`, `.gitmodules`, `docs/**` — `typescript/` has **zero** dirty rows), `typescript/src/parse/leaf.ts` = 399 lines / 14,876 B / mtime 2026-07-03, byte-identical to what both challenges and both readers audited · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two fresh reader returns over the module's two challenge files — reader-α (`L-18-hostile-reread-leaf-library`: 23 confirmation cells · 7 kills · 5 misses · 12 superlatives) and reader-β (`leaf`: 25 confirmation cells · 6 kills · 7 misses · 12 superlatives). Both challenge files (`audit/parse-that-modules/leaf/challenge-L-library.md`, 459 lines; `challenge-C-consumption.md`, 842 lines) were re-read **whole** by this seat.
**Supersession (recorded, nothing lost)**: this record supersedes the 2026-08-05 r1 apotheosis at this same path, which adjudicated a *different* reader corpus (`leaf-L18-hostile-reread-library` / `leaf-C-consumption-L18-rereader`). The r1 roster identities **PT-LEAF-1..25** and its minors/infos/kills are preserved for life (anti-rename); r1 rulings are carried where this corpus does not touch them and are **corrected in exactly one place** (ruling 2 below: r1's ruling-4 preservation of C-M4 on one receipt is overturned at the archive bytes). Where r1's content is restated here it was re-verified, not copied, except where marked *(r1-carried)*.
**Method (incredulity)**: every cross-reader disagreement, every kill of a load-bearing cell, and every NEW missed row was re-derived against the bytes by this seat with its own instruments before adoption; suspiciously-agreed cells (the "verbatim" math.test cite, the census table, the dead-FLAG greps) were independently re-measured, not spot-trusted — and one suspiciously-agreed cell **failed** (ruling 3). Nothing in parse-that was armed (no `enableDiagnostics`, no `memoize`); **zero parse-that code was executed by this seat** — every adjudication below is static (read + grep + git archaeology). Readers' transcription-probe measurements are adopted as corroboration only, never as the deciding instrument where a source derivation exists. Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (all read-only)**: full read of `leaf.ts` (whole, 399 — every cited anchor checked, incl. `:172`'s "59 `all()` sites", `:321-324`'s sticky re-flag + label, the 9 `as ParserFunction` casts, the 16 `as ParserState<unknown>` casts, the 6 dead `isError` stores); `parser.ts:1-30` (five-symbol leaf import at `:6`; phantom `ParserFunction<T>` `:14-16`; `FLAG_*` `:20-22`), `:75-105` (then keeps `undefined` at `:90`), `:128-140` (chain, no restore), `:185-210` (skip), `:235-245` (opt merges then `ok(undefined)`), `:255-266` + `:305-320` (non-merging failure arms — the C-M2-kill deciders), `:380-400` (wrap `!discard` → `all`), `:435-525` (call/trim whole — the FLAG decider), `:530-545` (many zero-progress guard), `:700-712` (`_initWhitespace()` at `:711`); `state.ts:40-60` (`furthest=-1`, `ok +=`, per-construction `suggestions`/`secondarySpans` arrays), `:88-100` (`unsafeCall` vs `unsafeCallRaw`), `:150-180` (`"trimWhitespace"` `:157`, `"whitespace"` `:158`, mutable `ParserContext.name?`); `utils.ts:25-50` (mergeErrorState whole — the two unguarded array writes), `:155-168` (`skipWhitespace` `<=32` at `:159-164`); `core.ts` whole (leaf block `:16-25`); `src/parse/index.ts` whole (diagnostics+`skipWhitespace` on `:5`, leaf on `:9`); `package.json` (`version` `:3` = 1.0.0, `sideEffects:false` `:6`, `proof:*` `:39-49` — **`proof:perf` at `:48`, included in `proof:all` at `:49`**); `ls test/` (**13** `.test.ts` specs, no `leaf.test.ts`) + `ls test/benchmarks/` (`pt-b3-fusion.bench.ts` **exists**, 6,791 B, mtime 2026-07-03); `pt-b3-fusion.bench.ts:1-20/:35-60/:100-145` (axis 1 = `fusedAll3 = all(pa,pb,pc)` vs `unfusedThen3 = pa.then(pb).then(pc)`; axis 2 = first-char dispatch vs sequential `any()` over the real CSS function bucket; axis 3 = JSON regression anchor whose own table writes `"-"` beside `"0-9"` at ~`:140-141`); `scripts/proof-perf.mjs:1-32` (clauses A / B′ / C verbatim; `--expose-gc`; imports `dist/parse.js`) + `scripts/perf-baseline.json` (522 B, checked in); `test/math.test.ts:14-40` (the verbatim decider); `test/debug.test.ts:105-120` (the vacuous-flag-test decider); repo greps: `.flags` **writes** = 1 (`parser.ts:496`; `:438`/`:442` are reads), `Parser.call(` callers = 2 (`parser.ts:493` inside the discarded closure + `state.ts:93` inside `unsafeCall`), `unsafeCall` callers = **0** (`unsafeCallRaw` is a different method, 5 sites), `FLAG_EOF` assignments = **0** (defined `:22`, read `:466`, never set), `Span` in `parser.ts` = the type-import at `:2` only (`secondarySpans` `:64` is a different identifier), `.trim(x,false)`/`.wrap(a,b,false)` sites in src+test = **0**; dist: `ls -la` (**all artifacts mtime 2026-07-29 14:20** vs src 2026-07-03), `core.js:1` → `packrat-entry-CS1td-8B.js`, whose line 1 → `diagnostics-DDazRHgl.js`; `PACKRAT_ARMED` `:678/:682/:714/:722`; `:881` guard / `:882` `console.error`; `diagnostics-DDazRHgl.js:14-16` (guard covers only `:14`); `dist/leaf.d.ts` whole (all cites exact: `:3/:4/:5-23/:24/:25/:27/:28/:29/:30`); value.js archive @ `164343c1^`: `src/parsing/index.ts:158-163` (keyword shields), `:205-216` (crash 1, verbatim), `:430-470` (**the C-M4 decider** — buckets unannotated; `fnGeneric`/`fnDashed` are the only `Parser<any>` annotations; `Function_:` is an annotation, not an `as`); `color/color.ts:125-132` (the sole `matchFunction` site), `:165-172`, `:298-316` (**`colorMixColorPct` verbatim** — annotation + `as` of a fixed 2-tuple over a 1-or-2-arity `all()`), `:640-675` (`dispatchTable: Record<string, Parser<ValueUnit>>` author-declared homogeneous; 5 literal keys + 52 looped letters = 57 entries; `:673` `as Parser<ParsedColorUnit>` narrowing + the 10-line comment); `math.ts:140-152` (crash 2, verbatim); `stylesheet/stylesheet.ts:272-282` + `:526-536` (leading-`opt()` tail-detection compensations, verbatim); `timeline/scroll-timeline.ts:180-186` (both-optional + compensation comment); consumption census re-run with BOTH instruments (bounded call sites `(^|[^.[:alnum:]_])sym\(`: any **93** · all **61** · string **46** · regex **19** · dispatch **6** · eof **1** · trimStateWhitespace **0**; the challenge-§1a prefix-only recipe: any **183** occurrences / string **454** occurrences / 172·423 lines); `whitespace` refs **89** = `.trim(` **41** · `.skip(` **10** · `.next(` **7**; HEAD: `src/parsing` absent, `package.json` `parse-that` hits = 0; `INBOX.md:77` (O-15 verbatim: 1.1.0 ask owner-held, "NO code change requested", PT-01 `:881` cite, PT-04 7,761/7,762, PT-07 both sentences).
**STOP/law compliance**: `/Users/mkbabb/Programming/parse-that-css-totality-p2` → `No such file or directory` (re-verified this session; not created). parse-that touched read-only, main checkout only. **Sole write = this file.**

## Scoreboard

**53 adjudicated roster rows — 7 BLOCKER · 19 MAJOR · 22 MINOR · 5 INFO** (r1's 48 identities preserved; one row re-graded MAJOR→MINOR by kill; three new rows admitted from this corpus's misses; two new INFOs). **18 killed-claims register entries, all SUSTAINED** — including one kill (K6) that **overturns an r1 ruling**, and one r1-carried kill (K3) that **both fresh readers missed**. **12 missed rows ADMITTED** (5 from reader-α, 7 from reader-β; three become new roster rows, the rest sharpen existing cells). **12 superlatives sustained** (r1's 11 + one new from reader-α). **5 adjudicator corrections against the readers** — the largest being a *repeated citation-inheritance event*: both fresh readers re-inherited the same false "verbatim" cell that r1's ruling 1 had already corrected in the prior corpus.

Blocker-weight survivors of record, unchanged from r1 and re-affirmed by this corpus: **PT-LEAF-2** (`all()` drops `undefined` and compacts — live in the library's own green suite, paid for by **two production crashes** and three consumer post-mortems, now with the positional law that explains which sites crashed) and **PT-LEAF-7** (three mutually inconsistent whitespace alphabets, none CSS's, `.trim()` silently substituting a third for the parser the caller passed — live at 41 of the sole consumer's own sites). Realized in-constellation impact today is **zero at every hop**: the consume edge was severed at `164343c1` (C-i1, re-measured this session), so every blocker is a defect of the **published 1.0.0 contract** and of the **X·P adoption premise**, not a shipping outage — which is exactly why the new PT-LEAF-27 (semver pricing) matters.

## The incredulity seat's own work — ruled disagreements and corrections

The two readers disagreed, or one inherited a false cell, at seven load-bearing points. Each was re-derived by this seat before ruling.

1. **L-M12 — the head-on contradiction. RULED for reader-α; reader-β's confirmation cell CORRECTED.** Reader-α **killed** the challenge's premise ("No benchmark file, no `test/benchmarks/` entry"); reader-β **confirmed** the row at MAJOR with the cell "package.json:39-45 proof:\* scripts cover manifest/no-css-surface/subpath/packrat×4 — none targets leaf arity." At the bytes: `test/benchmarks/pt-b3-fusion.bench.ts` **exists** (6,791 B, mtime 2026-07-03 = the 1.0.0 cut), self-declares "the MEASUREMENT substrate for `proof:perf`", and its axis (1) benches precisely the fusion claim under attack (`fusedAll3 = all(pa,pb,pc)` vs `unfusedThen3 = pa.then(pb).then(pc)`), axis (2) benches first-char dispatch vs sequential `any()` over the real CSS function-name bucket, axis (3) anchors a JSON regression floor; it is CI-gated at **`package.json:48`** (`proof:perf`, `--expose-gc`), included in `proof:all` (`:49`), with `scripts/proof-perf.mjs` clauses (A)/(B′)/(C) asserting the fusion shape two ways against a checked-in `scripts/perf-baseline.json`. Reader-β's evidence range stopped at `:45` — an under-inclusive read that endorsed a false premise; note the challenge row even contradicts *itself* (its own falsifier lists "perf" among the `proof:*` scripts). **Kill SUSTAINED** (convergent with r1's K1). Surviving core re-graded **MINOR (L-M12′)**: the arity-2/3 **unroll vs the general arm** is benched nowhere (the bench compares fused-vs-then-chained and dispatch-vs-any, not arm-vs-arm), no test pins arm equivalence, and the one falsifiable number in the unroll comments — `leaf.ts:172` "59 `all()` sites" — is **measurably false by 2** (61 at `164343c1^`, this seat's census). The true half of reader-β's row (no `test/leaf.test.ts`; 13 specs, none targeting leaf) stands as L-m11.
2. **C-M4 — RULED for reader-β; reader-α over-credited; r1's ruling 4 OVERTURNED.** Reader-α confirmed the C-M4/C-M5/C-M8 bundle including "forcing casts at the parse boundary"; reader-β killed both receipts. This seat read the archive bytes directly. (a) `164343c1^:src/parsing/index.ts:449-461`: the buckets (`bucketT`…`bucketDash`) carry **no annotations** — they are inferred from `any(...)`; only the upstream arms `fnGeneric` (`~:437`) and `fnDashed` (`~:444`) are declared `Parser<any>`, and `:466` `const Function_: Parser<any> = dispatch({` is an **annotation, not an `as`**. The challenge's "every bucket declared `Parser<any>` (`:455-464`)" is false at the bytes. (b) The receipt r1's ruling 4 preserved — `color/color.ts:673` `as Parser<ParsedColorUnit>` — is a **narrowing past the author's own homogeneous declaration**: `:645` declares `const dispatchTable: Record<string, Parser<ValueUnit>>` by hand, so the proposed heterogeneous signature (`T[keyof T] extends Parser<infer V> ? V : never`) yields `Parser<ValueUnit>` — **identical** — and the cast survives the cure; the 10-line comment at `:663-672` says the cast codifies a construction invariant (every arm builds `ParsedColorUnit`), not a homogeneity concession. The row's own falsifier ("would make both receipts cast-free") fires against it. **C-M4 (PT-LEAF-13) re-graded MAJOR→MINOR**: the bare type fact stands (a `Record<string, Parser<T>>` cannot express a heterogeneous table; `any()` infers a union) with **zero consumer receipts**; the W2 wrapper requirement (v) survives on design-forward grounds only. This supersedes r1's "survives on one receipt".
3. **The math.test.ts "verbatim" cell — BOTH readers CORRECTED (a repeated citation-inheritance event).** Reader-α: "The in-tree site is verbatim at test/math.test.ts:18-21"; reader-β: "Live in parse-that's own suite: test/math.test.ts:18-21 verbatim `all(…).map(([, exponentSign, exponent]) => …)`". This seat's read of `test/math.test.ts:18-22`: the live source is a **block-bodied** arrow with an explicit `return`, and the binding carries a **trailing `.opt()` on `:22`** that the challenge's fenced quote omits — exactly the correction r1's ruling 1 made against the *prior* corpus. Two fresh readers re-inherited the challenge's reshaped block as "verbatim" rather than re-reading five lines of test file. The **substance survives intact and is re-derived clean by this seat**: on `"…e2"` the inner `all(regex(/[eE]/), regex(/[-+]/).opt(), digits)` yields `["e","2"]` (sign dropped, digits shifted into the sign's position), the map emits `"e2undefined"`, and the suite is green only because `parseFloat` truncates at `'u'` (`Number()` yields `NaN`); the trailing `.opt()` is itself load-bearing — the outer `all(numberPart, exponent)` drops the absent exponent too, caught only by `:35`'s `?? ""`. Verdicts unaffected; the cells are corrected; the event is recorded as fleet evidence per the ColorSpaceSelector DISSENT-1 precedent.
4. **The `.trim()`/FLAG tangle — reader-α's kill and missed-row SUSTAINED at the bytes; reader-β's naming slip corrected.** This seat's read of `parser.ts:435-525` + the grep triple settles it: `trim()`'s tag branch (`parser.ts:488`, testing `parser.context?.name === "whitespace"`, tag set at `leaf.ts:398`) constructs `flaggedParser`, writes the repo's **only** `.flags` assignment onto it (`:496`), *then never references it again* — the returned Parser is built from the `whitespaceTrim` closure (`:498-517`). `Parser.call()`'s only callers are `:493` (inside the discarded object) and `state.ts:93` (inside `unsafeCall`, which has **zero** callers); `FLAG_EOF` is **never assigned anywhere in the tree**. So both challenges' FLAG-routing cites are dead-code attribution (kill K4), ~40 lines of `call()`/FLAG machinery are unreachable in-realm, and the one in-tree invocation — `test/debug.test.ts:115` `pEof.call(state3)`, comment-labelled "Exercise the EOF flag path via call()" — carries `flags === 0`, takes the `:438` fast path, and asserts a label that actually comes from `eof()`'s own merge at `leaf.ts:16`: **a vacuous test that would stay green if `call()` were deleted** (reader-α's missed row, verified). Reader-β's C-B9 mechanism prose is exact ("the returned closure at parser.ts:498-517 calls trimStateWhitespace and NEVER invokes the passed parser"), but its C-B5 cell's phrase "dodges it via the **FLAG_TRIM_WS** tag branch" repeats the dead-mechanism *name* inside an otherwise-correct cell — a citation-inheritance-shaped slip, verdict unaffected, recorded.
5. **Severity contests — five rulings (the two readers split five grades).** (a) **C-B6/L-M2 (range hijack): BLOCKER sustained** (reader-β's grade; reader-α's MAJOR recorded as the reachability dissent — both live tables dodge it). Grounds: the trigger key `"+-."` is the CSS number-start set *verbatim*, the failure silently admits a separator as a value head, there is no escape and no documented precedence, and the package's own bench table writes a range key beside a bare `-` key — the trap is one keystroke from first-party code. (b) **C-B7 (digit-key hoisting): MAJOR sustained** (reader-α's grade; reader-β's BLOCKER recorded as dissent). Both live tables dodge it; construction-time-throw cure; but top-of-MAJOR because the JSDoc names `"0-9"` as its own example and the sole consumer wrote the override contract down as "verified against parse-that's dispatch" — true for letters, totally inverted for digits (reader-β's sharpening: inversion holds in *either* insertion order, letters honour last-wins — re-derived from OrdinaryOwnPropertyKeys). (c) **C-B3/L-M14 vs C-B4/L-M5 — the r1 split SUSTAINED**: the null-arm negative delta needs only a caller honouring the declared `RegExpMatchArray | null` signature → **BLOCKER (PT-LEAF-4)**; the re-entrancy desync needs a re-entrant `matchFunction` → **MAJOR (PT-LEAF-9)**. Reader-α's flat MAJOR on both is preserved as the zero-call-site dissent (the sole historical site, `color/color.ts:129`, returns `null` and dodges both — re-read this session). (d) **C-B8 (non-string postures): BLOCKER sustained** (reader-β; reader-α folded it at MAJOR) — it falsifies the corpus premise (PT-07 "5/5 throw", verbatim at `INBOX.md:77`) that the debt-4 boundary guard is specified from, and `whitespace` *silently succeeds* (`0 >= undefined` → ToString → empty-match arm, derived from source). (e) **C-B9/L-M7 (whitespace family): BLOCKER sustained** (reader-β; reader-α's MAJOR preserved as the ASCII-invisibility bound — all three classes agree on ASCII, so the divergence is invisible to any ASCII corpus → MEASURE-AT-OPEN #7).
6. **The census recipe (§1a/§10) — reader-β's kill SUSTAINED, with an instrument caution against the killer.** This seat ran both instruments at `164343c1^`. The table's numbers are **bounded call-site counts**: `(^|[^.[:alnum:]_])sym\(` reproduces **93/61/46/19/6/1/0 exactly** (eight of eight rows). The doc's *stated* recipe (prefix boundary only, no trailing bound) yields **183** `any` occurrences — the very "inflated" figure §10 blames on `\w`-vs-POSIX — so §10's lesson misdiagnoses the cause (the missing trailing paren, not the class spelling) and, written as it is to be carried forward, would make the next seat "correct" right numbers into wrong ones. Reader-α's framing ("re-run with the corrected POSIX class") is harmlessly incomplete — its own printed rows carry the `(` so it ran the correct instrument. Caution against reader-β: its claimed exact reproduction of the inflated `string=443` did **not** reproduce under four instrument variants here (454 occurrences with and without line-start; 423/172 line-counts) — the kill's substance is unaffected, but the register law is: **state the counting unit (occurrences vs lines) beside the pattern**, or the number is not portable.
7. **Probe-dependence of the L-M5 numerals — reader-α's caution ADOPTED.** The desynchronized offset equals `lastIndex` of whatever nested parse ran last minus `savedOffset` — a property of the **probe**, not the module: 8 on an 8-char nested source (the L-challenge's shape), 1 (C-challenge) or 6 (its seat-3) on others. Recorded so a later seat reproducing one number does not "correct" a right row into a wrong one; the invariant, not the numeral, is what W1's monotonicity oracle asserts (MEASURE-AT-OPEN #2).

**Adjudicator's corrections register (against this corpus's readers)**: (1) reader-β's L-M12 confirmation cell — corrected (ruling 1); (2) reader-α's C-M4 "forcing casts" cell — corrected (ruling 2); (3) **both** readers' math.test "verbatim" cells — corrected (ruling 3, a repeated inheritance event); (4) reader-β's "FLAG_TRIM_WS tag branch" naming slip — corrected, verdict intact (ruling 4); (5) reader-β's §10-kill reproduction number — instrument-variance noted (ruling 6). One r1-carried kill **neither fresh reader found**: K3 — the L-challenge's B6 parenthetical asserts a `discard = false` default; the byte truth is `discard: boolean = true` (`parser.ts:~482`), B6 requires an explicit `false`, and there are **zero** such call sites in parse-that src+test (this seat's grep, re-run this session). Per the ColorSpaceSelector precedent these are fleet evidence: the readers' instruments were otherwise re-derived clean, and their *disagreements* were exactly where the truth needed a third instrument.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/corpus/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec rather than landing in a unit · **NO-WAVE-OWNER** where no X·P wave owns it (incumbent parse-that repair; evidence rides the standing O-15 letter channel, owner-held — and per PT-LEAF-27, *split by semver class*). X·P executes nothing until the owner's begin-word; every routing is a booking, not an act. Line anchors are `ef10d5b`; re-resolve before any cure (D-19).

### Blockers

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-LEAF-1** | L-B1 / C-B1 + α-miss | `all()` at arity 1 returns the child's bare value against a published mapped-tuple `.d.ts` — and (re-found independently by reader-α) diverges in **four** dimensions at once: declared-vs-actual type, value shape, `undefined` handling (arity 1 returns raw `undefined` where arity ≥2 would drop it — a *third* behaviour), and backtracking (none of fuseAll's six restores is in the path) | `leaf.ts:161` raw `parsers[0].parser` vs restores at `:195/:226/:263`; `dist/leaf.d.ts:25`; cast-laundering `:163`; **consumer receipt (β-miss, verbatim this session)**: `164343c1^:color/color.ts:304-309` `colorMixColorPct` — the fixed 2-tuple written TWICE (annotation + `as` over the very `all()`) for a parser whose runtime arity is 1-or-2 | **W2** — the algebra's `sequence` admits no arity short-circuit; arity 1 is a spec case with a stated shape AND a stated rollback law (EQ-5). W1 rider: arity-0/1/2/3/N equivalence oracle (MEASURE-AT-OPEN #6) |
| **PT-LEAF-2** | L-B2 / C-B2 | `all()` drops every `undefined`-succeeding child and compacts left: declared arity fixed, runtime arity input-dependent; **live in the library's own green suite** (`"e…undefined"` masked by `parseFloat`); paid for by the consumer in **two production crashes** + three prose post-mortems + a band-wide idiom law + a structural gate | drops `leaf.ts:199/207/230/238/246/267`, truncations `:209/:248/:270`; producers `parser.ts:241` (opt), `leaf.ts:342/:355`, `:14` (eof); `test/math.test.ts:18-22` (corrected cite, ruling 3); crash receipts verbatim this session: `164343c1^:index.ts:205-216` ("stops is not iterable", **leading** position) and `math.ts:144-150` ("args is not iterable", **leading** position); `parser-band.md:108` idiom law + `:134` G8 exist because of these six lines | **W2** — the decision the algebra turns on: ONE sequence operator, absence explicit, `.opt()`-under-sequence legal again, G8 retired. PT-LEAF-22 is its missing premise; **PT-LEAF-28 is its positional law** |
| **PT-LEAF-3** | L-B3 / C-B5 + L-M6 | `regex()` rejects unconditionally at EOF **without merging**, so every nullable pattern — including exported `whitespace` (89 consumer refs) — fails at EOF with `furthest=-1`, and the rendered error points at the backtracked offset (truncated input, the commonest authoring error, displays offset 0) | `leaf.ts:327-330` (no merge; contrast `:360/:291/:303/:16`); `leaf.ts:397`; `state.ts:52`; `parser.ts:60`; the `test()` path `:344-357` already handles EOF correctly, so the guard is a semantics-changing fast exit; reachability shield corrected per kill K8: **keyword shields** (`164343c1^:index.ts:159-162`), not parens | **W2** — terminal-tier totality law (COMP-1) + the RETIRE-regex ruling (C-§7, sustained *as rescoped by the new INFO row*); the furthest-merge asymmetry becomes a diagnostic-conservation law. W1 rider: EOF-inclusive corpus row. Incumbent one-line deletion rides O-15 — **as a 2.0.0 event, never a minor** (PT-LEAF-27: it widens acceptance) |
| **PT-LEAF-4** | L-M14 / C-B3 | The documented `null` arm of `matchFunction` makes the offset delta **negative** (failed sticky exec resets `lastIndex` to 0): a *successful* parse whose cursor rewound to absolute 0 — input consumed **twice** (`["aaa","FB","aaa"]` from a 4-char input); every downstream monotonicity guard is one-sided and blind to it. BLOCKER per ruling 5c (reader-α's MAJOR = the zero-call-site dissent) | `leaf.ts:319/:337-340`; `state.ts:55-57`; guards `parser.ts:538/:605`; sole historical site `164343c1^:color/color.ts:129` returns `null` and dodges (re-read this session) | **W2** — explicit **offset-monotonicity law** on every operator; W1 monotonicity oracle (MEASURE-AT-OPEN #2). Incumbent two-line cure rides O-15 (1.1.0-eligible: strictly narrows a corruption, changes no accepted parse) |
| **PT-LEAF-5** | C-B8 | Three postures toward one non-string input class, and the most-consumed export **silently succeeds** (`whitespace.parseState(42)` → `isError:false` via `0 >= undefined` → ToString → empty-match arm) — falsifying the corpus premise (PT-07 "5/5 throw", verbatim `INBOX.md:77`) the debt-4 boundary guard is specified from. BLOCKER per ruling 5d | `leaf.ts:327` / `:344` / `:354-357` (source-derived); contrast `:285/:297/:136` (raw TypeError); `parser-band.md:119` debt 4 | **REFINEMENT FOLD** — W2's JS-boundary guard re-specified as a **positional `typeof` check at entry**, never throw-inference; W1's boundary corpus gains the eight-probe row |
| **PT-LEAF-6** | C-B6 / L-M2 | Any 3-char dispatch key with middle `-` is silently a range with no escape and no precedence note: `"+-."` — the CSS number-start set, verbatim — admits `','` (a separator, charCode 44 ∈ 43..46) as a value head; reversed ranges map nothing silently; the in-tree bench itself writes a range key beside a bare `-` key. BLOCKER per ruling 5a (reader-α's MAJOR dissent recorded) | `leaf.ts:116-119`, JSDoc `:86-88`; `pt-b3-fusion.bench.ts:~140-141` (re-read this session) | **W2** — dispatch-wrapper requirement (i): explicit range constructor, throw on reversed/ambiguous keys; the LUT is re-authored, never imported (C-§7 sustained) |
| **PT-LEAF-7** | C-B9 / L-M7 + L-m7 | **Three mutually inconsistent whitespace alphabets ship in one package, none of them CSS's** ({09-0D,20} / JS `\s` incl. NBSP-BOM-LS / `<=32` incl. NUL+C0 — all three on the root barrel, `index.ts:5/:9`), and `.trim()` silently substitutes the charCode scanner for the parser the caller passed, dispatching on a **mutable public string tag** (`state.ts:158` + `name?`) — a forged `context.name = "whitespace"` silently discards the supplied parser. No mechanism has a complement channel: COMP-1 unsatisfiable **by construction**. BLOCKER per ruling 5e (reader-α's ASCII-invisibility bound preserved inside the row) | `leaf.ts:383/:397-398`; `utils.ts:159-164`; `parser.ts:481/:488/:498-517` (the live route is `whitespaceTrim`, NOT the FLAG machinery — kill K4); 41 consumer `.trim(whitespace` sites (re-measured: 41/10/7 of 89) | **W2** — span-emitting `trivia` operator, class fixed by css-syntax-3 §4.2 **citation** (never harvested from any in-tree class); all three mechanisms retire together; tag-dispatch banned (identity, not string, selects behavior). W1 rider: non-ASCII sweep (MEASURE-AT-OPEN #7) |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-LEAF-8** | C-B7 | `Object.entries` integer-key hoisting makes the JSDoc's own example range `"0-9"` unoverridable by a digit key **in either insertion order**, while letters obey last-wins — silently inverting the override contract the consumer verified and wrote down (`164343c1^:index.ts:463-465`, verbatim). MAJOR per ruling 5b (reader-β's BLOCKER dissent recorded) | `leaf.ts:113/:87`; ECMA-262 OrdinaryOwnPropertyKeys; both live tables dodge (the 57-key color table has no single-digit override — re-read) | **W2** — wrapper requirement (ii): tables are ordered `Array<[key, parser]>` or `Map`, never a plain object |
| **PT-LEAF-9** | L-M5 / C-B4 | `regex()` reads the closure-singleton `sticky.lastIndex` **after** invoking arbitrary user code — a re-entrant `matchFunction` desynchronizes value from offset (past `src.length`; `eof()` then spuriously succeeds). MAJOR per ruling 5c; the numerals are probe-dependent (ruling 7) | `leaf.ts:322/:333/:337-340`; the packrat tier got epoch save/restore for exactly this class, the leaf that hands control to user code got nothing | **W2** — same monotonicity/re-entrancy law as PT-LEAF-4; regex RETIREs from the terminal tier (the AC-1/AC-2 host-object bet — see the new INFO row for the honest statement). MEASURE-AT-OPEN #2 |
| **PT-LEAF-10** | L-M1 / C-m3 | `dispatch()`'s Int8Array stores an unbounded intern index: 128-255 fail **closed**, ≥256 fail **open** (256→`parsers[0]`, 257→`parsers[1]`, 300→`parsers[44]`); reader-α's added sting adopted: **index 255 stores as −1, byte-identical to the unmapped `fill(-1)` sentinel**. Reachability honestly bounded (≥128 distinct parsers in one table; the largest real table is 57) | `leaf.ts:101/:104-111/:114/:119/:122/:137-141`; Int8 wrap arithmetic; every entry interned before chars examined (`:114`) | **W2** — wrapper requirement (iv): bound the intern index or widen (`Int16Array`, 256 B, once per construction; 1.1.0-eligible incumbent cure) |
| **PT-LEAF-11** | L-M9 / C-M3 / C-M10 | Three spellings of "try this alternative", three post-failure offset disciplines: `any()` arity ≥2 restores, `any()` arity 1 leaks, `dispatch()`'s hit path never captures `savedOffset` at all — committed choice sold by its own JSDoc as the drop-in for backtracking `any()`, with no label on selected-arm failure; both consumer tables survived only inside re-restoring wrappers (luck, not contract) | `leaf.ts:76/:134-145` vs `:46/:51-52/:66-67`; leaking arm from two primitives via `chain` (`parser.ts:131-138`, no restore); JSDoc `:82-85` | **W2** — *backtracking choice* and *committed choice* become two **named** operator families, each with a stateable EQ-5; the loop-not-table correction stands: no table-side adapter can fix a leak in the loop |
| **PT-LEAF-12** | C-M5 | Non-ASCII dispatch keys are silently dropped (out-of-bounds TypedArray writes are no-ops) while the construction-time label still advertises them — CSS idents admit non-ASCII from U+0080 | `leaf.ts:101/:122/:128-132/:137` | **W2** — wrapper requirement (iii): non-ASCII keys rejected loudly at construction |
| **PT-LEAF-14** | L-M10 / C-M9 | The `parser ↔ leaf` cycle: `export let whitespace` (non-optional, never initialized in-module) + cross-module top-level `_initWhitespace()` under `"sideEffects": false` — correctness hangs on barrel statement order; an ESM entry evaluating `leaf.js` first assigns a still-TDZ `let` and throws at src level. Bundle-scope safe (single chunk). The 3-symbol cure is killed (K2): `parser.ts:6` imports **five**, and the two omitted (`whitespace`, `_initWhitespace`) ARE the deferral | `leaf.ts:395-399`; `parser.ts:6` (re-read) + `:711` (re-read); `package.json:6`; core.ts `:7` before `:16-25`, index.ts `:2` before `:9`; nothing pins the order | **REFINEMENT FOLD** — W0's fresh root consumes src: load-order proof obligation (or consume dist only, stated); W2 bans in-cycle mutable exports. Incumbent cure rides O-15 |
| **PT-LEAF-15** | L-M11 | `ParserFunction<T>` is phantom (`T` unused): all **9** `as ParserFunction<X>` casts assert nothing and **16** `as ParserState<unknown>` casts launder a variance hole — 25 assertions in 399 lines; the mechanism by which PT-LEAF-1/-2 pass `tsc --strict` silently | `parser.ts:14-16` (re-read); both cast censuses re-counted against the full leaf.ts read: 9 at `:57/72/148/211/250/272/294/306/366`, 16 at `:16/44/49/54/62/69/142/193/201/224/232/240/261/291/303/360` | **W2** — type-soundness law: the algebra's function type carries `T` on both sides; casts are a counted, gated budget |
| **PT-LEAF-16** | L-M13 / C-M7 | `.parse()` on failure returns whatever `state.value` holds — frequently a plausible partial of a *different shape* (`"b"` where success yields a 3-array) — so an `undefined`-check boundary invariant **passes failed parses**; and the printed error view is not the returned one (`parser.ts:66` vs `:74`) | `parser.ts:77-79/:61/:66/:74`; fuseAll error arms never reset `value` (`:194-198/:224-228/:262-266`) | **REFINEMENT FOLD** — W1's JS-boundary invariant upgraded from convention to law: entry via `parseState` + `isError` only; `.parse()` truthiness banned with this row as the written reason |
| **PT-LEAF-17** | C-M1 + C-M11 | `./core` ships leaf's three label constructions but withholds `enableDiagnostics`/`mergeErrorState` (labels unobservable on the tier) — while shipped `dist/core.js` **already pulls the packrat chunk and the diagnostics chunk in two hops**, so the tier-isolation promise (`core.ts:3-5`) is false and the consumer pays for the capability it is denied; `proof:subpath` checks existence + `typeof`, never a module edge — structurally vacuous for its stated purpose | `core.ts:16-25` (re-read; the `:19-26` cite drift stands corrected); `utils.ts:33`; `dist/core.js:1` → `packrat-entry-CS1td-8B.js:1` → `diagnostics-DDazRHgl.js` (re-read, both line 1s); `test/subpath-gate.mjs` | **W2** — EQ-4 owns the class: labels are first-class values, never mode-gated (leaf's *production* already complies — superlative 8). **W4 rider**: subpath isolation asserted over import graphs, not file existence |
| **PT-LEAF-18** | C-M6 | `trimStateWhitespace` is dead public surface on both barrels (**0** consumers ever — re-measured this session), not even a `Parser`; the project's dead-export gate is hardcoded to two past deletions and structurally cannot catch the next instance | `leaf.ts:372-391`; `index.ts:9`/`core.ts:23`; `proof-no-dead-combinator.mjs` banned list + vacuity guard | **NO-WAVE-OWNER** — incumbent deletion, owner-held via O-15 (**2.0.0 event**: removes an export). **W4 rider**: release gates enforce standing properties, not enumerated corpses |
| **PT-LEAF-19** | C-M8 + stale anchors | 19 lines of internal tranche archaeology ship in the published `.d.ts` directly above `dispatch` (`dist/leaf.d.ts:5-23`, re-read), and its version anchors ("0.12.0 … RETRACTED in 0.13.0") sit **below** the 1.0.0 artifact carrying them | `dist/leaf.d.ts:5-23` vs `package.json:3` | **NO-WAVE-OWNER** — incumbent doc repair (CHANGELOG relocation + anchor refresh), O-15 channel (1.1.0-eligible) |
| **PT-LEAF-20** | α-miss ≡ r1 PT-LEAF-20 | The entire `FLAG_*`/`Parser.call()` machinery is **dead code** (~40 lines incl. the only FLAG_EOF consumer); `.trim()` allocates and discards a Parser + closure + context per construction for it; **and the one test that claims to cover it is vacuous** (`debug.test.ts:109-118`: comment says "Exercise the EOF flag path via call()", `pEof.flags === 0`, the `:438` fast path runs, the asserted label comes from `eof()`'s merge at `leaf.ts:16`) — the second instance of PT-LEAF-18's structural point, invisible to the dead-export gate, and the falsifier of both challenges' FLAG-routing prose (kill K4) | this seat's grep triple (ruling 4): sole `.flags` write `:496`, sole `.call()` callers `:493` + `state.ts:93`, `unsafeCall` callers 0, FLAG_EOF assignments 0; dead surface `parser.ts:20-22/:441-477`, `state.ts:92-94` | **NO-WAVE-OWNER** — incumbent deletion, O-15 (internal-only: 1.1.0-eligible). **W4 rider** shared with PT-LEAF-18; the vacuous test is deleted with the machinery or rewritten against `.eof()`'s real path |
| **PT-LEAF-21** | *(r1-carried; mechanism re-verified at the bytes this session)* | The two `discard:false` public APIs are built on `all()` and inherit the compaction whole: `wrap(start,end,false)` → `all(start,this,end)` (`parser.ts:392-395`, uncast), `.trim(p,false)` → `all(p,this,p) as unknown as Parser<T[]>` (`:484-486`, double-asserted) — arrays of length 1-3 by input, or hard-fail at EOF via PT-LEAF-3. Bound: **zero** `discard=false` call sites anywhere (re-grepped this session) | `parser.ts:392-395/:484-486` (re-read); trace via `leaf.ts:355` + `:327` | **W2** — the sequence-algebra decision inherits both first-party compositions as acceptance witnesses; W1 oracle row |
| **PT-LEAF-22** | *(r1-carried; `parser.ts:90` re-verified)* | `Parser.then` **keeps** `undefined` (unconditional 2-tuple at `:90`) while `all()` **drops** it — two sequencing operators with two value algebras in one package; leaf's fusion docblock (`:174-176`) advertises the then→all migration without stating it is not shape-preserving. The missing premise of PT-LEAF-2's cure decision | `parser.ts:90` (re-read) vs the six drop sites; `leaf.ts:174-176` | **W2** — ONE sequence operator with ONE stated `undefined` algebra; migration-equivalence becomes a property test (MEASURE-AT-OPEN #6) |
| **PT-LEAF-23** | L-M8 | PT-01 confirmed at the bytes and extended: `mergeErrorState`'s two array assignments are **outside** the diagnostics guard — two fresh arrays per furthest-advance on the shipping default path, driven by leaf's seven merge sites; the armed tier's `console.error` is uninjectable; O-15's `:881` cite drifts one line (`:881` = guard, `:882` = the call — re-read at the bytes) | `utils.ts:33-35` (re-read: guard covers only the `expected` line); `dist/diagnostics-DDazRHgl.js:14-16` (re-read); merge sites ×7; `dist/packrat-entry-CS1td-8B.js:881-882` (re-read) | **W2** — R-LAW-3's delivery side: diagnostics are values, allocation-free when disarmed, printed by no one. Incumbent one-line guard rides O-15 (1.1.0-eligible). Alloc counts re-measured at W1 (MEASURE-AT-OPEN #3) |
| **PT-LEAF-24** | L-M4 | `fuseAll` allocates the result array **before** the first child runs — every failed `all()` allocates garbage on exactly the reject-path leg `parser-band.md:117` makes a binding bench debt; the cure is a pure hoist (`:199` is the first write) | `leaf.ts:191→193, :222→224, :257→260` (all three arms re-read) | **REFINEMENT FOLD** — W1's reject-path bench leg gains an alloc-count oracle (MEASURE-AT-OPEN #4); W2 law: the deliverable is allocated after the first commit point. Incumbent hoist 1.1.0-eligible |
| **PT-LEAF-25** | L-M3 | One API, two element kinds: arity 2-3 return PACKED literals, arity ≥4 permanently HOLEY `new Array(n)` — the IC pollution the module's own comments claim to avoid; arity-4 consumer in-tree (`print.test.ts:15`). Both fresh readers and r1's own probe now agree the `fill(undefined)` cure measures PACKED (the r1-K15 overturn is moot in this corpus); the property stays engine-dependent | `leaf.ts:191/:222` vs `:257`; reader-α's five-predicate probe + r1's node-v26 probe, concordant | **REFINEMENT FOLD** — W2 law: one construction shape (`[]` contiguous preferred — no engine assumption); W1 pins the elements-kind predicate on the lane's node (MEASURE-AT-OPEN #1). Incumbent `fill` 1.1.0-eligible |
| **PT-LEAF-27** | NEW (β-miss 1) | **SEMVER IS UNPRICED across the whole corpus**: parse-that is published 1.0.0 (`package.json:3`) and the standing ask is a **minor** (O-15: "evidence packet ATTACHED to the standing 1.1.0 ask (owner-held); NO code change requested" — verbatim `INBOX.md:77`; HEAD commit `ef10d5b` IS the ask letter), yet the challenges' top-ranked cures are breaking: the L-§7 rank-1 deletion of `leaf.ts:327-330` is a semantic **widening** (inputs that reject today would accept), every `all()`/`any()`/`dispatch` semantics change and all five C-§7 RETIREs (5 of 9 exports) are 2.0.0 events. Shipping the EOF-guard deletion in a 1.1.0 would be the exact defect class `parser-band.md`'s harness law exists to prevent | `package.json:3`; `INBOX.md:77` (re-read verbatim); the cure lists of both challenges | **REFINEMENT FOLD** — W4's release condition and the O-15 channel both gain the pricing law: every proposed cure carries a semver class. **1.1.0-eligible set**: PT-LEAF-4 (delta gate), -10 (Int16Array), -19 (doc move), -20 (dead internal machinery), -23 (guard), -24 (hoist), -25 (fill/contiguous), L-m1 (dead imports), L-m9, L-m13. **2.0.0 events**: PT-LEAF-1/-2/-3/-6/-7/-8/-11/-18 and every export retirement |
| **PT-LEAF-28** | NEW (β-miss 2) | **The drop-undefined defect is positionally conditional, and the consumer's history proves it**: 8 live `.opt()`-under-`all()` sites at `164343c1^` — a TRAILING dropped element is accidentally safe for positional destructuring (unsafe only for `.length`/spread/iteration); a LEADING or MIDDLE drop silently binds the wrong value. **Both recorded production crashes are leading-position** (`index.ts:205-216`; `math.ts:144-150` — the second crash, uncited by either challenge), and the surviving compensations are tail-detection at exactly the non-trailing sites (`stylesheet.ts:280/:535`, `scroll-timeline.ts:181-185` — all re-read verbatim this session). Consequence: `parser-band.md:108/:134`'s blanket ban is a **defensible but unstated approximation** (a structural graph-walk cannot see position) | the 8-site census (β) with 5 sites re-read verbatim by this seat: color.ts:169 (trailing), :305 (trailing), stylesheet.ts:274 (**leading**), :530-532 (**two leading**), scroll-timeline.ts:183-184 (**both optional**) | **W2** — the sequence decision states the positional law explicitly (absence-representation must be safe in ALL positions); **W1** — corpus rows for leading/middle/trailing optionals; parser-band G8 gains the annotation that its ban over-approximates (retire it with PT-LEAF-2's cure, not before) |

### Minors (22) and info (5)

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| L-m1 *(extended by β-miss 6)* | Dead `Span` type-import is a **two-site pattern**: `leaf.ts:3` AND `parser.ts:2` (re-verified: no other `Span` token in parser.ts; `secondarySpans` is a different identifier); root cause = tsconfig sets `strict` but no `noUnusedLocals`/`noUnusedParameters`; both are residue of the 1.0.0 `*Span` excision | NO-WAVE-OWNER (O-15; 1.1.0-eligible); the tsconfig gap is the general cure |
| L-m2 / C-m1 | `eof<T>()` phantom generic; published `Parser<unknown>` (`dist/leaf.d.ts:3`, re-read) | W2 — eof RETIREs into a total-consumption judgment (C-§7 sustained: 0 real call sites; its `undefined` value invisible to `all()`) |
| L-m3 | `any()` arity 1 builds the general closure then discards it (`:58-73` bare else, `:76`) | folds into PT-LEAF-1/-11's short-circuit deletion |
| L-m4 | `makeParser` dead indirection; generic cast away at **3 of 6** sites (`:24/:78/:163` — the challenge's "4 of 6" killed, K9; re-counted from the whole-file read) | NO-WAVE-OWNER |
| L-m5 / C-m7 | `matchFunction === ""` is an undocumented sentinel: zero-width success, `undefined` value, the regex's consumed length **discarded** (`:341-343`) | W2 — the regex-RETIRE absorbs it |
| L-m6 | "zero-alloc default path" header vs `substring` allocation on every non-empty match (`:315-316` vs `:350`) | NO-WAVE-OWNER (comment truth) |
| L-m7 | `.trim()`'s fast path dispatches on the mutable public `context.name` string tag — forged tag ⇒ the supplied parser is silently ignored (mechanism inside PT-LEAF-7) | folds into PT-LEAF-7 |
| L-m8 | `dispatch` conflates non-ASCII with unmapped at the same error label (`:137/:142`) | folds into PT-LEAF-12 |
| L-m9 / C-m4 *(instance added, β-miss 5)* | `internParser` is `indexOf` inside the entries loop — O(n²) construction; **the live worst case is the consumer's own 57-entry color table** (`164343c1^:color/color.ts:645-661`, re-read: 5 literal keys + 52 looped letters), which also builds a ~57-element dead label | folds into PT-LEAF-10's wrapper |
| L-m10 *(instance added)* | All three construction labels are dead weight unless armed (PT-01 corollary); the 57-key table's spread+map+2-join label is the measured-shape instance | W2 — EQ-4 (PT-LEAF-17) |
| L-m11 | **No `test/leaf.test.ts`** — 8 public exports, the densest hot path, zero direct spec (13 specs re-counted this session, none targets leaf); the direct cause of PT-LEAF-1/-2/-25 shipping | W1 — the lane's oracle suite is the successor; incumbent gap rides O-15 |
| L-m12 | No declared frame budget; each level costs a frame against PT-04's 7,761 ceiling (verbatim `INBOX.md:77`); G-9 "DEPTH IS DECLARED" | REFINEMENT FOLD — W1 publishes k; superlative 9 is the counterweight |
| L-m13 | Redundant `state.isError = true` dead stores in fuseAll error arms — **6** sites `:196/204/227/235/243/264` (the challenge's prose "4" killed, K9; re-counted) | NO-WAVE-OWNER (1.1.0-eligible) |
| C-m2 | `regex`/`whitespace` typed `Parser<string>` while the empty-match arm sets `undefined` (`:355`; `dist/leaf.d.ts:27/:29` re-read) | W2 — the trivia/scan redesign absorbs it |
| C-m5 | `string("")` zero-width unconditional success which `all()` **keeps** (unlike `undefined`) — the value-vs-absence asymmetry in miniature | W2 — `succeed` becomes a named operator (band precedent) |
| C-m6 | `_initWhitespace` declared in the published `.d.ts` (`dist/leaf.d.ts:30`) though no specifier reaches it | NO-WAVE-OWNER (surface noise; the *sealing* is superlative 11) |
| L-M12′ *(re-graded survivor, ruling 1)* | The arity-2/3 unroll vs the general arm is benched nowhere and pinned by no test; the unroll comments' figures are uncited and the one checkable number — `leaf.ts:172` "59 `all()` sites" — is **measurably false** (61 at `164343c1^`, both readers + this seat concordant) | REFINEMENT FOLD — W1 equivalence oracle (MEASURE-AT-OPEN #6); the comment figure is corrected in any wave touching the file |
| **PT-LEAF-13** *(re-graded MAJOR→MINOR, ruling 2)* | C-M4's bare type fact: `Record<string, Parser<T>>` cannot express a heterogeneous table and `any()` infers a union — true, with **zero consumer receipts** (both challenge receipts killed at the archive bytes; the `:673` cast survives the proposed cure) | W2 — wrapper requirement (v) stands on design-forward grounds (heterogeneous inference so V stays assignable under G-10) |
| **PT-LEAF-26** | NEW (α-miss 4) — `regex()` silently re-compiles every caller pattern with `y` (`leaf.ts:321-322`, re-read), so `^`-anchored patterns match only at offset 0 with no diagnostic (a real footgun for a ported scanner regex); adjacent: the error label is built from the ORIGINAL flags (`:324` `/${r.source}/${r.flags}`) while the pattern that ran carries `+y` — an armed diagnostic prints a regex that is not the one evaluated | W2 — folds into the regex-RETIRE rationale; NO-WAVE-OWNER incumbent doc note (1.1.0-eligible) rides O-15 |
| C-miss-3 *(r1-carried; mechanism re-verified from source)* | A labelless `mergeErrorState(state)` at `offset > furthest` **erases** an accumulated expected-set and reallocates both arrays — reachable exactly when non-merging failure paths (regex EOF guard `:327-330`; the negate arms at `parser.ts:~262-264`; `minus` `:314-318` — all re-read) leave `furthest < offset`; `any()`'s only merges are labelless (`:54/:69`). C-M2's "provably dead" stays killed (K5): the branch is live-and-destructive | W2 — diagnostic-conservation law: merge may never destroy D |
| L-miss-M4 *(r1-carried)* | `regex()`'s two success paths write offset under two disciplines — absolute (`:349`, immune by construction) vs relative (`:340`, the root of PT-LEAF-4/-9) | folds into the monotonicity law (cure-shaping) |
| L-miss-M5 *(r1-carried)* | `Parser.state` is per-Parser mutable state a nested parse clobbers before the outer parse renders its error | W2 — no operator-owned mutable parse state; NO-WAVE-OWNER incumbent |
| C-miss-4 *(r1-carried)* | `string<S extends string>(str: S): Parser<S>` is a one-token backward-compatible literal-type carry-through | W2 — terminal-tier signature law (1.1.0-eligible) |
| C-miss-5 *(r1-carried)* | Manifest publishes no `main`, no top-level `types`, no `"./package.json"` — a silent adoption wall for legacy tooling | NO-WAVE-OWNER (non-breaking manifest add, 1.1.0-eligible), O-15; W4 rider: resolution matrices |
| C-i1 (INFO) | The consume edge is severed at HEAD (re-verified: `src/parsing` absent, 0 `parse-that` hits in package.json); the last-real-consumption census is exact — **93 any · 89 whitespace (41/10/7) · 61 all · 46 string · 19 regex · 6 dispatch · 1 eof (in a comment) · 0 tSW** — re-derived this session with the bounded-call-site instrument | frame of record for every bound above |
| C-i2 (INFO) *(r1-carried)* | `parsethat-surface-gaps.mjs:4`'s comment is wrong but the prototype-local install is shasum-identical 4/4 — RED-7 numbers comparable (OP-7) | recorded so no later reader voids the corpus on a false alarm |
| PT-Q5 anchors (INFO) | The RETRACT note's version anchors (0.12.0/0.13.0) are stale below the 1.0.0 artifact | folds into PT-LEAF-19 |
| **provenance-drift (INFO)** | NEW (α-miss 5) — the `dist/` both challenges and both readers measured is a **Jul-29 build of the Jul-3 source** (all artifacts mtime 2026-07-29 14:20 vs src 2026-07-03 — re-measured); every byte cite re-verifies today, but the chunk names (`packrat-entry-CS1td-8B.js`, `diagnostics-DDazRHgl.js`) are content-addressed: the next `vite build` renames them and silently voids the provenance of four rows across both challenge documents | recorded as risk, not defect: any re-run **re-derives the chunk name from `dist/core.js:1`'s import specifier**, never hardcodes it (MEASURE-AT-OPEN #11) |
| **C-§7-regex-overstated (INFO)** | NEW (β-miss 7) — C-§7's RETIRE-regex verdict claims "four independent reasons, each sufficient"; three of the four are ~5-line repairs (the EOF-guard deletion; the delta hoist+gate; the entry-boundary typeof guard, which is not in leaf at all). The disposition rests on the fourth alone — the AC-1/AC-2 host-object bet (no zero-import Wasm lowering), an architectural commitment, not a defect | REFINEMENT FOLD — W2 states the retirement as the dual-target bet, with the note that absent AC-3, `regex` is repairable in ~5 lines. The wave acts on §7; it must not read four defects where there is one bet |

## Killed-claims register — 18 entries, all SUSTAINED (one overturns an r1 ruling)

| # | killed claim | adjudication |
|---|---|---|
| K1 | L-M12's premise: "No benchmark file, no test/benchmarks/ entry, no ledger row" (reader-α's kill vs reader-β's confirmation) | **KILL SUSTAINED at the bytes by this seat (ruling 1)** — `pt-b3-fusion.bench.ts` exists, benches the two claims, CI-gated at `package.json:48` + `proof:all:49` + `proof-perf.mjs` clauses A/B′/C + checked-in baseline. Reader-β's cell corrected (range stopped at `:45`). Survivor = L-M12′ MINOR. Convergent with r1's K1 |
| K2 | L-i1/L-M10's cure: "all, eof, trimStateWhitespace are the only three symbols parser.ts needs" | **KILL SUSTAINED** (both readers convergent; `parser.ts:6` re-read = **five**; the omitted two are the deferral). Defect stands; cure incomplete |
| K3 | Challenge-L B6's parenthetical: ".trim() … documented default argument (`Parser.trim(parser = whitespace, discard = false)`)" | **KILL SUSTAINED (r1-carried — BOTH fresh readers missed it)** — `discard` defaults **true** (`parser.ts:~482`, re-read); B6 needs an explicit `false`; **zero** `discard=false` call sites in parse-that src+test (re-grepped this session). B6's probe passes explicit `false` so its measurement stands; the prose default-claim does not |
| K4 | Both challenges' FLAG_TRIM_WS routing mechanism (L's B7 cite `parser.ts:488-496`; C-B5's "routes through FLAG_TRIM_WS/trimStateWhitespace `parser.ts:443-465`") | **KILL SUSTAINED (ruling 4)** — dead-code attribution: `flaggedParser` is built, flagged, and discarded; the live path is the `whitespaceTrim` closure `:498-517`. Both rows' conclusions survive (the charCode scanner IS the live alphabet). Reader-β's C-B5 naming slip corrected |
| K5 | C-M2's "the merge is provably dead / the branch is impossible" | **NARROWING SUSTAINED** (both readers convergent; mechanism deciders re-read: regex EOF guard, the negate arm `parser.ts:~262-264`, `minus` `:314-318` — all fail without merging, so `offset > furthest` CAN fire and is then **destructive**, erasing `expected` and reallocating). C-M2's surviving point — no label slot on the busiest export — confirmed |
| K6 | C-M4's receipts: "every bucket declared `Parser<any>` (`:455-464`) … both receipts paid in casts" | **KILL SUSTAINED AND EXTENDED (ruling 2) — OVERTURNS r1's ruling 4**: buckets unannotated; `:466` is an annotation; the `color.ts:673` cast is a narrowing past the author-declared homogeneous table and **survives the proposed cure**, so C-M4 has zero receipts. Row re-graded MINOR (PT-LEAF-13) |
| K7 | §1a's stated counting recipe + §10's `\w`-vs-POSIX lesson | **KILL SUSTAINED (ruling 6)** — the recipe-as-stated yields 183 `any` occurrences (re-measured); the table numbers are bounded **call-site** counts (`sym\(`), all eight rows re-derived exactly. §10's carried-forward lesson misdiagnoses the cause. Instrument caution recorded against the killer's own "string=443" (not reproduced under four variants: 454 occ / 423 lines) — state the counting unit beside the pattern |
| K8 | C-B5's reachability shield: "10 sites … whose only shield was that a `)` always followed" | **KILL SUSTAINED** — the shields are **keywords/tokens** (`164343c1^:index.ts:159-162` side-keywords; `color.ts:301-302` `.skip(istring("hue"))` — both re-read): a grammar-shape argument, not a delimiter argument. C-B5 itself unaffected |
| K9 | Count/cite drift bundle: L-m4 "4 of 6" → **3**; L-m13 "4 sites" → **6** (self-contradicting its own cite list); "12 .ts specs" → **13**; `core.ts:19-26` → `:16-25`; `state.ts:157` → `:158`; `parser.ts:498` → `:499`; W2 `:249-251` → `:247-248`; W2 `:239` → the D row | **ALL SUSTAINED** — load-bearing ones re-derived by this seat (L-m4, L-m13, spec count, core block, state row); the rest adopted from convergent reader agreement. No substantive claim moved |
| K10 | Both fresh readers' "verbatim at test/math.test.ts:18-21" cells (and the challenge's fenced block they inherit) | **KILL SUSTAINED (ruling 3, this seat's own read)** — block-bodied arrow, explicit `return`, trailing `.opt()` on `:22`. Substance re-derived clean and PT-LEAF-2 unaffected. Recorded as a **repeated** citation-inheritance event (the same cell r1 corrected in the prior corpus) |
| K11 | *(r1-carried)* L-challenge superlative 4's "any() and dispatch() allocate nothing per invocation" | **NARROWED** — false on the reject path (`utils.ts:34-35` unguarded, re-read); accepted-path half survives as superlative 4 |
| K12 | *(r1-carried)* C-§8's "leaf holds NO process-global mutable state; every export is a pure closure factory" | **KILL SUSTAINED** — `export let whitespace` is module-global mutable state mutated cross-module (`leaf.ts:395/:398`, `parser.ts:711` — re-read); the closure-singleton sticky carries mutable `lastIndex` across the user-code boundary |
| K13 | *(r1-carried)* C-B9's ASCII-rendered probe block | **KILL SUSTAINED** — space (32) is in `trimStateWhitespace`'s class, so the rows hold only with invisible U+00A0/U+000B; evidence blocks must use escapes (MEASURE-AT-OPEN #7 carries the law) |
| K14 | *(r1-carried)* L-B1's "neither candidate's gate catches it" | **RESCOPED** — "unexercised", not "uncaught"; defect unaffected |
| K15 | *(r1-carried)* "parser-band.md:74-78 states 'No speed claims outside the printed table'" (cited 3×) | **KILL SUSTAINED** — the sentence lives in the G7 **gate** row; `:74-78` is the HONESTY LAW section. Substance strengthened, cite wrong |
| K16 | *(r1-carried)* C-§7's "dispatch is the one thing value.js could not have hand-rolled cheaply" | **KILL SUSTAINED** — HEAD `src/css/grammar.ts` is the shipping hand-rolled replacement (pre-split + lowered-name equality, zero LUT); the megamorphic-any problem is an artifact of the combinator design. The AC-3 LUT argument stands on its own merits |
| K17 | *(r1 record, now moot in this corpus)* r1's L-axis reader killed the `fill(undefined)` cure as self-contradictory; r1 overturned that kill by measurement | **OVERTURN STANDS; dispute extinct** — both fresh readers independently measure/derive fill→PACKED and store-by-store→HOLEY as simultaneously true. The engine-dependence caution survives as MEASURE-AT-OPEN #1 |
| K18 | Reader-α's "L-M5 M4 numerals" row (self-declared not-a-kill) | **ADOPTED AS CAUTION (ruling 7)** — the numerals are probe properties; the module invariant is `offset := lastIndex(last nested run) − savedOffset`, unbounded in both directions; carried into MEASURE-AT-OPEN #2 so no seat "corrects" a right row by its own probe's numeral |

## Superlatives — 12 sustained (L-18 runs both ways)

1. **`string()`'s single-char fast path** (`leaf.ts:282-294`): one charCode compare, the **shared `str` reference** stored on success — zero allocation on the hottest leaf in any grammar.
2. **`regex()`'s default path picks `test()` over `exec()` AND writes the offset absolutely** (`:344-353`): structurally immune to both offset-corruption classes that kill the custom-match arm; 18 of the consumer's 19 historical sites ride it (the 19th genuinely needs the capture group — and dodges PT-LEAF-4 by returning `null`).
3. **`trimStateWhitespace` as code** (`:372-391`): zero-alloc, no regex engine, fast-exit at `:378`, explicit code-point comment. Its only sin is being one of three alphabets.
4. **`any()`/`dispatch()` allocate nothing per invocation on the accept path** (`:42-72` one integer; `:134-145` two) — narrowed per K11: the reject path pays `utils.ts:34-35`'s unguarded arrays.
5. **`dispatch`'s one-expression guard** (`:136-137`): `charCodeAt` past EOF → NaN → `NaN < 128` false → labelled error — EOF guard, non-ASCII guard, and unmapped path in one branchless expression (the same silence is C-M5's defect).
6. **`Int8Array(128).fill(-1)` is the right structure** — the width is the bug (PT-LEAF-10), the design is not; the one artifact with a clean zero-import Wasm lowering (AC-3's literal shape).
7. **The PT-Q5 RETRACT note's honesty discipline** (`:90-98`) — a shipped seam removed for having zero consumers, re-introduction condition named precisely, **executed and gated** (proof-perf clause B′ asserts the seam is off the surface and re-anchors the dispatch win to the real CSS bucket). Its location and stale anchors are the defects; the posture is the house standard.
8. **Construction-time-constant labels** (`:127-132/:278/:324`): zero error-path allocation, nothing printed, no global touched — leaf's diagnostic *production* satisfies R-LAW-3 in full; the whole PT-01/EQ-4 problem is delivery-side (with PT-LEAF-26's one blemish: the label prints the original flags, not the sticky pattern that ran).
9. **`fuseAll` raises the recursion ceiling**: n children threaded in ONE frame vs then-chains' n−1 live nested frames (`parser.ts:82-97` re-read against `leaf.ts:186-272`) — every fused sequence buys back PT-04 budget; a "simplification" back to then-chaining would *lower* the ceiling. (Structural; falsifier rides MEASURE-AT-OPEN #5.)
10. **Every leaf success path writes `state.value` explicitly** — the drop-filter can never read a stale value: PT-LEAF-2 is a *policy* defect atop an airtight state discipline; and the fusion **inherited** the policy faithfully (blame belongs upstream of PT-B3, per S-5-as-narrowed by K16-r1/C-miss-2).
11. **The arity-0 identities are correct with no arity-0 arm written** (`all()` → `ok([])`, `any()` → labelled failure) and **`_initWhitespace` is correctly sealed** (absent from both barrels, no wildcard specifier — the house rule the rest of the surface should be held to).
12. **NEW (reader-α, adopted): `ParserState`'s diagnostic slots cost no hidden-class transition** — `expected?: string[]` is emitted as a real class field (`dist/packrat-entry-CS1td-8B.js:~300`, under ES2022 class-field semantics), so the property exists from construction and the first failure does not reshape the object. The two array initializers beside it are the live cost (PT-LEAF-23); the SHAPE is stable — an easy V8 trap the author avoided without comment. A defect hunted for and refuted is recorded with the same rigor as one found.

## MEASURE-AT-OPEN residue — bench-dependent rows for the lane's W1 harness

Inherited law: one fresh process per cell; nothing armed; every probe disposable-PID.

1. **Elements-kind predicate (PT-LEAF-25)** — re-run the five-predicate `--allow-natives-syntax` probe on the lane's **pinned** node (three independent probes now concur: fill→packed, store-by-store→holey, literal→packed, truncation preserves kind). The W2 law pins ONE construction shape against the measured predicate, not folklore.
2. **Offset-monotonicity oracle (PT-LEAF-4/-9)** — asserts `result.offset ∈ [entry, src.length]` over every operator including hostile `matchFunction` pairs (null-arm synth; re-entrant). Carries K18's law: the invariant is `offset := lastIndex(last run) − savedOffset`; no probe numeral is portable.
3. **Diagnostic realloc counts (PT-LEAF-23)** — mechanism adopted (2 arrays/furthest-advance, unguarded at the bytes); magnitudes re-measured on the W1 rig's reject-path leg.
4. **Reject-path alloc hoist (PT-LEAF-24)** — band debt 2's own leg; before/after alloc-count under `--expose-gc` via the existing `proof:perf` mechanism.
5. **Fused-vs-chained depth ceiling (superlative 9's falsifier)** — depth-to-RangeError fused vs then-chained, against PT-04's 7,761 baseline.
6. **Arity-equivalence property suite (PT-LEAF-1/-22/L-M12′)** — arity 2/3 unrolls vs the general arm, then-vs-sequence shape equivalence, and **PT-LEAF-28's positional rows** (leading/middle/trailing optionals) — the missing `leaf.test.ts`'s successor.
7. **Non-ASCII whitespace sweep (PT-LEAF-7)** — ASCII corpora cannot separate the three classes; the totality oracle carries NUL/C0/NBSP/BOM/U+2028/VT rows **with escapes, never raw glyphs** (K13's lesson).
8. **Module-edge assertion (PT-LEAF-17's W4 rider)** — the release gate imports shipped `./core` in a bare process and asserts the loaded-module set, not `existsSync`.
9. **Dispatch probe-design law** — arms must be total on any first character (`regex(/./).map(tag)`) so a mis-dispatch is observable; both challenge seats hit the masking trap independently.
10. **Census instrument law (ruling 6)** — function exports are counted as bounded call sites (`(^|[^.[:alnum:]_])sym\(`), value exports as bounded references; every census states its counting unit (occurrences vs lines) beside its pattern. The §10 prose is struck; the numbers stand.
11. **Chunk-name re-derivation rule (provenance-drift INFO)** — any re-run of the dist byte cites derives the chunk filename from `dist/core.js:1`'s import specifier at that moment; hardcoded content-addressed names void silently on the next build.

## Closing verdict

Both fresh reader returns **survive the incredulity seat with corrections that sharpen rather than reverse** — but this corpus needed the seat more than r1's did: the two readers **flatly contradicted each other on L-M12** (ruled at the bytes for the kill), split five severity grades (all five ruled, both dissents preserved), and **both** inherited the same false "verbatim" cell the previous adjudication had already corrected — a repeated citation-inheritance event now recorded twice as fleet evidence. One kill from this corpus (K6, C-M4's receipts) was strong enough to **overturn an r1 ruling**: the archive bytes show the sole preserved receipt is a narrowing that survives the proposed cure, so PT-LEAF-13 falls from MAJOR to MINOR. All 12 admitted misses are adjudicated real; three become roster rows (PT-LEAF-26 sticky re-flag, PT-LEAF-27 semver pricing, PT-LEAF-28 the positional law), and PT-LEAF-27 changes how every other row leaves the building: the incumbent-repair channel is owner-held at a **1.1.0** ask, so the roster now carries an explicit semver class on every cure — the highest-ranked fix in the L-challenge's own cure list is a semantic widening that must not ride a minor.

The module's adjudicated shape is unchanged in substance from r1 and now sealed against a second, adversarial corpus: **five genuinely excellent terminals and one right data structure, wrapped around three n-ary combinators whose declared types do not describe their runtime behavior, one dead 40-line control-flow machine with a vacuous test, and a whitespace story that is wrong three different ways at once.** Every consumer-visible defect routes: **W2** owns the algebra-class rows (the sequence/undefined decision PT-LEAF-2/-21/-22 with its positional law PT-LEAF-28, the choice-family split PT-LEAF-11, the terminal totality + trivia + monotonicity laws PT-LEAF-3/-4/-7/-9, the dispatch wrapper requirements PT-LEAF-6/-8/-10/-12/-13, the type-soundness law PT-LEAF-15, EQ-4 delivery PT-LEAF-17/-23); **REFINEMENT FOLD** sharpens W0 (src-consumption load-order, PT-LEAF-14), W1 (the `.parse()` ban PT-LEAF-16, the boundary respecification PT-LEAF-5, the bench legs PT-LEAF-24/-25), and W4 (edge-asserting release gates + **the semver pricing law PT-LEAF-27**); **NO-WAVE-OWNER** carries the incumbent repairs on the owner-held O-15 channel, now split 1.1.0-eligible vs 2.0.0. Eleven dynamic rows are banked to MEASURE-AT-OPEN under W1's law. Nothing lost: head, tail, or interval — including r1's, whose roster this record carries whole.

— end of apotheosis r2. Both challenge files and both reader returns remain immutable beside this record per E-1/E-3; the r1 record's content is superseded-by-carriage, not erased (its every surviving row is present above under its original id); dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
