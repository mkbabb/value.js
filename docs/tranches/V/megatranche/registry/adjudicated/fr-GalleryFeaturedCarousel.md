# fr-GalleryFeaturedCarousel — adjudicated apotheosis (M-23/L-18 fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-24 · **Registry**: value.js branch `tranche-u` · **Subject tree**: fourier-analysis `HEAD cd26c653` (both challenge axes' git-clean attestation; this seat's whole-file reads matched every L/C-axis line cite exactly).
**Corpus**: `docs/tranches/V/megatranche/audit/fourier-components/GalleryFeaturedCarousel/` — challenge-D-design.md (340, opus-5[1m]) · challenge-L-library.md (242, opus-5[1m]) · challenge-C-consumption.md (597, opus-5[1m]) — over `web/src/components/visualization/gallery/GalleryFeaturedCarousel.vue` (91 lines) and its child/parent/store/API seams.
**Readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every reader-vs-reader conflict was re-derived by this seat with its own instruments; suspicious agreements were spot-re-measured, not trusted.
**This seat's own instruments (all static + node, zero browser probes — probe parsimony; product source READ-ONLY)**: whole-file read of the subject (91 lines, every anchor line-exact); `GalleryView.vue:60-320` (featuredEntries/nonFeatured partition, the two public refetch watchers :95-98/:111-114, handleSetTier/:132-135 + handleDelete/:137-141, the complete `selectedHashes` writer set :150/:154-163/:192/:204-205, the carousel mount :249-258, the marquee's double guard :265-270, the grid survival arm :290); `stores/gallery.ts` :30-42 (dead-filter refs) / :84-100 (`entries.value = []` synchronous, five statements before the await) / :136-160 (setTier/deleteEntry both `await resetAndFetch()`, returned entity discarded) / :186-210 (`like()` = pure local bump, `const liked = true`); `GalleryCard.vue` at :1-12 (imports incl. the two dead ones), :28-36 (declared `toggle-select` emit + retyped tier union), :84-96 (`v-if="adminMode"` checkbox wrapper + `@click.stop`), :138-165 (aria-pressed, slug payloads, tier Crown), :193-231 (hover transition on `--ease-apple-spring`, `--deferred-section-size: 17rem` comment, the symmetric 12px featured glow), :302-308 (the card's sole PRM block); **installed glass-ui 4.0.0 read at the bytes**: `dist/styles/utilities/a11y-overrides.css:1-40` (BOTH PRM arms verbatim — the D-11 decider) + the full import chain `web/src/style.css:3` → `styles` root → `utilities.css:19`; `dist/styles/utilities/base.css:343-380` (`.fading-scroll--x` = overflow-x + mask ONLY, no `overflow-y`) and `:415-440` (the fade animations are **gated on `[data-fade-start]`/`[data-fade-end]` presence attrs** — the cure-vehicle decider); export map by node: **4.0.0 = 80 keys, `./carousel`/`./fading-scroll`/`./metric-badge` PRESENT, `./checkbox` ABSENT**, peers `@mkbabb/value.js ^0.10.0 || ^0.11.0` + `@lucide/vue ^1.16.0` + `embla-carousel-vue ^8.0`; **producer at the tags** (`/Users/mkbabb/Programming/glass-ui`, working tree now 8.0.0): `git show v7.0.0:package.json` → `./checkbox` count 0; `v8.0.0` → count 1; repo greps re-run fresh: `scroll-padding` → **exactly 1 hit** (`PaperSidebar.vue:159`), `overscroll-behavior` → 6 sites none gallery, `like` over `web/src/lib/api.ts` → **empty**, `def .*like|/like` over `api/routers/` → **empty**, GalleryTier retype census → 6 SFC/handler literal sites + 2 `"all"|…` variants vs the one export at `types.ts:96`, `lucide-vue-next/dist/esm/Icon.js:41` auto `aria-hidden`, e2e = 8 specs / 0 reach a card, embla trio hoisted + undeclared in `web/package.json` (which carries `@mkbabb/value.js ^0.13.0` and `lucide-vue-next` in devDependencies); `router/index.ts:81-90` (`/gallery` **lazy** `component: () => import(...)`) + `main.ts` (`router.isReady().then(() => app.mount(...))`) — the C-5 scope decider; `basis-display.ts:1-7` + `colors.ts:75-97` whole; `GalleryMarquee.vue:82-88` (the padding comment lives at :82-83, the mask at :85-88 — the D-drift check); `GalleryDraftsSection.vue:58`; banked-identity greps across `registry/adjudicated/` (no fr-GalleryMarquee/GalleryView/GalleryCard row exists; `marquee` → 0 hits; `GalleryTier` → fr-AdminFlaggedPanel only; FR-AH-2 · MISS-FONT read at fr-AppHeader.md; FR-CP-10 read at fr-CoefficientsPanel.md; the C-20 kill read at fr-EquationPanel.md:117).

## Scoreboard

**31 adjudicated rows: 0 BLOCKER (three demotions ruled) · 9 MAJOR booked · 15 MINOR booked · 5 INFO booked · 2 folds-by-reference. 10 kills banked (5 sustained from the readers · 3 minted or sharpened by this seat against a reader · 2 evidence-cell kills). 6 missed finds adopted after re-proof (4 reader-DU · 2 reader-LC), one recorded as a pre-emptive kill.**

Heaviest survivors: **FR-GFC-1** (the featured strip destroys itself on every mutation AND on every public search-pause/filter-change — three dead controls refetch an identical page), **FR-GFC-3** (the like affordance couples to no operation on either side of the wire), **FR-GFC-9** (the D.W4.c empty-state marquee is mounted behind two mutually unsatisfiable guards and can never render — the D corpus's orphan verdict was right and the L axis's contradiction-filing was wrong).

## Ruled disagreements (the seat's actual work)

| # | question | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **PRM hover motion** (D-11) | KILLED — glass ships a universal PRM blanket | CONFIRMED MINOR — "the card's only PRM block is :304-308; fourier's only global PRM block is style.css:92-96" | **DU wins — D-11 is DEAD (K1).** | `a11y-overrides.css` read verbatim in fourier's installed 4.0.0: arm 2 = `*:not([data-allow-motion]) { transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important }` — `transform` struck from the property list; arm 3 re-kills `[data-allow-motion]` ("accessibility is absolute"). Chain verified: `style.css:3` → glass `styles` root → `utilities.css:19`. Under PRM the `transform 0.25s var(--ease-apple-spring)` transition cannot run; the residue is an instantaneous 4px offset, not 2.3.3 motion (scope matches banked fr-CollapsibleSection M-3). LC's cells are true of fourier's OWN files and false of the loaded cascade — a **citation-inheritance event**, the THIRD banked instance on this exact producer file (fr-AdminFlaggedPanel ruling 1, fr-CoefficientsPanel R-1). |
| R-2 | **D-7's WCAG 2.5.8 framing** | CONFIRMED MAJOR ("exception forfeited; survives either branch of D-4") | KILLED the framing; MINOR survivor (missing affordance) | **LC's narrowing wins (K8); the affordance substance rides FR-GFC-7 at MAJOR.** | The "survives either branch" argument fails on its own corpus: D-4 (same author) holds that in Chromium ≥121 the `::-webkit-scrollbar` rules are IGNORED — where the author's declaration has no rendering effect, the target is UA-determined in the only sense 2.5.8 operates on. The criterion genuinely bites only where the 4px thumb paints (WebKit), and D-4's precedence half is itself UNPROVEN-NEEDS-LIVE — so 2.5.8 pass/fail is engine-split AND contingent, unfit to carry a MAJOR alone. Second overstatement killed with it: "no non-drag, non-wheel way to advance" — every card is `role="button" tabindex="0"` (GalleryCard.vue:72-73); D's own F-3 and C's §6.1 concede it. DU's grade preserved as dissent. |
| R-3 | **C-5's scope** (basis-pill freeze) | Rescoped: PATH-DEPENDENT (deep link = fallback hexes; in-app nav = capture-time resolved values); durable half = theme-toggle desync on every path | CONFIRMED universal: "module evaluation strictly precedes mount … fallback hexes … forever" | **DU wins (K4).** | This seat's read: `router/index.ts:83-85` — `/gallery` is a LAZY import; `main.ts:11` mounts after `router.isReady()`. Deep link → chunk (and `basis-display.ts`) evaluates during `isReady()`, before `App.vue` `onMounted` `resolveVizColors()` → fallback capture. In-app navigation → chunk evaluates at navigation time, AFTER resolve → captures resolved values. LC's "strictly precedes mount" is false for the in-app path — LC re-committed a smaller version of the very absolutization it was re-reading. The durable defect survives both corrections and is MAJOR: a module-scope `const` (`basis-display.ts:1-7`) can never follow the `MutationObserver` re-resolve (`App.vue:13-17`), so a theme toggle strands the pills at capture-time colours on EVERY path. C-5's "the pills are exactly the surface the D.W4.d repair existed to protect" is FALSE (that repair darkens `--viz-amber`; basis-display reads only fourier/chebyshev/legendre) — killed with it. |
| R-4 | **Where the unreachable-marquee defect books** | Books it here as MISS-2 (MAJOR); registry-swept first | Kills L-6 via the same derivation; defers booking to the sibling GalleryMarquee/GalleryView lanes | **Book HERE (FR-GFC-9); anti-rename guard attached.** | This seat's read of GalleryView.vue:265-270 + :64-66: outer guard `!gallery.entries.length && !gallery.loading` forces `featuredEntries ≡ []`; inner guard `featuredEntries.length >= 4` is false for all reachable states. No fr-GalleryMarquee/fr-GalleryView/fr-GalleryCard row exists in the registry (this seat's ls + greps), so deferring books it nowhere. The sibling GalleryMarquee/GalleryView CHALLENGES reached it independently (LC's citations) — when those seats' apotheoses land, this row is the identity they fold to, not a second booking. |
| R-5 | **D-1/C-3 severity** | MAJOR, with "MISS-1 re-rates toward genuinely blocking" | MAJOR on banked precedent (fr-AdminFlaggedPanel v-if-subtree ruling; FR-AUL-24 family row) | **MAJOR, blocker-weight flagged; both BLOCKER filings preserved as dissent.** | The banked family precedent (identical v-if subtree destruction + focus-to-body, ruled MAJOR with BLOCKER dissent) governs; MISS-1 widens blast radius (whole public gallery) without adding data loss. Grades grade the populated surface; the aggravator is stated in the row. |
| R-6 | **Deferred-size row severity** (D-15/L-9/C-13) | INFO | MINOR | **INFO.** | `contain-intrinsic-size: auto <length>` makes it a self-correcting first-paint transient (producer's own anti-jump doc); all three axes bound it so; a defect with no steady state and unproven visibility is INFO. LC's MINOR preserved as dissent. |
| R-7 | **Cure vehicle for the edge fade** | `<FadingScroll>` component | (C-6's "three characters of class list away" = the bare utility) | **The COMPONENT, never the bare class (K6).** | This seat's read of base.css:415-440: every fade animation is gated on `[data-fade-start]`/`[data-fade-end]` presence attrs, which the `<FadingScroll>` component sets from its props. The raw class alone leaves the registered customs at `initial-value: 0px` = sharp floor = **no fade on any engine**. And `.fading-scroll--x` declares NO `overflow-y` (grep over the whole sheet: exactly :367 `overflow-x` and :377 `overflow-y`, one per modifier) — so D-5's "it also cures D-3" and the §7 table cell "cure available: `.fading-scroll--x` owns the axis floor" are refuted at the bytes. The C axis read this correctly; the D axis misread the producer comment. |

## Adjudicated roster

Dispositions are **ADJUDICATED, not VERIFIED** — routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave. Line anchors are HEAD `cd26c653`; re-resolve before executing any cure.

### MAJOR

- **FR-GFC-1 = D-1 / C-3 ⊕ MISS-1(DU) — MAJOR (blocker-weight; two BLOCKER filings in dissent).** The section unmounts for a full network round trip on `set-tier` and `delete` (`gallery.ts:85` clears synchronously, five statements before the `:90` await → `featuredEntries` → `[]` → `GalleryView.vue:250` v-if false), destroying scroll offset, focus and ~17rem of layout twice per action — and **the unmount is NOT admin-only**: `GalleryView.vue:95-98` (search, 300ms debounce) and `:111-114` (sort/tierFilter/basisFilter, immediate) both fire `resetAndFetch()`, and `searchQuery`/`tierFilter`/`basisFilter` have **zero query-builder or `.filter()` readers repo-wide** (this seat's grep; `resetAndFetch` sends `{limit: 20, sort, owner}` only) — so every anonymous visitor who types a search and pauses tears the strip down, refetches a provably identical page, and rebuilds it scrolled to card #1. The asymmetry is authored into the prop signature: the grid takes `loading` and its guard carries `|| gallery.loading` (`:290`); the carousel's `defineProps` (`:6-10`) cannot. Adjudicated cure direction (the D-1 skeleton cure is KILLED — K5): patch the entry in place off `api.setVisualizationTier`'s returned entity (`api.ts:506-516`, discarded at `gallery.ts:142`) per the store's own `recordView`/`restore` idiom, and delete or gate the three dead public refetch triggers. **→ F.W4** (view/store seam) + **F.W5-W8 rider** (the list contract owns whether search/tier/basis become real params — see FR-GFC-4). |
- **FR-GFC-2 = D-2 / L-1 / C-4 — MAJOR (two BLOCKER filings in dissent).** `adminMode` is forwarded (`:34`) but neither `:selected` nor `@toggle-select` is wired (`:32-40`; emits `:12-17` declare no `toggle-select`), so every featured card in admin mode paints a focusable, labelled, permanently-inert `Checkbox` (`GalleryCard.vue:85-96`; the declared emit at `:31` terminates — no $attrs fallthrough). WCAG 4.1.2 stands. C-4's **phantom-selection** framing is the adjudicated form (see K2): the severed seam plus `handleSetTier`'s non-reconciliation yields a toolbar reading "1 entr(ies) selected" with no ticked box on screen. Fan-out correction: the cure spans **two live hosts** (carousel + grid-parity), not three/four — the marquee copies are unreachable code (FR-GFC-9). Severity MAJOR: admin-only surface, single-item Crown path (`GalleryCard.vue:162` ← `:38`) intact. **→ F.W4** (mirror `GalleryInfiniteGrid.vue:36/:41`; one decision across the LIVE hosts). |
- **FR-GFC-3 = C-1 — MAJOR.** The `like` emit couples to no operation on either side of the wire: `gallery.ts:189-199` is a pure local bump (`const liked = true` — un-like unreachable, `GalleryView.vue:128` can only take the add branch), `api.ts` has no wrapper (grep empty, re-run), `api/routers/` has no route (grep empty, re-run; only `liked_ips` projection exclusions). The tree carries its own contradiction: `gallery.py:3` asserts the like endpoint was carved out TO `visualizations.py`, which has no such route, while `gallery.ts:190-192` asserts no endpoint exists under the CRUD shape — and `cursors.py:22` ships `"likes": "likes"` in SORT_KEYS: the server sorts by a counter nothing can increment. Heart, counter and `aria-pressed` (`GalleryCard.vue:140`) all lie. **→ F.W5-W8** (add the operation or remove the affordance — the honest repair is stated in C §7.1 and adopted: no third option ships). |
- **FR-GFC-4 = C-2 / L-3 — MAJOR.** `entries` is featured ∩ the loaded 20-row cursor window, not the featured set: no `tier` param at any layer (`api.ts:397-402` · `visualizations.py:288-294` · `gallery.py:38-41` · `cursors.py:20-23`), `fetchNextPage` appends so the strip silently grows during vertical scroll, and a featured row outside the window renders NO Featured section at all until paged in (C-2's materialise-mid-scroll scenario is source-certain). The `tierFilter` Select is a dead control costing a round trip (shares the dead-control census with FR-GFC-1). Both readers' routing adopted: this is an operation-surface hole, not a component patch. **→ F.W5-W8** (list contract: tier param or a dedicated bounded featured fetch). |
- **FR-GFC-5 = D-5 / L-5 / C-6 — MAJOR.** The edge fade (`:68-74`) is a verbatim static instance of the producer's named-and-retired **R8-08 "Shy" defect** (base.css:17-20, :343-346 — "half-erased the first card's chrome at rest"), at HALF the tokenized ramp (`0.5rem` vs `--mask-fade-width: 1rem`, itself RETIRE-COORDINATED; the sibling marquee uses 3rem — three fade widths across two adjacent strips), state-blind (the whole scoped block has no `animation-timeline`/`scroll(`/`@property`), feathering both edges even with no overflow, and erasing exactly the card `scroll-snap-align: start` (`:89`) just parked at the port's leading edge. Producer already solved the snap interaction (`SNAP_TOLERANCE = 12`, constants.d.ts). Cure verified available at BOTH pins (`./fading-scroll` in the 4.0.0 map and at `git show v7.0.0`), **vehicle = the `<FadingScroll>` component** (R-7). Evidence corrections carried: the scroll-padding grep cell killed (K7 — 1 hit, `PaperSidebar.vue:159`, different axis, and its existence STRENGTHENS the convention argument); no compensating `scroll-padding-inline-start` on this port. **→ F.W3/W4** (shadow retirement, uplift-free). |
- **FR-GFC-6 = D-3 / C-7 / L-4 — MAJOR.** `overflow-x: auto` with no `overflow-y` and no top padding (`:64-65`, `:49-51`) computes `overflow-y: auto` (CSS Overflow 3 §3.1) and clips the block-start ink of every card — unreachable by scrolling: the symmetric 12px featured glow (`GalleryCard.vue:228-231` — 100% of this strip by construction), the hover lift (`:209-213`), the focus outline (`style.css:139-143`); bottom = 8px padding vs a 12px glow, so the bottom clips too. L's F-1 self-kill re-verified (`shadow.css:9-10` cartoon shadows are down-right zero-blur → inside the bottom padding) and its narrowing adopted. The in-repo cure precedent is the marquee's own comment (`GalleryMarquee.vue:82-83`, `padding: 0.75rem 0`). Adjudicated cure = the padding half ONLY — the `.fading-scroll--x` cure cell is killed (K6/R-7). **→ F.W3/W4** (rides FR-GFC-5's adoption; `padding-block` regardless). |
- **FR-GFC-7 = D-6 / C-8 (⊕ D-7 rescoped, R-2) — MAJOR.** Four anonymous divs and a styled `<span>`: no landmark, no heading, no carousel semantics, no prev/next affordance — while the shadowed `./carousel` ships `aria-roledescription` carousel/slide, ArrowLeft/Right handling, and `CarouselPrevious/Next` at BOTH pins (dist read; v7.0.0 git-show), with the Embla peer trio already hoisted in `web/node_modules` and `embla-carousel-vue` undeclared in `web/package.json` (one-line F.W0 cost, C-8's honoured concession). The heading absence has no ancestor rescue (`GalleryView.vue:249-258` wraps it in nothing; the view's heading list is empty). D-7's 2.5.8 arm rides here as the engine-split MINOR residue (WebKit-only where the 4px thumb actually paints), not as a second MAJOR. Free-scroll+snap vs Embla's transform track is a behaviour change — why this is MAJOR not BLOCKER, and why FR-GFC-5 is the higher-confidence half. **→ F.W3/W4** (shadow retirement decision) + **F.W0 rider** (declare `embla-carousel-vue` if adopted). Mechanism-adjacent precedent: FR-CP-10 (the second component instance of "a 4px scrollbar as the only affordance"; not a re-book — different host, banked there for the aside, booked here for the strip). |
- **FR-GFC-8 = C-5 as rescoped (R-3) — MAJOR.** `basisDisplay` (`basis-display.ts:1-7`) snapshots the `VIZ_COLORS` proxy at module evaluation into a plain literal; `resolveVizColors()` mutations (`App.vue:11` mount + `:13-17` MutationObserver theme re-resolve) can never retro-write it; `GalleryCard.vue:121` projects it as `--pill-c` into background, border AND text (`:270-274`). Adjudicated scope: **deep link to `/gallery` = fallback hexes (`#bf4040`/`#3d72b8`/`#9545b8`) in both themes; in-app navigation = capture-time resolved values; theme toggle = stale on every path.** The in-repo counter-idiom is three files away (`useCoeffHover.ts:60-65`, render-time read with the hazard named). Wave lock adopted verbatim from C §7.2: the colors.ts migration must fix the SNAPSHOT, not merely swap `cssVarToHex` for value.js's parser, or the wave closes green on a surface that never changed colour. **→ F.W2** (the colors.ts seam; if the forming specs collapse W2 into W3/W4, this rider travels with the colors.ts deletion — census W.L5). |
- **FR-GFC-9 = MISS-2(DU) — MAJOR (net-new; kills K3).** The D.W4.c empty-state design never ships: `GalleryView.vue:266` opens the empty-state block on `!gallery.entries.length && !gallery.loading`; `:270` mounts `<GalleryMarquee>` on `featuredEntries.length >= 4`; `:64-66` derives `featuredEntries` FROM `gallery.entries` — the outer guard forces the inner set empty, so the mount predicate is **false for all inputs**. The "living preview band" the empty gallery was redesigned around is unreachable code; the authoring comment at `:259-264` shows the slip (it anticipates only "a true cold-empty DB" without noticing every empty state is cold-empty by construction). Consequences adjudicated with it: (a) the D corpus's orphan verdict (`D/waves/W4.md:82`, `challenge-P4.md:272-274`) is VINDICATED and L-6's contradiction-filing must be withdrawn (K3); (b) the card fan-out is 2 live callsites, re-scoping FR-GFC-2's cure; (c) three of L-16's four gallery-family native loops (`GalleryMarquee:29,36,52`) sit in unreachable code — the F.W4 denominator needs re-derivation (FR-GFC-28); (d) `GalleryMarquee.vue` (134 lines) + its permanent-local `./scrolling-text` uplift question (ABSENT at v5/v7, git-show-verified) currently burden F.W1 for a component that renders nowhere. **→ F.W4** (mount it reachably — e.g. hoist above the empty-state guard with its own ≥4 predicate — or delete it; one authored decision). **Anti-rename**: when the GalleryMarquee/GalleryView seats' apotheoses land, their independently-derived instances of this defect fold to THIS row, one identity. |

### MINOR

- **FR-GFC-10 = D-4 / L-7 — MINOR.** `scrollbar-width: thin` with no `scrollbar-color` (`:66`) beside an ungated `::-webkit-scrollbar` block (`:77-84`) is the exact live+dead alias the producer's `.scrollbar-thin` utility documents and prevents (`base.css:149-171`, `@supports not (scrollbar-color: auto)` gate; tokens `--scrollbar-thumb` at `color-radius.css:131`). Hand-rolled thumb fainter (15% vs 25%) and thinner (4px vs 8px) than the system floor. Engine-precedence half UNPROVEN-NEEDS-LIVE; the authoring divergence is source-certain. Utility is in the always-imported bundle — F.W1-independent. **→ F.W3/W4.**
- **FR-GFC-11 = D-9 — MINOR.** `text-amber-400` on the header Crown (`:23`) where `--tier-featured` exists in all three arms + the Tailwind bridge and paints the identical Crown at `GalleryCard.vue:150`. Theme-blind in dark mode; structurally unreachable by the D.W4.d contrast-carry class of repair. D's HONEST COUNTER adopted (the token is marginally worse on light contrast; 1.4.11 does not bite — `:24` carries the meaning). **→ F.W3/W4.**
- **FR-GFC-12 = D-10 — MINOR.** Label group inset 0.25rem past the content it labels (`:49-51` vs `:53-59`); the sidebearing falsifier is mechanically sound (container padding displaces icon and text equally); sibling counter-evidence at `GalleryInfiniteGrid.vue:26-27`. **→ F.W4.**
- **FR-GFC-13 = D-12 / L-14 / C-15 — MINOR.** `v-if="entries.length > 0"` (`:21`) is unreachable-false at the sole mount (`GalleryView.vue:250`; grep = 2 hits, both GalleryView); both readers reproduced the compiled dead branch by execution. Disposition CONSTRAINED by K5: which guard survives is decided together with FR-GFC-1's loading design — deleting the parent's guard in isolation re-creates the skeleton-false-promise failure this seat killed. **→ F.W4**, folded into FR-GFC-1's cure.
- **FR-GFC-14 = D-13 — MINOR.** Physical properties (`to right`, `padding-left`, `padding: 0 1rem`) where the producer authors edge-relative/logical; capped honestly (no `dir`, no i18n). **→ F.W3/W4** with FR-GFC-5.
- **FR-GFC-15 = L-8 — MINOR.** Dead `@reference "tailwindcss"` (`:47`; the block uses zero Tailwind features). Both readers independently reproduced the census: 53 scoped-style SFCs / 35 with `@reference` / exactly 9 feature-free, 7 of 9 in `gallery/` — a directory-shaped copy-paste. **→ F.W4** (hygiene sweep, the 9-file list rides). |
- **FR-GFC-16 = L-10 — MINOR.** Nested horizontal scroller with no `overscroll-behavior` against a six-site in-repo convention (this seat's grep: PaperSidebar ×2, PaperView, PaperSearch ×2, MobileFloatingToc; zero in gallery/). Gesture outcome UNPROVEN-NEEDS-LIVE; the omission is proven. **→ F.W4.**
- **FR-GFC-17 = L-11 / C-11 — MINOR.** All four emit signatures say `hash` (`:14-16`); every payload is a `slug` (GalleryCard :141/:162/:171/:180); the contract makes the noun load-bearing (`gallery.ts:16-24`, `types.ts:211` "never identity") and the store signatures are already converged — the leaf/relay layer is the last holdout. Type-only cure, vue-tsc-caught. **→ F.W4.**
- **FR-GFC-18 = L-12 / C-12 — MINOR.** Prop optionality drifts across the relays of the same card (`adminMode?`/`likedHashes?` here and in the dead marquee vs required in the grid); latent (sole parent supplies both), and C-12's silent-failure form adopted: omit `likedHashes` and `isLiked` pins false forever, `aria-pressed` lying with no type error. **→ F.W4.**
- **FR-GFC-19 = L-13 — MINOR.** `onClick` escapes the handler cache while its three siblings are cached (`hasScopeRef` on the v-for `entry`), minting a fresh closure per card per parent render and defeating the child's props fast path. **Reproduced by BOTH readers independently with the repo's own `@vue/compiler-sfc`, token-for-token** — the best-evidenced row in this corpus. One-line cure (`click: [entry: Visualization]` on GalleryCard) fixes the grid's identical wart in the same edit. **→ F.W4.**
- **FR-GFC-20 = C-9 — MINOR.** A carousel-initiated delete leaves the stale slug in `selectedHashes` (`handleDelete` :137-141 never touches it; complete writer set re-derived — no pruning watcher exists), and the batch path posts the stale set. Fold-in by reference: the `BatchResponse.errors` reporting arm is itself dead per banked **FR-AUL-13** (never populated by admin.py) — partial failures report green. **→ F.W4** + **F.W5-W8 rider**.
- **FR-GFC-21 = C-10 (devDependency half; baseline half killed, K9) — MINOR.** `lucide-vue-next` is a runtime import (`:4`) declared in devDependencies; convention-note cap honoured (reka-ui/clsx/cva/tailwind-merge sit there too); both icon packages installed side by side; `@lucide/vue ^1.16.0` is the peer at the PIN, not an uplift delta. **→ F.W0/F.W1** (one of the 35 rename sites).
- **FR-GFC-22 = C-16 — MINOR.** GalleryCard ships two dead imports into every render path (`VIZ_COLORS` :9, `PathPreview` :10 — one hit each, never referenced); no gate can catch it (`strict` without `noUnusedLocals`; `verbatimModuleSyntax` emits both). Attributed to GalleryCard, inherited here as 100% of what this component renders. **→ F.W4** + **F.W0 rider** (`noUnusedLocals`).
- **FR-GFC-23 = MISS-3(DU) — MINOR (net-new).** In the carousel, admin mode converts a ~24px square of every card into a null target: the checkbox wrapper's `@click.stop` (`GalleryCard.vue:85-89`, correct in the grid) combines with the severed seam (FR-GFC-2) so the click neither selects NOR opens the card — compounding FR-GFC-7's affordance poverty. Registry-swept: no banked row covers it. **→ F.W4** (dies automatically with FR-GFC-2's cure; recorded so the fix is asserted, not assumed).
- **FR-GFC-24 = MISS-GT(LC) — MINOR (net-new).** The `GalleryTier` union is retyped inline at six SFC/handler sites (this seat's census: carousel `:15`, grid `:19`, card `:29`, marquee `:15`, modal `:28`, `GalleryView.vue:132`; plus two `"all" | …` variants) against the one canonical export (`types.ts:96`) that the same tree already imports elsewhere (`gallery.ts:3/:138`, `api.ts`, AdminFlaggedPanel). A fourth tier is a six-file template edit with no compiler assistance at the missed site. **The D-axis inversion is corrected on the record**: D·S-3 credits the inline literal as a superlative — the guarantee is real but `GalleryTier` delivers it plus a single edit point; the credit belongs to the import that was declined. Prior art declared, not re-booked: FR-AFP-59 banks the ADJACENT widening limb (`tier: string | null`), not this retype limb. **→ F.W3/W4** (one import, six deletions; close with the sibling family's count in one breath).

### INFO

- **FR-GFC-25 = D-15 / L-9 / C-13 — INFO (R-6).** `--deferred-section-size: 17rem` calibrated for the ~15rem grid cell (`GalleryCard.vue:199-206` verbatim), imposed on this 16rem cell (~18rem actual). Self-correcting first-paint transient by the `auto` prefix (producer's anti-jump doc). Magnitude → SS-13. **→ F.W4** measurement.
- **FR-GFC-26 = D-14 — INFO.** No count, no orienting prose (vs `GalleryDraftsSection.vue:58`'s MetricBadge — D's :59-64 cite corrected). Defer-honestly disposition adopted: `./metric-badge` → `./metric` at v7 (git-show-verified), so the count lands during **F.W1** in the `./metric` idiom, never today.
- **FR-GFC-27 = L-15 — INFO.** Zero coverage at every available level (8 e2e specs, none reaches a card; no vitest repo-wide — this seat's re-run). FR-GFC-2/-9 are exactly the class one admin-mode component test would catch. **→ F.W4** (test rider on the cure wave).
- **FR-GFC-28 = L-16 / D-16 — INFO.** R5-7 class member (v-for on a native `<div>` `:28`, invisible to callsite-keyed derivation; the sibling grid is the control) and one of the two live callsites behind R3-12's `basisLabels` duplicate. Denominators re-run and exact (33 v-for sites). **Count correction carried**: FR-GFC-9 puts `GalleryMarquee:29,36,52` in unreachable code — the 17-native/16-component split and the R6 reconciliation need re-derivation before any spec quotes them. **→ F.W4** (instance-denominator law; fold to the banked R5-7 routing).
- **FR-GFC-29 = MISS-CB(LC) ⊕ D·S-1's verified surface — INFO (net-new).** The root-barrel `Checkbox` import (`GalleryCard.vue:5`) cannot be narrowed to a subpath at the pin OR at the uplift target: `./checkbox` ABSENT at 4.0.0 (80-key map, this seat's node run) and at v7.0.0, PRESENT only at v8.0.0 (git-show, both tags). D·S-1's zero-break-surface verdict survives — but only because Checkbox rides the barrel (`v7.0.0:src/index.ts:88` still re-exports it); a wave reading S-1 as "tighten the imports during uplift" finds no subpath until 8.0.0. Baseline-drift note: producer working tree is now 8.0.0 (v8.0.0 tagged 2026-08-09, after the challenges' 2026-08-06 authoring; their "producer latest 7.0.0" was true when named) — every producer claim in this record is verified at the TAGS, not at HEAD. **→ F.W1** (footnote on the uplift transaction).

### FOLDS by reference (identity guard — never re-booked)

- **FOLD-1 = D-8 (`.cm-serif` cannot produce Computer Modern) → banked FR-AH-2 · MISS-FONT** (`fr-AppHeader.md`, blocker-weight font-truth cluster). This seat re-verified the evidence independently (`--font-serif-math`: one hit in the producer dist — the consumption; zero over `web/src`+`public`+`index.html`); subject `:24` is one of the 17 `.cm-serif` files. D-8's "one `@theme` line" disposition is CORRECTED by the banked root cause: `@mkbabb/glass-ui/styles/fonts` is a deliberate unimported subpath — the decision is FR-AH-2's F.W4 + GLASS-RELAY, not a local one-liner. Cross-referenced, zero-drop; no new row.
- **FOLD-2 = C-14 (value.js peer range violated at the pin) → banked** at `fr-EquationPanel.md:117` (identical two-clause range vs web's `^0.13.0`) and fr-CanvasOverlayButton (npm ls ELSPROBLEMS reproduced). Re-measured by both readers and corroborated by this seat's node run. Latent for this subtree (Badge/Button/root-Checkbox pull no value.js chunk). F.W1 rider carried: at v7/v8 the peer moves to `^4.0.0` — the uplift RELOCATES the violation, it does not merely repair it.

## Killed-claims register

- **K1 · D-11 ("the inherited hover motion is unguarded under PRM … glass-ui ships no universal PRM reset") — KILLED; reader-LC's CONFIRMED refuted (R-1).** The universal blanket exists at the installed pin (`a11y-overrides.css`, both arms read verbatim by this seat), is reached end-to-end, and strikes `transform` from `transition-property` with `!important`. The marquee's local guard is belt-and-braces over the blanket, not evidence of a gap. Third banked citation-inheritance instance on this file.
- **K2 · L-2 ("'Unfeature' is a batch button that can never act … dead by construction") — KILLED (both readers concur; writer-set verified by this seat).** Reachable path: tick a normal card in the grid → click its Crown → `handleSetTier` never reconciles `selectedHashes` → "Unfeature" is live against a genuinely featured slug. C-4's phantom-selection framing books instead (FR-GFC-2); L-2's MAJOR rested on the false absolute.
- **K3 · L-6's falsifier + contradiction-filing ("the marquee is live at HEAD; the D-corpus orphan finding is stale") and the four-callsite / three-host fan-out counts (L-6, D-2 §scope, D-16) — KILLED by FR-GFC-9.** The mount predicate is unsatisfiable for all inputs; the D corpus was right; L's §5 cure item 1 prescribes ~4 lines of repair to unreachable code. Fan-out = two live callsites.
- **K4 · C-5's absolutizations ("fallback hexes in both modes, forever"; "the pills are exactly the surface the D.W4.d repair existed to protect") — KILLED (R-3).** Path-dependent capture (lazy route + isReady mount, this seat's read); `--viz-amber` is never read by basis-display. The durable theme-desync half books as FR-GFC-8. Reader-LC's "module evaluation strictly precedes mount" cell corrected on the same evidence.
- **K5 · D-1's prescribed cure (loading prop + skeleton ghosts + delete the parent guard) — KILLED as structurally unsound (cure-adjudicated-separately law, ColorSpaceSelector addendum §2).** `gallery.loading` is global to the list: with the parent guard deleted, the skeleton row fires on every refetch of every gallery with zero featured rows — and with MISS-1 in view, on every public search pause. The store-side patch-in-place D-1 itself subordinated is the sound cure (FR-GFC-1).
- **K6 · D-5's "`.fading-scroll--x` … also cures D-3" + its §7 cure cell + C-6's "three characters of class list away" — KILLED at the installed bytes (R-7).** The modifier declares overflow-x + mask only (no `overflow-y` anywhere in the sheet); the fade animations are gated on presence attrs only the `<FadingScroll>` component sets — the bare class fades nothing on any engine. The C axis's §"sets only the overflow and leaves padding to the consumer" is the correct reading.
- **K7 · D-5's evidence cell "`grep -rn scroll-padding web/src` → zero hits" — KILLED as stated.** One hit (`PaperSidebar.vue:159`, this seat's re-run). The substantive claim survives and STRENGTHENS (an in-repo precedent for exactly this compensation, never adopted by the gallery family) — but a zero-count that is not zero is a provenance defect a later lane will re-run and mistrust the row for. Survivor renamed inside FR-GFC-5.
- **K8 · D-7's "the finding survives either branch of D-4" 2.5.8 argument + C-8's "no non-drag, non-wheel way to advance" — KILLED / RESCOPED (R-2).** Engine-split and contingent on an UNPROVEN precedence cell; tabbable cards concede the advance path in both axes' own falsifier sections. The affordance substance rides FR-GFC-7.
- **K9 · C-10's stale-baseline framing ("false about the exact package the uplift renames … glass 7's peer") — KILLED per the banked C-20 kill** (`fr-EquationPanel.md:117`): `@lucide/vue ^1.16.0` is the peer at the installed 4.0.0 (this seat's node run) — a standing divergence at the pin, not an uplift delta. An error the fleet had already ruled on, re-committed.
- **K10 · challenge-D's systematic sibling-file line drift — RECORDED as a provenance defect (same class banked at fr-CoefficientsPanel:6).** Spot-verified by this seat: `GalleryMarquee.vue` padding comment lives at :82-83 (D cites :77-79), mask at :85-88 (D cites :81-84); `GalleryDraftsSection.vue` MetricBadge at :58 (D cites :59-64); `GalleryInfiniteGrid.vue` `loading` at :8 (D cites :11). No substance changes — every cited fact exists at the corrected line — but repair waves must take anchors from THIS record, not from challenge-D. The L and C axes verified line-exact everywhere sampled.

## Missed finds — adopted after re-proof

1. **MISS-1 (DU)** — the unmount is public, not admin-only; three dead controls each fire `resetAndFetch()`. Re-proven by this seat (watchers `:95-98`/`:111-114`; zero readers for all three filters; `{limit, sort, owner}` only). **Folded into FR-GFC-1** as the blast-radius arm.
2. **MISS-2 (DU)** — the unreachable marquee mount. Re-proven; **booked FR-GFC-9**, the corpus-consequence row.
3. **MISS-3 (DU)** — the admin dead click zone. Re-proven from the card's wrapper; **booked FR-GFC-23**.
4. **MISS-4 (DU)** — recorded as a PRE-EMPTIVE KILL, not a defect: lucide icons auto-set `aria-hidden="true"` (`Icon.js:41`, this seat's read) — no future reader should file the header Crown as an unhidden glyph; GalleryView's explicit `aria-hidden` attrs are belt-and-braces. Does NOT touch the banked text-glyph rows (fr-EquationPanel NotationPills, fr-BasisSelector) — those are `<span>` glyphs and stand.
5. **MISS-GT (LC)** — the GalleryTier retype census + the D·S-3 inversion. Re-proven by this seat's own grep (6+2 sites). **Booked FR-GFC-24**.
6. **MISS-CB (LC)** — the forced root-barrel Checkbox until 8.0.0, with the tag-level re-verification of every producer claim. Re-proven at both tags by this seat. **Booked FR-GFC-29**.

## Superlatives (provenance preserved)

- **L-13 as method**: the strongest single evidence item in the corpus — a compiled-artifact proof of a source-invisible defect, independently re-executed to token-identical output by BOTH readers with the repo's own compiler.
- **The L axis's executed denominators are exact** (33 v-for / 35 `@reference` / 53 scoped / 9 feature-free, filenames character-exact per reader-LC) and it refused to close what it could not reach (R6 registry marked UNPROVEN). Its two hardest calls (L-2, L-6's contradiction) are also its two kills — provenance discipline and judgment are different muscles.
- **The C axis's honoured falsifiers cost it its own headlines**: C-4 found the phantom path against its interest; C-8 conceded the hoisting + non-drop-in; C-7 read `.fading-scroll--x` correctly where the D axis did not — the one line that would have prevented D-3's unsound cure. C-1's docstring-vs-comment contradiction plus `cursors.py:22` is genuine archaeology, line-exact into the Python tree.
- **Convergent correct refusal**: all three axes independently ran and killed the same two tempting claims (2.1.1 keyboard-unreachable; mask-clips-the-lift) — the strongest corpus-level signal that the surviving rows are real.
- **L F-1 / D F-4 + L F-2**: falsifiers that EARNED their keep — F-1's shadow-token kill narrowed L-4 to the strictly-true glow/lift/outline claim; two independent routes (producer ruling + Tailwind v4 pipeline; Safari 15.4 baseline + no autoprefixer) converged on the same inversion (the marquee's `-webkit-mask` is the redundancy; the subject is on the right side).
- **The component's own craft, verified at the bytes**: textbook snap wiring (`x mandatory` on the port :67, `align: start` on the item :89, `flex-shrink: 0` :87); the fixed-width wrapper is LOAD-BEARING (snap geometry immunized against `contain-intrinsic-size` guessing — the naive L-16 fix would have destroyed it; any refactor must preserve it); `:key="entry.slug"` is the one right answer under a contract that forbids `content_hash` as identity — and matters precisely because FR-GFC-1 replaces the list wholesale so often; the two-argument relay at `:38` preserves what the reflexive `$event` form would silently truncate into `api.setVisualizationTier`'s PUT body, and it is house style (the grid writes the same arrow), cached, costing nothing; nil teardown contract (no state, no listeners, no lifecycle — nothing to leak, in a family whose banked registry carries FR-AUL-15); zero `:deep`/`!important` — the CSS stops at the child's boundary while the child itself does reach. Of the ~30 distinct defects adjudicated here, not one lives in this file's 18 script lines.
- **Server-side, unrecorded by all three axes (reader-LC's find, adopted)**: `cursors.py:58-79`'s compound keyset predicate (`$or` on sort-field + `_id` tie-break, stale-key 400, limit+1 next-cursor) is exactly what makes a page-boundary duplicate `:key` impossible — the restraint of L F-5 ("I will not manufacture a defect from a possibility") was correct, and the server earns the credit it withheld. Banked so the next reader is not tempted.
- **D·S-1's uplift claim survives a harder test than it set itself**: every subpath re-verified at `git show v7.0.0` AND the root-barrel SYMBOL check (`index.ts:88` still exports checkbox) — zero break surface at the symbol level, with FR-GFC-29's footnote as the one caveat.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. Chromium ≥121 precedence: whether declared `scrollbar-width` suppresses same-element `::-webkit-scrollbar` (FR-GFC-10's engine half; decides FR-GFC-7's 2.5.8 residue). Chromium + Firefox + Safari 18.2+.
2. Exact clipped pixel count at the strip's block-start edge; whether any engine paints a vertical scrollbar on `.featured-scroll` (FR-GFC-6).
3. Whether Chromium's classic thin-scrollbar gutter (~11px) exceeds the 8px `padding-bottom` and overlaps card ink (reader-LC's new hazard; no axis raised it).
4. Whether the mask at `:68-74` also feathers the native scrollbar thumb at the strip's ends (reader-DU's new hazard) — if so, the sole pointer affordance is half-erased exactly where a user reaches for it, compounding FR-GFC-5/-7/-10 into one interaction failure.
5. Overscroll chaining / WebKit back-swipe at the strip's ends (FR-GFC-16).
6. Rendered face of `.cm-serif` (FOLD-1's live half; the cascade is closed statically).
7. First-paint settle magnitude from 17rem-vs-~18rem (FR-GFC-25).
8. **The cheapest high-value probe on this list**: observed basis-pill colours per navigation path (deep link `#bf4040` vs in-app resolved) and the theme-toggle no-op on both — decides whether the colors.ts wave must also fix the snapshot (FR-GFC-8's wave lock).
9. CLS/pixel magnitude of the FR-GFC-1 unmount (mechanism source-certain; magnitude not).
10. Rendered target size of the scrollbar thumb per engine (closes the 2.5.8 residue with #1).
11. L-16's 17-native/16-component split re-derivation and the R6 registry reconciliation — post-FR-GFC-9 the count is stale regardless; R6 artifacts unreachable from this checkout.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance event (evidence about the fleet)**: reader-LC confirmed D-11 by repeating the D-axis's "no universal PRM reset" cell against the two fourier-local greps — the exact blind spot banked twice before on this same producer file (fr-AdminFlaggedPanel ruling 1; fr-CoefficientsPanel R-1, where the LC-seat ALSO inherited it). The predicted class keeps firing; a11y-overrides.css should be on every fourier reader's mandatory-read list.
2. **Severity dissents preserved**: D-1 and D-2 were filed BLOCKER by their authors (and L-1 BLOCKER by the L axis); reader-DU flagged MISS-1 as re-rating FR-GFC-1 "toward genuinely blocking." Ruled MAJOR on family precedent (R-5); if the forming spec grades the public-path churn as its own surface, the blocker-weight flag is the hook.
3. **Reader-LC's MINOR on the deferred-size row** preserved against this seat's INFO (R-6).
4. **Reader-DU's booking of the C-10 kill** rested partly on the banked C-20 precedent rather than a fresh tag-read; reader-LC re-verified at all three trees (4.0.0/v7/v8). Both reach the same verdict; LC's evidence cell is the one to quote.
5. **Baseline drift, dated**: the challenges named "producer latest 7.0.0" truthfully on 2026-08-06; v8.0.0 was tagged 2026-08-09; this record (2026-08-24) verifies producer claims at tags v7.0.0/v8.0.0 and notes the working tree is 8.0.0. Future readers: the uplift target named by the census (7.0.0) is no longer producer HEAD — the forming F.W1 spec must restate its target explicitly.

## Verdict

**DEFECTIVE at the seams, exemplary at the leaf** — 31 adjudicated rows (0 BLOCKER · 9 MAJOR · 15 MINOR · 5 INFO · 2 folds), 10 kills banked, 6 missed finds adopted: the 18-line relay itself is among the cleanest files in the gallery family, but it is the sole render surface for two operations that do not exist (like, tier), it destroys itself on every mutation and every public search pause, its admin surface is inert-with-affordances, its 45 CSS lines hand-roll four producer primitives — twice reproducing defects the producer retired by id — and the empty-state design its family was rebuilt around is unreachable code. Dispositions are ADJUDICATED, not VERIFIED; routings are agglomeration input for the forming X·F specs (M-25); nothing here stamps a wave.

— end of apotheosis. The three challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
