claude-fable-5

# kf-ChannelOptions — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: see the SUBSTRATE RULING below — it is this record's headline. Registry branch `tranche-u` (value.js).
**Corpus**: three challenge axes by `claude-opus-5[1m]` — `challenge-D-design.md` (20 defects / 5 superlatives), `challenge-L-library.md` (R1+R2+R3 merged: 37 / 9, with one in-place self-falsification §9.0 and one self-downgrade §9.4), `challenge-C-consumption.md` (19 / 5, superseding a weaker draft) — attacked by two hostile re-readers: **RR-A** (design-and-usage) and **RR-B** (library-and-code). Both re-reader returns were themselves challenged by this seat before adoption.
**Method (L-18/M-23)**: every verdict-level disagreement between the re-readers was re-derived against the tree with this seat's own instruments; every kill was re-executed at its decisive file; every net-new MAJOR+ find was re-verified at source before banking; and — because the kf-AmigaScene precedent demands it — the substrate itself was interrogated, which neither re-reader did. **No browser tooling** (law); all rulings static + dist/blob reads; the irreducibly visual residue rides to SS-13 under KF.W9.
**Arbiter's own instruments this session (all read-only)**: `ChannelOptions.vue` whole (609 L, disk); `useTimingFunctionEditor.ts` whole (237 L, disk); `TimingFunctionPanel.vue` whole (166 L, disk); `LayerConfigPanel.vue` whole (92 L, disk); `animationOptionsStore.ts` :40-122 + `hashSharing.ts` :28-46; `useAnimationSync.ts` polling census; `PlaybackRibbon.vue` :19/:119-121/:184/:198; `ChannelControls.vue` :95-140; `useKeyframesPaneReveal.ts` :115-140; `KeyframesStringControls.vue` :125-170; `useKeyframeOps.ts` :58-90; `easingGroups.ts` family/item census (10 families / 29 items); `animationDescriptions.ts` NAMED_EASING_BEZIER quart/quint rows; `src/animation/engine/options.ts` (disk AND `origin/master`, whole normalize block); `waapi/eligibility.ts` :138-179 + drift stat; installed **glass-ui 7.0.0** dist: `labeled-field.js` all four compiled props tables + emits + LabeledSelect/LabeledSwitch render bodies + `inheritAttrs` count (0); `select-DD6Ly6xg.js` Select/SelectItem/SelectLabel/viewport (`max-h-[inherit] overflow-y-auto`); `easing.js` (EasingPicker props, `initialSteps ?? 4` unclamped seed, Slider `min:1 max:12`, `"Steps (n) — " + raw`, `ease-out-back` constant, `watch(modelValue, …, {deep,immediate})` write-through + `q()` echo suppressor); `class-names-Cpy5eaBk.js` conflict-group table; `styles/utilities/base-misc.css` (whole `@layer components` wrap, `.gold-shimmer`), `a11y-overrides.css` (PRM allow-list, forced-colors, `touch-hit-area`), `dock/styles/controls/{icon-button,touch-floor}.css`; installed **reka-ui** dist: `shared/useForwardProps.js` whole, `Select/SelectRoot.js` :74-82, `Select/SelectContent.js` :126, `Switch/SwitchRoot.js` :64-96; git forensics: `git rev-list --count HEAD..origin/master` (41), `git show origin/master:package.json` (:77 glass-ui devDep; :37 check script), per-file `git diff` disk↔`8281638c`↔`origin/master` over the whole blast surface; executed greps: `focus()\|nextTick\|aria-expanded\|inert` in ChannelOptions (0), `easingItems` consumers (0), component/composable names in `test/` (0), `gold-shimmer` in demo (2), `Object.assign` in animationOptionsStore (:80).

## SUBSTRATE RULING (read first — it rescopes two BLOCKERs)

The corpus and BOTH re-readers cite "HEAD `8281638c`". **The tree they actually measured is neither that commit nor any commit.** The checkout is the SACRED stale `8281638c`, **41 commits behind `origin/master` `81a56990`** (census §0 schism, SCH-1) — and the **working tree is dirty across the whole demo**: for this component's blast surface the disk equals `origin/master` byte-for-byte (`ChannelOptions.vue`, `LayerConfigPanel.vue`, `animationOptionsStore.ts`, `easingGroups.ts`, `animationDescriptions.ts` — my per-file diffs: 0 lines vs master, hundreds vs `8281638c`) **except three named reversions/lags**: (1) `package.json` lacks the `"@mkbabb/glass-ui": "7.0.0"` devDep that `origin/master:77` carries; (2) `useTimingFunctionEditor.ts` lacks master's **EE-02 css-twin** on `setAnimationTimingFunction` (disk `{ fn }`, master `{ fn, css }` + `timingFunctionLiteralFor` passed through); (3) `TimingFunctionPanel.vue:144` lacks the same EE-02 twin on the bezier-drag write. Consequences, each verified by this seat at both coordinates:
- **L·B-3/C·B-2 (phantom dependency) is FALSIFIED AT HEAD** — the SCH-1 ruling and the kf-AmigaScene C-1 precedent apply verbatim. The disk-manifest fact was real; the tree-naming was wrong. Both re-readers "re-verified at HEAD 8281638c" against the disk manifest — a **second recorded citation-of-tree event** in this program.
- **L·B-2's two measured consequences (WAAPI demotion + export-serializer identity-miss) are CURED at `origin/master`** by EE-02 at both write sites. The disk WIP would **regress the landed cure if ever committed as-is** — KF.W0's §B-12 reconciliation must disposition these three files explicitly.
- Everything else in this roster **survives at `origin/master` verbatim**: the phantom props (line-identical), the dead switch, the store defaults (`5s`/`alternate`/`ease-in-out`, `steps:100`, quad `[0.2,0.65,0.6,1]`), `normalizeIterationCount`'s `"∞"` acceptance, `normalizeDuration`'s throw on `""`/`"5"`, the WAAPI css-twin refusal, and `check` at master = `tsc … && npm run proof:structure` — **still no vue-tsc**.
Line anchors below bind to the **disk** (= what all five prior seats measured); re-resolve against the reconciled tree before any cure executes.

## Scoreboard

**58 ADJUDICATED-CONFIRMED (6 BLOCKER · 19 MAJOR · 25 MINOR · 8 INFO) · 11 KILLED (two of them kills-of-kills, one a substrate falsification of a row all five prior seats confirmed) · 9 net-new re-reader finds BANKED (one escalated by this seat to carry a GLASS-OWNED producer half) · 2 RESCOPED-BY-SUBSTRATE · 10 UNPROVEN-NEEDS-LIVE rows to SS-13/KF.W9.**

Blocker headline: **KF-CO-1** — the `:is-open` phantom does not merely waste a mutex: through glass-ui's Boolean-cast `open` prop it leaves the **direction, fill-mode and blend dropdowns permanently CONTROLLED-CLOSED — they can never open at all.** All three axes read the correct `.d.ts`, filed the phantom, and graded the damage "~nil"; RR-B alone walked the cast/forward/passive chain. This seat re-derived every link and adds the producer half no prior seat filed.

Status verbs per M-25: dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; routings are agglomeration input keyed to the census wave sketch **KF.W0–KF.W10** (`CENSUS-2026-08-03.md` §wave-sketch). Behavioral rows agglomerate into one proposed wave-surface, **OPTIONS-UNIT** (NO-WAVE-OWNER; SS-1/SS-2 spec authoring to consume), sequenced after the KF.W4 vue-tsc gate — the kf-CSSCodeEditor EDITOR-UNIT precedent.

## Ruled disagreements (the seat's actual work)

| id | RR-A (design-and-usage) | RR-B (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **L·B-3 / C·B-2** | CONFIRMED BLOCKER, "verified at HEAD 8281638c" | CONFIRMED BLOCKER, manifest re-probed | **BOTH OVERRULED — FALSIFIED AT HEAD (SCH-1)** | `git show origin/master:package.json` → `:77 "@mkbabb/glass-ui": "7.0.0"` exact devDep; checkout 41 behind; disk manifest genuinely lacks it. One identity with census F-1 ≡ kf-AnimationControlsGroup L-3/C-1 ≡ kf-AmigaScene C-1 — discharged by KF.W0 §B-12, never re-booked. Both re-readers re-ran the probe against the dirty disk while naming a commit: the AmigaScene lesson (*"a challenge lane's 're-run today' is worthless without naming the tree"*) now has a re-reader-tier instance. |
| **Mutex damage** | Carried D-1 §4's "written and never read", left the axes' "damage ~nil" notes standing | KILLED all three mitigation notes; filed the controlled-closed BLOCKER with an SSR prop-chain probe | **RR-B wins; chain re-derived link-by-link by this seat; + a producer half RR-B did not file** | My reads: `labeled-field.js` LabeledSelect compiled props `open: { type: Boolean }` **no default** → Vue casts the absent prop to `false`, and the render passes `open: e.open` **unconditionally** to the inner Select; glass-ui `Select` (`select-DD6Ly6xg.js`) declares `open:{type:Boolean}` and forwards via `useForwardPropsEmits`; reka `useForwardProps` (read whole) forwards every key **assigned in `vnode.props`** whose value ≠ undefined — `open:false` is assigned, so it lands on `SelectRoot`, where `useVModel(props,"open",emits,{ passive: props.open === void 0 })` (:79-81) goes **non-passive** → controlled at `false`; `SelectContent.js:126` `present = forceMount \|\| rootContext.open.value` → permanently false. The bespoke easing `<Select>` at `:204` binds no `open` → not in vnode.props → not forwarded → passive → works: exactly the observed asymmetry. **Producer half (this seat's own, GLASS-OWNED)**: because the cast+unconditional-forward happens inside LabeledSelect, **every** consumer that does not actively drive `open` gets a select that can never open — a 7.0.0 vendor defect (`default: undefined` is the one-token producer cure) → **glass-ui BH relay** (owner edict). Consumer cure here: `:is-open`→`:open` ×3 — which also makes the mutex real, because `openSelect` then actually drives the prop. |
| **L·B-4 (the keyframes-pane curve rewrite)** | Left STANDING **NOT ENDORSED** (scope declaration: chain read, not exercised) | CONFIRMED BLOCKER (R3), four-file chain re-walked | **CONFIRMED — this seat closed all four links itself** | (1) `useKeyframeOps.ts:71-82` (disk, read): writes six stored fields incl. `stored.timingFunction = options.timingFunction` (the literal) and **never** `cubicBezierOptions`/`stepOptions`; (2) `ChannelControls.vue:98` `v-if="hasSurface('controls') && selectedControlSurface === 'controls'"` — fresh mount per tab switch (the Monaco pane at :130-137 is deliberately force-mounted, the contrast is authored); (3) `ChannelOptions.vue:538-542` unguarded `updateTimingFunctionFromName(stored.timingFunction)`; (4) `useTimingFunctionEditor.ts:140-158` uses the literal **only** to compute the kind, rebuilds from the stored quad `[0.2,0.65,0.6,1]` / `{steps:100,"jump-start"}`, and `:166-167` **persists the re-derived literal**. Two representations of one curve, a writer for each, a reconciler for neither. Survives at `origin/master` (EE-02 adds a twin; it does not stop the re-derivation). |
| **Dead-switch mechanism** | "uncontrolled and false-seeded: toggles only its own internal ref" | "controlled at false: renders off AND toggle only emits" | **RR-B's mechanism adopted; RR-A's variant corrected in the killed register** | LabeledSwitch compiled props end `modelValue: { type: Boolean }` — **no default** → absent casts to `false`, and the render passes `"model-value": e.modelValue` **unconditionally** (my read of the compiled body); reka `SwitchRoot.js:69-73` then computes `passive: props.modelValue === void 0` = false → **controlled**, `checked = modelValue === trueValue` permanently false, `toggleCheck` only emits. Same cast mechanism as KF-CO-1, second component. The observable both RRs agree on stands: renders OFF regardless of `layerConfig.enabled`, and `@update:checked` listens for an event that is never fired (emits = `["update:modelValue"]` only; `inheritAttrs` count 0). |
| **L·M-3 vs C·m-6 (dead `:progress`)** | Sided with L (C's rebuttal cites a reader inside `v-if`) | Sided with L, refuting C·m-6 explicitly | **L·M-3 CONFIRMED; C·m-6's decline OVERRULED** | My whole-file read: `currentT`/`isAnimPlaying`/`isAnimStarted` are read **only** inside `<Teleport v-if="active">` (:377-401); `:progress="normalizedProgress"` (:317) → `currentT` (:506-510) is therefore the sole per-frame tracked dependency of every **inactive** channel's render; `TimingFunctionPanel.vue:69` declares `progress?` and the 166 lines never read it. C·m-6's "already read at :380" holds only for the one active channel. Magnitude stays live-only (bounded by the sync loop's settle window). |
| **L·B-2 severity/framing** | Kept BLOCKER whole | Killed the "throwing export button" framing (contained); kept BLOCKER on the WAAPI half | **RESCOPED-BY-SUBSTRATE — both halves CURED at HEAD; framing kill CONFIRMED; design residue MAJOR** | My reads: `KeyframesStringControls.vue:134-168` wraps `compileToCSS` in try/catch → `toast.error("Export CSS failed 🔧")`, and at disk/master the export path is a typed **refusal report** (`compiled.refusals` surfaced verbatim) — never an uncaught crash. And the substrate: `origin/master` carries EE-02 at **both** css-less write sites (my diffs, quoted hunks) — `{ fn, css }` restores WAAPI eligibility (`eligibility.ts:175-178` refusal keyed on `css === undefined`, present at both trees) and satisfies the serializer without the identity lookup. Survives at HEAD: the `setTimingFunction` bypass, the engine's `applyTimingFunction`-doesn't-propagate gap (forwarded to the engine lane), N-10's double write per drag, and the registry-foreign-closure fragility (`namedEasing` mints fresh closures — RR-B's identity probe stands). |
| **D-§5 hedge (dropdown scroll cap)** | Discharged it statically | (did not contest) | **RR-A's discharge CONFIRMED** | `select-DD6Ly6xg.js` renders the viewport with `max-h-[inherit] overflow-y-auto` (my read) — a `max-h` on SelectContent (:225, token `min(24rem,60dvh)` via layout.css) is inherited by the scrolling viewport; the 29-item catalogue is reachable by construction. The hedge dies rather than riding to SS-13. |

## Adjudicated defect roster

Identity-deduped: one row per defect, all axis ids attached, original ids for life. Net-new re-reader finds banked as KF-CO ids. Anchors = disk (= `origin/master` for every cited file except the three SUBSTRATE reversions).

### BLOCKER

- **KF-CO-1 (RR-B net-new; missed by all three axes) — the direction / fill-mode / blend dropdowns are permanently CONTROLLED-CLOSED.** Chain in the ruling above, every link this seat's own. Blast: `ChannelOptions.vue:91-113, :115-137`, `LayerConfigPanel.vue:8-18` — three of the card's four dropdowns are non-functional controls wearing functional chrome; the `@update:open` echo makes the mutex bookkeeping *appear* live while the read half is severed. Kills the "damage ~nil" trio (killed register #5). Disposition: **OPTIONS-UNIT** (consumer: `:is-open`→`:open` ×3, which also makes the D-1 §4 mutex real) **+ glass-ui BH relay** (producer: `open`/`modelValue` Boolean-cast props need `default: undefined` — LabeledSelect and LabeledSwitch both; a 7.0.0 defect that breaks every passive consumer). SS-13's first witness: one click on `direction`.
- **KF-CO-2 · D-1 = L·M-1 = C·M-1..M-4 — the phantom-prop family: 21 bindings across two files reach nothing.** `tooltip` ×10, `label-class` ×5, `:descriptions` ×3, `:is-open` ×3 (counting corrections adopted — killed register #9). Compiled props tables re-dumped by this seat: no `tooltip`/`labelClass`/`descriptions`/`isOpen` anywhere in `labeled-field.js`; `inheritAttrs` 0 → dead DOM attributes. The whole explanatory layer (5 field tooltips + 5 panel tooltips + 3 description tables) is inert; KF-CO-1 is this family's sharpest tooth. Enabling condition = the KF.W4 gate hole (below). Disposition: **OPTIONS-UNIT** (rename/rewire pass: `tooltip`→`description`, `:descriptions`→ hand-render or vendor ask, `label-class`→ delete per KF-CO-31).
- **KF-CO-3 · L·B-1 = C·B-1 — the five option handlers persist engine-REJECTED values outside the guard, and the bucket is a constructor argument.** Verified at disk = master: store write textually outside `trySetOption` at :33-41/:52-60/:80-88; `direction`/`fillMode` (:102-108/:126-132) have **no guard at all**; `normalizeDuration` at `origin/master` (my read, whole) still throws on `""`/`"5"` (only `== null` escapes); `getStoredAnimationOptions` (:83-122, read) re-seeds only a missing/empty bucket — a poisoned one has keys; no error boundary. A mid-keystroke value bricks the cube scene at next boot for 7 days. The `#state=` ingress (`hashSharing.ts:28-46` shape-only + `animationOptionsStore.ts:80` wholesale `Object.assign`) is **one identity with kf-AnimationControlsGroup M-10** (the controlOptionsStore instance) — folded by reference; this record adds the animationOptionsStore instance as blast-radius rider, not a second booking. Disposition: **OPTIONS-UNIT** (persist-inside-guard via the N-1 factory; or validate-on-read at the bucket).
- **KF-CO-4 · L·B-4 (R3 §9.1) — a curve authored in the Keyframes pane is silently rewritten to a different curve on the next tab switch.** RULED above; all four links this seat's own. The root is the store's **two representations of one curve** with no reconciler — the exact inverse of the I.W2.S3 literal discipline the same file documents. Disposition: **OPTIONS-UNIT**, and the spec obligation is R3 §9.7's: the fix must *pass the stored literal through*, never re-derive; a repair that only adds the css twin (EE-02 already landed at master) leaves this shipping.
- **KF-CO-5 · D-2 = L·M-8 — both collapsed panel rows keep every control in the tab order and the AT tree.** `:552-583` (my read): the hiding mechanism is `0fr` + `overflow:hidden` + `opacity:0` + `pointer-events:none` — none removes focusability; the hidden rows hold a real `<button>` (pencil), a `tabindex="0"` div, and the vendor drag canvas. `grep inert ChannelOptions.vue` → 0. The house cure is written and applied 30 lines from the mount site (`ChannelControls.vue:123-137`, `:inert="!keyframesActive"` with the aria-hidden-focus rationale in prose — my read). Also opens the K-6 keyboard-only state (pencil reachable while advanced open → both flags true). Disposition: **OPTIONS-UNIT** a11y leg (`:inert` per row); visual consequence → SS-13.
- **KF-CO-6 · D-3 — the closed easing trigger prints the run-on `ease-in-outslow start & end`.** Chain re-verified at the two links this seat could open: the call site puts both spans in SelectItem's default slot with no `text-value` (:240-271, my read); glass-ui SelectItem renders the `description` slot **outside** `SelectItemText` (my dist read: `[l(_(w),{default:…}), g(t.$slots,"description")]`) — the two-part shape the demo wants exists and is unused. reka's textContent-publish/print links adopted from the three prior seats' congruent derivations; the rendered string itself → SS-13. Disposition: **OPTIONS-UNIT** (move the gloss to the `description` slot; or pass `text-value`).

### RESCOPED-BY-SUBSTRATE

- **L·B-2 — twin-strip + registry-foreign closures.** TRUE on the measured disk; **both measured consequences CURED at `origin/master` (EE-02)**; the throwing-button framing killed (register #7). Residue at HEAD, adjudicated **MAJOR** into OPTIONS-UNIT: the direct-field-write bypass of `setTimingFunction`, the engine-side `applyTimingFunction` frame-propagation gap (**forwarded to the engine lane**, per L's own handoff note), N-10's double write, and the N-17 flatten tripwire. **KF.W0 obligation**: §B-12 must reconcile the three lagging disk files so the landed EE-02 cure is not clobbered by a stale-WIP commit.
- **L·B-3 = C·B-2 ≡ census F-1 — FALSIFIED AT HEAD** (ruled above). FOLD → **KF.W0 §B-12**; component rider preserved (four subpath edges, `:143-146`/`TimingFunctionPanel:26-31` authored against 3.4.0/4.0.1 while 7.0.0 resolves — the unpinned-drift half is *live* and is what KF-CO-1/2/18 measure).

### MAJOR

- **KF-CO-7 · L·M-2 = C·M-8 — nothing typechecks the demo — FOLD → KF.W4 (identity: kf-App KF-APP-4 = kf-CSSCodeEditor KF-CE-16; booked once, never re-booked).** Re-verified at BOTH coordinates by this seat: master's `check` = `tsc --noEmit && … && npm run proof:structure` — still no vue-tsc. Component rider: 403 template lines ungated is the enabling condition for KF-CO-1/-2/-8 and the `as any`/handler-shape tail. The OPTIONS-UNIT is sequenced **after** the KF.W4 gate lands (the KF-CE-16 precedent).
- **KF-CO-8 (both RRs net-new) — LayerConfigPanel's `enabled` switch is dead in both directions.** `:64-65` binds `:checked` / `@update:checked`; the vendor knows neither name (my props/emits dump); the Boolean-cast chain (ruling above) makes it render permanently OFF while `layerConfig.enabled` may be true, and clicks change nothing. The layer kill-switch is inert chrome. Disposition: **OPTIONS-UNIT** (`:model-value` + `@update:model-value`) + the same BH-relay producer note; visual state → SS-13.
- **KF-CO-9 (RR-A net-new) — the stored step count (100) is 8× outside the vendor editor's 1–12 authoring domain.** My dist reads: `initialSteps ?? 4` seeded **unclamped**; the authoring Slider is `min:1 max:12 step:1`; the label prints the raw ref (`"Steps (n) — " + …`) → the panel renders "Steps (n) — 100" beside a thumb pinned at 12, and the first drag emits ≤12, defeats `isSeedEcho` (steps differ), and **persists the collapsed literal**. Compounds KF-CO-10 (all three steps aliases start from 100). Disposition: **OPTIONS-UNIT** (either clamp the store default into the vendor domain or file the domain question with the C·M-5 spec); readout → SS-13.
- **KF-CO-10 · L·M-10 = C·M-7 + RR-A's third alias — all THREE 'Steps' family entries are silent aliases for `steps(100, jump-start)`.** `easingGroups.ts:92-97` offers `steps`/`step-start`/`step-end` with three distinct glosses; `parseTimingFunction` collapses the keywords to kind `steps` (§9.0 probe, re-run by both RRs); the steps branch builds from the stored options and ignores the picked name; `timingCurveUtils`' correct `steps(1, …)` special case is unreachable for its own keywords; the Select highlight visibly jumps onto "steps". Disposition: **OPTIONS-UNIT** (dispatch on the *name* before kind-collapse — reanimates code already written); pin with the test the catalogue suite already implies.
- **KF-CO-11 · L·M-4 — mount-time clobber of a scene's own easing authority**, per-tab-switch (fresh mount verified). The "single writer" falsifier is corrected (killed #8: `useKeyframeOps` writes through a destructured alias) without disturbing the conclusion — that second writer fires only on a keyframes edit. Disposition: **OPTIONS-UNIT**, sequenced per R3 §9.7 (KF-CO-12 first makes the guard trivial).
- **KF-CO-12 · L·M-9 (R3) — the five fields render the persisted store, never `animation.options` — fiction on every non-cube scene**, and that fiction is the input to KF-CO-11/-4's re-apply. `getStoredAnimationOptions` read whole: seeds from defaults, never from the animation. The remedy seam exists (`reverseCSSTime`, used by the sibling writer). Disposition: **OPTIONS-UNIT** (seed the bucket from `animation.options` at creation).
- **KF-CO-13 · L·M-5 (as §9.0-corrected) — the pencil flattens `smooth-step-3` and `ease-in-bounce` to linear `[0,0,1,1]` and persists it; `convertedFromName` is computed and never rendered.** Verified: both curves in the catalogue (`:60`, `:89`), both absent from `NAMED_EASING_BEZIER` (my grep shows the table's quart/quint rows, no bounce/smooth-step); `:196-207` miss→linear→persist; destructured at `:479`, zero template reads. Disposition: **OPTIONS-UNIT** (import `bezierPresets`, delete the duplicate table, render or delete the disclosure).
- **KF-CO-14 · L·M-6 = C·M-6 — a fallible synchronous call sequenced ahead of an unrelated await in an unobservable async hook: a throw strands `directions`/`fillModes` as `[]` forever.** Verified at disk AND master: the explicit `throw new TypeError` (`useTimingFunctionEditor.ts:141-144`) is statement one of `onMounted` (:538-547); no `app.config.errorHandler`. Reachable via the same unvalidated `#state=` ingress. The D-12 *mechanism* is killed separately (register #2); this ordering hazard is independent and stands. Disposition: **OPTIONS-UNIT** (reorder or read `kfEngine()` synchronously — which dissolves the ceremony too).
- **KF-CO-15 · L·M-7 — the sync bridge polls `effectiveT`/`started`/`reversed` and not `options.duration`, so the teleported rail's `:max` goes permanently stale after a duration edit.** Verified: polling census (my grep of `useAnimationSync.ts` :33-55); `PlaybackRibbon.vue:119-121` computed over markRaw options consumed at `:19`; keyed-by-id + `v-show` → no healing remount. Disposition: **OPTIONS-UNIT** (add duration to the bridge).
- **KF-CO-16 · L·M-3 — the dead `:progress` prop is the sole per-frame render dependency of every inactive channel** (ruled above; C·m-6's decline overruled). Disposition: **OPTIONS-UNIT** (two deletions + `v-if` the detail panel); frame-cost magnitude → SS-13.
- **KF-CO-17 · C·M-5 — the EasingPicker seeding stack (`:key` remount + `seedPreset` + 25-line `isSeedEcho` + hardcoded `"ease-out-back"`) is a workaround for a vendor limitation that does not exist in installed 7.0.0.** Verified in dist: the modelValue watch write-through (`{deep,immediate}`, sets mode/handles/steps-clamp/term) plus the vendor's own by-value echo suppressor `q()`; the panel binds `:preset/:steps/:term` and never `:model-value`; the comment cites 4.0.1. This kills L·S-6's premise (register #6) — though the mount-emission half of the guard still does real work today. Disposition: **OPTIONS-UNIT spec item, not a mechanical swap** (C's honest scoping adopted: a synthesised `EasingPickerValue` needs `css`+`fn`); the booked vendor asks (initialPoints, label-action slot) stand.
- **KF-CO-18 · D-4 — `.gold-shimmer` cannot paint at this call site.** Verified: the whole vendor sheet is `@layer components` (byte-read), `.gold-shimmer` carries `color: transparent` inside it; `text-muted-foreground` is a real utility in the last layer; the call site keeps it unconditional (:152-159); 2 demo hits, no tie-breaker. The only rendered acknowledgment of a detail easing never appears — what makes KF-CO-24 a *silent* dead end. Disposition: **KF.W6** (glass-consumption discipline: drop the color utility when shimmering, or a vendor variant).
- **KF-CO-19 · D-5 — the gold pencil computes ≈2.08:1 on the light card (bounded [2.08, 2.16] across the glass composite); dark passes 7.01:1.** Both RRs re-derived the arithmetic independently to 4 decimals; adopted. The STY-4 consolidation means the cure is one token in `design-idioms.css:36`. Disposition: **KF.W6** (with D-6/D-13; light-arm gold token); exact composite → SS-13.
- **KF-CO-20 · D-6 — the 'advanced' chevron computes ≈2.01:1 light / ≈2.46:1 dark (`opacity-50` on an already-muted role) — the only navigability mark fails 1.4.11 in BOTH arms.** Adopted on the twin recomputations. Disposition: **KF.W6**.
- **KF-CO-21 · D-7 = C·m-5 = L·N-6 — visible label `easing` vs accessible name `Timing function` (WCAG 2.5.3), and the `<label>` is programmatically dangling** (no `for`, wraps nothing — my read :151-161, :219). The five wired rows get the association from the vendor's controlId seam; only the hand-rolled row loses it. Disposition: **OPTIONS-UNIT** a11y leg.
- **KF-CO-22 · D-8 — the `easing` label tooltip opens on mouse hover only** — `as-child` onto a non-focusable `<label>`; reka injects no tabindex under asChild and early-returns on touch (reader-verified in dist, spot-checked by this seat's read of the call site incl. the `cursor-help` mouse-only cue). Note the *pencil's* tooltip is fine (its trigger is a real button) — the row is the label specifically. Disposition: **OPTIONS-UNIT** a11y leg (dies naturally if the label becomes a wired `for`/`aria-labelledby` label per KF-CO-21).
- **KF-CO-23 · D-9 — selecting `cubic-bezier` or `steps` from the dropdown — entries whose entire meaning is "open an editor" — opens nothing and gives zero feedback** (`openEditorOnChange` written at exactly one site, the pencil — my read :44/:87-91/:171-174; and KF-CO-18 silences the shimmer acknowledgment). Disposition: **OPTIONS-UNIT** (route the two draft kinds through the label-click path, or open on selection).
- **KF-CO-24 · D-10 — the pencil is a 24×24 CSS-px target; glass-ui's coarse-pointer floor EXPLICITLY excludes `compact`; the demo's `.tap-floor` sits unused.** Verified in the vendor sheets (`:not(.dock-icon-button--compact)`; compact padding default `0.25rem`; icon-sm 16px) — and this seat adds: the vendor ALSO ships an unused `@utility touch-hit-area` (44px coarse-pointer ::before) in `a11y-overrides.css`, so *two* sanctioned cures exist. Back control = 28×28, same class. Disposition: **KF.W6**.

### MINOR

- **KF-CO-25 · D-13 (sharpened by RR-A) — the SelectLabel override resolves determinately: family headings render at 10px (`--type-admin-label`, the admin-chrome rung) and `px-2` is inert against the surviving `pl-8`.** My dist reads confirm the resolution machinery: vendor default `py-1.5 pl-8 pr-2 text-dropdown-secondary font-semibold`; the hand-rolled `cn` keys `font-size` (admin-label listed) and `text-color` as separate groups, `padding-x`/`padding-l` separate. The one remaining inference (Tailwind v4 emits `pl-*` after `px-*`) is high-confidence static; 10px-in-a-dropdown taste → SS-13. **KF.W6.**
- **KF-CO-26 · D-14** — `div[role=button]` with no `aria-expanded`/`aria-controls`/`.focus-ring` (my grep: 0 hits for all three). OPTIONS-UNIT a11y leg.
- **KF-CO-27 · D-15 = C·m-2 = L·N-2** — pencil carries a reka Tooltip AND a native `title` (falls through — DockControl declares no `title`), same string, two timings; Back uses a third shape. OPTIONS-UNIT hygiene.
- **KF-CO-28 · D-16** — enter/exit chrome for one navigational act shares neither geometry, hit-area, gap rhythm, nor label color (my reads :281-298 vs :336-351). OPTIONS-UNIT.
- **KF-CO-29 · D-17** — asymmetric optical inset: `px-3` pushes the chevron 12px inside the shared right edge; `gap-x-3` (two-child justify-between) and `justify-end` (content-sized wrapper) both inert. OPTIONS-UNIT.
- **KF-CO-30 · D-18** — mono-as-data applied three ways in one card (dropdown name mono+register, trigger neither, CSS-literal fields none, sibling numeric Input mono). KF.W6 register pass.
- **KF-CO-31 (RR-A net-new) — the label-register split is real but COLOR-ONLY and wrong-direction, settled statically: C·M-4's UNPROVEN-NEEDS-LIVE tag is DISCHARGED.** glass-ui's `.glass-label` default is `--foreground` / `--type-small` / 500 (vendor-scoped `data-v`, unreachable from demo CSS) — size and weight already match the dead `label-class`; only the color differs, so the five wired labels render full-foreground beside three hand-rolled muted labels: an aesthetic incoherence, the inverse of the authored intent, and the vendor default is the higher-contrast side. Cure = delete the dead `label-class` ×5 and pick one register. OPTIONS-UNIT (with KF-CO-2); one live computed-style confirm remains ranked low in SS-13.
- **KF-CO-32 · D-19** — fixed directional glyphs + physical `ml-auto pl-2`/`text-right` in an RTL-ready vendor context. NO-WAVE-OWNER hygiene.
- **KF-CO-33 · D-20 = L·N-11/N-12 + RR-A's three further inert declarations** — dead hooks and dead layout: `.panel-stack` styled nowhere; `easing-edit-btn` a "NAMED BEHAVIORAL SEAM" with zero consumers citing three `proof:bezier-*` gates that do not exist (the retired-proof-idiom feedback is value.js law; the keyframes grep stands on its own — and note master's `check` *does* carry a `proof:structure` script, so the idiom is live-but-elsewhere in this repo); outer grid `gap-4 items-center` with one in-flow child; PLUS CardContent's `gap-2` (one child) and both `relative`s (zero absolute/fixed descendants — my grep). The R3-ceiling breach (609L, no sibling sheet, unenforced) rides the same row. OPTIONS-UNIT hygiene sweep.
- **KF-CO-34 (RR-B net-new) — `useTimingFunctionEditor` imports the whole EASING_GROUPS module solely to build the dead `easingItems` export** (my greps: `:24-26` sole use, zero consumers repo-wide) — merges L·N-7 (`activeCurvePath` also consumerless) and C·m-3. Deleting the export deletes a module-graph edge. OPTIONS-UNIT hygiene.
- **KF-CO-35 (RR-B net-new) — the z-index field commits on `@change` while every sibling commits per keystroke, and `parseInt(…) || 0` silently coerces unparseable AND legitimate-zero alike** (my read, LayerConfigPanel:39-46). OPTIONS-UNIT.
- **KF-CO-36 · C·m-8** — the catalogue under-consumes the preset registry by the six Quart/Quint curves whose quads already sit in `NAMED_EASING_BEZIER:33-38` (my read). OPTIONS-UNIT catalogue pass.
- **KF-CO-37 · C·m-1** — LayerConfigPanel's header comment cites the host `:deep` rule ChannelOptions deleted (tombstone :594-603). Prose-truth; rides any wave touching the pair.
- **KF-CO-38 · C·m-4 = L·N-13** — one vendor emit, two handler shapes (`v ?? false` vs bare) — dead-defensive divergence on an inert mechanism (until KF-CO-1's fix makes it live — reconcile then). OPTIONS-UNIT.
- **KF-CO-39 · C·m-9** — engine-gated dropdowns render a selected value against `items: []` with no `placeholder`; one microtask normally, permanent under KF-CO-14 — and the vendor's `SelectValue` `list.filter(Boolean)` makes the placeholder cure sound (RR-A verified). OPTIONS-UNIT.
- **KF-CO-40 · the L·N tail, adjudicated in bulk at filed severities** — N-1 (five duplicated handler bodies = where KF-CO-3's fix lands once), N-3 (sync returns its input renamed; `isReversed` polled for no consumer), N-4 (∞ vs the word `infinite`, two spellings + the unreachable `=== Infinity` arm — D-11's surviving half lives HERE), N-5 (three docstring drifts incl. the false "empty value is omission"), N-8 (dead COLOR_SPACE/HUE_METHOD tables + bottom-of-file imports), N-9 (`advancedOpen` colocated under the wrong docstring — verified :37-38), N-10 (the drag path writes options+frames twice per pointermove — verified in both files), N-14 (zero tests over the component and all three composables — my grep, 0 hits), N-15 (four `as any` on the enum handlers = C·m-7). RR-B spot-checked this tail against the tree; this seat's own reads overlap it at N-9/N-10/N-14/N-4; RR-A's non-endorsement is preserved in DISSENT.

### INFO

- **L·N-16** Teleport imported from vue (compiler built-in; the import is inert — visible in my read at :437). · **L·I-1** usePlaybackToggle 37 lines for two statements. · **L·I-2** unguarded `String(key)` → `"null"` reaches the TypeError thrower. · **L·I-3** no boundary under a failed engine warm (`main.ts` `.catch(() => undefined)` then mounts — my read). · **L·I-4** `wake()` twice per scrub tick (idempotent). · **L·I-5** module size (403-line template; six multi-statement inline arrows). · **L·I-6** the one version comment that re-verifies TRUE against 7.0.0 (LabeledField still has no label-action slot) — narrows the unpinned-drift charge and is why the wrapper fallback is warranted today. · **RR-A INFO** — the inert-declaration census (folded into KF-CO-33). All NO-WAVE-OWNER; sweep with OPTIONS-UNIT.

## Killed-claims register

1. **D-11's `∞` round-trip trap — KILLED, re-verified by this seat at BOTH trees.** `normalizeIterationCount` explicitly accepts `"∞"` alongside `"infinite"`/`"Infinity"`/`Infinity` (my read of disk AND `origin/master`). Nothing throws, nothing goes stale; the engine met its consumer halfway (superlative). The D axis re-filed at MAJOR a hypothesis the L axis had already killed by enumeration (K-7) — a corpus-internal repetition both RRs caught. Surviving halves live at KF-CO-40/N-4 (two spellings) and inside KF-CO-3 (the panel uses none of the vendor's `invalid`/error surface).
2. **D-12's "chunk request, not a microtask" — KILLED, re-verified.** `main.ts` mounts only after `warmKfEngine()` settles (my read of the `Promise.all(...).finally(mount)` block); the dynamic import is memoized; the empty-item frame is never user-reachable. C·INFO-4 had it right. The narrow placeholder residue is KF-CO-39.
3. **L·M-5's step-start sub-claim — self-falsified in place (§9.0), re-probed by both RRs, adopted.** `parseTimingFunction("step-start"/"step-end")` → kind `steps`; M-5's set is two curves; the keywords' true fate is the worse KF-CO-10.
4. **D-§5's live hedge on the dropdown cap — DISCHARGED STATICALLY in the component's favour** (viewport `max-h-[inherit] overflow-y-auto`, my dist read). A kill that *removes* an SS-13 row.
5. **The mutex-mitigation trio (D-1 §4 "written and never read" as the ceiling · L·M-1 "damage ~nil" · C·M-3 "impact probably nil") — ALL KILLED by KF-CO-1's chain.** The truth is inverted: the state is written, *forwarded*, and weaponized — `false` reaches the vendor and locks three dropdowns shut.
6. **L·S-6's superlative premise ("the vendor EasingPicker has an emit-only modelValue") — KILLED against installed 7.0.0** (write-through watch + vendor echo suppressor, my dist read). The mount-emission half is true and the guard still does work; the *rationale comment* (4.0.1) believed unverified is C·M-5's exact warned class. The C axis's own supersession note documents its earlier draft falling into the same trap — preserved as the method lesson.
7. **L·B-2's "throwing export button on a CSS-animation IDE" framing — KILLED.** The export handler catches and toasts (my read), and the shipped path surfaces typed refusals verbatim. The WAAPI/serializer substance was real on the measured disk and is CURED at HEAD (substrate ruling).
8. **L·M-4's "single writer, grep-verified" falsifier — KILLED as stated** (the literal grep is shape-blind to `useKeyframeOps`' destructured-alias write, my read :71-82); the finding's conclusion is unaffected, but a repair trusting that grep would miss half the reconciliation surface.
9. **Binding-count arithmetic — corrected**: D-1's "twelve" is 14 in its own table; L's "twenty" omits `LayerConfigPanel:13` — the true cross-file total is **21**. No consequence touched.
10. **RR-A's dead-switch mechanism ("uncontrolled, false-seeded") — corrected to CONTROLLED-at-false** (the Boolean cast + unconditional `model-value` forward, my compiled-body read): the switch not only ignores real state, it cannot even toggle its own thumb. A kill that makes the finding *worse*.
11. **L·B-3/C·B-2 as a HEAD fact — FALSIFIED BY SUBSTRATE** (SCH-1; `origin/master:77`). The manifest probes were honest against the disk; the tree-naming was not. Recorded beside AmigaScene's C-1 as the program's second citation-of-tree event — this time surviving TWO hostile re-reads.

(The corpus's own K-1..K-7 pre-killed candidates are noted as received and are not re-counted.)

## Superlatives (provenance preserved; L-18 runs both ways)

- **PRM honest by delegation, and the delegation holds** (D·S-1; verified by this seat: the vendor allow-list `opacity, color, background-color, border-color, box-shadow` excludes `grid-template-rows` — the size morph is removed, the legibility fade survives at 0.1s). Zero local PRM code, correct reduction.
- **The focus-ring inset** (D·S-2 = L·S-9): `overflow:hidden` (mandatory for 0fr↔1fr) clips outside-drawn rings; `padding:2px; margin:-2px` buys exactly the bleed at zero layout cost, arithmetic written down; `min-height:0` the correct grid companion. Held in honest tension with KF-CO-5.
- **The token pair is balanced deliberately** (D·S-3, both arms ≈5:1 within 0.42 of each other) — what makes KF-CO-19/20 diagnosable as *local* failures, not a broken palette.
- **`min(50dvh, 480px)` with the reason named** (D·S-4) — correct unit, dual bound, and the rejected alternative documented.
- **The `.labeled-field-grid` subgrid idiom** (RR-A's own superlative, adopted): one derived label-column width across rows AND across the component boundary — LayerConfigPanel's fragment root makes its rows direct grid children without either file re-authoring the rule; `:deep`-free, vendor-respecting, with a tombstone instead of a shim.
- **`timingFunctionLiteralFor` — the one-persist-seam law** (L·S-3 = C·S-1): never the bare token, always the re-parseable literal, with the constructor-rejection reason documented. The same file contains the law and (on duration/delay/iterations) its violation — extending it is exactly KF-CO-3's cure.
- **`trySetOption` discriminates the typed error** (L·S-2 = C·S-3): re-throws what it does not own over a constructor-assigned `name` that survives minification. KF-CO-3 is the unguarded line *beside* the guard.
- **`timingFunctionState`'s type guard shields a live value.js Result-contract hole** (C·S-4): `parseTimingFunction(null|42|{})` throws a raw TypeError out of a Result-returning parser — **the banked R1 class**; every string returns `ok:false` cleanly. Evidence FOLDED by reference to the value.js parser lane (lane-library §4.6 / parser-proof R1) — one identity, no re-booking here.
- **`<Teleport … defer>` used for its actual purpose** (L·S-8): later-sibling target, `v-if` single-ribbon invariant, provide/inject following the component tree. Three subtleties, one attribute.
- **The shared ticker + the sync gate reasoned from the deadlock** (L·S-1 = C·S-2; **identity: kf-AnimationControlsGroup's L-S1/L-S4 rows** — cross-referenced, not re-banked): resumes only on inputs the loop does not own; ChannelOptions honours all three entry points.
- **The store keyed to the animation's identity at both ends** (L·S-7 = C·S-5) — what kills K-3 and makes the setup-time `getStoredAnimationOptions` safe.
- **The vendor viewport meets the token-through-bracket cap correctly** (this seat's addition while executing kill #4): `max-h-[var(--easing-dropdown-max-h)]` is the sanctioned custom-property consumption AND the vendor inherits it into the scrolling viewport — idiom and vendor correct by construction.
- **The corpus posture itself** (RR-B's, endorsed): §9.0 falsifies its own predecessor in place and files the worse truth; §9.4 self-downgrades on a failed reachability probe and prints the failed half. Two of this record's cleanest rulings were possible only because the axes stated executable falsifiers. The counter-lesson is the substrate: **five seats stated the tree and none interrogated it.**
- **EE-02 at `origin/master`** (this seat's substrate observation): the owner's upstream work independently cured L·B-2's two consequences at both write sites, with the failure mode named in the committed comment — the engine/demo co-design loop closing a corpus BLOCKER before the corpus was adjudicated.

## UNPROVEN-NEEDS-LIVE residue (SS-13 inputs, ranked; witnesses ride KF.W9)

1. **KF-CO-1** — one click on the `direction` trigger (the cheapest possible confirmation of the controlled-closed chain). Then the same on `blend`.
2. **KF-CO-8** — the `enabled` switch rendering OFF while `layerConfig.enabled` is true; a click changing nothing.
3. **KF-CO-9** — "Steps (n) — 100" beside a slider thumb at 12; the first-drag persist.
4. **KF-CO-6** — the rendered trigger string (`ease-in-outslow start & end`), and its truncation on a narrow rail.
5. **KF-CO-5 + KF-CO-10-focus** — what the user sees when focus sits inside a collapsed `opacity:0` row (does the browser scroll the invisible control into view?).
6. **KF-CO-18/19/20** — computed `color` on the shimmer label; composited contrast for pencil and chevron over the live glass plate.
7. **KF-CO-24** — rendered box sizes of the compact pencil (24×24 derived) and Back (28×28).
8. **KF-CO-16** — the per-frame cost magnitude of the dead `:progress` on a 3-channel scene.
9. **The mobile-Drawer teleport residual** (D-axis §5, correctly declined as a defect): RibbonBar unmounted while `active` — does the playback ribbon silently vanish?
10. **Taste rulings**: 10px family headings (KF-CO-25, value now proven); the label color split (KF-CO-31, statically settled, one confirming diff); WAAPI re-delegation timing after an easing write (post-EE-02).

## Routing summary (M-25 agglomeration input)

- **KF.W0 · Substrate Settle**: the L·B-3 discharge (≡ census F-1, SCH-1) + the **three-file disk↔master reconciliation** (package.json devDep · both EE-02 css-twin sites) so the landed cure cannot be regressed by a stale-WIP commit + the re-anchor law over every anchor in this record.
- **KF.W4 · K-Quartet Hygiene**: KF-CO-7 (≡ KF-APP-4/KF-CE-16, vue-tsc + CI) — the sequencing head; the OPTIONS-UNIT may not open before it.
- **KF.W6 · Glass Suffusion**: KF-CO-18/19/20/24/25/30 (token, register, tap-floor discipline).
- **KF.W9 · Safari Visual Audit**: the SS-13 list above.
- **glass-ui BH relay** (owner edict, standing formation invariant): the Boolean-cast producer defect on `LabeledSelect.open` / `LabeledSwitch.modelValue` (`default: undefined` cure) + the standing asks (label-action slot, EasingPicker initialPoints) re-cited.
- **Engine lane (keyframes.js src)**: the `applyTimingFunction` frame-propagation gap (L·B-2's preserved half) + the `parseAnimationCSS.ts` per-stop timing-function drop (R3-forwarded, lane-library §4.6 seam).
- **value.js parser lane**: the `parseTimingFunction` non-string raw-TypeError (R1 class) — evidence forwarded, identity already banked.
- **NO-WAVE-OWNER → OPTIONS-UNIT** (proposed wave-surface for SS-1/SS-2): everything else — the one-line capability restorations first (`:is-open`→`:open` ×3, switch `model-value` rewire), then the persist/representation/authority cluster (KF-CO-3/4/11/12), the steps cluster (KF-CO-9/10/13/23/36), the a11y leg (KF-CO-5/21/22/26 + focus management), and the hygiene tail. **completableAlone-shaped**: that packet alone restores three dropdowns, a working layer switch, honest field values, un-brickable persistence, and a truthful easing pipeline.

## DISSENT — preserved, not ruled away

1. **RR-A's scope declaration is honored, not penalized**: it re-derived the load-bearing rows and named the tail it did not endorse (L·N/I tail, L·B-4). Its four missed-finds (steps-domain, focus, third-alias, label-split) and its statical discharge of two UNPROVEN tags were the corpus's sharpest additions after RR-B's blocker. On the two rows where the readers split (B-4, the dead-switch mechanism), RR-B was right both times — but RR-B also inherited the tree-citation error it was uniquely positioned to catch, having run git-adjacent probes.
2. **The substrate lesson, sharpened for the fleet**: AmigaScene's rule ("name the tree") is insufficient — *both re-readers named a tree and measured a different one.* The rule this record adds: **a cited HEAD must be accompanied by `git status --porcelain` over the blast surface**; a dirty file's evidence binds to the disk, not the commit.
3. **Severity tension on D-3 (run-on trigger), preserved**: BLOCKER rests on "the resting label of the primary control is garbled" (both RRs, adopted); a defensible MAJOR reading exists (single control, correct behavior, garbled prose). If SS-13 shows the trigger truncating before the concatenation point on the default rail, the severity question reopens downward — the row itself does not.
4. **The producer/consumer split on KF-CO-1**: RR-B's "one-line fix (`:is-open`→`:open`) repairs the dropdowns" is true for THIS demo only because the mutex then actively drives the prop. The producer defect (any passive consumer is broken) is this seat's own extension and awaits the BH relay's verdict; if glass-ui rules the Boolean cast intended, every LabeledSelect consumer must be told `open` is effectively required.
5. **HEAD movement note**: this adjudication postdates the challenges by up to three days; every anchor cited above was re-verified by this seat against the disk at adjudication time, and the disk↔master byte-equality claims are per-file diff outputs, not assumptions.

## Closing verdict

**ADJUDICATED against the measured dirty worktree over stale checkout `8281638c`, re-derived at `origin/master` `81a56990`: 58 confirmed rows (6 BLOCKER · 19 MAJOR · 25 MINOR · 8 INFO) — the headline is KF-CO-1, the controlled-closed dropdown chain all three axes graded "~nil" and one re-reader alone caught, now carrying a GLASS-OWNED producer half to the BH relay; 11 claims killed with executed refutations, including the substrate falsification of the phantom-dependency BLOCKER all five prior seats confirmed and the EE-02 cure that retires L·B-2's consequences at HEAD; 9 net-new re-reader finds banked (dead switch, steps domain, focus management, the third steps alias, the label split, and four more); identity guards held (census F-1, KF-APP-4's vue-tsc, kf-ACG's M-10 hashSharing ingress and L-S1 ticker, value.js R1 — folded by reference, zero re-bookings); routings are M-25 agglomeration input — KF.W0 owns the three-file reconciliation, KF.W4 the gate that makes the repairs stick, KF.W6 the token/register packet, KF.W9 the ten SS-13 witnesses, and everything behavioral agglomerates into the proposed OPTIONS-UNIT for SS-1/SS-2 spec authoring.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.

---

## ADDENDUM R2 (E-3 — appended, never rewriting) · second re-reader pair adjudicated · standing record re-verified

**Seat**: fresh Fable adjudicator (relaunch dispatch) · model receipt: `claude-fable-5`, observed in this seat's own system prompt. **Date**: 2026-08-07.
**Occasion**: this dispatch arrived carrying a SECOND pair of hostile re-reader returns (design-and-usage · library-and-code) distinct from the pair the standing record adjudicated — different kills, different net-new finds, no substrate interrogation and no controlled-closed find. The standing record was found already landed at this path, line-1 receipt identical. Ruling on the file itself: **dated evidence is never rewritten** — the standing record stands; this addendum (a) re-verifies its decisive claims seat-independently, (b) adjudicates the second pair's residue, banking what is net-new and killing what fails. Nothing below reopens a settled row except where marked CORRECTION.

### A. Standing-record re-verification (this seat's own instruments, all read-only)

Every load-bearing ruling re-derived from scratch this session, at the subject tree `/Users/mkbabb/Programming/keyframes.js`:
- **Substrate**: checkout `8281638c` on `master`, **41 behind `origin/master` `81a56990`** (my `rev-list --count`); blast surface dirty exactly as ruled — `ChannelOptions.vue`/`LayerConfigPanel.vue`/`animationOptionsStore.ts` **0 diff lines vs `origin/master`**; `useTimingFunctionEditor.ts` diff shows precisely the **EE-02 css-twin reversion** (disk `{ fn }`, master `{ fn, css }` + `timingFunctionLiteralFor` pass-through, hunks quoted in my transcript); `origin/master:package.json` devDependencies carries `"@mkbabb/glass-ui": "7.0.0"`, disk does not. **SUBSTRATE RULING CONFIRMED WHOLE.**
- **KF-CO-1 chain, every link re-executed**: `labeled-field.js` LabeledSelect props `open: { type: Boolean }` (no default) + render `open: e.open,` unconditional (my node dump); glass Select `open:{type:Boolean}` + `useForwardPropsEmits`; reka `useForwardProps` read whole — forwards every camelized `vnode.props` key whose value ≠ undefined, so the cast `false` IS forwarded; `SelectRoot.js:79-81` `passive: props.open === void 0` → controlled-at-false; `SelectContent.js:126` `present = forceMount || open` → permanently false. The bespoke easing Select escapes because an unassigned key never enters the forward set. Call sites: `:is-open` at ChannelOptions `:96`/`:120` + LayerConfigPanel `:11` — three dropdowns. **CONFIRMED.**
- **KF-CO-8**: LabeledSwitch compiled `modelValue: { type: Boolean }` no default; `SwitchRoot.js:69-76` passive check + `toggleCheck` emit-only under controlled; call site `:checked`/`@update:checked` (`:64-65`). **CONFIRMED.**
- **Killed #1** (`normalizeIterationCount` accepts `"∞"`, options.ts:49-62 read) and **killed #2** (`main.ts:50-52` `Promise.all([warmKfEngine()…]).finally(mount)`) — **both re-verified**.
- **KF-CO-25 sealed by execution** (was "high-confidence static"): the producer's own merger run on the real strings — `t('py-1.5 pl-8 pr-2 text-dropdown-secondary font-semibold', 'text-admin-label text-muted-foreground px-2 py-1')` → `pl-8 pr-2 font-semibold text-admin-label text-muted-foreground px-2 py-1`. `text-admin-label` survives: **10px headings determinate**, and `text-dropdown-secondary` is **deleted** (see KF-CO-43).

### B. CORRECTION to the standing record (one)

**KF-CO-15's "keyed-by-id + `v-show` → no healing remount" is wrong as stated, by the record's own KF-CO-4 ruling.** `ChannelControls.vue:98` `v-if`s ChannelOptions per control-surface tab switch (the fresh-mount fact KF-CO-4 rests on), and PlaybackRibbon lives inside ChannelOptions' own `<Teleport v-if="active">` (`:377-401`, my read) — so any Controls→Keyframes→Controls round-trip recreates the ribbon and re-evaluates `effectiveDuration`. The `:max` staleness is **real but self-healing on a tab bounce**, permanent only within an uninterrupted Controls session. Severity holds (the rail is wrong exactly while the user is scrubbing against it); the cure is unchanged. The `v-show` reasoning covers channel switching only.

### C. Re-reader-vs-re-reader ruling (second pair), sealed by execution

The second pair contradicted each other on the `cn` mechanism: the design axis claimed `text-dropdown-secondary` falls to the `/^text-/` colour catch-all and is deleted; the library axis claimed "nothing is dropped and Tailwind's emission order decides." **The design axis is right; the library axis's mechanism is REFUTED by my execution above.** The conflict table (read raw) carries TWO font-size enumerations — stock `text-(xs…9xl)` and semantic `text-(micro|small|caption|body|prose|admin-label|heading|…|mono-micro)` — and **neither contains `dropdown`/`dropdown-secondary`**, so those fall through to `text-color` and die to any later colour utility. (The banked SwatchHoverMenu K-1 executed the same merger on padding groups; the two executions agree on the resolver mechanics: first-match group, last-wins.)

### D. Net-new rows banked from the second pair (each re-verified at source by this seat)

- **KF-CO-41 (MAJOR) — the card carries two row grammars, and the easing field silently breaks the file's own documented invariant.** `:24` opens `.labeled-field-grid` (five subgrid rows, one derived label column — the H.W11.I1 comment at `:17-23` states the invariant verbatim), `:138` closes it, `:147` opens the easing unit as a stacked full-width `flex flex-col gap-1` OUTSIDE the grid; LayerConfigPanel then rejoins the grid in the sub-pane. The wrapper comment (`:140-146`) justifies the wrapper (no label-action slot — still true in 7.0.0, cf. L·I-6) and never addresses the layout divergence it caused. All three axes analysed this row's typography and accname; none its geometry. **OPTIONS-UNIT** (the label row can sit in the grid's label track with the trigger spanning; or the divergence gets authored prose).
- **KF-CO-42 (MAJOR) — wrong primitive, and it is the root of three filed symptoms.** `grep -rln DockControl demo/` → exactly three files: two real docks + this Card (my run). Choosing `DockControl compact` inside a Card is what produces KF-CO-24 (compact is opted out of the dock touch floor), KF-CO-27 (`title` falls through — Button takes `aria-label`, as the sibling TimingFunctionPanel's Back button proves in the SAME card), and half of KF-CO-28. **Producer note for the BH relay**: `DockControl.vue.d.ts:14-18` advertises "the HIT CELL stays the full `--dock-control-size` (≥44px on coarse…) — A11Y-5's defect dies by construction" — a guarantee that is **dock-scoped and silently void** at non-dock call sites; the docstring should say so. **OPTIONS-UNIT** (swap both call sites to `Button emphasis="quiet" icon-only` + `aria-label`; this discharges KF-CO-27 and re-scopes KF-CO-24's cure).
- **KF-CO-43 (MINOR · GLASS-OWNED) — vendor `cn` misclassifies glass-ui's OWN font-size utilities as colours.** Execution transcript in §A: `text-dropdown-secondary` (a font-size utility: `.text-dropdown-secondary{font-size:var(--dropdown-text-secondary)}`) is deleted by a later `text-*` colour because both font-size enumerations omit `dropdown`/`dropdown-secondary` (and the `text-[length:…]` arbitrary forms). Latent here only because `text-admin-label` re-supplies a size; live for every consumer passing colour alone — silent loss of the dropdown type scale, no diagnostic. **glass-ui BH relay** (third producer item, beside the Boolean-cast pair).
- **KF-CO-44 (MINOR) — `openEditorOnChange` leaks on the two early-return paths, refuting KF-CO-23's absolute.** My read `:170-212`: `onEditIconClick` sets the flag then delegates; the `cubic-bezier` branch returns WITHOUT `updateTimingFunctionFromName`, so the stored literal never changes and the watch (`:84-92` — the flag's ONLY clearer, my read) never fires; the flag survives. Sequence: pencil on an already-bezier curve → close panel → pick `steps` from the dropdown → the editor auto-opens FROM the dropdown — precisely what the flag's docstring (`:44`) exists to prevent. The `steps` branch leaks identically when the persisted literal is already byte-identical. KF-CO-23's "opens nothing" stays true for the first selection; the one-shot contract is broken. **OPTIONS-UNIT** (clear the flag on every exit path; dies naturally if KF-CO-23's cure routes dropdown draft-kinds through the label-click path).
- **KF-CO-45 (MINOR) — the setup-time store-leaf capture detaches on whole-value replacement; cross-tab is the live path.** `getStoredAnimationOptions` returns a leaf INSIDE the `useStorage` ref's object graph (store read, `:67-95`), captured in a plain `const` at `:459` and prop-drilled. vueuse's storage-event handler does `data.value = read(event)` — whole-object replacement (my read of the installed dist, `update(event)`); `listenToStorageChanges` defaults true. Two tabs: tab A's panel keeps rendering the orphaned graph and every subsequent write lands on an object no watcher sees — silent persistence loss until reload. (The two in-repo replacers are followed by navigation/reload today; `applySharedAnimationState`'s per-scene `Object.assign` also swaps subtree identity.) **Qualifies** superlative L·S-7/C·S-5 ("keyed at both ends" covers animation identity, not store identity). **OPTIONS-UNIT** (read through the ref per access, or re-resolve on `storage`).
- **KF-CO-46 (MINOR, cure-rider on KF-CO-5) — the prescribed `inert` cure is structurally incomplete, and the sibling's full idiom cannot be transplanted.** `ChannelControls.vue` pairs `:inert` WITH `content-visibility: hidden` (+ `display:none` fallback) — the second half is what removes the subtree from find-in-page/selection, and it is **incompatible** with this component's `0fr↔1fr` height morph (skipped content can't size the `1fr` track). Honest cure: `inert` per row PLUS an accepted find-in-page residual, or a collapse redesign — a repair claiming "one attribute, as next door" would report done while the panels stay page-searchable. Spec obligation for the OPTIONS-UNIT a11y leg.
- **KF-CO-47 (MINOR, cure-rider on KF-CO-2) — `tooltip`→`description` is a rename that changes the design.** `description` renders persistent copy beneath the label, wired into `aria-describedby` (compiled `${id}-description`) — not a hover tooltip. The KF-CO-2 rewire pass must DECIDE hover-vs-persistent per field, not rename blindly; a blind rename would stack five always-visible glosses into the card.
- **KF-CO-48 (MINOR, engine-lane rider) — one defect, two product behaviours: refusal vs throw.** `backward.ts` (my read `:245-275`): the `@keyframes` block emitters run FIRST and unguarded — both call `serializeEasing`, which throws for a twinless closure — while only `animationShorthand` sits in the try/catch that records the designed `custom-renderer` refusal; `compileToCSS` (`:352-`) has no outer guard. The demo's export button catches + toasts (killed-register #7 stands), so this is the **engine lane's** asymmetry, filed beside the `applyTimingFunction` gap already forwarded. At `origin/master` this component no longer triggers it (EE-02); the asymmetry itself is unchanged.

**Folded, not re-booked**: the second pair's dead-switch find ≡ **KF-CO-8** (independent convergence, recorded); its four-gap-values census (12/6/12+12/4 px for one label↔affordance relation, plus three block spacings in one column) rides **KF-CO-28/29** as the census rider; its ~40-name `proof:*` prose-rot class ≡ the banked kf-ChannelControls **L-19 → KF.W4-PROSE (census S-2)** — this record adds only the two citations load-bearing HERE to **KF-CO-33**: `design-idioms.css:3`'s `proof:idioms` authority claim behind `.text-gold`/`icon-sm` (both consumed at `:176`/`:185`), and `DESIGN.md:236`'s `proof:style-file-ceiling` behind the R3 rule this 609L file violates.

### E. Second pair's killed-claims register, adjudicated

1. **D-3 sub-claim (b) — "the run-on is the combobox's accessible name, compounding D-7" — KILL CONFIRMED.** `aria-label="Timing function"` (`:219`) wins accname step 2C over descendant content; KF-CO-6 is visual-only, KF-CO-21 independent; the axis double-counted the harm.
2. **D-16's authority citation — KILL CONFIRMED as a mis-citation.** `DESIGN.md` §7 governs `.stage-legend` verb lines on manipulable scenes, not panel chrome symmetry; the quoted fragment was lifted from a sentence about scene legends. KF-CO-28 stands on tree evidence alone — and the SAME §7 supplies "Cursor-only or home-only hints are not sufficient," now attached to **KF-CO-22** as its in-tree (not just WCAG) authority.
3. **The library axis's "cn drops nothing; emission order arbitrates" — KILLED by execution** (§C). Would have suppressed KF-CO-43.
4. **KF-CO-15's permanence sub-claim — KILLED** (§B correction; the second pair's library axis caught it, this seat verified the ribbon's mount seam).
5. **Transcript correction, conclusion intact**: `parseCssScalar("500m")` returns `{number, 500, unit:"m"}` (not a keyword payload); it still throws — `tryParseTime` accepts only `s`/`ms`. Recorded so no repair chases a phantom parser behaviour.
6. Both second-pair readers independently re-killed D-11 (`"∞"`), D-12 (chunk-vs-microtask) and C·m-6's decline — **convergent** with standing killed #1/#2 and the C·m-6 overrule; recorded as independent confirmation, not new rulings.

### F. Superlative additions (second pair's, verified in the reads above)

- **The row-stack collapse geometry is correct in a way no axis noticed**: three sibling grid rows each animating its own `0fr↔1fr` track means card height = the SUM of tracks, so a panel swap linearly interpolates `(1−t)·A + t·B` — strictly between the endpoint heights, no overshoot, no measured JS, no absolute-position overlay. The block the focus-ring-inset superlative lives in is itself the right shape.
- **`normal-case` on the curve name is a precise counter, not decoration**: the sibling `SelectLabel` rung's `text-admin-label` carries `text-transform: uppercase`; someone read the vendor sheet and wrote the one class that stops the register bleeding onto item rows.

### G. SS-13 additions (append to the standing list)

11. **Dropdown WIDTH overflow**: the item description spans carry `whitespace-nowrap` with no `truncate`, inside a SelectContent with `min-w` and **no max-width** — height is capped (KF.CO-25's token), width is not; longest gloss "pull back & overshoot" beside a 20-char mono name. Live measure.
12. **Focus-ring 2px bleed sufficiency** across the panels' controls (EasingPicker canvas, slider thumb, switch) — the inset superlative's own caveat, promoted to a witness row.
(The second pair's "computed-style diff of the duration vs easing labels" row is SUPERSEDED — KF-CO-31 already discharged it statically.)

### H. Updated scoreboard

**66 ADJUDICATED-CONFIRMED (6 BLOCKER · 21 MAJOR · 31 MINOR · 8 INFO) · 15 KILLED · 17 net-new re-reader finds banked across both pairs · 12 UNPROVEN-NEEDS-LIVE rows to SS-13/KF.W9.** Routings unchanged plus: KF-CO-41/44/45/46/47 → OPTIONS-UNIT; KF-CO-42 → OPTIONS-UNIT + BH-relay docstring note; KF-CO-43 → glass-ui BH relay (third producer item); KF-CO-48 → engine lane. Identity guards held (KF-CO-8 convergence, KF.W4-PROSE ≡ ChannelControls L-19, SwatchHoverMenu K-1 merger-mechanics corroboration — zero re-bookings).

— end of addendum R2. The standing record above, the three challenge files, and both pairs of re-reader returns remain immutable per E-1/E-3.
