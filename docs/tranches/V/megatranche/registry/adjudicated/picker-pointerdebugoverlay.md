`claude-fable-5`

# picker-pointerdebugoverlay — adjudicated apotheosis (M-23/L-18, fresh Fable adjudicator)

**Seat**: apotheosis arbiter · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **HEAD**: `ee9474e4` (branch `tranche-u`) · **Component receipt**: `demo/picker/visual/PointerDebugOverlay.vue` 286 lines, SHA-256 `5529d038…c56a27` — byte-identical to the hash in every challenge file, so all line anchors in this record are current. Composed seat: `DebugEventLog.vue` (SHA `61b8aaee…fb6b3f`) + `usePointerDebug.ts` (SHA `a1ad5b7c…db23aa`), 703 lines total.
**Corpus**: `challenge-C-implementation.md` (8 findings) · `challenge-D-design.md` (r1, 7 findings — Codex-provenance, quarantine-governed) · `challenge-D-design-r2.md` (GOVERNS: 7 rulings on r1 + D-N1…D-N10 + 5 superlatives, live-probed) · `challenge-L-library.md` (5 findings). **Readers adjudicated**: reader-DU (design-and-usage) and reader-LC (library-and-code), both hostile re-reads of the full corpus.
**Law compliance**: this file is this seat's ONLY write; product source read-only throughout; zero browser tooling (every live number in this record is r2's cell, labeled so); motion-quarantine FALSE rows NOT carried — D#4 is barred by **M-28 FALSE / M-29 FALSE / M-31 FALSE** (M-30 UNPROVEN, a copy judgment), disposition table `motion-quarantine.md:265` records the withdrawal. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.
**Method (the incredulity seat's own instruments, all static)**: whole-file reads of PointerDebugOverlay.vue (286), usePointerDebug.ts (281), DebugEventLog.vue (136); ColorPicker.vue at :100-106/:138-144/:172-178; `--z-debug` grep (1 reach `:136`, 0 definitions in demo/src); glass-ui `dist/styles/tokens/scheme-motion.css` z-ladder census (**17 rungs**, `--z-max: 9999` present, no `--z-debug`); `foundation.css` `--z-*` census (`--z-ornament: 20` at `:393`, sole local rung) + `:695-790` read (WHCM `:where(button…)` focus roster; BR-6 prefers-contrast token/roster; BR-7 reduced-transparency roster); `animations.css:184-193` PRM guard read; token history `git log -S'--z-debug'` → `40fa935d` (add, 03-16) / `2457e922` (consumer, 03-16) / `c84504d3` (delete, 03-25); **offline production proof**: `origin/gh-pages` HEAD `17c10267` (2026-03-24) — `assets/index-DS-0HlXP.css` contains `--z-debug:99999` + `.debug-overlay`, `assets/index-Dq7-wwjC.js` contains the byte-identical minified substring gate; glass-ui `useClipboard.d.ts` read whole (status/CopyResult/resetMs/timeoutMs/onCopyError) + demo consumer census (3 sites) + `execCommand` census (1 hit, `:123`) + **`writeClipboard` imported and called by the mounting parent itself** (ColorPicker.vue:138/:325 — this seat's own find); colour-literal count over the pair (**37 occurrences / 34 distinct**); `var(--…)` census (5 distinct in parent, one dead; **0 in child**); aria-live/status/alert/log/tabindex grep (0 hits both files); `safe-area-inset` grep (0 in demo) + `position: fixed` grep (1 in demo = this overlay); test-coverage grep over test/ + e2e/ (`PointerDebug|debug-overlay|debug=1` → 0 hits, both suites populated); `forceReleaseAll\b` census (3 hits, all in-module); `useSliderTouchGates.ts:12-20` type-duplication read; DESIGN.md §Z-tier + §Bespoke-literals reads; `seat.css:55` `--z-ornament` reach; sibling record `picker-debugeventlog.md` read (ADJ-6, ADJ-9 — identity guards + one cross-record correction below); CARRY-CUT-LEDGER routing read (**CC-081 names the PointerDebugOverlay a11y+clipboard limb → X-W8**; CC-027 → X-W0; CC-030/031 → X-W1).
**WCAG arithmetic spot-check (this seat's own)**: `.debug-btn` border `rgba(255,255,255,0.2)` composited over the 95%-opaque action bar on a white page → 1.80:1 (reader-DU's exact figure reproduced independently); the 3:1 SC 1.4.11 floor is missed in the best case.

## Scoreboard

**34 ADJUDICATED-CONFIRMED — 2 BLOCKER · 9 MAJOR · 14 MINOR · 9 INFO. 13 KILLED with written refutations. 19 reader-recovered misses adjudicated into the 34 (every load-bearing one re-derived by this seat). 10 superlatives (merged identities). 8 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

The seat's shape, adjudicated: a well-architected instrument (two-layer zero-cost gating, two-layer non-perturbation, bounded buffer, correct teardown) whose **one dead token has blinded it for four months** — the working tree paints the picker card over the instrument's answers, phone expansion is a one-way trap — while a **substring gate ships the whole 703-line subsystem to the public product**, where (on the still-deployed March build, whose CSS still resolves `--z-debug:99999`) it is fully functional for any stranger whose URL contains `debug=1` as a substring, `?nodebug=1` included. The cures are small; the corpus's own prescribed cures are, in three places, provably wrong (K-11, K-12, and the SEQ-1 ordering trap), which is why this record adjudicates cures separately from defects (the ColorSpaceSelector MT-CSS-A §2 law, applied).

**Route of record: X-W8 (CC-081, "PointerDebugOverlay a11y+clipboard" verbatim)** for every component cure · **X-W1** for the test/gate row (CC-030/031) · **X-W10** for the design-canon rows · **X-W0** notes: the CC-027-mandated D-axis re-run was satisfied by r2 and is adjudicated here.

## Ruled disagreements (the adjudication's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **C#6** (unguarded browser globals) | KILLED (unreachable) | CONFIRMED at INFO | **KILLED** | Same facts both sides; the booking follows reachability. The only invocation path is `@click` on a rendered button inside `Teleport to="body"` (:2, :55) — a live DOM is a precondition of the call. No SSR path exists (no `@vue/server-renderer`/nuxt in package.json). A row with no reachable failure is not a roster row; the one live limb (clipboard-API absence) is CLIP-1's `no-api` branch and rides there. |
| **D#7 severity** (parallel visual system) | MAJOR | MINOR | **MAJOR, merged with BR-BYPASS** | reader-LC graded without reader-DU's bypass find. This seat re-read `foundation.css:728-748` and `:764-790`: `prefers-contrast: more` escalates via tokens the seat never reads (`--muted-foreground`, `--border`, `--card-edge`, `--glass-tint-strength`, `--glass-level`) + a selector roster it never matches; `prefers-reduced-transparency: reduce` kills `backdrop-filter` on a roster excluding `.debug-overlay`, whose literal `blur(8px)` (:149) survives the user's explicit request. That is a functional bypass of two ratified contracts (U-F57 BR-6/BR-7), not cosmetics. One identity, MAJOR (row 10). |
| **L#1/L#3 vs L-SPLIT** (view owns five concerns) | CONFIRMED MINOR | KILLED as contrived | **KILLED as prescribed; survives inverted as CLIP-1** | The decomposition L prescribes is contrived (K-9: the DOM-walking boundary was ALREADY drawn at H.W3 Lane B — docblock at `usePointerDebug.ts:104-111` matches the code; a "producer-owned diagnostic sheet" is KISS-banned). But the clipboard limb inverts rather than dies: the service exists, shipped, adopted 3× in demo — and, this seat's sharpening, **imported by the very component that mounts the overlay** (`import { writeClipboard }` ColorPicker.vue:138, called `:325`). The correct row is a one-edit adoption (row 8), not a build. |
| **D#7 forced-colors rider** ("zero coverage in the app's 8 blocks") | KILLED with count correction "7 not 8" | CONFIRMED via grep | **Kill limb stands; BOTH counts corrected** | The substantive kill is right: `foundation.css:702-720` binds a WHCM focus outline via `:where(a[href], button, …):focus-visible` and all four debug controls ARE `<button>`s — focus affordance IS covered class-agnostically. But reader-DU's correction is itself miscounted twice: foundation.css has **one** `forced-colors: active` block (`:678`), not two (demo-owned total = 2, with GradientStopEditor.vue:353), and glass-ui dist carries such blocks in **≥10 files**, not 4. Neither count matters to the verdict; both are recorded so neither propagates. The narrow truth survives inside row 10: surface policy and the Reset-vs-Copy distinction are genuinely uncovered. |
| **Colour-literal count** (r2: "31") | 37 occurrences | not re-derived | **37 occurrences / 34 distinct** (this seat's grep) | r2's 31 matches neither reading. The substantive claim is understated either way: `DebugEventLog.vue` reaches ZERO tokens, and of the parent's 5 distinct `var()` reaches one is the dead `--z-debug`. |
| **C#8** (missing `type="button"`) | confirmed inside D-N5 | INFO, consequence killed | **Consequence KILLED (K-6); attribute inconsistency folds into D-N5** | `Teleport to="body"` makes the rendered subtree a direct child of `<body>`; a form owner is impossible by ancestry and no `form=` attribute exists. The inconsistency (`:10` has it, `:49/:55/:58` do not) is real and dies inside row 5's cure. |
| **SEQ-1 booking** (r2's step-1 "ship first and independently") | MAJOR missed finding | not booked | **ADOPTED as a binding X-W8 sequencing law** | Geometry in-source (`:133-141`: fixed, bottom-left, 280px × up-to-35dvh, `rgba(0,0,0,0.92)`): restoring the rung alone resurrects the killed D#2c at 390×844 in the expanded state — a near-opaque slab over the `.channel-rows` r2 itself measured (`r2:82`). Mitigations bound it (collapsed default S-1; `pointer-events: none` body ⇒ "drag a control you cannot see", not "cannot drag") but do not clear it. Ruling: the z-restore may NOT land alone; it lands coupled with a small-viewport expanded-state re-form or guard (row 11). |
| **Cross-record: sibling ADJ-9(d)** (picker-debugeventlog KEEP ruling) | — | — | **KEEP ruling VINDICATED, mechanism CORRECTED** | The sibling record adopted r2:207's cure text ("move `pointer-events: none` off `.debug-scroll` onto the rows") as the designated landing mechanism. Both readers independently proved that text a double no-op (K-11: the property inherits; the rows already carry it). The corrected cure — **ADD `pointer-events: auto` to `.debug-scroll`, KEEP the row-level `none`** — makes the child's `:61-67` declarations genuinely load-bearing for the first time, which is a *stronger* ground for the sibling's KEEP than the one it cited. X-W8 sequences the two records together; the dated sibling file is not rewritten (E-3), this note travels with the wave. |

## Adjudicated defect roster

Format: **id · severity · claim** — proof (compressed; `✓A` = re-derived by this seat at the bytes) — **ROUTE**.

### BLOCKER (instrument-scoped: the surface is flag-gated, so no product journey breaks; D-N4 is what makes it product-facing at all)

1. **D-N1 · BLOCKER · `--z-debug` is orphaned; `.debug-overlay` computes `z-index: auto` and the picker card paints over the entire status column — a regression vs the shipped build.** ✓A sole reach `:136`, zero definitions (demo/src grep); foundation.css defines only `--z-ornament: 20` (`:393`); glass-ui's served ladder = 17 rungs, none named `--z-debug`. Invalid-at-computed-value-time ⇒ `auto`; `.app-layout` is not a stacking context, `.pane-wrapper--left { z-index: 1 }` wins (r2's static chain, reader-DU re-verified). ✓A regression corroborated fully offline: token added `40fa935d`, deleted `c84504d3` (2026-03-25); `origin/gh-pages` HEAD `17c10267` (deployed 2026-03-24, one day earlier) still ships `--z-debug:99999` in `assets/index-DS-0HlXP.css`. Dark ~4.5 months. **CURE CORRECTION (adjudicated, folding reader-DU's ladder find)**: the one-line KISS cure is `z-index: var(--z-max)` — the 9999 ceiling already served. r2's own prescription ("re-declare at the top of the ladder above `--z-modal: 140`") is WRONG twice: its ladder block omits 7 of the 17 rungs (`--z-panel/hovercard/tooltip/fullscreen/toast/toggle/max`), so "above modal" would sit BELOW toast (160), toggle (999) and max (9999); and `--z-modal` is glass-ui's, not foundation.css's. Occlusion percentages are r2's cell (residue U-2). **ROUTE: X-W8 (CC-081), bound by row 11's sequencing law.**
2. **D-N2 · BLOCKER · at 390×844 the expanded header is occluded and not hit-testable; the disclosure is a one-way trap.** Mechanism ✓A as a strict consequence of D-N1: the header is the SOLE collapse affordance (`pointer-events: auto` only at `:167`/`:244`; no Escape handler, no close button, no outside-dismiss anywhere in `:1-129`; `collapsed` `:73` component-local) — painted under `.pane-wrapper--left`, it cannot receive a trusted tap; the only exit is reload. Compounding: `collapsed = ref(true)` means the first tap springs the trap. The 97.5% figure, `elementsFromPoint` stack and Playwright actionability timeout are r2's cell (U-2). **ROUTE: X-W8, same commit as row 1 + row 11.**

### MAJOR

3. **D-N4 · MAJOR · the activation gate is an unparsed substring match with no build condition, and it ships to production.** ✓A `usePointerDebug.ts:34-39` — `hash.includes("debug=1") || search.includes("debug=1")`; `?nodebug=1` satisfies it by construction; no `URLSearchParams`, no `import.meta.env.DEV` anywhere in the subsystem; static import ColorPicker.vue:141 + unconditional render `:103` (the `v-if` is inside, `:4`) ⇒ untree-shakeable. ✓A **production reachability proved offline by this seat**: the deployed `assets/index-Dq7-wwjC.js` carries the byte-identical minified gate; the deployed CSS carries `.debug-overlay`. Corollary that sharpens both readers: on the deployed March build the token still resolves, so the stranger who reaches it gets a FULLY FUNCTIONAL console at z 99999 — six document-level capture listeners, a 500 ms interval, a one-tap UA/screen/DPR clipboard export, and a DOM-walking capture-release Reset (`:227-254`, `:112-145`). The live `color.babb.dev` render is r2's cell (U-4). The should-this-exist framing is an owner ruling (residue U-8). **ROUTE: X-W8 (parse the flag + build-gate the subsystem).**
4. **D-N3 · MAJOR · the event-log scrollport advertises `overflow-y: auto` while removed from hit-testing — the flight recorder's history is pointer-unreachable.** ✓A `:232-237` + no `tabindex` in the file; `MAX_EVENTS = 80` vs `max-height: 35dvh` makes overflow the steady state. Two reader corrections adopted, both ✓A: severity — `DebugEventLog.vue:33` reverses, so the visible rows are the NEWEST ("history truncated", not "entire payload lost"); causation — the `pointer-events: none` at `:236` is INERT (inherited from `:151`), so r2 cited a non-causal line. **ADJUDICATED CURE** (replacing K-11's no-op): `.debug-scroll { pointer-events: auto }`, row-level `none` KEPT — hit-testing falls through inert rows to the auto scrollport. Keyboard limb UNPROVEN (U-1: Chromium 127+ keyboard-focusable scrollers may already reach it — "unreachable by every modality" is one modality too strong). **ROUTE: X-W8, sequenced with sibling ADJ-9(d) per the cross-record ruling.**
5. **D-N5 · MAJOR · no accessible structure: null role, zero live regions, state text inside the control's accessible name, plain-div headings, unassociated key/value pairs, inconsistent button typing.** ✓A every static limb: root `:5` carries only `class`; grep 0 for `aria-live|role="status"|alert|log|tabindex` across both files; `FROZEN?`/`copied!`/glyph are children of the `:9` button so its name mutates (`Debug −` → `Debug FROZEN? −`); `State` (`:27`) and `Events (n)` (DEL `:4-6`) are divs; `:49/:55/:58` lack `type="button"` while `:10` has it (C#8's consequence killed, K-6). AX dump and `liveRegions: 0` are r2's cell. **ROUTE: X-W8** (role="region"+label on the panel, `role="status"` for copy outcome via CLIP-1's `status`, `role="alert"` for FROZEN, real headings, `type="button"` ×3). The sibling's ADJ-8 DECLINE of live-region announcements for the event FEED stands — the live regions here are for the two discrete state chips only, not the stream.
6. **D#1/C#2 · MAJOR · `aria-controls="debug-body"` references an ID that exists nowhere; the cure is structural, not a one-attribute add.** ✓A grep: exactly one hit in the tree — the attribute itself (`:13`). ✓A the intended target `<template v-if="!collapsed">` (`:22`) has TWO sibling roots (`.debug-scroll` `:24`, `.debug-actions` `:48`) and in the DEFAULT collapsed state neither is mounted — so r2's step-4 cure ("`id="debug-body"` on the disclosure target") is killed (K-12). KISS cure: keep the body mounted under `hidden`, one wrapper carries the id; `aria-expanded` (`:12`, already correct — S-5) and `aria-controls` finally agree. **ROUTE: X-W8.**
7. **D#5/C#3 · MAJOR · copy reports success unconditionally; failure has no surface.** ✓A `:123` drops the `execCommand` boolean; both branches set `copied.value = true` (`:113`, `:125`); bare `catch {}` at `:115` swallows the clipboard rejection; one outcome span (`:18`), no error state in the file. Compounded by D-N1: the sole confirmation chip lives in the occluded column. Dies with row 8. **ROUTE: X-W8.**
8. **CLIP-1 · MAJOR (reader-LC recovered; adopted + sharpened) · `copyJSON` is a divergent hand-rolled re-implementation of the producer clipboard the app has already adopted — every clipboard defect in this corpus is a symptom of the non-adoption.** ✓A glass-ui 7.0.0 `useClipboard.d.ts` read whole by this seat: `status: "idle"|"pending"|"success"|"failure"`, discriminated `CopyResult` with `no-api` branch, `resetMs`, `timeoutMs` watchdog ("some platforms leave writeText pending forever" — the exact iOS pathology class this instrument diagnoses), `onCopyError`; reader-LC's read of the shipped impl (generation counter + `clearTimeout` + `onScopeDispose`) kills C#4/C#5 by construction. ✓A three demo adopters (App.vue:362, MixResultDisplay.vue:31, GradientEasingEditor.vue:94); ✓A the overlay holds the repo's SOLE `execCommand` (`:123`). **This seat's sharpening, missed by both readers and all four axes: the mounting parent itself already imports and calls the primitive — `import { writeClipboard } from "@mkbabb/glass-ui"` at ColorPicker.vue:138, invoked `:325` — the producer path is live in the very component that mounts the overlay.** Cure: delete `:74`/`:109-128`, adopt `useClipboard`, bind `status` to the row-5 status region. Closes rows 7, 17, 18, the clipboard limb of K-5, and half of row 5 in one edit. **ROUTE: X-W8 (CC-081 names this limb verbatim).**
9. **FRZ-1 · MAJOR (reader-LC recovered; ✓A) · `onGlobalPointerCancel` omits the frozen-reset its `pointerup` twin performs — a `pointercancel` strands the FROZEN alarm permanently.** ✓A compare `:180-192` (up: refresh activity, delete, recompute, `if (!hasActivePointer) { frozen=false; frozenSince=0 }`) against `:204-210` (cancel: delete, recompute, gauge, log — no frozen clear, and no `lastPointerActivity` refresh either). After a latched freeze terminated by `pointercancel` — the single most likely terminator of the iOS capture-loss pathology — the `:242` guard (`hasActivePointer && …`) can never re-evaluate the flag; on touch, `pointermove` requires contact, so the stale red alarm survives arbitrary idle. That the other three transitions (down `:164`, up `:186`, forceReleaseAll `:98`) all clear it makes this an omission, not a choice. One line. **ROUTE: X-W8, same commit as rows 1–2 (see row 20's compounding note).**
10. **D#7+BR-BYPASS · MAJOR (merged identity; severity ruled above) · the seat is a separate visual system that functionally bypasses two ratified app-level a11y contracts.** ✓A `prefers-contrast: more` (foundation.css:728-748) and `prefers-reduced-transparency: reduce` (`:764-790`) both operate via tokens/rosters the seat neither reads nor matches — a user's explicit escalation or reduction request produces byte-identical debug output, including row 15's failing `#666` and the surviving `blur(8px)` (`:149`). ✓A 37 literal occurrences / 34 distinct vs 5 `var()` (one dead); **0 tokens in the child**; no `.glass-*` rung; font `"SF Mono", "Fira Code", monospace` diverges from the served `--font-stack-mono` in BOTH files (`:143`, DEL `:75` — FONT-DUP, row 29, folded here for cure but booked separately so the child copy is not lost). Forced-colors: the narrow truth after K-13 — surface tiering and the Reset-vs-Copy distinction are uncovered; focus outlines are not. **ROUTE: component cure X-W8; the canon/roster law X-W10.**
11. **SEQ-1 · MAJOR (reader-DU recovered; adopted as law) · r2's remediation ordering is self-defeating: step 1 shipped alone converts BLOCKER D-N2's fix into the very occlusion r2 refuted as D#2c.** Geometry ✓A in-source (`:133-141`); the phone re-form is r2's step 5, "cosmetics last". Post-restore, expanded-on-phone hides the sliders the instrument exists to diagnose (bounded: input passes through — `:151`; collapsed default — S-1). **BINDING on X-W8: the z-restore lands coupled with a small-viewport expanded-state re-form (bottom sheet / height-position guard), never alone.** No axis modeled the post-fix state; that is itself the finding. **ROUTE: X-W8 sequencing law.**

### MINOR

12. **D#6 · MINOR · Reset, Copy JSON and Clear share identical geometry although Reset is destructive and mutates the fault under inspection.** ✓A `.debug-btn { flex: 1 }` (`:250`), colour-only differentiation (`:275-285`), zero local forced-colors handling; `forceReleaseAllPointers` (`:112-145`) walks live selectors (reader-DU verified `.spectrum-picker`/`.touch-gate-target` in-tree) releasing capture for logged pids + 0…10, no confirmation. **ROUTE: X-W8.**
13. **D#2a · MINOR · bottom-anchored fixed overlay ignores safe-area insets.** ✓A `:134` `bottom: 8px`; `safe-area-inset` = 0 hits in ALL of demo, and this is demo's ONLY `position: fixed` element (both greps this seat's) — the single site where the omission can bite, on the collapsed pill S-1 otherwise gets right. **ROUTE: X-W8 (rides row 11's re-form).**
14. **D#2b · MINOR · physical `left` inset ignores RTL; gauge rows flip under an RTL root.** ✓A `:135` physical inset; `justify-content: space-between` (`:209-213`) inherits direction. r2's computed values are its cell. **ROUTE: X-W8.**
15. **D-N6 · MINOR · `.debug-val-false` `#666` at 10 px fails WCAG 1.4.3 — the false reading of every boolean gauge, in the lowest-contrast ink while `true` gets green.** ✓A `:228-230`/`:144`; reader-LC's static bound (≤3.66:1 on pure black) means the failure survives any reachable backdrop; the live 3.47 is r2's composite. `formatGauge`'s words-not-colours (S-2) keeps this a contrast defect, not a comprehension defect. **ROUTE: X-W8.**
16. **D-N7 · MINOR · the disclosure has no authored focus treatment while its sibling buttons get a 2 px ring.** ✓A `.debug-btn:focus-visible` exists (`:270-273`); `.debug-header:focus-visible` does not; neither `focus-ring.css` nor foundation ring tokens are reached (grep). Post-K-13 nuance: under WHCM the `:where(button)` outline covers it; in normal mode the UA hairline on near-black is the defect. **ROUTE: X-W8.**
17. **C#4 · MINOR · unretained feedback timers — a second copy inside 1.5 s is truncated by the first timer.** ✓A `:114`/`:126`, no handle, no `clearTimeout`, no unmount cancel. Ruling on the unmount limb: trivial (a late `copied.value = false` on an unmounted component is harmless), the truncation race is the substance. Killed by construction under row 8. **ROUTE: X-W8 (dies with CLIP-1).**
18. **C#5 · MINOR · textarea fallback has no `finally`; a throw between append and remove leaves DOM residue holding the full payload AND escapes as an unhandled rejection.** ✓A `:117-124` straight-line inside a catch; reader-LC's second consequence verified — `copyJSON` is async, `@click` (`:55`) discards the promise. Dies with row 8. **ROUTE: X-W8.**
19. **PE-DEAD · MINOR (both readers recovered — ONE identity) · three of the four `pointer-events` declarations in the composed surface are inert restatements of one inherited value, and the mis-belief they are load-bearing produced r2's no-op cure.** ✓A `pointer-events` inherits; `:151` supplies `none` to everything; only `:167`/`:244` opt back; therefore `:236`, DEL `:62`, DEL `:66` change no computed value; DEL `:2`/`:60` comments attribute the behaviour to the wrong declarations. Consequence already ruled at K-11 + the cross-record note (sibling ADJ-9(d) mechanism corrected; row-level `none` becomes load-bearing under row 4's cure). **ROUTE: X-W8, same edit as row 4.**
20. **LEAK-1 · MINOR (reader-LC recovered; ✓A) · `onGlobalPointerUp`'s overlay early-return skips the bookkeeping as well as the logging — a release over the header/actions leaks the pid and manufactures a false FROZEN alarm.** ✓A `:181` return precedes `:184` delete (and the `:183` activity refresh); the `pointerdown` filter (`:160`) is correct because skipping an INSERT is harmless — skipping a DELETE is not (`Map.delete` on absent keys is free). Correct shape: unconditional bookkeeping, gated logging. **Compounding note (this seat's):** today the trigger is desktop-only (the phone header is not hit-testable, D-N2) — the moment row 1 lands, it becomes phone-reachable, so rows 9/20 belong in the SAME commit as the z-restore. **ROUTE: X-W8.**
21. **CLR-1 · MINOR (reader-DU recovered; ✓A) · `Clear` clears only half the panel — gauges survive and read as current.** ✓A `clearEvents` (`:91-93`) splices events, never touches `state.gauges`; post-Clear, `lastDown.*`/`touches`/`lastForceRelease` persist under the heading `State` with no staleness marker — a correctness hazard in a flight recorder whose purpose is "what was true at the freeze". **ROUTE: X-W8.**
22. **EGR-1 · MINOR composite (reader-DU recovered) · the instrument has no reliable data egress at all: no text selection (inherited `none` body-wide), no scroll (row 4), and an export that lies (row 7).** Three separately-booked limbs compose into one property; any partial landing leaves zero egress. Booked as a **delta obligation on X-W8**: the wave's close must witness all three limbs together (select-or-scroll + truthful export status). **ROUTE: X-W8.**
23. **BTN-CONTRAST · MINOR (reader-DU recovered; arithmetic reproduced by this seat) · `.debug-btn` boundary contrast is bounded at 1.66–1.80:1 vs SC 1.4.11's 3:1 — the plain `Clear` button, the one control with no colour cue, is the worst case.** ✓A this seat's independent composite (white-page extreme → border 1.80:1) matches reader-DU exactly; the 95%-opaque bar bounds the ratio across every backdrop. **ROUTE: X-W8.**
24. **TEST-0 · MINOR (reader-LC recovered; ✓A) · the entire 703-line subsystem has zero coverage across every suite — the mechanism of the 4.5-month dark period.** ✓A grep `PointerDebug|debug-overlay|debug=1` over populated test/ + e2e/ → 0 hits. One Playwright spec loading `?debug=1`, expanding, asserting a non-`auto` computed z-index + a visible gauge value would have caught rows 1, 2 and 4 at the causing commit. **ROUTE: X-W1 (CC-030/031 own gate authoring; the spec itself may ride X-W8's cure commit as its witness).**
25. **DOC-1 · MINOR (both readers recovered; merged) · the governing design register is stale and silently violated by this seat — the doc that would have caught D-N1 never enumerated it.** ✓A three falsifications, all this seat's own reads: DESIGN.md §Z-tier asserts "All z-index reaches route through glass-ui's `--z-*` tokens" — falsified by `--z-debug` (undefined anywhere) and `--z-ornament` (demo-local, reached at `seat.css:55`); §Z-tier documents `--z-popover` as **70** vs the served **130**; §Bespoke-literals cites `PointerDebugOverlay.vue:266` for the blink that lives at `:179` (87 lines off — `:265-266` is `.debug-btn:hover`). **ROUTE: X-W10 (canon re-author), X-W8 rider corrects the rows it touches.**

### INFO

26. **D-N8/C#7 · INFO · the export embeds a UA/screen/DPR fingerprint and carries no schema version or snapshot identity.** ✓A `:85-91`, no `version`/`schema`/id. Tear risk dead (K-10). One producer, one consumer — versioning is INFO, the fingerprint matters only via row 3 and is defused by its gate cure. **ROUTE: X-W8 (rides rows 3/8).**
27. **D-N9 · INFO · body-teleport puts the four controls last in the tab order; expanding moves focus nowhere.** Mechanism ✓A from `:2`/`:14`; ordinals are r2's cell (U-2). **ROUTE: X-W8.**
28. **GATE-ONCE · INFO (reader-DU recovered; ✓A) · the flag is captured once at setup (`:42`, plain const) — no in-session toggle, no reactive re-read, no in-product re-entry in a hash-routed app.** The statically-provable half of unscored D-N10 (the navigation trigger stays unproven, U-3). **ROUTE: X-W8 (rides row 3's parse cure).**
29. **FONT-DUP · INFO (reader-DU recovered; ✓A) · the mono stack is authored twice and diverges from the served token in both copies** (`:143`, DEL `:75`) — a visible two-typeface split on macOS vs every other mono surface. Cure inside row 10; booked so the child copy is not lost. **ROUTE: X-W8.**
30. **DEAD-BRANCH · INFO (reader-LC; ✓A) · `describeElement`'s SVG guard is unreachable** — post-`instanceof HTMLElement` narrowing (`:24`), `el.className` is `string`; the `:27-29` ternary's else branch and one truthiness test are dead. Coexists with S-F: the defensiveness is praiseworthy, one layer of it is dead. **ROUTE: X-W8 (rides).**
31. **DEAD-EXPORT · INFO (reader-LC; ✓A) · `forceReleaseAll` sits on the public surface with zero external consumers** (grep: 3 hits, all in-module; the overlay wires Reset to `forceReleaseAllPointers`) — two reset verbs where only one is reachable, and the weaker one silently does not walk the DOM. **ROUTE: X-W8.**
32. **GAUGE-GUARD · INFO (reader-LC; ✓A) · `:97` writes a gauge directly, bypassing the enabled-guard `setGauge` exists to enforce** — adjacent to the guarded spelling (`:142`); the only path that can mutate state while the instrument is nominally off. **ROUTE: X-W8.**
33. **TYPE-DUP · INFO (reader-LC; ✓A) · `useSliderTouchGates.ts:16-18` imports the function as a type to re-derive, locally, the `UsePointerDebugReturn` alias exported at `:278`.** One import line replaces three. **ROUTE: X-W8.**
34. **CSS-DUP · INFO (reader-LC; ✓A) · `.debug-section`/`.debug-section-title` byte-duplicated across the pair (`:196-207` / DEL `:47-58`); DEL `:75-77` re-declares three inherited properties.** IDENTITY GUARD: the sibling record's ADJ-9(a) holds the sharper scoped-CSS analysis of WHICH copy is genuinely needed — one identity, cross-booked, cure lands once at X-W8. **ROUTE: X-W8.**

**Discharged-by-sibling (not a roster row)**: the index-keyed reversed list (`DebugEventLog.vue:9-11`/`:33`) both readers relayed is ALREADY BOOKED as picker-debugeventlog **ADJ-6** — anti-rename identity guard; not re-booked here.

## Killed-claims register

- **K-1 · D#4 ("FROZEN? blinks forever; no reduced-motion rule") — QUARANTINE-BARRED and false.** `motion-quarantine.md:119-121` rules **M-28 FALSE / M-29 FALSE / M-31 FALSE** (M-30 UNPROVEN — a copy judgment on the `?`, not motion); `:265` records the withdrawal. ✓A both `!important` guards re-read: `animations.css:184-193` + glass a11y-overrides; a scoped (0,2,0) rule cannot outrank them. The ABSENCE of a local rule is the correct architecture (S-4). May not be carried.
- **K-2 · D#2c ("280 px overlay covers essential phone UI") — polarity inverted in the live bytes**, two independent grounds: `z-index: auto` (row 1) means the product covers the overlay; `:151` `pointer-events: none` means the body cannot intercept input even when painting. Dead as written — and CONDITIONALLY resurrected by the corpus's own step-1 cure, which is exactly row 11's law.
- **K-3 · D#3 (touch-target limb) — refuted by measurement against the only binding standard**: header 278×30.2, buttons 83.3×27 clear WCAG 2.2 SC 2.5.8 (24×24); the claim names no standard; `touch-action: manipulation` on both (`:168`, `:259`) evidences deliberate touch tuning. The 10 px type-size limb is an owner-taste residue (U-7), not a probe; the real type defect is row 15.
- **K-4 · C#1 (`inject(POINTER_DEBUG_KEY)!` can crash) — unreachable by construction.** ✓A sole mount site ColorPicker.vue:103; provider installed in the same setup scope `:175-176` before any child renders; no dynamic import, no export from any entry, no test harness (row 24). The idiom is accurate documentation of an invariant (same pattern at SpectrumCanvas.vue:52, ComponentSliders.vue:117, equally safe).
- **K-5 · C#6 (unguarded `navigator`/`screen`/`document`) — unreachable** (ruled disagreement 1): rendering requires `document.body`; invocation requires a click on a rendered button; no SSR path in the project. The clipboard limb survives only as row 8's `no-api` branch.
- **K-6 · C#8's failure scenario (form-submit) — structurally impossible.** `Teleport to="body"` (`:2`) makes the subtree a direct child of `<body>`; form ownership is by ancestry or `form=`, neither acquirable. The attribute inconsistency survives at INFO inside row 5.
- **K-7 · L#2 ("no disabled/unavailable branch") — half false.** ✓A the disabled branch is the template's first line (`:4` `v-if="debug.state.enabled"`) backed by the provider gate (`:154`): disabled ⇒ zero DOM, zero listeners, zero interval (S-A). Only the provider-UNAVAILABLE branch is missing, and K-4 proves it unreachable.
- **K-8 · L#4/L-PORTAL ("Teleport target unconfigurable; host applications cannot choose") — category error.** `demo/` is excluded from the published package (`files` allowlist; no `exports` subpath reaches it); there are no host applications. A configurable portal is a parameter with exactly one possible argument — KISS-banned by name.
- **K-9 · L-SPLIT (four-service decomposition) — contrived AND ignorant of the tree.** ✓A the DOM-walking boundary was already drawn at H.W3 Lane B (`usePointerDebug.ts:104-111` docblock matches `:112-145`; the view calls one method); a producer-owned diagnostic sheet asks glass-ui to absorb a picker-specific dev instrument. The clipboard limb survives INVERTED as row 8 — adopt, don't build.
- **K-10 · L-TEAR (mutable-state snapshot tear) — impossible.** `buildExportJSON` (`:83-106`) is one synchronous `JSON.stringify`, no await, no yield point; C#7 itself said so. Only the versioning residue survives (row 26).
- **K-11 · r2's D-N3 cure (step 2: "move `pointer-events: none` off `.debug-scroll` onto the rows") — a no-op in BOTH halves.** ✓A the property inherits; `:151` already supplies `none`; nothing between root and scrollport re-opts; deleting `:236` changes nothing — and the rows already carry it (DEL `:62`/`:66`). The correct cure is the OPPOSITE declaration (row 4). The `tabindex`/name half of step 2 is sound and survives in row 4's cure.
- **K-12 · r2's D#1 cure (step 4: "id on the disclosure target") — insufficient as stated.** ✓A two sibling roots under the `v-if`; in the default collapsed state neither is in the DOM, so `aria-controls` still dangles — the exact failure being cured. Structural cure in row 6.
- **K-13 · D#7's forced-colors rider as stated ("zero `.debug-*` selector in any forced-colors block" ⇒ zero WHCM coverage) — over-broad.** ✓A `foundation.css:702-720` covers all four controls' focus affordance via class-agnostic `:where(button…)`. The narrow truth (surface tiering + Reset/Copy distinction uncovered) survives inside row 10. Both readers' block-counts corrected (ruled disagreement 4).

## Superlatives (provenance preserved; merged identities)

- **S-1 (r2, re-verified by both readers) · the default is right.** `collapsed = ref(true)` (`:73`, "start collapsed so it doesn't interfere"); the collapsed pill is the one geometry correct at both viewports (r2's cell).
- **S-2 (r2) · booleans are words, not colours.** `formatGauge` (`:76-80`) `"YES"`/`"no"` — semantics survive forced-colors collapse; what keeps row 15 a contrast defect only.
- **S-3 (r2 + reader-LC's strengthening; reader-DU's S-B merged — ONE identity) · the instrument refuses to perturb what it measures, at BOTH layers.** CSS: `:151` `none` with `auto` only at `:167`/`:244`. Data: `isDebugOverlay()` early-returns on all six capture listeners (`:160/:181/:195/:205/:214/:218` — reader-LC's `:205` correct, reader-DU's `:206` off-by-one). Rows 4/19/20 are this instinct over- and mis-applied at three points, not absent.
- **S-4 (r2 + M-29) · motion governed centrally, not duplicated.** The `:179` blink defers to two `!important` global guards; the absence r1 scored as a defect is the architecture.
- **S-5 (r2) · `aria-expanded` is real** (`:12`), bound to the ref the click mutates — half a disclosure contract, correctly wired.
- **S-A (reader-DU) · two-layer zero-cost gating.** Flag absent ⇒ `:154` skips all six registrations AND the interval; `:4` ⇒ zero DOM. The gate's QUALITY is row 3's defect; its ARCHITECTURE is right.
- **S-C (both readers, merged) · `ORPHAN_POINTERS` (`:221-224`) encodes the fault signature, not just the events** — cross-correlates two independent input models to name stuck pids; the exact iOS capture-loss pathology this repo's memory records as the tool's reason to exist. The one place bookkeeping is redundantly verified rather than trusted — the discipline rows 9/20 lack.
- **S-D (reader-DU; ✓A) · bounded by construction.** `MAX_EVENTS = 80` enforced at `:74-76`; an always-on recorder that cannot grow without bound.
- **S-E (reader-DU; ✓A `:256-264`) · complete, capture-flag-correct six-listener teardown + interval clear** — the mismatch that silently fails to remove is absent.
- **S-F (reader-DU) + S-G/S-H (reader-LC) · defensive craft**: `describeElement` cannot throw (`:23-32`, one layer dead per row 30); the H.W3 Lane B docblock asserts a boundary that is still true (`:104-111` vs `:112-145`); `t?.hasPointerCapture?.(e.pointerId) ?? false` (`:82`) — three correct guards in one expression.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1 · D-N3 keyboard limb.** r2 tested keys with the HEADER focused only. Chromium 127+ keyboard-focusable scrollers may make `.debug-scroll` Tab-reachable (no focusable descendants). Probe: Tab-traverse into the scrollport, PageDown. Decides whether "unreachable by every modality" holds or drops one modality.
- **U-2 · every r2 live number** (occlusion table incl. 100%/97.5%/30.1%; scrollHeight 2829/243; target rects; AX dump + liveRegions 0; composited ratios vs `rgb(16, 5.6, 17.2)`; tab ordinals 26–29/30; 398-byte payload; RTL computed values) — adopted as the chromium-cell witness, re-measure before quoting as fact or asserting a delta against them.
- **U-3 · D-N10 flag-drop.** One unreproduced full-frame navigation dropping `?debug=1`. Stays an epistemic record; only its static half (row 28) is booked. Must not be promoted without a reproduction.
- **U-4 · production A/B currency.** r2 cites `assets/index-DJ8Ije_5.css` on `color.babb.dev` (2026-08-03); the LOCAL `origin/gh-pages` ref is `17c10267` (2026-03-24) carrying `index-DS-0HlXP.css`/`index-Dq7-wwjC.js` — either the local remote-tracking ref is stale or r2's filename cell is wrong. The SUBSTANCE (token + gate + surface deployed) is proved offline either way. **Corollary the tranche must hear (reader-DU's, adopted): the deployed product is a 2026-03-24 build predating the `demo/@` death (`a61094e3`) and Glass 7 — every "production is correct" A/B in this corpus is really "a March build is correct".** One live fetch of the deployed manifest settles it; rides X-W1's deploy-truth rows (DR-10/DR-22 per CC-002).
- **U-5 · iOS virtual-keyboard interaction** with `position: fixed; bottom: 8px` — never tested by any axis; input for row 11's re-form.
- **U-6 · LEAK-1 real-session reachability** (release-over-header without capture): proven by construction, gesture frequency unmeasured; desktop-only until row 1 lands, phone-reachable after.
- **U-7 · D#3 type-size limb** (10 px extended reading) — owner taste ruling, not a probe.
- **U-8 · should the seat exist at all** — owner ruling (r2 answers "not as gated"). Settled for the ruling's use: deletion touches ColorPicker.vue:103 + :175-176 + the `visual/` pair; the composable SURVIVES any UI removal (live instrumentation consumers: ComponentSliders.vue:117 → useSliderTouchGates, SpectrumCanvas.vue:52).

## INCREDULITY — corrections of the readers themselves

1. **Reader-DU's forced-colors count correction ("7 not 8") is itself wrong twice** — foundation.css holds ONE such block, glass-ui dist ≥10 files (this seat's census). The kill limb survives on its substantive ground alone. A correction that miscounts while correcting a count is exactly why this seat re-derives.
2. **Reader-DU's `:206` early-return cite is off by one** (`:205`); reader-LC correct.
3. **Both readers and all four axes missed ColorPicker.vue:138/:325** — the mounting parent already imports and calls `writeClipboard`. Row 8 is sharpened accordingly.
4. **The colour-literal dispute is settled at 37/34** (occurrences/distinct); r2's 31 matches neither.
5. **Cross-record**: sibling ADJ-9(d) inherited r2:207's no-op cure as its KEEP rationale; the corrected mechanism (row 4) vindicates the KEEP on stronger ground. The dated sibling file stands unedited (E-1/E-3); X-W8 carries the correction.
6. Reader-LC's severity qualifier on D-N1 ("BLOCKER within the seat") is adopted over r2's unqualified BLOCKER — the honest scope of the word.

## Closing verdict

**ADJUDICATED: instrument-RED at HEAD `ee9474e4` — 34 rows survive (2 BLOCKER: the orphaned `--z-debug` rung that has blinded the instrument for ~4.5 months and the phone one-way trap it causes; 9 MAJOR incl. the production-shipping substring gate proved offline in the deployed bundle, the producer-clipboard non-adoption whose primitive the mounting parent already imports, the stranded-FROZEN state machine, and the BR-6/BR-7 bypass; 14 MINOR; 9 INFO), every component cure routed to X-W8 per CC-081 under the SEQ-1 coupling law (the z-restore never lands alone), TEST-0 to X-W1, the canon rows to X-W10; 13 claims killed with written refutations, including three of the corpus's own prescribed cures (K-11, K-12, and r2's ladder-blind rung placement — the KISS cure is `var(--z-max)`); D#4 stays dead under M-28/M-29/M-31; the seat's core disciplines — zero-cost gating, two-layer non-perturbation, bounded recording, fault-signature encoding, correct teardown — are superlatives the r1 axes misread as defects. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis. The four challenge files, the evidence-r2 tree, and both reader returns remain immutable beside this record per E-1/E-3.
