`claude-fable-5`

# picker-pointerdebugoverlay — adjudicated apotheosis r2 (M-23/L-18, fresh Fable adjudicator)

**Seat**: apotheosis arbiter, round 2 · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **HEAD**: `209f47c3` (branch `tranche-u`) · **Component receipts** (this seat's own `shasum -a 256`, byte-identical to every challenge file's): `PointerDebugOverlay.vue` 286 lines `5529d038…c56a27` · `DebugEventLog.vue` `61b8aaee…fb6b3f` · `usePointerDebug.ts` `a1ad5b7c…db23aa` — 703 lines total; every line anchor in this record is current.
**SUPERSESSION (recorded so nothing is lost)**: this file replaces, at the same path and by orchestrator direction, the prior same-day apotheosis (r1 seat, HEADs `ee9474e4`/`08208bac`) after two fresh hostile re-readers challenged the full corpus INCLUDING that record — reader-DU killed its colour-census cell, and this seat's programmatic recount VINDICATES the kill (K-17). Everything else in the r1 record was re-adjudicated: carried where it survived (with provenance "r1 seat"), corrected where it did not. The four challenge files and the evidence-r2 tree remain immutable; only this registry row is superseded.
**Corpus**: `challenge-C-implementation.md` (8) · `challenge-D-design.md` (r1, 7 — Codex-provenance, quarantine-governed) · `challenge-D-design-r2.md` (GOVERNS: 7 rulings on r1 + D-N1…D-N10 + 5 superlatives, live-probed) · `challenge-L-library.md` (5) · the r1 apotheosis. **Readers adjudicated (round 2)**: reader-DU (design-and-usage) · reader-LC (library-and-code), both hostile re-reads of the whole corpus.
**Law compliance**: this file is this seat's ONLY write; product source read-only throughout; zero browser tooling (every live number is r2's chromium cell, labeled so); motion-quarantine FALSE rows NOT carried — D#4 stays barred by **M-28 FALSE / M-29 FALSE / M-31 FALSE** (M-30 UNPROVEN, a copy judgment), withdrawal at `motion-quarantine.md:265`; SCALE-PRESS (row 26) is deliberately booked in the tokenization register ONLY so no PRM claim rides it. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.
**Method (the incredulity seat's own instruments, all static)**: whole reads of all three seat files; `--z-debug` grep (1 reach `:136`, 0 definitions in demo/src; 0 hits in the whole installed glass-ui package); glass-ui z-ladder census by value (**17 distinct rungs** `--z-behind -10 … --z-toggle 999, --z-max 9999`, defined in BOTH `dist/styles/theme/bridges.css` AND `dist/styles/tokens/scheme-motion.css` — settling the readers' file dispute; reader-LC's "34" counts the `--z-index-*` aliases); token history `git log -S'--z-debug'` → `40fa935d` add (03-16) / `2457e922` consumer / `c84504d3` delete (2026-03-25); **offline production proof re-run**: `origin/gh-pages` HEAD `17c10267` (2026-03-24), `assets/index-DS-0HlXP.css` carries `z-debug`; **programmatic colour-literal count** (node, regex over both `<style>` blocks): parent 23 + child 14 = **37 occurrences / 31 DISTINCT**, six repeats exactly `#e0e0e0, rgba(255,255,255,0.08), #ff4444, #4caf50, #888, #666`; clipboard census (`useClipboard` = **3** demo adopters: App.vue:362, MixResultDisplay.vue:31, GradientEasingEditor.vue:94; `writeClipboard` = 13 more files **including the mounting parent ColorPicker.vue:138/:325**; `execCommand` = 1 hit, the overlay `:123`); `data-reka-slider-thumb` grep over node_modules → **0 files** while `reka-slider-thumb` matches only reka's Slider CSS-custom-property files; `debug-body` grep (1 hit = the attribute); test-coverage grep `PointerDebug|debug-overlay|debug=1` over populated test/ + e2e/ → **0**; `index.html` read (`dir="ltr"` at `:12` with a header comment that itself books the RTL ledger; viewport meta `:16` has **no `viewport-fit=cover`**); `vite.config.ts:286` `host: true`; foundation.css reads (`:393` `--z-ornament: 20` sole local rung; `:702-720` WHCM `:where(button…)` focus roster; `:728-750` BR-6 token+roster; `:764-790` BR-7 enumerated roster excluding `.debug-overlay`); `animations.css:184-193` PRM guard + glass `a11y-overrides.css` transition-property clamp; `shell.css:19-28` (`.app-layout` relative, NOT a stacking context) + `:94-96` (`.pane-wrapper--left { z-index: 1 }` under the comment "the no-z-hacks law — the slot assigns the layer"); glass-ui `base.css` press canon (`.interactive-item:active { scale: var(--scale-press-sm) }`, `.tap-squish…:active { scale: var(--scale-press) }`); `rtl-logical-property-audit.md:50/:60-66` (seat booked BY NAME, "dev tooling — lowest priority", disposition "booked to the component-owning lanes / a future i18n wave"); `DESIGN.md:267` stale row; `useSliderTouchGates.ts:16-18` type re-derivation (file now at `demo/picker/controls/ComponentSliders/composables/`); POINTER_DEBUG_KEY census (3 injectors, 1 provider ColorPicker.vue:175-176; mount `:103`, import `:141`); sibling `picker-debugeventlog.md` ADJ-6/ADJ-9 reads; X routing verified (`X/waves/W8.md:462` CC-081 overlay limb + G-11 "PointerDebugOverlay defects are cured" + the `:92` modify-carve row; `W7.md:627` corroborates the limb split).

## Scoreboard

**41 ADJUDICATED-CONFIRMED — 2 BLOCKER · 10 MAJOR · 18 MINOR · 11 INFO. 18 KILLED with written refutations (incl. three of the corpus's own cures, one reader's cure, and the r1 record's census cell). 21 reader-recovered misses adjudicated (10 net-new rows beyond the r1 record; every load-bearing one re-derived at the bytes). 12 superlatives. 9 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs. 1 row discharged-by-sibling (identity guard).**

The adjudicated shape: a well-architected instrument (zero-cost gating, two-layer non-perturbation, bounded recording, correct teardown) blinded ~4.5 months by one dead token, sprung as a one-way trap on the platform it exists for, shipped whole to production behind a substring gate — and, the round-2 deepening, whose **state machine poisons itself**: the one recovery action cannot reach the Map that caused the freeze (FRZ-2), `pointercancel` strands the alarm (FRZ-1), a release over the panel leaks the pid (LEAK-1), and on the LAN/phone origin the tool exists for, **every copy runs the lying fallback path** (row 7). The corpus's prescribed cures are provably wrong in four places (K-11, K-12, K-18, and the SEQ-1 trap) — cures are adjudicated separately from defects (MT-CSS-A §2, applied throughout).

**Route of record: X-W8** (CC-081 overlay limb, verified at `W8.md:462`/G-11) for every component cure · **X-W1** for TEST-0 (CC-030/031) and the U-4 deploy-currency probe · **X-W10** for the design-canon rows (DOC-1, the D#7 canon law).

## Ruled disagreements (the adjudication's actual work)

| id | reader-DU | reader-LC | r1 seat | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| **D#2a** (safe-area) | KILLED — cure is a no-op | CONFIRMED MINOR | CONFIRMED MINOR | **RESCOPED → INFO, dependency-booked. DU wins.** | `index.html:16` has no `viewport-fit=cover` (my read) — without the opt-in, iOS resolves every `env(safe-area-inset-*)` to 0, so r2's prescribed cure ("add `env(safe-area-inset-bottom)` on phone") adds **0px by construction**; `env(safe-area` = 0 hits app-wide, so no app contract is diverged from; r2's home-indicator reading is a Chromium-emulation cell that does not model non-cover letterboxing. LC and the r1 seat confirmed the *bytes* (true) while never testing the *cure*. Fires only if a future phone re-form (row 12) adopts `viewport-fit=cover` — booked as that dependency, not as a defect with a cure. |
| **D#2b** (RTL physical inset) | KILLED — duplicate of a ruled ledger row | CONFIRMED MINOR | CONFIRMED MINOR | **RESCOPED → INFO, already-booked. DU wins (identity guard).** | `rtl-logical-property-audit.md:50` books this file BY NAME ("dev-only overlay… lowest priority") under the `:60-66` disposition ("booked to the component-owning lanes / a future i18n wave"); `index.html:1-11`'s own header comment cites that ledger; `dir="ltr"` hard-set `:12`; no i18n dependency exists; r2 induced RTL by assignment. Re-booking a ruled row without citing the ruling violates the anti-rename law (MT-CSS-A §5). The two-line `inset-inline-start` swap may ride X-W8 incidentally; the IDENTITY stays with the U ledger. |
| **C#5** (no `finally` in the textarea fallback) | CONFIRMED MINOR (+ unhandled-rejection limb) | KILLED — no reachable thrower | CONFIRMED MINOR | **KILLED. LC wins (K-14).** | Between append `:121` and remove `:124` sit `ta.select()` (cannot throw on a live textarea) and `execCommand("copy")` (returns `false`, does not throw, in every engine still shipping it). DU's unhandled-rejection limb is *contingent on the same unreachable throw*, so it dies with it. The block is deleted whole by row 8's adoption cure regardless; `try/finally` would be hygiene on a corpse. |
| **D-N3 cure mechanism** | ADD `auto` to `.debug-scroll`, KEEP row `none` | "the only edit needed is the DELETION at `:236`" | ADD `auto` (r1 ruling) | **DU + r1 win; LC's prescription KILLED (K-18) — and DU's rider corrected.** | `pointer-events` INHERITS; `:151` supplies `none` to the whole subtree; deleting `:236` leaves the scrollport computing `none` by inheritance — LC's cure is the same no-op as r2's K-11, in mirror image (LC even proved the inheritance itself in its own missed #3, then drew the wrong edit). **This seat's correction of DU's rider**: under DU's own cure the gauge `.debug-section` *also* inherits `auto` — it sits INSIDE `.debug-scroll` (`:24-45`) — so expanded-state input-swallowing over the gauge area is intrinsic to scrollability, not avoided. The cure's real virtue is **state-scoping**: collapsed (the default) keeps full pass-through because the scrollport does not exist; the S-3 trade is confined to the expanded scroll area, and the wave must witness the recorder-blindness there (`isDebugOverlay` hides those events) knowingly. |
| **Colour census** | 37 occ / 31 distinct; kills r1's "34" | "~31 raw literals" | 37 / 34 | **37 / 31 — DU's kill VINDICATED (K-17).** | This seat's programmatic count (node, both style blocks): parent 23 + child 14 = 37 occurrences; exactly six repeats (`#e0e0e0`, `rgba(255,255,255,0.08)`, `#ff4444`, `#4caf50`, `#888`, `#666`) → **31 distinct**. r2's "31" was the distinct count and was right all along; the r1 record's "34 distinct" is not reproducible and dies. |
| **z-ladder home** | `scheme-motion.css`, 17 rungs | `bridges.css`, 34 tokens | `scheme-motion.css`, 17 | **17 distinct rungs, defined in BOTH files; all three citations partial.** | My census by value across glass-ui dist CSS: 17 distinct `--z-*` rungs (`-10…9999`), present in `theme/bridges.css` AND `tokens/scheme-motion.css`; LC's 34 counts the `--z-index-*` aliases. The load-bearing facts are unanimous and re-proved: **no `--z-debug` anywhere in the installed package (grep whole-package: 0)**, `--z-max: 9999` served. LC's kill of r2:73's under-scoped `dist/*.css` glob stands (K-16). |
| **`useClipboard` adopter census** | 5 sites incl. MixPane/GenerateControls/GradientVisualizer | 3 named in r1 | 3 sites | **3 `useClipboard` + 13 `writeClipboard` files — DU conflated the two primitives.** | My grep: `useClipboard` exactly 3 (App.vue:362 resetMs 2000, MixResultDisplay.vue:31 resetMs 1500, GradientEasingEditor.vue:94 resetMs 1400); DU's other citations are `writeClipboard` call sites. Substance UNCHANGED — the producer family is adopted app-wide and the identical-1500ms twin exists — but the labels are corrected so the wave imports the right primitive. |
| **FRZ-2 cure text** | — | "one line: clear the Map… alongside `:100`" | not booked | **Mechanism CONFIRMED; cure text corrected.** | `:100` is inside `forceReleaseAll` at OUTER scope — there is no line "alongside `:100`" that can see `activePointers` (declared `:156`, inside the `enabled` block). The real one-edit cure is hoisting the Map declaration above `:95` (or registering a reset hook inside the block). A cure that cannot be typed where prescribed is not one line. |

## Adjudicated defect roster

Format: **id · severity · claim** — proof (compressed; `✓A` = re-derived by this seat at the bytes) — **ROUTE**.

### BLOCKER (instrument-scoped: the surface is flag-gated, so no product journey breaks; D-N4 is what makes it product-facing at all)

1. **D-N1 · BLOCKER · `--z-debug` is orphaned; `.debug-overlay` computes `z-index: auto` and the picker card paints over the entire status column — a ~4.5-month regression vs the shipped build.** ✓A sole reach `:136`, zero definitions (demo/src); foundation.css's only local rung is `--z-ornament: 20` (`:393`); the served ladder = 17 rungs, none named `--z-debug`, whole-package grep 0. Invalid-at-computed-value-time ⇒ initial `auto`. Occlusion chain ✓A static: `.app-layout` (shell.css:19-28) is `position: relative` with no stacking-context property, so `.pane-wrapper--left { z-index: 1 }` (`:94-96` — sitting under the comment "the no-z-hacks law — the slot assigns the layer"; the seat that broke the law is this one, by dead token) paints above the overlay's z-auto level in the root stacking context. Regression ✓A offline: added `40fa935d`, deleted `c84504d3` (2026-03-25) on the premise glass-ui bridges the ladder; `origin/gh-pages` (`17c10267`, 2026-03-24) still ships `--z-debug:99999`. **CURE (adjudicated)**: `z-index: var(--z-max)` — the served 9999 ceiling. r2's "re-declare above `--z-modal: 140`" stays killed as ladder-blind (six served rungs sit above modal). Occlusion percentages are r2's cell (U-2). **ROUTE: X-W8 (CC-081 overlay limb, G-11), bound by row 12's SEQ-1 law.**
2. **D-N2 · BLOCKER · at 390×844 the expanded header is occluded and not hit-testable; the disclosure is a one-way trap.** ✓A strict consequence of row 1: the header is the SOLE collapse affordance (`pointer-events: auto` only at `:167`/`:244`; no Escape, no close, no outside-dismiss in `:1-129`; `collapsed` component-local `:73`); painted under `.pane-wrapper--left` it cannot receive a trusted tap; only exit = reload; `collapsed = ref(true)` means the first tap springs it. 97.5%/`elementsFromPoint`/actionability figures are r2's cell (U-2). **ROUTE: X-W8, same commit as row 1 + row 12.**

### MAJOR

3. **D-N4 · MAJOR · the activation gate is an unparsed substring match with no build condition, and it ships to production.** ✓A `usePointerDebug.ts:34-39` — `hash.includes("debug=1") || search.includes("debug=1")`; `?nodebug=1` matches at index 2 by construction; zero `import.meta.env` in the subsystem; static import ColorPicker.vue:141 + unconditional render `:103` (the `v-if` is inside, `:4`) ⇒ untree-shakeable. Production ✓A offline: the deployed March build carries the gate AND still resolves the token, so a stranger's `?nodebug=1` gets a FULLY FUNCTIONAL console at z 99999 — six capture listeners, 500 ms interval, one-tap UA/screen/DPR export, DOM-walking capture-release Reset. Live render is r2's cell (U-4); existence question is the owner's (U-8). Cure = both halves: `URLSearchParams` parse (+ hash half) AND a build gate. **ROUTE: X-W8.**
4. **D-N3 · MAJOR · the event-log scrollport advertises `overflow-y: auto` while removed from hit-testing — the flight recorder's history is pointer-unreachable.** ✓A `:232-237`, no `tabindex` anywhere; `MAX_EVENTS = 80` vs `35dvh` makes overflow the steady state. Corrections adopted (✓A): causation — `:236` is INERT, the causal declaration is `:151` (inherited); scope — `DebugEventLog.vue:33` reverses, so visible rows are the NEWEST ("history truncated", not "payload lost"). **CURE (ruled above)**: `.debug-scroll { pointer-events: auto }`, row-level `none` KEPT (becomes load-bearing for the first time); LC's deletion-only edit killed (K-18); the state-scoping virtue and the expanded-area recorder-blindness trade are recorded for the wave's witness. Keyboard limb UNPROVEN (U-1). **ROUTE: X-W8, sequenced with sibling ADJ-9(d).**
5. **D-N5 · MAJOR · no accessible structure: null role, zero live regions, state text inside the control's accessible name, plain-div headings, unassociated key/value pairs, inconsistent button typing.** ✓A root `:3-7` carries only `class`; grep 0 for `aria-live|role="status"|alert|log|tabindex` in both files; `FROZEN?`/`copied!`/glyph are children of the `:9` button so its name mutates; `State` (`:27`) and `Events (n)` (DEL `:4-6`) are divs; `:49/:55/:58` lack `type="button"` while `:10` has it (form-submit consequence stays killed, K-6). AX dump is r2's cell. **ROUTE: X-W8** (region role+label; `role="status"` for copy via row 8's `status`; `role="alert"` for FROZEN; real headings; `type` ×3). Sibling ADJ-8's DECLINE of live regions for the event FEED stands — live regions here are the two discrete chips only.
6. **D#1/C#2 · MAJOR · `aria-controls="debug-body"` references an ID that exists nowhere; the cure is structural.** ✓A grep: one hit = the attribute (`:13`). Both readers independently re-proved K-12 (LC's missed #4 = DU's kill): the `v-if` body `:22` has TWO sibling roots (`:24`, `:48`), and in the DEFAULT collapsed state neither is mounted — `id="debug-body"` alone dangles exactly when it matters; `<template>` can carry no id. Cure: keep the body mounted under `hidden` with ONE wrapper carrying the id, so `aria-expanded` (`:12`, correct — S-5) and `aria-controls` agree. **ROUTE: X-W8.**
7. **D#5/C#3 + CLIP-PRIMARY · MAJOR (reader-DU's recovery adopted — the round-2 re-grade) · copy reports success unconditionally, AND the lying fallback is the PRIMARY path on the instrument's sole target platform.** ✓A `:123` drops the `execCommand` boolean; both branches set `copied.value = true` (`:113`, `:125`); bare `catch` `:115`; one outcome span `:18`, no error state. **CLIP-PRIMARY ✓A**: `vite.config.ts:286` `host: true` (the memory-recorded LAN mobile-testing seam) ⇒ the phone loads `http://<LAN-IP>:9000`, an insecure context ⇒ `navigator.clipboard` is `undefined` ⇒ the `catch` runs on EVERY copy — the path the file's own `:116` comment calls "older browsers" IS the normal path for the iOS-Safari-over-LAN job this tool exists for. Converts D#5 from polish to the instrument's ordinary export behaviour. iOS `select()`/`setSelectionRange` rider → U-9. Dies with row 8. **ROUTE: X-W8.**
8. **CLIP-1 · MAJOR · `copyJSON` is a divergent hand-rolled re-implementation of the producer clipboard the app already adopted — every clipboard defect in this corpus is a symptom of one non-adoption.** ✓A glass-ui 7.0.0 ships `useClipboard` (status/`CopyResult` with `no-api` branch/`resetMs`/`timeoutMs` watchdog/`onCopyError`) + `writeClipboard`; my census: 3 `useClipboard` + 13 `writeClipboard` files, **including the mounting parent — ColorPicker.vue:138 imports it, `:325` calls it** (r1 seat's find, re-verified); MixResultDisplay's `resetMs: 1500` is the identical constant the overlay hand-rolls at `:114`/`:126`; the overlay holds the repo's sole `execCommand`. Cure: delete `:74`/`:109-128`, adopt `useClipboard`, bind `status` to row 5's status region — closes rows 7, 17, 25's egress limb, and half of row 5 in one edit. Adopter-label correction ruled above. **ROUTE: X-W8 (CC-081 names this limb).**
9. **FRZ-1 · MAJOR (r1 seat; re-verified ✓A) · `onGlobalPointerCancel` omits the frozen-reset its `pointerup` twin performs — a `pointercancel` strands the FROZEN alarm.** ✓A `:204-210` (delete, recompute, gauge, log — NO frozen clear, NO `lastPointerActivity` refresh) vs `:180-192` (all four). After a freeze terminated by `pointercancel` — the likeliest terminator of the iOS capture-loss pathology — the `:242` guard can never re-clear; on touch, `pointermove` requires contact, so the stale red alarm survives arbitrary idle. The other three transitions (`:164`, `:186`, `:98`) all clear it: an omission, not a choice. **ROUTE: X-W8, same commit as rows 10/19.**
10. **FRZ-2 · MAJOR (reader-LC recovered — the round-2 headline find; ✓A whole-lifecycle trace) · Reset permanently poisons the freeze detector: `forceReleaseAll` clears the flag but lexically cannot reach the `activePointers` Map, so the phantom entry that CAUSED the freeze survives the one recovery action — and every subsequent ordinary release re-latches a false FREEZE.** ✓A `activePointers` declared `:156` INSIDE `if (enabled…)` (`:154`); `forceReleaseAll` declared `:95` at OUTER scope; full lifecycle: set `:169`, delete `:184`/`:206`, reads `:177/:185/:191/:207-208/:221/:223/:239/:247` — **no `.clear()`, no reassignment, anywhere**. Sequence ✓A: freeze (phantom pid never fires up/cancel) → Reset (`:100` flag false, `:98-99` unfrozen) → next tick renders the contradiction `activePointer: no` beside `activePointers: 1` (`:238-239`) → ONE ordinary press-release re-latches via `:185` (`size > 0` — the phantom) → 2.5 s idle → `:242` fires FREEZE_DETECTED with stale `stuckPids` (`:247-251`). Permanently. Second consequence ✓A: `:221-224` fires false ORPHAN_POINTERS on EVERY subsequent `touchend` — the log fills with false alarms on the target platform, and S-C's signature mechanism becomes the false-alarm generator. The instrument reports the exact fault it exists to detect, on a healthy page, forever after its own recovery action. Cure text corrected (ruled above): hoist the Map above `:95`. **ROUTE: X-W8, same commit as rows 9/19 — the three-defect state machine is one seam.**
11. **D#7+BR-BYPASS · MAJOR (merged identity) · the seat is a separate visual system that functionally bypasses two ratified app-level a11y contracts.** ✓A `prefers-contrast: more` (foundation.css:728-750) and `prefers-reduced-transparency: reduce` (`:764-790`) both operate via tokens/rosters the seat neither reads nor matches — an explicit escalation or reduction request produces byte-identical debug output, including row 15's `#666` and the surviving literal `blur(8px)` (`:149`), which the BR-7 roster and its `--glass-level: 0` lever cannot reach. ✓A census settled: **37 literal occurrences / 31 distinct** vs 5 `var()` in the parent (one dead) and **0 in the child**; font hand-authored twice (`:143`, DEL `:75`) diverging from the served mono token (FONT-DUP, row 36, folded for cure). Post-K-13 narrow truth: surface tiering + the Reset-vs-Copy distinction are uncovered under forced colors; focus outlines are not. **ROUTE: component cure X-W8; canon/roster law X-W10.**
12. **SEQ-1 · MAJOR (reader-DU re-derived; standing law) · r2's remediation ordering is self-defeating: step 1 shipped alone converts D-N2's fix into the very occlusion r2 refuted as D#2c.** ✓A geometry in-source `:133-141` (fixed bottom-left, 280px wide, up to 35dvh, 92% black): restore the rung alone and the expanded panel paints over the `.channel-rows` the instrument exists to diagnose, at the viewport r2 itself measured. Bounded (input passes through `:151`; collapsed default S-1), not cleared. **BINDING on X-W8: the z-restore lands COUPLED with a small-viewport expanded-state re-form or height/position guard, never alone.** No axis modeled the post-fix state — that omission is itself the finding. **ROUTE: X-W8 sequencing law.**

### MINOR

13. **D#6 · MINOR · Reset, Copy JSON and Clear share identical geometry although Reset is destructive and mutates the fault under inspection.** ✓A `.debug-btn { flex: 1 }` `:250`; colour-only differentiation `:275-285`; `forceReleaseAllPointers` (`:112-145`) walks live selectors releasing capture for logged pids + 0…10, no confirmation. **ROUTE: X-W8.**
14. **NAME-1 · MINOR (reader-DU recovered, NEW; ✓A) · `Reset` and `Clear` are near-synonyms mapped to the two most different actions, the more dangerous one carrying the vaguer name — and under forced colors the words are the ONLY discriminator left.** ✓A `:49-54` Reset → the DOM-walking capture release; `:58-60` Clear → wipe the recorder; no `aria-label`/`title`/scope text on either; r2's own probe shows the danger/copy backgrounds collapse byte-identical under forced colors, leaving the defective words as the sole signal. No axis examined the naming. **ROUTE: X-W8 (one cure with rows 13/5).**
15. **D-N6 · MINOR · `.debug-val-false` `#666` at 10 px fails WCAG 1.4.3 — the false reading of every boolean gauge sits in the lowest-contrast ink while `true` gets green.** ✓A `:228-230`/`:144`; both readers' arithmetic agrees (3.47 live-composite; this seat re-derives the static bound: L(#666)=0.133 ⇒ ≤3.66:1 against pure black, and ≈3.2:1 through the 92% panel over even a white page — no reachable backdrop rescues it). S-2 keeps it a contrast defect, not a comprehension defect. **ROUTE: X-W8.**
16. **D-N7 · MINOR · the disclosure has no authored focus treatment while its sibling buttons get a 2 px ring.** ✓A `.debug-btn:focus-visible` `:270-273`; no `.debug-header:focus-visible`; my grep confirms demo/styles' ONLY `:focus-visible` is foundation.css:717, inside the WHCM block (`:678`) — in the normal register the header falls to the UA hairline on near-black; glass-ui's `.focus-ring` utility unadopted. **ROUTE: X-W8.**
17. **C#4 (truncation limb) · MINOR · unretained feedback timers — a second copy inside 1.5 s is truncated by the first timer.** ✓A `:114`/`:126`, no handle, no `clearTimeout`. Leak limb killed (K-15). Dies by construction under row 8 (the producer impl retains and cancels). **ROUTE: X-W8.**
18. **PE-DEAD · MINOR (both readers; one identity) · three of the four `pointer-events` declarations are inert restatements of one inherited value, and the mis-belief they are load-bearing produced two no-op cures.** ✓A `:151` supplies `none`; only `:167`/`:244` opt back; `:236`, DEL `:62`, DEL `:66` change no computed value; DEL `:2`/`:60` comments credit the wrong lines. Consequences ruled at K-11/K-18; row-level `none` becomes load-bearing under row 4's cure. **ROUTE: X-W8, same edit as row 4.**
19. **LEAK-1 · MINOR (r1 seat; re-verified ✓A) · `onGlobalPointerUp`'s overlay early-return skips bookkeeping as well as logging — a release over the header/actions leaks the pid and manufactures a false FROZEN.** ✓A `:181` return precedes the `:184` delete and `:183` refresh; the `pointerdown` filter (`:160`) is correct because skipping an INSERT is harmless — skipping a DELETE is not. Correct shape: unconditional bookkeeping, gated logging. Desktop-only until row 1 lands; phone-reachable after — same commit as rows 9/10. **ROUTE: X-W8.**
20. **CLR-1 · MINOR (reader-DU; ✓A) · `Clear` clears only half the panel — gauges survive and read as current under an emptied log.** ✓A `clearEvents` (`:91-93`) never touches `state.gauges`; `lastDown.*`/`touches`/`lastForceRelease`/`lastForceReset` persist under `State` with no staleness marker — pre-clear values presented as present state, in a flight recorder whose purpose is "what was true at the freeze". **ROUTE: X-W8.**
21. **GAUGE-ORDER · MINOR (reader-DU recovered, NEW; ✓A) · the gauge panel has no stable membership or ordering — rows appear one at a time in insertion order, so a readout scanned by position reflows mid-diagnosis.** ✓A `v-for` over the plain reactive object `:28-32`; `setGauge` (`:86-89`) creates keys lazily: `lastDown.*` on first down (`:174-176`), `activePointer`/`silenceMs` on the first 500 ms tick (`:238-240`), `touches` on first touch (`:215`), `lastForceRelease`/`lastForceReset` on reset (`:97`/`:142`), `spec.*` from SpectrumCanvas.vue:52. No declared schema, no placeholders, no grouping despite the `global.`/`spec.` namespaces. For a glance-at-a-fixed-row instrument, an unstable row set is a design defect; no axis looked at the list as a list. **ROUTE: X-W8.**
22. **GAUGE-2 · MINOR (merged: r1 GAUGE-GUARD + reader-LC's two-idioms limb; ✓A) · one user action writes two near-identically-named gauges through two idioms, one bypassing the module's own enabled-guard.** ✓A `:97` direct mutation (bypasses `:87`'s guard) vs `:142` `setGauge` — the operator sees `lastForceRelease` and `lastForceReset` as two readings of one click, differing by a word; `clearEvents`/`forceReleaseAllPointers` likewise unguarded while `log`/`logEvent`/`setGauge` all guard — two postures in one module, and the only path that can mutate state while nominally off. **ROUTE: X-W8.**
23. **STR-1 · MINOR (reader-LC recovered, NEW; ✓A) · the composable hard-codes the view's presentational class as its self-exclusion contract — no shared constant, no test; a rename silently turns the instrument into an observer of itself.** ✓A `isDebugOverlay()` (`:149-152`) gates all six listeners on `closest(".debug-overlay")`; the string's only definitions are the sibling view's `:5`/`:132`; zero tests reference either file. r2's own step 5 prescribes the exact cosmetic sweep (adopt a `.glass-*` rung) that would sever it. This is the load-bearing mechanism under superlative S-3, resting on an unguarded literal. **ROUTE: X-W8 (export the constant beside POINTER_DEBUG_KEY).**
24. **SEL-DEAD · MINOR (reader-LC recovered, NEW; ✓A) · `[data-reka-slider-thumb]` in the Reset selector matches nothing in the entire dependency tree — a dead selector in the recovery path whose comment advertises reach it does not have.** ✓A my grep: `data-reka-slider-thumb` → 0 files under node_modules; reka's Slider files carry only the CSS custom property `--reka-slider-thumb-transform` (the likely confusion); the other selectors are live (`.slider-track`/`.slider-thumb` in glass-ui's shipped Slider; `[data-reka-collection-item]` in reka's Collection; `.spectrum-picker`/`.touch-gate-target` in-tree). The `:116` comment ("catch reka-ui slider internals too") certifies the dead reach. **ROUTE: X-W8.**
25. **FOCUS-STEAL · MINOR (reader-DU recovered, NEW; ✓A) · the fallback export steals focus into the temporary textarea and never restores it — on the path row 7 proves primary, to controls that are already the last tab stops.** ✓A `:122` `ta.select()` focuses; `:124` removes the focused node ⇒ focus resets to `<body>`; no `activeElement` capture/restore. Compounded by row 7 (default path on the LAN origin) and D-N9 (tab stops 26–29/30, r2's cell) — a keyboard operator re-traverses the app per copy. Dies with row 8. **ROUTE: X-W8.**
26. **SCALE-PRESS · MINOR (reader-DU recovered, NEW; ✓A — tokenization register ONLY, no PRM claim carried) · the press affordance diverges from the producer's motion canon, and the file's own comment certifies a half-done tokenization.** ✓A `:267-269` `transform: scale(0.95)` vs the served canon (`base.css`: `.interactive-item:active { scale: var(--scale-press-sm) }`, `.tap-squish…:active { scale: var(--scale-press) }`) — magnitude is a literal where a token is served, and `transform` clobbers where the canon's independent `scale` composes; the `:260` comment ("tokenize to glass-ui canon") certifies the block as tokenized while the press three lines down stays raw. Quarantine checked: M-31 (FALSE) rules only the PRM register of `:261-269` and does not bar this row. **ROUTE: X-W8 (inside row 11's cure).**
27. **EGR-1 · MINOR composite (reader-DU) · the instrument has no reliable data egress at all — selection (inherited `none`), scroll (row 4), and export (row 7) are each broken, and they compose to zero trustworthy exits.** Any partial landing still leaves zero egress. **Booked as a delta obligation on X-W8**: the close witnesses all three limbs together. **ROUTE: X-W8.**
28. **BTN-CONTRAST · MINOR (r1 seat's own find + arithmetic; carried) · `.debug-btn` boundary contrast is bounded at ≈1.66–1.80:1 vs SC 1.4.11's 3:1 — the plain `Clear` button, the one control with no colour cue, is the worst case.** Border `rgba(255,255,255,0.2)` over the 95%-opaque bar bounds the ratio below 3:1 across every backdrop (spot-re-derived: ≈1.80 in the white-page extreme). **ROUTE: X-W8.**
29. **TEST-0 · MINOR (both readers; ✓A re-grepped) · the entire 703-line subsystem has zero coverage across every suite — the mechanism of the ~4.5-month dark period.** ✓A `PointerDebug|debug-overlay|debug=1` over populated test/ + e2e/ → 0. One spec loading `?debug=1`, expanding, asserting non-`auto` computed z-index + one visible gauge value fails at `c84504d3` and catches rows 1/2/4 at the causing commit. **ROUTE: X-W1 (CC-030/031); the spec may ride X-W8's cure commit as witness.**
30. **DOC-1 · MINOR (merged: r1 seat + reader-LC; ✓A) · the governing design register is stale and silently violated by this seat — the doc that would have caught D-N1 never enumerated it.** ✓A DESIGN.md:267 cites `PointerDebugOverlay.vue:266` for a blink living at `:179` (`:265-266` is `.debug-btn:hover`); r1 seat's §Z-tier falsifications carried: "all z reaches route through glass-ui `--z-*`" is falsified by `--z-debug` (undefined) and `--z-ornament` (demo-local), and its `--z-popover: 70` vs the served 130. **ROUTE: X-W10 (canon re-author); X-W8 rider corrects rows it touches.**

### INFO

31. **D#2a · INFO (RESCOPED — ruled above) · no safe-area handling — a dependency, not a defect.** Bytes real (`:134` `bottom: 8px`); cure provably 0px without `viewport-fit=cover` (absent app-wide, `index.html:16`); fires only if row 12's phone re-form adopts cover. **ROUTE: rider on X-W8's re-form; no standalone row.**
32. **D#2b · INFO (RESCOPED — ruled above) · physical `left` inset + direction-inheriting flex under an induced RTL root.** Already booked BY NAME in `rtl-logical-property-audit.md:50` (dev-only, lowest priority); `dir="ltr"` hard-set; identity stays with the U ledger (anti-rename). The two-line logical swap may ride X-W8 incidentally. **ROUTE: existing U-ledger booking.**
33. **D-N8/C#7 · INFO · the export embeds a UA/screen/DPR fingerprint and carries no schema version or snapshot identity.** ✓A `:85-91`, no `version`/`schema`/id in `:82-107`; tear impossible (K-10); one producer, one human consumer — a `v: 1` key is cheap if row 8 touches the function. Matters only via row 3; defused by its gate cure. **ROUTE: X-W8 (rides).**
34. **D-N9 · INFO · body-teleport puts the four controls last in the tab order; expanding moves focus nowhere.** Mechanism ✓A (`:2`, `:14`); ordinals r2's cell (U-2). Compounds row 25. **ROUTE: X-W8.**
35. **GATE-ONCE · INFO (r1 seat; ✓A) · the flag is captured once at setup (`:42`, plain const)** — no in-session toggle, no reactive re-read, no in-product re-entry in a hash-routed app. The statically-provable half of unscored D-N10 (U-3). **ROUTE: X-W8 (rides row 3).**
36. **FONT-DUP · INFO (✓A) · the mono stack is authored twice, diverging from the served token in both copies** (`:143`, DEL `:75`) — a visible two-typeface split on macOS. Cure inside row 11; booked so the child copy is not lost. **ROUTE: X-W8.**
37. **DEAD-BRANCH · INFO (r1 seat; ✓A) · `describeElement`'s SVG guard is unreachable** — post-`instanceof HTMLElement` narrowing (`:24`), `className` is `string`; the `:26-30` ternary's else limb is dead. Coexists with S-F. **ROUTE: X-W8 (rides).**
38. **DEAD-EXPORT · INFO (r1 seat + reader-LC; ✓A) · `forceReleaseAll` sits on the public surface with zero external consumers** (`:273`; the view wires Reset to `forceReleaseAllPointers`) — two reset verbs where only one is reachable, and the weaker one silently does not walk the DOM. Note: row 10's cure changes what BOTH verbs must clear. **ROUTE: X-W8.**
39. **TYPE-DUP · INFO (r1 seat; ✓A at the moved path) · `useSliderTouchGates.ts:16-18`** (now `demo/picker/controls/ComponentSliders/composables/`) imports the function as a type to locally re-derive the `UsePointerDebugReturn` alias exported at `:278`. One import replaces three lines. **ROUTE: X-W8.**
40. **CSS-DUP · INFO (r1 seat + reader-LC's sharpening; ✓A) · `.debug-section`/`.debug-section-title` byte-duplicated across the pair (`:196-207` / DEL `:47-58`); DEL `:75-77` re-declares three inherited properties.** IDENTITY GUARD: sibling ADJ-9(a) holds the sharper scoped-CSS analysis of which copy is genuinely needed (the child's `.debug-section-title` descendant copy IS needed; its `.debug-section` root copy is not) — one identity, cross-booked, cure lands once. **ROUTE: X-W8.**
41. **LIFE-1 · INFO (reader-LC recovered, NEW; ✓A) · six document-level capture listeners + the 500 ms interval install at CALL time (`:154`/`:227-235`), while teardown is a conditionally-effective `onUnmounted` (`:256`)** — a latent permanent leak the moment the exported composable (`:41`) is called outside a setup scope. Today's sole call site is in setup (ColorPicker.vue:175); the module documents no constraint. Standard cure: `onScopeDispose`/`useEventListener`. **ROUTE: X-W8 (rides).**

**Discharged-by-sibling (not a roster row)**: the index-keyed reversed list (`DebugEventLog.vue:9-10`/`:33`), re-relayed by reader-LC, is ALREADY BOOKED as picker-debugeventlog **ADJ-6** (✓A re-read: cure = a monotonic event id minted in `log()`, routed X-W8) — identity guard, not re-booked here.

## Killed-claims register

- **K-1 · D#4 ("FROZEN? blinks forever; no reduced-motion rule") — QUARANTINE-BARRED and false.** M-28/M-29/M-31 FALSE (M-30 UNPROVEN), withdrawal `motion-quarantine.md:265`. ✓A both `!important` guards re-read (`animations.css:184-193`; glass `a11y-overrides.css` clamps `animation-*` AND strikes `transform` from the transition property list); a scoped (0,2,0) rule cannot outrank them. The absence of a local rule IS the architecture (S-4). May not be carried.
- **K-2 · D#2c ("280 px overlay covers essential phone UI") — polarity inverted in the live bytes**: `z-index: auto` (row 1) means the product covers the overlay; `:151` means the body cannot intercept input even where it paints. Dead as written — CONDITIONALLY resurrected by the corpus's own step-1 cure, which is exactly row 12's law.
- **K-3 · D#3 (type/touch-target sizes) — refuted against the only binding standard**: header 278×30.2 and buttons 83.3×27 CSS px clear SC 2.5.8 (24×24); the claim names no standard; `touch-action: manipulation` on both (`:168`, `:259`) evidences deliberate touch tuning. Type-size limb = owner taste (U-7); the real type defect is row 15.
- **K-4 · C#1 (`inject(POINTER_DEBUG_KEY)!` can crash) — unreachable by construction.** ✓A census: exactly 3 injectors (overlay `:72`, ComponentSliders.vue:117, SpectrumCanvas.vue:52), all descendants of the sole provider (ColorPicker.vue:175-176, same setup, provide precedes child render; Teleport walks the component tree, not the DOM). House idiom, accurate documentation of an invariant.
- **K-5 · C#6 (unguarded `navigator`/`screen`/`document`) — posits a caller that cannot exist.** `buildExportJSON`/`copyJSON` are module-private; the only invocation is `@click` on a rendered button (`:55`) inside a live DOM; `isDebugEnabled()` returns `false` when `typeof window === "undefined"` (`:34-35`) so `:4` renders nothing; no SSR path in the project. The clipboard-absence limb survives only as row 8's `no-api` branch.
- **K-6 · C#8's failure scenario (form-submit) — structurally impossible.** `Teleport to="body"` (`:2`) makes the subtree a direct child of `<body>`; form ownership is by ancestry or `form=`, neither acquirable. The attribute inconsistency survives inside row 5.
- **K-7 · L#2 ("no disabled/unavailable branch") — refuted by the template's first line.** `:4` `v-if="debug.state.enabled"` + the provider gate (`:154`, `:64`, `:80`, `:87`): disabled ⇒ zero DOM, zero listeners, zero interval (S-A). Only the provider-UNAVAILABLE branch is missing, and K-4 proves it unreachable.
- **K-8 · L#4 ("Teleport target unconfigurable; host applications cannot choose") — category error.** `package.json` `files` excludes everything under `demo/`; all exports resolve under `dist/`; there are no host applications and exactly one consumer. A configurable portal is a parameter with one possible argument — KISS-banned by name.
- **K-9 · L-SPLIT (four-service decomposition + producer-owned diagnostic sheet) — contrived AND ignorant of the tree.** ✓A the DOM-walking boundary was already drawn at H.W3 Lane B (`:104-111` docblock matches `:112-145`); a glass-ui-owned picker diagnostic inverts `feedback_glass_ui_first_class` into producer-dumping. The clipboard limb survives INVERTED as row 8 — adopt, don't build.
- **K-10 · L#5/C#7 tear limb — impossible.** `:83-106` is one synchronous `JSON.stringify`, no await, no yield; C#7 conceded it in the same finding. Versioning residue survives at row 33.
- **K-11 · r2's D-N3 cure (step 2: "move `pointer-events: none` off `.debug-scroll` onto the rows") — a no-op in BOTH halves and a latent regression as one reading.** The property inherits; `:151` already supplies `none`; the rows already carry it (DEL `:62`/`:66`). Correct cure = row 4's OPPOSITE declaration. The `tabindex`/name half of step 2 survives in row 4.
- **K-12 · r2's D#1 cure (step 4: "`id="debug-body"` on the disclosure target") — insufficient as stated.** Two sibling roots under the `v-if`; in the DEFAULT collapsed state neither is mounted, so the reference dangles exactly when the cure is meant to bite; `<template>` carries no element. Both round-2 readers re-derived this independently. Structural cure in row 6.
- **K-13 · D#7's forced-colors rider as stated ("zero `.debug-*` selector in any forced-colors block" ⇒ zero WHCM coverage) — over-broad.** ✓A foundation.css:702-720 covers all four controls' focus affordance via the class-agnostic `:where(button…)` roster; demo carries 2 real forced-colors blocks (foundation.css:678, GradientStopEditor.vue:353 — `:665` is a comment), glass-ui dist ≥10 files. The narrow truth (surface tiering + Reset/Copy distinction uncovered) survives inside row 11.
- **K-14 · C#5 (no `finally`; DOM residue + DU's unhandled-rejection limb) — no reachable thrower (ruled disagreement).** `ta.select()` cannot throw on a live textarea; `execCommand("copy")` returns `false` rather than throwing; DU's rejection limb is contingent on the same unreachable throw. The whole block is deleted by row 8; `try/finally` would be hygiene on a corpse.
- **K-15 · C#4's leak limb — not a leak.** Two single-shot 1500 ms timers whose closures capture one ref; post-unmount they fire once into a detached ref and are collected. The truncation limb survives as row 17.
- **K-16 · r2:73's evidence line ("grep `dist/*.css` → zero `--z-*` hits") — a glob artifact proving the wrong thing** (reader-LC's kill, re-proved on the correct path by this seat: the rungs live under `dist/styles/**`; whole-package `z-debug` grep → 0). D-N1 stands; the defective evidence line does not.
- **K-17 · the r1 apotheosis's census cell ("37 occurrences / 34 distinct; r2's 31 matches neither reading") — killed by reader-DU, VINDICATED by this seat's programmatic recount: 37 / 31.** r2's "31" was the distinct count and was correct all along. Recorded so the wrong number cannot propagate into row 11's cure sizing — and as the round-2 proof that even adjudication cells must be re-derivable.
- **K-18 · reader-LC's own D-N3 cure ("the only edit needed is the DELETION at `:236`") — the K-11 no-op in mirror image.** Deleting `:236` leaves the scrollport inheriting `none` from `:151`; the computed value does not change. A reader that proved the inheritance in its own missed #3 and then prescribed the inheritance-blind edit is the strongest argument this corpus offers for adjudicating cures separately from defects.

## Superlatives (provenance preserved; merged identities)

- **S-1 (r2; both readers re-verified) · the default is right, and it is the one geometry correct at both viewports.** `:73` `collapsed = ref(true)` with intent recorded; `.debug-collapsed` self-sizes (`:154-157`); 67.6×32.2 clear of every control at both viewports (r2's cell).
- **S-2 (r2) · booleans are words, not colours.** `formatGauge` (`:76-80`) `"YES"`/`"no"` — semantics survive forced-colors collapse; keeps row 15 contrast-only; ahead of the child's colour-only severity backgrounds.
- **S-3 (r2 + both readers; LC's caveat adopted) · the instrument refuses to perturb what it measures, at BOTH layers.** CSS `:151` + JS `isDebugOverlay()` early-returns on all six listeners (`:160/:181/:195/:205/:214/:218`). Rows 4/18/19 are this instinct over- and mis-applied at three points, not absent — and the JS layer rests on row 23's unguarded string literal.
- **S-4 (r2 + M-29) · motion is governed centrally, not duplicated.** The `:179` blink defers to two `!important` global guards; the absence r1 scored as a defect IS the architecture.
- **S-5 (r2) · `aria-expanded` is real** (`:12`) — bound to the ref the click mutates; half a disclosure contract, correctly wired.
- **S-A (both readers) · two-layer zero-cost gating.** Flag absent ⇒ `:154` skips all six registrations AND the interval; `:4` ⇒ zero DOM; with the flag absent the subsystem costs one boolean. Row 3 indicts the gate's QUALITY, not its architecture.
- **S-B (reader-LC, NEW this round; ✓A) · the collapsed panel is nearly free.** The collapsed template reads only `enabled`/`collapsed`/`frozen`/`copied` (`:4-20`); `gauges`/`events` sit behind `:22`'s `v-if`, so Vue never tracks them and the 500 ms tick does not re-render the closed overlay.
- **S-C (both readers) · `ORPHAN_POINTERS` (`:221-224`) encodes the fault SIGNATURE** — cross-correlating two independent input models to name stuck pids; the exact iOS pathology this repo's memory records as the tool's reason to exist. **Caveat (row 10)**: after Reset, the same mechanism becomes the false-alarm generator — the discipline is right, its state is not.
- **S-D (both readers) · bounded by construction, batch-correct.** `MAX_EVENTS = 80` enforced by excess-computing `splice` (`:74-76`) — holds even under batch pushes or a lowered cap.
- **S-E (r1 seat; ✓A `:256-264`) · complete, capture-flag-correct six-listener teardown + interval clear** — the mismatched-flag silent failure is absent on all six.
- **S-F (both readers) · defensive craft that is actually correct.** `t?.hasPointerCapture?.(e.pointerId) ?? false` (`:82`); `describeElement` cannot throw on any `EventTarget` (`:23-32`, one dead layer per row 37); per-pid `try/catch` around `releasePointerCapture` (`:135-139`); the H.W3 Lane B docblock (`:104-111`) asserts a boundary that is still true.
- **S-G (method) · `challenge-D-design-r2.md` remains the corpus's standard** — byte-pinned SHA receipt (still matching this seat's own hash), re-runnable probe, and the root cause proved HISTORICALLY (`c84504d3` named). Its three cure errors (K-11/K-12, the ladder-blind rung) do not diminish the finding work; they sharpen the law that cures are adjudicated separately.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

- **U-1 · D-N3 keyboard limb.** r2 pressed keys with the HEADER focused — the wrong element. Chromium 127+ ships keyboard-focusable scrollers and `.debug-scroll` qualifies (overflowing, no focusable descendants). Probe: Tab into the scrollport, PageDown. Wheel/drag limbs are structurally settled; iOS (no keyboard) unaffected; "unreachable by every modality" over-reaches by exactly one browser register until probed.
- **U-2 · every r2 live number** (occlusion 100%/97.5%/30.1%; scrollHeight 2829/243; rects 278×30.2, 83.3×27; AX dump + liveRegions 0; composited `rgb(16, 5.6, 17.2)`; tab ordinals 26–29/30; 398-byte payload; RTL computed values) — chromium-cell witness, re-measure before quoting as fact or asserting a delta.
- **U-3 · D-N10 flag-drop** — one unreproduced navigation; only the static half (row 35) is booked; must not be promoted without a reproduction.
- **U-4 · production A/B currency.** r2 cites `assets/index-DJ8Ije_5.css` live (2026-08-03); the local `origin/gh-pages` ref (`17c10267`, 2026-03-24) carries `index-DS-0HlXP.css` — either the tracking ref is stale or r2's filename cell is wrong. The SUBSTANCE (token+gate+surface deployed) is proved offline either way; the corollary stands: the deployed product predates the `demo/@` death and Glass 7, so every "production is correct" A/B in this corpus may mean "a March build is correct". One live manifest fetch settles it — rides X-W1's deploy-truth rows.
- **U-5 · iOS virtual-keyboard / `dvh` interaction** with `bottom: 8px` + `max-height: 35dvh` — the panel resizes as Safari chrome collapses; never tested; input to row 12's re-form.
- **U-6 · LEAK-1 gesture frequency** — proven by construction, unmeasured in real sessions; desktop-only until row 1 lands, phone-reachable after.
- **U-7 · D#3's type-size limb** (10 px extended reading) — owner taste, not a probe.
- **U-8 · should the seat exist at all** — owner ruling (r2 answers "not as gated"). Settled for the ruling's use: deletion touches ColorPicker.vue:103/:141/:175-176 + the `visual/` pair; the composable SURVIVES any UI removal (live consumers: ComponentSliders.vue:117 → useSliderTouchGates, SpectrumCanvas.vue:52).
- **U-9 · iOS fallback-copy efficacy** (reader-DU's rider): `ta.select()` on a zero-opacity fixed textarea is the documented iOS case wanting `setSelectionRange` — if true, on the LAN origin even the fallback's copy silently fails, upgrading row 7 from "lies on failure" to "always lies on the target platform". Device-cell probe; not repo-provable.

## INCREDULITY — corrections of the readers (and of the record they killed)

1. **Reader-DU's kill of the r1 census cell is VINDICATED** (37/31, programmatic recount) — the one place round 2 falsified the r1 apotheosis, executed here as K-17 and the supersession note.
2. **Reader-DU's `useClipboard` adopter list conflates the two producer primitives** — 3 `useClipboard` + 13 `writeClipboard` files (my census). Substance intact; labels corrected for the wave.
3. **Reader-DU's D-N3 cure rider mis-states its own advantage** — under the add-`auto` cure the gauge block still inherits `auto` (it lives inside the scrollport); the cure's true virtue is state-scoping, ruled above.
4. **Reader-LC's D-N3 prescription is killed by the inheritance LC itself proved** (K-18) — the mirror image of the r2 error it corrected.
5. **Reader-LC's FRZ-2 cure text cannot be typed where prescribed** (`:100` is outside the block that owns the Map); the mechanism is confirmed, the edit is a hoist.
6. **Both readers' z-ladder file citations are partial** — the 17 rungs live in BOTH `bridges.css` and `scheme-motion.css`; LC's "34" counts aliases. No substantive claim moved.
7. **Reader-LC re-books the sibling's ADJ-6 and half of DOC-1 without citing the rulings** — identities guarded here, not re-booked.
8. **Reader-DU's D#2a/D#2b kills are adjudicated as RESCOPES, not kills** — the bytes both readers proved are real; what dies is the *booking* (a 0px cure; a duplicate identity). Rows 31/32 preserve the facts at their honest altitude.

## Closing verdict

**ADJUDICATED: instrument-RED at HEAD `209f47c3` — 41 rows survive (2 BLOCKER: the orphaned `--z-debug` rung and the phone one-way trap it causes; 10 MAJOR incl. the production-shipping substring gate proved offline in the deployed bundle, the self-poisoning freeze-detector state machine FRZ-1/FRZ-2/LEAK-1 whose one recovery action manufactures permanent false alarms, the clipboard non-adoption whose primitive the mounting parent already imports, and the lying export that runs its fallback as the PRIMARY path on the LAN/phone origin the tool exists for; 18 MINOR; 11 INFO), every component cure routed to X-W8 (CC-081 overlay limb, G-11) under the SEQ-1 coupling law — the z-restore never lands alone; TEST-0 to X-W1; the canon rows to X-W10; 18 claims killed with written refutations, including three of the corpus's own cures (K-11, K-12, the ladder-blind rung), one reader's cure (K-18), and the superseded r1 record's census cell (K-17, recount 37/31); D#4 stays dead under M-28/M-29/M-31; D#2a/D#2b rescoped to their honest bookings (0px cure; already-ruled U-ledger identity); the seat's core disciplines — zero-cost gating, two-layer non-perturbation, bounded batch-correct recording, fault-signature encoding, capture-correct teardown — remain superlatives the r1 axes misread as defects. Dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave.**

— end of apotheosis r2. The four challenge files, the evidence-r2 tree, and both round-2 reader returns remain immutable beside this record per E-1/E-3; the superseded r1 apotheosis's surviving substance is carried above with provenance.
