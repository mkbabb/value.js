claude-fable-5

# fr-GalleryCard — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-24 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/gallery/GalleryCard.vue` (309 lines) + its consumer chain (GalleryView / GalleryInfiniteGrid / GalleryFeaturedCarousel / GalleryMarquee / GalleryCardModal) and the basis-display/colors seam.
**Substrate**: fourier HEAD `cd26c65` (2026-07-03) — re-pinned by this seat (`git log -1`), identical to all three challenge pins. **Dirty-tree disclosure**: 28 dirty rows repo-wide; **the subject file is CLEAN at HEAD**; 7 subject-adjacent files drifted by fourteen equal-length content-substitution lines (glass-ui renames only: `UnderlineTabs`→`SegmentedTabs variant="underline"` GalleryView:13/:223, `DialogContent variant`→`surface` :402 + modal:72, `glass-medium`→`glass-resting` GallerySearchBar:80, GalleryAdminBanner cosmetics). **All line anchors in this record are stable across the drift**; none of the fourteen lines is an adjudicated citation. Producer glass-ui working tree is **8.0.0**; every producer claim below was discharged with `git show v7.0.0:…` per BASELINE LAW.
**Corpus**: 52 accusation rows across three axes — challenge-D (design, 23 defects + 4 superlatives, opus-5[1m]), challenge-L (library, 13 defects + 2 INFO + 5 superlatives), challenge-C (consumption, 14 defects + 5 superlatives) — at `docs/tranches/V/megatranche/audit/fourier-components/GalleryCard/`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Both returns arrived through the orchestration channel; every load-bearing or disputed cell was **re-derived by this seat against the tree with its own instruments** before adoption.
**Method (L-18/M-23)**: both readers were attacked before either was adopted. The three reader-vs-reader verdict conflicts (R-1 marquee, R-2 tier paint, R-3 R3-12) were each ruled with fresh reads; every kill was re-derived (one kill-cell fragment was itself killed, R-6); every missed find was re-proven before banking; two shared cells died under this seat's own instruments (K-8's both-axis attribution, K-13's bundle-cost cell — the second against reader-LC's own prediction).
**Arbiter's own instruments this session (all static + node, zero browser probes — probe parsimony; product source read-only)**: full reads of GalleryCard.vue (309), GalleryView.vue (443), GalleryMarquee.vue (135), GalleryFeaturedCarousel.vue (92), GalleryInfiniteGrid.vue (55); targeted reads of GalleryCardModal.vue (tier badge :84-92, `VIZ_COLORS.fourier` live read, emits), basis-display.ts (whole, 7 lines), colors.ts:15-100 (all four `cssVarToHex` arms + both `#888888` returns + `resolveVizColors`), stores/gallery.ts:130-200 (the `like()` body **with** its :189-192 comment; the setTier slug-identity comment), App.vue:5-20, types.ts:94-98/:205-240, api/routers/gallery.py:28-40, lib/api.ts:288-296, style.css:36-49 (18px root **below** 768px) + :135-143 (`.gallery-card:focus-visible`); reka `CheckboxRoot.js:118-160` (the `onKeydown` Enter no-op at **:122**, no stopPropagation); lucide `Icon.js:41` (`aria-hidden:"true"` spread absent slot/a11y prop) + defaultAttributes.js; e2e/gallery.spec.ts census (139 lines, **0** keyboard/press/Enter/Space/gallery-card/marquee hits); installed glass-ui 4.0.0 dist: scheme-motion ease enumeration (no `-apple-spring`) + z register :333-341 + bridges :235-242, a11y-overrides PRM block :6-17 / coarse `[data-size="icon"]` floor :115-122 / `@utility touch-hit-area` :151-170, base.css `.tap-squish` :258-279 (six-leg transition · `scale` longhand press · PRM carve + the verbatim CLOBBER comment), button chunk (0 keydown; base `btn-pill tap-squish focus-ring…`; ghost arm's two `aria-pressed:` legs; `icon: h-(--control-h-md)`; `data-size` reflected), badge chunk (`outline: "text-foreground"`; base `rounded-badge border`), cn radius regex (verbatim), scale-paper (`--scale-hover-dock:1.1` · `--scale-press:0.96` · `--scale-press-btn`→0.97), color tokens (`--card`/`--tier-*`/`--like`, both arms); producer at the tag: `git show v7.0.0:` Button.vue:15-40 ✓, shadow.css (`-3px 3px…var(--cartoon-ink-lead)` + `--glass-material-rim`) ✓, badge/index.ts (base retires `border`; `outline:"badge-atom--outline text-foreground"`) ✓, glass-atom.css:109-123 (rim on every badge) ✓, package.json exports = **74 keys, `./checkbox` ✗ `./metric-badge` ✗** (node), README `ease-apple-spring` = **0 at v7.0.0 and at 8.0.0-HEAD, present at v4.0.0:211**; repo-scope: `ease-apple-spring` = **9 lines / 4 files = 5 declaration sites** (VV:426 · GallerySearchBar:188-189 · GalleryCard:196/:295 · AppHeader:280 — reconciling M-α6's "five"), `PathPreview` = **1 hit tree-wide** / 69 lines, `entry.title` readers = **0**, `GalleryTier` inline re-declarations = **9 lines / 8 files** (+2 widened `"all"|…` forms), `toggle-select` handlers = grid+view only, `GalleryMarquee` references = import + **one** mount site, api decorator census = **44** (`@gallery_router.get("/cursor")` at gallery.py:37 among them), zero like verbs; built `dist/assets/GalleryView-DkLhexRv.css` read whole (11,629 B, scope `data-v-4970e46f` matches the SFC rule-for-rule): `[data-selected]` @3791 **before** `[data-tier=featured]` @4098 / `[data-tier=saved]` @4396, `.admin-overlay-btn` compiled `transition-property:transform,translate,scale,rotate` + `--tw-duration:.15s` + `border-radius:3.40282e38px`, `.like-btn:hover,.like-btn.liked{color:var(--like);background:0 0}`, the whole `.marquee-*` block + 2 keyframes + PRM guard **shipping**, `.path-preview` in **no** built sheet; V8 run `new Date("2026-08-06 12:34:56.789012+00:00")` → parses with correct offset; **own OKLCH→OKLab→LMS³→sRGB + CSS-Color-4 gamut-map (chroma bisection, ΔEOK≤0.02) + WCAG instrument**, validated by reproducing four thrice-derived figures (tier light **1.56/2.77**, dark **9.69/6.54**) before producing the disputed ones (`--like` light **3.60** / dark **4.88**, `#ea4747`/`#ef6c6c`); registry sweeps of all banked fr-*.md for every fold target and for `likedHashes|toggle-select|GalleryMarquee|GalleryTier` (only FR-AFP-59's adjacent widening row exists); intake rows R3-12/R5-7/R6-8 read verbatim at `audit/codex-provenance/intakes/lane-fourier-r3-r6.md`.

## Scoreboard

**39 rows ADJUDICATED-CONFIRMED (1 BLOCKER · 14 MAJOR · 15 MINOR · 9 INFO), of which 8 FOLD BY REFERENCE to banked registry rows · 13 KILLS in the killed register (3 minted or extended by this seat) · 12 missed finds banked (8 DU · 5 LC · 1 shared) · 8 superlatives with provenance.**

Blocker-weight survivor: **B-2/D-2/C-2** — the root's unguarded `@keydown.enter.prevent`/`@keydown.space.prevent` cancels the activation of all five focusable descendants and opens the modal instead; in admin mode a keyboard user can not like, select, feature, save or delete anything, and the e2e surface has zero keyboard coverage.

Headline structural ruling: **GalleryMarquee never mounts** — its sole mount site (GalleryView:269) sits under `v-if="!gallery.entries.length && !gallery.loading"` (:266) while its own guards require `featuredEntries.length >= 4` (:270, and Marquee:27), and `featuredEntries` is a filter of `gallery.entries` (:64-66). The two conditions are unsatisfiable together. This kills the corpus's aria-hidden-focus MAJOR, voids two reader cells, and banks a new MAJOR (134 lines of unreachable SFC whose CSS provably ships).

Status law: dispositions are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25); nothing here stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **L·M-3 / L·I-2** — aria-hidden marquee duplicate | CONFIRMED M-3 at MAJOR (live-vs-latent left to SS-13) | KILLED both live arms: the marquee is unreachable | **LC wins — KILLED (K-9), and LC's missed find banked (G-LC1, MAJOR).** | My GalleryView read: mount site :269 under `:266 !gallery.entries.length` ∧ own guard `:270 featuredEntries.length >= 4` with `featuredEntries = entries.filter(tier==="featured")` (:64-66) — `entries.length===0 ⟹ featuredEntries.length===0 ⟹ false`, always; `grep GalleryMarquee web/src` → exactly the import (:26) and this element. DU read the child's guard (Marquee:27) but not the parent's. Residue survives at MINOR (hardcoded `role`/`tabindex`, no `inert`/`focusable` opt-out — GalleryCard:72-73, props :19-24). |
| R-2 | **C·C-5 / L·D-1** — the tier predicate's visible artifact | Killed the paint ("nothing paints") → MINOR | MAJOR "the empty box is the default rendering for the whole gallery" | **DU wins — MINOR (K-4).** LC's severity dissent preserved. | GalleryCard:149's div carries `flex items-center justify-center w-6 h-6 rounded-full` — no background, no border, no class hook; my whole-sheet read: every `[data-tier]` rule is root-scoped (`.gallery-card[…]`) or the modal's (`.modal-tier-badge[…][data-v-9a41965d]`) — nothing can reach it. What ships is a fully transparent 24px box at the end of a `justify-between` row; the only physical delta is ≤4px of footer height against the like row's ~20px. The predicate defect is real (`tier?: GalleryTier`, zero server writers outside three admin `$set`s); the *visible circle* is not. The modal's copy is worse and paints: `v-else` Bookmark + `{{ entry.tier }}` interpolating empty inside a painted `bg-background/70` pill (modal :84-92, my read) — rider on the modal's record. |
| R-3 | **R3-12 attribution** (D·D-22 + C·C-9) | KILLED the attribution; proposed the card/modal byte-clone (M-10) as the likely mechanism | CONFIRMED: "this is the mechanism behind R3-12 … both D and C cite it correctly" | **BOTH CORRECTED — attribution KILLED for both axes (K-8), LC's confirmation cell OVERRULED, DU's replacement mechanism corrected.** | R3-12 verbatim (lane-fourier-r3-r6.md, §1): `len(openFamilies)=35`, `len(set(canonical(row)))=28`, the 7 duplicates being "both Paper-search callsites, `GalleryCard` basisLabels, `MorphPhaseConfig` easingNames" — a **deriver-side canonicalization collapse**, not a runtime key collision and not a byte-clone. The sibling adjudicated corpus already carries the true mechanism: GalleryFeaturedCarousel challenge-L:185 — the basisLabels loop registers from **two live component callsites** (carousel :32 + grid :30), so a callsite-keyed deriver books one source loop twice (note "both Paper-search callsites" is the same shape). The `:key="b.label"` collision defect itself stands on its own legs at MINOR. |
| R-4 | **D-1/B-1 severity at the fold** | MAJOR ("MAJOR is the ceiling") | MINOR ("the banked severity governs") | **Fold at the banked MINOR (fr-BasisCanvas M-α6 → F.W1); DU's MAJOR recorded as a site-severity input for the F.W1 agglomeration.** | The identity is banked; the banked row's own dissent (fr-BasisCanvas.md:157) disclaims the per-site consequence — which is this record's additive part: at GalleryCard the ONE `transition` shorthand carries all three animated properties, so IACVT kills hover-lift, press-squish, tier-glow and selection-ring transitions *and* `like-bounce` (`animation-name:none`; the PRM guard :304-308 vacuous). My census: 5 declaration sites / 4 files (reconciling M-α6's "five"). Both original axes' BLOCKER is killed (K-6): nothing functional, informational or reachable changes. |
| R-5 | **Severity conflicts** (D-6/L·D-7: DU MAJOR / LC MINOR · C-7: DU MINOR / LC MAJOR · like-name: DU MAJOR / LC MINOR) | — | — | **D-6 MAJOR · C-7 MINOR · like-name MAJOR** — each losing reader's dissent preserved. | D-6: zero failure posture on the card's sole content element, over a bare URL builder (api.ts:292-294, my read) with no upstream owner — the fr-BasisCanvas error-posture precedent (M-α1 class) puts posture absence on a flagship surface at MAJOR; incidence stays data-dependent (SS-13). C-7: every payload is `entry.slug` consistently end-to-end (`entrySlug` = slug, store comment my read) — the §1-precept vocabulary violation plus an unbranded-Set trap, prevention-class, no live break → MINOR. Like-name: the sole unnamed control in the file, and the only one a non-admin can operate; AT hears a bare mutable number as the name → MAJOR. |
| R-6 | **Accessible-name evidence mechanism** + DU's own kill-cell arithmetic | defaultAttributes has no `aria-hidden` ⇒ contributes nothing to the name; D-17 correction cites the 220px `.marquee-item` as "the sharper consumer" | `Icon.js` spreads `aria-hidden:"true"` when no slot/a11y prop | **LC's mechanism adopted; DU's conclusion intact; DU's marquee arithmetic VOIDED by this seat.** | My read of lucide `Icon.js:41`: `...!slots.default && !hasA11yProp(props) && { "aria-hidden": "true" }` — the glyph is hidden by construction, which is the exact operative fact (DU's cell was true of defaultAttributes but not the mechanism). And DU's corrected D-17 arithmetic ("71.6% of the hard 220px `.marquee-item`") describes a subtree R-1 proves can never mount — the surviving coarse-pointer figure is the grid's: 3 × 49.5px + 2 × 4.5px = **157.5px of a 252px track (62.5%)** at the 18px sub-768px root (style.css:41/:45-47, my read). |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · disposition → route.** Full proof cells live in the immutable challenge files; anchors re-verified by this seat unless marked (adopted). Missed finds carry G-DU/G-LC ids.

### BLOCKER

- **B-2 / D-2 / C-2 — BLOCKER.** `@keydown.enter.prevent` / `@keydown.space.prevent` on the `role="button"` root (GalleryCard:78-79) carry no `.self` and no target guard over five focusable descendants (Checkbox :90-95, like :135-145, admin ×3 :157-183). Keydown bubbles; the root emits `click` (modal opens) and `preventDefault()` cancels the descendant `<button>`'s activation — activation is a default action run after dispatch only if the canceled flag is unset. Re-proven by this seat: glass Button chunk **0** keydown handlers; reka CheckboxRoot.js:**122** installs only an Enter no-op via `withKeys(withModifiers(()=>{},["prevent"]),["enter"])` with **no stopPropagation**; Vue's `.space` resolves. The pointer path is deliberately sealed three times (`@click.stop` :88/:141/:156 — S-2), which is what makes the cure two `.self` tokens. Untested: e2e gallery spec = 6 tests, **zero** keyboard hits (my census). C's Enter-double-activation arm and its reka-negative cell are killed (K-2, K-3); the Space arm's per-engine keyup gate is the one UNPROVEN residue. Not banked (registry keydown rows are different mechanisms). **ADJUDICATED → F.W3/W4.**

### MAJOR

- **C-4 — MAJOR here · FOLDS BY REFERENCE to fr-BasisCanvas D-1/BC-1/C-1 (banked BLOCKER → F.W1).** `cssVarToHex` (colors.ts:22-54, my read: four arms, `#888888` at :26 and :53) cannot read the pin's `oklch()`/`light-dark()` `--viz-*` forms, so `resolveVizColors()` writes the sentinel into fourier/chebyshev/legendre after boot; only `--viz-amber` survives via fourier's own `hsl()` override. C's §8 interlock — **curing the snapshot alone turns every basis pill grey; C-3 and C-4 are one repair** — is the sharpest statement in the corpus and rides as a rider on the banked row. Do NOT re-book.
- **D-4 / L·M-1 / C-3 — MAJOR here · FOLDS BY REFERENCE to fr-BasisCanvas D-2/BC-5/C-3 (banked MAJOR → F.W1).** basis-display.ts:3-7 (read whole) copies three `reactive()` reads into a plain literal at module eval; `--pill-c` (:121) can never track a theme flip. Riders this record adds: the fr-App R-1 entry-route dependency is binding and neither axis carried it; the component-specific consequence is the pill contrast **at `--card`** (light 4.17/3.97/4.40 · dark 2.55/2.67/2.42 — D's table, reproduced to 2dp by reader-DU's instrument, distinct from fr-BasisSelector B-3's `--neutral-0` figures). D-4's prescribed cure is killed (K-5). Do NOT re-book.
- **D-3 — MAJOR, SCOPE-CORRECTED.** All three of `[data-selected]` (:221), `[data-tier="featured"]` (:228), `[data-tier="saved"]` (:233) compute to (0,3,0) with the scope attr; source order decides and tier is later — a selected tier-bearing card is pixel-identical to an unselected one, while the comment :219-220 asserts the opposite. **Built-sheet receipt (this seat)**: `GalleryView-DkLhexRv.css` selected @3791 < featured @4098 < saved @4396. Scope correction ratified by my GalleryView read: the split :64-70 sends featured entries only to the carousel, where selection is **unwired** (G-LC2), so the invisibility is *observable on saved-tier cards in the grid only* — D's "fails precisely on the cards an admin batch-operates" is half-refuted, half-superseded by the sharper G-LC2. Only the D axis found this. **ADJUDICATED → F.W3/W4.**
- **D-5 — MAJOR.** The footer Crown/Bookmark glyph is the only tier channel and fails WCAG 1.4.11 in light: `--tier-featured` **1.56:1**, `--tier-saved` **2.77:1** against `--card` — reproduced exactly by this seat's own gamut-mapped instrument (dark passes 9.69/6.54); no redundant text/label/title (:149-152). Same light-arm-authored-dark class as banked fr-AdminAuditLog AA-3 (different tokens, different consumer — cross-ref, not a fold). Constructive cure seat = G-DU8's missing plate. **ADJUDICATED → F.W3/W4**, with a glass-BH relay rider for the token family (AA-3 precedent).
- **D-8 — MAJOR here · FOLDS BY REFERENCE to fr-AdminAuditLog AA-2 (census-extension row → F.W1).** The v7.0.0 Button re-cut (`variant`→`emphasis`, `size="icon"`→`iconOnly`) breaks this file's 4 callsites through 8 attributes; verified at the tag by this seat (`git show v7.0.0:…Button.vue:15-40` — no alias, no shim). D's repo-scale 96 `variant=` / 77 `size=` / 35 files reproduced by reader-DU's independent parse (twice-derived; adopted) and rides AA-2 as a rider. Export-map note: at the tag the map is **74 keys** (my node run) — AA-2's "66" and FR-AUL's "67" were live-tree measures at drift; the tag number governs. Do NOT re-book.
- **L·M-2 — MAJOR.** `likedHashes: Set<string>` passed whole (:22/:34) and replaced by identity per like (GalleryView:45/:127-129) ⇒ one like re-renders the entire unbounded grid — while the adjacent line passes the derived scalar (`GalleryInfiniteGrid:35` vs `:36`, my read) and the modal gets the derived boolean too (`GalleryView:393`, LC's sharpening, my read): the parent speaks the right idiom to two of three consumers. Masks the timeAgo staleness (cure together). Not banked (my registry grep → 0). Only LC's axis found it. **ADJUDICATED → F.W3/W4.**
- **C-1-as-corrected — MAJOR (BLOCKER framing killed, K-1).** There is no like write verb anywhere (my 44-decorator census; `grep like api.ts` → 0) while the server carries the full substrate (`likes:int=0`, sort key, `(visibility,likes,-id)` index — adopted, spot-checked); the store's `like()` hardcodes `liked=true` with a monotonic unguarded local bump (gallery.ts:193-197) **under a comment documenting the stopgap** (:189-192, my read — the cell C excised); the card advertises `aria-pressed` for a toggle nothing can honour, and the shipped "likes" sort orders a column only the legacy migration writes. **ADJUDICATED → F.W5–W8** (the verb/contract decision), display arm (aria-pressed + re-click guard) **→ F.W3/W4**.
- **D-6 / L·D-7 — MAJOR (R-5).** Zero error/loading posture on the sole content element: no `@error`/`@load`/placeholder/`decoding`/intrinsic size over a bare URL builder to a separately-lifecycled asset FK; per-card image failure has no owner anywhere in the chain. LC's MINOR dissent preserved; incidence → SS-13. **ADJUDICATED → F.W3/W4.**
- **D-7 / C·I-2 — MAJOR.** The entity's human identity is write-only: `title`/`description`/`tags`/`n_harmonics`/`fork_count` render nowhere (`entry.title` readers = **0**, my grep); the card's label, `alt` and `aria-label` all spend the machine FK; client sort exposes 3 of the server's 5 keys (`popular`, `most-forked` unreachable). C·I-3's transposition (operations with no client are invisible to client↔operation joins — R6-8's empty-leaf converse) rides as a census note. **ADJUDICATED → F.W5–W8** (CRUD-union seam), render arm **→ F.W3/W4**.
- **G-LC1 — MAJOR (missed find, reader-LC; re-proven by this seat — see R-1).** `GalleryMarquee.vue` is unreachable dead code: sole mount site under mutually-exclusive guards (GalleryView:266 ∧ :270, via :64-66), with the authoring comment :259-264 reasoning about `gallery.entries` while binding `featuredEntries` — self-refuting. The dead subtree ships: my built-sheet read finds the whole `.marquee-*` block, both keyframes and its PRM guard in the GalleryView chunk. Consequences: two of GalleryCard's four registered callsites can never instantiate (the census-denominator note → SS-3/SS-4), and the marquee's PRM guard inflates the census's PRM count. **ADJUDICATED → F.W3/W4** (delete or re-point — an authored empty-state design decision, not a mechanical prune).
- **G-LC2 — MAJOR (missed find, reader-LC; re-proven).** Admin multi-select is inert at 2 of 3 consumers: the carousel declares no `toggle-select` and binds no `selected` (GalleryFeaturedCarousel:12-17/:32-40, my read — the controlled Checkbox is pinned false and emits into the void), and the grid receives only `nonFeaturedEntries` (:68-70/:291) — so **no featured entry can ever be selected and the batch toolbar's `unfeature` verb (:330) can never reach a featured entry** (it can only no-op non-featured slugs); the only live unfeature path is the per-card Crown toggle. The missing half of the A.W5.c wave the card's own comment :82-84 announces. Not banked (my grep). **ADJUDICATED → F.W3/W4.**
- **G-DU1 (≡ LC's cognate) — MAJOR (missed find, both readers; ruled R-5/R-6).** The like button has no accessible name: children are an `aria-hidden` lucide Heart (Icon.js:41, my read) and `{{ entry.likes }}` — AT announces the bare count as the name of a toggle ("42, toggle button, not pressed"); the views stat likewise. The asymmetry proves oversight: `title` on all three admin buttons, `aria-label` on Checkbox and root. The admin buttons' native-`title`-as-name folds to banked **fr-EquationModeToggle FR-EMT-10** (F.W3); the unnamed-control row is distinct and unbanked. **ADJUDICATED → F.W3/W4.**
- **G-DU2 — MAJOR (missed find, reader-DU; re-proven).** The ARIA toggle contract is inverted: `aria-pressed` sits on the one control that cannot toggle (like — no un-like path exists), while the two genuine toggles (:162 featured, :171 saved — `tier === X ? 'normal' : X`) carry no `aria-pressed`, no pressed paint, their state legible only from the 1.56:1 glyph and border. Symmetric, free cure: move the attribute. **ADJUDICATED → F.W3/W4.**
- **G-DU3 — MAJOR (missed find, reader-DU; re-proven at the byte level).** `.admin-overlay-btn`'s scoped `@apply transition-transform duration-150` (:258) clobbers the glass Button's six-leg `.tap-squish` transition: built sheet ships `transition-property:transform,translate,scale,rotate` + `--tw-duration:.15s` (my read @~5217), and scoped SFC styles are unlayered so they outrank the `@layer components` base — the glass variant's hover cross-fade becomes instant and the press `scale` loses its spring for a 150ms bezier. glass-ui's own base.css:243-257 comment names this exact failure mode ("would otherwise CLOBBER `.btn-pill`'s surface legs… the live-readback RED" — read verbatim by this seat). All three axes read this style block; none saw it. **ADJUDICATED → F.W3/W4** (one cure with G-DU4: drop the transform legs, ride the house tokens).

### MINOR

- **D-1 / L·B-1 — MINOR at the banked severity · FOLDS BY REFERENCE to fr-BasisCanvas M-α6 (→ F.W1)** (R-4). `--ease-apple-spring` exists nowhere (re-swept: 0 definitions in web/src + node_modules CSS; producer ships `--ease-apple` + `--ease-spring*`); IACVT kills the whole `transition` shorthand (all three legs, including the two valid ones) and the `animation` shorthand (`like-bounce` unreachable, PRM guard vacuous). Additive per-site consequence + the 5-site/4-file census are this record's contribution; BLOCKER killed (K-6); the README relay cell killed (K-10 — discharged upstream). Do NOT re-book.
- **L·D-1 / C·C-5-as-rescoped — MINOR** (R-2). `v-if="entry.tier !== 'normal'"` over optional `tier?` is true for `undefined` — the dominant uncurated population (no server default; three admin `$set` writers only) — rendering an invisible 24px box; correct guard `tier === 'featured' || tier === 'saved'`. The painted-artifact claim is killed (K-4); the modal's painted-empty-pill copy is that record's rider. **→ F.W3/W4.**
- **L·D-3 / D-11 / C-6+C-10 — MINOR.** Two dead imports (:9-10). The `VIZ_COLORS` half **FOLDS to fr-App K-13 / MG-θ's gate row (→ F.W0)**. The `PathPreview` half is new: 1 reference tree-wide = the dead import ⇒ a 69-line component orphaned by transitivity — the fossil of an abandoned vector-preview integration (the one fourier-specific thing the card could show, cf. D-7). The bundle-cost cell is killed (K-13): `.path-preview` ships in **no** built sheet. **→ F.W3/W4** (delete or complete — a design decision).
- **L·M-4 / D-13 / C-8(a) + L·D-2 / C-12 — MINOR · FOLDS BY REFERENCE, nothing new to book.** The `timeAgo` family (5 copies, 2 dialects, render-frozen, no NaN guard, non-ISO wire) is banked at **fr-AdminUserList FR-AUL-17** + **fr-AdminFlaggedPanel FR-AFP-32** (both name GalleryCard:53-61; → F.W3; serializer → F.W5–W8; JSC → SS-13); the `basisLabels` verbatim clone + 7-site `startsWith("fourier")` normaliser at **fr-BasisSelector M-10** (→ F.W3/W4). The never-ticks arm (masked by L·M-2 — cure together) is already inside FR-AFP-32's "render-frozen" limb.
- **L·D-4 — MINOR.** The inner `:data-tier` binding (:149) is dead: my whole-sheet read confirms every `[data-tier]` selector is root- or modal-scoped; the modal has a class hook for its inner badge and GalleryCard does not. Only the L axis found it. **→ F.W3/W4.**
- **L·D-5 / D-22 / C-9-as-corrected — MINOR** (R-3). `:key="b.label"` is a derived, collision-capable key (`:39` folds all `fourier*`; server accepts any 16 strings — `active_bases: list[str]`, zero validators, adopted+spot-read); the R3-12 authority is stripped from both axes. Cure (`:key` on raw `b`) rides M-10's normaliser unit. **→ F.W3/W4.**
- **D-9 — MINOR.** The scoped PRM block :304-308 is doubly redundant (dead animation per D-1; the global `*:not([data-allow-motion])` neutraliser owns it — a11y-overrides:6-17, my read), and counting it in the census PRM row over-states coverage (as does the dead marquee's guard, G-LC1). D's "correct PRM outcome" reasoning is right everywhere except G-DU4's one site. **→ F.W3/W4.**
- **G-DU4 — MINOR (missed find, reader-DU; mechanics re-proven).** The press transforms compound instead of overriding: base presses via the `scale` **longhand** (`.tap-squish:active { scale: var(--scale-press) }` = 0.96 / cva `active:scale-(--scale-press-btn)` = 0.97 — my reads) while the consumer presses via `transform: scale(.95)` (:263-265, built sheet confirms) — individual transforms compose (`translate∘rotate∘scale∘transform`), net ≈ **0.912**, and `cn()` buckets classes, not properties, so they can never dedupe. Rider: base.css's PRM carve resets only `scale` ("Under PRM I retire the press transform entirely" — read verbatim), so the consumer's `transform` leg survives PRM as an instant squish — the one site where D-9 is too generous. **→ F.W3/W4** (with G-DU3).
- **G-DU5 — MINOR (missed find, reader-DU; reproduced by this seat's own instrument).** `--like` fails 1.4.3 AA light at **3.60:1** (`#ea4747` over `--card`; dark 4.88 passes) at 0.875rem normal text (:283/:286-290) — and it is also the *hover* colour of the un-liked counter whose resting ink measures 5.12:1: interacting lowers contrast. The only card ink no axis measured. **→ F.W3/W4** (rides D-5's token-family relay rider).
- **D-14 + D-17-as-corrected — MINOR · mechanics FOLD to fr-AdminUserList FR-AUL-19.** The Checkbox `class="h-4 w-4"` is a byte-identical no-op restating the base (chunk read); both attractive a11y readings were self-falsified by the axis and verify at the pin (`touch-hit-area` halo; `[data-size="icon"]` coarse floor). Corrected arithmetic (R-6): 157.5px of a 252px grid track (62.5%) at the 18px root; the marquee arm voided. **→ F.W3/W4** rider.
- **D-10 — MINOR (unruled by both readers; seat-confirmed from its own SFC read).** Seven unreconciled gap values (incl. the `gap-[0.2rem]` tell at :120) and three vertical rhythms in a 14rem-track tile; horizontal `px-3` is the one disciplined axis. **→ F.W3/W4.**
- **D-12 / D-15 / D-16 / D-18 / D-23 — MINOR (cluster, all re-anchored in this seat's SFC read).** One type size + one ink for 100% of card prose (the ink itself passes — 5.12/5.68, adopted thrice-derived); the pill row renders unconditionally with `py-0.5` and drops unknown bases silently; `z-5` (:87/:156) bypasses the named z register (tokens at scheme-motion:333-341, bridged :235-242 — D's `components.css` provenance corrected, DU's kill ratified); alt/aria spend the FK (composes with D-7); the thumbnail is permanently `opacity-85` and `object-cover`-cropped in a hard 4:3 frame over a plate designed as an underlay. **→ F.W3/W4.**
- **G-LC3 — MINOR (missed find, reader-LC; census re-run by this seat).** `GalleryTier` is exported once (types.ts:96) and re-declared inline **9 times across 8 files** (my grep: card :29, modal :28, grid :19, carousel :15, marquee :15, GalleryView :132, GallerySearchBar :17 + :24 widened, store :33 widened) — the payload type is hand-retyped across the whole emit chain; eight coordinated edits to add a tier, seven of which `vue-tsc` will not demand. Adjacent-but-distinct from banked FR-AFP-59/31 (widening); repair rides the M-10-style single-home unit. **→ F.W3/W4.**
- **G-DU8 — MINOR (missed find, reader-DU; constructive half of K-4).** The tier chip is markup for a chip that never paints — `rounded-full` on a surface-less div is dead CSS — and it is the natural home for the plate that cures D-5: the file already ships the recipe two elements away (`.basis-tint`'s 12% self-tinted plate). Not a stray circle; a missing one. **→ F.W3/W4** (one cure with D-5).
- **L·M-3-residue — MINOR** (R-1). Hardcoded `role="button"` + `tabindex="0"` with no `focusable`/`inert` opt-out prop; the only escape is undocumented single-root fallthrough, used nowhere. A contract observation once the live arm died. **→ F.W3/W4.**

### INFO

- **D-19a/b — INFO.** Two load-bearing comment claims are false at the pin (`size="icon"` is token-driven, not `h-10 w-10`; badge `outline` ships `text-foreground` only — both chunk-read by this seat). Any wave touching the file corrects them. **→ F.W3/W4** rider.
- **D-20 — INFO.** `rounded-full` (:120) does not dedupe `rounded-badge` through the pin's `cn()` (regex read verbatim; built sheet ships `border-radius:3.40282e38px` alongside); inert (both compute a pill), fixed at 7.0.0's class-names. **→ F.W1** rider.
- **D-21 — INFO (uplift, silent visual).** `--shadow-cartoon`/`-hover` flip sign at v7.0.0 (`3px 3px` → `-3px 3px var(--cartoon-ink-lead)` — both sides `git show`-verified by this seat): every card stamp mirrors with no source change and no typecheck signal. Wants the F.W1 visual-regression checkpoint. **→ F.W1.**
- **L·I-1 / C·I-1 — INFO (NOT a fourier defect — both axes' restraint ratified).** The root-barrel `Checkbox` import is forced: no `./checkbox` subpath at 4.0.0 (80 keys) **nor at v7.0.0 (74 keys, my node run — `./metric-badge` also gone, corroborating the census break surface)**. Glass-BH relay item per standing law; survives the uplift. **→ F.W1** epistemic carry + relay.
- **L·D-6 — INFO · FOLDS to fr-BasisSelector i-3 (→ F.W0 rider).** `Record<string,…>` + `.filter(Boolean) as` cast; `noUncheckedIndexedAccess` absent is the named gate flag. Do NOT re-book.
- **G-DU6 — INFO (missed find, reader-DU; producer side verified at the tag by this seat).** At v7.0.0 the Badge's edge changes mechanism: base retires the `border` keyword; `outline` becomes `badge-atom--outline` (box-shadow `--glass-material-rim` + class-level border), and `.badge-atom` adds the rim to **every** badge (glass-atom.css:109-123, `git show`) — so `.basis-tint`'s border-colour projection rides a changed mechanism and each pill silently gains a 0.5px rim. Narrows D·S-2's "survives the re-cut unchanged" to the prop level. **→ F.W1** (uplift visual note).
- **G-DU7 — INFO (missed find, reader-DU; re-proven; narrows a banked cell).** The ghost arm's `aria-pressed:bg-foreground/10 aria-pressed:text-foreground` legs (chunk read) are dead at this callsite: the unlayered scoped `.like-btn:hover,.like-btn.liked{color:var(--like);background:0 0}` (built sheet, my read) outranks them by specificity (0,3,0 with the scope attr vs 0,2,0), and `.liked` is co-present with `aria-pressed` by construction. Banked **fr-CanvasOverlayButton** cites GalleryCard:135-143 as riding the upstream paint — true that no `[aria-pressed]`-keyed local rule exists, but the class-keyed rule overrides both legs, so the upstream paint does not reach this control either; pressed state is ink-alone at G-DU5's 3.60:1. Registry-cell amendment note. **→ F.W3/W4** rider.
- **G-LC5 — INFO (missed find, reader-LC; adopted on Vue-runtime grounds + this seat's anchor reads).** Declaring `click: []` (:27) shadows the native event — a consumer's `@click` routes to the emit and never reaches the DOM node (`.stop`/`.capture` meaningless), and the payload-less emit forces every owner to close over the loop variable (grid :37, carousel :36 — my reads). Composed with the hardcoded tabindex there is no escape hatch on either path. **→ F.W3/W4** rider.
- **L·I-2-as-corrected / R5-7 — INFO · NO-WAVE-OWNER (SS-3/SS-4 census-methodology input).** The R5-7 class (callsite-keyed derivers blind to native loops; blind to per-entry fan-out in native wrappers) is genuine, but LC's marquee illustration is drawn from an unreachable subtree (K-9) — which is the *sharper* indictment: a callsite-keyed deriver scores two callsites as live that can never render. Any GalleryCard instance denominator must count carousel/grid native wrappers and exclude the dead marquee pair.

## Killed-claims register

- **K-1 · C-1's BLOCKER framing + two evidence cells — KILLED** (both readers; ratified + extended by this seat). (a) "`api/routers/gallery.py` has no routes at all" — false: `@gallery_router.get("/cursor")` at :37 (my read); C's `@router\.` grep pattern is structurally blind to every prefixed router (my census: 44 decorators, none spelled `@router.`). (b) C quoted `like()` as :189-198 and excised the :189-192 comment documenting the deliberate stopgap — a documented decision half-fires C's own falsifier. The substance survives at MAJOR.
- **K-2 · C-2's "Enter double-activation" arm — KILLED** (both readers; ratified). Enter activation is a default action gated on the canceled flag after full dispatch; the ancestor's `preventDefault()` means no synthetic click is ever dispatched — open-instead-of-act, not act-and-open. C labeled the wrong arm "fully static".
- **K-3 · C-2's reka cell ("no keydown handler at all — grepped: zero occurrences") — KILLED** (both readers; ratified at CheckboxRoot.js:122, my read). A case-sensitivity artifact (`onKeydown`); L's cell was line-exact. The verdict survives on L's mechanism; the stated proof does not.
- **K-4 · C-5/L·D-1's visible artifact ("a bare circle appears" / "an empty 24px box in every footer") — KILLED** (reader-DU; ratified with this seat's source + whole-built-sheet reads). No background, no border, no reachable rule: nothing paints; ≤4px of footer-height participation is the entire physical delta. MAJOR-by-incidence dies with it (R-2).
- **K-5 · D-4's prescribed cure ("make `color` a getter … either restores reactivity") — KILLED** (both readers; ratified). Both prescriptions restore reactivity to a resolver that returns `#888888` for every `oklch()` token — the cure alone makes the surface worse. C-4 §8 is the correct statement; the banked registry routes them as one repair (fr-BasisCanvas D-1 + D-2 → F.W1).
- **K-6 · D-1/B-1's BLOCKER severity — KILLED** (both readers; ratified). Function, layout, information and reachability all intact; hover/press/glow/ring still apply instantly; one 300ms bounce never plays. The fleet banked this token at MINOR (M-α6). Two axes independently inflating a banked MINOR to BLOCKER is a corpus calibration signal, recorded in DISSENT.
- **K-7 · C-8(b)'s "renders `NaNd ago`" — KILLED** (both readers; ratified by this seat's own V8 run: the space form parses with correct offset). The wire-format fact is real and already banked (FR-AFP-32, which also banked this same kill as K4); the rendered consequence is not demonstrated on the project's engine.
- **K-8 · D-22 + C-9's R3-12 attribution — KILLED FOR BOTH AXES; both readers corrected (this seat's own ruling, R-3).** R3-12 is a deriver-side canonicalization collapse whose GalleryCard duplicate is attributable to the two registered component callsites (per the intake's own duplicate set and the GalleryFeaturedCarousel corpus), not to the label key (C's "supplies the mechanism" — overreach), not to the card/modal byte-clone (DU's replacement — wrong object), and LC's "both cite it correctly" is overruled. The key-collision defect survives on its own evidence.
- **K-9 · L·M-3's live arm + L·I-2's marquee illustration — KILLED** (reader-LC's kill RATIFIED against reader-DU's MAJOR confirmation; this seat's own GalleryView read is the decider, R-1). The falsifier L declared dead ("show that the duplicate track never mounts") fires: GalleryView:266 ∧ :270 are unsatisfiable together. No aria-hidden-focus violation, no duplicated tab stops, ever.
- **K-10 · D-1's glass-BH relay cell (README advertises the token) — KILLED as a relay item** (reader-LC; ratified by this seat's `git show`: present at v4.0.0:211, absent at v7.0.0 and at 8.0.0-HEAD). The producer already deleted the fiction; the relay would arrive as noise. The token's absence at every version stands.
- **K-11 · D-16's provenance + D-17's arithmetic — KILLED as cells, substances confirmed** (reader-DU; ratified). The z register lives at scheme-motion.css:333-341 + bridges:235-242 (my reads), not `components.css` alone; D-17's px assumed a 16px root but fourier sets 18px below 768px (style.css:41/:45-47, my read) — 157.5px / 252px track. **Extension minted by this seat (R-6): DU's own corrected cell cites the 220px `.marquee-item` as "the sharper consumer" — voided; that consumer can never mount (K-9).**
- **K-12 · D·S-3 / L·S-4's "exactly one hole" — KILLED as stated, re-scoped to the colour register** (reader-DU; ratified). The motion register hand-tunes six magic numbers against shipped tokens — `scale(1.1)` at :261 is numerically identical to `--scale-hover-dock` (scale-paper.css:25, my read) — and two of them cause G-DU3/G-DU4. The colour discipline superlative survives re-scoped (S-4).
- **K-13 · C-6's bundle-cost cell ("the `.path-preview` rule ships to every user") and L·D-3's CSS-retention prediction — KILLED BY THIS SEAT.** `.path-preview` appears in **no** built sheet (my grep over `dist/assets/*.css`), while the same dist demonstrably ships the dead marquee's CSS — the sheet discriminates, and Rollup shook the unused SFC import, CSS included. LC's compiler-stage retention fact stays true; the bundle-stage consequence does not follow. Currency-of-dist caveat recorded in UNPROVEN. The orphaned-component defect survives untouched.

## Superlatives — with provenance (L-18 runs both ways)

1. **The `content-visibility` adoption** (:71, :199-206) — all three axes + both readers; utility, var-name, `auto <len>` form and 30rem default verified in base.css:477-480 by this seat; the 17rem estimate derived in-comment against the grid's `minmax(14rem,1fr)`; DU's extended falsifier (all three consumers definite-sized) survives — the dead marquee only removes a consumer, never the claim.
2. **The pointer contract is airtight, which is what makes the blocker a two-token cure** — three deliberate `@click.stop` seals (:88/:141/:156); no nested control leaks a card-open on the pointer path, including the reka Checkbox that fires its own click. Both readers; re-read by this seat.
3. **`:data-selected="selected || undefined"`** (:76) — the correct attribute-omission idiom, load-bearing against the presence selector at :221; typed `boolean | undefined` so the coercion cannot misfire. (C's S-2; ratified.)
4. **Colour-register token discipline** — zero hex, zero `!important`, zero raw `cubic-bezier()`, `color-mix` for every tint, every referenced colour present at both 4.0.0 and v7.0.0 (spot-verified by this seat at both trees). Re-scoped per K-12: the *motion* register is six magic numbers.
5. **Zero teardown surface, provably** — two computeds + one pure function; no lifecycle, watcher, timer, listener, observer or subscription; safely mountable in an unbounded `v-for`. (LC; my whole-SFC read.)
6. **Zero-CLS lazy image + a cross-file dependency that is real** — `aspect-[4/3]` reserves the box before `loading="lazy"`; the header comment points at `style.css`'s `.gallery-card:focus-visible` and the rule exists with the `--ring` token (:138-142, my read; normally where audits find rot).
7. **The consumer-side variant-projection idiom** (`.basis-tint` / `.like-btn` / `.admin-overlay-btn` + `--pill-c`) — the standing `feedback_glass_ui_first_class` shape, and *why* the Badge callsite survives the v7 re-cut at the prop level while the Button callsites do not. Its two costs are G-DU3/G-DU7 — failures of the technique at single sites, not of the pattern.
8. **The corpus's own falsification discipline** — D-14/D-17 drafted and killed their own attractive a11y findings against the producer's compensating utilities (both mechanisms verify at the pin), and C/L independently cleared the forced barrel import from the export map. L-18 run in the direction that costs the axis a finding, three times.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. The Space arm of B-2: bubble-phase keydown `preventDefault()` suppressing the descendant's keyup activation per engine. One Playwright walk (Tab → like button → Space → assert no navigation and the like fired) converts the whole blocker to measured.
2. Contrast of the three admin-overlay glyphs over `variant="glass"`'s translucent wash on an arbitrary user thumbnail — content-dependent, statically indeterminate; `--delete` is already marginal at 4.16/4.06 against bare `--card` (adopted).
3. C-9's 16-pill wrap overflow and D-23's real-contour crop rate — latent-on-data.
4. Whether the dead marquee's and PathPreview's **JS** survives tree-shaking in a fresh production build, and whether `dist/` is current with HEAD — this record used the shipped sheets only to corroborate independently-derived statics (D-3 order, G-DU3 clobber, K-13), and the scope hash + rule-for-rule match are the honest but not conclusive witness.
5. The realized coarse-pointer overlap: 157.5px of admin chrome + the 24px checkbox in a 252px track — cascade proven, pixels not.
6. D-13's tz-skew arm (naive timestamps read back as local) pending the driver's `tz_aware` posture.
7. The D-5/G-DU8 cure's delta: a tier plate that moves 1.56:1 → ≥3:1 needs a paint check in both arms.

## DISSENT — preserved, not ruled away

1. **Reader-vs-reader inversion recorded (evidence about the fleet).** On the marquee, reader-DU *confirmed* a corpus MAJOR whose falsifier was one parent-guard read away, while reader-LC ran the falsifier and killed it; on the R3-12 attribution the roles reversed — LC endorsed the corpus's borrowed authority while DU killed it (with the wrong replacement mechanism). Neither reader is systematically the skeptic; the incredulity seat exists because skepticism is not conserved across rows.
2. **Severity dissents preserved**: LC's MAJOR on the tier predicate (incidence argument, R-2) and on C-7's vocabulary row; DU's MAJOR on the D-1 site consequence (R-4); LC's MINOR on D-6 and on the like-name row. If SS-13's live pass shows the invisible tier box shifting real layouts, or a broken-thumbnail rate above noise, R-2/R-5 re-open.
3. **Severity-calibration signal**: two independent axes filed a banked MINOR (`--ease-apple-spring`) as BLOCKER, and C filed a documented stopgap as BLOCKER by excising the documenting comment. Killed cells clustered in the C axis (K-1, K-2, K-3, K-7 — four of its cells against one each for D's); a corpus that ships "grepped: zero occurrences" for a line that exists cannot be trusted on its other negatives, and this record re-derived every C negative it adopted.
4. **Export-map count drift across seats**: 74 (this seat + reader-DU, at the v7.0.0 tag) vs 67 (fr-AdminUserList) vs 66 (fr-AdminAuditLog) — the latter two were live-producer-tree measures at different drift moments. Tag measures govern; `./checkbox` is absent in every measure, so no conclusion moves.
5. **This seat's own adoption disclosure**: D-8's 96/77/35 repo counts (twice-derived, D + reader-DU's independent parse — not run a third time here); the frozen-pill contrast table (D + reader-DU to 2dp — this seat's instrument validated on six other figures instead); LC's `compileScript` retention run; the api `likes`-substrate line anchors (spot-checked, not read whole). Each is labeled (adopted) at its row.

## Closing verdict

**ADJUDICATED — the keydown-hijack BLOCKER stands alone at the top (B-2/D-2/C-2 → F.W3/W4); 39 rows confirmed (1 BLOCKER · 14 MAJOR · 15 MINOR · 9 INFO) with 8 folded by reference to banked rows (fr-BasisCanvas D-1+D-2/M-α6 · fr-AdminAuditLog AA-2 · fr-App K-13 · FR-AUL-17/FR-AFP-32 · fr-BasisSelector M-10/i-3 · FR-AUL-19 · FR-EMT-10) and never re-booked; 13 kills banked including this seat's own three (the both-axis R3-12 attribution, the marquee arithmetic void, the PathPreview bundle-cost cell); 12 missed finds adopted after re-proof, led by the unreachable GalleryMarquee and the inert featured-tier selection; routings are M-25 agglomeration input for the forming X·F specs — F.W1 owns the token/package/uplift cluster, F.W3/W4 owns the keyboard/selection/contrast/dead-code cluster, F.W5–W8 owns the like-verb and write-only-identity contract cluster, and the census-denominator notes ride NO-WAVE-OWNER to SS-3/SS-4; nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
