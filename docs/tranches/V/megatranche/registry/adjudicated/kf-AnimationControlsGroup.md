claude-fable-5

# kf-AnimationControlsGroup — adjudicated apotheosis (M-23/L-18, fresh-Fable arbiter)

**Seat**: fresh Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Subject tree**: `/Users/mkbabb/Programming/keyframes.js` @ HEAD `8281638c` (branch `master`) · registry branch `tranche-u` (value.js).
**Corpus**: three challenge axes by `claude-opus-5[1m]` — `challenge-D-design.md` (15 defects / 5 superlatives), `challenge-L-library.md` (9 / 4), `challenge-C-consumption.md` (10 / 4) — attacked by two hostile re-readers: **RR-D** (design-and-usage) and **RR-L** (library-and-code). Both re-reader returns were themselves challenged by this seat before adoption.
**Method (L-18/M-23)**: every verdict-level disagreement between the re-readers was re-derived against the tree with this seat's own instruments; every re-reader kill was re-executed at its decisive file; every re-reader MAJOR-grade net-new find was re-verified at source before banking. No claim below rests on a citation this seat did not re-open. **No browser tooling** (law); all rulings are static + dist-read; the irreducibly visual residue is carried to SS-13.
**Arbiter's own instruments this session (all read-only)**: `AnimationControlsGroup.vue` whole (336 L) + `AnimationControlsGroup.css` whole (223 L); `useControlsKeyboardShortcuts.ts` whole (72 L); `KfPillTabs/useKfPillTabs.ts` whole (93 L); `ChannelControls.vue` :1-10/:140-200/:265-330/:370-415; `ControlsPaneWrapper.vue` :30-60/:140-150/:190-235/:250-260; `scene-facility/index.ts` :80-115; `controlSurfaces.ts` :40-120; `useCubeDemo.ts` :105-130; `useAmigaDemo.ts` :145-160; `useAnimationProgress.ts` whole; `TransportDock.vue` :115-135/:355-370; `hashSharing.ts` whole; `controlOptionsStore.ts` :40-75; `storeUtils.ts` :20-36; `useDemoTicker.ts` :1-12; `KeyframeTimeline.vue` :1-6 (at `instrument/timeline/`); `design-idioms.css` :42-64; `test/demo/state/no-shadow-playback-authority.test.ts` :1-25/:80-96; installed glass-ui 7.0.0 `dist/keyboard.js` whole (alias map, skip predicate, dispatcher, `registerShortcut` scope-dispose) and `dist/styles/glass/{ladder,a11y-fallback}.css`; executed greps: `timeline-expanded-target` (2 hits), `aria-expanded|aria-controls` under `demo/components/instrument/transport/` (0), `@mkbabb/glass-ui` in both manifests (0/0), `@layer` in ladder.css (1, wrapping the whole body); executed `npx prettier --check` on the pair (`.vue` warns, `.css` passes); census re-read `formation/keyframes/lane-frontend.md` §0 F-1, §7.3 F-5, §10 wave order.

## Scoreboard

**37 ADJUDICATED-CONFIRMED (2 BLOCKER · 6 MAJOR · 22 MINOR · 7 INFO) · 9 KILLED (with refutations, 2 of them kills-of-kills) · 11 net-new re-reader finds BANKED · 9 UNPROVEN-NEEDS-LIVE rows to SS-13.**

Blocker pair: **L-1** (the channel-first `sliderUpdate` un-does the T.B8 SCRUB cure for every facility scene while the proof gate stays green certifying the opposite) and **D-1** (the document-singleton portal sink stacks *every* painting channel's KeyframeTimeline — **three** on cube and amiga, two on spring — into one clipped box; D-1 is restored to BLOCKER by this seat's own enumeration, see rulings).

Status verbs per M-25: dispositions here are **ADJUDICATED**, not VERIFIED — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets are named from the census lane taxonomy (`lane-frontend.md` §10): **KF.W1-DEP** (F-1 declare+lock) · **KF.W2-TABS** (S-1) · **KF.W3-SHIM** (F-5) · **KF.W4-PROSE** (S-2) · **KF.W5-PARTIALS** (S-5/6/7) · **KF.W6-TIMELINE** (S-3/S-4, "wants its own spec") · **KF.W7-TOKENS** (namespace audit). Everything the census does not own is marked **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Ruled disagreements (the seat's actual work)

| id | RR-D (design-and-usage) | RR-L (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-1 severity** | BLOCKER — "the axis UNDER-SCOPED it by ~3x": group scenes stack THREE timelines | MAJOR — "one scene (spring), gated on one affordance" | **BLOCKER — RR-D wins; RR-L's demotion premise is killed by enumeration** | My own reads: `useCubeDemo.ts:116-120` `new AnimationGroup(rotationAnim, matrixAnim, hoverAnim)` (3, each stamped `superKey = SCENE_ID`); `useAmigaDemo.ts:153` `new AnimationGroup(spinning, bouncingX, bouncingY)` (3, all stamped); `facilityFromGroup` (scene-facility/index.ts:92-109) emits one channel per group animation, **each carrying `animation: anim`**; `controlSurfaces.ts:51-55` grants every painting channel the BUILT_IN triad incl. `timeline`; `ControlsPaneWrapper.vue:46-51` mounts ALL hosts under `v-show`; `ChannelControls.vue:186` Teleports into the one unconditional sink (`.vue:79-88`), predicate `isTimelineVisible` (:377-379) reading the store bucket **shared per scene** (`getStoredAnimationGroupControlOptions(animation)` → `animation.superKey`, ChannelControls.vue:275). So the flagship cube scene stacks THREE KeyframeTimeline instances in one `overflow-hidden` box on expand; amiga three; spring two. "One scene" is false; the affordance is a first-class transport control broken on the majority of scenes. |
| **D-5 magnitude vs reachability** | Killed the "60dvh empty glass sheet" sub-claim (max-h is a MAX; no height declaration; ≈26 px used height) but did not chase reachability | Strengthened reachability via `hashSharing` **but re-asserted "painting a 60dvh empty glass sheet"** | **BOTH corrected; row survives at MAJOR on the a11y half** | Geometry — my whole-file read of `AnimationControlsGroup.css`: the mobile fixed rule (:179-184) declares `position:fixed; left:0; right:0; bottom:var(--dock-menubar-reserve)` and **no height**; the desktop placement (:219-222) is `grid-row: bottom` (auto); `.vue:85`'s `max-h-[var(--panel-max-h)]` is a ceiling, not a height. An empty expanded sink paints ~26 px of glass chrome (py-3 + border), not 60dvh — RR-D's kill stands and RR-L's re-assertion of the magnitude is struck. Reachability — my read of `hashSharing.ts`: `isValidState` (:29-45) checks only `typeof s.controls === "object"`; `restoreStateFromParam` (:56-69) → `applySharedControlState` (controlOptionsStore.ts:59-64) `Object.assign`s object-valued buckets wholesale — a crafted/stale share link CAN seed `isTimelineExpanded: true` into a surfaceless scene's bucket. Real path, small paint: a fixed ~26 px glass strip at z-dock over the stage. The unconditional a11y half (zero `aria-expanded`/`aria-controls` in the whole transport tier — my grep: 0 hits; nameless, role-less sink) is what carries MAJOR. |
| **D-3 grade + mechanism** | CONFIRMED coupling + added the fifth (JS) site; left the rem-band branch open, MAJOR posture | MINOR; killed the MAJOR grade (branch unsettleable here) and the auto-placement mechanism (`col-start-1 row-start-1`) | **MINOR — RR-L's grade; RR-D's fifth site adopted; the band branch stays UNPROVEN with this seat's spec reading recorded** | My reads: `AnimationControlsGroup.css:114` px @media vs `:197` `@container … (min-width: 64rem)`; `ControlsPaneWrapper.vue:254` `useMediaQuery("(max-width: 1023px)")` — a **JavaScript** declaration of the same boundary that decides which branch renders at all (:118 Drawer vs :143 desktop rail). Five sites, three unit systems, zero coupling comments. Mechanism: `ControlsPaneWrapper.vue:147` does carry explicit `col-start-1 row-start-1`, so RR-L's precision correction holds. Spec reading (recorded, not ruled): css-contain-3 evaluates relative units in `<container-condition>` against computed values (rem per the root element); nothing in the tree pins root font-size, so under that reading the [1024, 1280) dead band opens **only** for users with a raised browser default font size — an a11y-relevant population, but conditional. One WPT/live probe settles it — SS-13's highest-value single item. Bonus prose fault (this seat's own): `.css:195` cites "the @container max-width:64rem fork above" — no such fork exists; the mobile fork is `@media`. Third self-contradiction in one file; folds into D-3/D-9. |
| **L-4(b) cure** | (carried the axis's cure) | KILLED the prescribed `map[n] === self` idiom — not implementable on Vue's function-ref signature | **Kill adopted** | Vue calls the function ref with `null` on unmount; no element handle exists in that call, so the identity check cannot be written where the axis prescribed it. And my read of `ControlsPaneWrapper.vue:47/:51` confirms the loop is keyed by `host.animation.id` while the map is written by `host.name` — two identities. A structurally sound repair rebuilds the registry from `controlHosts` or aligns the two keys; the roster row (L-4(a), MINOR) carries that corrected cure. The (b) staleness hazard stays PLAUSIBLE-unproven (no in-scene channel-membership mutation found by either reader or this seat). |
| **Keyboard clearance** | Filed the tablist/scrub collision as a MISSED MAJOR, directly contradicting the L axis's "Not charged" clearance | (did not contest) | **RR-D wins; the L-axis clearance is KILLED** | My own three reads close the chain: `useControlsKeyboardShortcuts.ts:53-58` registers ArrowLeft/Right, Shift+arrows, Home, End with `preventDefault: true`; glass-ui `dist/keyboard.js`'s skip predicate `f()` covers **only** INPUT/TEXTAREA/SELECT/`isContentEditable`/`.monaco-editor` and the dispatcher is a bare window keydown listener; `useKfPillTabs.ts:64-90` handles the same keys on `role="tab"` **buttons** with `e.preventDefault()` at :84 and **no `stopPropagation()`**. The event bubbles to window; both handlers run: arrowing the tab strip also scrubs ±1%, Home/End jump tabs AND slam the playhead. The L axis cleared plain-letter shortcuts from the INPUT/TEXTAREA/SELECT skip without checking that the demo's composite widgets are div/button-based. The reka-`<Select>` half (typeahead + Home/End in the open listbox) is PLAUSIBLE — reka's internal propagation was not read — and is sub-marked accordingly. |
| **F-1 severity at this component** | MAJOR (as L-3) | MAJOR, with the double-count argument (C-1's BLOCKER re-charges one repo fact per component) | **FOLD to census F-1 (KF.W1-DEP) at MAJOR — identity guard, not re-booked** | My greps: `@mkbabb/glass-ui` = 0 in `package.json`, 0 in `package-lock.json`; installed 7.0.0. The component's evidence rider (what the census could not see): `useControlsKeyboardShortcuts.ts:1` is the sole binding site of all 18 shortcuts, so resolution failure deletes the entire keyboard surface in one stroke. One identity — census F-1 (RED) — this record adds the blast radius, never a second booking. |
| **D-2 / D-15 kills** | Killed both, with residuals | Killed both, with residuals | **Both kills CONFIRMED by this seat's own file reads** | `ladder.css` byte 1 opens `@layer components {` and the grep count is 1 wrapping the whole body — D-2's "unlayered" load-bearing fact is false. `a11y-fallback.css` names `.glass-wash` explicitly in the forced-colors `border: 1px solid CanvasText` list — D-15's "not for .glass-wash" is false; **and** the `background: Canvas` list is `.glass-floating, .glass-overlay, .glass-drawer` only, so the opaque-backing residual survives to SS-13. Both residuals are banked as roster rows (M-5; UNPROVEN #4). |

## Adjudicated defect roster

Ids keep their axis names for life; re-reader net-new finds are banked as M-1..M-11. Line anchors are keyframes HEAD `8281638c`; re-resolve before executing any cure.

### BLOCKER

- **L-1 / C-2 — ADJUDICATED BLOCKER · the channel-first `sliderUpdate` silently un-does the T.B8 SCRUB cure for every facility scene, and the proof gate stays green.**
  Proof (this seat's own): `AnimationControlsGroup.vue:244-254` — the `find` by `c.animation.id` matches for every facility channel and returns at :251; `groupSliderUpdate` (:253), the sole `SCRUB` carrier (`useAnimationGroupPlayback.ts:146`), is unreachable. `scene-facility/index.ts:104-108` `setProgress` = `setChildTime(...).render()`, **no dispatch**. cube/amiga/square all build via `facilityFromGroup`. The gate: `test/demo/state/no-shadow-playback-authority.test.ts` imports the composable **through the F-5 shim** (:21) and calls its `sliderUpdate` directly (:83-88, asserting cube/spin t=555) — the component is never mounted, so the branch production takes is invisible, while the header (:12-13) claims "BITE: reds the instant `sliderUpdate` stops dispatching SCRUB." A green gate certifying a property production does not have; scrub-then-reload on cube loses the playhead — verbatim the regression the cure's own comment names.
  Terminal disposition: **NO-WAVE-OWNER** (SS-1/SS-2 spec input). Spec obligations recorded: (i) the channel branch dispatches `SCRUB` (or the machine write moves into `ChannelHandle.setProgress`), (ii) **the gate moves to the component seam** — a mounted-component (or wrapper-exported) test that reds on the current tree — before anything else in this component is touched; any repair wave reading today's green gate will route around the defect. Cross-ref KF.W3-SHIM: the same test file's shim import is the F-5 repoint site.
- **D-1 — ADJUDICATED BLOCKER (restored; see ruling) · one document-singleton portal sink, N Teleport sources — expanding the timeline stacks every painting channel's KeyframeTimeline into one clipped box.**
  Proof: sink minted unconditionally with one id (`.vue:79-88`); source per painting channel (`ChannelControls.vue:186`), all mounted (`v-show`, `ControlsPaneWrapper.vue:46-51`), no selection gate (`isTimelineVisible` reads the scene-shared store bucket, :377-379); Vue performs no arbitration between Teleports naming one target. Scope by this seat's enumeration: cube 3 · amiga 3 · spring 2 (all animations stamped with the scene `superKey`, so one bucket flips all instances). The selected channel's timeline may not even be the visible one — hosts stack in `controlHosts` order under `overflow-hidden` + `max-h`.
  Terminal disposition: **NO-WAVE-OWNER**, must be settled **before** KF.W6-TIMELINE (any timeline-cluster swap inherits the portal architecture). Cure space recorded, not chosen: per-channel targets, a selection-gated source, or this component becoming the single owner of the expanded instrument (it already computes `activeTimelineRef`, :198-201). Rendered-stack geometry → SS-13 (UNPROVEN #2).

### MAJOR

- **L-2 — MAJOR · `scrubActive` repeats L-1 on the keyboard axis; six bindings, zero test coverage.** `AnimationControlsGroup.vue:264-271` returns before `groupScrubActive` whenever `selectedChannel` resolves; bound to ArrowLeft/Right, Shift+arrows, Home, End (`useControlsKeyboardShortcuts.ts:53-58`); the component passes its own wrapper (:327-328). Dual identity axes confirmed (`c.animation.id` at :246 vs `c.name` at :187). Disposition: **NO-WAVE-OWNER**, same spec row as L-1 (one shared helper + the seam gate covers both).
- **L-3 / C-1 — MAJOR · FOLD → census F-1 (KF.W1-DEP).** Phantom `@mkbabb/glass-ui` (0/0 in manifests, 7.0.0 installed, `.npmrc` `legacy-peer-deps` silences it). Component rider: three specifiers (`.vue:124`, `.vue:126`, `useControlsKeyboardShortcuts.ts:1`), the third being the sole binding site of all 18 shortcuts. **Identity guard: booked once at the census; never re-booked per component.**
- **D-5 — MAJOR (RESCOPED per ruling) · the disclosure region has no accessible identity and no content predicate.** Unconditional half: the sink carries no role, no name, no `aria-expanded`/`aria-controls` anywhere in the transport tier (my grep: 0) against three toggles; focus never moves in or restores out; the only state announcement is the visual string "Timeline expanded below" (`ChannelControls.vue:161`). Rescoped half: the empty-sheet path is REACHABLE (hashSharing seeding, M-10) but paints ~26 px of fixed glass chrome, not 60dvh (geometry ruling). Disposition: **NO-WAVE-OWNER** (a11y spec input; pairs with M-6). A watcher forcing `isTimelineExpanded = false` on surfaceless scenes closes the residual occlusion path for free.
- **M-1 (RR-D net-new) — MAJOR · the global scrub shortcuts collide head-on with the demo's own APG tablist.** Fully verified for the tablist (see ruling): ArrowLeft/Right/Home/End registered window-level with `preventDefault: true`; glass-ui's skip predicate covers only true form elements; `useKfPillTabs` preventDefaults but never stops propagation — arrow-navigating the control-tab strip simultaneously scrubs the playhead; Home/End jump tabs AND slam it to 0/1. The reka-`<Select>` half (typeahead vs `R`/`[`/`]`/`1`/`2`/`3`; Home/End in the open listbox) is PLAUSIBLE pending reka propagation facts (SS-13/UNPROVEN #8). Disposition: **NO-WAVE-OWNER** for the demo-side scoping cure **+ glass-ui BH relay** (owner edict): the registry offers no composite-widget awareness and no `defaultPrevented` check — a producer seam. Must be an input to KF.W2-TABS (the collision survives any tablist implementation).
- **M-2 (RR-D net-new) — MAJOR · the expanded timeline is `overflow-hidden` under a max-height cap with no scroll container — and the collapsed path IS scrollable, so "expand" removes scrollability.** Verified: `.vue:82` unconditional `overflow-hidden`; neither CSS placement adds a scroller; `KeyframeTimeline.vue:3` root Card is `overflow-visible` (no internal scroller); the collapsed in-pane path rides `paneScrollable ? 'overflow-y-auto' : …` (`ControlsPaneWrapper.vue:36`). Any timeline taller than 60dvh has permanently unreachable content. Disposition: **NO-WAVE-OWNER**, same expanded-timeline spec as D-1; hidden-content magnitude → SS-13 (UNPROVEN #3).
- **M-3 (RR-D net-new) — MAJOR · `registerShortcut("Delete", …)` also binds BACKSPACE through glass-ui's alias table.** Verified in `dist/keyboard.js`: `var l = { …, delete: ["backspace","delete"], … }` and the matcher accepts any alias; the registration (:65) carries no modifier; the only guard is the form-element skip. A stray Backspace on any button/tab/slider/body deletes the selected keyframe, undisclosed by the label or the shortcuts modal. Undoable via Mod+Z — the sole mitigation, and why this is not higher. Disposition: **NO-WAVE-OWNER** (demo: require the explicit key or disclose the alias) **+ glass-ui BH relay** (the alias table is producer behavior, invisible at the API surface).

### MINOR

- **M-4 (RR-D net-new) — MINOR · Escape is globally bound to `reset()` with no overlay scoping.** Registration verified (:51, no preventDefault); the dispatcher's only Escape-specific behavior is reversing its own registry order — arbitration among glass-ui shortcuts only, no open-overlay guard. Whether each overlay's dismissal also fires it depends on reka's per-overlay propagation (not read) — registration facts CONFIRMED, per-overlay both-fire PLAUSIBLE (UNPROVEN #9). Disposition: NO-WAVE-OWNER, keyboard spec row with M-1/M-3.
- **M-5 (RR-D net-new; D-2's corrected residue) — MINOR · the intended top hairline renders as a four-sided outline.** `.glass-wash` supplies `border: 1px solid var(--glass-border-accent)` on all sides from `@layer components`; Tailwind utilities (`@layer utilities`) win the properties they set — `border-t` sets only `border-top-width`, `border-border/50` recolors all four sides. Painted result: a full 1px outline in `--color-border`/50 — the "hinged to the stage" intent does not render. One-token cure (`border-x-0 border-b-0` or a bespoke top rule), **not** layer surgery. Disposition: NO-WAVE-OWNER (rides any expanded-timeline restyle).
- **M-6 (RR-D net-new) — MINOR · the Teleport severs the tab→tabpanel containment.** Verified: while expanded, the `role="tabpanel"` (ChannelControls.vue:146-172) holds only the placeholder + Collapse button, while the real instrument sits in a role-less, nameless div elsewhere in the DOM with no `aria-owns`/`aria-controls` tie. The structural half D-5's cure (role+name on the sink) cannot fully repair — the tab→panel relationship itself is what the move breaks. Disposition: NO-WAVE-OWNER, one a11y spec row with D-5.
- **M-7 (RR-L net-new) — MINOR · `useAnimationProgress` reassigns a fresh object per frame; both this component and the 403-line TransportDock re-render at 60fps while playing, to drive a dot that renders only inside an open dropdown.** Verified: fresh `p` per tick, `animationProgress.value = p` (useAnimationProgress.ts:16-42); sole consumer `dotStyle` (TransportDock.vue:361-364) applied on a `v-if="isPlaying"` span inside `<SelectContent>` (:119-131), mounted only while the channel dropdown is open. All three axes read this loop as a superlative; the `{ guard: isPlaying }` they praise is the same line that makes the cost unconditional while playing. The correct shape (per-name refs, or gating the loop on the dropdown) sits on top of the L-S4 foundation, not against it. Disposition: NO-WAVE-OWNER (efficiency).
- **M-8 (RR-L net-new) — MINOR · `animControlRefs` is a deep `reactive()` over component public instances.** Verified `:191`; values are `ChannelControls` instances written at `ControlsPaneWrapper.vue:51`. `reactive()` deep-proxies on read, so the keyboard actions dispatch into proxies and every derived-computed read deep-tracks the child's exposed surface; `shallowReactive` (or `markRaw` on write) is the correct primitive for an instance registry. Same line as L-4/L-8 — one repair, three rows. Disposition: NO-WAVE-OWNER.
- **M-9 (RR-L net-new) — MINOR · the Teleport target contract is an unshared magic string across two files, in a subtree that owns a typed injection-key module.** Verified: exactly two hits repo-wide (`.vue:80` mints, `ChannelControls.vue:186` hardcodes); `transport/injectionKeys.ts` exists and is the established seam. A rename on either side silently disables the whole feature (dev-warning only). Disposition: NO-WAVE-OWNER (rides the D-1 cure — any per-channel-target redesign must produce a shared constant anyway).
- **M-10 (RR-L net-new) — MINOR · `applySharedControlState` writes arbitrary base64-decoded URL objects into the persisted store on a shape-only check.** Verified: `hashSharing.ts:29-45` validates only `typeof === "object"`; `controlOptionsStore.ts:59-64` `Object.assign`s object-valued buckets wholesale — no per-field validation, no scene-id whitelist. The reachability carrier for D-5's residual and an unvalidated-input contract defect in its own right. Disposition: NO-WAVE-OWNER.
- **D-3 — MINOR (rescoped per ruling) · one desktop/mobile boundary in three unit systems across five sites (four CSS + one JS), no coupling comment, and a stylesheet that contradicts itself about which fork died** (`.css:39` vs `:114`; plus this seat's `:195` "the @container max-width:64rem fork above" ghost). The rem-band branch → SS-13 (UNPROVEN #1, with this seat's spec reading recorded). Disposition: NO-WAVE-OWNER.
- **D-4 — MINOR · the expand/collapse motion is nominal.** All three sub-faults re-verified in my own reads: `transition-[max-height,opacity]` with no opacity value anywhere in the branch classes or `.glass-wash`; every skin property outside the two-property list (plate/rim/padding snap 450ms before the box settles); collapse cannot animate — `:disabled="!isTimelineExpanded"` empties the target in the same patch (`defer` affects target resolution, not the toggle), and the entering content runs a horizontal `slide-in-from-right-2` inside a vertically-growing box. PRM snaps all of it anyway (superlative S-4). Disposition: NO-WAVE-OWNER (motion polish; rides the expanded-timeline spec).
- **D-6 — MINOR · proportion inversion: "expand" buys height, never width, inside the narrowest column — and applies a token documented as mobile-only to the desktop placement.** Verified: `[rail]` = `--rail-width` = `clamp(25rem, 33svi, 32rem)` (design-idioms.css:47, my read) = 400-512 px; `--panel-max-h: 60dvh` (:48) whose comment (:46) says "caps MOBILE panels", applied on the unconditional class list; the mobile rule is full-bleed (`left:0; right:0`) — the small form factor gets the width the large one is denied. Usability verdict → SS-13 (UNPROVEN #5). Disposition: NO-WAVE-OWNER (D-1's cure decides the geometry).
- **D-7 — MINOR · the `--rail-width` rationale contradicts the shipped token; the container-query payoff it claims is false for this token.** Verified at both ends: `.css:22` + `:40-44` assert `clamp(20rem, 26cqi, 30rem)` "tracking the work-area card"; shipped is `clamp(25rem, 33svi, 32rem)` — `svi` is a viewport unit, so the rail tracks the raw viewport, exactly what the comment claims the promotion stopped. Disposition: NO-WAVE-OWNER (prose-truth repair; any wave touching the file corrects it).
- **D-8 / C-7 — MINOR (merged) · `controls-layout--open` and `controls-layout--stage-*` are applied and matched by nothing.** `--closed`/`--railless` are live (`.css:64/:67`; `--railless` also queried by the live-session-mobile probe per RR-L's grep). The `--stage-undefined` sub-claim is latent-only (EditorShell defaults `"subject"`; no direct host exists) — killed as a live path, kept as a type note (give `stageMode` a default or make the class conditional). Disposition: NO-WAVE-OWNER (hygiene).
- **D-9 — MINOR · not formatter-clean, and nothing in the repo would catch it.** Re-executed by this seat: `npx prettier --check` → `[warn] …AnimationControlsGroup.vue` (the `.css` passes); no `format` script; `lint` = `depcruise src` (demo untraversed). The retrofit tell (TooltipProvider wrapping an unindented subtree, `</TooltipProvider>` after a stray blank line at :112) verified in my whole-file read. Disposition: NO-WAVE-OWNER (hygiene).
- **D-10 / C-5 / L-9 — MINOR (merged) · the tooltip register: a 100ms/0ms provider triplicated down one ancestor chain, shadowing App's grouped register for the whole scene surface.** Verified: `App.vue` defaults (reka 700/300) → `.vue:2` `100/0` → `ChannelControls.vue:2` byte-identical (my read of its line 2). D-10's flicker mechanism is KILLED (see register); the adopted reading is C-5's: grouping is disabled scene-wide, each tooltip pays the full 100ms, and the pair has three declaration sites and no owner. RR-L's addition banked: one depth-3 provider mounts **per painting channel** — three siblings on cube. Note: keyframes HEAD `8281638c` ("provide tooltip context for the routed control group") postdates the challenge; the providers verified present at this HEAD. Disposition: NO-WAVE-OWNER (one owner for the register; delete the descendant re-provides). Taste verdict → SS-13 (UNPROVEN #5); WCAG 1.4.13 exposure → UNPROVEN #6.
- **D-11 / C-8 — MINOR (merged) · a destructive `resetAllStores()` + `window.location.reload()` stays wired to a branch nothing can take.** Verified: `.vue:106` handler; TransportDock's sole emitter passes `false` (:158) and its own comment (:368, my read) records the `true` producer moved to the settings menu; the emit signature still types the boolean. If anything ever re-emits `true` through this channel it fires with no confirmation. Disposition: NO-WAVE-OWNER (delete the arm + narrow the emit type).
- **C-3 — MINOR · `superKey?` is optional in the type and required in fact.** Mechanism verified: `storeUtils.ts:33` `animation!.superKey` dereferences the undefined both guards fell through with; `:176` is setup's first statement. Reachability premise KILLED (see register) — no live host omits it. A prop type that lies about optionality, not a latent crash with a caller; the `?? "default"` on the same line shows the intended behavior the guard order loses. Disposition: NO-WAVE-OWNER (make it required, or fix the guard order — either is one line).
- **C-4 — MINOR (RR-L's correction adopted) · `extraTabs` is a 3-hop dead prop typed against a renderer that drops two of its five fields.** Inert end to end (three forwarders, no supplier, the only live host takes the machine branch); `SegmentedTabOption`'s `icon`/`tooltip` have no renderer in `KfPillTabs`. A prop no host supplies misleads only a future host — MINOR, not MAJOR. Disposition: **FOLD → KF.W2-TABS/KF.W4-PROSE** (= census S-2's sharp instance; one identity, the census row owns it).
- **C-9 — MINOR · app-global document singletons (`#rainbow-gradient` paint server + the `<Teleport to="html">` Toaster) mounted from a per-scene `:key`-remounted component.** Every navigation tears down and recreates the app's toast host. The S-9 candidate (vue-sonner while glass-ui 7.0.0 ships `./toast`) is **routed to the S-census** (shadow-row verdicts are census-owned), not adjudicated here. Disposition: NO-WAVE-OWNER (lift `DemoGlobalChrome` above the `:key` boundary).
- **L-4(a) — MINOR (RR-L's grade adopted) · `animControlRefs` is parent-owned state written by a child through a prop; the component's own comment claims an ownership the writes contradict.** Verified `:191`/`:27`/`ControlsPaneWrapper.vue:51`. The (b) leak is bounded by the `:key` remount (not a leak); the staleness hazard stays PLAUSIBLE. **Corrected cure carried per the ruling** (rebuild from `controlHosts` / align the two keys — not the axis's inexpressible guard). Disposition: NO-WAVE-OWNER (one repair with L-8 + M-8).
- **L-5 — MINOR · census F-5's "zero consumers" evidence for the `useAnimationGroupPlayback` shim is wrong — and the one consumer is the suite that proves the L-1 contract broken.** Verified by this seat: the test's :21 import string traverses `composables/useAnimationGroupPlayback`; the lane's scan root excluded `test/`. Correction of evidence, not law: the no-backwards-compat charge stands. Disposition: **FOLD → KF.W3-SHIM** with the corrected consumer census (the deletion wave must repoint the test import — and per L-1, that test must *move to the component seam* in the same stroke, not merely repoint).
- **L-6 — MINOR · the transport-axis expression is triplicated inside 30 lines** (verified byte-identity :182 vs :209-210 in my whole-file read; fourth shape in the wrapper). `watchEffect` tracks the same operands either way — reading the computed is dependency-identical. Disposition: NO-WAVE-OWNER (hygiene).
- **L-8 — MINOR · the `any` surface is load-bearing on exactly the members that carry the keyboard actions; the typed widening target exists** (`ChannelControls.vue:410-414` `defineExpose` of precisely the three members read — verified). Five unchecked double-optional call sites go silently no-op on any child rename. Disposition: NO-WAVE-OWNER (one repair with L-4(a)/M-8: `Record<string, InstanceType<typeof ChannelControls>>` on a `shallowReactive`).

### INFO

- **C-6 — INFO · root barrel for one Provider beside a same-file `/tabs` leaf import; six siblings use `/tooltip`.** The dual-instance hazard is dead (same chunk); cost is dev graph + convention. NO-WAVE-OWNER.
- **C-10 — INFO · the demo consumes keyframes.js through a source self-alias, never the published exports map.** Deliberate and documented; recorded as the blind spot that scopes every "works" claim on this axis to the source surface. No wave.
- **L-7 — INFO (RR-L's demotion adopted) · the selection `watchEffect` writes its own tracked dependency.** Converges today; a `watch` with an explicit source is the honest idiom; fragility noted. NO-WAVE-OWNER.
- **D-12 — INFO · `--rail-track` instantiates the census §6.3 flat-namespace hazard on an inheriting root; measured collision surface zero** (0 hits in glass-ui dist styles). **FOLD → KF.W7-TOKENS.**
- **D-13 — INFO · seven `proof:*` gate citations resolve to zero runnable gates** (two `proof:` scripts exist; one cited name matches a DEMO_ROSTER observation, prefix-less). The value.js owner's retired-proof-idiom feedback is value.js law, not keyframes law — the finding stands on the local grep alone. NO-WAVE-OWNER (prose-truth hygiene).
- **D-14 — INFO · two byte-identical rule bodies** (`.css:64-69`). NO-WAVE-OWNER (hygiene).
- **M-11 (RR-L net-new) — INFO · SUP-2's "zero runtime edge to keyframes.js" is scoped one hop too wide.** Verified by this seat: `useAnimationProgress.ts:5` → `useRafLoop` → `useDemoTicker.ts:3` `import { RAFPlayback } from "@mkbabb/keyframes.js"` — a VALUE import instantiated at module scope (:8). The claim survives only because `RAFPlayback` rides the LIGHT static barrel. Recorded so no future pass reads SUP-2 as "nothing in this subtree touches keyframes.js at runtime." No wave.

## Killed-claims register

1. **D-2 (the unlayered-`.glass-wash` cascade mechanism) — KILLED, re-verified by this seat.** `dist/styles/glass/ladder.css` opens `@layer components {` at byte 1; the entire body, `.glass-wash` included, is layered. The axis grepped only the import-manifest files (genuinely 0) and generalized without opening the file carrying the rule. Tailwind v4's layer order puts utilities last, so `border-t`/`border-border/50` **win**, the `/50` tuning is not discarded, and `--glass-border-accent` does not paint. The surviving residue is a different, smaller defect — banked as **M-5**. Method lesson: an unexecuted falsifier ("ladder.css carries no @layer") stated as executed.
2. **D-15 (forced-colors misses `.glass-wash`) — KILLED, re-verified by this seat.** `glass/a11y-fallback.css` — the sibling of the file the axis searched — names `.glass-wash` explicitly in the `border: 1px solid CanvasText` repaint, unlayered, reached via `accessibility.css`. The one surviving thread: `.glass-wash` is absent from the `background: Canvas` list → the opaque-backing question rides to SS-13 (UNPROVEN #4), not to a defect row.
3. **D-5's "60dvh empty glass sheet" magnitude — KILLED (RR-D's kill adopted; RR-L's re-assertion struck).** `max-h` is a ceiling; no height declaration exists in the grid placement, the mobile fixed rule, or the class list. An empty expanded sink paints ~26 px of chrome. The a11y defect survives; the severity theater does not.
4. **L-4(b)'s prescribed cure (`if (el) map[n] = el; else if (map[n] === self) delete map[n]`) — KILLED (RR-L's kill adopted).** Vue's function ref receives `null` on unmount — no `self` handle exists in that call; and the loop keys by `host.animation.id` while the map is written by `host.name`. The corrected cure is carried on the L-4(a) row.
5. **C-3's reachability premise (the "playground" host that may omit `super-key`) — KILLED (RR-L's kill adopted).** One `<EditorShell>` mount repo-wide, always binding `:super-key` off a required `SceneDescriptor.superKey`; the playground exists only in comments. The row survives as a type-contract lie at MINOR.
6. **C-7's `--stage-undefined` live-path sub-claim — KILLED (RR-L's correction adopted).** `EditorShell` defaults `stageMode: "subject"` and App always binds it; the literal `controls-layout--stage-undefined` class requires a direct host that does not exist. Latent type note only.
7. **D-10's flicker mechanism — KILLED (RR-D's correction adopted).** The skip window makes *subsequent* tooltips open instantly; `skipDelayDuration: 0` removes that grace, so each open pays the full 100ms — fewer opens per sweep, not more. The row survives on C-5's grouping-disabled reading.
8. **RR-L's D-1 demotion premise ("one scene, spring") — KILLED by this seat's enumeration** (cube 3 / amiga 3 / spring 2; shared per-scene bucket; BUILT_IN triad; v-show host multiplicity — all my own reads). A kill of a kill: the falsifier "any scene having ≥2 painting channels" was answered by the D axis for spring only and by RR-L no further; the group scenes were the majority case all along.
9. **The L axis's "Not charged" clearance of the shortcut surface — KILLED by M-1's evidence.** The clearance reasoned from the INPUT/TEXTAREA/SELECT skip without checking that the demo's own tablist and the transport's Select are div/button-based composite widgets outside it. Recorded as the corpus's method lesson: *a skip-predicate clearance is a claim about the widget inventory, and the inventory is part of the claim.*

## Superlatives (provenance preserved; L-18 runs both ways)

- **S-4 (D axis; verified by RR-D, chain re-walked) · PRM delegation holds and the component is right to carry no local guard.** The unlayered `!important` override rewrites `transition-property` to an allow-list excluding `grid-template-columns` and `max-height` — both snap; the entering `animate-in` clamps. Census §6.5's "unverified statically" upgraded to proven for this component; **no PRM defect filed** — declining to file is the harder half of an audit, done correctly.
- **L-S1 / SUP-4 (L + C independently; dist re-read by this seat) · 18 discarded disposers, provably correct.** `dist/keyboard.js`'s `registerShortcut` ends `return t() && n(l), l` — `getCurrentScope() && onScopeDispose(unregister)` — and the composable is called top-level in setup, so all 18 auto-dispose across the `:key="superKey"` remounts. Banked so no future pass "repairs" it into bookkeeping the callee already does; the falsifier that flips it (calling past an await) is stated.
- **S-2 (D axis; comments re-read by this seat) · the container-query promotion gets both subtle things right, reasons written down**: `inline-size` not `size` (definite block-size; descendant-collapse footgun), and the base template unconditional because an element cannot match its own `@container` query — with only descendant placements inside the fork.
- **S-5 (D axis) · RTL-correct by construction** — named grid lines carry the semantics, logical/axis-neutral properties throughout, the single physical pair symmetric. Zero `[dir]` rules needed.
- **SUP-1 / SUP-2 / SUP-3 (C axis) · the dependency skeleton is exemplary and was measured**: the 1.1 KB `value.js/math` leaf everywhere `clamp` is needed (the direct cause of the R1-unreachable verified negative — value.js's banked R1 row respected, not re-booked); the sole keyframes.js edge `import type`; no reach past public class surface. **Carried with M-11's scope correction** (the runtime `RAFPlayback` edge one hop out).
- **L-S2 (L axis) · every reactive-destructured prop crosses composable boundaries as a getter, enforced by the callee types**; the one by-value capture (`superKey`) is safe only by the host's `:key` — borrowed correctness, named as such.
- **L-S3 (L axis) · `:deep()` exactly once, exactly where the fragment-root child makes it mandatory**, bounded by a child combinator.
- **L-S4 / N-6 (L + C) · the RAF dogfood done right**: one module-level `RAFPlayback`, subscriber set, visibility reconcile, complete two-level teardown — and M-7's efficiency find sits *on top of* this correct foundation, not against it.
- **RR-D NEW · KfPillTabs is a principled, WCAG-reasoned deviation from glass-ui-first** — upstream aria defect named, deviation bounded, roving-tabindex core extracted and unit-testable. **Tension with census S-1 (RED, "retire onto SegmentedTabs") preserved, not ruled**: S-1's verdict is the census's; this superlative is the record of what KF.W2-TABS must not lose in the swap (the tablist semantics are right — the *global shortcuts* are what break the strip, M-1).
- **RR-D NEW · the stylesheet's comments name the FAILURE each rule prevents** (the G8 dock-band derivation; the `--dock-top-band-reserve-stable` oscillation cure with the measured ±8px and the reason the sheet anchor keeps the live token). Design documentation of a quality the audit found nowhere else — even as D-3/D-7/D-9 convict the same file of prose drift. Both are true; the drift findings are about keeping *this* standard honest.
- **METHOD (RR-L) · all three axes carried per-claim falsifiers and recorded verified negatives**; two of the corpus's kills (D-2, D-15) were only possible because the axes stated falsifiers precise enough to execute. The counter-lesson is D-2 itself: a falsifier *stated* is not a falsifier *run*.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs, ranked)

1. **D-3's rem band** — whether `rem` in a `<container-condition>` tracks the root's computed font-size. This seat's spec reading (css-contain-3: container-condition units evaluate against computed values; rem per the root) says the [1024, 1280) dead band opens **only under a raised browser default font size** — a real a11y population, conditional trigger. One WPT check or one live probe at a 20px default settles it; highest-value single item.
2. **D-1's rendered stack** — which of the three stacked cube timelines survives the `overflow-hidden` clip; stack height; whether the duplication reads as one corrupted instrument.
3. **M-2's hidden content** — how much a representative scene's timeline loses under the 60dvh no-scroll clip.
4. **D-15 residual** — forced-colors separation of the mobile fixed panel: CanvasText outline confirmed in source; opaque backing absent from the `background: Canvas` list; one forced-colors screenshot settles.
5. **Taste severities** — D-4's 450ms skin-before-box pop; D-6's 400-512px × 60dvh usability; D-10's 100ms/0ms register beside App's 700/300.
6. **WCAG 1.4.13** for the tooltip register (reka `disableHoverableContent` default through the glass-ui wrapper).
7. **L-4(b) staleness** — no in-scene channel-membership mutation found by three readers; absence of evidence, held PLAUSIBLE.
8. **M-1's reka-Select half** — whether typeahead/Home/End inside the open listbox propagate to window (the tablist half needs no probe; it is source-decided).
9. **M-4's per-overlay both-fire** — which reka overlays stop Escape propagation before the window registry sees it.

## DISSENT — preserved, not ruled away

1. **RR-L's severity conservatism vs RR-D's scope escalation.** On the two rows where they collided head-on (D-1, D-5), each was right about a different half: RR-L's *geometry* discipline killed D-5's magnitude theater while its *scope* discipline failed on D-1 (it verified spring one level deeper than the axis and stopped — the enumeration that decided the ruling was one `grep AnimationGroup(` away). RR-D's 3x scope correction was the single most valuable act in the corpus, and its own D-5 kill the second. Neither reader was uniformly the stronger seat; the tri-fold earned its keep here.
2. **The keyboard clearance kill (register #9) is also a claim about glass-ui.** The registry's design (window listener + form-element skip + alias table + no `defaultPrevented` check) is defensible for app chrome and wrong under composite widgets; the demo is the first consumer dense enough to surface it. The BH relay should carry the *inventory* argument, not just the two symptoms (M-1, M-3).
3. **Census S-1 vs the KfPillTabs superlative** — recorded above, unresolved by design: retirement is a census verdict; the superlative is the preservation spec for the swap.
4. **HEAD movement note**: keyframes HEAD `8281638c` ("fix(demo-shell): provide tooltip context for the routed control group") postdates the challenge corpus; every anchor in this record was re-verified at that HEAD by this seat. Line drift from the challenges was zero at every site this seat re-opened.

## Closing verdict

**ADJUDICATED at HEAD `8281638c`: 37 confirmed rows (2 BLOCKER · 6 MAJOR · 22 MINOR · 7 INFO) — the blocker pair is L-1's false-green SCRUB seam (the gate must move to the component seam before any repair) and D-1's portal stacking (three timelines in one sink on the flagship scene, restored to BLOCKER by enumeration); 9 claims killed with executed refutations (both glass-ui cascade kills re-verified at the file, one kill-of-a-kill each way); 11 net-new re-reader finds banked, three of them MAJOR keyboard/overflow defects the original axes cleared or brushed past; identity guards held (F-1, S-2, F-5, §6.3, R1 folded by reference, zero re-bookings); routings are M-25 agglomeration input for the forming X·KF specs — KF.W1-DEP/W2-TABS/W3-SHIM/W7-TOKENS own their folds, everything behavioral is NO-WAVE-OWNER for SS-1/SS-2, and 9 residue rows ride to SS-13.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/transport/AnimationControlsGroup.css` · `demo/components/instrument/transport/AnimationControlsGroup.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **AnimationControlsGroup.css=IDENTICAL · AnimationControlsGroup.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**D-13's seven are ENUMERATED** at `docs/tranches/X/keyframes/W0/GATE-ROSTER.md` §6(i), re-derived at the ref of record: **8 distinct `proof:*` names over 16 citations** on this record's subject surface, of which `live-session-mobile` is the record's own carve-out (DEMO_ROSTER **row 6**, `scripts/demo-roster.mjs:11`; `scripts/observe/demo/live-session-mobile.mjs` present) ⇒ **8 − 1 = the banked seven**, reproduced by measurement, **no bank correction owed**. Family disposition stands as banked: **NO-WAVE-OWNER (prose-truth hygiene)**.

**One correction, appended not rewritten**: the row's *"two `proof:` scripts exist"* is a stale-worktree reading. At the ref of record there are **THREE** npm entry points (`package.json:50/:51/:52`) over **NINE** `scripts/gates/` files — GATE-ROSTER §1.

**REPOINT, not strike, for one of the seven**: `proof:no-shadow-playback-authority` names a LIVE vitest suite, `test/demo/state/no-shadow-playback-authority.test.ts`, whose own `:1` reads *"proof:no-shadow-playback-authority — T.B8 (lane 30 rec 1), the vitest half."* Deleting that citation would delete a true sentence (GATE-ROSTER §5).

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

