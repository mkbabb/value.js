# FlagReportDialog — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · served model: `claude-fable-5`

**Seat**: fresh Fable adjudicator (incredulity seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-04 · **HEAD**: `f52d079a` · branch `tranche-u` · subject `demo/palettes/browser/dialog/FlagReportDialog.vue` (101 lines, unchanged since `a61094e3`; every anchor below re-resolved at this HEAD).
**Corpus (r2 governs)**: `audit/components/FlagReportDialog/challenge-{C-implementation,D-design,L-library}.md` — 3 axes, ~47 accusations. **Re-readers adjudicated**: `flagreportdialog-l18-design-and-usage` · `FlagReportDialog` (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement between them was re-derived against the tree by this seat.
**This seat's own instruments (all static; zero browser probes — probe parsimony)**: full read of FlagReportDialog.vue (whole file); BrowsePane.vue:165-175/:285-302 + full `flagPalette` grep (never nulled); useAdminFlagged.ts:75/:89/:101/:115-131 (all four swallows); api model.ts:25-26, schema.ts:25/:70-73, routes/flags.ts:14-34 (401/400 paths, **201** on success), service/flags.ts:28-48 (404/own-palette/11000→409); MigratePalettesDialog.vue:2,:3,:7-10,:15,:24,:32,:58; e2e/smoke/flows/palette-flag.spec.ts:30-49 (stub fulfils **200**); compiled `dist/button-Bu9F4uU6.js` de-minified around the props block and setup — `variant` occurrences **0**; `m = loading || disabled===true || disabled==="true"`, `h = !asChild && as==="button"`, `disabled: h ? m : void 0`, `aria-disabled` only when `!h`; template body = `renderSlot` only, **zero** spinner nodes; `components/button/styles.css` — sole loading rule `.button[data-loading]{cursor:progress}`; the destructive rules (`[data-tone="destructive"]` → `--button-tone: var(--destructive)`; `:not([data-tone="neutral"])` → filled) exist and are never reached; `radio-group/styles.css` verbatim (`--radio-face:1.125rem`, `--radio-seat:var(--touch-target,2.75rem)`, offset −13px, **gap: calc(seat−face) = 26px**, `display:flex;flex-direction:column`, forced-colors block present); `dialog-TNRDkcE4.js` class strings extracted verbatim (Content `grid w-full max-w-lg gap-4 …`; Header `flex flex-col gap-y-1.5 text-center sm:text-left`; Footer `flex flex-col-reverse sm:flex-row sm:justify-end sm:gap-x-2`; Description `text-sm text-muted-foreground`; Title `text-subheading leading-none tracking-tight`; Close `focus-ring absolute right-(--overlay-pad-inline) top-(--overlay-pad-block) rounded-sm opacity-70 …` wrapping `<X class="w-4 h-4">` — no padding, no min-size); reka-ui `dist/Dialog/` — `aria-modal` grep **0**, `useHideOthers` at DialogContentModal.js:47, `role:"dialog"` at DialogContentImpl.js:77; glass-ui `styles/theme/radius.css` (`@theme{--radius:0.625rem; --radius-input:var(--radius)}`) vs `styles/components.css` (`:root{--radius:0.25rem}`) vs `styles/index.css:1` (`@layer theme, base, components, utilities` + `@import "./components.css" layer(components)`); censuses at HEAD: `<textarea` in demo = 1 (this file) · `Textarea` = 0 · `<Label`/`ui/label` usage = 0 · `useId` = 0 · `strictTemplates` anywhere = **0**; `demo/ui/` = 19 dirs, `.vue` files = 0; EmptyState.vue:45-47 (`tag="div"` ×3 via **direct** `@mkbabb/glass-ui/watercolor-dot` import at :70; producer d.ts has no `tag`) — the K1 decider; useDialogBrowseActions.ts:37-46/:68-75 (F2 verbatim); useVersionHistory.ts:8-12 (dead `PaletteDialog/` path); tsconfig.demo.json paths vs package.json exports re-diffed at HEAD (phantom root + `/parsing` + `/units`; untyped `/css` + `/value`); motion-quarantine.md — `FlagReportDialog` hits **0**, M-03/M-29 family rulings read verbatim; t-card-color-census.md:92-93 (D3 ColorInput "acceptable as-is" beside D4 this-file "2W"); Ad-interactive-states.md:201/:242/:247 (the dead spinner cited as the exemplar); X-tranche routing surfaces: X/COHESION.md §4, X-W7 §4 File Bounds + G13 mutation list, X-W8 charter + :112, X-W4/X-W1 heads.

## Scoreboard

**32 CONFIRMED (4 BLOCKER · 11 MAJOR · 15 MINOR · 2 INFO) · 14 KILLED · 13 MISSED-RATIFIED (2 arbiter-original) · 7 UNPROVEN-NEEDS-LIVE.**
The component is **owned by no X wave** (X-W7's File Bounds stop at BrowsePane/PaletteCardMenu; its G13 mutation table — rename, tag, version, publish, unpublish, fork, vote, feature, delete, delete-all — has **no flag/report row**). Terminal shape: **X-W7 rider else boundary**, per the MiniColorPicker convention (COHESION §4).

## Ruled disagreements (the seats' actual conflicts, decided by this seat's own evidence)

| id | reader D&U | reader L&C | RULING | Deciding evidence (mine) |
|---|---|---|---|---|
| **Button `loading` cure** | endorsed in its CONFIRMED D-2 cell ("the primitive it hand-rolls") and in missed #1 ("keeps the control reachable") | KILLED (K2: no visible indicator) | **L&C wins, and both are extended**: `loading` renders **no indicator** (sole CSS: `cursor:progress`; template = `renderSlot` only) **and** — neither reader saw this — `blocked = loading ∥ disabled` binds as **native `disabled`** on a native button, so `:loading` removes the control from the tab order and blurs a focused button exactly as `disabled` does. The "keeps it reachable" limb of D&U's missed #1 is FALSE. | de-minified setup: `m = r(() => p.loading || p.disabled === !0 || …)`, `disabled: h.value ? m.value : void 0`, `aria-disabled` only for non-native. Cure as-written (bind `:loading`, delete Loader2) would delete the only visual mark AND eject focus mid-flight. Correct cure: `:loading` **plus** an in-slot indicator (producer-sanctioned — `.button > svg` sizing rules exist), **plus** the SS-6 producer ask: loading should suppress activation via `aria-disabled`+click-guard, not native `disabled`. |
| **C-17 `aria-modal`** | CONFIRMED (MINOR, PLAUSIBLE) | KILLED (K3) | **KILLED — L&C wins.** Not a defect, wrong owner, not consumer-actionable. | my greps: `aria-modal` = 0 across reka Dialog dist; `useHideOthers(currentElement)` at DialogContentModal.js:47 applies `aria-hidden` to siblings — the stronger inertness mechanism; deliberate, widely-adopted pattern; reka-owned, not glass-owned. INFO observation at most; no relay owed. |
| **D-2/C-4 severity** | BLOCKER | MAJOR | **MAJOR.** The wrong-outcome harms (mis-filed report, failure-as-success) are already carried at full BLOCKER weight by A-2/A-3; A-5's own harm is a missing affordance + premature erase. | roster A-5 |
| **D-17 reduced-motion limb** | carried (no M-xx names this file) | killed (M-03/M-29 family) | **KILLED — the Law governs.** No M-xx row names FlagReportDialog (my grep: 0), but the charge — "the central guard freezes rather than substitutes" — is the identical central-governance charge ruled FALSE at M-03/M-04/M-29/M-31. Re-carrying it under a new component name is the anti-rename failure. The untokenised `animate-spin` + physical `mr-2` limbs survive (A-27). | motion-quarantine.md:94-95/:120-122 read verbatim |
| **D §4 programmatic-focus "unhandled state"** | left UNPROVEN | KILLED (K9) | **KILLED.** `:focus-visible` deliberately not matching script-focus is the platform contract; the producer's only focus rule is `:focus-visible`-scoped (my read of radio-group styles). Not a defect, no live probe owed. | radio-group/styles.css |
| **D-7 · D-8 · L-9 severities** | MAJOR ×3 | MINOR ×3 | **MINOR ×3.** D-7: derivable, real, one-edit cure (A-16). D-8: the certain limbs are structural; the sliced-line pixel limb is residue (A-17). L-9: lattice hygiene, zero runtime harm (A-20). | roster |
| **D-10 homing** | consumer FAIL (MAJOR) | consumer limb MINOR + producer relay (K7) | **L&C wins.** `text-sm text-muted-foreground` is hard-coded in the producer's DialogDescription (my extraction); the consumer passes no class — charging the consumer with a per-instance-override violation for NOT overriding is incoherent. Consumer keeps the sibling-divergence limb (A-18); the off-matrix + muted-role default is an SS-6 relay row. | dialog-TNRDkcE4.js |

## Adjudicated defect roster

Line anchors are HEAD `f52d079a`; re-resolve before any cure (D-19). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

**BLOCKER**
- **A-1 (D-1/C-5/L-2)** — `variant="outline"`/`variant="destructive"` are dead props on glass-ui 7 Button (:38, :41-42); the destructive moderation commit renders byte-identical to Cancel while the producer's destructive rendering (`[data-tone="destructive"]` → filled `var(--destructive)`) sits unreached one selector away. Compiled props block has no `variant` (grep 0); defaults secondary/neutral. Repo-wide class: 51 sites/22 files (ledger-recorded). **Disposition: component sites → X-W7 rider else boundary; the 51-site sweep → boundary row (no X wave owns it — X-W8's re-point does NOT cure it, see KILL-1); recurrence-stopper = `vueCompilerOptions.strictTemplates` (repo grep: 0) → boundary row; producer dev-mode `variant` reject → SS-6.**
- **A-2 (D-3/C-1/C-14)** — form state unbound from its subject: `reason`/`detail` reset only in `onSubmit`'s finally (:97-98); Cancel emits close only (:38); BrowsePane never nulls `flagPalette` (my grep: set at :292, never cleared) so the instance outlives every palette — a cancelled report re-opens **pre-armed on a different palette with the previous reporter's prose**, and the flag insert is unique-indexed (service/flags.ts:38-48 → 409 forever; unfixable once mis-filed). Claim proven by C's probe P3 + source; D's own harness is circular (KILL-10) and the "one Enter files it" framing killed (KILL-13). **Disposition: X-W7 rider else boundary** (cure: `:key="paletteSlug"` at the host + close-transition reset).
- **A-3 (C-3/L-6/D-11 + r2-missed M4/M6)** — every API failure renders as success: `report()` swallows all (useAdminFlagged.ts:126-130), host closes unconditionally (:298-299); 401/400/404/own-palette/409 all live server paths (my read). Violates the cluster's own ruled F2 precept, written verbatim next door (useDialogBrowseActions.ts:41-45, :69) with the host rail (`onForkError → showFeedback`) already wired. Scope widened per r2-missed M4: **four** identical swallows in the same 150-line module (:75/:89/:101/:128 — one a destructive admin delete); the module posture is the unit. Second 400 path (routes/flags.ts:21-24 `z.enum` reject) is ungated and is exactly A-13's typo scenario. **Disposition: X-W7 rider else boundary** (error-surface arm is adjacent to W7 G13's charter, which currently lacks the flag row entirely).
- **A-4 (C-2)** — unbounded `paletteName` interpolation (:10-12; no break/clamp anywhere in file or demo styles) destroys the dialog at the API-legal 100-char max (schema.ts:25). Mechanism source-derived: DialogContent is a producer CSS **grid** (`grid w-full max-w-lg gap-4`, my extraction) whose items default `min-width:auto`, so one unbreakable token blows the track; page-level overflow oracle blind (overflow inside the fixed overlay). **Disposition: X-W7 rider else boundary** (name-length is the missing axis of W7's own "truth does not depend on how many …" charter).

**MAJOR**
- **A-5 (D-2/C-4/L-5/C-11)** — `submitting` + `Loader2` unreachable by construction (no `await`; emit synchronous; finally clears in the same tick — measured false at 24 samples/2.5s by C, at 4 phases by L); `onSubmit`'s async/try/finally is false ceremony; the finally **erases the form before the parent's request resolves** (the real in-flight appearance = a form deleting itself). Cure per the ruled disagreement above — `:loading` + in-slot indicator + producer focus-retention ask; never `:loading` bare. **Disposition: X-W7 rider else boundary + SS-6 (two producer asks).**
- **A-6 (r1-missed #1, arbiter-ratified with correction)** — the disabled Report button is **removed from the keyboard tab order**: `:disabled="!reason || submitting"` (:43) renders native `disabled` (my de-minify: `disabled: h ? m : void 0`, h true here), which is unfocusable; no explanation surface anywhere. The corpus's own evidence contains the defect scored as a pass — C's negative result #4 records the tab cycle "textarea → Cancel → Close → radio" and calls the trap intact; Report is absent from the cycle. Correction to r1: the producer's `loading` is NOT the reachable alternative (see ruled disagreement). **Disposition: X-W7 rider else boundary; the native-disabled-under-loading design → SS-6.**
- **A-7 (D-5 + r1-missed #3 ≡ r2-missed M2)** — adjacent radio hit targets overlap 13.05px ×3, and the mechanism is **inverted from D's telling**: the producer ships the compensating geometry (44px seat, 18px face, −13px offset, **gap 26px = seat−face** so seats tile exactly — my read); the consumer's `class="flex flex-col gap-2"` (:16, utilities layer beats components layer) collapses it to 8px, and the `:17-21` wrapper rows detach items from the group. No `pointer:coarse` gate — a mouse click 4px under "Spam" selects "Copyright violation". Cure = delete the class (restores 26px → pitch ≈ 49px > 44px seat, no overlap even with the wrapper rows); D's row/seat restructure killed (KILL-4). **Disposition: X-W7 rider else boundary; the edict-5 lesson (utility gap on a producer group) generalises — boundary note for any RadioGroup consumer.**
- **A-8 (D-6/C-8)** — radio group has no accessible name and `required` unannounced (:16 bare; producer RadioGroupProps carries `name`/`required`/`invalid`, all unused); the question is wired to the dialog (`aria-describedby` via reka), not the group. **Disposition: X-W7 rider else boundary.**
- **A-9 (C-7/L-4 name limb)** — the textarea has no accessible name at all (:29-34: v-model/placeholder/class/maxlength and nothing else; all four labels target radios); name falls to the placeholder, which evaporates on input. Statically decidable. **Disposition: X-W7 rider else boundary.**
- **A-10 (D-4/C-6/L-4)** — hand-rolled `<textarea>` (sole raw textarea in demo, census re-run: 1/0) where glass-ui ships a full `Textarea` via `/forms` (the seam the demo already uses for Input); carried T-census row D4 ("2W", t-card-color-census.md:93) still unexecuted two tranches on. Severity nuance ratified from r2: sibling row D3 rules the identical `bg-background` on ColorInput "a form-field, not a plate — acceptable as-is", so the **opacity** limb is weaker than the axes stated; the un-adopted-primitive limb is the finding. **Disposition: X-W7 rider else boundary.**
- **A-11 (D-9/C-16)** — below 640px the composition forks three ways with zero decisions, all producer defaults read verbatim by this seat: centred header over a left body (`text-center sm:text-left`), Report paints **above** Cancel (`flex-col-reverse`), and the two buttons stack **flush at 0px** (`sm:gap-x-2` — gap exists only where they don't stack); the only responsive statement in 101 lines is `sm:max-w-md` (:3). **Disposition: consumer composition → X-W7 rider else boundary; DialogContent size-axis + mobile footer-gap default → SS-6.**
- **A-12 (C-10/C-18 + r2-missed M6 limb)** — the single e2e gate asserts one fact (reason string on the wire) and stays green under ≥7 named mutations, six being live defects above; no unit test at any level (grep: 0); and the stub fulfils **200** where production returns **201** (spec:34 vs routes/flags.ts:31) — harmless only because nothing reads the status, which is the same vacuity twice. **Disposition: X-W7 rider else boundary** (W7's G13/G14 network-oracle idiom is the correct template).
- **A-13 (L-3/C-13)** — `FLAG_REASONS` has two homes (api model.ts:25-26 `as const` + z.enum at the wire; the component's plain array :79-84) with the type widened to bare `string` at every seam (:76, BrowsePane :296, types.ts, api/palettes.ts) and `maxlength="500"` a third hand-copy of schema.ts:72. A typo typechecks green and 400s at runtime — where A-3 reports it as success. Constraint verified: demo imports nothing from `api/` → the single home must be a new shared contract module (L-3's derivation form; C-13's third-literal cure killed, KILL-9). **Disposition: X-W7 rider else boundary** (contract-module altitude; note SS-4 CRUD/contract union as the eventual owner of the shared-contract shape).
- **A-14 (L-7) — OFF-COMPONENT, FOLD-IDENTITY.** tsconfig.demo.json#paths vs package.json#exports drift re-diffed at this HEAD by this seat: phantom `@mkbabb/value.js` root + `/parsing` + `/units` (targets absent from dist), untyped `/css` + `/value`; latent, not live. **Fact-identical to the ColorSpaceSelector adjudication's L-14 and X-W8:112's "L-22 ≡ W-HYGIENE, another owner" — ONE identity, already booked. Disposition: FOLD-IDENTITY → W-HYGIENE config parity. No new row (anti-rename).**
- **A-15 (r2-missed M1) — PRODUCER-OWNED.** glass-ui overrides its own `--radius` (0.625rem, `@theme` in theme/radius.css) with compiled-Tailwind stock defaults (`:root{--radius:0.25rem}` in components.css) imported at `layer(components)`, which outranks the theme layer (index.css:1 layer order — all three files read by this seat). Every `--radius`-derived token (input/button/lg) flattens to the shadcn-era 4px repo-wide; this is the true cause of L-8's 4px and of every "shadcn-era rounded-input" judgement in the corpus. L-8's consumer cure (define `--radius-input` in demo/styles) killed as a harmful consumer override of a producer token. **Disposition: SS-6 BJ-letter register (major producer row); consumer doc limb at A-21.**

**MINOR**
- **A-16 (D-7 + r1-missed #6 ≡ r2-missed M7)** — proximity inverted (question 24px from its options; unrelated optional field 12px), and the mechanism is a **second spacing system**: consumer `gap-3 py-2` (:15) + `gap-2` (:16) stacked on the producer grid's own `gap-4` + header `gap-y-1.5` — all four measured gaps decompose exactly (6 / 16+8 / 12 / 8+16, my arithmetic from extracted producer strings). The interior rhythm is the accidental sum of two systems; the cure is one spacing owner (delete `py-2`, let the grid's gap-4 be the section interval), not a re-tuned third number. **X-W7 rider else boundary.**
- **A-17 (D-8 rescoped)** — fixed `h-20` + `resize-none` + `maxlength=500` + no counter on a line-quantised control; producer ships `rows`/`resize:"content"`/LabeledField description. Structural limbs certain; the sliced-fourth-line and hiddenPx figures ride in residue. **X-W7 rider else boundary.**
- **A-18 (D-10 rescoped + r1-missed #2)** — the single load-bearing question is triply demoted: not the group's name (A-8), producer-defaulted below the closed type matrix (`text-sm`), **and painted in the muted colour role** (`text-muted-foreground` — the missed limb, confirmed from my extraction) while its answers are `text-small` at full ink; the sibling cures size only (`text-small font-display`, Migrate :8) so the two dialogs hold two jurisdictions; the DialogTitle override ships beside its own recorded producer-root candidate (:5-9). **Consumer consistency limb → X-W7 rider else boundary; the off-matrix + muted DialogDescription default → SS-6.**
- **A-19 (L-1 observation limb, RESCOPED from BLOCKER)** — 19 pure re-export barrels, zero `.vue` (my census), dual spelling for Dialog (3 barrel vs 2 subpath sites in one family). The mechanism and cure died (KILL-1). **Disposition: FOLD-IDENTITY → X-W8 (barrel subtraction is its charter: 19 barrels/92 imports). X-W8 must NOT claim this cures A-1 — it does not.**
- **A-20 (L-9)** — `useDialogBrowseActions` homed under `dialog/` + re-exported from the "dialog cluster" barrel, sole consumer a pane; docstring itself says host-agnostic; dead `PaletteDialog/` path still cited at useVersionHistory.ts:9-10 (my read). **X-W8.a census candidate (graph subtraction), else boundary.**
- **A-21 (L-8)** — DESIGN.md:195 asserts `--radius-input` = 8px; resolves 4px today (via A-15's producer conflict) and 10px after the producer cure — wrong under either cascade. Doc names tokens, not transcribed values. Ledger line-citations in L-8/L-9 are dead (KILL-12); cite by heading. **Boundary (doc-truth row), paired to A-15.**
- **A-22 (D-12/C-12/L-10)** — `paletteSlug` required, destructured, never read (:68/:71 only); host re-derives its own copy (:298). Dies free with A-2's `:key` cure (the prop finally gets the identity job). **X-W7 rider else boundary.**
- **A-23 (D-13)** — five lines of manual `:open`/`$emit` plumbing (:2, :68-77) beside a sibling using `defineModel` (Migrate :2/:58 — D's :53 citation drifted, KILL-11); with `defineModel` the close transition A-2 needs is a single watchable. **X-W7 rider else boundary.**
- **A-24 (D-14)** — ASCII straight quotes around the palette name in display prose (:11); collides with names containing quotes. **X-W7 rider else boundary.**
- **A-25 (D-15/C-15)** — the Close affordance is a 16×16 box (producer source: no padding utility, icon-sized — my extraction), last tab stop, against the 44px seat the same producer gives the radios in the same dialog. WCAG 2.5.8 framing killed (KILL-5; spacing exception satisfied — isolated corner target); internal-consistency framing carried. **SS-6 (producer-owned).**
- **A-26 (D-16)** — "Other" imposes no obligation: `("other", undefined)` is a submittable moderation record with no information (:83, :43 sole gate, :94 normalises blank→absent, server `.optional()` agrees); producer `requirement` seam exists. **X-W7 rider else boundary.**
- **A-27 (D-17 rescoped)** — `animate-spin` is a raw Tailwind keyframe, not a house token, and `mr-2` is a physical margin in an RTL-booked codebase (:46); reduce limb KILLED per M-03/M-29 (Law). Moot while A-5 keeps the node unreachable; dies inside A-5's cure. **Rides A-5.**
- **A-28 (r2-missed M5)** — hard-coded document-global ids `reason-${value}` (:22-23) on a component exported through two public barrels; two instances = duplicate ids, labels bind to the first. `useId` usage in demo: 0 (my census). **X-W7 rider else boundary.**
- **A-29 (r1-missed #5)** — glass-ui `Label` (with the `requirement` seam A-26 needs) is re-exported by the demo's own barrel and used **zero** times (my census); all three axes routed labels to LabeledField and never named it. **Rides A-8/A-26's cure; else boundary.**
- **A-30 (r1-missed #4)** — sibling geometry vocabulary divergence: `sm:max-w-md` (breakpoint-gated, radius inherited) vs Migrate's `rounded-dialog max-w-sm` (unconditional, radius declared) — two dialogs, one folder, three disagreements; producer has no size axis, so a house convention is the only discipline and there is none. **X-W7 rider else boundary; size-axis ask rides A-11's SS-6 row.**

**INFO**
- **A-31 (C-19)** — the Report menu item survives a successful report (PaletteCardMenu gate has no flagged term; nothing writes one back; the `{flagged:true}` response is discarded) — the natural second act is a 409 rendered as success by A-3. **Rides A-3.**
- **A-32 (D-18)** — the component has never been judged against a rendered frame (0 of 60 captures; entry gated behind a live commons), and the prior audit **propagated the unreachable spinner as the exemplar** (Ad-interactive-states.md:242 verbatim, re-read by this seat; :201 cites this file as "the correct minimal form"). Corrective: overlay/state-driven capture rows. **X-W1 harness input (visual-golden matrix must drive overlays, not only routes).**

## Killed-claims register

1. **L-1 mechanism + L-2/greenfield-1 cure ("type-erasing hop"; "subpath imports make the 51 sites compile errors") — KILLED, decisively.** TS re-exports are type-transparent; the decisive counterexample needs no barrel at all: EmptyState.vue:45-47 passes `tag="div"` three times through a **direct** `@mkbabb/glass-ui/watercolor-dot` import (:70), the producer d.ts has no `tag`, and vue-tsc exits 0. Unknown attrs are Vue fallthrough by design; deleting `demo/ui/` changes zero typecheck outcomes. The real gate hole is `strictTemplates` (repo grep: 0), which the ledger itself had already named and L dropped. Observation limb survives at A-19 (MINOR).
2. **The `Button :loading` cure as written by all three axes and endorsed in r1's confirmed cell — KILLED-AS-STATED, twice over (this seat's own extension).** (a) No visible indicator ships (`cursor:progress` is the entire CSS; template renders slot only). (b) `loading` feeds the same `blocked` computed that binds **native `disabled`** on native buttons — it ejects a focused control from the tab order mid-flight, reproducing A-6/C-9's failure inside the cure. Correct form: `:loading` + in-slot indicator + SS-6 ask (aria-disabled semantics for loading).
3. **C-17 (`aria-modal` missing) — KILLED as a defect** (r1's CONFIRMED overruled): reka applies `useHideOthers` (aria-hidden siblings) deliberately; reka-owned; INFO at most, no relay.
4. **D-5's row/seat restructure cure — KILLED.** The producer already computes the exact compensation (gap = seat − face = 26px, my read); the consumer's one class defeats it; the cure is a deletion. D's "the consumer sets the row rhythm" mis-attributes: a correct rhythm was overwritten, not never chosen (edict 5 in its purest form).
5. **C-15's WCAG 2.5.8 normative framing — KILLED** (spacing exception: isolated corner target passes). Ergonomic/internal-consistency framing carried at A-25.
6. **D-17's reduced-motion limb + D §7's §6 FAIL row — KILLED per the motion-quarantine family (M-03, M-04, M-29, M-31 FALSE: central governance is the design).** No M-xx names this file; the charge is the same charge. Law satisfied by citation.
7. **D §4's "focused (programmatic) unhandled" row — KILLED**: `:focus-visible` not matching script focus is the platform contract; deduct one from D's "nine unhandled states".
8. **D-10's `text-sm` consumer-FAIL limb — KILLED (mis-homed)**: the default is hard-coded in the producer's DialogDescription; the consumer passes no class; Edict 5 cannot charge a consumer for not overriding. Producer relay + sibling-consistency limb at A-18.
9. **C-13's cure (a third hand-written literal union) — KILLED**: re-commits the two-homes defect as three; L-3's single-home derivation adopted (A-13).
10. **D-3's own evidence — KILLED as proof (claim rescued).** D's harness mounts its own app instance and flips props on it; it can only ever measure "state persisted". A-2 stands on C's probe P3 (real UI) + source structure.
11. **D's sibling citations (`Migrate :19,26,32` · `:53` · `:8-10`) — KILLED as coordinates** (actual: :15/:24/:32 · :58 · :7-10, my sed reads); C cited the same sites exactly. Substance survives; provenance drifted on the axis whose method was live probing — recorded against AXIS-PROMOTIONS coordinate durability.
12. **L-8/L-9's DEFECT-LEDGER line citations (`:72804`, `:13805`) — KILLED as coordinates** (regenerated ledger; contents elsewhere; underlying facts independently true). Standing rule: cite ledger findings by heading, never by line.
13. **D-3's "One Enter files it" — KILLED as input mechanics** (initial focus = first radio; Enter doesn't activate radios; no form ancestor; filing needs a click or Tab→Report). The BLOCKER stands without it; wave specs must not inherit the framing.
14. **D §2.2's "in dark mode the affirmative button is Cancel" — KILLED as causation**: the asymmetry is the `disabled` state (glass-capsule-hover gated on `!disabled`), identical in light; with a reason chosen the buttons are byte-identical in both schemes — which is A-1, and the dark frame adds nothing.

## Missed register (ratified into the roster above)

r1: A-6 tab-order removal (with this seat's loading correction) · A-18's muted-colour limb · A-7's producer-gap inversion (≡ r2 M2) · A-30 sibling geometry · A-29 Label unused · A-16's dual spacing system (≡ r2 M7). r2: A-15 producer `--radius` conflict · A-7 mechanism (shared credit) · C-9 mechanism correction (carried into residue #1) · A-3's module-posture widening (M4) · A-28 static ids (M5) · A-12/A-3's second-400 + 200/201 stub (M6). **Arbiter-original (this seat): (i)** `loading` binds native `disabled` on native buttons — the corpus's converged cure ejects focus and the producer relay must carry the aria-disabled ask; **(ii)** the flag mutation is absent from X-W7's own G13 mutation-ownership table — the routing hole is itself a COHESION §4 finding: the one wave chartered "every mutation has exactly one owner and a visible result" does not list this mutation.

## Superlatives (provenance preserved)

1. **glass-ui's radio-group stylesheet** (r2; re-derived by this seat) — face/seat/offset/gap as one token-derived system with forced-colors handling: the exact three-quantity separation PROPORTION-AUDIT §5 law 7 demands, defeated here by one consumer class, not by producer neglect.
2. **useDialogBrowseActions' F2 precept-carrying comment** (both readers; read verbatim by this seat) — the house wrote its rule down next door and implemented it, which is the only reason A-3 is provable rather than arguable.
3. **The C seat's control discipline** (both readers) — the WebKit falsification of the SwiftShader "stalled transition", published as a negative with a fleet warning, plus its own Escape-retraction: L-18's job done unprompted.
4. **api model.ts:25-26 + schema.ts:70-73** (r2) — one home, one derivation, wire-enforced; the server half of the contract A-13 says the demo failed to consume.
5. **The two hand-copied constants are both semantically right** (r1) — `maxlength=500` matches the schema to the character and `trim() || undefined` matches `.optional()`'s meaning, not just its number.
6. **The provenance comment at :5-8** (r1) — names the tranche, the overridden default, the intent, and its own retirement condition; the form to propagate even while A-18 discharges the debt.
7. **Structural hygiene** (r1/C) — 101 lines, zero hooks/listeners/observers/timers/rAF: constitutionally immune to the PRM-RAF class; and **vite.config.ts's generated alias set** (r2) — the pattern A-14's identity-fold simply asks tsconfig to copy.

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **C-9 focus-after-submit asymmetry** (BODY vs restored trigger). Leading mechanism now r2-M3's: the finally clears `reason` → `!reason` → native `disabled` on the focused button → blur to body a round-trip before close; reka's `triggerElement` is null on BOTH paths (programmatic open, no DialogTrigger), so the measured Cancel-restore itself needs re-driving. NOTE: the naive `:loading` cure reproduces this via KILL-2(b).
2. **D H-1** — dialog possibly opening with a reason pre-selected (an unchosen moderation report). Both readers suspect harness artifact; source offers no mechanism. One deterministic probe owed — severity justifies it.
3. **Dark-scheme readings** — textarea `rgb(11,10,9)`, ΔL≈0.36; structural half (opaque `bg-background` in a glass overlay) already confirmed at A-10.
4. **D-8's sliced-fourth-line + scrollH/hiddenPx figures** (font-metric dependent); structural limbs already carried at A-17.
5. **H-2 forced-colors** — narrowed: the radios ship a producer forced-colors block (my grep); the raw textarea's `bg-background`/`border-input`/`ring-ring/40` remain unaudited under HCM.
6. **H-3 200% zoom / text scaling** against the fixed `h-20`.
7. **D §2.1's one-session ratios** (textareaPctOfDialog 17.5 et al.) — qualitative claims independently confirmed; the specific numbers are single-probe.
All seven ride A-32's corrective: the X-W1 visual-golden harness must capture overlay states (this dialog's ~8 states × 4 matrices), not only routes.

## Closing verdict

**DEFECTIVE — ADJUDICATED-REAL.** Four BLOCKERs survive hostile re-reading and this seat's counter-challenge (dead destructive register · identity-unbound state · failure-as-success · unbounded subject interpolation), on a moderation surface with an immutable server record; the corpus's single converged cure was itself defective in a way only the compiled producer bytes reveal; and the component is claimed by **no X wave** — every consumer row routes **X-W7 rider else boundary** (COHESION §4 accretion), with A-14/A-19 identity-folded to their existing owners, five producer rows to the SS-6 BJ letter, and the G13 flag-mutation hole recorded as this apotheosis's own contribution to the cohesion table.

— end of apotheosis. The r2 challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## ADDENDUM (E-3, appended — the record above is dated evidence and is not rewritten)

**Seat**: a SECOND fresh Fable adjudicator · served model: `claude-fable-5` (observed in this seat's own system prompt, not inherited) · **Date**: 2026-08-05 · **HEAD**: `25317e31` (tree moved `f52d079a` → `25317e31`; FlagReportDialog.vue **byte-stable** — every roster anchor re-read verbatim by this seat before any ruling below).

**1 · Convergence attestation (a receipt about the fleet, recorded per the ColorSpaceSelector DISSENT convention).** This seat re-ran the adjudication blind — full re-reads of the subject, host wiring (BrowsePane :168-173/:288-299, no null-assignment), useAdminFlagged's four swallows, the api contract (model :25-26 · schema :25/:70-73 · routes :14-31 · service :28-48), the sibling's exact lines (:2/:3/:7-10/:15/:24/:32/:58), the 49-line e2e spec, compiled `button-Bu9F4uU6.js` (props block, `m = loading || disabled`, `disabled: h ? m : void 0`, slot-only body), button/radio-group/dialog producer CSS, reka Dialog dist (`aria-modal` 0 · `useHideOthers` at DialogContentModal.js:47), the radius layer-cascade triple, the exports↔paths diff, motion-quarantine (M-03/M-29 verbatim; `FlagReport` hits 0), t-card-color-census :92-93, Ad-interactive-states :201/:242, and all four demo censuses (`<textarea` 1 · `Textarea` 0 · `Label` consumers 0 · `useId` 0 · demo/ui 19 dirs / 0 `.vue`) — and independently reached the same 4-BLOCKER roster, the same 14 kills, and **the same two arbiter-original finds** (the `loading`→native-`disabled` KILL-2(b) extension; the X-W7 flag-mutation ownership hole). Two independent Fable seats, disjoint sessions, one verdict: the record above is convergence-hardened, not single-witnessed.

**2 · Correction — A-1's recurrence-stopper IS wave-owned (routing delta, verified from the wave spec itself).** The roster row A-1 and KILL-1 book `vueCompilerOptions.strictTemplates` as "→ boundary row". Wrong: **X-W7 owns it by name** — scope 1 closes "the drift class structurally by turning on `strictTemplates`", G3 gates it (population MEASURE-AT-OPEN), `tsconfig.base.json` sits in W7's §4 File Bounds, and W7's own Triumvirate clause anticipates the surfaced-drift halt. The AdminFlaggedPanel adjudication (AF-6) already routes its variant sites through the same G3. **A-1's disposition is amended: recurrence-stopper → X-W7 G3 (owned, gated), not a boundary row.** The component-site cure and the producer dev-reject relay stand as written.

**3 · Correction — the 51-site `variant` fleet is an ALREADY-RULED identity (anti-rename guard).** A-1 books "the 51-site sweep → boundary row (no X wave owns it)". The fleet identity is **MT-BTN1**, ruled at the AdminUsersPanel adjudication and cross-booked at AdminFlaggedPanel AF-6 ("fleet = the ALREADY-RULED MT-BTN1 identity — one identity, no re-book") and at MigratePalettesDialog A-5 (family → X-W8/CC-081 as its cure home). **A-1's fleet limb is amended: FOLD-IDENTITY → MT-BTN1; no new boundary row** — exactly the guard this record itself applies at A-14/A-19.

**4 · One census refinement.** The instruments line records "`strictTemplates` anywhere = 0". True over `tsconfig*`/`package.json` (still un-enabled at `25317e31`, re-verified) — but the string is present in the tree at X-W7.md (scope 1, G3) and in DEFECT-LEDGER prose: the gate is authored, not absent. The absence-of-enablement fact A-1 relies on is unchanged.

**5 · Everything else stands as written.** No roster row, kill, superlative, or residue item required amendment at `25317e31`; the NO-WAVE-OWNER/"X-W7 rider else boundary" shape, the SS-6 producer register (A-15 radius cascade re-verified from all three files this session), and the A-32/X-W1 overlay-capture worklist are re-affirmed.

— end of addendum. Nothing above this section was altered.
