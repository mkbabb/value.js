Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component CubeScene)

# kf-CubeScene — adjudicated apotheosis

**Date** 2026-08-07 · **Corpus** `audit/kf-components/CubeScene/challenge-{D-design,L-library,C-consumption}.md` (all three `claude-opus-5[1m]`, dated 2026-08-04; L and C are two-round files) · **Re-readers adjudicated**: design-and-usage (reader A, Fable) + library-and-code (reader B, Fable), both hostile, returns consumed whole.

**Substrate ruling (read first — it decides one BLOCKER in each direction).** The local checkout `/Users/mkbabb/Programming/keyframes.js` is the SACRED stale tree `8281638c`, 41 commits behind `origin/master` `81a56990`, with a 252-file dirty worktree. Reader B filed the dirty worktree as a corpus-invalidating BLOCKER (its M-1: "the corpus audited uncommitted code not on master"). **This seat's own diff INVERTS that ruling**: `git diff origin/master --name-only -- demo/scenes/cube/ demo/state/ demo/app/dock/MbabbMenu.vue demo/components/instrument/transport/` returns **zero cube-unit files** — `demo/scenes/cube/**` (all of it, including the `useTransformState.ts` "rewrite"), `controlOptionsStore.ts`, `MbabbMenu.vue`, `RibbonBar.vue`, `ChannelControls.vue`, `ControlsPaneWrapper.vue` are **byte-identical between the worktree and `origin/master`**. The corpus therefore audited TRUE-HEAD content; the stale artifact is the local *commit*, not the audited tree. Every reader-B "describes uncommitted code" caveat (on L-5, L-9, L-14, C-7, P-1, P-6) is **VACATED**. The residual worktree-vs-frontier drift in the blast surface is 5 files / +10 −78 — `App.vue` and `EditorShell.vue` comment rewording + a `HeaderRibbon` ref/`mode` line (this seat's diff; no topology change), plus **package.json/lock: the worktree DELETES `origin/master`'s exact devDep `"@mkbabb/glass-ui": "7.0.0"`** — an uncommitted regression, and the only place the "phantom dependency" exists at all.

The manifest trifecta (all three probed by this seat): worktree = **no glass-ui declaration** (the uncommitted deletion) · local HEAD `8281638c` = `optionalDependencies: { "@mkbabb/glass-ui": "6.0.0" }` with a full lock entry · `origin/master` = `devDependencies: { "@mkbabb/glass-ui": "7.0.0" }` exact — **matching the installed 7.0.0**. Consequences: axis L's L-1 BLOCKER is dead three ways; reader B's M-2 ("every glass-ui verdict measured against a version `npm ci` would not install") dies at the frontier — the corpus's glass-ui API verdicts were measured against **exactly the version true HEAD pins**, and the "verify against 6.0.0" residue is discharged; reader B's M-3 (optionalDependencies hazard) is a local-HEAD-only artifact. All line anchors in this record bind to the worktree ≡ `origin/master` content for the cube unit; KF.W0's re-anchor law applies before any cure executes.

**Method.** Every verdict-level disagreement between the two readers was re-derived by this seat against the tree with its own instruments, never vote-counted. Instruments this session (static + repo-local only, zero browser, zero execution against product source — probe parsimony): the full three-file challenge corpus read whole; `git rev-parse HEAD` / `git status --short | wc -l` (252) / `git show HEAD:package.json` / `git show origin/master:package.json` / `git rev-parse origin/master` / `git diff origin/master --stat|--name-only` over the blast surface and whole repo / `git diff origin/master -- demo/app/App.vue demo/components/instrument/shell/EditorShell.vue` (the topology decider); **EditorShell.vue read whole** (the `#target`→`AnimationControlsGroup :key="superKey"` remount decider, `:74-104`); App.vue:20-110 + `:185-300` greps (`activeSceneKey`/`activeSceneComponent`/`currentSuperKey`); scenes.ts:115-150 (`homeScene.superKey = "home"`, cube `superKey: CUBE_SCENE_ID`); useSceneMachineShellBinding.ts:125-147; MbabbMenu.vue:85-105 + controlOptionsStore.ts:60-100 (the C-1 throw mechanism, re-read at source); useTransformState.ts:165-175 (the endpoint collapse); shipped-CSS extractions from `dist/gh-pages/assets/index-CL_QYCiO.css` — the universal PRM reset `:not([data-allow-motion]){…animation-duration:.01ms!important…}` (present; the D-16/D-8 decider), `.icon-sm,.icon-sm svg{width:calc(var(--spacing)*4)}` = **1rem** vs `--icon-sm:.875rem` (the D-25 decider), `.cube-side[data-v-8a65c7ef]{…transition:--lit .16s linear…}` (the M-5 spot-check); glass-ui `dist/styles/utilities/a11y-overrides.css` head (source of the PRM reset); RibbonBar.vue icon-class grep; identity-guard greps across every banked kf-* apotheosis (`vue-tsc`, `tabs-content`, `isPlaying`, `pinch-zoom`, `SCH-1|phantom`) + targeted reads of kf-App.md rows KF-APP-1/-4/-12/-14/-17/-30/M-A and kf-AnimationControlsGroup.md D-1/M-9/L-4. Where BOTH readers independently verified the same producer/tree fact and this seat's spot-checks found no crack (the compiled Button `emphasis: {default:"secondary"}`, the HoverCardTrigger `as:"a"` default, the InputProps member list, the D-7 luminance table, the C-2/C-15/C-16 greps), the corroborated cell is adopted without a third derivation.

## Scoreboard

**49 rows ADJUDICATED-CONFIRMED (0 BLOCKER · 15 MAJOR · 23 MINOR · 11 INFO) · 12 killed claims registered · 9 net-new misses adopted into the roster (6 reader-A · 2 reader-B · 1 this seat's own) · 10 UNPROVEN-NEEDS-LIVE residues for SS-13.**

Headline survivors: **the matrix-editor defect family** — five MAJORs rooted in one 227-line composable (`useTransformState.ts`): NaN-writing reset (L-3), the T·R·S rebuild that both destroys hand-edited cells AND collapses the Matrix channel to zero-delta (L-4+M-6), free-text cells that throw once per rAF frame (L-5/C-6), unowned undisposed tweens (C-7/L-13/L-14), and deep-ref + per-frame recompile (L-9) — plus **3× MatrixEditor** through the ungated per-channel slot (L-6/C-15, the same portal architecture banked as ACG D-1).
Headline kills: **all three corpus BLOCKERs are dead or demoted** — L-1 (phantom dep) is substrate-dead thrice over; C-1 (ppMode) demotes and folds to the banked KF-APP-1; D-1 (keyboard-unreachable toggle) grades unmounted code. And **reader B's substrate BLOCKER inverts**: the corpus audited the frontier, not a phantom.
Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; routings below are agglomeration input (M-25) for the SS-1/SS-2 spec authoring, keyed to the census wave sketch KF.W0–KF.W10.

## Ruled disagreements (this seat's actual work)

| dispute | reader A (design-and-usage) | reader B (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L-1 phantom dep** | CONFIRMED BLOCKER (with a CI-path softening) | KILLED (declared at local HEAD; npm-ci mechanism wrong; demo build off the merge path) | **KILLED — B wins, extended by the frontier** | Triple refutation: worktree-only absence is an **uncommitted deletion of `origin/master:package.json`'s exact devDep 7.0.0** (my `git show` + `git diff origin/master -- package.json`); local HEAD declares optionalDep 6.0.0 (falsifier (a) of the challenge satisfied verbatim); and `npm ci` reconstructs node_modules from the lock — it never resolves an import specifier, so the stated failure mechanism is wrong even on the worktree. Identity: ≡ census **F-1/SCH-1**, already ruled FALSIFIED-at-HEAD at kf-AmigaScene — ONE identity, folded, never re-booked. Reader A confirmed a manifest state that exists only in an uncommitted local edit. Residue → KF.W0. |
| **M-1 substrate** (B's own BLOCKER) | not raised | BLOCKER — "the corpus audits a 252-file dirty tree; the cube core differs from master" | **RESCOPED-BY-INVERSION** | My diff: the cube unit + every load-bearing seam file is **byte-identical worktree↔`origin/master`**. B compared against the WRONG baseline (the stale local commit, 41 behind). The corpus audited true-HEAD content; B's per-finding "uncommitted code" caveats are vacated, and B's own corollary ("P-1's adoptCompiled watch never fires at HEAD") is vacated with them — at the frontier the watch fires exactly as the corpus says. Surviving residue: the checkout schism itself + the uncommitted glass-ui devDep deletion (would revive F-1 if committed) → KF.W0 §B-12. |
| **L-7 remount topology** | remount HAPPENS (recorded its own failed kill attempt) | remount DOES NOT happen on home↔cube (killed L-7's scenario; filed M-4 against the in-tree comment) | **A wins — this seat's own EditorShell read** | EditorShell.vue:74-104: the `#target` slot is forwarded into `<AnimationControlsGroup :key="superKey">`'s `#animation-content` outlet (`:100-102`) — slot content renders INSIDE the keyed subtree, and slot scope is irrelevant to lifecycle. `homeScene.superKey = "home"` (scenes.ts:126-134), cube `superKey = CUBE_SCENE_ID` (scenes.ts:138-144); App.vue:191 forwards it. The key changes on the home↔cube crossing → the whole subtree (CubeScene included) is destroyed and recreated. B's App.vue-Suspense argument is true but subordinate: the Suspense key ("cube") never changes, but its keyed ANCESTOR does. **L-7 stands in full, original home-hero scenario included; B's M-4 premise and its L-7 "fires only on cube→other→cube" correction are REFUTED.** The in-tree comment (useSceneMachineShellBinding.ts:133-144) is mechanically TRUE — its `__home__`/`Cube` literals are stale prose (already booked at kf-App), its remount claim is not. |
| **PRM universal reset** (D-16's PRM half / D-8's falsifier) | KILLED D-16's PRM half — a universal reset EXISTS in the shipped CSS | CONFIRMED D-16 whole, "no global PRM reset exists … consistent with D-8's enumeration" | **A wins — this seat's own extraction** | The shipped stylesheet contains `@media (prefers-reduced-motion:reduce){:not([data-allow-motion]){transition-property:opacity,color,background-color,border-color,box-shadow!important;transition-duration:.1s!important;animation-duration:.01ms!important;animation-iteration-count:1!important}…}` (my grep -o against `index-CL_QYCiO.css`; source = glass-ui `dist/styles/utilities/a11y-overrides.css:1`). `!important` on a universal selector wins irrespective of layers; nothing in the demo carries `data-allow-motion`. The Loader2 `animate-spin` is a CSS animation → fully neutralised under PRM → **D-16's motion half is dead**. D-8 SURVIVES only because the Roll + the two `useTransformState` tweens are engine motion (WAAPI `Element.animate()` / rAF), which CSS `animation-duration` does not govern — but D-8's written falsifier ("I enumerated all 46 PRM blocks; no global reset") is false, D-16 was built on it, and **reader B repeated the false cell as corroboration** — a recorded citation-inheritance event (B's D-16 row and its D-8 row both inherit the corpus's enumeration without re-deriving). |
| **D-25 icon sizing** | KILLED the challenge's "no visual consequence" clearing — the class and the token differ | silent | **A wins — this seat's own extraction** | Shipped CSS: `.icon-sm,.icon-sm svg{width:calc(var(--spacing)*4);height:…}` = **1rem/16px**; the `--icon-sm:.875rem` token is a separate variable consumed elsewhere. RibbonBar's icons use the CLASS (`class="icon-sm"`, my grep at RibbonBar.vue ~:22/:30/:42); CubeScene's use `w-3.5 h-3.5` = .875rem/14px (my grep, `:190/:197`). A real **12.5 % delta inside the very strip D-3/C-4 charge** — promoted from "cleared authority nit" to a fourth divergence axis on the D-3/C-4 row. |
| **C-3 consequence half** | KILLED (default emphasis is "secondary", not "primary"; C-3 read a d.ts type list as a value) | same kill, independently | **Kill CONFIRMED, unanimous** | Both readers read the compiled `dist/button-B7c944jy.js` props block (`emphasis: { default: "secondary" }`) independently; corroborated-cell adoption. The cube's buttons render at the SAME emphasis every house RibbonBar button passes explicitly — no loudness delta. D-2's mechanism (phantom `variant` prop, DOM fall-through, glass-wash capsule instead of an outline) survives on D-2's reading. |
| **M-6 endpoint collapse** (B's escalation of the corpus's own §7.3 kill) | not raised | the corpus's "defensible rebase" kill is wrong — writing the SAME matrix into BOTH endpoints is a collapse | **B's escalation ADOPTED, folded into L-4** | My read of useTransformState.ts:171-172: `matrix3dEnd.value = createMatrix(transformationMatrix); matrix3dStart.value = createMatrix(transformationMatrix);` — identical values into both keyframe endpoints, immediately recompiled into the Matrix channel (useCubeDemo.ts:57-71). A rebase moves the START; this zeroes the DELTA — any pre-start drag flat-lines the Matrix channel, and the failure reads as "the matrix animation is broken", not "the drag did it". One root (`:129-175`), two symptoms (cell destruction + channel collapse) = ONE roster row. |
| **D-9 severity** | MINOR (the px/rem mixture is house-wide — 7+8 blocks in the shipped sheet; the challenge itself concedes it) | MAJOR held | **MINOR-RESCOPED — A wins** | The corpus's own `:271` concession names the idiom house-wide; a house-wide unit-policy defect booked MAJOR against one component is the scope error the ColorSpaceSelector L-9.1 law exists for. The engineered-against H3 collision keeps the cube as the sharpest witness; the row books MINOR here with a house-wide breakpoint-unit rider, collision render → SS-13. |
| **C-1 severity/attribution** | MAJOR as a CubeScene finding (throw site is MbabbMenu; "ownership borrowed") | same demotion | **MAJOR, unanimous — and the row is ALREADY BANKED** | Identity guard: ≡ **KF-APP-1** (kf-App.md:41 — the throw message pinned by that seat's own node execution; `MbabbMenu.vue:98-101` byte-identical at worktree, HEAD, AND `origin/master`; "zero working writers … the `setPPMode` twin must be disposed in the same motion as the KF-APP-17 headerLeft sweep"). My re-read of MbabbMenu.vue:98-101 + controlOptionsStore.ts:66-96 confirms the mechanism (plain-bucket return; `stored.value` undefined). FOLD by reference — this record adds only the CubeScene-side annotations (the `:63` seeding ownership; the C-14 bucket-split interplay: a repaired MbabbMenu writes whichever bucket is CURRENT, which on home is not the bucket CubeScene reads). |
| **D-1 BLOCKER + the headerLeft cohort** | demoted to latent (C-2 scope), mechanisms preserved at MINOR/INFO | demoted whole-cohort to INFO (dead code) | **Demotion CONFIRMED; ≡ KF-APP-17; B's flat-INFO grading adopted with A's mechanism preservation** | C-2's grep is exhaustive and both readers re-ran it (5 hits, zero mounts; App.vue fills six named slots, never `#header-left` — my App.vue:20-102 read corroborates). Unmounted code cannot carry a live WCAG failure: D-1, D-4/L-30, D-5/L-25/C-19b, D-6, D-12, D-13, D-14, D-15, L-24 all grade **INFO-latent**, recorded as the KF-APP-17 sweep's decision checklist (fill-vs-delete). **This seat's net-new composition (ARB-1, below) settles the fill-vs-delete question harder than either reader did.** |
| **D-5 severity** (unnamed `role="group"`) | mechanism confirmed; MAJOR killed to INFO (no SC failure; the explicit `role:"card"` is a no-op on the hover branch) | INFO | **INFO, unanimous after A's kill** | `group` is a structural role ARIA does not require a name on; the corpus's "strictly worse than no role" has no criterion behind it, and L-25 (INFO) graded the same evidence correctly in the same corpus. Latent regardless (C-2). |

## Adjudicated defect roster

Identity-deduped: one row per defect, all axis ids attached. Anchors bind to the worktree ≡ `origin/master` cube unit (re-anchor before any cure — KF.W0 law). Routing targets = the census wave sketch (KF.W0–W10); **NO-WAVE-OWNER** rows are SS-1/SS-2 spec-authoring intake — they agglomerate into one *cube scene-repair wave* the forming specs must create, with the matrix-editor family as its core.

### BLOCKER

*None survive at CubeScene scope.* The corpus's three BLOCKER candidates: L-1 killed (substrate, three ways), C-1 demoted MAJOR and folded to the banked KF-APP-1 (the throw is real, shipped, and user-reachable — but it is the dock's row, already adjudicated), D-1 demoted to latent (unmounted). The nearest live BLOCKER-weight condition adjacent to this component is ACG D-1 (three stacked timelines), banked at kf-AnimationControlsGroup.

### MAJOR

- **C-2 / KF-APP-17 — CONFIRMED (MAJOR, FOLD by reference).** `headerLeft` (CubeScene.vue:118-144, exposed `:252`) projects into `EditorShell.vue:18`'s `#header-left`, which App.vue — the sole EditorShell consumer — never fills. ~47 lines + 3 of 4 glass-ui imports unreachable; demotes the entire popover cohort to latent. The single most consequential reachability finding in the corpus, and the question axis D never asked. **Disposition: ≡ KF-APP-17 (banked, NO-WAVE-OWNER, one motion with KF-APP-1); this record contributes the latent-cohort checklist (INFO row below) + ARB-1's composition law.**
- **C-1 / KF-APP-1 — CONFIRMED (MAJOR at this scope, FOLD by reference).** `ppMode` has zero working writers: CubeScene's own `setPPMode` (`:85-87`) is dead inside the unmounted `headerLeft`; MbabbMenu's `togglePpMode` throws `TypeError` on every click (`stored.value` undefined off the plain-bucket store return — this seat re-read both files). Every ppMode-dependent surface (CubeTarget.vue:53/:82 face branch, `.ppmycota-cube` CSS, MISS-4's relit interplay) is dead render. **Disposition: ≡ KF-APP-1 (banked, NO-WAVE-OWNER; cure law: never land two writers). CubeScene-side rider booked here: any revived writer must resolve the C-14 bucket split (home writes "home", CubeScene reads "cube") in the same motion.**
- **L-2 / C-5 — CONFIRMED (MAJOR).** `isPlaying` (CubeScene.vue:65, exposed `:250`) has no writer anywhere: not in `SceneExposedApi` (sceneExposedApi.ts:16-34), not in the shell binding (the only write-back is `isStarted`, useSceneMachineShellBinding.ts:264-265). Three consumers dead-end: the `.idle-hover.playing .cube{will-change:transform}` compositor promotion never fires (CubeTarget.css:58-61 — the exact G5 perf contract the rule documents); `applyTransformToContainer` degenerates to `isStarted` alone; `useTransformState`'s first parameter `isGroupPlaying` is never read in the 227-line body. Bonus prose contradiction: SquareScene cites a "cube/amiga contract: onPlayStateChange writes isPlaying" that exists in no scene. **Disposition: NO-WAVE-OWNER (delete-or-wire decision: `facility.isPlaying()` exists; the SquareScene prose corrects in the same motion). Cross-ref: the `isPlaying` shadow family also appears at the banked kf-AnimationControlsGroup/kf-AnimationVisualizer rows — the cure must pick ONE playing-state authority.**
- **L-3 — CONFIRMED (MAJOR, reader-B escalation adopted).** `syncTransformations(reset=true)` writes `Math.acos(m00)` into `rotate.x` — NaN whenever scale > 1 (bounds `[0.4, 3]`, user-settable), and **radians into a degrees field** (57.3× dimensional error, masked only because reset targets identity). Reached from the ribbon Reset button CubeScene itself renders (`:186-190`), per frame for 500 ms. **B's escalation stands**: the NaN fires the pre-start deep watcher → `mat4.fromXRotation(NaN)` → all-NaN composite → `createMatrix` throws `TypeError` from inside a rAF callback (P-6's own boundary validation is the thrower). **Disposition: NO-WAVE-OWNER (matrix-editor family; the decomposition needs a real polar/Euler extraction or removal — `acos(m00)` is not a rotation read).**
- **L-4 + M-6 — CONFIRMED (MAJOR; the corpus's own "defensible rebase" kill OVERRULED).** The drag watcher rebuilds from T·R·S only and writes the SAME matrix into BOTH endpoints (useTransformState.ts:171-172 — re-read by this seat): (a) every hand-edited off-diagonal (shear, the three "P" perspective cells the editor deliberately exposes) is silently destroyed, no undo, no dirty flag; (b) the Matrix channel's start-to-end delta collapses to zero — after any pre-start drag, Play animates nothing on that channel while Rotations still spins, so the breakage reads as an unrelated bug. `syncTransformations(false)` copies back translate only, so the next drag composes from stale R/S. **Disposition: NO-WAVE-OWNER (one spec row: preserve unrepresentable cells or declare the editor T·R·S-only; and NEVER write both endpoints from one pose — the rebase, if wanted, moves start alone).**
- **L-5 / C-6 (S-9) — CONFIRMED (MAJOR).** Matrix cells are free-text glass-ui `Input`s: `:start`/`:end`/`:step` are not `InputProps` members (21-member d.ts, no `number` type in the union — both readers' independent reads, corroborated) and fall through as invalid attributes; no `inputmode`, so mobile gets an alphabetic keyboard. `parseFloat("-")` → NaN → `NumericAnimation` lerps to NaN → `withMatrixCell` throws once per rAF frame for 300 ms, from the first keystroke of any negative number. **S-9 shadow registered by the corpus stands**: glass-ui ships `/number-field` (bounded numeric control) and the demo consumes zero of it. **Disposition: NO-WAVE-OWNER (matrix-editor family; evaluate `/number-field`, not a mechanical swap — the cell doubles as the `selectedMatrixCell` click-target); S-9 extends the census shadow census.**
- **C-7 / L-13 / L-14 — CONFIRMED (MAJOR).** Every cell edit spawns `void new NumericAnimation(...).play(...)` — handle discarded, no `onScopeDispose`, the pending rAF at `:203` uncancelled; slider drags stack N concurrent tweens over one cell, each from a stale origin, last-writer-per-frame wins, and the Input's displayed text is rewritten by the animation the previous keystroke started. `changeGraphPerspectiveAnim` (650 ms) is not a group member, so `animationGroup.stop()` and the visibility-pause both miss it. Contrast: CubeTarget disposes its roll (`onScopeDispose`, `:234-236`) — the discipline exists one file over. **Disposition: NO-WAVE-OWNER (matrix-editor family: retain + stop handles; scope teardown; group-or-stop the perspective anim). Typing-symptom shape → SS-13.**
- **L-6 / C-15 — CONFIRMED (MAJOR; cross-ref ACG D-1).** `tabsContent` is instantiated once per transport channel: the chain App.vue:61-63 → EditorShell.vue:92-94 → ControlsPaneWrapper.vue:45-49 (`v-for` over `controlHosts`, wrapped in `v-show`, not `v-if`) → ChannelControls.vue:32/:180 (both outlets bare, no gate — while the built-in panels at `:98`/`:149` DO gate) is verified end-to-end by both readers and structurally corroborated by this seat's banked-row reads. The cube's three channels ⇒ **3 MatrixEditors, 48 Inputs, 3 `[role=tabpanel][data-state=active]` divs**, the panel-slide animation ×3, and C-7's per-frame cost ×3. CubeScene's own `:158-169` comment claims a mirror-gating the parent does not implement. **Identity: the SAME v-show host-multiplicity architecture banked as kf-AnimationControlsGroup D-1 (BLOCKER — three stacked timelines) — one portal/host architecture, two symptom surfaces. Disposition: NO-WAVE-OWNER, settled WITH the ACG D-1 cure (per-host targets or selection-gated outlets) — not separately; DOM count (predicts 3) → SS-13.**
- **L-7 — CONFIRMED (MAJOR, original scenario INTACT — see ruling).** The unmount-persisted transform is restored into the model and never painted at mount: both paint sites are callback-only; the watcher is not `immediate`; OrbitalDrag's container style is gated off pre-start. The home-hero crossing DOES remount (this seat's topology ruling), so the stated scenario — drag the die on the landing, navigate to cube, die back at rest until the first delta snaps — is live. Adopted sharpenings: **(A's miss)** the relit lighting DOES paint the restored pose at first render (`faceLit` is a plain computed over restored `rotate`; `--lit` binds on `.cube-side` unconditionally) — the die renders square-on with the lighting of a rotated one, *worse than invisible*; **(B's wrinkle)** `matrix3dStart/End` seed to identity, never from `initialTransform`, so the Matrix channel ignores the restore entirely. **Disposition: NO-WAVE-OWNER (mount-time paint + seed coherence, one row).**
- **L-8 / C-9 — CONFIRMED (MAJOR, FOLD by reference).** No `.vue` file is type-checked by any script or CI job (`check` = bare `tsc`; CI runs `check:lib`; no vue-tsc anywhere; `env.d.ts` types every SFC as `DefineComponent<{},{},any>`), and the store type omits `selectedMatrixCell` (7 undeclared read/write sites in MatrixEditor). **Identity: ≡ KF-APP-4 (banked → KF.W4, born-RED trivial) + KF-APP-30 (the `matrixOptions` type hole + the `:194` unguarded deref, banked NO-WAVE-OWNER).** This record adds only: the type hole is the proven enabling condition for D-2/C-3, C-6, L-18 and L-26 in THIS component — five findings one `vue-tsc --noEmit` would have caught.
- **L-9 — CONFIRMED (MAJOR; reader B's HEAD caveat VACATED).** `matrix3dStart`/`matrix3dEnd` are deep `ref()`s replaced wholesale ~60×/s (fresh 17-object trees from pure `createMatrix`/`withMatrixCell`), each replacement firing `adoptCompiled(compileMatrixAnimation())` — a fresh `CSSKeyframesAnimation` + full `fromVars` parse+compile per flush, for the duration of every tween and drag. The correct primitive (`shallowRef(markRaw())`) is used four times in the same file (P-4). At the frontier this is exactly the shipped code — B's "at HEAD the watch never fires" applied only to the stale local commit. **Disposition: NO-WAVE-OWNER (shallowRef + debounce-to-edit-boundary; the `adoptCompiled` verb itself is P-1-exemplary — the defect is the trigger rate).**
- **C-16 + D-10 — CONFIRMED (MAJOR).** `matrixOptions.fixed` has zero readers repo-wide (3 hits, all inside the button that owns it — both readers re-ran the grep): the ribbon Lock/Free toggle changes nothing but its own caption. D-10 (inverted icon/label semantics + missing `aria-pressed`) folds in as the *presentation* of the dead control — labelling a lie more precisely is not the cure; deciding the control's fate is. **Disposition: NO-WAVE-OWNER (give `fixed` its evident consumer — pin the matrix against drag/slider writes in useTransformState:197-215 — or delete button + field + type row). String-index/destructure escape hatch noted as the one grep-immune falsifier class (both readers checked; none found).**
- **D-2 / C-3 — CONFIRMED (MAJOR; consequence half killed).** `variant: "outline"` is not a prop of the installed (= frontier-pinned) glass-ui 7 Button; the string lands in the DOM as a junk attribute and the buttons render as default glass-wash capsules at `emphasis: "secondary"` — the same emphasis the house strip passes explicitly, so the corpus's C-3 "loudest treatment" rhetoric is dead while the phantom-prop mechanism stands. The corpus's systemic sweep is adopted: exactly four `variant:` Button sites demo-wide, all four in `h()` render functions (CubeScene ×2, SpringScene ×2) — the idiom that escapes the SFC compiler and every eye. **Disposition: NO-WAVE-OWNER (cube scene-repair, glass-conformance packet; the SpringScene pair rides the same commit; KF-APP-4 is why it compiled).**
- **D-3 / C-4 + D-25-inverted — CONFIRMED (MAJOR).** The two ribbon buttons re-implement `RIBBON_BUTTON_CLASS` and diverge inside the same strip on FOUR axes now: radius (`rounded-lg` beats the design system's pill by layer order — cascade adjudicated from the shipped artifact by both readers, corroborated), type rung (`text-small` vs `text-body`), inline padding (`px-3` vs the `.button` default), and **icon size** (`w-3.5` 14px vs the house `.icon-sm` class 16px — this seat's own extraction; the challenge had CLEARED this axis on a token/class confusion). `RIBBON_BUTTON_CLASS` is module-local and unexported — the seam has no shareable token, which is why it drifted. A's precision nits carried: the demo's own `--radius-lg` theme rebind is itself overridden by glass-ui's components-layer `:root`; `font-medium` is redundant, not divergent. **Disposition: NO-WAVE-OWNER (export the constant or a `RibbonButton` recipe; one commit with D-2/C-3).**
- **D-7 — CONFIRMED (MAJOR).** Three of six face numerals fall to 1.36–1.72:1 in dark mode against the 3:1 large-text floor: the face palette is six theme-invariant `:root` literals (single occurrence in the 571 KB build — both readers verified) while the numeral ink inherits theme-reactive `text-foreground`. Face-4 arithmetic reproduced independently by both readers (1.368/1.356 — agreeing to the second decimal); A's `font-bold` correction adopted (the numeral is weight-400 by `demo-typography`; the 3:1 large-text threshold holds on size alone). **Disposition: KF.W6 (token/theme discipline — the face palette gets a dark arm or `light-dark()`, per the AmigaScene D-4/D-5 precedent); rendered ratios with sheen/specular overlays → SS-13.**
- **D-8 + M-7 — CONFIRMED (MAJOR; falsifier statement corrected — see ruling).** The engine PRM gate is opt-in (`respectReducedMotion` default false) and the cube gates ONE of its FOUR engine motions: the 650 ms graph settle (hand-rolled matchMedia, P-3's correct semantics). Ungated: the 1.1 s up-to-four-revolution two-axis overshooting Roll (D-8), the 300 ms cell tween and the 500 ms `easeInBounce` reset (B's M-7, adopted — `NumericAnimation` accepts the flag, numeric.ts:82-89). The CSS-side universal PRM reset (which kills D-16's spinner) cannot reach any of these — they are WAAPI/rAF. **Disposition: KF.W9 (PRM mechanism unification, the intensity form per the AmigaScene D-7 precedent; the corpus's PRM-census sharpening rides: cube = 4 engine motions, 1 gated).**

### MINOR

- **D-9 — CONFIRMED (MINOR-RESCOPED, house-wide rider).** The hero-recede guard is `@media (max-width:1023px)` (px) while its `lg:` counterparts compile to `(width>=64rem)` — desynchronizing at enlarged root fonts (1024–1280 px band at 20 px root) and reviving the exact H3/TYP-1 hero/die collision the block exists to prevent; plus the 1023.5 px half-pixel hole. House-wide idiom (7+8 mixed blocks in the shipped sheet). **Disposition: NO-WAVE-OWNER house-wide breakpoint-unit unification rider (the cube is the sharpest witness); collision render → SS-13. Adjacent kf-App KF-APP-8 (dead pt-band comment, hero-vs-die geometry) is a different defect at the same surface — cross-referenced, not folded.**
- **D-11 — CONFIRMED (MINOR).** `.btn-interactive` is a phantom class: zero rules in the 571 KB shipped artifact, zero in glass-ui, eight consumer sites; provenance traced to the glass-ui `b0debec` retirement under a "false zero-site verdict" where only the two `.rainbow-*` were restored. No focus contract lost (Button applies `.focus-ring` itself — the corpus's own downgrade, verified). **Disposition: KF.W6 (the phantom-token/phantom-utility audit per KF-APP-6's re-cut — this is the utility-class instance; 8-site sweep listed in the corpus).**
- **D-16 (a11y half) + spinner-over-rendered-cube (A's miss, adopted) — CONFIRMED (MINOR).** The loading affordance is dishonest twice: a bare `Loader2` with no role/name/live-region (AT sees an empty scene), painting OVER a fully-rendered, interactive die — `showLoader` means "no animation selected yet", not "loading" (the six faces render unconditionally, CubeTarget.vue:28-90). The PRM half is DEAD (killed — the universal reset neutralises the CSS spin). **Disposition: NO-WAVE-OWNER (one row: make the state honest — either a real loading state or no overlay — plus `role="status"`+name if kept).**
- **D-17 / L-27 / C-10 — CONFIRMED (MINOR).** CubeScene's wrapper is a byte-equivalent clone of CubeTarget's root (identical 8-utility class list bar `relative`, identical inline `touch-action:none; overscroll-behavior:contain`, identical `@wheel.prevent`) — two nested centering grids, two non-passive wheel listeners on one path. The outer div's sole real job is hosting the recede class. **Disposition: NO-WAVE-OWNER (collapse to one owner; the recede class stays on the survivor).**
- **C-8 / L-28 — CONFIRMED (MINOR).** The facet's `{label, icon}` re-create — byte-identically — the `SURFACE_META` fact the registry's own docblock exists to single-source; `SceneFacet.label`/`.icon` have zero readers repo-wide. Narrows P-7 without overturning it. **Disposition: NO-WAVE-OWNER (narrow `SceneFacet` to `{surface}` per controlSurfaces.ts:72-73's own type, or delete the two fields).**
- **C-11 — CONFIRMED (MINOR).** `easeInBounce` (a value.js function reference, bypassing the kf registry-string idiom the two compliant sites use) survives the scene's own T.A3 "ONE settle-motion language" edict at the exact site the ribbon Reset drives — the named-and-condemned easing. Also the only reason `@mkbabb/value.js/easing` appears in the subtree. **Disposition: NO-WAVE-OWNER (one-line: `"ease-out-back"` via the registry; the import dies with it).**
- **C-17 + SCENE_SURFACE_TABS (A's sharpening) — CONFIRMED (MINOR).** `:146-156` describes `CONDITIONAL_SURFACES`/`activeControlConditionals` as live wiring in the present tense; `:244-247` records their death; every repo hit for both identifiers (and A's third corpse, `SCENE_SURFACE_TABS`, `:151`) is a `//` comment — zero declarations. The stale block reads as authoritative wiring documentation. **Disposition: NO-WAVE-OWNER (delete `:146-156`; C-17's correction of the corpus's own L-4 superlative "returns nothing" → "returns six prose lines" is preserved).**
- **C-18 / D-18 — CONFIRMED (MINOR).** Target attachment is one `onMounted` guard with no else/warn/retry, while the facility (and its `identity`, the shell's SCENE_READY precondition) is built unconditionally — a target-less scene still presents as ready and playable. **Disposition: NO-WAVE-OWNER (warn-else or make `facility` conditional on attachment); window-reachability → SS-13.**
- **L-10 / L-11 / L-12 — CONFIRMED (MINOR).** Dead exports with prose asserting they are called (the sliders call `updateMatrixCell`, not `updateTransformations`); dead imports (`MATRIX_AXES`, `transformSliderOptions`) + a dead type re-export; a composable returning 6 members to a consumer that destructures 2. **Disposition: NO-WAVE-OWNER (hygiene, one commit; KF-APP-4/K3 `noUnusedLocals` closes the class).**
- **L-16 — CONFIRMED (MINOR; consumer half ≡ KF-APP-12, FOLD).** The expose comment cites a member (`animationGroup`) that `defineExpose` does not carry; the consumer-side `#tabs-trigger` forwarding to an undeclared member is **banked at KF-APP-12** (delete-the-chain, NO-WAVE-OWNER) — folded by reference, not re-booked. **Disposition: NO-WAVE-OWNER (comment correction rides any touching wave).**
- **L-17 — CONFIRMED (MINOR, FOLD by reference).** The cube is registered twice (lazy + static) with the lazy half dead and `warmScene("cube")` a no-op. **≡ KF-APP-14 (banked, NO-WAVE-OWNER)** — folded; this record adds nothing new.
- **L-18 / C-12a — CONFIRMED (MINOR).** Exposed render-fns drift from the declared protocol (`VNode|null` and `VNode[]|null` against `() => VNode`); runtime-fine, type-wrong, invisible under KF-APP-4. **Disposition: KF.W4 rider (the vue-tsc row surfaces it; fix the protocol types in the same motion).**
- **L-19 / C-13 — CONFIRMED (MINOR).** Asymmetric persistence (three fields deep-copied, `matrix` aliased by reference) over a field nothing writes — `TransformState.matrix` is dead data cloned every mount and persisted every unmount as a permanent identity. B's refinement adopted: the alias is the raw `Float32Array` buffer of the dying instance (Vue leaves typed arrays unproxied). **Disposition: NO-WAVE-OWNER (delete the dead field, or copy it like its siblings if a writer ever lands).**
- **L-20 / D-24 — CONFIRMED (MINOR, FOLD by reference).** `:194` derefs `matrixOptions.fixed` unguarded four lines above the `?.`-guarded reads, with the initialiser in a different component. **≡ KF-APP-30 (banked; the collapsed-pane repro is that row's SS-13 question)** — folded.
- **L-26 / C-12b — CONFIRMED (MINOR).** A dead emit wire: parent listener (`onResetMatrix`), child emit declaration, child emitter — none connected to any trigger; the live Reset is the ribbon button. **Disposition: NO-WAVE-OWNER (delete the three artefacts).**
- **L-29 / D-21 — CONFIRMED (MINOR).** `hideLoader` names one of its two effects (loader gate + hero-recede layout band), is passed a value (`isHome`) named for neither, and the file's own header must gloss it. **Disposition: NO-WAVE-OWNER (rename `isHome`/`isBackdrop`; the child's `showLoader` is the honest shape).**
- **M-5 (B's miss, adopted) — ADJUDICATED-CONFIRMED (MINOR).** The `transition-[background-color,opacity] duration-panel ease-in-out` triple on `.cube-side` (CubeTarget.vue:43) is dead twice: the SFC's scoped stylesheet is emitted UNLAYERED — this seat extracted the shipped rule `.cube-side[data-v-8a65c7ef]{…transition:--lit .16s linear;…}`, and an unlayered `transition` shorthand resets property/duration/timing over the `@layer utilities` triple — and `.cube-side` never carries the background-color anyway (it lives on `.face-lacquer`). Doubly latent: its intended trigger (the pp face swap) is itself dead under KF-APP-1. **Disposition: NO-WAVE-OWNER (move the transition to the element that changes, inside the scoped sheet — or delete).**
- **M-8 (B's miss, adopted) — CONFIRMED (MINOR).** controlOptionsStore.ts:90-94 declares "the store owns this one migration/default boundary; editor components only consume it" (this seat read the comment in situ) — and both cube components violate it (`CubeScene.vue:63 ppMode ??=`, `MatrixEditor.vue:120 matrixOptions ??=`). The named, violated contract is the unifying root of KF-APP-30's type drift, L-20's nullability schizophrenia, and C-14's bucket split. **Disposition: NO-WAVE-OWNER (move both seeds into the store's boundary; the KF-APP-30 cure inherits this shape).**
- **MISS-1 (A) — CONFIRMED (MINOR).** CubeScene's `[role=tabpanel]` (`:172`) omits the `tabindex="0"` (and id/labelledby) the parent's built-in panels carry under an explicit house comment stating the reason (ChannelControls.vue:85-102) — the one revealed panel a keyboard user cannot land on, diverging from the very contract `:158-169` claims to mirror. **Disposition: NO-WAVE-OWNER (rides the C-15 cure — the panel is being restructured anyway).**
- **MISS-2 (A) — CONFIRMED (MINOR; qualifies S-D3).** `@property --lit { inherits: true }` (CubeTarget.css:5-10) is a DOCUMENT-GLOBAL registration on an unprefixed three-letter generic name, emitted from a scoped stylesheet Vue cannot scope (at-rules escape `[data-v-*]`; the shipped registration is global). Every element now computes an inherited `--lit: .5`, and any future fleet `--lit` is silently type-constrained. Zero collisions today; the sibling `CubeAxisLines` gets it right (`--axis-active { inherits: false }`). Cross-check: kf-App's probe confirms `--lit` + `--axis-active` are the demo's only `@property` registrations. **Disposition: NO-WAVE-OWNER (prefix `--cube-lit` and/or `inherits:false`; the S-D3 superlative keeps its load-bearing credit with this qualifier).**
- **MISS-3 (A) — CONFIRMED (MINOR; target half latent).** The demo ships its own documented WCAG 44 px utility (`.tap-floor`, design-idioms.css:81-82) with ZERO consumers, glass-ui ships two more mechanisms (`[data-control-target]` coarse-pointer floor; `@utility touch-hit-area`), and the cube's only bespoke pointer target (the 32 px ppmycota trigger, `m-0 p-0` zeroing any rescue) uses none of the three. The trigger is latent (C-2); the zero-consumer utility is live house hygiene. **Disposition: NO-WAVE-OWNER (the KF-APP-17 sweep decides the trigger; the `.tap-floor` liveness question rides KF.W6's utility audit with D-11).**
- **MISS-4 (A) — CONFIRMED (MINOR, latent on the KF-APP-1 cure).** In ppMode the `--lit` consumers (`.face-lacquer`, `.face-relit`) sit inside `v-if="!ppMode"` while the `--lit` binding and the relit computation run regardless — six dead custom-property writes per rotation tick, and the signature re-lit-die egg silently off; adjacent, all six ppMode faces render the identical mark, so the Roll egg's random-face landing becomes indistinguishable — two eggs cancelling. Unreachable today (ppMode permanently false); goes live the day KF-APP-1 is cured. **Disposition: NO-WAVE-OWNER (rides the KF-APP-1/-17 motion as its acceptance checklist); rendered ppMode faces → SS-13 (post-cure).**
- **MISS-5 (A) — CONFIRMED (MINOR).** CubeScene reaches into CubeTarget's private `--side-size` via `:deep()` and collides with CubeTarget.css's own rule for the same property at the same breakpoint; the parent wins by a specificity accident (`:deep()` drops the child scope attribute and adds a class: (0,2,1) vs (0,2,0)), with no compile-time link — a rename in either file silently breaks the hero-recede sizing rung, and no gate would see it (KF-APP-4). S-D2 celebrated this exact block without noting its one un-tokened cross-component coupling. **Disposition: NO-WAVE-OWNER (promote `--side-size` to a declared contract token or pass it as a prop).**

### INFO

- **L-21 / D-22 — CONFIRMED.** ~26 % of the script is comment archaeology narrating migrations away from imports the file no longer has (incl. an eight-line paragraph on a deleted watcher); the third and largest instance of census S-2. **NO-WAVE-OWNER (prune to load-bearing rationale; C-17's stale block is the charged subset).**
- **L-22 / C-14 — CONFIRMED.** The component keys its store bucket `"cube"` unconditionally while the shell keys `"home"` on the landing — two buckets, one mounted component; inert today, the exact seam the KF-APP-1 cure lands on (rider booked in the C-1 row above).
- **L-23 — CONFIRMED.** A `computed` over three pure index functions — a module constant wearing a reactive wrapper, re-allocated per instance.
- **D-19 — CONFIRMED.** Inline `touch-action`/`overscroll-behavior` style where the `touch-none`/`overscroll-contain` utilities exist and are used elsewhere.
- **D-20 — CONFIRMED (sharpened at the frontier).** Both provenance headers stamp "glass-ui 4.0.0"; at the frontier the pin AND the installed copy are 7.0.0 — B's "three-way divergence" collapses to a clean two-way (comments vs everything else), which is worse for the comments. **NO-WAVE-OWNER (rides any touching wave).**
- **D-23 — CONFIRMED.** The ppMode skin re-enters the documented filter/preserve-3d flattening hazard at leaf level — safe today (childless leaf), one nesting level from the post-mortem's BLOCKER. **NO-WAVE-OWNER (a one-line callsite comment; latent under KF-APP-1).**
- **C-19a — CONFIRMED.** Root-barrel glass-ui import where `./popover`/`./button` subpaths exist; sideEffects-declared, so a correct bundler shakes it. **NO-WAVE-OWNER (rides any subpath sweep); chunk-graph consequence → build-level, not SS-13.**
- **C-20 — CONFIRMED.** Four writers contend for `transform` on one element (three group channels + the direct painter), coordinated only by `isGroupStarted` — and C-5 kills the other half of that state. By construction (the group's `singleTarget` blend case); recorded as the blend stress case. **NO-WAVE-OWNER (rides the L-2/C-5 cure).**
- **L-31 — CONFIRMED-UNADJUDICATED.** The panel gates on the raw store where the parent gates on the DFA projection; divergence window = the NAVIGATE→SCENE_READY transition; no surviving failure scenario constructed by any of the three seats (neither reader traced `isActiveSceneHost`; nor did this one). Preserved at the corpus's own INFO with the falsifier standing.
- **MISS-6 (A + B's M-4(a) residue) — CONFIRMED.** Prose/tree divergences inside the target's read set: CubeTarget.css:57 justifies the G5 `will-change` decision by an idle-bob the same file records as deleted twenty lines up; cubeTransformStore.ts:6-8 describes a separate "home screen CubeTarget" that does not exist (one consumer, and home renders the same CubeScene). The shell-binding comment B charged in M-4(b) is NOT in this row — its mechanism is TRUE (the remount ruling); only its `__home__`/`Cube` literals are stale, and that cell is already booked at kf-App. **NO-WAVE-OWNER (comment corrections ride their touching waves).**
- **LATENT COHORT (the KF-APP-17 sweep checklist) — mechanisms CONFIRMED, all INFO-latent.** Recorded once, severities dead until the fill-vs-delete decision: D-1 (trigger chain unfocusable: reka `HoverCardTrigger` `as:"a"` href-less + nameless bare div — mechanism verified by both readers from installed sources), D-4/L-30 (4 s wall-clock force-close under a stationary pointer; controlled path authoritative; no re-open without leave+return), D-5/L-25/C-19b (unnamed `role="group"`; the explicit `role:"card"` is a hover-branch no-op), D-6 (one coarse-pointer tap fires popover-toggle + ppMode-toggle at once), D-12 (dead `p-4`, never-binding `min-w`, redundant `cursor-pointer` — all three verified on the shipped build), D-13 (two adjacent identical-destination links), D-14 (emoji line neither text nor decoration, gratuitous `innerHTML`), D-15+MISS-3 (32 px touch target, inverted against pointer class, none of the three house mechanisms applied), L-24 (hover-vs-coarse gesture asymmetry). **ARB-1 (below) rules the sweep's direction.**

### ARB-1 — this seat's own net-new finding (MAJOR-weight repair law, booked as the KF-APP-17 rider)

**The `headerLeft` "fill" repair arm is a trap: it lands inside an inert toolbar.** `EditorShell.vue:18`'s `#header-left` outlet sits inside `HeaderRibbon`'s `#items` slot (this seat's read, `:16-50`) — and kf-App's **M-A BLOCKER** (banked, this seat re-read the row) establishes the installed HeaderRibbon renders its items `inert` + `aria-hidden` + collapsed at rest, with a non-focusable anchor and a touch-skipping hover opener, and that the lockout mechanism **persists at `origin/master`**. So repairing C-2 by filling `#header-left` would mount the pp-mode toggle and its hovercard inside a container that is keyboard- and AT-dead at rest and has no reliable touch route — re-creating D-1's substance one level up, behind a producer defect that is glass-ui-owned and already relayed. **Ruling: until the M-A producer relay lands, the DELETE arm is the only self-contained C-2/KF-APP-17 cure** (delete `headerLeft` + the 3 dead glass-ui imports + the auto-dismiss machinery + the latent cohort; re-home the pp-mode toggle where KF-APP-1's cure lands its one writer — the dock menu is the natural host). Neither reader composed these two banked rows. **Disposition: NO-WAVE-OWNER, binding sequencing law on the KF-APP-1/-17 motion.**

## Killed-claims register (12)

1. **L-1 (axis L's BLOCKER; reader A CONFIRMED) — KILLED, three independent ways.** (i) Frontier: `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep (this seat's `git show`) — the absence is an uncommitted worktree deletion; (ii) local HEAD declares optionalDep 6.0.0 + full lock entry — the challenge's own falsifier (a), satisfied verbatim; (iii) `npm ci` never resolves import specifiers, so the stated mechanism could not produce the stated failure even on the worktree (B's refutation, adopted). Identity: ≡ census F-1/SCH-1, ruled at kf-AmigaScene — folded, not re-booked. *Corpus-wide lesson (second occurrence): a manifest claim without naming the tree is worthless; reader A "confirmed a BLOCKER" by re-running greps against an uncommitted local edit.*
2. **Reader B's M-1 (substrate BLOCKER) — RESCOPED-BY-INVERSION.** The cube unit is byte-identical worktree↔`origin/master` (this seat's diff); the corpus audited frontier content; the stale artifact is the local commit B used as its baseline. Every B caveat riding it (L-5/L-9/L-14/C-7/P-6 "uncommitted", P-1 "watch never fires at HEAD") is vacated. Residue → KF.W0 §B-12 (checkout reconciliation + the uncommitted devDep-deletion decision).
3. **Reader B's M-2 (installed 7.0.0 vs pinned 6.0.0) — KILLED at the frontier.** `origin/master` pins 7.0.0 exact = the installed copy; every glass-ui API verdict in the corpus is measured against exactly the version true HEAD pins. The "verify against 6.0.0" UNPROVEN residue is discharged — no 6.0.0 verification is owed by anyone.
4. **Reader B's M-3 (optionalDependencies hazard) — KILLED at the frontier** (devDep, not optionalDep, at `origin/master`). Historical local-HEAD artifact only; recorded for the KF.W0 reconciliation narrative, charged nowhere.
5. **Reader B's M-4 premise + its L-7 "scenario correction" — REFUTED.** CubeScene DOES remount on home↔cube (`#target` renders inside `<AnimationControlsGroup :key="superKey">`; keys differ — this seat's own EditorShell/scenes.ts reads). The in-tree shell-binding comment is mechanically true. M-4(a)'s cubeTransformStore-docblock nit survives, folded into MISS-6.
6. **D-1 as a BLOCKER + live grading of the headerLeft cohort (D-4/D-5/D-6/D-12/D-13/D-14/D-15/L-15/L-24/L-25/L-30) — KILLED to INFO-latent.** The control never renders (C-2, exhaustive grep, twice re-run). Axis D enumerated four falsifiers and never asked whether the render fn mounts. Mechanisms preserved as the sweep checklist; ARB-1 rules the sweep's direction.
7. **D-5 at MAJOR — KILLED to INFO** (structural `group` role needs no name under any SC; the explicit `role:"card"` is a no-op on the branch that runs; L-25/C-19b graded the same evidence correctly).
8. **D-16's PRM half + D-8's falsifier statement — KILLED.** The shipped stylesheet carries glass-ui's universal `!important` PRM reset (this seat's own extraction); the spinner is CSS-animated and fully neutralised. D-8's finding survives on engine-motion grounds alone; its "I enumerated all 46 PRM blocks, no global reset" cell is false as written, and **reader B repeated it as corroboration — a recorded citation-inheritance event** (the second in this record, after A's L-1 confirm).
9. **C-3's consequence half ("default emphasis primary — the loudest treatment") — KILLED** (compiled default is `"secondary"`; C-3 read a resolved-defaults type list as a value; the cube's buttons sit at the same emphasis as the whole strip). D-2's reading survives; C's repair-step-4 becomes an idiom fix, not a visual fix.
10. **D-25's clearing ("no visual consequence — same value") — KILLED** (the `.icon-sm` CLASS is 1rem, the `--icon-sm` TOKEN is .875rem; the challenge compared the wrong pair — this seat's extraction). Promoted into the D-3/C-4 row as the fourth divergence axis.
11. **The corpus's own §7.3 kill of the endpoint collapse ("rebasing the start is defensible design") — OVERRULED** (writing the same matrix into BOTH endpoints is a delta collapse, not a rebase — B's M-6, this seat's re-read). Lives inside the L-4 row.
12. **Reader B's P-1 vacation ("the celebrated adoptCompiled watch never fires at HEAD") — KILLED at the frontier** (the deep-ref replacement code IS `origin/master`; the watch fires; P-1's credit and L-9's charge both stand exactly as the corpus wrote them).

**Preserved kill-records (not new kills):** reader A's own attempted L-7 kill, recorded failed by A and now confirmed-failed by this seat's independent topology read — the rare case of a reader documenting its own falsification correctly; the corpus's H-1 (ribbon-throw window — mechanism nuance lives on as KF-APP-30's SS-13 question) and H-2 (easing-import bundle leak) self-kills, both standing; axis L's §7.3 kills (controlled-open sanctioned; role:"card" valid; adoptCompiled/store coherence), all standing.

## Identity-guard folds (never re-booked)

- **C-1 ≡ KF-APP-1** (banked) — the ppMode zero-writers + MbabbMenu throw; this record adds the bucket-split rider.
- **C-2 ≡ KF-APP-17** (banked) — the headerLeft dead contract member; this record adds the latent-cohort checklist + ARB-1's sequencing law.
- **L-8/C-9 ≡ KF-APP-4** (banked → KF.W4) + **the `matrixOptions` hole + `:194` deref ≡ KF-APP-30** (banked) — this record adds the five-findings-one-gate enablement note.
- **L-16's consumer half ≡ KF-APP-12** (banked) — the `#tabs-trigger` dead chain.
- **L-17 ≡ KF-APP-14** (banked) — the double registration.
- **L-1 ≡ census F-1/SCH-1** — ruled at kf-AmigaScene; this record adds the worktree-deletion frontier fact.
- **L-6/C-15 ≡ (architecture) ACG D-1** (banked BLOCKER at kf-AnimationControlsGroup) — the v-show host-multiplicity portal architecture; the tabs-content limb books HERE, the cure is ONE architecture decision there.
- **The touch-action pinch-zoom question ≡ AmigaScene MISSED-F** (banked house-wide census, 6 scene sites incl. cube) — B's UNPROVEN row folds by reference; the cube witness rides that row's KF.W9 audit.
- **The cube subject-a11y gap ≡ AmigaScene D-2's shared-idiom cure** ("a shared subject-a11y idiom for cube AND amiga") — no corpus row here charges it directly; noted so the shared cure's scope is not re-litigated.

## Superlatives (provenance: corpus → readers → this seat)

Adopted, with the readers' corroborations and this seat's qualifiers:

1. **P-1 (adoptCompiled)** — the demo drove a first-class engine verb into existence; **restored whole at the frontier** (kill 12). The rate is L-9's; the verb is exemplary.
2. **P-2 (visibility-pause honesty)** — predicate + thunks; only what the hide paused is resumed.
3. **P-3 (PRM snaps to the terminal attitude)** — reduced motion means less motion, not less scene; the best PRM semantics in the demo, on the scene otherwise charged by D-8.
4. **P-4 (`shallowRef(markRaw())` ×4)** — which is precisely why L-9's deep refs read as oversight, not style.
5. **P-5 (the torn-down timer)** — cleared on every transition AND unmount, handle nulled; contrast the roll's dangling 1.2 s timeout in the same unit.
6. **P-6 (transformMath.ts total boundary validation)** — 181 pure lines, precise typed throws, zero mutation/coercion; that every guard is reachable as an uncaught throw is the caller's failure (L-5). Frontier-confirmed (kill 2 vacates B's caveat).
7. **P-7 (facets as data)** — genuinely landed inversion; narrowed by L-28 (two of three fields dead).
8. **P-8 / C-L-1 (the LIGHT/HEAVY boundary, both directions)** — four sites, three idioms, all correct, on the one scene in the eager boot chunk; round 2 opened this as an accusation and the barrel refuted it.
9. **P-9 (the `--lit` repaint elision + unit colocation)** — a measured optimisation that names its mechanism, bounds its cost (1 % luminance), and rides the existing cadence.
10. **S-D1/S-D2 (logical properties + zero magic numbers in the recede band)** — with MISS-5's qualifier: the one cross-component coupling in the celebrated block has no token contract.
11. **S-D3 (`@property --lit` is load-bearing)** — without it the relight steps instead of gleaming and reads as a frame-rate fault; **qualified by MISS-2** (the registration is document-global, unprefixed, `inherits:true`).
12. **S-D4 (the removed bloom documented by mechanism)** — the filter/preserve-3d post-mortem hands a future designer the reason the obvious fix cannot work; invariant re-verified by reader A.
13. **S-D5 (compositor + z discipline)** — transient promotion only, resident `will-change` removal recorded by name, every z-index earning its rung; the `.playing` half of the promotion being dead is L-2's charge, not this credit's.
14. **A-credit: the axis-lock reveal (CubeAxisLines)** — an invisible keyboard-modifier constraint given a spatial channel off state already published, with `inherits:false` scoping (the correct choice `--lit` missed), a demoted resting register, and a motion-free second tell (`border-style` while locked) that survives PRM and low vision. Genuinely excellent interaction design, uncredited by all three axes.
15. **A-credit: the shadow veil re-derives depth per theme** — `color-mix(in srgb, var(--background) …)` sinks a turning face toward the PAGE background in either theme; proves D-7 is a one-layer omission, not an absence of theme thinking.
16. **A-credit: the Roll trigger's touch parity** — `@dblclick` → drag-disjoint `useDoubleTap` on an element owning a competing continuous gesture; the one input-modality decision that goes the right way.
17. **B-credit (meta): the corpus's falsifier discipline is load-bearing** — L-1's written falsifier is what let one `git show` settle it; ten corpus self-kills + a second-pass self-correction (C-17 on L-4) are why this adjudication could be surgical. This seat adds the counter-lesson: two of the falsifiers were answered against the wrong tree, and one enumeration (the PRM blocks) was inherited unverified by a hostile re-reader — falsifier discipline is necessary, not sufficient; **name the tree, re-derive the enumeration**.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **D-7 rendered ratios** — dark theme, die at rest; sample the LIT face (sheen + specular shift the pixel unfavourably). The crayon-composite math is settled (both readers reproduced face 4 ≈ 1.36:1).
2. **C-15/L-6 DOM count** — `document.querySelectorAll('.matrix-grid').length` with the Matrix channel selected; the claim predicts **3**. One query settles the MAJOR.
3. **L-5/C-7 symptom shape** — console spam vs a wedged rAF loop when `withMatrixCell` throws inside `onFrame`; whether the Input's text is visibly overwritten mid-entry.
4. **D-9 collision** — 1024–1280 px CSS width under a 20 px root font; the geometry says the hero and die must touch; only a render proves it.
5. **L-7's snap** — the home-drag → navigate → first-delta snap, and MISS-4/the relit-mismatch first frame (restored lighting on unrestored geometry).
6. **C-18's window** — whether the one-shot attach can miss in any real mount ordering.
7. **KF-APP-30's collapsed-pane repro** (banked there; listed for the shared SS-13 roster) — `#ribbon-content` without MatrixEditor's panel ⇒ fresh-bucket throw at `:194`.
8. **Post-cure only**: the ppMode faces (`--filter-brand-color` over `--secondary` at .75 α — contrast undecidable from tokens) + the dead-`--lit` per-tick cost; moot until KF-APP-1 lands.
9. **Latent, sweep-dependent**: D-1's tab-order across engines (only if the ARB-1-gated fill arm ever runs).
10. **Cross-repo, NOT SS-13**: whether value.js `parseCssValue`/`parseCssScalar` internally dispatch into the colour grammar for non-colour declarations (the C-L-2 residual, untraced by all five seats) — routed as a census annotation to the V·π parser program, where that surface is already under proof.

## Wave routing summary (M-25 agglomeration input)

- **KF.W0** — the substrate reconciliation: sync the sacred checkout to `81a56990`+; RULE on the worktree's uncommitted glass-ui devDep deletion (committing it as-is revives census F-1); re-anchor law for every line cite in this record.
- **KF.W4** — KF-APP-4 fold (vue-tsc; five CubeScene findings are its witnesses: D-2/C-3, C-6, L-18, L-26, KF-APP-30's hole) + L-18 protocol-type fixes + the K3 `noUnusedLocals` class (L-10/L-11).
- **KF.W6** — D-7 (face palette dark arm) · D-11 + MISS-3's `.tap-floor` (the phantom-utility audit, KF-APP-6's re-cut) · D-20 rides.
- **KF.W9** — D-8+M-7 (PRM unification, intensity form; the cube = 4 engine motions / 1 gated correction to the census §6.5 tally) + the SS-13 roster above + AmigaScene MISSED-F's cube witness.
- **NO-WAVE-OWNER (the cube scene-repair wave the SS-1/SS-2 authoring must create)** — the matrix-editor family as the core packet (L-3 · L-4+M-6 · L-5/C-6/S-9 · C-7/L-13/L-14 · L-9 · C-16+D-10 · MISS-1 · M-5 · M-8), the state/identity packet (L-2/C-5+C-20 · L-7 · L-19 · L-29), the glass-conformance packet (D-2/C-3 + D-3/C-4+D-25inv, with the SpringScene rider), the KF-APP-1/-17 motion with **ARB-1's sequencing law (delete arm only, until the glass-ui M-A relay lands)** + MISS-4's checklist + the C-14 bucket rider, the C-15 limb settled WITH ACG D-1's portal-architecture cure, and the hygiene tail (D-16 · D-17/C-10 · C-8/L-28 · C-11 · C-17 · C-18 · L-16 · L-26 · MISS-2 · MISS-5 · MISS-6 · the INFO tail).

## Closing verdict

**ADJUDICATED.** 49 rows confirmed (0 BLOCKER · 15 MAJOR · 23 MINOR · 11 INFO), 12 claims killed, 9 net-new misses adopted (6 reader-A, 2 reader-B, 1 this seat's ARB-1), 10 live residues to SS-13. All three corpus BLOCKERs died or demoted — two of them (L-1, and reader B's M-1) on the same substrate lesson pointing in opposite directions: **name the tree before you grade it**. The component's real condition is a well-crafted scene (nine standing superlatives, two of them uncredited by any axis) wearing one genuinely broken instrument — the matrix editor, whose five MAJORs share three files and one repair wave — plus a dead brand feature already banked at kf-App, and a per-channel portal architecture already banked at kf-AnimationControlsGroup. Nothing here stamps a wave: X·KF specs are forming, and this record is their CubeScene intake.

— end of apotheosis. The challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
