# shell-paneslot — adjudicated apotheosis (M-23/L-18 fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: apotheosis adjudicator (incredulity seat) · **Date**: 2026-08-09 · **HEAD**: `c654824e` · branch `tranche-u`.
**Corpus**: `audit/components/shell-paneslot/` — challenge-C (PS-1..PS-9, Opus 5), challenge-D (D-1..D-11, Opus 5), challenge-L (L-1..L-11, Opus 5) against `demo/shell/PaneSlot.vue` (129 lines) and its consumer wiring (`App.vue`, `usePaneRouter.ts`, `useViewManager.ts`, `viewSchema.ts`, `usePaletteWiring.ts`, `animations.css`, `shell.css`, `overture.css`). All three challenge files are **EXISTS-ORIGINAL** (`registry/HYDRATION-LEDGER.md`, area-shell.json) — no M-xx motion-quarantine row governs the corpus directly, but `audit/codex-provenance/motion-quarantine.md §1` is the standing authority for every PRM cascade claim and is applied below.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted: every verdict where they disagree (PS-3's premise, PS-4's severity, D-4's filing, D-7's final limb, the D-1 kill) was re-derived against the tree by this seat, and the biggest missed-find claims were re-walked link by link with my own reads.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony)**: full reads of PaneSlot.vue (whole), App.vue (whole, 418 lines), usePaneRouter.ts (whole, 231), useViewManager.ts (whole), main.ts (whole), viewSchema.ts §unions, useOverture.ts:140-250, ColorPicker.vue:89-95/:149-164/:245-267/:370-385, HeroBlob.vue:5-30/:180-250, Dock.vue greps (:16/:18/:37/:40-41/:71-72/:132/:153-198), shell.css:15-135, animations.css:180-280, overture.css:10-80, usePaletteWiring.ts:30-130, ErrorBoundary.vue greps, PalettesPane.vue:46-50, ActionBarLayer.vue:54-70, e2e/smoke/mobile/walk.spec.ts:44-60/:133-140, demo/DESIGN.md:268-292, VISUAL-CONSTITUTION.md:30-34/:116-120/:139-143; Vue 3.5.35 runtime source at four decisive sites (`runtime-core.cjs.js:1510-1600` appear-substitution + enter-cancel, `:1795-1820` setRef function-old-ref gap, `:2930-2985` KeepAlive Comment-bail + oldest-first LRU, `:5470-5480` per-patch setRef, `:6580-6600` setRef-before-deactivate); `node` against package.json#exports (7 keys, no `.` root), `ls dist` (index.d.ts MISSING; subpaths = color/css/easing/math/quantize/transform/value), `wc -l dist/subpaths/css.d.ts` → 382, `grep -c "_2"` → 25; census greps: `onActivated|onDeactivated` in demo/ → exactly 2 hits, both HeroBlob.vue; `paneslot` in PROPORTION-AUDIT.md → 0 rows; `find test -name '*.test.ts'` → **26**, with `demo/shell` touched (status-lamp.test.ts, picker-blob-config.test.ts).

## Scoreboard

**22 corpus rows CONFIRMED (2 BLOCKER · 10 MAJOR · 7 MINOR · 3 INFO, two of those demotions) · 9 claims KILLED with refutations · 15 missed finds submitted by the re-readers, 15 verified by this seat (10 adopted as new rows, 5 folded into existing identities) · 10 UNPROVEN-NEEDS-LIVE residue items → visual-audit inputs.**

Blocker-weight survivors: **L-2** (the mobile command channel is structurally dead — verified by enumeration this seat: `colorPickerRef.value` has one writer, App.vue:325, inside `onDesktopLeftMount`, wired only at App.vue:105/:131, both desktop; the mobile slot App.vue:83-91 passes no `:on-mount`) and **L-1/PS-2/D-5** (the app's only `<KeepAlive>`, PaneSlot.vue:120, publishes no activation contract; demo-wide census 1 `onActivated` / 0 `onDeactivated`; `runtime-core.cjs.js:6596-6598` returns into `deactivate` before any unmount hook, so `window` keydown handlers with `preventDefault` arms survive every route).

The corpus's single most consequential accusation — **D-1**, "the `mode="out-in"` prohibition is falsified by measurement, BLOCKER, delete it" — is **KILLED as filed** (unpreserved probe that, as described, never reaches the failure path; see the register). Its residual question survives only as UNPROVEN-NEEDS-LIVE.

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-1** | KILLED (probe unpreserved, under-specified, intra-corpus contradiction) | UNPROVEN-NEEDS-LIVE + K-7 kills the cure | **KILLED as filed — both readers substantially right; DU's kill adopted whole, LC's K-7 folded in** | The component dir holds only `frames/desktop-comount-right-slot.png` — no probe script, no RESULTS artifact (my `ls`). The quoted probe output (challenge-D:69-77) names no transition CSS, no duration, no wait: with zero-duration CSS, runtime-dom's `whenTransitionEnds` resolves immediately and out-in degenerates to a synchronous swap that never exercises the `afterLeave → instance.update()` handoff the prohibition (PaneSlot.vue:12-23) blames. The same corpus asserts the opposite as settled fact (challenge-L:616-618). And K-7 is decisive on the cure: with `mode="out-in"`, ten bare `defineAsyncComponent`s (usePaneRouter.ts:69-78, no loadingComponent/delay/Suspense) turn D-6's empty slot into an every-unresolved-swap event — the D axis's own D-8 paragraph concedes the coupled hazard. A BLOCKER whose evidence is unpreserved, whose probe does not test the shipped configuration (async panes + real `--duration-fast` leave + the rAF mirror), and whose cure regresses under its own sibling's measurement, does not survive. Residual → UNPROVEN row 1. |
| **PS-3** | KILLED at MAJOR; carried MINOR without the "no cover" premise (quarantine cascade: cover EXISTS) | CONFIRMED MINOR but on the 0.01ms-clamp premise; K-6 kills the 58-106ms attribution | **CONFIRMED MINOR — DU's premise wins; LC's confirmed-row premise cell is itself KILLED against the quarantine authority** | `motion-quarantine.md §1` (measured, sha-pinned): under `reduce`, glass-ui `a11y-overrides.css`'s `*:not([data-allow-motion])` (0,1,0) beats demo `animations.css:184-193`'s `*` (0,0,0), both `!important` — the SERVED value is `transition-duration: 0.1s` with the property list rewritten to `opacity, color, background-color, border-color, box-shadow`. So a ~100ms leave window still runs, the outgoing pane is still in flow (D-2), and the one-frame deferred mount IS covered under PRM. LC's row restated the demo 0.01ms clamp as the served cascade — the exact stale-cell class the quarantine exists to catch; struck. K-6's arithmetic is also adopted: one rAF ≈ 16ms cannot account for 58-106ms; the remainder is chunk latency (D-6's). What survives: PaneSlot.vue:90 gates the deferral on a STRING (`if (!transitionName)`) while App.vue:302 computes `prmInstant` and never passes it down — a correctness/consistency defect, MINOR. |
| **PS-4** | CONFIRMED MINOR ("harmless by luck") | K-1: demote to INFO — writes are inert per `hasChanged` | **INFO — LC's K-1 wins on Vue semantics** | `RefImpl`'s setter gates trigger on `hasChanged(newValue, oldValue)`; App.vue:323-328 re-assigns the SAME instance on colour-tick re-renders, so the 360 writes notify no dep and cannot re-trigger App's render effect. The reason it "does not loop" is the equality gate, not luck — DU's "by luck" understates a structural guarantee. The churn mechanism is real (App.vue:90/:109/:135 inline arrows as `v-bind` PROPS defeat cacheHandlers; `rightProps("about")` closes over `model.value` + `deps.cssColor()` at usePaneRouter.ts:146-151, so `desktopRight.props` changes identity per tick — verified) and it IS PS-1's hazard surface during swap windows, but as a standalone row the cost is one closure allocation + compares per render. Dies with the channel inversion. |
| **D-4** | CONFIRMED MAJOR (in this corpus; PRM limb structurally sound) | K-8: misfiled — belongs to the App seat | **CONFIRMED MAJOR here, cross-booked — LC's kill DEMOTED to a scope annotation** | Every mechanism is verified live: App.vue:120 binds `pane-wrapper--ghost` off the INCOMING config; App.vue:403-409 sets `visibility:hidden; position:absolute; opacity:0; content-visibility:auto`; App.vue:59 drops `pane-container--dual` in the same frame (shell.css dual grid = the column switch). The ghost is the right slot's EMPTY-STATE contract and the three call sites are this component's public API surface — the corpus that audits the slot audits its wiring. `registry/adjudicated/App.md` carries **no** ghost row (my grep), so booking it here loses nothing and orphaning it to a ledger that didn't take it loses everything. One identity, cross-booked to App's ledger by this line. PRM limb re-scoped: the displacement is pure LAYOUT (`position`/`content-visibility`/grid-template are untouched by both guards — quarantine §1 clamps only `animation-*`/`transition-*`), so PRM-invariance of the layout jumps is structurally sound; but the axis's "byte-identical" TABLE includes transform-derived slide positions, which under the served `reduce` cascade (transform REMOVED from the transition set) cannot slide — the pixel rows go to UNPROVEN with that rider. |
| **D-7 final limb** | KILLED (comment misattribution) | (not addressed) | **KILLED — DU right** | App.vue:113-114's "always in DOM to preserve KeepAlive scroll position" annotates the DESKTOP-RIGHT wrapper staying mounted when `right === null` (the ghost) — read in place this seat. The breakpoint `v-if`/`v-else` (App.vue:77/:94) is the separately-documented X6 single-mount law whose stated purpose is the opposite (one live picker/WebGL context per viewport). The underlying fact — a breakpoint cross unmounts every cached pane — is TRUE and carried as an observation inside D-7; the "defeats a stated purpose" claim is not. |
| **D-5 HeroBlob limb** | KILLED | K-2: KILLED | **KILLED — unanimous, and verified** | HeroBlob.vue: `blobPaused` park is real (`:paused` bound at :17; set by the idle timer at BLOB_IDLE_MS+SLEEPY_POSE_MS; cleared on activity), and `onActivated` (:246-250) is the documented wake-gray cure paired with that park. The one site the axis cited as evidence of the debt is the one site that implements the contract correctly. The listener half of D-5 is untouched and CONFIRMED. |
| **D-8** | canon limb KILLED; two-clock limb folds into PS-1 | K-7 records the four-way corpus contradiction on the rAF | **canon limb KILLED; two-clock limb CONFIRMED inside PS-1/L-3** | VISUAL-CONSTITUTION:141 prohibits a "rAF-delayed **blank**"; PaneSlot's rAF produces a delayed SWAP with the outgoing pane painted throughout (default-mode Transition keeps it in flow) — the predicate is unmet, and the axis's own next paragraph concedes it. The §5.1 two-live-subtrees violation is real and rides PS-9. The four incompatible dispositions of the same forty lines (D-1 delete it / D-8 it's the canon violation / L "well-reasoned, survives" / PS-3 keep-but-gate) are recorded as a fleet-calibration fact; this seat's ruling is: the rAF one-shot is correctly IMPLEMENTED (see superlatives) and architecturally COUPLED — no wave may delete the simultaneous mode, the rAF mirror, or add loading states in isolation; they move together or not at all. |
| **K-5 (test truth)** | (not addressed) | counts wrong, substance survives | **ADOPTED with my own numbers** | `find test -name '*.test.ts'` → **26**, and demo/shell IS touched (status-lamp.test.ts imports `../demo/shell/dock/status-lamp`; picker-blob-config.test.ts resolves `demo/shell/dock/ActionToolbar.vue`). Restated precisely: **zero tests touch PaneSlot.vue, usePaneRouter.ts, or useViewManager.ts**, and the dock action-bar dispatch path (`usePaneRouter.ts:196-222`) is exercised by no spec — the C axis's mutation argument (deleting PaneSlot.vue:124 leaves every gate green) stands unrebutted. |

## Adjudicated defect roster

Severities are this seat's final ruling; dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `c654824e`; re-resolve before any cure. Wave routing per `registry/CARRY-CUT-LEDGER.md` §1 (CC-004→X-W5 · CC-005→X-W6 · CC-036→X-W2 · CC-037→X-W3 · CC-003 remainder→X-W4 · CC-007→X-W8 · CC-008→X-W9 · CC-009→X-W10 · CC-031→X-W1).

### Blockers

- **L-2 — BLOCKER — the mobile PaneSlot passes no `:on-mount`; the entire imperative command channel is structurally dead at mobile viewports.** Proven by enumeration (this seat): sole writer App.vue:325 in `onDesktopLeftMount` (:323-328); `on-mount` occurs at App.vue:105/:131 only (desktop); mobile slot :83-91 omits it; breakpoint gate :310-312. Dead consumers: App.vue:38 (dock action-bar feed), :41-42 (`@commit-edit`/`@cancel-edit` — the edit is silently discarded while `mobilePaneIndex` still flips to 1), usePaneRouter.ts:156-157, usePaletteWiring.ts:66-72 (first-colour-only fallback), :106-119/:122-126; `emitStartEdit` burns 40×50ms then `console.warn` (:37-58). Reachability confirmed: PalettesPane.vue:48 `@start-edit` ← the mobile pane-index-1 surface (usePaneRouter.ts:177-184). **ABSORBS reader-LC's missed find (one identity, same root): the three action-pane refs (`generatePaneRef`/`gradientPaneRef`/`mixPaneRef`, App.vue:317-319) are written only by the desktop `onMount` callbacks, while `actionBar` (usePaneRouter.ts:188-228) is computed off the route with NO viewport gate and reaches the dock through App.vue:39 → Dock.vue:40-41 → :153 `DockLayer v-if="hasAnyActionBar"` → :157 `GenericActionBar` (verified, no `isDesktop` on the path) — nine visibly-enabled buttons on three mobile views, every one a double-optional-chained silent no-op.** → **X-W5** (typed action contract): panes register commands (`onActivated`/`onDeactivated`-filled registry, provided once), breakpoint-invariant by construction; the poll, the `paneRefs` triple, and eight `?.()` maskings die in the same commit.
- **L-1 / PS-2 / D-5 (listener half) — BLOCKER — PaneSlot owns the app's only `<KeepAlive>` (PaneSlot.vue:120) and publishes no activation contract; cached panes' global listeners are immortal.** Census re-run this seat: 1 `onActivated` / 0 `onDeactivated` in all of demo/. Shipping sites: ColorPicker.vue:377 (`window keydown`, torn down only in `onUnmounted` :381-382 — which never runs: `runtime-core.cjs.js:6596-6598` returns into `deactivate` before any unmount-hook path, verified) with `preventDefault` arms on Escape/Enter-while-editing and cmd+k (:249-267, verified) + `useMagicKeys()` (:247); useSliderTouchGates.ts document `pointercancel`; ImageEyedropper.vue:239 conditional (mounted only while open). The census is **two shipping sites + one conditional** — the usePointerDebug row is killed (see register). The 23-line header documents everything except the one thing mounting here changes: `onUnmounted` becomes "runs never". → **X-W5** (the shell owns one gated key listener / the registry above; the header states the contract first). WebGL-context-retention hypothesis rides the **X-W2** re-measure.

### Major

- **PS-1 / L-3 (+ D-8's two-clock limb, + D-9's per-patch note) — MAJOR — the `onMount` JSDoc ("on mount, and null on unmount", PaneSlot.vue:47-52) is false, and App.vue files instances under the wrong pane.** Runtime verified this seat: setRef fires on every patch (`:5475-5476`), a function ref is invoked unconditionally (`:1814-1815`), and the stale-old-ref branch handles only string/Ref old-refs — a function old-ref is never retired (`:1799-1813`). App.vue:323-328 re-derives identity from `currentConfig` (synchronous with the route) while PaneSlot renders one rAF behind (:86-97) plus async-chunk latency — the outgoing instance is filed under the incoming pane's name. Good half verified too: setRef(null) fires before the deactivate branch (`:6589-6591`), so `onMount(null)` genuinely fires on deactivation. The 2 190 ms hold and the burst rows → UNPROVEN (the KeepAlive-activate patch path also fires setRef, bounding cached-target windows to ~1 rAF — LC's structural counter, adopted). → **X-W5**: report the key with the instance, or invert (`defineExpose({ instance, key })` + one derivation) — cross-wiring becomes unrepresentable.
- **D-2 — MAJOR — the default (simultaneous) mode leaves two in-flow panes in one ordinary box for the whole overlap.** Structurally certain from bytes read this seat: no `mode` attr (PaneSlot.vue:112-119); no `position: absolute` on any `vj-enter` leave override (animations.css:228-277); wrappers are flex-column/block/flex (shell.css:110-118 + App.vue:77/:96/:119). The pixel censuses (674→1254, the 722px teleport, 26-27 frames) → UNPROVEN; the co-mount itself needs no probe, and `frames/desktop-comount-right-slot.png` corroborates. → **X-W5**, COUPLED (see D-8 ruling): out-in-or-absolute-positioning + Suspense/loading + the rAF disposition move as one architecture, gated behind the UNPROVEN row-1 re-probe.
- **D-3 — MAJOR — the header's defensive claims are false, now FOUR of them.** (1) "CROSS-FADES": opacity pinned 1 on both sides (animations.css:228-236) — the CSS's own comment says "travel, not a fade"; PaneSlot.vue:21-23 and demo/DESIGN.md:288-291 both assert the fade. (2) "height-bounded (min-h-0 + --content-max-h)": the token caps `.pane-container` (shell.css:75), not the slot — with LC's K-9 precision adopted: transitively binding in the single-child case, false exactly for the co-mount it is invoked to defend. (3) "never jumps the layout": refuted structurally by D-2. (4) **ABSORBED from reader-DU's missed find, verified against `runtime-core.cjs.js:1521-1553` this seat**: "a late async chunk's arrival lands through … `.overture-appear-*`" (PaneSlot.vue:53-59, restated App.vue:113-117) is impossible — appear hooks are substituted only while `!state.isMounted`; a resolved chunk enters through the ENTER family (`vj-enter-*`, or the `''` no-op family at boot), and `@after-appear` never fires for it. → **X-W5** (the header is rewritten to the truth by whatever wave touches the file; the doc mirror rides PS-7).
- **D-4 — MAJOR (cross-booked to App's ledger) — `pane-wrapper--ghost` flips at the START of a dual→single swap**, hiding/absolutizing the departing right pane mid-leave while the grid drops `--dual` in the same frame (slide distance doubles mid-motion). Verified: App.vue:59/:120/:403-409. PRM: the layout limbs are guard-invariant (quarantine §1 clamps only animation/transition properties); the slide-position pixel rows → UNPROVEN with the transform-dropped rider. **ABSORBS reader-DU's missed find (verified): the ghost's exit fade is dead code — `transition-opacity duration-200` (App.vue:119) transitions opacity alone, while `visibility`/`position`/`content-visibility` flip on frame 1, so the affordance fades in and never out; `duration-200` is also a raw utility where DESIGN.md's recipe calls for tokens.** → **X-W5** (flip the ghost after the leave; one geometry truth), motion tokens → **X-W10**.
- **D-6 — MAJOR — ten async panes with no loading and no error state; the boundary is at the wrong granularity and latches.** Verified: usePaneRouter.ts:69-78 (bare `defineAsyncComponent` ×10); App.vue:50 wraps the whole grid; ErrorBoundary.vue `caught` latches with `reset()` as the only clearer and no route watch (my grep). The 2107-2505ms cold-route durations → UNPROVEN (dev-inflated; the axis says so itself). → **X-W5** (per-slot boundary keyed on `componentKey`, real loading affordance — the D-1-kill's coupling clause applies: this lands BEFORE or WITH any transition-mode change, never after).
- **D-7 — MAJOR — the `:max` values are hand-counted against a route table in another file, and the left-slot comment is inverted.** Verified this seat: LeftPane = 11 members (viewSchema.ts), keys are full view names (usePaneRouter.ts:165), `:max="6"`/`:max="9"` (App.vue:107/:88 — 11 and 15 distinct keys respectively), and the LRU prunes the OLDEST key on a miss (`runtime-core.cjs.js:2975-2978`, hit-refresh at :2973-2974) — admin visits evict the non-admin panes, the exact opposite of App.vue:97-100's claim. Scope limits carried: the right slot's comment is CORRECT (the `empty` key renders a Comment; KeepAlive bails before caching — Comment-bail verified at `:2945-2947`), and the breakpoint-cross limb survives only as an observation (misattribution killed). → **X-W5** (derive the bound from the registry or drop it).
- **PS-9 (+ D-8's §5.1 limb) — MAJOR — a wholesale `<main>` replacement with no announcement, no busy state, no focus contract; during the co-mount two live a11y subtrees coexist, neither inert nor AT-hidden.** Verified: PaneSlot.vue:111-129 renders no wrapper/role/aria; App.vue:47's static label never names the arrived pane though `leftLabel`/`rightLabel` exist unused (viewSchema.ts); nothing sets `inert`/`aria-hidden` on the outgoing pane; VISUAL-CONSTITUTION §5.1 ("no observer or microtask sees zero or two active subtrees") verbatim at :118. Honest negative preserved: PaneSlot contributes zero rows to the REPORT's tap-target/nameless-button counts. → **X-W5** (the atomic-commit + announcement contract IS the "one route one scene" charter).
- **L-4 — MAJOR — `usePaneRouter.ts` is a four-concern module and the dock imports its own types back out of it.** Verified: registry :69-95, prop factories :126-161, slot computeds :163-184, DockAction types + per-view tables :38-58/:186-228; Dock.vue:24 and GenericActionBar.vue:4 `import type … from "../usePaneRouter"` (type-only — the defect is ownership, not bundle weight, LC's note adopted). Nine private pane method names dispatched through `Ref<any>`. → action/type home moves with the **X-W5** registry; module split → **X-W8**.
- **L-5 — MAJOR — the shell statically depends on the palettes FEATURE for a platform capability, two paths to one session concept.** All five `SESSION_PORT_KEY` edges verified (Dock.vue:18, DockViewSelect.vue:8, ProfileSection.vue:14, MobileMenuDropdown.vue:13, SlugEditLayer.vue:5) + the direct edge ColorInput.vue:133 + the `shell → palettes → shell` type back-edge. Closure arithmetic (135/54/40%) → UNPROVEN. → **X-W8** (SESSION_PORT_KEY to `platform/auth/`; five one-line rewrites).
- **L-6 — MAJOR — features import upward into the app root's private `boot/`.** Both edges verified (ColorPicker.vue:129 `OVERTURE_KEY`; aurora-harmony-stops.ts:23) **plus reader-LC's third edge, verified: ColorPicker.vue:131 imports `COLOR_TARGET_PORT_KEY` from `../palettes/usePalettePorts` — feature-to-feature.** The directory-naming inversion (`demo/color-picker/` = the app; `demo/picker/` = the component) is real and aggravating. → **X-W8**.
- **NEW (reader-DU missed find 1, adopted) — MAJOR — deep-link cold boot strands the overture at B3 for the whole session; the hero-blob ornament never mounts again.** Every link verified this seat: main.ts:5-7 mounts synchronously before `router.isReady()`; useViewManager.ts:43-46 resolves the undefined initial name to `"picker"`; the EAGER ColorPicker mounts with `appear` under the 440ms+40ms `overture-appear` grammar (overture.css); the router then resolves, the key flips, and `leave()` cancels the in-flight appear — `el[enterCbKey](true)` → `cancelHook = onAppearCancelled || onEnterCancelled` (`runtime-core.cjs.js:1555-1585`, read this seat) — and PaneSlot binds ONLY `@after-appear` (:118). `noteLeftPlateSettled` is the sole non-PRM writer of `b3` (useOverture.ts:206-208; PRM arm :191-195); no b3 ⇒ no b3Complete ⇒ the B4 watcher (:154-183) never opens ⇒ ColorPicker.vue:164 `ornamentOpen` stays false ⇒ `<HeroBlob v-if="blobReady && ornamentOpen">` (:94-95) never mounts, for the session. Inverted a11y outcome: PRM users are immune, motion-enabled users are not. Cure is one binding (`@appear-cancelled`) or reporting the beat from the pane's own `onMounted`. → **X-W2** (boot choreography), live witness rides the visual audit.
- **NEW (reader-DU missed find 2, adopted) — MAJOR — every non-`/` cold boot mounts and permanently KeepAlive-caches the eager ColorPicker the user never asked for** (same chain links 1-2; usePaneRouter.ts:21/:83 = the one eager component; PaneSlot.vue:120 then retains it with its keydown listener and `useMagicKeys` scope live via the confirmed L-1 mechanism). This is the Q14/CC-036 boot-cost class sitting one line above the seat's subject. Cure: `router.isReady().then(() => app.mount("#app"))`. → **X-W2**.
- **NEW (reader-LC missed find 2, adopted) — MAJOR — the shell carries TWO disagreeing `isDesktop` predicates in violation of its own written single-truth law.** Verified: App.vue:310-312 `useBreakpoint("(min-width: 1024px) and (min-aspect-ratio: 1.1)")` (glass-ui) drives `data-layout` + the slot v-if; Dock.vue:71 `useMediaQuery("(min-width: 1024px)")` (@vueuse) — width-only. At 1024×1366 portrait the pane grammar is mobile while the dock believes desktop (`:always-expanded="!isDesktop"` :132, `mobileEditActive` :72, ActionBarToggle's `:is-desktop` :185). The law is stated twice (DESIGN.md:361; shell.css's MOB-1 block, read this seat) and neither document knows the third witness. → **X-W5**.
- **NEW (reader-DU missed find 4, adopted) — MAJOR — VISUAL-CONSTITUTION §3 law 6 forbids the mobile "global pane selector" and "left/right split state" in the same sentence D-11 quoted for scrolling — and the shell ships both.** Verified: VISUAL-CONSTITUTION:32 verbatim; Dock.vue:15/:197-198 (`PaneSegmentedControl` under `.dock-mobile-panes`, mobile-grammar-only per shell.css); the split state is `mobilePaneIndex` (useViewManager.ts:59-70) consumed by usePaneRouter.ts:177-184 — `usePaneRouter.mobile` IS the split. Either the canon or the shell must move; that adjudication is owner-visible. → **X-W5** (the canon re-author is in CC-004's charter).

### Minor

- **L-7 / PS-8 — MINOR — `componentFor(name: string | null)` widens the closed unions, forcing the unreachable masking fallback `return ColorPicker` (:94), stacked on `useViewManager`'s `isViewId(name) ? name : "picker"`.** Verified; the branch set exhausts LeftPane ∪ RightPane. LC's K-4 kill of the `/#/does-not-exist` corroboration is adopted (the router catch-all redirects first; the visual-REPORT row evidences the ROUTER, not this line). → **X-W3**, one identity with CC-037/K-25's "non-fail-closed pane fallback" row — typed `componentFor(name: LeftPane | RightPane)` makes the fallback unwritable.
- **PS-3 — MINOR (demoted; ruled above)** — the deferral gate is a string test standing in for a motion predicate; `prmInstant` sits one prop away. → **X-W10** (motion policy), premise per quarantine §1 only.
- **PS-6 / D-9 / D-10 hygiene cluster — MINOR — dead `max = 5` default (:32, all sites pass `:max`); `any` in the public prop surface (:52) propagating to `ref<any>` ×3 (App.vue:317-319 — NOTE this seat's correction: `colorPickerRef` at App.vue:223 is TYPED `InstanceType<typeof ColorPicker> | null`, so the corpus's occasional "ref<any> ×4" phrasing is wrong by one) and `PaneActionRefs { Ref<any> }` ×3; the `onMount!` non-null assertion re-asserting a separate read (:124); `transitionName: ""` as a load-bearing boot sentinel (:43-44 + App.vue:87/:106/:132; the honest forms are `:css="false"` or a boolean).** ABSORBS two verified reader-LC missed finds: the `| undefined` arm in `component: Component | null | undefined` (:38) is uninhabitable (producer type is `Component | null`), and **usePaneRouter.ts:100 already types one handle narrowly (`{ commitEdit; cancelEdit } | null`) eleven lines from the `Ref<any>` triple — proof the cure costs three interfaces.** → **X-W5** (dies with the typed channel).
- **PS-7 — MINOR — shipped docs assert the forbidden mode: DESIGN.md:278 names `panes/PaneSlot.vue` (no such dir), e2e/smoke/mobile/walk.spec.ts:48/:54/:137 describe `Transition mode="out-in"` and use it to justify `test.setTimeout(60_000)` (verified verbatim this seat).** ABSORBS reader-DU's verified missed find: DESIGN.md:270 cites the PRM guard at `animations.css:32-60` — the stagger utility; the guard lives at :184-212. → docs die with the **X-W5** file touch; the spec's timeout-justification fix rides **X-W1** (a gate that cannot fail for the right reason).
- **L-8 — MINOR — `ActionBarLayer.vue`'s in-SFC re-implementation of the removed glass-ui composable carries `void opts.containerEl; // signature parity with the retired producer composable` (verified :64-68).** Local one-liner + the standing glass-ui BH relay (a public content-swap composable). → **X-W4**.
- **L-9 — MINOR — IDENTITY FOLD (anti-rename).** tsconfig.demo.json paths: 3 dead entries / 2 live subpaths undeclared / "CLOSED 8-key set" false by one / the bare-specifier comment claim false — re-verified this seat (exports = 7 keys, no `.`; dist/index.d.ts missing; css+value shipped). Fact-identical to the ColorSpaceSelector adjudication's L-14, already FOLDed with a born-RED generator-parity command — ONE identity, cross-cited, never re-booked. → rides the existing booking (**X-W8** config parity).
- **L-10 — MINOR — `dist/subpaths/css.d.ts` duplicates `/color`'s type vocabulary as `_2` privates** — re-verified this seat: 382 lines, 25 `_2` hits. Program-wide library-band row recurring across sibling apotheoses; harm labeled hypothesis by its own axis; the npm-4.0.0 baseline comparison → UNPROVEN. → **X-W9** (one shared foundation declaration, per-subpath rollup change).
- **NEW (reader-DU missed find 5, adopted) — MINOR — on mobile every swap enters from the LEFT and leaves to the LEFT.** Verified: App.vue:77 gives the mobile wrapper `--left`; animations.css:228-231 therefore governs every mobile pane; the `--right` geometry (:233-236) is unreachable on mobile by construction — forward and back are motion-identical and the segmented control's direction is never expressed. → **X-W10**.
- **NEW (reader-DU missed find 7, adopted) — MINOR — `:on-appeared` on the desktop-right slot wires a documented empty function** (`noteRightPlateSettled`, useOverture.ts:230-232, body = a comment — verified), and it is doubly dead: the right pane at boot is async, and per the D-3(4) appear-gating proof `@after-appear` cannot fire for a late chunk anyway. Same species as L-8's parity param, in the L axis's own subject, plus a fresh prop identity per render (the PS-4 path). → **X-W2**.
- **NEW (reader-LC missed find 5, adopted) — MINOR — `whenColorPickerReady`'s give-up has no error posture**: a 2s user-visible stall terminates in `console.warn` and a `void` return no caller can observe (verified :49-58); on mobile this is the ONLY signal that an edit died. → **X-W5** (dies with the registry; until then the give-up needs a user-facing answer).

### Info

- **PS-4 — INFO (demoted; ruled above).** → dies with **X-W5**'s inversion; the hoist-the-arrows cleanup is free.
- **PS-5 — INFO (hypothesis, correctly labeled by its axis) — `component` is never watched; a component change under a stable key would be ignored forever.** Unreachable today (componentFor is pure in the key). → **X-W5** (a single `slot` object prop states the co-variance).
- **D-11 / L-11 / register gap — INFO — the mobile shell is a fixed non-scrolling viewport against VC §3 law 6's scroll limb (shell.css:19-27 verified); three hand-written wrapper class strings disagree (left centres, right does not); and `grep -i paneslot docs/tranches/V/PROPORTION-AUDIT.md` returns nothing while §3 requires a row for every route-level region — the pane slot IS the route-level region.** → **X-W10** (the register row + the proportion pass; the scroll grammar rides the law-6 adjudication in X-W5).
- **NEW (reader-LC missed find 3, adopted) — INFO — name collision: usePaneRouter.ts:61 exports `interface PaneSlot` shadowing `PaneSlot.vue` in the same two-file directory** (verified); the one module that renders slots from the router's output cannot import both under their own names. → **X-W5** (rename with the split).
- **NEW (reader-DU missed find 9, adopted) — INFO — PaneSlot parameterises the swap grammar but hard-codes the boot grammar**: `appear-from/active/to-class="overture-appear-*"` (:115-117) are string literals naming rules in the app root's private `boot/overture.css` — the same upward-dependency direction L-6 files as MAJOR, recorded as a PASS by the L axis. → **X-W2/X-W8** seam note.

## Killed-claims register

1. **D-1 as filed** (BLOCKER, "falsified by measurement", "delete the mirror state and the prohibition") — killed; refutation in the ruled-disagreements table (unpreserved artifact; probe never reaches the `afterLeave` handoff; intra-corpus contradiction; K-7's cure-regression). Residual → UNPROVEN row 1. Every "consequence of D-1" framing in challenge-D inherits this.
2. **PS-3's MAJOR premise, both readers' versions** — DU's target ("no cover under PRM: 0.01ms") and LC's confirmed-row restatement of the same 0.01ms cascade are killed against `motion-quarantine.md §1`'s measured winner (0.1s + property rewrite; cover exists). The 58-106ms attribution is killed on arithmetic (one rAF ≈ 16ms; the rest is D-6's chunk latency). Carried MINOR.
3. **D-5's HeroBlob limb** ("resumes a render loop it never pauses") — killed; the park is real and documented (HeroBlob.vue:17/:217-223, verified), and the site is the counter-example, not the evidence.
4. **D-8's canon citation** — killed; the canon prohibits a rAF-delayed BLANK and no blank exists (the outgoing pane covers the deferral); the axis's own paragraph concedes the predicate unmet. The two-clock limb survives inside PS-1.
5. **D-7's final limb** ("breakpoint cross defeats App.vue:113-114's stated purpose") — killed; the comment governs the desktop-right ghost wrapper, not breakpoint crossing (read in place); the X6 law's purpose is the opposite. The rotation-clears-cache fact is carried as an observation.
6. **PS-4's harm story and MAJOR severity** ("re-triggers App's render effect; does not loop only because the value settles") — killed on `hasChanged` semantics; demoted INFO.
7. **L-1's usePointerDebug census row** as a shipping leak — killed; the entire registration block is behind the `debug=1` URL gate (isDebugEnabled), so the shipping census is 2 sites + 1 conditional.
8. **L-7's visual-REPORT corroboration** (`/#/does-not-exist` byte-identity) — killed as misattributed; the router catch-all redirects before `componentFor` or the view fallback is reached. The type argument stands alone.
9. **PS-C test-truth counts** ("25 tests; not one touches demo/shell") — killed on both numbers (26; two suites touch demo/shell — re-run this seat); the substantive vacuity claim survives restated (zero coverage of PaneSlot/usePaneRouter/useViewManager; the dock dispatch path untested; mutation 2 uncatchable).

Also ruled against a reader: **reader-LC's K-8** (D-4 misfiled) is demoted to a scope annotation — D-4 stays on this ledger, cross-booked to App's (which carries no ghost row). **Reader-LC's K-9** is adopted as a precision edit inside D-3, not a kill.

## Missed-finds adjudication

15 submitted (DU 9 · LC 6), 15 verified by this seat with own instruments, 0 rejected. Adopted as new rows: B3 appear-cancel strand (MAJOR), eager-picker cold-boot cache (MAJOR), dual isDesktop (MAJOR), VC law-6 split-state limb (MAJOR), mobile left-only motion (MINOR), ghost dead exit fade (MINOR, inside D-4), right-plate dead pair (MINOR), give-up error posture (MINOR), PaneSlot name collision (INFO), welded appear classes (INFO). Folded into existing identities: the fourth false header claim (→D-3), the nine dead action-bar buttons (→L-2), DESIGN.md:270's stale citation (→PS-7), the typed-narrow sibling (→hygiene cluster), the `| undefined` arm (→hygiene cluster). The two boot-chain finds are the corpus's most consequential additions: three Opus axes each read PaneSlot's `<Transition>` and none noticed the unbound cancel hook; the fresh-eyes seats did.

## Superlatives — preserved with provenance

- **The exit law** (animations.css:252-277, verified): leave strictly shorter than enter (`--duration-fast`+`--ease-out` vs `--spring-snappy`), with the F2 mid-flight-collision diagnosis recorded in place. It is the only reason D-2's co-mount is merely ugly.
- **Compositor discipline** (animations.css:243-249, verified): `will-change: transform` minted only on `*-active`, dropped with the class — "never a standing will-change layer", stated and honoured, against a constellation history of layer-promotion epidemics.
- **The rAF one-shot is implemented correctly and completely** (PaneSlot.vue:78-97/:108, verified): cancel-before-reschedule, `commit()` reads live so A→B→C mounts only C, cancelled on unmount, and PaneSlot is never itself KeepAlive-cached so `onBeforeUnmount` is the right hook. Not a member of the PRM-RAF loop epidemic. (Correct implementation; coupled architecture — see the D-8 ruling.)
- **The same-key live-props gate** (:99-106): the "not mid-swap" predicate for the cost of one comparison, correctly commented.
- **Correct reactivity depth** (:74-76): `shallowRef` for component/props cells, plain `ref` for the key, zero `defineModel` — the project's own documented hazard avoided by the prescribed shape.
- **viewSchema.ts** — pure data, closed unions, one narrowing predicate, documented rationale per field; it is what makes L-7 provable at all.
- **ErrorBoundary is correctly BUILT even though wrongly PLACED** (verified: `role="alert"` + `aria-live="assertive"` + `tabindex="-1"` + `nextTick` focus + real button + `return false` ownership). D-6 is about granularity and latching, never construction.
- **The `[data-layout]` single-truth stamp** (App.vue:2/:310-312 + shell.css MOB-1 block): the CSS witness IS the JS witness, with the retired portrait-tablet pathology named — which is precisely why the Dock's third witness (missed find, MAJOR) is a defect and not a style choice.
- **The B4 gate** (useOverture.ts:143-183, verified): waits on animation STATE via `getAnimations`, filters infinite/scroll-driven timelines, bounds with a 500ms rIC ceiling + Safari fallback — better engineering than the strand that starves it.
- **Import closure = one file** (PaneSlot.vue:25, `verbatimModuleSyntax`-correct inline `type`); the two-grammar separation (appear vs vj-enter, ":the two grammars never share classes") is the right idea even though the late-chunk half of its docstring is false.
- **HeroBlob's `onActivated`** is the single site in the demo that got the KeepAlive contract right (park + wake + named fallback fence) — and two axes cited it as evidence of the debt.
- **challenge-C's negative-proof section and mutation argument** are the best-calibrated auditing in the corpus: eleven named non-findings including a self-killed hypothesis, and three exact mutations proving the gates vacuous — the shape of a coverage argument that earns its verdict.

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

1. **The out-in re-probe** — whether `mode="out-in"` still strands under vite dev on Vue 3.5.35 when composed with the REAL configuration (`--duration-fast` leave, `defineAsyncComponent` panes, the rAF mirror), against `demo/shell/PaneSlot.vue` itself, script + RESULTS committed. **No wave may delete PaneSlot.vue:12-23 before this runs.**
2. **All D-2/D-3/D-4 pixel figures** (674→1254 mobile wrapper, 722px teleport, 512→1042 width, 26-27-frame censuses, the PRM "byte-identical" table) — mechanisms structurally confirmed; numbers unpreserved. Rider: under `reduce` the served cascade drops `transform` from the transition set (quarantine §1), so slide positions cannot be byte-identical to the animated path — any re-measure must account.
3. **PS-1's 2 190 ms hold** — mechanism certain; magnitude entangled with chunk latency and bounded by the KeepAlive-activate setRef path for cached targets.
4. **D-6's cold-route durations** (2121/2505/2107/2110 ms) — dev-server-inflated; the unhandled STATE is confirmed, the numbers must not be quoted as production figures.
5. **The interactive Cmd+K / Escape / Enter swallow on a non-picker route** — listener survival + `preventDefault` arms confirmed statically; needs one manual keystroke, not a rig.
6. **The B3 strand's live witness** — deep-link cold boot to a non-picker view, later return to `/#/`, hero blob absent; the static chain is complete but the percept should be captured.
7. **L-5's closure arithmetic** (135 files / 54 palettes / 40%) — edges confirmed; counts from unpreserved scripts.
8. **L-10's regression baseline** (npm 4.0.0's 350 lines) — the duplication is measured (382/25 this seat); the regression comparison is not reproducible from this tree.
9. **PS-4's 360-writes counter and D-5's add=3/rem=0 listener rows** — no longer load-bearing (both derivable statically); recorded for completeness.
10. **PS-9's a11y probe row** — unnecessary (absence readable from the template) but a swap-announcement AFTER witness will be wanted by the X-W5 wave.

## Closing verdict

**DEFECTIVE — ADJUDICATED.** Two blockers survive every attack: the mobile viewport's entire imperative command surface is structurally dead (L-2, by enumeration, now including nine dock action-bar buttons), and the app's only KeepAlive converts `onUnmounted` to "runs never" for eleven panes with no published contract (L-1/PS-2/D-5). Ten MAJORs stand — four of them found only by the hostile re-readers, including a fully-static boot-choreography strand (`@appear-cancelled` unbound) that permanently amputates the product's signature ornament on deep-link boots. The corpus's loudest claim (D-1's out-in reversal) is killed as filed and survives only as the gating live re-probe; the rAF mirror, the simultaneous mode, and the missing loading states are ruled one COUPLED architecture that X-W5 must move as a unit. Dispositions route to X-W5 (the bulk: typed command registry, swap architecture, a11y commit contract, canon law-6 adjudication), X-W2 (boot: eager-mount, B3 strand, dead right-plate hook), X-W3 (the fail-closed pane fallback, one identity with K-25), X-W8/X-W9/X-W10/X-W1/X-W4 (boundaries, dist types, motion law + proportion register row, gate honesty, glass relay). Nothing here stamps a wave; every anchor re-resolves at cure time. Original challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.
