# SearchFilterBar — adjudicated apotheosis (M-23/L-18, fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (M-23 §1 / L-18) · **Date**: 2026-08-06 · **HEAD**: `3487d73e` (branch `tranche-u`; every line anchor is this seat's own working-tree read — re-resolve before any cure per D-19; `demo/palettes/browser/search/` clean in `git status`).
**Corpus**: `docs/tranches/V/megatranche/audit/components/SearchFilterBar/` — C axis r2→r5 (`challenge-C-implementation.md` unsuffixed governs, C-1..C-35 + H-x) · D axis pass1→pass5 (`challenge-D-design.md` governs, D-x/D2-x/P3-x/P4-x/P5-x) · L axis pass1→pass5 (`challenge-L-library.md` governs, L-x/LP2-x..LP5-x/P4-x) + probes/evidence/shots trees. The two returns adjudicated here are the **r2 hostile re-reads** (design-and-usage · library-and-code); both were attacked before either was adopted — this is the incredulity seat, and three of its rulings go **against** a reader (RULED #1, #4, #6 below).
**Subject**: `demo/palettes/browser/search/SearchFilterBar.vue` (249 lines, read whole by this seat) + consumer `demo/palettes/BrowsePane.vue` + sibling exemplar `UserSortMenu.vue` + sibling `TagEditPopover.vue`. Child `MiniColorPicker.vue` has its **own adjudicated apotheosis** (`registry/adjudicated/MiniColorPicker.md`, r2, 2026-08-04) — child-owned rows are **cross-booked to their MCP-x identities, never re-booked** (anti-rename).

**Arbiter's own instruments this session (zero browser tooling per law; all static + one type-gate run)**: full read of SearchFilterBar.vue (249 lines — every parent-side citation in both returns resolves byte-exact at HEAD); UserSortMenu.vue:1-20 (`variant="ghost"` at :8, `size="xs"` at :9, **and the uncharged `(v: any) => $emit('update:sort', v as string)` at :20** — this seat's own sharpening of the laundering family); TagEditPopover.vue:20-30 (`:checked`/`@update:checked` at :27-29, `:key="tag.name"` at :24, `hover:bg-accent/50` at :25); BrowsePane.vue:4-27 (the tray IS shipping: in-flow `flex flex-col gap-3`, `SearchBar` + slotted SearchFilterBar), :190, :329-356 (clearFilters zeroes the count; `colorSearchParams` + `Math.hypot` + `radius 0.15` + the "client-side is instant" comment + a `p: any` on the data path); useBrowsePalettes.ts:28-32/:40/:52-61/:114-120 (typed `sortMode` union; `value as typeof sortMode.value` — the unchecked `as` boundary); demo/palettes/api/palettes.ts:22-31/:44-54 (colorL/A/B/Radius declared AND serialised); api schema.ts:80-93 (`z.enum` sort/tier + color params validated); api crud-list.ts:159-181 (`colorMatcherFor`, same 0.15 default); demo/ui/dropdown-menu/index.ts (one line, all 14 symbols); demo/styles/utils.css:138-146 ("Compose, never replace" verbatim); PalettesPane.vue:28-36 (same `search-seated` SearchBar, same model); producer bytes: `checkbox/Checkbox.vue.d.ts` (**only** modelValue/defaultValue/disabled/value/id/class; emits **only** `update:modelValue`), `button/Button.vue.d.ts` (**no `variant`**, grep 0; `ButtonEmphasis` incl. `quiet`), compiled `emphasis: { default: "secondary" }` (button-Bu9F4uU6.js), `radio-group/styles.css` (**`.radio-group__item:focus-visible .radio-group__face { box-shadow: var(--focus-ring-shadow) }`** + forced-colors `outline: 2px solid Highlight` arm + seat algebra `--radio-seat: var(--touch-target, 2.75rem)`), `checkbox/styles.css` (**`.checkbox__seat` = 44px absolute-centred**; root 1rem face) + compiled class `checkbox control-surface glass-control-edge focus-ring tap-squish` + `checkbox__seat` rendered in glass-ui.js, `utilities/components.css` (file opens `@layer components {`; `@utility` census = exactly `shadow-cartoon`, `shadow-cartoon-hover`, `shadow-soft`, `shadow-elevated`, `shadow-modal`; **`.shadow-cartoon-sm { box-shadow: var(--shadow-cartoon-sm); translate: 0 -1px; }`**), `tokens/shadow.css` (plain `:root`), `theme.css` (**0** `@theme` blocks, **0** `shadow-cartoon` occurrences), `utilities/base.css` (`.focus-ring:focus-visible` 0-2-0 ring-only replace; the full `.interactive-item` recipe incl. `:focus-visible`/`:active`/`[data-disabled]`/`--radius-lg`), `a11y-overrides.css` forced-colors arm covering `.focus-ring:focus-visible` AND `.interactive-item:focus-visible`, `glass/material.css` (**`.glass-wash, .glass-quiet, .glass-resting, .glass-card { contain: paint }`**), field-control chunk (**`{ "aria-invalid": e, ...t } = i`** destructure + `Input inheritAttrs: !1` + `invalid?: boolean` in input/types.d.ts, 0 `aria-label`); repo counts re-run: spec files test/e2e/demo-test = **97** (drifted from the corpus's 94), subject references in them = **0**; `toString(16)` in src/ = **0**; package.json#exports = 7 keys no root; `strictTemplates|vueCompilerOptions` in tsconfig*/vite.config = **0**; `hover:shadow-cartoon` repo-wide = **1** (the subject, :76); `oklchStopToHex` present in glass-ui composables d.ts; tailwind lib.js `color-mix(in oklab, ${e} ${r}, transparent)`; `demo/palettes/browser/index.ts` consumers = **0**; **the P4-1 decider run by this seat**: `npx vue-tsc -p …/evidence-p4-L/tsconfig.strict.json --noEmit` → **10 unique diagnostics** (SFB 5·52·53·89·93·95·112·115 + MCP 48·51), pasted into my session log; canon verified verbatim: OPTICAL-BENCH-COMPOSITIONS.md:39 ("search/filter is a shallow leading tray"), VISUAL-CONSTITUTION.md:32 law 6 ("Secondary controls may enter a shallow disclosure region"), proportion-register.md:73-74 (PR-30/PR-31 `ALL18` mechanism rows), the P5-M1 grep re-run = **0 hits**; motion-quarantine.md §1 (both guards live; Guard 2 `*:not([data-allow-motion])` wins `transition-*`; **zero** M-xx rows name SearchFilterBar — no FALSE motion row exists to carry, and the one PRM claim in this corpus is killed consistent with that file, K-9).

## Scoreboard

**43 rows CONFIRMED (3 BLOCKER · 20 MAJOR · 15 MINOR · 5 INFO), of which 11 the original axes never filed** (7 from reader-A, 5 from reader-B, 1 overlap, + this seat's UserSortMenu:20 sharpening folded in) **and 1 severity demoted by this seat against reader-A** (SFB-24). **3 child identities CROSS-BOOKED to MiniColorPicker.md (MCP-2 · MCP-3 · MCP-7), zero re-bookings.** **14 claims KILLED** — including two BLOCKERs, one MAJOR whose mechanism was mutually exclusive with a banked MAJOR (LP3-1 vs C-17, settled from producer bytes), one reader-vs-reader conflict ruled (P4-3), and one reader's arithmetic settled by this seat's own gate run (P4-1: **10, not 11**). **11 residue items → UNPROVEN-NEEDS-LIVE** (visual-audit inputs).

Blocker-weight survivors: **SFB-1** (the Tags filter is a dead control that paints and announces state the product does not hold), **SFB-2** (the colour search silently substitutes a different colour for almost every input and asserts success), **SFB-3** (the count badge is clipped by the producer's own `contain: paint` and absent from the accessible name — the count exists for no one).

## Ruled disagreements and challenges (the incredulity seat's actual work)

1. **P4-3 — the one true reader-vs-reader conflict. RULED: reader-A wins; the disposition dies, the fact survives at MINOR.** Reader-B CONFIRMED "the `.filter-option` fork is forced, not chosen — record blocked-upstream"; reader-A KILLED the disposition. Both facts verify: `menuRowClass.d.ts` is unexported (B, accepted) AND `.interactive-item` is published, reachable CSS (`utilities/base.css` inside `@layer components`, imported at foundation.css via `@mkbabb/glass-ui/styles` — my read), AND the full `DropdownMenu*` family is one line away in `demo/ui/dropdown-menu/index.ts` (my read: all 14 symbols). Two compliant paths exist **today**; "blocked-upstream" would park a fixable row on a relay. Disposition: the demo fork is a demo defect (SFB-34, fixable now); the unpublished TS helper is a MINOR producer note riding the standing BH relay (publish `menuRowClass` or bless `.interactive-item` as the consumer contract).
2. **P4-1's arithmetic — RULED by this seat's own instrument.** Reader-B killed "11 errors on this component alone"; my re-run of the preserved strict config at HEAD emits **10 unique diagnostics, 8 on SearchFilterBar + 2 on MiniColorPicker** — B's kill CONFIRMED, and P4-4's split re-derives as **6 real drift / 4 producer-declaration noise** (SFB:93 `aria-label`, :95 `@keydown`, :115 `@click`, MCP:51 `@click` — all four reach the DOM by fallthrough/forward; verified `inheritAttrs: !1` + `forwardedAttrs` spread on Input, no `inheritAttrs` on Button). The substance of P4-1 (the gate is blind by one unset flag) is untouched and CONFIRMED (SFB-17).
3. **LP5-3 — SPLIT.** Reader-B confirmed it whole; reader-A killed the focus-ring leg. A is right: `.filter-option` sits on `<label>`s (:22/:34/:38/:50), which are not focusable — `:focus-visible` can never match, so the "lossy re-declaration drops the ring" leg prescribes a cure that paints nothing, and no ring is owed at that node (the producer controls inside each row carry their own registers — verified from radio-group/styles.css and the compiled `focus-ring` checkbox class). The `:active`/`[data-disabled]`/`--radius-lg`-vs-`--radius-md` legs and the zero-consumption fact stand (SFB-34).
4. **Reader-A's mr-1.5 severity — DEMOTED MAJOR → MINOR by this seat.** The fact is verified (:117 `mr-1.5` inside a producer Button whose `.button` gap is `calc(0.375rem * var(--ui-scale))` — doubled interval, physical axis) and the grep-zero across the 17-file corpus is real. But a 6px over-interval plus an RTL mirror edge on a row every executing cure already rebuilds is a rider, not a MAJOR; the RTL leg files as a family row with the badge's `-right-1 -top-1` (SFB-24). A MAJOR here would outrank SFB-13 (a false accessible contract) — severity follows consequence.
5. **The height row RE-HOMED (P5-B1's kill absorbed).** Reader-A killed P5-B1's species framing on canon this seat re-verified verbatim (the tray IS shipping at BrowsePane.vue:4-27; law 6 sanctions a shallow disclosure). What survives is what the corpus already banked four ways: **the disclosure is not shallow** — :16 sets no height contract, the producer PopoverContent carries no `max-h-*`/`overflow-y` (r2's dist verification, accepted), and a second dialog nests inside. Booked once as SFB-6, MAJOR, with the occlusion/zoom fractions in residue (R-2/R-4). P5-B1's "everything below is downstream of this" framing is dead; no wave may cite it.
6. **P5-B3 — RESCOPED, and reader-A's own rescope is tightened.** The kill of the WCAG 1.4.11 framing is sustained (1.4.11 does not demand a 3:1 fill against the container; the identifying glyph is the governed relation, and the pass never measured it). A's residual MAJOR is adopted **conditionally**: the structural premise (52%-alpha `glass-wash` composited over the same wash family, findable only by hairline + overhang) is source-plausible, but every number is unmeasured — so SFB-23 is MAJOR-RESCOPED with its entire quantitative basis in residue (R-3), and the re-argument MUST be on the glyph, per the kill.
7. **C-7's denominator drifted and neither reader caught it**: the corpus and both returns say 94 spec files; this seat counts **97** at HEAD (subject references still **0**). The claim survives — the count cell is corrected here so no wave gates on 94.

## Adjudicated defect roster

Original corpus ids kept in parens for life (anti-rename). Line anchors = HEAD `3487d73e`. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Wave owners: **X-W7** owns `SearchFilterBar.vue` by name (W7.md:89 modify; :288 names `:52,:53`, family rate 2/2 with TagEditPopover; :299 names the dead `update:selectedTags` emit) · **X-W4** owns it by name for a11y-control-targets (W4.md:126, :236) · X-W1 = re-gate/CI (CC-029..034) · X-W8 = barrels/config graph subtraction (CC-007) · X-W9 = parser+library surface (CC-008) · X-W10 = design/type/motion canon (CC-009). Producer seams ride the standing glass-ui BH relay (owner edict), never a local patch.

### BLOCKER

| id | claim | proof (arbiter's own unless noted) | disposition |
|---|---|---|---|
| **SFB-1** (C-1/D-1/D2-B1/P3-B1/P4-B1/L-1/LP2-1/C-35) | The tag Checkbox binds `:checked`/`@update:checked`, which glass-ui 7 does not declare; `toggleTag` (:197-203) is unreachable; the box ticks (reka `passive` branch) and **lies** — a filter UI that announces state the product does not hold | :51-55 my read; Checkbox.vue.d.ts my read (only `modelValue…class`; emits only `update:modelValue`); my strict-gate run reproduces TS2353 at (52,38)/(53,38); sibling TagEditPopover:27-29 identical (family 2/2) | **X-W7** — already named at W7.md:288/:299 (`:model-value`/`@update:model-value` at both sites; the dead `update:selectedTags` emit comes live) |
| **SFB-2** (C-2/D-19/D2-B4/P3-B4/P4-B5/L-5) | `applyColorSearch` gates a **total CSS parser** behind `/^#[0-9a-f]{6}$/i` (:218) and silently searches `pickerHex` for every other input — including the empty string — then lights the badge unconditionally (:220); the placeholder (:92) advertises `hsl(...)` the gate rejects; `parseColorIn` is imported (:145) and reached one line past the gate (:206) | :213-225 my read, structurally certain from the bytes; the `"none"`-channel guard (:207-209) defends the impossible while the gate silences the possible | **X-W7** — the colour-search commit; cure keeps and **widens** the :205-211 throw-guard (it becomes the live path when the regex dies — superlative S-7) |
| **SFB-3** (D2-B3/P3-B3/P4-B2) | The count badge (`absolute -right-1 -top-1`, :7-12) is clipped by the trigger's own `contain: paint` (the trigger carries `glass-wash`; material.css my read) AND `aria-label="Filters"` (:5) overrides the subtree per accname — the digit renders for no one and is spoken to no one. NOTE for the reformation: the governing D pass-5 demoted this mechanism-proved cross-engine BLOCKER to a state-matrix line; it is re-carried numbered here | material.css `contain: paint` on `.glass-wash` = my read; class-list capture ×4 in-corpus (accepted); accname override = spec-mechanical; the 49.3% painted-away pixel figure → R-9 | **X-W7** (badge/trigger recomposition) · accessible-name half cross-booked **X-W4** |

### MAJOR

| id | claim | proof | disposition |
|---|---|---|---|
| **SFB-4** (C-4/D-19/D2-M1/P3-M10/P4-B1/L-2 + LP2-2's surviving core) | `variant="ghost"` is not a `ButtonProps` member — inert at :5 and :112; renders `data-emphasis="secondary"` (filled `glass-wash glass-capsule`) where the author wanted `quiet` | Button.vue.d.ts grep `variant` = 0 (my read); compiled default `secondary` (my read); TS2353 ×3 in my gate run (SFB 5/112, MCP 48) | **X-W7**. Binding rider: the exemplar **UserSortMenu.vue:8 carries the same dead prop** — the `ghost → emphasis="quiet"` migration lands in the exemplar in the SAME change (see SFB-11); the 51-site repo census rides its own scope (the ColorSpaceSelector scope law) |
| **SFB-5** (C-17) | `hover:shadow-cartoon-md` (:76) generates **no rule**: `.shadow-cartoon-sm/md/lg` are `@layer components` classes, not `@utility`s; token in plain `:root`; 0 `@theme` blocks — `transition-shadow` transacts nothing on hover | My own @utility census + layer read + theme.css greps; repo-wide `hover:shadow-cartoon` = 1 (the subject) | **X-W7** — dies with the swatch row; see also SFB-25 (the `translate: 0 -1px` rider) and R-1 |
| **SFB-6** (C-3/C-15/C-27/P5-B2 structural + P5-B1-as-rescoped) | The disclosure is **not shallow**: :16 sets no height contract, producer PopoverContent ships no `max-h-*`/`overflow-y`, a second dialog (MiniColorPicker) nests inside; the menu's whole vertical economy is unowned | :16 my read; producer-dist absence = r2's verification, accepted; RULED #5 above | **X-W7** (consumer height/composition) + **BH relay** (PopoverContent height contract is a producer seam); occlusion/zoom numbers → R-2/R-4 |
| **SFB-7** (C-5/D-12/D2-m1/P3-m1/P4-M1/LP2-6) **≡ MCP-13** | The pending state is fiction: `async` with zero `await` (:213-225) — `searching` flips true→false in one tick; spinner (:102), `:disabled` (:98), all four `disabled:` utilities (:99) unreachable; parent handler synchronous too | :213-225 my read (emit synchronous, `finally` same-tick) | **CROSS-BOOKED → MiniColorPicker.md MCP-13, X-W7** — ONE identity; its binding cure-caveat (nothing consolidates onto the pending arm until the flag spans the upstream search) carries |
| **SFB-8** (C-16/P4-M2/P5-M3's `p-0`+`h-8` legs) | Two dead per-instance overrides: `p-0` (:16) beaten by the producer's `px-`/`py-` longhands (shorthand ordered first in the v4 sheet); `h-8` (:5) beaten by `.button`'s `min-block-size` — `w-8` lands, `h-8` does not, the 32×40 trigger ships | Mechanisms decidable from shipped bytes (both readers, accepted; consistent with my Button/styles reads); rect ×4 captures in-corpus | **X-W7**; the third P5-M3 row (PRM) is KILLED — K-9 |
| **SFB-9** (P5-M5) | Tag list has no length contract: no truncate/`title`/`min-w-0`; `.filter-option` flex with no shrink basis; authored `overflow-y: auto` mints an unauthored `overflow-x: auto` (CSS overflow-3); tag names are admin data | :49/:56/:240-247 my read; overflow-3 computed-value rule accepted | **X-W7**; geometries → R-6 |
| **SFB-10** (P5-M6) | 0-tag and failed-fetch render identically as **nothing** (:47 `v-if`); no loading/error/retry arm anywhere; props (:147-152) carry no request state; upstream `catch {}` (useTagEdit) | :47/:147-152 my read | **X-W7** (state contract) |
| **SFB-11** (P5-M8/D-4/D2-M6/P3-M7) | The sanctioned menu idiom is 58 lines away (UserSortMenu: DropdownMenu + Label + RadioGroup, `size="xs"`, `aria-hidden` glyphs) and the full family is re-exported in-repo; the migration stopped next door | UserSortMenu.vue:1-20 + dropdown barrel my reads | **X-W7**. Binding caveats (both readers converge): the exemplar is NOT clean — `variant="ghost"` at :8 (and the `v: any … as string` at :20, this seat) migrate in the same change; `size="xs"` is at **:9** not :12; the "one-line cure" framing is dead (K-14) |
| **SFB-12** (P5-M9) | "Search/filter chrome is one family" broken: both panes bind the SAME model (`pm.searchQuery.value`) through the same `search-seated` SearchBar — one query state, two placeholders, filter menu on Browse only | BrowsePane.vue:4-27 + PalettesPane.vue:28-36 my reads; canon verbatim | **X-W7** |
| **SFB-13** (C-19/D2-M8) | After a typed search the swatch fill AND its `aria-label` (:77-78) name a colour different from the applied filter — `applyColorSearch` never writes `pickerHex`; a false contract even for regex-ACCEPTED input | :175-187 (only writers) + :213-225 my read | **X-W7** |
| **SFB-14** (C-6) | "Clear all filters" unmounts itself while focused: `v-if` (:110) on the very count (:189-195) its activation zeroes (BrowsePane:329-332) | All four ends my read; structurally certain | **X-W7** — cure `:disabled` + stay mounted (also stops the menu resizing under the pointer) |
| **SFB-15** (LP5-1) | The published `@mkbabb/value.js` surface has **no hex writer** — parses `#rrggbb`, cannot print it; five consumer re-implementations incl. MiniColorPicker:103; glass-ui carries `oklchStopToHex` — capability flowing the wrong way on the inv-K-1 edge. The only corpus finding naming a cause, and it invalidates pass-1/2's prescribed cures (no library call to make at the last hop) | `toString(16)` src/ = 0, exports = 7 keys no root, oklchStopToHex present — all my greps | **X-W9** (library surface); consumer swaps follow the library, cross-note X-W7 |
| **SFB-16** (L-3/C-29/D2-m5) | The colour filter runs client-side over one loaded page and duplicates the API's shipped matcher; the wire params exist at BOTH ends and are dead (`currentFilterOpts` omits them) | All four ends my reads (BrowsePane:336-355 · palettes.ts:27-30/:50-53 · schema.ts:89-93 · crud-list.ts:159-181) | **X-W7** — three lines in `currentFilterOpts()` (see superlative S-2) |
| **SFB-17** (P4-1 substance; arithmetic per RULED #2) | The gate is blind by configuration: `strictTemplates` unset (repo grep 0, mine); with the preserved config the pair goes red with **10** diagnostics (my run) — 6 real drift / 4 noise (SFB-43). Overturns pass-1's producer-side closures | My gate run, pasted in session log | **X-W1** (re-gate wave) — flip the flag or adopt an equivalent gate; the 4 noise rows are producer d.ts gaps → BH relay note |
| **SFB-18** (C-7) | Zero coverage: **97** spec files (corpus said 94 — drift corrected, RULED #7), 0 reference the subject or child; three green signals (vitest, vue-tsc, eslint exit 0) assert coverage of a surface that has none | My re-count | **X-W1**; harness-cannot-mount-SFC identity = **MCP-39** (cross-book, one identity) |
| **SFB-19** (C-34) | The producer's render-effect canary for exactly the SFB-1 class exists (`reka-binding-idiom.test.ts`, header: "vue-tsc + units MISS them") and was never adopted; it is producer-repo test code, not shipped | File existence + header accepted from both readers (worktree read); the operative ask is r5's relay item 6 | **X-W1** (adopt the assertion shape) + **BH relay** (publish as consumer-facing contract) |
| **SFB-20** (reader-B missed #1, NEW; + this seat's :20 sharpening) | The closed `sort`/`tier` vocabularies are laundered to bare `string` across the emit seam (:154-161, `String(v)` at :21/:33) and restored downstream by an unchecked `as` (useBrowsePalettes:116-118); `tier` crosses with no cast at all; the only guard on a server-validated enum is a literal array in a template file; **UserSortMenu:20 ships the same idiom** (`v: any` + `as string`); off-vocabulary = a 400 at the wire, not a compile error | All ends my reads; schema `z.enum` verified | **X-W7** (typed emit seam on the owned file), cross-note X-W5 (typed action contract) |
| **SFB-21** (reader-B missed #2, NEW) | The D axis's banked error-state cure is structurally inoperative: the producer field-control destructures **exactly `aria-invalid` out** of forwarded attrs; the working path is the declared `invalid?: boolean` prop, never passed (:88-96). Under strictTemplates the dead binding is indistinguishable from the :93 noise — P4-4's triage applied mechanically would wave through the one binding that genuinely does not work | `{ "aria-invalid": e, ...t } = i` + `invalid?: boolean` + `inheritAttrs: !1` — my greps | **X-W7** as a **binding cure-caveat** on the error-state build; a11y cross-note X-W4 |
| **SFB-22** (reader-A missed #3, NEW) | The swatch's only accessible name is a raw hex literal (:78) — read out character-by-character; also the sole colour carrier under forced-colors; the repo owns a colour-name resolver (useColorNameResolution, 5 consumers). Sharpens C-30: 41 name-mutations per drag are 41 mutations of an unreadable string | :75-79 my read; resolver existence accepted (L-corpus enumeration) | **X-W4** (owns bar-side naming; MCP-20 precedent) |
| **SFB-23** (P5-B3-as-rescoped, RULED #6) | The trigger's affordance is a hairline: 52%-alpha wash over the same wash family inside a glass pill — real, corroborated passes 1+4 ("invisible except for a hairline" in dark). The 1.4.11 BLOCKER framing is dead (K-10); the re-argument is on the glyph | Structural premise from material.css (my read); ALL numbers → R-3 | **X-W7**; MAJOR contingent on R-3's glyph measurement |

### Cross-booked child identities (CONFIRMED here, booked THERE — zero re-bookings)

| corpus id | ≡ identity | note |
|---|---|---|
| C-33 (HSV↔hex write-back annihilates saturation; overturns r3 §4.2) | **MCP-2 (BLOCKER)** in MiniColorPicker.md | the parent echo :175-178/:68 is one edge of that loop; live thumb numbers → its residue |
| C-23 (drag latch: no pointercancel/lostpointercapture) | **MCP-3 (BLOCKER)** | canonical cure = SpectrumCanvas discipline, incl. MCP-42's ordering guard |
| C-24 (onPickerHexUpdate clobbers the typed query every tick) | **MCP-7 (MAJOR)** | same identity family as MCP-2: a controlled component rewriting its controller; parent handler owned X-W7 |

### MINOR

| id | claim | disposition |
|---|---|---|
| **SFB-24** (reader-A missed #1, **DEMOTED** per RULED #4) | Doubled icon-label interval: `mr-1.5` (:117) atop the producer `.button` gap = 12px vs the 6px system interval, on the menu's dominant row; physical-axis family with the badge's `-right-1 -top-1` (:9) under `dir="rtl"` (P4-M8 kin). Cure: delete `mr-1.5` | **X-W7** rider |
| **SFB-25** (reader-A missed #2, NEW) | The 1px optical-centre disagreement has its mechanism: `.shadow-cartoon-sm` carries `translate: 0 -1px` (my read) and the swatch (:76) is the one row element with no competing translate utility — P3-m4's measurement, finally connected; the MiniColorPicker sites are immune because Tailwind utility translates beat the components-layer rule | **X-W7** rider (`translate-none`, or the caster dies per VISUAL-CONSTITUTION §2 / P3-M6) |
| **SFB-26** (reader-A missed #4, NEW) | Two colour-mix spaces paint one popover's hovers: `.filter-option:hover` `in srgb` (:248) vs `bg-muted/50` → tailwind `in oklab` (:99, lib.js verified); also intra-file, tighter than LP5-3's cross-file version — adopting `.interactive-item` (itself srgb) alone leaves the fork standing | **X-W7**; the mixing-space law → **X-W10** |
| **SFB-27** (reader-A missed #5, NEW) | The tag scroller (:49) has no `overscroll-behavior` — end-of-scroll chains the wheel to the pane behind an anchored popover (the corpus's 253px drag-away, at any zoom); every in-corpus `overscroll-contain` proposal aimed at the PRODUCER; this is the consumer's own scroll container, one class, no relay | **X-W7** rider |
| **SFB-28** (reader-B missed #3, NEW) | The colour-search commit exists twice (:180-187 vs :213-225); the picker copy honours none of the guards (no `searching` check/set, no `try`) — the reentrancy guard protects one of two entry points, and every prescribed C-2 cure rewrites only one copy | **X-W7** — one commit function |
| **SFB-29** (P5-M2's surviving legs + P5-M4's mechanism) | Menu-geometry forks, mechanism source-certain: radio pitch = seat-gap `calc(2.75rem − 1.125rem)` = 26px vs tag rows `gap-0.5` = 2px; shrink-to-fit `<label>` hover boxes vs full-width column; the area INVERSION (Sort outweighs Find-by-Color) follows | **X-W7**; all rendered numbers → R-5/R-7 |
| **SFB-30** (P4-M7's residue per K-8) | The TRUE a11y rows the killed "namelessness" claim shadowed: two unnamed `radiogroup`s (:21/:33, no `aria-label`; `.section-label` divs :20/:32 carry no `id`) + the doubled utterance from the sibling `<span>` | **X-W4** |
| **SFB-31** (P5-m1/D-15/D2-M5/P3-m2/P4-m1) | Four section headings speak the Fira mono caption voice (`.section-label` = producer `text-mono-caption`, my read) and `.filter-option`'s `font-family: var(--font-serif)` (:243) is a no-op alias with a misleading name (foundation.css's own comment). FAMILY row — producer recipe + repo idiom, not local authorship | Sites **X-W7**; the voice law → **X-W10** |
| **SFB-32** (P5-m2) | `w-60` (:16) + the literal pile (`max-h-28`, `h-7 w-7`, `pr-16`, `h-6 px-2`, `gap-1.5`, `gap-0.5`, `-right-1 -top-1`) in a container-scaled system (VISUAL-CONSTITUTION §3 law 7) | **X-W7**; law → **X-W10** |
| **SFB-33** (P5-m3) | Two keyboard models in one menu: roving-tabindex RadioGroups (one stop each) vs one stop per tag checkbox — keyboard cost scales with data, compounding with the 112px window | **X-W4** |
| **SFB-34** (LP5-3-as-split per RULED #3 + LP5-2/LP5-4 + P4-3's fact per RULED #1) | `.filter-option` forks the published `.interactive-item` lossily (`:active`/`[data-disabled]` legs dropped, `--radius-md` vs `--radius-lg`; the recipe consumed 0 times, documented once in prose); the focus-leg and its cure are DEAD (K-5); `colorToHexString` alias + 3-of-20 `color-utils` facade feed the child's hand-rolled writer; `menuRowClass` unexported = producer note | **X-W7** (adopt `.interactive-item` or the DropdownMenu idiom, SFB-11); facade rows cross-note X-W9's consumer sweep; `menuRowClass` → **BH relay** |
| **SFB-35** (L-9) | `demo/ui/` = 19 dirs / 19 files / 20 lines of pure re-export (counts reproduced by reader-B; badge + dropdown-menu already present) | **X-W8** (CC-007 barrels); identity shared with the repo-wide `../ui` extirpation (ColorSpaceSelector L-4 / W-HYGIENE) — ONE identity |
| **SFB-36** (L-10) | tsconfig.demo.json `paths`: 3 phantom keys, 2 omissions, "CLOSED 8-key set" comment false — **identity = ColorSpaceSelector L-14** (W-HYGIENE config parity, born-RED generator command already booked there). Cross-ref only, NOT re-booked | **X-W8** via the existing L-14 row |
| **SFB-37** (L-11/LP2 §4) | G-DEMO eslint boundaries scoped to deleted trees (`demo/@` gone; `demo/palettes/**` in no rule's scope; eslint exit 0 on the subject) — **identity = ColorSpaceSelector L-5(a)** (lint-glob liveness, born-RED `--print-config` already booked). Cross-ref only | **X-W8** via the existing L-5(a) row |
| **SFB-38** (P4-2) | `demo/palettes/browser/index.ts` — the self-declared "stable public API" has **zero** consumers (my grep); both real reaches go to the sub-barrel. P4-2's delete-not-collapse disposition adopted over LP2-5 | **X-W8** |

### INFO

| id | claim | disposition |
|---|---|---|
| **SFB-39** (reader-A missed #7, NEW) | The naming triangle: file says Search (renders no search field — the field is BrowsePane's SearchBar), control says Filters (:5), first section is Sort (:19-28) which the badge deliberately excludes; the badge counts neither sort nor the text query. Cheapest fix in the ledger, first thing a reader trips on | **X-W7** |
| **SFB-40** (reader-B missed #4, NEW) | Both tag lists key `v-for` on `tag.name` (:50; TagEditPopover:24) while `Tag` carries `id` and `category` exists precisely because names are scoped — latent list-identity bug in surfaces already declared dead | **X-W7** |
| **SFB-41** (reader-B missed #5, NEW) | The child repeats the mixed-emit idiom charged only against the parent (MiniColorPicker :2/:51 `$emit` vs typed `emit` at :70/:107) — recorded for parity; dies with the child rebuild | X-W7 rider (child) |
| **SFB-42** (C-14/L-12/P4-m4) | Vestigial root wrapper (:2 wraps exactly one child — `gap`/`items-center` transact nothing); `@reference` (:236) buys nothing (scoped block uses only `var(--…)`); `$emit` at :21/:33 beside the typed `emit` (:154) | **X-W7** |
| **SFB-43** (P4-4, arithmetic per RULED #2) | Strict-template triage: **6 real / 4 noise** (not 6/5 of 11); classification sound (fallthrough verified), denominator corrected by this seat's run | Rides SFB-17 (X-W1) |

## Killed-claims register

| id | killed claim | refutation (sustained/derived by this seat) |
|---|---|---|
| **K-1** | **LP3-1 (MAJOR)** — the swatch's focus ring is "unconditionally beaten" by `shadow-cartoon-sm` because "every Tailwind utility is in `utilities`" | FALSE on all three premises, settled statically: `.shadow-cartoon-sm` is NOT a utility (my @utility census: 5 names, none `-sm/-md/-lg`; token in plain `:root`; theme.css 0 `@theme`); both rules share `@layer components`, where `.focus-ring:focus-visible` (0-2-0) beats it (0-1-0); the live sample was a t=0 read of a running `transition-shadow`. **Mutually exclusive with SFB-5 (C-17), and the bytes decide for C-17.** The real defect on the element is D-6's replace-not-compose → R-1. Both readers killed; verified here |
| **K-2** | **LP2-2 (MAJOR severity argument)** — with `variant` inert, `emphasis` defaults to "primary … the loudest register" | The compiled default is **`secondary`** (`emphasis: { default: "secondary" }`, my read); four independent in-corpus captures agree. The inert-prop core survives as SFB-4; the severity paragraph dies. Both readers |
| **K-3** | **P5-M7 (MAJOR, governing D pass)** — "no focus register in any of the three colour arms" for either selection species | Producer bytes (my reads): the radio ring lives on the CHILD face (`.radio-group__item:focus-visible .radio-group__face { box-shadow: var(--focus-ring-shadow) }` + forced-colors `outline: 2px solid Highlight`); the checkbox carries the compiled `focus-ring` class with base.css + a11y-overrides arms. P5-M7 re-filed with a weaker instrument (scripted `.focus()` never sets focus-visible) a claim pass-4 had already overturned with a real keyboard |
| **K-4** | **P5-M2's control-box leg / D-8 / C-9's 2.5.8 charge** — "44×44 radio beside a 16×16 checkbox, below the 24×24 floor" | `.checkbox__seat` = `var(--touch-target, 2.75rem)` absolute-centred, rendered as an `aria-hidden` span in every compiled Checkbox (my reads) — both species present identical 44px seats around 18px/16px faces. A producer superlative read as a producer defect (S-4). The pitch/hover-box legs survive (SFB-29) |
| **K-5** | **LP5-3's focus-ring leg** — ".filter-option drops the ring; none of the rows can show one; cure = `.interactive-item` on the rows" | The rows are non-focusable `<label>`s — `:focus-visible` can never match, the cure paints nothing, and no ring is owed at that node (the inner producer controls carry their own, K-3). Other legs survive (SFB-34) |
| **K-6** | **P5-B1 (BLOCKER)** — "the decided tray was transposed into an overlay species; everything below is downstream" | The tray IS shipping (BrowsePane:4-27, my read); VISUAL-CONSTITUTION law 6 sanctions a shallow disclosure (verbatim, my read). The true residue is the banked height defect — re-homed SFB-6, MAJOR. The occlusion arithmetic → R-2. No wave may inherit the "root of everything" framing |
| **K-7** | **P5-M1 (MAJOR)** — "no row in the proportion register = the root cause" | Grep re-run by this seat: 0 hits — but the register declares "rows group by mechanism, not wording" (verbatim), and PR-30/PR-31 are `ALL18` rows owning this component's focus/selection/micro-mark mechanisms (my read of :73-74). At most a W0-revalidation site-coordinate ask, MINOR-informal; not booked |
| **K-8** | **P4-M7 (MAJOR)** — "every option is nameless; the wrapping `<label>` is not consulted" | `<button>` is labelable; ancestor `<label>` names it — measured three independent times in-corpus (`nameFrom: relatedElement` ×5), incl. r4's own recorded hypothesis-kill; pass 5 silently dropped it without recording the kill. TRUE residue filed as SFB-30 |
| **K-9** | **P5-M3's PRM row** — the `.filter-option` transition counted among "three dead declarations" | Category error: the rule is live in the default arm; under PRM the app-wide a11y guard rewrites the property list BY DESIGN (`background-color` preserved at 0.1s) — motion-quarantine.md §1's measured two-guard law, and **zero M-xx rows name SearchFilterBar**; by the finding's test every transition in the repo is "dead". `p-0`/`h-8` remain genuinely dead (SFB-8) |
| **K-10** | **P5-B3 (BLOCKER)** — 1.01:1 fill contrast "fails WCAG 1.4.11" | 1.4.11 governs identifying information, not fill-vs-container; a glyph-identified quiet control is conformant by design, and the glyph was never measured. Rescoped MAJOR → SFB-23, argument owed on the glyph (R-3) |
| **K-11** | **P4-3's disposition (reader-vs-reader, RULED #1)** — ".filter-option is forced; record blocked-upstream" | Two compliant paths exist today, both verified by this seat (`.interactive-item` published+reachable; the 14-symbol DropdownMenu barrel). The unexported-TS-helper fact survives as a BH-relay note inside SFB-34. Reader-B's disposition dies; reader-A's kill sustained |
| **K-12** | **P4-1's arithmetic** — "11 errors on this component alone"; P4-4's "5 noise" | This seat's own run: **10** unique diagnostics, 8+2 across two files; noise list enumerates exactly 4. Substance survives (SFB-17/SFB-43) |
| **K-13** | **The pass-1/r4 MiniColorPicker coordinate system** (file "152 lines"; watchers/machines at wrong spans) | The file is 164 lines; correct coordinates are the ones passes 2/3/5 use — already adjudicated in MiniColorPicker.md's kill family. Binding: no wave opens a cure at pass-1/r4 coordinates. Carried, not re-derived |
| **K-14** | **P5-M8's clean-exemplar framing** — "a one-line cure … `size=\"xs\"` (`:12`)" | `size="xs"` is at :9; the exemplar ships the same dead `variant="ghost"` at :8 (my read) — four D passes tabulated it as the model and omitted the one row where the two files agree. The transposition row survives (SFB-11) WITH the migration caveat |

## Superlatives (provenance preserved)

- **S-1 · `demo/styles/utils.css:140-145`** — "Compose, never replace": a two-line rule whose comment states mechanism (layer vs unlayered), failure mode (silent beat) and discipline — verified verbatim by this seat. It is the cure the swatch (:76) never received (R-1/D-6), cited three times by the corpus without being recognised as the cure. (reader-A)
- **S-2 · the colour-search wire** — complete at BOTH ends and never driven: `colorL/A/B/Radius` declared + serialised (palettes.ts), validated (schema.ts z.coerce), implemented (`colorMatcherFor`, same 0.15 default). SFB-16's cure is three lines, not a feature build. All four ends re-read by this seat. (reader-B)
- **S-3 · `vite.config.ts` self-alias generation from `package.json#exports`** — anchored regexes with the postmortem in the comment; the discipline tsconfig.demo.json's hand-typed twin lacks (SFB-36's identity). (reader-B; also the ColorSpaceSelector record's S-row — one artifact, praised twice, consistently)
- **S-4 · glass-ui's seat architecture** (`--radio-seat`/`.checkbox__seat`, verified in producer bytes by this seat) — PROPORTION-AUDIT §5.7 law 7 solved invisibly for both species with forced-colors arms for face, checked state and focus. **Three audit passes filed this superlative as a defect** (K-3/K-4). (reader-A)
- **S-5 · the producer canary** `reka-binding-idiom.test.ts` — the one assertion in the ecosystem that fails on SFB-1; finding it converts "add tests" into "adopt the shape that exists" (SFB-19). (both readers)
- **S-6 · `demo/palettes/browser/search/index.ts`** — 5 lines, named re-exports, `MiniColorPicker` deliberately internal; the one seam in the cluster exactly as wide as it needs to be — against the 46-line parent with zero consumers (SFB-38). (both readers)
- **S-7 · `hexToOklab` throws on `"none"` channels** (:205-211) — failure-explicit, matching the library's posture; unreachable today only because the SFB-2 gate precedes it. **Binding: keep and widen when the gate dies — never delete with the regex.** (reader-A)
- **S-8 · the corpus's printed retractions** — r4 retracting its own CSSOM walk "as evidence for anything, including the parts of it that agreed with me", and L-pass3 retracting its layer-blind probe: the retractions are what let K-1 settle statically. (reader-B)
- **S-9 · SearchFilterBar.vue:82-86** — the S.W5-3/S-17 comment tying a per-instance `font-mono` to its register law: a jurisdiction-carrying exception; the one type decision the SFB-31 family row must NOT touch. (reader-A)
- **S-10 · BrowsePane.vue:210-220** — a defensive branch with provenance: names the exact Vue warning, the upstream cause, and the wave that added it. (reader-B)

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **R-1** — the swatch's settled focus state (:76): my static cascade says the ring wins and **REPLACES** the cartoon elevation (D-6's compose-never-replace defect, animated by `transition-shadow`); L-pass3's live cell says absent. Decide with a real keyboard Tab, ≥300ms settle, both engines. Whichever way it falls, K-1's mechanism stays dead.
- **R-2** — P5-B1's occlusion figures (37.9%/54.3%, stubbed 12-palette wall). Numbers only; the species argument is dead (K-6).
- **R-3** — P5-B3's composited contrast (1.01:1 / 1.19:1, `triggerProudOfField` 2.3px) **plus the glyph contrast 1.4.11 actually governs** — SFB-23's MAJOR is contingent on this cell.
- **R-4** — P5-B2's 200%-zoom reachability fractions (structural half CONFIRMED in SFB-6).
- **R-5** — P5-M4's area-budget percentages (mechanism certain: 26px vs 2px pitch; the inversion is real).
- **R-6** — P5-M5's tag-scroller geometries (stubbed 10-tag catalogue).
- **R-7** — P5-M2's rendered pitch/hover-box numbers (56.97 vs 32.97px; 61.14→182px).
- **R-8** — P5-i2/P3-M9: click-outside close drops focus to `<body>` (Escape/trigger restore correctly) — two passes, two engines, two schemes: carry **PLAUSIBLE**, do not re-probe from scratch.
- **R-9** — SFB-3's painted-away figure (49.3%) and the `contain` computed read (mechanism source-certain from material.css).
- **R-10** — the WebKit tab-traversal divergence (3-of-17, exits at press 4) — contested in-corpus, its own author calls it hypothesis; **must not be carried as a finding** until reproduced with Full Keyboard Access state recorded.
- **R-11** — C-33's rendered thumb teleport + quantisation tables → MiniColorPicker.md's residue (child-owned).

## DISSENT / notes preserved

1. **Identity guards (anti-rename), stated once**: C-33≡MCP-2 · C-23≡MCP-3 · C-24≡MCP-7 · C-5-cluster≡MCP-13 · harness≡MCP-39 · tsconfig≡ColorSpaceSelector-L-14 · eslint-globs≡ColorSpaceSelector-L-5(a) · `demo/ui` extirpation≡ColorSpaceSelector-L-4/W-HYGIENE. Eight identities that a careless formation would book twice; none is booked twice here.
2. **Reader-A's mr-1.5 MAJOR** is preserved as its position (2× the system interval on the dominant row + an RTL direction defect + a zero-mention corpus gap); this seat demoted to MINOR (RULED #4). If the owner weighs corpus-blindness as its own severity axis, SFB-24 is the row to revisit.
3. **Reader-B's "blocked-upstream" position on P4-3** is preserved: the TS-typed recipe genuinely is unpublishable today, and B's instinct (record producer gaps as producer gaps) is the right law applied to the wrong layer — the CSS layer was published all along.
4. **Both readers converged independently** on K-1's kill, K-2's kill, and the exemplar-drift fact (A as missed #6, B as kill #14) — treated as one identity each; the convergence was checked for citation-inheritance and their evidence chains differ (A argues from C-17's mutual exclusion; B from the utility census + the D-pass1 opposite measurement), so it is corroboration, not inheritance.
5. **Severity ordering (unresolved, preserved)**: reader-A ranks SFB-3 (the badge) among the blockers on mechanism-proof strength; a case exists that SFB-1 alone outranks everything (a dead control announcing false state on a data-filtering surface). The wave cures both; no ruling required.

## Closing verdict

**ADJUDICATED r2 — A FILTERS MENU WHOSE THREE LOUDEST CLAIMS ARE FALSE: the Tags section is a dead control that ticks and lies (SFB-1), the colour search substitutes a different colour and asserts success (SFB-2), and the filter count renders for no one and is spoken to no one (SFB-3).** 43 rows confirmed (3·20·15·5) — 11 of them never filed by the original axes — 3 child identities cross-booked to MiniColorPicker.md, 14 claims killed (two BLOCKERs, one mechanism settled byte-statically against a banked MAJOR's mutually-exclusive twin, one reader-vs-reader conflict ruled, one arithmetic corrected by this seat's own 10-diagnostic gate run), 11 residue cells handed to the live pass with their instruments named. Rows land **X-W7** (the owned bulk: bindings W7.md:288/:299, colour-search, menu recomposition, wire hookup, typed emit seam) · **X-W4** (naming, groups, keyboard economy) · **X-W1** (strictTemplates gate, coverage, canary) · **X-W8** (barrels/config, via existing identities) · **X-W9** (the hex writer) · **X-W10** (voice/spacing/mix-space canon) · **BH relay** (PopoverContent height, `menuRowClass`/`.interactive-item` contract, canary publication, d.ts attr gaps) — every row exactly one disposition, original ids for life. These dispositions are ADJUDICATED, not VERIFIED; nothing here stamps a wave. Original challenge files and both reader returns remain immutable beside this record per E-1/E-3.
