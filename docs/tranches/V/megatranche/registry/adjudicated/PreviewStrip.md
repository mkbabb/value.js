# PreviewStrip — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · **model receipt: `claude-fable-5` (Fable 5)** — observed in this seat's own system prompt, not inherited

**Seat**: apotheosis adjudicator (incredulity seat) for `demo/color-session/color-chips/PreviewStrip.vue` (76 lines) + its 4-module closure (`sample.ts`, `color-utils.ts`, `picker-color.ts`) and both hosts (`GenerateControls.vue:245/:274`, `AuroraPane.vue:132`).
**Date**: 2026-08-06 · **HEAD at adjudication**: `8eca29f0` · branch `tranche-u`. Challenge corpus authored at `f36f780c` (C pass-2's own provenance note; `PreviewStrip.vue` byte-unchanged from `c654824e`). All line anchors re-resolved by this seat against the working tree before citation (D-19).
**Corpus (r2 governs)**: `challenge-C-implementation.md` (pass 2, supersedes pass 1) + `challenge-C-implementation.pass-1-2026-07-28.md` · `challenge-D-design.md` · `challenge-L-library.md` (pass 2) + its pass 1 · probe trees `probes-D2/`, `probe-L-*.mjs`, `probe-C2-*.mjs`.
**Readers adjudicated**: reader-DU (design-and-usage hostile re-read) · reader-LC (library-and-code hostile re-read). Both were attacked before either was adopted; every verdict-level disagreement was re-derived by this seat with its own instruments.

**Arbiter's own instruments this session (static + node against the tree and the shipped dist; ZERO browser tooling per law — probe parsimony)**: full source reads of PreviewStrip.vue (whole), PreviewRamp.vue (whole), sample.ts (whole), aurora-harmony-stops.ts (whole), generate-color.ts:1-40/:90-150/:200-250, color-utils.ts:18-30, picker-color.ts:200-215, MixConfigBar.vue:55-75, GenerateControls.vue:85-101/:225-250/:258-280/:295-305, AuroraPane.vue:125-135, o14-preview-truth.spec.ts:155-175/:330-395, test/preview-chips.test.ts:1-40 (+ `grep -c aurora` → **1**, the false docstring itself), pass-1 C-9/C-10 (:370-407); **the C-14/C-2/ramp-leg decider (my own node run against `dist/subpaths/css.js`)**: `serializeCssColor` emits `oklch(62% 0.27 9.8deg)` opaque, `oklch(62% 0.27 9.8deg / 50%)` at α=0.5, **no suffix at α=1** — and the o14 regex (`[\d.]+`) extracts **0** triples from that dialect, **1** from the aurora dialect; **the D-3 decider (my own rng-stream instrument, replicating `generatePalette`'s verbatim draw order)**: first-7 (l,c) draw positions identical across count 8 vs 12 for `golden` and `analogous` only; **shifted** for complementary/triadic/split-complementary/random (each consumes one draw per index inside `generateHues` BEFORE the l/c loop begins) — so exactly ONE harmony (`golden`, zero hue draws) is prefix-stable, `analogous` moves by hue geometry (`step = spread·2/(count−1)`), and the other four move by l/c stream shift; token-chain reads in the installed glass-ui 7.0.0 dist: `menuRowClass` emits `interactive-item glass-menu-row … text-dropdown`, `.text-dropdown{font-size:var(--dropdown-text)}`, `--dropdown-text: var(--control-text)`, `--control-text: calc(var(--type-small) * var(--ui-scale))`, `--space-phi-5: 2.618rem` + `--space-phi-6: 4.236rem` shipped in `tokens/sizing.css`, exports map ships `./chip` first-class; VISUAL-CONSTITUTION.md:75 read verbatim ("control or label, including dropdown options → text-small"); ring-occlusion.json read whole (fontChain: chip 16.4px ← `DIV.interactive-item` 16.4px ← `DIV.max-h-[inherit]` **18.608px** — the size is SET at the row, not inherited from the scroll container; sweep counts 1→12: held 8..12 / painted 7 / truncated true); repo censuses: `2\.618` in demo → the two chip literals + `--phi-4: 2.618rem` (foundation.css:462, Tailwind-bridged :156) + the ColorComponentDisplay clamp family; `data-color-surface` → exactly the 2 CSS rule bodies (:694/:837), zero adopters; `min-w-\[17rem\]` in GenerateControls.vue → **2**, AuroraPane uses `min-w-menu` (4 sites); `PreviewStrip|preview-strip|STRIP_SEGMENT_CAP` in test/ e2e/ → motion-quarantine.md rows naming this component → **0**; package.json read live: dependencies exactly `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}`, exports = 7 keys no `.`, version 4.0.0, no `dist/*.d.ts` at root, `dist/subpaths/` = 7 pairs (no parsing/units); probe forensics: `probe-L-resolve.mjs` → **0** hits for `resolveModuleName|paths` (the L2-2 attribution kill), `probes-D2/forced.mjs` → **0** hits for `count|slider|Slider` (the mask-erasure kill). Sibling apotheoses read for identity guards: **PaletteColorStrip.md** (PCS-2/PC-7 roster identity + AdminTagsPanel grade-lives-at-home precedent, PCS-10 chip-family residue, PCS-13 ≡ L-5(a), PCS-30 ≡ L-14, PCS-31 RTL readiness, K-7 glass-Strip-mint kill), **GenerateControls.md** (R-A o20 arbiter run 1-failed/1-passed, R-C `data-color-surface` DEAD ruling, R-E preview-bench + cap-asymmetry note, wave `V·MEGA-W-GEN-1`, GenerateControls.vue PINNED), **AuroraPane.md** (AP-5 X.W6.i bounds rider, AP-9 false-oracle row, AP-19 aurora homing, AP-27 aria-hidden equivalent, R-5 LabeledField ruling), **ColorSpaceSelector.md** (format exemplar; L-14/L-5(a) identities); routing joints: `docs/tranches/X/waves/W1.md` (CC-031 e2e-HARD + oracle-slate), `W6.md`/`W7.md` (:208-212 mask-fade idiom)/`W8.md`/`W9.md` (.d/.f/G29/G31)/`W10.md` heads; CARRY-CUT-LEDGER §1.B–§1.M.

**Environment disclosure**: no browser was opened in this seat. Every ruling below is decidable from source + the shipped dist + the corpus's own committed artifacts; the readers' live cells (chromium) are adopted as corroboration only and labeled UNPROVEN-NEEDS-LIVE where they are load-bearing as pixels.

## Scoreboard

**35 rows CONFIRMED (1 BLOCKER-class BY IDENTITY · 17 MAJOR · 11 MINOR · 6 INFO) · 14 kills sustained in the register — 13 reader kills ratified (each re-derived by this seat) plus ONE reader-confirmation cell killed by this seat's own instrument (reader-LC's D-3 counter-scoping) · 11 missed-by-the-axes findings adopted (readers' union, all re-verified; 3 resolved by identity with sibling apotheoses) · 16 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

**Blocker-weight survivor — the verification vacuum, a MERGED condition**: the component family's entire oracle surface is structurally unpassable or nonexistent, unobserved by CI, and doubly asserted otherwise in prose. o20's strip leg compares author-dialect stamps to computed styles (string-unequal by construction — my dialect run; ≡ GenerateControls R-A); **the o14 ramp leg has the identical disease** (PS-7, the corpus-wide miss: `parseOklchTriples` extracts 0 triples from the `%`/`deg` dialect the shipped serializer emits, so its own `expect(painted.length).toBe(stamped.length)` is `expect(N).toBe(0)`); zero tests name PreviewStrip; e2e is absent from CI (≡ X-W1 B3); and two docstrings assert a vitest oracle over the aurora truth function that does not exist (≡ AP-9). The one cited working precedent in the corpus is not working. Nothing guards the component whose declared subject is truth.

## Ruled disagreements (the seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-3 scoping** | killed the generalization: "only `golden` is index-invariant; for five of six harmonies the l/c stream is shifted" | killed the same limb the OPPOSITE way: "true for 5 of the 6 harmonies, false for one — golden/complementary/triadic/split-complementary/random are prefix-stable because generatePalette draws seedHue once then exactly two rng values per index" | **reader-DU wins; reader-LC's counter-scoping is KILLED by this seat** | My rng-stream instrument against the verbatim draw order (generate-color.ts:90-150/:213-232): `generateHues` consumes **one draw per index** for complementary/triadic/split-complementary/random (the ±5° jitter) and ALL of those draws precede the l/c loop — so changing count moves the stream position of every l/c pair and the first seven COLORS change even though the first seven HUE BASES do not. Measured: first-7 (l,c) draw positions identical across count 8 vs 12 for golden and analogous only; analogous then moves anyway via `step = spread·2/(count−1)`. **Exactly one harmony freezes: `golden`.** reader-LC's "draws exactly two rng values per index in fixed order" is false — it forgot the jitter draws. The finding itself survives at full strength on the shipped default: `harmony = ref("golden")` (useColorGeneration.ts:22), so every Preset-menu row IS frozen above the cap on first open (the E-3 sweep is real; the painted hues step by exactly 137.5°). C pass-2's own note (:339-342) was correctly golden-scoped all along. |
| **D-1 grade** | MAJOR (downgrade: casualty is a 1px hairline on an aria-hidden decorative chip) | BLOCKER (as the axis filed) | **MAJOR — reader-DU wins** | The occlusion mechanism is certain from paint order (inset shadow above own background, below in-flow descendants; PreviewStrip.vue:63-64 vs :67-70, no padding/border so children cover the padding box; PreviewRamp escapes via `background-image` on self, :35). But the casualty is a decorative hairline: nothing unusable, no value misreported. Precedent cuts the same way: the sibling's boundary-absence row (PCS-5, "no boundary of any kind … collapses into the card ground") was graded MAJOR at its home. The BLOCKER register in this program is reserved for shipped falsehood or destroyed capability (ColorSpaceSelector L-1 class). The structural root is PS-1 (the drift), and the cure is PS-2's well/gutter — not a shadow relocation. |
| **C-1 grade + cure** | MAJOR | BLOCKER, and killed pass-1's prescribed cure (stamp `visible`, host stamps a scalar total) as structurally unsound for o20's element-wise referent | **MAJOR, with reader-LC's cure-kill ADOPTED** | The dual referent is real and provably unequal above the cap (my read of :44 vs :32-35; the sweep's held 8..12/painted 7). But `data-stops` is a test-facing DOM attribute — no user-facing surface misreports. MAJOR by the same discipline as D-1. The cure-kill stands on its own logic: o20:89-105 asserts ELEMENT-WISE stamp↔plate equality; a scalar `data-preview-total` cannot serve it, and above the cap the DOM would carry no full-stop list at all. A sound split needs two stop lists or the cap moved to the host — the alternative pass-1 itself offered and did not carry through the gestalt (the C-6 "cure adjudicated separately from defect" law, ColorSpaceSelector addendum item 2). |
| **C-2 grade + state** | (confirmed via C-5; killed its negative proof — see K-8) | BLOCKER; its own live run cell (2 failed at the dock fixture) left UNPROVEN | **MAJOR — resolved BY IDENTITY** | ≡ **GenerateControls.md R-A/G1**: that arbiter ran the spec fresh — **1 failed / 1 passed**; test 2 deterministically RED on the exact encoding mismatch, test 1's failure a transient of the s3:D14 instability class. reader-LC's "2 failed at dock.ts:78" cell (from C pass-2's P-5) is superseded by that fresh run — the deterministic truth is one leg RED-by-dialect, one leg green-but-vacuous, CI runs neither. The o20 repair is already gated at `V·MEGA-W-GEN-1` G1; the CI limb is ≡ X-W1 CC-031 (B3/B7: 6 of 6 Playwright projects CI-orphaned). Not re-booked; identity preserved. |
| **C-6/D-5 grade** | MINOR (2.4% aspect error on a 42px decorative plate = code hygiene) | MAJOR ("φ² only by coincidence" + token ignored twice) | **MINOR — reader-DU wins** | The mechanism (one law, two unit systems: `2.618rem` root-relative × `1em` inherited) is certain from source and confirmed in the artifact (ratio 2.5548 at 16.4px local em). But both axes' severity rested on the "anchored to no type role" story, which is DEAD (see K-1): the em anchor is `--type-small × --ui-scale` via the row's own `.text-dropdown` — the constitution's ratified dropdown-option rung (VC:75, my token-chain read). What remains is a unit-hygiene defect plus token duplication, cured in one line. Merged here: D-7.4 (the literal ×2 vs `--phi-4`) and the missed triple-naming row (glass ships `--space-phi-5: 2.618rem` in the same served cascade — THREE names + two raw literals for one quantity). |
| **D-8 grade** | MINOR (RTL limb doubly latent; perceptibility limb live) | MAJOR (as the axis filed) | **MINOR — reader-DU wins** | The RTL limb is latent twice over (reka's SelectContent stamps `dir: rootContext.dir.value`, default ltr — reader-DU's producer read; ≡ PCS-31's precedent: `index.html` hard-pins `dir="ltr"` as BOOKED U-F58 readiness). The forced-colors limb is KILLED (K-3). What is live is a ~4.5px alpha ramp on a ~6px sliver as the only 7-vs-12 signal — real, small, and dying anyway with PS-9's structural-elision cure. The mask's TRUTH violation is a separate, stronger row (PS-10) that neither axis filed. |
| **L2-2 grade** | MINOR | MAJOR | **MINOR — resolved BY IDENTITY** | Fact-identical to ColorSpaceSelector L-14 ≡ PCS-30 (the same tsconfig.demo.json block, the same 3 phantom targets, 2 omissions, the same false "CLOSED 8-key set" comment — re-verified live by this seat: exports = 7 keys, no `.`, no dist root d.ts). That identity is graded MINOR and already carries a born-RED generator-parity command; X-W8.md:112 explicitly declines it. The AdminTagsPanel precedent (grade lives at the identity's home) forbids re-grading it MAJOR on a sibling docket. reader-LC's independent `ts.resolveModuleName` reproduction is adopted as the mechanism cell of record (self-name resolution through the real exports map; both configs decoration). |
| **D-2 disposition** | killed whole (gate 1 + the 22-of-24 framing + the dark-scheme BLOCKER row), conceding "the real finding is D-1 plus the missing well" | (did not adjudicate D-2; filed its census UNPROVEN) | **RESCOPED, not wholly killed** | The census-as-defect-count DIES (K-6): per-stop contrast against the menu ground is a property of the previewed DATA — a pastel preview must look pastel, an analogous pair must be near-identical; gate 1 as written would forbid the component from ever truthfully previewing them, contradicting the constitution clause D-3 itself cites (VC:194 "truthful previews") and WCAG 1.4.11's essential carve-out. The dark-scheme "BLOCKER" state row dies the same way (any opaque specimen on an inverted ground inverts; a scheme-adaptive arm would falsify the specimen). What SURVIVES is D-2's own cure paragraph: the specimen well — VC §2 assigns "palette" an "opaque/quiet neutral stage; the specimen supplies color" — and the structural gutter, which is the BOUNDARY meeting 3:1, not the specimen. That surviving core merges into PS-2. |
| **C-11 cure story** | confirmed the row and sharpened it (`.generate-swatch` IS on the roster, so one route's plate keeps colors while its own preview chips blank) | confirmed the row; separately found `[data-color-surface]` has ZERO adopters — falsifying the "established escape hatch" mechanism story | **Row CONFIRMED; the prescribed one-attribute cure is OVERRULED BY PRIOR RULING** | GenerateControls.md **R-C** (ruled before this corpus existed): `[data-color-surface]` is a DEAD selector with zero producers, deletable, and `V·MEGA-W-GEN-1` deletes it from foundation.css. PCS-2's rider already names the collision: X-W7.c must pick ONE mechanism (hook revival or roster-class membership) for the whole strip/chip family, "never both waves acting blind". The C-axis cure ("add `data-color-surface`, no roster edit") would adopt a selector another adjudicated wave is deleting. The membership defect stands (BLOCKER-class BY IDENTITY at PC-7); the mechanism is decided ONCE at X-W7.c, with the preview chips named in the decision. |

## Adjudicated defect roster

Severity discipline: BLOCKER = shipped falsehood/destroyed capability; grade-lives-at-home for identity folds. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. "Chip-family residue" = the NAMED NO-WAVE-OWNER identity minted at PaletteColorStrip.md PCS-10 ("the chip-family unification residue — PreviewStrip/PreviewRamp/dot-rows — named so it is not lost"); every row routed there joins that one identity for the next formation boundary, never a second name.

### BLOCKER-class

- **PS-3 · C-11 + D-4 — forced-colors/print erase the chip's entire payload; the component is absent from both arms of the demo's own COLOR-SURFACE ROSTER; the 42px reservation outlives the glyph.** Payload is exclusively child `background-color` (:50) + a box-shadow ring — both WHCM-substituted; `forced-color-adjust` never declared; neither `.preview-chip` nor any chip selector in foundation.css:678-694 or the print arm :830-841 (my reads); the sharpest instance is reader-DU's: `.generate-swatch` IS rostered (:689), so in WHCM the `/generate` plate keeps its true colors while the preview chips of the same screen blank. On `/atmosphere` the chip is the row's only second-line content → six labels over blank lines. Rendered pixels (all-white segments, `distinct: 1`) are chromium-cell, UNPROVEN-NEEDS-LIVE here; the mechanism is structurally certain. **DISPOSITION: FOLD → PC-7 (grade lives there: BLOCKER) → X-W7.c**, carrying (i) the **R-C reconciliation rider** (see ruled disagreements — the wave picks roster-class vs hook ONCE; `data-color-surface` is currently ruled DEAD and deleting), (ii) the reservation decision (structure that survives WHCM, or collapse — "auto is not a decision", D-4's one surviving sentence), (iii) PreviewRamp enrolled in the same act. **NAMED GAP**: if X-W7.c's file bounds exclude `color-chips/`, this row degrades to the chip-family residue at execution — recorded so it cannot be silently lost.

### MAJOR

- **PS-1 · C-7 / D-7.1 — `.preview-chip` is minted twice in two disjoint `<style scoped>` blocks and the drift is load-bearing**: `inline-flex` + `overflow: hidden` (PreviewStrip.vue:57/:62) vs `inline-block`, no overflow (PreviewRamp.vue:41), same four core declarations otherwise; scoped compilation means no single edit reaches both; the F7/F8 ring rationale comment survives only on the ramp (:46); `color-chips/index.ts:2-4` claims "ONE focused common module … never a per-pane copy". This drift is the proximate cause of PS-2's asymmetry (ring paints on the ramp, never on the strip) — the strongest structural finding in the corpus, both readers agreeing. **Rider (D-7.3, un-carried by both readers, adjudicated here)**: glass-ui 7.0.0 ships `./chip` first-class (my exports read), so the demo's unrelated `.preview-chip` class is a name collision with the design system's own vocabulary; and PCS K-7 already killed the "lift into a glass Strip primitive" cure (no such export exists — any producer-rooting is a dated glass-forward BH-relay ask, never a local mint). **DISPOSITION: chip-family residue (≡ PCS-10, identity preserved) — NO-WAVE-OWNER, named**; the rooting question rides the standing glass-ui BH relay with the `./chip` census pasted.
- **PS-2 · D-1 + D-2(core) — the declared inset hairline ring never paints (occluded by the component's own opaque children), and the plate has no specimen well: the payload sits directly on translucent menu glass with no boundary of any kind.** Mechanism certain from CSS paint order (see ruled disagreements); the corpus's controlled A/B (identical resolved shadow, payload location the only variable → 1.00 CSS px vs zero) is the exemplary instrument and is adopted; live pixel columns UNPROVEN-NEEDS-LIVE. The surviving D-2 core: VC §2 assigns this species an opaque/quiet stage + structural gutters; the boundary (not the specimen) owes 3:1. Note the coupling this seat adds: the plate's having NO background of its own is also what makes PS-10's mask lie possible — one well cures both. **DISPOSITION: the well/gutter/boundary cure rides the X-W7.c boundary-class decision beside PCS-5 (same class, same print-rider: `@media print * { box-shadow: none !important }` kills any inset-ring cure in print); the strip-local edit is chip-family residue.** MAJOR (grade ruling above).
- **PS-4 · C-14 (+ L2-8 limb) — `stampStops` is documented "ONE serialization" and is a bare `join("|")`; the two hosts of this one component stamp two mutually unparseable dialects, and the repo's only `data-stops` reader parses neither of the two that matter.** My own node run: the shipped `serializeCssColor` emits `oklch(62% 0.27 9.8deg)` (generate + ramp chain, traced by this seat through MixConfigBar → sampleInterpolationRamp → serializeStop → colorToCss → serializePickerColor); aurora hand-rolls `oklch(0.7123 0.1 240)` (aurora-harmony-stops.ts:26-28/:39); o14's `parseOklchTriples` extracts 0 and 1 triples respectively (my regex run). L2-8 folds in as the same defect's other face: one host re-implements the library's serializer in five lines in the repo that exists to dogfood it, while `serializeCssColor` is published on `/css` two modules away. **DISPOSITION: the canonical-serialization cure is chip-family residue; the aurora serializer swap rides X.W6.i beside AP-5 (whose bounds rider already extends to `aurora-harmony-stops.ts`).** Any cure must cover all three dialect sites and re-oracle BOTH species (PS-7).
- **PS-5 · C-1 — `data-stops` carries the truth-function referent and the paint referent at once; above the cap they are provably unequal, so o20 and o14 want opposite things from one attribute.** :44 stamps ALL stops, :47-51 paints `visible` = `slice(0, 7)`; sweep artifact: held 8..12 / painted 7. MAJOR (grade ruling above); pass-1's split-referent cure killed as filed (K-12). **DISPOSITION: chip-family residue, with a REGISTERED COUPLING to `V·MEGA-W-GEN-1` G1** — o20's seed-exact law cannot be re-authored honestly until the above-cap referent is decided (two lists, or cap-at-host).
- **PS-6 · C-2 — o20, the strip's only oracle, is structurally unpassable on its dialect leg and no CI gate runs it.** ≡ **GenerateControls.md R-A/G1 — identity preserved, not re-booked**; the fresh arbiter run of record is 1-failed/1-passed (deterministic RED on encoding), superseding this corpus's 2-failed cell. CI limb ≡ **X-W1 CC-031** (e2e restored HARD; oracle-slate successor asserts no CI-orphaned project). **DISPOSITION: FOLD both identities.**
- **PS-7 · missed-DU#1 (ADOPTED, re-proven by this seat) — the o14 ramp-leg byte-identity test, the corpus's one cited working precedent, is structurally RED for the RAMP species too; no green paint-oracle exists for either chip species anywhere in the repository.** My own chain-trace + node run: the mix chips' stamps ride the `%`/`deg` dialect; `stamped = stops.flatMap(parseOklchTriples)` = **0** triples; computed `background-image` canonicalizes unitless → `painted.length = N`; the assertion at o14:378-381 is `expect(N).toBe(0)` — RED whenever chips render, and the preceding `expect(n).toBeGreaterThan(0)` fails if they don't. Red-and-unobserved beside o20 (e2e not in CI). This kills C-2's negative proof in both passes (K-8) and invalidates pass-2's "reuse parseOklchTriples" cure — the parser cannot read the format the repository actually stamps, in either species. **DISPOSITION: rides PS-4's canonicalization (both species re-oracled together); CI limb ≡ X-W1.**
- **PS-8 · C-5 — vacuous gate: zero tests or e2e reference PreviewStrip by any name; the o14 chip leg is structurally unable to reach the strip species** (visits Mix only — PreviewRamp hosts; reads `backgroundImage`, which is `none` on a strip; `STRIP_SEGMENT_CAP` is module-private so no oracle could name the cap); test/preview-chips.test.ts exercises `sampleInterpolationRamp` only — a function the strip never calls; and the pass-1 mutation argument stands (delete the paint binding, every gate stays green). My grep: 0 hits across test/ e2e/. **DISPOSITION: the strip oracle is authored with the chip-family residue cure; the CI limb ≡ X-W1 CC-031.**
- **PS-9 · D-3 / C-3 (as rescoped by this seat's instrument) — above the cap the preview stops tracking the control that drives it, on the SHIPPED DEFAULT: `golden` is the one prefix-stable harmony and it is the default, so on first open every Preset row paints one picture for counts 8–12, distinguished only by a mask on a 6px sliver.** Scoping settled at ruled disagreements: five of six harmonies escape (my rng-stream run) — reader-LC's opposite scoping killed (K-5), reader-DU's kill of the D-axis generalization ratified (K-4). The failure is of COMPLETENESS and DISCRIMINATION, not of content (the head slice is a true prefix of what selecting yields) — D-3's "not truthful" headline overstates; its structural observation stands (a head slice is the least informative elision; the app's own PALETTE-CONTRACT SVG/PNG strips render all N). **DISPOSITION: chip-family residue — the elision becomes structural and honest (resampled range + explicit count, or plate-length-follows-data), the adjudicated cure altitude; the D-axis's per-route prescriptions are design options, not rulings.**
- **PS-10 · missed-DU#2 (ADOPTED, verified) — the truncation mask makes the seventh painted stop report a color that is not that stop's color, and the reported color is a function of whatever page content sits behind the translucent menu.** Verified by this seat: the plate declares NO background (:56-65 — radius, size, overflow, shadow only), so `mask-image: linear-gradient(90deg, black 20%, transparent 95%)` on `:last-child` (:73-75) composites the segment straight through to the SelectContent glass and the live page — by scheme, route, scroll position, and the aurora field's current frame. sample.ts:15-19 states the governing law verbatim: "a chip that approximates the library output is FORBIDDEN." The cap discards stops honestly; the mask is the ONE place the component paints a stop AND paints it wrong — a truth violation neither axis filed (both saw only imperceptibility/direction). **DISPOSITION: chip-family residue — the mask dies with PS-9's structural cue; PS-2's well would independently close the composite-through channel.**
- **PS-11 · D-6 — on `/atmosphere` the chip alone occupies the SelectItem `#description` slot, buying every Harmony row a +15.7–18.9% height tax for an aria-hidden mark at ~1.5% of row ink, on the exact route PROPORTION-AUDIT PR-10 orders TIGHTENED.** Producer mechanism verified (the description slot is unconditionally a second flex line); numbers are chromium-cell (UNPROVEN-NEEDS-LIVE as pixels, internally consistent across two seats). The "six chips near-identical pink family" limb stays UNPROVEN (seed-dependent). **DISPOSITION: X.W6.j** — the AuroraPane row re-composition owns this anatomy (beside AP-14/AP-27/AP-30c), with a **reconciliation note**: AP-R-5's ruling preserved the strip and the SelectItem description mechanism against a LabeledSelect migration; D-6's protagonist-promotion cure (chip into the name lane, one line) is compatible with that ruling and must be decided inside X.W6.j's re-composition, not beside it.
- **PS-12 · C-8 / D-7.2 — a third mint of the same species is imported into the same file**: GenerateControls.vue:16/:20 imports both `PaletteColorStrip` and `PreviewStrip`, rendered on one screen (:135 vs :245/:274); diverged prop shapes, a11y, truncation policy; the sibling carries the WEIGHT_FLOOR legibility mechanism the preview chips lack and lacks the cap they have. **≡ PCS-10 (grade ruled MAJOR there; identity preserved, not re-booked).** **DISPOSITION: FOLD → PCS-10** — strip-side consolidation at X-W7.c, chip-family residue for the rest.
- **PS-13 · L2-1 — `package.json#dependencies` declares glass-ui (5.2M) and keyframes.js (608K) as RUNTIME dependencies of a library whose 10 emitted ESM files contain ZERO bare specifiers** (reader-LC's scan + `libraryExternal: []` by construction; keyframes.js is imported NOWHERE in this repo, dev or prod; glass-ui correctly declares value.js as an optional peer — the shape value.js fails to mirror). Deps re-read live by this seat. The already-public 4.0.0 manifest is the only shipped defect in this corpus. The self-install/6MB-per-consumer framing is killed (K-9). **DISPOSITION: X-W9.f (the 4.1.0 cut edits package.json; G29/G31 already read the manifest) with a NAMED GAP** — no X file names the devDependencies move; W9.f must adopt this row explicitly or it degrades to NO-WAVE-OWNER at execution.
- **PS-14 · L2-3 — the published `Color<S>` is non-distributive, so every space-generic demo call site defeats the type system: 9 casts, two of them (`sample.ts:82`, `picker-color.ts:116`) inside this component's stop-producing closure.** Cast census re-cited; reader-LC's kill of the axis's "no library change" demo-side cure is SUSTAINED (their 8-module compile: baseline 0 → alias-swap 15 errors → distributive-alias 6 errors; the `Extract<AnyColor,{space:S}>` mint is the load-bearing adapter until the library publishes a distributive `ColorIn` and retargets `convertColor`/`mixColors` return positions). **DISPOSITION: X-W9.d (public-surface law) with a NAMED GAP** — `ColorIn` appears in no W9 mechanism text; adopt explicitly or degrade to NO-WAVE-OWNER.
- **PS-15 · L2-5 — the demo layering law has rotted open: the two eslint objects carrying the layer bans glob `demo/@/**` (deleted at W43), and the forbidden feature→boot edge is live one hop from the subject** (`aurora-harmony-stops.ts:23` imports `color-picker/composables/boot/atmosphere-calibration`, serving AuroraPane.vue:132's stops). **≡ ColorSpaceSelector L-5(a) ≡ PCS-13 — ONE identity → X-W8** (W8.md:42/:250 deletes the dead globs; born-RED `eslint --print-config` command already banked). reader-LC's narrowing kill sustained (K-11): the `demo/color-picker/**` globs in the same object are LIVE — "every demo gate matches zero files" is false by one object. The live-edge datum itself **≡ AP-19** (the bidirectional aurora area edge, NO-WAVE-OWNER at the formation boundary) — both identities preserved, nothing re-booked.
- **PS-16 · L2-6 (+ missed-LC#3 folded) — `picker-color.ts` is a second color model inside the chip's closure: two re-minted library type names (53 + 33 references), a 17-space channel-range table and immutable per-channel setters the library does not publish — plus DEAD public surface: `ChannelMeta`, `channelMeta`, and `PickerColorError` (the module's ENTIRE error taxonomy) have zero external consumers**, which is what makes L2-7's throwing seam indiscriminable at every call site. **DISPOSITION: NO-WAVE-OWNER, named** — the promotion question (channel model down into the library) is X-W9-adjacent but unnamed in any X file; the dead exports die with whichever wave first touches the module.
- **PS-17 · L2-7 — "sample an interpolation into k stops" has six homes, two identically-valued k constants under two names, four failure policies — and the chip module's own row breaks its declared contract: `sample.ts:48-50` documents "returns null when the preview has nothing TRUE to say" while :82's `serializeStop → … → valueOrThrow` THROWS on the same line.** Static escape path confirmed by this seat's read; a live crashing input remains unconstructed (residue). **DISPOSITION: NO-WAVE-OWNER, named** — the consolidation spans mix/gradient/ink/chips and no X wave owns the set; the null-vs-throw contradiction is chip-family residue (it lives in sample.ts).
- **PS-29 · missed-LC#1 — two docstrings assert a vitest oracle over the aurora truth function that does not exist** (aurora-harmony-stops.ts:11-13; test/preview-chips.test.ts:5-9 naming `resolveCalibratedAtmosphere` among its "library leaves"): that host's stop pipeline has zero coverage at any level while the prose claims otherwise. Verified by this seat (`grep -c aurora` in the test file → 1, the docstring itself). **≡ AP-9 (MAJOR, X.W6.i — o25/o26 oracle surface) — identity preserved, FOLD**; this docket adds only the `test/preview-chips.test.ts:5-9` limb, which AP-9's cure must also correct.

### MINOR

- **PS-18 · C-6/D-5(core) + D-7.4 + missed-DU#4 (merged) — one golden-plate law written on two unit systems (`2.618rem` × `1em`), the literal hardcoded in both chip files, while THREE names for the same 2.618rem quantity coexist in one served cascade** (`--phi-4` foundation.css:462 + Tailwind bridge :156, glass-ui's shipped `--space-phi-5` — my dist read — and the two raw literals). φ² holds only where local em = root rem, which is nowhere measured. **DISPOSITION: chip-family residue for the one-line unit fix; the token-name unification (demo re-mints a producer token under a different name AND ordinal) is an X-W10 canon question, recorded there.**
- **PS-19 · D-8 (as regraded) — the elision cue is a ~4.5px alpha ramp on a ~6px sliver, physical-direction (`90deg`) over a flex row.** RTL limb doubly latent (≡ PCS-31 readiness ledger); forced-colors limb killed (K-3). **DISPOSITION: chip-family residue — dies with PS-9's structural cue.**
- **PS-20 · D-9 — `v-if="stops.length >= 1"` admits a single-stop "palette" wearing full palette grammar** (reachable: `:min="1"`, GenerateControls.vue:301; sweep[0] recorded it live); the ramp's floor is `>= 2` for the stated reason. **DISPOSITION: chip-family residue.**
- **PS-21 · C-13 (structural limb) — the one un-memoized read in a file that owns the computed pattern** (`stampStops(stops)` template call at :44 beside the :32-35 computeds), and both hosts call truth functions in template position, minting fresh array identities per row per render — AuroraPane's re-running a full calibrated atmosphere resolve per harmony per render. Timing numbers UNPROVEN-NEEDS-LIVE; adjacent bench of record: GenerateControls R-E (0.96ms at count 5 / 2.80ms at 12 for the truth functions — the cost is the DOM, not the math, exactly as C pass-2 argued). **DISPOSITION: chip-family residue; the aurora per-open cost limb rides X.W6.i's harness (AP-5/AP-9 instruments).**
- **PS-22 · C-10 (pass-1 orphan, carried) — the elision count ("12 colours · showing 7") is unavailable to AT anywhere in the row, and the strip is informative-but-hidden.** Aurora limb **≡ AP-27 (X.W6.j — an sr-only/`aria-describedby` sentence derived from the same stops); identity preserved.** Generate limb: chip-family residue (the description lane owes the count when truncation fires). **DISPOSITION: as split.**
- **PS-23 · missed-DU#3 ≡ missed-LC#2 (convergent, + my own run) — `serializeStop`'s alpha-trimming `.replace(/ \/ 1\)$/, ")")` is unreachable dead code written against a serializer that does not exist**: at α=1 the shipped serializer emits no suffix at all; at α≠1 it emits ` / 50%` (percent) — my node run, both branches. A legacy shim in the module whose subject is "the ONE serialization", one layer below `stampStops` — it dies inside PS-4's cure. **DISPOSITION: chip-family residue.**
- **PS-24 · missed-DU#5 — the chip's proportion leaked into its host as `min-w-[17rem]` twice** (GenerateControls.vue:229/:263, comments attributing the number to this chip), against the house token law (`--menu-min-w`/`min-w-menu`, foundation.css:109/:445) that the sibling host obeys at 4 sites (my greps). **DISPOSITION: GenerateControls.vue is PINNED (per its own adjudication) — BANKED to the W25 pinned-file executor rows with the `17rem → min-w-menu` reconciliation named** (the honest width may exceed 11rem; the executor re-derives the token or a chip-family width token, never a literal).
- **PS-25 · L2-2 — the tsconfig.demo.json paths block: 3 phantom targets, 2 omissions, a comment false by one.** **≡ ColorSpaceSelector L-14 ≡ PCS-30 — ONE identity, NO-WAVE-OWNER preserved** (X-W8 declined; born-RED generator command banked at the identity's home). Grade ruling above (MINOR).
- **PS-26 · L2-4 — working-tree `dist/subpaths/css.js` ≠ the 4.0.0 registry artifact by 1 byte of entry-local minifier allocation** (chunks byte-identical; behaviour-neutral; proves `npm run prepare` does not reproduce the published bytes). Not re-run by this seat; reader-LC's cmp loop adopted. **DISPOSITION: FOLD → X-W9.f — a fresh dated 4.1.0 cut regenerates dist and moots the divergence (G29).**
- **PS-27 · L2-9a — the library's src-mirror test tree holds TWELVE demo suites (count corrected by reader-LC against the axis's 10, verified at the axis's own HEAD), including the chip module's only unit oracle**, against vitest.config's stated law. **DISPOSITION: NO-WAVE-OWNER, named** (test-tree re-homing has no X owner).
- **PS-28 · L2-9b — `demo/shared/utils.ts:16` justifies its debounce fork by citing a library root-barrel export that does not exist** (no `.` key, no root d.ts, ERR_PACKAGE_PATH_NOT_EXPORTED — re-verified via my package.json read + reader-LC's probe re-run). **DISPOSITION: rider on X-W9.d** — whichever way W9.d rules the root-`.` question, this comment is corrected in the same act.

### INFO

- **PS-31 · D-12 — the strip's honest-absence arm is unreachable in both hosts** (generatePalette returns ≥1; the calibrated palette is non-empty); the module's absence law is live only on the ramp, where it is genuinely exercised and oracled. Recorded absence-of-exercise, not a defect to cure. **Chip-family residue note.**
- **PS-32 · D-13 — fixed 4px `--radius-sm` on an em-scaled plate: corner character 24–29% of height, end segments losing ~7% painted area to arcs.** Correctly INFO at the axis. **Chip-family residue.**
- **PS-33 · missed-DU#6 — the docblock contradicts ITSELF**: :6-8 promises "the strip a row shows is what selecting it yields … paints them as equal hard segments" (them = all stops) eight lines above the cap paragraph; the cheapest repair in the corpus, in the file whose subject is truthfulness. Verified by my whole-file read. **Chip-family residue (first line of any cure commit).**
- **PS-34 · missed-LC#5 — four ceilings for one quantity on one screen (docs 1–50, slider 1–12, cap 7, sibling ∞), and `STRIP_SEGMENT_CAP` is module-private, falsifying its own "a NAMED taste knob so no writer mints it" rationale — no writer can consume it.** Adjacent to (not identical with) GenerateControls R-E's arbiter note (cap-asymmetry vs the exported `RAMP_SAMPLE_COUNT`). **Chip-family residue.**
- **PS-35 · D-10 (RESCOPED by this seat — neither reader carried it)** — the `/generate` name lane (Fraunces `.font-display`) and description lane (`text-micro` 11px) sit off VC §4's closed type matrix, and the chip's proportion is measured against those unratified rungs. But the name-lane voice is a DELIBERATE, DOCUMENTED register (GenerateControls.vue:235-239's P9-R5/T-40 provenance, the ColorSpaceSelector precedent) in a PINNED file. A deliberate authored register conflicting with a closed canon matrix is a CANON RECONCILIATION, not a component defect. **DISPOSITION: recorded for X-W10 (canonical design law) — the matrix either admits the dropdown display-voice register or the W25 executor retires it; the chip-side dependence dies with PS-18's unit fix either way.**
- **PS-36 · missed-DU#7 — corpus integrity: the governing C pass-2 silently orphaned pass-1's C-9 and C-10** (confirmed by my read of pass-2's tables: absent from :331-337 and both family rows). **DISCHARGED BY THIS RECORD** — C-10 carried at PS-22; C-9 carried in the residue (below); no re-audit needed.

## Killed-claims register

Every kill re-derived by this seat; refutations of record. Original claims stay immutable in their axis files per E-1/E-3.

1. **K-1 · D-5/E-5's em-anchor attribution ("inherited from the popover's scroll container … no type role in the lane") — KILLED (reader-DU ratified by my token-chain instrument).** The axis's own artifact refutes it: ring-occlusion.json fontChain shows the scroll container at **18.608px** and the chip at **16.4px** — the size is SET at `DIV.interactive-item`, glass-ui's own menu row, via `.text-dropdown` → `--dropdown-text` → `--control-text` = `--type-small × --ui-scale` (my reads of menuRowClass + components.css + tokens/sizing.css), which is precisely VC:75's ratified rung for "dropdown options". The rem-vs-em mechanism survives (PS-18); the "no relation to earn" rhetoric and the 17.1%-spread arithmetic's cross-artifact inconsistency (lane-type 2.6505 vs ring-occlusion 2.5655 for the same measurement) die.
2. **K-2 · C-12's causal story ("the chip's 1em resolves against the option's NAME lane, `.font-display`") — KILLED (reader-DU ratified).** `.font-display` is a font-FAMILY utility (Fraunces); GenerateControls.vue:240's name span carries no size class; name span and chip BOTH inherit the row's 16.4px from the same `.text-dropdown` ancestor. The measurement was right; the attribution — C-12's sole contribution over C-6 — was wrong.
3. **K-3 · the forced-colors mask-erasure limb (D-4's "a 12-stop and a 5-stop palette are the same blank rectangle", D-8's "under forced colors the mask computes to none") — KILLED (both readers convergent; ratified by my probe read).** `forced.mjs` never drives the count slider (my grep: 0 hits for count/slider); it measured the DEFAULT count 5 — below the cap — where `mask: "none"` is the normal untruncated value. Independently, `mask-image` is not in the WHCM substitution set. The weaker conclusion (the cue is useless in WHCM because every segment becomes one system color) survives unmeasured — it rides PS-3.
4. **K-4 · D-3's unscoped mechanism ("generatePalette extends a stable prefix, so every count ≥ 7 paints the same seven colours" — stated as a general property carrying the "six of twelve reachable states" headline) — KILLED as a generalization (reader-DU ratified by my rng-stream instrument).** Only `golden` draws nothing in `generateHues`; the four jittered harmonies consume one draw per index before the l/c loop, shifting every (l,c) pair with count; `analogous` moves by hue geometry. The golden-scoped instance survives at full strength as PS-9 (golden IS the default).
5. **K-5 · reader-LC's counter-scoping ("true for 5 of the 6 harmonies … complementary/triadic/split-complementary/random are prefix-stable because generatePalette draws seedHue once then exactly two rng values per index in fixed order") — KILLED BY THIS SEAT'S OWN INSTRUMENT.** The premise omits the jitter draws inside `generateHues` (:127-146). Measured: first-7 (l,c) stream positions differ across counts for all four jittered harmonies. A reader-confirmation cell inverted by the arbiter — recorded as the session's citation-independence event: reader-LC's cell repeated the structure of pass-1's C-3 wording rather than re-deriving the draw order.
6. **K-6 · D-2's defect-count limbs (the "22 of 24 chips fail" census-as-defect, gate 1's universal per-stop 3:1 floor, the dark-scheme "BLOCKER — reading direction inverts" state row) — KILLED (reader-DU ratified; D-2's core RESCOPED into PS-2).** Per-stop contrast against the menu ground is a property of the previewed palette — the information, not a defect; gate 1 as written forbids truthful pastel/analogous previews, contradicting VC:194 and the essential-graphics carve-out; an opaque specimen inverts on any inverted ground by necessity. The census stands as DATA (residue).
7. **K-7 · C-6's secondary limb ("`display: inline-flex` is INERT … dead code by the no-legacy edict") — KILLED (reader-DU ratified).** The blockification is imposed by the HOSTS (flex-item children of the wrapper span and of the producer's `flex flex-col` description wrapper — both verified in my host reads); CSS Display §2.7 blockifies a flex item's `inline-flex`. An authored value a host context overrides is not a shim; the edict targets dual paths.
8. **K-8 · C-2's negative proof, both passes ("the O-14 ramp leg survived exactly this hazard … reuse the ramp leg's `parseOklchTriples`") — KILLED (reader-DU ratified; re-proven by this seat's own chain-trace + regex run).** The ramp leg has the identical disease (PS-7): its stamps ride the same `%`/`deg` dialect and its own parser extracts zero triples from them. The corpus's proposed cure would propagate a parser that cannot read the repository's actual stamps, in either species.
9. **K-9 · L2-1(b)'s consumer-facing framing ("makes every downstream consumer install a duplicate pinned copy of value.js … ≈6.0 MB … 37× tax") — KILLED (reader-LC sustained).** npm dedupes keyframes' exact 4.0.0 pin against a hoisted 4.0.0; the nested copy exists only because THIS workspace root cannot dedupe against a registry tarball. True downstream cost ≈5.8 MB of phantom deps, no self-duplicate until a version ≠4.0.0 publishes — the axis's own §2 said so and its headline contradicted it. The manifest defect stands (PS-13).
10. **K-10 · L2-3(b)'s "no library change, deletes both casts today" demo-side cure — KILLED (reader-LC sustained).** Their 8-module compile under the real tsconfig: the proposed alias produces 15 errors (or 6 with the distributive form) because `Color<S>` for generic S is not assignable to the `AnyColor` union — the demo's `Extract` mint is the load-bearing adapter. Only the library-side retarget reaches zero (PS-14's route).
11. **K-11 · L2-5's title claim ("EVERY demo layering gate matches zero files") — KILLED as stated (reader-LC sustained; consistent with the ColorSpaceSelector L-5(a) record).** The :233-234 `demo/color-picker/**` globs are live with a defined rule. The accurate — still damning — statement: the two objects carrying the LAYERING law are dead, and every `@components`-alias ban is vacuous.
12. **K-12 · C-1's prescribed cure (chip stamps `visible`; host stamps scalar `data-preview-total`) — KILLED (reader-LC sustained).** o20's law is element-wise against the plate's N swatches; a scalar cannot serve it and the full-stop list would vanish from the DOM. The cure-vs-defect separation law (ColorSpaceSelector addendum 2) applies verbatim.
13. **K-13 · L2-2's evidence attribution ("probe-L-resolve.mjs — ts.resolveModuleName, paths removed") — KILLED (reader-LC sustained; my grep: 0 hits for `resolveModuleName|paths` in the banked probe).** The finding survives on reader-LC's own reproduction; the corpus's reproduction command cannot produce its pasted output — recorded as corpus-integrity evidence.
14. **K-14 · C-11's mechanism story ("a named global policy with an opt-in hook that a later component forgot to opt into") — KILLED (reader-LC's zero-adopter census ≡ GenerateControls R-C, already ruled: `[data-color-surface]` is a DEAD selector with zero producers, deletable, and an adjudicated wave deletes it).** The hook was never established; the cure's cost story was false and the one-attribute prescription is overruled by prior ruling (see PS-3's rider). The membership defect is untouched.

## Superlatives — what must survive every cure (provenance: reader union, spot-verified by this seat)

1. **THE SAMPLING LAW** (`sample.ts:5-24`, read whole by this seat): previews sample through the library's own `mixColors`, never CSS gradient interpolation — "the preview must show what THE APP computes" — honored in code at :75. The single strongest idea in the neighbourhood; every cure above preserves it. *(reader-LC)*
2. **Zero color math in the view.** The chip's only imports are `computed` and `stampStops`; stops arrive pre-computed and paint verbatim. This separation is why every adjudicated cure lands without touching color logic. *(both readers; my whole-file read)*
3. **Motion: structurally absent and honestly documented.** No transition/animation/rAF/`<Transition>` anywhere (my grep); motion-quarantine.md carries ZERO rows naming this component (my grep — no M-xx row exists to carry, satisfying the FALSE-row law by vacuity); PreviewRamp:14's "Static paint — no motion, PRM-neutral" is exactly true. *(reader-DU; verified)*
4. **Decoration semantics done right**: `aria-hidden="true"` with the semantic payload left to name + description; measured accNames are the bare labels. The component correctly declines a role. (PS-22 asks for a text EQUIVALENT of the elision, not a role.) *(reader-DU)*
5. **Zero idle cost by construction**, documented at the seam (both hosts narrate the unmount law); 0 `[data-stops]` nodes with menus closed, measured twice. *(both readers)*
6. **Correct overflow priority**: `flex: none` on the plate vs `min-w-0` on the wrapper — the prose truncates, the specimen never does; measured overflowX 0 at 320px. *(reader-DU)*
7. **Exemplary decision documentation**: the cap is a named constant with its rejected alternative recorded (:10-16) — three adversarial seats could adjudicate it precisely because the file argued in the open. (Its two prose defects, PS-33/PS-34, are repairs to a virtue.) *(reader-DU)*
8. **`data-stops` as an in-DOM machine-readable truth referent** is the right testability affordance — it is what made o14/o20 writable and this entire audit measurable; only its serialization is broken, a fixable leaf. *(both readers)*
9. **Adversarial craft worth naming**: the D axis's E-1 controlled A/B — two injected boxes, identical resolved shadow, payload location the only variable — is the correct way to prove a rendering claim, and it is why D-1's mechanism survived hostile re-reading even as the axis's severities did not. *(reader-DU; adopted as the program's template for paint-order claims)*
10. **`valueJsSelfAlias` generated from `package.json#exports` with anchored regexes and its prevented failure documented inline** (vite.config.ts:23-50) — redundant per L2-2's mechanism, but the reasoning quality is the house standard. *(reader-LC)*
11. **The joint-dedupe oracle** (test/preview-chips.test.ts:65-76): asserts the exact algebraic chain length against an independent recompute — the one gate in this neighbourhood that would actually catch a sampler regression. *(reader-LC)*

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

Each row names its decider. These are INPUTS to the next visual-audit session, not findings.

1. **D-2's rendered-contrast census as measurement** (24 chips; worst pair 1.013:1; the atmosphere light-tail/dark-head inversion) — corroborated by two seats, undisputed as DATA; its defect-attribution is settled (K-6). Decider: re-run `contrast.mjs` after PS-2's well lands — the BOUNDARY, not the stops, owes the floor.
2. **D-1's pixel A/B at DSF 4** (C1 = 1.00 CSS px vs C2 = 0; live component matching C2 in both schemes) — mechanism certain; pixels chromium-cell.
3. **PS-3's rendered WHCM result** (all-white segments, `distinct: 1`, the white-rectangle frame) — mechanism certain; pixels unmeasured here, and the corpus notes the shipped forced-colors matrix arm was WebKit-captured and inconclusive program-wide.
4. **C-13/P-3's timing** (53–71ms long tasks at count 12, 0 at count 1; 0 across 11 closed-menu slider steps) — structural mechanism confirmed; milliseconds live. Adjacent bench of record: GenerateControls R-E.
5. **D-6's row-rhythm numbers** (50.14–51.54 vs 43.33px; 1.49% ink) and its **pink-family discrimination limb** (seed-dependent; contradicted in principle by far-apart harmony hues — needs frames across ≥3 seeds).
6. **All measured geometry** (41.9×16.3 rects; ratios 2.5548…3.0166; the 390px 14px arm; 4px radius) — mechanisms proven from source; resolved pixels live.
7. **D-8's RTL frames** (`D7-rtl-mask-inverted.png`) and the reka `dir` stamping behaviour — latent by two layers; decider: the day the producer honors document `dir`.
8. **D-11** (selected row's chip pixel-identical) and **D-14** (disabled-row alpha compositing) — one needs a live open menu, the other has no rendering host today.
9. **C-9 (pass-1 orphan, carried here)** — the unpaintable-stop hypothesis (`oklch(0.63 0 NaN)` silently painting transparent while `data-stops` asserts a color): no live producer found by any seat; `resolveCalibratedAtmosphere` is the only guard. Decider: a fuzz probe over the aurora atoms domain, or the honesty arm lands with PS-4/PS-17's seam validation and the question closes structurally.
10. **reader-DU's sub-pixel seam hypothesis** (at the cap, 6/7 boundaries fall on fractional device pixels — the promised "equal HARD segments" may render as antialiased blends): decidable by `edge-profile.py` pointed at a SEGMENT SEAM at DSF ≥ 4; the primitive exists.
11. **AuroraPane's per-open resolve cost** (six calibrated atmosphere resolves per menu open, template-position) — static reading only; rides X.W6.i's harness.
12. **L2-4's byte-divergence re-run** at the current tree (node_modules may have re-resolved since the corpus) — mooted by the 4.1.0 cut if W9.f lands first.
13. **PS-5/PS-7's born-RED demonstrations as live runs** (the o14 ramp-leg RED and the above-cap referent split) — structurally certain here; a fresh `npx playwright test o14` transcript should be banked at the cure wave's open as its BEFORE.
14. **The E-2 frames' bleed-through claim** ("page content visibly bleeding through the menu behind three rows of chips") — rides PS-2's well decision; needs the specimen-well AFTER frame.
15. **PS-24's honest minimum width** — whether `min-w-menu` (11rem) actually seats chip + longest description, or a new token is owed; measured at the W25 executor's sitting.
16. **The o20 flake class** (R-A: test 1 transient) — already registered at GenerateControls; named here only because any chip-oracle re-author inherits the same dock-fixture instability.

## Closing verdict

**DEFECTIVE — adjudicated.** No solo BLOCKER survives on this docket: the two the axes filed (D-1, C-1/C-2's pair) are MAJOR under the program's severity discipline, and the third (forced-colors) is BLOCKER-class BY IDENTITY with its grade held at PC-7. The blocker-weight condition is the **verification vacuum**: every oracle over both chip species is structurally unpassable or nonexistent, none runs in CI, and the prose asserts otherwise in three places — for a component whose entire reason to exist is a truth law. 35 rows confirmed (1 blocker-class identity · 17 MAJOR · 11 MINOR · 6 INFO); 14 kills of record, one of them this seat's own inversion of a reader-confirmation cell (K-5, decided by instrument, not preference); 11 missed findings adopted, three resolved into standing identities (AP-9, R-C, PCS-10) rather than re-booked. Primary routes: **X-W7.c** (roster/well/boundary class, with the R-C mechanism decision and a named bounds gap) · **X.W6.i/j** (aurora serializer, oracle, row re-composition) · **X-W9.d/.f** (library type surface + the 4.1.0 cut, both with named gaps) · **X-W8** and **X-W1** (standing identities) · the **chip-family residue (≡ PCS-10)** carries the component's own consolidation to the formation boundary as ONE named NO-WAVE-OWNER identity. The component's best ideas — the sampling law, the pre-computed-stops seam, the in-DOM truth referent, zero rest cost — survive every one of its defects, and the cures above are ordered so that they must.

— end of apotheosis. The challenger records, both reader returns, and all dated evidence remain immutable beside this file per E-1/E-3. Nothing here stamps a wave: dispositions are ADJUDICATED, not VERIFIED.
