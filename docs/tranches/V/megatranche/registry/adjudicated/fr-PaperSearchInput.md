claude-fable-5

# fr-PaperSearchInput — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-25 · **Subject tree**: `/Users/mkbabb/Programming/fourier-analysis` @ `cd26c65`, working tree carries the uncommitted tri-package uplift (audited pin = WORKING TREE, resolved glass-ui **4.0.0** in `web/node_modules` — re-confirmed by this seat's own node read) · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the component's three challenge files — **reader-DU** (slug `PaperSearchInput`: 17 confirmation rows · 6 kills · 4 misses · 6 superlatives + a 6-item unproven set) and **reader-LC** (slug `fr-PaperSearchInput`: 27 confirmation rows · 9 kills · 3 misses · 6 superlatives). Challenge files (`audit/fourier-components/PaperSearchInput/challenge-D-design.md` 57K, `challenge-L-library.md` 40K, `challenge-C-consumption.md` 31K) spot-read by this seat at every disputed row (D-M3's four census cells, the L §10 cure limb + the §4 probe output at L:115-127, C-3's falsifier at C:140-152); the readers' representations of the corpus are accurate everywhere I checked.
**Method**: neither reader was adopted before being attacked. Every verdict-level disagreement (D-B1/D-B2/C-2/C-3 grades, D-M8's grade, the K-2/K-3 census kills vs reader-DU's "reproduce EXACTLY" confirmation, K-4 vs reader-DU's C-4 cure cell, the AxeBuilder cell) was re-derived by this seat with its own instruments, and both load-bearing NEW missed rows (reader-LC's shared-hook orphaning, reader-LC's `@lucide/vue` lock gap) were re-verified end-to-end before admission. **Two of the readers' kills were themselves found defective** (one overturned, one sustained-in-direction with both of its cells corrected), and **one cell error in each reader** was caught that no other instrument had — the r2 idiom keeps earning its cost.
**This seat's own instruments (all read-only, zero browser probes — probe parsimony)**: full read of `PaperSearchInput.vue` (whole, 69 lines); `PaperSearch.vue:23-29/:41-100/:200-215`; `usePaperSearch.ts:20-60`; `PaperSearchModal.vue:52-78/:102-107`; `PaperSearchDropdown.vue:35-48/:64-69`; `PaperSidebar.vue:51-62/:132-165/:188-192`; `MobileFloatingToc.vue:104-112/:259-265`; **a quote-aware, comment-stripped python census of every `<Button>` tag in `web/src`** (the D-M3 decider — both readers' scanners fail on this tree: naive tag regexes terminate at the literal `>` inside `v-if="navStack.length > 0"` (PaperView.vue:398-405), and tag-scoped scans that skip comment-stripping absorb 9 doc-comment `<Button variant=…>` mentions across 7 visualization/gallery files); a `data-size` enumeration over glass-ui **7.0.0**'s entire `dist` (installed at value.js, version re-read) split by file type; `dist/components/button/Button.vue.d.ts:4-5` (7.0.0 unions); installed 4.0.0 producer reads — peerDependencies (`@lucide/vue ^1.16.0` first member), **top-level** `dist/styles/components.css` (all four `.h-\(--control-h-*\)` + `w-` twins pre-compiled — NOTE: `dist/styles/utilities/components.css` is a DIFFERENT file that carries none of them; this seat initially grepped the wrong one and records the two-files-one-name trap), `utilities/components.css:205-238` (`.input-bar`/`.input-bar-field` recipes read whole), `tokens/offsets-sizing.css:148-152/:175-178`, `tokens/light-dark.css:17-21`; a python scan of `web/package-lock.json` (glass-ui 4.0.0 entry records the `@lucide/vue` peer; **no** `node_modules/@lucide/vue` package entry exists; `lucide-vue-next`, `class-variance-authority`, `clsx`, `reka-ui`, `tailwind-merge` all `"dev": true`) + on-disk `@lucide/vue` 1.20.0 + `web/package.json` (absent from both dep sections); `e2e/visual-baseline.spec.ts:25-68` read (PAGES includes `{slug:"paper", path:"/paper"}`, 3 viewports, documentElement occlusion gate ≤2px) and `paper-performance.spec.ts:38`; `ls e2e/*.spec.ts` → **8**; AxeBuilder import census → **visualization-ux.spec.ts:2 AND visualization-crud.spec.ts:2**; `grep -rn "transition: all" src/components/paper/` → 4 hits, all A.W3.d comments, `PaperSearch.vue` grep-c 0; the shared-hook census (`paper-search-action-btn` Input×2 + Modal×2; `-result`/`-badge`/`-number`/`-label` Dropdown + Modal each; `-icon`/`-input`/`-input-wrap` Input only); installed `@vue/runtime-dom` `runtime-dom.cjs.js:1495/:1499/:1515/:1535` (the `composing` guard, read verbatim); `lucide-vue-next@1.0.0` `defaultAttributes.js` (24×24) + `Icon.js:41` (`aria-hidden` when no slot); `src/style.css:1-6/:40-50/:136-143`.
**Environment disclosure**: every ruling below was decidable from source + installed producers + node/python. The shipped `web/dist` is quoted NOWHERE as pin evidence (STALE-DIST law, banked twice — fr-PaperSearch correction 5, fr-PaperSearchModal K-6 — and both readers applied it correctly here).

## Scoreboard

**34 adjudicated roster rows — 1 BLOCKER · 17 MAJOR · 14 MINOR · 2 INFO** — of which **13 FOLD BY REFERENCE** into banked fr-/PSM-/MFT- identities (the identity guard did more work on this component than on any prior seat's: the family's mechanism rows were all banked before this file's turn came). **13 killed-claims register entries** — one reader kill **OVERTURNED** (K8: a killer's cell killed, the fr-PaperSearch K8 precedent's second occurrence), one **sustained in direction with both of its own replacement cells corrected** (K7). **7 missed rows ADMITTED** (4 from reader-DU, 3 from reader-LC — each reader again missed the other's entire missed set; fleet evidence, recurring). **9 superlatives sustained.** **4 adjudicator corrections against the readers · 2 register corrections against banked rows** (recorded as notes, never rewrites — dated evidence stands).

Blocker-weight survivor: **C-1/L-§1** — the fold-arm of the banked family BLOCKER (fr-PaperSearch D-1/L-1/C-B1 · PSM-1): this 69-line file emits four class hooks and can honour one; the wrap is **the only element in the whole search family that the scoped sheet reaches**, which makes this component simultaneously the family's sole live-styled member and the host of its two worst live geometry defects (D-B2, D-M2). The severest NEW facts are both misses the r2 pass bought: the prescribed D-1 cure **orphans five cross-SFC shared hooks** (reader-LC — a mandatory same-commit rider now, PSM-13's class), and the audited tree is **not reproducible from its own lockfile** (reader-LC — `@lucide/vue` required-peer filed nowhere).

## Ruled disagreements and corrections (the incredulity seat's actual work)

| # | row | reader-DU | reader-LC | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **D-M3 census cells** (F.W1 budget) | CONFIRMED with "all four grep counts reproduce EXACTLY: 35 files / 113 `variant=` / 38 `size="icon"` / 49 ghost·29 outline" | K-2 kills the method: "scoped to actual `<Button>` tags: **96** variant / **37** icon; ghost 48, outline 25" | **BOTH cell sets REJECTED — this seat's figure is the budget** | The greps DO reproduce (I ran them: 35/113/38) — but they measure files, not Button tags: the 113 includes the 14 `<Slider variant="standard">` sites + 1 MetricBadge + 9 comment mentions + other non-Button variants. Reader-LC's tag scan has two defects of its own: (a) it counts **comment-resident tags** — 9 `variant=` + 2 `size="icon"` sit inside doc comments (AnimationControls.vue:213, CanvasOverlayButton.vue:5, GalleryCard.vue:253/:276, GalleryCardModal.vue:218/:233/:246, GallerySearchBar.vue:205, BasisSelector.vue:250 — every one listed by my stripped-diff); (b) a naive `<Button\b[^>]*>` regex **terminates at the `>` inside `v-if="navStack.length > 0"`** (PaperView.vue:398-405), dropping that tag's `variant="glass" size="icon"` — which is why LC's 37 misses a real site while its 96 keeps 9 phantom ones (and its own value breakdown sums to 94, contradicting its headline). **Adjudicated F.W1 template budget: 87 real `<Button variant=…>` sites · 36 real `size="icon"` sites · 35 files** (ghost 46 · outline 21 · glass 10 · destructive 4 · default 4 · link 1 · secondary 1), **plus** 9+2 comment-resident mentions (typecheck-invisible, grep-poisoning — doc updates owed) and 17 non-Button `variant=` sites in Button-importing files. D-M3's headline (7.0.0 deletes `variant` + `icon`; CENSUS-2026-08-03.md:102-106 omits the prop-contract break) survives on ANY of the four figures. |
| 2 | **K-3 / K8** (the challenge's `[data-size=…]` enumeration) | Confirms the challenge cell: "exactly `sm`, `md`, `lg`; no `icon`" | K-3 KILLS the cell: "a scan over the whole shipped `dist/**/*.css` returns xs, sm, md, lg, xl, control, dock" | **K-3 OVERTURNED — the challenge's cell stands; a killer's cell killed** | My split scan over the installed 7.0.0: **CSS attribute selectors** = `[data-size=sm]`×5 · `[data-size=md]`×5 · `[data-size=lg]`×7 — nothing else, exactly as the challenge stated for "7.0.0's stylesheet". The quoted forms `data-size="xs"/"xl"/"control"/"dock"` live **only in JS chunks and d.ts** (stamped attributes of other components) — reader-LC's scan was not the `*.css` scan its sentence claims. Adopted residue from the kill: `ButtonSize = Extract<Size,"xs"|"sm"|"md"|"lg">` (Button.vue.d.ts:5, my read) — the TYPE union carries `xs` though no `[data-size=xs]` rule ships; and the material conclusion (no `icon` member; `iconOnly` carries the meaning) was never in dispute. Second occurrence of the fr-PaperSearch-K8 class: kills get re-derived too. |
| 3 | **L §10 cure limb** ("fixing §1 gives `variant` its job back") | CONFIRMS the limb inside C-4: "structurally correct: a parent scope id cannot reach a grandchild, so the `--floating` overrides must become a child-side class" | K-4 KILLS the limb: plain relocation works; the ancestor compound carries no attribute | **K-4 SUSTAINED — reader-LC wins; reader-DU's cell corrected** | Vue's scoped compiler attributes only the SUBJECT compound — the challenge's own probe output proves it (challenge-L-library.md:121-122, re-read by this seat: `.paper-search--floating .paper-search-input-wrap[data-v-PARENT]`). Relocated into `PaperSearchInput.vue`'s `<style scoped>`, the rule compiles to `.paper-search--floating .paper-search-input[data-v-INPUT]`: the subject is the child's own element (stamped), and the un-attributed ancestor compound matches the parent's root `<div class="paper-search paper-search--floating">` (PaperSearch.vue:44, my read) at runtime — CSS matches the DOM, not the component tree. No prop plumbing needed; and the corollary stands: **the scope fix does NOT retire m-1** — the dead `variant` prop must be deleted or employed on its own merits. |
| 4 | **D-B1 grade** | BLOCKER (with its own correcting cell: default state ≈ ~190px into ~177px — a ~13px overrun on an unproven intrinsic-width figure, not the 292px the finding's words imply) | MAJOR, folded to banked MISS-A2 | **MAJOR — reader-LC wins; identity guard governs** | The defect IS banked: fr-PaperSearch MISS-A2 (~295px worst-case demand into ~184px, "worst-case exactly while typing with results showing") — one identity, already MAJOR, already routed. Reader-DU's honest correction is the strongest argument AGAINST its own grade: a default-state overrun inside the error bar of an unmeasured intrinsic-input figure cannot carry BLOCKER. ADMITTED as cells on the banked row: the default-state ~13px arithmetic (only icon+input mount — both Buttons are v-if-gated, `:48`/`:59`, my read), and `scrollbar-gutter: stable` (PaperSidebar.vue:158, my read) further shrinking the budget. |
| 5 | **D-B2 grade** | BLOCKER | MAJOR | **MAJOR** | Oversized-but-functional controls are the family's banked geometry class (fr-PaperSearch D-4 MAJOR, fr-MobileFloatingToc M-8 MAJOR — the same `--control-h-md` × coarse × 18px-root chain, FR-COB-24 identity); this registry reserves BLOCKER for surfaces that do not paint, do not navigate, or cannot build. Delivery verified by this seat at the **top-level** `dist/styles/components.css` (all four `.h-\(--control-h-*\)` + `w-` twins pre-compiled; index.css:201 imports it; style.css:3 pulls the chain) — the 40px is real and so is the `icon-sm` cure (0 uses, 36 `size="icon"` sites). K2's touch-cell correction adopted (ruling row below). |
| 6 | **C-2 / C-3 grades** | BLOCKER / BLOCKER | MAJOR / MAJOR | **MAJOR / MAJOR** | C-2 folds to banked identities (fr-PaperSearch MISS-B3 "MAJOR disposition row" + PSM-9 MAJOR): a shipped-and-unimported producer twin is a retirement decision, not a shipping breakage — and K16 already killed the deadlock objection. C-3 is NEW to the family and REAL (my lock scan: cva/clsx/reka-ui/tailwind-merge + lucide-vue-next all `"dev": true`, so `npm ci --omit=dev` cannot yield a working tree TODAY), but its blast is a foot-gun in formation documents (lane-frontend.md:69 / DESIGN.md:32 book live runtime peers as dead devDeps) — the harm path runs through a future sweep, and the cure is a manifest move + a census correction. MAJOR, routed where the census consumers live (F.W1). |
| 7 | **D-M8 grade** (floating arm deletes the focus border) | MINOR ("what is lost is the authored container affordance, not all focus indication") | MAJOR | **MINOR — reader-DU wins** | Both rules are alive (same subject, the wrap is the child root; `--floating`'s `border: none` later in source at PaperSearch.vue:204-209, my read) — so the floating arm genuinely loses the component's only live authored focus affordance. But the input's UA ring survives (banked S-1: "square, offset, outside the rounded wrap" — an accident, and still an indicator), so "no focus indication on one of its two arms" over-states. MINOR, banked into the D-16 repair-hazard group: any cure reviving authored rules must hand the floating arm a replacement affordance in the same commit. |
| 8 | **M-4 / C-5 grade** (template writes the composable's refs past its own API) | (books the pattern inside confirmations without a standalone grade) | MAJOR | **MINOR — identity guard** | The pattern is banked: fr-PaperSearch C-M2 (write-through-props, INFO group) + C-M3 (ruling 8: MINOR, dual-register — the inferred `PaperSearchState` is what makes the children's writes typecheck). This file's cells (`:43` query write, `:45` isOpen write, `:63` close; `open()` exists and is bypassed, usePaperSearch.ts:55-57 my read) are instances of that identity. FOLD at the banked grade; reader-LC's MAJOR noted and rejected — re-grading a banked identity from a leaf is the anti-rename rule's failure mode. |
| 9 | **Coverage cells** (M-5/C-12/L-M-5/D-i3) | K-c kills the falsifiers; cell: "AxeBuilder is imported only in visualization-ux.spec.ts" | K-6 kills the same falsifiers; cell: AxeBuilder at visualization-ux:2 AND visualization-crud:2 | **Kills SUSTAINED (convergent); reader-DU's axe cell CORRECTED** | My census: `AxeBuilder` imported in **two** specs (visualization-ux.spec.ts:2, visualization-crud.spec.ts:2) — reader-LC's cell right, reader-DU's wrong. Both are `/visualize`-gated, so the substance every party agrees on holds: axe never runs `/paper`. Spec count: **8** files (my ls) — reader-DU right, the L challenge's "nine" wrong. The corrected statement of record: `/paper` IS navigated (paper-performance.spec.ts:38; visual-baseline.spec.ts paper row × 3 viewports — both read by this seat), so the true void is **zero assertions on any `paper-search*` selector and zero axe on the route** — "zero mounts / no paper route" must never be quoted from the corpus again. |
| 10 | **ui-glyph emission** (this component's two action glyphs: 12px or 16/24px?) | UNPROVEN — names the registry conflict, sides with K1 on four instruments | D-M2 re-derived off the installed producer (`ui-glyph` → 0 in shipped styles) | **REGISTRY CONFLICT CONFIRMED — held UNPROVEN, decision vehicle = the banked F-1 pre-gate rider** | fr-PaperSearch K1 rules the `[&_svg:not([class*=size-])]:size-(--ui-glyph)` register DEAD (reader-α's fresh build: 0 emissions) while fr-PaperSearchModal PSM-24/R-3 rules it LIVE-on-first-4.0.0-build — both banked, same week, opposite ways. This seat's own static reads side with K1: no compiled rule anywhere in 7.0.0-or-4.0.0 shipped styles (`class*=size-` occurs once, in the offsets-sizing.css:175 comment; `--ui-glyph` declared :177-178, consumed by zero rules), concordant with fr-MobileFloatingToc i-4 and fr-PaperSidebar's postcss parse. Dated rulings are not rewritten: the conflict is RECORDED, and fr-MobileFloatingToc already minted the decider — **F-1, the F.W1 pre-gate emission build**. PSM-24's routing must be re-derived at F-1; until then every glyph-size cell in this corpus is conditional. |

**Adjudicator corrections against the readers (4).** (1) Reader-DU confirmed D-M3's census cells as "reproduce EXACTLY" — true of the commands, false of the quantity; confirming a grep is not confirming a measurement (ruling 1). (2) Reader-DU's C-4 cure cell repeated the challenge's scoped-CSS misconception that the probe output in the same file refutes (ruling 3). (3) Reader-DU's axe-import cell wrong by one file (ruling 9). (4) Reader-LC's K-2/K-3 kills both carried defective replacement cells — one overturned outright, one corrected by this seat's stripped census (rulings 1-2). **Register corrections against banked rows (2, recorded as notes for the forming specs — the dated files stand):** (a) fr-PaperSearchModal SS-13 #10 calls the IME mechanism "not derivable from source" — it is: the `vModelText` composing guard is verbatim in the installed `@vue/runtime-dom` (`:1495/:1515/:1535`, this seat's read); only the live IME behaviour needs a session. (b) fr-PaperSearch ruling 2 / D-16 books the **1.71:1 state-delta** against an SC 1.4.11 floor; per reader-DU's D-i2 reading (adopted): 1.4.11 compares a component's visual information against ADJACENT colours (that is MISS-A6's live 1.930:1 failure), while a 3:1 floor on the CHANGE between states is the AAA 2.4.13 register — the banked verdicts stand (weak affordance, repair hazard), but SS-3/SS-4 spec authoring must cite the two figures under the right criteria.

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Dispositions are **ADJUDICATED, not VERIFIED** (M-25); routes name the forming X·F wave surface that will own the row. Line anchors are working-tree @ `cd26c65`; re-resolve before any cure.

### Blocker (1)

- **C-1 / L-§1 / L-§2 — BLOCKER, FOLD BY REFERENCE → banked fr-PaperSearch D-1/L-1/C-B1 + fr-PaperSearchModal PSM-1 (one identity, one repair).** The component's arm: PaperSearchInput.vue emits four class hooks (`paper-search-icon` :36, `-input` :40, `-action-btn` :51/:62, plus the wrap :35) and has NO `<style>` block (whole-file read, ends at `</template>`:69); every rule for the first three lives in the parent's scoped block (PaperSearch.vue:41 `<style scoped>`, :62-100/:211-213) and compiles onto subjects that never carry `data-v-a7d16b8c`. Only the wrap — the child ROOT — is stamped, making this file **the sole live-styled element in the family**. L-§2's dropdown/modal extension folds to the same banked identity (Dropdown = two-root fragment :35/:65; Modal roots on Teleport :41). **Route F.W3/W4**, now carrying TWO mandatory same-commit riders: the banked repair-hazard group (D-14/D-16/D-26/MISS-A5) **and the newly admitted MISS-LC1 shared-hook rider below** — the prescribed relocation as written in BOTH challenge repair plans would mint a new dead-rule set.

### Majors (17)

- **C-2 — MAJOR, FOLD → banked MISS-B3 + PSM-9.** `@mkbabb/glass-ui/search` ships at the installed 4.0.0 (`./search` in the exports map — re-verified) and is imported 0 times; K16 already killed the deadlock objection; the honest costs (3-field scorer, no `number`×18/`rawTex`×6) ride the banked row. **F.W3/W4** decision row.
- **C-3 — MAJOR (NEW to the family; ruling 6).** `cva`/`clsx`/`reka-ui` are LIVE glass-ui runtime peers on this file's own import chain (button chunk → cn → clsx, both readers verified; peer list re-read by this seat) while lane-frontend.md:69 / DESIGN.md:32 book them for deletion as "dead devDeps"; `tailwind-merge` alone is genuinely dead (doc-comment-only — challenge C's precision, sustained). Strengthened and lock-verified: all four plus `lucide-vue-next` are `"dev": true` in `package-lock.json`, so `npm ci --omit=dev` fails TODAY. K12 kills the challenge's own falsifier (npm auto-installs peers; `npm run build` unrunnable per banked MISS-A7). **Route F.W1** (manifest section moves + the census/lane-doc contradiction correction), cross-noted F.W0.
- **D-B1 — MAJOR, FOLD → banked MISS-A2 (ruling 4).** Dead `min-width:0` + unsized `<input>` (no `size` attr, :37-46) in the 220px grid track (PaperView.vue:562), inside the 2px-bordered 0.625rem-padded scroll panel (PaperSidebar.vue:155-163). Admitted cells: the corrected default-state arithmetic (~13px overrun, both Buttons v-if-gated) and `scrollbar-gutter: stable` (:158). Preflight-width falsifier closed at the pin by both readers, concordant. **F.W3/W4**; the cure's gate must be CONTAINER-scoped (see MISS-DU3).
- **D-B2 — MAJOR (ruling 5).** Both action Buttons render `size="icon"` = `h/w-(--control-h-md)` → **40px** fine / **67.5px** coarse-sub-768 / **60px** coarse-≥768 (token chain re-read: offsets-sizing.css:148-152, light-dark.css:17-21, root 1.125rem→1rem at style.css:40-50) against ~17px authored (dead) and the hand-corrected **20px** twin one panel over (`.sidebar-top-btn` 1.25rem, PaperSidebar.vue:188-192, same `<Button variant="ghost" size="icon">` :54-62 — line-exact, my read). `icon-sm` ships in the CVA union AND its utilities are pre-compiled (top-level components.css — delivery verified, the K1 emission test PASSED); repo uses = 0 of 36. K2's touch-cell correction incorporated. **F.W3/W4** (the `icon-sm` swap, which also defuses MISS-DU1); the 7.0.0 size-vocabulary death rides D-M3's F.W1 row.
- **D-M1 — MAJOR LATENT, FOLD → banked D-14 + PSM-4.** Placeholder 1.888:1 / glyph 2.04:1 / action glyph 1.89:1 at the authored 45%-dilution recipes; reader-DU's independent recompute calibrated against the token's own 5.21 comment (instrument agreement to 3 d.p. with the banked cells). Dead today; the C-1 cure ARMS it — same-commit rider banked. **F.W1 token re-ink + F.W3/W4 gate.**
- **D-M2 — MAJOR, FOLD → banked MISS-A1.** The leading `<Search>` (:36) renders lucide's 24×24 default (defaultAttributes re-read) at full dilution-token colour; the authored 0.8rem box (PaperSearch.vue:63-64) is dead; `aria-hidden="true"` stamped (Icon.js:41), so it is also invisible to AT. Glyph-final-size cell CONDITIONAL on the F-1 emission decider (ruling 10). **F.W3/W4.**
- **D-M3 — MAJOR (ruling 1; census cells replaced by this seat's).** glass-ui 7.0.0 deletes BOTH props this file passes: no `variant` in the props object (button-Bu9F4uU6.js:10-31, both readers read verbatim at the named baseline), no `icon` in `ButtonSize`; the defaults' glass predicate is TRUE → the two flat ghost affordances become glass capsules at `md` geometry. CENSUS-2026-08-03.md:102-106 enumerates removed subpaths/members only — the Button prop-contract deletion is absent: **contradiction-by-omission, sustained**. Adjudicated blast radius: **87 template `variant=` sites · 36 `size="icon"` · 35 files** (+9/+2 comment-resident, +17 non-Button). K1 (the `variant="standard"` sub-cell) killed — those are Sliders, and `standard` is a live documented member of Slider's own union. **Route F.W1** (budget row for the tri-package uplift; the typecheck-vs-fallthrough limb stays UNPROVEN pending F.W0).
- **C-7 / D-M4 — MAJOR, FOLD → banked D-9/D-10/D-11/C-M7/C-B3/C-M1 + PSM-23.** Zero ARIA in the file (grep 0); a hand-rolled combobox driving `selectedIndex` whose ONLY render is a dead scoped class (`is-selected` — PaperSearch.vue:132-135, dead by C-1's mechanism; Dropdown:46 / Modal:88 re-verified); `title` the sole accessible name on both icon Buttons (a real accname — not an axe failure — but touch-invisible on the arm that exists only for touch). **F.W3/W4** (`./dialog` + combobox semantics per the banked cure).
- **D-M5 — MAJOR LATENT (reader-LC's booking adopted; K11 cell correction in).** Three type ladders in one 220px panel (raw `0.78rem` — TWO sites, PaperSearch.vue:75 + PaperSidebar.vue:269, my grep — vs `@apply text-sm` vs `@apply text-base` vs the producer's `--control-text`); glass-ui's iOS anti-zoom guard is INERT (`.ios` gated, base.css:310-316; no code sets the class — grep 0); the C-1 repair makes 14.04px live on the mobile root and ARMS the zoom. Same-commit rider class; zoom itself SS-13. **F.W3/W4.**
- **D-M7 / C-6 / m-3 — MAJOR.** The control labelled "Clear search" (:64) calls `close()` (:63) — four state writes including `query = ""` (usePaperSearch.ts:48-53, my read) — and is `v-if`-gated on the very value it clears (:59), deleting itself under the pointer; no focus-restoration path exists (the only watcher fires on open, :20-25; `focus()` exposed :27-31 but uncalled on this edge). Reader-LC's floating-arm extension ADMITTED: there `close()` cascades through MobileFloatingToc's watcher into full bar unmount — teardown is total. Banked adjacents (fr-MobileFloatingToc M-13, PSM-26/PSM-27) are the same family's OTHER seats; this file's self-deleting-control arm is its own. **F.W3/W4** (real `clear()`; the Dialog model collapses the trio).
- **M-2 / C-10 / L-M-2 — MAJOR, FOLD → banked fr-PaperSearch L-5.** ⌘/Ctrl-K opens invisible state below 1024px; this file owns the only `isOpen→focus` edge (:20-25) and its host is `display:none` there (PaperSidebar.vue:135/:138); the mobile host's `searchActive` is unreachable from `open()`. **F.W3/W4.**
- **M-3 — MAJOR (reader-LC; premises re-verified).** No focus restoration on modal collapse: the watcher keys on `isOpen` alone; `isExpanded` true→false (toggle or Escape branch) strands focus — arrows/Enter/Escape dead until pointer re-entry. Distinct from the banked keyboard-death rows (those book focus-on-result; this books the collapse edge). **F.W3/W4.**
- **L-§3 / C-M4 — MAJOR, FOLD → banked L-2/C-M4 (ruling 3, re-grade sustained) + PSM-5.** Truncated-prefix cache narrowing; trimmed keys reset the chain at every space (the flagship example does not reproduce — K13 banked); producer caches untruncated. Both readers sustain the banked overturn of the corpus's BLOCKER grade; so does this seat. **F.W3/W4** via producer adoption.
- **M-1 — MAJOR, FOLD → banked L-11 duplication cell.** This file and PaperSearchModal are the same component twice (watcher ≡ :17-25/16-24; binding ≡ :42-43/:57-58 — byte-parallel, my reads); K13's correction folded in: the copies have ALREADY drifted (X at `h-3` "Clear search" vs `h-3.5` "Close" — same `close()`, two labels, the first symptom of the defect the row names). **F.W3/W4.**
- **C-11 — MAJOR, FOLD → banked D-7/L-7.** This file raises `isOpen` on `@focus` (:45) and owns no close edge; the dismiss backdrop is floating-only (Dropdown:65-69, my read) — the sidebar arm latches until Escape/selection. **F.W3/W4.**
- **MISS-LC1 — MAJOR (NEW, reader-LC; census re-run by this seat — ADMITTED as a mandatory rider).** The corpus's OWN prescribed cure orphans five cross-SFC shared hooks: `paper-search-action-btn` is emitted by Input (:51/:62) AND Modal (:64/:73); `-result`/`-badge`/`-number`/`-label` by Dropdown AND Modal (my grep census, all sites listed in instruments). Both repair plans (L §10 step 1, D §5 step 3) assign each class to exactly ONE child's `<style scoped>` — after which the modal's action Buttons and its 30 result rows are unstyled under a green typecheck, and the three surviving modal-local overrides (PaperSearch.vue:308-319) would compose on top of nothing. Cures: hoist the five shared hooks global (the style.css:129-143 pattern), duplicate into both children, or make the result row a component. **F.W3/W4 — same-commit rider on the C-1 repair, PSM-13's class** (the second registry row where the repair arms the defect).
- **MISS-LC2 — MAJOR (NEW, reader-LC; re-verified end-to-end by this seat's lock scan).** The audited tree is NOT reproducible from its own lockfile: glass-ui 4.0.0 declares `@lucide/vue ^1.16.0` a REQUIRED peer (first member — my read); the lock RECORDS that peer requirement inside the glass-ui entry yet contains **no** `node_modules/@lucide/vue` package entry; on-disk sits 1.20.0 — unlocked, unmanifested (absent from both dep sections of package.json). glass-ui's `createLucideIcon` chunk is reached from dock/select/configurator/dropdown-menu/collapsible/tabs — subpaths this app imports — so a faithful `npm ci` yields module-eval throws one hop from this file's :3/:4. Third member of the C-3/C-8 misfiling class, and the worst: a required peer filed NOWHERE. **Route F.W0** (substrate reproducibility, beside banked MISS-A7) **+ F.W1** (the manifest lands with the uplift).

### Minors (14)

- **m-1 / C-4 — MINOR (ruling 3).** Required prop `variant` declared (:9), threaded from both hosts, read never (grep: :9/:49/:60 — the latter two are Button's own); no gate sees it (no eslint, `noUnusedLocals` off). K-4's corollary is the disposition: the scope fix does NOT give it a job — delete it or employ it deliberately. **F.W3/W4.**
- **m-2 / C-9 — MINOR, FOLD → the fleet dead-barrel class** (banked BY NAME at fr-PaperSearch MISS-B5, which cites "PaperSearchInput C-9"). Barrel importers = 0, re-verified. **F.W3/W4.**
- **m-4 / D-M6 — MINOR (both readers converge; L's grade adopted over D's MAJOR).** The `:value`+`@input` pair (:42-43) bypasses `vModelText`'s composing guard — verbatim in installed runtime-dom (:1495/:1515/:1535, my read; **fully static**, correcting banked SS-13 #10's "not derivable from source" cell) — so every intermediate IME keystroke searches; duplicated at Modal:57-58. English-LaTeX corpus bounds the blast. **F.W3/W4** (v-model or two-line composition guard, both files).
- **m-6 / C-N6 — MINOR, FOLD → banked L-8/C-N6.** Undisposed debounce timer; no `onScopeDispose` in the family. **F.W3/W4.**
- **D-M8 — MINOR (ruling 7).** `--floating` deletes the wrap's border (`border: none`, :204-209) that the live `:focus-within` affordance rides (:58-60 — same subject, same (0,3,0), later source order wins) and replaces it with nothing; the input's UA ring survives (banked S-1 accident). D-16 repair-hazard bank. **F.W3/W4.**
- **M-4 / C-5 — MINOR, FOLD → banked C-M2/C-M3 (ruling 8).** Direct ref writes past the composable's own `open()`; raw refs returned unprotected; the one correct channel (`expand` emit :13-15/:52 → PaperSearch:29) sits in the same 69 lines. **F.W3/W4** spec (narrow per-child interfaces + `open()/close()/clear()` as sole mutators).
- **M-5 / C-12 / L-M-5 / D-i3 — MINOR, FOLD → banked L-11 test-gap cell (ruling 9; corrected statement of record).** 832-LOC family, zero unit tests, zero e2e assertions on any `paper-search*` selector, sole gate `vue-tsc -b` (structurally blind to dead selectors); `/paper` IS navigated by two specs; axe (imported in two specs) never runs the route. D-i3's residue INFO within. **F.W3/W4.**
- **D-m1 / D-m4 / D-m6 / D-i1 — MINOR (omnibus, reader-DU's grades sustained).** Debounced re-lay of the row (the expand Button mounts on the 120ms clock the clear Button ignores — banked D-29/C-N3 adjacency); six geometry literals against imported tokens (`calc(var(--radius) - 2px)` = 2px, verified); ASCII ellipsis + `placeholder="Search paper..."` duplicated across both files; unguarded focus↔isOpen cycle (:45 + :20-25). **F.W3/W4.**
- **D-m5 — MINOR (new to the family).** No mobile input hygiene: `type="text"` and nothing else (:37-46 — no `type="search"`, `enterkeyhint`, `inputmode`, `autocapitalize`, `autocorrect`, `spellcheck`, `autocomplete`) on a field whose floating arm exists only for touch (MobileFloatingToc:106 `lg:hidden`). **F.W3/W4.**
- **C-8 — MINOR, FOLD → banked PSM-33.** Four runtime lucide symbols (:4) from devDependencies (lock `"dev": true`, my scan); rides the F.W1 rename ×35 with the manifest move. **F.W1.**
- **MISS-DU1 — MINOR (NEW, reader-DU; operands verified by this seat — ADMITTED).** The field grows ~16px TALLER on the first keystroke: the wrap (alive; `align-items:center`, 0.3rem padding, 1.5px border — :47-56 my read) sizes to its tallest item; empty ≈ 36.6px (24px glyph + padding + border); the undebounced clear Button (`v-if="search.query.value"` :59) mounts at a DEFINITE 40px (`h-(--control-h-md)` pre-compiled — delivery re-verified) → ≈ 52.6px; `<PaperSearch>` is `.sidebar-nav`'s FIRST child (PaperSidebar.vue:51), so the whole Contents tree translates ~16px down on the first character and back up on clear. All three axes priced the Buttons' arrival as horizontal; the live consequence is vertical. Registry-swept (no banked row books the field's own vertical growth). Cured by the same `icon-sm` swap as D-B2. Shift's perceptibility → SS-13. **F.W3/W4.**
- **MISS-DU2 — MINOR (NEW, reader-DU; recipe read whole by this seat — ADMITTED).** glass-ui 4.0.0 ships a GLOBAL, scope-proof `.input-bar-field` recipe (utilities/components.css:222-233: `flex:1; min-width:0; border:none; outline:none; background:transparent; font-size:var(--type-small)` + a 50%-muted `::placeholder`) — five of the six declarations that die in `.paper-search-input`, including D-B1's whole `min-width:0`, reachable via style.css:3 and immune to the scope defect BY CONSTRUCTION (the fira-code/S-12 mechanism; the `.kbd`/PSM-25 missed-adoption precedent). Honest costs sustained: mono family needs one override; the `.input-bar` CONTAINER (2.25rem glass bar, backdrop-filter) does not transfer. **Rider on the F.W3/W4 cure, never a substitute for the D-1 relocation** (S-1's masking rule).
- **MISS-DU3 — MINOR (NEW, reader-DU; spec + CSS-Overflow-3 reasoning verified — ADMITTED).** The repo already runs a checked-in horizontal-overflow gate on `/paper` at three viewports (visual-baseline.spec.ts:31/:41-50/:61-68, read by this seat) and it is STRUCTURALLY BLIND to D-B1: `.sidebar-nav`'s `overflow-y:auto` (with no `overflow-x` declared — :151-165, my read; `overscroll-behavior-x` is not `overflow-x`) computes `overflow-x` to `auto`, so row overflow is absorbed by the panel's own scroll port and never reaches `documentElement.scrollWidth`. Consequence adopted into D-B1's disposition: the cure's assertion must be CONTAINER-scoped (`.sidebar-nav` scrollWidth vs clientWidth). **F.W3/W4** gate design.
- **MISS-DU4 — MINOR (NEW, reader-DU; template re-read — ADMITTED).** The field's chrome is pointer-dead: no `<label>`, no click-to-focus on wrap or glyph (:35-46) — so the largest mark in the field (the 24px glyph) and all wrap padding are inert hit area on both arms, while the needed `focus()` method already exists (:27-31) and the floating arm spans nearly the whole bar (`.floating-toc-bar--search > :first-child { flex:1 }`, MobileFloatingToc:261-264, my read). A wrapping `<label>` is also the cheapest cure for C-7's naming half. Registry-swept (MFT M-3 books the TRIGGER span — different element). **F.W3/W4.**

### Info (2)

- **D-i2 — INFO (register note; correction (b) above).** The focus-state CHANGE measures ≈1.71:1 — an AAA-2.4.13-register figure; the binding AA comparison is the resting 1.930:1 (banked MISS-A6, F.W1 token re-ink). Recorded so the forming specs cite each figure under its right criterion. **NO-WAVE-OWNER** — SS-3/SS-4 spec-authoring input.
- **MISS-LC3 — INFO (NEW, reader-LC; emission scans re-run in substance by this seat — ADMITTED).** Not one rule but the Button base's whole `[&_svg…]` register (three utilities incl. `shrink-0`/`pointer-events-none`) is absent from the producer's shipped CSS; local consequence nil (`.btn-pill > * { flex-shrink:0 }` covers it). Extends banked K1. **GLASS-RELAY rider** (the letter names three rules, not one), decision at **F-1**.

## Killed-claims register — 13 entries

- **K1 · D-M3's `variant="standard"` sub-cell — SUSTAINED (both readers, convergent).** The 14 sites are `<Slider variant="standard">` (+1 MetricBadge), and `standard` is a live, documented member of Slider's own 4.0.0 union (`index.d.ts:43`). The challenge's unrestricted grep was attributed to the Button surface enumerated one line above it. D-M3's headline untouched.
- **K2 · D-B2's "44px on touch" cell — SUSTAINED (reader-DU), with the viewport conditional made explicit.** At the sub-768 root (1.125rem) the coarse floor is `2.75rem` = **49.5px** — the same root the finding's own 67.5px cell uses; 44px is true only at the ≥768px coarse root (1rem). Internally inconsistent as written; the `icon-sm` cure unharmed. (PSM K-5's viewport-conditional class, third occurrence.)
- **K3 · D-i3's + C-12's falsifiers ("no spec navigates /paper" / "name a spec that mounts it") — SUSTAINED (both readers, convergent; cells corrected by this seat, ruling 9).** paper-performance.spec.ts:38 and visual-baseline.spec.ts's paper row × 3 viewports answer both. Surviving substance: zero assertions, zero axe on the route.
- **K4 · D-S2's published receipt (`grep "transition: all" → no match`) — SUSTAINED (reader-DU; reproduced by this seat: 4 hits, every one an A.W3.d comment whose words are "no `transition: all`").** The substantive claim survives with the corrected receipt (`PaperSearch.vue` grep-c 0). Fifth banked occurrence of citation-inheritance-inside-a-confirmation (fr-EasingPicker · fr-CollapsibleSection DISSENT-1 · fr-MobileFloatingToc R-4/K-13 · fr-PaperSearch K1-class).
- **K5 · D-§0.1 / D-M2's shipped-dist evidence limbs — SUSTAINED (both readers; STALE-DIST law).** `web/dist` predates the uplift (HEAD pins ^3.1.0); `control-h` grep over it → 0. Every conclusion re-anchored to the INSTALLED producer before any F-wave may quote it. Folds to banked correction 5 / PSM K-6.
- **K6 · D-m3's "unreachable empty state" limb — SUSTAINED (reader-DU).** `canExpand` gates ENTRY only (PaperSearch.vue:28); once expanded, refining to zero renders the modal's `v-else` "No results" (:104-106, my read) — the same mechanism the corpus's own m-5 and banked PSM-8 assert, so the corpus contradicts itself. The surviving, important finding: neither INLINE arm has ANY empty state (Dropdown gates the whole panel on `length > 0`, :37 — my read).
- **K7 · reader-LC's K-2 (census-method kill) — SUSTAINED IN DIRECTION, both replacement cells CORRECTED (ruling 1).** The file-level 113/38 are method artifacts, as K-2 said — but K-2's own 96 counts 9 comment tags and its 37 drops the quote-broken PaperView site. Figure of record: **87/36/35 (+9/+2 comments)**.
- **K8 · reader-LC's K-3 (data-size enumeration kill) — OVERTURNED (ruling 2).** The challenge's stylesheet enumeration (`sm`/`md`/`lg` only) is CORRECT; xs/xl/control/dock live in JS chunks, not `*.css`. Residue adopted: `ButtonSize` type carries `xs`; `icon` absent everywhere. A killer's cell killed — second occurrence of the fr-PaperSearch-K8 class.
- **K9 · L §10's "fixing §1 gives `variant` its job back" — SUSTAINED (reader-LC, ruling 3).** Plain relocation works (subject-compound attribution; the challenge's own probe output is the proof); the dead prop is NOT retired by the fix. Reader-DU's C-4 cure cell corrected.
- **K10 · L-m5's consequence limb (Minimize2 unmounts "exactly when it is the collapse control") — SUSTAINED (reader-LC).** The expanded surface carries its own unconditional collapse Button (Modal:61-69, re-read), plus overlay-click and Escape — three live paths; the Input's Minimize2 is behind the overlay in that state.
- **K11 · D-M5's "0.78rem appears exactly once" cell — SUSTAINED (reader-LC; my grep: two sites, PaperSearch.vue:75 + PaperSidebar.vue:269).** The token-bypass finding strengthens (the magic number was COPIED, not tokenised); the uniqueness cell is false.
- **K12 · C-3's falsifier (`npm rm class-variance-authority && npm run build`) — SUSTAINED (reader-LC).** Mis-specified twice: npm ≥11 re-satisfies the peer on install, and `npm run build` cannot run at the pin (banked MISS-A7's vite-8/manualChunks break — which silently disables every build-based falsifier in all three challenge files). The correct probe is `npm ci --omit=dev`, which fails today (lock `"dev": true` ×4 — this seat's scan).
- **K13 · M-1's "character-identical" cell — SUSTAINED (reader-LC; my read: Modal's `<X class="h-3.5 w-3.5">`, `title="Close"`).** The duplication survives; the copies have already drifted in glyph scale AND label — two names for one destructive `close()`, itself a finding the corpus walked past (folded into M-1's roster row).

## Superlatives — 9 sustained (provenance per row)

1. **Challenge-D §0's corpus-fold table** (challenge-D-design.md:19-40) — eleven rows publishing the file's own non-originality BEFORE its findings, relation stated per row; every fold claim resolved correctly when checked (reader-DU verified; this seat spot-checked two). The reason this three-axis corpus was cheap to re-read.
2. **Challenge-D §5's repair ordering** — states unprompted that curing C-1 first ARMS three regressions and orders the cure to defuse them: the same mandatory-rider insight the fleet minted as PSM-13 after the neighbouring corpus failed to have it, reached on the same substrate in the same week. (This seat's MISS-LC1 admission adds the fourth armed regression the ordering missed — the standard it set is the standard it is judged by.)
3. **Challenge-D §1's authored-ratio table** — six dead declarations as ratios against the three foreign authorities that actually size each member; every cell attackable, all six reproduced (reader-DU re-ran; this seat re-verified the lucide and control-h authorities independently).
4. **Challenge-L §8's published self-kills** — four candidate claims raised and killed by their own falsifiers, including padding-cannot-override-a-fixed-`h` — reasoning the fleet independently minted six weeks later as PSM K-5. All four kills re-held under reader-DU's re-derivation.
5. **Challenge-C §0's import table with load-bearing zeros** (C-13/C-14: value.js 0 · keyframes 0 · fourier-API 0, each with its own falsifier, closed over the import closure) — twice ratified in the registry (PSM-36/S-11, C-Z1/C-Z2); it bounds this 69-line file's tri-package exposure to the lucide rename.
6. **In the component: both public surfaces are exactly one member wide and both are LIVE** — `defineExpose({focus})` :31 with a real three-level caller chain (→ PaperSearch:15-19 → MobileFloatingToc), and `defineEmits<{expand:[]}>` :13-15 fired once :52 into a live handler — while both sibling files declare a `select` emit and never fire it. Folds to banked superlative 7 (fr-PaperSearch), re-verified link-by-link by reader-LC.
7. **In the component: teardown posture clean by construction** — grep for listeners/timers/rAF/document/window exits empty (both readers ran it; the instance-bound watcher's deferred continuation is null-guarded :23). The contrast sits one file over: Modal's `document.querySelector` (:32) is the banked wrong-duplicate steering defect.
8. **In the component: producer consumption done right even where the sizing inside it is wrong** — declared export-map subpath import (:3), both CVA members valid at the pin, `title` landing by attrs fallthrough (no `inheritAttrs` suppression — reader-LC's chunk grep), type-only `import type` discipline exact across all six family files. The 4.0.0 posture is correct; the 7.0.0 hop (D-M3) is priced, not feared.
9. **D-B2's prescribed cure passes the emission test the registry made mandatory** — `.h-\(--control-h-xs\)`/`.w-\(--control-h-xs\)` ARE pre-compiled in the installed top-level components.css (this seat re-verified, after first grepping the wrong same-named file — the receipt is the point): `icon-sm` would actually paint, unlike the `--ui-glyph` register K1 killed. A challenge that proposed a cure and happened to propose a reachable one.

## UNPROVEN-NEEDS-LIVE residue — SS-13 visual-audit inputs

1. **The intrinsic min-content width of the unsized `<input>`** and the overflow resolution inside `.sidebar-nav` (clip vs scrollbar vs auto-minimum shrink) — banked as fr-PaperSearch SS-13 #3; the corrected ~13px default-state overrun makes the live measure MORE load-bearing (it sits inside the unmeasured figure's error bar). Gate the cure container-scoped (MISS-DU3).
2. **The F-1 emission decider** (ruling 10): whether the consumer's Tailwind emits `[&_svg:not([class*=size-])]:size-(--ui-glyph)` — fr-PaperSearch K1 (killed) vs PSM-24/R-3 (live-on-next-build) cannot both stand; one fresh build at F.W1's pre-gate settles this component's glyph cells (12px vs 16/24px) and the D-§3 glyph-box ledger row. Static weight: K1's side (four instruments, incl. this seat's).
3. **iOS auto-zoom at 14.04px** once the C-1 repair makes `0.78rem` apply on the mobile root — arithmetic and the inert `.ios` guard are static; the zoom is not (D-M5).
4. **`document.activeElement` after the clear Button unmounts itself** (D-M7) — removal static, destination UA-dependent.
5. **MISS-DU1's ~16px ToC shift** — film before/after in the same pass as #1 (same repair's delta); the height arithmetic is static.
6. **`variant="ghost"` at 7.0.0: hard vue-tsc error vs silent fallthrough** (D-M3 limb 1 asserts both) — unmeasurable until F.W0 clears the banked MISS-A7 build break; the 87-site budget is typecheck-shaped only if the error fires.
7. **Live IME behaviour** on the composition bypass (m-4) — the MECHANISM is static (registry correction (a)); only the composed-character UX needs a session.

## Closing verdict

**DEFECTIVE BY INHERITANCE, AND THE FAMILY'S MOST REPAIR-CRITICAL 69 LINES.** Nothing here re-litigates the banked mechanism rows — 13 of 34 rows FOLD by reference, the identity guard holding against two readers' re-grade pressure in both directions — but this file is where the family's cure will succeed or fail: it hosts the only live-styled element (the wrap), the two worst live geometry defects (40-67.5px controls beside a 20px hand-corrected twin; a 24px glyph fronting a 14px label), the self-deleting destructive control, and now TWO mandatory same-commit riders on the C-1 relocation — the shared-hook orphaning (MISS-LC1) that both challenge repair plans would have shipped under a green typecheck, and the armed contrast/zoom/focus regressions already banked. The r2 pass also bought two substrate facts bigger than the component: the tree cannot be rebuilt from its own lockfile (MISS-LC2 → **F.W0**), and the F.W1 uplift budget every prior figure got wrong is now pinned at **87 variant / 36 icon / 35 files** with both readers' methods dissected in the record. Roster: **1 BLOCKER · 17 MAJOR · 14 MINOR · 2 INFO**; kills: **13**, one overturned against a reader, one killer's cells replaced; routes: the repair cluster **F.W3/W4** (with the MISS-LC1 + repair-hazard riders same-commit), the census/manifest/budget rows **F.W1** (D-M3 · C-3 · C-8 · MISS-LC2's manifest arm, plus the F-1 pre-gate decider), substrate reproducibility **F.W0** (MISS-LC2, beside MISS-A7), one **NO-WAVE-OWNER** spec-authoring note (D-i2's register correction), one **GLASS-RELAY** rider (MISS-LC3, three rules not one), and **F.W5-W8 empty** (the banked zeros hold). Two register corrections against banked rows travel as notes for SS-3/SS-4 — the dated files stand untouched.

— end of apotheosis r2. The three challenge files and both reader returns remain immutable beside this record per E-1/E-3; dispositions are ADJUDICATED, not VERIFIED (M-25); the routings are agglomeration input to the forming X·F specs, and execution awaits the owner's begin-word.
