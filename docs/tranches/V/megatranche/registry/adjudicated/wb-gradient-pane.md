# wb-gradient-pane — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (the incredulity seat) · **Date**: 2026-08-11 · **HEAD**: `c6072a70` · branch `tranche-u`.
**Corpus**: three challenge axes (C/D/L, single round — no r2+ files exist for this component; the three govern) against `demo/workbenches/gradient/GradientPane.vue` (29 lines, SHA-256 `193b938e…6169880`, byte-identical to the challenge receipts) and its transitive shell + visualizer chain.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted. Every kill was re-derived by this seat with its own instruments; the two verdict-level severity conflicts (C-3, L-3) and one grade-inflation (reader-LC's one-frame mis-assignment) were ruled by my own traces, not by picking a reader. Three reader evidence CELLS were corrected on my re-derivation (see Ruled disagreements rows R-4/R-5/R-6) while their rows' substance survived.
**Arbiter's own instruments (all static + grep against source and installed dist; ZERO browser probes — probe parsimony, per the ColorSpaceSelector/EasingSpecimenStrip precedent)**: whole-file reads of `GradientPane.vue` (29 L), `GradientVisualizer.vue` (279 L), `PaneSlot.vue` (129 L); targeted reads `useGradientModel.ts:40-180` (birth stops :79-82, length-keyed watcher :89-100, `removeStop` ≤2 guard :122-125, `setStopsFromColors` :142-149, atomic `applyCSS` :158-168), `useGradientCSS.ts:30-330` (`COALESCE_RESOLUTION=32` :42, `serializeGradient` linear/conic-only angle, `sampleCoalescedStops`, `serializeCoalescedGradient` radial drops direction), `usePaneRouter.ts:40-50/:69-80/:100-115/:170-215` (`DockAction.disabled?` present, `PaneActionRefs` = three `Ref<any>`, all ten panes `defineAsyncComponent` with no `loadingComponent`, mobile index-1 ⇒ right pane, the gradient action triple), `App.vue:38-39/:40-50/:70-135/:265-275/:305-350/:355-365` (mobile PaneSlot carries NO `:on-mount`; both desktop slots do; `gradientPaneRef = ref<any>`; `onDesktopLeftMount(el: any)` reads `currentConfig.value.left` at call time; compound aspect breakpoint; `provide(CSS_COLOR_KEY, cssColorOpaque)`; `useClipboard({resetMs:2000})` → `linkCopied`), `Dock.vue:29-75/:125-190` (`hasAnyActionBar` viewport-ungated; width-only `useMediaQuery("(min-width:1024px)")`; `:always-expanded="!isDesktop"`; the router's bar arrives on `:generic-action-bar` at App.vue:39; the W6-8 title-retirement comment), `GenericActionBar.vue` (whole — **it DOES bind `:disabled="act.disabled"`**), `ActionBarToggle.vue:55-110` (slot renders at every breakpoint; only label + separator/chevron are desktop-gated), `ActionButton.vue` disabled census (`aria-disabled` + `disabled` + `opacity-50`), `viewSchema.ts:161-169` (gradient = dual-pane, right `palettes`), `PaneHeader.vue:8-25/:41-60/:140-230` (h3 title; `.pane-scroll-fade` = exactly `contain` + `scroll-timeline`; `@supports (animation-timeline: scroll())` gate; from-state = base-state; the :148-152 position-mapped-scrub exemption comment), `GradientCodeEditor.vue:30-115` (truce, `role="status"`, `aria-invalid`, blur-only re-sync), `GradientEasingEditor.vue:88-106` (`useClipboard({resetMs:1400})` + `copiedRow`), `GradientStopEditor.vue:60-80/:230-245` (`selectedStop … ?? null`); installed glass-ui 7.0.0: `DockControl.vue.d.ts` prop union `{shape, compact, active, type, disabled, as, asChild, class}` — **no `title`**; `useClipboard.d.ts:10-40` discriminated `CopyResult` + never-rejects contract; repo censuses: `cssColorOpaque` in GradientPane.vue = 1 hit (:8, assigned never read) and the byte-identical twin `GeneratePane.vue:10`; `selectedStopId|selected-stop-id` outside GradientVisualizer.vue = **0** hits; `<h1` in demo = **0**, `<h2` = AboutPane.vue:49 + ColorNutritionLabel.vue ×5 only; X wave ownership resolved from `docs/tranches/X/waves/W5.md:25-45/:150-200` (X.W5.a `bindPane`/required-generic `onMount`/gate A5 H1 walk; X.W5.c one-mount fork deletion), `W6.md:38-70/:150-230/:441-444` (GradientPane.vue in W6's file bounds; X.W6.a intervals-array deletion + stop-owned easing; X.W6.b "flat heading outline corrected"; X.W6.c literal dialect), `W8.md:120-130/:183-192/:325-335` (X.W8.d G-10 names `GradientPane.vue:8`'s inject deletion; W8 opens only after W6 closes); `shell-paneslot/challenge-L-library.md` §L-2 (BLOCKER, mobile no-`:on-mount`) read at its header.
**Motion/PRM law**: `audit/codex-provenance/motion-quarantine.md` rows re-read by this seat — **M-33 is FALSE and is NOT carried** (the "bottom fade" does not exist; kill K-2 below cites it as binding); **M-34 is TRUE and IS carried** (the one surviving reduced-motion obligation in all 46 files; its mechanism is the quarantine's own NEW defect MQ-1, carryable). No other M-xx row names this component.
**Routing authority**: `registry/CARRY-CUT-LEDGER.md` + `docs/tranches/X/waves/W0–W11`. Where no X row owns a surface, the row is **NO-WAVE-OWNER** at the formation boundary (AdminListSkeleton precedent). Identities cross-booked to standing registers are marked ⇄ and are ONE identity, never re-booked (anti-rename).
**Status verbs**: every disposition here is **ADJUDICATED**, never VERIFIED — nothing in this file stamps a wave.

## Scoreboard

**22 roster rows ADJUDICATED (0 new BLOCKER · 7 MAJOR · 11 MINOR · 4 INFO) · 6 kills SUSTAINED (every one re-derived by this seat) · 12 missed-by-the-axes findings ADOPTED (readers' union, all re-verified; 1 demoted on my own trace) · 3 reader evidence cells CORRECTED · 5 UNPROVEN-NEEDS-LIVE residue rows → visual audit.**

The component's worst truth is not in its 29 lines: **on every mobile/portrait viewport all three of its published dock commands are dead** (GP-11, shell-owned, ⇄ shell-paneslot L-2 BLOCKER), and on desktop the commands that do run are dishonest — Reset does not reset the easing (GP-12), the Direction slider asserts an effect radial does not have (GP-13), and Copy copies a string the user has never seen and is never told about, with its failure result thrown away (GP-14 + GP-3). All three challenge axes read only the wrapper; every one of those four was missed. Standing cross-book, NOT re-booked here: the pane transitively hosts the already-adjudicated **ESS C-01 BLOCKER** (pane-killing easing throw, third seam through `colorAtPosition` — `adjudicated/wb-gradient-easingspecimenstrip.md`, routed X-W6); this record's verdict does not launder it.

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **R-1 · C-3 severity** | MAJOR ("not underspecified — mis-specified"; hard-pinned `savedPalettes[0]`) | MINOR (same facts) | **MAJOR** | My read of `GradientVisualizer.vue:110-116`: three silent exits AND the source hard-pinned to `savedPalettes[0]` — never the active or chosen palette — against the dock's published promise "Seed gradient stops from a saved palette." (`usePaneRouter.ts:210`). The zero-saved-palettes state is the app's most common new-user state, `DockAction.disabled?` exists unused, and re-running after saving a palette silently changes which palette seeds. A published command that is both mute and mis-specified is MAJOR; LC argued the same harm and under-labeled it. |
| **R-2 · L-3 severity** | INFO ("nothing misbehaves; the type system simply cannot see") | MAJOR (rename `reset` → vue-tsc green → dock button silently dead) | **MAJOR — LC wins** | The pane-side surface is actually TYPED (my read: `GradientPane.vue:9` `ref<InstanceType<typeof GradientVisualizer> \| null>`; `defineExpose` publishes a typed instance surface) — the erasure is 100% consumer-side: `PaneActionRefs { gradient: Ref<any> }` (`usePaneRouter.ts:107-111`), `ref<any>(null)` ×3 (`App.vue:317-319`), `onDesktopLeftMount(el: any)` (:323). LC's rename scenario is concrete (`any.reset?.()` compiles against nothing), and the SAME `any` legalises GP-15's wrong-instance write. A hole that converts a compile error into a silently dead control is not INFO. |
| **R-3 · LC missed #2 grade** (one-frame wrong-instance ref assignment) | — (not found) | MAJOR | **CONFIRMED mechanism, DEMOTED to MINOR** | I re-derived the mechanism end-to-end and it is real: `PaneSlot.vue:124`'s inline arrow ref is a fresh identity every render; Vue's `shouldUpdateComponent` re-renders PaneSlot on any prop change while `liveComponent`/`liveKey` trail by one rAF (:74-97), so `setRef` fires the NEW arrow against the still-mounted OUTGOING instance while `currentConfig.value.left` already reads `"gradient"` (`App.vue:327`) — the outgoing pane lands in `gradientPaneRef` for ≤1 frame; the symmetric case poisons `colorPickerRef` with the gradient instance on swap-away. But the window is one animation frame, self-healing (unmount/deactivate nulls the ref — `unmount` unsets refs BEFORE the keep-alive `deactivate` return), and exploiting it requires a dock click inside ~16ms of a view swap. Defect class and cure are GP-11's (`bindPane` keyed by `liveKey`); as a standalone row it is MINOR. NUANCE recorded against LC's framing: the second `?.` at `usePaneRouter.ts:208-210` is NOT pure masking — it is the only thing that makes this window swallow instead of throw; the provably-inert `?.`s are the pane's own (:12-14). |
| **R-4 · evidence correction — `GenericActionBar` "no `:disabled`"** | (cell in DU's missed #1: "passed by no gradient action") | cell: "renders three ActionButtons … and no `:disabled`" | **LC's cell FALSE, row substance STANDS** | My whole-file read: `GenericActionBar.vue` binds `:disabled="act.disabled"` onto `ActionButton`, and `ActionButton` renders it fully (`aria-disabled` + native `disabled` + `opacity-50`, my grep :17/:18/:27). The plumbing exists END-TO-END; the sole gap is that no gradient action ever sets `disabled` (`usePaneRouter.ts:208-210`). This makes the cure a pure data change (`disabled: !paneRefs.gradient.value` is reactive in the existing computed) — the row gets STRONGER, the cell had to die. |
| **R-5 · evidence correction — "the expansion buys nothing when every interval is linear"** (DU missed #4 sub-clause) | asserted | — | **Sub-clause FALSE, row STANDS** | `coalescedCSS` bakes interpolation-SPACE fidelity, not just easing: with all-linear intervals and `interpolationSpace: "oklch"` (the default, `resetGradient` :122), the 32-sample expansion still buys oklch-space blending the browser's default sRGB interpolation cannot express. The defect is the SILENCE and the absence of any way to obtain the compact authored form the editor displays — not the expansion itself, which is defensible on two axes. The corrected row is GP-14. |
| **R-6 · `relative` on the wrapper — "unverified" (LC)** | inert (via `contain: layout`) | left unproven | **SETTLED — DU's derivation adopted, residue retired** | `GradientPane.vue:19`'s only child is the Card at :20, which carries `.pane-scroll-fade` → `contain: layout style paint` (`PaneHeader.vue:54-57`, my read). Per css-contain, layout containment makes the Card the containing block for all absolutely-positioned descendants — nothing inside the Card can ever resolve against the outer `relative`, and nothing outside it exists. `relative` is provably dead alongside the proven-dead `mx-auto`; GP-20 carries both, no live probe needed. |

## Adjudicated defect roster

Line anchors are HEAD `c6072a70`; re-resolve before executing any cure (the D-19 lesson). Rows GP-11..GP-22 are the adopted missed findings (readers' union, each re-verified by this seat).

- **GP-1 · C-1 / D-3 / L-1 — dead ambient-color injection — ADJUDICATED MINOR → X-W8.d (gate G-10, which names `GradientPane.vue:8` verbatim; W8 opens only after W6 closes).** `inject(CSS_COLOR_KEY)!` assigned and never read — 1 occurrence in 29 lines (my grep). No runtime hazard: `App.vue:271` provides the key on every route, and a never-dereferenced binding cannot throw; harm is review-legibility + a false coupling signal. All three axes led with this as SOURCE-RED — demotion sustained from both readers. **RIDER**: the byte-identical twin `GeneratePane.vue:10` (my grep: assigned, never read) is NOT named by G-10 — the gate must adopt it or the twin survives the cure.
- **GP-2 · C-3 — `seedFromPalette` is mute and mis-specified — ADJUDICATED MAJOR (ruled R-1) → X-W6 (`GradientVisualizer.vue` in W6's file bounds; NAMED GAP: no W6 gate names the seed contract — the wave adopts it explicitly or the row degrades to NO-WAVE-OWNER at execution) + the availability limb rides GP-11's X-W5.a cure (readiness → `DockAction.disabled`).** Three silent exits (`:111` no port, `:112` no palettes/`[0]` undefined, `:113` <2 colors); source hard-pinned to `savedPalettes[0]`; no record of source chosen or acceptance.
- **GP-3 · C-4 — the copy result is discarded — ADJUDICATED MAJOR → X-W6 (copy-feedback family; ⇄ the ESS record's M-ESS-3 row precedent, same wave).** Not "returns void" (C's mechanism cell is wrong — `GradientPane.vue:13` forwards `Promise<void> | undefined`): the producer returns a discriminated `CopyResult` that provably never rejects (`useClipboard.d.ts:17-22,37`, impl try/catches into `{ok:false,reason}`) and `GradientVisualizer.vue:127-129` awaits and DROPS it. The cure ships twice in the same app — `App.vue:362-363` and, sharpest, `GradientEasingEditor.vue:94-102` in the SAME workbench (`useClipboard({resetMs:1400})` → visible `copiedRow`). On a color tool's primary export action, denial is indistinguishable from success.
- **GP-4 · C-5 (restoration limb; the "fades" premise died with M-33) — selection silently dropped after reset/seed — ADJUDICATED MINOR → X-W6.b (owns the selection grammar; NAMED GAP: no b-gate names post-command selection).** `setStopsFromColors` mints fresh ids (`useGradientModel.ts:142-149` `uid()`); neither `resetGradient` (:118-125) nor `seedFromPalette` (:110-116) touches `selectedStopId` (:51) — the selection points at a dead id and resolves to "nothing selected" (`GradientStopEditor.vue` `?? null`, my read). Graceful, never mis-bound — MINOR.
- **GP-5 · D-1 — provenance/state invisible at the pane boundary — ADJUDICATED INFO, recorded as a design position, NOT booked.** The provable half: `GradientPane.vue:18-29` renders no status region and the header strings are static. But the failure channel exists one frame down (`GradientCodeEditor.vue:107-114` `role="status"`), and "modified" is not a state the model holds (no dirty bit, no baseline). X-W10's design-law lane is the natural forum if anyone wants it.
- **GP-6 · D-2 — three commands exposed, no availability/confirmation language — ADJUDICATED MINOR → split: availability limb X-W5.a (bindPane readiness feeds the EXISTING `disabled` plumbing — see R-4: the chain `DockAction.disabled → GenericActionBar → ActionButton` is already complete); confirmation limb rides GP-3's X-W6 row.** Rescoped from the axis's SOURCE-RED: the in-pane Copy control is visible and works (`GradientVisualizer.vue:254`); Reset and Seed genuinely have no affordance anywhere but the dock.
- **GP-7 · D-T1 — async-mount blank: no loading component, no Suspense — ADJUDICATED MINOR → X-W5 (PaneSlot.vue + usePaneRouter.ts in W5.a's file carve; NAMED GAP: no W5 gate names the async-mount blank interval — adopt at wave-open or NO-WAVE-OWNER).** Mechanism certain: all ten panes are `defineAsyncComponent` with no `loadingComponent`/`delay`/`errorComponent` (`usePaneRouter.ts:69-80`, my read) and `PaneSlot.vue` mounts them bare (no `<Suspense>` in the file). Fleet-wide, not gradient-local. Perceptibility = residue U-3.
- **GP-8 · D-CC (reduced-motion limb) ⇄ AB-8 / motion-quarantine M-34/MQ-1 — the header scrub survives `prefers-reduced-motion: reduce` — ADJUDICATED MINOR → X-W10.d (CC-099, gate G-4: `animation-timeline` moves inside `@media (prefers-reduced-motion: no-preference)`).** ONE identity with the AboutPane record's AB-8 — cross-booked, never re-booked. Mechanism re-derived: `GradientPane.vue:20` hosts `--pane-scroll`; three scroll-driven animations (`PaneHeader.vue:177-194`) take progress from the timeline, so both duration clamps are inert (quarantine measured byte-identical `matrix(0.805831,…)` under `reduce`). The honest counter-argument is preserved on the record: `PaneHeader.vue:148-152` argues a position-mapped scrub needs no PRM gate; the quarantine measured anyway and the obligation stands. Blast radius: nine panes.
- **GP-9 · L-3 — the imperative facade's type is erased at BOTH ends — ADJUDICATED MAJOR (ruled R-2) → X-W5.a (its mechanism text names the cure verbatim: `PaneSlot.onMount` becomes required and generic, `usePaneRouter` gains `bindPane(slot)` and owns typed refs).** Consumer side: `Ref<any>` ×3 + `el: any`; a renamed command passes `vue-tsc` and silently kills the dock button; the same `any` legalises GP-15.
- **GP-10 · L-4 — command/feedback drift is measured, not hypothetical — ADJUDICATED MINOR → X-W6 (adopt `useClipboard` in the gradient copy path; one clipboard posture per app).** Three postures in one codebase: `App.vue:362` stateful-with-lamp, `GradientEasingEditor.vue:94` stateful-with-lamp, `GradientVisualizer.vue:128` fire-and-forget. The prescribed "shared workbench command/result rail" stays REFUTED as contrivance (see K-5); the measured need is exactly GP-3 + GP-6's disabled bit.
- **GP-11 · MISSED (both readers) — on every mobile/portrait viewport ALL THREE published commands are dead; two have no other affordance anywhere — ADJUDICATED MAJOR → X-W5.a (`bindPane` on ALL THREE PaneSlot call sites, `onMount` required — the W5.a mechanism text is this row's cure) · ⇄ shell-paneslot challenge-L §L-2 (BLOCKER, same root, `colorPickerRef` enumeration) — ONE root identity, this row is its unenumerated gradient/generate/mix limb.** Four-link chain, every link re-verified by this seat: (1) the mobile PaneSlot passes no `:on-mount` (`App.vue:77-92` region, my read — both desktop slots do, :105/:131); (2) `PaneSlot.vue:124` attaches no ref without it; (3) `gradientPaneRef`'s only writer is `onDesktopLeftMount` (`App.vue:323-328`) — null for the whole mobile session; (4) the dock renders the bar anyway (`hasAnyActionBar` viewport-ungated, `Dock.vue:41`; `ActionBarToggle` renders at every breakpoint; `:always-expanded="!isDesktop"`), so `usePaneRouter.ts:208-210` optional-chains into null forever. Copy CSS survives only via the in-pane duplicate (:254); Reset and Seed have NO other control in the tree. Aggravator confirmed: the two breakpoints disagree (`App.vue:310-312` compound aspect query vs `Dock.vue` width-only) — a ≥1024px portrait tablet gets the mobile slot AND the desktop dock. Corrected cure economics per R-4: the `disabled` plumbing already exists end-to-end; X-W5.c's fork deletion then dissolves the class. All three axes read the 29-line wrapper; the corpus's most user-visible defect was in the caller.
- **GP-12 · MISSED (reader-DU) — Reset does not reset: per-interval easing survives it — ADJUDICATED MAJOR → X-W6.a (its mechanism DELETES the `intervals` parallel array and hangs easing on the stop that opens the interval — after which `setStopsFromColors` minting fresh stops cures this by construction). RIDER: no W6.a gate names RESET — a1 tests insert-migration only; the wave must add the reset arm or this row can ship uncured under green gates.** My re-derivation: `resetGradient` (`GradientVisualizer.vue:118-125`) never touches `intervals`; the only interval writer is the watcher keyed on `stops.value.length` (`useGradientModel.ts:89-100`) which only appends/truncates; reset from the default 2-stop state leaves length at 2 — the watcher never fires — so a `steps(4, jump-end)` authored on interval 0 survives a command whose dock description is "Reset gradient to defaults." From N stops, truncation keeps index 0's custom curve — precisely the one kept.
- **GP-13 · MISSED (reader-DU) — the Direction slider is live, styled, and effect-free under Radial while its readout keeps moving — ADJUDICATED MAJOR → X-W6 (`GradientVisualizer.vue` in bounds; NAMED GAP: no W6 gate names type/direction coherence — adopt or NO-WAVE-OWNER at execution).** Both serializers re-read by this seat: `serializeGradient` and `serializeCoalescedGradient` push an angle for `linear` (≠180) and `from …deg` for `conic` — radial takes neither branch; the model's own comment knows (`useGradientModel.ts:55` "ignored for radial"); the UI does not (`:227-234` ungated `<Slider>` + live `{{ direction }}°` readout in every type). The render tile beside it paints `coalescedCSS`, so the number moves and the picture does not — the UI actively asserts a false causation. `SliderProps.disabled?` exists (glass d.ts); the four-state cure is one bound prop.
- **GP-14 · MISSED (reader-DU; sub-clause corrected R-5) — Copy CSS copies a string the pane never shows — ADJUDICATED MAJOR → X-W6 (NAMED GAP; adjacent to X.W6.c's c3 "ONE literal dialect" gate, which must widen to the copy path or this row degrades to NO-WAVE-OWNER).** The editor displays `simpleCSS` (`GradientVisualizer.vue:259` — compact, round-trippable, "authored literals survive"); the Copy control in the SAME section header (:252-257) writes `coalescedCSS` — up to ~33 machine-mixed sub-stops (`COALESCE_RESOLUTION=32`). The expansion is defensible (easing AND interpolation-space fidelity — R-5); the silence is not: no label, no way to obtain the compact form shown. WYSIWYG breaks on the tool's raison d'être, and GP-3 means a denied copy of the unseen string reports nothing at all.
- **GP-15 · MISSED (reader-LC; DEMOTED, ruled R-3) — one-frame wrong-instance assignment through the `any` refs — ADJUDICATED MINOR → X-W5.a (same `bindPane(instance, liveKey)` cure as GP-11; registration keyed by `liveKey` kills the call-time `currentConfig` read).** Mechanism certain (inline-arrow ref identity + rAF-trailing commit + call-time config read); window ≤1 frame, self-healing, swallowed by the router's `?.` — which is therefore NOT dead ceremony (nuance recorded in R-3).
- **GP-16 · MISSED (reader-DU) — flat heading run: pane title and three section titles all `<h3>`, zero `<h1>` in the app — ADJUDICATED MINOR → X-W5.a gate A5 (fourteen-ViewId H1 walk, born-RED at 0) ⇄ AB-12 (ONE identity for the app half) + the gradient-local limb at X.W6.b, whose mechanism text already says "the flat heading outline corrected so the protagonist is named at instrument rank".** My greps: `<h1` = 0 app-wide; on the gradient view "Gradient, Interpolation, Easing, CSS" + the right pane's "Palettes" land as five h3 peers; the only h2s (About subtree) sit UNDER an h3 — inconsistent in both directions.
- **GP-17 · MISSED (reader-DU) — the in-pane Copy control passes `title`, which `DockControl` does not declare, violating the app's own register law — ADJUDICATED MINOR → X-W6 (one-attribute cure: `aria-label`, per Dock.vue's own W6-8 comment "native `title` retired dock-wide").** Producer prop union re-read by this seat: no `title` — it falls through as the native UA-tooltip attribute; accname resolves only via last-resort `title`; on touch — where the dock's Copy is dead (GP-11) and this is the ONLY working copy — no label ever shows.
- **GP-18 · MISSED (reader-DU) — on mobile the Tools bar offers gradient commands while Palettes is on screen — ADJUDICATED MINOR → X-W5.c (one mount, one column: `mobilePaneIndex` and every JS breakpoint fork DELETED — the state dissolves by construction).** Re-verified: the bar keys off `viewManager.currentView` only (`usePaneRouter.ts:203`); gradient is dual-pane (`viewSchema.ts:161-169`, right `palettes`); mobile index 1 shows the right pane (`usePaneRouter.ts:178-184`). Compounds GP-11 (the offered commands are also no-ops).
- **GP-19 · MISSED (both readers) — `.pane-scroll-fade` is a lie: the name promises a fade the rule has never contained — ADJUDICATED MINOR → ⇄ AB-22 (NO-WAVE-OWNER, the pane-chrome-recipe hygiene bundle; ONE identity, this row adds the RENAME arm: `.pane-scroll-host` at the definition + nine call sites, pure rename).** Graded MINOR over DU's INFO-lump: the misnomer single-handedly manufactured a false audit finding (M-33) that cost a live Playwright probe to kill — a name that expensive is a code defect. Nine panes carry it (`PaneHeader.vue:41-53`'s own consumer census).
- **GP-20 · MISSED (both readers; completed R-6) — the wrapper's `relative w-full mx-auto h-full min-w-0` carries two provably dead utilities — ADJUDICATED INFO → ⇄ AB-22 (same NO-WAVE-OWNER bundle).** `mx-auto` dead (w-full, no max-w; centering lives on the slot wrapper `App.vue:77`); `relative` dead by containment (R-6). Byte-identical in GeneratePane/ExtractPane/MixPane — fleet idiom, not a gradient defect.
- **GP-21 · MISSED (reader-LC) — `GradientVisualizer` publishes a `selectedStopId` two-way model no parent binds — ADJUDICATED INFO → X-W6.b (which re-authors the selection grammar; demote to a local ref or bind it — decided there, not drifted).** My tree-wide grep: zero hits outside the file itself; `GradientPane.vue:25` binds nothing.
- **GP-22 · MISSED (reader-LC; nuanced R-3) — the pane's own `?.` operators (:12-14, second position) are provably inert ceremony — ADJUDICATED INFO → X-W6.a (dies with any facade restructure).** `defineExpose` at `GradientVisualizer.vue:131` is unconditional and top-level; the child is a static import with no `v-if`; the typed pane ref makes the property non-optional. Contrast preserved: the ROUTER-side second `?.` is load-bearing (GP-15) — delete the right ones.

## Killed-claims register (every kill re-derived by this seat before sustaining)

- **K-1 · C-2 ("the component can silently turn three public commands into no-ops" — at the pane boundary) — KILL SUSTAINED.** My re-derivation: static import (`GradientPane.vue:5`), no `v-if`/`v-show` (:25), unconditional `defineExpose` (`GradientVisualizer.vue:131`), typed non-null-for-mounted-lifetime ref (:9); "before mount" unreachable from the only caller (ref-capture fires during patch, post-child-mount); "after teardown" unreachable under `<KeepAlive>` (refs are unset BEFORE the deactivate return, so the swallow happens in the shell, never the pane). The axis's instinct is vindicated at a file it never opened — GP-11 — where its prescribed cure (an `UNAVAILABLE` return from the wrapper) would fix nothing, because nothing reads the return value.
- **K-2 · D-4 ("a bottom fade… can suggest hidden content") — KILL SUSTAINED; motion-quarantine M-33 (FALSE) is binding and may not be carried.** Re-verified at source: `.pane-scroll-fade` = `contain: layout style paint` + `scroll-timeline: --pane-scroll block`, nothing else (`PaneHeader.vue:54-57`); the quarantine's live probe measured `mask-image: none`. The axis read the class NAME. The conditional second sentence compounds it by conditioning on an unreachable state (K-3).
- **K-3 · D-T4 ("empty/unseeded … explicit invitation to seed" required) — KILL SUSTAINED.** The state cannot occur: born with two stops (`useGradientModel.ts:79-82`), `removeStop` refuses below two (:122-125), `setStopsFromColors` early-returns on `[]` (:142-143), and the child treats zero stops as a programming error (`GradientVisualizer.vue:66` throw). Prescribing empty-state design for a structurally unreachable state is a false defect. (Same construction the ESS apotheosis independently ruled a superlative.)
- **K-4 · D-T6 ("failure … non-destructive diagnosis and retry … absent") — KILL SUSTAINED for the pane's largest failure surface.** `parseVerdict` (`GradientVisualizer.vue:98-108`) names the rejection, routes to a `role="status"` live region with `aria-invalid` (`GradientCodeEditor.vue:86-114`), keeps the user's text verbatim (the truce, :34-41/:64-81), and the apply is atomic (`useGradientModel.ts:151-168` — "the whole complete model swaps in, or NOTHING changes"). Plus the shell `ErrorBoundary` (`App.vue:47-50`). What survives of the row is exactly GP-3/GP-6 (copy/reset/seed feedback) — a materially narrower claim than the table's "absent".
- **K-5 · L-2 ("imports the concrete `GradientVisualizer` rather than a typed gradient-workbench port … prevents isolated reuse") — KILL SUSTAINED, and the prescribed cure (GradientPaneHandle + GradientSession + shared command/result rail) is REFUTED as contrivance.** Self-contradicting (the same document's trace table rules the edge "appropriate composition"); one implementation, one consumer, zero named reuse sites (my grep); a port for that is the wrapper-that-does-not-exist-yet the standing KISS edict bans. Decisive: the three new abstractions would leave the ONE place the commands actually die (`App.vue`'s missing `:on-mount`) untouched. The axis prescribed architecture where the tree needed one prop.
- **K-6 · D-5 ("the pane has no signature of its own … not another anonymous card wrapper") — KILL SUSTAINED.** (a) The anonymous wrapper is the deliberate fleet grammar (byte-comparable in GeneratePane/ExtractPane/MixPane; `PaneHeader.vue:12-20` "all 9 panes inherit") — measured against the house `feedback_no_god_modules`/KISS law, 29 lines of zero-logic shell is the TARGET shape (reader-LC's superlative, adopted). (b) The signature exists one frame down: the render tile, the normalized eased rail, the per-interval accordion, the live editor — 279 lines the axis never opened. A "no signature" verdict delivered without reading the pane's only content is not a finding.

## Superlatives (provenance preserved)

1. **The editor truce** (both readers): never rewrites focused text, blur-only canonical re-sync gated on a clean parse, rejected text kept verbatim beside a `role="status"` verdict with `aria-invalid`, atomic apply beneath — `GradientCodeEditor.vue:34-41/:64-81/:86-114` + `useGradientModel.ts:151-168`. Single-handedly refutes the D table's "failure … absent" row.
2. **The render tile as text alternative with an owned paint stack** (both): `role="img"` + descriptive `aria-label` on a purely painted surface, one scoped material contract (`background: var(--tile-render), var(--alpha-checker)` with explicit origin/clip/repeat/size — "Never a per-callsite `background` shorthand assembly"), alpha-checker ground so transparency reads as transparency (`GradientVisualizer.vue:214-225/:266-278`).
3. **Label-in-name holds on all four controls** (reader-DU; WCAG 2.5.3): "Type"⊂"Gradient type", "Space"⊂"Interpolation space", "Hue"⊂"Hue interpolation", "Direction"⊂"Gradient direction" (:163-233). Nothing in the corpus noticed.
4. **Engine-invariant rest state by construction** (reader-DU): every scroll-driven animation inside `@supports (animation-timeline: scroll())` with `from` byte-identical to base (`PaneHeader.vue:177-223`); the shrink ratio derived closed-form via `tan(atan2(…))` so the endpoint law survives the fluid clamp at every band. Recorded WITH its shadow: the same construction is why M-34/MQ-1 survives (GP-8) — excellence and obligation on one mechanism.
5. **One gutter scale, nine panes / the 29-line target shape** (merged, both readers): `GradientPane.vue:24` mirrors `PaneHeader.vue:11` exactly and repeats verbatim across the fleet; the "anonymous card wrapper" the D axis disparaged is the system working (K-6).
6. **One sampling law** (reader-DU): `sampleCoalescedStops` feeds the render tile, the normalized rail, and every per-interval specimen from a single function — handles, render, and specimen can never disagree about what an easing does; the rail deliberately normalizes to a 90° strip to separate the editing axis from the render axis (`useGradientCSS.ts:171-212/:245-280`).
7. **`colorAtPosition` refuses to lie** (reader-LC): three explicit throws instead of a silent `?? "#000"`, with the cured defect's provenance in the header comment (W5-11). **CAVEAT, this seat's own**: the praise attaches to the explicitness, NOT to totality — this function is the third seam of the already-adjudicated ESS C-01 BLOCKER (hover with a back-eased interval kills the pane; `adjudicated/wb-gradient-easingspecimenstrip.md`). The superlative and the blocker share a line; the cure is booked there.
8. **The producer clipboard boundary is exactly right** (reader-LC): discriminated `CopyResult`, never rejects, documents its own ergonomics — every demerit in GP-3 belongs to the consumer that drops it.
9. **The correct copy posture ships eight lines of import away** (reader-LC): `GradientEasingEditor.vue:94-102` — proof the house pattern was available, understood, and adopted inside the same workbench that also ignores it.

## UNPROVEN-NEEDS-LIVE residue (→ the visual audit, task inputs)

- **U-1** — safe-area / soft-keyboard obscuration (challenge-D:16,:29): the pane declares no `env(safe-area-inset-*)`/visual-viewport handling and the sole text entry is a contenteditable inside an `overflow-y-auto` host — whether the keyboard occludes it and whether the host can recover is device-only. Both readers concur; the axis's own "unproven" stands.
- **U-2** — selected-stop ring visibility against a live gradient rail of arbitrary luminance (challenge-D:25): focus-visible rings and the selection ring are declared (`border-white` vs `border-white/80`); rendered contrast over a user-chosen ramp is unbounded from source. Adjacent to the ESS record's a11 white-ramp gate — the stop-editor's own record owns the cure; this cell is the pane-level witness.
- **U-3** — perceptibility of GP-7's async-mount blank: mechanism certain (no loadingComponent, no Suspense); duration is chunk-size/network-dependent. Probe parsimony forbade measuring here.
- **U-4** — narrow-height reachability (challenge-C:23): rail + accordion + editor stack in one scroll host with no sticky selection anchor; whether the selected control leaves reach at a given height is live-layout.
- **U-5** — the `InstanceType`/`defineExpose` resolution under this repo's exact vue-tsc (reader-LC's honest caveat underpinning GP-22/K-1's typing arm): standard Vue behavior relied upon; the cheap witness is a deliberate unknown-method reference erroring under `npm run typecheck` (requires the `pretypecheck` build). Static-cheap, not visual — recorded so it is not lost.

## Closing verdict

**DEFECTIVE — NO NEW BLOCKER; SEVEN MAJORS, AND THE WORST OF THEM LIVE IN THE CALLERS.** The 29-line wrapper is close to the house target shape (K-6, superlative 5) — and it fronts a command surface that is dead on an entire platform class (GP-11, shell-owned, ⇄ shell-paneslot L-2), dishonest about reset (GP-12), direction (GP-13), and what Copy copies (GP-14), and mute about every outcome (GP-3, GP-2). All three challenge axes mis-graded the one harmless line they could see (the dead inject) as SOURCE-RED while missing all four command-truth defects; two of their five kills died on states that are structurally unreachable and a fade that does not exist (M-33 binding). Routing: X-W5.a carries the shell mass (GP-9/GP-11/GP-15 + the H1 walk), X-W5.c dissolves GP-18, X-W6 carries the gradient-truth mass (GP-2/GP-3/GP-4/GP-10/GP-12/GP-13/GP-14/GP-17/GP-21/GP-22 — four under NAMED GAPS the wave must adopt at open or they degrade to NO-WAVE-OWNER; GP-12 carries a reset-arm rider on a1), X-W8.d G-10 deletes the dead inject (+ the GeneratePane twin rider), X-W10.d owns the reduced-motion scrub (GP-8 ⇄ AB-8), and the pane-chrome hygiene pair (GP-19/GP-20) joins AB-22's NO-WAVE-OWNER bundle. Five residue rows go to the visual audit. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this file.

— end of apotheosis. The three challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.
