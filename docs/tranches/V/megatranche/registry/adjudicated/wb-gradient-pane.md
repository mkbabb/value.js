# wb-gradient-pane — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (the incredulity seat) · **Date**: 2026-08-10 · **HEAD**: `54a97489` · branch `tranche-u`.
**Corpus**: three challenge documents (C-implementation · D-design · L-library, all against `demo/workbenches/gradient/GradientPane.vue`, 29 lines, SHA-256 `193b938e…`) — a REPORT-AUTHORED corpus whose three axes read the 29-line wrapper and almost nothing beyond it.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every kill was re-derived by this seat with its own instruments; the four verdict-level reader conflicts (C-2, D-2, the M-34 carriage refusal, C-3's booking) were ruled by this seat's own traces of the tree, never by picking a reader.
**Arbiter's own instruments (all static + grep against source and installed dist; ZERO browser probes — probe parsimony, per the ColorSpaceSelector/easing-strip precedents)**: whole-file reads of `GradientPane.vue` (29 L), `GradientVisualizer.vue` (279 L), `useGradientModel.ts` (193 L); targeted reads of `App.vue` :70-140/:300-370 (mobile slot :77-93 carries NO `:on-mount`; desktop `:on-mount` at :105/:131 only; compound breakpoint :310-312; `ref<any>` trio :317-319; `onDesktopLeftMount` :323-328; `useClipboard({resetMs:2000})` :362), `usePaneRouter.ts` :40-50/:100-115/:188-212 (`handler: () => void` :46; `disabled?: boolean` exists at :45 and is passed by NO gradient action; `PaneActionRefs = Ref<any>` ×3; gradient actions :208-210 verbatim), `PaneSlot.vue` :52/:124 (`onMount?: (instance:any)=>void`; `:ref="onMount ? … : undefined"` — no callback, no ref, ever), `Dock.vue` (:41/:71/:132/:153/:157/:182-185 — `useMediaQuery("(min-width: 1024px)")` width-only; action-bar layer + toggle carry no desktop gate), `viewSchema.ts` :161-169 (gradient view first-class), `useDockAdminMode.ts` :26 (gradient in `userViews`), `PaneHeader.vue` :21/:40-57/:82-98/:105-124/:135-151/:177-194 (`.pane-scroll-fade` = exactly `contain` + `scroll-timeline`; `<h3>` title; veil mask is a TOP-of-pane feather on a −14px overhang), `GradientStopEditor.vue` :20-30/:60-75/:118-135 (emit contract; `selectedStop … ?? null`; `onHandlePointerDown` writes `selectedId` AND emits `select`), `useGradientCSS.ts` :188-193/:239-241 (`parseColorIn` on the coalesce path; two `if (!interval) throw` sites), `picker-color.ts` :100-116 (`parsePickerColor` THROWS `PickerColorError` on parse failure), `color-utils.ts` :8-16, `palettes/types.ts` :1-6 (`css: string`, unvalidated) + `usePalettePorts.ts` grep (`parseCssColor|validate` → **zero hits**), installed glass-ui 7.0.0 `dist/composables/dom/useClipboard.d.ts` whole (`CopyResult` discriminant; `useClipboard` status machine; the "rather than a lossy boolean" docstring verbatim), `GeneratePane.vue` :10 (the dead-inject twin), `MixPane.vue`/`GeneratePane.vue` description strings, repo censuses: `<h1` in demo/*.vue outside ui/ → **zero files**; `Seed from palette|Reset gradient|Copy CSS` in e2e/ → **zero hits** (gradient e2e exists — `o21-gradient-rail.spec.ts`, `views/gradient.spec.ts` — but never dispatches the three commands); `ls test/` → gradient tests are parser-level only. **The :87 dead-throw was re-proved by this seat's own induction, including the unsorted-list case the reader never considered** (`updateStop` does not re-sort, so unsorted lists are REACHABLE — but the first index `j` with `p_j > position` has `p_{j-1} ≤ position` by minimality, and that pair enters the loop body; line 87 stays unreachable even unsorted). **X wave ownership resolved from `docs/tranches/X/waves/` (W4.md:34/:83/:272; W5.md:33-46/:155-172; W6.md:47-62/:156-213; W10.md:73/:202) + `registry/CARRY-CUT-LEDGER.md`.**
**Motion/PRM law**: `motion-quarantine.md` rows for THIS corpus re-read at :124-130 and :267 — **M-33 FALSE** (the "bottom fade" is withdrawn and BARRED from carriage) · **M-34 TRUE, SURVIVING (MQ-1)** — the one live reduced-motion obligation in all 46 report-authored files, and this record carries it (reader-LC's refusal to carry is OVERRULED below).
**Status verbs**: every disposition here is **ADJUDICATED**, never VERIFIED — nothing in this file stamps a wave.

## Scoreboard

**6 axis rows CONFIRMED (2 MAJOR · 4 MINOR; two of them rescoped in place) · 8 kills SUSTAINED (every refutation re-derived by this seat) · 12 missed-by-the-axes findings ADOPTED (readers' union, all re-verified: 1 BLOCKER · 2 MAJOR · 6 MINOR · 3 INFO) · dispositions: X-W5.a ×2 · X.W4.d ×3 (two split) · X-W6 ×9 · X-W10 ×2 · X-W1 ×1 (split) · 5 UNPROVEN-NEEDS-LIVE residue rows → visual audit.**

Blocker-weight survivor: **M-1** — the pane's ENTIRE public command surface (Reset · Copy CSS · Seed from palette) is permanently inert on every phone and every portrait tablet: the dock renders three labelled, described, enabled controls whose dispatch ref is only ever captured by the DESKTOP pane slots. Statically decidable end to end; not one of the three axes left the 29-line file to find it, while axis C asserted a nearly identical harm (C-2) at the one place it provably cannot occur. The same chain kills `generate`'s and `mix`'s commands (App.vue:326/:331).

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-2** (silent command no-ops) | KILLED as located — expose is unconditional, mount is unconditional, the caller short-circuits one level up; phenomenon promoted to missed #1 | CONFIRMED "C-2a" (null-ref half, MAJOR) / KILLED "C-2b" (missing-method half) | **RESCOPED — folded INTO M-1; killed as located, phenomenon carried.** | Both readers agree on every byte; they differ only in bookkeeping. My reads: GradientVisualizer.vue:131 `defineExpose({resetGradient, copyCSS, seedFromPalette})` is unconditional over three plain function declarations (:110/:118/:127) — the method-absent limb can NEVER fire, and the trailing `?.` per method (GradientPane.vue:12-14) is dead defensive syntax that would make a future rename typecheck-clean and silent (LC's inversion adopted: the `?.` is the defect, not the guard). GradientPane.vue:25 mounts the child unconditionally, and the axis's three named windows (pre-mount / conditional removal / teardown) are unreachable from the sole caller — App.vue's capture nulls one level UP (:327), so usePaneRouter.ts:208-210 short-circuits before the pane's chain is ever consulted. The REAL null-ref harm is upstream and permanent-on-mobile — that is M-1's identity, and C-2 rides it as a limb rather than earning a second row (anti-double-booking). |
| **D-2** (commands "invisible… no availability, confirmation, or failure language") | KILLED on its operative harm (the dock supplies icon+title+description; the axis inferred absence from a 29-line file) | CONFIRMED (MINOR, as a pure code fact: the pane renders zero command affordances or status bindings) | **KILLED AS WRITTEN — DU wins; residue re-homed, not re-booked.** | The operative claim is "a parent can invoke work the user cannot see or understand." My read of usePaneRouter.ts:206-211: every command carries a Lucide icon, a `title`, and a full sentence `description`, rendered as the labelled Tools action-bar layer (Dock.vue:153/:157). The parent is not anonymous and the user is not blind — the axis never opened the router. What survives of D-2 is exactly two facts, both already owned elsewhere: no failure/confirmation language (= C-4's freight) and no availability state (= M-1/C-3's freight — `disabled?: boolean` exists at usePaneRouter.ts:45 and no gradient action passes it). LC's "confirmation" restates those owners; keeping it as a third row would book one fact twice. Bitter irony recorded: on mobile the labelled affordance IS the harm — three described buttons that can never fire (M-1). |
| **M-34 / MQ-1 carriage** (reduced-motion clause, challenge-D:33) | CONFIRMED (MINOR) — re-derived the scroll-scrub chain and the inert PRM clamp | "this corpus makes no motion claims … no M-xx row is implicated and none is carried" | **DU wins — LC's refusal is OVERRULED; MQ-1 is carried.** | The quarantine itself already ruled this: motion-quarantine.md:130 rules M-34 **TRUE** and :267 records "**M-34 SURVIVES** — its reduced-motion proof is genuinely owed (MQ-1)". LC mistook the closure-condition phrasing for a non-claim; the governing authority says otherwise, and the law bars carrying FALSE rows, not TRUE ones. Mechanism re-derived by this seat: GradientPane.vue:20 `.pane-scroll-fade` HOSTS the named timeline `--pane-scroll` (PaneHeader.vue:54-57) driving three scroll-driven animations (:177-194, incl. the title scale to `--pane-title-shrink-ratio` ≈0.618); the app's PRM guard clamps `animation-duration`, which is INERT against a scroll-progress timeline (quarantine probe3: identical `matrix(0.805…)` transform at scrollTop 200 under `reduce`). Fleet blast radius: 9 panes. |
| **C-3 booking** (seedFromPalette provenance) | CONFIRMED (MINOR) + a separate missed MAJOR ("dead button for every first-run user; label misdescribes") | CONFIRMED (MAJOR), absorbing the silent exits into the axis row | **LC's booking wins — ONE row, C-3, RESCOPED to MAJOR.** | Same surface, same function, same cure (GradientVisualizer.vue:110-116); the axis genuinely circled the argument-less signature — it under-located, it did not miss. Splitting signature-shape (MINOR) from the exits (MAJOR) would mint two names for one identity (anti-rename). C-3 absorbs the readers' sharpenings: three silent exits, the `[0]` pin, the `>= 2` gate, the never-disabled control, and the indefinite-article label lie. |
| **M-1 severity** | MAJOR | BLOCKER | **BLOCKER.** | Not one command, not one pane: the whole advertised imperative surface of three workbenches (gradient/generate/mix) is dead on the entire mobile + portrait-tablet breakpoint, silently, forever, on first-class routes (viewSchema.ts:161-169; useDockAdminMode.ts:26) — and the widened tablet band (M-5's predicate split) makes the surface larger than "phones". Same weight class as the mix record's MX-1. |
| **Test-coverage row** (LC missed #9) | — (not raised) | MINOR — "no test mounts GradientPane, no smoke spec exercises the gradient Tools bar" | **CONFIRMED WITH CORRECTION.** | LC's `ls test/` phrasing understated the e2e side: gradient e2e DOES exist (`o21-gradient-rail.spec.ts`, `views/gradient.spec.ts` — X-W6.a13 even counts their dead-DOM assertions). The surviving fact is my own grep: `Seed from palette|Reset gradient|Copy CSS` across e2e/ → ZERO hits — nothing anywhere dispatches the three commands from either layout, which is exactly the gap that let M-1 ship. |

## Adjudicated defect roster

Line anchors are HEAD `54a97489`; re-resolve before executing any cure (the D-19 lesson). Route legend: a wave id means that wave's bounds own the surface today (resolved from `docs/tranches/X/waves/*.md`); **NO-WAVE-OWNER** rows are booked for the next formation boundary.

- **M-1 (absorbs C-2) — ADJUDICATED BLOCKER → X-W5.a (primary) + X.W4.d (the typed dispatch seam).** The three gradient dock commands are guaranteed no-ops at the entire mobile/portrait-tablet breakpoint. Four-link chain, every link re-read by this seat: (1) the mobile `<PaneSlot>` (App.vue:77-93, `v-if="!isDesktop"`) passes NO `:on-mount`; only the desktop slots do (:105/:131). (2) PaneSlot.vue:124 `:ref="onMount ? (el) => onMount!(el) : undefined"` — absent callback, the dynamic component gets no ref at all, and `onMount` is optional (:52), so it fails silently. (3) `gradientPaneRef` (App.vue:318) has one writer, `onDesktopLeftMount` (:323-328) — never invoked below the desktop query `(min-width: 1024px) and (min-aspect-ratio: 1.1)` (:310-312) — so the ref is null for the whole mobile session. (4) The commands still render there: `actionBar` (usePaneRouter.ts:188-212) is computed from `currentView` alone; Dock.vue:153/:157/:182-185 render the layer and toggle with no desktop gate; handlers `paneRefs.gradient.value?.reset?.()` short-circuit on null. No error, no console output, no disabled state. Same chain kills `generate` (regenerate/save/copyColors) and `mix` (App.vue:326/:331). **The cure is already PROGRAMMED**: X-W5.a — "`bindPane` on all three `PaneSlot` call sites with `onMount` required and generic" — and X-W4.md:83 names "the mobile `:on-mount` gap — X-W5 unit a" explicitly. This row binds the adjudicated evidence to that programmed cure; the typed-receipt half rides X.W4.d (CC-043). C-2's identity is carried here as a limb: killed as located, real upstream.
- **C-4 (≡ D-2 residue) — ADJUDICATED MAJOR → split: in-pane consumption X-W6 · command-rail result semantics X.W4.d.** Copy denial is unrepresentable — but not for the axis's stated reason. Letter corrected by both readers and re-verified: GradientPane.vue:13 returns `Promise<void> | undefined`, not `void`. The real loss is GradientVisualizer.vue:127-129: `async function copyCSS() { await writeClipboard(coalescedCSS.value); }` DISCARDS the discriminated `CopyResult` the producer built deliberately — glass-ui `useClipboard.d.ts` declares `{ok:true} | {ok:false; reason: "clipboard-api"|"no-api"}` with the docstring "rather than a lossy boolean", plus a full `useClipboard` status machine (`idle|pending|success|failure`, `resetMs`, `timeoutMs` watchdog, `onCopyError`) that App.vue:362 ALREADY consumes for the share link. Copy is exposed twice (dock usePaneRouter.ts:209; in-pane GradientVisualizer.vue:254) and neither path can report failure — a denied clipboard (insecure context, permission) is indistinguishable from success. Cure = consumption of the shipped primitive, never a new rail (see the L-4 kill).
- **C-3 (rescoped, absorbs the readers' "dead button" find) — ADJUDICATED MAJOR → X-W6 (GradientVisualizer.vue owned; the disabled-state language rides X.W4.d).** `seedFromPalette` (GradientVisualizer.vue:110-116) is the whole implementation: `if (!pm) return` (silent exit 1 — `pm` is `inject(LIBRARY_PORT_KEY)` with no `!`, :30); `savedPalettes.value[0]?.colors` (silent exit 2 — an EMPTY library, the default state of every first-run user); `colors.length >= 2` (silent exit 3 — a saved single-color palette). No return value, no error, no state change, no record of which palette seeded. The source is hard-pinned to index `[0]`; the dock copy "Seed gradient stops from **a** saved palette" (usePaneRouter.ts:210) promises a selection the code never offers; the control is never disabled although `disabled?: boolean` sits unused in the very interface it rides (usePaneRouter.ts:45). Repeated calls against a reordered library are causally unauditable — the axis's original clause, true as far as it went.
- **M-2 — ADJUDICATED MAJOR → X-W6.a (the cure is structurally programmed: intervals array DELETED, easing hangs on the stop; `setStopsFromColors` becomes a total replacement).** `reset()` does not reset the gradient: `resetGradient` (GradientVisualizer.vue:118-125) restores stops/type/direction/space/hueMethod/verdict and never touches `intervals`; the only other writer is the length-keyed watcher (useGradientModel.ts:89-100), so a same-length reset (the default 2-stop case) keeps the user's authored easing verbatim, and a shrinking reset truncates to `[intervals[0]]` — the authored curve again. `seedFromPalette` growing 2→N pads with `linearInterval()` while retaining the old `intervals[0]`: a freshly seeded gradient with one bespoke interval and N−2 linear ones, no UI cue. The codebase's own ruling contradicts this — GradientVisualizer.vue:102-105 documents that a successful parse "re-seeds every interval to the `linear` preset (easing-disposition §1.6/D3)"; wholesale replacement via `setStopsFromColors` is the same class and must reseed identically. Sub-clause (reader-DU's limb, same identity): the reset literals hand-duplicate the model's seed (GradientVisualizer.vue:119-123 vs useGradientModel.ts:75-83) with nothing binding the two sites — the model exposes no `reset()`, so the duplication is forced by a composable-surface gap. One row, one cure home.
- **M-3 — ADJUDICATED MAJOR (structural; live reachability = residue U-4) → X-W6.a (programmed verbatim: "`setStopsFromColors` validated through the `parseCssColor` oracle").** Seeding feeds unvalidated persisted strings into a computed that THROWS during render: GradientVisualizer.vue:112 → `setStopsFromColors` (useGradientModel.ts:142-149, zero validation) → `coalescedCSS` (bound at :224) → useGradientCSS.ts:192-193 `parseColorIn` → picker-color.ts:109-113 `throw new PickerColorError("Invalid CSS color", …)`. `PaletteColor.css` is an unvalidated `string` (palettes/types.ts:1-6) from the persisted/remote store (my grep of usePalettePorts.ts: no parse/validate anywhere); no try/catch exists on the five-hop path; a throw in a render-path computed reaches App.vue's ErrorBoundary and replaces the pane subtree — killing `reset()`, the only route back, with it. Two further `throw` sites ride the same path (useGradientCSS.ts:189/:240-241). The correct posture already exists one function over: `applyCSS` returns `{ok:false, reason}` and mutates nothing (useGradientModel.ts:151-168). The parser is known to reject real inputs (the parser-proof gate's R1 `parseCssColor("oklch()")` shipping crash).
- **C-1 ≡ D-3 ≡ L-1 — ADJUDICATED MINOR → X-W6 (GradientPane.vue owned; the GeneratePane twin has no listed owner — rider, NO-WAVE-OWNER at the formation boundary).** `const cssColorOpaque = inject(CSS_COLOR_KEY)!` (GradientPane.vue:8) is the file's sole occurrence; the template (:18-29) never reads it. Not scaffolding — MixPane makes the identical injection and consumes it (:84), proving the token live and this copy rot; GeneratePane.vue:10 carries the same dead line (blast radius exactly two, my read). Invisible to tooling: `<script setup>` makes every top-level binding template-reachable, so no unused-local check can fire. The `!` asserts a contract the file then declines to use. All three axes found this — the corpus's one unanimous, correctly-located row.
- **C-5 (fade premise struck per M-33; the contract limb survives with the readers' mechanism) — ADJUDICATED MINOR → X-W6.a/.b (selection + focus grammar own the surface).** After `resetGradient` or `seedFromPalette`, `setStopsFromColors` MINTS FRESH IDS for every stop (useGradientModel.ts:144-148 `id: uid()`); neither function clears `selectedStopId` (GradientVisualizer.vue:51, never reset in either body); the stale id survives into GradientStopEditor's `selectedStop = stops.find(…) ?? null` — the user's active selection silently evaporates while focus stays on the dock control. No scroll-to-selection or focus-restoration contract exists (true as the axis wrote it); the mechanism the axis lacked is the id-minting.
- **M-4 (≡ L-3 rescoped — one identity: the typed seam erased one hop up) — ADJUDICATED MINOR → X.W4.d (CC-043 names it: "typed action-bar debt (`ref<any>` dispatch, two contracts)").** L-3's description is accurate (anonymous expose object, no declared handle/result/error type) but mislocated as a pane defect: GradientPane.vue:9 carries the ONLY real typing on the whole path (`ref<InstanceType<typeof GradientVisualizer> | null>`), erased at usePaneRouter.ts:108-111 (`PaneActionRefs = Ref<any>` ×3), App.vue:317-319 (`ref<any>`), PaneSlot.vue:52 (`(instance: any) => void`). Consequence: renaming or deleting `reset` — or, as today, never binding the ref at all — typechecks clean. This is why vue-tsc cannot see M-1. The prescribed cures at the pane end are killed separately (below); the fix belongs at the caller.
- **M-5 — ADJUDICATED MINOR → X-W5 (unit c: "delete every JS breakpoint layout fork").** Two disagreeing "desktop" predicates: App.vue:310-312 mounts panes on `(min-width: 1024px) and (min-aspect-ratio: 1.1)` under a comment claiming the JS and CSS "can never disagree", while Dock.vue:71 styles dock chrome on width-only `useMediaQuery("(min-width: 1024px)")`. A 1024px portrait tablet gets the MOBILE pane slot (no ref capture — M-1's widened band) under DESKTOP dock chrome. The in-code one-query guarantee has a third consumer it does not cover.
- **M-6 — ADJUDICATED MINOR → X-W5.a (the visible route `<h1>` is programmed there) + X.W6.b ("the flat heading outline corrected so the protagonist is named at instrument rank").** The semantic outline is flat and top-truncated: the pane title is `<h3>` (PaneHeader.vue:21; content "Gradient") and all three subsections are ALSO `<h3>` (GradientVisualizer.vue:149/:241/:253 — Interpolation/Easing/CSS); my census: NO `<h1>` anywhere in demo/*.vue outside ui/, and the few `<h2>`s sit nested inside `h3`-titled panes — an inverted outline (WCAG 1.3.1; axe `heading-order` + `page-has-heading-one`). The visual hierarchy is correct; only the semantics lie. Not one of the three axes contains the words "aria", "heading", or "landmark" — a total a11y blind spot over a subtree that is otherwise scrupulous about it (see superlatives).
- **M-7 — ADJUDICATED MINOR → X-W6 (GradientPane.vue owned; voice canon is X-W10 content — consumed, not minted, per W6 §5 model law).** The pane description (GradientPane.vue:21 "Build gradients with per-interval easing and CSS output.") is the voice break of the seven-pane set: (a) sole description whose object restates its own title under the same heading; (b) the `with` coordinates a manner phrase and a bare product noun, parsing as "build … with CSS output" when CSS is the OUTPUT; (c) "interval" is producer jargon (useGradientModel.ts:83) appearing in zero user-facing strings — the visible headings say Interpolation/Easing/CSS. Sibling strings re-read by this seat (MixPane.vue:75, GeneratePane.vue:32) and consistent with reader-DU's full census. Recorded with the irony that challenge-D:13 PRAISED this string — the design axis inverted its one available prose finding.
- **M-8 — ADJUDICATED MINOR → X-W6.a (dies with the one-owner selection write; adjacent to gate a8's one-gesture-one-write law).** Selection is written twice through two redundant channels: GradientStopEditor.vue:129-130 does `selectedId.value = id` AND `emit("select", id)`; GradientVisualizer subscribes to both (:140 `v-model:selected-id` + :144 `@select` performing the identical assignment). The `select` emit has exactly one listener doing what the model binding already did — dead API, two writes per click, no stated ordering.
- **M-9 — ADJUDICATED MINOR → X-W1 (oracle liveness + the mandatory falsifier, CC-031) + X-W5.a (the `bindPane` gates are the natural home for a both-layouts dispatch assertion).** Zero coverage of the command surface: unit tests are parser-level (`gradient-parse`, `gradient-v4-consume`); gradient e2e exists but — my grep — nothing in e2e/ ever dispatches Reset/Copy CSS/Seed from palette from either layout. The exact gap that let M-1 ship. (LC's row adopted with the correction recorded in the ruled-disagreements table.)
- **M-10 — ADJUDICATED INFO → X-W6 (rides any GradientVisualizer touch; a plain `ref` says the same thing honestly).** `GradientVisualizer` publishes a two-way `selectedStopId` model (:51, defaulted, threaded to the stop editor) that its one and only mount site ignores (GradientPane.vue:25 bare `<GradientVisualizer ref="visualizerRef" />`; :5 is the sole importer, both readers' greps agree). Contract surface leaked on a private single-consumer component; harmless today; it also falsifies L-3's implicit claim that the child's surface is three functions — it is three functions plus a model.
- **M-11 — ADJUDICATED INFO → X-W6 (one-line hygiene riding any GradientPane touch).** Three of five utilities on the outer wrapper are inert or duplicated in a 29-line file: `mx-auto` is a no-op (`w-full`, no `max-w-*` — the constraint lives on the ancestor slot); `w-full`/`h-full`/`min-w-0` restated verbatim on the Card one line down; `relative` is copy-paste residue from MixPane's canvas-positioning need — GradientPane has no absolutely-positioned child resolving against it (the header veil resolves against sticky `.pane-header` itself, PaneHeader.vue:82-98).
- **M-12 — ADJUDICATED INFO (recorded, not charged) → X-W10 (a fleet-level register question, not a wb-gradient-pane defect).** The in-pane Copy CSS control (GradientVisualizer.vue:254-256) is labelled only by native `title` — an accessible name via HTML-AAM (screen readers fine, NOT a `button-name` failure) but no tooltip on touch, leaving a bare glyph on exactly the breakpoint where the dock copy is dead (M-1). It follows the demo's established in-pane convention (MixResultDisplay, ExtractControls), and the "native title retired" law is scoped dock-wide, not pane-wide — reader-DU raised the conformance charge and killed it on that evidence; this seat sustains the kill and records the register question.

## Killed-claims register (every refutation re-derived by this seat)

1. **D-4 / C-5's fade premise — KILLED and BARRED (motion-quarantine.md M-33, FALSE; withdrawal recorded at :267).** There is no bottom fade. `.pane-scroll-fade` (GradientPane.vue:20) is defined once, unscoped, at PaneHeader.vue:54-57 and sets exactly two properties: `contain: layout style paint` and `scroll-timeline: --pane-scroll block`. The only mask in the file (PaneHeader.vue:91-96) feathers the header veil's −14px BOTTOM OVERHANG downward into the plate — a top-of-pane device at the opposite end of the surface. The quarantine's live probe measured `mask-image: none` on the scroll host. Both axes reasoned from the class NAME — a misnomer surviving a retired mechanism — and never opened the rule. Every consequence derived from the fade is void; the safe-area/keyboard clause sharing C:15/D:16 is unrefuted but unproven → U-1.
2. **C-2 as located — KILLED (rescoped into M-1; ruled above).** (a) `defineExpose` is unconditional — the method limb can never short-circuit; (b) the child mounts unconditionally — the ref limb cannot fail within a mounted pane; (c) the axis's teardown window is intercepted one level up (App.vue:327 nulls first). The trailing `?.` chains are not defensive depth, they are silencers-in-waiting. Phenomenon real, upstream, far worse than claimed.
3. **D-2 as written — KILLED (ruled above).** The parent is not anonymous: usePaneRouter.ts:206-211 gives all three commands icon + title + full description, rendered as the labelled Tools layer. Inferred absence from a 29-line file; the surviving residue (failure language, availability state) is owned by C-4 and C-3/M-1 respectively, not re-booked.
4. **L-2 — KILLED as self-nullifying.** The finding concedes "that is fine inside the demo" and contradicts its own axis's prescription eleven lines later ("Keep `GradientVisualizer` private"): a component cannot be both private and reached through an inversion port. The live structure already delivers the asked-for privacy by directory (GradientPane.vue:5 imports a child directory with no other importer — both readers' greps, spot-confirmed by this seat). A typed port over a 29-line shell's single private consumer is the exact contrivance the standing KISS edict bans.
5. **L-4 + L:28 ("introduce a shared workbench command/result rail") — KILLED AS PRESCRIBED.** The rail already ships twice: glass-ui `useClipboard` owns the complete success/failure/pending semantics (status machine + watchdog + `onCopyError`, my d.ts read — verbatim the semantics the axis says must be introduced), and the demo already ships result presentation (ActionFeedback chip, per reader-DU, with its own recorded `aria-live` gap). The defect is CONSUMPTION (C-4), not absence; building the third rail would re-implement a shipped producer surface against the glass-ui-first law.
6. **C:19-23 verification contract as prescribed (typed receipts, `UNAVAILABLE` returns, `GradientPaneHandle`, `GradientSession`) — KILLED at the prescribed end of the wire.** Every receipt the pane could return is discarded upstream by construction: `handler: () => void` (usePaneRouter.ts:46), `Ref<any>` (:108-111), `ref<any>` (App.vue:317-319), `(instance: any) => void` (PaneSlot.vue:52). Three new abstractions on the 29-line wrapper would produce output thrown away one hop later while leaving M-1 untouched. Ordered cure ruled: bind the ref (X-W5.a), de-`any` the caller (X.W4.d), and only then is a typed receipt observable. Per the ColorSpaceSelector addendum item 2: the cure is adjudicated separately from the defect — C-4's defect stands; C's cure does not.
7. **L import-table row on `../../ui/card` ("hides the producer package/subpath") — KILLED.** demo/ui/card/index.ts is one line naming the producer in full (`export { Card, … } from "@mkbabb/glass-ui"`). A one-line re-export that names its producer is a seam, not a concealment — and the demo/ui layer's fate is X-W8's own row, not this component's.
8. **D state table rows `initializing` + `failure` (and the `empty` premise) as closure conditions — KILLED as unearnable.** The gradient model is synchronous and local end to end: zero `async|await|Promise|fetch|setTimeout` across all three composables (readers' grep, consistent with my whole-file read of useGradientModel.ts); stops are born inline (:79-82); `applyCSS` returns a synchronous discriminant already SURFACED (`parseVerdict` → GradientCodeEditor :258-262). No interval exists in which an initializing skeleton could show; no operation can fail into diagnosis-and-retry — except clipboard denial, which the table omits and C-4 owns. The `empty/unseeded` row's premise is unreachable: the model births two stops and `removeStop` floors at 2 (useGradientModel.ts:122-125). Three "delegated" rows are correct architecture for a composition shell. The blanket SOURCE-RED at D:9 rests substantially on this table; what survives of it is one earned row (copy success = C-4) and one honest unknown (mobile keyboard = U-1).

## Superlatives (provenance: reader-DU unless marked; re-verified by this seat where its own instruments reached)

1. **The render tile is the model a11y-and-material citizen** (re-verified): `role="img"` + real `aria-label` + `data-testid` on a surface that could have been a decorative div (GradientVisualizer.vue:219-225); paint stack owned in one scoped rule stating its law — "Never a per-callsite `background` shorthand assembly" (:266-278); and the tile exists to give the direction slider an honest surface (:151-156 names the defect it cured).
2. **Every valueless control in the subtree is named** (re-verified): `aria-label` on Gradient type :165, Interpolation space :182, Hue interpolation :199, Direction slider :232 — four for four, which makes the heading-order lapse (M-6) an isolated miss, not neglect.
3. **`applyCSS` is a model reference point** (reader-LC; re-verified): atomic apply-or-nothing with a surfaced `{ok, reason}` verdict (useGradientModel.ts:151-168), the header comment naming the exact desync defect it replaced — precisely the typed-receipt discipline challenge-C demands at :21, already shipped one layer below the file the axis audited, unnoticed by it.
4. **One sampling law, two consumers** (both readers; re-verified): `colorAtPosition` feeds the stop editor's add-ghost AND the color a bar-click mints (:139, :90-92), so preview and committed value cannot diverge; the comment records the regression that motivated it ("the old fixed-teal insert is dead; an added stop is invisible until moved").
5. **The closed-form type ratio** (re-verified at PaneHeader.vue:135-151): the scroll-shrink endpoint derived as `tan(atan2(heading, display-1))` so the law survives the viewport-fluid clamp at every band, degenerating to exactly 1 where the rungs coincide — a hand-tuned constant would have been correct at one viewport. `@supports`-gated to its consumer's engine set.
6. **The engine-invariant rest state** (re-verified): every scroll-driven animation lives inside `@supports (animation-timeline: scroll())` with from-state = base-state (PaneHeader.vue:177-194), so non-supporting engines paint the identical designed rest header. The right progressive-enhancement shape; MQ-1 is a gap in an otherwise carefully reasoned motion design.
7. **Contrast reasoned against the real composited ground** (PaneHeader.vue:118-124, re-verified): the caption routes to `--ink-muted` with the measurement recorded inline — raw `--muted-foreground` measured 4.29:1 on the TRUE header ground, veil included — refusing the standard nominal-token failure, for all 9 panes in one class list.
8. **Disciplined CSS locality, codified** (re-verified): `.pane-scroll-fade` colocated unscoped in PaneHeader.vue with its justification written down (:41-53), under demo/DESIGN.md's no-new-global-utility law. The one cost is the stale NAME — exactly what fooled challenge-D:16 into asserting a fade that does not exist (kill #1).
9. **Producer-grade error types already wait at this boundary** (both readers; d.ts re-read whole by this seat): `writeClipboard`'s discriminated result "rather than a lossy boolean" and `useClipboard`'s watchdog for platforms that leave `writeText` pending forever. The design-system layer did the unglamorous work correctly; C-4 is purely a consumer declining the gift.
10. **The model guards its own shape** (reader-LC; re-verified): `removeStop` floors at 2, `addStop` re-sorts on insert, the watcher maintains `intervals.length === stops.length − 1` both directions — stated, centralized, one home. The recorded residues (length-keyed watcher blind to same-length replacement = M-2; `updateStop` does not re-sort = X-W6.a's own row) are gaps IN the invariant, not absence of one.

## UNPROVEN-NEEDS-LIVE residue → visual-audit inputs

- **U-1 — Mobile keyboard obscuration / safe-area behaviour** (challenge-D:16/:29, the surviving limb of kill #1). No `env(safe-area-inset-*)`, no `dvh/svh` in the pane; the ancestor chain resolves against a viewport height a static read cannot measure. Whether the CSS section and code editor stay reachable with a soft keyboard raised needs a device/emulation. The axis's one honestly-open row.
- **U-2 — MQ-1 perceptual severity.** The PRM contract violation is proven and probe-measured (title scrubs to ≈0.618 under `reduce`); whether the shipped scale induces discomfort is a design judgment this seat declines to assert from source. The cure is programmed regardless (X-W10, W10.md:202).
- **U-3 — Focus-visible rendering and tab reachability of the stop handles and easing tiles** (challenge-D:25). Handles are pointer-driven with capture (GradientStopEditor.vue:118-135); no `:focus-visible`/`tabindex` found in the subtree — but keyboard reachability needs live traversal. X-W6.a6/b1 gates already target this; the visual audit should witness the RED.
- **U-4 — Live reachability of the render-throw (M-3).** The unguarded five-hop structure is proven statically; whether any string currently persisted in real saved palettes is rejected by `parseCssColor` needs the running app and real data. The structural defect stands regardless.
- **U-5 — A pixel witness of M-1 on a real phone viewport.** The chain is statically complete (no desktop gate exists on the layer, toggle, or actions); one committed capture of the Tools bar open at 390×844 with a dead press closes the last inferential inch and becomes the wave's BEFORE frame.

## Closing verdict

**SOURCE-RED SUSTAINED — but re-grounded almost entirely off-axis.** Of the three axes' twelve substantive claims, six survive (two rescoped, one with its letter corrected, one absorbed into an upstream identity) and eight are killed — including every prescription — while the twelve adopted reader findings carry the corpus's only BLOCKER and both remaining MAJORs, all living in files the axes never opened. The 29-line pane is a nearly blameless composition shell over a genuinely excellent subtree; its real defects are one dead inject, one voice break, and a command facade whose three verbs are severed on mobile (X-W5.a), untyped above it (X.W4.d), lossy inside it (C-4 → X-W6), lying about reset (M-2 → X-W6.a), and able to detonate the pane through an unvalidated seed (M-3 → X-W6.a). Every terminal disposition lands on a wave whose bounds own the surface today; the sole NO-WAVE-OWNER residue is the GeneratePane dead-inject twin, booked for the next formation boundary. Dispositions are ADJUDICATED, not VERIFIED; the five U-rows are this component's visual-audit slate.

— end of apotheosis. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
