# wb-gradient-pane — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator (the incredulity seat) · **Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u`.
**Corpus**: three challenge documents (`audit/components/wb-gradient-pane/challenge-{C-implementation,D-design,L-library}.md`; no r2 variants exist in the directory, so these ARE the governing corpus; all three receipt the same 29-line SHA `193b938e…`) against `demo/workbenches/gradient/GradientPane.vue` + its child (`GradientVisualizer.vue`), its shell channel (`usePaneRouter.ts` / `App.vue` / `PaneSlot.vue`), and the installed glass-ui 7.0.0 clipboard producer.
**Re-readers adjudicated**: reader-DU (design-and-usage) · reader-LC (library-and-code). Both were attacked before either was adopted; every kill was re-derived by this seat with its own instruments; the one head-on verdict conflict (C-2: DU CONFIRMED-narrowed vs LC KILLED) was ruled by my own trace, not by picking a reader.
**Arbiter's own instruments (all static + grep against source, installed dist, and the dated evidence corpus; ZERO browser probes — probe parsimony)**: whole-file read of `GradientPane.vue` (29 L, matches the corpus SHA receipt by content); `GradientVisualizer.vue:1-160` + `:232-262` (inject posture `:30`, dead `defineModel` `:51`, `colorAtPosition` `:64-88`, `parseVerdict` `:98-108`, `seedFromPalette` `:110-116`, `resetGradient` `:118-125`, `copyCSS` `:127-129`, `defineExpose` `:131`, unconditional child at the pane's `:25`, Easing gate `:239`, in-pane `DockControl title="Copy CSS"` `:253-257`, `:parse-verdict` binding `:259-261`); `usePaneRouter.ts:38-58` (`DockAction.disabled?` present), `:69-78` (ten bare `defineAsyncComponent`s, no loading/error component), `:107-111` (`gradient: Ref<any>`), `:196` + `:203-213` + `:219` (action registrations; `rotateOnClick` on both Reset and Regenerate; gradient actions never set `disabled`); `App.vue:36-43` (Dock outside the breakpoint fork, `:generic-action-bar` fed), `:77-110` (mobile PaneSlot passes NO `:on-mount`; desktop-left passes `onDesktopLeftMount`), `:271` (`provide(CSS_COLOR_KEY, cssColorOpaque)`), `:310-330` (`isDesktop` compound query; `gradientPaneRef = ref<any>(null)` written only in `onDesktopLeftMount`), `:351` (`usePaletteWiring` → `providePalettePorts` → `provide(LIBRARY_PORT_KEY)` at `usePalettePorts.ts:243` — **app-level, every route**), `:362` (`useClipboard({resetMs:2000})` share-link idiom); `PaneSlot.vue:40-60` + `:80-130` (rAF-deferred commit; per-render inline function `:ref`); `Dock.vue` (GenericActionBar at `:157` ungated by `isDesktop`; `:71-72` desktop query drives only `always-expanded`/edit); `GenericActionBar.vue:27` `:disabled="act.disabled"` → `ActionButton.vue:17-18` `disabled` + `aria-disabled` (the interim cure is fully plumbed); `useGradientModel.ts:79-86` (born with two stops), `:122-125` (`removeStop` refuses <2), `:142-168` (`setStopsFromColors` mints fresh `uid()` per stop; `applyCSS` atomic with the verdict comment); `gradientParse.ts:44-49` (`GradientParseResult` discriminated) + `:261` (`stops.length < 2` reject); `GradientStopEditor.vue:26` (`selectedId` defineModel), `:61-68` (`selectedStop = find ?? null`), `:129-185` (selection writes), `:205-240` (add ghost `aria-hidden`; real `<button type="button">` handles with computed positional `aria-label`), `:277-295` (remove chip `v-if="selectedStop && removable"`), `:317` (`.gradient-rail` owned contract); `GradientEasingEditor.vue:29/:94-103` (sibling `useClipboard` self-clearing confirmation); installed `@mkbabb/glass-ui/dist/composables/dom/useClipboard.d.ts:1-40` (`CopyResult` discriminated union; `writeClipboard` docstring prescribing `const { ok } = await writeClipboard(text)`; four-state `status`); `eslint.config.js:71/:82` (`@typescript-eslint/no-unused-vars` AND `vue/no-unused-vars` both off); `keys.ts:8` (typed `InjectionKey`); censuses: `cssColorOpaque` in panes (MixPane `:15+:84` live, BrowsePane `:98+:201` live, GeneratePane `:10` dead, GradientPane `:8` dead), `inject(CSS_COLOR_KEY` = 9 files, `<h1` in demo = **0 files**, `<h2` = {AboutPane, ColorNutritionLabel} only, `<h3` in demo/workbenches = GradientVisualizer.vue alone (+ PaneHeader's own `<h3` title at `:21`), `selectedStopId` identifier = GradientVisualizer.vue only (`:51/:140/:144`), `GradientVisualizer.vue` importer = GradientPane.vue:5 alone, `GradientPane|seedFromPalette|resetGradient|"Seed from palette"` across test/ + e2e/ = **zero hits**; `motion-quarantine.md:124-125` (M-33 FALSE / M-34 TRUE) + `:267` (fade withdrawn, M-34 survives); `PaneHeader.vue:21/:41-57/:177-215` + `animations.css:184-193` (duration-clamp guard, inert vs `animation-timeline`); dated cross-corpus evidence read: `audit/components/App/challenge-C-implementation.md:148-201` (App C-2, measured mobile probe) and `audit/components/shell-paneslot/challenge-C-implementation.md:36-133` (PS-1 + PS-probe4 timeline, `gradientPaneRef := ColorPicker` @t=2935 / `:= GradientPane` @t=4210); X wave ownership from `docs/tranches/X/waves/`: W6 file list carries `GradientPane.vue` + the whole gradient tree; W5 carries `App.vue`/`PaneSlot.vue`/`usePaneRouter.ts` with the `bindPane`/`onMount(instance, liveKey)` mechanism named at `:35/:161-163`; W8 carries `GradientPane.vue` modify-carve with **G-10 = the `:8` inject deletion named at `W8.md:127/:188/:330`**; W10 item 5 + `:199-209` books the PaneHeader scroll-timeline PRM cure (CC-099, G-4 probe).
**Motion/PRM law**: M-33 (**FALSE** — "there is no bottom fade") may not be carried and is not: every fade-dependent limb in the corpus dies with it. M-34 (**TRUE** — the one surviving reduced-motion obligation across all 46 files) is carried, re-derived by this seat against source: `.pane-scroll-fade` (GradientPane.vue:20 → PaneHeader.vue:54-57) hosts the named `--pane-scroll` timeline driving three scroll-driven animations (`PaneHeader.vue:177-194`, incl. a real `transform: scale()` title shrink) which the duration-clamp guard cannot touch.
**Status verbs**: every disposition here is **ADJUDICATED**, never VERIFIED — nothing in this file stamps a wave.

## Scoreboard

**7 axis rows CONFIRMED/RESCOPED (0 BLOCKER · 4 MAJOR · 3 MINOR) · 9 kills SUSTAINED (every one re-derived by this seat) · 1 verdict-level conflict RESCOPED (C-2) · 9 missed-by-the-axes findings ADOPTED (readers' union, all re-verified; 1 BLOCKER + 1 MAJOR cross-booked to their owning corpora, never double-minted) · 1 reader-unproven row RESOLVED by this seat (the LIBRARY_PORT provider trace) · dispositions: X-W6 ×7 · X-W5 ×5 · X-W8 ×3 · X-W10 ×1 · X-W1 rider ×1 · 5 UNPROVEN-NEEDS-LIVE residue rows → visual audit.**

Blocker-weight fact (cross-booked, not re-minted): **every command this pane exposes is dead on mobile** — the dock renders Reset / Copy CSS / Seed-from-palette as named, described, enabled controls at every phone viewport, and each dispatches onto a ref that is written only by the desktop-left mount callback. Measured at `audit/components/App/challenge-C-implementation.md:148-201`; the identity lives there (App C-2) and in shell-paneslot PS-1; this record carries the limb.

The verdict-shape correction this adjudication exists to make: all three axes stamped the 29-line file **SOURCE-RED**, and all three were wrong about *where* the red is. The file itself is close to an ideal thin composition boundary (typed child ref, five imports, zero business logic). Every live defect sits one level down (the visualizer's discarded `CopyResult`, silent seed, dangling selection) or one level up (the shell's untyped, mobile-unwired, async-raced ref channel; the PRM-immune header motion). SOURCE-RED is sustained **for the product surface the file fronts**, not for the file.

## Ruled disagreements (this seat's actual work)

| id | reader-DU | reader-LC | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-2** | CONFIRMED, narrowed MINOR — "between view-switch and chunk resolution `paneRefs.gradient.value` is null … clicks are swallowed by the outer `?.`" | KILLED — "the named mechanism cannot fire … the inner `?.` is unreachable dead defensive code"; substance re-derived one level up as missed rows | **RESCOPED — LC's kill of the axis's mechanism SUSTAINED; DU's outer-channel substance CONFIRMED under C-2's id, corrected by the measured timeline.** | The axis's named mechanism is dead by construction, verified: `GradientVisualizer` is a static import (`GradientPane.vue:5`), unconditional in the template (`:25`, no v-if), and `defineExpose({ resetGradient, copyCSS, seedFromPalette })` (`GradientVisualizer.vue:131`) matches all three forwarded names — if `visualizerRef.value` is non-null all three methods exist, so the inner `?.` chains at `:12-14` can never be the swallow site. The REAL swallow site is the shell hop: `paneRefs.gradient.value?.reset?.()` (`usePaneRouter.ts:208`). DU said "null during the window"; the measured PS-probe4 timeline (shell-paneslot corpus `:115-133`) shows it holds a **foreign instance** (`ColorPicker`, then `GeneratePane`) for most of a 1275 ms window on `/#/gradient`, with null only in sub-windows — either way the second `?.` swallows. Both readers are right about the facts and wrong about the row: one row survives, at the outer seam, MINOR on desktop (bounded window; interim cure — the `DockAction.disabled` bit — verified fully plumbed `GenericActionBar.vue:27` → `ActionButton.vue:17-18`), with the permanent mobile limb cross-booked (below). |
| **D-33 / M-34 severity** | MINOR | MAJOR (probe3 measured: `matrix(0.805831,…)` byte-identical under `reduce` and `no-preference`; guard fired and powerless; 9 sibling panes) | **MAJOR — LC wins.** | My own derivation concurs with the quarantine's: the app guard (`animations.css:184-193`) clamps `animation-duration` only; `PaneHeader.vue:177-194`'s three animations take progress from `animation-timeline: --pane-scroll`, not the clock — the clamp is structurally inert, and one of the three is a genuine spatial `transform: scale()` on the title. A measured, guard-immune, vestibular-relevant transform on 9 of 9 panes is not MINOR. Scope law (ColorSpaceSelector addendum 1): the count lives fleet-wide → the identity is fleet-wide and **already owned**: X-W10 item 5 + `:199-209` books exactly this cure (move the declarations inside `@media (prefers-reduced-motion: no-preference)`; CC-099; the `prm-scroll-timeline.mjs` G-4 probe). This record carries the GradientPane limb and must not re-mint the wave. |
| **D-table "initializing" row** | filed as missed INFO ("the concrete substance behind D's unproven row — which the axis asserted without ever locating the async registration") | CONFIRMED MINOR ("the claim is accurate, though it is a shell-owned gap") | **CONFIRMED MINOR — one identity, LC's booking; DU's provenance note preserved.** | My read of `usePaneRouter.ts:69-78`: all ten panes are bare `defineAsyncComponent(() => import(…))` — no `loadingComponent`, `errorComponent`, `delay`, or `timeout`; no Suspense in demo/shell. The axis's cell is true; the axis never earned it (it cited no registration site) — recorded as a method fact, not a reason to strike a true row. Fleet-wide → shell-owned → X-W5. DU's "silent blank on failed chunk fetch" limb stays partially UNPROVEN (whether a rejected chunk propagates to the App-level ErrorBoundary is a Vue-runtime behaviour — residue U-4). |
| **C-3 vs DU's unproven #5** (is `pm` ever genuinely undefined?) | left UNPROVEN ("did not trace every provider ancestor") | asserted three bail paths without resolving the provider | **RESOLVED by this seat — the `!pm` limb is dead in production; C-3 is confirmed on the other two limbs.** | My trace: `App.vue:351` calls `usePaletteWiring` → `providePalettePorts` (`usePaletteWiring.ts:60`) → `provide(LIBRARY_PORT_KEY, libraryPort)` (`usePalettePorts.ts:243`) — app-setup-level, so the port exists on every route that can render this pane. The LIVE silent no-ops in `seedFromPalette` (`GradientVisualizer.vue:110-116`) are (a) zero saved palettes — the first-run state — and (b) `savedPalettes.value[0]` holding fewer than 2 colors; plus the un-chosen hardcoded FIRST palette. The dock button stays enabled and labelled throughout. |
| **Heading hierarchy grade** (DU missed #2; LC silent) | MAJOR | — | **MAJOR SUSTAINED, with the fleet-outline arm split off.** | My censuses: PaneHeader renders every pane title as `<h3>` (`:21`); GradientVisualizer's three section heads are ALSO `<h3>` (`:149/:241/:253` region — verified at `:239-253`); `<h1>` = 0 files in demo; `<h2>` only in scenes/about; `<h3>` in demo/workbenches = this one file. So on `#/gradient` the rotor lists four peer h3s — "Gradient" does not scope its own sections (WCAG 1.3.1/2.4.6). The local fix (h4 in GradientVisualizer, a W6 file) is severable from the fleet's missing h1/h2 outline, which is a fleet identity for the next formation — booked separately so the local row is not held hostage to fleet churn. |

## Adjudicated defect roster

Line anchors are HEAD `9c779130`; re-resolve before executing any cure (the D-19 lesson). Cross-booked rows carry their owning corpus id and are counted here as limbs, never as second mintings.

- **C-1 / D-3 / L-1 — ADJUDICATED MINOR → X-W8 (already booked: X.W8.d gate G-10 names `GradientPane.vue:8` by line; sequenced after X-W6 per W8's cross-wave clause).** `inject(CSS_COLOR_KEY)!` assigns `cssColorOpaque`, never read — sole occurrence in the 29-line file (my read). Provider exists (`App.vue:271`), so zero runtime symptom — pure dead boundary declaration behind a `!`. Paste provenance re-verified: MixPane `:15+:84` and BrowsePane `:201+:98` genuinely consume theirs; GeneratePane `:10` and this file are the two dead copies. Why gates missed it: `@typescript-eslint/no-unused-vars` off (`eslint.config.js:71`) and `vue/no-unused-vars` off (`:82`), and vue-tsc cannot flag script-setup top-level bindings (template-exposed by construction) — both readers' account, re-verified. L-1's "undocumented" clause is KILLED separately (below); the deadness stands alone.
- **C-2 (RESCOPED, ruled above) — ADJUDICATED MINOR (desktop limb) → X-W5.** During the async-chunk window after switching to `/#/gradient`, the dock renders three enabled, labelled gradient commands that dispatch into `paneRefs.gradient.value?.…?.()` while the ref holds null or a foreign instance (measured 1275 ms, shell-paneslot PS-probe4) — silent no-op, no receipt. Interim cure already plumbed and unused: `DockAction.disabled?` (`usePaneRouter.ts:52`) → `GenericActionBar.vue:27` → `ActionButton.vue:17-18` (`disabled` + `aria-disabled`); structural cure is X-W5's own named mechanism (`PaneSlot.onMount` required + generic, invoked `(instance, liveKey)`; `usePaneRouter.bindPane` owning the refs — `W5.md:35/:161-163`). The axis's inner-`?.` mechanism is REFUTED (dead defence; child static + unconditional + expose-names exact).
- **C-3 — ADJUDICATED MAJOR → X-W6 (GradientVisualizer.vue in W6's file list).** "Seed from palette" is a labelled, described, permanently-enabled dock command (`usePaneRouter.ts:210`) whose implementation (`GradientVisualizer.vue:110-116`) hardcodes `savedPalettes.value[0]` (the user never chooses a source, against the dock's own copy) and silently no-ops on the first-run zero-palettes state and on a <2-color first palette — no message, no receipt, no state change; the forwarder cannot distinguish that from success. The `!pm` limb is DEAD (this seat's provider trace, ruled above) — the cure needs no availability protocol, only a chosen source and a surfaced outcome. Contrast discipline in the SAME file: the parse path carries `parseVerdict` (`:100-108`) — the receipt idiom exists locally and was not applied here.
- **C-4 / L-4 (+ DU's missed copy-feedback row FOLDED — same identity, sharpest statement) — ADJUDICATED MAJOR → X-W6.** The pane's headline affordance — Copy CSS, from BOTH the dock action (`usePaneRouter.ts:209`) and the in-pane control (`GradientVisualizer.vue:253-257`, verified) — discards the producer's typed receipt: `copyCSS` (`:127-129`) awaits `writeClipboard(coalescedCSS.value)` and drops the `Promise<CopyResult>`, whose own docstring prescribes `const { ok } = await writeClipboard(text)` (my d.ts read, `:31-37`). The promise resolves `{ok:false, reason:"clipboard-api"|"no-api"}` — it never rejects — so failure (e.g. any non-secure-context load: the LAN mobile-test host the repo supports via `server.host: true`) is total silence, and success shows nothing either. The correct idiom ships one file away (`GradientEasingEditor.vue:94-103`: `useClipboard({resetMs:1400})` + a status-derived, self-clearing tick — verified) and at app scope (`App.vue:362`). This is non-adoption of an existing shared primitive; the axes' "typed receipt architecture" cure is KILLED below — the fix is a destructure plus surfaced state.
- **L-3 — ADJUDICATED MAJOR → X-W5 (the typed `bindPane`/`onMount(instance, liveKey)` rewrite is W5's own named mechanism), with an X-W6 rider to type the pane's exposed surface.** The command channel is `any` at every shell hop — `PaneActionRefs { gradient: Ref<any> }` (`usePaneRouter.ts:110`), `gradientPaneRef = ref<any>(null)` (`App.vue:318` region, verified), `onMount?: (instance: any) => void` (`PaneSlot.vue:52`) — so renaming any exposed key type-checks clean, lints clean (both unused-vars rules off), tests clean (zero coverage — see below), and silently converts a dock command into a no-op. **Correction to all three axes**: the pane's OWN ref is properly typed (`ref<InstanceType<typeof GradientVisualizer> | null>`, `GradientPane.vue:9`) — the untyped hops are shell-owned; SOURCE-RED at this file was mis-addressed.
- **D-33 / M-34 — ADJUDICATED MAJOR (grade ruled above) → X-W10 (CC-099 already books the cure; this row must not re-mint it).** GradientPane's Card carries `.pane-scroll-fade` (`:20`), making it a host of the named `--pane-scroll` timeline whose three scroll-driven animations — including the real `scale()` title shrink — survive `prefers-reduced-motion: reduce` byte-identically (quarantine probe3, measured; mechanism re-derived by this seat: the duration clamp is inert against `animation-timeline`). Fleet scope: 9 panes. The rendered vestibular-harm threshold stays residue U-3.
- **D-table "initializing" (+ DU's async-fallback missed row FOLDED — one identity, ruled above) — ADJUDICATED MINOR → X-W5.** Ten bare `defineAsyncComponent`s (`usePaneRouter.ts:69-78`), no loading/error component anywhere in the shell: slow chunk = empty slot with no skeleton holding visualizer geometry; failed chunk = at best an ErrorBoundary catch (unproven, U-4), at worst a permanent silent blank. Compounds C-2's window (the dock is enabled while the slot is empty). Fleet-wide, shell-owned.

### Adopted missed findings (readers' union, each re-verified by this seat)

- **M-GP-1 (LC) — all three exposed commands are DEAD ON MOBILE — BLOCKER, CROSS-BOOKED → X-W5 (identity = App corpus C-2 + shell-paneslot PS-1; measured there, `dockActionWorked FALSE` at mobile-390).** Premises independently re-verified on today's tree: mobile PaneSlot (`App.vue:83-91`) passes no `:on-mount`; `gradientPaneRef` is written only in `onDesktopLeftMount` (`:323-328`); the branches are mutually exclusive (`v-if="!isDesktop"` `:77` / `v-else` `:94`); the Dock renders OUTSIDE the fork (`:36-43`) and `GenericActionBar` is ungated by `isDesktop` (`Dock.vue:157`, my read — desktop drives only `always-expanded` and the edit bar). So at every phone/portrait-tablet viewport the Tools layer offers three named commands that evaluate `null?.reset?.()` forever. The single most important fact about this pane's public surface, and none of its three axes left the 29-line file to find it. Counted here as a limb; the booking lives at App/X-W5.
- **M-GP-2 (LC) — the desktop ref holds a FOREIGN instance for >1 s after navigation — MAJOR, CROSS-BOOKED → X-W5 (identity = shell-paneslot PS-1; PS-probe4 measured `gradientPaneRef := ColorPicker` t=2935 → `:= GradientPane` t=4210).** Mechanism premises re-verified: per-render inline function `:ref` (`PaneSlot.vue:124`); `onDesktopLeftMount` re-derives identity from `currentConfig.value.left` (route-synchronous) while the rendered pane trails by a rAF (`PaneSlot.vue:86-97`) plus a chunk load. This is the corrected mechanism under the rescoped C-2.
- **M-GP-3 (DU) — heading-hierarchy violation: the pane's three section heads are peers of their own pane title — MAJOR (grade ruled above) → X-W6 (h3→h4 in GradientVisualizer, a W6 file, under X-W10's design law).** Census re-run by this seat (four peer h3s on the route; zero `<h1` in all of demo; `<h3` in workbenches = this file alone). The fleet-outline arm (no h1/h2 ancestors anywhere) is a SEPARATE fleet identity for the next formation boundary — recorded, not folded, so neither row hides the other.
- **M-GP-4 (DU) — `selectedStopId` is never invalidated when `reset`/`seedFromPalette` replace the stop set — MINOR → X-W6.** Re-verified end-to-end: `setStopsFromColors` mints fresh `uid()` per stop (`useGradientModel.ts:142-149`); both commands route through it; no clear. Consequence chain verified: `GradientStopEditor.vue:68` `selectedStop = stops.find(…) ?? null` → the selected ring (`:236-239`) and the remove chip (`:285` `v-if="selectedStop && removable"`) both vanish while the model holds a non-null selection — presents as no-selection while holding one. Bounded (the Easing gate `GradientVisualizer.vue:239` keys off lengths, verified). **Precision correction to DU**: the selection is written at `:144` AND via `v-model:selected-id` (`:140`, GradientStopEditor writes it at `:129/:152/:160/:178/:185`) — "assigned in exactly one place" is false; the defect (no invalidation on wholesale replacement) survives untouched. This is C-5's replacement row (the C-7 precedent): the real post-command state defect the axis groped at and misnamed.
- **M-GP-5 (LC) — `GradientVisualizer` declares a two-way `selectedStopId` model no consumer binds — MINOR → X-W6.** `defineModel<string|null>("selectedStopId")` (`:51`); sole consumer is `<GradientVisualizer ref="visualizerRef" />` (`GradientPane.vue:25`) — no prop, no listener; identifier grep across demo = the three in-file lines. Dead public surface on the edge the L axis certified "appropriate composition"; correct declaration is a local `ref`.
- **M-GP-6 (DU) — Reset destroys all authored state in one click, no confirm/undo, wearing the same playful `rotateOnClick` spin as the NON-destructive Regenerate — MINOR → X-W6 (command semantics) + X-W5 rider (the dock's motion vocabulary is `usePaneRouter.ts:196/:208`, verified identical flags).** `resetGradient` (`:118-125`) overwrites stops, type, direction, space, hue method — per-interval easing dies with the stops. No snapshot anywhere in the gradient tree. MINOR because destructive-without-undo is a fleet pattern (mix "Clear", `:219`), but gradient destroys the most authored state.
- **M-GP-7 (LC) — zero test coverage on the entire exposed surface — MINOR → X-W6 (its gates already ADD-never-replace `o21-gradient-rail.spec.ts` per `W6.md:161/:186`) + X-W1 rider (falsifier law).** My grep across test/ + e2e/ for the component name, all three command names, and the dock copy: zero hits. Combined with `Ref<any>` + both unused-vars rules off: the three-command public API is defended by nothing — not types, not lint, not tests. The enabling condition for C-2/M-GP-1 shipping silently.
- **M-GP-8 (LC) — four contradictory injection postures for two keys in one subtree — MINOR → X-W8 limb (the GradientPane `!` dies with G-10) + cross-component note.** Verified all four: GradientPane `:8` asserted-then-unused; GeneratePane `:11` `inject(LIBRARY_PORT_KEY)!` asserted then dereferenced unguarded (`:19` `pm.createPalette` — the one latent TypeError site, **wb-generate-pane's corpus, cross-cited not booked here**); GradientVisualizer `:30` unasserted + guarded (correct — though the guard's branch is now known-dead, see C-3 ruling); ExtractWorkbench `:218` explicit default (correct). The `!` advertises a hard dependency doing no work — exactly what let the dead import survive review.
- **M-GP-9 (DU) — `mx-auto` is a no-op class on a `w-full` unconstrained wrapper — INFO → X-W8 carve rider (GradientPane.vue is W8 modify-carve); fleet idiom (GeneratePane `:30`, ExtractPane `:2`), not worth a standalone repair.** Verified: no `max-w-*` on the wrapper or Card.

## Killed-claims register (every kill re-derived by this seat before sustaining)

- **D-2 ("all three exposed commands are invisible … work the user cannot see or understand") — KILL SUSTAINED (both readers).** The only invoking parent renders all three as labelled, described dock controls (`usePaneRouter.ts:203-213`: title + prose description per command; `Dock.vue:157` GenericActionBar; copy additionally in-pane at `GradientVisualizer.vue:253-257` — all read by me). The axis inferred product invisibility from file scope and never followed `defineExpose` to its consumer. What truly is invisible is the **result** — that survives as C-4, not D-2.
- **D-4 fade clause ("a bottom fade … can suggest hidden content") — KILL SUSTAINED; QUARANTINED-FALSE (M-33) and may not be carried.** Re-verified independently: `.pane-scroll-fade` sets exactly `contain: layout style paint` + `scroll-timeline: --pane-scroll block` (`PaneHeader.vue:54-57`) — no mask, no gradient, no overlay. The class name is a misnomer surviving a retired mechanism; the axis read the NAME. (A rename rider belongs to whichever wave touches PaneHeader — X-W10 owns it.)
- **D state-table "failure" row ("non-destructive diagnosis and retry | absent") — KILL SUSTAINED, on BOTH limbs.** Parse failure: `parseVerdict` (`GradientVisualizer.vue:100-108`) surfaces the named reason into GradientCodeEditor (`:259-261`, verified), backed by the atomic swap-or-nothing contract (`useGradientModel.ts:151-168`, comment names the killed desync bug). Render failure: the App-level ErrorBoundary wraps every pane (`App.vue:48-50` region, read) with `role="alert"` + focus management + retry (per reader-LC's file read, consistent with the U-F58 comment I read at `App.vue:48`). The row asserts absence of shipped features.
- **D state-table "empty/unseeded" row ("explicit invitation to seed | absent") — KILL SUSTAINED (LC).** A phantom state, verified at all three gates by this seat: born with two stops (`useGradientModel.ts:79-84`), `removeStop` refuses <2 (`:122-125`), the only wholesale path is fed by a parser rejecting <2 stops atomically (`gradientParse.ts:261` + `:44-49`). Designing an empty-state invitation would design for an unreachable state — and the axis demanded it as a closure condition.
- **D-5 ("the pane has no signature of its own … instrument sheet") — KILLED as a defect (DU's refutation adopted; LC's classification adopted: D-1/D-5 are direction, not falsifiable defect claims).** The thin shell is the documented fleet idiom (byte-similar wrappers at GeneratePane/ExtractPane; ExtractPane carries the rationale comment; PaneHeader `:15-18` "all 9 panes inherit"). A bespoke signature for one pane breaks the uniformity these files enforce and is barred by the standing KISS/no-contrivance edict. Recorded as direction for X-W6's instrument work, carrying no defect weight.
- **L-2 ("imports the concrete GradientVisualizer rather than a typed port … prevents isolated reuse") — KILL SUSTAINED.** Self-refuting ("That is fine inside the demo" — the axis's own sentence); exactly one importer (my grep); no reuse requirement exists; a port for a single-consumer workbench is textbook contrivance.
- **L "Target boundary" / C "Required verification contract" (GradientPaneHandle + GradientSession + shared command/result rail + UNAVAILABLE protocol) — KILLED as prescribed (both readers, independently).** Every named problem already has a shipped primitive, verified by me: copy outcomes — `CopyResult` + four-state `useClipboard` (installed d.ts); availability — `DockAction.disabled` plumbed end-to-end (`GenericActionBar.vue:27` → `ActionButton.vue:17-18`); parse failure — named `reason` surfaced. Three new abstraction layers to fix a dropped return value and an unset bit. Per the ColorSpaceSelector addendum 2 (the cure is adjudicated separately from the defect): the defects survive (C-2/C-3/C-4/L-3); their axis cures do not.
- **L-1 "undocumented ancestor dependency" clause — KILL SUSTAINED.** `CSS_COLOR_KEY` is a typed `InjectionKey<ComputedRef<string>>` in a shared named module (`keys.ts:8`, read), single provider (`App.vue:271`, read), 9 injecting files (my grep), prose contract in two composables (per DU, consistent). The deadness is C-1; "undocumented" is false.
- **C-5 ("no scroll-to-selection or focus-restoration contract"; "the wrapper owns … fades") — KILL SUSTAINED, all limbs.** Fades: nonexistent (M-33). Focus-restoration: the commands are dock-button-invoked; focus correctly remains on the activating control — moving it would be the WCAG 3.2.1 violation (no `focus()` call anywhere in the chain, verified). Scroll-to-selection: the commands re-seed the whole set; nothing to scroll to. The axis pattern-matched a generic checklist while the actual post-command defect in the same lines (M-GP-4, dangling selection) went unfiled — the replacement row carries it.

## Superlatives (provenance preserved; each spot-verified by this seat)

1. **The add-ghost affordance** (`GradientStopEditor.vue:213-227`, read): a dashed twin of the handle species filled with the exact ramp color a click would mint, over `var(--alpha-checker)`, `pointer-events-none`, `aria-hidden="true"`, with the intent comment ("the affordance replaces the instruction line", W5-11). Showing the outcome instead of describing it — both readers, sustained.
2. **Real buttons for draggable handles** (`:231-240`, read): every stop is a native `<button type="button">` with a computed positional `aria-label`; the direction Slider carries `aria-label="Gradient direction"` (`GradientVisualizer.vue:232`, read). Far above the usual div-with-listeners bar.
3. **Atomic parse with a named verdict** (`useGradientModel.ts:151-168` + `GradientVisualizer.vue:100-108`, both read): swap-whole-or-nothing, `{ok:false, reason}` surfaced, the killed desync bug recorded in the comment — precisely the state-product the D axis declared absent (the sharpest illustration that the axes read one file of a stack).
4. **The self-clearing copy confirmation one file away** (`GradientEasingEditor.vue:94-103`, read): `copiedRow` derived from the LIVE clipboard status, no timer juggling — the exact idiom the pane's primary copy path should have used, which is what makes C-4 the corpus's sharpest miss.
5. **Producer API honesty** (glass-ui `useClipboard.d.ts:16-37`, read): a discriminated `CopyResult` "rather than a lossy boolean", typed failure reasons, a watchdog for platforms that hang `writeText` — the design system hands consumers a real failure channel; the consumer drops it on the floor.
6. **Scroll-timeline containment reasoning** (`PaneHeader.vue:41-57`, read): the unscoped block justifies its unscoped-ness AND its colocation (producer + sole consumers in one file), and names what `contain` isolates. Also the reason the phantom "bottom fade" was decidable in minutes. (The same block's animations are the M-34 defect — rigour and defect coexisting in one file.)
7. **Owned material contracts over per-callsite shorthands** (`.gradient-rail` `GradientStopEditor.vue:195-203/:317`; `.gradient-render-tile` `GradientVisualizer.vue:220-223/:271`, verified): the invariant written where it can be read, the tile the honest cure for the direction-slider's formerly invisible effect (comment `:151-156`, read).
8. **The ≥2-stop invariant held by construction at three independent gates** (born-with-two / removeStop refusal / parser reject — all read), making `colorAtPosition`'s throw an honest assertion, not a fallback.
9. **The pane file is genuinely right-sized** (LC, adopted as this record's verdict-shape correction): 29 lines, typed child ref, zero business logic. Judged as a composition boundary it is close to ideal; three SOURCE-RED stamps on it were a category error about which file owns the bugs.

## UNPROVEN-NEEDS-LIVE residue (→ the visual audit, task inputs)

- **U-1 — mobile keyboard / safe-area reachability** (D-4 tail, D-table row; the axis itself marked it unproven). Static facts pinned: `h-full overflow-y-auto`, no `env(safe-area-inset-*)`, no `dvh`, no `interactive-widget`, no scroll-into-view on selection. Needs a real mobile viewport with GradientCodeEditor's input focused.
- **U-2 — focus-ring and selection-ring visibility against the rail's own painted ramp** (D-table "editing" row). Mechanism exists (real buttons, `border-white` ring `:236-239`, hoisted focus ring per the U-F25 comment); rendered contrast over a user-chosen gradient is live-only — and per the easing-strip precedent, a contrast that depends on the user's gradient is unbounded, not passing.
- **U-3 — the vestibular-harm threshold of the M-34 surviving motion** (0–120px title `scale()` + veil ramp under `reduce`). Mechanism measured and adjudicated MAJOR; whether the rendered magnitude crosses a harm threshold for an affected user is the one judgment the static tree cannot settle. Feeds X-W10's G-4 probe.
- **U-4 — async-chunk REJECTION posture**: does a failed pane chunk propagate to the App ErrorBoundary's `onErrorCaptured`, or render a permanent silent blank? Vue-runtime behaviour; not asserted without measuring. (The LOADING gap needs no probe — no loadingComponent exists, D-initializing stands statically.)
- **U-5 — focus destruction after Reset/Seed**: both commands replace every stop id; if focus sat on a stop handle, its node unmounts. No `focus()` anywhere in the chain (static); where focus lands afterward is live-only. Couples to M-GP-4's dangling selection.

Resolved out of residue by this seat: the LIBRARY_PORT provider trace (app-level, every route — `App.vue:351` → `usePalettePorts.ts:243`); DU's unproven #5 is closed and C-3 sharpened by it.

## Closing verdict

**DEFECTIVE — SOURCE-RED SUSTAINED AT THE PRODUCT SEAMS, RE-ADDRESSED AWAY FROM THE FILE.** The 29-line pane is close to an ideal thin shell — the corpus's three SOURCE-RED stamps mis-located every live defect by exactly one level. What survives adjudication: 7 axis rows (4 MAJOR — the silent seed, the discarded `CopyResult` on the pane's headline affordance, the `any`-typed command channel, the PRM-immune header motion) and 9 adopted misses, headed by the cross-booked mobile BLOCKER (every command this pane exposes is a dead button on every phone) and its desktop sibling (a measured 1275 ms foreign-instance window). 9 kills are sustained on this seat's own re-derivations — including both axis cures, which prescribed three abstraction layers where a destructure, a bit, and an h4 were owed. Routing: X-W6 carries the gradient-tree mass (7 rows), X-W5 the shell channel (5, two cross-booked), X-W8 the dead-inject/carve limbs (3, G-10 already names the line), X-W10 the fleet PRM cure (already booked as CC-099), X-W1 a falsifier rider; nothing lands NO-WAVE-OWNER. Five residue rows go to the visual audit. Dispositions are ADJUDICATED, not VERIFIED; no wave is stamped by this file.

— end of apotheosis. The three challenge documents and both re-reader returns remain immutable beside this file per E-1/E-3.
