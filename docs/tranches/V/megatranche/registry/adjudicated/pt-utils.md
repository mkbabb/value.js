claude-fable-5

# pt-utils — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: apotheosis, parse-that module `utils` · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-06 · **Subject tree**: `/Users/mkbabb/Programming/parse-that` @ master `ef10d5b` (re-verified this session), `typescript/src/parse/utils.ts` = 186 lines, read whole by this seat · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated**: two fresh reader returns over the module's two challenge files — reader-α (`parse-that/utils · L-18 hostile re-read · lens=library-correctness`: 27 confirmation cells · 9 kills · 6 misses · 8 superlatives) and reader-β (`utils-consumption-api-L18`: 26 confirmation cells · 10 kills · 7 misses · 6 superlatives). Both challenge files (`audit/parse-that-modules/utils/challenge-L-library.md`, 306 lines; `challenge-C-consumption.md`, 605 lines; both `claude-opus-5[1m]`) re-read **whole** by this seat.
**Method (incredulity)**: every reader-vs-reader contradiction, every kill of a load-bearing cell, every severity split, and every NEW missed row was re-derived against the bytes with this seat's own instruments before adoption — and the whole corpus was **identity-guarded against the four sibling adjudications that already own parts of this file's surface** (`pt-diagnostics` — which adjudicated utils.ts's entire diagnostics tier; `pt-core` — the line/column trio and the /core topology; `pt-debug` — the renderer; `pt-leaf` — the whitespace-alphabet family and the dead FLAG machinery). That guard is this record's largest single contribution: **both readers filed as NEW misses five findings the registry already owns**, and both graded two rows against standing family precedent. Nothing armed (`enableDiagnostics()` never called, `memoize()` never constructed, no bench, no package import); every dynamic probe was a verbatim source transcription in a bare `node -e` process. parse-that read-only, main checkout only. Dispositions are **ADJUDICATED, not VERIFIED**.
**This seat's own instruments (all read-only)**: `utils.ts` whole (186 — every cited anchor lands); `state.ts:95-140` (clone; the three engines — `getColumnNumber` `:111-117` inclusive-fromIndex, `getLineNumber` `:119-124`, `getLineAndColumn` `:127-134` `offset-1`); `parser.ts:1-10` (the six-symbol utils import at `:4`), `:40-80` (parseState/parseStateInner; `:60` furthest fallback; `:67-69` guarded console.error), `:405-478` (wrap's close-fail arm `:417-423` incl. the caller-side slice at `:419`; the FLAG `call()` block `:437-478` incl. `:466` FLAG_EOF and `:469-472`), `:525-636` (many `:538` zero-progress break BEFORE the value store; sepBy checkpoints), `:638-641` (`.eof()` = `skip(eof())`, NOT the FLAG path), `:644-688` (recover whole: `:666` ungated collect → `:670` sync → `:672-677` pop-without-restore → `:681` `ok(sentinel)`); `leaf.ts:275-345` (string merges with label `:291/:303`; regex EOF guard `:327-330` merge-free), `:370-399` (trimStateWhitespace: `:378` fast-exit guard, `:383` the actual class {9-13,32}; `_initWhitespace` `:396-399`); all five entry files whole (index/core/diagnostics/utils-entry/packrat-entry); `debug.ts:55-75/:200-228` (addCursor's default-offset `getLineAndColumn()` at `:59`; formatDiagnostic's reads — 7 of 8 fields, never `d.offset`); `packrat.ts:345-360` (the clone/snapshot grow-pass site); greps: `collectedDiagnostics` = exactly {95,115,139,143,147}; `\.recover(` over src+test+scripts = **0**; `mergeErrorState(` = parser 15 + leaf 7 = **22**; `parser(state as ParserState<unknown>)` = **10** of the 35 casts; line-engine callers = `debug.ts:59` alone; `formatDiagnostic|formatAllDiagnostics` = debug.ts:200/:235/:238 only, on no entry; the C·D-01 receipt grep re-run verbatim = **7 lines** (utils 33/41/44/53/59/133 + parser 62); FLAG_EOF assignments = **0** (`.flags` write = `:496` on the discarded flaggedParser); `trailing-content` writers = state.ts:26 (type) + parser.ts:470 (dead branch) only; `addSecondarySpan|resetErrorState` extra-module consumers = **0**; utils symbols over test/+scripts/ = **0 files** (enable/disable appear as fixtures only); dist: byte sizes **2,039 / 3,516 / 40,576 / 2,465** (utils.js / diagnostics chunk / packrat chunk / utils.cjs — /utils graph total **46,131 B**), `utils.js:1-2` imports BOTH chunks, diagnostics chunk **0 imports** with a **12-entry export map** (addSecondarySpan + resetErrorState absent), `utils-entry.d.ts` whole (no ParserState), `diagnostics.d.ts` whole, `utils.d.ts` = 14 declares incl. mergeErrorState/collectDiagnostic, `parser.d.ts:64` recover public, `core.d.ts` whole (no diagnostics door), **no `dist/debug.js`** + 0 formatter hits in either chunk + `files:["./dist"]`; `package.json:6` sideEffects + the 5-door exports map; `vite.config.ts` entry `utils: utils-entry.ts`; **node probes (transcriptions)**: `getLineAndColumn("\nx",0)` → **{line:2, column:-1}** vs the utils inline math → **{line:1, column:0}**; `getColumnNumber("a\nb",1)` → 0 vs `.column` 1; `skipWhitespace({src:42})` → NO-THROW offset 0, boxed `String` → silently works, NUL/ESC/BEL consumed, null → TypeError; `skipBlockComments("/* oops")` ≡ `("/2")` → offset 0 both; class censuses `<=32` = 33 cps, trim = 6 cps, trim−css = **[11]** exactly, `<=32` over-consumes **28** vs css-syntax-3; value.js side: `src/css/types.ts:10-27` (frozen 8-code ParseIssue + non-empty tuple), `stylesheet.ts:436-439` (the `\s|;` skip; `indexOf("*/", cursor+2)` at `:438`, `failure(...)` at `:439`); `INBOX.md:77` O-15 verbatim; `W2.md` O-8/EQ-4/R-LAW-3 cites at :253/:266/:281; `parser-band.md:116` debt 1 verbatim; sibling registries: `pt-diagnostics.md` (rows 2/4/5/6/8/10/14/17/19/22/24/25/26/27/28/29/30/34 + kills K1/K4/K10 + Seat-2 receipt), `pt-core.md` (rows 3/11/14/31), `pt-debug.md` (M-1/MISS-1/MISS-3), `pt-leaf.md` (PT-LEAF-3/-5/-7/-17/-20/-23/-27).
**STOP/law compliance**: `/Users/mkbabb/Programming/parse-that-css-totality-p2` → `No such file or directory` (re-verified; never created). **Sole write = this file.**

## Scoreboard

**43 adjudicated confirmation cells — 22 pt-utils-PRIMARY roster rows (1 BLOCKER · 9 MAJOR · 9 MINOR · 3 INFO) + 21 identity cells CONFIRMED and cross-booked at their PRIMARY homes in pt-diagnostics / pt-core / pt-debug / pt-leaf** (anti-rename: one identity, one home, no row re-booked under a second name). **14 killed-claims register entries, all SUSTAINED** — including one head-on reader-vs-reader contradiction ruled at the bytes and two kills where one reader endorsed the exact superlative the other reader killed. **11 missed rows ADMITTED** — but the identity guard shows **5 of the 11 were already in the registry before either reader wrote them** (a fleet lesson: the misses were real against the challenge corpus and stale against the registry). **11 superlatives sustained** (two rescoped). **7 adjudicator corrections against the readers**, the sharpest being a dead-code attribution both readers AND the pt-diagnostics record repeat: `parser.ts:469-472` is inside the never-armed FLAG_EOF branch (PT-LEAF-20's corpse), so "on every failed `.eof()`" is false — `.eof()` routes through `skip(eof())` at `parser.ts:638-641`.

Blocker-weight survivor of record, PRIMARY here: **PT-UTILS-1** — the two byte-scanners, the module's genuinely good half and AC-3's named substrate, are welded by source-module co-residence into the diagnostics chunk and reachable through no door that does not also deliver both process globals and (via `/utils`) the 40,576-byte packrat chunk carrying the PACKRAT_ARMED latch. The diagnostics-tier blockers this corpus re-confirmed (the global buffer, content-free-by-default + console coupling, O-8 totality) are REAL and stand — at their pt-diagnostics identities, where they were already adjudicated with live witnesses (P3/P4/P5) this corpus's static traces corroborate.

## The incredulity seat's own work — ruled disagreements and corrections

1. **The S-2/S-6 head-on contradiction — RULED for reader-α's kills; BOTH readers corrected on the evidence.** Reader-α KILLED challenge-C's S-2 ("the diagnostics-off hot path is allocation-free … PT-01 is a surface defect, not a cost defect") and challenge-L's S-6 ("the disabled path builds nothing at all"); reader-β SUSTAINED both ("S-2 and S-6 hold and compose") — while simultaneously confirming L-U-05's mechanism (the `:34-35` unguarded allocations) at MAJOR, an internal contradiction in reader-β's own return. At the bytes: `utils.ts:34-35` sit outside the `:33` guard and execute on every disarmed furthest-advance — the kill of S-2-as-stated is correct. For S-6, the callee-side guard at `:71` is real, but the disabled path builds at the CALLER: `parser.ts:419` evaluates `state.src.slice(savedOffset, openEnd)` on every failed `wrap()` close, unconditionally — **one live site, not two**: reader-α's second site, `parser.ts:469-472` ("on every failed `.eof()`"), is **dead code** — FLAG_EOF has zero assignments in the tree (sole `.flags` write `:496`, on the discarded `flaggedParser`; `.eof()` = `skip(eof())`, `:638-641`; = pt-leaf K4/PT-LEAF-20). Kills SUSTAINED as corrected; the narrow true kernels survive as superlative 5's rescope. This correction also lands on reader-α's C·D-05 "strengthening" and on **pt-diagnostics row 14's cell** ("internal writers parser.ts:419/:469 as filed") — `:419` is the only live internal instance.
2. **Severity contests — four rulings, two of them family-precedent corrections against BOTH readers.** (a) **D-01/L-U-04** (content-free default + console coupling): α MAJOR (library lens) vs β BLOCKER → **BLOCKER at identity** — pt-diagnostics rows 4+5 already hold it at BLOCKER with EQ-4's by-name disqualifier; α's lens-relative MAJOR recorded as dissent. (b) **D-07/L-U-02** (destructive reset; wrong-offset sync-fail path): α MAJOR vs β BLOCKER → **MAJOR at identity** — pt-diagnostics rows 8+9 adjudicated exactly this trace at MAJOR with an honest bound this corpus lacked ("observable only when the enclosing parse ultimately fails with its deepest probe before the recovery point") and a live witness (P4). β's BLOCKER recorded as dissent; family consistency wins. (c) **D-03/L-U-07** (welded scanners): α MAJOR vs β BLOCKER → **BLOCKER, PRIMARY HERE** (PT-UTILS-1) — the pt-core row 3 precedent (the /core face of the same graph, BLOCKER) plus K-6/O-8 adoption law; challenge-C's honest bound (structural availability, globals inert at import, no measured runtime penalty) is preserved inside the row. (d) **L-U-05** (the `:34-35` allocations): BOTH readers MAJOR → **MINOR at identity** — pt-diagnostics row 22 already re-rated exactly this row MINOR (its K4: "two young-gen arrays per new-furthest event bounded by distinct offsets"), with magnitude banked to MEASURE-AT-OPEN. Correction against both readers; the α-extension (a failed parse pays 4 arrays, 2 pure garbage at `parser.ts:52/:61-64`) is adopted as the row's sharpest cost cell.
3. **The line/column cluster — both readers' M-1 is REAL, and the registry already owned it.** This seat's own probes reproduce the decider: `getLineAndColumn(0)` on a newline-leading source → `{line:2, column:-1}` (the ECMA `lastIndexOf` position-clamp at `state.ts:128`), while the utils inline math at `:107-110` → `{line:1, column:0}` — the duplicate is the CORRECT engine, so both challenges' "presently equal / call the helper" cells die (kills K1/K2 sustained by my own instrument). But **pt-core row 11 is PRIMARY** for the engine trio, **pt-diagnostics rows 20/30** hold the clamp witness and the formatDiagnostic header-vs-caret self-contradiction, **pt-diagnostics K1** already ruled the cure direction ("install the slice-first algorithm INTO state.ts, never the reverse"), and **pt-debug MISS-1** holds the renderer blast radius. Both readers filed all of it as NEW misses. ADMITTED as corroboration; cross-booked; zero new ids. Same ruling for reader-β's getColumnNumber "fourth engine" (inside pt-core row 11's cell) and for reader-α's M-4 (regex EOF merge-free = **PT-LEAF-3**, BLOCKER at pt-leaf — α's armed-mode sharpening, `any(regex,regex)` at EOF yielding `expected: []`, is recorded as a rider on that identity).
4. **Reader-α's M-5 (zero-width-sync orphan) — mechanism VERIFIED by this seat, identity pre-owned.** The static trace is airtight at the bytes: `:666` push → sync succeeds at zero width → `:681` `ok(sentinel)` with `offset === savedOffset` → many()'s `:538` breaks BEFORE the value store → element dropped, parse can return OK at `:551` with the diagnostic stranded (pop runs only in the `:672-677` sync-FAIL arm). But pt-diagnostics **row 19** + its MEASURE-AT-OPEN row 4 already hold the zero-width-sync truncation-plus-stranded-diagnostic finding. ADMITTED as the mechanism's first full static trace; cross-booked.
5. **Reader-β's packaging misses — two of four pre-owned, two PRIMARY here.** The renderer gap (Diagnostic has no reachable renderer; formatters dropped from the bundle while `debug.d.ts` ships) = **pt-diagnostics row 17 + pt-debug M-1**, already BLOCKER-graded there — cross-booked. `/diagnostics` omitting ParserState = **pt-diagnostics row 24**, where both prior readers' kills were HELD and the row adjudicated **MINOR** (strict/NodeNext compile witness: TS infers the type at every realistic call site) — so reader-β's MAJOR grade for the same shape is corrected by precedent, and the same precedent re-rates its /utils twin (C·D-17) to **MINOR** (PT-UTILS-17). The two genuinely new rows: `/core` ships the buffer's WRITER with no reader/clearer (PT-UTILS-9), and the D-16 stem collision (PT-UTILS-7) — neither appears in any sibling roster.
6. **Counts and receipts — every disputed number re-measured.** mergeErrorState call sites = **22** (both readers' kill of "27" sustained; matches pt-diagnostics' census). utils.ts runtime exports = **14**, untested-as-subjects = **12** (kill of "9 of 13" sustained). The C·D-01 printed grep receipt returns **7 lines, not 13** (α's kill sustained; the census CONCLUSION — utils is the sole writer of all three channels, every content-bearing write gated — re-verified true independently). The 35-cast attribution: **10 are invocation casts** imposed by `ParserFunction<unknown>` (β's kill sustained; 25 attributable to utils signatures). `sideEffects: false` sits at `package.json:6` (α right, C's `:5` off by one) and does NOT contradict inert let-initializations (β's kill of α's endorsement sustained — the import-time-effects reading is pt-diagnostics row 35's, too); β's tree-shaking-mechanism kill also sustained (the surviving defect is structural: no source-module split + the unshakeable CJS arm).
7. **One kill EXTENDED by this seat**: β's kill of C-§2's "all six are live". β proved addSecondarySpan/resetErrorState dead (0 extra-module consumers; absent from the chunk's 12-entry export map — both re-verified). This seat adds: **addSuggestion's sole extra-module call site is the dead FLAG_EOF branch** (`parser.ts:469`), so its only runtime-reachable caller is intra-module (`reportUnclosedDelimiter:74`) — and therefore **`Suggestion.kind`'s `"trailing-content"` member has NO reachable writer**: the closed 2-member union (state.ts:26) is 1-member at runtime. New roster row PT-UTILS-18.
8. **Superlative rescope (against reader-α)**: L's S-4 "under W2.md:253 O-8 this module's flag PASSES" is too strong — O-8 forbids process-global mutable state wholesale, and pt-diagnostics row 6 (BLOCKER) already counts `diagnosticsEnabled`'s reads/writes in its 6-of-6 O-8-totality census. The true, preserved kernel: the flag is a **two-way toggle, not a one-way latch** — it passes the anti-LATCH distinction that PACKRAT_ARMED fails, which matters for the cure's shape, not for O-8 conformance.

**Adjudicator's corrections register**: (1) reader-β's S-2/S-6 endorsement — corrected (ruling 1); (2) reader-α's `:469-472` live-site attribution in M-3 and C·D-05 — corrected, dead code (ruling 1); (3) both readers' L-U-05 MAJOR — re-rated MINOR at family precedent (ruling 2d); (4) both readers' D-17-class MAJOR — re-rated MINOR at row-24 precedent (ruling 5); (5) both readers' filing of registry-owned rows as new misses — identity-guarded (rulings 3-5); (6) reader-α's S-4 O-8 scope — rescoped (ruling 8); (7) reader-β's "44 KB" and reader-α's "~46 KB" door price — pinned: **46,131 B** measured (2,039 + 3,516 + 40,576).

## Adjudicated defect roster — pt-utils PRIMARY rows

Routing law (= pt-leaf's): **W0..W4** where an X·P wave owns the surface (W0 pause/fresh-root · W1 harness/corpus/bench · W2 dual-target algebra · W3 totality/recovery · W4 PLAW-BIND/release) · **REFINEMENT FOLD** where the finding sharpens a wave's spec · **NO-WAVE-OWNER** where no X·P wave owns it (incumbent parse-that repair; rides the owner-held O-15 channel, **semver-priced per PT-LEAF-27**). X·P executes nothing until the owner's begin-word; every routing is a booking. Anchors are `ef10d5b`; re-resolve before any cure (D-19).

### Blocker

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-UTILS-1** | L-U-07 / C·D-03 / C·D-14 (ruling 2c) | The two byte-scanners — the module's stated reason to exist and AC-3's named substrate — cannot be delivered without instantiating both module globals, from ANY door: they share a source module with `diagnosticsEnabled` and `collectedDiagnostics`, so Rollup welds them into the 0-import diagnostics chunk; all five entries reach it; and the `/utils` subpath advertised as their home prices at **46,131 B** (utils.js:2 also pulls the packrat chunk carrying PACKRAT_ARMED and the console.error), CJS arm unshakeable. Honest bound preserved: globals inert at import; structural availability, not measured cost — a conforming consumer has no lawful door, which is the K-6/O-8 kill class | utils.ts:6/:95 vs :159-186; dist/diagnostics chunk 0 imports, exports `skipWhitespace as b` / `skipBlockComments as s`; utils.js:1-2 both imports verbatim; byte census this seat's own; sideEffects/tree-shaking sub-claims KILLED (K9/K10) — the defect is the missing source-module split + the CJS arm, stated clean | **W2** — scan primitives land as combinator-surface citizens (clause 12; the split into `scan.ts`+diagnostics is AC-3's stated precondition) · **W4 rider** — subpath isolation asserted over import graphs (shared with PT-LEAF-17's rider and pt-core row 3) · incumbent source split **1.1.0-eligible** (chunk regraph, no surface change) rides O-15 |

### Majors

| id | merged ids | claim (adjudicated) | proof anchor | terminal disposition |
|---|---|---|---|---|
| **PT-UTILS-2** | L-U-08 + C·D-11 utils residue | `skipWhitespace`'s own JSDoc says "ASCII whitespace"; the predicate `<= 32` silently consumes **all 28 non-CSS code points** below space incl. NUL (css-syntax-3 §3.3: U+0000 → U+FFFD, an ident code point), ESC, BEL — measured. Package context: this is the worst of THREE public whitespace classes (33 / 25 / 6 cps), and the near-CSS one ({9-13,32}, off by exactly U+000B) already ships in `.trim()`'s own hot path — the challenge's "two classes" + trimStateWhitespace-attribution cells are KILLED (K3: leaf.ts:378 is a fast-exit guard; :383 is the class) | probes + censuses this seat's own (28 / [11] exact); utils.ts:158 vs :162 | **W2** at the PT-LEAF-7 identity (trivia class fixed by css-syntax-3 CITATION, all mechanisms retire together); utils-side residue = the doc/name falsity — NO-WAVE-OWNER doc fix **1.1.0**; predicate change itself **2.0.0** (widens/narrows acceptance) |
| **PT-UTILS-3** | L-U-09 / C·D-12 | An unterminated `/*` is observationally identical to a legitimate division `/` (both leave offset at the slash — measured `identical: true`); css-syntax-3 §4.3.2 calls it a parse error consuming to EOF; the module's own `reportUnclosedDelimiter` sits 90 lines above unused (internal inconsistency, not an unbuilt feature); the named downstream emits exactly the diagnostic this destroys (`stylesheet.ts:438-439` `failure(..., ["closing comment"], cursor)`) | utils.ts:177-183 vs :66-80; value.js contrast re-read (cite corrected: `:438-439`) | **W2/AC-3** — the span-emitting scanner carries a labelled unterminated-comment failure; incumbent distinguishability change (park at `src.length` or return a signal) is **2.0.0** (behavior change) |
| **PT-UTILS-4** | L-U-10 | Four non-string postures from one export, one a SILENT NO-OP: `src=42` → no-throw empty-document (`(42).length` undefined kills the loop guard); null/undefined → one TypeError shape; `{length:5}`/array/Uint8Array → another; boxed `String` → silently WORKS (α's fourth posture, re-measured). Extends O-15 PT-07 to `/utils` — the silent no-op is strictly worse than the raw throw PT-07 flagged; the boundary law must cover `/utils`, not only `.parse()` | this seat's probe table reproduces α's in full + the boxed-String row | **REFINEMENT FOLD** — W1's JS-boundary corpus gains the 8-probe `/utils` row; W2's boundary law = positional `typeof` at entry (shared with PT-LEAF-5), never throw-inference |
| **PT-UTILS-5** | C·D-13 | The scanners are COMP-1-hostile by construction: both end `state.offset = i;`, return void, write no complement channel — they destroy exactly the bytes W2's C is defined to hold (whitespace, comment bodies), with no hook. The TECHNIQUE survives (superlative 5); the SIGNATURE cannot serve AC-3 unmodified | utils.ts:164/:185; W2 COMP-1/EQ-2/EQ-6 cites verified | **W2** — AC-3 authors the complement-emitting scanner on the kept technique; raw exports RETIRE (C-§7 sustained) — a **2.0.0** event, priced |
| **PT-UTILS-6** | C·D-09 | The retention rationale names a consumer that does not consume it: utils.ts:150-156 says present-tense the scanners are the primitives "value.js's canonical CSS grammar" drives — value.js has no parse-that dependency in any class, greps only prose asserting the absence, and hand-rolls its own scanner. A KEEP decision resting on a false consumer claim | utils.ts:150-156; value.js package.json + `src/` greps re-verified | **NO-WAVE-OWNER** — incumbent doc repair, O-15 channel, **1.1.0** |
| **PT-UTILS-7** | C·D-16 + β-miss 6 rider | dist stem collision: `dist/utils.js` is built from `utils-entry.ts` (vite entry map) while `dist/utils.d.ts` is vite-plugin-dts's emission of `utils.ts` — same stem, different modules; sibling-convention resolvers attach the 14-function declarations to the 7-export runtime, so `import { mergeErrorState } from "…/utils"` typechecks and crashes. Unique collision in the package (verified). Rider: NO gate can see it — dist-surface.test.ts covers only the root barrel, subpath-gate.mjs asserts existence + 3 typeofs, nothing compares a subpath's `types` stem to its `import` stem | vite.config.ts entry map; both d.ts files read; exports map pairing; gate files read at the cited scopes | **W4** — release gate: per-subpath declaration↔runtime parity (the exact direction pt-diagnostics row 17 and pt-debug M-1 also need); incumbent source-stem rename **1.1.0-eligible** |
| **PT-UTILS-8** | C·D-05 + α-M-3 as corrected | Callee-side arming forces call-site cost: `mergeErrorState(state, label?)` takes a string, not a thunk, so consumer label expressions evaluate disarmed; `isDiagnosticsEnabled` is unreachable from every subpath so consumers cannot branch. The library's own ONE live internal instance: parser.ts:419's `state.src.slice(savedOffset, openEnd)` on every failed wrap() close. The `:469-472` instance both readers cited is DEAD (ruling 1) | utils.ts:28/:16; parser.ts:419 live vs :466-476 dead (FLAG_EOF unassigned — grep this seat) | **W2** — arming as a parse parameter dissolves the class; label cost law: construction-time labels (already the leaf idiom) or thunks. The dead branch deletes with PT-LEAF-20 (**1.1.0**) |
| **PT-UTILS-9** | β-miss 2 (NEW) | `/core` ships the collected-diagnostics buffer's WRITER with no reader and no clearer: `Parser.recover` is public on the tier (parser.d.ts:64) and `parser.ts:666` writes the module-global buffer ungated, but core.d.ts exports none of get/clear/pop — a /core-only consumer using its own tier's documented combinator leaks diagnostics it cannot read, pop, or empty. The API-semantics half of the isolation falsity: survives any bundling fix. Complements pt-diagnostics row 3 (readers without Parser) — NO subpath is closed over the recover lifecycle | core.d.ts + parser.d.ts:64 read this seat; grep: parseState never clears | **W4** — subpath lifecycle-closure gate; dissolves entirely under **W2**'s D-in-the-result (the buffer RETIREs — pt-diagnostics' held verdict) |
| **PT-UTILS-10** | L-U-03 as corrected + α-M-6 | **12 of 14** runtime exports untested-as-subjects (kill K8's arithmetic); the scanners and the whole collection tier have ZERO behavioral coverage; and the tier is not merely untested but UNREACHED — `.recover(` has **0 call sites** in src/test/scripts, so the library has never once executed its own recovery combinator (the severity modulation both challenges' BLOCKERs owed) | greps this seat's own (0 files; 0 recover sites); 9 proof-*.mjs enumerated, none names utils | **W1** — the lane's oracle suite is the successor (scanner 8-probe row, recover corpus rows incl. the zero-width-sync witness); incumbent gap rides O-15 |

### Minors and info

| id | claim (adjudicated, compressed) | disposition |
|---|---|---|
| PT-UTILS-11 | The `:34-35` disarmed-path array allocations — **MINOR at identity** (pt-diagnostics row 22 PRIMARY; both readers' MAJOR corrected, ruling 2d); α's cost cell adopted: a failed parse pays 4 empty arrays, 2 immediately overwritten (parser.ts:52/:61-64) | identity; magnitude = MEASURE-AT-OPEN 1 |
| PT-UTILS-12 | mergeErrorState's fluent return dead at all **22** sites (count corrected); advertises chainable/pure for the hottest in-place mutator | NO-WAVE-OWNER (**1.1.0** signature doc); W2 absorbs in the append-to-D law |
| PT-UTILS-13 | `ParserState<unknown>` signatures impose **25** of the 35 caller casts (10 are ParserFunction's — K11); none of the 8 exports touches `.value`, so `<T>` erases all 25 free; annotation style inconsistent across 100 lines | W2 type-soundness law (shared with PT-LEAF-15) |
| PT-UTILS-14 | skipBlockComments re-implements the `<=32` skip inline with no shared constant — the PT-UTILS-2 predicate fix must land twice | folds into PT-UTILS-2's cure |
| PT-UTILS-15 | skipBlockComments is misnamed (skips whitespace too) — the "double-scans" consequence KILLED (K14: post-skipWhitespace the `<=32` branch cannot fire; one O(1) redundant call); naming/API-clarity ground only | NO-WAVE-OWNER (doc **1.1.0**; rename **2.0.0**) |
| PT-UTILS-16 | Type provenance laundering: Suggestion/SecondarySpan reach the barrel via utils.ts:3 while ParserContext/Span come from state.js directly — two routes, two answers in the .d.ts graph | NO-WAVE-OWNER; dissolves under W2's ParseIssue adoption |
| PT-UTILS-17 | `/utils` exports functions whose sole parameter type it does not export (utils-entry.d.ts read whole; no ParserState) — **MINOR by row-24 precedent** (TS infers at realistic call sites; both prior readers' kills held there; β's MAJOR corrected) | W4 golden-declaration surface, with pt-diagnostics row 24 |
| **PT-UTILS-18** | **NEW (this seat's)**: `Suggestion.kind`'s `"trailing-content"` member has no reachable writer — sole writer parser.ts:470 sits in the dead FLAG_EOF branch; the closed 2-member union is 1-member at runtime; sharpens C·D-06/pt-diagnostics row 14 (the downstream's 8-code taxonomy needs are unmet by a union that is effectively `"unclosed-delimiter"`) | W2 (ParseIssue is D's element type per AC-4); the dead branch deletes with PT-LEAF-20 (**1.1.0**) |
| PT-UTILS-19 | Dual public home for the scanners, no declared primary; runtime identity single (one chunk — β's honest-bound check preserved); contract ambiguity + doubled semver surface | identity with pt-diagnostics row 28 (the six-function twin); W4 canonical-home declaration |
| PT-UTILS-20 (INFO) | PT-03 re-verified at the bytes and NOT sited here; `diagnosticsEnabled` is a genuine two-way toggle (S-4 as rescoped, ruling 8) — the anti-latch distinction shapes the cure, O-8 still fails wholesale (pt-diagnostics row 6) | record |
| PT-UTILS-21 (INFO) | PT-04 not sited here — no recursion in utils.ts; the ceiling the module LACKS is the per-diagnostic O(n) allocation (see MEASURE-AT-OPEN 2; the cost row lives at pt-diagnostics row 23 / pt-core row 11 identities with the K1-ruled cure direction: keep the slice-first arithmetic, drop the duplicate prefix copy) | record |
| PT-UTILS-22 (INFO) | Frame rows: the brief's `src/parsing/` consume edge is a phantom (surface = `src/css/**`, 52-export frozen barrel — pt-diagnostics row 1 holds the calibration); RED-7 gate unrunnable from this workspace (parse-that not installed — epoch rule, cited-not-re-measured); dist under measurement is a 2026-07-29 rebuild of 2026-07-03 source — chunk names are content-addressed, re-derive from `dist/core.js:1`'s specifier on any re-run (pt-leaf's provenance law, carried) | record |

### Identity cross-book table — 21 cells CONFIRMED at their PRIMARY homes (no new ids)

| corpus cell(s) | PRIMARY home | note |
|---|---|---|
| L-U-01 / C·D-02 (global buffer, 5 sub-failures) | **pt-diagnostics row 2** (BLOCKER) | every anchor re-verified this seat; α's nested-pop direction fix adopted into the cell's reading (outer pops INNER's stranded record) |
| L-U-04 / C·D-01 (content-free default; console coupling) | **pt-diagnostics rows 4+5** (BLOCKER) | α's falsifier-rebuttal (labels are construction-time — leaf.ts:132/:278/:324) adopted as the cost decider; receipt-grep kill K13 noted on the cell |
| O-8 totality of the surface | **pt-diagnostics row 6** (BLOCKER) | grounds ruling 8 |
| L-U-02 / C·D-07 (destructive reset; sync-fail amnesia) | **pt-diagnostics rows 8+9** (MAJOR, honest bound + P4) | α's scoping correction adopted: the "six labels destroyed" half is ARMED-only; the shipping-default loss = offset amnesia + non-monotone furthest |
| L-U-06 / C·D-23 (live getter; rebind-on-clear; mutable fields) | **pt-diagnostics rows 10+11** (MAJOR) | ParseIssue contrast (frozen, readonly) verified |
| C·D-04 / C·D-06 (readers-exported/writers-hidden; closed union) | **pt-diagnostics row 14** (MAJOR) | cell corrected per ruling 1 (`:469` dead); sharpened by PT-UTILS-18 |
| C·D-08 (Diagnostic ↛ ParseIssue, five fields) | **pt-diagnostics row 25** (MINOR there) | EQ-4 verbatim re-verified |
| L-U-12 (write-only `Diagnostic.offset`) | **pt-diagnostics row 26** | formatDiagnostic's 7-of-8 reads re-verified |
| L-U-14 / C·D-22 (`found` lossy; EOF-ambiguous) | **pt-diagnostics row 27** | |
| L-U-16 (expected tri-state; V8 shape fine) | **pt-diagnostics row 34** / pt-leaf sup 12 | α's S-3 re-verification concordant |
| L-U-17 (clone drops the accumulation; armed grow-pass loss) | **pt-core row 14** PRIMARY / pt-diagnostics row 21 | packrat.ts:351/:356 re-read |
| L-U-11 / C·D-10 / α-M-1 / β-miss 1 (engine duplication; clamp bug; "presently equal" dead) | **pt-core row 11** PRIMARY / pt-diagnostics rows 20+30+23, K1 | this corpus's probes corroborate; cure direction pre-ruled |
| α-M-2 / β-miss 5 (getColumnNumber) | **pt-core row 11** | in-cell |
| C·D-15 (/core isolation false at the build) | **pt-core row 3** (BLOCKER) | byte closures concordant |
| C·D-11's three-alphabet story | **pt-leaf PT-LEAF-7** (BLOCKER) | utils residue = PT-UTILS-2 |
| α-M-4 (regex EOF merge-free) | **pt-leaf PT-LEAF-3** (BLOCKER) | armed-mode `expected:[]` sharpening recorded as rider |
| α-M-5 (zero-width-sync orphan) | **pt-diagnostics row 19** + M-A-O 4 | this seat's `:538`-trace = the mechanism's first full static derivation |
| β-miss 3 (no reachable renderer; declarations ship) | **pt-diagnostics row 17** / pt-debug M-1 | `files:["./dist"]` verified |
| β-miss 4 (/diagnostics omits ParserState) | **pt-diagnostics row 24** (MINOR) | grounds the D-17 re-rate |
| L-U-07's four-concerns mis-factoring | **pt-diagnostics row 29** | the Goldilocks reading survives there |
| C·D-18 / dual-home class | **pt-diagnostics row 28** | PT-UTILS-19 is the scanner-side face |

## Killed-claims register — 14 entries, all SUSTAINED

| # | killed claim | adjudication |
|---|---|---|
| K1 | L-U-11's equivalence + cure ("semantically equal … call getLineAndColumn") | **SUSTAINED by this seat's probe** — false at f=0 over a leading newline ({1,0} vs {2,-1}); the cure would inject a wrong line + negative column; the duplicate is the CORRECT engine (= pt-diagnostics K1, independently concordant) |
| K2 | C·D-10's "two engines, presently equal, free to drift" | **SUSTAINED** — drift already happened, at exactly the offset the four probe rows (all ≥1) skip |
| K3 | C·D-11's trimStateWhitespace attribution + "two classes" headline | **SUSTAINED at the bytes** — leaf.ts:378 is a fast-exit guard; :383 = {9-13,32}, a THIRD class, off css-syntax-3 by exactly U+000B (census this seat); the finding strengthens as restated |
| K4 | C's S-2 ("diagnostics-off hot path is allocation-free … surface not cost defect") | **SUSTAINED (ruling 1)** — self-refuted by :34-35 at the bytes; reader-β's endorsement corrected; the narrow `expected`-undefined kernel survives in superlative 5 |
| K5 | L's S-6 ("the disabled path builds nothing at all") | **SUSTAINED AS CORRECTED (ruling 1)** — one LIVE caller-side site (parser.ts:419); the `:469-472` site is FLAG-corpse dead code (correction against reader-α and pt-diagnostics row 14's cell) |
| K6 | C's S-4 unconditional credit ("resets the state … CORRECT") | **NARROWED** — needed on the sync-success branch, applied before the branch is known; the defensible superlative is placement-specific (α's kill and β's caveated confirmation converge here) |
| K7 | "27 call sites" (L-U-05/L-U-13) | **SUSTAINED** — 22 (15+7), re-measured; the doc's own enumeration lists 22 |
| K8 | "9 of the module's 13 exported functions — ~70% — untested" | **SUSTAINED** — 14 exports, 12 untested (86%); the doc undercounts its own case |
| K9 | L-U-07's "contradicts `sideEffects: false`" | **SUSTAINED (β's kill; α corrected)** — inert let-initializations are not import-time effects; concordant with C·D-03's honest bound and pt-diagnostics row 35 |
| K10 | L-U-07's tree-shaking mechanism ("let bindings not removable once any export retained") | **SUSTAINED (β's kill)** — removability turns on retained references; the true defect is the missing source split + the CJS arm |
| K11 | L-U-15's "all 35 casts imposed by the callee's signature choice" | **SUSTAINED** — 10 of 35 are invocation casts (grep this seat); 25 attributable |
| K12 | C-§2's "All six are live — consumed by parser.ts:4 and debug.ts:6" | **SUSTAINED AND EXTENDED** — addSecondarySpan/resetErrorState: 0 extra-module consumers, absent from the 12-entry chunk map (dead export keywords); + this seat: addSuggestion's only extra-module site is dead code → PT-UTILS-18 |
| K13 | C·D-01's printed writer-census grep receipt ("returns exactly … 13 lines") | **SUSTAINED** — the pattern returns 7 lines (re-run verbatim); the alternation cannot match the `.suggestions =`/`.secondarySpans =` assignments it lists; the census CONCLUSION independently re-verified true |
| K14 | C·D-20's "double-scans whitespace" consequence | **SUSTAINED** — post-skipWhitespace the next unit is >32; the `<=32` branch cannot fire; cost = one O(1) call; the misnomer survives alone (PT-UTILS-15) |

## Superlatives — 11 sustained (L-18 runs both ways)

1. **The module is a true runtime leaf** — 0-import chunk, type-only imports erased under verbatimModuleSyntax, breaking the state→debug→utils paper cycle with TDZ-free global initialization (both readers; my grep: 0 `^import`).
2. **The furthest-offset algebra and per-state threading are genuinely right** — three-way `>`/`===`/`<` split, `includes`-deduped expected-set, the four channels on the ParserState instance; the comment at :20-26 is TRUE for exactly what it claims (the trio), and four of five channels already satisfy O-8 — the hard part of the X·P migration is done (both readers; = pt-diagnostics S-bank).
3. **The V8 shape is stable** — ES2022 class fields make :33 a store, not a shape transition; α re-verified the falsified-hypothesis discipline (= pt-leaf sup 12).
4. **The toggle is not a latch** — :8/:12 two-way, round-tripped in tests; the anti-LATCH contrast with PACKRAT_ARMED shapes the cure (sever the effect, keep the reversibility) — RESCOPED per ruling 8: it still fails O-8 wholesale.
5. **The scanners' machine is right even where their meaning is wrong** — `indexOf("*/", i+2)` memchr-class, allocation-free, monomorphic; the `+2` correctly refuses `/*/` as a closed comment (α's extension — statically certain: search from index 2 cannot see the overlap at 1); value.js independently converged on the identical technique and offset (`stylesheet.ts:438`); and the narrow S-2/S-6 kernels survive here: `expected`'s disarmed non-allocation at :33, the callee-side guard placement at :71.
6. **utils' inline line/column math is the MOST correct of the package's four engines** — the only one returning {1,0} at offset 0 of a newline-leading source; both challenges asked for it to be deleted in favor of the buggy helper; any cure keeps the arithmetic and drops the duplicate copy (β + pt-diagnostics K1, my probe concordant).
7. **The zero-progress guards close the infinite-loop class by construction** — many():538 / sepBy's checkpoints mean a zero-width-sync recover() cannot spin the loop; the cost is the stranded-diagnostic bookkeeping (row 19 identity), not liveness (α, verified in my trace).
8. **collectDiagnostic's inbound defensive copies are load-bearing** — :120-122's three spreads are what keep already-collected Diagnostics from being blanked by the very reset at :127; neither challenge credited why they must exist (α).
9. **trimStateWhitespace's class is the house's near-CSS outlier-detector** — {9-13,32}, one code point (U+000B) from css-syntax-3, live on the default `.trim()` path: the cure for PT-UTILS-2 is a two-line alignment with code that already ships, not a redesign (both readers; my census).
10. **Rollup's export-map pruning is a free dead-surface oracle the project already runs and does not read** — addSecondarySpan/resetErrorState exported in source, absent from the 12-entry map: "exported in source, reachable from no entry" is computed on every build (β; the cheapest cure for the gate gap in PT-UTILS-7's rider).
11. **Challenge-C's §7 keep/wrap/retire table is the corpus's most valuable artifact** — every law anchor it leans on re-verified at its cited line; β's two added rows adopted: RETIRE `Diagnostic.line`/`column` as VALUE fields (freezing a location engine's output into D is what EQ-4 excludes — retiring them dissolves the four-engine disagreement class at once), and score `/core`+`/diagnostics` as TIERS (PT-UTILS-9 and row 3 are tier-layout defects that survive any file split).

## MEASURE-AT-OPEN residue — bench-dependent rows for the lane's W1 harness

Inherited law: one fresh process per cell; nothing armed; every probe disposable-PID; counting units stated beside every pattern (pt-leaf ruling 6); non-ASCII rows use escapes, never raw glyphs (K13-of-pt-leaf).

1. **Disarmed-path allocation magnitude** (PT-UTILS-11 = pt-diagnostics row 22) — 2 arrays/furthest-advance + the 4-array failed-parse floor (parser.ts:52/:61-64): measure on W1's reject leg; heapUsed deltas inadmissible (pt-diagnostics K3 — they see retention, not young-gen churn).
2. **collectDiagnostic O(n·k) line/col cost** (PT-UTILS-21 frame) — n-chars × k-recovered-failures on the recovery leg, UNARMED (the path is ungated); cure candidate measured in the K1-ruled direction only (slice-first arithmetic into state.ts, one prefix copy dropped).
3. **Scanner class sweep** — the 28-code-point over-consumption row set (NUL/C0/ESC) + the U+000B trim delta, escapes only; asserts whichever trivia class W2 cites, against all three shipping classes.
4. **`/utils` door price re-derivation** — byte census re-run with chunk names derived from `dist/core.js:1`'s import specifier at that moment (content-addressed names void on rebuild; this dist is a 2026-07-29 rebuild of 2026-07-03 source).
5. **`/utils` boundary probe table** — the 8-row non-string posture table incl. the boxed-String and numeric-silent-no-op rows, run against the published package, asserting the W2 typeof-at-entry law once it exists.
6. **Zero-width-sync orphan witness** (row 19 identity) — the many():538 trace as an executable corpus row: parse succeeds, buffer length must be 0; RED today by construction.
7. **Subpath closure + declaration parity gates** (PT-UTILS-7/-9 riders) — import shipped `/core` and `/utils` in bare processes: assert the loaded-module set, assert each subpath's `types` stem resolves declarations of the module its `import` stem was built from, and assert the recover lifecycle (write/read/clear) is closed on any subpath that ships any of it.

## Closing verdict

Both fresh reader returns **survive the incredulity seat, and both needed it** — not primarily for their facts, which are byte-exact almost everywhere this seat re-measured them, but for their **frame**: neither reader checked the registry, so between them they re-filed five findings the pt-diagnostics/pt-core/pt-debug/pt-leaf adjudications already own (the line/column clamp, the fourth engine, the renderer gap, the /diagnostics typing wart, the zero-width orphan), re-graded two rows against standing family precedent (L-U-05 MINOR per row 22's kill; the D-17 class MINOR per row 24's compile witness), and split head-on on a superlative pair (S-2/S-6) that the bytes settle for the kill — with the correction that one of the two "live" cost sites both readers cited is the FLAG corpse's dead code, an error shared by the pt-diagnostics record itself and now corrected against all three. The one reader-vs-reader contradiction, four severity contests, six disputed counts, and every load-bearing kill were re-derived at the source by this seat's own instruments; two genuinely new rows survived into the roster from the misses (the /core writer-no-reader tier hole; the D-16 stem collision), and one new row is this seat's own (the writer-dead `"trailing-content"` union member).

The module's adjudicated shape, stated once: **utils.ts is two modules wearing one file** — a diagnostics tier whose every defect is already adjudicated at pt-diagnostics (RETIRE-in-whole, held), and a pair of byte-scanners whose technique is excellent (superlatives 5/9), whose semantics are wrong three ways (class, unterminated-comment posture, boundary), and whose only doors deliver the globals, the latch chunk, and a colliding declaration file. Routing: **W2** owns the algebra-class rows (the scan-union split + citizen scanners PT-UTILS-1/-5, the trivia-class law at the PT-LEAF-7 identity PT-UTILS-2, the labelled unterminated failure PT-UTILS-3, parameter-arming PT-UTILS-8, the type laws PT-UTILS-13/-18); **W4** owns the packaging truths (parity + closure gates PT-UTILS-7/-9/-17/-19); **W1** owns the coverage successor and the seven MEASURE-AT-OPEN rows (PT-UTILS-10); the **REFINEMENT FOLD** takes the boundary respecification (PT-UTILS-4); and **NO-WAVE-OWNER** carries the incumbent repairs on the owner-held O-15 channel, each semver-priced per PT-LEAF-27 (1.1.0: the source split, the doc repairs, the stem rename, the dead-branch deletion; 2.0.0: any predicate, posture, or export retirement). Nothing lost: head, tail, or interval — the cross-book table carries every identity to its primary home under its original name.

— end of apotheosis. Both challenge files and both reader returns remain immutable beside this record per E-1/E-3; sibling adjudications are cited, never rewritten (their one shared stale cell — the `:469` liveness — is corrected here as an addendum-style note, binding on any wave consuming row 14); dispositions are ADJUDICATED, not VERIFIED; execution awaits the owner's begin-word.
