# AdminFlaggedPanel — adjudicated apotheosis · seat model: `claude-fable-5` (Fable 5, observed in this seat's own system prompt, not inherited — recorded per M-23/L-18)

**Seat**: fresh-Fable adjudicator (the incredulity seat) over two hostile re-readers.
**Date**: 2026-08-03 · **HEAD at this seat's reads**: `64aca0a2` (branch `tranche-u`; the corpus was authored at `c654824e`, axis-C at `e39da983` — three HEADs disclosed; every anchor below was re-resolved by this seat against the working tree, and my own reads are the anchor authority).
**Corpus**: 3 challenge axes against `demo/palettes/browser/admin/AdminFlaggedPanel.vue` (153 L) + `demo/palettes/useAdminFlagged.ts` (150 L) — `challenge-D-design.md` (26 rows, Opus 5), `challenge-C-implementation.md` (15 rows + 19 executed repros, Opus 5), `challenge-L-library.md` (11 rows, Opus 5). All three EXISTS-ORIGINAL per HYDRATION-LEDGER — no REPORT-AUTHORED discount.
**Readers adjudicated**: reader-A (design-and-usage) · reader-B (library-and-code). Both were attacked before either was adopted; every verdict-level disagreement (6) was re-derived by this seat with its own instruments; two of reader-B's headline "missed" rows were run against the standing registry and found ALREADY RULED.

**Arbiter's own instruments (all static; zero browser probes — probe parsimony; no repro re-runs needed where source construction decides)**: full reads of AdminFlaggedPanel.vue (whole, 153 L), useAdminFlagged.ts (whole, 150 L), PaginationBar.vue (whole), AdminListSkeleton.vue (whole), dateFormat.ts (whole), EmptyState.vue:1-30/:80-110, AdminNamesPanel.vue:50-65, AdminPane.vue:1-6/:60-70/:115-132, BrowsePane.vue:290-305, usePalettePorts.ts (grep census + wc = 275 L, `flagged` at :73/:191/:229), useAdminAuth.ts:1-45, client.ts:145-165, api-problem.ts:15-40; api reads: `palette/service/flags.ts` (whole — the D-3 decider), `palette/repository/flag.ts:1-130` (the C-F-2/F-3 decider), `admin/service/palettes.ts:1-80`, `admin/auth.ts:35-55`; canon: PALETTE-CONTRACT.md head + :90-110 ("live, product-proportionate palette authority"; §2 categorical localStorage ban); producer bytes: glass-ui 7.0.0 `Skeleton.vue.d.ts` (whole — `class` is the sole prop), `--skeleton-*` grep over the entire dist → **0 hits**, `--type-caption` (fluid-only) vs `--control-text-sm: calc(var(--type-caption) * var(--ui-scale))`, the `pointer: coarse` `--ui-scale: 1.5` / `--control-floor: 2.75rem` block; censuses run by this seat: `tone=` in demo = **3** (PalettesPane:116 Button, subject :81 Badge, AdminUsersPanel:175 Button) · `variant="outline|ghost"` = **29+28 across 26 files** · `reporterSlug` in demo = **1** (types.ts:104) · `beforeEach|meta.admin` readers = **0** · `resting beacon` = AdminNamesPanel.vue:59-60 (line-wrapped — NOT in the subject) · `h-7 px-2` in the subject = lines **10, 90, 96** · PaletteColorStrip importers = PaletteCard + MixSourceSelector + GenerateControls (3 live) · `role="list"` in demo/palettes = PaletteCardGrid only · `.pageSize`/`badgeVariants` external consumers = **0** each · `ml-auto` = 8 / `ms-auto` = 0 · `shots/forced-colors-desktop/` = **5 files** (adminusers, blob, browse, gradient, picker — flagged absent) · fixture `admin-populated.ts:28` type path into nonexistent `demo/@` + `:153` `json("", 204)`. Registry cross-reads: AdminNamesPanel.md (X-map + AdminGate/C-6/D-6/D-8 routings), AdminListItem.md (C-3 list-semantics ruling, AL-M3/M4/M6, M-12 note), AdminListSkeleton.md (**A-3 and A-4 — decisive for two demotions below**), AdminUsersPanel adjudication identities (MT-AU1, MT-BTN1, MT-UI1) as cited by those records; tranche-X wave bounds: X-W3 (G-18 P0, X.W3.6 guard + `admin/service/palettes.ts` in File Bounds, `api/**` = X-W3 property), X-W7 (AdminFlaggedPanel.vue + AdminPane.vue + BrowsePane.vue **modify**; :42-43/:92-96 export swap CLAIMED; :238-243 flagged-palette delete confirmation CLAIMED; :373 the `slice(0,5)` sibling NAMED; G3 strictTemplates), X-W5 (one-route-one-scene), X-W4 (semantic controls), X-W8 (MT-UI1 umbrella + G-9 skeleton attrs + lint-glob re-point), X-W10 (type/motion canon).

## Scoreboard

**42 axis rows CONFIRMED (2 BLOCKER-class) · 15 claims KILLED · 16 reader-missed rows adjudicated (10 booked new, 2 folded into existing rows, 4 resolved to already-ruled identities) · 10 UNPROVEN-NEEDS-LIVE residue items → visual-audit inputs.**

Blocker-weight survivors: **AF-1** (an unauthenticated visitor is told the moderation queue is clear — on film in all four shipped captures) and **AF-2** (a one-click, unconfirmed, un-undoable delete of another user's palette, in a suite whose constitution mandates one dangerous-confirmation grammar). The panel's tell, verified by this seat line-by-line: every major intention in its comments (:20-21, :68-70, :86-89) is written down and false at runtime — through the auth path, through the wire shape, and through a prop glass-ui 7 removed.

## Ruled disagreements (the seat's actual work)

| # | row | reader-A | reader-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| 1 | **D-3** | CONFIRMED-as-filed (MAJOR) | headline KILLED, residual MAJOR | **B wins — RESCOPED-MAJOR** | My read of `api/palette/service/flags.ts`: docstring :5-7 + the 11000→`ConflictError` translation :47-49 establish the unique `(paletteSlug, reporterSlug)` index; :34-36 rejects self-flagging. One person CANNOT report seven times — `flagCount` ≡ distinct reporters by construction, so the axis's headline ambiguity ("seven people vs one person seven times") does not exist, and its cure ("N flags from M reporters") is N≡M always. The residual is real and MAJOR: `reporterSlug` is on the wire (repo `$push`), in the client type (types.ts:104, my grep: the sole demo hit), rendered at zero sites — a cross-palette bad-faith reporter is unidentifiable and unmutable, against VISUAL-CONSTITUTION.md:218 "Actor … remain visible". Reader-A confirmed the axis's consequence without checking the write path — recorded in DISSENT. |
| 2 | **D-16** | quotation KILLED (misattribution), facts refiled MINOR | CONFIRMED, quoting ":88-89 'never a resting beacon on every row'" | **A wins — the quote is a fabricated self-contradiction; B inherited it** | My read of the subject :86-89, verbatim: "…the delete is a QUIET icon (ink at rest, red only on hover/focus), never its equal-weight red twin." The phrase "never as a resting beacon on every row" lives at **AdminNamesPanel.vue:59-60** (line-wrapped, which is why single-line greps miss it — I confirmed with a multiline grep). The axis quoted a different file's comment as the subject's own; reader-B repeated the misquote in its confirm cell — a citation-inheritance event (DISSENT #1). The two template facts survive as an ordinary design judgment at MINOR: a resting `tone="destructive"` count pill on every row (:81) beside a flat `variant="secondary"` plate on `reason` (:112), the only triage datum. |
| 3 | **D-10.1** (specimen scales) | KILLED (not the same job) | CONFIRMED ("two specimen scales in one suite") | **A wins — KILLED** | AdminListItem's `w-8 h-8` slot holds ONE specimen (a single proposed colour, AdminNamesPanel:46); the subject's :52-58 is a STRIP of up to five overlapped discs for a palette. Five 32px discs at `-space-x-1` = 32 + 4×28 = **144px** of leading rail on a ≈462px content strip — a third of the row; the 20px rung is content-driven, and VersionHistoryDrawer:54 uses the same `h-5` rung for the same strip job (in-family consistency). The real defect at this site is L-8 (the strip is not the seam-exported `PaletteColorStrip`) — booked there. Suite-register drift as a genuine concern is the ALREADY-RULED AL-M6 identity (AdminListItem adjudication), not this row. |
| 4 | **D-11 arithmetic** | corrected: jump = 84px, gap survives, `{}` trigger | repeated the axis's "20px + gap-3" | **A wins** | By construction: a five-disc strip at `-space-x-1` (Tailwind v4: negative `margin-inline-end` on all but the last) measures 20 + 4×16 = **84px**; CSS flex `gap` applies between adjacent items regardless of an item's zero width, so `gap-3` is NOT recovered. And per AF-26 the wire ships `palette: {}`, so `(item.palette?.colors ?? [])` is empty in the COMMON deleted case while the :71-75 "palette deleted" annotation does not fire either — collapse and missing annotation co-occur. Defect CONFIRMED-MAJOR with corrected quantity; B's transcription of the axis arithmetic recorded in DISSENT. |
| 5 | **D-15 severity** | MINOR | MAJOR | **MINOR** | The structural fact is certain (no `size` passed; `md` recipe padding around a hand-stamped caption font). But whether the caption size actually WINS is undecided: `text-mono-caption` is a custom `@utility` a tailwind-merge-style resolver cannot classify, so both font-size classes survive and CSS source order decides — reader-A's own unproven cell. A defect whose optical outcome is unproven does not carry MAJOR. Residue U-6. |
| 6 | **reader-B misses #1/#2** (Skeleton dead props; skeleton-ink seam) | — (not filed) | filed as MISSED-MAJOR ×2 | **Demoted to CROSS-BOOK — both are ALREADY RULED** | My read of `registry/adjudicated/AdminListSkeleton.md`: **A-3** rules the `surface="glass" variant="breath"` dead attrs (producer `Skeleton.vue.d.ts` = `class` only — re-verified by this seat) with terminal disposition **X-W8 (X.W8.b · G-9)** under a null-DELTA proof, plus the strictTemplates double-cover note; **A-4** rules the dead `--skeleton-*` token seam (my own dist-wide grep: 0 readers — re-verified) as **GLASS-OWNED → BH relay** (producer half) + **NO-WAVE-OWNER** (the false utils.css:37-39 sentence + E1-R2 prose). The three axes of THIS corpus genuinely missed both — that is a true finding about the corpus — but per the anti-rename law each is ONE identity, counted at the AdminListSkeleton adjudication, never re-booked here. |

**Smaller reconciliations**: L-2 barrel count — 18 one-line barrels of which **17** target the glass-ui root and 1 (`input`) already uses the prescribed subpath spelling, `alert` the comment-bearing outlier; A's "17 onto root" and B's "18 at 1 line" are both true and the axis's "18 onto the root" is not. `flagged` is constructed at usePalettePorts.ts:**73** (A right, B's :75 off by two, immaterial). Reader-A's L-8 cell says "wording overreach noted in killed" but its killed register contains no L-8 entry — a bookkeeping defect in A; the substance is B's L-8 kill (adopted below).

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. X-map per the CARRY-CUT-LEDGER: X-W3 access/route-closure/API-policy (owns `api/**`) · X-W4 semantic controls · X-W5 one-route-one-scene · X-W7 palette domain/CRUD (owns AdminFlaggedPanel.vue, AdminPane.vue, BrowsePane.vue, the export swap, the flagged-delete confirmation, the `slice(0,5)` sibling — all by name) · X-W8 graph/config subtraction (MT-UI1, G-9, lint globs) · X-W10 canonical design+motion. GLASS-OWNED rows leave by the standing BH relay. Line anchors re-resolve before any cure.

### Blocker-class

| id | sev | claim (adjudicated) | disposition |
|---|---|---|---|
| **AF-1** (D-1/L-1/C-F-1) | **BLOCKER-as-filed** | No token ⇒ `if (!token) return` at useAdminFlagged.ts:60-61 BEFORE `loading=true` ⇒ the triple (false, null, []) ⇒ the TRUE-EMPTY plate at :37-41. Zero router-guard readers of `meta:{admin:true}` (my grep: 0); AdminPane.vue:64 mounts on route alone; on film in all four shipped captures (dock `Login` + `0 flagged` + `· NOTHING FLAGGED ·` simultaneously; REPORT.json `failedRequests: []`). The exact P0 the file's own :20-21 comment claims cured. | **ONE identity — the ruled AdminGate (MT-AU1), cross-booked, no new booking.** Guard half = **X-W3** (G-18 P0, X.W3.6 fail-closed `beforeEach`); pane-side unauthorized state + deletion of the 21 `if (!token) return` sites = **X-W7**. Severity note: the AdminNamesPanel cross-book capped this class ("capability inert, no data on the wire" — the server 401s regardless); the cap is honored at the identity while X-W3's own gate keeps it P0. |
| **AF-2** (D-2/C-F-9) | **BLOCKER** | One click on the trash glyph (:93-101) soft-deletes another user's palette — no Dialog, no undo, no announced result (compounded by AF-5's silent failure) — while AdminUsersPanel.vue:157-181/:263-333 gates the same operation class behind a Dialog, authored by the SAME W5-12·F-8 wave. VISUAL-CONSTITUTION.md:218 "dangerous confirmation" is binding suite grammar. | **X-W7** — explicitly claimed (W7.md:238-243 names flagged-palette delete among the destructive Admin seats). |

### Major

| id | sev | claim (adjudicated) | disposition |
|---|---|---|---|
| AF-3 (D-3 rescoped) | MAJOR | `reporterSlug` on the wire and in the type, rendered nowhere — the actor is designed out; a cross-palette bad-faith reporter cannot be identified or muted. Headline ambiguity KILLED (ruling #1). | **X-W7** (row anatomy: `reporter · reason · when`). |
| AF-4 (D-4/C-F-4) | MAJOR | The :16/:22/:37 chain excludes the :44 `v-for` (no `v-else`): error plate + stale interactive rows render together; refresh paints 2 skeletons above N stale rows; the catch never clears `items`. C's R1/R2 executed repros + my read of the template concur. | **X-W7** (one derived state union, template total over it). |
| AF-5 (D-5/C-F-7) | MAJOR | Both mutation catches are `console.warn` and nothing else (:88-90, :100-102); no pending/failed/succeeded state; glass-ui 7 ships `loading?: boolean` on Button, used zero times. B's categorical "no live region" KILLED (PaginationBar:18-20 mounts one, hidden at pageCount≤1) — the substantive claim stands: nothing announces a moderation RESULT. | **X-W7**. |
| AF-6 (D-6/C-F-8) | MAJOR | `variant` is not a glass-7 Button prop (d.ts re-read; compiled defaults `emphasis:"secondary"`); all four buttons render `data-emphasis="secondary"` — the :86-89 asymmetric-weighting doctrine does not render. Census (mine): 57 occurrences / **26** files / 21 in the admin cluster (C's "36 files" killed; B's "essentially all 28 ghost are dead Button props" killed — 8 ride WatercolorDot where `variant` is real). Cure correction sustained: `emphasis="secondary"` is the DEFAULT — writing it is a byte-identical no-op; the landing cure is `emphasis="quiet"` + `tone="destructive"` + `size="xs"`/`iconOnly`. | Subject sites → **X-W7** rebuild (G3 `strictTemplates` makes the class a compile error); fleet = the ALREADY-RULED **MT-BTN1** identity (AdminUsersPanel adjudication) — one identity, no re-book. |
| AF-7 (D-7) | MAJOR | `h-7` loses to `.button { min-block-size: var(--control-h-sm) }` = 36px; on coarse pointers `--ui-scale:1.5`/`--control-floor:2.75rem` (my token reads) yield the ≈27×54 portrait lozenge; the producer's `size="xs"` (28px) and `iconOnly` rungs exist unused. Site enumeration corrected: **three** sites (:10, :90, :96) — :29 carries none, so Retry takes the producer's `sm` padding while its siblings take a hand-stamped 8px: two accidental control widths (A's kill, sustained by my grep). | **X-W7**. |
| AF-8 (D-8) | MAJOR | ≈74% of the toolbar is void between the only datum and its only control — `flex-1` at :8 sits BETWEEN them; AdminAuditPanel:23-30 composes the same strip the other way. A's independent re-measure off the shipped capture reproduces the axis to ~1px. | **X-W7**. |
| AF-9 (D-9) | MAJOR | The loading shadow is a single-tier AdminListItem grammar standing in for a two-tier row whose height grows with `item.flags.length`, and `v-for="i in 2"` against `pageSize = 20`. | **X-W7** (call-site shape/count; must NOT ride X-W8's null-DELTA G-9 unit — the AL-M4 caution). |
| AF-10 (D-10 main) | MAJOR | The panel opts out of the one review-row anatomy (AdminListItem consumed by Names/Users, hand-rolled here at :44-123), and the only row carrying destructive commands has no hover/focus-within/transition affordance (:47 vs AdminAuditPanel:62). Sub-limbs D-10.1/D-10.2 KILLED separately. | **X-W7** (row-anatomy rebuild; AL-M6/AL-M8 kindred identities live at the AdminListItem adjudication). |
| AF-11 (D-11) | MAJOR | Deleted-palette rows collapse the left anchor: a zero-child flex strip is zero-wide; jump corrected to **84px** with `gap-3` surviving (ruling #4); trigger broadened — the `{}` wire makes this the COMMON case and the annotation dead simultaneously. | **X-W7** (reserve the slot unconditionally — the AdminListItem construction). |
| AF-13 (D-13) | MAJOR | Fraunces on control labels — `font-display` on Dismiss (:90) and Retry (:29) — against the closed type matrix (VISUAL-CONSTITUTION.md:75: controls = Plus Jakarta Sans); `text-caption` is not a control role at all. | **X-W10** type-matrix sweep (AdminNamesPanel D-8 identity family); the subject's sites also die locally in the X-W7 rebuild. |
| AF-14 (D-14) | MAJOR | The reporter's free-text prose set in the mono voice, single-line `truncate`, no `title`, and by flex arrangement the ONLY item that can lose width (:112 `shrink-0`, :118 `ml-auto shrink-0`). | **X-W7** (canon authority X-W10). |
| AF-26 (C-F-2/F-2 + B-miss #6 folded) | MAJOR | The wire sends `palette: {}` never `null`: the `$project` at flag.ts builds a computed sub-document, `$unwind` preserveNullAndEmptyArrays, so a lookup miss emits an empty document — truthy; `v-if="!item.palette"` (:72) is unreachable dead code and the :68-70-forbidden bare-slug-empty-strip state ships. THREE-way contract fiction (B's missed row folded in): api's own type says `palette?: … | undefined` under a docstring teaching absence (my read :17-41), the client type says `| null`, the wire says `{}` — the PRODUCER's type is as wrong as the consumer's, so correcting types.ts alone leaves the fiction taught upstream. | Client branch (`item.palette?.slug`) + client type to wire truth = **X-W7**; api `$project` null-normalization + the producer type/docstring correction = `api/**` → **X-W3 rider (unclaimed by name — File Bounds do not list flag.ts)**, else NO-WAVE-OWNER at the formation boundary. |
| AF-27 (C-F-3/F-3) | MAJOR | Delete does not clear the queue: service soft-deletes and never touches `flags` (my read — its :6 docstring CLAIMS "cascade votes/flags"; the body cascades nothing; `deleteByPaletteSlug(slug, session)` sits unused in the same repo file), the `$lookup` has no `deletedAt` filter so the soft-deleted palette rejoins fully populated, `total` derives from `flags` and cannot change — while the client optimistically splices. The row returns on refresh, looking alive; the queue is unclearable by its own primary destructive action. | Client half (server-truthed mutations + reload with page clamp) = **X-W7**. Server half: `admin/service/palettes.ts` IS in X-W3's File Bounds (modify) and X.W3.6/G-17 rework it — but the flag cascade is unclaimed by name → **X-W3 rider**, else NO-WAVE-OWNER; the docstring lie must be corrected by whichever wave touches the file. |
| AF-28 (C-F-5/F-5) | MAJOR | Emptying the last page strands the panel: mutations decrement `total`, `pageCount` collapses, PaginationBar unmounts (`v-if="pageCount > 1"`), `page` stays high, refresh re-requests the empty offset forever — "No flagged palettes." over N remaining. C's C3 executed repro; mechanics re-read by this seat. | **X-W7** (same reload-with-clamp cure as AF-27). |
| AF-29 (C-F-6/F-6) | MAJOR | No request identity: no sequence token, no AbortController, unawaited `loadFlagged()` from next/prev, boolean `loading` cleared by the first `finally` — out-of-order paints the wrong page; Refresh carries no `disabled`/`:loading`. (The transport-timeout residual is the distinct MT-AU1 §3(4) NO-WAVE-OWNER identity at the AdminListSkeleton adjudication — not re-booked.) | **X-W7**. |
| AF-30 (C-F-10/F-10) | MAJOR | The gate is vacuous: zero unit/component tests (my grep concurs), presence-only e2e, nothing clicks Dismiss/Delete; fixture rot verified by this seat — `admin-populated.ts:153` answers `DELETE …/palettes/…` with `json("", 204)` while the live route returns 200 `{deleted:true}` and client.ts:155 calls `res.json()` unconditionally (empty body throws), and `:28` type-imports from `demo/@/lib/palette/types` which does not exist (type-erased, unchecked — e2e sits in no tsconfig program). | Fixture/CI half = **X-W1** (CC-031 fixture repair + hard e2e); behavioral battery (five states + click-through, the six named mutations must fail it) = **X-W7 rider** on its G13/G14 battery (the AL-M10 precedent). |
| AF-35 (L-2) | MAJOR | `demo/ui/` is a 19-directory alias layer owning nothing (17 one-liners onto the root, `input` already on the subpath, `alert` the outlier — reconciled count); the dual path is live inside single `<script setup>` blocks; the subject reaches the shim at :139-140. Dev-server byte figure stays labeled dev-only (axis's own honesty clause). | Subject's two lines die in the **X-W7** rebuild; the layer's deletion = the standing **MT-UI1** identity under **X-W8** — one identity, no re-book. |
| AF-36 (L-3) | MAJOR | The three standing boundary lint rules are dead — the banned specifier group names the pre-W43 alias form and the `demo/@` globs match nothing (`ls demo/@` → absent, my run) — while `browser/index.ts:5-8` cites them as live law; the banned class is present in the subject (:142 raw cross-area `.vue` into barrel-less `demo/shared/ui/`). A's mechanism correction sustained: the :229-235 object also carries LIVE `demo/color-picker/**` globs — an active rule with a dead pattern, not an unmatched glob. | **X-W8** umbrella — ALREADY RULED into the AdminUsersPanel wave's substrate item (globs re-pointed at the live tree); cross-booked, no second booking. |
| AF-37 (L-4) | MAJOR | The ports refactor: 275 L where the god module was 153; adminPort 34 / browsePort 32 members; the header's "no consumer injects a member outside the port it named" falsified — `flagged` provided on BOTH ports from one instance (:73 → :191 and :229) with both reaches live (subject :149-150; BrowsePane:298); the subject injects 34 members to consume one. | **X-W7** (partition, not covering) — cross-booked to the AdminUsersPanel adjudication's adminPort five-seam BANK. |
| AF-38 (L-5) | MAJOR | `useAdminFlagged` owns two concepts with two audiences — the token-gated moderation queue and the area's one unauthenticated write path (`report`, :119-131) — in a single shared instance a browsing visitor reaches. | **X-W7** (split `useFlagReport` / `useFlaggedQueue`; `flagged` leaves browsePort, killing AF-37's dual injection). |
| AF-39 (L-6) | MAJOR | Offset pagination duplicated byte-for-byte between useAdminFlagged (:49-57/:105-117) and useAdminAudit; `PaginatedResponse<T>` and the `useFilteredList` extraction idiom already exist in-directory. | **X-W7**, sequenced after the M-LC1 ownership convention (the AdminNamesPanel L-6 precedent — grafting a generic onto three load conventions is the banned contrivance). |
| AF-42 (L-9) | MAJOR | The export dual path is live: `export.ts` (4,184 B) ships the app, `export/` (12 modules) is the byte-exact contract whose sole consumer is a test, deliberately name-dodging; `./export` names a file AND a directory. | **X-W7 — explicitly CLAIMED** (W7.md:42-43 + File Bounds :92-96: delete `export.ts`, route `usePaletteExport` at the serializers). |

### Minor / Info

| id | sev | claim (adjudicated) | disposition |
|---|---|---|---|
| AF-12 (D-12) | MINOR | Full box rule (:47 `border-card-edge`) + internal rule 8px lower (:106 `border-border/50`) — two mints — in a register PR-05 zeroed. A's demotion sustained: the boxed-row grammar is suite-wide (= the AdminListItem D-5 NO-WAVE-OWNER identity, cross-booked); the file-local delta is the inner rule only. | Inner rule → **X-W7**; suite grammar → the AdminListItem D-5 identity. |
| AF-15 (D-15) | MINOR | Badges take `md` padding around hand-stamped caption type at :81/:112; `size="sm"` is the shape being hand-drawn. Severity ruled at #5; optical outcome = residue U-6. | **X-W7**. |
| AF-16 (D-16 refiled) | MINOR | A resting saturated destructive pill on a non-actionable count while `reason` — the only triage datum — is one flat plate, the `tone` axis unused on it. The "internal contradiction" framing is DEAD (ruling #2). | **X-W7**. |
| AF-17 (D-18/C-F-12) | MINOR | `aria-label` on a role-less div is not exposed; two skeletons each announce a bare "Loading". Family: AdminAuditPanel:36, AdminNamesPanel:26. | **X-W4** — the ALREADY-RULED AdminNamesPanel D-11/C-9 identity (one wrapper `role="status"` + the skeleton atom drops its per-instance role at X-W8); cross-booked. |
| AF-18 (D-19) | MINOR | Provenance ambiguous by a year (`formatDate` = month+day only) in the one queue whose ordering signal is recency; the sibling uses `formatTime` for the same class of datum. | **X-W7** (one provenance format). |
| AF-19 (D-20) | MINOR | No sort/filter/facet in the one panel that is a work queue; server-side `listFlagged(limit, offset)` takes no sort/filter argument either. | Client → **X-W7**; server argument surface = `api/**` → NO-WAVE-OWNER rider. |
| AF-20 (D-21 truncation limb) | MINOR | `.slice(0, 5)` with no `+N` residue mark — while VersionHistoryDrawer:59-63 renders exactly that mark for the same idiom in the same feature. Occlusion-direction limb KILLED (reversed — disc 1 is the most occluded). | **X-W7 — named by the wave itself** (W7.md:373). |
| AF-21 (D-22) | MINOR | Zero motion vocabulary on the panel whose actions are irreversible — rows vanish on the next frame. The axis correctly refused to launder absence into PRM compliance; **no motion-quarantine row names AdminFlaggedPanel and no FALSE row (M-03/M-07/M-17/M-26/M-29 cited by the reader as the inverse-error precedents) is carried here** — quarantine law honored. | **X-W7** rebuild rider; motion canon authority X-W10. |
| AF-22 (D-23) | INFO | The Admin route is ≈36.4% of main beside a co-equal companion, with the mobile pane selector alive — against the constitution's full-main-width order. Owner is the route composition, not this file. Card-height figures = residue U-1. | **X-W5** — the AdminNamesPanel D-4/D-1.3 viewSchema identity, cross-booked (this route's row dies in the same edit). |
| AF-23 (D-24) | INFO | The same nullity asserted FOUR times (count, ghost trio, eyebrow, display line — the reader's undercount correction adopted; ghost limb booked as AF-47). | **X-W7** (subtraction precedes explanation). |
| AF-24 (D-25) | INFO | One class of datum, two homes: header Badge for Users/Names, in-body toolbar count invented for Flagged/Tags/Audit (`adminCount` default: null — my read :117-131). | **X-W7** (one count, one home — the AdminNamesPanel D-6 identity). |
| AF-25 (D-26) | MINOR | `item.flags` unbounded in the template AND in the aggregation (`$push` with no `$slice` — my read); the only write-side bound is one-flag-per-reporter (B's resolution of the axis's open cap hypothesis, verified by my flags.ts read). | Client cap/disclosure → **X-W7**; api `$slice` → NO-WAVE-OWNER rider. |
| AF-31 (C-F-11 + B-miss #8 folded) | MINOR | BOTH `formatDate` AND `formatTime` carry dead try/catch (neither `toLocaleDateString` nor `toLocaleString` throws on Invalid Date — the string ships to screen); C named only half the module, B's fold adopted (formatTime feeds AdminAuditPanel:71). | **X-W7**. |
| AF-32 (C-F-13) | MINOR | Removing a row strands keyboard focus on `<body>`; the file registers no focus management (whole lifecycle = `onMounted` :152); ErrorBoundary.vue models the cure in-repo. Exact post-unmount target = residue U-8. | **X-W7**. |
| AF-33 (C-F-14) | MINOR | The count Badge is a bare unlabelled number; the toolbar count publishes "0 flagged" while loading and while unauthenticated — outside the `v-if` chain — against the A-3 law its own parent enforces in a comment (my read of AdminPane :117-122). | **X-W7** (dies with AF-4's total template + AF-24's one-home count). |
| AF-34 (C-F-15) | MINOR | Inline `style="font-variant: small-caps"` (:74) for a named register; `h-7 px-2` per instance; the `@click` binding split (:10 handler-ref vs :29 call, again at :131-132). Cursor sub-claim RESOLVED (A): the producer's `.button` ships `cursor: pointer`, so :96's copy is redundant noise, not three missing cures. | **X-W7**. |
| AF-40 (L-7) | MINOR | PaginationBar and AdminListSkeleton are generic controls homed in `admin/`, absent from the seam (index.ts exports exactly the five panels; B's extension: AdminListItem is a third unexported). | **NO-WAVE-OWNER** — homing is unclaimed by any X unit; must not ride X-W8's null-DELTA skeleton edit (AL-M4 caution). Booked for the formation boundary. |
| AF-41 (L-8) | MINOR | Four parallel "render palette colours" implementations, two near-identical; the seam-exported `PaletteColorStrip` is not used by any of the three hand-rolls. B's framing kill sustained by my grep: the named concept has THREE live consumers (PaletteCard, MixSourceSelector, GenerateControls), so any `variant`/`max` growth changes three existing render sites — the cure's scope is wider than the axis stated. | **X-W7** (with AF-20). |
| AF-43 (L-10) | INFO — POSITIVE | The demo dogfoods only the published value.js surface; zero `src/` reaches; the self-alias derives from `package.json#exports`; the subject imports value.js not at all, correctly. Same mechanism the ColorSpaceSelector apotheosis crowned. | No action. |

### Adopted from the readers' missed registers (new bookings)

| id | sev | claim (verified by this seat) | disposition |
|---|---|---|---|
| AF-44 (A-miss #1) | MAJOR | N identically-named "Dismiss" buttons with no programmatic association to their palette (:90 — visible label only; the row has no role/labelledby; :65 is a plain span), while the rarer destructive twin IS disambiguated (:97). `namelessButtons: 0` measures existence, not distinctness — all three axes cleared the route on it. | **X-W7** (per-row `aria-label` interpolation, the :97 pattern). |
| AF-45 (A-miss #3) | MAJOR | `text-caption` on the panel's only labelled command opts the LABEL out of the coarse-pointer scale: `--type-caption` is viewport-fluid only, never `--ui-scale`-multiplied (my token reads), while the box grows to 54px — a 1.5× box/type gap `size="xs"` (whose font IS `--control-text-sm = calc(var(--type-caption) * var(--ui-scale))`) closes for free. D-7 found the height arm, D-13 the family, D-15 the same distinction on Badge — none carried it to the Button label. Ripples through `text-mono-caption` at :73/:81/:112/:118. | **X-W7** (dies with AF-7). |
| AF-46 (A-miss #4) | MINOR | The swatch separator ring is minted from the page paper (`border-background` → `--neutral-0`, 4%-lightness in dark) while sitting on the Card plate (`--card`, 17%-lightness) — the only thing making the overlap legible is drawn against a surface that is not there; AdminNamesPanel:46 uses the owned `border-card-edge`; VersionHistoryDrawer:54 repeats the flagged spelling (two-site family). | **X-W7**. |
| AF-47 (A-miss #5) | MINOR | The TRUE-EMPTY plate seats the specimen-plate INVITATION register (dots default true; EmptyState's own header names the affect) on a moderation queue whose good state is emptiness — the fourth simultaneous nullity mark. The cure is a real design question for the atom's owner: EmptyState.vue restricts shedding to one sanctioned reason (my read of the `dots` doc comment), so `:dots="false"` is not a legal call-site patch. | **NO-WAVE-OWNER** (atom-owner design question; EmptyState sits in `demo/shared/ui/`, outside X-W7's named claims) — formation boundary, beside AF-40. |
| AF-48 (A-miss #6) | MINOR | `ml-auto` at :118 is a physical property pinning a row-end datum inside an audited RTL matrix (states.mjs :25-26/:50 — my read): under `dir=rtl` the auto margin absorbs the free space at the row's END side, so the date clusters against the detail and the void lands at the wrong edge; `ms-auto` = 0 uses repo-wide vs 8 `ml-auto`. Flagged has never been captured in RTL (residue U-2). | **X-W7** (one token). |
| AF-49 (A-miss #7) | MINOR | The palette name (:65) and owner slug (:76) both `truncate` with no `title`/wrap/expansion — and :97's delete label interpolates the very string the moderator may only ever have seen clipped, so the unconfirmed destructive command (AF-2) names a target the operator cannot fully read. | **X-W7** — the AL-M3 identity (AdminListItem adjudication) extended to this file's surfaces; cross-booked. |
| AF-50 (A-miss #8) | MAJOR | The admin bearer lives in `localStorage` (useAdminAuth.ts:15/:21-23/:35-37 — my read) against PALETTE-CONTRACT §2's categorical ban ("no … localStorage … auth path in the browser" — the contract's head declares itself the LIVE authority; §1-§4 are W45-owned). The corpus filed it only as a parenthetical, and every proposed gate cure (including X-W3's own X.W3.6, which reads "the existing `useAdminAuth` state") is built ON TOP of the violating store. | **NO-WAVE-OWNER** at the formation boundary — no X wave's bounds claim the auth substrate; recorded beside X-W3 so its guard landing is never read as discharging the contract row. |
| AF-51 (B-miss #3) | MAJOR | An expired/wrong token renders as a backend outage: api 401/403 (my read of admin/auth.ts:43-51) → `ApiProblem` whose `.message` is the problem title → `loadError` → the FIXED sentence "The flag queue is unreachable." with only a forever-failing Retry; `logout()` exists and is unreachable from this surface. Both axes' negative proofs cleared the error path by mistaking a 403 for an outage. | **X-W7** — extends the AdminNamesPanel C-6/L-9 identity (typed `instanceof` dispatch over the discriminated transport errors; useAdminFlagged's `catch (e: any)` is a further site of that family). |
| AF-52 (B-miss #4) | MAJOR | The user-facing flag report succeeds unconditionally at the call site: BrowsePane:296-300 awaits `report()`, discards the `{flagged} | undefined` return, closes the dialog — while `report()` returns `undefined` from its catch — so a 409 "Already flagged" and a 401 both read as a filed report. Real reachable failures (my flags.ts read: ConflictError, AuthenticationError). | **X-W7** (BrowsePane is in its File Bounds; the cure lands with AF-38's split). |
| AF-53 (B-miss #5) | MAJOR | Singleton state survives unmount: re-entering `/#/admin/flagged` re-requests the stale `page` and paints the PREVIOUS visit's rows as live until it resolves (the `v-for` is outside the chain, AF-4), with the AF-28 trap reachable by navigation alone. C's "composition-root singleton, so remounting does not multiply state" was true about multiplication and false as a clearance. | **X-W7** (state reset on mount or route-keyed state; dies in the same machine as AF-4/AF-28). |
| AF-54 (B-miss #7) | MINOR | Dead widened surface around the subject: `pageSize` exported by two composables with zero external consumers (my grep), `badgeVariants` re-exported by the shim with zero consumers — the alias layer widens the surface it forwards. | `pageSize` dies in AF-39's extraction (**X-W7**); `badgeVariants` dies with MT-UI1 (**X-W8**). |

**Cross-booked identities (recorded, counted elsewhere — no new bookings)**: AF-X1 list semantics (A-miss #2; verified — zero `role="list"` in the admin cluster vs the documented PaletteCardGrid precedent) = the AdminListItem **C-3 ≡ D-1** ruling (MINOR, containers + `listitem`, route X-W7) extended to this panel's containers. AF-X2 Skeleton dead attrs (B-miss #1) = **A-3** at the AdminListSkeleton adjudication (X-W8 G-9). AF-X3 the dead `--skeleton-*` seam (B-miss #2) = **A-4** there (GLASS-relay + NO-WAVE-OWNER; the E1-R2 certification may not be cited for this component until re-measured). AF-X4 truncate/no-title = AL-M3 (carried as AF-49's identity note).

## Killed-claims register

1. **D-3 headline (BLOCKER framing)** — "seven people vs one person seven times indistinguishable; brigading structurally invisible": impossible by backend construction — unique `(paletteSlug, reporterSlug)` index + self-flag rejection (flags.ts, my read); `flagCount` ≡ distinct reporters; the "N flags from M reporters" cure is N≡M. Residual re-filed as AF-3.
2. **D-16 quotation** — "the resting red directly contradicts the panel's own stated doctrine `:88-89` *red only on hover/focus, never a resting beacon on every row*": the phrase is AdminNamesPanel.vue:59-60's, line-wrapped; the subject's :86-89 says "never its equal-weight red twin". The self-contradiction payload is fabricated by misattribution. Facts re-filed as AF-16.
3. **D-10.1** — "same suite, same job, 20px vs 32px": not the same job (one specimen vs a five-disc strip; 32px discs would cost 144px of rail); the strip family is internally consistent at `h-5`; the true defect is AF-41.
4. **D-10.2** — "the row at :47 has no `min-w-0` — it inherits the ~850px-at-390 blowout": refuted by construction — `overflow-hidden` zeroes the automatic minimum size (CSS Sizing 3 §5.2) exactly as `min-w-0` does; AdminListItem needed the explicit class precisely because it declares no overflow; the inner column carries `min-w-0` besides (:61). Arguably a superlative misread as a defect (see superlative #1).
5. **D-21 occlusion direction** — "the 5th disc is partly hidden behind the 4th": reversed — `-space-x-1` compiles to a negative `margin-inline-end` on all but the last child, pulling each SUCCESSOR (which paints on top) over its predecessor; disc 5 is fully visible, disc 1 the most occluded.
6. **D-7 site enumeration** — "`h-7 px-2` at four sites (:10, :29, :90, :93)": three sites (10, 90, 96 — my grep); :29 carries no geometry, which yields the sharper finding (two accidental control widths) the miscount hid.
7. **D-11 arithmetic** — "width 0 … jumps 20px + gap-3": the jump is 84px and `gap` survives a zero-width item; also mis-triggered ("when `item.palette` is null") — the `{}` wire is the reaching case. Corrected inside AF-11.
8. **C-F-8 blast radius** — "29 outline + 28 ghost across **36 files** … essentially all 28 ghost are dead Button props": 26 files (my census, agreeing with D's 57/26); and 8 of the ghost/outline occurrences ride WatercolorDot where `variant` is a real prop, plus `outline` is a valid glass-7 **Badge** variant — a sweep executed against "all 57" would break valid props.
9. **D-6 cure half** — "`emphasis="secondary"` … at every site, in one sweep": a no-op — secondary IS the compiled default (C's own DOM dump shows `data-emphasis="secondary"` today). The asymmetry cure is `quiet`/`tone`/`size`/`iconOnly`, and the 29 outline sites need a decision, not a restatement of the default.
10. **C-F-7 parenthetical** — "no live region": PaginationBar:18-20 carries `aria-live="polite" aria-atomic="true"` and mounts unconditionally from :126-133 (hidden only at pageCount≤1 — the fixture artifact behind the axis's R6 zero-count). The substantive no-announced-result claim survives in AF-5.
11. **L-11 headline** — "`:81` is the only `tone=` usage in the entire demo": three sites (my grep — PalettesPane:116, subject :81, AdminUsersPanel:175). The narrower Badge-only claim is true and survives as superlative #3.
12. **L-3 mechanism sentence** — "the file globs match zero files; all three rules are inert": the :229-235 object leads with live `demo/color-picker/**` globs — an ACTIVE rule with a DEAD banned-pattern, a different failure mode. The verdict (dead law cited as live) survives in AF-36.
13. **L-2 barrel count** — "18 of 19 barrels … re-exporting the glass-ui root": 17 target the root; `input` is one line onto `/forms` — exactly the prescribed spelling — and the row's own body concedes it. Structural verdict unaffected (AF-35).
14. **L-8 framing** — "the one with a name, a home, and a public export is used by none of them": PaletteColorStrip has three live consumers (my grep) — materially misleading for the cure's scope (AF-41 carries the correction).
15. **D-17 evidence-gap enumeration** — "`shots/forced-colors-desktop/` contains only `adminusers.png`": five files (my `ls`). The material claim — Flagged never observed in forced-colors/RTL/zoom/keyboard/reduced-motion — is TRUE and drives residue U-2.

## Superlatives (with provenance)

1. **`overflow-hidden` at :47 is the F-1 grid-track cure by a mechanism neither the sibling nor three axes recognised** (reader-A; ratified by this seat against CSS Sizing 3 §5.2). The row is structurally immune to the blowout that made the moderation flow INOPERABLE at 390px — a present cure, spelled differently. The single most-inverted judgment in the corpus.
2. **The W5-a11y floor held** (both readers; corroborated by REPORT.json `namelessButtons: 0` across four matrices): every icon-only command named (:10, :97), every glyph `aria-hidden` (:11, :100) — the only in-code claim in this file that survived contact with the tree unamended.
3. **:81 is the demo's glass-7 `tone`-axis migration frontier** (both readers; census corrected to 3 `tone=` sites, this the only Badge one) — and the same template speaks both axes correctly (`tone` for a status at :81, `variant="secondary"` for a style plate at :112), exactly as the producer's doc comment prescribes. The file is simultaneously the most- and least-migrated in the suite.
4. **D-22's refusal to launder absence into PRM compliance** (reader-A) — "that is not compliance, it is absence" is precisely the discrimination the motion-quarantine spent its FALSE rows enforcing; no quarantine row names this file, and the axis earned that by declining to manufacture one.
5. **`EmptyState.vue` — the best-reasoned atom in the graph** (both readers; key lines re-read by this seat): two species unconflatable by construction (`role="alert"` error arm that deliberately sheds the invitation; `role="status"` empty arm), machine truth quarantined to a mono `detail`, and the certified `--ink-muted` rung threaded once for all eight consumers — with the caveat that AF-47 shows the invitation register can still be seated in the wrong affect by a consumer.
6. **`PaginationBar.vue`** (reader-A; re-read whole by this seat): `aria-live="polite" aria-atomic` on the page sentence, both icon buttons named, `:disabled` correctly bound — the one control in the subtree that announces its own state change, and the falsifier of C-F-7's categorical claim. (It also carries two of the family's dead `variant="outline"` attrs — nothing is wholly clean here.)
7. **`api/palette/service/flags.ts:26-51`** (reader-B; load-bearing for kill #1): the one place in the request path where an error is CLASSIFIED — self-flag → ValidationError, missing palette → NotFoundError, 11000 → typed ConflictError behind the unique index. The constraint that killed D-3's headline.
8. **The derived port types** (reader-B): `PalettePorts = ReturnType<typeof providePalettePorts>` — a port's declared surface cannot drift from what is provided; the same header refuses a free back-compat re-export. The partitioning is wrong (AF-37); the type mechanism is exemplary.
9. **`vite.config.ts` deriving the self-alias set from `package.json#exports`** (reader-B) — the same structurally-incapable-of-drift mechanism the ColorSpaceSelector apotheosis crowned; here it delivers AF-43's clean census.
10. **The comment discipline is why 153 lines sustained three axes and two re-readers** (reader-A; endorsed with the inversion): :15, :20-21, :68-70, :86-89 each name the wave, the finding id, and the reason — every false claim killed and every true one confirmed was reachable only because the author wrote the intention beside the code. The defect pattern of this whole corpus is that the intentions were never verified against a rendered state; the writing is what made the verification possible.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **U-1** — D §0's card-height cells (`card 512.0 × 774.0`, `dead height 166.2px`) do not reproduce against the WebKit capture (≈512 × 515 CSS px, reader-A's re-measure); the toolbar arm reproduces to ~1px, calibrating the block. Different engine/session; needs a live Chromium re-measure. (Card WIDTH and the 36.4% ratio are reproduced — AF-22 stands on those.)
- **U-2** — Flagged has NEVER been captured in forced-colors, RTL, 200% zoom, keyboard-focus, or reduced-motion (my `ls` across the six state dirs). D-17's forced-colors erasure (swatches + hover-only destructive semantics) and AF-48's RTL mis-pin are both mechanism-certain, render-unwitnessed. Highest-value single capture: `/#/admin/flagged` populated, forced-colors + rtl-desktop.
- **U-3** — D-10.2's 390px populated-queue overflow probe: mechanism REFUTED statically, so no longer load-bearing; the axis's hedged `overflowX: 0`-on-an-empty-card observation remains formally undischarged.
- **U-4** — D-22's pointer-landing hypothesis (post-delete reflow lands a different palette's delete under a stationary pointer; variable row heights make it plausible) — needs a populated live queue.
- **U-5** — the composited contrast of the `text-muted-foreground` toolbar count on THIS card in both schemes: the structural retirement history is fact (EmptyState `.plate-ink`); the ratio here was never measured, and per the AdminListSkeleton adjudication the E1-R2 certification may not be cited for this component until re-measured (one identity with its residue #1).
- **U-6** — whether D-15's optical conclusion renders: `text-mono-caption` is a custom `@utility` the class-merger cannot classify as a font-size; the winner is CSS source order. Needs `getComputedStyle` on a rendered Badge.
- **U-7** — mobile control geometry: 28×54 by construction, ≈27×54 measured off the shipped raster, ≈27×50 by D's live probe — the 4px delta (`--ui-coarse-scale` override vs Safari emulation artifact) is unresolvable from static bytes plus a raster.
- **U-8** — F-13's exact post-unmount focus target (the absence of any focus management is source-certain; the landing spot is live behaviour).
- **U-9** — C's three labelled hypotheses stay open live-only questions, carried un-booked: H-1 (unreproduced cold-boot render throw swallowed silently by ErrorBoundary), H-2 (a ~1.26s repeating admin poll in Names territory), H-3 (query-string-before-hash defeating route resolution — router seat).
- **U-10** — whether the raw `var(--muted)` skeleton ground (post-A-4: the certified ink recipe is severed) still reads as shadow-not-content on this card in both schemes — one identity with the AdminListSkeleton residue; listed here because this panel mounts two of the shadows.

## DISSENT — preserved, not ruled away

1. **Citation-inheritance events (evidence about the fleet, not the tree).** Reader-B's D-16 confirm cell repeats the axis's misattributed ":88-89" quote verbatim, and its D-11 cell repeats the axis's "20px + gap-3" arithmetic — both corrected by reader-A and independently re-derived false by this seat. B's OTHER cells on the same files (the member-count re-tally, the `{}`-wire broadening) are genuinely its own. The verdict classes survive; the inherited cells did not. Same lesson as the ColorSpaceSelector DISSENT #1: verbatim repetition of a challenger's quantitative cell without re-derivation is detectable.
2. **Reader-A's inverse event**: A confirmed D-3's consequence as filed without checking the write path that forbids it — the one place B out-verified A. Neither reader dominates; the tri-fold's premise held.
3. **Severity friction, preserved**: B ranked D-15 MAJOR on the producer-rung argument; ruled MINOR here because the optical outcome is undecided (U-6). B's reading revives if the live probe shows the md-padding/caption-type mismatch rendering as filed.
4. **The AdminGate cap**: this corpus filed AF-1 as BLOCKER ×3; the standing cross-book (AdminNamesPanel C-5/L-10) carries the "capability inert, no data on the wire" cap. Both are recorded; X-W3's G-18 keeps the route-closure P0 regardless of the label — the friction is about the word, not the work.
5. **Reader-A's phantom kill pointer** (L-8 "overreach noted in killed" with no such entry) — a bookkeeping defect in an otherwise disciplined return; the substance existed only in B's register. Recorded so the next formation's return contract can require kill-register ids to resolve.

— end of apotheosis. The three challenge files, the repro directory, and both reader returns remain immutable beside this record per E-1/E-3; this file is the corpus's single adjudicated exit.
