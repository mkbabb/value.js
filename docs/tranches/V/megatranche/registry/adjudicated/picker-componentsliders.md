# picker-componentsliders — adjudicated apotheosis (M-23/L-18, arbiter-F) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.

**Seat**: fresh Fable adjudicator (M-23/L-18 quartet-challenge apotheosis).
**Date**: 2026-08-07 · **HEAD**: `ee9474e4` · branch `tranche-u` · subject `demo/picker/controls/ComponentSliders/ComponentSliders.vue` (395 lines) + colocated `ConsoleRail.vue`, `composables/{useSliderTouchGates,useSliderAnnouncements,sliderAnnouncement}.ts`.
**Pin check (this seat's own run)**: `shasum -a 256` → `a61b5ed39703af205d6ba0f4923d32daeaaf55cf9c2e21e22030a01fa82ef327` — byte-identical to the CARRY-LEDGER §D pin. **NO DRIFT** across the whole challenge→re-read→adjudication arc.
**Corpus**: 3 seat-authored challenge axes (challenge-C/D/L, all Opus 5 [1m], r2 governs) → 2 hostile re-readers (RR-A design-and-usage · RR-B library-and-code) → this seat.
**Method (L-18)**: both re-reader returns were attacked before either was adopted. Every verdict-level disagreement (D-3 severity, L-2 severity, L-3's skew limb, L-6's two treatments, the mobile row box) was re-derived against the tree by this seat with its own instruments; every adopted MISSED finding was re-proven from bytes, not inherited. Zero browser probes (probe parsimony; browser tooling forbidden for this seat) — every ruling below is static-decidable or explicitly banked as live residue.
**Arbiter's own instruments this session**: full reads of ComponentSliders.vue (395), useSliderTouchGates.ts (133), useSliderAnnouncements.ts, sliderAnnouncement.ts (81), useSliderGradients.ts (95), valueDomain.ts (50), main.ts; ranged reads of picker-color.ts (:40-70/:95-165/:190-216), useColorPipeline.ts (:20-35/:45-95/:110-150/:315-335), useColorParsing.ts:58-92, hydrate.ts:75-112, ErrorBoundary.vue:55-70, readoutReservation.ts (:1-70/:85-125), ConsoleRail.vue (:1-40/:85-95), ConfigSliderPane.vue:210-222, SpectrumCanvas.vue (:9-13/:225-240), App.vue (:45-55/:138-142/:243-247), foundation.css (:438-444/:560-580/:650-700/:820-845), shell.css:60-90, EasingSpecimenStrip.vue:38-50, useHeightTransition.ts:44-52; installed glass-ui 7.0.0 dist bytes: `useTouchGate-B4mzQcHJ.js` (module registry :4, sniff :19, add :68, dispose :69-73), `glass-ui.css` spectrum-slider rule census (thumb `width: calc(var(--slider-thumb-size,1rem)*.75)`, track `height: …*1.5` + `background:var(--slider-track-bg,…)`, range `background:0 0`, `[data-size=sm|md|lg]` arms); repo greps: `pointerType` in the gates file (0), `scrollIntoView` (exactly 3 sites), `DIGITS` (exactly 3 lines), `--type-mono-caption` (3 consumers / 0 definitions demo+glass dist), `container-type` in demo (shell.css:83 only), `spectrumFieldIsLight` (2 consumers incl. this SFC), demo/ui census (19 dirs / 0 .vue); tsconfig.demo.json paths vs `package.json#exports` (8 vs 7, re-derived); `ls src` (no `units`); test/e2e surface reads (slider-announcement.test.ts imports, a11y-slider-operation.spec.ts `valueNow`, color-space-switching.spec.ts:18-30); authority reads VISUAL-CONSTITUTION.md:19/:78/:83-84/:104/:182, PROPORTION-AUDIT.md:49/:72; `motion-quarantine.md` in full; CARRY-CUT-LEDGER.md §0-§1 (X-wave owners; CC-003/CC-046/CC-084/CC-105/CC-106); frames/forced-colors-console.png read directly by this seat.
**Motion law compliance**: this corpus is NOT among the 46 quarantined files. No motion-quarantine FALSE row is carried: M-26/M-27 (the ConsoleRail PRM rows, FALSE) assert the *CSS* motion is centrally governed — this record agrees and carries nothing against it. The one motion finding adopted below (R-27) is a *JS-path* claim in the MQ-1 blind-spot family (progress not clocked by `animation/transition-duration`), which the two guards structurally cannot reach; challenge-D's own negative proof 1 ("Reduced motion is correct") is marked over-broad, not FALSE — it measured only the CSS stagger.

## Scoreboard

**26 CONFIRMED (2 BLOCKER · 15 MAJOR · 8 MINOR · 1 INFO) · 15 KILLED · 12 MISSED-ADOPTED (3 MAJOR · 7 MINOR · 2 INFO, several folded into confirmed rows) · 10 UNPROVEN-NEEDS-LIVE residue rows.**

Blocker-weight survivors: **R-1** (the touch gate swallows the first *mouse* pointerdown on every touch-capable device — capability tested, modality never read; decidable from source + dist bytes alone) and **R-2** (a legal CSS Color 4 `none` channel reaches an unguarded `valueOrThrow` in root setup; the ErrorBoundary is mounted one level too low to catch it). Strongest re-reader contribution: the forced-colors roster protects the transparent `.slider-range` while the ramp lives on `.slider-track` — the one failure the roster exists to prevent, on the component whose purpose is showing color (R-26, adopted MAJOR, verified from bytes on both sides plus the frame).

## Ruled disagreements (the adjudication's actual work)

| row | RR-A | RR-B | RULING | deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **D-3/C-D6 accessible name** | CONFIRMED **MAJOR** (cites VC §7) | CONFIRMED **MINOR**; cited-law limb KILLED | **MINOR — RR-B wins** | VISUAL-CONSTITUTION.md:182 read verbatim: "the operable slider exposes the identical channel name/value/unit **through `aria-valuetext`**" — the obligation is scoped to the valuetext, which complies. The name defect survives on general a11y grounds only, and the correct valuetext partially mitigates at focus time. RR-A's three-names extension (ConsoleRail.vue:32 lowercase `${component} channel` on the `role="tab"` — verified) folds in and gives the cure its third site. |
| **L-2/D-13 barrels severity** | MINOR | MAJOR (substance) | **MINOR at this component's scope; the repo-wide identity is X-W8's** | Scope law (ColorSpaceSelector-apotheosis addendum item 1): the component owns 2 of ~90 alias edges (:95-96, verified 19 dirs/0 .vue). One identity, already FOLDed (ColorPicker L-6 / ColorSpaceSelector L-4 lineage) — never re-booked at component severity. RR-B's count-drift note stands (L printed 92/37/68; RR-B measured 90/37/82; substance unaffected). |
| **L-3 artifact-skew limb** | UNPROVEN (asks for a re-run) | CONFIRMED (re-ran shasum: css SKEWED, 25 `_2` hits vs 0 published) | **CONFIRMED via RR-B's re-run** | RR-B's run IS the re-derivation RR-A demanded; the drift table itself re-verified by this seat (tsconfig.demo.json:42-49 = 8 keys incl. dead root/`parsing`/`units`, missing `/css`+`/value`, the "CLOSED 8-key set" comment false by one; exports = exactly 7). Skew limb routes to the library build (X-W9), parity to config hygiene (X-W8). |
| **L-6 treatment** | cascade limb CONFIRMED MINOR (SpectrumCanvas only); headline killed | measured limb KILLED wholesale; boundary half survives via D-D9 | **both adopted — compatible** | Verified: SpectrumCanvas.vue:11 carries `touch-gate-target`; its `<style scoped>` :227 + `.spectrum-picker { transition: box-shadow … }` :230-233 = (0,2,0) beats the unscoped :256 (0,1,0) — the fade is lost on that ONE foreign consumer. The four channel wrappers (:57-60) have no competing scoped rule — "dead on its principal consumer" is false. Boundary half = R-13. |
| **mobile row box (44 vs 32)** | C reports strip 44/gap 6; D reports 32 + "44 via ::before" | flags the contradiction | **UNPROVEN-NEEDS-LIVE (residue row 5)** | From source the strip floor is 32px (`clamp(2rem,7cqi,2.625rem)` @358px container); no served rule produces a 44px thumb *height* under coarse (this seat re-checked: the demo `::before` is `position:absolute` and cannot change layout height; glass dist ships no coarse slider block). Both cures (R-5 rhythm, R-21 hit area) re-measure first. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `ee9474e4` (subject bytes = the §D pin); re-resolve before any cure. Wave owners per `registry/CARRY-CUT-LEDGER.md` (letter X ratified 2026-08-03). The SFC itself is a pinned receiver: every row that edits `ComponentSliders.vue` (or `ExtractControls.vue`) is additionally **BLOCKED-ON the glass 8.0.0 repin census event** (CC-105 — which also declares this file's `--slider-track-bg` feed at :197 DEAD at 8.0.0, no alias).

- **R-1 · BLOCKER · CONFIRMED — the gate tests device CAPABILITY, never event MODALITY; first mouse pointerdown eaten on every touch-capable device.** useSliderTouchGates.ts:57-68 — `if (!gate.isTouchDevice) return;` then capture-phase `e.stopPropagation()` (:62); `pointerType` = 0 hits in the file (this seat's grep); the predicate is glass dist `useTouchGate-B4mzQcHJ.js:19` `"ontouchstart" in window`, evaluated once at construction; a SECOND copy of the sniff at :27-28 is consumed at ComponentSliders.vue:203. Mechanism source-decidable; the two-context click table is residue row 1. **→ X-W6** (per-event `e.pointerType === "touch"` predicate) + glass BH relay for the event-shaped `useTouchGate` (producer half); SFC-touching edits ride the CC-105 census.
- **R-2 · BLOCKER · CONFIRMED — legal `none` reaches an unguarded throw in root setup; the boundary sits one level too low; the warm path wedges the console.** Chain verified: picker-color.ts:104-107 `valueOrThrow` (Result→exception), :151-158 `channelNumber` throws `Missing ${space}.${key}`, :195-204 `clampPickerColor` passes `none` through by design; useColorPipeline.ts:75 `convertPickerColor(model.value.color,"hsv")` bare at composable-body level while the watcher 11 lines down wraps the identical call in try/catch (the author's own receipt); App.vue:245 calls it in setup; ErrorBoundary is INSIDE App's template (:50/:140) and main.ts:5-7 mounts bare — a descendant's `onErrorCaptured` cannot catch an ancestor's setup throw. Warm arm: useColorParsing.ts:75 commits the `none` color (inner guard :76-83 covers only stableHue), then useSliderGradients' watch getter (~:52-56) and formatted map (~:71-73) throw inside the injected pipeline's reactive graph. Blast radius NARROWED per RR-B's kill (hydrate.ts:98-110 catches cross-space conversions; the blank needs a same-space/`none`-preserving deep link). Stack-trace provenance caveat carried (see killed K-10). **→ X-W5** (demo Result propagation: delete `valueOrThrow` from the render path, `none`-total channel read, hoist the boundary above App) with the library half upstream at **X-W9** (CC-084's `Result` battery); the none-URL born-RED gate also enters X-W1's deploy matrix as a query-parameter state.
- **R-3 · MAJOR · CONFIRMED — forced colors: rest paints a permanent opaque ring and the active state loses ALL THREE deltas.** :253-257 `outline: 3px solid transparent` (outline-color is in the forced-colors adjustment set); the three active indicators are all color-only and all erased: :261-263 outline-color, :266-268 box-shadow→none, :271-274 thumb `background-color`/`border-color` inversion (RR-A's third-indicator extension, verified — the axis's "only two indicators" enumeration is killed, K-8). Corroborated by this seat's own read of frames/forced-colors-console.png: four identical black pill rings, zero state. **→ X-W6** (non-color active channel now); durable producer state rides CC-003/BH relay.
- **R-4 · MAJOR · CONFIRMED — the "ONE container-scaled rhythm law" has structurally unreachable ceiling arms.** :329/:332 clamps; sole container `.pane-wrapper` (shell.css:83, the only `container-type` in demo — this seat's grep) capped ≤512px by shell.css:69-72 + `--pane-max: 32rem` (foundation.css:441); 42px needs 600px, 8px needs 533px — neither reachable; desktop viewport-invariant at 35.84/7.68. The "exactly two values" rhetoric is killed (K-7): the clamp is genuinely continuous over containers 457–512px (arithmetic; whether any shipped layout traverses that band is residue row 7). Verbatim duplicate at ConfigSliderPane.vue:216 (verified, incl. the coarse `::before` idiom at :218+). **→ X-W6, canon echo X-W10** (one rhythm source; the ConfigSliderPane copy dies in the same row).
- **R-5 · MAJOR · CONFIRMED (promoted from D-16's hypothesis) — the wrapper map is never pruned; detached reka subtrees retained and iterated by both consumers.** :56 `if (el)` discards Vue's null-on-unmount call; no `delete` in the SFC or composable; key sets non-monotone across spaces (picker-color.ts:52-70); useSliderTouchGates.ts:53 binds five listeners per entry, useSliderAnnouncements.ts:35-44 writes ARIA per entry. RR-A's abstention-kill and RR-B's promotion both adopted (K-5). CURE CONSTRAINT (RR-B's missed INFO, adopted): the inline-arrow ref re-creates identity per render, so a bare `delete`-on-null branch would churn every live key on every patch — the fix requires a stable ref identity or the per-row child component. Live retention counts = residue row 3. **→ X-W6**, BLOCKED-ON the CC-105 census; note CC-106 deletes `useSliderAnnouncements.ts` outright at the same census (reka `getValueText` landed at glass HEAD), removing the map's second consumer.
- **R-6 · MAJOR · CONFIRMED — hex is an undesigned state that ships: four rows, permanently blank meters, valueless announcements.** Statically decidable: resolveColorSpace maps hex→rgb so :120-125 renders four rows; useSliderGradients.ts:63-68 returns `{hex:…}` only; :160 misses, :161 switches the alpha fallback OFF for hex, :167 returns `""`; sliderAnnouncement.ts:68-81 degrades to the bare noun. The axis's cure is killed (K-11): `colorComponents` (useColorPipeline.ts:117-122) carries no alpha row and returns `[["hex",0]]`, whose `sliderValue("hex")` → `channelMeta("rgb","hex")` throws (picker-color.ts verified) — the cure trades a blank meter for a render crash. **→ X-W6** (hex renders an honest single-cell composition; alpha row preserved).
- **R-7 · MAJOR · CONFIRMED — a color-space change destroys keyboard focus.** :47 `:key="animationKey"` container re-key bumped at :143; no `focus(` in the file (this seat read all 395 lines); per-row `:key="component"` at :52 already re-keys what changes; `alpha` is remounted needlessly; contradicts the file's own chassis-persistence law (:20-24). Unconditional — a remount is DOM, not motion; PRM does not suppress it. **→ X-W6.**
- **R-8 · MINOR · CONFIRMED (downgraded — ruled above) — the accessible NAME is the ambiguous raw key in shouting caps; THREE names for one channel, two homophones.** :66 `${component.toUpperCase()} channel` vs ConsoleRail.vue:32 `${component} channel` (spoken identically) vs the correct `channelLabel(space,component)` (sliderAnnouncement.ts:60-66) already imported by the sibling (useSliderAnnouncements.ts) and arguing this exact case in its docblock (:11-14). Constitution limb killed (K-12). **→ X-W6** (name ← `channelLabel`; three sites).
- **R-9 · MAJOR · CONFIRMED — the meter's `min-width: 6ch` under-reserves the widest legal string and the overflow is taken out of the slider track.** :313; oklch excluded from INTEGER_LEAST_COUNT (readoutReservation.ts:53-60) ⇒ `359.0deg` = 8 advances vs 6 reserved; the meter is a flex sibling of the `flex-1 min-w-0` wrapper with `white-space: nowrap` (:315). The prescribed cure is killed twice over (K-1). Interplay: R-24's `deg→°` mapping shrinks the worst case by 2ch. **→ X-W6** (reservation and format derive from one home; never a rendered per-cell `ch` min-width — the R4 prohibition, readoutReservation.ts:112-115, is binding).
- **R-10 · MAJOR · CONFIRMED — tsconfig `paths` drift: 3 dead keys + 2 missing keys off the 7-key exports map; the "CLOSED 8-key set" comment false by one.** Re-derived exactly by this seat (see instruments). The component's only library import (:98 `/math`) short-circuits the exports map at typecheck. IDENTITY GUARD (anti-rename): fact-identical to the ColorSpaceSelector apotheosis's L-14 → ONE identity. **Parity → X-W8; the `css.d.ts` `_2`-shadow skew limb → X-W9** (library build).
- **R-11 · MAJOR · CONFIRMED — the library owns the space taxonomy but publishes no channel domain; the kelvin bound has two homes in two packages.** src/color/model.ts:91 vs picker-color.ts:62 (both read); the other 16 spaces' domains (:52-70) have no library counterpart; `normalizedChannel` (:160-163) is entirely demo knowledge. **→ X-W9** (the `CHANNELS`/`ChannelSpec` export on the 4.1 surface; `PICKER_CHANNELS` demotes to a re-export).
- **R-12 · MAJOR · CONFIRMED — `controls/` reaches into `display/`'s private module for a concept neither owns; `_component` declared and ignored; the docblock cites three dead APIs.** :100 import; readoutReservation.ts:62-64 signature; `COLOR_SPACE_RANGES`/`COLOR_SPACE_DENORM_UNITS`/`getColorSpaceBound` = 0 hits in src/ (re-verified: `src/units` does not exist). **→ X-W6** (leastCount beside PICKER_CHANNELS now; X-W9's `leastCount` on ChannelSpec is the terminal home).
- **R-13 · MAJOR · CONFIRMED — the whole `<style>` is global but only the touch-gate cluster is justified; five private names ship globally; the rhythm was taken by copy-paste.** :238 no `scoped`; justification :239-252 covers `.touch-gate-*` only; `.sliders-console`/`.channel-meter`/`.channel-rows`/`.channel-strip`/`.channel-slider` are this component's alone; the :326-327 "offer" was declined by clone (ConfigSliderPane.vue:216). Cascade limb (MINOR, scoped): the unscoped `transition: outline-color` (:256) is deterministically overridden on SpectrumCanvas ONLY (ruled above); the touch-gate cluster's one home is `demo/styles/`. **→ X-W6**, BLOCKED-ON the CC-105 census (both pinned receivers).
- **R-14 · MAJOR · CONFIRMED — per-instance redefinition of the producer's `--glass-bg-quiet`, scheme-forked by a `.dark` descendant selector.** :299-303, named by its own comment (:285-298) as a direct override; owner edict 5. Sibling shape adopted (RR-A missed, MINOR): the `rounded-panel` utility over the Card's `rounded-card` stamp (:9-10 comment, :30) — same edict-5 shape, with the caveat that the T-34 owner verbatim covered the material, not the radius. **→ glass BH relay** (named veil clarity/α lever + an in-plate radius rung), consumer residue X-W6; rides CC-003.
- **R-15 · MAJOR · CONFIRMED — canon §5's axis composition is not adopted: label structurally detached (no `aria-controls`/`for`/`labelledby` — verified against ConsoleRail's template), unit glued into the value string, numeric entry absent, four new slider mechanics in the consumer.** VC:104 verbatim; PR-05 on the double boundary. **→ glass BH relay + X-W10 canon**; note A-17 (no chassis arms exist at 8.0.0) makes this a producer-roadmap ask, not a demo edit — the composition-adoption judgment itself is residue row 10.
- **R-16 · MINOR · CONFIRMED (downgraded from BLOCKER — ruled in killed K-2/K-3) — the drag thumb is 12 CSS px inline in every matrix; the coarse extension grows only the block axis of the SliderRoot (the value-JUMP zone, never the grab zone).** Producer CSS re-read by this seat: thumb `width: calc(var(--slider-thumb-size,1rem)*.75)`, track `height: …*1.5`, `[data-size]` arms sm/.5rem · md/1rem · lg/1.5rem — the rung EXISTS and this SFC already drives three sibling tokens (:197-201). The genuinely producer-blocked part is narrower than either axis stated: inline and block both derive from ONE variable, so a ≥24px inline thumb costs a 48px track — decoupled thumb/track rungs are the actual producer ask. SC 2.5.8 is met via the spacing exception (C's own row-pitch measurement + the block-rhythm arithmetic); this is ergonomics, not a normative AA failure. **→ X-W6** (consumer token, priced) + glass relay (decoupling); BLOCKED-ON the CC-105 census; coarse thumb-height mechanism = residue row 4.
- **R-17 · MINOR · CONFIRMED — the design-system boundary is crossed through 1-line re-export barrels; three live import idioms, colliding inside this component's own folder.** :95-96; demo/ui = 19 dirs / 0 implementations (this seat's census); ConsoleRail.vue:90-93 mixes the barrel with three granular subpaths (RR-B's corrected citation adopted; L's `:5-12` was wrong). The 87× metric is killed (K-14). IDENTITY GUARD: the repo-wide extirpation is ONE identity at **X-W8** (CC-007); the component's two edges die there.
- **R-18 · MINOR · CONFIRMED — `--type-mono-caption` does not exist; three consumers, three fallbacks; here it resolves to a vw-driven clamp inside the cqi-only region.** 3 consumers / 0 definitions re-verified in demo AND glass dist by this seat; fallbacks diverge exactly as filed (`var(--type-caption)` vs `0.6875rem` ×2); shell.css:79-81 is the law. **→ X-W10** (one typographic role; retire the phantom in one sweep).
- **R-19 · MINOR · CONFIRMED — two physical holdouts in a logical stylesheet; the meter inks CSS-serialization `deg` where the module's own doc promises `°`.** :313-314 vs :332/:352/:355; :172 + picker-color.ts:50 vs sliderAnnouncement.ts:5; `%` renders as a sign in the same column. RTL adjacency: CC-046 (A-16) already rules the glass side — consume `dir`/`inverted`, never internals. **→ X-W6** (`text-align: end`, `min-inline-size`, unit display mapping in the least-count owner).
- **R-20 · MAJOR · CONFIRMED — 21 `useTouchGate()` instances eagerly allocated; at most 4 live; union-by-bare-key collides `a`/`b` across four spaces.** :31-40; union re-enumerated by this seat from picker-color.ts:52-70 = 20 + alpha. AMPLIFIED + BOUNDED by RR-B's missed finding (adopted, MINOR, verified in dist): on touch devices all 21 join a module-global Set walked on EVERY document touchstart (:4-13, :68) — a per-interaction cost no axis priced — but `getCurrentScope() && onScopeDispose` (:73) provably disposes per mount, so there is no cross-mount leak. **→ X-W6** (per-live-strip gates).
- **R-21 · MAJOR · CONFIRMED — the component's private derived state lives in the shared session pipeline, so a ramp throw is not contained to the console.** useSliderGradients.ts:25 `componentsSlidersStyle` — sole consumer this SFC (:194); `currentColorRanges` — sole consumer ConsoleRail; the warm-path throw sites (R-2) sit inside the injected pipeline's reactive graph. Owner edict 1, in the direction that maximizes blast radius. **→ NO-WAVE-OWNER** — the COLOR_MODEL_KEY role-split identity (the ColorSpaceSelector apotheosis's MT-CSS-2) has NO row in CARRY-CUT-LEDGER; flagged for the next formation boundary so the identity is not silently dropped (L-15.8).
- **R-22 · INFO · CONFIRMED — dead `unknown`-cast payload; two watchers on one source; per-render array identity to the rail.** :120-125 (both consumers destructure `[component]` only — :34, :51), :138 + :143. CURE CONSTRAINT (RR-B's missed alpha finding folded here, MINOR, verified): the dead alpha literal says `max: 1` while READOUT_CH's live alpha row says `max: 100` (readoutReservation.ts:96) and `meterText` scales ×100 (:163) — THREE implicit homes with a 100× disagreement, benign only because one is dead; typing the dead literal (L-10's own cure) would make the wrong bound live and flip `readoutCh`'s `%`-scale branch. **→ X-W6** (keys-only computed; alpha's meta gets ONE home — terminally X-W9's ChannelSpec).
- **R-23 · MAJOR · CONFIRMED — the gates are vacuous: three named mutations that break the component's stated contracts keep both suites green.** Verified by this seat: test/slider-announcement.test.ts imports only the two pure functions; a11y-slider-operation.spec.ts's `valueNow()` reads `aria-valuenow` exclusively; nothing mounts the SFC or imports either colocated composable. Method limb killed (K-9): the "complete test surface" grep missed color-space-switching.spec.ts (exists, drives the selector — verified :18-30), which exercises the re-key path without naming the component. **→ X-W6** (the three born-RED gates: hex meters non-empty; hasTouch-desktop first mouse click drives; wrapper-key set equals the space's channel set).
- **R-24 · MINOR · CONFIRMED — `valueDomain.ts` is a 3-line alias whose docblock describes a deleted architecture AND states the opposite of the mutation contract.** Alias verified (:48-50 → `clampPickerColor`); dead citations verified (no `COLOR_SPACE_RANGES`/`getColorSpaceBound`/`src/units`); PLUS RR-B's missed limb (adopted, verified verbatim): the docblock promises "IN PLACE … Returns its argument" while `clampPickerColor` `.map()`s into a fresh `buildColor` product and mutates nothing — safe today only because both call sites reassign. **→ X-W8** (alias dies under graph subtraction; the true paragraphs move to `clampPickerColor`'s docblock).
- **R-25 · MINOR · CONFIRMED — the 44px hit extension is gated on the PRIMARY pointer (`pointer: coarse`) while the JS gate keys on an any-pointer-shaped sniff.** :345 + the :342-344 comment misstating MQ4. Collapses to a non-question under R-1's per-event cure. **→ X-W6.**
- **R-26 · MAJOR · MISSED-ADOPTED (RR-A) — FORCED COLORS: the tier-1 color-display roster names the WRONG ELEMENT, so all four channel ramps and the alpha checker lose their color entirely in WHCM.** Re-proven by this seat from bytes on both sides: foundation.css forced-colors block (:679-696) lists `.glass-slider[data-variant="spectrum"] .slider-range`; glass dist paints the ramp on `.slider-track` (`background:var(--slider-track-bg,…)`) and renders the range `background:0 0` — a transparency foundation.css itself restates at :571-574. `forced-color-adjust` protects a transparent CHILD of the element carrying the color. Visually confirmed by this seat's read of frames/forced-colors-console.png (four empty white tracks, zero ramp, zero checker). foundation.css is UNPINNED — the one-selector cure is unblocked today. **→ X-W6, canon echo X-W10.**
- **R-27 · MAJOR · MISSED-ADOPTED (RR-A) — MOTION/PRM: the rail-letter jump calls `scrollIntoView({behavior:"smooth"})` unguarded; the app-wide guard cannot reach it; the call violates a house prohibition with a named incident.** :213; census re-run by this seat: exactly 3 `scrollIntoView` sites in demo — the prohibition itself (EasingSpecimenStrip.vue:40-46, "Never `scrollIntoView` … the O-19 desktop flat-netting root cause", which then reads PRM at :47 and writes `scrollLeft` on one owned port), this SFC, and useHeightTransition.ts:48 (the second live violation). Per CSSOM View, an explicit `behavior:"smooth"` wins over the guard's `scroll-behavior: auto !important` (animations.css:184-193) — the MQ-1 blind-spot family. Carries NO motion-quarantine FALSE row (M-26/M-27 concern governed CSS motion; this is the JS path). Challenge-D's negative proof 1 is corrected, not carried. **→ X-W6 (motion-sourced gate, MQ-1 precedent), canon echo X-W10; both live sites in one row.**
- **R-28 · MAJOR · MISSED-ADOPTED (RR-A) — KEYBOARD/AT: `:step` is 0.001 of the NORMALIZED domain, decoupled from the per-space least count — 3-4 consecutive arrow presses produce an identical meter AND identical `aria-valuetext` in six spaces including default rgb.** Re-derived by this seat: :68-70 hard-codes 0/1/0.001; rgb r ∈ 0..255 ⇒ 0.255/press against a 0-decimal format (INTEGER_LEAST_COUNT contains rgb); hue 0.36°/press against integer `deg` in hsl/hsv/hwb/lch; the valuetext composes from the same meter string (useSliderAnnouncements → sliderValueText). The exact failure class U-F27/BR-4 exists to prevent, invisible to the existing e2e (asserts `aria-valuenow` only — verified). Consumer-curable (`:step` from the space's least count); not glass-blocked. **→ X-W6.**
- **R-29 · MINOR · MISSED-ADOPTED (RR-B) — two dead `DIGITS` constants, one exported through the pipeline's public surface with zero consumers.** Re-run by this seat: exactly 3 hits (useSliderGradients.ts:14 dead-on-arrival duplicate; useColorPipeline.ts:28 definition; :331 exported, consumed by nobody). **→ X-W8** (dead-code subtraction on the god module R-21 argues about).
- **R-30 · MINOR · MISSED-ADOPTED (RR-B) — `PickerColorError` carries two incompatible throw contracts and the machine code is what the end user reads.** Verified: the class (:97-102) exists for `issues: ParseIssue[]`; `parsePickerColor` (:109-113) honors it; `valueOrThrow` (:104-107) inverts it (machine token in the message slot, empty issues) at every other throw site; ErrorBoundary.vue:61 renders `err.message` verbatim — corroborated by challenge-C's own probe UI text ("… color_missing_alpha Try again"). **→ X-W5** (dies with R-2's `valueOrThrow` deletion; the boundary gets a human string + diagnostics channel).
- **R-31 · INFO · MISSED-ADOPTED (RR-A) — `@media print` carries the same wrong-element bug AND omits the slider entirely, so the channel ramps print blank inside a `#fff !important` veil.** Verified foundation.css:822-841: the faithful-ink list has no slider row in either form. **→ X-W6, rides R-26's row.**

## Killed-claims register

- **K-1 — design:D-4's cure ("export `readoutCh` … feed `min-inline-size` from it").** `readoutCh(space,component)` ALREADY EXISTS, exported, at readoutReservation.ts:117-120 over the READOUT_CH table (:89-107) — the cure prescribes writing a shipped function; and feeding a rendered min-width re-instates the mechanism the R4 re-scope killed by measurement (:1-15, "39-51% of every cell was empty reservation … no per-cell `ch` reservation returns"; the prohibition restated at :112-115, seven lines above the header the axis quoted). Both re-readers killed it; this seat re-read the file and concurs. The FINDING stands (R-9); the cure must be transposed.
- **K-2 — design:D-2's producer-blocked framing / impl:D-9's "producer RELAY, not consumer override."** The rung exists: `--slider-thumb-size` with three named `[data-size]` arms, re-read in dist by this seat; this SFC already drives three siblings of the same token family. What IS producer-blocked is only the thumb/track coupling (one variable drives both at ×0.75/×1.5). The blocked/unblocked partition in both original axes is wrong at this row.
- **K-3 — design:D-2's BLOCKER severity ("SC 2.5.8 … 12px in EVERY matrix", nominated to lead the packet).** The Spacing exception is met and the sibling axis measured it (row pitch 43.5px; track click-operable); the block-rhythm arithmetic (R-4's clamps) independently guarantees >24px undisturbed circles. Ergonomics MINOR, not a normative AA failure. Two axes contradicted; C was right.
- **K-4 — impl:D-13 ("`aria-live=\"off\"` is dead code").** Killed by the governing authority BY NAME: VISUAL-CONSTITUTION.md:182 requires exactly this attribute on exactly this element ("ordinary text uses `aria-live=\"off\"` …"). :84 is the compliance token. Actioning this row would take the component OUT of conformance. Both re-readers killed; this seat re-read the clause and concurs.
- **K-5 — design:D-16's abstention ("not a finding until that probe runs" / routed probe-only).** The mechanism is source-decidable (R-5); the claimed blocker is false — e2e/smoke/color-space-switching.spec.ts:18-30 already drives the exact widget (verified); and two sibling axes had reproduced it live. Unearned self-doubt; promoted.
- **K-6 — lib:L-6's headline ("dead on its principal consumer … doomed at authorship").** The seat measured the WRONG ELEMENT: its own probe printed `cls: "spectrum-picker …"`. The principal consumers (the four channel wrappers in the same SFC) carry no competing scoped rule and the fade applies unopposed (RR-A's citation of D's re-measurement, `{tp:"outline-color", td:"0.3s"}` ×4). Correct scope: lost on ONE of three foreign consumers, by SpectrumCanvas's own scoped rule (verified :227-233). Survives as R-13's MINOR cascade limb.
- **K-7 — design:D-1's "EXACTLY TWO VALUES … the step did not become a function."** The clamp is continuous over containers 457-512px by the report's own arithmetic; the load-bearing claims (dead ceilings, desktop invariance) survive in R-4; the rhetoric does not.
- **K-8 — design:D-6's "ITS ONLY TWO INDICATORS."** Three: the thumb inversion at :271-274 was omitted (verified). The verdict survives (all three are color-only and erased); a finding whose force is exhaustiveness must enumerate exhaustively.
- **K-9 — impl:D-15's "complete test surface" method + design:D-3's line citation.** The name-based grep missed color-space-switching.spec.ts (exists, verified); the aria-label is at :66, not :63. Provenance defects in receipt-based reports; substance survives (R-23, R-8).
- **K-10 — impl:D-2/lib:L-1's pasted stack traces as file:line receipts.** None of the frame line numbers resolve to the named function in live bytes (picker-color.ts:154 is inside `channelNumber`, not `valueOrThrow`, etc. — re-checked by this seat against :104-158). Consistent with Vite-transformed output; a provenance caveat, not fabrication — the claim is independently confirmed from source structure (R-2). No downstream seat may cite those stacks into the tree.
- **K-11 — impl:D-3's cure ("consume the pipeline's `colorComponents`").** Actively breaking: no alpha entry (deletes the alpha slider in all 17 spaces, against the file's own W4-3 gate), and hex returns `[["hex",0]]` whose channel lookup throws `Unknown rgb channel: hex` — a render crash for a blank meter. Verified from useColorPipeline.ts:117-122 + picker-color.ts. The defect (R-6) stands; the cure does not.
- **K-12 — design:D-3's cited law ("§7 requires … the name half does not comply").** §7 scopes the identical-tuple obligation to `aria-valuetext` (verbatim re-read); the valuetext complies fully. The finding survives on general grounds at MINOR (R-8).
- **K-13 — lib:L-1's blast radius ("ANY hand-edited deep link with a `none` channel is a blank page").** hydrate.ts:98-110 catches conversion failures and falls through to storage/default (verified); the blank requires a same-space or `none`-preserving link — exactly the report's own repro URL. BLOCKER real, radius narrowed (R-2).
- **K-14 — lib:L-2's "87× larger runtime entry module … per import site."** Measures shim size, not delivered cost; the chunks are shared and the root barrel tree-shakes in production. Real costs are dev-graph breadth + typecheck surface — an order of magnitude smaller than argued. The finding (R-17) never depended on the figure.
- **K-15 — lib greenfield lattice's "plateLuma.ts … neither consumer is a slider."** One of the two consumers IS this component (ComponentSliders.vue:99/:180 — re-verified by this seat's grep). The rename premise is false; `controls/`-root colocation for a two-sibling helper is correct.

## Superlatives (provenance per row)

- **`composables/sliderAnnouncement.ts` is the best module in the cluster** — zero Vue, zero DOM, two frozen tables, the problem stated before the solution (:11-14, the `b`/`l`/`a` collisions), a DESIGNED degradation (:68-81, "never an empty announcement"), its own producer debt declared (:16-19), and the cluster's only unit test covering exactly the collisions the docblock names. Every axis and both re-readers independently held it up as the standard the SFC fails to meet. (All three seats + this seat's full read.)
- **ComponentSliders.vue:171 — the `-0.0` kill.** One line strips the minus from a zero-rounded value on the signed axes (lab/oklab a·b, ictcp, jzazbz). Instrument-grade care at one point of a 250-unit domain; noticed by neither original axis. (RR-A; re-verified.)
- **`useSliderTouchGates.ts` teardown discipline** — every listener has a matching removal closure (:94-100), drained on re-attach (:50-51) AND unmount (:126-130), `{capture:true}` passed to `removeEventListener` (the detail almost everyone gets wrong), plus the document-level `pointercancel` force-release net (:112-121) written for a named iOS incident. Incident-driven code whose only guardian is itself (R-23). (Both re-readers; re-verified line-by-line.)
- **glass `useTouchGate`'s defensive disposal** — `getCurrentScope() && onScopeDispose` (dist :73) deletes the gate from the module registry and restores `touchAction`; it is what bounds R-20 to a per-mount cost rather than a leak. Producer-side correctness the consumer gets free; no original axis credited it. (RR-B; re-verified in dist bytes.)
- **`readoutReservation.ts:1-18` — a comment that prevents regressions.** Records the deleted mechanism, the measurement that killed it (39-51% empty reservation), the replacement, and a standing prohibition — which single-handedly refuted a later seat's cure (K-1). (Both re-readers; re-verified.)
- **`clampPickerColor` (picker-color.ts:195-204) is total across three channel regimes and correct in each** — `none` passes, hue WRAPS (`((c%360)+360)%360`, correct per CSS), linears clamp, alpha has its own `none`-aware arm. (RR-B; re-verified.)
- **`picker-color.ts:70`'s `satisfies Record<SpaceId, readonly ChannelMeta[]>`** — the one keyword keeping the 17-space duplication honest until R-11's library home exists: a new library space becomes a demo compile error. (RR-B; re-verified.)
- **`foundation.css`'s two-tier forced-colors architecture (:666-700)** — the reasoning is right (keep the color content, let chrome adopt system colors, `:where()` zero-specificity focus fallback) and worth preserving verbatim; only its slider row names the wrong element (R-26). (RR-A; re-verified, incl. the frame.)
- **`demo/styles/animations.css:30-54`** — the stagger lives ENTIRELY inside `no-preference` with the invariant named (`inv-N-9 PRM`) — the strictly stronger construction, confirmed by the motion-quarantine's own S-3. (RR-A + quarantine; cross-checked.)
- **`ErrorBoundary.vue` is a properly built boundary** (`role="alert"`, assertive, focus-managed via `nextTick`, `return false` containment — re-read :55-70). R-2 is a MOVE, not a rewrite. (RR-B; re-verified.)

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

1. **R-1's two-arm click differential** (plainDesktop drives / touchCapableDesktop eaten) — the mechanism needs no browser; the differential is the born-RED gate and must be RE-RUN, never cited from challenge-C's P8 table.
2. **R-2's warm-path permanence** ("the console never recovers") — whether Vue leaves the computed/watcher wedged vs re-throwing per read is live-only; the throw itself is static.
3. **R-5's retention counts** (`mapKeys:9 / detached:5` after 8 switches; the 21 is enumerable, the census is not).
4. **The coarse thumb HEIGHT mechanism** — three sources report 12×44, no served rule produces it (glass ships no coarse slider block; the `::before` is absolute and cannot change layout height): the reported mechanism is wrong even if the number is right. Re-measure before R-16.
5. **The mobile row box contradiction** (C: strip 44/gap 6 · D: 32 + "44 via ::before"; source says 32) — re-measure before R-4/R-16 cures; C's inter-row hit test is only sound under C's numbers.
6. **R-3/R-26 forced-colors computed values** (rest ring `rgb(0,0,0)` ×4; ramp loss) — structure and frame are in evidence; the cure gate needs a forced-colors context witness.
7. **R-4's measured container widths** + whether any shipped layout actually traverses the 457-512px continuous band (arithmetic says viewport ≈ 470-540 single-pane; unmeasured).
8. **R-2's rendered blank mount** (`app innerText len: 0`, the flat `#b1748f` field) — statically entailed, pixel evidence live-only.
9. **design:D-7's veil-vs-well tier** ("~15% variation survives the blur") — a live compositing measurement plus an OWNER ruling (VC §2), not a static verdict; the α override (R-14) is its symptom either way.
10. **R-17's delivered-bundle consequence** (does deleting demo/ui change the production bundle at all) — needs a gh-pages build diff; forbidden here by probe parsimony.

## Closing verdict

**DEFECTIVE — ADJUDICATED.** Two BLOCKERs survive hostile re-reading and independent re-derivation (the modality-blind touch gate; the `none`-channel root-setup throw above a well-built boundary), fifteen MAJORs stand with them, and the re-read added three MAJORs the three-axis corpus missed entirely — the wrong-element forced-colors roster, the unguarded smooth scroll against a house prohibition, and the least-count-blind `:step`. Fifteen claims died on re-derivation, including two cures that would have shipped regressions under green gates (K-1, K-11) — the cure-adjudicated-separately law (ColorSpaceSelector addendum item 2) earned its keep twice here. The subject file remains byte-frozen at the §D pin; every disposition above is a routing (X-W5/X-W6/X-W8/X-W9/X-W10, CC-003/CC-105/CC-106 blocks, one NO-WAVE-OWNER flag for the orphaned pipeline-split identity), not a stamp. Original challenge records and both re-reader returns remain immutable beside this file per E-1/E-3.
