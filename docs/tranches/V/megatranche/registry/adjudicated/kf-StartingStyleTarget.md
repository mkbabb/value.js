Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component StartingStyleTarget; receipt read from this seat's own system prompt, not inherited).

# kf-StartingStyleTarget — adjudicated apotheosis r1 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-24 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — local HEAD `8281638c` (stale, 1 ahead / **41 behind** `origin/master` `81a56990`, 2026-07-18); worktree dirty, but **worktree ≡ origin/master on the entire read set** (my `git diff origin/master --stat` this session over StartingStyleTarget.vue / SpringScene.vue / useCompiledEntry.ts / useSpringDemo.ts / useSpringLinearStops.ts / playback-idiom.css / CopyButton.vue → CopyButton.vue 1 line only, the banked KF-CB-1 repair). The worktree edit at target `:41` (`variant="outline"` → `emphasis="secondary"`, my `git diff HEAD`) is a **backport of the frontier's own repair** — every roster row below is frontier-durable by identity. · **Registry tree** value.js `tranche-u` · **Corpus (r1)**: two fresh hostile re-reader returns over `audit/kf-components/StartingStyleTarget/challenge-{D-design,L-library,C-consumption}.md` (all three axes `claude-opus-5[1m]`) — **reader-A** (design-and-usage: 23 confirmed · 5 kills · 5 misses · 6 unproven · 8 superlatives) and **reader-B** (library-and-code: 24 confirmed · 8 kills · 4 misses · 9 unproven · 11 superlatives). Both returns consumed whole; all three challenge files re-read whole by this seat.

**Method (incredulity).** Every inter-reader contradiction, every kill, every claimed-new miss, and every cell where a verdict is load-bearing was re-derived against the bytes before adoption. Zero product-source writes, zero installs, zero browser tooling; **sole write = this file**. Instruments this session: whole reads of StartingStyleTarget.vue (216 L), useCompiledEntry.ts (84 L), all three challenge files; SpringScene.vue:30-50/:75-80/:130-160/:195-201; useSpringDemo.ts:54-58/:68-72/:378-428; SpringPhysicsFacet.vue:27-46; springKeys.ts:1-8; CopyButton.vue:2-5/:25-30; playback-idiom.css:14-20/:70-90; font-roles.json:70-84; style.css:155-165; design-idioms.css:79-86/:240-250; useSpringHotPath.ts:43-50; `demo/composables/scene-facility/index.ts:6/:35`; animation.ts:88; installed glass-ui 7.0.0 dist verbatim: `@utility touch-hit-area` (the `pointer-events: none` decider), dock `touch-floor.css` (the working `::after` twin), the PRM `!important` head of `utilities/a11y-overrides.css`, the forced-colors `.focus-ring:focus-visible` restore, `@utility text-mono-caption`, Button props (`emphasis: { default: "secondary" }`; `grep -c variant` → 0), the `index.css → accessibility.css → utilities/a11y-overrides.css` import chain (`@layer` count 0 in both); lucide `defaultAttributes.mjs` whole (no `aria-hidden`; `grep -rl` → 0); git reads (HEAD package.json:71 `optionalDependencies` glass-ui **6.0.0** vs origin/master:77 exact devDependency **7.0.0**). **Executed probes (node against the repo's own dist, read-only)**: the full `compileToEntry` spec — `eligible: true, refusals: 0`, base `display: none`, `.is-open`, `overlay`, `translateY` all present, payload **2190 chars / 21 lines / longest line 943**; `springLinearStops` response-invariance — max|Δv| **2.000e-5** for response 0.2→1.0 vs **3.040e-1** for ζ 0.86→0.45; `springTimingFunction(…).css === springLinearStops(…)` → **true**. Registry instruments: kf-CopyButton.md (KF-CB-2/3/5/6/8/9/14/25/33 + killed #8 K9 + the W0–W10 census taxonomy), kf-EasingSidebar.md (KF-ES-8, KF-ES-28), kf-EasingScene.md (KF-ES-2), kf-PlaybackRibbon.md (D-10, N-1, N-6), kf-ChannelOptions.md (KF-CO-24), kf-CubeScene.md (MISS-3), kf-MbabbMenu.md MM-29 (cited from the returns), kf-RibbonBar RB-1 (cited from the returns).

## Scoreboard

**39 roster rows — 0 BLOCKER · 10 MAJOR (2 of them folds into banked rows) · 21 MINOR · 8 INFO (3 folds).** Both axis BLOCKER pairs demoted with dissents preserved (D-1/D-2 and C-1/C-2 → MAJOR); the L axis's sole BLOCKER **killed by baseline law**. **7 net-new rows admitted** from the readers' missed sections, each re-derived at the bytes (one — the artifact viewport — independently converged on by BOTH readers, and re-executed by this seat). **Killed-claims register: 12 entries**, including one **overruled reader confirmation** (reader-A's D-22 promotion dies to the banked K9 kill), one killed kill-adjacent framing (D-7a), and one correction that **inverts two banked registry prescriptions** (the `touch-hit-area` producer defect). **10 superlatives** (one retired pair, one banked rider). **10 UNPROVEN residues** → SS-13.

**Headline.** The component is a right-shaped 216-line leaf whose live CSS is the best `@starting-style` authoring in the demo (five sustained craft superlatives, three re-executed by this seat) — wrapped in a **truth problem**: the copy-pasteable artifact is written in the inverse polarity of the card it labels itself as compiling, from a hand-typed twin, displayed uppercase, in a viewport my own execution shows holds 943-char lines in a ≤768px box; the primary verb is rendered twice with the scene declaring the other copy primary; and the transport's "Entry" channel is a second instance of the banked KF-ES-2 targetless-channel family. The corpus's own discipline mostly held — its three failures are one class (falsifiers scoped to the file or repo when the decider lives in `node_modules` or in git), and the incredulity yield ran in both directions: reader-A's D-22 confirmation dies to a bank kill it never swept, both readers inherited the L axis's "~700-char linear() bodies" gloss in the same sentences as measurements that refute it, and reader-A's touch-hit-area find corrects two banked rows that prescribed a decorative cure.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for SS-1/SS-2, keyed to the census wave taxonomy (KF.W0–W10 per kf-CopyButton.md:36) with **NO-WAVE-OWNER** for the spring artifact-truth packet.

## Rulings (this seat's actual work)

| # | dispute | RULING | deciding evidence (this seat's own) |
|---|---|---|---|
| 1 | **D-22: reader-A CONFIRMS ("PROMOTED TO DECIDED-AT-SOURCE" — lucide ships no `aria-hidden`) vs reader-B KILLS by the bank** | **READER-A OVERRULED — the kill stands (K-5)** | The bank already ruled this exact claim dead: kf-CopyButton.md killed-register #8 (K9) — "`button`'s Children-Presentational rule already prunes SVG descendants; the aria-hidden omission changes nothing exposed." Both icons here sit inside real `<button>` elements (glass Button's `as` defaults to button; CopyButton's root is `<button>`). Reader-A's byte-fact is TRUE (my read of `defaultAttributes.mjs`: no `aria-hidden`, no `role`; `grep -rl` → 0) and **non-load-bearing** — a decided-at-source promotion of an exposure the a11y tree never exposes. The same rule powers banked KF-CB-9 in the opposite direction, which is the finding that matters at this site. First overruled reader-confirmation in this component's ledger. |
| 2 | **Reader-A's MISS-2 (`touch-hit-area` cannot expand a tap target) — silent in reader-B; would invert two banked rows** | **ADMITTED (KF-SST-30) and the banked rows gain annotation riders** | Verified verbatim at the installed dist: the `@utility touch-hit-area` `::before` expander carries **`pointer-events: none`** — excluded from hit-testing; the 2.75rem box is decorative. The contrast is in the same package: dock `touch-floor.css` builds the identical `::after` expander for the dock triggers and **omits** `pointer-events: none` — that one works. Consequences: D-23's prescribed cure ships a 16×16 target with a 44px ghost; banked **KF-CO-24** ("two sanctioned cures") and **kf-CubeScene MISS-3** ("three mechanisms") both count the decorative twin as working — annotate per E-3, never rewrite. Producer-owned → glass-ui BH relay (owner edict). The demo's `.tap-floor` (design-idioms.css:82-85, verified) remains the only in-reach mechanism that enlarges a box — still zero adopters (banked KF-CB-25). |
| 3 | **The artifact-viewport miss — filed independently by BOTH readers (A: MAJOR; B: MINOR)** | **ADMITTED at MAJOR (KF-SST-9); B's MINOR preserved as dissent; both readers' "~700-char" cell corrected (K-11)** | My own execution of the shipped spec: **2190 chars / 21 lines / longest line 943** — reproducing reader-A's figures exactly. A 943-char `whitespace-pre` line in a ≤768px measure is ~11 viewport-widths of horizontal scroll at ~9px/char (and >5k px even at a conservative 6px/char — the conclusion does not ride the font model); 21 lines vs `max-h-32` = 128px scrolls both axes; the `<code>` has no `tabindex`/`role`/name. The one element whose entire purpose is transcription (`:50-55`, `:58`) is unreadable by construction AND displayed uppercase (KF-SST-5) — the composite on the panel's deliverable is MAJOR. The keyboard arm is engine-dependent (Chrome 127+ focusable scrollers; Safari not) → SS-13. **Cell correction**: both readers repeat the L axis's "four ~700-char linear() bodies" gloss — 4×700 > the measured 2190; the bodies are ~430 chars each (the 943-char line carries two). Reader-A repeated the stale gloss in the same sentence as its own contradicting measurement — a citation-inheritance micro-event, same class as the ColorSpaceSelector DISSENT-1 record. |
| 4 | **Reader-B's Entry-channel miss (targetless transport channel) — silent in reader-A** | **ADMITTED at MAJOR (KF-SST-10), KF-ES-2 family; two of B's cells corrected (K-12)** | Chain re-verified whole this session: `useCompiledEntry.ts:53-63` builds `entryAnim`, sets `.name = "Entry"`/`.superKey`, never calls `setTargets` (`grep -rn setTargets demo/scenes/spring/` → **0**); `useSpringDemo.ts:414-426` registers it as the second transport channel whose `setProgress` writes `entryAnim.t`; `animation.ts:88` `set t(v) { this._playback.t = v; }` — a pure state write over `targets: []`; `useSpringDemo.ts:70` `view.value = name === "Entry" ? "discrete" : "solver"` — load-bearing as a SWITCH, inert as MOTION; the ":380-384 the decoy is DEAD … TWO REAL channels" comment verified verbatim — the deleted decoy was replaced by a second decoy. Family ≡ banked **KF-ES-2** (kf-EasingScene, BLOCKER there: painting channel over a targetless animation) — second instance, second scene, booked here as the site row, mechanism never re-booked as novel. Corrections: B cites the facility contract at `src/animation/scene-facility/index.ts:35` — the file is `demo/composables/scene-facility/index.ts:35` (content exact, my read); B's quoted progress getter elides the shipped `dur > 0` guard + clamp (my read of :416-421). Substance unchanged; cells corrected so a cure author reads the right file. |
| 5 | **Severity splits on six D rows — reader-A holds the axis's MAJORs (D-3, D-4, D-8, D-9, D-10) and BLOCKER (D-2); reader-B demotes all six** | **Reader-B's grades adopted on five; D-2 lands MAJOR (between the two); A's grades preserved as dissents** | D-2 → **MAJOR**: the banked family instance KF-ES-8 is MAJOR with the identical harm sentence, and the CopyButton at `:59` carries the correct-case bytes — the "pastes dead CSS" harm has a working escape hatch on the primary path; A's BLOCKER dissent recorded. D-3 → MINOR: the happy path converges after resolve; the divergent state is the async first paint plus the silent-failure path (KF-SST-12's row). D-4 → MINOR: the rendered PRM outcome is acceptable **by the vendor's accident** (my read of the `!important` head: translate/scale absent from the allow-list snap; opacity fades 0.1s); the harm is a false MANDATORY comment, dead code, and the fragility that a vendor allow-list change silently alters PRM behavior with no local defense. D-8/D-9/D-10 → MINOR **with named escalation triggers**: each has a decided source-half (dead `min-h-0`; flex-rules ordering; no border/outline/forced-colors block) and a percept-half that only SS-13 can witness — if SS-13 witnesses the clip, the title truncation, or the boundaryless card at supported viewports, the row escalates to MAJOR. Grade-setting, not re-grading — this is r1 and these are the bank grades. |
| 6 | **Reader-B kills L-M3's aggravation #2 ("the ribbon twin renders at default `emphasis: 'primary'`")** | **KILL SUSTAINED (K-3)** | My own grep of the installed dist: `emphasis: { default: "secondary" }`; `variant` occurrences 0. The ribbon copy renders at exactly the in-card copy's emphasis; no weight divergence exists. L is the one seat that asserted a default it never read; C ("the two agree by accident, not by contract") and D were right. The parent finding survives whole. |
| 7 | **L-B1 (the L axis's sole BLOCKER) — both readers kill it** | **KILL CONFIRMED at git (K-1) ≡ census SCH-1 — never re-booked** | My reads: HEAD `8281638c` package.json:71 declares `"@mkbabb/glass-ui": "6.0.0"` (optionalDependencies); origin/master `81a56990` :77 declares exact devDependency `"7.0.0"` = the installed version. Only the uncommitted worktree deletes the declaration; the axis grepped the worktree and attributed the result to master. Sixth one-baseline-short event in this program; folds to the banked SCH-1/KF.W0 §B-12 row. Everything downstream leaning on F-1 dies with it (C-6's escalation leg, D's read-set preamble). |
| 8 | **The PRM superlative contradiction — L-S2 and C-S★5 praise the exact declaration D-4 proves dead** | **BOTH SUPERLATIVES RETIRED (K-2); D-4 stands** | The decider re-read verbatim this session: `a11y-overrides.css` opens `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { … transition-duration: 0.1s !important; transition-property: opacity, color, background-color, border-color, box-shadow !important } }`, reached unlayered via `index.css` (last import `./accessibility.css`, no `layer()`) → `accessibility.css:1` → `utilities/a11y-overrides.css`; `@layer` count 0 in both; `data-allow-motion` demo-wide 0. `!important` on a universal selector beats the component's normal-weight `transition: none` on both longhands. Two axes sold as exemplary what the third proved inert — and neither superlative's falsifier ever left the file. D-4's own falsifier-provenance cell is also corrected (K-9): `styles/utilities.css` does NOT import a11y-overrides; the chain is index.css → accessibility.css. Banked corroboration: KF-CB-6 names this same kill switch. |
| 9 | **L-mi5's mechanism ("`tier: {}` has no default, so `tier=\"resting\"` is not in effect — half fiction") vs D's own pre-kill K-1** | **MECHANISM KILLED (K-4); D-19's weaker form survives** | Both readers concordant with the axis-D §Killed K-1 byte: Surface derives tier from the private map `{ elevated: "resting", … }` with Card's `material` defaulting `"elevated"` — `data-tier="resting"` IS stamped, `.glass-resting` IS live. The "fiction" framing would send an implementer to add a prop that changes nothing. The surviving row is the undeclared-vendor-default-chain dependency (KF-SST-25). |
| 10 | **D-7's two legs — reader-A confirms whole at MAJOR; reader-B kills the width framing and folds the forced-colors leg** | **B's split adopted (K-6): (a) → INFO/SS-13, (b) → MINOR fold into the banked RB-1 ordering family** | (a) mechanics confirmed (playback-idiom.css:16-19 `width: 100%`, my read; glass `.button` declares no width) but the defect inference dies: the ribbon twin is deliberately full-width in a `grid-cols-1 w-full` cell (SpringScene.vue:139, my read), so a matching full-width in-card control is the consistent reading, and `shrink-0` in a flex-col parent governs height — it was never evidence of shrink-to-fit intent. Percept → SS-13. (b) is a real cascade fact re-verified this session (`.btn-playback:focus-visible { … outline: none }` demo-side and later; glass's forced-colors restore lists `.focus-ring:focus-visible` — my dist read) — the exact three-rule ordering banked as **RB-1** (kf-RibbonBar), folding with KF-CE-13 → KF.W9. |
| 11 | **Small-cell corrections, both directions** | **RECORDED** | Against the corpus: the substrate header ("master @ 8281638c") is false for the audited bytes — at HEAD, target `:41` was `variant="outline"` (BOTH copies of the verb carried the dead prop, and D-21/C-10's restated-default row did not exist at HEAD); the frontier probe rescues everything (read set ≡ origin/master) — K-10. Against reader-A: the "~700-char bodies" inheritance (ruling 3). Against reader-B: the facility path + progress-getter cells (ruling 4). Adopted from reader-A: the `.code-token` sharpening (design-idioms.css:245-248 verified verbatim — "in-prose API names (camelCase, case-preserving mono)" — the purpose-built idiom `:17-19` needs and does not use); the L-S3 falsifier-cell correction (Surface DOES declare `inheritAttrs: !1`; the seam survives via `mergeProps` style-merge — K-8). Adopted from reader-B: the substrate-correction probe; the D-2 cure kill (K-7 — banked MM-29: `normal-case` cancels only `text-transform`, the 0.1em caps tracking rides on, destroying the mono column alignment; the true house cure is `.code-token` or `text-mono-small`). This seat's own enrichment on KF-SST-6: glass `accessibility.css` ALSO ships `[aria-pressed="true"]` border indication under both `forced-colors: active` and `prefers-contrast: more` (my read) — the withheld attribute forfeits **three** vendor affordances, not one. |

## Adjudicated defect roster — id · severity · claim · disposition

Routing law: **KF.W0–W10** from the census taxonomy (kf-CopyButton.md:36); **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (the spring artifact-truth packet). Anchors bind to worktree ≡ `origin/master`; re-resolve before any cure. Original axis ids preserved for life inside each row (anti-rename).

### MAJOR

- **KF-SST-1 · D-1 = L-M3 = C-4** — **the card's primary verb (Reveal/Dismiss) is rendered twice, simultaneously, and the scene declares the OTHER copy primary; the ribbon twin is configured through `variant`, which is not a glass-ui 7.0.0 Button prop** (dead attribute; my dist grep → 0). In-card `:40-47`; ribbon twin SpringScene.vue:138-156 (frontier-live, `git diff origin/master` empty); gates re-walked (selectedControl pinned `"spring"`; App renders ribbonContent unconditionally); "its domain verb is the primary control for that face" verified. This seat's note: the scene comment declares the ribbon extra **deliberate** ("the cube model — domain controls beside the standard transport"), so the cure is a decision about WHICH copy survives, not a mechanical deletion — and the in-card copy is the one the file's own one-surface law (`:64-70`) indicts. The self-legislation irony is exact: the law was applied to the presets four lines below the violation. **MAJOR** (D's BLOCKER as dissent; the sub-`lg` ribbon-reachability arm decides deletion-vs-breakpoint → SS-13). The `variant` cell cross-books to the forthcoming kf-SpringScene adjudication (one identity). → **NO-WAVE-OWNER** (one-surface decision, one commit with KF-SST-6's state repair).
- **KF-SST-2 · C-1** — **`response` is provably inert on the demonstrated card while a live slider exposes it and the card's own readout prints it as the easing parameter.** Third independent execution this seat: max|Δv| = **2.000e-5** (the `toFixed(5)` floor) for response 0.2→1.0 vs **3.040e-1** for ζ — mechanism at linear-stops.ts:49 (`maxDuration = response * 4` enters only the sampling window; the normalized spring is self-similar under it). Card duration = the constant `var(--duration-slow, 500ms)`; slider live at SpringPhysicsFacet.vue:27-36 (0.1–1.2, my read); readout `:74-76` under "eased by". The ζ axis IS live, so the demonstrator is half-true, not dead. Cure caveat priced (both readers concordant): `transition-duration: calc(response × 4s)` reaches 4.8s at slider max — the repair needs a range decision, not a one-liner. **MAJOR** (C's BLOCKER as dissent — harm is demo-fidelity/honesty, not a break). → **NO-WAVE-OWNER** (instrument-truth core).
- **KF-SST-3 · L-M1 = C-2 (+ C-3 folded — one identity)** — **the "compileToEntry() artifact" does not reproduce the card it labels itself as compiling**: base=CLOSED vs base=OPEN, `.is-open` vs `.is-hidden` (zero `.is-open` markup bindings in the tree — 3 hits, all spec/comment), `transform: translateY(…) scale(…)` vs independent `translate`/`scale`, `500ms` vs the 450ms token, plus the unclaimed `overlay` line the card's list lacks — all five divergences reproduced by this seat's own probe. The `:41` comment's "match the card" assertion is false on 2 of its 3 claims (C-3). The emitter is blameless (three-rule grammar unconditional, entry.ts:429-447); the consumer's spec is the inverse of its subject. A designer who pastes verbatim gets a permanently invisible element; the page itself is safe by Vue scoping. **MAJOR** (C's BLOCKER as dissent — artifact integrity, not a live break; a working CopyButton ships beside it). → **NO-WAVE-OWNER** (the artifact-truth spec, one commit with KF-SST-4; the duration leg lands with KF-SST-27's token decision).
- **KF-SST-4 · L-M2** — **the "dogfood" input is a hand-typed twin of the card** — `ENTER_KEYFRAMES` (useCompiledEntry.ts:24-27) vs the scoped endpoints, different property model, different duration, **no shared constant, token, test, or type** (`ENTER_KEYFRAMES` 1 occurrence; `.discrete-card` only in the SFC + the spec key) — while `:5-8` and `:126-128` claim "dogfooding the emitter, not re-typing its output by hand." The mechanism behind every KF-SST-3 divergence and the standing drift vector. **MAJOR.** → **NO-WAVE-OWNER** (single source of truth for the endpoints — the spec must derive from what the card consumes, or vice versa).
- **KF-SST-5 · D-2** — **the copy-pasteable artifact and the header's API caption render UPPERCASE at 0.1em tracking, corrupting case-sensitive selectors/idents** (`.DISCRETE-CARD`, `.IS-OPEN`, `KF-ENTRY`, `SPRINGLINEARSTOPS()`). Vendor utility verified verbatim (`text-transform: uppercase` + `--type-tracking-caps`); zero countervailing `text-transform` in reach. **Family ≡ banked KF-ES-8** (same vendor utility uppercasing CSS identifiers on a fidelity surface, MAJOR there) + the kf-SequenceAxis D-12 relay — site booked here, mechanism cited, never re-booked. Cure corrected (K-7): `normal-case` alone is the banked MM-29 half-cure (tracking rides on); the house owns the exact idiom — `.code-token`, "case-preserving mono" (verified verbatim) — and `text-mono-small` declares neither corruption. **MAJOR** (A's BLOCKER as dissent: the CopyButton carries the correct-case bytes, so the primary path has an escape hatch). → **NO-WAVE-OWNER** (one copy/register motion with the KF-ES-8 family; one presentation spec with KF-SST-9/19).
- **KF-SST-6 · D-6** — **the two-state disclosure carries no `aria-expanded`/`aria-pressed`/`aria-controls`; the `.btn-playback[aria-pressed="true"]` state style it opts into is consequently dead** (idiom sheet read verbatim; Button synthesizes nothing — dist props enumerated; the controlled card has no `id`; the ribbon twin equally bare, so neither copy shows which way the boolean sits). Demo-wide `aria-expanded|aria-controls` grep → **0** (my run — the fleet family banked at kf-ControlsPaneWrapper, corroborated). This seat's enrichment: glass `accessibility.css` ships `[aria-pressed="true"]` border indication under BOTH `forced-colors: active` and `prefers-contrast: more` (my read) — the withheld attribute forfeits **three** vendor affordances. Rider: the idiom's `border-color` half is separately dead per banked kf-PlaybackRibbon **N-1** (`border: 0` wins), so only the background tint would paint even when cured. **MAJOR.** → **NO-WAVE-OWNER** (one commit with KF-SST-1: the surviving copy gets the state). 
- **KF-SST-7 · L-M4 — FOLD BY REFERENCE → banked KF-CB-2** (kf-CopyButton.md:44, MAJOR, frontier-durable): `copyText(text)` unawaited/uncaught at CopyButton.vue:52; a failed copy is announced to AT as a success. Not re-booked; the local bite recorded — the copy affordance IS this panel's deliverable, and it is the artifact's only uncorrupted route (KF-SST-5). L's BLOCKER dissent already preserved in the banked row.
- **KF-SST-8 · D-5 — FOLD BY REFERENCE → banked KF-CB-6** (kf-CopyButton.md:48, MAJOR): CopyButton's engine-driven inline-style motion is ungated for PRM and structurally exempt from the glass kill switch; the banked row names THIS file's PRM block explicitly as facet (ii) and kills the options-bag cure (the only lever is `g.respectReducedMotion`). Not re-booked.
- **KF-SST-9 · net-new (reader-A MISS-1 ≡ reader-B miss-3, independently convergent; ruling 3)** — **the artifact viewport is structurally incapable of displaying its own content, and it is an unnamed, keyboard-precarious scroll container.** This seat's own execution: **2190 chars / 21 lines / longest line 943** under `whitespace-pre` in a ≤768px measure with `max-h-32` — both-axes scrolling, the longest line >5k px under ANY plausible glyph model; no `tabindex`, no `role`, no accessible name (WCAG 2.1.1 arm engine-dependent; 4.1.2 arm decided). The panel's stated purpose is that a designer READS it (`:50-55`) — and what it displays is also uppercased (KF-SST-5) and polarity-inverted (KF-SST-3). Registry-swept: no scrollable-region 2.1.1 row exists in the bank (nearest mechanisms all differ). **MAJOR** (B's MINOR as dissent). → **NO-WAVE-OWNER** (ONE artifact-panel presentation spec with KF-SST-3/4/5: what it prints, how it prints it, how it is reached); keyboard arm → SS-13.
- **KF-SST-10 · net-new (reader-B miss-1; ruling 4)** — **the spring transport's "Entry" channel is TARGETLESS: selectable, scrubbable, driving no DOM** — while the comments announce "TWO REAL channels" three lines after recording the deletion of the previous decoy, and the facility's own contract line ("Present ⇒ the triad is HONEST for this channel (it paints)", `demo/composables/scene-facility/index.ts:35` — path corrected from B's cell) indicts it. Chain verified whole this session (zero `setTargets` in the folder; `set t` a pure state write; the channel load-bearing as the view switch, inert as motion — the card's real motion is a native CSS transition wholly outside the engine). **Family ≡ banked KF-ES-2** (BLOCKER at kf-EasingScene) — second instance, second scene; site booked here, mechanism cited. **MAJOR.** → **NO-WAVE-OWNER** (the facility-honesty contract, one spec with KF-ES-2's cure; the rendered transport surface when Entry is selected → SS-13).

### MINOR

- **KF-SST-11 · D-3 = L-mi2 = C-5** — displayed ≠ copied in the fallback state (`springCss` on screen, `copyableCss` to clipboard — different by construction), under a `compileToEntry() artifact` label describing neither; structurally reachable at every mount (css seeded `""`, filled post-`await`). **MINOR** (A's MAJOR as dissent — the happy path converges after resolve; the permanent arm is KF-SST-12's). → **NO-WAVE-OWNER** (one state-model spec with KF-SST-12).
- **KF-SST-12 · D-20 = L-mi3 (+ C-13's pre-positioned-failure rider)** — `eligible`/`refusals` discarded (`:76` reads `out.css` only; entry.ts's 9-reason refusal taxonomy returns `css: ""` indistinguishable from pre-resolve); `void recompile()` unguarded, so a chunk rejection or future colour-leaf throw degrades silently to the mislabelled fallback. L's honest scoping sustained and re-executed by this seat (`eligible: true, refusals: 0` — the refusal path is latent; the reachable path is the import rejection). **MINOR.** → **NO-WAVE-OWNER** (loading/error/refusal presentation in the same state-model spec).
- **KF-SST-13 · D-4** — the "MANDATORY PRM degrade" is inert: the vendor's unlayered `!important` universal rule overrides `transition: none` on both longhands (verified verbatim; ruling 8). Rendered PRM is acceptable **by the vendor's accident** (translate/scale absent from the allow-list; 0.1s opacity fade; display instant) — the harm is a false MANDATORY comment, dead code, and an undefended fragility. Kills L-S2/C-S★5 (K-2). **MINOR** (A's MAJOR as dissent). → **NO-WAVE-OWNER** (prose + PRM posture: either adopt `data-allow-motion` semantics deliberately or delete the dead block and say why); rendered PRM witness → KF.W9.
- **KF-SST-14 · D-7(b), with (a) demoted (ruling 10)** — conscripting `.btn-playback` imports the forced-colors focus regression: demo-side `outline: none` at equal specificity, later in source order, defeats glass's `.focus-ring:focus-visible` Highlight restore (both sheets unlayered — my reads); `forced-colors` demo-wide grep → 0, nothing restores it (WCAG 2.4.7). **≡ the banked RB-1 three-rule ordering** (kf-RibbonBar) — fold, extended to this mount; witness rides KF-CE-13 → **KF.W9**. The (a) 48rem-width leg: mechanics confirmed, defect framing killed — deliberate ribbon-consistency reading; percept → SS-13. **MINOR.**
- **KF-SST-15 · D-8** — `min-h-0` at `:26` is dead (scoped unlayered `min-height: 7rem` beats the layered utility) and with every sibling `shrink-0` the card has a hard incompressible floor against `overflow-hidden`. Source-half decided; the ~440px sum and any rendered clip are model → SS-13, **escalates to MAJOR on a witnessed clip at a supported viewport**. **MINOR** (A's MAJOR as dissent). → **NO-WAVE-OWNER.**
- **KF-SST-16 · D-9** — header hierarchy inversion: the title carries `truncate` (auto-min-size 0 — it shrinks to nothing) while the decorative caption's `whitespace-nowrap` makes its min-content unshrinkable — the loudest rung yields to the quietest, decided by the flex rules alone. Truncation point → SS-13, **escalates on a witnessed mobile truncation**. Note the shipped cure: glass `.card-title` carries `overflow-wrap: anywhere` (C-8's observation) — adopting the family fixes this for free. **MINOR** (A's MAJOR as dissent). → **NO-WAVE-OWNER** (with KF-SST-28).
- **KF-SST-17 · D-10** — the card `@starting-style` demonstrates has no border, no outline, and no forced-colors block anywhere in the file; the wash-vs-plate edge models at ~1.19:1 light / ~1.27:1 dark (two independent analytic derivations concordant to the third decimal; reader-A proved two of the axis's four model assumptions at the bytes — `--glass-level` IS registered initial 1; only `.glass-floating` re-declares the backdrop). Under forced colors the element loses every boundary while the motion still runs. The file's own `.active-preset-chip` proves the surviving idiom (`outline: 1px dashed`, `:148` — outlines survive forced colors). Percept + blur-over-graph-paper → SS-13, **escalates on witness**; forced-colors half folds with KF-CE-13 → **KF.W9**. **MINOR** (A's MAJOR as dissent). → **NO-WAVE-OWNER** (boundary cure = an outline, per the file's own idiom).
- **KF-SST-18 · D-11 (K-2 demotion sustained twice)** — the wash's AA margin rests on the unreferenced `glass/ladder.css` `--muted-foreground` repoint; passing in both arms (axis 4.77/4.67; reader-A's recompute 4.89/5.03 — the demotion is if anything understated), authored as a bare `14%` with no contrast reasoning. Fragility, not violation. **MINOR.** Rendered ratios → SS-13. → **NO-WAVE-OWNER** (annotation beside the wash).
- **KF-SST-19 · D-12** — the T.D4 mono contract satisfied by a class that renders nothing: `:17-19` is a digit-free mono UI label wearing `tabular-nums` to pass `monoAllowedSelectors` while `_monoContract` (read verbatim this session, incl. "A new demo-authored mono UI label reds the census") licenses no such leaf; the honest use sits at `:74-76`. Laundering family ≡ KF-ES-8's census arm. **MINOR.** → **NO-WAVE-OWNER** (one motion with KF-SST-5: the caption becomes a `.code-token` chip + body-voice text).
- **KF-SST-20 · D-13** — stale rationale contradicting the ratified colour authority ("repointed red by Lane B" vs style.css:155-163's "red exits the chrome entirely", read verbatim; playback-idiom.css records the same correction for its own comment — this file was not swept). **MINOR.** → **NO-WAVE-OWNER** (prose sweep).
- **KF-SST-21 · D-14 = L-mi4 = C-11** — dead references: `ToggleChip` ×1 (3 tree hits, all comments), `SpringSidebar` ×3 (`find` → empty) + the imported composable's docstring citing BOTH its consumers wrongly (deleted file + wrong line; real consumers SpringTrace.vue:42 and `:116`). **MINOR.** → **NO-WAVE-OWNER** (prose sweep, one checklist with KF-SST-20/22).
- **KF-SST-22 · D-15** — ~30% of the file is rationale prose (65/216 by reader-A's mechanical count, axis-concordant) against DESIGN.md §6's ownership rule, with three blocks provably wrong or inert (D-13, D-14, D-4) — the concrete cost of minting authority at the call site. **MINOR.** → **NO-WAVE-OWNER** (prose sweep).
- **KF-SST-23 · D-16** — two spacing systems in one file; the preset chip's block padding is `0.05rem` = **0.8px** on a `--radius-pill` chip (verified at `:146`) — the rounded geometry the token buys is never expressed. **MINOR.** → **NO-WAVE-OWNER** (hygiene).
- **KF-SST-24 · D-17** — the plate has inline padding and zero block padding (`px-6 lg:px-8`, raw children; glass's φ-derived `--card-pad-block` defined and never consumed — dist read concordant), PLUS reader-A's strengthening verified in my SFC read: `justify-center` at `:11` distributes nothing because the `:25` child is `flex-1` — header and preset line sit flush against the rounded edges unconditionally. **MINOR.** → **NO-WAVE-OWNER** (dies if KF-SST-28 adopts the Card family).
- **KF-SST-25 · D-19 (= L-mi5 as-corrected, ruling 9)** — the header comment asserts `tier="resting" surface="glass"` props the template does not pass; the register is reached only through an undeclared private vendor map that DESIGN.md §3 and style.css's `[data-tier="resting"]` tint reclaim both hang off. **MINOR.** → **NO-WAVE-OWNER** (pass `tier` explicitly or cite the chain once, correctly).
- **KF-SST-26 · L-mi6 = C-6** — one duration, three numbers (token 450ms; this file's fallback 500ms ×4; sibling 420ms; artifact hardcodes 500 — the KF-SST-3 duration leg), plus two radius fallbacks each 1.33–2× off the resolved tokens (components-layer `--radius-lg` 0.5rem vs written 1rem; `--radius-md` 6px vs written 0.5rem). C-6's F-1 escalation leg dies with K-1. **MINOR.** → **NO-WAVE-OWNER** (one token-truth commit; the duration decision is KF-SST-2/3's).
- **KF-SST-27 · L-mi1 = C-7** — the preset-identity predicate duplicated verbatim (same `1e-6`, same two params) across `:104-108` and SpringPhysicsFacet.vue:165-167, both verified whole; `springPresets.ts` is the obvious home. **MINOR.** → **NO-WAVE-OWNER** (hoist).
- **KF-SST-28 · C-8 + reader-A MISS-4 (one identity: the declined Card family)** — consumes the `Card` shell but hand-rolls the header glass-ui ships (`CardHeader`/`CardTitle`/`CardContent` exported; the sibling consumes the family), AND the consequence C-8 never drew: the title is a `<span>` at heading scale with no heading semantics, the plate no named region — the specimen is invisible to heading/landmark navigation. **Family ≡ banked KF-ES-28** (second site, cited not re-booked fresh). `.card-title`'s `overflow-wrap: anywhere` is simultaneously the KF-SST-16 cure. **MINOR.** → **KF.W6** (family adoption decision) + **NO-WAVE-OWNER** (heading/landmark one-liner if the family is declined).
- **KF-SST-29 · reader-A MISS-3** — the card's only copy affordance declines the `label` prop (verified: default `"Copy to clipboard"`; EasingTarget.vue:38 `label="Copy easing literal"` proves the house idiom), so its accessible name never identifies what it copies — on a 16×16 glyph in a panel with exactly one copyable thing. Distinct row from banked KF-CB-3 (the latch) and KF-CB-10 (N identical names) — the family cited, this call-site's declined prop booked. **MINOR.** → **NO-WAVE-OWNER** (one line, lands with the KF-CB spec).
- **KF-SST-30 · reader-A MISS-2 (ruling 2)** — **glass-ui 7.0.0's `@utility touch-hit-area` cannot expand a tap target: its expander is `pointer-events: none`** (verified verbatim; the dock's `::after` twin omits it and works). Kills D-23's prescribed cure; **corrects banked KF-CO-24 and kf-CubeScene MISS-3** (annotation riders per E-3 — both count the decorative twin among their "sanctioned cures"); leaves `.tap-floor` (real box, zero adopters — banked KF-CB-25) the only working in-reach mechanism. **MINOR here; producer-owned.** → **glass-ui BH relay** (owner edict — the fond) + **KF.W6** (the touch-target family decision inherits the correction).
- **KF-SST-31 · reader-B miss-2** — the heavy entry compile is unthrottled AND ungated by view: every 0.01-step slider tick fires a full `compileToEntry` (spring solve + four-body serialization ≈ 2.2KB of string building), including in the solver view where the readout is not even mounted — in a folder that ships the house throttle seam one file away (`useSpringHotPath.ts:45-48`, verified verbatim: "the shared few-Hz cold-path throttle seam"). Family ≡ banked kf-CubeScene L-9 / KF-KC-18 (cited, not re-booked as novel). Rider: ~110 concurrent unguarded promises share `css.value` with no generation token — ordering holds today only by constant await-count. **MINOR.** → **NO-WAVE-OWNER** (gate on `view === "discrete"` + the house throttle; one commit with KF-SST-12's guard).

### INFO

- **KF-SST-32 · D-18** — no `dir="ltr"` on the code artifact; latent (zero `dir=` demo-wide, my grep — project-level RTL posture, per the kf-SequencePlayhead D-15 precedent). **INFO** (axis MINOR as dissent). → **KF.W9** (RTL posture).
- **KF-SST-33 · D-21 = C-10 (+ fold)** — "eased by" twice in one plate for two different objects; "Hello, spring."; `emphasis="secondary"` restates the dist default — the redundant-prop leg **folds to banked kf-PlaybackRibbon N-6**. Substrate note (K-10): this row exists only at the worktree/frontier bytes — at HEAD the attribute was the dead `variant`. **INFO.** → prose sweep rider.
- **KF-SST-34 · L-i1/L-i2 = C-12** — `inject(KEY)!` as provider contract (fleet idiom, sole mount verified), the ~30-member `SpringDemoContext` typed for a 5-member need (springKeys.ts:4 verified) — why KF-SST-2's cure has no narrow seam — and the split ref idiom (both halves correct Vue; nested refs in a plain injected object do not auto-unwrap). **INFO.** → fleet wave posture.
- **KF-SST-35 · C-9** — barrel import where subpaths exist; house-consistent. **INFO.**
- **KF-SST-36 · C-13 — FOLD BY REFERENCE → banked KF-CB-33** (negative finding upheld AND predictive): the R1 colour-parser class is unreachable here (opacity/transform only; `eligible: true` re-executed by this seat); the pre-positioned failure is KF-SST-12's unguarded seam. Not re-booked; recorded as a KF.W3 scoping input, per the banked row.
- **KF-SST-37 · reader-B miss-4** — `useCompiledEntry`'s exported signature erases the engine's Vars generic with `CSSKeyframesAnimation<any>` (`:46`, verified), and the `any` crosses the scene-facility seam at useSpringDemo:414 — unenforced today for the banked KF-CB-8 reason (demo/ never typechecked on the merge path). **INFO.** → KF.W4 rider (with the KF-CB-8 gate-topology row).
- **KF-SST-38 · reader-A MISS-5 — FOLD BY REFERENCE → banked kf-PlaybackRibbon D-10**: the accent-on-accent-wash label contrast fails at this mount too (A's composite 3.46/3.61 within 0.12 of the banked figures; no large-text exemption at `--type-body`/500). Blast-radius extension recorded; the banked cure applies (`--accent-kf-foreground` ink on solid `--accent-kf`); no row booked. **INFO.**
- **KF-SST-39 · D-23 — FOLD BY REFERENCE → banked KF-CB-14 + KF-CB-5** (16×16 zero-padding copy target; demoted MINOR there with the WCAG 2.5.8 spacing exception D-23 itself concedes) — now carrying KF-SST-30's correction: the axis's prescribed cure is the decorative twin. **INFO** (fold).

## Killed-claims register — 12 entries

| # | killed claim | adjudication |
|---|---|---|
| **K-1** | **L-B1 (BLOCKER)** — "glass-ui undeclared + unlocked; cannot build from clean install" | **KILLED BY BASELINE LAW** (ruling 7; both readers convergent, re-verified at git by this seat: HEAD :71 optionalDep 6.0.0 / origin-master :77 exact 7.0.0 = installed / only the worktree deletes it). ≡ census **SCH-1** → KF.W0 §B-12 — never re-booked as a component row. C-6's F-1 escalation leg and D's read-set preamble die with it. |
| **K-2** | **L-S2 + C-S★5** — the two PRM superlatives ("the media block actually wins" / "correctly incomplete … the author knew it") | **BOTH RETIRED** (ruling 8) — the vendor's unlayered `!important` universal rule wins both longhands; the praised reasoning is real in the comment and inoperative in the cascade; `display` snaps only by the vendor's allow-list accident. Neither falsifier ever left the file. D-4 (KF-SST-13) stands. |
| **K-3** | **L-M3 aggravation #2** — "the ribbon twin renders at default `emphasis: 'primary'` — two different-weight commands" | **KILLED at the dist** (ruling 6; my grep: `emphasis: { default: "secondary" }`). No weight divergence; the parent finding survives whole. The only L cell asserting a value it never read. |
| **K-4** | **L-mi5's mechanism** — "`tier: {}` has no default, so `tier=\"resting\"` is simply not in effect — half fiction" | **KILLED** (ruling 9) — the Surface material→tier map stamps `data-tier="resting"`; both halves of the comment are true of the RENDERED output. D's own §Killed K-1 pre-killed this; L re-derived and shipped it anyway. Survivor = KF-SST-25. |
| **K-5** | **D-22 + reader-A's promotion** — "decorative lucide icons not hidden from AT; PROMOTED TO DECIDED-AT-SOURCE" | **KILLED BY THE BANK** (ruling 1) — kf-CopyButton killed-register #8 (K9): Children-Presentational prunes SVG descendants of `button`; the omission changes nothing exposed. Reader-A's byte-fact (lucide ships no `aria-hidden` — verified) is true and non-load-bearing. **First overruled reader-confirmation in this ledger.** |
| **K-6** | **D-7(a) framing** — "a two-word toggle renders 48rem wide … `shrink-0` cannot help" | **DEFECT FRAMING KILLED** (ruling 10) — mechanics confirmed, intent inference unsupported: the ribbon twin is deliberately full-width, matching width is the consistent reading, `shrink-0` governs the block axis in this flex-col. → INFO percept, SS-13. The (b) leg survives as KF-SST-14. |
| **K-7** | **D-2's prescribed cure** — "`normal-case` … applied at five sites; this file omits it" | **PARTIAL KILL** — banked **MM-29** proves `normal-case` cancels only `text-transform`; 0.1em caps tracking rides onto a `whitespace-pre` mono block, destroying the alignment D itself names as mono's purpose. True house cure: **`.code-token`** (design-idioms.css:245-248, "case-preserving mono" — verified verbatim, unused here) or `text-mono-small`, or `tracking-normal` beside. |
| **K-8** | **L-S3's falsifier cell** — "`Surface-DOHf5u2R.js` contains no `inheritAttrs`" | **CELL KILLED** (reader-A's correction, adopted) — Surface declares `inheritAttrs: !1`; the seam survives TWO boundaries because Card re-merges `attrs.style` and Surface's `mergeProps` MERGES style. The superlative's conclusion survives on the merge-semantics ground (S-3 below), more fragile than L stated. |
| **K-9** | **D-4's falsifier-provenance cell** — "`styles/utilities.css` imports it with no `@layer` wrapper" | **CELL CORRECTED** (my import-chain read): utilities.css imports only base/base-misc/metal/components/btn; the real chain is `index.css` (last import, unlayered) → `accessibility.css` → `utilities/a11y-overrides.css`. Conclusion unaffected; recorded so a cure author reads the right file. |
| **K-10** | **All three axes' substrate header** — "keyframes.js master @ `8281638c`" | **CORRECTED, with a finding inside the correction** (reader-B's probe, re-run by this seat): the axes read WORKTREE bytes — at HEAD, target `:41` carried `variant="outline"` (both verb copies dead-propped; KF-SST-33's restated-default leg did not exist at HEAD). The frontier probe rescues everything: read set ≡ origin/master (sole diff = CopyButton's banked 1-word repair), so every roster row is frontier-durable by identity. |
| **K-11** | **Both readers' "four ~700-char `linear()` bodies" cells** (inherited from the L axis's elision note) | **CELLS CORRECTED BY EXECUTION** (ruling 3; this seat's probe): total 2190 chars — the bodies are **~430 chars each**; the 943-char line carries two. Reader-A repeated the stale gloss in the same sentence as its own contradicting measurement — a citation-inheritance micro-event (the ColorSpaceSelector DISSENT-1 class). The geometry conclusion is unchanged and strengthened by being exact. |
| **K-12** | **Reader-B's Entry-channel citation cells** — the facility contract at "`src/animation/scene-facility/index.ts:35`"; the progress getter "`() => entryAnim.t/dur`" | **CELLS CORRECTED** (ruling 4): the contract lives at `demo/composables/scene-facility/index.ts:35` (content exact); the shipped getter carries a `dur > 0` guard + clamp. Substance of KF-SST-10 unchanged. |

## Superlatives (provenance preserved)

1. **The `transition-behavior` split** (`:172-179`) — shorthand list, then the longhand AFTER (the shorthand resets it), with the correct reason recorded in place. All three axes + both readers convergent (S-1 = L-S1 = S★4 — the corpus's one unanimous positive). Reader-A's provenance adopted: this is the demo's ONLY `transition-behavior`/`allow-discrete` author (two other hits, both prose) — no specimen to have copied; the correctness is authored.
2. **Independent `translate`/`scale` instead of a `transform` list, held across all three states** (`:169-170`, `:186-187`, `:195-196` — my SFC read). Each property carries the spring `linear()` on its own timeline. The same file's artifact uses the older `transform` model — the live CSS is the more modern of the two, the opposite of the usual drift direction (and exactly what KF-SST-4's twin fails to match).
3. **`--spring-ease` survives TWO `inheritAttrs: false` boundaries** (Card AND Surface — K-8's corrected ground): Card re-forwards `attrs.style`, Surface's `mergeProps` merges style. A library-computed `linear()` driving a native CSS transition — zero rAF, zero engine instance, zero teardown — riding two silent-failure seams correctly. (L-S3 = S★2, restated on the merge-semantics ground.)
4. **`springTimingFunction(…).css === springLinearStops(…)` — byte-identical**, re-executed by this seat → `true`. The "ONE springLinearStops surface" claim is exact on the easing axis: the artifact's easing string and the live `--spring-ease` are literally the same string. (S★3, third independent execution.)
5. **The geometry is RTL-clean by construction** (D-S2) — block-axis motion only, symmetric paddings, no physical-side declaration in the scoped block (my whole read). KF-SST-32 is a bidi TEXT gap, not a geometry gap — the axis drew that line correctly itself.
6. **`@starting-style` survives the Vue scoped-CSS transform** (S★7 = L's Not-claimed #1) — the scoped plugin skips only `keyframes` at-rules; `.discrete-card` inside `@starting-style` receives `[data-v-…]`. Sole author demo-wide (reader-A's provenance) — a leak would have had no witness; checked rather than assumed, filed so no later pass re-spends the probe.
7. **`:shadow="false"` is a REAL fork** (S★1) — Card overrides Surface's `false` default to `true` at the dist (both readers' extractions concordant); the "FORK I5-shadow" brand is accurate. And every suspected phantom token resolved (S★6) — in a repo with a documented dangling-`--spring-*` history. The author was right about the prop-comment that mattered; D-19 is the one that drifted.
8. **`.active-preset-chip` uses `outline`, not `border`** (`:148`, verified) — survives forced colors where the sibling card's box-shadow dies (KF-SST-17's own observation), and out-of-flow so the 0.8px-padded geometry is unperturbed. The right instinct, correctly executed, on the wrong element.
9. **The K.W4 S5 de-duplication is the right call, rescoped** (D-S4, per the kf-CopyButton S-3 precedent): retiring the triple-rendered preset picker and DEMOTING the information to one truthful result line with a `custom` fallback is textbook elision — recorded as a NEUTRAL precondition of KF-SST-1 as much as a virtue: the identical law was not turned on the component's own primary verb four lines above the comment stating it.
10. **The corpus's falsifier discipline, adjudicated** (both readers' closing observations adopted, and this seat concurs after attacking it): D published a §Killed with a self-demoted would-be BLOCKER (K-2, sustained twice over); L published a §Not-claimed pre-spending five probes; C published a negative finding (C-13) and honestly reported failing to find the sample-count divergence it went looking for. The corpus's failures are ONE class — falsifiers scoped to the file/repo when the decider lives in `node_modules` (the PRM kill switch, the Button default) or in git (the baseline). **RETIRED**: L-S2/C-S★5 (K-2). **RIDER carried on D-S5** (CopyButton's AT announcement order): the order discipline is banked-sustained (kf-CopyButton superlative 8) but must never be re-sold without the KF-CB-9 placement rider — the sink is a descendant of the `<button>` the a11y tree may flatten.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **The ribbon's sub-`lg` reachability** (sheet-at-peek, SpringScene.vue:46-50 / useSheetState) — decides whether KF-SST-1's in-card copy is a duplicate (delete) or a responsive fallback (breakpoint). Desktop is decided against the fallback reading (shell force-opens at ≥1024px).
2. **KF-SST-15's clip** — does the work area drop under the ~440px incompressible floor at any supported viewport (escalation trigger).
3. **KF-SST-16's truncation point** — the title-vs-caption budget at 390px (escalation trigger; the ordering is decided).
4. **KF-SST-17's percept** — is the boundaryless card legible as a card entering/exiting, both themes, over the graph-paper field the analytic model cannot cover (escalation trigger); plus KF-SST-18's rendered ratios.
5. **Forced-colors renderings** — KF-SST-14's missing focus ring and KF-SST-17's total boundary loss; decided at the cascade, never witnessed; fold with KF-CE-13 → KF.W9.
6. **KF-SST-9's keyboard arm** — whether the artifact scroller is reachable in engines without default-focusable scroll containers (Safari; Chromium <127); the missing role/name is decided on source regardless.
7. **KF-SST-14(a)/KF-SST-33's percepts** — the 768px toggle proportion; whether the twin verbs read as an obvious double (L-M3's own UNPROVEN).
8. **The Entry-channel rendered surface** (KF-SST-10) — what the transport presents with Entry selected: the KeyframeTimeline over `targets: []`, the scrubber moving against a card that does not.
9. **Banked KF-CB-9's announcement probe** — if the nested `role="status"` region is silent, the copy affordance on THIS card has zero working feedback on any channel, which re-weights KF-SST-7/11/29/39 together (the banked "single highest-value SS-13 probe" reaches through this mount).
10. **`@starting-style` re-fire on `.is-hidden` → visible** in the shipping engines (the `:182` comment asserts it; the spec says yes; not executable statically).

## Identity guards (fold-by-reference — never re-booked)

- **K-1 ≡ census SCH-1 ≡ F-1** → KF.W0 §B-12 (the substrate row; sixth one-baseline-short event recorded there).
- **KF-SST-7 ≡ KF-CB-2 · KF-SST-8 ≡ KF-CB-6 · KF-SST-36 ≡ KF-CB-33 · KF-SST-38 ≡ kf-PlaybackRibbon D-10 · KF-SST-39 ≡ KF-CB-14/KF-CB-5** — folds, blast-radius extensions recorded in place.
- **KF-SST-5 sites the KF-ES-8 family · KF-SST-10 sites the KF-ES-2 family · KF-SST-28 sites the KF-ES-28 family · KF-SST-31 sites the kf-CubeScene L-9/KF-KC-18 family** — second instances booked as site rows, mechanisms cited, never re-minted.
- **KF-SST-30 CORRECTS banked KF-CO-24 and kf-CubeScene MISS-3** — annotation riders (E-3: annotate beside, never rewrite); the producer defect rides the glass-ui BH relay.
- **KF-SST-1's `variant` cell cross-books to the forthcoming kf-SpringScene adjudication** — one identity; the verb-duplication row lives HERE (anti-rename).
- **KF-SST-6's border-half rider ≡ kf-PlaybackRibbon N-1 · KF-SST-33's prop leg ≡ kf-PlaybackRibbon N-6** — cells, not rows.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the K-1/SCH-1 fold (§B-12). — **KF.W3**: KF-SST-36's scoping input (per KF-CB-33). — **KF.W4**: KF-SST-37 rider (with KF-CB-8's gate-topology row). — **KF.W6**: KF-SST-28 (Card-family adoption) · KF-SST-30's consumer half + the touch-target family correction (with KF-CO-24/KF-CB-14) · KF-SST-39's fold. — **KF.W9**: forced-colors witnesses (KF-SST-14/17, KF-CE-13 family) · rendered PRM (KF-SST-13) · RTL posture (KF-SST-32) · the SS-13 list. — **glass-ui BH relay** (owner edict, the fond): KF-SST-30's producer defect — `touch-hit-area`'s `pointer-events: none` expander vs the dock's working twin, one utility, two behaviors, the general-purpose name on the decorative one.
- **NO-WAVE-OWNER — the spring artifact-truth packet (SS-1/SS-2 authoring must home it):** the **artifact core KF-SST-3 · 4 · 5 · 9 (+ 11 · 12 · 26)** — ONE spec: a single source of truth for the endpoints, the true polarity/duration, a case-preserving register, a readable+reachable viewport, one honest state model with a refusal surface; **the verb/state pair KF-SST-1 · 6** — which copy survives, and the survivor gets `aria-expanded`/`aria-pressed` (buying three vendor affordances); **the instrument-truth row KF-SST-2** (the response/duration decision, priced range caveat); **the facility-honesty row KF-SST-10** (with KF-ES-2's cure spec); the **hot-path row KF-SST-31** (view gate + house throttle, with 12's guard); the **prose-truth sweep KF-SST-20 · 21 · 22 (+ 13's dead block, 25's chain, 33's leads)**; the **hygiene tail KF-SST-15 · 16 · 17 · 18 · 19 · 23 · 24 · 27 · 29**.

## Verdict

**ADJUDICATED.** Two hostile readers and an incredulous third seat leave the record cutting both ways: every axis BLOCKER demoted with its dissent preserved (one killed outright by baseline law — the program's sixth one-baseline-short event), one reader confirmation overruled by a bank kill it never swept, one inherited "~700-char" gloss corrected by this seat's own execution in the same numbers that expose the inheritance, two banked registry prescriptions inverted by a single verified `pointer-events: none`, and two superlatives retired because the declaration they praised loses the cascade. What survives is precise: a component whose live `@starting-style` CSS is the best-authored motion block in the demo — five craft superlatives, three re-executed — standing directly on top of an artifact panel that misstates its polarity, its duration, its case, its provenance, and its own legibility, beside a verb rendered twice under a one-surface law the file itself legislates, and a transport channel that is honest as a switch and false as a channel. 0 BLOCKER · 10 MAJOR (2 folds) · 21 MINOR · 8 INFO; 12 kills; 7 net-new admissions; 10 residues to SS-13. The repair is a packet, not a rewrite: one artifact spec, one surviving verb with real state, one honest channel, one prose sweep. Nothing here stamps a wave; every routing is agglomeration input for the forming X·KF specs. Original ids — D/L/C for life, KF-SST-1..39 for life.

— end of apotheosis r1. The three challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/spring/StartingStyleTarget.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **StartingStyleTarget.vue=+1/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` appears in this record but NOT in the re-anchor sense** — it is a component-local id here, and the record-qualification rule (KF-W0 §Carry C-1.R row 6) keeps this sweep from booking a dead-token cluster as a re-anchor obligation. Eight records are in this class: kf-ChannelOptions · kf-CubeScene · kf-KeyframesStringControls · kf-OrbitalDrag · kf-SquareScene · kf-SquareInstrument · kf-StartingStyleTarget · kf-TimelineCaret.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — **KF-SST-15 · KF-SST-16 · KF-SST-17**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here. **Nothing above this line is
rewritten** — no edit, no re-grade, no renumber, no deletion, no new id (E-1 immutable bytes / E-3
addenda-not-patch). This addendum records a MEASUREMENT STATE, not a grade: **the banked severity is
unchanged and stays exactly where the record put it.***

- **armed trigger** — register row **6** of thirteen, `evidence/W9/ESCALATION-REGISTER.json` (id as banked there: `KF-SST-15/-16/-17`); ceiling if it fires: **MAJOR**; roster cell(s): `—`.
- **measurement named by the trigger** — the `@starting-style` entry transition's rendered first frame, **STAMPED** (KF-AX-4) — an un-stamped frame cannot be placed in the entry sequence and does not discriminate.
- **measured value** — **NONE. The trigger did not run.**
- **resulting severity** — **PROVISIONAL (unrun)**. The banked grade stands; it is neither inherited as confirmed nor moved.
- **exact precondition** — one stamped first frame. No stamped entry frame was captured in any cell this wave. the `chromium` and `chromium/emulated-forced-colors` cells DID open at `.d` (8 rows, 2 + 2 captures), so this trigger is **unrun, not foreclosed** there: it needs a pass that mounts the subject. the `safari-app/desktop` cell was open and productive at `.d` (4 sessions, OP-4 taken, 3 shots) and **closed mid-seat**: session creation began returning *"You must enable 'Allow remote automation'…"*; `safaridriver --enable` → **`Password:Password is not valid`** (an interactive admin authorization no seat here can supply); the running Safari is the **owner's** (pid 23725, started 16:23:59, 6 windows) and was not quit. **Precondition: 'Allow Remote Automation' re-enabled by a hand that can answer an admin prompt, or the owner's Safari restarted.** The `data-allow-motion` producer-inert policy stays decided ONCE demo-wide (KF-SST-13, third witness; S-3) and is **not** re-decided here.
- **substrate of this wave** — keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD; frontier moved to `3e81f500`, the pin is its ancestor, **no re-pin on a seat's authority**). Served bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448`, built in the §0m.2 capture clone and **re-hashed at this seat, double-run identical**. Capture: **none for this row**; shot-sha256: **none**.
- **G-KFW9-13 reading** — this row closes the gate's *"unrun triggers reported PROVISIONAL at close"* arm, **not** its *"measured value written back"* arm. The gate stays **RED**.
