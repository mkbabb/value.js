**Served model id**: `claude-fable-5` (Fable 5) — M-23/L-18 fresh-adjudicator receipt, observed in this seat's own system prompt, not inherited.

# AdminListSkeleton — adjudicated apotheosis (M-23/L-18, fresh-Fable seat)

**Seat**: apotheosis adjudicator (incredulity seat) · **Date**: 2026-08-03 · **HEAD**: `64aca0a2` · branch `tranche-u`.
**Corpus**: 3 challenge axes (`audit/components/AdminListSkeleton/challenge-{D-design,C-implementation,L-library}.md`, r2 governs, all SOURCE-RED) against `demo/palettes/browser/admin/AdminListSkeleton.vue` (24 lines) and its six call sites.
**Re-readers adjudicated**: hostile re-reader 1 (design-and-usage) · hostile re-reader 2 (library-and-code). Both returns were attacked before either was adopted; every load-bearing byte claim was re-derived by this seat's own instruments.
**Arbiter's own instruments this session (all static; zero browser probes — probe parsimony)**: full reads of AdminListSkeleton.vue (whole), AdminListItem.vue (whole), AdminAuditPanel.vue (whole), AdminTagsPanel.vue:1-80, AdminUsersPanel.vue:40-150, AdminFlaggedPanel.vue:10-109, AdminNamesPanel.vue:24-96, `demo/styles/utils.css:30-69`, `demo/palettes/useAdminAudit.ts:45-70`, PaletteCardSkeleton.vue:20-36/:87-125, ShadowPalette.vue:30-60/:100-115; installed producer bytes: `node_modules/@mkbabb/glass-ui/dist/components/skeleton/Skeleton.vue.d.ts` (whole), `dist/data-table-BygKg6ZA.js:125-150` (the compiled Skeleton), `dist/glass-ui.css` skeleton rules extracted with their `@media` wrappers (python slice around `skeleton-scan`), `dist/styles/tokens/scheme-motion.css` (`--duration-shimmer: 5s`), glass-ui package version (7.0.0); censuses: `grep -rn 'skeleton-glass-bg|skeleton-shimmer-tint|skeleton-shimmer-delay|skeleton-ink' glass-ui/dist/` → **0 hits**; `aria-busy` in demo/ → **0**; `admin-list-skeleton` in demo/ → **1** (the declaration); bare `--spacing:` overrides in demo/styles + glass-ui/dist/styles → **0**; `AbortController|AbortSignal|timeout` in demo/palettes/api/*.ts → **0**; `skeleton-ink-register` consumers → 4 (ShadowPalette:43, PaletteCardSkeleton:34, AdminTagsPanel:53, AdminListSkeleton:9). Formation cross-reads: `registry/adjudicated/AdminUsersPanel.md` (MT-AU1 §2/§3 — L-6, Δ-11, scope items), `registry/CARRY-CUT-LEDGER.md` (CC-005..010, CC-075/076/081), `docs/tranches/X/waves/W3.md` (no transport deadline), `W5.md` (shell status region ≠ this node), `W7.md` (file bounds, strictTemplates G3, :627), `W8.md` (:13/:47/:165/:309-317 G-9), `W10.md` (no skeleton row), `audit/codex-provenance/motion-quarantine.md` rows M-09..M-15.
**Quarantine law honoured**: M-11 and M-13 are FALSE rows and are not carried; M-10/M-12/M-14 cited as discharged/true; M-09 carried in its non-motion sense; M-15 left UNPROVEN, unadjudicated.

## Scoreboard

**6 CONFIRMED (2 MAJOR families + 1 MAJOR producer-seam + 2 MINOR + 1 INFO) · 10 KILLED (incl. one reader superlative killed by this seat) · 10 MISSED adopted (9 from the readers, 1 new from this seat) · 6 UNPROVEN-NEEDS-LIVE.**

Headline: **the corpus filed the accessibility defect upside-down and never filed the producer regression at all.** The axes accused the skeleton of over-announcing; the live bytes prove the admin loading state is structurally SILENT (producer-hidden children inside empty `role="status"` shells, author-written collection labels resting on role-generic divs). And the component's entire certified ink recipe (`--skeleton-glass-bg`, E1-R2 ΔL certification) is dead against installed glass-ui 7.0.0 — zero reads across the whole dist — a finding no axis and only one re-reader produced.

## Ruled disagreements between the re-readers (this seat's actual work)

| # | subject | reader 1 (design-and-usage) | reader 2 (library-and-code) | RULING (deciding evidence = this seat's own) |
|---|---|---|---|---|
| 1 | **the `skeleton-ink-register` seam** | MISSED-MAJOR: the token contract is DEAD at the producer; utils.css still certifies it as live | SUPERLATIVE #1: "a real single source… guaranteed a bounded ΔL … by construction. Four independent consumers read it verbatim" | **Reader 1 wins; reader 2's superlative is KILLED as written.** My grep over the entire `glass-ui/dist/`: `skeleton-glass-bg` / `skeleton-shimmer-tint` / `skeleton-ink` = **0 hits**. The served rule is `.skeleton[data-v-cd03d0b0]{…background:var(--muted)…}` and the sheen is a hardcoded `color-mix(in oklab, var(--foreground) 10%, transparent)` (my python slice of glass-ui.css). Consumers *declare* the vars; the producer never *reads* them. The guarantee reaches only ShadowPalette's direct readers (`.shadow-seg{background:var(--skeleton-ink)}` :104-113 — my read). One calibration in reader 2's favour: utils.css:40-41 labels the *shimmer-tint* line honestly aspirational ("once glass-ui's `::after` reads it"), and PaletteCardSkeleton:20-22 concedes the same — the FALSE prose is the *background*-seam sentence (:37-39 "every skeleton surface … reads this … through glass-ui's `--skeleton-glass-bg` seam") and the E1-R2 certification (:44-55), both stated as present fact. |
| 2 | **AdminTagsPanel as the shipped cure** | "ONE `role=status` … with N **bare** `<Skeleton>` children — proving the cure is available, in-house, and cheap" | "wrapping five **bare** `<Skeleton>` children (:57-64) that carry no role of their own" | **Both readers WRONG on "bare" — corrected by this seat.** AdminTagsPanel.vue:57-64 passes `surface="glass" variant="breath"` on all five Skeletons (my read). The container-owned `role="status"` pattern IS the right exemplar (that limb stands), but the exemplar carries five dead attr pairs of its own — and they sit **outside X-W8 G-9's census as written** (G-9 names only `AdminListSkeleton.vue`; W8:165/:310). New arbiter finding, see A-3 rider. |
| 3 | **row-frame duplication census** | (did not count; killed the "copied classes" limb on the min-w-0 drift) | "duplicated THREE times, not two" (:11, :9, Audit :62) | **Both undercounted; the adjudicated census is FIVE homes** — AdminListItem:11 + AdminListSkeleton:9 + AdminAuditPanel:62 + AdminUsersPanel:80 (border split onto :68) + AdminFlaggedPanel:50 (border on :47) — already ruled at `adjudicated/AdminUsersPanel.md` MT-AU1 §2 L-6 and re-verified line-by-line by this seat. One identity, per the anti-rename guard; A-1/A-5 cite it, never re-mint it. |
| 4 | **`aria-label` on the role-less wrappers** | "silently dropped by **every conforming engine** … discarded, not merely ignored" | "the name is discarded by the accessibility tree" (same absolutism) | **CONFIRMED as a defect, DEMOTED as a mechanism** — the precedent calibration governs: MT-AU1 §3 Δ-11 (C-26's corrected ground) already adjudicated the same fact at the same file class (`:46/:135 put aria-label on role-generic divs — ARIA 1.2 prohibits it — behaviour undefined, engine-dependent, demonstrated not assumed`). The defect (six author-written collection names resting on a prohibited attribute) is byte-proven at all six sites (my reads); the *universal-drop* claim moves to UNPROVEN-NEEDS-LIVE. Both readers' bottom line (the names don't reliably reach anyone) survives; their certainty does not. |
| 5 | **over- vs under-announcement** | kills the D-1/C-1 harm mechanism, CONFIRMS a "structural limb" as its own MAJOR row, files silence as missed | kills C-1/D-1 whole, refiles silence as missed | **Merged into ONE identity (A-2)** — keeping a confirmed "structural" row *and* a missed "silence" row for the same bytes would double-book. The per-row `role="status"` placement, the producer-hidden children, the prohibited wrapper names, and the absent completion signal (C-5/M-09) are one defect with one cure locus. Mechanism inversion recorded in the killed register. |

## Adjudicated defect roster

Line anchors at HEAD `64aca0a2`; re-resolve before any cure (D-19).

### A-1 — false geometry at 4 of 6 call sites, worst case a category error · **MAJOR** · merged D-2 / C-4 / L-4 + reader-2 missed #3
**Claim**: the skeleton's fixed anatomy (w-8 h-8 swatch :13 · two text bars :15-16 · h-7 w-14 pill :18, zero props :22-24) promises shapes three panels never render, and one call site previews an entirely different component family.
**Proof (this seat's reads)**: (a) AdminAuditPanel real rows (:59-79) have NO leading swatch and NO trailing action — ~2.75rem of phantom leading chrome plus a phantom pill, and the loading wrapper `grid gap-2` (:36) sits inside a loaded rhythm of `grid gap-3` (:2, rows direct children) — the one panel whose spacing also shifts (reader 1 missed #5, verified; Users :46/:64 gap-3/gap-3 and Names :26/:43 gap-2/gap-2 are internally consistent). (b) AdminUsersPanel rows (:78-131) have no swatch and ONE content line (slug-pill + Badge :91-105) against the skeleton's two. (c) AdminFlaggedPanel rows lead with a five-up `-space-x-1` h-5 w-5 swatch cluster (:52-58) and trail Badge + labelled Dismiss + icon button (:81-102) against one circle and one pill. (d) **The sharpest instance, filed by no axis**: AdminUsersPanel:135-137 renders `AdminListSkeleton ×2` as the loading state for `PaletteCard`s (:140) — while the purpose-built `PaletteCardSkeleton` ships with `count`/`variant` props (:87-94) and is cited by AdminListSkeleton's own header comment (:3). Only AdminNamesPanel (via AdminListItem :44/:94) approximates the grammar, and its trailing edge is two buttons, not one pill.
**Terminal disposition**: **NO-WAVE-OWNER — flagged as a carry-cut gap.** The identity is adjudicated (MT-AU1 §2 L-6 / §3 D-28: "five row-grammar homes → one … the skeleton stops lying"), but MT-AU1 appears in **no** CARRY-CUT-LEDGER row and no X wave: X-W7 modifies every admin panel yet its scope (Checkbox, export, specimen, mutations, dismissal, formatting) omits loading-state anatomy and its file bounds omit AdminListSkeleton.vue; X-W8's only touch is dead-attr deletion under an explicitly **null-DELTA** gate (G-9, W8:309-317) that forbids anatomy changes by construction. Books at the next formation boundary with the MT-AU1 L-6 identity and this roster's census.

### A-2 — the admin loading state is structurally silent to AT; the status boundary sits at the wrong node · **MAJOR** · merged D-1 / C-1 / L-3 (rescoped, mechanism inverted) + C-5 (M-09) + reader missed #2/#3
**Claim**: every constituent of the announcement chain is broken in the silent direction: (1) each skeleton root hard-codes `role="status" aria-label="Loading"` (:10-11) but the producer force-hides every child — the compiled Skeleton sets `"aria-hidden": "true"` unconditionally and strips inbound `role`/`aria-*` from fallthrough (my read of `data-table-BygKg6ZA.js:132-147`: `inheritAttrs:!1`, `filter(([e]) => e !== "role" && !e.startsWith("aria-"))`) — so 2-3 permanently-empty live regions per list; (2) all six loading wrappers are role-less `<div>`s carrying author-written names (`aria-label="Loading audit log"` Audit:36, "Loading users" Users:46, "Loading palettes" Users:135, "Loading flagged palettes" Flagged:16, "Loading pending proposals" Names:26, "Loading approved names" Names:76) — ARIA-prohibited naming on `generic`, per the Δ-11/C-26 calibration (disagreement #4); (3) no completion signal and no `aria-busy` anywhere in demo/ (my census: 0 hits) — the swap is a bare `v-if` chain (M-09 TRUE, non-motion sense). The focus limb is dead: nothing in the skeleton is focusable, so severity rides announcement alone. **Class extension (this seat)**: PaletteCardSkeleton:33-36 — the house's own "announcing side" of the skeleton doctrine — has the identical structure (`role="status" aria-label="Loading palette"` over all-hidden children), so the silence is house-wide, not admin-local.
**Proof of the cure's availability**: AdminTagsPanel:51-56 already puts ONE `role="status" aria-label="Loading tags"` on the container (with the "bare children" correction of disagreement #2).
**Terminal disposition**: **NO-WAVE-OWNER.** Identity extends MT-AU1 §3 Δ-11 (which adjudicated the :46/:135 generic-name sites and cured op-results, not load-completion) — Δ-11's wave was likewise never cut into X. X-W5's shell `role="status"` region (W5:36/:251) is a *route-settlement* node and may never be cited as this cure. Books at the next formation boundary as one row: status boundary → the collection node; children decorative; completion signal or `aria-busy` contract decided once, house-wide.

### A-3 — `surface="glass"` / `variant="breath"` are not producer axes; they serialize into the DOM · **MINOR** · D-3 / L-1 (M-12/M-14 TRUE)
**Claim & proof**: byte-exact, re-verified by this seat: `Skeleton.vue.d.ts` declares `{ class?: HTMLAttributes["class"] }` only; the compiled component filters out only `role`/`aria-*`, so `surface`/`variant` survive to the rendered div — 4 junk attribute pairs per row × up to 3 rows per panel. No functional or visual harm (the scan runs regardless; M-12 notes this file's axis was the corpus's one correct reduced-motion row).
**Terminal disposition**: **X-W8 (X.W8.b · gate G-9)** — already owned and gated RED with this exact byte basis (W8:315-316), under a null-DELTA proof. **Arbiter rider, recorded here because my only write is this file**: G-9's census as written misses `AdminTagsPanel.vue:57-64` — five sibling `surface`/`variant` pairs on the same primitive in the same directory (disagreement #2); and X-W7's G3 (`strictTemplates: true`, W7:41/:309) will make the entire dead-attr class a compile error, double-covering both files. The next conformance pass over W8 should widen G-9's grep by one filename; no re-spec is performed here. Reader 2's adjacent-file corroboration adopted: PaletteCardSkeleton:99's `blockVariant` computed feeds only `:variant=` bindings (:44/:58/:64/:75) on a primitive that accepts none — a dead computed with a live-looking rationale; PaletteCardSkeleton sits in X-W7's file bounds.

### A-4 — the certified ink seam is dead against glass-ui 7.0.0; utils.css still certifies it as live · **MAJOR** · reader-1 missed #1 (adopted; reader-2 superlative killed)
**Claim & proof**: ruled at disagreement #1. `AdminListSkeleton.vue:9` (and AdminTagsPanel:53, PaletteCardSkeleton:34) declare `skeleton-ink-register`, whose three custom properties (utils.css:56-60) are read **nowhere** in the installed producer (my dist-wide grep: 0). The served block background is `var(--muted)`; the sheen is hardcoded. utils.css:37-39 asserts the seam as present fact and :44-55 carries the E1-R2 ΔL certification ("guaranteed a bounded ΔL from the well in BOTH schemes by construction") — a guarantee that reaches none of the three Skeleton-composing consumers. Only ShadowPalette's scoped rules (:104-113) consume `--skeleton-ink` directly and still work. This is a producer-major-bump regression class, and the library-boundary axis — which read the producer d.ts for the *props* — never checked the *token* contract.
**Terminal disposition**: **split.** Producer half — **GLASS-OWNED → the standing glass-ui BH relay** (owner edict, a fond): one letter asking glass-ui to either read `--skeleton-glass-bg`/`--skeleton-shimmer-tint` (the letter-L9 seam utils.css:40-41 already anticipates) or rule the seam dead so consumers stop declaring it; bank on the next glass major. Consumer half (the false utils.css:37-39 sentence + the E1-R2 prose + three dead declarations) — **NO-WAVE-OWNER** at the next formation boundary; no X wave's bounds include utils.css for this purpose. Whether `var(--muted)` still reads as shadow-not-content on the admin ground in both schemes: UNPROVEN-NEEDS-LIVE (residue #1) — the E1-R2 certification may NOT be cited for this component until re-measured.

### A-5 — the shadow has already drifted from the row frame it exists to mirror · **MINOR** · L-2 (rescoped) + reader missed #4
**Claim & proof**: AdminListItem:6-11 carries `min-w-0` on the row root under a five-line comment naming the S.W5-12/F-1 invariant and the ~850px-at-390 bug it fixed; AdminListSkeleton:9 — a grid item in the same structural position — omits it. Content columns diverge: `gap-0.5` + `overflow-hidden` (:17) vs `gap-1.5`, no overflow (:14). Only the header comment binds the two files. Honest bound (both readers, adopted): no live overflow today — every skeleton child is `shrink-0` or `min-w-0`, min-content well under a 390px track — so this is a broken *documented invariant*, not a broken layout. The axis's "copied classes" wording is killed below; the drift it predicted is the fact.
**Terminal disposition**: **NO-WAVE-OWNER, folded with A-1** (one cure: the shared frame; identity MT-AU1 L-6, five homes census per disagreement #3).

### A-6 — INFO residue, recorded so nothing is re-discovered
`data-slot="admin-list-skeleton"` (:8) has zero consumers (my grep: 1 hit = the declaration) — house-legitimate convention (PaletteCardSkeleton declares :33 AND selects :120), consistency not waste. The wrapper block `<div class="grid gap-N" aria-label><AdminListSkeleton v-for …/></div>` repeats six-fold with three counts and two gaps — the duplication is what made A-2's wrapper bug six-fold; the sibling PaletteCardSkeleton models the `count` prop the leaf lacks. No route; dies with A-1/A-2's cure.

## Killed-claims register

1. **D-VERDICT motion limb** ("motion … unspecified", challenge-D:7) — **BARRED BY QUARANTINE, M-11 FALSE.** The motion is producer-specified at the `@media (prefers-reduced-motion:no-preference)` gate (my python slice of glass-ui.css confirms the animation declaration lives wholly inside the gate; `--duration-shimmer: 5s` in scheme-motion.css) and measured ELIMINATED under reduce (M-10, probe2-skeleton.mjs). The file's own :13 concedes it three lines later.
2. **D-CLOSURE reduced-motion cell** (challenge-D:19) — **BARRED, M-13 FALSE**; discharged by M-10/M-12. The ready/empty/error limb of that closure line is a state contract, untouched by the quarantine, and lands in A-2.
3. **C-VERIFICATION reduced-motion obligation** (challenge-C:15) — already discharged TRUE by measurement (M-10); cannot be carried as open.
4. **C-2 / D-5 — "unbounded" narrow-container / large-text behavior** — **REFUTED on both limbs against live bytes** (both readers, re-verified): the text bars carry `max-w-[60%]`/`max-w-[40%]` inside a `flex-1 min-w-0` column with `shrink-0` fixed neighbours (:13-18); and every utility is rem-derived — my grep for a bare `--spacing:` override across demo/styles + glass-ui/dist/styles: 0 hits, so the whole shadow scales 1:1 with root font-size. Residual truth (narrower than filed): the swatch+pill are a real fixed floor.
5. **D-4 error limb** ("no error … escalation; can persist indefinitely") — **REFUTED in the files the axis read**: all five loading sites order loading → `EmptyState variant="error"` + Retry → empty (Audit:42-53, Users:51-62, Flagged:22-33, Names:30-41/:80-91), backed by catch→`loadError` + `finally` clearing `loading` (useAdminAudit.ts:64-70, my read). Surviving residual: a hung socket never rejects — 0 `AbortController|AbortSignal|timeout` hits in demo/palettes/api/ (my census). That residual's cure was specced at MT-AU1 §3 scope (4) (`AbortSignal.timeout` in the transport client) and, like the rest of MT-AU1, was never cut into X (X-W3 carries no deadline row — my grep). **Residual → NO-WAVE-OWNER, MT-AU1 §3(4) identity preserved.** Reachability: UNPROVEN-NEEDS-LIVE (residue #4).
6. **C-3 — fallthrough `aria-hidden` policy hypothetical** — **NON-FINDING** (both readers, concordant; verified): all six call sites are bare `v-for`+`:key`; the imagined parent exists nowhere; "no explicit typed/owned policy" is unfalsifiable prose. INFO-grade at most.
7. **D-1 / C-1 announcement MECHANISM ("duplicate announcements")** — **REFUTED AND INVERTED**: `role="status"` announces content deltas, and every child is producer-hidden (`aria-hidden="true"` unconditional, my read of the compiled Skeleton) — the regions are permanently empty; there is nothing to duplicate. The true defect is total silence = A-2. The structural placement limb survives inside A-2; the filed harm dies here.
8. **L-2 "copied classes" limb** — **FACTUALLY FALSE in the direction that matters**: the strings are not copies (`min-w-0` missing, `gap-0.5` vs `gap-1.5`, `overflow-hidden` missing — my reads); the frozen-duplication risk the axis asserted has already resolved into unremarked drift = A-5, and the census is five homes, not two (disagreement #3).
9. **L-T / D-CLOSURE prescriptive targets** (`AdminListRowFrame` + `ResourceState` + producer motion tokens) — **not truth-apt against bytes**; the motion clause is M-15 UNPROVEN with its premise already shipped; the error-semantics clause is pre-empted (kill #5 — error is already collection-owned everywhere); the shared-frame clause survives only as the open prescription riding A-1's owner. Per the cure-adjudicated-separately law (ColorSpaceSelector addendum §2), none of it inherits evidential weight from the defects.
10. **Reader-2 superlative #1 (the ink recipe "guaranteed … by construction")** — **KILLED BY THIS SEAT** at disagreement #1: the guarantee's delivery path does not exist in the installed producer (0 dist hits). The recipe's *authorship* survives as a corrected superlative below.

## Superlatives — with provenance, corrected where owed

1. **One loading grammar, propagated with provenance.** AdminListSkeleton:2-6 names the wave, the invariant, the sibling it matches, and what it replaced; every call site carries the intent in its own voice (Users:44-45 "never a centered generic spinner", Audit:35, Flagged:15, Tags:49-50). An entire admin surface converted from spinner to row-shadow with the reasoning legible at every site. (Both readers; verified.)
2. **Proportional text bars.** :15-16 pair fixed widths with `max-w-[60%]`/`[40%]` in a `flex-1 min-w-0` column, `shrink-0` neighbours — exactly the property C-2/D-5 claimed absent. (Both readers; verified.)
3. **Text-size resilience by construction.** Every utility rem-derived; no `--spacing` override anywhere (my 0-hit census); the 200%-text cell of challenge-D:19 is satisfied without a media query. (Both readers; verified.)
4. **Motion correctly delegated, and the axis knew it.** The component authors zero motion; the producer's scan is the only `no-preference` gate in the whole of glass-ui.css, eliminated (not clamped) under reduce (M-10/M-12) — plus producer-owned `prefers-reduced-transparency` and `forced-colors` arms on the same primitive (reader 2; the media-query wrappers verified by my python slice). This file's D axis was the one of 46 that cited gating at all (M-12 note).
5. **error ≠ empty is implemented, not asserted, at every call site** — domain-specific message + Retry + `finally` discipline; a dead backend can never costume as a clear ledger. (Both readers; verified — kill #5's evidence.)
6. **The house argues both sides of skeleton a11y on purpose.** ShadowPalette:36-40 (aria-hidden, no role — "a shimmering aria-hidden plate does not lie to AT") vs PaletteCardSkeleton:28-31 ("loading ≠ empty now splits on ANNOUNCEMENT … HERE work IS happening"). The doctrine is stated and defensible; A-2's finding is that the announcing side does not actually announce — the deviation is legible as a defect precisely because the doctrine exists. (Reader 2; both comments verified.)
7. **CORRECTED — the ink recipe's authorship.** `.skeleton-ink-register` (utils.css:56-60) is one `color-mix(in oklab, var(--well-bg), var(--foreground) 15%)` tone-step with the scheme-adaptive direction falling out of the token, four consumers, zero duplication of the mix — genuinely good design **whose delivery is severed at the producer boundary** (A-4). Credit the recipe; do not cite its guarantee.
8. **CORRECTED — AdminTagsPanel's container pattern.** The one-region/decorative-children shape any remediation should copy — minus its own five dead attr pairs (disagreement #2).

## UNPROVEN-NEEDS-LIVE residue (visual-audit inputs)

1. **Ink re-certification (from A-4)**: whether the served `var(--muted)` skeleton block reads as SHADOW, not content, against the admin panel ground in BOTH schemes. The E1-R2 ΔL certification does not currently apply to this component; live measurement required before it is ever cited again.
2. **AT matrix (from A-2)**: exact NVDA/JAWS/VoiceOver behaviour on (a) insertion of a `role="status"` region whose entire content is `aria-hidden`, and (b) name exposure of `aria-label` on role-generic divs (the Δ-11 "engine-dependent, demonstrated not assumed" law). The zero-announceable-content structure is proven statically; the resulting silence is not.
3. **CLS magnitude (from A-1)**: AdminAuditPanel ~2.75rem horizontal inset + gap-2→gap-3 rhythm change; AdminUsersPanel ~0.75rem × 3 rows and the total PaletteCard-shape mismatch at :135. Structural mismatch proven from source; layout-shift score needs live layout.
4. **Hung-fetch stranding (from kill #5's residual)**: no timeout/abort exists anywhere in demo/palettes/api/, so a hanging socket strands the skeleton — requires a network-throttled probe to decide reachability.
5. **AdminNamesPanel geometric near-coincidence** (reader 2): whether the skeleton's 2rem content column matches the `text-small`+`text-mono-small` stack by design or accident — the tokens' computed line-heights were not resolvable by static read.
6. **M-15** (motion-quarantine UNPROVEN) — carried forward unchanged, not adjudicated here, per quarantine law.

## Closing verdict

**SOURCE-RED AFFIRMED, RESCOPED-INVERTED.** The axes' red is real but points the wrong way at every load-bearing joint: the accessibility defect is silence, not duplicate announcements (A-2); the coupling defect is drift already happened, not copies frozen (A-5); the responsiveness accusation was the opposite of the shipped bytes (killed); and the gravest defect on the file's root line — a dead, still-certified producer token seam (A-4) — was filed by no axis and denied by one re-reader's superlative. Terminal ledger: A-3 is already owned and gated (**X-W8 G-9**, with this record's census-extension rider + X-W7 strictTemplates double-cover); A-4's producer half leaves by the **glass-ui BH relay**; A-1, A-2, A-5, A-4's consumer half, and the hung-fetch residual are **NO-WAVE-OWNER** — the MT-AU1 identities (L-6, Δ-11, §3(4)) were adjudicated in V and never cut into X's wave set, and that carry-cut gap is itself a finding this file hands to the next formation boundary. Dispositions here are ADJUDICATED, not VERIFIED; nothing in this record stamps a wave.

— end of apotheosis. The r2 challenge corpus and both re-reader returns remain immutable beside this file per E-1/E-3.
