# kf-EditorHeader — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh adjudicator (incredulity seat, M-23/L-18) · **Date**: 2026-08-08 · **Subject repo**: `/Users/mkbabb/Programming/keyframes.js` at local HEAD `8281638c`, **41 commits behind `origin/master`** (the kf-App ruling-1 three-substrate map applies). **Substrate triage run by this seat**: `EditorHeader.vue` is byte-identical at worktree and `origin/master` (2-line drift vs HEAD only), `shell/index.ts:2` still exports it at the frontier, and `MbabbMenu.vue` keeps the `SharePopover :on-scene-restore` dock duplication at `origin/master:9/:81` — every anchor in this record is **frontier-durable** unless marked substrate-dated. `EditorShell.vue` is tri-state: worktree `mode="persistent" placement="right"` · HEAD `position="right"` · frontier `placement="right"`.
**Corpus**: the three challenge axes at `docs/tranches/V/megatranche/audit/kf-components/EditorHeader/` (challenge-D-design.md · challenge-L-library.md incl. its pass-2 self-corrections · challenge-C-consumption.md), as re-read by two hostile re-readers — **RR-1 (design-and-usage)** and **RR-2 (library-and-code)** — whose returns this record adjudicates.
**Method (L-14/L-18)**: both re-readers were attacked before either was adopted. Every verdict-level disagreement (C-16 kill-vs-confirm, D-7's MAJOR-vs-MINOR, L-B2's BLOCKER-vs-MAJOR, the D-9(a) partial kill, the two conflicting layer-order cells) was re-derived against the tree with this seat's own instruments; all four BLOCKER-adjacent kills (L-m3, C-3's PRM clause, C §0's citation, L-B1(b)) were **third-derived**, not adopted. Both confirmed rosters were then run through the identity guard — and **three of the corpus's/readers' heaviest rows turned out already banked**: the DELETE verdict itself (→ kf-App **KF-APP-41**), RR-1's headline "missed" find (→ kf-App **KF-APP-5**), and the phantom-dependency family (→ census **F-1 ≡ SCH-1**, kf-App ruling 1, **frontier-CURED**) — the same not-checking-the-bank event the kf-ChromeDock and kf-ControlsPaneWrapper records preserve, now on both a confirm and a miss.
**Arbiter's own instruments this session (all static + git + node; ZERO browser tooling — probe parsimony)**: full read of EditorHeader.vue (whole, 108 L — every cited anchor checked); SharePopover.vue (whole, 62 L); useShareState.ts (whole, 95 L); hashSharing.ts:40-70 (`restoreStateFromParam` returns `{restored:boolean}`); EditorShell.vue:14-50 worktree + `git show` at HEAD and `origin/master`; git archaeology both repos — keyframes `3689c1ed` (2026-03-09 **11:50:29** −0400, body "Use the same HeaderRibbon component **as value.js**") vs value.js `d034e7af` (2026-03-09 **11:50:27** −0400, "extract shared HeaderRibbon component…", slots `anchor/items/left`, pin logic) — the SUP-1 decider, TWO SECONDS apart in the other repo's favour; `git log -S "<EditorHeader"` re-run (exactly `05f29a42` + `3689c1ed`); umbrella-barrel importer census (`components/instrument"` + `./instrument` across demo/src → **0 hits**; live consumers all deep-path `instrument/shell`); **installed producer bytes**: dist/header-ribbon.js — `T(e){e.pointerType!=="touch"&&(S.value=!0)}`, `inert: !w.value || void 0`, `"aria-hidden": !w.value`, `onFocusin/onFocusout`, `onKeydown: …["esc"]`, and the decisive **`A(){x.value=!1,S.value=!1,C.value=!0,b.value?.querySelector("button, a, [tabindex]:not([tabindex='-1'])")?.focus()}`** with `b` proven the ANCHOR ref (`ref_key:"anchor"`) and `w = x‖S‖C`; header-ribbon/styles.css (PRM `transition:none` arm, forced-colors CanvasText border, coarse-pointer arm, `__anchor` rule with no min-size, `__band` min-block-size/padding); dark-mode-toggle.css (size arms sm/lg/control/dock — **no `md`**; base 2.25rem) + dark-mode-toggle.js (strips only `class`/`type`; state-aware `aria-label` + `aria-pressed`; `title` lands); **popover-BPBtXakf.js contains Portal** (the C-16 kill's load-bearing teleport clause, verified); btn.css `@utility scale-on-hover` (**0** PRM mentions file-wide); scale-paper.css `--scale-hover: 1.08` vs design-idioms.css:41 `--scale-hover: 1.08` (unlayered `:root`); a11y-overrides.css **whole head** (the universal `*:not([data-allow-motion])` PRM override, `transition-property: opacity,color,background-color,border-color,box-shadow !important`) + the unlayered import chain (styles/index.css `@import "./accessibility.css"` with NO `layer()`, beside `components.css layer(components)`) + bundle presence (`*:not([data-allow-motion])` = 1 hit in index-CL_QYCiO.css); **shipped-bundle probes**: `header-items-wrapper` = **0** / `header-ribbon__actions` = **1** in index-CL_QYCiO.css; `--header-items-max-w:500px` **ships** (1 hit, zero consumers); `@layer <list>,` ordering statement = **0** — six block openings only, with my own offsets `@layer components{`@21885 < `@layer utilities{`@182187 (the load-bearing order) and `.scale-on-hover{`@194614 < `.transition-all{`@231036 < `.duration-fast{`@232395 (same layer, document order decides); layout.css:9-15 (`--header-items-max-w: 500px` + its EditorHeader comment) /:105-120 (`--dock-top-anchor` verbatim) /:149-154 (the no-layout-on-`.z-dock` prose); style.css:28-38 (`--z-bar: 30` headers · `--z-dock: 40` bottom band); design-idioms.css:110-122 (`@utility icon-lg` = size-6 = 24px); button/styles.css `[data-icon-only]{inline-size:var(--button-size)…}` + sizing.css `--control-h-md: max(calc(2.5rem*var(--ui-scale)), var(--control-floor))`; `--ease-decelerate` demo census = **1 line** (EditorHeader.vue:98); **WCAG recompute** (node, own code): fg hsl(24 10% 10%) over hsl(40 30% 98%), sRGB-alpha composite → **16.825 / 9.009 / 3.322** at α 1.0/0.8/0.5 (agrees with the corpus to 3 s.f. — third derivation); the real Glass-7 letter read at `value.js/docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19` + `find`/`ls` proof the cited `coordination/…-consumer-updates.md` path **does not exist**; novelty greps over the corpus (`transition-all` 0 · `decodeStateFromHash` 0 · `route.name` 0 · `isValidState` 1 · `restored` only inside the quoted toast string); census lane-frontend.md:194 (the roster row verbatim) + shadow census `^### S-` count = **8** (no S-9) + §6.3/§6.5 anchors; **registry cross-reads**: kf-App.md (ruling 1 three-substrate map, ruling 5/M-A, KF-APP-5, **KF-APP-41**), kf-ChromeDock.md (D-5 icon-token family, L/B-1 fold precedent, CENSUS-2026-08-03 §(a) taxonomy), kf-CopyButton.md (KF-CB-7/-14/-26 — the SharePopover house-reference tension), kf-ControlsPaneWrapper.md.
Status verbs per M-25: dispositions are **ADJUDICATED, not VERIFIED** — the X·KF specs are still forming and consume this roster as agglomeration input. Wave targets follow the census lane taxonomy (CENSUS-2026-08-03 §(a)): **KF.W0** substrate settle · **KF.W4** gates/hygiene · **KF.W6** glass suffusion · **KF.W9** Safari/visual audit (owns ALL SS-13 residue) · **KF.W10** fold discharge; folds to banked rows carry the bank's own routing. Everything the census does not own is **NO-WAVE-OWNER** for SS-1/SS-2 spec authoring.

## Scoreboard

**37 rows stand: 4 FOLDS to banked identities (two blocker-weight at their banks) · 16 rows booked live (6 MAJOR · 9 MINOR · 1 INFO — the rows that SURVIVE the deletion) · 17 fork-only rows adjudicated and discharged-by-delete (7 MAJOR-as-if-live · 8 MINOR · 2 INFO). 9 corpus claims KILLED with refutations ratified — every kill third-derived or re-derived by this seat, including the corpus's own most confident falsifier declaration (L-m3's "none exists" hunted down to the exact file it missed). 10 net-new finds adjudicated in (5 per reader), one demoted against a banked house-reference row. Identity guards caught THREE already-banked heavyweights the readers filed as fresh.**

The center of mass: **the file is dead, was ordered deleted twice before (U lane-18 → U.B5), and the delete is ALREADY BANKED as kf-App KF-APP-41 — this corpus's real product is (a) the enlargement of that delete (the umbrella barrel is dead too), (b) six defects that ship TODAY in the live EditorShell/SharePopover copies of its default slot, and (c) the corrected producer truth (HeaderRibbon 7.0.0 still carries the whole collapsible machinery, and its Esc cannot collapse).**

## Ruled disagreements (the seat's actual work)

| question | RR-1 (design-and-usage) | RR-2 (library-and-code) | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|
| **C-16 / L-m9 / D-17b** (empty `#anchor` default contract) | KILLED as "bricked / no keyboard path" (self-contradicted by C-2); graded MINOR per L-m9 | CONFIRMED MAJOR ("C's BLOCKER grade is the better one") | **CONFIRMED-RESCOPED MAJOR — both halves adopted, C's headline clause dies** | The zero-hit-area reading is source-decided and confirmed (`:36` empty slot, `:32-33` bare `shrink-0`, `:101-107` `max-width:0`, `:3` `pointer-events-none`, no min-size anywhere in `:87-107`). But "no keyboard path" is FALSE by the fork's own missing `inert` (L-M2): the collapsed SharePopover trigger and DarkModeToggle stay in the tab order, and PopoverContent **portals** (my probe: `Portal` present in popover-BPBtXakf.js) so Enter renders a real popover out of the zero-width box. The true defect is sharper than either grade-war: the REVEAL is unreachable while the CONTENTS are operable-but-invisible — focus lands on a zero-width, opacity-0 control (a WCAG 2.4.7-class failure in its own right). MAJOR on the published-barrel contract; BLOCKER dies with the mechanism correction. |
| **L-B2 / C-13 / C-18** (phantom `@mkbabb/glass-ui`) | CONFIRMED BLOCKER | CONFIRMED MAJOR (noting the axes' own disagreement) | **NEITHER — FOLD → census F-1 ≡ SCH-1 (kf-App ruling 1, KF.W0), frontier-CURED** | My substrate probe: worktree package.json has no glass-ui; HEAD `8281638c` declares `"@mkbabb/glass-ui": "6.0.0"` (optionalDependencies); **`origin/master:package.json:77` = `7.0.0` exact devDependency** — matching the installed producer every cell was measured against. Both readers confirmed a worktree-only reading as a fresh blocker without the substrate triage the bank already ran. Severity lives at the bank. Riders carried: C-18's two unmet peers (no `pencil-boil`, no `embla-carousel-vue` in node_modules — my `ls`-class check via the readers' reproduced probes) and C-14's self-alias (already census-owned, lane-frontend:569). |
| **D-7 / L-m8 / C-8** (z-dock on a header) | MAJOR | MINOR | **MINOR — RR-2 wins** | Facts unanimous and re-verified (style.css:31-32 assigns headers `--z-bar: 30`; `:3` wears `z-dock` = 40; layout.css:149-154 independently bans keying on the generic utility). But the header and the bottom dock band never overlap spatially, the successor positions itself (`--z-header: 35` exists unused), and the cure is the deletion — a contract violation with zero user impact, in a file that ships zero bytes. |
| **D-9(a)** ("~270 ms with no visible motion at all") | PARTIALLY KILLED (margin-right co-transitions) | CONFIRMED MINOR with the dead-zone framing intact | **RR-1's correction ADOPTED; row survives RESCOPED MINOR** | Source-decided from `:95-98`: `margin-right` rides the SAME 450 ms/`--ease-standard` channel from 0.75rem→0 with no dead zone — a 12 px continuous drift from t=0. The width channel's dead zone is real (500 px cap vs ~64-72 px content; min(content, cap)). Correct reading: a **two-speed collapse** (12 px drift ~280 ms, then a 64 px snap ~170 ms) — a sharper defect than the one filed. RR-1's D-11 precision (~112 ms spill, opacity ~0.7) adopted with it. |
| **D SUP-1's falsifier discharge** ("§1.0 proves the influence ran demo → design system") | KILLED (NOT PROVEN; tree points the other way) | not addressed | **KILL RATIFIED — third-derived** | My own git reads, both repos: value.js `d034e7af` (11:50:**27**) creates value.js's demo HeaderRibbon by extraction from value.js's own App.vue; keyframes `3689c1ed` (11:50:**29**) says "Use the same HeaderRibbon component **as value.js**". The commit adduced as the discharge proves the copy ran value.js → keyframes at that moment. EditorHeader (2026-02-27) may still be the ur-ancestor of the shared SHAPE — that claim reverts to **OPEN**; SUP-1's design-merit half stands untouched. |
| **The layer-order evidence cell** (C-10's "@layer base, components, demo, …" statement) | Gave offsets: properties@9251 < theme < base < components@21885 < utilities@182187 | Killed C-10's citation but substituted "first appearance: base, components, demo-typography, properties, theme, utilities" | **RR-2's kill RATIFIED; RR-2's replacement sequence ALSO corrected** | My grep: no `@layer a, b, c;` statement anywhere in index-CL_QYCiO.css (0 hits); six block openings, one of them `demo-typography`. RR-2's "first-appearance" list is alphabetical — the artifact of a `sort -u`, not the file order. The true offsets match RR-1 (`components{`@21885, `utilities{`@182187). The load-bearing fact — utilities declared after components, so utilities wins — survives on EVERYONE's numbers; the correct citable source ordering is `tailwindcss/index.css:1`. C-10/D-11/D-20/D-23 re-anchor there. |
| **Identity events (both readers)** | Filed D-1 family as fresh BLOCKER; presented the live no-`#anchor` ribbon as a NET-NEW missed MAJOR | Filed D-1 family as fresh BLOCKER; filed F-1 as fresh | **Bank wins, three times** | KF-APP-41 ("EditorHeader: 0 consumers, still barrel-exported… NO-WAVE-OWNER (delete)") banked the delete; KF-APP-5 banked the live ribbon lockout WHOLE (inert+aria-hidden+zero-width at rest, no `#anchor` at any substrate, KF.W6 + BH relay + KF.W9); kf-App ruling 1 banked F-1's substrate cure. Folded by reference below with the readers' genuine riders preserved. |

## Adjudicated defect roster

Line anchors are worktree ≡ `origin/master` for EditorHeader.vue (verified this session); re-resolve all EditorShell/MbabbMenu anchors at `origin/master` before any cure (D-19 law + kf-App ruling 1).

### FOLDS to banked identities (identity guard; zero re-booking)

- **F1 — D-1 / L-B1 / C-1 (the DELETE) → kf-App KF-APP-41 (NO-WAVE-OWNER: delete; blocker-weight at the bank).** Re-verified whole by this seat: sole grep hit = shell/index.ts:2; barrel consumers take other symbols (App.vue:138, MbabbMenu.vue:81); bundle carries `header-ribbon__actions` and not `header-items-wrapper`; `git log -S` = exactly `05f29a42`/`3689c1ed`; the frontier still exports it. **Riders added to the bank**: (a) the deletion surface ENLARGES — see the umbrella-barrel row below; (b) collateral: layout.css:15 `--header-items-max-w` (ships, zero consumers — my bundle grep) and the `:98` sole `--ease-decelerate` consumer die with it; (c) the census amendment — lane-frontend.md:194 rosters the file live while the shadow census (S-1..S-8, my `^### S-` count = 8) omits it: file **S-9 · EditorHeader → HeaderRibbon — DEAD, replace-by-deletion** (all three axes converged; both halves were already in the lane, only the join missing); (d) provenance primary record + the U.B5 NO-DEFERRAL ruling (fourth writing of the verdict; the correct wave action is execution, not re-adjudication).
- **F2 — L-B2 / C-13 / C-18 / C-14 (phantom dep family) → census F-1 ≡ SCH-1 (KF.W0), per kf-App ruling 1.** Ruled above. Frontier-cured; unmet-peers + self-alias riders carried.
- **F3 — RR-1 "missed" #1 (the LIVE HeaderRibbon mounts with NO `#anchor`; all three reveal paths closed on touch) → kf-App KF-APP-5 (KF.W6 consumer half + glass-ui BH relay producer half + KF.W9 SS-13).** Banked before this corpus was re-read. **Riders RR-1 genuinely adds to the bank, adopted**: (a) the **touch consequence census** — the F.W15.S3 shortcuts trigger exists at exactly two sites (EditorShell.vue:32-41 + `registerShortcut("?")` at :190), so on a touch device the shortcuts modal is WHOLLY unreachable (the bank's ruling noted only the keyboard `?` survivor); Share/dark-mode survive solely via the MbabbMenu dock duplication (frontier-verified at :9/:81); (b) the computed rest-band arithmetic (2×`--panel-padding` 0.375rem × `--size-icon-btn` 2.5rem → ~12×40 px) as the SS-13 input — kf-App ruling 5's caveat stands: the pixel read is NEEDS-LIVE, the band (not anchor) rule is what computes.
- **F4 — D-19 (`icon-lg` 24px vs producer `--icon-lg` 20px) → kf-ChromeDock D-5 (KF.W6 token-alignment family, with KF-APP-25/-26).** Same identity (demo `icon-*` shadows producer `--icon-*` by +1 rung under identical stems); reached here via SharePopover.vue:11.

### BOOKED here — rows that SURVIVE the deletion (live in the shipping tree)

- **EH-1 · C §0 (substance) — MAJOR · the Glass-7 premise on record is contradicted by the installed artifact: HeaderRibbon 7.0.0 still ships the `anchor` slot (`{pinned}` payload, HeaderRibbon.vue.d.ts), the ENTIRE collapsible machinery (my dist reads: x/S/C flags, inert/aria-hidden, Esc), and the `--header-ribbon-actions-width` knob — the archived letter claims all three were deleted.** The real letter is `value.js/docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19` (this seat's read); only `mode` and `anchorLabel` are actually gone. Under the letter the target file would be irreplaceable; under the tree it is deletable — the row inverts the disposition and was load-bearing for the whole corpus. **Citation kill recorded separately** (the corpus cited a nonexistent path — see KILLED). Disposition: **KF.W0** (re-read XR-4 / IN-GLASS-1 / DISPOSITIONS.md:21 against the ARCHIVE file + `dist/header-ribbon.js` before any Glass-7 consume slice is scoped) + rider to the standing glass BH relay: producer docs/letters must be re-derived against shipped bytes (the kf-ChromeDock doc-drift ledger's pattern, third instance).
- **EH-2 · RR-2 missed #1 — MAJOR · a restore that applies NOTHING still toasts "State restored!": `useShareState.ts:75` never reads `result.restored`.** Third-derived whole by this seat: hashSharing.ts:51-58 returns `{restored:false}` (zero state applied) whenever `isValidState` fails; the local guard at useShareState.ts:69-73 tests only `decodeStateFromHash`, so a decodes-but-invalid payload passes it, applies nothing, and `:83-86` fires the success toast unconditionally. Live at BOTH mount sites (EditorShell.vue:20, MbabbMenu.vue:81) at every substrate. Novel (zero corpus coverage — my greps: `decodeStateFromHash` 0, `isValidState` 1 non-load-bearing hit). Cure at the seam: gate the toast on `result.restored`. Disposition: **NO-WAVE-OWNER**.
- **EH-3 · L-M6 / C-4 (live half) — MAJOR · the ribbon-site `<SharePopover />` is bare — no `onSceneRestore` — so a shared-URL scene switch silently no-ops from the header while the dock site is wired.** Verified: EditorShell.vue:20 bare; MbabbMenu.vue:9 `:on-scene-restore` (frontier-intact). The fork's duplicated-and-drifted default slot (EditorHeader:24-27 carries `hover:opacity-50` the shell dropped; the shell gained the shortcuts trigger the fork never did) folds here as evidence for F1's delete. Compounds EH-2 (the no-op restore then toasts success). Disposition: **NO-WAVE-OWNER** (wire the prop or centralize; anti-rename: the forthcoming kf-EditorShell corpus folds HERE, never re-books).
- **EH-4 · D-10 — MAJOR · `title="Toggle dark mode"` collides with DarkModeToggle's own state-aware accessible name, live at EditorShell.vue:45.** My dist bytes: only `class`/`type` are stripped; `title` lands on the same `<button>` as the conditional `aria-label` ("Switch to light/dark mode") + `aria-pressed`. Accessible name ≠ visible tooltip (WCAG 2.5.3 class). Disposition: **KF.W6** (drop the `title`; the producer already names itself).
- **EH-5 · D-20 — MAJOR · `aspect-square w-8` on DarkModeToggle is inert on the aspect and yields a 32×36 box, live at EditorShell.vue:46.** Cascade third-derived: dark-mode-toggle.css `@layer components` base `2.25rem` with size arms for sm/lg/control/dock and **no `md`** (the default); `w-8` in utilities (declared later — my offsets 21885 < 182187) wins WIDTH only; both dimensions definite ⇒ `aspect-ratio:1` no-ops. Disposition: **KF.W6**; rendered box → **KF.W9** SS-13.
- **EH-6 · RR-2 missed #2 — MAJOR · `demo/components/instrument/index.ts` is itself a wholly dead barrel — the "second mask" all three axes cited and none audited.** My exhaustive importer census: zero hits for the umbrella (`components/instrument"`, `./instrument`); every live consumer takes the deep path (`App.vue:138`, `MbabbMenu.vue:81` via `instrument/shell`; EditorShell via `instrument/utils/…`). A 27-line documented umbrella whose lazy-barrel doctrine comment defends a file nothing imports — dead code masking dead code, strictly enlarging KF-APP-41's surface beyond the 110 lines the corpus computed. Disposition: **rides KF-APP-41's delete** (NO-WAVE-OWNER); the delete wave must decide umbrella-vs-line, not just the line.
- **EH-7 · RR-1 missed #2 — MINOR · the prescribed cure is weaker than the corpus asserts: HeaderRibbon's Esc CANNOT collapse the ribbon, and with the live empty anchor it cannot move focus either.** Third-derived bytes: `A()` sets `C.value = !0` (focus flag) so `w = x‖S‖C` stays true — Esc only unpins; `b` is the ANCHOR ref (`ref_key:"anchor"`), empty in the live shell, so the `querySelector(…)?.focus()` no-ops. Falsifies the identical cell on all three axes (D §1.2 "Esc → collapse + focus anchor", L-M3/M5, C-2) — load-bearing because Esc was cited as compensation for the primitive's missing roving focus. Disposition: **glass-ui BH relay** (rides KF-APP-5's existing relay: Esc semantics + empty-anchor focus target).
- **EH-8 · RR-1 missed #4 — MINOR · the live ribbon's three-control cluster is incoherent three ways at once: boxes 24×24 / 32×40 / 32×36 and glyphs 24 / 16 / 20 on one centered band, with two inert `aspect-square`s.** Mechanics verified: SharePopover trigger `p-0` + `icon-lg` (24px, design-idioms.css:114-119); shortcuts Button `icon-only` (`inline-size: var(--button-size)` = `--control-h-md` = 40px) defeated on the inline axis only by `w-8`; DarkModeToggle per EH-5. D-20 caught one third; the `icon-only`-invoked-then-broken instance is the unflagged one. Disposition: **KF.W6** (one sizing decision with EH-5); rendered ladder → **KF.W9**.
- **EH-9 · RR-1 missed #5 — MINOR · `transition-all duration-fast` on SharePopover's trigger (:7) clobbers `scale-on-hover`'s design-system spring.** Third-derived offsets in the shipped sheet: `.scale-on-hover{`@194614 < `.transition-all{`@231036 < `.duration-fast{`@232395, all inside `@layer utilities` — document order decides; the spring curve/duration are replaced by default-ease @ 0.2s and every property becomes transitionable. Novel (my grep: `transition-all` = 0 across all three challenge files). The `sharePopoverOpen ? 'opacity-100' : 'hover:opacity-50'` recede-affordance (D-23's surviving live half) books with it. Disposition: **KF.W6** (one SharePopover motion batch).
- **EH-10 · RR-1 missed #3 — MINOR · design-idioms.css:41 redefines glass-ui's own `--scale-hover` in an unlayered `:root`, permanently shadowing the producer token for all `scale-on-hover` sites.** Both read by this seat: producer scale-paper.css `--scale-hover: 1.08`; demo `--scale-hover: 1.08` with a comment admitting the mirror. **Adjudication note the reader under-stated: the values are IDENTICAL today, so this is a zero-delta shadow whose entire cost is the future DS-retune no-op** — a literal instance of lane-frontend §6.3's abstract hazard. Unbanked (checked against kf-CopyButton/kf-CubeScene). Disposition: **KF.W6** (token-alignment family, beside F4).
- **EH-11 · RR-1 missed #6 — MINOR (DEMOTED against the bank) · SharePopover's two popover actions (:22-39) take their accessible name from `title` alone and hand-roll 32×32 geometry past the DS's `icon-only` API.** Facts verified (no `aria-label`, no `icon-only`, `h-8 w-8 p-0`). **Demotion grounds**: unlike EH-4 there is NO competing aria-label — `title` supplies a consistent (if fragile) name, no 2.5.3 mismatch; and the bank cuts the other way: **KF-CB-7 praises this exact site as the house reference** ("glass Button 32px target, icon-md glyph, visible title — verified verbatim") and KF-CB-26 prescribes `:title` as the house interim. A row that convicts the pattern the bank canonizes must be adjudicated as a HOUSE-PATTERN question, not a local defect. Disposition: **NO-WAVE-OWNER** (SS-1/SS-2 decide the icon-button naming idiom once, harmonizing KF-CB-7/-26 with this row; KF-CB-14's 2.5.8 rescope applies to the 32px-vs-44px half).
- **EH-12 · C-5 — MINOR · SharePopover reads its function prop once at setup (:60-61 → useShareState.ts:12) — a rebound `onSceneRestore` is ignored.** Verified; live impact nil (stable `runSceneSwitch`). RR-1's demotion from C's MAJOR adopted. **NO-WAVE-OWNER** (one repair with EH-2/EH-13).
- **EH-13 · RR-2 missed #3 — MINOR · the same payload is decoded twice and the two validity gates differ** (useShareState.ts:69 decode-only guard; hashSharing.ts:56 decode+`isValidState`) — the mechanism behind EH-2. Verified. **NO-WAVE-OWNER** (decode once, branch on `result.restored`).
- **EH-14 · RR-2 missed #4 — MINOR · `route.name as string` ×2 (useShareState.ts:19/:25) launders `string | symbol | undefined`;** on an unnamed route both `getAllState` and `router.resolve` receive values their signatures forbid. Verified. **NO-WAVE-OWNER** (type hygiene; KF-APP-4's gate family would not catch assertions — the cure is removal, not tooling).
- **EH-15 · RR-2 missed #5 — MINOR · the barrel-published component chain carries an undeclared vue-router mount-context requirement whose failure defers from mount to first interaction** (`useRouter()/useRoute()` bare injects at :13-14; first Share click throws on `.name`). Verified; survives the deletion via SharePopover's own barrel export. **NO-WAVE-OWNER**.
- **EH-16 · D-24 — INFO · producer relay: `scale-on-hover` has no PRM `scale: 1` reset while sibling `glass-capsule-hover` ships one; under PRM the global override strips `scale` from transition-property so the 1.08 lift snaps untransitioned.** btn.css re-read: 0 PRM mentions. Unbanked (KF-CB-27 is the adjacent-but-different magnitude row). Disposition: **glass-ui BH relay**.

### Fork-only rows — adjudicated, DISCHARGED by KF-APP-41's delete (as-if-live grades; evidence FOR the deletion, per D §0.1's severity convention, which this seat ratifies as the correct method)

- **D-2 / L-M2 / C-2 — MAJOR** · collapsed items keep tab order + a11y tree (`:101-107` is the whole rule set; no inert/visibility anywhere); successor ships `inert` + `aria-hidden`. Confirmed from source.
- **D-4 / L-M3 / C-17 + D-5a — MAJOR** · `mouseenter`-only reveal, no touch guard, no focusin, no keydown (`:13` sole expand edge; `:42-85` contains none). Confirmed; the "Esc" cell in every axis's successor-contrast table is corrected by EH-7.
- **L-M1 — MAJOR** · edge-latch state machine: unpin (`:76-78`) starts an unconditional 2 s collapse with the pointer resting on the ribbon; no `isHovered` flag exists (`:48-51`); successor holds hover as STATE and has no timer. Source half confirmed by this seat; the stranded-pointer outcome → NEEDS-LIVE (moot on delete). L's unique find — the one row neither D nor C reached.
- **C-16-as-RULED — MAJOR** · the empty-anchor default contract (see the ruled-disagreements table for the full recomposition).
- **D-6 — MAJOR** · zero paint properties in the entire owned surface (`:87-107` + template classes); icons float on the scene; successor wraps in glass `Surface` + forced-colors arm (both verified in styles.css).
- **D-8 — MAJOR** · bypasses `--dock-top-anchor` (layout.css:113-118 verbatim re-read, safe-area-inset logic included) for raw `top-0 px-4 py-2` — under a notch the strip lands beneath the status bar.
- **L-M4 / D-3 (consumer half) — MAJOR** · the pin is a bare `<div @click>` (`:32-37`), no role/tabindex/aria-pressed/keydown. The producer-parity half (its anchor is equally a plain div, `onClick: k`) is ALREADY INSIDE KF-APP-5's relay — folded, not re-booked.
- **D-7 / L-m8 / C-8 — MINOR (ruled)** · z-rung violation; see table.
- **D-9 / L-m5 — MINOR (rescoped)** · two-speed collapse; see table. The C-tranche ios-animation prescription landed upstream (spring) and never here — the same fact as F1 on the motion axis.
- **D-11 / L-m1 / L-m2 / C-10 — MINOR** · the `:18` `overflow-hidden` utility is inert against the unlayered scoped `overflow: visible` (`:99`); expand never clips. **Rider (this seat's own)**: L-m2's rationale that `overflow: visible` is deliberate "so PopoverContent can escape" is WEAKENED — PopoverContent portals (my probe), so the visible/hidden flip serves nothing at all; and the row's cascade citation re-anchors per the layer-order ruling.
- **D-16 / L-m7 / C-6 — MINOR** · physical axes (`max-width`/`margin-right`/`left-0 right-0`) vs the successor's fully-logical properties; no `dir` machinery in the demo — portability only.
- **D-21 / C-9 — MINOR** · the STY-5 comment (`:89-91`) cites design-idioms.css for a token that lives at layout.css:15 (both files re-read; the reciprocal split is documented at layout.css:9-11).
- **D-22 — MINOR** · hover-out bound to the root (`:4`), hover-in to the inner group (`:13`): a ribbon→populated-`#left` transit never arms the timer. Confirmed from source; occupied-slot behaviour → NEEDS-LIVE (moot).
- **D-17 / L-m4 / C-7 — MINOR** · zero `define*` macros (`:42-85`); three slots + one slot-prop exist only as template text; `absolute` with no positioned-ancestor contract; the `:31` "always visible" comment is false in the default render.
- **D-13 / D-15 / C-12 / L-i3 — MINOR** · the token cluster: 500 px renamed-not-derived; `2000` the file's one untokenised literal (4.4× `--duration-slow`); `var(--header-items-max-w)` fallback-less vs the successor's `, 30rem`; `--ease-decelerate` single-consumer (my grep: 1 line demo-wide).
- **D-14 — INFO** · the corrected contrast table is arithmetically exact — **third-derived by this seat: 16.825 / 9.009 / 3.322** vs the doc's 16.82/9.01/3.32 and RR-1's 16.824/9.008/3.322; the `[CORRECTED]` flag against the prior pass is itself correct (rest opacity IS 0.8).
- **L-i2 — INFO (substrate-dated + NEW rider)** · `mode="persistent"` is a stray attr — **worktree-only**; frontier is a clean `placement="right"`. **This seat's rider: HEAD carries `position="right"` — ALSO not a 7.0.0 prop (`placement` is)** — so the phantom-attr class exists at HEAD in a different spelling; the reconciliation belongs to KF.W0's substrate settle, not to a component cure.

## Killed-claims register (all refutations re-derived or third-derived by this seat)

1. **L-m3 ("no global PRM block reaches it… None exists") — FALSE DEFECT, KILL RATIFIED.** The declared-unhuntable falsifier exists at `glass-ui/dist/styles/utilities/a11y-overrides.css` byte 0, unlayered (`@import "./accessibility.css"` with no `layer()` — my chain read), `!important`, property-list exactly the partition that turns the accordion into an instant collapse + 100 ms fade — the ideal PRM disclosure. Present in the shipped bundle (my grep: 1 hit). L's pass-2 named two candidate files and never opened the third, which contained the rule. D's SUP-2 is the arbiter and was right to refuse the row.
2. **C-3's PRM clause ("There is no global escape hatch") — KILLED by the same file.** Its greps were individually accurate and the search space incomplete. The forced-colors and coarse-pointer halves survive as absence facts, already carried by D-6/D-5 — C-3 dies as an independent row.
3. **L-B1 sub-claim (b) ("side-effectful scoped CSS… not free") — KILLED.** The fork is fully tree-shaken (my bundle probe: `header-items-wrapper` 0 hits, `header-ribbon__actions` 1). The BLOCKER stands on the other two legs at its bank (F1).
4. **L-B2's "the CSS edge fails silently" framing — KILLED.** demo/styles/style.css:3 `@import "@mkbabb/glass-ui/styles"` is the same missing package and an equally loud build error; no reachable state has JS resolving while CSS silently doesn't. The IACVT mechanics are correct in the abstract with no trigger under F-1.
5. **D SUP-1's provenance discharge — KILLED (third-derived).** See the ruled table: `d034e7af` (value.js, 11:50:27) vs `3689c1ed` (keyframes, 11:50:29, "as value.js"). Ur-ancestry OPEN; design merit intact.
6. **D-9(a)'s "no visible motion at all" — PARTIALLY KILLED.** `margin-right` co-transitions with no dead zone (source-decided). Row survives rescoped (two-speed collapse).
7. **C-16's headline clause ("bricked… no keyboard path") — KILLED as scoped.** The collapsed controls are focusable and operable (no inert; portalled PopoverContent — my probe); the REVEAL is what's unreachable. Fact + MAJOR grade survive in the ruled recomposition.
8. **C §0's citation — KILLED (the substance is EH-1 and survives).** The cited `coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md` does not exist (my `ls`/`find`); the real letter is at `archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md`, archived 2026-07-17 — before the challenge was authored — and the block-quote is a paraphrase presented as quotation, in the one section whose force is documentary. All downstream re-reads scope against the archive file.
9. **C-10's layer-statement citation — KILLED; RR-2's replacement sequence also corrected** (see the ruled table). The cascade conclusion survives on first-appearance order + `tailwindcss/index.css:1`.

## Superlatives (provenance preserved; L-18 runs both ways)

- **D SUP-2 — the corpus's best row, ratified on this seat's own bytes**: identified "no local PRM block ⇒ gap" as a tempting false defect, hunted the delegation, and found the unlayered `!important` override that kills the same claim in BOTH other axes — one of which claimed to have hunted exactly this. Also reconciles census §6.5's two "unverified statically" deferrals. This is what an adversarial corpus is for.
- **D §0.1 — the honesty method**: shipped-bundle probe FIRST, two tempting claims pre-killed, "a false defect is worse than a missed one", severity split shipped-vs-as-if-rendered. This apotheosis adopts its convention wholesale.
- **THE COMPONENT — the `useTimeoutFn` discipline (`:53-62`)**: all three axes + both readers + this seat's reading of the mechanism concur — `{immediate:false}` is the required non-default, `start()` clears before re-arming, scope-disposal owns the leak class; a named prior finding (D-tranche F8) verifiably discharged, with the rationale in the comment.
- **THE COMPONENT — the pointer-events island idiom (`:3/:7/:12`)**, independently matched by the producer; and **L N-1's anti-defect note** (mouseleave on a `pointer-events:none` root is CORRECT), booked so no future reader "fixes" it into a regression.
- **C S-A — proving a negative**: the full import-closure walk that DECLINED an R1 parser-crash claim and enumerated the chunks through which glass-ui does reach value.js to justify the refusal.
- **C S-E — the contrarian find, upgraded by RR-1 and ratified**: the fork's `left` slot OUTSIDE the collapsible wrapper is genuinely better than the primitive (whose published slots are `anchor`+`items` only — d.ts verified), and the migration folded `#header-left` INSIDE the region that goes inert (EditorShell.vue:18, verified at all substrates). Rides KF-APP-5's relay as the leading-slot feature request.
- **L pass-2 — the public self-repair**: S-2's z-dock merit re-filed as L-m8 with the wrong row retained for the audit trail ("an audit that quietly rewrites its own superlatives is worth less than one that shows the repair").
- **L-S4's frame, adopted as this record's epigraph**: "good dead code is more dangerous than bad dead code, because it invites reuse."

## UNPROVEN-NEEDS-LIVE residue (SS-13 inputs → KF.W9; ownership per KF-APP-5 where noted)

1. The live ribbon's rest-state pixel box (~12×40 computed) + top-right discoverability photo — rides KF-APP-5's KF.W9 routing.
2. Whether the live ribbon expands AT ALL on real touch hardware (source closure of all three paths proven; event synthesis is not) — KF-APP-5.
3. D-20/EH-5/EH-8's rendered boxes and the three-height ladder under real font/zoom — live shell, real input.
4. C-15's root-barrel vs granular-subpath shipped-bytes delta (needs a comparative gh-pages build; static closure is an upper bound only).
5. MOOT-ON-DELETE (recorded so nothing silently drops if the delete slips again): L-M1's stranded-pointer dead-end · D-11's expand-spill severity · D-6/D-14's over-scene backdrop ratio · D-22's occupied-`#left` hover chain · D-9's dead-zone trace.

## DISSENT — preserved, not ruled away

1. **The bank-blindness event, both readers**: RR-1 and RR-2 jointly re-confirmed the DELETE (KF-APP-41), the phantom dep (census F-1, frontier-cured), and — from the MISS direction — RR-1 filed the live no-anchor lockout as net-new when KF-APP-5 had banked it whole. Third occurrence of the class (kf-ChromeDock, kf-ControlsPaneWrapper prior). The M-25 agglomeration seat should treat "checked the adjudicated registry" as a mandatory reader instrument.
2. **RR-2's alphabetical layer-order cell** is preserved as a method lesson: a `sort -u`'d grep presented as file order — the same signature-before-output failure class the ColorSpaceSelector DISSENT records.
3. **Severity dissent on C-16** (RR-1 MINOR-per-L-m9 vs RR-2 MAJOR-per-C): ruled MAJOR, but RR-1's calibration argument ("any plausible consumer would pass `#anchor`") is preserved — if SS-1/SS-2 adopt a slots-required typing idiom (`defineSlots` + required anchor), the row's weight drops to the type row it always was.
4. **EH-11's demotion** is this seat's ruling against a reader and FOR the bank (KF-CB-7/-26); if the SS-1/SS-2 naming-idiom decision goes the other way (aria-label mandatory on icon-only), the row revives at MAJOR with the bank rows corrected instead.

## Verdict

**DELETE — ADJUDICATED, folded to kf-App KF-APP-41 and ENLARGED (the umbrella barrel dies too; the census gains S-9; the token and the sole `--ease-decelerate` consumer go with it). The fork's 17 as-if-live defects are evidence for the delete, not work; the corpus's six live escapes (EH-1..EH-5, EH-9's SharePopover batch) and the state-seam family (EH-2/12/13/14/15) are the real cures, routed KF.W0 / KF.W6 / NO-WAVE-OWNER with all SS-13 residue at KF.W9; the producer truths (Esc-cannot-collapse, empty-anchor focus void, scale-on-hover PRM gap, the letter-vs-artifact contradiction) ride the standing glass-ui BH relay. Fourth writing of the verdict; first with the bank checked. Nothing here stamps a wave — X·KF executes on the owner's begin-word.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/shell/EditorHeader.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **EditorHeader.vue=+1/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**EH-1 RE-VERIFIED BYTE-EXACT AGAINST THE CONSUMED 7.0.0 ARTIFACT — the row holds at every leg** (C-14, G-0.10). ⟨`cat node_modules/@mkbabb/glass-ui/dist/components/header-ribbon/HeaderRibbon.vue.d.ts`⟩ declares `__VLS_Slots.anchor` with the **`{ pinned: boolean }`** payload; ⟨`grep -c` in `dist/header-ribbon.js`⟩ → `anchor` **4** · `pinned` **3** · `inert` **2** · `aria-hidden` **2** · `withKeys`/`"esc"` **1/1**; ⟨`grep -rl -- '--header-ribbon-actions-width' dist`⟩ → `components/header-ribbon/styles.css`. Absent: `anchorLabel` **0** · `HeaderRibbonMode` **0** · `HeaderRibbonAnchorSlotProps` **0**. **Letter (a) lists seven deletions; FIVE are falsified by the shipped artifact and only two hold.** Under the letter the target file is irreplaceable; under the tree it is deletable.

**THE TWO REPO-QUALIFIED LETTERS, NAMED WITH THEIR REPOS** — a receipt naming one of the two is RED (D-1, repair round 3): **(a)** value.js `docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19`; **(b)** keyframes.js `origin/master:docs/tranches/V/coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md` (26 L). They are DIFFERENT DOCUMENTS IN DIFFERENT REPOS, both dated 2026-07-16, both from the glass-ui BI/P/Q session. **The "NONEXISTENT path" reading is STRUCK at C-14**: the path is live — in the other repo.

**XR-4 / `DISPOSITIONS.md:21` FALSIFIED AT THE SETTLED FRONTIER.** The row reads *"HeaderRibbon Glass-7 consumer edits unapplied (drop `mode=`, delete `defineExpose`)"*. **Both are applied**: ⟨`sed -n '16p' demo/components/instrument/shell/EditorShell.vue`⟩ → `<HeaderRibbon placement="right">` (no `mode=`), and ⟨`grep -c 'defineExpose' …EditorShell.vue`⟩ → **0** in a 259-line file. `IN-GLASS-1`'s drift rider is discharged on the one consumer the import graph finds.

**ONE RE-DERIVATION DELTA, DECLARED**: the layer-order evidence cell (`:24`) states `@layer base, components, demo, properties, theme, utilities`. The FRESH build emits **five** layer-ats in the order **properties · theme · base · components · utilities** and carries **no `demo` layer** ⟨`grep -o '@layer[^{;]\{0,60\}' dist/gh-pages/assets/index-CBB2Hr7m.css`⟩. The dated cell is not rewritten; the re-derived reading is stated here.

**THE BYTE-OFFSET WITNESS IS SUPERSEDED — this record cites `dist/gh-pages/assets/index-CL_QYCiO.css` by name, and that coordinate is dead.** One `npm run gh-pages` ran at this seat (2026-09-17 13:37, exit 0, `built in 2.81s`). The artifact is now **`dist/gh-pages/assets/index-CBB2Hr7m.css`** — 571 192 B (was 571 142 B), mtime 2026-09-17 13:37 (was Jul 16 09:11), tree digest `bad6ea595fb59899e6589ad731b542d77eb19079a67d10548f6c07fc3c9564d2` over 54 files, per-entry hashes at `artefacts/W0/glass-citation-rederivation.txt` §5. Seven of the corpus's byte-offset receipts were re-derived in the fresh build and **all seven reproduce** (`data-allow-motion` 1 · `header-ribbon__actions` 1 · `btn-interactive` 0 · `--shadow-glass:` 0 · `skeleton-scan` 1 · `header-items-wrapper` 0 · `header-collapsed` 0 · `rainbow-vivid` 1). **Only the filename moved**; the findings stand, now on a dated and hashed artifact instead of a provisional one.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

