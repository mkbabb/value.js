# AuroraPane — adjudicated apotheosis (M-23/L-18, arbiter-F) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, declared not inherited

**Date**: 2026-08-04 · **HEAD at adjudication**: `e8393685` · branch `tranche-u` · corpus line anchors resolve at the corpus HEADs (`c654824e`/`f36f780c`, docs-only drift across them per the C seat); re-resolve every anchor before executing any cure (D-19).
**Subject**: `demo/scenes/atmosphere/AuroraPane.vue` (201 lines) + `aurora-atoms.ts` (71) + `aurora-harmony-stops.ts` (40); parent `demo/scenes/ConfigSliderPane.vue`; provider `demo/color-picker/composables/boot/useAtmosphere.ts`; producer door `@mkbabb/glass-ui@7.0.0` (installed dist).
**Corpus (r2 governs)**: `challenge-D-design.md` (Opus 5 [1m], pass 2, 19 defects incl. N-1..N-7/C-1..C-3) · `challenge-C-implementation.md` (Opus 5 [1m], pass 2, C-1..C-17) · `challenge-L-library.md` (Opus 5 [1m], pass 2, P2-1..P2-5/P2-C + pass-1 L-1..L-13 carried). Pass-1 files preserved beside them, consulted only where r2 cross-cites.
**Hostile re-readers adjudicated**: reader-DU (`aurorapane-l18-hostile-reread-design-and-usage`) · reader-LC (`AuroraPane` slug, library-and-code). Both were attacked before either was adopted; every verdict-level disagreement was re-derived by this seat with its own instruments, and every kill either ratified by measurement or re-litigated.

**Arbiter's own instruments this session (static + node against the installed dist + tracked-frame image reads; zero browser probes — probe parsimony)**: full source reads of AuroraPane.vue (whole), ConfigSliderPane.vue (whole), aurora-atoms.ts (whole), aurora-harmony-stops.ts (whole), useAtmosphere.ts:60–250, PreviewStrip.vue (whole), PaneHeader.vue:20–32, usePaneRouter.ts:75–90, aurora-bracket.test.ts:105–184, aurora-motion.test.ts:7/:26; **the C-17/L-9 decider**: three `tsc --strict` compiles against the real `AuroraAtoms` union — shipped ternary exit 0, plain `atoms.medium = { kind }` exit 0, deliberate `"not-a-medium"` sanity TS2322 exit 2; **the chromaCounterpoint decider**: node against `@mkbabb/glass-ui/aurora` `resolveAtoms` — at `chromaVariance:1`, adding `chromaCounterpoint:true` moves stop0 C **0.2069 → 0.03** while the max-min chroma-spread statistic stays **0.1846 in both** (the exact blindness of C-6's probe), and without `chromaVariance` it is a documented no-op; dist greps: `@property --glass-accent … initial-value: transparent` (property-regs.css, sole assignment metal.css, demo census 0), compiled `.text-caption{` idx **13602** < `.text-dropdown{` idx **13646** in one sheet (components.css) with `@utility text-caption` = 5 declarations incl. `font-style: italic` (semantic.css), `define MAX_NUCLEI 8` ×2 (aurora.js), `AuroraMedium` = 10 members (presets.d.ts:52), `SelectionValue = string | number` + the shipped `isSelectionValue` guard (_shared/selection.d.ts), SelectTrigger `size?: "sm"|"default"`, SelectItem `hideIndicator?: boolean` + the `"none":"start"` gutter branch (select bundle), LabeledSelect slot surface = `{ error }` only with `items: readonly string[]`, LabeledField `default(props: LabeledFieldSlotProps)` + `controlLabelable`, ConfiguratorRow.vue.d.ts:38–50 verbatim ("No a11y for/id wiring … use LabeledField directly"), `--select-font` dist readers = **0** vs foundation.css:373 declaration, `--menu-min-w: 11rem` + `--min-width-menu` theme bridge (foundation.css:445/:118); repo censuses re-run: `InstrumentChassis` in demo = 0, `Labeled*` in demo = 0, Configurator-lattice in demo = 1 (the declining comment), `<SelectTrigger` = 13 / `size=` = 0, AuroraPane mount = `usePaneRouter.ts:77` + `componentFor` **:88** (correcting reader-DU's ":85"), tsconfig.demo.json = 8 value.js paths vs package.json#exports = 7 keys (3 phantom / 2 omitted incl. `/css`), color-chips barrel: `serializeStop` + `RAMP_SAMPLE_COUNT` external consumers = **0**, `auroraAtoms.seed` writes at useAtmosphere.ts:143/:370, `writeClipboard` discriminated-result doc (useClipboard.d.ts:30–37), deps exactly `{glass-ui, keyframes.js}` with reka-ui a devDependency; authorities read verbatim: OPTICAL-BENCH-COMPOSITIONS.md:46/:80, VISUAL-CONSTITUTION.md:21/:75/:78/:83–84/:88–92/:214, PROPORTION-AUDIT.md:13–16/:79; tracked frames **read as images by this seat**: `pass2/frames/D-menu-kb.png` (ring on the highlighted row, selected "Analogous" unmarked; every option italic; option text indented past an empty gutter; three slab sliders WITH inline numeric readouts "Noise 0.50"/"Zones 6"; Zones thumb pegged; HARMONY…MOTION headerless above a ruled FIELD well) and `pass2/frames/D-rows-3x.png` ("Scattered"'s final `d` ascender cut by a straight vertical edge; "Smooth"/"Drifting" clear); routing joints read: `docs/tranches/X/waves/W4.md` (§X.W4.b), `W5.md:60`, `W6.md` (goal, §4a lanes, X.W6.h/i/j whole, H1), `W7.md` (§4/§4a/G19), `W8.md:100–112`, `W9.md:54`, `W10.md`/`W11.md` heads; standing registers consulted: `adjudicated/ConfigSliderPane.md` (§1 spectrum ruling, C-9 rescope, D-10, G-PAINT, RELAY B-2/B-3), `adjudicated/ColorSpaceSelector.md` (format exemplar + L-14 identity).

**Motion/PRM disclosure**: no motion row is carried from this corpus. The pane declares no motion (C negative proof 1: no rAF/listener/observer/timer surface; the aurora loop is glass-owned, root-verified); `motion-quarantine.md` carries no aurora/atmosphere row; X.W6.j's transition gates are already MOTION-SOURCED · PENDING-QUARANTINE by the wave's own text. No FALSE-row citation exists to launder.

---

## Scoreboard

**34 CONFIRMED rows in the adjudicated roster (1 BLOCKER · 18 MAJOR · 11 MINOR · 4 INFO) · 13 KILLED claims · 15 missed-by-the-corpus rows adopted (13 from the readers, verified; 2 new from this seat) · 11 UNPROVEN-NEEDS-LIVE residue items → visual audit.**

Blocker-weight survivor: **D/N-1** — the binding Atmosphere composition (OPTICAL-BENCH-COMPOSITIONS.md:46/:80, P122 preview-dominant 66.6666667/33.3333333, "form sections not Cards", boundaries `[]`) is inverted by an inspector-only Card with two hairlines and no bounded stage — **with a program-state correction neither reader made**: the InstrumentChassis consumption both readers certified as the required housing is **DEAD by X ruling** (CC-108/A-17; X.W6 §H1 "no copied chassis" is a *fact to preserve by subtraction*). The composition defect survives whole; the readers' cure vocabulary does not. The owner is the X-W5 scene contract adopted at **X.W6.j** (stage/inspector/action as a routed scene; j3 = Blob/Atmosphere persistent preview).

The corpus's own strongest mechanism finding survives intact and owned: **C/C-2 + C/C-3 + C/C-9** — the pane's only truth-bearing affordance omits the seam guarantee the painted field applies, the guarantee itself is algebraically broken (mean-tested, per-stop-clamped; the direction flip re-uses the wrong side's headroom — re-derived from `useAtmosphere.ts:96–109` by this seat), and the only oracle compares the strip to itself at the exact seed of maximal divergence.

---

## Ruled disagreements (the seat's actual work)

| # | dispute | reader-DU | reader-LC | RULING | deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| R-1 | **C/C-17 — is the identical-branch ternary a real TS narrowing device?** | Endorsed C-17's INFO grading ("the narrowing intent … is real") inside its confirmed C-15/16/17 bundle | KILLED by tsc measurement | **KILLED — reader-LC wins; reader-DU's sub-endorsement OVERRULED** | This seat's own three-compile harness: the shipped ternary and the plain `atoms.medium = { kind };` BOTH exit 0 under `--strict` against the real discriminated union; the deliberate `"not-a-medium"` write exits 2 with exactly TS2322, proving the harness sees real errors. The ternary is decoration; L-9's one-line cure is the surviving form; the `:86–88` comment explains an `amount` distinction the expression does not make. |
| R-2 | **C/C-6's chromaCounterpoint limb — "a genuine producer no-op at this dist … a coordination packet"** | silent | KILLED by dist measurement | **KILL RATIFIED by independent re-measurement — the packet is RETRACTED** | This seat's node run: at `chromaVariance:1`, `chromaCounterpoint:true` moves stop0 **C 0.2069 → 0.03** (the documented sage-whisper pin, atoms.d.ts:136–143) while the palette's max−min chroma spread is **0.1846 both ways** — C's probe compared exactly that spread scalar, so a pinned extreme was invisible to it. Bonus precision: without `chromaVariance` the atom is a no-op **by documented design** ("When true (and `chromaVariance > 0`)"). A packet reporting "chromaCounterpoint is dead" would report a defect that does not exist. |
| R-3 | **D/C-1 — does `text-caption`'s italic arm land?** | CONFIRMED MAJOR (order idx 13602/13646 + frames) | Demoted to UNPROVEN-NEEDS-LIVE ("equal specificity; winner is generated-source order, needs a live computed read") | **CONFIRMED — reader-DU wins** | The demotion is over-cautious twice over: (a) this seat's own grep of the ONE compiled sheet places `.text-caption{` at byte 13602 and `.text-dropdown{` at 13646 — same layer, dropdown later, so the font-size arm loses **statically**; (b) the italic arm needs no cascade ruling at all — `text-dropdown` declares font-size only and `font-style` has no other declarant (demo grep: hljs.css only); (c) the D seat's live probe already measured computed `font-style: italic` on the trigger, and this seat read both frames: every trigger value and all eighteen options render slanted. Sharpest authority adopted from reader-DU's own missed-INFO: PROPORTION-AUDIT.md:15's W20/W29-**frozen** sentence — "Dropdown options remain Plus Jakarta Sans `text-small` control copy and non-bold" — names the exact surface; VC:75/:78 closes the matrix. |
| R-4 | **C/C-7's h-9 figures — "36px where the design system resolves 60px"** | CONFIRMED (token math) | 60px demoted to needs-live | **SPLIT — both halves adopted at their true rungs** | Static-certain (this seat): `h-9` = flat 2.25rem in the utilities layer on 4 triggers; `size?: "sm"|"default"` shipped and unused (my census: 13 `<SelectTrigger` in demo, **0** pass `size=`); the coarse floor `--control-floor: var(--touch-target, 2.75rem)` = 44px is defeated by construction. The resolved **60px** (`2.5rem × --ui-scale 1.5`) is a chromium-cell live read (C's p3-live) — adopted as corroboration, labeled chromium-cell, cross-engine confirmation rides the visual audit. The defect stands MAJOR on the static half alone. |
| R-5 | **D's Move 2 — "consume LabeledSelect"** | KILLED (structurally impossible; names LabeledField in its missed list) | KILLED (same, with the d.ts quotes) | **KILL RATIFIED jointly; the constructive cure ADOPTED** | This seat's d.ts reads: LabeledSelect's entire slot surface is `{ error }` and `items: readonly string[]`/`modelValue: string` — no per-item slot, so it deletes the T-17 PreviewStrip (real producer API, SelectItem `description` slot) and forces display-string atoms. The right primitive is **LabeledField** — `default(props: LabeledFieldSlotProps)` exposing `controlId`/`labelledBy` plus `controlLabelable` documented for exactly a composite non-labelable root — wrapping the existing `<Select>`. And the mechanism is **already authored at X.W4.b** ("adopt `@mkbabb/glass-ui/labeled-field` … `controlLabelable: false` … move trigger height from `class="h-9"` to `size="sm"`") — AuroraPane.vue is absent from W4.b's file bounds, so the pane's own adoption lands in X.W6.j's re-composition citing W4.b as the mechanism of record. |
| R-6 | **D/N-2's authority + root cause** | Killed the §4.2/§5.14 citation (ColorSpaceSelector-scoped); re-grounded on §4.1; root = registered `@property` | Same root, "stronger than the axis gave" | **BOTH RATIFIED** | This seat read the scoping verbatim: VC:90 opens "ColorSpaceSelector retains its listbox option semantics…" under §4.2's component-register head; PROPORTION-AUDIT:79 item 14 is ColorSpaceSelector's. The surviving general authority is VC:83–84 (§4.1 "never color-only"; "Focus remains visibly distinct from selection"). Root cause re-verified: `@property --glass-accent { syntax: "<color>"; inherits: true; initial-value: transparent; }` — a registered property is never guaranteed-invalid, so the `var()` chain can never reach `currentColor`; sole assignment in the whole dist is `.metal-rainbow-rim`; demo assigns neither token. This is a **producer-default defect for every consumer**; D's original "defined-but-transparent at :root in this app" underexplains it. |
| R-7 | **The dual-chassis contradiction (reader-LC missed #5): D prescribes InstrumentChassis, L prescribes Configurator, neither reconciles** | — | Raised as MINOR, unresolved | **RESOLVED BY PROGRAM RULING — this seat's own discovery** | X.W6's goal criterion and gate H1 (read by this seat): "**the producer's InstrumentChassis is dead** (CC-108) … A-17 killed the producer arms"; H1 is a subtraction gate failing any unit that "factors two instruments' stages into one parameterised housing". So NEITHER reader's chassis is adopted: the route composes the **X-W5 scene contract** (stage/inspector/action per routed scene) at X.W6.j. The Configurator-lattice ADOPT cure was additionally already **REFUSED at the standing ConfigSliderPane adjudication** (its C-9 rescope: `useConfiguratorState` is preset-modeled — `resetCurrent()` restores the *active preset's* baseline, not defaults — the state machine does not express this pane's model). Both hostile readers confirmed L/P2-1's ADOPT cure without consulting `adjudicated/ConfigSliderPane.md`; recorded in DISSENT as a register-blindness event. |
| R-8 | **L/P2-2's severity rung after its consequence kill** | Confirmed mechanism MAJOR, killed the consequence | Killed the headline, noted "the MAJOR rung rests on it" | **MECHANISM MAJOR, CROSS-BOOKED — the rung survives on the standing CSP record, not on the killed headline** | The identical mechanism is already adjudicated at `adjudicated/ConfigSliderPane.md` (C-3/D-3/D-4/D-14 → the **G-PAINT single cut**, where the arbiter there proved the naive retire-the-variant cure collapses the thumb to width 0 and that the demo CAN paint the range — facts that supersede both this corpus's cure and its counter-cure). ONE identity, ⇄CSP, never re-booked. The W8 tension is recorded: X-W8 lists `ConfigSliderPane.vue:202` under CC-105 BLOCKED-ON the 8.0.0 repin with receiving surface X-W4.g — the CSP G-PAINT ruling and the CC-105 booking must be reconciled by the X-W0 sitting, not silently by either wave. |

---

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. "⇄CSP" = one identity with the standing `adjudicated/ConfigSliderPane.md` register (anti-rename); wave ids are tranche-X (`docs/tranches/X/waves/`).

**BLOCKER**

- **AP-1 · D/N-1 — BLOCKER.** The binding Atmosphere composition is inverted: OPTICAL-BENCH-COMPOSITIONS.md:46 binds P122 preview-dominant (66.6666667/33.3333333, "form sections not Cards") and :80 binds boundaries `[]`/reserve `none`; the route ships an inspector-only `Card tier="resting"` (ConfigSliderPane.vue:99–102 under AuroraPane.vue:110) with two hairlines (:233 border-bottom, :250 border-top) and no bounded stage. PROOF: authorities read verbatim by this seat; both source files read whole; `InstrumentChassis` demo census 0 (mine). Reader-DU's sharpening adopted: the `[]` inventory governs the chassis's typed boundary set; the clause that binds the two demo hairlines is VC:92's final sentence ("every other composition retains no divider"). Rhetoric demoted per reader-DU: the aurora paints the full viewport around the card (frame read by this seat) — the absent thing is a *bounded dedicated stage*, not all preview; "0 % preview"/"60.2 % of main" figures ride the residue. **PROGRAM-STATE CORRECTION (this seat's, against both readers)**: the "producer ships the required housing; consume `./instrument-chassis`" cure is STALE — CC-108/A-17 rule the InstrumentChassis dead and X.W6 H1 *forbids* rebuilding anything chassis-shaped. **DISPOSITION: X.W6.j** (the X-W5 scene contract adopted per route; j3 Blob/Atmosphere persistent preview; j4's 720×450 short-landscape arm covers pass-1 D-10's truncation). H1 stands guard over the cure's shape.

**MAJOR**

- **AP-2 · D/N-2 — MAJOR.** The selected option paints zero pixels: the marker inks `var(--select-dot-color, var(--glass-accent, currentColor))`; `--glass-accent` is a **registered** property (`initial-value: transparent`) so the chain can never fall through; neither token is assigned in demo (census 0) and the dist's sole assignment is `.metal-rainbow-rim`. `aria-selected="true"` is announced, nothing is drawn. PROOF: property-regs.css + select bundle + censuses, all mine; frame D-menu-kb.png read by this seat (ring on `data-highlighted` "Split Complementary"; selected "Analogous" unmarked). Authority: VC:83–84 §4.1 (the §4.2/§5.14 citation is killed, K-6). Producer-default defect for every consumer. **DISPOSITION: consumer half → X.W6.j** (one token assignment at the route re-composition; `hideIndicator` is admissible only as the AP-26 interim); **producer half → the standing glass-ui BH relay riding X-W9's coordination-packet step (§12)**, with the corrected root cause (the registration's initial-value, not undefined-only fallthrough).

- **AP-3 · D/N-3 = C/C-6 — MAJOR.** Dark mode ships the producer's own *named* "dark-leg defect": `lightnessScheme`/`lBand`/`hueSpread`/`chromaVariance` are live at the consumed dist (atoms.d.ts:104–135, read verbatim; reader-LC measured all four through `resolveAtoms`), the demo asserts the contrary as fact in three places (useAtmosphere.ts:233–236 "the atoms door ships no scheme/lBand (GAP-L2, probed at this dist)" — read by this seat; atmosphere-calibration.ts:24–30; aurora-atoms.ts:31–33), and `DEFAULT_AURORA_ATOMS` sets no scheme. PROOF: mine + both readers convergent. The chromaCounterpoint limb is KILLED (K-3) and severed from this row. **DISPOSITION: X.W6.i gate i2** — the wave already carries this row as **CC-066** with the dist probe pasted ("lightnessScheme 2 occurrences, lBand 4 in dist/aurora.js") and a closes-either-way contract (land the band + delete the three stale comments, or one dated §D letter). ONE identity with CC-066; not re-booked.

- **AP-4 · C/C-1 — MAJOR.** On the shipped `"css"` render tier, five of seven knobs are structurally inert and the pane is never told: `auroraRenderMode` is a setup-local const (useAtmosphere.ts:158, read by this seat), the css-tier atmosphere is `paletteToCssGradient(resolvedPalette)` (:207–211 — palette only), the pane injects only `AURORA_ATOMS_KEY` (:42), and the repo's own honest-terminal law (:283–290) was applied to the canvas, never the controls. PROOF: structural half re-derived whole by this seat; the byte-identical-PNG nulls with two positive controls are C's chromium-cell probe (residue U-3). **DISPOSITION: X.W6.i** — i1's X:ATMO-1 ("named atoms move ≥ stated ΔE2000, from committed frames") is the natural oracle; the substrate-honesty provide + disabled/degraded states ride the same unit's files (`useAtmosphere.ts`, `AuroraPane.vue` both in .i's bounds).

- **AP-5 · C/C-2 — MAJOR.** The harmony PreviewStrip breaks the O-14 law its own header states (aurora-harmony-stops.ts:6–8): the strip resolves `resolveCalibratedAtmosphere({...atoms, harmony}).palette` bare (:38) while the painted field is `guaranteeSeamOffset(resolveCalibratedAtmosphere(…), seed)` at BOTH resolve sites (useAtmosphere.ts:170/:181) — divergence up to `DERIVE_SEAM_FLOOR = 0.06` L whenever the guard fires. PROOF: all four sites read by this seat. Reader-LC's referent kill ADOPTED (K-10): in dark, `--saved-bg-*` derives through `deriveAurora(seed,{scheme:"dark"})` (:237–247, read by this seat), so C's probe referent conflates two divergences on that leg — the structural omission stands independent of the probe. Magnitudes (210/510, Δ≤44 rgb) → residue U-4. **DISPOSITION: X.W6.i**, with a **bounds rider**: `aurora-harmony-stops.ts` is absent from .i's §Files — the wave extends its bounds or this row re-homes at the formation boundary.

- **AP-6 · C/C-3 — MAJOR.** `guaranteeSeamOffset` does not deliver its own guarantee: headroom is computed on the palette MEAN (`0.98 − meanL` / `meanL − 0.02`, :103) while the clamp applies PER STOP (:108), so a pinned extreme shorts the realised mean below the floor; and when `dir` flips (:104) the required travel becomes `FLOOR + |delta|` in a direction whose headroom was never tested. PROOF: re-derived from :96–109 by this seat, independent of any probe. Reader-DU's citation caveat adopted: "internal spread preserved" lives at :73, not :88–90 — the phrase exists, the anchor drifted. Failure frequencies (90/1200, 9/120) → residue U-5. **DISPOSITION: X.W6.i** (same bounds rider as AP-5; the cure must precede AP-5's re-point, per C's own sequencing).

- **AP-7 · C/C-4 = L/L-2 — MAJOR.** Three of ten shipped media (`kuwahara`, `metal`, `metal-gradient`) are unreachable through the pane (MEDIA lists 7, presets.d.ts:52 declares 10 — both read by this seat), and `AuroraMedium[]` structurally cannot catch a subset; the other three vocabularies are complete (readers' counter-check), so the sole drifted one went unnoticed. PROOF: mine + reader-LC's dist resolution of all three kinds. Cure of record: the exhaustive `Record<AuroraMedium, string>` label map (kills AP-23's `label()` masher in the same construct). **DISPOSITION: X.W6.i** (the stale-producer-fact family the atmosphere harness owns; the map itself lands wherever the rows are rebuilt — .i then .j run serial on this file by W6 §4a).

- **AP-8 · C/C-5 — MAJOR.** The Zones slider caps at 6 (AuroraPane.vue:103) on a hand-copied producer fact that is stale — the shipped ceiling is 8 (`define MAX_NUCLEI 8` ×2, my grep) — and the repo contradicts itself inside one `it()`: aurora-bracket.test.ts:112 "Glass 7 raised the ceiling from 6 → 8" beside :123 `expect(landed.nuclei.length).toBe(6); // the ceiling, fully used`, with aurora-atoms.ts:34–38 asserting "`MAX_NUCLEI = 6` … atom-UNREACHABLE" (all read by this seat). Frame: the thumb pegged at the track terminal at 6 (my read). The owner MANDATE §0.5 ("a few more zones") was parked on a ceiling that lifted. **DISPOSITION: X.W6.i** — same stale-fact class as i2's three comments; **rider**: i2's comment list must extend to aurora-atoms.ts:34–38 + the bracket test's `:123` comment, and the literal `6` leaves the pane (derive or producer-export packet).

- **AP-9 · C/C-9 — MAJOR.** Nothing tests this component: sole mount `usePaneRouter.ts:77`/`componentFor` :88 (my census; correcting reader-DU's ":85"); the O-14 unit oracle (aurora-bracket.test.ts:170–184, read by this seat) compares `auroraHarmonyStops` to the identical bare call the strip makes, at seed `"oklch(0.62 0.27 9.8)"` — structurally incapable of witnessing AP-5, and deleting the seam guarantee makes it pass harder (C's M4 inversion proof); aurora-harmony-stops.ts:11–13 cites `test/preview-chips.test.ts` as its oracle — the file EXISTS (97 lines, my wc) with **0** aurora occurrences (C's "does not exist" heading killed, K-9); o18's census selects the parent's `.configurator-row` rows and the enum rows sit outside `.console-well` by construction (slot before the well — my source read). **DISPOSITION: X.W6.i** (o25/o26 are the wave's new oracle surface; the O-14 real-referent leg — read `data-stops`, select, read `--saved-bg-0..3`, assert equality — rides there; the o18 blind spot self-heals when AP-10's re-composition moves the rows into the well).

- **AP-10 · C/C-10 = D/D-3 = L pass-1 L-5 — MAJOR.** The pane rebuilds the row primitive its parent's own header forswears (ConfigSliderPane.vue:6–10), with no label column (`.aurora-row` flex space-between — trigger left edge a function of label length), and renders the enum block OUTSIDE the `.console-well` its sibling population lives in (default slot at :110 precedes the well at :122; `.console-well` paints a plate, foundation.css:350) — one pane, two row grammars. PROOF: structure re-derived whole by this seat; frame confirms labels at one x, boxed FIELD below; the 58.7/50.1px spreads are live measurements (residue U-7). **DISPOSITION: X.W6.j** (the row re-composition; one spine, one well).

- **AP-11 · D/C-1 — MAJOR (ruled at R-3).** `text-caption` is HALF-applied: its font-size arm loses to `text-dropdown` by compiled order (13602 < 13646, my grep) while its `font-style: italic` arm has no competitor and lands on all 4 triggers + 18 options (8 stamps: AuroraPane.vue:122/:129/:142/:146/:156/:160/:170/:174), breaching the closed role matrix (VC:75/:78) and the W20/W29-**frozen** sentence naming dropdown options (PROPORTION-AUDIT:15 — read verbatim). The producer default underneath (`text-dropdown` → `--type-small`) is already canon-conformant; the cure is deletion with **no replacement class**. **DISPOSITION: X.W6.j** (the stamps die in the re-composition; AP-24's glyph clip dies with them).

- **AP-12 · C/C-7 (h-9 half) = L/L-8 — MAJOR (ruled at R-4).** Four per-instance `h-9` overrides (flat 2.25rem, utilities layer) defeat the tokenized control height incl. the coarse-pointer floor (`--control-floor: 2.75rem` = 44px), reproducing a shipped `size` axis no demo call site uses (13 triggers, 0 `size=` — my census). The 60px resolved figure is chromium-cell. **DISPOSITION: X.W6.j** via the **X-W4.b mechanism of record** (`size="sm"`; AuroraPane absent from W4.b's bounds — the pane's own edit rides .j).

- **AP-13 · C/C-8 — MAJOR.** The four enum labels ink at raw `--muted-foreground` (AuroraPane.vue:194–200), the exact token this repo has ruled against by name on this pane's own surface (PaneHeader.vue:22–30 "never raw `--muted-foreground`" — read verbatim; the sibling rows re-ink at `--ink-muted`, ConfigSliderPane.vue:204–206), on a *different* (glass) ground than the well the sibling sits on — the scoping validated structurally. Ratios (3.50–3.94:1) → residue U-1. **DISPOSITION: X.W6.j** — the `.aurora-row-label` block deletes whole in the re-composition; once the rows live in the well, o18's existing census leg reaches them (no new oracle needed for this row).

- **AP-14 · D/N-4 — MAJOR (ruled at R-5).** glass-ui ships the labeled-form-control layer; ConfiguratorRow's own doc forbids routing an accessible form control through it ("No a11y for/id wiring … use LabeledField directly", read verbatim); demo consumption = 0 (my census); the pane's hand-rolled `<span class="aurora-row-label">` wires nothing. Cure of record: **LabeledField** `layout="horizontal"` `:control-labelable="false"` wrapping the existing Select — preserves the PreviewStrip, closes AP-10/AP-13/AP-31's seats in one move (reader-DU's missed M1, verified against the d.ts by this seat). **DISPOSITION: X.W6.j** (mechanism = X-W4.b).

- **AP-15 · D/N-5 — MAJOR (count corrected).** The producer exposes `amount?` on every textured medium (atoms.d.ts:160–171/:45–54, read verbatim); `setMedium` writes `{ kind }` on both branches — so **SIX** of the seven exposed media (incl. Pastel — reader-DU's kill K-8 adopted) carry a permanently unwritable amplitude axis, against VC:214's "every select/axis has an observable effect". Companion confirmed: `setArrangement` (:81–84) rebuilds `zones` from scratch — structurally lossy for any sibling key. **DISPOSITION: X.W6.j** (an amount slider mounting for textured kinds; i1's oracle witnesses it).

- **AP-16 · L/P2-1 — MAJOR, ⇄CSP (ruled at R-7).** The fork FACTS stand (ConfigSliderPane re-rolls reset/scroll-port/footer-dock/section-header while its header names `useConfiguratorState` and declines it — my source read; lattice consumption census = 1 comment). The ADOPT-the-lattice cure is **superseded twice**: by the standing CSP adjudication's C-9 rescope (preset-model mismatch — `resetCurrent()` restores the active *preset*, not defaults) and by CC-108's no-chassis posture. ONE identity at `adjudicated/ConfigSliderPane.md`; not re-booked here.

- **AP-17 · L/P2-2 (mechanism) — MAJOR, ⇄CSP (ruled at R-8).** Spectrum variant + `--ink-muted`-as-track-fill confirmed (recipe verbatim in dist; ConfigSliderPane.vue:146/:202; boot stamp useAtmosphereBoot.ts:103); the "no value readout" consequence KILLED (K-5). Identity = CSP C-3/D-3/D-4/D-14 → **G-PAINT single cut**; the CC-105/X-W4.g BLOCKED booking tension is recorded for the X-W0 sitting.

- **AP-18 · L/L-6 + C/C-16 — MAJOR, ⇄CSP.** Stringly `SliderDef.key`/`Record<string, unknown>` surface with safety in exactly one of two consumers (BlobPane's `NumericAtomPath` guard vs AuroraPane's bare strings + the two `as unknown as` casts at :111/:113 — my source reads). Reader-LC's missed READ-half adopted as sharpening (this seat verified: `readPath` → `undefined` → `fmt()`'s `v.toFixed(3)` — a render-time TypeError, the inv-N-1 white-screen class, latent for the same structuredClone/Object.assign reasons as AP-21): the corpus booked only the weaker write-half failure. Identity = CSP's typed-lens rewrite (`SliderDef<T>`, keyof-derived paths); the pane-side casts die when it lands.

- **AP-19 · L/L-1 — MAJOR — NO-WAVE-OWNER.** The aurora concept has no home: `useAtmosphere.ts` (color-picker/boot) statically imports `scenes/atmosphere/aurora-atoms` while `aurora-harmony-stops.ts` (scenes) imports `color-picker/boot/atmosphere-calibration` — a bidirectional area edge, both edges aurora's — and the eager boot chunk carries a module living in a lazy pane's directory (usePaneRouter.ts:77, my read). No X wave mints `demo/atmosphere/` (X-W8 is subtraction-only for `demo/ui`). **Recorded for the next formation boundary**; interim guard: no new edge in either direction.

**MINOR**

- **AP-20 · D/N-7 = L/L-7 — MINOR.** The four setters type on reka-ui's `AcceptableValue` (a devDependency — deps read by this seat) past glass-ui's own `SelectionValue = string | number` emit, manufacturing four `String(v) as …` launders that validate nothing. Arbiter's addition: the producer also ships the `isSelectionValue` guard (\_shared/selection.d.ts, my read) if a runtime seam is ever wanted; the type-level retype alone suffices. **DISPOSITION: X.W6.j** (retype at re-composition; drop the coercions).

- **AP-21 · C/C-12 — MINOR (latent).** Three fallbacks disagree with the defaults they shadow (`?? "composed"` vs `"scattered"`, `?? "breathing"` vs `"drifting"` — the exact value U33 changed, `?? 4` vs `6` — all read against aurora-atoms.ts by this seat); unreachable today (structuredClone seeding + Object.assign reset), masking-fallback class regardless; `DEFAULT_AURORA_ATOMS` is imported two lines above. **DISPOSITION: X.W6.j** (accessors read the imported default; no hand-copied literal survives).

- **AP-22 · C/C-15 — MINOR (hypothesis, honestly labeled).** `inject(AURORA_ATOMS_KEY)!` with no default is a white-screen trapdoor the moment AP-9's component test exists; the default is already in the file's imports. **DISPOSITION: X.W6.j.**

- **AP-23 · C/C-11 — MINOR.** `label()`'s mechanical split-and-titlecase renders "Vangogh" (and would render `kuwahara` as a bare token); correct for 6 of 7 by luck of spelling. Dies inside AP-7's `Record<AuroraMedium, string>` map. **DISPOSITION: X.W6.i** (with AP-7).

- **AP-24 · D/N-6 (first half) — MINOR.** The italic face clips its own terminal glyph — the value box is sized to the advance with `overflow: hidden`, undetectable by any scrollWidth check; **frame read by this seat**: D-rows-3x.png shows "Scattered"'s final `d` ascender terminated by a straight vertical cut. Second half ("Analogous ≡ Monochrome identical triples") stays demoted (my frame read: similar, low-contrast, distinguishable — residue U-8). Dies with AP-11's deletion. **DISPOSITION: X.W6.j.**

- **AP-25 · C/C-13 + L/P2-C — MINOR, ⇄CSP.** 12px inline thumbs on every viewport; demo-owned (spectrum-only `× .75` width rule + the demo's own block-only coarse `::before` — both verified in the standing CSP record); L's self-correction of pass-1's attribution ratified. Identity = CSP G-PAINT + RELAY B-2.

- **AP-26 · reader-DU missed #5 (ADOPTED) — MINOR.** The producer reserves the indicator gutter unconditionally unless `hideIndicator` (select bundle `"none":"start"` branch, my read); with AP-2's alpha-0 marker, all 18 options carry a permanently blank ~24px reserve — a visible layout hole (frame: option text indented past nothing). `hideIndicator` is the one-line **interim only** — this seat's rider: the house direction (VC:90's ColorSpaceSelector precedent) is the producer gutter WITH a real marker, so the terminal cure is AP-2's token + packet, and the interim must be reverted when the marker inks. **DISPOSITION: X.W6.j (interim decision recorded either way).**

- **AP-27 · reader-DU missed #6 (ADOPTED) — MINOR.** The PreviewStrip is `aria-hidden="true"` with no text equivalent anywhere (PreviewStrip.vue:43; no `text-value` enrichment at the SelectItem — my reads): a non-visual user receives none of the palette information the affordance exists to convey. Correctly MINOR (the harmony *name* carries the primary information); all three axes evaluated the aria-hidden only as name-pollution (sound, C negative proof 5). **DISPOSITION: X.W6.j** (an sr-only or `aria-describedby` sentence derived from the same stops).

- **AP-28 · reader-LC missed #2 (ADOPTED) — MINOR.** Copy JSON and Reset disagree about what "the config" is: `copyAsJson` stringifies the LIVE atoms into which useAtmosphere writes `seed` on every colour change (:143/:370 — my greps), while the pane's header declares seed not-a-knob and Reset deliberately excludes it (aurora-atoms.ts:16–21). The copied JSON pins a colour the pane disclaims; pasting it back freezes a seed Reset cannot clear. The copy LEG (silent discriminated-result drop) is ⇄CSP D-10; the SHAPE decision (exclude seed or document it) is this surface's. **DISPOSITION: X.W6.i** (the atoms-door shape decision rides the harness unit's files).

- **AP-29 · L/P2-4 + reader-DU missed #2 — MINOR (cure killed, K-12).** The caps-eyebrow recipe is hand-rolled twice, 5/5 declaration-identical (my byte-compare of AuroraPane.vue:194–200 vs ConfigSliderPane.vue:237–243; `tracking-caps` = exactly those two sites). L's `text-admin-label` cure is DEFECTIVE on three axes (10px fixed vs the fluid `--type-small` clamp; +weight 500/line-height 1; `text-muted-foreground` re-commits AP-13's blocker ink) — and **X.W7.g already owns the consolidation** with a different target (the OM-15 dialect census: "dialects consolidated on `.section-label`"; AuroraPane = 6 census hits; "4 of which re-declare uppercase/tracking that `text-mono-caption` already applies" — read by this seat). **DISPOSITION: X.W7.g** (the wave's own target rung, not `text-admin-label`).

- **AP-30 · reader-DU missed #3 + D/C-2 + reader-DU missed #4 — MINOR (voice/IA cluster).** (a) The pane's description prose enumerates five of its seven controls, omitting Harmony (the strip-bearing protagonist knob) and Arrangement, and "Tune the field's SHAPE" is false of colorEnergy/harmony, which are palette knobs by the producer's own doc (verified against :115 and atoms.d.ts:96–102). (b) The leaf enum label and the group heading are byte-identical in voice (D/C-2 — the 1/√φ mis-jurisdiction kill stands; PROPORTION-AUDIT:13–15 is Picker-frozen, read verbatim). (c) The enum population is an UNNAMED group — four heading-voiced labels under no heading, above a ruled FIELD header (frame read by this seat) — the IA half no axis stated. **DISPOSITION: (a) → X.W7.g** (OM-15 REDUCE class); **(b)+(c) → X.W6.j** (the re-composition names the group and demotes the leaf voice).

**INFO**

- **AP-31 · L/P2-5 — INFO, ⇄W-HYGIENE.** tsconfig.demo.json's value.js path map is wrong on 5 of 8 rows vs package.json#exports (re-derived exactly by this seat: 3 phantom — bare, `/parsing`, `/units`; 2 omitted — `/value`, `/css`, the latter used 10× incl. this pane's closure). IDENTITY: X-W8 names it "L-22 ≡ W-HYGIENE, another owner"; fact-identical to ColorSpaceSelector L-14. ONE identity; not re-booked.

- **AP-32 · L/L-13 — INFO.** aurora-motion.test.ts:7 cites a module path retired at W43 while :26 imports the live path (both read by this seat). **DISPOSITION: X.W6.i** — joins the i2/AP-8 stale-prose deletion list (bounds rider: the file is not in .i's list).

- **AP-33 · reader-DU missed #7 (ADOPTED) — INFO.** `--select-font` is a dead DEMO token: declared at foundation.css:373, **zero readers in the entire consumed dist** (my grep). D's coordination packet 3 mails a producer about a token the producer cannot act on — the packet is **RETRACTED**; the cure is a one-line demo deletion. **DISPOSITION: X.W6.j** (dies in the re-composition's style sweep).

- **AP-34 · reader-LC missed #4 (ADOPTED) — INFO — NO-WAVE-OWNER.** `serializeStop` + `RAMP_SAMPLE_COUNT` on the color-chips public barrel have zero external consumers (my census); with L/P2-3's `stampStops` mis-homing (one zero-dependency line co-housed with the parser-dragging sampler), the barrel's surface is half dead. color-session surface, no X owner named. **Recorded for the formation boundary.**

---

## Killed-claims register — refutations of record

- **K-1 · C/C-7 second half — "`text-caption` is a DEAD class … overrides nothing."** FALSE. The utility declares FIVE arms (semantic.css, read by this seat); only font-size loses; `font-style: italic` has no competing declarant and renders in both tracked frames (my image reads). C's p9-tokens probe measured fontSize alone — an instrument that could only see the losing arm. D/C-1 governs (R-3). C's *cure* (delete) survives; its rationale does not — and the deletion is mandatory (frozen-matrix breach + AP-24's clip), not cosmetic.
- **K-2 · C/C-17 — "a real TS narrowing device … not dead code."** REFUTED by this seat's own measurement (R-1): both forms compile clean under `--strict` against the real union; the sanity injection proves the harness. Reader-DU's endorsement of the INFO grading is overruled with it. L-9's one-line cure is the surviving form.
- **K-3 · C/C-6's chromaCounterpoint limb — "a genuine producer no-op … a coordination packet."** REFUTED by this seat's own dist run (R-2): stop0 C 0.2069 → 0.03 at `chromaVariance:1`, exactly the documented sage-whisper pin; C's spread-scalar probe was structurally blind to a pinned extreme. **The packet is retracted.**
- **K-4 · D §5 Move 2 — "render the four enum atoms as `LabeledSelect layout='horizontal'`."** Structurally impossible: sole `error` slot + flat `readonly string[]` items (d.ts read by this seat) — adopting it deletes the T-17 strip (the pane's only truth-bearing affordance, certified real API by D's own §4) and ships raw kebab identifiers. The constructive cure is LabeledField (AP-14).
- **K-5 · L/P2-2's consequence — "a control with no value readout at all except thumb position."** FALSE by the audited file itself (`:name="fmt(read(def.key))"` on every row) and by this seat's frame read ("Noise 0.50", "Zones 6" inline). The narrower survivor — no filled/unfilled proportion — rides AP-17 ⇄CSP.
- **K-6 · D/N-2's authority — VC §4.2 / PROPORTION-AUDIT §5.14.** Mis-jurisdictioned: both clauses are ColorSpaceSelector-scoped (read verbatim by this seat). The defect survives whole on §4.1 (VC:83–84).
- **K-7 · D/C-1 claim-4 sub-endorsement — "`min-w-menu` inert."** FALSE, and self-contradicted within D's own r2 (:324 "inert" vs :381–383 measuring it CLAMP two rows at 320px). `--min-width-menu: var(--menu-min-w)` = 11rem is a real theme key (foundation.css:118/:445, my read) — live, merely non-binding at wide viewports. (The `--select-font` half of the same sentence is separately corrected by AP-33: dead, but demo-dead, not a producer seam.)
- **K-8 · D/N-5's count — "five of the seven media."** `Exclude<AuroraMedium,'smooth'>` includes `pastel`, which the pane ships: **six** of seven, and the "2 kinds × 1 amplitude" framing misdescribes a real seven-member kind axis. Defect wider than stated; carried corrected (AP-15).
- **K-9 · C/C-9's heading — "its cited oracle does not exist."** The file exists (97 lines, 4 passing its, my wc); what is absent is aurora coverage — which C's own body says correctly. Heading imprecision recorded; the row survives (AP-9).
- **K-10 · C/C-2's referent limb — `--saved-bg-*` as "the material the page actually paints."** True on the light leg only: dark ground derives through `deriveAurora(seed,{scheme:"dark"})` (useAtmosphere.ts:237–247, my read) — a different derive than the field. The structural omission stands without the probe (AP-5).
- **K-11 · D/N-1's rhetoric + cure — "0 % preview / the protagonist region does not exist" and "consume InstrumentChassis."** The rhetoric overstates (the aurora paints the viewport; the absent thing is a bounded stage — my frame read); the cure is stale against CC-108/A-17 (the InstrumentChassis is DEAD; H1 forbids chassis-shaped rebuilds). The composition breach itself survives at full BLOCKER weight (AP-1).
- **K-12 · L/P2-4's cure — `class="text-admin-label text-muted-foreground"`.** Defective on three independent axes (~40% shrink to a fixed 10px rung; +weight/leading changes breaking the claimed 5/5 identity; re-inks the exact token AP-13 indicts) — and X.W7.g's own consolidation target (`.section-label`) supersedes it (AP-29).
- **K-13 · D coordination packet 3 — the `--select-font` seam.** Mailed to a producer with **zero** readers of the token in its entire dist (my grep). Not a seam that fails to reach — a demo declaration whose consumer no longer exists. Retracted; one-line demo deletion (AP-33).

---

## Superlatives — preserved with provenance

- **The T-17 harmony strip is the best-designed thing in this closure** (all three axes + both readers concede; this seat's frame read confirms six distinct palettes): each candidate row previews the palette IT would resolve from the LIVE atoms — seed-exact, through the producer's real per-item slot, `aria-hidden` so it pollutes neither name nor typeahead, stamped on `data-stops` as its own oracle hook, honest mask-fade truncation past 7 stops, and computed only while `SelectContent` is mounted (the "zero rest cost" claim at AuroraPane.vue:36–38 is true — C negative proof 12). The chronic C2 palette-blind disease does not manifest here. That its truth function is 0.06 L off its referent (AP-5) is a seam defect in an affordance whose *shape* is exactly right.
- **The seed/shape ownership seam is stated in prose and honoured in code**: `DEFAULT_AURORA_ATOMS` carries NO seed (aurora-atoms.ts:16–21), `structuredClone` at both provider and reset means the module default is never aliased, and Reset restores shape without touching the colour (C negative proof 8, live). AP-28 is the one unexamined mirror (Copy), not a crack in the seam itself.
- **The throw-free-getter discipline** (useAtmosphere.ts:122–146, :76–85): a named white-screen class (inv-N-1) prevented structurally, with the reasoning in the file; `guaranteeSeamOffset` keeps the same closed-form, returns-untouched-on-unreadable-seed posture — the algebra is wrong (AP-6), the shape is right.
- **The bracket test is a real regression posture** (aurora-bracket.test.ts:49–80): calibration pinned strictly inside an owner-ratified envelope on every co-varied axis, with the producer defaults it moves off recorded so a producer change resurfaces loudly. It is the drift detector the MEDIA vocabulary never got (AP-7) — and even its stale `:123` comment sits one line from its own correction, which is what makes AP-8 legible as drift rather than confusion.
- **The T.W1 demo-dogfood keystone holds**: every value.js import in the 16-module closure is a real published subpath (`/color`, `/css` — reader-LC's census, spot-confirmed by this seat's exports read); `verbatimModuleSyntax` clean throughout; zero deep or bare imports.
- **The corpus's negative-proof discipline is worth preserving as method**: C recorded its own refuted a11y hypothesis (the aria-label/AX-value suppression guess, disproved by raw CDP read); L raised and killed two of its own suspects (`--tracking-caps` undefined-property; the `fmt()` regex); both readers each killed limbs of claims they otherwise confirmed. Seats that only report confirmed guesses are not measuring — these did.

---

## UNPROVEN-NEEDS-LIVE residue — the visual-audit inputs (task #4)

Each item's structural half is settled above; only the named quantity needs a live cell. Chromium-cell figures from the C/D probes are adopted as corroboration, never as cross-engine fact.

- **U-1 (AP-13)**: the label contrast ratios — light 3.89–3.94:1 / dark 3.50–3.63:1 — pixel-sampled under a live chromatic plate. Token divergence is static-certain; the ratios (and C's correction of pass-1's 6.12:1 dark figure) need the live matrix.
- **U-2 (pass-1 D-2)**: SelectContent bleed-through — 193,875 px² / α 0.7488 / α 0.52 / blur(11px). Qualitatively corroborated in D-menu-kb.png (slabs visible through the open menu, my read); the figures are live-only.
- **U-3 (AP-4)**: the byte-identical-PNG nulls for the five shape knobs on the css tier (with the two positive controls that make the nulls mean something). Mechanism proven statically; pixels live. Feeds i1's ΔE2000 oracle directly.
- **U-4 (AP-5)**: the 210/510 seed×harmony divergence rate and Δ≤44-rgb magnitudes.
- **U-5 (AP-6)**: the 90/1200 guarantee-miss and 9/120 spread-crush frequencies — both mechanisms proven by reading :96–109; only the rates are live.
- **U-6 (AP-12)**: `--control-h-md` resolving 60px under `--ui-scale: 1.5` on coarse pointers — chromium-cell measured; needs the Safari cells.
- **U-7 (AP-10)**: the 58.7px desktop / 50.1px mobile control-column spreads (mechanism static-certain; magnitudes live), plus D's 320px `min-w-menu` clamp arm (176/176/181.9/181.9).
- **U-8 (AP-24 second half)**: Analogous-vs-Monochrome strip discriminability at the F7 2.618rem chip width — "similar but distinguishable" in the tracked frame (my read); a live perceptual-delta measurement decides whether the demoted claim revives.
- **U-9 (C/C-14)**: whether the Slider root sets `inheritAttrs: false` — unreadable in the minified bundle; the dual `aria-label` rests on C's live strict-mode violation. ⇄CSP RELAY B-3 carries it.
- **U-10 (AP-1)**: the 60.2%-of-`<main>` card geometry (arithmetic checks; live rect confirmation rides the scene-contract witness).
- **U-11 (reader-LC's raised-and-mostly-killed gamut hypothesis)**: the strip paints `oklch()` while the css-tier field paints hex — in-gamut at the shipped default (reader-LC's linear-sRGB check), possibly divergent at extreme seeds. Not carried as a defect; one live probe at a high-chroma seed settles it permanently.

---

## DISSENT / method notes — preserved, not ruled away

1. **Register-blindness event (recorded as evidence about the fleet)**: both hostile readers confirmed L/P2-1's ADOPT-the-Configurator cure and neither consulted the standing `adjudicated/ConfigSliderPane.md`, whose C-9 rescope had already refuted that cure's state-machine half on the producer's own d.ts (preset-model, not defaults-model). The M-23 protocol assumes the challenge corpus is the whole prior; it is not — **standing adjudicated registers are part of the prior**, and this apotheosis's cross-bookings (⇄CSP ×5, ⇄W-HYGIENE ×1) exist because this seat checked.
2. **Anchor drift, minor but real**: reader-DU cited `usePaneRouter.ts:85` for the mount branch (it is :88) and the corpus cited "atoms.d.ts:155-167"/":160-176" for the union that lives at :159–171 in the installed dist. No verdict turned on any of them; recorded per the evidence-portability clause.
3. **Reader-LC's needs-live posture on D/C-1 and the 60px figure** was epistemically honest but under-credited evidence already in the corpus (D's live computed `font-style` read; the compiled-order fact). Preserved as the correct *default* posture wrongly applied to already-witnessed rows.
4. **The X-W8 CC-105 vs CSP G-PAINT tension** (AP-17): two live registers claim the `--slider-track-bg` sites — one BLOCKED-ON the 8.0.0 repin with receiving surface X-W4.g, one ruling the paint demo-curable now. This seat routes the identity to the CSP register (the later, measurement-backed ruling) and flags the reconciliation to the X-W0 sitting rather than silently electing either.
5. **What this seat could not witness**: no browser tooling was permitted; every live figure in this record is the workers'/challengers' cell, labeled so. The two frame reads are this seat's own eyes on tracked PNGs and are treated as image evidence, not as new measurements.

---

## Closing verdict

**DEFECTIVE — adjudicated.** One BLOCKER (the inverted binding composition, its cure re-aimed from a dead chassis to the X-W5 scene contract at X.W6.j), eighteen MAJOR (led by the strip/seam/oracle truth triad at X.W6.i and the substrate-honesty row), eleven MINOR, four INFO; thirteen corpus claims killed with refutations of record — two of them (the ternary, chromaCounterpoint) settled by this seat's own instruments against live disagreement, and two proposed coordination packets retracted before they could mail a producer about defects that do not exist. Primary owners: **X.W6.i** (atmosphere harness; riders: aurora-harmony-stops.ts + aurora-motion.test.ts bounds, the AP-8 stale-comment extension) and **X.W6.j** (scene adoption + row re-composition via the X-W4.b LabeledField mechanism); **X.W7.g** takes the voice/prose cluster; **⇄CSP** and **⇄W-HYGIENE** identities stay where they already live; **AP-19 and AP-34 go to the formation boundary as NO-WAVE-OWNER**. The component's best idea — a truth-bearing preview that costs nothing at rest — survives every one of its defects, which is precisely why the seam it lies across (AP-5/AP-6) is worth the repair.

— end of apotheosis. The challenger records and both hostile-reader returns remain immutable beside this file per E-1/E-3; dated evidence unrewritten.
