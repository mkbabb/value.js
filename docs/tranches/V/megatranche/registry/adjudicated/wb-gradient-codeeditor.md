# wb-gradient-codeeditor — adjudicated apotheosis (M-23/L-18 fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh-Fable adjudicator · **Date**: 2026-08-12 · **HEAD**: `9c779130` · branch `tranche-u`. `git diff c654824e -- GradientCodeEditor.vue src/css/grammar.ts` is EMPTY — every challenge-era read of the two load-bearing files holds byte-for-byte at today's HEAD.
**Corpus**: `docs/tranches/V/megatranche/audit/components/wb-gradient-codeeditor/` — D axis r1/r2/r3 (`challenge-D-design.md` = r3), C axis r1/r2/r3 (`challenge-C-implementation.md` = r3), L axis r2 + final (`challenge-L-library.md`); **the r2-governs rule honored** (AXIS-PROMOTIONS-2026-08-03): where rounds disagree, the later re-run's measurement governs and the earlier file stands as dated evidence.
**Workers adjudicated**: two hostile re-readers — design-and-usage · library-and-code — each returning confirmed/killed/missed/unproven/superlatives over the full challenge corpus.
**Method**: this seat re-derived every kill, every load-bearing confirmation, and every claimed miss with its own instruments before adopting anything. The two readers agree on every verdict-level disposition (no ruled disagreement existed); the adjudication work here was therefore *incredulity*, not tie-breaking — three reader claims were sharpened, one reader overstatement corrected, one arbiter instrument-error recorded as a method lesson (see DISSENT).
**Arbiter's own instruments this session (static + node against dist; zero browser probes — probe parsimony)**: full source reads of GradientCodeEditor.vue (all 117 lines), gradientParse.ts (all 301), grammar.ts:55-394, useGradientCSS.ts (five slices incl. :38-62/:145-206/:278-334), GradientVisualizer.vue:90-263, useGradientModel.ts:145-170, ErrorBoundary.vue, App.vue:5-142, shell.css:48-58, hljs.css:1-59, foundation.css (:1/:56/:81/:128/:143/:350-357/:695-720/:764-790), focus-ring.css:1-33, e2e/smoke/views/gradient.spec.ts:225-270, demo/shared/utils.ts:19-43, ColorInput.vue:190-220, useColorParsing.ts:55-90, ColorPicker.vue:278-295, src/value.ts:1-20, src/css/types.ts:20-30; **node against `dist/subpaths/css.js`**: the throw class (`oklch()` `rgb()` `hsl(  )` `rgb( / )` `calc()` `foo()` `min()` `steps()` `z-9()` all THREW `TypeError: Cannot read properties of undefined (reading 'replace')`; `oklch(` → ok:false; `parseCssValue("foo()")` → ok:false vs `parseCssScalar("foo()")` → THROWS; `parseStylesheet("a{color:oklch()}")` THREW vs `"a{width:foo()}"` ok:false) and the silent-accept family (`oklch(/ 0.7 0.1 145)` `rgb(/ 1 2 3)` `rgb(1 2 3 /)` `oklch(0.7 0.1 145 / )` `oklch(0.7 0.1 145/)` `rgb(,,,255 0 0,,,)` `rgb(1,2,3,)` **all ok:true**); **@vue/compiler-sfc compile of the live SFC** (registerLanguage lands INSIDE `setup()`, line 18 of emitted output — the K1 decider); installed-producer reads: glass-ui `ladder.css` (`@layer components`, `.glass-wash` incl. `position:relative`), `material.css` (`:where(.glass-wash …) > * { --glass-cell-backdrop-filter: none }`), `a11y-fallback.css` (forced-colors: `.glass-wash { border: 1px solid CanvasText }` AND `::before/::after { display:none }`), `control-surfaces.css` **read whole** (the `.input-pill` base rule with five `var(--duration-fast)` transitions is present verbatim), `_shared/field-control.css` (`min-block-size: max(5lh, calc(5rem * var(--ui-scale)))`, `max-block-size: var(--textarea-content-max, 12lh)`), `dist/useClipboard-D36OTaeT.js` **read whole** (writeClipboard returns `{ok,reason}`, never throws; `useClipboard` ships the full idle/pending/success/failure FSM + `onCopyError`); @vueuse/core 14.3.0 `shared/dist/index.js` (debounceFilter region :320-357 holds bare timers, zero `tryOnScopeDispose`); repo censuses: `useDebounceFn` in demo = **0**, `focus-visible:ring-ring/40` ×22 + `ring-ring` ×3 = 25 colour occurrences / 12 files, `--color-ring` defined NOWHERE (1 prose hit focus-ring.css:11; glass-ui dist 0), `--focus-ring-inner` consumers = GradientStopEditor.vue only (App.vue:201 is a comment), contenteditable hosts = exactly 3, `rounded-lg` = exactly 3 files, `demo/ui` = 19 dirs, `@src` in demo = 0, `cn(` = 1 (its definition), package.json#exports = 7 keys no `.` root, PaneSlot.vue:120 `<KeepAlive :max>` / App.vue:88 `:max="9"`, motion-quarantine.md rows naming this component = **0**.
**Motion/PRM law**: no motion-quarantine M-xx row touches this component; D2-4 is a duration-*token* misuse (0.3s where the producer's field rung and `--default-transition-duration` both say 0.2s), not a motion/PRM assertion — nothing FALSE-rowed is carried.

## Scoreboard

**28 adjudicated defect identities (3 BLOCKER-weight · 15 MAJOR · 9 MINOR · 1 INFO) · 13 killed claims RATIFIED (0 kills overturned) · 13 reader-filed misses RATIFIED (+2 arbiter sharpenings) · 14 UNPROVEN-NEEDS-LIVE residue rows.**

Blocker-weight survivors: **A-1** (a partial parser under a total published type — one mid-thought keystroke on the shipped default evicts the whole workspace), **A-2** (the same two library lines silently ACCEPT a family of invalid CSS — the throw's mirror, which the standing cure #1 does not touch), **A-3** (the declared glass plate paints nothing; the certified crayon-contrast guard does not apply at this site).

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Line anchors are HEAD `9c779130`; re-resolve before executing any cure (D-19; see also A-28). Routing follows CARRY-CUT-LEDGER's X-map (X-W6 = the Gradient-pilot temporal-scene wave; X-W9 = parser/library apotheosis; X-W10 = canonical design law; X-W5 = route/scene shell; X-W4 = semantic-controls remainder; X-W1 = re-gate; X-W8 = graph subtraction; X-W7 = palette/CRUD ownership).

- **A-1 — BLOCKER · CONFIRMED (ids D-1 / D-r2 §3.1 / C-R15 / L-1 + reader-2 miss M-A).** `parseCssColor` is declared total (`ParseResult`, src/css/types.ts:25-27 — no throw channel) and throws on the closed empty-call class; **the class is `<any-ident>()`, not `<color-function>()`** — grammar.ts:279 matches `^([a-z][\w-]*)\(` for ANY ident before dispatching :181 `slash[0]!.replace(...)`, where splitTopLevel (:79/:85) has dropped every empty part. Re-proven by this seat against dist: `oklch()` AND `calc()` AND `foo()` all throw the same TypeError from `parseCssColor` DIRECTLY — so the throw reaches this component through BOTH oracles (isColorToken at gradientParse.ts:93; angleToDegrees/percentValue via `parseCssScalar` at :98/:112), and `red calc(50% - 2px)` is ordinary CSS. The surface is internally inconsistent: `parseCssValue("foo()")` → ok:false while `parseCssScalar("foo()")` throws (parseScalarInternal:316 consults the colour parser first); `parseStylesheet("a{color:oklch()}")` throws while `"a{width:foo()}"` returns ok:false — all re-measured this session. Escape: GradientCodeEditor.vue:55-61 debounced emit → GradientVisualizer.vue:102-108 `onParseCSS` (no try) → App.vue:50-140's ONLY ErrorBoundary wraps BOTH panes; ErrorBoundary.vue:68 `return false` silences the console. **→ X-W9** (library totality — the type becomes true), with the demo-side blast-radius halves booked at A-8 (debounce lifecycle, X-W6) and A-14 (boundary, X-W5). Cure lock: totality alone is INSUFFICIENT — see A-2.
- **A-2 — BLOCKER · CONFIRMED (reader-2 miss, ratified; no prior corpus id).** The SAME two lines (grammar.ts:79/:85 empty-drop) produce silent WRONG ACCEPTS in the opposite direction — re-measured this session, all invalid CSS, all `ok:true`: `oklch(/ 0.7 0.1 145)`, `rgb(/ 1 2 3)`, `rgb(1 2 3 /)`, `oklch(0.7 0.1 145 / )`, `oklch(0.7 0.1 145/)`, `rgb(,,,255 0 0,,,)`, `rgb(1,2,3,)`. Slash POSITION and comma ARITY are destroyed before :177-181 ever sees the body. L-1's cure #1 (guard the index) stops the crash and leaves every accept standing; a "validity oracle" (gradientParse.ts:16) that says yes to `rgb(1 2 3 /)` is worse than one that throws, because nothing downstream can detect it. **→ X-W9**, gated in BOTH directions (throw class → ok:false AND accept family → ok:false); cure #1 alone may not ship. Note the demo's own `splitTopLevelCommas` (gradientParse.ts:52-67 keeps empties, :210-211 rejects them) is the correct primitive — see superlative S-2.
- **A-3 — BLOCKER · CONFIRMED (ids D-2 / D-r2 §3.2 / L-3).** The component's declared `glass-wash` plate paints NOTHING: hljs.css:56-59 `.hljs { background: transparent }` is UNLAYERED (0 `@layer` in the file, imported at foundation.css:81) and glass-ui's plate lives in `@layer components` (ladder.css:1, read this session) — per Cascade 5, unlayered beats every layer at equal specificity (0,1,0 each). The crayons were certified against the rung-2 well (hljs.css:21-33 "all ≥4.5") and that well is not this surface — the ink sits on the raw seed-driven ambient field. The inversion is real: under `prefers-reduced-transparency` the plate DOES paint (foundation.css:786-789, unlayered `!important`). Kill-ratifications folded in: the `bg-well`-on-the-same-element cure is structurally defeated by the same layer fact (utilities layer < unlayered; the sibling "proves" the rung only because its `bg-well` rides the `.readout-rail` WRAPPER, GradientEasingEditor.vue:176 — read this session), the backdrop-filter sub-claim carries no weight (producer's `:where(...) > * { --glass-cell-backdrop-filter: none }` is deliberate nested-cell policy, verified in material.css), and the seed-tint clause self-refutes (a transparent box shows the ambient field, the clause's first named exemption). **→ X-W10** (the unlayered-vs-layered cascade law: layer `hljs.css` or re-home every code plate onto a parent; `.console-well` at foundation.css:352-356 wins by source order and is a legal ground) **+ X-W6** (this component's wrapper — the same wrapper A-21 needs). Cure lock: any green must be a PAINT measurement, never a class-list grep.
- **A-4 — MAJOR · CONFIRMED (ids M-1 ⊃ D2-1/C-R19 + C-R20 — one defect seen from three ends).** The editable readout is UNFAITHFUL: `serializeGradient` (useGradientCSS.ts:147-162, read this session) emits neither `in <space>` nor the hue method, so the displayed default `linear-gradient(90deg, oklch(…) 0%, oklch(…) 100%)` is a gradient any browser interpolates in sRGB while the pane renders the OKLCH ramp; the Copy control writes a DIFFERENT artifact (GradientVisualizer.vue:254/:127-129 `coalescedCSS` — 33 resampled stops, `Number(value.toFixed(12))` floats via grammar.ts:285) with no label or toggle; and the pane's own parser REJECTS the L4 syntax that would express the missing parameter (`in oklch` → gradientParse.ts:128-154 preamble fall-through → :236 `unparseable color "in"`; `90deg in oklch` mis-names `90deg` — both re-derived from source this session). The killed short-circuit cure stands killed: coalescing bakes the interpolation SPACE (sampleCoalescedStops :175-206 runs `parseColorIn`/`mixColors` in `interpolationSpace`/`hueMethod` — verified), so returning `simpleCSS` for all-linear intervals would silently downgrade every non-sRGB export, the default state. The correct cure is the one M-1 determines: serialize `in <space>`/hue into `simpleCSS`, teach the parser to eat it, and the display/copy divergence collapses on its own. **→ X-W6.**
- **A-5 — MAJOR · CONFIRMED (ids D3-1 / C-R17 + C-R10).** A byte-identical no-op edit destroys every authored easing: onInput (:59-62) has no dirty check, onParseCSS applies every emit, applyCSS (useGradientModel.ts:158-167, read this session) swaps `intervals` wholesale, and gradientParse.ts:295-298 seeds EVERY interval `linearInterval()`. The same settled parse also mints fresh stop ids (:252/:256 `uid()`), so any identity held on a stop silently drops. No commit affordance exists in the CSS region (GradientVisualizer.vue:250-262). **→ X-W6** (dirty-check + easing/id reconciliation, or an explicit apply gate — on a TRUE ground, see A-3's cure lock).
- **A-6 — MAJOR · CONFIRMED (ids C-R4 + D-5 + D-6).** The verdict lifecycle is broken in all three arms: `parseVerdict` is written ONLY at GradientVisualizer.vue:107 and :132 (verified — the Direction slider :232-233, Space :181 and Hue :198 Selects never touch it), so after a rejection any other-path model change rewrites the editor with valid CSS while `aria-invalid` + `border-destructive` + the stale reason persist; there is no pending arm (`string | null` — stale truth impersonates current truth for every 500 ms window, both directions); and the emptied field is rendered INVALID in spec meta-notation (`parseGradientCSS("")` → :196 `not a <type>-gradient(…) function`) with the trap closed by :72's `if (!hasError.value)` — the emptied buffer stays red indefinitely. **→ X-W6.**
- **A-7 — MAJOR · CONFIRMED (ids D-r3 §3 / D-3 / C-R7 — clipping; C-R5 — newline fusion).** The verdict `<p>` (:107-114) is the last child of a column inside GradientPane.vue:20's `overflow-y-auto` Card with nothing calling scrollIntoView — announced outside the visible scrollport (pixel geometry live-only, structure source-certain). Separately, :60 reads `textContent`, which fuses the line breaks the write path preserves (:52 `innerHTML` + `whitespace-pre-wrap`), so a mid-token Enter concatenates two lines into a different-but-valid literal — write/read asymmetry confirmed from source. **→ X-W6.**
- **A-8 — MAJOR · CONFIRMED (ids C-R18 / L-16, mechanism per kills K2/K3 — both ratified).** The 500 ms timer is never cancelled (no `onUnmounted`/`onDeactivated`/`.cancel()` in the file — all 117 lines read) and BOTH standing cures are wrong: `onBeforeUnmount(cancel)` cannot fire on navigation (PaneSlot.vue:120 `<KeepAlive :max>` DEACTIVATES, App.vue:88 `:max="9"` right-sized so eviction never happens), and `useDebounceFn` neither exists in the demo (my census: 0) nor cancels on scope dispose (vueuse 14.3.0 debounceFilter :320-357 — bare timers, zero `tryOnScopeDispose`, verified). The observed cross-route damage is real for the OPPOSITE reason filed: everything stays mounted, so the stale emit lands in the still-wired App-root boundary over whatever pane the user now sees. Correct hook: `onDeactivated` + `onBeforeUnmount`, or kill the shared-timer shape entirely in the rebuild. The discipline exists one directory away (ColorPicker.vue:383 calls `.cancel()`). **→ X-W6.**
- **A-9 — MAJOR · CONFIRMED (ids D-7 / D2-6 / C-R6 / L-4 / L-15, with kill K6's rhetoric correction; absorbs D3-3, D2-4, D-8).** A hand-rolled text-entry control wearing the CONTAINER ladder: glass-ui ships the field contract twice over — `_shared/field-control.css` (`[data-kind="textarea"]`: `max(5lh, calc(5rem * var(--ui-scale)))` / `var(--textarea-content-max, 12lh)` — NOT the component's flat `5rem`/`12rem`, K6 ratified from my own read) and `control-surfaces.css` `.input-pill` (present VERBATIM with the `[aria-invalid="true"]` destructive arm the component's :92 already feeds, and five `var(--duration-fast)` transitions — my whole-file read; see DISSENT for the instrument lesson). The component instead layers Tailwind `border-destructive`/`border-border/40` over the producer's border (utilities beat @layer components — the wash rung never renders), paints the press-lit specular `::before` OVER the code glyphs (material.css `z-index:1`, `plus-lighter`, hover/active brightening — D3-3), authors 0.3 s `--duration-normal` inline where the producer's field rung and foundation.css:128's `--default-transition-duration: var(--duration-fast)` both say 0.2 s (D2-4; the r2 citation nit stands: the rule is in control-surfaces.css, 0 hits in dist/glass-ui.css — both re-checked), and reaches `rounded-lg` where DESIGN.md:195 names `rounded-input` for text inputs (D-8; census = exactly 3 files, mine). The contenteditable census is 3 hosts (mine). **→ X-W4** (semantic-controls remainder: adopt the producer field contract) **+ glass-ui BH relay** (owner edict — the two adoption blockers are producer asks: `--radius-pill` hard-pin / native-control selector, i.e. an `asChild`-shaped arm + a radius token; cite this record).
- **A-10 — MAJOR · CONFIRMED (ids D2-2 / L-14 / reader-1 miss M-2 + kill K7 — all ratified, re-censused).** `--color-ring` is defined NOWHERE (my grep: demo 1 prose hit, glass-ui dist 0), so `focus-visible:ring-ring/40` paints opaque `currentcolor` and Tailwind's ring `box-shadow` REPLACES the glass material stack rather than composing (utilities > components). The mis-scoping is the finding: 25 colour-token occurrences across 12 files (my census; K7's line-vs-occurrence correction ratified — `ring-2` is a width utility and not downgraded), and exactly ONE file consumes the certified `--focus-ring-inner/-outer` recipe (GradientStopEditor.vue — App.vue:201 is a comment, checked). This is the application's DOMINANT focus idiom, silently degraded house-wide; focus-ring.css:9-15 documents this exact death as U-F25 and cures it only for its own consumers. **→ X-W10** (root token mint or house-wide swap — a per-component recompose leaves 24 sites broken); this component's own recompose rides A-9's rebuild.
- **A-11 — MAJOR · CONFIRMED (id D3-2; WHCM-narrowing kill ratified).** foundation.css:698-719's forced-colors restoration roster (read this session) contains neither `[role="textbox"]` nor `[contenteditable]`; the element carries no `tabindex` and `outline-none` kills the UA default — it falls through every arm while its sibling `.readout-rail` is in the tier-1 roster. The kill of r3 §5(1) stands and runs FURTHER my way: glass-ui a11y-fallback.css forces `.glass-wash { border: 1px solid CanvasText }` under forced-colors (my read), so the resting and destructive borders are plausibly INDISTINGUISHABLE in real WHCM — and the same block sets `::before/::after { display:none }`, so A-9's specular is already suppressed there. Paint truth in either direction is UNPROVEN-NEEDS-LIVE (real Windows HCM; the WebKit emulation demonstrably did not apply colour substitution). **→ X-W10** (roster arm + the WHCM cell), selector fix itself is one line.
- **A-12 — MAJOR · CONFIRMED (ids D-4 / C-R23 as re-framed by the ratified D-4-FRAMING kill + reader-2 miss #4 + D-13/C-R11 + L-5).** The highlighting LIES while the editor is used (spans persist; new characters inherit the neighbouring token's crayon — r2's measurement governs; "dead while focused" was the select-all artifact), is never restored after a rejection (:72 gates the blur re-render on `!hasError`, and the :76-79 watcher fires only on model change — so the error state, where reading broken syntax matters most, keeps raw text indefinitely), and its fallback is an unreachable masking `catch` feeding `innerHTML` (:43-49; language registered at :8). The vendored highlighter (dedicated `vendor-highlight` chunk, deferred pane — the mitigations are real) is thereby shipped for a surface that is unhighlighted or wrong exactly when used. **→ X-W6** (the overlay idiom cures both the lying spans and the truce violation in one shape; the gz number stays UNPROVEN-NEEDS-BUILD).
- **A-13 — MAJOR · CONFIRMED (ids C-R21 + C-R22).** Unbounded ingress into a bounded control: `100turn` → 36000 deg with no clamp anywhere on gradientParse.ts:96-108 → :218-220 → useGradientModel `direction.value` → `<Slider :min="0" :max="360">` (GradientVisualizer.vue:232-233 — all read this session). And any radial/conic parse writes `direction = 0` over the user's setting (:214), the linear no-preamble arm writes 180 — silent resets with no verdict. Rendered ARIA state live-only. **→ X-W6.**
- **A-14 — MAJOR · CONFIRMED (ids L-17 + C-R16/D-14 + D3-4).** The boundary the crash lands in: (a) claims "This panel" while wrapping BOTH panes (App.vue:50-140, string verified); (b) is sticky — `caught` clears only via the button, no route watcher (ErrorBoundary.vue read whole); (c) paints, per the structurally-sound stacking derivation, only the glass Button — `.atmosphere-canvas` is `absolute inset-0` z-auto (App.vue:11), `.pane-main` sets no position (shell.css:50-57), the fallback is an unpositioned in-flow block, and glass surfaces carry `position:relative` (ladder.css, my read) — non-paint itself UNPROVEN-NEEDS-LIVE; (d) `reset()` remounts the subtree from seeds — the KeepAlive cache dies with it, so "Try again" restores the seed and destroys the user's work, falsifying the component's own :38-40 contract. The component itself is exemplary a11y (S-6); its placement and paint order are the defects. **→ X-W5** (per-pane boundaries + positioned fallback + route-reset arm).
- **A-15 — MAJOR · CONFIRMED (ids reader-1 miss M-3 + C-R24).** The gate over the pane's loudest defect is vacuous: gradient.spec.ts:233/:262 assert `toBeVisible` (my read), which passes for any non-empty box regardless of viewport position and does not auto-scroll — so the clipped verdict (A-7) has sat under a GREEN gate for four audit rounds. C-R24's mutation claim (delete `if (!hasError.value)` → all green) is statically supported (no test covers the empty-call class or the blur gate; test/v4-css-public.test.ts asserts round-trips only) but was never run — UNPROVEN as a run. **→ X-W1** (re-gate: in-viewport assertions, the one-time falsifier discipline, and A-1/A-2's born-RED dist oracles).
- **A-16 — MAJOR · CONFIRMED (id L-2 + kill K5 ratified).** The gradient grammar is library-shaped code living in the demo (301 + 334 lines; `grep gradient src/` = 0) and its justifying docblock premise is FALSE (the library's tree DOES distinguish `red 30%, blue` from `red, 30%, blue` — reader-2's dist measurement, shapes confirmed by my type read); the REAL library gap is empty-segment arity (`red,,blue` → args=2, indistinguishable). K5 stands on my own read of src/value.ts:3-20: `CssScalar`/`CssCall` carry no source text or spans, so the prescribed thin-projection cure would DELETE the authored-literal feature (gradientParse.ts:18, :252/:256) unless the library first ships per-node spans — an ask no filed cure makes. **→ X-W9** (empty-segment fidelity + the spans decision at the library boundary); the demo projection re-homes only after that lands (X-W6 rider, sequenced).
- **A-17 — MINOR · CONFIRMED (ids D-10 + reader-1 miss M-5, SHARPENED by this seat).** The Copy control sits 40 px from the field it acts on while the same pane's rail does the identical verb inside the rail with a `Check`-swap confirmation (GradientEasingEditor.vue:176-188, read). Sharpening from my producer read: `writeClipboard` RETURNS `{ok:false, reason:"no-api"|"clipboard-api"}` and never throws — GradientVisualizer.vue:127-129 `await`s and DISCARDS the result; and the SAME producer chunk ships `useClipboard` with the full status FSM + `onCopyError`. This is not a missing try — it is a discarded result contract with the correct affordance one import away. **→ X-W6.**
- **A-18 — MINOR · CONFIRMED (reader-1 miss M-6).** Five of fourteen rejection strings speak the implementation ("isn't modeled"); the most-hit string (empty field, :196) is the one with neither plain language nor a remedy. The voice is the component's entire prose surface. **→ X-W6** (copy pass riding A-6's lifecycle rebuild; the model-or-reject DISCIPLINE itself is a superlative and must survive, S-1).
- **A-19 — MINOR · CONFIRMED (ids D3-6 / C-R13).** `spellcheck="false"` and nothing else — no `autocapitalize`/`autocorrect`/`inputmode`/`plaintext-only`/`@paste` (attribute set read; repo grep 0). Corruption consequence stays UNPROVEN-NEEDS-DEVICE. **→ X-W6.**
- **A-20 — MINOR · CONFIRMED (ids C-R9 + C-R12 + kill K1's surviving residue).** `modelValue` with no `update:modelValue` emit (:16-29 — v-model compiles and does nothing); `innerHTML` reassignment on every blurred model tick destroys native undo and selection (:52 + :76-79); and `hljs.registerLanguage` runs at SETUP time per instantiation (my compile — K1's import-time/sideEffects contradiction is FALSE and stays killed; the ownership nit is what survives). **→ X-W6** (all three die in the rebuild).
- **A-21 — MINOR · CONFIRMED (reader-1 miss M-4).** `glass-wash` and `overflow-y-auto` share one element; the producer's overlays are `absolute inset-0` children (material/grain, my read), so in the overflow state the specular, grain and inset hairline scroll OUT of the scrollport with the content. The material contract assumes a non-scrolling host. **→ X-W6** (the wrapper — same wrapper as A-3's) **+ glass-ui BH relay** (document or harden the non-scrolling-host assumption; owner edict, cite this record).
- **A-22 — MINOR · CONFIRMED (id D2-5).** No `dir`/`unicode-bidi` isolation on a CSS-string surface; VISUAL-CONSTITUTION.md:154's LTR-isolation row applies. **→ X-W6** (one attribute in the rebuild).
- **A-23 — MINOR · CONFIRMED-AS-EDGE (id L-6).** `demo/ui/` = 19 dirs / 19 one-line re-export `index.ts` files; this component's parent consumes through it (GradientVisualizer.vue:9-10). **IDENTITY GUARD (anti-rename)**: this is the W-HYGIENE demo/ui extirpation row (ColorPicker adjudication; restated at ColorSpaceSelector L-4) — ONE identity, already folded. **→ X-W8**, this record adds only the two consumer edges.
- **A-24 — MINOR · CONFIRMED (id L-7).** `@src` alias + `sourceExportPlugin()` + the justifying comment are all dead (my grep: 0 demo hits; the only `?source` hits are the plugin and the comment). **→ X-W8.**
- **A-25 — MINOR · CONFIRMED (id L-8 + reader-2 miss #5).** utils.ts's docblock claim is false at HEAD (no `.` export exists — my node read: 7 subpath keys, main undefined); `cn` has exactly ONE occurrence repo-wide (its definition — my census), so the `clsx`+`tailwind-merge` deletion condition is DISCHARGED, not conditional. **→ X-W8.**
- **A-26 — MINOR · CONFIRMED-AS-IDENTITY (id L-13).** tsconfig.demo.json#paths: 8 hand-written keys vs 7 shipped exports, 3 phantom dist targets, and `/css` — this component's own import specifier (gradientParse.ts:21) — absent. **IDENTITY GUARD**: fact-identical to the ColorSpaceSelector adjudication's L-14 (W-HYGIENE exports↔tsconfig parity, born-RED generator command already booked) — ONE identity, not re-booked. **→ X-W8** where that row rides.
- **A-27 — MAJOR · CONFIRMED (reader-2 miss #3, re-verified by this seat).** `parsePickerColor` throws on ANY rejection (picker-color.ts:109-113) and has six call sites with five guard postures; the sixth — ColorPicker.vue:282-289 `onStartEdit`, a click on a saved-palette swatch feeding store/API-supplied `originalCss` — is UNGUARDED (my read: no try). A single non-canonical stored literal evicts the workspace with no keystroke. Five-of-six is a convention, not an invariant — the same criticism L-16 levels at the missing `.cancel()`. **→ X-W7** (palette/CRUD ingress ownership); NOTE: A-1's X-W9 totality SHRINKS but does not close this (PickerColorError on ok:false remains). 
- **A-28 — INFO · CONFIRMED (reader-1 miss M-8, spot-checked).** D-r1's script-block anchors drift −2..−4 against a byte-identical file (r1:161 cites :63-65 for the emit that lives at :55-57; r1:291 cites :57-60 for onInput at :59-62 — both re-read this session); template anchors are exact; r2/r3 exact. **NO-WAVE-OWNER** — registry method note: any wave navigating this corpus re-resolves r1 anchors before editing (D-19 restated).

## Killed-claims register — 13 rows, all RATIFIED (0 overturned)

Design-and-usage reader's six: **(K-D1)** D-r1 §2.3 palette-contract clause — self-refuting against its own D-2 (a transparent box shows the ambient field, the clause's first exemption); r2:488-490 reached the correct negative independently. **(K-D2)** D-2's backdrop-filter prop — producer nested-cell policy (`:where(...) > * { --glass-cell-backdrop-filter: none }`, my read of material.css); the background-nulling core is untouched. **(K-D3)** D-r3 §5(1) WHCM narrowing — the run's own un-stripped box-shadow proves the emulation applied no colour substitution; my a11y-fallback read (border→CanvasText on every glass rung) points the OTHER way; disposition UNPROVEN-NEEDS-LIVE (A-11). **(K-D4)** D2-1's identity-easing sub-claim + short-circuit cure — coalescing bakes the interpolation space (my read of sampleCoalescedStops); the cure would silently downgrade every non-sRGB export; M-1/A-4 is the correct frame. **(K-D5)** the `bg-well`-on-the-element cure — defeated by the exact unlayered-beats-utilities fact the same corpus diagnoses; the sibling's rung rides a WRAPPER (my read of GradientEasingEditor.vue:176); any cure must re-parent the plate or layer `hljs.css`. **(K-D6)** D-4-FRAMING — the r2-governs rule decides it: spans persist and LIE (inherited crayon), they do not die; severity argument re-seated on the true behaviour (A-12).

Library-and-code reader's seven: **(K1)** L-11's import-time/sideEffects contradiction — FALSE; my own `@vue/compiler-sfc` compile puts `registerLanguage` inside `setup()` (emitted line 18); the per-instantiation ownership nit survives at A-20. **(K2)** L-9/L-16's unmount mechanism — KeepAlive DEACTIVATES (PaneSlot.vue:120, my read); everything stays mounted and the boundary catches; determines the correct hook. **(K3)** the `useDebounceFn` cure — both grounds false by my own instruments (demo census 0; vueuse 14.3.0 debounceFilter holds bare timers, no scope registration); would ship green and fix nothing. **(K4)** L-1's only-ingress premise — ColorInput.vue feeds the same oracle every keystroke and survives only by a bare-swallow catch (useColorParsing.ts:83-88, my read) — the masking fallback L-1 forbids is already shipping one directory away; the inconsistency, not uniqueness, makes this component the crash site. **(K5)** L-2's thin-projection cure — authored literals are unrecoverable from a span-less `CssValue` tree (my read of src/value.ts); repairable only via a per-node-spans ask no cure makes (A-16). **(K6)** L-4's "identical constants" rhetoric — `max(5lh, calc(5rem*var(--ui-scale)))`/`12lh` vs flat `5rem`/`12rem` (my read of field-control.css); duplication survives, byte-identity does not. **(K7)** L-14's 27 count — line-count posing as usage count mixing a width utility; true figure 25 colour occurrences / 12 files (my census; A-10 books the corrected number).

## Superlatives — provenance preserved

- **S-1** gradientParse.ts's model-or-reject discipline: named-token rejections, remedy clauses, "Silent-drop is forbidden (P2-17)" honored line-for-line (my full read); e2e gates the radial refusal. Survives every rebuild.
- **S-2** The demo's `splitTopLevelCommas` keeps empty segments and rejects them explicitly — MORE correct than the library primitive it is accused of duplicating, and the exact fidelity whose absence produces A-1 AND A-2. The library should adopt the demo's semantics, not vice versa.
- **S-3** The editor truce (:34-40) is the right architecture for a controlled contenteditable, documented with rationale, implemented faithfully; every failure here is of the contract AROUND it. All three axes + both readers agree independently.
- **S-4** `linearInterval()` byte-matches glass-ui's `<EasingPicker>` linear seed by stated design (useGradientCSS.ts:44-61, my read) — a cross-producer agreement that deletes a phantom-diff bug class by construction.
- **S-5** `applyCSS` is genuinely atomic with its replaced pathology named in the docblock (useGradientModel.ts:151-157, my read) and a regression witness in e2e (the Easing heading stands after rejection).
- **S-6** ErrorBoundary.vue is exemplary a11y with WCAG clauses cited in source (role=alert/assertive, focus management, real Button, `--ink-muted` detail rung); only its placement and paint order are defects (A-14).
- **S-7** demo/shared/utils.ts's debounce is correct and precisely documented; the defect is call-site discipline (2 of 9 consumers honor `.cancel()`).
- **S-8** hljs.css:1-34 publishes its own contrast referent — the only reason A-3 is provable. A guard that names its ground is falsifiable; that is the standard.
- **S-9** The corpus's best work: L-3's retraction-with-a-better-mechanism plus r2's same-frame control experiment (a bare `.glass-wash` sibling that painted while the editor didn't) — the strongest single piece of evidence in the corpus, and it retires SYNTHESIS.md:121's Chromium-collapse explanation of record.
- **S-10** vite.config.ts:37-50's generated export-map aliases — the reason the demo is a real proof of the public API; its hand-written tsconfig twin is what rotted (A-26). The strongest argument for the generated form.
- **S-11** Minimal-import discipline on the highlighter (lib/core + one language, dedicated chunk, async pane) — three correct decisions on a dependency that is nonetheless the wrong dependency (A-12).

## UNPROVEN-NEEDS-LIVE residue — the visual-audit inputs

1. D3-5 verdict-vs-specimen font size (16.4 px vs 14 px) — both elements share `text-mono-small`/one token by my static read; two seats report the anomaly; if real, the cause is outside the component. Measure live at 1440 and ≤480.
2. A-3's delivered contrast matrix — conclusion (every crayon below 4.5 on the live field) robust across two disagreeing measurement sets; no individual ratio is durable; measure per-scheme on the live seed ground, never gate on a stored ratio.
3. Verdict-clip pixel rectangles (desktop/mobile) — structure source-certain (A-7); geometry live-only.
4. `break-all` exact severance columns — mechanism definitional; columns live-only.
5. C-R21's rendered ARIA state (`aria-valuenow` beyond `aria-valuemax`) — the absent clamp is source-certain; what glass-ui's Slider writes is live-only.
6. Forced-colors paint truth in EITHER direction — needs real Windows HCM (K-D3); includes my a11y-fallback derived expectation that resting/error borders converge on CanvasText.
7. Native-undo destruction key sequence — mechanism certain, sequence undriven.
8. iOS autocapitalize/smart-substitution corruption — attributes absent (measured); corruption needs a device.
9. L-5's 12,670 B gz — UNPROVEN-NEEDS-BUILD (the r3-era dist stub is on record).
10. L-3's computed-style pixel values (transparent editor / reduced-transparency `rgb(253,245,236)`) — mechanism source-confirmed; pixels live-only.
11. L-14's resolved ring paint (`currentcolor` 2px) — token absence source-confirmed; Tailwind runtime fallback unmeasured this session.
12. Boundary non-paint of icon/message/detail (A-14c) — stacking derivation sound; paint live-only.
13. L-16's cross-route reproduction (Gradient keystroke → Mix fallback) — code path fully consistent (via K2's corrected mechanism); unobserved this session.
14. C-R24's mutation run (delete the :72 gate, suite stays green) — statically supported, never executed (source edits forbidden to every audit seat).

## DISSENT / method notes — preserved, not ruled away

1. **Arbiter instrument-error, recorded as a lesson (the K7 species, in this seat's own hands).** My first `grep -c "input-pill"` against the producer's ONE-LINE minified control-surfaces.css returned 1 (grep counts LINES) and nearly manufactured a false kill of L-15's verbatim quote; a whole-file read showed the rule present exactly as cited. On minified single-line sources, occurrence censuses must use `grep -o | wc -l` or a whole-file read. This is the same line-vs-occurrence confusion K7 kills — the trap catches adjudicators too.
2. **Sharpening, not kill — reader-2's M-A mechanism note.** M-A frames the any-ident throw as reaching the component "through `parseCssScalar`, the SECOND oracle." True and proven — but the sharper fact (my probe) is that `parseCssColor` ITSELF throws on `calc()`/`foo()` (grammar.ts:279 dispatches any ident), so the FIRST oracle (`isColorToken`) is equally live for the extended class. The class statement in A-1 carries the correction.
3. **Sharpening — reader-1's M-5.** "No failure path" understates the producer: `writeClipboard` returns a result object and `useClipboard` ships the status FSM in the same chunk (my read). The defect is a discarded contract, which makes the cure smaller than filed (A-17).
4. **Reader agreement disclosure.** The two readers disagreed on NO verdict-level disposition, which under the suspicious-agreement law is itself a datum. This seat treated agreement as untrusted and re-derived: 13/13 kills ratified on independent instruments, 13/13 misses ratified, 3 sharpenings and 1 count correction (A-10's file count: 12, both readers consistent) applied. The predicted citation-inheritance species (stale-cell repetition) was probed for — r1's anchor drift (A-28) is drift, not inheritance; no inheritance event was found between the two readers.
5. **Severity ordering, unresolved and preserved.** A case exists for ranking A-4 (the unfaithful artifact) at blocker weight: it is the pane's core promise (an editable, copyable truth) failing silently in the default state. This seat keeps it MAJOR because no work is destroyed and no false science is asserted — but the next formation may re-rank; the routing does not change.

## Closing verdict

**ADJUDICATED — RED, three blocker-weight identities.** The library publishes a total type over a partial parser whose one guard-line failure both throws (`<any-ident>()` — one mid-thought keystroke evicts the entire workspace through a silenced boundary) and silently accepts invalid CSS (the mirror family no filed cure touches); the component's declared plate is a cascade-layer casualty that voids its certified contrast guard; and around them stand fifteen majors — an unfaithful display/copy artifact whose own parser rejects the syntax that would make it honest, a no-op edit that destroys authored work, a verdict lifecycle wrong in all three arms, a dominant focus idiom that is empty house-wide, and a green gate structurally incapable of seeing the pane's loudest failure. The roster routes: X-W9 (A-1, A-2, A-16), X-W6 (A-4..A-8, A-12, A-13, A-17..A-22 — the Gradient-pilot rebuild), X-W10 (A-3, A-10, A-11), X-W5 (A-14), X-W4+relay (A-9), X-W1 (A-15), X-W8 (A-23..A-26, two identity-folds), X-W7 (A-27), one NO-WAVE-OWNER method note (A-28). Thirteen kills ratified, zero overturned; the truce, the model-or-reject discipline, and the demo's safer splitter are the three things every cure must leave standing. Dispositions are ADJUDICATED, not VERIFIED — no wave is stamped by this record.

— end of apotheosis. The challenger corpus and both reader returns remain immutable beside this file per E-1/E-3.
