claude-fable-5

# kf-CopyButton — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: fresh-Fable adjudicator · model receipt: `claude-fable-5` (Fable 5), read from this seat's own system prompt, not inherited.
**Date**: 2026-08-07 · **Registry tree**: value.js `tranche-u`.
**Corpus adjudicated (r2)**: two hostile re-reader returns over the full three-axis challenge corpus for `demo/components/CopyButton.vue` (113 L, 4 mount sites) — reader-1 ("design-and-usage": 27 confirmation cells · 6 kills · 9 misses · 8 superlatives) and reader-2 ("library-and-code": 19 confirmation cells · 10 kills · 9 misses · 7 superlatives). All three challenge files re-read **whole** by this seat (`audit/kf-components/CopyButton/challenge-{D-design,L-library,C-consumption}.md`, 411/262/484 L, all opus-5[1m]).
**Method (incredulity)**: every cross-reader disagreement and every kill of a load-bearing cell was re-derived against the bytes with this seat's own instruments before adoption. The head-on contradictions (.tap-floor adopters, focus-ring count, the F-1/HEAD lock state, the S-3 superlative, the D-19 forced-colors arm) were decided by fresh extraction, not by vote. **Zero browser tooling** (law); all rulings static + installed-dist + git reads; the irreducibly visual residue rides to SS-13. Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input.
**This seat's own instruments (all read-only)**: full read of CopyButton.vue (whole, 113 — every cited anchor checked: `:2-16` template incl. the `:15` status span INSIDE the `:2` button, `:27-30` props destructure, `:32/:4/:54` the exhaustive isCopied set, `:40-43` options, `:49` shallowRef, `:51-63` handleClick, `:65-102` hook, `:86`/`:90` the blank scars — `:89` is a live `transform: scale(1.25)`, `:95-99` the poke+setTargets order, `:104-113` scoped block); clipboard.ts (whole, 8 L); worktree easing-registry.ts `:18-34` DIRECT_NAMES/registryNames + `:123-136` throw; constants/types.ts `:25-37` (TimingFunctionNames = `BezierPresetName | "steps" | …` — NOT 12 flat names) + `:191-196` `| string`; constants/defaults.ts `:82/:84/:87`; group/group.ts `:57` PRM field, `:73` bare `singleTarget`, `:159-161` ctor derivation, `:195-204` setTargets recompute (typed `...HTMLElement[]`); group/lifecycle.ts `:79-81` (the ONLY group-path PRM consult); the full `respectReducedMotion` grep (engine/play-lifecycle.ts `:214`/`:376` = standalone-only — the K5 decider); group/waapi.ts `:31-33` refusal; orchestration-api.test.ts `:142-148` verbatim incl. `:147` `not-a-real-easing`; ci.yml `:27-77` (gates job = check:lib/build:lib/test:lib/proof:publish; `demo-correctness` `if: schedule || workflow_dispatch`, comment `:50-52`); all four mount sites re-read (EasingTarget.vue `:35-39` + EasingTarget.css `:48-53` PRM block / `:58-63` gap / `:68-78` sizing+hover; StartingStyleTarget.vue `:59` + `:122-124` copyableCss + `:209-215` PRM; KeyframesEditor.vue `:62-95` — toolbar `:63-69`, WandSparkles `:73`, CopyButton `:82-85`, sibling `:87-95` with `:90` `:aria-pressed` and `:91` ring; KeyframeCard.vue `:20-26` — the bare `<X @click>` at `:20-25` beside CopyButton `:26`, `:66` `index: number`); **KeyframeCardList.vue found at `demo/components/instrument/keyframes/components/` (NOT the path both readers cited)** — v-for `:key` `:5`, card `:7`, `:index="i"` `:12`; SharePopover.vue `:31-39` verbatim; useToolbarKeyboard.ts `:20-49`; design-idioms.css `:40-41` `--scale-hover: 1.08`, `:73-79` focus-ring, `:81-85` tap-floor, `:92-115` icon-* (icon-md = size-5); main.ts `:50-54`; style.css `:1-10`; contested greps re-run: `tap-floor` demo → **2 hits, BOTH the definition** (comment `:81` + selector `:82`, zero adopters) · `focus-ring` demo → 11 lines, **4 element adopters** (SquareScene.vue:46, SpringTarget.vue:63, SpringHeatmap.vue:30, KeyframeCard.vue:45) · `aria-live` demo *.vue → exactly 1 · `server` in vite.config.ts → 0 · `data-allow-motion` demo → 0 · `bounceInEase` in dist/gh-pages → 1 file; installed glass-ui 7.0.0: a11y-overrides.css whole head (the universal `*:not([data-allow-motion])` PRM kill switch; the forced-colors focus list is **ALL class-scoped** — `grep -oE '[^-a-zA-Z.]button:focus-visible'` → **0**, the 3 substring hits are `.dock-*-button`/`.dark-mode-toggle-button`), accessibility.css import order + state-attribute keys, scheme-motion.css `--duration-fast: 0.2s`, scale-paper.css `--scale-press: 0.96`; group-zone doc-rot grep (`./scheduler` → entries.ts:16, group.ts:170/:264; no scheduler.ts; yield-batch.ts exists); `ls demo/components/` → CopyButton.vue + 2 dirs.
**Substrate disclosure — the four-layer ruling (this seat's git reads + the banked kf-App precedent)**: local HEAD `8281638c` is a **divergent stale fork** — 1 ahead of merge-base `a59d3a22`, **41 commits BEHIND `origin/master` `81a56990`** (the kf-App apotheosis banked this three-layer schism as census **SCH-1**; re-confirmed here). (a) **HEAD**: value.js pinned `^3.1.0` (lock resolves 3.1.0), glass-ui declared `optionalDependencies: 6.0.0` **AND locked (3 lock entries — contra reader-2's "0 at HEAD")**, and the easing resolver is a wholly different `getTimingFunction` over value.js 3's `timingFunctions` registry. (b) **Worktree** (what all three axes and both readers audited): a whole-library Value-4 migration in flight — version 5.3.5→6.0.0, value.js →4.0.0, easing-registry.ts 220 lines changed to the `resolveTimingFunction`/`registryNames` form every axis cites, `optionalDependencies` **deleted** and lock glass-ui hits **0** while style.css:3/:9 still import glass styles — the F-1 phantom is TRUE of the worktree only. CopyButton.vue itself is byte-identical HEAD↔worktree. (c) **Frontier `origin/master`**: glass-ui **7.0.0 exact devDependency** (F-1 CURED — kf-App/SCH-1), value.js **4.0.0** in `dependencies`, the new resolver — **and `CopyButton.vue:42` reads `timingFunction: "easeInBounce"`: THE BLOCKER IS ALREADY REPAIRED AT THE FRONTIER** (this seat's probe; `git diff origin/master -- demo/components/CopyButton.vue` = exactly that one word, the worktree carrying the dead name the frontier fixed). (d) Frontier durability of everything else, probed row-by-row: the isCopied latch (same 3 lines), the `fade-out` blank scars (`:86`/`:90` identical), clipboard.ts's bare await (identical), the m-6 test (identical — and now false at the frontier, where value.js 4.0.0 makes `bounceInEase` NOT "a real registry curve"), the `"steps"` phantom (identical), the nightly-only ci.yml (identical `:53-55`). **The frontier repaired exactly one word and none of the paper trail that produced it.** All line anchors below are worktree-at-`8281638c`; re-resolve before any cure (D-19).
**Law compliance**: product source read-only; no browser tooling; **sole write = this file**.

## Scoreboard

**38 adjudicated roster rows — 1 BLOCKER (folded to EE-01, ALREADY-CURED-AT-FRONTIER) · 12 MAJOR · 15 MINOR · 10 INFO.** **12 killed-claims register entries, all SUSTAINED** — one EXTENDED by this seat's frontier probe, one sustained-in-direction with its own mechanism corrected at the bytes. **17 missed rows ADMITTED** (the nested-live-region MAJOR convergent across both readers; reader-2's substrate row promoted to the governing ruling). **7 adjudicator corrections against the readers** — including one reversal of a reader-1 confirmation cell (the phantom "bare `button:focus-visible`" forced-colors arm, a substring-grep artifact). **10 superlatives sustained, 1 rescoped, 1 retired.** Identity guards held: the blocker folds to **EE-01** (keyframes DISPOSITIONS.md:69, BUILD W1 — evidently executed at the frontier), the glass-ui dependency family folds to **census SCH-1** (kf-App ruling 1 → KF.W0), forced-colors folds to **KF-CE-13**, census S-7/F-1/§6.4/§6.5 cited never re-booked — zero re-bookings.

Blocker-weight facts: **KF-CB-1** is real at the audited bytes and dead code at the frontier — the terminal risk is no longer "fix the word" but "don't re-regress it": the stale fork's in-flight migration re-lands the dead name over the frontier's repair. The live frontier-frontmost defects are **KF-CB-4** (the promised icon swap was never written), **KF-CB-3** (the write-once name latch), **KF-CB-2** (false success announced to AT), and **KF-CB-9** (the sole working feedback channel sits where the a11y tree is entitled to flatten it away).

## Ruled disagreements (the incredulity seat's actual work)

| point | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **Substrate / "at HEAD"** | treated worktree bytes as "LIVE SOURCE", HEAD-equivalence untested | K1/K2: the cited resolver chain is uncommitted worktree; HEAD pins value.js 3.1.0 | **Reader-2 SUSTAINED + EXTENDED** | My git reads confirm K1/K2 whole (HEAD `^3.1.0` lock-resolved, `getTimingFunction` at HEAD, 220-line easing diff, ~94 dirty src rows). EXTENSION neither reader ran: **origin/master, 41 ahead, already carries the one-word repair** (`easeInBounce` at :42) — so K1's own sequencing advice ("the fix must land WITH the migration commit") is superseded: the fix landed upstream; the fork must reconcile (KF.W0), and the danger inverts to a **re-regression** if the local migration is rebased carelessly. |
| **F-1 / glass-ui state** | re-verified "LIVE": package.json 0 hits, lock 0 (worktree) | K3/M5: corpus F-1 "REFUTED at HEAD — optionalDep 6.0.0 declared; the lock carries ZERO glass-ui entries at both HEAD and worktree, so npm ci fails on desync" | **K3 SUSTAINED IN DIRECTION; reader-2's mechanism CORRECTED** | `git show HEAD:package-lock.json | grep -c glass-ui` → **3** (the optionalDep mirror :61 + the resolved 6.0.0 tgz entry :614-616). At HEAD, manifest and lock are CONSISTENT at 6.0.0 — `npm ci` at HEAD does not fail; it silently **downgrades** the installed 7.0.0 (exactly the kf-App/SCH-1 banked ruling, which reader-2 did not consult). The phantom is worktree-only; **cured at the frontier (7.0.0 exact devDep)**. ONE identity = census SCH-1 → **FOLD by reference to KF.W0**, zero re-booking; every "blocked on F-1" gate in this corpus (D-9, C-15, M-6 leg 2) is RE-SCOPED: not blocked at the frontier. |
| **.tap-floor adopters** | "2 occurrences: the definition and a single adopter" | K6: both hits ARE the definition; **zero adopters; dead CSS** | **Reader-2 wins; reader-1's cell CORRECTED** | My grep: exactly `design-idioms.css:81` (comment) + `:82` (selector). No adopter exists anywhere in demo/. D-5's "house practice" premise dies; the dead utility is admitted as KF-CB-25. |
| **focus-ring count** | endorsed D-7's "11 sites" | K7: 11 LINES, only **4** element adopters | **Reader-2 wins; reader-1's cell CORRECTED** | My grep: 4 class applications (SquareScene.vue:46, SpringTarget.vue:63, SpringHeatmap.vue:30, KeyframeCard.vue:45) + definition ×3 + prose ×2 + token reads ×2. Counting-unit law (pt-leaf ruling-6), third occurrence in this program. Sharpening the kill misses: **KeyframeCard.vue:45 puts `.focus-ring` on its code block in the same file that mounts CopyButton bare at :26** — the contract is known in-file and still declined. D-7's verdict survives at 4 adopters. |
| **D-19 forced-colors arm** | "PARTIAL CORRECTION: a11y-overrides contains a bare `button:focus-visible` arm that DOES reach CopyButton" | (did not test) | **REVERSED — this seat** | The 3 `button:focus-visible` grep hits in the installed a11y-overrides.css are substrings of `.dock-icon-button:focus-visible`, `.dock-tab-button:focus-visible`, `.dark-mode-toggle-button:focus-visible`; the full forced-colors selector list is ALL class-scoped (`.focus-ring`, `.interactive-item`, `.dock-*`, `.field-control`, `.input-pill`). `grep -oE '[^-a-zA-Z.]button:focus-visible'` → 0. **No glass rule reaches a bare button in any mode.** D-19 reverts to axis-D's original form — stronger, not weaker — and folds to KF-CE-13. A substring-grep artifact in a confirmation cell: the same failure class as C's §5.4 name-shaped grep, in the reader that praised that self-kill. |
| **S-3 superlative (symmetric frames)** | KILLED — "the same document files the same fact as a defect; crediting the absence of state as economy inverts the finding" | retained S-3 as a sustained superlative | **Reader-1 wins; reader-2's cell CORRECTED** | Reader-2 confirms L·M-5 (no icon swap, comments promise a persistent state change) AND credits the symmetric frames for "retiring" the cleanup that persistence would need — the identical internal contradiction reader-1 killed in L. RESCOPED: "the resting frame is the start frame" is recorded as a neutral mechanical fact, the precondition of KF-CB-4, not a virtue. |
| **D-2/M-1/C-3 severity (the latch)** | BLOCKER | MAJOR | **MAJOR — dissent recorded** | Permanent corruption of an actionable control's accessible name at 4 sites with no reset path is severe, but the control still copies; axes L+C and reader-2 grade MAJOR. Axis-D's and reader-1's BLOCKER preserved as dissent. |
| **D-10/M-5/C-10 severity (no swap)** | MINOR | MAJOR | **MAJOR — this seat's elevation, on the frontier fact** | At the frontier the blocker is already cured, which makes this the **shipped tree's frontmost sighted-feedback defect today**: the design contract stated twice in-file ("the icon swap is the sighted feedback") is unimplemented — a 200 ms superimposed pulse, no persistent state, `fade-out` driving no opacity (scars verified at `:86`/`:90`). Reader-1's MINOR priced it as latent behind the blocker; the frontier retires that pricing. |
| **C-11/M-4 severity (SVG ref annotation)** | INFO | MINOR | **INFO — dissent recorded** | No live failure (write path is `style.setProperty`, SVG-safe); demo/ is never typechecked on the merge path (KF-CB-8), so "the falsity is what passes the build" has no enforcement teeth today. The real row is the library's `setTargets(...HTMLElement[])` contract vs its own demo animating SVG — routed as a library letter (KF.W5). Reader-2's MINOR as dissent. |
| **D-6's cure** | killed it (K3-r1): options-bag flag is inert; `g.respectReducedMotion = true` is the only lever | K5: same kill, independently | **CONVERGENT — SUSTAINED at the bytes** | My grep: `anim.options.respectReducedMotion` is read at exactly play-lifecycle.ts:214/:376 — the standalone path a managed child never takes; the group path consults only `group.respectReducedMotion` (lifecycle.ts:80). The D axis's prescribed repair would ship a false green. |

## Adjudicated defect roster — id · severity · claim · proof · terminal disposition

Routing law: **KF.W0–W10** named from the census taxonomy (`formation/keyframes/CENSUS-2026-08-03.md:179-206`: W0 Substrate Settle §B-12 · W1 Mail Cure · W2 Parse Façade · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Tri-Fold Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation Settle · W9 Safari Visual Audit · W10 Fold Discharge & Close); behavioral repairs with no census owner are **NO-WAVE-OWNER (SS-1/SS-2 spec input)**. Standing rider: KF.W6's S-7 reshell decision (KF-CB-7) may absorb the shell-family rows (KF-CB-5/12/13/14/20/21/26) — if the glass `Button` swap lands, they discharge with it. Routings are bookings, not acts.

### BLOCKER

- **KF-CB-1 · D-1 = L·B-1 = C·C-1 = EE-01 (identity fold — never re-booked)** — `timingFunction: "bounceInEase"` resolves to nothing under value.js 4.0.0; the ctor throws inside the uncaught async `onMounted`; `group` is never assigned; zero sighted feedback at all four sites. Proof: unanimous three-axis static chain re-spot-checked by this seat (worktree registry `:18-34`/`:135`; no try/catch `:65-102`; no errorHandler in demo), the corpus's node probes, and the value.js program's own browser receipt (DP2-02 pageerror). **TRUE at the audited bytes; ALREADY CURED at `origin/master`** (this seat's probe: frontier `:42` = `"easeInBounce"`; the diff is exactly one word). Identity: **EE-01**, dispositioned `born V (Value-4 migration regression) → BUILD W1` at keyframes `docs/tranches/V/DISPOSITIONS.md:69` — W1 evidently executed upstream. → **FOLD to EE-01 + KF.W0** (Substrate Settle): the reconciliation's one obligation here is that the stale fork's in-flight migration **must not re-land the dead name over the frontier's repair** — the gate EE-01 already carries (`bounceInEase` pageerror→0 on /#/easing,/#/spring) is the born-RED witness if it does. The surviving phantom trail (prose at easing.ts:44 + waapi/eligibility.ts:169, the m-6 test) is NOT cured at the frontier → KF-CB-18/KF.W4.

### MAJOR

- **KF-CB-2 · D-3 = L·B-2 = C-2 (LAN arm killed)** — `copyText(text)` at `:52` unawaited/uncaught (not even the `void` the file writes correctly at `:62`); `isCopied` + the live-region announcement fire unconditionally, so a failed copy is announced to AT as a success; the util's toast channel declined; the lone divergent consumer among the repo's try/catch siblings. Frontier-durable (clipboard.ts identical). Severity MAJOR (L's BLOCKER as dissent). The insecure-context/LAN supporting arm is dead (K2/K4: no `server` block in vite.config.ts — verified). → **NO-WAVE-OWNER** (SS-1/SS-2): await in try/catch, success-state only on resolve, failure announced.
- **KF-CB-3 · D-2 = L·M-1 = C-3 (+ L·i-3 folded)** — `isCopied` is a write-once latch (`:4/:32/:54` exhaustive); the accessible name is permanently "Copied to clipboard" after one click, the `label` prop destroyed (EasingTarget's `label="Copy easing literal"` spent on first use); `liveStatus` likewise never cleared. Frontier-durable. MAJOR (axis-D + reader-1 BLOCKER as dissent). → **NO-WAVE-OWNER**: a real copied state with a reset arc driving name + glyph (+ `aria-pressed`, which buys KF-CB-33's hook free).
- **KF-CB-4 · D-10 = L·M-5 = C-10 (+ the twice-written false comment folded)** — `@keyframes fade-out` drives no opacity (blank scars verified at `:86`/`:90`; `:89` is a live transform — reader-1's line-cite kill of D-10's `:89` sustained); both blocks land the 100% frame identical to the resting frame under `fillMode: "forwards"`; the "icon swap" promised at `:13-14` AND `:33-34` was never written — the sighted feedback, once KF-CB-1 is reconciled, is a 200 ms superimposed double-stroke pulse with no persistent state. **MAJOR — elevated by this seat: at the frontier this is the live frontmost sighted-feedback defect.** Composite legibility → SS-13. → **NO-WAVE-OWNER** (SS-1/SS-2), one spec with KF-CB-3 (the state is the design; the animation is the transition into it); the duplicated comment collapses to one statement sited at the keyframes.
- **KF-CB-5 · D-4 = L·M-3 = C-4** — zero intrinsic box: every child out of flow (both glyphs `position:absolute` `:105-112`; `.sr-only` absolute per glass components.css); the sizing contract lives in a consumer's CSS comment (EasingTarget.css:68-74 verbatim); the abspos `width:100%`-against-padding-box algebra (reader-1's verified sub-claim) makes "16 px glyph in a 32 px target" structurally inexpressible. Four sites, four dialects. → **KF.W6** (the reshell owns the box; a default `w-4 h-4` is the interim floor). 0×0 painted geometry → SS-13.
- **KF-CB-6 · D-6 = L·M-2 = C-8, MERGED with reader-1's three PRM misses** — reduced motion is ungated on both surfaces (`options` carries nothing; `g.respectReducedMotion` unset, default false; sole group-path consult lifecycle.ts:80) and the motion is per-frame inline style, unreachable by ANY cascade rule. The merged facets (all verified): (i) a **global PRM kill switch DOES exist** — glass a11y-overrides.css opens with `*:not([data-allow-motion]) { animation-duration: 0.01ms !important … }`, reachable via style.css:3 — and engine-driven inline-style motion is structurally exempt from it (`data-allow-motion` used 0 times in demo; the axes' falsifiers grepped only src/+demo/ and missed it); (ii) two of CopyButton's own four HOSTS carry local PRM blocks that cannot reach their guest (EasingTarget.css:48-53, StartingStyleTarget.vue:209-215) — the engine-vs-cascade seam failing twice inside single files; (iii) the library's PRM snap targets the final frame = the resting frame, so a correctly-gated CopyButton renders a PRM user **nothing** — the persistent-state repair (KF-CB-3/4) must land BEFORE the flag, inverting every axis's repair order. D-6's prescribed options-bag cure is KILLED (convergent K5). → **NO-WAVE-OWNER** (SS-1/SS-2): `g.respectReducedMotion = true` at `:96`, sequenced after the persistent copied state.
- **KF-CB-7 · D-9 = census S-7, escalated "evaluate"→"decided"** — bespoke shell where SharePopover.vue:31-39 ships the identical affordance correctly (glass `Button` 32 px target, `icon-md` 20 px glyph, house focus/hover, visible `title` — verified verbatim; icon-md = size-5 per design-idioms.css:103-108). **F-1 gate RESCOPED by the substrate ruling: cured at the frontier (glass-ui 7.0.0 exact devDep) — the swap is no longer dependency-blocked.** Reshell constraint (S-2): a real `<button>` must remain in the DOM or CopyButton silently drops out of the toolbar's roving-tabindex cohort (useToolbarKeyboard.ts:41-49 collects `button` descendants; its docblock names CopyButton). → **KF.W6** (Glass Suffusion owns S-7 partials, census :197).
- **KF-CB-8 · L·M-6 leg 1, leg 2 rescoped** — the only gate that observes this component is nightly/manual-only (`ci.yml:53-55` `if: schedule || workflow_dispatch`; ":50-52 does not block library merges"; merge path = `check:lib` over `tsconfig.lib.json` → demo/ never typechecked) — **frontier-durable** (identical at origin/master :53-55). Leg 2 ("npm-ci-blocked by F-1") is TRUE only of the uncommitted worktree, FALSE at HEAD (lock consistent at 6.0.0 — this seat's correction), CURED at the frontier. Rescoped narrative: the battery was not "disarmed" so much as bypassed — the value.js program's own browser audit caught EE-01 (DP2-02) and the frontier repaired it; the structural row is that **no merge-path gate can catch the next one**. → **KF.W4** (gate topology; whether the roster earns a merge-path seat). Nightly run colour → residue (a `gh run list`, not live-gated).
- **KF-CB-9 · reader-1 miss #1 = reader-2 miss M1 (convergent) — the live region is a DESCENDANT of the `<button>`** (`:15` inside `:2-16`, verified), and `button` is Children-Presentational in ARIA — the component's only working feedback channel sits where the accessibility tree is entitled to flatten it to a string consumed by nothing (the `:4` `aria-label` overrides content in the name computation). All three axes made this construction their superlative on ORDER grounds (which remain correct) without checking PLACEMENT; `aria-live` grep → exactly 1 in demo, so no house pattern exists to inherit. If it does not announce, the component has zero working feedback on ANY channel — materially different from "the a11y channel is the sole survivor". → **NO-WAVE-OWNER** (SS-1/SS-2): hoist the sink to a sibling of the control (or one app-level polite sink). AT behaviour = the single highest-value SS-13 probe.
- **KF-CB-10 · reader-1 miss #2 — N byte-identical accessible names in the keyframe list, pre-click** — KeyframeCard.vue:26 passes no `label`, so every card's button exposes the default "Copy to clipboard"; the disambiguating datum is already in hand and discarded (`index: number` declared at KeyframeCard.vue:66; `:index="i"` passed at KeyframeCardList.vue:12 — verified; **path correction: the list lives at `instrument/keyframes/components/KeyframeCardList.vue`, not the path both readers cited**). A resting-state WCAG 2.4.6/4.1.2 defect at the most-instantiated site, not a post-click consequence of KF-CB-3. → **NO-WAVE-OWNER** (`label=\`Copy keyframe ${index + 1}\``).
- **KF-CB-11 · reader-1 miss #3 (call-site-scoped) — the destructive control beside CopyButton is not a control**: KeyframeCard.vue:20-25 is a bare Lucide `<X @click … data-destructive>` — no button, no role, no tabindex, no accessible name — keyboard-unreachable, invisible to AT, and structurally outside the toolbar cohort's `button` query. Verified at the bytes. The copy affordance ships a live region; the DELETE beside it ships nothing. Scoped to KeyframeCard but it is the immediate context any CopyButton repair touches. → **NO-WAVE-OWNER** (SS-1/SS-2 must widen to the row).
- **KF-CB-12 · D-7 = C-15 (count rescoped by K7)** — no focus affordance; opts out of the demo's declared SINGLE `:focus-visible` contract (4 real adopters, not 11); a third dialect on the sibling button in the same toolbar (`:91` `focus-visible:ring-2 focus-visible:ring-accent`); no glass rule reaches bare buttons in ANY mode (this seat's D-19 reversal makes D-7 stronger); the sibling's bespoke ring is itself ~1.37:1 (reader-1's re-computation, adopted). UA ring survives (no `outline-none`) — MAJOR not BLOCKER stands. → **KF.W6** (`.focus-ring` for both controls, or the reshell's ring).
- **KF-CB-13 · D-8** — no hover/active/disabled; hover improvised per site in three dialects (scale-on-hover at KeyframesEditor; colour-hover at EasingTarget.css:76-78; nothing at KeyframeCard/StartingStyleTarget); the house idiom (`--scale-hover: 1.08`, 13 sites) declined. → **KF.W6** (with KF-CB-7).

### MINOR

- **KF-CB-14 · D-5 as-rescoped (K6 applied)** — 16 px pointer targets at 2 of 4 sites, 24 px at the boundary at the other two; the `.tap-floor` "house practice" premise is DEAD (zero adopters — the utility is KF-CB-25's dead CSS); what survives is the WCAG 2.5.8 boundary math (plausibly excepted by spacing, per D-5's own honest qualification) and the in-house 32 px answer (SharePopover). Demoted MAJOR→MINOR by this seat: the conformance case is exception-rescued and the cure is owned by KF-CB-5/7. → **KF.W6**.
- **KF-CB-15 · m-4 = C-7** — `g.singleTarget = false` pokes derived state (bare undocumented field `:73`; ctor mis-derives `undefined === undefined → true` over target-less children `:159-161`; `setTargets` recompute `:195-204` silently reverts any future poke; waapi.ts:31-33 refusal is the unstated consequence). Cure verified against the ctor: pass each icon at construction. → **NO-WAVE-OWNER** hygiene (rides the KF-CB-4 spec edit).
- **KF-CB-16 · D-16 = m-3 = C-16** — two compile-time-constant keyframe strings re-parsed through the full stylesheet grammar per instance, unmemoized (no cache on the `resolveKeyframes` path — corpus-verified); N cards pay 2N parses. m-3's dogfood counterweight keeps this MINOR. → **NO-WAVE-OWNER** hygiene (hoist to module scope); wall-clock → residue.
- **KF-CB-17 · m-5** — repeat click re-announces to AT but drops the pulse (`beginPlay` re-entrancy); the two feedback channels have opposite repeat semantics, and the inverted one is the one the comments call primary. → **NO-WAVE-OWNER** (rides KF-CB-4).
- **KF-CB-18 · m-6, SHARPENED by the frontier probe** — orchestration-api.test.ts:142-148 asserts `cssTwinFor("bounceInEase")` undefined under a comment calling it "a real registry curve"; `:147` proves in-file that the assertion cannot distinguish real from phantom. **Frontier-durable: the frontier repaired the demo and left the test + the prose phantoms (easing.ts:44, waapi/eligibility.ts:169) standing — the trap that produced EE-01 is still armed upstream.** → **KF.W4** (K-Quartet easing hygiene), one commit with the `| string` letter (KF-CB-29).
- **KF-CB-19 · m-7** — zero test coverage for a four-consumer shared leaf with a clipboard side effect, an engine dependency, and a live region (grep → 0, corpus-verified; lane-20 already classes it cross-tier). → **NO-WAVE-OWNER** (the SS-1/SS-2 spec's acceptance test).
- **KF-CB-20 · D-12** — colour duel: `text-foreground` dead at EasingTarget by cascade-layer order (unlayered scoped author CSS beats the `utilities` layer), live at the other three; exactly one of two declarations wins per site and which one flips. Adopted on reader-1's verification. → **NO-WAVE-OWNER** hygiene (dies in the reshell).
- **KF-CB-21 · D-14** — 4 of 8 root utilities inert against preflight (`p-0 m-0 bg-transparent border-0`). → **NO-WAVE-OWNER** hygiene.
- **KF-CB-22 · D-15 = m-1 (reader-2's reframe adopted) + i-4 folded** — no unmount teardown: the group's rAF loop ticks ~200 ms into detached SVG nodes; the naked `requestAnimationFrame` handle at `:58` is discarded, its callback writing `liveStatus` on a torn-down instance; a lifetime/write-to-detached defect, NOT a leak (nothing retains the group). The in-repo exemplar is TypingDots' `onBeforeUnmount`. → **NO-WAVE-OWNER** hygiene.
- **KF-CB-23 · C-9** — two constant `@keyframes` authored as JS template literals: unlinted, untokenized, invisible to the stylesheet layer (`/*css*/` is editor convention, no toolchain hook) — the surviving kernel of census §6.4 after the mechanism strike (KF-CB-34), and the error class a linted stylesheet keyframe would likely have caught (a `fade-out` with no opacity channel). → **NO-WAVE-OWNER** (decide with KF-CB-4's rewrite).
- **KF-CB-24 · reader-2 miss M3, verified worktree AND frontier** — `TimingFunctionNames` advertises `"steps"` (types.ts:27), which throws at runtime (bare `steps` fails CSS Easing L1 parse; absent from registryNames): the closed roster offered as the type-level protection carries its own phantom, so deleting `| string` alone is insufficient. → **KF.W4** (with KF-CB-18/29).
- **KF-CB-25 · reader-2 miss M4** — `.tap-floor` is dead CSS: a documented WCAG utility with zero call sites (my grep: the 2 hits are its own comment + selector). → **KF.W6** hygiene (adopt-or-delete).
- **KF-CB-26 · D-17 + reader-1 miss #7, merged** — icon-only with no visible label or tooltip at any site; the `label` prop routes exclusively into `aria-label` (its default is tooltip prose never shown); the single-element root means `title`/`aria-describedby` DO fall through and no site uses either. The one-line interim (`:title="label"`) matches the house reference (SharePopover `title=`) and is NOT dependency-gated. → **NO-WAVE-OWNER** (interim `:title`; the reshell's Tooltip supersedes).
- **KF-CB-27 · reader-1 miss #8, verified at the tokens** — the motion constants sit outside both scales: `duration: 200` vs glass `--duration-fast: 0.2s` (scheme-motion.css — same value, unnamed) and `scale(1.25)` ×3 vs the house lift `--scale-hover: 1.08` / `--scale-press: 0.96` — ~3× the house magnitude, invisible to S+3's style-block-only falsifier. → **KF.W6** (the `--kf-*` / motion-token decision must include script-side constants).
- **KF-CB-28 · C-5** — no `defineEmits`; copy success is unobservable, and the owner design doc already specifies a consumer that needs it (easing.md:238-241 verbatim, verified — the readout flash is unimplementable against this contract). The only row anchored to stated owner intent. → **NO-WAVE-OWNER** (SS-1/SS-2: `copied`/`failed` emits).

### INFO

- **KF-CB-29 · C-13** — the `| string` arm (types.ts:195) collapses the roster; `tsc` green on a name the runtime rejects — the mechanism by which the value.js rename shipped. K10's characterization corrections adopted (the roster is `BezierPresetName | …`, not "12 names", and carries its own phantom → KF-CB-24). → **KF.W4** library letter, one commit with KF-CB-18/24.
- **KF-CB-30 · C-11 = L·M-4, ruled INFO** — `useTemplateRef<HTMLElement>` on functional Lucide icons resolves to `SVGSVGElement`; the false annotation is what typechecks `setTargets`; benign on the reached path (grep-verified HTML-only members → 0). Library letter: the element contract should be `Element` (the demo animates SVG). → **KF.W5**. Reader-2's MINOR as dissent; `transform-box` rendering → residue.
- **KF-CB-31 · C-12 = i-2 (+ m-2 folded)** — the `:45-48` "resolves within microtasks" comment is true only via the unnamed main.ts:50-54 pre-mount warm (verified); lifted elsewhere the await is a real chunk fetch with no pending state, and the post-await `!` assertions (`:98-99`) become the sharp edge; a rejected engine load is memoized forever and swallowed at boot. → **NO-WAVE-OWNER** observation (name the coupling; the `unmounted` guard).
- **KF-CB-32 · D-19, restored to its original form (this seat's reversal)** — the component emits none of the state attributes glass's contrast/forced-colors layer keys on (notably `aria-pressed`, shipped by the sibling at KeyframesEditor.vue:90), and — corrected — **no** glass focus arm reaches it in forced-colors either. → **fold by reference to KF-CE-13** (demo-wide forced-colors row) → **KF.W9**; the `aria-pressed` hook lands free with KF-CB-3's state.
- **KF-CB-33 · C-14, negative finding UPHELD and now PREDICTIVE** — the R1 colour-parser class is unreachable (transform/opacity only), yet value.js broke this component through the easing/css organ (`parseTimingFunction` + `bezierPresets`) — exactly the class a §4.6-scoped parser wave would miss. EE-01 is the proof the prediction already fired once. → recorded as a **KF.W3** (Parser Consumption) scoping input.
- **KF-CB-34 · census §6.4/S-7 mechanism strike (all three axes + both readers, convergent; census line re-read by this seat)** — "style-injection from script, bypassing the cascade" is wrong twice: nothing is injected (parse → in-memory frames → per-frame inline styles), and inline styles are maximally INSIDE the cascade. Load-bearing: it is WHY no cascade PRM rule can reach the motion (KF-CB-6). Census conclusion upheld, mechanism struck — the census row should be annotated, never rewritten (E-3).
- **KF-CB-35 · reader-2 miss M6** — `Partial<InputAnimationOptions>` is a tautology (the type is already `Partial<{…}>` at types.ts:182), copied from the library's own signatures. → **KF.W5** free win.
- **KF-CB-36 · reader-2 miss M7, verified** — group-zone doc-rot: three comments direct to a `./scheduler` module that does not exist (entries.ts:16, group.ts:170/:264; the module is `yield-batch.ts`). → **KF.W5** prose sweep.
- **KF-CB-37 · reader-2 miss M8, verified** — CopyButton.vue is the sole file at `demo/components/` root in a tree that homes everything by feature (`ls`: 1 file + instrument/ + playback/). → **KF.W8** (Structure & Colocation Settle), same wave as the KF-CB-7 reshell decision.
- **KF-CB-38 · reader-2 miss M9** — the component's only non-vendor import statically pulls vue-sonner (clipboard.ts:1, module scope) for a channel it declines; mitigated (other consumers load it anyway) but the leaf's declared feedback story and its import graph disagree. → **NO-WAVE-OWNER** observation.

## Killed-claims register — all SUSTAINED, refutations re-executed or extended by this seat

1. **K1/K2 (reader-2) · the "at HEAD" attribution** — SUSTAINED at git (HEAD = value.js ^3.1.0 + `getTimingFunction`; the cited chain is the uncommitted migration; ~94 dirty src rows, not "a demo/bench change") and **EXTENDED**: the frontier already repaired the blocker line, inverting the sequencing advice (see the ruled table). Every src/ anchor in all three axes is worktree-only.
2. **K3 (reader-2) · census F-1 "absent from package.json"** — SUSTAINED IN DIRECTION (HEAD declares optionalDep 6.0.0), mechanism CORRECTED (HEAD lock = 3 consistent entries, not 0; npm ci at HEAD downgrades rather than fails). ONE identity with census SCH-1/kf-App ruling 1 → KF.W0. The worktree phantom remains real and is what reader-1 measured.
3. **K5 (reader-2) = reader-1's D-6-cure kill (convergent)** — the options-bag PRM flag is inert on the group path; verified at play-lifecycle.ts:214/:376 vs lifecycle.ts:80. The only lever is `g.respectReducedMotion`.
4. **K6 (reader-2) · D-5's .tap-floor adopter** — SUSTAINED at my grep: zero adopters; both hits are the definition. Inverts the premise into KF-CB-25.
5. **K7 (reader-2) · D-7's "11 controls paint the house ring"** — SUSTAINED: 4 element adopters; 11 = lines. Conformance point survives; suite-wide rhetoric dies.
6. **K8 (reader-2) = reader-1's D-13 kill (convergent)** — `copyableCss` is a template literal with an unconditional prefix (StartingStyleTarget.vue:122-124, verified); the named empty-string scenario cannot occur. Residual empty-`text` contract note survives as INFO only, filed nowhere as a defect.
7. **K4 (reader-2) = reader-1's C-2-deployment kill (convergent)** — `server.host: true` is a value.js fact imported cross-repo; vite.config.ts has no server block (verified). The D axis's own §7 pre-kill named the failure mode.
8. **K9 (reader-2) · D-11 severity** — SUSTAINED: `button`'s Children-Presentational rule already prunes SVG descendants; the aria-hidden omission changes nothing exposed. D-11 → INFO idiom-only (absorbed; not separately rostered). The same rule powers KF-CB-9 in the opposite direction.
9. **K10 (reader-2) · C-13's "closed, accurate roster of 12"** — SUSTAINED at types.ts:25-37: the first arm is `BezierPresetName` (30 keys) and the roster carries `"steps"`, its own phantom (KF-CB-24).
10. **Reader-1's kill of C-1's CI-tension dilemma** — SUSTAINED (the `if:` at ci.yml:55 explains everything; the "green run" falsifier was unfalsifiable-by-construction) and now doubly moot: the frontier repaired the defect the dilemma was about.
11. **Reader-1's kill of L·S-3 (symmetric-frames superlative)** — SUSTAINED; reader-2's retention CORRECTED (ruled table). Recorded as the neutral precondition of KF-CB-4.
12. **Reader-1's kill of D-10's `:86`/`:89` line cite** — SUSTAINED at the bytes (scars at `:86`/`:90`; `:89` is a live transform). An implementer following D's §8 step 2 would edit a transform.

## Adjudicator corrections register (against this corpus's readers)

1. **Reader-1's D-19 "partial correction"** — the "bare `button:focus-visible` arm" in glass a11y-overrides is a substring-grep artifact (`.dock-tab-button:focus-visible` et al.); no bare-element rule exists in any mode. REVERSED; D-19 restored to axis-D's original, stronger form (KF-CB-32).
2. **Reader-1's D-5 cell** — ".tap-floor … the definition and a single adopter": false; zero adopters (K6's fact, present in reader-1's own confirmation).
3. **Reader-1's D-7 cell** — "11 sites" endorsed without a counting unit; 4 adopters. Third occurrence of the pt-leaf ruling-6 counting-unit law in this program.
4. **Reader-2's M5/K3 lock claim** — "the lock carries ZERO glass-ui entries at both HEAD and worktree": false at HEAD (3 entries, consistent at 6.0.0); the kf-App apotheosis had already banked the correct three-layer shape, and the reader did not consult the registry it was writing into.
5. **Reader-2's S-3 retention** — contradicts its own L·M-5 confirmation; the identical inversion reader-1 killed in the L axis (citation-inheritance-shaped: an axis superlative re-endorsed without confronting the reader's own defect row).
6. **Both readers' KeyframeCardList path** — the file lives at `instrument/keyframes/components/KeyframeCardList.vue`; both cited the parent directory. Claims survive (v-for `:5-7`, `:index` `:12` verified); anchors corrected.
7. **Both readers' frontier blindness** — reader-2 established the fork but probed no frontier; reader-1 probed neither. The kf-App precedent (frontier-durability for every blocker) was available in the registry both readers cite. The single most consequential fact in this adjudication — the blocker is already cured upstream — was reachable by one `git diff origin/master -- <file>`.

## Superlatives — sustained, with provenance

1. **SUP-1/S-1 · LIGHT/HEAVY boundary consumption, byte-perfect** (all three axes + both readers; import list re-verified by this seat): `import type` for the heavy types, `loadAnimationEngine` the only value, published barrel, never `@src`. The packaging thesis of the library, honored by a 113-line leaf — which still failed, because the failure was in a value it passed, not a path it took.
2. **S-2 · a real `<button type="button">` root** — earns native activation/focus/role AND free membership in the roving-tabindex cohort (useToolbarKeyboard.ts:41-49 + docblock naming CopyButton, verified). The binding constraint on any KF-CB-7 reshell.
3. **SUP-2 · `shallowRef` for the group handle** — protects the reference-identity `singleTarget` derivations (`:159-161`/`:195-204`, both verified) from proxy-breaking; a correctness guard, not a perf preference.
4. **S-4 · `void group.value?.play()`** — correct named fire-and-forget, the library's own idiom; the sole reason KF-CB-1 degraded to silence instead of a per-click TypeError; and the standing indictment of `:52`, ten lines up, which floats the failure-bearing promise bare.
5. **S-5 · Vue 3.5 reactive props destructure keeps `text` live** — `:52` copies current text; concretely load-bearing at the two continuously-recomputed bindings; discharges the U-era lane-24 runtime-object-props row. The falsifier runs both ways (pre-3.5 this identical line is a wrong-string copier).
6. **S+2 · the contrast table** (axis D; reader-1 re-derived every luminance to published digits) — all four theme×path combos clear 3:1, the quietest clears 4.5:1; the payoff of `currentColor` + tokens. Adopted as token math (chromium-independent).
7. **S+3 · zero token footprint — RESCOPED** by KF-CB-27: true of the scoped block `:104-113` (no custom property, no colour literal), false of the file (script-side `duration: 200` / `scale(1.25)` are off-scale unnamed motion constants). The narrowed superlative survives; the falsifier's scope was the flaw.
8. **The live-region ORDER discipline** (S+1/SUP-3's kernel) — present-at-mount, empty-until-fired, correctly re-armed. Survives KF-CB-9, which attacks placement, not order; the technique transplants intact to a sibling sink.
9. **CORPUS · the D axis's §7 killed-candidates table** — including the pre-kill of the exact cross-repo LAN false-defect the C axis then filed (K4/K7 here), and the Lucide functional-fallthrough rescue chain. Adversarial discipline the readers rightly named exemplary.
10. **CORPUS · the C axis's §5.4 self-kill** — the name-shaped-grep lesson (`warmEngine` vs `warmKfEngine`), which this adjudication invoked twice more (the `button:focus-visible` substring artifact; the focus-ring counting unit).
11. *(Retired: L's S-3 — see killed register #11.)*

## UNPROVEN-NEEDS-LIVE residue (SS-13 / KF.W9 visual-audit inputs)

1. **Whether the nested `role="status"` region announces at all** in VoiceOver/NVDA/JAWS despite Children-Presentational (decides KF-CB-9 — the highest-value probe: if silent, the component has zero working feedback on any channel).
2. Whether an unsized `<CopyButton>` paints 0×0 under preflight (KF-CB-5's latent case; all four sites size it today).
3. The superimposed clipboard+check composite at the 50% frame at 16-24 px — state change or double-stroke muddle (KF-CB-4; `ClipboardCheck` = `Clipboard` + tick, so it may read acceptably).
4. `scale(1.25)` transform-origin behaviour on the outermost `<svg>` (KF-CB-30's rendering half).
5. Whether one rAF is enough separation for repeat SR announcements, and the hidden-tab drop (bounds on superlative #8).
6. Double announcement on refocus after copy (`aria-label` change + status region).
7. Wall-clock cost of 2N stylesheet parses at realistic K (KF-CB-16; structure proven, milliseconds not).
8. The nightly `demo-correctness` colour — one `gh run list --workflow=ci.yml`; re-read at the FRONTIER, where EE-01 is repaired and the glass dep is whole (the worktree-era predictions about this gate are dated by the substrate ruling).
9. Legibility of the UA focus ring around a transparent 16 px box on `--card` (KF-CB-12).
10. Whether the 1.25× pulse visibly exits the consumer-declared box at the 16 px sites (~2 px/edge; EasingTarget's neighbour is ~7 px away across `gap: 0.45rem`).

## Closing verdict

**ADJUDICATED at keyframes.js worktree-on-`8281638c` (a stale local fork, 41 behind `origin/master` — the four-layer substrate ruling is this seat's governing addition): 38 roster rows — 1 BLOCKER, real at the audited bytes and ALREADY CURED at the frontier (folded to EE-01/KF.W0, with the re-regression guard as the surviving obligation) · 12 MAJOR · 15 MINOR · 10 INFO; 12 kills sustained (one extended by the frontier probe, one corrected in its own mechanism against the banked kf-App record); 17 misses admitted — chief among them the nested live region that may silence the component's only working channel, the N-identical-names resting defect, the keyboard-unreachable destructive sibling, and the glass PRM kill switch every axis's falsifier missed; 7 reader corrections including one reversed confirmation cell (the substring-phantom forced-colors arm); identity guards held (EE-01, census SCH-1/F-1, KF-CE-13, S-7, §6.4, §6.5 — zero re-bookings). The component is a well-mannered consumer wearing an unimplemented design: its boundary discipline, button semantics, and reactivity choices are genuinely exemplary, and every one of its failures — the dead name it was handed, the swap that was never written, the latch with no reset, the sink nested where the tree may flatten it — is a failure of what it promises rather than how it reaches the library. Routings are M-25 agglomeration input: behavioral rows NO-WAVE-OWNER for SS-1/SS-2 under the KF.W6-reshell-may-absorb rider; structural folds to KF.W0/W3/W4/W5/W6/W8; visual residue (10 rows) to KF.W9/SS-13. Nothing here stamps a wave; X·KF executes on the owner's begin-word.**

— end of apotheosis. The challenge corpus and both reader returns remain immutable beside this file per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/CopyButton.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **CopyButton.vue=+1/-1**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.


## ADDENDUM 2026-09-17 (X.KF.W9 · `.d` measured, `.e` landed) — **KF-CB-9**

*Appended by seat `.e`, the only seat §Bounds `:69` permits to write here; **measured by seat `.d`**, which
was forbidden to land it. **Nothing above this line is rewritten** — no edit, no re-grade, no renumber, no
deletion, no new id (E-1 / E-3). Every bullet is a dated MEASUREMENT; none re-books a row or moves a severity.*

- **cell** — `chromium` (the AT cells are UNREACHABLE-IN-CELL — `at/voiceover-safari` on both legs, `at/nvda`/`at/jaws` for want of a Windows host)
- **capture** — none for this row — the reading is a live DOM/computed-style read, banked unedited at `evidence/W9/W9d-raw-chromium-cells.json`
- **stamp** — substrate keyframes.js `55e9bf0d2391bbc6d9871bb3f0555a6225daae92` (COHESION §0m.2 HOLD) · bundle `1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448` (built in the §0m.2 capture clone, **measured and double-run** at `.d` and **re-hashed identical at `.e`**)

- **The PRECONDITION is CONFIRMED**: a nested `role="status"` inside a `<button>` is present on the shipped surface — the structural half of the registry's single highest-value SS-13 probe.
- **NET-NEW: the probe has a THIRD possible outcome its two-branch framing does not enumerate.** The copy button ships in **two divergent forms on one route**: `"Copy curve literal"` carries **no nested `role="status"` at all**, while `"Copy easing literal"` carries one. A probe framed as *"the live region either is or is not announced"* cannot express *"the surface is not one surface."* **The row is not re-graded and the probe is not re-authored here** — the finding is recorded so whoever runs the AT pass runs it against both mounts.
- **The UTTERANCE stays UNREACHABLE-IN-CELL**: what an AT says is what an AT cell measures, and all three are foreclosed on this host with their bounds measured. **S-8 family (vi) is DECLARED UNSPENT** — no probe in it is spent by this reading, and none may later be skipped on the ground that a seat "already looked".
