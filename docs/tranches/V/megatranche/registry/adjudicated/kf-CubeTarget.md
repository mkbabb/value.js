Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the apotheosis seat for kf demo component CubeTarget)

# kf-CubeTarget — adjudicated apotheosis

**Date** 2026-08-07 · **Corpus** `audit/kf-components/CubeTarget/challenge-{D-design,L-library,C-consumption}.md` (all three `claude-opus-5[1m]`, dated 2026-08-04) · **Re-readers adjudicated**: design-and-usage (Fable) + library-and-code (Fable), both hostile, returns consumed whole.

**Substrate ruling (read first — it decides one BLOCKER family and one kill-of-a-kill).** There are THREE trees, and the two re-readers measured different ones without saying so. (a) The SACRED stale checkout HEAD `8281638c` (41 commits behind `origin/master` `81a56990`). (b) The DIRTY working tree — the state every grep/Read in the corpus and both returns actually touched. (c) `origin/master`. This seat's own git instruments settle the terrain: **the entire cube demo blast surface on disk is byte-identical to `origin/master`** (`git diff origin/master -- demo/scenes/cube/ demo/composables/useDoubleTap.ts demo/styles/` → EMPTY), so every reader anchor in `CubeTarget.vue`/`CubeTarget.css`/`useCubeRelit.ts`/`CubeAxisLines.vue`/`orbital-drag/**`/`useDoubleTap.ts`/`style.css`/`layout.css`/`brand.css` is **origin-current** — a materially better anchor state than the AmigaScene record's. The exceptions: `matrix-editor/*` + `useCubeDemo.ts` drifted from the stale HEAD but disk ≡ origin (the `Math.acos` block survives at BOTH, re-read by this seat at `origin/master:demo/scenes/cube/matrix-editor/useTransformState.ts:69-71`); `src/` on disk is an uncommitted refactor differing from BOTH commits (88 files vs origin; `frame-compiler.ts` deleted, `play-lifecycle.ts` split, `css-animation.ts` renamed), so this seat re-derived the L·B-2/L·M-4 engine chain **at the `origin/master` commit itself**: dot-join at `compile/value/compile.ts:61` (`path === "" ? key : ${path}.${key}`), silent object-skip + unvalidated `setProperty` at `compile/value/compile.ts:270-284` (with one new `undefined → removeProperty` arm), `unflatten = false` at `engine/css/animation.ts:62` (`:113` `unflatten = transform != null`), and the flat-map branch at `engine/interpolate.ts:304-307`. **Both BLOCKER mechanisms survive the engine refactor verbatim.** The dependency terrain: `git show 8281638c:package.json` → `optionalDependencies: { "@mkbabb/glass-ui": "6.0.0" }`; `git show origin/master:package.json:77` → `"@mkbabb/glass-ui": "7.0.0"` **exact devDep** (census SCH-1, the kf-AmigaScene C-1 ruling); the working tree carries an **uncommitted** hunk deleting the glass-ui declaration outright (vs origin: the 7.0.0 devDep line removed; installed = 7.0.0). Reader A (design-and-usage) ran git and got the stale-HEAD facts right; reader B (library-and-code) measured the dirty working tree and reported it as "the manifest" — and that error propagated into one of its confirms AND one of its kills (rulings below). KF.W0's re-anchor law applies before any cure executes.

**Method.** Every verdict-level disagreement between the two readers was re-derived by this seat against the tree with its own instruments, not vote-counted. Instruments this session (static + git only; zero browser, zero installs — probe parsimony): `git log/status/show/diff` across all three trees incl. the package.json triangulation and the demo-surface ≡ origin proof; whole reads of CubeTarget.vue (239 L), CubeTarget.css (154 L), useCubeRelit.ts (87 L), useDoubleTap.ts (84 L), CubeAxisLines.vue (90 L), useTransformState.ts (227 L, disk ≡ origin); OrbitalDrag.vue:1-110 + :190-352; useOrbitalPointer.ts:55-239; orbital-drag/index.ts:60-110 (bounds + `defaultTransformState`); style.css:140-160 (the crayon block) + its full `@layer`/`@import` skeleton; CubeScene.vue:15-25/:185-228; useCubeDemo.ts anchor re-resolution (`setTargets` :154-158, PRM guard :162-166, AST authoring :84-87); engine chain at `origin/master` (cited above) + `constants/defaults.ts:79-89` (iterationCount 1 / fillMode forwards / useWAAPI true / respectReducedMotion false); `node_modules/@mkbabb/glass-ui/dist/styles/tokens/on-glass-fg.css` READ WHOLE (the D·M-2 cure decider — four fixed muted-ink tokens, nothing surface-derived); `grep -rln "transform: {" demo/` (3 files — one MORE than either reader's cell, ruled below); `grep -rln useDoubleTap|doubleTap|dblclick|onRoll|cube--rolling test/ scripts/` → exit 1 (zero gate coverage, my own run); `test/demo/scenes/cube-scene.test.ts` — the `--lit` assertions lock front-vs-back (Z) only, leaving L·M-1's Y flip unasserted; App.vue:50 (the one gesture hint). KEY_LIGHT arithmetic re-derived by hand (top 0.20 / bottom 0.80 at rest — replicates both readers). Where both readers independently re-derived the same figure set (the D·M-2 contrast table to two decimals, the six FACE_NORMALS, the six ROLL_FACES attitudes), the double-agreement is adopted as corroborated without a third derivation.

## Scoreboard

**51 rows ADJUDICATED-CONFIRMED (2 BLOCKER · 14 MAJOR · 23 MINOR · 12 INFO) · 17 killed/rescoped claims registered · 17 net-new misses adopted into the roster · 8 UNPROVEN-NEEDS-LIVE residues for SS-13.**
Headline survivors: **L·B-1 + L·B-2** — the component's signature feature, the Roll, is dead twice over (the trigger can never fire; the animation could never paint), both silently, with zero gate coverage; and the net-new **acos MAJOR** — the matrix editor's Reset writes `Math.acos(diagonal)` RADIANS (NaN above 1) into the degrees-typed rotate triple every consumer trusts. Headline kills: the glass-ui phantom-dependency BLOCKER is **substrate-dead** (fold to census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1 — never re-booked), and the corpus's most-praised perf comment (the T.A5 `--lit` quantization) is **defeated eight lines below it** by `transition: --lit 160ms linear`.
Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; routings below are agglomeration input (M-25) for the SS-1/SS-2 spec authoring, keyed to the census wave sketch KF.W0–KF.W10.

## Ruled disagreements (this seat's actual work)

| dispute | reader A (design-and-usage) | reader B (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|
| **L·B-3 / glass-ui family** | KILLED (HEAD declares `optionalDependencies` glass-ui 6.0.0; the absence is an uncommitted edit) + net-new MAJOR (6.0.0 pin vs 7.0.0 installed, optional-bucket silent-skip) | CONFIRMED at MAJOR ("absent from both package.json and package-lock.json"), plus a kill of the flat-cube scenario via "43 bare imports → build fails" | **A's kill CONFIRMED; B's confirm SUBSTRATE-DEAD; A's net-new MAJOR ALSO killed at origin** | My git triangulation (substrate ruling): B measured the dirty working tree; at the stale HEAD the dep is declared (6.0.0 optional) and at `origin/master` it is an **exact 7.0.0 devDep matching the installed 7.0.0** — so B's "absent from the manifest" is false at every commit, B's build-fails mechanism dies with it, and A's 6.0.0-pin/optional-bucket hazard is real ONLY of the stale checkout and dead at origin. ONE identity: census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1 (FALSIFIED at HEAD-of-origin, discharged by KF.W0 §B-12) — fold by reference. The one LIVE residue this record adds: **the uncommitted working-tree hunk deletes the glass-ui devDep** — if ever committed as-is it resurrects F-1 for real; a KF.W0 reconciliation row, not a component defect. |
| **D·S-4 (preserve-3d superlative)** | kept (qualified by F-1) | KILLED ("consuming a utility from a package in neither package.json nor the lock is an undeclared-dependency edge") | **B's kill ITSELF KILLED — superlative reinstated, qualified** | B's kill premise is the same working-tree misread. At both commits the package is declared; consuming `.preserve-3d` from the design system rather than re-declaring it locally is the right idiom. Qualification survives: the working-tree deletion (above) is the only thing standing between this superlative and unconditional. |
| **T.A5 quantization superlative** (L·S-3/C·S-4/D·S-1) | ADOPTED as superlative ("claims REPAINTS — which is precisely what it buys") | KILLED — `CubeTarget.css:79` `transition: --lit 160ms linear` pins the repaint rate to the transition, not the write rate | **B wins — my own derivation** | CubeTarget.css:79 read by this seat: the shorthand sits on `.cube-side`, the very element the quantized write lands on; `@property --lit` is `inherits: true` (:6-10) and `.face-relit` consumes it through `color-mix` + two gradients (:126-141). Every distinct 0.01 step (re)starts a 160 ms UA interpolation whose inherited per-frame values recompute the child's background — during a continuous orbit at least one face is always inside a transition window, so repaint activity is pinned by the transition machinery regardless of `toFixed(2)` vs `(3)`. The comment's "order-of-magnitude fewer real repaints" cannot follow; the WRITE/invalidation elision is real. Reader A re-adopted the corpus's claim without reading line 79; the C axis's own S-4 cites the transition **in the same breath** as the repaint claim — a recorded citation-blindness event. Net repaint delta = SS-13. |
| **D·m-9 (the lying cursor)** | KILLED (input is NOT dead — the drag lands via `.idle-hover` → container; `cursor: move` stays honest) | CONFIRMED as REWRITTEN (no busy treatment; 1200/1100 slack; cursor inherited) | **A's kill stands for the claim AS FILED; B's rewrite folds as residue** | My reads of CubeTarget.css:22-24, CubeTarget.vue:17-27, OrbitalDrag.vue:2/:280/:346-352: with `.cube` out of hit-testing the hit falls to `.idle-hover` (no pointer-events rule; OrbitalDrag's scoped `div` rule cannot reach slotted content, but `cursor` INHERITS from the container) and bubbles to `containerRef` — a mid-roll drag starts normally, so the pointer does not promise dead input. Residue (no busy/aria-busy/dimming; the hand-tuned 1200 ms over an 1100 ms arc) folds into the release-family row (roster #20). ONE identity, MINOR. |
| **L·i-1 mechanism** | repeated L's version ("a cancelled first tap leaves `lastTapAt` seeded") | CORRECTED — backwards; `lastTapAt` is written ONLY in the pointerup arm; the real hazard is a cancelled DRAG skipping the `moved` reset | **B wins — my own read** | useDoubleTap.ts:70-83 whole: both `lastTapAt` writes AND the `moved → lastTapAt = 0` reset live inside the pointerup handler; a cancelled pointer fires no pointerup, so it can neither seed nor launder — what survives cancellation is a PRIOR genuine tap's seed, laundered only if the second tap still lands within the 300 ms window. Narrow; INFO stands with B's mechanism. A's verbatim repetition of L's backwards cell is a recorded citation-inheritance event. |
| **D·B-1 severity** | BLOCKER (1.7× figure "reproduced to the digit") | MAJOR (sign source-decidable; ratio is a modelling claim) | **MAJOR** | The component functions; the signature material inverts in the default (light) arm — a quality inversion, not a functional break. Sign proven by construction (the veil is `var(--background)` = near-white in the light arm; both channels lighten); the 1.67× magnitude and per-`--lit` composites are blend-model claims → SS-13. |
| **C·D-2/D·M-4 reachability** | confirmed with the masking note | MECHANISM confirmed, stated SYMPTOM killed ("there is no first roll") | **B's rescope adopted: CONFIRMED-LATENT** | Both readers agree on every mechanical link (absolute `from`, `replace` by omission, no model write-back, the `useTransformState` painter re-targeting the same `.cube` — all re-verified by this seat at :197-215/:207-211 with `targetRef` seated from `cubeTargetRef.value?.cubeEl`, CubeScene.vue:205-210). The observable snap is unreachable until B-1 AND B-2 are cured; ordered strictly after, one continuity row. |
| **Nested-object census cell** | "grep → only useSquareDemo + CubeTarget" | same cell | **BOTH cells corrected; the claim survives** | My `grep -rln "transform: {" demo/` → THREE files: the third is `useEasingDemo.ts:290` — a nested **type parameter** on a `.fromString()` construction (CSS-string path, never `parseAndFlattenObject` over an authored object), so it is benign. CubeTarget.vue:213-214 remains the sole nested-object-literal-with-default-renderer call site; quote the corrected census in any repair order. |
| **acos row (B's net-new MAJOR)** | not raised | filed MAJOR with the full NaN chain | **ADOPTED + SHARPENED by this seat** | Verified at disk AND `origin/master` (:69-75): radians into a degrees-typed field; Euler-from-diagonals is not a decomposition (m00 = cos y·cos z); scale bounds `[0.1, Infinity]` (index.ts, my read) make m00 > 1 ordinary, so `Math.acos(>1)` = NaN on the Reset path (CubeScene Reset → `animateUpdateMatrix(from, identity, true)` → per-frame `syncTransformations(true)`, :119). **Sharpening (this seat's own):** each garbage write also trips the deep watcher (:197-215), whose rAF `updateTransformations()` rebuilds `matrix3dEnd` FROM the garbage rotate+scale and repaints the target — the debounced painter and the 500 ms reset animation fight over `matrix3dEnd` for the whole arc, a second-order churn neither reader named. |

## Adjudicated defect roster

Identity-deduped: one row per defect, all axis ids attached. Anchors = the dirty working tree, which for every demo file cited **≡ `origin/master`** (substrate ruling); engine anchors cite the `origin/master` commit paths. Routing targets = census wave sketch (KF.W0–W10); **NO-WAVE-OWNER** rows are SS-1/SS-2 spec-authoring intake — they agglomerate into a *cube scene-repair packet* the forming specs must home (census §1: no wave owns demo scene behavior; the kf-AmigaScene record already banked the same gap).

### BLOCKER

- **1 · L·B-1 — CONFIRMED (BLOCKER).** The Roll's trigger is unreachable: `useOrbitalPointer.ts:222` captures every pointer to OrbitalDrag's CONTAINER unconditionally; `.cube` is a slotted descendant; `useDoubleTap` advances `lastTapAt` only in its pointerup arm on `cubeEl` (:70-83), and pointer capture retargets every subsequent event to the capture element — the descendant is off the path; `releaseCapture` runs from the document handler during the dispatch that already excluded `.cube`. The asymmetry is exact: `useDragScrub.ts:120` captures on `el` itself, which is why the Square/Spring eggs live. Zero gate touches any of the three eggs (my grep, exit 1). **NO-WAVE-OWNER (cube Roll-repair packet: capture on the element that listens, or listen where the capture lives). One-line live falsifier = SS-13 (optional — spec-derivation is unambiguous).**
- **2 · L·B-2 — CONFIRMED (BLOCKER; re-proven at `origin/master` by this seat).** The Roll's effect is a CSSOM no-op: the nested `{ transform: { rotateX } }` with the DEFAULT renderer flattens to dotted keys (`compile/value/compile.ts:61`) which `transformTargetsStyle` hands to `setProperty("transform.rotateX", …)` — silently discarded (:270-284). `unflatten` stays false absent a custom renderer (`engine/css/animation.ts:62/:113`); the flat-map branch is `interpolate.ts:304-307`; WAAPI cannot rescue (no CSS twin for `ease-out-back`; both readers verified, adopted as corroborated). Survives the engine refactor that deleted every line number the corpus cited — the mechanism is structural. Sole such call site in the demo (census cell corrected, ruling above). **NO-WAVE-OWNER (same packet; the cure is the sibling's own AST idiom — `useCubeDemo.ts:84-87` `transformList(transformCall("rotateX", …))`).**

### MAJOR

- **3 · MISSED-B1 (adopted from reader B; sharpened) — CONFIRMED (MAJOR).** `useTransformState.syncTransformations(reset=true)` writes `Math.acos(diagonal)` — radians, wrong math, NaN-unguarded — into the degrees-typed `rotate` consumed by `useCubeRelit` (×DEG) and `quaternionEuler` (×DEG2RAD); the same diagonals are also assigned as `scale`. Reachable in one click (Reset); self-heals at t=1, which is why it shipped. Plus this seat's watcher-feedback sharpening (ruling table). All three axes read this file; none flagged it. **NO-WAVE-OWNER (matrix-editor repair: a real decompose — the origin tree's new `transformMath.ts` is the natural home — plus a domain clamp).**
- **4 · L·M-1 — CONFIRMED (MAJOR).** `KEY_LIGHT = [0.45, 0.6, 0.66]` has +Y in a Y-DOWN frame: all six FACE_NORMALS re-derived from the CSS by both readers and cross-checked by this seat's arithmetic — at rest top `--lit` 0.20, bottom 0.80; the die is uplit while the comment says "up-and-right". The scene's own test asserts only the Z axis (front>back — my read of cube-scene.test.ts:54-59), so the flip is uncovered. One-character fix. **NO-WAVE-OWNER; SS-13 perceptual check under the graph's `rotate3d(-1,1,0,30deg)` attitude (the relit model ignores that constant tilt — the "room" is the graph frame, noted for whoever captures).**
- **5 · L·M-2 / C·D-2 / D·M-4 — CONFIRMED-LATENT (MAJOR).** The roll's terminal pose is never committed to the model; both frames absolute under `replace`; the matrix painter obliterates the landed pose on the next pre-start model change; 5-of-6 landings non-identity ⇒ roll #2 opens with a ≥90° cut. The `:219-221` "re-bases as usual" comment is false for the next roll. Unreachable until #1/#2 are cured — ordered strictly after, one continuity row. **NO-WAVE-OWNER.**
- **6 · L·M-3 / D·M-5 (+ MISSED-B5 folded: the `:168-170` composition promise is FALSE pre-start — the roll would REPLACE the drag-painted `matrix3d` on `.cube`, since pre-start the orbit lives on `.cube`, not the container) — CONFIRMED (MAJOR).** Three-to-four uncoordinated writers own `.cube`'s inline transform (roll · the group's three animations · the rAF-debounced matrix painter), switched against a second rendering authority on the container by `isPlaying || isStarted`, with nothing clearing `.cube` when the switch flips. B's sharpening adopted: WITHIN the group the compositor does arbitrate; the unarbitrated set is (group rAF) × (roll rAF) × (painter rAF). **NO-WAVE-OWNER (one transform-ownership spec row).**
- **7 · L·M-4 — CONFIRMED (MAJOR, library-side).** The default DOM renderer silently discards object values AND unsupported property names (`compile/value/compile.ts:270-284` at origin — no diagnostic on either arm) in a library whose stated posture is fail-explicit (`engine/options.ts` header; the easing seam throws typed errors) and which already carries a structured `diagnostics` channel. The enabler for #2; `Vars<T = any>` makes the misuse invisible to `npm run check`. **NO-WAVE-OWNER (library apply-seam: a dotted-key/object-value diagnostic row — pairs with #2's cure; distinct from KF.W3's R1 parser row, no identity overlap).**
- **8 · D·B-1 (ruled MAJOR) — CONFIRMED.** The re-lit die's shadow model inverts in the LIGHT theme — the "shadow" veil is `var(--background)` (near-white, `hsl(40 30% 98%)`), so an away-face renders brighter than a lit face; the light arm is the DEFAULT (`:root { color-scheme: light }`; `.dark` added only by the boot script). Sign source-decidable; magnitude = SS-13. **KF.W6 (theme-discipline packet: the veil must be a DARK mix in both arms — e.g. shadow from a fixed dark token or `color-mix` against black, never `--background`).**
- **9 · D·M-2 (+ MISSED-A6 folded as the ROOT CAUSE; prescribed cure KILLED — register #9) — CONFIRMED (MAJOR).** Three of six numerals fail the WCAG 1.4.3 3:1 large-text floor in the dark arm (green 1.71 / yellow 1.35 / cyan 1.56 — both readers re-derived independently to two decimals; adopted as corroborated); the light arm passes on an 11 % technicality. Root cause (reader A's net-new, premises verified by this seat at style.css:148-153): `--face-1…6` are the six raw sRGB corners at 0.8 α — a 12.7× luminance spread across ONE object in an otherwise perceptually-parameterised token graph; the "hue-EXACT" hoist preserved an untuned palette (this also qualifies D·S-4's hoist praise). The honest cure is a per-face-aware foreground AND/OR re-cutting the crayons at a common oklch lightness — **not** `on-glass-fg.css`, whose four fixed muted-ink tokens this seat read whole (register #9). **KF.W6.**
- **10 · D·M-1 / C·D-1 — CONFIRMED (MAJOR).** The Roll omits `respectReducedMotion` (engine default false — defaults.ts:87, my read); glass-ui's global PRM kill reaches CSS `animation`/`transition` only and the Roll is an inline per-frame `setProperty` write — the one motion in the scene no CSS kill can reach, beside a sibling that hand-rolls the guard (`useCubeDemo.ts:162-166`, my re-anchor) and a stylesheet asserting "Reduced-motion was already still" (false). Reader A's corrected-falsifier discipline verified and adopted. Currently masked by #1/#2 — noted, severity held (the cure is one option). **KF.W9 (PRM mechanism unification — same wave as the AmigaScene D-7 family; NEW instance, distinct call site, booked here).**
- **11 · D·B-2 (ruled MAJOR) + MISSED-A10 (discoverability) — CONFIRMED; FOLD-BY-REFERENCE.** One a11y hit in the whole tree (`aria-hidden` on the relit span); no name, role, tabindex, keyboard path (x/y/z are drag MODIFIERS only), or focus treatment on the scene's protagonist; MatrixEditor is not an equivalent path (gated + drives raw cells). The sighted-user half (reader A's net-new, verified: `cursor: move` — the translate affordance — is the ENTIRE discoverability story for five gestures; the one hint `App.vue:50` covers one and disappears on start). **IDENTITY: the kf-AmigaScene D-2 ruling already banked the cure as a SHARED subject-a11y idiom "for cube AND amiga" — this row folds into that identity, contributing the cube particulars (die-state name, axis-lock state, roll affordance, gesture legend). NO-WAVE-OWNER (that shared row); KF.W9 witnesses.**
- **12 · D·M-3 — CONFIRMED (MAJOR).** An indefinite spinner means "nothing selected" (both terms synchronous — CubeScene.vue:20, my read); no `role="status"`/label/live region; Lucide emits no aria at all; under PRM the glass-ui kill freezes it into a motionless spinner reading as "hung" (reader A's self-retraction verified and adopted — the §5.5 discipline is the corpus's best habit). **NO-WAVE-OWNER (loading-state packet with #21/#22).**
- **13 · D·M-6 / L·m-8 — CONFIRMED (MAJOR).** The x/y/z latch rides unguarded `window` keydown/keyup (`useOrbitalPointer.ts:169-176`, my read — no target/editable/dialog check) with NO blur/visibilitychange reset; shift/ctrl/meta self-heal via `syncModifiers` on every pointer event (:62-66) — the asymmetry proves oversight. Since P.W5.S3 a stuck latch lights a 1000vw axis line and silently single-axis-constrains every subsequent drag. **NO-WAVE-OWNER (latch-trust row: focus guard + blur/visibility reset; CubeAxisLines is the component that made it visible).**
- **14 · D·M-7 — CONFIRMED (MAJOR).** `ppMode` deletes numeral, crayon, lacquer, rim, and the whole re-light at once; two unlabelled decorative divs; all six faces identical; plates overhang the rounded corners (no radius, no overflow clip — brand.css read); `--lit` still computed and bound, consumed by nobody. **NO-WAVE-OWNER.**
- **15 · C·D-3 — CONFIRMED (MAJOR; consolation clause killed — register #14).** `.cube--rolling { pointer-events: none }` does not lock the drag (bound to the container; the hit falls through `.idle-hover`) and — B's sharpening, adopted — suppresses nothing else either: `useDoubleTap`'s listeners never fire under #1, and the `rolling` guard at `:190` already exists. The rule achieves nothing; its comment misleads. **NO-WAVE-OWNER (delete or move the lock to the element that owns the listener).**
- **16 · C·D-4 — CONFIRMED (MAJOR).** `faceLit` is a pure computed over `transform.value.rotate`, whose only producers are the drag/slider paths — no engine writer touches the model, so the "pinned" light welds to the die through the tumble and the whole Rotations playback. Perceptual magnitude = SS-13. **NO-WAVE-OWNER (decide: track engine frames, or document drag-only — "do not leave it silently half-lit").**

### MINOR

- **17 · C·D-5 / D·m-6 — CONFIRMED.** `30cqb` has no eligible block-axis container (`.controls-layout` is deliberately `inline-size`-only; grep: `size` only in SpringHeatmap; glass-ui dist declares none) → falls to 30 svb; width tracks the card, height the viewport; `AnimationControlsGroup.css:22-24` asserts the opposite in writing. Reader A's corrected ancestry (cqi DOES resolve) adopted; slot chain closed by reader A's trace. No identity collision: the value.js layout-gestalt cqb rows concern a different repo's shell. **NO-WAVE-OWNER (shell/layout hygiene + the stale sentence).**
- **18 · C·D-6 — CONFIRMED.** The latch payload is consumed lossily (modifiers dropped), so Shift+X (translate lock) lights a line documented on both sides as a ROTATION lock. **NO-WAVE-OWNER.**
- **19 · C·D-7 — CONFIRMED.** The scoped-slot publication of `pressedKeys` — whose comment names this exact consumer — is dead API repo-wide; the emit is the defensible survivor (CubeAxisLines renders outside the slot). **NO-WAVE-OWNER.**
- **20 · C·D-8 / L·m-2 + D·m-9-residue + MISSED-B2 (floating promise) — CONFIRMED (one release-family identity).** `rolling` raised before a fallible await with no try/finally; the 1200 ms timer never captured/cleared (`onScopeDispose` stops only the anim); `rollAnim.play()` is a bare floating promise (:218 — `play()` is async and can reject; the house idiom two files over is `void …play(…)`, and the engine's own completion promise would delete the hand-tuned 1200/1100 pair AND the no-busy-treatment residue in one move). C's honest-reachability note verified (engine warmed pre-mount; memoized) — hence MINOR. **NO-WAVE-OWNER (one cure: `try { await rollAnim.play() } finally { rolling.value = false }`).**
- **21 · C·D-9 — CONFIRMED.** The loading state is a singleton bespoke idiom (4 grep lines, all this file); glass-ui ships `Progress` `indeterminate`. Extends census S-6 (booked once, here). Ordering note re-scoped by the substrate ruling: the dep is DECLARED at origin, so "blocks on F-1" reduces to "blocks on the KF.W0 reconciliation". **KF.W6 (evaluate, not mechanical swap).**
- **22 · C·D-10 + MISSED-A9 — CONFIRMED.** The loader renders INSIDE the preserve-3d die on its z=0 plane, larger than the die (30cqi×30svb vs 225 px), faces ungated on `showLoader` — and (A's net-new, verified) it is transform-COUPLED to the subject: a pre-start drag rAF-paints `matrix3d` onto `.cube`, so the "loading" placeholder tumbles in 3D with the die it stands in for. **NO-WAVE-OWNER; legibility = SS-13.**
- **23 · L·m-1 — CONFIRMED.** Dead `animation` class on `.cube` — no rule in the loaded cascade. **NO-WAVE-OWNER.**
- **24 · D·m-1 + MISSED-A2 (the cascade truth) — CONFIRMED.** The Tailwind transition triple on `.cube-side` is not merely unmatched — it never APPLIES: CubeTarget.css is UNLAYERED (my read of style.css's layer skeleton: Tailwind + the demo's own overrides are all layered; the SFC sheet is not), and `:79`'s `transition: --lit 160ms linear` is a SHORTHAND that resets transition-property/duration/timing outright, unlayered-beats-layered. Changes the fix (delete the three classes) AND arms a guard: folding the SFC styles into `@layer components` would flip the winner and silently kill the `--lit` interpolation — with zero gate coverage. **NO-WAVE-OWNER (with the @layer guard clause).**
- **25 · D·m-4 — CONFIRMED.** The die shrinks 21.9 % at the 1024 px breakpoint as the stage widens; the desktop cap (15 rem) sits below the tablet cap (18 rem) against the tree's own "protagonist size" prose. **NO-WAVE-OWNER.**
- **26 · D·m-5 — CONFIRMED.** Numeral/face ratio unlocked by construction (viewport type ramp vs scene-local min() clamp, no shared token); 17→24 % swing. **NO-WAVE-OWNER (with #25 — one sizing row).**
- **27 · D·m-2 + MISSED-A8 — CONFIRMED.** `justify-items-center` is inert on the two flex containers — and on `.cube` ALL THREE centring utilities plus `display: flex` itself are inert (every child absolutely positioned; centred by `inset: 0; margin: auto`, my read). **NO-WAVE-OWNER.**
- **28 · D·m-3 / L·m-3 / C·D-14 — CONFIRMED (characterisation corrected — register #11).** Stage wrapper duplicated parent-to-child; three stacked non-passive wheel handlers. NOT byte-equivalent: CubeScene's copy carries the load-bearing `cube-stage--hero-recede` conditional — any dedup must hoist `relative` and keep the conditional. **NO-WAVE-OWNER.**
- **29 · D·m-7 — CONFIRMED.** `--z-content` (the semantic editor rung; glass-ui-owned, B's provenance correction adopted) spent on a doubly-redundant local rung three lines from the `z-10` exemption essay. **NO-WAVE-OWNER.**
- **30 · D·m-8 / C·D-13 — CONFIRMED.** `@property --lit` / `--axis-active` are document-GLOBAL registrations from `<style scoped>` blocks; `--lit`/`--color` are the most generic names in the scene tree; instantiates census §6.3 (no `--kf-*` namespace). No collision today. **KF.W6 (namespace lane; rides the MISSED-H guard precedent from kf-AmigaScene — prefix moves must account for `@property` re-registration).**
- **31 · D·m-10 — CONFIRMED.** `1000vw` bare literal against the layout doctrine; a non-`none` `filter` declared unconditionally at rest on three full-stage boxes with `.axis-line--locked` already computed as the free gate. Paint cost = SS-13; doctrine breach proven. **NO-WAVE-OWNER.**
- **32 · D·m-11 — CONFIRMED.** No forced-colors story: face identity is background-color alone, axis identity border-color/opacity/filter; the dashed→solid tell is the sole survivor (state, not identity). ppMode makes it terminal (#14). **NO-WAVE-OWNER; SS-13 under KF.W9's audit.**
- **33 · D·m-12 + MISSED-A4 + D·i3 (the stale-prose family, one row) — CONFIRMED.** Eight proven-stale assertions in one component's blast radius: the deleted idle-bob narrated as live (:57); "comes alive on … hover" with zero hover motion — the promotion rule fires for an animation that does not exist (A's net-new, verified: the only hover rule is the will-change gate; `hoverAnim` is a group member, not a hover); dead `font-bold` (three ways); "Reduced-motion was already still"; "re-bases as usual"; "COMPOSES with whatever orbit"; the ACG cqb sentence; `useCubeRelit`'s "up-and-right". `.idle-hover`-as-dead-class KILLED (register #10) — it is the live half of the will-change gate; a rename, not a deletion. Census-stale z rows fold at INFO #51. **NO-WAVE-OWNER (comment-truth sweep; the D·i3 pattern row is its rationale).**
- **34 · MISSED-A3 — CONFIRMED.** OrbitalDrag re-introduces a RESIDENT `willChange: transform` on the die's 3D ancestor for the entire post-start session (`containerStyle` :72-73, my read; `isStarted` latches on first Play) — the exact G5 anti-pattern the component's own stylesheet condemns and dismantled per-face. Qualifies D·S-3. Layer magnitude = SS-13. **NO-WAVE-OWNER.**
- **35 · MISSED-A5 — CONFIRMED.** It is not a die: opposite faces sum 4/6/11, never 7, under the component's own "The cube IS a six-faced die (1–6)" claim and a Roll egg built on the die reading. One-line `cubeSides` reorder + `ROLL_FACES` comment relabel; both readers verified the ROTATIONS and neither checked the NUMBERS. **NO-WAVE-OWNER.**
- **36 · MISSED-A7 — CONFIRMED.** The re-light is room-frame in one channel of three: only the SCALAR is room-frame; the specular's position (78 % 18 % face-local), the lacquer angle (145deg baked) and the veil direction (180deg face-local) rotate WITH the face — the `.bottom` face darkens its own "down" edge toward the viewer. The orthogonal half of #16 (geometry welded even when #16's mechanism works). **NO-WAVE-OWNER (one lighting-truth row with #4/#16).**
- **37 · MISSED-B4 — CONFIRMED.** `TransformState.matrix` is write-once dead data: declared, cloned, persisted across scenes (CubeScene.vue:224 — my read, the sole `.matrix` reach), never read or written by anything that renders. The correct residue of the killed L·m-4 (register #7). **NO-WAVE-OWNER (delete the field or document the persistence intent).**
- **38 · MISSED-B6 — CONFIRMED.** Zero test/gate coverage for all three `useDoubleTap` eggs (my grep, exit 1) — the full causal story for how a 100 %-dead trigger shipped; the one cube unit test locks exactly the axis (Z) that is correct and not the one (Y) that is flipped; `useDoubleTap.ts:16-18` anticipates a gate that does not exist. **NO-WAVE-OWNER (gate-authoring input for the Roll-repair packet: two real down/up pairs, assert the tumble).**
- **39 · L·M-5 — CONFIRMED-RESCOPED (MAJOR → MINOR annotation).** The four no-fallback glass-ui reaches (`--background`, `--z-content`, `duration-panel` — the demo's sole use — and `text-display-2`) are real cascade-exposure facts and stand as the zero-import ≠ zero-exposure annotation (with `.preserve-3d` ×4); their SEVERITY was inherited from the substrate-dead B-3 trigger and falls with it. Rides the F-1 identity, never re-booked. **KF.W0 annotation + KF.W6 token hygiene rider.**

### INFO

- **40 · L·m-9 — CONFIRMED.** The quantization elides (some) repaints, never renders — fresh array per recompute; B's sharpening adopted: Vue's `patchStyle` sets every key unconditionally, so all six `setProperty` calls fire per render regardless. Pairs with the killed T.A5 superlative (register #12). **NO-WAVE-OWNER.**
- **41 · C·D-11 — CONFIRMED.** The model contract is "hand me a mutable object" — the child writes through it, `update:transform` is never emitted, the chain rides one shared object identity; `useTransformState`'s `isGroupPlaying` parameter is dead (my whole-file read: one occurrence, its own signature). **NO-WAVE-OWNER.**
- **42 · C·D-12 / L·m-7 — CONFIRMED.** `props.isPlaying || props.isStarted` has a provably dead term (`isPlaying ⇒ isStarted` at the shell); the complementary gate is written differently in another file with no shared constant; `props.`-vs-bare split in one template. **NO-WAVE-OWNER.**
- **43 · L·i-1 — CONFIRMED (mechanism = reader B's, ruling table).** No `pointercancel` in `useDoubleTap`; the narrow laundering window is a seeded single surviving a cancelled drag inside 300 ms. All three consumers. **NO-WAVE-OWNER.**
- **44 · L·i-2 — CONFIRMED.** Two sanctioned engine-access idioms in one folder; both correct per `kf-engine.ts`'s own doc; worth one comment line. **NO-WAVE-OWNER.**
- **45 · L·i-3 — CONFIRMED.** `--rotationX` cross-file contract with no back-reference; works via the element-aware resolve (B verified the resolve path; adopted). **NO-WAVE-OWNER.**
- **46 · D·i1 — CONFIRMED.** `contain: style` inert (counters/quotes only); its accidental virtue is that paint/layout containment here would re-trip the T.A1 flattening trap. **NO-WAVE-OWNER.**
- **47 · D·i2 / L·m-5 — CONFIRMED.** `<span class="contents" v-if>` wraps one child; `<template v-if>` is identical and cheaper. **NO-WAVE-OWNER.**
- **48 · L·m-6 — CONFIRMED.** `cubeSides` (untyped) and `ROLL_FACES` (typed ReadonlyArray) are per-instance compile-time constants under two conventions. **NO-WAVE-OWNER.**
- **49 · MISSED-B3 — CONFIRMED.** Two of the Roll's four options restate engine defaults (iterationCount 1, fillMode forwards — defaults.ts:79-89, my read) while the three that would matter (`respectReducedMotion`, the composite operator, the WAAPI-silently-refused easing) are the ones omitted or inert — the options object's signal-to-noise is inverted. **NO-WAVE-OWNER (rides #10/#5's cures).**
- **50 · MISSED-B7 — CONFIRMED.** glass-ui's PRM override strips the `--lit` and `--axis-active`/`filter` transitions by property-list replacement (the `!important` list excludes custom properties and filter), so both eggs lose their documented smoothing under PRM — and CubeAxisLines.vue:59's "PRM-respecting via the wrapper below" describes a wrapper that does something else. Defensible behavior, false prose, invisible from the component. **NO-WAVE-OWNER (comment truth; KF.W9 adjacent).**
- **51 · D·§7 / L·S-6 census correction — CONFIRMED.** `lane-frontend.md:253/:442` and `style.css:37-39` all still record CubeAxisLines' raw `z-index:-10`; the tree reads `var(--z-behind)` (:67, my read). The census inherited the staleness from the contract prose — book the correction once, here. **KF.W0 (census/comment reconciliation).**
- **52 · MISSED-B8 — CONFIRMED.** OrbitalDrag's `defineModel` default is a shared mutable module singleton with a shallow `Object.assign` repair that would alias its nested objects for every future instance — unreachable from CubeTarget (`required: true`, populated), a latent trap for the next consumer, made invisible by #41's contract. **NO-WAVE-OWNER.**

## Killed-claims register (refutations, quotable)

1. **L·B-3 as BLOCKER (and reader B's re-confirmation at MAJOR: "absent from both package.json and package-lock.json").** SUBSTRATE-DEAD. My git: stale HEAD declares glass-ui 6.0.0 in `optionalDependencies`; `origin/master:77` declares it as an EXACT 7.0.0 devDep matching the installed 7.0.0; the absence is an **uncommitted working-tree hunk**. Reader B re-verified a dirty working tree and reported it as the manifest — the same lesson as the AmigaScene C-1 event: *a "re-run today" is worthless without naming the tree.* ONE identity: census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1; fold by reference, discharged by KF.W0 §B-12. New live residue booked there: the uncommitted deletion itself.
2. **Reader B's kill-mechanism for the flat-cube scenario ("43 bare glass-ui imports → Vite fails, so no silently-flat state exists").** Dies with #1's premise: at both commits the manifest declares the package and `npm ci` installs it — the build neither fails nor flattens. The kill's OUTCOME (no flat die at a fresh clone) is right; its mechanism is the same working-tree misread.
3. **Reader A's net-new MAJOR (the 6.0.0 `optionalDependencies` pin + two-major drift + silent-skip bucket).** True ONLY of the stale checkout; at `origin/master` the pin and the install agree (exact 7.0.0 devDep — the optional bucket is gone). Demoted to a KF.W0 stale-checkout note. Recorded with respect: A was the only party in five documents to run `git show HEAD:package.json`.
4. **Reader B's kill of D·S-4 (the preserve-3d superlative).** The kill's factual premise ("appears in neither package.json nor the lock") fails at both commits — killed; the superlative is REINSTATED as qualified (right idiom; declaration lives at origin; the working-tree deletion is the only cloud).
5. **C·§5's F-1-exemption row ("CubeTarget … one of the 21 unaffected files").** Zero-import ≠ zero-exposure: `.preserve-3d` ×4 plus #39's four token reaches route through the style cascade the shadow census never enumerated, and the component's mount chain (CubeScene, App, style.css) imports glass-ui regardless. Reader B's structural half adopted; its manifest half re-scoped by #1.
6. **L·M-5 at MAJOR.** Severity inherited from the substrate-dead trigger; rescoped to the #39 annotation.
7. **L·m-4's mechanism ("`transform.value.matrix` is a reactive Proxy over a typed array — 16 traps per write").** Vue never proxies a Float32Array (`targetTypeMap` → INVALID → raw target returned; `traverse` skips it). Both readers killed it identically with the same `@vue/reactivity` citations — adopted as corroborated. The true residue is roster #37.
8. **D·m-9 as filed ("the cursor lies … input is dead").** Input is NOT dead: the drag lands via `.idle-hover` → container and runs normally mid-roll (the same mechanism #15 proves). `cursor: move` stays honest for the gesture that works. Residue (no busy treatment; 1200/1100 slack) folds into roster #20.
9. **D·M-2's prescribed cure ("`on-glass-fg.css` exists precisely to pair text to the surface beneath it").** This seat read the file whole: four FIXED muted-ink tokens (`--on-glass-muted`, `--on-glass-muted-strong`, `--input-on-glass`, `--progress-track-on-glass`) — none derived from any surface; all would fail against the dark faces. The defect stands in full; the remedy would ship a second failure. The ColorSpaceSelector addendum law re-proven: *the cure is adjudicated separately from the defect.*
10. **D·m-12's sub-item ("`.idle-hover` … carries no rules of its own" ⇒ delete).** It is the live left half of `.idle-hover.playing .cube { will-change: transform }` — deleting it breaks the G5 promotion the same file argues for. A misnomer (rename), not a dead class.
11. **"Byte-equivalent" / "identical class string" characterisation of the duplicated wrapper (L·m-3/C·D-14).** CubeScene's copy carries the load-bearing `:class="{ 'cube-stage--hero-recede': props.hideLoader }"` (the J.W7a mobile hero fork) plus the `relative` delta. The duplication survives (roster #28); a dedup executed on "byte-equivalent" would drop the mobile hero-band split silently.
12. **The T.A5 quantization superlative (corpus L·S-3/C·S-4/D·S-1; reader A's re-adoption OVERRULED).** Defeated eight lines below its subject: `CubeTarget.css:79` `transition: --lit 160ms linear` on the write-target element, with the registered inheriting `<number>` feeding `.face-relit`'s color-mix + gradients — every quantized step (re)starts a 160 ms per-frame UA interpolation, so during a live orbit the repaint rate is pinned by the transition, not the write rate. The write-elision is real; the comment's "order-of-magnitude fewer real repaints" is not established and cannot follow from quantization alone. C·S-4 praised the transition in the same breath as the claim it nullifies — recorded as citation-blindness. Net delta = SS-13.
13. **Reader A's L·i-1 mechanism repetition ("a cancelled first tap leaves `lastTapAt` seeded").** Backwards — `lastTapAt` is written only in the pointerup arm; a cancelled pointer cannot seed. Reader B's inverse mechanism adopted (ruling table). A citation-inheritance event: the challenge's cell survived one hostile re-read unexamined.
14. **C·D-3's consolation clause ("what the rule DOES achieve is suppressing useDoubleTap's own listeners").** Nothing to suppress — those listeners never fire under #1, and the `rolling` guard at `:190` already exists. The rule achieves nothing (reader B's sharpening, verified).
15. **C·D-2/D·M-4's stated observable symptom ("5 of 6 rolls are followed by a visible snap").** There is no first roll; both axes spent a MAJOR on roll #2 of an animation that neither rolls nor paints. Mechanism CONFIRMED; symptom LATENT; ordered strictly after #1/#2 (roster #5).
16. **The nested-object census cell (both readers: "grep → only useSquareDemo + CubeTarget").** My grep returns THREE files; the third (`useEasingDemo.ts:290`) is a nested type parameter on a `.fromString()` path — benign, but the cell as written would fail re-verification. The "sole call site" claim survives corrected.
17. **D·B-1 at BLOCKER.** Ruled MAJOR — a quality inversion in the default arm, not a functional break; the 1.67× figure is a blend-model claim (SS-13), the sign is source-decidable and stands.

## Superlatives (provenance-pinned; L-18 runs both ways)

1. **Two hand-maintained parallel tables, both proven exact by independent derivation.** FACE_NORMALS 6/6 against the CSS face rotations (both readers derived independently; this seat re-checked the two `rotateX(±90)` rungs — the exact place the KEY_LIGHT bug becomes visible), and ROLL_FACES 6/6 including the counter-intuitive right/left sign inversion — the second table attached to an egg that has never once executed. Cross-file, cross-language, zero drift.
2. **One Euler convention across four implementations, no drift** — `litFor`'s sequential rewrites = Rx·Ry·Rz; the quaternion pair is its exact inverse; `useTransformState` builds the same product in mat4s. (Reader B's four-site extension of L·S-2, adopted.)
3. **The T.A1 scar comment is the general rule, not a patch note** — `filter` as a grouping property forcing used `transform-style: flat`, plus the refusal to reintroduce ANY compensating ancestor shadow. It also correctly powered the D axis's own §5.4 kill.
4. **Textbook LIGHT/HEAVY boundary discipline + the post-await unmount re-check** — type-only class import, runtime behind `loadAnimationEngine()` at point of need, `cubeEl` re-read after the await with the lock unwound, `onScopeDispose` matched (for the anim; the timer is roster #20). Matches the house precedent verbatim.
5. **`rollAnim` as a bare `let`** — the markRaw hazard sidestepped by construction; every sibling that parks engine objects in reactivity needs four `markRaw` calls.
6. **`useDoubleTap`'s `moveTolerance` reset** — the subtle half of drag-disjointness, present and correct; its tragedy is that on this one consumer it never runs.
7. **OrbitalDrag closes the two-way-binding echo loop by VALUE IDENTITY, not a flag** (`OrbitalDrag.vue:298-312`) — an echo of its own write is byte-identical and skipped; anything else re-seeds the quaternion, with the `flush` reasoning at the seam. Neither original axis recorded it; reader A surfaced it.
8. **D·S-4 reinstated, qualified (register #4):** consuming `.preserve-3d` from the design system and hoisting the crayons into named tokens is the right idiom — with the caveat that the hoist was hue-exact preservation of an untuned palette (roster #9) and the declaration's fate currently rides an uncommitted hunk (KF.W0).
9. **The corpus's retraction discipline** — the D axis's `[CORRECTED]`/§5.5 self-kills all verified out and one UPGRADED a finding (the PRM falsifier run against the resolved cascade); both readers replicated the chain. The habit this program should demand everywhere — and the counter-exhibit is register #12/#13, where un-retracted cells survived a hostile re-read by inheritance.
10. **The census is right about size and the carve is a clean seam** — 239/154 L confirmed exactly, the sourced-stylesheet split at the concern boundary, colocated sub-units within discipline; both readers went looking for a Goldilocks finding and agreed there isn't one.

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

1. **L·M-1 + D·B-1 in one capture:** the die at rest, both themes, under the graph's `rotate3d(-1,1,0,30deg)` attitude — is the upper face brighter or darker than the lower; does the light-arm away-face outshine the lit face. (The relit model ignores the graph tilt — the "room" is the graph frame; expect the key direction offset by 30°.)
2. **L·B-1's one-line falsifier (optional):** instrument `cubeEl.addEventListener('pointerup', …)`, double-click the die, observe zero hits.
3. **Register #12's net repaint delta:** paint profile of `toFixed(2)` vs `(3)` with `transition: --lit 160ms linear` live — which mechanism dominates.
4. **D·M-2's modelled column:** numeral contrast with lacquer + relit stack composited (the base column alone already carries the verdict).
5. **#22's loader legibility:** the spinner at z=0 inside the 0.8-α die — and its 3D tumble under a pre-start drag.
6. **#31 + #34 layer/paint costs:** whether the zero-parameter drop-shadow allocates a filter surface at rest on three 1000vw boxes, and whether the resident `willChange` pins a compositor layer wrapping the perspective subtree post-start.
7. **#17's drift salience:** the loader's mis-centring as cqi (card) and svb (viewport) diverge across window shapes.
8. **#25/#26 perceptual:** whether the die "stops reading as a die" across the 288→225 px cliff and the 17→24 % numeral-ratio swing (the geometry is confirmed arithmetic).

## Identity guards (fold-by-reference — never re-booked)

- **L·B-3 / L·M-5 / C·§5-row ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1** — ONE identity, FALSIFIED at HEAD-of-origin, discharged by KF.W0 §B-12. This record adds only (a) the cascade-exposure annotation (#39: `.preserve-3d` ×4 + four token reaches) and (b) the NEW working-tree-deletion residue (KF.W0).
- **D·B-2 ≡ kf-AmigaScene D-2's shared subject-a11y idiom row** ("for cube AND amiga" — that ruling's own words). Folded; the cube particulars (die-state name, axis-lock state, roll affordance, gesture legend, #11's discoverability half) are net-new content attached to that identity, not a second booking.
- **D·M-1/C·D-1 → the same KF.W9 PRM-unification wave as kf-AmigaScene D-7/C-15/L-m8** — distinct call site, NEW row, same wave; the census §6.5 tally amendment stays owned by that wave.
- **D·m-8/C·D-13 ≡ census FE §6.3 (no `--kf-*` namespace)** — instantiated, not re-opened; rides the kf-AmigaScene MISSED-H guard (documented-override policy + `@property` re-registration caveat added here).
- **C·D-9 extends census S-6** (third bespoke loading idiom) — the extension is booked once, here; the S-9 stage-surface row remains kf-AmigaScene's.
- **#51 ≡ the lane-frontend :253/:442 stale z rows + style.css:37-39** — one census-correction identity, booked once.
- **#17 has NO identity with the value.js layout-gestalt cqb rulings** (different repo, different shell) — checked against the adjudicated registry before booking.

## Routing summary (M-25 agglomeration input)

- **KF.W0**: the F-1 discharge + the working-tree reconciliation (the uncommitted glass-ui-devDep deletion + the src/ refactor-in-flight) + the #51 census/prose z correction + the re-anchor clause (demo surface ≡ origin; engine anchors re-cited at origin in this record).
- **KF.W6**: #8 (light-arm veil) · #9 (numeral contrast + palette root-cause; the on-glass-fg cure retired with rationale) · #21 (loading idiom, evaluate) · #30 (namespace/@property guard) · #39 (token-hygiene rider) — one coherent theme/token packet.
- **KF.W9**: #10 (PRM, intensity form per the AmigaScene precedent) + witnesses for #8/#9/#11/#32 + the SS-13 list above.
- **NO-WAVE-OWNER — the cube scene-repair packet (SS-1/SS-2 authoring must home it; no census wave owns demo scene behavior)**: the Roll stack #1·#2·#5·#15·#20·#38·#49 (one four-defect repair with its born-RED gate) · the transform-ownership row #6 · the lighting-truth rows #4/#16/#36 · the matrix-editor repair #3 · the latch row #13 · #12/#22 loading-state · #14 ppMode · #17 shell hygiene · #18/#19 latch seam · #23–#29/#31–#37 hygiene · all remaining INFO rows. Library-side: #7 (the apply-seam diagnostic — pairs with #2's cure; distinct from KF.W3's R1 row).

## Verdict

**ADJUDICATED.** The component is the program's cleanest case of *ornament outrunning verification*: two exactly-derived orientation tables, four-site Euler coherence, textbook async discipline — wrapped around a signature feature that has never executed (trigger dead by pointer-capture retargeting, effect dead by CSSOM discard, zero gate coverage), a lighting model whose one uncovered axis is the flipped one, and a matrix Reset that writes NaN-prone radians into a degrees-typed model every consumer trusts. The corpus survives hostile re-reading well (51 rows confirmed), but this component's kill register carries the program's sharpest meta-lesson twice over: one re-reader re-verified a dirty working tree as "the manifest" (killing and confirming the same defect family in opposite directions), and the corpus's most-quoted perf superlative was defeated by a line eight rows beneath the one everybody cited. Anchors here are origin-current for the whole demo surface; both BLOCKER mechanisms were re-proven at `origin/master` against the refactored engine. Nothing in this record stamps a wave: every disposition is agglomeration input for the forming X·KF specs, original ids for life.

— end of apotheosis. The challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
