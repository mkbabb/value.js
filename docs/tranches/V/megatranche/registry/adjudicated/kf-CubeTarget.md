Served model id: `claude-fable-5` (fresh Fable adjudicator, M-23/L-18 — the r2 apotheosis seat for kf demo component CubeTarget; receipt read from this seat's own system prompt, not inherited).

# kf-CubeTarget — adjudicated apotheosis r2 (M-23/L-18, fresh-Fable incredulity seat)

**Date** 2026-08-07 · **Subject tree** `/Users/mkbabb/Programming/keyframes.js` — HEAD `8281638c` (stale, 2026-07-28), `origin/master` `81a56990`, worktree dirty · **Registry tree** value.js `tranche-u` · **Corpus** `audit/kf-components/CubeTarget/challenge-{D-design,L-library,C-consumption}.md` (all three `claude-opus-5[1m]`), all three **re-read whole by this seat**.

**Corpus adjudicated (r2):** two fresh hostile re-reader returns over the full challenge corpus — **reader-α** (design-and-usage: 40 confirmation cells · 7 kills · 8 misses · 11 superlatives) and **reader-β** (library-and-code: 43 confirmation cells · 6 kills · 8 misses · 8 superlatives).

**Supersession (recorded, nothing lost).** This record supersedes the 2026-08-07 r1 apotheosis at this same path, which adjudicated a *different* reader pair. The r1 roster identities **#1–#52** and its 17-entry killed-claims register are **preserved for life** (anti-rename) and are carried wherever this corpus and this seat's instruments do not touch them. They are **corrected in exactly one defect family and one grade**: the matrix-painter family (rulings 1–2 below — r1's #5 obliteration arm, #6 writer census, #22 MISSED-A9 sub-claim, and #3 repaint sharpening all rested on a write that never happens) and #29 (regraded MINOR→INFO, ruling 6). Where r1 content is restated here it was **re-verified at the bytes**, not copied; carried-unrestated rows are marked by reference.

**Substrate (re-derived by this seat, not inherited).** The r1 triangulation **reproduces exactly**: `git show HEAD:package.json` → `optionalDependencies: { "@mkbabb/glass-ui": "6.0.0" }` (:70-72); `git show origin/master:package.json` → `"@mkbabb/glass-ui": "7.0.0"` exact devDep (:77); the worktree carries an **uncommitted hunk deleting the declaration** (grep of worktree package.json → no match; lock → 0). `git diff origin/master -- demo/scenes/cube/ demo/composables/useDoubleTap.ts demo/styles/` → **EMPTY** — every demo anchor cited below is origin-current. `src/` on disk differs from BOTH commits (86 files vs HEAD, 87 vs origin; disk keeps the `frame-compiler.ts`/`value-ast.ts` layout, origin carries the `compile/frame/`+`compile/value/` refactor) — so every load-bearing engine mechanism below was verified at **both** trees. **Both of this corpus's readers measured the dirty worktree without naming the tree** — the second corpus in a row in which a library-side reader filed the worktree as "the manifest" (ruling 3).

**Method (incredulity).** Every head-on disagreement between the two readers, every kill of a load-bearing cell, every claimed-new BLOCKER, and every miss admitted to the roster was re-derived against the bytes with this seat's own instruments before adoption. Zero product-source writes, zero installs, zero browser tooling; sole write = this file. Instruments this session: the git triangulation above (+ `git grep` at `origin/master`); whole reads of CubeTarget.vue (239 L), CubeTarget.css (154 L), useCubeRelit.ts (87 L), useDoubleTap.ts (84 L), CubeAxisLines.vue (90 L), useTransformState.ts (227 L, disk ≡ origin), transformMath.ts:30-124 (`createMatrix` — **the decider**), value-ast.ts:375-401 on disk **and** `origin/master:src/animation/compile/value/compile.ts:280` (the object-skip at both trees), useOrbitalPointer.ts:55-239, OrbitalDrag.vue:40-110 + :255-353, orbital-drag/index.ts:63-80 (`defaultTransformState`), CubeScene.vue:9-30/:55-80/:105-130/:200-260, useCubeDemo.ts:70-175, useSceneMachineShellBinding.ts:258-268 (the `isStarted` write-back — reader-β's citation verified REAL), useAnimationSync.ts:40-70, style.css:88-278 excerpts, brand.css:12-26, `@vueuse/shared` `tryOnScopeDispose` (:119-125, the no-op-without-scope decider), `reka-ui/dist/HoverCard/HoverCardTrigger.js` (`as` default `"a"`) + glass-ui dist `PopoverTrigger` hover-branch (extracted verbatim), useDragScrub.ts:116-130 (the contrast citations), main.ts:30/:50 (`warmKfEngine`), `constants/types.ts:39` (`Vars<T = any>`), `engine/animation.ts:424` (`async play(): Promise<void>`), test/compile/transform-style.test.ts (scalars-only — the gate gap), gate greps (`useDoubleTap|onRoll|cube--rolling` in test/+scripts/ → exit 1), and identity-guard greps + targeted reads across the banked registry (kf-CubeScene.md rows C-1/L-2/L-3/L-4/L-9/latent-cohort-D-1/MISS-3/MISS-4, kf-AmigaScene, census lane-frontend). Contrast arithmetic for the new axis-line row re-derived by hand. Where both readers independently re-derived the same figure set (the D·M-2 table, FACE_NORMALS 6/6, the KEY_LIGHT 0.20/0.80 pair, the 288→225 px cliff), double agreement + r1's third derivation is adopted as corroborated.

## Scoreboard

**59 adjudicated roster rows — 3 BLOCKER · 14 MAJOR · 27 MINOR · 15 INFO** (r1's 52 identities carried, one regraded, four cells corrected inside one family; **8 net-new rows admitted** #53–#60, of which one is the record's new headline). **25 killed-claims register entries** (r1's 17 carried + 8 new, including one **reversal of a double kill** — the sharpest event in the record). **16 missed rows adjudicated** from this corpus (8 became new roster rows, 8 folded into standing identities). **12 superlatives** (r1's 10 + 2 new). **8 → 10 UNPROVEN-NEEDS-LIVE residues** for SS-13.

**Headline.** The corpus's two Roll BLOCKERs (L·B-1 trigger-dead, L·B-2 paint-dead) survive a second hostile re-read intact — and this corpus adds a **third, wider one**: `transformTargetsStyle` is handed a `Matrix3dCall` **object** it is coded to skip, so the matrix/slider painter — the only pre-start writer of the die's orientation — **has never painted anything**. A pre-Play drag rotates nothing; the matrix editor's Reset moves no die; the home hero (where `isStarted` is never written) never responds to drag at all. Three challenge axes wrote ~2,000 words about this painter; two of them **used it to kill a true defect**; the r1 seat **verified the kill**; reader-β of this corpus is the only party in six documents to read the nine lines against the seventeen-line object they are handed.

Dispositions are **ADJUDICATED, not VERIFIED** — X·KF specs are still forming; every routing is M-25 agglomeration input for the SS-1/SS-2 spec authoring, keyed to the census wave sketch KF.W0–KF.W10. Original ids for life.

## Ruled disagreements and corrections (this seat's actual work)

| # | dispute | reader-α (design-and-usage) | reader-β (library-and-code) | RULING | deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| 1 | **The matrix-painter no-op** (β's missed #1, filed BLOCKER) | not raised — α **confirmed** D·M-5 *including* "useTransformState.ts:197-216 rAF watch → transformTargetsStyle(…) → `.cube`" as a live writer | the painter writes NOTHING: `matrix3dEnd` is a plain `{kind:"call",name:"matrix3d",args}` object and the renderer skips objects; kills L/M-2 + D/D-M5-core; REVERSES the L§7.3/D§5.2 double kill | **β ADOPTED at BLOCKER (roster #53); α's cell CORRECTED; r1 #3/#5/#6/#22 corrected** | This seat's own chain, both trees: `createMatrix` returns `{ kind: "call", name: "matrix3d", args: numeric.map(matrixScalar) }` (transformMath.ts:69-89, disk ≡ origin); both call sites hand it whole under the `transform` key (useTransformState.ts:121-125, :207-211); the renderer's second guard `if (value !== null && typeof value === "object") continue;` sits at disk value-ast.ts:396 **and** `origin/master` compile/value/compile.ts:280. The run loop is unaffected (it feeds serialized leaves — refreshAuthoredSink/`serializeCompiledValue`, value-ast.ts:376-384 — which is what makes the ":29-30 same painter the run loop drives" comment true-but-misleading). Reachability: `isPlaying`/`isStarted` init false (CubeScene.vue:65-66); sole `isStarted` writer is the transport write-back (useSceneMachineShellBinding.ts:264-265 — β's cite **verified real** at `demo/app/scene/`); home registers no adapter (App.vue:281) — the hero is **permanently pre-start**. The engine's only regression test for the seam is scalars-only (transform-style.test.ts:21-29, my read). |
| 2 | **The two challenge kills of "pre-start drag does nothing"** (challenge-L §7.3 withdrawn-claim 3; challenge-D §5.2 killed-candidate 2) | α re-verified challenge-D's kill implicitly by confirming D·M-5's painter cell | β: "both kills rest on the same false premise and must be reversed … a double kill of a kill" | **β SUSTAINED — the candidate was TRUE** | Same bytes as ruling 1. Challenge-L §7.3's own sentence — *"This same painter is what makes M-2 true"* — is the honest dependency that falsifies M-2 the moment the painter is falsified; challenge-C §5.2's "complementary by design" and r1's #22 "(A's net-new, verified) … tumbles in 3D with the die" both verified the call-site chain, never the write. Recorded as the program's cleanest **kill-inheritance** event: a false mechanism, cited approvingly by four documents, laundered one true defect out of the corpus twice. |
| 3 | **L·B-3 / the glass-ui family** | KILLED (build fails without the package, no flat-render state; fold to census F-1; "downgrade to INFO, do not double-count") | CONFIRMED BLOCKER — "package.json declares exactly ONE `@mkbabb` dependency … glass-ui appears in neither" | **α's kill SUSTAINED (route corrected); β's confirm SUBSTRATE-DEAD** | My triangulation (substrate block): the absence is an **uncommitted worktree hunk**; at HEAD the dep is a 6.0.0 optionalDependency, at origin an exact 7.0.0 devDep matching the installed 7.0.0. β re-verified a dirty worktree as the manifest — the **same event as r1's reader-B and the kf-CubeScene corpus's reader-B**: three independent library-side seats, one repeated instrument error. α's verdict is right but its route ("grep package.json → no match") is the same contaminated instrument — α reached the right answer because its kill leaned on the build-fails counterfactual, not the manifest. ONE identity: census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1, FALSIFIED at HEAD-of-origin, discharged by KF.W0 §B-12 — fold by reference (r1 register #1–#5 carried whole). |
| 4 | **D·m-9 (the lying cursor)** | KILLED — "input is dead" is false; drag lands via `.idle-hover` → container; doubly moot under L·B-1 | CONFIRMED at MINOR (no busy treatment, 1200/1100 slack) — while conceding in its own parenthesis "the input is not actually dead either" | **α's kill SUSTAINED for the claim as filed; β's residue folds** | r1 register #8 upheld on my own reads (CubeTarget.css:22-24; OrbitalDrag.vue:2/:280/:346-352; `cursor` inherits from the container). A confirmation whose proof concedes the claim's premise is a kill wearing a confirm's severity — the residue (no busy treatment; the hand-tuned 1200 ms) is already roster #20. |
| 5 | **Severity contests** | L·B-1 BLOCKER · D·B-2 MAJOR ("core function remains reachable via the transport") · L·M-1 MAJOR | L·B-1 MAJOR ("a documented easter egg is dead, no core flow is") · D·B-2 BLOCKER · L·M-1 MINOR ("the exact rest pair does not survive the graph rotation") | **L·B-1 BLOCKER sustained · D·B-2 MAJOR sustained · L·M-1 MAJOR sustained** — all three r1 grades hold; dissents recorded | L·B-1: the pair B-1+B-2 is the component's signature feature at 100 % dead with zero gate coverage — and with roster #53 admitted, "no core flow is dead" is no longer even true of the component, which weakens β's calibration ground. D·B-2: MAJOR per r1 #11's fold (the scene's core watch-function is transport-reachable). L·M-1: the sign error is proven wrong math regardless of the tilt; β's *insufficiency caveat* is adopted into the cure clause of #4/#56 — the one-character fix is necessary, not sufficient. |
| 6 | **D·m-7 (`--z-content` on `.face-numeral`)** | KILLED — "the claimed contradiction runs the doctrine backwards": using the semantic token IS the conforming form; residue a style preference at INFO | CONFIRMED at r1's MINOR with the provenance correction (glass-ui-owned token) | **α's kill ADOPTED; r1 #29 REGRADED MINOR→INFO** | My read of style.css:36-39's contract ("use the SEMANTIC z-* utility; do NOT introduce a raw z-[N]") against CubeTarget.css:144-146: the declaration conforms to the doctrine's letter; the surviving observation — a document-scale rung spent inside `.cube-side`'s own stacking context, three lines from the `z-10` exemption essay — is coherence commentary, not a defect. β's provenance half (r1 already adopted) stands. |
| 7 | **The doc-listener leak** (β's missed #2, filed MAJOR) | not raised | mid-drag unmount leaks three document listeners + the component closure; the :178-180 comment's `tryOnScopeDispose` promise is false | **ADMITTED, DEMOTED MAJOR→MINOR (roster #54)** | Mechanism confirmed whole by this seat: the `useEventListener(doc, …)` calls run inside `onPointerDown` — a DOM event callback where `getCurrentScope()` is undefined — and vueuse's `tryOnScopeDispose` is a hard no-op without a scope (`@vueuse/shared` :119-125, my read); the onMounted registrations (:263-292) by contrast dispose correctly (lifecycle hooks run under the instance scope). **But the durability is narrower than β claims**: the leaked document `pointerup`/`pointercancel` handlers remain live after unmount and *themselves* call `removeDocListeners()` at the next release (useOrbitalPointer.ts:194-216), so the leak self-heals at the first post-unmount pointer release; the permanent-leak state requires a gesture that ends with neither event ever dispatched. What survives at MINOR: a window of stale `drag()` writes into the shared model after unmount, the HMR/scene-switch closure retention until the next release, and a comment asserting a teardown mechanism that cannot run. |
| 8 | **Adjudicator's corrections register (against this corpus's readers)** | — | — | — | (1) α's D·M-5 confirmation cell names the painter as writer (a) — corrected, ruling 1; α re-verified the seat chain and inherited the write. (2) α's own killed-register arithmetic correction against challenge-D ("1000vw is TEN viewport widths, not 1000×") — **adopted**; neither the challenge nor r1 caught it. (3) β's L·B-3 substrate misread — ruling 3. (4) β's severity inflation on the leak — ruling 7. (5) This seat's own initial suspicion that β's `useSceneMachineShellBinding.ts` citation was phantom is **retracted**: the file exists at `demo/app/scene/` and :264-265 is the write-back, exactly as β cited — incredulity cuts both ways, and the check is recorded because an unchecked "phantom citation" charge is itself a citation-inheritance seed. |

## Adjudicated defect roster

**Carried whole from r1 (re-anchored, unrestated):** rows **#1–#52** stand as ADJUDICATED with their r1 severities, proofs, and routings, EXCEPT as corrected below. The full r1 text is superseded at this path but its rows are quotable by id; anchors remain origin-current for the demo surface (substrate block). Routing law unchanged: **KF.W0 / KF.W6 / KF.W9** where a census wave owns the surface; **NO-WAVE-OWNER** rows are SS-1/SS-2 spec-authoring intake (the *cube scene-repair packet* — no census wave owns demo scene behavior).

### Corrected r1 rows (the painter family + one regrade + corroborations)

- **#3 · the acos Reset row — CORRECTED + FOLDED-BY-IDENTITY.** The defect (Math.acos radians/NaN into the degrees-typed model, useTransformState.ts:69-75 — re-read this session at disk ≡ origin) stands, but it is **≡ kf-CubeScene L-3 (banked 2026-08-07, MAJOR)**, which owns `useTransformState` and carries the sharper escalation this record now adopts: the NaN fires the deep watcher → `mat4.fromXRotation(NaN)` → all-NaN composite → **`createMatrix` throws `TypeError` from inside the rAF callback** (transformMath.ts:78-83's own finiteness guard is the thrower). r1's "repaints the target … the debounced painter and the reset animation fight over `matrix3dEnd`" sharpening is **corrected**: the painter never paints (#53) — the fight is model-only (slider thrash + relit re-shading + the OrbitalDrag quaternion re-seed), and on the NaN arm it terminates in the throw. Fold to kf-CubeScene L-3; this record contributes the painter-no-op correction. **NO-WAVE-OWNER (the matrix-editor family row, owned at kf-CubeScene).**
- **#5 · roll-pose continuity — RESCOPED (MAJOR, CONFIRMED-LATENT).** The **obliteration arm is dead** (nothing repaints `.cube` pre-start — ruling 1); post-fix, a pre-start drag would leave a rolled pose *standing* while the shading shifts under it. The surviving latent arms: (a) roll #2 opens at the authored absolute `from` under `replace`+`forwards` — a ≥90° cut in 5/6 cases, engine-internal and real the moment #1/#2 are cured; (b) the group's first frame overwrites the rolled pose. Ordered strictly after #1/#2. **NO-WAVE-OWNER.**
- **#6 · transform ownership — RESCOPED (MAJOR).** The live writer set on `.cube` is **the group's three animations + the roll** (both engine rAF); the matrix painter is *authored as* a third writer and is inert (#53) — which is itself the sharpest fact in the row: the architecture has an arbitration problem AND one of its three arbiters has never once fired, so no gate ever observed the collision. The container remains the second rendering authority, switched by `isPlaying || isStarted` with no hand-off or clear (unchanged). The "same rotation composed twice → pop at first Play" mechanism stays killed (register #18). **NO-WAVE-OWNER (one transform-ownership spec row; must be re-derived after #53's cure, which adds the painter back as a REAL third writer).**
- **#22 · the loader placement — CORRECTED (MINOR).** The structural facts stand (sibling of the faces, z=0 centre plane, larger than the die, faces ungated on `showLoader`); the r1 MISSED-A9 sub-claim — "a pre-start drag rAF-paints matrix3d onto `.cube`, so the placeholder tumbles in 3D with the die" — is **killed** (register #21): nothing paints `.cube` pre-start. α's kill of challenge-D's "placeholder is 20 % larger than the thing it stands in for" *framing* is adopted alongside (nothing is stood in for — the die renders fully beneath the spinner). **NO-WAVE-OWNER; legibility = SS-13.**
- **#29 · `--z-content` on the numeral — REGRADED MINOR→INFO** (ruling 6). **NO-WAVE-OWNER.**
- **#34 · the resident container `willChange` — SUSTAINED (MINOR), sharpened.** Corroborated independently by β (missed #7: OrbitalDrag.vue:72-73, gate `isPlaying || isStarted` latches for the session). Cross-ref adopted from the banked kf-CubeScene L-2/C-5: **`isPlaying` has no writer anywhere**, so `.idle-hover.playing .cube { will-change }` — the component's own celebrated transient hint — **never fires**; post-start, the condemned resident ancestor hint is the *only* promotion that ever occurs. The S-3 superlative's qualification deepens accordingly. **NO-WAVE-OWNER.**
- **#42 · the dead disjunct — SUSTAINED (INFO), identity note added.** β's derivation (`isPlaying ⇒ isStarted` at useAnimationGroupPlayback.ts:54-56) is subsumed by the banked kf-CubeScene L-2/C-5 (no writer at all for the cube's `isPlaying`); one identity, owned there. **NO-WAVE-OWNER.**
- **Corroborated without change (this corpus independently re-found r1 rows):** **#20** (release family — β missed #8 re-proves `async play(): Promise<void>` at engine/animation.ts:424, my read; the `try/await/finally` cure stands), **#24** (the unlayered `transition:` shorthand clobber — both readers' missed rows fold in; the @layer guard clause stands), **#50** (the false "PRM-respecting via the wrapper below" comment — α missed #1 folds in; my read of CubeAxisLines.vue:58-63 confirms nothing below is a PRM wrapper), **#52** (the `defineModel` shared-mutable-singleton default — β missed #6 re-proves at OrbitalDrag.vue:43-49 + index.ts:63-80, with the `structuredClone` asymmetry at :60), **#13/#18** (the latch rows), **#17** (cqb/svb), **#33** (stale-prose family). Independent re-discovery by a fresh corpus is the strongest corroboration class this program produces; all six identities stay booked once.

### NEW rows admitted from this corpus (#53–#60)

- **#53 · BLOCKER — the matrix/slider painter is a silent no-op; the die's only pre-start orientation writer has never painted.** (β missed #1; ruling 1; every premise re-derived by this seat at both trees.) `useTransformState` hands `transformTargetsStyle` a `Matrix3dCall` **object** under the `transform` key at both call sites (:121-125 Reset/animate path, :207-211 the rAF-debounced drag watcher); the renderer skips objects (`value-ast.ts:396` on disk ≡ `compile/value/compile.ts:280` at origin) — so the write is skipped on every target, every call, forever. Consequences, all static-proven: a **pre-Play drag rotates nothing** (the quaternion moves, `transform.rotate` moves, all six faces re-shade via `faceLit` — the die does not turn); the **matrix editor's Reset and slider edits move no die** pre-start; and on the **home hero** — where `isStarted` has no writer (home registers no adapter, App.vue:281; the sole write-back is the transport's, useSceneMachineShellBinding.ts:264-265) — **the flagship landing-page subject never responds to drag rotation at all**, in every shipped state. Post-start the container renders the orbit, which is why the defect has been invisible whenever anyone pressed Play first. This is #7's silent object-skip **instantiated in the one place it is load-bearing**, it is the false premise under two challenge kills and four r1 cells (rulings 1–2), and the engine's sole seam test is scalars-only (transform-style.test.ts, my read). Cure classes, adjudicated separately from the defect per the addendum law: serialize at the call site (`matrixValues` → a `matrix3d(…)` string — the data is already validated) or make the renderer fail-explicit (#7's cure), preferably both. **NO-WAVE-OWNER (cube scene-repair packet, ordered WITH #7's library-side diagnostic row; the born-RED gate is one pre-start drag + one `getComputedStyle(.cube).transform` assertion). Visible signature = SS-13 #9.**
- **#54 · MINOR — mid-drag unmount strands OrbitalDrag's document listeners; the code comment promises a teardown that cannot run.** (β missed #2, demoted by ruling 7.) `useEventListener(doc, …)` ×3 registered inside `onPointerDown` (useOrbitalPointer.ts:225-232) — no active scope in an event callback, `tryOnScopeDispose` no-ops (`@vueuse/shared:119-125`), and the comment at :178-180 asserts the opposite ("vueuse's tryOnScopeDispose covers an unmount mid-drag"). Self-heals at the next pointer release (the leaked document handler performs the removal); the MINOR residue is the stale-write window into the shared model, closure retention across HMR/scene switches, and the false comment — the house form is one honest setup-scope registration (useDragScrub.ts:128-130, my read). **NO-WAVE-OWNER (latch/pointer seam row with #13/#18).**
- **#55 · MINOR — the capture acquire is unguarded at the one site that raises the drag flags first.** (β missed #3, verified.) `containerRef.value!.setPointerCapture(event.pointerId)` (useOrbitalPointer.ts:222) sits after `startDrag` (flags up) and before the doc-listener registration; a throw (inactive pointerId / disconnected element) strands `isDragging`/`isTouching` true with no listeners — a phantom drag until the next pointerdown. The same file hardens the **release** half with a try/catch and an iOS comment (:188-192), and the sibling primitive guards the **acquire** (useDragScrub.ts:116-121, my read) — the asymmetry proves oversight. **NO-WAVE-OWNER (same seam row).**
- **#56 · MINOR — the "pinned" key light is pinned to the graph's frame, not the room: the scene's own 30° opening attitude is invisible to the lighting model.** (α missed #5 ≡ β missed #4 — found independently by both readers; elevates r1's SS-13-note-only treatment to a roster row.) `useCubeDemo` parks `.graph` — an **ancestor** of `.cube` — at `rotate3d(-1,1,0,30deg)` (authored :144-151, PRM arm writes the literal at :166, both my reads); `litFor` rotates normals by `transform.rotate` alone (useCubeRelit.ts:46-67) and dots with a KEY_LIGHT expressed in the pre-graph frame. The docstring's promise ("The light is PINNED in the room", :10-12) is unmet **at rest** — a constant ~30° error compounding #4's sign flip, and the reason β's insufficiency caveat on the L·M-1 cure is right: `0.6 → −0.6` fixes the model's frame, not the screen's. The composable receives no graph attitude at all (CubeTarget.vue:149 passes only `transform`) — a contract gap, not bad math. **NO-WAVE-OWNER (one lighting-truth row with #4/#16/#36); perceptual magnitude = SS-13 #1.**
- **#57 · MINOR — the `.z` axis line is projectively degenerate: `rotateY(90deg)` puts the stroke in a plane containing the view direction.** (α missed #2, mechanism verified by this seat: CubeAxisLines.vue:43-46 gives every line `width: 1000vw; height: 0; border: 1px dashed`, differentiated purely by transform — `.x` identity, `.y` `rotateZ(90deg)` in-plane, `.z` `rotateY(90deg)` out-of-plane — as direct children of `.graph`, which carries `perspective: 1200px`, CubeTarget.css:14-16.) The Z lock's full-stage stroke projects to a wedge/line toward the vanishing point rather than a legible axis — which also punctures challenge-D §5.3's kill of the 1.4.1 colour-only claim ("also distinguished by orientation"): the orientation redundancy holds for X-vs-Y and fails for exactly Z. Drawn result = SS-13 #10. **NO-WAVE-OWNER (axis-reveal row).**
- **#58 · MINOR — the LOCKED Y axis line fails WCAG 1.4.11's 3:1 non-text floor in the light arm.** (α missed #4; arithmetic re-derived by this seat: `--axis-y: hsl(120 47% 47%)` = rgb(64,176,64), L ≈ 0.325 → vs light `--background` hsl(40 30% 98%), L ≈ 0.956 → **≈2.7:1** (α computed 2.53 against a tinted stage; either figure is below 3:1); siblings pass — `--axis-x` ≈ 4.2:1, `--axis-z` ≈ 6:1; the dark arm passes ≈7:1.) At rest (0.45 opacity) all three are decorative furniture; **locked at opacity 1 + solid stroke, the line IS the state signal** (CubeAxisLines.vue:53, :73-75) and carries meaning below the floor, in the same default arm D·B-1 lands in. Exact backdrop pixel = SS-13 #11. **KF.W6 (axis-token contrast, with the #8/#9 theme packet); KF.W9 witnesses.**
- **#59 · INFO — the `var(--lit, 0.5)` / `var(--axis-active, 0)` fallbacks are unreachable.** (α missed #7, verified: both properties are `@property`-registered with `initial-value`s — CubeTarget.css:6-10, CubeAxisLines.vue:37-41 — so an unset registered property substitutes its initial value and the `var()` fallback arm can never be selected: CubeTarget.css:132/:138/:139, CubeAxisLines.vue:53/:55/:56.) Inert defensive code that invites divergent edits. **NO-WAVE-OWNER (rides #30's namespace/@property row).**
- **#60 · INFO — raw motion durations beside tokenised easing in one unit.** (α missed #8, verified: `transition: --lit 160ms linear` (CubeTarget.css:79) and the three 180 ms literals (CubeAxisLines.vue:60-63) hard-code time while the same declarations route the curve through `var(--ease-standard, ease)`; the sibling scene tokenises both halves, and the unit's one duration-token reach — `duration-panel` — is the wrong register AND dead (#24).) **NO-WAVE-OWNER (token-hygiene rider with #39/KF.W6).**
- **NOT admitted — folded to standing identities:** α missed #6 (the ppMode toggle's keyboard inoperability — reka `HoverCardTrigger` `as:"a"` href-less over a nameless click-div; mechanism verified by this seat from the installed reka + glass-ui dist) **≡ the banked kf-CubeScene latent-cohort D-1 + KF-APP-17**, where it is further ruled LATENT because **ppMode has zero working writers** (kf-CubeScene C-1 ≡ KF-APP-1 — `setPPMode` lives in an unmounted `headerLeft`; the menu toggler throws). The same banked fact adds a **latency rider to r1 #14** (the ppMode material deletion is dead render until KF-APP-1's cure lands) — carried, not re-booked. α missed #1 → #50; α missed #3 / β missed #5 → #24; β missed #6 → #52; β missed #7 → #34; β missed #8 → #20.

## Killed-claims register (r2 additions — r1's 17 carried by reference)

18. **Challenge-L §7.3 + challenge-D §5.2's double kill of "dragging before Play does nothing visible" — REVERSED.** Both kills cite the useTransformState painter as the complementary pre-start writer; the painter writes nothing (`Matrix3dCall` → object-skip, both trees — ruling 1). The candidate was TRUE and is now roster #53. Challenge-L's own dependency sentence ("this same painter is what makes M-2 true") falsifies its M-2 in the same stroke. The register law this event mints: **a kill that rests on a mechanism is only as dead as the mechanism — verify the write, not the call site.**
19. **r1 roster #5's obliteration arm, #6's painter-writer cell, #3's watcher-repaint sharpening, and reader-α's D·M-5 writer cell (a)** — one root, four corrected cells (rulings 1–2). r1's "(re-verified by this seat at :197-215/:207-211)" verified seating, not writing — recorded as the r1 seat's one inherited cell.
20. **Reader-β's L/B-3 BLOCKER confirmation ("glass-ui appears in neither package.json nor the lock").** Substrate-dead — the worktree's uncommitted deletion reported as the manifest; false at both commits (HEAD 6.0.0 optional; origin 7.0.0 exact devDep). The **third** library-side dirty-tree event across three corpora — promoted from anecdote to pattern: *the library axis reads manifests, and manifests are exactly where the worktree lies.*
21. **r1 MISSED-A9's sub-claim ("the loading placeholder tumbles in 3D with the die under a pre-start drag")** — nothing paints `.cube` pre-start; killed inside #22, structural loader facts untouched. Alongside it, challenge-D D-m6's "the placeholder is 20 % larger than the thing it stands in for" framing — nothing is stood in for; the faces render fully beneath the spinner (α's kill, corroborating C:D-10 and r1 #22).
22. **Reader-β's D·m-9 re-confirmation** — killed as filed (ruling 4; r1 register #8 upheld). A confirmation that concedes its own premise in a parenthesis is a kill.
23. **Challenge-D D-m10's magnitude cell ("`1000vw` — a bare literal 1000× the viewport")** — arithmetic: `1vw` is 1 % of the viewport, so `1000vw` is **ten** viewport widths (α's correction, adopted; unnoticed by the challenge, r1, and reader-β). The magic-number and resident-filter halves of the row (#31) are untouched.
24. **D·m-7 as a MINOR defect** — reader-α's doctrine-backwards kill adopted; using the semantic `--z-content` token is the *conforming* form under style.css:36-39's own contract. Regraded INFO (#29), residue = coherence commentary.
25. **Reader-β's MAJOR grade on the doc-listener leak** — the durable-leak scenario dies at the next pointer release, by the leaked handler's own teardown (ruling 7). Mechanism and false comment survive at MINOR (#54).

## Superlatives (r1's ten carried; two added)

11. **The R1-parser-unreachability negative proof (challenge-C S-1), sustained through two hostile re-reads.** A negative established on three structural legs (colour rides `var(--face-N)` and never becomes a parsed string; the only parse leaves are `"0deg"` and computed-number templates; the sole value.js edge is the certified grammar-free `/math` leaf) with its one near-miss named (`"ease-out-back"` → `parseTimingFunction` rejects → registry resolves). Both readers re-verified independently; adopted as the model for proving absence with structure rather than with a grep that found nothing.
12. **`createMatrix`/`matrixValues`/`matrixValueAt` are fail-explicit done properly — in a demo, on an interactive path** (transformMath.ts:69-124, my read: arity + finiteness validated on every read and write, typed `RangeError`/`TypeError` with the offending index named). Reader-β's pairing observation is the record's best irony and is adopted verbatim: it is **the exact discipline `transformTargetsStyle` lacks two call sites away** — the demo-side data structure validates itself perfectly and is then handed to a renderer that silently drops it whole (#53). The instructive pairing, not the flattery, is the superlative.

*(Qualification updates to carried superlatives: S-3/D·S-3's transient-`will-change` discipline now carries the #34 + kf-CubeScene L-2 double qualification — the transient hint never fires because `isPlaying` is never written, while the resident ancestor hint always does; r1 #12's kill of the T.A5 quantization superlative stands unchallenged by this corpus.)*

## UNPROVEN-NEEDS-LIVE (SS-13 visual-audit inputs)

Carried: r1 items 1–4 and 6–8 unchanged (the rest-pose lighting capture under both themes and the 30° attitude; the optional B-1 falsifier; the quantization-vs-transition repaint profile; the D·M-2 modelled column; the drop-shadow/willChange layer costs; the cqi/svb drift; the size-cliff percepts). r1 item 5 is **amended** (its "3D tumble under a pre-start drag" clause died with #53): capture the spinner at z=0 inside the 0.8-α die, static. Added:

9. **#53's on-screen signature** — a pre-Play drag on the hero: expected result is a motionless die whose six faces re-shade as the pointer moves (the quaternion and `--lit` track; the pose does not). Distinctive, easily diagnosed, and the definitive live confirmation of the record's new BLOCKER.
10. **#57's drawn result** — the `.z` line under `perspective: 1200px`: thin wedge, dot, or nothing.
11. **#58's true backdrop** — the locked Y line's pixel over the stage tint in the light arm (the ~2.5–2.7:1 range is token-derived; the stage compositing is not).

## Identity guards (fold-by-reference — never re-booked)

- Carried whole from r1: the F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1 identity (+ the worktree-deletion residue at KF.W0); D·B-2 ≡ kf-AmigaScene D-2's shared subject-a11y row; D·M-1/C·D-1 → KF.W9 PRM unification; D·m-8/C·D-13 ≡ census FE §6.3; C·D-9 extends census S-6; #51 ≡ the stale-z census/prose identity; #17's non-identity with the value.js layout-gestalt cqb rows.
- **#3 ≡ kf-CubeScene L-3** (banked, same session) — one acos defect, owned where `useTransformState` is owned; this record contributes the painter-no-op correction to that row's consequence chain.
- **#42 ⊂ kf-CubeScene L-2/C-5** (`isPlaying` has no writer) — the sharper superset; also qualifies #34 and the S-3 superlative.
- **α missed #6 ≡ kf-CubeScene latent-cohort D-1 / KF-APP-17**, latent under **KF-APP-1 ≡ kf-CubeScene C-1** (ppMode zero writers) — which also attaches the latency rider to r1 #14.
- **#53 has NO identity with kf-CubeScene L-4/M-6/L-5/L-9/C-7** — those rows corrupt or churn the *compiled Matrix channel and the model*; #53 kills the *direct paint path*. Same composable, disjoint mechanisms; checked row-by-row against the banked record before booking. (#53's existence re-colors kf-CubeScene L-4's symptom prose — "Play animates nothing on that channel … reads as an unrelated bug" — the pre-start editor was ALSO painting nothing, so the editor's whole pre-start visual contract was dead, not merely lossy; noted for that record's owner, not re-booked.)

## Routing summary (M-25 agglomeration input)

- **KF.W0**: unchanged from r1 (F-1 discharge; worktree reconciliation — the glass-ui devDep deletion + the src/ refactor-in-flight; census/prose z correction; re-anchor clause) — all re-affirmed by this seat's own git.
- **KF.W6**: r1 set (#8 · #9 · #21 · #30 · #39) **+ #58** (axis-token contrast) **+ #60** (duration-token rider).
- **KF.W9**: #10 (PRM) + witnesses for #8/#9/#11/#32/**#58** + the SS-13 list.
- **NO-WAVE-OWNER — the cube scene-repair packet** (SS-1/SS-2 authoring must home it): the Roll stack #1·#2·#5·#15·#20·#38·#49 (one four-defect repair with its born-RED gate) · **#53 + #7 as one paired seam repair (demo serialization + library diagnostic), with its own born-RED gate (pre-start drag → computed transform asserts)** · the transform-ownership row #6 (re-derived post-#53) · the lighting-truth rows #4/#16/#36/**#56** · the matrix-editor family (→ kf-CubeScene's rows, #3 folded) · the latch/pointer seam #13/#18/#19/**#54**/**#55** · loading-state #12/#22 · #14 (latent under KF-APP-1) · #17 · the axis-reveal rows #31/**#57** · hygiene #23–#28/#32/#33/#35/#37 · INFO rows #29/#40–#48/#52/**#59**.

## Verdict

**ADJUDICATED (r2).** The r1 verdict's shape survives — ornament outrunning verification — but this corpus deepens it by one decisive turn: the component's *third* dead render path was not merely missed but **actively used as refuting evidence**. Three challenge axes and the r1 seat all treated the matrix painter as the mechanism that kept the pre-start drag coherent; two of them killed a true defect with it; reader-β alone read the nine renderer lines against the object they receive, and this seat re-derived the chain at both trees. The die's signature roll has never fired, its roll animation could never paint, and its pre-start orientation painter has never written — three independent silent-failure mechanisms on one 239-line component, each invisible to every gate, the third invisible to five consecutive audit documents. The repeated instrument lessons are now law-grade: *name the tree before quoting a manifest* (three library-side seats, three corpora, same error), and *a kill is only as dead as its mechanism*. Nothing in this record stamps a wave; every disposition is agglomeration input for the forming X·KF specs. Original ids #1–#60, for life.

— end of apotheosis r2. The challenger records, both r1 reader returns, and both r2 reader returns remain immutable beside this file per E-1/E-3; the r1 text at this path is superseded whole, its rows carried by id.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/scenes/cube/CubeTarget.css` · `demo/scenes/cube/CubeTarget.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **CubeTarget.css=IDENTICAL · CubeTarget.vue=IDENTICAL**.

**Reading**: the subject file is **BYTE-IDENTICAL** at `8281638c` and at the ref of record, so this record's line anchors into it bind **unchanged**. No line re-resolution is owed for it — and that is a measurement, not an assumption.

**`D-19` does not appear in this record** (⟨`grep -c 'D-19'`⟩ → 0). The re-anchor obligation reaches it through this stamp alone, which is why the stamp is written at every one of the 58 and not only at the 42 the token reaches.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

