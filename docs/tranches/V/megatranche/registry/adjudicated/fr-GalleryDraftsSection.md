claude-fable-5

# fr-GalleryDraftsSection — adjudicated apotheosis (M-23/L-18, fresh-Fable incredulity seat)

**Seat**: adjudicator-F · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited.
**Date**: 2026-08-24 · **Subject**: `/Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/gallery/GalleryDraftsSection.vue` (108 lines, read whole this session) + its sole consumer `GalleryView.vue`, the store seam (`stores/gallery.ts` `publishDraft`, `stores/workspace.ts` `_saveDraftNow`/`refreshDrafts`, `lib/draftStorage.ts` whole), the API seam (`api/routers/images.py`, `api/lib/crud/{slugs,idempotency}.py`, `api/services/{database,image_storage}.py`), and the installed glass-ui 4.0.0 / producer v7.0.0–v8.0.0 disclosure chain.
**Substrate**: fourier HEAD `cd26c65` — but the evidence tree is the **dirty working tree** (26 modified files incl. the subject and `web/package.json`; see m-18): the committed subject byte at `:58` is `<MetricBadge :amount=…>`, the working-tree byte is `:value` — re-pinned by this seat's own `git diff`. Installed glass-ui **4.0.0** (my node/python reads of the installed package; 80-key exports map) · producer glass-ui HEAD `350f7a90` with tags v7.0.0/v8.0.0 read via `git show` (BASELINE LAW — producer HEAD has moved twice since the challenges).
**Corpus**: 3 challenge files (D design 637 lines · L library 253 · C consumption 848) at `docs/tranches/V/megatranche/audit/fourier-components/GalleryDraftsSection/` — ~64 accusation rows, 5 pairwise-overlapping BLOCKER claims, 18 superlative rows.
**Readers adjudicated**: reader-1 (design-and-usage hostile re-read) · reader-2 (library-and-code hostile re-read), returns via the orchestration channel; the challenge files sit immutable beside this record.
**Method (M-23/L-18)**: both readers were attacked before either was adopted. Every kill was re-derived with this seat's own instruments; one reader-kill was found INVERTED against the other reader's endorsement of the same cell and settled by a byte read (R-1); one reader CONFIRMED row was overturned by the other's git archaeology, ratified by this seat's own run (R-2); one reader's net-new MAJOR was overturned on the identity guard (R-3). **Two corpus rows (C-C-20, D-i-5) were dropped silently by BOTH readers and are adjudicated by this seat** (DISSENT-3).
**Arbiter's own instruments this session (static + node/python/git against source and installed dists; zero browser probes — probe parsimony; product source read-only)**: subject read whole (108); `python` byte-read of `dist/button-BNDWhAZb.js` — `defaultVariants: { variant: "default", size: "default" }` **present** at index 4630, and the `grep -o 'defaultVariants:{[^}]*}'` artifact **reproduced** (exit 1 — line-based grep vs pretty-printed newlines; the R-1 decider); the `outline` variant string (`border border-input bg-background hover:bg-accent …`), the `ghost` string (`bg-transparent text-foreground/70 hover:bg-foreground/8 …`), and the props block (`variant,size,class,type,disabled,asChild,as:{default:"button"}` — **no `loading`**) all python-extracted from the same chunk; `grep -rn "drafts-header\|draft-item" web/src web/e2e` → 3 hits incl. **`web/e2e/gallery.spec.ts:38` `page.locator(".draft-item")`**; `grep -rn savedSnapshots web/src` → exactly 4 (types.ts:90 · GalleryView.vue:78/:79 · the sole writer workspace.ts:102 `savedSnapshots: []`); `grep -rn deleteDraft web/src web/e2e` → definition only (draftStorage.ts:87); `grep -rn 'startsWith("fourier")'` → **7 hits / 7 files** (incl. `canvas-drawing/labels.ts`); `grep -rn "function timeAgo" web/src` → exactly **5**; `stores/gallery.ts:215-262` read whole — `publishDraft` :237, the pre-`try` `if (!draft.contour) throw` :238, **`const slug = await auth.ensureUser()` :241**, the `["fourier-epicycles"]` substitution :249-252, `etags.set(data.slug, etag)` :257, **`toast("Published!", "success", { slug })` :258 — the USER slug**, `resetAndFetch()` :259, the sibling `publish()` :219-230 with the same idiom correctly bound; `auth.ts:75-82` `ensureUser(): Promise<string>` returns `userSlug`; `useToast.ts:20-22` renders `${message} (${options.slug})`; `workspace.ts:93-105` (`_saveDraftNow` — the `savedSnapshots: []` literal :102, `saveDraft(raw).catch(console.warn)` :105), :112-126 (uploadImage — `contour.value = null` then `_saveDraftNow()`, the **thumbnail-regeneration rationale comment** :119-121), :408-410 (**`refreshDrafts` = `listDrafts().catch(() => [])`**); `GalleryView.vue` :47/:73-81 (`publishedHashes` = a Set of `e.slug` — slugs, not hashes)/:100-107/:205-216/:356-371; `draftStorage.ts` :6-15 (the false key contract) :31-32 (keyPath `imageSlug`) :38-40 (v2 index over `visualizationSlug`) :75-78 (dead reader) :87 :96-105; **git archaeology (the R-2 decider)**: `git log --diff-filter=A -- web/src/lib/draftStorage.ts` → `415effd`, `git show 415effd:web/src/lib/draftStorage.ts` → `DB_VERSION = 1`, `git show 415effd:web/src/lib/types.ts:89-98` → the identical eight fields **including `lastOpenedAt: string`**; installed `a11y-overrides.css:1-20` — the blanket PRM gate (`*:not([data-allow-motion]) { animation-duration: 0.01ms !important; … transition-property: opacity, color, background-color, border-color, box-shadow !important }` — `transform` absent from the list); `base.css:174-178` (`.focus-ring:focus-visible` outline:none + shadow), `scale-paper.css:65-68` (both ring legs outer, zero offset), `surfaces.css:119-120` (`.btn-pill { @apply inline-flex … border-none }`); installed exports map python-read: 80 keys, `./collapsible` TRUE, `./metric-badge` TRUE, `./checkbox` FALSE; `git show v7.0.0:package.json` → metric/collapsible exports = `['./collapsible','./metric']` (no `./metric-badge`); `git show v7.0.0:src/components/button/Button.vue` → `emphasis?/size?/loading?`, **no `variant`**; fourier `api`: images thumbnail GET (no `Depends`; `Cache-Control: public, max-age=86400` :162-164), `generate_slug` = 4× `secrets.choice` (slugs.py:40-42), idempotency no-header→`handler()` (idempotency.py:68-70), `visualizations.create_index("content_hash")` PLAIN :98 vs flags compound `unique=True` :140, `image_storage.py:105-128` dedup-hit thumbnail **regeneration** (the C9-provenance comment + `_generate_thumbnail` + `$set thumbnail_uri`); `api.ts:288-297` (three bare URL builders) :371-380 (`createVisualization`, no Idempotency-Key); `grep -rn thumbnailUrl web/src` → ImageUpload.vue:5/:65 + GalleryCard.vue:7/:100 + subject :4/:77 + the definition (**Modal imports `overlayUrl`, not `thumbnailUrl`** — the K-7 decider); `GalleryCard.vue:65-69` (the D.W4.c comment) :70-80 (the cure) :108-109 (`truncate flex-1 min-w-0 font-mono` slug + `whitespace-nowrap shrink-0` **`timeAgo(entry.created_at)`**); `style.css:1-3` (glass styles import — the PRM reach) :38-47 (root 1.125rem below 768px) :136-143 (the four D.W4.d rescue classes — neither subject hook among them); `ui/CollapsibleSection.vue:39` (the byte-identical header span — anchor corrected from D's :38); `raw-findings.json:1871-1876` — the M-critique audit's evidence cell naming **`GalleryDraftsSection.vue:91 <Button variant="outline">Publish (muted-foreground idle)`** verbatim; stale-dist re-run: `web/dist/assets/*.css` all `Jun 12 18:13` vs installed glass-ui `Jun 17 21:48`; **registry identity sweep** over `adjudicated/*.md`: `GalleryDraftsSection` → **0** (unbanked as a component) but five defect identities already banked — fr-BasisSelector M-10 (:59 — 7 normaliser sites + ×4 mode labels + the gallery clone, one repair unit), FR-AUL-17 (:55) + FR-AFP-32 (:86) (timeAgo family), FR-AFP-4 (:47) (images-router public-cache/no-auth posture), FR-AFP-19 (:66) (rest-state tone family), FR-EQR-14 (:52) (glyph-register emission), FR-AUL-24 (:62) (focus-to-body family), fr-AnimationControls C-27 (:93) (`"fourier-epicycles"` magic string, 28×/11 files), fr-CollapsibleSection F-1/F-3/B-1/M-2/M-3/M-6 (whole file re-read — the K-6 decider).

## Scoreboard

**35 adjudicated register rows (2 BLOCKER · 10 MAJOR · 18 MINOR · 5 INFO) + 6 identities FOLDED BY REFERENCE (never re-booked) · 12 kills in the killed register · 10 missed finds banked (3 folded into composite rows) · 10 superlatives with provenance.**

Blocker-weight survivors: **B-1** — the panel's primary verb is keyboard-unreachable while the repo's own D.W4.c sweep cured the identical pattern 30 lines away; **B-2** — the publish path is a composed lie: the reconciliation field is write-only-empty, the row never leaves, the button re-enables, and a second press mints a duplicate public visualization with server confirmation — and the one affirmative signal prints the wrong identifier (m-13).

Status law: dispositions below are **ADJUDICATED, not VERIFIED** — the X·F specs are still forming; every routing is agglomeration input (M-25), and nothing in this record stamps a wave.

## Ruled disagreements (the incredulity seat's actual work)

| # | Dispute | reader-1 | reader-2 | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|---|
| R-1 | **Does glass 4.0.0's Button cva declare `defaultVariants`?** (r1's K5 kill of D-§7.5 vs r2's re-endorsement of D-§7.5 inside its cascade-layer INFO row, plus r2's D-M-7 "second unbooked delta" built on the absence) | K5: PRESENT — python-read; D's grep is a line-based artifact | "I also re-confirm D's §7.5: grep → empty, so no height class is emitted and `py-2` governs" | **reader-1 wins, decisively.** My own python read: `defaultVariants: {\n\t\tvariant: "default",\n\t\tsize: "default"\n\t}` at index 4630 of the installed chunk; the grep artifact reproduced exactly (exit 1 — newlines inside the object literal defeat line-based `grep -o`). Consequences ripple: the header Button (no `size`) is height-pinned `h-(--control-h-md)` = 40px fine / 60px coarse **today**; D's §7.5 self-refutation was wrong (the ORIGINAL claim was right); r2's D-M-7 sharpening ("4.0.0 ships NO defaultVariants … the header gains the size ladder at the uplift") collapses — the header is height-pinned at BOTH pins, no new delta; r1's K8 (S-7 rhythm kill) and missed-find #3 (coarse composition) stand on the corrected premise. **r2's endorsement cell is an instrument-inheritance event** (it re-ran D's broken instrument instead of reading the byte) — recorded in DISSENT-1. C's S-5 falsifier cell stated the defaultVariants correctly all along; C wins the byte across all three axes. |
| R-2 | **L-8/C-C-14(b)'s NaN arm** ("a v1 record lacking `lastOpenedAt` renders `NaNd ago`"; C: "not determinable from the tree") | CONFIRMED L-8 with the NaN consequence in its proof cell | KILLED by git archaeology | **reader-2 wins.** My own run: draftStorage.ts born at `415effd` with `DB_VERSION = 1`, and `git show 415effd:web/src/lib/types.ts:89-98` shows the identical eight-field `WorkspaceDraft` **including `lastOpenedAt: string`**. No v1 record can lack the field; `"NaNd ago"` is unreachable; C's "not determinable" was one `git show` from determined. Survivor: the internal incoherence (guards at :25/:39/:44 against an unguarded :88) as dead defensive code — m-10, MINOR. |
| R-3 | **C-C-3/L-5's booking** (basis-key normalization + mode-label erasure) | Confirmed C-3 as a net-new MAJOR — "the sharpest pure-DESIGN defect in the corpus, and only the C axis found it"; registry sweep ran `grep GalleryDraftsSection adjudicated/` → 0 and stopped | FOLD BY REFERENCE to banked fr-BasisSelector M-10 | **reader-2 wins on the identity guard.** My read of fr-BasisSelector M-10 (:59): it already banks the 7-site `startsWith("fourier")` census (my grep re-run: exactly 7 files, incl. `labels.ts` — L's 6 was one short), the mode-label map re-authored ×4, the verbatim gallery clone, and ONE repair unit (`BasisKey` union + `normalizeBasisKey()` + `basisModeLabel()` + `satisfies`) → F.W3/W4. r1's sweep was **component-name-keyed** and structurally cannot find defect-identity folds — the fleet lesson (DISSENT-2). The subject's stake books as riders on F-1, not a new row. |
| R-4 | **D-M-5's grade** (`.cm-serif` → generic serif) | FOLD at MINOR to banked fr-CollapsibleSection F-3 | CONFIRMED at MAJOR, no fold | **reader-1 wins.** fr-CollapsibleSection F-3 (my re-read, :44) already banks this exact identity (= fr-BasisSelector m-3 + fr-AppHeader GLASS-RELAY; 26 hits / 17 files; **regraded MAJOR→MINOR** by that ruling) → F.W1 token parity + the standing glass BH relay. A banked regrade governs its identity; the subject's rider (three faces in one 108-line row, the header on the brand-absent register) books at MINOR under F-3. |
| R-5 | **B-2's quantifier** ("100 % clipped in the collapsed state") | Regraded BLOCKER→MAJOR, kept "total in the collapsed state" | MAJOR; quantifier KILLED — `.btn-pill` is `inline-flex`, so the root's line-box strut geometry decides whether the content box extends below the Button's border box, and that is not source-derivable | **Grade agreed at MAJOR; reader-2's quantifier objection ADOPTED.** My reads confirm every substrate link (outline:none + pure-outer two-leg ring; padding-free `overflow-hidden` root; the four D.W4.d rescue classes exclude both subject hooks; forced-colors-only rescue). Top/left/right legs are provably clipped in BOTH states (`w-full` + no padding); the bottom leg in the collapsed state rides font-strut arithmetic → SS-13. The collapsed state is *likely* worse (expanded, the bottom leg paints inward over the first row's border-t) but not provably total. |
| R-6 | **D-M-8's grade** (meta line has no overflow contract) | MAJOR | MINOR | **MAJOR as composed** (M-7 below): r1's missed-find #3 — verified on the R-1-corrected premise — shows the coarse-pointer lift grows both Buttons in two axes (`--ui-scale` 1.5×, `--control-floor` 44px, glyph 16→24px, `--control-text` ×1.5) while the thumbnail/gaps are literal and `shrink-0` routes 100 % of the width increment into the one column with no overflow strategy, on the same viewport class where `style.css` up-sizes the root font (my :38-47 read). The base leg alone is r2's MINOR; the dissent is preserved. |
| R-7 | **D-M-4's grade** (the ~1.02–1.04:1 row hover) | MAJOR, with the net-new ghost-inversion rider | MINOR (bundled with the border ratios) | **MAJOR.** My python read confirms the rider's substrate: the ghost variant hovers at `bg-foreground/8` — the disclosure toggle gets a 4× stronger hover than the row carrying the primary verb. Composed with B-1 (the wash is the row's ENTIRE state vocabulary) and the same-family tone finding (M-6's FR-AFP-19 fold), MAJOR is the family grade; r2's MINOR preserved as the standalone-arithmetic reading. |

## Verdict register — adjudicated defect roster

Format: **id · severity · claim · decisive anchor · terminal disposition → route**. Full proof cells live in the challenge files and reader returns, incorporated by reference; every load-bearing anchor re-verified by this seat unless marked (adopted). Line anchors are the WORKING TREE (see m-18); re-resolve before any edit.

### BLOCKER

- **B-1 = L-1 = D/B-1 = C-C-6 — BLOCKER (unanimous, ratified).** Two activation-bearing bare `<div cursor-pointer @click>` open-targets (`:72-75`, `:83`) with no `role`/`tabindex`/`keydown` (my grep → 0); the emit is the ONLY edge into a draft's workspace (`GalleryView.vue:370`). Every draft can be published by keyboard; none can be opened. The repo already adjudicated and cured this exact class 30 lines away — `GalleryCard.vue:65-69` carries the D.W4.c provenance comment and :70-80 the canonical cure; `style.css:136-143` enumerates four D.W4.d rescue classes, neither subject hook among them (all my reads). The cure is three attributes + two handlers on `:83` with zero event-plumbing risk (S-6: Publish is a sibling, not nested) — and the repair MUST respect i-2: `.draft-item` is a live e2e selector. **ADJUDICATED → F.W3/W4** (mirror D.W4.c; add `.draft-item:focus-visible` to the D.W4.d block, which retires m-12's dead `drafts-header` question; post-cure ring visibility = SS-13 #8).
- **B-2 = C-C-1 ∘ L-3 (∘ m-13 rider) — BLOCKER.** The publish path composed end-to-end, every link my own: `savedSnapshots` has ONE writer — the unconditional literal `[]` at `workspace.ts:102`, re-zeroed on every save/open/upload — so `GalleryView.vue:78`'s `!d.savedSnapshots?.length` is invariantly true and `unpublishedDrafts === workspace.drafts` for every input; `publishDraft` (gallery.ts:237-262) never calls `saveDraft`/`refreshDrafts` and ends at `resetAndFetch()`; `publishedHashes` (:73-75) is a Set of freshly-minted 4-word *visualization* slugs that can never equal a draft's `imageSlug`. THE ROW NEVER LEAVES. The button re-enables (`GalleryView.vue:214`), and the second press mints a second public visualization: `createVisualization` sends no `Idempotency-Key` (api.ts:371-380), the middleware is explicit `no header → handler()` (idempotency.py:68-70), and `content_hash` carries a PLAIN index (database.py:98; contrast flags' deliberate `unique=True` :140). The component's props contract (`publishing: boolean`, no published-state channel) is what makes the hole undeclarable at the render. **ADJUDICATED → split**: props/reconciliation contract → **F.W3/W4**; server-side dedupe/idempotency for the create path → **F.W5-W8** (CRUD/provenance union). Riders: m-13 (the success toast prints the USER slug); the `publishedHashes` name is a misnomer (it holds slugs) — fix with the contract.

### FOLDED BY REFERENCE (identity guard — banked rows, never re-booked)

- **F-1 = C-C-3 = L-5 (+ r2-missed-4 rider) = fr-BasisSelector M-10 (banked; → F.W3/W4)** — R-3's ruling. Subject's stake, booked as riders on that identity: (i) `:43-44` is the seventh normaliser site; (ii) the Drafts tab renders the single word "Fourier" for a mode the adjacent Gallery tab names "Epicycles"/"Series" (GalleryCard.vue:41-47, byte-verified by the readers, mapping divergence adopted); (iii) **r2's missed find, verified by this seat**: an empty `active_bases` renders as NOTHING here while publish silently substitutes `["fourier-epicycles"]` (gallery.ts:249-252, my read) — the preview surface and the entity it mints disagree by construction; overlaps banked fr-AnimationControls C-27 (the magic string) but the *substitution invisibility* is the new cell.
- **F-2 = L-4 = D/i-3 = FR-AUL-17 + FR-AFP-32 (banked twice; → F.W3 one shared util)** — my grep re-run: exactly 5 `function timeAgo`, two dialects ("just now" ×3 vs "0m ago" ×2, AdminFlaggedPanel hardened to `string | null`). Both banked rows post-date the challenges; neither reader's axes could have cited them, but the finding is not new. Never re-book.
- **F-3 = D-M-5 = fr-CollapsibleSection F-3 (= fr-BasisSelector m-3 + fr-AppHeader GLASS-RELAY; banked at MINOR; → F.W1 token parity + glass BH relay)** — R-4's ruling. Local rider at MINOR: the subject's header is among the 17 consumers falling to UA generic serif inside an app whose `--font-sans` is Computer Modern; D's own i-4 (the body-face fork) rides the same banked identity.
- **F-4 = C-C-8 = FR-AFP-4 (banked: the images-router no-`Depends` + `Cache-Control: public, max-age=86400` posture) + R3-7b (securityGate RED_0_OF_45); → F.W5-W8 image-contract work** — my reads confirm the thumbnail GET carries the same posture as the banked blob GET; one router, one identity. New cells contributed by this corpus: the 2²⁸ slug-entropy arithmetic (generate_slug = 4× `secrets.choice` over 128-word lists, my read) and the *drafts-specific* framing (the pre-publication surface rides a public cache). Deployment auth-proxy question stays UNPROVEN (#5).
- **F-5 = D/m-1 = C-C-11 = FR-EQR-14 / FR-COB-18 (banked glyph-register sweep, MINOR; emission leg OPEN)** — the cascade leg is real (lucide writes `width/height` as presentation attributes — adopted, thrice-derived; the cva's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` extracted from the chunk by this seat); the EMISSION leg is exactly the question banked open at FR-EQR-14 and fr-CollapsibleSection M-7, and the shipped dist is inadmissible (my mtime re-run: Jun 12 vs Jun 17). D's flat "Yes, `@source` settles it" cell (with the `:214` miscitation — the live line is :222, adopted from the fr-EasingPicker precedent) is killed as an assertion (K-5 of reader-2's register, ratified); the row FOLDS. → **F.W1 PRE-GATE** (`npm run build` + one grep; no browser).
- **F-6 = C-C-9 = L-15 — INFO; → NO-WAVE-OWNER (SS-3/SS-4 census methodology; F.W5 provenance-contract input)** — the template-`src`-binding operation edge is structurally invisible to a function-keyed operation↔client model (the R5-7 dual), and the subject's native `<div v-for>` wrapping a Button callsite is a clean R6-5 instance (derived multiplicity 1 vs live `|drafts|`). Both axes folded rather than re-derived — correctly. K-7's evidence-cell correction rides here.

### MAJOR

- **M-1 = D/B-2 (regraded, R-5) — MAJOR + GLASS-RELAY rider.** The disclosure toggle's focus ring is a pure-outer box-shadow (`outline: none`, base.css:174-178 + scale-paper.css:65-68, my reads) inside a padding-free `overflow-hidden` root (`:51`) — top/left/right legs provably clipped in both states; the collapsed state likely total but strut-dependent (SS-13 #2). Cheapest cure: move `overflow-hidden` onto the list wrapper at `:66` (sound on both branches of the residue). RELAY (standing BH edict): a producer focus indicator that is 100 % outward-painting with `outline: none` is clipped by ANY `overflow-hidden` consumer — cite this component as the witness. → **F.W3/W4** + relay.
- **M-2 = D/M-1 = C-C-7 = L-12(b) — MAJOR.** No `aria-expanded`/`aria-controls`/heading semantics on a hand-rolled disclosure whose header span is byte-identical to `ui/CollapsibleSection.vue:39` (my read); the `./collapsible` primitive is exported at the INSTALLED pin (my exports read — available today, not post-uplift). **The cure is the PRIMITIVE, never the wrapper** — K-6: the wrapper is banked-defective across exactly the axes the cure would buy (fr-CollapsibleSection B-1 ruled hang at the F.W1 target, M-2 unmountOnHide teardown, M-3 ungated scrollIntoView, M-6 no controlled open — my re-read). C-7's own text reached this landing; D §8 step 2 did not. → **F.W3/W4**, sequenced with fr-CollapsibleSection's F.W1 gate.
- **M-3 = C-C-4 = L-2 — MAJOR.** Publish is enabled on drafts statically known to throw: `if (!draft.contour) throw` is the first statement of `publishDraft`, OUTSIDE its own try (my :237-238 read), landing on GalleryView's catch as a toast. Reachability is the default for a fresh upload (`contour.value = null` → `_saveDraftNow()`, my :112-126 read); the disqualifying field is in the component's own prop type. One-keystroke gate: `:disabled="publishing || !draft.contour"`. → **F.W3/W4**.
- **M-4 = D/M-3 = C-C-5 = L-3(1) (surviving core; two legs killed K-3/K-4) — MAJOR.** `publishing: boolean` is a list-wide flag for a per-row operation: one publish disables every row; no row identity, no busy affordance (the installed Button has no `loading` prop — my props read); native `disabled` drops focus to `<body>` — **folds to the banked FR-AUL-24 focus-to-body family**. The 7.0.0 `loading` prop (my v7 read) is the uplift-side cure, requiring the `boolean → string | null` prop widening D correctly flags as unbudgeted. → **F.W3/W4** (contract) + **F.W1** (the loading-prop budget note).
- **M-5 = D/M-7 — MAJOR, net-new to the census.** At v7.0.0 `ButtonProps` has NO `variant` (my `git show v7.0.0` read) — `variant="ghost"`/`variant="outline"` become stray DOM attributes, both buttons re-render as the default secondary glass capsule, with zero typecheck signal (fallthrough attrs). r2's "second delta" sharpening is DEAD (R-1) — the header is height-pinned at both pins; the surviving break is the material/emphasis change alone, repo-wide across the 35 button import sites. → **F.W1** (census: add the variant→emphasis sweep row; manual, vue-tsc-silent).
- **M-6 = D/M-4 (+ r1-missed-1 fold; R-7) — MAJOR.** The row hover computes to ~1.02–1.04:1 (token arithmetic adopted, thrice-agreed) and is the row's entire state vocabulary; the ghost toggle hovers 4× stronger than the row carrying the primary verb (my ghost-string read); and the primary verb itself rests at the muted rung on a no-fill plate — `variant="outline"` = `border-input bg-background` (my read) + the consumer's `text-muted-foreground` override (`:94`), the exact line the repo's own M-critique audit booked (**raw-findings.json:1874, verbatim — my read**). **The tone leg folds to banked FR-AFP-19** (rest-state tone family) with this new anchor. → **F.W1** rest-state tone + **F.W3/W4** row-state vocabulary; perceptibility SS-13 #3.
- **M-7 = D/M-8 ∘ r1-missed-3 (composed; R-6) — MAJOR.** `:85` has no truncate/nowrap/line-clamp below a `truncate` line; row height becomes a function of data (sibling contrast: GalleryCard :108-109's explicit priority pair, my read). Composed leg (verified on the R-1-corrected premise): under `(pointer: coarse)` both Buttons scale in two axes off tokens while the thumbnail and gaps are literal and the Publish button is `shrink-0` — the entire increment lands on the undefended column, on the viewport class where the root font is up-sized (my style.css read). D/S-6 celebrated the lift and never priced the row. → **F.W3/W4** (one overflow contract + pin row geometry to `--ui-scale`); wrap breakpoint SS-13 #4.
- **M-8 = D/M-9 — MAJOR.** No list semantics on a homogeneous counted collection whose cardinality the header announces; the established local cure (explicit `role="list"`/`"listitem"` on flex containers) sits in two same-directory siblings (adopted, verified by both readers; ancestor grep negative). → **F.W3/W4**.
- **M-9 = C-C-10 — MAJOR.** `draftStorage.ts:6-15` documents a key contract no writer implements ("the visualizationSlug once saved"); `WorkspaceDraft` has no such field; the v2 index (`:38-40`) is over a property no record carries — permanently empty — and its sole reader (`:75-78`) has zero callers (my reads). The component's key usage (`:74/:77/:83`) is correct only because the doc is wrong; one honest implementation of the documented lift breaks every thumbnail and open-edge in this panel. → **F.W3/W4** (reconcile doc/type/index/writer as one edit).
- **M-10 = C-C-2 (regraded from BLOCKER, K-12) — MAJOR.** No delete verb anywhere: `deleteDraft` is defined and never wired (my grep); this panel is the sole drafts UI; logout clears the array, not the store; rows carry full harmonic payloads; no cap/eviction/LRU (draftStorage read whole). Composed with B-2 the list is monotonic and unremovable. → **F.W3/W4** (wire the verb); quota behaviour SS-13 #6.

### MINOR

- **m-1 = D/M-2 first half (PRM leg KILLED, K-2) — MINOR.** Motion incoherence at NORMAL motion: the chevron transitions 200 ms while the content hard-swaps by `v-if`. Under `reduce` the producer blanket strips the rotation entirely (transform not in the forced list — my a11y-overrides read), so the PRM regression claim was exactly inverted. → F.W3/W4 (dies inside M-2's primitive adoption).
- **m-2 = D/m-2 = C-C-12 — MINOR.** MetricBadge's `<div>` root inside the Button's native `<button>` (phrasing-content violation; both roots read from the installed dists by the readers, adopted); cured free at the uplift (v7 `Metric` renders `<span>` — `git show` adopted). → F.W1 (rides the metric rename).
- **m-3 = D/m-3 — MINOR.** The count badge passes none of `label`/`abbreviation`/`labelPosition`/`unit` against the sibling banner's ×6 labeled convention; the toggle's accname computes to a bare "My Drafts 3". → F.W3/W4.
- **m-4 = D/m-6 = L-9 = C-C-17 (+ D/m-7 rider) — MINOR.** The root `v-if` (`:51`) is invariantly true (sole consumer gates on the same array — my :356-371 read); the empty state is designed twice in two files with two different insets (the populated state owns `mx-4`, the empty is full-bleed) — the component cannot own its own empty state. → F.W3/W4.
- **m-5 = D/m-8 = L-6 = C-C-16 — MINOR.** `getBasisLabel(draft)` runs twice per row per render (`:86`/`:87`); empty result leaks a leading space (whitespace-condense, no trim). One view-model `computed` retires this and m-9 together (the readers' shared cure, adopted). → F.W3/W4.
- **m-6 = D/m-9 = L-12(a) — MINOR.** `collapsed` is destroyed on every tab visit (the tab is `v-if`, no KeepAlive — adopted, both readers' greps); collapsing yields a dead-end tab containing one lone header bar (compounds m-4). → F.W3/W4.
- **m-7 = D/m-11 = L-11 = C-C-15 (+ D/m-4) — MINOR.** No `@error` on the thumbnail (the tree's only `@error` is ImageUpload — adopted; the server documents real 404/410 paths — adopted); `:alt="draft.imageSlug"` duplicates the visible `:84` string — `alt=""` is correct for a captioned thumbnail. Systemic (GalleryCard same) — lane carry. → F.W3/W4.
- **m-8 = D/m-12 — MINOR.** Truncated identifier with no recovery (`title`/Tooltip/describedby) against an 81-char slug grammar (adopted, dependencies.py:41). → F.W3/W4.
- **m-9 = L-7 = C-C-13 — MINOR.** `timeAgo` reads `Date.now()` non-reactively; labels freeze for the life of the mounted tab; `@vueuse/core` (a dependency) ships `useTimeAgo`. Bounded by the tab remount — C's narrowing is why this is MINOR. → F.W3 (rides F-2's shared util).
- **m-10 = L-8 residue (NaN arm KILLED, R-2) — MINOR.** Dead defensive incoherence: `?? ""` twice on a non-optional string (`:25`), `?.`/`?? []` on non-nullable fields (`:39`), a `?.` on a total Record (`:44`), against an unguarded `:88` — the guards and the omission cannot both be right, and the compiler is silent either way (strict without noUncheckedIndexedAccess — adopted). → F.W3/W4.
- **m-11 = L-13 (scope correction SUSTAINED; C-C-14(a) overreach killed, K-11) — MINOR.** `localeCompare` on ISO-8601 identity-formatted operands: cost + host-locale coupling; correctness intact (uniform punctuation elision); the census's UTF8_BYTEWISE_CODEPOINT law governs identity ordering in the audit deriver, not a UI display sort. → F.W3/W4 (plain comparison).
- **m-12 = L-10(a) = C-C-18 (+ the `drafts-header` half of D/m-5; the `draft-item` half KILLED, K-1) — MINOR.** Zero-rule `<style scoped>` still stamps a scope id on every element (the banked 4-file gallery sweep corroborates — fr-AdminFlaggedPanel:11, adopted); `drafts-header` is a dead hook. The cleanup is identity-guarded by i-2. → F.W3/W4.
- **m-13 = r2-missed-1 (re-proven by this seat) — MINOR.** The Publish success toast prints the USER slug: `const slug = await auth.ensureUser()` (:241, returns `userSlug` — my auth.ts read) rebinds the name the toast consumes at :258, while the visualization's real identity (`data.slug`) is used correctly one line up for the ETag map (:257); the sibling `publish()` (:219-230) uses the identical idiom correctly bound — a rebound-variable copy-paste, provable in-file. All three axes traced this exact path; none read the binding. → F.W3/W4 (one-token fix, lands with B-2's contract).
- **m-14 = r2-missed-2 (re-proven) — MINOR.** `refreshDrafts` = `listDrafts().catch(() => [])` (my :409 read): a failed IndexedDB read renders as the parent's "No drafts yet. / Upload an image…" empty state — wrong copy inviting redone work; the write side swallows too (`:105` console.warn). Three silent-empty rungs in one chain (with m-4's dead branch). → F.W3/W4.
- **m-15 = r2-missed-3 (re-proven) — MINOR.** The unversioned thumbnail URL + 24 h `public` cache negates the deliberate re-upload regeneration: the client comment (workspace.ts:119-121) and the server's dedup-hit regen branch (image_storage.py:105-128, my reads) exist precisely to freshen thumbnails, and the bare `thumbnailUrl` (api.ts:292-294) + `max-age=86400` (images.py:162-164) mask the result for up to a day on this very surface. Distinct from F-4's auth/cache posture identity (that row books WHO can fetch; this books WHAT they see) — cross-referenced, not merged. → F.W5-W8 (rides the image contract).
- **m-16 = r1-missed-4 (∘ D/i-2) — MINOR.** No typographic hierarchy in the row: identifier and metadata share one size, separated by colour alone (`:84`/`:85`), against a sibling that solved the same two facts with one line and explicit priority; compounding, `.fira-code` applies `liga`/`calt` programming ligatures to a machine slug the sibling renders in plain `font-mono` — same string, two shapings, and the grammar admits `--` runs. Ligature rendering SS-13 #7. → F.W3/W4.
- **m-17 = r1-missed-2 (re-proven) — MINOR (r1's MAJOR preserved as dissent).** The relative-time REFERENT collides across adjacent tabs: `timeAgo(draft.lastOpenedAt)` here (`:88`) vs `timeAgo(entry.created_at)` in GalleryCard :109 (my read) — the identical visual token, two clocks, no disambiguating noun. Net-new: neither FR-AUL-17 nor FR-AFP-32 touches the referent. Cure is a word ("opened 3h ago") or `<time :datetime>`. → F.W3 (rides F-2). 
- **m-18 = r1-missed-5 = r2-missed-5 (evidence-integrity, re-proven) — MINOR; → NO-WAVE-OWNER (fleet method) + extends the banked fr-CollapsibleSection F-1 working-tree rider.** All three axes declare `cd26c65` as their evidence tree while reading the dirty working tree — my own `git diff`: 26 modified files including the SUBJECT (`:58` committed `:amount` vs working `:value` — and all three axes quote `:value`) and the manifest (^3.1.0→^4.0.0 et al., the uncommitted M.W1a bump). Every verdict is a working-tree verdict wearing a HEAD label — right for the tree that will be committed, unreproducible from the commit named. r2's anchor-drift register (a dozen ±1–3 slips across all three axes, no verdict changed) folds here at INFO weight; re-resolve all anchors before driving any edit (D-19).

### INFO

- **i-1 = D/M-6 = L-14 (severity ruled: K-10) — INFO.** `./metric-badge` is present at the installed/adopted pin (my exports read) and absent at v7.0.0 (my `git show`) — not broken today; the break is F.W1's. D's genuinely additive half survives as the budget rider: the sibling banner's `label-position` ×6 / `color` ×2 have no clean v7 equivalent — "7 files" reads as 1 trivial + 6 with prop loss. → F.W1.
- **i-2 = C-C-19 — INFO, the cleanup identity guard.** `draft-item` is a live Playwright selector (`e2e/gallery.spec.ts:38`, my grep — the sole drafts-tab render assertion in a repo whose whole automated gate is 8 e2e specs); `drafts-header` is dead. Only one is safe to delete; C alone got this right. → rides B-1/m-12.
- **i-3 = D-§7.1/§7.2 = C-I-1 (with the §7.5 leg INVERTED per R-1) — INFO record.** The corpus's best epistemic moment — three seats independently opened and killed the same cascade-layer false positive (`rounded-none` vs `.btn-pill`, layers verified) and D killed its own aria-hidden suspicion into S-5 — is preserved WITH its asterisk: the celebrated §7.5 self-refutation was itself wrong on a grep artifact, and one reader re-endorsed it. Falsifiers that fire against the finding are the expensive kind; they still need byte-level receipts.
- **i-4 = C-C-20 (dropped by BOTH readers; adjudicated by this seat) — INFO.** The props/emits payloads are far wider than the render needs: the template reads three fields of `WorkspaceDraft[]` and emits the whole entity back (my subject read); a row-view type + `publish: [slug]` would sever the persistence coupling and would have made M-3's precondition undeclarable. → F.W3/W4 (shapes B-2's contract cure).
- **i-5 = D/i-5 (dropped by BOTH readers; adjudicated by this seat) — INFO.** `Upload` iconography on an action that uploads nothing (publish = make-visible); `Globe`/`Send` carry the model. The only glyph on the row's primary verb. → F.W3/W4 (rides M-6's tone pass).

## Killed-claims register

- **K-1 · D/m-5's `draft-item` half + L-10's falsifier ("grep web/e2e → no hits. Claim survives.") — KILLED** (both readers convergent; my own grep is the receipt: `web/e2e/gallery.spec.ts:38`). L-10's falsifier was declared run and mis-reported — a fabricated-instrument event; D's grep was scoped to `web/src` and generalized. The `drafts-header` half survives (m-12); C-C-19 was right and is the identity guard.
- **K-2 · D/M-2's reduced-motion leg + C-C-7's "lost PRM arm" table row — KILLED** (both readers; my own read of the installed blanket: `animation-duration: 0.01ms !important` + `transition-property: opacity, color, background-color, border-color, box-shadow !important`, reached via `style.css:3`). Consequences run OPPOSITE to the claim: the chevron rotation is suppressed entirely under reduce (transform absent from the forced list); `transition-colors` is clamped and is the library's deliberately-preserved safe set. D's falsifier grepped only the consumer entry. The motion-incoherence half survives (m-1).
- **K-3 · D/M-3's "no success signal" leg — KILLED** (reader-1; my read: `toast("Published!", "success", …)` at gallery.ts:258, on the success path). D's falsifier read the parent handler whole and stopped at the store boundary. Poetic justice: the toast that exists prints the wrong slug (m-13).
- **K-4 · D/M-3's "on success the row simply vanishes" leg — KILLED** (both readers; my savedSnapshots grep). The filter is vacuous; the row is byte-identical after success and NOT unmounted; the correct reading is strictly worse than D's (it is what makes B-2's duplicate-mint reachable). D read the filter's intent as its behaviour.
- **K-5 · D-§7.5 (the corpus's own REFUTED entry) — the REFUTATION is killed; the original claim was right** (reader-1's K5, settled by this seat's python read: `defaultVariants` present; grep artifact reproduced). The header Button is height-pinned `h-(--control-h-md)` today; `py-2` cannot grow it. **Reader-2's re-endorsement of the dead refutation dies with it (instrument-inheritance, DISSENT-1), and reader-2's D-M-7 "second unbooked delta" sharpening collapses on the same byte.** Absence-of-grep-hit is not absence-of-fact in a pretty-printed bundle.
- **K-6 · D §8 step 2 / D-M-1's implied cure (adopt `ui/CollapsibleSection.vue`) — KILLED** (reader-1; ratified by this seat's re-read of the banked fr-CollapsibleSection roster: B-1 ruled hang at the F.W1 target, M-2 teardown, M-3 net-new PRM defect, M-6 no controlled open — adopting the wrapper BUYS defects the subject lacks and cures neither m-6 nor the geometry). The defect (M-2) stands; the cure routes to `@mkbabb/glass-ui/collapsible`. C-C-7's landing was correct.
- **K-7 · C-C-9's consumer-enumeration cell — KILLED, conclusion intact** (reader-1; my grep: ImageUpload.vue:5/:65 IS a consumer, GalleryCardModal is NOT — it imports `overlayUrl`). Three consumer files, three `<img :src>` bindings; the "every consumer is a template binding" conclusion survives on corrected membership. (Nit on the kill itself: reader-1's "four consumer sites" is 3 bindings/3 files/6 grep lines — the corrected cell needed its own precision.)
- **K-8 · D/S-2 ("character-for-character") + D/S-7 ("shared vertical rhythm") — KILLED as superlatives** (reader-1; S-7 dies on K-5's corrected mechanism — a 40/60px height-pinned header vs content-driven ~64px rows share no vertical rhythm; S-2's strings differ materially). The horizontal `px-3` half of S-7 survives as an observation.
- **K-9 · L-8's NaN arm = C-C-14(b)'s "not determinable from the tree" — KILLED** (reader-2; ratified by this seat's own `git show 415effd` run: v1 carried `lastOpenedAt`). `"NaNd ago"` is unreachable; a question one `git show` decides may not be filed UNPROVEN. Residue = m-10.
- **K-10 · D-M-6's MAJOR grade ("a hard build break at `:8`") — KILLED, fact retained** (both readers; my exports reads at both pins). A forward-tense fact wearing a present tense; L-14's INFO adopted (i-1) with D's prop-loss budget rider carried.
- **K-11 · C-C-14(a)'s R3-14 authority ("exactly the defect class adjudicated at R3-14") — KILLED as framing** (both readers; L-13's scope correction sustained): the UTF8_BYTEWISE_CODEPOINT law binds identity/reproducibility ordering in the audit deriver, not a UI display sort. The narrowed claim survives (m-11).
- **K-12 · C-C-2's BLOCKER grade (and with it C's two-BLOCKER framing) — KILLED, substance confirmed at MAJOR** (both readers, ratified): a missing verb on an unbounded local cache whose only concrete harm is self-declared live-undecidable cannot carry the corpus's top grade beside B-2's shipping correctness failure. Grade-discipline precedent: fr-AdminUserList killed-claims #15.

## Superlatives — with provenance (L-18 runs both ways)

1. **★★★ The non-mutating sort, proven load-bearing three times independently** (`:22-25` — `.slice()` first in the chain; `props.drafts` is the parent's cached computed, so an in-place sort would reorder the workspace store from inside a render dependency). All three axes stated AND ran the falsifier; both readers ratified; my subject read confirms the shape.
2. **★★★ C-C-19** — the single most valuable row per byte in the corpus: it caught `draft-item` as a live e2e selector while BOTH peer axes filed it dead (one with a mis-reported falsifier), and wrote the only warning that keeps the obvious cleanup from silently breaking the suite. My grep is the receipt.
3. **★★ D's S-5 self-kill** (aria-hidden): opened as a defect, killed by reading the installed lucide `hasA11yProp` spread, promoted to superlative in public. The model L-18 refutation record — and the standard K-5 shows the same corpus failing elsewhere.
4. **★★ L-13's scope correction against its own corpus** — refusing to let an adopted law reach a line it does not govern, narrowing its own severity as the cost. C-14(a) made the opposite move on the same line and lost (K-11).
5. **★★ The `:key` is the storage layer's own primary key** (`:69` = the IndexedDB `keyPath`, draftStorage:31-32 — my read): uniqueness enforced, not hoped; index-key DOM recycling foreclosed under the live reorder.
6. **★ The nested-interactive trap avoided by construction** (`:91` Publish is a SIBLING of the open-targets) — which is exactly what makes B-1's cure three attributes with zero event-plumbing risk. L/S-6 drew the consequence first.
7. **★ Zero-CLS lazy image by construction** (`:73`/`:79`/`:80` — box reserved before the byte); matches the sibling posture.
8. **★ `basisDisplay[key]?.label ?? b`** (`:44`) closes a hole the compiler cannot see (total Record index signature, no noUncheckedIndexedAccess) and picks the defensible drop policy for a surface where the user owns the data; the divergence from the siblings is F-1's, not this row's.
9. **★ Producer-first subpath + typography consumption with C-S1's honest self-falsifier** (the byte saving nullified by the wrapper's bare-barrel import + manualChunks — and the file holding that edge is the one M-2 retires; one coordinated change).
10. **★ L's explicit negative finding** (no canvas/rAF/WebGL contact; the only pixel surface is a server `<img>`) — naming the absence is what makes the zero-teardown observation true rather than lucky, and it is the discipline half this corpus's confirmation cells lacked.

## UNPROVEN-NEEDS-LIVE — SS-13 visual-audit inputs

1. **THE EMISSION HINGE (F-5)** — does the fourier build emit `[&_svg:not([class*=size-])]:size-(--ui-glyph)` via the `@source "../*.js"` backstop (index.css:222)? Banked open at FR-EQR-14/fr-CollapsibleSection M-7. NOT a browser question: one `npm run build` + grep at the F.W1 pre-gate; the shipped dist (Jun-12 stale, my re-run) is inadmissible either way.
2. **M-1's collapsed-state bottom leg** — whether the root's content box extends below the inline-flex Button's border box (font-strut geometry; R-5). One frame: focus the header, screenshot expanded + collapsed.
3. Perceptibility of the ~1.02–1.04:1 hover and 1.16/1.10:1 borders on a real panel (M-6) — the arithmetic is confirmed; the just-noticeable-difference judgement rides live.
4. M-7's exact wrap breakpoint — and whether coarse-pointer control growth alone forces it before any narrow viewport does (pair with the <768px root up-size).
5. Whether a deployed instance fronts `/api/images` with an auth proxy (F-4) — the dev proxy does not; production topology is not in the tree.
6. IndexedDB quota behaviour under M-10's unbounded growth, and the UX when `saveDraft` rejects into `console.warn` (m-14's write half).
7. Fira Code `calt`/`liga` substitution over a `--`-bearing slug (m-16) — font-and-shaper dependent.
8. Post-B-1-cure focus-ring visibility for the open-targets — whether the repair needs the `.draft-item:focus-visible` D.W4.d entry (which retires m-12's dead-hook question in the same edit).
9. The composed three-family font cascade under the live document (F-3's rider) — each link source-derived, the composition not.

## DISSENT — preserved, not ruled away

1. **Instrument-inheritance inside reader-2's return (the R-1 event).** Reader-2 re-ran D's line-based `grep -o` against a pretty-printed bundle, reproduced its empty output, and CONFIRMED D-§7.5's dead conclusion inside the very row that celebrated the corpus's falsifier discipline — then built a "net-new sharpening" of D-M-7 on the false premise. The C axis had stated the byte correctly all along (its S-5 falsifier cell). Same fleet lesson as fr-CollapsibleSection DISSENT-1, now in its third costume: **a confirmation cell needs a byte-level receipt, not a re-run of the accused instrument** — re-running a broken instrument reproduces its brokenness with higher confidence.
2. **Reader-1's identity-guard miss (the R-3 event).** Reader-1 swept the registry by COMPONENT NAME (`grep GalleryDraftsSection adjudicated/` → 0) and on that basis crowned C-C-3 "the sharpest pure-DESIGN defect in the corpus… only the C axis found it" — while fr-BasisSelector M-10 already banked the defect identity whole, down to the 7-site count and the gallery clone. Name-keyed sweeps cannot find defect-identity folds; sweep by mechanism (the normaliser expression, the shared util name, the router posture), as reader-2 did five times correctly in the same return.
3. **Both readers dropped corpus rows silently.** C-C-20 (wide payload contract) and D/i-5 (Upload iconography) appear in neither return as confirmed, killed, nor folded — adjudicated by this seat (i-4, i-5). A reader return must dispose of every corpus row; a silent drop is indistinguishable from a miss.
4. **Grade dissents preserved**: reader-2's MINOR on the M-7 base leg and on M-6's hover arithmetic (R-6/R-7 — if SS-13 shows the wrap never fires at real breakpoints and the hover reads on-panel, both regrade down); reader-1's MAJOR on m-17's referent collision (if SS-13's pass shows users cross-reading the two tabs' timestamps as one vocabulary, it regrades up).
5. **Reader-1's B-2/C-1 booking dissent preserved** (kept C-C-1 standalone-BLOCKER + the prop-shape row separate; this record composes them as B-2 ∘ M-4): the composition is this seat's, chosen because the duplicate-mint consequence is only reachable through the composed chain; the constituent facts are identical in both bookings.
6. **This seat's adopted-not-rerun cells, enumerated**: the M-4/M-6 token arithmetic (thrice-agreed contrast ratios); lucide's `Icon.js` attribute-spread mechanics; the `MetricBadge` dist root + prop table; the v7.0.0 `withDefaults` size default; the `bg-muted/30`-vs-`hover:` twMerge non-conflict; GalleryCard/Modal's byte-identical mode-label branches; the CollapsibleSection wrapper's geometry cells (left chevron, actions-outside-trigger); tsconfig strictness flags. Every other load-bearing cell was re-derived with the instruments listed in the header.

## Closing verdict

**ADJUDICATED-DEFECTIVE — a 108-line panel whose two BLOCKERs are both lies to the user: the primary verb is keyboard-unreachable in the one gallery file the repo's own D.W4.c sweep never reached, and the publish path is a composed falsehood (a write-only-empty reconciliation field, a vacuous filter, a re-enabling button, an idempotency-free POST over a non-unique index) that mints duplicate public visualizations and then toasts the wrong slug about it. Beneath them: 10 MAJORs (the clipped producer focus ring → glass relay; the hand-rolled disclosure whose cure is the installed primitive and provably NOT the banked-defective wrapper; the statically-doomed Publish gate; the list-wide boolean with the FR-AUL-24 focus fall; the typecheck-silent variant→emphasis uplift break; the imperceptible-hover/muted-verb tone family with the repo's own critique audit as provenance; the undefended meta column that the coarse-pointer token lift force-feeds; absent list semantics; a draft-key contract that is safe only because its documentation is false; and the missing delete verb), 18 MINORs (three of them net-new missed finds verified by this seat: the user-slug toast, the silent IndexedDB-failure empty state, the cache-vs-regeneration seam), 5 INFOs (two rescued from a double reader drop), 6 identities folded by reference (basis normalization → fr-BasisSelector M-10; timeAgo → FR-AUL-17/FR-AFP-32; cm-serif → fr-CollapsibleSection F-3; thumbnail posture → FR-AFP-4; glyph emission → FR-EQR-14; the R5-7 template-binding dual → census methodology), and 12 kills — among them one reader's endorsement of the corpus's own celebrated-but-wrong self-refutation, settled by a single byte read, and one reader's net-new crown jewel returned to the registry row that already owned it. Routings are M-25 agglomeration input: F.W1 owns the uplift sweeps, the emission pre-gate, the tone family, and the fold riders; F.W3/W4 owns the keyboard cure, the disclosure primitive, the publish contract, and the row rework; F.W5-W8 owns the server dedupe and the image cache/auth contract; the method rows go to SS-3/SS-4 with NO-WAVE-OWNER. Nothing here stamps a wave.**

— end of apotheosis. The three challenge files remain immutable beside this record per E-1/E-3; the two re-reader returns are incorporated by adjudication above.
