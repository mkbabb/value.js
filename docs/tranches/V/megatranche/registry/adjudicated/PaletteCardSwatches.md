SEAT RECEIPT — served model id: `claude-fable-5` (Fable 5), FRESH FABLE ADJUDICATOR (M-23/L-18), observed in this seat's own system prompt, not inherited.

# PaletteCardSwatches — adjudicated apotheosis (M-23/L-18, incredulity seat)

**Date**: 2026-08-06 · **HEAD**: `3ea0144c` · branch `tranche-u` · **Subject**: `demo/palettes/browser/card/PaletteCard/PaletteCardSwatches.vue` (96 L) + its inseparable closure: `SwatchHoverMenu.vue` (93 L), `composables/useHoverPopover.ts` (67 L), `composables/useLeaveTimer.ts` (17 L), `composables/useHeightTransition.ts` (88 L).
**Drift disclosure**: `git diff --stat c654824e..HEAD` over the cone (`demo/palettes/browser/card/ demo/styles/ demo/ui/ src/`) is **EMPTY** — the cone is byte-identical to the corpus HEAD `c654824e`; every line anchor below resolves at both.
**Corpus**: `challenge-D-design.md` · `challenge-C-implementation.md` · `challenge-L-library.md` (each Opus 5 `claude-opus-5[1m]` by declared receipt, 2026-07-29, at `c654824e`; the current files govern) + 6 probe scripts + 16 committed captures incl. `evidence-hover-panel-offscreen.png`.
**Re-readers adjudicated**: design-and-usage (reader 1) · library-and-code (reader 2). Both were challenged back: two reader-vs-reader collisions ruled (R-1, R-2), one hypothesis upgrade ratified (R-3), two severity/headline demotions ratified over the other reader's carry (R-4, R-5), one scope caveat elevated to a wording kill (R-6), one confirmed limb demoted (R-7), and one kill EXTENDED by this seat to a cell neither reader booked (K-7).
**Motion law**: `audit/codex-provenance/motion-quarantine.md` re-read at the cited rows this session — **M-16 TRUE, M-19 TRUE (carried); M-17 FALSE (its "no reduced-motion branch" claim NOT carried** — but its probe record is legitimately cited as the no-preference property-list witness: `transform` absent from BOTH lists, so `active:scale-95` never animates in either mode). Reader 2 declined M-17's FALSE limb explicitly; reader 1 carried only M-16/M-19 — both compliant.
**Wave authority for routing**: tranche X ratified — `docs/tranches/X/waves/W0..W11`. **X.W7** owns `demo/palettes` whole and its file table lists `PaletteCard/PaletteCardSwatches.vue` as **modify** (W7.md:101): X.W7.c (Palette specimen + arbitrary-N law; props-only `PaletteSpecimen`, zero nested interactive controls, G8–G12) · **X.W7.d (Entity inspector and CRUD ownership — per PR-26/VC:102 the card body's expand/hover-swatch-action surface is REMOVED and per-colour verbs move to the selected inspector)** · X.W7.f (display formatting) · X-W10 (motion/canon). Every component-local cure below is a requirement on the SUCCESSOR surface, not a patch to this file.
**Sibling-record identity guard (anti-rename law)**: `registry/adjudicated/PaletteCard.md` (this seat class, 2026-08-06) adjudicated the same cluster from the parent's corpus and BANKED the shared spine — PC-1 (PRM never-completes), PC-2 (floating-panel), PC-3 (swatch-not-a-control incl. the as-child touch drop), PC-21 (CopyResult discarded), PC-23 (timers), PC-24 (padding canon), PC-35 (post-hoc alphas), PC-38 (size-literal triplication), PC-39 (PaletteDialog ghost). **One identity each — this record folds onto those rows and never re-books them**; it adds only what this corpus sharpens. Both registry files fold into X.W7.c/d at wave open.
**Arbiter's own instruments (all static; zero browser tooling per law)**: full reads of all five subject files; PaletteCard.vue spans :33-37/:44-46/:53-59/:93-104/:139-152/:193-197/:220-230/:246-255/:290-296/:330-335; CurrentPaletteEditor.vue:28-42 (+`transform` grep — the L-8 decider); useSwatchActions.ts:7/:40; producer bytes: `WatercolorDot.vue.d.ts` prop set (no `tag`), compiled `watercolor-dot.js` (`inheritAttrs` + `aria-hidden:"true"` ×2 + `pointerEvents:"none"`), `useClipboard-D36OTaeT.js` whole head (the R-2 decider: try/catch → `{ok:false,reason}` — **cannot reject**), `dist/dom.js:38-52` (`useBreakpoint` un-memoized, scope-disposing), `Popover.vue.d.ts:2/:9` (`PopoverTriggerMode = "click" | "hover"` — the C-7 cure decider), `a11y-overrides.css` head bytes (the K-1/PS-4 decider: PRM `transition-property: opacity, color, background-color, border-color, box-shadow !important` under `*:not([data-allow-motion])`), glass `package.json` (v7.0.0; exports = **74** keys; `sideEffects:["*.css"]`); Vue runtime `hasExplicitCallback` at runtime-dom.cjs.js:135/:196/:232 (vue 3.5.35); demo bytes: foundation.css:56 (glass import), :117 (`--max-width-tooltip: 12.5rem`), :582-587 (`.slug-pill` recipe), :725-750 (`prefers-contrast: more` block incl. `.slug-pill { border-width: 2px }`), focus-ring.css:1-16 postmortem; eslint.config.js:230-240/:275-281 + `ls demo/@` (No such file); EmptyState.vue:40-48 (three ghost dots, `aria-hidden`) — resolving reader 2's declined verification; canon verbatim: VISUAL-CONSTITUTION.md:21/:91/:102, PROPORTION-AUDIT.md §5 items 12–13, proportion-register.md:69 (PR-26), om-14 FORMAT-AUDIT.md:116-118 (B5–B7); greps re-run exit-checked: `floating-panel` (4 hits, 0 rules, 0 in glass dist), `btn-interactive` (0 in glass), the 116-char recipe (exactly 6: PaletteCardSwatches :44/:51/:58 + CurrentPaletteEditor :46/:49/:52), `tag="button"` (**6 live + 1 comment-prose**), the slug-pill literal (exactly 2: foundation.css:586 + the subject :9), `slug-pill` consumers (4 files), `ring-ring` fleet (**25 occurrences / 12 files**), `--color-ring|--ring:` (0 declarations anywhere; sole hit = the focus-ring.css:11 postmortem), `writeClipboard` import census (12, all root-barrel) + the 6 palettes call sites, demo/ui barrel register (**18 of 19 from the bare root; input alone from `/forms`**), and the C-12 vacuous-gate grep (`test/ e2e/` → **0 files**).

## Scoreboard

**41 adjudicated rows CONFIRMED-family (2 BLOCKER · 17 MAJOR · 17 MINOR · 5 INFO), of which 11 are IDENTITY-FOLDS to already-banked adjudications (PaletteCard.md ×9, GenerateControls.md ×1, ColorSpaceSelector.md ×1) and 9 are missed rows adopted from the re-readers (both decks; only ONE was convergent) · 13 claims KILLED/DEMOTED with written refutations (2 of them reader-vs-reader rulings, 1 an arbiter extension neither reader booked) · 11 UNPROVEN-NEEDS-LIVE residue rows → visual-audit inputs.**

Blocker-weight headline: **the component's entire verb surface is unreachable on every input class, and the canon has already ordered the surface deleted.** The hover panel seats its actions in a class no stylesheet declares (PS-1); the swatch face discards `tag`/`aria-label`/click on the hover path AND the reka trigger merge on the touch path (PS-2); the hybrid-device third class satisfies neither oracle and gets NO panel at all (PS-34); under PRM the disclosure never completes and strands a zero-pixel focusable control per collapse (PS-4); and PR-26/VC:102 rule the whole expand/hover-action pattern REMOVE — X.W7.d is the terminal cure for more than half this roster.

## Ruled disagreements (the incredulity seat's actual work)

| id | design-and-usage (reader 1) | library-and-code (reader 2) | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **R-1 · the WCAG 2.5.8 citation on D-6/C-4** | K5: the normative failure is NOT established — the spacing exception is evaluated against adjacent TARGETS, and every neighbour is a non-target | re-CONFIRMED C-4/D-6 whole, "below WCAG 2.5.8's 24 × 24 floor", MAJOR | **reader 1 WINS; the normative clause is STRUCK, the design finding survives (PS-7)** | D-6's own role table (challenge-D-design.md:343) classifies the pill "**inert text**" — an inert span is not a target and cannot defeat the Spacing exception it was cited against; the dots below are `pointerEvents:"none"` (producer bytes, my read) — not hit-testable, not targets; the nearest true target (the Palette-menu Button, PaletteCard.vue:96-104) sits in another row beyond a 24 px radius. On today's tree the exception plausibly APPLIES; no hard SC 2.5.8 failure is demonstrated. Reader 2 repeated the corpus's normative cell without re-deriving the exception — recorded in DISSENT as citation-inheritance-adjacent. **Cure-order rider adopted (reader 1's, correct and unique)**: the exception evaporates the instant PS-2's cure makes the dots real 40 px targets 12 px below — the successor must size the copy seat BEFORE or WITH the swatch-activation cure. |
| **R-2 · L-7's ":16 is the worst of the six — a floating rejection is unhandled"** | endorsed the inventory AND the ordering ("the only one that does not even `void` it is correct") | K1: `writeClipboard` CANNOT reject — try/catch returns `{ok:false,reason:"clipboard-api"}` | **reader 2 WINS; the sub-limb is KILLED (K-9)** | My own byte read of `useClipboard-D36OTaeT.js:3-16`: missing API → `{ok:!1,reason:"no-api"}`; rejection → caught, `console.warn`, `{ok:!1,reason:"clipboard-api"}`. The d.ts documents the discriminated result as the primitive's whole point. There is no unhandled-rejection hazard; :16 is exactly as bad as the other five. The parent finding (result discarded 6/6, feedback surface idle) is untouched — and is an IDENTITY-FOLD to PaletteCard.md PC-21 (PS-23). |
| **R-3 · C-11 (hover re-renders all N)** | carried as UNPROVEN hypothesis, per the source's own label | UPGRADED to confirmed MINOR: determinate from source | **reader 2 WINS; upgrade RATIFIED (PS-28)** | useHoverPopover.ts:17 `reactive({top,left})`; PaletteCardSwatches.vue:31 spreads that object INSIDE the `v-for` (:26) — the spread reads every enumerable key in this component's render effect, so `positionPanel`'s writes (:22-23) re-run the whole render, minting N fresh prop objects and patching all N `SwatchHoverMenu` children. Vue semantics, no measurement required. The per-frame COST stays live-only (residue V-7, sharpening PaletteCard.md U-8 from "premise real" to "mechanism adjudicated"). |
| **R-4 · L-6 severity (root-barrel `writeClipboard`)** | CONFIRMED MAJOR (structural claim; weights unproven) | K4: marginal cost ZERO here (barrel already loaded 3 ways by this subtree), the lint cure would fail 18 of the demo's own 19 barrels, `sideEffects:["*.css"]` tree-shakes, "68 subpaths" is 74 → MINOR | **reader 2 WINS; DEMOTED to MINOR (PS-29)** | My barrel census: 18/19 `demo/ui/*/index.ts` re-export from bare `@mkbabb/glass-ui` (input alone uses `/forms`) — the root register is the demo's deliberate seam, so the proposed lint wall is mis-scoped; exports = 74 (my count); sideEffects verified. The correct-idiom contrast (useHoverPopover.ts:3 `/dom`) survives. **IDENTITY ≡ GenerateControls.md row 23 (CLIPBOARD-ROOT-REGISTER, already booked at MINOR with the same 12-site census)** — folded, not re-booked. |
| **R-5 · L-5's headline ("the boundary law matches zero files")** | adopted as filed, MAJOR | K5: two of three gates match zero files; G-DEMO-3b's glob array leads with LIVE `demo/color-picker/**`; only the BAN pattern is a retired alias | **reader 2 WINS on the sentence; finding survives rescoped (PS-16)** | My eslint.config.js:230-240 read: the object carries `demo/color-picker/**/*.{ts,vue}` (live tree) beside four dead `demo/@/…` globs; :275-281's composables object is wholly dead; `ls demo/@` → No such file. Corrected headline: "the ban targets an unwritable retired alias, and two of three gates glob a deleted tree." **IDENTITY ≡ ColorSpaceSelector.md L-5(a)** (lint-glob liveness, born-RED `eslint --print-config` command already booked) — folded. L-5's honest negative (the card barrel correctly excludes this private component; the seam holds by convention) is preserved and lands in SUPERLATIVES. |
| **R-6 · D-13's blast radius ("swallows every click in the panel")** | confirmed WITH the wording caveat: teleported DOM bubbles through the real tree to `<body>`, never through this root | confirmed as filed, wording unqualified | **reader 1 WINS; wording KILLED, finding scoped (K-12, PS-20)** | `<Teleport to="body">` (SwatchHoverMenu.vue:37) moves real DOM; native events from the panel's buttons bubble button→panel→body and never traverse PaletteCardSwatches.vue:2's `@click.stop`. The dead-zone claim is true of the in-flow expanded region (~111 px) — which is what the finding's own measurement means — and false of the teleported panel. |
| **R-7 · D-14's `items-start` limb** | confirmed as a no-op "while a single row exists" | declined: conditional, not dead — it becomes load-bearing whenever wrap rows differ in height (the `sm:` size rung, the `ghost` variant) | **reader 2 WINS; limb DEMOTED to conditional (K-13)** | `items-start` on a `flex-wrap` container is `align-items` — inert at one row, semantic at two+. A "dead declaration" claim requires all reachable states; N and viewport are unbounded (PS-25). The `min-w-0` limb IS dead on the primary path (parent is a plain block at `layout="default"` — my PaletteCard.vue:40 read via the corpus, re-derived) and stays confirmed. |

## Adjudicated defect roster

Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave. Original corpus ids ride for life; PS-ids are this record's spine. Line anchors resolve at HEAD `3ea0144c` (= corpus bytes).

### BLOCKERs

- **PS-1 · BLOCKER — `.floating-panel` is declared in no stylesheet anywhere; the teleported hover action panel is `position: static`, full-body-width, off-screen at x = −viewportWidth/2; the inline `top`/`left` written by useHoverPopover.ts:22-23 are inert.** (D-1/C-2/L-1; both readers; my greps: 4 hits — SwatchHoverMenu.vue:42 sole use, 3 prose/comments — zero rules in demo/src and zero in glass 7.0.0 dist. Provenance: `c84504d3` deleted `demo/@/styles/floating-panel.css` — `position:fixed; z-index:var(--z-overlay)` — on a commit message claiming glass-ui provides it; it does not. The two seats' geometry is ANALYTICALLY corroborated: a static block child of `<body>` under `translateX(-50%)` must land at x = −w/2, and both independent probes satisfy the relation exactly at different viewports.) **IDENTITY ≡ PaletteCard.md PC-2 → X-W7.c/d** (the hover path dies with the specimen split). **Cure-level rider (K-4)**: the corpus's interim `glass-floating` substitution is REFUTED — it computes `position: relative` (L-1's own table) and would restore the surface while leaving the panel in flow at document end, a visible panel in the WRONG place that could read as "fixed". The only corpus prescription that closes the geometry whole is L-8's (`positionPanel` returns a complete `{position:"fixed", top, left, transform}` style) — and the headline cure (one Popover, both pointer classes) makes both moot.
- **PS-2 · BLOCKER — the swatch is not a control on ANY input path: glass-ui 7 `WatercolorDot` declares no `tag`, sets `inheritAttrs:false`, hardcodes `aria-hidden="true"` + `pointer-events:none` — so `tag="button"`, `:aria-label`, `@click.stop` (hover path, SwatchHoverMenu.vue:32-35) and the `PopoverTrigger as-child` merge (touch path, :13-20) are ALL silently discarded; `vue-tsc` exits 0 over all of it.** (D-2/C-1/L-2; both readers; reader 2 ran the typecheck itself. Producer d.ts + compiled bytes re-read by this seat. The dead chain SwatchHoverMenu:35 → PaletteCardSwatches:36 → PaletteCard:152 → useHoverPopover:51-54 is source-traced. Reader 1's nuance adopted: the panel still OPENS because the pointer listeners ride the wrapper div (:2-6), which is exactly why the desktop branch fails visibly (PS-1) while the touch branch fails silently.) Canon: VC:91 (P051 removes the `tag="button"` branch in the clean major) — verified verbatim. Causation **consumer debt, not a glass seam** (≡ GenerateControls.md R-D; the producer never promised `tag`). **IDENTITY ≡ PaletteCard.md PC-3 → X-W7.c** (named geometric activation seat) **+ X-W7.d** (per-colour verbs to the inspector). The 6-live-site dead-`tag` census (my grep; D-2 said "5" — K-8) rides the standing glass relay as migration input only, already booked at GenerateControls.md.

### MAJORs

- **PS-3 · MAJOR — the hover panel carries `aria-hidden="true"` over three focusable native `<button>`s (axe `aria-hidden-focus`, WCAG 4.1.2), teleported to document end with no focus trap/restore/Escape, and its self-declared mitigation (SwatchHoverMenu.vue:38-39 "the reka-ui Popover (touch path) is the accessible route") names a route PS-2 proves dead.** (D-3/C-3; reader 1's demotion BLOCKER→MAJOR ratified — it is a consequence of PS-1/PS-2, not an independent axis; the compounding `focus-visible:ring-2` on buttons declared invisible to AT (:44/:51/:58) is confirmed.) **→ X-W7.d.**
- **PS-4 · MAJOR — under `prefers-reduced-motion: reduce` the expand/collapse Transition NEVER COMPLETES: `done()` is gated exclusively on a `height` transitionend (useHeightTransition.ts:35-39/:66-70), the central PRM guard strikes `height` from the transition set entirely (a11y-overrides.css bytes, my read; author-`!important` beats the author-normal inline shorthand at :30/:61), and the 2-arity hooks mean Vue 3.5.35 installs NO fallback (`hasExplicitCallback`, runtime-dom.cjs.js:135/:196/:232, my read).** Both re-readers derived this independently — the corpus's ONLY convergent missed row — while the corpus's own D-7 limb 1 asserted the exact inverse (K-1). **IDENTITY ≡ PaletteCard.md PC-1 → X-W7.c/d + X-W10 rider** (a JS-completed transition may never gate `done()` on a property the central PRM policy strikes). **This corpus ADDS to PC-1's record, adopted from reader 1's M2**: (a) LEAVE — the collapsed subtree stays mounted at `height:0; opacity:0` under `overflow-hidden`, leaving the `Copy slug` `<button>` (:13-19) a zero-pixel focusable tab stop, one per collapsed card, accumulating across the Browse wall — a WCAG 2.4.3/2.4.7 defect that exists ONLY for reduced-motion users; (b) ENTER — the :44-46 cleanup is unreachable, pinning a stale inline pixel height that clips any later re-wrap (crossing the `sm:` rung of `w-9 h-9 sm:w-10 sm:h-10`). Cite M-03/M-07 posture per the quarantine: the 0.1 s colour floor is correct central governance, NOT part of this defect.
- **PS-5 · MAJOR — the animated property is layout-forcing `height` read off a synchronous `scrollHeight` with deliberate double-forced reflow (:29-32, :54-56, :61-63), on bespoke 350/250 ms durations the file's own header admits match no token (:1-7), while the tokenized alternative (`--vj-morph-*`) is in use in the same parent (PaletteCard.vue:358-363).** (D-7 limb 2; both readers.) **→ X-W7.c** (the disclosure dies; any successor motion rides `@mkbabb/glass-ui/expandable-container` or tokens) **+ X-W10** (duration canon).
- **PS-6 · MAJOR — the press leg of all four buttons is untimed in BOTH motion modes (`active:scale-95` under `transition-colors`, whose property list excludes `transform` at no-preference and under reduce alike), and the ruled migration target for this exact recipe — `btn-interactive`, T.W5-R5 via demo/DESIGN.md:237,247 — does not exist in glass-ui 7 (my grep: zero matches).** (D-7 limb 4 + L-3, merged per reader 2's shape; motion-quarantine M-16/M-19 TRUE carried, M-17's FALSE limb NOT carried.) The second dead-class-as-contract instance after PS-1 — the architectural thesis (class-name channels are unversioned, uncontracted, unlinted) is confirmed on two independent classes. **→ X-W7.c** (Button atom carries press choreography) **+ X-W10** (the DESIGN.md ruling cites a dead target — canon correction obligation).
- **PS-7 · MAJOR — affordance inversion: the inert slug pill wears the bordered-control signature while the row's only real control is a 16 × 16 px bare glyph (`p-0.5` + `w-3 h-3`, no min-size), with the ratified cure eleven lines up in the parent (PaletteCard.vue:93-104 `Button icon-only size="sm"` + the S.W5-4 comment naming this exact defect).** (D-6/C-4 as RESCOPED by R-1: the **WCAG 2.5.8 normative clause is struck (K-5)** — the spacing exception plausibly applies on today's tree because no neighbour is a target — and D-6's audit-attribution sentence is struck (K-7). The finding stands on affordance, undersized control, and the ratified sibling cure — all confirmed.) **Cure-order rider**: size the copy seat before/with PS-2's cure, or the 2.5.8 exception genuinely fails the day the dots become targets. **→ X-W7.c/d.**
- **PS-8 · MAJOR — the icon-button recipe is hand-rolled six times verbatim across two files (my grep: PaletteCardSwatches :44/:51/:58 + CurrentPaletteEditor :46/:49/:52) plus the `p-0.5`/`shrink-0` near-copy at :14, with the producer atom already imported by the parent.** (D-8/C-6/L-4; D-8's premise sentence KILLED as written — K-3 — the variance it denies is the very thing D-6 indicts. D-8's atom table also routes the slug pill to `@mkbabb/glass-ui/chip`; corrected by PS-18 — the RATIFIED home is the in-repo `.slug-pill` recipe.) **→ X-W7.c.**
- **PS-9 · MAJOR — `focus-visible:ring-ring/40` reaches a token that does not exist in the served cascade: no `--color-ring`/`--ring` declaration anywhere (my grep; the sole textual hit is the focus-ring.css:11 postmortem naming this exact death), so the utility emits nothing and bare `ring-2` falls back to `currentColor`.** (D-9; reader 2's sharpened Tailwind-v4 mechanism adopted: a `ring-<name>` utility compiles only when `--color-<name>` exists in the theme — and glass-ui's bridge ships `--color-border`/`--color-accent`/… but no `--color-ring`.) Fleet condition: **25 occurrences / 12 files** (my count), against the mandated ONE-token dual-contrast recipe (focus-ring.css:4-7). Painted-pixel half is residue V-3. **→ X-W7.c** (the successor uses the focus-ring recipe) **+ X-W10** (fleet sweep under the one-recipe law).
- **PS-10 · MAJOR — the disclosure has no payload: the expanded panel re-renders the SAME `palette.colors` array the collapsed strip already shows (PaletteCard.vue:33-37 vs :139-141), and on `/#/palettes` (no `show-slug`) the expansion adds literally nothing.** (D-4; both readers; canon verbatim: PR-26 verb **REMOVE / ADD-AFFORDANCE** — "detail/verbs/durable status move to selected inspector; compact swatches remain data" — and VC:102 — "The card body owns no expand … or hover-only swatch-action path".) **→ X-W7.d — the terminal cure for this row and, transitively, for PS-1/PS-3/PS-14/PS-16/PS-20/PS-26/PS-27/PS-28.**
- **PS-11 · MAJOR — N = 0 is a named, reachable state (the parent's own S.W2 W2-9 comment, PaletteCard.vue:220-223) that expands into an undesigned 24 px blank band — ruled with a border precisely when the slug row is absent (:23) — while the designed empty mark sits unused (EmptyState.vue:40-48, three ghost `WatercolorDot`s, verified by this seat, resolving reader 2's declined check).** (D-5.) **→ X-W7.c** (the specimen decides the zero-colour state explicitly; converges with PaletteCard.md PC-25).
- **PS-12 · MAJOR — the padding ladder is 12 px (`px-3`, plus internal 10/12/12 drift: `pt-2.5` vs `pt-3`) against the named canon `C = --spacing(4)` = 16 px (PROPORTION-AUDIT §5 item 12, verified verbatim).** (D-10; reader 2's qualification adopted: the parent shares the deviation — a card-cluster non-conformance, understated if booked against the leaf alone.) **IDENTITY-family ≡ PaletteCard.md PC-24 → X-W7.c.** Fold; this corpus adds the leaf's internal asymmetry straddling the border-t.
- **PS-13 · MAJOR — the provenance chip is the boldest ink on the card (700 vs the palette identity's 500) and is tinted with the palette SEED, which VC:21 forbids outside named lanes; sibling chips tint with ROLE tokens.** (D-11; both readers; canon verbatim.) **Weight limb RESCOPED by this seat against PS-18's find**: `font-bold` is the ratified `.slug-pill` recipe's own weight (foundation.css:586) — the hierarchy inversion indicts the RECIPE fleet-wide (all 8 migrated sites), so its cure sits at the recipe/canon level, not this leaf. Seed-tint limb stays leaf-owned (`safeFirstColor` from `palette.colors[0]`). Counterweight honestly carried: the ink is contrast-certified by construction on the named WELL tier (see SUPERLATIVES). **→ X-W7.c/f + X-W10** (weight rung at the recipe). 
- **PS-14 · MAJOR — false press affordance: the card's pointer-phase press spring (`v-bind="press.handlers"`, PaletteCard.vue:24) runs under a swatch that then does nothing — `@click.stop` at :2 stops the CLICK phase only, and the dot itself is `pointer-events:none`.** (C-5; structurally decidable; spring amplitude residue V-4.) Converges with PaletteCard.md PC-20 (press on the container). **→ X-W7.c G11 / X-W7.d.**
- **PS-15 · MAJOR — one affordance, two implementations, and the branch that ships to every desktop is the broken one (`v-if="!canHover"` reka Popover vs the `v-else` hand-rolled Teleport) — which is exactly why no gate ever exercised the working branch.** (C-7; the PANEL_LAYOUT constant's shared-LAYOUT-cannot-arrest-SEMANTIC-drift reading confirmed.) **Cure buildable TODAY** (reader 2's find, verified by this seat): glass Popover ships `trigger?: "click" | "hover"` + `openDelay`/`closeDelay` (`PopoverTriggerMode`, my d.ts read) — one Popover for both pointer classes deletes useHoverPopover.ts (67 L), useLeaveTimer.ts (17 L) and the Teleport branch with zero new abstractions. **→ X-W7.c.**
- **PS-16 · MAJOR (RESCOPED per R-5) — the import-boundary law documented as protecting this component's seam is dead law: the ban pattern is an unwritable retired alias (`@components/custom/palette-browser/**`), two of three gates glob the deleted `demo/@` tree, and the third's live glob (`demo/color-picker/**`) guards a different tree.** (L-5 as corrected by K-11.) The seam holds by convention — the barrel correctly excludes this private component (honest negative preserved). **IDENTITY ≡ ColorSpaceSelector.md L-5(a)** (lint-glob-liveness fold, born-RED command already booked). Not re-booked.
- **PS-17 · MAJOR — panel-centring ownership is split across the composable and its consumers, and the two consumers DISAGREE: useHoverPopover emits the swatch CENTRE as `left` (:23), PaletteCardSwatches adds `translateX(-50%)` in transit (:31), CurrentPaletteEditor passes the style with NO transform (:35, my read — the only `transform` in that file is a scoped `transform-origin`).** (L-8; both readers.) Invisible today only because PS-1 makes the offsets inert; it surfaces the moment PS-1 is cured — the corpus's sharpest latent-regression insight, and the anti-resurrection guard for PS-26. **→ X-W7.c** (dies whole under the one-Popover cure; if any interim survives, `positionPanel` returns the COMPLETE style).
- **PS-18 · MAJOR [MISS-adopted, reader 1] — the slug pill is the ONE surviving hand-rolled copy of the repo's own ratified `.slug-pill` recipe (my grep: exactly 2 hits — the foundation.css:586 definition and the subject :9), and by staying a literal it silently opts out of the `prefers-contrast: more` border bump every other slug chip receives (foundation.css:725-750: `.slug-pill { border-width: 2px }` in the elevated-contrast selector list — the hand-rolled copy is not `.slug-pill`, so in the one mode where a user asked for visible boundaries this chip alone stays a 1 px seed-tinted hairline).** Eight sites migrated (dock menus ×4, slug bar ×2, admin ×2 — consumer files verified); this one did not. Corrects TWO corpus mis-prescriptions: D-8's `chip`-atom routing and L-4's icon-only grep scope. Cure is one token: `class="slug-pill truncate max-w-tooltip"`. **→ X-W7.c/f.**

### MINORs

- **PS-19 · MINOR — the `border-border/15` divider is in the DOM and not on the screen, and the file's four post-hoc alpha families contradict the parent's own retirement edict (PaletteCard.vue:45-46).** (D-12; **IDENTITY ≡ PaletteCard.md PC-35** — the folder-wide census is banked there.) **This corpus ADDS the rider (reader 1's M5, verified bytes)**: `prefers-contrast: more` re-derives `--border` toward full-ink (foundation.css:736), but Tailwind's `/15` compiles to `color-mix(… 15%, transparent)` ON TOP of the token — the modality cure is throttled to 15 % of its declared weight in exactly the mode built to fix it. A post-hoc alpha on a modality-managed token defeats modality management — the structural reason the idiom was retired. Fold → PC-35 → X-W7.c under X-W10.
- **PS-20 · MINOR (SCOPED per R-6) — the ~111 px expanded region is a click dead-zone: the bare `@click.stop` at :2 swallows clicks that would collapse/select the card, and per PS-2 nothing inside responds either. The teleported panel is explicitly OUT of this claim (K-12).** (D-13.) **→ X-W7.d.**
- **PS-21 · MINOR — `min-w-0` at :22 is a dead declaration on the primary path (block-formatting parent at `layout="default"`); the `items-start` limb is DEMOTED to conditional per R-7.** (D-14.) **→ X-W7.c.**
- **PS-22 · MINOR — one box's position is owned by three files with the type WIDENED at the middle hop (`reactive({top,left})` → `Record<string, string | number>` :82 → `CSSProperties` SwatchHoverMenu:73); structural typing passes all three, so key drift is not a compile error.** (D-15/L-9.) **→ X-W7.c** (dies with the composable).
- **PS-23 · MAJOR at the bank — the clipboard verdict is discarded at 6/6 palettes sites (my grep census exact) while `ActionFeedback` + `showFeedback` sit wired to nothing.** (D-16/C-8/L-7, with the "worst of six / floating rejection" sub-limb KILLED per R-2/K-9.) **IDENTITY ≡ PaletteCard.md PC-21 → X-W7.d.** Fold; counted in the MAJOR column to keep severities consistent across the registry.
- **PS-24 · MINOR — raw CSS colour strings interpolated into accessible names at :43/:50/:57 ("Edit color oklch zero point six two…").** (D-17; cross-booked LINE-EXACT with om-14 FORMAT-AUDIT B5–B7 **RAW-ARIA**, verified by this seat.) **→ X-W7.f** (the formatting facility owns the spoken form) **/ X-W7.d.**
- **PS-25 · MINOR — no cap, no scroll, no ceiling at large N (unconditional `v-for`, no `slice`, store stores `colors` verbatim), while the codebase ships the idiom twice; D-18's in-repo cites are substance-true at DRIFTED lines (VersionHistoryDrawer.vue:53, PreviewStrip.vue:34 — reader 1's correction adopted).** (D-18.) **→ X-W7.c** (the arbitrary-N law is X.W7.c's own title).
- **PS-26 · MINOR — panel position is computed ONCE inside `nextTick` with a magic `−42`; no scroll/resize/ResizeObserver/clamp/flip exists anywhere in the file (whole-file read, both readers + this seat).** (C-10; the visible symptom stays hypothesis — masked by PS-1 — the ABSENCE is source-certain.) Anti-resurrection guard: PS-1's cure must not revive this; converges with PS-17. **→ X-W7.c.**
- **PS-27 · MINOR — `useLeaveTimer` has no scope disposal (17 lines, no vue import at all); a card unmounting inside the 250 ms window fires the callback against a disposed scope — latent, benign today (a ref write), correctly graded by the corpus itself.** (C-9.) **IDENTITY ≡ PaletteCard.md PC-23.** Fold. Contrast discipline: glass's own `useBreakpoint` disposes (`dom.js:49`).
- **PS-28 · MINOR (UPGRADED per R-3) — one hover re-renders all N swatches: the reactive style object is spread inside the `v-for`, so every `positionPanel` write mints N fresh prop objects and patches N children where one element's `:style` write would do.** (C-11, hypothesis→confirmed mechanism; cost residue V-7.) **→ X-W7.c** (dies with the composable).
- **PS-29 · MINOR (DEMOTED per R-4) — `writeClipboard` reaches the glass root barrel where the published `./dom` subpath exists and the correct idiom sits three files away (useHoverPopover.ts:3); 12/12 demo import sites use the root.** (L-6 minus its killed impact figures and mis-scoped lint cure — K-10.) **IDENTITY ≡ GenerateControls.md row 23.** Fold.
- **PS-30 · MINOR — the swatch-size literal `w-9 h-9 sm:w-10 sm:h-10` is owned twice + doc-comment copy + a fourth hand-rolled instance, and this component's REQUIRED `swatchClass` (:83) makes the producer default permanently unreachable on this path.** (L-10.) **IDENTITY ≡ PaletteCard.md PC-38.** Fold.
- **PS-31 · MINOR — a domain rule lives as a negated `boolean | undefined` in a presentational leaf (`v-if="!isLocal"` :42) while the same parent computes `PaletteKind` and hands it to a sibling child (PaletteCard.vue:225 vs :142).** (L-11.) **→ X-W7.d** (capability gating becomes the inspector's, typed).
- **PS-32 · MINOR — clipboard ownership splits inside one parent/child pair eleven lines apart: slug copy executes locally (:16), colour copy emits up (:59 → PaletteCard.vue:331-334); four homes total across the feature.** (L-12.) **→ X-W7.d.**
- **PS-33 · MINOR [MISS-adopted, reader 1] — the truncating slug pill (`truncate max-w-tooltip` = 200 px, foundation.css:117) has no `title`/tooltip while the equivalently-truncating palette name one row up has one (PaletteCard.vue:53-59 `:title`); the adjacent button copies `displaySlug` IN FULL — what is copied is not what is shown, with no way to see the difference.** (The C-axis's own probe used `aVeryLongUserSlugName` and drew no conclusion.) Adjacent to PaletteCard.md PC-33 (disjoint recovery), distinct instance. **→ X-W7.c/f.**
- **PS-34 · MINOR [MISS-adopted, reader 1] — the THIRD device class no axis enumerated: on hover-capable touch devices (touchscreen laptops, iPad + trackpad) the panel is ABSENT ENTIRELY — branch selection reads the media query alone (`(hover: hover)` ⇒ Popover never mounts) while the hover opener applies a SECOND oracle and bails on `e.pointerType === "touch"` (useHoverPopover.ts:27, verified). One component reads pointer capability through two incompatible oracles three files apart; their intersection is an unreachable branch.** Not off-screen (PS-1), not swallowed (PS-2): absent. **→ X-W7.c** (the one-Popover cure collapses both oracles). Live witness residue V-10.
- **PS-35 · MINOR [MISS-adopted, reader 2] — every swatch mounts an unconditional `<Teleport to="body">` (:37) whose content guard sits one level in (:41 `v-if="open"`): M × N Teleport instances + anchor comments for zero output on every expanded card; the corpus's own probe photographed the residue (`bodyTail: "<div><!--teleport start--><!--teleport end--></div>"`) without recognising it.** Cure is one token (`<Teleport v-if="open">`) — moot under the one-Popover cure. **→ X-W7.c.**
- **PS-36 · MINOR [MISS-adopted, reader 2] — `useBreakpoint("(hover: hover)")` is instantiated once per PaletteCard (useHoverPopover.ts:11 via PaletteCard.vue:255, again via useSwatchActions.ts:40) for a global environment fact; the producer creates a fresh `matchMedia` + listener per call with no cache (dom.js:38-52, my read) — duplication, not a leak (it disposes correctly).** The inverse of the corpus's L-13 (state hoisted too high; the environment query pushed too low — in the same composable). **→ X-W7.c/d.**

### INFO

- **PS-37 · INFO [MISS-adopted, reader 2] — useHoverPopover.ts:8's doc block names `PaletteDialog` as a consumer; no such file exists.** **IDENTITY ≡ PaletteCard.md PC-39** (which already books this exact line plus constants.ts:6 and DESIGN.md:386). Fold.
- **PS-38 · INFO — the seam runs across one interaction rather than around one concern: 8 props / 8 emits, zero reactive state; 4 props forwarded verbatim, 5 emits re-emitted verbatim; five files own one popover (state PaletteCard.vue:246-255 · geometry useHoverPopover:20-24 · transform :31 · DOM SwatchHoverMenu:37-51 · CSS the DELETED floating-panel.css). Reader 1's causal sentence stands: no single file's review surface contains both the class name and its absence — which is why PS-1 survived four months.** (D-20/L-13; reader 2's sharpening adopted: the hoist rationale is one `onBeforeCollapse` null.) **→ X-W7.c/d** (the split IS the cure).
- **PS-39 · INFO — `readonly PaletteColor[]` here vs mutable `PaletteColor[]` in the sibling fed the same array, plus bare `defineProps` vs the 3.5 reactive destructure.** (L-14.) **→ X-W7.c.**
- **PS-40 · INFO — vacuous gate: my re-run of the corpus grep confirms ZERO spec files reference the component, its composables, classes, or aria-labels — the load-bearing explanation for how two BLOCKERs survived the Glass 7 adoption (W44).** (C-12; both readers re-ran it; this seat re-ran it.) **→ X-W7.c** (born-RED reachability gates on the successor are a wave-open obligation).
- **PS-41 · INFO [MISS-adopted, reader 2] — the file's anti-drift device guards the wrong half: `PANEL_LAYOUT` protects the six layout tokens that never drifted, while the copy-pasted `<WatercolorDot>` block HAS drifted (the hover copy carries `@click.stop`, :35; the touch copy does not — verified).** **→ X-W7.c.**

## Killed-claims register (refutations preserved; nothing ruled away silently)

1. **K-1 · D-7 limb 1** ("the PRM guard is bypassed — inline transition beats stylesheet `!important`; the PRM user gets the full 350 ms") — **KILLED, both readers, ratified on this seat's own bytes**: author-origin `!important` beats author-normal inline declarations (Cascade L4 §6.4.1); the operative guard is glass's `*:not([data-allow-motion])` at (0,1,0), which doesn't shorten the height transition — it STRIKES `height` from the property list (a11y-overrides bytes, my read; measured on ten carriers at motion-quarantine.md:57-66). D-7's instrument was `panelStyleAttr` — the author's own string, unreadable by any stylesheet — where `getComputedStyle(...).transitionProperty` was required. The real defect is the opposite-signed PS-4.
2. **K-2 · D-7 limb 3's harm clause** ("reduced-motion users are smooth-scrolled") — **KILLED, both readers**: `scrollIntoView` lives in `onAfterEnter`, reachable only via `done()`, which PRM makes unreachable (PS-4). The un-gated call survives as a code smell on the no-preference path only.
3. **K-3 · D-8's premise sentence** ("the identical 116-character class string appears four times… :14, 44, 51, 58") — **KILLED as written, reader 1, ratified by my grep**: :14 is `p-0.5` + `shrink-0`; only three are byte-identical; the denied variance is precisely what produces D-6's 16 px box. The finding survives; the sentence must not be adopted.
4. **K-4 · L-1's interim cure** ("`glass-floating` as the bespoke panel's surface") — **KILLED, reader 1, ratified**: `glass-floating` computes `position: relative` (L-1's own table) — the substitution restores the paint and leaves the panel in flow at document end; an r2 taking that branch ships a visible panel in the wrong place that could read as "fixed". The cure is adjudicated separately from the defect (ColorSpaceSelector.md addendum item 2 is the standing law). The headline one-Popover cure is unaffected.
5. **K-5 · D-6/C-4's WCAG 2.5.8 normative failure claim** — **KILLED per ruling R-1** (reader 1 over reader 2): the Spacing exception is evaluated against adjacent TARGETS; the pill is inert text (D-6's own table), the dots are not hit-testable, the nearest true target is beyond a 24 px radius — the exception plausibly applies and no hard failure is demonstrated. The design finding survives whole (PS-7), with the cure-order rider.
6. **K-6 · L-2 consequence #4** ("40 × 40 px — below the 44 px tap-target floor, matching the visual audit's smallTapTargets rows") — **KILLED, reader 1, ratified**: an element removed from hit-testing has no tap target to measure (strictly worse, and L-2's own consequence #1 already says so); 44 px is AAA/HIG, not SC 2.5.8's 24; and L-15 in the SAME file proves the audit rows exclude this component entirely.
7. **K-7 · D-6's audit-attribution sentence** ("It is the largest contributor to this component's share of the audit's 60 smallTapTargets rows… /#/browse = 4 per matrix") — **KILLED, THIS SEAT'S OWN EXTENSION, booked by neither reader**: the corpus refutes itself — C-4:242-245 states verbatim that the button "is NOT in the visual audit's smallTapTargets: 60 tally, because no card ever rendered in any capture (C-13)", and D-19/C-13/L-15 triangulate the mechanism. The component's targets are ADDITIVE to the audit, contributors to nothing yet.
8. **K-8 · D-2's "5 surviving sites"** — **KILLED, reader 2, ratified by my grep**: 6 live sites + 1 comment-prose hit; D's own list prints six. Cosmetic; the finding is unaffected.
9. **K-9 · L-7's ":16 is the worst of the six — a floating `Promise<CopyResult>`… a rejection is unhandled"** — **KILLED per ruling R-2** (reader 2 over reader 1's endorsement): `writeClipboard` cannot reject (producer bytes, my read); :16 is exactly as bad as the other five. Parent finding intact (PS-23 → PC-21).
10. **K-10 · L-6's impact apparatus** (17.8×/218.9 KB weights as cost, "58 extra module requests", "46 extra chunk edges", the root-specifier lint ban, "68 subpaths") — **KILLED, reader 2, ratified**: marginal cost at this subtree is zero (the barrel is loaded three other ways); the lint cure would fail 18 of the demo's own 19 barrels (my census); `sideEffects:["*.css"]` tree-shakes; exports = 74. Demoted MINOR (PS-29).
11. **K-11 · L-5's headline** ("the boundary law matches zero files") — **KILLED, reader 2, ratified on my eslint read**: G-DEMO-3b's glob array leads with the live `demo/color-picker/**`; the corrected sentence is "the ban targets a retired alias and two of three gates glob a deleted tree" (PS-16).
12. **K-12 · D-13's wording** ("swallows every click in the panel") — **KILLED per ruling R-6**: teleported DOM's native events bubble through the real tree to `<body>`, never through this root's `@click.stop`. Scoped to the in-flow expanded region (PS-20).
13. **K-13 · D-14's `items-start` limb** ("inert on the primary path") — **DEMOTED per ruling R-7**: conditional, not dead — load-bearing whenever wrap rows differ in height (`sm:` rung, ghost variant, unbounded N per PS-25). The `min-w-0` limb stays confirmed dead.

## Superlatives (provenance preserved; asterisks load-bearing)

1. **The corpus's negative-results discipline survived hostile re-reading twice.** L's self-inoculation (challenge-L-library.md:638-641: pre-refuting its own probe artifact "so nobody re-reports my own console line as a finding", declining the untested Safari claim), C's labelled hypotheses with the failed-measurement evidence printed (:388-397), D's published self-correction of a false 2.38:1 contrast parse (:601). Both readers planted no false positives from any of them. The epistemic standard the programme should be held to.
2. **D-20's structural sentence** — "Five files own one popover. That distribution is exactly why D-1 could survive four months: no single file's review surface contains both the class name and its absence." Verified distribution (PS-38); the finding a cure should be designed against, and X.W7.c/d's shape already is.
3. **The three axes' converged cure is buildable today with zero producer changes** (reader 2's find, verified by this seat): glass Popover ships `trigger: "click" | "hover"` + delays; the cure deletes 84 lines of composable plus the Teleport branch. Three seats independently prescribing a cure the producer already ships is rare and correct.
4. **`safeFirstColor` is contrast-certified BY CONSTRUCTION against the NAMED surface tier** — PaletteCard.vue:228-230: "the card IS the rung-2 WELL (Q4) — ink certifies on THAT tier", `useSafeAccentFn("well")`. Whatever is wrong with the chip's lane and weight (PS-13), its ink is guarded by a named derivation, not luck.
5. **The composite `v-for` key** (:27 `` `${color.css}-${i}` ``) — duplicate hues are legal palette data and the LEAF got the key right where the older sibling (`PaletteColorStrip.vue`, bare index) did not.
6. **`PANEL_LAYOUT`*** (SwatchHoverMenu.vue:64-66) — a shared constant that documents its own threat model. *Asterisk: PS-41 — it guards the six tokens that never drifted and misses the duplicated dot block that did.
7. **The W5-a11y comments are honest engineering prose that made the defects findable** — above all SwatchHoverMenu.vue:38-39, which documents its own a11y trade in the file where the trade is made. *Asterisk: the trade is void (PS-2/PS-3) — but a codebase whose defects are discoverable from comments the author wrote against themselves is an auditable codebase.
8. **The card barrel's seam discipline** — `browser/card/index.ts` exports six public faces by NAME with the bundler rationale inline, and correctly EXCLUDES this private component; nothing outside the folder imports into it (L-5's honest negative, preserved through the R-5 rescope).
9. **`verbatimModuleSyntax` hygiene is clean in a genuine 96-line leaf** — `import type { PaletteColor }` beside correct value imports; 8 props / 8 emits / zero state; the no-god-modules edict met even as the seam PLACEMENT is rightly indicted (PS-38).
10. **The repo documented the exact trap the file walked into** — focus-ring.css:1-16 is a postmortem naming both deaths of U-F25, including verbatim the empty `--ring`/`--color-ring` token PS-9 re-convicts. The analysis was already on file; only the migration never reached these four lines.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **V-1** — PS-1's rendered geometry (x=−720/w=1440 and x=−756/w=1512). ANALYTICALLY corroborated (both satisfy x = −w/2 exactly, from independently written probes at two viewports — the strongest evidence in the corpus) ≡ PaletteCard.md U-2; re-witness only in the successor's before-frames.
- **V-2** — the expansion geometry family: card-height deltas (+63.94 N=0 / +111.11 N=5 / +181.52 N=24), D-5's `panel {w:458, h:24, children:0}`, N=24's 168 nodes / 24 filter graphs.
- **V-3** — every contrast/type numeral: 10.29:1 / 5.78:1 / 5.08:1 / 5.97:1 / 1.53:1 / 1.66:1; 20.35 px / 16.4 px, weights 500/700; and PS-9's painted ring pixel `rgb(28,25,23)` (mechanism source-proven; the browser's resolution of the dead token is not re-measured).
- **V-4** — PS-14's spring amplitude (`--card-press-t` 0→0.9741; 172→164 px) ≡ PaletteCard.md U-7.
- **V-5** — PS-4's DOM witnesses: the accumulating zero-pixel focusable `Copy slug` stops across a collapsed Browse wall under PRM, and the clipped re-wrap after a stale-height expand crosses the `sm:` rung. Mechanism closed on bytes; the photographs need a live PRM session ≡ PaletteCard.md U-1, extended.
- **V-6** — D-19/C-13/L-15: that ZERO of the 60 visual-audit captures rendered this component populated (loopback origin latching `misconfigured` in availability.ts). Triangulated by three seats via three routes; neither reader nor this seat opened the shots. **Standing obligation on the next visual audit: seed populated palettes, and treat this component's target/name tallies as ADDITIVE.**
- **V-7** — PS-28's cost figure (the mechanism is adjudicated; the per-frame price at N=12–24 is not) — sharpens PaletteCard.md U-8.
- **V-8** — the 14-Tab focus-cycle transcript of an expanded card (feeds PS-3's before-frame).
- **V-9** — L-4's claimed `PaletteSlugBar.vue:84` `p-1` near-copy — the only recipe cite no seat (including this one) verified.
- **V-10** — PS-34's live witness: one finger tap on a `(hover: hover)` touchscreen — expected result: nothing mounts, nothing opens.
- **V-11** — glass-ui's `@utility touch-hit-area` (`::before` with `pointer-events:none` — reserves layout without enlarging the hit region; zero demo consumers). NOT a finding against this component (reader 1 correctly withdrew it); recorded as a **BH/BI relay question for the glass-ui seat**: is it intended as a hit-area cure? It cannot currently be one.

## DISSENT — preserved, not ruled away

1. **The union principle (evidence about the fleet).** Two hostile re-readers each surfaced five missed rows; exactly ONE was convergent (PS-4). Eight of nine adopted misses were single-reader finds. A single incredulity pass under-harvests; the M-23 two-reader shape earned its cost on this component, and adjudication must always consume both decks whole.
2. **Citation-inheritance-adjacent event (R-1).** Reader 2 re-confirmed the corpus's WCAG 2.5.8 cell verbatim after reader 1 had published a clause-level refutation of the exception analysis. The verdict class survived (MAJOR design defect); the normative cell did not. A normative citation inherits no weight from repetition — it must be re-derived against the SC's own exception text (the ColorSpaceSelector DISSENT-1 lesson in a new costume).
3. **The wrong-instrument lesson (K-1).** D-7 read `panelStyleAttr` — the style attribute, the author's own string — to conclude the cascade was bypassed. The attribute cannot witness the cascade; `getComputedStyle` was required, and the truth was not merely opposite but WORSE (PS-4). A probe must read the layer its claim is about; generalizes MT-F019 and PaletteCard.md DISSENT-1.
4. **Reader 1's partial endorsement of K-9's target** ("does not even `void`… is correct") shows a reader ratifying an ORDERING claim without checking the producer's failure surface first. Half an endorsement can launder a false hazard as surely as a false confirmation; the producer bytes were one read away.
5. **Probe-parsimony compliance**: this seat ran zero browser tooling; every ruling above is static (source + dist bytes + canon). The residue rows are precisely the claims that law leaves to the visual audit — nothing in them is load-bearing for any disposition.

## VERDICT

**ADJUDICATED — GUILTY AS RESCOPED, THE CORPUS CORRECTED ON THIRTEEN CELLS AND THE READERS ON FOUR.** 41 rows survive (2 BLOCKER · 17 MAJOR · 17 MINOR · 5 INFO): a 96-line leaf whose every verb path is dead on every input class — hover actions seated in a class no stylesheet declares, swatch semantics discarded by the producer contract on both branches, a third device class served by neither oracle, and a PRM disclosure that never completes and strands invisible tab stops — inside a surface PR-26/VC:102 have already sentenced to REMOVAL. 13 claims killed or demoted with written refutations, including two reader-vs-reader rulings (the WCAG 2.5.8 citation struck, the floating-rejection hazard struck) and one kill neither reader booked; 11 rows identity-fold onto PaletteCard.md / GenerateControls.md / ColorSpaceSelector.md banks and are never re-booked; 9 missed rows adopted from the two decks, led by the slug-pill `prefers-contrast` opt-out and the hybrid-device absent panel. Every disposition routes to ratified tranche X — **X-W7.c/d carry the component whole (W7's file table already lists it; the inspector split is the terminal cure for half the roster), X-W7.f the formatting rows, X-W10 the motion/canon/recipe folds** — plus one BH/BI relay question (V-11) and zero NO-WAVE-OWNER rows. Eleven residue rows are the visual audit's input deck, V-6's seeding obligation first. Original challenger records and both re-reader returns remain immutable beside this file per E-1/E-3.
