claude-fable-5

# kf-SharePopover — adjudicated apotheosis (M-23/L-18, fresh-Fable adjudicator)

**Seat**: apotheosis adjudicator · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited — recorded against L-11/L-14.
**Date**: 2026-08-24 · **Substrate of record**: keyframes.js `origin/master` = `81a56990` (CENSUS-2026-08-03.md:8); local HEAD `8281638c` is stale/divergent (`git merge-base --is-ancestor` → NO); worktree ≡ frontier for BOTH target files (`git diff origin/master -- SharePopover.vue useShareState.ts` = EMPTY, this seat's run), so every line anchor below resolves at the frontier. The worktree's stripped glass-ui manifest (`package.json`/`package-lock.json` both ` M` with the row deleted) remains an unexplained local artifact → KF.W0 §B-12.
**Corpus**: three axes (D design 24 defects/2 BLOCKER · L library 15/1 BLOCKER · C consumption 14/0) over `demo/components/instrument/shell/SharePopover.vue` (62 L) + `useShareState.ts` (95 L), challenged by two hostile re-readers (design-and-usage · library-and-code), adjudicated here.
**Method (M-23/L-18)**: both re-reader returns were attacked before adoption. All four reader-vs-reader verdict disagreements (R-1..R-4 below) were re-derived by this seat with its own instruments; every kill of an axis BLOCKER was re-executed, not trusted; every fold anchor into the banked registry was resolved by read (kf-EditorHeader EH-2/3/8/9/11/12/13/14/16 · kf-MbabbMenu MM-2/4/15/25/29/37/43 + K-1/K-8/K-12 + sup-8/12 + SS-13 probe :152 · kf-EditorShell L-14/C-3, RR-2 M2, RR-1 #6, C-22 · kf-CopyButton KF-CB-7/KF-CB-26 · kf-CSSPasteDialog R-9 · census SCH-1).
**Arbiter's own instruments this session (all static + node against the installed dist; zero browser probes — probe parsimony)**: full reads of SharePopover.vue + useShareState.ts (whole) · git probes (`origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"`, lock ×3; status/diff triangulation) · reka-ui 2.9.9 dist: `PopoverTrigger.js` (`type: _ctx.as === "button" ? "button" : void 0`, `as` default `"button"`, `as-child` forwarded into `Primitive`), `PopoverContentImpl.js:140/:148` (`aria-labelledby: triggerId` + `role: "dialog"`), `PopoverRoot.js:21-25` (`modal` default **false**) · glass-ui 7.0.0 installed: brace-match of `dist/glass-ui.css` (ONE `@layer components` block 2548..18827; `.dropdown-menu__content{z-index:var(--z-popover)}` at byte 29179 OUTSIDE it; no `.popover-content` rule; `dist/styles/index.css` imports `../glass-ui.css` UNLAYERED beside the sole `layer(components)` import), `popover-BPBtXakf.js` (base class byte-exact; `keepDockOpen` ×2; **zero** `"button"` occurrences; `modal` forwarded), `btn.css` (`--spring-smooth`, not `--spring-press`), `a11y-overrides.css` verbatim (0.1s + minted property list + `[data-allow-motion]` carve-out + **`@utility touch-hit-area`** — see rider), `scale.css` `--type-caption: clamp(0.75rem, 0.71rem + 0.21vw, 1rem)`, typography `text-mono-caption`/`text-mono-small` verbatim, `field-control.css` (`block-size: var(--field-control-height)`; `font-size: var(--field-control-font)` = `--control-text`), `header-ribbon/styles.css` (`[data-placement="right"]{inset-inline-end:0}` + `__band{flex-direction:row-reverse}` + `:not([data-expanded]) __actions{max-inline-size:0;opacity:0}`) · demo: EditorShell.vue:16-47, MbabbMenu.vue:1-14/:85-97, hashSharing.ts:20-70, animationOptionsStore.ts:76-81 + controlOptionsStore.ts:59-64 (the partial-drop filter, verbatim), clipboard.ts (whole, 8 L), design-idioms.css:73-120, iosTextEntry.ts:10-11, EditorShell.vue:115/:133 (`initIOSPlatformClass`) · repo greps: `gap-1.5` = 9 hits/7 files · `scale-on-hover` = 13 sites/10 files · `transition-all` in demo .vue = SharePopover:7 + TimelineTrack:24/:69 (uniqueness HOLDS) · `.restored` readers = 0 (one prose comment) · `tap-floor` consumers = 0 · `touch-hit-area` consumers = 0 · `header-right` = 1 (the declaration) · `<form` = 0 · `keepDockOpen` in demo = 0 · `EditorHeader` refs = 1 (own barrel).

## Scoreboard

**32 CONFIRMED (6 MAJOR · 18 MINOR · 8 INFO) · 17 KILLED · 7 net-new rows the axes missed (6 adopted from the re-readers + 1 found by this seat) · 10 UNPROVEN-NEEDS-LIVE residue rows → SS-13.**

No blocker survives. Both axis BLOCKERs and the one BLOCKER-graded superlative-inversion die under substrate triangulation, byte-level cascade derivation, and the banked K-8 calibration (BLOCKER on this program = shipping-crash-or-install-failure). The worst surviving truth is behavioural: **the share feature can report "State restored!" while restoring nothing (SP-2), partially (SP-22), or onto the wrong scene (SP-3)** — one cure family, already partly banked.

## Ruled disagreements (the seat's actual work)

| id | reader design-and-usage | reader library-and-code | RULING | Deciding evidence (this seat's own) |
|---|---|---|---|---|
| **R-1 · `type="button"` (D·m-3/C-10)** | CONFIRMED MINOR-latent ("glass Button injects type, only the raw trigger is exposed") | KILLED (reka merges `type="button"` onto the as-child child) | **KILLED — reader 2 wins** | My read of installed reka 2.9.9 `PopoverTrigger.js`: `type: _ctx.as === "button" ? "button" : void 0` with `as` defaulting `"button"` and `as-child: props.asChild` forwarded into `Primitive` — under as-child the Slot merges the attr onto the raw `<button>`, which therefore SHIPS `type="button"`. D's asserted mechanism (glass strips and conditionally re-adds) does not exist in the consumed build: the glass popover chunk contains ZERO `"button"` strings. Moot besides — `<form` = 0 demo-wide. Reader 1's confirmation adopted D's nonexistent mechanism without opening reka's trigger; the outcome is the opposite of the filing. |
| **R-2 · `keepDockOpen` (D·m-7)** | CONFIRMED MINOR | KILLED (no reachable gap at either mount) | **KILLED — reader 2 wins, revival condition recorded** | (a) The EditorShell mount sits in glass `HeaderRibbon` (EditorShell.vue:16), not a `GlassDock` — no dock context resolves; the prop is inert by construction. (b) At MbabbMenu the hold exists one level up: `open = defineModel<boolean>("open")` (:96) mirrors the menu into ChromeDock's hold ("The controlled open model exists only to hold ChromeDock expanded while this menu is open", :2-3, my read), and the banked rulings keep the menu open under the popover (kf-MbabbMenu ruling 3 NON-MODAL; superlative 12, `isLayerExist` document-order). The gap the prop would close is already closed. REVIVES if any repair relocates SharePopover out of the menu into the dock band directly — the relocating wave must pass `keep-dock-open`. |
| **R-3 · hand-rolled geometry severity (D·M-4)** | MAJOR ("partially corrects banked KF-CB-7") | MINOR (banked EH-11) | **MINOR held — the bank governs until the probe overturns it; reader 2's BANK-CORRECTION adopted as a rider** | The facts are unanimous and re-derived here (`.button{min-block-size:var(--button-size)}` vs `h-8`'s `height` → 32×36 fine / 32×54 coarse; `data-control-target` gated on the unpassed `iconOnly`). EH-11 (kf-EditorHeader.md:50) adjudicated this exact site MINOR with reasons that still hold. But reader 2 is RIGHT that two banked cells are factually wrong on the block axis: KF-CB-7's "glass Button 32 px target" and EH-11's "32×32 geometry" phrasing. Both corrected by rider below; SS-13 probe 2 is the decider, and a MAJOR regrade of SP-8 rides its outcome. |
| **R-4 · caps-tracking severity (D·M-3)** | MAJOR | MINOR | **MINOR — double-count guard** | One class token (`text-mono-caption`) produces two independent harms in the same declaration: the 0.1em caps tracking (this row) and the ~12px font-size (SP-4, the sharper harm, which ALL THREE axes missed). Grading both arms MAJOR would book one token twice at top weight. The tracking arm is real, mechanism-identical to banked MM-29 at a different site, and cured by the same one-token edit as SP-4 (`text-mono-small`) — MINOR. The MAJOR in this declaration is SP-4's. |

**Grade rulings inside agreed verdicts**: D·B-1's BLOCKER and C-3/D-M-7's MAJORs die against banked calibration (K-8) and banked house-pattern rulings (EH-11 + KF-CB-7/KF-CB-26) — see the roster and kill register.

## Adjudicated defect roster

Dispositions are ADJUDICATED, not VERIFIED — X·KF specs are forming; routings are agglomeration input (M-25). Line anchors resolve at `origin/master` `81a56990`; re-resolve before any cure (D-19). Wave targets from the census taxonomy (CENSUS-2026-08-03.md:179-206): KF.W0 substrate · KF.W6 glass suffusion (demo) · KF.W9 Safari visual audit; NO-WAVE-OWNER rows feed SS-1/SS-2 spec authoring directly.

### MAJOR

- **SP-1 · MAJOR · `p-2` on PopoverContent is DEAD** — cn()'s three disjoint padding buckets (`px-`/`py-`/`p-`) never collide it with the base's longhands, and Tailwind 4.3.0 emits `.p-2` before `.px-(--overlay-pad-inline)`/`.py-(--overlay-pad-block)` in the same utilities layer — the glass overlay rung (1rem/1.272rem) ships and the class persists in the DOM looking operative. Doubly derived (reader 1: shipped-sheet offsets `.p-2`@208048 < px@208293 < py@208888; reader 2: in-memory compile + executing the installed cn export as an oracle); base class byte-verified by this seat in `popover-BPBtXakf.js`. Provenance D·M-1. NOT banked. **→ KF.W6** (delete `p-2`; if compression is truly wanted, `[--overlay-pad-inline:0.5rem]` rides the primitive's own √φ calc). SP-32 (echo classes) rides this edit.
- **SP-2 · MAJOR · a restore that applies NOTHING still toasts "State restored!"** — `useShareState.ts:69` probes with `decodeStateFromHash` (truthy-only), `:75` applies with the strictly stronger `isValidState` (`typeof state === "object"`, hashSharing.ts:32), `:83-86` toasts unconditionally; `restored` has ZERO readers repo-wide (this seat's grep: 0 outside hashSharing's constructions + one prose comment). `NDI=` → 42 → guard passes → nothing applies → success toast. Provenance L·D-1/EH-2. **FOLD ≡ kf-EditorShell.md:47 (RR-2 M2, MAJOR, NO-WAVE-OWNER) + kf-EditorHeader.md:41 (EH-2)** — not a re-booking; this record adds the SharePopover-side anchors.
- **SP-3 · MAJOR · the sole prop is optional and the live ribbon mount omits it** — `onSceneRestore?:` (:56-58) vs MbabbMenu's REQUIRED declaration (:92) vs `<SharePopover />` bare in EditorShell.vue:20's `#header-right` default, which renders (this seat: `header-right` grep = 1 hit, the declaration; App.vue supplies none). A pasted cross-scene share URL applies state, switches nothing, toasts success. Provenance C-1/L·D-3. **FOLD ≡ kf-EditorShell.md:46 (L-14/C-3, MAJOR — cure law: thread the prop as a getter) + kf-EditorHeader.md:42 (EH-3, whose anti-rename clause names this fold) + kf-MbabbMenu MM-25.**
- **SP-4 · MAJOR · the share-URL field renders at ~12px — below the repo's OWN ruled and unit-tested 16px iOS no-zoom floor — and the DS default it overrides was zoom-safe** — `text-mono-caption` binds `font-size: var(--type-caption)` = `clamp(0.75rem, 0.71rem + 0.21vw, 1rem)` → ~12.2px at 390px (this seat's read of installed scale.css); it WINS over `field-control.css`'s `--field-control-font` (components layer < utilities layer); the displaced default was `--control-text` ≈ 21px on coarse via `--ui-scale`. The repo's ruled remedy exists and is unused here: `clampIOSNoZoomFontSize = Math.max(fontSize, 16)` (iosTextEntry.ts:10-11, consumed by CSSCodeEditor, asserted in test), and EditorShell.vue:133 calls `initIOSPlatformClass()` on the very shell that mounts this popover. Consequence: iOS auto-zoom on focus, un-restored on blur, on a right-anchored ribbon. **NET-NEW (reader 1 MISS-1)** — all three axes stopped one property short in the same declaration; class precedent banked at kf-CSSPasteDialog.md:47 (R-9, MAJOR), cited as precedent, not identity. **→ KF.W6** (cure: `text-mono-small` / size-driven — one edit with SP-7) **+ KF.W9 witness** (the zoom behaviour is hardware-only).
- **SP-5 · MAJOR · the field has no programmatic label and none of the input semantics the primitive exposes** — only `v-model`/`placeholder`/`class`/`@keydown.enter` pass (:16-21, this seat's read); no `id`, no label, no `inputmode`/`enterkeyhint`/`autocomplete`; glass exports `./labeled-field` first-class. C-8's pre-conceded falsifier is HONOURED: `type="url"` is genuinely wrong (the field deliberately accepts a bare base64 param, useShareState.ts:59-62); `inputmode`, `enterkeyhint="go"` (the `@keydown.enter` at :20 IS the submit) and `autocomplete="off"` have no such downside. Provenance D·M-5/C-8. **→ KF.W6.**
- **SP-6 · MAJOR · zero state coverage** — empty input silently returns (:44-45, no feedback; `Button.disabled` shipped unused); `shareState` is async with no re-entry guard (`Button.loading` shipped + documented unused); both error paths toast elsewhere while the field keeps painting valid (`field-control` ships `:user-invalid`/`data-state="invalid"` skin unused); and `loadHashInput` is never reset anywhere (:16 the only initialiser — this seat read both files whole), so the placeholder carrying the only instruction is permanently hidden after first use. Stale arm ≡ L·D-8/C-12, folded INTO this row. Provenance D·M-6. **→ KF.W6.**

### MINOR

- **SP-7 · MINOR (ruled, R-4) · `normal-case` cancels the case but not the 0.1em caps tracking** on a long, case-significant, pasted string — installed `text-mono-caption` verbatim carries `letter-spacing: var(--type-tracking-caps)`; `.text-mono-caption`@215120 < `.normal-case`@222740 in the built sheet (reader 1), so case dies and tracking survives. Mechanism ≡ banked MM-29 (kf-MbabbMenu.md:80) at a different site — new-site booking citing the mechanism, not a re-book. **→ KF.W6** (`text-mono-small`, cn-merges cleanly; same edit as SP-4).
- **SP-8 · MINOR (ruled, R-3) · `h-8 w-8 p-0` hand-rolls `iconOnly`, forfeits the coarse floor, and never yields the 32px it names** — `min-block-size: var(--control-h-sm)` is a different property from `height`, so the buttons are 32×36 fine / 32×54 coarse; `data-control-target` (the coarse floor's SOLE hook) is emitted only under the unpassed `iconOnly`. Provenance D·M-4/L·D-10/C-4. **FOLD ≡ EH-11 → KF.W6**, carrying BANK-CORRECTION rider 1 and the D wrong-version rider (installed radius is `var(--radius-pill)`, not `calc(var(--button-size)/2)`). Regrade rides SS-13 probe 2.
- **SP-9 · MINOR · the Input's `h-8` defeats the coarse floor OUTRIGHT — the row's largest touch deficit (60px → 32px)** — `.field-control[data-kind="input"]{block-size:var(--field-control-height)}` (this seat's read) is THE SAME property as `height`, so there is no `max()` rescue of the kind the buttons get; coarse `--control-h-md` = max(60px, 44px) = 60 collapses to 32 — a 28px deficit on the one control the user must hit to paste. **NET-NEW (reader 1 MISS-4)**: D printed the number in its own table and drew no consequence; C framed it as `--ui-scale` desync only. Same cure family as SP-8 (drop the height override, let `size` drive), distinct mechanism, distinct consequence. **→ KF.W6.**
- **SP-10 · MINOR · `transition-all duration-fast` overwrites `scale-on-hover`'s spring shorthand with a bezier on `all` at 0.2s** — settled statically (built-sheet order `.scale-on-hover`@194614 < `.transition-all` < `.duration-fast`, discharging C-5's UNPROVEN marker); uniqueness re-verified by this seat (13 sites/10 files; only SharePopover.vue:7 carries both on ONE element — TimelineTrack's pair sits on different nodes). Provenance D·M-2/C-5. **FOLD ≡ EH-9 → KF.W6 with a MUST-CARRY cure rider (rider 2)** — the axes' prescribed bare deletion ships a snapping `hover:opacity-50`.
- **SP-11 · MINOR (BLOCKER killed) · the trigger is a 24×24 tap target with zero padding** — raw `<button>` `p-0`, sole child `icon-lg` = 24px, none of the three declared 44px floors reachable ( `[data-control-target]` needs Button+iconOnly; `.tap-floor` has ZERO consumers; and see rider 3 for the fourth, also-unused floor this seat found). Passes 2.5.8 exactly, fails the house's own 44px bar by 45%. Provenance D·B-1/C-11; K-8 kills the BLOCKER; C-11's demo-wide-gap framing is the honest one. **FOLD ≡ EH-8 (the 24/32×40/32×36 cluster) + kf-EditorShell RR-1 #6 (the raw-trigger row) → KF.W6** (glass `Button emphasis="quiet" icon-only` swap — collapses SP-11/SP-26/SP-27 in one edit).
- **SP-12 · MINOR (MAJOR grades overruled) · three naming mechanisms in 62 lines; the trigger — the first control met — has the weakest** — `aria-label` only on the trigger, `title` only on the pair, `TooltipProvider` paid for app-wide, the sibling in the same slot doing it fully. HOUSE-PATTERN per the bank: EH-11 demoted this exact site (no competing aria-label = no 2.5.3 mismatch; KF-CB-7/KF-CB-26 canonize `title` as the house interim). Provenance D·M-7/C-3/L·D-15. **→ SS-1/SS-2 house-idiom decision; the SS-13 AT probe (residue row 7) is the hinge.**
- **SP-13 · MINOR · double decode with divergent validity predicates** — the probe/applier gap IS SP-2's vector; one decode, one predicate, one verdict. Provenance L·D-2. **FOLD ≡ EH-13.**
- **SP-14 · MINOR (latent) · `origin + resolved.href` drops the history base** — vue-router 5.1.0 hash `createHref` strips everything before `#` (both readers at the bytes); latent solely because the deploy-of-record is an apex domain, while `vite.config.ts:250 base: "./"` states the contradicting intent. Cure: `new URL(resolved.href, location.href)` — NEVER replacing the `router.resolve` seam (superlative 1's lock). Provenance L·D-5/C-7 (C's K-1 records the inflated version as its best pre-killed false positive). **NO-WAVE-OWNER** (behaviour cluster).
- **SP-15 · MINOR · `route.name as string` ×2 launders `string | symbol | undefined`** — invariant holds at runtime (all records named `s.id`; the nameless catch-all only redirects); the first cast buys nothing (`getAllState` accepts undefined). Provenance L·D-6. **FOLD ≡ EH-14.**
- **SP-16 · MINOR (latent) · the callback prop is snapshotted once at setup** — `useShareState(props.onSceneRestore)` closes over the value; reactivity defeated; live bindings are stable consts so no misbehaviour today. Provenance L·D-7/C-9/D·i-4. **FOLD ≡ EH-12 + the kf-EditorShell L-14 cure law (thread as getter).**
- **SP-17 · MINOR (latent) · floating `router.replace` promise in a bare catch that swallows its diagnostic** — un-awaited, un-caught, the clipboard error unbound, and the toast asserts "URL updated" before the navigation settles (useShareState.ts:33-40, this seat's read). Provenance L·D-9. **NO-WAVE-OWNER** (behaviour cluster).
- **SP-18 · MINOR · root barrel and subpath in one eight-line import block** — 4 symbols from `.` (64-chunk closure reaching `@mkbabb/value.js/css`) beside `Input` from `./forms`, when `./popover` (6 chunks) and `./button` (11) are published; graph width, NOT crash exposure — C-6's refusal to inflate is adopted verbatim (≡ banked MM-37 exposure-not-reachability). Provenance L·D-11/C-6. **→ KF.W6** (subpath rewrite).
- **SP-19 · MINOR · no `isInsideToaster` guard on the one surface in the pair where a toast and an open popover provably coexist** — both `loadFromInput` error paths toast AND leave the popover open; both sibling overlays install the demo's own guard; `PopoverContent` emits the hooks unbound. Provenance D·m-5, adopted by both readers. **→ KF.W6.**
- **SP-20 · MINOR · the field is not focused on open** — `openAutoFocus` exists precisely for this; nothing binds it; the placeholder is an imperative. Provenance D·m-6. **→ KF.W6.**
- **SP-21 · MINOR · `align="start"` is wrong at BOTH live mounts** — the ribbon host is `placement="right"` → `inset-inline-end: 0` AND `__band{flex-direction: row-reverse}` (this seat's read of the installed sheet), making the FIRST `#items` child render RIGHTMOST with ~22px of room for a start-aligned 288px surface → structurally unreachable alignment; the collision middleware, not the author, decides placement. At MbabbMenu the surface overhangs a 272px `align="end"` panel. Correct value `align="end"` — which the demo uses at MbabbMenu.vue:6 and the bank praises for exactly this geometry (kf-MbabbMenu sup-8). **NET-NEW (reader 1 MISS-2)** — the only axis contact was a superlative clause praising the value (killed, K-15). **→ KF.W6 + SS-13** (residue row 3).
- **SP-22 · MINOR · a THIRD false-success mode: partial application reports total success** — both store appliers FILTER out any bucket whose value is not an object (`animationOptionsStore.ts:76-81` + `controlOptionsStore.ts:59-64`, byte-identical, verbatim re-read by this seat) and `restoreStateFromParam` returns `{restored: true}` with no knowledge of what was dropped; a partially-corrupt payload applies partially and toasts unqualified success. Distinct from SP-2 (the nothing-applied path); same cure family (make `restored` count, not boolean). **NET-NEW (reader 2 miss #1)** — all three axes cited these exact lines and none read the predicate. **NO-WAVE-OWNER.**
- **SP-23 · MINOR · one of the feature's four exits emits its user-facing copy from a shared utility** — "Link copied to clipboard!" is passed OUT of the feature (useShareState.ts:31) and rendered by `clipboard.ts:6` (this seat read the util whole: 8 L, hard-coupled to vue-sonner for every caller), while the other three toasts are local. Split error posture; cheap cure (return a boolean, toast at the call site). **NET-NEW (reader 2 miss #2)** — every axis read clipboard.ts and none named the split; tempers axis-D S-4. **NO-WAVE-OWNER.**
- **SP-24 · MINOR · the surface's hierarchy is inverted against its own name** — trigger and row both name SHARE first; the share action is the last 32px of the row while the load affordance takes ~70%; both Buttons byte-identical `size="sm" emphasis="quiet"` with `emphasis="primary"` shipped unused. Includes the m-11 accuracy residue (the dialog IS named "Share animation" — see K-7 — and that name under-describes a load-dominant surface). Provenance D·m-10/m-11. **→ SS-1/SS-2.**

### INFO

- **SP-25 · INFO · non-modal popover holding the shell's only text input — Tab walks out of an open `role="dialog"`** — glass forwards `modal` (default false, this seat's read of reka `PopoverRoot.js:21-25`) → `PopoverContentNonModal`, no trap, no scroll lock. House-posture question (reka's own default), not a defeated-default bug. **NET-NEW (reader 2 miss #3). → SS-1/SS-2 posture + SS-13 (residue row 9).**
- **SP-26 · INFO · the `opacity-100` branch is dead** (:8 — the true branch changes nothing). Provenance D·m-2. Rides the SP-11 Button swap.
- **SP-27 · INFO · the demo's declared SINGLE keyboard-focus affordance is skipped on the trigger** — `.focus-ring` absent; UA outline survives (no `outline-none`), which is exactly why this is INFO, not a 2.4.7 failure (C-10's reasoning adopted). Rides the SP-11 Button swap.
- **SP-28 · INFO · `icon-md`/`icon-lg` are invisible to glass's `[class*="size-"]` glyph-sizing escape hatch** — correct today only by layer order (components@21885 < utilities@182187), not by the designed handshake. Provenance D·i-2. **→ glass-ui BH relay** (rider 3 bundle).
- **SP-29 · INFO · `ArrowRight` has no RTL mirror** — latent; the demo declares no RTL at all. Provenance D·i-3 (the `align` clause of that row is KILLED, K-15). Recorded for a future RTL wave.
- **SP-30 · INFO · copy register** — `"..."` where the house uses real typography, two exclamatory strings, and a toast description restating its own title. Provenance D·m-9 (adopted by neither reader; this seat verified from source and books it at floor weight). **→ SS-1/SS-2.**
- **SP-31 · INFO · the third call site is a corpse** — EditorHeader.vue is barrel-only (this seat: 1 ref repo-wide), so the mount arithmetic is 2 live of 3. **FOLD ≡ kf-EditorHeader.md:117 (DELETE-ADJUDICATED) + kf-EditorShell.md:95** with the correction BOTH readers under-cited: rolldown already shakes it from every built chunk — the delete is source-hygiene; the orphan `--header-items-max-w` survives separately.
- **SP-32 · INFO · `z-popover w-72` restate the primitive's own defaults** — cn collides and keeps byte-identical copies; two echoes flanking SP-1's one dead token make :14 read as three deliberate overrides. Provenance D·i-1. Rides SP-1.

## Killed-claims register

1. **L·D-12 BLOCKER + C's §5 gate + D's §7 "F-1 must land first" — "glass-ui is in neither manifest; `npm ci` cannot build this file" — SUBSTRATE-DEAD.** This seat's own git probes: `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep, lock ×3 incl. the registry tarball row; HEAD declares+locks 6.0.0; the absence exists ONLY in the uncommitted dirty worktree. Already ruled at census SCH-1 ("FALSIFIED at HEAD … the cure is DOCS §B-12, not a dependency wave") and re-offended at kf-MbabbMenu K-1 — this corpus makes it at least the SIXTH firing of the class. Every fix-ordering statement gated on it, in all three axes, is void as written. → the artifact itself is KF.W0 §B-12's.
2. **D·B-2(c) + L·D-4 — "the popover paints beneath its host (130 < 140), ordering PROVEN" — REFUTED at the bytes.** This seat's brace-match of the INSTALLED sheet: ONE `@layer components` block (2548..18827); `.dropdown-menu__content{z-index:var(--z-popover)}` at byte 29179 sits OUTSIDE it, imported UNLAYERED by `dist/styles/index.css` — so it outranks the layered `z-modal` utility and BOTH portalled siblings resolve to 130; DOM order decides and the later-opened popover paints on top. ≡ banked MM-15/MM-4. The ordering FACT the axes claim proven is false, not merely its visual consequence. Residue: SS-13 probe (row 5) — if `z-index` reads 140 live, this kill and MM-15/MM-4 die together.
3. **D·B-2 arm (b) — "focus moves read as focusOutside; the menu unmounts; the popover never paints" — BANKED-KILLED.** kf-MbabbMenu superlative 12 (`isLayerExist` document-order) + ruling 3 (NON-MODAL by Boolean-cast; no trapped FocusScope to escape). The "exhaustive disjunction, both arms bad" collapses with arm (c) dead.
4. **D·B-2 BLOCKER grade — GRADE KILL + FULL FOLD.** Arm (a) interactive-descendant-of-menuitem ≡ banked MM-2; arm (b) label-is-not-the-control ≡ MM-3 (actuator measured at the same 24×24 button); K-8 fixes BLOCKER at shipping-crash-or-install-failure and the pointer path works. The residual design input — a trigger slot would make the nesting unnecessary — is fair SS-1/SS-2 spec input, not blocker-weight, and not a first booking.
5. **D·S-1 — the PRM superlative — REFUTED, wrong-version, and the installed rule does the OPPOSITE.** Installed `a11y-overrides.css` read verbatim by this seat: `transition-duration: 0.1s !important` with a MINTED property list (`opacity, color, background-color, border-color, box-shadow`) and a `[data-allow-motion]` carve-out the axis says does not exist. The quoted `0s`/"no-resurrection" doctrine lives in the 8.0.0-dev producer tree, which names 7.0.0's behaviour as the vector it removes. The CONCLUSION (hover scale doesn't transition under PRM) survives by the opposite mechanism — `scale` is absent from the minted list. ≡ banked MM-43/ruling 10 + EH-16 (GLASS-OWNED, BH relay standing).
6. **D §0 producer provenance, corpus-wide — WRONG-VERSION SOURCE.** D read `/Users/mkbabb/Programming/glass-ui/src` at 8.0.0 (43-file dirty) while the demo consumes installed 7.0.0 `dist/` only. Concretely wrong at this component: `--spring-press` (installed: `--spring-smooth`, this seat's byte-check), the `ACKNOWLEDGE_WINDOW_MS` "signed strike" (an 8.0.0 `transition-control` SURFACE-ONLY rationale, misattributed to a transform), the `calc(var(--button-size)/2)` radius (installed: `var(--radius-pill)`), and S-1's PRM text. ≡ banked K-12/ruling 9. No SharePopover finding collapses — every load-bearing mechanism reproduces in the installed dist (both readers re-derived; this seat spot-checked) — but four evidentiary cells and one superlative are void. C is clean (read `dist/` throughout).
7. **C-13 — "unnamed `role=dialog`" — REFUTED statically where the axis marked UNPROVEN.** This seat's read of installed reka `PopoverContentImpl.js:140/:148`: `aria-labelledby: triggerId` beside `role: "dialog"`; the trigger carries `aria-label="Share animation"`. The dialog IS named; C's own falsifier fires; D·m-11 filed the correct residual (→ SP-24). The DERIVATIVE survives: glass's `ariaLabel` prop genuinely never reaches the click+portal dialog branch — a real producer API gap → BH relay (rider 3), with the stated consequence dead.
8. **D·m-3 + C-10's `type="button"` half — KILLED (ruling R-1).** The raw trigger ships `type="button"` via reka's as-child merge; glass 7.0.0 has no strip-and-re-add (zero `"button"` strings in the chunk); moot besides (`<form` = 0). Reader 1's contrary confirmation overruled on this seat's own read.
9. **D·m-7 `keepDockOpen` — KILLED (ruling R-2).** Inert at the ribbon (no dock ancestor); already-held at the menu (the `open` defineModel dock-hold + banked non-dismissal rulings). Revival condition recorded in R-2.
10. **D·m-8 — "`gap-1.5` is a singleton" — KILLED BY ITS OWN DECLARED FALSIFIER.** The axis wrote the grep and did not run it; this seat did: 9 hits across 7 files, four in overlay/panel contexts. 6px is a house rung.
11. **D·M-2's site census ("the only one of EIGHT sites") — COUNT KILL.** 13 sites / 10 files (this seat's grep; C had it right). The uniqueness itself HOLDS — `transition-all` appears in demo .vue only at SharePopover:7 and TimelineTrack:24/:69, and TimelineTrack's pair sits on different elements.
12. **D §3/§7.2 EditorShell anchors + the "32×32" sibling cell — WRONG AT EVERY BASELINE.** The Tooltip/Button block is :30-43 (this seat's read; a nine-line F.W15.S3 comment occupies :20-28); at stale HEAD the code is glass-6 (`IconTooltip`/`variant="ghost"`) and D's quoted spelling does not exist there at all. And by D's OWN M-4 mechanism the un-`size`d icon-only sibling is 32×40 (`--control-h-md`), not 32×32 — ≡ banked EH-8. The outlier table's baseline is broken; the outlier CONCLUSION survives via EH-8's corrected numbers.
13. **L §Substrate — "HEAD `8281638c` … no substrate drift" — FALSE DECLARATION.** L quotes worktree-only bytes (`emphasis="quiet"`) while naming a HEAD that carries `variant="ghost"`; origin/master is 41 commits ahead. Harmless for this component (worktree ≡ frontier for both files, this seat's empty diff) — but it is the same unnamed-baseline failure that produced the axis's own BLOCKER, in the same document.
14. **D·m-1's rest arm — "rest is 16.83:1" — HALF-KILL.** The arithmetic is right and thrice-derived (16.825/9.009/3.322, ≡ EH D-14), but the live host is HeaderRibbon, whose installed sheet collapses `__actions` to `max-inline-size: 0; opacity: 0` at rest (this seat's read) — at rest the trigger is zero-width and fully transparent, not 16.83:1. The hover arm stands and is residue row 1. Rest-state lockout ≡ banked KF-APP-5 / EH F3.
15. **D·S-3's base-path sub-claim + D·i-3's `align` clause — SUPERLATIVES POINTED AT DEFECTS.** `router.resolve` does NOT survive a base path — hash history derives base at runtime and `createHref` strips it whole; that is precisely SP-14, filed by the other two axes. And `align="start"` is not "correctly logical … flips for free" — it is the wrong value at both mounts (SP-21). Both parent superlatives survive only as tempered (superlative 1).
16. **C·C-2's census amendment ("register S-9 candidate") — ALREADY CLOSED, TWICE.** The raw-trigger row is banked (kf-EditorShell RR-1 #6) and the S-9 slot is spoken for by an adjudicated booking (kf-EditorHeader.md:117). Refused per anti-rename.
17. **C §0's "useShareState.ts (91 L)" — 95 L** (this seat's read; L and the banked instrument list agree). Trivial; recorded because C's superlatives rest on "read whole".

## Superlatives (provenance preserved; all re-verified or tempered by this seat)

1. **THE COMPONENT — `router.resolve()` with the reason stated inline** (L S-2, D S-3-as-tempered): the seam that makes the round-trip hold; the SP-14 repair must wrap its output in `new URL(href, location.href)`, never replace it. The base-path sub-claim is killed (K-15); the practice stands.
2. **THE COMPONENT — the hash round-trip is correct in three non-obvious, un-tested ways** (L S-1; both legs of the `+`→`%2B` encode/decode composition re-derived by reader 2 at the vue-router bytes): history-mode agreement, `+` survival, `=` padding survival. The unguarded-legs residual is the right thing to book.
3. **THE COMPONENT — the controlled-open contract is complete AND deliberately asymmetric** (C S★-2, L D-8's contrast): closed on all three completed exits, deliberately NOT closed on the two validation failures, leaving the bad input visible. The distinction most implementations get wrong — and the reason SP-6's error gap is a gap rather than a compounding bug.
4. **THE COMPONENT — zero teardown surface, Goldilocks split** (L S-3/S-6, C S★-1; both readers re-grepped every listener/timer/observer class): two refs, two closures, nothing to leak; the SFC holds zero logic; colocated without a manufactured directory. Why nearly every behavioural cure above lands without touching the SFC.
5. **THE COMPONENT — clean on the flat-namespace hazard** (reader 1's own credit, against D·i-1's mis-framing): no `<style>`, zero custom properties, zero runtime CSS — against lane-frontend §6.3's 98 unprefixed demo properties, this file contributes none.
6. **THE COMPONENT — the semantic z-rung honoured against a written prohibition** (L S-4, D S-5), tempered by K-2: the spelling is exact and the rung is inert against its host — both surfaces at 130, the same shape as banked MM-4's irony.
7. **THE COMPONENT — two error messages that discriminate two real failure modes** (L S-2 adjunct), with the honest caveat: a schemeless paste lands on the less accurate of the two.
8. **THE COMPONENT — `@keydown.enter` survives glass Input's `inheritAttrs: false`** (C S★-4, verified in the vendor chunk, not assumed): `forwardedAttrs` strips exactly one key and it is not a listener.
9. **THE CORPUS — C's §4 falsified-before-filing table and L's §4 killed-hypotheses table.** C's K-1 ("share links broken in production" — killed against the apex deploy-of-record, downgraded to latent) is the model entry and this seat re-verified the kill; C-6's explicit refusal to inflate graph width into crash exposure (≡ MM-37) and D·m-11's "worth recording because the obvious stronger claim is false" are the postures this program exists to buy. Tempering: reader 1 confirmed one killed-wrong row (R-1) by inheriting D's mechanism — even a hostile re-reader's confirmation is only as good as the vendor file it did not open.
10. **THE CORPUS — every axis marked its livable-only magnitudes UNPROVEN-NEEDS-LIVE rather than guessing.** Two were discharged statically by the readers (C-5's which-side-wins; C-13's accname — the latter fatally for the axis's own claim), which is the marker working as designed.

## Riders and bank corrections

- **RIDER 1 (BANK-CORRECTION, adopted from reader 2, ruled R-3)**: kf-CopyButton.md KF-CB-7's "glass Button 32 px target" cell and kf-EditorHeader.md EH-11's "32×32 geometry" phrasing are wrong on the block axis — `min-block-size: var(--control-h-sm)` is unbeatable by `h-8`'s `height`; true boxes 32×36 fine / 32×54 coarse. Any wave consuming those rows re-derives against this record; SS-13 probe 2 decides; SP-8 regrades MAJOR if the coarse 54px box is confirmed.
- **RIDER 2 (MUST-CARRY on banked EH-9, adopted from reader 1 MISS-3)**: the cure for SP-10 is `transition-[opacity] duration-fast` or the Button swap — NEVER the bare deletion both axes prescribe. `scale-on-hover`'s shorthand resets `transition-property` to `scale` alone, so deletion ships a zero-frame `hover:opacity-50` snap; the held-up sibling (DarkModeToggle) gets its opacity fade from its own component sheet, which the raw button lacks. On the opacity leg, `duration-fast` is DS-conformant to the token.
- **RIDER 3 (GLASS-OWNED bundle → the standing glass-ui BH relay, owner edict)**: (a) `PopoverContent.ariaLabel` never reaches the click+portal dialog branch (K-7's surviving derivative); (b) the glyph-sizing probe `[class*="size-"]` cannot see the demo's `icon-md`/`icon-lg` family (SP-28); (c) **this seat's own find, no axis or reader named it**: installed 7.0.0 ships `@utility touch-hit-area` — the DS's coarse-pointer 44px floor for NON-Button elements, exactly what SP-11's raw trigger needed — with ZERO demo consumers AND an expansion pseudo carrying `pointer-events: none`, which by CSS hit-testing semantics excludes the pseudo's box from event targeting, i.e. the utility appears self-neutralising for its stated purpose. UNPROVEN-NEEDS-LIVE (residue row 10) before the relay asserts it as a producer defect; (d) the PRM inversion remains GLASS-OWNED per MM-43/EH-16 — no local patch.
- **IDENTITY GUARDS (anti-rename)**: SP-2/SP-3/SP-8/SP-10/SP-11/SP-12/SP-13/SP-15/SP-16/SP-31 are FOLDS into banked rows — original ids for life, never re-booked; EH-3's explicit anti-rename clause is honoured; C-2's S-9 amendment refused (K-16). SP-4 cites kf-CSSPasteDialog R-9 as class precedent, not identity.
- **SUBSTRATE**: every anchor in this record resolves at `origin/master` `81a56990`; the worktree's stripped manifest is KF.W0 §B-12's artifact; no cure in any consuming wave may be authored against local HEAD.

## UNPROVEN-NEEDS-LIVE residue (SS-13 visual-audit inputs)

1. Hovered trigger-glyph contrast over the REAL painted backdrop (HeroAurora/grid beneath the ribbon), both themes — flat figures final (16.825/9.009/3.322; 0.32 headroom over 1.4.11's 3:1). Sample a mid-tone aurora stop.
2. Rendered boxes of the three-control row at both pointer modes — predicted 32/36/36 fine, 32/54/54 coarse. **The decider for RIDER 1 and SP-8's grade**; 32×32 in both modes would vindicate the banked cells and kill the correction.
3. SP-21's overflow: rendered position/shift of the `w-72` surface at the ribbon mount (desktop + phone) — how far `avoidCollisions` drags it from the authored `align="start"`.
4. SP-4's zoom witness on real iOS hardware: auto-zoom at ~12px on focus; non-restoration on blur leaving the right-anchored ribbon off-screen. Source arithmetic final; platform behaviour not synthesizable.
5. Stacking outcome at the MbabbMenu mount: computed `z-index` on `[data-slot="dropdown-menu-content"]` — 130 confirms K-2 (DOM order then decides, popover expected on top); 140 kills K-2 AND banked MM-15/MM-4 together. Rides banked SS-13 probe 1 (kf-MbabbMenu.md:152).
6. Focus restoration after the async programmatic close (`await copyText` → close a task after the click) — trigger, `<body>`, or lost.
7. AT announcement of the two `title`-only Buttons — the hinge for SP-12's house-pattern ruling (EH-11/KF-CB-7/KF-CB-26) against a real screen reader.
8. Rendered popover padding (1rem/1.272rem predicted) — near-certain; the measured sheet predates the glass-7 pin; one computed-style line closes it.
9. SP-25's observable: where Tab lands from the URL field with the surface open.
10. RIDER 3(c): whether `touch-hit-area`'s `pointer-events: none` pseudo actually extends the hit area anywhere (expected: it does not) — the one-probe input the BH relay needs.

## Verdict

**ADJUDICATED — no blocker survives; 32 confirmed (6 MAJOR · 18 MINOR · 8 INFO), 10 of them FOLDS into the banked registry; 17 claims killed including both axis BLOCKERs (substrate-dead; refuted-at-the-bytes), one axis's entire producer-evidence base (wrong-version), and two reader-vs-reader disputes ruled by this seat's own vendor reads; 7 net-new rows the axes missed, headlined by the 12px iOS share field (SP-4) and the partial-apply false success (SP-22); the share feature's one systemic truth — success is reported unconditionally across three distinct failure shapes — is one cure family, already anchored in the bank; glass-consumption cures route to KF.W6, behaviour cures to NO-WAVE-OWNER for SS-1/SS-2, witnesses to KF.W9/SS-13, and the manifest artifact to KF.W0.**

— end of apotheosis. The three challenge files and both re-reader returns remain immutable beside this record per E-1/E-3.

---

## REF-OF-RECORD — KF.W0 · G-0.9 · APPENDED 2026-09-17 (E-3: nothing above is rewritten)

SERVED MODEL: claude-opus-5[1m] · seat **KF.W0.e**

**REF OF RECORD — the one ref this record cures against**: keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`**. Since the §B-12 substrate settle of 2026-09-17 that coordinate **is** the sacred checkout's `HEAD`: ⟨`git rev-parse HEAD`⟩ → `81a56990736ced5b5edde0b84c527680ac7689b1` · ⟨`git rev-list --count HEAD..origin/master`⟩ → `0` · ⟨`… origin/master..HEAD`⟩ → `0`. The local pin **`8281638c` is DISQUALIFIED**; the pre-settle working tree survives only as snapshot commit **`6d280ee7bec7793846b2e2e1d250e1ea0a21859a`** and is never an anchor.

**PER-FILE SUBSTRATE OFFSET (C-21 — this pass is per-FILE, never per-repo).** Subject: `demo/components/instrument/shell/SharePopover.vue`. ⟨`git diff --numstat 8281638c 81a56990 -- <subject>`⟩ → **SharePopover.vue=+2/-2**.

**Reading**: the subject file **MOVED** between the disqualified pin and the ref of record, so every line anchor in this record must be **re-resolved at `81a56990` before any cure** (the `D-19` law). The offset is stated above rather than a repo-wide verdict, because KF-TD-2 proves the direction is per-FILE.

**`D-19` — the re-anchor / re-resolve-before-cure law — IS CARRIED BY THIS RECORD IN ITS RE-ANCHOR SENSE**, and this stamp discharges that routing word. Census at the corpus bytes, predicate stated so it reproduces (*a `D-19`-bearing line that also matches `re-anchor|re-resolve|Re-anchor|Re-resolve`*): **34 records / 39 lines** in the re-anchor sense, **42 records / 94 lines** by bare token. NO RE-BOOK: the content is carried whole at KF-W0 §Carry C-2, C-21 and G-0.9.

**C-19 LEDGER MEMBER — dirty-manifest / registry-sweep law, firing enumerated and DATED.** The ledger's corrected denominator is **30 members + 2 anti-firings enumerated separately** (the two anti-firings, kf-CubeAxisLines and kf-AnimatedText, are events where the law did NOT fire and are never swept as firings). Re-derived at the corpus bytes this seat: the C-19 cell names **32 distinct records**, minus those 2 ⇒ **30**. This record's disposition: **the firing is a DATED RECORD, not a live defect** — its cause (a manifest/tree claim filed without naming its baseline) is extinguished by this stamp, which names the baseline for every row in the file.

**§6's WRONG-VERSION-SOURCE row is RE-DERIVED AGAINST THE CONSUMED ARTIFACT, and it is corrected in ONE of its four legs** (C-13's law, G-0.10). Measured at `node_modules/@mkbabb/glass-ui/dist` (7.0.0, 225 files): `--spring-press` → **2 files** (`styles/theme/bridges.css`, `styles/tokens/scheme-spring.css`) and `--spring-smooth` → **14 files**. **BOTH SHIP.** The row's direction — D read an 8.0.0 source tree while the demo consumes the 7.0.0 dist — HOLDS; its exclusivity claim (*"installed: `--spring-smooth`"*, implying `--spring-press` is absent) does **not**. E-3: the dated cell is not rewritten; the measurement is appended.

**The other three legs re-derive as banked**: `ACKNOWLEDGE_WINDOW_MS` → **0 occurrences** in the consumed dist (an 8.0.0 surface-only rationale; the citation does not resolve in the artifact under audit, and is STRUCK against it); `--radius-pill` → **29 files** (present, so the `calc(var(--button-size)/2)` claim is the 8.0.0 spelling); S-1's PRM text unchanged.

**The stripped worktree manifest is EXPLAINED and DISCHARGED**: it was a checkout-lag artefact of the 41-behind tree, and the settle removed it. The devDep at the ref of record is `"@mkbabb/glass-ui": "7.0.0"`, exact — ruled at `MANIFEST-RULING.md`.

**Scope of this addendum**: it states WHICH REF this record cures against and the measured per-file offset that binds its anchors. It books no cure, spends no budget, re-opens no disposition and renames no id. Every correction above is an APPENDED statement beside dated evidence, per E-3.

