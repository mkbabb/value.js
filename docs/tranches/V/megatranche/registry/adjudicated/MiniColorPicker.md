# MiniColorPicker — adjudicated apotheosis **r2** (M-23/L-18, fresh-Fable seat) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (M-23 §1 / L-18) · **Date**: 2026-08-04 · **HEAD**: `9ce6634a` (branch `tranche-u`; every line anchor in this record is this seat's own working-tree read — re-resolve before any cure per D-19).
**SUPERSESSION NOTE (E-1/E-3-conformant)**: an r1 apotheosis for this component exists, committed at `5e146edd` — it adjudicated an **earlier pair of reader returns** whose missed-findings lists differ materially from the r2 returns adjudicated here (r1's reader-B return had no harness finding; r1's reader-A missed-list shares only 2 of 6 rows with r2's). Git preserves r1 immutably; this r2 record adjudicates the r2 returns, **keeps r1's MCP-x ids for every matching identity (anti-rename)**, carries r1's six unique rows forward — each re-verified from live source by this seat's own instruments, never adopted on trust — and records the one severity where r2 ruling departs from r1 (MCP-1, see DISSENT §1).
**Corpus**: `docs/tranches/V/megatranche/audit/components/MiniColorPicker/` — challenge-C-implementation.md (C-1..C-8) · challenge-L-library.md r2 (L-1..L-9 + two self-withdrawals) · challenge-D-design.md r2 GOVERNS over challenge-D-design-pass1-c654824e.md (D-1..D-22 · N-1..N-11) · probes probe*.mjs/probe-D2-1..4/probe-L9-* + shots/ + evidence/. HYDRATION-LEDGER.md:87-89: all three challenge files **EXISTS-ORIGINAL** (verified by this seat) — no Codex-report provenance taint.
**Readers adjudicated**: reader-A (`MiniColorPicker-L18-design-and-usage`) · reader-B (`MiniColorPicker`, library-and-code). Both were attacked before either was adopted; every reader-vs-reader disagreement was re-derived by this seat.
**Subject**: `demo/palettes/browser/search/MiniColorPicker.vue` (164 lines, read whole) + sole consumer `demo/palettes/browser/search/SearchFilterBar.vue` (249 lines, read whole).

**Arbiter's own instruments this session (zero browser tooling per law; all static + repo greps)**: full reads of both SFCs; hand re-derivations of the loop algebra (`#010102` → h=((1−1)/1+4)/6 = **240° exact**; `#4488cc` **fixed point** — decode (210, ⅔, 0.8), re-encode #4488cc; seed HSV(210,.6,.8) = **#528fcc**; `(210,.6,.03)` → #030508 → **216°**, re-verifying r1's K-8; `Math.max(0,Math.min(1,NaN))` = NaN → `"#NaNNaNNaN"`); the **K-6 decider run by this seat** (24px-diameter circle on a 12px rail extends 12−6 = **6px** past each edge; both neighbours sit `mt-2` = **8px** away at :26/:39 → no intersection, centre-to-target 14px > 12px radius); the MCP-7 watcher-order derivation (:107 registers before the `immediate` watcher at :110; sat 0.6→0.6667 during setup; one pre-flush emit `#4488cc` — corroborated by reader-B's jsdom run and r1's node run, both on the repo's Vue); producer bytes read: `Button.vue.d.ts` (**no `variant`**; `ButtonSize` includes `"xs"`; `loading`/`type`/`disabled` real), `slider/types.d.ts:4` (`SliderVariant = "standard" | "spectrum"`), `sizing.css` (`--control-h-xs: 1.75rem·--ui-scale`, `--control-floor: 0px`; φ ladder = `--space-phi-5/6` ONLY), popover dist (`forceMount` grep = **0**; `[--overlay-pad-inline:1rem]` present), `typography/semantic.css` (`text-caption` = `--font-text` + **`font-style: italic`**), `typography/utilities.css` (`@utility fira-code { --font-mono; font-feature-settings: "liga","calt" }`; `text-mono-small` exists) vs `demo/styles/utils.css:9-11` (`.fira-code` family-only shadow); harness census (**reader-B M1 decider**): `vitest.config.ts` has **no `plugins` key** (whole file read; a present vitest.config pre-empts vite.config), `include = ["test/**/*.ts","demo/test/**/*.ts"]`, `demo/test` = exactly 3 files none mounting an SFC, `@vue/test-utils` **zero call sites** in test/demo/e2e (declared `^2.4.10` in package.json:96); zero-coverage grep re-run (`MiniColorPicker|sv-canvas|Open color picker` over test/ demo/test/ e2e/ = **0**); canonical lines: SpectrumCanvas.vue `role="img"`+reactive label (:5-9 with the ruling comment), `@pointercancel`/`@lostpointercapture` (:18-19), touch gate (:11-21), releaseCapture try/catch "Element may be detached", `rect.width === 0 || rect.height === 0` guard, library `clamp`, lost-capture-clears-drag, `onUnmounted` teardown, `overflow: visible`; spectrumLuma.ts:1-35 read whole ("ONE function, ONE threshold … Share the function, never copy the constant"; `SPECTRUM_LUMA_FLIP = 0.5`); stableHue `> 0.01` census = **3 sites** (useColorPipeline.ts:92, useColorParsing.ts:43, useColorParsing.ts:~78 — the third the L axis under-counted); probe sources read as evidence about the probes: probe-D2-4.mjs (**click-only** — every arm `page.mouse.click`, plus a mid-field reset click; no drag anywhere), probe-D2-3.mjs:68-69 (right-end arm `fx = 1.0` → `rail.x + rail.width` = **first coordinate OUTSIDE the box**), probe-D2-3.json rail rows (x=0 actuates `left: 0%`/clipped 50; x=100% leaves `left: 50%` — the click missed), probe-D2-1.json `desktop-light.H1` (`beforeSearchClick: "oklch(0.72 0.31 145)" → afterSearchClick: "#4488cc"`, `clobberedWithZeroPointerContact: true`); law texts verified verbatim: VISUAL-CONSTITUTION.md:17 (Watercolor/data the only ornamental colour-bearing species), :96-99 (select→tune→commit; "every spatial action has a keyboard/numeric equivalent"), :115, :125 ("pointer canvas is not the sole keyboard control"), :154 (LTR-isolated spans); PROPORTION-AUDIT.md PR-06/:50, PR-08/:52, :70 item 5 ("operable ornaments without names are forbidden"); DESIGN.md cartoon-shadow paragraph naming MiniColorPicker as consumer-of-record; motion-quarantine.md §1 (both guards live; **zero** MiniColorPicker rows); routing authorities: `docs/tranches/X/waves/` W0–W11 exist; `SearchFilterBar.vue` owned by name (W4.md:126 modify + :236; W7.md:89 modify + :288 names `SearchFilterBar.vue:52,:53`); **no X wave names MiniColorPicker.vue** (repo grep).
**Evidence-cell disclosure**: interaction/pixel probes in this corpus are playwright-webkit except the forced-colors census (chromium, per r1's engine read of probe-D2-2.mjs); no claim in this r2 record is browser-derived by this seat.

## Scoreboard

**45 rows CONFIRMED (2 BLOCKER · 12 MAJOR · 20 MINOR · 11 INFO) · 15 claims KILLED (13 reader kills sustained on this seat's re-derivation · 1 kill decided between the readers · 1 kill is this seat's own, against reader-A) · 17 rows absent from the original three axes adjudicated in (11 = the r2 readers' missed-union, all confirmed · 6 = r1 carries, all re-verified from source by this seat) · 11 UNPROVEN-NEEDS-LIVE residue items → visual-audit inputs.**

The unanimous terminal cure — **delete MiniColorPicker.vue and rebuild the affordance on the session model + producer primitives** — is ADOPTED by all three axes, both r2 readers, r1, and this seat. Binding cure-caveats: MCP-13 (the consolidation target's pending state is fiction), MCP-44's DESIGN.md edit-carry, and K-11 (the Input does NOT already parse arbitrary CSS — fix SearchFilterBar.vue:218 before or with the deletion, or the ratified tray is fiction too).

**Routing law**: `SearchFilterBar.vue` is owned by name → **X-W4** (a11y-control-targets) and **X-W7** (palette domain; goal line "every mutation has exactly one owner and a visible result"). `MiniColorPicker.vue` is named by **no** X wave → per the ApiOfflineChip precedent its rows ride as **X-W7 rider (palette-tree home, unclaimed by name), else NO-WAVE-OWNER at the formation boundary**. Fleet identities cross-book, never re-book: dead `variant=` → **MT-BTN1** (r1's fleet-51 census, attributed); `demo/ui` barrels → **X-W8**'s own census. Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

## Ruled disagreements (the seat's actual work)

| # | question | reader-A | reader-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| 1 | **C-3/L-6 severity** (keyboard-dead surfaces) | BLOCKER | MAJOR | **MAJOR — B wins.** | The FEATURE keeps a degraded keyboard path: the sibling Input (SearchFilterBar.vue:88-96) takes a typed 6-digit hex and searches. Access impaired, not severed; the impairment's sharpest edges carry their own rows (L-7 caps the path; MCP-6 records A's true sharpening that Tab→Enter in the dialog is the DESTRUCTIVE default). WCAG 2.1.1 A stands as cited. |
| 2 | **C-4/D-7** ("12px rail against the WCAG 2.2 SC 2.5.8 24px floor; the spacing exception cannot rescue it — a 24px circle intersects the SV field 8px above and the output row 8px below") | MAJOR, exception unavailable | MINOR, exception plausibly claimable | **MINOR — B wins; A's arithmetic is KILLED (K-6, this seat's own).** | 24px **diameter** → radius 12. Rail `h-3` = 12px → the circle extends 12−6 = **6px** past each rail edge; both neighbours sit `mt-2` = **8px** away (my reads of :26/:39). 6 < 8; centre-to-target = 14px > 12px radius — **no intersection**, the exception plausibly applies. A treated the circle as reaching 12px past the edge. Ergonomic residual + clipping half survive → MCP-14. |
| 3 | **N-4 + L-4 severity** (fill-blind markers / bypassed luma law) | MAJOR | MAJOR (L-4) | **MINOR, ONE identity (MCP-16) — this seat demotes BOTH readers.** | The structural halves are certain (I verified the compositing algebra: premultiplied `#000→transparent` over `#fff→hue` gives exactly HSV(h,s,v); :19 paints the thumb its own field pixel; :34 `hsl(hue,100%,50%)` ≡ the :27 gradient at x=h/360; `border-white` literals :18/:33; spectrumLuma.ts bypassed). But the corpus's own evidence bounds the consequence: L-4's honesty note captured the live plate and found `shadow-cartoon-sm` rescues thumb legibility — the axis REFUSED the consequence, and the pixel-ratio cells are unproven (R-4, and the 1.283:1 cell fails to reconcile with the WCAG formula per r1's arithmetic). Severity follows the proven consequence, which is a law/coherence breach, not measured illegibility. |
| 4 | **L-3 severity** (stableHue law re-invented with the strictly weaker `d === 0`) | (inside the L-1 cluster, MAJOR) | MAJOR | **MINOR — subsumption, this seat.** | The harm flows entirely through MCP-2's echo loop (already BLOCKER); standing alone the row is duplication-with-drift. Canonical sites verified ×3 by this seat — one more than the L axis counted (MCP-35). |
| 5 | **D-8 severity** (inert overrides; `variant="outline"` not a prop) | MAJOR | (mechanism only, MINOR) | **MINOR (MCP-31); the live consequence re-homed to MCP-40.** | Dead classes and a fall-through attribute are inert; the user-visible harm (36px vs 24px Search mismatch) is owned by the xs-rung row. `variant` absence verified in the installed d.ts by this seat; the fleet identity cross-books to MT-BTN1 (r1 census, attributed), never re-booked MAJOR here. B's tailwind-merge mechanism for the surviving `p-2.5` cell adopted. |
| 6 | **C-5's mount-emission-lost sub-claim** ("the mount-time `update:hex` does not reach the parent") | UNPROVEN — "contradicts a straightforward reading of Vue's watcher semantics"; left to live | KILLED — jsdom repro on the repo's Vue: the emit fires and drives `colorText` | **KILLED (K-3) — B wins; A's lean was right but abstention left the roster short.** | My own derivation: :107 registers first (old value = seed `#528fcc`), the `immediate` watcher at :110-125 mutates sat 0.6→0.6667 during setup, `currentHex` → `#4488cc`, exactly one pre-flush emit. B's jsdom run and r1's independent node run agree to the byte. The Vue-semantics half of such claims IS statically decidable; killing the negative births **MCP-7**. The conflicting live cell (`colorText: ""`) survives only as R-1. |
| 7 | **L-1's weight** (hand-rolled colour math vs the shipped library) | kill of "BLOCKER-by-drift-magnitude"; ownership confirmed MAJOR | SEVERITY CORRECTED from BLOCKER → MAJOR; statistic killed as unverifiable | **MAJOR (MCP-1) — both readers sustained; the r1 dissent recorded.** | Every divergence is bounded at ±1 LSB (L's own re-run; B's re-run of the persisted probe: 53,361 samples / 6,024 mismatches / max Δ = 1; mechanism = `Math.round` vs the library's epsilon-relative `roundHalfEven`). B's "the 4.2313% figure is unverifiable" is itself corrected: r1's arbiter REBUILT the sweep and reproduced 39,730/938,961 = 4.2313% exactly (receipt in r1's DISSENT §1, preserved in git) — the statistic is real; the BLOCKER weighting is what dies. r1 held BLOCKER on ownership grounds — see DISSENT §1. |
| 8 | **N-1's residual severity** (corner-click dead zone) | "MINOR" | "MINOR" (residual inside K-1) | **MINOR (MCP-29), both sustained.** | Kill unanimous and re-verified (K-1); the surviving defect is a swallowed discrete click/tap in ~13.7px² per corner of a 174×112 surface whose primary gesture (captured drag, clamped) reaches every extreme. |

## Adjudicated defect roster

Ids continue r1's scheme (anti-rename); MCP-39..47 are new to r2. "X-W7 rider" = palette-tree home, file unclaimed by name, **else NO-WAVE-OWNER at the formation boundary**.

| id | sev | claim (ids) | proof basis | terminal disposition |
|---|---|---|---|---|
| **MCP-2** | **BLOCKER** | The hex wire + the parent's echo destroys hue AND saturation irreversibly on an ordinary drag: unquantised floats (:138-139) → 8-bit round (:103) → emit (:107) → parent echo (SearchFilterBar :175-178, :68) → self-rewrite from the damaged hex (:110-125). Hue latches at fabricated values; sat collapses to 0 on any grey echo because :117-118 write BEFORE the `d === 0` guard at :119 (**L-9 / C-2 / D-6** — one identity, three filings, per B's primacy kill K-15) | Loop re-walked edge-by-edge in live source by this seat; `#010102 → 240°` re-derived exact; `#4488cc` verified a fixed point (corruption settles, never returns — the L axis's own N-2 self-withdrawal mechanism); both watchers default `flush:'pre'` so corruption lands before paint (A). L's table-coordinate cell corrected (K-8). | **X-W7 rider** — dies with the rebuild; the cure carries a colour, not a hex string, and splits the tuning channel from the commit channel (with MCP-7/MCP-8-family rows). |
| **MCP-3** | **BLOCKER** | Interrupted drag sticks forever, then bare hover mutates the colour: no `@pointercancel`, no `@lostpointercapture`, no `onUnmounted`; capture taken (:129/:144) never released; flags are non-reactive setup-scope `let` (:79-80) cleared only by element-bound `@pointerup` (:133/:148); instance root `<Popover>` (:2) outlives PopoverContent, and the producer popover has **no forceMount** (my dist grep) so content truly unmounts mid-press (**C-1 / D-4**; reproduced 4× by two seats, screenshot in evidence/) | All 164 lines read by this seat; canonical cure one directory away, line-verified (SpectrumCanvas: both bindings, try/catch release, lost-capture-clears-drag, unmount teardown). Final live hop = R-3. | **X-W7 rider** — the rebuild inherits SpectrumCanvas's capture discipline, including the ordering/guard cure for MCP-42. |
| MCP-1 | MAJOR | The component reimplements the published library it exists to demonstrate: import list :61-63 = vue + two `../ui` barrels, **zero** value.js / color-session imports; :85-105 hand-rolls HSV→hex, :110-125 hex→HSV; the private quantiser (`Math.round`) disagrees with the library's `roundHalfEven` on a measurable fraction of the lattice, bounded at ±1 LSB (**L-1**, ruled #7; the dogfood keystone vite.config.ts generates self-aliases from `#exports` precisely so this cannot happen by accident) | Import list and both converters read by this seat; B's re-run of the persisted probe (11.29% of 53,361, max Δ=1) + r1's rebuilt 4.2313%/938,961 receipt both stand; severity = ownership/dogfood breach, not rendering. | **X-W7 rider** — delete/rebuild on the session model + producer primitives. |
| MCP-4 | MAJOR | Both spatial axes keyboard-inoperable: bare `<div>`s with only pointer handlers, no role/name/tabindex/keydown in 164 lines; the dialog's sole focusable is the Search button (**C-3 / L-6 / D-3**, ruled #1). WCAG 2.1.1 A; VISUAL-CONSTITUTION.md:99/:125 verified verbatim by this seat | :8-15/:24-31 my read; probe-D2-1 focusables census corroborates; the canonical ruling (`role="img"` + reactive label, deliberately not `role="slider"`) is written and implemented at SpectrumCanvas.vue:5-9. | Picker surfaces die with the rebuild (**X-W7 rider**); bar-side keyboard hardening → **X-W4** (owns SearchFilterBar.vue by name, W4.md:126). |
| MCP-5 | MAJOR | Under forced colors the instrument is inoperable furniture: rail = borderless inline gradient (:26-27, nothing survives `background-image: none`), SV plate = two background-image layers (:159-163), dot = inline background; zero `forced-color-adjust` / `@media (forced-colors)` arms (**N-3**; BLOCKER→MAJOR by both readers — the sibling Input survives as a system-drawn control) | Structural halves source-certain (my full read); pixel census = chromium cell, R-5. The "cannot be patched" cure-claim is killed (K-10). | **X-W7 rider** — producer primitives carry the forced-colors arm. |
| MCP-6 | MAJOR | The picker's own Search commits its private seed over the user's typed CSS colour — zero contact with either colour surface; and per A's sharpening it is also the dialog's DEFAULT KEYBOARD action (open → Tab → Enter), since Search is the only focusable (**N-2 / D-5**) | :51 emits `currentHex` (local refs only); SearchFilterBar :180-187 overwrites BOTH `pickerHex` and `colorText`, no warning, no undo; probe-D2-1 H1 read by this seat (`oklch(0.72 0.31 145)` → `#4488cc`, `clobberedWithZeroPointerContact: true`, duplicated dark). | Parent half → **X-W7** (owned by name); picker half rides the rider. |
| **MCP-7** | MAJOR | **Mount-time self-write clobber (reader-B missed M2 ≡ r1 MCP-7)**: the immediate prop watcher normalises the seed (sat 0.6→0.6667), `currentHex` moves #528fcc→#4488cc, exactly one pre-flush emit fires, and the parent unconditionally writes the picker's value into the user's text field (:175-178) — so the child owns `colorText`, at mount AND on every pointer sample; every Filters-tray open silently seeds/overwrites the typed colour | **Statically derived by this seat** (watcher registration order + pre-flush semantics), corroborated by B's jsdom run and r1's node run on the repo's Vue; the mount is ordinary (no forceMount). D pass-2's negative that "ruled it out" is killed (K-4 — its probe opened the tray, thereby mounting the child into an empty field, BEFORE typing; the clobber window was never exercised). DOM witness = R-1. | **X-W7** (parent handler + wire type) — same identity family as MCP-2: a controlled component that rewrites its controller. |
| MCP-9 | MAJOR | Zero tests of any kind; deleting the component keeps every gate green; the component sits two popovers deep and in zero of the 60 shipped visual captures (**C-8**) | Grep re-run by this seat = 0 hits across test/ demo/test/ e2e/. Every BLOCKER above was one unit test away. | **X-W1 rider** (coverage slate) + behavioural coverage rides the rebuild (**X-W7 rider**). |
| **MCP-39** | MAJOR | **C-8's prescribed cure is not executable at HEAD (reader-B missed M1, NEW)**: the vitest harness cannot mount an SFC at all — `vitest.config.ts` carries **no `plugins` key** (and pre-empts vite.config, so `@vitejs/plugin-vue` never reaches the test transform); `@vue/test-utils@^2.4.10` is a paid-for dependency with **zero call sites**; every `mount()` in the repo lives under megatranche audit dirs on bespoke configs. All three axes filed the absence of tests as a component finding; it is ALSO a repo-level harness finding, and it is why C-8's mutation list is trivially green | **Verified whole by this seat**: vitest.config.ts read (no plugins; include globs), `demo/test` = 3 files none mounting SFCs, test-utils call-site grep = 0, package.json:95-96. | **X-W1 rider** — the harness change is a precondition of every SFC-level oracle any wave writes; NO-WAVE-OWNER if W1 declines scope. |
| MCP-10 | MAJOR | A second SV-plate implementation (the gradient recipe + thumb law copied from the canonical instrument) and a **fourth private HSV store** outside the colour session that owns HSV; the seed is also a third, disagreeing encoding of the parent's default (**L-2 / L-5 / D-1**; the "same default" phrasing killed at K-5 — #528fcc ≠ #4488cc, which is WORSE: boot-state disagreement reconciled by writing into the controller, MCP-7) | :159-163 vs the canonical two-layer recipe; :76-78 vs useColorPipeline's stableHue/HSVCurrentColor injection chain (axes' greps accepted: the palettes tree never injects COLOR_MODEL_KEY). | **X-W7 rider** — the rebuild consumes the session model; useSpectrumPlateStyle is the extracted seam. |
| MCP-11 | MAJOR | The stringly `hex?: string` boundary lies twice: the guard (:111) silently drops every CSS form the sibling Input advertises (`#48c`, named, `oklch(...)`; alpha stripped from `#3b82f680`), while lenient `parseInt` accepts-and-mangles garbage (`#12345g` → b=5/255; `#zzzzzz` → NaN state → emits `#000000`); the parent then masks the `hsl(...)` its own placeholder invites — :218 discards non-6-hex text and substitutes `pickerHex` with no error (**C-5 core / L-7**; B's split preserved: the misparse half is latent today since the parent only feeds the picker its own output) | `#12345g`/`#zzzzzz` walks re-derived by this seat (NaN falls through `d===0`; `switch(NaN%6)` matches nothing); :218 read verbatim; `parseColorIn` already imported at :145 (L-7's :137 anchor corrected by B). Cell corrections carried: C-5's cure path `src/units/color` is stale (live tree: `src/color/`). | **X-W7** — carry a colour, not a hex string; **binding with K-11**: fix :218 in the same change. |
| MCP-12 | MAJOR | The hue rail is hand-rolled (`<div>` + inline six-stop gradient + three raw handlers + absolutely-positioned handle, :24-36) where the producer ships `SliderVariant = "spectrum"` with disabled/invalid/marks/orientation/dir/motion — already consumed by this repo at ComponentSliders (**D-10**; owner edict 4) | `slider/types.d.ts:4` read by this seat. | **X-W7 rider** — rebuild on `<Slider variant="spectrum">`. |
| **MCP-13** | MAJOR | **The pending state two axes lean on is fiction (r1 carry, re-verified)**: `applyColorSearch` (:213-225) is `async` with **zero `await`** — `searching` flips true→false inside one synchronous tick, so the Loader2 spinner (:102), the `:disabled` arm (:98) and the double-fire guard (:214) are all unreachable; D-9's "only one has a pending state" and N-11's "wired to the inline button" both credit an affordance that cannot render | **This seat's own read of :213-225** (emit is synchronous; `finally` runs same-tick; no render between). Cure-critical: consolidation texts route onto this fictional affordance. | **X-W7** (owns the file AND "every mutation has … a visible result"). **Binding cure-caveat**: nothing consolidates onto the inline button's pending arm until the flag actually spans the upstream search. |
| **MCP-40** | MAJOR | **Two controls named Search in one 218px surface, two build systems, and the rung both hand-forge already ships (D-9 + reader-A missed #2, NEW)**: the child puts a dead `h-6` on a producer Button shipping at the 36px `sm` rung (:47-54); the parent abandons Button for a raw `<button class="… h-6 …">` (:97-104) owning the (fictional, MCP-13) spinner — while `ButtonSize` includes `"xs"` and `--control-h-xs` = 1.75rem with `--control-floor: 0px`. The corpus escalated a producer-gap relay for a gap that does not exist | `Button.vue.d.ts` + `sizing.css` read by this seat; both call sites read; 36-vs-24px mismatch = the axes' measurement. | **X-W7** (both sites in the owned file); no BH relay needed — the rung exists. PROPORTION-AUDIT PR-06 governs. |
| MCP-14 | MINOR | 12px rail as sole hue affordance (ergonomics — NOT a flat SC 2.5.8 failure, K-6) + both markers clipped at domain extremes by `overflow-hidden` × centre-translation; probe: 50% of the handle gone at hue 0 (**C-4 clip half + N-7b**) | :10/:26 overflow-hidden, :18/:33 translations, my read; probe-D2-3.json rail[0] read by this seat; canonical sets `overflow: visible` for exactly this. | **X-W7 rider**. |
| MCP-15 | MINOR | The stableHue domain law re-invented strictly weaker: `d === 0` (:119, exact greys only) vs the canonical `s·v > 0.01` (near-achromatic band — exactly where MCP-2 detonates) (**L-3**, ruled #4) | Canonical sites verified ×3 by this seat (see MCP-35); useColorParsing's own comment states the powerless-channel law. | **X-W7 rider** — dies with the private store. |
| MCP-16 | MINOR | Markers fill-blind by construction (interior ≡ the pixel beneath — thumb :19 paints its own field colour; handle :34 ≡ the rail gradient at x=h/360) with scheme-invariant `border-white` literal rings, bypassing spectrumLuma.ts — the one thing its docstring exists to prevent (**N-4 + L-4**, one identity; ruled #3, demoted from both readers' MAJOR) | Compositing algebra verified by this seat; spectrumLuma.ts read whole; the visual consequence stays HYPOTHESIS by the corpus's own honesty note (R-8), the ratio cells suspect (R-4). | **X-W7 rider** — the rebuild reads `spectrumFieldIsLight`. |
| MCP-17 | MINOR | No `touch-action` on either drag surface, no touch gate; scroll-steal consequence correctly HYPOTHESIS, the missing declarations fact (**C-6 / D-12**) | My read (:10/:26, scoped block styles only backgrounds); canonical gate at SpectrumCanvas :11-21. | **X-W7 rider**. |
| MCP-18 | MINOR | Static seven-hex gradient inlined per-instance (:27) eight lines above the file's own comment (:158) assigning exactly that case to CSS (**C-7**) | Both lines in my read. | **X-W7 rider**. |
| MCP-19 | MINOR | Type/register defects, one identity: the most important datum in the surface renders as a muted ITALIC caption — `text-caption` (italic serif role, producer bytes verified) with family overridden back to mono, italic kept (:44), while `text-mono-small` ships in the same producer; local `.fira-code` (utils.css:9-11) shadows the producer utility and drops `"liga","calt"`; `rounded-lg` (:10) is a non-role radius (**D-13 + D-18**) | semantic.css + utilities.css bytes read by this seat; utils.css read. | Sites die with the rebuild (**X-W7 rider**); the class law → **X-W10** (type canon). |
| MCP-20 | MINOR | The dialog's accessible name is the imperative that opened it AND a live function of the value being edited (trigger `:aria-label` interpolates `pickerHex`, rewritten per pointer sample; reka labels the content from the trigger) (**N-5**; A's own MAJOR→MINOR downgrade sustained — the churn half is asserted, names compute on entry) | SearchFilterBar :78 + :175-178 + MiniColorPicker :107, my reads; reka `aria-labelledby`←triggerId per A's producer read. | Bar side → **X-W4**; otherwise dies with the rebuild. |
| MCP-21 | MINOR | The hue handle is vertically centred by coincidence: no `top`/`inset-block`/`-translate-y-1/2` (:32-35) — flush only because handle h-3 = rail h-3; the sibling thumb (:18) uses the other contract. Two markers, two positioning contracts, one instrument (**N-8**) | My read. | **X-W7 rider**. |
| MCP-23 | MINOR | The specimen dot is a hand-rolled second swatch species — unnamed, not `aria-hidden` (:40-43) — beside the producer's WatercolorDot, the constitution's ONLY ornamental colour-bearing species (VC:17 verified) (**N-10**; the "§4.2/P051 25-call manifest" citation is unresolvable — method note §4) | :40-43 my read; WatercolorDot producer + 18-consumer premise = axes' census, accepted. | **X-W7 rider**. |
| MCP-24 | MINOR | No disabled/pending arm anywhere: props are `open`/`hex` only (:65-68); `searching` never reaches the child; `ButtonProps.loading` exists unused (**N-11**) — corrected by MCP-13: the parent's own flag is fiction too | My reads; `loading` verified in the d.ts by this seat. | **X-W7 rider**, coupled to MCP-13's caveat. |
| **MCP-25** | MINOR | **Clear-all leaves the colour instrument stale (r1 carry, re-verified)**: `onClearAll` (:227-232) resets `colorSearchActive` and `colorText` but never `pickerHex`; the empty string cannot reach the child (guard :111); the trigger swatch keeps painting AND announcing the retired filter colour while the badge reports no active colour filter | **This seat's own read** of :227-232 + :76-79. | **X-W7** (owned file; clear semantics). |
| MCP-26 | MINOR | Paint, hit and marker geometry are three different rectangles (**reader-A missed #1 ≡ r1 MCP-26**): `border border-border` (:10) makes `getBoundingClientRect` (border box, 174×112) the pointer map while the gradient (padding-box origin) and the %-positioned thumb (padding-box containing block) live in 172×110 — the column rendering the pure hue returns S=172/174, the pure-white column S=1/174, the marker up to 1px off the hand at extremes. The correctly-attributed version of what N-1 blamed on `rounded-lg`; invisible to all three axes because the canonical instrument carries no border | CSS-certain from :10/:19/:137-139, my reads; `--border` fully opaque (foundation.css:735 per A, accepted). | **X-W7 rider** — outline/ring instead of border, or measure the content box. |
| MCP-27 | MINOR | The `sat = ref(0.6)` seed is dead in the only shipping configuration (the immediate watcher overwrites before first paint) AND encodes a different colour (#528fcc) than the parent's #4488cc — two disagreeing defaults, one unreachable (**D-20**, upgraded from A's INFO tail by K-5's arithmetic; replaces the killed L-5 sub-claim) | My arithmetic both directions. | **X-W7 rider**. |
| MCP-28 | MINOR | No zero-rect guard (**reader-B missed M4, NEW**): `updateCanvas`/`updateHue` divide by rect dimensions unchecked; the `Math.max(0,Math.min(1,x))` idiom is NaN-transparent (verified) → `currentHex = "#NaNNaNNaN"` → emitted into the Input and the swatch → Search feeds it to `parsePickerColor`, which **throws** inside a click handler with no catch (`applyColorSearchFromPicker` has no error arm). Canonical guard exists (SpectrumCanvas `rect.width === 0` check; library `clamp`); every axis enumerated SpectrumCanvas's cures and none listed this one | NaN chain re-derived by this seat; throw site = picker-color.ts:109-113 (axes + r1, accepted); :180-187 catch-less, my read. | **X-W7 rider**. |
| MCP-29 | MINOR | Killed-N-1/N-7 residual: a discrete click/tap in the ~13.7px² rounded-corner quarter-discs and the rail's clipped cap is swallowed; drag (captured + clamped) reaches every extreme, so the domain is NOT truncated (ruled #8) | K-1/K-2 evidence; probe rows read by this seat. | **X-W7 rider** — full-rect hit area, rounding as paint clip. |
| MCP-31 | MINOR | Inert local overrides: `variant="outline"` (:48) is not a glass-7 Button prop (dead DOM passthrough; vue-tsc green — the gate-blindness law); `p-2.5` (:6) loses to the producer's `px/py-(--overlay-pad-*)` longhands (B's tailwind-merge non-conflict mechanism, adopted); dead `h-6` re-homed to MCP-40 (**D-8**, ruled #5) | d.ts verified by this seat; measured 20.352px/16px = the axes' cell, consistent with the dist class string I read. | Variant arm **CROSS-BOOK → MT-BTN1** (r1 fleet-51 census, attributed); locals die with the rebuild; unlayered-producer-CSS seam → **BH relay note** (R-7). |
| **MCP-41** | MINOR | **One visual species, two opposite interactivity contracts, 60px apart (reader-A missed #3, NEW)**: the dialog trigger (:75-79, 28px) and the inert specimen dot (MiniColorPicker :40-43, 24px) are the same mark — `rounded-full border-2 border-border shadow-cartoon-sm` + colour fill — one opens a dialog, the other does nothing and is not `aria-hidden`. No axis noticed the identity; PROPORTION-AUDIT:70 item 5 forbids exactly this | Both class lists in my reads; PA:70 verified verbatim by this seat. | **X-W7** (both marks in/under the owned file). |
| **MCP-42** | MINOR | **A second, independent route into MCP-3's latched state (reader-B missed M3, NEW)**: the flag is set (:128/:143) BEFORE the unguarded `setPointerCapture` (:129/:144), which throws `NotFoundError` on a stale pointerId — aborting the handler with the flag latched and NO capture taken; the prescribed pointercancel/lostpointercapture cure cannot fire for a capture never taken. Canonical guards the symmetric call in try/catch | Order and absence of guard = my read; throw semantics = spec-standard, accepted. | **X-W7 rider** — order the flag after the capture, or guard the call (binding on MCP-3's cure). |
| **MCP-43** | MINOR | **Wrong error at the wrong altitude in the consumer (reader-B missed M5, NEW)**: `hexToOklab` (:205-211) guards OKLab `"none"` channels — unreachable for every input its two callers can produce (OKLab is rectangular; the :218 gate admits only 6-hex) — while the REACHABLE failure (`PickerColorError` on an unparseable string) is unhandled at BOTH call sites (:213-225 try/finally with no catch; :180-187 bare). The function is also misnamed: it takes any string | :205-225 + :180-187 my reads; throw site accepted as above. | **X-W7** (owned file; error posture). |
| **MCP-44** | MINOR | Two shadow languages 100px apart: `shadow-cartoon-sm` on the SV thumb (:18) vs plain `shadow-sm` on the hue handle (:33) — and DESIGN.md names MiniColorPicker as consumer-of-record of the cartoon rungs, so the claim is half-false in the tree (**D-14** + r1 MCP-33's deletion-carry, folded) | Both lines my read; DESIGN.md cartoon paragraph verified by this seat. | **X-W7 rider**; **binding deletion-carry**: the commit that deletes the file edits DESIGN.md's consumer list in the same change. |
| MCP-22 | INFO | `as-child` trigger over an unfallbacked, untyped `#trigger` slot — omit it and the component is silently unopenable; the one consumer complies by convention (**N-9**, both readers INFO) | :3-5 + :65-74 my read. | **X-W7 rider**. |
| MCP-30 | INFO | The honest residual of killed N-6: the producer's non-integer 20.352px overlay padding bears no ratio to the 8/8/6 inner rhythm; no φ rung exists at this scale to have consumed (`--space-phi-5/6` only — verified) (**N-6 residue**, K-7) | sizing.css read by this seat. | Dies with the rebuild; spacing law → **X-W10**. |
| MCP-32 | INFO | `open: boolean` REQUIRED forces the controlled pattern; producer `defaultOpen` exists — the handle buys only the manual close at :183 (**D-22**) | :65-68 my read; d.ts per axes. | Dies with the rebuild. |
| MCP-34 | INFO | Capture taken on `e.target`, not `e.currentTarget` (:129/:144) — correct today only because both children are `pointer-events-none` (**r1 carry, re-verified**) | My read (:18/:33 confirm the children). | Dies with the rebuild. |
| MCP-35 | INFO | Parent guard redundancy (`text.startsWith("#")` dead beside the `^#`-anchored regex, :218) + the `0.01` stableHue constant lives at **three** session sites, not two — the L axis under-counted its own strongest premise (**r1 carry, re-verified**: useColorPipeline.ts:92, useColorParsing.ts:43 and :~78) | All three sites + :218 in my reads. | Guard → **X-W7**; session-side triplication → the session-model wave (X-W6 family), else **NO-WAVE-OWNER**. |
| MCP-36 | INFO | The picker's opener carries the surface's only motion and is the one declaration off the duration/easing tokens: `transition-shadow` bare (:76) vs `transition-colors duration-fast` (:99) in the same file (**reader-A missed #4**, demoted from A's MINOR — one missing utility class; token conformance, NOT a PRM defect — see Motion-law note) | Both lines my read. | **X-W10** (motion/token canon). |
| MCP-37 | INFO | Prose defects (**r1 carry, re-verified**): the `W5-a11y: … needs accessible name` comment (:74) reads as an open TODO above the line that satisfies it; `sv-canvas` names a `<div>` after both `<canvas>` and the real SpectrumCanvas — naming the shadow copy after the original is how the duplication hid | :74 + :10 my reads. | Dies with the rebuild; comment fix rides any wave touching SearchFilterBar. |
| MCP-38 | INFO | `demo/ui/*` = 19 single-line pure re-export shim dirs, reached by the subject at :62-63; the dual-path charge was correctly self-withdrawn by the L axis (**L-8**) | Census = both readers (19 dirs), consistent with X-W8's own figures. | **CROSS-BOOK → X-W8** — one identity, already owned. |
| **MCP-45** | INFO | Cursor vocabulary contradicts the interaction (**reader-A missed #5, NEW**): `cursor-pointer` (activate affordance) on the drag-only rail beside `cursor-crosshair` on its sibling; with no keyboard path and both markers `pointer-events-none`, the cursor is the rail's ONLY affordance signal — and it names the wrong gesture | :10/:26/:18/:33 my reads. | Dies with the rebuild. |
| **MCP-46** | INFO | Neither hand-rolled `<button>` in the call site declares `type` → both default `submit`; latent (no ancestor form on the Browse route), detonates the first time the filter row is embedded in a form; the producer exposes `type` on ButtonProps precisely so this is declarable (**reader-A missed #6, NEW**) | :75-79/:97-104 my reads; `type?: ButtonHTMLAttributes["type"]` verified in the d.ts by this seat. | **X-W7** (owned file). |
| **MCP-47** | INFO | The remaining INFO tail, verified live by reader-A and spot-consistent with this seat's reads: **D-16** anisotropic pointer gain (174×112 for a unit square — 1.554:1, undecided); **D-17** no empty/loading/error/disabled arm in 164 lines; **D-19** the hex readout unisolated for RTL (law-grade under VC:154, verified verbatim); **D-21** two emit idioms in one file (`emit` :70/:107 vs `$emit` :2/:51) | A's row, anchors consistent with my full read. | Dies with the rebuild; D-19's law → **X-W4**. |

## Killed-claims register

All fifteen sustained or decided on this seat's own instruments; the strongest-worded first.

| K | claim | refutation (arbiter-verified basis) |
|---|---|---|
| **K-1** | **N-1 (BLOCKER)** — "the four corners of the S×V domain are not hit-testable … pure white and the pure hue **cannot be selected by any input this component accepts**. A colour picker that cannot pick white." | Unanimous kill, re-verified: probe-D2-4.mjs issues **only `page.mouse.click`** (my read — every arm, plus the reset click); but the primary gesture DRAGS and the maths CLAMP — :129 captures, :132 keeps firing, :138-139 `Math.max(0,Math.min(1,…))` → drag past the top-left corner lands exactly sat=0/val=1 = `#ffffff`, stable under re-echo (`d===0` early-returns). A's second blade sustained: even for clicks the mechanism is wrong — S=0 needs `clientX ≤ rect.left` (the left EDGE, alive in the probe's own control row `#808080`), and the corner rows' domain-meaning column is wrong for all four (at +1,+1 S=0.0057, V=0.991 → `#fdfeff`; no click could ever return white, so "white unreachable by click" was unfalsifiable as probed). Residual → MCP-29; the real edge-imprecision cause is MCP-26, which N-1 mis-attributed to `rounded-lg`. **CORPUS-INTEGRITY: the governing pass-2 file headlines this false BLOCKER — any downstream seat reading only its receipt block inherits it (A's warning, adopted; this record is the counter-authority).** |
| **K-2** | **N-7a** — "the hue rail's right end does not actuate" (rounded-cap hit-clip). | Probe off-by-one, verified in source by this seat: the right arm clicks `rail.x + rail.width * 1.0` = the first coordinate OUTSIDE the box (probe-D2-3.mjs:68-69); the JSON shows the miss (`left` stays 50% from the previous arm). At the sampled mid-height a 6px-radius pill cap insets the hit region by zero, so the stated mechanism is impossible where it was probed; and :144 captures + :153 clamps → hue 360 reachable by drag (degenerate anyway: `hsl(360)`≡`hsl(0)`, the gradient's own last stop). Handle-clipping half survives → MCP-14. |
| **K-3** | **C-5 sub-claim** — "the mount-time `update:hex` emission does not reach the parent." | Ruled #6. Registration order + pre-flush semantics derived by this seat; B's jsdom run and r1's node run agree byte-for-byte (`[["update:hex","#4488cc"]]`). The conflicting live cell survives only as R-1. Killing this negative births MCP-7. |
| **K-4** | **D pass-2 negative #2** — "opening the picker autofills the colour text field: FALSE." | Non-sequitur by probe construction: the child mounts when the OUTER tray opens — necessarily before a user can type into an Input living inside that same PopoverContent (layout-certain from SearchFilterBar :16-105, my read) — so the probe seeded an empty field and then observed no clobber of text that did not exist yet; the type→close→re-open window was never exercised. Same failure shape as the L axis's withdrawn N-2 (lattice round-trip), as B noted. |
| **K-5** | **L-5 sub-claim** — "the seed (210,.6,.8) is a third literal encoding of the same default as `pickerHex = '#4488cc'`." | Not the same colour: HSV(210,.6,.8) = **#528fcc** (my arithmetic); #4488cc is HSV(210,**⅔**,.8). The truth is sharper and worse → MCP-27 + MCP-7. |
| **K-6** | **C-4 framing (reader-A sustained it; THIS SEAT kills it)** — "the SC 2.5.8 spacing exception cannot rescue the rail: a 24px circle centred on the rail intersects the SV field 8px above and the output row 8px below." | Arithmetic error: 24px is the **diameter**. The circle extends 6px past each rail edge; both neighbours are 8px away; 6 < 8 and 14 > 12 — no intersection, the exception plausibly applies. Ergonomic residual → MCP-14. |
| **K-7** | **N-6 (MAJOR)** — "five unrelated literals and none of them come from the producer ladder." | A's kill sustained: `mt-2`/`gap-1.5` compile to `calc(var(--spacing)·n)` — producer-token-derived, which is what VC:33 asks; `--overlay-pad-*` are element-scoped (present in the dist class string this seat read), so their `:root` absence evidences nothing; `--space-phi-3/4` do not exist to have been consumed (sizing.css verified). Residual → MCP-30 (INFO). |
| **K-8** | **L-9 table cell** — "(sat .60, val .03) → #010102 → hue 240." (r1 carry) | This seat's arithmetic: (210,.6,.03) → **#030508** → decodes to **216°**; #010102 (→240° exact) lives at v≈0.008. Wrong coordinate, exact mechanism — MCP-2 unharmed. Recorded so no wave builds a repro on the mislabeled row. |
| **K-9** | **L-1's BLOCKER-by-magnitude** — "disagrees on 4.2313% of the HSV lattice", carrying the BLOCKER. | Ruled #7: the rate is real (r1's rebuilt-sweep receipt corrects B's "unverifiable" cell) but every disagreement is one LSB from a rounding-MODE difference at the last bit — L's own re-run concedes it ("no sample diverged by ≥2 bytes … an ownership defect, not a rendering bug"). Severity re-homed at MCP-1 MAJOR. |
| **K-10** | **N-3 cure-claim** — "cannot be patched by a `@media (forced-colors)` block; the vocabulary is background-image; therefore delete." | A's kill sustained: `forced-color-adjust: none` exists for exactly this content class, and the probe records it unused (`"auto"` on all six measured elements). The defect is real (MCP-5); the impossibility is not, and "delete" needs no false premise — it stands on the other 44 rows. |
| **K-11** | **D-cure-2's premise** — "the existing Input already accepts and parses any CSS colour through `parseColorIn`." | A's kill sustained, verified at the line by this seat: :218 discards everything that is not 6-digit hex and substitutes `pickerHex` BEFORE `parseColorIn` ever runs; today the Input accepts strictly LESS than the picker's own guard, with a lying placeholder (:92). The corpus's own L-7 is the refutation. **Binding on the executing wave**: deleting MiniColorPicker without fixing :218 ships a hex-only field with a dead placeholder, not the ratified tray. |
| **K-12** | **D-11's quantifier** — "`side='top'` is hard-coded, so the child ALWAYS launches upward over its own opener." | A's kill sustained: side/align are collision-checked preferences (reka Popper, `avoidCollisions` default true), and the corpus's own mobile arm reports the child landing BELOW — the corpus refutes its own universal. The 40.5% one-viewport eclipse survives as R-6. |
| **K-13** | **C-5's supporting inference** — "`hexLabel` is #4488cc, only reachable if sat moved 0.6→0.6667, so the prop watcher DID run." | B's kill sustained: `pickerHex` is INITIALISED to `"#4488cc"` (:170, my read) and the label interpolates it (:78) before any emit can occur — zero evidence about the child's watchers. (The conclusion happens to be true — MCP-7 — but by K-3's road, not this one.) |
| **K-14** | **C-4's geometry datum** — "hueThumb: [16,16] … at hue=360 half the 16px thumb is gone." | B's kill sustained: the hue handle is `w-3 h-3` = 12×12 (:33, my read); 16×16 is the SV thumb (:18). C-4's own overhang arithmetic (6 of 12) contradicts its own cited datum. Clipping finding survives with the right marker attached (MCP-14). |
| **K-15** | **L-9's claim of primacy** — "added by the re-run seat … first pass explicitly ruled this out." | B's kill sustained as accounting: D pass-1 filed the same 210°→240° defect as D-6 and C filed it as C-2 with the saturation half L-9 lacks. One defect, three filings — the programme counts ONE (MCP-2); L-9's genuinely new legs (irreversibility, fixed-point collapse) are credited inside it. |

**Cell corrections recorded, not kills**: C-5's cure path `src/units/color` → live `src/color/`; L-7's `parseColorIn` import anchor :137 → **:145**; L-4's headline "the one thing its own docstring forbids" — the docstring forbids copying the CONSTANT; this file dropped the CONCEPT (B's precision, adopted); L's ±1-3-line anchor drift in spectrumLuma/useSpectrumPlateStyle cites (quoted text exact); D2-N2's "zero pointer contact" → "zero contact with either colour surface"; the probes' evidence-cells were unlabeled by both readers (webkit interaction / chromium forced-colors).

## Superlatives — provenance preserved

1. **`demo/picker/controls/spectrumLuma.ts`** (both readers; read whole by this seat) — a 35-line module whose entire purpose is to make a threshold un-copyable, and says so: "ONE function, ONE threshold — instrument coherence … Share the function, never copy the constant." It names its three consumers, states the model (`v·(1−s/2)`) and why. It predicted MCP-16 before the corpus existed, and the defect is legible ONLY because this producer wrote down the law being violated.
2. **`SpectrumCanvas.vue`** (both readers; six cure sites line-verified by this seat) — every cure the subject lacks, present WITH its reason: both cancel bindings; releaseCapture in try/catch annotated "Element may be detached"; lost-capture-clears-drag; the zero-rect guard; library `clamp`; rAF coalescing with cancel-on-stop; unmount teardown; `overflow: visible`; and the a11y RULING as a comment — "2D saturation×lightness picker — not a linear slider, so `role='img'` with a reactive descriptive label, not `role='slider'`." Three audit seats independently used it as the standard of comparison, and it held.
3. **The L axis's second-pass self-withdrawal** (reader-A's "best piece of work in the corpus"; spans read by this seat) — retracting its own N-2 with the transferable law "a round-trip that starts on the quantised lattice cannot detect quantisation. Begin round-trip probes at the continuous input, never at the encoded one," while SALVAGING the fixed-point argument that makes MCP-2's corruption permanent. Self-refutation + salvage + a reusable rule; the same failure shape it names was then found AGAIN in D pass-2's negative #2 (K-4) — the law generalises.
4. **challenge-D-design.md §8** (reader-A) — negative proof done properly: measured 5.392:1/5.297:1 contrast and REFUSED the easy defect, keeping only the narrower true one (the readout's ROLE is wrong, not its legibility — MCP-19); refused "black is unreachable"; and marked one part of the condemned file CORRECT to carry forward. A report arguing for deletion that still protects a good line of the corpse is a report doing its job.
5. **`vite.config.ts` self-alias generation from `package.json#exports`** (both readers) — anchored regexes so the demo physically cannot import a specifier a real npm consumer could not write, with the exact postmortem in the comment. It is why MCP-1 is legible as a BREACH and not a style choice.
6. **`demo/palettes/browser/search/index.ts`** (both readers; tree-verified) — named re-exports only, MiniColorPicker deliberately withheld (cluster-private). Correct encapsulation discipline drawn around a component that should not exist.
7. **`src/color/operations.ts` roundHalfEven** (reader-B) — an epsilon-relative tie test that folds `[76.5, 76.5, 76.50000000000001]` to one byte where a naive test splits them. One quantiser, one law — the exact law MCP-1 breaks.
8. **`demo/color-session/useColorParsing.ts:43-45`** (reader-A; verified with its comment by this seat) — "Powerless colors have no numeric hue; retain the last deliberate hue." Correct CSS Color 4 powerless-channel reasoning in one line; the law :119 reinvented weaker (MCP-15).
9. **`MiniColorPicker.vue:157-163`** (both readers, both D passes) — the one unambiguously right thing in the condemned file: static overlay in scoped CSS, only dynamic `--hue` through `:style`, principle written down. Protected by name in every cure text, including this one.
10. **Receipt discipline** (reader-A) — C's HEAD-drift notice ("HEAD is `e39da983`, not `c654824e` as the work-order states") and L's two-commit drift confirmation; **evidence persistence** (reader-A) — L persisting probe-L9-* beside the report "since the scratchpad is session-temporary and L-9 is a BLOCKER": the persisted probe is exactly what let reader-B re-run the magnitude measurement a session later. Evidence that outlives its session is the difference between an audit and an anecdote.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs; state the evidence cell)

- **R-1 (decides MCP-7's DOM witness)**: read `input[aria-label="Search by CSS color"]` **`.value` as a property, never the attribute**, immediately after opening the Filters tray, then across type → close-tray → re-open. The reactivity half is settled (K-3); C-5's `colorText: ""` cell conflicts and must be re-measured exactly this way.
- **R-2**: D-2 — trigger below the fold, no scroller, iPhone-14 viewport and 200% zoom (pass-1-only captures; structurally corroborated — producer PopoverContent class string declares no max-height/overflow). **Attribution binding (reader-A's note, adopted): this is a SearchFilterBar/producer-overlay defect; deleting the subject does not cure it — book it against the bar, not the corpse.**
- **R-3**: MCP-3's final live hop — reka unmounting PopoverContent mid-press in the exact stuck-drag sequence (structure certain; witness optional; screenshot already in evidence/).
- **R-4**: MCP-16/MCP-20 pixel cells — the 1.283:1 ring ratio does not reconcile with the WCAG formula (r1 computed 1.07:1 for the same pair); re-sample scanlines; also N-5's nine-names AT churn (announce-on-entry vs re-announce).
- **R-5**: MCP-5's forced-colors pixel census (chromium cell; structural cause source-certain).
- **R-6**: D-11's 40.5% one-viewport trigger eclipse + edge deltas (live layout).
- **R-7**: MCP-31's `p-2.5`-dead cascade (unlayered producer `.popover-content` beating a layered utility — 20.352px vs 16px); if confirmed, carry to the **glass-ui BH relay** as a producer-CSS-layering seam.
- **R-8**: MCP-16's consequence — does `shadow-cartoon-sm` rescue thumb legibility on light fields (the corpus's own HYPOTHESIS), and does the weaker `shadow-sm` rescue the rail handle at hue 60?
- **R-9**: C-6's iOS scroll-steal half (coarse-pointer cell; both absences already proven).
- **R-10**: C-5's `#zzzzzz` render — NaN state and `#000000` emit are certain; whether `hsl(calc(NaN*1deg))` resolves to red or invalidates at computed-value time is engine-specific.
- **R-11**: C-3's focus-restoration row (close via the parent's `open` flip lands focus on the inline Search, not the trigger swatch) — reka dismiss-path dependent, never re-driven.

**Witness law** (AdminUsersPanel precedent): corpus PNGs under `audit/components/MiniColorPicker/` are untracked (`.gitignore` `*.png`); any wave citing them force-adds its before/after frames or the witness does not exist.

## Motion-law note (M-xx)

No motion-quarantine **FALSE** row is carried anywhere in this record. The three MiniColorPicker challenge files are **EXISTS-ORIGINAL** in HYDRATION-LEDGER.md:87-89 (verified), outside the 46-file REPORT-AUTHORED quarantine subject set, and motion-quarantine.md contains **zero** MiniColorPicker rows (my grep). MCP-36 is token conformance, not a PRM defect — both app-wide guards are live as SERVED bytes per motion-quarantine.md §1, and `box-shadow` riding Guard 2's surviving transition list is reader-A's cited mechanism, consistent with that section.

## DISSENT and method notes — preserved, not ruled away

1. **r1 divergence (the one substantive ruling change)**: r1 held MCP-1 at BLOCKER "resting on ownership/dogfood, not rendering," with reader-B's ±1-LSB caveat adopted into the cell. Both r2 readers rule MAJOR; this seat concurs — severity follows the proven consequence plus law-breach weight, and the cure is identical at either grade. r1's position is preserved here verbatim-in-substance; if the owner re-weighs dogfood-breach severity programme-wide, MCP-1 is the row to revisit, and r1's rebuilt-sweep receipt (4.2313%, git `5e146edd`) remains the statistic of record.
2. **Reader-A's abstention, ruled but honoured**: A labeled the mount-emission question UNPROVEN while correctly stating it "contradicts a straightforward reading of Vue's watcher semantics"; B decided it with an instrument. The lesson (also r1's): the Vue-semantics half of such claims IS statically decidable, and abstaining left A's roster without MCP-7 — the corpus's most user-visible missed defect. A's caution was epistemically honest; A's D-corpus coverage and its two structural kills (K-7, K-10) were the wider net.
3. **Severity incoherence across the corpus, resolved here**: the echo loop was BLOCKER(L)/MAJOR(C)/MAJOR(D); keyboard was BLOCKER(C,D)/MAJOR(L); forced-colors BLOCKER→MAJOR mid-corpus. A downstream ledger deduping by severity would have picked arbitrarily — the rulings table is the single severity authority for these identities now.
4. **Unresolvable citation, quarantined**: challenge-D-design.md:366's "§4.2/P051 has a live 25-call manifest" carries no path and neither reader nor this seat could resolve it; MCP-23 stands on the verified premises only. No wave may cite that manifest without first finding it.
5. **Unanimity risk, disclosed**: axes, both readers, r1 and this seat all reach delete-and-rebuild. Per the suspicious-agreement corollary: this seat's concurrence rests on its own instruments — the K-6 arithmetic (which CORRECTED a reader while leaving the verdict intact), the watcher-order derivation, the harness census, and the fixed-point algebra — not on convergence.
6. **The K-1 corpus-integrity flag re-recorded**: the governing pass-2 design file headlines a killed BLOCKER ("a colour picker that cannot pick white"). This apotheosis is the counter-authority; any seat that reads only that file's receipt block inherits a falsehood.

## Closing verdict

**ADJUDICATED r2 — DELETE-AND-REBUILD SUSTAINED; TWO BLOCKERS (MCP-2 echo-loop corruption, MCP-3 latched drag), r1's THIRD DEMOTED TO MAJOR ON BOTH READERS' BOUND (MCP-1).** A 164-line private fork of the library it exists to demonstrate drives a self-corrupting echo loop through a leak-prone, keyboard-dead, forced-colors-blind pointer surface that no test reaches — and no test CAN reach, because the harness cannot mount an SFC (MCP-39) — while the canonical instrument, the shared luma law, the producer slider variant and button rung, and the session model all sit one import away. 45 rows confirmed (2·12·20·11), 15 claims killed — including one BLOCKER, two probe artifacts, two false cure-premises now binding on the executing wave (K-11, MCP-13), and one reader's arithmetic corrected by this seat (K-6) — 17 rows the original axes never filed adjudicated in, 11 residue items handed to the live pass with their evidence cells named. Rows land **X-W4** (bar a11y + D-19) · **X-W7** (owned bar rows + palette-tree riders) · **X-W1 rider** (coverage + harness) · **X-W10** (type/motion/spacing canon) · **MT-BTN1 / X-W8** (cross-books) · **BH relay** (producer CSS seam, on R-7's confirmation); everything on MiniColorPicker.vue itself rides the **X-W7 rider else NO-WAVE-OWNER at the formation boundary** — flagged so the deletion this entire corpus agrees on does not strand unowned. Nothing here stamps a wave; everything here is runnable by the session that does.

— end of apotheosis r2. The challenger corpus, both r2 reader returns, the committed r1 record (`5e146edd`), and all dated evidence remain immutable beside/behind this file per E-1/E-3.
