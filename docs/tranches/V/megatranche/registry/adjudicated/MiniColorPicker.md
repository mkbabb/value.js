# MiniColorPicker — adjudicated apotheosis (M-23/L-18, arbiter-F) · model receipt: `claude-fable-5` (Fable 5), observed in this seat's own system prompt, not inherited

**Seat**: fresh Fable adjudicator (M-23 §1 / L-18) · **Date**: 2026-08-04 · **HEAD**: `b651e4b3` (branch `tranche-u`; component source re-read whole at the working tree — all line anchors in this record are this seat's own reads, re-resolve before any cure per D-19).
**Corpus**: `docs/tranches/V/megatranche/audit/components/MiniColorPicker/` — challenge-C-implementation.md (C-1..C-8) · challenge-L-library.md r2 (L-1..L-9, with its own two self-withdrawals) · challenge-D-design.md r2 governs over challenge-D-design-pass1-c654824e.md (D1-D1..D-22 · D2-N1..N-11) · probes probe.mjs/probe2-5/probe-D2-1..4/probe-L9-* + shots/ + evidence/.
**Readers adjudicated**: reader-A (`MiniColorPicker-L18-design-and-usage`) · reader-B (`l18-minicolorpicker-library-code`). Both were attacked before either was adopted; every reader-vs-reader disagreement was re-derived by this seat with its own instruments.
**Subject**: `demo/palettes/browser/search/MiniColorPicker.vue` (164 lines, read whole) + its sole consumer `demo/palettes/browser/search/SearchFilterBar.vue` (249 lines, read whole).

**Arbiter's own instruments this session (zero browser tooling — per law; all static + node)**: full reads of both SFCs; the L-1 lattice sweep re-run against `dist/subpaths/color.js` (938,961 samples — see DISSENT §1 for this seat's own recorded probe bug and the corrected run); the M1/killed-C-5 decider re-built verbatim on the repo's own Vue (`node_modules/vue`, runtime.esm-bundler): state after immediate watcher `210 / 0.6666… / 0.8`, **emits after one tick `[["update:hex","#4488cc"]]`**, seed hex independently re-derived `#528fcc`; hand re-derivations of the loop algebra (`#010102 → h=240` exact; `(210,.6,.03) → #030508 → 216`; `#4488cc` fixed point; `#12345g → #123405` char-exact; `#zzzzzz → NaN → #000000`); src/color/operations.ts:305-329 read (roundHalfEven, ULP tolerance, sole byte quantiser); producer bytes: `Button.vue.d.ts` (emphasis/tone/size/iconOnly/loading/type/disabled/class — **no `variant`**), `slider/types.d.ts:4` (`SliderVariant = "standard" | "spectrum"`), popover dist grep `forceMount` = **0** (reka Presence — remount is the ordinary path), `typography/semantic.css` (`text-caption` = `font-style: italic` on `--font-text`; `text-micro` real), `typography/utilities.css` (`@utility fira-code { … font-feature-settings: "liga", "calt" }`; `text-mono-small` exists) vs `demo/styles/utils.css:9-11` (`.fira-code` family-only shadow); censuses: `grep -rn "MiniColorPicker|sv-canvas|Open color picker" test/ demo/test/ e2e/` = **0**; `<Button …variant=…>` tags demo-wide = **51 across ~29 files** (perl multiline census; matches the AdminUsersPanel L-3 / MT-BTN1 fleet figure exactly; MiniColorPicker.vue:48 owns 1); `emphasis=` in demo = 2; stableHue `0.01` sites = **3** (useColorPipeline.ts:92, useColorParsing.ts:43, **:78**); DESIGN.md:188 names MiniColorPicker verbatim; SpectrumCanvas.vue :18-19 (@pointercancel/@lostpointercapture), :81-91 (releaseCapture try/catch "Element may be detached"), :115 (`rect.width === 0 || rect.height === 0` guard), :234 (`overflow: visible`); spectrumLuma.ts:1-22 docstring read; picker-color.ts:109-113 throw path; probe sources read as evidence about the probes: probe-D2-4.mjs (click-only, `page.mouse.click`, no drag anywhere), probe-D2-3.mjs:68-69 (rail arm `fx = 1.0` → `rect.x + rect.width` = first pixel OUTSIDE), probe-D2-1.mjs openChain (Filters clicked at :35-39 BEFORE `typeFirst` fills at :40-45), probe-D2-2.mjs (pixel scans = **webkit** cell; forced-colors = **chromium** cell, `forcedColors:"active"` at :116-120); routing authorities: X wave specs W1/W4/W6/W7/W8/W10 heads + file lists (`SearchFilterBar.vue` owned by name at W4.md:126 and W7.md:89; W7.md:288 already names `SearchFilterBar.vue:52,:53`; **no X wave names MiniColorPicker.vue**), ApiOfflineChip/AdminUsersPanel/AdminFlaggedPanel/AdminListSkeleton adjudications (identity + routing precedents).
**Evidence-cell disclosure**: every interaction/pixel probe in this corpus is a **playwright-webkit** cell except the forced-colors census (**chromium**); neither reader labeled this. No claim in this record is browser-derived by this seat.

## Scoreboard

**38 rows CONFIRMED (3 BLOCKER · 10 MAJOR · 18 MINOR · 7 INFO) · 8 claims KILLED (all sustained on this seat's own re-derivation; 1 kill decided against reader-A's abstention) · 12 missed-by-the-axes findings adjudicated in (readers' union — all confirmed) · 9 UNPROVEN-NEEDS-LIVE residue items → visual-audit inputs.**

The unanimous cure across all three axes and both readers — **delete MiniColorPicker.vue and rebuild the affordance on the session model + producer primitives** — is ADOPTED as the terminal shape, with reader-B's M4 carry (the DESIGN.md:188 citation) and reader-A's missed-1 caveat (the consolidation target's pending state is fiction) both binding on the executing wave.

**Routing law**: `SearchFilterBar.vue` is owned by name (X-W4 a11y-control-targets; X-W7 palette domain — whose goal line "every mutation has exactly one owner and a visible result" covers the pending-state row). `MiniColorPicker.vue` is named by **no** X wave → per the ApiOfflineChip precedent, its rows ride as **X-W7 rider (palette-tree home, unclaimed by name), else NO-WAVE-OWNER at the formation boundary**. Fleet identities cross-book, never re-book (anti-rename): dead `variant=` = AdminUsersPanel L-3 / **MT-BTN1** (fleet 51 — my census matches to the unit; the vue-tsc exit-0 blindness law recorded there binds here too); `demo/ui` barrels = **X-W8**'s own hard-gate census (19 barrels / 92 imports). Dispositions are **ADJUDICATED, not VERIFIED** — nothing here stamps a wave.

## Ruled disagreements (the seat's actual work)

| # | question | reader-A | reader-B | RULING | Deciding evidence (arbiter's own) |
|---|---|---|---|---|---|
| 1 | **C-5's mount-emission-lost sub-claim** ("same code, two behaviours depending on mount context; correctness at mount is a scheduler coincidence") | UNPROVEN — "two independent seats measured it not landing; not decidable without a browser" | KILLED — reproduced the watcher topology on the repo's Vue; the emit fires | **KILLED — B wins, on this seat's own re-run.** | I rebuilt :76-125 verbatim on `node_modules/vue` (3.5.x runtime.esm-bundler): the immediate prop watcher moves sat 0.6→0.6667 during setup, the pre-flush `watch(currentHex, …)` fires exactly once after one tick, `emits = [["update:hex","#4488cc"]]`. Teleport/Presence relocate vnodes; they do not suppress a child's pre-flush watcher or its emit — and the C axis's own R1 control (challenge-C-implementation.md:234-237) shows the emit firing on the same Vue build. The axis's conflicting live cell (`colorText: ""`) is preserved as residue R-1 with B's property-vs-attribute hypothesis. **Consequence: the inverse is a live defect — MCP-7 (M1).** |
| 2 | **C-4's flat WCAG 2.2 SC 2.5.8 failure** ("spacing exception unavailable because mt-2 puts the SV field and output row inside a 24px circle centred on the rail") | CONFIRMED MAJOR | KILLED (framing) — the spacing exception plausibly applies | **KILLED (framing) — B wins on arithmetic.** | A 24px-**diameter** circle has radius 12. Rail h-3 = 12px, so the circle extends 6px past each rail edge; the nearest targets sit `mt-2` = **8px** away (SV field above, output row below; my reads of :26/:39) → 6 < 8, no intersection; distance from rail centre to the SV field is 6+8 = **14px > 12px radius**. Reader-A treated 24 as a radius. The ergonomic residual (a 12px drag rail as the only hue affordance in a keyboard-dead control) + the clipping half survive at MINOR → MCP-14. |
| 3 | **D2-N6's framing** ("five unrelated literals, none from the producer ladder", MAJOR) | KILLED in framing (mt-2/gap-1.5 ride `--spacing`) | CONFIRMED MINOR | **RESCOPED — both partly right; A's kill of the MAJOR framing sustained, B's MINOR residual adopted** → MCP-30. | `mt-2`/`gap-1.5` are Tailwind spacing-scale utilities resolving through `--spacing` (the very token the axis's own evidence block records) — "none from the ladder" is false. The true residual is smaller: the inner rhythm is not on the producer φ ladder and is unrelated to `--overlay-pad-*`; `p-2.5` is separately dead (MCP-31). |
| 4 | **C-3/L-6 severity** (keyboard-inoperable surfaces) | BLOCKER | MAJOR | **MAJOR — B wins.** | The FEATURE has a degraded keyboard path: the sibling `Input` (SearchFilterBar.vue:88-96) accepts a typed 6-digit hex from the keyboard and searches. Access is impaired, not severed — and the impairment is worsened by L-7 (hsl() masked) and MCP-7 (the path is clobbered at tray re-open), which is exactly why those rows carry their own weight. WCAG 2.1.1 Level A stands as cited. |
| 5 | **D2-N3 severity** (forced-colors inoperability) | BLOCKER | MAJOR | **MAJOR — B wins.** | Inoperable furniture, no data destroyed; the text input survives forced colors as a system-drawn control. Structural cause is source-certain (all chrome is `background-image`/inline background, rail has no border, zero `forced-colors` arms in 164 lines — my read); only the pixel census stays live (R-5). |
| 6 | **D2-N5 severity** (dialog accessible-name churn) | MAJOR | MINOR | **MINOR — B wins.** | Dominated by MCP-4: the user class exposed to per-pointermove name churn cannot operate the surfaces that cause it. Mechanism source-certain (SearchFilterBar.vue:78 template + :175-178 + MiniColorPicker.vue:107). |
| 7 | **D2-N4 / L-4 severity** (fill-blind markers / bypassed luma helper) | MAJOR ×2 | MINOR ×2 | **MINOR, ONE identity (MCP-16).** | The by-construction argument is exact (I verified the algebra both for the SV thumb and the rail handle) — but the L axis itself labels the visual consequence HYPOTHESIS (`shadow-cartoon-sm` supplies an offset dark shadow), and B's arithmetic shows the measured 1.283:1 cell does not reconcile with the WCAG formula (#ffffff vs #ffff00 = **1.07:1**) — a suspect cell (R-4). Same defect seen from two sides; spectrumLuma.ts exists to cure precisely it. |
| 8 | **L-3 severity** (weaker stableHue guard) | MAJOR | MINOR | **MINOR — B wins.** | The damage flows through MCP-2's continuous-plate mechanism, not the hex lattice (B's subsumption argument, consistent with my own re-derivations at v=0.008 vs v=0.005). The row survives as a duplication-with-drift fact. |
| 9 | **reader-A missed-1 severity** (the pending state is fiction) | MAJOR | — (not filed) | **MAJOR — A sustained.** | Statically certain from :213-225: `async` with zero `await`; `searching` true→false inside one synchronous tick; no render can observe it — Loader2 (:102), `:disabled` (:98) and the `if (searching.value) return` double-fire guard (:214) are all dead. During real upstream latency the user gets nothing and can double-fire. Perfect X-W7 fit ("every mutation has … a visible result"), and it is **cure-critical**: three cure texts (D-9, D-17's loading row, D2 cure step 4) consolidate onto this fictional affordance. |
| 10 | **D1-D8 severity** (inert overrides: `variant="outline"`, dead `p-2.5`) | MAJOR | — (partial) | **DEMOTED MINOR (MCP-31), variant arm CROSS-BOOKED.** | The load-bearing half is the fleet class already ruled RED-gated at AdminUsersPanel L-3 / MT-BTN1 (51 sites at my census — exact match); re-booking it MAJOR here would double-count one identity. Local facts (1 of 51; `p-2.5` beaten by unlayered producer `.popover-content`) ride the rebuild; the unlayered-producer-CSS seam is flagged for the BH relay. |

## Adjudicated defect roster

Severities are this seat's rulings. "X-W7 rider" = the ApiOfflineChip precedent (palette-tree home, file unclaimed by name), else NO-WAVE-OWNER at the formation boundary.

| id | sev | claim (ids) | proof basis | terminal disposition |
|---|---|---|---|---|
| **MCP-1** | **BLOCKER** | The component reimplements the published `@mkbabb/value.js/color` surface it exists to demonstrate; 4.2313% lattice disagreement with the library quantiser; zero library imports (**L-1**). | **Re-run by this seat** (corrected instrument, see DISSENT §1): 938,961 samples → **39,730** mismatches = **4.2313%**, maxDelta 1; first example `{h:0,s:0,v:0.3}` → component #4d4d4d vs library #4c4c4c — all figures match both readers to the digit. Mechanism verified in source: operations.ts:305-312 roundHalfEven (ULP-tolerant) applied at :324/:329 vs :103 `Math.round`; hard case `hsv(0,0,0.3)` → lib 76 / component 77 (my run). Import list :61-63 = vue + two `../ui` barrels only. B's caveat adopted: divergence ≤1 LSB — the BLOCKER rests on ownership/dogfood, not rendering. | **X-W7 rider** — delete/rebuild on the session model + producer primitives. |
| **MCP-2** | **BLOCKER** | The hex wire + the parent's echo destroys hue AND saturation irreversibly on an ordinary low-value SV drag (**L-9 / C-2 / D1-D6**). | Loop re-walked edge-by-edge in live source (:138-139 → :103 → :107 → SearchFilterBar :175-178 → :68 → :110-125). My re-derivations: `#010102` → h = ((1−1)/1+4)/6 = **240° exact**; sat clobber at :118 (v→0 forces sat 0.6→0); `#5252cc` is a hue-240 **fixed point** (re-encode of decode reproduces itself), so the corruption settles and never returns — the L seat's own N-2 self-withdrawal mechanism, confirmed. The table-coordinate cell is killed (K-8). | **X-W7 rider** — dies with MCP-1; the cure carries a colour, not a hex string, and separates the tuning channel from the commit channel (with MCP-7/MCP-8). |
| **MCP-3** | **BLOCKER** | No `pointercancel` / `lostpointercapture` / `onUnmounted`; capture taken (:129/:144) and never released; an interrupted press leaves `canvasDragging` stuck for the life of the instance, after which bare hover rewrites the colour (**C-1**). | All 164 lines read by this seat: flags are non-reactive `let` (:79-80); sole clearers :133/:148 are element-bound `@pointerup`; instance root is `<Popover>` (:2) so the flag outlives PopoverContent; producer popover has **no forceMount** (my dist grep) so content truly unmounts. The canonical cure is one directory away and line-verified: SpectrumCanvas.vue:18-19/:81-91/:168-175/:216-224. Census correction adopted: 16 recovery-handler hits / 5 files, zero here (C's 15/6 was stale). Live final hop = R-3. | **X-W7 rider** — the rebuild inherits SpectrumCanvas's capture discipline. |
| MCP-4 | MAJOR | Both colour surfaces are bare divs — no role, no name, no tabindex, no keyboard path; the dialog's only focusable is the Search button (**C-3 / L-6 / D1-D3**; ruled #4). | :8-15 and :24-31 carry class/:style/@pointer\* and nothing else (my read); sole focusable at :47-54. VISUAL-CONSTITUTION.md:99/:125 as quoted by all three axes; canonical carries the ruling in prose and implements it (SpectrumCanvas.vue:4-9). WCAG 2.1.1 A. | Picker surfaces die with MCP-1 (**X-W7 rider**); the bar-side keyboard path hardening → **X-W4** (owns SearchFilterBar.vue:126 by name). |
| MCP-5 | MAJOR | Under forced colors the instrument is inoperable furniture: every colour-bearing pixel is background-image/inline background, the rail has no border, and there is no forced-colors arm in the file (**D2-N3**; ruled #5). | Structurally certain from my full read (:27, :159-163; zero `forced-color-adjust`/`@media (forced-colors)`); axis measurement (chromium cell, `forcedColors:"active"` — probe engine verified by this seat) corroborates; pixels = R-5. | **X-W7 rider** — producer primitives carry the forced-colors arm. |
| MCP-6 | MAJOR | Pressing the picker's own Search commits the picker's private seed and overwrites the user's typed CSS colour — no contact with either colour surface required (**D2-N2 / D1-D5**). | Source-closed: :51 emits `currentHex` (derived only from local refs); the typed text is never parsed into the picker; SearchFilterBar :180-187 then destroys `colorText`. B's qualification adopted: "zero pointer contact" was rhetorical — the honest statement is "zero contact with either colour surface". VC:96-97 select→tune→commit as quoted. | **X-W7** (SearchFilterBar owned by name) + rider for the picker half. |
| **MCP-7** | MAJOR | **Mount-time self-write clobber (reader-B missed M1)**: the immediate prop watcher normalises the seed (sat 0.6→0.6667), `currentHex` moves #528fcc→#4488cc, the pre-flush watcher emits once, and the parent writes the picker's seed into the colour text Input — so every re-open of the Filters tray silently replaces the typed colour with `#4488cc`. | **Proven at the reactivity level by this seat's own run on the repo's Vue** (emits `[["update:hex","#4488cc"]]` one tick after setup); remount-per-open is ordinary (no forceMount, my grep); SearchFilterBar :175-178 writes `colorText` unconditionally. The C axis's conflicting DOM cell is R-1 (property-vs-attribute hypothesis). All three axes walked past it; the D pass-2 negative that "ruled it out" is killed (K-4, probe sequence artifact). | **X-W7** (parent handler + wire type) — same identity family as MCP-2: a controlled component that rewrites its controller. |
| **MCP-8** | MAJOR | **No cancel anywhere (reader-A missed #2)**: every pointer sample is a commit into the sibling text input (`update:hex` doubles as live-tuning AND commit channel); Escape/outside-click/`miniPickerOpen=false` restore nothing; the only escape from an accidental drag is retyping from memory. | Source-certain: :107 fires per sample; :175-178 writes both refs unconditionally; no watch on `open`, no snapshot, no reset in :127-154. VC:96-97/:115. | **X-W7** — the rebuild separates channels and adds revert-on-dismiss semantics. |
| MCP-9 | MAJOR | Zero test coverage of any kind; the component can be deleted and every gate stays green (**C-8**). | My grep: 0 hits across test/ demo/test/ e2e/; the cluster barrel deliberately withholds the component so no barrel test can reach it. Every BLOCKER above was one unit test away from being caught. | **X-W1 rider** (oracle/coverage slate) + behavioral coverage rides the rebuild (**X-W7 rider**) — the ApiOfflineChip AP-4 pattern. |
| MCP-10 | MAJOR | Second SV-plate implementation with the gradient recipe and thumb placement copied from the canonical instrument, plus a **fourth private HSV store** outside the colour session that owns HSV (**L-2 / L-5 / D1-D1**). | Byte-comparisons verified (:159-163 vs useSpectrumPlateStyle.ts two-layer recipe; :19 vs the plate's left/top law); `COLOR_MODEL_KEY` grep over demo/palettes/ = 0 (both readers, mechanism accepted); B's ±2-line anchor drift in L-2 noted, content correct. The "third literal encoding of the same default" sub-claim is **killed** (K-5) and replaced by MCP-27. | **X-W7 rider** — the rebuild consumes the session model; useSpectrumPlateStyle is the already-extracted seam. |
| MCP-11 | MAJOR | The stringly hex boundary is a lie twice over: the type admits what the guard discards (3/4/8-digit, named, functional colours — silently), `parseInt` mangles partial garbage (`#12345g` → `#123405`; `#zzzzzz` → NaN state → `#000000`), and the parent silently masks the `hsl(...)` its own placeholder advertises (**C-5 core / L-7**). | `#12345g → #123405` re-derived char-exact by this seat; the NaN walk re-derived (NaN falls through `d === 0`, `switch (NaN % 6)` matches nothing); SearchFilterBar :92 placeholder vs :218 fallback-to-pickerHex with no error arm (my read). Corrections recorded: C-5's cure cites `src/units/color` — **stale, no such path** (live: `src/color/`); L-7's import anchor is :145 not :137. | **X-W7** — "carry a colour, not a hex string"; the parent already imports `parseColorIn` (:145). |
| MCP-12 | MAJOR | The hue rail is hand-rolled where the producer ships the variant this repo already consumes (**D1-D10**). | `SliderVariant = "standard" | "spectrum"` verified in the installed d.ts by this seat; ComponentSliders already consumes it (axis-verified); :24-36 rebuilds it from a div + inline gradient + three raw handlers. | **X-W7 rider** — rebuild on `<Slider variant="spectrum">`. |
| **MCP-13** | MAJOR | **The pending state two axes prescribe as the surviving Search owner does not exist (reader-A missed #1)**: `applyColorSearch` is `async` with no `await`; `searching` flips true→false in one synchronous tick; the spinner, the disabled arm, and the double-fire guard are all unreachable code — and three cure texts consolidate onto this fiction. | Statically certain from my read of :213-225 (+ :98-:102). Ruled #9: MAJOR sustained. | **X-W7** (owns the file AND the goal line "every mutation has … a visible result"). **Binding cure-caveat**: no consolidation may route onto the inline button's pending affordance until the flag actually spans the upstream search. |
| MCP-14 | MINOR | 12px drag rail as the sole hue affordance (ergonomics, not a flat SC 2.5.8 failure — K-6) + both markers clipped at domain extremes by `overflow-hidden` × centre-translation (**C-4 clipping half + D2-N7 second half**). | Clipping statically certain (:10/:26 overflow-hidden; :18 −translate-x/y-1/2; :33 −translate-x-1/2 → 3/4-clipped thumb at S=0,V=1; half-clipped handle at hue 0/360); canonical sets `overflow: visible` at :234 for exactly this (my read). | **X-W7 rider**. |
| MCP-15 | MINOR | stableHue law re-invented with a strictly weaker threshold: `d === 0` (:119) vs the canonical `s·v > 0.01` (**L-3**; ruled #8). | Canonical sites verified ×3 by this seat (see MCP-35 for the third the L seat under-counted). Subsumed-in-mechanism by MCP-2. | **X-W7 rider** — dies with the private store. |
| MCP-16 | MINOR | Markers are fill-blind by construction (interior ≡ the pixel beneath) and the only information-bearing pixel is a scheme-invariant hardcoded white ring — bypassing spectrumLuma.ts, the one thing its docstring forbids (**L-4 + D2-N4**, one identity; ruled #7). | Algebra verified by this seat both for the thumb (:19 = HSV at its own (S,V)) and the rail handle (:34 hsl ≡ the :27 gradient at x=h/360, checked on the first sixth); spectrumLuma.ts:1-22 read (SPECTRUM_LUMA_FLIP, "Share the function, never copy the constant"); B's analytic 1−ln2 = 30.685% LIGHT-region check adopted. Ratio cells suspect → R-4. | **X-W7 rider** — the rebuild reads `spectrumFieldIsLight`. |
| MCP-17 | MINOR | No `touch-action` on either drag surface, no touch gate; the iOS scroll-steal half stays HYPOTHESIS as the axis itself labeled it (**C-6**). | Zero touch-action in the file (my read); canonical gate verified (SpectrumCanvas :11-21). | **X-W7 rider**. |
| MCP-18 | MINOR | Static 7-stop rail gradient inlined per-instance at :27, eight lines above the file's own comment (:158) assigning exactly that case to CSS (**C-7**). | Both lines read; A's correction adopted: not "the only raw hex in the repo" — :161-162 carry #000/#fff. | **X-W7 rider**. |
| MCP-19 | MINOR | Type register defects, one identity: the hex readout takes the serif-italic `text-caption` role and overrides family back to mono keeping the italic; `text-micro` off the closed §4 matrix; local `.fira-code` shadows the producer utility and drops its ligature settings (**D1-D13 + D1-D18**). | Producer bytes verified by this seat: `text-caption` italic in semantic.css; `fira-code` with `font-feature-settings: "liga","calt"` in utilities.css vs demo/styles/utils.css:9-11 family-only; `text-mono-small` (the wanted role) exists. | Sites die with the rebuild (**X-W7 rider**); the class law = **X-W10** (type canon). |
| MCP-20 | MINOR | The dialog's accessible name is the imperative that opened it AND a live function of the value being edited — churning per pointer sample (**D2-N5**; ruled #6). | Mechanism source-certain (SearchFilterBar.vue:78 + :175-178 + :107); AT-observable churn = R-4-family residue. | Bar side → **X-W4**; dies with the rebuild otherwise. |
| MCP-21 | MINOR | The hue handle is vertically centred by coincidence — no top/inset/translate-y; it lands centred only because handle h-3 = rail h-3; the sibling thumb uses the other positioning contract (**D2-N8**). | :32-35 vs :18, my read. Both readers credit this as a D-seat catch the other axes missed. | **X-W7 rider**. |
| MCP-22 | MINOR | The trigger is an `as-child` slot with no fallback and no typed requirement; omit it and the component is silently unopenable (**D2-N9**). | :3-5 + :65-74, my read; the one consumer satisfies the contract by convention. | **X-W7 rider**. |
| MCP-23 | MINOR | The specimen dot is a hand-rolled second swatch species (no accessible name, no aria-hidden) beside the producer's WatercolorDot — the register the constitution names as the only ornamental colour-bearing species (**D2-N10**). | :40-43, my read; producer component + 17 demo consumers per the axis (not re-counted; not load-bearing). | **X-W7 rider**. |
| MCP-24 | MINOR | No disabled/pending arm anywhere in the surface; the child's Search never receives the parent's flag though `ButtonProps.loading` exists (**D2-N11**) — with the correction that the parent's flag itself is fiction (MCP-13). | :47-54 + :65-68, my read; `loading` verified in the producer d.ts by this seat. | **X-W7 rider**, coupled to MCP-13's caveat. |
| MCP-25 | MINOR | **Clear-all leaves the colour instrument stale (reader-A missed #3)**: `onClearAll` never resets `pickerHex`; the empty string can't reach the child (guard :111); the swatch keeps painting and *announcing* the retired filter colour while the badge says no colour filter is set. | Source-certain from my reads (:227-232; :76-79 trigger binding + aria-label). | **X-W7** (owns the file; clear semantics). |
| MCP-26 | MINOR | **Paint, hit and marker geometry are three different rectangles (reader-A missed #4)**: the 1px border makes `getBoundingClientRect` (border box) the pointer map while %-positioning resolves against the padding box, displacing the marker from the pointer by up to 1px toward centre at every extreme; the border also paints OVER the outermost gradient ring where S=0/V=1 live. | CSS-certain from :10/:19/:137-139 (background shorthand resets clip to border-box; abs-positioned % resolves vs padding box). Sub-pixel magnitude, structural kind. | **X-W7 rider**. |
| MCP-27 | MINOR | **The `sat = ref(0.6)` seed is dead in the only shipping configuration AND encodes a different colour than the parent's** (reader-B missed M2): the immediate watcher overwrites sat to 0.6667 before first paint; HSV(210,.6,.8) = #528fcc ≠ #4488cc. | My arithmetic (both hexes re-derived); replaces the killed L-5 sub-claim (K-5). | **X-W7 rider**. |
| MCP-28 | MINOR | **No zero-size-rect guard (reader-B missed M3)**: a zero rect NaN-poisons sat/val → `currentHex = "#NaNNaNNaN"` → `--hue: NaN` invalidates the plate → Search feeds it to `parsePickerColor`, which **throws** inside a click handler with no catch on the path. | `Math.round(NaN).toString(16) === "NaN"` (checked); throw at picker-color.ts:109-113 (my read); canonical guard at SpectrumCanvas.vue:115 (my read). Low reachability; real error-posture asymmetry. | **X-W7 rider**. |
| MCP-29 | MINOR | The surviving residual of killed N-1/N-7: a **discrete click/tap** at each rounded corner of the SV field (~13.7px² per corner) and at the rail's clipped cap is swallowed by the hit-clip; click-to-set and touch taps mis-register near extremes. Drag reaches every extreme (capture + clamp), so the domain is NOT truncated. | Probe data (webkit cell) + my algebra; see K-1/K-2. Ruled MINOR (A said "at most MAJOR", B MINOR; the only lost gesture is corner-click on a 174×112 surface). | **X-W7 rider** — hit area = full rect, rounding as paint clip (the axis's own cure line, adopted). |
| MCP-30 | MINOR | Inner rhythm off the producer φ ladder / unrelated to `--overlay-pad-*` — the honest residual of D2-N6 (ruled #3). | See ruling #3. | Dies with the rebuild; the spacing-law question = **X-W10**. |
| MCP-31 | MINOR | Inert local overrides: `variant="outline"` (:48) is not a glass-7 Button prop — 1 of the **fleet-51** (my census, exact match to MT-BTN1); `p-2.5` (:6) is beaten by the producer's unlayered `.popover-content` (**D1-D8**; ruled #10). | Producer d.ts read by this seat; fleet census mine; the p-2.5 cascade = R-7. | Variant arm **CROSS-BOOK → MT-BTN1** (AdminUsersPanel L-3; the vue-tsc-exit-0 gate ban binds); locals die with the rebuild; the unlayered-CSS seam → **BH relay note**. |
| MCP-32 | INFO | `open` forced controlled; producer owns uncontrolled open (`defaultOpen`) — the handle buys only the manual close at :183 (**D1-D22**). | Producer d.ts per axes; :65-68 my read. | Dies with the rebuild (**X-W7 rider**). |
| MCP-33 | INFO | **Deletion carry (reader-B missed M4)**: `demo/DESIGN.md:188` names MiniColorPicker as a consumer-of-record of the cartoon shadow rungs — and the claim is already half-false (thumb `shadow-cartoon-sm` :18 vs handle plain `shadow-sm` :33, two shadow languages). | DESIGN.md:188 verified verbatim by this seat. | **Binding on the executing wave**: the deletion edits DESIGN.md:188 in the same commit. |
| MCP-34 | INFO | Capture taken on `e.target` not `e.currentTarget` (:129/:144); correct today only because the children carry `pointer-events-none` (**reader-B missed M5**). | My read; canonical uses currentTarget. | Dies with the rebuild. |
| MCP-35 | INFO | Parent guard redundancy (`startsWith("#")` dead beside the anchored regex, :218) + the copied `0.01` stableHue constant exists at **three** session sites, not two — the L seat's lattice argument under-counts its own strongest premise (**reader-B missed M6**). | All three sites verified by this seat: useColorPipeline.ts:92, useColorParsing.ts:43, useColorParsing.ts:78. | Guard: **X-W7**; the session-side triplication: noted to the wave landing the session-model consolidation (X-W6's catalog family), else **NO-WAVE-OWNER**. |
| MCP-36 | INFO | The picker's opener carries the only motion in the surface and is the one declaration off the duration/easing tokens (`transition-shadow` bare at :76 vs `duration-fast`/`--ease-standard` in the same file) (**reader-A missed #5**). | SearchFilterBar reads mine. Token conformance, NOT a PRM defect — see Motion-law note. | **X-W10** (motion/token canon). |
| MCP-37 | INFO | Prose defects: the `W5-a11y: … needs accessible name` comment (:74) reads as an open TODO directly above the lines that satisfy it; `<!-- SV canvas -->`/`.sv-canvas` names a `<div>` after both `<canvas>` and the real SpectrumCanvas — naming the shadow copy after the original is how the duplication survived (**reader-A missed #6**). | My reads. | Dies with the rebuild; the comment fix rides any wave touching SearchFilterBar. |
| MCP-38 | INFO | `demo/ui/*` is a 19-directory pure re-export shim; the dual-path charge was correctly self-withdrawn by the L seat (**L-8**). | Reader-B re-ran the census (19 dirs); consistent with X-W8's own hard-gate figures (19 barrels / 92 imports — my read of W8 head). | **CROSS-BOOK → X-W8** — one identity, already owned, never re-booked. |

## Killed-claims register (all sustained by this seat's own instruments)

| K | claim | refutation (arbiter-verified basis) |
|---|---|---|
| **K-1** | **D2-N1 (BLOCKER)** — "the four corners of the S×V domain are not hit-testable … white and the pure hue **cannot be selected by any input** … a domain the design silently truncated." | Probe source read by this seat: probe-D2-4.mjs issues **only `page.mouse.click`** — no drag in the file. But :129 takes `setPointerCapture` and :138-139 **clamp**, so a press anywhere in the field dragged past the top-left yields sat=0, val=1 → `#ffffff` exactly (same for the pure hue, S=1, and #000000). The capture is the same mechanism C-1 proves leaks — N-1 and C-1 cannot both be read literally. The axis's own data refutes its geometry: the stated rule `(x−8)²+(y−8)² ≤ 64` predicts (2,2) dead (72 > 64) while its own probe measured (2,2) **alive**; and its "best reachable white" (#d0d1d1) contradicts its own measured #f8fafb. Residual → MCP-29 (MINOR). |
| **K-2** | **D2-N7 first half** — "the hue rail's right end does not actuate; hue 360 unreachable" (rounded-cap hit-clip). | Probe artifact, verified in probe-D2-3.mjs:68-69: the right arm clicks at `rail.x + rail.width` = **rect.right, the first pixel outside** (half-open interval), while the left arm uses fx=0.0, the first pixel inside. The named mechanism is also impossible at the sampled y (vertical midline of a pill — the cap reaches the full right edge there). And :144 captures + :153 clamps to 360 on drag. Second half (50%-clipped handle at hue 0) survives → MCP-14. |
| **K-3** | **C-5 sub-claim** — "the mount-time `update:hex` emission does not reach the parent … same code, two behaviours depending on mount context (reka Presence/Teleport) … correctness at mount is a scheduler coincidence." | **This seat's own run on the repo's Vue**: the verbatim topology emits `[["update:hex","#4488cc"]]` exactly once, one tick after setup. Teleport/Presence move vnodes, not scheduler flushing; the axis's own R1 control shows the same build emitting. The conflicting live cell (`colorText: ""`) survives only as R-1. Killing this negative births MCP-7. |
| **K-4** | **D pass-2 negative proof #2** — "the mount of the filter popover autofills the colour text field: FALSE (typed text retained)." | Non-sequitur by probe construction, verified at probe-D2-1.mjs:31-45: `openChain` clicks Filters (mounting the picker, which emits into the then-empty field) BEFORE `typeFirst` fills the text; the clobbering sequence (type → close tray → re-open) is never exercised, and re-opening only the inner popover cannot re-run the prop watcher (hex unchanged). |
| **K-5** | **L-5 sub-claim** — "the seed (210, 0.6, 0.8) is a third literal encoding of the same default as `pickerHex = ref('#4488cc')`." | Not the same colour: HSV(210,.6,.8) = **#528fcc**; #4488cc is HSV(210,**0.6667**,.8) — my arithmetic, corroborated by both readers' seed prints. The true relation is sharper and worse (MCP-27 + MCP-7): the component boots to a colour the parent's swatch is not showing and reconciles by writing into its own controller. |
| **K-6** | **C-4 framing** — "12px hue rail = flat WCAG 2.2 SC 2.5.8 AA failure; the spacing exception is unavailable." | The exception's arithmetic was mis-run by the axis AND by reader-A: 24px **diameter** → 12px radius; the rail's circle extends 6px past each edge; the nearest targets are 8px away (my reads of :26/:39); 6 < 8 and 14 > 12 — no intersection, exception plausibly applies. Ergonomic residual → MCP-14. |
| **K-7** | **D2-N6 framing** — "five unrelated literals and none of them come from the producer ladder" (MAJOR). | `mt-2`/`gap-1.5` are `--spacing`-derived ladder utilities (the axis's own evidence block records the token). Residual (φ-ladder/overlay-pad coherence) → MCP-30 (MINOR). |
| **K-8** | **L-9 table row** — "(sat .60, val .03) → #010102 → hue 240; a 30° swing." | My arithmetic: (210,.6,.03) → **#030508** → decodes to **216°** (6° swing); the 240° result lives at v≈0.008 (#010102 → ((1−1)/1+4)/6 = 240 exact) — precisely what L-9's mechanism paragraph states. Wrong coordinate, exact mechanism; MCP-2 unharmed. |

**Cell corrections recorded, not kills**: C-1's recovery-handler census 15/6 → **16/5**; C-7's "only raw-hex in the repo" (the same file's :161-162 carry #000/#fff); C-5's cure path `src/units/color` → **`src/color/`** (stale tree); L-7's import anchor :137 → **:145**; L-2's useSpectrumPlateStyle anchors run ~2 lines high; D2-N4's 1.283:1 ratio cell does not reconcile with the WCAG formula (1.07:1 computed — suspect, R-4); D2-N2's "zero pointer contact" → "zero contact with either colour surface"; neither reader labeled the probes' **webkit** evidence cell.

## Superlatives — provenance preserved

1. **`demo/picker/controls/spectrumLuma.ts:1-22`** (both readers; verified verbatim by this seat) — a module whose entire reason to exist is to make a threshold un-copyable, and says so: "ONE function, ONE threshold — instrument coherence … Share the function, never copy the constant." It predicted L-4/MCP-16 before the corpus existed; B's analytic check (LIGHT region = 1 − ln 2 = 30.685%) adopted.
2. **`SpectrumCanvas.vue`'s capture discipline** (reader-B; spot-verified :18-19/:81-91/:115/:234 by this seat) — pointercancel + lostpointercapture + releaseCapture with the detached-element catch + zero-rect guard + `overflow: visible` + flush-pending-coords-before-cancel: the cures for MCP-3/MCP-14/MCP-28, written one directory away with the reason attached to each.
3. **The scoped-CSS seam at :157-163** (reader-A; both D passes explicitly protect it) — static overlay in CSS, only `--hue` through `:style`; the one thing in the file correctly fenced off from every cure.
4. **`vite.config.ts` self-alias generation from `package.json#exports`** (reader-B) — the demo cannot resolve a specifier a real npm consumer could not write; machinery that makes a class of dogfood breach unrepresentable, with the postmortem attached.
5. **`src/color/operations.ts:305-329`** (reader-B; read by this seat) — roundHalfEven with ULP-scaled tolerance as the single byte quantiser. One quantiser, one law — the exact law MCP-1 breaks.
6. **The corpus's self-withdrawals** (both readers; spans :416-425 and :538-566 read by this seat) — the L seat retracting its own dual-path charge after measurement, and withdrawing its own first-pass negative with the transferable lesson "a round-trip that starts on the quantised lattice cannot detect quantisation." The rarest thing in an audit corpus, twice in one file.
7. **`demo/palettes/browser/search/index.ts`** (both readers) — the barrel deliberately withholds MiniColorPicker (named re-exports only, PI-6): correct scoping discipline around a component that should not exist.
8. **HYPOTHESIS labeling discipline** (both readers) — C-6's scroll-steal half and L-4's visual consequence graded as unproven by their own authors; where this corpus failed, it failed by asserting geometry it did not measure (N-1) or clicking outside the element (N-7); where it held, it held because of this habit.

## UNPROVEN-NEEDS-LIVE residue (→ visual-audit inputs)

- **R-1 (decides MCP-7's DOM witness)**: read `input[aria-label="Search by CSS color"]` **`.value` as a property** (never the attribute) immediately after opening the Filters tray, then across a type → close-tray → re-open cycle. The reactivity half is PROVEN (this seat's run); the C axis's `colorText: ""` cell conflicts and must be re-measured exactly this way.
- **R-2**: D1-D2 — the trigger below the fold with no scroller at 390×664 and at 200% zoom (pass-1-only measurement; pass 2 declined to re-spend; would be severe if it reproduces).
- **R-3**: MCP-3's final live hop — reka unmounting PopoverContent mid-press in the exact stuck-drag sequence (structural defect already certain; witness optional).
- **R-4**: D2-N4/D1-D7 pixel cells — the 1.283:1 ring ratio does not reconcile with the WCAG formula (1.07:1 computed); re-sample scanlines; also D2-N5's nine-names AT churn.
- **R-5**: D2-N3's forced-colors pixel census (chromium cell; structural cause source-certain).
- **R-6**: D1-D11's 40.5% trigger eclipse + D2's edgeDelta {left 29, right 3} (live layout).
- **R-7**: D1-D8's `p-2.5`-dead cascade (unlayered producer `.popover-content` beating a layered utility — measured 20.352px vs 16px by the axis; re-witness, and carry to the BH-relay note if confirmed).
- **R-8**: MCP-16's visual consequence (does `shadow-cartoon-sm` rescue thumb legibility on light fields? the axis's own HYPOTHESIS).
- **R-9**: C-6's iOS scroll-steal half (coarse-pointer cell; both absences already proven).

**Witness law reminder** (AdminUsersPanel precedent): the corpus PNGs under `audit/components/MiniColorPicker/` are untracked (`.gitignore` `*.png`); any wave citing them must force-add its before/after frames or the witness does not exist.

## Motion-law note (M-xx)

No motion-quarantine **FALSE** row is carried anywhere in this record; no M-xx quarantine row touches this component (both readers concur; MiniColorPicker declares zero motion). MCP-36 is token conformance, not a PRM defect — glass-ui's reduced-motion guard keeps box-shadow in the surviving transition list (motion-quarantine.md §1 Guard 2, as cited by reader-A), so edict 6 is not engaged.

## DISSENT and method notes — preserved, not ruled away

1. **This seat's own recorded probe bug (the L-9-lesson, worn by the arbiter)**: my first L-1 sweep returned **0 mismatches** across 938,961 samples — because `toRgba8` is Result-typed and takes `{gamut:"clip"}`, my call returned `{ok:false}` on every sample, and `Math.abs(byte − undefined)` is NaN, which compares false against `> 0`. A probe that read a signature without reading what it meant. The corrected run (hard-fail on any non-ok Result) reproduced both readers **to the digit** (39,730 / 4.2313% / maxDelta 1 / #4d4d4d-vs-#4c4c4c). Recorded because it is the strongest possible corroboration of the readers' independence — a wrong instrument produces a DIFFERENT wrong number, not the same right one — and because the general rule (assert `ok:true` before consuming any Result) belongs beside MT-F019.
2. **Reader-vs-reader abstention ruled**: reader-A declined C-5's mount-emission sub-claim as "not decidable without a browser"; reader-B decided it with a node repro. This seat's independent re-run vindicates B — the Vue-semantics half of such claims IS statically decidable, and abstaining on it left A's roster without MCP-7, the corpus's most user-visible missed defect. Preserved as a method lesson, not a demerit: A's caution was epistemically honest and its D-corpus coverage (D1 rows B never adjudicated) was the wider net.
3. **Reader-B's severity dissent on L-1 preserved**: "the divergence is never more than 1 LSB, so the BLOCKER rests on ownership/dogfood, not on rendering." Adopted into MCP-1's cell verbatim; if a future owner ruling re-weighs dogfood-breach severity, that is the sentence to revisit.
4. **The evidence-cell gap**: every interaction probe in this corpus is playwright-webkit; the exemplar's cell-labeling law (chromium/webkit/safari as separate cells) was not observed by the axes and by neither reader. The re-probes in R-1..R-9 should state their cell.
5. **Unanimity risk, disclosed**: both readers confirmed the corpus's three BLOCKERs and the deletion cure; so does this seat. Under the suspicious-agreement corollary this is worth stating: the three BLOCKERs were each re-derived here with independent instruments (corrected sweep; Vue repro + algebra; full-file read + dist grep), not adopted on convergence.

## Closing verdict

**ADJUDICATED — ALL THREE BLOCKERS SUSTAINED; DELETE-AND-REBUILD ADOPTED AS THE TERMINAL SHAPE.** A 164-line private fork of the library it exists to demonstrate (MCP-1) drives a self-corrupting echo loop (MCP-2, MCP-7, MCP-8) through a leak-prone pointer surface (MCP-3) that no test reaches (MCP-9), while the canonical instrument, the shared luma law, the producer slider variant, and the session model — the four things whose absence constitutes the defect — all sit one import away. 38 rows confirmed (3 BLOCKER · 10 MAJOR · 18 MINOR · 7 INFO), 8 claims killed on this seat's own re-derivations (including one BLOCKER and one probe-artifact pair), 12 missed findings adjudicated in, 9 residue items handed to the live pass, fleet identities cross-booked to MT-BTN1 and X-W8 without double-count. Rows land X-W4 (bar a11y) · X-W7 (owned file + palette-tree riders) · X-W1 (coverage rider) · X-W10 (type/motion canon) · MT-BTN1/X-W8 (cross-books) · BH relay (producer CSS seam note); everything on MiniColorPicker.vue itself rides the X-W7 rider **else NO-WAVE-OWNER at the formation boundary** — flagged so the deletion this whole corpus agrees on does not strand unowned. Nothing here stamps a wave; everything here is runnable by the session that does.

— end of apotheosis. The challenger corpus, both reader returns, and all dated evidence remain immutable beside this file per E-1/E-3.
